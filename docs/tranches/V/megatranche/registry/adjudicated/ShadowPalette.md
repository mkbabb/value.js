# ShadowPalette — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed from this seat's own runtime, not inherited from the spawn label

**Seat**: the fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code), each of which re-read the full challenge corpus against the tree before returning.
**Date**: 2026-08-06 · **HEAD**: `3487d73e` (branch `tranche-u`; both subject files clean in `git status`; every line anchor in this record re-resolved at this HEAD by this seat's own reads — `ShadowPalette.vue` 115 L whole).
**Corpus**: `audit/components/ShadowPalette/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md`. **Provenance, verified by this seat**: NO `challenge-*-r2.md` exists (directory listed — r2 governs where it exists; here the three r1 files govern their own axes); all three are **EXISTS-ORIGINAL** at `registry/HYDRATION-LEDGER.md:124-126` (hashes sighted), so the thin-Codex UNVERIFIED discount does not apply. `codex-provenance/motion-quarantine.md` **exists and is tracked at this HEAD** (landed 2026-08-03 per X-W0 HG-11 / W6 H2 addendum — reader-2's "does not exist" provenance cell is dated) and carries **ZERO** ShadowPalette / shadow-palette / shadow-seg rows: no M-xx FALSE row constrains any motion claim here, and none is carried. C-seat = Opus 5 [1m], D-seat = Opus 5 [1m], L-seat = Opus 5 [1m] per their own receipts.
**Method (L-18)**: both re-reader returns were attacked before either was adopted. Every kill was re-derived against the tree, the installed producer dist, or this seat's own arithmetic; every load-bearing confirmation was spot-verified at source; the readers' four verdict-level disagreements (the C-3/D-6 fold threshold, the C-4 "internal contradiction" limb, D-7's surviving severity, L-4's axis) were **ruled**, one reader-2 settlement was overruled, and one reader-1 kill-cell overstatement was corrected inside a sustained kill. No browser was opened and no live probe run (probe parsimony): rulings are decidable from source, installed producer bytes, prior-registry identities, and the two committed screenshots — which this seat read **as images with its own eyes** (`shots/safari-desktop-light/extract.png`, `shots/safari-mobile-light/extract.png`), discharging one reader-flagged UNPROVEN row (the mobile caption orphan) into CONFIRMED.

**Arbiter's own instruments this session (all static, each re-runnable)**: whole read of ShadowPalette.vue (template + script + scoped block); ExtractWorkbench.vue:90-205 (morph seat, three keyed branches, caption, `swatch-class` literal, truncation-honesty comment); ExtractControls.vue:12-35 (numeric k label, slider 1..16, `aria-label` with no `aria-valuetext`); PaletteCard.vue:10-40 (the S.W5-10/S-15-A NO-overflow-hidden note verbatim + `cartoon-surface` + `rounded-t-card`/`rounded-l-card` strip pass); PaletteCardSwatches.vue:18-36 (`pt-3` + `border-t border-border/15`); PaletteCardSkeleton.vue:30-90 (byte-identical root class minus `shadow-palette`; `surface`/`variant`/`--skeleton-shimmer-delay` fall-through; `w-12 h-12 sm:w-14 sm:h-14`); PaletteColorStrip.vue:30-72 whole computed (WEIGHT_FLOOR 0.08, fallback chain, `Math.max(100/n, 0.5)`); EmptyState.vue:28-48 (the R12 one-seat ruling + trio); o9-shadow-palette.spec.ts:1-25/:50-62/:80-124/:130-165/:248-263; animations.css:30-80 (`.stagger-children` + the three-family law verbatim) and :184-193 (the reduce guard — duration/iteration/transition only, **never `animation-delay`**); foundation.css:94-130 (the `@theme` block incl. `--default-transition-duration: var(--duration-fast)` — the D-7 lever), :328 (`--well-bg: color-mix(in oklab, var(--card) 92%, var(--foreground) 8%)` — the K-1 decider), :685-695 + :830-840 (`.shadow-swatch` on both rosters); utils.css:30-80 (ink recipe, E1-R2 prose, the stale specimen-walk block); eslint.config.js:228-300 (G-DEMO globs verbatim; `ls demo/@` → No such file or directory); VISUAL-CONSTITUTION.md:26-36/:184-203 (laws 2/8; :186 located in the Palette-library section; :198 Extract verbatim; :202 Mix). Producer bytes: `Skeleton.vue.d.ts` (`{ class? }` only), `glass-ui.css` seam greps `--skeleton-glass-bg`/`--skeleton-shimmer-delay`/`--skeleton-shimmer-tint` → **0/0/0**, `card/styles.css` `@utility cartoon-surface { position:relative; border-width:2px; box-shadow:var(--shadow-cartoon-md) }`; `tailwindcss/theme.css:440` (`--animate-pulse: pulse 2s cubic-bezier(0.4,0,0.6,1) infinite`) + `:457` (`50% { opacity: 0.5 }`, no fill-mode, no 0%/100% stop). Censuses: `var(--skeleton-ink)` readers → utils.css setters + **ShadowPalette.vue only**; `aria-live|role="status"|sr-only|visually-hidden` over `demo/workbenches/extract/` → **0**; `w-12 h-12 sm:w-14 sm:h-14` → **exactly 3 files**; importers terminating at `palettes/browser"` (the top-level seam) → **0**. Routing authorities read: X wave specs — W7.md:64-65/:105/:124/:134 (o9 modify-carve)/:262-277 (**X.W7.g**)/:536-556 (**G20**, born-RED 276px@k16 measured)/:596; G19 (caption + eyebrow); W10.md:75/:113/:305-306 (**deletes the animations.css:184/:202 reduce-kill blocks**); W5.md:103-104, W4.md:134 (`demo/styles/utils.css` modify-carve), W11.md:343-344 (X-W8 owns the 19 `demo/ui` barrels + stale ESLint paths); prior registry: ColorSpaceSelector.md (L-5(a) dead-glob fold + born-RED `eslint --print-config`; L-14 config parity), AdminTagsPanel.md (ATP-35 identity; the routing law).

## Scoreboard

**35 rows CONFIRMED (0 BLOCKER · 17 MAJOR · 14 MINOR · 4 INFO) · 12 kills in the register (all re-derived; one settled a reader-vs-reader conflict, one overruled a reader-2 settlement) · 11 missed-by-the-axes findings adjudicated (readers' union, all confirmed) + 1 cross-wave hazard added by this seat (the X-W10 guard-deletion interaction, folded into SP-31) · dispositions routed X-W7 ×22 (+5 partial) · X-W8 ×3 · GLASS-OWNED ×2 (fold, one relay) · 5 NO-WAVE-OWNER rows (+3 partial).**

All four charged BLOCKERs died or demoted on re-derivation: D-1/D-2 demote to MAJOR (the canon breach is real; the "48% of the pane" magnitude is k-dependent and part-unproven), D-3's light-mode 1.000:1 "born-RED blank" headline is **refuted by token arithmetic** (K-1; the symmetric polarity inversion survives at MAJOR), and L-1's "erases itself" grade demotes against the redundant numeric k readout (K-9). The spine that survives: **the one interaction the component exists for — live k — breaks its own choreography three independent ways (SP-1/SP-2), the sole gate reads declared values and cannot see any of them (SP-7), and the seat itself is the species the ratified canon forbids on this exact route (SP-3), with the cure already owned wholesale by X.W7.g + G20.**

## Challenges to the re-readers (the incredulity seat's actual work)

| target | challenge | RULING |
|---|---|---|
| reader-1's kill of C-3's "count ≥ 9" vs reader-2's "both axes are right, differing only on boundary equality" | `maxDelay(8) = 0.22·8 + 0.24 = 2.000 s` = exactly one period, so at k=8 the last swatch's phase offset is **0** — phase-identical to strip cell #1, the wave's leading edge. The stated "strip → meta → swatches, one wave" ordering is already dead at the boundary stop; "exceeds" (C-3's verb) is not the design's failure condition. | **reader-1's kill SUSTAINED, reader-2's settlement OVERRULED** (K-4). Adjudicated threshold: **k ≥ 8** (D-6 correct; the design holds on 7 of 16 slider stops); C-3's step 6(d) "re-run at k ≥ 9" under-tests by one stop. |
| reader-1's kill of the C-4/D-5 "internal contradiction" limb vs reader-2's confirmation ("the D6 prose is false as rendered") | This seat read ShadowPalette.vue:100-102 whole: *"The ladder steps fade INTO the plate by a color-mix step (never element opacity — D6); the pulse's opacity swing is MOTION on top of the ink, not the ink itself."* The D6 clause's subject is the **ladder** — which IS built in color-mix (:107/:110/:113) — and the sentence itself names the pulse as element-opacity motion. Coherent prose, not self-contradiction; a color-mix rung is a fixed painted value, the pulse a time-varying multiplier. | **reader-1's kill SUSTAINED** (K-5). What survives is narrower: the trough **product** (rung α × 0.5) was never certified — SP-18, MINOR. Reader-2's MAJOR grade on C-4/D-5, which leaned on the contradiction limb, demotes with it. |
| reader-1's confirmed D-7 row (kept "keyframes live in neither demo/styles nor glass-ui — homeless") vs reader-2's kill of that limb | This seat read foundation.css:94-130: the demo owns a `@theme` block and already uses the exact idiom D-7 says does not exist — `--default-transition-duration: var(--duration-fast); --default-transition-timing-function: var(--ease-standard)` aliasing Tailwind's untokened defaults to house tokens. `--animate-pulse` is a Tailwind v4 `@theme` variable; overriding it in that block is the supported house lever. | **reader-2's kill SUSTAINED** (K-6); reader-1's carried "homeless" clause is struck from SP-22's claim. Surviving residue (magic constants, `2·count+2` inline style objects/render, tokenless callsite, unused `.stagger-children` idiom) ruled **MINOR** — it couples to SP-20's duplication and lands in a wave that rewrites the file anyway. |
| L-4's MAJOR "structure" grade vs reader-2's re-label (design/contract, MINOR) and reader-1's MINOR | No shared function, no code path where the two silhouettes must agree; L-4 itself concedes the ghost has no colour to seed with. Re-labelling a design finding onto the structure axis to raise severity is the move this seat exists to catch. | **MINOR, contract/design axis** (SP-19). L-4's cure as written is additionally **killed** (K-11): `WatercolorDot.color` is a required prop — "seeded on the index rather than a colour" is unspellable; the repaired cure (fixed placeholder ink + `:seed="String(i)"`, precedent EmptyState.vue:45-47) travels with the row. |
| L-1's BLOCKER vs both readers' MAJOR | The honest live-k value renders as a numeric label directly above the plate (ExtractControls.vue:15-17, sighted); the plate is `aria-hidden` so no AT-facing information exists to lose; border, hairlines and the swatch row survive under WHCM per the mechanism both readers verified. A decorative redundant register losing one of three sub-registers in one display mode is not a BLOCKER. | **MAJOR** (SP-8), reader-2's demotion adopted; the "erases itself" framing killed (K-9). |
| Both readers' "the REMOVE-vs-TIGHTEN disposition is an un-owned owner ruling" residue | This seat read X.W7.g + G20 (W7.md:262-277/:536-556): the formation **already owns the surface** — "Cut the ShadowPalette plate's mass on both `ShadowPalette.vue` and `PaletteCardSkeleton.vue`, and delete the `ExtractWorkbench.vue:163-166` caption"; "the caption dies under every option; `o9-shadow-palette.spec.ts`'s premise is re-ruled in the same commit, never left inverted." | The residue is **PARTIALLY DISCHARGED by routing**: mass-cut + caption-kill + o9 re-rule are ordered; whether the species survives at reduced mass rides X.W7.g's execution and its Triumvirate clause. The VC-vs-R12 authority tension (EmptyState.vue:31-40 + o9:12-23 encode R12; VISUAL-CONSTITUTION post-dates it, `ca4dcd20`) is recorded for that ruling, not re-litigated here. |
| reader-1's D-4-never kill cell ("equal width is the expected case once k ≥ 13, since the mean share is 1/13") | The floor applies per segment: a dominant cluster above 8% stays proportional at any k, so equal width is **reachable** (near-uniform images where every share ≤ 0.08; also the no-weights fallback `Math.max(100/n, 0.5)`), not "expected". This seat read the whole computed. | Kill **SUSTAINED on reachability alone** (K-8) — refuting "never" needs one reachable case; the "expected case" reasoning is corrected and not carried. |
| reader-2's provenance cell ("motion-quarantine.md does not exist, verified 2026-08-03") | The file is tracked at this HEAD (32,260 bytes, landed 2026-08-03 per W0 HG-11) — the cell was true only intra-day. | Corrected; the **conclusion holds either way**: zero ShadowPalette rows, no M-xx constraint. |

## Adjudicated defect roster

Line anchors are HEAD `3487d73e`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Routing law: rows land on the X wave that owns the surface. **X-W7** carries `ShadowPalette.vue`, `PaletteCardSkeleton.vue`, `PaletteColorStrip.vue`, `PaletteCardSwatches.vue`, `card/index.ts`, `EmptyState.vue` (carve), `ExtractWorkbench.vue` (carve), and `o9-shadow-palette.spec.ts` (carve) — §X.W7.g + G19/G20 name this exact plate. **X-W8** owns barrels + stale ESLint paths + the 19 `demo/ui` shims. **X-W10** owns motion canon (and plans to delete the global reduce guard — see SP-31). **X-W4** carves `demo/styles/utils.css`. `demo/styles/foundation.css` (the rosters), `useExtractSession.ts`, and `ExtractControls.vue` appear in **no** X wave's file list at the needed scope — named gaps flagged inline. Rows whose surface no X wave names are **NO-WAVE-OWNER**, owed to the next formation boundary.

### MAJOR

- **SP-1 (C:C-1 + C:C-2 — one mechanism, two symptoms).** The live-k interaction destroys the plate's own choreography: a k rise appends `:key="i"` nodes whose animations start at **insertion time** against a mount-origin delay ladder (ShadowPalette.vue:53/:56), and the same tick rewrites `count`-dependent delays (:63/:67/:77) on the 2 + min(old,new) **running, never-re-created** animations — per CSS Animations, phase = (currentTime − delay) mod duration with startTime unchanged, an instantaneous phase teleport. Host threads k LIVE (ExtractWorkbench.vue:159 ← useExtractSession.ts:44/:171; slider 1..16, ExtractControls.vue:28-30). Mechanisms structurally certain from source + spec semantics; the measured telemetry (eight startTimes, +1272/+638/+1292 ms discontinuities, the 700→820 ms teleport with `animationRestarted:false`) is live-only residue. **Disposition: BUILD → X-W7** (X.W7.g/G20; if a living register survives the mass cut, the re-ruled o9 must assert one shared startTime + monotone phase, not declared delays — SP-7).
- **SP-2 (C:C-3 / D:D-6 — the ladder outruns the period).** `maxDelay(count) = 0.22·count + 0.24 s` vs a 2.000 s pulse period (tailwindcss/theme.css:440, verified) — the wave folds back on itself at **k ≥ 8** (adjudicated threshold; see Challenges/K-4): the "one wave, strip → meta → swatches" reading holds on 7 of the slider's 16 stops and inverts where the plate is busiest (k=12: swatch #3 at 1.98 s sits 20 ms of phase from seg #1; k=13 maxDelay 3.10 s = 1.55 periods). Arithmetic re-derived by this seat; both axes' k=12/k=13 values reproduce exactly. **Disposition: BUILD → X-W7.**
- **SP-3 (D:D-1 — the seat is the species the canon forbids on this route).** VISUAL-CONSTITUTION.md:198 (Extract, verbatim): "the undeveloped state remains contextual, **not a giant shadow placeholder**"; §3 law 2 (≤15% invitation tray) and law 8 (no equal size/shadow competition); :202 (Mix) bans the filler outright. Geometry re-derived statically: ~148 px @ k=5, 214 @ k=12 (matches D-1's live 214 exactly), 276-278 @ k=13, against a 180 px stage (`min-h-[180px]`, layout="column") — and **G20's own born-RED measurement (276 px of pure skeleton @ k=16) independently confirms it**. Shadow asymmetry: ghost `shadow-cartoon-sm` vs a shadow-less dashed drop zone, sighted in both screenshots by this seat. The VC:186 citation is killed (K-7 — wrong section, wrong instrument); the "48% of the pane" ratio stays UNPROVEN (k- and layout-dependent; at the k=5 default the ghost is *smaller* than the stage — sighted). **Disposition: BUILD → X-W7 (X.W7.g + G20 own the cure; the surviving REMOVE-vs-TIGHTEN question rides that wave's execution + o9 re-rule, ordered in the same commit).**
- **SP-4 (D:D-2 — two "nothing yet" grammars in one viewport, three in one column).** Sighted by this seat in safari-desktop-light/extract.png: the Extract pane's undeveloped result is an opaque cold-neutral slab with a cartoon cast — the coldest object in a warm scene — while the sibling My Palettes pane, same frame, speaks the dashed translucent WatercolorDot trio over `· EMPTY PLATE ·`; inside the left column the drop zone states "nothing yet" as dashed/tinted/weightless (ImageDropZone `border-2 border-dashed border-primary/30 bg-primary/5`). Mobile: the ghost is the heaviest object below the invitation (sighted). CAVEAT preserved: EmptyState.vue:31-40 is a **ratified in-tree ruling** deliberately splitting the registers; which register dies is the X.W7.g ruling, not a measurement. **Disposition: BUILD → X-W7.**
- **SP-5 (D:D-3, structural limb — the continuity premise is void; polarity inverts at the swap, in BOTH schemes).** Glass-ui 7.0.0's Skeleton reads none of the house seams (this seat's greps: `--skeleton-glass-bg`/`--skeleton-shimmer-delay`/`--skeleton-shimmer-tint` → 0/0/0 in the shipped bundle; d.ts = `class` only), so PaletteCardSkeleton's `surface`/`variant`/delay declarations are inert and the skeleton renders `var(--muted)` while the ghost renders `--skeleton-ink`. Both readers' independent oklab computations agree within 0.005: light — ghost ink **darker** than well, `--muted` **lighter**; dark — inverse. The plate flips raised↔sunken mid-interaction in both schemes. The light-mode 1.000:1 "blank" headline is **killed** (K-1). **Disposition: split — demo halves BUILD → X-W7 (both plates in W7's bounds); the missing producer tone/stagger/register seams are GLASS-OWNED → the standing glass-ui BH relay (with SP-12).**
- **SP-6 (D:D-4 — the instrument lies about its output shape, four limbs less one).** (1) `flex-1` equal strip vs the population-weighted developed strip (WEIGHT_FLOOR computed read whole by this seat) — with the "never equal-width" absolutism struck (K-8); the workbench prints the dominant share as display type one element above (ExtractWorkbench.vue:122-128, sighted). (2) Meta row: ghost 40 px (`py-2.5` + `h-5`) vs the real row's `text-subheading` + Badges + Button — direction certain statically, live 56 px magnitude UNPROVEN. (3) Cartoon rung: `shadow-cartoon-sm` vs `cartoon-surface` = `--shadow-cartoon-md` (producer bytes read — a confirmed sm→md jump). (4) `gap-px` hairlines + missing `rounded-t-card` vs the developed strip. **Disposition: BUILD → X-W7.**
- **SP-7 (C:C-5 / D:D-10 — the sole gate is vacuous over two of three stages and locks in the design it protects).** o9 queries `.shadow-seg` ONLY (:83/:108/:150); zero test references to `shadow-swatch|shadow-block` outside the SFC (grep verified); the live-k leg asserts element COUNT only (5→6→5); `assertPulsesLive` reads `getComputedStyle().animationDelay` — the **declared** value, correct while the rendered animation is shattered, so SP-1/SP-2 are invisible by construction; duration bounded only from below; and the rest-presence assertion (:136-142) makes a green suite evidence *for* the SP-3 violation. All four of C-5's green-mutation claims verified against the spec's actual assertion set. **Disposition: BUILD → X-W7** (o9 modify-carve is in W7's bounds; W7.g orders the re-rule in the cure commit).
- **SP-8 (D:D-8 / L:L-1 — forced-colors: the plate dissolves, the one pinned element is the least meaningful, the state has never been captured).** Roster membership byte-verified by this seat: `.shadow-swatch` sits in foundation.css's tier-1 `forced-color-adjust:none` block (~:690) — whose own docblock defines tier 1 as "surfaces whose whole PURPOSE is to show a color" — and again in the print block (~:835, `print-color-adjust:exact !important`), though it shows no colour and is aria-hidden. Under WHCM the tier-2 plate/segs/blocks adopt system colours while the swatch row alone keeps a ~1.1:1 author grey over an uncalibrated Canvas; the producer primitive ships the exact designed fallback the hand-roll forfeits (`forced-colors:active { .skeleton { opacity:.18; background:canvastext } }`, bytes read). The two axes cross-corroborate the swatch-retention limb; rendered pixels UNPROVEN-NEEDS-LIVE, and `shots/forced-colors-desktop/` still has **no extract.png** (5 files, verified). L-1's BLOCKER framing killed (K-9). **Disposition: BUILD → X-W7 for the component-side cure (leave the rosters / consume the primitive), with a NAMED GAP: `foundation.css` is in no X wave's list at roster scope (X-W5/X-W10 carve it narrowly elsewhere) — the roster rows degrade to NO-WAVE-OWNER if W7 does not adopt the two lines.**
- **SP-9 (L:L-2 — the component lives in the wrong feature; a green gate forbids its own feature from rendering it).** Census re-run by both readers, exhaustive: the ONLY render is ExtractWorkbench.vue:159, importing from `../../palettes/browser/card`; inside `demo/palettes/` — two barrel exports + prose, **zero renders**; and o9's `assertNoFillers` (:59-62) asserts zero ghosts at every palette-browser host, so the house has a standing test that this file is in the wrong place. Contrast PaletteCardSkeleton (real renders, BrowsePane ×2). **Disposition: BUILD → X-W7** (W7.g decides the species' fate and touches `card/index.ts`; the file's home follows the ruling).
- **SP-10 (reader-2 missed #1 — the "stable public API" seam has ZERO consumers).** `demo/palettes/browser/index.ts` ("External consumers reach the feature through THIS seam") is 46 lines of dead re-export surface: this seat's own grep for importers terminating at `palettes/browser"` → **0 hits**; all four real reaches go to sub-barrels (ExtractWorkbench, MixSourceSelector, GenerateControls → `browser/card`; App.vue → `browser/dialog`). The front door has no visitors — the enabling half of SP-9 no axis checked. **Disposition: BUILD → X-W8** (barrel liveness; either the seam dies or the four reaches re-point — one decision, X-W8's surface).
- **SP-11 (L:L-3 — the boundary rule the seam cites as "standing" matches nothing).** eslint.config.js:232-238 governs `demo/@/components`/`demo/@/lib` (ABSENT — `ls demo/@` fails, verified) plus live `demo/color-picker/**`; :246-249/:296 ban `@components/custom/palette-browser/**/*.vue` — an alias killed at W43/RF-15 (tsconfig.demo.json records it), 0 occurrences repo-wide; the composables object governs an absent tree. So `browser/index.ts:6-8`'s "the G-DEMO-3b boundary enforces it standing" is a false green — the enabling condition for SP-9/SP-10. **IDENTITY GUARD (anti-rename)**: the dead-glob condition is fact-identical to ColorSpaceSelector adjudication **L-5(a)** — already FOLDed to the lint-glob-liveness wave with a born-RED `eslint --print-config` command; never re-booked. The NEW limb here is the barrel's false citation. **Disposition: the glob fold keeps its L-5(a) identity (X-W8 owns stale ESLint paths per W11.md:343-344); the false-citation line dies with any W7/W8 barrel touch — a named one-line duty.** (Readers' file-count squabble — 16 vs 17 vs 29 for `demo/color-picker` — is metric-dependent and immaterial; the ABSENT/PRESENT verdicts are what the finding rests on, and all hold.)
- **SP-12 (L:L-5 — the tone-stepped staggered skeleton-block concept has no producer home; both consumers invented one, in opposite directions).** Producer surface verified whole: one prop, hardcoded `var(--muted)` + `var(--radius-input)`, `--duration-shimmer` the sole seam. PaletteCardSkeleton wrote a wish into the producer API and got nothing (all five invented seams 0-occurrence); ShadowPalette wrote a private fork and forfeited the three environment carve-outs. utils.css:56-60 mints a three-property block for a contract that does not exist. **Disposition: GLASS-OWNED → the standing BH relay (the `--skeleton-bg`/`--skeleton-delay`/register ask), demo halves ride X-W7; no local `@media (forced-colors)` patch (a fourth private copy) permitted meanwhile.**
- **SP-13 (reader-2 missed #4 — the "ONE loading-ink recipe" is a no-op on three of its four consumers; the house ships TWO loading inks while its docblock asserts one).** This seat's reader census: `var(--skeleton-ink)` is read by **ShadowPalette.vue alone** (utils.css's other hits set further dead properties); PaletteCardSkeleton/AdminListSkeleton/AdminTagsPanel render `<Skeleton>` → `var(--muted)`, so `.skeleton-ink-register` does literally nothing on them, and the E1-R2 certification governs a single element type while utils.css:36-43 claims "every skeleton surface in the app". **Disposition: NO-WAVE-OWNER as a row (utils.css named-gap; X-W4 carries `demo/styles/utils.css` modify-carve as the named candidate); discharge follows SP-12's relay + X-W7's plate cure.**
- **SP-14 (reader-1 M1 ≡ reader-2 missed #2, convergent — both ghosts clip at the card root, the exact mechanism the house RETIRED).** ShadowPalette.vue:43 and PaletteCardSkeleton.vue:34 both carry root `overflow-hidden`; PaletteCard.vue:15-19 records the ratified opposite verbatim ("NO overflow-hidden (S.W5-10 / S-15-A): a card-level radius clip rasterizes 1-bit at compositing-layer bounds; the strip clips its OWN corners below — an interior clip keeps normal AA") and implements it (`rounded-t-card`/`rounded-l-card` passed to PaletteColorStrip, which clips itself) — all read by this seat. Corner AA therefore changes register at the very swap all three files call "ONE plate developing in place". The clip **is load-bearing today** (K-3 — the strip's square corners would paint through `rounded-card` without it; rounded corners sighted in both screenshots): the cure gives the ghost strip its own radius, never a bare deletion. No axis caught it; D-11 asserted the opposite. **Disposition: BUILD → X-W7.**
- **SP-15 (reader-1 M2 — the border jumps 1px → 2px at the swap, on top of the shadow-rung jump).** Both ghosts: Tailwind `border` (1px) + `shadow-cartoon-sm`; the developed card carries no width utility and takes `border-width: 2px` + `--shadow-cartoon-md` from `@utility cartoon-surface` (producer bytes read by this seat). At the morph the plate gains 1px of border per side and steps a shadow rung simultaneously — a true layout jump inside the seat ExtractWorkbench.vue:94-95 calls "a material change, not a layout jump". **Disposition: BUILD → X-W7.**
- **SP-16 (reader-1 M3, from the K-10 kill — the Extract seat never announces completion; the tool's entire output lands in silence for AT).** This seat's grep: zero `aria-live`/`role="status"`/`sr-only`/`visually-hidden` in `demo/workbenches/extract/`. The chain is each-part-correct, whole-part-broken: idle ghost aria-hidden (correct), PaletteCardSkeleton `role="status"` (correct) but UNMOUNTED at completion by `<Transition mode="out-in">`, PaletteCard mounts `role="article"` — no surviving region carries "5 colors extracted". The k slider also has `aria-label` with no `aria-valuetext` (ExtractControls.vue:26-31, read), so the live-k re-segmentation has no non-visual equivalent. **Disposition: NO-WAVE-OWNER with named candidates — X-W7 carves ExtractWorkbench.vue (for W7.g) and X-W4 owns the semantic-controls axis; ExtractControls.vue is in no X file list (named gap).**
- **SP-17 (reader-2 missed #3 — unscoped global rules keyed on one SFC's private template class; every prescribed cure orbit orphans them silently).** foundation.css's two roster rows select `.shadow-swatch`, defined nowhere but ShadowPalette.vue:75/:112 (Vue scoping adds an attribute, it does not rename the class — the match is real, empirically corroborated by L's probe). Consequence neither D-8 nor L-7 states: L-7's rename AND L-5's deletion each orphan foundation.css:~690/:~835 with zero compiler/linter/test signal. Any SP-8/SP-19/SP-35 cure **must** carry the two foundation.css lines in the same commit. **Disposition: NO-WAVE-OWNER for the coupling mechanism (foundation.css gap, as SP-8); recorded as a binding rider on every wave touching the swatch class.**

### MINOR

- **SP-18 (C:C-4 / D:D-5, numbers limb only — the derived rungs were never certified; the swatch rung is near-invisible).** Both readers' independent oklab tables agree within 0.005 with each other and with the axes' live probes: swatch rung ~1.10:1 at rest, ~1.05:1 at the pulse trough (α × element-opacity product), against the E1-R2 certification that covers the base ink alone. Contradiction limb killed (K-5); the plate is aria-hidden so WCAG 1.4.11 is not binding — the breach is of the file's own "reads as BLOCKS" contract. **Disposition: BUILD → X-W7** (certify the rungs or let them die with the mass cut).
- **SP-19 (C:C-6 / L:L-4 / D:D-4.1 — perfect circles where the instrument's real output is a seeded irregular WatercolorDot).** `rounded-badge` → `--radius-pill` = 9999px; the product renders WatercolorDot silhouettes — the disagreement sighted by this seat in the desktop screenshot (hard discs left, irregular dashed silhouettes right, same frame). Axis re-label to MAJOR-structure rejected; L-4's cure as written killed (K-11), repaired cure travels. **Disposition: BUILD → X-W7.**
- **SP-20 (C:C-7 — one wave, two implementations).** All four stagger expressions byte-duplicated between ShadowPalette.vue:56/:63/:67/:77 and PaletteCardSkeleton.vue:50/:60/:66/:77 (verified); the sibling already speaks custom properties; utils.css:42 records the house performing this exact lift for the INK and not the wave. G20 itself names the shared geometry ("a one-file cure leaves the mass at the loading state"). **Disposition: BUILD → X-W7.**
- **SP-21 (C:C-8 — three strip geometries in one morph seat).** ghost `gap-px`+`flex-1` · skeleton no-gap `100/count%` · developed no-gap population-weighted — all three inside the same `<Transition name="vj-morph" mode="out-in">` (ExtractWorkbench.vue:102-168, read). **Disposition: BUILD → X-W7.**
- **SP-22 (D:D-7, tokens limb — untokenized motion, magic constants, inline style churn).** Neither 2s nor the curve is a house token; 0.12/0.1/0.34/0.22 live in per-render inline `:style` objects (2·count+2, 34 @ k=16); the tokenized PRM-gated `.stagger-children` idiom (animations.css:34-54, read) sits unused. Fourth-name limb killed (K-2), no-lever limb killed (K-6). **Disposition: BUILD → X-W7 under X-W10 law** (no fourth *transition* family; house tokens for any surviving motion).
- **SP-23 (D:D-9 — under PRM the claimed at-rest k readout rests on a 1px ~1.4:1 hairline).** The global guard (animations.css:184-193, read: duration/iteration only, no fill-mode; Tailwind pulse has no fill-mode) settles the plate at base opacity — 1.402/1.435:1 IS the resting number, best case, carrying the file's own :47-50 charter. The honest readout is the numeric k label one element up. **Disposition: BUILD → X-W7** (dies with the mass cut or the species).
- **SP-24 (D:D-11a / L:L-6 + L:L-8 — dead identity channel, dead seam properties).** `.shadow-palette`: 4 hits repo-wide, no CSS rule anywhere, Tailwind cannot generate one (no `--shadow-palette` theme key); `--skeleton-shimmer-tint` and `--skeleton-glass-bg`: zero readers app-wide and producer-wide (this seat's census). Reader-2's mechanism correction to L-9/L-6 prose adopted where relevant: the class exists in the DOM via the template — scoping does not mint it. **Disposition: the class token dies in X-W7's file touch; the utils.css seam lines are the SP-13 surface (NO-WAVE-OWNER, X-W4 candidate).**
- **SP-25 (L:L-9 — the oracle reaches past the published contract into scoped CSS).** o9 finds the ghost by `data-slot` (:55) then asserts on `.shadow-seg` in all three legs (:83/:108/:150; verified) — pinning the exact class SP-35 renames and SP-12's cure deletes. **Disposition: BUILD → X-W7** (the ordered o9 re-rule asserts published data-slots).
- **SP-26 (reader-1 M4 — the ghost omits the developed card's swatch-region separator and top pad).** PaletteCardSwatches.vue:22-24 (read): `pt-3` + conditional `border-t border-border/15`; ghost `px-3 pb-3` only (:71). A third un-named limb of SP-6's shape lie; no separator visible in either screenshot (sighted). **Disposition: BUILD → X-W7.**
- **SP-27 (reader-1 M5 ≡ reader-2 missed #5, convergent — utils.css's specimen-walk docblock is stale-wrong about this exact component).** utils.css:66-68 (read): "the ShadowPalette **true-empty ghost** (STILL) reads it as fill" — both halves false: ShadowPalette carries no `.specimen-seg` (its scoped block reads `--skeleton-ink` only, whole-file read) and has not seated at true-empty since T.W6.5/R12 (EmptyState.vue:34-37 + o9 enforce it); PaletteCardSkeleton.vue:104-107 states the correct opposite. The designated single source of truth contradicts the tree and the file it governs. **Disposition: NO-WAVE-OWNER — a correction duty binding on any wave touching utils.css (X-W4 carve is the named candidate).**
- **SP-28 (reader-1 M6 — the morph-identity swatch scale is a hand-copied literal in three files).** `w-12 h-12 sm:w-14 sm:h-14` verbatim at ShadowPalette.vue:75, PaletteCardSkeleton.vue:76, ExtractWorkbench.vue:150 (this seat's census: exactly 3) — the host's prop copy makes it three-way drift, not C-7's two-way. **Disposition: BUILD → X-W7.**
- **SP-29 (L:L-10 / C:C-9 / D:D-12b — the k domain has no home; `count` is unvalidated on a public export).** Range declared only on the slider; four independent `5` defaults across four files (all verified by the readers, spot-checked); `count?: number` admits values no producer emits — Vue's guard warn-and-empties non-integers/negatives (runtime bytes verified by both readers), and reader-2's refinement is adopted: `count = 0` passes the guard **silently**. Hypothesis-labelled (slider-clamped today, one consumer). **Disposition: the ghost's default dies in X-W7's plate cure (prop required); the K_RANGE home is a NAMED GAP — `useExtractSession.ts` in no X file list.**
- **SP-30 (D:D-12a — the caption the aria-hidden plate delegates all meaning to breaks its own typographic unit on mobile).** **CONFIRMED BY THIS SEAT'S OWN EYES** (safari-mobile-light/extract.png): `· UNDEVELOPED PLATE — FEED IT AN` / `IMAGE ·` — the closing dot orphaned. Was reader-disputed (r1 confirmed / r2 unproven); the screenshot read settles it. **Disposition: BUILD → X-W7 — discharge by ordered deletion (W7.g: the caption "dies under every option").**
- **SP-31 (reader-1 M7, ELEVATED by this seat's cross-wave finding — the PRM guarantee is correct only by accident, and the accident is scheduled to end).** The global guard never touches `animation-delay` (animations.css:184-193, read), and the plate's safety additionally rests on Tailwind's pulse shipping no fill-mode/0%-stop (theme.css:457, read). **This seat adds: X-W10 orders the deletion of exactly those reduce-kill blocks (W10.md:113 "delete `:184` and `:202`"), moving PRM conformance to per-site `no-preference` gating — and ShadowPalette's `animate-pulse` is unconditional (:55/:62/:66/:75).** After W10, the ghost pulses under reduced motion unless X-W7's cure lands the motion PRM-conditional (or the species dies) first. Bounded by a loud gate: o9's PRM leg asserts `duration < 0.1` (:117-119, read) and goes RED at that moment — but a gate that fires cross-wave is a sequencing constraint, not a cure. **Disposition: BUILD → X-W7 with a binding SEQUENCING RIDER against X-W10** (plate motion must be `no-preference`-gated or dead before the global guard dies).

### INFO

- **SP-32 (L:L-11 — tsconfig.demo.json types a different published surface than package.json#exports ships).** 7 exports keys, no `.` root; three phantom typed paths (root, `/parsing`, `/units`) pointing at files that do not exist; two shipped subpaths untyped; the block's "CLOSED 8-key set" comment false by one. Reader-2's kill of the "typechecks green, crashes at runtime" mechanism adopted (K-12): phantom paths dead-end and TS falls through to the exports map — dead-pointer drift, not a false green. **IDENTITY: ColorSpaceSelector L-14 / AAP-32 / ATP-35 — one identity, already NO-WAVE-OWNER at the formation boundary; never re-booked. Disposition: NO-WAVE-OWNER (identity row).**
- **SP-33 (L:L-12 — demo/ui is 19 one-line alias shims; no cure adds a twentieth consumer).** Verified counts; ShadowPalette imports nothing today. **Disposition: X-W8's subtraction; recorded here as a constraint on the SP-12 cure (import `@mkbabb/glass-ui` directly).**
- **SP-34 (reader-2 missed #6 — C's headline cure undercounts its own cost ~2× and its ladder breaks the oracle it depends on).** `:key="count"` re-mounts 38 nodes at k=16, not "≤19" (arithmetic re-followed by this seat against the template: 1+1+16+1+2+1+16); the negative-delay ladder fails o9's `delay ≈ i·0.12` assertion (:100) unless cure #6 lands with it; `mod()` support is narrow. **Disposition: a binding rider on X-W7's cure authoring — the cure is adjudicated separately from the defect (the MT-CSS-A law).**
- **SP-35 (L:L-7 — author classes squat the producer's generated `shadow-*` utility namespace, on the same line that consumes it).** Present facts confirmed (`shadow-cartoon-sm` is a live producer theme namespace; `.shadow-seg` et al. share the prefix); the collision is a self-labelled hypothesis about a future token — reader-2's INFO demotion adopted. Any rename executes **with SP-17's roster rider**. **Disposition: rides X-W7's file touch.**

## Killed-claims register

Twelve kills, each re-derived by this seat — none carried on a reader's word alone.

- **K-1 — D-3's BLOCKER headline: light-mode skeleton blocks = the well "to four significant figures", 1.000:1, "the plate goes blank", "the born-RED-blank class … reappearing".** REFUTED by token arithmetic, decider read at source: D-3's own reproduction asserts "`--well-bg` resolves to the same `hsl(38 26% 95%)` in light" — but foundation.css:328 defines `--well-bg` as `color-mix(in oklab, var(--card) 92%, var(--foreground) 8%)` with `--card: hsl(30 85% 96%)`; `hsl(38 26% 95%)` is `--muted`/`--neutral-1`, a different token. The D seat substituted one for the other and then "measured" the identity it assumed. Both readers' independent recomputations: light `--muted` rgb(246,243,239) vs `--well-bg` rgb(233,225,217) → **1.171:1**, block LIGHTER than well — a reduced but visible step, corroborated twice by the repo's own token censuses (hljs.css:21-24; ExtractControls.vue:120). D-3's dark row reproduces correctly (~1.47:1). No blank, no born-RED recurrence; the BLOCKER grade dies, the symmetric polarity inversion survives as SP-5.
- **K-2 — D-7's "pulse is a fourth animation name, a defect under animations.css's law".** Category error, law read verbatim by this seat: the three-family law (animations.css:57-62) governs **Vue `<Transition>`/`<TransitionGroup>` family names** (vj-enter/vj-morph/vj-celebrate), not CSS keyframes; the same file defines `@keyframes stagger-child-in` at :34, and the demo legally ships `animate-spin` in 8 files. ShadowPalette declares no `<Transition>` at all.
- **K-3 — D-11's "overflow-hidden on the root clips nothing".** BACKWARDS — load-bearing: the full-bleed square-cornered strip is the plate's first child; the root clip is the only thing rounding its top corners to `rounded-card` (rounded corners sighted in both screenshots by this seat). The real defect is the inverse — the mechanism was RETIRED house-wide (PaletteCard.vue:15-19) — inverted into SP-14. Consequence: L's Greenfield "NO overflow-hidden" shell, applied naively, squares the ghost's corners.
- **K-4 — C-3's "count ≥ 9" fold threshold (and reader-2's "both axes are right" settlement).** OFF BY ONE / OVERRULED: `0.22·8 + 0.24 = 2.000 s` = exactly one period — the last swatch is phase-identical to the strip's leading edge at k=8; the stated ordering has one boundary and it is **8** (D-6 correct: 7 of 16 stops hold). C-3's "re-run at k ≥ 9" remediation under-tests the boundary stop.
- **K-5 — C-4/D-5's "internal contradiction: the D6 sentence says never element opacity but animate-pulse IS element opacity" (reader-2's confirmation of it overruled).** MISREADS THE SENTENCE, read whole by this seat (ShadowPalette.vue:100-102): the D6 clause's subject is the tone LADDER, which is in fact color-mix-built exactly as claimed; the same sentence names the pulse as element-opacity motion. Coherent, accurate prose. The genuinely defective residue — the uncertified trough product — survives as SP-18.
- **K-6 — D-7's "the house has no lever on it at all / it lives nowhere the project owns" (reader-1's carriage of it struck).** FALSE: `--animate-pulse` is a Tailwind v4 `@theme` variable; the demo owns a `@theme` block (foundation.css:94) and already uses this exact idiom for this exact purpose (`--default-transition-duration`/`-timing-function` aliased to house tokens, :120-129, read by this seat). One line in the block D-7 never opened supplies the lever.
- **K-7 — D-1's reliance on VC:186 ("Request-bound skeletons exist only while real work is in flight").** WRONG INSTRUMENT: :186 sits in the Palette-library-and-Browse section (location verified) and governs request-bound skeletons — which is PaletteCardSkeleton, correctly `v-if="isProcessing"`-gated. ShadowPalette is an idle instrument face announcing nothing. The finding never needed it: VC:198 is a direct hit in the Extract section and stands alone.
- **K-8 — D-4's "an extracted strip is therefore never equal-width".** FALSIFIED by the strip's own algorithm (computed read whole): when every share ≤ the 0.08 floor, all floor and renormalize equal; the zero-weight fallback is equal by construction. Reader-1's "expected case once k ≥ 13" reasoning is corrected (a dominant cluster stays proportional at any k) — the kill stands on reachability alone. The limb's substance at the k=5 default survives inside SP-6.
- **K-9 — L-1's BLOCKER grade and "the instrument face erases itself … all that remains is a white rectangle".** OVERSTATED, contradicted by L-1's own measurement: the swatch row, border and hairlines survive; the lost strip is a redundant readout — the honest live-k value is the numeric label directly above the plate (ExtractControls.vue:15-17, read); the plate is aria-hidden so 1.4.11 does not bind. Mechanism kept at MAJOR (SP-8).
- **K-10 — C's negative proof "Accessibility contribution — ZERO defects, correctly."** TRUE OF THE COMPONENT, FALSE OF THE SEAT, and stated as a clean bill for the state: this seat's grep confirms zero announcement machinery in the entire workbench; the `role="status"` element is unmounted at completion by `mode="out-in"` and no live region survives to carry the result. Filed as SP-16.
- **K-11 — L-4's prescribed cure ("render WatercolorDot variant=ghost seeded on the index rather than a colour").** STRUCTURALLY UNSOUND as written: `WatercolorDot.color` is a REQUIRED prop and the silhouette is seeded by color + seed (d.ts read by reader-2, consistent with the EmptyState usage this seat read) — the prescribed form is unspellable, immediately after L-4's own concession that the ghost has no colour to seed with. The repaired cure (fixed placeholder ink + `:seed`, precedent EmptyState.vue:45-47) travels with SP-19.
- **K-12 — L-11's false-green mechanism ("a future `@mkbabb/value.js/parsing` import typechecks green and crashes at runtime").** DOES NOT FOLLOW from L-11's own tree measurements: all three phantom `paths` targets point at files that do not exist, so the substitution fails and TS falls through to the exports map, which publishes none of them — the import ERRORS at typecheck. Dead-pointer drift only; severity rationale unsupported. (Identity row SP-32 unaffected.)

## Superlatives — with provenance, each re-proved or sighted

1. **The E1-R2 ink recipe (utils.css:57)** — one `color-mix(in oklab, var(--well-bg), var(--foreground) 15%)` expression, no per-scheme fork, self-inverting direction because `--foreground` flips with the scheme. Both readers independently recomputed 1.402:1 light / 1.435:1 dark from raw producer tokens — a 0.03 spread from a single line — and the docblock's "bounded ΔL in BOTH schemes by construction" survives adversarial recomputation, with the failure it replaced recorded beside the cure (probed 1.02:1). A design docblock that is arithmetically TRUE at the rung it certified.
2. **The announcement split (ShadowPalette.vue:36-44 vs PaletteCardSkeleton.vue:28-36)** — loading ≠ empty resolved on ANNOUNCEMENT, not motion: the idle face is aria-hidden with the reason stated where a maintainer will hit it ("a shimmering aria-hidden plate does not lie to AT"); the sibling that mounts only under `isProcessing` carries `role="status"`; the gate asserts both legs. All three challenge axes tried and failed to fault it — and it is precisely why SP-16 is the host's gap, not the ghost's.
3. **o9's positive control (:255-262, read)** — after asserting the erroring Browse pane wears neither ghost nor trio, the test re-asserts the sibling pane KEEPS its trio: "the error exemption is scoped to the erroring surface, never a global amputation." Scoped-absence proof with an inline rationale, plus the per-pane scoping helper and the stated "the oracle re-points, never deletes" discipline — the right posture for a census gate under redesign, even though the same file's assertion set is SP-7's vacuum.
4. **The 8% weight floor (PaletteColorStrip.vue:30-72, computed read whole by this seat)** — twelve lines of honest instrument: named constant with rationale at the prop, explicit three-stage fallback chain (supplied weights → the palette's own `weight` fields → equal), `n === 0 → []`, and `Math.max(100/n, 0.5)` keeping a 16-way split visible. The very shape the ghost flattens away.
5. **The truncation-honesty rule (ExtractWorkbench.vue:135-141, sighted in this seat's read)** — "truncate may trim trailing digits at narrow widths; the full readout rides title + select-all (never a lying readout)", enforced on the next line. A named, implemented invariant — the standard the ghost's own "shows its output shape" claim fails, written by the same hand one screen away.
6. **PaletteCard.vue:15-19 (read verbatim)** — engineering documentation that names the defect mechanism ("rasterizes 1-bit at compositing-layer bounds"), the alternative, and the ticket, and that the codebase actually implements — the standard against which SP-14 became measurable at all. Its sibling note is the load-bearing clip itself (ShadowPalette.vue:43): called dead by an axis, actually correct for the shape it draws; its only fault is that the house later found a better mechanism and this file was not brought along.
7. **card/index.ts:1-3 (PI-6)** — "NAMED re-exports only … SFC scoped `<style>` is a side-effecting import; named re-exports let the bundler tree-shake unused members per consumer": a non-obvious, true bundling rule, correctly explained, with zero dead members at the sub-barrel — in sharp contrast to the dead top-level seam one directory up (SP-10).
8. **The vj-morph seat (ExtractWorkbench.vue:102, read)** — one seat, three explicitly keyed contents, on the exact ratified family for "ONE surface, NEW content" (animations.css:64-67). The three plates disagree about their internals (SP-6/SP-14/SP-15/SP-21); the transition machinery around them is idiomatic.

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

1. **Forced-colors rendering of `/#/extract`** — mechanism fully static-confirmed (SP-8/SP-17); the rendered result (L's `rgb(255,255,255)` readbacks, the swatch's retained oklab) is single-probe, single-cell. **Input: capture `shots/forced-colors-desktop/extract.png` — the matrix's verified hole (5 files, none extract).**
2. **SP-1's animation telemetry** — the eight startTimes, the +1272/+638/+1292 ms discontinuities, the 700→820 ms mid-flight teleport with `animationRestarted:false`. Mechanisms are structurally certain; the millisecond witnesses belong to the re-authored o9 (real-animation assertions: shared startTime, monotone phase, post-k-change re-run at k ≥ 8), not to prose.
3. **SP-3's pane-proportion magnitudes** — the ghost's own heights are static-certain (148/214/276-278 px) and G20-corroborated; the "48% of the 642px pane" ratio and any "out-sizes the protagonist" claim are k- and viewport-dependent (at k=5 the ghost is smaller than the stage — sighted). **Input: the G20 measured-height gate at k=16 IS the live witness; no separate capture needed.**
4. **SP-6 limb 2's live 56 px meta row** — direction certain from content (a `text-subheading` + Badges + Button row cannot equal a 20px block row); the magnitude is a rendered fact.
5. **SP-35's namespace collision** — unreachable until a `--shadow-seg`-shaped token exists; no probe can advance it; it discharges by rename-with-roster-rider or by SP-12's deletion.

(The former sixth residue — the mobile caption orphan — was discharged into SP-30 by this seat's own screenshot read.)

## Closing verdict

**DEFECTIVE — ADJUDICATED, 0 BLOCKER standing.** The component is a well-documented, announcement-correct, 115-line instrument face whose premise the ratified canon forbids on its one route, whose defining interaction shatters its own choreography at three independent joints past half the slider's range, whose morph-continuity story is void at both the material seam (dead producer contract) and the geometry (clip, border, shadow, separator, silhouette), and whose only gate reads declared values and therefore certifies all of it green. Every plate-side cure has one owner — **X.W7.g under G19/G20, with the o9 re-rule ordered in the same commit** — flanked by X-W8 (dead seam, dead lint law), the glass-ui BH relay (the Skeleton seams), five NO-WAVE-OWNER rows owed to the formation boundary, and one binding sequencing rider this record adds: **the plate's motion must be PRM-conditional or dead before X-W10 deletes the global reduce guard it silently rides.** Original challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.

— end of apotheosis. Adjudicated 2026-08-06 at HEAD `3487d73e` by the fresh-Fable seat (`claude-fable-5`).
