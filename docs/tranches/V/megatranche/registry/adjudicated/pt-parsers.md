claude-fable-5

# pt-parsers — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · value.js HEAD `c654824e` (branch `tranche-u`) · **Subject**: `parse-that` module `typescript/src/parse/parsers/` — `index.ts` 8 + `json.ts` 52 + `csv.ts` 20 + `utils.ts` 23 = 103 lines, at `ef10d5b`, `typescript/` tree byte-clean (re-verified this session; the wider working tree carries unrelated rust/docs rows, none under `typescript/`).
**Corpus adjudicated**: `challenge-L-library.md` (Opus 5 [1m], 19 defects · 6 superlatives · 4 refutations) · `challenge-C-consumption.md` r2 (Opus 5 [1m], 18 defects · 5 superlatives, r1 folded whole) · two hostile re-reads (`L-18-hostile-reread-parsers-library`, `L-18-hostile-reread-consumption-parsers`), both of which re-derived the challenges and disagree with each other in four places.
**LAW CHECK — clean.** `ls /Users/mkbabb/Programming/parse-that-css-totality-p2` → No such file or directory (this session). parse-that read-only throughout; single write = this file. No `memoize`, no `enableDiagnostics` — the PT-03/PT-01 latches were never armed; every probe is a cold `parseState`/`parse` against `typescript/dist/` by absolute path (node v26.0.0, darwin arm64).
**This seat's own instruments**: full read of `json.ts`/`csv.ts`/`utils.ts` (whole, 95 impl lines); `parser.ts:18-30/:55-80/:437-478/:488-518/:523-556/:569/:592-625/:638-642/:700-710`; greps `\.call(` (2 sites: `state.ts:93`, `parser.ts:493`), `unsafeCall\b` (definition only); `test/json-vectors.test.ts:22-34`, `test/csv.test.ts:1-16,73-78`, `test/json.test.ts:5-12`, `test/subpath-gate.mjs:25-60`, `test/dist-surface.test.ts:6-35`, `scripts/proof-no-dead-combinator.mjs:8-32,49-81`; `README.md:32-38`, `docs/api.md:228-250`; `X/parse-that/waves/W1.md` (OP table, G-4, G-9 whole incl. the ~2,500 sentence) + `W2.md:125-200` (scope, PLAW-BIND, not-in-scope); sibling records `pt-index.md`, `pt-leaf.md`, `pt-lazy.md` (identity guards); value.js consumption edge re-derived (deps = glass-ui+keyframes.js only; 5-name grep → 0; `node_modules/@mkbabb/` lacks parse-that; `src/css/grammar.ts:128-132` verbatim); `dist/utils.js:1-2` chunk graph; **probe battery** (23 sections) incl. the 7-vector `invalid.jsonl` dual-idiom run, 6 rejected-input partials, 8 csv rows + a 0/500 seed-pinned rejection census, a **cold-vs-warm depth bisect in a fresh process**, the retention probe, mint-count deltas, and the raw-U+0001 oracle split.

## Scoreboard

**23 defect identities CONFIRMED (3 BLOCKER · 13 MAJOR · 7 MINOR) · 10 reader-found misses ADOPTED (2 MAJOR · 6 MINOR · 2 INFO) · 12 killed claims SUSTAINED (4 of them rulings against one reader or the other) · 9 superlatives BANKED · 5 MEASURE-AT-OPEN cells.** Dispositions are ADJUDICATED, not VERIFIED. Blocker-weight survivor: **PT-PARS-2** — the shipped `csvParser` is a total-accept function with no record separator, under a doc that names RFC 4180 at the exact line my probe reproduces collapsing.

## The incredulity seat's own work — four reader-conflict rulings + two seat findings

The two hostile re-reads agree on almost everything; where they conflict, this seat re-derived and RULED:

| # | conflict | RULING | deciding evidence (mine) |
|---|---|---|---|
| RC-1 | **"green on the first run"** (L:MAJOR-5 sub-claim / C-8): reader-L KILLED it; reader-C CONFIRMED C-8 carrying the phrase | **reader-L wins; the kill stands.** Reader-C's own missed-1 concedes the point it elsewhere confirms | My dual-idiom run over all 7 `invalid.jsonl` vectors: `parseState().isError` = **7/7 reject** (the rejection power is real) BUT `.parse()` returns **truthy on 7/7** (`[]`×5 · `[1]` · `[["a",1],["a",2]]`), so `expect(result).not.toBeUndefined()` — the suite's ONLY idiom, `json-vectors.test.ts:29` — **passes on every malformed vector**. A negative arm in the file's own idiom fails 7/7 on the first run; greenness requires the `parseState`+`isError` idiom the suite does not use (`parser-band.md:108`'s mandate, proven load-bearing) |
| RC-2 | **the FLAG_EOF "cure three lines away"** (L §7 item 4): reader-L KILLED it as misleading; reader-C repeated it inside its L-MAJOR-8 confirmation | **reader-L wins.** The dead `parser.ts:466-476` branch lives inside `Parser.call()`, which my grep (and pt-leaf's, independently) shows is **never invoked from library code** — 2 `.call(` sites total, both dead ends (`state.ts:93` inside zero-caller `unsafeCall`; `parser.ts:493` inside the discarded `flaggedParser`). The live one-token cure is `Parser.eof()` at `parser.ts:638-642`. A wave taking the L-disposition literally would resurrect a dead algebra instead of appending `.eof()` |
| RC-3 | **C-18's point figures**: reader-L carried 3,275 vs 3,262 as "mutually consistent"; reader-C killed the figures (its own runs: 3,275 array; object swinging 1,225–1,625) | **reader-C wins, and the truth is worse than either states.** SEAT FINDING (fresh process, same node v26.0.0): array-arm deepest-OK = **1,580 COLD**, then **2,305 after 5,000 warm-up parses in the same process** — the ceiling is **JIT-tier-dependent** (optimized frames are smaller, so warm processes reach ~2× deeper). Five data points now span **1,580–3,275** (array) / **1,213–1,631** (object) on one box. No point figure is citable, ever; only the interval and the categorical claim (thrown `RangeError`, not `isError`; ≪ bare-lazy 7,761; `JSON.parse` clears ≥ 40,000) survive. Reader-L's "consistent within bucketing" framing is RULED too generous |
| RC-4 | **L:MAJOR-4's harness charge** ("any harness budgeting against 7,761 will pass a crashing grammar"): reader-L confirmed it load-bearing; reader-C killed it as overstated | **reader-C wins.** My own read of `W1.md` G-9: the ~2,500 composed-ceiling sentence is already on the page ("cand-O's single lazy back-edge … ~2,500-frame ceiling becomes an ordinary ok:false"), so the ignorance charge dies. What survives is the **gate-wording defect**: G-9's normative sentence ("bounded below the measured `Parser.lazy` ceiling with stated margin") names only the bare-lazy figure — a corpus declared at 5,000 satisfies it literally and crashes every JSON-shaped composed grammar. RC-3's seat finding sharpens the cure: the declared bound must clear the **COLD** composed ceiling, because the readers' warm figures are best cases, not floors |

**Seat finding A (RC-3 above)** — the JIT-variance datum is new to the whole corpus: neither challenge nor either reader observed that the ceiling moves *within one process* with optimization state. It converts G-9's "DECLARED, NOT DISCOVERED" from a discipline into a necessity: there is no stable number to discover.
**Seat finding B — identity guards neither reader applied (anti-rename).** Three rows of this corpus are already adjudicated in sibling records and must FOLD, not re-book: C-6 (= `pt-index.md` **C-M1**, `export *` blind to the dist-surface regex, routed W4 "derive the surface, never regex it"); L:MINOR-4 (= `pt-leaf.md` **PT-LEAF-3**, the regex-EOF non-merge, routed W2 + W1 EOF-corpus rider); C-7 (kin of `pt-leaf.md` **PT-LEAF-17**'s W4 rider, subpath isolation asserted over import graphs, never file existence). The depth-rider fold row also already exists (`pt-leaf.md` L-m12 → REFINEMENT FOLD); this record strengthens that row rather than opening a second.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Line anchors at `ef10d5b`; re-resolve before any cure. "Seat" = this session's own probe/read; reader measurements adopted only where marked concordant.

| id | folds | sev | claim + proof | terminal disposition |
|---|---|---|---|---|
| **PT-PARS-1** | C-1 / L-M10 | **BLOCKER** | Every published usage example calls the exports as functions; both are `Parser` instances and the call throws. Seat: `typeof jsonParser === "object"`, `jsonParser()` → TypeError; `README.md:34-37` (verbatim, incl. the `// RFC 4180` label), `docs/api.md:230/:235` callable signatures, `leaf-parsers.md:122-129`, `CLAUDE.md:28-29`. Nothing in CI reads the docs against the artifact | **NO-WAVE-OWNER** — parse-that doc/tree repair, rides the O-15 letter channel (1.1.0 ask is live, owner-held). No X·P wave touches parse-that's checkout (W2 "Not in scope" verbatim) |
| **PT-PARS-2** | C-2 / L-B1 / L-B2 / L-M6 | **BLOCKER** | `csvParser` composite: (a) **no rejection power, structurally** — `many(min=0)`'s only failure branch is unreachable (`parser.ts:523/:550-551`); seat census 0/500 + 0/8 curated; (b) **no record separator** — `regex(/[^,]+/)` + `delim.trim()` eat `\n`/`\r\n`; seat: README's own `a,b,c\n1,2,3` → `[["a","b","c\n1","2","3"]]`; (c) RFC quoted-field failures — `a,"b""c",d` → two rows, escaped quote dropped; `a,,b` → `[["a"]]` at 1/4, three bytes in neither V nor C (COMP-1 shape) | grammar cures **NO-WAVE-OWNER** (O-15 channel; challenge-C's RETIRE verdict endorsed — no adapter recovers an unparsed record separator). **REFINEMENT FOLD**: W1 harness law — every grammar in the lane's corpus carries a **rejection leg**; an acceptance-only totality harness scores a total-accept parser at 100% (this module is the existence proof) |
| **PT-PARS-3** | C-3 / L-M1 | **BLOCKER** | `escapedString`/`quotedString` do not decode: `.map(([, esc]) => esc)` returns the escape LETTER; `string("u").skip(regex(/…{4}/))` discards the payload by `skip`'s construction (`parser.ts:189-210` returns the left value). Seat: `"\\u0041"` → `"u"`, `"\\n"` → `"n"`, `'"aAb"'` → `"aub"`; 3 of 6 classes corrupt, `isError=false`; `api.md:241` names the exact failing examples. Wrong-answer-with-success is the worst class; severity held at BLOCKER on the contract axis (the zero-consumer mitigation is PT-PARS-4's row, not a discount) | **NO-WAVE-OWNER** (RETIRE-AND-REPLACE per challenge-C §5 endorsed; repair-in-place is a breaking change on a zero-consumer export). W2 keeps it as a **COMP-1 witness class** for the algebra's escape clauses |
| **PT-PARS-4** | C-4 / L-m6 | MAJOR | Zero consumers anywhere (seat re-derived: value.js deps = glass-ui+keyframes only, 5-name grep → 0, not installed; keyframes self-declared parse-that-FREE); 5 of the root's 34 runtime exports; dead by the in-tree precept (`proof-no-dead-combinator.mjs:10-11` verbatim, enforcement a 2-name allowlist `:29-32`); contradicts `A.md:214`'s keep-ruling on the consumption axis only; the 1.0.0 `*Span` excision precedent is exact | **NO-WAVE-OWNER** — a parse-that release act (major bump), rides O-15. W2's "not in scope: adding parse-that to package.json" keeps the edge zero by law; **no wave may cite these five as inheritable surface** |
| **PT-PARS-5** | C-5 / L-M9 | MAJOR | `Parser<any>` ships (`json.ts:39-40` → `dist/parsers/json.d.ts:6` verbatim); `JsonValue` exported twice, types nothing; strict tsc exit-0 on four wrong bindings (reader-concordant, runtime-independent consequence of the declaration); contrast `csv.d.ts:3` `Parser<string[][]>` | **W2** — G-10 rider: `any` is structurally unsatisfiable against "exactly the frozen `/css` value types, zero extra properties" (`W2.md:236`); a candidate surface shipping `Parser<any>` fails admission. In-place fix NO-WAVE-OWNER |
| **PT-PARS-6** | C-6 | MAJOR | The publish gate's `namedExports()` regex (`dist-surface.test.ts:21`, seat-read) cannot see `export *` (`src/parse/index.ts:14` ↔ `dist/index.d.ts:10`, symmetric blindness, `missing` vacuously `[]`); the arithmetic closes: 29 gate-visible + 5 star-exported = 34 runtime | **FOLD → pt-index C-M1** (identity guard; already routed **W4**, "gate law: derive the surface, never regex it"). This record adds the parsers-tier witness only |
| **PT-PARS-7** | C-7 | MAJOR | `subpath-gate.mjs` iterates 4 subpaths but loads and asserts only `./core` + `./packrat` (seat-read `:25-60`: existence checks per field, 3 symbol asserts, GREEN message names two tiers) — `./utils`, the tier that homes this module, has no runtime assertion; reader-C's no-actual-drift probe honored (declared == runtime, 7 names, both module systems) | **FOLD → pt-leaf PT-LEAF-17's W4 rider** (subpath isolation/coverage asserted over import graphs and loaded symbols, never file existence), parsers-tier witness added. Gap is coverage, not a live break |
| **PT-PARS-8** | C-8 / L-M5 | MAJOR | The tests certify a fork: `csv.test.ts:5-16` re-declares the grammar byte-for-byte, never imports `csvParser` (seat-read); `json.test.ts:5-12` re-declares a DIFFERENT grammar (no exponent at `:10`, no escape handling, `any()` not `dispatch`); the only shipped-binding suite asserts `not.toBeUndefined()` (`json-vectors.test.ts:29`, seat-read) — which my RC-1 run proves passes on all 7 malformed vectors, i.e. **zero discriminating power across accept / prefix-accept / reject**; `invalid.jsonl` ships unread; `csv.test.ts:77`'s `length > 0` vacuous given PT-PARS-2(a) | **REFINEMENT FOLD** — W1 harness law, entry via `parseState`+`isError` ONLY (the band's `:108` mandate elevated from idiom to gate-checkable law: any harness leg reading `.parse()` truthiness measures nothing). Suite repair itself NO-WAVE-OWNER |
| **PT-PARS-9** | C-9 | MAJOR | `numberParser` is a false friend for the sole downstream's `<number>`: seat rows `+1`/`.5` reject (0 consumed), `01` → `0` at 1/2, `1.` → `1` at 1/2, `0x10` → `0` at 1/4, `1e400` → `Infinity` (String-printed, the JSON.stringify-null trap avoided); value.js `numberToken` verified verbatim (`grammar.ts:129-132`, anchored + finite-guarded). Ships the band's adjudicated non-finite debit (`parser-band.md:97/:141`) | **W2** — declared-divergence row: the incumbent utility ships the class the band REJECTED; any candidate numeral production is measured against `numberToken`'s semantics, never this export's. Repair NO-WAVE-OWNER (RETIRE-AND-REPLACE endorsed) |
| **PT-PARS-10** | C-10 / L-m1 | MAJOR | `quotedString(quote)` interpolates raw into a character class (`utils.ts:16`, one site, no escaping): `"\\"` → build-time SyntaxError (r1's row, sustained through C §8(a)'s self-correction); `"]"` → rejects its own well-formed input; `""` → zero-width quote accepting unquoted text. Severity RULED MAJOR (a public API that throws at construction for a legal-shaped argument), over L's MINOR | **NO-WAVE-OWNER** repair (one `replace`); **W2** keeps the AC-3/AC-4 reading: a quote character is a table row consumed by both siblings, never a per-call `new RegExp` |
| **PT-PARS-11** | C-17 | MAJOR | `.parse()` on failure can return a truthy, wrongly-typed value: seat `quotedString().parse('"unterminated')` → `["unterminated"]` (truthy, `Array.isArray`, declared `Parser<string>`) vs `"unterminated"` → `undefined` — two failure sites, two shapes (C §8(b) adjudication correct). Mechanism at the bytes: `wrap`'s close-fail restores offset, never resets `state.value`; `parse()` reads it (`parser.ts:77-79`) | **REFINEMENT FOLD** — the JS-boundary invariant (PT-07 successor clause, extending pt-leaf's fold row): the boundary asserts `isError` **and never reads `.value` under `isError`** — `.value` on error is garbage by construction, not merely `undefined`. See MISS-1 for the flagship landing |
| **PT-PARS-12** | C-18 / L-M4 | MAJOR | Two unbounded `Parser.lazy` back-edges (`json.ts:25/:28`, the only two, both TDZ-necessary); a deep document **throws `RangeError` across the JS boundary** rather than returning `isError` (`parseState` wraps in try/finally, never try/catch). CATEGORICAL half confirmed at the bytes and by seat probe (object arm THREW at 1,214). ORDINAL half: **point figures killed** (RC-3) — seat interval [1,580 cold – 3,275 best-warm] array / [1,213–1,631] object, JIT-tier-dependent, all ≪ bare-lazy 7,761 ≪ `JSON.parse` ≥ 40,000 | **REFINEMENT FOLD** — strengthens pt-leaf L-m12's existing G-9 row (one identity, not two): G-9's normative sentence must bound declared depth below the **cold composed** ceiling of the lane's deepest composed grammar with stated margin, never the bare-lazy figure. Numbers → **MEASURE-AT-OPEN #1**. Depth-as-algebra-parameter already W2 §3 law (debt 3) |
| **PT-PARS-13** | L-M7 | MAJOR | The utils trio are per-call factories: seat mint-deltas **15 / 8 / 2** `Parser` instances per call (`PARSER_ID` monotone), identity unstable, fresh `RegExp` per `quotedString()` (statically 1 explicit + 3 sticky clones = 4, sustaining reader-L's correction of L's own undercount); contrast module-scope `const` graphs in `json.ts`/`csv.ts`; docs teach the call-site pattern, and `leaf-parsers.md:139` passes an argument to 0-arity `escapedString` (TS2554 in TypeScript — the doc is worse than L says) | **W2** — lifecycle law witness: parsers are module-scope values with stable identity (dispatch interns by reference, `leaf.ts:104-111`, so factory idioms double table slots). Memo-key consequence held at conditional INFO (RC: reachable only via `memoize`, which this module provably cannot arm — S-4) |
| **PT-PARS-14** | L-M8 | MAJOR | The `FLAG_*` algebra is dead: sole `.flags` write lands on the discarded `flaggedParser` (`parser.ts:492-496` constructed, configured, never returned — seat-read whole); `FLAG_EOF` never set → `:466-476` unreachable; **`Parser.call()` is wholly uncalled** (RC-2: 42 lines dead in full, not merely its flagged tail) and `ParserState.unsafeCall` has zero callers; `Parser.trim()` mints 2, discards 1; this module pays 8 `.trim()` sites. Triple-verified (seat grep + reader-L + pt-leaf's instrument line, all concordant) | **W2** — kill-by-number ledger rows, no taste required: the successor algebra carries no flags channel, no dead choke point; `call()` (`parser.ts:437-478`) + `unsafeCall` (`state.ts:91-94`) + the discarded clone enumerated as provable-by-grep dead surface. The MAJOR-2 cure is `.eof()`, never FLAG revival (RC-2 ruling carried into the ledger text) |
| **PT-PARS-15** | C-14 / L-M2 | MAJOR | No `.eof()` anywhere in the module (grep 0; the method is live at `parser.ts:638-642`, seat-read, exercised at `test/debug.test.ts:113` per readers): every export is a prefix parser presented as a document parser; 6-10 prefix-accept rows reader-concordant (`{"a":1} garbage` at 8/15 etc.), `JSON.parse` throws on all; undetectable through `.parse()` (PT-07) | **NO-WAVE-OWNER** (one-token repair rides O-15); **W1 rider**: the lane's corpus carries trailing-garbage rows per grammar, asserted via consumed-offset, not value shape |
| **PT-PARS-16** | C-15 / L-M3 | MAJOR | Raw C0 controls U+0001–U+001F accepted inside JSON strings (RFC 8259 §7): the class `[^"\\]` admits them and `json.ts:23`'s `indexOf("\\")` fast path routes control-bearing backslash-free strings AROUND the `JSON.parse` oracle by construction. Seat: raw U+0001 `isError=false` vs `JSON.parse` THROWS. Cure is one range | **NO-WAVE-OWNER** (one-range repair, O-15); **W1 rider**: control-char rows in the equivalence corpus (the oracle-bypass shape is the lesson: a fast path must be probed on the class it skips the oracle for) |
| **PT-PARS-17** | C-11 | MINOR | `.parse()` declared `T`, returns `undefined` on (some) failures; strict tsc exit-0 over three non-nullable bindings holding `undefined` (reader-concordant; consequence of the `.d.ts`, no re-run needed). PT-PARS-11/MISS-1 supply the fourth and fifth shapes | **REFINEMENT FOLD** — same boundary-invariant clause as PT-PARS-11 (one fold row). In-place `T \| undefined` NO-WAVE-OWNER |
| **PT-PARS-18** | C-12 | MINOR | Mixed lifecycle: two module-singleton parsers + three factories in one 103-line module; `Parser.state` is public mutable (`parser.ts:26`, cite drift corrected), assigned every parse (`:66/:71`), process-global last-writer-wins on the singletons — and it is the only failure-reason channel the module leaves open (given empty `expected` + lying `.parse()`) | **W2** — O-8 witness class: no operator owns process-global mutable state; the successor's diagnostics are values returned, never fields on a shared instance. See MISS-2 for the retention half |
| **PT-PARS-19** | C-13 / L-R4 | MINOR | Every failure yields `expected: undefined` under the shipping default — but positionally accurate (`furthest` correct in 8/8 reader rows + seat's `"tru"` → 0). Mechanism ruled per the kill register: dispatch MINTS a label (`leaf.ts:128-132/:142`); the core's diagnostics gate discards it (`utils.ts:33`) — C-13's "dispatch produces none" misattribution stays killed; L-R4's framing ("positionally accurate, textually silent") is the record | **W2** — R-LAW-3/debt-1 witness (labels are values, never mode-gated); **W1 rider**: rejection-shape assertions in the lane bind `furthest`, never `expected` |
| **PT-PARS-20** | C-16 / L-m3 / L-m5 | MINOR | All three impl files import the root barrel that re-exports them (genuine cycle, initializers running mid-cycle); `Parser` import dead in `csv.ts:5` and `utils.ts:4` (seat-read whole; `noUnusedLocals` unset, `verbatimModuleSyntax` preserves emit); `export *` silently widens the root API (`sideEffects:false` = bundler's mercy, not a boundary); `utils-entry.ts` already demonstrates the leaf-import cure. Reader-C's mechanism correction adopted: the dist chunk DOES import `Parser` (seat-read `dist/utils.js:2`) — chunk merging, not per-file elision | **NO-WAVE-OWNER** (source hygiene, O-15); the `export *` widening arm folds into PT-PARS-6's pt-index C-M1 identity |
| **PT-PARS-21** | L-m2 | MINOR | Number grammar duplicated byte-identically (`json.ts:21` ≡ `utils.ts:22`); two incompatible string grammars 40 lines apart (one spec-exact, one 3-of-6 corrupt); the drift is realized: `test/json.test.ts:10` carries a third copy with no exponent (seat-read) | **NO-WAVE-OWNER**; W2 carries it as the AC-4 drift-with-nothing-to-stop-it exhibit |
| **PT-PARS-22** | L-m4 | MINOR | `regex()` at EOF skips `mergeErrorState` (`leaf.ts:326-330`); reachable through a module export (`numberParser().parseState("")` → `furthest=-1` vs `string`'s `0`, reader-concordant); masked when probed only through `jsonParser` (dispatch labels first) | **FOLD → pt-leaf PT-LEAF-3** (identity guard; already routed W2 terminal-totality + W1 EOF-corpus rider; incumbent deletion rides O-15 as a 2.0.0 event). This record adds only the public-export reachability witness |
| **PT-PARS-23** | L-m7 | MINOR | Goldilocks inverted: 103 lines is in-band, but `utils.ts` is dead weight by three independent measures (byte-duplicate terminal, zero consumers, wrong on 3/6 classes); against `parsers/index.ts:3`'s own "spec-grade" claim, neither survivor is spec-grade (json over-accepts on three axes; csv cannot reject) | **NO-WAVE-OWNER** — the RETIRE case's cohesion arm; carried beside PT-PARS-4 |

## Misses admitted into the roster — 10 rows (◆ = found convergently by both readers)

| id | sev | claim (adopting reader's, re-derived by this seat where marked) | disposition |
|---|---|---|---|
| **MISS-1 ◆** | MAJOR | `jsonParser.parse()` returns a **truthy, wrongly-typed partial on REJECTED input** — the flagship landing of PT-PARS-11, and neither challenge carried it. SEAT: 6/6 rejected inputs return truthy (`'{"a":1,}'` → `[["a",1]]`, `'[1,2,]'` → `[1,2]`, `'{"a":1,"a":2'` → the raw **pair array** — `Object.fromEntries` never applied, a value outside the `JsonValue` codomain); it is the exact reason RC-1's kill holds (all 7 invalid vectors pass the suite's assertion), it inverts L:S6 through the documented entry (`parse('[1,2,]')` ≡ `parse('[1,2]')` byte-identically), and `Parser<JsonValue>` would NOT catch it (`JsonValue[]` is a `JsonValue`; the pair-array escapes even that) | **REFINEMENT FOLD** — folded into the PT-PARS-11 boundary-invariant clause (never read `.value` under `isError`); **W1**: every rejection leg asserts through `parseState`, and any oracle diffing values first checks `isError` parity |
| **MISS-2** | MAJOR | The two singletons retain the last parsed `src` AND `value` for process life with no release API. SEAT: after one big parse, `jsonParser.state.src.length = 40,001` and `state.value` = the live 20,000-element array, both reachable from the module const; last-writer-wins swap confirmed. Shared across BOTH entries (S-5's identity property compounding in the wrong direction) | **W2** — the O-8 witness's retention half: per-instance globals with unbounded footprint are the class O-8 exists to exclude. Heap magnitude → MEASURE-AT-OPEN #5 |
| **MISS-3** | MINOR | `csvParser` silently strips apostrophes from legal RFC 4180 unquoted fields (the undocumented single-quote arm, `csv.ts:13`). SEAT: `'x'` → `[["x"]]` 3/3 — two bytes destroyed, no signal; `docs/api.md:237` claims plain RFC 4180. A fourth, orthogonal silent-corruption path | folded into PT-PARS-2's ledger (third COMP-1 breach in 20 lines); NO-WAVE-OWNER |
| **MISS-4** | MINOR | `csvParser` can "succeed" consuming **zero bytes**: SEAT `",,,"` → `isError=false`, value `[]`, consumed **0/3**; `"a,,b"` → 1/4. Both challenges' tables imply total-consumption; the sharpest single witness for PT-PARS-2(a) — success, empty value, no bytes read, no error channel | folded into PT-PARS-2; **W1** rejection-leg law cites this row as the canonical acceptance-harness false-pass |
| **MISS-5** | MINOR | Every failed top-level parse allocates a second `ParserState` + two immediately-overwritten arrays on the reject path with diagnostics off (`parser.ts:61-64` + class-field initializers, artifact-verified by reader-L) — zero-value work on the exact leg `parser-band.md:117` makes a first-class bench leg | **MEASURE-AT-OPEN #2** (joins pt-leaf's alloc cell); W2 R-LAW-3 delivery-side kin (pt-leaf PT-LEAF-23) |
| **MISS-6** | INFO | Kill-by-number extensions: `Parser.call()` dead in full + `./utils`'s dist chunk **contains** the packrat implementation (SEAT: `dist/utils.js:2` imports `packrat-entry-CS1td-8B.js`, 7 `PACKRAT_ARMED`/`makeMemoized` matches) — S-4's mechanism must read "arming requires a call site this module lacks", never "the code is in a different chunk" | folded into PT-PARS-14's W2 ledger + the S-4 bank's stated mechanism |
| **MISS-7** | MINOR | No consumer-facing document names ANY subpath: zero `@mkbabb/parse-that/` hits in README/docs/CLAUDE.md outside tranche planning; every example teaches the root-barrel import PT-PARS-20 flags — the `./utils` tier is undocumented at every published entry point | **NO-WAVE-OWNER** (O-15); W4 kin noted (surface documentation is part of publish discipline) |
| **MISS-8** | MINOR | `leaf-parsers.md:144` documents `numberParser` as "IEEE 754 (integers, floats, exponents)" — false in both directions — while `docs/api.md:249` accurately says "JSON-style" (SEAT-verified at the bytes). C-9's verdict survives on the name + measurements; the ledger carries the mitigation | folded into PT-PARS-9; NO-WAVE-OWNER |
| **MISS-9** | MINOR | `proof-no-dead-combinator.mjs`'s cross-tree consumer sweep has NO vacuity guard: `walk()` opens `if (!existsSync(dir)) return acc;` (SEAT-read `:71-73`) over filesystem-relative sibling paths — one checkout-layout change from a silent empty sweep; the definition-side check HAS the guard (`:49-58`), so the author knew the failure mode | **NO-WAVE-OWNER** (O-15; the cure is the three lines already written at `:49-58`, pointed at the walk count). W4 gate-law kin |
| **MISS-10** | INFO | `package.json` publishes no top-level `main`/`module`/`types` — exports-map only; legacy `moduleResolution` consumers resolve nothing from any entry. Does not bite value.js (bundler resolution); recorded on the consumption axis for the NEXT consumer | **NO-WAVE-OWNER**; packaging census kin of pt-index r1·NEW-J (W4) |

## Killed-claims register — 12 rows, each sustained or ruled by this seat

| # | killed claim | ruling |
|---|---|---|
| K-1 | L:MAJOR-5's "wiring `invalid.jsonl` … would be green on the first run; pure oversight" | **KILLED — RC-1** (seat-measured: the suite's own idiom passes 7/7 malformed; greenness requires the idiom the suite doesn't use). Parent defect stands and is WORSE than stated |
| K-2 | L:MAJOR-8's "call() is only ever its `:438-440` passthrough" | **KILLED — RC-2**: `call()` is never invoked from library code at all; the passthrough is dead too. Triple-verified (seat, reader-L, pt-leaf) |
| K-3 | L §7 item 4, "the cure sitting three lines away" (FLAG_EOF branch) | **KILLED — RC-2**: reviving it requires re-plumbing a wholly-dead dispatch path; the live cure is `Parser.eof()`. Reader-C's repetition of the framing ruled against |
| K-4 | L:MAJOR-7's "…+ 1 RegExp per `quotedString()`" | **KILLED as undercount** (reader-L sustained): 4 explicit `new RegExp` per call (1 + 3 sticky clones, seat-derived statically) + per-evaluation literals; finding stands, figure was low |
| K-5 | L:MAJOR-7's unconditional memo-key-ceiling consequence | **KILLED as over-claimed** (reader-L sustained): `getCijKey` is reachable only inside `makeMemoized`; this module provably cannot arm it (S-4). Held at conditional INFO |
| K-6 | L:S1's causal claim ("no backtracking on the reject path — which is why the 7 vectors fail at offset 0") | **KILLED** (both readers convergent; seat `furthest` = 1/1/6/1/3/15/1 with `offset` restored to 0 — offset 0 is the PRESENCE of backtrack-restores, not their absence) |
| K-7 | C:S-3's falsifier ("the empty expected set is itself evidence the dispatch table is real — shared receipt") | **KILLED** (reader-C sustained): the expected-set is gated at core `utils.ts:33`, blind to dispatch-vs-`any`; the table's realness is proven by interning counts instead (S-1) |
| K-8 | C-18's point figures 3,262 / 1,631 and the derived 4.8× / 6× ratios | **KILLED AND SHARPENED — RC-3**: five measurements span 1,580–3,275 on one box; seat-demonstrated JIT-tier dependence (1,580 cold → 2,305 warm, one process). Only the interval + categorical claim are citable |
| K-9 | L:MAJOR-4's "any harness that budgets against 7,761 will pass a grammar that crashes" (the ignorance charge) | **KILLED — RC-4** (seat-read W1.md: the ~2,500 composed sentence is already on G-9's page). Survivor: the gate-WORDING defect, routed REFINEMENT FOLD |
| K-10 | C-13's mechanism ("a dispatch miss produces no expectation at all") | **KILLED as misattribution** (reader-C sustained): dispatch mints and passes a label (`leaf.ts:128-132/:142`); the core discards it. L-R4's framing is the record |
| K-11 | C-10's pattern gloss (`[^]\]+` = "any char then one-or-more `]`") | **KILLED as mis-transcription** (reader-L sustained): the built source is `[^]\\]+` — empty negated class, then a mandatory literal backslash, then `]+` — unsatisfiable for ordinary input, not merely permissive. Measured outcome unchanged |
| K-12 | Cite-drift bundles (C: `parser.ts:552-554/:557/:25/:70`, `proof-no-dead:11-13`, `dist-surface:20-34`, `W2.md:252`; L: `parser.ts:83`, `package.json:47`; plus C-4's `A.W3.md:320` "confirmed them" and C-16's "rollup strips them" mechanism) | **SUSTAINED as INFO** — all substance-preserving; the load-bearing ones re-verified at the bytes by this seat (`:26` state · `:550-551` many-success · `:555` unsafeSetValue · `:638` eof). Recorded so no wave chases a stale anchor |

## Superlatives — 9 banked (L-18 runs both ways)

1. **S-2 (both challenges, both readers, seat-corroborated)** — `json.ts:22-24`'s `indexOf`-gated slice/`JSON.parse` split is the module's best fact: spec-exact on all six escape classes, its only divergence (raw C0) caused by the character class, not the branch. The module contains its own counter-example to PT-PARS-3 and exports the wrong one — the conviction needs no external oracle.
2. **S-1/S-3 (dispatch)** — the head table is real: `Int8Array(128)` built once, reference-interning collapses 8 keys to 6 slots, range keys live. KEEP as internal fixture, demote the export (challenge-C §5 endorsed); the AC-2/AC-3 head-dispatch witness for W2.
3. **S-4 (packrat/diagnostics-inert by construction)** — zero `memoize`/`mergeMemos`/`enableDiagnostics` across the 103 lines; usable law for W1 G-4 (a provably-unarmed first corpus) — with MISS-6's mechanism correction: the packrat CODE ships in the `./utils` chunk; only the ARMING is unreachable. Assert the call-site absence, not chunk separation.
4. **S-5 (dual-entry identity)** — seat-measured `root.jsonParser === utils.jsonParser` TRUE; one instance under both module systems; a build-output property asserted by no gate (C-7's fold names the cure) — and the identity that makes MISS-2's retention shared.
5. **sepBy's checkpoint discipline** (`parser.ts:592-618`, seat-read) — trailing separators reject with zero grammar special-casing; genuine at `parseState`, **inverted through `.parse()`** (MISS-1's caveat is part of the bank).
6. **`many()`'s zero-width guard** (`parser.ts:538`, seat-read) — the load-bearing invariant `csv.ts` leans on without a word; `csvParser.parse("")` terminates because the core defuses the canonical hang.
7. **Backtracking-safe string terminal** (reader-L) — `json.ts:22`'s alternation arms are first-character disjoint under a sticky flag: O(n) accept AND reject on the one terminal most exposed to untrusted input; ReDoS-free without claiming so.
8. **Lazy path touches zero process-global state** (reader-L) — `Parser.lazy` → `createLazyCached`'s closure-local cache, not the module-global WeakMap sibling; what makes S-4's O-8 compliance total. The successor's bounded-back-edge operator preserves this.
9. **The arithmetic closes** (reader-C) — 29 gate-visible names + 5 star-exported = 34 runtime exports, ESM and CJS alike; PT-PARS-4 and PT-PARS-6 are one fact counted two ways, leaving an exact, taste-free invariant for any excision or gate repair.

## MEASURE-AT-OPEN residue (W1 harness cells; no number here is citable as a constant)

1. **Composed depth ceilings** — carry as INTERVALS with JIT-state disclosed: array [1,580 cold – 3,275 warm-best] · object [1,213 – 1,631] · bare-lazy 7,761 (O-15, immutable) · `JSON.parse` ≥ 40,000. W1's cell must measure **cold AND warmed** in separate labeled runs; any G-9 declared depth clears the cold floor with margin. (Seat protocol: fresh process, bisect immediately = cold; 5,000 tier-up parses then re-bisect = warm.)
2. **Reject-path allocation** — the 2 dead arrays + second `ParserState` per failed parse (MISS-5); joins pt-leaf MEASURE-AT-OPEN #3 on the same rig leg (`parser-band.md:117`).
3. **Mint-count baselines** — `quotedString()` 15 · `escapedString()` 8 · `numberParser()` 2 · `.trim()` 2-minus-1-discarded (seat-verified deltas); the allocation leg's per-call-factory exhibit.
4. **CSV rejection census** — 0/500 seed-pinned (seat: LCG 12345 over a 12-char CSV alphabet); re-runnable falsifier for PT-PARS-2(a) against any repaired grammar.
5. **Singleton retention footprint** — heap delta of `state.src` + `state.value` pinned by the module consts after a large parse (seat: 40,001 chars + 20,000 elements live post-return); the O-8 exhibit's magnitude.

## Closing verdict

**DEFECTIVE — RETIRE-THE-SURFACE, with the grammar convicted by its own module.** All three BLOCKERs and ten of thirteen MAJORs are **in the grammar, not the packaging** — no adapter recovers a record separator that was never parsed, four hex digits `.skip()` discarded, or a stack frame that was never bounded — and the zero-consumer measurement (re-derived by this seat in every direction) means the excision precedent (`*Span`, 1.0.0) applies at cost zero. The two challenges survive adjudication almost whole; the two hostile re-reads survive with four rulings between them (RC-1..RC-4, two against each); the seat adds the JIT-variance datum that retires depth point-figures permanently, and the sibling identity guards (pt-index C-M1 · pt-leaf PT-LEAF-3/PT-LEAF-17/L-m12) that keep this record from double-booking three already-adjudicated identities. Routing: **every in-place repair is NO-WAVE-OWNER** (parse-that's tree is outside all X·P wave scopes; the channel is the owner-held O-15/1.1.0 letter); **W2** takes the kill-by-number dead-surface ledger, the `Parser<any>`/G-10 rider, the O-8 lifecycle+retention witnesses, and the numberParser declared-divergence row; **W4** absorbs the two gate-law folds via their sibling identities; the **REFINEMENT FOLD** takes four spec-sharpeners (rejection-leg law · `parseState`-only entry idiom · the boundary invariant that never reads `.value` under `isError` · G-9's cold-composed-ceiling wording); **five cells bank to MEASURE-AT-OPEN**. What the module leaves behind worth keeping is exactly what the banks record: one correct escape strategy, one real dispatch table, one clean checkpoint discipline, and a proof that a 103-line showcase can be packrat-inert by construction.

— end of apotheosis. The two challenge records and both reader returns remain immutable beside this file per E-1/E-3.
