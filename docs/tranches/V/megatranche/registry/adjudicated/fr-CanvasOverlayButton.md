claude-fable-5

# fr-CanvasOverlayButton — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/CanvasOverlayButton.vue` — 25 lines / 595 B / sha256 `35f3db919432e330205348c8fed2132892a5d36f514873a792a77bc5d85b5efd`, **re-taken this session**, exact match to `FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md:31` (`F8-REACH-02`, workflow `N.C39`, HOLD is RED). File read whole by this seat.
**Substrate**: fourier HEAD `cd26c65` (re-pinned; 28 dirty paths, subject NOT among them) · installed `@mkbabb/glass-ui@4.0.0` (80-subpath exports) · producer read ONLY at `git show v7.0.0:` (74-subpath exports; the working tree is 172 dirty paths at `7d9abe08`, `CHANGELOG.md:3` = "8.0.0 (unreleased — accruing)" — re-verified, and load-bearing for one kill). Vue `3.5.38` installed. **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: three challenge files read WHOLE by this seat — challenge-D (design, 17 defects + 4 superlatives, opus-5[1m]), challenge-L (library, 11 defects + 4 superlatives + the §3 R3-7 derivation, opus-5[1m]), challenge-C (consumption, 14 defects + 5 superlatives, opus-5[1m]) — plus the returns of two hostile re-readers, **reader-DU** (design-and-usage: 23 confirmation cells · 10 kills · 6 misses · 6 superlatives) and **reader-LC** (library-and-code: 17 confirmation cells · 9 kills · 5 misses · 7 superlatives), arriving through the orchestration channel.
**Method (incredulity)**: every reader-vs-reader conflict, every kill of a load-bearing cell, and every NEW missed row was re-derived against the trees by this seat with its own instruments before adoption. Two reader cells died under this seat's reads (R-3, R-7); one reader "UNPROVEN-NEEDS-LIVE" filing was overruled as statically decidable and decided (R-1); one identity both readers booked as BLOCKER was demoted to a fold by the registry's own precedent (R-4). Zero browser probes — every ruling below is bytes (read + grep + git + node). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing is agglomeration input (M-25); nothing here stamps a wave.
**This seat's own instruments (all read-only; sole write = this file)**: subject read whole (25); `shasum`/`wc -c`; repo-wide `grep -rn CanvasOverlay` excl. node_modules/docs/.git → **0**; the 66-SFC zero-inbound sweep re-run from scratch (66 counted; zero-inbound = exactly {`visualization/CanvasOverlayButton.vue`, `equation/InfoCard.vue`}); router `component:` records all literal (7) + `defineAsyncComponent` = GalleryView.vue:31-33 string-literals only + vite.config.ts plugins = latexPaperPlugin+vue only; `variant="glass"` census with per-line classification (12 lines / 8 files = **9 live sites / 7 files + 2 prose (subject:5, GalleryCard.vue:253) + 1 dead attribute (subject:18)**); `size="icon"` = 38 lines / 21 files = **36 attribute sites (35 live + subject:19) + 2 prose (subject:5, GalleryCard.vue:254)**; `is-active` census classified mechanically: **30 lines = 11 scoped-CSS rule lines / 7 files + 17 `:class` bindings + 1 component-prop binding (`PaperView.vue:344 :is-active="isActive"`) + 1 prose (subject:7)**; `aria-pressed` = 16 lines, local `[aria-pressed="true"]` rules = exactly BasisSelector.vue:269/274 + GallerySearchBar.vue:218 + GalleryCardModal.vue:241 (NO GalleryCard rule; GalleryCard.vue:135-143 = `variant="ghost"` + `:aria-pressed="isLiked"`); live-site reads FullscreenViewer.vue:105-125 (close action, `Minimize2 class="h-5 w-5"`, no aria-label, directly over the canvas layer), ConvergenceTimeline.vue:55-70+:120-128 (`is-playing` third vocabulary; BOTH svgs `class="size-3"` at :63/:64), EquationView.vue:270-285 (HoverCard→HoverCardTrigger as-child→`<Button variant="glass" size="icon">`, svg `size-[18px]` :277), EasingPicker.vue:8-30 (the menuitemradio doctrine comment ~:11-15); tsconfig.json whole (20 lines, `include` glob at **:19**, zero `vueCompilerOptions`); package.json:8 build gate; vite.config.ts (`"vendor-ui"` at **:52**, keyed on the package ROOT); CollapsibleSection.vue:2 root-barrel import; `npm ls` → **ELSPROBLEMS reproduced this session** (value.js 0.13.0 invalid vs glass-ui peer `^0.10.0 || ^0.11.0`); installed runtime-core.cjs.js `:5011-5015` (`if (opt[0]) { if (isAbsent && !hasDefault) { value = false; }`), shouldCast at :5089, `rawCastValues` + `!hasOwn(castValues, key)` in setFullProps; esm-bundler `:4635` `cloneVNode(root, fallthroughAttrs, false, true)` + `:7965` `ret[key] = toMerge[key]`; reka-ui Primitive.js whole (`inheritAttrs:false`, `as` default "div", `h(props.as, attrs, …)`, no type/name injection); installed glass-ui dist: **components.css `@layer` count = 0**, rule order by byte offset `hover\:bg-(--glass-bg-resting)` **33222** < `active\:bg-(--glass-bg-floating)` **39707** < `aria-pressed\:bg-[color-mix(…)]` **43548** (pattern = the escaped class name; offsets are pattern-relative — state the instrument beside the number), `.active\:scale-\(--scale-press-btn\):active{scale:var(--scale-press-btn)}` present UNLAYERED, the aria-pressed **fallback pair** verbatim (`{background-color:var(--foreground)}` then the `@supports color-mix(in lab…)` arm); index.css ~:201 `@import "./components.css"` under the P9 header + the `@source "../*.js"` BACKSTOP prose read whole; base.css `@layer components` opens at :43 (one real block; a second "@layer" string at :2 is prose), `.tap-squish` :258-269, `:active` :270-272, PRM reset :273-279 (no !important); scale-paper.css `--scale-press-sm: 0.97` / `--scale-press-btn` :40-41, `--focus-ring-width: 2px` + shadow ALREADY `var(--focus-ring-width)` at 4.0.0 :65-67; color-radius.css `--ring: hsl(24 10% 10%)` :102; surfaces.css :46-56 (MUTUALLY-EXCLUSIVE prose + R8-17), `.glass-btn.is-active,[aria-pressed="true"]` :111-116, `.btn-pill { @apply … border-none }` :119-120, `.btn-glass { backdrop-filter }` :182-184; ladder.css `@layer components` :6 + `.glass-wash { … border: 1px solid var(--glass-border-wash) … }` :36-41; glass.css imports ladder :48 BEFORE surfaces :49; a11y-overrides.css PRM `transition-property: opacity,color,background-color,border-color,box-shadow !important` :12-16 (no `scale`), coarse floor `[data-size="icon"]` :115-121 with the "Targets Button size=icon" comment :108-114; offsets-sizing.css `--control-h-md` :151, `--ui-glyph` :177 **with the producer's own comment KEEPING the `:not([class*=size-])` escape "intact (an explicit size-9 still wins)"** :175-176, `--touch-target: 2.75rem` :461; light-dark.css coarse `--ui-scale: 1.5` / `--control-floor` :17-22; dock icon-button.css active cohort :109-115; button-BNDWhAZb.js cva base verbatim (incl. `[&_svg:not([class*=size-])]:size-(--ui-glyph)`), glass arm verbatim, `link` arm verbatim (**no aria-pressed leg**); index.d.ts variant union = **13 members**; 4.0.0 exports = 80 keys, `./toggle-chip` true, `./button` true; producer tag: v7.0.0 Button.vue :15-31 (no `variant`; `ButtonSize` no `"icon"`; iconOnly doc **:24**/prop :25; `type ?? "button"` at **:55**; `data-control-target` at **:92**), `git show v7.0.0:src/components/button/styles.css | grep -c aria-pressed` → **0**, tag-wide `aria-pressed` in `*.css` = 15 lines = 3 prose + 12 rules (accessibility.css:13,34 media-gated; material.css:447; 7 dock + 2 tabs), material.css cohort at **:428-452** (working tree :352 begins `.glass-wash` — `.glass-material` struck at HEAD), v7 exports = **74 keys, `./toggle-chip` FALSE**, `./button`/`./chip`/`./switch`/`./toggle-group` true, responsive.css:3-8 coarse floor keyed `[data-control-target]`; archaeology `git show be24948^:…CanvasOverlayButton.vue` = 14 lines, `<button class="glass-btn" :class="{ 'is-active': active }">`, **no docblock, no aria-pressed**; `git log --follow` 5 commits (9146d3f 03-14 · a10174f 03-15 · 2f53d5d 03-16 · be24948 05-26 · 262c3d0 06-02); megatranche docs: lane-frontend.md grep `emphasis|iconOnly|variant="glass"|size="icon"|ButtonSize` → **0** (break table :470-481 = metric-badge/hover-card/hover-popover/dock members/ToastVariant/lucide/3 peer floors; :508 budget verbatim; :101 inventory row verbatim; :213 does count "35 @mkbabb/glass-ui/button" imports with no break row), formation/fourier/CENSUS-2026-08-03.md same grep → **0**; lane-fourier-r3-r6.md R3-7 row (TRUE/ADOPT-AS-FACT, `66/64/Unmounted 2`, never named); **F8-REACH-01 (`N.C11`, InfoCard.vue, `b2c2e718…`/1,516, HOLD is RED) sits one row above F8-REACH-02 in the same delta table**; `git status --porcelain web/src/components/equation/InfoCard.vue` → ` M`, diff = 1+/1− (`:amount=` → `:value=`); fourier DELETE orders verbatim at W3-button-ledger.md:93, M.md:141, M-bump-migration.md:56, A8-no-legacy-sweep.md:29; registry identity greps (adjudicated/*.md) for ELSPROBLEMS / btn-glass / CanvasControlsDock / unlayered / root-font.

## Scoreboard

**28 adjudicated roster rows — 2 BLOCKER · 9 MAJOR · 10 MINOR · 7 INFO** — covering every corpus row that survived, with **1 corpus BLOCKER (C-2) demoted to a FOLD-BY-REFERENCE** on the registry's own banked precedent and **1 fleet identity (the Button prop-axis census amendment) FOLDED to fr-BasisSelector M-7**, never re-booked. **15 killed-claims register entries, all SUSTAINED** — including two kills of reader cells by this seat and one overruling of a reader's "needs-live" filing. **10 missed finds ADMITTED** (6 reader-DU · 5 reader-LC, two pairs recomposing into one row each), three of them producer-class. **7 superlatives sustained with provenance.** **7 ruled disagreements.**

Blocker-weight survivors: **FR-COB-1** (a five-times-ordered, never-executed deletion of a zero-consumer file, with the skip un-minuted on the branch F.W0 must land-or-abandon — the maximal consumption defect) and **FR-COB-2** (the glass-ui 4→7 hop deletes BOTH props this file passes; the break axis is absent from both census instruments; the `variant` limb is SILENT under fourier's strictTemplates-less vue-tsc; and the WCAG 2.5.5 coarse floor re-keys across the hop so a mechanical migration silently drops it on all 35 live `size="icon"` sites).

The adjudicated disposition is the readers' convergent one, sharpened: **LIFT-THEN-DELETE** — the dead file is the only place on fourier's canvas-control surface where the producer's toggle contract was read correctly, and a bare `git rm` converts a paper defect into a live a11y regression.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **Is the hover/active/pressed cascade decidable without a build?** DU decided it from shipped bytes; LC filed its own missed-row half as "UNPROVEN-NEEDS-LIVE (build the sheet and read the order)" | decided: pressed bg WINS over hover/active by source order in the shipped sheet | "decidable in one line … but not from source alone; filed for SS-13" | **DU wins; LC's needs-live filing OVERRULED and decided.** | glass-ui ships the compiled utilities **pre-built and UNLAYERED** in `dist/styles/components.css` (`grep -c "@layer"` → **0**), `@import`-ed unconditionally at index.css ~:201 under the P9 header ("shipped build-independently so a bare consumer paints them"), which fourier reaches at style.css:3. Order re-measured by this seat: hover **33222** < active **39707** < aria-pressed **43548** — equal (0,2,0) specificity, later wins. Any twin fourier's own JIT mints lands in `@layer utilities` and **loses to the unlayered sheet unconditionally**, so the consumer build cannot change the outcome — only the CSS-size budget (residue kept, SS-13 #7). |
| R-2 | **What suppresses hover feedback on a pressed control** — DU's missed row said "hover changes ONLY border-color"; LC's missed row proved the border arms paint NOTHING on any `<Button>` | pressed-bg-wins mechanism (correct); border-limb cell wrong | `border-none` chain (correct); order half filed unproven | **COMPOSED — each reader corrects the other's half; the composite is worse than either filed.** | LC's chain verified at the bytes: cva base always emits `btn-pill`; surfaces.css:119-120 `.btn-pill { @apply … border-none }` (Tailwind v4 `border-none` = `border-style: none`, width untouched); ladder.css:36-41 `.glass-wash { border: 1px solid var(--glass-border-wash) }`; BOTH in `@layer components` at (0,1,0); glass.css imports ladder **:48** before surfaces **:49** → `border-style: none` wins on source order; `hover:border-(--glass-border-resting)` / `active:border-(--glass-border-floating)` are border-COLOR utilities over a zero-style box → **zero pixels, fleet-wide**. Composite (FR-COB-7): on a pressed glass Button, hover paints **nothing at all** and pointer-down changes only `scale`. DU's cell "hover changes ONLY border-color" is corrected (K-14); DU's order derivation stands (R-1). |
| R-3 | **The census arithmetic** — `is-active` (DU: "10 selectors / 7 files"; LC: "12 scoped CSS lines … 11 :class bindings", summing to 24 of its own 30) and the `variant="glass"` decomposition (DU: "9 real attribute sites, 3 prose"; LC: 9 live + 2 prose + 1 dead) | 10 CSS / 7 files; 3 prose | 12 CSS; 11 bindings; LC's variant split exact | **BOTH corrected on `is-active`; LC exact on `variant`.** | Mechanical classification by this seat: `is-active` = **30 lines = 11 scoped-CSS rule lines / 7 files** (MobileFloatingToc 371,376 · PaperSidebar 240,251 · EasingPicker 84,95 · GallerySearchBar 159 · FunctionInput 244 · EquationModeToggle 63 · AppHeader 340,345) **+ 17 `:class` bindings + 1 prop binding (PaperView.vue:344) + 1 prose (subject:7)**. DU undercounts CSS by one, LC overcounts CSS by one AND undercounts bindings by six, and LC's own row is internally inconsistent (12+11+1 ≠ 30). `variant="glass"`: 12 lines = **9 live / 7 files + 2 prose + 1 dead attribute (subject:18)** — LC exact; DU's "3 prose" mis-classifies the subject's dead attribute as prose (K-10). Substance unharmed everywhere: every CSS rule is compound-scoped, none reaches a `<Button>`, and a mechanical `.is-active` sweep would corrupt the PaperView prop. |
| R-4 | **C-2's booking altitude** (glass-ui peer-INVALID; `npm ls` ELSPROBLEMS) — both readers confirmed it as BLOCKER | BLOCKER confirmed | BLOCKER confirmed | **BOTH OVERRULED on booking — FOLD BY REFERENCE (identity guard).** | The identity is **already banked**: fr-AnimationControls **C-1** ≡ fr-BasisCanvas **C-8** (census §1 pins + the G33 gate), with the standing ruling "component-altitude INFO, never re-book per component" (fr-AnimationControls R-14; the same demotion was applied to fr-BasisSelector's reader). This seat reproduced ELSPROBLEMS live this session (fourth independent reproduction; value.js 0.13.0 invalid vs `^0.10.0 \|\| ^0.11.0`) and re-verified C-2's honest scoping (the `./button` chunk closure has zero value.js reach). The fact is true, the row is not this component's to book. |
| R-5 | **C-S2's scope** ("only CanvasOverlayButton relies on the upstream cva paint; the other aria-pressed glass-Button toggles each have hand-rolled local CSS") | not addressed at this cell | KILLED on GalleryCard | **LC's kill SUSTAINED — and it narrows both challenges' S-1/S-2 superlatives.** | GalleryCard.vue:135-143 is `variant="ghost" … :aria-pressed="isLiked"` and the tree's complete local-rule set is BasisSelector:269/274 + GallerySearchBar:218 + GalleryCardModal:241 — **no GalleryCard rule** (this seat's grep). The `ghost` arm carries the same `aria-pressed:bg-[…]` leg, so a live, mounted control rides the upstream paint exactly as the dead one does. Both challenges' "only/the one file" superlatives survive **only** under the narrow reading "the canvas-control-dock surface" (which is what L-10 actually measures — 7 dock toggles + ConvergenceTimeline, zero ARIA). Recorded in the roster at that scope and no wider. |
| R-6 | **The second unmounted workflow's disposition** (LC filed "the disposition leaves InfoCard.vue undisposed" as a missed MAJOR) | named it via L §3 adoption | missed MAJOR as filed | **ADMITTED WITH CORRECTION — this seat's own catch: the disposition demand already EXISTS.** | **F8-REACH-01** (`N.C11`, `equation/InfoCard.vue`, sha `b2c2e718…`/1,516, "exact KEEP_WITH_MOUNT, ISOLATED_HARNESS, or DELETE; HOLD is RED") sits **one row above F8-REACH-02 in the same table** — uncited by the L-challenge's §3, by C's §0 corpus fold, and by both readers. So InfoCard is not "undisposed"; it is disposed-in-demand and unexecuted, exactly like the subject. The live sting survives whole: the frozen M.W1a tree is spending a `:amount=`→`:value=` edit on it **right now** (` M`, 1+/1−, re-verified), and lane-frontend's `./metric-badge` budget ("7 imports / 6 files", listing InfoCard.vue:4) is 6 files but only **5 live**. FR-COB-10 books both with the correction. |
| R-7 | **D-16's mechanism cell** — LC confirmed the challenge's mechanism verbatim ("JIT-minted into @layer utilities, which Tailwind v4 orders after components"); DU asserted a different, stronger one | unlayered pre-compiled utility beats every layer | @layer utilities > @layer components, as the challenge filed | **DU's mechanism is the operative one; LC's cell is challenge-inherited.** | The competing `active:scale-(--scale-press-btn)` utility is **shipped pre-compiled in the unlayered components.css** (rule extracted verbatim by this seat), so it defeats the `@layer components` PRM reset (base.css:273-279, no !important) **regardless of the consumer's Tailwind version, layer order, or whether the JIT runs at all** — version-independent where the challenge's mechanism is build-contingent. Verdict (MINOR — the PRM transition allow-list omits `scale`, so the survivor is instantaneous, not animated) is unanimous and unchanged. Recorded as a citation-inheritance-shaped event per the ColorSpaceSelector DISSENT-1 / pt-leaf ruling-3 precedent: the cell repeats the challenge where the bytes say more. LC's generalization instinct was right anyway — see FR-COB-8, the producer class DU alone named. |

**Adjudicator's corrections register (against this corpus's readers)**: (1) DU's "hover changes ONLY border-color" cell — corrected by LC's border-none chain, verified here (R-2/K-14); (2) LC's `is-active` arithmetic (12/11, self-inconsistent at 24-of-30) and DU's "10 selectors" — both corrected to 11/17/1/1 (R-3); (3) DU's "3 prose" variant-glass split — corrected (R-3); (4) both readers' C-2 BLOCKER booking — overruled by identity guard (R-4); (5) LC's D-16 mechanism cell — challenge-inherited, corrected (R-7); (6) LC's needs-live filing on the cascade order — overruled, decided from shipped bytes (R-1); (7) both readers and all three challenges failed to cite **F8-REACH-01** one row above the row they all cite (R-6). Instrument-unit note (pt-leaf K7's law): DU's byte offsets (33216/39716/43542) and this seat's (33222/39707/43548) differ by pattern anchor, order identical; DU's "16 aria-pressed occurrences" vs this seat's 12 matching lines in the chunk is occurrences-vs-lines — state the counting unit beside the pattern.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law (census lane taxonomy): **F.W0** substrate/land-or-abandon · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5–W8** CRUD/provenance union · **F.W9/W10** close · **NO-WAVE-OWNER** = producer (glass-ui BH relay) or spec-authoring input for SS-3/SS-4. Line anchors are HEAD `cd26c65` / installed 4.0.0 / tag v7.0.0; re-resolve before any cure.

### BLOCKER

- **FR-COB-1 = D-2 / L-1 / C-1 (+ F8-REACH-02) — BLOCKER.** Dead module: zero consumers, unreachable by all four resolution channels (static grep **0** repo-wide excl. docs; dynamic = 3 string-literal `defineAsyncComponent` at GalleryView.vue:31-33; router = 7 literal lazy records; no unplugin-vue-components), under **five** verbatim DELETE orders never executed (W3-button-ledger.md:93 · M.md:141 · M-design-language.md:70 · M-bump-migration.md:56/:199 STEP 2i · A8-no-legacy-sweep.md:29, four re-read verbatim this session) with the skip self-reported (partial-prior-run.json:207-211) and **un-minuted on the 28-path frozen M.W1a tree** (subject not among the dirty paths). Byte identity to `F8-REACH-02` re-taken: `35f3db91…5efd` / 595. Zero-inbound membership re-derived independently (the 66-SFC sweep). Orphaned at `2f53d5d` (2026-03-16, "CanvasOverlayButton no longer imported") = **145 days at this adjudication**, two post-mortem maintenance passes (`be24948`, `262c3d0`). L-1's conditional preserved: if F.W0 rules ABANDON on the M.W1a tree wholesale, the governance limb falls to MAJOR; the deadness does not. **ADJUDICATED → F.W0**: minute the STEP-2i divergence, execute `git rm`, and rule **F8-REACH-01 and F8-REACH-02 in one breath** (R-6) — with FR-COB-17's lift landing before or with the rm.
- **FR-COB-2 = D-1 / L-2 / C-11 / C-12 — BLOCKER (component instance; FLEET IDENTITY FOLDED → fr-BasisSelector M-7, never re-booked).** glass-ui 7.0.0 deletes **both** props this file passes: `v7.0.0:Button.vue:15-31` has no `variant` key and `ButtonSize = Extract<Size,"xs"|"sm"|"md"|"lg">` (no `"icon"`); `index.ts` exports no shim. The file gates the build anyway: tsconfig include is a glob (**:19**) and `build = vue-tsc -b && vite build` (package.json:8) — reachability is not the selector. The axis is absent from BOTH census instruments (this seat's grep: lane-frontend.md → 0 hits, CENSUS-2026-08-03.md → 0 hits; the :470-481 break table's only Button-adjacent row is DockIconButton; :508 budgets the hop without it — while :213 counts 35 `./button` importers with no break row). Two limbs NEW beyond the banked M-7 identity, both verified here: **(a) the `variant` limb is SILENT** — no `vueCompilerOptions`/`strictTemplates` anywhere in fourier's tsconfig (whole-file read), Volar defaults `strictTemplates:false`, so `variant="glass"` falls to `$attrs` as a junk DOM attribute with zero diagnostic while the button silently re-tiers to defaults (`glassMaterial` = tone neutral + emphasis primary|secondary, both defaults — it keeps *looking* glassy); `size="icon"` is by contrast a hard TS2322. **(b) the WCAG 2.5.5 coarse floor RE-KEYS across the hop** — 4.0.0 keys `[data-size="icon"]` (a11y-overrides.css:115-121, comment naming Button by name; the chunk reflects `data-size`), v7.0.0 keys `[data-control-target]` (responsive.css:3-8), which Button.vue:92 sets from `iconOnly` — so a mechanical `size="icon"`→`size="md"` sweep **silently drops the 44px floor on all 35 live sites**; only `size="md" iconOnly` preserves it. Corrected budget for the census (K-12): **9 live `variant="glass"` sites / 7 files** and **36 real `size="icon"` sites (35 live)** — not 12/8 and not 38. **ADJUDICATED → F.W1** (fleet: fold into the M-7 prop-level-diff re-derivation with the two limbs and corrected budget) **+ F.W0** (this file is the only member whose complete cure is deletion).

### FOLDED BY REFERENCE (identity guard — never re-booked)

- **C-2 (peer-INVALID install, ELSPROBLEMS)** **= fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8** — census §1 pins + G33, component-altitude INFO by standing ruling (R-4). Reproduced live this session; the `./button` subgraph's zero value.js reach re-verified (C-2's own honest scoping).
- **The Button prop-value-axis census amendment (fleet scope)** **= fr-BasisSelector M-7** (F.W1 SCOPE AMENDMENT: prop-level diff over every surviving subpath). FR-COB-2 contributes this component's instance, the silent/loud asymmetry limb, the touch-floor re-key limb, and the corrected 9/36 budget to that banked identity.

### MAJOR

| id | merged ids | claim (adjudicated) | proof anchor (this seat's) | terminal disposition |
|---|---|---|---|---|
| **FR-COB-3** | D-3 / L-3 / C-3 | `.is-active` (line 21) is **inert** on this element; the mechanism is the `btn-glass`/`glass-btn` transposition. The only button-family rule is `.glass-btn.is-active,[aria-pressed="true"]` (surfaces.css:111-116); the cva emits `btn-glass` (a backdrop re-point, :182-184), never `glass-btn`; every other `.is-active` rule is dock/layer-scoped; fourier's 11 local rules are all compound-scoped (R-3 census); the subject has no `<style>`. Re-adding `.glass-btn` is NOT the cure: surfaces.css:46-56 declares it and `.btn-pill` MUTUALLY EXCLUSIVE (the R8-17 clipping defect). Archaeology verified byte-for-byte: `be24948^` = `<button class="glass-btn" :class="{'is-active': active}">`, no docblock, **no aria-pressed** — one commit destroyed the class's paint hook, added the correct ARIA binding, and authored the sentence calling the dead class canonical | surfaces.css :46-56/:111-116/:119-120/:182-184; `grep -c glass-btn` chunk → 0; `git show be24948^` | dies with the file (**F.W0** rm). The fourier-wide vocabulary consolidation (`.is-active`/`.is-active-sub`/`.is-playing`/`.liked`/the `:is-active` PROP at PaperView.vue:344 — five spellings) → **F.W3/W4**, with the sweep-law rider: never key the sweep on a bare `is-active` grep (it corrupts the prop) |
| **FR-COB-4** | C-4 ∘ D-7/C-8 survivors | `aria-pressed="false"` is emitted on **every** instance where the prop is omitted (Boolean cast: runtime-core :5011-5015, shouldCast :5089 — omission → `false`, never `undefined`) — a WCAG 4.1.2 role misstatement for any action use, hard-coded one level above every future call site while the tree's own doctrine (EasingPicker ~:11-15) reasons correctly about the exact hazard. Scope settled by this seat: the tri-state IS reachable by an explicit `:active="undefined"` (setFullProps puts the key in `rawCastValues` → `isAbsent` false → value stays `undefined` → attribute removed), so D-7's narrow store-field scenario survives as the explicit-binding limb and C-8/L-11 die (K-1/K-2) | runtime-core.cjs.js :4955-4984/:5011-5015; compiled props `{active:{type:Boolean,required:false}}`; Primitive.js (`inheritAttrs:false`, attrs → host) | dies with the file; the LAW carries to **F.W3/W4**: the lifted `aria-pressed` idiom (FR-COB-17) binds ONLY on true toggles — never on the FullscreenViewer close action (K-11) |
| **FR-COB-5** | D-5 / L-7 / C-5 | Icon-only-button wrapper with **no accessible-name discipline** anywhere in its contract (whole API = one boolean; no label prop/default/defineSlots/warning; Button + Primitive inject nothing — read whole). The producer names the delegated duty at v7 (`iconOnly` doc **:24**: "accessibly named icon command"). Already shipping at the exact sites the wrapper was written for: FullscreenViewer.vue:110-112 (`Minimize2`, no aria-label, no title) and ConvergenceTimeline.vue:61 (nameless play/pause with no ARIA state — a named-less toggle announces a state change of nothing) | subject :11-13/:23; Button.vue.d.ts; Primitive.js; live reads | **F.W3/W4** — the icon-command naming sweep (36 `size="icon"` sites are the denominator); the wrapper's own instance dies with the file |
| **FR-COB-6** | D-15 | The uplift **removes the pressed paint**: `v7.0.0:src/components/button/styles.css` has **0** `aria-pressed`; the tag-wide enumeration (15 lines = 3 prose + 12 rules, re-run by this seat) leaves only material.css:447 reaching a default-register Button, and its sole declaration is `--specular-intensity: 0.16` on a `::before` lens — not background/border/ink. Every surviving `<Button :aria-pressed>` toggle keeps semantics and loses its background delta at F.W1. Direction not uniformly bad: prefers-contrast/forced-colors users GAIN a 2px border (accessibility.css:9-43) | `git show v7.0.0:` greps + material.css **:428-452** | **F.W1** rider: author an explicit pressed treatment or re-home the toggles onto surfaces that retain pressed paint at 7 (dock/tabs families) — decided when the M-7 prop-diff re-derivation runs. Producer question (was the removal intentional?) rides the BH relay (FR-COB-28). Residue: SS-13 #3 |
| **FR-COB-7** | D-6-rescoped ∘ DU-miss-1 ∘ LC-miss-1 | **The composed cascade row (R-1/R-2), producer-owned.** (a) The pressed register is single-channel (background only). (b) The `glass` arm's hover/active **border arms are dead on every `<Button>` fleet-wide** — `.btn-pill`'s `border-none` beats `.glass-wash`'s `border: 1px solid` on same-layer source order (ladder :48 < surfaces :49), and the cva's border utilities are color-only over a zero-style box. (c) The pressed background **wins over hover and active** by unlayered source order (33222 < 39707 < 43548) — so a toggled-ON control gives **zero visible feedback** to hover and only `scale` to press: the user reaching to turn a control OFF — the moment most needing confirmation — gets the least. D-6's "hover out-shouts pressed" is dead in both directions (K-7); the channel-count comparison for the OFF state survives | this seat's cascade reads (R-1/R-2 cells) | **NO-WAVE-OWNER (producer)** — glass-ui BH relay, top of the FR-COB-28 list. Consumer awareness note → **F.W3/W4** (any fourier pressed-state work must not assume a border channel exists) |
| **FR-COB-8** | DU-miss-3 (producer class) | glass-ui ships its pre-compiled utilities **UNLAYERED** (components.css, 0 `@layer`, imported unconditionally at index.css ~:201), so every utility there structurally outranks glass-ui's **own** `@layer components` accessibility resets — a producer defect CLASS of which FR-COB-12 (PRM squish) is one instance; any `@layer components` PRM/forced-colors/prefers-contrast reset with an unlayered utility twin is defeated the same way. Independently corroborated: fr-AppHeader's instruments already observed the zero-`@layer` sheet (different rows) | components.css + base.css :43/:270-279 | **NO-WAVE-OWNER (producer)** — BH relay as a CLASS (emit the P9 sheet into a layer, or hoist the a11y resets out of layers); consumer must NOT patch locally (the S-4 discipline) |
| **FR-COB-9** | D-4 / C-9 | The docblock is false on both halves, self-contradictory across two consecutive lines ("legacy" :6 vs "matching the glass-ui canon" :7), stale-stamped ("A.W3.b" predating the 3.1→4.0 hop; HEAD is `cd26c65` tranche-N), and imprecise ("Forwards every attr/listener" — `active` is consumed, `variant`/`size` are set). Authored by `be24948` in the same diff that falsified it (FR-COB-3's archaeology). Dead code that *asserts a design rule* is a teaching artifact aimed at the 9 live sites | subject :2-8; `git show be24948^` | dies with the file (**F.W0**); the register lesson (deadness column for design-asserting inventory rows — lane-frontend.md:101 carries this file as a live role) → **F.W0** substrate correction |
| **FR-COB-10** | L §3 / R3-7 ∘ LC-miss-2 (corrected R-6) | The two anonymous unmounted workflows of adjudicated intake row **R3-7** (TRUE/ADOPT-AS-FACT, `66/64/Unmounted 2`, never named) are NAMED and re-derived a third time by this seat's independent sweep: **CanvasOverlayButton.vue + equation/InfoCard.vue** — and the second is accruing cost NOW (uncommitted `:amount=`→`:value=` edit inside the frozen M.W1a tree, re-verified ` M` 1+/1−). Refinements: lane-frontend's `./metric-badge` budget "7 imports / 6 files" is **5 live files + 1 unmountable**; and (this seat's R-6 catch) **F8-REACH-01 already demands the InfoCard ruling** one row above F8-REACH-02 — the corpus and both readers cited one row of a two-row table | the 66-SFC sweep; F8 delta table; git status/diff | **F.W0** (rule both REACH rows in one breath) + **F.W1** (metric-badge budget correction rides the census amendment) |
| **FR-COB-11** | C §2 S-2 / L §4 (corpus contradiction) | The M-run's raw-findings.json:2968 (".dock-icon-button.is-active has NO backing paint") is **stale against the installed 4.0.0**: dock icon-button.css:109-115 paints five declarations off the `--dock-active-*` cohort (+ material.css specular + touch-floor arms) — authored the day before the 4.0.0 install cured it. Load-bearing consequence: the 7 live dock toggles are a pure **a11y** defect (no ARIA), not a paint defect — invisible to the eye, visible to AT and to the v7 prefers-contrast/forced-colors arms that key on ARIA exclusively. Also: the DOCK-ACTIVE producer ask was answered upstream (v7 material-wide cohort) — record satisfied, do not re-send | icon-button.css :109-115 (re-read); v7 accessibility.css/material.css | **F.W3/W4** — the corpus correction rides with FR-COB-17's lift; the DOCK-ACTIVE ask closure → the BH-relay note (FR-COB-28) |

### MINOR

| id | merged ids | claim (adjudicated, compressed) | disposition |
|---|---|---|---|
| **FR-COB-12** | D-16 ∘ R-7 | The press squish survives PRM — mechanism corrected to DU's: the shipped **unlayered** `active:scale-(--scale-press-btn)` rule (verbatim in components.css; `--scale-press-btn` → 0.97) defeats the `@layer components` reset (base.css:273-279) unconditionally, version-independently. MINOR stands: the PRM `transition-property` allow-list (a11y-overrides :12-16) omits `scale`, so the survivor is instantaneous — a design-consistency defect against base.css's own stated intent, not WCAG 2.3.3 | instance of FR-COB-8's class → BH relay; SS-13 #4 one-line confirmation |
| **FR-COB-13** | D-9 / L-5 / C-6 | No `type="button"` pinned; installed Button supplies no fallback (`type: u.type`, no `?? "button"`; contrast `as:{default:"button"}`), Primitive injects none; latent only because fourier has zero `<form>`s (grep 0). Cured upstream at v7 Button.vue **:55** | latent-MINOR; dies with the file; the v7 cure is an under-booked HOP BENEFIT → **F.W1** census credit |
| **FR-COB-14** | L-4 / C-14 / D-10 | Every pinned attribute is defeasible by fallthrough (esm-bundler :4635 cloneVNode + :7965 last-writer-wins; no defineOptions/inheritAttrs in the compiled output); `class` merges while the rest override, so a caller can force `aria-pressed="false"` + `.is-active` on one element with no type error. D-10's "UNPROVEN-NEEDS-LIVE" flag STRUCK — fully decidable from the installed runtime, decided by both readers and re-verified here | dies with the file; the wrapper-law (pin via `inheritAttrs:false` + pre-placed `v-bind="$attrs"`, or don't wrap) → SS-3/SS-4 spec input, NO-WAVE-OWNER |
| **FR-COB-15** | L-9 / C-10 / D-8 / L-8 | Goldilocks failure: unified **zero of 9** live sites; the single natural consumer (FullscreenViewer.vue:110, verbatim a canvas-overlay icon button over the canvas layer) hand-rolls it; the name/filing assert a role held by nothing since `2f53d5d` (the real canvas chrome is dock-family, whose paint this host can never carry); the authoring wave's own ledger ruled "`<Button variant='glass' size='icon'>` IS the surface" (W3-button-ledger.md:93, verbatim) | dies with the file (**F.W0**); the ledger ruling is the standing wrapper policy → **F.W3/W4** |
| **FR-COB-16** | D-11 | No busy/loading register; the tree already conflates in-flight with on (CanvasControlsDock.vue:71 `is-active: publishing`, verified). Installed Button has no `loading`; v7 ships it (`loading` → data-loading + aria-busy + activation suppression) — an IMPROVE row | **F.W1** hop-benefit credit; the dock conflation instance rides FR-COB-17's lift list |
| **FR-COB-17** | L-10 ∘ S-1/S-2 lift | A bare `git rm` deletes the tree's only correct reading of the producer's toggle contract on the canvas-control surface: the 7 dock toggles (CanvasControlsDock :54/:59/:71/:77/:87, EditorControlsDock :143/:148) + ConvergenceTimeline:61 carry class-vocabulary state with **zero ARIA**, and the v7 prefers-contrast/forced-colors arms key on ARIA exclusively. The dock toggles DO paint (FR-COB-11), so the defect is AT-only. Cure = the 8-site `:aria-pressed` lift **in the same commit as the rm** — L's list, NOT C's (K-11: C's version adds `aria-pressed` to a one-shot close action and miscounts its own list) | **F.W3/W4** (the lift rider, sequenced with F.W0's rm); toggle-vs-action law from FR-COB-4 governs the lift |
| **FR-COB-18** | DU-miss-2 (corrected) | The glyph-scale contract has a class-name escape hatch LIVE in the tree: the cva sizes slot glyphs via `[&_svg:not([class*=size-])]:size-(--ui-glyph)`, so any child whose class contains `size-` opts out of the coarse `--ui-scale` growth — ConvergenceTimeline :63/:64 (`size-3` → 12px in a 60px coarse box = **0.20**, half the house 0.40) and EquationView :277 (`size-[18px]`), while `h-5 w-5` children DO scale ((0,2,1) beats (0,1,0)). **Adjudicator correction**: the hatch is a DOCUMENTED producer contract (offsets-sizing :175-176 "KEEPING the `:not([class*=size-])` … escape intact — an explicit size-9 still wins"), so this is consumer-side misuse + the wrapper's absent slot contract, not a producer accident | **F.W3/W4** (glyph-register sweep row: size-* children on icon Buttons); qualifies FR-COB-22's 0.40 |
| **FR-COB-19** | DU-miss-5 | EquationView.vue:274-283 stacks a counted census break (`./hover-card` removed → `<Popover>`, folding at 5.0.0) and an uncounted one (the inner Button's `variant`/`size`, FR-COB-2) on ONE call site whose `as-child` merge semantics change in the same commit — the concrete single-file instance of the census gap, and one a member-wise export-map re-run would still miss (both members survive their subpaths) | **F.W1** sequencing note on the M-7 amendment |
| **FR-COB-20** | C-7 | The interaction contract is entirely implicit — no defineEmits/defineOptions/defineExpose, no test anywhere (`web/e2e` 8 specs, zero references); the mechanism by which "never exercised" becomes "cannot be regression-detected" | dies with the file; the no-test-for-wrappers observation → SS-3/SS-4 input |
| **FR-COB-21** | C-13 (evidence half only) | Installed 4.0.0 ships `ToggleChip` (`./toggle-chip` in the 80-key map; reka Toggle root, real v-model, `variant="cell"` square icon cards) — a strictly stronger primitive for the pressed-register job. The DISPOSITION half is dead (K-4): `./toggle-chip` is **absent from the 74-key v7 exports** — the successor must be re-derived at the tag (survivors: `./chip`, `./switch`, `./toggle-group`, dock) | dies with the file; successor re-derivation → **F.W1** (rides M-7); the "cure adjudicated separately from defect" law re-affirmed |

### INFO

| id | merged ids | claim | disposition |
|---|---|---|---|
| **FR-COB-22** | D-12 | Glyph-to-box ratio 0.40, scale-invariant by construction (`--ui-glyph` 1rem / `--control-h-md` 2.5rem, both × `--ui-scale`); the invariance is deliberate and credited; the constant is thin against the 0.50–0.60 icon-button register — and per FR-COB-18, 0.40 is not what two live sites even render | **F.W3/W4** register note; producer constant question rides the BH relay |
| **FR-COB-23** | D-13 | Focus-ring contrast ≈**1.94:1** against `--background` (both readers re-derived independently, concordant to 2 decimals; formula/tokens verified here: 30% of `--ring` hsl(24 10% 10%) over `--neutral-0`), below 1.4.11's 3:1; unchanged in kind at v7. Correction sustained: the 4.0.0 token ALREADY reads `var(--focus-ring-width)` — D-13's "7.0.0 merely renames" mischaracterises 4.0.0. Applicability over a canvas backdrop is SS-13 #2. Forced-colors arm passes (outline 2px Highlight) | **NO-WAVE-OWNER (producer)** — BH relay member; inherited, not authored here |
| **FR-COB-24** | D-14 | fourier's root-font fork (style.css :40-49, `html{font-size:1.125rem}` under 768px, re-read whole) inflates every rem-denominated glass-ui control token 12.5% and compounds with coarse `--ui-scale: 1.5` → 67.5px, not the 60px the token author computed. Shared-substrate INFO; fr-AnimationControls/fr-AppHeader instruments observed the fork without booking it — one identity, first booked here | **F.W1** token-parity note (the 7.0 ladder is re-tuned against a 16px root) |
| **FR-COB-25** | D-17 | Dated provenance, verified whole: 5 commits; the name was earned until `2f53d5d` (2026-03-16) deleted the corner-geometry role; `be24948` (2026-05-26) is the single commit that broke the class, added the ARIA binding, and wrote the false canon; `262c3d0` (2026-06-02) the subpath re-point. Two maintenance passes on a corpse = the empirical price of five skipped DELETE orders | folds into FR-COB-1's F.W0 minute |
| **FR-COB-26** | DU-miss-4 | The pressed/hover backgrounds ship a machine-generated non-color-mix fallback that paints **solid `var(--foreground)`** under `text-foreground` ink (verbatim pair extracted: `{background-color:var(--foreground)}` then the `@supports (color:color-mix(in lab…))` upgrade) — foreground-on-foreground, 1:1, on any engine without lab color-mix. Every current baseline engine supports it; no producer review has seen the generated arm | **NO-WAVE-OWNER (producer)** — BH relay member |
| **FR-COB-27** | LC-miss-5 | C-S1's fourth credit (leaf import "preserves the `vendor-ui` manualChunks split") is unearned: the entry is keyed on the package ROOT (`vite.config.ts:52`, off-by-one from C's :53-57) and the split is indifferent to leaf-vs-barrel discipline; the actual barrel offender is CollapsibleSection.vue:2. C-S1's axes 1–3 (bare leaf specifier, resolvable at runtime + types, survives the hop) stand | struck credit recorded; no route |
| **FR-COB-28** | DU-miss-6 (process) | The corpus's producer-owned findings carry ONE relay instruction (D-16's) where the standing formation invariant requires all. **The consolidated glass-ui BH relay list from this record**: (1) FR-COB-7 — dead border arms fleet-wide + pressed-suppresses-feedback composition; (2) FR-COB-8 — the unlayered-P9-defeats-layered-a11y-resets CLASS (FR-COB-12 one instance); (3) FR-COB-6 — v7 pressed-paint removal: intentional?; (4) FR-COB-23 — the 1.94:1 focus ring; (5) FR-COB-26 — the generated color-mix fallback; (6) the `btn-glass`/`glass-btn` near-collision naming hazard (FR-COB-3's mechanism — two utilities one letter-order apart in one file); (7) record DOCK-ACTIVE satisfied upstream, do not re-send (FR-COB-11) | **NO-WAVE-OWNER** — one relay letter, seven items, sent at F.W0/F.W1 open by whoever holds the mail seat; this record is the packet |

## Killed-claims register — 15 entries, all SUSTAINED

| # | killed claim | adjudication |
|---|---|---|
| K-1 | **L-11** — "omitted `active` yields a third ARIA state; `<CanvasOverlayButton>` vs `:active='false'` expose different roles" | **KILL SUSTAINED at the bytes** (both readers convergent; re-derived): Boolean cast at :5011-5015 makes omission `false`, never `undefined` — the two spellings render identically. The third state exists only via explicit `:active="undefined"`, the distinction L-11 never drew |
| K-2 | **C-8** — "explicit `:active='undefined'` also coerces to false; the tri-state does not exist" | **KILL SUSTAINED at the bytes**: setFullProps writes the present key into `rawCastValues` → `isAbsent` false → no cast → `undefined` survives → attribute removed. The consumer CAN reach undefined by exactly the binding C-8 says fails; C-8 and C-4 are the two halves of one defect (FR-COB-4) |
| K-3 | **D-7's premise** — "optional prop with no default yields `undefined`" | **PARTIAL REFUTATION SUSTAINED**: false for the dominant (omission) case; the explicit-undefined scenario and the `withDefaults` cure survive inside FR-COB-4 |
| K-4 | **C-13's disposition** — "DELETE-with-named-successor `@mkbabb/glass-ui/toggle-chip`" | **KILL SUSTAINED**: `'./toggle-chip' in v7.0.0 exports` = **False** (74 keys, re-run). C-13 prescribes a successor the wave it serves deletes — the exact dead-landmark error D §1 indicts in M-bump-migration.md:56. Evidence half survives (FR-COB-21) |
| K-5 | **D's "14 variants" / "aria-pressed on ALL 14"** (×3 in D) | **KILL SUSTAINED**: the installed union is **13** (index.d.ts re-read); `link` carries no aria-pressed leg (arm extracted verbatim). C-9's "12 of 13, all but link" is the correct statement; D's S-1(b) durability universal falls with it |
| K-6 | **D-S1/D-§7's "highest-value live probe" + C-S2's `@source`-contingency** — "build web/ and grep the emitted CSS; absent ⇒ S-1 collapses, D-6 → BLOCKER" | **KILL SUSTAINED**: no probe is needed — the utility ships pre-compiled in the unlayered components.css, imported unconditionally (P9 header verbatim: the glob is the BACKSTOP, the emission is PRIMARY). S-1 stands on bytes; D-6 does not escalate; the corpus's most-advertised open probe is closed from disk |
| K-7 | **D-6's "hover is a louder signal than pressed — a state-hierarchy inversion"** | **KILL SUSTAINED TWICE OVER** (R-1/R-2): the pressed bg beats hover/active by unlayered source order, AND the border arms paint nothing — the rendered composition is pressed-suppresses-everything (FR-COB-7), the opposite of the filed inversion. The OFF-state channel-count half survives |
| K-8 | **L's 7.0.0 provenance channel** (header ":19", L-6 `material.css:352-377`, bare `src/` paths) | **KILL SUSTAINED**: the producer working tree is 172 dirty paths on an 8.0.0-accruing branch (re-verified; drift from LC's 162 is the tree moving again — the kill's own point); the quoted cohort sits at **:428-452 at the tag** and the working tree's :352 block begins `.glass-wash` with `.glass-material` struck. Substance survives at the tag (re-verified here); the citations do not reproduce. D/C anchored to `git show v7.0.0:` and stayed reproducible — the method split IS the lesson |
| K-9 | **D-2's parenthetical** — "grep empty (only the file's own two self-references, lines 5 and 21)" | **KILL SUSTAINED**: the grep is empty INCLUDING the file (lines 5/21 contain no `CanvasOverlay` substring; line 3's prose is lowercase-hyphenated) — a row cannot be simultaneously empty and self-referential. C-1 stated it correctly. BLOCKER unaffected |
| K-10 | **Line/count drift bundle** (both challenges + both readers) | **ALL SUSTAINED, EXTENDED BY THIS SEAT**: tsconfig include :19 (L-2 said :20) · v7 `data-control-target` :92 (D-1 said :89) · iconOnly doc :24 (D-5 said :26; L-7's :25 is the prop) · `type` cure :55 (D-9 said :54) · D-3's "all 8 `.is-active` rules" → **11** · DU's "10 selectors" → 11 · LC's "12 CSS / 11 bindings" → 11 / 17 (and 24≠30 internally) · DU's "3 prose" variant lines → 2 prose + 1 dead attribute · vendor-ui at :52 (C said :53-57) · metric-badge row cited at :471 and :472 across documents. No verdict moved; this corpus's warrant is file:line provenance, so the drift is booked |
| K-11 | **C's terminal disposition** — "FullscreenViewer.vue:110 and the six DockIconButton sites should carry `:aria-pressed`" | **KILL SUSTAINED**: :110 is a one-shot close ACTION (verified in situ) — adding `aria-pressed` manufactures precisely C-4's defect; and "six" enumerates seven. L's 8-site list (7 dock + ConvergenceTimeline, FullscreenViewer excluded) is the correct lift (FR-COB-17) |
| K-12 | **D-1/C-10/C-12's budgets** — "12 sites / 8 files" and "38 sites … floor dropped on all 38" | **KILL SUSTAINED**: raw grep lines published as site counts. Correct: 9 live / 7 files; 36 real (35 live); the floor-drop denominator is 35. L-2/L-9 subtracted before publishing and are the only axis that did |
| K-13 | **C-S2's "only CanvasOverlayButton relies on the upstream cva paint; the other toggles each have hand-rolled `[aria-pressed]` CSS"** | **KILL SUSTAINED** (R-5): GalleryCard.vue:135-143 is a live `variant="ghost"` + `:aria-pressed` with NO local rule — breaks both the "only" and the "each". Both challenges' cognate superlatives survive only at canvas-control-dock scope |
| K-14 | **reader-DU's own missed-row cell** — "with `aria-pressed='true'`, hovering changes ONLY border-color" | **KILLED BY THE OTHER READER, ratified here** (R-2): the border-color change paints zero pixels (border-style none). Hover on a pressed control changes nothing. DU's row survives with the stronger consequence |
| K-15 | **reader-LC's D-16 mechanism cell + its "not decidable from source alone" filing** | **CORRECTED / OVERRULED** (R-7/R-1): the operative mechanism is the shipped unlayered sheet (version-independent), not the consumer JIT's layer order; and the cascade order was decided from disk. Verdicts unchanged; the cell is challenge-inherited — recorded as fleet evidence per the pt-leaf ruling-3 precedent |

## Superlatives — 7 sustained, with provenance (L-18 runs both ways)

1. **L §3 — the corpus's best single work, now thrice-derived.** The per-basename inbound sweep that NAMES intake R3-7's anonymous `Unmounted 2`: this seat re-ran it from scratch (66 SFCs; zero-inbound = exactly the two named files), making three independent derivations in agreement — then costed it (the frozen tree's live InfoCard edit; the 6→5 metric-badge budget) and, per R-6, anchored it to the F8-REACH-01 row everyone else missed. Naming an adjudicated integer and then costing it is what an audit lane is for.
2. **C-3/D-17's archaeology, verified byte-for-byte and sharpened.** `be24948^` = `<button class="glass-btn">…`, no docblock, **no aria-pressed**. One commit simultaneously: destroyed the class's only paint hook, wrote the sentence calling it canonical, **and added the one correct decision in the file** — the same diff is the origin of the defect and of the durable idiom. C-3 alone also refused the obvious repair on producer prose (MUTUALLY EXCLUSIVE / R8-17).
3. **D's §8 epoch discipline, vindicated by contrast.** Every D/C 7.0.0 claim reproduces at `git show v7.0.0:` four days and two working-tree drifts later; L's working-tree citations are 76 lines adrift (K-8). The pinned-authority law holds: cite the epoch or the cite rots.
4. **`:aria-pressed="active"` (line 20) — more durable than any axis established.** It needs no JIT (K-6: shipped unlayered, imported unconditionally), it WINS the background cascade over hover/active (R-1), it is the arm the `.glass-btn` primitive honours, the arm v7's prefers-contrast/forced-colors arms key on exclusively, and a DOM attribute needing zero migration at F.W1 — qualified only by FR-COB-6 (v7 removes the default-register paint) and R-5 (GalleryCard also rides it; the "only correct reader" credit holds at dock scope).
5. **The file itself (D-S4/C-S3/C-S4/L-S2/L-S3, unanimous).** 25 lines, no `<style>`, no `!important`, no local token, no lifecycle surface, one leaf subpath present in both exports maps, `active` declared-not-$attrs. Against the tree's chronic register (the cartoon-card shim ×22, 7 `!important`s, four `spectrumColor` bodies), this is the discipline the tree lacks — every defect here is contract-level or inherited, none is an override.
6. **The corpus contradicts its own prior record with a dated reason** (C §2 S-2 / L §4): raw-findings:2968 declared stale against the installed 4.0.0 with the exact curing install named, and the load-bearing consequence drawn (dock toggles = pure a11y defect). Rarer and more valuable than confirmation; ratified at the bytes here (FR-COB-11).
7. **L-9/L-2's counting discipline** — the only axis that subtracted prose (and the dead site) from grep lines before publishing a migration budget; both subtractions re-verified exact (R-3). The two readers that repeated raw lines as budgets each inflated F.W1 by 2–3 phantom rows (K-12).

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. **The pressed tint's composited ΔL\*** over real `/visualize` canvas backdrops, both themes — now governing the ENTIRE toggle contract (per FR-COB-7 the background is the only channel, hover included), not one of two channels as D-6 filed.
2. **FR-COB-23 applicability**: the focus ring composites over a rendered canvas, not `--background`; the 1.94:1 arithmetic is settled, the true adjacent surface is not.
3. **FR-COB-6 residual**: whether v7's `--specular-intensity: 0.16` ::before lift clears perceptibility and 1.4.11's 3:1 once the background delta is gone. Rule inventory static and complete.
4. **FR-COB-12 confirmation**: computed `scale` on `:active` under emulated PRM — one DevTools line; the cascade derivation is settled.
5. **C-11's `-b` masking hour**: whether today's `vue-tsc -b` swallows the TS2322 exit (fourier's plan-synth.json:158 already prescribes `--noEmit`); only the failure hour moves, never the failure.
6. **C-5's computed accessible name** in a real AT tree (the static chain is dispositive as far as source goes).
7. **Whether fourier's own build mints `@layer utilities` twins** of the shipped unlayered utilities — immaterial to every verdict (unlayered wins either way); determines only the F.W1 CSS-size budget of one-copy-or-two. The last committed sheet (2026-06-12) predates the 4.0.0 install and cannot answer it.

## Closing verdict

**DEFECTIVE-AND-DEAD; disposition LIFT-THEN-DELETE (ADJUDICATED, routed, not stamped).** Both hostile re-reads survive the incredulity seat **with corrections that sharpen rather than reverse** — and this corpus needed the seat at exactly seven points: the two readers held complementary halves of the cascade truth (R-1/R-2 — composed here into FR-COB-7, a producer defect stronger than either filed), both mis-counted the same census by one in opposite directions while one contradicted itself (R-3), both booked an already-banked identity as a fresh BLOCKER (R-4), one inherited a challenge mechanism the bytes outrank (R-7), one filed as needs-live what the shipped sheet decides (R-1), and all five documents in the corpus cited one row of a two-row table (R-6). The roster routes: **F.W0** owns the substrate act (minute STEP-2i, `git rm`, rule F8-REACH-01+02 together); **F.W1** owns the census amendment as a fold into fr-BasisSelector M-7 (with the silent-variant and touch-floor-re-key limbs, the corrected 9/36 budget, the EquationView stacked-site sequencing note, and the pressed-paint/type-button/loading hop deltas); **F.W3/W4** own the lift (8 sites, toggle-only), the naming sweep, the vocabulary consolidation with the prop-corruption guard, and the glyph-hatch row; **NO-WAVE-OWNER** carries the seven-item producer relay (FR-COB-28) on the standing glass-ui BH channel. Nothing lost: head (the five-times-ordered deletion finally sequenced), tail (the aria-pressed idiom lifted before the file dies), or interval (every killed claim retired with its refutation on the record).

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; the X·F specs consume this as agglomeration input (M-25); execution awaits the owner's begin-word.
