# wb-gradient-easingspecimenstrip — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator, SECOND SEAL) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator (the incredulity seat) · **Date**: 2026-08-12 · **HEAD**: `9c779130` · branch `tranche-u`.
**SUPERSESSION (nothing lost)**: this file replaces the FIRST SEAL at this path (2026-08-09, HEAD `54a97489`, never committed) after a **second hostile re-read round** produced materially new returns — the two rounds' finding sets are disjoint at the edges (this round adds the ink-rung, payload-clobber, blast-radius, pairing and ordering rows; the first round's M-ESS-3/4/5/6/8/9/10 were never re-litigated). Every first-seal row that survived is **CARRIED below under its original id**; every first-seal kill is restated with this seat's re-derivation. A working copy of the first seal's full text was parked in this session's scratchpad before overwrite.
**Corpus**: eleven challenge documents (C passes 1–4 · D passes 1–4 · L r2/r3/r4; **r2-and-later govern**) against `demo/workbenches/gradient/GradientVisualizer/easing/EasingSpecimenStrip.vue` (215 lines) + its catalogue/row/model/CSS dependency chain and the installed glass-ui 7.0.0 producer surfaces.
**Re-readers adjudicated (round 2)**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted. Every kill was re-derived by this seat with its own instruments before sustaining; every adopted "missed" finding was first checked against the **sibling adjudications** (`wb-gradient-visualizer.md`, `GradientStopEditor.md`, `wb-gradient-pane.md`) for an existing identity — three of the readers' five biggest "misses" turned out to be independent rediscoveries of rows already adjudicated elsewhere, and are identity-guarded here, never re-booked (the ColorSpaceSelector anti-rename law).
**Arbiter's own instruments this session (all static + grep/node against source and installed dist; ZERO browser probes — probe parsimony)**: whole-file reads of `EasingSpecimenStrip.vue` (215 L), `easingCatalogue.ts` (230 L), `useSpecimenRows.ts` (74 L), `EasingAuthoringStage.vue` (116 L), `GradientEasingEditor.vue` (295 L), `useGradientModel.ts` (193 L), `ErrorBoundary.vue` (87 L); targeted reads `useGradientInterpolation.ts:27-39`, `useGradientCSS.ts:50-80/:185-210`, `GradientVisualizer.vue:45-100/:130-150/:236-246`, `GradientStopEditor.vue:6-16/:42/:72/:98-113`, `App.vue:47/:50/:140` + repo-wide `onErrorCaptured` census (1 site), `useContrastSafeColor.ts:44-60/:347-362`, `ink.ts:13-16`, `picker-color.ts:104-107`, `src/subpaths/math.ts:1-17`, `src/foundation/math.ts:113-120`, `src/easing.ts:33-68`; node against dist: `Object.keys(bezierPresets)` = **30**, order pinned (`smooth-step-3` at index 5, the cubics at 12-14); installed glass 7.0.0 dist: `glass.css` = exactly **18** `@import`s, **0** naming glass-chip (`glass/glass-chip.css` exists on disk; `grep -rln` over dist/styles → **zero importers**); `glass-capsule.css` full block (warm floating fill · pill radius · unconditional `--glass-shadow-floating` · unguarded `:hover { scale: 1.015 }` · `:not([data-press-armed]):active` press scale); chip base class list `"glass-chip glass-capsule accent-tone …"` + `$attrs` reserve regex `role|aria-pressed|data-state|name|required`; `easing.js` role census `{group ×1, slider ×1, status ×2}` — **zero `role:"img"`**; `initialPreset ?? "ease-out-back"`; the model-watcher apply bytes (`Array.isArray(e.points) … && (P(0,…), P(1,…))`, `Number.isFinite(e.steps) && (F.value = clamp 1..12)`, term list check); `fading-scroll-*.js` (conditional `role: region`, unconditional `tabindex:"0"`, **and a JS scroll-listener fallback calling `setProperty("--fade-start"/"--fade-end")`**); `base-misc.css` physical `to right` mask; `tokens/offsets.css` `--fade-scroll-width: 1rem`; `theme/radius.css` `--radius: 0.625rem` + `--radius-input: var(--radius)` vs `components.css` `--radius: 0.25rem` imported `layer(components)` (index.css layer order read) vs `tailwindcss/theme.css:508` `--radius: 0.25rem`; `segmented.css` `.segmented-tabs--vertical { --bouncy-slider-radius: var(--radius-strip) }` (imported by index.css — my read of its 38-import line); reka-ui `Toggle.js` `dataState = computed(…)` emission; repo censuses: `radius-strip` in demo = **0**, scrollbar-suppression rules in strip + base-misc = **0**, `localStorage|sessionStorage|useStorage` in `demo/workbenches/gradient/` = **0**; `test/gradient-v4-consume.test.ts:55-58` and `o17-easing-composition.spec.ts:51/:117/:121/:189` read verbatim; the verbatim r4 `radius-strip` find|grep re-run (→ **two** lines, not one); X wave ownership re-resolved TODAY: `X/waves/W6.md:61` (`easingCatalogue.ts` modify) + `:218` (Files: `GradientEasingEditor.vue`, `EasingAuthoringStage.vue`, `easingCatalogue.ts`) — **neither `EasingSpecimenStrip.vue` nor `useSpecimenRows.ts` in any X wave file list** (sole mention repo-wide: W1.md); sibling-adjudication identity sweep (GV-6 · GV-12 · GV-23 · GV-24 · GV-35 · GV-36 · GV-37/GV-66 · GV-41 ≡ GradientStopEditor D-2/G2c · R2-1 exposure vectors).
**Motion/PRM law**: my grep over `audit/codex-provenance/motion-quarantine.md` → **zero M-xx rows name this component or its chain**; all five challenge files are `EXISTS-ORIGINAL` in `registry/HYDRATION-LEDGER.md:226-230` (my read) — no FALSE row exists to carry. Every PRM claim in this record was re-derived against dist bytes.
**Status verbs**: every disposition here is **ADJUDICATED**, never VERIFIED — nothing in this file stamps a wave.

## Scoreboard

**39 axis rows adjudicated: 1 BLOCKER · 21 MAJOR · 14 MINOR · 3 INFO (plus L-11 identity-folded, not re-booked) · 12 kills SUSTAINED (every one re-derived by this seat) · 11 round-2 missed findings ADOPTED (2 new MAJOR + 1 elevated MAJOR + 3 identity-credited to sibling adjudications + 5 smaller) · 8 first-seal M-ESS rows CARRIED · 14 UNPROVEN-NEEDS-LIVE residue rows → visual audit (one first-seal residue SETTLED STATICALLY this session).**

Blocker-weight survivor: **C-01, as widened by the first seal and cross-confirmed by the sibling seats** — the Gradient pane is torn down by any easing whose **range leaves [0,1]** (never the family name), through THREE consumer seams that demote the library's total `Result` into a bare render-path `throw`; the third seam (`colorAtPosition` → the stop rail's hover ghost, continuous `t`) fires on **plain pointer movement**, no press, accordion closed. The library is blameless at every seam (`src/color/operations.ts:90` returns `err`, never throws — my read).

## Ruled disagreements (this seat's actual work, round 2)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **Reveal-under-the-fade** (both readers' independent miss; ≡ first-seal M-ESS-1) | MAJOR — "erases a third of the only selection signal … at the exact moment the reveal fires" | MINOR — with the caveat "`--fade-start/--fade-end` are `@property`-registered `initial-value: 0px`, so in a browser without `animation-timeline: scroll()` the mask is fully opaque and the defect is inert" | **MAJOR — DU's grade; LC's inertness caveat REFUTED by this seat.** | My read of the compiled `fading-scroll-*.js`: alongside the scroll-driven keyframe arms there is a **JS fallback** — `addEventListener("scroll", g, {passive:!0})` driving `setProperty("--fade-start", …)` / `setProperty("--fade-end", …)` — so the 16px feather paints in EVERY engine, `animation-timeline` support or not. The seat's `:64-70` nearest-edge math is EXACT (zero inset), so the reveal deterministically parks the just-selected 44px tile with its leading or trailing 16px under a transparent-to-opaque alpha ramp, in both directions, LTR and RTL alike — and selection at this seat is ink-only (C-03). The cure is ours and two expressions wide (inset `dx` by the resolved `--fade-scroll-width`), or glass's `scrollToActive` ask. The ~36% fraction stays U-6; the mechanism and its universality are now source-certain. |
| **C-17 / D-03 grade** (hover vs selected label ink) | MINOR — wording correction: "the selected register is three declarations … hover cancels exactly one (label colour); `font-weight: 600` and the accent glyph survive" | MINOR | **MINOR-while-C-03-stands — the readers' magnitude correction is adopted over the first seal's MAJOR.** | My read of `:204-214`: the selected register is four declarations (glyph stroke + stroke-width, label color + weight); `:212-214` ties `:208-211` at equal specificity — (0,3,0) bare, (0,4,0) with the scope attribute appended to both; the readers' differing arithmetic is a nit, the tie and the source-order verdict are identical — and cancels ONE of the four, producing the incoherent accent-glyph-over-neutral-bold-label state, sticky on touch (no `(hover:hover)` guard anywhere in the SFC — my read). The first seal graded the whole register erased; the second round's narrower measurement is correct. The C-03 coupling stands: the day glass ships the chip sheet the pressed wash returns and this row shrinks further. |
| **R4-1 grade + diagnosis** (`cubicBezierToString`) | MINOR — "a dead published symbol with no user-visible consequence … the disposition is what makes it a blocker for the release, not for the strip" | killed r4's BLOCKER outright; missed-finding limb re-homes the cure: the `/math` barrel's own docstring ("parse-that-FREE … zero CSS grammar" — my read of `src/subpaths/math.ts:1-5`) is violated by a `cubic-bezier(…)` emitter (`src/foundation/math.ts:113-120`, my read) | **MINOR at this component — both readers sustained; the first seal's MAJOR is demoted; IDENTITY: ≡ GV-12** (wb-gradient-visualizer: "`/math` bezier serializer: unused, 2dp-lossy, test-pinned wrong law → **X-W9 coordination else NO-WAVE-OWNER**"). | One identity, already booked at the visualizer's adjudication with the same routing this record needs; LC's barrel-contract limb is ADOPTED as the grep-checkable form and travels with GV-12 (the retire-vs-move decision is X-W9's, and either arm is a public-surface change riding a version cut). Nothing at this seat blocks on it: zero production callers (first-seal 4-home census, LC's independent re-grep). |
| **D-42 grade** (undesigned states) | MAJOR — five states never designed; `disabled` shipped-but-unreachable while 3 of 27 choices are pane-lethal; truncation-to-stadium mechanism | MAJOR (as D-41/D-42 pair) | **MAJOR — the second round's grade replaces the first seal's INFO-scoped `D-42-disabled` row, which is subsumed as a sub-row with LC's caveat intact (disabling is NOT the C-01 cure — it would mask the defect).** | My reads concur on every limb: `:98-107` never passes `disabled` though the chip declares it and forwards to Toggle; the `[data-disabled]` style AND the `prefers-contrast: more` arm both live only in the orphaned `glass-chip.css` (my orphan-census); `.tile-label { white-space: nowrap }` (`:199`) with no `text-overflow` widens tiles instead of truncating; the empty state is impossible by construction (`GradientVisualizer.vue:238` gate + `useGradientModel.ts:123` ≤2-stop refusal — both my reads). |
| **C-10 / D-20 / L-6 grade** (seat restyles the producer root) | (not re-filed) | MINOR | **MINOR — LC's grade; first-seal MAJOR demoted.** | `:163-170` re-declares four of `SHAPE.cell`'s declarations + adds an inline-only `min-width` floor; no producer SIZE rung reaches specimen scale (my dist read of the chip size map at the first seal, spot-consistent today) — edict-5 hygiene plus a genuine producer rung ask, not a MAJOR defect of this seat. The touch-floor half rides D-21/M3. |
| **D-21 grade** (touch target) | MINOR — "the miss is 0.2px (0.45%), clears WCAG 2.5.8 AA comfortably; filed MAJOR on both axes, it is MINOR at worst, cured for free when glass unorphans the sheet" | MINOR (inside C-10) | **MINOR — sustained against both axes' MAJOR.** | The 43.8px reconstruction (5+22+2+10.8+4) reproduces from the SFC's own padding/gap/line-height (my read of `:163-170/:176-178/:195-196`); the producer's coarse-pointer 2.75rem floor sits in the orphaned sheet and returns with M3. |
| **Blast radius** (LC missed #1) | — | MAJOR, filed as new | **CONFIRMED — but IDENTITY: ≡ GV-6** (wb-gradient-visualizer: "single ErrorBoundary = two-pane outage → **X-W5**; MX-7 cross-ref"), already adjudicated; adopted here as a CROSS-REF on C-01's severity statement, never a second booking. | My own re-derivation concurs with the reader: `App.vue:50-140` is the ONE `<ErrorBoundary>` and it encloses the whole `pane-container` inside `<main aria-label="Color tool panes">` (:47); repo-wide `onErrorCaptured` census → 1 site (`ErrorBoundary.vue:59`); on catch the fallback replaces the entire two-pane subtree (`v-if`/`v-else` slot, my read); `reset()` remounts, and `useGradientModel` re-seeds the two hard-coded stops (`:79-82`) — zero persistence in `demo/workbenches/gradient/` (my grep). So one tile press blanks BOTH panes and recovery discards the gradient document. C-01's blast statement carries this; the cure home is X-W5's (per-pane boundaries), per GV-6. |
| **Stop↔interval pairing** (LC missed #3) | — | MAJOR, filed as new — "inserting or removing a stop anywhere but the tail silently re-assigns every downstream interval's easing … pressed tiles and head names change on rows the user never touched" | **CONFIRMED — but IDENTITY: ≡ GV-41 ≡ GradientStopEditor D-2 pairing arm (CONFIRMED BLOCKER there, G2c cure: insert-time interval splice + stop-owned easing → X-W6.a).** Adopted as this component's WITNESS of an adjudicated defect, not a new booking. | My own re-derivation concurs line-for-line: `useGradientModel.ts:89-100` reconciles by LENGTH alone (tail push/tail truncate) while `addStop` (:116-120) sorts into the middle and `removeStop` (:122-125) filters by id; intervals carry no identity (`GradientInterval = EasingPickerValue`, `:49`); the strip-visible symptom (pressed tiles jump rows) rides the same index keying as GV-37/GV-66's `openInterval`/`tuneOpen` rows. The reader's worked case matches GradientStopEditor's G1/G2c repro verbatim. One identity, three independent derivations — the row is as solid as anything in the program. |
| **Producer default = fatal curve** (DU missed #4) | MINOR, filed as new — `EasingPicker` initialises `initialPreset ?? "ease-out-back"`, y2 = 1.275 | — | **CONFIRMED — IDENTITY: ≡ the R2-1 uncontrolled-`initialPreset` exposure vector already carried at wb-gradient-visualizer** ("now sharpened by this seat's measurement that the default preset's fn(0.5) throws … the masked-today verdict rests entirely on EasingAuthoringStage's controlled binding"). MINOR, riding the same glass BH relay as the C-01 seam notice; masked at this seat by the controlled `:model-value`. | My own dist grep this session: `initialPreset ?? "ease-out-back"` verbatim. Any consumer mounting `<EasingPicker>` uncontrolled boots on a curve this app cannot render. |
| **D-01** (pass-1 BLOCKER: "the strip IS the tiny nested widget the canon names by name") | KILLED — pronoun misread; the canon names the strip as LEGITIMATE support | (not re-filed) | **KILL SUSTAINED; the substance stays rostered as D-01-rescoped (the missing `/easing` route), per the first seal.** | My own canon reads TODAY: `VISUAL-CONSTITUTION.md:50` lists "catalogue, **specimen strip** and code inspector" as the Easing composition's support members; `:210` — "Catalogue and specimen strips support the curve rather than reducing **it** to a tiny nested widget" — the referent of "it" is THE CURVE (the readers' cited `:208` is two lines of drift, content byte-identical). "By name" is false twice over; what §7 forbids is what D-40 measures (the curve at 10.42rem behind a second click). |

## Adjudicated defect roster

Line anchors are HEAD `9c779130` — every anchor cited below was re-resolved by this seat TODAY; re-resolve again before executing any cure (the D-19 lesson). **NAMED GAP (◆, re-verified this session)**: X-W6's file lists name `easingCatalogue.ts` (W6.md:61) and `GradientEasingEditor.vue` + `EasingAuthoringStage.vue` + `easingCatalogue.ts` (W6.md:218) — **neither `EasingSpecimenStrip.vue` nor `useSpecimenRows.ts` appears in any X wave file list** (sole repo-wide mention: W1.md). Rows marked ◆ route **X-W6 only if W6 adopts both files explicitly; otherwise they degrade to NO-WAVE-OWNER at execution** (the PCS-1/PCS-19 precedent; consistent with GV-35's NO-WAVE-OWNER booking of the same surface).

### BLOCKER

- **C-01 (+D-18 / C-3 / C-6; third seam folded; GV-24 cross-ref; blast radius ≡ GV-6) — ADJUDICATED BLOCKER → X-W6 (.c/.d), blast-radius limb X-W5.** The lethal predicate is **`range(fn) ⊄ [0,1]`**, not the family name (both readers re-derived DU's grid table offline to six decimals; the visualizer seat's K-19 ruling refines the per-door mechanics: `ease-in-out-back` fn(0.5)=0.6067 is LEGAL at the ink door and detonates through the multi-sample walk doors instead). Three seams demote the library's `err` to a bare render-path `throw`: `useGradientInterpolation.ts:37` — reached by `useSpecimenRows.ts:52-59` (`fn(0.5)` per row) AND by `GradientVisualizer.vue:64-88` `colorAtPosition` (`easedT` at `:78`) via `:139` `:color-at` → `GradientStopEditor.vue:72`'s hover-ghost computed (`:98` sets `hoverPos` on pointermove) — **hover over the gradient bar with a back-eased interval present crashes the pane with no press and the accordion closed**; and `useGradientCSS.ts:~205-208` inside the coalesce walk (all serializers; `openIntervalRamp` = GV-24's sixth door). Shipped fatal instances: the three `back` presets (`src/easing.ts:60-62`: y-coords −0.28 / 1.275 / −0.55+1.55 — my read); ~62% of the picker's own authoring y-domain (±0.6 overshoot clamp) is fatal (DU, from producer bytes). Blast radius: the ONE `ErrorBoundary` (`App.vue:50-140`) blanks BOTH panes; `reset()` re-seeds the model's two default stops (`useGradientModel.ts:79-82`; zero persistence — my greps) — the user's document is destroyed. **Cure re-homed per the first seal's ruling (sustained)**: totality lands inside `interpolateStopColors` + `sampleCoalescedStops` — the two functions where progress meets `mixColors` — or all named call sites are edited and the gate sweeps `colorAtPosition` explicitly; the governing C doc's two-seam cure and its stop-count gate are necessary, NOT sufficient. Coordinate with X-W6 mechanism item 1 so ONE totality decision lands.

### MAJOR

- **C-06 / L-1 — MAJOR → X-W6.d (IDENTITY: ≡ GV-23, already booked there).** `FAMILY_ORDER` (`easingCatalogue.ts:174`) used as a FILTER (`:191`) silently drops all six quart/quint presets — built at `:181`, discarded at `:191`; my dist census: exactly 30 keys. Six first-class library presets render as `custom` with no tile pressed while the producer's own menu (glass `H = Object.keys(P)`) offers all 30; the module docstring (`:13-15` "the SAME catalogue … never a second mint") is false on both clauses. Cure: order-hint, never filter; exact-equality catalogue gate.
- **C-03 / L-5 / M3 — MAJOR → GLASS-OWNED (BH relay per owner edict; bank with M3/I-9; NO local patch — MT-F014).** `dist/styles/glass/glass-chip.css` ships and NOTHING imports it (my census TODAY: `glass.css` = 18 imports, zero chip; dist-wide `grep -rln` → zero; no `./styles/*` wildcard export). Seven registers unreachable on every consumer: `--radius-card` cell radius, the `[data-mode=selectable][data-state=on]` accent wash, the `::after` flood + `--chip-flood-t` press punch, the coarse-pointer 2.75rem floor, `prefers-contrast: more`, `[data-disabled]`. The imported `glass-capsule.css` wins by default: pill radius, floating shadow, warm floating-tinted fill (my read). The strip's `:160-162` docblock ("carries press/hover semantics + the pressed wash") is false in the shipped build and must be corrected when the producer fix lands.
- **C-05 / D-40 (+ D-40-forward-half) — MAJOR → X-W6 (files in W6 list) + one glass hook ask (BH relay).** The zero-letterbox law is DEAD CODE: `EasingAuthoringStage.vue:48-50` queries `svg[role='img']`; the compiled canvas emits `role:"group"` (my role census: zero `"img"` in easing.js). `vbRatio` frozen at `ref(1.2)` (`:45`); onMounted + watch + both rAFs + five `:deep` declarations (four `!important`, `:104-115`) bind to nothing. **Forward-half (LC's sharpest cross-axis catch, sustained)**: the naive re-key would ACTIVATE `transition: aspect-ratio` (`:114`) — an animation of a layout property, currently inert by accident; the block's own PRM comment notwithstanding, any cure must decide that transition deliberately. Cure: stable canvas hook (`data-easing-canvas`) as a glass ask; delete the inert apparatus. Geometry numbers stay U-3. (Row primarily credits the sibling seat wb-gradient-easingauthoringstage; rostered here because this corpus proved it.)
- **C-02(b) / L-12(2) — MAJOR → GLASS-OWNED (BH; app-wide).** Three values for one token, decided by cascade layer — all three read by this seat TODAY: `@theme` `--radius: 0.625rem` + `--radius-input: var(--radius)` (theme/radius.css) vs `components.css` `:root { --radius: 0.25rem }` imported `layer(components)` — later than `theme` in `@layer theme, base, components, utilities` (index.css) — vs Tailwind's stock `--radius: 0.25rem` (theme.css:508). Every derived token collapses 10px → 4px; consumed at this seat by `GradientEasingEditor.vue:274` `.rail-btn { border-radius: var(--radius-input) }`. Live computed value = U-14. No consumer re-declaration (MT-F014).
- **C-04 / D-38-mechanism — MAJOR → X-W6.** Twenty-seven in-plate tiles carry `.glass-capsule`'s unconditional `box-shadow: …, var(--glass-shadow-floating)` (my dist read) inside a card whose own contract is "flat on the plate … no shadow (DESIGN.md § Depth)" (`GradientEasingEditor.vue:108-110`, my read) — the canon rows (VISUAL-CONSTITUTION one-surface-one-tier; PR-05) verified verbatim by DU. Structural contradiction certain; the ΔL asymmetry and the OM-4 grey-band attribution stay U-2/U-10.
- **D-37 / C-02(a) / L-12 / R4-2-as-refined — MAJOR → X-W6.d (under X-W10's law) + the fleet-census rider (LC missed #8, INFO, adopted).** Four declared radius tokens resolving to six effective radii with no derivation law; nested corners non-concentric at four sites; every declaration cited checks at its line (`:114/:153/:176/:242/:274`; the strip itself declares ZERO radii — DU re-verified, my reads concur where sampled). The cure adopts the producer's live `--radius-strip` role token (see the R4-2 kill — the token has a producer consumer, which STRENGTHENS the adoption cure). **Rider**: LC's fleet census (rounded-md ×14 / non-role rungs 74 vs role tokens 27, ≈2.7:1) makes the derivation law a fleet edict with a migration behind it — wave-sizing input for W6.d/W10, not a scope change here.
- **C-07 — MAJOR → X-W6.d.** The generic `steps` tile is a misreporting trap: `tileIdFor` (`:219-223`) maps EVERY non-literal steps interval onto the one tile whose printed label is `n = 4` and whose literal is fixed `steps(4, jump-end)` (`:187`); `onTileToggle` drops `on === false` (`:33-35`), so pressing the lit tile is a no-op — a user on `steps(7, jump-start)` sees a pressed tile labelled `n = 4` and cannot press their way back. The strip's own stated law (`:7-8` "pressed tile IS the interval's curve") is false in the steps case.
- **C-08 / R4-3 / D-24 (+ R4-3(a)/L-7 comment-falsity limb, MINOR, folded) — MAJOR → X-W6 ◆ (≡ GV-35, booked NO-WAVE-OWNER there — the ◆ resolves the same way) + the semantics half as a glass ask (BH).** Glass's port takes an unconditional `tabindex:"0"` and exposes `role="region"`+name ONLY when `ariaLabel`/`ariaLabelledby` is passed (my dist read); the strip passes neither (`:84`) and instead names the non-scrollable `.strip-row` (`:85-90`, `width: max-content` at `:129`). The focusable scroller is a nameless roleless generic. Folded limb: `:58-60`'s "its documented DOM contract" is FALSE — `fading-scroll` appears in no published `.d.ts` (readers' grep, spot-consistent with my d.ts read at the first seal). Chip's `$attrs` reserve (`role|aria-pressed|data-state|…` — my dist read) blocks the consumer from curing tile semantics locally — genuine producer ask.
- **L3-4 — MAJOR → X-W6 ◆ + glass ask.** A 27-way single-select hand-built from 27 independent boolean toggles inside a bare `role="group"` while glass publishes `./toggle-group` at a real exports key; no roving tabindex anywhere.
- **L3-1 — MAJOR → X-W6 (.a/.d coordination).** The persisted domain model is typed by a widget's v-model payload: `GradientInterval = EasingPickerValue` (`useGradientModel.ts:12,:49` — my read), forcing every tile payload to fabricate the OTHER mode's parameters (`:124-125`, `:149-151`) and the model to persist a live callable `fn`. **This is the enabling condition for the payload-clobber row (N-2)** — cure them together; coordinate with GV-8's "easing needs its own persisted home" amendment.
- **L3-2 — MAJOR → X-W1 (+X-W8 rider).** The library's publish gate imports demo feature internals four levels deep (`test/gradient-v4-consume.test.ts:7-11`), one of TEN test files importing `../demo/` (LC's census) — a demo rename reds `npm test` for a non-library reason.
- **L-2 — MAJOR → X-W9 coordination (+X-W6.d consumer half).** `/css` parses timing functions and cannot serialize them; the inverse is hand-rolled three times across two packages (`easingCatalogue.ts:48-56`, `useGradientCSS.ts:53-62`, glass's own compiled readout — LC verified all three; my reads concur on the two demo mints), guarded by exactly one e2e assertion. The missing serializer and the R4-1/GV-12 retirement are ONE X-W9 decision.
- **L-3 — MAJOR → X-W6 ◆ (consumer swap) + the producer door already exists.** Reduced motion has five demo homes plus glass's published-and-unused `./motion-core` (`useReducedMotion`/`readReducedMotion` — readers' d.ts read); this file is the demo's only vueuse-PRM site (`:12,:47`). NOTE the behaviour itself is CORRECT (see superlative 1) — this is sourcing, not conduct.
- **D-28 — MAJOR → X-W6 ◆.** Twelve pixels of unowned space above the strip because `.strip-row` is `display: inline-flex` (`:126`): the inline-level box makes the port generate a line box whose strut overhangs the flex box's first-item baseline (the 9px eyebrow); `width: max-content` (`:129`) already supplies intrinsic sizing, so the cure is the one word `flex`. Both readers independently re-derived the mechanism; DU calls it the best-diagnosed row in the corpus — this seat concurs.
- **D-29 — MAJOR → glass ask (a `:presets="false"` door, same channel as `:readout`/`:playback`) + X-W6 seat half.** Two selection surfaces for one model in one open row, and the producer's combobox CANNOT reflect state: no `presets` prop exists (d.ts read); every tile press routes through `setHandle` → preset ref = `"custom"` → the combobox renders its placeholder. The two surfaces are structurally guaranteed to disagree. (Adopted on the readers' dist bytes; my own watcher-byte read concurs on the `setHandle` path.)
- **D-41 / D-08 — MAJOR → X-W6 ◆ under X-W10's type law.** The strip hand-rolls a 9px rung (`:149`, `:195` — `0.5625rem`, my reads) below the house's smallest named rung (`--type-micro` 11px, which the chip cell recipe already applies and the seat overrides DOWN); the card's largest ink is the machine literal (16.4px from the `--type-small` clamp — reproduces from tokens) while its twenty-seven choices are its smallest: a 1.82:1 protagonist inversion. The selection signal rides the 9px labels.
- **D-42 — MAJOR (ruled above) → X-W6 ◆ state enumeration; disabled sub-row keeps LC's caveat.** Error / disabled / overflow-affordance / truncation / prefers-contrast never designed; the one arm that would rescue the isoluminant selection lives in the orphaned sheet (C-03's freight); `white-space: nowrap` with no `text-overflow` is the stadium-tile mechanism.
- **D-17 (carried, first seal) — MAJOR → X-W6 ◆.** The `custom` state has no rendering: `tileIdFor` → null, the reveal early-returns (`:53`), no chip pressed, while the row head prints `custom` — reachable via C-06's six drops AND free authoring; the strip's `:19` docblock asserts the state away.
- **D-09 (carried, first seal) — MAJOR → X-W6 ◆ under X-W10's law.** Bare polyline in an empty unit viewBox — no frame, baseline, or axis; `steppedEase(1,"jump-start") ≡ 1` makes `step-start`'s portrait a horizontal rule at the top of an invisible box, unreadable against the component's own shape-recognition thesis (`:184`).
- **D-04 / D-33 (carried, first seal) — MAJOR → X-W6 ◆ under X-W10's law.** Seven `border-left`-family rules paint a grouping line (`:143-146` — my read) in a composition whose binding inventory sets retained lines to `none`, atop a 28px-vs-4px spacing ratio that already carries the grouping.
- **D-01-rescoped (carried, first seal; pass-1 framing KILLED, see register) — MAJOR → X-W6 (routed-scene criterion; canon coupling X-W5).** The canon'd `/easing` route with the 19–22rem curve protagonist does not exist (router grep: zero `easing` routes — first seal, spot-consistent); the strip is support for a protagonist that was never cut.

### MINOR

- **C-17 / D-03 — MINOR-while-C-03-stands (ruled above) → X-W6 ◆.** Hover cancels one of four selected-register declarations at equal specificity by source order; no `(hover:hover)` guard; sticky on touch, composing with M-ESS-6's sticky scale on the same gesture.
- **C-09 / D-25 — MINOR → X-W6 ◆.** WCAG 2.5.3 fails on exactly 1 of 27: `:aria-label="tile.id"` (`:105`) vs visible `n = 4` (`:187`). Both readers enumerated all 27 pairs independently; my spot-walk of `familyLabelFor` concurs.
- **C-10 / D-20 / L-6 — MINOR (ruled above) → glass rung ask (BH) + X-W6 ◆ consumer half.**
- **C-11 / D-23 / L-4 — MINOR → X-W6.** One full 27-Chip strip + `useMediaQuery` listener mounted per interval forever (`v-for` `:112` → `v-show` `:144`, ramp `v-if`-gated at `:152` but the strip not — my reads); also what forces the subtree-scoped querySelector the strip documents (`:39-41`). Node arithmetic ~134/strip; page-share figures U-5-adjacent (LC's DOM-mass numbers stay unproven).
- **D-05 / D-34 — MINOR → ours X-W6 ◆; producer mask half GLASS-OWNED (bank with M3).** Physical `border-left`/`padding-left` (`:143-146`) in a direction-reversing row; the producer's mask is physical `to right` (my dist read). One word per property ours; the mask glass's.
- **D-21 / L-6 — MINOR (ruled above) → discharged by M3's unorphan; no local cure.**
- **R4-1 / GV-12 — MINOR (ruled above) → X-W9 coordination else NO-WAVE-OWNER, per GV-12's standing booking; the barrel-contract limb (N-11) travels with it.**
- **L3-3 — MINOR → X-W6.** `useSafeAccentFn` reaches boot via `inject(INK_AMBIENT_KEY)!` while two sibling consumers inject the same key null-tolerantly (LC's four-site census) — two disciplines, one key; per the ColorSpaceSelector L-5(b) precedent this is a house-contract question, kept at MINOR as a consistency row, not a defect of the assert.
- **L3-5 (+ N-8: LC missed #6, the cheaper cure — ADOPTED; ≡ first-seal M-ESS-7, one identity) — MINOR → X-W6.** The Result-unwrap is duplicated (`easingCatalogue.ts:98-104`, `useGradientCSS.ts:71-77`, glass's own compiled copy), each typed by one factory's `ReturnType`; **a correct generic already exists un-exported at `demo/color-session/picker-color.ts:104-107` (`valueOrThrow<T, E extends Readonly<{code:string}>>` — my read)** — exporting it retires both demo copies today, no producer packet, no semver event. LC's find materially cheapens the disposition L3-5 filed.
- **L3-6 — MINOR → X-W6.d + glass ask.** One union, two exported names (`JumpTerm` vs value.js's `JumpPosition`), paid with a no-op cast (`:136` — my read).
- **L3-7 — MINOR → glass ask (BH).** Glass's easing defaults are modelled as constants (`DEFAULT_STEP_COUNT`, `MAX_OVERSHOOT` et al., readers' d.ts read) and hidden from the published barrel, so the demo restates them by hand (`:124-125`, `:187`).
- **L3-8 — MINOR → X-W6.d.** The strip's event payload carries a factory closure (`payload: () => EasingPickerValue`), typing the parent's contract by the catalogue's internal shape; every field is readonly — a frozen constant conveys the same guarantee.
- **L-9 — MINOR → X-W6.d.** `isStepsInterval` (`:207-209`) accepts a `mode`/`css` disagreement instead of forbidding it, against the module's own `css`-is-truth law (`:43-45`).
- **L-11 — IDENTITY-FOLDED, not re-booked**: fact-identical to ColorSpaceSelector L-14 ≡ GV-9 ≡ XP-21 ≡ MX-20 (tsconfig↔exports parity, banked NO-WAVE-OWNER, X-W8 declined by name). One identity; the phantom `parsing`/`units` paths and the false "CLOSED 8-key set" comment are that row's evidence, already pinned.

### INFO

- **C-13 — INFO → X-W6 ◆.** `.specimen-strip` applied once (`:84`), styled by nothing — yet it is every axis's probe selector; per PCS-22: land a stable hook before ARIA/class cleanup or the corpus's own oracles break.
- **C-14 — INFO → X-W6.d.** `bezierLiteral` destructures four of an unbounded array (a 3-element input emits `undefined` into CSS text); `stepsLiteral` accepts n ≤ 0; both exported with zero external consumers (≡ first-seal M-ESS-9's deletion row — sequencing-coupled to X-W9, not silently absorbed).
- **L-13 — INFO → NO-WAVE-OWNER (cross-repo; keyframes.js packet).** keyframes.js hard-pins `@mkbabb/value.js: "4.0.0"` exact as a dependency (readers' read of the installed manifests; glass models it correctly as a peer) — guaranteed duplicate on-disk copy downstream; a keyframes-side one-line change.

### Adopted missed findings — round 2 (each re-verified by this seat unless marked identity-credited)

- **N-1 (DU missed #1) — the D6 guard is applied at the WRONG REFERENT: one accent ink certified against ONE surface rung (and the TEXT floor) is painted on THREE other rungs, one of which the seat itself built BECAUSE the raw pane failed contrast — ADJUDICATED MAJOR → X-W6 ◆ (strip/rows halves) + .d (editor half).** My own chain, every hop read this session: `useSpecimenRows.ts:40` `useSafeAccentFn("resting")` → `:69` `safeCss(mid)` with NO floor argument; `TIER_BG_TOKEN` maps `resting → --glass-bg-resting`, `floating → --glass-bg-floating`, `well → --well-bg` — genuinely different referents (`useContrastSafeColor.ts:50-54`); the one resting-certified ink is then published as `--motion-accent` (`GradientEasingEditor.vue:115`) and consumed (a) on the row card (`:262` head glyph — the certified rung, correct), (b) inside `.readout-rail`, which is `bg-well` (`:176`) — a well the seat's own comment (`:167-175`) says was added because the raw pane composited ~2.7:1 — for `.rail-btn--on`/`.rail-tick` (`:290/:293`), uncertified; (c) on the pressed tile's glyph+label (`EasingSpecimenStrip.vue:205/:209`) over `.glass-capsule`'s floating-tinted+warm fill (my dist read), uncertified; (d) inside the authoring stage's repainted `var(--well-bg)` card (`EasingAuthoringStage.vue:94`) via glass's `--easing-curve-accent: var(--motion-accent, …)` fallback chain (readers' bytes). Floor half: `ink.ts:15-16` publishes `TEXT_CONTRAST_FLOOR = 4.5` / `GRAPHICS_CONTRAST_FLOOR = 3`; `safeCss`'s own docstring names the graphics floor for rails (my read) — yet the selected ink, whose dominant consumer is a 1.25→1.75px sparkline stroke, takes the default 4.5 text floor while the 9px label it also inks sits below any type rung (D-41). Mis-rung AND mis-floored, decidable from source; rendered deltas U-11. Cure: one ink per surface (`useSafeAccentFn("floating")`/`("well")` per consumer, `GRAPHICS_CONTRAST_FLOOR` for glyph strokes) — three lines — or a per-surface ink map on the row. **This row also AMENDS first-seal superlative 7**: the certification rigour is real; its referent is wrong for three of four consumers.
- **N-2 (DU missed #2) — every tile press silently destroys the OTHER authoring mode's parameters, and the licensing comment is false — ADJUDICATED MAJOR → X-W6.d, coupled to L3-1.** My own hops: `easingCatalogue.ts:119-126` bezier payloads hardcode `steps: 4, term: "jump-end"`; `:145-153` steps payloads carry `points: [0,0,1,1]` under `:149`'s "steps mode never reads points" — false for this seat, because `GradientEasingEditor.vue:73-76` emits the WHOLE payload and `useGradientModel.ts:134-140` REPLACES all six interval fields (my read), then `EasingAuthoringStage.vue:76-77` binds the interval into `<EasingPicker :model-value>` whose compiled watcher APPLIES incoming `points` via `setHandle` and incoming `steps`/`term` via clamped writes (my dist byte grep this session: `Array.isArray(e.points) … && (P(0,…), P(1,…))`, `Number.isFinite(e.steps) && (F.value = …1..12…)`). Concrete loss, both directions: author `cubic-bezier(0.3, 0.9, 0.7, 0.1)` by drag → press `steps` to compare → flip back = `linear`; tune `steps(7, jump-start)` → press any bezier tile → flip back = `steps(4, jump-end)`. No warning, no undo, and the persisted `css` never recorded the discarded half. Adjacent-but-distinct from C-07 (the inert tile) and D-29 (the second surface) — no axis in eleven documents read the payload's inactive-mode fields. Cure is structural: mode-scoped payloads merged over the current interval, or the model carries a per-mode cache — L3-1's re-typing is the same edit.
- **N-3 (both readers) — the reveal parks the selected tile under the producer's fade feather — ADJUDICATED MAJOR (ruled above) → X-W6 ◆ (≡ first-seal M-ESS-1, elevated; the inertness caveat settled statically by the JS-fallback bytes).**
- **N-4 (LC missed #1) — blast radius — CONFIRMED, IDENTITY ≡ GV-6 → X-W5** (ruled above; carried inside C-01's severity statement).
- **N-5 (LC missed #3) — length-only stop↔interval pairing — CONFIRMED, IDENTITY ≡ GV-41 ≡ GradientStopEditor D-2/G2c → X-W6.a** (ruled above; this component is a fresh WITNESS: pressed tiles and head names jump rows the user never touched).
- **N-6 (DU missed #4) — producer default preset is a pane-killing curve — CONFIRMED, IDENTITY ≡ the R2-1 `initialPreset` exposure vector → glass BH relay rider** (ruled above).
- **N-7 (DU missed #5) — the catalogue's tile ORDER is inconsistent and the eyebrow mislabels: `cubic` is the only family not reading `in / out / in-out`, because `smooth-step-3` (a Hermite smooth-step, `cubic-bezier(0.65, 0, 0.35, 1)`) is filed into `cubic` (`:167`, the comment admits the filing is borrowed from kf) and Object-key order (index 5 of 30 — my dist census) lands it FIRST, so the family reads `cubic → smooth` — ADJUDICATED MINOR → X-W6.d.** Prior censuses counted tiles, families and radii; none censused order. Also feeds C-09: `steps` mixes a parameter label (`n = 4`) with position labels (`start`/`end`).
- **N-8 (LC missed #6) — `valueOrThrow` already exists un-exported — ADOPTED, folded into L3-5 above.**
- **N-9 (LC missed #7) — `SpecimenRow.name` is derivable from `tileId` (`specimenNameFor` IS `tileIdFor(interval) ?? "custom"` — my read of `:228-230`), so every row runs the 27-entry scan twice and persists two fields that can only drift — ADJUDICATED INFO → X-W6.** Cost negligible (C-axis measured 0.00093 ms); the drift channel is the point.
- **N-10 (LC missed #8) — the radius incoherence is fleet-wide (74 non-role rungs vs 27 role tokens) — ADOPTED as the D-37 row's wave-sizing rider (INFO).**
- **N-11 (LC missed #5) — the `/math` barrel violates its own "zero CSS grammar" docstring (`src/subpaths/math.ts:1-5` vs the `cubic-bezier(…)` emitter it re-exports at `:16` — my reads) — ADOPTED as the grep-checkable limb of R4-1/GV-12 (MINOR, folded).**

### Carried first-seal rows (never re-litigated in round 2; re-anchored where this seat re-read the lines)

- **M-ESS-3 — the copy control confirms by mutating its own accessible name (`:181` conditional `aria-label`, `useClipboard({resetMs:1400})`, no live region — my reads this session) — MINOR → X-W6.**
- **M-ESS-4 — the `.strip-row` padding comment (`:130-132`) names a mask-clearance mechanism content padding cannot reach (the mask is PORT-anchored — my dist read) — MINOR → X-W6 ◆.** Matters because it makes "increase the padding" look like a cure for N-3.
- **M-ESS-5 — `--motion-accent`'s fallback register splits inside one card (`:262` → `--muted-foreground` vs `:290/:293` and strip `:205/:209` → `--foreground` — my reads); all four dead today (R4-4 kill) but declared and disagreeing — MINOR → X-W6.** R4-4's INFO residue (delete `SpecimenRow.ink`'s `| null`, the `:115` ternary's false branch, the CSS fallbacks) folds here.
- **M-ESS-6 — producer sticky-hover geometry: `.glass-capsule-hover:hover { scale: 1.015 }` unguarded (my dist read this session; PRM-reduce arm present, no quarantine conflict) — MINOR → GLASS-OWNED (bank alongside MT-F026, BH relay).** Composes with C-17 on the same touch gesture.
- **M-ESS-8 — the catalogue's failure mode is an IMPORT-TIME throw (`SPECIMEN_FAMILIES = buildFamilies()` at module eval, `:198`; `easingValue` throws `:103`) that escapes any pane boundary — MINOR → X-W6.d (IDENTITY: ≡ GV-36; a construction test joins the C-12 gate).**
- **M-ESS-9 — `bezierLiteral`/`stepsLiteral`/`SpecimenFamily` exported with zero external consumers — INFO → X-W6.d, sequencing-coupled to X-W9 (subsumes C-14's disposition).**
- **M-ESS-10 — the host docblock's instantiation law is falsified by its own template ("v-show, never v-if" at `GradientEasingEditor.vue:17` vs `v-if` at `:208` — my reads this session) — INFO → X-W6.**
- **C-12 (first-seal roster, re-affirmed by LC this round) — MAJOR → split: o17 re-key + falsifier → X-W1; born-RED unit invariants → X-W6.** The entire unit gate is `> 20`-of-27 + a NaN-substring sweep (`test/gradient-v4-consume.test.ts:56-58`, read verbatim TODAY — 7 tiles of slack conceal C-06's six drops); the one e2e oracle pressing a fatal tile (`:117/:189`) is gated behind `discloseAuthoring`'s dead `svg[role='img']` selector (`:51`) — dead by C-05, so the presses never execute. The coverage story shares a root cause with the defect it was written to catch.

## Killed-claims register (every kill re-derived by this seat before sustaining)

1. **D-01 pass-1 BLOCKER ("the canon forbids the strip by name") — KILLED (DU's kill sustained on my own canon reads, ruled above).** The canon names the specimen strip as a legitimate support member (VISUAL-CONSTITUTION.md:50) and the forbidden reduction's referent is THE CURVE (:210). Substance survives as D-01-rescoped.
2. **D-02 ("the entire selected register rides a producer-private attribute whose CSS does not exist") — KILLED (DU's three-error refutation sustained).** My reads: the seat's OWN `[data-state="on"]` scoped CSS (`:204-211`) paints; `data-state` is reka-ui's public state emission (`dataState = computed(…)` in Toggle.js — my grep) which glass's chip deliberately RESERVES (`$attrs` filter — my grep) and glass's own orphaned sheet styles; the prescribed `is-pressed` class would duplicate standard state and diverge the day M3 lands. The durable half is C-03, filed separately.
3. **R4-1-as-BLOCKER (the r4 document's grade + "the grammar owner disagrees" diagnosis) — KILLED (both readers; ruled above).** Zero callers; valid-if-verbose CSS; the symbol lives in a barrel that disclaims grammar. Facts survive at MINOR ≡ GV-12.
4. **R4-2's evidence limb ("one declaration, zero consumers, in either repo" for `--radius-strip`) — KILLED, on this seat's own verbatim re-run of the r4 command: TWO lines, not one.** The consumer is `.segmented-tabs--vertical` in `segmented.css`, which `index.css` imports (my read). The demo half (0 hits) is true; the adoption cure survives STRENGTHENED (producer precedent exists). A verbatim-looking probe transcript that omits a matching line is the exact failure mode this program's re-reads exist to catch — recorded as an evidence-quality event against the r4 axis.
5. **R4-3(b) (the reveal is RTL-unsound for lacking `normalizeHorizontalScrollLeft`) — KILL SUSTAINED (first seal, re-derived).** `:62-76` computes a physical delta between two `getBoundingClientRect()`s and applies it via `scrollBy` — no absolute `scrollLeft` read exists to normalize; correct under RTL by construction.
6. **R4-4 (the `var(--motion-accent, var(--foreground))` fallback "silently discards the D6 certification") — KILLED (both readers; my reads concur).** `safeCss(css, floor?): string` returns `certifyAccentInk(…)` unconditionally (`useContrastSafeColor.ts:353-359`); no null path exists; `row.ink` is never null; the fallbacks are DEAD CODE, not a live bypass. Residue folds into M-ESS-5. **The real certification defect at this seat is N-1 — a different claim, which R4-4's fixation on the fallback obscured.**
7. **D-39 item 2 ("no scroll affordance at all"; `offsetHeight − clientHeight = 0` generalized) — KILLED (DU's kill sustained + my own greps).** Zero scrollbar-suppression rules in the strip or `.fading-scroll` (my grep TODAY); the zero delta is macOS overlay-scrollbar behaviour, not a component property; and the 16px feather affordance EXISTS in every engine (my JS-fallback read). The defensible weak-affordance row is D-42's wording and lives there.
8. **D-16 / §0.3 ("heads the overflow list in 10 of 10 matrices") — KILLED (the L-r4 adjudication's refutation sustained).** The harness's `bleeding` detector flags any child of an `overflow-x: auto` port wider than the port — the DEFINED state of this component; the route's own `overflowX` is 0 in all four matrices; §0.3 records its own contradiction (the strip flagged while off-screen). Harness rows, not product rows.
9. **C-15 / L-10 as "STILL RED" — KILLED (LC's ledger-hygiene kill sustained).** The array-getter watch (`:50-51`) allocates a tuple per re-evaluation and produces zero extra callbacks; the source documents themselves claim no defect. INFO-as-style; recorded so no repair wave inherits a phantom RED.
10. **L3's "0 → 27 duplicate `data-specimen` ids" limb — KILLED (LC's kill sustained).** A data attribute is not an id; no validity or a11y violation; the real consequence (wrong-strip queries) is already cured by the subtree scoping at `:55-57`. L-4/C-11's disposition unaffected.
11. **D-42's press-scale limb ("NO surface delta today") — KILL SUSTAINED (first seal, re-derived TODAY).** `.glass-capsule-hover:not([data-press-armed]):active { scale: var(--scale-press-sm, 0.97) }` ships in the IMPORTED sheet (my grep); `data-press-armed` emitters exclude Chip (first-seal census), so the `:not()` is satisfied and press feedback EXISTS. What is missing is the SELECTION punch — C-03's freight.
12. **Late-pass header line counts (216/231/117) — KILL SUSTAINED (first seal).** 215/230/116 by wc; drift only; every line citation this seat resolved TODAY still lands exactly.

## Superlatives (provenance preserved; two amended)

1. **The reveal's PRM gating is load-bearing, not ceremonial** (`:74`): the `scrollBy` options argument overrides CSS `scroll-behavior` by spec, so both app-wide guards are structurally powerless here — one line stands between a `reduce` user and a full-width animated pan; the single case central policy cannot reach, implemented locally and correctly.
2. **The `scrollIntoView` refusal (`:37-46` + `:55-76`)** — names the exact defect it prevents (the O-19 flat-netting root cause: `block:"nearest"` walks EVERY scrollable ancestor), then makes the bug unreachable by construction: one axis, one element, subtree-scoped query (necessary — sibling rows mount hidden twins, my template read). Attacked across both rounds (the RTL kill above IS an attack); it held every time. Its one genuine flaw is N-3's zero inset — a two-expression fix that leaves the architecture intact.
3. **The literal-mint law (`easingCatalogue.ts:38-56`) is byte-faithful and proven right retroactively by R4-1/GV-12**: identity is string equality against the persisted `css`; both independent consumers converged on `+n.toFixed(3)`; the outlier is the library function with the unit tests. The seat picked correctly — which is why retirement-or-move, not adoption, is the X-W9 cure.
4. **One glyph painter, zero re-derived curve maths** (`glyphPath` — one definition, three seats, my grep concurs with both rounds). The boundary that made the C axis's NaN-freedom a negative proof instead of a defect.
5. **The library is total and blameless at every seam the consumer breaks**: uniform `Result` factories; `err`, never throw, in `color/operations` (my read). Every crash in this corpus is a consumer demotion — and the ErrorBoundary forwarding the library's own error code verbatim (`ErrorBoundary.vue:61`) is what let four passes name the predicate exactly.
6. **Glass's `ChipProps`/`$attrs` discipline makes the wrong call unrepresentable** — the producer got the CONTRACT right; only its stylesheet is unreachable (C-03) — precisely why a local CSS patch would be the wrong repair.
7. **The per-specimen ink is genuinely certified — AMENDED by N-1**: the OKLab certification chain is real rigour (my read), but its referent surface is wrong for three of its four consumers and its floor is wrong for its dominant consumer. The rigour survives; the praise is now conditional on the rung fix.
8. **The empty state is impossible by construction** (`GradientVisualizer.vue:238` + `useGradientModel.ts:123` — my reads): two independent upstream guards; nothing to design or test.
9. **The seat's own contrast reasoning shows its work** (`:180-191`: prior value, measured failure, design consequence, chosen cure — my read): the standard the selected-ink path should have met, and did not (N-1).
10. **Round-2 evidence discipline (about the corpus, recorded for the fleet)**: DU reproduced the C axis's grid table offline to six decimals, rebuilt D-41's type numbers from raw tokens, and reconstructed D-21's 43.8px to the tenth without a browser; LC re-derived the back-family sweeps against dist with clean controls. When these axes say a number came from a probe, it did. And the corpus self-corrects in public against its own interest (C-5/C-6/C-7 retire earlier claims; D-36 narrows D-30) — every correction narrows. That discipline is why most of what survived these re-reads survived this seat.

## UNPROVEN-NEEDS-LIVE residue (→ the visual audit, task inputs)

- **U-1** — every rendered-pixel contrast figure (selected↔unselected 1.16/1.11:1 · eyebrow ratios · dark resting label straddling 4.5). Directions follow from source; the honest framing stands: an ink derived from the user's own gradient midpoint is unbounded by construction, not passing.
- **U-2** — D-38's shadow-causality A/B (ΔL 0.084 light vs 0.0131 dark). Mechanism source-certain (unconditional capsule shadow); the injection experiment and decoded luminances are live-only.
- **U-3** — D-40's authoring-stage geometry (166.7px plot, 59.3% letterbox, 71.8% empty well): mechanism certain (dead law + producer inline clamp); `38cqi` needs a container context.
- **U-4** — D-39's rigidity table (`scrollWidth` 1482 constant across five arms): the absence of any responsive law is source-certain; the port widths and visible-tile counts are measurements.
- **U-5** — D-37's tile-geometry census (44/45.2px histogram, chord table, 11-of-27 negative label clearance) and C-11's DOM-mass ratios: causes source-certain; arithmetic rendered.
- **U-6** — N-3's fraction (16px feather over ~44px tiles ≈ 36% of the just-selected tile; 25 of 27 tiles affected; two end-gate exemptions). **The companion question from round 2 — whether the feather paints at all without `animation-timeline` — is SETTLED STATICALLY this session (JS fallback in the compiled composable); only the fraction wants a frame.**
- **U-7** — C-01(b)/(c) driven repros (producer preset menu; two keystrokes from default): premises verified in dist; repros live-only; NOT needed to carry the conclusion (the ghost path is static and stronger).
- **U-8** — C-16 keep-in-view flake (1 observation vs 9 green re-runs): unresolved; harden with `{flush:"post"}` on hygiene, not on a reproduced defect.
- **U-9** — D-41's rendered type sizes (16.4/14.384/9px at the served viewport) — the clamp arithmetic is token-certain; the serve is a measurement.
- **U-10** — OM-4's owner-visible grey band: wholly the shadow union, or partly the capsule warm fill at 27× overlap? Attribution needs the pixel diff.
- **U-11** — N-1's rendered deltas: the resting-certified ink's actual contrast on the capsule fill (floating+warm) and on `--well-bg`, both schemes, across representative gradient midpoints. The mis-rung is source-decided; the magnitude of harm is live.
- **U-12** — D-31's denominator: the probes measured a shadow on the interval row card that NO rule this round or the readers could locate declares (`:114` is radius+border only). Either a probe mis-attribution or an unlocated rule — and if the card is genuinely flat, C-04's violation is LARGER, not smaller.
- **U-13** — the dark-arm "cyan with no other coordinate in the scheme" claim: a scheme-wide hue census; the mechanism (ink = arbitrary user colour) is source-certain, the uniqueness is not.
- **U-14** — the live computed value of `--radius` / `--radius-input` in the served cascade (the C-02(b) mechanism is source-certain; the resolution is a cascade fact wanting one `getComputedStyle` read).

## Closing verdict

**DEFECTIVE — BLOCKER SUSTAINED, WIDENED, AND NOW FULLY CROSS-REFERENCED.** The pane-killing predicate (`range(fn) ⊄ [0,1]`) is real, library-blameless, reachable through three consumer seams including plain pointer hover, and its blast radius is the whole two-pane workspace plus the user's document (≡ GV-6). Thirty-nine axis rows survive adjudication (1 BLOCKER · 21 MAJOR); twelve kills are sustained on this seat's own re-derivations (two of them evidence-quality events against the corpus's own r4 axis); eleven round-2 misses join the record — **two genuinely new MAJORs no prior seat held (N-1 mis-rung certification · N-2 payload clobber)**, one elevated MAJOR settled statically (N-3), three identity-credited to sibling adjudications rather than double-booked, and the first seal's eight carried rows keep their ids. Routing: X-W6 carries the wave-shaped mass — **with the re-verified NAMED GAP: W6 must adopt `EasingSpecimenStrip.vue` and `useSpecimenRows.ts` into its file lists or the ◆ rows degrade to NO-WAVE-OWNER** — X-W9 the serializer decision (≡ GV-12), X-W1 the oracle re-key and test-tree hygiene, X-W5 the boundary/blast-radius limb (≡ GV-6), and five producer rows ride the standing glass-ui BH relay (M3 · chip-rung · `:presets` door · fade/`scrollToActive` · easing constants), banked per MT-F014/MT-F026. Fourteen residue rows go to the visual audit; one first-seal residue is settled statically. Dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this file.

— end of apotheosis, second seal. The eleven challenge documents and both round-2 re-reader returns remain immutable beside this file per E-1/E-3; the first seal's surviving substance is carried above under original ids.
