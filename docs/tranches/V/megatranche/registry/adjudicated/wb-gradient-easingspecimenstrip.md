# wb-gradient-easingspecimenstrip — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator (the incredulity seat) · **Date**: 2026-08-09 · **HEAD**: `54a97489` · branch `tranche-u`.
**Corpus**: eleven challenge documents (C passes 1–4 · D passes 1–4 · L r2/r3/r4; r2-and-later govern) against `demo/workbenches/gradient/GradientVisualizer/easing/EasingSpecimenStrip.vue` (215 lines) + its catalogue/row/CSS dependency chain and the installed glass-ui 7.0.0 producer surfaces.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted; every kill was re-derived by this seat with its own instruments; the one head-on verdict conflict (C-01(d): DU CONFIRMED vs LC KILLED) was ruled by my own trace of the tree, not by picking a reader.
**Arbiter's own instruments (all static + grep/node against source and installed dist; ZERO browser probes — probe parsimony, per the ColorSpaceSelector precedent)**: whole-file reads of `EasingSpecimenStrip.vue` (215 L, trailing-newline checked), `easingCatalogue.ts` (230 L), `EasingAuthoringStage.vue` (116 L, targeted), `GradientVisualizer.vue:55-149`, `GradientStopEditor.vue:60-100`, `GradientEasingEditor.vue:14-215 + :262/:274/:290-293`, `useSpecimenRows.ts:35-75`, `useGradientInterpolation.ts:27-40`, `useGradientCSS.ts:38-80/:100-120/:175-215/:245-280`, `src/easing.ts:30-150` (30-key PRESETS census re-counted; `CubicBezier`/`steppedEase` validation read; `steppedEase(1,"jump-start") ≡ 1` re-derived arithmetically), `src/foundation/math.ts:105-125`, `demo/color-session/ink.ts:128-145`, `useContrastSafeColor.ts:350-362`; installed glass 7.0.0: `dist/styles/glass/glass-chip.css` (exists, 3133 B; `grep -rln "glass-chip.css" dist/` → **zero importers**), `dist/styles/glass/glass-capsule.css` (`:hover { scale: 1.015 }` unguarded · `:not([data-press-armed]):active { scale: var(--scale-press-sm, 0.97) }` · PRM-reduce arm zeroing both · unconditional pill radius + floating shadow), `dist/styles/index.css` (`@layer theme, base, components, utilities` + `@import "./components.css" layer(components)`), `dist/styles/components.css` (`:root { … --radius: 0.25rem }`), `dist/styles/theme/radius.css` (`@theme … --radius: 0.625rem; --radius-input: var(--radius); --radius-strip: 0.75rem`), `dist/components/tabs/styles/segmented.css` (`.segmented-tabs--vertical { --bouncy-slider-radius: var(--radius-strip) }`), `grep -c 'role:"img"' dist/easing.js` → 0, `dist/easing.js` sr-only "-0.6 to 1.6" present, chip `$attrs` filter `role|aria-pressed|data-state|name|required` (+ a `tabindex` variant), `SHAPE.cell = "glass-chip--cell flex-col gap-1.5 px-2 py-2.5 text-micro"`, `FadingScroll.vue.d.ts` `ariaLabel?/ariaLabelledby?` + `tabindex: "0"` in the compiled render, `base-misc.css` physical `to right` mask + scroll-driven `gl-fade-*` keyframes + `tokens/offsets.css` `--fade-scroll-width: 1rem`, `data-press-armed` emitters = exactly {button, dark-mode-toggle, dock}, package exports `./toggle-group` present; reka-ui `Toggle.js` emits `"data-state": dataState.value`; repo censuses: `shape="cell"` = 1 site, `.specimen-strip` = 1 hit (the attribute), `cubicBezierToString` homes = exactly {src/foundation/math.ts, src/subpaths/math.ts, test/math.test.ts, test/v4-c1.test.ts}, `bezierLiteral|stepsLiteral|SpecimenFamily` external consumers = 1 prose comment (`e2e/smoke/views/gradient.spec.ts:287`), router `easing` routes = 0; `test/gradient-v4-consume.test.ts:55-57` and `o17-easing-composition.spec.ts:48-51/:111/:117/:121/:143/:189` read verbatim; X wave ownership resolved from `docs/tranches/X/waves/` (W6 file list, W9 file list, W1 oracle scope) + `registry/CARRY-CUT-LEDGER.md`.
**Motion/PRM law**: my grep over all 38 rows of `audit/codex-provenance/motion-quarantine.md` — **zero M-xx rows name this component** (its corpus is workflow-authored, not among the 46 REPORT-AUTHORED files), so no FALSE row exists to carry. Every PRM claim in this record was re-derived against dist bytes: the capsule hover/press scales carry a real `prefers-reduced-motion: reduce` arm; the fade mask's `gl-fade-*` keyframes are scroll-driven (`animation-timeline: scroll(self inline)`) — the MQ-1 clamp blind spot applies to them, noted for completeness, but they animate a mask inset, not spatial motion. The one live PRM decision (the reveal's `scrollBy` `behavior` option) is a SUPERLATIVE, below.
**Status verbs**: every disposition here is **ADJUDICATED**, never VERIFIED — nothing in this file stamps a wave.

## Scoreboard

**22 axis rows CONFIRMED (1 BLOCKER · 15 MAJOR · 4 MINOR · 2 INFO) · 6 kills SUSTAINED (every one re-derived by this seat) · 1 verdict-level conflict RESCOPED (C-01(d)) · 9 missed-by-the-axes findings ADOPTED (readers' union, all re-verified; the third-seam BLOCKER folded into C-01) · dispositions: X-W6 ×20 (two files under a NAMED GAP) · X-W9 ×1 · X-W1 ×1 (split) · GLASS-OWNED/BH ×4 · 10 UNPROVEN-NEEDS-LIVE residue rows → visual audit.**

Blocker-weight survivor: **C-01, widened** — the Gradient pane is torn down by any easing whose range leaves [0,1], through **three** consumer seams that demote the library's `Result` to a bare render-path `throw` — and the third seam (`colorAtPosition` → the stop-editor's hover ghost, continuous `t`) means an interval already carrying a `back` curve kills the pane on **plain pointer movement over the gradient bar**, with the easing accordion closed. The library is blameless at all three (`src/color/operations.ts:65,:90` return `err`, never throw).

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-01(d)** (grid-dependence law) | CONFIRMED — "the grid formula is byte-exact … the fatal set genuinely moves with the user's stop count" | KILLED — "refuted as a general law by a third seam … `cubic-bezier(0,0,1,1.1)` is fatal on the ghost path, not a survivor" | **RESCOPED — both halves survive, neither framing does.** | My own trace: `useGradientCSS.ts` `COALESCE_RESOLUTION = 32` and `stepsPerInterval = max(2, round(32/(stops.length-1)))` are exactly as DU verified — the grid law is TRUE **of the two seams it measured**. But `GradientVisualizer.vue:64-88` `colorAtPosition` evaluates `easingFnOf(interval)(t)` at `:78` with `t` derived from pointer position quantized only to 0.1% (`GradientStopEditor.vue` `getPosition` — `Math.round(…*10)/10`), i.e. up to ~1000 distinct `t` per interval — dense enough that any curve whose range leaves [0,1] over a non-degenerate excursion has fatal samples. So "`cubic-bezier(0,0,1,1.1)` survives" is FALSE of the product, and the product-level law is the simple one: **fatal iff `range(fn) ⊄ [0,1]`**. DU's carried conclusion (no enumerate-the-bad-curves cure is sound) SURVIVES and is strengthened; DU's "byte-exact" cell survives seam-scoped; LC's kill of the survivor-claim is SUSTAINED. |
| **C-01 cure** (the governing C doc's prescription) | carried inside the C-01 confirmation ("a `rampProgress(fn, t)` beside `easingFnOf` … so both seams inherit totality, then delete both `throw` sites") | missed-row: cure UNSOUND AS WRITTEN — three call sites in three modules; a sibling helper confers no inheritance; the C-12 gate sweeps only `serializeCoalescedGradient` and passes green over the third throw | **LC wins — the cure is re-homed.** | My reads: the eased `t` becomes a mix progress at exactly three sites — `useGradientCSS.ts` grid loop (`:197-208`), `useSpecimenRows.ts:52-59` (`fn(0.5)`), `GradientVisualizer.vue:78-85` (continuous). A helper "beside `easingFnOf`" edits none of them by existing. Per the ColorSpaceSelector addendum item 2 (**the cure is adjudicated separately from the defect**): totality must land **inside `interpolateStopColors` and `sampleCoalescedStops`** — the two functions where progress meets `mixColors` — or all three call sites are edited and the gate sweeps `colorAtPosition` explicitly. The C doc's own gate snippet (sweeping `serializeCoalescedGradient` at 2/3/4/5/9 stops) is necessary but NOT sufficient; adopted with the third-seam arm added. |
| **C-17/D-03 grade** (hover erases selection ink) | MAJOR ("one of only two selection signals the control owns") | MINOR | **MAJOR-WHILE-C-03-STANDS, demoting to MINOR the day glass ships the chip sheet.** | My read of `:204-214`: both rules are (0,4,0) after scope-attribute injection; `:212` is later; no `(hover:hover)` guard. With `glass-chip.css` orphaned (my grep: zero importers) the `[data-state="on"]` accent wash never paints, so the accent label + heavier glyph stroke ARE the selection register — hover deletes half of it, and sticky touch-hover makes the deletion persistent on mobile. The grade is conditional and says so; the coupling to C-03 is recorded so neither wave cures blind. |
| **R4-1 grade** (the byte-wrong dead serializer) | MAJOR (listed) | MAJOR (explicit dissent from r4's BLOCKER: "no runtime reachability … a semver-scheduling problem, not a wave blocker") | **MAJOR — both readers sustained against the r4 document.** | My census: `cubicBezierToString` lives in exactly four homes (definition `src/foundation/math.ts:113-120` with the pointless `formatNumber` closure, re-export, two test files incl. the `v4-c1` census pin) and has **zero** production callers. Dead published surface cannot block a wave; what survives of r4's BLOCKER framing is the SEQUENCING fact — retirement is a public-surface change and must ride a version cut — which is routing, not severity. Routed to **X-W9** (owns `src/foundation/math.ts`, `src/subpaths/math.ts`, `test/math.test.ts`; its lane .c names foundation/math). |
| **D-42** (press feedback) | KILLED the "NO surface delta today" row — press scale ships in the IMPORTED `glass-capsule.css`; the orphaned rule is a SELECTION scale, two different scales conflated | CONFIRMED "D-42-disabled" (the `disabled` door never passed) — a different sub-row | **NO CONFLICT — both adopted.** | My dist reads settle DU's kill: `.glass-capsule-hover:not([data-press-armed]):active { scale: var(--scale-press-sm, 0.97) }` is in the imported sheet; `data-press-armed` is emitted only by {button, dark-mode-toggle, dock} (my grep), never Chip, so the `:not()` is satisfied and press-down feedback EXISTS. The orphaned `.glass-chip--interactive` scale keys on `--chip-flood-t` — a selection punch, dead with C-03. The state-coverage table's active/pressed row is corrected; what is missing is the selection punch (C-03's freight). LC's disabled-door INFO row stands with its own correct disposition (disabling is NOT the C-01 cure — it would mask the defect). |

## Adjudicated defect roster

Line anchors are HEAD `54a97489`; re-resolve before executing any cure (the D-19 lesson). **NAMED GAP (applies wherever marked ◆)**: X-W6's file list names `easingCatalogue.ts`, `GradientEasingEditor.vue`, `EasingAuthoringStage.vue`, `useGradientCSS.ts`, `useGradientInterpolation.ts`, `GradientVisualizer.vue`, `GradientStopEditor.vue` — but **neither `EasingSpecimenStrip.vue` nor `useSpecimenRows.ts` appears in any X wave's file list** (my grep across `docs/tranches/X/waves/*.md`; sole mention anywhere is W1's B13 exports row). W6's mechanism item 4 ("Easing instrument coherence", CC-060/CC-063) owns the surface class, so rows on those two files route **X-W6 with the NAMED GAP: W6 must adopt both files explicitly or the row degrades to NO-WAVE-OWNER at execution** (the PaletteColorStrip PCS-1/PCS-19 precedent).

- **C-01 (+D-18, + LC's third seam folded in) — ADJUDICATED BLOCKER → X-W6.** Three render-path seams demote the library's total `Result` to a bare `throw`: `useGradientInterpolation.ts:37` (reached by `useSpecimenRows.ts:52-59` `fn(0.5)` for every row, AND by `GradientVisualizer.vue:78-85` with continuous pointer-derived `t` via `:139` `:color-at` → `GradientStopEditor.vue` `ghostColor` computed on every bar `pointermove`, AND by `onAddStop` bar-click) and `useGradientCSS.ts:~206-208` inside the grid loop (reached by `serializeIntervalRamp`/`serializeRailRamp`/`serializeCoalescedGradient`). The three `back` presets (`src/easing.ts`: `[0.6,-0.28,…]`, `[0.175,0.885,0.32,1.275]`, `[0.68,-0.55,0.265,1.55]`) are the shipped instances; the predicate is `range(fn) ⊄ [0,1]` (ruled above). Reachability: a tile press, the producer's own preset menu (all 30), two `Shift+ArrowUp` from default (sr-only y-domain "-0.6 to 1.6" read in dist), or **hover over the gradient bar with a back-eased interval already present — no press, accordion closed**. Cure re-homed (ruled above): totality inside `interpolateStopColors` + `sampleCoalescedStops`; delete all three throws as unreachable; gate sweeps all three seams including `colorAtPosition`. W6's mechanism item 1 already names "the parallel `intervals` array + its watcher + three throws" — this row must be reconciled with that text so ONE totality decision lands, not two.
- **C-05 / D-40 — ADJUDICATED MAJOR → X-W6 (file in list) + one glass hook ask (BH relay).** The "zero letterbox" law is dead: `EasingAuthoringStage.vue:48-50` queries `svg[role='img']`; glass 7's compiled canvas emits `role: "group"` (`grep -c 'role:"img"' dist/easing.js` → 0, my run). `vbRatio` frozen at `ref(1.2)` (`:45`); `onMounted`/`watch`/both rAFs dead; five `:deep` declarations (four `!important`) inert; the producer's inline `block-size: clamp(200px, 38cqi, 320px)` + `aspect-ratio: 1` govern unopposed. Cure: stable canvas hook (`data-easing-canvas`) as a glass ask; delete the inert apparatus. Geometry numbers (166.7px plot, 59.3% letterbox) stay UNPROVEN-NEEDS-LIVE (U-3); the mechanism is certain.
- **C-12 — ADJUDICATED MAJOR → split: o17 re-key + falsifier → X-W1; the born-RED unit invariants → X-W6.** Unit coverage is two slack assertions (`test/gradient-v4-consume.test.ts:55-57`, read verbatim: `> 20` against 27, plus a NaN-substring check) — 7 tiles of slack conceal C-06's six drops and all three lethal tiles. The one e2e oracle that presses `ease-out-back` gates BOTH its tests (`:111`, `:143`) behind `discloseAuthoring`'s `svg[role='img']` visibility assertion (`:48-51`) — dead by C-05, so the presses at `:117/:121/:189` never execute. Adopted gate: the C doc's total-domain sweep (stop counts varied) + exact-equality catalogue + LC's import-time construction test + the third-seam arm. X-W1 owns oracle liveness and the mandatory falsifier (CC-031).
- **C-06 / L-1 / L3 — ADJUDICATED MAJOR → X-W6.** `FAMILY_ORDER` (`easingCatalogue.ts:174`) used as a filter (`:191`) silently drops 6 of 30 published presets (quart×3, quint×3 — built at `:181`, discarded at `:191`; my 30-key census of `src/easing.ts` PRESETS confirms). Loss propagates into identity (`SPECIMEN_TILES` flatMap `:201` → `tileIdFor` `:219-223`), so six first-class library presets render as `custom` with no tile pressed, while the producer's own menu still offers all 30. The module docstring (`:13-15` "the SAME catalogue … never a second mint") is false on both clauses. Cure: order-hint, never filter; exact-equality gate.
- **C-03 / L-5 / R4-M3 — ADJUDICATED MAJOR → GLASS-OWNED (BJ; bank with M3/I-9; BH relay per owner edict; NO local patch — MT-F014).** `dist/styles/glass/glass-chip.css` ships (3133 B, my `ls`) and nothing imports it (my `grep -rln` over dist → zero); `glass.css`'s 18-import list omits it. Everything selectable about the tile — `--radius-card` cell radius, the `[data-state="on"]` accent flood, `--chip-flood-t`, the coarse-pointer 2.75rem floor, `prefers-contrast: more`, `[data-disabled]` — is unreachable; the imported `glass-capsule.css` pill radius + floating shadow win by default. The strip's `:160-162` docblock is false in the shipped build and must be corrected when the producer fix lands.
- **C-04 — ADJUDICATED MAJOR → X-W6.** The in-plate tile carries `.glass-capsule`'s unconditional floating shadow (`box-shadow: var(--glass-rim-top), var(--glass-rim-bottom), var(--glass-shadow-floating)`, my dist read) inside a card whose declared law is "flat on the plate … no shadow" (`GradientEasingEditor.vue:108-110`, my read). Structural contradiction certain; ΔL numbers U-4.
- **C-02(b) / L-12(2) — ADJUDICATED MAJOR → GLASS-OWNED (BH; app-wide).** Three values for one token, decided by cascade layer: `@theme` `--radius: 0.625rem` + `--radius-input: var(--radius)` (radius.css, my read) vs `components.css` `:root { --radius: 0.25rem }` (my read) imported `layer(components)` — strictly later than `theme` (index.css layer order, my read) — vs `demo/DESIGN.md:195`'s documented 8px. Consumed here at `GradientEasingEditor.vue:274` `.rail-btn { border-radius: var(--radius-input) }` (my grep). No consumer re-declaration (MT-F014).
- **C-07 — ADJUDICATED MAJOR → X-W6.** Surjective identity into a one-way toggle: `tileIdFor` returns generic `"steps"` for ANY non-literal steps interval (`:219-223`) while that tile's literal is fixed `steps(4, jump-end)` (`:187`), and `onTileToggle` drops `on === false` (`EasingSpecimenStrip.vue:33-35`) — which is exactly what reka's controlled `Toggle` reports for an already-pressed tile (data-state emission read in `node_modules/reka-ui/dist/Toggle/Toggle.js`). The one control naming `steps(4, jump-end)` cannot emit it. Cure: emit on press regardless of `on`.
- **C-08 / R4-3(a) / D-24 — ADJUDICATED MAJOR → X-W6 ◆ (+ the semantics half is a glass ask).** Glass's focusable scroll port (`tabindex: "0"` unconditional, my dist grep) is nameless and roleless because the seat passes neither `ariaLabel` nor `ariaLabelledby` (both published with docstrings, my d.ts read; consumer `:84` passes nothing) and instead names the non-scrollable inner `.strip-row` (`:87-89`). Sharpening sustained: the consumer cannot own tile semantics either — chip's `$attrs` filter strips `role|aria-pressed|data-state` (my dist read) — so that half rides the BH relay.
- **L3-4 — ADJUDICATED MAJOR → X-W6 ◆ + glass ask.** A 27-way single-select hand-built from 27 independent toggles inside a bare `role="group"` while glass publishes `./toggle-group` at a real exports key (my package.json read). The composite cure is half-blocked by the same `$attrs` filter — genuine producer ask, recorded with C-08's.
- **C-17 / D-03 — ADJUDICATED MAJOR-while-C-03-stands (ruled above) → X-W6 ◆.** `:212-214` beats `:208-211` at equal specificity by source order; no `(hover:hover)` guard; sticky on touch.
- **D-17 — ADJUDICATED MAJOR → X-W6 ◆.** The `custom` state has no rendering: `tileIdFor` → null, strip early-returns the reveal (`:53`), no chip pressed, while the row head prints `custom` — reachable via C-06's six drops AND free authoring. The strip's `:30-32` docblock asserts the state away.
- **D-09 — ADJUDICATED MAJOR → X-W6 ◆, under X-W10's design law.** Bare polyline in an empty unit viewBox — no frame, baseline, or axis. My own derivation: `steppedEase(1,"jump-start")` is the constant 1 (offset=1, jumps=1, clamp), so `step-start`'s portrait is a horizontal rule at the top of an invisible box — unreadable against the component's own `:184` shape-recognition thesis.
- **R4-1 — ADJUDICATED MAJOR (grade ruled above) → X-W9.** Published, census-pinned (`test/v4-c1.test.ts:334`), zero-caller `cubicBezierToString` with a `toFixed(2)` law byte-incompatible with both consumers' `toFixed(3)` (29/30 presets mismatch — LC's independent re-measure, spot-consistent with my reads). Retirement is the cure and is a semver event; the demo's minters (`bezierLiteral`/`stepsLiteral`) die in X-W6 (see the dead-exports row) — sequencing coupled, not merged.
- **D-04 / D-33 — ADJUDICATED MAJOR → X-W6 ◆ under X-W10's law.** Seven `border-left`-family rules paint a grouping line (`:143-146`, my read) in a composition whose binding inventory sets retained lines to `none` (OPTICAL-BENCH-COMPOSITIONS `:78-79/:90`, DU's verbatim re-read adopted), atop a 28px-vs-4px spacing ratio that already carries the grouping.
- **D-05 / D-34 — ADJUDICATED MINOR → ours X-W6 ◆; the producer mask half GLASS-OWNED (bank with M3).** Physical `border-left/padding-left` in a direction-reversing row (VISUAL-CONSTITUTION `:151` logical-axis law); producer's `.fading-scroll--x` mask is physical `to right` driven by logical signals (my dist read of base-misc.css + the scroll-driven keyframes).
- **D-01 — ADJUDICATED MAJOR → X-W6 (the Easing instrument as a routed scene; canon re-author coupling to X-W5).** The canon'd peer `/easing` route with the 19–22rem curve protagonist does not exist — my router grep: zero `easing` routes. The strip is support for a protagonist that was never cut. W6's success criterion ("each live instrument — … Easing … owns its own stage … as a routed scene") is the owner surface.
- **D-20 / L-6 / C-10 — ADJUDICATED MAJOR → glass ask (specimen/xs rung) via BH relay + X-W6 ◆ consumer half.** The seat re-declares four of the producer cell recipe's declarations and adds `min-width` (`:163-170` vs `SHAPE.cell`, my dist read); no SIZE rung reaches specimen scale, so the variant genuinely belongs upstream — "ask glass" is the correct disposition, not `size="sm"`.
- **C-11 — ADJUDICATED MINOR → X-W6.** One full 27-Chip strip + `useMediaQuery` listener mounted per interval forever (`v-for` → `v-show`, never `v-if` — `GradientEasingEditor.vue`, my read); also what forces the querySelector scoping the strip documents at `:39-41`.
- **C-09 / D-25 — ADJUDICATED MINOR → X-W6 ◆.** WCAG 2.5.3 on exactly 1 of 27: `:aria-label="tile.id"` (`:105`) vs visible `n = 4` on the generic steps tile (`:187`). Both readers walked all 27 independently; my spot-walk of the label grammar (`familyLabelFor`) concurs.
- **D-42-disabled — ADJUDICATED INFO → X-W6 state enumeration.** The producer's `disabled` door exists and is never passed while 3 of 27 choices destroy the pane; recorded as an unenumerated state, with LC's own caveat sustained — disabling is NOT the C-01 cure.
- **C-13 — ADJUDICATED INFO → X-W6 ◆.** `.specimen-strip` is applied once and styled by nothing (my count: 1 hit in demo/) — yet it is every axis's probe selector. Per the PCS-22 precedent: land a stable hook (`data-slot`) before any ARIA/class cleanup, or the corpus's own oracles break.

### Adopted missed findings (readers' union, each re-verified by this seat)

- **M-ESS-1 (DU) — the reveal parks the selected tile INSIDE the producer's fade feather, in LTR — ADJUDICATED MAJOR → X-W6 ◆.** `:65-70` computes EXACT edge alignment; the producer feathers exactly that zone (`--fade-scroll-width: 1rem`, my read of tokens/offsets.css; mask + scroll-driven ramp read in base-misc.css; the `s <= 12` end-gates present in the compiled composable). Mechanism certain statically; the ~36%-of-tile fraction is U-6. Cure is ours and local: clamp `dx` so the tile lands clear of the resolved fade width.
- **M-ESS-2 (LC) — the third throwing seam — FOLDED into C-01** (it is the BLOCKER's sharpest limb, not a separate row).
- **M-ESS-3 (DU) — the copy control confirms by mutating its own accessible name — ADJUDICATED MINOR → X-W6.** `:aria-label="copiedRow === row.index ? 'Copied' : …"` + `useClipboard({ resetMs: 1400 })`, both read by me; no live region; icon swap unlabelled. House pattern: stable name + `role="status"`.
- **M-ESS-4 (DU) — the `.strip-row` padding comment names a mechanism the declaration cannot reach — ADJUDICATED MINOR → X-W6 ◆.** The mask is PORT-anchored (my dist read), so content padding buys zero mask clearance; the 2px is only (insufficient) block-overflow headroom. Matters because it makes "increase the padding" look like a mask cure.
- **M-ESS-5 (DU) — `--motion-accent`'s fallback register splits inside one card — ADJUDICATED MINOR → X-W6.** My grep: `:262` falls to `--muted-foreground` while `:290/:293` and strip `:205/:209` fall to `--foreground`. All four are dead TODAY (R4-4 kill), but they are the declared behaviour and they disagree; the closed row's identity glyph gets the worst answer.
- **M-ESS-6 (DU) — producer sticky-hover geometry: `.glass-capsule-hover:hover { scale: 1.015 }` with no `(hover:hover)` guard — ADJUDICATED MINOR → GLASS-OWNED (bank alongside MT-F026, BH relay).** Verified byte-for-byte in the imported sheet, PRM-reduce arm present (no quarantine conflict); on coarse pointers the last-tapped tile stays enlarged — composing with C-17's ink defect on the same gesture.
- **M-ESS-7 (LC) — the `easingValue` guard duplicated verbatim across two modules, typed by `ReturnType<typeof CubicBezier>` while called with three OTHER factories — ADJUDICATED MINOR → X-W6.** Both copies read by me (`easingCatalogue.ts:98-104`, `useGradientCSS.ts:71-77`); the annotation type-checks by coincidence of all factories returning the identical `Result`; the published `Result<EasingFunction, EasingIssue>` is importable and unused.
- **M-ESS-8 (LC) — the catalogue's failure mode is an IMPORT-TIME throw that escapes the pane boundary — ADJUDICATED MINOR → X-W6 (a construction test joins the C-12 gate).** `SPECIMEN_FAMILIES = buildFamilies()` at module eval (`:198`) → `easingValue` throw (`:103`); latent only because `CubicBezier` rejects nothing in the shipped quads (validation read at `src/easing.ts:122-123`, x-domain only). A module-eval throw blanks the route chunk before any boundary mounts.
- **M-ESS-9 (LC) — `bezierLiteral`/`stepsLiteral`/`SpecimenFamily` exported with zero external consumers — ADJUDICATED INFO → X-W6, sequencing-coupled to R4-1/X-W9.** My grep: sole external mention is a prose comment in `gradient.spec.ts:287`. The deletion is free — which is exactly why it must not silently absorb the X-W9 serializer decision (the demo's `toFixed(3)` identity law is load-bearing; see superlative 3).
- **M-ESS-10 (LC) — the host docblock's instantiation law is falsified by its own template — ADJUDICATED INFO → X-W6.** "Picker instances stay ALIVE (v-show, never v-if)" (`GradientEasingEditor.vue:16-18`, my read) vs `<EasingAuthoringStage v-if="intervals[row.index]"` (`:207-208`). Behaviour coincidentally conforms (specimenRows skips interval-less indices); the comment degrades a corpus that leans on docblocks as declared contracts.

## Killed-claims register (every kill re-derived by this seat before sustaining)

- **R4-2 (`--radius-strip` "one declaration, zero consumers, in either repo") — KILL SUSTAINED.** My own grep over dist CSS returns TWO homes: the `@theme` declaration AND `.segmented-tabs--vertical { --bouncy-slider-radius: var(--radius-strip); }` in `components/tabs/styles/segmented.css` — a live producer consumer with a live semantic. The demo half (0 hits) is true; the "dead token" framing that carried the r4 argument is false; the 12px in-card rung cure survives, now with producer precedent.
- **R4-4 (the `var(--motion-accent, var(--foreground))` fallback "silently discards the D6 certification") — KILL SUSTAINED.** My reads: `safeCss(css: string, floor?): string` (`useContrastSafeColor.ts:353-359`) delegates to `certifyAccentInk` (`ink.ts:130-143`) whose every exit is a non-empty string; `mid` either serializes or throws (that throw IS C-01). `row.ink` can never be null; the `| null` type, the `:115` false branch, and the CSS fallbacks are all dead surface — hygiene at INFO (folded into M-ESS-5's register-split row), not a bypassed guard. LC's footnote preserved: the genuinely silent site is `ink.ts`'s `if (!accent) return css` — a different file, essentially unreachable on this path, NOT what R4-4 filed.
- **R4-3(b) (the reveal is RTL-unsound for lacking `normalizeHorizontalScrollLeft`) — KILL SUSTAINED.** My read of `:62-76`: `dx` is a physical delta from two `getBoundingClientRect()`s applied via `scrollBy({left: dx})` — no absolute `scrollLeft` read exists to normalize; correct under RTL by construction. The producer symbol is real and irrelevant to this code.
- **D-42 press-scale limb ("NO surface delta today") — KILL SUSTAINED** (ruled above; two scales conflated; press feedback ships in the imported sheet and I verified the `data-press-armed` `:not()` is satisfiable).
- **D-02 ("`data-state` is a foreign component's undocumented internal") — KILL SUSTAINED.** My reads: glass's own shipped `glass-chip.css` keys its selection register on `[data-mode="selectable"][data-state="on"]`; reka's `Toggle.js` emits `"data-state"` as its documented styling hook; the chip's `$attrs` filter deliberately reserves it. Keying scoped CSS on it is CONFORMANT; a locally-minted class would diverge from the producer's own selector the day the M3 fix lands. The narrow survivor (the seat hand-inks a register the producer recipe should own) is already better stated in C-03/D-30.
- **Header facts (216/231/117) — KILL SUSTAINED.** My `wc -l` with trailing-newline check: **215 / 230 / 116**. Late-pass drift only; every line citation I resolved in this adjudication still lands exactly.

## Superlatives (provenance preserved)

1. **The reveal's PRM gating is load-bearing, not ceremonial** (DU's find, the corpus's most consequential PRM line): `behavior: prefersReducedMotion.value ? "auto" : "smooth"` (`:74`) is the ONLY guard reaching this animation — the `scrollBy` options argument overrides CSS `scroll-behavior` by spec, so both app-wide guards (animations.css clamp; glass a11y-overrides) are structurally powerless here. One line stands between a `reduce` user and a full-width animated pan. Consistent with the motion-quarantine's MQ-1 lesson: option-driven and timeline-driven motion evades the two-guard clamp.
2. **The `scrollIntoView` refusal (`:37-46` + `:62-76`)** — names the exact defect it prevents (the O-19 flat-netting root cause), then earns it structurally: one axis, one element, by construction. Attacked twice by LC and once by me (the RTL kill above IS the attack); it held every time. Both readers call it the strongest code in the corpus; sustained.
3. **The literal-mint law (`easingCatalogue.ts:38-56`), proven right retroactively by R4-1**: identity is string equality against persisted `interval.css`; both consumers converged on `+n.toFixed(3)` and the outlier is the library function with the unit tests. The seat picked correctly — which is why the X-W9 retirement, not adoption, is the cure.
4. **One glyph painter, zero re-derived curve maths** (`glyphPath`, two files, one definition — my grep concurs with both readers). The boundary that made C's NaN-freedom a negative proof instead of a defect.
5. **The library is total and blameless at every seam the consumer breaks** (LC): uniform `Result` factories with validated domains; `err`, never throw, in `color/operations`. Every crash in this component is a consumer demotion. The pane boundary printing the library's own error code verbatim is what let three passes name the predicate exactly.
6. **Glass's `ChipProps` makes the wrong call unrepresentable** (LC): discriminated `mode` with `never`-closed doors, runtime `$attrs` filter matching the types (my dist read). Sharpens M3's disposition: the producer got the CONTRACT right; only its stylesheet is unreachable — precisely why a local CSS patch would be the wrong repair.
7. **The per-specimen ink is genuinely certified, not token-named** (`useSpecimenRows.ts:40,:69` → real OKLab certification against live surface lightness — my read of the chain). D-30's scope critique stands; the rigour is real.
8. **The empty state is impossible by construction** (`GradientVisualizer.vue:239` + `useGradientModel`'s ≤2-stop refusal): two independent upstream guards; no empty branch to design or test.
9. **The seat's own contrast reasoning shows its work** (`:182-186`: 45% measured ~2.6:1, raised to 65%) — the only place in the corpus where a contrast decision carries its measurement; the defect is that the method never reached the eyebrow or label rung.

## UNPROVEN-NEEDS-LIVE residue (→ the visual audit, task inputs)

- **U-1** — every rendered-pixel contrast figure (selected↔unselected 1.16/1.11:1 · eyebrow 2.77/2.94:1 · dark resting label straddling 4.5 · pass-1's 7.10/6.02/3.24). Directions follow from source; ratios are canvas decodes over a user-chosen gradient — C-4's framing is the honest one: a contrast value that depends on the user's gradient is unbounded, not passing.
- **U-2** — D-38's shadow-causality A/B (ΔL 0.084 vs 0.0131, the 6.4× asymmetry). Mechanism CONFIRMED from dist (unconditional capsule shadow); the injection experiment and decoded luminances are live-only.
- **U-3** — D-40's authoring-stage geometry (166.7px plot, 59.3% letterbox, 71.8% empty well): mechanism CONFIRMED (C-05 dead law + producer inline clamp, both read); `38cqi` needs a container context.
- **U-4** — D-39's rigidity table (`scrollWidth` 1482 constant across five arms; 70.4%→85.2% off-port): the absence of any responsive law is source-certain (`width: max-content`, no queries); the port widths are measurements.
- **U-5** — D-37's tile-geometry census (44/45.2px two-value histogram, chord table, 11-of-27 negative label clearance): cause source-certain; arithmetic rendered.
- **U-6** — M-ESS-1's fraction (16px feather over 44px tiles ≈ 36% of the just-selected tile ramped toward transparent; 25 of 27 tiles): mechanism source-certain; the fraction and the two end-gate exemptions want one live frame.
- **U-7** — C-01(b)/(c) driven repros (producer preset menu; two keystrokes from default): premises verified in dist (menu from the full preset table; sr-only y-domain read); the repros are live-only. Note: NOT needed to carry the conclusion — the ghost path (folded into C-01) is stronger and static.
- **U-8** — C-16 keep-in-view flake: 1 observation vs 9 green re-runs; unresolved. Honest disposition stands — harden with `{flush:"post"}`, delete the `nextTick`, on hygiene not on a reproduced defect.
- **U-9** — D-42's `prefers-contrast: more` premise is source-CONFIRMED (the arm lives only in the orphaned sheet); D-41's rendered type sizes (16.4/14.384/9px) are live.
- **U-10** — whether OM-4's owner-visible "grey band" is wholly the shadow union or partly the capsule warm fill at 27× overlap: DOM-walk consistent with source (no other backgrounds declared — my read of `:124-215`); the attribution split needs the pixel diff.

## Closing verdict

**DEFECTIVE — BLOCKER SUSTAINED AND WIDENED.** The pane-killing predicate is real, library-blameless, and reachable through THREE consumer seams — the third (hover over the gradient bar, adopted from reader-LC) requires no press and breaks the governing document's own two-seam cure, which this record re-homes to the mix boundary. 22 axis rows survive adjudication (1 BLOCKER · 15 MAJOR); 6 kills are sustained on this seat's own re-derivations (including two evidence-quality kills against the governing r4 document itself); 9 reader-adopted misses join the roster; the coverage story is itself defective (a 7-tile-slack unit gate and an e2e oracle dead on the same selector as the defect it was written to catch). Routing: X-W6 carries the wave-shaped mass — **with a NAMED GAP it must close by adopting `EasingSpecimenStrip.vue` and `useSpecimenRows.ts` into its file list or those rows degrade to NO-WAVE-OWNER at execution** — X-W9 the library serializer retirement, X-W1 the oracle re-key, and four producer rows ride the standing glass-ui BH relay (M3/MT-F014/MT-F026 banks). Ten residue rows go to the visual audit. Dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this file.

— end of apotheosis. The eleven challenge documents and both re-reader returns remain immutable beside this file per E-1/E-3.
