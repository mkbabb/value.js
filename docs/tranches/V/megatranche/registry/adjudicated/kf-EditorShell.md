# kf-EditorShell — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-08 · **Subject repo**: `/Users/mkbabb/Programming/keyframes.js`, worktree HEAD `8281638c` (same HEAD as the kf-App / kf-ChromeDock / kf-ChannelControls adjudications — anchors directly comparable). **Substrate caveat carried from kf-App ruling 1**: `EditorShell.vue`, `SharePopover.vue`, `MbabbMenu.vue`, `EditorHeader.vue`, `package.json` are all DIRTY at the worktree (this seat's `git status`); `origin/master` is 41 commits ahead — **every cure re-resolves anchors at `origin/master` first** (D-19 law). Frontier probes were run for every load-bearing row.
**Corpus**: the three challenge axes at `docs/tranches/V/megatranche/audit/kf-components/EditorShell/` (challenge-D-design.md · challenge-L-library.md · challenge-C-consumption.md, all authored 2026-08-04), as re-read by two hostile re-readers — **RR-1 (design-and-usage)** and **RR-2 (library-and-code)** — whose returns this record adjudicates.
**Method (L-14/L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (the WCAG 2.1.1 keyboard question, D-2's kill, D-16's sub-claim, D-20, D-27's px-census, the D-1 rest-geometry figure, L-17's grade, L-16's cure, the deletion's severity) was re-derived against the tree with this seat's own instruments; the two claims whose life-or-death turned on a single mechanism (the reka menu-item nesting; the sRGB compositing model) were **third-derived**, not adopted. Every re-reader row — confirmed AND missed — was then run through the registry bank per the kf-ChromeDock DISSENT-2 law, and **four jointly-confirmed rows plus one RR-2 "missed" row turned out to be already banked** (D-1/L-1/C-1 → KF-APP-5 · L-2/C-2 → census SCH-1/F-1 · L-4/C-6 → KF-APP-4 · C-12 → KF-APP-21 · RR-2's SURFACE_META find → KF-APP-13 ≡ kf-ChannelControls L-7/C-4) — the third consecutive record to catch this event.
**Arbiter's own instruments this session (all static + git + one node oracle; ZERO browser tooling — probe parsimony)**: full read of EditorShell.vue (262 L, worktree); SharePopover.vue :1-30/:50-62; useShareState.ts :60-90; hashSharing.ts :8-60; layout.css px-census (`grep -n px` → :15/:139/:140 the three `:root` literals); reka-ui `Menu/MenuItem.js` :55-80 (the SELECTION_KEYS handler WHOLE, incl. its own comment "if keydown causes focus to move, prevents keydown from firing on the new target") + `Menu/MenuContentImpl.js` :195-215 (`if (isKeyDownInside) { if (event.key === "Tab") event.preventDefault(); }`) + `Dialog/DialogTitle.js` (`as: { default: "h2" }`); installed glass-ui 7.0.0: `header-ribbon/styles.css` WHOLE-tokenized (band `min-block-size: var(--size-icon-btn)` + `padding: var(--panel-padding)`; actions base rule `overflow: clip`; collapsed arm; coarse-pointer arm; forced-colors arm), `tokens/offsets.css` (`--panel-padding: 0.375rem`), `button/styles.css` focus-visible grep = **0**, `dark-mode-toggle.css` `:focus-visible { box-shadow: var(--focus-ring-shadow, …) }`; heading census: `grep -rn "<h1" demo/` → **exactly one element** (EditorStartScreen.vue:27), `demo/scenes/` headings → EasingTarget.vue:24 `<h2>` only, TimingFunctionPanel.vue:14 `<h3 class="text-title">` mounted via ChannelOptions.vue:312; **producer git** (`/Users/mkbabb/Programming/glass-ui`, read-only): `4bf53962` dated 2026-08-04 "feat(reduction): land BK #18 W-DELETE — the Φ5 deletion spine", `merge-base --is-ancestor` master → **true**, `--stat | grep -c header-ribbon` → **7**, `ls src/components/ | grep -i header` → empty, `grep -c header-ribbon package.json` → **0**, `docs/consumer-evidence/header-ribbon.md:3` still reads "**RETAINED** persistent-only"; **consumer frontier git**: `origin/master:EditorShell.vue` → `:16 <HeaderRibbon placement="right">` (no `mode`) + `:116 import { HeaderRibbon } from "@mkbabb/glass-ui/header-ribbon"` **still present**; `origin/master:MbabbMenu.vue` → `:8/:18 <DropdownMenuItem @select.prevent …>` wrapping `<SharePopover :on-scene-restore>` and `<DarkModeToggle>` **verbatim**; `git diff HEAD -- EditorShell.vue` → `-variant="ghost" -size="icon" / +emphasis="quiet"` with `aspect-square w-8` byte-identical on both sides (the C-5 decider); **executed node oracle** (own code, hsl→sRGB, gamma AND linear compositing, WCAG Y + L*): reproduces the corpus's LINEAR table (1.14/15.89/4.31/33.15) AND both readers' GAMMA figures (fine ΔL* 2.34/2.19, major 8.65/10.62; CR 1.061/1.046 fine, **1.249/1.249** major) **to every digit** — the D-2 decider; **registry cross-reads**: kf-App.md (ruling 5 + KF-APP-4/-5/-6/-13/-21/-24/-39/-55/-56 + the three-substrate map), kf-ChromeDock.md (D-18/C-1 + DISSENT 2/3), kf-ChannelControls.md (L-14/C-12 fold law), kf-AnimationControlsGroup.md (C-4, C-3-kill, C-9, L-S1/SUP-4), kf-ControlsPaneWrapper.md (N-1/C-5, C-3-kill), kf-CubeScene.md (substrate ruling + the :106 headerLeft-trap sequencing law), CENSUS-2026-08-03 §(a) lane taxonomy; corpus citation spot-checks at challenge-D :112/:118/:136-145/:151, challenge-L :63, challenge-C :203 — all verbatim as the readers quoted (zero fabricated citations found).
Status verbs per M-25: dispositions are **ADJUDICATED, not VERIFIED** — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets follow the census lane taxonomy (CENSUS-2026-08-03 §(a)): **KF.W0** substrate settle · **KF.W4** gates/hygiene · **KF.W6** glass suffusion incl. the phantom-token audit · **KF.W9** Safari/visual audit (owns ALL SS-13 residue) · **KF.W10** fold discharge; folds to banked rows carry the bank's own routing. Everything the census does not own is **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring.

## Scoreboard

**51 rows stand: 40 booked here (8 MAJOR · 23 MINOR · 9 INFO) plus 11 FOLDS to banked identities — four of them blocker-weight at their banks (the inert-ribbon lockout at KF-APP-5; the producer's outright DELETION of HeaderRibbon, first-banked in this record as a rider on KF-APP-5 + census SCH-1; the phantom dependency at SCH-1/F-1, frontier-cured; the no-SFC-typecheck gate at KF-APP-4). 13 corpus claims KILLED on executed refutations — the two heaviest (D-2's 7.5×–14× theme-asymmetry table; the D-1[R2] WCAG escalation as evidenced) third-derived before ratification. 14 net-new re-reader finds adopted. The identity guard caught FIVE re-reader rows already banked, including one filed as "missed by all three axes" that the bank has carried since kf-App.**

Blocker-weight center of mass: the shell's header chrome rides a producer component that (a) renders its three controls `inert`+`aria-hidden` behind a hover-only ~12×52 px blank sliver (banked, KF-APP-5), (b) **no longer exists at the producer's master** (`4bf53962`, 2026-08-04, ancestor of glass-ui HEAD — every axis's "pin and re-read" remediation is unbuildable), while (c) the frontier consumer still imports the deleted subpath. And the WCAG 2.1.1 question the corpus left self-contradictory is now settled statically: **there is no keyboard route to Share or the theme toggle anywhere in the app** — the ribbon is inert (KF-APP-5), the shortcut registry has no binding (19 calls, three independent enumerations), the dock's collapsed state has zero tab stops (banked, kf-ChromeDock D-18/C-1), and the dock's menu copies are keyboard-inoperable by reka construction (this record's third-derived lemma, frontier-durable).

## Ruled disagreements (the seat's actual work)

| question | RR-1 (design-and-usage) | RR-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **The WCAG 2.1.1 keyboard path** (RR-1 missed #2 "unconditional, MAJOR" vs RR-2's KILL of D-1[R2] + its UNPROVEN "a reveal route plausibly exists") | No keyboard path anywhere: MenuItem swallows activation | Escalation UNSUPPORTED; dock route unexamined; needs live | **Both right at different layers; the substance is BANK-SUSTAINED and now lemma-complete** | kf-App ruling 5 ALREADY sustained 2.1.1 BLOCKER for Share + dark-mode at KF-APP-5 (the `?` route exempts only the shortcuts trigger). RR-2's kill of the **filing** is ratified — challenge-D:118's sole evidence (the 19-shortcut enumeration) says nothing about the dock path, three lines under :114's "Share and Dark mode survive via MbabbMenu". RR-1's lemma closes exactly that gap and this seat third-derived it at the bytes: reka `MenuItem.js` SELECTION_KEYS → `event.currentTarget?.click()` + `preventDefault()` — the click dispatches on the *menuitem*, bubbles to `@select.prevent`'d `handleSelect`, and the nested `<button>` is never activated (reka's own comment even warns keydown must not reach a new target); `MenuContentImpl.js:205` preventDefaults Tab inside content, so the inner control can never be focused either. `MbabbMenu.vue:8/:18` carries the nested-in-menuitem shape **verbatim at `origin/master`** (my probe) — frontier-durable. RR-2's "GlassDock binds onFocusin" hedge dies against the bank: kf-ChromeDock D-18/C-1 (BLOCKER, banked) proves the collapsed dock's only focusable subtree is `inert`, so onFocusin cannot fire — the route is dead one layer *earlier* too. Three independent dead layers = statically decided; the SS-13 item narrows to a courtesy witness. Mouse and touch survive (a direct click/tap lands on the inner button and bubbles). |
| **D-2** (graph substrate "7.5×–14× more present in dark") | KILLED — linear-light compositing artifact | KILLED — same, independently | **KILL RATIFIED — third-derived** | My node oracle reproduces the corpus's table under the LINEAR model to every digit (1.14/15.89/4.31/33.15) and both readers' GAMMA figures to every digit (fine ΔL* 2.34 light / 2.19 dark; major 8.65 / 10.62; CR **1.249 : 1.249** major, 1.061 : 1.046 fine). `color-mix(in srgb, …)` composites in device sRGB; the corpus's own §S-4 falsifier (challenge-D:145) quotes the gamma numbers beside the linear table and reconciles them with the wrong explanation. **Anti-cure lock**: challenge-D §7 step 5 ("light needs ≈3–4× the alpha") must NOT execute — it would break a substrate that is near-invariant across themes in BOTH metrics. RR-1's residual nit adopted: the fine tier is 1.061 vs 1.046, not "identical to three significant figures" (the major tier is). The mechanism's superlative is STRENGTHENED (S-1 below). |
| **D-16's sub-claim** ("`<main>` contains no heading at all") | REFUTED as stated | CONFIRMED whole (repeated the sub-claim) | **RR-1 wins; RR-2's cell corrected** | My reads: `TimingFunctionPanel.vue:14 <h3 class="text-title">` mounts via `ChannelOptions.vue:312` — inside the `<main>` at EditorShell.vue:74; `EasingTarget.vue:24 <h2>` enters `<main>` through `#target` → `#animation-content` (EditorShell.vue:100-102). Conditional and two landmarks deep, but not absent. The parent claim (h1 + all hero prose outside `<main>`, home route) is CONFIRMED — and the sharper inverse (RR-1 missed #3) is adopted MAJOR: my census finds **one `<h1>` element demo-wide**, gated on `showStartScreen` = home only. |
| **D-20** ("two controls grow; the third dims — inverted hover semantics") | REFUTED as framed | CONFIRMED as filed | **RR-1 wins; RR-2's cell corrected** | `SharePopover.vue:7` — the base class string contains `scale-on-hover` (my read); line 8 adds the `hover:opacity-50` ternary. All three controls grow; one ADDITIONALLY dims toward the demo's own `.is-disabled` vocabulary. The surviving defect is a self-contradictory single control (grows toward the pointer while fading toward disabled), booked D-20-RESCOPED MINOR; challenge-D:553's cure aims at the wrong element. |
| **D-27's second sub-claim** ("`--header-items-max-w: 500px` is that file's ONE raw-px literal") | REFUTED — three literals | Repeated the "one raw-px literal" cell in its own missed M7 | **RR-1 wins; RR-2's cell corrected** | My grep: layout.css `:root` carries `:15 500px`, `:139 --caret-offset: 14px`, `:140 --resize-handle-inset: -4px`. The rhetorical force ("the exception outlives the only thing it excepted") does not land; the orphan-token half survives at INFO (with C-22). |
| **D-1/C-1 rest geometry** (12×40 vs 12×52) | ≈12×52 (band padding + uncollapsed cross-size) | kept ~12×40 while citing the padding token that disproves it | **RR-1 wins — and kf-App ruling 5's "genuinely undecidable statically" cell is REFINED** | My styles reads: `.header-ribbon__actions` clips the INLINE axis only (`max-inline-size: 0; overflow: clip`); the 40px `Button` (`--control-h-md`, D-18) still sets the row's cross size; band border-box = 40 + 2 × `--panel-padding` (0.375rem, offsets.css) = **52px** under preflight border-box; `min-block-size: 2.5rem` is not binding. The band-box IS derivable; what stays live-only is whether a featureless 12×52 sliver reads as an affordance (KF-APP-5's KF.W9 item, figure corrected). |
| **L-17** ("installed HeaderRibbon matches NO producer revision") | PLAUSIBLE (endpoints only) | CONFIRMED (per-commit enumeration on the producer path) | **CONFIRMED at master scope; RR-1's caveat kept as a scope note** | RR-2 enumerated all seven commits touching the path; the installed combination (no `mode` prop + collapse gate with the persistent disjunct removed + bare div anchor) matches none. Branch/registry-tarball scope stays open but is MOOT under the deletion. Value: proves `mode="persistent"` at :16 was once correct and load-bearing — a fossil of a contract cut underneath the consumer, not author error. Folds as evidence into census SCH-1 (reproducibility) + the KF-APP-5 rider. |
| **L-16's cure** (+ RR-2 missed M4) | confirmed at source level | confirmed but cure "structurally unsound"; whole barrel layer dead | **RESCOPED — the import is RIGHT, the barrel is the defect; grade demoted to MINOR** | The shell's child IS the app's main content; App.vue:73-85 records that the keyed `<Suspense>` shipped a blank viewport — "route through the async barrel" is REFUTED. RR-2's M4 supplies the true shape: THREE `defineAsyncComponent` exports with zero consumers, an umbrella barrel with zero importers, two docblocks asserting a guarantee no code path can consume. Cure = delete the barrel layer, keep the static import. |
| **The deletion's severity** (RR-1 MAJOR vs RR-2 BLOCKER) | MAJOR | BLOCKER | **Blocker-weight at its banks, with a sequencing law** | See the fold roster. The frontier consumer still imports the deleted subpath (my `origin/master:EditorShell.vue:116` probe), so census SCH-1's KF.W0 substrate settle would break the build the moment glass-ui advances past `4bf53962`. |

**Cure-level rulings inside agreed verdicts**: (a) **D-6's mechanism** — RR-1's spec correction adopted: per CSS Flexbox 1 §4.1 the static position of an abspos child of a flex container IS determined by `align-items`/`justify-content`; the classes at :60 are inert here only because EditorStartScreen resolves both axes explicitly (`left-0` + the `.hero-band` φ seat). RR-2 repeated the folk mechanism ("out of flex flow, so the centering never applies") — corrected; the finding stands MINOR. (b) **D-22's compounding half** — killed on my DialogTitle.js read (`as: { default: "h2" }`): the dialog outline is h2→h3, contiguous; the primary (deck/hint as peer `<h2>`s) stands MINOR inside C-11/D-4's cluster. (c) **C-5's attribution half** — killed on my own diff: `aspect-square w-8` is byte-identical on both sides of the migration hunk (`variant="ghost" size="icon"` → `emphasis="quiet" icon-only`); and `size="icon"` was never a glass-ui 7.0.0 ButtonProps member, so HEAD engaged no size axis either. The geometry survives whole as D-18/C-5; the "restore" framing in C §8 step 5 must read "introduce". (d) **L-12's hedge** — RR-2's kill ratified: glass-ui's `Tooltip` is a bare forward to reka `TooltipRoot`, whose `injectTooltipProviderContext()` has no fallback and throws; the axis declined a true finding on a false premise (an under-claim is also a corpus defect, L-18 both ways). C-4 books at RR-2's MINOR grade. (e) **L-14's cure law** — any threading of `onSceneRestore` must pass a getter or thread reactively: `SharePopover.vue:56-61` captures `props.onSceneRestore` by value at setup into `useShareState` (my read); the prescribed "thread from App to :20" reproduces the staleness one hop out (RR-2 M6, verified).

## Adjudicated defect roster

Line anchors are worktree `8281638c`; EditorShell.vue, SharePopover.vue, MbabbMenu.vue, package.json are all dirty — **re-resolve at `origin/master` before any cure**. Dispositions are ADJUDICATED (M-25); routings are agglomeration input for the forming X·KF specs.

### Blocker-weight FOLDS (identity guard; zero re-booking)

- **D-1 / L-1 / C-1 (+ L-3, D-15 as riders) — FOLD → kf-App KF-APP-5 (banked BLOCKER: KF.W6 consumer half + glass-ui BH relay + KF.W9 rest-state).** The three header controls `inert` + `aria-hidden` + zero inline-size at rest; `#items` only, no `#anchor`; hover-only reveal that discards touch. Both re-readers re-confirmed end-to-end without checking the bank (the recurring identity-guard event). **Riders this record adds to the bank**: (a) **the completing WCAG 2.1.1 lemma** — the dock's MbabbMenu copies are keyboard-inoperable by reka construction (`MenuItem` SELECTION_KEYS click-on-the-item + `@select.prevent`; Tab preventDefaulted in content), frontier-durable at `origin/master`, so with the banked kf-ChromeDock D-18 (collapsed dock = zero tab stops) **no keyboard route to Share or theme-toggle exists anywhere in the app** — kf-App ruling 5's 2.1.1 grounds are now complete against the strongest objection; (b) **geometry refined**: the rest sliver is ≈12 × 52 px, not 12 × 40 (band padding + the uncollapsed 40px cross-size; ruling above) — refines kf-App ruling 5's "undecidable statically" cell; the affordance-perception half stays KF.W9; (c) **L-3 is frontier-CURED** (the phantom `mode="persistent"` deleted at `origin/master:16` — kf-App's probe, re-run here) and **L-17 reframes it as a fossil**, not author error; (d) the deletion rider below.
- **THE DELETION (RR-1 missed #1 ≡ RR-2 missed M1; ONE identity) — blocker-weight rider, FIRST-BANKED HERE, on KF-APP-5 + census SCH-1 (KF.W0).** glass-ui master commit `4bf53962` (2026-08-04, ~4 h after the corpus was written; ancestor of producer HEAD — my own git reads) deletes `src/components/header-ribbon/` whole (7 paths incl. the contract test; exports 73→68; zero `header-ribbon` keys in producer package.json). The frontier consumer **still imports `@mkbabb/glass-ui/header-ribbon`** (`origin/master:EditorShell.vue:116`, my probe) — the constellation's sole consumer of a component that no longer exists upstream. Consequences ruled: (i) every axis's remediation step 1 ("declare glass-ui ≥ the 4e8c6387 cut and regenerate the lock" — L §6.1, C §8.1, D §7.1) is **unbuildable**; both readers' kills of it ratified, incl. RR-2's kill of L §1.0's escape-hatch framing (L-1 was never a pinning problem — the design system retired the component under its only consumer); (ii) **sequencing law**: KF.W0's substrate settle must NOT advance the installed glass-ui past `4bf53962` before the shell's chrome host migrates off `/header-ribbon` — else the build dies at :116; (iii) KF-APP-5's BH-relay **producer half is MOOT** (the producer answered the standing question by deletion; the relay updates to an FYI + the consumer-evidence correction); (iv) producer-side relay content: `docs/consumer-evidence/header-ribbon.md:3` still says "RETAINED persistent-only" — the producer's own evidence doc contradicts its HEAD (my read). The migration decision (which chrome host replaces the band) is the cheapest close of the banked blocker AND of D-15/D-17 below — **NO-WAVE-OWNER** (SS-1/SS-2 design decision) with the KF.W0 sequencing tripwire.
- **L-2 / C-2 — FOLD → census SCH-1/F-1 (KF.W0), per kf-App ruling 1: frontier-CURED** (`origin/master` declares glass-ui 7.0.0 as an exact devDependency; the phantom exists only at the dirty worktree — whose package.json deletion kf-CubeScene's substrate ruling calls "an uncommitted regression"). Both re-readers booked it MAJOR/"BLOCKER" without the substrate triage — carried corrected, the same event kf-ChromeDock L/B-1 recorded. Rider retained: the installed dist still carries the pre-cut `--header-ribbon-actions-width` / `min(32rem, …)` markers (C-2's stale-build tell, corroborating L-17).
- **L-4 / C-6 — FOLD → kf-App KF-APP-4 (KF.W4 gates lane).** No vue-tsc anywhere; `check` = bare tsc ×2; `lint` = `depcruise src`; CI's merge path never touches the demo. **Riders this component contributes**: the L-6/C-18 unread `const props` and the `object|null` annotation-laundering below are members of the class this gate would catch; C-21 (zero tests name EditorShell/HeaderRibbon; vitest excludes `test/demo/**` on the merge path) rides as the test-coverage cell.

### MAJOR (booked here)

- **RR-1 missed #3 (adopted; my own census) — MAJOR · on 6 of the app's 7 routes the document contains NO `<h1>` — and no heading at all except a conditional `<h3>` two landmarks deep.** `grep -rn "<h1" demo/` → exactly one element (EditorStartScreen.vue:27), rendered only under EditorShell.vue:60's `v-if="showStartScreen"` (App binds `:show-start-screen="isHome"`); `demo/scenes/` contains one `<h2>` (easing). The shell owns both the gate and the `<main>` landmark, so it is the sole owner of a document skeleton that exists only on home. Fails axe `page-has-heading-one`/heading-order in a repo that ships `audit:lighthouse`. All three axes examined only the home route. Disposition: **NO-WAVE-OWNER** (a11y spec surface, one family with D-16 and the KF-APP-5 cluster).
- **D-16 — MAJOR (RESCOPED per ruling) · the page's only `<h1>` — the declared LCP element — and all hero prose sit outside `<main>` and outside every landmark** (EditorShell.vue:60-64 is a bare sibling `<div>`; `role="toolbar"` is not a landmark). The "no heading at all in `<main>`" sub-claim is STRUCK (conditional h3/h2 exist inside — ruling above); the true outline on home is h1(outside)→h3(inside). Disposition: **NO-WAVE-OWNER** (same surface as the row above; the cure is one decision about where the subject's skeleton lives).
- **L-14 / C-3 — MAJOR · the default `#header-right` SharePopover is mounted unwired (`<SharePopover />`, :20) so the ribbon share path cannot switch scenes, while the MbabbMenu twin is wired — and navigation success is reported regardless.** My reads: SharePopover.vue:56-61 optional `onSceneRestore` → useShareState; useShareState.ts:79-81 conditional navigation, :83-86 unconditional `toast.success("State restored!")`; App supplies no `#header-*` slot so the unwired default is what renders. **Cure law (RR-2 M6, verified)**: the prop is captured by value at setup — thread as a getter or the fix inherits the staleness. Disposition: **NO-WAVE-OWNER** (wire or delete the ribbon copy with the migration decision).
- **RR-2 missed M2 (adopted; every hop re-read) — MAJOR · an invalid share payload silently no-ops and still reports success, in BOTH SharePopover mounts including the wired dock one.** My reads: `decodeStateFromHash` returns `JSON.parse(json)` typed `object | null` — any non-null JSON scalar (`"NQ=="` → the number 5) passes useShareState.ts:69-73's truthiness guard; `restoreStateFromParam` re-decodes and `isValidState` (typeof object) rejects it → `{ restored: false }`; `result.restored` has zero readers; the popover closes and "State restored! — Animation state loaded from shared URL." fires unconditionally. Orthogonal to L-14/C-3 (survives its fix). **Rider (RR-2 M3)**: the `: object | null` annotation laundering `any` is the exact type-system defeat that admits this — one identity with the cure; the KF-APP-4 gate class. Disposition: **NO-WAVE-OWNER** (read `restored`; make `decodeStateFromHash` honest — one validator, not two disagreeing ones).
- **D-15 — MAJOR · `role="toolbar"` with none of the APG keyboard contract (no roving tabindex, no arrow nav — the module's only handlers are pointer/focus/esc), and the shell never passes `ariaLabel`, so the demo's only toolbar announces the producer default "Header actions".** RR-1's deeper cell adopted: the Esc handler *expands* (sets focus-within true) and focuses into the empty anchor — an inversion of the dismiss idiom. Disposition: rides the **KF-APP-5 migration decision** (the replacement chrome either carries a real toolbar contract or drops the role); dies with `/header-ribbon`.
- **D-17 — MAJOR · the header band is the one chrome band outside the φ/work-area anchor chain** (`position: fixed; inset-block-start: 0; padding: 1rem` from the producer sheet; no `--dock-*`/`--phi` term; no `data-dock-tether` — the demo's two opt-ins are ChromeDock:214 and TransportDock:4; no demo rule targets `.header-ribbon`). **Cross-ref KF-APP-6**: the chain it stands outside is itself currently IACVT-voided by the `--dock-margin` phantom — the contrast (EditorStartScreen obeys the chain) is prose-true and paint-limited until that bank cures. Disposition: **NO-WAVE-OWNER** (the replacement host must seat the band in the chain; one decision with the migration).
- **D-18 / C-5 — MAJOR · `aspect-square w-8` cannot produce a square: both glass primitives set explicit block-sizes that `aspect-ratio` loses to — Button 32×40 (fine) / 32×60 (coarse), DarkModeToggle 32×36; identical classes, three boxes.** Cascade convergently derived by both readers (RR-2 empirically against the built sheet's layer order); token math re-checked. The attribution half ("introduced by this migration") is KILLED (ruling above). The producer publishes the right instruments unused (`Button size`, `DarkModeToggleProps.size` incl. `"control"`/`"dock"`). Disposition: **NO-WAVE-OWNER** (consume the size axes; delete the utility pair).
- **D-19 — MAJOR · the design stakes itself on `proof:appearance-suffusion` clause g and `proof:hero-two-focal`; neither gate exists** (package.json has exactly `proof:publish` + `proof:owner-golden`; scripts/gates/ has no suffusion/two-focal). Three live citation sites: EditorShell.vue:235-237, EditorStartScreen.vue:15, and layout.css:29-31 (the token authority — missed by the axis, found by RR-1). RR-2's file-list correction adopted (one historical mention in scripts/observe, not three). Disposition: **NO-WAVE-OWNER** (prose surgery; whether the legibility clause becomes a REAL gate is a KF.W4 registry-truth decision — cross-ref D-29's coordinate sweep).

### MAJOR-weight FOLDS

- **L-15 / C-9 — FOLD → census S-2 / KF.W2-TABS (the dead-tabs-plumbing family: banked kf-AnimationControlsGroup C-4, kf-ControlsPaneWrapper N-1/C-5, KF-APP-12).** `#tabs-content` severs its scoped slot props (:92-94 bare) while both siblings forward with `v-bind`; ControlsPaneWrapper publishes `selected-animation`/`is-playing` and EditorShell — the barrel-exported public hop — is the one that drops them. Zero live consequence (App binds nothing). This record contributes the family's missing edge; the whole chain deletes/re-forms in the tabs wave.
- **L-17 — FOLD as evidence → census SCH-1 (ruling above).** The installed artifact is provably an unpublished intermediate: no producer revision on the path carries (no `mode`) + (collapse gate sans persistent disjunct) + (bare div anchor). Largely mooted by the deletion; retained because it re-dates L-3 as a fossil and pins the reproducibility caveat every producer-bytes cell in this corpus inherits.

### MINOR (booked here; NO-WAVE-OWNER unless noted)

- **D-5 / L-10 — MINOR** · the scoped block credits `design-idioms.css` for tokens that live in layout.css:32-35, states 12% for an 11% token, and carries two dead fallbacks (5%/12%) diverging from the live values — while `background-size` reads the same tokens bare (the asymmetry is real). L-10's ":247" corrected to :246 (my read). Prose+token cure in one motion with the D-2 anti-cure lock.
- **D-7 — MINOR** · sub-44px targets (w-8 = 32px inline; SharePopover's unboxed 24px glyph) while the demo's own `.tap-floor` remedy has zero consumers (grep: definition only). Rides the a11y spec surface; cured for free by the missed-#6 trigger unification.
- **D-8 / C-17 — MINOR** · style.css:18-40's "strictly ascending, demo-OWNED" z-contract omits `--z-header: 35` (and seven more producer rungs), the rung the shell's own ribbon occupies below `--z-dock: 40` in the same block-start band; the contract's grep-based enforcement cannot see drift arriving through a consumed component. Occlusion question → **KF.W9**.
- **D-10 / L-5 / C-8 — MINOR** · `headerRibbonRef` defineExposed with zero readers repo-wide, at a component exposing no instance API (corroborated by the producer's own consumer census). Dies with the migration.
- **D-11 / L-9 — MINOR** · `:key="superKey"` full-subtree remount compensating a one-shot store read (AnimationControlsGroup.vue:176) while App.vue:229 writes the reactive form of the same expression — the transport hard-cuts while the subject cross-fades. Cross-refs: banked kf-AnimationControlsGroup C-9 (the remount tears down app-global chrome — lift `DemoGlobalChrome`) and L-S2 (`superKey` by-value capture "safe only by the host's :key — borrowed correctness"). Perceived severity → **KF.W9**.
- **D-12 — MINOR** · `--scale-hover: 1.08` live-shadows the identical glass-ui token (invisible today = the hazard); `--graph-line-*` minted inside `<style scoped>` are latent global identifiers (scoped scopes selectors, never custom-property names) — the census §6.3 `--kf-*` hazard's structural analogue, since `--kf-*` itself does not manifest (both readers' greps).
- **D-20-RESCOPED — MINOR (ruled)** · one control (the share trigger) simultaneously lifts 1.08 toward the pointer and dims to the `.is-disabled` 0.5 vocabulary — a self-contradictory composite, not a cluster inversion; EditorHeader.vue:26 carries the same composite. Cure re-aimed at SharePopover.vue:7-8.
- **D-21 — MINOR** · icon-rung discord in one 8px cluster: `icon-lg` (24) beside `icon-sm` (16), inner buttons `icon-md` (20), from the family whose own comment records the 61-call-site scar it exists to prevent.
- **D-23 — MINOR** · the file fails the repo's own prettier (both readers executed it; RR-2's corrected line-length census adopted: :3=113, :60=129, :190=124, + the :75-103 block un-indented inside `<main>`). Nothing on the merge path runs it.
- **D-24 / L-8 / C-7 (prose half) — MINOR** · `hasControlSurfaces` + `extraTabs` justified entirely by "the playground" — a host that exists only in comments (banked kill: kf-AnimationControlsGroup C-3's reachability premise). The `extraTabs` chain itself **FOLDS → banked kf-AnimationControlsGroup C-4 ≡ census S-2** (one identity; the census row owns it; kf-ChannelControls L-14/C-12 already folded there). Booked here: the prose that manufactures a phantom host to justify five defaults.
- **D-25 — MINOR** · no `viewport-fit=cover` anywhere, so every `env(safe-area-inset-*)` term in layout.css's anchor chain resolves to 0px — on a page whose shell calls `initIOSPlatformClass()`. Not banked (my kf-App grep); the four stranded terms each carry a comment explaining why a notched device needs them. Notch witness → **KF.W9**.
- **D-26 — MINOR** · RTL half-migrated: the ribbon flips via `inset-inline-end` while the hero pins `absolute left-0 w-screen` (its own comment says "the left *reading* edge") and the scoped gradients are physical `to right`/`to bottom`. Cross-refs: KF-APP-55 (honest no-claim, terminal) + kf-ChromeDock D-28 (exposures-recorded pattern) — EditorShell's exposures recorded on the same a11y spec surface.
- **C-4 — MINOR (RR-2's downgrade ratified; L-12's hedge killed)** · the `IconTooltip`→reka swap makes EditorShell hard-require an ancestor `TooltipProvider` it neither supplies nor declares (reka throws; HEAD commit `8281638c` is the scar), while its own children self-provide and `shell/index.ts` publishes the component as reusable. Cross-ref **KF-APP-21** (the provider-policy row; the frontier keeps App's root provider deliberately — cure is config unification there, plus declaring the requirement here).
- **C-11 / D-4 — MINOR** · four disclosure idioms across four controls (glass Tooltip / native `title` / bare aria-label / native `title` on the inner pair), and `title="Toggle dark mode"` is a non-prop absorbed by useAttrs layering a permanently-stale string over the toggle's live computed accname. D-4's decision not to file WCAG 2.5.3 endorsed by both readers and this seat.
- **C-12 — FOLD → kf-App KF-APP-21** (the 700ms-vs-100ms provider split is the banked row's exact cell; EditorShell.vue:30's tooltip sits outside `<main>` under App's bare provider).
- **C-13 — MINOR** · two identical emit pass-throughs, two spellings (:85 named handler vs :86 inline) — hygiene.
- **C-15 — MINOR** · root-barrel `Button` beside three subpath imports with `./button` published — the kf-ChromeDock m-3/R3-6 shape, recurring.
- **C-19 — MINOR · FOLD (parser class) → kf-App KF-APP-56 ≡ megatranche R1 / census risk-3 (KF.W2/W3 own the class).** Component rider booked: EditorShell.vue:239-248 establishes `color-mix(in srgb, …)` as the demo's token idiom — the exact form value.js 4.0.0 rejects — while a sibling scene pipes computed token text through `parseCssColor` and throws on `!ok` (RR-2 reproduced R1 live against the installed 4.0.0). The square-scene seam is spec input for whichever component adjudication owns `useSquareTumble`.
- **L-6 / C-18 — MINOR** · `const props = withDefaults(…)` never read (:135 sole hit); `superKey: undefined` a no-op default; the sibling file uses the 3.5 reactive-destructure form. KF-APP-4-gated class.
- **L-16-RESCOPED + RR-2 M4 (merged; ruled) — MINOR** · the lazy transport/instrument barrel layer is dead whole: three `defineAsyncComponent` exports with zero consumers, an umbrella with zero importers, two docblocks advertising a guarantee no path consumes — while the shell's static import is CORRECT (main content; the Suspense scar). Cure: delete the barrel layer + false prose; the "route through the barrel" remedy is REFUTED.
- **RR-1 missed #4 (adopted; mechanism my own read) — MINOR** · `.header-ribbon__actions`' base rule sets `overflow: clip` unconditionally (expanded too), so the shell's chosen micro-interaction — the 1.08 `scale-on-hover` lift on both header controls — is cropped at the row's box (~1.6px top/bottom on the 40px Button; ~1.28px inline-end on the toggle). Filed four times by the corpus, never asked whether it has room. Crop screenshot → **KF.W9**; dies with the migration.
- **RR-1 missed #5 (adopted; census my own greps) — MINOR** · three keyboard-focus treatments in the one cluster: DarkModeToggle gets the producer ring (`:focus-visible { box-shadow: var(--focus-ring-shadow…) }`), the glass Button has ZERO authored `:focus-visible` (my grep of button/styles.css → 0; UA outline), SharePopover's bare button likewise — and the demo's own declared "SINGLE keyboard-focus affordance" (`.focus-ring`, design-idioms.css:73-79, four live call-sites) is on none of them. Not a 2.4.7 failure (UA ring survives); a conformance defect against the demo's own idiom.
- **RR-1 missed #6 (adopted) — MINOR** · the SharePopover trigger is a hand-rolled `<button>` + five reset utilities re-implementing an unstyled icon button between two glass primitives, while the same file's inner affordances use glass `Button`/`Input` — a `feedback_glass_ui_first_class`/`feedback_root_styling` breach that is also the common root of D-4 (no tooltip), D-7 (unboxed target), and the missed-#5 focus gap. One `<Button emphasis="quiet" icon-only>` cures four rows.
- **RR-1 missed #7 (adopted; S-dvh superlative RESCOPED accordingly) — MINOR** · half the `@supports not (height: 100dvh)` fallback is dead and the dead half regresses: `.grid-background` is `fixed inset-0`, so when `h-dvh`/`w-dvw` drop at parse time `inset: 0` already sizes it to the viewport — the fallback then forces static `100vh`/`100vw` (scrollbar overhang; non-tracking). The `.editor-shell` arm is real and correct. Cure: delete the `.grid-background` arm; re-scope the praise. Legacy-cell witness noted as likely unavailable (no dvh-less cell in the matrix) — decidable from the spec regardless.
- **RR-2 missed M6 (adopted; verified) — MINOR** · `useShareState(props.onSceneRestore)` captures the callback by value at setup — benign at the one wired site (stable `runSceneSwitch`), but the idiom the repo elsewhere avoids, and the L-14 cure inherits it unless threaded as a getter. Booked for count integrity; the cure law lives in the L-14 row.

### INFO

- **D-13 — INFO** · `#backdrop` carries no out-of-flow contract; holds today by HeroAurora's `fixed inset-0` host discipline (both sides document the invariant — see S-2). One sentence in the slot doc cures it.
- **D-14 — INFO** · one command, four names (accname / tooltip / registry label / modal title — and the modal renders the registry label, so the button lists itself under a third name), plus the ZWJ-emoji hint in App's `hint` prop. Copy sweep.
- **D-28 — INFO** · `<Transition name="fade" appear>` starts the declared LCP `<h1>` at opacity 0; ≈1 frame (the corpus's own honest calibration). Trace before acting → **KF.W9**.
- **D-29 — INFO** · 29% of the file is ledger prose in coordinates that no longer resolve (T.D13, F.W15.S3, inv δ, …) plus the two phantom gates (D-19). The practice is still the best thing in the file (S-6); the coordinates need one sweep with D-19's cure.
- **L-11 / C-10 — INFO** · `initIOSPlatformClass()` — an app-bootstrap document mutation — executes in a leaf component's setup; main.ts owns every sibling global. Idempotent; misplaced, not broken.
- **L-7 / C-16 + RR-1 missed #8 — INFO** · `#header-left`/`#header-right` have zero overriding hosts, the right default is an indivisible three-control bundle, and the names are vestigial: both render into ONE right-anchored flex row (`row-reverse` applies to the band, not the actions' children — my styles read), minted for the ribbon where they were never accurate.
- **C-21 — INFO** · zero tests name EditorShell/HeaderRibbon/the glass seam; rides KF-APP-4's test-coverage cell.
- **C-22 + D-27 (first half) + RR-2 M7 — INFO** · EditorHeader.vue: barrel-exported, zero consumers, duplicates the ribbon incl. the same unwired `<SharePopover />` and the grow-and-dim composite — and RR-2's correction adopted: it does NOT ship (rolldown shakes the unused barrel member; markers absent from every built chunk), so deletion is source-hygiene, while the orphan `--header-items-max-w: 500px` DOES survive into the shipped cascade. The "one raw-px literal" cell is corrected (three literals — ruling above).
- **RR-1 missed #9 (adopted) — INFO** · index.html:38-39 declares the LCP `<h1 class="text-display-4">` — the live element is `text-display-mega` (`text-display-4` exists only as a scoped mobile font-size step); style.css names the correct rung twice. The single stale copy is the artefact a performance engineer reads first.

## Killed-claims register (executed refutations; nothing re-litigable without new evidence)

1. **D-2 (MAJOR, the 7.5×–14× theme-asymmetry table + its §7 step 5 cure) — KILLED, third-derived.** The corpus composited `color-mix(in srgb, …)` in linear light; browsers composite in device sRGB. My node oracle reproduces the corpus's table under the wrong model and both readers' figures under the right one, to every digit; the corpus's own S-4 falsifier carries the gamma numbers on the same page. Real spread: major 1.23× (dL* 8.65/10.62), fine INVERTS (2.34 light vs 2.19 dark); WCAG CR 1.249:1.249 major. **The remediation must not execute** — it would over-ink a mechanism that already delivers parity. Fine-tier 8-bit step = 7/255 in BOTH arms (the "collapses to one tier in the light arm" conclusion dies with it).
2. **D-1[R2]'s WCAG 2.1.1 escalation AS EVIDENCED — KILLED (RR-2), substance re-established at the bank.** The filing's sole evidence (the shortcut-registry enumeration) proves nothing about the dock path its own Blast-radius paragraph asserts survives; a Level-A verdict cannot ride evidence that never examined the alternate route. The CLAIM is nonetheless true and now lemma-complete (ruled above) — the kill is of the epistemics, and it is preserved because a fix wave quoting D-1:118 verbatim would be citing an argument that was invalid when written.
3. **D-3's mitigation cell / D-1:114 ("the dock copies are the better ones: … normal focus order, no inert wall") — KILLED (RR-1), third-derived by this seat.** reka `MenuItem` SELECTION_KEYS dispatch `click()` on the *item* (cancelled by `@select.prevent`; the nested button never activates) and `MenuContentImpl` preventDefaults Tab — the dock copies are mouse/touch-only, frontier-durable. The parent D-3 (two authorities for one command, three copies with EditorHeader) stands.
4. **D-16's "no heading at all in `<main>`" sub-claim — KILLED (RR-1's rescope ratified; RR-2's re-confirmation corrected).** TimingFunctionPanel's `<h3>` and EasingTarget's `<h2>` mount inside `<main>` (my reads).
5. **D-20 as framed ("two grow, the third dims") — KILLED (RR-1 ratified; RR-2's cell corrected).** SharePopover.vue:7's base string carries `scale-on-hover`; all three grow. Rescope booked.
6. **D-22's compounding half ("h3 under DialogTitle skips h2") — KILLED.** reka DialogTitle defaults `as: "h2"` (my read); the outline is contiguous.
7. **D-27's second sub-claim ("that file's one raw-px literal") — KILLED (RR-1 ratified; RR-2's M7 cell corrected).** Three `:root` px literals (my grep).
8. **D-6's mechanism ("abspos child removed from flex layout, so alignment has no effect") — KILLED, conclusion retained.** Flexbox §4.1: the static position of an abspos flex child IS align/justify-determined; inert here only because both axes are explicitly resolved.
9. **D-1/C-1's 12×40 rest-geometry figure — CORRECTED to ≈12×52** (inline-only clip; band padding; my styles+token reads). Booked because both axes and one banked cell reason about affordance from this rectangle.
10. **C-5's attribution half ("introduced by this migration — HEAD used the primitive's own size axis") — KILLED (RR-2 ratified by my own diff).** `aspect-square w-8` byte-identical across the hunk; `size="icon"` never existed in 7.0.0's ButtonProps. D-18 survives whole.
11. **L-12's hedge ("claiming 'throws' would be a false defect") — KILLED (RR-2 ratified).** The mechanism is fully decidable from the installed tree; the axis declined a true finding on a false premise. L-18 runs both ways.
12. **L-1 §1.0's escape-hatch framing ("no reproducible build exists in which L-1 is absent" — implying one exists to reach) — KILLED (RR-2 ratified).** No publishable glass-ui can supply an L-1-free `/header-ribbon`; the component is retired, not unpinned.
13. **Remediation step 1 across all three axes ("declare glass-ui ≥ the cut; nothing below is reproducible until it lands") — KILLED as unbuildable** (the deletion, my producer git reads). The corpus's causal ordering — fix L-2 first and L-1/L-3 fall out — is exactly inverted: the migration decision comes first.

**Reader-cell corrections by this seat (recorded against the returns, not the corpus)**: (a) RR-2's D-16, D-20, and "~12×40" cells re-confirmed corpus errors RR-1 had already disproven — and its missed-M7 repeated the killed "one raw-px literal" cell; (b) RR-2's missed M5 (the SURFACE_META duplicate, "no axis notices the duplicated table") is **already banked** at KF-APP-13 ≡ kf-ChannelControls L-7/C-4, byte-identity and all — an identity-guard miss inside a "missed" claim; the `extraTabsFrom` name-pair rider is added to that bank; (c) RR-1's "MbabbMenu.vue:8-14" anchors are worktree-dated — the shape is verbatim at `origin/master` (my probe), which is what makes the lemma durable; (d) RR-2's "GlassDock binds onFocusin … a reveal route plausibly exists" contradicts the bank it never consulted (kf-ChromeDock D-18: the focusable subtree is inert).

## Superlatives (provenance preserved; L-18 runs both ways)

- **S-1 · THE SUBSTRATE MECHANISM (both readers convergent; third-derived by this seat's node oracle)** — `EditorShell.vue:238-248` derives both graph tiers from one `color-mix(in srgb, var(--foreground) N%, transparent)` rule, and under the compositing model browsers actually use it is near-invariant across themes in BOTH metrics (CR 1.249:1.249 major; dL* 8.65/10.62; fine-step 7/255 both arms). The one-line token mechanism delivers exactly what its comment claims; the corpus filed a MAJOR against the file's best fact, and the killed D-2 carries the anti-cure lock protecting it.
- **S-2 · backdrop layering by DOM order, zero z-index (D-axis + both readers)** — `#backdrop` before `.grid-background`, two positioned `z-index: auto` fixed layers painting in document order, no containing-block traps, the invariant documented from BOTH sides (EditorShell.vue:5-9; HeroAurora.vue names the slot as its contract). The correct counter-example to the same file's L-7 slot-bundle trap.
- **S-3 · PRM honesty across three producer arms, composed correctly by the consumer (RR-1, one layer deeper than the axis)** — the a11y-overrides transition allowlist EXCLUDES `scale` (the hover lift snaps, not speeds), the fade is guarded, and the ribbon reveal carries its own reduce arm. None authored by the shell; credit is for composing without breaking any of the three.
- **S-4 · the `<main>` landmark comment (RR-2; premise verified)** — :67-73 rejects `display: contents` with the true mechanism (strips the box AND the implicit role) and its "single center cell" premise checks out (every sibling out of flow). Read with D-16: the box is correct and the page's subject was left outside it.
- **S-5 · the shortcut that registers itself into the modal it opens — FOLD → banked kf-AnimationControlsGroup L-S1/SUP-4** (the discarded-disposer-is-correct identity, already banked with its falsifier). This file's own addition: `{ label, group }` makes binding and discovery ONE object (`KeyboardShortcutsModal` renders the registry), so they cannot drift. Three axes and both readers each independently checked the "leak" and killed it — the bank exists so no future pass repairs it into bookkeeping.
- **S-6 · the comment corpus as a design artefact (RR-1)** — 77 of 262 lines, preserving *why each alternative lost* (:67-73, :201-207, :5-9); it is what let three axes and two readers falsify a dozen hypotheses statically. D-29 is right that the coordinates rotted; the practice stays the bar. Same voice in design-idioms.css's recorded scars (`icon-*`'s 61-call-site bug; `.focus-ring`; `.tap-floor`) — the tragedy is the shell's own cluster consumes none of the three.
- **S-7 · the dvh fallback — RESCOPED to the `.editor-shell` arm** (per the missed-#7 ruling): both axes covered, the licensing spec fact ("a browser without dvh also lacks dvw") written at the site, unlayered-scoped-beats-utilities placement confirmed empirically by RR-2 in the built sheet. The `.grid-background` arm is struck from the praise.
- **S-8 · type-only engine contact (RR-2)** — `import type { AnimationGroup }` is the file's entire contact with the library under test; the shell holds no engine state, so an engine leak cannot originate here — why C §5 correctly cleared the R1 class as "conduit, not originator".
- **S-9 · the producer's consumer census (RR-2; my own reads of the doc)** — `consumer-evidence/header-ribbon.md` cites its one consumer by file:line, counts per mode, and volunteers the consumer's own dead ref-forward. It is the reason L-3/L-5/L-17 were decidable without an install — and post-`4bf53962` it contradicts the producer's HEAD ("RETAINED" over a deleted tree), which is itself the sharpest cell in the deletion relay.

## UNPROVEN-NEEDS-LIVE residue (SS-13 inputs → KF.W9, ranked)

1. **The rest-state affordance read** — the ≈12×52 empty glass pill at the top-inline-end corner (geometry now settled; whether a fine-pointer user can perceive it as a control is not). Rides KF-APP-5's existing KF.W9 item with the corrected figure; frontier build preferred.
2. **The keyboard/touch courtesy witness** — one Tab-walk (expect: no stop reaches Share/theme anywhere) and one touch-tap on the dock menu rows (expect: inner buttons activate). Statically decided both ways; the witness is for the record, not the verdict.
3. **D-8's occlusion** — expanded ChromeDock at 375×667 vs the ribbon rect (z 40 over z 35, shared block-start band); EditorShell.vue:29's "no occlusion (inv δ)" is true of the bottom dock and unestablished for the top one.
4. **The clip-crop screenshot** — the hover lift inside `overflow: clip` (~1.6px/~1.28px derivation static; one hover frame confirms).
5. **D-11/L-9's perceived hard-cut** — the transport remount beside the subject cross-fade.
6. **D-28's LCP delta** — the `appear` fade's ≈1-frame deferral; trace before acting.
7. **The fine-tier visibility question** — whether a 7/255 step reads AT ALL under HeroAurora's wash, in either arm (the parity is settled; the absolute presence is display/eye).
8. **C-12's felt 700ms** — rides KF-APP-21's banked residue.
9. **The dvh-less legacy cell** — the `.grid-background` fallback arm's regression is spec-decided; a live confirmation needs an old-Safari cell the matrix likely lacks (noted, not demanded).

## DISSENT — preserved, not ruled away

1. **The identity-guard event, third consecutive record, now in BOTH row classes.** Both re-readers re-confirmed four banked rows as fresh (KF-APP-5, SCH-1/F-1, KF-APP-4, KF-APP-21), and RR-2 filed a banked identity (KF-APP-13's SURFACE_META fork) under "missed by all three axes". kf-ControlsPaneWrapper recorded the miss-direction, kf-ChromeDock the confirm-direction; this record has both in one return. The M-25 agglomeration law restates itself: EVERY re-reader row — confirmed, killed, or missed — runs through the bank before booking.
2. **The RR-2 kill vs RR-1 lemma on WCAG 2.1.1.** RR-2's position — a Level-A verdict may not ride evidence that never examined the alternate path — is correct and preserved as method law even though the claim it struck is true. RR-1's position — the alternate path was examinable statically all along — is also correct. The synthesis (kill the filing, complete the lemma, sustain at the bank) is this record's ruling; neither reader alone had it.
3. **Inter-bank tension recorded, not resolved**: kf-ChromeDock's substrate caveat says MbabbMenu is "MOVED at the frontier (−146 lines)"; kf-CubeScene's substrate ruling says MbabbMenu is "byte-identical between the worktree and origin/master". This seat's own probe shows the load-bearing shape (the `@select.prevent` menuitem wrappers around SharePopover/DarkModeToggle) present verbatim at `origin/master`, which is all this record relies on — but the two banked cells cannot both be right and the KF.W0 substrate settle should reconcile them.
4. **Severity dissent on the deletion** (RR-1 MAJOR vs RR-2 BLOCKER) — ruled blocker-weight at the banks; RR-1's grade preserved as the component-local view (the shell still renders today against the installed artifact; the break is at the next producer advance).
5. **kf-App ruling 5's "rest-state geometry genuinely undecidable statically" cell** — refined, not contradicted: the ANCHOR rule is sizeless (that cell's fact), but the BAND box derives from `min-block-size` + `--panel-padding` + the uncollapsed 40px cross-size. The live question was always the affordance, never the rectangle.
6. **RR-2's downgrade of C-4 to MINOR** dissents from axis C's MAJOR; adopted here (one host, provider present, demo excluded from the published surface), with axis C's grade preserved as the view from the barrel-export contract.

## Closing verdict

**ADJUDICATED at worktree `8281638c` (EditorShell.vue and its whole blast surface dirty; anchors re-resolve at `origin/master` before any cure) — RED.** 51 rows stand: 8 MAJOR · 23 MINOR · 9 INFO booked here, plus 11 folds to banked identities of which four are blocker-weight at their banks — the inert-ribbon lockout (KF-APP-5, now carrying this record's completing WCAG 2.1.1 lemma: no keyboard route to Share or theme-toggle exists anywhere in the app, dead at three independent layers, frontier-durable), the producer's outright deletion of HeaderRibbon under its sole consumer (first-banked here, with the KF.W0 sequencing tripwire: settle no substrate past `4bf53962` before the shell migrates off `/header-ribbon`), the phantom dependency (SCH-1, frontier-cured), and the no-SFC-typecheck gate (KF-APP-4). Thirteen corpus claims killed on executed refutations — the heaviest a compositing-model artifact that had filed a MAJOR against the file's single best mechanism, killed to every digit by this seat's own oracle and locked against its own remediation. The shell is genuinely well-made where it is structural (the substrate retint, the DOM-order backdrop contract, the landmark box, the PRM composition, the self-registering shortcut) and wrong where it trusts chrome it does not own and prose it did not re-read: its entire header band rides a component the design system has deleted, reachable by hover alone, measured by no gate that exists. Routings are M-25 agglomeration input for the forming X·KF specs: KF.W0/W4/W6 own the folds, KF.W9 owns the nine-item SS-13 residue (rest-state affordance first), the migration decision and everything behavioral are NO-WAVE-OWNER for SS-1/SS-2. Nothing here stamps a wave.

— end of apotheosis. The three challenge axes and both re-reader returns remain immutable beside this file per E-1/E-3.
