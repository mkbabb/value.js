claude-fable-5

# kf-ChannelControls — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter)

**Seat**: fresh Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ HEAD `8281638c` (branch `master`) · registry branch `tranche-u` (value.js).
**Corpus**: three challenge axes by `claude-opus-5[1m]` — `challenge-D-design.md` (18 defects / 5 superlatives), `challenge-L-library.md` (20 / 4), `challenge-C-consumption.md` (16 / 3) — attacked by two hostile re-readers: **RR-D** (design-and-usage) and **RR-L** (library-and-code). Both re-reader returns were themselves challenged by this seat before adoption.
**Method (L-18/M-23)**: every verdict-level disagreement between the re-readers was re-derived against the tree with this seat's own instruments; every kill was re-executed at its decisive file; every net-new find was re-verified at source before banking; every suspiciously-agreed cell that a banked row or a wave will lean on (the C-4 value-identity claim, the §0 grep, the cube-3 enumeration) was re-measured, not spot-trusted. **No browser tooling** (law); all rulings static + installed-dist reads; the irreducibly visual residue rides to SS-13.
**Arbiter's own instruments this session (all read-only)**: `ChannelControls.vue` whole (456 L); `KfPillTabs.vue` whole (124 L); `RibbonBar.vue` :1-20; `ControlsPaneWrapper.vue` :16-66 + `.css` :44-52/:120-150; `useKeyframesPaneReveal.ts` :50-136; `useTabStripScroll.ts` whole (80 L); `useScrollFade.ts` :88-116; `useTimeline.ts` :18-30 (+ `createGlobalState|useStorage` grep → 0); `transport/composables/useKfPillTabs.ts` whole (4 L); `KfPillTabs/useKfPillTabs.ts` :20-30; `surfaceTabs.ts` :1-30 vs `controlSurfaces.ts` :130-165 (label-level diff, both copies); `controlOptionsStore.ts` :8-16; `ChannelOptions.vue` :375-380/:461-477; `AnimationControlsGroup.vue` :76-90/:190-198/:313-322; `App.vue` :52-64/:88-100/:305-312; `CubeScene.vue` :165-180; `EditorShell.vue` extraTabs lines; `tab-idiom.css` whole; `useCubeDemo.ts` superKey/group greps; `design-idioms.css` :44-49/:102; installed glass-ui 7.0.0: `glass/material.css` (`contain: paint` set), `glass/ladder.css` (`--muted-foreground` repoint inside the wash rungs), `tokens/on-glass-fg.css` + `tokens/dark-arm.css` (`--on-glass-muted` both arms), `fonts.css` (`font-weight: 200 800` — variable face), `components/fading-scroll/` (exists; `axis`/`fadeStart`/`ariaLabelledby`), `theme/radius.css` (`--radius-strip: 0.75rem`, `--radius-tab`), `tokens/shadow.css` (`--shadow-cartoon-md: -3px 3px …` — LEFT and DOWN), `Button.vue.d.ts` (`loading?: boolean`), `SegmentedTabs.vue.d.ts` (`SegmentedTabsSemantics = "toggle" | "tabs"`), `a11y-overrides.css` PRM grep (1) vs `tw-animate.css` (0); executed greps: `TABS_EXTERNALLY_MANAGED_KEY` (5 hits), `tabsContentEl` (2), `role="tab` (7), `aria-labelledby|aria-controls` (0), `single-surface-panel` (self-only), `tab-trigger` (prose-only), `dir=` (0), `parseCssColor` (1 file, `scenes/square/`), shim consumers (ChannelControls.vue:230 sole); census re-read `formation/keyframes/lane-frontend.md` F-1/F-5 (§7.3 covers BOTH shims; §10 step 3 names `ChannelControls.vue:230`), S-1 :264, S-2 :308, §10 wave order.

## Scoreboard

**50 ADJUDICATED-CONFIRMED (2 BLOCKER · 12 MAJOR · 27 MINOR · 9 INFO) — 42 booked here, 8 FOLDED by reference to banked kf-AnimationControlsGroup / census rows (identity guard, zero re-bookings) · 9 KILLED (with executed refutations, incl. one kill-of-a-confirm) · 10 net-new re-reader finds BANKED (N-1..N-10) + 4 riders + 1 arbiter-own rider · 11 UNPROVEN-NEEDS-LIVE rows to SS-13.**

The net-new blocker of this corpus: **L-2/C-2** — `:key="storedControls.selectedControl"` on the teleported wrapper remounts `KeyframeTimeline` on any surface switch while expanded, silently destroying every authored keyframe and the whole undo/redo trail. The other blocker-weight fact (N stacked timelines) is the **same identity** as the banked kf-AnimationControlsGroup D-1 and is folded, not re-booked — this record contributes the source-side evidence the sink-side booking lacked.

Status verbs per M-25: dispositions are **ADJUDICATED**, not VERIFIED — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets from the census lane taxonomy (`lane-frontend.md` §10): **KF.W1-DEP** (F-1) · **KF.W2-TABS** (S-1) · **KF.W3-SHIM** (F-5) · **KF.W4-PROSE** (S-2) · **KF.W6-TIMELINE** (S-3/S-4). Everything the census does not own is **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring.

## Ruled disagreements (the seat's actual work)

| id | RR-D (design-and-usage) | RR-L (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-4 mechanism + grade** | CONFIRMED MAJOR; strengthened via shadow-direction (cartoon shadow casts bottom-LEFT, the extra 12px sits on the affordance-free right) | KILLED the scrollbar-gutter diagnosis + demoted MINOR (outer `.controls-pane` is the pane scroller; `RibbonBar.vue:2` carries the byte-identical inset — a systematic pane idiom) | **MINOR — RR-L's grade and systematic-idiom fact adopted; RR-D's shadow evidence BANKED; the scrollbar-locus question goes to SS-13** | My reads: `ControlsPaneWrapper.vue:33-36` binds `paneScrollable ? 'overflow-y-auto' : 'overflow-hidden'` on the OUTER pane — RR-L's premise real; but ChannelControls' inner `flex-1 min-h-0 overflow-y-auto` (:19/:85) is height-bounded inside the `h-full` column, so WHICH box scrolls is layout-dependent — neither reader may claim it statically. `RibbonBar.vue:2` `pl-4 pr-7 pb-2` byte-identical — two sites, one propagated idiom, not local accretion. `ControlsPaneWrapper.css:129-133`: "the cartoon offset shadow casts bottom-LEFT" + `--shadow-cartoon-md: -3px 3px …` (my token read) — the 12px extra rides the side with zero shadow affordance while the left (needing up to 7px) got 4px less. Raw asymmetry CONFIRMED; diagnosis CONTESTED; visual weight → SS-13. |
| **D-15** | KILLED as filed and REFILED live: the tablist's own `contain: paint` clips the 3px focus ring at its 2px padding box | Left UNPROVEN ("requires layout measurement") | **RR-D's refile ADOPTED at MINOR (latent) — statically derived by this seat** | My reads: glass-ui `material.css` `.glass-wash, .glass-quiet, .glass-resting, .glass-card { contain: paint; }` — the TABLIST itself carries `glass-wash` (`KfPillTabs.vue:17`); paint containment clips ink overflow at the padding edge; track `padding: 0.125rem` = 2px (:85); ring = `outline: 2px` + `offset: 1px` = 3px past the pill border box (:120-123). 3 > 2 ⇒ ~1px of the keyboard-focus indicator clips on the strip perimeter (pills flush at the content box on the outer edges). D-15's "fits with 1px to spare" reasoned against the WRONG clip (the header's 4px composite); the nearer boundary is the tablist's own. Latent only because §0 keeps the strip off-screen. Banked as **N-1**; rendered extent → SS-13 only if the strip is restored. |
| **L-9** | KILLED the failure scenario ("a header that appears post-mount" is unreachable — `tabsExternallyManaged` is injection-constant per instance; the only other branch selector is dead per D-7/L-4); survives as INFO fragility | CONFIRMED MINOR ("structurally fragile, currently latent") | **RR-D wins; RR-L's MINOR confirm is STRUCK — a kill-of-a-confirm** | My whole-file read of `useTabStripScroll.ts`: the resolve is a one-shot `onMounted` querySelector (:65-74) — the structural observation is real; but the header's `v-if` (:56) is static per instance in BOTH hosts, so no reachable state loses a fade that ever existed. A defect row needs a failure mode; this has a fragility note. Demoted **INFO**. |
| **S-B (contrast superlative)** | KILLED the working: inside `.glass-wash` glass-ui repoints `--muted-foreground` to `--on-glass-muted`, so every number in the table is computed from the wrong token | Carried it as UNPROVEN (composited backdrop) | **Working KILLED (this seat's own ladder.css read); verdict UNPROVEN → SS-13** | My grep of `glass/ladder.css`: `:where(.glass-card, .glass-resting, .glass-quiet, .glass-wash) { … --muted-foreground: var(--on-glass-muted); … }` — verbatim. Operative ink = `hsl(30 26% 35%)` light / `hsl(34 16% 72%)` dark (my token reads), not the `--neutral-5` pair the axis computed. Both substitutions move in the safe direction, so the pass likely survives — but no ratio in the corpus is bankable. Re-derivation over the composited plate is SS-13 input #2. |
| **D-10's variable-face half** | Verified (`font-weight: 200 800`) | UNPROVEN ("did not verify the installed face") | **SETTLED by this seat's own grep — D-10 fully CONFIRMED (latent)** | `dist/styles/fonts.css` carries `font-weight: 200 800` (variable face). The metric-affecting `transition: font-weight` under the "only the activation channels change" comment (`KfPillTabs.vue:101-105`, 500→600 at :115-117, `flex-shrink: 0` row) is a live mechanism the moment the strip renders. Removed from the unproven ledger. |
| **D-2/C-3 runtime framing** | Corrected both directions: at most ONE orphan tabpanel exposed at a time (mutually-exclusive `v-if`s + `inert` on the keyframes pane); AND a FOURTH orphan tabpanel ships from `CubeScene.vue` via `h()` | (kept "three regions ship"; killed only the BLOCKER grade) | **RR-D's both corrections ADOPTED; defect CONFIRMED MAJOR** | My read of `CubeScene.vue:171-180`: `storedControls.selectedControl === "matrix-controls" ? h("div", { role: "tabpanel", "data-state": "active" }, [h(MatrixEditor, …)]) : …` — gated on the RAW store, no `tabindex`, invisible to the corpus's `role="tab` grep (7 hits, none of them this). The a11y tree exposes one unnamed tabpanel per view, not three; the enumeration was four sites, not three. Both the count and the search method are corrected; the conclusion (no tablist, no name, zero `aria-labelledby` app-wide — my grep: 0) survives untouched. |
| **L-5's two dispositions** | Split "never renders" from "runs and does nothing": `useTabStripScroll` registers listeners/observers and the :408 watch runs two null-guarded probes per switch ×N hosts | KILLED the cost clause: `builtInTabs` is NEVER evaluated (lazy computed; third conjunct behind `length === 1`, false for every live scene) | **BOTH adopted — they are about different rows and are simultaneously true** | My read of :331-336: conjunct order is `tabsExternallyManaged && length === 1 && builtInTabs…` — in the App the first passes, the second is false for every live scene (cube/amiga/square 3-4, easing 4, spring 4, sequence/home 0 with no wrapper mount), so the third never evaluates and RR-L's short-circuit kill stands. Separately `useTabStripScroll` (:398) and the watch (:408) DO execute forever with no effect — RR-D's sharper disposition language is what the deletion spec should inherit. |
| **C-4 value-identity (suspicious agreement)** | asserted "value-identical today" | asserted "copies are value-identical today" | **RE-MEASURED by this seat — identity HOLDS; near-miss recorded** | Copy B (`surfaceTabs.ts:12-19`) carries `easing → "Curve"`, `spring → "Physics"` — the exact "T.E8-class edit" C-4 hypothesized as the future drift event. This seat diffed copy A (`controlSurfaces.ts:145-160`) at the label level: it ALSO carries Curve/Physics (the T.E8 edit landed in both). Identity holds; the latent-drift verdict stands as filed. Recorded because both re-readers inherited the identity claim without a label-level diff — one edit to one file away from live drift, and nobody would have noticed. |

## Adjudicated defect roster

Ids keep their axis names for life; net-new re-reader finds are banked as N-1..N-10. Line anchors are keyframes HEAD `8281638c`; re-resolve before executing any cure.

### BLOCKER

- **L-2 / C-2 — ADJUDICATED BLOCKER · `:key="storedControls.selectedControl"` on the teleported timeline wrapper destroys all authored keyframes and the undo/redo trail on any surface switch while expanded.**
  Proof (this seat's own): `ChannelControls.vue:187-191` — the `:key` sits three lines under the comment (:182-185) whose stated purpose is to DECOUPLE the Teleport lifecycle from mounts; `isTimelineVisible` (:377-379) stays true on `isTimelineExpanded` alone, so `selectedControl` is free to move while the node stays mounted and keyed. `useTimeline.ts:22-27` opens `state = ref({ keyframes: [], … })` literal-empty; my grep for `createGlobalState|useStorage` in the file → 0 — no rehydration path exists (the strongest falsifier, attacked and dead). Independent writers of `selectedControl` while expanded: `App.vue:307-310` (`onDockSelectControl` via ChromeDock's `<Select>`), `AnimationControlsGroup` `switchTab` (:315-320), `ChannelControls.vue:403`. Silent, unrecoverable, one-token cause; the `defer`+`:disabled` pair already handles relocation, so the key buys a 3-token entrance animation with the user's data.
  Terminal disposition: **NO-WAVE-OWNER** (SS-1/SS-2 spec input), and it MUST be settled before **KF.W6-TIMELINE** — any timeline-cluster swap that inherits the `:key` inherits the data loss. Cure space recorded, not chosen: drop the key; if the entrance effect is wanted, retrigger it without keying the stateful subtree.
- **D-1 / L-1 — BLOCKER · FOLD → banked kf-AnimationControlsGroup D-1 (the portal stacking: one singleton sink, N Teleport sources; cube/amiga 3, spring 2). Identity guard — NOT re-booked.**
  This record contributes the **source-side evidence** the sink-side booking lacked, all re-verified by this seat: (i) the discriminator exists and is ignored — `active` is supplied (`ControlsPaneWrapper.vue:60`) and forwarded to ChannelOptions (:108) but never read by this component; the Teleport (:186) has no `active` term and `isTimelineVisible` (:377-379) reads only the shared store; (ii) the condemning contrast one level down: `ChannelOptions.vue:377` `<Teleport v-if="active" to="#controls-ribbon-target" defer>` — the correct idiom, present in the same subtree; (iii) the Teleport also bypasses `hasSurface('timeline')` entirely (L-8's sharp half) — a persisted `isTimelineExpanded: true` mounts a timeline for a scene whose DFA set excludes it; (iv) the N-instance cost is not confined to the expanded case: with `selectedControl === "timeline"` collapsed, all N hosts mount a `KeyframeTimeline` (each with its own `useRefHistory` capacity-50 deep-clone trail), N−1 inside `display:none`. Rendered-stack geometry stays at the group record's SS-13 row (its UNPROVEN #2 — one screenshot of spring expanded remains the cheapest decisive probe in either corpus).

### MAJOR

- **§0 / C-1 / D-3 / L-5 — MAJOR (C-1's BLOCKER grade killed) · the entire tab-strip half of the component is unreachable in the shipped app; census S-1's liveness premise is CONTRADICTED.** My greps: `TABS_EXTERNALLY_MANAGED_KEY` = exactly **5** hits (decl `injectionKeys.ts:4`; `App.vue:135` import + `:169` unconditional `provide(…, true)`; `ChannelControls.vue:240` import + `:277` inject); one `createApp`; one importer. `:56` `v-if="!tabsExternallyManaged"` is permanently false ⇒ KfPillTabs (:74-82), `stripOptions` (:313-324), the `extraTabs` pipeline, `useTabStripScroll` (:398 — executes forever, effect-free), the `.tabs-overflow-*` block (:441-455), and the T.G9 interaction warm (:79-80) never paint. Census correction: S-1's "It is live, not dead" — the render SITE is live, the rendered NODE is not; **§10 step 2 is a DELETE, not a REPLACE**. Disposition: **FOLD → KF.W2-TABS** carrying (a) the liveness correction, (b) RR-D's runs-vs-renders disposition split, (c) the spec obligation to **re-home the T.G9 interaction warm onto ChromeDock's control `<Select>`** before deletion (else the perf mechanism dies with the branch — L-6).
- **D-2 / C-3 — MAJOR (D's BLOCKER grade killed; framing corrected per ruling) · orphan `role="tabpanel"` regions with no tablist, no `role=tab`, no id/`aria-labelledby` anywhere in the shipping document — four sites, one exposed at a time, and the file names the rule it breaks at :29-30.** My greps: `role="tab` → 7 hits, none a live tablist (KfPillTabs never renders; ChromeDock draws `<Select>`, my read :235); `aria-labelledby|aria-controls` → 0 app-wide; plus the grep-invisible fourth panel (`CubeScene.vue:171-180`, `h()`-rendered, raw-store-gated, no tabindex — mutually inconsistent with its three siblings). Disposition: **NO-WAVE-OWNER** (a11y spec input; must be an input to KF.W2-TABS — the panels' naming story has to be decided with the strip's fate; pairs with the banked group D-5/M-6 rows on the teleport-destination side).
- **L-3 — MAJOR (L's BLOCKER grade killed by both re-readers; adopted) · one real Monaco editor force-mounted PER CHANNEL on the first idle tick.** My read of `useKeyframesPaneReveal.ts`: `scheduleIdleWarm()` unconditional at setup (~:102) in every mounted instance (v-show hosts, my wrapper read); the `{immediate:true}` warm watch keys off the SHARED store; the mount gate (:130) consults `hasSurface` (scene-wide) and `keyframesWarmed`, never `active`. Cube spins 3 editors, 2 permanently inert — inverting the file's own T.G9 goal (bytes deferred once, instances multiplied by N). Resource multiplication, not breakage ⇒ MAJOR. Disposition: **NO-WAVE-OWNER** — one cure family with D-1/L-1's missing `active` gate (the same one-line class of fix covers the Teleport, the pane mount, and the warm).
- **L-7 / C-4 — MAJOR · `SURFACE_META` exists TWICE with independent bodies; the two tab hosts read different copies; "one authority, no drift" is false at both assertion sites.** My reads: copy A `controlSurfaces.ts:134/:145/:189/:278` vs copy B `surfaceTabs.ts:6/:12/:21/:25` — copy B imports only `BUILT_IN_SURFACES`/`ControlSurface` and re-declares all four symbols; ChannelControls imports A (:245-250), ChromeDock imports B (split-brain import block), TransportDock reads B's `dockCardinality`. **Value-identity re-measured by this seat at the label level — both copies carry the T.E8 `Curve`/`Physics` edit; identity holds today** (see ruling; one one-file edit from live drift). The cited `proof:dfa-derived` gate is unrunnable (two `proof:*` scripts ship, neither it). **Rider (RR-L net-new, verified)**: in the shipped app this component never reads the registry at all — `builtInTabs` has two readers, the dead strip and the short-circuited third conjunct of `isSingleSurfaceScene`, both lazy ⇒ the :245-250 imports are load-bearing only for the type checker, so the lockstep prose is void twice over. Disposition: **NO-WAVE-OWNER** (collapse copy B into copy A; repoint ChromeDock/TransportDock; correct both invariant comments).
- **C-6 — MAJOR · the vendor prose asserts glass-ui 4.0.1 facts against an installed 7.0.0 that answers BOTH fork rationales; the file contradicts itself (pill at :57-65 vs `variant="underline"` at :220-228).** My own d.ts read: `SegmentedTabsSemantics = "toggle" | "tabs"` + `semantics?:` — material and ARIA role decoupled, closing the design question census §10 step 2 left open (a census-level correction: step 2's residual verification is answered at the contract level; emitted DOM → SS-13, non-gating since §0 makes it delete-not-port either way). **Arbiter-own rider**: `KfPillTabs.vue:45-46` — "This local tablist remains until the published Glass component can provide pill material with tablist semantics and roving focus" — that condition is MET by installed 7.0.0 (`semantics="tabs"` + `variant="pill"` + `useTabRovingFocus`); even the fork's newest justification is stale. Disposition: **FOLD → KF.W2-TABS + KF.W4-PROSE**.
- **C-7 — MAJOR · the exposed keyframes handle is a nullable the parent cannot see; RibbonBar's four keyframes actions paint live and silently no-op through the warm/async-resolve window.** My reads: `defineExpose` (:410-414) hands the raw template ref (precision: typed `InstanceType<typeof KeyframesStringControls>` at :372, NOT `any` — the defect is nullability, not typing); `RibbonBar.vue:13` gates the block on the same tick the warm fires; the four `@click="activeKeyframesRef?.…?.()"` buttons carry no `:disabled`/`loading` while glass-ui `Button` ships both (my d.ts grep). Doubly-deferred pane (warm gate + Monaco chunk) ⇒ the window exists statically; duration → SS-13. Disposition: **NO-WAVE-OWNER** (expose a readiness flag beside the handle; bind the levers).
- **C-8 / D-6 — MAJOR · the timeline tabpanel is empty in the ordinary (selected + collapsed) case; the widget renders as its sibling outside every panel — inverted state coverage (the panel has content only when the timeline is elsewhere).** My template read: the panel's only child is the `v-if="isTimelineExpanded"` placeholder (:156-171); `</div>` at :172; slot :180; Teleport :186 — siblings. The empty panel still carries `tabindex="0"` (D-14's sharpest site). The keyframes pane (:129-137) proves the file already knows the correct force-mount-inside-panel resolution. Disposition: **NO-WAVE-OWNER**, same expanded-timeline spec family as D-1 (the structure and the portal must be settled together, before KF.W6-TIMELINE).
- **D-5 — MAJOR · both async panes ship with no loadingComponent/errorComponent/delay/timeout, outside the app's only `<Suspense>`.** My reads: :252-253 bare `defineAsyncComponent`; App's Suspense (:90-99) wraps the scene component with a `SceneSkeleton` fallback — the controls chain descends outside it. Chunk-load failure ⇒ permanently blank labelled panel, no message, no retry; chunk-load window ⇒ empty `role=tabpanel` with no indicator (compounds C-7). Disposition: **NO-WAVE-OWNER** (async-boundary states; the `ChannelOptions.vue:531-534` "honest pre-load frame" shows the house idiom).
- **D-7 / L-4 — MAJOR · `isSingleSurfaceScene` is unreachable under the live T.B2 derivation and its 20-line rationale names its own counterexamples (easing/spring are 4-member sets, not singletons).** Enumeration adopted from both readers and consistent with this seat's reads of the derivation chain; `single-surface-panel` grep → self-referential only (my run). A second structurally-different mount path nobody can review against a rendered frame, carrying its own copy of the D-4 inset. Disposition: **NO-WAVE-OWNER** (delete the branch); the counterfactual rationale block rides **KF.W4-PROSE**.
- **L-8 — MAJOR · three readers of "the active surface" split across two authorities inside a file whose comments claim "one authority, no drift"; the steady-state double-panel hypothesis was honestly withdrawn by the axis and stays dead (re-run by both re-readers).** The unambiguous half — the Teleport gated on NEITHER the projection NOR `hasSurface('timeline')` — is carried as rider (iii) on the D-1/L-1 fold. Disposition: **NO-WAVE-OWNER** (contract row; one authority for the gates, `:key` dies with L-2).
- **F-1 / L-20 / D-0 — MAJOR · FOLD → census F-1 (KF.W1-DEP). Identity guard — booked once at the census, never per component.** Component rider: `ChannelControls.vue:219` (root-barrel `TooltipProvider, Button`) is the first import that fails on a clean `npm ci`; every glass-ui token/utility/d.ts claim in this record is resolved against the INSTALLED 7.0.0 and inherits F-1's reproducibility caveat.
- **(fold) teleport-destination a11y — MAJOR · FOLD → banked kf-AnimationControlsGroup D-5 (sink has no role/name/`aria-expanded`/`aria-controls`; focus never moves in or restores out) + M-6 (the Teleport severs tab→tabpanel containment; no `aria-owns` tie).** RR-D re-derived the same facts from the source side and graded MAJOR; the identity is the group record's — no re-booking. This record adds only corroboration: my `aria-labelledby|aria-controls` grep → 0 app-wide at this HEAD.

### MINOR

- **D-4 — MINOR (rescoped per ruling) · `pl-4 pr-7` = a 12px unbalanced inset on the content plate over a symmetric 12/12 parent; replicated byte-identically at `RibbonBar.vue:2` (a propagated idiom, two sites); the documented rationale in the cluster is shadow clearance and the shadow casts bottom-LEFT — the extra padding rides the affordance-free side.** Scrollbar-locus and visual weight → SS-13. NO-WAVE-OWNER (two-site fix).
- **D-8 — MINOR · the overflow probe measures a box that can never scroll (`.kf-pill-tabs` is `inline-flex`, no overflow; `overflowStart = scrollLeft > threshold` pinned 0 — my `useScrollFade.ts:96-99` read) while `scrollActiveTabIntoView` scrolls the header; `.tabs-overflow-left/-both` unreachable; indicator desynchronised by construction.** Citation correction banked: 4 declarations, not 8. Dies with **KF.W2-TABS**.
- **D-9 — MINOR · `.glass-wash` nested in `.glass-wash` — the same ladder rung twice (plate, blur, grain, rim, 1px border, `contain: paint` — each doubled), equal 12px radii at 8px/2px insets, non-concentric by construction.** The a11y half of the doubled rung is N-1 (the ring clip). Composited delta → SS-13. Dies with **KF.W2-TABS**.
- **D-10 — MINOR (fully confirmed per ruling — variable face verified by this seat) · `transition: font-weight` on a variable font animates the strip's own metrics; every pill right of the active one slides each selection change; the "only the activation channels change" comment lists the one metric-affecting channel. PRM users spared by the substrate blanket; nobody else.** Latent under §0; dies with **KF.W2-TABS**.
- **D-11 — INFO→held at INFO in the INFO section per the D axis's own grade; listed here for adjacency only.** (See INFO.)
- **D-12 — MINOR · physical-axis chrome + direction-bound prose; no logical properties; `dir=` grep → 0 (my run) so latent, correctly graded.** The "Timeline expanded below" copy is spatially honest on both current layouts (verified by RR-D against both placements). NO-WAVE-OWNER.
- **D-13 — MINOR · the `#tabs-trigger` slot pipeline is forwarded through FOUR ancestors (App :53-59 — live at my read → EditorShell → AnimationControlsGroup → ControlsPaneWrapper) into a component declaring only two `tabs-content` slots (:32, :180); the next scene exposing `tabsTrigger` loses it silently; contradicts the file's own :47-51 "retire" claim.** RR-L's hop-count correction (four, not three) adopted. NO-WAVE-OWNER.
- **D-14 — MINOR · `tabindex="0"` on panels with focusable content (:101, :133 — the reveal-focus needs `-1`, and :133 already toggles to −1 when inactive, so the instrument is in hand); the empty timeline panel's :153 is the sharpest instance.** NO-WAVE-OWNER (rides the D-2/C-8 a11y spec row).
- **C-13 — MINOR · a "single DOCUMENTED vendor-DOM contract" exemption claimed for a raw querySelector into a FIRST-PARTY 124-line SFC (my `useTabStripScroll.ts` whole read: the docblock names `<SegmentedTabs>`/sonner; the strip is KfPillTabs, one `defineExpose` from a real handle).** Dies with **KF.W2-TABS**; the prose half rides KF.W4-PROSE.
- **C-14 — MINOR · the global `[data-state="active"][role="tabpanel"]` enter animation (tab-idiom.css:75-79, global via design-idioms.css) re-fires over the force-mounted Monaco subtree on every switch-back, in the same frame the `content-visibility` cache is released.** Mechanism confirmed at my tab-idiom read; cost → SS-13. NO-WAVE-OWNER.
- **C-15 — MINOR · 48 lines of `.tab-trigger-*` skin with zero consumers (my grep: prose-only hits), plus two false prose claims (this file's :213-214 "survive for the scene tab triggers"; tab-idiom.css's own header "imported by AnimationControls.vue … loads whenever AnimationControls mounts" — it loads globally via design-idioms.css).** Joint fact with N-3. Delete + **KF.W4-PROSE**.
- **L-6 — MINOR (L's MAJOR killed by RR-L's bounds; adopted) · the T.G9 interaction-warm contract is void in the shipped shell (the warm hooks ride only the dead strip — my grep: two sites).** Damage bounded by the rIC(4000)/1500ms fallback; latency delta → SS-13. Carried as the KF.W2-TABS re-home obligation (see the C-1 row).
- **L-11 — MINOR · `useTemplateRef<any>("keyframesPaneEl")` (:388) on a literal div + the `$el ?? value` unwrap two lines under the comment stating the ref is a plain div (my `useKeyframesPaneReveal.ts` read).** NO-WAVE-OWNER.
- **L-12 — MINOR · `selectControl(key: string | number)` + `.toString()`: the number arm is unreachable from both callers (KfPillTabs emits `[value: string]`; `switchTab` passes a string — my AnimationControlsGroup read).** NO-WAVE-OWNER.
- **L-13 — MINOR · both composable option types declare writable `Ref` for readonly template refs; RR-L's verified extension banked: the inversion also runs the other way at `useTabStripScroll.ts:11` (`overflowClass: Ref<string>` fed by a ComputedRef).** NO-WAVE-OWNER.
- **L-16 — MINOR · engine fields bound as props across the `markRaw` boundary: `setTargets` REASSIGNS `this.targets` (a new array identity) with live demo callers, so `KeyframeTimeline` can hold a stale target array until an unrelated dep re-renders; RR-L's twin-precision banked: the `options` half mutates in place (live-but-untracked, the milder failure).** Stale frame → SS-13. NO-WAVE-OWNER.
- **L-17 / C-11 — MINOR · icon metadata resolved from SURFACE_META, carried on builtInTabs, silently dropped (KfPillTabOption = `{label, value, disabled?}` — my read; extras pre-stripped at :318-321, builtins spread through untouched) — two treatments of one non-fact, and a capability regression vs `SegmentedTabOption`'s `icon?`/`tooltip?`.** Dead under §0, live the moment anyone "fixes" C-1 by un-hiding the strip. FOLDs into **KF.W2-TABS** (census S-1 capability record).
- **(fold) L-14 / C-12 — MINOR · FOLD → banked kf-AnimationControlsGroup C-4 / census S-2 (the `extraTabs` chain: `SegmentedTabOption[]` threaded three hops, re-typed `KfPillTabOption[]` at the terminal consumer; defaulted `() => []`, nothing overrides — my EditorShell read; structurally assignable, so contract-quality, not a type error).** One identity; the census row owns it.
- **(fold) L-15 / C-10 — MINOR · FOLD → census F-5 (KF.W3-SHIM).** The 4-line `transport/composables/useKfPillTabs.ts` shim (my read, verbatim), sole consumer `ChannelControls.vue:230` (my grep) — census §10 step 3 names this exact repoint; the duplicate-name hazard (`KfPillTabs.vue` beside `KfPillTabs/`) rides the same row (census §7.4). Violates `feedback_no_backwards_compat`; under §0 the whole triple deletes together.
- **(fold) L-10 / C-5 / D-16 — MINOR (C's MAJOR killed by RR-L; consistent with the banked grade) · FOLD → banked kf-AnimationControlsGroup D-10/C-5/L-9 (the tooltip register: App defaults → group `100/0` → this component's byte-identical third provider, ×N per scene, HEAD's own commit message contradicted).** One identity — the group row already names ChannelControls.vue:2 as the descendant re-provide to delete. No re-booking.
- **(fold) expanded-timeline motion — MINOR · FOLD → banked kf-AnimationControlsGroup D-4 (opacity transitioned but never changed; skin properties outside the transition list snap; collapse cannot animate — the Teleport empties the box in the same tick `max-height` starts collapsing).** RR-D re-derived the identical three consequences from this side — same identity, not re-booked. **Rider banked (new)**: the affordance's motion vocabulary is self-inconsistent — the placeholder promises a downward move (`ChevronDown` + "Timeline expanded below", :160-161) while the widget's entrance is `slide-in-from-right-2` (:190).
- **(fold) Teleport magic strings — MINOR · FOLD → banked kf-AnimationControlsGroup M-9 (`#timeline-expanded-target` unshared across two files beside the established `injectionKeys.ts` seam).** **Extension banked (RR-L net-new, verified by this seat)**: the SECOND contract pair — `#controls-ribbon-target` authored at `RibbonBar.vue:7` and consumed at `ChannelOptions.vue:377` (both my reads) — four literals, two contracts, zero shared declarations; the M-9 cure must cover both.
- **N-1 (RR-D net-new; D-15 refiled per ruling) — MINOR · the keyboard focus ring on `.kf-pill-tab` is clipped ~1px at the tablist's own paint-containment boundary (2px padding vs 3px ring)** — statically derived by this seat (see ruling); latent under §0; the honest cures avoid geometry changes (`outline-offset: -1px` + widened ring, inset ring, or dropping `glass-wash` from the tablist, which D-9 wants anyway). Rides **KF.W2-TABS**.
- **N-2 (RR-D net-new) — MINOR · glass-ui 7.0.0 ships `<FadingScroll>` — the exact primitive `useTabStripScroll` + `useScrollFade` + the 15-line mask block hand-roll — and `ChannelControls.vue:51-54` names it while hand-rolling anyway.** My own reads: `dist/components/fading-scroll/FadingScroll.vue.d.ts` (`axis`/`fadeStart`/`fadeEnd`/`ariaLabel`/`ariaLabelledby`); a second demo consumer of `useScrollFade` exists (`ControlsPaneWrapper` scroll-fade), so the disposition is delete-the-dead-fade AND adopt-the-vendor-primitive wherever a fade is actually wanted — a clean glass-ui-first precept row none of the three axes raised. Rides **KF.W2-TABS**.
- **N-3 (RR-D net-new) — MINOR · the `.kf-pill-tab` skin is a second authored copy of the dead `.tab-trigger-base`/`.tab-trigger-pill` rules** — my side-by-side read: identical `color-mix` 5%/8% fills, identical `--radius-lg`, identical three-property transition (font-weight included) on the same tokens, identical 500→600; differing only in padding/size-token/line-height. C-15 (zero consumers) + §0 (never renders) joined: the deletion wave removes ONE pill skin duplicated across two files, both copies unreachable. **Token rider**: glass-ui ships semantic `--radius-strip: 0.75rem` / `--radius-tab: var(--radius-pill)` (my read) for exactly this object; the strip reaches for the generic panel/lg pair — S-D's arithmetic is beautiful on the wrong register. Rides **KF.W2-TABS**.
- **N-4 (RR-D net-new) — MINOR · a fourth orphan `role="tabpanel"` renders from `CubeScene.vue`'s render function through this component's own `tabs-content` slot** (my read, :171-180): raw-store-gated (a fourth L-8 bypass), no `tabindex` (inconsistent with its three siblings), invisible to attribute greps. **Method note for the rest of the corpus: attribute greps do not see `h()` prop objects, and this repo uses render functions for scene bodies.** NO-WAVE-OWNER (rides the D-2 a11y spec row).
- **N-5 (RR-L net-new) — MINOR · `tabsContentEl` is a fully dead template ref** — my grep: exactly 2 hits (template :85, declaration :374), no reader, not exposed; dead in BOTH hosts, so it survives every C-1 disposition. NO-WAVE-OWNER (hygiene).
- **N-6 (RR-L net-new) — MINOR · the DFA alphabet is typed at one end and stringly at the other**: `selectedControl: string` (my `controlOptionsStore.ts:12` read) while `ControlSurface` is a closed union imported into this very file and used in exactly one signature (`hasSurface`, :297); the three panel gates compare untyped strings to literals — a typo compiles clean and renders no panel. Typing the store field makes the gates exhaustive-checkable at zero runtime cost. NO-WAVE-OWNER.

### INFO

- **L-9 — INFO (demoted per ruling) · one-shot `onMounted` tablist resolve with no re-resolve path — a fragility note without a reachable failure mode.** NO-WAVE-OWNER.
- **D-11 — INFO · fluid `--type-small` in a fixed-padding chip; half the token pair consumed (leading inherits body); the clean cure is glass-ui's `text-small` utility which binds the pair.** Dies with KF.W2-TABS.
- **D-17 — INFO · the "≤4-tab strip never overflows" claim vs ~419px estimated at the ~332px rail floor — glyph-advance estimate, UNPROVEN; moot unless the strip is restored.** → SS-13 (conditional).
- **D-18 — INFO · vertical 8px/12px asymmetry beside D-4's horizontal 12px — accretion tell, undocumented.** NO-WAVE-OWNER.
- **C-9 — INFO · root-barrel glass-ui import beside a same-directory sibling using `/tooltip`; both barrels re-export (RR-D confirmed against dist), so discipline, not breakage.** NO-WAVE-OWNER.
- **C-16 — INFO · `KeyframesAnimation<any>` at the central prop and every emit payload — repo-wide idiom inherited from `AnimationGroup<any>`, correctly not scored against this file; recorded for the library-boundary lane.** NO-WAVE-OWNER.
- **L-18 — INFO · "THE ONE WRITER" is N writers (per-host watch instances over one shared bucket; `!==`-guarded so the effect is benign; the invariant NAME is false).** KF.W4-PROSE.
- **L-19 — INFO · ~45% of the file is provenance prose with counterfactual blocks**: the `.tab-trigger-*` "HERE" claim (:207-214) vs a scoped-only style block; `<SegmentedTabs>` ownership prose across five sites; "no scroller is wired here" (:41-55) vs the scroller at :398; all three colocated composables self-documenting as `AnimationControls.vue`'s; a live source-layout decision (:341-345) resting on `proof:*` gates that do not exist (two ship; the retired grep idiom — value.js law noted, the local grep carries the finding). **FOLD → KF.W4-PROSE** (census S-2), with C-6's riders and N-7.
- **N-7 (RR-D net-new) — INFO · KfPillTabs.vue:41-44 asserts a type re-export `<script setup>` cannot perform, above a plain `import type`; the claimed import path has zero consumers (the sole consumer routes through the F-5 shim).** KF.W4-PROSE.
- **N-8 (RR-D net-new) — INFO · icon-sizing idiom inconsistent within one 15-line block: `w-6 h-6` (:160) vs the demo's own `icon-sm` utility (:168; `@utility icon-sm` at design-idioms.css:102 — my reads). Separately, repo-wide: `@lucide/vue` defaults carry no `aria-hidden`, so decorative icons are exposed unnamed — recorded, not component-owned.** NO-WAVE-OWNER.
- **N-9 (RR-L net-new) — INFO · `builtInTabs`' filter is the literal body of `hasSurface` declared immediately above (:297-303, my read) — two copies of one DFA rule in a single-authority file; the second copy is what a future gating edit misses.** NO-WAVE-OWNER.
- **N-10 (RR-L net-new) — INFO · `useKeyframesPaneReveal` registers two separate watchers over `keyframesActive` (warm ~:107; reveal-focus ~:123 — my read), and the focus half fires in all N hosts on a shared-store flip; correctness rests on `.focus()` no-op'ing inside `display:none` ancestors — no `active` term, no guard, trivially foldable into one watcher.** NO-WAVE-OWNER (family of L-3).

## Killed-claims register

1. **C-1's BLOCKER grade — KILLED (both re-readers; adopted).** Unreachable code costs bytes, review surface, and a false census premise; it produces no wrong behavior and no data loss. MAJOR is the honest ceiling; BLOCKER stays reserved for the stacked-portal fold and L-2's silent loss of authored work.
2. **D-2's BLOCKER grade — KILLED (RR-L, per the D axis's own grading note).** No ARIA required-parent rule, no automated failure, one user class — permanent pattern break at MAJOR. **L-3's BLOCKER grade — KILLED the same way** (cost, not breakage; the T.G9-inversion argument is preserved in DISSENT).
3. **D-4's scrollbar-gutter mechanism + MAJOR grade — KILLED (RR-L's kill adopted with this seat's amendment).** The outer `.controls-pane` is itself the pane scroller (`ControlsPaneWrapper.vue:33-36`, my read), so the "scrollbar renders 40px from the pane edge" arithmetic does not follow — but the inner `overflow-y-auto` div is height-bounded too, so the true scroll locus is layout-dependent and NEITHER reader may assert it statically. The asymmetry survives at MINOR (systematic, two sites — `RibbonBar.vue:2` byte-identical); RR-D's shadow-direction evidence is banked on the surviving row.
4. **D-15 as filed ("the ring fits, with 1px to spare") — KILLED and REFILED (RR-D's kill adopted; my own derivation).** The axis totaled slack to the WRONG boundary (the header's overflow-hidden); the nearer clip is the tablist's own `contain: paint` at its 2px padding box (glass-ui `material.css`, my read) — the 3px ring is clipped ~1px TODAY (latent only via §0). Refiled as N-1; the verdict inverts from "latent dependency" to "live-when-rendered focus-appearance defect."
5. **S-B's contrast working — KILLED (RR-D's kill, re-executed by this seat at ladder.css).** The pill's operative foreground inside two `.glass-wash` plates is `--on-glass-muted` (`hsl(30 26% 35%)` / `hsl(34 16% 72%)` — my token reads), not the `--neutral-5` pair the table computed. Every number is void; both substitutions move in the safe direction so the pass likely survives — → SS-13, and the superlative is struck as stated.
6. **L-9's failure scenario — KILLED (RR-D's kill adopted); RR-L's MINOR confirm STRUCK (a kill-of-a-confirm).** `tabsExternallyManaged` is a plain injected constant per instance; the header's `v-if` is static in both hosts; a "header that appears post-mount" is unreachable. The one-shot-resolve fragility survives at INFO.
7. **L-5's cost clause ("`builtInTabs`' `.map(SURFACE_META)` allocation is pure waste on every DFA change, ×N") — KILLED (RR-L; conjunct order re-read by this seat).** `builtInTabs` sits behind `length === 1`, false for every live scene; lazy computeds mean zero evaluations, zero allocations — which simultaneously STRENGTHENS the deadness verdict (see the L-7 rider: the registry is never read at runtime).
8. **L-5's "dead/unreachable at runtime" labels on `useTabStripScroll(…)` (:398) and the `reMeasure` watch (:408) — CORRECTED (RR-D).** They execute, forever, on every instance — listeners, observers, and two null-guarded probes per surface switch ×N hosts — and merely have no effect. "Never renders" and "runs and does nothing" are different dispositions; the deletion spec inherits the second (larger) runtime saving.
9. **Citation-accuracy cluster (conclusions unaffected, recorded because these documents present as provenance-exact).** D-8's "8 declarations" → **4** (mask-image + -webkit pairs, my read); C-3's own evidence line under-reports its grep (the true `role="tab` return is 7 hits including the three tabpanels the finding is about — my run); C-1's and RR-D's "exactly 4 lines" for the `TABS_EXTERNALLY_MANAGED_KEY` grep → **5** (two imports — my run; RR-L had it right); C-7 cited RibbonBar's gate at :16 — it is **:13** (my read).

## Superlatives (provenance preserved; L-18 runs both ways)

- **S-A / SUP-1 / C-L-2 (all three axes independently; both re-readers; chain re-walked by this seat) · the inactive-pane cache is genuinely exemplary.** Force-mount + `content-visibility: hidden` cache; `inert` (not bare aria-hidden) with the avoided defect NAMED at call site and composable; `@supports not (…)` fallback trading the cache for the one-visible-pane invariant, and saying so; tabindex mirroring; reveal-focus on nextTick; complete two-timer teardown (`onScopeDispose` + vueuse scope — my read). RR-D's extra verification banked: `:inert="false"` sets the DOM property (no stale-attribute trap). The praise is for the mechanism; L-3 is a separate fault in WHO gets one.
- **SUP-2 (L axis; falsifier re-run by both re-readers) · `useSelectedControlSurface`'s suspend-on-leave gate is a real cure** — the active surface is a pure function of (DFA set × stored pick), and the reconciling write is gated on `superKey === ACTIVE_SCENE_KEY` so a leaving host cannot corrupt the leaving scene's bucket; provided atomically with the scene id. It is the reason L-8's steady-state hypothesis had to be withdrawn.
- **SUP-3 (L axis; re-read by this seat at `ChannelOptions.vue:461-477`) · `trySetOption` catches BY ERROR NAME and re-throws everything else** while the store still records the raw string — the best consumption of a fail-explicit engine API in the repo, against lane-library §7.5's five inconsistent postures.
- **SUP-4 (L axis) · `useKfPillTabs` implements the half of the APG tablist contract most implementations drop** (DOM focus moves with the roving stop; wraparound over the enabled subset; the prior bug named in the docblock; never Tab-unreachable). The retire verdict stands (7.0.0 ships `useTabRovingFocus`); this records what the KF.W2-TABS swap must be verified not to regress. **Cross-ref (banked)**: the group record's M-1 — the global scrub shortcuts collide with this very tablist; the strip's semantics are right, the window-level shortcuts are what break it.
- **S-C (D axis — an honest self-kill; blanket verified by this seat: a11y-overrides PRM 1 hit, tw-animate 0) · the PRM contract is carried at the substrate**, neutering the file's three unguarded motion primitives including the infinite `animate-bounce`. The residual is real and banked: `useTabStripScroll.ts:55` `scrollIntoView({ behavior: "smooth" })` overrides the user's PRM preference from JS (latent under §0).
- **S-D (D axis; token values re-confirmed) · the concentric-radius arithmetic inside the strip is exactly right (12 − 2 = 10)** — and it is what convicts the wrapper one level up (D-9). Sharpened by N-3's rider: right arithmetic, wrong register (`--radius-strip`/`--radius-tab` exist).
- **S-E (D axis) · degradation is designed, not assumed** — the `@supports` correctness fallback, the paired `-webkit-mask-image` with the fixed regression documented as fixed, the single-sourced `--mask-fade`. That the rules are unreachable is the file's tragedy, not the craft's fault.
- **C's L-1 (C axis; grep re-run by this seat: `parseCssColor` = 1 file, `scenes/square/useSquareTumble.ts`, outside this closure) · the value.js R1 blast radius through this subtree is ZERO**, and the one grammar edge reached (`parseTimingFunction`) is consumed on the result contract with diagnostics surfaced — the best posture available. **Identity guard: corroborates the banked value.js R1 row; nothing re-booked.**
- **C's L-3 (C axis; both re-readers) · the 6-event emits contract is typed and holds 1:1 across three hops, zero string drift, no `$emit` escape hatches** — the cleanest surface in the file.
- **METHOD (both re-readers) · the corpus carried per-claim falsifiers precise enough to execute** — five of this record's nine kills were possible only because the axes stated their falsifiers; and RR-D's kill-and-refile of D-15 (killing a claim to file a STRONGER one) is the incredulity posture working as designed.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs, ranked)

1. **D-1/L-1's rendered stack** — carried at the banked group record (its UNPROVEN #2); one screenshot of spring with the timeline expanded remains the cheapest decisive probe in either corpus. Not double-booked here.
2. **S-B re-derived** — `--on-glass-muted` over two stacked `.glass-wash` plates (specular `::before` at plus-lighter, grain `::after`, rim, tint-zeroed by the demo override); the light arm's headroom was thin on the OLD (void) numbers; no ratio is bankable without a frame.
3. **C-14's cost** — the global panel-slide re-firing across the restored Monaco subtree in the cache-release frame.
4. **L-16's stale-target frame** — whether a post-mount `setTargets` lands after a `KeyframeTimeline` mount in a live session (the missing reactive edge is proven; the ordering is not).
5. **C-7's window duration** — network/CPU fact; existence is static.
6. **D-9's composited delta** — the doubled rung's visual weight.
7. **L-6's latency delta** — bounded by rIC(4000)/1500ms; perceptibility not statically decidable.
8. **The `text-small font-medium` utility-order hazard** (RR-D's deliberate non-filing, preserved: `@utility text-small` writes `font-weight: 400`, `font-medium` writes 500, same layer, emission-order-decided; a 14+-site repo idiom, so a loss would be visible everywhere) — decidable from the built stylesheet.
9. **C-6's `semantics` → emitted DOM** — d.ts-proven; non-gating (§0 makes it delete-not-port regardless).
10. **D-17 / N-1's rendered form / D-4's scroll locus** — ONLY if the strip is restored / at SS-13's frame respectively.
11. **F-1 reproducibility precondition** — every glass-ui claim in this record resolves against an installed tree neither manifest declares; nothing is reproducible from a clean `npm ci` until census F-1 (KF.W1-DEP) lands.

## DISSENT — preserved, not ruled away

1. **L-3's severity (preserved against my own ruling).** RR-D's grading note deserves the record: T.G9's whole purpose was Monaco's LCP cost, and multiplying instances by channel count on the flagship scene "is close to the line." If SS-13 measures material main-thread or memory cost from the two inert editors, the MAJOR/BLOCKER boundary should be revisited by the spec that owns it.
2. **RR-D's teleport-destination MAJOR vs the fold.** RR-D independently derived the destination-side a11y failure and graded it MAJOR as a net-new find; this seat folded it to the banked group D-5/M-6 identity instead of re-booking. The grading instinct was right — the banked D-5 is already MAJOR — but the identity guard outranks the fresh booking. Recorded so the SS-1/SS-2 authors see the same defect was found twice from two sides, which is evidence of its reality, not two defects.
3. **The C-4 near-miss (method note).** Both re-readers asserted the two SURFACE_META copies "value-identical today" without a label-level diff; copy B visibly carries the exact edit (`Curve`/`Physics`) the axis named as the future drift event, and only this seat's diff of copy A confirmed the edit landed in both. Suspicious agreement on an identity claim is exactly where citation-inheritance hides — the ColorSpaceSelector record's addendum item 3 generalizes; it held here by luck of one commit touching both files.
4. **RR-D's own grep miscount** ("exactly 4 lines" for the §0 grep; truth 5) — inside the corpus's single most load-bearing fact. The conclusion was never at risk (the extra hit is an import), but the seat records that even kill-heavy re-readers inherit counts; the arbiter re-ran it.
5. **HEAD movement note**: zero line drift observed at every site this seat re-opened against `8281638c`; the group apotheosis (same HEAD, same day) reported the same. Anchors remain re-resolve-before-cure per the D-19 lesson.

## Closing verdict

**ADJUDICATED at keyframes HEAD `8281638c`: 50 confirmed rows (2 BLOCKER · 12 MAJOR · 27 MINOR · 9 INFO), of which 8 fold by reference to banked kf-AnimationControlsGroup / census identities with zero re-bookings — the net-new blocker is L-2/C-2's `:key` remount silently destroying authored timeline work (must be settled before KF.W6-TIMELINE), while the stacked-portal blocker contributes its source-side evidence to the already-banked D-1; 9 claims killed with executed refutations (three severity demotions from BLOCKER, one kill-of-a-confirm, one kill-and-refile that produced a STRONGER row); 10 net-new re-reader finds banked (N-1..N-10) plus 4 riders and 1 arbiter-own rider (the KfPillTabs :45-46 stale contingency); the census is corrected twice (S-1's liveness premise — the strip is source-live, runtime-dead, so KF.W2-TABS step 2 is a DELETE; §10 step 2's open design question — closed by 7.0.0's `semantics` contract); routings are M-25 agglomeration input for the forming X·KF specs — KF.W1-DEP/W2-TABS/W3-SHIM/W4-PROSE own their folds, everything behavioral is NO-WAVE-OWNER for SS-1/SS-2, and 11 residue rows ride to SS-13.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
