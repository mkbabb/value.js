# picker-spectrumcanvas — adjudicated apotheosis · seat model receipt: `claude-fable-5` (Fable 5, observed in this seat's own system prompt — M-23/L-18)

**Seat**: fresh Fable adjudicator (the incredulity seat) · **Date**: 2026-08-07 · branch `tranche-u`.
**Corpus**: three challenge axes (D-01..D-18 design · C-1..C-17 implementation · L-1..L-13 library) against `demo/picker/controls/SpectrumCanvas/SpectrumCanvas.vue` (272 lines) + `composables/useSpectrumPlateStyle.ts` + the shared `demo/picker/controls/spectrumLuma.ts`. All three challenge files are **EXISTS-ORIGINAL** in `registry/HYDRATION-LEDGER.md` (`0318e27c…`, `dac259a8…`, `eab6d101…`) — no REPORT-AUTHORED taint.
**Re-readers adjudicated**: design-and-usage (reader A) · library-and-code (reader B), both hostile re-reads of the full corpus.
**Method**: both returns were attacked before either was adopted. Every verdict-level disagreement (D-05's boundary fact, D-11's print roster, D-18 kill-vs-INFO, C-3 proven-vs-unproven, D-04 severity, L-1's mechanism, the spectrumLuma praise, M-8's severity) was re-derived against the tree by this seat with its own instruments; agreements on load-bearing rows (D-02/D-08's producer rule, the clamp gate, the vacuous gates, the dead exports) were re-measured, not spot-trusted.
**This seat's own instruments (all static + node against dist; ZERO browser probes — probe parsimony, and this seat is barred from browser tooling)**: full reads of `SpectrumCanvas.vue` (whole), `useSpectrumPlateStyle.ts` (whole), `spectrumLuma.ts` (whole), `demo/color-session/useColorUrl.ts` (whole — the C-3 decider); `ComponentSliders.vue:236-275` (the unscoped touch-gate block — the D-05 decider); `foundation.css:668-700` (forced-colors roster) + `:815-845` (print block — the D-11b decider) + `:272-291` (gamut vocabulary); `picker-color.ts:40-70` (PICKER_CHANNELS: `xyz: [percent×3]`, `jzazbz` jz max 0.222, kelvin single-entry) + `:195-217` (`clampPickerColor`/`pickerColorToHex`); `valueDomain.ts:32-49` + `useColorPipeline.ts:14,:51,:67` (the ONE write gate's clamp wiring); `MiniColorPicker.vue:5-22,100-106,156-165`; `demo/color-picker/index.html:1-12` (`dir="ltr"` hard-pin); `App.vue:164/:200` import order; `ExtractControls.vue:136-144`; `DESIGN.md:330-340` + `ls demo/styles/` (no `style.css` exists); `src/color/model.ts` SPACE_SCHEMA read census + `src/color/anchors.ts:315-330` (the 39,001-step `xyzToKelvin` scan, read verbatim); installed glass-ui 7.0.0: the full `.watercolor-swatch[data-v-292b9032]` rule extracted from `dist/glass-ui.css` (declares `position:relative`, a three-part `box-shadow`, seeded `border-radius`, `display:inline-block` — and NO `border`), `useWatercolorBlob.d.ts:1-26` (defaults `4000`/`[20,80]`; wobble on the library's ONE `useRAFLoop`, "frozen under `prefers-reduced-motion`"), `dist/dom.js` export tail (`t as useTouchGate`, `a as writeClipboard`), the `3e3` constant in `useTouchGate-B4mzQcHJ.js`; **node against `dist/subpaths/color.js`**: `hsv(0,0.6,0.25)→rgb = [63.75, 25.499999999999993, 25.5]` → `toRgba8 = [64,25,26,255]`; `hsv(14,1,1)→rgb = [254.99999999999997, 59.49999999999997, 5.7e-14]`; white→xyz `[0.9504559, 1, 1.0890578]`; white→jzazbz jz `0.2220652`; Z-clamped-to-1 white → `toRgba8 = [255,255,244,255]` (**#fffff4**); repo greps: `SPECTRUM_LUMA_FLIP` 0 external hits, `spectrumLuma(` 0 external hits, `spectrumFieldIsLight` exactly 2 consumers, `gamut-overlay` exactly 2 hits repo-wide (`spectrumLuma.ts:6` prose + `foundation.css:682`), `var(--gamut-*)` exactly 1 consumer (`ComponentSliders.vue:392`), no test/e2e file touches `spectrumLuma|useSpectrumPlateStyle|spectrumFieldIsLight`; `tsconfig.demo.json:42-49` (8 hand entries incl. bare/`parsing`/`units`) vs `package.json#exports` (7 keys, no `.`); e2e: `reactivity-instant.spec.ts` asserts only text-change + median≤50, `a11y-modality-support.spec.ts:301-305` asserts the `role="img"` shape.
**Motion law**: no `motion-quarantine.md` FALSE row touches this component — the nearest picker rows are M-23 (picker-colorcomponentdisplay's vacuous PRM cell, struck there) and none names SpectrumCanvas; M-12 (**TRUE**, AdminListSkeleton) is cited below only as the pattern-context for this component's own PRM gating, which this seat verified directly in source (`SpectrumCanvas.vue:243-257` nests keyframes AND the animation declaration inside `no-preference`). D-14/M-1 are original to this corpus (EXISTS-ORIGINAL). No FALSE motion row rides in this record.
**Status discipline**: every disposition below is ADJUDICATED, not VERIFIED — nothing here stamps a wave.
**Second-pass reconciliation (same seat, same date)**: the merged re-reader return was reconciled against this record with fresh instruments (this pass's own: the full producer `.watercolor-swatch[data-v-292b9032]` rule re-extracted from dist bytes; the print block re-read at `foundation.css:815-841`; `useColorUrl.ts` whole re-read; the M-8/M-9 dist runs re-executed — `[63.75, 25.499999999999993, 25.5]` → `[64,25,26,255]`, white-XYZ Z `1.0890577507598784` → clamped → `[255,255,244,255]` = `#fffff4`, white-jz `0.2220652495357387` > 0.222; `PROPORTION-AUDIT.md` §5 card laws and `VISUAL-CONSTITUTION.md` §2/§3/§5.2 re-read verbatim; `ExtractControls.vue:136-142` re-read scoped; `glass-ui` exports map + `dist/canvas.js` + `useCanvas2D.d.ts` read; `usePointerDebug.ts:118,:121` + `App.vue:164/:200` re-greped). Four kills the merged return carries that this record's rows partially embedded are now integrated (D-03-CAUSE, D-06-HOOK, D-10-HOOK, D-05-TIER), three missed rows added (M-16 canvas substrate · M-17 corner clipping · M-18 rationale comment), and one merged-return confirm overruled (C-9-structural — the C-9-MECH kill stands).
**Third-pass reconciliation (the r2 hostile re-reader pair, same seat, same date — E-3 addendum, prior rows untouched)**: a fresh pair of hostile re-reads of the full r2 corpus (design-and-usage · library-and-code; returns delivered to this seat by the orchestrator, load-bearing cells quoted in the §THIRD-PASS section below) was adjudicated with this pass's own instruments: `SpectrumCanvas.vue:110-224` re-read whole (the `:163` short-circuit; the `:123-125` sentinel-before-write order; the `:148-150` capture-bookkeeping overwrite; the `:207-208` `dotPos` clamp; the `:138` capability-not-modality gate branch); `grep suppressDeactivate demo/` → ZERO (the producer's `useTouchGate-*.js` ships it); `grep pointerType SpectrumCanvas.vue` → ZERO; **node against `dist/subpaths/color.js`**: `lab(92,88.8,20)→hsl = [346.045, −12.9515, 1.0366]` (saturation NEGATIVE, lightness > 1), `→hsv` v = `1.5109915`, `hsv(0,0.6,0.25)→rgb = [63.75, 25.499999999999993, 25.5]` → `toRgba8({gamut:'clip'}) = [64,25,26,255]` (M-9 re-reproduced third time); `picker-color.ts:49-61` (`percent` = [0,1]; `hsl` = [hue, percent, percent]) + `:195-202` (`clampPickerColor` min/max); `e2e/smoke/oracles/o18-contrast-census.spec.ts:648-690` read (a live `test(...)` asserting `.plate-caption` `toBeVisible()` + a `censusElement` + a computed-style identity check) vs `grep plate-caption demo/` → exactly 4 PROSE comments, zero markup/CSS, vs `playwright.config.ts:147-153` (`testIgnore` carves admin/mobile/safari/perf/reactivity — NOT `oracles/`); e2e census grep (`spectrum` reaches at least `reactivity-instant`, `a11y-modality-support`, `a11y-web-modality`, `webgl-blob-idle` + the o18 oracle + two perf specs); `ColorPicker.vue:64-72` (bare `<SpectrumCanvas />` in an unlabeled flex div); `useSpectrumPlateStyle.ts:28-45` re-read (ONE computed returns `background` + `--spectrum-shadow` + `touchAction`, incl. the `pan-y`-until-active arm). Outcome: **8 new missed rows adjudicated in (M-19..M-26), 7 new register kills, two r2 confirm-cells OVERRULED against standing kills (D-17-RNG repeated by r2-A — citation-inheritance event #2; C-9's forced-layout mechanism confirmed by BOTH r2 readers — C-9-MECH stands), one pass-1 residue retired (C-15 → killed as filed), and pass-1's C-3/L-1/L-2/D-01/D-02 rulings independently corroborated.**

## Scoreboard

**43 CONFIRMED identities (2 BLOCKER · 28 MAJOR · 11 MINOR · 2 INFO) · 26 KILLED claims · 26 MISSED rows adjudicated in** — pass 1+2: 35/19/18; pass 3 (r2): +8 confirmed (M-19..M-26), +7 kills, +8 missed. MAJOR-headline missed rows across passes: the entry-animation de-registration (M-1), the domain-table clamp family (M-8 + M-19 + M-20 — the write gate destroys what the table cannot represent, and the display side clamps the wreckage into a fiction), the unconsumed producer canvas substrate (M-16), the orphaned `.plate-caption` oracle (M-21), and the modality-blind gate + unused `suppressDeactivate` pair (M-22/M-23).

Blocker-weight survivors: **D-04/C-6** — the product's dominant instrument has zero keyboard coordinate and the constitution's two named numeric axes (SS5.2) do not exist anywhere in the product; and **C-16** (upstream locus) — a `none`-channel deep link throws inside App setup above `ErrorBoundary` and blanks the app. The corpus's indictment: all three axes walked the ONE write gate (`clampColorToSpaceDomain`) and none checked the domain table against the spaces' real ranges — both re-readers' best work was in their missed sections.

## Ruled disagreements (the adjudication's actual work)

| row | reader A (design-and-usage) | reader B (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-05 boundary fact** | Killed D-05b: the plate DOES carry a reserved boundary channel — an unconditional 3px transparent outline from `.touch-gate-target` | Confirmed D-05 stating "no outline (the 3px touch outline is… only under `touch-gate-active`)" | **A wins; B's evidence cell corrected.** The kill of D-05b is SUSTAINED and it reframes the cure. | `ComponentSliders.vue:253-257`: `.touch-gate-target { outline: 3px solid transparent; outline-offset: 1px; transition: outline-color … }` — the outline DECLARATION is unconditional; only `outline-color` is state-gated (`:261-263`). `SpectrumCanvas.vue:11` carries `touch-gate-target` in the STATIC half of the class binding, every device. A's probe fact (`plateOutline: "rgba(0,0,0,0) solid 3px / offset 1px"`) and this seat's read agree. D-05 survives MAJOR — the plate has no VISIBLE boundary at rest — but the cure is "give an already-reserved ring a resting value", not "invent a material law" (M-7 folded in). |
| **D-11 print roster** | Killed D-11b: `.gamut-overlay` is rostered ONCE (forced-colors :682); the print claim is false | Confirmed D-11 repeating "`:831` rosters it for print too" | **A wins; B inherited a false cell.** | This seat's grep: `gamut-overlay` = exactly 2 hits repo-wide (`spectrumLuma.ts:6` prose, `foundation.css:682`). The print block (`foundation.css:822-838`, read whole) lists `.spectrum-picker, .gradient-rail, .rail-handle, .swatch-row > *, .generate-swatch, .shadow-swatch, .watercolor-swatch, [data-color-surface]` — no `.gamut-overlay`. B repeated the challenge's false limb without re-deriving — a citation-inheritance event, recorded in DISSENT. Parent D-11 unaffected (one orphan roster entry is still an orphan). |
| **C-3 proven vs unproven** | UNPROVEN-NEEDS-LIVE ("attribution circumstantial; not decidable from source alone") | CONFIRMED MAJOR structurally | **B wins — decidable from source, and this seat decided it.** | This seat's whole-file read of `demo/color-session/useColorUrl.ts`: `syncModelToUrl` (debounce 300, `:51-65`) does `router.replace` with the newly-serialized color → `route.query.color` changes → the `:73-76` watch fires `applyUrlToModel()` with **no echo guard in the URL→model direction** (`syncGen` guards only model→URL, `:59-61`) → `applyUrlToModel` unconditionally builds a NEW converted color object (`:32-41`) and calls `updateModel` → new object identity → the component's `watch(() => model.value.color)` fires with `spectrumIsSource` already spent → `rawS/rawV` nulled (`SpectrumCanvas.vue:65-75`). Exactly one wipe per gesture, ~300ms after release; the persistence comment at `:58-60` is falsified by construction. Only the FELT magnitude rides UNPROVEN. |
| **D-18** | KILLED (interpretation false: hue does not collapse — rgb(26,0,0) vs rgb(0,0,26) at v=0.10; the <32 threshold measures darkness, not hue-degeneracy); refile scheme-invariance at INFO under D-01 | CONFIRMED at INFO (fraction arithmetically necessary; "inherent to every HSV plate; scheme-invariance of a specimen field arguably correct") | **KILLED as filed — A's label; B's arithmetic absorbed into the residue.** | Both readers agree on every fact; the row's FILED claim ("every hue collapses to the same pixels", a standalone proportion defect at MINOR) is the thing on trial, and it is false — A's counterexample is decisive and B's own severity challenge concedes the design premise. The surviving true observation (the fraction is byte-identical across light/dark/forced-colors because the plate does nothing for scheme) rides as an INFO rider under D-01. |
| **D-04 severity** | BLOCKER ("unambiguous violation of a written law with zero mitigating path") | MAJOR | **BLOCKER — A wins.** | `VISUAL-CONSTITUTION.md:125` (SS5.2) names the exact obligation ("expose two named numeric axes using the same Slider law; pointer canvas is not the sole keyboard control"); `SpectrumCanvas.vue:6-22` has no tabindex/keydown/focus path at all (this seat's read); the mitigation fails because the sliders expose the SELECTED space's channels, which contain neither s nor v unless the user first switches to HSV (B's own cell). Zero keyboard access to the product's dominant instrument is capability-zero, the same class this program books BLOCKER elsewhere. B's position preserved in DISSENT. |
| **L-1 mechanism + disposition** | Confirmed with the axis's mechanism ("the duplicate repeated the library *wrong*: `Math.round` vs `roundHalfEven`") | Killed L-1-MECH: the divergence is the LIBRARY's float drift, not the mini's rounding mode | **B wins on the mechanism — re-proven by this seat's own dist run — and A wins on the disposition (its missed row 2).** | This seat ran it: `convertColor(hsv(0,0.6,0.25),'rgb').channels = [63.75, 25.499999999999993, 25.5]` — g and b are analytically identical (both 25.5); no rounding mode can separate them; the library's XYZ-anchored route drifts g by 7e-15 (~1e9× the `roundHalfEven` EPSILON window) and quantizes to `[64,25,26]`. MiniColorPicker's direct HSV→RGB is the exact one. L-1 stays CONFIRMED MAJOR as duplication; its moral inverts (→ M-9, the library defect); and the L-axis's cure ("delete the mini's plate") is REVERSED — see M-2. |
| **spectrumLuma praise** | Superlative: "the shared luma regime is enforced rather than asserted… zero copied thresholds" | Killed D-NEGPROOF-5: the praise is unearned — the constant is never imported, `spectrumLuma()` never called externally; 2 of 3 exports are dead | **B wins; A's superlative RESCOPED.** | This seat's greps: `SPECTRUM_LUMA_FLIP` outside the defining file → nothing; `spectrumLuma(` outside it → nothing; `spectrumFieldIsLight` → exactly 2 consumers (`ComponentSliders.vue:99,180`; `useSpectrumPlateStyle.ts:11,53`). "Never duplicated" is true only because nothing consumes the constant at all; the docblock's third consumer (`useGamutOverlay`) died at `a68ecdc1`. The single-home INSTINCT survives as a superlative; the module's surface does not (→ M-11, M-12). |
| **M-8 severity** | (not filed by A) | BLOCKER ("the ONE write gate silently mutates every colour the plate writes there") | **MAJOR — demoted, rationale in DISSENT.** | The defect is fully real (this seat's end-to-end run: white → xyz Z=1.0890578 → clamped to 1 → `#fffff4`; jzazbz jz 0.2220652 vs declared max 0.222) and structurally invisible to the clamp oracle. But the program's BLOCKER register is capability-zero / crash / product-lying-at-boot (D-04, C-16, ColorComponentDisplay M-1's snap-to-white); here the corruption is bounded (≤11/255 on one channel, opt-in space) while the readout truthfully reports the corrupted model. MAJOR with teeth, not BLOCKER. |
| **D-10 mechanism** | Confirmed citing `overflow: visible` "explicitly declared, not defaulted" (:234) | Killed D-10-MECH: the declaration is INERT — `visible` is the initial value; deleting :234 changes nothing; the escape is caused solely by the −50% translate on a 28px marker | **B's kill SUSTAINED, with A's precision kept.** | Both facts hold at once: line 234 IS an explicit declaration (this seat's read) AND it is causally inert — the cited "mechanism" would survive its own deletion, and the implied cure requires ADDING containment, not removing a line. `:234` is separately dead CSS. D-10/C-10's substance (escape at all four extremes; the ink regime computed from a field the marker is half off) is untouched and CONFIRMED. |

**Cure-level rulings inside agreed verdicts**: D-07 confirms on cause 2 ONLY (layout-inducing `left`/`top` per frame on the `feDisplacementMap`-filtered producer root — the producer's own d.ts documents the engineering this consumer defeats); cause 1 (the per-frame `--spectrum-shadow` write) is refiled as dead weight under D-16 per A's kill D-07b — the plate's "entire subtree" is one WatercolorDot child (this seat counted: the template has exactly one). D-16's cure deletes the hover shadow and the custom property dies with it, discharging D-07's cause 1 for free.

## Adjudicated defect roster — confirmed rows

Terminal dispositions route to the tranche-X waves per `registry/CARRY-CUT-LEDGER.md` (X-W1 = re-gate · X-W2 = boot boundary · X-W4 = semantic controls, the CC-003 glass-independent partition · X-W8 = graph subtraction · X-W9 = parser + library apotheosis · X-W10 = canonical design/motion law). Line anchors are this seat's reads at 2026-08-07; re-resolve before executing any cure (the D-19 lesson). **No row is NO-WAVE-OWNER.**

### BLOCKER

- **D-04 / C-6 — CONFIRMED (BLOCKER) → X-W4.** Zero keyboard coordinate on the dominant instrument; `role="img"`, no tabindex, no keydown, no aria-live in the file (this seat's whole read); the constitution's two named numeric axes (SS5.2, `VISUAL-CONSTITUTION.md:125` + :99) exist nowhere — the route's named numeric axes are the SELECTED space's channels, never s/v. The shipped a11y gate CERTIFIES this shape (see C-7). Severity ruled over B's MAJOR (table above).
- **C-16 — CONFIRMED (BLOCKER, upstream locus) → X-W2.** A `none`-channel deep link parses at `hydrate.ts:98-110` (same-space convert is the identity), then `useColorPipeline`'s unguarded setup-time `convertPickerColor(model.color,"hsv")` throws `color_missing_channel` through `valueOrThrow` inside App.vue's own setup — above `ErrorBoundary` (App.vue:180). Structurally confirmed by B; the mechanism chain is consistent with this seat's reads of `picker-color.ts:104-107` and `operations.ts:18-29`. Books at the boot boundary, not this component; C-15 (`dotPos`'s unguarded `channelNumber`) rides as a RIDER on this fix — it becomes live only if the cure admits `none` rather than rejecting it.

### MAJOR

- **D-01 — CONFIRMED (MAJOR, demoted from the filed BLOCKER) → X-W4.** The dominant instrument of a colour-space laboratory is a hard-coded sRGB-HSV field whose only reactive input is hue (`useSpectrumPlateStyle.ts:30-45`, this seat's read: `selectedColorSpace` appears nowhere in the composable); every reachable pick is inside sRGB by construction (`SpectrumCanvas.vue:120-129`). The headline byte-identity evidence is killed (D-01b below) — the finding stands on source, not on the measurement. INFO rider (ex-D-18): the plate is also scheme-invariant by construction — the bottom ~12.55% is near-black in light, dark and forced-colors alike; arithmetically necessary from the gradient (max channel = 255·v).
- **D-02 — CONFIRMED (MAJOR, BLOCKER framing killed) → X-W4.** `.spectrum-dot { position: absolute }` (0,2,0) loses the tie to the producer's `.watercolor-swatch[data-v-292b9032]{…position:relative}` (0,2,0) — this seat extracted the full producer rule from dist bytes (declares position/box-shadow/transform/border-radius/display, NOT border) — so the dot is an in-flow flex item and under `dir="rtl"` renders at 156.8% of the plate's inline size. Tie direction proven two independent ways: A's three-probe-fact + arithmetic closure (1.568192 vs measured 1.5682) and B's structural order proof (App.vue:164 imports the picker before :200 imports foundation.css → the SFC style is emitted first, producer wins; the Tailwind `absolute` utility sits in `@layer utilities` and loses to the unlayered producer sheet regardless). No user can reach RTL today (`index.html:11` hard-pins `dir="ltr"` — this seat's read), hence MAJOR not BLOCKER; the U-audit's grep-based RTL inventory structurally could not see a TS-computed offset. Production-bundle CSS order rides UNPROVEN.
- **D-03 — CONFIRMED (MAJOR; causal sentence corrected per D-03-CAUSE) → X-W4.** `pointercancel` commits the aborted gesture: all three terminations funnel to `stopDragging()` (`:177-200`, this seat's read), and no pre-gesture colour is stored anywhere in the file, so a revert is structurally impossible. The `pendingCoords` flush at `:191-194` is NOT the cause (killed below): every pointermove already committed through the rAF coalescer (`:97-108` → `:129`), so the flush holds at most sub-frame residue and deleting it changes nothing. The cure is the pre-gesture LATCH the challenge's own cure paragraph names — an implementer steered to the flush would ship a one-line fix that fixes nothing. Canon: "Tuning is continuous and interruptible" (SS5). iOS edge-swipe mid-drag is the live path.
- **D-05 — CONFIRMED (MAJOR, mechanism corrected; D-05b + the WCAG framing killed) → X-W10.** The plate has no VISIBLE boundary at rest — its own content supplies #fff and #000 at the corners; at-rest box-shadow is `0 0 0 0 transparent` (`:232`); the forced-colors tier-1 grant preserves the content and the white-on-white worst case. BUT the boundary channel already exists: an unconditional, permanently-reserved 3px transparent outline at the right offset (ruling table). Cure = assign the reserved ring a resting value (M-7 folded in). The measured ratios (1.538 light / 1.091 forced-colors) ride UNPROVEN with A's antialiasing caveat.
- **D-06 — CONFIRMED (MAJOR; canon hook struck per D-06-HOOK) → X-W10.** Five height declarations for one box (`h-[20dvh] min-h-24 max-h-40 lg:h-[14rem] lg:max-h-none`, `:11`, this seat's read); two symmetric [0,1] axes get anisotropic, viewport-dependent resolution; 64px discontinuity at `lg`. The finding stands on measurement and structure alone — the filed SS3-law-7 citation is killed (it is a SPACING law; see the register). MINOR rider M-3: `dvh` is the DYNAMIC unit — the plate's block size changes live under the finger during the `pan-y` scroll the gate deliberately permits (`touchAction: "pan-y"` until active, `useSpectrumPlateStyle.ts:41-43`; `@touchmove.passive` `:20`), re-scaling the pointer→v map mid-gesture. No axis named the unit.
- **D-07 — CONFIRMED (MAJOR, cause 2 only; cause 1 refiled under D-16) → X-W4.** Per-frame `left`/`top` writes on the `feDisplacementMap`-filtered producer root (`useSpectrumPlateStyle.ts:60-61`) — the producer's own d.ts (read by this seat) documents that the silhouette is static and liveness rides a COMPOSITOR transform precisely to avoid re-rasterizing the filter; the consumer reintroduces per-frame layout on exactly that element. The 20.6ms magnitude is UNPROVEN (contended dev host).
- **D-08 (+ B's missed marker-position limb, folded) — CONFIRMED (MAJOR) → X-W4.** Four attempts to style the producer from outside; one works (`border`, because the producer declares none), three vanish silently: `position:absolute` and `box-shadow: var(--shadow-sm)` lose the source-order tie (producer declares both — dist bytes, this seat), `&:hover{transform:none}` is unreachable (producer binds `pointerEvents:"none"` inline; the template repeats `pointer-events-none`). Folded limb: the template ALSO declares Tailwind `absolute` (`:28`), dead via cascade layers — the file states its positioning intent twice and neither statement reaches the DOM. Owner edict 5's exact failure class.
- **D-09 (+ A's missed shadow-register rider M-5) — CONFIRMED (MAJOR) → X-W10.** The 2px ink ring crosses the WatercolorDot's seeded organic edge at a site the canon names ("no ring/shadow/pseudo-element crosses the seeded edge", SS4.2 `VISUAL-CONSTITUTION.md:91`; P051 lists "Spectrum" by name). Rider M-5: `box-shadow: var(--shadow-sm)` (`:264`) is a SECOND attempted prohibited register on the same face, dead only by accident of the cascade — a repair that deletes it as "dead code" would record canon compliance as hygiene; delete it as an SS4.2 violation.
- **D-10 / C-10 — CONFIRMED (MAJOR, mechanism corrected) → X-W4.** The marker escapes the field at all four extremes (half off at edges, three-quarters at corners: 28px box, −50% translate, percentage anchor) while the ink regime's domain is the field only (`spectrumFieldIsLight(s,v)`). The `overflow: visible` citation is killed as mechanism (ruling table) and `:234` is separately dead CSS. Cure reference: MiniColorPicker's `overflow-hidden` containment (M-2) — the filed PROPORTION-card-law-3 hook is killed as an inverted reading (D-10-HOOK, register below); the finding needs no canon hook and the containment decision is now ONE reconciliation with M-17's corner clipping.
- **D-11 — CONFIRMED (MAJOR; D-11b's print limb killed) → X-W10 (decide the state) + X-W8 (the dead vocabulary dies or lives with the decision).** Out-of-gamut is a fully designed, tokenised, forced-colors-rostered state with zero implementation: `gamut-overlay` = 2 hits repo-wide (prose + one roster entry, this seat's grep); the four `--gamut-*` tokens have ONE surviving consumer, repurposed as a slider-thumb needle colour (`ComponentSliders.vue:392`, this seat's grep); `DESIGN.md` still calls it "the truth-line overlay's whole vocabulary". The canonical trigger (an out-of-sRGB boot colour) is live.
- **D-12 / C-8 / L-13 — CONFIRMED (MAJOR, ruled over B's MINOR) → X-W4.** The accessible name misnames the channel (HSV *value* announced as "lightness" — `useSpectrumPlateStyle.ts:24-28`, this seat's read), names channels absent from the selected space, and is never announced (label mutation on a non-focusable `role="img"`, no live region in the file). Recomputed every drag frame, heard zero times. Three seats filed it independently; the AT-truth row beside D-04's operability row.
- **D-13 / L-3 / C-11 — CONFIRMED (MAJOR) → X-W4, producer-API half on the glass-ui BH relay.** The touch gate is an undiscoverable mode whose entire visible affordance is authored in a sibling SFC's deliberately-unscoped `<style>` (`ComponentSliders.vue:238-274`, comment names the cross-SFC reach; this seat's read) — the component consumes `touch-gate-target`/`touch-gate-active` (`:11-12`) it does not define. Third declaration site `ExtractControls.vue:140` is SCOPED and border-radius-only (this seat's precision — both readers over-called it "conflicting"). L's cure is adopted: `useTouchGate()` (a glass-ui primitive) should return the class binding so no consumer spells the name — relay the API request; the consumer half books at X-W4. SUBTRACTION.md:28's one-cascade-owner law.
- **C-1 — CONFIRMED (MAJOR, this seat's own read where A was silent) → X-W4.** No `pointerId` guard anywhere: `handleSpectrumMove` gates on `isDragging` alone (`:159-166`), `handleSpectrumUp`/`Cancel` call `stopDragging()` unconditionally, `capturedPointerId` is assigned (`:149`) and nulled but compared to NOTHING. A second pointer steers the pick and its pointerup kills the primary drag.
- **C-2 — CONFIRMED (MAJOR) → X-W4.** No `event.button`/`isPrimary`/`buttons` check in the file (this seat's read of every handler); right-press picks and captures; a swallowed pointerup leaves buttonless hover painting with no `buttons===0` self-heal. The repo's own `forceReleaseAllPointers()` walker leads its selector list with `.spectrum-picker` — the codebase already concedes the leak class.
- **C-3 — CONFIRMED (MAJOR, ruled UP from A's unproven; mechanism this seat's own) → X-W4.** The documented dot-persistence invariant is false: the 300ms URL echo wipes `rawS`/`rawV` (full chain in the ruling table). Cure note: `useColorPipeline`'s reference-identity sentinel (`lastWrittenModel`) is the correct pattern, two files away — B's superlative, adopted.
- **C-4 — CONFIRMED (MAJOR; component half) → X-W4 · library half → X-W9.** The plate writes blindly through `selectedColorSpace` with no representability check (`:127-129`); Kelvin is one-channel and selectable (PICKER_CHANNELS single-entry, this seat's read), so the (s,v)→K map is degenerate and the plate contradicts itself on screen. Library half (this seat read the source verbatim): `xyzToKelvin` is a literal 39,001-iteration linear scan calling `kelvinToXyz` per step (`anchors.ts:317-330`) — B measured 1.71ms/op; route the scan to X-W9. The live readout-collapse sequence rides UNPROVEN.
- **C-5 (+ B's missed threshold rider M-12) — CONFIRMED (MAJOR) → X-W4, unit oracle → X-W1.** The border regime is hue-blind: `spectrumLuma(s,v) = v·(1−s/2)` substitutes 0.5 for the hue-dependent luminance term; at pure yellow the white border measures 1.07:1 (B recomputed all five hues to 2 decimals; the arithmetic premise is in the module's own docblock, this seat's read). Rider M-12: the predicate sits exactly ON its strict `>` threshold across the entire s=1,v=1 top edge (1·0.5 = 0.5, not > 0.5) — a `>=` edit inverts the border across the plate's most-used row with zero test failure (this seat's grep: no test touches the module).
- **C-7 — CONFIRMED (MAJOR) → X-W1.** The gates are vacuous: `reactivity-instant.spec.ts` asserts only that the readout CHANGES and the median ≤50ms (this seat's read of the assertion set) — axis inversion, s/v swap, clamp deletion and a constant luma all stay green; `a11y-modality-support.spec.ts:301-305` asserts the `role="img"` shape D-04 prosecutes — a passing gate that PINS the defect; zero unit tests touch the two pure modules (this seat's grep).
- **L-1 (+ M-2) — CONFIRMED (MAJOR; mechanism corrected, disposition REVERSED) → X-W8 (unification) feeding X-W4 (the rebuild).** A second whole spectrum plate exists (`MiniColorPicker.vue` — identical gradient formula, identical dot placement, `sv-canvas` class; this seat's read) with hand-rolled conversions through the `demo/ui` shims. The mechanism moral inverts (ruling table): the divergence is the LIBRARY's float drift (M-9), and the mini's direct HSV→RGB is the exact one. Disposition reversed per M-2: the mini is the repo's own reference implementation of three flagship defects' cures — `border border-border` (D-05), `overflow-hidden` (D-10), a plain-div thumb with nothing competing for `position` (D-02/D-08) and a fixed height (D-06). Unify ON the mini's containment; carry the flagship's luma regime into it; do NOT "delete the mini's plate outright" as the L-axis prescribed.
- **L-2 — CONFIRMED (MAJOR) → X-W9.** The library privately owns the channel model and publishes none of it (SPACE_SCHEMA/SPACE_IDS/isAnyColor/makeColor absent from the published surface — B's census), so the demo cloned the whole 17-space table (`picker-color.ts:52-70`, byte-diffed identical by B; this seat verified the demo table verbatim). Sequenced with M-10.
- **L-4 (+ C-17, one residue identity) — CONFIRMED (MAJOR) → X-W8.** Residue of the canvas deleted at `a68ecdc1`: the name (no canvas — C-14), the orphan captionless `<figure>` (D-15), a docblock naming the deleted `useGamutOverlay` as a consumer (this seat's grep: the comment is the only hit), 3 of 4 `--gamut-*` tokens with zero consumers + the fourth repurposed under a stale comment, a never-reactive `ref` (`isDragging` — template-unread, this seat's read), and the dead `overflow: visible` line (:234). Nomenclature outliving its referent costs on every read and nothing ever fails.
- **L-5 — CONFIRMED (MAJOR) — IDENTITY-FOLD, not re-booked.** `tsconfig.demo.json:42-49` advertises 8 hand-written entries incl. a bare `@mkbabb/value.js`, `/parsing`, `/units` (all phantom) and omits the shipped `/value` + `/css`; exports = exactly 7 keys, no `.` (this seat re-verified both files). **Fact-identical to the ColorSpaceSelector apotheosis's L-14**, already FOLDed to the standing config-parity row with a born-RED generator command → that row rides X-W8's hygiene band. ONE identity, anti-rename; this corpus adds only a fresh witness.
- **L-6 — CONFIRMED (MAJOR) → X-W8, discharging structurally with D-17's subtraction.** Ownership cycle: `usePointerDebug.ts:117-122` hardcodes the component's private scoped `.spectrum-picker` class twice while the component injects the composable — rename the class and the iOS capture-leak recovery silently stops covering the plate. NOTE: SUBTRACTION.md W20 deletes the debug provider whole (D-17), which deletes the walker and the cycle with it — sequence L-6 behind D-17, don't cure it twice.

### MINOR

- **D-14 — CONFIRMED (MINOR) → X-W10, cure site shared with M-1.** Untokenised motion (`420ms`/`180ms` literals beside a correctly-tokenised sibling transition) animating `background-size` — a third motion species. The real weight of these three lines is M-1. Not a motion-quarantine row (EXISTS-ORIGINAL; the PRM gate is separately correct — see superlatives).
- **D-15 — CONFIRMED (MINOR) → X-W4 (dies in the rebuild's template).** Captionless single-child `<figure>` with `m-0` cancelling its one UA behaviour; A's sharpening adopted: an UNNAMED `role="figure"` boundary is announcement noise layered on D-12's wrong name.
- **D-16 (+ D-07's cause 1, refiled here) — CONFIRMED (MINOR; two sub-limbs killed) → X-W10.** A second cartoon shadow of identical geometry nested inside the card's own, seed-tinted, hover-only — unreachable by keyboard (C-6) and touch. Stands on PR-05/PR-07 + the seed-tint lane law; the Instrument-veil citation is killed (D-16b), the fallback rider is killed (D-16-FALLBACK). Deleting it deletes `--spectrum-shadow`'s only consumer and D-07's cause 1 with it (this seat's grep: 2 hits total).
- **D-17 — CONFIRMED (MINOR; RNG limb killed by both readers) → X-W8.** Production debug instrumentation under a hard `inject(POINTER_DEBUG_KEY)!` with eleven `debug.*` sites (count verified by A), at a surface SUBTRACTION.md:29 (W20) dispositions for deletion. Still on disk.
- **C-12 / L-9 — CONFIRMED (MINOR) → X-W4, producer half on the glass relay.** The gate deactivates mid-drag after 3s (the `3e3` constant verified in dist bytes by this seat; the non-restarting `handleScrollCheck` path per B's decompile) and TWO owners write `touch-action` on the same element (glass-ui imperatively; `useSpectrumPlateStyle.ts:41-43` declaratively — this seat's read). The drift limb stays hypothesis (UNPROVEN).
- **L-7 — CONFIRMED (MINOR, STRENGTHENED by the hedge-kill) → X-W8.** Root-barrel imports of `useTouchGate` where `./dom` publishes it; the axis's own exculpatory hedge is killed — `writeClipboard` is on the same `./dom` subpath (this seat read the export tail), so the fourth site is a fourth wrong edge and the picker tree can reach zero bare-barrel imports now.
- **L-8 — CONFIRMED (MINOR) — IDENTITY-FOLD.** `demo/ui` = 19 pure re-export shims; the demo reaches WatercolorDot directly while MiniColorPicker routes through the shims — two paths to one producer. Same identity as the standing demo/ui extirpation row (ColorPicker/ColorSpaceSelector adjudications) → X-W8, anti-rename.
- **L-11 — CONFIRMED (MINOR) → X-W4 rider.** `isTouchDevice` copied verbatim from glass-ui internals under a comment claiming reuse, while every gate the file constructs already exposes it and SpectrumCanvas reads it the right way.

### INFO

- **C-14 / §0 — CONFIRMED (INFO) → X-W4 rider (the rebuild renames).** `SpectrumCanvas` contains no canvas; three of the commissioning brief's four prime suspects have no referent; every comment already says "the plate".
- **L-12 — CONFIRMED (INFO) → X-W8/X-W1 riders.** The two sibling controls split gesture/presentation at opposite seams, both justified by the same LoC cap; neither seam has unit coverage; the SFC hard-asserts two injections so it cannot mount outside ColorPicker.

## Missed rows — adjudicated in

- **M-1 — MAJOR — the entry animation de-registers the instrument for 600ms on every mount while the plate stays fully interactive → X-W4 (readiness/registration), token half with D-14 → X-W10.** (Reader A.) Re-derived by this seat from source: `both` supplies BACKWARDS fill, so for the 180ms delay the plate computes `opacity: 0` — invisible and hit-testable, with `@pointerdown` bound unconditionally and no readiness guard; for the following 420ms both gradients paint at `background-size: 120%` anchored at `0% 0%`, so the colour SHOWN at plate fraction f is the field value at f/1.2 (see s≈0.417 at the midpoint, get s=0.5) and the black ramp's terminus sits 20% below the plate's bottom edge. Animating `background-size` on a gradient whose geometry IS the coordinate system de-registers the instrument; all three axes read the block and costed only its rasterization. On-screen witness rides UNPROVEN (one frame capture); the mechanism and arithmetic are source-certain.
- **M-2 — MAJOR (disposition row) — the repo already contains the reference implementation of three flagship cures, 165 lines away, and the L-axis prescribed deleting the better half → folded into L-1's ruling (X-W8/X-W4).** (Reader A.) This seat verified every cited byte of `MiniColorPicker.vue` (containment, border, plain-div thumb, fixed height, static CSS gradient with only `--hue` live — the last detail neither reader named: the mini keeps the gradient OUT of per-frame inline style entirely).
- **M-3 — MINOR — `dvh` is the dynamic unit; the plate resizes under the finger during permitted `pan-y` scroll → rider on D-06 (X-W10).** (Reader A.) Unit semantics + the gate's own `pan-y` grant verified in source.
- **M-4 — MINOR — unargued producer-default overrides on a measuring instrument: `cycle-duration 2000` vs default 4000, `range [15,85]` vs [20,80] → X-W10.** (Reader A.) Defaults verified by this seat in the producer d.ts; PRM-safe (the wobble rides the library's ONE `useRAFLoop`, "frozen under prefers-reduced-motion" — producer doc, this seat's read), so a legibility/edict-5 cost, not an a11y one. Continuous non-communicative motion at twice the house cadence on the app's front door; D-09's cure does not touch it.
- **M-5 — MINOR — the dead `box-shadow` is a second attempted SS4.2 register, not hygiene → folded as D-09's rider (X-W10).** (Reader A.)
- **M-6 — INFO — prose defects: the `:202-204` comment names three consumers for a two-item set (naming one of them twice — verified in this seat's read); `DESIGN.md` locates the gamut vocabulary "in `style.css :root`" — no `style.css` exists under `demo/styles/` (this seat's `ls`) — while describing an overlay deleted a tranche ago → X-W8.** (Reader A; the DESIGN.md phantom verified verbatim by this seat at `DESIGN.md:336`.)
- **M-7 — INFO — the unconditionally-applied `touch-gate-target` reserves a permanent 3px transparent ring on every desktop session — the exact boundary channel D-05 says the plate lacks → folded into D-05's cure (X-W10).** (Reader A; the decider of the D-05 ruling.)
- **M-8 — MAJOR (demoted from B's BLOCKER, rationale above) — the demo's domain table cannot represent white in XYZ (`percent×3`, max 1) or jzazbz (jz max 0.222 < white's 0.2220652), and the ONE write gate silently corrupts every such colour: plate top-left in XYZ mode → `#fffff4`, readout pins z at 1.000 → X-W4 (the table + gate) with an X-W9 rider (the library states the true ranges).** (Reader B.) Re-proven END-TO-END by this seat's own dist run and reads of `PICKER_CHANNELS`/`clampPickerColor`/the `useColorPipeline:51,:67` wiring. The clamp oracle structurally cannot catch it (no test hands it a LEGITIMATE coordinate above a declared bound). All three axes walked this write path; none checked the table against the spaces' real ranges.
- **M-9 — MAJOR — `convertColor` accumulates matrix float drift through the XYZ anchor, so analytically identical channels serialize to different bytes and `roundHalfEven`'s tie branch is effectively unreachable for any converted colour → X-W9.** (Reader B.) Re-proven by this seat: `[63.75, 25.499999999999993, 25.5]` → `[64,25,26]`; `hsv(14,1,1)` → r=254.99999999999997, b=5.7e-14. The deliberate CSS-Color-4 tie rule is dead code on every converted path; 8-bit round-trips unstable; L-1's moral inverts onto the library.
- **M-10 — MAJOR — the colour-space table is declared FOUR times (library SPACE_SCHEMA + anchors' HUE_INDEX + css/grammar's CSS_COLOR_SPACES + the demo clone), and the authoritative copy's `hueIndex`/`css` fields plus `SPACE_IDS` are dead → X-W9, sequenced with L-2.** (Reader B.) This seat's read census of `model.ts` confirms every SPACE_SCHEMA read touches only `.channels.length`/`in`-checks, and `operations.ts` reads `HUE_INDEX`, not the schema.
- **M-11 — MINOR — 2 of `spectrumLuma.ts`'s 3 exports are dead surface; the module's self-presentation ("the shared home of a three-consumer regime") is false in both numbers → X-W8, cure with C-5.** (Reader B; this seat's greps are the proof.)
- **M-12 — MINOR — the flip threshold is degenerate exactly where the function is most exercised (s=1,v=1 → exactly 0.5 under strict `>`) → rider on C-5 (X-W4) + the X-W1 unit oracle.** (Reader B.)
- **M-13 — MINOR — `spectrumIsSource` is set BEFORE the write that justifies it (`:125` vs `:129`); a throw strands the sentinel and the next legitimate external clear is swallowed → X-W4.** (Reader B; verified in this seat's read. Fix independent of C-3's: set after success.)
- **M-14 — (folded into D-08) — the marker's position is declared twice, in two vocabularies, both dead.** (Reader B.)
- **M-15 — INFO — `event.currentTarget as HTMLElement` cast + a module-scope raw DOM reference held for the drag's lifetime, while a typed `spectrumRef` for the same node already exists → X-W4 rider.** (Reader B; verified `:56` vs `:135/:150`.)
- **M-16 — MAJOR — glass-ui 7.0.0 already publishes the exact canvas substrate every cure in the corpus prescribes building from scratch, and it is consumed NOWHERE in the demo → cure-law rider on X-W4 (and a guard on every canvas-shaped cure in this record).** (Merged re-reader return; every limb re-verified by this seat.) The installed producer's exports map carries `./canvas`; `dist/canvas.js` exists; `useCanvas2D.d.ts` (read whole, this pass) ships a `DprPolicy` defaulting to `min(devicePixelRatio, 2)`, `respectReducedMotion` default `true` that paints ONE static frame and parks the loop with a live `matchMedia` change listener, an IntersectionObserver offscreen seam with its own suspend reason (`"off-screen-io"`, distinct from the content-visibility path so the two detectors never cross-lift), tab-hidden suspend, and `dispose()`. `grep -rn "glass-ui/canvas|useCanvas2D|useCanvasLifecycle" demo/` → ZERO hits (this seat's run). The commissioning brief's two phantom suspects (per-frame redraw cost, devicePixelRatio handling) are already solved by the producer, and D-01's stage-canvas cure, D-11's overlay cure, and the L-axis's greenfield `useField2dPointer` lattice are ALL mis-scoped against owner edict 4 (glass-ui is the design system — consume the published substrate; author no third canvas lifecycle in demo/ and request no new producer primitive before checking `./canvas`). Any X-W4 rebuild that reaches for a canvas MUST route through this subpath or state why not.
- **M-17 — MINOR — the plate's 12px corner radius clips the field at all four corners while the pointer map keeps those coordinates addressable: the domain's four extremes — (0,0), (0,1), (1,0), (1,1), exactly the coordinates C-5 and the D-18 residue name as high-traffic — are selectable but never painted → rider on D-10's containment reconciliation (X-W4, canon echo X-W10).** (Merged re-reader return; verified by this seat: `:231` `border-radius: var(--radius-xl)` → `12px` in the producer's `dist/styles/theme/radius.css`, read this pass; `background-image` paints to the border box and is clipped by the radius, so each corner loses a ~12×12px quadrant of field to the card behind it; the pointer map at `:117-121` clamps to the RECT, not the painted region.) The containment cure must resolve clip-the-marker vs inset-the-domain vs square-the-corners as ONE decision — D-10 (marker overhang), M-17 (field clipping) and the `overflow: visible` residue (L-4) are three symptoms of the same unreconciled geometry.
- **M-18 — INFO — the SFC states its own channel model correctly at `:2` ("Direct saturation/value field") and incorrectly at `:4-5` ("2D saturation×lightness picker" — the very comment that JUSTIFIES the `role="img"` decision D-04 prosecutes), while `useSpectrumPlateStyle.ts:23` states it correctly ("HSV s/v are 0–1 fractions") two lines above the label rendering v as "lightness" → rider on D-12's cure (X-W4).** (Merged re-reader return; both comment sites verified in this seat's whole reads.) The prescribed rename of the announced channel leaves the WRONG comment standing as the recorded rationale for the a11y shape — the cure must correct the rationale comment in the same edit or the error re-derives.

## Killed-claims register

- **D-01b — KILLED (reader A; sustained).** "Byte-identical background across Lab/OKLCh/HSL + stationary marker" is the CORRECT outcome of that measurement even in a space-aware design showing the same colour — hue is preserved across the projection switch. Rhetorical, not diagnostic; D-01 stands on source.
- **D-02b / D-02-SEVERITY — KILLED (both readers; sustained by this seat's `index.html:11` read).** "Shipped locale mode" is false — `dir="ltr"` hard-pinned, no RTL toggle, no i18n layer; the comment above it declares the seam-only scope. MAJOR, not BLOCKER; BR-10's `scrollWidth` gate structurally cannot catch it.
- **D-05b — KILLED (reader A; SUSTAINED against reader B's contrary cell).** "No boundary declaration at all" is refuted by the corpus's own probe AND this seat's read: an unconditional 3px transparent outline is reserved on the plate. The WCAG 1.4.11 normative framing is separately over-claimed (the SC governs component/state identification, not a content region's outer edge). D-05 survives on the design argument.
- **D-07b — KILLED (reader A; sustained).** The "entire subtree" invalidated by the custom-property write is ONE WatercolorDot child (this seat counted the template). Cause 1 refiled as dead weight under D-16; D-07 stands on cause 2.
- **D-10-MECH — KILLED (reader B; sustained with A's precision).** `overflow: visible` at `:234` is explicitly declared AND causally inert (the initial value; deleting it changes nothing; the cure requires ADDING containment). Separately dead CSS under L-4.
- **D-11b — KILLED (reader A; SUSTAINED by this seat's grep — and reader B's confirm cell repeated the false limb).** The print block (`foundation.css:822-838`) does not contain `.gamut-overlay`; the roster entry count is one (forced-colors :682), not three. Parent D-11 unaffected.
- **D-16b — KILLED (reader A; sustained).** The "Instrument veil" citation is scoped to a tier the same report's D-05 argues the plate does not occupy — self-contradiction; the row stands on PR-05/PR-07/seed-tint.
- **D-16-FALLBACK — KILLED (reader B; sustained by this seat's read).** `--spectrum-shadow` is written unconditionally into the plate's inline style (`useSpectrumPlateStyle.ts:40`, always-resolving computed), so the "wrong fallback" never resolves on the element carrying the rule.
- **D-17b / D-17-RNG — KILLED (both readers; sustained by this seat's `:163` read).** `debug.state.enabled &&` short-circuits; `Math.random()` runs on zero pointermoves unless `?debug=1`. The C axis had it right in its own negative-proof table — the corpus contradicted itself and the D reading loses.
- **D-18 — KILLED as filed (ruling table).** Hue does not collapse in the bottom band (rgb(26,0,0) ≠ rgb(0,0,26)); the <32 threshold measures darkness, not hue-degeneracy. The true scheme-invariance residue rides as D-01's INFO rider.
- **L-1-MECH — KILLED (reader B; SUSTAINED by this seat's own dist run).** The rounding-mode moral is INVERTED: the library's float drift (7e-15, ~1e9× the EPSILON tie window) produces `[64,25,26]` for analytically-equal g/b; `Math.round` in the mini is exact on the direct path. → M-9.
- **C-9-MECH — KILLED (reader B; sustained).** "Forced synchronous layout per frame" is not established — rAF callbacks run before that frame's style/layout, and the prior frame's layout completed before paint; a forced reflow needs an interleaved writer, unmeasured. The supporting numbers are unusable (idle p50 = 50.4ms on a triple-loaded host). Surviving nit: read the rect once at pointerdown, invalidate on resize/scroll.
- **D-NEGPROOF-5 — KILLED (reader B; SUSTAINED by this seat's greps).** The D-axis's "three consumers, never a copied constant" praise: two consumers, and the constant is never imported by anyone — "never duplicated" in the degenerate sense. Rescopes reader A's superlative (below).
- **L-7-HEDGE — KILLED (reader B; sustained by this seat's `dom.js` read).** The hedge's premise is false — `writeClipboard` ships on `./dom` — so the fourth barrel edge is a fourth instance, not an excuse, and L-7 is stronger than filed.
- **C-6-as-mitigated (the "use the sliders" escape) — KILLED (unanimous; recorded so no cure adopts it).** The sliders expose the SELECTED space's channels; s and v are reachable only after a space switch the keyboard user has no reason to know about.
- **D-03-CAUSE — KILLED (merged return; sustained by this seat's read).** The filed mechanism — cancel commits BECAUSE `stopDragging` flushes `pendingCoords` — is a mis-attributed cause: every move during the gesture already committed through the rAF coalescer (`:97-108` → `:129`), so the flush holds at most one sub-frame residue and deleting it cures nothing. The challenge's own cure paragraph (latch at pointerdown) contradicts its mechanism paragraph. D-03's FINDING is untouched; its causal sentence would mislead an implementer.
- **D-06-HOOK — KILLED (merged return; sustained by this seat's verbatim read of `VISUAL-CONSTITUTION.md:33`).** SS3 law 7 — "Spacing is container-scaled from glass-ui tokens. No desktop-tight/mobile-airy fork and no breakpoint pile" — governs SPACING rhythm; `h-[20dvh] min-h-24 max-h-40 lg:h-[14rem] lg:max-h-none` is an element BLOCK-SIZE ladder using no spacing tokens. Stretching a spacing law over a sizing declaration manufactures a canon violation the constitution does not state (the "breakpoint pile" phrase's spirit is at most arguable, and an argued spirit is not a citable law). D-06's measured anisotropy stands without it.
- **D-10-HOOK — KILLED (merged return; sustained by this seat's read of PROPORTION-AUDIT §5 card law 3).** "Renderer, icon or touch footprints may reserve collision space only on the axis where collision exists" is a RESTRICTION on reserving space — a permission clause — not a mandate to reserve. D-10's "no space is reserved on either axis" reads a prohibition as an obligation; the law is silent on this component. The physical finding (half the marker outside the plate at v=1) is independently confirmed and needs no hook.
- **D-05-TIER — KILLED (merged return; sustained by this seat's read of the §2 material table).** "The plate is assigned no tier" is asserted where the table names a candidate: the Structural-glass row reads "dock, header, **primary plate**" — and "plate" is this codebase's own word for this exact surface in every comment (`spectrumLuma.ts:5`, `useSpectrumPlateStyle.ts:1`, `SpectrumCanvas.vue` `:202`, the composable's own name). The tier may be assigned and simply not honoured (a chromatic field is not "neutral Clear-Ice/Smoke family"); either reading yields a defect, but the categorical no-tier sentence is contestable and may not be re-cited as canon fact. D-05's measured/boundary half is untouched; D-16b's kill survives on its own grounds under EITHER tier reading (whichever row applies, the veil citation is unproven for this surface).

## Superlatives — with provenance

- **The rAF single-shot coalescer** (`:97-108`): guard on `spectrumRafId === null`, primitive coords only (never a pooled event), flush-on-stop, cancelled on BOTH teardown paths — the CONSTELLATION-audit PRM-RAF epidemic does not reach this file, and the C axis proved the negative with line evidence rather than assuming it. (Both readers; this seat's read concurs.)
- **`releaseCapture()`** (`:81-91`): idempotent, try/catch for the detached-node case with an honest comment, both locals nulled UNCONDITIONALLY outside the catch, reached from all three exits + `onUnmounted`. The one gesture invariant in the file that is airtight. (Both readers; this seat's read concurs.)
- **The reduced-motion gating is stronger than the app-wide guard, unprompted**: keyframes AND the animation declaration both nested inside `@media (prefers-reduced-motion: no-preference)` (`:243-257`, this seat's read) — under `reduce` the animation NAME is never assigned rather than being clamped to 0.01ms. The pattern motion-quarantine M-12 (TRUE) singles out in the producer, done here by a consumer. All three axes certified it; both readers concur; ironically these are the same three lines that carry M-1.
- **The raw-coordinate persistence idea** (`:58-75`): a precision instrument keeping the marker where the user placed it instead of jittering through an HSV round-trip is the right instinct, with the contract and clearing rule written down. The IMPLEMENTATION is C-3/M-13's spendable boolean; the intent is one of the file's best decisions, and the correct mechanism (reference-identity, `lastWrittenModel`) already exists two files away in `useColorPipeline`. (Reader A the intent; reader B the neighboring mechanism; both adopted.)
- **The forced-colors policy** (`foundation.css:668-698`, this seat's read): a reasoned two-tier law in prose, tier-1 `forced-color-adjust: none` for colour-truth surfaces, chrome at UA default, plus the U-F25 real-outline restoration — and it is oracle-ASSERTED. D-05 is right that it never gave the surface an edge; the policy itself is the most thoughtful a11y decision in the component's blast radius.
- **`role="img"` chosen for a written-down reason** (`:4-5`): the file rejects `role="slider"` explicitly and correctly (a slider with no single value would lie to AT). Wrong conclusion, real reasoning — more than most of the tree records. (Reader B.)
- **The C axis's 15-row negative-proof table** (`challenge-C-implementation.md:614-636`): hazard → verdict → file:line for each negative (ValueUnit nesting N/A, defineModel stale-read ABSENT with the S.W2 cite, stableHue HANDLED, leaks CLEAN); it is also what killed D-17's RNG limb. The best-executed section in the corpus. (Reader B; spot-rows re-verified.)
- **Module hygiene**: `useTemplateRef`, injection-based model (no defineModel round-trip — the S.W2 transposition structurally removes this repo's recorded stale-read caveat), `verbatimModuleSyntax` correct including `import type { useTouchGate }` for a `ReturnType` position, 272 lines, one colocated composable, one shared helper. (Reader A; this seat's read concurs.)
- **RESCOPED — the shared-luma single-home**: the ARCHITECTURE (one predicate, one home, two real consumers, no copied threshold anywhere — this seat's greps) is right and is why C-5 has a single-site cure. The module's SURFACE is not (M-11 dead exports, M-12 degenerate threshold, a docblock naming a dead consumer). A superlative with its boundary on record.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **All wall-clock/CDP magnitudes**: D-07's 20.6ms-per-move / 1768.9ms RecalcStyle, C-9's p50/longTask tables — dev-server, unbundled, on a host running three concurrent seats (C-9's own idle p50 = 50.4ms). Both named causes are source-confirmed; re-measure on a quiet host against the BUILT bundle before any number gates anything.
2. **Production-bundle CSS emission order** for the D-02/D-08 `position` tie: dev/module-graph order is proven; the gh-pages bundle's order is not (SPCD-probe5.mjs's output was never committed — the single evidence gap in the corpus's strongest chain). One built-bundle read closes it.
3. **C-3's felt magnitude**: the wipe mechanism is now source-proven (this seat); the visible dot-jump size per space's lossiness is live-only.
4. **C-4's live Kelvin readout collapse** (three picks → one 1000K value): structural premise fully confirmed; the in-app sequence unreproduced under probe parsimony.
5. **C-12/L-9's touch-action drift** limb: shared ownership proven; an actual divergence never reproduced; the 3000ms constant verified in dist bytes by this seat.
6. **D-05's contrast matrix** (1.538 light / 1.091 forced-colors corner): recorded and internally consistent, with A's antialiasing caveat on the "inside" sample; direction safe, exact ratios carry a sampling caveat.
7. **M-1's on-screen misregistration**: mechanism + arithmetic source-certain; one live frame capture during the 600ms window would witness the see-s≈0.417/get-s=0.5 delta.
8. **AT announcement behavior** for D-12/D-15 (what NVDA/JAWS/VoiceOver actually say crossing the unnamed figure into the misnamed img): structural halves proven; surfaced strings are not.
9. **L-1's quantitative divergence figure** (2.72% of 10,830): mechanism re-proven here; the sweep lived outside the repo.
10. **C-15**: conditional rider on C-16's cure (live only if `none` becomes admissible) — carry with that fix, not as a defect.
11. **D-01's byte-identity presentation** (needs `stableHue` survival timing): the structural finding does not need it; do not re-cite the byte cell.
12. **The D-18 residue fraction** (12.15% measured vs 12.55% derived): arithmetic corroborated; the live pixel census is the probe's.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet)**: reader B's D-11 cell asserts "`:831` rosters it for print too" — the challenge corpus's false limb — while reader A's kill D-11b disproved it with the same grep this seat re-ran. B's OTHER cells on the same row (the two-hit census) are its own and correct. Same event class as the ColorSpaceSelector apotheosis's L-10 stale-cell finding: verbatim repetition without re-derivation is detectable, and was detected.
2. **Reader B's D-05 evidence cell is wrong** ("outline only under `touch-gate-active`") — the declaration is unconditional, only the color is gated. B confirmed the right verdict on a wrong fact; A's probe-grounded kill decided it.
3. **Reader A's C-3 UNPROVEN hedge is overturned** — the mechanism was decidable from one whole-file read A did not perform. Recorded symmetrically with A's correct overturning of B's D-05 cell: each reader missed once where the other's instrument reached.
4. **M-8 severity (B: BLOCKER → ruled MAJOR)**: B's position preserved verbatim — "the ONE write gate silently mutates every colour the plate writes there… the entire near-white region the plate's top-left corner exists to reach." This seat demoted on the program's BLOCKER register (capability-zero/crash/boot-lie) versus a bounded ≤11/255 corruption in an opt-in space. If the owner's severity law weighs silent-instrument-corruption as capability-class, the row promotes without re-adjudication.
5. **D-04 severity (B: MAJOR → ruled BLOCKER)**: B's demotion argument (the sliders exist; the constitution row could be read as aspirational) preserved; ruled against because the written law names the surface and the mitigation requires knowledge the keyboard user cannot acquire from the interface.
6. **D-18 (A: KILL vs B: INFO-confirm)**: ruled KILL-as-filed; B's arithmetic and its "inherent to every HSV plate" severity challenge are absorbed into the D-01 rider. The disagreement was about labels, not facts.
7. **Both readers over-called ExtractControls.vue:140** as a "conflicting third declaration" — it is scoped and border-radius-only (this seat's read). D-13's two-owner substance is untouched.
8. **Reader A's superlative on the luma regime partially unearned** (D-NEGPROOF-5 sustained); A's OTHER superlatives all survived re-derivation, and A's kill-rate on its own axis's rows (D-01b/D-05b/D-07b/D-11b/D-16b/D-17b/D-02b — seven kills of D-axis limbs) is the corpus's best evidence of non-inheritance.
9. **The merged re-reader return (second pass): two overrules, two corroborations.** (a) Its C-9-structural CONFIRM ("a forced synchronous layout per drag frame") is OVERRULED — the C-9-MECH kill stands: rAF callbacks run before the current frame's style/layout pass, the prior frame's marker write was flushed at that frame's own render, and the reactive style write lands in the post-callback microtask; a forced reflow needs an interleaved same-frame writer nobody measured. (b) Its `spectrumLuma` superlative ("the law holds: both live consumers import the function; zero duplicated constants") repeats the unearned cell — true only degenerately, since the CONSTANT is imported by no one and 2 of 3 exports are dead; the D-NEGPROOF-5 rescope stands on this pass's re-greps. Credited corroborations: the merged return's independent arithmetic closure of D-02's RTL fraction (1.56820 to five figures) and its independent derivation of the D-18 threshold (max<32 ⇒ v<0.1255) both check out and are absorbed as evidence cells.

## THIRD-PASS — the r2 hostile re-reader pair, adjudicated (E-3 addendum)

### Ruled disagreements (r2)

| row | r2 reader A (design-and-usage) | r2 reader B (library-and-code) | RULING | Deciding evidence (this seat's own, this pass) |
|---|---|---|---|---|
| **D-01 severity** | MAJOR (demoted: "the constitution nowhere requires the stage to *render* the selected space; an HSV square with a foreign readout is the industry-standard shape") | BLOCKER retained | **MAJOR — pass-1's ruling sustained; A's demotion rationale adopted as the record's.** | The defensible canon clause is SS5.2's single-coordinate-model sentence, not a render-the-space mandate. B's position preserved in DISSENT 10. |
| **D-17's RNG limb** | CONFIRMED, "including `Math.random() < 0.03` at `:163` evaluated on every pointermove" | (not re-filed; B's pass-1 ancestor killed it) | **OVERRULED — the D-17-RNG kill stands; citation-inheritance event #2.** | This pass's `:163` re-read: `if (debug.state.enabled && Math.random() < 0.03)` — `&&` short-circuits; the RNG runs on zero production pointermoves. r2-A repeated the corpus's false limb verbatim against a kill both pass-1 readers and this seat had already sustained. Recorded in DISSENT 11. |
| **C-9 / forced synchronous layout** | CONFIRMED ("gBCR inside the rAF callback… read-after-write reflow by construction, the most direct explanation of LayoutCount 221") + D-07-b kill built on it | CONFIRMED (merged C-9/D-07, same mechanism sentence) | **BOTH OVERRULED — C-9-MECH stands, third sustaining.** | Frame anatomy: the rAF callback runs BEFORE frame N's style/layout pass; frame N−1's marker write was flushed at N−1's own render; the reactive style write lands in the post-callback microtask. A forced reflow needs an interleaved same-frame writer nobody measured, and the only numbers offered are from a host at idle p50 50.4ms. The surviving nit is unchanged: hoist the rect read to pointerdown, invalidate on resize/scroll. The r2 pair's unanimity is preserved in DISSENT 12 — unanimity on an unmeasured mechanism is still unmeasured. |
| **C-3** | CONFIRMED (mechanism fully traced + probe jump at t=310ms) | CONFIRMED ("holds by static reading alone") | **CORROBORATED** — pass-1's ruling (this seat's own whole-file trace) now has three independent derivations plus the probe timing. | No new evidence needed; the `syncGen`-guards-one-direction cell matches this record's. |
| **C-15** | UNPROVEN (self-labelled hypothesis, kept) | KILLED — "mechanism misattributed: `convertColor(<none>, 'hsv')` returns `{ok:false}`; `valueOrThrow` throws inside `HSVCurrentColor` one computed upstream; `dotPos` cannot become the next crash site" | **B's kill SUSTAINED; pass-1 residue #10 retired.** | This seat's pass-1 read of `useColorPipeline.ts:106-109` (HSVCurrentColor = `withChannel(convertPickerColor(·,"hsv"),…)`) + B's node witness: a `none` channel cannot survive conversion into an hsv colour, so the unguarded `channelNumber` in `dotPos` is an unreachable branch under every admissible cure of C-16 that rejects `none`, and under one that admits it the crash is still upstream. C-15 is dead as filed, not conditional. |
| **L-1 / L-2 severity** | L-1 BLOCKER→MAJOR ("popover mini-picker in the palette search bar, not a shipped-path break") | L-1 + L-2 both demoted BLOCKER→MAJOR (sub-JND divergence; API-shape, nothing broken) | **CORROBORATED** — pass-1 already held both at MAJOR; the two demotion rationales are adopted as the record's severity prose. | No change. B's L-1-severity arithmetic (±1 in one 8-bit channel, below any perceptual floor) is the sharper cell. |
| **L-1 mechanism** | Killed L-1-b — at the headline sample the DUPLICATE agrees with the closed form; the library's tie window (~5.66e-15 at 25.5) is ~1 ulp too tight for the 7e-15 drift; "the divergence is caused by the library" | CONFIRMED L-1's original mechanism ("library quantizes with roundHalfEven, mini uses Math.round"); own sweep found **747** disagreements on the same nominal 10,830 grid | **A wins — the L-1-MECH kill sustained a third time, now with A's tie-window arithmetic adopted into M-9.** B's mechanism cell repeats the inverted moral. | This pass's third dist run: `[63.75, 25.499999999999993, 25.5]` → clip → `[64,25,26,255]`; g and b are analytically equal; no rounding mode separates them. A's window arithmetic (`Number.EPSILON·25.5 ≈ 5.66e-15` < error `7.1e-15`) is the sharpest statement of M-9 yet and is folded in. B's 747-vs-295 sweep discrepancy rides to the residue (R15). |
| **D-05's shape** | Confirmed with the refinement "part of it dissolves, not 'no boundary'" (bottom edges pass at 12.4/14.1) | Confirmed "the edge can never be self-evident — its own content supplies both #fff and #000" | **Both absorbed as precision; pass-1's ruling (cure = give the RESERVED 3px ring a resting value, M-7) unchanged.** Neither r2 reader saw the reserved outline — the corpus's best cure fact remains this record's alone. | B's unproven item — the touch-gate outline's own `color-mix(… 50%, transparent)` contrast was never measured by any axis — is adopted as new residue R13: if the activated ring ALSO fails 1.4.11, D-13 and D-05 are both worse than filed. |

### Missed rows (r2) — adjudicated in

- **M-19 — MAJOR — selecting HSL from the space selector silently destroys an out-of-sRGB colour: the shipped boot colour becomes WHITE → X-W4 (one cure decision with M-8: the write gate must gamut-map or reject, never clamp).** (r2 reader A.) Re-proven end-to-end by this seat this pass: `convertColor(lab(92,88.8,20),'hsl').channels = [346.045, −12.9515, 1.0366]`; `clampPickerColor` (`:195-202`, min/max against `percent` [0,1]) ⇒ `hsl(346, 0, 1)` = white. Distinct mechanism from M-8 (there the TABLE is wrong for legitimate coordinates; here the table is right for HSL and the clamp destroys an out-of-gamut conversion) — same gate, one cure decision. This is also the true reading of the challenge's own probe row: D's space matrix tabulated readout `346deg,0%,100%` + dot at `[5.55e-14%, 0%]` and scored it "the plate does not respond" — the plate responded correctly to a colour the gate had just destroyed. The D-01-b kill (below) is this row's surface.
- **M-20 — MAJOR — `dotPos` clamps out-of-domain HSV coordinates, so the marker misreports the colour's position on first paint with the shipped default → X-W4, rider on D-11 (the delta's mechanism) and D-12 (the aria fiction).** (r2 reader A.) Verified this pass: boot colour → hsv v = `1.5109915`; `clamp(·,0,1)` at `:207-208` pins the marker to `top: 0%`, collapsing the whole out-of-sRGB family above v=1 onto one pixel row with no indication; the announced "100%" (`useSpectrumPlateStyle.ts:27`) is a clamped fiction. The display-side twin of C-4's input-side degeneracy, and the mechanism UNDER D-11's measured [0,+48,+68] boot delta — three axes measured the symptom, none named the clamp.
- **M-21 — MAJOR — a live smoke-project oracle asserts on `.plate-caption`, the class of the component deleted at `a68ecdc1`; the selector exists nowhere in the product, so the gate cannot pass → X-W1 (the re-gate owns it) with an X-W8 echo (same `a68ecdc1` residue family as L-4).** (r2 reader B.) Verified this pass: `o18-contrast-census.spec.ts:648-690` is a live un-skipped `test(...)` doing `expect(page.locator(".plate-caption").first()).toBeVisible()` + a census + a computed-style identity check; `grep plate-caption demo/` → exactly 4 prose comments, zero markup, zero CSS; `playwright.config.ts:147-153` testIgnore carves admin/mobile/safari/perf/reactivity — NOT `oracles/`, so the smoke project runs it. The same commit that orphaned the `<figure>` orphaned its oracle; all prior seats told the deleted-caption story without checking whether anything still asserts on it. A structurally-red gate in the lane is either failing silently or the lane is not running — either way C-7's vacuity indictment gains a limb: the gate surface contains an assertion that cannot be true. Live disposition (fail vs timeout vs lane-filtered) rides R16.
- **M-22 — MAJOR — the touch gate keys off DEVICE CAPABILITY (`isTouchDevice` = `'ontouchstart' in window`), never off the event's actual modality, so on hybrid machines a mouse/pen user's first click is silently swallowed — and again after every 3s idle → X-W4 (consumer: gate on `event.pointerType === 'touch'`), producer half on the glass-ui BH relay (the gate API should accept per-event modality).** (r2 reader B.) Verified this pass: `:138` branches on `spectrumGate.isTouchDevice && !isActive` and `grep pointerType` over the SFC returns nothing; the capability bit is true on touch-screen laptops/iPadOS where the primary pointer is frequently a mouse or trackpad; the scroll-cancel escape (`@touchmove.passive`, `:20`) never fires for a mouse. D-13 measured the swallow on an emulated iPhone and framed it as touch-only discoverability; the modality bug underneath was unfiled by every prior seat. Real-hardware witness rides R14.
- **M-23 — MAJOR — the producer ships `suppressDeactivate()` precisely to hold a gate open across a long gesture; this component never calls it, so the 3s timeout does not merely drop the outline — it flips the computed `touchAction` from `none` to `pan-y` mid-drag and hands the axis to the page scroller under the user's finger → X-W4, folded at cure with C-12/L-9 (whose severity this row is the true weight of).** (r2 reader B.) Verified this pass: `grep suppressDeactivate demo/` → zero; the producer's dist ships it; the chain is complete in source — `x()` sets `isActive` false → `spectrumStyle` (`:41-44`, re-read this pass) recomputes to `touchAction: "pan-y"` → Vue writes it back while `isDragging` is still true. C-12 found the timeout and L-9 the dual ownership; neither connected them nor found the producer-sanctioned cure sitting in the API. Hardware witness rides R14.
- **M-24 — MINOR — `handleSpectrumDown` overwrites the capture bookkeeping without releasing the capture it discards (`:148-150`: no `capturedPointerId !== null` check, no `releaseCapture()` first), so `releaseCapture()` and `onUnmounted` can only ever release the LAST pointer → X-W4, C-1's third limb (the down handler; C-1 audited move/up/cancel and stopped).** (r2 reader B; verified in this pass's read.) A second finger rebinds both module locals and the first pointer's capture becomes untrackable — exactly the leak class `forceReleaseAllPointers()` exists for, whose selector list begins with `.spectrum-picker`.
- **M-25 — MINOR — `spectrumStyle` welds two cadences into one computed: the hue-only gradient template (byte-stable during an s/v drag — `stableHue` pins h) is rebuilt every frame alongside the per-frame `--spectrum-shadow`, only for Vue's patcher to diff the identical string away → X-W4 rider (dies with D-16's deletion + the rebuild; the split is the file's own colocation law applied to itself).** (r2 reader B; the single-computed shape re-verified this pass at `:30-45`.)
- **M-26 — MINOR — the product's dominant instrument has no visible name anywhere — no caption, heading, axis legend, or `aria-labelledby`; the only naming in the DOM is the aria string the corpus proved wrong (D-12) → X-W4 (the rebuild's naming layer), canon echo X-W10 (SS5.2 asks for two NAMED axes; there is not one name on screen).** (r2 reader A.) Verified this pass: `ColorPicker.vue:64-72` renders `<SpectrumCanvas />` bare in an unlabeled flex div; the deleted `SpectrumPlateCaption.vue` (81 lines, `a68ecdc1`) was the visible name and nothing replaced it — L-4/D-15 caught the syntax residue, M-21 the gate residue; this row is the user-facing consequence, and it makes M-21's orphaned oracle the tombstone of a REAL loss, not just a stale selector.

### Killed-claims register (r2 additions)

- **D-06-b — KILLED (r2 reader A; sustained by arithmetic).** "The block size follows the viewport's height" is false on 3 of D-06's own 6 viewports including both primary ones: `min-h-24`(96px)/`max-h-40`(160px) clamp the dvh arm and `lg:h-[14rem]`(224px) overrides it wholesale — 1440×900→224 (lg constant), 1023×900→160 (clamped; 20dvh=180), 390×844→160 (clamped; 20dvh=168.8). The dvh arm has authority only in the 480-800px height band below `lg`. The sharper true statement folds into D-06 as precision: the one declaration that looks responsive is INERT exactly where users are, and the box's height is really two magic clamps plus a breakpoint constant. (M-3's dynamic-unit rider survives — within the governing band the plate still resizes under the finger.)
- **C-11-UNMOUNT — KILLED (r2 reader B; sustained).** "The affordance silently disappears if ComponentSliders is ever unmounted" — false mechanism: SFC `<style>` injects at MODULE evaluation (dev: style tag at import; prod: emitted stylesheet), not at instance mount; unmounting leaves the cascade untouched. The real hazard is code-splitting/lazy-load or module deletion — exactly as L-3 states it. D-13's severity is right; C-11's failure mode may not be re-cited.
- **C-1-CURE — KILLED (r2 reader B; sustained by inspection).** The prescribed uniform guard `if (activePointer?.id !== event.pointerId) return;` applied to `handleSpectrumDown` evaluates `null?.id !== id` → true on the FIRST press and the component can never start a drag. The down handler needs the inverse rule (reject a second pointer, accept the first) — which is also the rule that fixes M-24. The one-`activePointer` collapse survives; the uniform guard may not be implemented as written.
- **C-15 — KILLED as filed (r2 reader B; ruled above).** Unreachable branch; the crash site is and remains the conversion inside `HSVCurrentColor`. Pass-1 residue #10 is RETIRED — do not carry C-15 even as a conditional rider.
- **C-7-SCOPE — KILLED (both r2 readers, independently; sustained by this pass's e2e grep).** "Exactly two specs touch this component" — at least FOUR live specs reach the plate (`reactivity-instant`, `a11y-modality-support`, `a11y-web-modality:133-151` — which pins `print-color-adjust` on `.spectrum-picker` AND resolves the plate by the `role="img"` name — and `webgl-blob-idle:66-74`, which clicks it), plus the o18 oracle (M-21) and two perf specs. The correction cuts in C-7's favour twice: the role="img" shape has at least four green pins to move, and one member of the gate surface asserts a selector that cannot match. C-7's vacuity verdict is STRENGTHENED.
- **L-7-TYPEONLY — KILLED as precision (r2 reader B; sustained).** `useSpectrumPlateStyle.ts:12`'s `import type { useTouchGate }` is erased at emit under `verbatimModuleSyntax` — a type-resolution nit, not a module-graph edge. L-7's runtime census corrects to: `SpectrumCanvas.vue:39` + `useSliderTouchGates.ts:15` (+ `ColorPicker.vue:138`'s `writeClipboard`, per the pass-1 hedge-kill) as the wrong runtime edges, the type site a consistency rider. L-7 stays MINOR; both prior kills coexist.
- **D-07-b-PARTIAL — PARTIALLY SUSTAINED (r2 reader A's kill of D-07's cure).** True half: the prescribed `translate` + shadow-deletion cure leaves a per-frame `getBoundingClientRect()` that should be hoisted to pointerdown (the standing C-9 nit) — a cure checklist item. Overruled half: its premise ("read-after-write reflow BY CONSTRUCTION… the most direct explanation of LayoutCount") is the same unmeasured mechanism C-9-MECH kills; the LayoutCount attribution may not be re-cited as established.

### DISSENT (r2 additions — preserved, not ruled away)

10. **D-01 severity (r2-B: BLOCKER)**: "the dominant instrument of a color-space laboratory ignores the one selection it exists to serve" — preserved verbatim; ruled MAJOR on A's industry-shape rationale and the canon's actual sentence. If the owner reads SS5.2's coordinate-model clause as a render mandate, the row promotes.
11. **Citation-inheritance event #2 (evidence about the fleet)**: r2 reader A's D-17 confirm cell repeats "`Math.random() < 0.03` at `:163` evaluated on every pointermove" — the corpus's false limb, killed in pass 1 by BOTH readers (one of them A's own axis-ancestor) and sustained by this seat's `:163` read twice. Same event class as pass-1's D-11 print-roster inheritance (DISSENT 1), now observed in the opposite reader. The fleet inherits unre-derived cells at a measurable rate; every future seat must treat any quantified per-event claim as unverified until the line is re-read.
12. **C-9 unanimity (both r2 readers + the pass-2 merged return = three confirmations of the forced-layout mechanism, all overruled)**: preserved because the seat may be wrong — if a live trace on a quiet host ever shows Layout entries attributed inside the rAF callback with the marker write in the same frame, C-9-MECH reverses and D-07's cure checklist gains the hoist as a MUST. Until measured, three unmeasured agreements remain zero measurements (R17).
13. **The r2 pair's own internal contradiction on L-1's sweep**: A's kill re-derived the headline sample and refuted the mechanism; B's confirm re-ran a 10,830 grid and got 747 disagreements where the challenge claimed 295 — while repeating the refuted mechanism sentence. BOTH numbers are now suspect (R15); the sample-level facts (this seat's three dist runs) are the only pinned cells.

### Residue additions (r2) — appended to the UNPROVEN-NEEDS-LIVE set

13. **R13 — the touch-gate outline's own contrast** (`color-mix(in srgb, var(--foreground) 50%, transparent)` against the card, both schemes): never measured by any axis across three passes; if it fails 1.4.11 the D-13 activation affordance and the D-05 cure channel (M-7's reserved ring) are both worse than filed. One computed-style + ratio probe closes it.
14. **R14 — real-hardware witnesses for the gate rows** (M-22's hybrid first-click swallow, M-23's mid-drag `pan-y` handover, C-12's 3s deactivation, D-13's first-tap swallow): every chain is source-complete from the producer's shipped bytes; no seat across three passes has touched real touch or hybrid hardware. Emulation established D-13's half only.
15. **R15 — L-1's quantitative divergence**: challenge 295 vs r2-B 747 on the same nominal grid, with the stated mechanism refuted three times — the composition is unknown and may be dominated by the library's own M-9 defect. Re-derive the sweep INSIDE the repo against dist before any number is quoted.
16. **R16 — M-21's live disposition**: the `.plate-caption` oracle structurally cannot pass; whether it currently reports RED, times out, or is being skipped by a runtime filter in the actual gate lane needs one `npx playwright test o18-contrast-census` run — which also dates how long the lane has tolerated a red member.
17. **R17 — the C-9 decider**: one performance trace on a quiet host, built bundle, single drag, checking whether any Layout entry is attributed within the rAF callback scope. Settles the three-times-overruled mechanism in either direction.

## Closing verdict

**SOURCE-RED, blocker-weight — adjudicated, not verified.** The gesture plumbing's teardown half is disciplined and the file reasons in writing more than most of the tree — and the instrument it serves misregisters its own coordinates on mount (M-1) and at boot (M-20), cannot be driven by a keyboard at all (D-04), commits cancelled gestures (D-03), lies to AT in name and channel (D-12) and carries no visible name at all (M-26), wipes its own persistence 300ms after every gesture (C-3), writes through a gate that corrupts two spaces' whites (M-8) and bleaches the boot colour to white on an HSL switch (M-19), swallows the first click of every hybrid-device mouse user (M-22), hands the drag axis to the page scroller after 3s while the producer's own remedy sits unconsumed (M-23), and styles a producer root that silently discards three of its four instructions (D-08) — all behind gates that assert only that something changed (C-7), one of which asserts a selector deleted a tranche ago (M-21). Terminal routing: the control-rebuild cluster (D-01, D-02, D-03, D-04/C-6, D-07, D-08, D-10/C-10, D-12+M-18+M-20, D-13, D-15, C-1+M-24, C-2, C-3, C-4-component, C-5, C-12/L-9+M-23, C-14, L-11, M-1, M-8+M-19, M-12, M-13, M-15, M-17, M-22, M-25, M-26, **M-16 as the cure-law: any canvas in the rebuild consumes the producer's `./canvas` substrate, never a third lifecycle**) → **X-W4** (M-22/M-23's producer-API half + D-13's gate API on the glass-ui BH relay); the design/motion-law cluster (D-05+M-7, D-06+M-3 with the D-06-b precision, D-09+M-5, D-11, D-14, D-16, M-4, M-17-echo, M-26-echo) → **X-W10**; the subtraction/hygiene cluster (D-17, L-1+M-2, L-4/C-17, L-5-fold, L-6, L-7, L-8-fold, L-12, M-6, M-11, M-21-echo) → **X-W8**; the library cluster (C-4-scan, L-2, M-9 with the tie-window arithmetic, M-10) → **X-W9**; the gate cluster (C-7-strengthened + M-21 + the luma/plate unit oracles) → **X-W1**; the upstream boot BLOCKER (C-16; C-15 killed as filed, nothing rides) → **X-W2**. No row is NO-WAVE-OWNER. The sixteen live UNPROVEN rows above (pass-1's #10 retired; R13-R17 added) are this component's visual-audit input set.

— end of apotheosis. The three challenge files and all re-reader returns (pass-1 pair, pass-2 merged, pass-3 r2 pair) remain immutable beside this record per E-1/E-3; prior-pass rows were not rewritten — the third pass is an E-3 addendum with its own instruments.
