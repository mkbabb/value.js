# wb-mix-pane — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.

**Seat**: incredulity adjudicator over two hostile re-readers · **Date**: 2026-08-24 · **HEAD**: `053f7d59`, branch `tranche-u` · **Corpus base**: `c654824e` — re-checked by this seat: `git status --porcelain` over `demo/workbenches/mix/ demo/palettes/ demo/styles/ demo/shell/ eslint.config.js` is EMPTY at HEAD, so every corpus anchor re-resolves unchanged (reader-LC's baseline note adopted after independent re-run).
**Corpus (r2 governs)**: `audit/components/wb-mix-pane/` — challenge-D-design.md (pass D over A/B/C), challenge-C-implementation.md (pass D over A/B/C), challenge-L-library.md (pass E over A/B/C/D), all priors preserved byte-identical per their own shasum receipts.
**Readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both returns were attacked, not transcribed: every kill was re-derived against the tree, every cross-reader conflict ruled on this seat's own instruments, and the two suspiciously-convergent missed-finds (the zero-length crash) merged only after independent re-derivation.
**Arbiter's own instruments this session (static + node against dist; zero browser tooling — probe parsimony)**: whole reads of MixPane.vue, useMixingState.ts, demo/palettes/mix.ts, MixSourceSelector.vue, MixResultDisplay.vue, useMixingAnimation.ts; usePaletteStore.ts:50-94 (the MR-24 decider); shell.css:15-140; animations.css:70-139; foundation.css:140-146/:320-332/:425-432/:464-485/:675-698; utils.css:92-110; App.vue:20-140/:310-335; usePaneRouter.ts:100-115/:210-230; Dock.vue:150-160; PaneSlot.vue:115-130; MixConfigBar.vue:78-95/:140-172; PaneHeader + EmptyState `--ink-muted` census (10 consumer files); producer bytes at the installed pin — `dist/components/button/styles.css` disabled rule extracted by node (`opacity: var(--opacity-disabled); cursor: not-allowed` + `pointer-events: none`, nothing else), `dist/styles/tokens/scheme-motion.css` z ladder (`--z-controls: 20 · --z-popover: 130 · --z-modal: 140 · --z-toast: 160`); greps re-run at HEAD: `--vj-morph|--vj-celebrate` in demo/workbenches/mix → 0 · `aria-live|role="status"|role="alert"` in mix → 0 · `.focus()` in mix → 0 · `onDeactivated` in demo → 0 · `vj-celebrate` consumers = ColorInput/ActionFeedback/AdminUsersPanel (never mix); tsconfig census (`noUncheckedIndexedAccess: true` at tsconfig.base.json:10; `noUnusedLocals`/`strictTemplates` in NO tsconfig); eslint.config.js:230-278 both glob objects re-read; W6.md:17/:337/:457 + chassis-fitness.md §0 (the CC-108 reversal chain); banked registry re-reads: wb-mix-resultdisplay.md (MR-7/-12/-13/-16/-18/-22/-24/-31/-33, K-10/-11/-12, sup. 6), wb-mix-animationcanvas.md (MX-1/-2/-3/-4/-6/-7/-14/-15/-20/-25/-31/-32/-35/-39/-41/-44, K-2, instruments block), App.md D-8, AuroraPane.md AP-1/AP-13, ColorSpaceSelector.md (format + L-5(a)/L-14 identities); motion-quarantine.md `grep -in mix` → one unrelated PaneHeader sentence at :162, so **no M-xx row exists for this component and none is carried**; `ls demo/palettes/export/*.ts` → 12; `ls -d dist/gh-pages` → absent; **node probes**: color-object shape from `dist/subpaths/{color,css}.js`, and an order-dependence run of the exact `mixColorSequence` fold (the F-1 decider — third independent instrument, numbers below).

## Scoreboard

**38 CONFIRMED (19 folds-by-reference to banked identities · 12 corpus rows booked/rescoped here · 7 re-reader finds admitted) · 16 KILLED (all 16 reader kills UPHELD on re-derivation; 0 kills killed) · 1 BANKED-ROW CORRECTION (MR-24's dedupe limb is false at source) · 12 UNPROVEN-NEEDS-LIVE residue rows.**

Blocker-weight survivors are all **banked, not re-booked**: MX-1 (`[data-mix-target]` never renders; fabricated convergence target), MX-3/ES-15 (both colors-mode add paths are dead producer faces and colors is the default mode), GAB-2/GAB-4 (the shell's `Ref<any>` channel, never operative in any layout). This corpus adds no new BLOCKER; its strongest new material is the zero-length-operand detonation (MP-1) and the order-dependent mix (F-1), both MAJOR, both previously unbanked.

## Ruled events (the adjudication's actual work)

| event | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **MR-24's dedupe limb** | D-7 CONFIRMED, folding to MR-24 verbatim ("always mints an identically-named palette"; "five clicks make five rows") | missed #3: the store DOES dedupe; MR-24's dedupe limb is false | **reader-LC WINS — banked-row CORRECTION booked** | My read of usePaletteStore.ts:61-81: `colorsMatch` (length + pairwise `css`) and `createPalette`'s find over `p.isLocal && name-case-insensitive && colorsMatch` → splice + unshift + `updatedAt` bump + **return existing**. Five clicks on one mix = ONE row, silently re-ordered. MR-24's surviving limb (Save emits into a void — no pending/success/failure register, return value discarded) is the real defect and is *worsened* by the correction: created vs silently-reordered are now indistinguishable. Reader-DU's D-7 proof cell repeated the banked false sentence without re-deriving it — a **citation-inheritance event**, recorded in DISSENT (the ColorSpaceSelector addendum item 3 class). |
| **The dock-dispatch throw** (reader-LC missed #2) | not filed | MAJOR: `startMix` is the one exposed member that throws, and its dock dispatch (usePaneRouter.ts:221) sits outside the ErrorBoundary | **ADMITTED, RESCOPED to MAJOR-latent — a sequencing constraint on X.W4.d** | My reads confirm every limb: `<nav class="dock-band">` + Dock close BEFORE `<main>`; ErrorBoundary is inside `<main>` wrapping only `.pane-container`; `writeClipboard` never rejects (banked byte-fact) and `clearSelection` cannot throw, so `startMix` is indeed the sole throwing member. But the throw is UNREACHABLE from the dock today: desktop, `colorPickerRef?.actionBarContext` is non-null on every mix layout so `ActionBarLayer v-if` always wins and the generic bar never renders (Dock.vue:153-157, my read — banked GAB-4); mobile, the generic bar renders but `mixPaneRef` is written ONLY by `onDesktopRightMount` and the mobile PaneSlot binds no `:on-mount` (App.vue, my read), so `?.()` masks every dispatch. The row's value is prospective: **the moment X.W4.d cures GAB-2 by binding refs, it arms an out-of-boundary throw** — the identical bad-operand mix becomes announced-and-recoverable from the in-pane button and a silent Vue-internal console error from the dock button. X.W4.d must land the guard (or boundary rescope) in the same change. |
| **The zero-length detonation** (both readers' missed #1) | MAJOR: the strategy control chooses between silent-empty and uncaught-throw | MAJOR: + the `!` assertions defeat `noUncheckedIndexedAccess`; + Mix's own Save manufactures the operand | **MERGED and CONFIRMED as MP-1 (MAJOR) — one identity, both limbs adopted** | Re-derived by this seat from mix.ts:69-125: `resultLength = discard ? minLen : maxLen`, so the `resultLength === 0` guard (:125) protects only `discard`; under `repeat` a zero-colour operand reaches `colors[0 % 0]` = `colors[NaN]` = undefined; under `distribute`, at i=0: `fracPos = -0, lo = -0, hi = Math.min(1, -1) = -1, t === 0` → `colors[-0]` = undefined — both then read `.css` and throw a raw TypeError (no code, no taxonomy — distinct from MX-4's four *typed* throw sites, registry-swept by both readers and by me). Gate limb verified: tsconfig.base.json:10 `noUncheckedIndexedAccess: true`; mix.ts:80/:88/:97/:100/:101 each carry `!`. Loop limb verified: `mixResult.value.colors` `[]` is truthy at MixPane.vue:44, so Save persists a zero-colour "Mixed Palette" that reappears as a selectable seat (no length filter at MixSourceSelector.vue:246-268; `canMix` counts containers, useMixingState.ts:50-53 — DB-4 subsumed here as the upstream half). |
| **F-1 order-dependence** | not reached | MAJOR, reproduced pass A's probe to 4dp | **CONFIRMED — now on a THIRD independent instrument (this seat's)** | My node run against `dist/subpaths/{color,css}.js`, emulating the exact `mixColorSequence` fold (weight/total, equal weights): `#ff0044+#22cc88+#4400ff` in **oklch/shorter** → fwd rgb (244.3, 37.5, −2.2) vs rev rgb (87.9, 110.5, 274.4), **max channel delta 276.6** — different colors entirely; same set in **hsl/shorter** delta 225.6; in **oklab** (the shipped default) delta 0.00 exactly. Sharpening this seat adds: order-dependence is *specimen-dependent* in the polar spaces (my `red+yellow+blue` hsl case cancelled to 0.00 by hue symmetry), so any born-RED gate must pin an asymmetric specimen set. Reachability: `INTERPOLATION_SPACES[0]` = oklch (my read of color-space-meta.ts:26-27), `hueMethod` defaults `shorter` — one dropdown away from the default. |
| **Severity of DD-1** | returned MAJOR (pass D filed BLOCKER) | — | **MAJOR upheld** | The structural half (the `vj-morph` family's own `--vj-morph-collapse`/`-expanded` height facility exists — animations.css:122/:131/:135, my read — and is unset by every mix consumer, grep → 0) is source-certain; the +86.7 px/−44 px container magnitudes are live-only residue. A defect whose certain half is "the swap is snapped rather than narrated" grades MAJOR. |
| **DD-3's mechanism** | sharpened against both pass A D-4 and its own DD-3: no element is viewport-height; the dual grid row is sized by the TALLER column and `h-full` propagates it | — | **ADOPTED — and the band gate is sharpened once more** | `.pane-container` declares NO height (shell.css:59-77: grid, max-width, margin, `max-height: var(--content-max-h)`, `min-height: 0`) — K-D4-MECHANISM verified. The dual grammar is gated on `@media (min-width: 1024px) and (min-aspect-ratio: 1.1)` (shell.css:129, foundation.css:471-476) — K-DD3-PI verified — **plus a limb neither reader cited: the layout witness is the JS-stamped `[data-layout]` attribute from `isDesktop` (the same width-AND-aspect query, App.vue), with the CSS media arms as un-media'd defensive witnesses (shell.css:98-125)**. A π that samples widths without the aspect term verifies the fix in the band where the bug does not exist. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Original ids for life (anti-rename). Wave routes follow the banked bounds re-read at wb-mix-animationcanvas.md's instruments block: X-W6.j holds MixPane + MixConfigBar + MixSourceSelector + MixAnimationCanvas.vue + useMixingAnimation.ts; X.W4.g is trigger-gated (impostor-seat carve `:168`/`:215` + face-only MixResultDisplay); X.W4.d holds the shell dispatch; X-W7 the export/strip units; X-W1 CI e2e; **no X wave holds mixStage.ts, useMixingState.ts, MixResultDisplay.vue beyond the face carve, ErrorBoundary.vue, or demo/palettes/mix.ts** — those route MX-CLUSTER / NO-WAVE-OWNER for the next formation boundary.

### Folds-by-reference — banked identities, NOT re-booked (19)

- **C-1 ≡ D-5(A) ≡ DC-2's premise** → **MX-1 + MX-3 + ES-15 + PreviewRamp R-1/R-11** (BLOCKER at home): dead colors-mode add faces, anchor never renders, fabricated target. Routes stand: MX-CLUSTER + X.W4.g (trigger-gated) + X-W6.j (honest settle).
- **DC-3 ≡ K-D8's corrected framing** → **GAB-4 + GAB-10 + GAB-17**: the dock action set never operative in ANY layout; no reachable Clear; `copyResult` is dead code → X.W4.d.
- **F-9 + C-3/D-1(L)** → **GAB-2** (BLOCKER at home) + MC-20/MX-19 (no `disabled` on dock rows) → X.W4.d.
- **DB-2** → **PC-11 + R-6** (button-in-button; zero of PaletteCard's 16 emits wired) → X-W7.c.
- **DD-5** → **MX-41** (MAJOR-latent, nameless 16 px hover-only remove) → X-W6.j / X.W4.g.
- **F-3** → **MR-7** (MixResult optional bag; `[]` truthy) → MX-CLUSTER.
- **D-2(C) ≡ MX-2** (out-in defers the anchor on every re-mix; fatal to all three corpus cures; PIN 3 constrains any cure to an anchor OUTSIDE the Transition) → MX-CLUSTER.
- **D-1(C)** → **MX-31** (banked ESCALATED: retained-composite smear, not bounded waste; `onDeactivated` = 0 re-verified) → X-W6.j (gate j2). The watcher-totality + lying-comment limbs ride the same row.
- **F-2** → **MR-13/GAB-17** (two clipboard paths; shell dispatch dies, plate seat sole owner) → X.W4.d.
- **B-1/C-2(L)** → **MX-1 + MX-6 + MX-39** (JSON through the DOM; catch mislabelled) → X-W6.j/X-W7 (writer), MX-CLUSTER (reader).
- **F-6** → **MX-20** ≡ ColorSpaceSelector L-14 (tsconfig↔exports drift) → banked NO-WAVE-OWNER (W8.md:112 declined; "another owner").
- **F-8** → **MC-14** (frozen 4.0.0 tarball; drift-as-contract-divergence KILLED at MC-K8 — the delta is private `_2` declarations) → X-W1.
- **F-11** → **MR-12** (export/ test-only; 12 files, see K-F11-COUNT) → X-W7.
- **F-13** → **MR-33** + SwatchHoverMenu K-5 (demo/ui alias layer; census corrected: 17 one-line bare-root, input via `/forms`) → W-HYGIENE fold.
- **F-5** → **MC-18** (17-wide space type, 9 offered; latent) → X-W6.j rider.
- **F-16 ≡ DC-7 ≡ D-24** → **MC-27 + MR-31** (dead `computed` import, MixPane.vue:2 — re-verified: one occurrence, the import; "dies free in whatever wave opens MixPane.vue, never minted as a row"). Five passes + two banked rows now carry this one dead import; **it must not be booked a sixth time**.
- **B-5** → gates-blind identity (no `noUnusedLocals`, no `strictTemplates` in any tsconfig — my grep) ≡ MC-K7 + MR-28's enforcement family → banked.
- **E-1-as-rescoped** → **MR-28 ≡ MX-35 ≡ ColorSpaceSelector L-5(a)** (+ 8 more banked cross-refs): the demo has effectively no live import-boundary enforcement; the headline quantifier and novelty claims die (K-E1-a/b below) → banked NO-WAVE-OWNER; re-pointing already inside MT-AU1 §3(9)/W8.
- **DD-6/DD-S2/DC-1's symptom** → **App.md D-8** (viewport-frame; mobile document-scroll = a RECOMPOSITION with a born-RED gate, never overflow-deletion) — the Mix limb rides that identity.

### Booked/rescoped corpus rows (12)

- **DD-2 — CONFIRMED (MAJOR).** Both in-plate captions ride raw `text-muted-foreground` (MixSourceSelector.vue:119 "Selected"; MixResultDisplay.vue:58 "Result") on `--well-bg` — a ground DARKER than the header's (`card 92% + foreground 8%`, foundation.css:328, my read) — while the certified `--ink-muted` rung ships and is consumed by 10 files including the sibling PaneHeader/EmptyState (my census), and PaneHeader.vue's own comment retires the raw token BY NAME ("never raw `--muted-foreground`, which measured 4.29:1"). Registry-swept: MR-16 covers :58's FAMILY/STYLE/WEIGHT only — the ink limb was unbanked; AP-13 banks the identical mechanism on a sibling pane (corroboration, not a fold). The 3.19:1 figure is live residue (R-MP1). Cure: the two token swaps, or the producer label rung that discharges DB-3 with it. → **X-W6.j** (:119) + **MX-CLUSTER** (:58, rides MR-16).
- **DD-3 — CONFIRMED (MAJOR, as-rescoped in Ruled events).** One `h-full` pair (MixPane.vue:61-62), two designs: below the width+aspect band a content-hug; at/above it the plate stretches to the taller sibling column and manufactures the void. π law: **sample the aspect boundary, not just 1023/1024** (a 1080×1750 portrait slab never runs the dual grammar). → **X-W6.j**, chassis vehicle forbidden (K-CHASSIS).
- **DD-4 — CONFIRMED (MAJOR).** The disabled primary verb is opacity-only in forced colors — producer bytes extracted by this seat: `.button:disabled, .button[aria-disabled="true"] { opacity: var(--opacity-disabled); cursor: not-allowed; }` + `.button:disabled { pointer-events: none; }`, no border/colour/weight/glyph delta; the demo's tier-1 roster (foundation.css:679-694) exempts colour-display surfaces only. Consumer site MixConfigBar.vue:162-170 (`:disabled="!canMix"`, no `aria-describedby`, no requirement copy — my read) — compounded by the arity lie (empty rack renders one silhouette, `MIN_COLORS = 1`, verb needs 2). **Cure hardening adopted**: `pointer-events: none` makes every hover-borne cure unreachable — the requirement line must be a PERSISTENT non-hover surface. → **X-W6.j**.
- **D-3 (pass A) — CONFIRMED (MAJOR).** The result is never invalidated: useMixingState.ts imports `{ ref, computed }` only (my whole-file read — no `watch`); `mixResult` is written only in `startMix` and nulled only in `reset`/`clearSelection`; changing space/hue/strategy/operands/mode leaves a stale result enabled, copyable, savable (MixPane.vue:112 `v-if="mixResult"`, :49-55, :38-46). Unbanked across all 178 adjudicated rows (readers' sweep, spot-confirmed). → **MX-CLUSTER** (useMixingState.ts, NO-WAVE-OWNER).
- **DD-1 — CONFIRMED (MAJOR, structural half; magnitudes live-residue).** The ghost→content swap is snapped: `--vj-morph-collapse`/`-expanded` default `none` (animations.css:122/:131/:135) and no mix file sets them (grep → 0, mine); the family's own height facility is declined at the exact site built for it. Chassis limb of the cure KILLED (K-CHASSIS); the surviving cure is the two custom properties + reserved result-region geometry under Mix's own routed scene (W6.md:17's ratified region requirement). → **MX-CLUSTER** (MixResultDisplay Transition) + **X-W6.j** rider.
- **D-7 (pass A) — CONFIRMED-AS-CORRECTED (MAJOR).** Save is silent: no emit, no status ref, no template reaction, the returned `Palette` discarded (MixPane.vue:38-46); `vj-celebrate` — the house one-shot feedback family — is consumed by ColorInput/ActionFeedback/AdminUsersPanel and never by mix (my grep). **The "always mints"/"five rows" limb DIES** (see Ruled events): the store dedupes and silently reorders, which makes the zero-feedback defect *sharper* — three outcomes (created / silently-reordered / would-fail) collapse into zero signal. → **X-W6.j** (MixPane.onSave) + the MR-24 correction below.
- **F-1 — CONFIRMED (MAJOR; third instrument).** N-ary mix is order-dependent in the polar spaces (numbers in Ruled events); operand order = click order; no order semantics declared anywhere in the UI. Cure adjudicated separately from the defect (ColorSpaceSelector addendum item 2): the fold is a correct weighted average in cartesian spaces — the defect is offering hue-arced spaces through an order-sensitive fold undeclared. Gate rider: pin an ASYMMETRIC specimen set (my hsl symmetry-cancellation finding). → **MX-CLUSTER** (demo/palettes/mix.ts, NO-WAVE-OWNER).
- **E-2 — CONFIRMED (MAJOR).** The injection Symbol is co-homed with the god provider: a value import of `LIBRARY_PORT_KEY` drags 29 modules / 107,417 B / 34.2% of MixPane's static closure (admin API + auth + transport) — reader-LC reproduced pass E's walker byte-exact at HEAD; the in-repo counter-proof (`demo/color-session/keys.ts`, 1 module / 1,474 B, type-only edges) is consumed by the SAME file one line later (MixPane.vue:10 vs :11). Cure shape (a leaf `demo/palettes/ports.ts`) is already scoped for a sibling port at AdminUsersPanel MT-AU1 §3(7). → **NO-WAVE-OWNER** (usePalettePorts.ts in no X bounds).
- **E-3 ≡ D-3(C) — CONFIRMED (MINOR).** One key, two contracts in a parent/child pair: `inject(LIBRARY_PORT_KEY)!` (MixPane.vue:16) vs `inject(...)` + `pm?.savedPalettes.value ?? []` (MixSourceSelector.vue:33-34, my reads); provider unconditional above every pane (sole `providePalettePorts` call site) — the child's guard is dead masking with a divergent failure shape (parent TypeError vs silent "No saved palettes yet" for a wiring fault). → **X-W6.j**.
- **E-4 — CONFIRMED (MINOR).** MixPane is a courier for `CSS_COLOR_KEY`: injected at :15 solely to prop down at :84; the child receives `cssColorOpaque?: string` and guards with silence (:69-73) — a required dependency made optional in transit, against the project's own injected-not-prop-drilled law. → **X-W6.j**.
- **F-4/D-2(L)/E-5 — CONFIRMED (MAJOR).** The library publishes two incompatible failure algebras (`Result.error` vs `ParseResult.diagnostics`), the demo erases both through throwing adapters 8 lines apart (picker-color.ts:104-112, my read), and **no combinator — not `ok`, not `err`, not any unwrap — reaches ANY of the seven published subpaths** (reader-LC verified all seven; E-5 had read one). 18 hand-rolled unwrap adapters in demo (11 non-test), four wrapping `mixColors`, two on this pane's own chain (banked MX-15/MX-4 carry the throw sites). The library limb — publish the algebra's consumption surface — is library-owned: → **NO-WAVE-OWNER** (X-W9 owns only parser R1; this is adjacent, not identical).
- **F-14 — CONFIRMED (MAJOR).** `test/` imports demo source (10 files; test/mix-v4.test.ts:3-4) and `test/` is typechecked by NO tsconfig program (reader-LC's grep census; coverage half banked MX-14). → coverage: **MX-CLUSTER**; program membership: **NO-WAVE-OWNER**.
- *(MINOR/INFO tail, booked with one line each)* **B-2/D-5** — mix.ts has zero importers in its own directory (census exact: MixConfigBar type-only + useMixingState + one test) → NO-WAVE-OWNER. **F-10 + F-12** — PaneHeader's unscoped block defines its parents' scroll host (comment admits the inversion verbatim); the 9-pane chrome literal drifts and Mix's variant alone adds `relative` — which MX-44 proves load-bearing and nothing asserts → NO-WAVE-OWNER (shared surface). **DC-5-as-killed-in-part** — the surviving geometry divergence (plate `rounded-xl` 12px vs `.dashed-well` `var(--radius-card)` 16px inside one fixture register; `bg-well` itself is the DESIGNED consumer, foundation.css:325-328 names this plate) → MX-CLUSTER rider on MR-22. **DC-6** — the pane root duplicates its only child's box; `mx-auto` is a no-op on `w-full`; the load-bearing `relative` is the Card's → X-W6.j (dies free). **DD-8** — the palette result states the same colors twice adjacently (dots :92-106, strip :109-116, my read); implementation duplication banked (MR-17/PCS-10), composition banked (PCS-19); the redundancy-as-information row is the unbanked contribution → X-W7.c coupled. **DD-9 (INFO)** — the mode tabs are the only `justify-center` in the four-file tree (:104, sole hit) against a full-bleed column → X-W6.j rider. **DB-4** — subsumed into MP-1 as the upstream gate half.

### Re-reader finds admitted (7 — MP ids minted; these had no corpus id)

- **MP-1 — MAJOR** (merged, both readers): the "Size mismatch" control is a user-facing choice between a silent wrong answer and a pane-destroying crash — full derivation in Ruled events. Graded MAJOR not BLOCKER: the precondition (a zero-colour palette resident) is proven representable (MR-7/MR-24 + CurrentPaletteEditor A-2(ii)) and manufacturable by this pane's own Save, but not proven on a first-time user's path. Cure must be totality in `getColorAtIndex`/`mixPalettes` (honest guard or typed refusal), never a consumer try/catch (masking-fallback ban, W6.md H3's standing rider). → **MX-CLUSTER** (demo/palettes/mix.ts, NO-WAVE-OWNER) + a `!`-audit rider on the same file (the gate-defeat limb).
- **MP-2 — MAJOR** (reader-DU): under PRM the pane's one verb produces a below-fold DOM insertion and NOTHING else — zero motion (by design, useMixingAnimation.ts:120-125), zero live region (grep 0, mine), zero focus move (grep 0, mine), and the narration is contractually the sole progress signal (MixPane.vue:106-110). Every pass filed the PRM fast path as a negative proof to preserve and the announcement gap as a separate row (banked MX-8/MR-11); **none joined them: the status region is the ONLY channel the PRM branch can ever have.** Cure-coupling constraint: any cure preserving the fast path ships the announcement in the same change. → rider ON **MX-8 (MX-CLUSTER)** + **X-W6.j**.
- **MP-3 — MAJOR-latent** (reader-LC, rescoped by this seat): the dock dispatch of `startMix` sits outside the application's only error boundary — unreachable today, ARMED by X.W4.d's own cure. Full ruling in Ruled events. → **sequencing constraint ON X.W4.d** (booked beside GAB-2's route so the wave cannot land the ref-binding without the guard).
- **MP-4 — MINOR-latent** (reader-DU): the add-slot ghost is doubly enrolled in the forced-colors tier-1 exemption (`.swatch-row > *` via its TransitionGroup parent, `.watercolor-swatch` via the producer root — roster verified foundation.css:679-694, my read), so the ONE affordance out of the empty state keeps its own sub-threshold ink (D-13's 1.52:1, live-residue) and is contractually prevented from WHCM rescue. Corrects pass C's mis-attribution ("that is the DC-2 family's fault") — the cause is the consumer's roster line. The same membership is banked as a superlative for the result dots (wb-mix-resultdisplay sup. 6) — both rulings are right; membership is per-surface. → **X.W4.g / X-W6.j** rider.
- **MP-5 — MINOR-latent** (reader-DU): the chip remove button carries `z-popover` (MixSourceSelector.vue:153, my read) — a portalled-overlay rung (`--z-popover: 130` in the producer ladder, my extract) on an in-well control; second z-tier misuse in this feature (MX-43 banks the canvas's), and MX-41 adjudicates this exact button without the rung. → rides **MX-41** (X-W6.j / X.W4.g).
- **MP-6 — INFO** (reader-DU): `mode="out-in"` on MixPane.vue:111 is dead configuration — a bare `v-if` with no `v-else` never has two children to sequence, and a re-mix reassigns `mixResult` without a vnode-type change so no transition runs at all (re-derived by this seat); the parent's comment narrates an ordering produced entirely by the `:ghost` prop and the CHILD's Transition — where the identical attribute is fatal (MX-2). → **X-W6.j** rider (dies free).
- **MP-7 — MINOR, CORRECTION TO BANKED MR-24** (reader-LC): the dedupe limb of MR-24 is false at source (ruling above). MR-24's no-register limb survives and is the row X-W6.j executes against; **the "five rows" sentence may not be quoted as fact by any successor**. Recorded under the anti-rename law as a correction, not a new booking; wb-mix-resultdisplay.md remains immutable — this file is the addendum beside it (E-3 discipline).

## Killed-claims register — all 16 reader kills UPHELD; 0 kills killed

Reader-DU's nine: **K-CHASSIS** (the corpus's eleven-times-repeated "compose Mix on InstrumentChassis" cure — W6.md:17/:337/:457 verbatim, my read: CC-108/A-17 killed the chassis, gate H1 forbids rebuilding the housing; chassis-fitness.md's 3/3 ADOPT was superseded; the REGION argument survives ratified — every "dissolves under the chassis" cure must be re-derived). **K-DD11** (canvas aspect squash impossible: `canvas.style.height = h` is written from the same `scrollHeight` BEFORE the bitmap sizing, useMixingAnimation.ts:137-143, my read — CSS box and bitmap are the same quantity by construction). **K-DC1-ATTRIBUTION** (the shell forbids document scroll — shell.css:19-27 `height:100dvh; overflow:hidden`, my read; deleting the Card's `overflow-y-auto` strands content; banked App.md D-8 concurs). **K-D4-MECHANISM** (nothing in the chain sets a height; the plate is sibling-height, not viewport-height — my shell.css read). **K-DC5** (partial: `bg-well` is the designed consumer by the token's own doc-comment; the geometry divergence survives, re-filed). **K-DD3-PI** (the band gate is width AND aspect — both media arms + the `[data-layout]` stamp, my reads). **K-DC2-CURE** (the one-line wrapper move sits INSIDE the out-in Transition — MixResultDisplay.vue:63 under :60, my read; banked MX-2/PIN 3 rules it fatal). **K-D25** (retiring the pane's only scroller under the shell's overflow:hidden strands content — same unsound move as DC-1's cure, opposite direction). **K-D8-MECHANISM** (Dock.vue:153-157 `v-if` chain, my read: the generic bar never renders on desktop; corrected disposition = DELETE the dead path, consistent with GAB-17).

Reader-LC's seven: **K-E1-a** (the "match ZERO files" headline — eslint.config.js:232-239 LEADS with two live `demo/color-picker/**` globs, my read; already killed thrice on the fleet record). **K-E1-b** (the novelty claim — the identity was banked at ColorSpaceSelector L-5(a) + ≥10 cross-refs before this corpus was read; E-1 folds, never headlines). **K-F13-BARREL** (19 dirs / 29 lines; alert/index.ts is 11 lines; input imports the `/forms` SUBPATH — falsifying "each single-line from the ROOT barrel"; banked K-5/K-12 concur). **K-L-VERDICT** ("no working entry point" — the palettes arm is a native `aria-pressed` button with real stamps, MixSourceSelector.vue:246-268, my read; banked K-2 twice; C-1's colors-mode phrasing stands, the generalisation does not). **K-F4-DOCK** (`copyResult` cannot fail — `writeClipboard` returns a Result and never rejects, banked byte-fact; the member that throws through the gap is `startMix` one line above → corrected into MP-3). **K-F11-COUNT** (twelve files, not eleven — types.ts omitted; `ls` re-run by this seat → 12; finding exact, count erratum). **K-N-E2** (`dist/gh-pages` does not exist at HEAD — my `ls`; the 698-byte figure may not be quoted as a standing defect; the CARRY-LEDGER §F build-lane reproduction duty stands).

## Superlatives — provenance preserved

1. **The ONE-CLOCK law, stated as a falsifiable invariant and kept** (useMixingState.ts:1-16/:79-106: no timers, one forward edge per transition, one-line re-entry guard) — all eight corpus passes + the banked arbiter + both readers + this seat's whole-file read concur.
2. **The PRM inversion, reasoned in place** (useMixingAnimation.ts:29-36/:116-125: `respectReducedMotion: false` BECAUSE the loop carries the phase machine's only forward edge; `arm()` completes-immediately) — the correct inversion of the repo's own ~40-loop PRM-RAF epidemic; the banked MX-38 rescope proves blind substrate adoption would re-mint the bug. *MP-2 is the design consequence this excellence carries — the praise and the announcement blocker are one mechanism.*
3. **Excision-not-reminting of a cascade-dead ring** (MixSourceSelector.vue:134-145: probe date, general law, deletion) — the correct response to a dead style, documented against re-addition. (reader-DU; verified in my whole-file read.)
4. **The de-emphasis law written three lines from a compliant consumer** (MixSourceSelector.vue:186-189: "'quieter' and 'illegible' must never collapse") — exactly right; aggravating context for DD-2 and banked MX-42, not a retraction.
5. **Two redundant surfaces removed with reasons left at the site** (:117-118, :269-270) — subtraction with stated rationale. (reader-DU; verified.)
6. **K-PALID identity discipline across the seam** (slug-keyed selection both files; `aria-pressed` not `aria-selected`; slug dedupe on palettes, deliberately not on colors) — reader-LC + pass D + this seat.
7. **The guard the pane needs already ships three files away, and the C axis found it** (sample.ts:58-65 try/catch around the SAME `parseColorIn`, read by the SAME pane's config bar — my read): converts "missing technique" into "declined one" — the strongest single paragraph in the corpus (challenge-C-implementation.md:304-316).
8. **The animation module dogfoods the library with zero hand-rolled math** (mixStage.ts pulls lerp/easing/mix from published subpaths; the ramp samples the same space+hue the math ran) — banked sup. 1; endorsed.
9. **The corpus's epistemics**: byte-identical predecessor preservation with shasum receipts, up-front probe-method gap declarations, a mid-seat error recorded against itself, and pass E withdrawing its own intended upgrade with the prior-pass evidence that killed it (N-E1) — the behaviour the program exists to produce. So is reader-LC's baseline-law note and reader-DU's kill of its own axis's eleven-times-repeated cure.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-MP1** — DD-2's 3.19:1 on both captions over `--well-bg` (direction certain — darker ground than the 4.29:1 header — figure needs a composited read; AdminTagsPanel/ApiOfflineChip precedent).
- **R-MP2** — DD-1's container timeline (+86.7 px in two jumps, −44 px card top, second jump on the settle frame).
- **R-MP3** — DD-4's forced-colors computed set (`opacity 0.5`, `color rgb(96,0,0)` — engine-dependent remap; the opacity-only producer recipe is byte-proven).
- **R-MP4** — DD-10 (SegmentedTabs selection-vs-focus collapse in forced colors; the axis's own HYPOTHESIS label carries; one frame with focus applied decides it).
- **R-MP5** — DC-4's 7.58:1 painted-area inversion (composition asymmetry source-certain; px² live-only; chassis cure limb dead).
- **R-MP6** — the tap-target rows no banked row carries: SegmentedTabs 27.5 px, Collapsible trigger 26.3 px (the SelectTriggers/verb are banked MC-3; the plate seats MR-14).
- **R-MP7** — D-13's add-ghost composited contrast (1.52:1 light / 4.44:1 dark) — load-bearing premise of MP-4; re-measure before executing that row.
- **R-MP8** — D-14's dark-well modal colour (ambient-seed-dependent).
- **R-MP9** — the out-in deferral magnitude: pass D measured 232-242 ms and attributed `--duration-morph: 0.3s`; banked MX-2 rules `--duration-fast` (~200 ms). Mechanism certain and banked; the magnitude is contested on the record and needs one clean re-take.
- **R-MP10** — challenge-C's getImageData centroid (255,491) vs true destination (64,653) — mechanism = MX-1, banked; frame not re-taken.
- **R-MP11** — the RED e2e run (locators statically unsatisfiable per MX-3 — entailed, not re-run; CI limb banked → X-W1).
- **R-MP12** — the C-1 live crash table (`oklch()` → TypeError) — throwing chain source-certain (MX-4/parser R1, X-W9); the in-page table and teardown snapshot were not re-taken; a prior seat's shared-browser mutation (R-2 at the canvas record) stands as the caution.

**Provenance note** (adopted from reader-DU's ledger read, not re-derived): all three wb-mix-pane axes are EXISTS-ORIGINAL; motion-quarantine carries no row for this component (re-verified by my grep — one unrelated PaneHeader sentence), so no M-xx FALSE row is carried anywhere in this record.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet).** Reader-DU's D-7 proof cell quoted banked MR-24's "five clicks make five 'Mixed Palette' rows" as confirmation; reader-LC read usePaletteStore.ts and found the store dedupes; this seat's own read settles it for reader-LC. A banked row is a citation, not a derivation — the exact class the ColorSpaceSelector DISSENT recorded (the 700-hardcode cell), now observed once more. Reader-DU's OTHER corrections on the same return (the DD-3 mechanism sharpening against its own axis, K-D4) were genuinely its own — partial vindication, same shape as worker-F's record there.
2. **Severity ordering (unresolved, preserved).** Reader-DU grades the DD-2 regression MAJOR on provenance ("the remediation landed 100 px away and never reached the darker ground"); reader-LC did not rank it. This seat preserved MAJOR without ruling on whether provenance aggravates severity — the cure is identical either way.
3. **Reader-LC's scope discipline exceeded its brief once**: its baseline note verified `origin/master` predates the whole mix workbench — useful, adopted — but its F-8 severity re-framing ("banked-downgraded to a non-reproducibility hygiene fact") relies on MC-K8's 53-symbol census which this seat did not re-run. Carried as banked, flagged as un-re-derived here.
4. **What no seat has yet done**: no pass, reader, or arbiter has run the palettes-mode mix under a screen reader, and MP-2 makes that the single highest-value live probe this component has left.

## Closing verdict

**DEFECTIVE — adjudicated, banked-coherent.** The component's blocker mass was already banked (MX-1/MX-3/GAB-2 and their cluster); this apotheosis adds no blocker but books two previously-unbanked MAJOR mechanisms in the pane's own math (MP-1's zero-length detonation; F-1's order-dependence, on a third instrument), joins the corpus's most-praised path to its most-cited gap (MP-2), corrects one banked row against source (MR-24's dedupe limb), arms one wave with the constraint that keeps its own cure from minting a silent failure (MP-3 → X.W4.d), and upholds all sixteen reader kills — including the death of the corpus's eleven-times-repeated chassis cure, whose region argument survives it ratified. Fold source intact: original ids for life; wb-mix-resultdisplay.md and wb-mix-animationcanvas.md remain immutable beside this file per E-1/E-3.

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file.
