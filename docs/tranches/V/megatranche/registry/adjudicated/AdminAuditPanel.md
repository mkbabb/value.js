# AdminAuditPanel — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed from this seat's own runtime, not inherited from the spawn label

**Seat**: the fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code), each of which re-read the full challenge corpus against the tree before returning.
**Date**: 2026-08-03 · **HEAD**: `64aca0a2` (branch `tranche-u`; subject files byte-unchanged since the axes' read HEAD `e39da983` — `git log e39da983..HEAD -- AdminAuditPanel.vue useAdminAudit.ts` → empty, re-run by this seat).
**Corpus**: `audit/components/AdminAuditPanel/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md` + `evidence/` (20 PNG) + `pi/` (probe suite + log). **Provenance, verified by this seat**: NO `challenge-*-r2.md` exists for this component (directory listed), so the three r1 files govern their own axes; all three are EXISTS-ORIGINAL in `registry/HYDRATION-LEDGER.md` (hashes sighted); `AXIS-PROMOTIONS-2026-08-03.md` carries **zero** AdminAuditPanel rows; `codex-provenance/motion-quarantine.md` carries **zero** AdminAuditPanel rows, so no M-xx FALSE row constrains anything here and **no motion defect may be carried** (the component's `transition-colors duration-fast` residue is ruled by M-17/M-26 — challenge-D's INFO-1 declined to file it, correctly).
**Method (L-18)**: both re-reader returns were attacked before either was adopted. Every kill was re-derived against the tree or the producer dist by this seat's own instruments; every load-bearing confirmation was spot-verified at source; two of the re-readers' own evidence cells were refuted and are recorded as kills K-8/K-9 below. No browser was opened (probe parsimony): every ruling here is decidable from source, installed producer bytes, the committed probe log, and `REPORT.json` — pixel magnitudes that are not are named UNPROVEN-NEEDS-LIVE and become visual-audit inputs.

**Arbiter's own instruments this session (all static, each re-runnable)**: whole-file reads of AdminAuditPanel.vue (120 L), useAdminAudit.ts (103 L), EmptyState.vue (105 L), dateFormat.ts, audit-log.ts, service/audit.ts, api-problem.ts, PaginationBar.vue (head), AdminListItem.vue (head), client.ts:128-158, types.ts:110-140, AdminFlaggedPanel.vue:37-52, AdminPane.vue:59-70; `pi/probe-output.log` whole; producer bytes: `Button.vue.d.ts` (no `variant`; `ButtonSize` includes `"xs"`; `iconOnly` documented), `badge/index.d.ts` (VARIANT/TONE/SIZE/SURFACE), badge chunk base class string (**includes `badge-atom`**), `styles/glass/glass-atom.css` badge rules, `styles/tokens/{light-dark,sizing}.css` coarse block; counting: `emitAuditEvent(` call sites outside audit-log.ts = **18**, of which **0** pass `ipHash`; `loadAuditLog` call sites = **6, all zero-arg** (SFC :30/:49/:115/:119 + composable :76/:83); `if (!token)` = **21 across 5 composables** (flagged 3 · tags 2 · colorNameQueue 5 · adminUsers 10 · audit 1); `after:|before:` in demo/palettes = **0**; `instanceof ApiProblem` in demo/palettes = **1** (useSlugMigration.ts:83); `variant="outline"` in browser/admin = **17** (incl. PaginationBar.vue:8/:22, which no axis named); `h-7 px-2` = **13**; `emphasis=` in demo = **2** (see K-8); `grep -rln useAdminAudit\|AdminAuditPanel test/ demo/test/` = **0**; e2e: ADMIN_VIEWS = users/names/flagged (audit absent), populated-fixture routes `/admin/audit` at :170 with seeded AUDIT (which **invents `ipHash` values the real server never sends** — the type lie propagated into the fixture), gotos = :19/:57/:98 only; `demo/@/` refs in e2e = **10**, `demo/@` does not exist; REPORT.json `/#/admin/audit` smallTapTargets extracted — `input 160×23` + 22×22 `Switch to slug` / `Generate new slug` / `Cancel` (PaletteSlugBar, **not dock**); `export.ts` (4,184 B) beside `export/` (12 modules, **no index.ts**), `usePaletteExport.ts:9 from "./export"` → the FILE. Routing authorities read: `registry/CARRY-CUT-LEDGER.md` §0/§1, X wave specs W3/W4/W5/W7/W8 (admin surface ownership derived below).

## Scoreboard

**34 rows CONFIRMED (4 BLOCKER · 14 MAJOR · 14 MINOR · 2 INFO) · 9 kills sustained in the register (7 from the re-readers, all re-derived; 2 NEW against the re-readers' own cells) · 11 missed-by-the-axes findings adjudicated (all confirmed; one demoted MAJOR→MINOR, two folded into sibling cures) · 9 rows NO-WAVE-OWNER at the X boundary.**

Blocker spine: the audit ledger fails as a *ledger* four independent ways — it lies to strangers (AAP-3), lies over its own stale rows (AAP-2), cannot survive its own guaranteed inputs (AAP-1), and cannot answer *who* (AAP-4). Every one is decidable from source; two are the exact species (`error ≠ empty`, `min-w-0`) the repo has already cured elsewhere and annotated, which is what makes this corpus damning rather than merely long.

## Challenges to the re-readers (the incredulity seat's actual work)

| target | challenge | RULING |
|---|---|---|
| reader-1's corroboration cell "`grep -rn "emphasis=" demo/` → 0 hits" (missed-1) | re-run by this seat → **2 hits** (PalettesPane.vue:113, AdminUsersPanel.vue:174, both `emphasis="text"`) | **REFUTED as stated** (K-8). The finding survives whole — the subject's :30/:49 `variant="outline"` are dead props on a Button with no `variant` axis — but the decorative repo-wide zero dies. Correct statement: no *admin-panel Button* uses the real axis while all five panels ship the dead one. |
| reader-1's D-20 residue framing ("the hypothesis survives on better evidence — VARIANT.secondary … **no border utility**") | badge chunk base class string read by this seat: `"badge-atom inline-flex items-center …"` — the base **includes `badge-atom`**, and `glass-atom.css` gives `.badge-atom { box-shadow: var(--glass-material-rim) }` + a real border under `prefers-contrast: more` | **PARTIALLY REFUTED** (K-9). The secondary badge is NOT background-only: every badge carries the rim. The forced-colors hypothesis narrows further (identity = bg + box-shadow, both typically suppressed/forced) and stays UNPROVEN-NEEDS-LIVE — reader-2's version of the kill is the accurate one. |
| reader-1's missed-3 grade (badge tone register, MAJOR) | the ledger stays *truthful* without a severity register; the missing-actor BLOCKER (AAP-4) already mandates one anatomy rework, and "the route's primary operator task" is asserted, not evidenced | **DEMOTED MAJOR→MINOR**, folded into AAP-4's anatomy cure (AAP-29). The producer facts (Badge `tone` axis, `TONE.destructive`) are verified and adopted. |
| reader-1's missed-4 (coarse-pointer 28×54) framed as unknown to the record | X-W4 gate **A3** (read by this seat) already cites `AdminAuditPanel.vue:30` verbatim and names both failure modes incl. "28px persists under `pointer: coarse`" | **CONFIRMED but re-homed**: missed by the three axes, NOT by the formation — the row is already RED-gated at X-W4/CC-045. Recorded so the next formation does not double-book it. |
| reader-2's L-4 scope correction (21 sites / 5 composables vs "four-panel hole") | independently re-counted by this seat: 3+2+5+10+1 = **21** | **CONFIRMED — reader-2's count binds.** (The AdminUsersPanel record's 22 was true at `c654824e`; the count is 21 at `64aca0a2`.) |
| both readers' convergent missed finding (filtered-empty) | independence check: challenge-D's state table (:135-142) enumerates seven states and omits the filtered-empty arm — verified verbatim by this seat; the finding is source-decidable | **CONFIRMED as a genuine axis miss** (AAP-6); the convergence of two hostile seats on a decidable claim is corroboration, not inheritance. |

## Adjudicated defect roster

Line anchors are HEAD `64aca0a2`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Routing law: rows land on the X wave that owns the surface (`W7.md`/`W4.md` both carry `AdminAuditPanel.vue` as modify; `W8.md` owns barrels/SCCs/dead attrs/globs; `W3.md` §X.W3.6 owns route-closure auth; `W5.md` owns the pane axis); rows whose surface no X wave names are **NO-WAVE-OWNER** and belong to the next formation boundary.

### BLOCKERS

- **AAP-1 (C:D-1 / D:D-3 / L:L-7) — the hand-rolled row drops `min-w-0` and the grid track blows.** AdminAuditPanel.vue:62 is the byte-prefix of AdminListItem.vue:11 minus the `min-w-0` that AdminListItem.vue:6-10 documents as the S.W5-12/F-1 cure ("blowing the track to ~850px at 390" — the repo's own post-mortem). Every precondition re-verified: grid parent (:2), nowrap `truncate` span (:76), `shrink-0` badge+timestamp (:67/:70), inner `min-w-0` (:64) inert per the atom's own comment until the row is zeroed; guaranteed worst input `count=… slugs=…` up to ~6KB (api batch.ts:54-56, slugs ≤50×120). Pixel magnitudes live-only (§UNPROVEN). **Disposition: BUILD → X-W7** (owns `browser/admin/*.vue`; the cure is the five-homes→one-atom composition — the row-grammar identity from the AdminUsersPanel record's L-6, anti-rename).
- **AAP-2 (C:D-2 / D:D-10a + the pager/count arms both axes missed) — the state chain does not own its terminal surfaces.** :36 `v-if` → :42 `v-else-if` → :56 `v-else-if` and the chain ENDS; the `v-for` (:59), PaginationBar (:83-90) and the toolbar count (:26-28) are siblings. Consequences, all source-decidable + probe-corroborated (`pi/probe-output.log` C-1/C-2): skeletons and the error plate paint ON TOP of retained rows; after a post-load failure the panel renders "The ledger is unreachable." above the stale rows it contradicts while the count asserts a stale/zero `total` (D:D-12, folded here) and the pager stays operable over the dead ledger (`total` written only on success, useAdminAudit.ts:62; PaginationBar gates only on hasPrev/hasNext). The file's own :40-41 comment declares the inverse invariant. C's prescribed cure is KILLED as incomplete (K-5): the terminal branch must own rows + pager + count, and the wrapper needs `min-w-0` or it inherits AAP-1 one level up. Two-member family: AdminFlaggedPanel.vue:37-48 has the identical break (verified). **Disposition: BUILD → X-W7.**
- **AAP-3 (C:D-3 / D:D-2 / L:L-4) — signed out, the ledger reports itself clear.** useAdminAudit.ts:49-50 `if (!token) return;` exits before `loading`/`loadError` are touched → :56 renders `· ledger clear · / No audit entries found.` to an unauthenticated visitor; REPORT.json corroborates (dock at Login, bodyTextLength 259, zero errors — perfect health asserting a falsehood). Class: **21 `if (!token)` sites across 5 composables** (this seat's count). Identity: the second member of AdminUsersPanel L-5's BLOCKER class ("authority costumed as empty"), one identity, never re-booked. **Disposition: BUILD → X-W3.6** (fail-closed `to.meta.admin` router guard + `NotFoundPane`; the 21 per-method guards DELETE behind that gate, per the MT-AU1 G-4b mechanism the X formation absorbed).
- **AAP-4 (D:D-1) — the ledger cannot answer *who*.** Server emits seven fields on every event (service/audit.ts:17-25, format :43-53; audit-log.ts:41 writes `actorSlug` unconditionally, null-coalesced); demo/palettes/types.ts:117-123 keeps five — `actorSlug` and `payload` erased at the boundary; the row paints three (:67/:70/:76); no detail-disclosure region exists. Canon (verified by both readers, verbatim-quoted, consistent with the AdminUsersPanel canon family): OPTICAL-BENCH Admin-Audit row, VISUAL-CONSTITUTION §7, PROPORTION-AUDIT PR-11. `impersonate` currently renders as a neutral pill naming the victim, not the perpetrator. **Disposition: BUILD → X-W7** (row anatomy: actor/scope/effect + disclosure; zero API change — the fields already ship). AAP-5 and AAP-29 fold into this cure.

### MAJOR

- **AAP-5 (reader-2 missed-1) — the client type lies in BOTH directions.** `AuditEntry` requires `target: string; ipHash: string` while the server declares both optional at every layer AND `ipHash` is **never on the wire at all**: 18 `emitAuditEvent` call sites, 0 pass it (this seat's grep); `format()` emits `ipHash: undefined`, JSON drops the key. `entry.ipHash.slice(0,8)` compiles clean and throws. The e2e populated fixture *invents* `ipHash` values (admin-populated.ts:80-82) — the lie propagated into the test estate. All three axes quoted the interface in full and filed only what was missing from it, never what was false in it. **Disposition: rides AAP-4's type rewrite → X-W7.**
- **AAP-6 (both readers' missed, convergent) — filtered-zero costumes as TRUE-EMPTY.** :56 gates on `entries.length === 0` alone; filters are server-side (useAdminAudit.ts:56-57) and Action is EXACT equality over a closed verb set (service/audit.ts:61) — one typo empties the ledger and the panel declares the *world* clear (`· ledger clear ·`), `0 entries` above, no clear-filter affordance. Third costume of the F-2 species (auth arm = AAP-3, count arm = AAP-2's fold); challenge-D's state table inspected the empty state and PASSED it (:139). EmptyState already carries the species split; the predicate must key on `total === 0 && !actionFilter && !targetFilter` with a third message. **Disposition: BUILD → X-W7.**
- **AAP-7 (reader-1 missed-1) — `variant="outline"` is not a prop of glass Button 7.0.0.** Verified at the d.ts by this seat (`emphasis/tone/size/iconOnly/loading` — no `variant`); both subject Buttons (:30, :49) render at default `emphasis="secondary"` with the declared register silently discarded; the panel is right on Badge (:67, real `variant`) and wrong on Button. 17 `variant="outline"` in browser/admin **including PaginationBar.vue:8/:22, which no axis and neither reader named**. Identity: the dead-variant class = AdminUsersPanel L-3 (fleet 51/22 at `c654824e`), already RED-gated. **Disposition: FOLD → X-W8** (scope 5 + the barrel re-point kills the class in the same touch; CC-081 limbs). Reader-1's count cell dies (K-8); the finding stands.
- **AAP-8 (D:D-7 + reader-2 missed-5 + reader-1 missed-6, folded) — two identical pills, two different server laws, and the range the wire already speaks is unreachable.** Action = exact-match over a closed 16-verb vocabulary the UI never reveals; Target = case-insensitive substring; byte-identical chrome (:7-22); labels exist only as placeholder + aria-label (visible name vanishes at first keystroke). AND the whole options surface is dead plumbing: `loadAuditLog(opts)` has **6 callers, all zero-arg**; `after`/`before` are typed, serialized, and implemented server-side with **zero** demo references (this seat's greps) — the cure is *wire it or delete it*, not "add a control". **Disposition: BUILD → X-W7** (Select for the closed verb set + persistent labels + the range decision).
- **AAP-9 (D:D-8) — the empty spacer starves the declared-elastic field.** :21 `flex-1 min-w-[6rem]` (Target, "the wide one") vs :23 `<div class="flex-1" />` — two `flex: 1 1 0%` claimants, one empty; under `flex-wrap` (:4) the void holds line 1 and strands the count+Refresh cluster flush-left on line 2. Mechanism source-certain; width table live-only. **Disposition: BUILD → X-W7.**
- **AAP-10 (C:D-4) — a 200 with the wrong shape throws in render with the error apparatus dark.** client.ts:152-155 throws only on `!res.ok`, else `return res.json()` unvalidated; :61-63 assigns straight through; probe C-8 reproduces both halves (`entries=undefined loadError=null` → render throw). Cure belongs at the ApiProblem boundary, not a `?? []`. **Disposition: NO-WAVE-OWNER** (`demo/platform/transport/` appears in no X wave's file list) — next formation boundary.
- **AAP-11 (C:D-5 / D:D-19) — no request sequencing anywhere on the surface.** No AbortController/token/in-flight guard (:48-71); `finally` clears `loading` on FIRST settle; four overlapping entry points (onMounted, debounce, un-disabled Refresh, pager); probe C-7 shows the stale page-2 response overwriting page 3. Last-writer-wins on an audit record presents the wrong 20 rows under the right page number. **Disposition: NO-WAVE-OWNER** (`useAdminAudit.ts` is in no X wave's file list).
- **AAP-12 (C:D-7) — vacuous gate: the row half has never rendered under any test.** Zero unit refs; the only e2e visit asserts a heading AdminPane owns + one button; ADMIN_VIEWS omits audit; the populated fixture routes the endpoint no spec visits (dead fixture data) — all re-verified by this seat. This is the mechanism behind AAP-1/AAP-2 surviving to HEAD. **Disposition: NO-WAVE-OWNER as a standalone row, with a binding rider: whichever wave cures AAP-1..AAP-3 lands the born-RED audit-route spec FIRST (E-1 twice-audit discipline; C's mutation table is the spec's seed).**
- **AAP-13 (D:D-5) — the audit route declares a palette companion + mobile pane selector three canon instruments order to zero.** Structural, better than the shots: viewSchema.ts:206-214 declares `right: "palettes", rightLabel: "Palettes"` (verified). **Disposition: FOLD → X-W5** (one route, one scene; the pane axis leaves viewSchema).
- **AAP-14 (D:D-4) — four painted edges per row against a boundary budget of one separator between adjacent rows, none terminal.** :62 `rounded-md border border-card-edge` on every row incl. the last; 20 rows = 80 segments vs budget 19; canon triple-cited by both readers verbatim. **Disposition: BUILD → X-W7** (row anatomy), under X-W10's boundary law.
- **AAP-15 (D:D-6) — the declared hierarchy is flat by construction and the badge is a chimera of two type rungs.** Producer bytes verified: `text-small` and `text-mono-small` share `--type-small`; Badge SIZE.md sets `text-[length:var(--control-text)]` while the instance adds `text-mono-caption` — tailwind-merge cannot collapse them; timestamp (provenance) rides the control rung one line above the file's own correct machine-string comment (:74-75). Which rung wins the size is live-only; the chimera is structural. **Disposition: BUILD → X-W7 cure under X-W10 law.**
- **AAP-16 (reader-2 missed-2 + C:D-11, folded) — the error channel reads the wrong field on BOTH arms.** `ApiProblem` calls `super(title)` so `.message` IS the RFC 7807 *title*; `.detail` is read NOWHERE in demo (this seat's grep — sole narrowing precedent useSlugMigration.ts:83); useAdminAudit.ts:64-66 `e?.message ?? …` pipes either a terse title ("Forbidden") or an unbounded non-ApiProblem `Error.message` (the measured 443-char wall) into EmptyState's `detail` slot, whose doc (:81-82) promises "the machine truth". Narrowing alone loses the good text — `.detail` must be preferred. D saw the terse arm, C saw the verbose arm, neither saw the mechanism. **Disposition: NO-WAVE-OWNER** (composable + shared-atom contract; unowned in X).
- **AAP-17 (L:L-5) — a file-beats-directory accident ships the uncertified exporter.** `export.ts` (4,184 B) shadows `export/` (12 modules, no index.ts); `usePaletteExport.ts:9` resolves to the FILE; the W51 RFC-8785 lattice's only consumer is its own byte-exact test — all re-verified. Reader-1 graded MINOR (off the subject's graph), reader-2 MAJOR. **RULED MAJOR** — severity attaches to the defect, and X-W7 scope item 2 already owns the cure verbatim. **Disposition: FOLD → X-W7 (scope 2).**
- **AAP-18 (L:L-2) — all three demo boundary lint objects glob a tree deleted at W43.** `demo/@` gone; G-DEMO-3a (the law L-1 breaches) cannot fire; browser/index.ts still advertises the enforcement. Identity: the dead-glob row from the ColorSpaceSelector record (L-5(a)) and AdminUsersPanel §3 — ONE identity. Reader-2's nuance adopted: the first object is live-but-vacuous (its `demo/color-picker/**` glob exists; its banned patterns are unmatchable). **Disposition: FOLD → X-W8 (scope 3: delete the dead globs + add the boundary ban that keeps the barrels dead).**

### MINOR

- **AAP-19 (C:D-12 / D:D-9 / L:L-8 + reader-1 missed-4, folded) — `h-7 px-2` re-pins a height the producer clamps and freezes the inline axis out of the coarse lift.** 28×36 fine-pointer (producer `min-block-size` beats `h-7`; `px-2`+12px glyph = 28px wide); under `pointer: coarse` the tokens resolve `--control-h-sm` = max(54px, 44px) = **54px** while the Tailwind-frozen inline axis stays 28px — a 28×54 capsule (mechanism static from `sizing.css`/`light-dark.css`, magnitudes live-only). 13 `h-7 px-2` copies. D's MAJOR grade demoted (reader-1 right: 8px on one axis at fine pointer); the coarse arm is the sharper defect. **Disposition: FOLD → X-W4 gate A3 / CC-045** (which already cites `AdminAuditPanel.vue:30` and both failure modes; cure = `size="xs" iconOnly` per K-1 — a consumer one-liner, NOT a producer relay).
- **AAP-20 (C:D-6 / D:D-19 / L:L-9) — the debounce timer outlives the panel and mutates the singleton port.** :110-117, no `onUnmounted`/`onScopeDispose` in the file (read whole); port provided once (usePalettePorts.ts:245, audit at :72/:228); AdminPane mounts the panel under bare `v-if` (:61) — probe C-3 reproduces the orphan fire. `watchDebounced` is already a dependency; the 300ms/reset-page-1 policy is the composable's invariant parked in a view. **Disposition: BUILD → X-W7.**
- **AAP-21 (C:D-8 / D:D-15) — the loading region's name is discarded (role-less div), three generic "Loading" announcements fire, the count has no live region, no list semantics.** :36 verified; AdminListSkeleton :10-11 `role="status"` ×3; the house grammar exists next door (PaginationBar :17, EmptyState :17/:28, AdminTagsPanel wrapper pattern). **Disposition: BUILD → X-W4** (file in W4's modify list; semantic-controls law), template arm coordinated with X-W7's anatomy cure.
- **AAP-22 (D:D-13) — offset pagination where the binding composition says stable cursor.** Source mechanism confirmed (offset + `sort timestamp:-1`; `CursorPaginatedResponse` already declared in the same types file); the skipped-event consequence needs concurrent writes (correctly HYPOTHESIS). **Disposition: NO-WAVE-OWNER** (API+client tuple; X-W3's scope does not carry it).
- **AAP-23 (D:D-14 + C:D-9, one surface) — the timestamp drops year/timezone, `2-digit` hour defeats `tabular-nums`, and the try/catch is dead code with "Invalid Date" reaching the DOM.** dateFormat.ts read whole; `toLocaleString` returns the string for NaN dates (probe C-4: `"1 entryx.yInvalid Datet"`); `formatDate` repeats the dead pattern. An audit trail is a legal artifact of *when*. **Disposition: NO-WAVE-OWNER** (`browser/dateFormat.ts` in no X file list).
- **AAP-24 (D:D-11 / C:D-10) — a hover promise with no affordance behind it, on a row no keyboard user can reach.** :59-63 verified — sole unpaired `hover:bg-accent/50` in the tree. Resolves WITH AAP-4: either the row gains the canon-mandated disclosure (becoming a real focusable control) or the hover dies. **Disposition: BUILD → X-W7 (rides AAP-4).**
- **AAP-25 (D:D-17) — machine strings un-isolated for bidi.** No `<bdi>`/`dir` in the SFC (grep 0); canon two-place rule verbatim-verified by both readers. **Disposition: BUILD → X-W7.**
- **AAP-26 (D:D-16) — Retry set in the display serif against a closed type matrix.** :49 `class="font-display"` verified; the family repeats it at 5 sibling sites; also a per-instance override (edict 5). **Disposition: BUILD → X-W7 cure under X-W10 law.**
- **AAP-27 (L:L-1) — the subject is a node on the Admin/provider type SCC.** Four edges re-verified by the readers, cycle membership re-measured at the AdminUsersPanel §3 madge run (4 chains, this panel in all four via the barrel); runtime-acyclic (`import type` + verbatimModuleSyntax). **Disposition: FOLD → X-W8 G-7** (Admin/provider seven-node SCC → one owner).
- **AAP-28 (L:L-6 / L:L-10) — 19-dir alias layer + a 34-member port injected to read one member while the same parent speaks two wiring contracts on adjacent lines.** Verified (:96-98 barrel imports; :107-108 inject-for-one; AdminPane props-vs-inject split). Identities: demo/ui deletion = X-W8 scope 2 (92 imports/48 files); port width = AdminUsersPanel L-2, riding X-W8's `usePalettePorts.ts` modify-carve. **Disposition: FOLD → X-W8.**
- **AAP-29 (reader-1 missed-3, DEMOTED) — one neutral plate for 16 verbs incl. `delete-user`/`impersonate`; the producer's `tone` axis (verified: `TONE.destructive` etc.) sits unused.** MINOR by this seat's ruling (see Challenges): an information-design arm of the anatomy rework, not a standalone falsehood. **Disposition: folds into AAP-4 → X-W7.**
- **AAP-30 (reader-1 missed-6) — the only visible field identification is the placeholder, which vanishes at the first keystroke** (aria-labels present, so AT is served; the sighted operator is not — on two pills whose matching laws differ). **Disposition: folds into AAP-8 → X-W7.**
- **AAP-31 (reader-2 missed-6) — the paginated-resource block is duplicated line-for-line between useAdminAudit and useAdminFlagged** (:37-38/:44-46/:73-85 vs :50-51/:55-57/:105-118 — correspondence spot-verified; `useFilteredList.ts` is the in-directory extraction precedent). Carried into the ledger here so it finally has an id. **Disposition: NO-WAVE-OWNER.**
- **AAP-32 (L:L-3) — tsconfig.demo.json `paths` drift (3 phantom keys · 3 dangling targets · 2 shipped subpaths missing · a header off by one).** Identity: ColorSpaceSelector L-14, RESCOPED to the W-HYGIENE config-parity fold; X-W8 explicitly declines it ("L-22 ≡ W-HYGIENE, another owner") yet **no X wave file claims it**. Reader-2's MINOR demotion sustained (phantom entries inert, no live break — the ColorSpaceSelector ruling). **Disposition: FOLD → the standing W-HYGIENE identity; NO-WAVE-OWNER in X — flagged for the formation boundary.**

### INFO

- **AAP-33 (L:L-11 / L:L-12)** — the admin pane host and its panels live in two homes (`palettes/admin/` vs `palettes/browser/admin/`; nothing about a ledger is "browser"), and two comments cite laws that are dead (browser/index.ts:7's eslint claim — dies with AAP-18 at X-W8; the `@components/` prose refs). **Disposition: NO-WAVE-OWNER (doc-drift arm partially discharged by X-W8).**
- **AAP-34 (reader-2 missed-7)** — the `demo/@/lib/palette/types` ghost path in e2e is broader than C filed (10 refs incl. a runtime path string; C's line cite off by 6), invisible to every gate (e2e outside both tsconfigs). Identity: the AdminUsersPanel §3 measurement row (10 refs · no such directory) — ONE identity. **Disposition: NO-WAVE-OWNER (candidate X-W1 e2e re-gate).**

## Killed-claims register

Seven kills sustained from the re-readers (each re-derived by this seat), two new against the re-readers themselves.

- **K-1 — KILLED: L:L-8's and C:D-12's producer-relay cure** ("a `size="xs"` icon rung … must go to glass-ui"). The installed producer already ships `ButtonSize = "xs" | …` and `iconOnly?: boolean` ("Square geometry for an accessibly named icon command") — read at the d.ts by this seat. No relay exists to raise; the cure is `size="xs" iconOnly`, one consumer line. The 13-site finding is untouched. *Method note: L applied exactly this producer-check discipline to Pagination/Empty two paragraphs later and skipped it here.*
- **K-2 — KILLED: D:D-20's stated proof** ("`badge-atom` is a marker class with no CSS rule", grepped in the wrong file). `dist/styles/glass/glass-atom.css` defines seven `badge-atom` rules incl. the base rim `box-shadow: var(--glass-material-rim)` and a bordered `--outline` register; the badge base class list includes `badge-atom` (chunk read). The hypothesis narrows and survives only as UNPROVEN-NEEDS-LIVE (see residue). *Corpus method note stands: glass-ui greps that read only `dist/glass-ui.css` are incomplete — `dist/styles/**` ships.*
- **K-3 — KILLED: D:D-9's attribution limb** ("four 22-23px controls from the dock"). REPORT.json extracted by this seat: `input 160×23` + `Switch to slug` / `Generate new slug` / `Cancel` at 22×22 — PaletteSlugBar, not dock. challenge-L's negative-proof table has it right; a checkable factual error inside a MAJOR.
- **K-4 — KILLED: D:D-10b's quantifier limb** ("a 1,510px jump … on every 300ms debounce tick"). Because the `v-for` sits OUTSIDE the chain (AAP-2), re-fetch skeletons ADD ~178px above retained rows — the 1,510px figure can only describe first load; and :112 `clearTimeout` means one load per typing pause, not one per tick. The shape-mismatch substance (skeleton anatomy ≠ row anatomy) stands and rides AAP-2's cure.
- **K-5 — KILLED: C:D-2's cure as prescribed** (`v-else` wrapper around the `v-for` alone). Leaves PaginationBar operable and the count asserting fiction beside the error plate, and the wrapper without `min-w-0` inherits AAP-1's blowout one level up. Folded as AAP-2's complete-cure spec.
- **K-6 — KILLED: C:D-16's ipHash limb** ("fetched on every request and never rendered"). Inverted: never fetched — 18 emit sites, 0 pass it; the conditional spread keeps the key off the document; the wire never carries it (this seat's greps). The real defect is AAP-5 (the type declares it REQUIRED). challenge-D's ":197 the one provenance field that did survive typing" repeats the same inversion.
- **K-7 — KILLED: C's verbatim-guarantee at the C-9 cell.** challenge-C:25 promises byte-verbatim probe quotes; :483 quotes `toolbar=""` glossed "(blank count)" where `pi/probe-output.log:19` reads `toolbar="undefined"` — the panel renders **"undefined entries"**, a *stronger* defect than filed. The guarantee is broken exactly where the finding rests wholly on quoted output.
- **K-8 — NEW KILL (this seat, against reader-1): "`grep -rn "emphasis=" demo/` → 0 hits".** Measured **2** at HEAD `64aca0a2` (PalettesPane.vue:113, AdminUsersPanel.vue:174). The AAP-7 mechanism is untouched; the decorative zero dies. L-9 law: unscoped decorative counts are how false rows get through — applied here to a re-reader.
- **K-9 — NEW KILL (this seat, against reader-1's D-20 residue framing): "the hypothesis survives on … no border utility".** The secondary badge carries the base `badge-atom` rim (K-2's bytes) and a `prefers-contrast: more` border exists producer-side. The surviving hypothesis is strictly narrower than reader-1 restated it; reader-2's framing binds.

## Superlatives (provenance preserved)

- **challenge-D's §0 public retraction** (:51-58, read verbatim by this seat): the seat measured its own replica's bare-span badge, caught the one-keystroke-from-filing "Badge has no chrome" error, re-ran with real `badgeVariants` output, printed corrected numbers, and wrote "is NOT a defect and is not filed." The rarest artifact in the corpus.
- **challenge-D's instrument honesty on forced-colors** (:966-969): declared WebKit's `forced-colors: active` emulation unreliable (query matched, no repaint) and self-filed D-20 as HYPOTHESIS / Reproduction: NONE — the correct disposition even though its structural premise was independently wrong (K-2).
- **challenge-C's controlling fact + mutation table**: the populated fixture routes `/admin/audit` (admin-populated.ts:170) with seeded entries no spec ever visits — "dead fixture data" — re-verified by this seat against every goto; and the seven-mutation table converts "there are no tests" from assertion to demonstration. It is why AAP-1/AAP-2 are credible as *shipped* defects.
- **challenge-L's negative-proof section** (:525-537): runnable commands for what is SOUND — zero src/ deep imports, every value.js specifier a published subpath, and the affirmative clearing of this component from the route's smallTapTargets (verified by this seat against REPORT.json; L right where D was wrong on the same fact).
- **The tree's own self-indictments**: AdminListItem.vue:6-10 carries the min-w-0 post-mortem that let three axes convict the regression statically; EmptyState.vue's two-species split with per-prop ruling provenance (:77-88) and the measured-contrast cure comment (:95-104); audit-log.ts's documented befitting-graceful carve-out ("NOT a silent swallow"); ApiProblem's complete RFC 7807 implementation — better than every consumer it has (AAP-16 is the consumers' failure, not its own).

## UNPROVEN-NEEDS-LIVE residue (the visual-audit inputs)

1. **AAP-1 magnitudes**: 732.5px in a 320px track / 22,370.4px @1440 / toolbar stretch / card clientWidth 510 vs scrollWidth 22,394. Mechanism source-certain; numbers rest on injected replicas.
2. **AAP-9 width table**: Action 128.0 vs Target 109.1/113.6/107.5/96.0 per viewport — plus the unexplained 109.1-vs-74.1 asymmetry between the two `flex-1` peers, which does not follow from `flex: 1 1 0%` alone and must be re-derived, not trusted.
3. **AAP-15**: which of the two competing font-size declarations wins the badge (the 16.4px cell), and the muted-color identity of timestamp vs target. The chimera itself is proven statically.
4. **D:D-20 forced-colors, premise CORRECTED**: badge identity = `bg-secondary` + `badge-atom` box-shadow rim; whether UA substitution erases the verb's objecthood needs a real WHCM session or Chromium forced-colors run. (`prefers-contrast: more` border ≠ forced-colors; do not conflate the cells.)
5. **AAP-19 coarse cell**: the 28×54 capsule under `pointer: coarse` — token math is static; a real coarse-pointer cell witnesses it (X-W4 A3 already specs this arm).
6. **Skeleton-vs-row geometry** (54.0 vs 73.0px, gap 8.0 vs 11.9) and the 209.0px true-empty void (D:D-18 — cure lands on the shell, cross-ref AAP-13).
7. **Mobile toolbar wrap coordinates** (D §1.2: spacer stranded at x=284.5 row 1; cluster at x=33/x=118.5 row 2).
8. **AAP-22 runtime consequence**: the window shift needs concurrent writes against a live backend.
9. **Motion: NOTHING.** Zero motion-quarantine rows for this component; INFO-1's 0.1s color-leg finding is ruled non-vestibular by M-17/M-26 and may not be carried as a defect by any consuming wave.

## Closing verdict

**ADJUDICATED (never VERIFIED — no wave is stamped by this file): 34 rows CONFIRMED — 4 BLOCKERs (the ledger lies to strangers, lies over its own stale rows, blows its track on guaranteed input, and cannot say who) · 14 MAJOR · 14 MINOR · 2 INFO; 9 kills sustained, two of them new against the re-readers' own cells; dispositions routed X-W3.6 ×1 · X-W4 ×2 · X-W5 ×1 · X-W7 ×15 · X-W8 ×5 · 9 NO-WAVE-OWNER rows + 1 standalone-rider flagged for the next formation boundary; the r1 corpus governs (no r2, no promotions, no motion rows); 8 residue items feed the visual audit.** The component's one genuinely good bone — EmptyState's two-species law — is honored in chrome and betrayed in wiring: every blocker here is a state that reaches the wrong species. Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
