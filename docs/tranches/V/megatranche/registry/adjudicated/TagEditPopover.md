# TagEditPopover — adjudicated apotheosis (M-23/L-18, arbiter-F) · seat model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Date**: 2026-08-06 · **HEAD**: `c654824e` · branch `tranche-u` · **Subject**: `demo/palettes/browser/search/TagEditPopover.vue` (87 lines; SHA-256 `961debf2…3cdc` re-hashed this seat, byte-identical to the D receipt).
**Corpus**: challenge-C (Opus 5 [1m], EXISTS-ORIGINAL, HYDRATION-LEDGER.md:130) · challenge-L (Opus 5 [1m], EXISTS-ORIGINAL, :131) · challenge-D (REPORT-AUTHORED, probe 0, :276) · probes 0–7 + `evidence/` (40 files).
**Readers adjudicated**: reader-1 (design-and-usage) · reader-2 (library-and-code) — both hostile re-reads of the full corpus; both attacked before either was adopted. Every verdict-level disagreement (L-8/K-5 · C-6#3 · C-8 severity · D-5 · L-7 severity · the exports-map count) was re-derived by this seat with its own instruments.
**Arbiter's own instruments (all static + node against dist + committed evidence; zero browser probes — probe parsimony; product source READ-ONLY)**: full read of TagEditPopover.vue, useTagEdit.ts, BrowsePane.vue:140-160/:190-230/:300-325, usePalettePorts.ts:150-195 (browsePort = 32 members, my count), SearchFilterBar.vue:14-18/:45-60, useBrowsePalettes.ts:155-200, demo/palettes/api/index.ts (7 modules incl. all 4 admin), browser/index.ts:1-46; installed glass-ui 7.0.0: glass-ui.js:428-452 Checkbox props/emits, components/popover/{index,Popover.vue,PopoverContent.vue}.d.ts, popover-BQGYXZyO.js (all 3 `openAutoFocus` occurrences extracted — forwarded, never prevented; the `{ role, "aria-modal", …rest }` strip), styles/components.css byte offsets (`.p-0{`@11161 · `.px-\(--overlay-pad-inline\)`@11323 · `.py-\(--overlay-pad-block\)`@11679), typography/{utilities,semantic}.css (`.section-label` = `text-mono-caption` + muted ink; `text-caption` is italic), package.json exports (node: **74 keys**) + `sideEffects: ["*.css"]`; reka-ui: PopoverRoot.js (modal default **false**), PopoverContent.js modal branch, PopoverContentImpl.js:115-155 (`trapped: _ctx.trapFocus` · `aria-labelledby: rootContext.triggerId` · `role: "dialog"`), PopoverContentNonModal.js (no trapFocus in its prop set), PopoverTrigger.js:20-45, PopperContent.js:238 (`translate(0, -200%)` — self-relative), **FocusScope.js:85-100 — the C-6#3 decider**; demo/ui census (19 dirs; checkbox+popover root re-exports; **input = `/forms` subpath**; alert = 11 lines + type export); api/src/modules/color/routes.ts:64-68 + service/queries.ts:106-110 (raw `TagDTO[]`, unconditional), api/src/modules/palette/etag.ts (whole), demo/palettes/api/palettes.ts:160-180, `ls api/src/middleware/` → does not exist; evidence/probe-6.json + probe-7.json (parsed via node) + probe-7-states.mjs:172-177 (the `rowHasFocusStyle` heuristic) + m-desktop-light.png (viewed); motion-quarantine.md:111 (M-20 TRUE); greps: `TagEdit|editTags|useTagEdit` over test/+e2e/ → exit 1; `<Checkbox` demo-wide → exactly 2 files; `<PopoverContent` demo-wide → 8 sites / 7 files, zero pass `ariaLabel`; `git log -S'update:checked'` → a1060e5b 2026-03-26 vs 8dc5d31c 2026-02-24; routing authorities read: X/waves/W7.md (§3 item 1, §4 file bounds :88-118, mechanism :180-186, G1/G2/G3 :284-320, G8 :366, G10 :405, G13 :442, W7.d :219-232, :269, :610-611), W8.md:40/:76/:222-231, W4.md (control law; SearchFilterBar :126/:236), W10.md (canon law), W3.md (api-side), adjudicated/AdminTagsPanel.md (ATP-19 identity; routing grammar).

## Scoreboard

**30 rows CONFIRMED (3 BLOCKER · 14 MAJOR · 9 MINOR · 4 INFO) · 12-row killed register — 11 reader kills sustained (one refutation text corrected against measurement, one extended by this seat's vendor read, one corrected against reader-2's own correction cell) + 1 NEW kill by this seat of a reader CONFIRMATION (C-6#3, demoted to UNPROVEN-NEEDS-LIVE) · 19 reader-missed findings adjudicated in · dispositions: X-W7 ×25 · X-W8 ×1 · GLASS-OWNED relay ×1 · X-W10-law riders ×3 · X-W1 input ×1 · NO-WAVE-OWNER ×3 (+2 partial arms).**

The headline is unchanged from the axes and survives every attack: **the component's primary code path has never executed in any commit.** Both BLOCKERs are born-dead (binding authored 2026-03-26, one month after reka killed the API it targets, 8dc5d31c 2026-02-24), invisible to the HARD type gate (fallthrough attrs are legal), untested (grep exit 1), and outside the 60-capture visual matrix. The consumer's own comment (BrowsePane.vue:147, "These components portal themselves via reka-ui (Sheet/Dialog)") misclassifies the surface in prose — the wrong-archetype ruling is written into the caller.

## Ruled disagreements (the seat's actual work)

| id | reader-1 | reader-2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-6#3** (focus never enters) | UNPROVEN-NEEDS-LIVE | CONFIRMED MAJOR (NonModal ⇒ `trapped:false` ⇒ no entry) | **UNPROVEN-NEEDS-LIVE — reader-1 wins; reader-2's mechanism KILLED (K-12)** | FocusScope.js:85-100: the mount-autofocus watchEffect is **not gated on `trapped`** — it dispatches AUTOFOCUS_ON_MOUNT and, unprevented, runs `focusFirst(getTabbableCandidates(container))`; glass-ui forwards `openAutoFocus` at all 3 render branches and prevents nothing (my extraction). So "non-modal ⇒ focus never moves" is false as a source derivation. The C-seat's live measurement (`focusOnOpen == focusBefore == tep-sentinel`) stands as an observation with a **named candidate mechanism this seat derives**: at open the panel contains only the spinner (catalog load is async), `getTabbableCandidates` returns ∅, `focus(container)` no-ops on a tabindex-less div, and the autofocus moment has passed by the time rows render. Needs a live probe with the catalog pre-loaded vs delayed, focus method recorded. |
| **L-8 normative limb** (retired loading idiom) | KILLED (K-5) | CONFIRMED MINOR ("the idiom the migration retired") | **KILLED — reader-1 wins** | Neither authority reaches the subject: utils.css:36-44 is a token rule for **skeleton surfaces**; AdminListSkeleton.vue:1-6 scopes its replacement claim to "every **admin** panel" (both read whole). What is actually wrong is behavioural and neither axis filed it — the 114 px loading→ready height jump with no announce and no shape (probe-7 `loading.panelH 134.3` vs probe-6 `panel.rect.h 248.3`). Rescoped: TEP-15 (MAJOR) replaces the vocabulary charge; the `@lucide/vue` bypass + 2-site centred census survive at MINOR (TEP-24). |
| **C-8 severity** | MAJOR (public reachability) | MINOR (axis's own filing) | **MAJOR — reader-1 wins** | Class-identity law (AdminTagsPanel precedent): the dead-`<Checkbox>` class is BLOCKER at the subject; its sibling site is reachable on `/#/browse` with **no session** and kills the public tag filter outright (SearchFilterBar.vue:51-55, read verbatim). A publicly-reachable member of a BLOCKER class does not file below MAJOR on "out of scope" grounds. Cure note: reader-2's M-5 two-cure narrowing is CONFIRMED **and already encoded in the wave** — X-W7.md:180-182 prescribes exactly the payload-discarding vs `v === true`-narrowing split. |
| **D-5** ("w-52 too narrow") | (silent; filed the sharper missed BLOCKER) | KILLED — "no overflow is produced" | **KILL SUSTAINED on provenance; refutation text CORRECTED** | D is probe-0 REPORT-AUTHORED asserting a rendering outcome — the kill stands. But reader-2's refutation clause is itself false by measurement: overflow IS produced — probe-7 `light.catOverflowRows: 4` (`catRight 602.2 > rowRight 599`), and the truncation D never measured is total (7 of 10 names unreadable). D-5's substance is subsumed upward into TEP-3, not resurrected. |
| **L-7/C-5 severity** | MINOR (asymmetry only; K-3) | MAJOR (kept) + M-4 (premise unproducible) | **MINOR — reader-1's grade, reader-2's M-4 cure direction** | Producer re-read: routes.ts:64-68 ("raw array — no envelope wrapping") → queries.ts:106-110 unconditional `TagDTO[]`. The failure state is not producible by the live contract; what survives is structural — the fossil X9 guard defending an impossible shape, the dead empty-state branch under it, and the real hole one level up: `client.ts:127 return res.json()` is an unchecked cast for **every** endpoint. Cure = delete the coercion + validate at the boundary, never coerce-everywhere. |
| **glass-ui exports count** | 74 keys | "70 keys, re-read via node" | **74 — reader-1 wins; a kill against reader-2's own correction cell** | My node run: 74 keys. Missing among demo/ui's 19: `./alert`, `./avatar`, `./checkbox`, `./radio-group`, `./skeleton` — the relay is **5** components (reader-2's own extension of the list is right; its recount was not). Reader-2's other two L-4 narrowings both verified and adopted: alert = 11 lines + `AlertVariants` type export; `input/index.ts` already rides the `/forms` subpath — so **17 of 19**, not 19, are root-barrel renames. |

**Further corrections (line-anchor hygiene, non-substantive)**: reader-2 cites BrowsePane.vue:196 for two different imports — actual: `./browser/search` at :190, `@mkbabb/glass-ui/search` at :195. Reader-1's D citations run one line low (D-1 = :15, D-3 = :17). probe-7's `keyboard.rowHasFocusStyle: true` is a **probe artifact** (probe-7-states.mjs:177 string-compares against `"rgb(0, 0, 0) none 0px"`; the measured `"3px none rgb(28,25,23)"` fails the compare while painting nothing — outline-style is `none`) and must never be quoted against TEP-21.

## Adjudicated defect roster

Line anchors are HEAD `c654824e`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

- **TEP-1 (C-1/L-1) · BLOCKER** — `:checked`/`@update:checked` bound to a glass-ui@7 Checkbox that declares `{modelValue…}` + `emits:["update:modelValue"]` only (glass-ui.js:428-452, my read); applied tags render unchecked, clicks emit nothing, save nothing; born dead a1060e5b (2026-03-26) vs 8dc5d31c (2026-02-24), both re-run this seat. Visual: m-desktop-light.png — empty circles beside Specimen 1's live `warm` chip. **→ X-W7** (X.W7.a; G1/G2/G3 born-RED, W7.md:284-320).
- **TEP-2 (C-2/L-2/D-1) · BLOCKER** — the `trigger` slot is never filled by the only consumer (BrowsePane.vue:148-155, read verbatim); PopoverTrigger renders zero elements, no anchor registers, `isPositioned` stays false, content pinned at `translate(0, -200%)` (PopperContent.js:238, my read) — self-relative, fully off-screen at every viewport; the `role="dialog"` surface carries a dangling `aria-labelledby` minted from `triggerId` (PopoverContentImpl.js:142-148). *Demoted sub-clause*: "no element to restore focus to" — see K-7. **→ X-W7** (X.W7.d: tags move to the selected-entity inspector, W7.md:219-232 — the transposition dissolves the popover and moots the Dialog-vs-submenu cure debate; both readers' cure kills K-8/K-9 bind any interim fix).
- **TEP-3 (reader-1 missed №1) · BLOCKER** — 7 of 10 rows render no readable tag name: `truncate` (:32) makes the name the row's only shrinkable item while the category is `ml-auto shrink-0` (:33); probe-6/7: `zeroNameRows 4`, `ellipsisOnlyRows 3`, `catOverflowRows 4`; the identity collapses and the decoration survives whole — inverted information hierarchy, visually confirmed this seat. All three axes walked past it. **→ X-W7** (design input to the inspector tag seat; the tag N-fixture must include a 22-char name so a recreated inversion goes RED).
- **TEP-4 (C-7 + L-9/C-10) · MAJOR** — vacuous gate: zero tests/e2e touch the component, composable, or `editTags` path (grep exit 1, re-run this seat); the HARD `vue-tsc` gate is structurally blind to the C-1 class (fallthrough attrs; exit 0 executed by the C-axis and reader-1 — two independent runs, not re-run here per probe parsimony); zero visual coverage (never mounted in 60 captures). Flourish killed at K-6. **→ X-W7** (G2 contract test + G3 `strictTemplates`); the visual-matrix gap (authenticated owned-palette browse state) **→ X-W1 input**.
- **TEP-5 (C-3) · MAJOR** — optimistic `emit` before the await (:76-77), return value discarded, every failure swallowed (`useTagEdit.ts:63-66` catch→warn→undefined); no rollback, no surface. Latent behind TEP-1; correctly self-labelled. **→ X-W7** (§3 item 3's 31-site console.warn table; G7; G10 already cites `TagEditPopover.vue:64-66,77` for the N_tags=11→400 no-visible-failure path; G13).
- **TEP-6 (C-4/L-6) · MAJOR** — a leaf owns If-Match policy: derives the validator (:73-74), silently downgrades `undefined→"*"` (useTagEdit.ts:54 → etag.ts:44 skips the check), discards the PATCH response, and BrowsePane.vue:312-319 re-caches the stale `updatedAt`. All four legs source-verified this seat; server twin: crud.ts bumps `updatedAt` always, `currentHash` only on content change. End-to-end lost update = HYPOTHESIS (legacy `currentHash === null` rows only) — both axes and both readers concur; must not be reported as observed. **→ X-W7** (X.W7.d; `useTagEdit.ts` in the modify list, :118).
- **TEP-7 (reader-2 missed M-3) · MAJOR** — `browsePort` publishes `remotePalettes` as a raw writable `Ref` and BrowsePane.vue:317 writes into it from outside the owning composable, which already owns the row-replacement idiom internally (useBrowsePalettes.ts:165-171). The unencapsulated port is what makes TEP-6's stale re-cache possible; the corpus's "fold the response back" cure writes through the same open door. Cure: `readonly()` + an owned `replaceRow`. **→ X-W7** (X.W7.d; usePalettePorts modify).
- **TEP-8 (L-3) · MAJOR** — 32-member port injected (my count of usePalettePorts.ts:157-192) for 2 members, one of which re-finds the object the parent already holds (`tagEditPalette`, BrowsePane.vue:305). Reader-1's perf-framing correction adopted (a ≤50-row `find` is irrelevant; ownership duplication is the defect). **→ X-W7** (X.W7.d).
- **TEP-9 (L-4 structure limb) · MAJOR** — `demo/ui/` alias layer: 17 of 19 modules are one-line root-barrel renames (alert & input are the counterexamples, K-10); the shim is why the root barrel is in the graph. Cost limb dead (K-2). **→ X-W8** (delete 19 barrels / 92 imports, W8.md:40; `checkbox/index.ts` carved in W7, deleted in W8 — W7.md:610-611); the **5** missing subpaths (`./alert ./avatar ./checkbox ./radio-group ./skeleton`) **→ GLASS-OWNED relay** via the standing BH/BI fond (owner edict).
- **TEP-10 (L-5) · MAJOR** — `paletteETag` implemented twice with type drift (`string|undefined`+`string` vs `string|null`+`Date`), no shared contract home, and the demo docstring cites `api/src/middleware/etag.ts` — verified this seat: **does not exist**. Downstream tell: `tags ?? []` at BrowsePane.vue:152. **→ NO-WAVE-OWNER** — the contract home spans demo+api; X-W3 is api-side only, X-W7 must-not-touch `api/**`; named for the next formation boundary. TEP-26 rides it.
- **TEP-11 (C-6.1/D-4 · M-20 TRUE, carryable) · MAJOR** — the async catalog swap is unannounced: zero live regions, no `aria-busy`, no `role="status"` in the 87-line file (probe-7 `loading.liveRegions: 0`); the sibling AdminListSkeleton already carries `role="status" aria-label="Loading"` (:10-11, my read). Motion itself is governed (probe-7 `reducedMotionSpin` 1e-05s/1) — no FALSE quarantine row carried. **→ X-W7** (the inspector seat copies the house pattern).
- **TEP-12 (reader-1 missed №2) · MAJOR** — `p-0` silently loses to glass-ui's overlay-pad recipe by source order (byte offsets verified exactly: `.p-0`@11161 < `px-(--overlay-pad-inline)`@11323/`py-…`@11679, both later ⇒ both win): panel measures `padding 20.35px 16px` with `p0Applied: true` → 28 px/side, a 150 px content column in a 208 px surface — the mechanical cause of TEP-3. Three-site family (`SearchFilterBar.vue:16 w-60 p-0`, `PaletteSlugBar.vue:88 w-auto p-1`). **→ X-W7** (both search/ sites; family census recorded for X-W10's canon).
- **TEP-13 (reader-1 missed №3 = reader-2 missed M-2, converged) · MAJOR** — `PopoverContent` ships a declared `ariaLabel?: string` prop ("Accessible name for the hover group or click dialog", d.ts read this seat) and deliberately strips `role`/`aria-modal` from fallthrough — the ONLY sanctioned naming seam; the component passes nothing, and the demo census is 8 sites / 7 files, **zero** pass it (re-run this seat). Three axes diagnosed the nameless dialog; all three prescribed markup outside the prop surface (K-9). One token closes it. **→ X-W7** (palettes sites incl. any surviving popover) **/ X-W4** (shell sites: ActionButton, ColorInput — the named-controls law).
- **TEP-14 (reader-1 missed №4; corrects K-1's direction) · MAJOR** — the only heading renders in the **identical register as ten repetitions of row metadata** (probe-6: heading and category both Fira Code 14.384px uppercase 1.4384px tracking; `headingEqualsCategoryRegister: true`) and 2 px smaller than the body it governs; :33 open-codes the `.section-label` recipe longhand (utilities.css read: `text-mono-caption` + muted ink). The measurable substrate of D-3's "no task hierarchy" — with D's direction inverted. **→ X-W7 cure under X-W10 law** (ATP-7 precedent).
- **TEP-15 (reader-1 missed №5; replaces L-8's normative limb) · MAJOR** — loading→ready jumps the anchored panel ~114 px (134.3 → 248.3, both probes) with nothing reserving space and nothing announcing; the house already solved both in one component (AdminListSkeleton: shaped to the row grammar AND `role="status"`). **→ X-W7** (skeleton recomposition; ATP-16 precedent; deletes the `@lucide/vue` import with it).
- **TEP-16 (C-11 + reader-2 missed M-6, folded) · MAJOR** — no busy, no disabled, no error surface, no close affordance (:64-78 + template :1-39); and M-6 sharpens the race: `updated` derives from the `currentTags` **prop**, which cannot have re-propagated within one tick — two same-tick toggles construct the second PATCH body **without the first tag**. A payload-construction loss, upstream of any ordering fix; live the moment TEP-1 cures. **→ X-W7** (X.W7.d single-owner mutation; G13's visible-result assertion).
- **TEP-17 (C-8, RAISED to MAJOR — ruled above) ** — the identical dead binding at SearchFilterBar.vue:51-55; 2 of 2 `<Checkbox>` sites in the demo (census re-run); publicly reachable without auth. Two cures, not one rename (M-5) — already encoded at W7.md:180-182. **→ X-W7** (X.W7.a, same stroke).
- **TEP-18 (C-9 + reader-2 missed M-1) · MINOR — FOLD, identity = AdminTagsPanel ATP-19** — three load paths for one permanently-frozen catalog; `loaded` is exported dead surface (zero `.loaded` readers) and `force` has zero callers, so the one invalidation seam has never been invoked. The identity is **already booked** at ATP-19 ("an admin tag write leaves TagEditPopover's catalog stale for the session") → X-W7. One identity, folded, never re-booked (anti-rename). The `{ immediate: true }` two-path collapse rides the same cure.
- **TEP-19 (L-7/C-5 as rescoped — ruled above) · MINOR** — consumer asymmetry (BrowsePane coerces via the X9 fossil, this file does not) + a dead empty-state branch under any non-array. Cure inverted per M-4: **delete** the X9 computed, validate once at `getTags()`. **→ X-W7** (BrowsePane + useTagEdit in bounds). The unbounded arm — `client.ts:127`'s unchecked `res.json()` cast for every endpoint — **→ NO-WAVE-OWNER** (transport boundary, named).
- **TEP-20 (C-6.4) · MINOR** — the `<label>` wraps the `<button role="checkbox">` plus the category, so every accessible name is `"warm temperature"`-shaped (probe-7 ariaSnapshot). Names remain unique/resolvable (`namedCheckboxProbe` all count 1) — MINOR is right. **→ X-W7** (W7.md:269's uncoupled-labels mechanism is the exact cure home).
- **TEP-21 (reader-1 missed №7) · MINOR** — pointer rows get `hover:bg-accent/50`, keyboard gets no `focus-within:` counterpart; the row paints nothing while its checkbox holds focus (probe-7 keyboard cell; the `rowHasFocusStyle:true` field is an artifact — see corrections). **→ X-W7**.
- **TEP-22 (reader-1 missed №8) · MINOR** — `ml-auto` is physical and mis-resolves under `dir=rtl` (probe-7: the auto margin absorbed 14.875 px on the wrong side); honest scope stands — no RTL commitment repo-wide (`dir=` grep 0), 8 physical-margin sites. Subject **→ X-W7** (`ms-auto`); census **→ X-W10 canon**.
- **TEP-23 (reader-1 missed №9) · MINOR** — clipped names have no recovery (`title`/tooltip absent; imports :42-47 read), producing the inversion where AT reads what sighted users cannot (ariaSnapshot vs `mute…` paint). Dies with TEP-3's width cure. **→ X-W7**.
- **TEP-24 (L-8 rescoped remainder) · MINOR** — direct `@lucide/vue` import (:45) for a loading affordance the DS pattern owns; centred panel-fill census = exactly 2 sites (this file :12, VersionHistoryDrawer.vue:18); the other seven `Loader2` sites are legitimate inline button-state spinners. **→ X-W7** (deleted by TEP-15's cure).
- **TEP-25 (reader-1 missed №6) · MINOR** — glass-ui ships `DropdownMenuCheckboxItem` (demo/ui/dropdown-menu/index.ts:1, re-read) and the opener is already a DropdownMenu (`PaletteCardMenu.vue:2/:86` — full path `demo/palettes/browser/card/PaletteCard/PaletteCardMenu.vue`, correcting both axes' unqualified citations); the hand-rolled row is bespoke against an existing DS primitive. Recorded as **cure input to X-W7.d**; L-2's submenu transposition remains an unvalidated design (residue №7).
- **TEP-26 (L-10) · INFO** — one 2-line pure function reached through a 7-module barrel dragging `deleteUser`/`impersonateUser`/`pruneEmptyUsers` + the side-effecting transport into a leaf's graph (api/index.ts read whole). Rides TEP-10's contract home. **→ NO-WAVE-OWNER**.
- **TEP-27 (reader-1 missed №10) · INFO** — `italic` restates `text-caption`'s own `font-style: italic` (semantic.css read). **→ X-W7** (rides any file touch).
- **TEP-28 (reader-1 missed №11) · INFO** — the decorative spinner is not `aria-hidden` (probe-7 `spinner.ariaHidden: null`); additive to TEP-11, distinct from it. **→ X-W7** (TEP-15's cure).
- **TEP-29 (reader-2 missed M-7) · MINOR** — useTagEdit.ts's docstring is stale three ways (`palette-browser/` migration paths do not exist; the "PaletteDialog shell" consumer claim has no reader — my grep: two `pm.tagEdit` readers only). Same drift class the corpus caught once (L-5) and missed here. **→ X-W7** (useTagEdit modify).
- **TEP-30 (reader-2 missed M-8) · INFO — anti-fix record** — browser/index.ts:35 re-exports `TagEditPopover` through the PI-6 seam; the sole consumer correctly reaches the sub-barrel (BrowsePane.vue:190) per the seam's own tree-shake rationale (:1-17, read whole). Recorded so no later seat "fixes" the import or deletes the seam export. **→ NO-WAVE-OWNER** (epistemic record).

## Killed-claims register

- **K-1 · D-3 "categories are microtext" — SUSTAINED (reader-1).** Measured inversion: category = heading register exactly (14.384px Fira Code uppercase, probe-6/7), name = 16.4px Jakarta. D is REPORT-AUTHORED prose without measurement; the umbrella survives inverted as TEP-14.
- **K-2 · L-4's 16.1×/9.4× cost limb — SUSTAINED (reader-1).** All figures are dev-rail artifacts (`.vite/deps`, `/@fs/` curl); `sideEffects: ["*.css"]` verified this seat — the JS is tree-shakeable and no production build was measured by anyone. The multipliers must not be carried as shipped cost (the true delta is residue №4).
- **K-3 · C-5's premise + MAJOR — SUSTAINED (reader-1).** Producer re-read this seat: raw unconditional `TagDTO[]`, comment says so verbatim. C-5's measurement used an injected state. Survivor rescoped at TEP-19 MINOR.
- **K-4 · C-2's "307px above the viewport" constant — SUSTAINED (reader-1).** `translate(0, -200%)` is self-relative (PopperContent.js:238, my read); −307.6 was the two-tag fixture's height ×2; the real catalog yields ≈−496.6. Conclusion (off-screen at every size) preserved in TEP-2.
- **K-5 · L-8's "retired idiom" normative limb — SUSTAINED (reader-1), overruling reader-2's CONFIRMED.** Ruled above; neither cited authority reaches a non-admin, non-skeleton surface. Replaced by TEP-15.
- **K-6 · C-7's "deleting `<script setup>` keeps CI green" — SUSTAINED (reader-2).** vue-tsc checks template expressions against setup scope; an empty context makes every template identifier a diagnostic and demo-typecheck is HARD since ef57230b. Reasoned from the toolchain, unexecuted (READ-ONLY law) — the flourish dies, C-7's substance stands in TEP-4.
- **K-7 · L-2's "keyboard focus is lost on dismiss" — SUSTAINED (reader-2) and EXTENDED by this seat.** Focus never provably enters (C-6#3 residue), so nothing is lost; and FocusScope.js:104-111's own unmount cleanup restores `previouslyFocusedElement ?? document.body` — captured independently of `triggerElement`. The extension also demotes the "no element to restore focus TO" sub-clause **both readers carried inside their C-2/L-2 confirmations**: reka's restoration does not route through the trigger alone.
- **K-8 · C-2's `PopoverAnchor` fallback cure — SUSTAINED (reader-2).** glass-ui popover exports exactly three members (index.d.ts read); executing the cure requires a raw reka-ui import — the design-system bypass the edicts forbid. Relay-or-transpose only.
- **K-9 · C-6#2's `aria-labelledby` one-liner — SUSTAINED (reader-2).** Not a declared prop (d.ts read); it would ride the same fallthrough mechanism C-1 indicts and collide with reka's own `aria-labelledby` binding. The declared seam is `ariaLabel` (TEP-13).
- **K-10 · L-4's "every module is a rename / delete 19 / 70-entry exports / relay 4" framing — SUSTAINED (reader-2), with reader-2's own count corrected (74 keys, not 70 — my node run).** 17 of 19 renames; alert carries doc + type; input already subpath; relay = 5.
- **K-11 · D-5 — SUSTAINED on provenance, refutation text CORRECTED (ruled above).** "No overflow is produced" is false by the corpus's own instrument (`catOverflowRows: 4`); the kill stands because D asserted an unmeasured rendering outcome, and the true defect (TEP-3) is worse than the one D guessed.
- **K-12 · reader-2's C-6#3 CONFIRMED verdict — NEW KILL, this seat.** The `trapped:false ⇒ no focus entry` derivation is refuted at FocusScope.js:85-100 (mount-autofocus is trapped-independent and unprevented through glass-ui's forwarding). The row returns to UNPROVEN-NEEDS-LIVE with this seat's async-empty-panel race as the candidate mechanism. A CONFIRMED-MAJOR resting on a false mechanism is more dangerous than an honest unknown — it would send the fixer to `modal`/`trapFocus` when the cure may be load-order.

## Superlatives (provenance verified this seat)

1. **Token discipline, repo-leading** (reader-1; verified): every class in the 87 lines resolves to a real glass-ui utility; zero hex, zero inline style, zero scoped `<style>`. Nobody fought the design system's ink — they fought its **contracts**.
2. **The W5-13 · F-9 comment (:69-72)** (reader-1): names the invariant (derive before the emit mutates), the fields the ETag reads, and the RFC 7232 fallback — at exactly the line where getting it wrong is silent. Its tragedy is TEP-1: the block has never executed.
3. **`api/src/modules/palette/etag.ts`** (reader-2; read whole this seat): the docstring states why the validator is `currentHash ?? updatedAt`, names the pre-version case, declares which resources carry no ETag; `assertIfMatch` distinguishes 428/412 and honours `*` and candidate lists. Every demo-side defect in TEP-6 is a deviation FROM this module.
4. **The captured-validator house idiom** (reader-2; verified at useBrowsePalettes.ts:158-163/:184-190) — every If-Match verb the composable owns derives `paletteETag(palette)` from the row in hand and folds the response back. *This seat's caveat*: `onRename`'s own `catch → console.warn` (:173-175) means the superlative covers validator lifecycle, not failure surfacing — those sites are rows in W7's §3-item-3 table too.
5. **The label-wraps-control hit target** (reader-1): a 150×30.9 row around a 16px control — WCAG 2.5.8 cleared by construction; TEP-20 is a fixable side effect, not a reason to unwrap.
6. **`browser/index.ts:1-17`** (reader-2; read whole): the rare barrel comment that explains why named-only re-exports exist (SFC styles are side-effecting imports), names its eslint enforcement, and pre-empts exactly the two wrong "fixes" TEP-30 guards against.
7. **The one-recipe loading system as a system** (reader-2): utils.css names its producer seam and consumers; AdminListSkeleton both announces and hides its ornament — the cure for TEP-11/TEP-15 is a copy, not a design.
8. **The C/L axis pair + both readers' epistemic hygiene** (verified): mechanisms probed at two runtime levels, birth-dated by `git log -S` (re-run, matches), and the one unreproducible arm labelled HYPOTHESIS at every seat that touched it — the label survived three hostile readings intact, which is the system working.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **Focus entry on open** (C-6#3, demoted at K-12): live probe with focus method recorded, catalog pre-loaded vs delayed — decides between the C-seat's measurement and FocusScope's unconditional autofocus; also settles whether TEP-15's height jump re-runs autofocus.
2. **Checkbox `:focus-visible` ring paint** under real keyboard navigation (probe-7's focus method unrecorded; the `3px none` cell is suggestive, not decisive).
3. **Category-ink contrast** (`rgb(112,89,66)` over the 0.808-alpha panel over a hot-pink→peach gradient): ≈5.5:1 against an opaque approximation; the true minimum needs sampling across anchor positions.
4. **The production-bundle delta of `demo/ui/`** — a `gh-pages` build, not a browser; K-2 killed the dev figure without establishing zero.
5. **The live geometry/emit numbers** (−307.6/−496.6, `emitted []`, sentinel identities) — carried on the authoring seats' committed receipts; mechanisms independently source-confirmed above.
6. **The end-to-end silent lost update** (TEP-6): requires a legacy `currentHash === null` row; `createPalette` always sets one. Hypothesis at every seat; stays one.
7. **Cure designs, not findings**: L-2's DropdownMenuSub transposition and X.W7.d's inspector tag seat both change the interaction model (PATCH-per-toggle semantics of TEP-16 unresolved by either); D's Kronecker state matrix (lifecycle × data × interaction × viewport) is the execution frame when the inspector lands — under the Apple-browser law.
8. **C-5's injected-state measurement** establishes the dead-branch reachability only, never the payload (K-3).

## Closing verdict

**DEFECTIVE — adjudicated whole.** A 4-month-old feature that has never once executed its purpose, wrapped in the repo's best token discipline and its best-commented dead code: two born-dead BLOCKERs (dead API, no anchor) under a third the axes missed (the readable-name collapse), all invisible to every gate the repo owns. 30 rows confirmed (3 BLOCKER · 14 MAJOR · 9 MINOR · 4 INFO), 12 kills ruled — including one reader CONFIRMATION killed on a false vendor mechanism — 19 reader-missed findings adjudicated in. Terminal routing: **X-W7 ×25** (X.W7.a checkbox contract + strictTemplates · X.W7.d inspector transposition + CRUD/port ownership · G1/G2/G3/G7/G8/G10/G13 already born-RED against this file's own lines) · **X-W8 ×1** (the shim dies) · **GLASS-OWNED ×1** (5 missing subpaths, BH/BI relay) · **X-W10-law riders ×3** · **X-W1 input ×1** (the matrix must mount this component) · **NO-WAVE-OWNER ×3 +2 partial** (the paletteETag contract home; the transport `res.json()` cast; the seam anti-fix record) — every row leaves with exactly one disposition, original ids preserved for life.

— end of apotheosis. The challenger corpus, both reader returns, and all dated evidence remain immutable beside this file per E-1/E-3.
