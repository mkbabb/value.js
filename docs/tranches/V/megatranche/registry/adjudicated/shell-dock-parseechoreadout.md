# shell-dock-parseechoreadout — adjudicated apotheosis · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited — recorded against L-11/L-14)

**Seat**: fresh-Fable adjudicator (M-23/L-18 — the incredulity seat).
**Date**: 2026-08-08 · **HEAD**: `027ca138` · branch `tranche-u` · **Component**: `demo/shell/dock/ParseEchoReadout.vue` (49 lines, SHA-256 `416468fd…` per all three challenge receipts).
**Corpus**: three r2 challenge files (`audit/components/shell-dock-parseechoreadout/challenge-{C,D,L}*.md`) + two hostile re-reader returns (design-and-usage · library-and-code), both attacked before either was adopted.
**Method**: every reader-vs-reader collision re-derived against the tree with this seat's own instruments; every "missed" row identity-guarded against the adjudicated registry before admission (three of the four heaviest rows turned out to be ALREADY BOOKED under other names — neither reader ran the guard); every load-bearing citation re-read in live bytes.
**This seat's own instruments (all static + node against dist; zero browser tooling — law)**: whole-file reads of ParseEchoReadout.vue (49 L), useColorParsing.ts (137 L), ColorInput.vue (377 L), picker-color.ts:40-218, useColorPipeline.ts:55-144/:160-184/:270-336 (return-key count = **38**, my own), src/color/operations.ts:1-60/:140-150, src/color/anchors.ts:365-380, src/css/grammar.ts:125-164, valueDomain.ts (whole), useSliderGradients.ts:45-80, ColorPicker.vue:340-360, App.vue:45-52/:138-142/:255-259, ActionBarLayer.vue:18-30/:112-120, keys.ts:1-10, foundation.css:100-108/:288-298/:660-706, DESIGN.md:330-346, useAtmosphereBoot.ts:95-110, docs R/PROGRESS.md:125-129, test/v4-c1.test.ts:184-194; **node against `dist/subpaths/{css,color}.js`**: all five `none` specimens parse ok, same-space conversion preserves `none` verbatim, cross-space `oklab` returns `color_missing_channel` for every non-hue `none` AND for non-powerless hue-`none`, `hsl(none 0% 0%)` (powerless) converts **ok**, all six serialize cleanly incl. `/ none`; **installed producer bytes**: glass-ui `dist/popover-BQGYXZyO.js` (`usesHoverRoot = trigger==="hover" && !matchMedia("(pointer: coarse)").matches`, evaluated once at setup; hover content stamps `role:"group", "aria-label": t.ariaLabel`; the trigger's non-hover branch renders reka `PopoverTrigger`; `t.asChild ? i : {...i, type:"button"}` **verbatim** — the type normalization dies in the as-child branch); reka-ui src `HoverCardTrigger.vue:36-44` (`@focus`/`@blur` as non-bubbling natives), `HoverCardContentImpl.vue:65-71` (`tabbables.forEach(t => t.setAttribute('tabindex','-1'))`), `Popover/PopoverTrigger.vue:33-44` (`aria-haspopup="dialog"`, `:aria-expanded`, `:aria-controls`, `@click="rootContext.onOpenToggle"`, `:type` only when `as==='button'`); `demo/ui/popover/index.ts` = a bare glass-ui re-export. Censuses re-run: `grep -ril gamut demo/` → **29 files** (decisive against a reader receipt — see K-14); `astEcho|gamutVerdict` → **3 product files, zero test/e2e hits**; DEFECT-LEDGER lines 45487/45582/45901/45906/63610/85226/86342/98689 read verbatim; adjudicated identity sweeps: `picker-componentsliders.md` R-2, `ColorInput.md` D-9/D-15/G3/G9 + its wave §"ParseEchoReadout rewired", `EmptyState.md` ES-13/R-8.
**Motion/PRM**: no motion-quarantine rows are implicated by this corpus; no M-xx id is cited and none is carried (nothing to launder).
**Status verbs**: every disposition below is ADJUDICATED, not VERIFIED — nothing here stamps a wave.

## Scoreboard

**16 CONFIRMED (1 blocker-class by identity, 2 MAJOR, 12 MINOR, 1 INFO) · 15 KILLED · 4 UNPROVEN-NEEDS-LIVE.**
The two heaviest truths of this corpus were both **already on the books under other names** — the `none`-throw ≡ picker-componentsliders **R-2** (BLOCKER, → X-W5/X-W9/X-W1) and the hover-unreachable mount ≡ ColorInput **D-15** (CONFIRMED, cure decided in that wave; ledger 45582 already prescribes persistent placement). Both re-readers presented them as new blockers; the identity guard is this seat's principal correction. The leaf itself is small, right-sized, and correct on several subtle points its accusers missed (see superlatives).

## Ruled collisions (the seat's actual work)

| # | collision | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R①| the `--ink-muted` bare read | SUPERLATIVE — the ONLY reference-correct site; fond normalizes TO it | missed-6: a DEFECT — sole site with no fallback | **reader-1 WINS; reader-2's row KILLED (K-13)** | DEFECT-LEDGER:63610 rules the two-arm form a MAJOR edict-2 masking fallback ("second arm provably dead… the masked value is the uncertified static one that failed the text floor"); :98689's cure is literally "Read `var(--ink-muted)` bare… a fond-level call to be made once"; EmptyState **ES-13** already adjudicates the whole seam ("two bare reads (ParseEchoReadout) prove the mask optional") — plate-side dedup → X-W7, static declaration NO-WAVE-OWNER. Reader-2 prescribed re-introducing the ruled-MAJOR idiom. |
| R②| M2's epistemic status | statically PROVEN BLOCKER | UNPROVEN-NEEDS-LIVE ("could not be settled by static reading") | **SETTLED STATIC — CONFIRMED, demoted BLOCKER→MAJOR (A-2)** | This seat's own producer-byte reads settle reachability without a browser: `usesHoverRoot` fine-pointer branch → reka HoverCard; `@focus`/`@blur` land via as-child on the `cursor-default` div at ColorInput.vue:10 (no tabindex; native focus does not bubble; Vue binds `focus`, not `focusin`); content tabbables forced `tabindex="-1"`. Demotion grounds: (a) identity already booked at ColorInput D-15 + ledger 85226/45582; (b) reader-1's uniqueness amplifier is FALSE (K-14). |
| R③| D-3 (the verdict's voice) | CONFIRMED (no consequence/recovery) | REFUTED (quiet-register claim factually false) | **RESCOPED — both half-right (A-8, K-9)** | The register half is false: `.gamut-verdict[data-clips="true"]` (:46-48) is a materially different voice. The consequence half is true: no mapping/clipping consequence surfaces though `toRgba8(gamut:"clip")` (picker-color.ts:214) and `mapPickerOklabToSrgb` are the app's own policy. And BOTH missed the sharpest fact about the amber: it violates the written gold law (see A-8) — the cure replaces it with `--gamut-edge`, mooting the register debate. |
| R④| D7/C5 severity | MAJOR | MINOR | **MINOR (A-3)** | `flashParseError` (useColorParsing.ts:54-58) bounds the contradiction window to 2 s per rejected edit; both surfaces are individually honest (the badge about the rejected text, the echo about the standing model); the surface that co-displays them is itself the A-2 defect. The live repro stands (DEFECT-LEDGER:86342) — the association is absent, and the cure is one identifier off the same key (`parseError` at useColorPipeline.ts:312, three lines above `astEcho`). |
| R⑤| gamutVerdict's `none` exposure | "measured for all five inputs" (all non-powerless specimens) | "the same exposure" as astEcho, both routes | **REFINED — reader-2 overstates Route A** | This seat's node run: `hsl(none 0% 0%)` (powerless hue — the Route-A product) converts to oklab **ok** (operations.ts:22-31 tolerates hue-`none` iff powerless). On Route A (pick black/white/grey → switch to hsl/hsv/hwb/lch/oklch) **only `astEcho` throws** (`channelNumber` — picker-color.ts:156 — is `none`-intolerant on every channel); on Route B (typed non-hue `none`) both throw. The verdict (A-1) is unchanged — the template reads `astEcho` first at :4 — but R-2's enumeration must carry the asymmetry. |
| R⑥| C1/L1 (inject assertion) | INFO, non-defect | REFUTED outright | **NON-DEFECT — one killed row (K-3), both refutations merged** | Parent asserts the same key first (ColorInput.vue:155) so the leaf can never be the failure point; key doubly provided (App.vue:257 + ActionBarLayer.vue:22); house idiom at 7+ consumers (ledger 45901). The guard-cure is edict-2-banned; the real structural act is already ledgered (45906: delete the re-provide) and is NOT this leaf's row. |
| R⑦| the identity guards neither reader ran | M1/M2 filed as NEW blockers; ColorInput dead-code filed as a new missed row | same for M1 | **THREE PRIOR IDENTITIES BIND** | (1) M1 ≡ **picker-componentsliders R-2** — already CONFIRMED BLOCKER, already routed (X-W5 demo Result propagation + X-W9 library half + X-W1 none-URL gate), and its warm arm already cites `useColorParsing.ts:75` committing the `none` color. (2) M2/M3 ≡ **ColorInput D-15** — already CONFIRMED with the producer-contract decider ("no focus mode exists") and an in-wave cure decision; ledger 45582 already prescribes the persistent region. (3) reader-2 missed-9 (ColorInput dead surface) ≡ **ColorInput D-9**, already gated (G9) with the canProposeName sweep caveat — out of this component's scope besides. Anti-rename law: ONE identity each; this record cross-books, it does not re-book. |

## Adjudicated defect roster

Line anchors are HEAD `027ca138`; re-resolve before executing any cure (the D-19 lesson).

- **A-1 · BLOCKER-class fact, identity R-2 · FOLD → R-2 → X-W5 (+ X-W9 library half; X-W1 none-URL gate)** — both producer computeds THROW on CSS Color 4 `none` channels the app itself parses, commits, and stores, and this leaf is their only consumer, rendering outside any error boundary. Chain, every link this seat's own: grammar.ts:135 accepts `none`; node-vs-dist: all specimens parse + serialize; same-space conversion (operations.ts:44-46) preserves `none`; `parseAndSetColor` commits it (useColorParsing.ts:60-75; the serialize equality check at :69 survives `none`); the write gate's clamp preserves it (valueDomain.ts:47-49 → clampPickerColor, picker-color.ts:197 `if (channel === "none") return channel`, :202 same for alpha); Route A needs no typing (ColorPicker.vue:348-354 watcher → updateToColorSpace, useColorPipeline.ts:170-173 — cross-space convert mints hue-`none` via finishChannels/isPowerless, same-space re-convert preserves it). Then `astEcho` → `channelNumber` → `throw PickerColorError("Missing …")` (picker-color.ts:156) at useColorParsing.ts:97, and on Route B `gamutVerdict` → `convertPickerColor(…, "oklab")` → `color_missing_channel` → `valueOrThrow` rethrow (useColorParsing.ts:108; picker-color.ts:104-107). Read during render at ParseEchoReadout.vue:4/:7/:20. No boundary: ErrorBoundary wraps only `<main>` (App.vue:47-50…:141); the dock mount (ActionBarLayer.vue:115 → ColorInput.vue:109) sits outside it. **All three r2 axes asserted the opposite** (C:11-12 "not presently reachable"/"always present", D:11 "always-present", L:11 "coherent today") — the corpus's largest shared error. Disposition: FOLD into R-2's X-W5 row, ADDING to its enumeration: `astEcho` (useColorParsing.ts:97) and `gamutVerdict` (:108) as throw sites, with the R⑤ asymmetry (Route A throws astEcho only). In-tree contradiction preserved: valueDomain.ts:21-22 declares `none` pass-through "the library's law" and test/v4-c1.test.ts:186-192 pins it as intended — while `channelNumber` treats it as a hard error.
- **A-2 · MAJOR (demoted from reader-1's BLOCKER), identity D-15 · FOLD → ColorInput D-15 → X-W4** — the readout's sole mount is inside a hover-triggered popover that keyboard cannot open on fine pointers and that mis-stamps dialog ARIA + a click toggle onto a non-focusable div on coarse pointers. Fine-pointer arm (M2), settled static by this seat (R②): focus handlers land on the tabindex-less wrapper div; content tabbables forced to −1. Coarse arm (M3), verified in both producer layers: reka PopoverTrigger merges `aria-haspopup="dialog"`/`aria-expanded`/`aria-controls`/`@click=onOpenToggle` onto the div via as-child; the `type:"button"` normalization is skipped in the as-child branch (dist verbatim) — so tapping the field to type toggles the readout, and AT is told a plain div opens a dialog. Cure already prescribed on the ledger (45582: persistent region placement, "decided in-wave" per ColorInput.md §wave). A-16 (the nameless `role="group"`, INFO — glass PopoverContent accepts `ariaLabel`, ColorInput.vue:94 passes only `class`) rides this row and becomes load-bearing the moment any popover form survives.
- **A-3 · MINOR · leaf-rewire arm (ColorInput wave §"ParseEchoReadout rewired") → X-W4** — the readout has no relationship to the parent-owned invalid-input state: a rejected edit leaves the last valid echo on screen while the badge (ColorInput.vue:87) reports rejection, and the popover additionally asserts `serializePickerColor(currentPhysicalColor)` (:104). `parseError` is exported from the IDENTICAL key three lines above the members the leaf already takes (useColorPipeline.ts:312 vs :315-316); the cure is one identifier at ParseEchoReadout.vue:31 plus an authored invalid-state association. Live repro on the ledger (86342). Severity per R④.
- **A-4 · MINOR · co-lands with A-1 → X-W5** — the root and both branch `v-if`s (:4/:7/:20) are permanently truthy (neither computed has a falsy return: useColorParsing.ts:104/:116) — three dead guards that ADVERTISE a defended absent-state posture while the real failure mode is a throw no `v-if` can catch (reader-2's sharpening, adopted). When R-2's cure makes the producers total (`Result`-shaped), the guards become real or die; they may not survive as decoration.
- **A-5 · MINOR · leaf rewire → X-W4** — `astEcho` names something that does not exist: the producer regenerates space id + formatted strings from the normalized model (`model.value.color`, :95); the parse product is discarded at :64-75; and — reader-2's sharpening, verified — after any space switch the echo reports the DISPLAY space, not the typed one (useColorPipeline.ts:170-173 rewrites the model's space). The misnomer is propagated into product comments (ParseEchoReadout.vue:2, ColorInput.vue:107 "AST + gamut verdict"). No user-visible string says "AST"; harm confined to identifiers/comments. The rename rides the already-named rewire arm.
- **A-6 · MINOR · leaf rewire → X-W4 (identity note: CC-068's catalog later owns the mapping — no re-book)** — the space label prints raw machine ids through CSS `uppercase` (`A98-RGB`, `SRGB-LINEAR`, `REC2020`, `OKLCH` — :12, from useColorParsing.ts:104) while `PICKER_SPACE_NAMES` (picker-color.ts:72-90; live, consumed at color-model.ts:10/:76) sits in the same module the producer already imports. Type cause verified: `computed<{ space: string; … }>` at :94 widens `SpaceId` to `string`, forfeiting the exhaustive-lookup pressure. Same color: "OKLCh" in the selector, "OKLCH" in the readout.
- **A-7 · MINOR · FOLD → X-W5 (the alpha arm of R-2's none-total read)** — alpha `none` renders as fully opaque: :101 gates the α row on `typeof color.alpha === "number"`, so `oklch(0.6 0.2 30 / none)` (parses, commits, serializes — this seat's node run) produces a readout indistinguishable from alpha 1. The alpha row is a hand-rolled special case (alpha absent from PICKER_CHANNELS) — precisely how the branch went unwritten.
- **A-8 · MINOR · BUILD → X-W6 (beside CC-068's gamut-mark vocabulary; canon echo X-W10)** — the out-of-gamut verdict paints `--color-gold`, violating the written token law twice over (DESIGN.md:336 "Measurement chrome only: the overlay reads in ink + the live color, never gold"; :342 "Gold stays OUT of the measurement chrome (overlay/readout/thumbs)") while the certified vocabulary sits unused (`--gamut-edge`/`--gamut-hatch`, foundation.css:292-297, already consumed by ComponentSliders). The dead `orange` second arm (`var(--color-gold, orange)` at :47; `--color-gold` unconditionally declared at foundation.css:104) — itself the edict-2 class — dies in the same one-line edit. This row also carries D-3's surviving half: the verdict states a fact with no stated consequence and no recovery action, though `toRgba8(gamut:"clip")` is the app's own policy — the X-W6 vocabulary re-author states the consequence.
- **A-9 · MINOR · FOLD → X-W4 (decided at the A-2 placement decision)** — no live-region/atomic policy (:3-24 carry zero ARIA), and the update paths are real (2000 ms trailing debounce — useColorParsing.ts:92; immediate Enter — ColorInput.vue:202-210; every slider write). CONFIRMED as source fact, MISFRAMED as a standalone cure: announcing content that AT cannot reach (A-2) treats the symptom. The announcement policy is authored where the placement is decided, not before.
- **A-10 · MINOR · BUILD → X-W6 (one formatter home; cross-note CC-070's pretty-formatting facility)** — `astEcho`'s formatter is a verbatim duplicate of `currentColorComponentsFormatted`'s (useColorParsing.ts:96-100 vs useSliderGradients.ts:70-75 — same iteration, same accessor, same `meta.unit === "%" && meta.max <= 1` predicate; this seat read both). One extraction would also have given the `none`-throw a single cure site — the duplication is why two surfaces share A-1's defect.
- **A-11 · MINOR · BUILD → X-W1 (joins R-2's none-URL gate in the re-gate wave)** — zero test coverage for the component and both producing computeds (this seat's grep: three product files, no test/e2e hits), despite `gamutVerdict` carrying epsilon logic that has ALREADY regressed once (R/PROGRESS.md R.W4 row: "the gamut verdict computed on display-denormalized OKLab, Δ inflated ~500× — cured to RAW OKLab"). A cured-then-unpinned regression is a live risk; the pin rides X-W1.
- **A-12 · MINOR · FOLD → X-W6 (beside the MT-CSS-2 COLOR_MODEL_KEY role-split identity)** — two presentation-only computeds live in a PARSING composable and ride the injected pipeline bag to every consumer; exactly one reads them. Count corrected by this seat: the return literal is **38 keys** (reader-1 said 35, reader-2 ~40; 38 matches the ColorSpaceSelector arbiter's independent count). The obligation meanwhile is negative: no new consumer of the bag.
- **A-13 · MINOR · leaf rewire → X-W4** — width-unstable, remount-happy instrument: `Number(display.toFixed(3))` (:99) strips trailing zeros so part lengths churn on every write; the strip is `flex-wrap justify-center` (:7) so wrap points move; and `:key="part"` (:15) keys by VALUE, failing `isSameVNodeType` for every child on every update — full span unmount/remount instead of a text-node patch. Challenge-C read this exact line and asked only whether keys could collide (K-4).
- **A-14 · MINOR · leaf rewire → X-W4** — mixed unit registers in one strip: percent-scaled channels beside a bare-fraction Greek-keyed alpha (`α 0.827` — :102 — the one part with no unit and no CSS channel letter, in a `fira-code` strip), and the kelvin row's unit restating its key (`kelvin 6500K`, picker-color.ts:62).
- **A-15 · MINOR · leaf rewire → X-W4 (canon echo X-W10)** — prose: `outside srgb gamut`/`in srgb gamut` (:21-22) mis-spell sRGB against the house spelling — including this file's OWN comment at :41 — and the in-gamut branch spends a row on a tautology against the §5 law-6 canon this very popover is already cited under (ledger 45487, "subtraction precedes explanation").
- **A-16 · INFO · rides A-2** — the nameless `role="group"` (mechanism verified in dist; ColorInput.vue:94 passes no `ariaLabel`). Moot while A-2 stands; load-bearing the moment it is cured.

**Identity-guard record (not roster rows)**: reader-2 missed-9 (ColorInput dead surface: `editTarget`, the 5-member `defineExpose`, `cssColorOpaque`/`canProposeName` destructures, `copyAndSetInputColor`) is CONFIRMED-ELSEWHERE ≡ ColorInput.md **D-9**, already gated (G9) with the live-`actionBar.canProposeName` sweep caveat — one identity, not re-booked here.

## Killed-claims register

- **K-1 — the totality premise (C:11-12, D:11, L:11 "always-present…coherent today")** — FALSE; both computeds throw on committed colors (A-1's chain, this seat's node runs). The corpus's shared premise, killed by measurement.
- **K-2 — C3/L3's revision-contract cure** — speculative generality against a hazard the axes themselves label hypothetical ("IF either computation later becomes asynchronous"); both values are plain computeds over ONE `shallowRef` with one synchronous write gate (useColorPipeline.ts:59-71, its own :70 comment states the property). Pure ceremony under the standing KISS law.
- **K-3 — C1/L1 as a defect + the provider-guard cure** — non-defect (R⑥): unreachable through the only mount path (parent asserts first, ColorInput.vue:155), doubly-provided key, house idiom at 7+ consumers. The guard cure is separately unsound: a masking fallback on a "what color is this" readout is the exact class edict 2 forbids (ledger 63610); the structural act is 45906's delete-the-re-provide, already booked.
- **K-4 — C2 (value keys might collide / need typed channel IDs)** — collisions structurally impossible: `PICKER_CHANNELS` is `Object.freeze(… satisfies Record<SpaceId, readonly ChannelMeta[]>)` with pairwise-distinct keys per space (picker-color.ts:52-70, re-read whole); parts are key-prefixed (:99); `α` is disjoint. The axis looked at the right line and asked the wrong question — the real keying consequence is A-13.
- **K-5 — C7 ("unknown spaces would fail upstream at the lookup")** — both halves false: an unknown space is unrepresentable (`satisfies` totality), and the lookup is not upstream — it is at useColorParsing.ts:96, inside the very computed the leaf renders. An invented failure mode, mislocated.
- **K-6 — L2 ("cannot be rendered/tested… ")** — false on "cannot": the component destructures exactly two keys; a test provides `{ astEcho, gamutVerdict }` under `COLOR_MODEL_KEY` with one cast. The genuine gap is the ABSENCE of any such test — restated as A-11.
- **K-7 — L4 (shared instrument/status primitive)** — no referent exists in glass-ui 7.0.0 or demo/; wrapping a two-word caption in Badge/chip adds the chrome the design removes. Generic prescription; the concrete conformance strays are A-8 (gold) and the eyebrow row of ES-13's fond (see superlative S-1's cohort).
- **K-8 — L6's boundary framing ("the leaf boundary CANNOT represent or associate that state")** — false: `parseError` rides the identical injection key, three lines from the members already taken (useColorPipeline.ts:312/:315-316). The boundary is not the obstruction; the wiring is absent. The defect half survives as A-3; the diagnosis and its adapter-cure die.
- **K-9 — D-3's quiet-register half** — factually false against :46-48 (65% gold, own rule, `data-clips` hook, lexically distinct states). Its own §6 contradicts its §3 within one document. Consequence half survives inside A-8.
- **K-10 — D-5 (terse notation unexplained)** — the notation is the app's channel vocabulary, minted by the same expression that labels the sliders behind the popover (useColorParsing.ts:98 ≡ useSliderGradients.ts:73, character-for-character — this seat read both). Explanatory copy in a hover caption strip is unfalsifiable taste against the KISS law. The duplication it walked past is A-10.
- **K-11 — D-6's forced-colors half** — proven-by-design, not unproven: foundation.css:665-706 is the documented two-tier WHCM policy; `.gamut-verdict` is deliberately tier-2 chrome (not on the tier-1 roster this seat re-read), and the state survives because the branches differ in TEXT (:21-22), not color alone. The contrast half is genuinely undecidable statically → U-1.
- **K-12 — the D/L "Target" blocks (`NormalizedColorReadoutModel`, adapter layer, revision protocol, shared live primitive)** — contrivance refused by standing owner law (`feedback_kiss_no_contrivance`, `feedback_no_god_modules`, the "overfit junk" precedent): a new type system for a 49-line leaf whose real cures are one identifier (A-3), total producers (A-1), and one rewire arm already named in the ColorInput wave. All three axes converged on the same over-build because none read the mount site.
- **K-13 — reader-2 missed-6 (`--ink-muted` needs the fallback)** — killed by ruled law (R①): the prescription IS the ruled-MAJOR masking idiom (ledger 63610); the bare read is the certified direction (98689; ES-13 adopts it as the in-tree counterexample). The residue reader-2 was reaching for (a static declaration for the boot-stamped token) is ES-13's already-booked NO-WAVE-OWNER row — not this leaf's defect.
- **K-14 — reader-1 M2's uniqueness amplifier ("`grep -ril gamut demo/` returns only ColorNutritionLabel.vue"; "the only surface that reports sRGB-clipping status")** — the receipt is FALSE: this seat's identical grep returns **29 files**, and the clipping truth has a drawn twin by construction — the spectrum truth-line overlay + p3 chip, whose agreement with this very verdict is the R.W4 record's own sentence ("the typed verdict and the drawn contour agree by construction"). The reachability mechanism survives (A-2); the BLOCKER inflation and the fabricated-or-botched receipt do not. Recorded as evidence hygiene: a false grep receipt inside an otherwise-correct finding is how citation-inheritance starts.
- **K-15 — reader-1 unproven-5 (the `--ink-muted` first-paint window) — RETIRED for this leaf** — the sole mount is user-hover-gated (300 ms open delay) behind app boot, and the boot writer is a `watch(…, { immediate: true })` (useAtmosphereBoot.ts:100-106, this seat's read); a hover cannot precede the watcher. The token-declaration residue lives at ES-13, where it is already booked.

## Superlatives (with provenance, adjudicated)

- **S-1 — the reference-correct token read** (reader-1, ADOPTED; reader-2's contrary row killed at K-13). `.echo-ink { color: var(--ink-muted); }` (:37-39) is the in-tree proof the masking arm is optional — the fond normalizes TO this file (ledger 98689; ES-13).
- **S-2 — a provenance comment that prevents a regression** (both readers). :8-11 records the dead `/70` alpha post-multiply, cites the ruling (D6, T.W3-5/A11Y-F4), and states the replacement principle — hierarchy by register, never by lowered contrast. Traceable to the ink-lane records.
- **S-3 — state in the DOM, severity in the CSS** (both readers, incl. the subtlety). `:data-clips` (:20) + `[data-clips="true"]` (:46) — and correct on the Vue detail: `data-clips` is not a boolean attribute, so `false` serializes and stays addressable.
- **S-4 — the verdict is exact, not approximate** (reader-2, VERIFIED by this seat). operations.ts:146 returns `ok(color)` — the SAME object — when in gamut, so the in-gamut delta is bit-exact zero and the 1e-6 epsilon never fires spuriously; the 32-step bisection is paid only by clipping colors. A cured design with its regression on the record (R.W4).
- **S-5 — zero leak surface, structurally** (reader-2). No lifecycle hook, timer, listener, watcher, ref, or DOM handle in 49 lines; born of the R.W4 gate-(c) size-cap decomposition (`9675ef3`) — right-sized by documented discipline.
- **S-6 — the two states differ by TEXT** (reader-1). The verdict survives CVD and WHCM tier-2 substitution with its information intact — the amber was already non-load-bearing before A-8 removes it.
- **S-7 — token-whole wrapping** (reader-1). `flex-wrap` + per-part `whitespace-nowrap` (:7/:16): `h 200deg` never splits at narrow dock widths.
- **S-8 (demoted from reader-1's roster) — the gold mix's craft** — `color-mix(in oklab, var(--foreground) 35%, …)` is perceptually the right interpolation and theme-tracking; the CRAFT is real, the canon assignment is wrong (A-8). Preserved as craft, not as endorsement.
- **S-9 — honest self-limitation in the r2 corpus** (reader-1). Challenge-C repeatedly stated hazards AND recorded their unreachability; it was wrong about reachability (A-1) while showing its work — which is exactly why the error was findable.

## UNPROVEN-NEEDS-LIVE (visual-audit inputs)

- **U-1** — contrast of the clips-state ink against the live `glass-floating` popover plate, both themes — undecidable statically (backdrop-filtered composite over the atmosphere canvas; `--ink-muted` boot-stamped). SURVIVES A-8's cure: the successor `--gamut-edge` (45% ink mix) needs the same live floor check on THIS plate. The surviving half of challenge-D-6.
- **U-2** — rendered blast radius of the A-1 throw (Vue comment-node degradation vs wider dock cascade; no boundary in the path — proven). Repro: pick black → switch space to HSL → hover the color input. (The slider console's parallel failure belongs to R-2's own record.)
- **U-3** — iOS Safari tap ordering on the coarse-pointer toggle (A-2/M3): whether the reka click toggle fires before or after the contenteditable takes focus — open or closed after a tap-to-type.
- **U-4** — screen-reader voicing of `α`, `200deg`, `6500K`, and the group name — moot until A-2's cure lands; then load-bearing.

## Closing verdict

**ADJUDICATED — SOURCE-RED at the mount and in the producers, not in the leaf's own 49 lines**: a readout whose only data path throws on colors the app itself commits (identity R-2 → X-W5/X-W9/X-W1), mounted behind a hover surface keyboard cannot open and touch toggles by accident (identity D-15 → X-W4), wearing one canon-violating ink (→ X-W6) — while the leaf itself is right-sized, leak-free, and reference-correct on the very token its cohort gets wrong. 16 confirmed (1 blocker-class by prior identity, 2 MAJOR, 12 MINOR, 1 INFO) · 15 killed · 4 live residues to the visual-audit lane. Three prior identities bind (R-2, D-15, ES-13/D-9); nothing is re-booked under a second name.

— end of apotheosis. The r2 challenge files and both re-reader returns remain immutable beside this file per E-1/E-3; this file is new, dated evidence untouched.
