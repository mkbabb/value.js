# shell-dock-mobilemenudropdown — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · served model: `claude-fable-5` (Fable 5)

**Seat**: incredulity adjudicator over two hostile re-readers — reader-1 (design-and-usage) · reader-2 (library-and-code) — who each re-read the full challenge corpus. Model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **HEAD**: `c654824e` · branch `tranche-u` · **Subject**: `demo/shell/dock/menus/MobileMenuDropdown.vue` (115 lines) + its byte-twin `ProfileSection.vue` where a finding is shared.
**Corpus (r2 governs)**: `audit/components/shell-dock-mobilemenudropdown/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md` (read whole by this seat) + committed evidence `frames/*.json|png`, `probe/*` (incl. `orphan-2-after-resize-1440.png`).
**Method (L-18)**: both reader returns were attacked before either was adopted. Every verdict-level disagreement (D-05(b), D-01 severity+specimen, L-1 census, C-3/L-4 severity, D-11 severity, the two toolchain claims, reader-2's four new missed rows) was re-derived against the tree with this seat's own instruments. One reader-2 missed row is KILLED by this seat; one reader-2 evidence cell (the "REAL slug") is killed by derivation from the server word lists.
**Arbiter's own instruments (all static, zero browser — probe parsimony)**: full reads of MobileMenuDropdown.vue (115) + ProfileSection.vue (181) + useUserAuth.ts:80-130 + useSlugMigration.ts:88-130 + usePalettePorts.ts:50-60/:120-140 + useAdminAuth.ts (whole) + usePaneRouter.ts:60-80 + useDockAdminMode.ts:55-70 + Dock.vue:1-6/:30-88/:205-224 + SlugEditLayer.vue:64-74 + shell/dock/index.ts + color-session/keys.ts + useContrastSafeColor.ts:296-325 + useAtmosphereBoot.ts:91; producer bytes: `primitive-B2t2jdqM.js` (whole — the as/asChild/as-child strip Set), `components/_shared/menu.css` (the `@layer components{…}` block CLOSES before the bare `.dropdown-menu-content{font-family:var(--dropdown-menu-font,inherit)}` rule — unlayered, verified), `components/dropdown-menu/styles.css` (`font-size:var(--dropdown-text)`; **zero** `max-inline-size|max-width` in either menu CSS file), `dock/styles/controls/touch-floor.css` (whole), `styles/utilities/base-misc.css` `.gold-shimmer` (both rules; no `!important` on color), `styles/typography/semantic.css` (`text-small` = flat `--type-small` + `--font-text`; `text-micro` sets size only), `styles/tokens/sizing.css` (`--ui-scale:1`, `--ui-coarse-scale:1.5`, `--control-text`, `--dropdown-text`), `styles/tokens/light-dark.css` coarse flip, `styles/glass/reveal.css` `[data-reveal="menu"]` + `motion-registers.css` (`--exit-overlay-duration: 0.15s`); demo bytes: foundation.css:1/:56/:118/:374 ("PROJECT OVERRIDE … **triggers**")/:445/:583-587; canon verbatim: VISUAL-CONSTITUTION §3.2 (the 400%-arm law + the 1440/390/320 arms), §4 (closed matrix; `prose/help → text-prose → Plus Jakarta Sans`; dropdown options → `text-small`), §6 ("exit is shorter than entry"), PROPORTION-AUDIT PR-16 + §5.5/§5.7/§5.8; censuses: `ls demo/ui`=19 / `wc -l`=29 / `DropdownMenuCheckboxItem|Group|Shortcut|AvatarFallback` consumers = 0/0/0/0 / glass exports map (74 keys; `./chip` YES, `./dropdown-menu` YES, `./avatar` NO) / `vueCompilerOptions|strictTemplates|checkUnknownProps` across all four tsconfigs + package.json + vite.config.ts + eslint.config.js = **0 hits** / `\blogout\b` minus userLogout → definition+export only, **zero call sites** / coverage grep `MobileMenuDropdown|aria-label="Menu"|Regenerate slug` over test/ e2e/ = **0** / eslint.config.js:232-238/:275-276 dead `demo/@` globs / slugWords.ts: four 128-word lists, longest 10/9/9/9 → **40-char production ceiling**, and `amaranthine|quokka|northern-marches` = **0 hits**.
**Registry cross-reads (anti-rename)**: `registry/adjudicated/Dock.md` L-6 (SESSION_PORT_KEY, BUILD at MT-DOCK-LAYERS-1, five consumer files incl. this one) · DELTA-3 (dead lint globs → W-HYGIENE) · DELTA-5 (four breakpoint predicates, BUILD) · DELTA-10 D3-06 (this file's `../ui/dropdown-menu` edge + the 55px GitHub row, carried labeled) · DELTA-11 (RTL wordmark, G-Q) · DELTA-14 D2-06 (dark accent 0.188→0.021 → convergent design loop) · L-19 (admin gold LATCH — distinct from the pill shimmer) · CC-083 (Dock.md arms → **X-W8**); `AdminAuditPanel.md` AAP-18/AAP-28 (dead-glob + demo/ui identities → X-W8); X-W4 scope 7 (the nine `?.()` dispatches); X-W5 gate A3 (mobile/desktop Regenerate parity; 38 breakpoint forks / 13 files); X-W1 (re-gate estate).
**Motion quarantine (M-xx law)**: this subject appears in **none** of motion-quarantine.md's 34 rows (§7.4 covers other components); **no FALSE M-row is carried here**. The one motion claim in this corpus (D-17) is killed below on this corpus's own producer bytes, consistent with MQ §S-1 (motion governed centrally).

## Scoreboard

**31 rows ADJUDICATED-CONFIRMED (2 BLOCKER · 14 MAJOR · 11 MINOR · 4 INFO/rider) · 11 claims KILLED · 9 rows the axes MISSED, booked by the readers and ratified (one reader-2 missed row killed by this seat).** Dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave.

Blocker-weight survivors: **MMD-1** (the `as-child` GitHub row — glass-ui actively STRIPS the prop; ~73% dead zone, keyboard-dead, at two sites) and **MMD-2** (`Regenerate slug` destroys the persisted identity BEFORE the replacement is confirmed, no catch, no error surface — the corpus's deepest chain-walk).

## Ruled disagreements (the seat's actual work)

| id | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-05(b)** `font-display` dead on the content | CONFIRMED MINOR with mechanism (`--dropdown-menu-font` unlayered rule) | UNPROVEN — "no competing font-family rule anywhere"; evidence "cuts AGAINST it" | **CONFIRMED — reader-1 wins; reader-2's negative grep is REFUTED** | The competitor exists and I read it in bytes: `_shared/menu.css`'s `@layer components{…}` block closes, then a **bare** `.dropdown-menu-content,.dropdown-sub-content{font-family:var(--dropdown-menu-font,inherit);…}` — unlayered, so it beats `@layer utilities`' `.font-display` unconditionally; foundation.css:374 supplies `--dropdown-menu-font: var(--font-mono)` under a comment claiming the override targets "**triggers**" — the var lands on the CONTENT. Explains the measured `Fira Code` on `[role=menu]` exactly. Reader-2 grepped a minified single-line file for a selector spelling it does not use. Bonus defect surfaced: the project override's stated intent and its landing site disagree — an authored decision to re-state explicitly. |
| **D-01** severity + specimen provenance | MAJOR; 62-char arm synthetic; "the measured 35-char arm … a genuine slug" | BLOCKER; "the 320px and 200%-zoom arms rest on a REAL slug" | **MAJOR — reader-1's severity; BOTH provenance cells corrected** | My derivation: slugs are `adj-verb-color-animal` over four 128-word lists whose longest members are 10/9/9/9 → **40-char hard ceiling**; `amaranthine`, `quokka`, `northern-marches` are in **no list** (grep = 0) — the 35-char probe specimen is ALSO seeded, not server-mintable. Reader-2's "REAL slug" cell is false; reader-1's "genuine slug" phrasing equally so. The finding survives **a fortiori**: 35 < 40, and the measured 349.5px at 35 chars already exceeds a 320px viewport with `docScrollW === docClientW` (unreachable) — the production worst case (~393px by the corpus's ~8.7px/char) is strictly worse. The 200%-zoom arm stays killed (canon: "The 400% arm is the live routed page at actual in-app Browser zoom, not a substituted CSS-width or responsive-emulation frame" — VISUAL-CONSTITUTION §3.2, verbatim). BLOCKER is not sustained: the canon-bound failure is the 320 arm with ~30-75px lost, the menu's rows still operable; the identity pill is what clips. |
| **L-1** census + cure | CONFIRMED MAJOR ("5 shell→palettes edges, all this Symbol; sole reverse ref type-only") | census+cure KILLED (3 more runtime edges via `usePaneRouter` dynamic imports; cure breaks the pane registry) | **CONFIRMED as a FOLD; reader-2's corrections ADOPTED; the identity is Dock-adj L-6** | My read of usePaneRouter.ts:69-78: `defineAsyncComponent(() => import("../palettes/{PalettesPane,BrowsePane,admin/AdminPane}.vue"))` — three lazy shell→palettes runtime edges the eager-closure walk cannot see. So L-1's "then `shell -> palettes` drops to 0" is false and its blanket `no-restricted-imports` ban would break the shell's own route registry. The Dock adjudication's L-6 (BUILD: platform owns KEY + port TYPE; gate = *value-import files* 5 → 0) survives reader-2's kill **as written** — its gate counts value-import statements, which the pane registry does not add to. ONE identity, folded, never re-booked. |
| **C-3 / L-4 / D-18** severity | MINOR | MAJOR | **MAJOR-as-family, FOLD** | The CSS-only split is one limb of Dock-adj DELTA-5 (four disagreeing breakpoint predicates — BLOCKER there, BUILD'd) and of X-W5's A3 parity gate (38 forks / 13 files, MEASURE-AT-OPEN). Severity attaches to the owning row; this record contributes the 78.7% fork measurement (bit-for-bit reproduced by reader-2) and the orphan witness. Reader-1's demotion of the orphan render to UNPROVEN is **partially overruled**: `probe/orphan-2-after-resize-1440.png` + the probe transcript are committed evidence (chromium cell, labeled); what stays live-only is cross-engine behaviour. |
| **D-11** severity (items 1-2) | MINOR | MAJOR | **MINOR** | Two missing separators are boundary rhythm; the hazard they compound (destructive row flush against Logout) is carried at full weight by MMD-2/D-07 and the new muted-ink row (M-B). Booking the same danger twice at MAJOR would double-count one consequence. |
| **reader-2 missed-4** — "`text-small` … silently disabling glass-ui's `--ui-scale` lever for every menu row" | (not filed) | MAJOR missed row | **KILLED by this seat** | Mechanism inverted. The producer's `.dropdown-menu__item{font-size:var(--dropdown-text)}` is **unlayered**; `text-small`'s `font-size:var(--type-small)` lives in `@layer utilities`. Unlayered beats layered — the producer's density scale **wins**, which is precisely why reader-2's own cited witness reads 21px (= 14 × the coarse 1.5) instead of a flat 14px. `text-small` governs family+weight only (any declaration beats inheritance). The true residue — the utility's size leg is dead code on menu rows — folds into M-A (the cascade-layer law), where the mechanism is stated the right way up. |
| **L-3/C-2 "nothing in the toolchain can catch it"** | (carried) | KILLED (vueCompilerOptions absent; strictTemplates would flag it) | **KILL SUSTAINED; the cure DEMOTED to a spike** | My grep: zero `vueCompilerOptions|strictTemplates|checkUnknownProps` across all four tsconfigs + package.json + vite.config.ts + eslint.config.js — the check exists and was never asked for, so the impossibility claim dies. But "one key, repo-wide, self-enforcing" is not yet a fact: strictTemplates also flags legitimate fallthrough attributes (e.g. `aria-label` on the `inheritAttrs:false` DockTrigger at :40 — the exact idiom this file uses CORRECTLY), so blast radius is unmeasured. Adopted as rider R-1 (spike, born-RED candidate), not as a fact. |
| **C-2 cure framing** ("producer gap") | (carried) | KILLED ("deliberate design — the strip helper") | **KILL SUSTAINED** | `primitive-B2t2jdqM.js` read whole by this seat: its entire body filters `new Set(["as","asChild","as-child"])` out of `useAttrs()`; the menu family then hardcodes `as:"div"`. reka's `MenuItem` DOES declare `asChild` — the producer built machinery to remove it. The BH relay must ask glass-ui to reverse a designed invariant (or ship a first-class link-row variant), not to fill an omission. C-2's blanket rejection of the consumer `@select` route is also killed — `DropdownMenuCheckboxItem` proves the producer ships row variants, and a `kind:"link"` row via `@select` is the L §5 shape. |

## Verdict register — adjudicated defect roster (id · severity · claim · proof · terminal disposition)

Line anchors are HEAD `c654824e`; re-resolve before any cure (D-19). "Twin" = the same bytes at ProfileSection.vue.

- **MMD-1 (C-2/L-3/D-02) — BLOCKER.** `as-child` on `DropdownMenuItem` (:92; twin :158) is actively STRIPPED by the producer; the anchor nests inside `role="menuitem"`; ~73% of the GitHub row is a dead zone that dismisses the menu; Enter navigates nowhere (WCAG 2.1.1 A); the row is 55.4px vs 44px siblings with the octocat stacked above the label (preflight `svg{display:block}` inside a non-flex anchor). Proof: d.ts prop set {disabled,textValue,inset,class}; the strip helper (read whole); reka MenuItem `.click()`-on-div; committed rects `frames/mmd-probe3.json` (deadFractionOfRow 0.732; `githubRowClick {newPages:0, menuStillOpen:false}`). **Disposition: NO-WAVE-OWNER (formation boundary) + glass-ui BH/BI relay re-framed per the kill above; consumer cure = `kind:"link"` row through `@select` (whole row live, ARIA-valid); the twin dies in the same touch; rider R-1 (strictTemplates spike) pre-empts the class.** Dock-adj DELTA-10 carries the 55px cell labeled; its subpath re-point does NOT cure this row — distinct identity.
- **MMD-2 (C-1) — BLOCKER.** `Regenerate slug` (:61; twin :87) is a fire-and-forget promise over a chain that clears the persisted identity BEFORE the replacement is confirmed, with no catch and no error surface. Chain re-verified line-exact by this seat: :61 discards the promise → useSlugMigration.ts:102 bare `await deps.userRegenerate()` (the >0-palettes branch's failures reach only `console.warn`, :110-114) → useUserAuth.ts `regenerate()`: `safeRemoveItem(localStorage, SLUG_KEY); clearPersistedToken();` **then** `await createSession()` which rejects on network failure and throws by hand on a missing slug. `logout()` (:85-93) is safe only because it swallows its own error — by accident, not design. No toast surface exists (vue-sonner retired). The in-memory ref keeps showing an identity that no longer exists on disk. **Disposition: NO-WAVE-OWNER — must be named at the formation boundary as the roster's top cure: `regenerate()` becomes register-then-swap (which is what its own comment already claims) + the port method becomes total. The seam is X-W8's structure (c) (`platform/auth/session.ts` + the same five consumer files); co-landing there is the natural cut, but no X wave currently claims the behaviour change.**
- **MMD-3 (D-01, mechanism + 320 arm) — MAJOR** (ruled above). No width law: producer clamps block only (zero `max-inline-size|max-width` in either menu CSS file — my grep); `min-w-menu` (:43) is a minimum (foundation.css:118/:445 = 11rem); `.slug-pill` (:585-587) has no truncation; `whitespace-nowrap` at :48. The 320 canon arm fails a fortiori of the 40-char production ceiling. **Disposition: NO-WAVE-OWNER — the cure is the producer `Chip` (exports map: `./chip` YES, verified) with producer-owned `max-inline-size` + middle-ellipsis + full slug as accessible name; one adoption co-closes D-14 and the D-04 treatment fork. The 62-char and zoom cells stay killed and are uncitable.**
- **D-03 — MAJOR.** Two copy verbs in one list; `Share color` confirms (`Copied!` swap, :88-90), `Copy slug` is mute; no `slugCopied` in the prop type (:16-19); handler SlugEditLayer.vue:68-70 `void writeClipboard(…)` discards the rejection path (insecure context/permission silent too). **Disposition: NO-WAVE-OWNER — one copy-feedback owner for both verbs and the twin (D's `useCopyFeedback` shape), never a second bespoke boolean prop.**
- **D-07 — MAJOR.** Zero pending/disabled/error anywhere (census 0, re-verified); :61 is the ONLY identity row without `@select.prevent` (:52/:88/:101 carry it; :55/:58/:61 do not), so selecting Regenerate closes the menu — and the pill at :47-50 inside that menu is the new slug's only mobile display surface; double-tap Logout fires two deletes. **Disposition: NO-WAVE-OWNER — canon (VISUAL-CONSTITUTION §7) names the Account Dialog as the owner of identity mutations; no X wave claims that composition. Named for the formation boundary together with MMD-2.**
- **C-4 — MAJOR.** The Dark mode row (:101-111; twin :167-177) is a boolean rendered as a stateless command — no `aria-checked`/`aria-pressed`, state only in an `aria-hidden` glyph, `@select.prevent` suppressing even the close signal (WCAG 4.1.2). Cure is free: `DropdownMenuCheckboxItem` is real, already re-exported by the demo barrel, **zero consumers tree-wide** (my census). **Disposition: NO-WAVE-OWNER (X-W4 is the semantic-controls class owner but does not list this file; candidate accretion flagged).**
- **D-15/C-5/L-7 — MAJOR.** The @mbabb card (:79-87; twin :144-152) is a bare `<div>` + `tabindex=0` anchor directly inside `role="menu"` — ARIA-invalid, unreachable by the roving focus, Tab-trapped; anchor 51.7×17.2. The hardcoded product identity (avatar URL/handle/tagline ×2) belongs in one platform module. **Disposition: NO-WAVE-OWNER; the RTL/bidi arm of the same block is ALREADY BUILT at Dock-adj DELTA-11 (G-Q) — not re-booked.**
- **D-08 (+ reader-2 missed-1 folded as ROOT CAUSE) — MAJOR.** An authenticated admin's whole mobile menu is `admin · @mbabb · Share color · GitHub · Dark mode` — no exit (:65-69; twin :95-99 equally actionless). Root cause ratified by this seat: usePalettePorts.ts:57 destructures only `{isAuthenticated, login}` from `useAdminAuth()` — **`logout` is dropped**; tree-wide census: definition + export, zero call sites; `adminToken` only ever transitions false→true, so useDockAdminMode.ts:62-64's "Exit admin mode on logout" watcher is **unreachable by construction**. **Disposition: NO-WAVE-OWNER for the surface; the dead-watcher fact is flagged to X-W8's `useDockAdminMode.ts` modify-carve + Dock-adj L-19's derivation cure (same file, adjacent mechanism — the wire-up decision alters the port surface R-6 froze, so it is a formation-boundary decision, not a silent rider).**
- **L-6/D-16 (mechanism) — MAJOR.** The accent is certified twice against two rungs: boot provides `SAFE_ACCENT_KEY` = `certifyAccentInk(pick, resting)` (useAtmosphereBoot.ts:91 + useContrastSafeColor.ts:302-311, my reads); Dock.vue:35/:209 passes it as `:css-color-opaque`; :23-24 certifies again against `floating`. The raw `CSS_COLOR_KEY` is injected one line away (Dock.vue:34) and unused here. Monotone-walk composition can only over-correct. **Disposition: FOLD → X-W8 (MT-DOCK-LAYERS-1 arms — `useContrastSafeColor.ts` and all five consumer files are in that wave's scope; arm accretion recorded here, id preserved). The dark-mode 0.188→0.021 reading is Dock-adj D2-06's cell — ONE identity, already routed to the convergent design loop, not re-booked. Drift-table magnitudes stay unproven (below).**
- **L-1 — MAJOR, FOLD** (ruled above) **→ Dock-adj L-6 ≡ this row → X-W8.** Reader-2's census corrections (3 lazy pane-registry edges; `ViewId` type-only reverse edge) travel as an addendum; L-1's "drops to 0" cure-cell and blanket eslint ban are killed and uncitable.
- **C-3/L-4/D-18 — MAJOR, FOLD** (ruled above) **→ Dock-adj DELTA-5 (X-W8 structure (h)/G-L) + X-W5 gate A3.** This record contributes: the reproduced 78.7% fork (170/108/85, longest run 28), both twins mounted with no `v-if` (Dock.vue:207/:217 — my read), `isDesktop` already computed at Dock.vue and passed to two siblings but not the menus, and the committed orphan witness. The mutex-release gap (`usePopupMutex` has only `onUnmounted(clearSwapTimer)`, no per-key release) rides the cure so a `v-if` unmount cannot latch `shouldKeepOpen` (Dock.vue:86).
- **C-9/L-8 — MAJOR.** Zero unit/e2e/oracle coverage; grep re-run by this seat = 0; the sole references anywhere are Dock.vue:11/:207. C's mutation argument (template → `<div class="lg:hidden"/>` keeps every gate green) is a correct vacuity proof and explains how MMD-1 and the dead shimmer survived to HEAD. **Disposition: NO-WAVE-OWNER (candidate X-W1 re-gate, the AAP-34 precedent): one born-RED mobile-viewport spec — ArrowDown reachability, `aria-checked` flip, Enter on GitHub (RED today), Escape focus return.**
- **D-05(a) — MAJOR.** Prose in the display face: :85 (twin :150) renders the tagline `text-micro italic … font-display` → Fraunces italic 11px. Canon verbatim-verified: `prose/help → text-prose → Plus Jakarta Sans`; matrix "closed across all eighteen compositions", P019 the sole exception; `text-micro` is size-only (producer bytes) and absent from the matrix. **Disposition: NO-WAVE-OWNER, cure under X-W10 law.**
- **D-05(b) — MINOR** (ruled above). `font-display` on the content is cascade-dead at three sites (:43; twin :70/:143) — beaten by the unlayered `--dropdown-menu-font` rule fed mono by foundation.css:374, whose own comment claims it targets *triggers*. **Disposition: NO-WAVE-OWNER, cure under X-W10 law — decide the mono-content intent explicitly, then delete the dead utility; any cure authored into `@layer utilities` re-loses (see M-A).**
- **D-04/L-5/C-6 — MAJOR.** The admin identity is two systems, both wrong: mobile = muted-grey pill one tap deep (:67); desktop = `.gold-shimmer` whose `color:transparent` is beaten by the inline `color: var(--color-gold)` (twin :96) — an infinite compositor animation with zero visible output (producer rules read by this seat; no `!important`). C-6's honest HYPOTHESIS is discharged statically. **Disposition: the one-admin-treatment decision → the convergent design loop (D2-06 adjacency); the pill mechanics ride X-W8's G-H pill-primitive family; distinct from Dock-adj L-19 (the gold LATCH), never conflated.**
- **D-11 (items 1-2) — MINOR** (ruled above). Two desktop separators absent on mobile (before Regenerate; after the identity card). Item 3 stays killed. **Disposition: NO-WAVE-OWNER — dies by construction with any section-model recomposition.**
- **D-12 — MINOR.** The theme glyph renders 16×24: `aspect-square w-4` (:108-109, my read) sets width only; lucide's presentation `height="24"` survives; `aspect-ratio` is inert with two definite dimensions. Exactly 4 sites = the twin pair. **Disposition: NO-WAVE-OWNER (one-line `h-4`; the real cure is a producer-owned icon slot rung).**
- **L-2/C-7/D-13 — MINOR here, FOLD.** 19 dirs / 29 lines / zero implementation (my census); `./dropdown-menu` published + zero consumers; `./avatar` absent (producer ask rides the BH relay). **Disposition: FOLD → X-W8 scope 2 (the AAP-28/demo-ui identity); this file's `dropdown-menu` edge is Dock-adj DELTA-10 D3-06, executable today. D-13's "three import paths" count stays killed (two mechanisms).**
- **L-9 — MINOR, FOLD → X-W8** (its SCC table names this exact edge). Verified: Dock.vue:4 `from "./"` ↔ index.ts exporting Dock.vue.
- **L-10 — INFO, FOLD → the W-HYGIENE dead-glob identity (Dock-adj DELTA-3 ≡ AAP-18; X-W8 scope 3 rider).** Verified :232-238/:275-276.
- **D-10 — MINOR.** PR-16 half-met (verbatim-verified): expanded state yes; named purpose no ("Menu" names the widget); perceptible label no. Explicitly NOT MT-F005's site — that identity is ColorInput-adj D-3, and this corpus is its further confirmation. **Disposition: NO-WAVE-OWNER (X-W4 shell-naming class candidate); the honest name follows the menu's post-subtraction contents.**
- **D-14 — MINOR.** One recipe, two override mechanisms 17 lines apart (:49 bound object vs :67 static string); `.slug-pill`'s own comment institutionalises it. **Disposition: dies with MMD-3's Chip adoption — NO-WAVE-OWNER.**
- **C-8 — INFO (record).** The mutex refuse-then-rescue round-trip is load-bearing and undocumented; premises verified (`swapDelay=180`, refusing setter, `onUnmounted(clearSwapTimer)` the only teardown). The 276ms figure stays unproven. **Disposition: source comment at `usePopupMutex.ts` + the release gap rides C-3's cure.**

### Missed by the axes — booked by the readers, ratified (and one killed) by this seat

- **M-A (reader-1) — MAJOR. The cascade-layer law.** glass-ui ships component CSS **unlayered** (and inconsistently so — `_shared/menu.css` wraps `.glass-menu-row` in `@layer components` then leaves `.dropdown-menu-content` bare, my read); `@import "tailwindcss"` puts every utility — including glass-ui's own `@utility` registrations — in `@layer utilities`. Unlayered beats layered, so authored utilities on producer surfaces silently paint nothing: `font-display` (:43 = D-05(b)); `text-small`'s size leg on all seven rows (family/weight win, size loses — the right-way-up statement of reader-2's killed missed-4); `px-2 py-1.5` on :46 restating the producer's exact values (dead code that looks alive). Cures authored into the losing layer re-lose. **Disposition: NO-WAVE-OWNER — a formation-level conformance law for X-W10, plus a BH relay question (is the producer's partial layering intentional?).**
- **M-B (reader-1) — MAJOR.** The single most destructive control wears the quietest ink: :61 (twin :87) is the ONLY row with `text-muted-foreground` — the app's de-emphasis voice by construction (useContrastSafeColor.ts:319-321, my read) — on the one irreversible mutation, the one row that hides its result (D-07), the one with no boundary from Logout (D-11). Colour is lying about consequence (VISUAL-CONSTITUTION §4.1). **Disposition: rides the MMD-2/D-07 recomposition — NO-WAVE-OWNER.**
- **M-C (reader-2) — MAJOR, folded into D-08 above** (the dead `adminAuth.logout` wire + unreachable watcher — verified by this seat).
- **M-D (reader-2) — rider R-1 (INFO).** `vueCompilerOptions` absent from every config (verified). Adopted as a SPIKE, not a fact: measure strictTemplates/checkUnknownProps blast radius against legitimate fallthrough (the `aria-label` idiom this very file uses correctly), then gate the undeclared-prop class repo-wide. Kills L-3's "nothing can catch it" either way.
- **M-E (both readers, converged) — MINOR.** No `AvatarFallback`: a blocked/failed third-party avatar request renders an empty circle; the fallback is already in the demo barrel with **zero consumers** (my census); the request is issued twice per route because both twins always mount. **Disposition: NO-WAVE-OWNER (one-child cure; empty-state posture recorded).**
- **M-F (reader-1) — MINOR.** Two `target="_blank"` links with no perceptible new-tab signal, in a menu where every other row acts in place; compounds MMD-1 (dead-zone tap = dismissal, live-zone tap = unannounced context switch). **Disposition: rides MMD-1's row cure — NO-WAVE-OWNER.**
- **M-G (reader-1) — MINOR.** The tagline is grammatically attached to the person, not the product (social-card anatomy, nothing marks the byline as the app's); survives any relocation of the block. **Disposition: rides D-15's identity-module move — NO-WAVE-OWNER.**
- **M-H (reader-2) — INFO.** Dead public surface inside the shim: `DropdownMenuGroup`/`CheckboxItem`/`Shortcut` re-exported with zero consumers (verified) — sharpens L-2 and makes C-4's cure import-free. **FOLD → X-W8 scope 2.**
- **M-I (reader-2) — MINOR, FOLD → X-W4 scope 7** (verbatim owner: "the nine optional-chained `?.()` dispatches … are deleted"). Verified: Dock.vue:61-62 `slugEditRef.value?.onStartSlugEdit()` / `?.onCopySlug()` — an unmounted layer is indistinguishable from success; exactly the silence X-W4 makes impossible.

## Killed-claims register (uncitable; refutations travel with the ids)

- **K-1 · D-09** (trigger fails the 44px floor — MAJOR) — REFUTED by producer bytes this seat read whole: `touch-floor.css` ships a centred `::after` seat `max(100%, 2.75rem)` square under `@media (pointer: coarse)` with **no** `pointer-events:none` (contrast the inert `@utility touch-hit-area`), so the operable target is ≥44×44; the 42.7×33.3 was the border box. D-09's own canon cite (§5.7) sanctions exactly this separation. The 7px neighbour gap survives only as rhythm observation.
- **K-2 · D-05(c)** (iOS text-autosize scrambles the rungs; cure `text-size-adjust:100%`) — REFUTED: the 1.5× is glass-ui's deliberate coarse-pointer scale (`--ui-coarse-scale:1.5` → `--dropdown-text`, verified in tokens), applied only where `--dropdown-text` is read — which is the non-uniformity the axis found mysterious. The cure greps to zero effect; the real knob is `--ui-coarse-scale`.
- **K-3 · D-17** (symmetric 0.35s entry/exit → BH relay) — REFUTED: `[data-reveal="menu"]` maps exit to `--exit-overlay-duration: 0.15s` (both files read); the axis measured the open state twice. Sending the relay would ask the producer to fix what it already does. Consistent with motion-quarantine §S-1; no M-row is implicated.
- **K-4 · D-01's 62-char headline + 200%-zoom arm** — the 62-char string is unmintable (40-char ceiling, derived); the zoom arm is a substituted-viewport frame the canon forbids verbatim. The finding itself survives at the 320 arm (see MMD-3).
- **K-5 · D-13's "three import paths"** — two mechanisms; the barrel and the root re-export are one path counted twice.
- **K-6 · D-11 item 3** ("unearned separator" as a mobile divergence) — both twins carry it byte-identically (:100 / twin :166, my reads); a two-site family observation at most.
- **K-7 · C-2's "producer gap" framing** — the strip is purpose-built (the helper's entire body); the relay asks for a reversal of design, and the consumer `@select` route is NOT disqualified.
- **K-8 · L-3/C-2's "nothing in the toolchain can catch it"** — the check exists (strictTemplates/checkUnknownProps) and is merely off; impossibility dies, the cure is R-1's spike.
- **K-9 · L-1's boundary census + "drops to 0" cure** — three lazy pane-registry edges + the type-only reverse edge (both verified); the blanket eslint ban would break `usePaneRouter`. The closure measurement and the token-drags-a-construction mechanism survive inside the Dock-adj L-6 identity.
- **K-10 · reader-2 missed-4** (`text-small` disables `--ui-scale`) — NEW KILL by this seat: inverted mechanism, refuted by layer order and by its own 21px witness (the scale it claims disabled is the one that painted).
- **K-11 · reader-2's "REAL slug" evidence cell on D-01** — NEW KILL by this seat: `amaranthine-quokka-northern-marches` contains zero words from the four server lists (grep 0); both probe specimens are seeded. The 35-char cell survives only as a conservative under-approximation of the 40-char ceiling.

## Superlatives (provenance preserved)

1. **challenge-C-implementation.md:30-76 (C-1)** — the corpus's high-water mark: the only seat that walked `@click` → port → `useUserAuth.regenerate()` and found destructive-before-confirming at the bottom. D and L read the same line and saw a missing separator. Depth of chain-walk beats breadth of surface-reading; both readers independently ranked it first and this seat concurs.
2. **`touch-floor.css`** (producer) — a textbook PROPORTION §5.7 implementation: 44×44 operable seat by centred `::after`, coarse-gated, 24px optics untouched. It is the reason D-09 is a false defect; recorded so the relay queue never "fixes" it.
3. **challenge-L-library.md §6** — four registry claims negated with positive evidence, incl. the shared-chunk proof that the mixed import paths do NOT fork the reka context, closed with "Stated so the cure is not over-scoped." Bounding one's own finding is the rarest discipline in the corpus.
4. **All three axes refused MT-F005 here and named ColorInput's `send-btn` independently** — three seats, three methods, one explicit negative each (D:502-504, C:398, L:362-368). With the two readers and this seat, the ColorInput-adj D-3 identity now carries seven concurring negatives; a close row can trust it.
5. **MobileMenuDropdown.vue:102-107** — a NEGATIVE design decision recorded with its reason (why DarkModeToggle cannot nest), verified live by L (one flip per tap; D58.i not live). Its only failing is C-4: it never supplied the state channel the nesting would have provided.
6. **ProfileSection.vue:103-110 + useContrastSafeColor.ts:52-78/:283-291** — the house knows how to diagnose cascade-death and referent bugs to their mechanism and write the law at the site; which is exactly what makes the adjacent unlearned lessons (the dead gold-shimmer eight lines away, the double-certified wiring under a correct instrument) legible as defects at all.
7. **The readers' own evidence discipline** — reader-2 reproduced L-4's difflib figures bit-for-bit before trusting them; reader-1 killed three findings with producer bytes the axes never opened; each seat that could not measure said so (C's gold-shimmer HYPOTHESIS; D's D-07 repro hypothesis). Two of the readers' cells still died under this seat's derivation (K-10, K-11) — the twice-audit edict earned its keep twice.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs — task #4 feeds)

1. **D-06** — the 4.03:1 / 5.21:1 composited-contrast pair: structurally real (80.8%-alpha veil over live chromatic ground; 11px non-bold ink; raw pixel samples committed), but the ground is a function of pick × route × scheme. Re-measure the **failing fraction** across picks/routes/schemes; single-seed cells are not a verdict.
2. **C-3's orphan across engines** — the chromium cell is committed (`orphan-2-after-resize-1440.png`, transcripts); webkit/safari cells are separate evidence (I-20). Also the focus-restoration fall-to-body arm.
3. **C-8's 276ms** swap latency and its rescue path — timing, not statically decidable; preserve the shape as a source comment regardless.
4. **L-6's drift table magnitudes** (7/7 picks, ~−5.5pp L / −12% C) — simulated rung model; direction sound (monotone-walk composition), quantities not established; D-16 measured zero delta at the boot seed. The dark-mode degeneracy question is ruled-elsewhere (Dock-adj D2-06 → design loop), not unproven here.
5. **L-2's dev-boot byte figures** — `node_modules/.vite/deps` cold; the structural half is fully confirmed and folded.
6. **The dead-zone percentages** (73.2/26.8) and row geometry — committed chromium rects corroborate a mechanism that is now statically proven; re-derive live only if a cure gate needs the exact fraction.
7. **The admin arm end-to-end** (D-08) under a real admin token against a live API — every element is source-certain; the composed journey has never been witnessed outside seeded localStorage.

## Closing verdict

**DEFECTIVE — ADJUDICATED: 2 BLOCKER · 14 MAJOR · 11 MINOR · 4 INFO/rider confirmed; 11 claims killed and uncitable; 9 axis-missed rows booked.** The component's through-line is a fork poured into a popover: the two twins share 78.7% of their bytes and every defect except C-3 exists twice. Terminal routing: the boundary/fork/ink/shim/SCC/glob families FOLD to their standing identities (Dock-adj L-6/DELTA-3/DELTA-5/DELTA-10/DELTA-11/D2-06 → X-W8, X-W5 A3, X-W4 scope 7, W-HYGIENE, design loop) — original ids for life, nothing re-booked; the identity-menu content itself (MMD-1, MMD-2, D-01/D-03/D-07/D-08/C-4/D-15/D-05/D-11/D-12/D-10/D-14, M-A/M-B/M-E/M-F/M-G) has **NO-WAVE-OWNER in X** and is flagged whole for the next formation boundary, with MMD-2 (silent identity destruction) named as the row no formation may open without. Dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this record.

— end of apotheosis. The r2 challenge corpus, both reader returns, and all committed evidence remain immutable beside this file per E-1/E-3.
