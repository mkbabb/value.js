claude-fable-5

# fr-GalleryAdminBanner — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject repo**: fourier-analysis HEAD `cd26c65` / tree `9a66411d` — **re-pinned by this seat, WITH the substrate finding GAB-13: the audited artifact is the DIRTY WORKTREE, not the certified tree** (28 modified rows; the subject's own diff is exactly six `:amount=`→`:value=` hunks; HEAD's `web/package.json` pins `^3.1.0/^2.2.0/^0.10.0/lucide latest` against the worktree's `^4.0.0/^4.3.0/^0.13.0/^1.0.0`). · **Corpus**: 3 challenge axes (`challenge-D-design.md` 27 defects / `challenge-L-library.md` 15 / `challenge-C-consumption.md` 16) against `web/src/components/visualization/gallery/GalleryAdminBanner.vue` (107 lines), re-read by two hostile re-readers whose returns this seat adjudicated.
**Workers adjudicated**: worker-DU (design-and-usage re-read) · worker-LC (library-and-code re-read). Both were attacked before either was adopted; every verdict-level disagreement was re-derived against the trees with this seat's own instruments; the suspiciously-agreed contrast table was re-measured, not spot-trusted.
**Arbiter's own instruments this session (all static + node; zero browser probes — probe parsimony; product source untouched)**: whole read of GalleryAdminBanner.vue (107, worktree); `git rev-parse HEAD`/`HEAD^{tree}` + `git status --porcelain` (28 dirty) + `git diff` on the subject (six hunks) + `git show HEAD:web/package.json`; gallery.ts:100-140 (activateAdmin toast / deactivateAdmin no-abort / refreshAdminStats bare-catch, verbatim); api.ts:45-70 (`abortable` aborts prior under same key; `abortInflight` + `isAbortError`), :244-256 (`adminFetch` → `coreFetch(path, /* abortKey */ path, …)`), :495-497; GalleryView.vue:132-135/:188-200/:218-250 + imports :25-33 (banner static, three admin siblings `defineAsyncComponent`); **web/index.html:20-30 — the dark-seeding decider** (fourier's own inline script, keys `theme`/`vueuse-color-scheme`); api/routers/admin.py:110-157 (tier pipeline + `total_entries = sum(tier_counts.values())` + match-less storage pipeline); api/services/image_storage.py:112-190 (`"bytes": len(content)` primary-only; thumbs as `{slug}.thumb` files, size recorded nowhere); web/tsconfig.json (no `vueCompilerOptions`); types.ts:110-118 (7 fields). Installed glass-ui **4.0.0**: 80 export keys incl `./metric-badge`/`./metric-cell`/`./metric-stack`, **no `./metric-pill` key**; components.css:96-105 stacked rule + :112-116 `__row` + :15 `max-width: var(--metric-badge-max-width, 8rem)` vs :101 relax-to-`none` (the two kill deciders, byte-read); MetricBadge chunk (`metric-badge cursor-pointer` unconditional; `truncate` ×2; `isEmpty ? void 0 : { color: n.color }`); button chunk (`outline: "border border-input bg-background hover:bg-accent …"` verbatim; `[&_svg:not([class*=size-])]:size-(--ui-glyph)`); offsets-sizing.css:177 `--ui-glyph: calc(1rem * var(--ui-scale))`; **dist/metric-cell.js appearance switch: `case "compact": return "glass-wash rounded-lg p-2"` / `"text-mono-small font-semibold tabular-nums"`** (worker-LC's kill leg, byte-confirmed); MetricPill.vue.d.ts (whole head) + `dist/index.d.ts:17` root-barrel export. Producer glass-ui: **version 8.0.0 at this seat's read, HEAD `350f7a90`** (drifted again since worker-DU's `8323c9b7` — live tree, version stable); tags v7.0.0/v8.0.0 both exist; `git show v7.0.0:src/components/metric/types.ts` (orientation axis, **no `color`**) + exports = **74 keys, metric = `./metric` only**; `git ls-tree v8.0.0 src/components/metric/` (**MetricCell.vue DELETED**; no MetricPill source anywhere at v8) + v8 types (`MetricPosture = "inline"|"stacked"|"cell"|"row"`, **`compact?: boolean`**, `loading?: boolean`, no `color`) + Metric.vue:44-51 (`data-loading` + `data-posture` + `aria-busy`) + styles.css `[data-posture="cell"]` selectors + exports = **70 keys, metric = `./metric` only**. Node runs: formatBytes verbatim (1048575→`1024.0 KB`, 1073741823→`1024.0 MB`, 1099511627776→`1024.00 GB`, NaN→`NaN GB`); **independent third contrast recomputation** (hsl/oklch→sRGB→WCAG luminance, alpha-composited in source order): 1.030 / 1.252 / 1.603 / 1.060 / 1.453 / 2.584 / 3.264 / 15.399 light, fallbacks 1.470 / 2.239 — every disputed row within 0.03 of both workers. Counts: `cm-serif` = **26 occurrences / 17 files**, `font-serif-math` = exactly 1 (the fallback itself); sibling `unit=` = **4** (AnimationControls:75, InfoCard:33, EquationPanel:79, EquationView:294); GalleryDraftsSection.vue:53 `variant="ghost"`; `abortInflight` = export + 4 workspace.ts call sites, zero admin. Registry anchors verified byte-level: fr-AnimationControls.md:121 (C-28) / :125 (L-13) / :144 (K-10); fr-FourierMorphDemo.md:53 (FMD-11 — "F.W1 must budget from 8.0.0, never the 7.0.0 snapshot"); fr-CanvasOverlayButton.md:59 (FR-COB-10 dirty-`:amount→:value` class + budget correction → F.W0) / :72 (FR-COB-18); fr-AdminUserList.md:64 (FR-AUL-26); fr-AdminFlaggedPanel.md:65/:88/:93; fr-AppHeader.md:48 (FR-AH-2, 26/17 counts banked) / :58 (FR-AH-12); fr-BasisSelector.md:50/:69/:88; fr-BasisCanvas.md:7 (the standing dirty-tree disclosure idiom); fr-AdminAuditLog.md:36 (AA-2) / :87 (AA-44); registry sweep `MetricPill|metric-pill` → 0 hits.

## Scoreboard

**29 adjudicated rows (1 BLOCKER · 12 MAJOR · 15 MINOR · 1 INFO) · 13 killed claims · 7 missed rows adopted · 8 superlatives · 8-item SS-13 residue.**

One-line verdict: a 107-line presentational leaf whose own contract is nearly immaculate (S-8), convicted at its two seams — the light arm erases the component's reason to exist (GAB-1, reproduced three ways to ±0.03), and every state it cannot express (loading / failed / logged-out-mid-flight) collapses into the same nothing while the store deletes the evidence (GAB-3/GAB-20/GAB-21). Both workers were corrected on cells they were surest of; two of the three original axes' prescribed cures are dead at the ruled 8.0.0 baseline, and the true migration seat — **`<Metric posture="cell">` with `compact` + `loading`** — was named by neither axis and only one worker.

**Status verbs**: every disposition below is **ADJUDICATED**, not VERIFIED — the X·F specs are still forming; routings are agglomeration input (M-25). Line anchors are the WORKTREE at HEAD `cd26c65` (see GAB-13); re-resolve before executing any cure (the D-19 lesson).

## Ruled disagreements (the seat's actual work)

| Locus | worker-DU | worker-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-1/D-B2/L·D-4 severity** | BLOCKER sustained | MAJOR; C-1's "uncurable in place" premise killed | **MAJOR — worker-LC** | The BLOCKER criterion's second half is false on the existing two-prop contract: once adminMode is entered, `stats === null && !loading` is reachable only via failure or a dead/absent token (refreshAdminStats fires at activation, gallery.ts:111, and after every mutation, GalleryView.vue:134/:194 — my reads), so a `v-else` is a correct, complete failure arm **today**; the store's real cure is one line (`catch {}` stops deleting the error). And the AA-1 precedent (BLOCKER at the sibling) is distinguished, not followed: AdminAuditLog rendered an **affirmative false statement** ("No audit entries") about the system of record; this banner renders an **absence**. DU's dissent preserved with a revival condition (below). |
| **D-B3 severity** | BLOCKER sustained | MAJOR | **MAJOR — worker-LC, per the registry's own FMD-11 precedent** | FMD-11 ruled the identical class (Button/Slider prop-axis loss at the uplift, silent-fallthrough mechanics included) "MAJOR · F.W1" with the BLOCKER dissent recorded. The subpath leg here is BANKED (census C-4; fr-AnimationControls L-13; FR-COB-10's 5-live-files correction); what is NEW is the prop delta — `color` deleted with no successor at BOTH v7.0.0 and v8.0.0 (my tag reads) + the axis renamed TWICE (`labelPosition`→`orientation`→`posture`) + this file the sole labelPosition+color combination site. strictTemplates-off silent fallthrough (my tsconfig read) keeps it MAJOR, not MINOR. DU's dissent preserved. |
| **The Logout name** | defect (missed MINOR) + kill of D §8 S-4's naming half | **superlative #5 praises the same divergence** ("states the SCOPE of the mode exit") | **DEFECT — worker-DU; LC's superlative sentence WITHDRAWN** | fr-AnimationControls K-10 is the decider: 2.5.3 requires the visible label be CONTAINED IN the accessible name — here visible `Logout` (:40) vs `aria-label="Log out of admin mode"` (:36), and "Logout" ⊄ "Log out of admin mode": the containment runs the FAILING direction, on the one control that exits an elevated-privilege mode. Graded MINOR (whitespace-tokenization gap, the FR-AFP-34 "2.5.3-adjacent" band) against the MAJOR precedents' zero-overlap gaps (FR-AUL-26, FR-AH-12). Cure: visible text "Log out". The rest of LC's superlative (aria-hidden ×2, named region) survives. |
| **Substrate severity** | INFO (fleet elected worktree-as-scope, L-13) | MAJOR + kill of D §9's byte-identity certification | **MAJOR → F.W0 — worker-LC** | My own runs settle every fact: 28 dirty rows; the subject's six-hunk diff; HEAD pins ^3.1.0/^2.2.0/^0.10.0. The condition is repair-shaping, not cosmetic: all three axes' line anchors and the shared "resolved producer is 4.0.0" premise are worktree facts no commit holds, and challenge-C §6 S-3 awards a superlative for the uncommitted edit itself. Mechanism already part-banked: FR-COB-10 books the same dirty-`:amount→:value` class for InfoCard and routes **F.W0**; fr-BasisCanvas §header sets the disclosure idiom. This row is the subject-file member site + the certification kill (K-9). |
| **C-12 symptom** | "the grid re-mounts, then unmounts again … double blink" | mechanism confirmed, symptom corrected: nothing re-raises `loading`; the flag LIES | **worker-LC** | My event-order re-derivation: B sets `adminStatsLoading=true` synchronously BEFORE dispatch (gallery.ts:126); dispatch aborts A (api.ts:54-58 under the constant key `/api/admin/stats`); A's rejection is swallowed and A's `finally` clears the flag in a microtask — after which nothing sets it true again. Observable: one unmount at A's start, one PREMATURE remount showing stale stats while B still flies, then a data swap. No second unmount; the defect is flag ownership (`loading` reads false mid-flight — the prop lies to the host). |
| **D-M7's rationale** | sustained D's strongest limb ("the operator cannot verify total = featured + saved + normal") as a real argument | limb REFUTED: the invariant holds by construction | **worker-LC** | admin.py:117-127, my read: `tier_counts` seeded `{featured,saved,normal: 0}`, rows folded only `if tier_id in tier_counts`, then **`total_entries = sum(tier_counts.values())`** — the seventh tile would render an identity that can never fail (zero verification value), while any off-enum/absent-`tier` row is silently dropped from the total with no signal. The finding survives at MINOR (GAB-17); the prescribed cure is corrected: `count_documents(not_deleted_filter())` + an "other" bucket FIRST, and only then is the `normal` tile a check rather than a tautology (GAB-16). |
| **The migration seat** | kill via v8.0.0 (MetricCell deleted; seat = `Metric posture="cell"`) | kill via bytes (`compact` = glass-wash + `text-mono-small`, not the claimed register; `./metric-cell` not a 7.0.0 subpath; prescribes `appearance="bare"` at the pin) | **BOTH KILLS ADOPTED, MERGED (K-4); the ruled seat is worker-DU's** | All legs byte-confirmed by this seat (metric-cell.js switch; v8 tree sans MetricCell; v7/v8 exports). Under FMD-11's baseline law (budget from 8.0.0, never the 7.0.0 snapshot — verbatim at fr-FourierMorphDemo.md:53), LC's residual "adopt MetricCell at the current pin, then rewrite at 7.0.0" still lands a component that is deleted one major later — one deletion becomes two. Ruled: **F.W1 targets `<Metric posture="cell">` from `./metric` at 8.0.0**, which also ships `compact?: boolean` (the abbreviating formatter GAB-8 needs) and `loading` → `data-loading`+`aria-busy` (the keep-the-box cure GAB-4 needs). MetricPill is NOT a fallback seat (GAB-29). |
| **The default-arm citation** | CORRECTED: fourier seeds from its own inline script, `web/index.html:21-32` | repeats the challenge's `dist/dark.js` cell | **worker-DU — and a citation-inheritance event recorded (DISSENT 1)** | web/index.html:20-30 read by this seat: fourier's own IIFE checks `theme`/`vueuse-color-scheme` + `prefers-color-scheme` and adds `.dark` itself; glass-ui's `darkModeSyncScript` is not in this path. Conclusion (light default for light-OS operators) unchanged; the citation matters for any repair spec touching theme seeding. |

## Adjudicated defect roster

Merged identities; original axis ids for life. Routing targets are the census lane taxonomy (F.W0 substrate · F.W1 tri-package uplift · F.W3/W4 shadow retirement + frontend audit · F.W5–W8 CRUD/provenance union · F.W9/W10), else NO-WAVE-OWNER.

### BLOCKER

- **GAB-1 · D-B1 (+ D-m4/L·D-8/C-8 fallback leg as control) — ADJUDICATED BLOCKER.** In the default light arm the admin chassis has no admin register and the two semantically-tinted numerals are illegible: plate/page **1.03**, border **1.25–1.27**, shield **1.60**, tile/plate **1.06**, `--tier-featured` value **1.45**, `--tier-saved` value **2.58** — against a 4.5:1 floor (value rung `text-mono-caption` caps at 16px, scale.css; no large-text relief). Dark arm passes throughout. Now reproduced **three ways to ≤0.03** (D axis, both workers, this seat's own hsl/oklch→WCAG pipeline: 1.030/1.252/1.603/1.060/1.453/2.584); the hard-coded fallbacks measure 1.470/2.239 — the failure is identical whether the token resolves or not (D's own falsifier, correctly applied). The colour chosen to distinguish the two admin-manipulable tiers is what erases them. Citation corrected (K-10 below): light-is-default derives from **web/index.html:20-30**, not `dist/dark.js`. **→ F.W4** — retint the chassis off calibrated tokens (the repo's own `--viz-amber` axe-carry at style.css:112-128 is the in-repo vocabulary — GAB-18 composes); the tier-semantics relocation rides GLASS-RELAY ask (a) under GAB-2. Replacement figures are SS-13/F.W4 work (no axis measured `--warning` against the light page).

### MAJOR

- **GAB-2 · D-B3 — MAJOR (ruled down; DU's BLOCKER dissent recorded).** The metric-family uplift is not a rename pass: `color` is deleted with **no successor at v7.0.0 AND v8.0.0** (both tag reads mine); the geometry axis renamed TWICE (`labelPosition`→`orientation` at v7→`posture` at v8 — any wave writing "orientation" off the challenge's table writes a prop that does not exist, K-5); `size` survives at both tags; strictTemplates is off so the dead props degrade to silent DOM fallthrough, not a typecheck failure. Sole-site fact verified: none of the six sibling MetricBadge files combines `labelPosition` with `color`. Subpath leg FOLDS BY REFERENCE (census C-4 · fr-AnimationControls L-13 · FR-COB-10's "5 live files + 1 unmountable" budget correction). **→ F.W1** (budget from 8.0.0 per FMD-11; the seat is K-4's ruled `<Metric posture="cell">`) **+ GLASS-RELAY ADOPTION-ASK (a)**: tier tint needs a home — a tone axis on Metric, or a ruling that tier semantics move to label/glyph.
- **GAB-3 · C-1 / D-B2 / L·D-4 — MAJOR (ruled down; DU's BLOCKER dissent recorded).** Loading, failed-fetch and loaded-never render identically as nothing: `:44` `v-if="stats && !loading"` is the only branch (no v-else, zero error/aria-live references file-wide); gallery.ts:127-134's bare `catch { // ignore }` deletes the error — the only silent admin store action (activateAdmin toasts, my read). Failure image: an expired token yields an amber box saying "Admin Mode" + Logout, with the whole downstream admin surface still enabled against the dead credential. Cure is two-sided and cheap: a `v-else` failure arm at the leaf (complete today — the ruled table) + the store ceasing to swallow; inline error state, **NOT via `useToast`** (the census [P1] `ToastVariant` break — D's ordering constraint upheld). **→ F.W4** (host + store). Revival condition for DU's BLOCKER: if the empty state ever renders an affirmative claim (the AA-1 shape), escalate.
- **GAB-4 · D-M4 / C-6 / L·D-5 — MAJOR.** Every `setTier` (GalleryView.vue:133-134) and every batch action (:194) unmounts the whole six-tile grid for the refresh duration — `adminStatsLoading` set true BEFORE the await with `stats` already populated, so `(stats && loading)` is the normal refresh state; the banner is a direct child of the `overflow-y-auto` scroller (:220) above the carousel and infinite grid. Floor static-certain: ≥42px (`--metric-badge-min-height-stacked: 2.625rem`, un-overridden). Third audit to file it; the re-grade from the 2026-05-18 "Low" is upheld — the earlier read predates the wiring to every mutation. **→ F.W4**, cure seat = the 8.0.0 `loading` posture (box stays, `aria-busy` announces); magnitude SS-13.
- **GAB-5 · D-M1 / C-4 / L·D-3 — MAJOR, FOLDS BY REFERENCE to banked C-28 (fr-AnimationControls.md:121, GLASS-RELAY).** Six inert counters wear the full interactive costume — `metric-badge cursor-pointer` + focus-ring classes unconditional in the chunk (my byte read), hover 1.02 lift / active 0.96 / focus ring in components.css:50-66 — with no @click/tabindex/role anywhere in :44-89. This file is the producer seam's **heaviest member site (6 of the repo's 12 instances)**, not a new row. K-6's PRM kill re-verified and upheld (a11y-overrides clamps the transition; an instantaneous transform is not SC 2.3.3). The uplift cures it free (no cursor/hover/press in the 7/8 metric family — DU's grep adopted). **→ member-site rider under C-28; discharges with GAB-2's F.W1 migration.**
- **GAB-6 · D-M2 / L·D-1 / C-3 — MAJOR.** `.admin-stat` half-neutralizes the primitive: its six unlayered scoped declarations win (unlayered beats `@layer components`; single-root class merge verified in the chunk), and the glass border (13%-fg vs a 3%-fg plate), backdrop-filter, highlight shadow, `overflow: hidden`, stacked min-height and the whole hover/press/focus block survive un-asked-for. The producer's 11-knob `--metric-badge-*` retheme surface goes entirely unused while the sibling `.metric-pill` (components.css:126-134) demonstrates the idiom. **One row struck from L's leak table** — `max-width: 8rem` does NOT survive (K-2). **→ F.W4** — the block deletes with GAB-2's migration; any interim edit must not "fix" by deepening the override fight.
- **GAB-7 · C-5 / D-M5 — MAJOR.** `formatBytes` bakes `" MB"` into `:value` (:83), so the unit is typeset as a numeral (`font-semibold tabular-nums` mono-caption — tabular-nums pinned to letterforms) and the producer's purpose-built baseline-aligning `.metric-badge__row` + `unit` slot (components.css:107-116, T.W2.T2 doc) sit inert. **Count corrected (K-7): FOUR of six siblings use `unit=`** (my grep — AnimationControls:75, InfoCard:33, EquationPanel:79, EquationView:294); this file is the sole concatenator. **→ F.W4** (the 8.0.0 atom keeps a first-class `unit`).
- **GAB-8 · D-M6 — MAJOR.** Raw unformatted counts (`total_entries`/`total_views`/`total_likes`) inside `truncate` (nowrap — defeats the AW.W26 `overflow-wrap: anywhere` hatch) inside an un-overridden `overflow: hidden` root on a `minmax(5rem,1fr)` track: a large count can render as a DIFFERENT number, unsignalled, as an ellipsis. Repo-wide formatter absence verified (one `toLocaleString` hit, a date). Cure-coupling upheld and now concrete: separators worsen the truncation threshold, so separators + the abbreviating formatter must land together — **8.0.0's `compact?: boolean` (+ locale) is that formatter** (my v8 types read). **→ F.W4** with GAB-2; exact glyph threshold SS-13. Note the app forks the root at 768px (style.css:38-49), so the 5rem track is 90px mobile / 80px desktop — per-arm thresholds differ.
- **GAB-9 · D-M8 — MAJOR (producer-side).** Every stacked-badge label is `text-muted-foreground/80` at `text-mono-micro` (fixed 0.6875rem, non-fluid) — **light 3.26:1** (three-way reproduced) vs the unconditional 4.5:1 floor for 11px text, weight 300, uppercase, tracked: the four-way worst case, on the only strings naming the six numerals. `.admin-stat` touches no colour/weight — this fails in every stacked MetricBadge in every consumer. **→ GLASS-RELAY ADOPTION-ASK (b)** (lift alpha or weight at the producer) **+ F.W4** consumer bump meanwhile.
- **GAB-10 · D-M9 + C-16 — MAJOR.** (a) The stat cluster appears/disappears/changes with zero announcement — no aria-live/status/aria-busy in the file OR any GalleryView ancestor (both workers' greps, spot-verified). (b) The axe keystone gate exists (visualization-ux:26-43 + visualization-crud AxeBuilder, wcag2a/aa/21a/21aa, serious/critical empty) and **no spec ever enters admin mode** — `grep -rln "admin" web/e2e/` → zero files across all specs; GAB-1's failures are exactly axe's `color-contrast` serious class, unverified by construction. Coverage half **FOLDS BY REFERENCE to AA-44** (fr-AdminAuditLog.md:87, → F.W9/W10); the live-region half is this file's own **→ F.W4** (rides GAB-4's `aria-busy` cure).
- **GAB-11 · D-M10 — MAJOR, FOLDS BY REFERENCE to AA-2** (fr-AdminAuditLog.md:36 — the 37-file/38/124 Button census-extension BLOCKER → F.W1) **+ FMD-11's 8.0.0 rider** (`variant`/`size="default"` axis loss re-confirmed at 8.0.0 there). This file contributes ONE site (:33) and yields priority — D's attribution honest. This file's own second-order note stands: `outline` is the union's only opaque-`bg-background` arm (recipe byte-read), punching a page-coloured rectangle through the amber wash once GAB-1's cure makes the wash visible; the in-plate register is ghost/quiet, as GalleryDraftsSection.vue:53 already does for the same role (my read). **→ member site F.W1; register note F.W4.**
- **GAB-12 · C-7 — MAJOR.** The wire contract is hand-mirrored (types.ts:110-118 ↔ api/models/gallery.py, isomorphic today; no codegen in web/package.json; `res.json()` unvalidated) and drift degrades into six silent em-dashes with the tier tint dropped (`isEmpty ? void 0 : { color }` — my chunk read) — visually indistinguishable from an empty database, with no console error, no toast (GAB-3 swallows), no test (GAB-10). Sustained MAJOR, not hypothetical: **the failure image is LIVE at certified HEAD** — the committed file passes the undeclared `:amount` prop, so all six tiles render exactly this em-dash state on the near side of the GAB-13 seam. Folds R6-8 / R3-7b / EA5 by reference. **→ F.W5–W8** (generated client / runtime boundary), test seam with AA-44.
- **GAB-13 · worker-LC missed (+ worker-DU concurring at INFO; ruled MAJOR) — THE SUBSTRATE IS UNCOMMITTED.** Facts at this seat's own runs: HEAD/tree exactly as certified, but 28 dirty rows including the subject (six `:amount=`→`:value=` hunks) and `web/package.json` (HEAD pins glass-ui `^3.1.0`, keyframes `^2.2.0`, value.js `^0.10.0`, lucide `latest`). Consequences: (i) D §9's "byte-identical … every read at that coordinate" certification is FALSE for the subject (K-9); (ii) challenge-C §6 S-3's superlative credits the uncommitted edit; (iii) every per-file F.W1/F.W3 budget and the "resolved producer is 4.0.0" premise rest on bytes `git stash` would erase; (iv) at HEAD all six badges render GAB-12's em-dash image. Class part-banked: FR-COB-10 (InfoCard's identical dirty edit, → F.W0) + fr-BasisCanvas's disclosure idiom — this row is the subject-file member + the certification kill. **→ F.W0** — land the WT diff (or record it explicitly as the audit baseline) BEFORE any per-file budget commits.

### MINOR

- **GAB-14 · L·D-7.** `formatBytes` mis-renders at both binary boundaries — each arm tests the raw count but formats the divided value: 1048575→`1024.0 KB`, 1073741823→`1024.0 MB`; no TB arm (1099511627776→`1024.00 GB`); `NaN`→`NaN GB`. Executed three ways (both workers + this seat). Kills D §8 S-1 (K-3). Reachable: unbounded `$sum` over db.images. → **F.W4** (dies into 8.0.0 `compact`+`unit`, or a four-line fix).
- **GAB-15 · L·D-10 + worker-LC missed (thumbnail leg).** The storage tile carries two uncorrected opposite biases under one unqualified label: the pipeline has NO `$match` (my admin.py:144-146 read — soft-deleted visualizations' assets still counted, `not_deleted_filter` never applied to images) AND `"bytes": len(content)` records the primary blob only while thumbnails are written as separate `{slug}.thumb` files with size recorded nowhere (my image_storage.py reads) — under-reports the thumb corpus, over-reports deleted assets; unbounded, non-cancelling. → **F.W5** (with the R3-7c client-gap rows) + label honesty **F.W4**.
- **GAB-16 · worker-LC missed.** `total_entries` is DERIVED, not counted: `sum(tier_counts.values())` over an in-enum fold — any document with absent/off-enum `tier` silently vanishes from the total (`GalleryTier` is an API-boundary literal, not a collection schema). Honest cure: `count_documents(not_deleted_filter())` + an "other" bucket. Refutes D-M7's headline rationale (R-6 ruling). → **F.W5**.
- **GAB-17 · D-M7 / C-11 / L·D-13.** `stats.normal` fetched, typed, produced, rendered by nothing (grep 0 in file; zero readers repo-wide). MINOR ruled (the three axes split MAJOR/MINOR/INFO): derivable, auto-fit grid takes a seventh tile free, no capability lost — and the "verification" limb is a tautology until GAB-16 lands. → **F.W4** (render it AFTER GAB-16, or drop the field with the F.W5 contract pass).
- **GAB-18 · C-9.** The one amber surface in the app that bypasses the app's own calibrated amber: raw `border-amber-500/30 bg-amber-500/[0.04] text-amber-400` vs style.css:112-128's explicit D.W4.d axe-carry (`--viz-amber` darkened to hsl(35 76% 35%) ≈4.6:1 because glass-ui's 3.54:1 failed AA); `amber-400` is LIGHTER than the value the project rejected. Composes directly with GAB-1; the cure token already exists. → **F.W4** (with GAB-1).
- **GAB-19 · C-10 (+ L·D-2 Claim B surviving leg, + D-i4).** The scoped comment credits the primitive's stacked geometry, then re-declares it (display/flex-direction no-ops — grid items blockify; :97 already columns), contradicts it (`align-items: center` silently overrides the documented `flex-start`), drifts from its own cited class string (`rounded` 0.25rem vs written 0.375rem; oklab vs srgb color-mix), and describes a "surrounding tile" that does not exist (the class lands ON the badge root). Stale A.W3.c provenance. → **F.W4** (dies with GAB-6).
- **GAB-20 · C-12 (symptom corrected, R-5).** The stats request shares an abort key with itself (`coreFetch(path, /* abortKey */ path)`, constant `/api/admin/stats`); no re-entrancy guard (contrast fetchNextPage's); aborted request A's `finally` clears the `adminStatsLoading` flag request B owns — `loading` reads false mid-flight, the grid remounts prematurely on stale data. The three-hop derivation stands as the corpus's most non-obvious true finding; the "double blink" cell dies. → **F.W4** (generation token or guard — same cure family as the sibling's AA-1/AA-7/AA-8 seam).
- **GAB-21 · worker-LC missed.** `deactivateAdmin()` neither aborts nor guards the in-flight stats request: an already-dispatched `getAdminStats` re-seeds `adminStats` AFTER an explicit logout (my gallery.ts:117-121 + :128 read), the Logout control being reachable during exactly that window (header row sits outside the `:44` gate). The house cure sits unused one module away — `abortInflight` exported and called at 4 workspace.ts transition sites, zero admin sites (my grep). Registry-swept: unbanked. One line: `api.abortInflight(["/api/admin/stats"])`. → **F.W4**.
- **GAB-22 · D-m4 / L·D-8 / C-8.** The hard-coded hex fallbacks (`#fbbf24`/`#60a5fa`) have drifted from the tokens they shadow in both arms and would pin a light-arm-only sRGB value if they ever fired; measured 1.470/2.239 vs the tokens' 1.453/2.584 — failing either way (GAB-1's control). K-7 upheld: the `color` prop lands as inline style, so `var()` is the only token route through it — only the stale hex is the defect; the drift-proof idiom is the `text-tier-*` bridge class. This file is the directory's lone fallback-bearing dissenter (siblings verified bare-token/bridge). → **F.W4** (dies with GAB-2's tint relocation).
- **GAB-23 · D-m1 — FOLDS BY REFERENCE to FR-AH-2** (fr-AppHeader.md:48, blocker-weight font-truth cluster, GLASS-RELAY already raised: `cm-serif` reads `var(--font-serif-math, serif)` — declared NOWHERE; root cause is the app never importing `./styles/fonts`) **+ fr-BasisSelector m-3**. Two challenge cells corrected: the live count is **26 occurrences / 17 files** (my grep; D said 15) and D's "Not in the corpus" is false — banked twice; D's grep covered `formation/fourier/` only, not the adjudicated registry. No new row; the banner title's member site discharges with FR-AH-2's one-decision cure.
- **GAB-24 · missed (worker-DU) — WCAG 2.5.3 on the banner's only control (ruled, see table).** Visible `Logout` not contained in accessible name `Log out of admin mode`; speech-input users saying what they see have no match on the mode-exit control. MINOR per the FR-AFP-34 band (single-space tokenization gap; MAJOR precedents FR-AUL-26/FR-AH-12 had zero overlap). Decider: K-10's containment direction. Filed chiefly because D §8 S-4 praises the divergence — an F.W4 spec preserving the superlative preserves the defect. Cure: visible text "Log out". → **F.W4**.
- **GAB-25 · missed (worker-DU) — the authored 16/14 icon hierarchy renders 16/16.** `<LogOut :size="14">` inside Button is overridden by the cva's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` (my chunk read; `--ui-glyph = calc(1rem × var(--ui-scale))`, offsets-sizing.css:177): lucide's `:size` emits presentation attributes, CSS wins, no `size-*` class → no escape hatch; the un-Buttoned Shield keeps 16px, so the pair renders 16/16 desktop and DIVERGES under coarse pointers (LogOut → 24). **FOLDS to the FR-COB-18 family** (fr-CanvasOverlayButton.md:72 — the hatch is a DOCUMENTED producer contract; consumer-side misuse) + fr-BasisSelector m-22(f). New member site → rides FR-COB-18's **F.W3/W4** glyph-register sweep.
- **GAB-26 · D-m9.** One-click, unconfirmed mode exit at the `ml-auto` edge (:37 → GalleryView:246 → deactivateAdmin) beside a `confirm()`-guarded destructive sibling (GalleryView:138-139, my read). Not data loss. → **F.W4**.
- **GAB-27 · D-m7 + C-3 (blur leg).** Six `backdrop-filter` compositing-layer REQUESTS over a uniform 4% wash inside the scroller — static-certain; the paint cost and the reduced-transparency arm's neutralization are SS-13. Blur over a uniform fill is an identity op (DU's framing adopted over L's "blurs the plate six times"). → **F.W4** (dies with GAB-2/GAB-6).
- **GAB-28 · The band (D-m2/m3/m5/m6/m8 · D-i1/i2/i3 · C-13/C-14 · L·D-11/D-14/D-15) — MINOR/INFO, itemized dispositions.** `aria-label="Admin mode banner"` names the widget and collides with the `banner` landmark word (rename to content, e.g. "Admin statistics"); no heading in banner OR host (grep 0, both files); magic `5rem` + zero responsive modifiers; the `abbreviation` channel unused (the documented container-query affordance for exactly this cluster); the only control painted as caption ink under six full-ink hover-lifting tiles (inverted hierarchy — discharges with GAB-11's ghost/quiet move); lowercase source strings rendered uppercase by the type utility (fix at neither end without knowing this); KB/MB/GB naming on 1024 divisors + unguarded negative/NaN input (dies with GAB-14's cure); six inert `size="md"` props (the default); **inert `@reference "tailwindcss"` pointed at the wrong entry — FOLDS BY REFERENCE** to the banked class rows (fr-AdminFlaggedPanel FR-AFP-39-adjacent · fr-CoefficientsSpectrum i-5(d)); note this file is NOT a zero-rule-scoped-block member (its block has rules); static import while all three admin siblings are lazy (honest INFO — Button/MetricBadge/lucide already in the eager graph); `loading` spent only as a hide-flag (:9, :44 — the affirmative treatment arrives with GAB-4's cure). lucide rename (D-i5) = 1 of the census's 35 booked sites, not re-booked. → **F.W4** riders.

### INFO

- **GAB-29 · missed (worker-DU) — MetricPill, the producer's own stacked-tile composition, recorded as a cure-falsifier, NOT a destination.** Installed at `dist/components/ui/metric-pill/` (doc verbatim: the audit-B-spec stacked recipe, a `density` knob driving the exact padding tokens `.admin-stat` re-derives by hand; prop surface a superset of everything this file passes INCLUDING `color`), reachable via the root barrel only — **no `./metric-pill` subpath in the 80-key map** (my check), and **no MetricPill source exists at v8.0.0** (my `ls-tree` — it dies with MetricBadge). Registry sweep → genuinely unbanked. Its value: the producer's stacked-tile idiom sat one directory over while two axes prescribed a different primitive; the wave must not adopt it off the name either. **NO-WAVE-OWNER** (SS-3/SS-4 record; the F.W1 seat remains K-4's ruling).

## Killed-claims register

- **K-1 · L·D-2 Claim A** ("MetricBadge ships NO stacked geometry; the consumer's flex column is load-bearing; exhaustive grep → zero rules"): DEAD — this seat's byte read of the installed dist: `.metric-badge--label-stacked` defined in full at components.css:96-105 (column, flex-start, gap, max-width none, min-height 2.625rem, padding-block), `.metric-badge__row` at :112-116, delivered via style.css:3 → index.css → utilities. The grep did not run as reported; the predicted collapse cannot happen; `.admin-stat`'s :103-104 are redundant, not load-bearing. Both workers killed it; C-10 and D-M2 had read the rule correctly. A load-bearing MAJOR resting on a false absence-proof.
- **K-2 · L·D-9 (+ the same row inside L·D-1's leak table)** ("max-width: 8rem survives; tiles detach above ~51rem"): DEAD — components.css:101 relaxes it to `var(--metric-badge-label-max-width, none)` inside the stacked arm all six sites activate; same layer, later, equal specificity; the producer's own :84-86 comment says so. C's §5 parenthetical had already killed it — L filed it twice anyway.
- **K-3 · D §8 S-1** (formatBytes superlative: "no gap, no overlap … each boundary lands in exactly one arm"): DEAD — the falsifier tested branch SELECTION, never the rendered string. Executed by both workers and this seat: 1048575→`1024.0 KB`, 1073741823→`1024.0 MB`. A superlative and GAB-14 cannot both be right about the same six lines; the MINOR wins. Running the code beat reading the code.
- **K-4 · D-M3 / D §6 items 8–9 / L·D-6 (the prescribed cures)** — merged three-leg kill, every leg byte-confirmed by this seat: (a) MetricCell is DELETED at v8.0.0 (`ls-tree`; "a cell is a POSTURE of one readout" — v8 types doc), so adopting it converts one deletion into two; (b) `appearance="compact"` returns `"glass-wash rounded-lg p-2"` + `"text-mono-small …"` (metric-cell.js verbatim) — a glass surface + a LARGER value type, recreating the GAB-6 override fight and worsening GAB-8, against the d.ts's own contradictory header (D quoted the wrong sentence without reading the implementation); (c) `./metric-cell` is not a subpath at v7.0.0 (74 keys, `./metric` only — naming it lands a second dead specifier). D §6 item 9's "MetricCell in a plain CSS grid" dies with (a): at 8.0.0 `posture="cell"` IS the tile seat on the one atom. Ruled seat: **`<Metric posture="cell">` from `./metric` at 8.0.0** (FMD-11 baseline law), gaining `compact` + `loading`. The duplication OBSERVATION (44 of 107 lines) survives.
- **K-5 · D-B3's rename row + worker-LC's inherited "orientation is the successor" cell**: DEAD at the ruled baseline — v8.0.0 renames the axis AGAIN to `posture` (`MetricPosture = inline|stacked|cell|row`, `data-posture` stamped — my tag reads); the second rename of one axis inside one major, the FMD-11 Slider pattern exactly. A wave budgeting `labelPosition → orientation` writes a prop that does not exist. The BLOCKER-weight facts of GAB-2 (subpath, `color`) stand at both tags.
- **K-6 · D §8 S-4 (naming half) + worker-LC superlative #5 (same sentence)**: DEAD/WITHDRAWN — the praised divergence is the GAB-24 defect (K-10's containment runs the failing direction). The aria-hidden ×2 + named-region halves of both survive.
- **K-7 · D-M5's count** ("Three of the six siblings use `unit`"): CORRECTED — FOUR (my grep; D's own §0 comparanda table lists the omitted EquationView row). Finding unharmed; a wave budgeting the idiom off "3 of 6" undercounts the precedent 25%.
- **K-8 · C-1's BLOCKER rationale** ("uncurable in place; requires widening the props contract"): DEAD — a `v-else` on the existing two-prop contract is a correct, complete failure arm today (R-1 ruling); the upstream cure is one line. Defect sustained at MAJOR (GAB-3).
- **K-9 · D §9's substrate certification** ("Every read here is against live web/src at that coordinate", byte-identical per R4-9): DEAD for the subject and web/package.json — the reads were against the dirty worktree (my `git status`/`git diff`/`git show` runs). Verifying a hash then reading the working tree is the certification failing open. → GAB-13.
- **K-10 · D-B1's `dist/dark.js:17` citation** (and worker-LC's repetition of it): CORRECTED — fourier seeds `.dark` from its own inline script at web/index.html:20-30 (keys `theme` / `vueuse-color-scheme`); glass-ui's sync script is not in the path. Same conclusion, wrong mechanism; recorded as DISSENT-1's citation-inheritance event.
- **K-11 · C-12's "double blink" symptom (and worker-DU's "remounts, then unmounts again" cell)**: CORRECTED — nothing re-raises `loading` after A's `finally`; the observable is one premature stale remount + a lying flag (R-5 re-derivation). Mechanism intact → GAB-20.
- **K-12 · D-M7's verification-value limb (and worker-DU's partial sustain of it)**: DEAD — `total_entries ≡ featured+saved+normal` by construction (admin.py:117-127, my read); the seventh tile would verify a tautology. → GAB-16/GAB-17.
- **K-13 · D-m1's provenance cells** ("15 call sites"; "Not in the corpus"): DEAD — 26 occurrences / 17 files (my count, matching banked FR-AH-2 exactly); banked twice (FR-AH-2, m-3). The defect folds; the novelty claim dies. Meta-note shared with K-1: **an absence-proof must enumerate the surface it searched** — D's registry-blind grep and L's not-run-as-reported grep are the same failure class the AdminAuditLog seat codified (its S-8).

## Missed-rows register (adopted; provenance = the worker that found it; premises re-verified by this seat)

Worker-DU: **2.5.3 Logout** (→ GAB-24, + kill K-6) · **glyph-scale override** (→ GAB-25, FR-COB-18 family) · **MetricPill** (→ GAB-29, the cure-falsifier) · substrate-at-INFO (concurrence absorbed into GAB-13).
Worker-LC: **substrate uncommitted** (→ GAB-13, ruled MAJOR, + kill K-9) · **deactivateAdmin no-abort** (→ GAB-21) · **total_entries derived** (→ GAB-16, + kill K-12) · **thumbnail bytes uncounted** (→ GAB-15 second leg).
Count: 7 adopted — 6 as rows, 1 as a second leg; three of them kill or correct a cell in the original corpus.

## Superlatives (with provenance; all re-verified)

- **S-1 · The D axis's contrast table is independently reproducible to two decimals — now three times.** Both workers recomputed from token bytes; this seat recomputed a third time with its own pipeline and landed within 0.03 on every disputed row (1.030/1.252/1.603/1.060/1.453/2.584/3.264/15.399; fallbacks 1.470/2.239). A design claim engineered to be falsified by arithmetic alone is the corpus's rarest artifact and should be the SS-13 bar.
- **S-2 · The D axis's line-anchor precision into the producer dist is exact** — sixteen-for-sixteen on the components.css table (workers), spot-confirmed on every anchor this seat pulled (:15, :96-105, :101, :112-116, the chunk class strings, the button recipe). It is what made K-1/K-2 mechanically decidable in both directions.
- **S-3 · Running beats reading, decisively** — the L axis alone executed formatBytes and found the boundary bug; the D axis walked the same six values, checked branch selection, and awarded the superlative K-3 killed. Both workers and this seat re-ran it; the standing lesson: **a falsifier must test the output, not the dispatch**.
- **S-4 · The C axis's §5 killed-candidates section did the falsifier L skipped** and pre-killed L's max-width MAJOR before it was filed (K-2), and its K-1/K-2 delivery-path falsifiers (the `@source "../*.js"` cure, the glass-ui.css fold) are the reason no false "styles never paint" row exists in this corpus. Recording negatives with mechanism is fleet law; this is its best recent specimen.
- **S-5 · C-12 is the corpus's most non-obvious true finding** — a three-hop static derivation (constant abortKey → shared-registry abort → orphaned finally) with the correct house comparator cited (fetchNextPage's guard). Its symptom half needed correction (K-11); its mechanism half survives every re-derivation, including this seat's.
- **S-6 · C-2 flagged its own weakest leg** ("CORPUS-DERIVED, not independently re-measured at producer HEAD") — and that declared provenance tier is exactly what let both workers discharge it cleanly at the tags. Declaring the provenance tier of each leg is what makes a corpus auditable; contrast K-9, where an undeclared substrate assumption failed open.
- **S-7 · D §7's kill discipline survives whole**: K-4 (touch-floor clamp read correctly), K-5 (inverted into D-M2's cascade evidence), K-6 (PRM transition-clamp read correctly, escalation refused), K-7 (inline-style route understood — only the hex convicted). All four re-derive at this seat's reads.
- **S-8 · The component's own four virtues, verified and to be preserved through every cure**: genuinely presentational (two props, one payload-free emit, zero store imports vs three token-minting siblings — every state defect above is host-curable); `label-position="stacked"` buys label-before-value AT reading order for free, and the 8.0.0 atom preserves the order (both workers verified the emission order in the compiled fns); `coalesceMetric` tests `e == null || e === ""` so a valid 0 renders `0` by construction; the icon/label aria pass is complete here and nowhere else in the directory — qualified by GAB-24: the one naming divergence in it is a defect, not the crown.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. GAB-1/GAB-9 figures on a live viewport, both theme arms (three-way static reproduction stands; real cascade/backdrop and whether the panel host's ground is `--background` vs `--card` — moves nothing >0.01 — unmeasured); axe pass once GAB-10's admin sweep exists.
2. GAB-1's cure direction: what `--warning`/`--destructive`-family tints actually measure against the light page — prescribed in kind, unmeasured in figure.
3. GAB-4's exact collapse (1-row vs 2-row auto-fit packing at a real gallery-panel width; ≥42px floor static-certain; note the 768px root fork).
4. GAB-8's ellipsis threshold in glyphs per arm (mechanism static-certain; 90px/80px tracks differ per arm).
5. GAB-5's perceptual half — whether the 1.02 lift on a 1.06:1 tile reads as a click affordance.
6. GAB-27 — whether six backdrop-filter layers actually composite in the default theme (reduced-transparency arm can zero them), and whether anything non-uniform ever sits behind the banner.
7. GAB-20/GAB-21 — the visible sequence under two real overlapping admin actions, and the post-logout re-seed window's observability.
8. L §4 S-4's residual — whether `.text-mono-micro{}` is emitted into the app's REBUILT css (`@utility`, rides the `@source` backstop; the committed dist artifact is stale and proves nothing) — plus F.W1's vue-tsc failure-mode rehearsal at 8.0.0 (FMD-11's open question: error vs silent fallthrough).

## Closing verdict

**ADJUDICATED — the half-read primitive on an invisible chassis, audited on bytes no commit holds.** 1 BLOCKER sustained (GAB-1, three-way-reproduced light-arm erasure of the component's purpose); 12 MAJOR — two of them ruled DOWN from worker-DU's BLOCKERs on the registry's own precedents (FMD-11, the AA-1 distinction) and one ruled UP from worker-DU's INFO (GAB-13 → **F.W0 before any budget commits**); 15 MINOR / 1 INFO routed — the mass to **F.W4** (failure arm + store honesty + live region + the token/registers cluster), the uplift seam to **F.W1** under the ruled seat **`<Metric posture="cell">` at 8.0.0** with two GLASS-RELAY asks (tier-tint home; the 3.26:1 label rung), the API denominator cluster to **F.W5–W8**, the gate extension riding AA-44 to **F.W9/W10**, MetricPill recorded NO-WAVE-OWNER as the falsifier that keeps the wave off two dead primitives. 13 kills — among them both original axes' migration cures, one substrate certification, one superlative each way, and one citation-inheritance event caught because two workers disagreed about a line only one of them had read. Preserve S-8's four virtues through every cure; the six worst tiles in the light arm sit inside one of the cleanest contracts in the gallery cluster, and the repair must not trade the second for the first.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet, not the tree).** Worker-LC's D-B1 cell repeats the challenge's `dist/dark.js` seeding mechanism verbatim; worker-DU re-derived and found fourier's own inline script (web/index.html:20-30 — confirmed by this seat). LC's conclusions survive; the cell was inherited, not derived — the ColorSpaceSelector-apotheosis event class, second recorded instance. Stale-cell repetition without re-derivation is detectable and is itself evidence.
2. **Worker-DU's BLOCKER positions on GAB-2 and GAB-3**, preserved with revival conditions: GAB-3 escalates if the failure state ever renders an affirmative claim (the AA-1 shape) or the host wires the banner's absence to any privileged action's enablement; GAB-2 escalates if F.W1's rehearsal shows the silent-fallthrough mode shipping to production rather than dying in the atomic transaction's gate.
3. **Worker-LC's "BASELINE LAW" tag re-proof vs FMD-11's 8.0.0 budget law** — both preserved: re-proving at the named tag is correct evidence discipline (it caught D's live-tree citation slippage); budget and cure-naming nonetheless bind to 8.0.0 (FMD-11). The two are compatible and the corpus should keep doing both.
4. **Producer live-tree drift, third recorded instance**: worker-DU read producer HEAD `8323c9b7`; this seat reads `350f7a90`; the AdminAuditLog seat recorded the same class at the 66/67-key measure. Version 8.0.0 and both tags stable — drift, not error; conclusions unchanged. Pin tag-reads, never HEAD-reads, in wave specs.
5. **Worker-DU's grading instinct on the substrate** (INFO, "the fleet has already elected to treat the working tree as the scope") — preserved as the counter-position to GAB-13's MAJOR: if F.W0 formally adopts the worktree as baseline, GAB-13 discharges to a disclosure line and DU's grading was the right one all along.

— end of apotheosis. The three challenge records and both worker returns remain immutable beside this file per E-1/E-3; this file is the session's only write.
