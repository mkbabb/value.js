claude-fable-5 (served model id)

# fr-SpeedSelect — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/visualization/SpeedSelect.vue` (70 lines, read whole by this seat; matches both readers' quotes byte-for-byte at :11-17, :23-26, :30-44, :48-69).
**Substrate**: fourier live tree (subject clean in WT) · installed glass-ui **4.0.0** (`web/node_modules/@mkbabb/glass-ui/package.json`, re-pinned by this seat) · producer glass-ui read AT THE TAGS: `v7.0.0` = tag obj `62ec299a` → commit `4ab12128`; `v8.0.0` = tag obj `478aa462` → commit `17a11bc5`; producer HEAD at this seat's read = **`407de2d3`** — moved again past the `96f0f257` both readers pinned. Pin evidence to tags, never to a working tree (the K-14 law, third occurrence).
**Corpus**: three axis challenges (`audit/fourier-components/SpeedSelect/challenge-{D-design,L-library,C-consumption}.md`, load-bearing rows re-read by this seat at :390-400, :428-436 (D), :134-141 (L), :391-404 (C)) × two hostile re-reader returns (**R1-DU** design-and-usage · **R2-LC** library-and-code), received in-brief.
**Method (M-23/L-18)**: every verdict-level disagreement between the readers was re-derived against the tree before ruling; every load-bearing kill was re-attacked. **One reader kill was itself killed by this seat (R2-LC's kill of D-05 — its central premise is false in the installed dist), one reader identity cell was killed (R2-LC's "SS-C-7 not banked"), one apparent inter-reader factual contradiction was dissolved (the v8.0.0 hash — annotated-tag object vs commit), and one reader SS-13 residue was settled statically (the ellipsis question, via UAX-14 LB23).** Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; product source untouched; this file is the sole write)**: SpeedSelect.vue whole; stores/animation.ts whole (146 — the :53 guard set, :56 `dur`, :63 latch, :67-69 parity, :116-126 scrub pair, :138-143 speed watcher); useWorkspaceLoader.ts:30-65 (`{once:true}` at :59, `if (as?.speed)` at :57, route watcher :35-47); AnimationControls.vue :58-61/:74-76/:94-98/:103/:115-117/:131/:204; `@reference` census over `src/components/visualization/` → **10 hits / 9 files** (widening R1-DU's four); `public/fonts.css:64-77` (self-hosted Fira Code, `font-display: swap`, unicode-range incl. U+00D7) + `index.html:19`; installed dist reads — `dist/styles/typography.css:44` (**imports `./typography/scale.css`** — the D-05 decider), `typography/scale.css:64-72` (**`@font-face "Fira Code Fallback"` — `local()` sources, ascent/descent/size-adjust overrides, zero payload, ON the critical path**), `glass/surfaces.css:241-250` (`.control-surface` four declarations + the `:hover` lift rule) and `:190-222` (`.input-pill` recipe + the "~15%α" prose ×2), `tokens/glass.css:160-183` (`--glass-border-floating` **19%** · the `--control-surface-*` register + hover mix), `dock/shell.css:79-96` (`contain: paint` + the nested-backdrop-cost rationale) + `:130` (dock's own `backdrop-filter`), `dock/layers.css:148-153` (inactive layer `opacity:0; visibility:hidden; pointer-events:none`), `dock/morph.css:331-336` (`:where(.glass-dock)` re-points `--muted-foreground`) + `:360-370` (the `@supports (color: contrast-color(white))` arm — the D-07-falsifier decider), `grep -c "@layer" dist/styles/components.css` → **0** (this seat's own run), `grep -c font-family dist/styles/select.css` → **0**, `dist/dock.js` — **`inert: !K(S) || void 0`** on `"dock-layer dock-layer--full"` + the complement on the summary layer (this seat's own extraction — the SS-C-15 executioner); producer at the tags — `git show v8.0.0:src/components/select/SelectTrigger.vue` (**props = `{disabled?, class?}` ONLY; variant/size REMOVED; the "ONE trigger, one register … a picker with no edge is not a picker" ruling comment verbatim; class list pins `control-surface glass-control-edge glass-capsule-hover h-(--control-h-md)` unconditionally**); registry re-reads — fr-AnimationControls :29 (R-7) / :54 (**C-2, "Four doors … (speed watcher, …)"**) / :58 (L-1, L-3) / :91 (**L-11+M-10, "drops a legit 0 … Off-set speed renders an empty trigger beside a live 3× badge"**), fr-GalleryCardModal :11+:42 (**GCM-1**, `loadVisualization` zero callers, → F.W4) and its instrument line (**"Plus Jakarta Sans Fallback IS declared — four local() metric faces"** — independent corroboration that the metric-fallback faces ride the critical path), fr-AppHeader :48 (**FR-AH-2 · MISS-FONT**) + :57 (FR-AH-11), fr-ConvergenceTimeline :7/:9/:25/:54 (**SR-1 / D·D-2 — the 8.0.0 prop-surface-behind-retained-subpath census class, producer then at "8.0.0 LIVE"**), fr-ContourSettings :61 (K-3/M-4 — glass-ui's cn DOES merge the height group, which is exactly why a bespoke class name escapes it).

## Scoreboard

**27 rows ADJUDICATED-CONFIRMED as booked (8 MAJOR · 10 MINOR · 9 INFO, counting all 6 reader-missed rows banked) · 6 identity FOLDS to banked registry rows (never re-booked) · 15 killed-register entries (1 minted by this seat against a reader's kill, 1 against a reader's identity cell) · 0 BLOCKERs survive as booked.**

Centre of gravity: **the corpus's unanimous #1 prescription is dead at the producer's true head.** All three axes converge on `<SelectTrigger size="sm" variant="ghost">`; both props were REMOVED at glass-ui v8.0.0 with no alias, on the producer's own written design ruling ("a picker with no edge is not a picker") — re-proved by this seat at the tag. Every geometry/material cure below is re-authored as CSS-or-catalog, none as the dead props, and the *design direction* of the invisible desktop trigger is now contra-producer — an F.W1 design decision, not a mechanical port.

## Ruled disagreements (the seat's actual work)

| id | R1-DU | R2-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-05** (dropped `"Fira Code Fallback"` rung) | CONFIRMED MINOR | **KILLED** ("the fallback face ships ONLY via the `/styles/fonts` subpath fourier never imports; differential exactly zero") | **CONFIRMED MINOR — R2-LC's kill is KILLED (K-10)** | R2-LC conflated the OFL woff2 *real-face corpus* (fonts subpath — FR-AH-2's fact) with the ZERO-PAYLOAD `local()` *metric-fallback* faces. This seat's dist reads: `typography.css:44` `@import "./typography/scale.css"`; `scale.css:64-72` declares `@font-face { font-family: "Fira Code Fallback"; src: local("SF Mono"), local("SFMono-Regular"), local("Menlo"), local("Consolas"), … ; ascent-override: 99.0161%; descent-override: 32.2052%; size-adjust: 99.9837% }` with the in-file rationale "carries NO payload, stays on the critical path, and covers the `font-display: swap` window". fourier imports `@mkbabb/glass-ui/styles` → index.css:150 `./typography.css` → scale.css. So under the token stack the swap window falls to a metric-matched local face; under the hand-spelled `"Fira Code", monospace` it falls to generic platform monospace inside a fixed clipping box. The differential is real, not zero. fr-GalleryCardModal's instrument line independently corroborates the fallback-faces-on-critical-path fact. R1-DU's own kill of the D §7 cure stands (see K-3): the fix is family-only `var(--font-mono)`, never `text-mono-small`. |
| **D-03 / SS-L-03 severity** (orphan `backdrop-filter`) | MAJOR (paint cost over the 60fps canvas) | MINOR (causal chain to the canvas refuted) | **MINOR — R2-LC wins** | Declaration real (surfaces.css:244-245 un-reset by `:55-56`; the blur token chains live). But this seat re-read the bounds: `dock/shell.css:96` `contain: paint` + `:130` dock-level `backdrop-filter` make the dock the trigger's backdrop root — the blur samples DOCK contents, never the epicycle canvas beneath (the producer's own :79-83 rationale names exactly this mitigation); and `layers.css:148-153` makes the hosting layer `visibility: hidden` in the dock's RESTING (collapsed) state, so the declaration does not paint at all at rest. Dead-CSS hygiene, not a frame-budget defect. Residual same-box cost → SS-13. Note the 4.0.0-era "dies free with `variant="ghost"`" disposal is ALSO dead at 8.0.0 — the cure is an explicit reset of both `backdrop-filter` longhands, or honest adoption of the register. |
| **SS-C-7 identity** (speed change re-seeds ping-pong forward) | FOLD to banked fr-AnimationControls **C-2** | "Not banked: R-2 reads the watch(speed) edge only as recovery" → books MAJOR | **FOLD — R1-DU wins; R2-LC's identity cell KILLED (K-11)** | fr-AnimationControls:54 **C-2** verbatim: "Every transport verb resumes forward … Four doors, all owned by this dock (**speed watcher**, `endScrub`, `setCanvasVisible(true)`, `play`)" — route **F.W4** store repair (+ F.W1 engine rider). R2-LC checked R-2 and stopped one row short. Its independent re-derivation (`cycle = Math.floor(t) = 0` for all t∈[0,1); parity derived, never stored — animation.ts:63-69 re-read by this seat) is adopted as evidence enrichment on the banked row, not a booking. |
| **D-08 / SS-L-01 / SS-C-6 + SS-L-06 identity & grade** (bare `<SelectValue/>` empty trigger; `if (as?.speed)` drops 0) | FOLD to banked L-11+M-10, MAJOR-as-banked | Mechanism MINOR-latent + SS-L-06 booked separately | **FOLD both — R1-DU wins on identity** | fr-AnimationControls:91 **L-11+M-10** carries BOTH legs verbatim: "`if (as?.speed)` drops a legit 0" AND "Off-set speed renders an empty trigger beside a live `3×` badge" — route **F.W4** (validate at the restore seam). The reka `SelectValue.js` placeholder-`""` internals (both readers, identical quotes) are enrichment on that row. R2-LC's kill of L's reachability leg is RATIFIED (K-12): the only live hydration is IndexedDB (`loadWorkspace`); the server leg is GCM-1's zero-caller `loadVisualization`. Latency is a property of the banked row, not a reason for a second one. |
| **SS-C-1 booking** (speed atom write-only-at-create) | Whole row MAJOR → F.W5 | Split: read-leg folds to GCM-1; write-leg new | **SPLIT — R2-LC wins** | fr-GalleryCardModal:42 **GCM-1** already banks the read-back leg (`loadVisualization` definition :197 / alias :238 / exports :447-448, zero callers — the identical grep) at blocker weight → F.W4 + F.W5-W8 rider. The WRITE leg — `VisualizationUpdate`/`VisualizationPatch` = the same five fields, `extra="forbid"`, while `animation_settings` is atom 4-of-5 in `atomdiff.py:38` — is genuinely un-banked and books MAJOR → **F.W5-W8**. |
| **SS-C-9 grade** (tooltip on a non-focusable div) | MINOR-as-banked | Kill of C's MAJOR as inflated re-book | **Agreement in substance — FOLD at MINOR** | fr-AnimationControls:29 **R-7** ruled this exact anchor MINOR ("duplicates accessible names already present … `SpeedSelect`'s own `aria-label`"); identity stays with intake **R3-7a** → F.W3. C's MAJOR at this coordinate does not survive; neither reader re-books. |
| **D-01 sub-claim** ("even `1×` loses; cannot display ANY value") + the ellipsis question | Killed as sub-pixel (0.8px) | Confirmed, with the no-wrap silent-clip derivation | **BOTH halves adopted; one SS-13 row SETTLED STATICALLY** | The 16.8px-vs-16.0px overflow is real arithmetic but sub-pixel rhetoric — dropped before any repair spec (R1-DU right); the finding is fully carried by `0.25×`/`0.5×` (42px vs 16px desktop / 47px vs 24px mobile). R2-LC's derivation is the keeper and it CLOSES R1-DU's unproven[0]: `[&>span]:line-clamp-1` compiles to `-webkit-box` + `overflow:hidden`; `-webkit-line-clamp`'s ellipsis fires only on a WRAP past the line count; UAX-14 LB23 (`NU × AL`) forbids a break inside `0.25×` (digits·period·U+00D7) — the string can never wrap, the ellipsis can never fire, the clip is silent mid-glyph. Proven without a browser. |
| **D-07 grade** | MINOR (with the portal second leg + a corrected falsifier) | INFO | **MINOR — R1-DU wins, narrowly** | Not purely latent: this seat read morph.css:360-370 — under `@supports (color: contrast-color(white))` the dock re-points `--muted-foreground: contrast-color(var(--card))`, so on supporting engines the desktop trigger and the portaled compact instance render DIFFERENT ink today. Dead intent + a live engine-conditional divergence = MINOR. D's original falsifier is unsound (K-8); the three-valued probe replaces it. |
| **v8.0.0 hash "conflict"** | `v8.0.0` = `17a11bc5` | `v8.0.0` = `478aa462` | **DISSOLVED — both right** | Annotated tag: `git rev-parse v8.0.0` → tag object `478aa462`; `v8.0.0^{commit}` → `17a11bc5` (this seat's run). No contradiction of substance. Recorded so no wave burns time on it. |
| **R2-LC missed row** (startLoop unguarded on `scrubbing`) | — (not returned) | MINOR | **RATIFIED MINOR, with a reachability bound minted by this seat** | animation.ts:53 guard = `!playing || !anyCanvasVisible || rafId !== null` — `scrubbing` absent while the sibling park-condition is present; :116-119 parks the clock with `playing` deliberately true; the speed watcher (:138-143) re-arms unconditionally. Mechanism certain. BUT the harmful polarity needs a speed write DURING a live drag (multi-touch, or `setCanvasVisible(true)` mid-scroll-drag) — and in the STUCK-latch state (banked L-1) the same restart is R-2's documented *recovery*. One predicate cures; harm window narrow. MINOR holds, no higher. → F.W4, same store edit as C-2's direction-becomes-state repair. |

## Adjudicated defect roster (booked rows — id · severity · claim · proof · route)

**MAJOR (8):**

| id | claim | proof (line-exact, re-anchored by this seat) | route |
|---|---|---|---|
| **D-01 / SS-L-04** | Fixed trigger widths (`:51` 3.5rem / `:63` 4rem) are smaller than furniture + label in every band; the two long labels lose 60%+, silently (mid-glyph — the clip is now statically proven silent, see ruled row 7). Demoted from D's BLOCKER under the banked still-operable law (fr-FunctionInput r.2): value survives in the accessible name/value. | Subject :51/:63/:53-54/:65-66; base trigger `px-3 py-2 [&>span]:line-clamp-1` + `h-4 w-4` chevron (both readers' identical chunk extraction); bands `style.css:41`/`:45-47`; D's three-band arithmetic is the sound one (L's table killed, K-6). Desktop: 56−24−16 = 16px vs 42px for `0.25×`; <640px compact-only: 72−27−3−18 = 24px vs ~47px. | **F.W4** (delete the widths; let `w-full`/content size govern) — the fix CANNOT be `size` props (dead at 8.0.0, K-14 context) |
| **D-02 / SS-L-02 / SS-C-5** | Literal heights (`:50` 1.75rem / `:62` 2rem) opt out of `max(calc(2.5rem·--ui-scale), --control-floor)` — defeating the coarse-pointer WCAG floor on the touch-only path: 36px painted vs the cohort's 67.5px at <640px, where the compact form is the ONLY speed control. MAJOR not BLOCKER: SC 2.5.5 is AAA; 2.5.8 AA (24×24) met at 36×72. | `offsets-sizing.css:148/:151` + `light-dark.css:17-21` (re-read); cascade decided by unlayered (0,2,0) vs unlayered (0,1,0) — this seat's own `@layer`-count 0 on components.css; scope-id reach proven at the banked fr-AnimationControls D-1 mechanism. Painted px → SS-13. | **F.W4** — delete the literals; the base `h-(--control-h-md)` (pinned unconditionally at 8.0.0) then governs |
| **D-03 / D-11 / SS-L-08 / SS-C-4 / SS-C-11** (merged) | The entire 22-line scoped stylesheet hand-rolls what the installed 4.0.0 already shipped as `variant="ghost"` + `size="sm"` — minus the parts that matter (the un-reset blur, the un-reset hover, the off-cohort heights). At 8.0.0 the props are GONE, so the hand-roll's *replacement* must be a design decision, not a prop swap. | Ghost arm verbatim in the 4.0.0 chunk (both readers); v7.0.0 props at the tag (R2-LC, `git show`); v8.0.0 removal at the tag (**this seat's own** `git show v8.0.0:…SelectTrigger.vue` — props `{disabled?, class?}`, the ruling comment, the unconditional class list). | **F.W4** (the CSS is re-authored against the 8.0.0 single register) + **F.W1** (the design decision: the producer has RULED against invisible pickers) |
| **D-04 / SS-C-3** | Trigger and items render the same glyphs in two typefaces — trigger forced mono (`:53`/`:65`), portaled items inherit the app cascade. BOTH axes named the wrong item face (Computer Modern Serif); the items resolve `font-serif` → glass bridge → Plus Jakarta stack → UNLOADED → system sans. Worse than filed: mono pill over OS-sans rows. | `select.css` font-family count 0 (this seat's grep); `bridges.css:68` → `scheme-motion.css:43`; family-attribution error FOLDS to banked **fr-AppHeader FR-AH-2 · MISS-FONT + FR-AH-11** — not re-booked. | **F.W4** — one edit with FR-AH-2's wire-the-fonts-or-commit decision; live fontFamily read → SS-13 |
| **D §5 U-2** | Across the uplift the DEFAULT trigger gains `glass-control-edge` (box-shadow rim `border:none` cannot null) + `glass-capsule-hover` (scale transition) — and at 8.0.0 this is **unavoidable by prop**: the corpus's "cured for free by `variant="ghost"`" inverts. Requires an explicit `box-shadow: none` (nowhere prescribed in the corpus) or abandoning the invisible-trigger design. | v7.0.0 ghost/edge/capsule verified at the tag by R2-LC (`SelectTrigger.vue:44-48`, `field-surfaces.css:84-86`, `glass-capsule.css:99-108`); v8.0.0 unconditional composition verified at the tag by **this seat**. | **F.W1** — with the SelectTrigger instance booked as a DELTA on the banked 8.0.0 census class (fr-ConvergenceTimeline **SR-1/K-8/D·D-2** — prop-surface breaks behind retained subpaths; FOLD for the class, this instance was unlisted in that budget) |
| **SS-C-1 (write leg)** | The persisted `speed` atom is settable only at CREATE/FORK: `VisualizationUpdate` (api) and `VisualizationPatch` (client) are the same five fields with `extra="forbid"`, while `animation_settings` is atom 4-of-5 of the version-identity set. Silent non-persistence of a preference the control claims to set. | `visualization.py:198-210`; `types.ts:256-262`; `atomdiff.py:38` (R2-LC's endpoint census adopted; spot-anchored). Read leg FOLDED to GCM-1 (below). Demoted from C's BLOCKER at component altitude (no capability loss in the control). | **F.W5-W8** (CRUD/provenance union) |
| **SS-C-2** | Atom 4 is semantically empty: `fps`/`max_circles`/`duration` have zero cross-wire readers, and the `duration` that `speed` divides is declared 4× with 3 values and a 1000× unit fork (20000ms store / 5000 client default / 30.0s server). `speed` agrees by luck, unconstrained on both sides (`shared.py:70`, no validator, beside a sibling carrying four). | Reader censuses agree and were spot-anchored by this seat (animation.ts:23; loader :53-58 seeds three fields only). Partial fold: `duration = ref(20000)` zero-writers is banked at fr-BasisCanvas **BC-20** (instrument), not re-booked. | **F.W5-W8** |
| **SS-C-8** | `{once:true}` animation seeding + in-place `/w/:slug` navigation + the 500ms-debounced writer over the same object = workspace B's persisted speed clobbered by workspace A's live value. Every structural precondition verified; only the fired ordering is unproven. | `useWorkspaceLoader.ts:50-60` (`{once:true}` at :59, this seat's read); route watcher :35-47 supports in-place nav; writer `VisualizationView.vue:52-64` + draft persist `workspace.ts:107` (R2-LC, verbatim). | **F.W4** (per-workspace re-seed at the loader) · ordering witness → SS-13 |

**MINOR (10):**

| id | claim | route |
|---|---|---|
| **D-05** | Hand-spelled `"Fira Code", monospace` drops `"Fira Code Fallback"` — the zero-payload metric-matched `local()` face glass-ui puts ON the critical path precisely for the swap window this fixed clipping box maximally suffers. R2-LC's kill is killed (ruled row 1). Cure: family-only `var(--font-mono)` — NOT `text-mono-small` (K-3). | **F.W4** |
| **D-06** | `@apply text-sm` replaces the `--control-text = calc(--type-small · --ui-scale)` comfort chain with a constant; on coarse pointers every sibling control's label grows 1.5× and this one does not. Survives the corpus's own prescribed cure verbatim (K-3). | **F.W4** (restore `text-dropdown` by deleting :54/:66) |
| **D-03-as-demoted / SS-L-03** | `border:none; background:none` strips the two cheap `.control-surface` declarations and orphans the two expensive ones — a live `blur(8px·--glass-level)` on a transparent element. Real dead-weight; canvas-cost chain refuted (ruled row 2). | **F.W4** — explicit reset of BOTH `backdrop-filter` longhands, or adopt the register honestly |
| **MISS-HOVER** (R1-DU missed[1], ratified) | `background: none` also deletes the producer's designed rest→hover step (`surfaces.css:248-250` `--control-surface-bg-hover`, read by this seat) and the 4.0.0 base carries no other hover mechanism — the desktop control has NO hover state: no border, no fill, no shadow, no hover change; sole rest affordance a 16px `opacity-50` chevron. Capability loss no axis charged. Class precedent (different mechanism): fr-CanvasOverlayButton FR-COB-7. | **F.W4** — same edit as D-03 |
| **D-07** | `color: var(--muted-foreground)` (:58) is dead intent — inside `.glass-dock` it resolves to `--foreground` (morph.css:331-336), which the portaled compact instance inherits anyway — EXCEPT on contrast-color engines, where the two instances diverge today (morph.css:360-370, this seat's read). MINOR per ruled row 8; falsifier corrected (K-8). | **F.W4** (delete; if intent is real, express it outside the dock's re-pointed scope) |
| **D-10 / SS-C-14 + MISS-PILL** (R1-DU missed[2], ratified) | The compact block is not a free-hand token fork — it is the producer's `.input-pill` register transcribed by value (1.5px border · pill radius · fixed height · retained fill; `surfaces.css:196-219` re-read by this seat), with the alpha frozen at the producer's STALE PROSE figure (~15%α at :198-199/:209-211) vs the shipped token (19%, `glass.css:162`). Answers D-07's "why is mobile more enclosed": two producer registers hand-copied into one file. The 14-month `:57`/`:68` carry still resolves line-exact. | **F.W4** — compact arm becomes `class="input-pill"` (survives 8.0.0; the fr-AdminUserList/fr-FunctionInput forward-safe register) + **GLASS-RELAY**: producer prose/token 15%-vs-19% drift (owner edict: every glass-ui-level find relays) |
| **SS-L-05 / SS-C-13** | `set: (v: string)` narrows reka's 5-case `AcceptableValue`; `parseFloat` unguarded; NaN would ride to `dur = duration/NaN` and survive `structuredClone` into the draft. Both axes' MINOR-by-reachability grading is the corpus's most disciplined call — ratified. | **F.W4** — dies with the catalog row below |
| **SS-L-07 / SS-C-10** | The value domain exists only as ten template literals; the adjacent control's idiom (`lib/easings.ts` → `EASING_OPTIONS` + `AnimationEasingName`) is one file away; no `lib/speeds.ts`, no `SpeedName`, `speed: number` on both wires. THE single highest-leverage change: a literal-union catalog makes the banked L-11+M-10 empty-trigger state unrepresentable at the type level. | **F.W4** (+ its type reaches the F.W5-W8 wire rows) |
| **SS-L-09** | Zero executing coverage: no vitest anywhere in web/, and the one mention of the component in the whole test surface — the recorded fix for an axe `button-name` CRITICAL — sits in the header of a permanently-`test.fixme`'d keystone (`visualization-ux.spec.ts:181-192`). | **F.W4** — un-fixme rides the F.W3 menu re-home (banked D-9/C-9); the speed round-trip joins `settings-persistence.spec.ts` |
| **MISS-SCRUB** (R2-LC missed[0], ratified with this seat's bound) | `startLoop()`'s guard (:53) omits `scrubbing` while including the sibling park-condition; the speed watcher re-arms the clock against a live drag. Narrow reachability (ruled row 10); one predicate cures. | **F.W4** — same edit as the banked C-2 direction-state repair |

**INFO (9):** **D-09** (`py-2` fully consumed by the literal height — 12px content box vs a 20px line box; nothing merged away, ContourSettings-K-3 mechanics) · **D-12** (`hideIndicator` shipped and unused; 128px panel on a 56px anchor; cure durable — prop survives at v8.0.0, R1-DU at the tag) · **D-13 + MISS-REF** (R1-DU missed[3], ratified and STRENGTHENED: `@reference "tailwindcss"` cannot see glass-ui's `@utility` registers, and this seat's census finds it house-style across **10 sites / 9 files** in the visualization directory — the repair is a RULE, "producer registers go in template `:class`, never scoped `@apply`", not ten patches; banked C-14 is the sibling instance; anchor correction: GlassTimeline is :80, not :79) · **D-14** (`flex-shrink:0` dead at :52 — block parent :95; live at :64 — flex parent :115; both parents re-read by this seat) · **D-15** (zero comments in 70 lines) · **SS-C-16** (both host sites hand-expand `:model-value`+`@update:model-value` around a published `v-model` — :96/:117 re-read) · **SS-C-12** (`SelectRootProps.disabled/name/required` forwarded nowhere) · **D §0 corollary** (glass-ui's `cn` is clsx + a Tailwind-shaped table; `"speed-trigger"` matches no group, so the base cohort ships and is overridden, never deleted — the load-bearing mechanism for D-01/D-09, banked precedent fr-ContourSettings K-3) · **MISS-WD** (R2-LC missed[1], ratified: `withDefaults(…, {compact:false})` buys nothing over Vue's boolean-absent casting; benign direction, stated because the producer took the opposite bite of the same mechanism at `ac471032`).

## Identity folds (banked — folded by reference, never re-booked)

| corpus row | banked identity | route lives there |
|---|---|---|
| SS-C-7 (speed → forward re-seed) | fr-AnimationControls **C-2** (:54 — the speed watcher is door 1 of 4) | F.W4 |
| D-08 / SS-L-01 / SS-C-6 + SS-L-06 (empty trigger; `if (as?.speed)` drops 0) | fr-AnimationControls **L-11 + M-10** (:91, verbatim both legs) | F.W4 |
| SS-C-9 / SS-L-11 (div tooltip anchor) | fr-AnimationControls **R-7 / D-10 / C-16** → intake **R3-7a** | F.W3 |
| SS-C-1 (read leg: `loadVisualization` zero callers) | fr-GalleryCardModal **GCM-1** | F.W4 + F.W5-W8 rider |
| D-04/SS-C-3 family-attribution + unloaded-stack truth | fr-AppHeader **FR-AH-2 · MISS-FONT** + **FR-AH-11** | F.W4 |
| The 8.0.0 substrate-drift CLASS (prop breaks behind retained subpaths) | fr-ConvergenceTimeline **SR-1 / K-8 / D·D-2** — the SelectTrigger variant/size instance books HERE as delta only (U-2 row above) | F.W1 |

## Killed-claims register

| # | claim (source) | refutation |
|---|---|---|
| K-1 | **SS-C-15** — combobox inside an `aria-hidden`, non-`inert` dock layer (C :391-404) | Dies by its own falsifier, sealed by THIS seat's extraction: `dist/dock.js` emits `inert: !K(S) \|\| void 0` on `"dock-layer dock-layer--full"` (+ the complement on the summary layer); no `aria-hidden` on those layers at all. CSS reinforces (`layers.css:148-153`). C's own witness is booked against ContourSettings' `ConfiguratorLayer`s, a different component. Registry corroboration: fr-FullscreenViewer instrument line. Both readers killed it; ratified. |
| K-2 | **SS-L-04 sub-claim** — "Fira Code not guaranteed loaded" (L :139) | fourier self-hosts it: `public/fonts.css:68-77` (`font-display: swap`, woff2, weight 300-700), linked at `index.html:19` — this seat's read. The subset even serves U+00D7. Residue is D-05's swap window, not an unresolved family. |
| K-3 | **D §7 step 2** — "`text-mono-small` … kills D-05, D-06, D-13" (D :431-435) | Refuted on D-06 (R1-DU, ratified): `text-mono-small` reads raw `--type-small`, not `--control-text = --type-small · --ui-scale` — it swaps one un-scaled constant for another; D-06 survives its own cure verbatim. Sound cure: family-only, `text-dropdown` left intact. |
| K-4 | **D §0 premise** — trigger geometry "emitted into `@layer utilities`" | This seat's own run: `grep -c "@layer" dist/styles/components.css` → **0**; the utilities ship unlayered at (0,1,0). Only `.control-surface` is layered. D's own falsifier anticipated it; conclusions unchanged; L's mechanism (:93) is the citation to carry. |
| K-5 | **SS-C-4 fragility mechanism** — "wins only on specificity; a producer move into a `@layer` would reinstate the chrome" | Both halves wrong: `.control-surface` is ALREADY layered, so the unlayered scoped rule wins by LAYER ORIGIN (stronger, not fragile); moving producer declarations INTO a layer weakens them against unlayered consumer CSS. The one leg that did fire at 7.0.0/8.0.0 is `box-shadow` — which C's list did name. Finding survives; C's reasoning must not be inherited. |
| K-6 | **SS-L-04 budget table** (L :134-137, re-read verbatim by this seat) | No cell arithmetically correct: subtracts 32/40px for a 24px `px-3`; 24px for a 16px chevron; computes the compact row at a band (rem 16) where it cannot render (`flex sm:hidden` + `html{font-size:1.125rem}` <768px); omits the 3px of border. Conclusion survives at the real bands; D's three-band table is the sound arithmetic. |
| K-7 | **D-01 sub-claim** — "even `1×` loses … cannot display ANY of its five values" | 16.8px vs 16.0px = 0.8px, ~10% of the trailing glyph — inside hinting/subpixel noise; cannot carry blocker weight. The finding rides the two long labels. (And the ellipsis half of the question is now settled statically — ruled row 7.) |
| K-8 | **D-07 falsifier** — "identical computed color today, else the selector is failing to reach" | Unsound: morph.css:360-370's `@supports (color: contrast-color(white))` arm makes divergence-on-supporting-engines the EXPECTED state with the selector reaching perfectly (this seat's read). Verdict unaffected; the SS-13 probe is three-valued. |
| K-9 | **Provenance slips** (C :201 `:37`-for-`:35`; C :211 `shared.py:69`-for-`:70`; C :50 `workspace.ts:238`-for-`:197`; D-06's `:174`-for-`:170`; D §5's `field/field-surfaces.css` path; the chevron `:82-84`-for-`:91-93` + `opacity-50`→token at v7; "`web/tests`" — does not exist, the surface is `web/e2e/`) | All resolve to wrong anchors in the live tree/tag; no verdict flips. Recorded because this corpus is provenance-graded and repair waves resolve anchors mechanically. This seat adds one: R1-DU's GlassTimeline `@reference` is :80, not :79. |
| K-10 | **R2-LC's kill of D-05** — "the fallback face ships ONLY via `/styles/fonts`; differential exactly zero" | **KILLED BY THIS SEAT** (ruled row 1): `typography.css:44` imports `scale.css`; the `local()` metric face is on fourier's critical path; the differential during the self-hosted swap window is metric-matched vs generic monospace. The reader inherited FR-AH-2's real-face fact into the fallback-face question — a scope-transfer error. |
| K-11 | **R2-LC's identity cell** — "SS-C-7 not banked; R-2 reads the edge only as recovery" | **KILLED BY THIS SEAT**: fr-AnimationControls **C-2** (:54) names the speed watcher as the first of four doors, at MAJOR, routed F.W4. One row below where the reader stopped reading. |
| K-12 | **SS-L-01 reachability leg** — "reachability is not hypothetical … any of the 45 operations may set speed=3 and the control is permanently blank" | `loadWorkspace` hydrates from IndexedDB only (`loadDraft`), never the API response; the server-sourced `loadVisualization` has zero callers (= banked GCM-1). No server-authored speed reaches `anim.speed` today. R2-LC's kill ratified; C and D graded it honestly. |
| K-13 | **SS-L-03/D-03 causal chain** — orphan blur "re-samples everything painted behind it" incl. the 60fps canvas | Dock backdrop root (`shell.css:130`) + `contain: paint` (`:96`, with the producer's own nested-backdrop rationale at :79-83) bound the sample to dock contents; collapsed-resting state is `visibility: hidden` (`layers.css:148-153`). Mechanism refuted, declaration real → MINOR (ruled row 2). Banked sibling mechanism: fr-AnimationControls M-4. |
| K-14 | **Baseline labelling** (D's "HEAD 7.0.0 `e286d992`"; C's `require()` probe of a live tree) | `e286d992` is 350 commits past tag v7.0.0 (R2-LC's rev-list, ratified); the path tell (`_shared/field/`) confirms. This seat adds: producer HEAD moved AGAIN (`407de2d3` at this read), and the readers' apparent v8.0.0-hash conflict is tag-object-vs-commit (ruled row 9). LAW restated: tags, never working trees — third banked occurrence of this failure class (fr-ConvergenceTimeline killed[8], fr-ColorSpaceSelector-adjacent K-12). |
| K-15 | **SS-C-9 at MAJOR** (C :282-320) | Re-book at inflated severity of a row banked and demoted at fr-AnimationControls R-7 (MINOR, "keyboard users lose zero information"). Identity stays with R3-7a. |

## Superlatives (with provenance)

- **S-a — zero-behavior surface** (L SUP-1 + D S-3/S-4, both readers, ratified): the SFC's entire behavior is one four-line writable `computed`; no lifecycle, listener, timer, observer, ref, or local motion — zero teardown obligation in a directory where fr-ConvergencePlot L-B2 banks an orphan rAF. L's self-bound kept: 20 of the 70 lines are the CSS carrying every defect.
- **S-b — the accessible name computes** (D S-2 + C SUP-3 + both readers at the primitives): `aria-label="Playback speed"` (:32) survives both reka (no `aria-labelledby` outranks it) and the 7.0.0 `inheritAttrs:false` filter (as/asChild only); WCAG 2.5.3 passes by substring against the visible "Speed" (:116). The recorded fix for an axe `button-name` CRITICAL, landed in the right file — guarded only by a `test.fixme` (SS-L-09).
- **S-c — import surface survives 4→7→8** (L SUP-2 + C SUP-1, extended by R1-DU, tag-verified by R2-LC): `exports["./select"]` byte-identical at v7.0.0/v8.0.0; all five symbols live; SpeedSelect touches none of the census break surface while its host carries three rows of it in five lines. The honest frame stands: import-surface immune, style-surface fully exposed (see U-2).
- **S-d — the glyph is served** (R1-DU's own, ratified by this seat's fonts.css read): U+00D7 lies inside the self-hosted subset's `U+0000-00FF`, so `0.25×` renders from ONE face with no fallback seam — a check no axis ran, and one that would have failed on a Greek or arrow glyph.
- **S-e — abstention as load-bearing design** (R1-DU's own, the strongest reading of D §5 U-7): `:37` passes `<SelectContent>` nothing, which is exactly why the value.js U-DROPDOWN collision-bound cure, `collisionPadding: 16`, popper positioning, and the trigger-width floor all reach this component for free — plus R2-LC's find that SelectContent stamps `data-glass-dock-portal`/`data-glass-dock-owner`, so the portaled listbox is dock-hold-aware unasked.
- **S-f — the lossless bridge** (C SUP-5 + D S-4): `String` ⇄ `parseFloat` is exact over the whole declared domain including Python-float JSON round-trip forms (`1.0 → 1`). The domain is the defect (SS-L-07); the bridge is not.
- **S-g — corpus discipline** (R1-DU's award, ratified): three axes published falsifiers that bit them and downgraded their own MAJORs (L SUP-1/SUP-2, SS-L-05, SS-C-13, SS-C-9's severed limb, C's negative-result reconciliation row) — which is why this seat's kills are narrow. Method superlative to challenge-C specifically (R2-LC's award): three self-inflicted demotions in one document.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **Clip witness per band/value**: `const s = document.querySelector('[aria-label="Playback speed"]>span'); [s.scrollWidth, s.clientWidth]` — arithmetic proven; the resolved Fira advance (0.6em) is the one estimated input. (The ellipsis half is SETTLED statically — silent clip, ruled row 7 — and is no longer a live question.)
2. **Coarse-pointer painted height** at 375×667: expect ~36px on the compact trigger vs the producer cohort's ~67.5px (`@media (pointer: coarse)` emulation; cascade already proven).
3. **Residual compositing cost** of the orphan blur with the dock EXPANDED and playback running — bounded by K-13 to the dock's own backdrop root; one trace vs a `backdrop-filter: none` local build, parsimoniously.
4. **D-07 three-valued probe** (corrected per K-8): desktop trigger `color` · compact trigger `color` · `CSS.supports('color','contrast-color(white)')` — identical-on-non-supporting confirms dead intent; divergent-on-supporting is the `@supports` arm, NOT a selector failure.
5. **Nested dismissal (C's L-2, still open)**: at <640px open ⋮ (`DropdownMenu :modal="false"`), open the speed listbox, observe whether `pointerDownOutside` dismisses the parent menu. R1-DU's stacking datum (both on `z-popover`; DOM order favors the Select) recorded, not settling.
6. **Trigger-vs-item computed `fontFamily`** — settles mono vs system-sans vs Computer-Modern three ways and simultaneously witnesses banked FR-AH-2.
7. **SS-C-8 ordering witness**: load workspace A → in-place navigate to B → touch speed/easing/bases → inspect B's draft row. All structural preconditions verified; the fired sequence is not.
8. **NOT DECIDABLE STATICALLY — owner/charter, not evidence**: whether F.W1's uplift target is 7.0.0 or 8.0.0. The corpus is framed at 7.0.0; the producer is past 8.0.0 (HEAD `407de2d3` at this read); the fleet has ratified the drift twice (fr-ConvergenceTimeline SR-1; this record's U-2). Every routing above is authored to survive either answer; the U-2/hand-roll rows are sharper under 8.0.0.

## DISSENT — preserved, not ruled away

1. **R2-LC's false kill of D-05 (K-10) is itself a scope-transfer event worth banking as method**: a banked registry fact (FR-AH-2, about the *real-face* corpus) was inherited into an adjacent question (the *metric-fallback* faces) without re-deriving the dist chain — the same citation-inheritance failure class the ColorSpaceSelector DISSENT §1 and fr-AnimationControls K-15 record. The reader's OTHER kills in the same return (K-1, K-12, K-13, K-14) were all first-rate and survived this seat's attack; the failure is selective inheritance, not general sloppiness.
2. **R2-LC's "not banked" cell on SS-C-7 (K-11)**: the reader checked R-2 and stopped one row short of C-2 in the same banked file it was quoting elsewhere. Registry sweeps must sweep the whole record, not the row that shares a name with the mechanism.
3. **Severity residue on D-03**: R1-DU holds MAJOR on the ground that a permanent blur adjacent to a 60fps canvas is a standing hazard whatever today's containment; this seat ruled MINOR on the producer's own verified mitigation. If a future glass-ui drops `contain: paint` from the dock shell, R1-DU's grading revives — the BANK trigger is a `grep "contain: paint"` on the installed dock/shell.css at any F.W1 uplift.
4. **Both readers' honest unproven ledgers are adopted wholesale** — including R2-LC's refusal to re-run `vue-tsc` (a `-b` build writes `tsconfig.tsbuildinfo`; not write-free) with the type fact confirmed from the d.ts instead. That refusal is the read-only law working as intended.

## Closing verdict

**ADJUDICATED — 0 BLOCKERs survive; 8 MAJOR (5 → F.W4, 2 → F.W5-W8, 1 → F.W1) · 10 MINOR · 9 INFO booked; 6 identities folded to banked rows; 15 kills ratified or minted, including one reader-kill killed and one reader identity-cell killed by this seat. The component is a disciplined 27-line script wearing a 22-line stylesheet that re-implements — by hand, by value, and now against a producer ruling that no longer ships the props it was imitating — two registers the library would have given it for free. The forming F.W1 spec must treat every `variant`/`size` cure in this corpus as DEAD and re-author against the 8.0.0 single-register trigger; the F.W4 edit is one file, mostly deletions; the type-level cure (a speed catalog) makes the worst banked state unrepresentable. Nothing here stamps a wave (M-25).**

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3.
