# shell-dock-actiontoolbar — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **HEAD**: `027ca138` · branch `tranche-u` · **Corpus**: 20 accusations (C-1..C-7 · D-1..D-8 · L-1..L-6, minus the M-32-struck cell) against `demo/shell/dock/ActionToolbar.vue`, lines 1–92, SHA-256 `bb73802a…3df8` — re-hashed by this seat at HEAD, byte-identical, 92 lines. All three challenge files are REPORT-AUTHORED, evidence count 0 (`registry/HYDRATION-LEDGER.md:292-294`).
**Re-readers adjudicated**: design-and-usage (R1) · library-and-code (R2). Both were attacked before either was adopted; every verdict-level disagreement (AT-1 and AT-2 severity, the dock-hold leak's proven/unproven split, R2's extract-limb of C-6, the crossfade mechanism) was re-derived by this seat against the tree with its own instruments.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling per the commission)**: full reads of ActionToolbar.vue (92), ActionButton.vue (137), ActionBarLayer.vue (158), GenericActionBar.vue (33); ColorPicker.vue:240-340; Dock.vue:40-170; viewSchema.ts:110-155; usePaneRouter.ts:30-50, :185-230; App.vue:30-45, :77-131, :315-336; useViewManager.ts:68-80; useColorPersistence.ts greps; installed glass-ui 7.0.0 dist: `popover-BQGYXZyO.js` (whole wrapper setup), `useClipboard-D36OTaeT.js` (whole) + `useClipboard.d.ts`, `dock.js` keepOpen/release bytes (`function I(){g.value++…}` / `L(){g.value=Math.max(0,g.value-1)…}`), `DockControl.vue.d.ts` doc-block, `density.css`/`overflow.css`/`controls/touch-floor.css` coarse-floor rules, `layers.css` full `.is-leaving` rule extraction; reka-ui dist: `Primitive/Slot.js` (`mergeProps(attrs, childProps)` — attrs first), `Primitive/Primitive.js`, `HoverCard/HoverCardTrigger.js:25-60`, `HoverCard/HoverCardRoot.js:35-60` (`useVModel` `passive: props.open === void 0`); tailwindcss dist (`-rotate` functional emits the standalone `rotate:` property); `@lucide/vue` cjs (`"stroke-width": 2` default); repo greps: `dock-layer-grid` (1 hit = the usage), `action-label` (1 hit = the template), `clearHover` (definition + expose only; `git log -S 'clearHover()'` → `79e24a36` deleted both callers), `--ring:`/`--color-ring:` (zero definitions anywhere in served CSS), `colorPickerRef` assignments (exactly one, desktop-only); focus-ring.css:1-40; motion-quarantine.md M-32 + adoption bar; X wave files W4/W5/W7/W8 for routing.

## SCOPE PIN (new, this seat's own derivation — neither reader made it)

`ActionToolbar` renders **only in desktop layout** (≥1024px) and only when the left pane is `color-picker` (views `picker` / `palettes` / `mix`). Proof: `App.vue:38` `:action-bar="colorPickerRef?.actionBarContext ?? null"`; `colorPickerRef` is assigned at exactly one site, `App.vue:325` inside `onDesktopLeftMount`, and the mobile PaneSlot (`App.vue:83-86`) passes **no** `:on-mount` — so on phone layouts `actionBar` is always null, `ActionBarLayer` never mounts, and the five color commands (Reset/Copy/Random/Palettes/Extract) do not exist on phones at all (the generic bar covers only generate/gradient/mix, `usePaneRouter.ts:188-227`). Consequences: (a) every "coarse pointer" defect below bites the **desktop-layout touch class** (iPad-landscape ≥1024, coarse-primary touchscreens) — not phones; (b) whether the five commands' phone absence is intended is an owner question, recorded in the residue, not a defect row here.

## Scoreboard

**21 adjudicated defect rows: 5 MAJOR · 9 MINOR · 7 INFO** — 10 corpus-claim rows confirmed (several rescoped/consolidated), 11 reader-found rows admitted after re-derivation. **10 killed claims** in the register below. Blocker-weight cluster: every MAJOR is a **consumer** defect whose cure ships in installed glass-ui 7.0.0 today (`DockControl`, `Popover keepDockOpen`, `useClipboard`, the coarse density floor) — the corpus's three axes each prescribed building what the dependency tree already contains.

## Ruled disagreements (the seat's actual work)

| dispute | R1 | R2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| AT-1 severity (clipboard) | MAJOR | MINOR | **MAJOR — R1** | It is a false-SUCCESS signal, not a missing register: ActionButton.vue:94 sets `isClicked` unconditionally → `:28` `action-flash` → the accent pulse paints for `{ok:false, reason:"no-api"/"clipboard-api"}` identically to `{ok:true}`. The producer typed `CopyResult` as a discriminated union expressly so it could not be discarded (`useClipboard.d.ts:31-36` "rather than a lossy boolean") and ships the full status register. Truthfulness defects at a primary command's boundary rank MAJOR regardless of failure frequency. |
| AT-2 severity (touch) | MAJOR | MINOR | **MAJOR — R1**, mechanism sharpened by this seat | On coarse-primary devices the tap's two `activeHover` writes (hoverKey, then null at ActionButton.vue:93) land in the **same synchronous task**, before Vue's flush — the reka root's `open` prop never transitions, so the popover never paints even for a frame (this also settles R2's "one-frame flash" residue statically: no flash is possible). On fine-primary touch (touch laptops) reka's `excludeTouch` guard blocks pointerenter-open and HoverCardTrigger has no click toggle — same outcome by the other branch. Net on ALL touch: zero descriptions ever, and every exploratory tap **executes the command**, including destructive Reset. Zero discoverability plus punished exploration on the exact device class the component serves (see SCOPE PIN) is MAJOR. |
| Dock-hold leak: proven vs UNPROVEN-NEEDS-LIVE | structural half proved; end-state live-gated | CONFIRMED static, per-tap monotonic | **CONFIRMED-STATIC — R2**, re-derived independently | Glass `Popover` emits `update:open` only on reka-initiated changes (`m(e){ n.open === void 0 && (s.value = e), o("update:open", e) }`); a **prop-driven** close only mirrors (`h(() => n.open, …)` — no emit). Coarse tap: Slot merges attrs-first → `[onOpenToggle, handleClick]`; `onOpenToggle` toggles a fully-controlled root (glass always binds `open`, so reka's `useVModel` is non-passive) → emits `true` → ActionButton.vue:84 `dock?.keepOpen()` (+1). `handleClick` then nulls the hover; the prop never transitions; reka's controlled computed still reads `open=false`, so the **next** tap toggles false→true and emits true AGAIN. No touch pointerleave exists; nothing ever emits `false`; `release()` (ActionButton.vue:86) is unreachable. `g.value` (dock.js: `I(){g.value++}` / `L(){Math.max(0,…)}`) is monotonic — Dock.vue:86-87's own balanced predicate pairs cannot drain a foreign +1. Second leak, all pointer types: hover → `keepOpen` → click Extract → `switchView("extract")` → viewSchema `extract.left="extract"` → colorPickerRef nulls → `<ActionBarLayer v-if>` (Dock.vue:156) unmounts the holder before pointerleave/blur can balance; ActionButton has no `onUnmounted`/`onScopeDispose`. The producer's own `keepDockOpen` prop does exactly this with an already-held flag + `onScopeDispose(S)` teardown (read in dist) — ActionButton passes every other Popover prop but not that one. Only the **symptom demo** stays live-gated. |
| R2's C-6 extract limb ("the Palette icon lights while the user is on the Extract surface") | refuted (desktop unmount) | asserted | **KILLED — R1**, extended by the SCOPE PIN | At view `extract`, `left: "extract"` (viewSchema.ts:133-135) → `onDesktopLeftMount` nulls `colorPickerRef` (App.vue:325) → the toolbar unmounts on desktop; and on mobile it never mounts at all (SCOPE PIN). The limb is dead in every layout. The **mix** limb (R1's) survives and carries AT-3: `mix.left = "color-picker"` (viewSchema.ts:142-151) keeps the toolbar mounted while `paletteActive = currentView !== "picker" || isEditing` (ColorPicker.vue:313) lights the Palette glyph beside an open Mix pane. |
| Crossfade mechanism (AT-12) | "instant cut; the 260ms timer is decorative" | not raised | **R1 CORRECT — this seat's own counter-hypothesis RETRACTED** | This seat initially derived a 260ms opaque superposition from a partial grep of `layers.css` (only the `transition` and `:not(.is-active)` rules). Full rule extraction found `.dock-layer.is-leaving { opacity: 0; visibility: visible; pointer-events: none; }` — the leaving face cuts to invisible in one frame (no opacity transition; the `[data-morphing]` ramp needs a morph in flight, and `is-press-keepalive` is not applied here). R1's cell and X-W8 G-8's authored cell ("the 260 ms timer times a window in which nothing animates") both stand. The wrong-anchor limb (`:not(.is-active)` → `absolute; inset: 0` against an unpositioned `dock-layer-grid`) is real but **inert** — the mispositioned face is invisible. Method lesson preserved in DISSENT. |

## Adjudicated defect roster

Line anchors are HEAD `027ca138`; re-resolve before executing any cure (the D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

### MAJOR

- **AT-1 — MAJOR — C-5/D-5 (result limb): the clipboard result is discarded and the button paints false success.** ColorPicker.vue:323-326 `copy: () => { updateModel(…); void writeClipboard(formattedCurrentColor.value); }` discards the producer's discriminated `CopyResult` (`useClipboard-D36OTaeT.js:3-14` always resolves `{ok}` / `{ok:false, reason}`); ActionButton.vue:92-98 flashes the accent success pulse unconditionally. The shipped cure (`useClipboard()` — status idle|pending|success|failure, `resetMs`, `onCopyError`, stale-call guard, scope teardown) sits unused in the same module. → **X-W4** (the `SceneActionSet` contract collapse is where copy's result register lands; ColorPicker.vue and ActionToolbar.vue are in the unit's file list).
- **AT-2 — MAJOR — D-2 (outcome confirmed, stated cause replaced): descriptions are unreachable on ALL touch, and exploratory taps execute commands.** Mechanism per the ruling above; five bare glyphs (ActionToolbar.vue:4,16,27,38,51) explained only via `aria-label`. The producer's pointer-adaptive Popover did the right thing; ActionButton.vue:93 cancels it. → **X-W4** (semantic controls; the DockControl + contract render path replaces the bespoke wiring; description copy itself is X-W7's register sweep — `W7.md:525` already counts ActionToolbar's 5 strings).
- **AT-3 — MAJOR — D-4 + C-6 + both readers' active-semantics rows, consolidated (ONE identity): the "active" seat is wrong three ways.** (i) Wrong predicate: `paletteActive` means "not on the picker view" — at `/mix` the Palette glyph lights while the open pane is Mix; and `isEditing ⟹ paletteActive` while the SAME button carries `:disabled="isEditing"` (ActionToolbar.vue:44,46) — accent-stroked AND 50%-dimmed on every edit, by construction. (ii) Wrong channel: `strokeWidth: '2'` is a no-op against Lucide's default `stroke-width: 2` (verified in dist), leaving hue as the sole signal (WCAG 1.4.1) — and the `action-pulse` keyframe's `100% { stroke: currentColor }` overrides the inline accent for 400ms on every click of the active button. (iii) No programmatic exposure: no `aria-pressed`/`data-active` on the plain `<button>`, while the sibling `DockControl` in the same row documents exactly that contract (`DockControl.vue.d.ts:19-22`). R2's extract limb killed (see ruling). → **X-W4** (aria-pressed is explicit in the wave text at `W4.md:308,:329`; `DockControl active` is the shipped cure; the predicate fix rides the contract collapse).
- **AT-4 — MAJOR — D-7 hardened (both readers, same identity): five hard-coded 32px hit cells inside a dock whose producer floor is 44px on coarse pointers.** `.action-button-wrapper { width: 2rem; height: 2rem; flex-shrink: 0 }` (ActionButton.vue:105-116) — raw literals, no token, consuming neither `--dock-control-size` nor the coarse `--dock-control-floor: var(--dock-touch-target, 2.75rem)` (overflow.css `@media (pointer: coarse)`, verified). Siblings Back (Dock.vue:154) and the mode toggle (ActionBarLayer.vue:129) clamp to 44; the five commands stay 32 with 24px glyphs. Note 32px **passes** WCAG 2.5.8 AA-minimum (24px) — the violation is the producer size-axis contract and in-row inconsistency, which is exactly X-W4's hard-gate second limb ("rides the producer size axis"). CAUTION for the wave: gate A1's fine-pointer 24×24 floor alone would go GREEN over this defect — the coarse-floor arm must be asserted separately (addendum candidate). → **X-W4**.
- **AT-11 — MAJOR — reader-found (both; proven/unproven split RULED CONFIRMED-STATIC above): unbalanced ref-counted dock hold; `keepDockOpen` bypassed.** Coarse-primary taps leak +1 each, monotonically, on all five buttons; the hover→Extract unmount leaks on any pointer. Symptom window: coarse pointer AND ≥1024px (`:always-expanded="!isDesktop"`, Dock.vue:132, masks nothing on phones because the component never mounts there — SCOPE PIN). Cure = pass the producer's `keepDockOpen` (or delete the hand-rolled hold with the DockControl/contract adoption). → **X-W4** (Dock.vue/ActionBarLayer in scope). **No X gate asserts hold balance today** — proposed born-RED gate for the next formation boundary: a smoke spec driving tap→tap→read the dock's held state (`keepOpenCount > 0` with zero open surfaces = RED).

### MINOR

- **AT-5 — MINOR — D-3/C-4 mechanism (atomicity limb killed, K-1): the two disabled commands are exactly the two whose "why" is unreachable.** Native `:disabled` (ActionButton.vue:18) suppresses all pointer/focus events → the hover card explaining Palettes/Extract can never open while disabled. Random-mid-edit does flow into `commitEdit`'s read (ColorPicker.vue:293,:327) but is revertible via the transactional edit (K-1) — the surviving defect is invisible rationale, not data loss. `DockControl`'s four-state `disabled` contract is the shipped cure. → **X-W4** ("an explicit `unavailable` state the Dock renders, never an optional call" — same family, `W4.md:280-282`).
- **AT-6 — MINOR — C-1/D-6/L-6 (unanimous): `canProposeName` is a dead prop.** Declared ActionToolbar.vue:72, referenced nowhere else in the file; the parent pays for it (ActionBarLayer.vue:106) while consuming the real value itself (:37,:49,:131). → **X-W8 G-10** (explicitly booked WITH the parent binding, `W8.md:325-326`).
- **AT-7 — MINOR — C-7/L-5 + R2's dead-refs row (one identity): dead imperative surface and two dead template refs.** `clearHover` + `defineExpose` (ActionToolbar.vue:87-91) — `git log -S` receipt `79e24a36` deleted both callers; R2's correction adopted: pure dead code, not a regression (ActionButton.vue:93 already performs it). `actionToolbarRef` AND `colorInputRef` (ActionBarLayer.vue:28-29, bound :103/:116) never read; idiom drift beside `useTemplateRef` at :83. → **X-W8 G-10** (all sites explicitly booked, `W8.md:327-329`).
- **AT-8 — MINOR (upgraded from R1's INFO by R2's decisive fact) — L-1/C-3 duplication limb: the prescribed registry ALREADY EXISTS one directory away.** `DockAction` (usePaneRouter.ts:38-47) + `GenericActionBar.vue` render the byte-identical root (`:15` == ActionToolbar.vue:2), the identical hover ref, and the identical update wiring — ActionToolbar is five hand-written rows of that pattern, missing only `active-style`/active semantics from the type. The drift is visible in the bytes (only :9 carries `icon-class`; :21/:32/:43/:56 are the holes where the others' were deleted). Unique-keys limb stays killed (K-6). → **X-W4** (the `SceneActionSet` collapse of `ActionBarContext` + `DockActionBar` is the unit's stated mechanism).
- **AT-12 — MINOR (demoted from R1's MAJOR — the cut is ugly but honest-adjacent; already wave-owned with a motion arm) — reader-found (R1): the documented 260ms sub-layer crossfade does not fade.** `dock-layer-grid` (ActionBarLayer.vue:101) matches zero rules in repo or dist (this seat's grep); `.dock-layer.is-leaving { opacity: 0 }` lands in one frame; the `SUB_LAYER_CROSSFADE_MS = 260` timer (:62) times a window in which nothing animates; the real producer crossfade lives in `DockCrossfade`, which the :54-61 comment declined. → **X-W8 G-8** (explicitly booked: shim retired against `<DockCrossfade>`, rendered-motion arm included; this adjudication confirms its RED cell).
- **AT-13 — MINOR — reader-found (R2): the 400ms click-feedback timer is unmanaged.** Bare `setTimeout` (ActionButton.vue:95-97), no handle, no clear, no unmount guard: a second click inside 400ms restarts nothing (class never removed) and is truncated by the first timer; unmount mid-animation leaves a live timer writing a disposed ref. Dies with the DockControl interruptible spring press. → **X-W4** (rider on the control adoption).
- **AT-14 — MINOR — reader-found (R1): the Reset hover spin snaps.** Tailwind v4's `-rotate-180` emits the standalone `rotate:` property (verified in tailwindcss dist: `l("rotate", …)`), absent from `transition-[transform,stroke]` (ActionButton.vue:25) — the 180° turn jumps both directions; `duration-normal` retunes only scale/stroke. → **X-W4** (rider — the bespoke hover CSS dies with the adoption).
- **AT-15 — MINOR — reader-found (R1): no toolbar semantics.** Bare `<div>` root (ActionToolbar.vue:2): no `role="toolbar"`, no group name, five tab stops mid-dock; the APG single-stop/roving pattern absent. L attributed this to a missing producer primitive (killed, K-3) — it is two attributes and a keydown handler. → **X-W4** (semantic-controls remit; addendum candidate: a G2-family arm for the toolbar group).
- **AT-17 — MINOR — reader-found (R2): ActionButton's dead API — `label`, `hidden`, and an unpainted class.** `label` (ActionButton.vue:65, rendered :32) and `hidden` (:69, gating :2) are passed by neither consumer and unreachable via `DockAction` (no such fields); `.action-label` has zero CSS rules anywhere (this seat's grep); prop `hidden` shadows the native HTML attribute (a consumer's `hidden` would be swallowed). → **X-W8 G-10 extension** — G-10's five-site census is short by these rows; addendum candidate for the next formation boundary (E-3: appended, never rewriting the wave file).

### INFO

- **AT-9 — INFO — C-2/L-4 typing limb: `activeHover`/`hoverKey` are `string`, not a command-ID union.** A typo in any of the five `hover-key` literals would type-check and kill that popover silently. R2's caveat adopted: `GenericActionBar` feeds `DockAction.key: string`, so the union must be generic, not a literal enum. → **X-W4** (the "token union, not `string`" clause is verbatim in the unit mechanism).
- **AT-10 — INFO — L-2: glyphs, emits, and labels are three parallel lists with nothing tying them.** Confirmed literally; the one place the correspondence has actually drifted is AT-16. Folded into AT-8/AT-9's cure. → **X-W4**.
- **AT-16 — INFO — reader-found (R1): `hover-key="browse"` names a live sibling ViewId while the button routes to `palettes`.** viewSchema.ts:124-127 defines a real `browse` view; Dock.vue:55 routes the emit to `palettes`. Load-bearing for popover exclusion, so nothing breaks — a false name pointing at a live concept. → **X-W4** (the token union makes it unspellable).
- **AT-18 — INFO (demoted from R2's MINOR — the seat finds no live `pointer-events: none` ancestor post-T-31, so the dead binding has no behavioral consequence) — `class="pointer-events-auto"` on the `<Popover>` root reaches no element.** Glass `Popover` is `inheritAttrs: false` and renders reka roots (which emit no DOM node) with an explicit prop list and no attrs spread (verified in dist). The identical string on `PopoverContent` (:35) IS live. → **X-W8 G-10 extension** (dead binding).
- **AT-19 — INFO — reader-found (R1): `focus-visible:ring-ring/40` reaches a token the repo documents as empty.** Zero `--ring`/`--color-ring` definitions in the served cascade (this seat's grep); focus-ring.css:10-13 records this exact failure (U-F25) and ships the cure (`--focus-ring-inner/outer`) unused here. The ring falls back to `currentcolor` — visible, arguably higher-contrast, but undesigned. **Scope law** (ColorSpaceSelector apotheosis, addendum 1): the count lives repo-wide (~22 `ring-ring/40` sites) → **NO-WAVE-OWNER** — booked for the next formation boundary as a repo-scope focus-ring parity sweep; this component's site rides whichever wave touches the file first.
- **AT-20 — INFO — reader-found (R1): `copy` is a read verb with a hidden persisted write.** ColorPicker.vue:324 `updateModel({ inputColor })` before the clipboard write; `inputColor` persists across reload (useColorPersistence.ts:27,:96). Defensible ("show what was copied") but undeclared: the emit is typed `copy: []` and the description promises a read. → **X-W4** (declare it in the contract, or split it).
- **AT-21 — INFO — reader-found (R1): four blank-line fossils** (ActionToolbar.vue:21,:32,:43,:56) — the visible residue of the drift C-3 predicts. → **X-W4** (dies with any touch of the file).

## Killed-claims register

- **K-1 — C-4 atomicity limb ("no atomic edit policy is encoded") — REFUTED, both readers, verified by this seat.** ColorPicker.vue:272 `preEditModel` snapshot (with the :283-284 comment justifying reference-holding against `updateModel`'s wholesale-replace), :291-301 `commitEdit`, :303-309 `cancelEdit`, Escape/Enter bound :250-262. The policy the axis demands be built is already encoded and reachable. Surviving mechanism carried at AT-5.
- **K-2 — C-5 ordering limb ("double activation and out-of-order completion… pending locks, typed receipts") — REFUTED as over-prescription.** Three commands are synchronous last-write-wins over one ref; the two navigations are idempotent at the router (useViewManager.ts:73 `if (id === currentView.value) return` — this seat's read); the sole async path always resolves a caught union (dist read) — no rejection, no ordering-visible state. Under the KISS/no-contrivance law the prescribed machinery prevents no failure. The real async gap is the DISCARDED result — carried at AT-1.
- **K-3 — L-3 ("toolbar, tooltip, icon button, and command-result behavior remain outside the producer library") — FALSE as framed.** The producer ships all four (`DockControl` + its ≥44px/`aria-pressed`/four-state-disabled doc-block; `Popover`; `useClipboard`; dock overflow), and the SAME parent file already consumes `DockControl` (ActionBarLayer.vue:129). The defect is non-adoption in one demo component; the prescribed `DockCommand[]` producer primitive would found a second (fourth) dock-control lineage. Per the standing law: **the cure is adjudicated separately from the defect** — L's cure is refuted while L's underlying facts survive at AT-3/AT-4/AT-8/AT-15.
- **K-4 — L-4 framing ("shared mutable string state… hover state should remain internal") — REFUTED; it inverts the component's best design.** The parent owns the single ref; children derive visibility (`isOpen = computed(() => activeHover === hoverKey)`) and can only request changes through a declared event. That IS the single-open invariant, correct by construction. Making hover "internal" would delete it. Typing limb survives at AT-9.
- **K-5 — D-2 stated mechanism ("touch users cannot depend on hover explanations" as cause) — REPLACED.** The producer degrades by pointer type (dist: `trigger === "hover" && !coarse`); the failure is the consumer's same-tick cancellation. Outcome carried at AT-2 with this seat's no-flash sharpening.
- **K-6 — C-3 unique-keys limb ("no invariant ensures unique hover keys") — not a live defect.** Five distinct literals in one 60-line template; the registry consumer feeds the same value to `:key`, so a duplicate trips Vue's dev warning first. Duplication limb carried at AT-8.
- **K-7 — D-7 label/overflow limb ("no overflow/label mode is specified") — REFUTED as stated.** A label mode is specified (`label` prop + span) and producer overflow is shipped (`.dock-overflow-wrap`); what is missing is threading and paint — carried at AT-17. Target-size limb carried at AT-4.
- **K-8 — D:21 reduced-motion cell — MAY NOT BE CARRIED: quarantined FALSE as M-32** (motion-quarantine.md:123; adoption bar :266 "the reduced-motion cell is struck"). Both readers complied; this seat re-read the row. No PRM defect is raised against `action-pulse`/`action-spin`.
- **K-9 — R1's own WHCM-focus hypothesis — self-killed pre-report, recorded so it is not re-raised.** Tailwind utilities live in `@layer utilities`; the forced-colors block in foundation.css is unlayered; unlayered wins — the WHCM outline survives `focus-visible:outline-none`.
- **K-10 — R2's C-6 extract limb ("the Palette icon lights while the user is on the Extract surface") — KILLED by this seat.** Dead on desktop (unmount via viewSchema `extract.left` + App.vue:325) and dead on mobile (never mounts — SCOPE PIN). The mix limb survives inside AT-3.

**Not carried (taste, not defect)**: D-1's flat-hierarchy row — both readers concur it is a design judgement (a partial hierarchy already exists in the bytes: Reset alone spins, Palettes alone has an active style, the two navigations alone disable); the grouping prescription goes to the owner, not a wave.

## Superlatives — provenance verified by this seat

1. **Single-open by construction** (both readers; R2's articulation adopted): `isOpen = computed(() => activeHover === hoverKey)` — one parent-owned string, children derive-never-write; mutual exclusion as a type-level consequence. Two axes read this as the defect; it is the best thing in the file.
2. **The W5-a11y native button, pinned by CI**: ActionButton.vue:12-19 — real `<button type="button">`, `:aria-label="title"`, focus-visible ring, `aria-hidden` glyph — and o10d-display-voice-census.spec.ts:356 drives it **by accessible name** (`getByRole("button", { name: "Copy color" })`), so the label cannot silently rot.
3. **Provenance-carrying comments**: ActionButton.vue:37-41 (the T.W4-6 · T-15/F7 font-cascade fix: defect, mechanism, wave, cure in four lines) and ActionBarLayer.vue:53-61 (the shim documents what it succeeds, the contract it preserves, and the standing glass relay note). The standard the rest of the tree should be held to — even though AT-12 shows the shim's documented fade does not exist.
4. **The accent is contrast-guarded at the source**: ActionBarLayer.vue:105 feeds `safeAccent` (SAFE_ACCENT_KEY, `useContrastSafeColor`) — hover stroke, click flash, and active stroke all ride one WCAG-guarded token. The obvious critique of a color tool was closed before it could be made.
5. **The producer popover degrades by pointer type** (dist-verified): hover→click swap under `(pointer: coarse)`, plus focus-opens on the trigger. The confirmed touch defect is the consumer undoing a genuinely good primitive.
6. **The edit is transactional**: ColorPicker.vue:272-309 — snapshot, commit, cancel, Escape/Enter, with the comment justifying the snapshot's soundness. The single strongest refutation in the corpus (K-1).
7. **Colocation and single-ownership pinned by test**: test/picker-blob-config.test.ts:13-17 records WHY the file lives in `demo/shell/dock/`; :50-53 asserts exactly one `writeClipboard(` in the picker and one `:icon="Copy"`/`emit('copy')` here — a structural invariant enforced by assertion, not prose.
8. **The component is honestly thin, and every correct cure makes it thinner**: 92 lines, five pure emits, one hover ref. All three axes prescribed growth (registries, receipts, locks); the adjudicated direction is deletion + adoption — consistent with the tranche's own X-W4/X-W8 specs, authored independently of this corpus.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **Served control geometry at coarse ≥1024px** (AT-4): `getComputedStyle` on `.action-button-wrapper` vs a sibling `DockControl` under iPad-class emulation — settles the exact 32-vs-44 delta (root font-size and `--ui-scale` dependent). Source facts proved.
2. **Dock-hold symptom demo** (AT-11): tap a toolbar button twice under coarse emulation, read the dock's held state / auto-collapse; and hover→Extract on fine pointer, same read. The **verdict** is static and does not depend on this — the demo is for the wave's witness pair. (R2's "one-frame popover flash" sub-question is SETTLED STATICALLY: no flash — the prop never transitions.)
3. **Focus-ring fallback contrast** (AT-19): whether the `currentcolor` 2px ring clears 3:1 over `--glass-bg-floating` in both schemes (WCAG 2.4.11/1.4.11).
4. **The actions↔input cut** (AT-12): one frame-capture pair confirming no mid-opacity frame — corroborates X-W8 G-8's RED cell; the perceptual severity call (MINOR here) is revisable on that capture.
5. **Owner question, not a probe**: is the five-command toolbar's total absence on phone layouts (SCOPE PIN) intended mobile grammar or an unnoticed regression? No static evidence decides intent; neither reader nor challenger noticed the fact.
6. **Coverage note** (R2, adopted): no test or e2e exercises this component's behavior — the only references are source-text assertions and one comment. None of the 21 rows would be caught by today's suite.

## DISSENT / method notes — preserved, not ruled away

1. **This seat's retracted counter-hypothesis** (recorded per the worker-self-error precedent): a partial grep of `layers.css` (matching only `transition` and `:not(.is-active)` fragments) missed `.dock-layer.is-leaving { opacity: 0 }` and nearly minted a false "260ms opaque superposition" correction against R1 and against X-W8 G-8's authored cell. Full-rule extraction settled it. Lesson: **a CSS mechanism claim is not derived until every rule touching the class has been read** — partial greps produce confident wrong cascades.
2. **R1's severity instincts held where R2's were conservative** (AT-1, AT-2 both ruled MAJOR); **R2's mechanism work held where R1 was cautious** (the leak ruled CONFIRMED-STATIC on R2's derivation, independently re-walked). Neither reader dominated — the split is exactly what the two-seat design is for.
3. **Both readers independently found the same two headline misses** (the dock-hold leak; the touch-target floor) with compatible evidence and no shared phrasing — treated as independent corroboration, not citation-inheritance. The readers' disjoint miss-sets (R1: crossfade, rotate-snap, role=toolbar, ring token, hidden write; R2: click timer, dead attrs, dead refs, registry-exists) are individually verified above.
4. **The corpus's failure mode, named**: all three REPORT-AUTHORED axes were factually clean at the line level (zero fabrications across 20 accusations — rare and creditable) but failed at INFERENCE: stopping at the file boundary, prescribing producer primitives the installed producer already ships, and reading the component's best invariant as its defect. The adjudication's structural lesson matches the standing scope law: **an accusation's cure must be re-derived against the installed dependency tree before it is allowed to shape a wave.**

## Closing verdict

**ADJUDICATED RED — confirmed in the corpus's direction, inverted in its prescription.** 21 rows survive (5 MAJOR · 9 MINOR · 7 INFO): 10 corpus claims confirmed (several rescoped), 11 reader-found admissions, 10 claims killed with written refutations. Every MAJOR is a consumer-side defect whose cure ships in installed glass-ui 7.0.0 today; the terminal dispositions route **15 rows to X-W4** (three of them riders — AT-13/AT-14/AT-21 — that die with the control adoption), **5 rows to X-W8** (G-8 and G-10 explicitly booked for three; AT-17/AT-18 as G-10 census-extension addenda), and **1 repo-scope row to NO-WAVE-OWNER** (AT-19, the focus-ring parity sweep, booked at the scope where its count lives). The component's correct end-state is **smaller**: delete the dead surface, hand the five seats to `DockControl`, thread the producer's clipboard register and `keepDockOpen`, and let the typed contract make the misnomer and the mis-derived active seat unspellable.

— end of apotheosis. The challenger record and both re-reader returns remain immutable beside this file per E-1/E-3.
