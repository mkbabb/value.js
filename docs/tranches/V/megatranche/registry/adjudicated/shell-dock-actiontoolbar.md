# shell-dock-actiontoolbar — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed from this seat's own runtime, not inherited from the spawn label

**Seat**: the fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code), each of which re-read the full challenge corpus against the tree before returning.
**Date**: 2026-08-08 · **HEAD**: `8d7e17fe` (branch `tranche-u`; every line anchor in this record re-resolved at this HEAD by this seat's own reads — ActionToolbar.vue 92 L whole, ActionButton.vue 137 L whole, ActionBarLayer.vue 158 L whole).
**Corpus**: `audit/components/shell-dock-actiontoolbar/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md`. **Provenance, verified by this seat**: NO `challenge-*-r2.md` exists (directory listed — r2 governs where it exists; here the three r1 files govern their own axes); all three are REPORT-AUTHORED with evidence count **0** at `registry/HYDRATION-LEDGER.md:292-294` (rows sighted); `AXIS-PROMOTIONS-2026-08-03.md` carries **zero** actiontoolbar rows; `codex-provenance/motion-quarantine.md` row **M-32** rules the D axis's reduced-motion cell **FALSE** (struck at :266; probe4 measured `animation-duration: 1e-05s` on the real `.action-icon`) and :283 records the C·L axes as motion-free — no PRM defect may be carried here by any consuming wave. The corpus receipt (`SHA-256 bb73802a…3df8`, 92 lines) matches the live file **byte-for-byte at this HEAD** — the readers and this seat adjudicated the same bytes.
**Method (L-18)**: both re-reader returns were attacked before either was adopted. Every kill was re-derived against the tree or the installed producer dist by this seat's own instruments; every load-bearing confirmation was spot-verified at source; the one genuine mechanism dispute between the readers (whether the coarse-pointer dock-hold leak is statically decidable or needs a live run) was **settled by this seat's own dist derivation** — reader-2's static verdict wins; three severity settlements were ruled, one reader-1 MAJOR was demoted on this seat's own neutralizing evidence, and one reader-2 confirmed limb was killed on the other reader's mount analysis. No browser was opened (probe parsimony): every ruling is decidable from source, installed producer bytes, and one local Tailwind compile; pixel magnitudes that are not are named UNPROVEN-NEEDS-LIVE and become visual-audit inputs.

**Arbiter's own instruments this session (all static, each re-runnable)**: whole-file reads of ActionToolbar.vue / ActionButton.vue / ActionBarLayer.vue; scoped reads of ColorPicker.vue:240-334 (the edit transaction + actionBarContext), Dock.vue:40-158, viewSchema.ts:115-154, App.vue:315-330 (`onDesktopLeftMount`), useViewManager.ts:72-78 (`switchView` early return), keys.ts:10-20, GenericActionBar.vue whole, usePaneRouter.ts:38-47 (`DockAction`), focus-ring.css:1-40, test/picker-blob-config.test.ts:10-55, e2e o10d-display-voice-census.spec.ts:353-358; producer bytes: `dist/popover-BQGYXZyO.js:1-80` (the **decisive** read: `keepDockOpen` prop + `x` held-flag + `onScopeDispose(S)`; the coarse-pointer `matchMedia` branch; `m(e)` emits only from reka emissions while the `h(() => n.open, …)` prop-watch syncs **without** emitting), `dist/useClipboard-D36OTaeT.js` whole (CopyResult union; the full `status` register with stale-call guard + `onScopeDispose`), `dist/components/dock/DockControl.vue.d.ts:1-45` (hit-cell ≥44px on coarse; `active` → `aria-pressed`+`data-active`; four-state `disabled`), `dockContext.d.ts:27-30` ("Reactive `keepOpenCount > 0`… `data-held`"), `dock/styles/density.css` + `overflow.css` (`--dock-control-floor: var(--dock-touch-target, 2.75rem)` under `@media (pointer: coarse)`; `--dock-control-size: max(…, var(--dock-control-floor))`), `dock/styles/layers.css` (`.dock-layer { transition: visibility 0s … }` — **no opacity transition**; `.is-leaving { opacity: 0 }` flat); reka-ui: `Primitive/Slot.js:16` (`mergeProps(attrs, firstNonCommentChildren.props)` — attrs FIRST), `Popover/PopoverRoot.js:32-47` (`useVModel(props,"open",emit,{ …, passive: props.open === void 0 })` — **non-passive when `:open` is bound**, so trigger-toggles emit and prop-driven closes never do); `@lucide/vue` `defaultAttributes.mjs:15` (`"stroke-width": 2`) + `Icon.mjs:30,36`; **a live Tailwind v4 compile in this session**: `hover:-rotate-180` emits `rotate: calc(180deg * -1)` under `@media (hover: hover)` — the standalone `rotate` property, absent from `transition-[transform,stroke]`'s list; counting greps: `dock-layer-grid` = **1 hit repo-wide, 0 in glass dist** (the sole usage line); `action-label` = **1 hit** (the template line; no rule anywhere); `clearHover` = 4 hits, 2 an unrelated `clearHoveredPath`, **0 callers**; `--ring:`/`--color-ring:` definitions in demo/ + glass dist styles = **0**; `ring-ring/40` in demo = **22**; `git log -S "clearHover()"` → `79e24a36` deleted the only two call sites; X-wave ownership reads: `X/waves/W4.md` §X.W4.d (SceneActionSet; file list includes ActionToolbar/ActionBarLayer/GenericActionBar/keys.ts/ColorPicker) + X.W4.a CC-040, `W7.md` file list + G19 (OM-15 census: `ActionToolbar.vue` 5 "Click to…" strings), `W8.md` §X.W8.d + G-8/G-10 (which already name canProposeName, clearHover, both dead refs, and the crossfade shim); `grep -rn ActionButton docs/tranches/X/` = **0 hits** — `ActionButton.vue` appears in **no** X wave's file list, the named gap of this record.

## Scoreboard

**20 rows CONFIRMED (0 BLOCKER · 5 MAJOR · 9 MINOR · 6 INFO) · 10 kills in the register (8 sustained from the readers, re-derived; 1 killed reader-limb-against-reader; 1 reader self-kill preserved) · 11 missed-by-the-axes findings adjudicated (readers' union; 2 further mechanism limbs folded into sibling rows) · dispositions routed X-W4.d ×8 · X-W8 ×3 (all three already named by W8's own gates) · 1 copy-register rider on X-W7 G19 · 9 NO-WAVE-OWNER rows (+2 partial limbs), with one named file-list gap: `ActionButton.vue` is in no X wave.**

Blocker-weight survivor: none — but the MAJOR class is coherent: **every one of the five is a producer facility bypassed by a 137-line demo file** (the clipboard result union discarded → false-success paint; the pointer-adaptive popover self-cancelled → touch loses all five explanations; the ref-counted dock hold hand-rolled without the release discipline the producer's own `keepDockOpen` ships; the 44px coarse floor undercut by a hard `2rem`; the `aria-pressed` seat unstamped beside a sibling that stamps it).

## Challenges to the re-readers (the incredulity seat's actual work)

| Dispute | This seat's re-derivation | Ruling |
|---|---|---|
| **Leak decidability** — reader-1 held the dock-hold end-state UNPROVEN-NEEDS-LIVE; reader-2 reported the per-tap leak CONFIRMED statically | Settled by this seat's own reads, all three links: (i) reka `Slot.js:16` merges attrs FIRST → one coarse tap runs `[onOpenToggle, handleClick]`; (ii) reka `PopoverRoot.js:34` `passive: props.open === void 0` — glass always binds `:open`, so the v-model is **non-passive**: the toggle's setter emits `update:open(true)` → glass `m(true)` → `onHoverOpenChange(true)` → `keepOpen()` (+1); (iii) glass's prop-watch (`h(() => n.open, …)`) syncs the close **without emitting**, and non-passive reka reads the prop directly — no `update:open(false)` exists anywhere in the close path, so `release()` is unreachable. Monotonic per tap. | **reader-2 wins — CONFIRMED static** (AT-5); the live `data-held` read demoted to corroboration-only residue. |
| reader-1's MAJOR vs reader-2's MINOR on the clipboard row (C-5/D-5) | It is not a missing register but a **false affirmative**: ActionButton.vue:94 sets `isClicked` unconditionally → the accent `action-pulse` paints success while `writeClipboard` returns `{ok:false, reason:"no-api"}` — a reachable production condition (insecure-context LAN serving is this repo's documented mobile-test mode), and the producer typed the union precisely so the outcome could not be dropped. A primary command lying about success outranks a missing status readout. | **reader-1 wins — MAJOR** (AT-1). |
| reader-1's MAJOR vs reader-2's MINOR on the touch self-cancellation (D-2) | On coarse pointers the dock is `always-expanded` and the five glyphs (Dices, Camera…) are the action surface; the self-cancel leaves an entire input class with zero sighted explanation AND no non-destructive way to learn what Reset does before firing it (the same tap that fails to open the card also fires `@action`). The aria-label reaches AT only. | **reader-1 wins — MAJOR** (AT-2), mechanism replaced per K-7. |
| reader-1's MAJOR on the phantom 260 ms crossfade | The mispositioning limb cannot paint: `layers.css` gives `.is-leaving` flat `opacity: 0` + `pointer-events: none` in the same frame (transition list is `visibility` only), so the absolutely-positioned outgoing face is invisible for its whole 260 ms life. What survives: an instant cut where the comment promises a crossfade, a decorative timer, and a phantom `dock-layer-grid` class (1 hit repo-wide, 0 rules anywhere — this seat's grep). X-W8's G-8 already states the same fact ("the 260 ms timer times a window in which nothing animates") and owns the cure (`<DockCrossfade>`). | **DEMOTED to MINOR** (AT-9), FOLD → X-W8 G-8; the phantom-class fact is this record's addition to that gate. |
| reader-2's C-6 limb "the palette icon lights while the user is on the Extract surface" | reader-1's mount analysis re-verified by this seat: viewSchema `extract: { left: "extract" }` → App.vue `onDesktopLeftMount` nulls `colorPickerRef` when `left !== "color-picker"` → `actionBar` null → `<ActionBarLayer v-if="actionBar">` unmounted at /extract on desktop. The limb is unreachable where charged; the /mix mis-signal (left stays `color-picker`) is the live one. | **Limb KILLED** (K-9); the mobile mount path is unchecked → residue item 6. |
| reader-2's MINOR on the dead `pointer-events-auto` root binding | The class is dead (glass `Popover` is `inheritAttrs: !1`, renders reka roots with explicit prop lists, no attrs spread — this seat's dist read) but it is vestigial, not a trap: the dock has rendered in-flow since T-31 (Dock.vue:121-127 comment; the `pointer-events` punch-through retired), so no `pointer-events: none` ancestor exists for the class to falsely promise an escape from. The `:35` content binding IS live. | **DEMOTED to INFO** (AT-20). |
| reader-1's INFO vs reader-2's MINOR on the five-block duplication (L-1/C-3) | reader-2's evidence is decisive and this seat re-read both files: `DockAction` (usePaneRouter.ts:38-47) + `GenericActionBar.vue` render the byte-identical root (`flex items-center justify-around flex-1`), the identical `activeHover` ref, the identical update closure — the prescribed registry exists one directory away and only `active-style` is missing from its shape. A duplication whose cure is adoption-not-authoring, with drift already visible in the bytes (one `icon-class`, four blank-line gaps), is a defect, not an observation. | **PROMOTED to MINOR** (AT-14), BUILD → X-W4.d. |

## Adjudicated defect roster

Line anchors are HEAD `8d7e17fe`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Routing law: rows land on the X wave that owns the surface (X.W4.d's file list carries ActionToolbar.vue, ActionBarLayer.vue, GenericActionBar.vue, keys.ts, ColorPicker.vue; X.W8.d G-8/G-10 already name this component's dead surface and the crossfade shim; X-W7 G19 owns the "Click to…" copy census). **`ActionButton.vue` appears in no X wave's file list** — every row whose cure lives there is NO-WAVE-OWNER with the gap flagged, owed to the next formation boundary.

### MAJOR

- **AT-1 (C:C-5 / D:D-5 core — the false-success clipboard) — the copy command discards a typed failure and paints the success pulse anyway.** ColorPicker.vue:323-326 `copy: () => { updateModel(…); void writeClipboard(formattedCurrentColor.value); }` drops a `CopyResult = {ok:true} | {ok:false, reason}` the producer returns precisely so it cannot be dropped (dist read: `no-api` on missing/insecure clipboard, `clipboard-api` on rejection, `console.warn` only); ActionButton.vue:94-98 sets `isClicked` unconditionally → :121-131 `action-pulse` paints the accent flash on failure. The full cure is shipped and unused: `useClipboard()` with `status idle|pending|success|failure`, `resetMs`, `onCopyError`, stale-call guard, scope-dispose teardown. Emits are all-void (ActionToolbar.vue:77-83). **Disposition: BUILD → X-W4.d** (the SceneActionSet's "no action can silently do nothing" is this row's law; ColorPicker.vue + keys.ts in its file list); the paint half lives in ActionButton.vue — **named gap**.
- **AT-2 (D:D-2, mechanism replaced) — one tap both fires the action and closes the explanation it just opened; touch users get five bare glyphs.** The producer did the right thing (coarse pointers get a click-to-open reka `PopoverRoot`, not HoverCard — dist `matchMedia("(pointer: coarse)")` branch); the consumer cancels it: reka Slot merges attrs-first, so a tap runs reka's `onOpenToggle` (→ `activeHover = hoverKey`) then ActionButton.vue:93 `emit("update:activeHover", null)` in the same tick — the card never paints, and `@action` fires regardless. RotateCcw/Copy/Dices/Palette/Camera are unexplained to sighted touch users; only AT hears `:aria-label="title"`. The description register is separately wrong for touch ("Click to…" ×5 — the OM-15 census already counts these, W7.md G19). **Disposition: NO-WAVE-OWNER** (the self-cancel lives in ActionButton.vue — named gap); the copy rewrite rides **X-W7 G19**; any cure must co-resolve AT-5 (same tap chain).
- **AT-3 (D:D-4 + C:C-6 folded + both readers' missed limbs — the Palette "active" state lies through one weak channel).** (a) ColorPicker.vue:313 `paletteActive = currentView !== "picker" || isEditing` means "not on the picker view": at /mix (viewSchema `mix: { left: "color-picker", right: "mix" }` keeps the toolbar mounted) the Palette glyph paints its active stroke while the open surface is Mix. (b) The `|| isEditing` term makes lit-and-disabled **entailed**, not possible: every edit session paints ActionToolbar.vue:46's accent stroke and :44's `disabled` (→ `opacity-50`) on the same button. (c) The only signal is hue: `strokeWidth: '2'` is a no-op against Lucide's default `stroke-width: 2` (defaultAttributes read) — WCAG 1.4.1. (d) Tapping the active button drops its accent for the 400 ms pulse (`action-pulse` 100% frame `stroke: currentColor` outranks the inline `activeStyle` while the class lives). (e) No `aria-pressed`/`aria-expanded` — while the sibling control in the same parent file (`ActionBarLayer.vue:129 <DockControl>`) documents `active` → `aria-pressed` + `data-active`. **Disposition: predicate + active-state contract BUILD → X-W4.d**; the AT-semantic + paint limbs live in ActionButton.vue — **named gap** (DockControl adoption is the shipped cure).
- **AT-4 (D:D-7's target-size limb, hardened by both readers) — the five primary commands are the only dock controls that undercut the producer's coarse-pointer floor.** ActionButton.vue:105-116 hard-codes a `2rem` (32px) hit cell (`flex-shrink: 0`, no token); glass clamps every sibling to `max(…, var(--dock-control-floor))` with `--dock-control-floor: var(--dock-touch-target, 2.75rem)` under `@media (pointer: coarse)` (density.css + overflow.css, read by this seat), and DockControl's d.ts states "hit cell stays the full `--dock-control-size` (≥44px on coarse)". So Back and the mode toggle sit at 44px beside five 32px targets in one row. WCAG 2.5.5; the in-dock inconsistency is the sharper charge. **Disposition: NO-WAVE-OWNER** (ActionButton.vue gap). Coordinate X.W4.a CC-040 — but note MT-F004's census is **sub-24px** targets; this 32px row is NOT in it, so CC-040 must not be assumed to cover it.
- **AT-5 (missed by all three axes; both readers' union, static verdict ruled above) — the hand-rolled dock hold leaks: +1 per coarse-pointer tap, +1 on unmount-while-open, no release path exists for either.** ActionButton.vue:81-88 calls `dock?.keepOpen()`/`dock?.release()` on `@update:open` with no held-flag and no scope dispose; the counter is ref-counted (`keepOpenCount`, dockContext.d.ts:30) and gates every collapse (Dock.vue:86-87 drives the same counter). Tap chain settled statically (see Challenges): the close is prop-driven and emits nothing, so `release()` never runs — monotonic across taps. Bites where `pointer: coarse ∧ min-width: 1024px` (iPad-class; phones are masked by `:always-expanded="!isDesktop"`). Unmount limb: Dock.vue:153/:156 `v-if` the layer away with a card open (e.g. hover Camera 300 ms → click → `switchView("extract")` → toolbar unmounts before any close event). The producer's own `Popover` ships the disciplined version — `keepDockOpen` prop + held-flag + `onScopeDispose(S)` (dist read) — and ActionButton passes every Popover prop except that one. **Disposition: NO-WAVE-OWNER** (ActionButton.vue gap); the cure is deletion of :84-87 in favor of `keep-dock-open`.

### MINOR

- **AT-6 (D:D-3, mechanism named) — the two disabled commands are exactly the two whose "why" is unreachable.** ActionButton.vue:18 sets native `:disabled` on the trigger; a natively-disabled button is unfocusable and dispatches no pointer events, so the hover-card path (reka HoverCardTrigger `onPointerenter`/`onFocus`) never fires — "Save, browse, and publish color palettes." is unreachable exactly while `isEditing` dims it. DockControl's four-state contract ("stays PRESENT but disabled") is the shipped answer. The "why only 2 of 5" rationale limb is design taste (see K-1 — the edit is transactional, so gating Reset/Random is a choice, not an omission). **Disposition: NO-WAVE-OWNER** (ActionButton.vue gap).
- **AT-7 (C:C-1 / D:D-6 / L:L-6) — `canProposeName` declared, never read; the parent still pays for it.** ActionToolbar.vue:72 + ActionBarLayer.vue:106; real consumers are :37/:49/:131. **Disposition: FOLD → X-W8 (G-10 names the prop AND the parent binding verbatim; already owned).**
- **AT-8 (C:C-7 / L:L-5 + reader-2's dead refs) — dead imperative surface, with the removing commit named.** `clearHover` + `defineExpose` (ActionToolbar.vue:87-91), 0 callers since `79e24a36` deleted both call sites (this seat's `git log -S`); reader-2's correction adopted: pure dead code, not a regression — ActionButton.vue:93 already performs what the deleted callers did. Plus both never-read parent refs `actionToolbarRef`/`colorInputRef` (ActionBarLayer.vue:28-29, bound :103/:116), beside a correct `useTemplateRef` at :83 — idiom drift in one file. **Disposition: FOLD → X-W8 (G-10 names all four items; already owned).**
- **AT-9 (missed; reader-1, DEMOTED by this seat) — the documented 260 ms actions↔input crossfade is an instant cut riding a phantom class.** ActionBarLayer.vue:53-94: the local `useLayerTransition` shim holds `leavingLayer` for a "crossfade window" no rule animates — `layers.css` transitions `visibility` only, opacity is flat 0/1, and the container class `dock-layer-grid` matches **zero** rules in repo or dist (this seat's grep; the borrowed `.dock-layer` rules apply by descendant match only). The real crossfade is `DockCrossfade`'s `--dock-t` machinery, deliberately not adopted per the comment. Demotion grounds in Challenges (the mispaint limb cannot render at opacity 0). **Disposition: FOLD → X-W8 (G-8 owns the retirement and already states the timer fact; this record adds the phantom-class evidence to its RED note).**
- **AT-10 (missed; reader-1, verified by this seat's own Tailwind compile) — Reset's signature hover spin snaps.** ActionToolbar.vue:9 `hover:-rotate-180` emits the standalone `rotate:` property (compile output sighted this session), which is outside ActionButton.vue:25's `transition-[transform,stroke]` list — the 180° turn jumps both directions. `duration-normal` beside it is live (glass bridges `--transition-duration-normal`) but retunes only scale/stroke, putting Reset on a different duration from its four siblings. **Disposition: NO-WAVE-OWNER** (the transition list lives in ActionButton.vue — gap).
- **AT-11 (missed; reader-1) — no group semantics: a bare `<div>` with five tab stops mid-dock.** ActionToolbar.vue:2 has no `role="toolbar"`, no accessible name, no roving focus (APG toolbar = one stop + arrows); keyboard traversal costs five Tabs between Back and the mode toggle. The L axis attributed this to a missing producer primitive (killed, K-3); it is two attributes and one keydown handler absent from this file. **Disposition: rider on X-W4.d** (the wave's single contract-driven render path is where group semantics land) — flagged: the obligation is NOT in W4.d's text, only the surface is.
- **AT-12 (missed; reader-2, merging reader-1's INFO) — `label` and `hidden` are dead props; `.action-label` is unpainted; `hidden` shadows the native attribute.** ActionButton.vue:65/:69: neither of the two consumers passes either (grep: ActionToolbar ×5, GenericActionBar ×1; `DockAction` has no such fields — unreachable by construction), so :3 `v-if="!hidden"` is always true and :32's span never renders; `action-label` has **no rule anywhere** (1 hit repo-wide = the template line) — were the prop threaded (which is D-7's own cure direction, K-5), it would paint unstyled; and a consumer's native `hidden` attribute would be swallowed as a prop. **Disposition: NO-WAVE-OWNER** (ActionButton.vue gap) — an X-W8.d-class row absent from G-10's census; flagged for the formation boundary.
- **AT-13 (missed; reader-2) — the 400 ms click-feedback timer is unmanaged.** ActionButton.vue:94-97: bare `setTimeout`, no handle, no `clearTimeout`, no unmount clear. A second click inside 400 ms restarts nothing (class already present; CSS animation needs removal + reflow) and the FIRST timer truncates it at t=400 while a stale timer fires at t=600; unmount mid-animation (reachable — Dock.vue:153/:156) leaves a live timer writing a disposed ref. The producer's clipboard register two imports away shows the intended discipline (clear helper + stale-guard + scope dispose). **Disposition: NO-WAVE-OWNER** (ActionButton.vue gap).
- **AT-14 (L:L-1 / C:C-3, PROMOTED — ruled above) — five hand-written invocation blocks beside the registry that already renders them.** ActionToolbar.vue:3-61 repeats the identical closure at :13/:24/:35/:48/:60; `GenericActionBar.vue` + `DockAction[]` is the same component generated from data, byte-identical root; only `active-style` is missing from `DockAction`. Drift visible: :9 alone carries `icon-class`; :21/:32/:43/:56 are the blank-line fossils of the others' (AT-19). **Disposition: BUILD → X-W4.d** (the SceneActionSet IS this consolidation; ActionToolbar becomes a five-row array or is deleted into GenericActionBar).

### INFO

- **AT-15 (C:C-2 / L:L-4, typing half only — the framing is K-4).** `activeHover: string | null` (ActionToolbar.vue:85; ActionButton.vue:61-62) binds nothing to the five literals or the emit names; a `hover-key` typo type-checks and kills that button's popover silently. reader-2's caveat adopted: the width is load-bearing for `GenericActionBar`'s `DockAction.key: string`, so the cure is a generic parameter, not a literal enum. **Disposition: X-W4.d** ("token union, not `string`" is verbatim in the wave's mechanism).
- **AT-16 (missed; reader-1) — `hover-key="browse"` names a live sibling concept the button does not route to.** ActionToolbar.vue:39 sits on the Palettes button (`@action` → `switchView("palettes")`, Dock.vue:55) while `browse` is a real ViewId with its own pane config (viewSchema.ts:124-132) watched by name elsewhere. Harmless today (the key is only the popover mutex identity) — but it is the ONE place L-2's feared command↔glyph↔label drift has actually happened. **Disposition: X-W4.d** (dies with the token union).
- **AT-17 (missed; reader-1, systemic) — `focus-visible:ring-ring/40` reaches a token the repo's own post-mortem records as empty.** ActionButton.vue:15; `--ring`/`--color-ring` defined **nowhere** in demo/ or glass dist (this seat's grep: 0 definitions); focus-ring.css:10-13 (U-F25) documents this exact emptiness; the invalid inner `var()` makes the ring fall back to `currentcolor` — visible, arguably higher-contrast, but undesigned, and the in-repo cure (`--focus-ring-inner/outer`) is unapplied. 22 `ring-ring/40` sites repo-wide — the identity is systemic. **Disposition: NO-WAVE-OWNER, repo-scope** (booking 22 sites behind this component would be the scope error the ColorSpaceSelector addendum's L-9.1 law names); flagged for the formation boundary.
- **AT-18 (missed; reader-1) — `copy` is a read verb with an undeclared persistent write.** ColorPicker.vue:324 `updateModel({ inputColor: … })` mutates session state that survives reload before touching the clipboard, while the emit is typed `copy: []` and the description promises a read-only action. Defensible ("show what was copied") but undeclared at the boundary. **Disposition: rider on X-W4.d** (the contract must declare the write or the handler must drop it).
- **AT-19 (missed; reader-1, cosmetic) — four blank-line fossils** (ActionToolbar.vue:21/:32/:43/:56) in the attribute slot the Reset block still fills — the visible residue of C-3's predicted drift, in the corpus's SHA-pinned specimen. **Disposition: dies with AT-14 (X-W4.d), incidental.**
- **AT-20 (missed; reader-2, DEMOTED — ruled above) — the root `class="pointer-events-auto"` on `<Popover>` is silently dropped** (`inheritAttrs: !1`, explicit prop list, no attrs spread; the :35 content binding is live). Vestigial since T-31 put the dock in-flow. **Disposition: NO-WAVE-OWNER** (ActionButton.vue gap), delete on next touch.

## Killed-claims register

Eight reader kills sustained (each re-derived at HEAD `8d7e17fe`), one reader-limb killed against the other reader, one reader self-kill preserved.

- **K-1 — C:C-4's "no atomic edit policy is encoded", KILLED (both readers; re-verified whole by this seat).** ColorPicker.vue:272 `preEditModel` snapshot (with the :283-284 comment proving WHY holding the reference is sound — `updateModel()` replaces wholesale), :291-301 `commitEdit` write-through, :303-309 `cancelEdit` restore, Escape/Enter bound :249-261. A mid-edit Reset/Random is fully revertible by the documented hatch — the policy the axis demands be built exists one file up the wire. Surviving limb (unlabelled asymmetric gating) carried at AT-6.
- **K-2 — C:C-5's ordering/double-activation/pending-lock limb, KILLED (both readers).** Three commands are synchronous local writes with last-write-wins over one `model` ref; the two navigations hit `switchView`'s identity guard (useViewManager.ts:73, read by this seat); the sole async path always resolves `{ok, reason?}` — no rejection, no order-visible state. Receipt IDs and pending locks would be machinery with no failure to prevent, against a codebase whose pinned law is KISS-no-contrivance. The real async gap is the DISCARDED result — AT-1.
- **K-3 — L:L-3's "toolbar, tooltip, icon button, and command-result behavior remain outside the producer library", KILLED (both readers).** glass-ui 7.0.0 ships all four — `DockControl` (44px coarse seat, `aria-pressed`, four-state disabled), `Popover` (pointer-adaptive), `useClipboard` (the full register), dock overflow (`.dock-overflow-wrap`) — and this dock consumes DockControl in the very same parent file (ActionBarLayer.vue:129) and same row (Dock.vue:154). The prescribed "producer command-toolbar primitive" would mint a second dock-control lineage beside the one in use. The defect is NON-ADOPTION in one demo file; the true residue (ActionButton hand-rolls what DockControl ships) is carried with measured consequences at AT-4/AT-3(e)/AT-6.
- **K-4 — L:L-4's "shared mutable string state" framing, KILLED (both readers).** ActionToolbar.vue:85 owns the single ref; children receive it readonly and request changes through a declared event; ActionButton.vue:79 `isOpen = computed(() => activeHover === hoverKey)` makes at-most-one-open correct by construction, for one line per consumer. Making hover "internal" per L's prescription would delete the invariant. Typing half survives at AT-15.
- **K-5 — D:D-7's "no overflow/label mode is specified" limb, KILLED (reader-1).** A label mode is specified in the component's own API (ActionButton.vue:65/:32) and a producer overflow mode is shipped (`.dock-overflow-wrap`); both are merely unused/unpainted — which is AT-12, a different defect than "unspecified". The target-size limb survives as AT-4 with the producer-floor mechanism.
- **K-6 — C:C-3's "no invariant ensures unique hover keys" limb, KILLED (reader-2).** Five distinct literals in one 60-line template; the other consumer feeds the same value to `:key` and `:hover-key`, so a duplicate surfaces as Vue's dev warning before a double-open popover. The duplication itself is AT-14.
- **K-7 — D:D-2's stated mechanism ("touch users cannot depend on hover"), KILLED as mechanism (both readers, convergent).** The producer already degrades `trigger="hover"` to click-to-open under `(pointer: coarse)` (dist read by this seat). The failure is the consumer's :93 nulling the toggle's own open in the same tick. Outcome carried at AT-2 with the replaced mechanism — fixing "hover" would be the wrong cure.
- **K-8 — D:D-8's reduced-motion audit cell — QUARANTINED, may not be carried.** motion-quarantine.md row **M-32** rules it FALSE (probe4: `animation-duration: 1e-05s` on the real `.action-icon`; both app-wide guards reach the pulse and the spin); :266 strikes the cell; :283 records the C·L axes as motion-free. No PRM defect exists on this component. AT-9/AT-10 are missing-motion defects (motion that fails to render), not PRM defects, and are not quarantined rows.
- **K-9 — reader-2's C-6 extract-mislabel limb, KILLED against reader-1's mount analysis (ruled above).** Unreachable at /extract on desktop; the /mix mis-signal carries the row (AT-3a); mobile path → residue 6.
- **K-10 — reader-1's own WHCM focus-loss hypothesis, self-killed pre-report, PRESERVED so it is not re-raised.** `focus-visible:outline-none` (0,2,0) would beat the U-F57 forced-colors bind (0,1,0) on specificity — but Tailwind v4 emits utilities into `@layer utilities` while foundation.css's forced-colors block is unlayered, and unlayered declarations outrank every layer. The WHCM outline wins. A correct pre-emptive kill, recorded with concurrence.

## Superlatives (provenance preserved)

- **Receipt accuracy from a probe-less corpus** (reader-1, verified by this seat): all three axes pin the same SHA-256, live `shasum` matches byte-for-byte at a HEAD four commits later, and every C and L finding names a construct that exists at the implied line — with HYDRATION-LEDGER evidence count **0**. The corpus's failures are failures of inference (stopping at the file boundary; prescribing primitives the dependency tree ships), never of fabrication.
- **Single-open by construction** (reader-2): `isOpen = computed(() => activeHover === hoverKey)` — one parent-owned string, children derive and never write, mutual exclusion as a type-level consequence. Two axes read this as the defect; it is the best thing in the component (K-4).
- **The transactional edit** (both readers): ColorPicker.vue:272-309 — snapshot, commit, restore, Escape/Enter, and a comment justifying the snapshot's soundness. The code that killed C-4 outright.
- **The W5-a11y button, load-bearing in CI** (both readers): a real `<button type="button">` with `:aria-label`, focus ring, free Enter/Space — and e2e o10d:356 drives it by accessible name (`getByRole("button", { name: "Copy color" })`), so the label cannot silently rot.
- **The accent is contrast-guarded at the source** (reader-1): ActionBarLayer.vue:105 feeds `safeAccent` (the `SAFE_ACCENT_KEY` WCAG-guarded token), not the raw `actionBar.cssColorOpaque` sitting on the same context object — the "pick a color that vanishes the chrome" critique was closed before it could be made.
- **Provenance-carrying comments** (both readers): ActionButton.vue:37-41 (defect, mechanism, wave, cure in four lines — T.W4-6 · T-15/F7) and ActionBarLayer.vue:53-61 (the shim documents its producer regression, its contract, and its own retirement condition as a standing relay note — which X-W8 G-8 has now answered).
- **Colocation pinned by test** (reader-2): test/picker-blob-config.test.ts asserts the single-owner rule for Copy by counting `writeClipboard(` and `:icon="Copy"` occurrences — a structural invariant enforced by assertion, not prose.
- **The component is honestly thin** (reader-1, adopted as this record's closing sentiment): 92 lines, one hover ref, five pure emits. All three axes prescribe growth (command registry with receipts, pending locks, a new producer primitive); every confirmed defect above is cured by subtraction or adoption — delete two dead members, adopt DockControl/DockCrossfade/`keepDockOpen`/`useClipboard`, read one result instead of discarding it — and the file gets smaller AND more conformant.

## UNPROVEN-NEEDS-LIVE residue (the visual-audit inputs)

1. **AT-5 corroboration only**: the dock's `data-held` stamp after hover→click→view-switch and after N coarse taps (the leak is CONFIRMED statically; a live read merely witnesses the stranded counter). Chromium coarse-pointer emulation at ≥1024px.
2. **AT-4 magnitudes**: served `getComputedStyle` of `.action-button-wrapper` vs a sibling `DockControl` under `(pointer: coarse)` — the exact 32-vs-44 delta and the gap between adjacent 32px cells.
3. **AT-17 consequence**: whether the `currentcolor` ring fallback clears 3:1 against `--glass-bg-floating` in both schemes (2.4.11/1.4.11) — one rendered contrast sample.
4. **AT-9 perception**: one frame-capture of the actions↔input toggle (X-W8 G-8's motion arm already specifies this exact probe — chromium :9000, both faces at +60 ms; do not duplicate, feed it).
5. **AT-2 cosmetic**: whether the coarse tap paints a one-frame popover flash before the same-tick close.
6. **K-9's open limb**: the mobile (<1024px) mount path for the toolbar at /extract — whether any route exists where the Palette glyph lights while Extract is the visible surface.
7. **Motion: nothing carryable.** M-32 struck (K-8); zero quarantine TRUE rows for this component; AT-9/AT-10 are missing-motion, not PRM.
8. **Coverage note for the consuming wave (not a live probe)**: reader-2's census stands — the only test references to this component assert its SOURCE TEXT; none of the twenty confirmed rows would be caught by the suite today.

## Closing verdict

**ADJUDICATED (never VERIFIED — no wave is stamped by this file): 20 rows CONFIRMED — 0 BLOCKER · 5 MAJOR (a primary command that paints success over a typed failure; a tap that closes the explanation it opened, leaving five unlabeled glyphs for an entire input class; an active state that is wrong at /mix, contradictory during every edit, hue-only, and AT-invisible; a 32px hit cell undercutting the producer's 44px coarse floor in its own row; a hand-rolled ref-counted dock hold that leaks per tap and per unmount) · 9 MINOR · 6 INFO; 10 kills re-derived (C-4's missing-policy, C-5's ordering limb, L-3's missing-producer framing, and L-4's shared-state framing all die against code already in the tree); severity settlements: 2 ruled MAJOR over a reader's MINOR (AT-1, AT-2), 2 demoted (AT-9, AT-20), 1 promoted (AT-14), 1 reader limb killed against the other reader (K-9); dispositions routed X-W4.d ×8 · X-W8 ×3 (each already named by W8's own G-8/G-10 — this record confirms and feeds them, never re-books) · 1 rider on X-W7 G19 · 9 NO-WAVE-OWNER (+2 partial limbs), with one named file-list gap flagged for the formation boundary: `ActionButton.vue` — the site of every MAJOR's cure or co-cure — appears in no X wave; the r1 corpus governs (no r2 exists, zero promotions, M-32 struck); 6 residue items feed the visual audit.** The component's signature is inversion: all three axes read SOURCE-RED and prescribe growth, but every confirmed defect is a shipped producer facility bypassed by 229 demo lines — the cure direction is subtraction and adoption, and the file that results is smaller than the one the challengers audited. Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
