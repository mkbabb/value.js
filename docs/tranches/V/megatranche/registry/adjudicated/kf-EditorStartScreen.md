claude-fable-5

# kf-EditorStartScreen — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ HEAD `8281638c` with a **CHURNING working tree — 252 dirty rows** (this seat's own `git status` count, re-measured today). `EditorStartScreen.vue` (191 lines) is **CLEAN**; **`TypingDots.vue` is DIRTY with a load-bearing flip** — HEAD carries `timingFunction: steppedEase(4, "jump-none")` plus a static `import { steppedEase } from "@mkbabb/value.js/easing"`; the working tree replaces the callable with the string `"steps(4, jump-none)"` and deletes the import (this seat's own `git diff HEAD`, byte-verified). That one flip toggles WAAPI eligibility, the "no static value.js edge" superlative, and the truth of TypingDots' own :25-26 comment — see rulings 5 and 7. **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two hostile re-reader returns (reader-DU, design-and-usage · reader-LC, library-and-code) over the component's three challenge files (`audit/kf-components/EditorStartScreen/challenge-D-design.md` 430 · `challenge-L-library.md` 570 · `challenge-C-consumption.md` 313). The challenge files were re-read by this seat at every ruling anchor (ND-1, ND-2 whole incl. its falsifier clause, the L §4 kill table whole, D-C1 whole, C's contradictions header, L §5-§6); the two returns were attacked before either was adopted.
**Method (incredulity)**: every verdict-level disagreement (ND-2 is the big one), every kill of a load-bearing cell, and every admitted miss was re-derived against the bytes with this seat's own instruments. Zero browser probes (probe parsimony; the lane's no-browser law holds — every dynamic claim routes to SS-13). One suspiciously-agreed cell **failed under this seat's re-derivation** (ruling 4 — an L-EST row BOTH readers confirmed at MAJOR dies against a live CI gate neither grepped for, the exact falsifier-shape kf-AnimatedText K4 had banked the day before). Two of the readers' "missed" findings turned out to be **re-discoveries of banked kf-App rows** (identity-guard events, ruling 2). Dispositions are **ADJUDICATED, not VERIFIED**; X·KF specs are still forming — every routing below is agglomeration input (M-25), not an act.
**This seat's own instruments (all read-only + git against the working tree)**: full reads of EditorStartScreen.vue (whole, 191) and TypingDots.vue (whole, 125, dirty bytes) + `git diff HEAD -- TypingDots.vue`; App.vue:220-258 (`currentChannels` un-gated at :227 under its own false ":222-226 `undefined` for home" comment; the `isHome`-gated `derivedSurfaces` sibling at :248-255 with the ":244-246 home exposes cube's PAINTING facility" concession); AnimationControlsGroup.vue:85-100 (the VERDICT #6 comment + `v-if="transportNames.length > 0"`) + :178-186 (`channels?.map(...) ?? Object.keys(...)`); useSceneMachineShellBinding.ts:240-258 (the home-Play navigate intercept verbatim); main.ts:50-54 (`void Promise.all([warmKfEngine().catch(() => undefined), fontsDecoded]).finally(...)` — byte-exact); style.css:1-5/:40-56 (the `@theme` block's false "there is no `--font-display-weight` token" comment)/:76-90 (the sole "Instrument Serif Fallback" `@font-face`: `src: local("Georgia")`, size-adjust 105.9310%, **no `font-style` descriptor**)/:90-91 ("Project-specific tokens — OUTSIDE @layer so they override glass-ui defaults" + `:root`)/:262-274 (the 8-selector `@layer demo-typography` weight/tracking block); index.html:45-76 (ONE preload = the upright latin woff2; the `ital@0;1` sheet `media="print" onload`; the "italic + latin-ext are not above the fold" comment verbatim); installed glass-ui 7.0.0: typography/scale.css **0 `@layer` declarations** + `--font-display-weight: 600; --type-weight-display: var(--font-display-weight)` verbatim, typography/semantic.css `text-display-mega { … line-height: var(--type-leading-display); letter-spacing: var(--type-tracking-display); font-weight: var(--type-weight-display); text-wrap: balance }` verbatim, tokens/scheme-motion.css `--type-leading-display: 1.05` / `--type-tracking-display: -0.015em` / `--z-controls: 20` (**correcting reader-DU's miss-provenance: the leading/tracking tokens live in scheme-motion.css, not scale.css**), theme/bridges.css `@theme inline { … --z-index-controls: var(--z-controls) … }`; library plumbing: constants/defaults.ts:86 `useWAAPI: true`, easing.ts:38-39 `CSS_FUNCTION_EASING` matching `steps(`, waapi/delegation.ts `animation.playback.loop(shadowTick)` (the per-instance shadow rAF), group/waapi.ts:29-33 (`!group.singleTarget` refusal), engine/play-lifecycle.ts:364-370 (managed-child `play()` throw) + :375-392 (PRM snap-to-final-frame → WAAPI-eligibility routing), orchestration/stagger.ts:15-22 (the docblock's `{ animation, options: { delay } }` example) vs group/types.ts:27-30 (`AnimationGroupInput` — **no `options` field**), load-engine.ts:123-124 (`enginePromise ??= import("./public")` — memoizes a rejection); scripts/observe/demo/usability.mjs:19-31/:72/:134-161 (the per-char hero contract clauses + the `heroReady` 30×300ms poll — the L-EST-4 decider); node_modules/@lucide/vue/dist/esm/Icon.mjs:30 (`strokeWidth: contextStrokeWidth = 2`); executed census `grep -rn '<h1|<h2|<h3' demo --include='*.vue'` → exactly h1×1 / h2×3 / h3×2, matching the C-axis table row-for-row; `container-type` in demo/styles → the sole opt-in utility at style.css:239; **registry cross-reads (the identity-guard bank)**: kf-App.md (KF-APP-2/-4/-6/-7/-8/-10/-18/-19/-23/-41 + ruling K-2's three-substrate `--dock-margin` decider incl. its own re-read of EditorStartScreen.vue:117-123), kf-AnimatedText.md (KF-AT-1/-2/-3/-6/-14/-17/-22/-24 + K4 + the P-1..P-11 probe set), kf-ChromeDock.md (the D-1→KF-APP-6 fold + the `c7f7c96d` provenance rider + the recorded "both re-readers re-filed a banked blocker" event), kf-CubeScene.md (D-9 breakpoint-desync adjacency), CENSUS-2026-08-03.md §(a) (the KF.W0..W10 taxonomy).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**38 adjudicated roster rows — 0 fresh BLOCKER · 5 MAJOR booked · 14 MINOR booked · 4 INFO clusters booked · 15 folds to banked identities** (of which two carry blocker-weight at their banks: KF-APP-6, census F-1). **13 killed-claims register entries** — 11 sustaining the readers' kills, **2 of this seat's own against cells the readers agreed on** (ruling 4's L-EST-4 kill; ruling 2's fold of a "missed" finding into its bank). **16 distinct admitted misses across the two readers, all adjudicated real** — two of them re-discoveries of banked kf-App rows, one (WCAG 2.2.2) a re-discovery of a banked kf-AnimatedText row. **12 superlatives sustained.** Both reader-confirmed BLOCKERs die as fresh bookings: **ND-1 folds to banked KF-APP-6** (the third registry event of readers re-filing that bank as a fresh blocker) and **ND-2 is killed by its own pre-declared falsifier** (reader-LC's kill, third-derived by this seat at today's bytes).

MAJOR-weight survivors of record: **KF-EST-1** (eight dead `proof:*` authorities cited as binding — five styled as OWNER locks — in the one file whose central defect class is prose asserting unwired machinery) and **KF-EST-2** (the italic display face is above the fold, unpreloaded, and its metric-matched fallback is upright-only under `font-synthesis: none` — the deck and hint paint upright for the whole swap window, and the in-file ":32 already loaded; zero new payload" is false in both halves). Realized shipping impact of the booked majors is latent-to-cosmetic; the banked folds (the IACVT mobile seat, the home transport that mounts against an owner verdict) carry the live geometry.

## The incredulity seat's own work — ruled disagreements and corrections

1. **ND-2 — RULED for reader-LC's KILL; reader-DU's BLOCKER confirmation CORRECTED.** The one verdict-level disagreement in the corpus, third-derived by this seat at today's bytes: `currentChannels` (App.vue:227) has **no home gate** — its own :222-226 comment asserts "`undefined` for home," while the gated sibling twelve lines down concedes the truth (":244-246 Home renders the SAME CubeScene component … so `sceneRef` on home exposes cube's PAINTING facility"). The un-gated channels flow into `transportNames = channels?.map(...) ?? Object.keys(animationGroup.animations)` (AnimationControlsGroup.vue:181-183), the `??` fallback the shell-binding's empty home group was built to feed **never fires**, `v-if="transportNames.length > 0"` is TRUE, and the TransportDock — with the app's only Play control — renders on home. Corroboration the corpus itself could not see: the dedicated home-Play navigate intercept at useSceneMachineShellBinding.ts:246-257 ("Home 'play' is a user gesture — it navigates to cube and auto-plays") exists **because** home Play is reachable. Decisively: **the challenge's own falsifier clause pre-declared exactly this downgrade** ("a `transportNames` derivation that is non-empty for the home `AnimationGroup` … downgrades this to the pass-1 D-4 MAJOR" — challenge-D-design.md:194-196, re-read whole), and reader-DU confirmed the BLOCKER from the comment without executing the falsifier. And the whole mechanism **is banked**: kf-App **KF-APP-7** booked the un-gated `currentChannels`, the false comment, and the VERDICT #6 violation, with both of ITS readers having closed every suppression falsifier. So: ND-2 dies at BLOCKER; reader-LC's "missed" home-transport MAJOR is a **re-discovery of KF-APP-7** (identity-guard event — folded, never re-booked); and this seat adds the surviving coupling neither reader named: **the deck's strongest clause ("below, then press Play.") is true today only by parasitism on the KF-APP-7 defect** — the transport is below and Play exists, because the guard is missing. KF-APP-7's one-line cure orphans the copy. The copy decision and the guard must land in ONE motion (KF-EST-7).
2. **ND-1 — identity-guard fold; both readers corrected on booking, not on substance.** Both re-readers confirmed `--dock-margin`-IACVT as a fresh BLOCKER with exemplary chain-walks — and the row was banked at **KF-APP-6** (kf-App, with kf-ChromeDock's `c7f7c96d` deletion-provenance rider and a three-substrate zero-definitions decider that already **re-read this component's :117-123 fallbacks** and derived the deterministic 52dvh seat). kf-ChromeDock recorded the identical event for ITS two readers; this is now the **third** registry occurrence of the same banked blocker being re-filed fresh. FOLD BY REFERENCE, nothing re-booked; the component-side residue that survives here is argumentative, not mechanical: the file's most-argued invariant (":82-86 Derived ENTIRELY from the work-area chain … No raw vh/px magic number") is false below `lg` — a comment-truth rider attached to the fold.
3. **The L-§4 italic kill — reader-DU's kill-of-the-kill SUSTAINED on this seat's own bytes.** The L-axis §4 row killed the "italic renders upright" candidate on two legs; both fail: (a) "`ital@0;1` — both faces, one payload" — the css2 endpoint emits separate per-style `@font-face` blocks with separate woff2 URLs, and index.html's single preload (:55-61) is the UPRIGHT latin file, the ital sheet deliberately non-render-blocking (`media="print" onload`, :69-76); (b) "the fallback chain carries true italics at every rung" — style.css:81-88 is the ONLY "Instrument Serif Fallback" face, `src: local("Georgia")`, **no `font-style` descriptor** (this seat's read) → the family holds a normal-only face; per CSS Fonts 4 §5 a matched family is not skipped because the requested style is absent, and `:root { font-synthesis: none }` forbids the compensating oblique. Nine of the ten §4 kills hold (several re-executed here — the condense-trap kill, the steps-string kill, the WAAPI row in one direction, see ruling 5); this one is the single bad entry, restored as **KF-EST-2** (MAJOR). Reader-DU's observation stands: publishing refuted candidates is what made the wrong one findable.
4. **L-EST-4 — BOTH readers corrected; this seat's own kill.** "Zero test coverage for the LCP hero" was confirmed MAJOR by both readers on `test/`-scoped greps. The tree refutes the scope: `scripts/observe/demo/usability.mjs` hard-polls `h1 .wave-char` (heroReady, 30×300ms then FAIL), asserts the per-char hero contract, the sr-mirror-derived glyph count, and the depth-text ban, and runs **HARD in CI** — the exact falsifier-shape kf-AnimatedText **K4** killed for the sibling file the day before, banked one directory away. Survivors, RESCOPED to MINOR (KF-EST-6): zero **vitest** coverage for all three files; the **TypingDots engine seam** (loadAnimationEngine + N infinite engine animations) exercised by no gate anywhere; and — per banked KF-AT-4 — the gate that does cover the hero computes its oracle with the subject's own UTF-16 counter, so its net has a shared hole. Two readers confirming a row at MAJOR that a banked kill-precedent already scoped is the negative-direction citation-inheritance signature this registry exists to catch.
5. **L-EST-5 — mechanism RULED substrate-split; reader-LC's kill of the deflation SUSTAINED at the working tree, and the killed row is accidentally TRUE at HEAD.** This seat re-derived the full chain: `useWAAPI: true` default; the string `"steps(4, jump-none)"` matches `CSS_FUNCTION_EASING` → a faithful `.css` twin → each single-target opacity-only dot IS WAAPI-eligible → `playViaWAAPI` installs a per-instance **shadow-tick rAF loop** (delegation.ts `animation.playback.loop(shadowTick)`). At HEAD, the callable `steppedEase(4, "jump-none")` gets no `.css` twin → `playRAF` → N per-instance **engine** rAF loops (reader-DU's original mechanism, correct AT HEAD). Both substrates: **N rAF callbacks per frame for a 3-glyph ellipsis** — the charge survives at MAJOR (KF-EST-5). The CURE is severed at both substrates (cure-separation law, ColorSpaceSelector addendum 2): `AnimationGroup` refuses multi-target (`!group.singleTarget`), a managed child's `play()` throws, and no per-child `delay`/`iterationCount` handling is evidenced anywhere under `src/animation/group/` — at the working tree the rewrite would additionally trade 3 compositor animations for 1 main-thread paint loop. The stagger docblock sub-finding is exact and library-side: stagger.ts:15-22's example passes `{ animation, options: { delay } }` where `AnimationGroupInput` (group/types.ts:27-30) has **no `options` field** → KF.W5.
6. **L-EST-2's cure — reader-LC's kill SUSTAINED on this seat's own layering read; D-C1's cure adopted.** glass-ui's `--font-display-weight: 600` sits in a scale.css containing **zero `@layer` declarations**, imported plain at style.css:3 — an unlayered `:root` declaration. Tailwind v4 `@theme` emits into `@layer theme`; unlayered beats layered, so the L-axis's "two lines into the `@theme`" cure loses to the very producer default it targets. The demo's own sheet documents the correct home in as many words (":90 Project-specific tokens — OUTSIDE @layer so they override glass-ui defaults"). The C-axis's `:root { --font-display-weight: 400; --type-tracking-display: 0; }` wins by source order among unlayered `:root` rules. Attached as the **cure-discrimination rider to banked KF-AT-22** (which named the one-line retire without choosing a home). The L-axis's remediation step 3 must never be executed as written.
7. **Substrate provenance — reader-LC's kill SUSTAINED and EXTENDED.** All three axes declare `8281638c` READ-ONLY and then cite working-tree-only bytes (the `"steps(4, jump-none)"` string; EditorShell line numbers). This seat's diff confirms the flip verbatim — and extends it: the dirty edit **orphans TypingDots' own :25-26 comment** ("`steppedEase` is the value.js CURVE for the discrete dot cadence"), which now names an import the working tree deleted; and it flips S-C1's "no static value.js edge" TRUE (worktree) ↔ FALSE (HEAD). Every consuming wave re-anchors both line cites AND mechanism claims against the substrate it lands on (the KF-AT-28 law, restated with a sharper witness: here the CHURN toggles a WAAPI eligibility bit, not just a line number).
8. **Severity contests.** (a) D-2: reader-DU's own BLOCKER→MAJOR amendment ADOPTED (structural premises certain — a `top`-only band inside double `overflow: hidden` clips, never scrolls; the wrap-count term is font-soft) — booked MAJOR, magnitude → SS-13. (b) D-5/D-C15 (PRM rest at 0.2): reader-DU's MINOR ruling with the C-axis's INFO reasoning ADOPTED at MINOR — the sharper surviving substance is the release-direction asymmetry from the D-S2 kill (reduce→no-reduce: the CSS wave re-arms, the engine dots stay frozen at 0.2 forever — no `watch`, no engine re-arm) → KF.W9 PRM unification. (c) L-EST-1's BLOCKER: reader-LC's double-counting demotion ADOPTED — the rating is census F-1's, inherited; per-component it is exposure, and 21 CSS-coupled files each booking F-1 as a BLOCKER would count one defect twenty-one times. FOLD. (d) ND-6/cqi: MINOR (reader-DU) over INFO (reader-LC) — a viewport unit wearing a container unit's name inside the file's own vh-ban is a latent trap plus a comment-truth defect, not merely a curiosity. (e) The wave-phase-wrap miss: reader-DU's "at 66 the delay equals exactly one cycle" corrected — 3600/55 = 65.45, so the wrap is near-phase at ≥66, not exact; substance intact at INFO.
9. **Reader-DU's kills of its own axis's arithmetic — SUSTAINED, spot-derived.** The ND-3-absolute kill (the −7.1px half-leading assumed a 1em content area; Instrument Serif's declared 1.424em gives −44.7px; the axis contradicted itself between ND-3 and D-10; the poster gap is 3× LARGER absolutely, 9× off the "4.9px" figure) re-derives cleanly against style.css:79's metrics. The D-10 collision kill (2-7px clearance, tight-not-touching) holds; the intra-h1 case one line up — clearance −0.04 to +0.01em — is the real finding and is booked (KF-EST-8). The provenance kills (style.css :64-71 vs :81-88; the C-axis's off-by-one prop/clamp anchors; AnimatedText :36 vs :37) all verified against this seat's own reads; folded into the re-anchor law.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0..W10** per CENSUS-2026-08-03 §(a) (W0 substrate settle · W4 gate/hygiene law · W5 D/L/C library tri-fold · W6 glass suffusion/demo · W8 structure & colocation · W9 Safari visual audit = the SS-13 surface · W10 fold discharge); rows the forming specs do not yet own are **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring. Line anchors are the 2026-08-08 working tree; **re-resolve before any cure** (D-19; ruling 7 — the churn moves mechanisms, not just lines).

### Folds to banked identities (fold-by-reference, anti-rename — never re-booked)

| corpus ids | banked identity | what this corpus adds to the bank | route |
|---|---|---|---|
| **ND-1** (both readers, BLOCKER) | **kf-App KF-APP-6** (`--dock-margin` phantom at all three substrates; the 52dvh hero fallback already derived at the bank) | The comment-truth rider: EditorStartScreen.vue:82-86's "Derived ENTIRELY … No raw vh/px" is false below `lg`; third registry event of readers re-filing this bank fresh | KF.W6 phantom-token audit; rendered seat → KF.W9 |
| **reader-LC miss-1** + ND-2's comment arm | **kf-App KF-APP-7** (un-gated `currentChannels`; TransportDock mounts on home against VERDICT #6; the false AnimationControlsGroup.vue:90-96 comment) | The copy-parasitism coupling (ruling 1) → KF-EST-7; the SS-13 ordering: probe the home Play control BEFORE any copy cure | NO-WAVE-OWNER (bank's one-line guard + comment cure) |
| **D-1** + L-EST-8's cross-ref legs + reader-LC's third-staleness extension | **kf-App KF-APP-8** (both hero-vs-die comments false; `pt-[var(--dock-top-band-reserve)]` grep = 0) | The design-idioms.css mis-home at CubeScene.vue:261 — **all three lines of one six-line comment now false**; the remediation unit is the comment block | NO-WAVE-OWNER; 390×844 overlap capture → KF.W9 (banked) |
| **D-4/D-C14's** "list"/"below" referent legs | **kf-App KF-APP-23** ("from the list ☰ below" points up at a collapsed circle, strengthened by KF-APP-6 `top: auto`) | The KF-APP-7 interplay: "below … Play" is true of the BOTTOM transport today, by defect — the referent table is substrate-of-defect-dependent | NO-WAVE-OWNER |
| **ND-4** + D-C5/L-EST-11/D-6 | **kf-App KF-APP-10/-18/-19** (the orientation cluster: one-sentence outline; six scenes h1-less; landmark poverty) | Riders: the h3 rung skips a level even on home (census h1×1/h2×3/h3×2, re-run this seat); one of the three h2s is `v-if`-conditional on a prop | NO-WAVE-OWNER (one a11y spec surface) |
| **L-EST-1 / D-C9 / F-1 leg** (both readers) | **census F-1/SCH-1** (glass-ui undeclared; frontier-cured per kf-ChromeDock's bank note) | The exposure evidence: `z-controls` is a glass-ui-GENERATED utility (`--z-index-controls: var(--z-controls)` in `@theme inline`, bridges.css — re-read this seat); the LCP node's whole type/ink/stacking contract crosses a boundary no import tool sees | KF.W0 §B-12 |
| **L-EST-2 / D-C1** (both readers) | **kf-AnimatedText KF-AT-22** (BG-6 shipped; `--font-display-weight` exists; both comments false; the redundant weight mechanisms) | **The cure-discrimination rider (ruling 6)**: `:root`, never `@theme` — the unlayered producer beats a layered theme emission; L-remediation step 3 is structurally unsound; this component adds a THIRD in-file redundancy (`:108 font-synthesis: none` scoped, beside :root's) | KF.W6 |
| **ND-7 / D-C2** (both readers) | **kf-AnimatedText KF-AT-3 + KF-AT-6** (the UTF-16 split; `splitText` named from this file, consumed zero times; the five-constraint unexecutable-migration set) | The C-axis's no-mechanical-swap posture independently re-derives the bank's constraint set from the consumption side — concordance recorded | KF.W6 (with KF-AT-4's same-wave oracle law) |
| **L-EST-6 / D-C6 / ND-10** | **kf-AnimatedText KF-AT-1** (the `const { offsetMs, cycleMs } = props` freeze) | Nothing new — the compiler receipt at the bank is the stronger form | KF.W6 |
| **L-EST-12** | **kf-AnimatedText KF-AT-2** (`$attrs` fan-out ×N inside the aria-hidden subtree) | Nothing new; anchor corrected :37 (reader-DU's kill, sustained) | KF.W6 |
| **L-EST-16** | **kf-App KF-APP-4** (no SFC typecheck/lint reaches demo) ≡ kf-AnimatedText KF-AT-12 | This component as one more latent-MAJOR enabling instance | KF.W4 |
| **L-EST-17** | **kf-AnimatedText KF-AT-24** (font-roles.json pins `h1.hero-display .wave-char`; no runner reads it) | The :47-48 `.hero-hint` binding — the manifest reaches into THIS file's scoped class too; the closed loop (manifest ↔ :180's dead `proof:font-census` cite) feeds KF-EST-1 | KF.W4 |
| **reader-DU missed** WCAG 2.2.2 | **kf-AnimatedText KF-AT-17** (infinite auto-start motion on the LCP element, no in-content mechanism) | The TypingDots arm (second infinite source in the same `<h1>`) + the structural-exclusion rider (`:18 pointer-events-none`, zero interactive elements — the one place a control belongs is the one place the design forbids one); reader-DU's MAJOR recorded as dissent against the bank's MINOR-with-contest | KF.W6 |
| **reader-LC missed** `.hero-dots` no-op | **kf-AnimatedText KF-AT-14** (the dots-wrap contract is prose, not a declaration) | The stronger form: the rule is a TOTAL no-op (`white-space: nowrap` over an `inline-flex` child with no interior text nodes); the real guarantee is Vue's whitespace-condense — the comment credits the wrong mechanism | KF.W6 |
| **reader-LC missed** EditorHeader dead barrel | **kf-App KF-APP-41** (0 consumers, still barrel-exported) | The file is dirty in the working tree — being maintained while unreachable; identity-guard event (re-discovery of a bank) | NO-WAVE-OWNER (delete) |

### Booked majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **KF-EST-1** | L:L-EST-3 (both readers, concordant) | Eight `proof:*` citations across two files name gates deleted by `70b32501`/`92746148` as **binding authority** — :15 styled as an OWNER lock, :56, :134, :159, :180 + TypingDots :45, :51, :59 — the sweep missed both files (their last touch predates it); package.json retains exactly `proof:publish`/`proof:owner-golden`; the only surviving `typing-dots` strings in scripts/ are past-tense prose | both readers' git archaeology, concordant to the commit; this seat re-read all eight in-file sites | **KF.W4** (the provenance-gate law: every `proof:`/baseline/allowlist citation grepped against the executable surface — the L-axis's own generalization, adopted); comment cures ride any KF.W6 wave touching the files, with the deletion commit's "direct product checks" posture read first |
| **KF-EST-2** | D:D-3 / C:D-C3; the L-§4 kill REVERSED (ruling 3) | Both italic runs (:138, :149) are above the fold; the single preload is the upright woff2; the ital sheet is deliberately non-render-blocking; the metric-matched fallback family is **upright-only** under `font-synthesis: none` → deck + hint paint UPRIGHT for the whole swap window; :32-33 "the ital@1 face is already loaded; zero new payload" is false in both halves, and index.html:47-49's preload policy predates T.D11 moving italic into the hero | this seat: index.html:45-76 + style.css:81-88 (no `font-style` descriptor) + :100; CSS Fonts 4 §5 normative | **KF.W6** (the font-loading decision: preload the ital woff2, or an italic-declared fallback face, or retire italic from above-fold — a design choice, named); swap-window filmstrip → **KF.W9** SS-13 |
| **KF-EST-3** | C:D-C4 / L:L-EST-7 / D:D-15 + reader-LC's `glyph` dead-prop miss (child instance) | The deck's public contract is an icon-sandwich (`{{ subtitle }} <List/> {{ subtitleSuffix }}` from three separately-defaulted props) with no way to suppress/reorder the glyph; `subtitleSuffix` has zero external consumers; of the five props across the hero's two children, FOUR (`offsetMs`, `cycleMs`, `count`, `glyph`) have never been passed by any consumer in the tree | :40-44, :66-77; TypingDots :30-41; grep evidence both readers, concordant | **KF.W6** (decide reusable-or-inline for the copy contract — L-EST-7's own framing; latent) |
| **KF-EST-4** | D:D-2 (reader-DU's amendment adopted, ruling 8a) | `.hero-band` declares `top` + `padding-inline` only — no bottom/height/max-block-size — inside `html,body { overflow: hidden }` + `.editor-shell` `overflow-hidden`: on 720-768px-tall desktop viewports the derived seat + the mega rung's wrapped height exceed the remaining viewport and the hint **clips** (never scrolls); reader-DU's independent re-derivation landed on the axis's own figures to the decimal (419.9px into 397.1px at 1280×720) | :88-94; style.css:214-217; EditorShell.vue:3; both derivations concordant, wrap-count font-soft | **KF.W6** (bound the band: `max-block-size` or a bottom seat); clip magnitude at 1280×720 / 1024×640 / 320×568-two-line-hint → **KF.W9** SS-13 |
| **KF-EST-5** | L:L-EST-5 as substrate-split (ruling 5) + reader-LC's mechanism amendment | N per-instance perpetual rAF callbacks for a 3-glyph ellipsis at BOTH substrates — worktree: WAAPI + N shadow-ticks; HEAD: N engine `playRAF` loops — where the library's group owns one driver; the group-rewrite cure is severed (multi-target refusal, managed-`play()` throw, no per-child delay/iteration evidence); stagger's own docblock example does not typecheck against `AnimationGroupInput` | this seat's full chain re-derivation (defaults :86, easing :38, delegation shadow-tick, group/waapi :29-33, play-lifecycle :364-392, stagger :15-22 vs types :27-30) | Library arm (docblock + the group's missing per-child delay/iteration story + the group-vs-WAAPI composition gap) → **KF.W5** pre-seeded tri-fold rows; component arm → **KF.W6** evaluate-not-mechanical-swap (a group viability TEST precedes any rewrite — not a browser probe) |

### Booked minors

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **KF-EST-6** | L:L-EST-4 **RESCOPED by this seat** (ruling 4; both readers corrected) | Zero **vitest** coverage for the three hero files; the TypingDots engine seam exercised by no gate anywhere; the live gate that DOES cover the hero (usability.mjs, HARD in CI) shares the subject's UTF-16 counter (banked KF-AT-4) — the "zero coverage for the LCP hero" headline is false | **KF.W4** (coverage decision rides the gate-law lane; fold the gate-scope law to KF-AT-4/K4) |
| **KF-EST-7** | ND-2 residue + D-4 copy legs + reader-DU's tautology & typing-indicator misses (ruling 1) | The page's only sentence, adjudicated referent-by-referent: "the list"/"☰" false (banked KF-APP-23); "below … press Play" **true only by parasitism on KF-APP-7**; "from the list ⟨List⟩" is a tautology (the glyph repeats the noun; deletable at zero loss — and the glyph is Lucide `List`, a bulleted list, not the ☰ the comment names); the typing-indicator idiom (a chat/loading signal) breaks the sentence with an ellipsis + lowercase continuation, and AT hears no ellipsis at all (mirror carries none; dots aria-hidden) | **NO-WAVE-OWNER** (one copy/design motion, landed WITH KF-APP-7's guard — never separately; SS-13 home-Play probe FIRST) |
| **KF-EST-8** | reader-DU's intra-h1 + leading misses + ND-3 as re-rated (ruling 9) | `line-height: 0.92` at the mega rung sets consecutive h1 lines to −0.04..+0.01em ink clearance (touch/overlap on any descender-bearing non-final line; `title` is public; the default escapes by letter-luck); the leading is the THIRD un-tokenized display-rung override (`--type-leading-display: 1.05` published — in scheme-motion.css, provenance corrected) that the KF-AT-22 cure leaves behind; ND-3 survives at MINOR on the relative-proportion claim only (0.25em vs 0.42em of own rank) | **KF.W6** (the leading decision closes all three); collision render with a descender title → **KF.W9** SS-13 |
| **KF-EST-9** | reader-DU's gutter miss; the L-S5 kill's constructive half | `padding-inline: clamp(2rem, 5vw, 4.5rem)` (:93) is the seat's ONE un-derived axis — a repo-hapax raw-vw clamp misaligned with the work-area card's inline edge by 14-26px at common widths, sign-flipping ≈2400px, under a comment claiming full derivation | **KF.W6** (derive the gutter from the work-area chain or document the deviation) |
| **KF-EST-10** | reader-DU's icon-weight miss | `<List>` inherits Lucide's `strokeWidth = 2` (Icon.mjs:30, re-read) → ≈2.19px strokes at the 0.8em/26.3px box vs ≈1.8px serif stems — the icon out-weighs the prose inside a file whose stated law is ":133-134 No weight above 400"; one-attribute cure exists (`absolute-stroke-width`) | **KF.W6** |
| **KF-EST-11** | D:D-5 / C:D-C15 + the D-S2-kill residue (ruling 8b) | Under PRM the dots rest at 0.2 by the engine's snap-to-final-frame (chain verified to play-lifecycle :375-381) — a GATED choice the file documents (:51-53, :119-123), so MINOR not MAJOR; the sharper defect is the **release asymmetry**: on reduce→no-reduce the CSS wave re-arms, the engine dots never do (no watch, engine evaluates PRM once at `play()`) | **KF.W9** (the census's named PRM-mechanism unification; folds census §6.5 by reference) |
| **KF-EST-12** | D:ND-5/D-17 | `vertical-align: baseline` (:169) doesn't omit a fix — it OVERRIDES preflight's `vertical-align: middle` (unlayered scoped beats `@layer base`), riding the glyph ≈0.145em high; the comment misdescribes the mechanism twice | **KF.W6** |
| **KF-EST-13** | D:ND-6 / C:D-C11 / L:L-EST-19 (MINOR ruled, 8d) | `6.2cqi`/`5.4cqi` (:185/:188) have no query container (sole demo `container-type` = the opt-in utility at style.css:239, applied nowhere in this chain; glass-ui dist ships none) → they resolve against the small viewport — a viewport unit wearing a container unit's name inside the file's own vh ban; latent flip if any ancestor ever gains containment | **KF.W6** |
| **KF-EST-14** | L:L-EST-18 (both readers; "the subtlest correct finding") | The mobile subtitle rung (:185) beats the base deck rung (:140) by source order alone, at equal scoped specificity, under a DIFFERENT class name — a naive media-query consolidation silently deletes the 1.5rem serif floor; `.hero-hint` shares one selector and is not exposed | **KF.W6** — as a SEQUENCING CONSTRAINT on any sheet-consolidation pass (re-key the override to `.hero-deck` in the same motion), per the L-axis's own step-7 warning |
| **KF-EST-15** | C:D-C8 / L:L-EST-15 | The var() fallback posture is inverted: fallbacks on the two `:root`-guaranteed demo tokens (:90-91, :120-121), none on eight bare glass-ui reads — two of which (`--type-display-4`, `--muted-foreground`) are absent from glass-ui's published override manifest (manifest.d.ts, verified by both readers row-for-row) | **KF.W6** |
| **KF-EST-16** | L:L-EST-8's token leg (comment legs folded to KF-APP-8) | `--start-hero-band: 34dvh` (layout.css:26) — the token whose only job is clearing THIS hero — is a raw-dvh hand-fit that cannot track the derived, ResizeObserver-fed band it approximates | **KF.W6** (the phantom/raw-token audit, with KF-APP-6/-25/-26) |
| **KF-EST-17** | reader-LC's async-posture miss | TypingDots' async `onMounted` has no failure arm — `loadAnimationEngine()` memoizes a REJECTED promise (`??=`, load-engine :123-124), main.ts already swallows the boot failure, so the one visible surface for a failed engine chunk is an unhandled rejection on the LCP node; `anim.play()` (:98) floats a promise from a documented error-propagating async fn | Component arm → **KF.W6**; the memoized-rejection design question → **KF.W5** (library roster) |
| **KF-EST-18** | L:L-EST-13 / C:D-C7 | `count` is reactive in the template (`v-for`), mount-once in the engine wiring (delays materialized at setup, anims constructed once, no watch in the 88-line script) — the half-reactive contract KF-AT-1 books for the sibling, here in the engine dimension | **KF.W6** |
| **KF-EST-19** | L:L-EST-9 | `EditorShell.vue:62`'s `<EditorStartScreen />` default-slot fallback is unreachable (App.vue always supplies the slot) and diverges from the live call by dropping `hint` — dead code that would silently change the page if it ever fired | **KF.W6** (delete with the seam pass) |

### Booked info

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **KF-EST-20** | L:L-EST-20 / D:D-19-21 + C:D-C13 + C:D-C12 + L:L-EST-14 + L:L-EST-10 | The seam/no-op cluster: `p-0` (:27) and `w-full` ×2 inert; EditorShell's flex-centering + doubled `pointer-events-none`/`z-controls` inert over an absolutely-positioned child; `stagger(count,…).delays(count)` twice-redundant; `hint: undefined` in withDefaults documents nothing; the 9-line T.D12 tombstone; the two-media-block duplication (constrained by KF-EST-14) | **KF.W6** (one hygiene pass) |
| **KF-EST-21** | C:D-C14's comment legs + this seat's ruling-7 extension + reader-DU's ink-ramp miss | Comment-truth cluster: :165 names ☰, the tree ships `List`; index.html:38 names `text-display-4`, the tree ships `text-display-mega`; TypingDots :25-26 names a `steppedEase` import the dirty tree deleted (NEW, this seat); the deck→hint "one ladder" is two mechanisms (alpha :143 vs token :153 — why the measured step is 2.06:1 light / 1.49:1 dark) | **KF.W6** (comment/mechanism cures ride the waves already touching each line) |
| **KF-EST-22** | reader-DU's wave-wrap miss (corrected, ruling 8e) + reader-LC's ref-order miss | Composition latencies on public props: titles ≥66 glyphs put the per-char delay past one cycle (3600/55 = 65.45) → concurrent ripples vs the documented single sweep, uncompensatable because `cycleMs` is frozen (KF-AT-1); TypingDots pairs `delays[i]` with `els[i]` on an undocumented `v-for` ref-array order, under an unverified `HTMLElement[]` generic | **KF.W6** |
| **KF-EST-23** | L:C-1 / C:D-C10 (both readers concordant) | The U-lane's "the hero has ZERO engine dependency" (lane-22:110-112, U.D.md:194) is false transitively — `<TypingDots />` sits inside the LCP `<h1>` and awaits the engine; F3's transposition verdict survives on corrected premises (glyphs paint engine-free at frame 0; the dots' late start is designed); the lane's `@utils/kfEngine.ts` path never existed; the main.ts elision charge is retired (ruling: the corpus's own ellipsis marked it — reader-LC's kill, byte-verified) | **KF.W10** (doc-truth re-open beside KF-AT-21's r-animation-sota row — same class, second and third documents; dated evidence never rewritten, addenda only) |

## Killed-claims register — 13 entries

| # | killed claim | adjudication |
|---|---|---|
| K1 | **ND-2 (BLOCKER)** — "no Play control on home; the compensating CTA never built" | **KILLED (reader-LC; third-derived by this seat at today's bytes + the KF-APP-7 bank; ruling 1).** The challenge's own falsifier clause fired exactly as pre-declared. Surviving parts: the false VERDICT #6 comment (banked, KF-APP-7), the zero-interactive-elements fact (rides KF-AT-17's fold rider), and the copy-parasitism coupling (KF-EST-7, this seat's addition) |
| K2 | **The L-§4 italic kill** — "ital@0;1 is both faces one payload; the fallback chain carries true italics at every rung" | **KILLED-THE-KILL SUSTAINED (reader-DU; this seat's own font-face + index.html reads; ruling 3).** D-3/D-C3 restored as KF-EST-2. The one bad entry in an otherwise 9-for-10 kill table |
| K3 | **L-EST-2's remediation** — "two lines in style.css's `@theme` retire both mechanisms" | **SUSTAINED (reader-LC; this seat's layering read; ruling 6).** Unlayered producer beats layered `@theme`; the defect stands (folded to KF-AT-22), the L-cure must never execute; D-C1's `:root` cure adopted |
| K4 | **L-EST-5's deflation #1 + its §4 row** — "WAAPI lowering is NOT forfeited; any claim it is would be false" | **SUSTAINED AT THE WORKING TREE, and accidentally TRUE at HEAD (ruling 5).** The kill was killed in the wrong direction only where the string easing lives; the substrate split is now part of the row (KF-EST-5) |
| K5 | **Substrate provenance (all three axes)** — "substrate `8281638c`, READ-ONLY" | **SUSTAINED AND EXTENDED (reader-LC; this seat's diff; ruling 7).** The axes read the dirty tree; the flip toggles a mechanism, not just line numbers; TypingDots' :25-26 comment orphaned by the same edit |
| K6 | **L C-1 secondary correction #2** — "the corpus's mount-gate quote elides the failure-tolerance" | **SUSTAINED (reader-LC; main.ts:50-54 byte-verified this seat).** The corpus's ellipsis covered precisely `.catch(() => undefined)`; the charge was a non-correction |
| K7 | **ND-3-absolute / D-10's "4.9px of real air"** | **SUSTAINED (reader-DU; arithmetic spot-derived, ruling 9).** 1em-content-area error; true poster gap ≈44.5px; ND-3 survives at MINOR on proportion only (inside KF-EST-8) |
| K8 | **D-10's descender collision** ("any descender collides with the deck") | **SUSTAINED (reader-DU).** 2-7px clearance — tight, not a collision; the 4×-tighter intra-h1 case nobody charged is the booked row (KF-EST-8) |
| K9 | **D-axis style.css provenance** (":64-71 verified" for a block at :81-88; φ tokens at :73-74 vs :66-67) + **C-axis line anchors** (:68-70 props, :186/:189 clamps, :54-60 preload) + **L-EST-12's :36** | **SUSTAINED (both readers; this seat re-verified the true anchors).** Substance survives everywhere; provenance did not; folded into the re-anchor law (ruling 7) — a corpus whose §8 asserts source-decidability claiming line-verification of lines that don't hold is itself evidence |
| K10 | **L-S5** ("the seat is derived, with zero magic numbers") | **SUSTAINED (reader-DU).** Self-refuting on its own second quoted line; re-scoped to the vertical seat; the constructive half booked as KF-EST-9 |
| K11 | **D-S2** ("PRM honoured on all three motion sources … the consistent corner") | **SUSTAINED-AS-QUALIFIED (reader-DU).** Concordant on reduce; divergent on release (CSS re-arms, engine doesn't) — the residue is KF-EST-11's sharpest content |
| K12 | **L-EST-4 as charged** ("zero test coverage for the LCP hero"), MAJOR, **confirmed by BOTH readers** | **KILLED BY THIS SEAT (ruling 4).** usability.mjs's per-char hero contract runs HARD in CI; the banked K4 precedent scoped this falsifier a registry-day earlier; survivors rescoped into KF-EST-6 at MINOR |
| K13 | **L-EST-1 at BLOCKER (per-component)** | **DEMOTED-AND-FOLDED (reader-LC's double-count argument adopted, ruling 8c).** One census defect, one booking — census F-1/SCH-1; the component contributes exposure evidence, not a second blocker |

## Superlatives — 12 sustained (L-18 runs both ways; provenance per row)

1. **The async-teardown guard survives every attack from all three axes and both readers** (TypingDots :69/:76/:103-107): the flag re-checked immediately after the real suspension point, `stop()` on every constructed anim, the array cleared — and the belt-and-braces reading (main.ts pre-resolves the engine, making the guard correct for a boot order the component doesn't control) is the right one. Re-walked at the dirty bytes by this seat.
2. **S-C2 is the corpus's best single act of method**: the one string that crosses into value.js was EXECUTED against the installed 4.0.0, not reasoned about — and the execution mattered more than the axis knew: the `.css` twin it observed is precisely the WAAPI-eligibility bit that ruling 5 turns on, and precisely what the dirty-tree flip toggles. Given the parser lane's R1 is a live shipping crash, checking the boundary string on the demo's most-viewed component was the correct spend of a read-only budget.
3. **Reader-LC's ND-2 kill is the best act of incredulity in the two returns** — it executed the challenge's own falsifier instead of trusting a comment that cites an owner verdict, found the un-gated edge, the dead `??` fallback, and the home-Play intercept, and landed on the same mechanism kf-App had banked. That its "miss" was a bank re-discovery does not diminish the read; it confirms the bank.
4. **Reader-DU's kills of its own axis's arithmetic** (K7/K8) — re-deriving half-leading from the declared font metrics against the axis's own two contradictory figures, then locating the REAL collision one line up where nobody charged it, is analysis at the standard this registry asks for.
5. **The two-tier a11y mirror** (banked superlative, kf-AnimatedText #1) — re-affirmed from the consumption side; nothing in this corpus dents it.
6. **The LCP text is fully painted at frame 0 by construction** (`charLift` 0% = identity, fill both, transform-only): the demo's own LCP node cannot be blanked by its own animation. What style.css:52-55 needs to be true, is.
7. **The engine-painted property carries its resting paint in CSS** (REST_OPACITY 0.2 in script AND stylesheet, symmetric keyframes, the reason written down at :119-123) — the discipline that makes the pre-resolve window a designed frame; genuinely rare.
8. **The light/heavy barrel boundary consumed exactly as designed** (type-only heavy import, static LIGHT `stagger`, engine only through the loader) — with the substrate caveat now attached: at HEAD one static value.js `/easing` edge exists, and it is ALSO correct consumption (a subpath, not a deep path).
9. **The metric-matched fallback face engineered for THIS h1** (Capsize derivation preserved verbatim, correct element, correct concern) — still exemplary, and still the very block whose upright-only face KF-EST-2 charges: both readings are true at once, and the corpus finally credits the investment.
10. **Token discipline at the site is total** — zero hex, zero bespoke color, zero `--kf-*` hazard across all three files (both readers; consistent with the census §6.3 fold at the bank).
11. **The D-axis's honesty note under ND-3** — probing whether a glass spacing token could have been used before charging that none was, finding exactly two φ rungs, and narrowing the claim: re-run independently by reader-DU; the single most trustworthy paragraph in the challenge corpus.
12. **The L-§4 killed-candidates table as an instrument** — nine of ten entries hold under third derivation, and publishing the reasoning is what let reader-DU find the tenth. The one wrong entry produced this record's KF-EST-2; the instrument worked exactly as designed.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (→ KF.W9)

Ordering matters; the first two decide arithmetic that several others consume. One pass, one pinned commit, re-anchored lines (ruling 7).

- **P-1 · The home Play control** (KF-APP-7 fold; run FIRST, before any KF-EST-7 copy cure): load `/`, query `[aria-label="Play animation"]` (TransportDock.vue:63). The static chain is unconditional; the last link (reactivity ordering of `sceneRef` → `currentChannels` before first transport render) is the only live term.
- **P-2 · The KF-APP-6 settler** (banked, restated): `getComputedStyle(document.documentElement).getPropertyValue('--work-area-top-offset')` on a <1024px viewport — empty string = IACVT confirmed; D-1/D-7/ND-6 arithmetic and the ChromeDock anchor question all hang off it.
- **P-3 · D-2's clip magnitude** (KF-EST-4): 1280×720 and 1024×640 home captures — does `.hero-hint`'s bottom exceed the viewport, clipped by `.editor-shell`? Plus the L-§4-handed 320×568 two-line-hint case. The failure mode is a CLIP, not a scroll — tell the audit explicitly.
- **P-4 · Hero-vs-die overlap rects** (banked at KF-APP-8): 375×667 / 390×844 `getBoundingClientRect()` of `.hero-band` vs the cube stage, plus whether `.cube-stage--hero-recede` applies on home; cross-ref kf-CubeScene D-9 (the px-vs-rem breakpoint desync that revives the collision at enlarged root fonts).
- **P-5 · The italic swap window** (KF-EST-2): cold-cache first-paint filmstrip — deck+hint upright duration before the ital woff2 lands. Statics all proven; only magnitude is live.
- **P-6 · Intra-h1 collision** (KF-EST-8): mega-rung render with a descender-bearing two-line `title` (e.g. "Design a spring") — exact cap-height/descender extents of the shipped face.
- **P-7 · Ink over the die and the aurora** (banked at KF-APP-2 — the 1.0:1 lacquer crossing; the HeroAurora 0.1 ceiling verified, the WebGL cube layer not): per-frame incidence where the prose crosses the brightest face; no legibility mechanism ships (`text-shadow|paint-order` grep = 0, both readers).
- **P-8 · The group-rewrite viability TEST** (KF-EST-5, → KF.W5, not a browser probe): whether a multi-target `AnimationGroup` honours three infinitely-iterating, differently-delayed children at all — no evidence in the tree either way.
- **P-9 · PRM release-direction witness** (KF-EST-11): toggle reduce→no-reduce live; the wave should resume and the dots stay frozen — the asymmetry that decides the KF.W9 unification's shape.

## Identity guards (anti-rename; fold-by-reference, never re-book)

- **kf-App KF-APP-6 / -7 / -8 / -10 / -18 / -19 / -23 / -41** and **kf-AnimatedText KF-AT-1 / -2 / -3 / -6 / -14 / -17 / -22 / -24 + K4** — fifteen fold rows above, ONE identity each; every rider this corpus adds is attached at the bank's id, never re-keyed.
- **Census F-1/SCH-1** (KF.W0) — the glass-ui declaration churn; this file adds the `z-controls`-generated-class exposure cell.
- **Census §6.5** (PRM conscientious-but-inconsistent) — KF-EST-11 is one instance, folded by reference.
- **Census S-5/S-8** — S-5's re-homing to the splitText-dogfood decision is the BANKED correction (kf-AnimatedText ruling); this corpus's C-axis independently re-derived the same TypewriterText category error — concordance recorded, nothing re-booked. S-8's KEEP survives with its justification repaired by KF-EST-6 (the live-exercise net exists but is usability.mjs, not test/).
- Successor kf-* records fold against **KF-EST-1..23** and the banks named here.

## Closing verdict

Both hostile re-reader returns survive with **substantive corrections in both directions**, and the seat's work concentrated exactly where they disagreed: the corpus's two loudest BLOCKERs both die as fresh bookings — one into a bank both readers re-confirmed without checking (ND-1 → KF-APP-6, the registry's third such event), one by its own pre-declared falsifier (ND-2, reader-LC's kill third-derived here at today's bytes, its "missed" replacement itself a bank re-discovery at KF-APP-7) — while a MAJOR both readers agreed on (L-EST-4) died against a CI gate neither grepped for, under a kill-precedent banked one file away. The tree's deepest churn is not a line number but a mechanism: one uncommitted edit flips the dots' easing between a callable and a string, and with it WAAPI eligibility, a superlative's headline, and a comment's truth — the re-anchor law now covers mechanisms, not just anchors.

The component's adjudicated shape: **a genuinely well-made hero — frame-0 LCP paint, the banked a11y mirror, correct barrel consumption, a real metric-matched fallback, exemplary teardown — whose prose repeatedly outruns its tree**: eight citations of deleted gates styled as binding locks, a false producer premise held in three redundant mechanisms, an italic story whose fallback face cannot deliver it, a four-referent instruction whose only true clause is true because of a banked defect in another file, and a derivation boast with one un-derived axis. Five booked majors, fourteen minors, four info clusters, fifteen folds; routings: **KF.W0** the dependency churn (folded), **KF.W4** the gate/provenance law rows (dead `proof:` authorities, the rescoped coverage row, the manifest coupling), **KF.W5** the library rows (stagger docblock, group per-child gaps, memoized rejection), **KF.W6** the component repairs under the named constraints (the `:root`-not-`@theme` cure, the KF-EST-14 consolidation constraint, cure-severed KF-EST-5), **KF.W9** the nine-probe SS-13 residue with P-1/P-2 ordered first, **KF.W10** the two U-lane doc-truth re-opens, **NO-WAVE-OWNER** the design/copy/orientation clusters for SS-1/SS-2. Nothing lost: head, tail, or interval.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; the X·KF specs are forming and these routings are their agglomeration input (M-25); execution awaits the owner's begin-word.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/shell/EditorStartScreen.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **EditorStartScreen.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**KF-EST-1's EIGHT SITES ARE ROSTERED BY NAME AND BY FAMILY** at `GATE-ROSTER.md` §6(iii): `:15` `proof:hero-two-focal` (styled as an OWNER lock) · `:56` `proof:design-refinement` · `:134` `proof:hero-deck-voice` · `:159` `proof:styling-idioms` · `:180` `proof:font-census` · `TypingDots.vue:45`/`:51`/`:59` `proof:typing-dots` ×3 — **six distinct names over eight sites**, all inside KF.W4's eight citation targets, all **struck-in-owner-wave KF.W4** under G-KFW4-8. The family, its id and its second file are named; the roster no longer disposes two of the eight under other names while losing the family.

**KF-EST-21's SCOPING IS CORRECTED and EST ruling 7's DIRECTION IS REVERSED — for `TypingDots.vue` ONLY** (C-21 / KF-TD-2's inversion). For that file the pre-settle worktree was IDENTICAL to `origin/master` and the local pin `8281638c` was the OUTLIER (+4/−9): the WAAPI-lane readings are the shipping truth and the rAF-lane readings describe stale HEAD. **The correction is scoped to that one file** and to no other subject of either row — a blanket per-repo re-anchor would corrupt it.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

