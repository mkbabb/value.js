claude-fable-5

# kf-DemoGlobalChrome — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter)

**Seat**: fresh Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ HEAD `8281638c` (branch `master`) · registry branch `tranche-u` (value.js).
**Corpus**: three challenge axes by `claude-opus-5[1m]` — `challenge-D-design.md` (15 defects / 3 superlatives / 2 self-kills), `challenge-L-library.md` (14 rows / 3 superlatives), `challenge-C-consumption.md` (10 / 3) — attacked by two hostile re-readers: **RR-D** (design-and-usage) and **RR-L** (library-and-code). Both re-reader returns were themselves challenged by this seat before adoption.
**Method (L-18/M-23)**: every verdict-level disagreement between the re-readers was re-derived against the tree with this seat's own instruments; every solo-reader MAJOR net-new find was re-executed at its decisive file before banking; every kill was re-opened. **No browser tooling** (law); all rulings static + dist-read; the irreducibly visual residue rides to SS-13.
**Arbiter's own instruments this session (all read-only)**: `DemoGlobalChrome.vue` whole (49 L); `KeyframesEditor.vue:170-210`; `useKeyframeOps.ts:25-40`; `useShareState.ts:75-90`; `AnimationControlsGroup.vue` mount + import lines (:115-118, :125, :129); `EditorShell.vue` grep (:75 keyed mount, :120 direct SFC import); `useTabStripScroll.ts:27-31/:67-71`; `transport/index.ts:1-14` whole; `design-idioms.css:8-25/:128-146`; `DESIGN.md:43-49`; installed `vue-sonner@2.0.9`: `lib/index.css` **wc -l = 643** + rules at :21/:43/:67-79/:80-91/:298/:385-399 + `grep -c "data-styled='true'"` → 18; `lib/index.js` :296-320 (constants + `useIsDocumentHidden` whole), :28-55 (`create`/`alreadyExists`), :410-416 (Toast component), :450-500 (timer refs), :590-645 (the timer `watchEffect` — the M-5 decider); installed `@mkbabb/glass-ui@7.0.0`: `dist/components/toast/index.d.ts` whole, `use-toast.d.ts` tone/surface/duration + POSITIVE_INFINITY doc, `dist/styles/tokens/shadow.css` `--shadow-lg` redeclaration, `theme/bridges.css` shadow bridges (exactly 5, all `--shadow-glass-*`); `reka-ui/dist/DismissableLayer/DismissableLayer.js:80-92` + `Dialog/DialogContentModal.js:55`; `@lucide/vue/dist/esm/icons/paintbrush.mjs` path count → **3**; shipped `dist/gh-pages/assets/index-CL_QYCiO.css`: Preflight `button,input,select,optgroup,textarea{font:inherit…background-color:#0000;border-radius:0}` present, `@layer utilities{` @ byte 182187 with `.w-64{` @ 191676 inside, `.shadow-lg{…#0000001a…}` + glass `:root{--shadow-lg:0 4px 20px var(--shadow-color)}` coexisting; **executed compile**: `@vue/compiler-sfc` `parse`+`compileScript({inlineTemplate:true})` on the target (the C-9 decider — `createStaticVNode` with `style="stop-color:var(--rainbow-red);"`, author's `Teleport` import surviving unreferenced); executed greps: `toast.*(` → 24, `demo-no-oversize` → **16 hits / 13 files**, barrel consumers → 0, `rounded-xl` → 2, `rounded-panel` → 2, `shadow-lg` in demo → 1, `forced-colors` in demo → 0, 🎉/🔧 → 4 sites, `gradientUnits` in target → 0, `glass-ui` in both manifests → 0/0; `git log`: HEAD `8281638c`, and `a59d3a22 fix(dependencies): consume Glass UI 6 from one registry core` present in history.

## Scoreboard

**33 ADJUDICATED-CONFIRMED (1 BLOCKER · 9 MAJOR · 17 MINOR · 6 INFO, five of them FOLDs to banked identities) · 10 KILLED (with executed refutations; two struck re-assertions, one kill-of-a-confirm) · 12 net-new re-reader finds BANKED · 10 UNPROVEN-NEEDS-LIVE rows to SS-13.**

The blocker: **D-1/L-1/C-1** — the demo's only notification surface has no stylesheet in source or in the shipped bundle; the toaster is never `position:fixed`, lands after a ≥100dvh `<body>` inside `html{overflow:hidden}`, and every one of the 23 publishing `toast.*` call sites (all typed, across 8 modules — the demo's entire parse/compile/export diagnostic channel) is structurally unreachable. Both re-readers confirmed all five legs independently; this seat re-verified the decisive ones (sheet is a separate subpath; zero CSS pulls in `lib/index.js`; the shipped bundle carries the runtime and not one byte of the sheet; fold geometry at `style.css:210-231`).

Status verbs per M-25: dispositions here are **ADJUDICATED**, not VERIFIED — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets from the census lane taxonomy (`lane-frontend.md` §10, as fixed in the kf-AnimationControlsGroup record): **KF.W1-DEP** · **KF.W2-TABS** · **KF.W3-SHIM** · **KF.W4-PROSE** · **KF.W5-PARTIALS** · **KF.W6-TIMELINE** · **KF.W7-TOKENS**. Everything the census does not own is **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring. The toast-swap verdict itself is **census-owned** (shadow row S-9, minted by this corpus and already routed to the S-census by the kf-AnimationControlsGroup adjudication).

## Ruled disagreements (the seat's actual work)

| id | RR-D (design-and-usage) | RR-L (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-9** | CONFIRMED at INFO ("idiom note, correctly graded") | KILLED by compilation (static hoisting; no per-render allocation exists) | **KILLED — RR-L wins; re-executed by this seat** | My own `compileScript({inlineTemplate:true})` run: the entire `<svg>` constant-folds into one cached `_createStaticVNode("<svg width=\"0\" height=\"0\" class=\"absolute\">…<stop offset=\"0%\" style=\"stop-color:var(--rainbow-red);\"></stop>…", 1)`. The compiler already emits the exact static `style` attribute C-9 prescribes; the claimed `:style`-vs-static contrast ("allocates six objects per render") does not exist — the two forms compile to the same bytes. An INFO whose mechanism is false invites the cleanup it warns against; struck. The kill STRENGTHENS superlative S-L1: the portable form costs nothing. |
| **D-4 / C-5 mechanism** | KILLED D-4's rendering ("ButtonFace UA chrome, system font ~13px") via Preflight; refiled the corrected mechanism as **M-3** | CONFIRMED C-5/D-4 at MAJOR, **re-asserting** "renders as a UA-default `<button>`" | **RR-D's kill adopted; RR-L's re-assertion struck; the row survives as M-3 at MAJOR** | My grep of the shipped bundle: Tailwind v4 Preflight is present verbatim — `button,…{font:inherit;…color:inherit;background-color:#0000;border-radius:0}` plus the universal `border:0;margin:0;padding:0` reset. Preflight strips every specific of D-4's described rendering: fill is transparent (not ButtonFace), face/size/colour inherit from the toast's own `text-body`/`text-background`. The corrected defect is **worse**: the demo's only error-recovery control renders as undifferentiated body text on its own full-width `grid-cols-1` row (sonner's `[data-button]` rules all gated `[data-styled='true']`, :153+; `actionButton: ''`; live at `useKeyframeOps.ts:33-37`, reached from :97/:113/:159) — no colour, weight, boundary, or inset cue that it is actionable. C-5's substance ("styling from neither side") survives inside M-3; its "bare UA `<button>`" phrasing does not. |
| **D-9 grade + attribution** | CONFIRMED at MAJOR ("three divergent rainbow enumerations"), plus an extra: "the demo's two authorities disagree on the family's cardinality" | KILLED the design-law attribution half; carried L-11's MINOR drift framing | **RR-L wins on both counts; RR-D's cardinality extra also demoted** | My reads: `DESIGN.md:45-49` pins seven members (red orange yellow green **cyan** blue violet) and forbids *inventing* another hue; `design-idioms.css:15-21` declares ALL SEVEN as custom properties (cyan at :21). `DemoGlobalChrome.vue:17-22` consumes six members OF the pinned family and invents nothing — omitting cyan is not prohibited. The invented hue (indigo) enters via glass-ui's `rainbow-vivid` consumed at `RibbonBar.vue:50` — a different file, a different owner. And RR-D's "cardinality disagreement" is a prose nit, not a set disagreement: "The rainbow six-colour family — … (+ the cyan mid-stop)" (:12-13) describes the same seven-member set DESIGN.md pins. Survivor: **D-9/L-11 as drift, MINOR** — three non-agreeing enumerations (this file's 6-stop 45°, `.progress-bar`'s 7-stop no-orange cyan-mid red-wrap at :131-144, glass's 7-stop indigo-bearing horizontal), sharpest at one control's two states (`RibbonBar.vue:50` vs :59). |
| **M-5 vs the id-keyed superlative** | Filed as MISSED defect: "the persistent-diagnostic idiom cannot work as designed… the paired `toast.dismiss` is then a no-op" | Filed the same lines as a NEW superlative: "the id-keyed self-healing diagnostic toast" (the `alreadyExists` update branch) | **BOTH halves adopted; RR-D's mechanism RESCOPED by this seat's own timer derivation** | My read of `lib/index.js:611-634`: the Toast's timer is a `watchEffect` reading `props.toast.*`; a republish with the same id maps the Observer's array to a NEW toast object (`create`/`alreadyExists`, :31-53), the effect re-runs, `onInvalidate` clears the old timeout, `startTimer()` restarts at full `remainingTime` (never decremented outside pause states). So while the user actively produces invalid input the diagnostic REFRESHES — "cannot work as designed" is false — and `toast.dismiss` (:205) is live whenever the repair lands within the window. The true defect is the **idle boundary**: no `duration` is set (`KeyframesEditor.vue:189-202`), so `TOAST_LIFETIME = 4e3` (:298) kills the diagnostic 4s after the LAST invalid event while the field is still invalid — the one toast that most needs `Number.POSITIVE_INFINITY` (documented verbatim in glass-ui's `use-toast.d.ts`) got the shortest life in the codebase (every other error site asks 10 000 ms — my grep: `KeyframesStringControls.vue:107/:148/:157/:164`, `useKeyframeOps.ts:35`). **M-5 banked MINOR as rescoped; the superlative stands WITH the caveat attached.** |
| **D-6/L-9 severity** | MAJOR (contract "becomes unfalsifiable"; token-bypass offsets) | MINOR (breach is latent until a cure lands; `--z-max: 9999` is glass-ui's, not the demo contract's — demo top is `--z-modal: 140`) | **MINOR — RR-L's grade; RR-D's repair-constraint preserved** | Today the toaster has NO z-index at all (D-1) — the 999999999 breach and the 24px/16px offset literals (`lib/index.js:296-297`) go live only at repair time. The demo's eight-rung ladder (`style.css:19-40`) enumerates no toast rung; glass-ui ships `--z-toast: 160` bridged and unused (`grep z-toast demo/` → 0). Ruled a contract-gap at MINOR with a binding spec obligation: any cure lands on the `z-toast` rung and derives its offset from the `--dock-band-reserve` family — never sonner's literals. RR-D's arithmetic nit stands corrected by RR-D itself: 999999999/140 = 7 142 857, not 7 143 571. |
| **M-1 (RR-D solo find) — the unlayered-import hazard** | BANKED MAJOR: the corpus's own prescribed cure (`import "vue-sonner/style.css"`) lands UNLAYERED and outranks the demo's whole layer stack | (not found) | **CONFIRMED MAJOR — re-derived at both files by this seat** | (a) `lib/index.css:385-399`: the `@media (max-width: 600px)` block's `[data-sonner-toaster] [data-sonner-toast]{left:0;right:0;width:calc(100% - var(--mobile-offset-left)*2)}` is NOT among the 18 `data-styled='true']` gates (my grep) — `unstyled:true` does not disable it. (b) Shipped bundle: `@layer utilities{` opens at byte 182187 and `.w-64{` sits at 191676 inside it. Per css-cascade-5, unlayered normal declarations beat every layered one — so the one-line fix all three axes prescribe as step 1 silently kills the demo's authored toast width on mobile and seats `z-index:999999999` above the contract by cascade ORIGIN, unreachable by any layered demo rung. The honest interim is `@import "vue-sonner/style.css" layer(vendor);` + a demo-side `z-toast` override — and the demo already owns this exact discipline (`design-idioms.css:9-11` declares `--rainbow-*` outside `@layer` with the rationale inline). |
| **M-L1 (RR-L solo find) — the per-toast `visibilitychange` leak** | (not found) | BANKED MINOR: `useIsDocumentHidden` leaks a document listener per toast, permanently | **CONFIRMED MINOR — re-read at the file by this seat** | `lib/index.js:306-316` verbatim: `watchEffect` RETURNS its cleanup (Vue discards a watchEffect return — cleanup must ride the `onCleanup` argument; the same file uses the correct `(onInvalidate)` idiom at four other sites) AND the discarded cleanup removes from `window` what was added to `document` — a double failure. Called at :493 inside the Toast component (:412) — **per toast**, so all 23 publishing sites append a permanent listener + retained closure, and the `:key="superKey"` remount cannot reclaim them. Live today (the `<li>`s mount; they are merely clipped). A fourth independent argument for the S-9 swap that no original axis had. |

## Adjudicated defect roster

Ids keep their axis names for life; net-new re-reader finds are banked M-1..M-7 (RR-D) and M-L1..M-L5 (RR-L); M-2 was found independently by both. Line anchors are keyframes HEAD `8281638c`; re-resolve before executing any cure.

### BLOCKER

- **D-1 / L-1 / C-1 — ADJUDICATED BLOCKER · the vue-sonner stylesheet is imported nowhere; every toast is structurally unreachable.**
  Proof (both re-readers independently; decisive legs re-verified by this seat): `DemoGlobalChrome.vue:45-49` is the whole `<script setup>` — no CSS import, and the sheet is a separate subpath export (`"./style.css": "./lib/index.css"`); `lib/index.js` pulls zero CSS and injects none; the positioning lives only in the unimported sheet (`position:fixed` at **:21**, `z-index:999999999` at **:43**, per-toast `position:absolute;opacity:0` at **:67-79** — all ungated by `data-styled`, my reads); the shipped `dist/gh-pages` carries the sonner runtime in two JS chunks and zero sonner bytes across all nine CSS assets; fold geometry `style.css:210-231` (`html,body{overflow:hidden;…min-height:100dvh}`, `height:100dvh` ≥1024px) with the Toaster teleported to `<html>` after the `<body>` box. Blast radius: **23 publishing calls + 1 `toast.dismiss` across 8 modules, all typed** (my grep: 24 total; the D axis's "24 publishing / all enumerated" tally is killed below). C-1's typography leg ("escapes the body baseline → UA face") is REFUTED — every text node carries explicit glass-ui classes and Preflight ships `font:inherit` — the BLOCKER stands on positioning alone. D-1's own line citations are fabricated (kill #7); the L/C citations are sound.
  Terminal disposition: **NO-WAVE-OWNER** (SS-1/SS-2 spec input), cured structurally by the census **S-9** swap; any interim import is bound by **M-1** (`layer(vendor)` + `z-toast` override, never bare). The decisive live check is SS-13 #1 — the cheapest probe on the page, and it gates the observability of D-2/D-3/D-7/M-3/D-11/D-13.

### MAJOR

- **D-5 / L-3 / C-2 — MAJOR · FOLD → census S-9 (identity guard; the swap verdict is census-owned).** glass-ui 7.0.0 — installed, its CSS already in the demo's cascade via `style.css:3` — ships the complete `./toast` family. Re-verified by this seat: `index.d.ts` exports all six components + `toast()`/`useToast()`; `use-toast.d.ts` declares `tone`/`surface`/`duration` with `Number.POSITIVE_INFINITY` semantics and `toast()` returns `{id, dismiss, update}` — the dismiss-by-handle falsifier fails. Cost honesty preserved: the toast chunk contains zero `Teleport` — mount placement stays the consumer's problem (pairs with the C-3 re-home). **Riders this record adds to the S-9 row**: (i) sequencing on census **F-1/KF.W1-DEP**; (ii) the swap retires M-L1's leak, C-4's guard module, and D-2/D-3/D-7/M-3/D-11/D-13/D-14 in one motion; (iii) the S-9 row contradicts `lane-frontend.md:398` ("Bespoke, no glass counterpart") — the census tally row must be corrected when S-9 is booked.
- **L-8 (F-1 exposure) — FOLD → census F-1 (KF.W1-DEP) — identity guard, not re-booked.** My greps: `glass-ui` = 0 in `package.json`, 0 in `package-lock.json`; installed 7.0.0. Component rider: all four toast utility classes (`text-body`/`text-small`/`bg-foreground`/`text-background`) resolve SOLELY through the phantom (glass `@utility` defs + `@theme inline` bridges; the demo declares none). **New evidence rider from RR-L, verified by this seat**: `a59d3a22 fix(dependencies): consume Glass UI 6 from one registry core` exists in history yet the declaration is still absent — F-1 is not merely open, it survived a commit that claimed to address the dependency.
- **D-8 / L-2 / C-3 — MAJOR · FOLD → the kf-AnimationControlsGroup adjudication's C-9 row (the banked mis-home identity), with a severity-raising evidence rider.** One identity: the "DOCUMENT-LEVEL singletons" sit inside `EditorShell.vue:75` `:key="superKey"` (seven distinct superKeys; the state machine documents the boundary) and are destroyed per scene switch — booked at kf-AnimationControlsGroup as C-9 (MINOR, "lift DemoGlobalChrome above the `:key` boundary"). **This corpus's riders upgrade the booked consequence to MAJOR**: vue-sonner's Observer is subscribe-only with no replay (`lib/index.js:14-27`, publish-then-append), and `useShareState.ts:79-86` (my read) calls `onSceneRestore(...)` BEFORE publishing `toast.success("State restored!")` — the primary share-restore flow's only success signal is dropped by construction on every cross-scene restore. The header's singleton claim also violates DESIGN.md §6's prose-authority rule. Survives D-1's fix; masked by it today (C-3's mutual-masking observation is the likeliest reason neither was caught). Cross-booked, never a second row.
- **D-2 — MAJOR · four toast types paint identical pixels.** `unstyled:true`, no `richColors`, one `toast` class, no per-type `classes` key (the hooks exist: `lib/index.js:652` binds `classes?.[toastType]`); icons render via the Toaster's fallback slots, 20×20 `currentColor` — all four the same `text-background`. Against the demo's own law (`DESIGN.md:43-44`: "Red is destructive only… marks delete, clear, error"). glass-ui's toast implements the tone axis the demo forfeits. Tally corrected (kill #8): 23 publishing sites, all typed. Disposition: **NO-WAVE-OWNER**; dissolves under S-9.
- **D-3 / L-4 — MAJOR · the type icon is a full-width grid row above the text, and `gap-1` separates the wrong pair.** `[data-icon]` and `[data-content]` are siblings under the `<li>` (render :697-712); the demo's `grid grid-cols-1 gap-1` makes them rows; the vendor row layout (`:80-91`, flex + gap 6px — my read) and the title/description 2px rhythm (`:150-153`) are both `data-styled`-gated off; `classes.content`/`icon`/`loader` unsupplied. Survives the stylesheet fix by design. Descriptions live at 6+ sites. Disposition: **NO-WAVE-OWNER**; dissolves under S-9; pixel outcome → SS-13 #2.
- **M-3 (RR-D net-new; replaces the killed D-4) — MAJOR · the Retry affordance renders as undifferentiated body text.** Full chain re-verified by this seat (ruling table): Preflight strips UA chrome (transparent fill, radius 0, padding 0, border 0, font/colour inherit); sonner's `[data-button]` rules are gated off; `actionButton: ''`; the button is a direct grid child → its own full-width row beneath the message with NO actionability cue of any kind. Live on the demo's sole error-recovery path (`useKeyframeOps.ts:33-37`). WCAG 3.2.4/2.5.8 are the floors. Disposition: **NO-WAVE-OWNER**; S-9's styled `ToastAction` cures.
- **D-7 — MAJOR · multi-sentence compiler diagnostics auto-dismiss with no dismiss control.** `closeButton` never enabled (prop default false; render branch never taken; `classes.closeButton:''` styles nothing); `TOAST_LIFETIME = 4e3`; the alt+T hotkey only expands/focuses and Escape only collapses — no keyboard dismissal path exists (both re-readers, mechanism at :1099-1109). The 10 000 ms diagnostics are the product value by the codebase's own comment. Touch: no hover, no pause. Disposition: **NO-WAVE-OWNER**; S-9 ships `ToastClose` + `POSITIVE_INFINITY`.
- **C-4 — MAJOR · `toastGuard.ts` exists solely to reach vue-sonner's private DOM.** 28-line module, exemplary in HOW it holds the wrong dependency (see superlatives); two dialog consumers verified. **Rider M-L4 (RR-L net-new, verified by this seat)**: the doctrine has a THIRD citer — `useTabStripScroll.ts:29/:69` invoke "the `[data-sonner-toaster]` disposition, D.W3" as governing precedent for an unrelated vendor-DOM reach — so retiring the guard under S-9 must also re-home the cited authority, or the repair orphans prose elsewhere. Disposition: **NO-WAVE-OWNER**, retires under S-9 with the M-L4 sweep.
- **M-1 (RR-D net-new) — MAJOR · the corpus's own prescribed cure is structurally unsound unlayered** (ruling table; both legs re-derived by this seat). Any interim stylesheet import MUST be `@import "vue-sonner/style.css" layer(vendor);` with a declared `z-toast` rung and a demo-side width/offset override — or skip straight to S-9. Disposition: **NO-WAVE-OWNER** (spec constraint on the D-1 cure; the check that proves it is SS-13 #9).

### MINOR

- **D-6 / L-9 — MINOR (ruled) · the toast surface is the one rung outside the demo's declared z-contract, and it binds neither rung nor offset.** Spec obligation recorded: cure lands on `z-toast` (160, already bridged, `grep demo/` → 0 consumers) and derives offsets from the `--dock-band-reserve` family. Occlusion geometry → SS-13 #6. NO-WAVE-OWNER.
- **D-9 / L-11 — MINOR (rescoped per ruling) · rainbow drift: three non-agreeing enumerations; one control paints two of them across its two states.** The design-law violation lands on `rainbow-vivid`'s indigo at `RibbonBar.vue:50`, not on this file's six in-family stops. NO-WAVE-OWNER (design reconciliation; decide indigo at the consumer, or pin one ramp).
- **M-2 (BOTH re-readers independently) — MINOR · `#rainbow-gradient` declares no `gradientUnits` → `objectBoundingBox` default → the paint resolves per referencing element.** My verification: zero `gradientUnits` in the target; `paintbrush.mjs` declares exactly **3** `<path>` children inheriting the stroke — three independent full spectra, the worst against a ~10.7×2.9 near-flat bbox, at 16px rendered size. This is the statically-decidable mechanism behind D-15's live-only symptom; D-15's single-box band arithmetic is not what the renderer does. Cure options recorded (userSpaceOnUse needs an explicit box; or 2–3 stops; or drop the gradient stroke). Perceptual verdict → SS-13 #5. NO-WAVE-OWNER.
- **D-10 — MINOR · `max-w-[90vw]` raw viewport literal + `w-64`/`lg:w-80` bypassing `--dock-panel-width`.** Arithmetic honest (binds below ~284px). NO-WAVE-OWNER (token hygiene; dies under S-9).
- **D-11(a) — MINOR · `px-4 py-3` inverts the overlay padding proportion** (0.75 vs glass toast's own 1.272 ≈ √φ — the φ pair verified in the toast chunk itself by RR-L). (b) as rescoped by kill #6: exactly two of six `classes` keys dead. NO-WAVE-OWNER; dies under S-9.
- **D-12 / L-13 — MINOR · the 0×0 `<svg>` is the demo's only decorative graphic without `aria-hidden="true"`** (RR-D's tightened census: 3 of the demo's 4 inline SVGs hidden; this is the sole exception). One attribute. AX-tree residue → SS-13 #7. NO-WAVE-OWNER.
- **D-13 — MINOR · no boundary under forced-colors (demo grep: 0; glass's forced-colors block targets state attributes a sonner `<li>` never carries) + a theme-blind black `shadow-lg`.** The shadow leg is M-4's tripwire. NO-WAVE-OWNER; S-9's bordered, tone-aware surface cures.
- **M-4 (RR-D net-new) — MINOR · FOLD → KF.W7-TOKENS (the census §6.3 flat-namespace hazard class, same identity family as the banked D-12/`--rail-track` row).** Verified by this seat in the shipped bundle: THREE definitions of the `shadow-lg` name coexist — Tailwind's theme default, glass-ui's `:root{--shadow-lg:0 4px 20px var(--shadow-color)}` (dist/styles/tokens/shadow.css, theme-aware), and the emitted `.shadow-lg` utility which inlined Tailwind's value at build time and reads NEITHER custom property at runtime; glass bridges only the five `--shadow-glass-*` tokens into `@theme inline`. The demo's sole `shadow-lg` (my grep: exactly this file) is the sole tripwire. Fold carries the component instance; the namespace audit owns the class.
- **M-5 (RR-D net-new, RESCOPED per ruling) — MINOR · the id-keyed invalid-offset diagnostics carry no `duration` and self-destruct 4s after the last invalid event while the field is still invalid.** Mechanism corrected by this seat's timer derivation (republish restarts the countdown; the defect is the idle boundary, not total dysfunction). One-token cure: `duration: Number.POSITIVE_INFINITY` on the two sites + the existing dismiss. NO-WAVE-OWNER.
- **D-14 / L-10 / C-6 — MINOR · `theme="system"` resolves from `matchMedia` while the app's authority is the localStorage-backed `.dark` class** (deliberate: the prop default is `"light"`, so `"system"` was typed on purpose). Inert today on two counts; a latent trap the day the sheet lands. Correct binding: the resolved app theme. NO-WAVE-OWNER; dies under S-9.
- **L-6 — MINOR · FOLD → the kf-AnimationControlsGroup adjudication's D-13 identity (dead `proof:*` gate citations; prose-truth hygiene) — cross-booked, not re-booked.** This seat's census is sharper than both readers': `proof:demo-no-oversize` survives at **16 hits across 13 files** (RR-L said 10; missed `useAnimationGroupActions.ts` and `useAnimationGroupPlayback.ts` among others). The header's SOLE stated reason for this file's existence cites the retired gate — which strengthens the C-3 re-home. A fix confined to this file leaves twelve files standing; the fold owns the sweep.
- **L-7 — MINOR · `to="html"` is load-bearing and undocumented.** Re-verified by this seat at both ends: reka's `DismissableLayer.js:82-84` sets `body.style.pointerEvents = "none"` for `disableOutsidePointerEvents` layers, `DialogContentModal.js:55` forwards it unconditionally (the demo's two dialogs ARE modal), and sonner's entire sheet contains exactly one `pointer-events` declaration (:298, `none`). A tidy-up to `body` passes every gate and click-kills toasts under the exact dialogs `toastGuard.ts` serves. One sentence of rationale is the cure; the fact must survive into the S-9 mount decision (glass's Toaster does not self-Teleport). NO-WAVE-OWNER.
- **C-7 — MINOR · zero props/emits, hardcoded document-global `id`, uniqueness held by an unasserted layout accident** (one `<EditorShell>` mount; multi-root fragment can never take fallthrough attrs). NO-WAVE-OWNER; the App-scope re-home makes it structural.
- **M-L1 (RR-L net-new) — MINOR · vue-sonner leaks a `document` `visibilitychange` listener per toast, permanently** (ruling table — double-failed cleanup verified verbatim; per-Toast call site; unreclaimed across `:key` remounts). The fourth independent argument for S-9; kills the cheap-repair path's sufficiency. NO-WAVE-OWNER (vendor defect; recorded, not patched locally).
- **M-L2 (RR-L net-new) — MINOR · `transport/components/` is a single-file generic bucket** (my `ls`: the only generic-named subdirectory in a taxonomy of owner-named ones, sole occupant = the component whose own header says it doesn't belong here). The C-3 re-home must DELETE the directory, not just move the file. NO-WAVE-OWNER.
- **M-L3 (RR-L net-new) — MINOR · the `transport/` and `instrument/` barrels — including three `defineAsyncComponent` wrappers with a load-bearing-sounding hazard rationale (T.F5) — have zero consumers repo-wide** (my grep: 0; the sole live import is the direct SFC path at `EditorShell.vue:120`). Dead exports wearing a rationale comment are the shape that survives audits; also the only guard on S-L2's duplicate-mount hazard. NO-WAVE-OWNER (hygiene; cross-ref KF.W3-SHIM's no-backwards-compat law).
- **M-L5 (RR-L net-new) — MINOR · the component bundles two unrelated singletons whose only shared property is a negative predicate, and all three axes' repair orders propagate the coincidence.** The paint server's sole consumer is a sibling in the SAME keyed subtree; the Toaster must OUTLIVE that subtree and is published to from App-level chains. The right decomposition is two moves (Toaster → App scope; `<defs>` → the ribbon's subtree, or deleted with the sonner half under S-9), not one tag. Binding spec input for the C-3 re-home. NO-WAVE-OWNER.

### INFO

- **L-12 / C-8 — INFO · `import { Teleport } from "vue"` is dead** — proven by this seat's own compile: the emitted render uses the compiler's auto-injected `_Teleport` while the author's import survives in the output unreferenced. NO-WAVE-OWNER (rides any touch of the file).
- **L-14 — INFO · zero test/script coverage of the sole toast mount; no `e2e/` directory exists.** The structural reason D-1 survived the `6487c7f8` bulk bump. Spec obligation: the S-9 wave lands with a mounted assertion at the component seam (the kf-AnimationControlsGroup L-1 lesson: gates at the seam, not through shims). NO-WAVE-OWNER.
- **C-10 — INFO · zero value.js/engine edges; R1 reachability CLEAN** (imports are `vue` + `vue-sonner` only; the parent's `@mkbabb/value.js/math` edge is at `AnimationControlsGroup.vue:125` — the live line, settling the readers' :131/:125 drift — and `/math` is not the parse surface). Identity guard: respects value.js's banked R1 row; recorded so parser waves skip this file. The space-separated-`hsl()` seam marker carries. No wave.
- **M-6 (RR-D net-new) — INFO · the de-facto per-type signal is hand-placed emoji in 4 of ~27 message strings** (my grep: `KeyframesStringControls.vue:101/:105/:139/:162` — 🔧 on two of six error surfaces, 🎉 on two of nine successes), announced literally by AT mid-sentence under `aria-live="polite"`. Given D-2, this accidental register is the strongest per-type cue the surface offers. NO-WAVE-OWNER; call-site sweep rides the S-9 migration.
- **M-7 (RR-D net-new) — INFO · `rounded-xl` is off the `rounded-panel` register** (my counts: 2 vs 2; glass's own Toast uses `rounded-panel`) — the toast diverges from the system on exactly the two family-membership properties (radius + padding proportion, D-11a) while conforming on colour and typography. NO-WAVE-OWNER; dies under S-9.
- **M-L4 (RR-L net-new) — INFO · the toastGuard doctrine's third citer** — carried as the rider on C-4 above; recorded separately so the sweep is greppable.

## Killed-claims register

1. **D-4's rendering mechanism ("UA-default `<button>`, light ButtonFace chrome, system font ~13px") — KILLED (RR-D's kill, re-verified by this seat in the shipped bundle); RR-L's re-assertion in its C-5 confirm cell is STRUCK.** Preflight supplies transparent fill, radius 0, padding/border 0, font/colour inherit — every specific of the described rendering is wrong, including D-4's own falsifier ("Preflight supplies no fill" — it supplies `#0000`). The underlying defect survives, corrected and worse, as **M-3**.
2. **L-5 ("three inert class placeholders… no call site uses `action`") — KILLED on its own falsifier, both re-readers concurring; re-verified by this seat.** `useKeyframeOps.ts:33-37` passes `action: { label: "Retry", onClick: retry }`, reachable from three sites. `actionButton` is live-and-broken, not dead; two of three keys are dead. The axis cited `useKeyframeOps.ts:33` in its own L-1 blast list without reading the options object.
3. **C-9 (the `:style`-vs-static contrast) — KILLED by compilation (RR-L's kill, re-executed by this seat); RR-D's confirm-at-INFO is struck.** The compiler constant-folds all six stops into one cached `createStaticVNode` carrying the exact static `style` string the claim prescribes. No six objects, no per-render allocation, no contrast. Strengthens S-L1.
4. **D-9's design-law attribution — KILLED (RR-L's kill, verified by this seat's reads of DESIGN.md:45-49 and design-idioms.css:15-21).** The file consumes six members OF the pinned seven-member family and invents nothing; the invented hue (indigo) enters through `rainbow-vivid` at `RibbonBar.vue:50`. Survivor carried as D-9/L-11 drift at MINOR. **RR-D's "two authorities disagree on cardinality" extra is demoted with it**: the declared set is the same seven; "six-colour family (+ the cyan mid-stop)" is prose framing, not a membership disagreement.
5. **C·S+3's "a `to="body"` teleport would have been equally correct" — KILLED (RR-D's kill, re-verified by this seat at DismissableLayer.js:82-84 + DialogContentModal.js:55 + index.css:298).** A body-teleported toaster is pointer-dead under both modal dialogs — the exact case `toastGuard.ts` exists to serve. `to="html"` is load-bearing for interaction, not merely tied for theme. The theme half of the superlative stands.
6. **D-11(b)'s "three of six `classes` keys are dead" — KILLED as tallied (RR-L; RR-D concurring in substance).** Exactly two are dead (`cancelButton`: zero `cancel:` call sites; `closeButton`: gated behind a never-set prop); the third is live-and-unstyled — which D's own body states one sentence later, contradicting its heading. Materially worse than the "completeness ritual" reading.
7. **D-1/D-3's CSS provenance (`index.css:1847`, `:~1900`, `:2201`) — KILLED as citations; the claims survive intact.** The file is **643 lines** (this seat's `wc -l`). The rules are real at :21, :43, :67-79, :80-91 (this seat's reads). The fabrication is localised to the D axis's two claims — the L/C citations for the same rules check out — but those line numbers must never be copied into a repair spec.
8. **D-2's tally ("all 24 sites are typed" + its enumeration presented as the 24) — KILLED as arithmetic.** One of the 24 grep hits is `toast.dismiss` (`KeyframesEditor.vue:205`, publishes nothing) and the enumeration silently omits the two `toast.error` calls at :189/:198 — precisely the pair carrying M-5. Correct: 23 publishing calls across 8 modules, all typed, + 1 dismiss. The substantive claim (zero default toasts) survives.
9. **RR-D's M-5 mechanism ("cannot work as designed… the paired dismiss is then a no-op") — KILLED as overstated by this seat's own timer derivation** (ruling table): the republish restarts the countdown, so the idiom works during active editing and the dismiss is live within the window. The rescoped defect (the idle-boundary 4s self-destruct) is banked.
10. **Citation-drift aggregate (recorded so the fix-wave chases nothing):** C-10's `AnimationControlsGroup.vue:131` → live **:125** (this seat's grep; RR-D's confirm repeated the stale :129-131 cell, RR-L corrected it); L-1's `index.css:22-24` → **:20-21**; RR-D's `:137-140` for the copyText 🎉 → **:139**; D's 7 143 571× → **7 142 857×**. Every other anchor this seat re-opened — the mount chain, scenes.ts septet, useShareState order, RibbonBar pair, toastGuard consumers, KeyframesStringControls durations, sonner internals — resolved exactly.

## Superlatives (provenance preserved; L-18 runs both ways)

- **S-1 / S-L3 (D + L, complementary halves) · the 0×0 `<svg class="absolute">` is right twice on one line**: zero intrinsic dimensions (not `display:none`) is the paint-server technique that survives every engine, and `absolute` keeps the multi-root fragment's second root from becoming a grid item in `<main>`'s auto-row grid (mount chain re-verified: `AnimationControlsGroup.vue:118` after `</TooltipProvider>`, direct child of `EditorShell.vue:74`'s grid).
- **S-3 / S+1 (D + C) · the paint-server rationale at :9-13 is load-bearing prose** — real mechanism, real (sole, grep-verified) consumer, real token home — and the praise is correctly scoped AWAY from the adjacent, false `:2-7` singleton clause. The scoping discipline is itself the superlative.
- **S-2 / S+2 (D + C) · the inverted token pair is AAA in both arms with zero literals** (~16.9:1 / 15.9:1 from shipped values; provenance caveat: D's quoted hsl literals do not appear in the bundle; the arithmetic lands within 1% regardless). Every class in the block resolves through glass-ui — zero ad-hoc hex, zero invented tokens — which is what makes S-9 a component swap, not a restyle. C checked instead of assuming ("this is the false defect I did not file") — the posture is the superlative.
- **S-L1 (L; STRENGTHENED by this seat's compile) · the `:style="{ stopColor: 'var(…)' }"` form is portable for the right reason AND free**: the compiler emits the static attribute form, hoisted and cached — the correct idiom at zero cost. (C-9's kill is this superlative's proof.)
- **S-L2 (L) · genuine singleton discipline that names its own expiry** — exactly one `<Toaster>`, one `#rainbow-gradient`; the async-barrel hazard that could have doubled both is dead (this seat: zero barrel consumers — a fact simultaneously banked as defect M-L3; the tension is recorded, not ruled away: the discipline is real, its only guard is disuse).
- **toastGuard.ts (named by C-4; seconded by both re-readers and this seat) · the model for holding a vendor coupling you cannot yet retire** — attribute named, semver pinned, reach centralised, exit condition stated, one `closest()` of substance. The correct way to hold a wrong dependency — now with a third citer (M-L4) to sweep when it retires.
- **design-idioms.css:9-11 (RR-D, demo-side) · the `--rainbow-*` family is declared outside `@layer` deliberately, with the rationale inline** — the mechanically correct way to outrank a layered vendor token, explained where it is done, and the exact discipline M-1 proves the vue-sonner repair needs. The demo already knows how; the toast surface is where it wasn't applied.
- **RR-L NEW (kept WITH the M-5 caveat) · the id-keyed self-healing diagnostic** (`KeyframesEditor.vue:180/:190/:199/:205`): matching ids take sonner's `alreadyExists` update branch — refresh, never stack — and the valid path dismisses by handle. A correct, non-obvious use of the id contract, and the strongest single argument that this surface was engineered rather than pasted — which is what makes the missing stylesheet the more striking omission. The one missing token is `duration` (M-5).
- **METHOD · D's K-1 self-kill (the "title is not bold" claim, killed by measured byte offsets) was reproduced by RR-D exactly** — two claims on that page died by their own falsifiers, as filed. The counter-lesson stands beside it: D's fabricated line numbers (kill #7) show a falsifier *cited* is not a falsifier *resolved*.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs, ranked)

1. **THE decisive probe** — fire any toast (Format CSS, `CSSCodeEditor.vue:186`) and read `getComputedStyle(document.querySelector('[data-sonner-toaster]')).position` — expected `static`; anything else kills the BLOCKER. Gates the observability of D-2/D-3/D-7/M-3/D-11/D-13.
2. Post-stylesheet-fix rendering: icon-above-title (predicted) vs beside (D-3/L-4 — gating and DOM order source-proven; pixels live-only).
3. Cross-scene share-restore toast survival (`?state=` URL with a differing `activeScene`) — the D-8/L-2/C-3 witness, masked until #1 lands.
4. Toast clickability over an open modal — the one-click empirical confirmation of L-7 (already settled from reka source by this seat).
5. Rainbow legibility at `icon-sm` — M-2's three-independent-spectra geometry is proven; whether it reads as rainbow or mud needs a magnified render.
6. Bottom-right toaster vs the transport dock band (`--dock-band-reserve`) occlusion at ≥1 breakpoint (D-6's offset clause).
7. AX-tree dump: is the 0×0 `<defs>`-only `<svg>` pruned in all three engines? (drops D-12 to INFO; does not restore the convention).
8. Whether the toaster's `aria-live` region, teleported OUTSIDE `<body>` (outside the HTML content model), is exposed and announced at all by NVDA/VoiceOver — if dropped, the demo has no error announcement channel whatsoever (the only other live region is `CopyButton.vue:15`). Routed, not asserted; do not assume either way.
9. **M-1's proof-of-need**: after any bare `vue-sonner/style.css` import, check at ≤600px whether toast width follows the demo's `w-64` or sonner's `calc(100% - var(--mobile-offset-left)*2)` — predicted sonner's, because unlayered beats `@layer utilities`.
10. The `<defs>` mount-order forward-reference one-frame check on `:key` remounts (RR-L's routed non-defect; folds into whatever SS-13 pass looks at the ribbon).

## DISSENT — preserved, not ruled away

1. **Neither re-reader was uniformly the stronger seat.** RR-D's Preflight kill of its own axis's D-4 (and the M-1 cascade find) were the corpus's two most valuable acts; but RR-D endorsed C-9 without compiling and inflated the rainbow-cardinality nit. RR-L's compile discipline killed C-9 and found the leak; but RR-L re-asserted the killed "UA-default button" rendering inside its C-5 confirm — a citation-inheritance-adjacent event (the killed cell's language survived one seat's kill into the other's confirm), recorded per the ColorSpaceSelector precedent as evidence about the fleet, not the tree.
2. **D-6 severity (ruled MINOR)** — RR-D's position preserved: the day ANY cure lands, an unranked 999999999 surface makes the z-contract's premise unfalsifiable, and by M-1 it does so by cascade origin, unreachable even by a demo-authored rung unless that rung is also unlayered. If a cure lands without the `z-toast` obligation, this row revives at MAJOR.
3. **M-5 vs the superlative** — both preserved by construction: the idiom is praised, its missing token is filed. RR-D's overstatement and RR-L's silence on duration were symmetric half-blindnesses on the same six lines.
4. **The severity of the mis-home fold** — this record raises the kf-AnimationControlsGroup C-9 booking from MINOR to MAJOR on the share-restore evidence. If the spec authors rule that a cross-booked identity keeps its original grade, the rider still binds: the re-home is a correctness fix, not colocation hygiene (contradicting five prior tranche lanes, as the L axis's reconciliation already argued).
5. **HEAD note**: all anchors re-verified at keyframes `8281638c`; drift from the challenge corpus at sites this seat re-opened was confined to the aggregate in kill #10.

## Closing verdict

**ADJUDICATED at HEAD `8281638c`: 33 confirmed rows (1 BLOCKER · 9 MAJOR · 17 MINOR · 6 INFO) — the blocker is the missing vue-sonner stylesheet that muted the demo's entire 23-site diagnostic channel in source and in the shipped artifact; 10 claims killed with executed refutations (Preflight kill re-verified in the bundle, C-9 killed by this seat's own compile, D-9's law-attribution withdrawn, two fabricated-citation registers pinned); 12 net-new re-reader finds banked, two of them MAJOR (the unlayered-import cascade hazard that invalidates the corpus's own step-1 cure, and the Retry-as-body-text correction); identity guards held — census F-1 and S-9, the kf-AnimationControlsGroup C-9 mis-home and D-13 dead-gate identities, and value.js's R1 row all FOLDED by reference, zero re-bookings, with this record's sharper censuses (16-hit dead-gate sweep, 23+1 call-site tally) attached as riders; routings are M-25 agglomeration input for the forming X·KF specs — KF.W1-DEP and KF.W7-TOKENS own their folds, the swap verdict is census-S-9-owned, everything behavioral is NO-WAVE-OWNER for SS-1/SS-2, and 10 residue rows ride to SS-13.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/transport/components/DemoGlobalChrome.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **DemoGlobalChrome.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**L-6's sweep is EXECUTED AS A ROSTER, per its own lock.** Banked: *"A fix confined to this file leaves twelve files standing; **the fold owns the sweep**."* The fold is `docs/tranches/X/keyframes/W0/GATE-ROSTER.md` §4.1, where all **16 hits across 13 files** are split per site with a named owner each. Re-measured: `demo/`-scoped **16 / 13**, reproducing the banked figure **exactly**; repo-wide **121 hits / 70 files, 57 of those under `docs/`** — the `docs/**` surface is **declared loudly out of scope**, never silently inherited.

**A 17th site exists under a wrap-inclusive predicate and is recorded, not merged into the banked figure** (E-3): `ChannelControls.vue:341` carries the name line-wrapped as `proof:demo-no-` / `oversize`, which the roster's own `-$` filter discards. GATE-ROSTER §2.2.

**The record's consequence travels**: this component's header cites the retired gate as the SOLE stated reason for the file's existence. That strengthens its C-3 re-home — which is not this wave's act.

**THE BYTE-OFFSET WITNESS IS SUPERSEDED — this record cites `dist/gh-pages/assets/index-CL_QYCiO.css` by name, and that coordinate is dead.** One `npm run gh-pages` ran at this seat (2026-09-17 13:37, exit 0, `built in 2.81s`). The artifact is now **`dist/gh-pages/assets/index-CBB2Hr7m.css`** — 571 192 B (was 571 142 B), mtime 2026-09-17 13:37 (was Jul 16 09:11), tree digest `bad6ea595fb59899e6589ad731b542d77eb19079a67d10548f6c07fc3c9564d2` over 54 files, per-entry hashes at `artefacts/W0/glass-citation-rederivation.txt` §5. Seven of the corpus's byte-offset receipts were re-derived in the fresh build and **all seven reproduce** (`data-allow-motion` 1 · `header-ribbon__actions` 1 · `btn-interactive` 0 · `--shadow-glass:` 0 · `skeleton-scan` 1 · `header-items-wrapper` 0 · `header-collapsed` 0 · `rainbow-vivid` 1). **Only the filename moved**; the findings stand, now on a dated and hashed artifact instead of a provisional one.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

