# AdminUsersPanel — adjudicated apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed, not inherited.
**Date**: 2026-07-27 · **HEAD**: `c654824e` · **Corpus**: 11 CHALLENGE-L accusations (L-1..L-11) against `demo/palettes/browser/admin/AdminUsersPanel.vue`.
**Workers adjudicated**: worker-F (Fable 5, `admin-users-panel-worker-f-adjudication`) · worker-O (Opus 5 [1m], `mt-au1-adminuserspanel-adjudication`).
**Method (L-14)**: both workers were attacked before either was adopted. Every disagreement was re-derived against the tree by this seat; the suspiciously-convergent agreed verdicts (L-2, L-3, L-5, L-6, L-8, L-10) were each independently re-measured rather than accepted. Arbiter's own instruments this session: full source reads of AdminUsersPanel.vue (391 lines, whole) / useAdminUsers.ts (whole) / usePalettePorts.ts:100-240 / AdminPane.vue:20-135 / useSlugMigration.ts (key lines) / ActionFeedback.vue (whole) / ColorPicker.vue:325-354 / viewSchema.ts:185-200 / usePaletteWiring.ts:140-155 / usePaneRouter.ts:155-175; eleven scoped counting instruments (each command recorded below with its output); one `npx vue-tsc -p tsconfig.demo.json --noEmit` run (exit 0 — the blindness measured, not assumed); `git ls-files` against every contested witness.
**Environment disclosure (L-12)**: no browser probe was run by this seat — every contested verdict decided statically; worker-O's live chromium measurements are cited only where my static re-derivation independently reached the same fact, except the G-3b/G-4a DOM discriminators which are adopted AS worker-O's cell (chromium 1.60.0 @1440×900) and never transferred to WebKit/Safari (I-20). Dev server :9000 is API-LESS — every data-backed arm (populated roster, live prune, expanded palettes) is UNVERIFIABLE-HERE and none of the eleven verdicts rests on one.

## Scoreboard

**7 CONFIRMED (L-2, L-3, L-5, L-6, L-8, L-10, L-11) · 4 RESCOPED (L-1, L-4, L-7, L-9) · 0 REFUTED · 0 UNVERIFIABLE-HERE at row level · 3 GLASS-OWNED riders (never rows).**
Blocker survivor: **L-5** — an unauthenticated stranger on `#/admin/users` is told as fact that the roster is empty (`· roster clear · / No users found.`) and handed operable Prune/Refresh controls, on a route with no auth meta. The L-1 BLOCKER grade is retired by both workers and by this seat's own re-derivation; L-1 survives as the MAJOR structural spine.

Arbiter measurement record (all this session, all re-runnable in a fresh checkout):

| fact | command (abbreviated) | measured |
|---|---|---|
| InstanceType refs in demo/ | `grep -rn --include='*.ts' 'InstanceType<typeof' demo/` | **3** (useAdminUsers.ts:27 · useSlugMigration.ts:30 · usePaletteWiring.ts:27) |
| defineExpose sites demo-wide | `grep -rn defineExpose demo/ --include='*.vue'` | **14**; ColorPicker.vue:331 = **12 members**, AdminUsersPanel.vue:389 = **5** |
| adminUsersPanelRef occurrences | `grep -rn adminUsersPanelRef demo/ \| wc -l` | **10** |
| adminPort members | hand-count usePalettePorts.ts:195-231 | **34** (14 user + 13 queue + 7 cross) |
| `pm.` executable uses in panel | read | **1** (:361; :233 is a comment) |
| ADMIN_PORT_KEY injectors | `grep -rln 'inject(ADMIN_PORT_KEY' demo/` | **5 files** |
| `<Button variant=` sites | python census over `<Button…>` opening tags | **51 in 22 files**; subject 5 (tags 21/32/58/112/122) |
| `variant`/`outline`/`ghost`/`inheritAttrs` in glass Button chunk | `grep -c … dist/button-Bu9F4uU6.js` | **0 / 0 / 0** |
| vue-tsc over the 51 dead props | `npx vue-tsc -p tsconfig.demo.json --noEmit` | **exit 0** (structurally blind) |
| demo/ui entries · barrel importers · both-paths | `ls demo/ui \| wc -l` · O's regex · comm | **19 · 48 · 24** |
| `.vue`-path imports in .ts · of them in useAdminUsers | grep | **25 · 0** |
| row-grammar homes in admin/ | `grep -rn 'items-center gap-3 px-3 py-2.5' …/admin/` | **5** (atom :11 + UsersPanel :80 + Skeleton :9 + Audit :62 + Flagged :50) |
| `if (!token)` in useAdminUsers.ts · demo/palettes | grep -c | **11 · 22** |
| SAFE_ACCENT_KEY injectors | `grep -rln 'inject(SAFE_ACCENT_KEY' demo/` | **6 files** |
| explicit `Transition` import · `<Transition` users | grep | **1 (:186) · 17** |
| `<Button>`-tag `h-<n>` sites | python census | **22 in 11 files**; subject 4 |
| tracked PNGs under megatranche | `git ls-files docs/tranches/V/megatranche/ \| grep -c png` | **0** (`.gitignore:34 *.png`, `!demo/**/*.png` only) |
| challenge-D refs in palettes harvest · DEFECT-LEDGER | grep -c | **0 · 0** (the 33KB file EXISTS at `audit/components/AdminUsersPanel/challenge-D-design.md`) |
| confirm engine homes | `grep -rln confirmOpen demo/ --include='*.vue'` | **1** (the subject) |
| ActionFeedback `onUnmounted` | grep -c | **0** |

## Ruled disagreements (the tri-fold's actual work)

| id | worker-F | worker-O | RULING | Deciding evidence |
|---|---|---|---|---|
| **L-1 "largest defineExpose"** | AdminUsersPanel's 5 members is "largest of the 14 sites" | ColorPicker.vue:331 = 12 members; 5 is joint-second | **O wins** | This seat read ColorPicker.vue:331-344: **12 members** (isEditing…actionBarContext). F's uniqueness restatement is refuted; the inversion itself is unaffected. |
| **L-1 class width** | Single-subject rescope | demo/palettes CLASS of 2 (+ useSlugMigration.ts) | **O wins** | useSlugMigration.ts:6 barrel type-import, :30 InstanceType ref, four `slugBarRef.value?.setError(…)` calls, :121 return member — read verbatim by this seat. Same shape, same directory. F never looked. |
| **L-4 verdict** | CONFIRMED (counts "corroborated within noise": 25 both / 49 barrel) | RESCOPED (24 both / 48 barrel; ledger's 35 REFUTED) | **O wins** | This seat measured: barrel-importer files = **48**, both-paths = **24** (comm of sorted file lists). The ledger's 35 is refuted, not noise-corroborated; F's 25/49 are each off by one. A row that survives on counts carries O's. |
| **L-5 witness standing** | "committed webkit shot … present on disk" | UNTRACKED — fails L-7; finding survives on code + live re-run | **O wins** | `git ls-files --error-unmatch …/safari-desktop-light/admin-users.png` → *"did not match any file(s) known to git"*; `.gitignore:34` is `*.png`. Present-on-disk ≠ committed. The verdict stands on the static path (below), not the screenshot. |
| **L-7 verdict** | CONFIRMED (window "narrower than stated but real") | RESCOPED (race needs first-prune→0; "leak" retired; cure incomplete — atom lacks unmount teardown) | **O wins** | This seat verified: `:disabled="emptyCount === 0 \|\| pruning"` (:25) makes the double-beat reachable only when the first prune returns **0** — and useAdminUsers.ts:195-198 `catch → warn → return 0` makes the failure branch the realistic path (the sharper defect: a THROWN prune renders the success-family copy "No empty users to prune"). ActionFeedback.vue: `onUnmounted` count = **0** — the moved atom must gain teardown or 12+ consumers inherit the hole. F's confirm was right on substance, blind on the cure. |
| **L-9 verdict** | CONFIRMED (with count correction) | RESCOPED (mechanism confirmed, both numbers refuted) | **O wins (label)** | Substance identical in both returns; injectors measured **6** (not 11) by this seat. Under L-9 law — which worker-F itself invoked against the challenger — a row whose load-bearing counts are refuted is RESCOPED to the mechanism with corrected counts, not confirmed-with-asterisk. |
| **L-11 fleet count** | 13 by a 3-line window ("methodology-sensitive") | 22 in 11 files, listed per-file | **O wins** | This seat's opening-tag census: **22 sites in 11 files**, file list byte-identical to O's. F's window measure undercounted; F itself flagged it as unreliable. |
| **L-13 gap** | "PaletteCard seat contributes zero rows naming this file" (checked the wrong absence) | challenge-D's 18 rows exist on disk, absent from every harvest and the ledger | **O wins** | `audit/components/AdminUsersPanel/challenge-D-design.md` = 33,498 bytes on disk; `grep -c challenge-D registry/harvest/area-palettes.json` = **0**; DEFECT-LEDGER refs = **0** — while three OTHER areas' harvests do cite their challenge-D seats, making the palettes omission conspicuous. Booked as born-RED gate G-9, not a deferral. |

Convergences verified rather than trusted (both workers agreed; this seat re-measured): L-2's 34/1 and the 6-prop/7-emit dual path; L-3's absent `variant` in Button.vue.d.ts:4-19 + 0 chunk hits + the 51-site census + vue-tsc exit 0 (**reproduced by my own run**); L-5's static path (useAdminUsers.ts:54-56 early-return before any loading/error write → panel :63); L-6's five homes; L-8's 1-of-3 confirm coverage (Flagged **:98** direct `deletePalette`, Tags raw `<button>` :98 with `deleteTag` at :101 — O's line exact, F's ":99" off by one); L-10's 1-of-17. The L-1 no-harm mechanism is a genuine convergence by two independent routes: F from pane-keying (verified by this seat: usePaneRouter keys PaneSlots by view name — `key: left` — so `subView` is fixed per AdminPane instance and a sub-view switch deactivates rather than remounts; App.vue KeepAlive `:max` = 9/:88, 6/:107, 4/:133 — both workers' App.vue line cites drift, the values are exact), O from the vue 3.5.35 runtime (deactivate moves the subtree without nulling the ref; unmount nulls the ref via setRef). Either route ends the same: deactivated ⇒ ref valid ⇒ the `?.` call LANDS on the cached instance; unmounted ⇒ ref null AND the script-setup state it would have cleared no longer exists; remount reinitializes :236-238. **No arm exists in which stale `userPalettes` survives to a remount.**

## Per-defect verdicts

### L-1 — state module holds the panel's instance handle · **RESCOPED** (BLOCKER→MAJOR structural class)
Every cited line exact at HEAD: useAdminUsers.ts:14 (SFC type via the `./browser/admin` barrel), :27 (InstanceType ref), `?.` mutations :95/:117/:132/:146; usePalettePorts.ts:123 + :197; AdminPane.vue:27 template ref + :134 alias; AdminUsersPanel.vue:389 (5 members incl. mutable `userPalettes`). The latent import cycle is real, erased at runtime only by `import type` + verbatimModuleSyntax. **Refuted**: the data-corruption reproduction (unreachable — see convergence note above); the "25 = 24 barrels + this file" arithmetic (the grep matches the subject **0** times — measured); "largest defineExpose" (ColorPicker = 12); "sole state-composable→component consumer" (useSlugMigration.ts is the identical shape). **Rescoped to**: a demo/palettes-wide layer-inversion CLASS of two sites (+ usePaletteWiring.ts:27 demo-wide, folded with re-trigger), MAJOR, the wave's structural spine — the only row whose cure deletes an expressive power rather than polices it. Repro: `grep -rn --include='*.ts' 'InstanceType<typeof' demo/palettes/` → 2 lines.

### L-2 — 34-member port injected to call one method · **CONFIRMED**
adminPort = **34 members** (hand-counted :195-231); the panel injects it (:234) and uses exactly one member on one executable line (:361 `await pm.loadUserPalettes(slug)`); AdminPane.vue:28-40 simultaneously feeds the same child 6 props + 7 emit handlers off the same port — one source, two mechanisms, one parent/child pair. The port's own header (:21-31, RF-15) claims dissolution of "ONE cross-everything injected blob" into "FIVE narrow ports" while PORT 4 re-aggregates users/queue/audit/flagged/tags/search/expansion. Standing note (O): :197 exists solely to carry L-1's handle — the two rows are one cure. Repro: `grep -n 'pm\.' demo/palettes/browser/admin/AdminUsersPanel.vue` → :233 (comment) + :361.

### L-3 — five dead `variant` props on a Button that has no `variant` · **CONFIRMED**
Button.vue.d.ts:4-19: emphasis/tone/size/iconOnly/loading/type/disabled/class — **no variant**; chunk `button-Bu9F4uU6.js`: `variant`/`outline`/`ghost`/`inheritAttrs` all **0**, so inheritAttrs defaults true and the string falls through `$attrs` to the DOM, rendering every subject button at default `emphasis` (secondary). Subject sites :22/:33/:58/:114 (outline) + :123 (ghost); `EmptyState variant="error"` (:53) and `Badge variant="secondary"` (:102) are REAL props, correctly excluded. Fleet = **51 sites / 22 files** (arbiter census = both workers'). The panel's own intent comment (:107-111, "quieted to ink-at-rest… never as 5 resting beacons") is an instruction glass-ui never receives. `vue-tsc` exit **0** over all 51 — the W44 hard typecheck is structurally blind to this class and may never be cited as its gate. Adopted from O's cell: live DOM census {variant: 5, emphasis: **0**} on `#/admin/users` — the zero is the discriminator (declared props are consumed, never rendered). **GLASS-OWNED rider** (cite INBOX I-20; masking-fallback ban — no local shim): Button `inheritAttrs:false` + explicit allowlist so an unrecognised design-token prop is a visible no-op; leaves as a BH-relay letter. Repro: the python census in the table + `sed -n '1,20p' node_modules/@mkbabb/glass-ui/dist/components/button/Button.vue.d.ts`.

### L-4 — two dialects for one design system in one file · **RESCOPED**
Subject confirmed verbatim: :188 `../../../ui/button`, :189 `../../../ui/badge`, :190-197 `@mkbabb/glass-ui/dialog` — while `demo/ui/dialog/index.ts` exports the same eight symbols unused; all 19 demo/ui entries are one-line re-exports (spot-read: button, badge, dialog). **Counts corrected**: barrel-importer files = **48** (ledger's 35 REFUTED; F's 49 off by one); both-paths files = **24** (F's 25 off by one). Bundle-cost arm stays HYPOTHESIS — never measured on the built artifact; per L-12 only `npm run gh-pages` output may ever decide it. Rescoped to: the subject's three import lines land in this wave; the 48-file demo/ui deletion is fleet wave **MT-UI1** (re-trigger: `grep -rlE 'from "(\.\./)+ui/[a-z-]+"' demo/ | wc -l` — 48 today, 0 at close). Repro: `grep -n 'from "\.\./\.\./\.\./ui/' demo/palettes/browser/admin/AdminUsersPanel.vue`.

### L-5 — unauthenticated ≠ empty roster · **CONFIRMED — the wave's BLOCKER**
Statically certain, no data needed, because the accused path never fetches: useAdminUsers.ts:54-56 `const token = getAdminToken(); if (!token) return;` exits BEFORE touching `loadingUsers`/`usersLoadError`, so initial state (users=[] / loading=false / error=null) selects AdminUsersPanel.vue:63 `· roster clear · / No users found.` — two lines below the component's own violated invariant (:49-50, "error ≠ empty… never costumes"). No route precondition exists: viewSchema.ts:188-196 declares `admin-users` with left/right/label/leftLabel/rightLabel/icon/accentHueShift and **no auth meta**; `if (!token)` guards = 11 in the file, 22 across demo/palettes. One correction to O's evidence (recorded, not verdict-bearing): usePaletteWiring.ts:147-149 fires `loadAdminUsers()` behind `adminUsers.value.length === 0` — a cache guard, so "unconditionally" is retired; **"with no authentication test" stands**, which is the defect. Adopted from O's cell with the discriminator that separates app-fact from environment-fact: token removed → body renders the roster-clear plate with 15 buttons, `hasUnreachable:false`, `skeletons:0` — **no request was attempted**, so this is the token-guard branch, not the API-less server (a failed fetch renders "The roster is unreachable.", :51-56). Witness standing: the safari-desktop-light PNG is UNTRACKED (fails L-7) — the verdict rests on the code path. Repro (fresh session): read useAdminUsers.ts:54-56 + AdminUsersPanel.vue:63 + viewSchema.ts:188-196; or run the wave's committed probe with localStorage token removed.

### L-6 — one row grammar, five storage sites · **CONFIRMED (widened)**
The literal `items-center gap-3 px-3 py-2.5` lives in **five** files in `demo/palettes/browser/admin/`: the atom AdminListItem.vue:**11** (declared home), AdminUsersPanel.vue:80 (border split onto wrapper :68), AdminListSkeleton.vue:**9** (naming its own transcription at :4-5), AdminAuditPanel.vue:62, AdminFlaggedPanel.vue:50 — the accusation said three; both workers found five; arbiter confirms five. The atom's only consumer is AdminNamesPanel (:44/:94). The skeleton's dishonesty is checkable: it predicts swatch + two lines + one lozenge; the row it becomes has no swatch, one line, two lozenges. Repro: the grep in the table.

### L-7 — feedback beat rebuilt without its cancellation · **RESCOPED**
Duplication confirmed: ActionFeedback.vue (homed under `PaletteCard/`, undiscoverable; props message/variant/visible/autoDismissMs=2500; watch does `clearTimeout` before re-arming; consumed at PaletteCard.vue:123, imported :180) vs the panel's bare `setTimeout(…, 3000)` at :308 — no handle, no cancel, no onUnmounted. **Narrowed** (O): the double-beat overlap needs the first prune to return 0 while `emptyCount > 0` — reachable chiefly through useAdminUsers.ts:195-198's `catch → return 0`, which is the sharper buried defect: **a thrown prune renders the success-family copy "No empty users to prune"** — the L-5 sin on a second path, booked into this wave's unit test. "Leaks its timer" retired for "uncancelled timer" (a post-unmount ref write is not an error; the closure holds ≤3s). **Cure corrected** (O, verified): ActionFeedback has **zero** `onUnmounted` — the move to `demo/shared/ui/` must add teardown or every consumer inherits the hole. The live observational arm is UNVERIFIABLE-HERE (prune is data-backed; API-less server); settled by reading; the gate is a unit test. Repro: `grep -n 'setTimeout' …/AdminUsersPanel.vue` + `grep -c onUnmounted …/ActionFeedback.vue` → 1 / 0.

### L-8 — one confirm home, three destructive panels · **CONFIRMED**
AdminUsersPanel.vue:254-286 is the repo's sole confirm machine (7 refs :255-261, `showConfirm` :263-278, `onConfirm` :283-286; `confirmOpen`/`function showConfirm` grep to this file alone) and every destructive action HERE routes through it (:288/:315/:326). The siblings do not: AdminFlaggedPanel.vue:**98** `@click="flagged.deletePalette(item.paletteSlug)"`, AdminTagsPanel.vue:101 `@click="tagsApi.deleteTag(tag.name)"` on a raw `<button>` (:98). The cause is in the tree (:280-282: Glass 7 folded ConfirmDialog onto the Dialog family). The invariant "a destructive admin action is confirmed" is currently enforced by which panel bothered — a safety row. The three-consumer `useConfirm`/ConfirmDialog composition is OURS and lands in-wave; a ConfirmDialog *preset* over the Glass 7 Dialog family is a **GLASS-OWNED rider** (INBOX I-20, BH relay). Repro: the greps in the table.

### L-9 — two transports for one colour concern · **RESCOPED**
Mechanism confirmed: :232 injects SAFE_ACCENT_KEY across palettes→color-session while `cssColor` arrives as a prop (:210) that AdminPane.vue itself injected at :94 from the same keys module. **Counts corrected**: SAFE_ACCENT_KEY injectors = **6 files** (not 11; arbiter list = O's list = F's count); O adds 14 palettes→color-session import edges; the report's ":133 inject" is actually the :134 alias comment region — the inject is :94. Ruling folded in-wave: prop-for-both at the panel (L-2's cure already ends the file at zero injects). MINOR. Repro: `grep -rln 'inject(SAFE_ACCENT_KEY' demo/` → 6.

### L-10 — explicit `Transition` import · **CONFIRMED**
Sole hit :186 among 17 `<Transition>` users. Behaviorally identical (the local binding resolves to the same component object) — "shadowing" retired; the defect is gratuitous divergence. Not catchable by an unused-import rule (the template consumes the binding) — needs this wave's grep gate to stay cured. One-token deletion, rides along. Repro: the grep in the table.

### L-11 — the `xs` rung rebuilt per call site · **CONFIRMED**
`h-7 px-2/2.5` + `font-display text-caption` hand-set on `<Button size="sm">` at class lines :24/:35/:116/:125 while Button.vue.d.ts:5 ships `ButtonSize = "xs" | "sm" | "md" | "lg"`. Fleet census (arbiter, opening-tag scoped): **22 sites / 11 files**, byte-identical to O's list; F's 13-by-window retired as methodology noise. Subject's 4 sites gated here; the other 18 fold to **MT-BTN1** (re-trigger: the census — 22 today, 4 die here, 0 at close). The sibling D-seat's "h-7 is inert under size=sm min-height" computed-style claim is NOT laundered in — unmeasured by every seat in this fold. **GLASS-OWNED rider** (INBOX I-20): if `xs` is the wrong height for a dock-adjacent admin toolbar, the token moves inside glass-ui — never back into eleven demo files. Repro: the python census in the table.

## The wave — MT-AU1 (re-authored, best of both, corrected)

**waveId**: MT-AU1 · **title**: AdminUsersPanel becomes a leaf: authority before roster, one declared prop vocabulary, one row anatomy, one confirm home · **bornRed**: true

**Scope (exact files)**: `demo/palettes/browser/admin/AdminUsersPanel.vue` (391→~200: zero inject, zero defineExpose, zero setTimeout, zero confirm engine, zero row transcription, zero `variant`, zero `h-<n>`, no `Transition` import; safeAccent + everything else via props/emits) · `demo/palettes/useAdminUsers.ts` (:14/:27/:95/:117/:132/:146/:206 deleted; disclosure state → new colocated `useAdminUserPalettes.ts`; all `if (!token)` guards die behind the gate) · `demo/palettes/useSlugMigration.ts` (:6/:30/:84-87/:121 — the class's second site; setError → prop/emit on PaletteSlugBar, whose :236 defineExpose shrinks accordingly) · `demo/palettes/usePalettePorts.ts` (:123/:197 deleted; adminPort 34→33) · `demo/palettes/admin/AdminPane.vue` (:27/:134 deleted; auth gate mounted at the composition boundary; safeAccent passed as prop) · `demo/palettes/browser/admin/AdminListItem.vue` (+`interactive` prop: role/tabindex/aria-expanded/keydown hoisted from the panel's :78-89, whose guard comment travels with it) · AdminListSkeleton.vue + AdminAuditPanel.vue + AdminFlaggedPanel.vue (compose the atom — five homes → one) · AdminFlaggedPanel.vue + AdminTagsPanel.vue (destructive clicks route through the shared confirm; the Tags raw `<button>` becomes a Button) · NEW `demo/shared/ui/ActionFeedback.vue` (MOVED from PaletteCard/ — never deleted — **gaining the onUnmounted teardown it lacks**) + `demo/shared/ui/ConfirmDialog.vue` + `useConfirm()` · NEW `test/demo/admin-auth-not-empty.test.ts` + `test/demo/admin-prune-feedback.test.ts` (jsdom + @vue/test-utils, both in devDeps) · `demo/shell/viewSchema.ts` (five admin views gain `auth: "admin"`, honoured by ONE guard site) · fleet-mechanical in-wave: the 51 `variant` renames (outline→`emphasis="secondary"`, ghost→`emphasis="quiet"` [+`tone="destructive"` where the intent is red], destructive→tone, default→primary).

**Structure over gates (L-8 law)**: deleting `InstanceType<typeof …>` from demo/palettes makes the layer inversion UNREPRESENTABLE (no handle → no cross-layer write, no latent cycle, no `?.` swallow); the AdminGate makes `getAdminToken()` a non-null contract inside the console, so the unauthorized state stops being a state the panel models (22 per-method guards DELETE, not re-check); the disclosure state owned by `useAdminUserPalettes` makes a swallowed clear inexpressible (direct call, no instance-liveness precondition); one row-grammar home makes a rhythm fork impossible; zero injects make a second data path unacquirable. Where structure is genuinely unavailable, gates: the dead-`variant` class cannot be made unrepresentable from demo/ (fallthrough attrs + vue-tsc blindness, **measured exit 0**) — grep + DOM gates here, producer allowlist via BH relay (masking-fallback ban: no local shim/wrapper/lint-plugin substitute).

**Gates** (every one RED today; the input that makes it RED recorded):

- **G-1 · inversion unrepresentable**: `test "$(grep -rn --include='*.ts' 'InstanceType<typeof' demo/palettes/ | wc -l | tr -d ' ')" -eq 0` — RED: 2 (useAdminUsers.ts:27, useSlugMigration.ts:30). The third site (usePaletteWiring.ts:27) is FOLDed; re-trigger = same command widened to `demo/`.
- **G-2 · no imperative surface, no port handle**: `test "$(grep -c 'defineExpose' demo/palettes/browser/admin/AdminUsersPanel.vue)" -eq 0 && test "$(grep -rn 'adminUsersPanelRef' demo/ | wc -l | tr -d ' ')" -eq 0` — RED: 1 / 10.
- **G-3a · declared vocabulary (static)**: the python opening-tag census (`<Button…>` containing `variant=`) `== 0` — RED: 51 in 22 files. vue-tsc is NOT this gate (exit 0 today, measured twice — worker-O and this seat).
- **G-3b · declared vocabulary (DOM, O's cell)**: wave-committed probe `audit/waves/MT-AU1/probe-au1.mjs` — chromium 1440×900, token removed, `#/admin/users`: buttons with `variant` attr === 0 — RED: 5 today, `emphasis` attrs 0 (the discriminator: declared props never reach the DOM).
- **G-4a · authority never costumed as empty (DOM)**: same probe: with no token, body must NOT contain "No users found."/"0 users", MUST contain the named authority plate, no operable Prune/Refresh — RED today (measured body: `Users 0 · … · ROSTER CLEAR · No users found.`, 15 buttons, zero requests attempted).
- **G-4b · authority asserted once, at the boundary**: `test "$(grep -rn --include='*.ts' 'if (!token)' demo/palettes/ | wc -l | tr -d ' ')" -eq 0 && grep -q 'auth: "admin"' demo/shell/viewSchema.ts` — RED: 22 / absent.
- **G-5 · one row anatomy**: `test "$(grep -rn 'items-center gap-3 px-3 py-2.5' demo/palettes/browser/admin/ | grep -v AdminListItem.vue | wc -l | tr -d ' ')" -eq 0` — RED: 4.
- **G-6 · one confirm home + honest failure beat**: `test "$(grep -cE 'setTimeout|confirmOpen' demo/palettes/browser/admin/AdminUsersPanel.vue)" -eq 0 && test "$(grep -rln 'useConfirm' demo/palettes/browser/admin/AdminUsersPanel.vue demo/palettes/browser/admin/AdminFlaggedPanel.vue demo/palettes/browser/admin/AdminTagsPanel.vue | wc -l | tr -d ' ')" -eq 3 && npx vitest run test/demo/admin-prune-feedback.test.ts` — RED on all three arms (setTimeout=1, confirm engine present, consumers=0, test absent). The test asserts the two arms no browser here can reach: (i) a second action-result inside the first's dismiss window survives its own full window; (ii) **a prune whose transport throws must not render success-family copy** (today: catch→0→"No empty users to prune").
- **G-7 · the panel is a leaf**: `F=demo/palettes/browser/admin/AdminUsersPanel.vue; test "$(grep -c 'inject(' $F)" -eq 0 && test "$(grep -c 'Transition } from \"vue\"' $F)" -eq 0 && test "$(grep -cE 'class=\"[^\"]*\bh-[0-9]' $F)" -eq 0` — RED: 2 / 1 / 4.
- **G-8 · witnesses exist in git (L-7 law)**: `test "$(git ls-files docs/tranches/V/megatranche/audit/waves/MT-AU1/ | grep -c png)" -ge 6` — RED: tracked megatranche PNGs = 0 repo-wide; `.gitignore:34 *.png` requires `git add -f`. An uncommitted witness does not exist.
- **G-9 · the L-13 harvest gap closes (NEW, arbiter)**: `test "$(grep -c 'challenge-D' docs/tranches/V/megatranche/registry/harvest/area-palettes.json)" -ge 1` — RED: 0 today while `audit/components/AdminUsersPanel/challenge-D-design.md` (33KB, 18 rows, 4 at BLOCKER grade) sits unharvested. The design half of this component may not be called closed until those rows are harvested AND adjudicated; adjudicating them from here would launder an unharvested report into the record, so the gate demands the harvest, and the adjudication follows the normal M-12 route.
- **G-suite regression floor**: `npx vue-tsc -p tsconfig.demo.json --noEmit && npx vitest run` — GREEN today; retained as regression guard only, with its L-3 blindness recorded above.

**π obligations**: chromium desktop-light + desktop-dark @1440×900, mobile-light @390×844; route `#/admin/users`; selectors: `[data-slot="admin-authority-plate"]` (post-wave), `[data-slot="admin-list-item"]`, `[role="alertdialog"]`; committed (force-added) under `audit/waves/MT-AU1/pi/` with `probe-au1.mjs` + JSON output. Per I-20, WebKit and real-Safari-26.4 (safaridriver :4599) are SEPARATE cells, never inferred from chromium. Authenticated roster/expanded/confirm-with-data states: UNVERIFIABLE-HERE (API-less) — every populated capture must declare its `page.route` fixture in its JSON header; owed to the first API-backed cell, NOT a gate of this wave.

**Δ obligations** (before→after, both halves committed): defineExpose 5→0 · panel inject 2→0 · state-module→SFC type-imports 2→0 (both class sites) · port handle lines 2→0 · dead variant attrs 51→0 (DOM census {variant:5, emphasis:0}→{0,5} on the route) · demo/ui imports in file 2→0 · row-grammar homes 5→1 · confirm coverage 1/3→3/3 · bare setTimeout 1→0 · h-7 overrides (subject) 4→0 · explicit Transition import 1→0 · `if (!token)` 22→0 · unauthenticated surface: roster-clear plate→authority plate.

**Carries**: L-1 BUILD (class, both palettes sites; BLOCKER narrative RETIRED with rationale; id preserved per anti-rename) · L-2 BUILD · L-3 BUILD demo-half; producer half GLASS-OWNED → BH relay (I-20), banked on `glass-ui version > 7.0.0 && grep -c inheritAttrs dist/button-*.js ≥ 1`, swept at every wave open · L-4 subject BUILD; fleet → **MT-UI1** (re-trigger 48→0) · L-5 BUILD (the BLOCKER) · L-6 BUILD (5 homes) · L-7 BUILD with corrected cure (teardown added at the move) · L-8 BUILD; ConfirmDialog preset → BH relay · L-9 BUILD (prop-for-both; corrected counts 6/14) · L-10 BUILD · L-11 subject BUILD; fleet → **MT-BTN1** (re-trigger 22→0); xs-height fallback GLASS-OWNED → BH relay · challenge-D 18 rows → G-9 (harvest, then M-12) · BANK (surplus structure, not the accusation): adminPort five-seam split beyond the users slice, re-trigger `grep -n 'adminColorQueue: colorQueue' demo/palettes/usePalettePorts.ts`.

**completableAlone**: YES. Every edit lives in `demo/palettes/**` + `demo/shared/ui/**` + `demo/shell/viewSchema.ts` + two test files — no src/, no api/, no glass-ui edits; ~13 files, one seam, all gates static greps or jsdom vitest runnable in any checkout after `npm i` (G-3b/G-4a need only the dev server, no API); the sole cross-cutting touch (viewSchema auth meta + one guard) is additive and leaves the 9 non-admin views' KeepAlive economy untouched (admin panes already fall off the LRU by design). If MT-AU1 were the only wave ever executed: the console stops telling strangers the roster is empty while handing them Prune; two composables stop reaching through a layer into component instances; 51 styling intents the design system silently discards become declared props; one row anatomy serves five sites and the skeleton stops lying; three destructive panels confirm instead of one; the panel mounts in a test with props alone.

## ADDENDUM (E-3 — appended, no ledger row edited)

ADDENDUM MT-AU1/§2 · AdminUsersPanel tri-fold arbitration, 2026-07-27, HEAD `c654824e`, arbiter-F (Fable 5). The eleven CHALLENGE-L rows were adjudicated by two blind workers (Fable, Opus) and this arbiter attacked both before adopting either: **7 CONFIRMED / 4 RESCOPED / 0 REFUTED**. Seven inter-worker disagreements were ruled — **all seven for worker-O**, each on arbiter re-measurement (ColorPicker's 12-member expose vs the claimed "largest 5"; barrel counts 48/24 vs 49/25 and the ledger's refuted 35; the untracked "committed" witness; the prune-race precondition + the atom's missing `onUnmounted`; the L-9 label under L-9 law; the 22-in-11 height census; the unharvested challenge-D 18 rows). The docket's severity ordering is AMENDED: **L-5 is the BLOCKER** (authority costumed as an empty roster, statically certain, live-discriminated in worker-O's chromium cell by hasUnreachable:false + skeletons:0 = no request attempted); **L-1 is MAJOR** — its data-corruption arm is unreachable by two independent mechanisms that converge (pane-keyed KeepAlive deactivation keeps the ref bound so the call lands; true unmount nulls the ref and destroys the very state a stale call would have cleared), and it survives as a two-site layer-inversion class whose cure is a deletion of expressive power. Recorded so it is never re-discovered: `npx vue-tsc -p tsconfig.demo.json --noEmit` exits 0 over all 51 dead `variant` props (measured independently by worker-O and by this arbiter) — the W44 hard CI typecheck is structurally blind to the fallthrough-attr class and may never be cited as its gate. Three riders are GLASS-OWNED (Button attr allowlist; the xs-height token question; the ConfirmDialog preset) and leave via the standing BH relay (INBOX I-20) with the masking-fallback ban binding: no local shim may stand in. Two evidentiary defects are recorded against the formation rather than the component: zero megatranche PNGs are tracked (`.gitignore:34`), so every visual witness cited anywhere in this docket fails L-7 until force-added (gate G-8); and challenge-D's 18 rows against this same file are on disk, in no harvest and no ledger (gate G-9) — the component's design surface is NOT closed by this apotheosis.

## DISSENT (preserved unresolved disagreement)

1. **Worker-F's methods dissent against the challenger seat — preserved verbatim in substance and ADOPTED**: "three of its eleven evidence blocks carry count-scoping flaws (the barrel-blind .vue-import grep presented as covering the subject import; SAFE_ACCENT 11-vs-6; the height-override fleet count) — every flawed count decorated a true defect this time, but under L-9 law decorative unscoped counts are how false rows get through." The arbiter extends the same finding to worker-F itself: its 25/49 barrel counts and 13-site height census were each off, and its "largest defineExpose"/"committed shot" restatements were refuted — worker-F applied L-9 discipline to the challenger and not to its own instruments. Worker-O's parallel note stands with it: "the fleet is not unreliable — it is unaudited."
2. **The ±1 measurement window (unresolved, immaterial)**: worker-F's wider-net 49 barrel-importers / 25 both-paths vs worker-O's and this seat's 48/24. The regex frames differ (F's net uncharacterized); no verdict turns on it; both sides' positions preserved. The binding numbers are the arbiter's: 48/24.
3. **Worker-F's probe-parsimony position on L-3** — preserved: F judged the design consequence from the d.ts + chunk grep alone, holding that "probe parsimony ruled a browser session out for a verdict the d.ts + chunk grep already decided." The arbiter agrees the static chain decides the VERDICT, and simultaneously records that worker-O's live {variant:5, emphasis:0} DOM census is the stronger *teaching* evidence and becomes gate G-3b. Both positions are right at their own altitude; no reconciliation needed, none forced.
4. **Worker-F's L-4/L-7/L-9 CONFIRMED labels** — preserved: F held that a true mechanism with flawed decorative counts is CONFIRMED-with-correction; O and the arbiter held RESCOPED under L-9 law. The rows' substance was never in dispute — only what a verdict label owes to its numbers. The ruling (RESCOPED) binds the registry; F's position is recorded so the label-law question can be settled at the formation level rather than silently re-litigated per seat.
5. **Worker-O's registry dissent (L-13)** — preserved verbatim in substance and converted to gate G-9: "if this component is declared adjudicated on the strength of my eleven verdicts, four accusations carrying BLOCKER severity vanish without a verdict. I have named them in CARRIES rather than adjudicating them, because adjudicating an unharvested report would launder it into the record by the same route." The arbiter concurs and has made the gap RED-visible rather than prose-visible.
6. **Line-number drift, both workers** (recorded, no ruling needed): F's atom :12→:11, Flagged ":99"→:98, App.vue ":80-84"→:88/:107/:133; O's App.vue ":78/:104"→:88/:107, AdminPane ":133 inject"→:94 inject/:134 alias, cssColor ":209"→:210. Every drift decorated a true fact; none was load-bearing; all corrected above.

---

# ADDENDUM MT-AU1/§3 — r3/r4 delta apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed by this seat from its own runtime, not inherited from the spawn label.
**Date**: 2026-07-28 · **HEAD**: `c654824e` (worker-O adjudicated at `7775473b`; the delta is docs-only) · **Corpus**: the r3/r4 delta accusations against `demo/palettes/browser/admin/AdminUsersPanel.vue` and its wiring — the fresh C/D/L rows plus refiles of the §1–§2 record.
**Workers adjudicated**: worker-F (Fable 5, `admin-users-panel-r3-delta-worker-F`) · worker-O (Opus 5 [1m], `adminuserspanel-r3-delta-worker-o`).
**E-3 discipline**: this section is APPENDED. Nothing in §1–§2 (the r2 apotheosis) is rewritten; where the delta amends an r2 gate the amendment is recorded here as an addendum, with the r2 text left standing as the historical ruling.
**Method (L-14)**: both workers were attacked before either was adopted. The two returns are NOT the same docket — worker-F ruled the Δ-clusters over the C-heavy corpus (C-1..C-22 + refiles), worker-O ruled the D/L-heavy corpus (D-23..D-33, L-19..L-24, C-23..C-26) — so "the same 25-defect corpus" in the spawn brief is a census error both seats independently flagged; the unified docket below is the union, deduped under the anti-rename rule. Every inter-worker disagreement was re-derived against the tree by this seat. Suspicious convergences spot-checked at source rather than accepted: the D-1 chain (all five links re-read), the focus-clip mechanism (:68/:82), the confirm-machine non-clearing (:283-286 + grep 0), the 12-warn/4-success-return census, the res.total discard, the dead lint globs (`--print-config` on the subject → 0 `no-restricted-imports`), the dead exports (0/0/0 consumers), the demo/@ ghost (ls: no such directory; 10 refs in e2e/), the glass Button surface (emphasis/tone present, `variant` absent, `./toast` shipped).
**Arbiter's own instruments this session** (each re-runnable): whole-file reads of AdminUsersPanel.vue (391 L), useAdminUsers.ts (221 L), AdminPane.vue, demo/palettes/api/admin-users.ts, the r2 record; scoped reads of api/src/modules/admin/service/users.ts:220-252, usePalettePorts.ts (:118-126/:140-146/:194-200/:245/:274), vitest.config.ts (whole), eslint.config.js (G-DEMO region), e2e/smoke/admin/fixtures/admin-populated.ts:15-30, usePaletteActions.ts (expandedId), EmptyState.vue (dots), probe-D3f.mjs:30-45, glass-ui `Button.vue.d.ts` + package.json exports; sixteen scoped counting commands (all outputs recorded below); **one `npx madge --extensions ts,vue --ts-config tsconfig.json --circular demo/palettes` run — reproduced worker-O's result byte-identically: 4 circular dependencies, the exact four chains**.
**Environment disclosure (L-12)**: no browser probe was run by this seat; the dev server is API-LESS and every data-backed arm would be an environment fact. Live cells cited below are adopted AS the owning worker's cells (chromium/webkit playwright, `page.route`-fixtured), never transferred to Safari (I-20), and never laundered: where a probe's fixture authored the number, that is said.

## §3 Scoreboard

**29 CONFIRMED · 12 RESCOPED · 0 REFUTED at row level (three named MECHANISMS refuted inside surviving rows) · 0 UNVERIFIABLE-HERE at row level (two arms so marked inside rescoped rows) · 2 GLASS-OWNED rows (+ 4 standing riders).**
The r2 record STANDS whole: L-5 remains a blocker (now one of SEVEN — the six fresh BLOCKERs below join it); the L-1 BLOCKER→MAJOR retirement is NOT overturned (worker-O attacked it with the r4 cycle evidence and could not reach the corruption arm; this seat concurs).

Arbiter measurement record (delta; all this session):

| fact | command (abbreviated) | measured |
|---|---|---|
| confirmAction ever cleared | `grep -c 'confirmAction.value = null' …AdminUsersPanel.vue` | **0** |
| console.warn in useAdminUsers.ts | `grep -c` | **12** |
| success-shaped failure returns | `grep -nE 'return 0;\|return \[\];'` | **4** (:155/:160/:188/:197) |
| `res.total` consumed | `grep -c 'res\.total' useAdminUsers.ts` | **0** |
| InstanceType, `.ts`-scoped demo/palettes · unscoped demo-wide | grep | **2 · 16** (the scoping is load-bearing — O's near-miss note adopted as gate text) |
| idempotencyKey homes under demo/palettes/api/ | `grep -rln` | **palettes.ts only** (0 admin wrappers) |
| AbortController/AbortSignal in demo/ | grep · wc | **0** |
| `<bdi`/`dir="ltr"` in the panel | grep -c | **0** |
| demo/@ refs in e2e/ · demo/@ exists | grep · ls | **10 · no such directory** |
| `no-restricted-imports` reaching the subject | `npx eslint --print-config demo/palettes/useAdminUsers.ts \| grep -c` | **0** (G-DEMO-1/3a glob the dead `demo/@/composables/**`; 3b globs dead aliases + live `demo/color-picker/**` but bans dead paths) |
| Admin exports in browser/index.ts | grep -n | **5** (:28-32) |
| dead-export consumers (onImpersonate/featurePaletteBySlug/deletePaletteAdminBySlug) | per-symbol grep excl. home file | **0 / 0 / 0** |
| PaginationBar consumers | grep -rln | **AdminAuditPanel + AdminFlaggedPanel only** (the atom sits in the subject's own directory) |
| circular deps in demo/palettes | `npx madge --extensions ts,vue --ts-config tsconfig.json --circular demo/palettes` | **4** — `usePalettePorts.ts > useAdminUsers.ts > browser/admin/index.ts > {Audit\|Flagged\|Tags\|Users}Panel.vue` |
| vitest vue plugin | read vitest.config.ts whole | **absent** (no `@vitejs/plugin-vue`; environment jsdom) — r2's G-6/G-4 unit gates were UNBUILDABLE as specced |
| glass-ui 7.0.0 Button axes · `./toast` export | grep d.ts · package.json | **emphasis/tone/size, NO variant · `./toast` SHIPPED** |
| probe-D3f prune fulfillment | read probe-D3f.mjs:36-38 | **hand-authored `{pruned: 3}`** — the "3 executed" is the fixture, not the server |
| worker-O probes banked | ls audit/components/AdminUsersPanel/probe-O-r3/ | **4 files** (mjs+json ×2) |
| expandedId ref · write namespaces | grep usePaletteActions.ts | **one ref (:24)** written from `palette.id` (:76) and `id` (:81) |
| EmptyState `dots` prop · passed by subject | grep | **exists, default true · passed 0 times** (:63/:138 bare) |
| server prune scope | read api/src/modules/admin/service/users.ts:228-251 | `users.findEmptyUserSlugs()` — **whole collection, no scope arg**, transactional delete of users+sessions |
| prune POST body (client) | read demo/palettes/api/admin-users.ts:69-71 | **no body** |

## §3 Ruled disagreements

| id | worker-F | worker-O | RULING | Deciding evidence |
|---|---|---|---|---|
| **D-27 label** | CONFIRMED (Δ-8, mechanism silently corrected to the composable catch) | RESCOPED — filed mechanism ("no catch at all", unhandled rejection, misses the warn floor) REFUTED; costume half survives | **O wins** | This seat read useAdminUsers.ts:153-162: the catch exists one module down and returns `[]`; a transport failure CANNOT reject the awaited promise. Under the §2-dissent-#4 label law (a row whose load-bearing claims are refuted is RESCOPED), O's label binds. F's silent correction is itself recorded as a laundering hazard: a worker must refute a wrong mechanism aloud, not paper over it. |
| **L-21 console homing** | RESCOPED — structure note, no gate, rides the port-split bank ("a move nobody specced") | CONFIRMED — in-wave re-home, gate on the barrel | **O wins** | "Nobody specced" is false: the accusation and O's wave both spec it. And the re-home is load-bearing for the cycle cure: this seat's own madge run shows the `browser/admin/index.ts` barrel amplifying ONE bad edge into FOUR cycles — three correctly-written panels dragged in by a sibling. Deleting the browse-seam containment is part of the minimal acyclic cure, not churn. Gate G-22. |
| **L-22 cure direction** | props-only leaf (r2 G-7's zero-inject arm; G-17 mounts "with props alone") | narrow `AdminUsersPort` injected; the 6 props / 7 emits / expose bridge all die; converge with the 3 inject-only siblings | **O wins** | L-22's defect is TWO conventions; any cure must end at one. Convergence-on-props rewrites three sibling panels and keeps AdminPane an adapter; convergence-on-narrow-port is the strictly larger deletion (6 props + 7 emits + the pane's adapter block + the expose bridge) and cures r2 L-2 at its root — the defect was the WIDTH of the port, never injection itself. r2 G-7's inject-count-0 arm is AMENDED by this addendum (see gates): the panel may inject exactly the narrow port key + the shared color keys, all from keys modules (G-19's law), and nothing else. F's position preserved in §3-DISSENT. |
| **D-1 cure / api edit** | demo-only honesty copy; true-count cure BANKED (MT-AU2-API) | additive api/ edit in-wave: `pruneEmptyUsers(token, slugs)` carries the identities | **O wins** | F's own dissent pre-concedes the ruling ("if the arbiter prefers the api edit in-wave, G-10 upgrades"). Structure beats gate: once the request must carry the slugs, a dialog cannot name a number the request does not carry — the fabrication becomes unrepresentable instead of policed. The edit is additive (no-body form remains until cutover); scope stays one service function + one route + one wrapper. G-10 upgraded accordingly. |
| **Wave identity** | MT-AU1, same identity, extended, never renamed | new wave MT-AU2, order-independent, "whichever lands second rebases" | **F wins** | Two waves overlapping on the same two files with mutual rebase obligations is churn by construction, and renaming is the mechanism the anti-rename rule exists to kill. ONE wave: MT-AU1 delta-unified. Every O gate folds in below (G-21..G-24, G-25/G-26 restatements). |
| **Δ-3/C-15 severity** | BLOCKER, with a recorded note that it is the least consequential of the six | not re-derived (O dissent #7) but its C-25 interaction asserted | **BLOCKER sustained** | The compound decides it: without an idempotency key (C-25, verified: 0 admin wrappers carry one) the double-click's two POSTs reach the server as two GENUINELY DISTINCT corpus prunes — the second destroys a set the operator never saw. Unguarded destructive re-entry + no replay protection is BLOCKER as a family. F's least-consequential note preserved in §3-DISSENT. |
| **D-1 headline evidence** | presented the probe cells as measured ("'Pruned 3 users'") | fixture echo — probe-D3f.mjs fulfils the POST with hand-authored `{pruned:3}` | **O wins** | This seat read probe-D3f.mjs:36-38: `route.fulfill(… JSON.stringify({ pruned: 3 }))`. "1 promised, 3 executed" is the fixture author's number and is DOWNGRADED to illustrative; the row stands on the three other legs (filtered-count source, `body: null` observed on real client code, server source enumerating the whole collection). Never re-quote the headline as a server observation. |
| **D-24 390×844 heights** | (rescoped into r2 L-11; no measurement) | 36 px both viewports, both engines, `min-height: 36px`; challenger's 54 px unrecoverable | **O's 36 px binds** | Only reproducible cell; widths agree to a tenth; verdict unaffected either way (`h-7` = 28 px obtains at zero measured cells — the override is inert AND forbidden). Challenger's 54 preserved un-overwritten in §3-DISSENT per O's own discipline. |

## §3 Per-defect verdicts (unified docket)

### The six fresh BLOCKERs — all CONFIRMED (joining §2's L-5, making seven)

- **Δ-1 ≡ D-1 ≡ L-19 ≡ C-2 — prune scope fabrication.** One row, two axes, merged for life (anti-rename). Five links re-verified by this seat at HEAD: panel :241 computes `emptyCount` over the FILTERED `users` prop; AdminPane.vue:28 binds it to `pm.filteredAdminUsers` while :33 binds `total-users` to the UNFILTERED page; dialog :290-291 quotes the filtered number twice; client :69-71 POSTs **no body**; server `findEmptyUserSlugs()` enumerates the **whole collection** and deletes users+sessions transactionally. Compounding (L-19's leg, O live-confirmed: fixture `total: 400` rendered as "5 users"): even unfiltered the number is a ≤50-row page (:59-60, `res.total` discarded — grep 0), and post-prune repair filters page 1 only (:191-193). Wrong in BOTH directions (fleet inverse arm: Prune disabled while empties exist off-view). Evidence correction binding: the "3 executed" is probe-D3f's own fixture. Repro: the five reads above + `grep -c 'res\.total' demo/palettes/useAdminUsers.ts` → 0. Cure: G-10 (request carries the slugs it names).
- **Δ-2 ≡ D-2 ≡ C-21 — five of six mutations resultless; refusal renders as success.** :96-98/:107-109/:122-124/:135-137/:147-149 catch→warn→nothing; prune's channel broken twice (:186-198 `!token`→0 AND catch→0) and :303-308 maps 0 to "No empty users to prune" — refusal, failure and nothing-to-do share one success-register sentence, ten lines below the file's own invariant (:49-50). 12 warns / 4 success-shaped returns, arbiter-measured. Elevates r2 L-7's thrown-prune arm to the family row. Repro: the greps + fleet DELETE-500 cell (dialog closes, rows unchanged, alerts 0). Cure: G-11 typed op-result.
- **Δ-3 ≡ C-15 — destructive confirm double-fires.** All three preconditions static: :283-286 fires the closure then closes, closure NEVER cleared (grep 0); footer confirm :175 has no `:disabled`; dialog leaves by transition (hit-testable window measured by the fleet: 271 ms chromium / 159 ms webkit; two POSTs, one receipt, both engines). BLOCKER sustained on the C-25 compound (no replay key → two genuinely distinct prunes). Cure: G-12 single-fire by construction + G-23 idempotency.
- **Δ-4 ≡ C-1 ≡ C-19 — expand race; wrong-owner destruction reachable.** :352-365 assigns from an awaited fetch with no generation guard; collapse neither cancels nor invalidates; one shared ref serves whatever row is open; :150 wires the card's admin delete to the ROW's `user.slug`, so a mis-rendered palette binds destruction to the wrong owner (badge decrement :118-121). Static mechanism certain; fleet cells (RACE_REPRODUCED, both engines) corroborate. Cure: G-13 `useAdminUserPalettes` settle-guard + owner from the palette's own `userSlug`.
- **Δ-5 ≡ D-3 — keyboard focus ring clipped to zero pixels.** :82's `ring-2` is an outward box-shadow at the row's border box; wrapper :68 is `overflow-hidden` with no padding, so the row's border box IS the wrapper's content box and every ring pixel falls in the clipped region; `outline: none` suppressed both engines (O first-hand: identical boxShadow/outline, remaining signal the 1.02–1.20:1 `bg-accent/50` wash). This row is the ONLY keyboard path to a user's palettes (:70-77's own comment) — WCAG 2.4.7 total loss. D-33's forced-colors arm: CONFIRMED at INFO (box-shadow forced to none; the tracked forced-colors capture is NOT a witness — booked to π, never gated on it). Cure: G-14, probe-asserted.
- **Δ-6 ≡ D-4 — RTL fabricates identifiers.** Slug split across two sibling spans :97-101, zero isolation (bdi grep 0); dialog quotes the raw slug :169 — two identities for one account in a destructive flow. **Mechanism corrected (O, adopted):** the reversal is flex main-axis direction under `direction: rtl` — the spans are blockified flex items (`.slug-pill` call site `flex items-baseline`) — NOT the bidi algorithm; a `unicode-bidi` fix would not cure it (and the bidi arm is separately uncovered). Rider: `mr-1` physical margin :119. Cure: G-15 one LTR-isolated text node.

### CONFIRMED — MAJOR

- **Δ-7 ≡ C-3 ≡ C-16 ≡ L-20 — pagination/search/sort are theatre.** Client declares and forwards limit/offset/q (:21-33); sole call `listUsers(token, 50)`; `res.total` discarded; `q` never sent (filter client-side :29-34); sort reorders an arbitrary page; off-page query renders ROSTER CLEAR with zero requests (fleet N4) — a false negative as truth beside a disabled Prune. PaginationBar sits unused in the subject's own directory (arbiter: consumers = Audit+Flagged only). Feeds Δ-1. Gate G-16.
- **Δ-9 ≡ C-5 — "0 users" above "The roster is unreachable."** :8 guards on `!loading` only; AdminPane :117-122 forks the same way; the file's :5-7 comment states the violated principle. Gate: asserted inside G-11's error-state test.
- **Δ-10 ≡ C-6 — nested-interactive rows.** :85-86 role=button/tabindex on a row CONTAINING two Buttons (:112-130); no aria-controls; WAI-ARIA 1.2 forbids (button = presentational children). The :344-345 target guard and :106 `@click.stop` exist solely to contain the nesting — they die with it. Cure: native disclosure `<button>` beside (not around) the action cluster.
- **Δ-11 ≡ C-7 — the destructive receipt is announced to no one, and a later beat is truncated by an earlier timer.** :16-20 bare span in a Transition, zero role/aria-live in the file; :308 arms a NEW 3 s timer without clearing the last; :46/:135 put aria-label on role-generic divs (C-26's corrected ground: ARIA 1.2 prohibits it — behaviour undefined, engine-dependent, demonstrated not assumed). Cure: op-result surface = role=status region owned by the moved ActionFeedback with teardown (r2 L-7 cure), gates G-11 + r2 G-6.
- **Δ-13 ≡ C-18 — Refresh never invalidates the open disclosure.** Nothing watches the users prop or loading; fleet K3: open row renders GEN-1 after Refresh while re-expand fetches GEN-2. Cure rides G-13's ownership module (reload-on-refresh is a module write path).
- **Δ-14 ≡ C-12 ≡ C-13 — the test substrate cannot see the component.** vitest.config.ts has NO `@vitejs/plugin-vue` (arbiter-read; plugin in devDeps, zero `mount(` anywhere) — **r2's G-6/G-4 unit gates were UNBUILDABLE as specced; G-17 is now their precondition, recorded so no future wave specs jsdom component tests without checking the plugin chain.** And e2e fixtures type-import from the deleted `demo/@` tree (10 refs, invisible under transpile-only). Gates G-17 + G-20.
- **Δ-15 — key homing.** :200 value-imports ADMIN_PORT_KEY from the 272-line wiring module; the repo owns the cure pattern (`color-session/keys.ts`, imported by the same file at :187). Gate G-19 (`palettes/keys.ts`).
- **L-21 — console homed inside the browse feature** (ruled CONFIRMED, see table). The browse barrel self-describes as the palette-browser public API and exports five admin panels (:28-32); the parent lives outside the tree that holds its body; the single genuine dependency runs the other way (PaletteCard). Gate G-22.
- **L-22 — three wiring channels at once, two cluster dialects** (ruled for the narrow-port direction). Subject: 6 props + 2 injects + 5-member expose simultaneously; siblings: 3 inject-only, 1 props-only. Gate G-25.
- **L-24 — four cycles, no gate can see any of them.** Arbiter-reproduced byte-identically. madge not a dependency; no `import/no-cycle`; `import type` under verbatimModuleSyntax erases the runtime edge — vue-tsc structurally blind. The row that converts r2 L-1 from argument into measurement. Gate G-21.
- **C-23 — a request that never answers leaves the control terminally spinning.** `pruning` set :295, cleared only by `onPruneDone` (:303), called only after the port's await settles (usePalettePorts :121-124, arbiter-read); AbortSignal in demo/ = **0** (arbiter). Server prune runs one transaction — the exact slow condition this control provokes. Cure at the transport ONCE (G-24), never a per-component watchdog.
- **C-24 — the identifier arrives broken: clipboard newline, accname split.** Two instruments, two engines, one root cause: the two-box pill under `display:flex` blockifies the spans; innerText/selection gain a newline, accname gains a space (`ghost-empt y-1733`) while the sibling delete button announces the UNSPLIT slug — one account, two names, in LTR. Cure: one text node, CSS tail-priority truncation, `slugHead`/`slugTail`/`SLUG_TAIL` (:246-252) die. Gate G-15.
- **C-25 — replay protection shipped, withheld from every admin destructive verb.** Server mounts idempotency globally; transport forwards the header from both helpers; callers: 3 hits all in palettes.ts, **0** in admin wrappers (arbiter). Saving a palette is replay-protected; corpus-wide prune is not. Rider upheld: the scope key may collapse an authenticated admin to `anon` — admin identity belongs in the key as part of the cure. Gate G-23.
- **C-26 — evidence correction of the r2-era C-20.** Adopted as the docket's methodological anchor: Playwright's `ariaSnapshot` is a normalised approximation, not the browser's tree; CDP `Accessibility.getFullAXTree` shows Chromium computing the name r2's C-seat said "both engines discard". The row it corrects survives on re-measured legs (ARIA prohibition + the triple "Loading" regions). A formation that only rewards new findings will not get corrections like this.
- **D-25 — one sentence, two denominators.** :8 UNFILTERED `totalUsers` · :11 FILTERED `emptyCount`, one interpunct apart — bindings arbiter-verified; the numerator is exactly what the dialog quotes. Cure: one received `RosterCounts`; compounded by Δ-7 (the "total" is itself a page).
- **D-28 ≡ L-6 — the anatomy fork shifts the identity column 44 px between Admin routes.** Arithmetically derived from class strings (56 px vs 12 px content start); folds into r2 L-6's one-home cure; the skeleton's dishonesty is its second consequence.
- **D-24 — the `h-7` override is both forbidden and inert.** O's cell binds (36 px at both viewports, both engines, `min-height` wins; 28 px never obtains); five dead geometry declarations read as contract. Folds into r2 L-11 / MT-BTN1. Rider: icon-only deletes 28×36 at 390 — sub-44 tap targets.

### CONFIRMED — MINOR / INFO

- **Δ-17** — one global `expandedId` on three ports, two ID namespaces (arbiter: one ref :24, written from `palette.id` :76 and slug-shaped `id` :81, compared against `palette.slug` :144). Folds into the banked adminPort split.
- **Δ-18 ≡ L-23** — three dead exports, 0/0/0 consumers (arbiter); `onImpersonate` is the sole reader of a live transport wrapper; the layer below purged this exact class one wave earlier and did not climb. Deleted in-wave; the deletion is the proof (Δ-ledger row + G-25 arm).
- **D-26** — same datum twice, 125 px apart, identical type (O's cell; source = one expression bound twice, AdminPane :122 + :33, arbiter-verified).
- **D-33** — forced-colors: focus treatment does not exist there (box-shadow forced off), AND the tracked forced-colors capture is indistinguishable from a light capture — a MATRIX-COVERAGE defect against the audit apparatus; carried into π obligations, never a code gate.
- **L-13** — `export.ts` + `export/` ambiguous specifiers; rename rides MT-UI1.
- **Δ-19 — design-canon cluster** (D-5/6/7/8/10/11/15/17/18/19/25-26 riders/30-31 halves/32-our-half): CONFIRMED as a class; four members statically certain (two-denominator sentence, duplicated datum, disclosure with zero visual open-state, two irreversible actions sharing Trash2 :119/:129/:176); remainder adopted as the fleet's chromium/webkit cells, landing in the wave's design pass under π obligations — never gated on eyeball re-measurement. No glass-owned visual re-booked (prefix-trap law honoured).

### RESCOPED

- **Δ-8 ≡ C-4 ≡ D-27** — filed mechanism REFUTED (see ruling); survives as: a FAILED nested read renders `· none pinned · / No palettes.` — the L-5 disease applied to the exact predicate Prune deletes accounts on (O live-confirmed under an injected 500, both engines, `unhandled: []`). Cure inside G-11's typed load family.
- **D-29** — half one CONFIRMED and strengthened (disclosure named after the whole row including "Delete"); half two ("count fuses onto slug") REFUTED as a reconstruction artefact (L-12: a bare-page rebuild cannot carry the Tailwind cascade) and REPLACED by the sharper true fact: the SLUG ITSELF splits in the accname. Cure unchanged, doubly motivated (G-15).
- **Δ-12 ≡ C-8 (+ C-9/10/11/22 riders MINOR)** — rescoped from "white-screens" to "caught at panel granularity, console fully denied" (:247-251 unguarded `slug.length`; ErrorBoundary replaces the whole console). Cure: total slug renderer + typed narrowing; G-17's malformed-row mount smoke.
- **Δ-16 ≡ L-15** — "match ZERO files" literally true for G-DEMO-1/3a; 3b matches ~4 live files but bans only dead aliases — net: the regime guards nothing where the subject lives (arbiter: print-config → 0). Substance confirmed, precision corrected. Gate G-18.
- **L-1 refile (×4 at BLOCKER)** — books ONCE into the §2 identity; retirement STANDS; ONE new arm folded: the remotePalettes divergence (:112-125 never writes `deps.remotePalettes` while :101-110 filters it; :87 proves overlap) — staleness not corruption, MAJOR, same cure, aligned with App.md W-PICKER-2 / ColorPicker.md L-8/D-16 (provided-not-exposed).
- **C-17 ≡ L-12 ≡ D-9** — corroboration of §2's L-5 blocker exactly as ruled; books once; gates G-4a/b unchanged.
- **Refile batch** — D-23→L-3 (with O's new DOM discriminator: literal `variant="outline"` attr beside `data-emphasis="secondary"` — declared prop as inert markup) · L-4-dialects · god-port→L-2 · row-grammar forks→L-6 · confirm-home→L-8 · safeAccent→L-9 · Transition→L-10 · h-7→L-11: all land in standing §2 rulings, MT-AU1/MT-UI1/MT-BTN1 cures, and the BH riders. No refile adds an arm §2 lacks.
- **L-7 widening (L-8 idx24 ≡ D-16 idx25)** — §2 ruling stands, widened with two verified facts: glass-ui 7.0.0 SHIPS `./toast` (arbiter-verified) — the ActionFeedback move must first answer "why not toast?" in one recorded line; and the beat is a flex sibling that displaces the very controls that caused it (fleet: 105 px).
- **D-30** — scale-inversion half CONFIRMED first-hand (status 16.4 px mono vs commands 14.38 px Fraunces italic — also confirms pass-2 D-6); the 176/68 px void figures rescoped to empty-roster-at-512px, not carried as general geometry.
- **D-31** — pixel ink-band arm **UNVERIFIABLE-HERE** (untracked capture, no producer script committed — L-6/L-7 refusal to launder; the missing arm is a TRACKED capture with a committed producing probe); statically-certain half stands: page-level empty ceremony at row scale, `dots` never passed (arbiter: prop exists default-true, passed 0 times).
- **L-16** — verifiable half only (the `?? 0` masking is real at :103/:241); the wire-shape divergence half **UNVERIFIABLE-HERE** (needs the live API; the missing arm is an API-backed cell). Folds into Δ-12's typed narrowing.

### GLASS-OWNED (rows — cite INBOX I-20; masking-fallback ban binding, no local patch)

- **D-32 — disabled Prune is opacity-only (colour/background byte-identical to enabled; opacity 0.5 the sole channel — O measured).** The subject sets no disabled styling; the treatment is glass-ui Button's own. Leaves by the standing BH relay with the allowlist letter. OUR half stays in-wave: the operator is never told WHY Prune is dead, and a disabled button leaves the tab order — a result-channel obligation folded into G-11, never a colour patch.
- **Dialog glass-material grade** (`DialogContent surface="glass"` on a destructive confirm) — producer-owned material question; travels as a BH-relay question on I-20 (does an opaque surface variant exist for destructive confirms?), never a value.js row.
- Standing riders (§2 + delta): Button attribute allowlist · xs/36 px height rung · ConfirmDialog preset · `Chip` primitive (would retire `.slug-pill`'s per-instance `:style`).

## §3 The wave — MT-AU1 delta-unified (ONE wave; re-authored, best of both, corrected)

**waveId**: MT-AU1 (same identity as §2, extended, never renamed) · **bornRed**: true
**title**: AdminUsersPanel becomes a truthful leaf with one honest wiring: the scope it states is the scope the request carries, every action answers in a typed register, one fire per confirm, one owner per disclosure, one text node per identifier, a focus ring that paints, a console homed where a cycle gate can police it — plus the test/lint/graph substrate that makes every gate buildable.

**Scope (delta over §2's, which stands)**: (1) `useAdminUsers.ts` — typed `Result` op-channel for all six mutations + the load family (12 warns→0; 4 success-shaped returns→0); roster becomes page-aware (`res.total` consumed, `q`/offset forwarded, the client-side filter dies); dead exports deleted; `:14`/`:27` handle lines die. (2) `api/admin-users.ts` — `pruneEmptyUsers(token, slugs: string[])`; every destructive verb gains `idempotencyKey: crypto.randomUUID()`; `impersonateUser` deleted with its dead reader. (3) **the one additive api/ edit**: `api/src/modules/admin/service/users.ts` + route accept the slug list and audit per-slug membership (no-body form remains until cutover). (4) `demo/platform/transport/client.ts` — `AbortSignal.timeout` composed once for ~30 wrappers. (5) the panel — honest prune dialog naming exactly the carried slugs; status-region beat (role=status, cancellable, one home = moved ActionFeedback with teardown; the `./toast` question answered in one recorded line first); ONE `<bdi dir="ltr">` text node per slug with CSS tail-priority truncation (`slugHead`/`slugTail`/`SLUG_TAIL` deleted); native disclosure `<button>` sibling to the action cluster (the :344 guard and :106 `.stop` die with the nesting); focus indicator on a paint-owning element; count guarded on error; `RosterCounts` from the port; PaginationBar mounted; total slug renderer (null/empty tolerant); `dots={false}` at :138; `me-1`; open-state marker. (6) **console re-home**: `demo/palettes/admin/` becomes the whole home (AdminPane + five panels + AdminListItem/Skeleton + PaginationBar + new index.ts seam); `browser/index.ts` loses its five Admin exports; `browser/admin/` ceases to exist; PaletteCard becomes one explicit cross-feature import. (7) **wiring convergence (ruled)**: narrow `AdminUsersPort` split from the 34-member adminPort; `adminUsersPanelRef` deleted everywhere; the subject loses all 6 props, all 7 emits, and its defineExpose, converging with its inject-only siblings; keys re-homed to `palettes/keys.ts` (+ ADMIN_USERS_PORT_KEY), injectors import keys modules only. (8) NEW `useAdminUserPalettes.ts` — settle-generation guard, refresh invalidation, owner from the palette's own `userSlug`. (9) substrate: `@vitejs/plugin-vue` into vitest.config.ts; eslint G-DEMO globs re-pointed at the live tree + `.ts`-never-imports-`.vue` ban; madge into devDeps wired into `npm run lint`; e2e fixture types re-pointed at `demo/palettes/types`. (10) NEW tests `test/demo/{admin-mount-smoke,admin-prune-scope,admin-op-result,confirm-single-fire,admin-user-palettes-race,admin-users-search,transport-deadline}.test.ts` + committed probes under `audit/waves/MT-AU1/` (PNGs force-added). NO glass-ui edits (riders via BH relay).

**Structure over gates (L-8 accounting)**: made UNREPRESENTABLE — a client-computed blast radius (the request requires the identities); a page mistaken for a corpus (the roster type carries total/offset/limit); a swallowed outcome (Result makes ignoring a failure a type error); a broken identifier (one text node cannot be split by a layout mode — copy, innerText, accname identical by construction); double-fire (the confirm closure is consumed, not invoked); the layer inversion (no handle exists to reach through; the re-home makes the edge absurd); an unbounded wait (the transport owns the deadline); the leaf→wiring value edge (keys module). Gates only where structure is unavailable: focus paint, bidi isolation, lint globs, pagination adoption, the module graph (an absence of permission, not machinery), and the dead-`variant` class (vue-tsc exit 0 — that gate may NEVER be cited for it).

**Gates** (§2's G-1..G-9 stand with two amendments; delta gates below; every RED input recorded):

- **AMENDMENT to G-7**: the zero-`inject(` arm becomes "injects ONLY keys-module symbols: ADMIN_USERS_PORT_KEY + shared color keys; imports of `usePalettePorts` in the panel = 0" (the L-22 ruling). The h-7 and Transition arms stand.
- **AMENDMENT to G-6/G-4**: G-17 is their build precondition (they were unbuildable as specced — recorded so it is never re-discovered).
- **G-10 · scope truth (upgraded per ruling)**: `npx vitest run test/demo/admin-prune-scope.test.ts` + probe arm: the prune POST body carries `{slugs:[…]}` whose length equals the dialog's numeral, both engines. RED today: body `null` measured on real client code; dialog quotes a filtered ≤50-row count; server enumerates the whole collection.
- **G-11 · typed op-result**: `test "$(grep -cE 'return 0;|return \[\];' demo/palettes/useAdminUsers.ts | tr -d ' ')" -eq 0 && test "$(grep -c 'console\.warn' demo/palettes/useAdminUsers.ts)" -eq 0 && npx vitest run test/demo/admin-op-result.test.ts` — RED: 4 / 12 / file absent. Asserts: 500-prune renders in the error register; 500-DELETE leaves a visible failure; failed nested read never renders "No palettes."; "0 users" never renders above the error plate; disabled Prune carries its reason.
- **G-12 · single-fire confirm**: `npx vitest run test/demo/confirm-single-fire.test.ts` — RED: file absent; closure never cleared (grep 0); :175 no `:disabled`; two POSTs measured.
- **G-13 · disclosure ownership**: `npx vitest run test/demo/admin-user-palettes-race.test.ts` — RED: file absent; race + stale-survive-refresh + wrong-owner emit all live.
- **G-14 · the focus indicator paints**: `node docs/tranches/V/megatranche/audit/waves/MT-AU1/probe-au1-focus.mjs` — ≥3:1 and >0 changed pixels outside the row interior, chromium+webkit × light+dark. RED: 4 cells at 1.02–1.20:1, ring contributes zero pixels.
- **G-15 · one identifier**: `F=<panel>; test "$(grep -c 'slugHead\|slugTail' $F)" -eq 0 && grep -q '<bdi' $F` + probe arms: pill innerText === slug; disclosure accname starts with the slug and contains no "Delete"; RTL cell tailLeftOfHead false. RED: 3 / 0 / split accname / reversed lefts measured.
- **G-16 · pagination truth**: `test "$(grep -c 'res\.total' demo/palettes/useAdminUsers.ts)" -ge 1 && npx vitest run test/demo/admin-users-search.test.ts` — RED: 0 / absent; fixture total 400 rendered as "5 users"; off-page query = ROSTER CLEAR with zero requests.
- **G-17 · vitest can mount an SFC**: `grep -q 'plugin-vue' vitest.config.ts && npx vitest run test/demo/admin-mount-smoke.test.ts` (incl. malformed-row fixture → N-1 live rows + one inert flagged row, console alive) — RED: plugin absent, zero mounts exist.
- **G-18 · boundary lint guards the live tree**: `npx eslint --print-config demo/palettes/useAdminUsers.ts | grep -q 'no-restricted-imports'` — RED: 0 today (dead globs, arbiter-verified).
- **G-19 · keys re-homed**: panel imports of `usePalettePorts` = 0 AND `test -f demo/palettes/keys.ts` — RED: import at :200, file absent.
- **G-20 · e2e fixture truth**: `! grep -rqn 'demo/@/' e2e/` — RED: 10 refs.
- **G-21 · the graph is acyclic and a gate can see it**: madge in devDeps, wired into `npm run lint`, `npx madge --extensions ts,vue --ts-config tsconfig.json --circular demo/palettes` exits clean — RED: not a dep, not wired, **4 cycles (arbiter-reproduced)**.
- **G-22 · the console has its own home**: `test "$(grep -c 'Admin' demo/palettes/browser/index.ts)" -eq 0 && test -f demo/palettes/admin/index.ts && test ! -d demo/palettes/browser/admin` — RED: 5 / absent / exists.
- **G-23 · replay protection**: `test "$(grep -c 'idempotencyKey' demo/palettes/api/admin-users.ts)" -ge 4` — RED: 0 (palettes.ts has 3; admin wrappers 0; server mounts the store globally).
- **G-24 · a request that never answers must end**: `test "$(grep -c 'AbortSignal' demo/platform/transport/client.ts)" -ge 1 && npx vitest run test/demo/transport-deadline.test.ts` — RED: demo-wide count 0.
- **G-25 · the wiring converges**: InstanceType `.ts`-scoped in demo/palettes = 0 (the `--include='*.ts'` scoping is LOAD-BEARING and must not be dropped — unscoped counts 6/16 include ordinary child refs) AND defineExpose = 0 AND defineProps = 0 AND `adminUsersPanelRef` demo-wide = 0 AND dead-export references = 0 — RED: 2 / 1 (5 members) / 1 (6 props) / 10 / 10 lines.
- **G-26 · witnesses exist in git (wave scope)**: `test "$(git ls-files docs/tranches/V/megatranche/audit/waves/MT-AU1/ | grep -c png)" -ge 8` — RED: 0; repo-wide 14 tracked PNGs, zero witnessing this component.

**π obligations**: §2's set stands PLUS: focus before/after pixel-diff pairs (chromium+webkit × light+dark, JSON beside PNGs, `git add -f`); one `dir=rtl` long-slug cell; one error-state cell (500 → no "0 users" above the plate); one populated-roster cell per engine with the `page.route` fixture declared in the JSON header (a populated screen is a SIMULATION of the server). Real Safari 26.4 via safaridriver :4599 = a separate cell owed to the first API-backed pass, never inferred (I-20). The forced-colors arm is UNCOVERED by the tracked matrix (Playwright forcedColors not honoured by WebKit; the existing capture is an ordinary light shot) — owed there too, never gated here.

**Δ obligations (before→after, both halves committed)**: prune POST body null→`{slugs:[…]}` = dialog numeral · warns 12→0 · success-shaped returns 4→0 · confirm closure cleared never→on-fire + in-flight disable · settle-guard/refresh-invalidation absent→present · focus 1.02–1.20:1/0 px→≥3:1 in 4 cells · slugHead/slugTail 3→0, `<bdi>` 0→≥1, pill innerText == slug, accname unsplit both engines · `mr-1`→`me-1` · res.total 0→≥1, q sent never→on-search, PaginationBar consumers-in-dir 0→1 · idempotencyKey admin wrappers 0→≥4 · AbortSignal 0→≥1 · madge cycles 4→0, madge dep false→true · Admin exports in browse barrel 5→0, `browser/admin/` present→gone · InstanceType (.ts, palettes) 2→0 · defineExpose 5-members→0 · defineProps 6→0 · adminUsersPanelRef 10→0 · dead-export refs 10→0 · panel imports of usePalettePorts 1→0, keys.ts absent→present · vue plugin absent→present, SFC-mounting tests 0→≥6 · live-tree lint rules 0→≥1 · demo/@ e2e refs 10→0 · dots passed 0→1 site · tracked wave PNGs 0→≥8 · plus every §2 Δ row unchanged.

**Carries**: all §2 carries stand verbatim (MT-UI1 48→0 · MT-BTN1 22→0 · BH riders · adminPort five-seam BANK — Δ-17 and the AdminNamesPanel-convergence question fold into it; L-13 rides MT-UI1). Discharged, recorded so nobody re-books: §2's G-9 is GREEN (challenge-D harvested — 13 refs; this delta is its adjudication). The MT-AU2-API preview bank is CLOSED — superseded by the ruled in-wave slugs edit. Retired with rationale: D-29's fusion claim (reconstruction artefact); D-27's no-catch/unhandled mechanism (refuted at :153-162 + `unhandled: []`); D-1's "3 executed" as a server measurement (fixture echo, probe-D3f.mjs:37).

**completableAlone**: YES. ~16 files under demo/palettes/** + demo/shared/ui/** + demo/platform/transport/client.ts + demo/shell/viewSchema.ts + vitest.config.ts + eslint.config.js + package.json + e2e fixtures + test/demo/** + committed probes; ONE additive api/ edit (ruled in). All gates are scoped greps, one madge run, jsdom vitest, or dev-server probes with `page.route` fixtures — runnable in a fresh checkout after `npm i`, no live mongo. If this delta-unified wave were the only wave ever executed: the console stops promising one deletion while the request authorizes an unbounded one; failures stop wearing success and emptiness costumes; a double-click stops meaning two corpus prunes; one user's palettes stop rendering under another's name with destruction wired to the wrong owner; keyboard operators can see where they are; the identifier survives RTL, the clipboard, and the screen reader as one string; the console lives where a cycle gate can see it; and the tests, lint, and graph assertions that guard all of it actually compile and actually match the tree.

## §3 ADDENDUM CLAUSE

ADDENDUM MT-AU1/§3 · AdminUsersPanel r3/r4-delta tri-fold arbitration, 2026-07-28, HEAD `c654824e`, arbiter-F (`claude-fable-5`, observed). Two blind workers (Fable, Opus) ruled partially DISJOINT slices of the delta corpus — itself recorded as a formation census defect: the spawn brief's "25-defect corpus" reconstructs to no seat, sum, or dedup (worker-F measured 139 raw rows / 8 seats) and must not be re-quoted. Unified docket: **29 CONFIRMED / 12 RESCOPED / 0 REFUTED at row level / 2 GLASS-OWNED rows**, with three named mechanisms refuted INSIDE surviving rows (D-27's no-catch, D-29's fusion, D-1's "3 executed" headline — each retired with rationale so it cannot be re-quoted). Six fresh BLOCKERs join §2's L-5 (seven standing). Eight inter-worker disagreements ruled: **six for worker-O** (D-27 label under the §2 label law; L-21 in-wave console re-home — the arbiter's own madge run showing the barrel amplifying one bad edge into four cycles decided it; L-22 narrow-port convergence, amending §2's G-7 inject arm by addendum; the D-1 api-shaped cure landing in-wave as one additive edit, per worker-F's own pre-concession; the D-1 headline downgraded to fixture-echo after the arbiter read probe-D3f.mjs:37; D-24's 36 px binding), **one for worker-F** (ONE wave — MT-AU1 extended, never renamed; worker-O's MT-AU2 gates folded in whole), **one sustained-with-note** (Δ-3 BLOCKER on the C-25 compound: two genuinely distinct corpus prunes from one double-click). Two corrections are RECORDED SO THEY ARE NEVER RE-DISCOVERED: §2's G-6/G-4 unit gates were UNBUILDABLE as specced (vitest has no vue plugin; zero SFC mounts exist — G-17 is now their precondition), and the demo's G-DEMO boundary-lint regime is dead where the subject lives (globs over the deleted `demo/@` tree; `--print-config` on the subject → 0). The r2 record otherwise STANDS whole, including the L-1 retirement (attacked with the r4 cycle evidence; not overturned) — L-1 gains one arm (remotePalettes divergence, staleness not corruption). GLASS-OWNED rows and riders leave by the standing BH relay (INBOX I-20) under the masking-fallback ban; no spectrum-seam visual re-booked (prefix-trap honoured). Worker records at `registry/adjudicated/delta-r3/AdminUsersPanel.worker-F.md` (as declared) and `audit/components/AdminUsersPanel/probe-O-r3/` (banked, verified on disk).

## §3 DISSENT (preserved unresolved disagreement)

1. **Worker-F's severity note on Δ-3/C-15, preserved verbatim in substance**: it is "the least consequential of the six: the second prune is near-idempotent server-side and a second per-user DELETE 404s, so the harm is duplicate destructive dispatch + audit-log noise rather than compounding data loss." The arbiter sustained BLOCKER on the C-25 compound; F's ground remains the best argument for MAJOR and is preserved for the formation-level severity canon.
2. **Worker-F's wiring position (props-only leaf), preserved**: F held the r2 G-7 line — zero inject, mount with props alone — and its G-17 smoke test was written against it. The ruling went to the narrow injected port (larger deletion, cluster convergence); F's position is recorded so the props-vs-port question is settled here ONCE and not re-litigated per panel. The AdminNamesPanel remainder (still props-only after this wave) is the acknowledged residual, bound to the adminPort five-seam BANK.
3. **D-24's 390×844 heights, unresolved between the challenger (54 px) and worker-O (36 px, both engines)**: the widths agree to a tenth; the divergence is height-specific and unrecovered. O's cell binds (only reproducible one); the challenger's number is preserved un-overwritten per O's own discipline. No verdict turns on it.
4. **Worker-O's dissent #7 against declaring the component adjudicated, endorsed and answered**: O did not re-derive C-1..C-21; worker-F did (its Δ-clusters), and this apotheosis unions the two dockets — the coverage hole O feared is closed by the union, not by either seat alone. Recorded because the census defect that caused it (two "same-corpus" workers holding different dockets) is a spawn-harness fault the formation must fix before the next tri-fold.
5. **Worker-F's silent mechanism-correction on Δ-8/D-27, recorded as a hazard**: F's note restated the TRUE mechanism (the composable catch) while labeling the row CONFIRMED — adjudicating the defect it wished had been filed rather than the one that was. O refuted the filed mechanism aloud. The formation lesson: a worker that silently repairs an accusation's mechanism launders the accusation's credibility; refute aloud, then rescope.
6. **Worker-O's L-12 lesson, preserved verbatim in substance**: "a reconstruction is not the component, and a seat that reconstructs markup to measure a computed-style-dependent property has built a witness that cannot see its subject" (D-29's fusion; the bare-page rebuild lacked the Tailwind cascade). Adopted at formation scale.
7. **Worker-F's census dissent, preserved**: the brief's "25" corresponds to nothing reconstructable; every downstream quotation of it should cite this addendum instead.
