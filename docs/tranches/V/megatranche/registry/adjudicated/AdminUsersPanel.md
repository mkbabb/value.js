# AdminUsersPanel — adjudicated apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed, not inherited.
**Date**: 2026-07-27 · **HEAD**: `c654824e` · **Corpus**: 11 CHALLENGE-L accusations (L-1..L-11) against `demo/palettes/browser/admin/AdminUsersPanel.vue`.
**Workers adjudicated**: worker-F (Fable 5, `admin-users-panel-worker-f-adjudication`) · worker-O (Opus 5 [1m], `mt-au1-adminuserspanel-adjudication`).
**Method (L-14)**: both workers were attacked before either was adopted. Every disagreement was re-derived against the tree by this seat; the suspiciously-convergent agreed verdicts (L-2, L-3, L-5, L-6, L-8, L-10) were each independently re-measured rather than accepted. Arbiter's own instruments this session: full source reads of AdminUsersPanel.vue (391 lines, whole) / useAdminUsers.ts (whole) / usePalettePorts.ts:100-240 / AdminPane.vue:20-135 / useSlugMigration.ts (key lines) / ActionFeedback.vue (whole) / ColorPicker.vue:325-354 / viewSchema.ts:185-200 / usePaletteWiring.ts:140-155 / usePaneRouter.ts:155-175; eleven scoped counting instruments (each command recorded below with its output); one `npx vue-tsc -p tsconfig.demo.json --noEmit` run (exit 0 — the blindness measured, not assumed); `git ls-files` against every contested witness.
**Environment disclosure (L-12)**: no browser probe was run by this seat — every contested verdict decided statically; worker-O's live chromium measurements are cited only where my static re-derivation independently reached the same fact, except the G-3b/G-4a DOM discriminators which are adopted AS worker-O's cell (chromium 1.60.0 @1440×900) and never transferred to WebKit/Safari (I-20). Dev server :9000 is API-LESS — every data-backed arm (populated roster, live prune, expanded palettes) is UNVERIFIABLE-HERE and none of the eleven verdicts rests on one.

## Scoreboard

**7 CONFIRMED (L-2, L-3, L-5, L-6, L-8, L-10, L-11) · 4 RESCOPED (L-1, L-4, L-7, L-9) · 0 REFUTED · 0 UNVERIFIABLE-HERE at row level · 3 GLASS-OWNED riders (never rows).**
Blocker survivor: **L-5** — an unauthenticated stranger on `#/admin/users` is told as fact that the roster is empty (`· roster clear · / No users found.`) and handed operable Prune/Refresh controls, on a route with no auth meta. The L-1 BLOCKER grade is retired by both workers and by this seat's own re-derivation; L-1 survives as the MAJOR structural spine.

Arbiter measurement record (all this session, all re-runnable in a fresh checkout):

| fact | command (abbreviated) | measured |
|---|---|---|
| InstanceType refs in demo/ | `grep -rn --include='*.ts' 'InstanceType<typeof' demo/` | **3** (useAdminUsers.ts:27 · useSlugMigration.ts:30 · usePaletteWiring.ts:27) |
| defineExpose sites demo-wide | `grep -rn defineExpose demo/ --include='*.vue'` | **14**; ColorPicker.vue:331 = **12 members**, AdminUsersPanel.vue:389 = **5** |
| adminUsersPanelRef occurrences | `grep -rn adminUsersPanelRef demo/ \| wc -l` | **10** |
| adminPort members | hand-count usePalettePorts.ts:195-231 | **34** (14 user + 13 queue + 7 cross) |
| `pm.` executable uses in panel | read | **1** (:361; :233 is a comment) |
| ADMIN_PORT_KEY injectors | `grep -rln 'inject(ADMIN_PORT_KEY' demo/` | **5 files** |
| `<Button variant=` sites | python census over `<Button…>` opening tags | **51 in 22 files**; subject 5 (tags 21/32/58/112/122) |
| `variant`/`outline`/`ghost`/`inheritAttrs` in glass Button chunk | `grep -c … dist/button-Bu9F4uU6.js` | **0 / 0 / 0** |
| vue-tsc over the 51 dead props | `npx vue-tsc -p tsconfig.demo.json --noEmit` | **exit 0** (structurally blind) |
| demo/ui entries · barrel importers · both-paths | `ls demo/ui \| wc -l` · O's regex · comm | **19 · 48 · 24** |
| `.vue`-path imports in .ts · of them in useAdminUsers | grep | **25 · 0** |
| row-grammar homes in admin/ | `grep -rn 'items-center gap-3 px-3 py-2.5' …/admin/` | **5** (atom :11 + UsersPanel :80 + Skeleton :9 + Audit :62 + Flagged :50) |
| `if (!token)` in useAdminUsers.ts · demo/palettes | grep -c | **11 · 22** |
| SAFE_ACCENT_KEY injectors | `grep -rln 'inject(SAFE_ACCENT_KEY' demo/` | **6 files** |
| explicit `Transition` import · `<Transition` users | grep | **1 (:186) · 17** |
| `<Button>`-tag `h-<n>` sites | python census | **22 in 11 files**; subject 4 |
| tracked PNGs under megatranche | `git ls-files docs/tranches/V/megatranche/ \| grep -c png` | **0** (`.gitignore:34 *.png`, `!demo/**/*.png` only) |
| challenge-D refs in palettes harvest · DEFECT-LEDGER | grep -c | **0 · 0** (the 33KB file EXISTS at `audit/components/AdminUsersPanel/challenge-D-design.md`) |
| confirm engine homes | `grep -rln confirmOpen demo/ --include='*.vue'` | **1** (the subject) |
| ActionFeedback `onUnmounted` | grep -c | **0** |

## Ruled disagreements (the tri-fold's actual work)

| id | worker-F | worker-O | RULING | Deciding evidence |
|---|---|---|---|---|
| **L-1 "largest defineExpose"** | AdminUsersPanel's 5 members is "largest of the 14 sites" | ColorPicker.vue:331 = 12 members; 5 is joint-second | **O wins** | This seat read ColorPicker.vue:331-344: **12 members** (isEditing…actionBarContext). F's uniqueness restatement is refuted; the inversion itself is unaffected. |
| **L-1 class width** | Single-subject rescope | demo/palettes CLASS of 2 (+ useSlugMigration.ts) | **O wins** | useSlugMigration.ts:6 barrel type-import, :30 InstanceType ref, four `slugBarRef.value?.setError(…)` calls, :121 return member — read verbatim by this seat. Same shape, same directory. F never looked. |
| **L-4 verdict** | CONFIRMED (counts "corroborated within noise": 25 both / 49 barrel) | RESCOPED (24 both / 48 barrel; ledger's 35 REFUTED) | **O wins** | This seat measured: barrel-importer files = **48**, both-paths = **24** (comm of sorted file lists). The ledger's 35 is refuted, not noise-corroborated; F's 25/49 are each off by one. A row that survives on counts carries O's. |
| **L-5 witness standing** | "committed webkit shot … present on disk" | UNTRACKED — fails L-7; finding survives on code + live re-run | **O wins** | `git ls-files --error-unmatch …/safari-desktop-light/admin-users.png` → *"did not match any file(s) known to git"*; `.gitignore:34` is `*.png`. Present-on-disk ≠ committed. The verdict stands on the static path (below), not the screenshot. |
| **L-7 verdict** | CONFIRMED (window "narrower than stated but real") | RESCOPED (race needs first-prune→0; "leak" retired; cure incomplete — atom lacks unmount teardown) | **O wins** | This seat verified: `:disabled="emptyCount === 0 \|\| pruning"` (:25) makes the double-beat reachable only when the first prune returns **0** — and useAdminUsers.ts:195-198 `catch → warn → return 0` makes the failure branch the realistic path (the sharper defect: a THROWN prune renders the success-family copy "No empty users to prune"). ActionFeedback.vue: `onUnmounted` count = **0** — the moved atom must gain teardown or 12+ consumers inherit the hole. F's confirm was right on substance, blind on the cure. |
| **L-9 verdict** | CONFIRMED (with count correction) | RESCOPED (mechanism confirmed, both numbers refuted) | **O wins (label)** | Substance identical in both returns; injectors measured **6** (not 11) by this seat. Under L-9 law — which worker-F itself invoked against the challenger — a row whose load-bearing counts are refuted is RESCOPED to the mechanism with corrected counts, not confirmed-with-asterisk. |
| **L-11 fleet count** | 13 by a 3-line window ("methodology-sensitive") | 22 in 11 files, listed per-file | **O wins** | This seat's opening-tag census: **22 sites in 11 files**, file list byte-identical to O's. F's window measure undercounted; F itself flagged it as unreliable. |
| **L-13 gap** | "PaletteCard seat contributes zero rows naming this file" (checked the wrong absence) | challenge-D's 18 rows exist on disk, absent from every harvest and the ledger | **O wins** | `audit/components/AdminUsersPanel/challenge-D-design.md` = 33,498 bytes on disk; `grep -c challenge-D registry/harvest/area-palettes.json` = **0**; DEFECT-LEDGER refs = **0** — while three OTHER areas' harvests do cite their challenge-D seats, making the palettes omission conspicuous. Booked as born-RED gate G-9, not a deferral. |

Convergences verified rather than trusted (both workers agreed; this seat re-measured): L-2's 34/1 and the 6-prop/7-emit dual path; L-3's absent `variant` in Button.vue.d.ts:4-19 + 0 chunk hits + the 51-site census + vue-tsc exit 0 (**reproduced by my own run**); L-5's static path (useAdminUsers.ts:54-56 early-return before any loading/error write → panel :63); L-6's five homes; L-8's 1-of-3 confirm coverage (Flagged **:98** direct `deletePalette`, Tags raw `<button>` :98 with `deleteTag` at :101 — O's line exact, F's ":99" off by one); L-10's 1-of-17. The L-1 no-harm mechanism is a genuine convergence by two independent routes: F from pane-keying (verified by this seat: usePaneRouter keys PaneSlots by view name — `key: left` — so `subView` is fixed per AdminPane instance and a sub-view switch deactivates rather than remounts; App.vue KeepAlive `:max` = 9/:88, 6/:107, 4/:133 — both workers' App.vue line cites drift, the values are exact), O from the vue 3.5.35 runtime (deactivate moves the subtree without nulling the ref; unmount nulls the ref via setRef). Either route ends the same: deactivated ⇒ ref valid ⇒ the `?.` call LANDS on the cached instance; unmounted ⇒ ref null AND the script-setup state it would have cleared no longer exists; remount reinitializes :236-238. **No arm exists in which stale `userPalettes` survives to a remount.**

## Per-defect verdicts

### L-1 — state module holds the panel's instance handle · **RESCOPED** (BLOCKER→MAJOR structural class)
Every cited line exact at HEAD: useAdminUsers.ts:14 (SFC type via the `./browser/admin` barrel), :27 (InstanceType ref), `?.` mutations :95/:117/:132/:146; usePalettePorts.ts:123 + :197; AdminPane.vue:27 template ref + :134 alias; AdminUsersPanel.vue:389 (5 members incl. mutable `userPalettes`). The latent import cycle is real, erased at runtime only by `import type` + verbatimModuleSyntax. **Refuted**: the data-corruption reproduction (unreachable — see convergence note above); the "25 = 24 barrels + this file" arithmetic (the grep matches the subject **0** times — measured); "largest defineExpose" (ColorPicker = 12); "sole state-composable→component consumer" (useSlugMigration.ts is the identical shape). **Rescoped to**: a demo/palettes-wide layer-inversion CLASS of two sites (+ usePaletteWiring.ts:27 demo-wide, folded with re-trigger), MAJOR, the wave's structural spine — the only row whose cure deletes an expressive power rather than polices it. Repro: `grep -rn --include='*.ts' 'InstanceType<typeof' demo/palettes/` → 2 lines.

### L-2 — 34-member port injected to call one method · **CONFIRMED**
adminPort = **34 members** (hand-counted :195-231); the panel injects it (:234) and uses exactly one member on one executable line (:361 `await pm.loadUserPalettes(slug)`); AdminPane.vue:28-40 simultaneously feeds the same child 6 props + 7 emit handlers off the same port — one source, two mechanisms, one parent/child pair. The port's own header (:21-31, RF-15) claims dissolution of "ONE cross-everything injected blob" into "FIVE narrow ports" while PORT 4 re-aggregates users/queue/audit/flagged/tags/search/expansion. Standing note (O): :197 exists solely to carry L-1's handle — the two rows are one cure. Repro: `grep -n 'pm\.' demo/palettes/browser/admin/AdminUsersPanel.vue` → :233 (comment) + :361.

### L-3 — five dead `variant` props on a Button that has no `variant` · **CONFIRMED**
Button.vue.d.ts:4-19: emphasis/tone/size/iconOnly/loading/type/disabled/class — **no variant**; chunk `button-Bu9F4uU6.js`: `variant`/`outline`/`ghost`/`inheritAttrs` all **0**, so inheritAttrs defaults true and the string falls through `$attrs` to the DOM, rendering every subject button at default `emphasis` (secondary). Subject sites :22/:33/:58/:114 (outline) + :123 (ghost); `EmptyState variant="error"` (:53) and `Badge variant="secondary"` (:102) are REAL props, correctly excluded. Fleet = **51 sites / 22 files** (arbiter census = both workers'). The panel's own intent comment (:107-111, "quieted to ink-at-rest… never as 5 resting beacons") is an instruction glass-ui never receives. `vue-tsc` exit **0** over all 51 — the W44 hard typecheck is structurally blind to this class and may never be cited as its gate. Adopted from O's cell: live DOM census {variant: 5, emphasis: **0**} on `#/admin/users` — the zero is the discriminator (declared props are consumed, never rendered). **GLASS-OWNED rider** (cite INBOX I-20; masking-fallback ban — no local shim): Button `inheritAttrs:false` + explicit allowlist so an unrecognised design-token prop is a visible no-op; leaves as a BH-relay letter. Repro: the python census in the table + `sed -n '1,20p' node_modules/@mkbabb/glass-ui/dist/components/button/Button.vue.d.ts`.

### L-4 — two dialects for one design system in one file · **RESCOPED**
Subject confirmed verbatim: :188 `../../../ui/button`, :189 `../../../ui/badge`, :190-197 `@mkbabb/glass-ui/dialog` — while `demo/ui/dialog/index.ts` exports the same eight symbols unused; all 19 demo/ui entries are one-line re-exports (spot-read: button, badge, dialog). **Counts corrected**: barrel-importer files = **48** (ledger's 35 REFUTED; F's 49 off by one); both-paths files = **24** (F's 25 off by one). Bundle-cost arm stays HYPOTHESIS — never measured on the built artifact; per L-12 only `npm run gh-pages` output may ever decide it. Rescoped to: the subject's three import lines land in this wave; the 48-file demo/ui deletion is fleet wave **MT-UI1** (re-trigger: `grep -rlE 'from "(\.\./)+ui/[a-z-]+"' demo/ | wc -l` — 48 today, 0 at close). Repro: `grep -n 'from "\.\./\.\./\.\./ui/' demo/palettes/browser/admin/AdminUsersPanel.vue`.

### L-5 — unauthenticated ≠ empty roster · **CONFIRMED — the wave's BLOCKER**
Statically certain, no data needed, because the accused path never fetches: useAdminUsers.ts:54-56 `const token = getAdminToken(); if (!token) return;` exits BEFORE touching `loadingUsers`/`usersLoadError`, so initial state (users=[] / loading=false / error=null) selects AdminUsersPanel.vue:63 `· roster clear · / No users found.` — two lines below the component's own violated invariant (:49-50, "error ≠ empty… never costumes"). No route precondition exists: viewSchema.ts:188-196 declares `admin-users` with left/right/label/leftLabel/rightLabel/icon/accentHueShift and **no auth meta**; `if (!token)` guards = 11 in the file, 22 across demo/palettes. One correction to O's evidence (recorded, not verdict-bearing): usePaletteWiring.ts:147-149 fires `loadAdminUsers()` behind `adminUsers.value.length === 0` — a cache guard, so "unconditionally" is retired; **"with no authentication test" stands**, which is the defect. Adopted from O's cell with the discriminator that separates app-fact from environment-fact: token removed → body renders the roster-clear plate with 15 buttons, `hasUnreachable:false`, `skeletons:0` — **no request was attempted**, so this is the token-guard branch, not the API-less server (a failed fetch renders "The roster is unreachable.", :51-56). Witness standing: the safari-desktop-light PNG is UNTRACKED (fails L-7) — the verdict rests on the code path. Repro (fresh session): read useAdminUsers.ts:54-56 + AdminUsersPanel.vue:63 + viewSchema.ts:188-196; or run the wave's committed probe with localStorage token removed.

### L-6 — one row grammar, five storage sites · **CONFIRMED (widened)**
The literal `items-center gap-3 px-3 py-2.5` lives in **five** files in `demo/palettes/browser/admin/`: the atom AdminListItem.vue:**11** (declared home), AdminUsersPanel.vue:80 (border split onto wrapper :68), AdminListSkeleton.vue:**9** (naming its own transcription at :4-5), AdminAuditPanel.vue:62, AdminFlaggedPanel.vue:50 — the accusation said three; both workers found five; arbiter confirms five. The atom's only consumer is AdminNamesPanel (:44/:94). The skeleton's dishonesty is checkable: it predicts swatch + two lines + one lozenge; the row it becomes has no swatch, one line, two lozenges. Repro: the grep in the table.

### L-7 — feedback beat rebuilt without its cancellation · **RESCOPED**
Duplication confirmed: ActionFeedback.vue (homed under `PaletteCard/`, undiscoverable; props message/variant/visible/autoDismissMs=2500; watch does `clearTimeout` before re-arming; consumed at PaletteCard.vue:123, imported :180) vs the panel's bare `setTimeout(…, 3000)` at :308 — no handle, no cancel, no onUnmounted. **Narrowed** (O): the double-beat overlap needs the first prune to return 0 while `emptyCount > 0` — reachable chiefly through useAdminUsers.ts:195-198's `catch → return 0`, which is the sharper buried defect: **a thrown prune renders the success-family copy "No empty users to prune"** — the L-5 sin on a second path, booked into this wave's unit test. "Leaks its timer" retired for "uncancelled timer" (a post-unmount ref write is not an error; the closure holds ≤3s). **Cure corrected** (O, verified): ActionFeedback has **zero** `onUnmounted` — the move to `demo/shared/ui/` must add teardown or every consumer inherits the hole. The live observational arm is UNVERIFIABLE-HERE (prune is data-backed; API-less server); settled by reading; the gate is a unit test. Repro: `grep -n 'setTimeout' …/AdminUsersPanel.vue` + `grep -c onUnmounted …/ActionFeedback.vue` → 1 / 0.

### L-8 — one confirm home, three destructive panels · **CONFIRMED**
AdminUsersPanel.vue:254-286 is the repo's sole confirm machine (7 refs :255-261, `showConfirm` :263-278, `onConfirm` :283-286; `confirmOpen`/`function showConfirm` grep to this file alone) and every destructive action HERE routes through it (:288/:315/:326). The siblings do not: AdminFlaggedPanel.vue:**98** `@click="flagged.deletePalette(item.paletteSlug)"`, AdminTagsPanel.vue:101 `@click="tagsApi.deleteTag(tag.name)"` on a raw `<button>` (:98). The cause is in the tree (:280-282: Glass 7 folded ConfirmDialog onto the Dialog family). The invariant "a destructive admin action is confirmed" is currently enforced by which panel bothered — a safety row. The three-consumer `useConfirm`/ConfirmDialog composition is OURS and lands in-wave; a ConfirmDialog *preset* over the Glass 7 Dialog family is a **GLASS-OWNED rider** (INBOX I-20, BH relay). Repro: the greps in the table.

### L-9 — two transports for one colour concern · **RESCOPED**
Mechanism confirmed: :232 injects SAFE_ACCENT_KEY across palettes→color-session while `cssColor` arrives as a prop (:210) that AdminPane.vue itself injected at :94 from the same keys module. **Counts corrected**: SAFE_ACCENT_KEY injectors = **6 files** (not 11; arbiter list = O's list = F's count); O adds 14 palettes→color-session import edges; the report's ":133 inject" is actually the :134 alias comment region — the inject is :94. Ruling folded in-wave: prop-for-both at the panel (L-2's cure already ends the file at zero injects). MINOR. Repro: `grep -rln 'inject(SAFE_ACCENT_KEY' demo/` → 6.

### L-10 — explicit `Transition` import · **CONFIRMED**
Sole hit :186 among 17 `<Transition>` users. Behaviorally identical (the local binding resolves to the same component object) — "shadowing" retired; the defect is gratuitous divergence. Not catchable by an unused-import rule (the template consumes the binding) — needs this wave's grep gate to stay cured. One-token deletion, rides along. Repro: the grep in the table.

### L-11 — the `xs` rung rebuilt per call site · **CONFIRMED**
`h-7 px-2/2.5` + `font-display text-caption` hand-set on `<Button size="sm">` at class lines :24/:35/:116/:125 while Button.vue.d.ts:5 ships `ButtonSize = "xs" | "sm" | "md" | "lg"`. Fleet census (arbiter, opening-tag scoped): **22 sites / 11 files**, byte-identical to O's list; F's 13-by-window retired as methodology noise. Subject's 4 sites gated here; the other 18 fold to **MT-BTN1** (re-trigger: the census — 22 today, 4 die here, 0 at close). The sibling D-seat's "h-7 is inert under size=sm min-height" computed-style claim is NOT laundered in — unmeasured by every seat in this fold. **GLASS-OWNED rider** (INBOX I-20): if `xs` is the wrong height for a dock-adjacent admin toolbar, the token moves inside glass-ui — never back into eleven demo files. Repro: the python census in the table.

## The wave — MT-AU1 (re-authored, best of both, corrected)

**waveId**: MT-AU1 · **title**: AdminUsersPanel becomes a leaf: authority before roster, one declared prop vocabulary, one row anatomy, one confirm home · **bornRed**: true

**Scope (exact files)**: `demo/palettes/browser/admin/AdminUsersPanel.vue` (391→~200: zero inject, zero defineExpose, zero setTimeout, zero confirm engine, zero row transcription, zero `variant`, zero `h-<n>`, no `Transition` import; safeAccent + everything else via props/emits) · `demo/palettes/useAdminUsers.ts` (:14/:27/:95/:117/:132/:146/:206 deleted; disclosure state → new colocated `useAdminUserPalettes.ts`; all `if (!token)` guards die behind the gate) · `demo/palettes/useSlugMigration.ts` (:6/:30/:84-87/:121 — the class's second site; setError → prop/emit on PaletteSlugBar, whose :236 defineExpose shrinks accordingly) · `demo/palettes/usePalettePorts.ts` (:123/:197 deleted; adminPort 34→33) · `demo/palettes/admin/AdminPane.vue` (:27/:134 deleted; auth gate mounted at the composition boundary; safeAccent passed as prop) · `demo/palettes/browser/admin/AdminListItem.vue` (+`interactive` prop: role/tabindex/aria-expanded/keydown hoisted from the panel's :78-89, whose guard comment travels with it) · AdminListSkeleton.vue + AdminAuditPanel.vue + AdminFlaggedPanel.vue (compose the atom — five homes → one) · AdminFlaggedPanel.vue + AdminTagsPanel.vue (destructive clicks route through the shared confirm; the Tags raw `<button>` becomes a Button) · NEW `demo/shared/ui/ActionFeedback.vue` (MOVED from PaletteCard/ — never deleted — **gaining the onUnmounted teardown it lacks**) + `demo/shared/ui/ConfirmDialog.vue` + `useConfirm()` · NEW `test/demo/admin-auth-not-empty.test.ts` + `test/demo/admin-prune-feedback.test.ts` (jsdom + @vue/test-utils, both in devDeps) · `demo/shell/viewSchema.ts` (five admin views gain `auth: "admin"`, honoured by ONE guard site) · fleet-mechanical in-wave: the 51 `variant` renames (outline→`emphasis="secondary"`, ghost→`emphasis="quiet"` [+`tone="destructive"` where the intent is red], destructive→tone, default→primary).

**Structure over gates (L-8 law)**: deleting `InstanceType<typeof …>` from demo/palettes makes the layer inversion UNREPRESENTABLE (no handle → no cross-layer write, no latent cycle, no `?.` swallow); the AdminGate makes `getAdminToken()` a non-null contract inside the console, so the unauthorized state stops being a state the panel models (22 per-method guards DELETE, not re-check); the disclosure state owned by `useAdminUserPalettes` makes a swallowed clear inexpressible (direct call, no instance-liveness precondition); one row-grammar home makes a rhythm fork impossible; zero injects make a second data path unacquirable. Where structure is genuinely unavailable, gates: the dead-`variant` class cannot be made unrepresentable from demo/ (fallthrough attrs + vue-tsc blindness, **measured exit 0**) — grep + DOM gates here, producer allowlist via BH relay (masking-fallback ban: no local shim/wrapper/lint-plugin substitute).

**Gates** (every one RED today; the input that makes it RED recorded):

- **G-1 · inversion unrepresentable**: `test "$(grep -rn --include='*.ts' 'InstanceType<typeof' demo/palettes/ | wc -l | tr -d ' ')" -eq 0` — RED: 2 (useAdminUsers.ts:27, useSlugMigration.ts:30). The third site (usePaletteWiring.ts:27) is FOLDed; re-trigger = same command widened to `demo/`.
- **G-2 · no imperative surface, no port handle**: `test "$(grep -c 'defineExpose' demo/palettes/browser/admin/AdminUsersPanel.vue)" -eq 0 && test "$(grep -rn 'adminUsersPanelRef' demo/ | wc -l | tr -d ' ')" -eq 0` — RED: 1 / 10.
- **G-3a · declared vocabulary (static)**: the python opening-tag census (`<Button…>` containing `variant=`) `== 0` — RED: 51 in 22 files. vue-tsc is NOT this gate (exit 0 today, measured twice — worker-O and this seat).
- **G-3b · declared vocabulary (DOM, O's cell)**: wave-committed probe `audit/waves/MT-AU1/probe-au1.mjs` — chromium 1440×900, token removed, `#/admin/users`: buttons with `variant` attr === 0 — RED: 5 today, `emphasis` attrs 0 (the discriminator: declared props never reach the DOM).
- **G-4a · authority never costumed as empty (DOM)**: same probe: with no token, body must NOT contain "No users found."/"0 users", MUST contain the named authority plate, no operable Prune/Refresh — RED today (measured body: `Users 0 · … · ROSTER CLEAR · No users found.`, 15 buttons, zero requests attempted).
- **G-4b · authority asserted once, at the boundary**: `test "$(grep -rn --include='*.ts' 'if (!token)' demo/palettes/ | wc -l | tr -d ' ')" -eq 0 && grep -q 'auth: "admin"' demo/shell/viewSchema.ts` — RED: 22 / absent.
- **G-5 · one row anatomy**: `test "$(grep -rn 'items-center gap-3 px-3 py-2.5' demo/palettes/browser/admin/ | grep -v AdminListItem.vue | wc -l | tr -d ' ')" -eq 0` — RED: 4.
- **G-6 · one confirm home + honest failure beat**: `test "$(grep -cE 'setTimeout|confirmOpen' demo/palettes/browser/admin/AdminUsersPanel.vue)" -eq 0 && test "$(grep -rln 'useConfirm' demo/palettes/browser/admin/AdminUsersPanel.vue demo/palettes/browser/admin/AdminFlaggedPanel.vue demo/palettes/browser/admin/AdminTagsPanel.vue | wc -l | tr -d ' ')" -eq 3 && npx vitest run test/demo/admin-prune-feedback.test.ts` — RED on all three arms (setTimeout=1, confirm engine present, consumers=0, test absent). The test asserts the two arms no browser here can reach: (i) a second action-result inside the first's dismiss window survives its own full window; (ii) **a prune whose transport throws must not render success-family copy** (today: catch→0→"No empty users to prune").
- **G-7 · the panel is a leaf**: `F=demo/palettes/browser/admin/AdminUsersPanel.vue; test "$(grep -c 'inject(' $F)" -eq 0 && test "$(grep -c 'Transition } from \"vue\"' $F)" -eq 0 && test "$(grep -cE 'class=\"[^\"]*\bh-[0-9]' $F)" -eq 0` — RED: 2 / 1 / 4.
- **G-8 · witnesses exist in git (L-7 law)**: `test "$(git ls-files docs/tranches/V/megatranche/audit/waves/MT-AU1/ | grep -c png)" -ge 6` — RED: tracked megatranche PNGs = 0 repo-wide; `.gitignore:34 *.png` requires `git add -f`. An uncommitted witness does not exist.
- **G-9 · the L-13 harvest gap closes (NEW, arbiter)**: `test "$(grep -c 'challenge-D' docs/tranches/V/megatranche/registry/harvest/area-palettes.json)" -ge 1` — RED: 0 today while `audit/components/AdminUsersPanel/challenge-D-design.md` (33KB, 18 rows, 4 at BLOCKER grade) sits unharvested. The design half of this component may not be called closed until those rows are harvested AND adjudicated; adjudicating them from here would launder an unharvested report into the record, so the gate demands the harvest, and the adjudication follows the normal M-12 route.
- **G-suite regression floor**: `npx vue-tsc -p tsconfig.demo.json --noEmit && npx vitest run` — GREEN today; retained as regression guard only, with its L-3 blindness recorded above.

**π obligations**: chromium desktop-light + desktop-dark @1440×900, mobile-light @390×844; route `#/admin/users`; selectors: `[data-slot="admin-authority-plate"]` (post-wave), `[data-slot="admin-list-item"]`, `[role="alertdialog"]`; committed (force-added) under `audit/waves/MT-AU1/pi/` with `probe-au1.mjs` + JSON output. Per I-20, WebKit and real-Safari-26.4 (safaridriver :4599) are SEPARATE cells, never inferred from chromium. Authenticated roster/expanded/confirm-with-data states: UNVERIFIABLE-HERE (API-less) — every populated capture must declare its `page.route` fixture in its JSON header; owed to the first API-backed cell, NOT a gate of this wave.

**Δ obligations** (before→after, both halves committed): defineExpose 5→0 · panel inject 2→0 · state-module→SFC type-imports 2→0 (both class sites) · port handle lines 2→0 · dead variant attrs 51→0 (DOM census {variant:5, emphasis:0}→{0,5} on the route) · demo/ui imports in file 2→0 · row-grammar homes 5→1 · confirm coverage 1/3→3/3 · bare setTimeout 1→0 · h-7 overrides (subject) 4→0 · explicit Transition import 1→0 · `if (!token)` 22→0 · unauthenticated surface: roster-clear plate→authority plate.

**Carries**: L-1 BUILD (class, both palettes sites; BLOCKER narrative RETIRED with rationale; id preserved per anti-rename) · L-2 BUILD · L-3 BUILD demo-half; producer half GLASS-OWNED → BH relay (I-20), banked on `glass-ui version > 7.0.0 && grep -c inheritAttrs dist/button-*.js ≥ 1`, swept at every wave open · L-4 subject BUILD; fleet → **MT-UI1** (re-trigger 48→0) · L-5 BUILD (the BLOCKER) · L-6 BUILD (5 homes) · L-7 BUILD with corrected cure (teardown added at the move) · L-8 BUILD; ConfirmDialog preset → BH relay · L-9 BUILD (prop-for-both; corrected counts 6/14) · L-10 BUILD · L-11 subject BUILD; fleet → **MT-BTN1** (re-trigger 22→0); xs-height fallback GLASS-OWNED → BH relay · challenge-D 18 rows → G-9 (harvest, then M-12) · BANK (surplus structure, not the accusation): adminPort five-seam split beyond the users slice, re-trigger `grep -n 'adminColorQueue: colorQueue' demo/palettes/usePalettePorts.ts`.

**completableAlone**: YES. Every edit lives in `demo/palettes/**` + `demo/shared/ui/**` + `demo/shell/viewSchema.ts` + two test files — no src/, no api/, no glass-ui edits; ~13 files, one seam, all gates static greps or jsdom vitest runnable in any checkout after `npm i` (G-3b/G-4a need only the dev server, no API); the sole cross-cutting touch (viewSchema auth meta + one guard) is additive and leaves the 9 non-admin views' KeepAlive economy untouched (admin panes already fall off the LRU by design). If MT-AU1 were the only wave ever executed: the console stops telling strangers the roster is empty while handing them Prune; two composables stop reaching through a layer into component instances; 51 styling intents the design system silently discards become declared props; one row anatomy serves five sites and the skeleton stops lying; three destructive panels confirm instead of one; the panel mounts in a test with props alone.

## ADDENDUM (E-3 — appended, no ledger row edited)

ADDENDUM MT-AU1/§2 · AdminUsersPanel tri-fold arbitration, 2026-07-27, HEAD `c654824e`, arbiter-F (Fable 5). The eleven CHALLENGE-L rows were adjudicated by two blind workers (Fable, Opus) and this arbiter attacked both before adopting either: **7 CONFIRMED / 4 RESCOPED / 0 REFUTED**. Seven inter-worker disagreements were ruled — **all seven for worker-O**, each on arbiter re-measurement (ColorPicker's 12-member expose vs the claimed "largest 5"; barrel counts 48/24 vs 49/25 and the ledger's refuted 35; the untracked "committed" witness; the prune-race precondition + the atom's missing `onUnmounted`; the L-9 label under L-9 law; the 22-in-11 height census; the unharvested challenge-D 18 rows). The docket's severity ordering is AMENDED: **L-5 is the BLOCKER** (authority costumed as an empty roster, statically certain, live-discriminated in worker-O's chromium cell by hasUnreachable:false + skeletons:0 = no request attempted); **L-1 is MAJOR** — its data-corruption arm is unreachable by two independent mechanisms that converge (pane-keyed KeepAlive deactivation keeps the ref bound so the call lands; true unmount nulls the ref and destroys the very state a stale call would have cleared), and it survives as a two-site layer-inversion class whose cure is a deletion of expressive power. Recorded so it is never re-discovered: `npx vue-tsc -p tsconfig.demo.json --noEmit` exits 0 over all 51 dead `variant` props (measured independently by worker-O and by this arbiter) — the W44 hard CI typecheck is structurally blind to the fallthrough-attr class and may never be cited as its gate. Three riders are GLASS-OWNED (Button attr allowlist; the xs-height token question; the ConfirmDialog preset) and leave via the standing BH relay (INBOX I-20) with the masking-fallback ban binding: no local shim may stand in. Two evidentiary defects are recorded against the formation rather than the component: zero megatranche PNGs are tracked (`.gitignore:34`), so every visual witness cited anywhere in this docket fails L-7 until force-added (gate G-8); and challenge-D's 18 rows against this same file are on disk, in no harvest and no ledger (gate G-9) — the component's design surface is NOT closed by this apotheosis.

## DISSENT (preserved unresolved disagreement)

1. **Worker-F's methods dissent against the challenger seat — preserved verbatim in substance and ADOPTED**: "three of its eleven evidence blocks carry count-scoping flaws (the barrel-blind .vue-import grep presented as covering the subject import; SAFE_ACCENT 11-vs-6; the height-override fleet count) — every flawed count decorated a true defect this time, but under L-9 law decorative unscoped counts are how false rows get through." The arbiter extends the same finding to worker-F itself: its 25/49 barrel counts and 13-site height census were each off, and its "largest defineExpose"/"committed shot" restatements were refuted — worker-F applied L-9 discipline to the challenger and not to its own instruments. Worker-O's parallel note stands with it: "the fleet is not unreliable — it is unaudited."
2. **The ±1 measurement window (unresolved, immaterial)**: worker-F's wider-net 49 barrel-importers / 25 both-paths vs worker-O's and this seat's 48/24. The regex frames differ (F's net uncharacterized); no verdict turns on it; both sides' positions preserved. The binding numbers are the arbiter's: 48/24.
3. **Worker-F's probe-parsimony position on L-3** — preserved: F judged the design consequence from the d.ts + chunk grep alone, holding that "probe parsimony ruled a browser session out for a verdict the d.ts + chunk grep already decided." The arbiter agrees the static chain decides the VERDICT, and simultaneously records that worker-O's live {variant:5, emphasis:0} DOM census is the stronger *teaching* evidence and becomes gate G-3b. Both positions are right at their own altitude; no reconciliation needed, none forced.
4. **Worker-F's L-4/L-7/L-9 CONFIRMED labels** — preserved: F held that a true mechanism with flawed decorative counts is CONFIRMED-with-correction; O and the arbiter held RESCOPED under L-9 law. The rows' substance was never in dispute — only what a verdict label owes to its numbers. The ruling (RESCOPED) binds the registry; F's position is recorded so the label-law question can be settled at the formation level rather than silently re-litigated per seat.
5. **Worker-O's registry dissent (L-13)** — preserved verbatim in substance and converted to gate G-9: "if this component is declared adjudicated on the strength of my eleven verdicts, four accusations carrying BLOCKER severity vanish without a verdict. I have named them in CARRIES rather than adjudicating them, because adjudicating an unharvested report would launder it into the record by the same route." The arbiter concurs and has made the gap RED-visible rather than prose-visible.
6. **Line-number drift, both workers** (recorded, no ruling needed): F's atom :12→:11, Flagged ":99"→:98, App.vue ":80-84"→:88/:107/:133; O's App.vue ":78/:104"→:88/:107, AdminPane ":133 inject"→:94 inject/:134 alias, cssColor ":209"→:210. Every drift decorated a true fact; none was load-bearing; all corrected above.
