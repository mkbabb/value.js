Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component RibbonBar; receipt read from this seat's own system prompt, not inherited).

# kf-RibbonBar — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-12 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), `origin/master` `81a56990`, worktree DIRTY; **the entire audited surface is worktree ≡ origin/master** (this seat's `git diff origin/master --stat` over RibbonBar.vue, design-idioms.css, style.css, useApplyCSS.ts, KeyframeTimeline.vue, useTimelineBuild.ts, ChannelControls.vue, controlOptionsStore.ts, controlSurfaces.ts → EMPTY), so every line anchor binds to both. `origin/master:package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` exact (my `git show`); the worktree manifests are dirty with 0 glass-ui hits — the K17/SCH-1 substrate law applies to every phantom-dep cell below. **Registry tree** value.js `tranche-u`.
**Corpus**: two fresh hostile re-reader returns over `audit/kf-components/RibbonBar/challenge-{D-design,L-library,C-consumption}.md` — **reader-DU** (design-and-usage: 24 confirmed rows · 6 kills · 8 misses · 5 superlatives · 8 unproven) and **reader-LC** (library-and-code: 25 confirmed rows · 8 kills incl. 2 self-kills · 6 misses · 6 superlatives · 8 unproven). Both returns consumed whole; all three challenge files re-read whole by this seat.
**Method (incredulity)**: every verdict-level disagreement — between the two readers, between either reader and the banked registry, and between the corpus and the tree — was re-derived at the bytes before adoption. Zero browser tooling; product source read-only; **sole write = this file**. Instruments this session: full reads of RibbonBar.vue (151 L, anchors all re-checked), design-idioms.css (301 L, whole), useApplyCSS.ts (69 L, whole), controlSurfaces.ts (whole — the D-13/m-5 decider); targeted reads of KeyframeTimeline.vue :125-145/:275-307 (the D-17 decider: CSSPasteDialog paste-text import + the defineExpose census), useTimelineBuild.ts :115-145 (`exportCSS` tail ends `navigator.clipboard.writeText` + toast — no file), ChannelControls.vue :92-102 (the **:98 controls-tabpanel `v-if`** — the m-6 decider) / :148-175 (expanded-timeline placeholder) / :375-380 (`isTimelineVisible`), ChannelOptions.vue :374-382 (`<Teleport v-if="active">`), useKeyframeBrushApply.ts :25-47 (`{ isApplied, toggle }` destructure — `clear` dropped), KeyframesStringControls.vue :1-22 (template = editor + hidden Paintbrush, **no in-pane apply button**), useControlsKeyboardShortcuts.ts :18-24 (`Ref<any>` dual shape), style.css :1-16 (import order: glass at :3, design-idioms at :14) + :88-94 (`color-scheme: light`); installed glass-ui 7.0.0: `dist/styles/utilities/a11y-overrides.css` whole line (unlayered — `grep -c @layer` → 0; the forced-colors block's selector list read verbatim), `dist/styles/tokens/scale-paper.css` (`--scale-hover: 1.08`, `--gold: oklch(0.751 0.147 84.2)`, **`--color-gold: var(--gold)`** — glass aliases the very token the demo shadows, `--rainbow-indigo: oklch(0.566 0.206 294.1)`), `light-dark.css` coarse block; **shipped sheet** `dist/gh-pages/assets/index-CL_QYCiO.css` extractions: `.text-gold{color:var(--gold)}` AND `.text-gold{color:var(--color-gold)}` (both present — the RB-2 decider), `.text-body` ×2 (full glass rule + theme-bridge `font-size`-only rule — the repair-trap decider), `.focus-ring:focus-visible` ×3 in order — glass base (`outline:none`), glass forced-colors group (`outline:2px solid highlight`), demo restatement (`outline:none`, LAST — the RB-1 decider); executed greps (exit-checked): `pl-4 pr-7` → **3 lines / 2 files** (RibbonBar.vue:2, ChannelControls.vue:17/:39), `emerald` in demo → **1** (RibbonBar.vue:42), `btn-interactive` in demo → **8 lines** (= the banked census), `aria-|role=` in RibbonBar.vue → **0**. Banked records leaned on (citable; axis assertions are not): kf-ControlsPaneWrapper (D-B4, D-M11, D-m12, D-m13, N-4, D-m8/mi-1, D-m9, D-m10, C-20, S-7, S-10, S-11, SS-13 #7/#10), kf-CubeScene (D-11, D-2/C-3, D-3/C-4), kf-ChannelControls (D-4, L-8, N-6, C-8/D-6, L-2/C-2, mi-9), kf-ChannelOptions (L·M-3 + the final addendum on KF-CO-15), kf-KeyframesStringControls (L-BL-1, D-5/L-M-4/C-4, D-13/L-M-7/C-6, N-5, D-21/C-5, L-M-9, the C-5 async-wrapper kill), kf-CSSCodeEditor (KF-CE-9, KF-CE-13, KF-CE-37), kf-KeyframesEditor (KF-KE-36, SUP-3, the D-29 reversal), kf-CopyButton (D-19 reversal, KF-CB-32), kf-DemoGlobalChrome (D-9/L-11, M-2, S-L2), kf-AnimationControlsGroup (M-8/L-4(a)), kf-App (KF-APP-4), census `lane-frontend.md` F-1/S-1..S-8/§6.3/§6.5 + `CENSUS-2026-08-03.md:177-206` (the KF.W0–W10 taxonomy).
Status verbs per M-25: dispositions are **ADJUDICATED, not VERIFIED** — the X·KF specs are still forming; every routing is agglomeration input for SS-1/SS-2, keyed to the census wave sketch KF.W0–W10.

## Scoreboard

**33 roster rows stand — 19 FOLDS to banked identities (two blocker-weight at their banks) · 14 booked here (3 MAJOR · 8 MINOR · 3 INFO) · 8 net-new rows admitted from the readers' 14 filed misses (RB-1..RB-8), each re-derived at the bytes · 2 reader misses BOOKING-KILLED as pre-banked (≡ kf-KeyframesStringControls N-5) · 14 killed-claims register entries, including TWO kills of reader confirmations settled by this seat's own source reads (D-17, m-6), one banked kill RE-FIRED against BOTH readers (the async-wrapper `InstanceType` cell), one substrate-dead re-fire against both (phantom dep), and ONE REGISTRY REPAIR (kf-ControlsPaneWrapper's S-11 superlative RETIRED — it praised a dead payload) · 7 superlatives · 7 UNPROVEN residues → SS-13.**

**Headline.** RibbonBar is the most heavily pre-banked component this seat has adjudicated: the rainbow-AA blocker, the five-token dead class string, the async-window silent no-ops, the phantom utility, the pl-4/pr-7 idiom, the directory split, the raw-store dispatch, the stringly alphabet, the hidden brush, the silent clipboard, the formatCSS latch, the `variant` phantom, and the unexported skin seam were ALL already banked across seven sibling apotheoses — and neither reader consulted the registry, so the corpus's loudest findings are third and fourth independent derivations folded home with zero re-bookings. The genuinely new yield is narrow and real: **RB-1** (the demo's unlayered `.focus-ring:focus-visible { outline: none }` lands AFTER glass's forced-colors `outline: 2px solid Highlight` in the shipped sheet — keyboard focus has NO indicator under Windows High Contrast on every glass Button in the demo; not an omission, a removal), **RB-2** (`.text-gold` is a demo shadow of a live glass utility of the same name — both rules ship, the demo's wins, glass's dies), and the D-17 inversion (the corpus's Import/Export-iconography defect dies on its premise — no file exists at either end — and its provable replacement is that three clipboard writes wear three unrelated glyphs). The recurring fleet failure fires again: both readers confirmed a cell the registry had killed (the async wrapper), both repeated the substrate-dead phantom-dep probe, and both confirmed m-6 without reading the parent's `:98` gate one file up.

## Rulings (the seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **D-1 severity — reader-DU BLOCKER vs reader-LC "MAJOR; the rendered band is SS-13's"** | **FOLD ≡ kf-ControlsPaneWrapper D-B4 — BLOCKER at the bank; LC's demotion overruled, preserved as dissent** | The bank already rates this BLOCKER with a third-derived stop table (worst 1.33:1); the band question changes nothing statically — every stop the demo owns fails 4.5:1 (best owned = red 3.78), so EVERY reachable label band fails; only glass's indigo (~1/7 of the ramp) approaches AA. Both readers' band correction (content box ≈11–89% of a `to right` ramp, crossing yellow 1.33) and reader-DU's theme-invariance proof (design-idioms.css `.dark` block :67-71 redefines only gold, never `--rainbow-*` — my read) fold in as riders that make the bank WORSE. Rendered geometry stays the bank's SS-13 #7. |
| 2 | **D-17 — reader-DU KILLS on premise vs reader-LC CONFIRMS ("both target local-file operations")** | **KILL SUSTAINED; reader-LC's confirmation cell KILLED — settled at the tails by this seat** | My reads: `openImportDialog` → `importDialogOpen = true` → `<CSSPasteDialog title="Import CSS @keyframes" description="Paste CSS @keyframes to load into the timeline">` (KeyframeTimeline.vue :134-142/:281-283) — a paste-TEXT dialog, no file input; `exportCSS` ends `await navigator.clipboard.writeText(css); toast.success("CSS copied to clipboard!")` (useTimelineBuild.ts :132-133) — no download. Neither command is a file operation; the Upload/Download convention is inapplicable. Reader-DU's replacement MISS-3 admitted (RB-3). |
| 3 | **m-6 — BOTH readers confirm ("v-show keeps a per-frame-updating `<PlaybackRibbon>` alive behind display:none")** | **KILLED — a kill-of-two-confirms, this seat's bytes + the banked kf-ChannelOptions addendum** | `ChannelControls.vue:98` (my read): the controls tabpanel — ChannelOptions inside it, and therefore the Teleport source and `<PlaybackRibbon>` — is `v-if="hasSurface('controls') && selectedControlSurface === 'controls'"`. Selecting Keyframes/Timeline UNMOUNTS ChannelOptions; nothing is teleported while the `v-show` target hides — the landing div hides EMPTY. The banked kf-ChannelOptions final addendum states exactly this ("ChannelControls.vue:98 v-ifs ChannelOptions per control-surface tab switch … any Controls→Keyframes→Controls round-trip recreates the ribbon"). Both readers traced only ChannelOptions' internal `v-if="active"` gate and never the parent's. The per-frame inactive-subtree cost that IS real is the channel axis, banked at kf-ChannelOptions L·M-3. Consequence for the S-1/SUP-A superlative: sustained-with-correction (below). |
| 4 | **M-2/C-1's "typed hop" cell — BOTH readers assert `InstanceType<typeof defineAsyncComponent(...)>` never carried the expose surface** | **BANKED KILL RE-FIRED against both readers** | kf-KeyframesStringControls' ruled kill (its own read of `@vue/runtime-core/dist/runtime-core.d.ts:1554-1556`): `defineAsyncComponent<T extends Component>(source): T` — the wrapper IS the resolved component type; `InstanceType` carries the `defineExpose` surface. The erasure is strictly downstream at `AnimationControlsGroup.vue:191`'s `reactive<Record<string, any>>` — already banked (kf-ACG M-8/L-4(a)); today the *.vue shim (B-1 ≡ KF-APP-4) erases everything anyway. Repair direction matters: fix B-1 + the ACG line and the :372 hop needs no work. The row survives at its banks; the aggravation cell dies. NEW rider admitted (verified :18-24): the SAME identifier is typed `Ref<any>` in useControlsKeyboardShortcuts.ts and consumed as an unwrapped instance in RibbonBar — two incompatible contracts from one producer, both `any`, swap-undetectable. |
| 5 | **D-13/m-5/C-13 — reader-DU confirms structurally (MINOR) vs reader-LC kills the enumerated mechanism and demotes to UNPROVEN** | **Reader-LC's kill SUSTAINED by this seat's controlSurfaces.ts read; row ADJUDICATED UNPROVEN ≡ kf-ControlsPaneWrapper SS-13 #10 (D-m7/D-E1, still unratified)** | `ControlSurface` is a closed 6-union; `surfacesFor` (:95-119, whole read) is total over (facility × selected channel): facet surfaces derive ONLY from scenes whose facilities declare them (easing→Curve, spring→Physics, cube's Matrix channel→matrix-controls) — exactly the three scenes with `ribbonContent`, each returning content for precisely its own facet; scenes without `ribbonContent` derive the triad (painting channels) or `[]` (sequence — which mounts no control pane at all per `selectedSurfaceFrom`). The three `null` returns are complementary to RibbonBar's built-in branches; D-13's "the enumeration cannot be written" is REFUTED — it was written. Surviving windows (all live-only): the controls-tab pre-Teleport sub-tick, a standalone `extraTabs` host, the `isActiveSceneHost` suspend window. C-13's honest form is the version that carries. |
| 6 | **D-2/C-5 grade — reader-DU BLOCKER · reader-LC MAJOR · the bank (kf-ControlsPaneWrapper's adopted miss) MINOR** | **FOLD with the bank's grade REPAIRED MINOR→MAJOR; BLOCKER declined** | The bank filed it as a rider on D-B4 ("no border, check, text change, or aria-pressed"). This corpus adds what the bank's cell lacked, twice-independently: zero ARIA in the whole file (my grep → 0), the compiled Button offers no pressed axis, BOTH correct in-repo precedents are live (KeyframesEditor.vue:89-90 `:aria-pressed="cssApplied"`; PlaybackRibbon.vue:56-58 aria-pressed-derived styling), and the state is consequential (`useApplyCSS.ts:45` `animation.paused = animation.started`). WCAG 4.1.2 + 1.4.1 on the file's only stateful control = MAJOR. BLOCKER is declined as double-counting: the same button's active state already carries the D-B4 BLOCKER on its contrast channel; the cure is one batch (aria-pressed + an edge cue + drop the hand-paint — see RB-4). Reader-DU's BLOCKER preserved as dissent. The "one fact, two competing UIs" sub-claim is killed unanimously (springEditAnim is a distinct animation, distinct `useApplyCSS` instance — both readers, concordant). |
| 7 | **The phantom-dep / "npm ci cannot resolve :132" cells (reader-LC L·i-1 CONFIRMED at INFO; reader-DU inherits F-1 framing)** | **SUBSTRATE-DEAD — the K17-class kill RE-FIRED against both** | `origin/master:package.json:77` pins glass-ui 7.0.0 exact (my git show, ×3 in the origin lock per the banked probes); the worktree manifests are dirty checkout-lag artifacts. kf-KeyframesStringControls L-M-9 already RETIRED the clean-`npm ci` probe by name. FOLD ≡ census F-1/SCH-1 → **KF.W0 §B-12**; reader-LC's genuinely new subpath-convention correction (18 distinct glass subpath imports / 44 sites; the "no demo file uses a subpath" house-convention claim true only for /button + /card) is adopted as a rider on the banked CPW C-20. |
| 8 | **Reader-LC MISS #1 (apply state outlives its only control) — admitted as filed?** | **ADMITTED at MINOR (RB-6) with its consequence cell KILLED by banked L-BL-1** | The premise is verified (my reads: KeyframesStringControls' template is editor + hidden brush, NO in-pane apply button; the ribbon's toggle is the sole affordance and lives behind `v-if === 'keyframes'`; `clear` is destructured away at useKeyframeBrushApply.ts:29). But the filed failure scenario — "both animate the same target simultaneously" — is dead at the bank: kf-KeyframesStringControls **L-BL-1** proves the injected sheet binds NOTHING (class token ≠ sheet selector, constructed never-equal), so the CSS half cannot animate anything. The surviving harm: forced-pause + dead sheet + class residue persist with no visible control on the other tabs, recoverable only by returning to Keyframes. Compounds banked N-5; rides APPLY-UNIT. |
| 9 | **Reader-LC misses #3 (`clear()` dead) and #4 (no unmount cleanup)** | **BOOKING KILLS — both PRE-BANKED ≡ kf-KeyframesStringControls N-5, verbatim** | N-5 already states both limbs ("`useApplyCSS.clear()` has ZERO callers … unmount-while-applied leaves the animation pinned paused and the class on every target"). My whole read of useApplyCSS.ts (69 L: `clear` declared :21, implemented :54-66, returned :68; no `onUnmounted`) and the `{ isApplied, toggle }` destructure confirm the bank a second and third time. Confirmations recorded; zero re-booking. |
| 10 | **L-SUP-2 / the scoped rule — both readers kill; the registry PRAISES it (kf-ControlsPaneWrapper S-11: "a rule that wins on merit")** | **REGISTRY REPAIR: CPW S-11 RETIRED; D-14/C-12 booked MINOR** | The rule reaches the DOM and recolours a border glass-ui 7.0.0 never draws: `.button { border: 0 }`, the only border rule is `.button:not([data-tone="neutral"])`, the compiled default is `tone: "neutral"`, RibbonBar never sets tone — verified by both readers against the compiled dist and the shipped sheet (`.ribbon-apply--active[data-v-76c81a6f]{border-color:#0000}` ships and changes zero pixels). S-11 verified the delivery mechanism of a dead payload — the same failure it is retired for here in L-SUP-2 (whose specificity cell was also wrong: glass's border rule is (0,2,0), EQUAL not beaten; layer order decides). The instrument choice (scoped rule over `!important`) keeps its craft credit; the comment misinforms and must be corrected by any touching wave. |

**Adjudicator's corrections register (against this corpus's readers):** (1) reader-LC's D-17 confirmation — ruling 2; (2) both readers' m-6 confirmations — ruling 3; (3) both readers' async-wrapper cells — ruling 4; (4) reader-DU's D-13 structural confirm as filed — ruling 5; (5) both readers' phantom-dep cells — ruling 7; (6) reader-LC's MISS #1 consequence cell — ruling 8; (7) reader-LC's misses #3/#4 as net-new — ruling 9; (8) reader-DU's D-1 BLOCKER as a NEW booking and reader-LC's MAJOR demotion — ruling 1 (it was already banked BLOCKER); (9) reader-DU's D-8 praise of `text-gold` as "the discipline the demo does own" — corrected by RB-2 (the discipline is a shadow). Neither reader consulted the registry; ~19 of their confirmed rows were already banked.

## Adjudicated defect roster — id · severity · claim · terminal disposition

Anchors bind to worktree ≡ `origin/master` `81a56990`; re-anchor before any cure (D-19 law). Routing: census KF.W0–W10 where a forming wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec intake.

### FOLDS to banked identities (identity guard; zero re-booking)

| id (this corpus) | banked identity | grade at bank | riders contributed by this corpus (adjudicated in) |
|---|---|---|---|
| **D-1** | kf-ControlsPaneWrapper **D-B4** | **BLOCKER** | Band correction (shrink-to-fit content box ≈11–89% of the `to right` ramp — crosses yellow 1.33:1; worse than filed); theme-invariance (`.dark` redefines only gold); 3rd+4th independent luminance derivations (agree ≤4dp). → NO-WAVE-OWNER (the D-B4/D-M11 batch); geometry = bank's SS-13 #7 |
| **D-2 / C-5** | CPW's banked Apply-state row (rider on D-B4) | MINOR → **repaired MAJOR** (ruling 6) | Zero-ARIA grep; both live in-repo precedents; the paused-flip consequence; killed sub-claim recorded. Cure = one batch with D-B4 + RB-4 |
| **D-3 / m-1 / C-2** | kf-CubeScene **D-11** → **KF.W6** (phantom-utility sweep, 8 sites) + CPW **D-M11** (delete-the-string cure) | MINOR (CubeScene) / MAJOR (CPW) | `scripts/proof-phantom-classes.mjs` named in LESSONS-LEARNED.md:603 and ABSENT from `scripts/` (both readers, concordant); the lane-frontend §6.3 widening note (phantom-class audit, not just token collision); cross-repo ColorInput.md D-2 family cross-referenced NEVER merged |
| **C-3 / D-4 / D-5 / D-6** | CPW **D-M11 + D-m12 + D-m13** (the five-token string: dead / unreachable / de-scaling; ruled cure = DELETE) | MAJOR + 2 MINOR | D-5's playback-idiom.css T.D3 register-law contrast (transport buttons are CONTROLS, weight 500 — one band, two weights); PlaybackRibbon.vue:38-45's recorded h-8→h-10 lesson (the file that documents the rule is not the one that breaks it); C-3's aggregate headline KILLED (both readers: `rounded-full` redundant-not-wrong; `gap-1.5` byte-identical at `--ui-scale: 1`); reader-LC MISS #6 admitted as the repair-trap rider: `.text-body` ships TWICE (glass full rule + theme-bridge `font-size`-only — my sheet extraction), so deleting the token restores weight 500/leading 1.1/scale-aware sizing in one move for a reason neither axis stated |
| **D-12 / M-1 / C-4** | CPW **N-4** (eight actions enabled on a store string through the async window; cure seam `:disabled="!activeKeyframesRef"`) | MAJOR | Persisted-pick entry path (`useStorage` rehydrates `selectedControl: "keyframes"` → four live buttons at first paint against a null ref for the whole vendor-monaco window); citation-drift note on useKeyframesPaneReveal line anchors |
| **B-1 / L·B-1** | kf-App **KF-APP-4** → **KF.W4** | MAJOR (gates lane) | Reader-LC's executed receipt (`npx tsc --noEmit --listFiles`: 126 demo files, **0** `.vue`) banked as a fresh born-RED witness |
| **M-2 / C-1** | kf-KeyframesStringControls **D-21/C-5** + kf-ACG **M-8/L-4(a)** + KF-APP-4 riders | MAJOR | Async-wrapper cell KILLED (ruling 4); NEW dual-shape rider: `Ref<any>` (useControlsKeyboardShortcuts.ts:20-22) vs unwrapped instance (RibbonBar :139), one producer, swap-undetectable — verified this seat |
| **M-3 / C-8** (format half) | kf-CSSCodeEditor **KF-CE-9 + KF-CE-37** → EDITOR-UNIT (KF-CE-37 already names RibbonBar.vue:28 as the PRIMARY unguarded path) | MAJOR | The isFormatting-latch session-corruption scenario is the banked KF-CE-9 cell verbatim — 3rd/4th derivations recorded |
| **M-3 / C-8** (copy half) | kf-KeyframesStringControls **D-13/L-M-7/C-6** | MAJOR | The guarded-vs-mute asymmetry table (6 of 8 honest) — concordant |
| **C-7** | kf-KeyframesStringControls **D-5/L-M-4/C-4** (the `display:none` brush twin; infinite loop in a never-unmounting pane) | MAJOR | Reader-LC's SELF-2 kill enriches the bank: Lucide icons are FUNCTIONAL components, so the template ref resolves to the real `<svg>` element — the targeting is mechanically sound; the target is merely invisible |
| **D-9** | kf-CubeScene **D-3/C-4** (the unexported `RIBBON_BUTTON_CLASS` seam; four competing item grammars) | MAJOR | NEW layout-root rider (reader-DU, verified citations): consumers return a bare vnode ARRAY (Cube), `w-full` GRIDs (Spring ×2), or a whole component (Easing) into a justify-center inline-pill flex row — a layout contract violated on top of the skin one |
| **m-2** | kf-CubeScene **D-2/C-3** (the `variant: "outline"` phantom prop, all four sites, glass-conformance packet) | MAJOR | 4/4-of-slot-consumers framing concordant; nothing new |
| **m-3(a) / C-10** | kf-ChannelControls **N-6** (stringly alphabet vs the closed union) | MINOR | Provenance kill carried (K8): RibbonBar.vue:133 imports `StoredAnimationGroupControlOptions`, NOT `ControlSurface` (my read); comparison sites re-pinned at FOUR (:8/:13/:69/:108) |
| **m-3(b) / C-6** | kf-ChannelControls **L-8** family (RibbonBar = the FIFTH raw-store bypass) | MAJOR (family) | Reader-LC's mount-order narrowing adopted: the reconciling `immediate` watch runs in ChannelControls' setup, which mounts BEFORE its later sibling RibbonBar (ControlsPaneWrapper :49 vs :91) — no first-render divergence; residue = the suspend window, UNPROVEN → SS-13 |
| **D-10** | CPW **D-m10** + kf-DemoGlobalChrome **D-9/L-11** (rainbow drift; the invented hue is glass's indigo AT this component's :50) | MINOR | The DGC attribution law holds against reader-DU's re-inherited "authoritative family" framing: the demo's 7-member family (cyan included) is in-family; the drift row carries |
| **D-11** | kf-DemoGlobalChrome **M-2** (no `gradientUnits` → per-path objectBoundingBox → 3 independent spectra) | MINOR | Nothing new; perceptual verdict stays DGC's SS-13 #5 |
| **D-15** | CPW **D-m9** + kf-CSSCodeEditor **KF-CE-13** → **KF.W9** | MINOR (demo-wide row) | Attribution corrected (reader-DU): the block that lands on these buttons is `utilities/a11y-overrides.css`, not `glass/a11y-fallback.css`; "unattempted" is wrong — attempted upstream, DEFEATED downstream = RB-1 |
| **D-16** | kf-ChannelControls **D-4** (the pl-4/pr-7 idiom) | MINOR | Count re-pinned: **3 lines / 2 files** (my grep; the bank's cell said two sites); reader-DU's sharpening (ControlsPaneWrapper.css mixes logical `padding-inline` mobile vs physical `padding-left/right` desktop within ONE file) adopted |
| **m-4** | CPW **D-m8/mi-1** (three homes; the directory named for the component doesn't contain it) | MINOR | The value.js U.B2 ruling citation (unexecuted move target) carried |
| **i-1 / F-1** | census **F-1 ≡ SCH-1** → **KF.W0 §B-12** — SUBSTRATE-DEAD as filed (ruling 7) | — | Subpath-convention correction rider on CPW C-20 |

### Booked here — MAJOR

- **D-7 (+ MISS-5 + MISS-6 as riders) — MAJOR · five sibling command strips with no group semantics, no accessible name, no roving tabindex, and focus destroyed on every tab switch; eight decorative glyphs unmarked.** Verified: `:12-15/:68-71/:107-110` bare flex divs, zero ARIA in the file (my grep), `v-if`/`v-else-if` branches unmount the focused element with no refocus and no live region. The complete cure is one directory away and already adjudicated correct: KeyframesEditor.vue:63-95's `role="toolbar"` + `aria-label` + `useToolbarKeyboard` roving set + `aria-hidden` decorative glyph (banked as correct at kf-KeyframesEditor superlative 2/SUP-3; its shadow-vs-glass evaluation rides KF-KE-36 → KF.W6). MISS-5 (the APG keyboard half — 4-8 tab stops inside a Drawer whose sequence already runs the pane) and MISS-6 (Lucide's `defaultAttributes` carry no `aria-hidden`; the sibling marks its glyph explicitly) are riders, both verified by the readers against the icon factory and unrefuted. Disposition: **NO-WAVE-OWNER** (one a11y spec row with the CPW role/name/aria-expanded cluster).
- **RB-1 (net-new; reader-DU MISS-1) — MAJOR · the demo's unlayered `.focus-ring:focus-visible { box-shadow: …; outline: none }` (design-idioms.css:76-79) silently DEFEATS glass-ui's forced-colors focus outline — under Windows High Contrast no ribbon button (and no glass Button demo-wide) has ANY keyboard-focus indicator.** This seat's own receipts: glass ships the cure unlayered (`@media (forced-colors: active) { .focus-ring:focus-visible, … { outline: 2px solid Highlight; outline-offset: 2px } }`, a11y-overrides.css — `@layer` count 0); the demo restates the selector unlayered with `outline: none` and imports LATER (style.css :3 vs :14); the shipped sheet's three `.focus-ring:focus-visible` rules extracted in order — the demo's `outline:none` is LAST. Same specificity (0,2,0), later wins, in forced-colors as everywhere; the surviving `box-shadow` is forced to none by the Forced Colors spec. WCAG 2.4.7/2.4.11 fail invisibly — the demo rule reads as a faithful restatement of glass's own base rule (which also sets `outline:none` and relies on the forced-colors arm to restore it). Not an omission (D-15's class) but a REMOVAL — subsumes-and-raises the banked D-m9/KF-CE-13 row. Disposition: **KF.W9** (the demo-wide forced-colors row) + the a11y spec surface; one live WHC witness → SS-13.
- **M-4 — MAJOR · the colocation inversion: 94 lines of cross-tree remote control disproved by the 9-line Teleport seam nine lines above them; the migration target ships in the same file.** Verified: `:5-9` publishes an empty landing pad and knows nothing (ChannelOptions fills it with 8 typed props — zero `any`); `:11-104` reaches into two components RibbonBar cannot see; KeyframeTimeline is ITSELF already teleported with `defer` (ChannelControls :186-191); vue `^3.5.35`. Hoisting the two ribbons into their owning feature clusters deletes the M-1/M-2/M-3 surface (~90 lines) at once. Neither reader found a constraint forbidding it; neither did this seat. MUST be specified together with the banked kf-ChannelControls L-2/C-2 `:key` blocker and the expanded-timeline family (one structural settlement). Disposition: **KF.W8** (Structure & Colocation Settle) as the RibbonBar-side spec input.

### Booked here — MINOR

- **D-8 / C-9 — MINOR · `text-emerald-500` is the demo's sole raw-Tailwind-palette colour (my grep: 1 hit), and the four-icon colour scheme carries no information** (inherited / gold / emerald / rainbow-stroke on one strip; zero coloured icons on the next). D-8's contrasting praise of `text-gold` is CORRECTED by RB-2 — the "discipline the demo does own" is itself a shadow. Disposition: **KF.W6** (token/theme discipline, the kf-CubeScene D-7 precedent lane).
- **D-14 / C-12 — MINOR · the file's only scoped rule is dead** (ruling 10 — payload proof + the S-11 registry repair). Dies in the D-B4/D-M11 batch; the :145-147 comment must be corrected by the same commit. **NO-WAVE-OWNER.**
- **C-11 — MINOR · two undeclared document-scoped dependencies** — the `#controls-ribbon-target` landing contract and the `url(#rainbow-gradient)` paint server, both documented one-way from the provider (DemoGlobalChrome's header names this ribbon as its reason). Cross-refs: DGC S-L2 (the singleton discipline), CPW S-10 (the `defer` correctness). A standalone host without DemoGlobalChrome renders a stroke-less invisible icon on a live button. **NO-WAVE-OWNER** (contract prose + the M-4 restructure shrinks the surface).
- **RB-2 (net-new; reader-DU MISS-2) — MINOR · `.text-gold` is an unlayered demo shadow of a LIVE glass utility of the same name, pinning a hand-copied colour.** This seat's sheet extraction: BOTH rules ship (`.text-gold{color:var(--gold)}` glass, in utilities; `.text-gold{color:var(--color-gold)}` demo, unlayered, later — permanently winning). Sharper than filed: glass itself declares `--color-gold: var(--gold)` (scale-paper.css, my read) — the demo shadows the CLASS **and** the alias TOKEN, then re-points the token at `hsl(43 74% 49%)` with an "matches glass-ui's --gold base" comment that is honest today (readers' conversion: within 1/255) and is exactly why a producer retune will silently not arrive. Third instance of the §6.3 flat-namespace hazard, first at the class level. Disposition: **KF.W6** (token-namespace lane; corrects D-8's cell).
- **RB-3 (net-new; reader-DU MISS-3, replacing killed D-17) — MINOR · three of the eight commands write the clipboard and none looks like it; the two file-transfer glyphs sit on the two commands with no file semantics.** Copy (Copy glyph) → clipboard; Export CSS (FileCode, emerald) → clipboard; Export (Upload) → clipboard (my useTimelineBuild read); Import (Download) → a paste-text dialog (my KeyframeTimeline read). Internal, measurable inconsistency appealing to no external convention; compounds D-18's naming pair ("Export CSS" and "Export" = two clipboard copies one tab apart). **NO-WAVE-OWNER** (one labeling/iconography pass with D-18).
- **RB-4 (net-new; reader-DU MISS-4) — MINOR (analysis row) · the bypassed emphasis axis is the common cause of D-1(≡D-B4) and D-14.** All eight buttons hard-code `emphasis="secondary"` — the compiled default, redundant ×8 — while the one stateful control hand-paints its ON state (`rainbow-vivid text-white`) instead of moving to `data-emphasis="primary"`, glass's tokenized, contrast-managed pressed rung. Taking the axis removes the AA failure by construction, deletes the dead border rule and its feeder class, and gives the bar one legible primary. No design ruling mandates the rainbow (the :145-147 comment defends a border, not semantics). Adopted as the CURE-DESIGN input for the D-B4 batch — per the ColorSpaceSelector law, the cure is adjudicated separately and this row is that adjudication. **NO-WAVE-OWNER.**
- **RB-6 (net-new; reader-LC MISS #1, consequence corrected — ruling 8) — MINOR · the Apply-CSS state outlives the only branch that renders its control**: the toggle lives behind `v-if === 'keyframes'` while the applied residue (forced pause + injected-but-inert sheet + target classes, per banked L-BL-1) persists across tabs with no visible undo until the user returns. Compounds banked N-5 (`clear()` unwired). Disposition: **NO-WAVE-OWNER (APPLY-UNIT)** — the state and its affordance get one lifetime.
- **RB-7 (net-new; reader-LC MISS #2) — MINOR · the four Timeline commands vanish exactly when the timeline is most visible.** Verified this seat: `isTimelineVisible = selectedControl === "timeline" || isTimelineExpanded` (ChannelControls :377-379) mounts the expanded timeline regardless of tab; the tab shows a "Timeline expanded below" placeholder (:156-170); the ribbon dispatches on the narrower `=== 'timeline'` (:69); Snapshot/Import/Export/Add CSS exist nowhere else (defineExpose census :296-307 — the in-pane bar owns only undo/redo/expand/clear). Persisted `isTimelineExpanded`, reachable in three clicks. The ribbon-side instance the banked L-8/expanded-timeline family lacked. Disposition: **KF.W7** (Timeline Evaluate — settle with L-2/C-2's `:key` blocker and C-8/D-6's empty-panel row).

### Booked here — INFO

- **D-18 / i-2 — INFO · label grammar unsystematised** (bare verbs + verb-object in one strip; `Copy` never says what; zero tooltips where the band uses them elsewhere); the sibling skin-divergence half lives at the kf-CubeScene D-3/C-4 fold. Rides RB-3's pass. **NO-WAVE-OWNER.**
- **RB-5 (net-new; reader-DU MISS-7) — INFO · `--scale-hover: 1.08` re-declared in the demo's unlayered `:root`** (design-idioms.css:41, "mirrors glass-ui's --scale-hover") — identical value to glass's scale-paper.css today (my reads), invisible until a producer retune doesn't arrive. Not reachable from RibbonBar's own markup; recorded for the namespace lane. → **KF.W6** (token lane, with RB-2).
- **RB-8 (net-new; reader-LC MISS #5) — INFO · the row-container class string is triplicated in-file** (`flex items-center justify-center gap-2 flex-wrap` ×3, :14/:70/:109) beside the RIBBON_BUTTON_CLASS hoist that exists to prevent exactly that. Hygiene; dies in the M-4 restructure. **NO-WAVE-OWNER.**

## Killed-claims register — 14 entries

| # | killed claim | adjudication |
|---|---|---|
| **K1** | **D-17** (Import/Export iconography inverted — "both target local-file operations") — including reader-LC's CONFIRMATION of it | Killed on its premise (ruling 2, this seat's tail reads: paste-text dialog + clipboard write; no file at either end). The Download=Import mapping is internally consistent with the dialog's own `:button-icon="Download"`. RB-3 is the provable replacement. A kill-of-a-confirm ACROSS readers — reader-LC restated the false premise as its proof cell |
| **K2** | D-2's "one `cssApplied` fact surfaced by two competing UIs" | Both readers, concordant: two facts — distinct `useApplyCSS` instances over distinct animations (`springEditAnim` at SpringPhysicsFacet.vue:119); ratified |
| **K3** | D-1's band mechanism ("label sits across the green→blue span; :14 justifies it there") | Both readers: `:14` is the row container; the button is shrink-to-fit; the true band ≈11–89% INCLUDES yellow 1.33:1 — killed toward understatement; conclusion worse, folded as a D-B4 rider |
| **K4** | **L-SUP-2** ("the scoped rule … mechanically sound, which I verified") **+ REGISTRY: kf-ControlsPaneWrapper S-11** ("a rule that wins on merit") | Ruling 10 — dead payload (no border exists on neutral tone), wrong specificity arithmetic ((0,2,0) EQUAL, layer decides); the superlative verified delivery of a no-op. S-11 RETIRED at the registry; the craft credit (scoped-rule-over-`!important`) survives as prose |
| **K5** | C-3's aggregate headline ("of its five tokens, none is both live and correct") | Both readers: `rounded-full` redundant (not incorrect), `gap-1.5` byte-identical on fine pointers; corrected form "four of five are dead, redundant, or defeat an axis" carries at the D-M11 fold |
| **K6** | D-15's attribution ("contrast-mode accessibility is unattempted"; sourced to `glass/a11y-fallback.css`) | The operative file is `utilities/a11y-overrides.css` (my read); glass ATTEMPTED it and the demo DEFEATS it — RB-1 is the corrected, stronger row |
| **K7** | m-5's enumerated empty-slot mechanism + D-13's "the enumeration cannot be written" | Ruling 5 — the enumeration was written (surfacesFor total; null returns complementary; facet surfaces only on ribbonContent-bearing scenes — this seat's read). Survivors ride the CPW SS-13 #10 residue |
| **K8** | L·m-3(a)'s "RibbonBar imports that very type at :133" (+ "three literals") | :133 imports `StoredAnimationGroupControlOptions`; four comparison sites (my read). Defect stands at its N-6 fold; the sentence dies |
| **K9** | BOTH readers' "the 'typed' hop is an async wrapper that never carried the surface" | Banked kill RE-FIRED (ruling 4): `defineAsyncComponent<T>` returns `T` (kf-KeyframesStringControls' ruled decider; kf-ChannelControls C-7 concordant). Erasure = ACG :191 + the B-1 shim, both banked |
| **K10** | BOTH readers' phantom-dep cells ("npm ci cannot resolve :132"; F-1 as live) | SUBSTRATE-DEAD re-fire (ruling 7): origin pins 7.0.0; the clean-ci probe was RETIRED by name at kf-KeyframesStringControls L-M-9 |
| **K11** | **m-6** — BOTH readers' confirmations ("v-show keeps a per-frame `<PlaybackRibbon>` alive behind display:none") | Ruling 3 — ChannelControls.vue:98 unmounts ChannelOptions (and the Teleport source with it) on every surface switch; the hidden landing div hides EMPTY. The real per-frame cost is the channel axis, banked at kf-ChannelOptions L·M-3 |
| **K12** | Reader-LC MISS #1's consequence ("both animate the same target simultaneously") | Killed by banked L-BL-1 (the injected sheet binds nothing); RB-6 carries the corrected residue |
| **K13** | Reader-LC misses #3/#4 as NET-NEW | BOOKING KILLS ≡ kf-KeyframesStringControls N-5 (ruling 9); recorded as 2nd/3rd derivations |
| **K14** | Reader-LC's SELF-1 (kebab-case slot props not camelised → slot always empty) and SELF-2 (component-proxy ref → brush no-ops) | The reader's own kills, both by execution/read (compiler camelises; functional-component ref = the svg element) — preserved as method exemplars; SELF-2's mechanism enriches the C-7 fold |

## Superlatives (provenance: corpus → readers → this seat; L-18 runs both ways)

1. **S-1/SUP-A — `v-show` on the Teleport landing pad — SUSTAINED-WITH-CORRECTION.** The verb is right (stable anchor + `defer` = no mount-ordering contract; a `v-if` target could strand a still-mounted source). CORRECTED (ruling 3): the "drops `<PlaybackRibbon>` with its scrub state on every round-trip" cost clause is overstated — ChannelControls :98 remounts ChannelOptions per surface switch anyway (banked at kf-ChannelOptions), so the scrub state dies regardless. The craft survives; the drama does not.
2. **SUP-B/D-SUP-1 — the touch target is compliant BECAUSE the author delegated ≡ CPW S-7** (fold by reference — the banked form: the design system holds the 54px floor AGAINST the call site's `h-8`). Both readers re-derived independently; concordant.
3. **SUP-C/D-SUP-2/C-S-C — zero shadow PRIMITIVES, QUALIFIED.** Imports = 8 glyphs + 3 glass components + 1 type; no hand-rolled card/button/tablist/motion; 3 lines of local CSS. The qualification is now load-bearing: the cascade AROUND the file shadows two glass classes that land on this very component (RB-1 `.focus-ring` arm, RB-2 `.text-gold`) — "zero shadow surface" is true of the file, false of its blast radius.
4. **SUP-D (both readers, sharpened by reader-LC; concordant with banked kf-KSC) — the Export CSS comment is TRUE and guards the most dangerous action.** `:32-35` is fully discharged by its tail (compileToCSS + verbatim CC-3 refusal surfacing); reader-LC's sharper fact adopted: it is the ONLY handler whose entire tail — including the same `copyText` call that goes uncaught in `copyCSS` — sits in try/catch, and the only ribbon-reachable path into value.js's throwing serializer. The best-defended action is the biggest one; three stale claims ship in the same 151-line file.
5. **SUP-E (reader-DU, new) — every command carries a visible text label; not one is icon-only.** The accessible name always exists, is always correct, and cannot drift — which is exactly what bounds D-2's damage to STATE (one attribute wide, not eight names wide). Contrast TransportDock's five hand-maintained aria-label strings.
6. **Reader-LC (new) — correct by construction on lifecycle**: the whole `<script setup>` is six statements — no listener, timer, observer, watcher, hook, or reactive state; nothing to tear down. Concordant with the L axis's cleared-hypotheses table (#11). The right shape for a relay whose targets are async, force-mounted, and style-injecting.
7. **S-3/L·S-3 — the Controls seam is the migration target, already shipping nine lines above the disease.** Feeds M-4 directly: the remediation needs copying, not designing.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **RB-1 witness** — Windows High Contrast (or forced-colors emulation): focus a ribbon button; assert NO visible focus indicator; then delete the demo rule and assert the Highlight outline returns. (The static cascade is closed; the witness is for the record.)
2. **D-19** — which clamp arm binds with `--glass-tint-strength-aa: 0%` (style.css :203-208 vs the "DARK substrate" rationale vs `color-scheme: light`): measure ribbon plate ↔ page and ink ↔ plate in the LIGHT theme and under `prefers-contrast: more`.
3. **D-20** — whether the "Persistent controls ribbon" (`flex-shrink-0`, no `position`) scrolls out of the pane; interacts with the banked CPW D-M2/MISS-2 height chain.
4. **D-13/m-5/C-13 residue ≡ CPW SS-13 #10** — the controls-tab pre-Teleport sub-tick and any standalone `extraTabs` host: is the empty cartoon Card visible at all? (This item was already the bank's decider; reader-LC's enumeration narrowed it to exactly these windows.)
5. **D-1/D-B4 rendered band ≡ CPW SS-13 #7** — worst-case stop under the glyph, live.
6. **MISS-8 (INFO rider on CPW D-M12)** — coarse-pointer wrap geometry: ribbon height and row count at 390px, keyframes tab, then the SpringScene `w-full` grid branch (D-9 rider).
7. **m-3(b)/C-6 reachability** — a live instance of the suspend-window divergence (three seats have failed to construct it statically; the structural row stands regardless).
8. **RB-6 witness** — apply → switch tab → Play → return: record the toggle/state incoherence (bounded by banked L-BL-1's inert sheet).
9. **m-1 residue** — what `.btn-interactive` once painted (unrecoverable from the tree; rides the KF.W6 sweep).

## Routing summary (M-25 agglomeration input)

- **KF.W0 §B-12**: the phantom-dep substrate schism (K10; manifests vs origin pins) — already banked, re-confirmed.
- **KF.W4**: B-1 ≡ KF-APP-4 (+ the listFiles receipt).
- **KF.W6**: the phantom-utility sweep (D-3/m-1/C-2 ≡ kf-CubeScene D-11, 8 sites); token/namespace rows RB-2 · RB-5 · D-8/C-9; KF-KE-36's toolbar-composable evaluation (D-7's cure source).
- **KF.W7**: RB-7 + the expanded-timeline family (with banked L-2/C-2 and C-8/D-6 — one settlement).
- **KF.W8**: M-4 (the two-ribbon hoist; the RibbonBar-side colocation spec input) + the m-4 fold's directory settle.
- **KF.W9**: RB-1 + the D-15 fold (the demo-wide forced-colors row ≡ KF-CE-13/D-m9).
- **NO-WAVE-OWNER (SS-1/SS-2 intake)**: the D-B4 batch (D-1 + D-2/C-5 + D-14/C-12 + RB-4 + the D-M11 string deletion — one edit family at the bank's sequencing); D-7 (+MISS-5/6) into the CPW a11y spec cluster; N-4's `:disabled` seam; C-11; RB-3 + D-18; RB-8; the kf-CubeScene glass-conformance packet folds (m-2, D-9).
- **EDITOR-UNIT / APPLY-UNIT**: M-3/C-8's two halves at their banks; RB-6 + the N-5 confirmations.
- **SS-13**: the nine probes above.

## Closing verdict

**ADJUDICATED at worktree ≡ origin/master `81a56990` — RED, but almost entirely PRE-BANKED RED: 33 rows stand (19 folds to seven sibling apotheoses with zero re-bookings — including both blocker-weight identities — plus 3 MAJOR · 8 MINOR · 3 INFO booked here), 8 net-new rows admitted (RB-1..RB-8) of which one is MAJOR and none was findable without reading the shipped sheet against the installed producer; 14 killed-claims entries including two kills of reader confirmations settled at the bytes (D-17's no-file-anywhere premise, m-6's unmounted-parent premise), one banked kill and one substrate-dead kill re-fired against BOTH readers, and one registry repair (CPW S-11 retired for praising a dead payload — the registry held to its own standard).** The component's terminal shape: a correctly-delegating, label-honest, lifecycle-clean relay whose every axis of drift — cascade, typography, state, a11y, iconography — was already adjudicated at its neighbours, and whose two genuinely new defects are both instances of one disease: the demo's unlayered restatement idiom, deliberate and documented for tokens, silently killing the producer's forced-colors cure (RB-1) and a live producer utility (RB-2). Nothing here stamps a wave; every disposition is agglomeration input for the forming X·KF specs, awaiting the owner's begin-word.

— end of apotheosis. The three challenge files and both fresh reader returns remain immutable beside this record per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/transport/controls-pane/RibbonBar.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **RibbonBar.vue=+9/-9**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**THE BYTE-OFFSET WITNESS IS SUPERSEDED — this record cites `dist/gh-pages/assets/index-CL_QYCiO.css` by name, and that coordinate is dead.** One `npm run gh-pages` ran at this seat (2026-09-17 13:37, exit 0, `built in 2.81s`). The artifact is now **`dist/gh-pages/assets/index-CBB2Hr7m.css`** — 571 192 B (was 571 142 B), mtime 2026-09-17 13:37 (was Jul 16 09:11), tree digest `bad6ea595fb59899e6589ad731b542d77eb19079a67d10548f6c07fc3c9564d2` over 54 files, per-entry hashes at `artefacts/W0/glass-citation-rederivation.txt` §5. Seven of the corpus's byte-offset receipts were re-derived in the fresh build and **all seven reproduce** (`data-allow-motion` 1 · `header-ribbon__actions` 1 · `btn-interactive` 0 · `--shadow-glass:` 0 · `skeleton-scan` 1 · `header-items-wrapper` 0 · `header-collapsed` 0 · `rainbow-vivid` 1). **Only the filename moved**; the findings stand, now on a dated and hashed artifact instead of a provisional one.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

