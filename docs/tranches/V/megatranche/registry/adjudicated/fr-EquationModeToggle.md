claude-fable-5 (served model id — this seat's own system prompt, not inherited)

# fr-EquationModeToggle — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator)

**Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/equation/EquationModeToggle.vue` (80 LOC, read whole by this seat) · fourier HEAD `cd26c653`, audited AS WORKING TREE (the one-line dirty delta is load-bearing — FR-EMT-25) · installed producer `@mkbabb/glass-ui@4.0.0` (80-key export map, my read) · producer working tree **8.0.0** (my read — both challenge axes declared 7.0.0; see FR-EMT-22).
**Date**: 2026-08-10 · **Corpus**: three challenge axes (D 18 rows · L 14+3 rows · C 19 rows) at `audit/fourier-components/EquationModeToggle/`, challenged by two hostile re-readers (design-and-usage · library-and-code), both returns adjudicated here.
**Method (M-23/L-18)**: every reader-vs-reader disagreement, every kill of a load-bearing cell, and every NEW missed row was re-derived against the trees by this seat with its own instruments before adoption. Zero browser probes — every ruling below is bytes (read + grep + git + node + python). Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments**: subject whole; EquationView.vue anchors `:43/:49-51/:82-84/:95-98/:114/:138/:141/:177-181/:269-271/:386-390/:426-434`; `lib/equation/types.ts:22/:31/:42-46`; `api/models/equations.py:20-21/:44-47`; `api/routers/equations.py:91/:96/:100-106`; `useEquationCache.ts:10-17`; `useCoeffHover.ts:13-18/:28-46`; `notation.ts:1-8`; `FunctionInput.vue:157-163/:244`; `GallerySearchBar.vue:69-70/:120/:159/:218`; `web/tsconfig.json` (0 `vueCompilerOptions|strictTemplates` hits); `git diff` on the subject (exactly one line, `glass-subtle`→`glass-wash`); installed dist: package.json exports (80 keys — `./toggle-group`, `./tabs`, `./button` all present), `ladder.css:36-42` (`.glass-wash` carries a literal `border: 1px solid var(--glass-border-wash)`), `material.css:180-225` (`.glass-wash:hover::before` → intensity 0.1, `:active::before` → 0.16), `base.css:174-178/:255-279` (focus-ring; tap-squish six legs + separate `:active` rule + PRM reset arm), `scale-paper.css:65-66`, `light-dark.css:17-22`, `offsets-sizing.css:150-151/:461`, `typography/scale.css:86-87` (`--type-micro: 0.6875rem /* 11px */`), `typography/utilities.css:50-56` (`text-mono-micro`), `tokens/glass.css:137/:159`, `segmented-tabs.css:27/:38/:47-48`, `button-BNDWhAZb.js` ghost + `sm` rows extracted verbatim, `tabs.js` `"option"` slot ×2 branches, `SegmentedTabs.vue.d.ts:56/:72` (**`modelValue: string`** — the FR-EMT-7 cure condition); Vue **3.5.38** `runtime-core.cjs.js` `useModel` setter early-return (the C-19 decider); producer 8.0.0 `package.json` + `Button.vue:1-18` ("It never composed a ladder rung and a capsule at once again") + `:46` `ButtonTone = Extract<Tone,"neutral"|"destructive">`; an independently written sRGB→WCAG-2.x pipeline (third derivation: **3.9422** / 4.6246 / 4.7087 / 5.1158 — agreeing with both readers to 3 significant figures on every disputed ratio); format-4 cmap parse of all four vendored CM woffs (**U+03A3 present in all four, U+2211 absent from all four** — the R1 missed-find re-proven, not trusted); `public/fonts.css` (CM at exactly two weights per style); fleet counts re-run: `variant="` = **123 / 41 files**, glass-ui/button importers = **35**, `size="icon"` = **38**, `title="` = **42 / 19 files** (reproducing reader-1 and the banked AA-2 instrument, not L-B1's 95/34); `reconstructed_points` and `term_count` in `web/` = **1 hit each, the type declaration** — and **0 hits across every banked `fr-*/kf-*/pt-*/wb-*` record** (the FR-EMT-20 novelty sweep); census `lane-frontend.md:135-140/:434-446` read in place.

## Scoreboard

**23 adjudicated defect rows (BLOCKER 1 · MAJOR 8 · MINOR 14) + 2 INFO · 9 killed claims ratified/minted · 11 reader-missed finds adopted (all re-proven here) · 6 superlatives · 8 UNPROVEN-NEEDS-LIVE → SS-13.**

Blocker-weight survivor: **FR-EMT-1** — the pane's DEFAULT mode renders the previous notation's equation under a fresh notation pill, and the component's one-prop contract has no channel to say so. Everything else on this file is the same single shape: **80 lines that name four producer contracts (`variant`, `size`, `focus-ring`/`tap-squish`, the glass material) and then discard the substance of each through 47 lines of unlayered scoped CSS.**

## Ruled disagreements (the adjudication's actual work)

| topic | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **Focus-clip geometry** (D-1/C-2/L-M1) | "100 % clipped" is FALSE — the interior-seam arcs survive inside the track's padding box; kill the absolutism, bank MAJOR | "the ring has nowhere to paint" (all four edges); MAJOR via the banked m-16 precedent | **Reader-1's geometry wins; MAJOR unanimous** | `.eq-toggle` is `display:flex`, zero gap/padding (:34-40); the 2px outset ring on a button's INTERIOR edge falls over the sibling — *inside* the clip box. Both buttons are `position: relative; z-index: auto` (:55), paint in DOM order, and the inactive sibling's background is `transparent` (:54), so the surviving crescent is not occluded. Clipped on three of four edges: a WCAG 2.4.11 area failure, not a clean 2.4.7 total-invisibility failure. D-1's own falsifier text ("no visible indication whatsoever") stakes the absolutism and dies with it. |
| **C-3 severity** (transition clobber) | MAJOR — "the single strongest find in this corpus" | MINOR — mechanism banked at fr-App MG-δ (adjudicated MINOR on the app's PRIMARY nav control); this file is a later declarant | **MINOR — precedent controls** | fr-App.md:77 (MG-δ, MINOR, → F.W3/W4) + fr-EasingPicker.md:67 (MISSED-A: "same defect CLASS as banked fr-ConvergenceTimeline D·D-4 — pattern identity noted, separate site, not a re-book"). A two-button leaf cannot out-price the primary nav under the same mechanism. Reader-1's observation that D and L both missed the clobber *while praising the same line* is accurate and preserved (DISSENT-2). |
| **D-4/C-7 severity** (3.94:1 active amber) | MAJOR | BLOCKER — "genuinely new; convicts the D.W4.d carry for every tinted surface in the tree" | **MAJOR (instance) + the fleet carry-recalibration booked as its own limb** | The instance is an AA normal-text failure on one state, light-mode-only, of one control — MAJOR by every banked contrast precedent (fr-AnimationControls D-5 books an arithmetic-ceiling ≈2.2:1 at MAJOR). Reader-2's escalation reason is the *fleet* limb — the carry at `style.css:113-122` was calibrated against `--background` and is mis-based for every tinted surface — and that limb is real, new (registry-swept: no banked row carries the tinted-plate figure), and booked inside FR-EMT-5 with its own routing. DISSENT-1. |
| **C-16 severity** (zero coverage) | MINOR | MAJOR | **MINOR — identity guard** | The absent unit runner is a repo-level standing fact the census already carries (lane-frontend §9, "carry 11 — no vitest"; cited by the L axis itself). This file's share is one assertion-free screenshot slug (`visual-baseline.spec.ts:34`, my grep). A repo-wide condition is booked at the scope where the count lives (the ColorSpaceSelector L-9.1 scope law); the leaf row is MINOR. |
| **FR-EMT-20 admissibility** (dead wire fields) | not filed | MAJOR missed find | **ADOPTED at MAJOR** | My own greps: `reconstructed_points` and `term_count` each appear in `web/` exactly once — the declaration (`types.ts:31/:45`). Producer side re-read: `equations.py:100-106` pays a per-term complex-exponential reconstruction (schema bounds `n_harmonics ≤ 200` × `n_eval_points ≤ 5000` → up to ~10⁶ exps/request) and serialises it for nobody. Same seam as FR-EMT-1, opposite direction: `/simplify` under-projects while `/compute` over-projects. 0 banked hits fleet-wide. |
| **L-M4's routing hedge** ("4.0.0 PLAUSIBLE → route F.W3 post-uplift") | hedge unnecessary — slot confirmed at the pin | same, with d.ts types | **KILLED (K-7); cure available at the installed pin** | `tabs.js` carries the `"option"` scoped slot in BOTH render branches (2 hits, my grep; both readers' verbatim extractions agree); `./toggle-group` is a real 4.0.0 export key with shipped files (my 80-key map read). No F.W1 dependency exists for the fold. |

## Adjudicated defect roster

Line anchors are working-tree at HEAD `cd26c653` + the one-line dirty delta; re-resolve before any cure (D-19). Identity guard applied throughout: banked identities are FOLDED by reference, never re-booked.

- **FR-EMT-1 = L-B2 / C-1 / D-9 — BLOCKER (seam-attributed).** `/equations/simplify` returns `{latex, energy_captured, term_count}` and **no sigma arm** (`types.ts:42-46`, `models/equations.py:44-47`); the sigma render is notation-dependent (`equations.py:96` `render_latex_sigma(terms, req.notation)`) and budget-independent (`:91` — the L §5.1 self-kill, re-verified); a notation flip routes ONLY to `doSimplify` (`EquationView.vue:177-181`), which writes `displayLatex` alone (`:138`); `displayLatexSigma` has exactly three occurrences (:39 rehydrate / :50 read / :114 compute-only write, my grep); `eqMode` defaults `"sigma"` (:43); `activeLatex` (:49-51) guards emptiness, not freshness, so a stale-but-truthy sigma wins; `:141` persists the mismatch across reload. The component's whole contract is `:5` — no `disabled`/`available`/`stale` input, so the parent has NO channel to mark the branch unavailable. All eight falsifiers across both axes re-verified; none holds. ATTRIBUTION: the defect lives at the EquationView/wire seam; the leaf's share is contract arity. **Rider (R1 missed-4, re-verified)**: the coefficient hover popover reads its LABEL KIND from the stale rendered markup (`useCoeffHover.ts:13-18/:28-46` — `closest('.eq-coeff')` + CLASS_MAP over `eq-an/eq-bn/eq-cn/eq-An`) while computing VALUES from fresh state — a second victim of the same staleness, one hop past where the L axis's trace stopped. **→ F.W5** (envelope: `SimplifyResponse.latex_sigma`, or explicit invalidation — filed beside intake R6-8 as the second instance of the partial-projection law) **+ F.W4 rider** (the mode-selector contract gains per-option availability; the fold at FR-EMT-7 supplies `disabled` for free).
- **FR-EMT-2 = D-1 / C-2 / L-M1 — MAJOR.** `overflow: hidden` on the padding-less pill track (:34-40) clips the producer's outset 2px `focus-ring` box-shadow (base.css:174-178, scale-paper.css:65-66 — no `inset`) and the forced-colors `outline-offset: 2px` arm (a11y-overrides) on three of four edges of both buttons (geometry ruled above); fourier's own focus-visible rescue set (`style.css:137-143`) omits `.eq-toggle-btn`; the producer forbids the pattern by name (glass-ui src `material.css:97`, configurator `styles.css:104-106`). Post-uplift escalation CONFIRMED: ≥7.0.0 adds `contain: paint` to the `.glass-wash` cohort, so deleting `overflow` alone stops being a cure — but note the 8.0.0 Button no longer composes ladder rungs at all (FR-EMT-22), so the escalation must be re-derived at the adopted pin. Mechanism identity **FOLDED → fr-CollapsibleSection m-16** (banked MINOR); booked MAJOR here as a strictly worse instance: sole controls of the card's overlay layer, no inline room anywhere, rescue-set omission. Paint extent → SS-13. **→ F.W3/W4** (structurally dissolved by FR-EMT-7's fold).
- **FR-EMT-3 = D-2 / L-M2 / C-6 — MAJOR.** Zero `aria-*`, zero `role` in the file; selection carried only by `.is-active` (:14/:24/:63-66) — while the pinned ghost variant already ships the full state register (`aria-pressed:bg-foreground/10 aria-pressed:text-foreground`, my verbatim extraction), the house pattern is documented IN THIS DIRECTORY (`notation.ts:3-8` names `aria-pressed` as the idiom), and three in-tree siblings conform (BasisSelector, GallerySearchBar:69-70/:120, CanvasOverlayButton). Under forced-colors the selected state is annihilated (no selected-state rescue at 4.0.0; the ≥7 toggle-group ships one). One binding supplies semantics + plate and deletes :63-66. Reader-2's sharpening adopted: `.is-active` is a producer-reserved state token (`surfaces.css` keys `.glass-btn.is-active, [aria-pressed="true"]` as synonyms) — this file adopts half the vocabulary. **→ F.W3/W4.**
- **FR-EMT-4 = D-3 / L-M3 / C-4 — MAJOR.** `:46-47` `padding: 0 0.6rem; height: 2.25rem` is the unscaled identity of `--control-h-sm` (`offsets-sizing.css:150`) with both clamps stripped; the coarse-pointer block (`light-dark.css:17-22`) intends `max(2.25rem × 1.5, 2.75rem)` and the producer's comment names the 44px floor; fourier re-pins neither scalar (my grep: 0). Cascade win holds by both routes (unlayered scoped CSS over `@layer`; (0,2,0) over the utility). Arithmetic re-derived: 40.5px phone / 36px tablet-desktop vs `max(60.75, 49.5)` / `max(54, 44)`. Rendered px → SS-13. **→ F.W3/W4** (dissolved by the FR-EMT-7 fold; NOT cured by F.W1 — the hardcode is consumer-side).
- **FR-EMT-5 = D-4 / C-7 — MAJOR (instance) + fleet carry limb.** The active label paints `--viz-amber` on `--card` darkened by an 8% foreground wash → **3.942:1** light-mode (third independent derivation this seat: 3.9422; readers: 3.942/3.9435) — under AA for 16px/11px text — and inverts hierarchy (selected 3.94 < unselected 5.116). Calibration arms all reproduce (amber/card 4.625 · amber/background 4.709 = the repo's own certified "≈4.6" · v7-v8 `--section-color-5` 5.11 published, ~4.36 on this plate — the uplift improves and still fails; **the cure is the wash, not the token**). The intervening `.glass-wash` plate is luminance-neutral over a flat card (`--glass-bg-wash` is card-over-card, `glass.css:137` — the check neither axis ran, run by reader-2 and re-verified here). **Fleet limb (new, registry-swept)**: the D.W4.d carry at `style.css:113-122` is calibrated against `--background` and is mis-based for every tinted surface; wherever live KaTeX ink shows through under the wash the ratio only worsens (SS-13). **→ F.W3** (a11y: drop the ink film / let `aria-pressed` supply the co-calibrated plate) **+ carry amendment** to `style.css:113-122` (re-derive against tinted surfaces — agglomeration input for the F.W3 spec, cross-cite FR-EMT-22 for the v8 token re-base).
- **FR-EMT-6 = C-3 — MINOR (class fold; severity ruled above).** `:51-52`'s two-leg `transition` shorthand (unlayered) clobbers BOTH `.tap-squish`'s and `.btn-pill`'s six-leg coherent sets (base.css:258-269 + surfaces.css:136-156, both with the producer's capitalised RED-witness-5 warning, my reads) — the press `scale: var(--scale-press)` still applies via the separate `:active` rule and SNAPS with zero duration. The producer's intra-library mitigation cannot reach an unlayered consumer rule. Class **FOLDED → fr-App MG-δ · fr-ConvergenceTimeline D·D-4 · fr-EasingPicker MISSED-A**; book only this file's `:52` as a further declarant site. Interaction note preserved (see superlative 5): the clobber destroys the transition, NOT the PRM `:active` reset — PRM correctness survives the defect. **→ F.W3/W4.**
- **FR-EMT-7 = D-5 / L-M4 / C-9 — MAJOR (shadow + census amendment + cure condition).** An 80-line hand-rolled binary segmented control whose own parent imports `SegmentedTabs` (`EquationView.vue:13`) and mounts it 82 lines above this child (:188-191 vs :270); the pinned primitive supplies `role`/`aria-orientation`, per-option `aria-pressed`, per-option `disabled` (the exact affordance FR-EMT-1 needs), Tooltip wrapping, a positioned indicator instead of a clip, and the `#option` scoped slot `{option, active}` — ALL at 4.0.0 (my dist reads); `./toggle-group` is ALSO a real 4.0.0 export (K-1). Census inversion CONFIRMED in place: `lane-frontend.md:137` carries no flag while `:138` flags the 47-LOC sibling; the file satisfies §4's own 🟡 CANDIDATE criterion verbatim (":434 — a producer subpath exists and is unimported"). **Cure condition (R2 missed-2, re-verified — the cure is adjudicated separately from the defect)**: `SegmentedTabs.vue.d.ts:56/:72` models a bare **`string`**, and the 4.0.0 toggle-group emits `AcceptableValue | AcceptableValue[]` — a naive fold erases the compile-checked `EquationDisplayMode` union all three axes crown as the file's superlative, in a tree whose only gates are `vue-tsc` + e2e. The fold must land with a narrowing setter (reject non-members), never the sketched two-liner; the parent already demonstrates the anti-pattern (`:190-191`'s `as`-cast). **→ F.W3** (shadow retirement, at the pin — no F.W1 dependency) **+ census amendment** (`lane-frontend.md:137` + §4 🟡 table row).
- **FR-EMT-8 = L-B1 (instance) / D §4 cross-check — MAJOR (fleet identity FOLDED → AA-2 / fr-BasisSelector M-7 / FR-COB-2, never re-booked).** `variant` is definition-absent on the producer Button at v7 AND v8 (`ButtonProps` = emphasis/tone/size/iconOnly/loading; 8.0.0 narrows `tone` to `ButtonTone`, my read); fourier has no `strictTemplates` (my tsconfig grep: 0), so both sites here fail SILENTLY into `emphasis: "secondary"` — a third glass plate stacking under the wrapper's wash at v7 (re-derive at v8: the Button no longer composes ladder rungs — FR-EMT-22). L-B1's novelty/severity/count framing is KILLED (K-6); what books here is the **instance**: two sites (:11/:21) → `emphasis="quiet"`, re-verified against the ADOPTED pin at F.W1 time. **→ F.W1** (rides the banked AA-2 census row).
- **FR-EMT-9 = C-5 / D-17 — MINOR.** Ghost is requested and then neutralised at the installed pin: `:49` kills `text-foreground/70`, `:54` kills every `hover:/active:/aria-pressed:` background in the ghost string (six declarations over four states, verbatim extraction); the only scoped hover rule restores color alone (:58-60), so an inactive segment has no hover or press plate. D-17's narrower contrast observation stays INFO-grade inside this row (both `--muted-foreground` 5.116 and `foreground/70` clear AA — my derivations). **→ F.W3** (dissolved by the fold).
- **FR-EMT-10 = D-8 / C-8 / L-m8 — MINOR (FOLDED → FR-AFP-34 + fr-ConvergencePlot D-10 rider, rides intake R3-7a).** Native `title` (:15/:25) over glyph-only content: the accessible names are the glyphs; the authored prose is demoted to a description; unreachable on touch/keyboard. The 42-occurrence / 19-file fleet measurement now reproduced by three independent instruments (L, reader-2, this seat). Carried limbs: CONVERT to Tooltip + `aria-label`, never delete (the glyphs are the only name — feeds FR-EMT-3's cure); R3-7a's `<Tooltip`-keyed budget is blind to the native-`title` surface (the banked D-10 rider's law, one more site). D-8(d)'s non-parallel prose rides along. **→ F.W3.**
- **FR-EMT-11 = C-16 — MINOR (scope-ruled above).** No unit runner exists (web/package.json scripts read whole; no vitest anywhere); `/equation` appears in exactly one spec as an assertion-free screenshot slug; `@axe-core/playwright` is installed and never pointed here. Repo-level gap = the census §9 carry, folded by reference; this file's share books here. **→ F.W9/W10** (the gates for FR-EMT-1/-5's cures ride their own waves).
- **FR-EMT-12 = D-7 / D-11 — MINOR.** Pill-inside-pill: both buttons keep `rounded-pill` at the track's own radius with zero gap → untinted lens voids at the interior seam; the DS solves the geometry with an inset indicator (`--bouncy-track-trim: 0.1875rem`, my read) and makes half-equality structural (`grid-auto-columns: 1fr`, `segmented-tabs.css:47`). `0.6rem` is off every grid in play and symmetric over asymmetric content (~1:1.63 split claimed — advances → SS-13; the absence of any equalising rule is source-decided). **→ F.W3** (dissolved by the fold).
- **FR-EMT-13 = D-18 — MINOR.** The two floating controls on the same card edge share `top: 0.5rem` (`EquationView.vue:426-434`) while the info button honours `--control-h-md` + the coarse clamp and the toggle does not — up to 27px height delta on touch inside a 10rem card. Same hardcode as FR-EMT-4; separate optical consequence. Rendered boxes → SS-13. **→ F.W3/W4.**
- **FR-EMT-14 = D-15 / C-15 — MINOR (latent).** Neither button emits `type`; the pinned Button forwards with no default → `type="submit"` by HTML default. `grep "<form" web/src` → 0, so portability-only. The ≥7 producer defaults `type` (banked at fr-BasisSelector M-7's improve-side) — F.W1 discharges it. **→ F.W1 note / F.W4.**
- **FR-EMT-15 = L-m3/C-18 · L-m4 · L-m5 · L-m6/C-10 — MINOR (contract-hygiene quartet).** (a) `EquationDisplayMode` is the wire module's only non-DTO export (`types.ts:22` — C-18's cite exact; L-m3's `:38` is drift); (b) `eqMode` alone among the panel's seven controls is not persisted (`CachedInputState` = six fields, my read; re-inits `"sigma"` every mount — compounds FR-EMT-1); (c) two model idioms in one directory (this file's 1-line `defineModel` vs NotationPills' 8-line manual pair); (d) hardcoded option twins with no exhaustiveness link to the union — a third member compiles clean and renders no control, while `NOTATION_OPTIONS` demonstrates the table idiom next door. **→ F.W4.**
- **FR-EMT-16 = D-12 / C-12 — MINOR.** Both font-stack literals are drifted partial copies (`:70` drops LMR + CMU Serif from `--font-sans`; `:75` drops the metric-compatible Fira fallbacks against which `:78`'s tracking is tuned). The load-bearing caveat CONFIRMED: `--font-serif-math` resolves nowhere in fourier — adopting `.cm-serif` today would REGRESS the Σ. The gap identity is banked fleet-wide — **FOLDED → fr-AppHeader FR-AH-2 (+ its GLASS-RELAY) · fr-BasisSelector m-3 · fr-EqCoefficientsPanel D/M-7**; book only this file's two literals. **→ F.W3/W4** (sequenced: wire the token FIRST). 
- **FR-EMT-17 = C-11 · L-m2 · D-13 · D-6(ii,iii)/C-13 · C-14 + the `text-mono-micro` join — MINOR (CSS residue, one cure).** Bare `ease` + hardcoded `0.15s` thirteen lines above the file's own canonical-token comment (`--duration-fast: 0.2s` exists); seven duplicated/dead declarations vs the producer base; absolute `px` type metrics immune to fourier's root ramp and `--ui-scale`; pressed tint 8% vs the DS's 10% register; and the join reader-2 minted (re-verified): `:74-79` is a drifted hand-copy of the producer's purpose-built `text-mono-micro` utility — same family, same 11px rung, opposite tracking sign (−0.045em vs +0.025em) — one utility is the single cure for four separately-filed rows. Adjacent banked register use: fr-CoefficientsSpectrum m-5 (different site, not a fold target). **→ F.W3/W4** (dissolved by the fold).
- **FR-EMT-18 = R1 missed-1 + missed-3 — MINOR (re-proven here, genuinely new).** The "Σ" is `&Sigma;` U+03A3 GREEK CAPITAL LETTER SIGMA set ITALIC at `font-weight: 600` — (a) it is the Greek *letter* (a variable, upright-operator convention violated), not the U+2211 N-ARY SUMMATION the card beneath renders; my cmap parse proves the choice is **font-forced** (U+2211 absent from all four vendored CM faces), so the cure is NOT a codepoint swap — render the operator from the math face KaTeX uses, or keep the letter upright; (b) CM is declared at exactly two weights (my fonts.css read), and per CSS-Fonts-4 matching a desired 600 resolves ascending-first to the **700 Bold Italic** face (`cmunbi.woff`) — the toggle's Σ is heavier than every `\sum` beneath it. All three axes examined this glyph's stack and size; none examined its codepoint or its resolved face. Optical mismatch → SS-13. **→ F.W4.**
- **FR-EMT-19 = R1 missed-2 — MINOR (corrects D-14's mitigation cell).** `.glass-wash` is NOT "purely decorative-inert here": the installed producer lifts the specular `::before` to **0.1 on hover and 0.16 on press** (`material.css:195-221`, my read — `.glass-wash:hover::before` / `:active::before` enumerated), compositing `plus-lighter` ABOVE both glyphs (the pseudo at z-1; buttons z-auto) at the centred fallback position (nothing writes `--mouse-x/y`). D-14's stacking read stands; its "wire it or drop it" prescription is under-specified — dropping the class deletes the track's only material and re-opens the HEAD dead-class state (FR-EMT-25). Adjacent to banked FR-COB-6 (v7 pressed-paint removal) — different mechanism, filed new. Perceptibility → SS-13. **→ F.W3/W4.**
- **FR-EMT-20 = R2 missed-1 + missed-5 — MAJOR (dead wire fields; admissibility ruled above).** `ComputeEquationResponse.reconstructed_points` (produced at `equations.py:100-106` at real compute cost, serialised, **zero consumers** — the client recomputes partial sums from `coefficients`) and `SimplifyResponse.term_count` (**zero consumers**) are dead projection on the same operation pair FR-EMT-1 convicts for under-projection. One envelope row, both directions. Registry-swept by this seat: 0 banked hits. **→ F.W5** (pairs with FR-EMT-1 in the partial-projection cohort beside R6-8).
- **FR-EMT-21 = R2 missed-3 — MAJOR (corpus meta-defect; verified in place).** The §4 shadow aggregate is not incrementable: `lane-frontend.md:446` headlines "9 components / ~1 990 LOC" while its own parenthetical sums to **2 079**; the banked fr-ConvergenceTimeline L·D-7 already stands a "10" (`fr-ConvergenceTimeline.md:53`, my read) for a DIFFERENT component; both challenge axes here independently minted "10" again for THIS component — three uncoordinated increments over an internally inconsistent base, with six 🟡 CANDIDATE members excluded from the aggregate. The figure must be **re-derived by enumeration** under X-9's member-scope law before any F.W3 budget consumes it. **→ F.W3** (census re-derivation; agglomeration input).
- **FR-EMT-22 = R2 missed-6 — MINOR (substrate re-pin; PARTIAL FOLD → fr-ConvergenceTimeline SR-1 + fr-EqCoefficientsPanel FR-EQC-16).** The producer is **8.0.0** (my read) while both the D and L axes declare 7.0.0. Banked drift rows cover the Slider path and the export census; the **Button+ladder path this component sits on is the unbanked limb**: at 8.0.0 the Button "never compose[s] a ladder rung and a capsule at once again" (my `Button.vue:1-18` read) and `tone` narrows — so FR-EMT-2's `contain: paint` escalation, FR-EMT-8's `emphasis="quiet"` target, and FR-EMT-5's token re-base all require re-verification at the ADOPTED pin, not 7.0.0. (The D axis was already reading past its declared pin: its `color-radius.css:309/:329` cites match the 8.0.0-era tree.) **→ F.W1** (the M-16/FR-EQC-16 re-pin gate, another confirmation).
- **FR-EMT-23 = R1 missed-5 — MINOR (banked-row correction, verified here).** fr-CanvasOverlayButton R-3's cell "every CSS rule is compound-scoped, **none reaches a `<Button>`**" is factually wrong on a point this component decides: at least three of its seven enumerated files put `.is-active` directly on a glass Button root — subject `:13-14`/`:63`, FunctionInput `:158-163`/`:244`, GallerySearchBar `:69`/`:159` (all re-read by this seat). Compound-scoping and reaching a Button are not exclusive: the parent's `data-v-*` lands on the Button's root, so the rule matches it. Consequence for the F.W3 sweep R-3 scopes: on Button-rooted sites, adding `aria-pressed` does not merely rename the hook — it ACTIVATES the ghost variant's `aria-pressed:` utilities and changes paint; `GallerySearchBar.vue:69-70` (BOTH `is-active` and `:aria-pressed` on one Button) is the in-tree migration exemplar. R-3's substance (a mechanical sweep would corrupt the PaperView prop) survives; the cell is corrected, the budget re-scoped. **→ F.W3/W4** (this record is the correction's home; R-3's dated record is not rewritten — E-3).
- **FR-EMT-24 = C-17 / S-4 / S-5 — INFO.** Zero value.js and zero keyframes.js consumption (my greps reproduce the census sets); the sole colour operation is a static CSS `color-mix()`; the glass import is a tree-shakable subpath in the conforming 42. This file is **off the F.W2 surface** — a load-bearing negative the F.W2 sweep must not violate. **→ census note.**
- **FR-EMT-25 = C S-3 / K-2 — INFO (immediate instruction).** The working-tree delta on this file is exactly one line, `glass-subtle` → `glass-wash` (my `git diff`); `glass-subtle` resolves NOWHERE (0 hits, dist + src), so at HEAD the track carried a dead class and no material at all; the wash rung is the documented correct tier for a detail tile and carries a real border (`ladder.css:36-42`, my read) that makes `:39`'s transition leg reachable. **LAND THIS DIFF** — the branch this tree rides must land-or-lose it. **→ F.W0** (substrate).

## Killed-claims register (all ratified or minted by this seat's own instruments)

1. **K-1 · D-5's uplift leg — "`./toggle-group` is a subpath ABSENT at 4.0.0 … nine defects are cured by F.W1; the F.W1 exemplar case" — KILLED.** The installed 4.0.0 manifest carries `./toggle-group` among 80 export keys with all files shipped (my read); `./tabs` likewise. The cure is available TODAY at the pin; the "nine defects retired at the uplift" framing mis-schedules the largest win on the file by a full wave. Surviving nuance (reader-2's): the 4.0.0 toggle-group is a thin reka passthrough, not the v7 rewrite D-5 sketches against — the fold target of record is `SegmentedTabs` (FR-EMT-7).
2. **K-2 · D-1/C-2's absolutism — "the focus ring is 100 % clipped / no indicator survives" — KILLED** (geometry ruled in the disagreements table; the clip itself is CONFIRMED and booked MAJOR at FR-EMT-2).
3. **K-3 · D-10 + L-m1 — "the `border-color` transition is dead; `glass-wash` contributes its rim through box-shadow/pseudo, never a border" — KILLED.** `ladder.css:36-42` gives `.glass-wash` a literal `border: 1px solid var(--glass-border-wash)` (my read); the token re-resolves across the theme flip. Both axes read the wrong file (`material.css` carries the rim; `ladder.css` the border) — L-m1 quoted a producer-source string that returns zero hits in the installed dist. The C axis killed this correctly at its own K-2 before either reader arrived. What survives is FR-EMT-17's narrow residue (bare `ease`, `0.15s` vs `--duration-fast`).
4. **K-4 · D-6(i) — "11 px is under the DS's 12 px caption floor; `size=\"sm\"` supplies `--control-text-sm`" — KILLED twice.** The scale ships the rung (`--type-micro: 0.6875rem /* 11px */`, my read) plus the purpose-built `text-mono-micro` utility; and the cva `sm` row is `"h-(--control-h-sm) rounded-pill px-3"` (my extraction) — it sets no text token, so D-6's coarse-pointer figure was computed off the wrong rung. Legs (ii)/(iii) survive inside FR-EMT-17.
5. **K-5 · C-19 — "clicking the active segment re-emits `update:modelValue`; the component cannot be relied on to emit only on change" — KILLED.** Vue 3.5.38's `useModel` setter early-returns before any emit when the value is unchanged (`runtime-core.cjs.js`, the guard read by this seat). The conclusion is inverted: the guarantee exists — it is the framework's. (It would be true of the sibling's hand-rolled pair, which is FR-EMT-15(c)'s point.)
6. **K-6 · L-B1's novelty, severity and arithmetic — "BLOCKER; the single largest UNCOUNTED line item; 95 sites / 34 files" — KILLED.** The fleet identity is banked three records deep and marked never-re-book (AA-2 · fr-BasisSelector M-7 · FR-COB-2, all read by this seat); my own counts (123 `variant="` / 41 files; 38 `size="icon"`; 35 button importers) reproduce the banked instrument, not L's subset count. The mechanism and the instance survive at FR-EMT-8; L's one genuine sharpening (silent-`variant` vs loud-`size="icon"` asymmetry) is already FR-COB-2's limb (a).
7. **K-7 · L-M4's routing hedge — "4.0.0 availability PLAUSIBLE, not CONFIRMED; route to F.W3 post-uplift" — KILLED** (the `"option"` scoped slot compiles into both render branches at the pin; C's K-3 had it; ruled in the table). The F.W3 routing stands for the correct reason (shadow-retirement wave), with zero F.W1 dependency.
8. **K-8 · C-9's aggregate arithmetic — "§4 moves from 9 / ~1 990 to 10 / ~2 070" — KILLED.** The base is internally inconsistent (headline 1 990 vs enumerated 2 079, read in place) and the increment collides with two other uncoordinated increments — FR-EMT-21 replaces all three with re-derivation by enumeration.
9. **K-9 · D-14's mitigation cell — "`glass-wash` is purely decorative-inert here" — KILLED** by FR-EMT-19 (hover 0.1 / press 0.16 arms enumerated for `.glass-wash` in the installed dist, my read). D-14's stacking read and its dead-`position: relative` observation survive.

## Superlatives (provenance attached; L-18 runs both ways)

1. **The contrast instrument (D-4/C-7 + both readers + this seat).** Three fully independent sRGB→WCAG pipelines now agree to three significant figures on every disputed ratio (3.942 / 3.9435 / **3.9422**), each calibrated against figures the repo or the producer had already published before asserting the new one — and reader-2 attacked the one omitted layer (the wash plate) and proved it luminance-neutral rather than assuming it. This is what a refutable number looks like; the tinted-plate figure convicts a fleet carry, not just a pixel.
2. **The C axis's kill discipline (§6, K-1/K-2/K-3).** Its K-2 caught an error BOTH other axes shipped as a finding — one of them with a falsifier it had already run and misread — by reading the working tree instead of HEAD and following the consequence into a live token. Its S-3 (the one-line `glass-subtle`→`glass-wash` diff curing a class that resolves nowhere) is the best single piece of work in the corpus; this seat re-ran both greps and the diff and seconds the instruction: **land it** (FR-EMT-25).
3. **L §5.1's self-kill.** The axis wrote its strongest possible BLOCKER framing (budget-staleness — the budget mutates constantly), then killed it against `equations.py:91/:96` (re-verified here: `simplify_series` takes budget, `render_latex_sigma` does not) and rewrote the defect to the notation-only form that survives four falsifiers. Six honest retractions across the three axes, each leaving a sharper claim.
4. **The model contract (`:5`), crowned by all three axes, both readers, and this seat.** The union is imported from the shared module, never re-minted; `required: true` closes the undefined arm; the model is never read in script, so the constellation's banked `defineModel` async-round-trip hazard cannot arise; the teardown surface is provably empty; and Vue 3.5.38 guarantees emit-only-on-change (the K-5 read). One caution now travels with the crown: FR-EMT-7's fold must not trade this compile-checked writer for a `string` cast — the cure condition is part of the record.
5. **PRM correctness by delegation, with the interaction nobody connected.** The component declares zero transforms; the press squish rides `.tap-squish`, whose PRM arm resets the scale in a separate rule (`base.css:271-279`, my read) — so FR-EMT-6's shorthand clobber destroys the *transition* and NOT the PRM reset. The file is PRM-correct without containing a PRM block, and stays so even inside its own defect.
6. **The `:51` provenance-comment practice.** A consumer file citing the wave that authored its rule (`A.W3.d`), on a token this seat verified resolves twice in the installed dist — the right shape of a design-system covenant in consumer code. That the rule thirteen lines above violates the same law (FR-EMT-17) is a MINOR against the file, never against the practice.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. Rendered extent of the surviving interior-seam focus arcs under `:focus-visible`, default arm and `forced-colors: active`; whether what survives clears WCAG 2.4.11's area + 3:1 tests (the clip RELATION is source-decided — FR-EMT-2).
2. Coarse-pointer rendered boxes: 40.5px vs `max(60.75, 49.5)` (FR-EMT-4) and the ≤27px sibling mismatch on the shared `top-2` baseline (FR-EMT-13).
3. The active label's effective backdrop wherever KaTeX ink shows through beneath the wash — the ratio moves only below 3.94 (FR-EMT-5).
4. Pill-in-pill seam optics (lens voids at the abutting arcs) and the ~1:1.63 `offsetWidth` split (FR-EMT-12) — radii and the absence of equalising rules are source-decided.
5. Specular gleam perceptibility at 0.1/0.16 `plus-lighter` over a 36px pill, and glyph-legibility cost on hover/press (FR-EMT-19).
6. C-L2's residue: whether `expanded` renders an N-term expansion clipped by `.eq-card { height: 10rem; overflow: hidden }` while the toggle keeps floating over the clip (rider on FR-EMT-1's family).
7. Whether the CM **Bold Italic** Σ is visibly heavier than KaTeX's `\sum` at rendered sizes (face selection is spec-decided — FR-EMT-18).
8. Live AXTree/axe readback for FR-EMT-3 (the source-side absence is decided; the announced experience is not).

## DISSENT — preserved, not ruled away

1. **Reader-2's BLOCKER on the contrast row (FR-EMT-5).** Its position: the finding convicts the D.W4.d carry for every tinted surface in the tree, and a carry-conviction outranks an instance. This seat ruled the instance MAJOR by banked precedent and booked the fleet limb explicitly instead — if the F.W3 spec's re-derivation finds further tinted-surface AA failures riding the same carry, reader-2's severity reading was the truer one and the limb escalates at that wave.
2. **Reader-1's MAJOR on the transition clobber (FR-EMT-6)** and its observation that the D and L axes praised `:51-52` while missing what the same line does to the producer's coherent set. The precedent-driven MINOR stands, but the observation is method evidence: a "compliant-looking" line is exactly where this class hides — the fourth consecutive corpus in which no axis opened the producer file under the consumer's shorthand.
3. **Reader-2's MAJOR on coverage (FR-EMT-11).** Its position: every other row in this record is ungated because of it. True, and still a scope error under the L-9.1 law — recorded so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small.
4. **The two readers' geometry disagreement on the focus clip** (all-four-edges vs three-of-four) was ruled for reader-1 on this seat's own paint-order derivation; if SS-13's live probe shows the interior arcs occluded in practice (e.g. by the active plate under stacked states), reader-2's stronger form revives with a pixel witness.

## Closing verdict

**DEFECTIVE — ADJUDICATED at 1 BLOCKER · 8 MAJOR · 14 MINOR (+2 INFO), with 9 killed claims and 11 adopted misses, all re-derived.** The single shape: a one-line, correctly-typed contract wearing 47 lines of unlayered CSS that re-derives, degrades, or clips everything the pinned producer already ships — while the seam above it cannot keep the promise the control's lit state makes. The terminal dispositions are three: **land the one-line diff (F.W0)**; **fold onto the pinned `SegmentedTabs` with the union-preserving setter (F.W3, no F.W1 dependency — dissolving FR-EMT-2/-3/-4/-6/-9/-12/-14/-17 structurally)**; and **fix the envelope, not the leaf (F.W5: `latex_sigma` on `SimplifyResponse` + the dead-field pair, beside R6-8's partial-projection law)**. Everything else is census truth: the shadow aggregate re-derived by enumeration, the carry re-based for tinted surfaces, the banked R-3 cell corrected, and every producer-fact re-verified at the ADOPTED pin — which is 8.0.0, not the 7.0.0 the corpus believed.

— end of apotheosis. The three challenge axes and both re-reader returns remain immutable beside this file per E-1/E-3.
