# fr-FrequencyGraph — adjudicated apotheosis (M-23/L-18 fresh-Fable adjudicator) · served model id: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh Fable adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-24 · **Registry**: value.js branch `tranche-u` · **Subject tree**: fourier-analysis branch `m/w1-bump-migration` (HEAD `cd26c65` per reader-LC's clean-tree receipt; this seat's whole-file read of the subject matches every L/C anchor).
**Corpus**: `docs/tranches/V/megatranche/audit/fourier-components/FrequencyGraph/` — challenge-D-design.md (claude-opus-5[1m], self-receipted line 1) · challenge-L-library.md · challenge-C-consumption.md — over `web/src/components/equation/FrequencyGraph.vue` (247) and its consumer/store/api/producer seams.
**Readers adjudicated**: reader-DU (design-and-usage re-read) · reader-LC (library-and-code re-read). Both were attacked before either was adopted; every reader-vs-reader divergence was re-derived by this seat with its own instruments.
**This seat's own instruments (all static + git/node; zero browser probes — probe parsimony; product source READ-ONLY; this file is the sole write)**: FrequencyGraph.vue read WHOLE (247 — every anchor in this record re-resolved against it); CoefficientsPanel.vue read WHOLE (26: `:components`/`:max-bars="40"`/`class="mb-2"`, `v-if="components.length"`, zero listeners — both readers' consumer surface confirmed); **producer glass-ui by git, the K-1 decider**: `git grep -n admin-label v7.0.0 -- src/styles` → **4 hits** (semantic.css:238 `@utility text-admin-label`, scale.css:86, bridges.css:16, typography.css:12); same grep at v8.0.0 → **0**; deletion commit `6b450f22` dated **2026-08-04** with `git show 6b450f22:package.json` → `"version": "7.0.0"`; tag v7.0.0 dated 2026-07-17; 8.0.0 bump `a8a6f66b` dated 2026-08-09; producer HEAD today `350f7a90` at 8.0.0; **installed fourier pins**: glass-ui **4.0.0** `dist/styles/typography/semantic.css:213-219` read verbatim (`text-transform: uppercase` + caps tracking + mono + 10px — the missed-uppercase decider), `dist/styles/tokens/color-radius.css:74` `--popover: var(--card)`, `dist/styles/tokens/dark-arm.css:64-65` `--card: hsl(24 8% 16%)` / `--popover: hsl(24 8% 16%)` byte-equal (the zero-figure-ground decider); **api models**: computation.py:43-45 `ComputeEpicyclesRequest { n_harmonics: int = 200 }` — **no Field bound at all**, vs visualization.py:76 `le=256` / :126,:187 `le=4096` (different models — the L-M6 bound decider); challenge-D-design.md read at §0 (fold table + the M-7 census row), M-3/M-4 whole; **anchor-drift grep** over challenge-D: font-size cited `:227` (live `:225`), opacity cited `:226` (live `:227`) — one consistent two-line transposition referenced across four rows (§A, M-2, m-7, m-8); **own re-derivations**: the M-8 path (for barH < 3 the corner-curve y(t) = (102−barH) + 3(1−t)², crossing the y=102 closing edge at t = 1−√(barH/3); figure spans [102−barH, 105−barH], exactly 3px tall, top edge strictly monotone in barH) and the S-2 floor (`Math.max(17n+11, 100)` at :33 → at n=1 the canvas is 100px, last-bar right edge 21, right gutter **79px**; the 7px identity holds only n ≥ 6); **registry identity sweeps**: kf-KeyboardShortcutsModal.md:83 K-2 + picker-pointerdebugoverlay.md:130 U-1 (Chromium ≥127 focusable scrollers — settled platform fact), fr-CoefficientsSpectrum B-3/M-6/M-10/m-17, fr-CoefficientsPanel FR-CP-2/4/6/8/9/10/16/17/21/23/29/31/32/35/38/40/43/45 (whole-file read), fr-App MG-β + B-2 (the "producer 7.0.0" roster reads), fr-EquationView RD-8 (the 66-vs-74-keys HEAD-quoted-as-tag precedent — the same error class as K-1) + its instruments line carrying the `--popover` token facts as instruments only, never as a defect row.

## Scoreboard

**37 adjudicated confirmed rows: 0 BLOCKER · 4 MAJOR booked + 6 MAJOR folds · 15 MINOR booked + 7 MINOR folds · 4 INFO booked + 1 INFO fold. 8 kills banked (6 upheld from the readers with this seat's own re-derivation · 2 minted or extended by this seat against a reader). 8 missed finds adopted after re-proof (5 DU · 3 LC), one demoted to INFO by ruling.**

Heaviest survivors are the banked fold cluster (FR-CP-8 tooltip-in-its-own-clip · FR-CP-4 nameless canvas · M-β1/FR-CP-2 the four-fork ramp · FR-CP-6 unconsumed contract · FR-CP-10 FadingScroll · FR-CP-16 seam invariants) plus four new MAJORs of this corpus's own: the scrollbar gutter eating the label band, text painted through half-alpha to ~2.03:1, the fixed-pixel island, and the axis that is annotated but never drawn.

## Ruled disagreements (the seat's actual work)

| # | question | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **C-1a booking** (per-hover bitmap reallocation) | FOLD → FR-CP-35, banked MINOR; "C's BLOCKER grade does not survive" | fresh-book **MAJOR** (~79 reallocations per 40-bar sweep, spec-correct "redundantly set" clause) | **DU wins — FOLD, banked grade.** | FR-CP-35 already books "draw() unconditionally resets the backing store (:58-61) and runs synchronously in mousemove on every bar/gutter transition" at MINOR with LC-of-that-corpus's MAJOR preserved as dissent and magnitude routed SS-13 (the R-8 ladder). Re-booking the same mechanism one file over at MAJOR is the re-grade the anti-rename law forbids. LC's spec citation (HTML §4.12.5 "or redundantly set to the value they already have") and the ~79/sweep count are ADOPTED as strengtheners into the banked record, not as a new row. |
| R-2 | **L-M5 grade** | font limb only, MINOR; DPR limb folds → FR-CP-35 | combined row MAJOR (font + DPR + theme) | **DU wins.** | The DPR and theme limbs are banked VERBATIM in FR-CP-35 ("no clock, no observer, no theme subscription: DPR + theme colour sampled at draw") at MINOR. Only the FONT limb is unbanked — my grep over the subject for `document.fonts|addEventListener|matchMedia` → 0; Fira Code is @font-face'd (fonts.css:69); draw() is reachable from exactly :139/:147/:157/:159. Books MINOR at FR-FG-8. |
| R-3 | **D-S-2's verification** (gutter symmetry "7/7 at n = 1, 12, 40, 60") | adopted into superlatives verbatim, incl. n=1 | KILLED the n=1 receipt (100px floor) | **LC wins — my own arithmetic.** | `canvasWidth = Math.max(n·17 + 11, 100)` (:33). At n=1: 28 → floored to 100; right edge 7+14=21; right gutter **79px** ≠ 7. The identity `right = left = 7` requires 17n+11 ≥ 100, i.e. **n ≥ 6**. The property survives as a superlative for every live n (12/40/60 re-derived, all 7/7); the claimed n=1 receipt does not — and reader-DU's superlative cell inherited it uninspected. Citation-inheritance event minted against DU (K-6, DISSENT-1). |
| R-4 | **L-M6/C-13's wire bound** | cites api/models/visualization.py:126,:187 `le=4096` as the wire fact | corrects: that model does not govern this path; `ComputeEpicyclesRequest` has NO upper bound; UI clamps 500 (BasisSelector.vue:30/:165); amplitude-descending slice makes 3-char labels routine, 4-char occasional | **LC wins — my own model read.** | computation.py:43-45 is a bare `n_harmonics: int = 200` with no `Field(le=…)` — the only unbounded sibling in a family of `le=256`/`le=4096` caps. DU attributed a different model's cap to this wire. LC's calibration correction to C-13's "|n| ≥ 100 routine" also adopted. Books at FR-FG-6. |
| R-5 | **D-M-8's "bowtie" clause** | "the closing edge back along y = 102 crosses the two quadratic segments" | "no cancellation, no bowtie — nonzero winding fills the union" | **Both right — merged by this seat's own derivation.** | The crossing is real: corner-curve y(t) = (102−barH) + 3(1−t)² meets the y=102 closing edge at t = 1−√(barH/3) ∈ (0,1) for 0 < barH < 3 — the path self-intersects. The fill consequence is LC's: nonzero winding paints the union, no visible cancellation is forced. What the raster actually shows at 9px scale → SS-13 (residue 4). The surviving defect books at FR-FG-19. |
| R-6 | **C-9 booking** (the contradicting display convention) | folded silently into the FR-CP-16 fold | separate MINOR row | **LC wins — separate row.** | FR-CP-16 books the enforcement gap; it nowhere records that `truncate_by_budget` (simplification.py:15-32) re-sorts kept terms by index "for display" — a second, mutually exclusive convention in the same repo that would break `maxAmplitude` (:38 reads `[0]` as the max) the day the reserved `#graph` seam (CoefficientsSpectrum.vue:8-9) is fed from it. A latent-seam FACT, not a duplicate of the hygiene row. Books at FR-FG-13, cross-ref FR-CP-16. |
| R-7 | **LC-missed-3 grade** (no `hover-harmonic: null` on unmount) | (not raised) | MINOR | **Demoted to INFO by this seat.** | The banked R-7(a) ruling (fr-CoefficientsPanel) already orders the selection contract — both emits included — DELETED (zero consumers; KISS law). A missing teardown emit in a contract adjudicated for deletion is spec input for the wire arm only, should the forming F.W4 spec overturn the delete. The find is real (emits fire only at :136/:146; the consumer's `v-if` unmounts mid-hover on all five store-clearing paths; `onUnmounted` imported at :2 and never called is exactly the missing hook) and is preserved at INFO, FR-FG-22. |
| R-8 | **The FR-CP-45 drift count** (DU: "exactly ONE anchor drifts" · LC: "systematically off; M-2, m-7, m-8 all inherit the slip") | — | — | **Merged — one transposition, four citations.** | My grep over challenge-D: D consistently believes font-size lives at `:227` (live `:225`) and opacity at `:226` (live `:227`) — ONE two-line mis-mapping, repeated in §A, M-2, m-7 and m-8. Everywhere else DU's 24 re-resolved anchors hold, including the two the CoefficientsPanel corpus recorded as drifted there. The conclusion both readers share — FR-CP-45's "take D's CLAIMS and L/C's LINES" is DOCUMENT-scoped, not axis-scoped — is ADOPTED at FR-FG-23; this corpus's D drifts at the BASELINE (K-1), not at the anchor. |

## Adjudicated defect roster

Routing law: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5-W8** CRUD/provenance union · **F.W9/W10** · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are forming; these routings are M-25 agglomeration input; nothing here stamps a wave. Line anchors are the subject tree at adjudication time; re-resolve before any cure (D-19). Identity guards: rows extending banked identities (**fr-BasisCanvas M-β1 · fr-CoefficientsPanel FR-CP-2/4/6/7/8/9/10/16/21/23/29/31/32/35/38/40/43 · fr-CoefficientsSpectrum B-3/M-6/M-10/m-17 · fr-App MG-β · kf-KeyboardShortcutsModal K-2 · census R3-10/R5-7/R6-6**) **fold by reference — never re-booked**.

### MAJOR band — new bookings

- **FR-FG-1 = D:M-4 / L-M4 — MAJOR.** The scroller's height equals the canvas height (:175 `height: 120px`, :174 `overflow-y-hidden`, :61), so a non-overlay horizontal scrollbar gutter is consumed from INSIDE the 120px box and clips the index-label band (baseline 116 at :111, 9px glyphs at :109, cap-top ≈109). The port always overflows: 17·40+11 = 691px content in ≤440px tracks (VisualizationView.vue:317-331). **Strengthened, not weakened, by K-2**: with the subject's own 4px `::-webkit-scrollbar` rule self-suppressed on Chromium ≥121 (the subject sets `scrollbar-width: thin` at :237), Chromium reserves the engine's own thin metric too — D's "digits survive on Chromium" reassurance is false. macOS overlay reserves nothing (D's own honest falsifier — why it survived the author's machine). Exact per-engine gutter px → SS-13. **→ F.W3/W4**.
- **FR-FG-2 = D:M-1 + §A (label-alpha arm) — MAJOR.** Index labels are painted through `ctx.globalAlpha = isActive ? 0.5 : 0.2` (:107) at 9px (:109) — ~2.03:1 light / ~2.51:1 dark, under half the 1.4.3 floor — and the alpha is applied to TEXT, breaking an otherwise-compliant token (`text-muted-foreground` → `--neutral-5`, chain verified at the installed pin). Both readers reproduced D's arithmetic blind (2.028 vs 2.03). The BAR-palette contrast arm (27/40 light-mode bars under 3:1, i=0 at 2.86:1) **FOLDS → FR-CP-2** (D-8's surviving arms; final roster SS-13). **→ F.W3/W4**.
- **FR-FG-3 = D:M-2 / i-6 — MAJOR.** A fixed-pixel island in an app whose root font-size is deliberately responsive (style.css:40-49: `html { font-size: 1.125rem }` with a ≥768px `1rem` arm): BAR_W/BAR_GAP/HEIGHT script consts (:25-27), 9px canvas raster (:109), `font-size: 11px` (:225) — no text-zoom or rem cascade can reach any of it (WCAG 1.4.4; full-page zoom scales CSS px and is unaffected — the charge is the text-zoom/rem register). The sibling 8px away uses rem-derived tokens. Reader-LC silent; reader-DU's proof holds on my read. **→ F.W3/W4**.
- **FR-FG-4 = D:M-9 — MAJOR.** The component names its y-transform (:168-170) and then draws no y-axis: draw() contains exactly two paint operations (the bar path :95-103, fillText :111) — no baseline rule, no tick, no gridline, no track; the baseline coordinate is already computed (pad.top + plotH = 102) and never painted; pad.bottom reserves a band and paints only labels into it. Made concrete by FR-FG-19: sub-3px bars overshoot a baseline the eye is given no way to see. The sibling ships at least a `bg-muted/50` track. **→ F.W3/W4**.

### MINOR band — new bookings

- **FR-FG-5 = D:M-3-as-corrected — MINOR (D's MAJOR demoted; mechanism killed at K-2).** The scoped block redefines `.scrollbar-thin` — a glass-ui-owned class (installed base.css:149-170 inside `@layer components`; byte-identical at the v7.0.0 tag, my git read) — and Vue emits the scoped rule UNLAYERED, winning per-declaration. The live modern-engine delta is exactly one declaration: `position: relative` (:238) — the containing block that CAUSES FR-CP-8. `scrollbar-width: thin` (:237) is the identical value the library declares; the library's `scrollbar-color` still paints (the subject never declares it); the ungated webkit pseudos (:240-246, 4px/`var(--border)`/2px vs the library's gated 8px/token/pill + track) are self-suppressed on modern engines and live only on legacy WebKit/Blink. Engine matrix → SS-13. Cure: dies with FR-CP-8's deletion + moving the containing block to `.freq-graph-host` (:216-218) — **already in the file, unused**, reader-LC's find. **→ F.W3/W4**.
- **FR-FG-6 = L-M6 / C-13-as-corrected — MINOR (bound per R-4).** Labels drawn with no measureText/elision/stride/clip at 17px pitch (:109-111, :25-26); `comp.index` is the SIGNED frequency (computation.py:113); at Fira Code ~0.6em advance, 3 chars = 16.2px (touching) routine on the amplitude-descending slice, 4 chars = 21.6px occasional; the live request model is UNBOUNDED (my read) with the UI clamping 500. Sibling allocates w-8 = 32px with an explicit sign. **→ F.W3/W4**.
- **FR-FG-7 = L-M1-as-narrowed — MINOR (L's MAJOR demoted; scenario killed at K-8) + CORRECTION to banked FR-CP-40.** `onClick` (:151-155) guards only `!== null` then indexes `displayComponents[hoveredBar]` — the template guard (:186) and loop bound (:75) cover the render path only, and the :157 watcher never re-clamps. The `[]` arm is unreachable (consumer `v-if` unmounts); the LIVE arm is a debounced shrink resolving while the pointer rests on a high slot → uncaught TypeError from a Vue handler (no `app.config.errorHandler`, LC's main.ts read). **FR-CP-40's clause "(shrink case guarded, so no crash and no gate sees it)" is corrected: true at :186/:75, false at :153.** **→ F.W3/W4** (one clamp line; rides FR-CP-40's watcher fix).
- **FR-FG-8 = L-M5 (font limb only, per R-2) — MINOR.** No repaint on font load: `ctx.font` set inside draw(); zero listeners/observers in the file (my grep); Fira Code is @font-face'd, so a spectrum that never changes keeps the fallback raster for the component's lifetime. Cure is L's own F-5 narrow one — a `document.fonts.ready` one-shot repaint (+ a matchMedia resolution listener for FR-CP-35's DPR limb) — **never** a substrate adoption (K-4). **→ F.W3/W4**.
- **FR-FG-9 = D:m-1 — MINOR.** Wrong z-band token: `z-[var(--z-controls)]` (= 20, the controls band) where the floating band is `--z-tooltip` (= 120; first-class `.z-tooltip` utility ships). Inert while FR-CP-8's clip governs; live the moment the tooltip is portalled. **→ F.W3/W4** (rides the FR-CP-8 cure; moot if the deletion cure lands whole).
- **FR-FG-10 = D:m-2 — MINOR.** Hand-typed near-miss of the shadow token family: `shadow-[0_4px_12px_rgba(0,0,0,0.12)]` (:190) vs `--shadow-soft` — a 12%-black drop shadow is invisible on the dark plate (`--card` dark = hsl(24 8% 16%), my token read), so the tooltip has no elevation cue at all in dark mode. Joins FR-FG-15. **→ F.W3/W4** (dies with the FR-CP-8 deletion).
- **FR-FG-11 = D:m-6 — MINOR.** The only prose explaining the transform is a `:title` on a `cursor: help` div (:168, :230), unlinked to the canvas it describes (zero `aria-*` in the file), `user-select: none` (:229) denying the formula's copy; ~1s delay, no touch or keyboard trigger. **→ F.W3/W4**.
- **FR-FG-12 = D:m-9 + m-5 + m-8 + DU-missed-2 — MINOR.** Typographic incoherence in a 120px box: the operator encoded two ways 35 lines apart (`log<sub>10</sub>` markup :169 vs Unicode `log₁₀` :204); **and `text-admin-label` carries `text-transform: uppercase` (installed semantic.css:213-219, my verbatim read), so the tooltip's log row renders the operator as `LOG₁₀(·+1)` — invalid notation in the one component whose credited best work is that exact operator** (latent while `logScale` is unwired per FR-CP-6; live the moment F.W4 wires it); axis label 3px out of register with the axis origin (`padding: 0 4px` :228 vs startX = 7 :73); four type sizes, two below the library's 10px micro floor. The register-imposition limb **FOLDS → FR-CP-21** (whose repair must ride the K-1 re-baseline). **→ F.W3/W4** riding **F.W1**.
- **FR-FG-13 = C-9 (per R-6) — MINOR.** The same repository holds a mutually exclusive display convention: `truncate_by_budget` re-sorts kept terms by `(abs(n), −n)` "for display" (simplification.py:15-32) — feeding that list through the reserved `#graph` seam breaks `maxAmplitude` (:38 assumes `[0]` is the max). Isolated today (equations.py:19-26 feeds the pre-truncation list); C's own "today it is safe" honesty adopted. Cross-ref FR-CP-16 (the enforcement point). **→ F.W5-W8** (validate/brand the ordering at the boundary) + **F.W4** (the seam annotation).
- **FR-FG-14 = C-10 — MINOR.** The x-position encodes amplitude RANK (:80, array ordinal) while the tick under it prints the harmonic INDEX (:111, signed n), under a component named FrequencyGraph whose axis label reads |c_n| — a Pareto chart of magnitude misdescribed by its name, its annotation, and its ticks. **Distinct from FR-CP-2** (which books hue-as-rank): this is POSITION-as-rank; identity checked, not folded. **→ F.W3/W4**.
- **FR-FG-15 = DU-missed-1 — MINOR.** The bespoke tooltip has ZERO fill contrast against the surface it floats over in BOTH theme arms — `--popover: var(--card)` light (color-radius.css:74) and byte-equal `hsl(24 8% 16%)` dark (dark-arm.css:64-65, my reads) — so its entire figure-ground is a 1.5px hairline at 1.90:1 light / 2.08:1 dark (under 1.4.11's 3:1), backed by FR-FG-10's invisible dark shadow. **Consequence adopted: this independently ratifies FR-CP-8's disposition — the repair is a deletion, not a positioning fix — because the hand-rolled skin has no figure-ground at ANY position, portalled or not.** Whether the glass Tooltip primitive's own content skin escapes the same token identity → SS-13 residue 9. **→ F.W3/W4** (rides FR-CP-8).
- **FR-FG-16 = DU-missed-3 — MINOR + CORRECTION to D:B-1 clause 2.** The scroll port (:172-176; overflowing, no focusable descendants, canvas has no tabindex) IS a keyboard tab stop on Chromium ≥127 and Gecko — unnamed, roleless, granting scroll and none of the 40 data values. The engine fact is banked, not asserted fresh (kf-KeyboardShortcutsModal K-2 · picker-pointerdebugoverlay U-1); the MECHANISM folds → fr-CoefficientsSpectrum M-6 (the sibling's port); THIS port is a new instance and the corpus asserts its opposite ("no keyboard register exists"), which is why it books rather than folds silently. B-1's core (no name, no route to the DATA) is untouched — it lives at FR-CP-4. Focus-ring visibility + SR announcement → SS-13. **→ F.W3/W4**.
- **FR-FG-17 = DU-missed-4 — MINOR.** The hover state's one unambiguous achromatic cue is declined: the bar path branches on `isHovered` (:86) but the label alpha branches on `isActive` only (:107, `isHovered` in scope and unused) — so during hover the harmonic number stays at 2.03:1 while the only feedback is a same-hue zero-offset bloom (:88-91) plus an 0.85→1.0 alpha step, on bars that already fail 3:1 (1.4.11 covers the state indicator too). One-line fix: pass `isHovered` into :107. Bloom perceptibility → SS-13. **→ F.W3/W4**.
- **FR-FG-18 = LC-missed-1 — MINOR.** The bar geometry is implemented TWICE as independently re-typed literals — `startX = BAR_GAP + 4` and the stride formula appear verbatim in draw() (:73, :80) and hitTest() (:123, :126) — with no shared layout function and no gate that could detect divergence (no vitest in web/, zero e2e references; the house pattern factors geometry out of paint one directory over in `canvas-drawing/`). Change one and every hover glows the wrong bar. Registry-swept: FR-CP-38 books the gutter/anchor ergonomics, FR-CP-35 the redraw cost — the duplication itself was unbanked. **→ F.W3/W4** (rides FR-CP-35's size/draw split — factor the layout once).
- **FR-FG-19 = D:M-8-as-demoted (K-3 survivor) — MINOR.** Bars below the 3px corner radius overshoot the (undrawn — FR-FG-4) baseline by 3−barH px (max 2.232px at the 0.008 floor) and their apparent HEIGHT stops varying while their position stays strictly monotone (my derivation: figure spans [102−barH, 105−barH], exactly 3px). The self-intersection is real (R-5) but nonzero winding fills the union. D's "renders identically" and "stops being monotone" are KILLED; D's one-line cure `r = Math.min(BAR_W/2, 3, barH)` is sound and adopted. Rasterised appearance → SS-13. **→ F.W3/W4**.

### INFO band

- **FR-FG-20 = D:i-5 / C-§0-as-strengthened — INFO.** Post-K-1, the correct census statement is STRONGER than D's: this file has **ZERO F.W1 break surface at the census's atomic 4→7 transaction** — the import surface is `vue` + one local type (:1-3); `fira-code`, `--z-controls`/`--z-tooltip`, `.scrollbar-thin` and `text-admin-label` ALL survive at the v7.0.0 tag (my git runs). The one asserted break (M-7) was false at the tag; D's recommended "CENSUS §5 typography addendum" dies with it. If F.W1 retargets to 8.0.0 the `text-admin-label` break returns — an owner/formation decision, carried in the K-1 relay, not a measurement. **→ F.W1 notes / NO-WAVE-OWNER**.
- **FR-FG-21 = LC-missed-2 — INFO.** `maxBars` is an unclamped public prop wired straight into a canvas dimension (:8, :29, :33, :58) — ~3,855 bars at dpr 1 crosses the 65,535px per-side UA ceiling into silent allocation failure and an undiagnosed TypeError at :64 — and the wire that feeds it is the family's only unbounded request model (my computation.py read; siblings carry `le=` caps). Latent (sole callsite passes 40; the 60 default has no user). **→ F.W5-W8** (bound the model with its siblings) + **F.W4** (clamp/document the prop). 
- **FR-FG-22 = LC-missed-3-as-demoted (R-7) — INFO.** No `hover-harmonic: null` on unmount while the sole callsite unmounts mid-hover on every store-clearing path — the unused `onUnmounted` import (:2) is exactly the missing teardown. Mooted by the banked R-7(a) DELETE ruling on the whole selection contract; preserved as the binding obligation on the wire arm should the forming spec overturn the delete. Rider on FR-CP-6. **→ F.W3/W4** (with FR-CP-6's disposition).
- **FR-FG-23 = DU-missed-5, merged per R-8 — INFO (corpus-quality, spec-authoring law).** FR-CP-45's "take D's CLAIMS and L/C's LINES" is restated as **document-scoped, not axis-scoped**: this subject's D document cites accurately at 24 re-resolved anchors (including both the CoefficientsPanel corpus recorded as drifted there), with exactly one two-line transposition repeated across four rows (my grep) — while carrying the corpus's only BASELINE failure (K-1) and its strongest reproducing instrument (§A). The drift here is at the baseline, not the anchor; a spec applying FR-CP-45 verbatim would discard 24 good anchors to guard one. **NO-WAVE-OWNER**.

### Folds (by reference — never re-booked)

- Canvas a11y (D:B-1 / C-4) **→ FR-CP-4** (MAJOR), with FR-FG-16's tab-stop register correction and reader-LC's cause correction adopted (the shield is the `test.fixme` keystones — FR-CP-9 — not the collapsed layer, which renders unconditionally per FR-CP-17).
- Tooltip-in-its-own-clip (D:B-3 / L-M2 / C-2) + the retired-construct re-introduction (L-M7) **→ FR-CP-8** (MAJOR; same adopted cause — the half-applied B.W2.c lift; "the repair is a deletion"), now independently ratified by FR-FG-15 and armed with LC's find that the correct containing block (:216-218) already exists unused.
- The `spectrumColor` four-fork (D:B-2 / M-11 / m-13 / L-B1 / C-6) **→ fr-BasisCanvas M-β1 + FR-CP-2 + fr-CoefficientsSpectrum B-3/M-10** — grade MAJOR per the R-4 ruling, LC-of-that-corpus's BLOCKER standing as dissent; L-B1's epicycle-vs-bar arithmetic (i=20: 168.4° vs 146.2° at nVis=80/n=40) adopted as a strengthener.
- FadingScroll unused over a 691px strip (D:M-5 / C-11) **→ FR-CP-10** (MAJOR; both pins verified by the readers).
- The unconsumed interaction contract (D:M-6 / M-12 / L-M3 / C-3) **→ FR-CP-6** (MAJOR, incl. the touch-dead-click limb) **+ fr-CoefficientsSpectrum m-17**.
- Undeclared seam invariants + upper-unbounded clamp (C-8 / L-m4) **→ FR-CP-16** (MAJOR; F.W5-W8 + F.W4).
- hitTest x-only gutters + entry-anchored tooltipPos (D:m-4 / L-m8 / L-m5 / C-14) **→ FR-CP-38(a)+(b)**.
- All-zero NaN spectrum (D:m-10 / L-m3 / C-12) **→ FR-CP-31** (+ LC's one-character cure `return max || 1` noted).
- hoveredBar ordinal survives recompute (L-m6) **→ FR-CP-40** (now carrying FR-FG-7's correction).
- `deep: true` dead walk (L-m1 / C-16a) **→ FR-CP-29** (mechanism per the banked R-2; LC's props-proxy refinement of L-m1's "fresh array literal" reason adopted — right conclusion, corrected reason).
- EB Garamond phantom family (D:M-10) **→ FR-CP-23**.
- Dead locals + toolchain blindness (D:i-1 / i-2 / L-i1 / i2 / C-17) **→ FR-CP-32**; the axe keystones **→ FR-CP-9** (LC's D-i-4 premise kill K-7 adopted).
- Imperative-loop census invisibility (L-m10 / C-17c) **→ FR-CP-43** (which explicitly demanded this file be checked — both readers correctly folded their "original" find).
- Bitmap reset + per-loop style reads + DPR/theme staleness (C-1a / C-1b / D:i-3 / L-m2 / D:m-11) **→ FR-CP-35** (R-1/R-2 here; strengtheners adopted; cure-wording correction at K-4).
- Four truncation literals (L-m9) **→ FR-CP-7 + FR-CP-2(a)** (the dead 60 default rides FR-CP-6's surface).
- The register imposition on the tooltip grid **→ FR-CP-21** (carrying the K-1 re-baseline obligation).

## Killed-claims register

- **K-1 — D:M-7 (+ its §0 "CONTRADICTED BY EXTENSION" row, its i-5 arm, its repair step 3, its CENSUS §5 addendum) — UPHELD, both readers, re-proven by this seat's own git runs.** The utility EXISTS at the F.W1 baseline: `git grep -n admin-label v7.0.0 -- src/styles` → 4 hits (semantic.css:238 et al.); v8.0.0 → 0; deletion `6b450f22` 2026-08-04. **This seat's nuance, softening the charge without weakening the kill**: `git show 6b450f22:package.json` → `"version": "7.0.0"` — the producer WORKING TREE self-reported 7.0.0 for five days (08-04 → 08-09 bump `a8a6f66b`), so the D axis (and the banked seats) read a tree that was version-lying, not a tag they misquoted. Under BASELINE LAW (K-12) the adopted tag decides regardless. **CROSS-REPO CORRECTION RELAY (not a re-book)**: banked **FR-CP-21** ("the utility is GONE at 7.0.0") and **fr-App MG-β** ("25-name roster, no text-admin-label") carry the same tree-as-tag error and must be re-resolved at the v7.0.0 TAG before F.W1 sizes any typography break; **every "producer 7.0.0" roster read taken in the 08-04..08-09 window is suspect** — fr-App B-2's `.paper-texture` premise is NAMED as needs-recheck (not adjudicated here). Precedent: fr-EquationView RD-8 (66-vs-74 keys, HEAD-quoted-as-tag). If F.W1 retargets 8.0.0, the break is real again and re-baselines rather than re-books.
- **K-2 — D:M-3's mechanism + divergence table + the M-4 Chromium reassurance — UPHELD, both readers.** The precedence is inverted: on engines supporting the standard properties (Chromium ≥121, Gecko, WebKit ≥18.2), setting `scrollbar-width`/`scrollbar-color` makes `::-webkit-scrollbar-*` pseudos IGNORED — the subject's own :237 self-suppresses its own :240-246. The library's `scrollbar-color` still paints (cascade is per-declaration; the subject never declares it); the table's "standard path: absent"/"track: absent" cells are dead code; §A's scrollbar row should present the `--scrollbar-thumb` figure; and M-4's Chromium arm is WORSE, not safer (feeds FR-FG-1). Core survives at FR-FG-5, demoted MINOR.
- **K-3 — D:M-8's "renders identically" + "encoding stops being monotone" — UPHELD, both readers, re-derived by this seat** (top edge = 102−barH, strictly monotone; figure exactly 3px tall). The bowtie clause merged at R-5. Survivor books FR-FG-19 at MINOR with D's own cure adopted.
- **K-4 — the prescribed canvas-lifecycle cures: C-1's `useCanvas2D` (BLOCKER framing) and L-M5's/banked FR-CP-35's `useCanvasSetup` clause — UPHELD, both readers.** (1) CLOCK: useCanvas2D is a per-frame rAF substrate (render(ctx, now) / arm/suspend/resume, IO + matchMedia) — adopting it installs a permanent frame loop in the repo's ONLY clockless canvas, manufacturing the rAF/PRM debt the census acquits this file of and destroying the credited superlative. (2) GEOMETRY: both substrates size from the container box; the subject's width is CONTENT-derived (17n+11) and deliberately exceeds its port — the horizontal scroll IS the design; either substrate deletes it. **Correction rider to banked FR-CP-35**: its cure directive ("hoist colour, split size/draw, rAF-coalesce hover") SURVIVES; its "house alternative (useCanvasSetup…) colocated and unapplied" clause does NOT — useCanvasSetup is a different contract, not an alternative. The sound narrow cure is L's F-5 pair (fonts.ready one-shot + resolution listener). C-1a's underlying facts fold at R-1.
- **K-5 — D:F-6's evidence clause ("the repo's only `reduce` block is scoped to tabpanel") — UPHELD, reader-DU.** Glass ships the universal PRM rule (a11y-overrides.css:6-16) reached via the verified import chain — the exact inherited-falsifier failure the registry banked as K-14 in the CoefficientsPanel corpus, now repeated by the D axis itself. Conclusion untouched (no motion here; PRM vacuously satisfied; F-6 stays correctly self-rejected).
- **K-6 — D:S-2's n=1 verification receipt, AND reader-DU's superlatives[4] cell that inherited it — UPHELD from reader-LC and EXTENDED against reader-DU by this seat's own arithmetic** (floor → right gutter 79px at n=1; identity holds n ≥ 6 only). The property itself survives as S-5. Citation-inheritance event recorded (DISSENT-1).
- **K-7 — D:i-4's premise ("axe keystones run and have never seen this component because the layer is closed") — UPHELD, reader-LC.** The keystones are `test.fixme` (banked FR-CP-9); visualization-crud:81-95 is the helper DEFINITION (live calls :529/:659); and the collapsed layer is no shield — FR-CP-17's CSS-only reveal mounts the canvas at boot. Conclusion (axe never evaluated it) survives on the corrected premise; same correction applies to L-i6 and C-3's reasoning.
- **K-8 — L-M1's failure scenario as filed (`[]`-branch TypeError on a stationary click) — UPHELD, both readers converging.** All five `epicycleData = null` sites UNMOUNT the component via the consumer's `v-if`; only the debounced-shrink residual survives, and it is real, unguarded at :153, and MINOR (FR-FG-7).

**Severity/re-book kills (rulings, not fact kills)**: reader-LC's MAJOR on C-1a and on the combined L-M5 row — rejected as re-grades of the banked FR-CP-35 identity (R-1/R-2); L-B1's BLOCKER — already adjudicated down at R-4 of the CoefficientsPanel corpus, both readers correctly declined to re-book; D's BLOCKER framing on C-1 dies with K-4. All preserved as dissent; the FR-CP-35 magnitude re-opens on SS-13 evidence as already banked.

## Superlatives (with provenance)

- **S-1 — the axis annotation** (D:S-1 · L:S-1 · C:S-3 · both readers · this seat's read): the applied y-transform rendered AS UI, mode-switched on the same prop `barFraction` reads, with the RATIONALE in the title ("The +1 shift admits zero amplitudes") and its own typographic register — it discharges a named 2026-05-18 audit finding at its own cited line. Bounded three ways: the log arm is dead at the sole callsite (FR-CP-6), it labels an axis never drawn (FR-FG-4), and the tooltip uppercases the same operator (FR-FG-12). Preserve verbatim through any repair.
- **S-2 — structural leak-immunity by enumeration** (both readers' independent greps → 0 hits for every clock/listener/observer primitive; census + lane-frontend corroborated): the only canvas in the repo that cannot leak and cannot compete for the frame budget; every listener template-bound. PRM vacuously satisfied. **This is the property K-4's rejected cures would have destroyed — the credit and the kill are the same fact; the board should not spend the virtue to buy an import.**
- **S-3 — D's §A measurement engine reproduces blind** (both readers re-derived independently: 2.861/2.86 vs D's 2.87; 4.713 vs 4.71; 2.028 vs 2.03; 1.90:1 border row exact): in a formation that has twice killed reader arithmetic mid-adjudication, a contrast model that reproduces to 3 s.f. at three rows should be QUOTED FORWARD, not re-derived. Its token inputs verified at both pins. (The same document's baseline failure is K-1 — the two facts coexist and FR-FG-23 is their reconciliation.)
- **S-4 — L's F-5 self-kill is the corpus's best epistemic move** (both readers): it raised the missing-ResizeObserver hypothesis, killed it against the tree ("canvasWidth is derived from data, not layout — the absence is CORRECT"), and thereby reached the single fact that decides the whole canvas-lifecycle question — the fact that kills both prescribed cures (K-4). The content-derived sizing at :31-34 is a deliberate, correct divergence from both house substrates.
- **S-5 — the gutter symmetry is derived, not eyeballed** (D:S-2 as bounded at R-3/K-6): right gutter = left = 7px algebraically, BECAUSE of the odd-looking `+ BAR_GAP + 8` term — for every n ≥ 6, which covers every live callsite; the n=1 receipt is killed by the 100px floor on the same line.
- **S-6 — the DPR idiom is textbook** (reader-LC, checked against the house composable): backing store at w·dpr, CSS size pinned separately, `setTransform(dpr,…)` so all coordinates are CSS px — the reason hitTest works without a scale factor. Correct idiom, incomplete lifecycle (FR-CP-35).
- **S-7 — the label colour is token-linked** (reader-LC): :108 reads the paint from the element's own `text-muted-foreground` — the only token-linked canvas text in fourier's canvas surface (grid.ts/labels.ts hardcode). Intent preserved through any repair; the mechanism (per-bar, in-loop, no theme repaint) is FR-CP-35's charge, and the Tailwind-v4 serialisation rider stays open (residue 8).
- **S-8 — the degenerate-data instincts are correct and correctly placed** (reader-LC): the 0.008 floor sits after the transform branch and before the pixel multiply (mode-invariant, yields a surviving hairline); `log10(x+1)` refuses to diverge at zero and the component SAYS so. Undone at the edges by FR-CP-31's NaN and FR-FG-19's overshoot — both one-line repairs, filed.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **FR-CP-8 residue**: exact clipped tooltip px at the left/bottom edges; whether hovering the last bar grows scrollWidth under the pointer (abspos descendant contributes to inline-end scrollable overflow — predicted, unobserved); rendered tooltip width (both axes estimate ~130-150px from source).
2. **FR-FG-1**: the per-engine reserved gutter for `scrollbar-width: thin` non-overlay — post-K-2 the Chromium arm bites too; how much of the digit band each engine clips.
3. **FR-CP-2 residue**: the full 40-bar contrast roster both arms + post-repair ratios (spot rows validated blind, S-3).
4. **FR-FG-19**: the rasterised sub-3px lozenge — bowtie, lump, or lost to antialiasing (geometry settled at R-5; pixels not).
5. **FR-FG-16**: whether Chromium paints a visible :focus-visible ring on this port; what a screen reader announces (the focusability itself is settled, banked K-2).
6. **FR-FG-17**: perceptibility of the same-hue bloom on failing-contrast hues; whether 0.85→1.0 reads as a state change.
7. **Touch (D:M-12 / C-K-2)**: whether a tap pins the tooltip with no dismiss path (synthetic-mousemove-before-click, engine-dependent; the static half — no pointer/touch/key handlers at all — is proven; C's withdrawal posture honest).
8. **FR-CP-35 rider (banked residue 8, still open)**: whether `getComputedStyle(canvas).getPropertyValue("color")` serialises to a `fillStyle`-parseable string under Tailwind v4 tokens — if not, every label silently inherits the previous bar's spectrum colour.
9. **FR-FG-15 residue**: whether the glass Tooltip primitive's content skin escapes the `--popover ≡ --card` identity — decides whether the FR-CP-8 deletion cure ALSO cures the contrast; requires reading dist/tooltip.js's applied surface classes.
10. **Not a measurement — an owner/formation decision, carried in the K-1 relay**: which producer version F.W1 adopts. At the census's 4→7 transaction this file's break surface is ZERO (FR-FG-20); at 8.0.0 the `text-admin-label` break returns and MG-β/FR-CP-21 re-baseline rather than re-book.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance events (evidence about the fleet, not the tree).** (a) Reader-DU's superlatives[4] repeated D-S-2's n=1 receipt verbatim ("at n = 1, 12, 40 and 60") — falsified by the 100px floor on the very line the superlative cites (K-6). The same reader independently killed D's baseline error (K-1) and re-derived §A blind — the inheritance was local, not systemic. (b) The D axis itself inherited the CoefficientsPanel falsifier's file-scoped PRM grep (K-5), the exact failure the registry had already banked as K-14. The pattern the family record now shows three times: kills demand re-derivation even when — especially when — the cell being copied sits beside genuinely original work.
2. **Reader-LC's re-book grades preserved as dissent**: MAJOR on the per-hover bitmap reallocation and on the combined font/DPR/theme row (R-1/R-2). Both re-open on the banked FR-CP-35 SS-13 magnitude evidence, not on argument.
3. **The K-1 nuance cuts both ways and is preserved whole**: the producer tree self-identified as 7.0.0 while carrying 8.0.0 deletions for five days. The D axis and the banked MG-β/FR-CP-21 seats read what the tree told them. The method lesson (tag-or-installed-pin, never a working tree) is law; the seats' diligence is not impugned — and the relay must re-check fr-App B-2's `.paper-texture` premise under the same suspicion window before F.W1 leans on it.
4. **R-7 minority position**: if the forming F.W4 spec overturns the banked delete and wires `hover-harmonic`, FR-FG-22 re-grades MINOR immediately and the `onUnmounted` teardown becomes mandatory — the dead import is the spec's reminder that the open half of a paired contract was authored without its close.
5. **Severity spread across the fork family, recorded without ruling**: fr-CoefficientsSpectrum banked its own `spectrumColor` instance at BLOCKER (B-3) while the FrequencyGraph/Panel instances stand at MAJOR under R-4 — one mechanism, three grades by surface. The M-25 agglomeration should read them as one repair with the Spectrum's animated-recolor as the aggravating instance, not as three independent defects.

## Closing verdict

**ADJUDICATED — 0 BLOCKER; 4 MAJOR + 15 MINOR + 4 INFO booked as FR-FG-1..23, 14 identities folded by reference (FR-CP-2/4/6/7/8/9/10/16/21/23/29/31/32/35/38/40/43 · fr-BasisCanvas M-β1 · fr-CoefficientsSpectrum B-3/M-6/M-10/m-17), never re-booked; 8 kills banked — K-1 (text-admin-label ALIVE at the v7.0.0 tag, re-proven by this seat's git runs, with the cross-repo re-baseline relay for banked FR-CP-21/MG-β and the named 08-04..08-09 suspicion window), K-2 (scrollbar precedence inverted), K-3 (sub-3px monotonicity), K-4 (both prescribed canvas cures structurally unsound — the narrow F-5 cure is the sound one, and FR-CP-35's "house alternative" clause is corrected), K-5 (PRM falsifier inherited), K-6 (the n=1 receipt, extended against reader-DU's inherited superlative cell), K-7 (dead keystones, not the collapsed layer), K-8 (L-M1's scenario narrowed to the real shrink residual, correcting FR-CP-40); 8 missed finds adopted after re-proof, the sharpest being the zero-figure-ground tooltip (--popover ≡ --card both arms — ratifying FR-CP-8's deletion cure from underneath) and the duplicated draw/hitTest geometry; routings are M-25 agglomeration input for the forming X·F specs — F.W3/W4 owns the frontend truth/a11y/geometry cluster, F.W1 owns the FR-FG-20 zero-break certification + the K-1 re-baseline obligation, F.W5-W8 owns the two boundary rows (FR-FG-13/21), NO-WAVE-OWNER carries the two corpus-law rows; nothing here stamps a wave.**

— end of apotheosis. The three challenge axes and both reader returns remain immutable beside this file per E-1/E-3.
