# GradientStopEditor — Adjudication (M-12 tri-fold, arbiter-F, L-14)

**MODEL RECEIPT**: arbiter-F ran on **Fable 5** (`claude-fable-5`), observed directly by this seat from its own system prompt, not inherited from the spawn label; adjudication session 2026-07-27, node v26.0.0 · darwin arm64, HEAD `c654824e`. Worker receipts: worker-F = Fable 5 (`claude-fable-5`, self-observed, recorded against L-11); worker-O = Opus 5 (`claude-opus-5[1m]`). The M-12 tri-fold assigns the F seats to Fable by design; this is not a violation of the all-Opus mark, which governs mechanical/challenge seats.

Corpus: 28 defects (D-1..D-14 design seat, L-1..L-14 library seat) against `demo/workbenches/gradient/GradientVisualizer/GradientStopEditor.vue` (392 lines) and its model tree. Two blind adjudicators returned; this seat attempted to refute both before adopting either.

---

## 0 · The headline ruling: the tri-fold's own contradiction was a defect wearing a disagreement's name

Worker-F and worker-O directly contradicted each other on L-14's precision arm — F: "the 15-digit literal does not reproduce at HEAD; my headless run yields rounded `oklch(74.98% 0.15006 145.24deg)`"; O: "re-derived LIVE, character for character: `oklch(74.979999519572% …)`". **This seat ran both cells and BOTH workers measured honestly — against different artefacts:**

- **Headless, the tree at HEAD** (`npx vite-node docs/tranches/V/megatranche/adjudication/arbiter-F/tmp/l14-decider2.ts`): the demo's exact mint chain (`interpolateStopColors` → `colorToCss` → `serializePickerColor` → library `serializeCssColor`, whose `format` is `Number(value.toFixed(12)).toString()` at `src/css/grammar.ts:283-285`) yields **rounded** literals at every probed t: `t=0.002 → oklch(74.98% 0.15006 145.24deg)`, `t=0.068 → oklch(74.32% 0.15204 153.16deg)`, `t=0.29 → oklch(72.1% 0.1587 179.8deg)`. Both on-disk builds agree: working-tree `dist/subpaths/css.js` AND installed `node_modules/@mkbabb/value.js@4.0.0` produce the identical rounded string on the same inputs (this seat's node runs).
- **Live, the dev server at :9000** (`node docs/tranches/V/megatranche/adjudication/arbiter-F/tmp/l14-live.mjs`, real chromium click on the rail): the SAME gesture mints `oklch(74.320000721154% 0.152039997837 153.159991346148deg)` — **15 significant digits, TODAY**, at the same t=0.068 where the tree yields `74.32%`.

**Therefore the long-running dev server serves a stale serializer — a THIRD code state, byte-different from both on-disk artefacts.** Worker-O's live cell was real; worker-F's tree cell was real; the ruling instrument is the tree at HEAD (waves are authored against the tree), so L-14's precision arm is **stale at HEAD and may not be cited** — but the live divergence is itself new, committed evidence for the L-8 family: the type gate, the headless runtime gate, and the served dev bundle are three different artefacts in one app. The tri-fold contradiction is the L-8 crack, demonstrated.

Standing consequence (binding, see Addendum §7): no literal/precision/serializer claim may be witnessed against the long-running dev server; the authoritative cell for mint-literal claims is headless vite-node against the tree, or a freshly restarted server.

---

## 1 · Method

Every factual claim below was re-derived by this seat: full reads of `GradientStopEditor.vue`, `useGradientModel.ts`, `useGradientCSS.ts`, `gradientParse.ts` (targeted), `useGradientInterpolation.ts`, `GradientVisualizer.vue`, `GradientPane.vue`, `color-utils.ts`, `picker-color.ts:206-211`, `src/css/grammar.ts:255-336`, `vite.config.ts:30-74`, `tsconfig.demo.json:31-51`, `e2e/smoke/views/gradient.spec.ts:155-175`, VISUAL-CONSTITUTION §4.1/§4.2/§5/§5.2/§7 verbatim; fresh scoped greps (`selectedStopId` = 3 hits all in GradientVisualizer.vue; `WatercolorDot` = 19 demo files / 0 in gradient; `@mkbabb/value.js/math` = 6 demo files — O's corrected count; `demo/ui` = 19 dirs; glass-ui 7.0.0 = 74 export subpaths incl. `./slider ./select ./chip ./button ./watercolor-dot`); node probes (both builds' `serializeCssColor`; `parseCssColor('oklch()')` THROWS `Cannot read properties of undefined (reading 'replace')` on working dist — L-2 re-verified); two vite-node runs and one live chromium probe (§0). Suspicious-agreement spot-checks (unanimous rows re-verified at source, beyond the two required): **L-1/D-2** (updateStop :127-131 no policy · addStop :118 sorts · intervals watch :89-100 tail-only), **L-5** (the five VALUE re-exports have ZERO consumers through the door — every door import across demo/test/e2e is type-only or the two constants; tests import direct — verified stronger than either worker stated), **L-7** (both cycle edges at the line; both type-only), **D-12/L-10** (:129-130 dual write; O's asymmetry arm verified — :152 and :160 clear without emitting), **D-7** (keyboard census :173-187; §5.2 row verbatim), **L-11** (:6/:71-73/:219-221; sole call site always passes), **L-12** (:80/:177/gradientParse clamp loop), **D-9** (GradientPane.vue:8 dead `cssColorOpaque` injection — O's find, verified), **L-8 facts** (7-key exports, no `.`; 8 hand-written tsconfig paths; 3 phantom targets ls-verified missing; no `/css` entry). Both workers' probe artifacts exist on disk (`audit/components/wb-gradient-stopeditor/evidence/p1-p5.mjs` + 4 PNGs; `audit/probes/wb-gradient-stopeditor/gate-{axis,seat,structure}.mjs`, `gate-order.mts`; worker-F's `adjudication/worker-F/tmp/repro-order.ts`).

Environment law honored: the dev server is API-LESS (no mongo) — data-backed arms are UNVERIFIABLE-HERE, never confirmed from a blank surface. All live cells are chromium/playwright-webkit; **no safari-app claim is made anywhere in this record** (I-20: separate evidence cells).

---

## 2 · Per-defect verdicts (28 rows; original ids for life)

| id | worker-F | worker-O | **ARBITER** | Deciding evidence |
|---|---|---|---|---|
| **L-1** | CONFIRMED | CONFIRMED | **CONFIRMED (BLOCKER)** | Unanimous + re-verified at the line: `gradientParse.ts` rejects non-decreasing; `addStop` sorts; `updateStop` (:127-131) has NO ordering policy and is the mutator this component drives on every pointermove/arrow. Both workers' round-trips (headless + live) agree to the digit; repro on disk (`worker-F/tmp/repro-order.ts`, `gate-order.mts` exit 1). The model serializes CSS its own parser rejects. Gate blindness real: e2e drags only the MIDDLE handle (re-read :155-175). |
| **L-2** | RESCOPED → π R1 | RESCOPED → parser band MT-F001 | **RESCOPED — same root, one identity** | Both destinations are the SAME row: MT-F001 (`registry/ROOT-FINDINGS.md`, 8-input class) = π row R1 (pinned authority). Re-verified: `parseCssColor('oklch()')` THROWS on working dist (this seat's run). Booked at the parser band adjudication; NEVER re-booked or renamed here (anti-rename/epoch rule). The runnable bank rides MT-GRADSTOP-1; no consumer try/catch (masking-fallback ban). |
| **L-3** | CONFIRMED | CONFIRMED | **CONFIRMED** | Border-box forward map (:75-81) vs padding-box inverse (:55-57, abs-positioned percentage) over a 1px-bordered rail — not inverse, re-derived by this seat in both languages; O's live G1b (`left: calc(0.2% + 9.96px)` from a click at the 0%-handle's own pixel) and F's arithmetic agree. Distinct from D-1 (different boxes, different magnitudes) — kept as its own arm, folded into the same axis cure. |
| **L-4** | CONFIRMED | CONFIRMED | **CONFIRMED** | Two implementations of one sampling concept verified at :64-88 (visualizer, exact) vs `sampleCoalescedStops` (32-discretised) — O's sharpening adopted: they are measurably NON-equivalent (ghost exact, rail discretised). No ` in <space>` clause in any emitted gradient though `interpolationSpace` is in scope (re-read :219-224, :300-304). O's cure-premise probe (CSS.supports `in oklch` = true in WebKit) stands. sRGB-deviation MAGNITUDE stays an honest unmeasured hypothesis. → W-GRADSTOP-3. |
| **L-5** | CONFIRMED | CONFIRMED | **CONFIRMED** | Verified STRONGER than either worker: every consumer through the `useGradientModel` door imports types or the two constants only; the five VALUE re-exports (:22-28) have zero consumers; the 3-hop constants chain vs MixConfigBar's 1-hop verified; both shims' comments name path-preservation. Dead doors. Ordering: L-7's leaf must land first. → W-GRADSTOP-3. |
| **L-6** | CONFIRMED (fold W-HYGIENE) | RESCOPED (demo-wide wave) | **RESCOPED — fold by identity → W-HYGIENE demo/ui extirpation** | Facts unanimous and re-verified (19 shim dirs; slider shim is one re-export line; three doors in GradientVisualizer.vue :9/:10/:12-13). GradientStopEditor itself imports NOTHING from demo/ui (verified: vue, @lucide/vue, one type) — O's ownership point wins the label; the identity is ALREADY BOOKED at the ColorPicker adjudication's L-6 (90-site extirpation) — one identity, never re-booked (anti-rename). The gradient tree's three-door callsites may ride any gradient wave touching those files. |
| **L-7** | CONFIRMED | CONFIRMED | **CONFIRMED** | Both cycle edges re-verified at the line (model:15-16 VALUE out; css:31-34 + parse:23-27 TYPE back). Type-only, erased by verbatimModuleSyntax — a declaration-graph disease, not a runtime hazard; it blocks L-5's deletion and denies OrderedStops a home. The leaf `model/types.ts` is W-GRADSTOP-3's first landing. |
| **L-8** | CONFIRMED (build W-TOOLCHAIN) | RESCOPED (toolchain wave, beside L-2) | **RESCOPED → W-TOOLCHAIN-ONE-ARTEFACT, strengthened by arbitration** | Facts unanimous, re-verified (7-key exports no `.`; 3 phantom .d.ts targets missing; no `/css` path; two byte-different builds). Not a GradientStopEditor defect (component resolves no value.js specifier). The tsconfig↔exports parity sub-arm is ALREADY BOOKED (ColorSpaceSelector adjudication L-14 → W-HYGIENE config parity; anti-rename — cross-referenced, not re-booked). The ONE-ARTEFACT arm books here and travels BESIDE the parser band (O: one causal chain), now carrying the arbiter's new third-artefact proof (§0): even the dev cell serves code that is neither build. |
| **L-9** | CONFIRMED (cost trimmed) | CONFIRMED (cost REFUTED by measurement) | **CONFIRMED — mechanism; cost clause struck** | Mechanism source-certain (:91-96 always-path during captured drag; no stopPropagation at :136-146; the :93 "fallback" comment is false). Cost arm: O MEASURED it (8 distinct `--rail-ramp` DOM writes across an 8-move real drag — Vue batches the two writes per event into one flush) and F's lazy-computed reasoning agrees; the "two 32-mix re-serialisations per move" prose is REFUTED and may not be quoted (Addendum §8). Real cost: 2× emit + 2× eager `stops.map` rebuild per event. FOLD with D-11. |
| **L-10** | CONFIRMED | CONFIRMED | **CONFIRMED** | Both arms verified: dual write channels (:129-130 → :140+:144) AND the outer `defineModel("selectedStopId")` (:51) that no parent binds (GradientPane.vue:25 bare; 3 repo hits total, all one file). Local ref in a public-API costume. FOLD with D-12; pure deletion. |
| **L-11** | CONFIRMED | CONFIRMED | **CONFIRMED** | :6 no-op destructure default on an already-optional prop; unreachable branch (sole call site always passes :139); the `?? null` fallback would MASK a future failure as a transparent ghost. Dissolves under W-GRADSTOP-3's `sampleAt`; interim required-prop lands in MT-GRADSTOP-1. |
| **L-12** | CONFIRMED | CONFIRMED | **CONFIRMED** | Three hand-rolled clamp sites (:80, :177, gradientParse position loop) vs published `clamp`+`scale` (`src/foundation/math.ts:2-12`; `scale` IS the axis map) with SIX demo files already importing `/math` (O's corrected count, grep-verified). Two idioms for one primitive inside the dogfood proof. |
| **L-13** | CONFIRMED (producer-routed cure) | GLASS-OWNED (relay only) | **GLASS-ROUTED (counted glass-owned)** | Facts unanimous and verified (88-line scoped re-derivation of focus-ring/forced-colors/hit-expander around raw elements; own comments cite the producer vocabulary; glass-ui ships ./slider). The defect's SUBSTANCE sits in our tree, but its cure (a `variant="rail"`/SliderThumb slot) is a PRODUCER change — glass-ui is not ours, no local patch may be proposed, and a wave gated on a sibling repo shipping is not completable (L-1). Disposition: relay to the standing glass BH inbox fond (owner edict 2026-07-12) + runnable bank (below). The a11y/interaction CONTRACT the composition would supply lands locally NOW via D-7's cure — accessibility does not wait on a sibling repo (O's ordering, adopted). Cells: I-20 respected — no safari-app claim. |
| **L-14** | RESCOPED (precision refuted; dialect survives) | CONFIRMED (live, char-for-char) | **RESCOPED — ruled in §0** | Both honest, different artefacts. At HEAD the mint rounds (this seat, three t values); live at :9000 it emits 15 digits TODAY (this seat, real click) because the served bundle is stale. Surviving arm (BOTH cells): the minted stop speaks percent-lightness + deg-hue beside authored fraction + unitless in ONE copyable readout — `oklch(74.32% 0.15204 153.16deg)` beside `oklch(0.65 0.18 265)` even when rounded. Dialect arm → W-GRADSTOP-3 (`formatColorLiteral`); precision citations banned (Addendum §5/§7); the stale-server fact travels to L-8's wave. |
| **D-1** | CONFIRMED (dissent: MAJOR not BLOCKER) | CONFIRMED (doc-comment arm narrowed) | **CONFIRMED at MAJOR** | The measured skew is unanimous and arithmetically re-derived by this seat (border-box ramp 0% at x=224 vs handle-track 0% centre at 235; 11px = 2.38% at W=462; agreement only at p=0.5). Severity RULED MAJOR (F's grade): nothing destroyed or lost; the BLOCKERs are L-1/D-2, D-3, L-2. Challenger's BLOCKER preserved in Dissent. Doc-comment arm NARROWED per O: :8-13 (echoed at useGradientCSS :257-264) is fairly read as DIRECTION normalization; but the phrase "share one axis by construction" is false under the geometric reading and may never be cited as alignment evidence (Addendum §2). |
| **D-2** | CONFIRMED | CONFIRMED (strengthened: easing re-pairs) | **CONFIRMED (BLOCKER)** | The model side of L-1, live-reproduced by both workers with convergent numbers. O's strengthening ADOPTED and verified structurally by this seat: `addStop` sorts while the intervals watch grows/shrinks the TAIL only (:89-100) — inserting a stop re-pairs every downstream authored easing = silent data loss, no reorder needed. The insert-pairing cure is added to MT-GRADSTOP-1 (this arbitration's amendment); the property gate G1 asserts both arms. |
| **D-3** | CONFIRMED | CONFIRMED (coarse-only scope) | **CONFIRMED (BLOCKER, coarse cell)** | Independently live-reproduced by BOTH workers on playwright-webkit iPhone-14 (tap at selected-handle centre +20px → count 3→2, silent, no undo); geometry closes by construction (chip 44px coarse ::before from top-11; z-20 beats z-10) — re-derived by this seat from the scoped CSS. O's fine-cell refutation recorded as SCOPE (no overlap at fine: chip ::before is max(1.5rem,100%)=24px). O's sharpening kept: the destructive band 14..22px is UNPAINTED. Evidence cell: playwright-webkit, not safari-app (I-20); mechanism is spec hit-test geometry. Interim geometric cure = G5; constitutional home (inspector) = W-GRADSTOP-2. |
| **D-4** | CONFIRMED | CONFIRMED | **CONFIRMED** | Class arithmetic re-derived (chip occupies y 44→68 of a 40px-tall root → 28px borrow, crossing the hr by 8px — byte-identical to both workers' measured 268.7 vs 260.7). PROPORTION-AUDIT §5.3 verbatim. O's narrowing kept: transient (v-if on selection) — but selecting a stop still repaints another section's boundary. G7. |
| **D-5** | CONFIRMED (§4.1 colour-only) | RESCOPED (wrong clause cited) | **CONFIRMED — authority corrected** | Constitution re-read by this seat: §4.1's bullet holds BOTH sentences. O's correction is RIGHT and binding: `scale(1.25)` is not a colour, so "never color-only" is not violated; the violated sentence is "Role, accessible name, state/value and associated error/status are explicit" (role null, aria-pressed/selected absent; at n=2 the chip is suppressed → no third cue). The tautological-specimen arm stands BY CONSTRUCTION (the handle paints `linear-gradient(c,c)` on a ramp that is c at that position); O's honesty adopted — his pixel probe was broken and discarded, the committed shot is the visual witness. Gates written against the corrected clause only. The same mis-citation may ride other components' D-rows (flag relayed in Dissent). |
| **D-6** | CONFIRMED | CONFIRMED | **CONFIRMED** | §7 verbatim re-read; greps re-run (19 demo files / 0 in gradient). O's ownership check decisive for BUILDability: `@mkbabb/glass-ui/watercolor-dot` is a PUBLISHED subpath (in the 74, verified) — a CONSUME, not a producer change; parts company with L-13/D-14 on exactly that line. → W-GRADSTOP-2. |
| **D-7** | CONFIRMED | CONFIRMED | **CONFIRMED** | Keyboard census structurally certain (:173-187 — Home/End/Up/Down/PageUp/PageDown/Space absent); no role/aria-value*/ordinal (both workers' live a11y dumps agree). §5.2 row verbatim: Home=0%/End=100% and the ordinal announcement are UNconditional; Up/Down "when supported" (F's nuance kept). → W-GRADSTOP-2; gates G3c/G3d-class, asserted against style.left never the rounded aria-label (Addendum §6). |
| **D-8** | CONFIRMED | CONFIRMED (narrowed: deliberate, but no alternative) | **CONFIRMED** | Mechanism source-certain (:87/:98 guard + always-on expanders); both workers' density measurements converge (160-165px addable of 462 with 12 stops; left-edge dead from frame one). O's narrowing adopted: the masking is documented and rationale-bearing (:361-373 re-read) — the DEFECT is the absence of any alternative add path + zero feedback in the masked band, not the guard itself. Cure = seat rail frees the meniscus; numeric entry lands with the inspector. → W-GRADSTOP-2. |
| **D-9** | CONFIRMED | CONFIRMED (strengthened) | **CONFIRMED** | All greps re-run by this seat (3/3/1 exactly as both stated; updateStop's cssColor arm dead at its one call site :95). O's strengthening VERIFIED: `GradientPane.vue:8` injects `CSS_COLOR_KEY` and never uses it — select→tune→commit broken at both ends. PR-07 open. → W-GRADSTOP-2 (inspector); the dead injection line dies in MT-GRADSTOP-1 (pure deletion). |
| **D-10** | RESCOPED (unreachable today) | CONFIRMED (unvalidated write path found) | **RESCOPED — latent + one unvalidated write path; end-to-end UNVERIFIABLE-HERE** | Split ruling. F wins the code-editor arm: gradientParse oracle-gates every token (verified :92-94), so the challenger's cited path cannot reach :247 — their T8 set the style directly, bypassing the component. O wins the write-path arm: `setStopsFromColors` (:142-149) writes `pm.savedPalettes.value[0]?.colors.map(c => c.css)` — server-origin strings — into `stops[].cssColor` with NO validity check (source-verified by this seat; F's "library-serialized" provenance was ASSUMED, not verified). End-to-end reproduction needs palette data = the API arm, absent in this cell (API-LESS server) → UNVERIFIABLE-HERE, honestly labeled. The CSS mechanism itself is proven (invalid literal in the `background` shorthand kills the checker ground — O's in-page measurement + spec semantics). Cure carries BOTH arms in MT-GRADSTOP-1: checker ground to its own layer; validate at setStopsFromColors. |
| **D-11** | CONFIRMED (cost trimmed) | CONFIRMED (cost refuted by measurement) | **CONFIRMED — same ruling as L-9** | One row, two seats, folded with both ids and the binding cost correction (Addendum §8). |
| **D-12** | CONFIRMED | CONFIRMED (asymmetry arm added) | **CONFIRMED** | Verified incl. O's asymmetry: the emit fires on exactly one of three selection transitions (:130 yes; :152, :160 no) — a future @select listener silently misses deselection. Pure deletion; FOLD with L-10. |
| **D-13** | CONFIRMED (latent) | CONFIRMED (latent; NO gate of its own) | **CONFIRMED as LATENT** | Four independent declarations of one geometric fact verified (:53, :235, :288, :380-390); currently in agreement (both workers). O's L-3-discipline adopted: a row with no reproduction carries no gate of its own — a gate authored green is presumed vacuous; it rides the axis cure as a structural requirement, with G4 as the tripwire at the next type-scale change. `--touch-target` stays producer-referenced. |
| **D-14** | CONFIRMED (INFO; producer-routed) | GLASS-OWNED | **GLASS-ROUTED (counted glass-owned)** | L-13's twin at INFO; same ruling, same boundary. The damning juxtaposition verified (producer Slider consumed at :232, forty lines from the hand-rolled mechanics; §5 :104 verbatim). Relay + bank; the a11y contract lands locally via D-7. |

**Tally: 21 CONFIRMED · 0 REFUTED whole · 5 RESCOPED (L-2, L-6, L-8, L-14, D-10) · 0 UNVERIFIABLE whole (D-10's end-to-end arm noted inside its row) · 2 GLASS-ROUTED (L-13, D-14).** Two clauses died inside surviving rows with refutations recorded (L-9/D-11 cost; L-14 precision-at-HEAD); one authority corrected (D-5); one severity re-graded (D-1 → MAJOR).

---

## 3 · THE WAVE — MT-GRADSTOP-1

**Title**: The model owns the ordinal AND the pairing, the rail owns one axis, and no tap that grabs may delete — GradientStopEditor's ordering/geometry/gesture core. **Born-RED. Completable alone.**

Shape ruling: worker-F's narrow core wave is ADOPTED over worker-O's 22-row single wave — O's own dissent concedes the L-1 strain and recommends a split; F's core + two named born-RED successors IS that split, taken cleanly. Three of O's contributions are FOLDED IN as amendments: (a) the interval-pairing cure (his G2c — clamp alone does not cure insert-time easing migration), (b) the aria-label-vacuity gate discipline, (c) the dead-injection deletion. O's branded `OrderedStops` + leaf `model/types.ts` remain W-GRADSTOP-3's opening move (the brand SUPERSEDES the clamp there; landing it now would drag the type migration into the core wave).

**Scope (4 demo files + 1 test + 1 e2e amendment):**
1. `demo/workbenches/gradient/composables/useGradientModel.ts` — `updateStop` position writes clamp into `[prev.position, next.position]` (model-owned ordering: a mutation violating non-decreasing becomes unwritable; no re-sort on move → interval↔pair identity stable under drags). **Amendment (O/G2c)**: `addStop` splices its interval at the insertion index (the containing interval splits; downstream pairs keep their authored easings) instead of relying on the tail-growth watch — the watch remains only as removal cleanup. `parseGradientCSS`'s reject branch survives as the foreign-input tripwire only.
2. `demo/workbenches/gradient/GradientVisualizer/GradientStopEditor.vue` — ONE axis: the ramp paints on the handle track (`background-position: HANDLE_HALF px 0; background-size: calc(100% − 2·HANDLE_HALF px) 100%`; pill caps hold flat terminal colour — which is what a 0%/100% stop means) and `getPosition`/`handleLeft` derive from ONE map over ONE box via `clamp`+`scale` from `@mkbabb/value.js/math` (L-12), making :8-13 true; `onHandlePointerMove` gains `e.stopPropagation()` and the bar's draggingId branch (:91-96) is DELETED; the `select` emit (:23, :130) is DELETED (defineModel is the one channel); `colorAt` becomes REQUIRED (fallback deleted); the remove chip moves fully below both coarse hit regions (chip-hit top ≥ scaled handle-hit bottom, DERIVED not hardcoded) and the root reserves the chip's block footprint via padding-block-end. **Amendment (D-10)**: the alpha-checker ground moves to its own background layer/element so an invalid stop literal can no longer clobber its own fallback.
3. `demo/workbenches/gradient/GradientVisualizer/GradientVisualizer.vue` — drops `@select` (:144); demotes the unbound `selectedStopId` defineModel (:51) to a plain ref. **Amendment (D-10)**: `setStopsFromColors` consumers pass validated colours — validation lands at the model boundary (`setStopsFromColors` filters/rejects via the `parseCssColor` oracle, same law as gradientParse).
4. `demo/workbenches/gradient/GradientPane.vue` — **Amendment (D-9/O)**: the dead `cssColorOpaque` injection (:8) is DELETED (one line; W-GRADSTOP-2's inspector re-introduces a USED injection if it wants one).
5. `test/gradient-order-invariant.test.ts` NEW — promoted from `adjudication/worker-F/tmp/repro-order.ts`, asserting BOTH arms: (i) after any position write through the public mutators, `serializeGradient` re-parses ok and `sampleCoalescedStops` is monotonic; (ii) after inserting a stop, every previously-adjacent pair that still exists keeps its authored interval (the easing does not migrate).
6. `e2e/smoke/views/gradient.spec.ts` — ADD (never replace): a neighbour-crossing drag + round-trip-verdict assertion + a coarse-cell tap-at-handle-centre+20px count assertion (closes the exact blind spot at :155-175 that L-1 shipped through).

**Gates (all RED today; commands runnable by a fresh session):**

| # | Gate | Command | RED today — the input |
|---|---|---|---|
| G1 | THE MODEL CANNOT SERIALIZE WHAT ITS PARSER REJECTS + AUTHORED EASINGS DO NOT MIGRATE | `npx vite-node docs/tranches/V/megatranche/adjudication/worker-F/tmp/repro-order.ts` AND `npx vite-node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-order.mts` (end-state: `npx vitest run test/gradient-order-invariant.test.ts`) | stops [0,50,100], first stop → 70: serialized `…70%, …50%, …100%` → `REJECT: stop positions must be non-decreasing`; AND G2c: cubic-bezier authored on pair a→c re-attaches to a→b after a 50% insert (index-keyed intervals) |
| G2 | LIVE ROUND-TRIP — leftmost handle dragged past a neighbour, readout re-fed: verdict null | `node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p4.mjs` (T10; end-state: new e2e `-g "neighbour-crossing drag round-trips"`) | fresh `#/gradient`, add at 50%, drag first handle to ~80% — stops [0,29,8,100] (F's live re-run), painted ramp descends, verdict = the rejection string |
| G3 | ONE AXIS — \|handleCenterX(p) − rampX(p)\| ≤ 1px for p ∈ {0,50,100} | `node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-axis.mjs` (or evidence/p1.mjs) | default 2-stop model, 1440×900: handle-0% centre 235 vs ramp-0% 224 → 11px = 2.38% (3.40% at iPhone-14 W=324) |
| G4 | THE MAPS ARE INVERSE — forward(inverse(p)) = p ± 0.05 at p ∈ {0,100}, read from style.left NEVER aria-label | same gate-axis.mjs (G1b arm) | forward(inverse(0)) = 0.226 → a click at the 0%-handle's own painted pixel mints 0.2% (O's live `calc(0.2% + 9.96px)`); also the D-13 tripwire at any future type-scale change |
| G5 | NO TAP THAT GRABS MAY DELETE — coarse cell: no point inside a handle's advertised hit region removes a stop; destructive/non-destructive hit regions disjoint | `node …/evidence/p4.mjs` (T11) or `…/probes/…/gate-seat.mjs` (G3a/G3b arms), playwright-webkit iPhone-14 | 3 stops, select mid, tap centre+20px → count 3→2, silent (both workers, live); the offending band 14..22px is UNPAINTED |
| G6 | ONE GESTURE, ONE OWNER — one pointermove on a captured handle = exactly ONE position write | `node …/evidence/p3.mjs` (T9) | one bubbling pointermove during a 4-step drag: bar 1 + handle 1 = two emissions/rebuilds per move (the :93 "fallback" comment is the always-path) |
| G7 | THE CHIP RESERVES ITS OWN GROUND — chip's border box intersects no sibling rule; root's layout box contains its footprint | `node …/evidence/p2.mjs` (T2) or gate-seat.mjs G3e arm | select any stop, desktop: chip paints y 244.7→268.7 across the hr at 260.7; elementFromPoint at the rule's y returns `BUTTON.rail-remove-chip` |

**π obligations**: MATRIX — chromium 1440×900 (dev :9000) + playwright-webkit iPhone-14 (the coarse cell); **safari-app is a SEPARATE cell (I-20) — nothing here transfers to it**; the audit `safari-*` shot dirs are webkit captures, cited as webkit only. ROUTE — `#/gradient` (hash URL; fully client-seeded — every gate is data-free, no blank-data-surface trap). SELECTORS — `[data-testid=gradient-stop-bar]`, `button[data-stop-id][aria-label^="Gradient stop at"]`, `.rail-remove-chip[aria-label="Remove selected stop"]`, `[aria-label="Gradient CSS"]`. **STALE-SERVER LAW (new, from §0)**: any literal/serializer claim runs headless vite-node against the tree, or against a freshly restarted dev server — the :9000 instance was PROVEN serving a stale serializer on 2026-07-27 (`scripts/dev/dev.sh` stays untouched — unowned dirty row). COMMITTED CAPTURES (force-added past `.gitignore *.png`): `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/witness/{before,after}-{cross-drag-rail-1440,axis-overlay-1440,touch-delete-iphone14,chip-hr-1440}.png` — a witness not in the wave's own commit does not exist. ENV: the dev server witnesses DOM-behaviour properties only; this wave makes NO bundle, perf, or safari-app claim.

**Delta obligations**: (D1) round-trip: before = the REJECT string on G1/G2's own outputs; after = re-parse ok + verdict null, same commands. (D2) axis: before = 235 vs 224 (11px); after ≤ 1px, same script. (D3) touch delete: before = 3→2; after = 3→3 (tap selects/no-ops), same block. (D4) dispatch: before = 2 writes per move; after = 1, same T9. (D5) chip: before = hrIsUnder chip; after = no intersection. (D6) pairing: before = easing migrated a→c to a→b on insert; after = pair-stable, same gate-order.mts. **EXPLICITLY NOT CLAIMED**: any drag-frame serialization cost delta (Addendum §8 — measured 1× per move already; no close may claim a win it did not measure).

**Structure (L-8 over gates)**: clamp-in-the-sole-mutator makes non-monotonic stops unrepresentable by mutation; splice-at-insert makes easing migration unrepresentable by insertion; deletion makes the drag double-path, the second selection channel, the masking fallback, and the dead injection unrepresentable (the code no longer exists); the checker-on-its-own-layer makes ground-clobbering unrepresentable. Gates persist only where no type can span two languages (CSS paint vs JS map — G3/G4), two hit regions (G5), or the round-trip algebra (G1/G2 as the standing tripwire).

**Completable alone**: YES — if this were the only wave that ever executed, the model could no longer serialize CSS its own parser rejects, an authored easing could no longer silently migrate, a touch user could no longer destroy a stop by grabbing it, the rail would tell one geometric truth its own doc-comment claims, and five dual paths would be deleted. Zero capability lost, no new surface, no dependency on any library fix, barrel deletion, toolchain change, or sibling repo. Four demo files + one test + one e2e amendment = one session at the 38%-landing discipline.

**Carries** (every row exactly one disposition, ids for life):
- **BUILD (this wave)**: L-1 + D-2 (G1/G2, incl. the pairing arm), D-1 + L-3 (G3/G4), D-3's geometric arm (G5), D-4 (G7), L-9 + D-11 (G6, cost correction recorded), D-12 + L-10 (deletion, no gate), L-11 (required prop), L-12 (clamp/scale), D-13 (structural rider on the axis cure; G4 its tripwire; NO gate of its own), D-10's two cure arms (checker layer + setStopsFromColors validation; end-to-end witness stays UNVERIFIABLE-HERE until an API cell exists), D-9's dead-injection line.
- **BUILD → W-GRADSTOP-2** "the §7 recomposition: seat rail + selected-stop inspector + WatercolorDot faces + full keyboard grammar" (D-5 w/ corrected authority, D-6, D-7, D-8, D-9's inspector, D-3's constitutional home): born-RED today by gate-seat.mjs (role null, four no-op keys, no ordinal; bar.x+3 mints nothing; grep WatercolorDot gradient = 0). Its producer arm rides the glass BH relay — no local mechanics growth; the spectrum blur(7px) seam stays glass-owned (cured in 8.0.0), un-re-booked.
- **BUILD → W-GRADSTOP-3** "one sampling law, one literal dialect, leaf types, branded OrderedStops" (L-4, L-5, L-7, L-14's dialect arm; the brand supersedes MT-GRADSTOP-1's clamp): born-RED today — gate-structure.mjs exit 1 (10 failures: no model/types.ts, no model/sample.ts, no ` in <space>` clause, the 7-name dead re-export block, …); minted `oklch(74.32% 0.15204 153.16deg)` beside authored `oklch(0.65 0.18 265)` in one readout (headless, the tree).
- **FOLD by identity**: L-6 → W-HYGIENE demo/ui extirpation (ColorPicker adjudication L-6 identity). L-2 → parser band MT-F001 = π row R1 (pinned, immutable; never re-booked here).
- **RESCOPED-BOOK**: L-8 → **W-TOOLCHAIN-ONE-ARTEFACT** (workspace-link so ONE artefact serves types and runtime; the missing `/css` mapping decision in the same commit; travels beside the parser band — one causal chain), carrying the §0 stale-server proof; its tsconfig-parity sub-arm stays W-HYGIENE config parity (ColorSpaceSelector L-14 identity, cross-referenced not re-booked).
- **GLASS-ROUTED**: L-13 + D-14 → the standing glass-ui BH inbox fond (owner edict 2026-07-12), plus BANKS below; their local a11y obligations land via D-7 in W-GRADSTOP-2.
- **RETIRE**: nothing whole. The two refuted clauses (L-14 precision-at-HEAD; L-9/D-11 serialization cost) die inside their surviving rows, refutations recorded.

**BANKS (runnable, swept at every wave open; a bank nobody runs is a deferral wearing a bank's name):**
1. **π-R1/MT-F001**: `node --input-type=module -e "const {parseCssColor}=await import('./dist/subpaths/css.js'); try{parseCssColor('oklch()');process.exit(0)}catch{process.exit(1)}"` — exits 1 today (this seat's run: THREW). When it exits 0: re-witness the code-editor arm (`linear-gradient(90deg, oklch(), blue)` through applyCSS must yield a verdict, not a throw) and gradientParse.ts:16-17's oracle claim becomes true as written.
2. **glass rail-variant (precise)**: `node -e "const s=require('fs').readFileSync(require.resolve('@mkbabb/glass-ui/package.json').replace('package.json','dist/slider.d.ts'),'utf8'); process.exit(/variant.*rail|SliderThumb.*slot/.test(s)?0:1)"` — exit 0 fires L-13/D-14's consume.
3. **glass-8 (coarse sweep)**: `node -e "process.exit(require('./node_modules/@mkbabb/glass-ui/package.json').version.startsWith('8')?1:0)"` — exits 1 when glass 8 lands, re-firing the producer-arm relay check.

---

## 4 · ADDENDUM — GRADSTOP-A (E-3: appended to the standing gradient authorities, never rewriting them)

1. **NEIGHBOUR-CROSSING CLAUSE** — no gradient close may cite a drag test as ordering coverage unless it includes a drag that attempts to cross a neighbour; `gradient.spec.ts` today drags only the MIDDLE handle strictly between neighbours — the exact blind spot L-1/D-2 shipped through. A completion check must also contain a serialize→re-parse round-trip after an independent position write; a monotonic-render check alone passes with L-1 fully present.
2. **ONE-AXIS CLAUSE** — the :8-13 "share one axis by construction" comment may never be cited as evidence in EITHER direction: the direction-normalization reading is available (worker-O's narrowing), the geometric reading is false by measurement (11px), and the claim is measured (handle-centre vs border-box ramp, ≤1px, p ∈ {0,50,100}) or not made.
3. **π-R1 BOUNDARY** — L-2's parser root is MT-F001 = π row R1, pinned and immutable (epoch rule); no gradient wave re-books, renames, or locally patches it (no try/catch in isColorToken — totality is the library's contract); the runnable bank is the sole coupling.
4. **PRODUCER BOUNDARY** — the multi-thumb rail species (L-13/D-14) lands as a glass-ui variant via the standing BH relay, never as further hand-rolled mechanics; WatercolorDot (D-6) is a CONSUME of the published `@mkbabb/glass-ui/watercolor-dot` and BUILDS here — producer-change vs producer-consume is the whole distinction; the spectrum blur(7px) seam stays glass-owned and un-re-booked; the masking-fallback ban forbids any local shim.
5. **LITERAL-DIALECT CLAUSE** — any wave touching mint/serialize asserts ONE colour-literal grammar across authored and minted stops; the "15-significant-digit" characterization is STALE AT THE TREE (headless: `oklch(74.32% 0.15204 153.16deg)`) and may not be cited against HEAD.
6. **READOUT-VACUITY CLAUSE** (worker-O's G1b lesson, binding) — the handle aria-label rounds to whole percent (:234); any assertion of sub-percent precision against the accessible name is vacuous by construction; position-precision gates read `style.left`.
7. **STALE-SERVER CLAUSE** (new, this arbitration) — the long-running dev server at :9000 was PROVEN (2026-07-27, arbiter's paired headless+live runs, scripts committed at `adjudication/arbiter-F/tmp/`) to serve a serializer diverging from BOTH on-disk builds. No literal, precision, or serializer claim may be witnessed against a dev server of unknown boot vintage; the authoritative cell is headless vite-node against the tree. This fact travels with L-8 to W-TOOLCHAIN-ONE-ARTEFACT as its sharpest exhibit: three artefacts, one app.
8. **COST-CLAIM CORRECTION (binding on the close)** — no close may cite "two 32-sample ramp re-serialisations per drag frame" as a cured cost: worker-O measured 8 distinct `--rail-ramp` DOM writes across an 8-move real drag (Vue batches the two model writes per event into one flush). The double dispatch is real and costs 2× emit + 2× eager `stops.map` rebuild per event; the serializer runs once per move. A wave claiming a perf win it did not measure is the vacuous-close class.

---

## 5 · DISSENT — preserved disagreements, verbatim where unresolved

1. **D-1 severity** — worker-F: "I contest the BLOCKER grade: the 11px/2.38% axis skew and the false doc-comment are fully confirmed, but nothing is destroyed or lost; the true blockers are L-1/D-2 …, D-3 … and L-2 …. I would rank D-1 MAJOR; the row stands CONFIRMED with both rankings carried." Arbiter RULED MAJOR; the challenger's BLOCKER grade is preserved here as the losing position.
2. **One wave vs. the split** — worker-O: "22 confirmed rows across a 392-line component plus a 4-module model tree is large for the 38%-landing rate L-1 was written against. If the arbiter wants strict L-1 compliance the honest seam is TWO waves … I would ship the split." Arbiter agreed in substance and shipped worker-F's core-plus-successors shape, which is the split; O's single-wave spec survives in the record as the alternative he himself argued against.
3. **L-13/D-14 category** — worker-F: "the defect itself (hand-rolled species in the consumer) is value.js-owned, NOT glass-owned." Worker-O: "a wave whose gate depends on a sibling repo shipping a variant is not individually completable under L-1 … Disposition: RELAY." Arbiter ruled the CATEGORY follows the cure's owner (GLASS-ROUTED) while recording that the defect's substance sits in our tree — F's ownership argument is preserved as live; if a future owner ruling re-homes the category, the disposition (relay + bank + local a11y-now) does not change either way.
4. **D-10** — worker-F: "I refute the today-reachability: every model input path is oracle-gated or throws upstream … Their T8 … bypassed the component. Latent only." Worker-O: "setStopsFromColors … writes server-supplied palette strings straight into stops[].cssColor with NO validity check whatsoever … UNVERIFIABLE-HERE end-to-end because the dev server is API-LESS." Arbiter's split ruling (§2) satisfied neither side fully: F's rescope label stands, O's write-path fact is adopted as the row's surviving substance. The end-to-end witness is an open obligation on the first API-backed evidence cell.
5. **D-5 clause flag (relayed)** — worker-O: "the same mis-citation ['never color-only' cited where the explicit-role/state sentence is the violated one] may ride other components' D-rows." This arbitration confirms the flag from the constitution's verbatim text and relays it to sibling adjudications.
6. **Worker-O's recorded probe failures** (kept as method law, none cited in any verdict): the aria-label gate that passed vacuously on a 0.2% error; the garbage-RGB getComputedStyle probe, discarded; the synthetic-pointerId capture rejection, re-run with real mouse events.
7. **Independence note** — worker-F: confirmations rest on independent instruments, with reliance on challenger measurements (D-8's addable figure, p5's iPhone geometry) verified for arithmetic consistency and declared. This arbitration held both workers to that standard and both survived it; the sole material assumption caught was F's D-10 provenance claim, ruled against him in §2.

---

*Written by arbiter-F (Fable 5) · 2026-07-27 · evidence scripts at `docs/tranches/V/megatranche/adjudication/arbiter-F/tmp/` (l14-decider.ts, l14-decider2.ts, l14-live.mjs) · all live cells chromium/playwright-webkit; no safari-app claim anywhere in this record (I-20).*

---

# r3 APOTHEOSIS (2026-07-28) — the fresh corpus tri-folded; arbiter-F's adjudication of worker-F (Fable) vs worker-O (Opus); the wave RE-AUTHORED

**MODEL RECEIPT**: arbiter-F ran on **Fable 5** (`claude-fable-5`), observed directly by this seat from its own system prompt, not inherited from the spawn label; session 2026-07-28, HEAD `7775473b`, darwin arm64. Worker receipts: worker-F = Fable 5 (`claude-fable-5`, self-observed); worker-O = Opus 5 (`claude-opus-5[1m]`, 1M-context seat, self-observed). M-12: the F seats are Fable by design.

**PROVENANCE NOTE**: worker-F wrote its delta draft directly into this registry file (the uncommitted +108 lines, "DELTA r3"). That draft is a WORKER return, not an adjudication; this section SUPERSEDES it in place. Its content is preserved in the tri-fold record and, where it survived arbitration, is folded below with attribution. E-3 honoured: r1 §0–§5 above are untouched and still binding.

## r3.1 · Method — what this seat re-derived itself (nothing inherited)

Attempted refutation of BOTH workers before adopting either. Instruments run by this seat TODAY:

1. **Full source re-reads at the line**: `GradientStopEditor.vue` (393 lines, whole), `useGradientModel.ts` (whole), `gradientParse.ts` (docblock, `isColorToken` :92-94, the ordering loop :283-291 — **`<`, never `<=`: equality is deliberately legal**, the fact that decides both the separation-law ban and the sort-cure premise), `GradientVisualizer.vue` (:51 unbound outer defineModel; :140 `v-model:selected-id` + :144 `@select` both consumed; :77 interval throw; :223 tile `w-20 sm:w-24`; `.gradient-render-tile` :271-279), `GradientPane.vue` (:8 dead `cssColorOpaque` inject — identifier occurs once; bare `<GradientVisualizer>` :26 — the outer model truly unbound), `GradientEasingEditor.vue` (:154 inline layer idiom; `.specimen-dot` :239-247 — a DIFFERENT recipe: `cover, 6px 6px`), `demo/picker/controls/ComponentSliders/ComponentSliders.vue` :185-205 (the ramp travels the producer's own `--slider-track-bg` seam), `demo/color-picker/App.vue` :44-52 (nav closes BEFORE `<main>`; ErrorBoundary INSIDE it), `demo/styles/foundation.css` :686-696 (**`.rail-handle` IS enrolled in the U-F57 `forced-color-adjust: none` roster** — the fact worker-F missed), `useGradientCSS.ts` :189/:240 + `GradientVisualizer.vue` :77 (all three "Gradient interval N is missing" throws verified at the cited lines).
2. **`npx vite-node …/evidence/parse-probe.ts`** (probe signature read first): 8/8 empty-argument colour functions THROW `Cannot read properties of undefined (reading 'replace')`; the drag-produced descending string REJECTS `stop positions must be non-decreasing (hard-stop reordering isn't modeled)`; `parseGradientCSS('linear-gradient(90deg, oklch(), blue)')` THROWS; `red, blue` parses ok. Bank 1 also re-run raw against `dist/subpaths/css.js`: **exit 1**.
3. **`npx tsc --noEmit -p tsconfig.demo.json --traceResolution`** (third independent run): `@mkbabb/value.js/css` → `/Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts`, Package ID `@mkbabb/value.js/dist/subpaths/css.d.ts@4.0.0` — repo dist, **no node_modules segment**. R1-C1 arbiter-confirmed.
4. **Toolchain/test-truth by own instrument**: exports = 7 keys no `.`; the paths block's 3 phantom targets (`dist/index.d.ts`, no `parsing`/`units` in `dist/subpaths/` — ls); `/css`+`/value` absent-and-working; installed copy pinned by `@mkbabb/keyframes.js` (`"@mkbabb/value.js": "4.0.0"`); `grep -rln playwright .github/workflows/` → **nothing**; `gradient-ruler-cap`/`gradient-rung` live ONLY in `o21-gradient-rail.spec.ts:135/:163`; `Perceived-space` only in `gradient.spec.ts:57/:112`; `useGradientModel` in test/ = ONE file, TYPE-only import (`test/gradient-v4-consume.test.ts:10`) — the mutation-vacuity proof holds by construction.
5. **`adjudication/arbiter-F/tmp/r3-forcedcolors-decider.mjs`** (NEW, committed with this record) — the D2-17 fork decided by measurement, both engines, `:9000/#/gradient`: chromium `{forcedActiveMatches:true, borderTopColor:"oklab(0.999994 …/0.8)", forcedColorAdjust:"none"}`; webkit `{forcedActiveMatches:true, borderTopColor:"oklab(1 0 0 / 0.8)"}`.
6. **Instrument existence**: every probe cited by either wave spec exists on disk (`evidence/WBGSE-O-r3-{gestures,contrast-crowding,crossdrag-forcedcolors}.mjs`, `WBGSE-D-probe{2,4,5,7}.mjs`, `p1–p5.mjs`, `parse-probe.ts`, `probes/…/gate-{axis,seat,structure,gesture}.mjs`, `gate-order.mts`) — a cited instrument that does not exist voids its gate; none did.
7. **Ripple bound for the cure ruling**: `grep -rln intervals demo/` outside `demo/workbenches/gradient/` → **zero** — the stop-owned-easing migration cannot escape the gradient tree.

Suspicious-agreement spot-checks (unanimous rows re-proved at source, beyond the required two): C1's three-policy triple (:127-131 bare map / :118 sort / parse reject), C14's mechanism (no `e.button` anywhere; `@pointercancel="onBarPointerUp"` :208 routes cancel through the minting path :101-110), C12's `e.preventDefault()` :120, C8's dual channel + no-emit asymmetry (:129-130 write+emit; :152/:160 write-only), C10's four arms (:318/:388 fallbacks, dead inject, `= undefined` :6, `:76 return 0`), C4's three arms, Lr2-8's drift, R1-C1, the parse throws. Environment law honoured: dev server API-LESS; no safari-app claim anywhere in this delta (I-20); live probes limited to the two decisive forks (probe parsimony).

## r3.2 · Rulings on the forks — where the workers disagreed, the tree decided

| fork | worker-F | worker-O | **RULING** | deciding evidence |
|---|---|---|---|---|
| **D2-17** | UNVERIFIABLE-HERE ("WebKit ignores the emulation") | REFUTED both arms | **REFUTED — O wins** | This seat's own decider (r3.1 item 5): BOTH engines report `(forced-colors: active)` matching — F's harness claim was itself unmeasured; and `.rail-handle` is in the U-F57 `forced-color-adjust: none` roster (foundation.css, source-verified), with chromium — the engine that genuinely forces — computing the border at the AUTHORED white/80. The premise "border-color is forced" is false. The arm was verifiable here with one 20-line probe; F's UNVERIFIABLE loses on L-2. Caveat recorded: webkit computes `forced-color-adjust` empty — webkit may emulate the query without forcing paint, so chromium is the authoritative forced-colors PAINT cell; either way the row dies. Surviving residue (no ARIA selection state) is already booked at r1 D-5/D-7. |
| **Ordering cure: clamp vs sort; stop-owned easing now vs W3** | clamp interim in core; splice-at-insert; stop-owned easing booked W3 | normalise-on-write (stable sort) + stop-owned easing together in core | **O wins — sort + stop-owned easing land TOGETHER in the core wave; clamp and splice are STRUCK as interim policies** | The r1 clamp ruling's own premise ("index-keyed intervals make re-sorting unsafe") dissolves the moment the easing hangs on its opening stop — O's derivation is r1's logic carried forward, not re-litigation. Clamp converts a data defect into a silent capability refusal (drag-across-a-neighbour stops dead unexplained — the same species as D-8's starvation); splice-then-supersede does the migration twice, which the house no-backwards-compat law forbids (no interim shims; migrate the consumer at the root). Equality-legal verified at source (`<` not `<=`) — hard stops survive a stable sort by construction. Ripple bound by this seat: `intervals` has ZERO consumers outside the gradient tree (r3.1 item 7); all three "interval N is missing" throws die with the array. Gates stay policy-neutral (they assert the invariant). F's position preserved verbatim in Dissent §r3-1, including its honest size worry. |
| **R2-A3 count** | CONFIRMED, "four files/six declarations" | RESCOPED, two sites | **O wins — TWO sites** | This seat read all four: `.gradient-rail` (:317-326) and `.gradient-render-tile` (:271-279) share the full material contract (border-box origin/clip, no-repeat/repeat, `100% 100%, 16px 16px`, `--shadow-sm`) — genuine copy-paste. GradientEasingEditor :154 is a bare two-layer inline shorthand (no origin/clip/repeat/size — the very idiom the contract's own comment deprecates, a different class); `.specimen-dot` :244-247 is a different recipe (`cover, 6px 6px`); ComponentSliders feeds the producer's `--slider-track-bg` seam (correct, producer-mediated). F conflated the ramp-over-checker LAYER IDIOM with the CONTRACT. Two-site dedup rider → W-GRADSTOP-3, INFO. |
| **D2-11 booking** | CONFIRMED → W-GRADSTOP-2 | RESCOPED → PR-09 (design band) | **O wins — RESCOPED to PR-09** | PR-09 ("Gradient/Easing protagonist subordinated", PROPORTION-AUDIT) is the standing identity; the 40px height is set by the pane composition and the 50/50 split by the shell — not this component's to build. Re-booking under a D2-number is the rename disease (L-5). Cross-cited: W-GRADSTOP-2's stage/seat recomposition must not silently pre-empt the design band; the core cure stays geometry-neutral. F's causal insight kept on the record (the 40px stage is WHY the phantom targets/exiled chip/invisible bands exist). |
| **D2-04 disposition + count** | BLOCKER; interim dual-contrast ring in core, gate born-RED | whole row → W-GRADSTOP-2; counts corrected (10/10 own-ramp; 1.00 white; "6/10" ramp-scoped) | **SPLIT — F's placement, O's measurements** | Severity BLOCKER for the class (a data-dependent TOTAL operability loss — both workers' white-ramp 1.00:1 agree; handles literally invisible). The interim cure is the file's OWN dual-contrast recipe (:342-349) generalized to resting/selected — a contrast floor in the exact style block the core wave already re-authors; leaving invisible controls until a later recomposition is a deferral. Constitutional home (seat rail + WatercolorDot faces) stays W-GRADSTOP-2. O's count-scoping BINDS (Addendum §17): 10/10 below 3:1 at 1.87–2.43 on the constant-lightness ten-hue ramp; exactly 1.00 on white; "6/10" may only be cited against the challenger's own ramp. |
| **C9 disposition** | core wave | W-GRADSTOP-2 (visual/motion contract) | **F wins — core wave** | The cure is mechanical and visually IDENTICAL (same scale ladder, moved to `:hover`/`[data-selected]` rules; inline style reduced to per-stop DATA `--stop-pos`/`--stop-color`): it lives in the exact inline-style block the axis cure rewrites anyway, and deletes `hoveredId`, `handleScale`, two listeners per handle. No recomposition semantics touched. |
| **C11 severity** | MAJOR | BLOCKER (challenger's grade carried) | **MAJOR** | Precision-destructive, data-recoverable (drag back); the BLOCKERs are the ordering family, the parser family, D-3's silent delete, D2-04's invisibility class. Challenger's BLOCKER preserved in Dissent §r3-2. |
| **C4's CI-wiring home** | W-TOOLCHAIN | W-HYGIENE | **W-TOOLCHAIN** | CI wiring is toolchain substance, and after R1-C1 guts the two-builds arm, W-TOOLCHAIN's remaining substance = the §0 stale-vintage proof + this. Either home was defensible; one is chosen so the row has exactly one. |
| **R2-A1 home** | core amendment | fold with D2-10 → W-GRADSTOP-2 | **SPLIT** | The model exposes `canRemove` in the core wave (the model file is already open; one home for the rule); the EXPRESSION of the floor as an explicit disabled state with its reason lands at W-GRADSTOP-2 with D2-10's one-removal-owner inspector. |

**Taxonomy ruling** (the systemic F/O divergence): identity governs BOOKING (L-5 — one identity, one row, ids for life), truth governs the VERDICT. A fresh row that re-proves a standing identity of THIS component is **CONFIRMED (fold)** — it is evidence, not a new defect. **RESCOPED** is reserved for rows whose owner lies outside this component's waves (parser band, design band, demo-wide hygiene) or whose claim was materially corrected (R2-A3's count, D2-15's cure). The tallies below use this rule.

## r3.3 · Per-row verdicts (fresh corpus: 42 accusation ids → 36 unified rows)

| unified row (accusation ids) | ≡ identity | **VERDICT** | deciding evidence (arbiter's own unless noted) |
|---|---|---|---|
| 1. C1 + D2-03 + Lr2-1 | r1 **L-1/D-2** | **CONFIRMED (BLOCKER, fold)** | Triple-policy verified at source (:127-131 / :118 / parse `<`); this seat's parse-probe run re-throws the REJECT on the drag's own output TODAY. Both workers' live drags agree to the digit (84/50/100; descending computed background). Cure re-ruled — r3.2 fork 2. |
| 2. C2 | r1 D-2 pairing arm | **CONFIRMED (BLOCKER, fold)** | Index-keyed `intervals` + tail-only watcher (:89-100) vs sorted insert (:118), source-certain. Stop-owned easing PROMOTED to the core wave (r3.2 fork 2) — it deletes the parallel array, the watcher, `updateInterval`'s index API and all three "interval N is missing" throws (sites verified). |
| 3. C3 | r1 **L-9/D-11** | **CONFIRMED (fold)** | Bar's draggingId branch :91-96 fires on every bubbling move (no stopPropagation :136-146; the :93 "fallback" comment false). Addendum §8 cost discipline binds: O's 0.223ms figure is per-invalidation, never per-frame. |
| 4. C4 | — new | **CONFIRMED (MAJOR)** | All three arms this seat's own instruments (r3.1 item 4): Playwright in NO workflow; ≥4 assertions target retired DOM; vitest coverage of the model = one TYPE import. Worker-O's own suite run (5 failed/8 passed, 3.6m) adopted for the count, with his scoping: `views:40` fails on the API-less cell's environment console error, `views:270` on a stale producer literal — cited as environment/stale, never as component defects. `HANDLE_HALF=0` vacuity mutation = GRADSTOP-A §13's canonical witness. |
| 5. C5 + Lr2-2 + D2-01(root) | r1 **L-2** = MT-F001 = π-R1 | **RESCOPED → parser band** | Re-thrown by this seat (8/8 + in-gradient). Cure = cand-O totality (parser band's ruled winner, 0/172); ALL THREE fresh seats + both workers refuse the local try/catch — masking-fallback ban, five-way convergence. MT-F024 recorded as alias; MT-F001 primary (anti-rename). Bank 1 the sole coupling. |
| 6. D2-01 (consequence arm) | — new arm | **CONFIRMED (BLOCKER); dock clause struck** | Both workers reproduced (paneCount 2→0, errs []); shell read by this seat: nav+Dock close BEFORE `<main>`, ErrorBoundary inside it — the throw kills BOTH panes (the My Palettes companion included) and the dock survives. "Destroys the whole application" may not be cited (§10); the blast-radius (one boundary, two independent panes) cross-cites to the App-shell identity, not re-booked. Telemetry law §10: the debounce-scheduled throw lands in `onErrorCaptured` — pageErr/consoleErr counts are structurally blind. |
| 7. C6 + D2-06 | r1 **D-7** (+D-8 arm) | **CONFIRMED (fold)** | Keyboard grammar a third of §5.2 (:173-187 verified); role/aria-value*/state null; O's new arm adopted: NO keyboard path to CREATE a stop exists at all. → W-GRADSTOP-2. |
| 8. C7 + D2-02 | r1 **D-1** | **CONFIRMED (fold; MAJOR stands)** | O's DOM measurement supersedes the PNG table: **±11.0px** at the terminals (centres 235/675 vs ramp 224/686), 0 at p=50 — the citable figure. Severity MAJOR per the r1 ruling; the fresh BLOCKER grades join Dissent §r3-3. |
| 9. C13 | r1 **L-3** | **CONFIRMED (fold)** | Two denominators, one axis: forward over the border box (:75-81) vs `handleLeft` over the padding box (:55-57) on a 1px-bordered rail — 304 vs 302 mobile, 462-frame desktop verified by O's DOM read. Dies with the one-map axis cure. |
| 10. C8 + Lr2-5 | r1 **L-10/D-12** | **CONFIRMED (fold; mechanism corrected)** | Dual channel at source (:129-130 write+emit; consumer :140+:144 both). Pass-2's correction ADOPTED: the parent binds `v-model:selected-id`, so `useModel` takes the parent path — same-tick read-after-write IS stale; today survives by ordering luck (`wasSelected` :123 before :129). O's asymmetry verified: :152/:160 write without emitting — an `@select` listener already misses deselection. Cure = deletion (one channel). |
| 11. C9 + D2-16(style arm) | — new | **CONFIRMED (MINOR)** | Self-documented trap (:30-33, :255-261) re-entered for `transition` :263. Cure in core (r3.2 fork 6): data-only inline; ladder to the sheet; `hoveredId`/`handleScale`/two listeners deleted. |
| 12. C10 | — new | **CONFIRMED (INFO)** | Four arms at source: unreachable `var()` fallbacks :318/:388; dead inject (GradientPane :8, occurrence 1); no-op `= undefined` :6 (sole call site always passes); `:76 return 0` — a null guard answering an unanswerable question with a DESTRUCTIVE value. All die in core (the :76 case becomes impossible: the rect is cached at pointerdown; the residual path throws). |
| 13. C11 | — new | **CONFIRMED (MAJOR)** | Independently reproduced by BOTH workers (10.11/10.12px for 1px of input; labels 0%→2%); mechanism source-certain (no grabDx; :145 emits absolute; the 4px dead-zone :138-144 wired only to deselect). Cure: grabDx + dead-zone-gated first emit + cached rect (also halves C3's forced layout). Severity — r3.2 fork 7. |
| 14. C12 | — new | **CONFIRMED (MAJOR)** | `e.preventDefault()` :120 suppresses focus-on-mousedown, verified at source; both workers measured activeElement=BODY + dead arrows after a real click. O's honest scoping adopted: Tab still reaches the grammar — the defect is "mouse select kills the keyboard until Tab-away". Cure = DELETION (`select-none touch-none` :203 + capture :133 already do the work). |
| 15. C14 | — upgraded | **CONFIRMED (MAJOR)** | Both workers reproduced deterministically (F: right-click 2→3; O: middle 2→3 AND right 3→4 — the middle-button arm needs no native-menu caveat and carries the gate alone). Mechanism at source: no `e.button` predicate anywhere; `pendingAdd` a bare latch. Worker-F's sharpening ADOPTED, source-verified by this seat: `@pointercancel="onBarPointerUp"` (:208) routes CANCELLATION through the minting path (:101-110) — a cancelled dead-zone touch also mints. Cure: `e.button !== 0` return at both pointerdowns + a dedicated cancel handler that ONLY disarms + the contextmenu removal species deleted. |
| 16. R1-C1 | corrects r1 L-8(b) | **CONFIRMED (correction accepted)** | THIRD independent traceResolution run, this seat's own (r3.1 item 3): repo dist, no node_modules; Package ID is identity, not provenance; the installed copy is keyframes.js's pin, consulted by nothing. L-8(b) WITHDRAWN (§9); cure = DELETE the tsconfig value.js paths block (this seat verified `/css`/`/value` resolve with no entry — the block is unnecessary, not incomplete); NO workspace link. r1 §0's stale-server proof untouched. |
| 17. Lr2-3 | r1 **L-13/D-14** | **GLASS-OWNED** | Unchanged from r1: substance in our tree, cure (`variant="rail"`/stops + thumb slots) is a producer change; glass-ui is not ours, no local patch (masking-fallback ban), a wave gated on a sibling repo is not completable. Relay to the standing glass BH inbox fond + banks 2/3; evidence cells per INBOX I-20 (webkit ≠ safari-app; no safari-app claim). A11y lands NOW via D-7/W-GRADSTOP-2. |
| 18. Lr2-4 | r1 **L-5** | **CONFIRMED (fold)** | Dead re-export doors re-verified (the 7-name loop; the two constants travel 3 hops vs MixConfigBar's 1). → W-GRADSTOP-3 after L-7's leaf. |
| 19. Lr2-6 | r1 **L-6** | **RESCOPED → W-HYGIENE demo/ui extirpation** | The ColorPicker adjudication's L-6 identity (90-site); GradientStopEditor itself imports nothing from demo/ui. One identity, one booking; never re-booked. |
| 20. Lr2-7 | r1 **L-7** | **CONFIRMED (fold)** | Both type-only cycle edges at the same import sites; leaf `model/types.ts` stays W-GRADSTOP-3's opening move — and now also homes the stop-owned `GradientStop` brand. |
| 21. Lr2-8 | r1 L-8(a) | **RESCOPED → W-HYGIENE config parity** | This seat's own measurement (r3.1 item 4): 3 dead mappings incl. the root; the "CLOSED 8-key set" comment false (7 keys); `/css`/`/value` absent-and-working = the block is unnecessary. Cure corrected by R1-C1: deletion. ColorSpaceSelector L-14 identity, cross-referenced. |
| 22. R2-A1 | — new | **CONFIRMED (MINOR)** | Two homes for one rule (:66 vs :122-123), source-verified. Split home per r3.2 fork 9: `canRemove` in the model (core); the explicit disabled state with reason → W-GRADSTOP-2 with D2-10. |
| 23. R2-A3 | — new | **RESCOPED (count corrected: TWO sites)** | r3.2 fork 3 — this seat's four-site read. Two-site dedup rider → W-GRADSTOP-3 (demo-owned, now); the glass-variant arm rides the L-13 relay. INFO. |
| 24. D2-04 | r1 **D-5** + measured arm | **CONFIRMED (BLOCKER for the class)** | r3.2 fork 5. White ramp ring:fill **1.00:1** (handles invisible, committed shot); O's in-page recomputation: 10/10 hues below 3:1 (1.87–2.43) on the constant-lightness ramp. Interim dual-contrast resting/selected ring in core (gate G11, born-RED); constitutional home W-GRADSTOP-2. Count-scoping §17 binds. |
| 25. D2-05 | r1 **D-3** | **CONFIRMED (fold; BLOCKER, coarse cell)** | O re-measured the coarse pseudo at exactly 44×44 and the chip's `top-11 z-20` DOM-order win; the 8px destructive band arithmetic (198.7..242.7 ∩ 234.7..278.7) makes G9's threshold derivable. playwright-webkit iPhone-14, never safari-app (I-20). |
| 26. D2-07 | r1 **D-8** (+D-7 naming) | **CONFIRMED (fold)** | Bar census `{role:null, tabindex:null, label:null, DIV, cursor:copy}`; hover-ghost-only affordance imperceivable on touch; each stop sterilises 44px (13.6% of the 324px mobile rail) with zero feedback; the :35-41 "self-evident" comment measured false for half the modalities. → W-GRADSTOP-2. |
| 27. D2-08 | r1 **D-9** | **CONFIRMED (fold)** | `selectedStopId` = 3 lines, all writes, no reader; select→tune→commit has no tune at either end (the dead inject is the far end). Inspector → W-GRADSTOP-2; the dead inject dies in core. |
| 28. D2-09 | r1 **D-4** | **CONFIRMED (fold)** | Chip `absolute w-6 h-6 top-11 z-20` on a root whose in-flow content is the 40px rail — a 28px borrow crossing the sibling hr (measured 244.7→268.7 vs 260.7). G10. |
| 29. D2-10 | — new | **CONFIRMED (MAJOR)** | Three destructive species at source (:164-167 / :180-182 / :284-300), zero confirm/undo on the route, the floor expressed as absence (v-if) + silence (early return) — PR-06 + the explicit-disabled-state law. One removal owner + explicit state → W-GRADSTOP-2; the contextmenu species dies in core with C14. |
| 30. D2-11 | **PR-09** (design band) | **RESCOPED → PR-09** | r3.2 fork 4. Facts verified (rail h-10; tile w-20 sm:w-24); booking outside this component's waves; cross-cited both directions. |
| 31. D2-12 | r1 **D-13** — UPGRADED | **CONFIRMED (reproduction supplied; gate real)** | The delta's cleanest win: at rootFS 20px (ordinary WCAG 1.4.4) the rem-derived `w-5` grows to 25px while the JS literal `HANDLE_HALF=10` stays — the 0% handle lands **+1.50px OUTSIDE** the rail, the exact condition the :49-53 comment claims fixed. Both workers reproduced. r1's no-gate discipline retires exactly because this gate is authored RED (§11). Rides the one-custom-property axis cure; G12. |
| 32. D2-13 | — new | **CONFIRMED (MINOR)** | O's heading census correction adopted: a "Gradient" h3 EXISTS (the pane title) but names nothing at instrument rank — the protagonist is unnamed AND the outline is flat (pane title at the same h3 rung as its subsections). → W-GRADSTOP-2 with D-7's group/name work. |
| 33. D2-14 | PR-07, inside r1 **D-9** | **CONFIRMED (fold)** | Position exists only in the whole-percent aria-label (:234) + the CSS string; no numeric entry in the tree. Already flagged open inside D-9 (PR-07) — folds, never re-books. One inspector discharges D2-08/D2-10/D2-14/D2-06's numeric arm — the argument for building it once. |
| 34. D2-15 | — | **RESCOPED (cure REFUTED; measurement + disambiguation arm survive)** | Crowding arithmetic re-derived (33.6–33.8px centre gaps < 44px coarse targets at 10 stops mobile). The proposed minimum-separation law is REFUTED as a capability deletion: CSS hard stops REQUIRE coincident positions and the parser deliberately admits equality (`<` — this seat, at source). §15 bans it. Disambiguation arm → W-GRADSTOP-2. |
| 35. D2-16 | r1 **D-6** | **CONFIRMED (fold)** | 64 WatercolorDot uses across 19 demo files, 0 in gradient; the canon names the site; watercolor-dot is a PUBLISHED subpath — a consume, buildable. → W-GRADSTOP-2; the inline-style arm folds into C9's core cure. |
| 36. D2-17 | — | **REFUTED (both arms, by measurement)** | r3.2 fork 1 — this seat's own decider + the foundation.css roster. Recorded with evidence so it cannot re-enter under another number. Residue (no ARIA selection state) already booked at D-5/D-7. |

**r3 tally (36 unified rows): 28 CONFIRMED · 1 REFUTED (D2-17) · 6 RESCOPED (C5-family → parser band; Lr2-6, Lr2-8 → W-HYGIENE; R2-A3 count-corrected; D2-11 → PR-09; D2-15 cure-refuted) · 0 UNVERIFIABLE-HERE · 1 GLASS-OWNED (Lr2-3).** Clauses refuted inside surviving rows: D2-01's dock arm; "6/10" as a general contrast count; the four-site paint-stack count; the per-frame serialization cost (standing §8). One pinned r1 clause WITHDRAWN by correction: L-8(b). No fresh row overturns any r1 CONFIRMED verdict; r1's D-13 no-gate and L-8 cure are amended by measurement; r1's clamp cure is superseded on its own premise (r3.2 fork 2).

## r3.4 · THE WAVE — MT-GRADSTOP-1 (r3-unified, arbiter's re-authoring; same identity as r1 §3, extended and re-ruled, never renamed)

**Title**: One collection owns the ordinal AND the easing, one box owns the axis, one gesture owns the grab, no button but the primary may mint, and cancel only disarms — GradientStopEditor's model + gesture + geometry core. **Born-RED. Completable alone.**

**Cure law (supersedes r1 scope 1, per r3.2 fork 2)**: stop-owned easing + normalise-on-write land TOGETHER; neither clamp nor splice-at-insert is authored (Addendum §14: no gradient close may land one without the other).

**Scope — 7 demo files + 1 new test + 1 e2e amendment, all in one tree, zero external ripple (arbiter-verified):**
1. `useGradientModel.ts` — `GradientStop` gains `easing` (the interval hangs on the stop that OPENS it; the last stop's is ignored); the `intervals` parallel array, its length watcher (:89-100) and the index-keyed `updateInterval` are DELETED; ONE normalising mutator `setStopPosition(id, position)` (map → stable sort; equal positions legal, so hard stops survive); `addStop`/`updateStop`'s private policies deleted; `canRemove` exposed (R2-A1); `setStopsFromColors` validates via the `parseCssColor` oracle (D-10's arm — end-to-end witness stays open until an API-backed cell exists); `applyCSS` follows the shape.
2. `GradientStopEditor.vue` — ONE axis (ramp painted on the handle track: `background-position`/`background-size` inset by the half-handle, read from ONE custom property both languages consume; one map over one box via `clamp`+`scale` from `@mkbabb/value.js/math`); `grabDx` recorded at pointerdown against a rect CACHED at pointerdown; first emit gated on the existing 4px dead-zone (C11); `e.preventDefault()` deleted from :120 (C12); `if (e.button !== 0) return` atop BOTH pointerdown handlers; a dedicated `pointercancel` handler that ONLY disarms; the `@contextmenu` removal species deleted (C14/D2-10); `stopPropagation` on handle move + the bar's draggingId branch (:91-96) and its false comment DELETED (C3); the `select` emit (:23/:130) DELETED (C8); `colorAt` REQUIRED; inline style reduced to per-stop DATA (`--stop-pos`, `--stop-color`) with the scale ladder/transition in the scoped sheet under `:hover`/`[data-selected]`, `hoveredId`/`handleScale`/both listeners deleted (C9); the interim dual-contrast resting/selected ring generalized from the file's own :342-349 recipe (D2-04); the `var()` fallbacks (:318/:388) deleted and `:76` made impossible-by-construction (C10); the chip's ground reserved by the root and its coarse hit region derived disjoint from the handle's (D-3/D-4 interim).
3. `GradientVisualizer.vue` — `@select` dropped; the unbound outer `selectedStopId` defineModel demoted to a plain ref; follows stop-owned easing through :64-88/:118-125; the :77 throw dies.
4. `GradientEasingEditor.vue` — rows re-derive from stop-owned easing; the interval-index coupling dies.
5. `useGradientCSS.ts` + `gradientParse.ts` — serializers/parser follow the shape; the :189/:240 throws die; the non-decreasing reject SURVIVES as the foreign-input tripwire only.
6. `GradientPane.vue` — the dead `cssColorOpaque` injection (:8) deleted.
7. `test/gradient-order-invariant.test.ts` NEW — the model's first behavioural test: (i) after any position write through the public mutators, serialize→re-parse ok + monotonic; (ii) an authored easing never migrates on insert/remove; (iii) equal positions serialize and round-trip (the hard-stop witness).
8. `e2e/smoke/views/gradient.spec.ts` + `o21-gradient-rail.spec.ts` — ADD neighbour-crossing drag + round-trip verdict + coarse tap-at-centre+20px count assertions; RE-ANCHOR the four dead-DOM assertions (o21:135/:163; gradient:57/:112) (C4). ADD-never-replace stays the law for living assertions.

**Gates (13, all RED today; every command runnable by a fresh session; instruments verified on disk):**

| # | Gate | Command | RED today — the input |
|---|---|---|---|
| G1 | The model cannot serialize what its own parser rejects; an authored easing never migrates | `npx vite-node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/parse-probe.ts` (end state: `npx vitest run test/gradient-order-invariant.test.ts`) | ARBITER RE-RAN TODAY: the drag's own output REJECTS `non-decreasing`; second arm: a `steps()` authored on 50→100 re-attaches to 25→50 after a 25% insert |
| G2 | Live crossing round-trip: leftmost handle past its neighbour, readout re-fed, verdict null | `node …/evidence/WBGSE-O-r3-crossdrag-forcedcolors.mjs` (first block; end state: e2e `-g "neighbour-crossing drag round-trips"`) | labels [84,50,100]; emitted CSS descends; computed background descends (O's run, digits matching F's) |
| G3 | ONE AXIS: \|handleCentreX(p) − rampX(p)\| ≤ 1px, p ∈ {0,50,100} | `node …/probes/wb-gradient-stopeditor/gate-axis.mjs` (or WBGSE-O-r3-gestures.mjs block B) | **±11.0px** at the terminals (DOM-measured: 235/675 vs 224/686), 0 only at p=50 |
| G4 | The maps are inverse: forward(inverse(p)) = p ± 0.05 at p ∈ {0,100}, read from style.left never aria-label | gate-axis.mjs G1b arm | forward(inverse(0)) = 0.226 — border-box forward vs padding-box inverse |
| G5 | A grab is not a teleport: press anywhere in the advertised hit region, move 1px → stop moves ≤ dead-zone | `node …/probes/wb-gradient-stopeditor/gate-gesture.mjs` (arm A) | 8px off-centre press + 1px travel → **10.11px** handle travel, label 0%→2% (both workers, independent digits) |
| G6 | Pointer select FOCUSES: after a real mouse click, activeElement IS the handle and ArrowRight moves it | gate-gesture.mjs (arm B) | today: activeElement BODY; 2×ArrowRight no effect |
| G7 | Only the primary button mints; cancel only disarms | gate-gesture.mjs (arm C) + WBGSE-O-r3-gestures.mjs (block C14) | middle-click 2→3 (no caveat); right-click 3→4 (chromium-headless cell); post-cure a pointercancel in the dead-zone must mint NOTHING (§12) |
| G8 | One gesture, one owner: one pointermove on a captured handle = ONE position write | `node …/evidence/p3.mjs` (T9) | 2 emits + 2 eager `stops.map` rebuilds per move (21/21); NO serialization-cost delta may be claimed (§8) |
| G9 | No tap that grabs may delete (coarse cell): destructive and grab hit regions disjoint | `node …/evidence/p4.mjs` (T11), playwright-webkit iPhone-14 | tap handle-centre+20px → 3→2, silent; the 8px band 234.7..242.7 inside the rail, unpainted |
| G10 | The chip reserves its own ground: border box intersects no sibling rule | `node …/evidence/p2.mjs` (T2) | chip paints y 244.7→268.7 across the hr at 260.7; elementFromPoint → the chip |
| G11 | Contrast floor (interim): resting/selected handle boundary:fill ≥ 3:1 at every stop of the white-ramp input | `node …/evidence/WBGSE-D-probe4.mjs` + the committed pixel-buffer method | `#ffffff→#fafafa` → ring:fill **1.00:1** (handles invisible; committed shot `D-03-white-ramp-handles.png`); ten-hue ramp 10/10 below 3:1 (§17 scoping) |
| G12 | Type-scale containment: at rootFS 20px the 0% handle stays inside the rail | `node …/evidence/WBGSE-D-probe2.mjs` (block 5) | handle 25px, JS inset 10 → **+1.50px overhang** — the :49-53 comment's own claimed fix, failing |
| G13 | Test truth: zero assertions target retired DOM; the amended files pass; the model has an executable owner | `npx playwright test e2e/smoke/views/gradient.spec.ts e2e/smoke/oracles/o21-gradient-rail.spec.ts --project=smoke` + `npx vitest run test/gradient-order-invariant.test.ts` | today: 5 failed/8 passed; 4 dead-DOM assertions; vitest side: one TYPE-only import — delete `.sort()` and every assertion in the repo stays green (§13) |

**π obligations**: MATRIX — chromium 1440×900 at `:9000` (G1–G8, G11–G13 DOM arms) + playwright-webkit iPhone-14 / 390×844 touch (G9 + crowding); **safari-app is a SEPARATE cell (I-20), nothing transfers; the audit `safari-*` shot dirs are webkit captures cited as webkit only**. ROUTE — `#/gradient`, fully client-seeded; every gate data-free (the API-less dev cell cannot fake one green). SELECTORS — `[data-testid=gradient-stop-bar]`, `button[data-stop-id][aria-label^="Gradient stop at"]`, `.rail-remove-chip[aria-label="Remove selected stop"]`, `[contenteditable=true]` (CSS field). STALE-SERVER LAW (r1 §0/§7, unaffected by R1-C1): literal/serializer claims run headless vite-node against the tree or a freshly restarted server; `scripts/dev/dev.sh` stays untouched (unowned dirty row). TELEMETRY LAW (§10): route-health witnesses are DOM facts, never error channels. COMMITTED CAPTURES (force-added past `.gitignore *.png`, or the claim is not made): `witness/{before,after}-{cross-drag-rail-1440, axis-overlay-1440, grab-teleport-1440, touch-delete-iphone14, chip-hr-1440, white-ramp-contrast-1440, rootfs20-overhang-1440}.png`.

**Delta obligations**: D1 round-trip (REJECT → re-parse ok + verdict null, same commands) · D2 axis (±11.0 → ≤1px) · D3 grab (10.11 → ≤dead-zone) · D4 focus (BODY+dead → BUTTON+moving) · D5 buttons (2→3→4 → 2→2→2, and cancel mints nothing) · D6 type-scale (+1.50 → ≤0 at 16px AND 20px) · D7 dispatch (2 → 1 write/move) · D8 touch delete (3→2 → 3→3) · D9 chip (hr-under → disjoint) · D10 pairing (easing migrates → pair-stable) · D11 contrast (1.00 → ≥3:1, same input) · D12 test truth (4 dead assertions + 5 failing → 0 + green, same commands). **EXPLICITLY NOT CLAIMED**: any drag-frame serialization-cost win (§8); any safari-app property; any bundle/perf property (the witness is a dev server — structurally blind to both).

**Structure over gates (L-8)**: easing hung on its opening stop makes migration-on-insert UNREPRESENTABLE (no index to mis-key); one normalising mutator makes a non-monotonic model unrepresentable by mutation while KEEPING drag-across-a-neighbour and hard stops; grabDx makes the teleport unrepresentable (the emit is a function of displacement, not absolute); the button predicate + cancel-only-disarm make phantom minting unrepresentable; deleting preventDefault makes focus follow the platform — no code left to get wrong; data-only inline style makes hover-shadowing unrepresentable; `canRemove` gives the floor one home; deletions kill the drag double-path, the second selection channel, the parallel array + watcher + three throws, the masking default, the dead inject, and the contextmenu species outright. Gates persist only where no type spans two languages (paint vs map: G3/G4/G12), two hit regions (G9/G10), contrast pixels (G11), the round-trip algebra (G1/G2), gesture totality (G5–G7), and the e2e cell (G13).

**Carries** (every row exactly one disposition, ids for life): **BUILD (this wave)** — L-1+D-2 (=C1/C2/Lr2-1/D2-03; cure per §14) · D-1+L-3 (=C7/C13/D2-02) · L-9+D-11 (=C3; §8) · L-10+D-12 (=C8/Lr2-5; deletion) · L-11 · L-12 · D-13+D2-12 (G12 real) · D-3's geometric arm (=D2-05; G9) · D-4 (=D2-09; G10) · D-10's two cure arms (checker layer; setStopsFromColors validation — end-to-end open until an API cell) · D-9's dead-inject line · C4's spec re-anchoring + the new vitest owner (G13) · C9 · C10 · C11 (G5) · C12 (G6) · C14+cancel-sharpening (G7) · R2-A1's model arm · D2-04's interim arm (G11). **BUILD → W-GRADSTOP-2** (the §7 recomposition: seat rail + selected-stop inspector + WatercolorDot faces + full keyboard grammar): D-5+D2-04's constitutional home · D-6+D2-16 · D-7+C6+D2-06 (+D2-17's aria residue) · D-8+D2-07 · D-9's inspector+D2-08+D2-14 (PR-07) · D-3's constitutional home · D2-10+R2-A1's state arm · D2-13 · D2-15's disambiguation arm · D2-01's `model-unavailable` face. **BUILD → W-GRADSTOP-3** (one sampling law, one literal dialect, leaf types): L-4 · L-5(=Lr2-4) · L-7(=Lr2-7; the leaf now also homes the stop-owned brand) · L-14's dialect arm · R2-A3's two-site rider. NOTE: the ordering-policy question does NOT reopen at W3 — it is settled in this wave (§14). **BUILD → W-TOOLCHAIN** — C4's CI wiring; W-TOOLCHAIN-ONE-ARTEFACT re-scoped per R1-C1 (stale-vintage proof only; no workspace link). **FOLD by identity** — L-2=C5=Lr2-2=D2-01(root) → parser band MT-F001=π-R1 (pinned; cand-O totality; MT-F024 alias recorded) · L-6=Lr2-6 → W-HYGIENE demo/ui (ColorPicker L-6) · L-8(a)=Lr2-8 → W-HYGIENE config parity (cure = deletion per R1-C1) · D2-11 → PR-09 (design band) · D2-01's blast-radius arm → the App-shell boundary-granularity identity (cross-cited, not re-booked). **GLASS-ROUTED** — L-13+D-14=Lr2-3 (standing glass BH relay + banks; the spectrum blur(7px) seam stays glass-owned, cured in 8.0.0, never re-booked). **RETIRE** — D2-17 (refuted both arms, evidence recorded); D2-15's separation-law cure (refuted, §15); the dead clauses (L-8(b), the dock arm, the per-frame cost prose, precision-at-HEAD) die inside surviving rows with refutations recorded.

**BANKS (runnable, swept at every wave open)**: (1) π-R1/MT-F001 — `node --input-type=module -e "const {parseCssColor}=await import('./dist/subpaths/css.js'); try{parseCssColor('oklch()');process.exit(0)}catch{process.exit(1)}"` — **exit 1 TODAY (arbiter's run)**; at exit 0, re-witness the code-editor arm (a typed `oklch()` yields a VERDICT, not an annihilated pane) and gradientParse's oracle claim becomes true as written. (2) glass rail-variant tripwire (r1 bank 2 verbatim). (3) glass-8 version tripwire (r1 bank 3 verbatim).

**Completable alone**: YES. If this were the only wave that ever executed: the model could no longer serialize CSS its own parser rejects; an authored easing could no longer migrate; hard stops and drag-reordering BOTH survive by construction; a grab could not teleport; a mouse user keeps the keyboard grammar; no stray button mints and no cancelled gesture commits; a touch user cannot destroy a stop by grabbing it; the handles cannot be invisible on light ramps; the rail tells one geometric truth at any text size; the behavioural gate stops asserting DOM retired in July; and the parallel array, the second selection channel, the drag double-path, the dead inject, the masking default and the contextmenu species cease to exist — with the model's first behavioural test standing behind it. Zero capability lost, no new surface, no dependency on the parser band, glass-ui, barrels, or the toolchain. This wave is LARGER than r1's core (7 files vs 4) — the price of refusing an interim ordering policy that the same program would immediately delete; the ripple is arbiter-bounded to one tree (r3.1 item 7). Worker-F's narrower two-stage seam is preserved in Dissent §r3-1 as the fallback shape if execution stalls mid-wave.

## r3.5 · ADDENDUM — GRADSTOP-A §9–§17 (E-3: appended; r1 §1–§8 untouched and binding)

9. **RESOLUTION-TRUTH** — the "two value.js's in one build" mechanism (r1-corpus L-8(b)) is a measured FALSE POSITIVE (three independent traceResolution runs, arbiter's included: repo dist, no node_modules segment; TypeScript's Package ID is identity, not provenance) and its phrase may not be cited by any wave or close. The tsconfig `@mkbabb/value.js` paths block is cured by DELETION, not repair — `/css` and `/value` already resolve with no entry (exports self-resolution is both proof and mechanism). No workspace link. The r1 §0/§7 stale-server law is UNAFFECTED.
10. **SHELL-SURVIVAL + TELEMETRY** — "route annihilation" (D2-01) means the pane surface inside `<main>`: BOTH panes (the unrelated companion included), all model state, silently. The nav/dock survive (shell order arbiter-verified); "destroys the whole application/dock" may not be cited. The throw rides a watcher-scheduled debounce into `onErrorCaptured` — pageError/console counts are STRUCTURALLY BLIND to this class; the witness is the DOM (stop count / bar presence), never an error channel.
11. **LATENT-ROW UPGRADE** — D-13/D2-12: the r1 no-gate discipline retires the moment a reproduction exists; the rootFS-20px reproduction (+1.50px overhang) makes G12 a real born-RED gate. The r1 principle (a gate authored green is vacuous) is exactly why the upgrade is legitimate — this gate is authored RED.
12. **GESTURE-TOTALITY** — every pointer entry point states its button predicate and its cancel disposition. Today NO handler inspects `e.button` (middle-click mints 2→3, right-click 3→4 — chromium cell; the middle arm carries the gate without caveat) and `@pointercancel="onBarPointerUp"` routes CANCELLATION through the minting path — cancel handlers may only DISARM. Witnesses: gate-gesture.mjs arm C; WBGSE-O-r3-gestures.mjs block C14.
13. **VACUOUS-SUITE** — "the suite is green" is not evidence for this component while the `HANDLE_HALF = 0` mutation would keep it green AND deleting `.sort()` from addStop keeps every assertion in the repository green (the model's only test/ reference is a type import — arbiter-verified). A gradient close must state which assertion catches each mutation. Today the suite is not even green (5/13 failing) and Playwright is wired to no CI job.
14. **ORDERING-CURE** (supersedes the r1 clamp ruling on its own premise) — the ordering invariant is discharged by NORMALISE-ON-WRITE (stable sort, equality legal) plus STOP-OWNED EASING, landed together; no gradient close may land one without the other. Clamping the sole mutator is banned as a cure: it silently deletes the drag-across-a-neighbour capability (the D-8 refusal species). Equality must remain legal — CSS hard stops require coincident positions and gradientParse deliberately admits them (`<`, never `<=`; arbiter-verified at source).
15. **SEPARATION-LAW BAN** — no wave may adopt a minimum-ordinal-spacing rule for stops (D2-15's proposed cure): it makes hard-stop gradients unauthorable. Crowding is cured by disambiguation (seat, fan-out, overflow), never by forbidding coincidence.
16. **FORCED-COLORS** — `.rail-handle` is enrolled in the U-F57 tier-1 roster (`forced-color-adjust: none`, foundation.css); no gradient row may assert its colour-coded state "collapses in forced colors". Playwright honours `forcedColors: "active"` in BOTH engines at the media-query level (arbiter's decider, committed at `adjudication/arbiter-F/tmp/r3-forcedcolors-decider.mjs`); caveat: webkit computes `forced-color-adjust` empty — chromium is the authoritative forced-colors PAINT cell.
17. **COUNT-SCOPING** — measured counts may only be cited as corrected: the axis skew is **±11.0px** (DOM), not ±10.5 (PNG); swatch contrast is ramp-specific (10/10 below 3:1 at 1.87–2.43 on the constant-lightness ten-hue ramp; exactly 1.00 on white; "6/10" only against its own ramp); the duplicated paint stack is **TWO sites** (`.gradient-rail`, `.gradient-render-tile`), not four/six — the other cited sites share only the layer idiom (arbiter's four-site read).

## r3.6 · DISSENT — preserved verbatim where unresolved

1. **Clamp vs sort (the tri-fold's one substantive cure fork)** — worker-F, ruling for clamp-interim: "clamp remains the core wave's interim law (the gates assert the INVARIANT — non-decreasing + round-trip — not the policy, so either cure passes them); the reorder-gesture question is REOPENED at W-GRADSTOP-3 where C2's stop-owned easing removes the hazard clamp exists to avoid." Worker-O, ruling for sort-now: "under clamp a user dragging the leftmost handle to the right simply stops at the neighbour, silently, which is the same species of unexplained refusal as the add-gesture starvation the same apotheosis books as a defect (D-8). Clamp was chosen at r1 because index-keyed intervals make re-sorting unsafe; once the easing hangs on its opening stop … that objection dissolves and sort becomes the strictly better cure." **Arbiter ruled for O** (r3.2 fork 2). Worker-F's position — including its real virtue, the smaller completable-now core — is preserved as the fallback seam, and the challenger's "clamping … is cheaper and strictly worse" stands as the argument that decided it.
2. **C11 severity** — challenger: BLOCKER ("relocates the stop by the whole grab offset"). Ruled MAJOR (precision-destructive, data-recoverable). The losing grade preserved.
3. **D2-02/D2-03 BLOCKER grades vs r1's D-1 MAJOR** — the fresh D seat re-asserts BLOCKER on the axis substance; nothing new decides severity; the r1 ruling stands; the fresh grade joins the preserved dissent (and O's refusal to re-litigate on identical facts is adopted as method).
4. **D2-17, worker-F's losing verdict** — preserved: "the shipped forced-colors matrix row runs WebKit, which ignores the emulation (the shot proves nothing — an environment fact, honestly recorded)". It lost because the claim was itself unmeasured while a 20-line probe settled it (both engines match the query; the roster kills the mechanism). Recorded as the r3 method lesson: UNVERIFIABLE-HERE requires showing the arm cannot be witnessed HERE, not that nobody has witnessed it.
5. **R2-A3, worker-F's four-site count** — preserved as the losing position; it conflated the ramp-over-checker layer idiom with the material contract (r3.2 fork 3). The idiom-vs-contract distinction is itself worth keeping: the easing editor's :154 inline shorthand is an instance of the PRE-cure idiom and may ride W-GRADSTOP-3's rider as a third consumer of the one home, without inflating the count.
6. **D2-01's dock arm** — the challenger overclaimed ("dock unmounted"); refuted by the shell read (both workers + arbiter). Recorded because the overclaim is the L-9 class: a probe measuring bodyLen without reading the shell.
7. **C14 cell limits** — worker-O: the right-button arm "may be swallowed by a native context menu on a real desktop", and macOS Safari untested (I-20). The middle-button arm carries the gate alone. Worker-F's dissent note kept: the challenger's earlier clean-state right-click did NOT mint in its cell while both workers' chromium runs minted deterministically — both records kept; the reproduction with its exact command governs.
8. **Worker-O's method note, preserved as law-grade caution** — "my first crossing-drag run moved the handle only to 36.8% — short of the neighbour — and produced a perfectly ordered model and a null verdict. Had I stopped there I would have reported D2-03 as REFUTED. A gate that does not attempt the crossing passes with the defect fully present." This is GRADSTOP-A §1's neighbour-crossing clause, re-proven from the inside.
9. **D-10's end-to-end arm** (carried from r1, still open) — the `setStopsFromColors` write path needs an API-backed evidence cell; the dev server is API-LESS and a blank data surface is an environment fact. UNVERIFIABLE-HERE inside a surviving row, not a whole-row verdict.

---

*r3 adjudicated and authored by arbiter-F (Fable 5, `claude-fable-5`) · 2026-07-28 · arbiter instruments: `adjudication/arbiter-F/tmp/r3-forcedcolors-decider.mjs` (new, run pasted in r3.1), parse-probe re-run, traceResolution re-run, bank-1 re-run (exit 1) · worker-F's in-place draft superseded by this section (provenance in header) · live cells chromium + playwright-webkit at :9000 (DOM-behaviour only, per the §7 stale-server law); no safari-app claim anywhere in this record (I-20).*
