# wb-gradient-easingeditor — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator (the incredulity seat) · **Date**: 2026-08-12 · **HEAD**: `9c779130` · branch `tranche-u`.
**Corpus**: ten challenge documents (C r1/r2/r3 · D r1/r2/r3 · L r1/r2/r3/r4; r2-and-later govern) against `demo/workbenches/gradient/GradientVisualizer/GradientEasingEditor.vue` (295 L by my `wc -l`; the corpus says 296 — trailing-newline drift) + its row/catalogue/model/CSS dependency chain (`useSpecimenRows.ts`, `easing/easingCatalogue.ts`, `easing/EasingAuthoringStage.vue`, `composables/useGradientModel.ts`, `composables/useGradientCSS.ts`) and the installed glass-ui 7.0.0 producer surfaces.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted; every kill was re-derived by this seat before sustaining; the one head-on verdict conflict (D3-03: DU KILLED vs LC CONFIRMED) was ruled by my own whole-file read of the law's source, not by picking a reader.
**Sibling identity law**: `wb-gradient-easingspecimenstrip.md` (2026-08-09, this same program) already adjudicated the shared chain's headline rows. Per the ColorSpaceSelector addendum item 5 (anti-rename), every fact-identical row here is CROSS-BOOKED to that record's identity, never re-booked — the riders section below names all of them.
**Arbiter's own instruments (all static + grep/node against source and installed dist; ZERO browser probes — probe parsimony)**: whole-file reads of `GradientEasingEditor.vue` (295 L), `useSpecimenRows.ts` (75 L), `EasingAuthoringStage.vue` (116 L), `vitest.config.ts` (31 L), `test/gradient-v4-consume.test.ts:1-59`; targeted reads `useGradientCSS.ts:36-133/:170-264`, `useGradientModel.ts:40-168`, `easingCatalogue.ts:95-231`, `src/css/types.ts:25-27` (the non-empty-tuple `ParseResult` read verbatim); producer probes: `grep -c 'role:"img"' dist/easing.js` → **0** (the corpus's sixth independent run), `--focus-ring-shadow: 0 0 0 var(--focus-ring-width) color-mix(…), 0 0 8px color-mix(…)` (zero offset — pinned from dist/styles; **DU's `scale-paper.css:1` path citation is WRONG — no such file installed; the value is real, the home is not**), `--surface-tint-4/6` shipped in two scheme arms, `--radius-card: var(--radius-2xl)`, `FadingScroll.vue.d.ts` `ariaLabel`/`ariaLabelledby` both published, `Input.vue.d.ts` **zero** slot declarations, `useClipboard.d.ts` `invalidate` published, `EasingPicker.vue.d.ts` `__VLS_Props` = exactly {mode, preset, steps, term, readout, playback, label}; manifest: `dependencies` = exactly `{"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}`, exports = 7 keys no root; `.github/workflows/ci.yml:36-37` build-precedes-test; bare `@mkbabb/value.js/*` specifiers in all three gradient composables (my grep); censuses re-run: `ink-muted` **0 in demo/workbenches/gradient/ vs 12 consumer files in demo/**, `@media (hover` = 1 demo hit, `surface-tint-` = 0 demo consumers, test/→`../demo/` = **11 files recursive** (correcting BOTH r4's "twelve" AND LC's flat-glob "10"); novelty checks against the 10-doc corpus: `COALESCE_RESOLUTION|stepsPerInterval` → **0 hits** (LC's miss is GENUINE), `quart` → hits in C-r2/L-r3/L-r4 (**DU's "no axis found it" is FALSE**), `ink-muted` → 0 hits (DU's miss is GENUINE); WCAG relative-luminance arithmetic re-derived by hand: L(112,89,66)=0.1098 · L(240,184,202)=0.5707 → **3.88:1** (DU's 3.90 checks) and L(224,215,206)=0.6890 → **4.62:1** (the r3 5.06-vs-4.62 internal inconsistency confirmed real); grid arithmetic re-derived: `stepsPerInterval = max(2, round(32/(stops−1)))` → stops 9 ⇒ 4 samples ⇒ `steps(4, jump-end)` ≡ linear at t∈{0,.25,.5,.75}, stops ≥17 ⇒ 2 samples, while `serializeIntervalRamp`'s 2-stop sub-model (:242-252) always yields 33; X wave ownership re-read from `docs/tranches/X/waves/`: W6 files table :51-61 owns `useGradientModel.ts`, `gradientParse.ts`, `useGradientCSS.ts`, `useGradientInterpolation.ts`, `EasingAuthoringStage.vue`, `easingCatalogue.ts` (+ `GradientVisualizer.vue`, `GradientStopEditor.vue` at :161/:202), W6.md:159 **already programs the deletion of the `intervals` parallel array, its length watcher and the index-keyed `updateInterval`**, W6.md:238 gate e2 already counts the stage's two rAF sites, W9.md:102/:279/G31 owns `package.json` and G31 already measures the dependency map, W1.md has **zero** vitest rows; r2 governing severity headers read for all of D2-01..D2-15 and C2-1..C2-11.
**Coordination catch (this seat's own)**: W6.md:59 lists the subject file at `GradientVisualizer/easing/GradientEasingEditor.vue` — the file actually lives at `GradientVisualizer/GradientEasingEditor.vue` (read there whole by me). W6 must correct the path or its file-table row points at a nonexistent file.
**Motion/PRM law**: my grep over `audit/codex-provenance/motion-quarantine.md` — **zero M-xx rows name any file in this corpus** (workflow-authored tree, not among the 46 REPORT-AUTHORED files), so no FALSE row exists to carry. D3-14 (coarse-pointer hover) is carried ONLY as UNPROVEN-NEEDS-LIVE, per both readers.
**Status verbs**: every disposition here is **ADJUDICATED**, never VERIFIED — nothing in this file stamps a wave.

## Scoreboard

**24 axis rows booked NEW (1 BLOCKER · 13 MAJOR · 9 MINOR · 1 INFO) · 13 identity riders CROSS-BOOKED to standing rows (incl. the C-01 route-kill BLOCKER at the strip apotheosis and 3 banked program identities) · 8 reader-adopted missed findings join (2 MAJOR · 4 MINOR · 2 INFO) · 11 kills sustained (every one re-derived) · 1 head-on verdict conflict RULED (D3-03) · 2 reader-corrections recorded against the re-readers themselves · 10 UNPROVEN-NEEDS-LIVE residue rows → visual audit.**

Blocker-weight survivor NEW to this record: **D3-02** — the component's primary control, the interval accordion head, paints **zero focus pixels** when keyboard-focused: `overflow-hidden` on a padding-less row (:114) clips a zero-offset `box-shadow`-only ring (:228-231; token value pinned by this seat from dist) on a child flush with the clip rect on all four sides — while the file's own comment (:220-221) claims the house focus register. The route-kill BLOCKER (three `back` tiles → `Result`-to-`throw` in render computeds) remains real and is cross-booked to the strip apotheosis's C-01, where it was already widened with the hover seam.

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D3-03 / D2-15** ("one ink per specimen" false) | KILLED — the law reads one-ink-per-ROW; the derivation is declared and working; the defective use is D2-11 | CONFIRMED MAJOR — "the row ink is overwhelmingly a function of the STOP COLORS, not of its curve; false in both directions" | **KILL SUSTAINED, RESCOPED** — DU wins the verdict; LC's arithmetic survives as a limb elsewhere. | My whole-file read of `useSpecimenRows.ts`: the docstring (:1-9) DECLARES the ink as "the eased ramp midpoint, contrast-certified" — a function of the two stops through the curve, per row. No source text claims the ink identifies the CURVE; the curve's identity channel is the glyph SHAPE (`glyphPath(fn)`, :67) and the name (:66). Four linear intervals over four stop pairs yielding four inks is the declaration executing, not failing. LC's collision fact (every fn with fn(0.5)=0.5 — linear, ease-in-out, expo, smooth-step-3, steps(4,jump-end) — takes one ink) is TRUE and folds into M-EE-4 (the comparison-poverty row) as its sharpening; the genuinely defective use of this ink — painting boolean control states — is D2-11, booked below. r2's own MINOR grade on D2-15 concedes the altitude. |
| **D3-01 grade** (head name below AA) | CONFIRMED **BLOCKER** (3.90:1 re-derived; VC:82 material-tier law) | Structural asymmetry source-confirmed; the RATIOS filed UNPROVEN-NEEDS-LIVE | **CONFIRMED at MAJOR — demoted; DU's BLOCKER preserved as dissent with a revival clause.** | Direction is as certain as static gets: three independent numbers agree (DU's 3.90 re-derived by MY hand-arithmetic from the sampled RGBs; r3's 3.86/3.73; the file's own :167-175 comment conceding ~2.7:1 for the byte-identical unwelled composite). But the registry reserves BLOCKER for route-kills, dead-law classes and zero-register operability (D3-02); a 3.9-vs-4.5 AA miss on de-emphasis-toned text, curable one class away, is the L-2-not-L-1 severity class of the ColorSpaceSelector precedent. REVIVAL: if the U-1 live composite confirms sub-4.5 on the head in BOTH schemes, the row re-grades BLOCKER at the visual audit. The cure is sharper than r3 knew — M-EE-1's `--ink-muted` rung. |
| **C2-2 / L-r3:L-1 severity** (FAMILY_ORDER drops quart/quint) | filed as a MISSED finding, "no axis in ten rounds found it" | escalated to **BLOCKER** | **MAJOR — the standing grade; BOTH readers corrected.** | (a) DU's novelty claim is FALSE: my corpus grep finds `quart` in C-r2 (C2-2's own header: "FAMILY_ORDER silently deletes 6 of the library's 30 presets"), L-r3 and L-r4 — DU re-derived an existing row and filed it as new; its genuine sharpening (the module docstring's "never a second mint" false on both clauses; the unsuppressed picker Select OFFERING what the gallery hides) folds into the identity. (b) LC's BLOCKER does not survive the severity register: r2 grades it MAJOR, and the sibling apotheosis already ADJUDICATED this identity MAJOR → X-W6 (its C-06). Silent capability loss + false identity (`custom`) is grave, but no route dies and no law class is voided. ONE identity, cross-booked. |
| **N-3's aside** (test/→demo reach count) | — | KILLED r4's "twelve"; measured 10 | **KILL SUSTAINED — and LC corrected too.** | My own recursive census (`grep -rl '\.\./demo/' test/ --include='*.ts'`) → **11 files**. r4's 12 does not close by enumeration; LC's 10 was a flat `test/*.ts` glob that misses the mirrored subdirs its own sibling-config finding depends on. The direction row (test/ reaching into demo/) is confirmed and worth its ruling — at 11. |
| **L-r1:L-1 framing** (glass-ui/keyframes as runtime deps) | — | KILLED as stated ("the published library runtime-depends"); carried r3's corrected manifest form | **KILL SUSTAINED; corrected row CONFIRMED MAJOR → X-W9.** | My own runs: `dependencies` = exactly the two entries (node read); dist ships zero bare external specifiers (LC's grep, spot-consistent with the strip apotheosis's inv-K-1 finding). The defect lives in `package.json`, not in `dist/` — and X-W9 G31 ALREADY measures this exact dependency map (my W9.md:373 read), so the reclassification rides the wave that owns the manifest. r1's wording must not reach a ledger row. |

## Adjudicated defect roster

Line anchors are HEAD `9c779130`; re-resolve before executing any cure (the D-19 lesson). **NAMED GAP (◆)**: as established at the sibling apotheosis, `EasingSpecimenStrip.vue` and `useSpecimenRows.ts` appear in NO X wave's file list — rows marked ◆ route X-W6 only if W6 adopts those files explicitly, else they degrade to NO-WAVE-OWNER at execution.

### New rows (this corpus)

- **D3-02 / D-01 — ADJUDICATED BLOCKER → X-W6.** A focused collapsed interval head paints zero focus pixels; the open row keeps a ~4px bottom band only. Mechanism re-derived wholly by this seat: row `rounded-card border border-card-edge overflow-hidden` with NO padding (:114 — padding box ≡ content box ≡ the clip rect), head `w-full … px-3 py-2` (:119) flush on all four sides when the panel is `v-show`-collapsed (:143-146 → display:none, zero height), focus expressed as `outline: none; box-shadow: var(--focus-ring-shadow)` (:228-231) whose token value — pinned by me from dist — is `0 0 0 <width> …, 0 0 8px …`: zero offset, 100% outside the border box, 100% clipped. The comment at :220-221 asserts the opposite of the shipped behaviour. Same dead register on `.rail-btn:focus-visible` inside the panel is NOT clipped (interior exists) — the kill is head-specific. WCAG 2.4.7 fails on the component's primary control. Citation correction: the token's home is NOT `scale-paper.css` (no such file installed).
- **D3-01 — ADJUDICATED MAJOR (grade ruled above; BLOCKER dissent + revival clause) → X-W6.** The curve name (:136) and ordinal (:124) carry `text-mono-small text-muted-foreground` on the raw translucent pane; the byte-identical triple 40px lower sits in `bg-well` (:176-177) because the file's own comment (:167-175) measured the unwelled composite at ~2.7:1 and cured ONLY the rail. Cure vector = M-EE-1 (`--ink-muted`); numeric closure = U-1/U-2.
- **D2-04 / L-r3:L-3 — ADJUDICATED MAJOR → GLASS ask (BH relay) + X-W6 consumer half.** The disclosed stage suppresses two of the producer's three surfaces (`:readout="false" :playback="false"`, EasingAuthoringStage.vue:78-79 — my read) but `EasingPicker` publishes NO prop to suppress its preset Select (my d.ts read: exactly 7 props); the producer's own 30-option menu renders under the 27-tile strip, and by the C2-2 identity the two surfaces DISAGREE by six entries. The seat's thesis (:8-12, "the strip selects, the picker authors") is half-enforceable by construction. Producer ask: a `presets`/rail-suppression axis; consumer half: adopt it when it lands.
- **L-r1:L-1-corrected — ADJUDICATED MAJOR → X-W9** (ruled above). Two runtime deps, zero imports from the published surface (`@mkbabb/keyframes.js` referenced 0 times in src/ AND demo/ — LC's grep), a declared package cycle and install-weight tax; both belong in devDependencies. Rides W9's manifest ownership beside G31.
- **L-r1:L-2 / L-r2-2 — ADJUDICATED MAJOR → X-W6.** `GradientInterval = EasingPickerValue` (useGradientModel.ts:49 — my read): the persisted domain type IS a Vue widget's v-model payload, propagated into the headless parser (`gradientParse.ts:22,27` — corroborated by W8.md:290's own edge census) with dead-field filler on every mint (`steps: 4, term: "jump-end"` on bezier tiles, `points: [0,0,1,1]` on steps tiles — easingCatalogue.ts:119-126/:145-153, my read). W6.md:202's model recomposition (`model/types.ts` new) is the programmed home; this row must be reconciled there.
- **D3-08 / C2-5(a) — ADJUDICATED MAJOR → X-W6 (programmed: W6.md:159).** Deleting a stop while a later row is open leaves ZERO open rows: `openInterval = ref(0)` (:61) is never clamped; the sync watcher truncates intervals from the END (useGradientModel.ts:96-98 — my read); `v-show="openInterval === row.index"` (:144) then matches nothing — no ramp, strip, literal, editor, or message. Presents as silence, not as the C-01 route kill, because `serializeIntervalRamp` returns `null` for the missing stop (:236-238 — my read; see M-EE-6).
- **D3-09 / C2-5(b) / D-09 — ADJUDICATED MAJOR → X-W6 (programmed: W6.md:159).** Interval identity is positional while stop identity is not: `addStop` inserts position-sorted (:116-120) while the watcher only appends `linearInterval()` at the END (:89-100), so a mid-gradient insert silently re-hosts every downstream authored curve; `removeStop` removes by id (:122-125) while FIVE stateful surfaces key on the ordinal (`openInterval` :61, `tuneOpen` :84, `copiedIndex` :95, the emit contract :48-50, the DOM ids :145/:205 + `:key="row.index"` :113). W6's stop-hosted-easing mechanism ("`GradientStop` gains `easing`; the parallel array DELETED") is the cure already programmed — this row must be reconciled with that text so ONE identity decision lands.
- **D3-04 + D3-05 / OM-4 — ADJUDICATED MAJOR → X-W6 under X-W10's law.** Proximity inversion (8px between sibling rows vs 10px between one row's own parts — :107 `gap-2`, :146 `gap-2.5`, head `py-2` :119) + one `rounded-card` declaration (:114; `--radius-card: var(--radius-2xl)` — my token read) spanning 40.94px and 196.36px boxes → a ~4.8× curvature swing flipping on click with no transition. Both of OM-4's owner readings from ONE declaration. Exact ratio inputs are committed probe JSON (frames-r3), residue-free.
- **D3-06 / OM-13 — ADJUDICATED MAJOR → X-W6 consumer half + GLASS ask (BH relay).** `bg-well` (an opaque `--card`-derived tone step, demo/foundation.css:328 per both readers) seated over the translucent pane → the material step's SIGN inverts between schemes (corroborated ACROSS ROUNDS from opposite verdicts: r1's own negative-results table measured the identical flip and ruled it sound; r3 measured it and ruled it a defect). Producer half verified by ME: `Input.vue.d.ts` declares zero slots — no trailing-adornment seat exists; the ask is a genuine producer gap. ΔL magnitudes → U-1.
- **D3-07 + D-04 — ADJUDICATED MAJOR → X-W6.** The row's ONE literal truncates at BOTH viewport extremes: `truncate flex-1 min-w-0` with `:title` as sole recovery (:177 — my read); the catalogue guarantees the SHORTEST possible literal is the 24-char boot default (all bezier tiles mint through `bezierLiteral`, easingCatalogue.ts:106-128 — my read), so any width that clips it clips 100% of the catalogue; the 38-char circ/back literals overflow at desktop per r1's font metrics (re-derived consistent by DU). Numbers → U-3.
- **D3-11 / R3-4 / C2-4 / D2-14 — ADJUDICATED MAJOR → X-W6.** Every interval row mounts the full 27-tile strip (:161 inside the v-for :112) AND a live `EasingPicker` (:203-212 — v-show wrapper, so the stage mounts on first render of every row) — my read. The docstring's justification (:16-18, picker-liveness) requires only the OPEN row's picker to survive. The tautological guard confirmed: `v-if="intervals[row.index]"` (:207) can never be false — `useSpecimenRows.ts:47-51` already `continue`s on falsy intervals and sets `index: i` — a masking guard beside a `!` assertion. The strip limb ≡ the sibling's C-11 identity (booked MINOR there); the picker limb is this row's escalation to MAJOR.
- **D-05 — ADJUDICATED MAJOR → X-W6.** Two hand-rolled disclosures (:61-71 + :119-147; :84-88 + :187-213) and two hand-rolled ghost icon buttons with bespoke hover wash, radius, transition and the D3-02-dead focus ring (:269-291) — while glass 7.0.0 ships `./collapsible`/`./expandable-container` with an accordion mode (`[data-disclosure=accordion]` read in glass.css at the sibling adjudication) and `./button`, and the same-folder sibling consumes the producer correctly. Adopting the producer disclosure also closes M-EE-3.
- **D2-11 — ADJUDICATED MAJOR → X-W6.** Boolean control states painted in the data-derived per-row ink: `.rail-btn--on` and `.rail-tick` take `var(--motion-accent, …)` (:289-294 — my read), set per-row from `row.ink` (:115) — so "authoring is open" and "copy succeeded" change hue per row and when the user edits the curve. Verified non-collision with the producer's one `--motion-accent` read (the `--easing-curve-accent` door — LC's dist check). This is the ink misuse D3-03's kill correctly re-points at.
- **D-03 / D2-08a — ADJUDICATED MAJOR → X-W6 ◆.** The ordinal `label: `${i+1} → ${i+2}`` is minted un-isolated (useSpecimenRows.ts:62 — my read) and rendered in prose context (:124); under UBA rule N1 (U+2192 neutral between two EN runs) an RTL paragraph renders `2 → 1` while the ramp below still runs stop1→stop2. Neither `row.label` nor `row.css` (:177) is LTR-isolated. Mint lives in the NAMED-GAP file — hence ◆.
- **D-13 — ADJUDICATED MINOR → X-W6.** `.specimen-dot + .specimen-dot { margin-left: -3px }` (:249-251) — the single physical property in an otherwise logical rule (:240-241): in RTL the pair goes tangent, the ":234 slightly overlapped — a pair" reading lost. Cure `margin-inline-start`.
- **D2-06a — ADJUDICATED MINOR → X-W6 ◆.** Two hardcoded 9px sizes (`font-size: 0.5625rem`, EasingSpecimenStrip.vue:150/:196) below the closed type matrix's smallest rung (--text-micro bridge). Statically certain; rendered sizes stay in the sibling's U-9.
- **D2-13 — ADJUDICATED MINOR → X-W6.** The head names `linear` while the rail exports and announces `cubic-bezier(0, 0, 1, 1)` — every tile including the five CSS keywords mints through `bezierLiteral` (easingCatalogue.ts:106-128, my read) for byte-identity with the picker (a real internal law, easingCatalogue.ts:38-45) paid for in the exported artifact. Cure must NOT break the identity law (superlative S-6).
- **D2-12 — ADJUDICATED MINOR → X-W6.** One surface drawn twice: the rail's `bg-well` (:176) and the stage's `:deep(.glass-card) { background: var(--well-bg); border: …; }` (EasingAuthoringStage.vue:93-99 — my read) — the same token, two edges, two radii, `gap-2.5` apart.
- **R3-7 + C2-7 — ADJUDICATED MINOR → X-W6 (sharpens the sibling's M-ESS-3 identity — one row).** `useClipboard` publishes `invalidate` (my d.ts grep) and the seat never calls it (:94-103 — my read): a tile press inside the 1400ms window leaves an affirmative tick + `aria-label="Copied"` beside a literal that is not on the clipboard; C2-7's limbs (failed copy silent; success never announced to AT) ride the same fix.
- **C2-10 / C-9 — ADJUDICATED MINOR → X-W6.** `toggleTune` spread-clones the whole record per press and never prunes (:86-88 — my read); `ref` is already deeply reactive.
- **resolvedEasingCache — ADJUDICATED MINOR → X-W6.** Module-scope, unbounded, keyed by user-typed CSS (useGradientCSS.ts:69 — my read); the in-repo precedent self-caps at 512 (`tintLCache` — both readers).
- **L-6 / L-r2-7 + L-11 — ADJUDICATED MINOR → X-W6 (barrel); demo/ui rides the W-HYGIENE banked identity.** 7 of 9 barrel names with zero consumers, a 3-hop dual path to constants the sibling workbench reaches in one (LC's enumeration, spot-consistent with my reads); `demo/ui/` = 18 one-line pass-throughs ≡ ColorSpaceSelector L-4's banked extirpation — not re-booked.
- **R3-8-in-kind — ADJUDICATED MINOR → X-W6 (magnitude KILLED, K-8).** `certifyAccentInk` uncached + the derivation over-subscribed (the whole `modelState` passed at :54-58 while :56-58's inputs never read `type`/`direction` — LC's trace, consistent with my useGradientModel.ts:102-109 read). LEDGER LAW carried from the kill: no tick-level perf number in this corpus may be inherited into any wave row without a fresh measurement.
- **D-16 — ADJUDICATED INFO → X-W6 (adjacent; GradientVisualizer.vue owned).** The three section headings use the palette-identity type role (`font-display text-subheading`, GradientVisualizer.vue:241/:149/:253) against the matrix's `text-heading`/Jakarta row.

### Identity riders (cross-booked — ONE identity each, never re-booked; original bookings at `wb-gradient-easingspecimenstrip.md` unless noted)

- **R-A · D2-01 ≡ C2-1 ≡ N-1/R3-1/C-1 ≡ the sibling's C-01 (BLOCKER → X-W6, widened there with the hover/ghost third seam).** My re-reads confirm every editor-side limb: `err` at operations.ts:90 (LC's :65/:90 citation correction adopted), the throws at useGradientInterpolation.ts:37 + useGradientCSS.ts:202-208, the render computeds (`useSpecimenRows.ts:52-59` fn(0.5); `openIntervalRamp` :63-67). LC's N-4 (four conversion sites + the unowned seam) FOLDS here as the cure-side census; its fourth site — `easingValue` throwing at MODULE EVAL via `buildFamilies()` (easingCatalogue.ts:103/:198, statically imported through useSpecimenRows:16) — ≡ the sibling's M-ESS-8. The re-homed cure (totality inside `interpolateStopColors` + `sampleCoalescedStops`) stands as ruled there.
- **R-B · D2-03 ≡ N-2/R3-2 ≡ the sibling's C-05/D-40 (MAJOR → X-W6 + glass canvas-hook ask).** My sixth `role:"img"` → 0 run; my whole-file stage read confirms ~35 of 116 lines dead (the :44-67 apparatus, :74 binding, :104-115 deep block incl. the T-48 `transition: aspect-ratio` riding a selector that matches nothing). LIMBS FOLDED: C2-8 (the `!important`s on producer internals — dead with the selector) and C2-9 (the rAF fan-out at :58/:65 — cheap but real; W6 gate e2 already counts exactly these two sites). LC's kill of the "frozen `--vb-ratio` is WRONG for overshoot" clause SUSTAINED by my read: the block that consumes the var is the block that matches nothing.
- **R-C · C2-2/L-r3:L-1 ≡ the sibling's C-06 (MAJOR → X-W6)** — severity + novelty ruled above; DU's docstring-falsity + surfaces-disagree sharpenings attach to the identity.
- **R-D · N-3/R3-3 ≡ the sibling's C-12 (MAJOR → split X-W1 oracle re-key / X-W6 unit invariants).** My own read of `test/gradient-v4-consume.test.ts:55-57` confirms the 7-tile slack; K-7 kills r1's "zero vitest files" sub-claim on the same file's :11.
- **R-E · D2-02 ≡ the sibling's D-01 (the missing `/easing` route; MAJOR → X-W6 routed scene).** READER-COVERAGE CORRECTION: r2 grades this BLOCKER and BOTH re-readers passed it in total silence — neither confirmed, killed, nor filed it. The identity stands at the sibling's MAJOR (a mis-seated protagonist, not a dead route); the silence is recorded as a fleet fact.
- **R-F · R3-5 ≡ the sibling's C-09/D-25 (label-in-name, exactly 1 of 27; MINOR → X-W6 ◆).**
- **R-G · R3-6/D2-09 ≡ the sibling's L3-4 (27 toggles, no radiogroup/toggle-group; MAJOR → X-W6 ◆ + glass `$attrs` ask).**
- **R-H · D2-05/L-r3:L-4 ≡ the sibling's D-20/L-6/C-10 (shape="cell" bought then fought; glass specimen-rung ask + X-W6 ◆).** My d.ts read confirms the radius-less SHAPE.cell map.
- **R-I · D2-10's DEFECT ≡ the sibling's C-08 (nameless focusable scroll port; MAJOR → X-W6 ◆).** The CURE is killed here (K-4): the producer already ships `ariaLabel`/`ariaLabelledby` (my grep) — one consumer prop, no relay.
- **R-J · DU-missed#5 (`.specimen-strip` dead class) ≡ the sibling's C-13 (INFO → X-W6 ◆, PCS-22 hook law).** READER CORRECTION: filed by DU as corpus-novel; it is already ADJUDICATED in the registry.
- **Banked identities (program-wide, not re-booked)**: C-4/L-r2-1 dead lint lattice ≡ ColorSpaceSelector **L-5(a)**; L-r4:C-2 tsconfig paths-bypass ≡ **L-14/XP-21/MX-20** (LC's `--traceResolution` run — "the exports map is never consulted" — attaches as the identity's sharpest instrument yet); demo/ui pass-throughs ≡ **W-HYGIENE**.

### Adopted missed findings (readers' union, each re-verified by this seat)

- **M-EE-1 (DU) — the house ships a contrast-CERTIFIED de-emphasis rung (`--ink-muted`) and the entire gradient workbench declines it — ADJUDICATED MAJOR → X-W6 (editor rows) ◆ (strip/rows files).** MY census: `ink-muted` = 0 in demo/workbenches/gradient/, 12 consumer files elsewhere in demo/, 0 mentions in the 10-doc corpus — the novelty is REAL. The five uncertified runs: GradientEasingEditor.vue:124/:136/:177/:275 + EasingSpecimenStrip.vue:152/:198. The irony DU names is exact and source-true: the component certifies its DECORATION (`safeCss(mid)`, useSpecimenRows:40/:69 — my read) while every TEXT run declines the certifier. This is D3-01's named cure; whether it alone clears 4.5 on the head → U-2.
- **M-EE-2 (LC) — per-interval easing progressively ERASED from the rendered gradient as stop count grows, while the row's ramp never degrades — ADJUDICATED MAJOR → X-W6.** MY re-derivation from source: the 32-sample budget is TOTAL (`stepsPerInterval = max(2, round(32/(stops−1)))`, :180-183), so density falls as 1/(N−1) and floors at 2, while `serializeIntervalRamp` builds a 2-stop sub-model (:242-252) that ALWAYS samples at 33 — at 9 stops `steps(4, jump-end)` emits t∈{0,.25,.5,.75} ≡ the linear diagonal (my arithmetic), at ≥17 stops every curve is 2-point-sampled. The docstring (:226-231 — "the strip shows the interval exactly as the gradient will render it") is materially false from 9 stops. ZERO corpus hits on `COALESCE_RESOLUTION|stepsPerInterval` (my grep) — ten rounds drove 20-stop gradients through this code without noticing. Lands with W6.md:202's sampling-law recomposition.
- **M-EE-3 (DU) — no heading route to any interval panel — ADJUDICATED MINOR → X-W6.** My read: the trigger (:117-122) has no heading wrapper/role/level; the only heading in scope is the section `<h3>`. APG accordion pattern; closes for free under D-05's producer-disclosure adoption.
- **M-EE-4 (DU, + LC's D3-03 arithmetic folded as its sharpening) — the exclusive accordion defeats the component's own bench thesis — ADJUDICATED MINOR → X-W6.** `toggleInterval` caps open rows at one (:69-71 — my read) in a file naming itself "the interval specimen bench" (:3); the only cross-interval surface is the 16px head glyph whose ink varies with the STOPS (and collides across curves with fn(0.5)=0.5 — LC's surviving limb), and no propagate/copy-across affordance exists (`updateInterval` and the emit are single-index — my reads). The interaction cost of N was measured by no axis.
- **M-EE-5 (LC) — vitest carries no value.js self-alias: the unit gate certifies `dist/` while `@src` imports certify source — ADJUDICATED MINOR → NO-WAVE-OWNER (next formation boundary).** MY reads: vitest.config.ts declares only `@src` (whole-file read); all three gradient composables import bare `@mkbabb/value.js/*` (my grep); `test/gradient-v4-consume.test.ts` mixes both in one file (:2 vs :8-11 — my read); ci.yml:36-37 orders build-before-test, so CI is safe BY ACCIDENT and a local `npm test` is not. W1.md contains zero vitest rows (my grep) — no wave owns test-harness config parity; adjacent to but distinct from the banked tsconfig identity.
- **M-EE-6 (LC) — two OPPOSITE error postures two lines apart, the harsher one unreachable — ADJUDICATED MINOR → X-W6 (joins the R-A totality decision).** My read of :236-240: the `null` arm always fires first (the length invariant makes the `throw` dead), and that `null` is load-bearing — it is WHY D3-08 presents as silence. One function silently degrades on one failure and would kill the pane on its neighbour, with no stated rule.
- **M-EE-7 (LC) — the editable CSS surface is the one serialization that cannot carry the model's easing; Apply re-seeds every interval to linear — ADJUDICATED INFO → X-W6.** LC's chain (GradientVisualizer.vue:258-259 binds `simpleCSS`; gradientParse re-seeds `linearInterval()` per pair) is consistent with my `applyCSS` read; the reset is documented policy (easing-disposition §1.6), so the row is the ASYMMETRY (show one form, copy a second, round-trip a lossy third), not a bug.
- **M-EE-8 (DU) — two ad-hoc hover washes; the producer's `--surface-tint-4/6` rungs shipped with zero demo consumers — ADJUDICATED INFO → X-W6.** MY runs: the 4%/6% hand-mixes at :225-227/:281-284 (read); `surface-tint-` = 0 demo hits; the tokens exist in dist in both scheme arms (my grep). An unadopted producer rung family, same class as the M-EE-1 gap.

## Killed-claims register (every kill re-derived by this seat before sustaining)

- **K-1 · D3-10 (head "reservation inverted") — KILL SUSTAINED.** DU's geometry is decisive and uses the seat's own committed frames: head content and strip port both resolve to 436px — different flow rows never competing for one inline axis; the slack is `flex-1 min-w-0` doing exactly what D3-07's cure needs it to do.
- **K-2 · D3-12 (stale `aria-expanded` on hidden controls) — KILL SUSTAINED.** The tune button is a descendant of the v-show panel: `display:none` subtrees are out of the accessibility tree in every engine — an attribute in the DOM is not a state exposed to AT. Surviving limbs already live at r1 D-06/D2-07 (residue) and D3-09 (booked).
- **K-3 · D3-03/D2-15 — KILL SUSTAINED, RESCOPED** (ruled above): the law reads per-row and the source keeps it; LC's collision arithmetic folds into M-EE-4; the ink's real misuse is D2-11.
- **K-4 · D2-10's CURE ("FadingScroll should accept role/label — relay to glass") — KILL SUSTAINED.** My d.ts grep: `ariaLabel`/`ariaLabelledby` both published; the compiled render conditions `role="region"` on them. The defect (R-I) is a one-prop consumer omission; a relay would burn a producer cycle on nothing.
- **K-5 · r1 D-14 (rail controls "pinned to the floor") — KILL SUSTAINED.** 5px padding (:273) around a 14px glyph = a 24×24 target: glyph and target are TWO quantities; this is PR-12's satisfied state and SC 2.5.8 met. r2's own C2-11 already re-graded the observation INFO.
- **K-6 · D2-06b (mono-small on the name = token abuse) — KILL SUSTAINED.** The names are CSS keywords and case-sensitive code identifiers; "value, code, or provenance" is the matrix row that fits; the :134-135 comment rejecting the uppercasing token is the canon's own render-over-name principle applied, not dodged.
- **K-7 · C-r1:C-3's sub-claim ("zero vitest files reference this component") — KILL SUSTAINED.** My read: `test/gradient-v4-consume.test.ts:11` imports `SPECIMEN_TILES` from the component's own catalogue; the file IS the unit gate. r1's BRE-alternation grep + mislabeled hit. The parent finding (gates RED; no CI browser step) survives intact at R-D.
- **K-8 · R3-8's MAGNITUDE (~17 ms/call, 110.88 ms/tick) — KILL SUSTAINED.** The corpus self-contradicts by ~10× (r2: 1.68 ms) before LC's bench lands 5–47× lower again with a 4.7× warm-up gap r3 claims not to exist. The row survives in KIND only (booked, with the no-inherited-numbers ledger law).
- **K-9 · N-2's consequence clause ("frozen `--vb-ratio` is wrong for exactly the cases the law exists for") — KILL SUSTAINED.** My whole-file read: the var's ONLY consumer is the dead `:deep` block — a value no rule consumes has no correctness to be wrong about.
- **K-10 · N-3's aside ("one of twelve test/ files reaching into demo/") — KILL SUSTAINED, both readers corrected** (ruled above: 11 by my recursive census; r4's 12 and LC's flat-glob 10 both fail enumeration).
- **K-11 · L-r1:L-1 as stated ("the published library runtime-depends on its demo's design system") — KILL SUSTAINED** (ruled above; the manifest-form row is booked → X-W9).

**Reader corrections (recorded against the re-readers, the L-14-corollary duty)**: DU filed TWO already-known findings as corpus-novel misses (the quart/quint drop — in three corpus documents; the `.specimen-strip` dead class — already adjudicated in this registry) and cited a nonexistent producer file (`scale-paper.css`) for a real token value; LC's flat test-glob undercounted the census its own finding class depends on. All four errors were caught by this seat's own instruments; none survives into a roster row.

## Superlatives (provenance preserved)

1. **The self-indicting comment as audit trail** (DU): :167-175 records the composited failure (~2.7:1), the cure, the precedent, and the benefit — D3-01 is only WRITABLE because the file diagnosed its own defect class and then under-applied the cure. A component that leaves a true audit trail against itself is worth more than one that leaves none.
2. **The `scrollIntoView` refusal** (both readers; ≡ sibling superlative 2): the strip's one-axis one-port reveal with the O-19 root cause named in place — attacked across two adjudications, held every time.
3. **The literal-mint identity law** (DU; ≡ sibling superlative 3): `easingCatalogue.ts:38-74` mirrors the producer byte-for-byte and survived the glass 7 major that broke two other couplings in the same directory (DU's independent re-verification of the mint against installed dist).
4. **The load-bearing PRM branch** (DU; ≡ sibling superlative 1): `behavior: prefersReducedMotion ? "auto" : "smooth"` is the ONLY guard that can reach a scrollBy options argument — the central clamps are structurally powerless there.
5. **The library is total and blameless at every broken seam** (LC; ≡ sibling superlative 5): `err`, never throw, at operations.ts:65/:90 — every crash in this corpus is a consumer demotion; stated plainly so no repair goes hunting in `src/`.
6. **The non-empty-tuple failure arm** (LC — verified by MY read of src/css/types.ts:25-27): `diagnostics: readonly [ParseIssue, ...ParseIssue[]]` makes `parsed.diagnostics[0].code` total by construction — a whole undefined-deref class killed at the type level; LC opened it as a suspected defect and had to close it.
7. **`applyCSS` swaps the whole model or nothing** (LC — my read of useGradientModel.ts:151-168): parse-first, atomic, explicit verdict, with the partial-apply failure it replaced recorded in place — the exact posture the R-A cure needs, shipping one file away.
8. **The generated self-alias vs its two hand-rolled twins** (LC): vite.config.ts derives its aliases from the exports map with the prevented bug documented — which is precisely what makes the tsconfig (banked) and vitest (M-EE-5) hand-copies legible as defects rather than taste.
9. **The measured-token comment** (DU; ≡ sibling superlative 9): the strip's 45%→65% opacity decision carries its measurement, the standard, and the state it must stay subordinate to — the method the text rungs never received (M-EE-1's exact irony).

## UNPROVEN-NEEDS-LIVE residue (→ the visual audit, task inputs)

- **U-1** — every composited contrast ratio for D3-01/D3-06 (head 3.86-3.90/3.73; eyebrow 2.47/2.79; rail 5.06-vs-4.62 — the internal inconsistency re-derived REAL by my arithmetic: the r3 record's two tables cannot both be right and its committed JSON holds rects, never luminances). Re-sample and COMMIT the luminances. D3-01's revival clause rides this row.
- **U-2** — whether `--ink-muted` alone clears 4.5:1 on the head over the live atmosphere (`resolveMutedInk` floors against the modeled resting rung, which should cover it — one live composite decides; the cure is not written CLOSED until it does).
- **U-3** — D3-07/D-04's numeric arms (152/207 clientW/scrollW at 320px; 387.7px in a 360px box at 1512; 70.6-84.6% catalogue hidden). Structural halves are certain (my reads); the numbers are single-capture.
- **U-4** — forced-colors: the spec-derived mechanism (box-shadow forced to none over `outline: none` at :228-231/:285-288) is sound, the rendered consequence unwitnessed; a real forced-colors arm is missing from the whole mega-tranche matrix (standing gap).
- **U-5** — D3-14 coarse-pointer hover persistence: HYPOTHESIS by its own seat; census source-true (my `@media (hover` = 1 demo hit); needs a real iOS arm; zero M-xx quarantine rows apply (my grep).
- **U-6** — D2-07's pressed/unpressed byte-identity of computed backgrounds (strip-adjacent; source-plausible from the stroke-only selection register).
- **U-7** — D3-13's zero-live-region claim: the committed instrument measured the whole DOCUMENT (`liveRegions: 2` — the producer's suppressed readout nodes), never the subtree; likely true, not evidenced. Re-scope the probe.
- **U-8** — the o17 "3 failed" run text: carry the selector + the `role:"img"` → 0 grep (now six independent runs), never the pasted run (it shared a dev server with a concurrent driver).
- **U-9** — ALL tick-level perf figures (C2-3's 869.3 ms CDP attribution; R3-8's 367.2 ms/tick; both readers' benches): mutually contradictory by 5–47×; the K-8 ledger law bars inheritance — any wave row wanting a number takes a fresh measurement.
- **U-10** — L-r2-3's DOM census (193 els/row; 18,541 path chars) and L-r3:L-9/L-10 (dev double-load; registry shadow): LC re-verified the substrate (real dir, sha-identical → benign today); the census itself is one capture.

## Closing verdict

**ADJUDICATED RED — a NEW operability BLOCKER lands atop an already-banked route-kill.** The interval head — the component's primary control — paints zero focus pixels by a mechanism this seat re-derived entirely from bytes (clip-rect coincidence × a zero-offset ring token pinned from dist), under a comment claiming the house register; the route-kill (three shipped tiles → `Result`-demoted throws in render computeds) stands cross-booked at the sibling apotheosis with its cure already re-homed. 24 axis rows are booked new (1 BLOCKER · 13 MAJOR · 9 MINOR · 1 INFO); 13 identities are cross-booked, never re-booked; 8 reader misses join — the sharpest two genuinely new to the whole ten-round corpus (`--ink-muted`, the house cure for the contrast row, unconsumed by the entire workbench; and the sampling budget that silently ERASES authored easing from the rendered gradient at ≥9 stops while the row's own preview swears otherwise). 11 kills sustained on this seat's own re-derivations; the one head-on reader conflict (D3-03) ruled by reading the law's source; four reader errors caught and recorded, including two false-novelty filings. Routing: **X-W6 carries the wave-shaped mass** (its :159 mechanism already programs the positional-identity cures; its file table must FIX the mislocated `GradientEasingEditor.vue` path at W6.md:59 and adopt the two NAMED-GAP files or ◆ rows degrade to NO-WAVE-OWNER) · **X-W9** the manifest reclassification (beside its own G31) · **X-W1** the oracle re-key (banked identity) · **two glass asks** ride the BH relay (preset-rail suppression; Input trailing slot) · **M-EE-5 goes NO-WAVE-OWNER** for the next formation boundary. Ten residue rows go to the visual audit. Dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this file.

— end of apotheosis. The ten challenge documents and both re-reader returns remain immutable beside this file per E-1/E-3.
