# MigratePalettesDialog — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.

**Seat**: incredulity/apotheosis · **Date**: 2026-08-04 · **HEAD**: `c654824e` · branch `tranche-u`.
**Corpus**: 43 accusations across three axes (C-1..C-15 implementation · D-1..D-15 design · L-1..L-13 library) against `demo/palettes/browser/dialog/MigratePalettesDialog.vue` (97 lines) and its behaviour composable `demo/palettes/useSlugMigration.ts` (126 lines), challenged by two hostile re-readers: **design-and-usage** (reader-1, Fable-side return) and **library-and-code** (reader-2, `MigratePalettesDialog` return).
**Method**: every reader disagreement (the L-2 mechanism split, five severity splits, both readers' kill registers, all eleven missed rows) was re-derived by this seat against the tree before adoption. This is an adjudication record — dispositions are **ADJUDICATED, never VERIFIED**; no gate has run and nothing here stamps a wave.
**This seat's own instruments (all static — probe parsimony; zero browser probes needed, every ruling decidable from source + installed dist)**: full reads of useSlugMigration.ts (whole), MigratePalettesDialog.vue (whole), SlugEditLayer.vue (whole), PaletteSlugBar.vue:180-243; viewSchema.ts:34-49 (14-member ViewId, no `saved`), router/index.ts:21-38 (14 named routes, path-based catch-all), useViewManager.ts:40-95 (getter-only `computed<ViewId>` + `as unknown as Ref<ViewId>` + `router.push({name:id})`), usePalettePorts.ts:79-89 (the launder pair, sole caller); usePaletteStore.ts:1-60, usePaletteActions.ts:40-58, useSession.ts:20-45, useUserAuth.ts:60-130; api db.ts:41 (unique slug index), palette crud.ts:132-137 (11000→ConflictError), session routes (POST / → `registerSession`), demo palettes/api/palettes.ts:60-85; barrels demo/ui/dialog + demo/ui/button + browser/index.ts + browser/dialog/index.ts; installed glass-ui 7.0.0: Button.vue.d.ts (no `variant`; `loading?`), axes.d.ts:16 ("NEVER a `variant` member"; SURFACES glass|veil|opaque), DialogContent.vue.d.ts (8 axes, no width axis), DialogFooter.vue.d.ts (exists), dialog chunk base class (`w-full max-w-lg`, DialogHeader `text-center sm:text-left`, DialogFooter `flex flex-col-reverse sm:flex-row sm:justify-end sm:gap-x-2`), button styles.css whole `.button` rule inside `@layer components` (justify-content:center · gap 0.375rem·scale · border-radius var(--radius-pill) · color var(--foreground) · cursor:pointer · `[data-emphasis="quiet"]` → `color: var(--muted-foreground)` · `[data-emphasis="primary"]` → 650); eslint.config.js:225-305 (dead `demo/@` globs beside live `demo/color-picker/**` globs; unspellable `@components/...` ban), package.json:20 (`"sideEffects": false`), o10d spec:448-471 (dead path; title-line-only assertions), all workflows (`grep playwright|test:e2e` → 0), App.vue:145-180 (global mount + stale sideEffects comment), foundation.css:85-105/214-222 (`@source "../../color-picker/**"` resolves to a nonexistent root dir; Fraunces = display rungs only), VISUAL-CONSTITUTION.md:19/33/66-78/82-83/100-101/115/144/186 all verbatim at the stated lines; motion-quarantine.md `grep -i migrate` → **0 rows** (no M-xx id constrains this component); counts re-run as third witness: `variant="` = **103 / 36 files** · `ls demo/ui` = **19** · PaletteSlugBar = **243** lines / SlugEditLayer = **119** · `aria-hidden` in demo `*.vue` = **79** · top-level browser-barrel importers = **0** · `MigrateChoice` refs = 3, all inside the SFC · lucide 1.17.0 defaultAttributes carry no aria attrs; live raw-`.vue` boundary violation found by this seat: `CurrentPaletteEditor.vue:193` imports `../status/ApiOfflineChip.vue` raw — the exact reach the dead G-DEMO-3b rule exists to forbid, shipping today.

## Scoreboard

**30 defect rows CONFIRMED (4 BLOCKER · 15 MAJOR · 7 MINOR · 4 INFO) · 1 RECORDED-CLEAN (motion) · 8 claims KILLED · 11 reader-missed rows ADOPTED (folded into the roster) · 10 NEEDS-LIVE residues → visual-audit inputs.**

The component itself is a disciplined 97-line pure view; **every behavioural defect lives in the composable, the auth seams, and the dead apparatus around it**. The strongest single defect: **A-2/C-2** — the primary action can publish the user's entire local library, publicly and permanently, under a throwaway identity discarded in the same gesture, while `ensureUser` sits injected and uncalled.

## Ruled disagreements (the adjudicator's actual work)

| dispute | reader-1 | reader-2 | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **L-2 mechanism** — where did the S.W2 typed-`ApiProblem` repair land? | CONFIRMED with the axis's phrasing: "the repair landed in the unmounted twin" | KILLED that phrasing: repair is LIVE at useSlugMigration.ts:78-88; the SINK is dead | **Reader-2 wins.** My reads: useSlugMigration.ts:78-87 carries the typed branch + the S.W2 comment (live composable, executed on every zero-palette switch); PaletteSlugBar.vue:216-221 (dead) and SlugEditLayer.vue:57-62 (live) BOTH still run `msg.includes("409")`. There is nothing to port back from the twin — the typed mapping only needs a mounted sink. **Citation-inheritance event recorded**: reader-1's confirmed claim line reproduces the axis's killed sentence verbatim-in-substance without re-derivation — the same fleet failure the ColorSpaceSelector DISSENT §1 documents. Reader-1's row survives; its mechanism cell does not. |
| **C-2 severity** | BLOCKER | MAJOR (precondition contamination noted) | **BLOCKER.** The precondition (local palettes + no persisted slug) is the natural first-run state of every visitor — savedPalettes need no account (usePaletteStore is a flat localStorage blob), and `useSession` step 3 mints-and-discards (`persistToken(res.token,false)`; `res.userSlug` dropped) against a `POST /sessions` that is a REGISTRATION (routes.ts:32). Outcome = permanent public orphaning of the whole library. Reachability + irreversibility = blocker-weight regardless of the contaminated probe run. |
| **D-2 severity** | MAJOR | BLOCKER | **BLOCKER, defect only — cure severed.** The premise is false for every user at every identity boundary (store is device-scoped, verified; login/regenerate touch only slug+token, verified), and the `discard` arm is byte-poorer than the no-dialog path (my diff of :61-70 vs :74-88: same two statements minus the typed 409/404/429 routing). Per the cure-severance law (ColorSpaceSelector addendum §2), the D-axis cure "retire the dialog" inherits no weight from the defect: one real question DOES survive the false premise — publish-before (old account) vs publish-after (new account) — so retire-vs-repair is an **OWNER-DECISION**, booked below. |
| **L-2/C-14 severity** | MAJOR | BLOCKER | **BLOCKER.** Not one dead file — a three-implementation error channel with zero working members (dead twin, permanently-null sink, un-awaited live caller whose catch cannot fire) on the identity-mutation path, compounded by A-1's misclassification limb. |
| **D-1 severity** | MAJOR | BLOCKER | **MAJOR, dissent preserved.** The dead-axis family is mechanical (103/36, third-witnessed); the instance's rendered harm (three identical pills on an irreversible choice) aggravates A-4/A-12 rather than shipping an independent destructive outcome. Reader-2's BLOCKER followed the D-axis's own severity key ("no hierarchy at all"), which is a per-axis key, not the roster's. |
| **L-6/C-12/D-12 severity** | MINOR | MAJOR | **MINOR.** Three hand-copies of one union with no compiler edge is a drift hazard, not a live break; the cure is one type-home line in the flow repair. |
| **D-3 grade after the killed headline** | MAJOR | MAJOR (explicit downgrade from BLOCKER) | **MAJOR, unanimous once the contaminated telemetry is severed.** The structural core (surface destroyed before work; no in-flight/error/success channel anywhere) stands on source alone. |

## Adjudicated defect roster

Anchors are HEAD `c654824e`; re-resolve before any cure (D-19). Dispositions: a tranche-X wave id where one owns the surface; **GLASS-RELAY** = producer-owned, rides the standing glass-ui BH relay (owner edict); **OWNER-DECISION** = product ruling required at the formation boundary; **NO-WAVE-OWNER** = no X wave names this surface — consumed at the next formation boundary (manifest below).

### BLOCKER

- **A-1 (L-1) — `setActiveTab("saved")` throws; the ViewId brand is widened to `string` and cast back with `as ViewId`.** Proof (mine): :55/:69/:76 call it; ViewId (14 members) and the route table (14 names; catch-all is path-based so a NAME push cannot fall through) contain no `saved`; `switchView` guard can never short-circuit; the launder pair usePalettePorts.ts:87-88 vs the widened contract :16-17. **Sharpened composite (adopting reader-1's kill + reader-2's missed row)**: the ONLY unhandled instance is the admin branch (:55 — outside any try, in an async fn the live caller doesn't await → unhandled rejection); in switch-mode EVERY dialog response throws at :69 AFTER `userLogin` succeeded (identity changed, navigation never happens, user told nothing, warn-swallowed); on the zero-palette path the :76 throw lands in the :77 catch as a NON-ApiProblem → `status` undefined → the else pipes the router's internal message into `setError` as a fake login failure. **Order law (adopted, load-bearing)**: A-1 must land BEFORE or WITH A-3's sink repair — mounting the sink first converts a silent bug into a user-visible lie on a successful login. Cure: `setActiveTab: (id: ViewId) => void`, ViewId imported from the shell that owns it; delete both casts; the author must then pick the real destination (`"palettes"`). **NO-WAVE-OWNER** (X-W5 rewrites viewSchema.ts — re-resolve anchors; the fix itself is nobody's named scope).
- **A-2 (C-2) — the primary action can publish the whole library to a ghost identity; `ensureUser` (deps:15) is injected and never called.** Proof (mine): publishAllLocal calls only `session.ensureSession()` (:34); useSession step 3 mints a session and discards `res.userSlug`; `POST /sessions` = `registerSession`; regenerate then deletes that session and registers a different user (useUserAuth.ts:112-127); the canonical verb one directory away does it right (`await ensureUser(); await session.ensureSession();`, usePaletteActions.ts:40-46). **X-W7** (scope 8 — "every palette mutation exactly one owner"; discharges together with A-9's canonical-verb adoption).
- **A-3 (L-2 + C-14, one identity) — the flow's error channel has three implementations and zero working ones.** Proof (mine): PaletteSlugBar.vue (243 lines) rendered by no template; `slugBarRef` permanently null so all four authored strings (:84-87) are no-ops; the live caller SlugEditLayer.vue:54 doesn't await, so its catch (:57-62) — which re-runs the exact substring idiom the S.W2 comment documents as broken — can never observe the rejection. Mechanism as RULED above (repair live, sink dead; nothing to port). Compounding (reader-2 missed, verified): the top-level browser barrel has ZERO importers and its :44 line is the only non-type ref to PaletteSlugBar — `useSlugMigration.ts:6` is `import type` (erased) — so the dead twin is not even reachable in the bundle. **NO-WAVE-OWNER** (delete `browser/slug/` + the barrel lines; surface a plain `slugError` ref SlugEditLayer binds; sequence per A-1's order law).
- **A-4 (D-2 + C-6 + L-11, one identity) — the dialog's premise is false, its "discard" arm is strictly worse than no dialog, and its two publish verbs hide the only thing that differs.** Proof (mine): STORAGE_KEY is a flat device blob with no userSlug coupling; login/regenerate write slug+token only; the discard arm (:61-70) = the no-dialog arm (:74-88) minus the typed routing; `transfer`≡`publish` modulo ordering around `userLogin`, and neither label names its destination account; the `"discard"` literal discards nothing. VISUAL-CONSTITUTION:186 (Device Drafts are a first-class owner state) and :101 verbatim. **Defect CONFIRMED BLOCKER · cure OWNER-DECISION**: retire the dialog and re-home bulk publish as a Library command (D-axis), or repair it into an honest publish-offer whose labels name the destination account (C/L axes). The surviving true question — old-account vs new-account publish — must be answered by whichever form survives.

### MAJOR

- **A-5 (D-1 + C-10, one identity) — `variant` is a retired vocabulary on glass-ui 7 Button; the authored hierarchy does not exist; the hand-rolled `text-muted-foreground` fallback is defeated.** Proof (mine): ButtonProps has no `variant`; axes.d.ts:16 names the retirement; the producer already ships the intended treatment (`[data-emphasis="quiet"]` → `color: var(--muted-foreground)`; `primary` → weight 650); family = 103/36 third-witnessed; vue-tsc exit 0 attested by two independent runs (axis + reader-2). Defeat MECHANISM unresolved statically — see NEEDS-LIVE §1. Cure: speak `emphasis="primary|secondary|quiet"`. **Family → X-W8** (CC-081's cure home — **scope corrected**: CC-081 says "2 consumers"; the census is 103 sites/36 files; per the scope law the row books where the count lives). Instance dies with the flow repair. Grade dissent recorded.
- **A-6 (C-1) — Escape / scrim / producer × silently abandon a committed action; dismissal is a hole in the response domain; `pendingMigrateAction` is left armed.** Proof (mine): `respond` emitted only from the three @click handlers; no `false` handler on the model; MigrateChoice has no cancel; :107-108 nulls the latch only inside onMigrateRespond. **NO-WAVE-OWNER** (flow repair: cancel as a first-class answer, every close path through one emit).
- **A-7 (D-3 structural + L-3) — the surface is destroyed before the operation begins; the journey has no loading/error/success state; every failure terminates at console.warn.** Proof (mine): :69-72 `open.value = false` precedes the emit; catches at useSlugMigration :42 (bare), :47, :113; glass-ui `loading?: boolean` unused; VISUAL-CONSTITUTION:83/:101. Measured-headline limb KILLED (below); structural core unanimous. **Swallow census → X-W7 scope 3** (the 31 `catch → console.warn` table); lifecycle (dialog outlives its operation) **NO-WAVE-OWNER**.
- **A-8 (C-3) — the publish transitions no state: `isLocal` never flips, the gate count never falls, the dialog re-arms forever; re-publish collides on a globally-unique slug and the 409 is swallowed.** Proof (mine): savedPalettes predicate `p.isLocal && p.id != null`; createAndSavePalette is a bare POST (both callers discard the returned server row — reader-2's missed limb folded here, with the softening that its docstring honestly declares the server-create verb, renamed at W5-13·F-2); db.ts:41 unique index across ALL users; crud.ts 11000→ConflictError; client `catch {}`. **X-W7 scope 8.**
- **A-9 (C-4) — dual publish path: publishAllLocal is a second, weaker implementation of the canonical `onPublish`, and the dialog's primary action runs the weaker one.** Proof (mine): side-by-side read — identity establishment, separated failure classes, typed result vs void + `catch {}`. The edict-2 violation in this flow. **X-W7 scope 8** (delete publishAllLocal; map the canonical verb; A-2 discharges with it).
- **A-10 (C-5 + D-13, one identity) — the only automated guard on this component reads a path deleted at `a61094e3`; readFileSync throws before any assertion; the e2e suite is in no CI workflow.** Proof (mine): o10d:448-461 reads `demo/@/...` (dir absent); surviving assertions grep one title line for `/font-display/`+`/font-medium/`; `grep playwright|test:e2e` across workflows → 0. **X-W1** (scope 3 hard e2e jobs + scope 5 no-CI-orphaned-project oracle; repoint or delete o10d there).
- **A-11 (D-4 + reader-1 missed rung, one identity) — type jurisdiction twice over: Fraunces on help prose and all three control labels (family), AND the description sits on the control rung (`text-small`) where the closed matrix demands `text-prose`.** Proof (mine): :8 `text-small font-display`, :16/:25/:33 `font-display`; matrix verbatim at VISUAL-CONSTITUTION:66-78 (closed, sole P019 exception); foundation.css states the three-voice law. The title's `font-display` (:7) is separately licensed (T.W4-6 comment + the o10d pin — itself dead per A-10). **Cure as corrected by reader-1's kill**: description → `text-prose` (family AND rung); actions drop `font-display` and take the control register. **NO-WAVE-OWNER** (file-local; rides the flow repair).
- **A-12 (D-5 + reader-1 missed autofocus + missed no-subject, one identity) — the action set has no owner and no Cancel; the top, first-focused, most-primary-sounding option files the library under the identity being abandoned; initial focus lands on that irreversible command in the same tick an Enter-key form submit opens the dialog; and the surface never shows its subject (N alone — no names, no swatches, no exclusion).** Proof (mine): :13-39 (three stacked Buttons, no cancel); :61-68 ordering vs labels; SlugEditLayer form `@submit.prevent` → `pm.onSlugSwitch` → `showMigrateDialog.value = true` synchronously — same-tick mount is static-certain; both probe seats measured focus on the publish command; the SFC renders title/description/count and nothing else; `openAutoFocus` exposed and unbound; SkipForward is a media-transport glyph. Enter-key-repeat limb → NEEDS-LIVE. **OWNER-DECISION with A-4** (the action-set design dissolves or reforms with the dialog's fate).
- **A-13 (D-6 + L-8/C-11 + L-9 + reader-1 missed DialogFooter, one identity) — every consumer class is dead against or fighting a deciding root, while the producer axes and the producer's own action-set primitive sit unused.** Proof (mine): the `.button` rule already carries cursor/pill-radius/center/6px-gap/foreground; `rounded-dialog` restated (sole site in the demo); `rounded-full` = Tailwind constant discarding `--radius-pill`; DialogContent's 8 axes all unpassed; **DialogFooter is exported by the very barrel this file imports from, exists in dist (`flex flex-col-reverse sm:flex-row sm:justify-end sm:gap-x-2`), and the action set is a bespoke `<div class="flex flex-col gap-2">`** — the bespoke div is what forces the `justify-start`/`gap-2` recipe downstream; if a vertical action stack is right, it is a DialogFooter variant at the root (owner edict 4). **NO-WAVE-OWNER** (flow repair). The `max-w-sm` row is REMOVED from this identity (killed, below).
- **A-14 (D-7 + C-8 structural) — focus restoration cannot succeed by construction; `closeAutoFocus` unbound; the opener unmounts in the tick the dialog opens.** Proof (mine): SlugEditLayer :54-56 (un-awaited call, then `slugEditMode.value = false` same handler) and :107 (`@click="slugEditMode = false; pm.onRegenerateSlug()"`); dropdown openers close with their menu; VISUAL-CONSTITUTION:115 names the required fallback; nothing implements it. Focus→BODY measurements NEEDS-LIVE (C's own contamination flag honored). **NO-WAVE-OWNER** (flow repair; X-W4 touches SlugEditLayer for target geometry — adjacent, not this defect; land beside it).
- **A-15 (D-9 + C-9 structural) — the modal does not occlude: a ~70%-opaque panel over arbitrary page state makes the decision surface's rendered contrast unboundable; `surface="opaque"` is one unused prop away.** Proof (mine): no `surface` passed; SURFACES includes `opaque`; VISUAL-CONSTITUTION:19/:82. Transmission arithmetic and the 3.63:1 figure NEEDS-LIVE. **NO-WAVE-OWNER** (flow repair).
- **A-16 (L-4) — `demo/ui/` is a 19-directory pure re-export shim creating a live dual entrypoint; two of this component's three imports ride it while siblings in the same feature import the published subpath.** Proof (mine): both barrels read (naked root-barrel re-exports); PalettesPane/AdminUsersPanel use `@mkbabb/glass-ui/dialog`; count = 19 (killed 20, below) — **and X-W8's own census independently says 19, a convergence receipt**. Reader-2 adjacent finds folded: `demo/ui/label` and `demo/ui/switch` have zero consumers. **X-W8 scope 2** (delete the 19 barrels; re-point 92 imports / 48 files).
- **A-17 (L-5) — the G-DEMO-3b boundary both barrels cite as "enforced standing" is dead: its globs name the deleted `demo/@` tree and its banned specifier an alias W43·RF-15 retired.** Proof (mine): eslint.config.js:232-247 + the fully-dead composables object :275-300; `ls demo/@` absent; **live counter-witness found by this seat: `CurrentPaletteEditor.vue:193` imports a raw `.vue` across the seam today, uncaught**. **X-W8 scope 3** (delete the six dead globs; add the ban that keeps the barrels dead).
- **A-18 (L-7 + reader-2 missed dead-seam, one identity) — wrong home, dead seam: the dialog lives in `browser/` which never uses it, and the "TOP-LEVEL SEAM" barrel that justifies the apparatus has zero importers anywhere.** Proof (mine): every browser/ reference is a barrel line or comment; real relations = App.vue global mount (via the `dialog/` sub-barrel), useSlugMigration, the shell dock triggers; `grep from ".../browser"` → 0. Softening noted: the barrel's own comment licenses sub-barrel reaches "(or a sub-barrel it re-exports)" — but with ALL consumers on sub-barrels the top seam is 46 lines of dead code whose one live effect is keeping PaletteSlugBar nominally exported (compounds A-3). The dialog belongs to the identity flow. **NO-WAVE-OWNER** (rehome with the flow repair; barrel deletion is an X-W8-shaped rider the formation should book beside W8's graph subtraction).
- **A-19 (reader-2 missed, adopted) — `SlugEditLayer.onSlugSubmit` is async with zero awaits: `slugSwitching` flips true→false in one synchronous tick, so the authored Loader2 spinner is unreachable, the disable guard's second clause never engages, and N submits can run N concurrent logins.** Proof (mine): whole-function read :39-66 — no suspension point; `finally` runs before any render flush; `defineExpose` publishes a permanently-false ref. The layer also closes itself (:56) rather than awaiting, so re-open-and-resubmit is free during flight. **NO-WAVE-OWNER** (land beside X-W4's SlugEditLayer target-geometry edits so the file is touched once).

### MINOR

- **A-20 (C-7)** — `transfer` unimplemented in regenerate mode; a template `v-if` is the only guard between the mode-independent emitted type and a silent no-op (:94-99 has no transfer branch). Flow repair: type the choice per mode. **NO-WAVE-OWNER.**
- **A-21 (D-10 + D-8#2, one identity)** — the sole cancel affordance is the producer's 16×16 close (below WCAG 2.5.8's 24×24), and the close sits inside the title's inline box (`titleClosePx = −16`; ink collision at 320px). Producer geometry (absolute close + no header inline-end reserve, my dist read), consumer-elected as sole exit by taking `showClose`'s default. **GLASS-RELAY** (owner edict: BH relay) + the consumer's showClose decision rides the flow repair. Measurements NEEDS-LIVE.
- **A-22 (D-11 + C-6-limb + L-10)** — dead `title` computed: both ternary arms byte-identical (:74-78). Flow repair. **NO-WAVE-OWNER.**
- **A-23 (D-12 + C-12 + L-6, one identity)** — `MigrateChoice` reachable through neither barrel; the union hand-copied at :29 and :106; three declarations, no compiler edge. My grep: 3 refs, all in-SFC (the C axis's "four lines" is off by one — recorded). Cure: one type home in the flow's types module. **NO-WAVE-OWNER.** Grade dissent (reader-2: MAJOR) preserved.
- **A-24 (reader-1 missed, adopted)** — all three decorative lucide icons reach the accessibility tree: no `aria-hidden`, lucide adds none (defaultAttributes read), each redundant to its adjacent label; the house pattern is explicit at 79 sites. Flow repair. **NO-WAVE-OWNER.**
- **A-25 (reader-2 missed, adopted)** — the `activeTab` dependency is dead and its value is a getter-only computed double-laundered into `Ref<string>` (`as unknown as Ref<ViewId>` then `as Ref<string>`): the sole caller always supplies `setActiveTab`, so the writing else-branch is unreachable — a required dep with no live reader whose only possible act is a write to a readonly computed. Flow repair (delete the dep). **NO-WAVE-OWNER.**
- **A-26 (C-15 + L-12)** — publishAllLocal serialises N POSTs behind an already-closed dialog and swallows every per-palette failure; no tally returned. **X-W7 scope 3** (with A-7's census) — the tally becomes the visible result scope 8 demands.

### INFO

- **A-27 (C-13)** — App.vue:170-175 and dialog/index.ts:2-5 assert a `./demo/**` sideEffects mark that package.json:20 (`"sideEffects": false`) contradicts — stale scaffolding that will teach the next reader to preserve a solved workaround. Corrected by whichever wave touches these files first. **NO-WAVE-OWNER.**
- **A-28 (D-14)** — no `count === 0` guard at the component though it is exported through two public barrels; pluralization correct at 0/1/n. **NO-WAVE-OWNER** (flow repair).
- **A-29 (D-15) — RECORDED-CLEAN, not a defect.** Zero animation/transition/keyframe declarations in the SFC; the residual 150ms opacity fade is producer-owned (glass-reveal), PRM-guarded; **motion-quarantine.md carries no row for this component (my grep — no M-xx id constrains, no FALSE row is carried)**.
- **A-30 (L-13)** — zero visual-matrix coverage by construction (double-gated on `savedPalettes.length > 0` + an interaction; no route). **X-W1** (the visual-golden harness needs a seeded-storage arm, or every gated modal stays permanently unwitnessed — the NEEDS-LIVE list below is that arm's first worklist).
- **A-31 (reader-1 missed, adopted)** — foundation.css:91's `@source "../../color-picker/**/*.{vue,ts,html}"` resolves OUTSIDE the tree (`<root>/color-picker/` does not exist — my `ls`); harmless only because :92 covers `demo/**`; the header comment claims the globs make the scan "EXPLICIT". One-line fix whenever the file is touched. **NO-WAVE-OWNER.**

**NO-WAVE-OWNER manifest (one identity-flow repair unit, for the next formation boundary)**: A-1 + A-3 (order law binding: A-1 before/with A-3's sink) + A-4/A-12's owner ruling + A-6, A-7-lifecycle, A-11, A-13, A-14, A-15, A-18-rehome, A-19, A-20, A-22..A-25, A-27, A-28, A-31. These cohere as ONE unit — the identity flow gets one home, one typed boundary, one error surface, one publish verb — and should be authored as a single wave rather than scattered.

## Killed-claims register

1. **D-3's measured headline** ("nothing published, account slug destroyed, user told nothing" as a measured indictment) — **KILLED, both readers, confirmed by this seat**: telemetry3 samples[0] shows `slug: null` at t=101ms with the dialog still open (nothing to destroy); `network: []` is the probe stack's own CORS failure (Idempotency-Key blocked — the header createAndSavePalette really sends, my read), not design-suppressed requests; and useUserAuth.ts:116-118's comment shows the in-memory slugRef is deliberately preserved. The durable-storage non-atomicity (SLUG_KEY removed before `createSession()`) is REAL and remains flagged to the auth seat inside A-7's cure context; the dramatic narrative is dead.
2. **D-8#1 / D-6's `max-w-sm` row** ("max-w-sm causes the 320px full-bleed") — **KILLED, both readers, confirmed by this seat's dist read**: the producer base class is `w-full max-w-lg` with no inline inset (placement.css widths cover sheets only), so the producer default full-bleeds at every viewport ≤512px; the consumer's `max-w-sm` narrows the failure window to ≤384px — a mitigation, not a cause. DialogContentProps exposes **no width axis**, so "producer-owned width" is not a reachable cure — the defect and the cure both belong to glass-ui → folded into **A-21's GLASS-RELAY**.
3. **D-8#3** (centered-header/left-actions fork as a consumer defect) — **KILLED (reader-1), confirmed**: DialogHeader's `text-center sm:text-left` is producer-authored; the SFC passes no class to DialogHeader; the consumer's real contribution (`justify-start`) is already A-13's. Double-count removed.
4. **D-4's cure** ("drop `font-display`, keep `text-small`") — **KILLED (reader-1), confirmed**: `text-small` is itself the control rung; the prescribed cure preserves the exact size-collapse the finding diagnoses. Corrected cure lives in A-11.
5. **L-1's roll-up limb** ("a live unhandled page error on the primary slug-switch path") — **KILLED-AS-STATED (reader-1), confirmed**: the probe's 3-segment slug fails `looksLikeSlug` (4 segments required) → the ADMIN branch (:55) — the only site outside a try; both dialog-driven sites are caught-and-swallowed. Replaced by A-1's sharper composite, which is worse than the original claim.
6. **L-2's mechanism sentence** ("the repair went to the dead twin") — **KILLED (reader-2), confirmed and RULED above**; reader-1's propagation of the dead sentence recorded as a citation-inheritance event.
7. **L-4's count** ("20-directory shim", ×4 in the axis + the lattice's −20 arithmetic) — **KILLED, both readers, third-witnessed**: 19. X-W8's independent census also says 19.
8. **C-14's line count** (244) — **KILLED (reader-2)**: 243, my `wc -l`. Recorded only because two axes quantify the same artifact and a formation reading them side-by-side would see two numbers.

## Missed-rows ledger (11, all adopted; verification state per row)

Reader-1: DialogFooter unused (→A-13, dist-verified) · description on the control rung (→A-11, matrix-verified) · autofocus-on-irreversible + Enter-submit adjacency (→A-12, structural half static-certain; key-repeat limb NEEDS-LIVE) · decision-surface-shows-no-subject (→A-12) · icons in the AT tree (→A-24, 79-site pattern verified) · dead @source glob (→A-31, `ls`-verified). Reader-2: dead top-level barrel, zero importers (→A-18/A-3, grep-verified, with the "(or a sub-barrel)" softening this seat added) · the :76-throw misclassified as login failure + the A-1/A-3 cure-order dependence (→A-1, source-verified — the sharpest single addition in either return) · SlugEditLayer async-no-await dead spinner (→A-19, source-verified) · dead `activeTab` dep + double-cast (→A-25, source-verified) · createAndSavePalette adopts no server row (→A-8, verified, with the docstring softening).

## Superlatives (provenance preserved)

- **COMPONENT — the decision/action boundary holds under 43 findings** (reader-1): the 97-line SFC is a pure view — no fetch, storage, listener, timer, or colour math; every behavioural defect lives in the composable. Re-verified by this seat's full read.
- **COMPONENT — delegated producer semantics work for free** (reader-1; C-measured): DialogHeader/Title/Description used as intended → `aria-labelledby`/`aria-describedby` present and resolving.
- **COMPONENT — `shrink-0` on all three icons; correct 0/1/n pluralization; the `v-if` at :23 doing real safety work** (reader-1; all three re-verified in source — the v-if is the only reason A-20's hole has never fired).
- **TREE — viewSchema.ts is the standard A-1 is measured against, not a participant** (reader-2): closed union, `Record<ViewId, PaneConfig>` totality, `isViewId` as a predicate not a cast. Union re-read by this seat.
- **TREE — savedPalettes' user-defined type predicate; onPublish's two-try typed-result shape; useSession's self-documenting three-tier resolution; usePalettePorts' RF-15 dissolution** (reader-2; the first three re-verified by this seat). The correct publish implementation sat one directory from the defective one the whole time.
- **PRODUCER — glass-ui documented A-5 before it was found** (reader-2): axes.d.ts:16's "NEVER a `variant` member". Re-read by this seat.
- **CORPUS — challenge-D's state-coverage table** (reader-1): nineteen states dispositioned against evidence, with the two unreachable ones refused rather than faked. **CORPUS — challenge-C's negative-results section** (both readers): thirteen named repo hazards attacked and cleared, its compiler-guard citation byte-exact, its own contaminated measurement self-flagged. **CORPUS — challenge-L's self-refutation block** (both readers): two hypotheses raised against its own MAJOR, tested, recorded REFUTED, and the finding self-demoted to its honest weight — the discipline this whole review exists to check for, unprompted.

## UNPROVEN — NEEDS-LIVE residue (the seeded-storage visual-audit worklist, A-30)

1. **The `text-muted-foreground` defeat mechanism** (A-5). Outcome doubly measured (two seats, two engines: ghost label = `rgb(0,0,0)` = primary); mechanism statically IRREPRODUCIBLE — `.button{color:var(--foreground)}` sits in `@layer components` and the utility in later `utilities`, which predicts the utility WINS; this seat also found no colouring rule on the capsule/wash classes. The real cascade winner is unnamed. The cure (`emphasis="quiet"`) is correct regardless.
2. **C-9's 3.63:1 contrast figure** — throwaway-decoder-derived, not re-derivable; the structural limb rides A-15.
3. **D-9's ~15% composited-transmission arithmetic and D-8's geometry table** (x/w/sideMargin/titleClosePx=−16) — mechanisms confirmed from source/dist; numbers single-probe.
4. **The Enter key-repeat limb of A-12** — same-tick mount + autofocus static-certain; whether a held Enter activates the newly-focused publish command is engine/timing-dependent.
5. **`variant="default"` rendered into the DOM** as a fallthrough attr — consistent with Vue semantics, harmless, unconfirmed statically.
6. **L-8's doubled `rounded-dialog` in the live className** (twMerge-collapse vs concat) — both sources confirmed statically; the rendered string is live-only.
7. **L-4's dev-closure measurements** (17 vs 56 modules; +40.6%) — structural dual-entrypoint confirmed; numbers need a running dev server.
8. **L-1's live stack-trace artifact** — the static chain is conclusive on its own; the artifact is not re-provable from this seat.
9. **Focus→BODY measurements** (A-14) — structural cause static-certain; the activeElement readings are live-only and C's instance is self-flagged contaminated.
10. **The 16×16 close-target measurement** (A-21) — producer geometry, two-seat-measured, live-only; `showClose?: boolean` default-true attested by the chunk and by the close button's measured presence in every probe.

## Verdict

**DEFECTIVE — 4 BLOCKER · 15 MAJOR · 7 MINOR · 4 INFO (30 confirmed rows, deduped identities) · 8 claims killed · 11 missed rows adopted.** A 97-line pure view holding its boundary perfectly, commissioned by a flow that is wrong at every layer that matters: a navigation that throws on success (A-1), a publish that orphans the library under a discarded identity (A-2), an error channel with three implementations and zero working ones (A-3), and a premise that is false for every user who sees it (A-4). Routing: A-2/A-8/A-9/A-26 + the swallow census → **X-W7**; A-5's family + A-16/A-17 → **X-W8**; A-10/A-30 → **X-W1**; A-21 + the width/inset geometry → **GLASS-RELAY**; the dialog's fate → **OWNER-DECISION**; the coherent remainder → **NO-WAVE-OWNER**, one identity-flow repair unit, order law A-1-before-A-3 binding. Dispositions are ADJUDICATED, not VERIFIED; the challenger records, both re-reader returns, and every probe artifact remain immutable beside this file per E-1/E-3.

— end of apotheosis.
