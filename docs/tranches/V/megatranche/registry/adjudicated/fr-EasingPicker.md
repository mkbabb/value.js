claude-fable-5 (served model id)

# fr-EasingPicker — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `fourier-analysis/web/src/components/visualization/EasingPicker.vue` (98 lines, `wc -l` by this seat) + `EasingCurvePreview.vue` (41) — the six-chip analytic-easing selector mounted solely inside AnimationControls' `DropdownMenuContent`.
**Substrate**: fourier `cd26c65` (2026-07-03) + dirty WT (re-pinned by this seat) · installed glass-ui **4.0.0** (peer map read directly) · installed value.js **0.13.0** (probed by node) · reka-ui 2.9.x (dist reads) · value.js source-of-record **4.0.0** (this repo, my own exports/grep/source reads) · producer glass-ui working tree at **8.0.0**, commit `1bc09dde` 2026-08-08 *"refactor(easing): land BK #85 W-EASING — EasingCurve extracted, the frame made constant, the staircase bound"* (git, this seat) — one full major past the 7.0.0 the D axis audited and four days newer than the newest challenge doc.
**Corpus**: three axis challenges (`audit/fourier-components/EasingPicker/challenge-{D-design 23+4, L-library 17+5, C-consumption 18+4}.md`, all read whole by this seat) × two hostile re-reader returns — **reader-DU** (design-and-usage) and **reader-LC** (library-and-code), delivered as separate structured returns, so this record DOES carry a reader-vs-reader ruled-disagreements table.
**Method (M-23/L-18)**: every verdict-level disagreement between the two readers was re-derived against the tree by this seat with its own instruments before either was adopted; every kill was attacked; suspiciously-agreed cells (the WCAG arithmetic, the Tab-swallow mechanism, the producer-arrival facts) were independently re-measured, not spot-trusted. One citation-inheritance event between reader-LC and the C challenger was detected and is recorded in DISSENT. Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node, read-only; zero browser — probe parsimony; product source untouched; this file is the sole write)**: full reads of EasingPicker.vue (98), EasingCurvePreview.vue (41), lib/easings.ts (127, whole), stores/animation.ts head+return, AnimationControls.vue imports/menu/CSS regions, VisualizationView.vue:48-66, useWorkspaceLoader.ts:50-60, workspace.ts:340-360 + grep census, api/models/shared.py:60-75 + visualization.py field grep (`animation_settings` on :129/:189/:244/:283), lib/colors.ts:20-55+:91-95, style.css:1-50/:110-130, MorphPhaseConfig.vue:40-57/:195-205, BasisCanvas.vue:410-424, e2e/visualization-ux.spec.ts:183-195; reka dist reads — **Primitive.js whole** (`inheritAttrs: false` at :12; manual `h(props.as, attrs, …)` spread at :26 — the C S-2 decider), MenuContentImpl.js:180-215 (`handleMountAutoFocus` focuses content; `arrowKeyOptions: "vertical"` at :196; `attributeName "[data-reka-collection-item]:not([data-disabled])"`; unconditional-on-modality `if (event.key === "Tab") event.preventDefault()`; Home/End over `collectionItems`; `data-reka-menu-content` :301 + `onKeydown: handleKeyDown` :318), Collection.js:6 (sole mint site), Toggle.js:63-72 (`"aria-pressed": unref(modelValue)` unconditional on the root; `type: _ctx.as === "button" ? "button" : void 0` — reka's own Toggle defaults `type`); installed glass-ui probes — ghost cva row extracted whole (**exactly 2** `aria-pressed:` utilities), `sm: "h-(--control-h-sm) rounded-pill px-3"`, glyph rule present in the cva base, `type: u.type` passthrough, toggle-chip `cell` array + `useForwardPropsEmits`, dropdown-menu chunk (`glass-floating` in the content class; RadioItem indicator `bg-current` present) + `dist/dropdown-menu.js` full export table (RadioGroup/RadioItem/Label/Group all present at 4.0.0), `dist/motion-curves.js:3` (**20 import specifiers / 19 unique symbols** — `CSSCubicBezier` aliased twice), emission greps (`h-\(--control-h-sm\)` in components.css = **1**; `ui-glyph` = **0**; `@source "../*.js"` at index.css:**222**), typography/scale.css:83-105 (**`--type-admin-label: 0.625rem` /* 10px */** above `--type-micro` 11px; `text-admin-label` @utility :213), offsets-sizing.css:134-152/:168-178, light-dark.css coarse block + `--card` light-dark, color-radius.css:41/:72/:84/:263-267 (`--viz-fourier/-chebyshev/-legendre` oklch; **`--viz-amber: var(--section-color-5)`**), dark-arm.css:43/:64, glass.css:25 (`--glass-opacity-floating: 0.80`) + :140, glass/surfaces.css:119-160 (the six-leg `.btn-pill` transition ending `scale … var(--spring-smooth)` + the CRITICAL clobber comment, verbatim; `.btn-pill > * { flex-shrink-0 }`), utilities/base.css:240-280 (`.tap-squish` same six-leg set + `:active { scale: var(--scale-press) }` + the PRM reset arm; `@layer components` opens both files), property-regs.css `viz-` grep = **0** (no color registration — corroborates the banked fr-BasisCanvas census); value.js 4.0.0 source reads — `package.json` exports (7 subpaths, no `"."`, no main/module/types), `grep timingFunctions src/` = **0**, `DIRECT_EASINGS` membership whole (:94-114: linear + five in-out kebabs **+ `ease-out-cubic` + `ease-out-expo` + `smooth-step-3` + `ease-in-bounce`**), PRESETS rows `"ease-in"/"ease-out"/"ease-in-out"/"ease-in-sine"` :37-41 + the three back rows :62-64, `easing()` :166-170, `subpaths/easing.ts` (all five `easeInOut*` re-exported); node probes over installed 0.13.0 — `timingFunctions` exists, **55 keys**, object identity `timingFunctions["ease-in-out-{sine,quad,cubic,expo,circ}"] === easeInOut*` **5/5 true**; producer 8.0.0 reads — exports map (**no `./dropdown-menu`**, has `./menu`/`./metric`/`./easing`/`./chip`; `./metric-badge` and `./toggle-chip` absent), `git show v7.0.0:package.json` (**`./dropdown-menu` present at 7.0.0** → the removal is NEW at 8.0.0), `git show v7.0.0:…/easing/constants.ts` (**VIEW_PAD :32 / MAX_OVERSHOOT :33 / VIEWBOX_FIT_SAMPLES :40 all present at 7.0.0** — the D-3 provenance decider), EasingCurve.vue:1-13 header (*"a 98-line name-colliding copy plus a 41-line preview in one repo alone"* — fourier's files are 98/41 exactly) + `EasingStroke { d: string; tone? }` + :106 `--easing-curve-accent: var(--motion-accent, var(--viz-legendre))`, constants.ts:6-12 (*"THE FRAME IS A CONSTANT, AND THAT IS THE POINT … Anything below that reads as a function of the curve is a regression"*) + VIEW_BOX derived-constant square at :47, easing/index.ts (EasingCurve + EasingPicker exported), chip/chipVariants.ts (`cell` at **:14**; `shape` axis at :22/:29), dock/index.ts:15-22 (trigger fold comment, `DockDropdownTrigger` DEFINITION-ABSENT); WCAG 2.x recomputations run end-to-end by this seat — accent `#8574F6` L=0.24161; light plate 3.402:1; dark plate hsl(24 8% 16%) L=0.02202 → **4.049:1**; 8%-washed dark → 3.669:1; ceiling vs white **3.60:1**; 80%-plate-over-black ceiling ≈4.49:1; selected border 60%-mix over light `--card` → composite (0.7075, 0.6620, 0.9614), L=0.4466 → **2.00:1**; fine-pointer content stack border-box → 7.6px overflow at authored SVG size; identity-guard sweep over the banked registry (fr-AnimationControls, fr-BasisCanvas, fr-App, fr-ConvergenceTimeline) — decisive, see below.

## Scoreboard

**39 rows ADJUDICATED-CONFIRMED (4 BLOCKER-weight · 12 MAJOR · 15 MINOR · 8 INFO — including 12 reader-missed rows banked and 6 folds-by-reference to already-banked identities) · 17 killed-register entries (16 corpus kills RATIFIED or AMENDED + 1 seat kill of a reader's own correction-cell) · 4 seat cell-corrections (CC-1..CC-4) + 1 seat upgrade (U-1) + 1 amendment to a BANKED sibling record (fr-AnimationControls L-15) · 7 superlatives ratified.**

The headline adjudication: **the banked fr-AnimationControls D-9/C-9 row (keyboard-unreachable easing chips, booked MAJOR with "blocker-class if the SS-13 keystroke confirms") is hereby AMENDED to BLOCKER on static grounds — no keystroke needed.** Both readers independently, and this seat's own MenuContentImpl read third, close every reach mechanism by source: the Tab swallow is unconditional on modality (killing C M-6's `:modal="false"` escape clause), `handleMountAutoFocus` places focus inside the swallowing region on open, and arrows/typeahead/Home/End are all keyed to a collection a bare `Primitive` button never joins. The control is pointer-only by construction.

The second structural fact of the record: **the producer answered this component mid-audit.** glass-ui 8.0.0's `EasingCurve` (landed 2026-08-08) names this exact fork by line count in its own header and ships the zero-state display primitive the F.W3 "retire the fourth fork" line always needed — which re-homes the retirement, kills two prescribed cures, and dates the whole corpus's 7.0.0 substrate.

## Ruled disagreements (reader-DU vs reader-LC — the incredulity seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **D-9 severity** | CONFIRMED MAJOR (count corrected 4→2) | Killed the MAJOR: demote to INFO, fold into D-4 | **LC wins — INFO, folded into D-4** | The ghost cva string is shared by every `<Button>` in the app and emitted once app-wide; nothing is "paid for" on this component's account (my chunk read confirms one shared string, exactly 2 `aria-pressed:` utilities). The surviving design fact — chassis selected-state protocol (`aria-pressed`) ≠ control state (`aria-checked`) — is already the load-bearing content of D-4 and D-1. |
| **D-7 floor cite** | Confirmed with `--type-micro` 11px as "the floor of the scale" | Killed the cite: floor is `--type-admin-label` 10px | **LC wins the cell; row stands** | My scale.css:86 read: `--type-admin-label: 0.625rem; /* 10px — fixed sub-control micro (NOT fluid) */` with a shipped `text-admin-label` @utility at semantic.css:213. The 9px literal is 1px under the DS floor, not 2px. Structural half (rides neither clamp ladder nor `--ui-scale`) untouched. |
| **C S-2 mechanism** | Killed: Primitive DOES set `inheritAttrs: false`; conclusion survives via manual attrs spread | S-5 repeats "no `inheritAttrs: false`, no multi-root" | **DU wins — and LC's repetition is a citation-inheritance event** | Primitive.js:12 `inheritAttrs: false`, :26 `h(props.as, attrs, { default: slots.default })` — read whole by this seat. Right verdict (role/aria-checked reach the `<button>`), wrong mechanism in both the C challenger and reader-LC. Recorded in DISSENT. |
| **D-3 producer-provenance kill** | "Reversed against the live producer — no `VIEWBOX_FIT_SAMPLES` at all" | "Accurate on 2026-08-04 (7.0.0), OVERTAKEN four days later by `1bc09dde`" | **LC wins — TRUE-AT-DATE, OVERTAKEN, not fabricated** | `git show v7.0.0:src/components/easing/constants.ts` → VIEW_PAD :32, MAX_OVERSHOOT :33, VIEWBOX_FIT_SAMPLES :40 all present. D-3's cite was correct when written; the live tree deleted the machinery and wrote the opposite doctrine (constants.ts:6-12, my read). The defect row (27.34%) is unaffected; the prescribed bounds-fit cure dies either way. |
| **motion-curves import count** | 18 (inherited from C) | 20 | **LC wins, seat-refined: 20 specifiers / 19 unique** | My parse of dist/motion-curves.js:3 — `CSSCubicBezier as r, CSSCubicBezier as i` double-aliases one symbol. (CC-3.) |
| **D-1 cure characterization** | Killed "one word" with three complications | Confirmed cure availability at the installed pin | **Both adopted — no true conflict; "one word" stays dead** | Availability: my dist/dropdown-menu.js export table (RadioGroup/RadioItem/Label/Group all at 4.0.0). Complications, seat-verified: (b) `arrowKeyOptions: "vertical"` (MenuContentImpl :196) — over a `repeat(3, 1fr)` grid ArrowDown moves *right* on screen, so the cure trades unreachable for mis-mapped unless the layout becomes a column or a 2-D handler is added; (c) `./dropdown-menu` present at v7.0.0, GONE at 8.0.0 → `./menu` (my exports reads). (a) the mandatory RadioItem indicator dot — spot-corroborated (`bg-current` in the RadioItem region of the chunk), adopted as DU filed it. The cure is adjudicated separately from the defect (the ColorSpaceSelector addendum-2 law). |
| **Producer-arrival severity** | missed #7, INFO | missed #3, MAJOR | **MAJOR as formation input** | The arrival re-homes an entire F.W3 disposition, kills two prescribed cures, converts EasingCurvePreview.vue from a re-style into a deletion, and dates the census a full major. That is budget-moving, not color. DU's INFO grading preserved in provenance. |
| **Dark-arm contrast row shape** | Standalone BLOCKER ("no scheme, no backdrop") | MAJOR amendment folded into D-2 | **One identity — folded into D-2, which is BLOCKER** | Same computation, one defect. DU's "ANY backdrop" phrasing holds only *under the 80% glass plate* (over pure black the bare accent would reach 5.83:1; under the plate the ceiling is ≈4.49:1 — my compositing). Scoped precisely, adopted. |
| **chipVariants cell line** | Kill-cell sub-correction: "line 15, not the cited :14" | (uncited) | **Seat kills DU's sub-correction** | My grep: `14:    cell: "glass-chip--cell flex-col gap-1.5 px-2 py-2.5 text-micro"`. D-4's original `:14` cite was right. The `shape`-axis half of DU's cell survives (chipVariants.ts:22/:29, my read). (CC-4.) |

**Seat corrections, upgrades, and one banked-record amendment:**

- **CC-1 (amendment to a BANKED record — fr-AnimationControls L-15).** That record's cell reads "*`easing` changes are unobserved, so changing easing while paused does not redraw*" (folding to fr-BasisCanvas BC-18). My BasisCanvas.vue:418-419 read: the render watcher lists `() => anim.easedT` explicitly alongside `() => anim.t`, and `easedT = fn(t)` recomputes when `easing` changes — so selecting an easing DOES repaint while paused at any `t ∉ {0, 1}`. The unobserved case is confined to the endpoints (where `f(0)=0, f(1)=1` for all six members — LC's honest limit). The banked cell is over-broad and is amended at its identity; both readers' S-3/write-only-frame-cost superlatives are the accurate statement.
- **CC-2 (LC's DIRECT_EASINGS parenthetical).** "Only linear + the five in-out rows are in DIRECT_EASINGS" is imprecise: my :94-114 read adds `ease-out-cubic`, `ease-out-expo`, `smooth-step-3`, `ease-in-bounce`. Net for the morph catalogue: **8 of the 22 keys stay analytic at 4.0.0; 14 drift to CubicBezier approximations.** The semantic-drift finding itself is RATIFIED and sharpened.
- **CC-3** — motion-curves = 20 specifiers / 19 unique (above).
- **CC-4** — chipVariants `cell` is at :14 (above).
- **U-1 (upgrade of DU's oklch-resolver residue).** property-regs.css registers no color property (`viz-` grep = 0, matching the banked fr-BasisCanvas census), so `getPropertyValue("--viz-legendre")` returns the authored token stream — the failure mode is source-decided, not serialization-dependent. Immaterial to the verdict (all forms fail all four regex arms), and the row itself is **already banked**: see the killed register, K-11.

## Adjudicated defect roster

Line anchors are fourier `cd26c65`+dirty and producer HEAD `1bc09dde`; re-resolve before any cure (D-19). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5-W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring. Identity guards (folds by reference, never re-booked) stated inline; the banked sibling records **fr-AnimationControls** (D-9/C-9, L-11+M-10, L-12/C-25, M-6, M-7, C-1, L-13), **fr-BasisCanvas** (D-1/BC-1/C-1, BC-18) and **fr-ConvergenceTimeline** (the 8.0.0 prop-surface break class; the root-font fact) already own several identities this corpus re-derived.

### BLOCKER-weight (4)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **D/D-1 + C/M-6** | The six easing chips are keyboard-unreachable inside the DropdownMenu (WCAG 2.1.1 Level A) — all four reach mechanisms (arrows, Tab, typeahead, Home/End) are keyed to `[data-reka-collection-item]`, which a bare glass `Button` (reka `Primitive`) never mints; the Tab swallow is **unconditional on modality**, and `handleMountAutoFocus` places focus inside the swallowing region on open, so `:modal="false"` rescues nothing. Pointer-only by construction; the correct ARIA (S-1) decorates elements no keyboard reaches. | MenuContentImpl.js:181-213/:301/:318; Collection.js:6; Primitive.js (whole); button chunk emits data-* only — all this seat's own reads | **F.W3** — identity = **banked fr-AnimationControls D-9/C-9, AMENDED to BLOCKER by this record** (the escalation condition is discharged statically; the banked "live keystroke" SS-13 item demotes to confirmatory). Cure = `DropdownMenuRadioGroup/RadioItem/Label` at the installed pin (export-table-verified) **with the three complications booked** (indicator dot; vertical arrow map over a 3-col grid ⇒ column layout or 2-D handling; `./dropdown-menu`→`./menu` at 8.0.0). The fixme'd keystone e2e is the covering-test row (MINOR block below). |
| **D/D-2 (+ both readers' contrast amendments, folded)** | The selected chip's 9px label fails WCAG 1.4.3 AA **in both schemes**: light 3.40:1 (washed ≈3.13-3.2:1), dark 4.05:1 bare / 3.67:1 under the chip's own 8% wash — and the accent's ceiling against pure white is **3.60:1**, so no reachable popover backdrop clears 4.5:1 in either scheme (under the 80% `glass-floating` plate the dark-side ceiling is ≈4.49:1). The "light-mode-only" scoping and the "dark arm passes 4.7:1" cell are both dead (killed register). Root cause: `--easing-accent: hsl(248 88% 71%)` is the one colour in the file with no scheme arm. | EasingPicker.vue:49/:86/:89/:95-97; light-dark.css `--card` pair; glass.css:25/:140; all four ratios recomputed end-to-end by this seat | **F.W3** — the cure is NOT the `--viz-amber` one-arm precedent (style.css:113-127, my read): both arms need re-minting, and the producer now ships the landing chain with both arms free — `--motion-accent` → `--easing-curve-accent` over `--viz-legendre` (EasingCurve.vue:106, my read; `--viz-legendre` live at the installed pin, color-radius.css:265). If EasingCurve adoption defers past F.W1, the two-arm retune is the F.W4 fallback. |
| **L/B-1 + C/B-1** | The import closure fails at value.js 4.0.0: no `"."` export (7 subpaths only; main/module/types absent — my package.json read) AND `timingFunctions` deleted (src grep = 0). The picker's own five functions survive at `./easing` (subpath read); the blocker is inherited from the co-resident morph catalogue. **LC's sharpenings RATIFIED with CC-2**: all 22 morph keys DO resolve through `easing(): Result` at 4.0.0 (PRESETS carries ease-in/out/in-out :37-39 and the three back rows :62-64), **but 14 of 22 silently drift analytic→CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo) — the morph curves change shape across the bump, unbooked anywhere else. | value.js 4.0.0 source (this repo, this seat); web/lib/easings.ts:9-16/:55-60 | **F.W1** — identity = census C-7 / lane-frontend:480 (5 bare-root sites; folded, not re-booked). Booked HERE as census sharpenings: the Result-shape rewrite for `EASING_PRESETS`, the 14-key semantic drift (needs an explicit acceptance decision, not a silent bump), and the costing correction from the duplicate-import row below. |
| **C/B-2** | Peer contract already violated today (installed glass-ui 4.0.0 peers value.js `^0.10.0 \|\| ^0.11.0` vs installed 0.13.0) + keyframes 4.3.0 hard-dep `^0.13.0` + optional glass `~4.0.0`; glass-ui itself consumes the dead bare root (motion-curves.js:3, 20 specifiers — CC-3). | Direct package.json reads by this seat (all three) | **FOLD BY REFERENCE — banked fr-AnimationControls C-1 = fr-BasisCanvas C-8, one identity, → F.W1.** This record contributes the corrected symbol count only. |

### MAJOR (12)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D/D-3 + L/M-2** | The curve preview paints at **27.34%** of its own box (scale = min(28/1.1, 19.6/1.6) = 12.25; 150.06/548.8; 7.875px gutter/side — seat-recomputed); 37.5% of the viewBox height is overshoot allowance no member of the six-key catalogue can use (all monotone in [0,1]; LC's node sampling + my source read of the six members). LC's 19% killed (register). | **F.W3** — the durable cure is `EasingCurve` adoption (constant square frame by producer doctrine — constants.ts:6-12/:47, my read), sequenced behind F.W1; DU's hand-picked viewBox cure is RETIRED (its stated provenance was overtaken and the producer unit supersedes it). |
| **D/D-4 + C/M-3 (D/D-9 folded in as INFO)** | ~40 lines of scoped CSS reimplement `ToggleChip variant="cell"` shipped at the installed pin (cell array read verbatim from toggle-chip.js; row-for-row match), and the target survives the uplift as `chipVariants({ shape: "cell" })` (:14/:22, my read; `./toggle-chip` export already absent at 7.0.0 → `./chip`). **The chassis question is statically decided by this seat's Toggle.js:68 read**: the reka Toggle root emits `aria-pressed` unconditionally, so ToggleChip under `role="menuitemradio"` double-exposes state — adoption is sound as *styling* only, never as chassis; the chassis is D-1's `DropdownMenuRadioItem`. | **F.W3** — styling adoption rides the D-1 cure; the "needs axe run" residual is killed (register); D-9's dead-pressed-vocabulary observation folds here at INFO. |
| **D/D-5 + C/M-1 + L/m-5** | `size="sm"` imports a fixed `--control-h-sm` = 36px fine / 54px coarse (token chain re-read; the height utility is PRECOMPILED — components.css grep = 1, emission-certain), the two-row content stack does not fit (border-box: 23px inner vs ~30.6px stack → **~7.6px overflow** at authored SVG size, 4px under the glyph clamp — C's arithmetic carried, D's 4.6px killed), and the chip discards the `--ui-scale` interior it inherits (surfaces.css :133/:135 overridden by fixed rem at :72-73; children `flex-shrink-0` both ends). | **F.W3** (dies with the chassis re-home) / **F.W4** if landed under the pin. Painted geometry → SS-13. Mobile numbers amended by the root-font row below. |
| **D/D-6 + L/M-1 + C/M-2** | Button's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` (cva-verified) squares the preview to 16px/24px and kills `:size="28"` — cascade leg proven (presentation attributes lose to any author rule; the scoped rule defends only display/flex-shrink); **emission leg still UNPROVEN** (ui-glyph = 0 in components.css; depends on the `@source "../*.js"` backstop at index.css:222 — C's cite exact, L's :214 off by 8). | **F.W3/F.W4** — contingent-MAJOR; the build grep (`npm run build` in web/ then grep `svg:not(\[class\*=size-\])`) is the settle command, no browser needed. Decides whether D-3 reads 27.3% or 39%. The in-repo control: MorphPhaseConfig.vue:47/:198-202 defends the byte-identical `.easing-preview` class with explicit width/height — the cleanest evidence cell, banked at INFO below. |
| **D/D-8 + L/i-2 + C/m-1** | Six authored `description` strings typed REQUIRED (easings.ts:73-84, my read) with zero readers repo-wide; the chips are the only dock controls with no tooltip (host wraps play/pause/speed/overflow — my host read). The copy the UI wants is already shipped and thrown away. | **F.W4** — wire `description` into the house `<Tooltip>`/`aria-description` (~2 lines); rides the standing tooltip budget. |
| **D/D-10** | Opaque hover fill (`var(--muted)` = `--neutral-1`, no alpha in either arm — token reads) inside the translucent `glass-floating` popover (chunk-verified), and the unlayered scoped rule beats every producer `@layer components` rule unconditionally. Internally inconsistent with the file's own :86 color-mix. | **F.W3** (dies with chassis: both ToggleChip-cell styling and RadioItem hover are translucent mixes) / **F.W4** one-line `color-mix` otherwise. |
| **L/M-3 (decides C/i-1)** | No unknown-value posture over a persisted domain strictly wider than the option set — now **five** unguarded hops (LC's fifth RATIFIED: workspace.ts:167-169 spreads a localStorage draft over the defaults; my read): server bare `str` → wire `string` → loader truthiness+assertion → typed store ref → equality-only binding. Failure: a radio group with zero checked members over a silently-linear animation. This row also kills C/i-1 (register): the field IS persisted and remixed — `animation_settings` on all four models, seat-verified. | Seed/normalization half: **FOLD — banked fr-AnimationControls L-11+M-10 → F.W4** (this record adds the fifth hop). Server half (Literal/enum on `AnimationSettings.easing`, shared.py:69): **F.W5-W8** — new booking, the CRUD/provenance union owns the model. |
| **C/M-4** | Zero props, zero emits — template-writes the global store while `SpeedSelect` three lines away in the same menu takes the house v-model contract + a `compact` arm the picker lacks (both files seat-read); a second in-tree easing target (MorphPhaseConfig) hand-rolls its own because the leaf is unreusable. | **F.W4** — v-model + compact arm. The store-action seam half is **FOLD — banked fr-AnimationControls L-12/C-25**. |
| **MISSED-A (both readers, convergent-independent — the strongest new find)** | The scoped `transition` shorthand at :79 — the file's *compliant-looking* A.W3.d line — CLOBBERS the producer's six-leg coherent set: both `.btn-pill` (surfaces.css:137-156) and `.tap-squish` (base.css:258-269) carry bg/border/shadow/color/opacity on `--ease-standard` PLUS `scale` on `--spring-smooth`, both in `@layer components`; the unlayered two-leg scoped shorthand deletes the `scale` leg (press SNAPS via `:active { scale: var(--scale-press) }` with zero duration) and the box-shadow/color/opacity legs, and sheds `.tap-squish`'s PRM reset arm. The producer documents this exact cascade bug as a prior live-readback RED in both files (comments read verbatim by this seat). This is D-19's concrete mechanism and the inverse of the defence the producer built. | **F.W4** (carry the full six-leg set or delete the declaration) — dies early if the F.W3 chassis re-home lands first. Same defect CLASS as banked fr-ConvergenceTimeline D·D-4 (different file — pattern identity noted, separate site, not a re-book). |
| **MISSED-B (DU) — WCAG 1.4.11 selection state** | The selected chip's ONLY non-text state indicators both sit under the non-text 3:1 floor: the 60%-accent border composites to **2.00:1** against the light plate (seat-recomputed end-to-end) and the 8% tint is ≈1.03:1 — so with the label also failing 1.4.3 (D-2), **every visual channel of the selected state is under its own floor**; a sighted user cannot reliably tell which easing is active. No axis checked the border. | **F.W3** — one cure event with D-2 (the producer accent chain clears both criteria by construction); exact ratio over the real blurred backdrop → SS-13 (AA verdict safe: ceiling ≈2.4:1 on any light backdrop). |
| **MISSED-C (DU) — host break surface, census amendment** | The picker's sole host carries three producer-removed surfaces, and one is NEW past every banked record: `./dropdown-menu` exists at 7.0.0 and is GONE at 8.0.0 → `./menu` (both exports maps read by this seat) — on top of the banked `./metric-badge` + `DockDropdownTrigger` rows. D §6's "narrow break surface" killed (register): if AnimationControls does not build, EasingPicker never paints — exposure is total and inherited. | **F.W1** — census amendment (the removed-subpath count is understated for 8.0.0); `./metric-badge`/`DockDropdownTrigger` identities stay with **banked fr-AnimationControls L-13** (folded). Extends the "breaks behind retained/renamed subpaths" class fr-ConvergenceTimeline D·D-2 opened. |
| **MISSED-D (both) — the producer answered mid-audit** | glass-ui 8.0.0 `1bc09dde` (2026-08-08) extracts `EasingCurve` — zero-state, `strokes: EasingStroke[]` where `d` is a PATH not a callable (so the analytic catalogue drives it directly; the README's "declines analytic catalogues" objection binds only the math) — and its header names this fork by exact line count (98+41, matching `wc -l`). Consequences: D-3 dies by construction on adoption; EasingCurvePreview.vue becomes a DELETION not a re-style; MorphPhaseConfig's twin re-homes onto the same unit; the accent gets both arms free; BUT the line re-enters the tri-package deadlock (glass 4→8) whereas DU's ToggleChip route was landable at the old pin — **that trade-off is the ruling the forming F.W3 spec actually owes**. | **F.W0** (substrate re-derivation: the census's 4→7 delta table is one major stale) + re-homes **F.W3**. DU's INFO grading preserved; adjudicated MAJOR as formation input. |

### MINOR (15)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D/D-11 + D/D-12 + D/D-13** | Three sibling-rhythm breaks in one popup column (two group-label type treatments; a 4px horizontal inset break against an exactly-mirrored vertical rhythm; two 1px dividers mixed in srgb vs oklab) — all three seat-verified against the host + subject. | **F.W3** (DropdownMenuLabel/Group adoption erases all three) / **F.W4** otherwise. |
| **D/D-14 + C/m-9(b)** | `:93` `transition: color 0.15s` unтokened, 14 lines below the cited law. | **FOLD — banked fr-AnimationControls M-7** (one identity; MISSED-A above is its newly-found upstream mechanism). |
| **D/D-15** | Shorthand-then-longhand padding split across interleaved declarations (:50/:53). | **F.W4** — rides any touch of the block. |
| **D/D-17 + D/D-18** | Decorative SVG with no `aria-hidden`/`role`/`<title>` (preview :18-24); `aria-label="Easing"` duplicating the visible heading instead of `aria-labelledby` (:16-17). Both cures native to the D-1 primitives. | **F.W3/F.W4** — same edit as D-1's cure. |
| **D/D-19** | Zero `prefers-reduced-motion` posture in the animation-curve selector itself — and MISSED-A shows it also *sheds* the producer's PRM arm it would otherwise inherit. | **F.W4** — fold with the census PRM/ungated-clock family (same fold the banked sibling records used). |
| **D/D-20** | Popup width is set by "Exponential" at 9px min-content (`min-width: 9rem`, `whitespace-nowrap`, `flex-shrink-0` both ends, `repeat(3,1fr)` — chain seat-verified static-certain; px unproven). | **F.W4** + SS-13 (one open-menu measurement). |
| **L/m-1** | Three lookups, three postures: `getEasingFn` guarded (:66), store guarded (:28), `getEasingSVGPath` bare-indexes `ANIMATION_EASINGS[name].fn` (:105, my read — note the OTHER sampler `easingCurvePath` :116 is guarded too, making the bare one the sole outlier of FOUR sites) — fed by the unchecked assertion at the picker. | **F.W4** — one `??` arm; composes with L/M-3. |
| **L/m-2 + D/D-22 + C/m-3** | Two load-bearing `as EasingName` template casts (renderList's object overload types `key` as `string` — cite verified by LC, adopted); one typed keys array in script halves them. | **F.W4**. |
| **L/m-3** | `easing` is the only bare-ref template-mutated store member; no seam for normalization/persistence. | **FOLD — banked fr-AnimationControls L-12/C-25** (the `setEasing` action is also MISSED-F's natural flush point). |
| **C/m-2 (+ LC's alias-rename INFO merged)** | Pure SVG util laundered through the pinia store module (animation.ts:10-11 re-exports, my read) — and the SAME re-export renames both the object and the type (`ANIMATION_EASINGS as EASING_OPTIONS`, `AnimationEasingName as EasingName`), so neither name greps the full consumer set — LC's explanation for why C's own census cells disagree (RATIFIED). | **F.W4** — import from `lib/easings` directly; delete the aliases. |
| **L/M-4 (fork-census half) + C/m-8** | Two in-tree curve renderers over two samplers (32-sample memoized vs 24-sample unmemoized) sharing the literal class name `.easing-preview` (4 grep hits, seat-verified; the twin is at `components/morph/`, correcting both axes' path). | **F.W3** — both re-home onto producer `EasingCurve` (MISSED-D); the "not-drop-in" half of the old disposition is killed (register). |
| **C/m-6 + C/M-6-test-leg (+ LC's zero-coverage missed row merged)** | The committed build predates the installed dep (Jun 12 vs Jun 17); the sole covering e2e is `test.fixme` at :192 whose unblock condition ("the guarded `^2→^3` bump") is stale by two majors against the `^4.0.0` pin (comment block seat-read); **no unit runner exists at all** (vitest = 0 in package.json and .bin, my greps) — every row in this record is regression-unguarded and the roles were never asserted. | **F.W4** (un-fixme + a picker interaction test in the test budget) + **F.W0** (stale-build fact rides substrate). |
| **MISSED-E (LC) — save-race** | The picker's write reaches persistence through a 500ms `watchDebounced` (VisualizationView :53-64, seat-read) with no flush-on-save: `saveVisualization()` reads `toRaw(animationSettings.value)` synchronously (workspace.ts:344-357, seat-read) — select-then-save inside the window persists the previous curve. Narrow reachability (menu dismiss + save within 500ms) honestly stated. | **F.W5-W8** (persistence semantics) — the flush seam is the banked L-12/C-25 `setEasing` action. |
| **MISSED-F (LC) — duplicate imports / catalogue subset** | easings.ts:9 and :10-16 are two import statements from the byte-identical specifier, and `ANIMATION_EASINGS` is a strict content-subset of `EASING_PRESETS` by object identity (**seat node probe: 5/5 `timingFunctions[k] === easeInOut*` true** at installed 0.13.0) — one function set under two key vocabularies (the concrete mechanism behind L/M-3's morph-name-leak leg), and the F.W1 "5 sites / 6 symbols" costing double-counts: the named-import block is deletable TODAY at the current pin. | **F.W1** (costing correction) + **F.W4** (the no-duplicate-imports hygiene). |
| **MISSED-G (DU) — root-font arm** | Every px figure in D-5/D-7/C-M-1/m-4/L-m-5 is desktop-only: fourier's root is 1.125rem below 768px (style.css:40-49, seat-read), inflating every rem literal 12.5% exactly where the menu grows its `flex sm:hidden` Speed group. Defects survive; the numbers are viewport-scoped. | **FOLD — the root-font fact is banked** (fr-AnimationControls M-6's 10.1px cell; fr-ConvergenceTimeline K-1); booked here as a numbers-amendment cell on D-5/D-7, not a new row. |

### INFO (8)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D/D-9** *(seat-demoted per the LC ruling)* | Ghost's two `aria-pressed:` rungs are inert here; the shared cva costs this component nothing; the protocol-mismatch substance lives in D-4/D-1. | Folded into D-4. |
| **L/m-6 + D/D-23** | `repeat(3, 1fr)` hard-codes an arity a "compact subset"-documented catalogue does not pin. | **F.W4**. |
| **L/i-3** | `@reference "tailwindcss"` bypasses the app's themed stylesheet for one `@apply text-sm`, which itself bypasses `--control-text`. | **F.W4**. |
| **L/i-4 + C/i-4** | Chips render `<button>` with no `type` (implicit submit; not live — portaled, no form ancestor). **Seat sharpening**: reka's own Toggle defaults `type: "button"` when `as === "button"` (Toggle.js read) — the gap is specifically glass Button's bare-Primitive path, and the cure idiom already exists one primitive over. | **GLASS-RELAY** (producer default), with the sharpened cite. |
| **L/i-5 + the S-4/S-5 family** | `getEasingSVGPath` called from a template binding; O(1) only via a module-level memo two modules away — defect framing (invisible contract) and superlative framing (provably six-bounded) both true simultaneously. | **F.W4** — a local `computed` carries the contract locally; no urgency. |
| **D/D-21** | Empty/loading/error genuinely N/A (compile-time catalogue — closed, not skipped); the sole open state question (focus-ring × 1.5px accent border stacking on the selected chip) is moot once D-1's cure lands. | SS-13 residue only; **NO-WAVE-OWNER**. |
| **L/§6 (R5-7 disposition)** | The native-template-loop class does NOT apply (one `v-for`, component-rooted — clean negative, intake-corroborated), and two sibling blind spots do (per-iteration native children; object-source arity across two module hops: an 18-instance surface deriving as 2). | **F.W4** — carries with the intake's R5-7/R6-5 family, exactly as L filed it. |
| **MISSED-H (DU) — the in-repo control** | MorphPhaseConfig.vue:47/:198-202 defends the byte-identical `.easing-preview` class with explicit `width: 40px; height: 20px` (seat-read) — the cleanest available control for D-6's defended-vs-undefended geometry, used by no axis. | Evidence cell attached to D-6; no separate wave row. |

## Killed-claims register (all attacked by this seat before ratification)

1. **C/i-1 ("unpersistable by construction; every viewer gets sine") — KILL RATIFIED.** C read `AnimationData` (the precomputed-trajectory payload) and missed `AnimationSettings` (shared.py:65-71, `easing: str = "sine"` at :69) carried by all four persisted models (visualization.py :129/:189/:244/:283 — seat grep) with a live write (VisualizationView :53-64) and seed (loader :56) at both ends. Self-contradictory with L/M-3 inside one corpus; L is right.
2. **D/X-D1 display half + L/M-4 "never will be a drop-in" half — KILL RATIFIED.** Superseded by `EasingCurve` at 8.0.0 (MISSED-D; header/contract/frame all seat-read). The EDITOR half of both axes' argument survives intact (producer EasingPicker remains a bezier/steps authoring editor with no catalogue prop).
3. **D-2's "dark arm passes ≈4.7:1" — KILL RATIFIED, seat-recomputed**: hsl(24 8% 16%) → L 0.02202 → **4.049:1**, below AA; washed 3.669:1. The row's severity survives *strengthened* (scheme-independent). The cited `bg-popover` backdrop was also wrong in kind (`glass-floating` — chunk-verified); numerically saved only because `--card`/`--popover` share the literal.
4. **L/M-2's "19% of the element area" — KILL RATIFIED**: 150.0625/548.8 = **27.34%** (L divided by a 28×28 square). The 7.875px gutter in the same sentence was right.
5. **D-3's producer-mechanism cite — KILL AMENDED (LC's version adopted)**: TRUE-AT-DATE at 7.0.0 (`git show`: VIEW_PAD/MAX_OVERSHOOT/VIEWBOX_FIT_SAMPLES at :32/:33/:40), OVERTAKEN by `1bc09dde`, which deleted the fit machinery and declared the constant frame — ironically shipping the *shape* of D-3's suggested cure while refuting its provenance and its bounds-fit prescription.
6. **D-9's "four `aria-pressed:` rungs" — KILL RATIFIED (exactly 2, seat-extracted)** + the "pays for dead weight" framing killed (shared cva string) → severity demoted to INFO, folded into D-4.
7. **D-1's "the cure is one word" — KILL RATIFIED** with the three complications (indicator dot · vertical-arrow map over a 3-col grid, `arrowKeyOptions: "vertical"` seat-verified · `./dropdown-menu` gone at 8.0.0, both exports maps seat-verified). The cure itself stands at the installed pin.
8. **D-4/C-M-3's "UNPROVEN-NEEDS-LIVE (axe run)" residual — KILL RATIFIED, statically decided**: Toggle.js:68 emits `aria-pressed` unconditionally; ToggleChip forwards without suppressing. Styling-only adoption; chassis = RadioItem.
9. **D §6 item 4 ("uplift break surface for this component is narrow / indirect only") — KILL RATIFIED**: the sole host carries `./dropdown-menu` (NEW removal at 8.0.0), `./metric-badge`, and `DockDropdownTrigger` (dock/index.ts fold comment seat-read). Exposure is total and inherited.
10. **C/S-2's mechanism ("no `inheritAttrs: false`") — KILL RATIFIED**: Primitive.js:12 sets it; the conclusion survives via the manual `h(props.as, attrs, …)` spread (:26). Reader-LC repeated the stale cell — see DISSENT.
11. **C/M-5's remedy leg ("route through the app's own resolver") — KILL RATIFIED, and the underlying resolver defect is ALREADY BANKED**: `cssVarToHex` has no oklch arm and the three basis tokens are oklch-only at the pin — this is **fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER → F.W1), folded by fr-App B-1/L-1/C-1; ONE identity, NOT re-booked here** (identity guard). This corpus's contribution: the kill of C M-5's prescription + U-1's registration-census corroboration.
12. **C/m-5's "dangling pointer" half — KILL RATIFIED**: `docs/tranches/A/coordination/CONSTELLATION.md` ends in exactly `coordination/CONSTELLATION.md` (my find run returns the two files C itself listed). Under-qualified, not dangling; the chronicity half stands as dated evidence.
13. **C/M-6's escape clause + UNPROVEN grading — KILL RATIFIED**: non-modal sets `trap-focus: false` only; MenuContentImpl's Tab swallow is unconditional on modality and fires inside `data-reka-menu-content` where `handleMountAutoFocus` already placed focus. No live probe needed — the basis of this record's BLOCKER amendment.
14. **D-7's floor cite ("11px, 2px under") — KILL RATIFIED**: `--type-admin-label: 0.625rem` (10px) with the same fixed-micro annotation and a shipped utility (seat reads). 1px under the floor; structural half unaffected.
15. **D-5's "4.6px overflows" — KILL RATIFIED**: border-box +3px of border → ~7.6px at authored SVG size (C's arithmetic carried).
16. **D masthead "stores/animation.ts (161)" — cosmetic KILL RATIFIED** (146, my `wc -l`).
17. **SEAT KILL — reader-DU's own correction-cell "chipVariants line 15, not the cited :14"**: my grep puts `cell` at :14; the original D-4 cite was right. (CC-4.)

## Superlatives (ratified, with provenance)

1. **The ARIA rationale (EasingPicker.vue:10-15)** — all three axes, both readers, the census (:629), and this seat: `role="group"` IS a permitted child of `menu`; `menuitemradio`+`aria-checked` chosen over `aria-pressed` for the right reason against a real temptation (the ghost variant's two pressed rungs would have styled selection for free); Vue renders `:aria-checked="false"` as the string `"false"` (aria-* not special-boolean), so unchecked members are announced. Its tragedy stands: correct ARIA on an unreachable control (D-1). Preserve the reasoning when the surface re-homes — same instruction as the banked fr-AnimationControls S-1.
2. **The leaf-subpath import (:2)** — C/S-1, both readers, and this seat's counterfactual check: the barrel would drag motion-curves' 20 bare-root value.js specifiers into this leaf's graph and make B-1 fire from this file. Import hygiene whose payoff is invisible until the migration.
3. **The bounded memo (easings.ts:99-108)** — the textbook shape of a leak, provably capped at six by the closed union key type and the single writer; recorded by L as a REFUTED defect rather than dropped (the L-18 discipline done right).
4. **The write-only frame-cost posture + the refcounted clock it writes into** — reads exactly one store member across three bindings, never `t`/`easedT`; zero lifecycle/listeners/rAF/watches in both files; and the selection repaints the canvas even while paused because BasisCanvas's render watcher lists `easedT` (seat-read :418-419 — the fact that also grounds CC-1's amendment of the banked L-15 cell). LC's un-banked S-6 ratified: `setCanvasVisible` reference-counts and parks the clock, so a selection can never wake a hidden canvas.
5. **The carry discipline (:44-47)** — names the token, the destination repo, the ledger, and the reason, unprompted, in a 98-line file; and the tree vindicates it — the upstream landing site shipped under a different name (`--motion-accent` → `--easing-curve-accent` over `--viz-legendre`, seat-read at EasingCurve.vue:106). The note now needs a date and a re-point, not a filing.
6. **The corpus's own falsifier discipline** — L §7 records four architecture-suggested defects killed by the tree with their falsifiers preserved; C :115 contradicts its own corpus where the tree demands it. Why L's arithmetic slip and C's model mix-up were recoverable errors, not structural ones.
7. **The producer's answer, arriving mid-audit** — two repos, no coordination, four days apart, same verdicts: EasingCurve.vue:5-8 names this fork by line count and mints the display primitive; constants.ts:6-12 independently reaches D-3's conclusion about the frame and goes one further ("anything that reads as a function of the curve is a regression"). Belongs on the record as evidence that the BH-relay economy works.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **The `--ui-glyph` emission leg** (D-6/L-M-1/C-M-2) — settle without a browser: `npm run build` in `web/`, then `grep 'svg:not(\[class\*=size-\])' dist/assets/*.css`. Decides 27.3% vs 39% and the `size` prop's deletability. The sibling `h-(--control-h-sm)` leg needs nothing (precompiled — seat grep).
2. **Presentation-attribute `var()` substitution** (LC's find, the one that could supersede D-3/D-6 entirely): `:stroke="color"` delivers the literal string `var(--easing-accent)` into an SVG presentation attribute; if the engine does not substitute, `stroke` resolves to nothing painted and the preview is INVISIBLE. One readback on an open menu: `getComputedStyle(document.querySelector('.easing-preview path')).stroke` — expect `rgb(133, 116, 246)`; `none` confirms. In-repo precedent HarmonicLevelGrid.vue:70 is itself unverified; the twin uses the safe `currentColor` idiom.
3. **Painted geometry** — the fine-pointer ~7.6px overflow, the coarse-pointer dead space, D-20's resulting popup width, D-21's focus-ring × accent-border stacking on the selected chip: one open-menu inspection covers all four (fine + coarse cells).
4. **MISSED-B's exact border ratio** over the real backdrop-blurred plate (AA verdict already safe — ceiling ≈2.4:1 on any light backdrop; the number, not the verdict, is open).
5. **Coarse-pointer target WIDTH** vs WCAG 2.5.5 44×44 — height is floored by `--touch-target`; width is grid-derived inside a 9rem-min popup. A measurement, not a derivation. No axis raised it; carried from reader-DU.
6. **The banked fr-AnimationControls "live keystroke" item** — demoted by this record from severity-deciding to confirmatory witness (the mechanism is source-closed).
7. **Production data**: whether any off-set persisted `easing` exists (banked item, unchanged); plus the oklch `getPropertyValue` string cosmetics (defect source-decided per U-1; string shape immaterial).

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet, not the tree).** The C challenger's S-2 asserted "no `inheritAttrs: false`" as the mechanism by which role/aria-checked reach the DOM. Reader-DU attacked and killed the cell against Primitive.js:12; reader-LC's S-5 repeated it verbatim-in-substance ("no inheritAttrs:false and no multi-root") while getting the conclusion right. This seat's own read settles it (inheritAttrs IS false; the manual attrs spread carries the conclusion). LC's OTHER cells on the same surfaces (the 20-symbol count, the v7 git archaeology) were genuinely its own — the inheritance was local to this cell, exactly the pattern the ColorSpaceSelector DISSENT-1 predicted generalizes.
2. **Severity ordering (unresolved, preserved).** Reader-DU ranks MISSED-B (the 2.0:1 selection border) "above D-2 in practice"; this record grades D-2 BLOCKER and MISSED-B MAJOR because the label failure is total in both schemes while the border is one channel of a three-channel state. One cure event covers both; no ruling required beyond grading.
3. **Reader-DU's kill of D-3's provenance was itself too strong** — it read the live tree correctly and the history not at all; LC's git archaeology is the model (kills must date their evidence when the substrate moved mid-corpus). Recorded as method, with DU's live-tree facts adopted wholesale elsewhere.
4. **The banked-record amendment (CC-1)** is filed with humility: fr-AnimationControls L-15's cell may have meant "the watcher does not list `easing` itself" — true — but its consequence clause ("changing easing while paused does not redraw") is false for all `t ∉ {0,1}` by the same watcher's `easedT` source. The amendment corrects the consequence, keeps the observation.
5. **What this seat could not check**: the Aug-4 producer snapshot beyond `v7.0.0` tags (the challenge docs' exact read-state is reconstructed from the tag, not the working tree of that day); and no browser cell exists in this record at all — every pixel claim above is derivation-labeled and lives in the SS-13 register.

## Closing verdict

**ADJUDICATED — competently-authored leaf on the wrong chassis, now answered by its producer.** The craft is real and banked (seven superlatives; the ARIA reasoning, the leaf import, the bounded memo, the write-only store posture). The defects are structural and converge on three events the forming X·F specs own: **(1) F.W3** — re-home the chassis (`DropdownMenuRadioGroup/RadioItem` for reach + producer `EasingCurve` for display + the accent onto `--motion-accent`/`--viz-legendre`), which discharges the amended D-9/C-9 BLOCKER, both contrast BLOCKER/MAJOR rows, D-3/D-4/D-5/D-6/D-10 and the rhythm/ARIA minors in one motion — with the 4.0.0-vs-8.0.0 sequencing trade-off named as the spec's owed ruling; **(2) F.W1** — the inherited value.js/peer deadlock with this record's census sharpenings (14-of-22 semantic drift; the deletable duplicate import; the NEW `./dropdown-menu` removal); **(3) F.W5-W8** — the persisted `easing` domain (server Literal + the debounce flush seam). Nothing here stamps a wave; every routing is M-25 agglomeration input. Original challenge docs and both reader returns remain immutable beside this file per E-1/E-3.

— end of apotheosis.
