# pt-index — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-05 · **Corpus HEAD**: parse-that `ef10d5b` (typescript/ tree unmodified), value.js branch `tranche-u`.
**Subject**: `parse-that/typescript/src/parse/index.ts` — the 14-line barrel, 34 runtime + 6(+1 star-carried) type exports, `.` entry of `@mkbabb/parse-that@1.0.0`; ~2,650-line transitive closure.
**Corpus adjudicated**: `audit/parse-that-modules/index/challenge-L-library.md` (31 defects, 7 superlatives, 3 corpus corrections; two-pass, opus-authored) + `challenge-C-consumption.md` (26 defects, 6 superlatives; three-pass union, opus-authored).
**Readers adjudicated**: rereader-L (`L-18-hostile-reread-parse-that-index-library`) · rereader-C (`parse-that-index-consumption-api-L18-rereader`). Both hostile re-reads ran static-only against the live tree; both produced kills, misses, and superlatives; they **disagree at verdict level on five rows** (C-B1, C-M5, C-M15, L:M-2, C-m5) and at severity level on one shared identity (C-M8 vs L:m-8).
**Method (L-14/L-18)**: every verdict-level disagreement was re-derived by this seat with its own instruments before either reader was adopted. This seat's own reads this session: the L challenge whole (1,005 lines); the C challenge's header, §0, C-B1/C-B2, C-M5, C-M6 head, C-M15, C-m1, §6–§8; live `state.ts:40-53`, `parser.ts:160-187` (mapState), `parser.ts:478-522` (trim, whole), `parsers/utils.ts:12-20` (quotedString); the css-parser workspace — `package.json` dependency block, installed `node_modules/@mkbabb/parse-that/dist/` listing, `shasum` of the packrat-entry chunk against the live tree (`be67e1d41bf4df86cd7648ca8f0c0d8d80de922e`, **identical both sides**), `denominator/denominator.test.ts:75-90`, and a workspace-wide import census (21 files importing `@mkbabb/parse-that`); the X·P wave heads W0–W4; the ColorSpaceSelector exemplar. **No bench, no execution of the library, no `enableDiagnostics()`, no `memoize()` — the PT-03 latch was never armed by this seat; the forbidden fresh root was not touched (both readers re-verified it absent 2026-08-05).**
**Law**: single write = this file. Dispositions below are **ADJUDICATED, not VERIFIED** — they route findings; they do not certify cures.

## Scoreboard

**68 adjudicated surviving rows: 5 BLOCKER · 27 MAJOR · 28 MINOR · 8 INFO** (52 merged identities from the two challenge files + 16 reader-missed rows adopted) · **15 killed claims** in the register · **13 superlatives** (7 L + 6 C, two strengthened, two partially undercut) · **9 MEASURE-AT-OPEN residue rows** for the W1 harness.

Blocker-weight survivors: **B-1/B-2/B-3/B-4** (the algebra-contract quartet: value-carrying failure, lying tuple, three-valued boundary, whitespace-rejects-EOF) and **C-B2(+M-9)** (the dual-graph latch construction O-8 forbids by name). **C-B1 is DEMOTED from BLOCKER** — the single largest adjudication of this record, decided by this seat's own instruments.

## Ruled disagreements (the seat's actual work)

| row | rereader-L | rereader-C | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-B1** ("routing law severed; empty consumer set; recorded nowhere") | CONFIRMED BLOCKER | KILLED unfenced arms → residue MINOR/INFO | **rereader-C WINS — DEMOTED.** The fenced claim survives as the axis **denominator**, not a blocker | This seat re-ran the decisive checks: `docs/tranches/V/megatranche/prototypes/css-parser/package.json` declares `"@mkbabb/parse-that": "1.0.0"` as a dependency; the installed dist is **byte-identical** to the live tree (shasum `be67e1d4…` both sides); **21 workspace files** import the package (cand-o/f/b/s grammars, idiom suite, fixtures, the surface-gaps probe); and `denominator/denominator.test.ts:80-87` **executably records** the not-yet-a-dependency state as "decree I-11 §2 unexecuted", asserting value.js deps `=== ["@mkbabb/glass-ui","@mkbabb/keyframes.js"]`. So the barrel has a live, installed, resolving, adjudicated consumer; the cut is a tracked pending decree with a passing gate, not an unrecorded severance. What survives: value.js-proper consumes zero (true, six commands), the value.js 4.0.0 changelog omission (C-i2, INFO), and the dead-combinator gate's two-name ban list vs its own precept (folds to C-m6). rereader-L confirmed the row without checking the prototypes workspace and inherited the challenge's own blind spot — recorded in DISSENT. |
| **C-M5** (unrunnability arm: "the probe cannot execute at all") | CONFIRMED whole | KILLED the arm; RED-7 substance survives | **rereader-C WINS on the arm; row survives MAJOR as rescoped** | Same instruments: the probe's workspace resolves its own `node_modules`; the C challenge measured `require.resolve` from the value.js repo root and generalized a wrong-root miss. W1.md designates the probe execute-no-write and G-4/G-5/G-9/G-10 invoke it. The RED-7 table itself was re-derived from source independently by BOTH readers (7 RED of 8 executed rows, 7-of-7 non-tautological, all owned by this barrel) and stands. Probe-line cites corrected: rows at `:22,:25,:26,:36,:37,:56,:57`, record row `:48-49`, `resetPackrat` row `:47`. |
| **C-M15** (`Suggestion`/`SecondarySpan` "read-only by construction — no sink") | KILLED to MINOR (sinks ARE published) | CONFIRMED MAJOR ("refinement correct as written") | **rereader-L WINS — MINOR** | This seat read live `state.ts:44-45`: `suggestions: Suggestion[] = [];` / `secondarySpans: SecondarySpan[] = [];` are **public, non-readonly, non-frozen instance fields** on the barrel-exported `ParserState` (`index.ts:3`); every `ParserFunction` holds a state by signature, so `state.suggestions.push({…})` is a published sink. The "no sink" refinement is false at the bytes. Surviving narrow claim: the library's gated emitters and its one higher-level error-shaper `reportUnclosedDelimiter` (utils.ts:66-80) are withheld, so a consumer can reproduce the data but not the arming discipline. rereader-C adopted the challenge row's self-refinement without re-testing it — a citation-inheritance event, recorded in DISSENT. |
| **L:M-2** (`_initWhitespace()` droppable under `sideEffects:false` — MAJOR) | CONFIRMED "as scoped" MAJOR | KILLED severity → MINOR (falsifier executed) | **rereader-C WINS — MINOR** | The row's own text pre-commits the downgrade ("if `_initWhitespace()` survives … downgraded to MINOR"). Its stated falsifier was executed twice independently — by the C challenge's own C-M11 probe and by rereader-C's four-cell rollup 4.59.0 run (both entries × both postures; `_initWhitespace()` survives in ALL FOUR via the live binding through `.trim()`'s default parameter). The surviving substance is C-M11's false-manifest-declaration row, which keeps its own MAJOR. Rollup retention re-pin at wave open → MEASURE-AT-OPEN. |
| **C-m5** ("~11 dead internal `.d.ts` files") | CONFIRMED MINOR at full scope | KILLED scope → 2 genuine orphans | **rereader-C WINS — MINOR at reduced scope** | The published types targets re-export through the internal `.d.ts` files by relative specifier (dist/index.d.ts → ./parser.js/./state.js/…; TypeScript resolves each to the sibling `.d.ts`), so 11 of 13 are **load-bearing**; deleting them breaks all five `types` conditions. Genuine orphans: `ansi.d.ts` + `debug.d.ts` only — and `debug.d.ts` is the sharp one (declares five functions with **no** `dist/debug.js` runtime: the phantom survives under M-4/C-M2). |
| **C-M8 vs L:m-8** (same Span-residue identity, filed MAJOR by C and MINOR by L) | MINOR (as L filed) | MAJOR (as C filed, gate-blindness argument) | **ONE identity — MINOR** | The defect is dead public surface (a type + two helpers with zero producers, zero consumers, two dead `import type`s): misleading, never wrong-answer. Under this corpus's own grading discipline, dead-surface rows sit at MINOR (m-4, C-m6); C's aggravator — hardcoded-name-list gate blindness — is a gate-design fact already scored at C-m6/C-M1 and cannot be double-charged here. The three symbols are already on W2 §6's RETIRE-17 list. |
| **L:M-13** (barrel import cycle; TDZ repro "PLAUSIBLE, NOT RUN") | Upgraded to CONFIRMED (static derivation) | CONFIRMED as-hedged | **Upgrade ADOPTED — CONFIRMED** | The prediction is decidable statically, no edit needed: `string`/`regex`/`dispatch`/`any` are `export function` declarations (hoisted-initialized, not in TDZ); `Parser` is a `class` declaration (TDZ); json.ts:15's first module-scope call reaches `new Parser` via `makeParser` — so reordering `index.ts:14` above `:2` throws `ReferenceError: Cannot access 'Parser' before initialization` on exactly the predicted symbol. The hedge was honest; hoisting semantics settle it. |

**Cross-file reconciliations inside agreed verdicts.** (a) B-3 vs C-M5 row 6: "5/5 non-string inputs throw" is TRUE for the probe's pinned grammar (`string("a")` dies at `charCodeAt` for all five) and FALSE as a general boundary statement (`eof().parseState([])` silently ACCEPTS) — both rows stand, and O-15 PT-07's general claim dies (C-1). (b) C-M12's load-bearing cite is `parser.ts:666` (the L file has it right; C's `:665` is a comment line). (c) The `enableDiagnostics`-arity probe row is `parsethat-surface-gaps.mjs:26` — C-M3 (`:29`) and C-M5 (`:27`) disagree with each other and with the tree.

## Adjudicated defect roster

Severities are this seat's terminal grades. Dispositions: **W0..W4** = the X·P wave whose units/gates own the surface · **REFOLD** = X·P REFINEMENT FOLD (the finding sharpens ratified spec text — W1.md/W2.md/parser-band.md/O-15 — and must be folded before those texts are next executed) · **NO-WAVE-OWNER** = no X·P wave owns it; recorded for parse-that's own next tranche (E-3 addenda posture) or the named repo's hygiene. Line anchors are HEAD `ef10d5b`; re-resolve before any cure (D-19).

### BLOCKER — 5

| id | claim | adjudicated proof anchor | disposition |
|---|---|---|---|
| **B-1** | `.parse()`'s failure sentinel is not `undefined` — `many(min≥1)`/`sepBy(min≥1)` return a truthy `[]` on failure; the failure value is an **arbitrary partial value** | parser.ts:77-79, :74; :553-556, :626-629 — both readers EXACT; corrects O-15 PT-07 arm (b) verbatim | **REFOLD** — widen W2.md:294's prohibition from `undefined`-on-failure to **any value-carrying failure**; parser-band.md:108's "never `parse()` truthiness" idiom is the interim law |
| **B-2** | `all()`'s tuple type lies — arity-1 unwrap + drop-`undefined` make runtime arity data-dependent; G8's "no `opt` under `all`" is an incomplete workaround (misses `eof()`, empty-`regex`, `matchFunction`-`""`) | leaf.ts:155-161, :199/:207/:230/:238/:246/:267; deliberate per leaf.ts:170-171 — both readers EXACT | **REFOLD** — widen parser-band.md:134 G8 + W2 idiom gates to all four `undefined`-producing arms; the candidate algebra's tuple soundness must be by construction, not by ban list |
| **B-3** | The non-string boundary is three-valued: throw (grammar-dependent leaf, not `parseState`) / silent-reject (coercion) / **silent-ACCEPT** (`eof().parseState([])` → ok; boxed String parses) | parser.ts:34-49 (no validation); leaf.ts:13, :327, :344, :350 — re-derived row-by-row by both readers; kills O-15 PT-07 arm (a) as a general claim | **REFOLD** — the W2 §3b JS-boundary guard must be a **positive `typeof x === "string"` admission test before any parse**, never a catch; corrects W1.md:582 / W2.md:161-162 |
| **B-4** | The published `whitespace` (`regex(/\s*/)`) cannot match at EOF — `regex()`'s unconditional EOF hard-fail (leaf.ts:326-330) makes a zero-or-more terminal reject valid input, one call from the default entry | leaf.ts:395-399 + :326-330 — static, both readers; **strengthened** by NEW-10: `.trim(parser, false)` routes the broken singleton into `all()`, defeating the challenge's own `.trim()`-escape falsifier (this seat read the `!discard` early-return at parser.ts:480-486 live) | **W2** — §6 already RETIREs the `whitespace` singleton; B-4 is the recorded reason-weight, and any candidate lowering's whitespace terminal carries a born-RED EOF-acceptance gate |
| **C-B2** (+L:M-9) | Nine of 34 exports are process-global latches published across **two independent module graphs** (ESM+CJS = two flags, two buffers, two one-way `PACKRAT_ARMED` latches, two MEMO tables); `resetPackrat` does not disarm; arming at construction; no published reader | packrat.ts:156/:217/:266/:290 (one write, `true`); dual graph proven statically by rereader-C at the bytes (both chunks carry independent latches); M-9's no-observer arm is why W1 mandates one-fresh-process-per-cell | **W2** — O-8 forbids the construction by name; the WRAP-9 ruling (diagnostics + packrat become parse parameters) owns the cure; M-9's harness consequence already encoded at W1 G-4/G-9 |

### MAJOR — 27

| id | claim | adjudicated proof anchor | disposition |
|---|---|---|---|
| M-1 | `sideEffects:false` vs ~36 eager `Parser` constructions at barrel import (`export *` → json/csv grammars at module scope; `PARSER_ID++` observable global mutation) | package.json:6; json.ts:15-52, csv.ts:7-20; parser.ts:18/:25 — qualitative claim static; 37-vs-1 count runtime | **W2** (RETIRE the showcases; the star line dies) · count → MEASURE-AT-OPEN |
| M-3 | `Parser.prototype.toString()` **throws** for any parser with the default `{}` context (`parserPrint: missing parser context name`) | parser.ts:31, :698-700; debug.ts:330-338 — EXACT; cand-F builds on the public constructor (parser-band.md:112) | **REFOLD** — DEBT-1 rider: the wave base inherits the construction; a default-context parser must stringify totally |
| M-4 (+C-M2) | The diagnostics feature is amputated: collectors published, the only renderers (`formatDiagnostic`/`formatAllDiagnostics`) ship in **no** bundle; `dist/debug.d.ts` declares five functions with no runtime file | debug.ts:200/:235; grep over all dist bundles → EMPTY; no dist/debug.js — both readers EXACT | **W2** (D-limb: diagnostics are values WITH published renderers) · phantom `.d.ts` → **W4** (release condition) |
| M-5 | Recovery is O(n) per collected diagnostic (unconditional `lastIndexOf` scan + full line-array `split`) → O(n·k) in the feature designed to collect many; render path re-splits per diagnostic | utils.ts:107-113; debug.ts:58/:100 — magnitude arithmetic KILLED (see register), complexity core survives | **REFOLD** — D-limb spec: newline index built once per parse on the threaded state; O(log n) per issue |
| M-6 | Dead flag machinery on the hot shape: `FLAG_EOF` never written (its whole labelled branch unreachable), `call()` reachable only via a two-hop dead chain, `unsafeCall` zero callers, `.trim()` allocates a flagged parser it throws away | parser.ts:466-476, :492-496; state.ts:91-94 — deadness re-verified; grep transcript corrected (see register) | **NO-WAVE-OWNER** (parse-that hygiene; the precept applies) |
| M-7 | `regex()` at EOF skips `mergeErrorState` — `furthest` stalls at −1, the label drops; truncated-input failures systematically under-report the expected-set | leaf.ts:326-330 vs :360; utils.ts:32-33 — EXACT; distinct from B-4 (same statement, opposite half) | **W3** — recovery/diagnostics-to-spec: truncated-input expected-sets are an EQ-4-family obligation |
| M-8 (+C-M3) | PT-01 sharpened: arming welds labels to an unconditional per-attempt `console.error` + full-source split; `isDiagnosticsEnabled` withheld; `mergeErrorState`'s label a no-op on the shipping default; `ParserState.toString()` is the same O(n) path **unarmed** | utils.ts:33; parser.ts:67-69; state.ts:136-138 → debug.ts:58 — shipped byte-exact; W2 R-LAW-3's throw-on-print probe is the kill condition | **W2** (EQ-4 + R-LAW-3 own it) · the unarmed-`toString` arm → REFOLD rider on R-LAW-3 |
| M-10 | `Parser.state` retains the parsed source forever (public field, zero clears), incl. the barrel's own module-singleton `jsonParser`/`csvParser`; a shared mutable per-instance slot on the parse path | parser.ts:26, :66/:68/:71 (2 writes, 1 read, 0 clears — corrected census); state.ts:48 | **REFOLD** — O-8 sharpener: the anti-latch law must also forbid instance-level retained parse state |
| M-11 | `ParserFunction<T>` is a phantom generic (`T` nowhere on the RHS) shipped in the `.d.ts`; all 26 casts against it are decorative — the seam-blindness that let B-2 ship | parser.ts:13-16; dist/parser.d.ts:2; count 17+9=26 confirmed, two cite errors corrected (register) | **W2** — typed-seam obligation of the candidate algebra |
| M-12 | `mergeErrorState` allocates two arrays per furthest-advance, ungated, on the failure path (cure: `length = 0`, since `recover()` reads them unarmed) | utils.ts:33-35, :121-122; call census corrected to **22** sites (register) | **NO-WAVE-OWNER** (hot-path hygiene; algebra replaces the function) |
| M-13 | `index.ts:14` creates a real import cycle back through the barrel; safety rests on unpinned statement order; TDZ crash **CONFIRMED by static derivation**; two of three back-edges dead | json.ts:5, csv.ts:5, parsers/utils.ts:4; tsconfig has no unused-flags | **REFOLD** — fresh-root module-graph law: no back-edges through the barrel, order-independence gated |
| M-14 | No error type anywhere: five failure channels, none typed; thrown messages leak internals and **vary by grammar** (`charCodeAt`/`startsWith`/`.length`/`.substring`) | `grep 'extends Error' src/` → zero; leaf sites verified | **W2** — the algebra's typed failure contract is the cure-by-construction |
| C-M1 | `export *` leaves 5 of 34 runtime exports outside the publish-discipline gate's regex in both directions — the exact 0.8.2 blindness the repo cured in a sibling gate and left here | dist-surface.test.ts:21; parsers/index.ts:5-8; proof-no-css-surface.mjs:6-14 (the cured sibling) | **W4** — release-condition gate law: derive the surface, never regex it |
| C-M4 | `createParserContext` published against a closed 29-literal name union that is itself unpublished — consumers cannot legally name a custom parser | state.ts:141-171 (29 counted by hand, both readers), :174, :180; absent from every entry `.d.ts` | **REFOLD** — DEBT-1: labelling must be open and first-class in the candidate |
| C-M5 (rescoped) | Every RED row of the band's surface-gaps probe is reached through an `index.ts` export — RED-7, 7-of-7 non-tautological, all owned here; **unrunnability arm STRUCK** (probe resolves from its own workspace — this seat verified) | parsethat-surface-gaps.mjs:11-13; corrected row lines :22/:25/:26/:36/:37/:56/:57 | **W1** — the probe is W1's instrument (G-4/G-5/G-9/G-10 invoke it verbatim) |
| C-M6 | `/core` ships the tier it disclaims: `parseState` calls `packratEnter/Exit` unconditionally, so packrat rides the default path and the 40,576 B chunk reaches every entry but `/diagnostics` | core.ts:4-5 (the promise); parser.ts:43-49 (the cause); dist/core.js:1 (the bytes) | **W2** — O-8 wrap: memoization as a parse parameter is the only cure that keeps the tier promise |
| C-M7 | `ParserState` welds an ANSI terminal renderer into every consumer bundle (browsers included) via `toString` → `statePrint` → ansi.ts — unshakeable because it is a method; the wanted renderers are meanwhile absent (C-M2): the inversion | state.ts:2, :136-138; debug.ts:5; ansi.ts:8-17; grep over dist | **W2** — R-LAW-3 corollary: no renderer on the state class; printers separable values |
| C-M9 (+NEW: decorator triple-defect) | `index.ts:7`'s `lazy` is a TS-4.x experimental method decorator that cannot compile under the package's own TS 5.8/no-experimentalDecorators config, is never exercised, **does not cache what its doc claims** (fresh `createLazyCached` closure per call), and binds `this` to the prototype | lazy.ts:26-43; tsconfig.json (18 lines, read whole by readers); the most-expected name bound to a dead export while `Parser.lazy` carries DEBT-3 | **W2** — RETIRE all three lazy exports; DEBT-3 (bounded back-edge as algebra parameter) is the successor |
| C-M10 | The barrel fan-in prices leaf-pure exports at ~400×: `containsDelimiter` (3 lines, zero imports) costs 36,359 B through `.`; reproduced **byte-exact** by rereader-C on an independent rollup run (all seven cells, zero drift) | split.ts:10-12; dist/parse.js:1-4 — structure static; bytes → MEASURE-AT-OPEN | **W2** (RETIRE split.ts from the barrel) · cells → MEASURE-AT-OPEN |
| C-M11 (+L:M-2 residue) | `sideEffects: false` is false of the shipped bytes (ansi's `process.stderr` read + `_initWhitespace()` at module level) and manifest-gate mandates the flag while never opening dist — it cannot fail for its intended reason | package.json:6; chunk :28 and :1416; manifest-gate.mjs:5, :38-40 — the harm half honestly killed by its own rollup probe (retention confirmed) | **W4** — release condition: manifest claims must be checked against artifact bytes |
| C-M12 | `Parser.recover()` writes the process-global journal **unconditionally** (sole ungated diagnostics behaviour) and retains the entry on success — parse #2 distinguishable from parse #1, unbounded growth | utils.ts:95, :102-128; parser.ts:**666**, :672-678, :681 (cites corrected) | **W2** — R-LAW-4/K-6 own it as a kill condition |
| C-M13 | `getCollectedDiagnostics()` returns a live alias; `clearCollectedDiagnostics()` swaps the backing store — a captured handle mutates underfoot before a clear and silently detaches after one | utils.ts:138-144 — EXACT both readers | **W2** — D-limb value semantics (EQ-4 compares against a store whose identity changes under the comparator) |
| C-M14 | The accumulator is published, its inverse unreachable: `popLastDiagnostic`/`resetErrorState` withheld though `recover()` itself depends on them — the R-LAW-1 rollback shape is not reproducible from the published surface | utils.ts:131-136, :146-148; parser.ts:674 | **W2** — D-limb |
| **NEW-A** (rereader-L missed-row, adopted) | `mapState` hands user code a prototype-chained **live alias** of the new state as the "old state" — only `offset`/`value` are own-properties; `src`/`isError`/`furthest`/diagnostics read through to the post-parse state; typed `ParserState<T>`, zero callers, zero tests, published on the kept `Parser` | **this seat's own read** of parser.ts:162-187: `const oldView = Object.create(state); oldView.offset = oldOffset; oldView.value = oldValue; return fn(state, oldView);` — verbatim | **W2** — KEEP-list rider: `Parser` rides into the wave base whole; `mapState` must be excluded or cured (the precept's shape, invisible to the export-census gates) |
| **NEW-B** (rereader-C missed-row, adopted) | `.trim(parser, false)` — the published non-discarding trim — returns `all(whitespace, this, whitespace)` **before** the whitespace-name guard, routing the EOF-broken singleton into `all()`: spurious rejection of any input whose value ends the string, compounded by B-1 (the errored state still carries the inner value). **Seat's rider**: the branch is also cast `as unknown as Parser<T[]>` — a second tuple-type lie (claims homogeneous `T[]`, delivers `[string?, T, string?]` minus dropped arms) in the same method | **this seat's own read** of parser.ts:480-486 — the `!discard` early-return verbatim | **W2** — trim semantics rider beside B-4/m-10 |
| **NEW-C** (rereader-C missed-row, adopted) | The `/utils` subpath is unshakeable and strictly worse than the root for the tier it serves: its entry module constructs the json/csv grammars at top level, so `skipWhitespace` costs ~36 KB through `/utils` under BOTH bundler postures vs 195 B through `.` under posture B — the five-way split delivers **three** distinct payloads, and the third is the worst | dist/utils.js structure static; cells rollup-measured by rereader-C | **W2** (RETIRE/re-home the scanners per COMP-1) · cells → MEASURE-AT-OPEN |
| **NEW-D** (rereader-C missed-row, adopted) | `.trim(customParser)` silently **discards** the caller's parser whenever its context is named `"whitespace"` — the only legal whitespace-shaped spelling in the closed 29-union (C-M4) — parsing with the internal ASCII scanner instead (comments silently not skipped). m-10's dual; no mutation of the singleton required | parser.ts:488 dispatch on the name; state.ts:158; verified against this seat's read of the trim body (the argument is never invoked on the flag path) | **W2** — trim semantics rider; the closed union makes the correct name the breaking one |

### MINOR — 28

| id | claim (compressed) | disposition |
|---|---|---|
| L:M-2 (demoted) | `sideEffects:false` + module-scope `_initWhitespace()` init of an `export let`: the false declaration survives; the crash half is dead for rollup (falsifier executed, retention confirmed in all four cells) | **W4** (rides C-M11) |
| C-M15 (demoted) | `Suggestion`/`SecondarySpan` published with gated emitters + `reportUnclosedDelimiter` withheld; the sinks themselves ARE public fields (this seat's read) | **W2** (D-limb) |
| m-8/C-M8 (merged, ruled) | Span residue: `Span`+`spanToString`+`mergeSpans` zero-producer/zero-consumer; two dead `import type`s; the S.H2 excision incomplete in the file that documents it | **W2** (already on RETIRE-17) |
| m-1 (+NEW-G part) | Three line/column APIs on `ParserState` disagree in base AND offset convention; two have zero callers | **W2** KEEP rider |
| m-2 | Two dead arrays per `ParserState` construction; `clone()` silently drops accumulated diagnostics (LR-grow loss) | **NO-WAVE-OWNER** |
| m-3 | `dispatch()` unbounded parser count → Int8 wrap at 128 = silent rejection of a registered character; posture contradicts `getCijKey`'s fail-loud `RangeError` one file over | **W2** KEEP rider (guard posture) |
| m-4 | Five hand-maintained export lists over one 34-symbol set; the gate checks three symbols | **W4** |
| m-5 | `utils.ts` is two unrelated modules fused; `/diagnostics` chunk carries the byte-scanners (bounded: shaken consumers pay 304 B) | **NO-WAVE-OWNER** |
| m-6 | `parserPrint` mints a duplicate graph for lazy parsers (two disjoint caches) and caches strings in a never-evicted Map keyed by a monotonic id | **NO-WAVE-OWNER** |
| m-7 | `getLazyParser`'s `undefined` sentinel conflates not-cached with cached-`undefined` under its own published generic | **W2** (RETIREs with the lazy tier) |
| m-9 | Dead `Parser` value-imports (csv.ts:5, parsers/utils.ts:4), emitted under `verbatimModuleSyntax`; no unused-flags in tsconfig | **NO-WAVE-OWNER** |
| m-10 | `whitespace.context.name` is public mutable state that reroutes every `.trim()` package-wide onto the B-4-broken fallback | **W2** (RETIRE + trim rider) |
| m-11 | `.parse()` accepts a prefix and reports success; composed with B-1 the return carries no information (three outcomes, one value) | **REFOLD** (entry contract: total-input or exposed rest) |
| m-12 | PT-04's depth ceiling ships unbounded/unparameterised/thrown; the DEBT-3 debt is owed one layer below where the band assigned it (C-3 re-homing) | **REFOLD** (charge the combinator layer explicitly) |
| C-m1 | `getLazyParser`/`createLazyCached` are internals of an unpublished tier, published as first-class | **W2** (RETIRE) |
| C-m2 | `skipWhitespace`/`skipBlockComments` retained for a consumer that left; `skipBlockComments` is CSS logic that cleared the excision gates by name-list evasion | **W2** (RETIRE per COMP-1) |
| C-m3 | One file's three types routed through two barrel lines/provenances | **NO-WAVE-OWNER** |
| C-m4 | `./package.json` not exported — version unreadable through the package's own surface | **W4** |
| C-m5 (rescoped) | Two genuinely orphaned published `.d.ts` (ansi, debug — the latter a runtime phantom); the other 11 are load-bearing | **W4** |
| C-m6 | 29 never-importable runtime exports vs 34 reachable; the dead-export precept's gate polices a two-name ban list (+ the C-B1 residue: the gate does not apply its precept to its own barrel) | **W4** |
| NEW-E (L-missed) | `dispatch()`'s key DSL disambiguates range-vs-literal silently by string length; no escape for literal `-` in a set; the precomputed label documents the misparse as intent | **W2** KEEP rider |
| NEW-F (L-missed) | Pre-sized result arrays ship HOLEY elements kinds (`new Array(n)` in fuseAll's general arm, `many`/`sepBy` est-paths) vs PACKED literals in the unrolled arms — the fusion note's own stated hazard | **W1** MEASURE-AT-OPEN (magnitude) · static fact recorded |
| NEW-G (L-missed) | Six zero-caller public methods on `ParserState` (`err`/`from`/`save`/`restore` + m-1's two); `restore` carries an unsuppressed bare `any` | **W2** KEEP rider |
| NEW-H (L-missed) | `regex()`'s sticky RegExp is per-parser mutable state written, then read back **across the `matchFunction` user-code callout** — the one re-entrancy seam the per-parse discipline misses; silent wrong offset advance | **W2** KEEP rider (regex is KEEP-8; ban or guard the seam) |
| NEW-I (L-missed) | Six `eslint-disable @typescript-eslint/no-explicit-any` directives for a linter that is neither installed nor configured — every `any` reads as reviewed and none was | **NO-WAVE-OWNER** |
| NEW-J (C-missed) | `dist/parse.cjs` — the root `require` target — is checked by no gate in `proof:all`; a formats regression ships green | **W4** |
| NEW-K (C-missed) | Published basename collisions: `dist/utils.js`/`dist/packrat.js` (entry bundles) share names with the declaration files of DIFFERENT source modules; only the explicit `types` keys save modern resolvers | **W4** |
| NEW-L (C-missed) | `quotedString(quote)` interpolates the caller's string unescaped into a regex character class — throws at construction for `"\\"`, silently builds a different grammar for `"]"`/multi-char (**this seat re-read the template literal live**) | **W2** (already RETIRE-listed; reason-weight) |

### INFO — 8

| id | claim | disposition |
|---|---|---|
| C-B1 (as rescoped) | The consumption denominator: value.js-proper consumes zero of the 34 (dependency deleted at `164343c1`); the sole live consumer is the adjudicated css-parser workspace (installed 1.0.0, byte-identical); re-add is decree I-11 §2, **tracked and unexecuted**, with a passing denominator gate | **W4** (the rebind/release seam records both the cut and the re-add) |
| i-1 | The barrel spends 3 of 14 lines on a tombstone that names a live gate — provenance, not clutter; move to the gate script | **NO-WAVE-OWNER** |
| C-2/C-i1 (merged) | O-15's `console.error` dist cite is off by one (`:882`, not `:881`; `:881` is the `if`); all four PT-03 cites byte-exact; `:262` is a second, opt-in `console.error` (parserDebug's default logger) | **REFOLD** (correct the letter's cite where downstream texts quote it) |
| C-i2 | value.js `4.0.0` §Breaking omits the parse-that dependency removal — value.js's record to correct | **W4** (record with the rebind) |
| C-i3 | No legacy resolver fallback (`main`/`types`/`module` absent) — a declared, gate-documented trade-off, not a regression | **NO-WAVE-OWNER** |
| NEW-M (L-missed) | `Parser.prototype.toString()` has a second unbounded failure mode: `parserPrint` recurses per graph node with no depth bound — the m-12 ceiling on a second mechanism | **NO-WAVE-OWNER** (recorded beside M-3) |
| NEW-N (L-missed) | `class Parser` spans 685 of parser.ts's 711 lines with no internal seam — the source-level reason C-M6's tier promise cannot be kept; barrel closure 2,650 lines for 34 exports | **REFOLD** (candidate design law: seams before tiers) |
| NEW-O (C-missed) | Systematic provenance drift across the challenge corpus (package.json blocks bracketed from opposite sides by the two files; five+ shifted load-bearing cites) — at least one document transcribed cites rather than re-reading | recorded here; see killed register |

## Killed-claims register — 15

1. **C-B1's unfenced arms** ("empty consumer set anywhere in the constellation" / "recorded nowhere in either release history" / BLOCKER) — killed by rereader-C, **confirmed by this seat's own instruments** (workspace dependency + byte-identical install + 21 importing files + the executable denominator gate). The fenced value.js-proper claim survives as INFO context.
2. **C-M5's unrunnability arm** ("the probe cannot execute at all … because of C-B1") — killed; wrong-root measurement generalized. The RED-7 substance survives.
3. **C-M15's "no sink / read-only by construction" refinement** — killed by rereader-L, confirmed by this seat at state.ts:44-45 (public non-readonly fields = published sinks). Row demoted MAJOR→MINOR.
4. **L:M-2's MAJOR severity** — killed by its own executed falsifier (rollup retains `_initWhitespace()` in all four entry×posture cells); the row's pre-committed downgrade clause applied. MINOR, folded into C-M11.
5. **C-m5's scope** ("~11 dead `.d.ts`") — killed to 2 genuine orphans; 11 of 13 are load-bearing via relative type re-export.
6. **L:M-5's magnitude arithmetic** ("four full-source passes… ~800 MB of slice copying") — killed (one op is O(1)-bounded at 20 chars; V8 sliced strings); the O(n·k) core survives on `lastIndexOf`+`split` alone.
7. **L:M-11's cast line list** — `parser.ts:703` carries no cast; the omitted 17th is `:493` (on M-6's own dead parser). Total 26 correct by offsetting errors. Claim survives.
8. **L:M-10's grep transcript** ("26, 66, 68, 71 — three writes, one read") — live grep returns 66/68/71; two writes, one read. Retention defect fully survives.
9. **L:M-6's `.call(` grep** ("parser.ts:493 only") — state.ts:93 is a second site; the dead chain becomes two-hop but stays dead (zero `unsafeCall` callers). Conclusion survives, strengthened.
10. **L:M-12's call census** ("twenty sites") — 22; `parser.ts:273` omitted, own list of 21 miscounted as 20. A wider set strengthens the finding.
11. **L:m-3's quoted byte** — the quote drops `.fill(-1)`, thereby describing a worse defect that does not exist (all-zeros table → mis-dispatch to `parsers[0]`); the actual wrap analysis survives. See superlatives — the dropped clause is itself scoring-worthy.
12. **C-M5's probe-line table** — systematically shifted (+1 on five rows, −4 on the two GUARD rows); corrected lines recorded above. Substance intact.
13. **C-M12/C-M14's load-bearing cites** — `:666` not `:665`, `:681` not `:680`, `utils.ts:127` not `:123`; the L file cites the same call correctly. Defects intact.
14. **Both files' package.json cite blocks** — proof scripts live at `:39-48`, `exports` at `:7-33`, `files` at `:51-53`; the two documents drift in opposite directions on the same 70-line manifest — diagnostic of transcription. All resting claims independently confirmed.
15. **L:M-13's PLAUSIBLE hedge** — killed in the row's favor: the TDZ prediction is statically decidable (class-TDZ vs function hoisting) and CORRECT. Upgraded to CONFIRMED.

## Superlatives — held to the same standard

- **S-1** `getCijKey`'s float64 key budget: correct, fail-loud, both prior defects documented in situ with their exact collisions — survives both hostile re-reads intact.
- **S-2** zero-advance guards make empty-matching parsers non-looping; both repetition combinators iterative. Survives.
- **S-3** the packrat re-entrancy unwind + `growLR`'s cap-free termination argument verify independently. Survives.
- **S-4** `ParserState`'s stable object shape — **strengthened**: rereader-L proved it at the shipped bytes (class fields emitted; no shape transition on first `expected` write), better evidence than the row's own.
- **S-5** `dispatch()`'s EOF (`NaN < 128` → false) and non-ASCII arms both correct. Survives. **Missed superlative adopted**: `.fill(-1)` at leaf.ts:101 is the third absent bug at the same site — the negative sentinel that makes the other two safe — quoted with the clause dropped by the very row (m-3) arguing the function lacks a guard.
- **S-6** `fuseAll`'s unrolling and `any()`'s type-correct arity-1 shortcut (the contrast isolating B-2) — survives, **partially undercut** by NEW-F: the general arm ships holey elements kinds the design note claims to avoid.
- **S-7** diagnostics per-state, packrat global-with-epochs — two subsystems, two deliberate soundness strategies, each explained in situ. Survives; `regex`'s sticky-lastIndex seam (NEW-H) is the recorded counter-example one layer down.
- **C:S-1** 34/34 source↔dist parity, gated. **C:S-2** the CJS live-binding getter for exactly the one mutable export — **generalizes**: present at both CJS entries. **C:S-3** the Span excision triple-gated with an in-file citation trail. **C:S-4** zero renames/default/namespace re-exports; the subpath gate tracks the manifest. **C:S-5** type-export hygiene exact at all six (+1 star-carried) positions. All survive.
- **C:S-6** the PT-03 latch is **statically eliminable** for graphs that never reference `memoize`/`mergeMemos` — the one corpus finding that moves toward the defence, correctly conditioned; fold measurement → MEASURE-AT-OPEN.
- **Missed superlative adopted** (rereader-L): `sepBy`'s pre-separator checkpoint gets trailing-separator rejection right in both directions, contract stated in the doc comment.
- **Method superlatives**: C-M11 kills its own harm half at the point of claim (the honesty that made the cite-hunts worth running); C-m6's 29-line census is exact at every line — plainly re-read, not transcribed.

## MEASURE-AT-OPEN residue (for the W1 harness; deterministic unless marked bench)

1. M-1's import-time construction count (37-vs-1, runtime import probe).
2. C-M10's seven tree-shake byte cells (already reproduced byte-exact once on rollup 4.59.0 — re-pin against the wave's dist).
3. NEW-C's `/utils` cells (36,435/36,680/36,289 both postures; 195 B root posture-B control).
4. The L:M-2/C-M11 rollup retention probe (four entry×posture cells; `_initWhitespace` survives — re-pin).
5. C:S-6's fold-to-zero (`grep -c PACKRAT_ARMED` → 0 on a memoize-free `/core` bundle; `packratEnter` folded to `return null`).
6. The posture-B equivalence bound (root == `/core` == 32,689 B for a leaf consumer — bounds M-1/C-M10 in the library's favour for bundled consumers).
7. NEW-F's holey-vs-packed throughput magnitude (**bench** — the only true bench row; static fact stands regardless).
8. O-15 PT-03's 1.47× and PT-04's 7,761-frame ceiling (cited receipts, never re-measured by any seat in this corpus — W1 G-9 re-establishes them).
9. C-M5's RED-7 executed from the css-parser workspace (now known runnable — turn the derived table into a run receipt).

## DISSENT — preserved, not ruled away

1. **Citation-inheritance events, both directions.** rereader-L confirmed C-B1/C-M5 at full strength by re-running the challenge's own value.js-root commands — faithfully reproducing measurements without questioning their scope; the workspace consumer sat one directory below the probe it declared dead. rereader-C adopted C-M15's self-refinement ("no sink") without testing the fields it named. Each reader independently caught the other's inheritance; this seat's instruments settled both. The lesson matches the ColorSpaceSelector record's addendum item 1: **a claim about a scope includes the scope**, and re-running a challenge's command at the challenge's chosen root is agreement, not verification.
2. **Severity ordering (unresolved, preserved).** rereader-C would rank NEW-B/.trim-family above M-7 (a wrong answer on valid input outranks an under-reported expected-set); rereader-L's file ranks by axis discovery order. The wave consumes both; no ruling required.
3. **rereader-2's C-B1 residue grade** ("MINOR changelog note") vs this seat's INFO-as-denominator + W4 routing: this seat holds that once the workspace consumer and the tracked decree are in evidence, the row is context that conditions every KEEP/RETIRE ruling rather than a defect with its own severity; the actionable residues are booked at C-i2 and C-m6. Preserved as a grading dissent.
4. **Both original challenge files' law compliance was impeccable** (STOP-checks, latch hygiene, single-write, honest hedges) even where their cites drifted — the M-2 row wrote the exact falsifier that later demoted it, and M-13 hedged a claim it could have won. That discipline is why 68 of the corpus's rows survived adjudication and only 15 claims died, twelve of them cite-level.

## Closing verdict

The module is **DEFECTIVE AT THE PUBLISHED BOUNDARY — adjudicated, with the interior genuinely above the median**. Five blockers survive hostile re-reading and this seat's incredulity: a `.parse()` that can hand back a truthy wrong answer (B-1), a tuple type whose runtime arity is data-dependent (B-2), a string boundary that can silently accept non-strings (B-3), a zero-or-more whitespace terminal that rejects valid input at EOF — reachable through `.trim(…, false)` too, defeating the challenge's own escape hatch (B-4+NEW-B) — and nine latch exports duplicated across two module graphs in exactly the construction the ratified algebra forbids by name (C-B2). The one BLOCKER that does not survive is the consumption-severance claim: **the barrel has a live, installed, byte-identical, adjudicated consumer** in the css-parser prototype workspace, and the dependency cut is a tracked, gated, pending decree — demoted by this seat's own instruments, not by argument. Routing: **28 rows to W2** (the algebra owns the failure contract, the WRAP-9, the RETIRE-17, and the KEEP-8 riders), **11 REFOLD** (they change ratified text — the PT-07 corrections, the positive string-admission boundary, the G8 widening, the O-8 instance-state sharpener, DEBT-1/DEBT-3 re-homings), **3 to W1** (the probe and the measurement residue), **10 to W4** (manifest truth, gate derivation, the rebind record), **1 to W3** (truncated-input diagnostics), **12 NO-WAVE-OWNER** (parse-that's own next-tranche communique), plus 3 recorded in place. Nothing here executes anything: X·P remains owner-gated, and every disposition above is a routing, not a begin-word.

— end of apotheosis. The two challenge files and both rereader returns remain immutable beside this record per E-1/E-3.
