claude-fable-5

# pt-index — adjudicated apotheosis, round 2 (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator · served model id `claude-fable-5` (Fable 5), stated per task law on line 1, observed in this seat's own system prompt — recorded against L-11/L-14.
**Date**: 2026-08-06 · **Corpus HEAD**: parse-that `ef10d5b` (`typescript/` tree unmodified; the dirty rows are `rust/` + `README.md` + `.cargo/` only — checked), value.js branch `tranche-u`.
**Subject**: `parse-that/typescript/src/parse/index.ts` — the 14-line barrel, **34 runtime + 7 type exports** (6 declared + `JsonValue` riding the star line), the `.` entry of `@mkbabb/parse-that@1.0.0`; ~2,650-line transitive closure.
**Corpus adjudicated**: `audit/parse-that-modules/index/challenge-L-library.md` (31 defects · 7 superlatives · 3 corpus corrections; two-pass, opus-authored) + `challenge-C-consumption.md` (26 defects · 6 superlatives; three-pass union, opus-authored).
**Readers adjudicated (r2)**: reader-L (`L-18-hostile-reread-parse-that-index`) · reader-C (`L-18-hostile-reread-parse-that-index-consumption-api`). Both static-only hostile re-reads of BOTH challenge files against the live tree; both produced kills, misses, and superlatives. They disagree at severity level on **B-1, B-2, B-3, C-M15** and at reconciliation level on **C-M11's premise, C-M4's second consequence, C-m5's scope, C-i1's weight** — and they **AGREE, wrongly, on C-B1 and C-M5's unrunnability arm**, which this seat kills with its own instruments.
**Supersession record (E-1/E-3 discharged in place)**: a round-1 adjudication occupied this exact path (dated 2026-08-05, uncommitted; it adjudicated two *different* rereader returns). Its substance is **absorbed, not lost**: its deciding evidence on C-B1/C-M5 (the css-parser workspace consumer) was **re-derived by this seat's own instruments** before adoption; its seat-found trim rows and quotedString row were **re-verified at this seat's own source reads** and folded into the roster (B-4 riders, AD-2 rider); its remaining unique rows are carried in §"Carried from r1". Nothing of r1 that survived scrutiny is dropped — head, tail, or interval.
**Method (L-14/L-18)**: every verdict-level disagreement — and every suspicious agreement — was re-derived before either reader was adopted. This seat's own instruments this session: challenge-C header + C-B1 block and challenge-L header + section map read directly; live whole-file reads of `index.ts`, `state.ts`, `utils.ts`, `lazy.ts`, `parsers/{index,csv,utils,json}.ts`; targeted reads of `parser.ts` (:1-100, :140-260, :430-712) and `leaf.ts` (:1-30, :95-215, :255-399) and `debug.ts` (six regions); dist reads (`parser.d.ts`, `state.d.ts:64-71`, `lazy.d.ts:9`, `parsers/json.d.ts`, `utils.js`+`utils.d.ts`, `packrat.js`+`packrat.d.ts`, `index.d.ts` specifiers, `parse.cjs:25-28`, `core.js:1`, `parse.js:1-4`, export-block count 34); `packrat.ts` latch census (:156/:217/:266/:290 + the :284-290 design note); `test/manifest-gate.mjs` whole; `scripts/proof-no-dead-combinator.mjs:1-70`; `package.json` whole with line numbers; **the css-parser workspace** — `prototypes/css-parser/package.json` (declares `"@mkbabb/parse-that": "1.0.0"`; description: *"Consumes the PUBLISHED package only"*), installed `node_modules/@mkbabb/parse-that` with the packrat-entry chunk **shasum-identical to the live tree** (`be67e1d41bf4df86cd7648ca8f0c0d8d80de922e`, both sides, this seat's run), the **21-file** import census, `denominator/denominator.test.ts:80-87` (the decree-I-11-§2 gate), and the probe's own header (*"Run from this workspace (it resolves the workspace's node_modules)"*); value.js `src/css/types.ts:25-27`; X·P wave heads `docs/tranches/X/parse-that/waves/W0..W4.md` + load-bearing cites `W2.md:253-257` (O-8), `:281-285` (R-LAW-3/4), `:292-295` (contract), `W1.md:434/:466` (G-4/G-5), `parser-band.md:108/:134`; the ColorSpaceSelector exemplar. **No bench, no library execution, no `enableDiagnostics()`, no `memoize()` — the PT-03 latch was never armed by this seat; the forbidden fresh root `/Users/mkbabb/Programming/parse-that-css-totality-p2` was not created or touched.**
**Law**: single write = this file. Dispositions are **ADJUDICATED, not VERIFIED** — they route findings; they do not certify cures. Line anchors are HEAD `ef10d5b`; re-resolve before any cure (D-19).

## Scoreboard

**68 adjudicated surviving rows: 4 BLOCKER · 30 MAJOR · 29 MINOR · 5 INFO** (52 merged corpus identities + 16 reader-missed rows adopted after this seat's spot-verification) · **21 killed claims** · **18 superlatives** (13 corpus + 5 reader-new, three scoped) · **7 MEASURE-AT-OPEN residue rows** · **5 carried-from-r1 rows**.

Routing: **32 → W2 · 12 → REFOLD · 1 → W1 · 2 → W3 · 10 → W4 · 11 → NO-WAVE-OWNER**.

Blocker survivors: **B-1** (truthy-`[]` failure at the headline entry), **B-2** (the lying tuple), **B-4** (whitespace rejects valid input at EOF — now with the `.trim(…, false)` route that defeats the corpus's own escape-hatch claim), **C-B2(+M-9)** (nine process-global latches across two module graphs — the construction O-8 forbids by name). **C-B1 and B-3 are both DEMOTED** — C-B1 by this seat's instruments against BOTH readers; B-3 by adopting reader-C's typed-entry fence over reader-L's grade.

## Ruled disagreements and suspicious agreements (the seat's actual work)

| row | reader-L | reader-C | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-B1** ("routing law severed; empty consumer set; recorded in neither CHANGELOG") | CONFIRMED BLOCKER | CONFIRMED BLOCKER | **BOTH OVERRULED — the severance/empty-consumer arms are KILLED; residue INFO (denominator)** | The challenge's own falsifier fences `docs/` and dismisses the one import it names with *"it cannot execute (C-M5)"* — a false support. This seat's instruments: `prototypes/css-parser/package.json` **declares** `@mkbabb/parse-that: 1.0.0`; the installed dist is **byte-identical** to the live tree (shasum `be67e1d4…` both sides); **21 workspace files** import the package (grammars, idiom suite, fixtures, the probe); `denominator.test.ts:80-87` **executably tracks** the value.js cut as *"decree I-11 §2 unexecuted"*. The barrel has a live, installed, resolving, adjudicated consumer; the cut is a tracked pending decree with a passing gate — a **contract migration** (see AD-6), not an unrecorded severance. Both readers re-ran the challenge's commands at the challenge's chosen root (`require.resolve` from value.js root; `node_modules/@mkbabb/` at value.js root) and inherited its blind spot — re-running a challenge's command at the challenge's root is agreement, not verification. Surviving arms: value.js-proper consumes zero (true — two prose comments only), the changelog silence (C-i2), the gate's two-name ban list vs its own precept (folds to C-m6). |
| **C-M5's unrunnability arm** ("the probe cannot execute at all") | CONFIRMED ("MODULE_NOT_FOUND from value.js") | CONFIRMED ("node_modules/@mkbabb/ has no parse-that") | **BOTH OVERRULED — arm KILLED; RED-7 substance survives MAJOR** | The probe's own header documents its resolution root: *"Run from this workspace (it resolves the workspace's node_modules)"* — and that workspace's `node_modules/@mkbabb/parse-that` exists and is byte-identical (above). Wrong-root measurement, generalized twice. The RED-7 table itself was re-derived from source independently by both readers and stands whole. |
| **B-1 severity** | BLOCKER | MAJOR ("needs non-default min≥1; typed entry steers to parseState") | **BLOCKER** | Parse failure is a *routine* event on the headline entry; `many(1)`/`sepBy(1)` is the bread-and-butter one-or-more idiom; the returned `[]` is truthy AND type-correct (`T[]`), so the documented `undefined`-sentinel check passes wrongly and silently. A wrong answer on the primary entry under ordinary use is blocker-weight. reader-C's fence recorded as dissent. Anchors re-pinned by this seat: `parser.ts:77-79`, `:555`, `:628`. |
| **B-2 severity** | BLOCKER | MAJOR (no rationale given) | **BLOCKER** | `all(p)` yields `V` where the shipped type says `[V]` (`leaf.ts:161`, read live) — every arity-1 destructuring breaks at runtime under a green typecheck; drop-`undefined` (six sites, re-pinned) shifts tuple positions data-dependently. Immediate, common, silent, on valid input. |
| **B-3 severity** (three-valued non-string boundary) | BLOCKER | MAJOR (`dist/parser.d.ts:10` types `parseState(val: string)` — reachable only from untyped JS) | **MAJOR — reader-C's fence adopted** | This seat read `dist/parser.d.ts`: both `parseState(val: string)` and `parse(val: string)` are string-typed, so every arm requires the consumer to violate the published type. The silent-ACCEPT arm (`eof().parseState([])` → ok; verified at `leaf.ts:13-14`) is real and genuinely new, and drives the REFOLD (positive `typeof` admission), but the *harm* is fenced. reader-L's BLOCKER recorded as dissent. |
| **C-M15** (`Suggestion`/`SecondarySpan` producers unreachable) | CONFIRMED MAJOR ("no published SINK") | CONFIRMED MINOR ("readable off a Diagnostic; only loss is emission") | **MINOR — on a third ground, this seat's own** | Live `state.ts:44-45`: `suggestions: Suggestion[] = [];` / `secondarySpans: SecondarySpan[] = [];` are **public, non-readonly, non-frozen instance fields** on the barrel-exported `ParserState` — `state.suggestions.push({…})` is a published sink, so reader-L's "no sink" refinement is **false at the bytes** (killed-claims #3). What is actually withheld is the arming discipline: the gated emitters + `reportUnclosedDelimiter` (`utils.ts:51-80`). MINOR. |
| **C-m5 scope** ("~11 dead internal `.d.ts`") | CONFIRMED at full scope | KILLED to 2 genuine orphans | **reader-C WINS** | This seat read `dist/index.d.ts`'s specifiers: it re-exports from `./parser.js`/`./state.js`/`./utils.js`/`./lazy.js`/… — TypeScript resolves each to the sibling internal `.d.ts`, so those files are **load-bearing** for every published `types` target. Genuine orphans: `ansi.d.ts` + `debug.d.ts` only (`ls dist/` run by this seat; `dist/debug.js` → No such file), and `debug.d.ts` is the sharp one — a declaration with no runtime anywhere (rides M-4). |
| **C-M11 premise** ("manifest-gate never opens dist/") | Confirmed as "reads manifest + existsSync, never opens a dist byte" | REFUTED ("Gate 3 existsSyncs ./dist/index.d.ts") | **RECONCILED — reader-C right on the letter, reader-L right on the substance** | This seat read `manifest-gate.mjs` whole: Gates 1+3 (`:24-35`, `:43-48`) do stat dist paths; **no gate reads dist bytes**. The L-19 conclusion survives on the narrow true ground: `existsSync` is not a purity check, so the gate that mandates `sideEffects:false` (`:38-41`) cannot fail for over-inclusion. |
| **C-M4 consequence 2** ("the 29-union is unreconstructible downstream") | Not addressed | KILLED (`NonNullable<ParserContext["name"]>` reconstructs it) | **KILL ADOPTED** | This seat read `dist/state.d.ts:64` (`parserNames` declared as the full readonly tuple) and `:66/:71` (the union inlined). Consequence 1 — `createParserContext` refuses any custom name, so a downstream grammar can legally name zero of its own productions — survives and carries the row. |
| **C-i1 weight** (O-15's `:881` cite) | KILLED as filed ("the letter cites the line it quotes — a rendering artifact") | CONFIRMED INFO ("off by one, same build") | **INFO, reader-L's softening adopted** | `:881` is the `if (isDiagnosticsEnabled()) {` guard; `:882` the call; the letter collapses them to one line and cites the guard. Worth recording (with the second, opt-in `console.error` at `:262` — `parserDebug`'s default logger, which this seat verified differs from `Parser.debug`'s `console.log` default), not worth a "corpus correction" category. |

**Reconciliations inside agreed verdicts**: (a) B-3 vs O-15 PT-07 — "5/5 throw" is TRUE for the probe's pinned grammar (`string("a")` dies at `charCodeAt` for all five inputs; challenge-L's own M-14 pass reproduces 5/5, self-contradicting its framing) and FALSE as a general boundary statement — the framing kill (register #5) stands with both readers. (b) C-M12's load-bearing cites are `parser.ts:666`/`:681` — re-pinned by this seat's grep. (c) The type-export census is **7**, not 6 — `JsonValue` rides the star line (`parsers/index.ts:6`, read live), an instance of the exact blindness C-M1 diagnoses.

## Adjudicated defect roster

Dispositions: **W0..W4** = the X·P wave whose units/gates own the surface (`docs/tranches/X/parse-that/waves/`) · **REFOLD** = the X·P REFINEMENT FOLD (the finding sharpens ratified spec text — W1.md/W2.md/parser-band.md/O-15 — and must fold before those texts next execute) · **NO-WAVE-OWNER** = no X·P wave owns it; parse-that's own next-tranche communique (E-3 addenda posture) or the named repo's hygiene.

### BLOCKER — 4

| id | claim | proof anchor (re-pinned this session) | disposition |
|---|---|---|---|
| **B-1** | `.parse()`'s failure sentinel is not `undefined` — `many(min≥1)`/`sepBy(min≥1)` return a **truthy, type-correct `[]`** on failure; the headline entry can hand back a wrong answer under ordinary use | parser.ts:77-79 (returns live `state.value`; the :61-66 errorState is display-only), :555, :628 (`unsafeSetValue([])` on both failure tails) — all read live | **W2** — the algebra's tagged-result contract is the cure-by-construction (`W2.md:292-295`'s failure arm carries **no value key**; the corpus-action "widening" is killed as moot, register #6). Interim law: parser-band.md:108's "entry via parseState + isError" idiom |
| **B-2** | `all()`'s tuple type lies — arity-1 unwrap (`parsers.length === 1 ? parsers[0].parser : fuseAll`) + drop-`undefined` (6 sites) make runtime arity data-dependent; G8's "no `opt` under `all`" ban misses three more `undefined` producers (`eof()` leaf.ts:14, empty-`regex` leaf.ts:342/:355, `matchFunction`-`""`) | leaf.ts:161 read live; drop sites re-confirmed in arity-2 and general arms; `any()`'s arity-1 arm is type-correct by contrast (Result = `V`) | **REFOLD** — widen parser-band.md:134 G8 + the W2 idiom gates to all four producer arms; tuple soundness by construction in the candidate algebra (W2 rider) |
| **B-4** | The published `whitespace` (`regex(/\s*/)`) **cannot match at EOF** — `regex()`'s first statement hard-fails at end-of-input before consulting the pattern, so a zero-or-more terminal rejects valid input one call from the default entry. **Seat riders (r1 rows re-verified at this seat's own read of the whole trim body)**: (i) `.trim(…, false)` returns `all(whitespace, this, whitespace)` at parser.ts:484-485 **before** the name guard — a `.trim()` call that IS broken, defeating reader-L's "only DIRECT uses are broken" scoping AND the challenge's own escape-hatch falsifier; the same branch is cast `as unknown as Parser<T[]>` — a second tuple lie; (ii) the flag path (:488) **never invokes the caller's parser**, so `.trim(customParser)` with the only legal whitespace-shaped name in the closed 29-union silently swaps in the ASCII scanner (`trimStateWhitespace`, charCodes 9-13/32 — not even `/\s*/`'s Unicode class) | leaf.ts:326-330 + :395-399; parser.ts:480-521 read whole by this seat | **W2** — RETIRE the `whitespace` singleton (already listed); B-4+riders are the reason-weight; any successor whitespace terminal carries a born-RED EOF-acceptance gate |
| **C-B2** (+M-9) | Nine of 34 exports are process-global latches published across **two independent module graphs** (ESM + CJS chunks each carry their own `PACKRAT_ARMED`, `diagnosticsEnabled`, `collectedDiagnostics`, `PARSER_ID`); the latch has exactly one write and it is `true` (:290, at CONSTRUCTION per the :284-290 design note); `resetPackrat` does not disarm; **no published reader** (M-9) | packrat.ts:156/:217/:266/:290 re-pinned by this seat's grep; dual-graph static per reader-C (`parse.cjs` requires distinct chunks) | **W2** — O-8 (`W2.md:253-257`, verified verbatim) forbids the construction by name; the WRAP-9 ruling (diagnostics + packrat as parse parameters) owns the cure; M-9's harness consequence lives at W1 G-4 (verified `W1.md:434`) |

### MAJOR — 30

| id | claim (compressed; proofs live in the reader returns + this seat's re-pins) | disposition |
|---|---|---|
| B-3 (demoted) | The non-string boundary is three-valued — throw (grammar-dependent, in a leaf, not `parseState`) / silent-reject / **silent-ACCEPT** (`eof().parseState([])` ok; boxed String parses) — `parseState` performs zero validation (parser.ts:34-49 read live); fenced by the string-typed entries (dist/parser.d.ts:10/:12, this seat's read) | **REFOLD** — W2 §3b's JS-boundary guard must be a **positive `typeof x === "string"` admission test**, never a catch; corrects W1.md:582 / W2.md:161-162's PT-07 readings |
| M-1 | `sideEffects:false` vs ~36 eager `Parser` constructions at barrel import (json.ts:15-52 + csv.ts:7-20 at module scope, read live; `PARSER_ID++` observable mutation); sub-count corrected: **one** `dispatch()`/Int8Array, json.ts:40 only | **W2** (RETIRE the showcases; the star line dies) · count → MEASURE-AT-OPEN |
| M-3 | `Parser.prototype.toString()` **throws** for any default-context parser (`{}` at parser.ts:31 → debug.ts default-arm `undefined` → `throw "parserPrint: missing parser context name"`, read live) — and the band's own cand-F `reject()` idiom builds exactly such parsers (probe :20) | **REFOLD** — DEBT-1 rider: a default-context parser must stringify totally in the candidate |
| M-4 (+C-M2) | The diagnostics feature is amputated: collectors published, the only renderers (`formatDiagnostic` debug.ts:200 / `formatAllDiagnostics` :235, read live) ship in **no** bundle; `dist/debug.d.ts` declares functions with **no runtime file** (`ls` run by this seat) | **W2** (D-limb: diagnostics are values WITH published renderers) · phantom `.d.ts` → **W4** release condition |
| M-5 | Recovery is O(n) per collected diagnostic (`utils.ts:107-113`: slice + lastIndexOf + line-array split, read live) → O(n·k) in the feature built to collect many; the "2k splits" rate killed (second split conditional on `secondarySpans.length > 0`, verified) | **REFOLD** — D-limb spec: newline index built once per parse on the threaded state |
| M-6 | Dead flag machinery: `FLAG_EOF` never written (grep: :22 def, :466 read only — this seat), `call()`/`unsafeCall` zero live callers, `.trim()` allocates a flagged parser it throws away (:489-497 vs the :514-517 return, read live); the "eighth field" shape claim killed (5 fields) | **NO-WAVE-OWNER** (parse-that hygiene; the substrate-deadcode precept applies) |
| M-7 | `regex()` at EOF skips `mergeErrorState` (leaf.ts:326-330, no merge call — read live) — `furthest` stalls, the label drops; truncated-input failures systematically under-report the expected-set | **W3** — recovery/diagnostics-to-spec obligation |
| M-8 (+C-M3) | PT-01 sharpened: arming welds an unconditional per-failed-parse `console.error` + full-source split (parser.ts:67-69 read live → debug.ts:58); `mergeErrorState`'s label is a no-op on the shipping default (utils.ts:33); `isDiagnosticsEnabled` withheld from every entry; `ParserState.toString()` is the same O(n) path **unarmed** | **W2** — R-LAW-3 (verified `W2.md:281-283`) owns it; unarmed-`toString` arm as REFOLD rider |
| M-10 | `Parser.state` retains the parsed source forever (public field, writes :66/:71, read :68, zero clears) including the module-singleton `jsonParser`/`csvParser` — a shared mutable per-instance slot on the parse path | **REFOLD** — O-8 sharpener: the anti-latch law must also forbid instance-level retained parse state |
| M-11 | `ParserFunction<T>` is a phantom generic (T nowhere on the RHS — src :14-16 AND dist/parser.d.ts:2, both read by this seat); all ~26 casts against it are decorative — the seam-blindness that let B-2 ship; cite-list corrected (register #12) | **W2** — typed-seam obligation of the candidate algebra |
| M-12 | `mergeErrorState` allocates two arrays per furthest-advance, ungated (utils.ts:34-35, read live), on the failure path; gating would be unsound (recover reads them unarmed) — cure is `length = 0` | **NO-WAVE-OWNER** (the algebra replaces the function) |
| M-13 | `index.ts:14` creates a real import cycle (three `from "../index.js"` back-edges, two dead); safety rests on unpinned statement order; the TDZ crash is **CONFIRMED by static derivation** (class-TDZ vs function hoisting — reader-L's upgrade adopted) | **REFOLD** — fresh-root module-graph law: no back-edges through the barrel; order-independence gated |
| M-14 | No error type anywhere: five failure channels, none typed; thrown messages leak internals and **vary by grammar** (`charCodeAt`/`startsWith`/`.length`/`.substring` — leaf sites read live) | **W2** — the typed failure contract is the cure-by-construction |
| C-M1 | `export *` leaves 5 of 34 runtime exports outside the publish-discipline gate's regex in both directions (dist-surface.test.ts:21, read live: `/export\s*\{([^}]*)\}/g`) — the exact 0.8.2 blindness the repo cured in the sibling CSS gate and left here | **W4** — gate law: derive the surface, never regex it |
| C-M4 | `createParserContext` is published against a closed 29-literal union (state.ts:141-171, counted live) that no downstream grammar can extend — consumers cannot legally name a custom parser (the cand-F idiom is a compile error); consequence-2 killed (register #7) | **REFOLD** — DEBT-1: labelling must be open and first-class |
| C-M5 (rescoped) | Every RED row of the band's surface-gaps probe is reached through an `index.ts` export — RED-7, 7-of-7 non-tautological, re-derived from source by both readers; **unrunnability arm STRUCK by this seat** (the probe resolves from its own workspace — instruments above); probe-line cites corrected (register #17) | **W1** — the probe is W1's instrument (G-4/G-5/G-9/G-10) · run-receipt → MEASURE-AT-OPEN |
| C-M6 | `/core` ships the tier it disclaims: `parseState` calls `packratEnter/Exit` unconditionally (parser.ts:43-48, read live), so the 40,576 B packrat-entry chunk reaches every entry but `/diagnostics` (dist/core.js:1 read by this seat); the fan-in is four-way at dist/parse.js:1-4 (+ `/utils` also imports the chunk) | **W2** — O-8 wrap: memoization as a parse parameter is the only cure that keeps the tier promise |
| C-M7 | `ParserState` welds an ANSI terminal renderer into every consumer bundle via `toString` → `statePrint` → ansi.ts — unshakeable because it is a method on the exported class; meanwhile the *wanted* renderers are absent (M-4): the inversion | **W2** — R-LAW-3 corollary: no renderer on the state class |
| C-M9 (+AD-14 rider) | `index.ts:7`'s `lazy` is a TS-4.x experimental method decorator (lazy.ts:30-43, read live: mutates `descriptor.value`) that cannot compile under the package's own TS 5.8/no-experimentalDecorators config, has zero uses, does **not** cache what its doc claims (each call mints a fresh Parser + closure — this seat's read), and carries a second phantom generic (`lazy<T>`, T nowhere — dist/lazy.d.ts:9 read live) | **W2** — RETIRE all three lazy exports; DEBT-3 (bounded back-edge as algebra parameter) is the successor |
| C-M10 | The barrel fan-in prices leaf-pure exports (~400×): `containsDelimiter` (3 lines, zero imports) drags the 40 KB chunk through `.` — structure confirmed static (core.js:1 unconditional); byte figures carried, not re-measured | **W2** (RETIRE split.ts from the barrel) · cells → MEASURE-AT-OPEN |
| C-M11 (+L·M-2 residue) | `sideEffects: false` is false of the shipped bytes (ansi's top-level `process.stderr` read; `_initWhitespace()` at parser.ts:711) and manifest-gate mandates the flag while never *reading* a dist byte (stats only — reconciled above): it cannot fail for its intended reason (L-19) | **W4** — release condition: manifest claims checked against artifact bytes |
| C-M12 | `Parser.recover()` writes the process-global journal **unconditionally** (parser.ts:666 — the sole ungated diagnostics behaviour, verified against the three gated siblings) and retains the entry on success (:681) — unbounded cross-parse growth (W2 K-6's "parse #100,001") | **W2** — R-LAW-4/K-6 own it as a kill condition |
| C-M13 | `getCollectedDiagnostics()` returns the live array; `clearCollectedDiagnostics()` swaps the backing store (utils.ts:138-144, read live) — a captured handle mutates underfoot, then silently detaches | **W2** — D-limb value semantics |
| C-M14 | The accumulator is published, its inverse unreachable: `popLastDiagnostic`/`resetErrorState` withheld though `recover()` itself depends on them (parser.ts:674 / utils.ts:127) — the rollback shape is unreproducible downstream | **W2** — D-limb |
| **AD-1** (reader-L miss, adopted — verified at this seat's read) | `regex(r, matchFunction)` can advance the offset **NEGATIVELY**: a failed sticky `exec` resets `lastIndex` to 0; a matchFunction returning non-empty on `null` (its **declared** contract, leaf.ts:319) yields `state.ok(match, 0 − savedOffset)` (leaf.ts:340) — rewinding to 0; `many`/`sepBy`'s `===` zero-advance guards are blind → unbounded loop + array growth from a published leaf. Falsifies S-2's unqualified form. **Rider (r1's NEW-H, same seam, verified)**: `sticky.lastIndex` is written by `exec`, then *read after the user callout returns* — a re-entrant matchFunction silently corrupts the advance | **W2** — KEEP-rider: regex is KEEP; the matchFunction seam is banned or guarded in the candidate; S-2 rescoped |
| **AD-2** (reader-L miss, adopted — verified, and broadened at this seat's read) | `escapedString()`/`quotedString()` are silently LOSSY: **every** escape is undecoded, not just `\uXXXX` — the map returns the escape *letter* (`"\n"`-literal → `"n"`; `A` → `"u"` with four hex digits consumed and discarded via `.skip`'s left-value return). **Rider (r1's NEW-L, verified)**: `quotedString(quote)` interpolates the caller's string unescaped into a RegExp character class (parsers/utils.ts:16) — throws or silently builds a different grammar. Contrast: json.ts:22-23's showcase decodes correctly via `JSON.parse` | **W2** — RETIRE-listed; this is the reason-weight |
| **AD-3** (reader-L miss, adopted — verified at this seat's read) | `csvParser` cannot separate rows: no newline production at any level (csv.ts read whole — `[^,]+` eats `\n`; both `.trim()`s route newlines into whitespace) → a multi-row document returns as ONE row, then `many()`'s zero-advance break; the "spec-grade combinator example" (parsers/index.ts:4) returns wrong answers on its headline input class | **W2** — RETIRE reason-weight |
| **AD-4** (reader-L miss, adopted — verified at this seat's read) | `mapState` hands user code a **prototype-chained live alias** of the NEW state as `oldState` (parser.ts:176-180: `Object.create(state)` + own `offset`/`value` only) — `src`/`isError`/`furthest`/diagnostics read through to the post-parse state; typed `ParserState<T>`; also mints a second hidden class, scoping S-4's "one shape" to the default path | **W2** — KEEP rider: `mapState` excluded or cured |
| **AD-5** (reader-C miss, adopted — verified at this seat's read) | Shipped **basename collisions**: `dist/utils.js` (the `/utils` entry bundle — 7 showcase names, export block read live) vs `dist/utils.d.ts` (declares the diagnostics/scanner module — intersection **2 names**); same structure for `packrat.js`/`packrat.d.ts`. Inside one tarball the specifier `./utils.js` resolves to two different modules (`dist/index.d.ts:4` types-side vs `dist/parse.js:4` runtime-side — both read live); only the explicit `types` keys save modern resolvers; basename-pairing tools get wrong declarations in both directions, with no error to warn them; no gate compares a subpath's `types` against its `import` | **W4** — release condition |
| **AD-6** (reader-C miss, adopted — verified at this seat's read) | The downstream already **shipped the WRAP-9 cure**: value.js `/css` returns diagnostics as VALUES — `ParseResult<T>`'s failure arm is `{ok:false; diagnostics: readonly [ParseIssue, ...]}` with **no value key** (src/css/types.ts:25-27, read live; `W2.md:292-295` holds it load-bearing). Consequence: challenge-C's "no usage evidence left to appeal to" method note is wrong — a live reference implementation of the W2 D-limb sits in the same repository; and C-B1 reframes from unexplained severance to **a contract migration whose paper trail is the type, not the changelog** | **REFOLD** — evidence row for W2's D-limb; corrects the challenge's §6 method note |

### MINOR — 29

| id | claim (compressed) | disposition |
|---|---|---|
| C-M15 (demoted, re-grounded) | `Suggestion`/`SecondarySpan` published; the *sinks* are public fields (this seat's read — the "no sink" refinement is dead); what is withheld is the arming discipline (gated emitters + `reportUnclosedDelimiter`) | **W2** (D-limb) |
| C-M8 / m-8 (merged — ONE identity, MINOR) | Span residue: `Span`+`spanToString`+`mergeSpans` zero-producer/zero-consumer (this seat's grep: only core.ts:11-12 + index.ts:3 re-exports); two dead `import type`s; the S.H2 excision incomplete in the file documenting it; C's gate-blindness aggravator already charged at C-M1/C-m6 — no double-billing | **W2** (RETIRE-listed) |
| L·M-2 residue | `export let whitespace` + module-scope `_initWhitespace()` under `sideEffects:false` — the false-declaration half survives (rides C-M11); the rollup-drop crash half is KILLED (register #4: the victim is self-cancelling) | **W4** (rides C-M11) |
| m-1 | Three line/column APIs on `ParserState` disagree in base AND anchor (state.ts:111-134, read live); two have zero callers (this seat's grep) | **W2** KEEP rider |
| m-2 | Two dead arrays per `ParserState` construction (:44-45, written only when armed); `clone()` copies 5 of 8 fields — drops accumulated diagnostics (LR-grow loss) | **NO-WAVE-OWNER** |
| m-3 (+reader-L miss #5 merged — verified arithmetically) | `dispatch()`'s key DSL: unbounded intern (O(n²) `indexOf`); idx 128-255 wraps negative → silent rejection of a registered char; **idx ≥256 wraps POSITIVE → routes to the wrong parser** (Int8 conversion is modular); non-ASCII keys silently discarded at the typed-array write; a literal `-` in a 3-char key is inexpressible (leaf.ts:113-124, read live) — the fail-silent posture `getCijKey` explicitly rejects one file over | **W2** KEEP rider (fail-loud guard posture) |
| m-4 | Five hand-maintained export lists over one 34-symbol set; the subpath gate asserts three symbols | **W4** |
| m-5 | `utils.ts` is two unrelated modules fused (the :150-156 banner admits it); `/diagnostics` chunk drags the byte-scanners | **NO-WAVE-OWNER** |
| m-6 | `parserPrint` mints a duplicate graph for lazy parsers (two disjoint caches: closure-local vs module WeakMap) + `PARSER_STRINGS` never evicts (cites corrected, register #16) | **NO-WAVE-OWNER** |
| m-7 | `getLazyParser`'s `undefined` sentinel conflates not-cached with cached-`undefined` (lazy.ts:8-9, read live); `WeakMap.has` is free | **W2** (RETIREs with the tier) |
| m-9 | Dead `Parser` value-imports (csv.ts:5, parsers/utils.ts:4 — both read live, zero uses), emitted under `verbatimModuleSyntax`, deepening M-13's cycle; falsifier corrected to word-boundary grep (register #14) | **NO-WAVE-OWNER** |
| m-10 | `whitespace.context.name` is public mutable state (leaf.ts:398; `context` public; no freeze) that reroutes every `.trim()` package-wide onto the B-4-broken fallback | **W2** (RETIRE + trim rider) |
| m-11 | `.parse()` accepts a prefix and reports success; composed with B-1, three outcomes share one return value | **REFOLD** (entry contract: total-input or exposed rest) |
| m-12 | PT-04's depth ceiling ships unbounded/unparameterised/thrown; the DEBT-3 debt is owed one layer below where the band assigned it (C-3 re-homing) | **REFOLD** |
| C-m1 | `getLazyParser`/`createLazyCached` are internals of an unpublished tier, published first-class; falsifier corrected — createLazyCached has TWO internal callers incl. lazy.ts:39 (verified at this seat's read) | **W2** (RETIRE) |
| C-m2 | `skipWhitespace`/`skipBlockComments` retained explicitly for "value.js's canonical CSS grammar" (utils.ts:150-156, read live) — which no longer imports the package; `skipBlockComments` is CSS logic that cleared the excision gates by name-list evasion | **W2** (RETIRE/re-home) |
| C-m3 | One file's three types routed through two barrel lines/provenances (state.ts declares all; index.ts:4 vs :6) | **NO-WAVE-OWNER** |
| C-m4 | `./package.json` not in the exports map (five keys, no pattern — read live) — version unreadable through the package's own surface | **W4** |
| C-m5 (rescoped) | Two genuinely orphaned published `.d.ts` (`ansi`, `debug` — the latter a runtime phantom); the other internal `.d.ts` are load-bearing via relative type re-export (this seat's read of index.d.ts specifiers) | **W4** |
| C-m6 | 29 never-importable runtime exports vs 34 reachable (46%), policed by a two-name ban list — **+ the C-B1 residue**: the dead-export precept's own gate does not apply its precept to its own barrel | **W4** |
| **AD-7** (reader-L miss #6, adopted — this seat's grep came back empty) | 7 of `ParserState`'s 13 methods dead or wrong (`err`/`from`/`save`/`restore`/`unsafeCall` zero callers; m-1's two inconsistent); `restore()` clears `isError` unconditionally — a semantics no caller asked for; the C-m6 census structurally cannot see class members | **W2** KEEP rider |
| **AD-8** (reader-L miss #7, adopted — verified) | `recover()` → `collectDiagnostic` → `resetErrorState` sets the LIVE enclosing parse's `furthest` to −1 mid-parse (utils.ts:127/:131-136) — after a successful recovery, the top-level error position re-derives from whatever fails next, possibly earlier; deliberate for the journal, blast radius the whole parse; ungated by the flag | **W3** |
| **AD-9** (reader-L miss #8, adopted — verified; zero callers confirmed by this seat's grep) | `map(fn, mapError = true)` launders every failure into a success (parser.ts:150 + `ok()`'s unconditional `isError = false`) — the published second parameter deletes the last remaining failure signal | **W2** KEEP rider (exclude or retire the parameter) |
| **AD-10** (reader-L miss #9, adopted — mechanics verified at debug.ts) | `parserPrint` uses falsy-zero id sentinels (`if (!id)` / `if (id)`) against a counter that starts at 0 — the process's first Parser skips the memo write and the lazy cycle-break; narrow reachability, same publicly-reachable function as M-3's throw | **NO-WAVE-OWNER** (recorded beside M-3) |
| **AD-11** (reader-L miss #11, adopted — verified: no try/finally) | `debugDepth++`/`--` straddle the wrapped parse with no unwind (debug.ts) — any throw permanently biases all later debug indentation; the same package gets this exact hazard right twice (parser.ts:43-48, packrat's epoch), both praised by S-3 | **NO-WAVE-OWNER** |
| **AD-12** (reader-C MISS-3, adopted) | The star line routes two parser SINGLETONS (module-scope, built at import, pin their last input per M-10) and three FACTORIES (fresh sub-graph + per-call `new RegExp` per invocation) through one indistinguishable line — opposite lifetime hazards, zero signal at the surface | **W2** (RETIRE) |
| **AD-13** (reader-C MISS-4, adopted — verified: `jsonParser: Parser<any>` at dist/parsers/json.d.ts) | `JsonValue` is published while its only producer ships `Parser<any>` (json.ts:40's eslint-disabled `any`) — the type can never reach a consumer's inference; the third half-export instance (with C-M2, C-M15) | **W2** (RETIRE) |
| **AD-14** (reader-C MISS-5, adopted — verified at dist/lazy.d.ts:9) | `lazy<T>` is a second phantom generic on the published surface — two inert type parameters on a 34-export surface is a pattern, strengthening M-11 | **W2** (rides C-M9) |
| **AD-15** (reader-L miss #10, adopted) | `class Parser` spans 685 of parser.ts's 711 lines, 25 methods, no internal seam — the source-level reason C-M6's tier promise cannot be kept (its import set rides with it into every entry); the constellation's no-god-modules precept, unenforced upstream | **REFOLD** (candidate design law: seams before tiers) |

### INFO — 5

| id | claim | disposition |
|---|---|---|
| C-B1 (as rescoped — this seat's ruling) | The consumption **denominator**: value.js-proper consumes zero of the 34 (dependency deleted at `164343c1`); the sole live consumer is the adjudicated css-parser workspace (installed 1.0.0, byte-identical, 21 importing files); the re-add is decree I-11 §2 — **tracked, gated, unexecuted** (denominator.test.ts:80-87) | **W4** (the rebind/release seam records both the cut and the re-add) |
| i-1 | The barrel spends 3 of 14 lines on a tombstone naming a live gate (`proof:no-span-surface`, package.json:46) — provenance, not clutter; and m-8 shows the excision it documents was itself incomplete | **NO-WAVE-OWNER** |
| C-i1 (+L·C-2, merged; weight ruled above) | O-15's `:881` cite is the guard line of its own one-line collapse — a rendering artifact, not a mis-cite; `:262` is a second, opt-in `console.error` (parserDebug's default logger ≠ `Parser.debug`'s `console.log` — verified) | **REFOLD** (correct the letter's cite only where downstream texts quote it) |
| C-i2 | value.js 4.0.0 §Breaking (:15-21) omits the parse-that dependency removal — with AD-6 in evidence, the record to correct is precisely this: the type is the paper trail, the changelog still owes the sentence | **W4** (record with the rebind) |
| C-i3 | No legacy resolver fallback (`main`/`types`/`module` absent) — a declared, gate-documented trade-off | **NO-WAVE-OWNER** |

### Carried from r1 (absorbed per the supersession record; not re-derived this session unless marked)

| id | claim | status · disposition |
|---|---|---|
| r1·NEW-C | `/utils` is strictly worse than the root for the tier it serves (its entry constructs the json/csv grammars at top level; ~36 KB under both postures) | structure static-plausible; cells → **MEASURE-AT-OPEN** |
| r1·NEW-F | Pre-sized result arrays ship HOLEY elements kinds (`new Array(n)` in fuseAll's general arm + many/sepBy est-paths — the allocation sites re-read by this seat; the *kind* claim and its magnitude are V8-runtime) | static fact stands; magnitude → **MEASURE-AT-OPEN** (the corpus's only true bench row) |
| r1·NEW-I | Six `eslint-disable @typescript-eslint/no-explicit-any` directives for a linter that is not installed (devDependencies read whole by this seat: no eslint) — every `any` reads as reviewed and none was | **verified this session** · NO-WAVE-OWNER |
| r1·NEW-J | `dist/parse.cjs` — the root `require` target — is opened by no gate in `proof:all` (manifest-gate stats `index.d.ts` only; subpath-gate imports ESM); a CJS formats regression ships green | plausible at cite level · **W4** |
| r1·NEW-M | `parserPrint` recurses per graph node with no depth bound — m-12's ceiling on a second mechanism, in the one debug path `toString` makes public | plausible (recursive `print` read this session) · NO-WAVE-OWNER (beside M-3) |

## Killed-claims register — 21

1. **C-B1's severance arms** ("empty consumer set" / "not even resolvable" / "recorded nowhere" / BLOCKER) — **SEAT KILL, overruling BOTH r2 readers**: the css-parser workspace declares, installs, and resolves the published 1.0.0 byte-identically; 21 files import it; the cut is executably tracked as decree I-11 §2. The challenge's falsifier fenced `docs/` on the strength of the false "cannot execute" support. Fenced residue survives (INFO row).
2. **C-M5's unrunnability arm** — **SEAT KILL, overruling both readers**: wrong-root measurement; the probe documents its own resolution root and that root resolves.
3. **C-M15's "no published sink" refinement** (reader-L) — **SEAT KILL**: `state.ts:44-45` are public non-readonly fields; the sink ships with the class. Row demoted to MINOR on the corrected ground.
4. **L·M-2's crash half** (rollup drops `_initWhitespace()`) — reader-L's kill adopted: `moduleSideEffects:false` retains the initializer of a reachable live binding, and `whitespace` is reachable from `Parser.trim`'s default parameter — the only graph that can drop it has no victim. Self-cancelling scenario.
5. **B-3's framing** ("O-15 PT-07's 5/5 is false — every clause false") — both readers' kill adopted: the probe pins `string("a")` + five inputs and all five die at `charCodeAt`; challenge-L's own M-14 pass reproduces 5/5, refuting its own B-3 framing. What survives is a widening (the silent-ACCEPT arm), scored above.
6. **B-1's corpus action** ("widen W2.md:294's prohibition") — both readers' kill adopted: the ratified failure arm carries no value key at all (`W2.md:292-295`, and shipping at value.js `src/css/types.ts:25-27` — both read by this seat); the class is foreclosed by construction.
7. **C-M4's consequence 2** ("the union is unreconstructible downstream") — reader-C's kill adopted, verified at `dist/state.d.ts:64/:71` (tuple declared; union inlined; indexed access reconstructs it).
8. **C-M11's premise sentence** ("never opens dist/") — reader-C's refutation adopted as a *narrowing* (Gates 1+3 stat dist paths; none reads bytes); the L-19 conclusion survives on the true ground.
9. **C-m5's scope** ("~11 dead `.d.ts`") — reader-C's kill adopted, verified: the internal `.d.ts` files are load-bearing via `dist/index.d.ts`'s relative re-exports; orphans = `ansi.d.ts` + `debug.d.ts` (2).
10. **M-1's "2 Int8Array(128)"** — one `dispatch()` at module scope in the package (json.ts:40; csv.ts constructs none — both files read whole by this seat). The 36-instance count stands.
11. **M-6's "eighth field" shape arithmetic** — Parser has FIVE fields (parser.ts:25-31, read live); the 8-field object is ParserState. Deadness claim untouched.
12. **M-11's cast-line list** — `:703` carries no cast; the omitted site is `:493` (inside M-6's own dead closure); 26 correct by offsetting errors; 28 tree-wide.
13. **M-5's "2k full-source splits"** — the second split is conditional on populated `secondarySpans` (armed + unclosed-delimiter only — verified at debug.ts); k splits unconditional; a ceiling was presented as a rate.
14. **m-9's falsifier as written** — the naive grep returns two hits per file (`csvParser`/`numberParser` substring-match); the word-boundary grep confirms the finding.
15. **C-M12/C-M14's load-bearing cites** — `:666`/`:681`/`utils.ts:127` (re-pinned by this seat's grep); the two challenge files disagree with each other on the same line.
16. **m-6's PARSER_STRINGS grep** — `257, 258` not `258, 259`; no-eviction claim re-verified.
17. **C-M5's probe-line table** — non-uniform drift (+1/−4); corrected lines `:22/:25/:26/:36/:37/:56/:57`, record row `:48`, resetPackrat `:47`. Substance re-derived at source either way.
18. **Both files' type-export census** ("6") — SEVEN: `JsonValue` rides the star line (parsers/index.ts:6, read live). The miscount is a live instance of C-M1's own diagnosis.
19. **Both files' package.json cite blocks** — the 70-line manifest read whole with line numbers by this seat: `sideEffects` :6, `exports` :7-33, scripts :34-50 (proof:* :39-49), `files` :51-53; `:57` is `@types/parsimmon`. Opposite-direction drift = transcription, not re-reading. Every resting claim independently confirmed.
20. **C-i1's "corpus correction" weight** — reader-L's kill adopted (rendering artifact; the letter cites the line it quotes); graded INFO.
21. **C-m1's falsifier** — createLazyCached has TWO internal callers (parser.ts:704 AND lazy.ts:39 — verified); the row's substance survives on the corrected instrument.

## Superlatives — held to the same standard

- **S-1** `getCijKey`'s float64 key budget: whole-offset addition, 53-bit reasoning sound, fail-loud `RangeError` at the boundary, both prior defects documented in situ. Survives both re-reads — and is the standing indictment of m-3's silent wrap two files over.
- **S-2** zero-advance guards — **SCOPED by AD-1**: holds as "no zero-advance hang"; false as "neither combinator can hang" (the negative-advance seam passes every `===` guard).
- **S-3** the packrat re-entrancy unwind + `growLR`'s cap-free termination — verified independently by reader-L; the design note's claim *verifies* rather than asserts. The best thing in the package.
- **S-4** `ParserState`'s stable shape — **SCOPED by AD-4**: a property of the default path; `mapState`'s `Object.create` mints the second shape and hands it to user code.
- **S-5** `dispatch()`'s EOF (`NaN < 128` → false) and non-ASCII read arms both correct; label precomputed. Survives — with the recorded asymmetry: the WRITE path silently discards what the read path handles (m-3 merged row), and the `.fill(-1)` sentinel (elided by m-3's own quote) is the third absent bug at the same site.
- **S-6** `fuseAll`'s unrolling is an honest allocation win; `any()`'s arity-1 arm is type-correct — the exact contrast isolating B-2 as a type lie, not a design necessity.
- **S-7** two subsystems, two deliberate soundness strategies (per-state diagnostics vs global-with-epochs packrat), each explained at the site; `collectedDiagnostics` (utils.ts:95) is the honest recorded exception — and AD-1's sticky-`lastIndex` seam is the counter-example one layer down.
- **C:S-1** 34/34 source↔dist parity in both formats, gated (export-block count re-run by this seat: 34).
- **C:S-2** the CJS build wraps exactly the ONE mutable binding in a getter (`parse.cjs:25-28`, read live) — the correct discriminating treatment of `export let` most barrels get wrong.
- **C:S-3** the Span excision triple-gated with an in-file citation trail (14-line file, 3-line provenance).
- **C:S-4** zero renames/default/namespace re-exports; the subpath gate reads the manifest instead of duplicating it.
- **C:S-5** type-export hygiene exact at every type position under `verbatimModuleSyntax` — at all SEVEN positions, the seventh (`type JsonValue`, parsers/index.ts:6) equally correct.
- **C:S-6** the PT-03 latch is statically eliminable for memoize-free graphs — the one corpus finding that moves toward the defence; premise verified (sole write inside `makeMemoized`); the fold itself → MEASURE-AT-OPEN.
- **NEW (reader-L)** `sepBy`'s pre-separator checkpoint rejects trailing separators in both directions with the contract stated in its doc comment (parser.ts:594-611 — re-read by this seat) — rarer than the packrat unwind.
- **NEW (reader-L)** `regex()`'s default path is allocation-free by construction (`test()` over `exec()`, inlined ok, one substring — leaf.ts:344-353, read live) — the single highest-leverage allocation decision in the package, and correct.
- **NEW (reader-L)** the `export let` + post-evaluation `_initWhitespace()` cycle break is the right *shape* for a genuine ESM cycle — the defect is the cycle (M-13), not its cure; both challenge files read a correct solution as an impurity.
- **NEW (reader-C)** `proof-no-dead-combinator.mjs` carries an explicit **VACUITY GUARD** (:50-59, read live: a known-live export must still match, else "fix the gate before trusting it") — precisely the L-19 property C-M11 finds absent in manifest-gate, present one directory over. The discipline exists; it was applied unevenly.
- **NEW (reader-C)** the barrel's ten alias-free export statements make the entire 34-symbol surface derivable by grep — the property that made a latch-safe, execution-free audit of this package possible at all.

## MEASURE-AT-OPEN residue (for the X·P.W1 harness; deterministic unless marked bench)

1. M-1's import-time construction count (37-vs-1; runtime import probe, fresh process).
2. C-M10's tree-shake byte cells (36,359 / 37,142 / 32,782 / 118 / 901 / 304 — carried by both readers as receipts, never re-measured in r2) + r1·NEW-C's `/utils` cells; re-pin against the wave's dist.
3. The rollup-retention probe for L·M-2/C-M11 (r1 reports a four-cell run at rollup 4.59.0; re-pin at wave open).
4. C:S-6's fold-to-zero (`grep -c PACKRAT_ARMED` → 0 on a memoize-free `/core` bundle).
5. O-15 PT-03's 1.47× and PT-04's 7,761-frame ceiling — cited receipts, never re-measured by any seat in this corpus; W1 G-9 re-establishes them.
6. C-M5's RED-7 executed **from the css-parser workspace** (proven resolvable by this seat) — turn both readers' derived tables into one run receipt; unarmed run only (the probe's no-`--arm` mode).
7. AD-1's negative-advance loop — statically certain; an optional two-line witness at wave open makes it exhibit-grade (bounded iteration guard; no arming). r1·NEW-F's holey-vs-packed magnitude (**bench** — the corpus's only true bench row).

## DISSENT — preserved, not ruled away

1. **The r2 citation-inheritance event, symmetric and double.** Both r2 readers confirmed C-B1 and C-M5's unrunnability by re-running the challenge's own commands at the challenge's chosen root — faithful reproduction, wrong scope, while the consumer sat one directory below the probe declared dead. The ColorSpaceSelector addendum's law restates itself: *a claim about a scope includes the scope*, and its corollary here — **a falsifier that fences on a factual support inherits the support's truth value**. The r1 seat found this independently with different readers; two rounds, four readers, one blind spot, twice killed by seat instruments.
2. **Severity dissents preserved**: reader-C holds B-1/B-2 at MAJOR (typed-entry steering; non-default arguments); reader-L holds B-3 at BLOCKER (the boundary contract, not the type fence, is the library's to keep). Both recorded; this seat's grades above are the rulings.
3. **reader-L's C-i1 posture** ("pedantic; not worth filing") vs reader-C's INFO row: ruled INFO — but reader-L's underlying point (a letter that collapses two lines cites the line it quotes) is adopted as the *reason* the row is INFO and not a correction.
4. **Grading dissent on C-B1's residue** (reader-C would file the changelog silence as MINOR): held at INFO — with AD-6 in evidence the silence is a documentation gap about an executed design act, and the actionable residues are booked at C-i2 and C-m6.
5. **Law compliance of the whole corpus was impeccable** — STOP-checks, latch hygiene, single-write discipline, honest hedges (M-13 hedged a claim it could have won; M-2 wrote the falsifier that later demoted it; C-M11 killed its own harm half at the point of claim). That discipline is why 68 rows survived and 21 claims died, fifteen of them cite-level.

## Closing verdict

The module is **DEFECTIVE AT THE PUBLISHED BOUNDARY — adjudicated, with an interior genuinely above the median**. Four blockers survive two hostile re-reads and this seat's incredulity: a headline `.parse()` that can return a truthy, type-correct wrong answer on a routine failure (B-1); a tuple type whose runtime arity is data-dependent (B-2); a zero-or-more whitespace terminal that rejects valid input at EOF — reachable through `.trim(…, false)` as well, defeating the corpus's own escape-hatch claim (B-4 + riders); and nine latch exports duplicated across two module graphs in exactly the construction O-8 forbids by name (C-B2). The two demotions are this record's spine: **C-B1 dies as a blocker because the barrel has a live, installed, byte-identical, adjudicated consumer** — the css-parser workspace — and the value.js cut is a tracked decree with a passing gate and a shipped successor contract (AD-6), a fact both r2 readers missed by re-running the challenge's commands at the challenge's root and this seat killed with its own instruments; **B-3 narrows to MAJOR** because every arm of the three-valued boundary requires the consumer to violate the string-typed entry the package ships. Routing: **32 rows to W2** (the algebra owns the failure contract, the WRAP-9, the RETIRE list, and the KEEP riders), **12 REFOLD** (the PT-07 corrections, the positive string-admission boundary, the G8 widening, the O-8 instance-state sharpener, DEBT-1/DEBT-3 re-homings, the seams-before-tiers law, the D-limb reference implementation), **1 to W1** (the probe, now proven runnable), **2 to W3** (truncated-input and recovery-position diagnostics), **10 to W4** (manifest truth, gate derivation, basename collisions, the rebind record), **11 NO-WAVE-OWNER** (parse-that's own hygiene communique), plus the r1 carries. Nothing here executes anything: X·P remains owner-gated behind W0's begin-word, and every disposition above is a routing, not a begin-word.

— end of apotheosis, round 2. The two challenge files and both r2 reader returns remain immutable beside this record per E-1/E-3; round 1's substance is absorbed above per the supersession record.
