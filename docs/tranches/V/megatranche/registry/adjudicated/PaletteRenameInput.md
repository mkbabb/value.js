Served model id: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited (M-23/L-18 receipt).

# PaletteRenameInput — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter)

**Seat**: fresh Fable adjudicator — the incredulity seat over two hostile re-readers.
**Date**: 2026-08-06 · **HEAD**: `3ea0144c` · branch `tranche-u` · **Component**: `demo/palettes/browser/card/PaletteCard/PaletteRenameInput.vue` (66 lines, SHA-256 `97483747…` per all three challenge receipts).
**Corpus**: challenge-C (6 findings) · challenge-D (5) · challenge-L (4 + target structure), all SOURCE-RED — r2 governs; the three files in `audit/components/PaletteRenameInput/` are the governing set.
**Readers adjudicated**: R1 (design-and-usage) · R2 (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement was re-derived by this seat against the tree with its own instruments.
**Arbiter's own instruments (all static; zero browser tooling — task law + probe parsimony)**: full reads of PaletteRenameInput.vue (whole) and PaletteCard.vue (whole, 364 lines incl. the scoped `.rename-morph` block); useBrowsePalettes.ts:55-185 (loadRemotePalettes / loadMoreRemotePalettes / onSortChange / onVote / onDeleteOwned / onRename); BrowsePane.vue:85-119 (the keyed v-for + the 16 card bindings); demo/DESIGN.md:95-123 (the material ladder, THE REGISTER LAW W3-3, the depth law RC-2); demo/styles/utils.css:105-155 (`.search-seated` + the font-inherit voice rule); demo/styles/animations.css:100-140 (vj-morph family) + :184-193 (PRM guard); installed glass-ui dist: `.input-bar` rule verbatim (floating border/bg/backdrop-filter, `height: var(--control-pill-h, var(--control-h-sm))`, `max-width: var(--max-width-input)`), `.input-bar-field` (`font-family: var(--input-bar-font, var(--font-mono, monospace))`), `--control-h-sm: max(calc(2.25rem*var(--ui-scale)), var(--control-floor))`, `--max-width-input: 24rem` (offsets.css), the `pointer: coarse` override (`--ui-scale: 1.5`, `--control-floor: 44px`), `useLiquidPress` dist (`onKeydown` pressing on Enter/Space/Spacebar, `onPointerdown` on button 0; d.ts handler roster :68-75); CurrentPaletteEditor.vue (aria-labelled commit/cancel pair, the S.W5-3 producer-Input comment, the `"…already exists."` banner, `saveCurrentPalette` name derivation); SlugEditLayer.vue:76-100 (the third inline-edit skeleton: DockControl + aria-label + `:disabled` pending lock + Loader2); PaletteCardMenu.vue:70-80 (the rename item); usePaletteActions.ts:60-63 (`onRenameSaved`, local/synchronous); api/src/modules/palette/schema.ts:25 (`paletteNameSchema = z.string().trim().min(1).max(100)`); demo/palettes/api/palettes.ts:91-110 (If-Match PATCH + `renamePalette`); `demo/ui/input/index.ts` (`export { Input } from "@mkbabb/glass-ui/forms"`) + `dist/index.d.ts` root-barrel omission of `./components/input` (grep count 0) + `ls dist/components` (button, input, label, labeled-field present); maxlength census (`FlagReportDialog.vue:33` sole demo hit, at `browser/dialog/` — R1's path cell was stale by one directory); motion-quarantine.md rows M-17 (FALSE, binding), M-18 (UNPROVEN), M-19 (TRUE), M-03 (the scope-shopping precedent); PaletteCardSwatches.vue W5-a11y comments :12/:40 + aria-label counts (5, and 1 in PaletteCardMeta).
**Wave vocabulary** per `registry/CARRY-CUT-LEDGER.md` and the CurrentPaletteEditor apotheosis: X-W1 gates · X-W4 semantic controls/action ownership · X-W5 scene/typed-action contract · X-W6 temporal scenes · X-W7 palette specimen/domain/CRUD · X-W8 graph/config subtraction · X-W10 design/motion law.
Line anchors are HEAD `3ea0144c`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

## Scoreboard

**21 adjudicated rows: 7 MAJOR · 11 MINOR · 3 INFO. Kills upheld: 4 (1 as-modified) · kills overturned: 1 (R1's C-1 kill). 1 reader superlative corrected for sign (R1's `.input-bar` praise). All three axis verdicts (SOURCE-RED) AFFIRMED — but the heaviest defects in this record entered through the re-readers, not the axes: all three axes stopped at line 66, and the component's worst behavior lives in the seams above it.**

Blocker-weight cluster: **A-3** (a rejected rename fails in total silence — the only async card action not wired to the card's own feedback rail) + **A-1/A-2/A-7** (the whole editor is invisible to AT naming and undersized below the WCAG 2.2 SC 2.5.8 24px floor) + **A-4** (the one floating-glass element seated inside a rung-2 well, re-lettering the palette's display voice to mono mid-edit, against two written laws).

## Ruled disagreements (the seat's actual work)

| id | R1 | R2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-1** (prop-watcher clobber) | KILLED ("no live path; no refresh exists") | CONFIRMED MAJOR ("a re-sort destroys the draft") | **BOTH OVERTURNED → RESCOPED MINOR (A-8)** | R1's kill fails on its own enumeration: "there is no refresh" is false — `loadRemotePalettes` (useBrowsePalettes.ts:63-89) wholesale-replaces `remotePalettes.value` at :74 and is user-triggered via `onSortChange` (:116-120, and the search/tag/tier paths); BrowsePane's `:key="palette.slug"` (:95) preserves the card instance across the swap, `renaming` stays true (PaletteCard.vue:233), and A-13 proves the editor has no dismissal path, so it IS mounted across refetches. R1 enumerated only in-place `palette.name` mutations and missed array replacement. But R2's trigger is equally wrong: `watch(() => name, …)` (:51) is a primitive-getter watcher — Vue fires it only on an `Object.is` value change, so an ordinary re-sort returning the SAME name is a no-op at :51; "the draft is silently destroyed" by a mere sort is false. The live path requires a refetch landing a *remotely changed* name (concurrent edit) while the editor is open — real, reachable, and exactly the conflict case the If-Match machinery anticipates, but concurrency-conditional. RESCOPED: latent conflict-clobber, MINOR. |
| **`.input-bar` register** | SUPERLATIVE ("genuine and well-earned producer consumption") | KILLED L's framing + missed-MAJOR (register-law violation) | **R2 WINS — CONFIRMED MAJOR (A-4); R1's superlative corrected for sign** | Verified against two written laws, both quoted verbatim in the tree. DESIGN.md:105-111: "glass-ui's `.input-bar` is CHROME (rung-3 dock furniture — floating glass, a 24rem dock cap, the mono voice); a field seated in a pane BODY is a rung-2 fixture and wears the SEATED register — the ONE opt-in `.search-seated`." DESIGN.md:117-118 (depth law RC-2): "interior fixtures are rung-2 tone-steps, NEVER heavier glass than their host." The host card IS the rung-2 WELL by its own comment (PaletteCard.vue:228, `bg-well` :19); the dist recipe I read carries `background: var(--glass-bg-floating)` + `backdrop-filter: var(--glass-blur-floating)` — heavier-than-host glass inside an opaque well, the exact "mud generator" DESIGN.md:98 names. Second limb verified: `.input-bar-field` defaults `font-family` to `var(--font-mono, monospace)`; the title it replaces is `font-display font-medium text-subheading` (PaletteCard.vue:55); the cure rule already ships at utils.css:152-154 (`.search-seated .input-bar-field { font-family: inherit }`). R1 praised the consumption *mechanics* (real) without asking whether the right recipe was chosen (it was not). |
| **L-5's prescription** ("promote the whole pattern to glass-ui if reused") | KILLED (grep for the component name → 1 consumer → "nothing is reused") | Revived at pattern scope (3 divergent copies) | **SPLIT — kill upheld AS MODIFIED (K-3); R2's census CONFIRMED as fact (A-17)** | R1's census answered the wrong question: L-5 says "the whole *pattern*", and the pattern has three in-repo copies, all verified by this seat — PaletteRenameInput (raw everything), CurrentPaletteEditor (producer `Input` + aria-labelled pair + duplicate banner), SlugEditLayer.vue:76-100 (DockControl atom + `aria-label="Switch to slug"` + `:disabled` pending lock + Loader2 spinner). A component-name grep cannot see a pattern. BUT the kill's conclusion survives on KISS grounds: no new producer inline-edit primitive is needed — the sufficient cure is atoms already installed and already imported into the parent (`Button` at PaletteCard.vue:166, used with `icon-only… aria-label` at :96-104; `Input` via `demo/ui/input` → `@mkbabb/glass-ui/forms`, root barrel confirmed NOT to export it). Promotion is a producer question and rides the standing glass-ui BH relay (owner edict), never a local build obligation. |
| **morph-budget severity** (3rem vs 68px) | MAJOR (M-3) | MINOR ("a wrong number in a right structure") | **MINOR (A-11); R1's MAJOR preserved as dissent** | Arithmetic re-derived by this seat from the dist tokens and confirmed identical to both readers: coarse pointer → `--control-h-sm` = max(2.25rem×1.5, 44px) = 54px, + `pt-1`(4) + `pb-2.5`(10) = **68px against the 48px `--vj-morph-expanded: 3rem` cap** (PaletteCard.vue:358-363) — a 20px/29% clip under `.vj-morph-enter-active`'s `overflow: hidden` (animations.css:104-110), then a snap when Vue drops the class; fine pointer = 50 vs 48, negligible. The defect fires on every touch rename but is transient (one `--duration-normal` enter) and blocks nothing; the cure is one derived line. MINOR, dissent recorded. |
| **press bleed** (A-5) | Filed alone (missed-MAJOR) | Silent — read the parent extensively, never connected `press.handlers` | **CONFIRMED MAJOR on this seat's independent re-derivation** | Not a disagreement but a single-witness row, so it was NOT spot-trusted: I re-read the dist myself. `useLiquidPress` handlers include `onKeydown` (pressing on `"Enter" || " " || "Spacebar"`, no target check) and `onPointerdown` (button 0), bound in the bubbling phase on the card ROOT via `v-bind="press.handlers"` (PaletteCard.vue:24). The editor stops only `click` (:2) and `keydown.escape` (:16); `pointerdown`/`keydown`/`keyup` bubble freely from the input and both buttons to the root. Consequences statically closed: tapping into the field squishes the whole card; every SPACE typed in a palette name pulses the card (keydown presses, keyup releases); Enter-to-commit squashes the card on exit. The `@click.stop` proves the authors reasoned about containment — at the wrong event layer. No motion-quarantine row covers PaletteCard's press handlers, so nothing bars carriage. |

## Adjudicated defect roster

Severities are this seat's; original ids preserved for life. "Dies in" = the row needs no own gate once the named cure lands.

**MAJOR**

- **A-1 (D-2/L-4 + R1/R2 converged) — unnamed, undersized icon actions.** :18-23 and :24-30 are raw `<button>`s with no `aria-label`/`title`/text — sole children bare lucide `<svg>`s (:22, :29); `p-0.5`(2px) around `w-3.5 h-3.5`(14px) = **18×18 CSS px**, and `.input-bar { align-items: center }` (dist, my read) means they never stretch to the 44px-floored bar — failing WCAG 2.2 SC 2.5.8's 24px AA floor, not merely the 44px platform guidance D cited (R1's sharpening, adopted). This is W5-a11y sweep residue: PaletteCardSwatches.vue:12/:40 carry the literal sweep comments and 5 aria-labels; CurrentPaletteEditor's near-identical commit/cancel pair is labelled and `p-2`; PaletteCard.vue:93-104 already retired "the 3rd copy of the hand-rolled icon-trigger recipe" onto `<Button icon-only … aria-label>` — :18-30 is the 4th copy. **Terminal: X-W4.**
- **A-2 (L-1/L-2) — zero producer structural contract.** Imports are exactly `vue` + `@lucide/vue` (:36-37); both actions hand-author padding/radius/hover/active-scale/focus/icon-sizing (byte-identical class strings :20/:26; MQ **M-19 TRUE**). The cure atoms are installed and wired: `Button` imported ten lines above the usage site (PaletteCard.vue:166), `Input` via the `demo/ui/input` barrel → `@mkbabb/glass-ui/forms` (root barrel omits it — R2's caveat, verified by this seat: grep of `dist/index.d.ts` → 0), `label`/`labeled-field` shipped in dist. **Terminal: X-W4** (A-16 and A-21 die inside this edit).
- **A-3 (C-6/D-4/L-3 converged + R1-missed silent-failure, the composite's proven form) — a rejected rename fails in total silence.** Leaf: `submit: [newName: string]; cancel: []` (:43-46) — no ack, no pending, no rejection channel; nothing disables, nothing awaits. Parent: PaletteCard.vue:285-288 closes the editor synchronously in the emit's tick. Seam: BrowsePane.vue:106 `@rename="(p, name) => pm.onRename(p, name)"` is the ONE card binding with no feedback leg — `@save`/`@delete`/`@set-visibility`/fork all route verdicts to `card.showFeedback(…)` (the rail is mounted directly beneath the rename row, PaletteCard.vue:123-128, `defineExpose({ showFeedback })` :244). `onRename` (useBrowsePalettes.ts:156-177) returns nothing and swallows every failure into `console.warn` (:174-176) while its own sibling `onDeleteOwned` (:144-154) returns `{success, message}`. The failures are designed-in, not corner cases: the write is an If-Match-guarded PATCH (palettes.ts:91-110; comment :159-160 "K.W2: PATCH REQUIRES If-Match") so 412/428 are live outcomes; 401 via `ensureSession` (:158); 400 via the server's `min(1).max(100)` (schema.ts:25). The user renames, watches the row collapse, sees the OLD name, and is told nothing. In-repo cure precedent: SlugEditLayer's `:disabled` pending lock + spinner. (PalettesPane path exempt: usePaletteActions.ts:60-63 is local/synchronous.) **Terminal: X-W7** (the CRUD verdict returns, mirroring `onDeleteOwned`); the leaf's typed emit/ack contract leg rides **X-W5**. A-19 folds in as the message surface.
- **A-4 (R2-missed REGISTER + R2's K-4 framing kill) — the register and depth laws, violated together.** :4 bare `class="input-bar max-w-sm"` seats rung-3 CHROME (floating glass + blur + the 24rem dock cap + baked mono voice — dist rule read verbatim by this seat) inside the card that IS the rung-2 WELL (PaletteCard.vue:228, `bg-well` :19) — against THE REGISTER LAW (DESIGN.md:105-111, which names `.search-seated` as THE opt-in and enumerates its consumers; this file is absent from that census) AND the depth law RC-2 (:117-118, heavier-than-host glass reserved for surfaces that float). Second limb: `.input-bar-field` re-letters the palette name display→mono mid-edit (title voice `font-display font-medium text-subheading`, PaletteCard.vue:55); utils.css:147-154 states the voice rule and ships the exact cure (`font-family: inherit`) this file skipped. Challenge-D:19's own state product demanded "typographically continuous" — the shipped state does the opposite. Neither axis saw either limb. **Terminal: X-W10** (seated register or producer `Input`, decided there); the ASK-D `variant="seated"` producer rung rides the standing **glass-ui BH relay**. A-20 dies inside this cure.
- **A-5 (R1-missed press-bleed) — typing in the rename field drives the card's liquid-press spring.** Ruled above; independently re-derived from dist. `@click.stop` guards the wrong event layer: `pointerdown`, `keydown`, `keyup` bubble from all three editor controls to the root's `v-bind="press.handlers"` — space-per-keystroke card pulses (palette names routinely contain spaces), squish on field entry, squash on Enter commit. **Terminal: X-W10** (motion containment at the editor wrapper: stop `pointerdown`/`keydown`/`keyup`, or an editor-aware gate in the press wiring). PRM leg → U-3.
- **A-6 (C-2, focus-restoration limb) — cancellation drops keyboard focus to `<body>`.** :53-56 focuses/selects on mount; no `onBeforeUnmount`, no stored `activeElement`, no restore. Both exits tear the editor out via `v-if` (PaletteCard.vue:114/:118/:287); the title span has no `tabindex` (:53-59) and the menu item that opened the editor is unmounted (`startRenaming` closes the menu, :280-283). WCAG 2.4.3. (The "how the editor was entered" limb is K-2's kill — separately vacuous.) **Terminal: X-W4.**
- **A-7 (D-1) — the field has no accessible name once it holds text.** :11-17 complete: no `<label>`, no `id`, no `aria-label`/`aria-labelledby`; the accname rests on `placeholder` alone — and R1's correction (adopted) makes it stronger than D filed: the field mounts pre-filled and selected (:49, :53-56), so the placeholder essentially never renders; there is no visible label at any time and the sole assistive name is a string sighted users see only after clearing the field. Same failure family as A-1 (WCAG 4.1.2); R2's MAJOR adopted over R1's MINOR — an unnamed form control is not a lesser defect than an unnamed button beside it. glass-ui ships `label`/`labeled-field`, unconsumed. **Terminal: X-W4.**

**MINOR**

- **A-8 (C-1-as-rescoped) — latent conflict-clobber in the prop watcher.** Ruled above. :51 unconditionally mirrors the prop with no dirty guard; live only when a user-triggered refetch (sort/search/filter → `loadRemotePalettes` wholesale replace, instance preserved by `:key="palette.slug"`) lands a remotely-changed name while the editor sits open (which A-13 guarantees it can). Otherwise dead weight — the editor re-inits from `name` on every mount (:49). **Terminal: X-W7**, with A-3's conflict semantics (a 412 and a clobber are the same event seen from two ends).
- **A-9 (C-5, DEMOTED from R1's MAJOR) — unbounded input against a 100-char server cap.** :11-17: no `maxlength`, `autocomplete`, `autocapitalize`, `spellcheck`, `enterkeyhint`; sole normalization the `.trim()` at :59; server bound real (schema.ts:25); demo precedent `FlagReportDialog.vue:33` (`browser/dialog/` — R1's path cell corrected). Demotion rationale: the row's teeth are entirely A-3's silence — once the verdict channel exists, a 101-char paste is a *messaged* 400 and the residue is one attribute. **Terminal: X-W4.**
- **A-10 (D-3, R1-sharpened) — commit, destructive cancel, and a decorative ornament are typographically indistinguishable.** :20/:26 byte-identical; the non-interactive Pencil (:7) is the same `w-3.5 h-3.5 text-muted-foreground` as both action glyphs — three indistinguishable glyphs in one row, one of them inert. No dirty state exists to indicate (`localName` compared to `name` only inside `onSubmit`). glass-ui Button's `emphasis`/`tone` axes encode exactly this. **Terminal: X-W4** (dies in A-2's adoption).
- **A-11 (R1-M-3 / R2-missed, converged arithmetic) — the rename morph budget under-declares its row by 29% on coarse pointers.** `--vj-morph-expanded: 3rem` (PaletteCard.vue:362) vs 68px actual on touch (arithmetic in the ruling table); clipped for the whole enter under `overflow: hidden`, then pops; leave snaps 68→48 before collapsing. Cure: derive (`calc(var(--control-h-sm) + 0.875rem)`) or `none` — note A-4's cure changes the row's height source, so sequence this AFTER the register cure. Severity dissent recorded (R1: MAJOR). **Terminal: X-W10.** Witness → U-6.
- **A-12 (R1-M-4 / R2-missed, converged) — Escape is under-scoped.** :16 binds `keydown.escape` on the `<input>` alone; both buttons are tabbable (focus-visible rings :20/:26) and Escape there is inert. The handler belongs on the `<form>` (:3-6), which already owns commit. C-4 noticed "only Escape has an explicit key handler" but never checked its scope. **Terminal: X-W4.**
- **A-13 (R1-M-7 / R2-missed, converged) — no dismissal path exists at all.** `renaming` writes: :233/:282/:287 + template :118 — no blur/focusout, no outside-click (grep across demo/palettes/: zero `onClickOutside` hits, both readers + my read of the leaf), no card-level Escape. Clicking anywhere else leaves the editor mounted with the title hidden; clicking the card body outside the wrapper still toggles expansion under the open editor (root `@click` :26; only the wrapper stops clicks). Load-bearing for A-8's reachability. **Terminal: X-W4.**
- **A-14 (R1-M-5) — all three icons exposed to AT.** :7/:22/:29 bare `<svg>`, no `aria-hidden="true"`; the in-file precedent does it correctly (PaletteCard.vue:103). Two attributes against a named convention. **Terminal: X-W4** (dies in A-1/A-2's edit).
- **A-15 (R1-M-6) — dead branch narrated by a false comment.** PaletteCard.vue:291-293 documents "keep the menu open visually until the input takes focus"; :317 exempts `rename` from menu-close; but `startRenaming` (:280-283) closes the menu unconditionally FIRST — the exemption is dead and the designed hand-off never happens. Verified by this seat. Either the guard+comment die or :281 is the bug; prose asserting behavior the tree contradicts is the repo's named failure mode. **Terminal: X-W4.**
- **A-16 (R1-M-8; MQ M-19 TRUE) — `active:scale-95` snaps with zero easing.** `transition-colors` (:20/:26) carries no `transform`/`scale` leg (MQ M-17's measurement), so the 5% shrink jumps — the one press surface in a card whose motion grammar is otherwise uniformly sprung (card `useLiquidPress` + `cartoon-surface`; menu trigger via producer Button). Dies in A-2's Button adoption (which composes `useLiquidPress` + `tap-squish`). **Terminal: X-W4 (no own gate).**
- **A-17 (R2-missed pattern census; L-5's true remainder) — three divergent inline-edit copies, one wave tag, opposite cures.** Verified: the S.W5-3 (S-17) tag appears in this file (:8-10, CSS-recipe cure — the wrong register per A-4) AND in CurrentPaletteEditor ("the producer Input speaks unmodified") — one wave, two different cures in sibling name editors; SlugEditLayer is the third skeleton and the most complete (aria-label + pending lock + spinner). Ranked, this leaf is the least complete of the three. Disposition: the census informs X-W4's cure; the promotion question rides the **glass-ui BH relay** (K-3's modification) — a new primitive is NOT prescribed here. **Terminal: X-W4 context + relay.**
- **A-18 (R2-missed) — the two ways a palette gets named enforce different rules.** CurrentPaletteEditor checks `savedPalettes` case-insensitively and messages `"…already exists."`; the rename path checks only non-empty + changed (:58-64) and PATCHes straight through. The identical check ships against the same `Palette[]` 100 lines away — feasibility proven, contract split real. Whether duplicates are an *error* at product level is U-7's owner question; the asymmetry is the defect regardless of the answer. **Terminal: X-W7** (the collision rule belongs in the store — the CurrentPaletteEditor apotheosis' A-2/L-8 precedent, one identity).

**INFO**

- **A-19 (C-3) — revert-on-empty is unannounced and collides with explicit cancel.** :58-64: clearing + Enter is indistinguishable from the X; the `min(1)` rule is never stated. Defensible design, unexplained — C's own hedge is correct. Folds into A-3's message surface. **Terminal: X-W7 (no own row).**
- **A-20 (R1-M-9 / R2-missed, converged) — `max-w-sm` restates the recipe's own 24rem cap** (dist `--max-width-input: 24rem`, byte-identical; utility layer wins only to set the same value) — a no-op today, a silent pin tomorrow, AND the one value the register law says must die on a seated field (`.search-seated { max-width: none }`). Contradicts the file's own :8-10 "instead of hand-forking" comment. Dies in A-4's cure. **Terminal: X-W10 (no own row).**
- **A-21 (R2-missed) — mixed emit idiom.** Typed `defineEmits` used in script (:61/:63) beside template `$emit('cancel')` (:16/:27) — two spellings for one of two events in 66 lines. Style only; folds into A-2's edit. **Terminal: X-W4 (no own row).**

## Killed-claims register

- **K-1 — D-5a ("transition classes declare no reduced-motion branch") — KILLED, formally barred.** MQ **M-17 rules the claim FALSE by measurement**; FALSE rows may not be carried (standing law). Independently re-verified: no Vue `<Transition>` exists in the file (the vj-morph is the PARENT's, :112); the only transition tokens are `transition-colors` (:20/:26), whose surviving colour legs under `reduce` are deliberate non-vestibular design (glass-ui a11y-overrides + animations.css:184-193, both read); `active:scale-95` is in neither property list, so no spatial motion exists to gate. "Not in this file" is scope-shopping — the M-03 precedent verbatim.
- **K-2 — D-5b ("auto-focus/select can be disorienting if editing is entered indirectly") — KILLED as a defect; residue → U-5.** MQ M-18 rules it UNPROVEN; source settles the antecedent: `renaming` goes true only through `startRenaming()` (:280-283), reached from exactly two explicit rename gestures — the `editableName`-gated title click (:56-58) and the menu item (PaletteCardMenu.vue:73-79 → :298). No indirect/deep-link/restore entry exists. Auto-focusing the field the user just asked for, with the replaced text selected, is correct behavior.
- **K-3 — L-5's prescribed cure — KILLED AS MODIFIED.** The "producer-owned inline-edit field" limb is over-built (installed atoms suffice — Button already in the parent, Input already barrel-wired) and mandatory promotion inverts the KISS/no-contrivance rule. MODIFICATION: R1's supporting census ("nothing is reused") was wrong-scoped — the *pattern* has three copies (A-17) — so the "if reused" conditional is satisfied in fact; the promotion question is real but producer-owned: it rides the glass-ui BH relay, not this repo's build queue.
- **K-4 — challenge-L's framing that `.input-bar` consumption is the leaf's one clean pass — KILLED (R2's kill, upheld).** The axis audited which layer owns the properties and never asked whether the right recipe was chosen; the recipe is rung-3 chrome in a rung-2 well (A-4). The axis's own SOURCE-RED verdict survives — on the opposite reasoning for this clause.
- **OVERTURNED — R1's kill of C-1.** Recorded here so the kill is not silently lost: its "no refresh exists" premise is false against useBrowsePalettes.ts:63-89/:116-120; the row lives as A-8 at RESCOPED-MINOR. A kill that survives only by under-enumerating the seam is the mirror image of a finding that survives only by over-claiming it.

## Superlatives — with provenance (both readers, merged; one corrected)

1. **The provenance discipline is what made this adjudication possible** (R2's observation, elevated; R1's superlative corrected for sign). :8-10 names the wave, the mechanism, and the intent of the recipe consumption in the source itself — which is exactly why the register violation (A-4) could be adjudicated against DESIGN.md at all. The consumption *instinct* (never hand-fork chrome class-by-class) is right and cost the file zero lines including the composite focus ring; the recipe *chosen* was the wrong rung. A silent hand-fork would have been both uglier and undetectable at this cost.
2. **`onSubmit` (:58-64) is a small total function that cannot emit a bad name** (both readers): trims, refuses empty, refuses unchanged, routes both refusals to the same `cancel` the X uses — no fall-through, no residue. R2's sharpening adopted: every suppressed no-op is an If-Match validator the server never has to churn — one fewer chance of a spurious 412 on the next real edit. C-3 counts toward SOURCE-RED the strongest thing in the file; the miss is a message, not the predicate.
3. **challenge-C finding 4 is the corpus's one self-limiting sentence** (both readers, independently): faced with the IME/Enter question it could not settle statically, it declined to file a defect and named the shape a real one would have to take. Correct on the merits too — implicit form submission with a submit button present is the right commit path. Preserved as U-2.
4. **The click-layer containment and the title invariant are deliberate and correct** (R1): :2 `@click.stop` keeps the editor from toggling card expansion; PaletteCard.vue:53-54 hides the title under "never twice on one card." The containment is incomplete one layer down (A-5) — the intent and the click-layer execution are right.
5. **The motion architecture is exemplary even where its one number is wrong** (both readers): `<Transition name="vj-morph">` + `.rename-morph` parameterizing GEOMETRY ONLY through `--vj-*` vars, exactly the family law at animations.css:60-66; centrally PRM-governed; zero local keyframes. This is why K-1 disposes of the reduced-motion claim in one line — the architecture had already answered it. A-11 is a wrong number in a right structure.
6. **Modern-idiom hygiene at the leaf** (R2): `useTemplateRef<HTMLInputElement>` with optional chaining — no `as any`, no non-null assertion; and both buttons carry the house `focus-visible` ring recipe (R1) — unnamed and undersized, but never invisible to a keyboard user.
7. **The `demo/ui/input` barrel** (R2): one line reaching `@mkbabb/glass-ui/forms` for a component the root barrel does not export — the boundary work is already done; this component simply never walked through the door.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1 — cursor inheritance into the field.** Card root sets `cursor-pointer` (:19); `cursor` inherits and neither recipe class resets it; whether the `<input>` shows a hand or an I-beam is UA-stylesheet-dependent. One hover probe.
- **U-2 — IME/composition commit.** Implicit form submission is active (:18-19); no `compositionstart`/`isComposing` guard exists; whether a composition-confirming Enter also submits is engine-dependent. Live IME session required (C-4's hedge honored; no defect filed).
- **U-3 — the PRM leg of A-5.** PaletteCard.vue:262 asserts the press is "PRM-instant by construction"; if accurate, the per-space squish under `reduce` becomes an instant jump — plausibly worse. Fully proven at `no-preference`; the reduced-motion character needs one live check. No MQ row covers PaletteCard's press handlers.
- **U-4 — perceptual weight of the mono re-lettering (A-4 limb 2)** at the card's actual type size, both schemes. The token fact is source-certain and law-violating regardless; the *jarringness* is a render question.
- **U-5 — AT experience of unconditional focus+select** (K-2's residue; MQ M-18 concurrence). Entry-path study, not a source question.
- **U-6 — the coarse-pointer witness of A-11**: one touch-emulated enter/leave capture of the 68px row under the 48px cap (clip + pop), before/after the cure.
- **U-7 — duplicate-name product semantics (owner question, not a probe).** The API constrains length only (schema.ts:25); whether a duplicate name is an error state is a design ruling A-18's cure needs before the collision rule gets one home.

## Closing verdict

**SOURCE-RED AFFIRMED on all three axes — but re-grounded: 21 adjudicated rows (7 MAJOR · 11 MINOR · 3 INFO), and the four heaviest (A-3 silent failure, A-4 register violation, A-5 press bleed, A-1/A-7 naming) were established by the re-readers and this seat from the seams above line 66, where all three axes declined to look. The component is a well-provenanced, correctly-predicated 66-line leaf wearing the wrong register, wired to a parent that silences its only failure channel and a card that flinches every time someone types a space into it. Every MAJOR routes to an owned wave (X-W4 · X-W5 · X-W7 · X-W10) — zero NO-WAVE-OWNER rows; the producer questions (seated rung ASK-D, inline-edit pattern) ride the standing glass-ui BH relay.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this file per E-1/E-3.
