claude-fable-5

# kf-KeyframesEditor — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/components/instrument/keyframes/KeyframesEditor.vue` (284 L + its 16-file closure) — reader-1 ("design-and-usage": 33 confirmation cells · 7 kills · 8 misses · 9 superlatives) and reader-2 ("library-and-code": 36 confirmation cells · 5 kills · 8 misses · 8 superlatives). All three challenge files re-read **whole** by this seat (`audit/kf-components/KeyframesEditor/challenge-{D-design,L-library,C-consumption}.md`, 730/550/709 L, all opus-5[1m]).
**Method (incredulity)**: every head-on reader contradiction and every kill of a load-bearing cell was re-derived against the bytes with this seat's own instruments before adoption — the two direct verdict collisions (D-29 border reset, L-M2/D-11 highlight permanence), the two competing kills of D-3, the C-M5 demotion, and both readers' PRM grep counts were decided by fresh extraction, not by vote. Two of reader-2's net-new rows were re-executed (`fromString` append; the apply-CSS emit) and one was **reversed** (M-6). **Zero browser tooling** (law); all rulings static + installed-dist + node-against-dist + git reads; irreducibly visual residue rides to SS-13. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only)**: full reads of `KeyframesEditor.vue` (whole, 284 — every cited anchor checked), `KeyframeCard.vue` (whole, 81 — the occlusion decider: cluster `:11-13` z-auto vs `<pre>` `:41-45` `relative` z-auto, siblings, tree order), `KeyframeCardList.vue` (whole — `:5` guarded vs `:11` unguarded, `frames: any[]` `:33-36`, `cardInstances` `:64-67`), `useHighlightCSS.ts` (whole — `highlight()` writes `innerHTML` on the **`<pre>`** `:122-123`, marker `""` vs `"true"` `:110/:124`, unconditional `remove()` `:58-61`), `useKeyframesState.ts` (whole — styleId `:16`, `getTmpAnimationName` `:41-43`), `useApplyCSS.ts` (whole), `useKeyframeBrushApply.ts` (whole — `getClassName: () => options.styleId` `:33`, module-scope `kfEngine()` `:6`), `useKeyframesParsing.ts` (whole — blank-first `:50`, `debounce(updateAllStrings, 100)` `:62`, dead watches `:79-84`/`:96-103`, false comment `:86-95`), `useKeyframeOps.ts` (whole), `KeyframesAddDialog.vue:20-52/:120-132` (the `DialogTitle` nesting, the `class`/`bottom` typos, the guarded twin), `contenteditable.ts` (whole — `range.insertNode`), `useSpringKeyframesEditor.ts:40-77` (5-stop seed; `seedKeyframes` = `fromString`+`parse` on the same instance), `ControlsPaneWrapper.vue:44-64` (`v-show` over all hosts), `ChannelControls.vue:130-146/:385-389` + `useKeyframesPaneReveal.ts` (`v-if="hasSurface('keyframes') && keyframesWarmed"`; `requestIdleCallback` + 1500 ms `useTimeoutFn` warm — **at `transport/channel-controls/`, not the path both readers cited**), `KeyframesStringControls.vue:68/:114` (same composables, same animation), `demo/styles/style.css:200-220` (**`@layer base { * { @apply border-border; } }` at :210-213 — the D-29 decider**), `design-idioms.css:128-146` (`.progress-bar` has no width/rest state), `src/animation/compile/emit/format-options.ts:60-78` (**`css = \`.${name} {\n${css}}\n\`` at :74**), `src/animation/engine/play-lifecycle.ts:205-222/:370-382` (**real standalone-path reads of `anim.options.respectReducedMotion` at :214/:376 — the M-6 decider, consistent with the banked kf-CopyButton grep**), `src/animation/constants/defaults.ts:84/:87` (`fillMode: "forwards"`, PRM `false`), installed glass-ui 7.0.0 `dist/components/toast/use-toast.d.ts` (**`ToastOptions = Omit<ToasterToast,'id'>`; dismiss only via the returned handle; no `toast.error` — the C-M5 decider**), `dist/styles/theme/radius.css` (`--radius-panel: var(--radius-xl)` = 12px vs `--radius-lg: var(--radius)` = 10px), `dist/styles/glass/material.css` (`.glass-card { contain: paint }` — the second D-6 trap; `.keyframes-editor-scroll { container-type: inline-size }` at SpringPhysicsFacet:233-238 is the first); **executed probes** (node against the built `dist/engine/index.js`): `fromString(css3stop)` → `templateFrames.length === 3`; second `fromString` + `parse()` → **6**, selectors `0,0,0.5,0.5,1,1` (append, never replace); `CSSKeyframesToString(a,'spring-3')` → **`.spring-3 { animation-name: spring-3; … }`** (the apply-mismatch decider); greps re-run: `respectReducedMotion` in demo → **7 lines / 4 live opt-in sites** (both readers' counts wrong), `keyframes-style` in demo → exactly `useKeyframesState.ts:16/:42`, `accent-census` → **`docs/tranches/T/verdicts/APPEARANCE-WAVES.json:25-27` names `proof:accent-census` as a T-era oracle** while package.json ships only `proof:publish`/`proof:owner-golden` and `scripts/gates/` holds only `surface/`+`visual/`.
**Substrate disclosure — the standing SCH-1 ruling re-confirmed**: local HEAD `8281638c` (branch `master`) is the same **divergent stale fork** the kf-CopyButton apotheosis banked — 1 ahead, **41 commits behind `origin/master`**, 252 dirty rows; all three axes and both readers audited the worktree. HEAD `package.json` declares `optionalDependencies: { "@mkbabb/glass-ui": "6.0.0" }` (my `git show` — reader-1's L-M7 sharpening confirmed); the worktree deleted it (the F-1 phantom is worktree-only); the **frontier declares glass-ui `7.0.0` exact + value.js `4.0.0`** (F-1 CURED — the banked SCH-1/kf-App ruling holds). **Frontier durability probed row-by-row (`git show origin/master:…`), which neither reader did**: (a) **CURED at the frontier**: the `[object Object]` blocker — frontier `KeyframeCardList.vue:11` reads `startScalar(frames[i].start)` with `startScalar` authored at `:44` (FE-3's BUILD-W1 evidently executed upstream); the dead `templateFrames` watch — frontier `useKeyframesParsing` watches `() => animation.templateFrames.length` (EE-03's prescribed cure verbatim, making the 10-line comment TRUE there). (b) **DURABLE at the frontier**: the frozen slider write (`frame.start.value = starts![i]` at frontier `:43`) and the `-10..110` domain; `parseCssScalar` at the start field (frontier `:186`); the apply-CSS class/selector mismatch (frontier `useKeyframesState:16/:42` + `useKeyframeBrushApply:33` byte-identical); the occluded action cluster (frontier `KeyframeCard` structure identical, `proof:accent-census` comment included); `frames: any[]` (frontier `:35` — the cure coerced with `unknown`, it did not type the seam); the zero-writer control watch (frontier `:80`); plain-`tsc` `check` (no `vue-tsc` at either end). All line anchors below are worktree-at-`8281638c`; re-resolve before any cure (D-19).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**66 adjudicated roster rows — 6 BLOCKER (2 already CURED-AT-FRONTIER and folded to their banked identities FE-3/EE-03… of which 1 is blocker-weight; 4 frontier-durable) · 31 MAJOR · 25 MINOR · 4 INFO.** **12 killed-claims register entries** — ten reader kills sustained (three decided against the OTHER reader's confirmation cell), one kill extended, and **one reader-2 missed row REVERSED by this seat** (M-6, against the banked kf-CopyButton grep the reader did not consult). **14 missed rows ADMITTED** (the occlusion convergent across both readers; reader-2's apply-mismatch promoted to the roster's headline blocker) **+ 1 miss FOLDED to a banked row it re-booked** (reader-1's M-8 = KF-CB-3). **9 adjudicator corrections against the readers.** **9 superlatives sustained, 2 rescoped by reference to this roster's own blockers.** Identity guards held: FE-3, EE-03, KF-CE-12, KF-CE-16, KF-CB-3, KF-CB-11, KF-CB-25, census F-1/SCH-1, S-7 pattern, S-9 (kf-DemoGlobalChrome), megatranche R1 — **zero re-bookings**.

Blocker-weight facts: the component's three primary affordances are **all dead or lying at the audited bytes** — the retiming Slider throws into a frozen selector on first touch (silently: Vue's handler wrapper eats the TypeError), the per-stop start field rendered `[object Object]` (cured upstream), and **Apply-CSS has never once applied anything**: the class it adds (`keyframes-style-<uuid>`) and the selector it injects (`.<uuid-lowercased>`) are structurally different strings — probe-proven by this seat, frontier-durable, and unnoticed by all three axes AND reader-1, each of which reasoned about the feature's teardown, sharing, and `aria-pressed` without noticing it never worked. Beneath the code plate, the per-card delete X, CopyButton, and `f`/`s` annotation are painted UNDER an opaque surface — invisible and pointer-unreachable.

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-29 · border `currentColor`** | KILLED — `@layer base { * { @apply border-border } }` exists (style.css:210-213); built CSS emits `*{border-color:var(--border)}` after preflight, same layer, later order | CONFIRMED — "no border-color reset in demo/styles (grep → 0)" | **Reader-1 WINS; reader-2's cell CORRECTED** | My read of `demo/styles/style.css:210-213`: the reset is there, written as `@apply border-border` — invisible to a `border-color` grep. A grep-shaped artifact inside a confirmation cell (the same failure class as kf-CopyButton correction #1). D-29 is DEAD; the toolbar wears the standard `--border` hairline. |
| **L-M2 / D-11 · highlight permanence** | CONFIRMED ("both `highlightAll()` sites dead after first pass; marker never cleared") | KILLED — Vue's patch targets the orphaned `<code>` (so highlighting is never "blown away"), and D-10's blank-first remount destroys the marker every projection (so it IS re-applied) | **Reader-2 WINS; reader-1's cell CORRECTED** | My read of `useHighlightCSS.ts:116-126`: `el.innerHTML = h.value` on the **`<pre>`** detaches the `<code>` element `KeyframeCard.vue:50` renders — Vue's later text patch lands on a detached node, so the visible hljs markup survives; and `useKeyframesParsing.ts:50` blanks `templateFrameStrings` on every projection, unmounting every card, so no `highlighted` marker outlives a projection and `:275-277` re-highlights fresh elements. "Lost permanently" is false on both counts. Survivors booked as **KF-KE-60** (the orphaned-`<code>` write channel — masked by D-10, armed the moment D-10 is fixed; plus per-keydown overhead and ≤1 s intra-window mis-tokenization). The one true one-shot is `KeyframesAddDialog.vue:33`'s `<pre>` (outside any v-for). |
| **D-3 · annotation contrast** | KILLED — premise inverted: the plate paints IN FRONT of the annotation (tree order), so the measured composite never renders | CONFIRMED BLOCKER (arithmetic re-derived to 3 s.f.) with an honest note that M-3 may moot it | **Reader-1 WINS — D-3 SUPERSEDED, not merely softened** | My read of `KeyframeCard.vue:10-51`: the annotation wrapper (`:11-13`, `absolute`, no z utility — the `z-0` at `:29` is an inner div) and the `<pre>` (`:45`, class ends `relative`) are z-auto siblings; CSS 2.2 App. E step 8 paints tree order; the `<pre>` is later and is the container's only in-flow child, so `top-2 right-4` sits inside its opaque plate. Both readers' arithmetic is correct and moot. The occlusion is the finding (**KF-KE-5**). |
| **Occlusion severity** | BLOCKER (miss M-1) | MAJOR (miss M-3) | **BLOCKER — reader-2's grade as dissent** | Two shipped affordances (per-stop delete, per-card copy) are invisible AND pointer-unreachable once the theme injects — which `onMounted → updateAllStrings → watch(cssKeyframesString) → highlightAll` guarantees. That is a functional kill, not a degradation. Rendered confirmation = SS-13 #1. |
| **C-M5 · vue-sonner shadows `./toast`** | KILLED to INFO — read `use-toast.d.ts`: no stable external id, no external dismiss, no `toast.error`; the fork is justified today | CONFIRMED MAJOR (evaluate posture) | **Reader-1 WINS; demoted to INFO-fold** | My own read of the installed `use-toast.d.ts`: `ToastOptions = Omit<ToasterToast, 'id'>` — a caller-supplied stable id is unspellable; `dismiss` exists only on the per-call return; tone is a prop, `action` is a Component/VNode, not `{label,onClick}`. The component's error posture (per-index id replacement `:180/:190`, explicit dismiss `:205`, Retry action) is not expressible on the primitive. FOLD by reference to the **S-9 evaluation identity already run at kf-DemoGlobalChrome** + a glass-ui BH-relay forward ask (id/dismiss/action surface). Not a defect row here. |
| **D-5 falsifier line ("grep → no hits")** | endorsed (cell said 5 hits, none in the family) | KILLED the line (3 hits) while confirming the defect | **Kill SUSTAINED and EXTENDED — both counts wrong** | My grep: **7 lines / 4 live opt-in sites** — `useSceneSwap.ts:45`, `animationOptionsStore.ts:49`, **`TypingDots.vue:91` and `AnimationVisualizer.vue:147`, both missed by reader-2**. The demo is demonstrably PRM-literate; this component's three animations are the outliers (all pass explicit literal options). The defect survives; every count in the corpus around it was wrong. |
| **Reader-2 miss M-6 · "PRM structurally unreachable for single animations"** | (not filed) | MINOR — `play()` never consults the flag; only Group/Sequence honour it | **REVERSED — this seat** | `src/animation/engine/play-lifecycle.ts:214` and `:376` are **real reads** of `anim.options.respectReducedMotion` on the standalone play path (up-front gate + live re-consult per tick), exactly as the banked kf-CopyButton apotheosis's grep recorded ("standalone-only — the K5 decider") — a registry row the reader did not consult. The brush and the 1 s sweep are standalone `CSSKeyframesAnimation`s, so `respectReducedMotion: true` in their options bags **works**; only the delete `AnimationGroup` needs the `g.respectReducedMotion` field. The claimed library gap does not exist; D-5's cure map is corrected accordingly (KF-KE-8). |
| **L-M1 premise ("re-sample = wholesale replacement")** | confirmed L-M1 as filed | KILLED the premise: `fromString` APPENDS; filed the doubling as miss M-2 | **Reader-2 SUSTAINED by this seat's own execution** | My probe against `dist/engine/index.js`: 3 frames → second `fromString`+`parse` → **6**, selectors `0,0,0.5,0.5,1,1`. `seedKeyframes()` doubles 5→10 on every press. And at the frontier, where the cured length-watch fires on the append, the doubling becomes **visible** (10 duplicated cards) instead of silent. |
| **frames[i] desync severity** | MINOR (miss M-5) | MAJOR (miss M-5, with the `adoptCompiled` compiler-transplant chain) | **MAJOR — reader-1's grade as dissent** | Reader-2's chain is the stronger one and every hop is in the tree: the co-mounted `KeyframesStringControls` commits through `updateFromString → adoptCompiled` (a wholesale compiler transplant), while `:11` reads `frames[i].start` unguarded ONE line below the guarded `:5`. |
| **Substrate / frontier** | audited worktree as live source | established nothing beyond worktree (its L-M7 sharpening did probe HEAD) | **Both frontier-blind — this seat's extension governs** | Two of the corpus's headline defects (FE-3, EE-03) are already repaired at `origin/master`; reachable by one `git show`. The kf-CopyButton apotheosis banked this exact lesson (its correction #7) in the registry both readers cite. Repeated verbatim. |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** named from the census taxonomy (`formation/keyframes/CENSUS-2026-08-03.md` §(a): W0 Substrate Settle · W1 Mail Cure · W2 Parse Façade · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Tri-Fold Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit · W10 Fold Discharge & Close). **KFED-UNIT** = **NO-WAVE-OWNER**, per the kf-CSSCodeEditor EDITOR-UNIT precedent: a proposed single wave-surface — *"the keyframes authoring surface made honest"* (the retiming control, the per-stop start field, the apply identity, the card action cluster, the projection/remount engine, the add dialog) — for SS-1/SS-2 spec authoring to consume; no census row names this repair, and scattering it across ten waves would orphan its shared root causes. It sequences **after** KF.W0 (two blockers are already repaired at the frontier; a careless rebase of the in-flight migration re-lands them) and **after** the KF.W4 `vue-tsc` gate (KF-CE-16 — nothing in the template layer is verifiable until a `.vue` file can fail a build). Routings are bookings, not acts.

### BLOCKER

- **KF-KE-1 · D-1 = L-B2 = census **FE-3** (cause: C-M7) — never re-booked.** Every per-stop start field and ghost label renders `[object Object]` (`KeyframeCardList.vue:11` `.toString()` on a bare frozen `KeyframeSelector`; probe re-confirmed by both readers); the correct renderer `selectorText()` sits in the closure's own import graph, used at `useKeyframeOps.ts:111`. **TRUE at the audited bytes; ALREADY CURED at `origin/master`** (this seat's probe: frontier `:11` = `startScalar(frames[i].start)`, authored at frontier `:44`). Identity: **FE-3**, dispositioned `born V → BUILD W1` at keyframes `docs/tranches/V/DISPOSITIONS.md` §D — W1 evidently executed upstream, including FE-3's own smoke-assertion rider. → **FOLD to FE-3 + KF.W0**: the surviving obligation is the re-regression guard (the stale fork's migration must not re-land `.toString()` over the frontier's repair; FE-3's `/\[object Object\]/` smoke assertion is the born-RED witness). The CAUSE is not cured: see KF-KE-17.
- **KF-KE-2 · L-B1 + D-2(read) + C-M3, one control, three independent kills — the retiming Slider is inert, illegible, and out of contract.** (i) `:43` writes `frame.start.value = starts![i]` into a value.js deep-frozen selector → `TypeError` at i=0, loop aborts, `:45` never runs — **and the user sees nothing**: Vue wraps template handlers in `callWithAsyncErrorHandling`, so the throw is a console line, not a surfaced error (reader-2's refinement, adopted). (ii) The model reads `[0,1]` fractions into a `-10..110 step 1` percent track: all seeded thumbs collapse into an 8.3–9.2 % band, and `:step="1"` cannot express any intermediate stop even after the unit fix. (iii) The declared domain offers 20 points value.js rejects at parse (`keyframe_selector_invalid, expected:["0%..100%"]` — probes). D-2's write-half (the 5000 % write) stays killed: the freeze precludes it, and **the ordering constraint is load-bearing for remediation** — fixing the freeze without the unit creates the 100×-destructive write L-B1 warned of. Frontier-durable (all three facets verified at `origin/master`). The correct helpers (`selectorPercent`/`percentSelector`, named-arm-safe) already exist in `demo/utils/keyframeSelector.ts`. → **KFED-UNIT** (one spec: read via `selectorPercent`, write by whole-selector replacement à la `:206-209`, domain `0..100`, fractional step, `marks`, per-thumb `aria-valuetext` — with KF-KE-34).
- **KF-KE-3 · C-B1 (+ C-m3 folded) — the start field validates through the wrong value.js entry point.** `parseCssScalar` (any CSS scalar) instead of `parseKeyframeSelector` (the keyframe grammar): admits `500%`/`-20%`, renders them, then fails ~1 s later as "Could not update keyframes" from a different op naming neither field nor stop; rejects `from`/`to` that the sibling add path accepts via `requireKeyframeSelector` (`useKeyframeOps.ts:174`) — two grammars in one component. Probes re-confirmed by both readers; chain verified in my `useKeyframeOps` read. Frontier-durable (`parseCssScalar` at frontier `:186`). → **KFED-UNIT** (`requireKeyframeSelector` at the field; the S-5/C-S5 error posture stays — it is the reference implementation asking the wrong function).
- **KF-KE-4 · reader-2 miss M-1, promoted to the roster's headline — Apply-CSS has never applied anything: the class and the selector are structurally different strings.** Chain, every hop this seat's own: class added to targets = `getClassName() = options.styleId` = `keyframes-style-${superKey}-${animationId}` (`useKeyframeBrushApply.ts:33` → `useApplyCSS.ts:47-49`); injected stylesheet = `CSSKeyframesToString(animation, getTmpAnimationName())` (`useKeyframesParsing.ts:37-40`) where `getTmpAnimationName()` strips the `keyframes-style-` prefix and lowercases (`useKeyframesState.ts:41-43`); the emitter's only rule is `.${name}` (`format-options.ts:74`). **Executed**: `CSSKeyframesToString(a,'spring-3')` → `.spring-3 { animation-name: spring-3; … }`; the class added would be `keyframes-style-spring-3`. `"keyframes-style-"+X ≠ X.toLowerCase()` unconditionally; repo grep shows nothing else keys on the class. Pressing Apply pauses the JS animation (`paused = started`), injects dead CSS, wiggles the brush, sets `aria-pressed="true"` — and the target freezes. All three axes and reader-1 audited this feature's teardown (L-M3), sharing (C-B2), and semantics (SUP-4) without noticing it never worked. Frontier-durable (all three hops byte-identical at `origin/master`). → **KFED-UNIT** (one apply identity: class = selector, or pass `getTmpAnimationName()` as the class; then re-derive L-M3/C-B2's residue against a working feature). SS-13 #2 witnesses the press signature.
- **KF-KE-5 · reader-1 miss M-1 = reader-2 miss M-3, convergent — the card's entire action cluster is painted UNDER the opaque code plate: invisible, pointer-unreachable.** This seat's own derivation at `KeyframeCard.vue:10-51`: the cluster (`:11-13`, `absolute top-2 right-4`, z-auto — the `z-0` at `:29` is an inner label div) and the `<pre>` (`:41-45`, class ends `relative`, z-auto) are siblings in one stacking context; App. E step 8 paints positioned z-auto descendants in tree order — the `<pre>` is later, on top, and is the container's only in-flow child, so the cluster's `top-2 right-4` sits inside its `min-h-32` opaque plate (D-9's un-layered `#ffffff`/`#0d1117`, layer-scan-confirmed by both readers). Hit-testing follows paint order: the `<pre>` (not `pointer-events:none`) eats the click. The delete X and per-card CopyButton are unreachable by pointer; CopyButton remains keyboard-focusable but paints invisibly. Supersedes D-3 (killed), reframes D-4 and D-33's gapless-pair sub-case. Frontier-durable (structure identical at `origin/master`). **BLOCKER** (reader-2's MAJOR as dissent). → **KFED-UNIT** (a z rung on the cluster or drop the `<pre>`'s `relative`; then D-4's shell cure and the annotation's contrast become live questions again). **SS-13 #1 — the single highest-value screenshot in the component.**
- **KF-KE-6 · C-B2 — one deliberate per-animation identity, two live owners, and the shadow state was never lifted with it.** Both readers re-proved the chain end-to-end; this seat re-verified the load-bearing hops: per-animation ids (`useKeyframesState.ts:15-19`), `ControlsPaneWrapper.vue:48` renders every channel host under **`v-show`** (all mounted), `ChannelControls.vue:130` force-mounts `KeyframesStringControls` once `keyframesWarmed` (idle warm: `requestIdleCallback` + 1500 ms fallback, `useKeyframesPaneReveal.ts` — **path correction: `transport/channel-controls/composables/`**), `KeyframesStringControls.vue:68/:114` runs the same composables with the same animation; `useHighlightCSS.ts:30-33` adopts the existing node and `:58-61` removes it unconditionally, no refcount; per-instance `isApplied`/`prevPaused` (`useApplyCSS.ts:27-28,44-45`). Reader-1's sharpening adopted: `useKeyframeBrushApply.ts:15` documents the sharing as intentional — the defect is the unlifted shadow state, not the shared identity. KF-KE-4 moots the "two owners of one working stylesheet" arm today (the stylesheet never worked); the `paused`/`prevPaused` cross-restoration and the node teardown mechanics stand on their own. → **KFED-UNIT** (lift `isApplied`/`prevPaused`/node ownership to the identity's altitude — per-animation, refcounted). Unmount ordering → SS-13 #5.

### MAJOR

- **KF-KE-7 · D-4, rescoped by KF-KE-5; a11y shell FOLDS to banked **KF-CB-11**.** The bare `<svg @click>` delete (unfocusable, roleless, unnamed) is already rostered at the kf-CopyButton apotheosis as KF-CB-11 — fold, never re-book. Surviving here: the 700 ms unskippable `AnimationGroup` gate before `removeKeyframeData` (both presets `duration: 700`, catalog-verified by the readers), no confirm/undo, no busy state, and the re-click window over indices about to shift. Today the control is pointer-unreachable outright (KF-KE-5) — the shell cure and the gate cure land together. → **KFED-UNIT**.
- **KF-KE-8 · D-5, merged and its cure map CORRECTED (M-6 reversal).** Three engine animations, one infinite (`useKeyframeBrushApply.ts:18-27` ±90° alternate 700 ms), none opting into PRM; engine default `false` (defaults.ts:87); glass-ui's CSS PRM block cannot reach WAAPI/inline-style writers. Corrected cure map (this seat): the brush and the 1 s sweep are **standalone** animations — `respectReducedMotion: true` in their options bags is honored at `play-lifecycle.ts:214/:376`; the delete group needs `g.respectReducedMotion = true` (group/group.ts:57). Sequencing nuance inherited from kf-CopyButton: PRM snaps to the FINAL frame — for the sweep that is 100 % width, which KF-KE-21's rest-state defect makes indistinguishable from idle; land the bar's rest state with the flag. → **KFED-UNIT**; mechanism unification rides **KF.W9**.
- **KF-KE-9 · L-M1 = census **EE-03** — never re-booked.** The `templateFrames` array watch never fires and warns per element at setup (probe re-confirmed on vue 3.5.35). Identity: **EE-03**, dispositioned `born V → BUILD W1` with the exact cure (`watch(() => animation.templateFrames.length)`) — **CURED at `origin/master`** (this seat's probe), where the `:86-95` comment becomes true. → **FOLD to EE-03 + KF.W0** re-regression guard (EE-03's watch-warn-count→0 gate). The re-sample desync consequence lives on as KF-KE-10's visibility change.
- **KF-KE-10 · reader-2 miss M-2, EXECUTED by this seat — `fromString` APPENDS; "re-sample from spring" doubles the stop set on every press.** `seedKeyframes()` (`useSpringKeyframesEditor.ts:73-76`) calls `fromString`+`parse` on the same instance; `addFrame` only pushes (no reset anywhere in `src/animation/**`); my probe: 3 → **6** frames, selectors `0,0,0.5,0.5,1,1` — so 5 seeded stops → 10 → 15, duplicated selectors, degenerate zero-length segments. At the audited bytes the doubling is also **silent** (the dead watch); at the frontier the cured length-watch projects it as 10 duplicate cards. Two-armed disposition: the demo reseed must clear-then-ingest (**KFED-UNIT**), and `fromString`'s append-on-repeat semantics on a `CSSKeyframes` ingest deserves a library letter (**KF.W5**: replace-or-document; the emit probe shows the same instance then serializes duplicated selectors).
- **KF-KE-11 · C-M4 — the `selectedKeyframesControl` watch has zero writers repo-wide** (3 hits: type, default, this reader — grep re-run by reader-2, spot-confirmed); with KF-KE-9, BOTH of the parsing composable's watches are dead at the audited bytes and the mutation-site calls are the sole projector. Frontier-durable (the watch survives at `origin/master` `:80`). → **KFED-UNIT** hygiene (delete or wire; correct the comment).
- **KF-KE-12 · L-M3, reframed by KF-KE-4 — `clear()` is never wired** (`useKeyframeBrushApply:29` drops it; `onUnmounted` pauses only the brush): unmounting with apply ON leaves `animation.paused === animation.started` stuck and the target carrying an orphan class — a class that (KF-KE-4) never had rules behind it anyway, so the user-visible residue is the permanently-frozen sweep. → **KFED-UNIT** (rides the apply-identity spec; wire `clear` into `onUnmounted`).
- **KF-KE-13 · L-M4 — a fresh compiled `CSSKeyframesAnimation` per keystroke**, un-debounced (`:215` outside the 1000 ms debounce that guards the mutation half), origin a raw contenteditable `@input`; competing `width` writers, self-terminating (the honest non-leak caveat stands). The correct null-guarded once-per-submit twin is at `KeyframesAddDialog.vue:125-131` (my read). → **KFED-UNIT**.
- **KF-KE-14 · L-M5 = banked **KF-CE-16** — never re-booked.** No `vue-tsc` anywhere; `check` is plain `tsc`; the template expressions carrying KF-KE-2 have zero static coverage. The gate-hole identity is already rostered at kf-CSSCodeEditor (KF-CE-16 → **KF.W4**, "the sequencing head of the whole record"). Frontier-durable. → **FOLD to KF-CE-16/KF.W4**; KFED-UNIT sequences after it.
- **KF-KE-15 · L-M6 (+ reader-1's undercount confirmed) = banked **KF-CE-12**'s extirpation identity.** Six deep `@src/animation/**` reaches in the closure (the readers' five + `demo/utils/keyframeSelector.ts:5` `namedSelectorToFraction`), 8 in the directory, one from `internal/`, against the ED-3 dogfood the same files cite; none published on either surface. KF-CE-12 already books the `convertPixelsToCh` breach and names `useKeyframesState.ts:1` as its twin. → **FOLD to KF-CE-12 → KF.W8** (publish-or-relocate + extirpation), with this roster's sharpened census attached.
- **KF-KE-16 · L-M7 = census **F-1/SCH-1** — never re-booked.** Worktree phantom (glass-ui undeclared, 0 lock entries) — but at HEAD `optionalDependencies: 6.0.0` IS declared (my `git show`, confirming reader-1's sharpening = the banked kf-CopyButton mechanism correction), and the frontier declares 7.0.0 exact. The phantom is an uncommitted-migration artifact; the subpath-import blast radius (`/dark`, `/forms`) is real until the fork reconciles. → **FOLD to SCH-1/F-1 → KF.W0**.
- **KF-KE-17 · C-M7 — `frames: any[]` erases the library's type at the one seam the editor owns** (sole consumer; `animation: KeyframesAnimation<any>` beside it) — the hole KF-KE-1 shipped through. **Frontier-relevant sharpening (this seat): the frontier's FE-3 cure coerces with `startScalar(start: unknown)` rather than typing the prop — the cause survives the symptom's repair.** → **KFED-UNIT** (type the prop `TemplateAnimationFrame<any>[]`), sequenced after KF.W4 makes it checkable.
- **KF-KE-18 · C-M8 — the component mutates its prop's object graph across eight sites; the emit surface cannot report it** (`keyframesUpdate` from exactly one site, inside an op this component never calls; `sliderUpdate` never emitted; the sole callsite binds neither). Two writers (scene + editor), one object, no contract in either direction — KF-KE-10's doubling is this defect read from the parent's side. → **KFED-UNIT**.
- **KF-KE-19 · C-M1 — the reactive-getter contract is defeated one line in** (`getAnimation()` called once at setup; every holder a snapshot; the eager `animation` in the `:261` options bag beside correctly-modeled `getCSSString`). Latent (markRaw const, no `:key`), filed as a contract defect — correctly. → **KFED-UNIT**.
- **KF-KE-20 · C-M2 — a transient modal's open state persisted to localStorage with a 7-day TTL** (`dialogOpen` a declared member of the preferences schema; sole reset on submit-success). A reload inside the TTL re-opens the modal unprompted. → **KFED-UNIT** (local ref; delete the schema member).
- **KF-KE-21 · C-M6 + reader-1 miss M-4, merged — the progress bar is a shadow of glass `./progress` (proposed census row S-10), has no progress semantics, completes as the debounced work starts, AND has no rest state**: `.progress-bar` declares no width (design-idioms.css:131-144, my read), a grid item → 100 % at idle; `fillMode: "forwards"` (defaults.ts:84, my read) leaves the sweep at 100 % — a permanently-full rainbow rule that collapses-and-refills per keystroke. → **KF.W6** (S-10 census row: glass `Progress`, evaluate) + **KFED-UNIT** (bind it to something true, or delete it).
- **KF-KE-22 · D-7 — the sticky per-card start field is `bg-transparent` over a 26 rem scrollport** (`KeyframeCard.vue:4` + `.keyframes-editor-scroll { max-height: 26rem; overflow-y: auto }`) — pinned text colliding with scrolling code beneath it. (Its `z-modal` half is KF-KE-41, demoted.) → **KFED-UNIT**.
- **KF-KE-23 · D-8 — `.text-small` binds the proportional body face onto both contenteditable CSS surfaces**; the 4-nbsp fake tab presupposes monospace; code size is viewport-fluid; the mono rung (`text-mono-small`) is used two lines up. Verified in the installed cascade by both readers. → **KF.W6**.
- **KF-KE-24 · D-9 — the runtime-injected hljs theme is un-layered and defeats `bg-transparent`**: a hard `#ffffff`/`#0d1117` plate inside the warm `--card` surface (1.42:1 tonal step in dark), and the opacity that powers KF-KE-5. → **KFED-UNIT** (layer the injected sheet / re-tokenize the theme) with a **KF.W6** rider.
- **KF-KE-25 · D-10 — every projection blanks `templateFrameStrings` first** (`useKeyframesParsing.ts:50`, my read): all cards unmount and remount per edit; caret, selection, focus destroyed. The ref write queues its flush before the awaits queue theirs — the empty state is guaranteed. Also the masking condition for KF-KE-60. → **KFED-UNIT** (project in place, keyed by frame id). Flash perceptibility → SS-13.
- **KF-KE-26 · reader-1 miss M-3 — the start field commits per keystroke with no commit gate**: glass Input emits per `input` (bundle-verified by the reader; adopted), each keystroke runs the full handler — typing `50%` toasts twice before succeeding, and the successful keystroke calls `updateAllStringsAndAnimation()` **synchronously** (`:210`), unmounting the field under the caret immediately (not after the 1 s debounce that softens the `<pre>` path). → **KFED-UNIT** (commit on blur/Enter; with KF-KE-3).
- **KF-KE-27 · reader-1 miss M-2 — the primary per-stop `<Input>` has no accessible name**, while two `for`-less `<Label>`s sit 30 px away naming nothing (`KeyframeCard.vue:3-8` vs `:31-38`, my read); `LabeledInput` ships on the same glass subpath the parent already uses. → **KFED-UNIT**.
- **KF-KE-28 · reader-2 miss M-4 — Tab-indent is write-only decoration**: `insertTabAtCursor` uses `Range.insertNode()` (my read of `contenteditable.ts`), which fires no `input`/`beforeinput`; `@input` is the card's only channel to the model, so the indent is never folded in and dies at the next reprojection — in a face where four nbsp don't align anyway (KF-KE-23). → **KFED-UNIT**.
- **KF-KE-29 · reader-2 miss M-5 (+ reader-1 miss M-5) — `frames[i]` unguarded one line below the guarded identical access** (`KeyframeCardList.vue:5` vs `:11`, my read), over two independently-projected arrays with no generation guard, while the co-mounted sibling can transplant the whole compiler (`adoptCompiled`) — a render-time `TypeError` corridor. MAJOR (reader-1's MINOR as dissent). → **KFED-UNIT** (`?.` + a generation counter on `updateAllStrings`).
- **KF-KE-30 · D-12 + D-13 — four sibling controls, three focus dialects, none the codified one; and `.focus-ring` is a REALIZED class-name collision**: glass-ui's layered copy (with `border-radius: var(--radius-pill)`) vs the demo's deliberately un-layered copy (without it) — the demo silently reshapes the vendor primitive's focus geometry repo-wide; `KeyframeCard.vue:45` consumes the ambiguous name. Extends census §6.3 from custom-property space into class space. → **KF.W6** (focus contract + the collision as a census §6.3 annotation, E-3 addendum-not-rewrite).
- **KF-KE-31 · D-14 + D-16 + D-17, merged — naked bespoke buttons where `Button`/`.interactive-item` are reachable (S-7 pattern), an icon-only toolbar with zero tooltips (`title=`/`Tooltip` grep → 0 while the parent uses `title=`), and the `opacity-70` wand reading as disabled inside a `justify-evenly w-full` anti-cluster.** → **KF.W6**.
- **KF-KE-32 · D-18 — `bg-background` painted over the `--card` surface the component just dropped a Card to honour** (1.42:1 near-black rectangle on warm brown in dark; tokens verified by both readers). → **KF.W6**.
- **KF-KE-33 · D-19 + L-m5 — the `framed=true` default branch is unreachable** (one render site, `:framed="false"`; the lazy barrel has zero consumers); 13 duplicated template lines incl. a twice-claimed `ref="cardList"` maintain a phantom. → **KFED-UNIT** hygiene (delete the prop or its default's prose; collapse the fork).
- **KF-KE-34 · D-22 — the retiming Slider has no name, label, readout, ticks, or domain explanation**; glass forwards ONE `aria-label` to ALL thumbs (reader-1's cure note adopted: `LabeledSlider` names the group; thumb identity needs per-thumb `aria-valuetext`). → **KFED-UNIT** (one spec with KF-KE-2).
- **KF-KE-35 · D-23 — `DialogDescription` nested inside `DialogTitle`** (verbatim at `KeyframesAddDialog.vue:24-31`, my read): corrupted accessible name, `<p>`-in-`<h2>`, Card primitive inside Dialog primitive, `DialogHeader` unused. → **KFED-UNIT**.
- **KF-KE-36 · D-15, carried as filed (unchallenged by both readers) — `useToolbarKeyboard` (111 L) shadows glass `useTabRovingFocus`**, with the honest toolbar-vs-tablist reservation intact: **evaluate**, not mechanical-swap (read the param type; if tab-coupled, drop to INFO). → **KF.W6** evaluate. (Its correctness is superlative #2.)
- **KF-KE-37 · D-20 + D-21 — no loading state (prose overclaims one) and start-field errors exiled to a global toast with nothing at the field** (no `aria-invalid`/`describedby`/tint; glass `InputProps.invalid` + the forced-colors `Mark` affordance unpassed — reader-1's sharpening adopted). **Severity-raising cross-reference (this seat): at the audited bytes every toast in the demo is INVISIBLE — the banked kf-DemoGlobalChrome BLOCKER (vue-sonner stylesheet imported nowhere) means this component's entire error posture currently surfaces NOTHING; the field-level marking is the only channel that would have survived.** → **KFED-UNIT**.

### MINOR

- **KF-KE-38 · D-24** — dead/typo'd classes: `bottom` ×2 (inert — reader-1's Monaco-scoped-only verification adopted), the literal word `class` at `KeyframesAddDialog.vue:41` (verified). → **KFED-UNIT** hygiene.
- **KF-KE-39 · D-25** — `overflow-x-scroll` on the 40 px toolbar (gutter reserved unconditionally); glass ships `.scrollbar-*`. → **KF.W6**. Painted gutter → SS-13.
- **KF-KE-40 · D-26 + D-27 + D-28** — three spacing no-ops, 32/24 px asymmetric footer rhythm, and `sticky bottom-0` + `m-4` floating the footer 16 px above the scrollport floor. → **KF.W6** polish.
- **KF-KE-41 · D-6, demoted MAJOR→MINOR (reader-1's kill-of-severity adopted, both traps verified by this seat)** — `z-modal` on a per-card input breaches the written z-contract, but the escape is doubly contained: `.keyframes-editor-scroll { container-type: inline-size }` AND glass `.glass-card { contain: paint }` at the sole host. A contract violation with no live consequence. → **KFED-UNIT** hygiene (`z-content`).
- **KF-KE-42 · D-30** — physical-only offsets (`top-2 right-4`); repo-level RTL posture, correctly scoped. → **KF.W6** repo row.
- **KF-KE-43 · D-31** — forced-colors reaches exactly one control by accident (`aria-pressed` on Apply — which KF-KE-4 makes a state LIE in the only mode that renders it); the rainbow bar and hljs plate erase under `Canvas`. → **KF.W9** (with the banked KF-CE-13-class demo-wide row).
- **KF-KE-44 · D-32** — prose: a description restating its title, "the target" as internal vocabulary in an aria-label, `Add Keyframes`/`Add keyframes` case drift, label-icon abutment. → **KFED-UNIT** hygiene.
- **KF-KE-45 · D-33 as-rescoped** — the `.tap-floor` dead-utility half **FOLDS to banked KF-CB-25** (zero adopters — that count is already rostered); surviving here: 24 px targets at the demo's own 44 px written floor and the gapless X↔CopyButton pair — a pair that is occluded outright today (KF-KE-5), so the touch-target question goes live only after the un-occlusion. → **KF.W6**.
- **KF-KE-46 · D-34 = L-m4** — `onUpdateStart` hand-rolls `percentSelector`, already exported and already imported by the closure — the third skipped-helper site, which is what made the blockers a pattern. → **KFED-UNIT** (with KF-KE-2/3).
- **KF-KE-47 · L-m2 (+ reader-2's refinement adopted)** — `cardInstances` is never truncated; after a removal the stale slot is `null` (Vue nulls function refs on unmount), so the neighbour-pop targets nothing; whether `AnimationGroup.of(…, jumpUp(null)).play()` no-ops or REJECTS (which would abort the delete entirely) → SS-13 #6. → **KFED-UNIT**.
- **KF-KE-48 · L-m3** — `tabsListEl` never bound; `getFormatWidth()` always `undefined`; the responsive print-width path is dead and props up KF-KE-15's deep import; the `!` at `:32` asserts a value that is always null. → **KFED-UNIT** hygiene.
- **KF-KE-49 · L-m6 (reader-2's sharpening adopted)** — the self-declared backwards-compat barrel re-exports 20 members with **6** dead (`tabsListEl`, `animationUUID`, `debouncedUpdateAllStrings`, `updateAnimationFromKeyframesString`, `storedControls`, `getFormatWidth`), against the standing no-shim law. → **KFED-UNIT** hygiene.
- **KF-KE-50 · L-m7** — stringly `templateRef: string` beside correctly-modeled getters in the same interface. → **KFED-UNIT**.
- **KF-KE-51 · L-m8** — module-scope `kfEngine()` in `useKeyframeBrushApply.ts:6` turns a swallowed warm failure (`main.ts` `.catch(() => undefined)`) into a chunk-eval throw for the whole scene; the component's own setup-scope read is the model. → **KFED-UNIT**.
- **KF-KE-52 · L-m9** — two writers of the `highlighted` marker disagree on truthiness (`""` at `:110` does not arm the guards; `"true"` at `:124` does — my read). → **KFED-UNIT** (one marker, one meaning; with KF-KE-60).
- **KF-KE-53 · C-m1** — root-barrel + subpath glass imports mixed in one closure; honestly filed as consistency-not-bundle-size (`sideEffects: ["*.css"]`); the blast-radius clause re-scopes with KF-KE-16's frontier cure. → **KF.W6**.
- **KF-KE-54 · C-m2** — `kf-engine.ts`'s "the one place value.js enters the graph" is false as written (three static edges in this closure alone); the cited gate scopes `src/` only; correctly filed documentation-accuracy, value.js IS declared. → **KFED-UNIT** prose cure; the ingress note folds by reference to the megatranche R1 identity (per the KF-CE-12 precedent) → **KF.W2/W3** scoping input.
- **KF-KE-55 · C-m4** — `kfControls.keyframes` written by the sibling, read by nothing. → **KFED-UNIT** hygiene.
- **KF-KE-56 · C-m5** — the side-effecting `format` callback prop beside a one-way emit on the same element; three hand-maintained writers of the same two mirror cells. → **KFED-UNIT**.
- **KF-KE-57 · C-m6** — `@update-c-s-s`, the hyphenation accident, declared at two levels. → **KFED-UNIT** hygiene (`update:css`).
- **KF-KE-58 · C-m7** — three async engine calls opt out of the closure's own `withErrorToastAsync` contract (`:45`, `:210`, `:280`); no `app.config.errorHandler`. Reachability honestly UNPROVEN; the asymmetry is the row. KF-KE-4 raises the stakes note adopted: with Apply inert, an unhandled rejection here can be a user's only signal. → **KFED-UNIT**.
- **KF-KE-59 · reader-1 miss M-6** — mixed radius vocabulary (`rounded-panel` vs `rounded-xl` resolve identically today — 12 px, my token read — and desynchronize on any `--radius-panel` retune; `rounded-lg` = 10 px beside them). → **KF.W6**.
- **KF-KE-60 · replacement rows for the killed L-M2/D-11 (this seat's booking, per the ruled table)** — (i) `highlight()`'s `innerHTML` write on the Vue-owned `<pre>` orphans the `<code>` vnode target: within one card's mount, model→view text patches land on a detached node — **masked today by KF-KE-25's remount-everything and ARMED the moment KF-KE-25 is fixed** (a repair-coupling trap the KFED-UNIT spec must state); (ii) per-keydown `highlightAll()` is pure overhead after a card's first paint; (iii) text typed after first highlight inherits a neighbouring token colour for ≤ ~1 s until the next reprojection — bounded, not permanent. The add-dialog `<pre>` (outside any v-for) is the one place the one-shot claim was true. → **KFED-UNIT** (highlight into the `<code>` element, or re-render + re-highlight on commit).
- **KF-KE-61 · reader-2 miss M-7** — deleting the last keyframe is a silent no-op: the editor duplicates the ops layer's guard (`:233-235`, my read) but drops its toast (`useKeyframeOps.ts:194-196`), leaving "Cannot remove last keyframe" with no live caller. → **KFED-UNIT**.
- **KF-KE-62 · reader-1 miss M-7 (+ provenance CORRECTED by this seat)** — the delete-X comment (`KeyframeCard.vue:15-19`) justifies `data-destructive` by citing `proof:accent-census` — which is **not fiction but not real either**: named as an oracle in `docs/tranches/T/verdicts/APPEARANCE-WAVES.json:27` (a T-era spec) and never shipped (package.json ships `proof:publish`/`proof:owner-golden` only; my grep). A five-line comment asserting machine-checked provenance for a marker nothing reads. The companion prose defect stands: `useKeyframeBrushApply.ts:15` is exactly the deliberate-sharing sentence C-B2's remediator needs. → **KFED-UNIT** prose cure (frontier-durable — the comment survives at `origin/master`).

### INFO

- **KF-KE-63 · D-35 = L-m1** — dead emits with the one divergent payload shape in the demo (`animationId` vs `animation`, 6 sibling declarations); the outbound interface is inert (see KF-KE-18). → **KFED-UNIT**.
- **KF-KE-64 · D-36** — the shared `#highlightjs-theme` teardown is safe only by parent/child unmount order; no refcount; latent until a third consumer. → **KFED-UNIT** note.
- **KF-KE-65 · L-i1 + L-i2** — the unguarded `!` where the dialog twin guards; the `Ï` dead-key branch guarding a reformat this surface doesn't have. → **KFED-UNIT** hygiene.
- **KF-KE-66 · reader-2 miss M-8** — every editor mount appends (and every unmount unconditionally removes) an empty `<style id="keyframes-style-…">` whether Apply is ever used; lifetime a function of mount ordering. → **KFED-UNIT** (with KF-KE-6's refcount).

## Killed-claims register — refutations re-executed or extended by this seat

1. **D-29 (killed by reader-1; reader-2's confirmation REVERSED)** — the exact falsifier D-29 named exists: `@layer base { * { @apply border-border } }` at `demo/styles/style.css:210-213` (my read); reader-1's built-CSS byte-position argument (same layer, later source order, wins) adopted. The toolbar border is the standard `--border` hairline.
2. **D-3 (killed by reader-1; reader-2's BLOCKER confirmation superseded)** — arithmetic correct on both readers' independent derivations, premise inverted: the plate paints in front of the annotation (my tree-order derivation at KF-KE-5). The `f`-row legibility question returns only after the un-occlusion, against `--card`, not the plate.
3. **D-2 write half (killed by reader-1, sustained)** — the freeze precludes the 5000 % write; strict-mode throw at i=0; L-B1 and D-2's write limb cannot both be true, and L-B1 is the one the probe proves. The remediation-ordering warning is carried inside KF-KE-2.
4. **L-M2 / D-11 permanence (killed by reader-2, sustained; reader-1's confirmation corrected)** — mechanism re-derived at my `useHighlightCSS`/`KeyframeCard` reads (ruled table). Survivors rostered as KF-KE-60.
5. **L-M1's "wholesale structural replacement" premise (killed by reader-2, SUSTAINED BY EXECUTION)** — my probe: `fromString` appends (3→6, `0,0,0.5,0.5,1,1`). The conclusion (post-re-sample desync) was right for the wrong reason; the true behaviour is worse and is KF-KE-10.
6. **D-5's falsifier evidence line (killed by reader-2, sustained and EXTENDED)** — my grep: 7 lines / 4 live opt-in sites, including two (`TypingDots.vue:91`, `AnimationVisualizer.vue:147`) reader-2's own kill missed. The defect survives; the corpus's counting around it did not.
7. **C-M5 as a MAJOR defect (killed to INFO by reader-1, sustained by my own d.ts read)** — glass toast cannot express a caller-stable id, external dismiss, or an action contract; the fork is justified today. FOLD to the S-9 evaluation identity (banked at kf-DemoGlobalChrome) + a BH-relay forward ask on the toast API. Not a roster row.
8. **D-6 at MAJOR (killed to MINOR by reader-1, sustained)** — both containment traps verified by this seat (`container-type: inline-size` at the host; `contain: paint` on `.glass-card`). Booked KF-KE-41.
9. **D-4's supporting sentence (killed by reader-1, sustained with a provenance nuance)** — no `proof:accent-census` gate exists in the tree; but the oracle IS specified in `APPEARANCE-WAVES.json:27` (my grep, which reader-1's `data-destructive`-shaped grep could not see) — "specified-but-unshipped", not pure fiction. The defect D-4 stands (as KF-KE-7); the prose row is KF-KE-62.
10. **L-i3's timing figure (killed by reader-1, sustained)** — `debounce(updateAllStrings, 100)` at `useKeyframesParsing.ts:62` (my read); the 1000 ms belongs to the ops debouncers. The caret concern survives inside KF-KE-25/26.
11. **C-M5/C-M6 "recoverable-line" framing (killed in part by reader-2, sustained)** — S-9/S-10 are EVALUATE census rows, not replace-now line-count; adopted in KF-KE-21's routing and the C-M5 fold.
12. **Reader-2 miss M-6 (REVERSED by this seat)** — `play-lifecycle.ts:214/:376` are real standalone-path PRM reads; the claimed library gap does not exist; the cure lives in the options bag at two of the three sites (ruled table; KF-KE-8).

## Adjudicator corrections register (against this corpus's readers)

1. **Reader-2's D-29 confirmation cell** — refuted by a rule its grep could not see (`@apply border-border`); a grep-shaped artifact in a confirmation, the third instance of this failure class in the program (cf. kf-CopyButton corrections #1/#3).
2. **Reader-1's D-11/L-M2 confirmation cell** — endorsed a permanence claim its own D-10 endorsement contradicts; reader-2's kill adopted; survivors re-booked under correct claims (KF-KE-60).
3. **Reader-2's missed M-6 REVERSED** — contradicted by the engine bytes AND by the banked kf-CopyButton grep in the registry the reader was writing into (the same not-consulting-the-registry failure as kf-CopyButton correction #4).
4. **Both readers' PRM demo counts wrong** (5 and 3 vs the true 7 lines / 4 opt-in sites) — the counting-unit law (pt-leaf ruling-6) again; neither count changed a verdict, both would have misdirected a remediator.
5. **Reader-1's miss M-8 re-booked a banked row** — the CopyButton `isCopied` name latch is **KF-CB-3** (kf-CopyButton roster, MAJOR there vs the re-filed MINOR); the identity guard exists precisely for this. FOLDED, not rostered.
6. **Both readers frontier-blind** — the corpus's two most consequential repairs (FE-3's `startScalar`, EE-03's length-watch) were live at `origin/master` throughout, reachable by one `git show` each; the kf-CopyButton precedent (its correction #7) was in the registry both cite. This seat's frontier probes are the governing addition to this record.
7. **Both readers' C-B2 path anchors drifted** — `ChannelControls.vue` and `useKeyframesPaneReveal.ts` live under `transport/channel-controls/`, not `controls-pane/`; every mechanism claim survives at the corrected paths (my reads).
8. **Neither reader caught the EE-03 identity** — L-M1's dead watch was already dispositioned (`DISPOSITIONS.md` §D, BUILD W1) with the exact cure the frontier now carries; both treated it as a fresh confirmation.
9. **Reader-1's M-7 provenance** — "there is no red-census proof" is true of `package.json`/`scripts/gates` and incomplete about the tree: the oracle is specified in a T-verdict artifact (`APPEARANCE-WAVES.json:27`). The comment's present-tense claim stays false; the history is not fiction.

## Superlatives — sustained, with provenance

1. **The scoped-highlight ownership contract** (all three axes + both readers; re-walked by this seat: `defineExpose({preEl})` → `getPreElements()` → the lazy getter at `:176-178`, zero `querySelector` in the path, the replaced bug named in prose) — the model seam, in the same file that got the apply seam (KF-KE-4) and the child-typing seam (KF-KE-17) wrong.
2. **`useToolbarKeyboard` is a correct WAI-ARIA toolbar** — true-modulo wrap, `[disabled]`+`aria-hidden` cohort filtering, container-based query, `queueMicrotask(refresh)`; both readers re-verified line-by-line. Correctness sustained; existence remains KF-KE-36's evaluate.
3. **The offset-field error posture (S-5/C-S5)** — the typed `ParseIssue` surfaced verbatim, stable per-index toast id, explicit dismiss, whole-selector replacement — the reference implementation of the write `:43` should have made, asking the wrong parser (KF-KE-3). The irony is exact and the tuple-indexing soundness (L §5 K-1) holds.
4. **`KeyframeCardList`'s formatter degradation (SUP-6/C-S2)** — `shallowRef` seeded null, published-accessor resolution, semantically-valid fallback, rationale written down; the proof the correct route existed for every KF-KE-15 reach.
5. **Animate-then-commit on removal (S-4/C-S4)** — awaits the exit motion against live DOM, current API, provenance recorded, no shim; the no-backwards-compat law honoured where a shim was cheapest.
6. **The heavy/light engine split (C-S1)** — type-only barrel import, setup-scope `kfEngine()` after the warm, every other touch through `loadAnimationEngine()`; the one divergence (KF-KE-51) is in a dependency.
7. **Icon-layer hygiene (SUP-3)** — `aria-hidden` on the wand, `pointer-events-none` on the brush glyph inside its button; both usually missed.
8. **`parseAnimationCSS`'s kept invariant + `withErrorToastAsync`** (reader-2's addition, adopted) — one grammar authority, no regex pre-detection, structured diagnostics converted to typed errors, Retry actions; why KF-KE-58 is an asymmetry rather than a missing posture.
9. **The engine's selector guard (`frame-compiler.ts:135-168`)** (reader-2's addition, adopted) — fail-explicit, total, correctly frozen; what makes KF-KE-2 a consumer defect, not a library one.
10. **RESCOPED — SUP-4 (`aria-pressed` on Apply)**: the semantics are right and bought the only forced-colors state indicator for free — but KF-KE-4 makes the asserted state a lie whenever it is `true`. The technique survives; the truth condition transfers to the apply-identity fix.
11. **RESCOPED — SUP-5 (the destructive token measures true)**: the `--accent-red` arithmetic holds (re-derived by reader-2 to published digits) and the T.D7/OD-6 discipline is real — on a glyph that is currently painted under an opaque plate (KF-KE-5). Token fact sustained; in-situ claim mooted until the un-occlusion. *(SUP-7, the CopyButton live region, is inherited RESCOPED by reference to banked KF-CB-9 — the sink is nested where the a11y tree may flatten it.)*

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **KF-KE-5's rendered result** — screenshot a card WITH the theme injected; attempt a pointer click on the X and confirm the `<pre>` swallows it; confirm a keyboard-focused CopyButton paints invisibly. The highest-value probe: if it holds, the card has no working per-stop delete or copy at all.
2. **KF-KE-4's press signature** — press Apply on `/spring`; predicted: the sweep freezes (paused = started), nothing visually applies, `aria-pressed="true"`; one click also settles L-M3's `prevPaused` branch.
3. **KF-KE-10 at the frontier** — press "re-sample from spring" twice; predicted: 10 then 15 duplicate cards (the cured length-watch makes the append visible).
4. **KF-KE-26's toast noise perceptibility** — per-keystroke `toast.error` replace-by-id cadence; note this probe is meaningful only AFTER the banked kf-DemoGlobalChrome toast-reachability blocker is cured (toasts are invisible at these bytes).
5. **KF-KE-6's unmount ordering** on route-leave (which owner removes the shared `<style>` first) — mechanics source-proven, ordering runtime-dependent.
6. **KF-KE-47's second-consecutive-delete** — whether `AnimationGroup.of(…, jumpUp(null)).play()` no-ops or rejects; a rejection silently aborts the delete (worse than filed).
7. **KF-KE-25's blank flash** and caret landing after the remount (microtask-turn dependent).
8. **KF-KE-58's reachability** — whether any engine-generated string makes `prettier.format(…, {parser:"scss"})` reject.
9. **D-17's dead-space figure** at real breakpoints and **D-25's painted gutter** (platform-dependent) — inherited, correctly self-marked by the D axis.
10. **KF-KE-2 post-fix regression frame** — after the unfreeze+unit fix, verify a mid-track drag writes 0.50 (not 50): the D-2 write-limb hazard is the thing the fix must not create.

## Closing verdict

**ADJUDICATED — RED, at keyframes.js worktree-on-`8281638c` (the standing SCH-1 stale fork, 41 behind `origin/master`; frontier durability probed row-by-row by this seat, which neither reader did): 66 roster rows — 6 BLOCKER · 31 MAJOR · 25 MINOR · 4 INFO. Two blockers are already repaired at the frontier and fold to their banked identities (FE-3's `startScalar`, EE-03's length-watch) with re-regression guards as the surviving obligation; four are frontier-durable, chief among them a feature that has never once worked (Apply-CSS's class ≠ selector, probe-proven) and an action cluster painted under its own code plate — the two defects every axis reasoned around without seeing. 12 killed-claims sustained (three deciding a head-on reader collision, one extended, one reader miss reversed against the engine bytes and the banked registry); 14 misses admitted, 1 fold-caught re-booking; 9 reader corrections. The component is the strongest argument in the program for the KF.W4 type gate and the KFED-UNIT spec: its seams that were DECLARED (the expose contract, the error posture, the heavy/light split) are exemplary, and its seams that were merely ASSUMED (the selector's type, the apply identity, the paint order, the projector watches) are where all six blockers live. Routings are M-25 agglomeration input: behavioral rows to KFED-UNIT (NO-WAVE-OWNER, SS-1/SS-2, after KF.W0 + KF.W4); folds to FE-3, EE-03, KF-CE-12, KF-CE-16, KF-CB-3/11/25, F-1/SCH-1 (KF.W0), S-9, R1; census asks S-10 + the §6.3 class-space annotation (KF.W6, E-3); one library letter (KF.W5, `fromString` append semantics); one producer relay (glass toast id/dismiss/action surface, BH channel); visual residue (10 rows) to KF.W9/SS-13. Nothing here stamps a wave; X·KF executes on the owner's begin-word.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.
