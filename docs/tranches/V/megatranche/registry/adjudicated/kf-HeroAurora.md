claude-fable-5

# kf-HeroAurora — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ HEAD `8281638c` (2026-07-28) with a **CHURNING working tree — 252 dirty rows**. `HeroAurora.vue` itself (128 lines, `demo/components/instrument/shell/`) is **CLEAN**, as is `App.vue` (this seat's scoped `git status --porcelain` on both paths → empty); `package.json`/`package-lock.json` are **DIRTY** and are the provenance trap that manufactured this corpus's one false BLOCKER. **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two hostile re-reader returns (**reader-DU**, design-and-usage · **reader-LC**, library-and-code) over the component's three challenge files (`audit/kf-components/HeroAurora/challenge-D-design.md` 404 lines · `challenge-L-library.md` 404 · `challenge-C-consumption.md` 315). All three challenge files were re-read whole by this seat; both returns were attacked before either was adopted.
**Method (incredulity)**: every cross-reader verdict contradiction (there are two direct ones — D-4 and the D-5 swirl-authorship arm), every kill of a load-bearing cell, and every admitted miss was re-derived against the bytes with this seat's own instruments. **Zero browser probes** (probe parsimony; the lane's no-browser law holds — every dynamic claim routes to SS-13). One node execution was run fresh by this seat against the installed dist (the light-drop + spread-delta + palette-band probe — the fifth independent execution of the corpus's central mechanism, and the decider for three rulings at once). Dispositions are **ADJUDICATED, not VERIFIED**; X·KF specs are still forming — every routing below is agglomeration input (M-25), not an act.
**This seat's own instruments (all read-only + one node run against the installed dist)**: full read of HeroAurora.vue (whole, 128); the six `.aurora-*` rules extracted whole from `node_modules/@mkbabb/glass-ui/dist/glass-ui.css` (placeholder at `opacity: var(--aurora-opacity-ceiling,1)` · `.aurora-canvas--armed` at the same var · both `grid-area:1/1` · **no placeholder-hide rule** · sole media query = PRM `transition-duration:1ms`); the compiled render fn at `dist/aurora.js` ~:188239 (root carries `style: { "--aurora-opacity-ceiling": o.value }`; the placeholder div emitted **unconditionally** in the children array — no gate); the fragment epilogue verbatim (`fragColor = vec4(col * uAlpha, uAlpha)`, premultiplied display-space); `resolveRenderMode` (`function M`) read whole — **no reduced-motion branch**; the Aurora.vue.d.ts docblock verbatim ("Cursor interaction is deliberately not wired here — use `useCursorInteraction` (**or call the exposed `setCursor` API**) … so the consumer controls pointer policy"); `setCursor` compiled (`function b(e, t, n = .8)`) + the defaults pair `l = .8, u = .25` (strength ceiling 0.8, cursor radius 0.25); the installed `atoms.d.ts` swirl doc verbatim ("the cursor SWIRL/luminance-lean axis, MEDIUM-GATED … **Default coupled to `light` when unset**"); **node probe**: `resolved.interactivity = {"swirl":true,"amplitude":0.5}` (light GONE) · cured control `{"light":true,"swirl":true,"amplitude":0.5}` · FINAL medium=crayon impasto=0 alpha=1 · saturation 0.913→0.92 · **exactly 7 of 12** ground keys change the resolved config (`medium, strokeOrient, granulation, canvasGrain, strokeAmount, strokeAnisotropy, saturation`) · palette L stops **0.4203 / 0.5270 / 0.6336 / 0.7403**; EditorStartScreen.vue:140-160 (`.hero-hint { … font-size: var(--type-title); color: var(--muted-foreground) }` — the **sole** `muted-foreground` hit across the three shell files, my grep) + installed `scale.css` `--type-title: 2.058rem` (≈32.9px at 16px root — WCAG large-scale); the **dependency decider**: `git show HEAD:package.json` → `:3 "version": "5.3.5"`, `:68 "@mkbabb/value.js": "^3.1.0"`, `:70-71 optionalDependencies { "@mkbabb/glass-ui": "6.0.0" }`; working tree → **zero** glass-ui hits in package.json AND package-lock.json; installed → **7.0.0**; `.npmrc` = exactly one line `legacy-peer-deps=true`; installed glass-ui `peerDependencies` verbatim: `@mkbabb/keyframes.js ^6.0.0` · `@mkbabb/value.js ^4.0.0` · `@mkbabb/pencil-boil ^0.9.2` (+7 more); `ls node_modules/@mkbabb/` → glass-ui, parse-that, value.js (**no pencil-boil**); forced-colors census: `accessibility.css`/`a11y-fallback.css`/`a11y-overrides.css` each carry the query, each with **0** `aurora` hits; reduced-transparency census: 1 hit each in `a11y-fallback.css`/`paper.css`/`glass-ui.css`, **0** in demo/; `grep -c vue-tsc package.json ci.yml` → 0/0; App.vue:139-142 verbatim ("colocated in `editor-shell/`…") vs `find demo -type d -name editor-shell` → **0**; shell/index.ts (exactly 4 of 8 exported, HeroAurora absent); proof scripts (exactly `proof:publish` + `proof:owner-golden`; `grep -rn "cursor-light|no-hand-rolled" scripts/ test/ src/` → **0**); the z-contract (style.css:29-30 `--z-content: 10` / `--z-controls: 20`; EditorStartScreen.vue:18 hero band `z-controls`; EditorShell.vue:60 slot wrap `z-controls`); registry authorities: `kf-App.md` rows **KF-APP-15/-16/-48** + its :140 hygiene-superlative sustain; `kf-AnimatedText.md` ruling 1 + **KF-AT-7/-12/-26(c)**; `formation/keyframes/CENSUS-2026-08-03.md:179-205` (the KF.W0..W10 taxonomy).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**20 adjudicated roster rows — 0 BLOCKER · 7 MAJOR (3 of them fold-by-reference to banked identities) · 7 MINOR · 6 INFO.** **14 killed-claims register entries** — 11 sustained reader kills (two of them kills of cells the OTHER reader had CONFIRMED), 2 seat corrections against reader-DU's gradings, 1 seat overruling of reader-LC's D-4 confirmation-plus-superlative. **10 distinct admitted misses across the two readers, all adjudicated real** (one — the unexportable ceiling const — found independently by both, the corpus's strongest convergence). **12 superlatives sustained.** **The headline BLOCKER of both original passes does not survive severity adjudication; the headline reassurance of the D axis (the WCAG vindication of the 0.1 ceiling) does not survive at all.**

MAJOR-weight survivors of record: **KF-HA-1** (the cursor-as-light cluster: dropped at the door, type-invalid against the installed artifact, and inert-by-construction on this ground even when cured — a dead named intent under four false prose lines) and **KF-HA-2** (reader-DU's discovery, verified whole by this seat: the delivered wash is structurally ~0.19, roughly DOUBLE the authored 0.1 bound and ABOVE the P-HERO 0.15 the amendment was cutting below — the placeholder is never hidden under the armed canvas and both ride the same ceiling). Realized shipping impact today is **latent-to-modest at every MAJOR**: the wash renders, the swirl interaction runs, nothing throws. These are contract, honesty, and bound defects — the exact register this formation audits.

## The incredulity seat's own work — ruled disagreements and corrections

1. **D-4 / the WCAG ceiling — the corpus's one direct verdict contradiction, RULED FOR reader-DU's KILL; reader-LC's CONFIRMED cell and its S-5 superlative OVERRULED.** reader-LC reproduced the D axis's arithmetic to three decimals and crowned it "the single most rigorous artefact in the corpus"; reader-DU killed the conclusion twice over. This seat re-derived both kill legs at the bytes and they hold. **(a) Wrong threshold**: the only `--muted-foreground` surface over this layer on the home route is `.hero-hint` (EditorStartScreen.vue:147-154), at `font-size: var(--type-title)` = 2.058rem ≈ **32.9px** (installed scale.css, my read) — WCAG **large-scale** text, SC 1.4.3 threshold **3:1**, not 4.5:1; the real margin is ~53%, and even the prototype's 0.15 passes. My grep across the three shell files finds exactly one muted-foreground declaration; the other hero surfaces are `--foreground`-based. **(b) Wrong number**: the axis's premise "the wash composites at exactly the ceiling" is false — see ruling 2; true coverage ≈0.19, at which the axis's own 4.5:1 arithmetic yields ≈4.04:1, i.e. had its threshold applied, the SHIPPED value would already fail. An AA boundary at 0.1167 for text actually rendered over this layer **does not exist**. RULING: D-4 is **KILLED as graded → INFO** (the arithmetic is exactly reproducible and is retained as a receipts-superlative); D-S-3 ("the owner's instinct landed on the correct side of an accessibility boundary") is **KILLED** — 0.1 stands on taste and the owner's amendment, exactly as originally recorded; reader-LC's S-5 is **amended** to "exemplary receipts, refuted conclusion." The lesson is the ColorSpaceSelector cure-separation law's evidential twin: *a reproducible computation inherits no correctness for its threshold premise or its compositing premise* — reader-LC re-ran the numbers and never asked what text they applied to.
2. **The double-layer stack (reader-DU MISSED #1) — ADJUDICATED REAL at MAJOR; verified whole by this seat; missed by reader-LC and by all three axes.** My own three receipts: (i) the six aurora CSS rules extracted whole — `.aurora-root>.aurora-placeholder { opacity: var(--aurora-opacity-ceiling,1) }` AND `.aurora-canvas--armed { opacity: var(--aurora-opacity-ceiling,1) }`, both `grid-area:1/1`, **no rule hides the placeholder when the canvas arms**; (ii) the compiled render fn emits the placeholder div unconditionally (no gate, no `isArmed` branch) and stamps the ceiling on the root as a custom property; (iii) the fragment epilogue is `fragColor = vec4(col * uAlpha, uAlpha)` with probed `alpha = 1` — the canvas is opaque wherever it draws. Composite = 0.81·page + 0.09·placeholder-ground + 0.10·canvas ≈ **0.19 presence** — roughly double the authored bound, above the 0.15 the amendment cut below; and `.aurora-canvas` transitions 0→ceiling over `--duration-slow`, so the home route **brightens from 0.10 to 0.19 presence shortly after first paint**, with the banked eager-arm row (KF-APP-16) putting that ramp at the LCP moment. All three axes and reader-LC treated 0.1 as the presence; the D axis stated it outright and computed a WCAG table on it (ruling 1's kill leg b). The mechanism is **producer-owned** (glass-ui's placeholder-not-hidden-when-armed under a uniform `opacityCeiling`); per the standing BH/BI edict the primary cure is a **glass-ui relay**, the consumer question ("was 0.1 or 0.19 the number the owner blessed?") is a KF.W6 decision, and the perceptual magnitude is SS-13's.
3. **KF-HA-1 severity — the BLOCKER dies; MAJOR ruled; reader-LC's BLOCKER preserved as dissent.** reader-LC filed the light cluster at BLOCKER ("the component's single named design intent is dead in the shipped build"); reader-DU killed the grade with the corpus's own halves joined: kills (b) and (c) establish `light` is **inert on crayon + impasto 0** — my probe confirms the fully-cured control differs from the shipped config by exactly one key (`interactivity.light`) that no shader path reads for `uMedium == 4` — so the visible delta between shipped and cured is **zero**; no user loses anything they could have had. The house calibration is already banked (kf-AnimatedText ruling 5a: C's "nothing fails at runtime" adopted at MAJOR over L's BLOCKER dissent for the same shape of defect) and this seat applies it identically. What remains is fully MAJOR: a type-invalid literal shipping past a gate that cannot see it (TS2345, twice independently compiled), a dead named intent carried in three registers (code, comment, T-verdict ledger), and four false prose lines. Also folded here: reader-LC's **rescope of the WGSL leg is adopted over reader-DU's standalone-MAJOR grading of D-1b** — `uLightDir`'s absence holds for the WebGPU cohort only (the WebGL2 net HAS the uniform; Safari/Firefox/WebGPU-less hosts take that leg), and kills (a) and (c) are backend-independent and each individually sufficient; stating leg (b) as a universal kill invites a reviewer who tests it on Safari to draw the wrong lesson.
4. **The D-5 swirl-authorship arm — the second direct contradiction, RULED FOR reader-DU's kill; reader-LC CORRECTED (citation-inheritance).** The D axis wrote "Nobody chose this … the author wrote `interactivity: { light: true }` and got maximum swirl" as collateral of the light drop; reader-LC endorsed it verbatim ("nobody authored the loudest knob in the file"). This seat's read of the **installed** atoms.d.ts settles it: the swirl axis is documented "**Default coupled to `light` when unset** (an interactive config reads on EVERY medium, not just painterly)" — authoring `light: true` is DOCUMENTED to yield swirl; the swirl is the sanctioned consequence of the authored intent, not collateral of the bug, and the `?? true` default fires with or without the drop. reader-LC repeated the challenge's framing against the producer's own contract without re-deriving — the same event class the ColorSpaceSelector DISSENT-1 recorded. What survives (both readers concordant, verified): the literal `1` at :94 against the compiled `n = .8` default, which no comment or verdict blesses, PLUS reader-DU's missed knob beside it — `setCursorRadius` is exposed and never called, so the one axis that runs does so at the library default **0.25** (my read of the defaults pair `l = .8, u = .25`): a quarter-viewport influence radius under a "more subtle" amendment, scrutinized by nobody.
5. **PRM synchronous-draw severity — MINOR ruled; reader-DU's MAJOR corrected by its own caveat.** The mechanism is the corpus's best library archaeology (sustained as superlative 10): `setCursor` → `wake()` → under PRM `tick()` runs **inline in the input task**, `raf` is permanently 0 so the short-circuit never fires, PRM never enters the suspend set, and the drawn frame is provably identical (`setPointer` PRM early-out + `masterTempo() = 0`). Both readers confirm it; they disagree on grade. reader-DU carried MAJOR while its own caveat concedes UAs coalesce `pointermove` dispatch to ~1/frame — which is the L axis's own published falsifier firing ("in which case the cost equals the non-PRM steady state") and the D axis's calibration ("each wake renders at most one frame and re-parks — a cost, not a runaway"). RULED **MINOR**: a display-rate wasted loop charged to the reduced-motion cohort, in the input task — real, cheap to cure (PRM early-out in the handler), not major. reader-LC's two adjacent misses merge here, both adjudicated real: the handler is also unguarded in the **cssOnly** cohort (runtime never constructed; `setCursor` is `inst?.` no-op) and across the **pre-arm window**, so the per-event `innerWidth`/`innerHeight` forced-layout reads (the L-addendum's self-caught C-1, sustained) run for nothing in all three degenerate states — and one cure (`useWindowSize()` from the already-imported `@vueuse/core`) removes the reads in every state at once.
6. **The C-1 phantom-dep BLOCKER kill — SUSTAINED, both readers concordant; the surviving identity FOLDS BY REFERENCE, never re-booked; reader-LC's peer-dependency discovery attached as the sharpening that closes the WHY.** Both readers killed axis C's BLOCKER identically and correctly (the axis attributed a dirty-worktree read to HEAD `8281638c`; my `git show HEAD:package.json` → `optionalDependencies { "@mkbabb/glass-ui": "6.0.0" }`, locked; CI builds committed refs). IDENTITY GUARD: the declaration churn is **already banked** — kf-AnimatedText ruling 1 folded it to census F-1/SCH-1 → **KF.W0** with the three-way-drift decider; this record adds no second booking. It adds two sharpenings to that fold: (i) **L:D-19's committed defect** (optional-field + 6.0.0-declared vs 7.0.0-installed major skew; the working-tree deletion is an uncommitted release-prep edit — `5.3.5→6.0.0`, value.js `^3.1.0→4.0.0`); (ii) reader-LC's discovery, **verified by this seat**: installed glass-ui 7.0.0's own `peerDependencies` demand `@mkbabb/keyframes.js ^6.0.0` and `@mkbabb/value.js ^4.0.0` — **both violated at HEAD** (5.3.5 / ^3.1.0) — silently absorbed by the one-line `.npmrc legacy-peer-deps=true`, with the third peer `@mkbabb/pencil-boil` **not installed at all**. That closes the open question: 7.0.0 is the glass-ui that pairs with the UNRELEASED kf 6.0.0, the dirty diff IS the pairing bump, and the corpus-prescribed cure "pin 7.0.0" is **not independently landable** at HEAD's 5.3.5 — the pin and the version bump are one commit, or 6.0.0 stays the correct pin. Corollary carried to KF-HA-1: every D-1 receipt in this corpus (five executions now, including mine) is scoped to 7.0.0, which `npm ci` at HEAD does not install.
7. **The @supports/S-6 cluster — concordant kills merged; the composite cure adopted that no single party proposed.** Both readers kill the L axis's S-6 ("the correct discharge of a house idiom"): a fallback for a redundant declaration is itself redundant — `.hero-aurora` is `position: fixed; inset: 0`, viewport-sized in every engine ever shipped, pre-`dvh` included, so the scoped `100dvh/100dvw` block AND its `@supports` fallback are wholly dead (reader-DU also correctly kills axis C's "two owners of one rule" framing — EditorShell's block targets its OWN two elements and never claimed the slot child; the scope-isolation fact stays as the reason a future reader must not delete the local block *and* rely on the shell). reader-LC's missed #4 completes it: the `.hero-aurora` wrapper itself is removable — Aurora renders a single root, so attribute fallthrough carries `class`/`aria-hidden` directly; deleting wrapper + scoped block removes one DOM node, 15 lines, the `@supports` block, and the whole S-6 argument. Adopted as the KF-HA-16 cure shape.
8. **Identity guard sweep (anti-rename) — three more folds, zero re-bookings.** (a) The eager-arm row is **KF-APP-16** (banked, KF.W6, magnitude → KF.W9); this corpus's deeper mechanism proof folds in as sharpening — the eager early-return precedes BOTH the idle defer and the intersection gate, the WebGL2 leg is synchronous inside the mount commit, axis C's BLOCKER grade was killed by reader-LC (concordant with the banked MAJOR-class grading; the intersection-gate sub-clause is inert for a `fixed inset-0` layer, the L-addendum's C-3), and the library's own deferred-path comment ("for a `fixed inset-0` Aurora this fires on the first watch tick — the idle defer still wins the first-paint budget back") makes the delete-the-prop cure **library-endorsed**. (b) The spread-override row is **KF-APP-48** (banked, KF.W6, already carrying the ≤7-of-12 correction — my probe independently reconfirms **exactly 7** changed keys, killing the D axis's "clobbers 12 fields" a second time). (c) The no-SFC-typecheck row is **KF-AT-12** (banked, KF.W4); this component supplies that row's sharpest consequence — a hard TS2345 shipping in the component a T-ledger row marks LANDED·REAL, plus the `13530f70` precedent (a version-bump break any typechecker would have caught, found instead by the hosted roster). (d) The colocation row is **KF-AT-26(c)** / U.B5 (banked, KF.W8); reader-DU's missed #3 attaches — App.vue:139-141 cites "`editor-shell/`", a directory that does not exist (my `find` → 0; the file lives in `instrument/shell/`), an active misdirection the U.B5 move-wave must correct in the same edit.
9. **The R1 cross-repo contradiction — carried up, not booked here.** Axis C's killed-hypothesis §4.1 and BOTH readers independently re-executed the same probe: pinned `@mkbabb/value.js@4.0.0`'s `./css` subpath handles `oklch()` forms cleanly (`ok`), and `var()`/`color-mix()` return **diagnostics, not throws**. This contradicts the memory-of-record framing "R1 = live `parseCssColor('oklch()')` shipping crash" as applied to this repo's pinned artifact — whatever R1 reproduces against, it is not 4.0.0's css subpath, and the genuine transitive exposure at this file is glass-ui's diagnostic→throw bridge (KF-HA-13). Three independent executions in one corpus is enough to carry: routed **NO-WAVE-OWNER** as a value.js-side registry carry (megatranche parser-band / STATE reconciliation surface), never a KF wave's row.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0..W10** per the census taxonomy (W0 substrate settle · W4 gate law · W5 D/L/C library tri-fold · W6 glass suffusion/demo · W8 structure & colocation · W9 Safari visual audit = the SS-13 surface · W10 fold discharge). Specs are forming; routings are agglomeration input (M-25). Line anchors are HEAD `8281638c` / the 2026-08-08 working tree (HeroAurora.vue and App.vue are clean; **package.json/lock are churning — re-resolve before any cure, D-19**).

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **KF-HA-1** | D:D-1(a/b/c) / L:D-1 / C:D-3+D-4 — severity ruled 3; WGSL leg rescoped per reader-LC | The cursor-as-light axis is dead three ways: (a) `interactivity: { light: true }` (:70) is dropped by `resolveAtoms` — the medium arrives one layer below the door via `...PAPER_WASH_GROUND` (:72), and the door assigns `interactivity` wholesale; (b) the same literal is a hard **TS2345** against installed 7.0.0 (`AuroraSmoothInteractivityAtom { light?: never }`; the light-bearing arm REQUIRES `medium`) — the defect is exactly one missing key; (c) even cured, `light` is **inert** on this ground: `mediumCrayon` never calls `relightImpasto` and `impasto: 0` makes both lit terms the identity — so the shipped-vs-cured visible delta is exactly ZERO. Leg (b′) `uLightDir` absent from the WGSL struct is **engine-conditional** (WebGPU cohort only; the WebGL2 net has the uniform), never a universal kill | this seat's node run (light GONE / cured control carries it / impasto 0); five independent executions total; atoms.d.ts union read; dispatch + relightImpasto call sites per both readers, concordant | **KF.W6** — one HeroAurora config motion with KF-APP-15/-16/-48. The cure is a DESIGN DECISION, not a patch (axis C's D-4 framing adopted): `medium:{kind:"crayon"}` restores type+carry but no light; the honest options are a stroke medium (abandons paper-wash), or delete the atom + the :57-59 prose (the owner's T-row wording was "done right OR removed") — decided WITH SS-13's P-1. The crayon-ground/light-axis incompatibility is producer-domain → **glass-ui BH relay** rides the standing edict. MAJOR (reader-LC's BLOCKER = recorded dissent) |
| **KF-HA-2** | reader-DU MISSED #1 — this seat's full verification (ruling 2) | The delivered wash is structurally ~**0.19**, roughly DOUBLE the authored 0.1 bound and above the P-HERO 0.15 the amendment cut below: placeholder and armed canvas are stacked `grid-area:1/1` siblings BOTH at `var(--aurora-opacity-ceiling)`, the placeholder is never hidden, the canvas draws opaque (`vec4(col * uAlpha, uAlpha)`, alpha 1); the canvas transition also ramps the page 0.10→0.19 presence just after arm — at the LCP moment under the banked eager row. Every axis and reader-LC computed on the false 0.1-presence premise | this seat: the six CSS rules whole; render fn unconditional placeholder + ceiling custom property; fragment epilogue; probed alpha=1 | Mechanism is producer-owned → **glass-ui BH relay** (placeholder-hide-on-arm, or a documented compose-mode). Consumer decision (which number was blessed; interim halved ceiling?) → **KF.W6**. Perceptual magnitude + the brightening ramp → **KF.W9** (SS-13 P-2). MAJOR |
| **KF-HA-3** | L:D-6 / D:D-3 + C:S-4's "both themes" scope-kill folded (reader-DU, sustained) | The dark leg is unaddressed while :55-56 claims "both themes": `config` is a plain setup-time literal — no `computed`, no token read, no `lightnessScheme` — seeded `#7c5ce6` (15.6° off-hue, ΔL −0.16, +54% chroma vs the dark token); my probe pins the derived ramp at L **0.42–0.74**, entirely the light-pastel band glass-ui's own doc names "the O-26 dark-leg defect"; the page carries a live dark toggle and the sibling grid ink DOES retint. The hue half of C:S-4 survives (the analogous ramp brackets both tokens' hues) but hue is one of three coordinates — bracketing a hue is not previewing a theme | this seat's palette-L probe; style.css:130 `light-dark()` token; atoms.d.ts lightnessScheme doc per both readers | **KF.W6** — thread the theme (`lightnessScheme` + reactive seed; the `OklchStop` seed form kills the KF-HA-13 seam in the same edit) or state in prose that the light band is deliberate at this ceiling. Magnitude → **KF.W9** (SS-13 P-3). MAJOR |
| **KF-HA-4** | L:D-5 / C:D-6 + the convergent cure-correction (both readers' miss, ruling in §killed K-note) | Both cited oracles (`proof:cursor-light-subtle` ×3 in-file + App.vue:44; `proof:no-hand-rolled-cursor-tracker` :5) were dissolved at `70b32501`; the owner-ruled 0.1 bound and the no-hand-rolled-tracker guard have ZERO executable coverage; the T stage-manifest row still names the dead oracle. AND the corpus's prescribed replacement is unwritable as specified: `HERO_AURORA_OPACITY_CEILING` (:46) is a `<script setup>` compile-local const — nothing to import; the assertable surface is the rendered `--aurora-opacity-ceiling` on `.aurora-root` (verified in the render fn) or an `export const` | this seat: proof-script census (exactly 2, neither named); 0 grep hits across scripts/test/src; the render-fn ceiling binding | **KF.W4** (gate law: rebuild the bound as a mount-and-read-the-style vitest, or export the const — the un-writable import form must not enter a spec). The in-file prose cites → KF.W6 file-touch; `docs/tranches/T/stage-manifests/home.json:9` → **KF.W10** (doc-truth addendum; dated evidence never rewritten). MAJOR |
| **KF-HA-5** | L:D-4 / C:D-5 — **FOLDS KF-AT-12** (banked, one identity) | No `.vue` file in keyframes.js is ever typechecked (no vue-tsc anywhere — my 0/0 grep; `check` = plain tsc; CI runs `check:lib` scoped to src/) — the enabling cause of KF-HA-1's TS2345 shipping unseen, in a component a T-ledger row marks LANDED·REAL, with the `13530f70` shipped-page-error precedent as the prior instance | this seat's greps; ci.yml:41-42; tsconfig.lib.json comment per readers | **Fold-by-reference → KF-AT-12 → KF.W4** (instrument law). This component's TS2345 is that row's sharpest consequence-witness; attach, never re-book. MAJOR (weight lives at the banked row) |
| **KF-HA-6** | L:D-19 + C:D-1-kill residue + reader-LC's peer-dep miss (ruling 6) — **FOLDS census F-1/SCH-1 via kf-AnimatedText ruling 1** | The glass-ui declaration is in churn (HEAD: optional 6.0.0 locked · worktree: deleted · installed: 7.0.0), the optional field promises a degradation path that does not exist for a load-bearing import, AND — the new leg, verified — installed 7.0.0's own peers (`keyframes.js ^6.0.0`, `value.js ^4.0.0`) are violated at HEAD, absorbed by `.npmrc legacy-peer-deps=true`, with `pencil-boil` absent entirely. "Pin 7.0.0" is NOT independently landable at 5.3.5 — pin and version-bump are one commit | this seat: the full dependency decider battery (HEAD show, worktree greps, installed manifest + peers, .npmrc, `ls node_modules/@mkbabb/`) | **Fold-by-reference → KF.W0** (census F-1/SCH-1 §B-12 substrate reconciliation, the banked three-way-drift row) with the peer-dep/.npmrc sharpening and the one-commit constraint attached. Bounds every KF-HA-1 receipt (all are 7.0.0-scoped; `npm ci` at HEAD installs 6.0.0). MAJOR (weight lives at the banked row) |
| **KF-HA-7** | D:D-2 / L:D-2 / C:D-2 (BLOCKER killed by reader-LC, sustained) — **FOLDS KF-APP-16** (banked, one identity) | `initStrategy: 'eager'` (:28) arms the GPU inside the mount commit — skipping the idle defer AND the intersection gate (inert here: `fixed inset-0` never leaves the viewport, L-addendum C-3) — against the file's own :8 "lazy WebGL arm past first paint"; the WebGL2 leg is synchronous compile-link; the arm re-runs on EVERY return to home (`v-if="isHome"`); the library's deferred-path comment refutes the only defence and makes prop-deletion library-endorsed | both readers' concordant chain (useAurora early return; armAsync sync WebGL2 leg); this seat re-read :8 vs :28 | **Fold-by-reference → KF-APP-16 → KF.W6** (delete the prop), magnitude → **KF.W9** (SS-13 P-4, >50 ms escalation bar, per-navigation re-arm measured). Severity MAJOR concordant with the banked grading; axis C's BLOCKER = killed (K11) |

### Minors

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **KF-HA-8** | D:D-5 / L:D-10 / C:D-9 — authorship arm KILLED (ruling 4) + reader-DU missed #5 | The one interaction dial runs at the library maximum: `setCursor(x, y, 1)` (:94) against the compiled `n = .8` default (~120° max domain-warp rotation at strength 1 vs ~96° at 0.8), unblessed by any comment or verdict — and `setCursorRadius` is exposed, never called, leaving the default **0.25** quarter-viewport radius unexamined beside it. The swirl axis itself is the DOCUMENTED consequence of authoring `light` (coupled default) — the "unauthored/collateral" framing is dead | **KF.W6** (one retune decision under the amendment: strength + radius together), loudness → **KF.W9** (SS-13 P-1) |
| **KF-HA-9** | L:D-3 / D:D-6 (MINOR ruled, ruling 5) + L-addendum C-1 (sustained) + reader-LC missed degenerate-states | Under PRM every forwarded mousemove executes one synchronous, provably-identical full-viewport draw inside the input task (wake→tick inline; PRM gates the reschedule, not the suspend set); the handler also runs its two forced-layout reads (`innerWidth`/`innerHeight`, :90-91) uselessly in the cssOnly cohort (runtime never built, `setCursor` = `inst?.` no-op) and the pre-arm window; the :80-82 "no style/layout flush … collapses to one write per frame" prose overstates on both counts | **KF.W6** — PRM early-out + `useWindowSize()` (already-imported dep) covers all states; the `wake()` PRM seam itself is producer-side → rides the **glass relay** with KF-HA-1/-2. Rate/cost → **KF.W9** (SS-13 P-5) |
| **KF-HA-10** | L:D-12 | `motion: "drifting"` resolves to a nonzero drift quadruple (probed: 0.015/0.015/0.008/0.05), so `needsAnimation()` never parks outside PRM — a whole-session full-viewport GPU loop under the least-visible layer on the page; the amendment cut presence, not cost. P-HERO provenance makes this a ruling question, not a fix order (the L axis's own calibration, adopted) | **KF.W6** (owner ruling: is the drift register blessed?), frame cost → **KF.W9** |
| **KF-HA-11** | L:D-9 + L:D-11 + reader-LC missed pointer-asymmetry (verified: the leave handler takes NO event and cannot gate) | Pointer policy is internally inconsistent three ways: (a) `pointerType !== "mouse"` (:87) drops pen HOVER though the comment justifies excluding touch only; (b) `onPointerLeave` (:97-99) is ungated, so touch/pen exits clear-and-wake an engagement only a mouse can create — under PRM that is a synchronous draw triggered by an input class the file excludes; (c) no `visibilitychange`/`blur` release — alt-tab leaves the field engaged at a stale attractor (`clearCursor` has no other caller; resume re-ticks with field intact) | **KF.W6** — one pointer-policy pass: `=== "touch"` exclusion form, gate the leave handler, one `visibilitychange` release line |
| **KF-HA-12** | D:D-7 (STRENGTHENED past its own falsifier — both readers concordant) + reader-DU MISSED #2 | Neither `forced-colors: active` nor `prefers-reduced-transparency: reduce` reaches the aurora — and BOTH are established house idioms this seat re-counted: the three global a11y sheets each carry forced-colors with 0 aurora selectors; a11y-fallback/paper.css carry reduced-transparency (0 in demo/). The system swept and the aurora was omitted from the sweep — a producer conformance gap, not a consumer invention | Primary cure → **glass-ui BH relay** (the aurora joins the a11y sweep); two-line consumer stopgaps (`display: none` under both queries in the component) → **KF.W6**; visual outcome at the true ~0.19 presence → **KF.W9** (SS-13 P-6) |
| **KF-HA-13** | C:D-8 (both readers re-executed, concordant) | `resolveAtoms` runs unguarded at `<script setup>` top level and glass-ui's bridge converts value.js DIAGNOSTICS to throws: `var(--accent-kf)` / non-opaque hex / `color-mix()` seeds all THREW in execution — one token-ising edit (the obvious next one; :54-56 names `--accent-kf` as the tracked hue) from a white-screened home route with no error handler anywhere (KF-HA-15). Latent today behind the compile-time hex | **KF.W6** — the `OklchStop` seed form (`seed: { L, C, h }`) removes the parse seam AND is the same edit as KF-HA-3's theme lever; else a guard. The bridge's throw posture is producer-domain → note on the **glass relay** |
| **KF-HA-14** | L:D-17 — **FOLDS KF-AT-26(c)** / U.B5 (banked) + reader-DU MISSED #3 (verified) | Single-consumer leaf in the shared shell tier, absent from its own barrel (4 of 8 exported), sole importer App.vue:142; U.B5 already ruled the trio move to `app/` and it is unexecuted; the tier is inverted at both ends (EditorHeader exported with zero importers). NEW: App.vue:139-141's mount comment cites "`editor-shell/`" — a directory that does not exist (my find → 0) — active misdirection for the very move-wave that will execute U.B5 | **Fold-by-reference → KF-AT-26(c) → KF.W8** (colocation settle); the stale-comment correction attaches to the same move edit, named so it is not lost |

### Info

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **KF-HA-15** | L:D-13 / C:D-7 / D:D-10 — INFO concordant across all five parties | Neither `onInitError` nor `@rendererStatus` bound; no app `errorHandler` (main.ts read whole by readers); a GPU-init failure is silent everywhere. Correctly INFO: the documented degrade (palette ground stays visible) is good UX; worth one console breadcrumb given the eager arm raises boot-time failure probability | **KF.W6** (one breadcrumb, optional) |
| **KF-HA-16** | L:D-14 / C:D-10 / D:D-9 + S-6 KILLED (ruling 7) + reader-LC missed #4 | The scoped sizing block AND its `@supports` fallback are wholly dead (`fixed inset-0` is viewport-sized pre-`dvh`), and the wrapper div itself is removable (single-root Aurora; attribute fallthrough carries class + aria-hidden): −1 DOM node, −15 lines, −the S-6 argument | **KF.W6** (the composite KISS reduction, adopted whole) |
| **KF-HA-17** | L:D-18 / C:D-11 + D:D-8 (2 of its 4 rows killed — K5, K6) | Prose truth: ":84 Aurora 5.x" is two majors stale against installed 7.0.0 and breaks the file's ~78-col wrap mid-sentence (the edit-scar and KF-HA-1 are the same event — the 5.x→7 window is when the medium-discriminated arms landed); the surviving false lines are :8 (lazy arm), :6-7 (PRM-safe CSS substrate — `resolveRenderMode` has NO PRM branch, my read; PRM is honoured by the frame-loop freeze, a different mechanism at a different layer), :57-59 (axis ON), :59-61 (verbatim/ONLY — 7 keys move) | **KF.W6** prose reconciliation (intent stays; library-internals narration dies — the D axis's own law) |
| **KF-HA-18** | L:D-8 / C:D-12 / D:dismissed-1 — **FOLDS KF-APP-48** (banked, carrying the ≤7-of-12 correction this seat reconfirmed) | The ground spread re-pins `saturation` (0.913→0.92, Δ0.007 inert) over the `colorEnergy`-derived limb; the durable trap is a future colorEnergy retune moving 3 of 4 limbs. Exactly 7 keys change; 5 already equal | **Fold-by-reference → KF-APP-48 → KF.W6**; no second booking |
| **KF-HA-19** | C:§4.1 + both readers' independent re-runs (ruling 9) | The "R1 = live `parseCssColor('oklch()')` shipping crash" framing does NOT reproduce against this repo's pinned value.js 4.0.0 css subpath (ok on all oklch forms; diagnostics-not-throws on var()/color-mix) — three independent executions | **NO-WAVE-OWNER** — a value.js-side registry carry (megatranche parser-band / STATE reconciliation), relayed up, never a KF wave row |
| **KF-HA-20** | L:D-15 / L:D-16 | Three explicitly-passed library defaults (`harmony`, `zones.arrangement`, `render-mode="auto"`) read as overrides but restate defaults; `{ passive: true }` on `pointerleave` (:109) is inert (non-cancelable event) | **KF.W6** (touch-in-passing with KF-HA-17) |

## Killed-claims register — 14 entries

| # | killed claim | adjudication |
|---|---|---|
| K1 | **C:D-1 (BLOCKER)** — "glass-ui is a phantom dependency … at HEAD `8281638c` the key is gone entirely" | **SUSTAINED (both readers, concordant; this seat's own `git show` receipts).** The axis read the dirty worktree and attributed it to HEAD; HEAD declares AND locks 6.0.0; CI builds committed refs. The false BLOCKER was shipped by the axis; the L-addendum had caught the identical trap against itself and withdrew in public (its C-2). Survivor = KF-HA-6, folded |
| K2 | **D:D-4 as graded** — "the 0.1 ceiling is load-bearing for WCAG AA … margin 2.2% … break-even 0.1167" | **SUSTAINED AND EXTENDED (ruling 1; this seat's own two-leg verification).** Wrong threshold (sole muted-fg surface = `.hero-hint` at ≈33px → large-scale 3:1) and wrong number (composite ≈0.19, at which the axis's own arithmetic INVERTS its reassurance). Demoted to INFO; the reproducible arithmetic survives as receipts |
| K3 | **D:S-3** — "the owner's aesthetic instinct landed on the correct side of an accessibility boundary" | **SUSTAINED.** The boundary does not exist for the text actually rendered; the presence is not 0.1 anyway. 0.1 stands on taste and the amendment — exactly as the tree originally recorded |
| K4 | **reader-LC's D-4 CONFIRMED cell + its S-5 superlative** ("the single most rigorous artefact in the corpus") | **OVERRULED BY THIS SEAT (ruling 1).** Reproducing figures to three decimals while inheriting both premises unexamined is the precise failure the cure-separation law names on the evidence side. Amended to: exemplary receipts, refuted conclusion |
| K5 | **L:§1 claim-2 graded TRUE** — "the PRM-safe CSS-gradient substrate (renderMode auto)" | **SUSTAINED (both readers; this seat read `function M` whole).** No reduced-motion branch exists; `"auto"` → `"webgl"` on all non-software hardware. PRM is honoured by the frame-loop freeze — a different mechanism; grading the sentence TRUE certifies a misattribution. D's D-6/D-8 reading wins |
| K6 | **D:D-8 row 3** — "the component hand-rolls it at :86-110" (against the docblock it quotes) | **SUSTAINED (reader-LC; this seat read the docblock verbatim).** The docblock's own parenthetical sanctions the exposed-`setCursor` door the component took. Consumption of a published contract, not a hand-roll; contradicts the same file's S-1 four pages later |
| K7 | **D:D-8 row 4** — "`...PAPER_WASH_GROUND` clobbers 12 fields" | **SUSTAINED (reader-LC; this seat's probe: exactly 7 change, 5 already equal).** Convergent with the correction already banked at KF-APP-48 — three independent measurements now agree |
| K8 | **C:D-4 leg 3 / D:D-1(b) as universal** — "on the browser the demo actually ships to, the uniform is not in the struct" | **SUSTAINED AS RESCOPE (reader-LC; adopted over reader-DU's standalone-MAJOR grading).** Engine-conditional: the WebGL2 net HAS `uLightDir` (Safari/Firefox/WebGPU-less/adapter-failure hosts). Kills (a)+(c) are backend-independent and each sufficient — recorded so a Safari test of leg (b) does not un-teach the finding |
| K9 | **D:D-5's "Nobody chose this … collateral of the light drop" arm** | **SUSTAINED (reader-DU; this seat read the installed swirl doc: "Default coupled to `light` when unset").** The swirl is the documented consequence of the authored intent; the `?? true` default fires regardless of the drop. reader-LC's endorsement = citation-inheritance, corrected (ruling 4). Survivors: the strength literal `1`, the unexamined radius 0.25 |
| K10 | **L:S-6** — "the dvh fallback is carried locally *because* it must be … correct discharge of a house idiom" | **SUSTAINED (both readers, ruling 7).** A fallback for a redundant declaration is itself redundant; `fixed inset-0` predates `dvh` in every engine. The scope-isolation FACT stays (the shell's scoped rule provably cannot reach the slot child) as the anti-half-delete warning |
| K11 | **C:D-2 at BLOCKER** — eager arm graded BLOCKER | **SUSTAINED (reader-LC).** The palette ground is the frame-0 surface either way; nothing breaks; magnitude self-conceded UNPROVEN. MAJOR, concordant with banked KF-APP-16. The forfeited-intersection-gate sub-clause is inert for `fixed inset-0` (L-addendum C-3) |
| K12 | **L:D-3 at MAJOR / reader-DU's MAJOR carry** — the "120–1000 Hz" rate premise | **SEAT CORRECTION (ruling 5).** The premise is the file's own comment, not engine behaviour; all target engines coalesce dispatch to ~1/frame — the L axis's own falsifier firing. MINOR; mechanism fully sustained |
| K13 | **C:S-4's "including the 'both themes' half"** | **SUSTAINED AS SCOPE-KILL (reader-DU).** Hue-bracketing both tokens is real (reproduced); a theme preview requires all three coordinates and the ramp is banded L 0.42–0.74 against a near-black dark ground. Not in conflict with KF-HA-3 once split — the superlative's stated scope was wrong |
| K14 | **L:§1 claim-11 / S-2 as absolutes** — "no DOM geometry read at all … no style/layout flush" graded TRUE | **SUSTAINED (the L-addendum's own self-kill; both readers concordant).** `innerWidth`/`innerHeight` are on the forced-layout list and run per event (:90-91). The substance survives amended: no ELEMENT-geometry read, no read-after-write interleave — thrash structurally impossible (setCursor touches no DOM). Cure = `useWindowSize()` |

## Superlatives — 12 sustained (L-18 runs both ways; provenance per row)

1. **The primitive was consumed, not re-invented** — the file's reason to exist, and it holds against the shadow census as the counter-example (52 code lines for a full-viewport interactive backdrop; the temptation refused is named in-file). All three axes + both readers; sustained also at the banked kf-App :140 row. (D:S-1 · L:S-1 · C:S-1)
2. **The three-ink layering claim is true in all three parts** — reader-DU extended the corpus's grid-only verification to the hero glyphs and the die, and this seat re-checked the mechanism: both backdrop siblings fixed at z-auto in tree order; the glyphs ride `z-controls` (=20, EditorStartScreen.vue:18 + the EditorShell.vue:60 slot wrap), the die rides `z-content` (=10) — the demo's z-contract (style.css:29-30) makes it structural. A three-part claim true by the simplest available mechanism. (reader-DU COMPONENT-1; seat-verified)
3. **No element-geometry read and no read-after-write interleave in the hot path** (the K14-amended form) — the T-CL-3 recurrence really is cured; `setCursor` mutates JS state only; the declined alternative does per-event `getBoundingClientRect`. The absolutism died; the engineering stands. (L:S-2 as amended · D:S-1 · both readers)
4. **Declining `useCursorInteraction` was correct twice over** — per-event rect read AND nuclei-CRUD authoring semantics on a decorative layer; the producer docblock explicitly sanctions the door taken (this seat read it verbatim — the same sentence that kills D:D-8 row 3). A justified bespoke in the census S-8 sense. (D:S-1 · L:S-7 · C:S-2 · both readers)
5. **Decorative-layer hygiene is complete and leak-free** — `aria-hidden` + `pointer-events-none` on the wrapper, library-level aria-hidden on both children, no focusable descendant, both listeners scope-torn via `useEventListener`, the leave target passed as a getter, the retired velocity cache deleted with its API rather than left as a dead closure. (D:S-4 · L:S-3 · C:S-5 · R1-13-a11y corroboration; banked at kf-App)
6. **Subpath discipline** — `@mkbabb/glass-ui/aurora`, a real exports-map entry, never the root barrel; house-consistent. (C:S-3 · reader-LC S-7)
7. **The D axis's arithmetic and citations are exact even where its conclusion died** — the WCAG table reproduces to three decimals from the real token chain, and its line/char-offset citations into a 3032-line bundle spot-check flawless (two coordinate systems, both exact). Wrong conclusion, exemplary receipts — recorded because that is what made the kill CHEAP: the falsifier that fires was published with the finding. (reader-DU CORPUS-1/-2; seat spot-checks)
8. **The `git show HEAD:` discipline and the public self-kill** — the L-addendum drafted a BLOCKER escalation, published its falsifier, watched it fire, withdrew in full, and recorded the trap ("both passes read dependency state from a dirty working tree and reasoned about CI, which builds committed refs"). The C axis lacked exactly this and shipped the corpus's one false BLOCKER. Standing practice for any finding whose blast radius runs through CI or a clean checkout — already law at the AnimatedText seat; re-earned here. (L:C-2 · both readers · ruling 6)
9. **The false-defect discipline was practised corpus-wide** — six dismissed in D, four killed in C, four withdrawn in L, each with the decider recorded; the load-bearing ones re-tested by the readers and two re-tested by this seat (the 7-of-12 arithmetic; the Tailwind-utilities kill's premise). An unrecorded clean probe is re-run forever; this corpus does not make that mistake. (all parties)
10. **The PRM synchronous-draw archaeology** — `setCursor` → `wake()` chased across three dist chunks to the inline-`tick()` under PRM, with the identical-frame proof at a line no axis had cited (`setPointer`'s PRM early-out). The finding no other axis reached; the D axis stopped halfway. Severity died (K12); the derivation is the corpus's best. (L:D-3 · both readers)
11. **The two readers' misses were COMPLEMENTARY, and one was convergent** — reader-DU found the compositing/a11y/prose stratum (double-stack, reduced-transparency, the stale directory cite, the radius knob), reader-LC found the manifest/pointer/degenerate-state stratum (peer-deps, leave-asymmetry, cssOnly no-op, the removable wrapper) — and BOTH independently found the unexportable-const cure defect. Independent convergence on a novel row is the anti-signature of citation-inheritance (the ColorSpaceSelector DISSENT-1 law) — it is why KF-HA-4 carries its cure-correction with confidence. (ruling record)
12. **Axis C proved a negative properly and against interest** — its §4.1 killed its own repo's standing R1 framing with executions, flagged the contradiction explicitly, and both readers re-ran it rather than inheriting it. Three-way execution agreement on a memory-contradicting fact is exactly what a registry carry should be made of. (C:§4.1 · both readers · KF-HA-19)

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (→ KF.W9)

All mechanisms below are source-certain; only magnitudes/UA behaviour need a rendered viewport. One pass, one pinned commit, re-anchored lines.

- **P-1 · The central visual question** (KF-HA-1/-8): does anything follow the cursor, and does it read as a light or a vortex? Source predicts NO light and a swirl at strength 1 (~120° max rotation), radius 0.25. This single probe decides the KF-HA-1 cure fork (restore-medium vs stroke-medium vs delete-the-axis — "done right OR removed"). Home route, mouse, both themes.
- **P-2 · The doubled presence** (KF-HA-2): does placeholder+canvas read as one field at ~0.19, and is the post-arm 0.10→0.19 brightening visible on the LCP route? Compare `opacity-ceiling="0.05"` (which lands composite ≈0.0975 ≈ the blessed 0.1) to settle which number the owner actually blessed in practice.
- **P-3 · Dark-leg magnitude** (KF-HA-3): toggle dark on home; the wash provably does not move; the ramp is L 0.42–0.74 over a near-black ground. Violet tint or pale film — the judgement `lightnessScheme` exists to make.
- **P-4 · Eager boot cost** (KF-HA-7 fold): trace `/` with and without `:runtime-options`, on a WebGPU engine AND a WebGL2-only engine; >50 ms attributed = escalation (the banked bar); measure the per-navigation re-arm (`v-if="isHome"`).
- **P-5 · PRM draw rate/cost** (KF-HA-9): reduced-motion on, move the mouse, count draws; expected display-rate, identical frames.
- **P-6 · Forced-colors + reduced-transparency outcome** (KF-HA-12): both proven structurally absent; what needs eyes is only how bad the untreated wash looks at the true ~0.19 presence — settles MINOR vs `display:none`-outright.
- **P-7 · (not visual — version probe)** KF-HA-1 against glass-ui **6.0.0** (what `npm ci` at HEAD installs): if 6.0.0's `light` carry was unconditional, the defect is a silent cross-major regression, not an authoring error — changes the cure AND makes KF-HA-5's gate gap the enabling cause twice. Needs one install (forbidden here; note the legacy-peer-deps constraint from KF-HA-6). Must resolve before the KF.W6 cure set executes.
- **P-8 · Heap/leak witness** (superlative 5's receipt): home→scene→home cycles; no retained listeners/GL contexts; the same trace observes P-4's re-arm cost.

## Identity guard — folds by reference (anti-rename; ONE identity each)

- **Census F-1/SCH-1** (glass-ui declaration churn) — KF-HA-6 folds there via kf-AnimatedText ruling 1 → **KF.W0**, carrying the NEW peer-dependency/.npmrc decider + the pin-and-bump-one-commit constraint. Never re-booked.
- **KF-AT-12** (no SFC typecheck anywhere) — KF-HA-5 folds there → **KF.W4**, attaching this component's TS2345 + the `13530f70` precedent as consequence-witnesses.
- **KF-AT-26(c)** / U.B5 (home-hero trio move to `app/`) — KF-HA-14 folds there → **KF.W8**, attaching the App.vue:139 stale "`editor-shell/`" cite so the move-wave corrects it in the same edit.
- **KF-APP-15** (static-import boot weight) — not re-booked here; named as the co-rider of the KF.W6 config motion.
- **KF-APP-16** (eager arm) — KF-HA-7 folds there → **KF.W6**/W9, attaching the mechanism sharpening + the killed BLOCKER grade.
- **KF-APP-48** (ground-spread override, ≤7-of-12) — KF-HA-18 folds there → **KF.W6**, with this seat's third independent 7-key confirmation.
- **Glass-ui BH relay** (owner edict, standing) — carries KF-HA-1's crayon/light incompatibility, KF-HA-2's placeholder-not-hidden-when-armed, KF-HA-9's `wake()` PRM seam, KF-HA-12's a11y-sweep omission, and KF-HA-13's diagnostic→throw bridge posture, as ONE relay packet.
- **Value.js megatranche registry** — KF-HA-19's R1-framing contradiction, relayed as a NO-WAVE-OWNER carry.

## Closing verdict

**ADJUDICATED — the component is a well-built shell around a mis-called library door, under prose that narrates a library two majors gone, over a compositing stack nobody had measured.** 0 BLOCKER survives adjudication (both original BLOCKERs die — one to provenance, one to severity calibration); 7 MAJOR (3 folded to banked identities: KF.W0 substrate, KF-AT-12 gate law, KF-APP-16), 7 MINOR, 6 INFO; 14 register kills including one direct overruling of a reader's confirmed-and-crowned cell (D-4 — the WCAG vindication of the 0.1 ceiling is dead on both its threshold and its compositing premise, and the true delivered presence is ~0.19, itself a new MAJOR); 10 reader misses adjudicated real; 12 superlatives sustained — the lifecycle, teardown, a11y-hygiene, and layering discipline are the census's counter-example and survive every attack. The cure mass routes: KF.W6 (the one config motion + prose truth + the KISS reduction), KF.W4 (the oracle rebuilt in an assertable form), KF.W0/W8 (folded), KF.W9 (eight SS-13 inputs), one glass-ui BH relay packet, one NO-WAVE-OWNER cross-repo carry. Nothing here stamps a wave; X·KF specs consume this record whole (M-25).

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/shell/HeroAurora.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **HeroAurora.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

