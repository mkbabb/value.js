claude-fable-5

# pt-state — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ master `ef10d5b` (re-verified this session; `typescript/` has zero dirty rows — all dirt is `rust/**`/docs), `typescript/src/parse/state.ts` = 189 lines · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: the module's two challenge files (`audit/parse-that-modules/state/challenge-L-library.md`, 539 lines, opus-5[1m]; `challenge-C-consumption.md`, 598 lines, opus-5[1m]) — both re-read **whole** by this seat — plus two fresh reader returns: reader-α (`state`: 19 confirmation cells · 3 kills · 8 misses · 7 superlatives) and reader-β (`state-C-consumption-L18-hostile-reread`: 17 confirmation cells · 6 kills · 5 misses · 4 superlatives).
**Method (incredulity)**: every cross-reader disagreement and every kill of a load-bearing cell was re-derived against the bytes by this seat before adoption. **Two reader confirmations were OVERRULED** (ruling 1, ruling 2 below) — high agreement with a challenge's own probe table is cheap; re-deriving the *interpretation* of the probe values is where this corpus's largest error lived. Zero parse-that code was executed: the one runtime instrument was a standalone pure-JS replication of the three position getters (no parse-that import) plus `createRequire().resolve()` probes and `node` export-set enumerations of the shipped dist. Nothing was armed (no `enableDiagnostics`, no `memoize`). Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments (all read-only)**: full read of `state.ts` (whole, 189); `core.ts`/`index.ts`/`diagnostics.ts`/`utils-entry.ts` (whole); `utils.ts` (whole, 186); `parser.ts:1-210, :330-357, :395-530, :683-711`; `leaf.ts:276-399`; `debug.ts` anchor census (`:12`, `:245`, `:248/:257`, `:268-278`, `:320/:339/:346`, `:358/:379`); `packrat.ts:79-100, :245-260, :278-300, :345-360, :475-488`; `tsconfig.json` (whole — no `useDefineForClassFields`, no `noUnusedLocals`); `test/subpath-gate.mjs:20-56`; dist: `core.js:1` (16-binding single import), chunk `:298-302/:341-349/:675-680/:720-724/:874-882`, `core.d.ts` (whole), `state.d.ts:24/:33-47/:64-74`; **byte-identity** `diff -q` of `dist/core.js` + `dist/packrat-entry-CS1td-8B.js` (40,576 B) against the prototype-workspace install → IDENTICAL; export probes: `/core` = **18 keys**, `.` = **34 keys**, `mergeErrorState`∉core, `addSuggestion`/`resetErrorState`/`isDiagnosticsEnabled`∉root; caller censuses re-run: `.err(` **0** · `.save()`/`.restore(`/`.from(` **0** · `unsafeCall`/`getColumnNumber`/`getLineNumber` declarations-only · `.clone()` = packrat.ts:351 only · direct `.value =` bypasses = **6** · `as any` = **0** across src/parse · `as unknown as` = 3 in state.ts; prototype workspace: `package.json` deps (`@mkbabb/parse-that: 1.0.0`), `node_modules` present, all three specifiers resolve **from the script's own path**, 19 importing files enumerated; W1.md:430-441/:570-580 (the 2026-08-03 recorded surface-gaps run); `cand-o/index.ts:9-18`, `cand-b-dispatch/grammar.ts:545-551`, `idiom/example.ts:192-198` (the three consumer codifications); `test/reentrancy.test.ts` test-name census; `README.md` `.parse(` census (`:19,:28,:36,:37,:54,:230` — `.parse()` is the documented API).

## Scoreboard

**31 adjudicated roster rows: 1 BLOCKER · 10 MAJOR · 16 MINOR · 4 INFO** (merged identities across both challenges + 12 adopted reader-missed rows) · **7 killed claims** (2 of them killing reader *confirmations*) · **7 superlatives** · **6 MEASURE-AT-OPEN residue rows.**

Blocker-weight survivor: **PT-STATE-1** — `.parse()`'s failure value is arbitrary and collides bit-for-bit with legitimate success values on a published, immutable 1.0.0 whose README documents `.parse()` as THE entry point; `err()`, the one construct that would prevent it, has zero callers.

## Ruled disagreements (the incredulity seat's actual work)

| # | subject | reader-α | reader-β | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| **1** | **C-5 / L-M-3 — the position getters** | CONFIRMED MAJOR ("returns 0, a line number under no convention"; "all four C-5 rows reproduce") | KILLED to MINOR (0-based convention real; only newline-pointing offsets miss) | **β WINS — α's confirmation OVERRULED.** Re-ruled **MINOR** | Standalone replication vs independently-computed ground truth, five cases: `getLineNumber` matches **0-based** truth on every non-newline offset (`"a\nb"`@2→1✓, `"a\nb\nc"`@4→2✓, `"abc"`@2→0✓, `"a\n\nb"`@3→2✓); `getColumnNumber` matches 0-based column likewise. The SOLE miss for both is `src[offset]==="\n"` (`"a\nb"`@1: line 1 vs truth 0; col 0 vs truth 1) — the **inclusive `lastIndexOf("\n", offset)`** at state.ts:113/:120 vs `getLineAndColumn`'s exclusive `offset-1` at :128. C-5 compared a 0-based method against a 1-based one and called the constant difference an error; 0-based lines are the LSP/tree-sitter convention. M-3's "every offset on every line after the first" dies at the first probe. What survives: two undocumented conventions + one shared newline-boundary bug + zero callers on two of three. |
| **2** | **C-§1 — "value.js consumes zero of this module; no installed consumer to break"** | CONFIRMED (INFO) | KILLED (four live in-repo consumers) | **β WINS — α's confirmation OVERRULED as generalized.** The *shipped-library* zero-edge survives as ruled (W2.md:193-194) | The prototype workspace `docs/tranches/V/megatranche/prototypes/css-parser/` declares `"@mkbabb/parse-that": "1.0.0"`, has it installed, and **19 files** import it — including `fixtures/g16-replay.test.ts:22` (`ParserState` **via `/core`**, 3-arg direct construction at :34) and `cand-s-spec/terminals.test.ts:16`. α's three §1 measurements were all root-scoped and correct at the root — wrong denominator. C-§6's "retirement cost is zero" is false as stated: retiring the ParserState ctor or the /core export breaks the band's own replay fixtures (small, prototype-confined, not zero). |
| **3** | **B-1 severity** | BLOCKER sustained | MAJOR (three consumer codifications + types.ts:27 bound) | **BLOCKER SUSTAINED**, β's bound folded in as the honest qualifier | The library axis is the axis of record for a *published, immutable* 1.0.0: README.md documents `.parse()` at :19/:28/:36/:37/:54/:230 and no line specifies a failure value. The three prototype comments (verified verbatim: `cand-o/index.ts:11-16`, `cand-b-dispatch/grammar.ts:547-551`, `idiom/example.ts:192-198`) bound the **program's** exposure, not the package's; the prototypes are marked "NOT production". W1's recorded run (:577) keeps it a live RED GUARD row. Ruling: BLOCKER at the surface, program-cost BOUNDED — and the bound is recorded inside the row, not as a severity discount. |
| **4** | **C-1's narrowed claim + attribution** | Confirmed C-1 at MAJOR (downgraded from BLOCKER); killed L-M-1's cure | Killed C-1-as-stated AND L-M-1; kept the substance at MAJOR | **BOTH KILLS SUSTAINED; substance survives at MAJOR (PT-STATE-3)** | Re-derived: `core.ts:7` exports `Parser`; `parser.ts:3` **value**-imports `parserDebug`/`parserPrint`, consumed at `parser.ts:695`/`:699` (prototype methods of the exported class — unshakeable by the challenges' own retention argument); `parser.ts:7` imports `packratEnter`/`packratExit`, called **unconditionally** at `:43`/`:47`. Deleting `state.ts:2` removes zero bytes from `./core`. And for non-bundling consumers `dist/core.js:1`'s single 16-binding import makes evaluation total regardless of symbol — C-1's claim is asserted exactly where it is false and disclaimed where it might be true. |
| **5** | **C-7 / L-m-4 severity (save/restore)** | MINOR (zero consumers, no shipped bug) | MAJOR (R-LAW-1's mark/rollback slot) | **MINOR** — α wins; β's algebra-slot framing is a *disposition* argument (RETIRE), not a severity fact | Verified: `save()` captures 2 of 8 fields (state.ts:75-77); `restore()` forces `isError = false` at :82; inline `value: any` emitted to `dist/state.d.ts:44-47`; `.save()`/`.restore(` → **0** callers anywhere. A dead published pair cannot demonstrate a shipped bug; it earns RETIRE at MINOR. |
| **6** | **C-4 severity (parserNames unexported)** | MAJOR | MINOR (type recoverable from the .d.ts) | **MINOR** — β wins | `dist/state.d.ts:64` ships the full 29-literal tuple type and :70-74 inlines all 29 into `createParserContext`'s emitted signature — `Parameters<typeof createParserContext>[0]` recovers the union; only *runtime* enumeration is blocked, and the symbol RETIREs for the algebra regardless (§6 of C, adopted). |
| **7** | **C-9 severity (/core lacks Suggestion/SecondarySpan types)** | MAJOR | MINOR (type imports fully erased) | **MINOR** — β wins | `import type { Suggestion } from "@mkbabb/parse-that/diagnostics"` adds zero bytes and zero module evaluation under any TS config. Naming/discoverability hygiene, not a tiering contradiction. The three-hop identity (state → utils re-export → debug re-import) is real and folds here. |

**One event recorded against the fleet, not the tree**: reader-α *confirmed* C-5/M-3 while its own transcript contains the exculpating values ("getLineNumber returns 1, while getLineAndColumn gives line 2 (correct)") — the probe numbers were right and the inherited interpretation wrong. This is the ColorSpaceSelector DISSENT-1 citation-inheritance failure in a new costume: agreeing with a challenger's *reading* of a correct measurement is as cheap as agreeing with a stale cell.

## Adjudicated defect roster

Anchors are `parse-that` @ `ef10d5b`; re-resolve before any cure (D-19). Dispositions: **W1**/**W2** = the X·P wave that owns the surface · **REFINEMENT FOLD** = sharpens an X·P spec · **NO-WAVE-OWNER** = parse-that-side cure with no X·P owner (rides the standing S.W1 1.1.0+ ask-letter channel — HEAD `ef10d5b` *is* that letter — or waits for a 2.0.0 where breaking).

### Blocker

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-STATE-1** | L/B-1 · C-2.3 · O-15 PT-07 (contradicted-and-worse) | `.parse()`'s failure value is whatever the last combinator left — `many(1)`-FAIL and `many(0)`-SUCCESS both yield `[]` (truthy; `if (!result)` passes); a failed `then` returns the first sub-parser's residue. `err()` (state.ts:62-67), the one paired transition, has **0 callers**; the live protocol is uncoupled raw writes (`parser.ts:553-555`/`:626-628` value=`[]`; `:93-96` value untouched); `parser.ts:74` returns the live state, `:78` returns `.value`. The corpus's own recorded row (W1.md:577 "returns undefined") **under-states** the tree: the residue is grammar-dependent. BLOCKER per ruling 3; β's bound recorded: three prototype seats independently codified the cure in writing and X·P's `ParseResult` non-empty tuple (types.ts:27) forbids the shape at the type level — program-cost bounded, package hazard undiminished. | `state.ts:47-53` (two unpaired public mutables — the enabling absence); `parser.ts:51-79`; `.err(` grep → 0 (re-run this session); README `.parse()` census | **REFINEMENT FOLD** — W2's failure-signal law names B-1 explicitly (value meaningful ⟺ `!isError`; the non-empty-tuple failure arm is the shape that makes it unrepresentable). **W1** keeps the RED GUARD row as a live probe (it runs — ruling K1). Incumbent cure (make `err()` the sole error transition; document the failure contract) is NO-WAVE-OWNER, 1.1.0-eligible, rides the ask letter. |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-STATE-2** | C-2 (.1/.2) | `ok`/`err`/`from` mutate the receiver and return it re-tagged (`return this as unknown as ParserState<S>` ×3 — the file's only 3 casts); the emitted `.d.ts` is indistinguishable from pure constructors; the three genuinely dangerous methods are the only undocumented ones (docblock census verified: `unsafe*` + `getLineAndColumn` documented, ok/err/from/save/restore/clone not); `ParserFunction` is published from `.` and `/core` so consumers must return through them. B-1's mechanism — held at MAJOR, not double-counted at BLOCKER (α's discipline adopted). | `state.ts:55-73`; `dist/state.d.ts:37-39`; `parser.ts:13-16`; `core.ts:7` | **W2** — state-identity law: result constructors are non-aliasing OR the in-place protocol is documented and mark/rollback is a separate first-class op (EQ-5/R-LAW-1). Incumbent: RETIRE from the algebra. |
| **PT-STATE-3** | C-1 = L/M-1, **re-attributed** (α-miss #2 adopted) | The `/core` docblock ("never pulls the diagnostics accumulator, the packrat tier", core.ts:3-5) is **false at the bytes** (`dist/core.js:1` → the 40,576 B packrat-entry chunk → diagnostics chunk; PACKRAT_ARMED at :678/:722; statePrint ×3; ANSI probe :28; json/csv third honest) and **ungated** (`subpath-gate.mjs:24-36` existence-only + `:44-56` three function checks — re-read this session, zero exclusion assertion). **RE-RULED ATTRIBUTION**: the retention edges are `core.ts:7` + `parser.ts:3` (parserDebug/parserPrint at :695/:699) + `parser.ts:7` (packratEnter unconditional at :43) — `state.ts:2` is a *fourth* edge, load-bearing only for bundling consumers importing ParserState alone. **No state.ts-confined cure exists.** Honest cures: amend the prose, or add the exclusion assertion born-RED and split Parser's debug/packrat edges behind a seam. | ruling 4; export probe `/core` = 18 keys | **W1** — the harness's subpath reading gains the missing exclusion assertion (born-RED until Parser splits or the prose amends). Library cure NO-WAVE-OWNER (prose amendment is 1.0.x-eligible; the split is 2.0.0). Violates W2 O-8 (absorbing state via the tier sold as clean) — cross-filed to the O-8 fold. |
| **PT-STATE-4** | L/M-2 = C-6 | `clone()` copies the diagnostic **anchor** (`furthest`) and drops the **payload** (`expected`/`suggestions`/`secondarySpans` re-initialized), violating state.ts:38-42's own documented pairing invariant; sole caller `packrat.ts:351` (LR grow re-eval) discards every label found in the grow pass — only `snapshot()` (offset/value/isError) survives; `parser.ts:61-65` routes AROUND clone() for the copy that matters; `resetErrorState` (utils.ts:131-136) proves the four travel as a unit. **Caveat neither challenge states** (α's find, verified): every writer of the dropped payload is gated on `diagnosticsEnabled` (utils.ts:33/:52/:58, default false) — on the shipping default the clone is coherent **by accident**; the defect fires only with diagnostics armed. | `state.ts:101-109`; dist chunk `:341-349`; `.clone()` grep → 1 | **W2** — copy law under EQ-4: any copy primitive copies `D` whole or resets its index; the incumbent RETIREs. |
| **PT-STATE-5** | L/M-4 = C-10 | Two arrays per ParserState, unconditional (state.ts:44-45 → chunk :301-302), for a write-gated-off feature; the restart branch re-allocates both **unguarded** on every furthest-advance while guarding `expected` one line above (utils.ts:32-35); the failure path allocates 4 and immediately aliases 2 away (`parser.ts:62-64` = dist `:876-878` — cite corrected from L's :878-880). Escape-analysis falsifier survives (state stored on the long-lived Parser, :66/:71). No cost figure asserted by anyone — bench-armed measurement is out of bounds. | `utils.ts:28-49` (whole, read this session) | **W1 MEASURE-AT-OPEN** #1/#2 (reject-path leg — parser-band's own debt row) + **W2** — `D` is a lazily-attached value in the state model, not two eager slots. |
| **PT-STATE-6** | L/M-5 | The hot object's one-shape guarantee exists only because `target: ES2022` defaults `useDefineForClassFields: true`; tsconfig (read whole, 18 lines) pins neither, `expected?: string[]` (:43, sole uninitialized field) survives to chunk :300 as a bare `expected;`. Flip the flag and `utils.ts:33`'s assignment ADDS the property on first failure-merge, splitting every state into two maps — `mergeErrorState` is on essentially every combinator's failure path. No gate reads tsconfig or asserts the own-property set. Three-character cure (`= undefined`) is flag-independent. Latent, not firing. | `tsconfig.json`; `state.ts:43`; chunk `:300` | **NO-WAVE-OWNER** (1.1.0-eligible three-char cure, rides the ask letter) + **W1 rider**: the harness asserts the 8-own-key set of a constructed ParserState at open (MEASURE-AT-OPEN #4 — cheap, not a bench). |
| **PT-STATE-7** | C-8 **+ α-miss #3 (the falsifier killed)** | `ParserContext.name` is a mutable optional the library rewrites post-hoc (`leaf.ts:397-398` sets `"whitespace"` bypassing the factory) and dispatches strategy on (`parser.ts:488`; fallthrough `this.wrap(parser, parser)` at **:520**, both challenges' :495 cite corrected). **UPGRADED**: C-8's falsifier ("regex(/\s*/) always succeeds zero-width … no acceptance difference") is **FALSE** — `regex()` fails at EOF (`leaf.ts:327-330`, before the pattern runs), so `.trim(regex(/\s*/))` takes the wrap path whose trailing end-call at `parser.ts:416` runs at post-inner offset and **fails the whole wrap when the inner consumed to EOF**, while `.trim()`'s charCode scanner (leaf.ts:372-391) cannot fail. `string("abc").trim(regex(/\s*/)).parse("abc")` rejects where `string("abc").trim().parse("abc")` accepts — a real **acceptance divergence** keyed on a mutable string tag, and per PT-STATE-1 the rejection isn't even visible in `.parse()`'s return. Derived analytically this session. | `leaf.ts:326-330/:367/:395-398`; `parser.ts:480-521`; `:400-426` | **W2** — closed operator union, no fallback kind (AC-2); tag-dispatch banned: identity, not a mutable string, selects behavior (matches pt-leaf PT-STATE-family precedent PT-LEAF-7). The divergence row joins W1's acceptance corpus. |
| **PT-STATE-8** | C-3 = L/m-2 | `Span`/`spanToString`/`mergeSpans`: zero call sites (grep re-run: declarations + barrels only), shipping on both tiers = 2 of `/core`'s 18 runtime keys; two dead type-imports (`parser.ts:2`, `leaf.ts:3`) invisible to a tsconfig with neither `noUnusedLocals` nor `noUnusedParameters`; the S.H2 gate enumerates the 15 excised *builder* names literally (`proof-no-span-surface.mjs:30-34`, C's :33-37 corrected) so it is GREEN over the residue **by construction**; index.ts:10-12's own excision rationale condemns the survivors verbatim. | `state.ts:8-19`; `core.ts:11-12`+:14 | **REFINEMENT FOLD** — gate-authoring law for every X·P gate: assert *reachability/exclusion*, never a name list (this is the second name-list-blind gate in the family). Excision itself NO-WAVE-OWNER (breaking → 2.0.0 ledger). |
| **PT-STATE-9** | **β-miss #1 (adopted)** | `/core` publishes the sanctioned extension point (`ParserFunction`) but **no label writer**: `mergeErrorState` — the sole entry to the expected-set — is on `.` and `/diagnostics` but **not** `core.ts` (verified at source and by export probe: ∉ the 18 keys). A `/core`-only consumer authoring a leaf can set `isError = true` and nothing else — every failure they can express is opaque, which is precisely parser-band debt #1's "labelled zero-width failure with named expectations", and the workaround re-crosses the boundary core.ts:3-5 sells. | `core.ts` (whole); export probe | **REFINEMENT FOLD** — W2: the tier the algebra builds on MUST co-publish the diagnostic writer with the extension point. Library side NO-WAVE-OWNER: additive export, 1.1.0-eligible, named in the ask letter's diagnostics-HIGH lane. |
| **PT-STATE-10** | **β-miss #2 (adopted)** | `suggestions`/`secondarySpans` are published, mutable, public array fields with **zero published writers on any tier**: `addSuggestion`/`addSecondarySpan` (the gated maintainers, utils.ts:51-61) appear in no barrel (verified at all four entries + export probe: ∉ the 34 root keys). The only way a consumer can add one is a direct `.push()`, which bypasses the `diagnosticsEnabled` gate and manufactures the exact state the module treats as impossible (non-empty suggestions, diagnostics off) — which `parser.ts:63-64` then aliases onto the errorState and `utils.ts:121-122` copies into a Diagnostic. | `state.ts:44-45`; `dist/state.d.ts:34-35` | **W2** — in the algebra, `D` is opaque and writer-mediated; published mutable substate with no published mutator is unrepresentable. Library side NO-WAVE-OWNER (export the writers or make the fields readonly — the latter is 2.0.0). |
| **PT-STATE-11** | **α-miss #1 (adopted)** | `mapState` hands the consumer callback an `Object.create(state)` view (`parser.ts:177-179`) typed as `ParserState<T>` (:162-167): it passes `instanceof`, its own keys are only `{offset, value}`, and every other field reads through the prototype into the LIVE state — `oldView.clone()` splices stale offset/value onto current isError/furthest/src; `oldView.toString()` renders the live error badge at the stale offset; each invocation mints a fresh hidden class. **Falsifies the universal form of L's S-1** ("one shape, every time") — the only instanceof-passing non-8-key shape in the family. Latent: `mapState` has zero consumers in-tree. Sole `Object.create` site in src (grep verified). | `parser.ts:162-187` (read this session) | **NO-WAVE-OWNER** — library cure: build the view via the constructor (or document it as a read-only projection). The S-1 correction is recorded in the superlatives. X·P wraps ParserState regardless. |

### Minors

| id | merged ids | claim (adjudicated) | terminal disposition |
|---|---|---|---|
| **PT-STATE-12** | C-5 = L/M-3, **re-ruled per ruling 1** | Three position getters, two undocumented conventions (0-based vs the documented 1-based), one shared inclusive-`lastIndexOf` bug firing only at `src[offset]==="\n"` (col clamps negative→0 at :116; line counts the terminated line as past); two of three have zero callers and no doc comment. The wholesale-wrongness both challenges allege is REFUTED by replication. | **NO-WAVE-OWNER** — retire `getColumnNumber`/`getLineNumber`, keep `getLineAndColumn` (C §6's verdict, now for the corrected reason: redundancy + convention drift, not wrongness). |
| **PT-STATE-13** | L/m-1 | Six public zero-caller methods (`save`, `restore`, `from`, `unsafeCall`, `getColumnNumber`, `getLineNumber` — census re-run this session), ~16% of the module dead public surface, all on both tiers, no `@internal`. `clone()`'s one caller is defective (PT-STATE-4). | **NO-WAVE-OWNER** — 2.0.0 excision ledger; the typed lesson (an unexercised public method that returns wrong answers is worse than an absent one) folds to the X·P surface-authoring law. |
| **PT-STATE-14** | C-7 = L/m-4, **ruled per ruling 5** | `save()` captures 2 of 8 fields; `restore()` forces `isError = false` unconditionally (:82), leaves the diagnostic quartet pinned, and takes an inline `{offset, value: any}` — the only undisciplined `any` in a project whose siblings carry explicit disables. Zero consumers ⇒ MINOR. | **W2** — mark/rollback is a first-class algebra op under R-LAW-1; the incumbent pair RETIREs. |
| **PT-STATE-15** | L/m-3 + β's bonus | Both "single choke point" docblocks false: `unsafeSetValue` has **6** direct `.value =` bypasses (census re-run: state.ts:81, packrat.ts:258/:298/:354, parser.ts:179/:350 — L's 8 was arithmetic drift); `unsafeCall` has **0** callers while ~35 inline `as ParserState<unknown>` casts are the live idiom. Bonus (β, verified): `parser.ts:348-350` is a dead read-write-back of `.value` around an offset restore. | **NO-WAVE-OWNER** — docblock correction + dead-statement deletion; the *idiom* keeps (superlative 1). |
| **PT-STATE-16** | C-4, re-ruled per ruling 6 | `createParserContext` published on both tiers; `parserNames` on neither (3 hits, all type positions; absent from both export lists — probed). Type recoverable from the .d.ts; runtime enumeration blocked. | **NO-WAVE-OWNER** — export it or retire the debug facility with it (C §6). |
| **PT-STATE-17** | C-9 + L/m-6, re-ruled per ruling 7 | `/core` ships ParserState without its two field types; the laundering path is a genuine three-hop identity (`utils.ts:1` imports from state, `:3` re-exports, `debug.ts:7` imports them back from utils). Erased type-imports make the harm hygiene-grade. The four-concern cohesion complaint survives with M-1's causality amputated (ruling 4). | **NO-WAVE-OWNER** — export types from `/core`; split concern 4 (`context.ts`) at the next breaking cut. |
| **PT-STATE-18** | C-11 | `createParserContext` has no return annotation; the .d.ts inlines all 29 literals with `name` REQUIRED against the all-optional `ParserContext` — adding a combinator name textually changes a different function's public declaration. Verified at `dist/state.d.ts:70-74`. | **NO-WAVE-OWNER** — one annotation (`: ParserContext`). |
| **PT-STATE-19** | C-12 + β's nuance | `"trim"` is produced nowhere (grep: state.ts:156 + debug.ts:274 only; `trim()` emits `"trimWhitespace"` at :494/:516, falls through to wrap at :520). β's correction verified this session: `debug.ts:273-274` is `case "wrap": case "trim": {` — a dead **label** on a live shared arm, not a dead branch. | **NO-WAVE-OWNER** — delete the label with the union member; folds into PT-STATE-7's closed-union requirement. |
| **PT-STATE-20** | C-13 = L/m-5 | `toString()` (state.ts:136-138) routes any incidental string coercion through the full diagnostic renderer — `debug.ts:58` full-source split + `state.ts:131` prefix materialization per render — and is PT-01's print payload at `parser.ts:67-69` (both challenges' :68-70 cite corrected; = chunk :881-882, O-15's ±1). Attribution held at MINOR for state.ts (parser.ts chooses to print); R-LAW-3 makes the print path unpassable for any lowering. | **W2 R-LAW-3 fold** — rendering is a free function over `D`, never a method a logger can trigger; `toString` RETIREs from the class. Armed-leg cost → MEASURE-AT-OPEN #3. |
| **PT-STATE-21** | L/m-8 + **α-miss #6 (adopted)** | Context retention is broader than the `this`-twice in then/or (parser.ts:101/:120, redundant per debug.ts's args-only arms): every construction copies the rest array (state.ts:182→:187); N-ary spread sites duplicate the whole parser list (`leaf.ts:77/:149/:162` — verified); `chain` retains the user closure (parser.ts:142) and `recover` retains the user sentinel (parser.ts:686 — verified) in `args` for process life, no release path. | **NO-WAVE-OWNER** + **MEASURE-AT-OPEN #5** (heap per 1k-parser grammar, only if a candidate hosts on parse-that). |
| **PT-STATE-22** | L/m-9 | The top-level Parser pins the whole last source + produced value graph (`parser.ts:66/:71`; state.ts:48-49 strong refs; no `clearState` anywhere). Bounded (overwritten next parse). | **NO-WAVE-OWNER** — document or add a release path. |
| **PT-STATE-23** | **β-miss #3 (adopted)** | `resetErrorState`, `isDiagnosticsEnabled`, `popLastDiagnostic` are exported from utils.ts but reachable from **no** published subpath (verified at all four entries + probe: ∉ the 34 root keys) — so a consumer can arm diagnostics (`enableDiagnostics` IS published) but can neither query the flag nor reset the error quartet as a unit; the cure both challenges prescribe for clone() is unreachable. A one-way public setter with no public getter is O-8's anti-pattern at the API layer. | **NO-WAVE-OWNER** — additive exports, 1.1.0-eligible, diagnostics-HIGH lane of the ask letter. **REFINEMENT FOLD**: O-8's wording gains "and no public toggle without a public query". |
| **PT-STATE-24** | **α-miss #4 (adopted)** | `debug.ts:245` `PARSER_STRINGS` — unbounded, never-cleared process-global Map keyed on the monotonic global parser id; entry is public and accidental (`Parser.toString()` → parserPrint, :698-700); no eviction, no cap, no clear API (verified: writes at :322/:340/:346). The family's only unbounded global growth; both challenges audited PACKRAT_ARMED/diagnosticsEnabled and missed it. | **NO-WAVE-OWNER** — cap or clear API; joins the module-global census W2's O-8 rules against. |
| **PT-STATE-25** | **α-miss #5 (adopted)** | `debug.ts:358/:379` — `debugDepth++`/`--` around `parser.parser(state)` with **no try/finally**; any throw (user map/chain callback, packrat's deliberate RangeError, PT-04's overflow) leaks the counter permanently and mis-indents all later debug output. The library uses `finally` for exactly this everywhere else (parser.ts:44-48, packrat epochs) — an inconsistency, not a style choice. | **NO-WAVE-OWNER** — one try/finally. |
| **PT-STATE-26** | **α-miss #7 (adopted)** | `ok`/`err`/`from`'s `offset` parameter is a **delta** (`this.offset += offset`, :56/:63/:70) while the class field of the same name is absolute; none of the three carries a doc comment, so the emitted declaration (`ok<S>(value: S, offset?: number)` beside `offset: number` at dist/state.d.ts:24/:37) gives a ParserFunction author the same word with the opposite meaning. The library's own leaf depends on the delta reading (leaf.ts:340). `state.ok(v, newOffset)` silently doubles the offset. | **NO-WAVE-OWNER** — doc comments + rename the parameter (`advanceBy`); folds into PT-STATE-2's W2 row as contract evidence. |
| **PT-STATE-27** | **β-miss #4 (adopted)** | Field-level gaps neither challenge measured against the 52-export /css contract: ParseIssue requires a `code` from a frozen 8-member union — ParserState has no code field anywhere, and `Suggestion.kind`'s 2 members are a **disjoint vocabulary, not a subset** (corrects C-14's framing); ParseIssue.expected is required while `state.expected` is `undefined` by default on the shipping path — the default path produces an unrepresentable ParseIssue, EQ-4's exact disqualifier (PT-01). | **REFINEMENT FOLD** — W2 prices the code-assignment layer explicitly; it is where EQ-4 is won or lost for any lowering hosted on this state. |

### Info

| id | merged ids | claim (adjudicated) | terminal disposition |
|---|---|---|---|
| **PT-STATE-28** | **α-miss #8 (adopted)** | `parserPrint` guards on a 0-based id with truthiness: `debug.ts:320` `if (!id)` and `:339` `if (id)` treat parser id 0 as absent — never memoized, and the lazy cycle-breaker is unreachable for it. Latent only because id 0 is `whitespace` (constructed first at parser.ts:711's `_initWhitespace()`), an accident of module-eval order. | **NO-WAVE-OWNER** — `id === undefined` guard. |
| **PT-STATE-29** | C-14 | `Suggestion.kind` closed at 2 members pinned to delimiter recovery (producers: utils.ts:74-79 + parser.ts:468-472 only). Correctly not-a-defect-today; the vocabulary question is PT-STATE-27's. | **REFINEMENT FOLD** (with 27). |
| **PT-STATE-30** | L/i-1 + L/i-2 | PT-04 (lazy depth) and PT-07 (non-string totality) are correctly homed ABOVE state.ts: no depth counter belongs on the hot state (the cure is a catch at parser.ts:43-48's existing try/finally); `src: string` is erased at runtime so the single guard point is `parseState` entry (`parser.ts:34`), and the observed messages are grammar-dependent (leaf.ts:285 vs :297) — any message-matching invariant is brittle. Agrees with W1.md:76's homing. | **W1** (G-9 harness gate + boundary corpus row) — already owned; recorded as confirmation. |
| **PT-STATE-31** | β CORPUS-INTEGRITY | `typescript/dist/` is untracked and locally built, and **byte-identical** to the tarball the prototype workspace resolves — re-verified this session by `diff -q` on `core.js` and the 40,576 B chunk (IDENTICAL). This is what makes every dist line-cite in this corpus normative for what the prototypes actually consume. | **W1** — the harness pins the dist hash it consumes (provenance rider). |

## Killed-claims register

| # | claim | killer(s) | this seat's re-derivation |
|---|---|---|---|
| **K1** | **C-§1a (MAJOR)** — "the RED-7 surface-gaps script cannot run; ERR_MODULE_NOT_FOUND; a born-RED gate that cannot execute is unfalsifiable in place" | both readers | **DEAD three ways, re-proven**: (1) the package is installed in the script's own workspace (`css-parser/package.json` deps + node_modules present); (2) `createRequire(<script path>).resolve()` returns real files for all three specifiers — Node resolves bare specifiers from the **importer's** URL, not cwd; the script's header says exactly this; (3) W1.md:436-441/:570-580 records the 2026-08-03 run with pasted rows. C's blanket demotion of the RED-7 rows to "a dated record" is **void**; the rows are live. The single most consequential error in the C challenge. |
| **K2** | **C-§1/§6 as generalized** — "no installed consumer to break; retirement cost zero" (and reader-α's confirmation of it) | reader-β; sustained, α overruled | Ruling 2. 19 importing files; ParserState constructed directly via `/core` in the band's own replay fixture. The shipped-library zero-edge survives as *ruled* (W2 not-in-scope), never as "nothing consumes this". |
| **K3** | **L/M-1's root-cause attribution + cure #2** — "remove state.ts:2 … letting ./core honour core.ts:4-6" | both readers | Ruling 4: the cure is a provable no-op — `core.ts:7` + `parser.ts:3/:7` retain debug/ANSI/packrat/diagnostics via `Parser` alone (:695/:699 prototype methods; :43 unconditional). Also cite drift: the prose is core.ts:3-5; dist/core.js is 1,336 B importing the 40,576 B chunk, not the chunk itself. |
| **K4** | **C-1's narrowed claim + BLOCKER rating** — "state.ts:2 is the only link on the ParserState-alone path; would pay for nothing if toString() did not exist" | reader-β | Ruling 4: for the non-bundling population C declares normative, `dist/core.js:1` makes evaluation total regardless of symbol — with state.ts:2 deleted the payload is unchanged. Asserted where false, disclaimed where possibly true. Substance survives at MAJOR (PT-STATE-3). |
| **K5** | **C-5's "not a line number under any convention" + M-3's "every offset on every line after the first"** (and reader-α's MAJOR confirmation) | reader-β; sustained by this seat's replication, α overruled | Ruling 1: getLineNumber/getColumnNumber are correct **0-based** getters everywhere except offsets pointing AT a newline. C compared 0-based to 1-based; L contradicted its own stated convention note within the same finding. Survivor re-booked as PT-STATE-12 (MINOR). |
| **K6** | **L/m-7** — "parserNames … ships in every bundle including ./core"; "forces the 20+ `args![0]` assertions" | reader-α | Re-verified: the tuple is fully tree-shaken — absent from both export lists AND the literal absent from the chunk (`"lookAhead"` appears once, as a createParserContext argument). The claim's own premise (never read at runtime) is why Rollup drops it — self-refuting. Surviving half = PT-STATE-18. |
| **K7** | **C-8's falsifier** — "regex(/\s*/) always succeeds zero-width … I measured no acceptance difference and claim none" | reader-α (missed-finding) | Re-derived analytically: `leaf.ts:327-330` fails ANY regex at EOF before the pattern runs; the wrap path's trailing end-call (parser.ts:416-422) therefore rejects inputs the fast path accepts. The kill *upgrades* its host row (PT-STATE-7). |

**Record hygiene (β's provenance census, preserved)**: C's line cites drift systematically (+1 tier-wide; `parser.ts:495` twice for a fallthrough that lives at :520 — the opposite branch; `proof-no-span-surface.mjs:33-37` → :30-34). L is materially cleaner (drifts: core.ts:4-6→3-5, dist:878-880→876-878, an m-3 tally of 13/8 where the tree has 12/6). None changed a verdict; two would land a re-reader on the wrong branch of `trim()`.

## Superlatives (L-18 runs both ways — all re-verified by this seat)

1. **`as any` = 0 across all of src/parse** (L/S-4 = C/S-1) — census re-run this session, empty result set; `as unknown as` = 3 in state.ts (the ok/err/from returns) and the `unsafe*` prefix makes the sanctioned erasure surface enumerable in one grep. The strongest thing in the module family. Keep verbatim through any refactor.
2. **The hidden class IS stable for constructor-built states** (L/S-1) — chunk :300-302 emits all three fields installed at construction; L's own falsified hypothesis is recorded, not suppressed, and correctly kept as M-5 on the mechanism's fragility. **Correction adopted**: "one shape, every time" is falsified by `mapState`'s Object.create view (PT-STATE-11) — the guarantee holds for every state the *constructor* makes.
3. **Per-parse diagnostic substate is O-8 implemented before O-8 was written** (L/S-2 = C/S-2) — state.ts declares zero top-level mutable bindings (in a family carrying ~11 module-globals elsewhere), and the proof is executable: `test/reentrancy.test.ts:28` "preserves the outer parse's furthest-offset across a nested .parse()" (name verified this session, plus the PT-Q1/PT-Q2 suites).
4. **`getLineAndColumn` is the one that gets the hard case right, for the stated mechanistic reason** (C/S-3) — this seat's replication: the exclusive `offset - 1` bound is exactly why the one input class that breaks its two siblings (offset AT a newline) is the class it handles; also the only documented one, the only consumed one (debug.ts:59), the only one taking an explicit offset. Keep verbatim.
5. **The mutable-single-cell architecture is vindicated by the band's own numbers** (L/S-5) — zero per-combinator state allocation, integer backtracking, and parser-band.md:39/:42's 1500 ns published vs 1247 ns cand-O: a combinator library beating the bespoke parser on its own corpus. The phantom-type cost is owned honestly in the `unsafe*` naming.
6. **`getCijKey` (packrat.ts:79-100) is the model posture the module under audit lacks** (neither challenge recorded it) — documents the exact prior defect it cures in-source, chooses a float64-safe key, and FAILS LOUD with a RangeError naming both budgets rather than silently aliasing. "Fail loud at the boundary rather than silently alias" is precisely what PT-STATE-1 shows `.parse()` lacks — present and working two modules away.
7. **The hitherto corpus cured B-1 at the consumer, in writing, three times, independently** (β) — cand-o/index.ts:11-16, cand-b-dispatch/grammar.ts:547-551, idiom/example.ts:192-198, each naming the concrete crash it prevents (verified verbatim). That is a challenge corpus doing its job, and it is the honest bound on PT-STATE-1's program cost. Adjacent: the allocation discipline the challenges indict state.ts for missing is executed well next door (many/sepBy pre-sizing, fuseAll's positional unrolling) — the two guaranteed-empty arrays are the anomaly, not the norm.

## MEASURE-AT-OPEN residue (bench-dependent rows for the lane's W1 harness — no figure asserted here; arming is out of this seat's bounds)

| # | row | what W1 measures | source rows |
|---|---|---|---|
| 1 | **Reject-path allocation leg** | arrays allocated per failed parse (default vs armed), incl. the per-furthest-advance unguarded pair (utils.ts:34-35) — parser-band's own "reject path as its own leg" debt | PT-STATE-5 |
| 2 | **Dead alias overwrite** | cost of parser.ts:61-64's construct-then-alias on the failure path (dist :875-878) | PT-STATE-5 |
| 3 | **Armed render cost** | per-failed-parse cost of the unconditional `console.error(toString())` — two full-source materializations (debug.ts:58 + state.ts:131) — on the diagnostics-armed leg only | PT-STATE-20 / PT-01 |
| 4 | **Shape assertion at open** | assert `Object.keys(new ParserState("x"))` = the 8-key order, every harness open (cheap runtime assert, not a bench) — turns PT-STATE-6 from invisible to gated | PT-STATE-6 |
| 5 | **Context retention footprint** | heap held by contexts (`args` rest arrays, spread copies, chain closures, recover sentinels) per 1k-parser grammar — only if a candidate hosts on parse-that | PT-STATE-21 |
| 6 | **PARSER_STRINGS growth** | interned bytes after any harness leg that prints a parser — only if debug printing enters a leg | PT-STATE-24 |

## Closing verdict

**WRAP-AND-RETIRE SUSTAINED, WITH BOTH CHALLENGES' HEADLINE ATTRIBUTIONS DEAD.** Of the module's 9 exported symbols, X·P wraps or retires 8 — the unqualified keeps are `getLineAndColumn` and the `unsafe*` *idiom* — and that §6 verdict of C survives every kill above because none of its rows depended on the killed causality. The three rulings that reshape the record: (1) **PT-STATE-1 is a BLOCKER on a published, immutable surface** whose program cost is honestly bounded by the corpus's own three written cures and by types.ts:27 — the bound lives inside the row, not in a severity discount; (2) **the `/core` tier cannot be made honest by any state.ts-confined cure** — the retention edges are `core.ts:7` + `parser.ts:3/:7`, so the choice is amend the prose or split `Parser`, and the subpath gate stays blind until W1's exclusion assertion lands born-RED; (3) **the position-getter indictment was a conventions error in the challenge, inherited by one reader** — re-ruled MINOR by replication, and the inheritance event is recorded because it is the fleet's known failure mode wearing new clothes. Twelve reader-missed rows were adopted after independent re-derivation — the sharpest being the `/core` label-writer absence (PT-STATE-9), the writer-less published diagnostic fields (PT-STATE-10), and the `.trim()` acceptance divergence (PT-STATE-7's upgrade), each of which lands directly on a W2 law the specs already carry. Nothing here authorizes execution; every cure is routed, every residue is measurable at open, and the original challenge files and reader returns remain immutable beside this record per E-1/E-3.

— end of apotheosis.
