claude-fable-5

# kf-TimelineCaret — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Anchors — the tree is a HYBRID and this record says so up front**: keyframes.js local `master` HEAD `8281638c` · `origin/master` `81a56990` · merge-base `a59d3a22`; **neither ref is an ancestor of the other** (my own `git merge-base --is-ancestor` runs, both directions NO). `TimelineCaret.vue`, `components/TimelineTrack.vue`, `demo/styles/layout.css`, `demo/styles/design-idioms.css` are **byte-identical at all three refs** (my `git diff --stat HEAD origin/master --` on the four → empty) — every caret-local claim is anchor-safe. The composable/engine tier (`KeyframeTimeline.vue`, `useTimelineOps.ts`, `timelineTypes.ts`, `useTimelineBuild.ts`, `timelineEngine.ts`, `snapshotCapture.ts`, `flattenVars.ts`) is ` M` vs HEAD and origin-side; `demo/utils/keyframeSelector.ts` + `demo/utils/formatEditorCSS.ts` are `??` UNTRACKED and **absent from HEAD's tree** (my `git ls-tree`). Registry branch `tranche-u` (value.js). Producers: `@mkbabb/glass-ui` 7.0.0 installed (HEAD declares `optionalDependencies: 6.0.0` + 3 lock entries; origin declares `devDependencies: 7.0.0`; the worktree declares NONE — all three states read by this seat); `@mkbabb/value.js` 4.0.0 installed (HEAD declares `^3.1.0`; origin/worktree `4.0.0`).
**Corpus**: three axis challenges (D: 19 defects/2 BLOCKER · L: 15/1 BLOCKER · C: 11/0 BLOCKER, all `claude-opus-5[1m]`) at `docs/tranches/V/megatranche/audit/kf-components/TimelineCaret/`, re-read by two hostile re-readers — **RR-α** (design-and-usage) and **RR-β** (library-and-code) — both returns adjudicated here, attacked before either was adopted.
**Method (L-18/M-23)**: every verdict-level disagreement (the C-1 confirm-vs-kill, the L-1 fold-vs-kill, the L-5 rung, the C-7 census cell, the C-9 mechanism, the C-4 fold-target, the K-6 branch kill) was re-derived against the tree with this seat's own instruments; the decisive C-1 falsifier NEITHER the axis nor RR-α ran (the bit-identity + import-verbatim trace) was run by this seat; four suspiciously-agreed cells (the 8-integer set, the glass declaration at HEAD, the forced-colors restoration list, the light-arm token identity) were re-measured, not spot-trusted. **No browser tooling** (law); product source READ-ONLY; the irreducibly visual residue rides to SS-13.
**Arbiter's own instruments (all static + git + node; zero writes to product source)**: read WHOLE — TimelineCaret.vue (70 L), keyframeSelector.ts (31 L), timelineEngine.ts (104 L), useTimelineOps.ts (88 L); read in load-bearing part — TimelineTrack.vue (:21-48, :60-107, :160-186, :198-215, :216-246), KeyframeTimeline.vue (:3-4, :100-110, :170-172), useTimeline.ts (:78-92), layout.css:137-141, design-idioms.css:70-82 + :220-230, index.html:6; **executed** `node -e` IEEE sweep: exactly 8 integers in [0,100] fail `(p/100)*100===p` — `[7,14,28,29,55,56,57,58]`, `29→28.999999999999996` — AND `29/100 === 0.29` is `true`/`Object.is` bit-identical (the C-1 decider), 0 and 100 uncorrupted; **git**: `show HEAD:package.json` / `origin/master:package.json` / worktree (the three glass-ui declaration states above; value.js `^3.1.0`@HEAD), `show HEAD:…useTimelineOps.ts` (`moveKeyframe` at :54-60 has NO `kf.selector` line, NO `percentSelector` import), `show HEAD:…timelineTypes.ts` (`percent: number; // 0–100` — no `selector` field), `ls-tree HEAD demo/utils/` (keyframeSelector.ts absent), HEAD lock glass-ui entries = 3; **greps**: `@click` on TimelineTrack.vue → **0** (the MISS-β3 decider; KeyframeTimeline's five `@click`s are all button-local), `.focus-ring` demo census → law at design-idioms.css:73-79 verbatim + 4 class consumers (SquareScene.vue:46, SpringHeatmap.vue:30, SpringTarget.vue:63, keyframes/KeyframeCard.vue:45), `addKeyframe(` callers → **0** (dead surface; the `addKeyframes` hits are the unrelated KeyframesEditor string path), `text-mono-caption` → 21 sites, `keyframeId` across all 40 banked kf-*.md → 0, `input-on-glass` across the bank → **1** (kf-CSSPasteDialog R-24 — correcting RR-α's zero-hit sweep), `selectorText` in src/ → `const` at format.ts:20 used :132/:201/:280/:313, never exported (emit/index.ts export list read; `serializeSelector` private at css-text.ts:58-59; the demo's exported copy at keyframeSelector.ts:7-12 importing library-private `namedSelectorToFraction` at :5), `useKeyframeOps.ts:9` imports `selectorText` from `@utils/keyframeSelector` — the DEMO copy (the KC-1 correction decider); **installed glass 7.0.0 dist**: `tokens/color-radius.css` `--foreground: hsl(24 10% 10%)` ≡ `--primary: hsl(24 10% 10%)`, `tokens/light-dark.css` light arms identical / dark arms distinct (the M-3 decider), `utilities/a11y-overrides.css` opens with the UNIVERSAL PRM blanket `*:not([data-allow-motion])` (the K-7 decider) and its forced-colors block restores exactly the enumerated list incl. `.focus-ring:focus-visible` (the M-5 + K-9 cure decider), `tokens/on-glass-fg.css` `--input-on-glass: hsl(36 40% 92%)` / `dark-arm.css` `hsl(26 12% 22%)`, `_shared/field-control.css` `--control-surface-bg: var(--input-on-glass)` (the MISS-α3 decider), `glass/ladder.css` `@container style(--glass-backdrop: light)` block present, rebinding `--muted-foreground: var(--foreground)` on `.glass-quiet` (the M-3 deep-arm premise), exports map `./forms ./label ./labeled-field ./number-field ./timeline`, NumberField d.ts default slot optional; **shipped bundle** `dist/gh-pages/assets/index-CL_QYCiO.css` (Jul-16, witness-staleness caveat): `.text-admin-label{` at 213488 + 217365, `.font-semibold{` at 166835 + **218810** — the LAST `.font-semibold` out-orders the last `.text-admin-label` inside `@layer utilities` (opens 182187), equal specificity → the weight residue is LIVE (the K-6 decider, refined to two-occurrence form); `clampIOSNoZoomFontSize` at `instrument/utils/iosTextEntry.ts:10`, applied only at `keyframes/CSSCodeEditor.vue:137`.

## Scoreboard

**25 ADJUDICATED-CONFIRMED (0 BLOCKER · 7 MAJOR · 13 MINOR · 5 INFO) booked here · 11 whole-row FOLDS + 4 partial arms by reference to banked kf-KeyframeTimeline (M2/D-3/M8/M1/D-17/D-28/D-9/N-11/N-6/D-18/C-15), kf-CSSCodeEditor KF-CE-16, kf-CSSPasteDialog R-24, and census F-1 (identity guard, zero re-bookings) · 17 KILLED with re-executed refutations — including ONE reader-vs-reader overrule (C-1: RR-α's confirmation overruled on this seat's own execution) and two BLOCKER demotions already ruled at the bank · 10 net-new missed rows adjudicated IN (8 booked, 1 merged as a deciding cell, 1 folded as a cure note) · 12 UNPROVEN-NEEDS-LIVE residue rows to SS-13.**

The component's center of gravity: a **70-line leaf whose interior discipline is genuinely excellent** (five product superlatives, all re-verified) sitting inside three seams it does not control — the parent's pointer field (banked M2), a lossy serializer it neither owns nor corrupts (killed C-1 → booked MISS-β2 at the true locus), and a design system it declines four primitives of (C-4/C-7/M-5/M-7, all → KF.W6). The heaviest NEW truth is the pair MISS-β1 (a read gesture manufactures selector-key collisions that silently drop keyframes from the artifact) and MISS-β2 (the serializer that corrupts integer percents exists in THREE private copies, so the cure must land three times and the banked KC-1 cure as written names a symbol that is not exported).

Status verbs per M-25: dispositions are **ADJUDICATED**, never VERIFIED — X·KF specs are forming; routings are agglomeration input. Wave targets from the census taxonomy (KF.W0–W10); this cluster is the census-named subject of **KF.W7 · Timeline Evaluate**, with glass/token/a11y-name rows to KF.W6, structure rows to KF.W8, the type-gate by fold to KF.W4, and the visual horizon at KF.W9/SS-13. **No row here is NO-WAVE-OWNER.**

## Ruled disagreements (the seat's actual work)

| id | RR-α (design-and-usage) | RR-β (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-1** (the sole-integer-writer / corrupted-export MAJOR) | CONFIRMED MAJOR ("the best-argued row in this corpus"), anchor-corrected only | KILLED — causal attribution refuted by C-1's own worked example | **KILL ADOPTED — RR-β wins, on this seat's own execution; RR-α overruled** | Three probes, all mine. (1) `importCSSToTimeline` stores the PARSED selector verbatim (`timelineEngine.ts:95-99` `selector: parsedSelector`), so an imported `29%` frame's export key is `${0.29*100}%` = `28.999999999999996%` from the moment of import — **the export never contained "29%"**; under this serializer the string `"29%"` is unrepresentable for a percent-29 frame. (2) `29/100 === 0.29` is bit-identical (`Object.is` true, my node run), so the caret's commit of 29 → `percentSelector(29)` → `{value: 0.29}` **changes zero bytes of the export** — C-1's dramatized no-op-edit harm cannot occur. (3) "Sole exact-integer writer" is false twice: `onMarkerKeydown` (TimelineTrack :203-214, my read) steps ±1/±10 and jumps Home/End — from any integral base (e.g. an imported `50%`, where `0.5*100` is exact) the arrow path writes integers straight through the corrupted set; and RR-β's own cited co-writer `addKeyframe` is DEAD (my grep: 0 callers) — a limb of the kill that needed correcting, not the kill. The float mechanism is real and **survives at its true locus, the serializer** — booked as MISS-β2. RR-α re-ran the 8-integer measurement (it reproduces; so did mine) but never ran the causal falsifier C-1 itself listed; preserved in DISSENT. |
| **L-1** (phantom glass-ui, filed BLOCKER) | Confirmed-as-fold: INFO → census F-1, + K-11 killing the BLOCKER double-count | KILLED whole: baseline-crossed at every committed ref | **BOTH ADOPTED, MERGED AS ONE KILL + ONE FOLD CELL** | My `git show` runs: HEAD carries `optionalDependencies {"@mkbabb/glass-ui": "6.0.0"}` + 3 lock entries → `npm ci` at HEAD INSTALLS glass-ui, silently downgrading the 7.0.0 on disk — it does not fail; origin/master carries `devDependencies: 7.0.0` exact → no defect at all; the "absent from both manifests" state exists ONLY in the uncommitted worktree (my worktree read: glass-ui in neither field). L-1's own falsifier ("a glass-ui entry appearing in package.json") is satisfied by one `git show`. The kill precedent is BANKED (kf-KeyframeCard K3-correction). K-11 stands independently: a row folded by reference to census F-1 cannot also be re-earned as this document's one BLOCKER. Residue: the layer-(b) token-exposure census (8 of 8 presentational classes resolve only through glass dist) is a genuine NEW CELL contributed to census F-1 — the rung belongs to F-1. |
| **L-5 rung** (iOS focus-zoom) | MINOR | MAJOR | **FOLD — the rung lives at the bank** | The mechanism, hazard, AND named cure are banked at kf-KeyframeTimeline **D-9** (MAJOR, → KF.W6): same 10px `--type-admin-label`, same viewport meta (my read of index.html:6), same unapplied in-repo cure. This is a SECOND SITE of a banked identity, not a new row — folded, contributing the caret-input cell + RR-α's material-omission cell (the repo ships `clampIOSNoZoomFontSize`, iosTextEntry.ts:10, applied only to Monaco at CSSCodeEditor.vue:137 — my grep — and axis-L's falsifier list never searched for it). Neither reader's local rung applies; both positions preserved in DISSENT. |
| **C-7 census cell** (focus ring) | K-9: population is NOT two — `.focus-ring` is a documented demo LAW with ≥4 consumers; C-7's grep censused around the idiom | Endorsed the two-element population with its own grep | **RR-α WINS — my own census decides** | `design-idioms.css:73-79` read verbatim: "The demo-owned :focus-visible contract — the SINGLE keyboard-focus affordance: `.focus-ring` paints glass-ui's --focus-ring-shadow…". My grep: 4 class consumers incl. `keyframes/KeyframeCard.vue:45` — the same instrument, the exact `focus-ring` + `outline-none` pairing this file needs. RR-β's grep (`focus:ring\|focus-visible:ring`) was correct FOR ITS PATTERN and wrong for the population — the bare class cannot match it. Consequence adopted: C-7 is STRONGER than filed (departs from a documented law, not a two-element peer group), and the cure order inverts — `.focus-ring:focus-visible` is IN glass's forced-colors restoration list (my dist read), so ONE CLASS discharges D·M-5 without waiting on the NumberField swap. |
| **C-9 mechanism** (three-file overflow coupling) | K-10: cells refuted — padding box, ~1px overhang not truncation; the EDIT INPUT is the real overflow dependent; ancestors provisioned for the ticks | Confirmed as filed | **RR-α's corrections ADOPTED; INFO survives corrected** | My TimelineTrack reads: `:24` declares `border border-border` → `top: 50%` resolves against the 46px PADDING box (mid 23, not 24); the 10px label spans ≈37-47 — a ~1px overhang, not a Card-clip truncation. The element that NEEDS `overflow-y-visible` is the `h-5` edit input (spans ≈37-57, hanging ~11px into `.timeline-track`'s own `margin-bottom: 1rem`, :218-221 my read). The ancestor overflow chain is demonstrably provisioned for the TICK labels (`-top-5` at :45; `margin-top: 1.25rem` reserved at :219). Booked INFO with this mechanism; the `--caret-offset` namespace arm folds to banked D-28. |
| **C-4/M-9 fold-target** (NumberField/Input shadow) | "FOLDS → banked kf-KeyframeTimeline C-15" | "NOT banked as a caret row" | **RR-β WINS — RR-α's citation is a MIS-FOLD** | Banked C-15 (registry line 79, re-read by this seat) is the `/timeline`-shadow split verdict — ScrubberTimeline/geometry, the playhead question. It says nothing about `/number-field` or `/forms`. Folding this NEW defect into that row would have silently discharged it into an adjudication that never examined it — the identity guard misused in the inverting direction. Booked MAJOR net-new (roster row 4). RR-α's OTHER contribution on the same row is adopted: the installed NumberField d.ts default slot is OPTIONAL (my read), retiring axis-D's own swap-landability falsifier. |
| **MISS-β1** (rounding × merge composition) | Folded L-14 → banked N-6, no composition | Booked missed MAJOR: the composition promotes the rounding row to artifact data loss | **BOOKED MAJOR — RR-β wins** | Every input re-verified mine: the merge is real and admitted (`timelineEngine.ts:42` comment verbatim; keyed on `selectorText(kf.selector)` at :40; the same map feeds BOTH `fromKeyframes` :51-53 and the export :62-72); the caret quantizes every commit to 101 slots reachable by `@blur` alone; banked N-6 attributes collisions to the ARROW path and never names the caret; the corpus rows (L-2/C-3/M-4) name retiming + spurious undo, never the drop. A composition neither parent makes = a bookable row (the N-10 precedent class). Parents cited, never re-booked. |
| **K-6 branch kill** (font-semibold ordering) | Killed axis-D's "no visual signal at all" second branch on a bundle byte-order read | — (not addressed) | **KILL ADOPTED — re-executed and REFINED by this seat** | My own `grep -bo`: TWO occurrences of each — `.font-semibold{` at 166835 AND 218810, `.text-admin-label{` at 213488 AND 217365. The LAST occurrences both sit inside `@layer utilities` (opens 182187), equal single-class specificity, and `.font-semibold` is later → the 500→600 weight residue is LIVE; only one of axis-D's two branches exists. RR-α's offsets (213468/218790) were single-occurrence measurements — right conclusion, incomplete census. Witness caveat inline: the bundle is Jul-16; ONLY the ordering cell leans on it. |

**Merged net-new pair (both readers, mutual corroboration, one row)**: RR-α MISS-5 ≡ RR-β K-BASE — the hybrid tree (roster row 6).

## Adjudicated defect roster — 25 rows

Ids are the corpus's own, for life; missed rows keep their finder's designation (α/β). Caret-local anchors are valid at ALL THREE refs (header); composable/engine anchors are **origin/master + worktree** — the tree that builds — and every wave curing them must say so (row 6). Every routing is M-25 agglomeration input for the forming X·KF specs.

### MAJOR (7)

- **L-2/C-3/D·M-4 — the caret is a rounding well, and its commit path dirties history even when it changes nothing**: `{{ Math.round(percent) }}%` (:13), `:value="Math.round(percent)"` (:18), `emit("update:percent", clamp(Math.round(val), 0, 100))` (:62); `commitEdit` is reachable by `@blur` ALONE (:22) and never compares against the incoming `percent` (:58-65). The model is continuous at every producer (drag `(x/rect.width)*100`, snapshot `scrubT*100`, import — my reads; `moveKeyframe` clamps without rounding). **Strengthened arm adopted (RR-β, re-verified mine)**: `moveKeyframe` allocates a FRESH selector object on every call (:60), and `useRefHistory(state, {deep, clone, capacity: 50, eventFilter: debounceFilter(100)})` (useTimeline :83-88, my read) keys on the deep watcher — so even a numerically-identical commit pushes an undo entry: a pure READ gesture (open, blur) mutates the document's history unconditionally, and retimes it whenever the percent was fractional. **→ KF.W7** (the cure must compare-before-commit AND display at model precision; the family precedent to cite is banked kf-MatrixEditor ME-42, a different component).
- **MISS-β1 (RR-β net-new, booked by ruling) — the caret's rounding is the cluster's strongest collision manufacturer, and a collision silently DROPS a keyframe from the built animation and the exported CSS while the track keeps painting two**: two keyframes at 42.2 and 42.4 each read "42%" (L-14, folded); opening and blurring each — a read gesture — makes them numerically identical; `percentSelector` yields the same fraction, `selectorText` the same map key, and the engine emits ONE frame (merge admitted at timelineEngine :42, keyed :40, feeding both the build :51-53 and the export :62-72 — my reads) while `state.keyframes` still holds two and both loops still render two (TimelineTrack :62-94, :97-106). UI and artifact disagree with no signal. Composition of the row above with banked **N-6** — in neither parent; both cited, neither re-booked. **→ KF.W7** (one row with the rounding cure: minimum-separation or coalesced rendering must be DECIDED, not inherited).
- **D·M-3 — in the light theme `--primary` and `--foreground` are byte-identical, so a hovered unselected caret is indistinguishable from the selected one (1.00:1)**: `:10` `isSelected ? 'text-primary font-semibold' : 'text-muted-foreground hover:text-foreground'`; installed `tokens/color-radius.css` + `tokens/light-dark.css` light arms identical, dark arms distinct (my reads); bridges bind `--color-primary`/`--color-foreground` straight through. The surviving signal is the 500→600 weight step at 10px — LIVE per the ruled K-6 re-probe, so exactly one signal, one weight notch, at the smallest type in the system. Registry sweep (mine): no banked row carries this mechanism. Deep arm (the `ladder.css` `@container style(--glass-backdrop: light)` rebind collapsing ALL THREE states on `.glass-quiet` — block verified present, resolution unproven) → SS-13. **→ KF.W6** (selection needs a non-color channel; joint with the D·M-5/C-7 cure — `aria-selected` is NOT the vehicle, see killed #6).
- **C-4/D·M-9 — a hand-rolled `<input type=number>` + 9 utility classes where the system ships `NumberField` and where the grandparent already imports glass `Input`**: exports map `./number-field`/`./forms`/`./labeled-field` (my read); `KeyframeTimeline.vue:171` `import { Input } from "@mkbabb/glass-ui/forms"` rendering the SIBLING field of the same keyframe (:105-109); NumberField's default slot optional → the swap is landable (axis-D's own falsifier retired). One cluster, two field idioms; the bespoke one carries B-2's name gap, M-5, M-7 and m-6. Mis-fold to banked C-15 corrected (ruled table); the locale sub-claim killed (#16); C-11's `/labeled-field` cell rides this row's swap. **→ KF.W6** (adoption row; the census S-3 amendment the axes propose is already banked as C-15's split verdict — the number-field half is THIS row).
- **D·M-7 — the edit field has no perceivable boundary against the surface it replaces (SC 1.4.11)**: `:19` `bg-background border border-border rounded` over TimelineTrack's `bg-muted/50` on `<Card cartoon tier="quiet">` (KeyframeTimeline :3, verbatim my read). Token chain re-verified at the installed pin (`--background` = `--neutral-0` = the PAGE ground). Direction structural; exact composited ratios SS-13 (same treatment the bank gave D-7). **Cure sharpened by MISS-α3** (below): glass ships the on-glass field surface — `.field-control` binds `--control-surface-bg: var(--input-on-glass)` (my dist read) — a one-line surface correction that does not wait on the NumberField evaluation. **→ KF.W6**.
- **MISS-α5 ≡ RR-β K-BASE (both readers, merged; process, fleet-wide) — the tree every document in this corpus calls "8281638c" is a HYBRID of two divergent lines, and the divergence lands exactly on the corpus's sharpest chain**: local `master` = `8281638c`, `origin/master` = `81a56990`, merge-base `a59d3a22`, neither an ancestor of the other (my runs). At HEAD: `demo/utils/keyframeSelector.ts` DOES NOT EXIST (untracked; `ls-tree` mine), `timelineTypes.ts` has no `selector` field, `moveKeyframe` has no `percentSelector` line (my `git show` reads) — so C-1's chain, L-2's selector limb and D·M-4's selector cell are unevaluable at the anchor all three axes declare; the three glass-ui/value.js declaration states differ per ref (header). The four caret-local files are anchor-safe (my diff). **This row also CORRECTS the banked kf-KeyframeTimeline header's "working tree = the audited tree" equation** — seven of that cluster's files are ` M` vs HEAD. A sixth one-baseline-short event for the fleet. **→ constraint on EVERY KF.W\* wave**: any spec consuming this registry states WHICH ref it cures against; the KF.W0-class anchor hygiene owns the durable fix.
- **MISS-β2 (RR-β net-new, booked; every limb re-executed by this seat) — the library privatizes its keyframe-selector serializer TWICE, publishes neither, and its demo authored a THIRD copy — so the float defect killed out of C-1 lives in three places and the banked cure prescriptions don't compile as written**: copy 1 `src/animation/compile/emit/format.ts:20-25` `const selectorText` (module-private; consumed at :132/:201/:280/:313); copy 2 `css-text.ts:58-59` `const serializeSelector` (private, byte-equivalent body); copy 3 `demo/utils/keyframeSelector.ts:7-12` (exported, and reaching library-private `namedSelectorToFraction` at :5 — the banked C-12 breach family at a second site). Neither library copy is exported (emit/index.ts export list read by this seat). **Consequences**: (a) the `value*100` integer corruption (my 8-integer probe) must be cured three times or the symbol published once; (b) banked **kf-KeyframeCardList KC-1**'s cure ("`selectorText`, format.ts:131 … one line, one import") names a non-exported symbol — the compilable import today is the DEMO copy (my `useKeyframeOps.ts:9` read), which is exactly the private-`src/` reach the bank convicts elsewhere; banked KF-KE-1's framing survives (it cites the demo-graph symbol). **→ KF.W8** (publish-or-relocate, joint with banked C-12) · cross-ref **KF.W2** (the emit façade); the KC-1 citation correction is recorded HERE, the dated record unrewritten (E-3).

### MINOR (13)

- **m-5/L-6/C-5 — `keyframeId` is dead API beside identity-free emits**: declared :36 (sole occurrence in 70 lines — my read), bound at TimelineTrack :100, read nowhere; `select` (:44) carries no payload so the parent closes over `kf.id` (:104-105). Registry sweep mine: 0 banked hits. The intended linkage (`id`/`aria-controls` to the sibling `role="slider"` marker) is also MISS-α2's cure — decide one shape, wire or delete. **→ KF.W7**.
- **L-7 — `const props =` is a dangling binding**: zero `props.` reads (my read); no `noUnusedLocals`, no eslint config, `.vue` outside every gate — root cause is the banked KF-CE-16 gate hole (KF.W4 fold). **→ KF.W7**.
- **L-9/m-8 — `min`/`max` are decorative and the implied `step=1` marks every fractional model value `:invalid` while the app commits it anyway**: no form/checkValidity/reportValidity in the cluster (readers' grep, spot-trusted against my track read); real enforcement is the `:62` clamp — which is PROTECTED (superlative 3). **→ KF.W7** (`step="any"` or the NumberField swap).
- **m-1/L-10 — focus is never requested; the entire exit contract (blur/Enter/Escape) hangs on an unasserted side effect of `select()`**: `:53-55` `nextTick(() => inputEl.value?.select())`, no `.focus()` in the file (my read). The design objection is source-decidable and stands; the Blink-no-op assertion is killed (#11); the probe is the component's single sharpest SS-13 item and carries the promotion trigger (a stuck-open editor = MAJOR). **→ KF.W7**.
- **L-11 — the open editor's wheel protection is accidental and non-local, and ctrl/⌘+wheel still zooms the rail underneath it**: no `@wheel.stop` on the input; the only shield is the ancestor `@wheel.prevent` (:31, my read), declared for zoom; `onWheel` has no is-editing guard. Distinct from banked M5 (the track scroll trap). **→ KF.W7**.
- **m-4 (+ MISS-α1 merged as its deciding cell) — the editor has NO discoverable entry point, in any state**: the selected branch carries no `hover:` variant (:10); no title/tooltip/underline/icon in the file (my read) against the marker's instructional `<Tooltip>` (:75); AND the residual signal axis-D credited does not exist — `cursor-pointer` sits on the ENTIRE track div (:24, my read), so the caret's own `cursor-pointer` (:9) differentiates nothing. kf-OrbitalDrag's banked cursor census covers the marker's grab cursors, never :24-vs-caret. **→ KF.W7**.
- **C-7 — the focus ring is hand-authored against the demo's own documented single-affordance law**: `:19` `outline-none focus:ring-1 focus:ring-primary` vs the design-idioms.css:73-79 law + 4 `.focus-ring` consumers incl. the same instrument's KeyframeCard (my census; ruled table). Stronger than filed; the bridged `--shadow-focus-ring` token additionally unconsumed. **→ KF.W6** (add `.focus-ring`, which also discharges the next row NOW).
- **D·M-5 — zero focus indicator under forced-colors**: `outline-none` genuinely removes (Tailwind v4 semantics, `.outline-none{…outline-style:none}` verbatim in the installed dist — reader-verified, spot-trusted), `ring-1` is an erased box-shadow, and the restoration list (my read) enumerates classes this input joins none of. SC level corrected to **2.4.7 AA** (killed #8). Cure order per K-9: `.focus-ring` first, primitive swap second. Adjacent banked D-20/D-21 are different elements/scopes — no fold. **→ KF.W6**.
- **m-2/C-6(a) — a numeric datum wears the full label register while the demo's numeric idiom — and the SAME datum 90 lines away — wears `text-mono-caption`**: KeyframeTimeline :102-104 renders `{{ Math.round(selectedKeyframe.percent) }}%` at `text-mono-caption font-semibold tabular-nums` (my read); 21-site idiom census (my grep); the governing rule at design-idioms.css:224-226 read verbatim. Cross-ref banked D-9/M10 (the register family) — the same-datum-two-registers cell is this row's own. **→ KF.W6**.
- **m-7 — the component name misdescribes the artifact and propagated the census mis-mapping**: it renders no caret/playhead/positional mark (whole-file read; the playhead is TimelineTrack :52-55). The ScrubberTimeline/geometry mapping-correction arm FOLDS to banked **C-15** (killed #15 — three axes re-derived a settled adjudication); the naming row itself is unbanked and books here. **→ KF.W8** (rename with the colocation move, banked N-11).
- **MISS-α2 (RR-α net-new) — Enter and Escape both DESTROY keyboard focus**: `commitEdit`/`cancelEdit` clear `isEditing`, the `v-else` unmounts the focused input, and nothing calls `.focus()` on a successor (my whole-file read) — the user lands on `<body>` at the top of the tab order. The natural restore target is the sibling slider diamond for the same keyframe — exactly what dead `keyframeId` would address. All three axes traced the blur triad; none noticed the focus goes nowhere. Registry sweep mine: no banked focus-restoration row for this cluster. Engine-behavior residue → SS-13; the source half stands. **→ KF.W7**.
- **MISS-α3 (RR-α net-new, one sweep-cell corrected) — the bespoke input paints the PAGE ground where glass ships the on-glass field surface**: `.field-control` binds `--control-surface-bg: var(--input-on-glass)` (light `hsl(36 40% 92%)` / dark `hsl(26 12% 22%)` — my dist reads) vs `:19`'s `bg-background` (= `--neutral-0`, the page ground) atop a `--card` plate. Turns D·M-7 from a primitive-swap into a one-line surface cure. **Correction to RR-α**: the token is NOT registry-virgin — banked kf-CSSPasteDialog **R-24** already names `--input-on-glass` as the editable-well surface; this row is a second site of that doctrine, cross-referenced not re-derived. **→ KF.W6**.
- **MISS-α4 (RR-α net-new) — the file's single most load-bearing line is uncommented in a cluster that comments heavily**: zero comments in 70 lines (my read) vs TimelineTrack's :57-61/:198-202/:229-232 reasoning blocks; the `:59` `if (!isEditing.value) return;` guard is the exact line a dedupe wave deletes, and challenge-C had to write the do-not-delete note in an AUDIT document (§2) instead of the code. The KF-CE-41 law: the comment is part of the defect surface. **→ KF.W7** (the cure commit carries the comment).

### INFO (5)

- **C-10 — `update:percent` is `v-model`-shaped but never consumed as one, and `v-model:percent` would bypass the ops layer** (skipping the selector re-derivation + `rebuild()` — my useTimelineOps read). C's reading beats L's cleared-table naive form; `defineModel` here would be actively wrong; rename the emit off the protocol. **→ KF.W7**.
- **C-9 — the three-node overflow coupling, with the ruled mechanism correction**: the real dependents are the edit input (hangs ~11px into the track's own margin) and the ~1px label overhang; ancestors provisioned for ticks; zero comments at any node. Namespace arm folds to banked D-28. **→ KF.W7** (comment the contract at all three nodes).
- **L-15 — silent swallow on unparseable input diverges from the cluster's toasting posture** (`toast.error` siblings — readers' cites, spot-checked); the axis itself flags it ruling-dependent (a garbage keystroke is arguably a cancel). Recorded as filed, unactioned pending the W7 posture ruling. **→ KF.W7**.
- **MISS-α6 (RR-α net-new) — the unit disappears on edit**: the `%` is a literal in the label branch (:13) and absent from the input branch (:14-27) — no suffix, no `aria-describedby`, no title — in a 40px box further eaten by the native spinner; `NumberFieldContent`/`labeled-field` exist to carry it. Discharges free with the C-4 swap. **→ KF.W6**.
- **MISS-β4 (RR-β net-new) — culling asymmetry inverted relative to cost**: `visibleTicks` windows a ~5-span loop while both N-node keyframe loops (full Tooltip subtree + caret per keyframe) are never windowed and are patched per pan frame then discarded by the clip. Registry sweep: no banked culling row. One line in the W7 spec. **→ KF.W7**.

## FOLDS — identity guard (anti-rename; zero re-bookings)

- **B-1/L-3/C-2 (pointerdown scrubs + captures on a node about to unmount) = banked kf-KeyframeTimeline M2 ≡ RR-B-7** (MAJOR, → KF.W7). All three axes re-filed the banked row; axis-D re-filed it at a rung the bank had already ruled against (killed #4). **Contributed cells**: MISS-β3's cure note — the guard the author DID write is INERT: `@click` listeners on the track = **0** (my grep; KeyframeTimeline's five `@click`s are button-local), so `:12`/`:25` `@click.stop` stop nothing — the fix REPLACES the inert guards with `@pointerdown.stop`, it does not stack a third modifier; and RR-β's kill of C-2's step (4) (`click` fires after `pointerup`; the editor opens after the gesture ends — press-drag scrubbing happens BEFORE, and capture releases at pointerup per L's own cleared row).
- **B-2/L-12/C-11's 4.1.2 arm (role-less click-only div; unnamed input) = banked D-3 + M8** (MAJOR, → KF.W7/W6). The BLOCKER rung + 2.1.1 limb are killed at the bank (K-3) and again here (#5). C-11's `/labeled-field` cell rides roster row 4.
- **D·M-8 (target size + failed spacing exception) = banked D-3's SC 2.5.8 arm** — the new cell contributed: the 24px spacing circles of caret and diamond INTERSECT (caret y≈31-55 vs diamond y≈12-36), so the exception fails; the counter-rotated hit pad contrast is banked S-1.
- **D·M-1/C-8 (0%/100% carets half-clipped; the tick cure 50 lines above) = banked M1** (MAJOR, → KF.W7). Reachability re-verified by RR-α (preset 0%/100% sites); pixel extent = the bank's SS-13 residue #4.
- **D·M-2 (one absolute `--caret-offset` vs state-dependent geometry) = banked D-17 + D-28** (MAJOR, → KF.W7). **Contributed corrections** (K-13, adopted): every table cell shifts ~1px — `top:50%` resolves against the 46px padding box (the track declares a border, :24 my read) — collapsed mid 23 not 24, clearance ≈1.7px not 2.69, expanded vertex 84.21 under scale-125; direction unchanged, and banked D-17 carries the same uncorrected figures — **both re-derive before any cure** (the D-19 law). Plus the z-cell: the marker holds `z-controls` (:66, my read), the caret no z-index — a hit-steal, not only an occlusion.
- **L-4 (nothing typechecks the contract at either end) = banked kf-CSSCodeEditor KF-CE-16** (→ KF.W4, the sequencing head). Every limb re-run by RR-β; scripts/env.d.ts facts already quoted in the banked kf-KeyframeTimeline instrument list.
- **L-5 (iOS focus-zoom) = banked D-9** (MAJOR, → KF.W6) — ruled table; second-site cell + the unapplied `clampIOSNoZoomFontSize` cell contributed.
- **L-8 (colocation split, child importing back up) = banked N-11** (INFO, → KF.W8). Axis-L's MINOR corrected to the bank's INFO.
- **L-14 (display rounding collapses distinct carets to one label) = banked N-6** (cell contributed: the caret-label projection); the COMPOSITION books as roster row 2.
- **m-3/L-13/C-6(b) (`font-mono` redundant against the register's own family bind) = banked D-18** (→ KF.W6; the bank already counts these two sites).
- **L-1 residue = census F-1** — the layer-(b) token-exposure census (8/8 presentational classes glass-resolved) contributed as a new F-1 cell; the defect-as-filed is KILLED (#3); the rung belongs to F-1.
- **Partial arms**: m-7's mapping-correction → banked **C-15** (killed #15); C-9's namespace arm → banked **D-28**; MISS-β3 → banked **M2** cure note; MISS-α3's token doctrine → banked **R-24** cross-ref.

## Killed-claims register — 17 rows, refutations re-executed

1. **K-BASE · all three axes' anchor declarations** ("Tree keyframes.js HEAD 8281638c… READ-ONLY evidence") — REFUTED at the refs by my own git runs: the chain C-1/L-2/M-4 build on does not exist at 8281638c (no keyframeSelector.ts, no `selector` field, no `percentSelector` in moveKeyframe). The findings survive re-anchored to origin/master + worktree; the provenance sentences are false. Booked as roster row 6; per-file anchor-safety map in the header.
2. **C-1 · "the caret is the sole writer of exact integer percents, and a no-op open-and-close rewrites the exported 29% to 28.999999999999996%"** — KILLED (ruled table; RR-α's confirmation OVERRULED). The export never contained "29%" (import stores the parsed selector verbatim — my read); the no-op commit is bit-identical (`Object.is(29/100, 0.29)` true — my run); the sole-writer claim dies on the arrow/Home-End path (integral bases step straight through the corrupted set) while RR-β's `addKeyframe` co-writer limb is itself corrected (0 callers — dead surface). The 8-integer measurement is EXACT (reproduced twice) and survives at MISS-β2's locus. C-1's own falsifiers (a)-(e) were the kill's instruments — the row was excellently built and wrong.
3. **L-1 · BLOCKER "npm ci cannot reconstruct glass-ui; blast radius 100%"** — KILLED baseline-crossed (ruled table): HEAD installs 6.0.0 (silent downgrade, 3 lock entries — my reads), origin installs 7.0.0 exact (no defect), the absent state is worktree-only; + K-11's double-count (a row folded to census F-1 cannot be re-earned as this document's BLOCKER). The kill precedent is banked (kf-KeyframeCard K3). All three axes measured the worktree while naming HEAD.
4. **B-1's BLOCKER rung** ("the only affordance cannot be exercised without side-effecting an unrelated subsystem") — REFUTED on program law + the bank: the identical mechanism is banked MAJOR (M2); BLOCKER is reserved for data loss/traps/flagship absence; the affordance works, the scrub is recoverable (useRefHistory), and the axis's own body concedes "the fix is one modifier." Inside the row, C-2's step (4) (post-open residual-drag scrubbing) dies on event order.
5. **B-2's BLOCKER rung + WCAG 2.1.1 limb** — already killed at the bank (**K-3**, registry line 120): 2.1.1 is written over FUNCTIONALITY; retiming is keyboard-operable on the sibling marker, which the axis cites before ruling the SC failed anyway — the same internal contradiction reproduced verbatim at challenge-D:61. Survives at MAJOR under 4.1.2 + 2.5.8 = banked D-3/M8; the true 2.1.1 residue is banked M3 (the MARKER).
6. **B-2's prescribed cure "declaring `aria-selected` would have bought the forced-colors treatment for free"** — REFUTED as structurally unsound (RR-α, adopted): `aria-selected` is valid only on option/tab/row/gridcell/columnheader/rowheader/treeitem; on this role-less div it is invalid ARIA that AT ignores — while the CSS attribute selector WOULD match, which is precisely the objection: the cure ships invalid ARIA to harvest a stylesheet. Real role or the primitive.
7. **D·M-6 · "WCAG 2.2 SC 2.4.11 Focus Appearance (AA) requires a 2px-perimeter-area indicator"** — REFUTED: 2.4.11 is Focus Not Obscured (Minimum), AA — an occlusion criterion; Focus Appearance is **2.4.13, AAA**. Wrong criterion AND an AAA requirement promoted to AA. The 1px-ring observation survives only as an AAA-grade note riding the C-7/D·M-5 cure; the contrast half passes.
8. **D·M-5's "WCAG 2.4.7 (A)"** — REFUTED in the letter: 2.4.7 Focus Visible is Level **AA**. Mechanism confirmed (roster); recorded because these documents present as provenance-exact and level drives triage.
9. **D·M-3's second branch** ("if the `@utility` out-orders `font-semibold`… no visual signal at all; both branches are bad") — REFUTED at the shipped bundle, re-executed and refined by this seat (ruled table): the LAST `.font-semibold` (218810) out-orders the last `.text-admin-label` (217365) in the same layer → the weight residue is present, not conditional. Jul-16 witness caveat on this cell only.
10. **D §0's PRM narrowing + SUP-4's "contributes zero surface to the demo's PRM enforcement"** — REASON REFUTED, conclusion intact: `utilities/a11y-overrides.css` opens with a UNIVERSAL `*:not([data-allow-motion])` PRM blanket (read verbatim by this seat) that does reach this component; the banked D-12 row says so too. No local guard is owed — for the right reason now.
11. **m-1's "select() is plausibly a complete no-op in Blink"** — REFUTED as a non-sequitur (RR-α, adopted): `select()` and the `setSelectionRange` family are separate spec hooks with separate applicability; nothing was cited transferring the `type=number` restriction. UNPROVEN-NEEDS-LIVE; the design half survives (roster).
12. **C-7's "two-element population / odd one out"** — REFUTED by my own census (ruled table): a documented LAW + 4 `.focus-ring` consumers; the finding gets STRONGER and the cure order inverts.
13. **C-9's truncation mechanism** ("centre 24px… removing `overflow-visible` silently truncates every caret label") — CELLS REFUTED (padding box; ~1px; the input is the dependent; ancestors serve the ticks), INFO survives corrected (roster).
14. **D·M-2's arithmetic cells** — border-box error, every row shifts ~1px in the direction that makes it WORSE; row folds to banked D-17 which carries the SAME uncorrected figures — both tables re-derive before cure (D-19).
15. **The three-axis "contradiction of the corpus" novelty claim** (D §0 / L C-1 / C §4 on the ScrubberTimeline/geometry mapping) — REFUTED as net-new: banked **C-15** already states and rules the split verdict, called "the corpus's best row" there. The substance is right; three documents independently re-derived a settled adjudication. Cite C-15.
16. **C-4's locale sub-claim** ("a comma-decimal locale types `29,5` and `parseFloat` returns `29`") — REFUTED (RR-β, adopted): a number input's `value` IDL is sanitized to the standard `.`-decimal serialization or `""`; `:60`'s `parseFloat` is locale-safe as written. The headline shadow survives untouched.
17. **C's S★6 superlative** ("F-1 immunity — its module graph is fully reproducible on a clean checkout") — KILLED by the same baseline error inverted (RR-β, re-verified mine): HEAD declares `@mkbabb/value.js ^3.1.0` while every claim was measured against installed 4.0.0 — a clean `npm ci` at the declared tree reproduces a DIFFERENT graph. True only at refs the challenge never names.

**Citation corrections (conclusions unaffected; recorded for provenance-exactness)**: C-1 cites keyframeSelector.ts:8 for the `*100` (it is :9) and useTimelineOps.ts:60-61 (:59-60); axis-D cites `:31` for `const p` (worktree :28); RR-α's "registry sweep → zero hits" for `--input-on-glass` is false by one (banked R-24); RR-α's fold of C-4/M-9 into banked C-15 is a mis-fold (ruled table); RR-α's K-6 byte offsets were single-occurrence measurements; the KC-1 "format.ts:131 selectorText" cure cell is corrected at roster row 7.

## Superlatives — provenance preserved (L-18 runs both ways)

- **S-1 · the `if (!isEditing.value) return;` re-entrancy guard (:59)** — exactly right and closed by construction: Escape clears the flag synchronously BEFORE the `v-else` teardown, so the trailing blur commits nothing; Enter-then-blur is idempotent. Named independently by all three axes and both readers; re-verified here by whole-file read. "Escape cancels but the blur commits anyway" is the canonical bug of this shape. **BOUNDED by MISS-α4**: it carries no comment — also the line a cleanup wave deletes.
- **S-2 · the empty-input trap is closed (:60-63)**: `parseFloat(… ?? "")` behind `!isNaN` — an invalid number input reports `value === ""` and the naive `Number("")` → 0 would retime to 0%; clear-and-blur is a no-op here. Deliberate, and rarer than it should be.
- **S-3 · `clamp` from the published subpath at the trust boundary** — one of five identical imports across the cluster (banked C-13's family); and challenge-C §2's protective non-finding is the corpus's single most valuable sentence: `min`/`max` constrain the stepper, NOT typed text — `:62` is where untrusted DOM text becomes a model number and must survive any triple-clamp dedupe. A non-finding written to protect live code.
- **S-4 · zero lifecycle surface, verified by exhaustion**: no listener/timer/rAF/observer/watch/hooks in 70 lines; the one async hop optional-chains, so a keyframe deleted mid-edit no-ops. The parent runs pointer capture + wheel + three touch handlers; the grandparent owns a rAF clock and is banked (L-20) for the OPPOSITE. The leaf holds the discipline the container lost.
- **S-5 · the `percent`/`position` split** — model datum vs pre-projected screen coordinate (useZoomPan-owned); the caret imports no zoom state and survives any zoom-model change; taking four scalars instead of a `TimelineKeyframe` is also what keeps `value.js/css` out of this module's graph. Looks redundant on paper; is the correct cut.
- **S-6 · correct keying under a re-sorting parent** (`:key="'caret-' + kf.id"`, TimelineTrack :99): a percent-driven re-sort patches by key, so an OPEN editor is never remounted from under the user — the exact hazard banked as kf-KeyframeCardList KC-8/KC-9 one directory away, opposite outcome.
- **S-7 · corpus method** — axis-D's §6 table of six self-killed claims, every row of every axis carrying an executable falsifier, and the fact that L-1 and C-1 both die on the precise probe their OWN falsifier names (`git show 8281638c:package.json`; C-1's step 1). A corpus that hands the re-reader the instrument that refutes it is doing the job — even where the verdict then goes against it.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (route with KF.W9)

1. **Whether `select()` focuses the unfocused `type=number` input in the shipped engines** — the component's sharpest probe; decides m-1/L-10's promotion AND MISS-α2's reachability. Probe: click a caret, read `document.activeElement`.
2. **Playhead-jump magnitude on caret press** — rides the banked M2 row; the emitted percent is the POINTER's, coinciding with the keyframe's only on a dead-center hit.
3. **0%/100% clip extent + legibility of a half-cut "100%"** — already the bank's SS-13 residue #4; not re-filed.
4. **The expanded+selected diamond vertex on the caret digits** — banked D-17's residue #3; both arithmetic tables (bank + axis) re-derive with the padding-box correction first.
5. **D·M-7's composited border/fill ratios** over the live quiet-Card plate and the expanded `bg-transparent` plate.
6. **Rendered forced-colors result** with the input focused (every deduction input source-verified).
7. **Whether "100" overflows `w-10`** at 10px + caps tracking minus the native spinner — probe `scrollWidth > clientWidth`.
8. **Whether `--glass-backdrop: light` resolves on the timeline Card** — the deep arm of D·M-3 (all three caret states → one color) and the falsifier of axis-D SUP-3's light-ladder contrast row.
9. **ctrl/⌘+wheel zoom under the open editor** — L-11's felt half; the mechanism is source-proven.
10. **iOS focus-zoom fire/restore on the caret input** — rides banked D-9's fold; preconditions source-proven.
11. **Whether removing the focused input moves focus to `<body>` in every engine** — MISS-α2's live half; the nothing-refocuses half stands statically.
12. **Witness refresh** — the K-6 ordering cell (killed #9) leans on the Jul-16 bundle; re-run `build:gh-pages` before trusting any byte-offset receipt; all token cells in this record were re-derived against the installed dist and are exempt.

## DISSENT — preserved, not ruled away

1. **RR-α's confirmation of C-1** — overruled, and the failure mode is worth naming: the reader RE-RAN the row's measurement (which reproduces perfectly) but never its causal falsifier, then graded the row "best-argued in this corpus" while its own K-BASE finding had already broken the row's anchor. Verification of the measurable half is not verification of the claim. RR-α's substantive position is preserved: the caret remains the chief INTERACTIVE integer feeder, and if a wave ever establishes an integers-only invariant at the UI, the serializer row (MISS-β2) inherits the full user-visible harm C-1 described.
2. **RR-β's endorsement of C-7's two-element census** — a grep-pattern census error in an otherwise instrument-heavy return; the same return independently corrected RR-α's L-1 and C-1. Recorded per the suspicious-agreement law: pattern-scoped greps inherit the axis's blind spot exactly where reader and axis share the pattern.
3. **RR-α's mis-fold of C-4/M-9 into banked C-15** — the identity guard misused in the INVERTING direction: folding a new defect into the wrong banked row silently discharges it. The guard exists to stop re-booking, not to stop booking. Caught by RR-β's NOT-banked cell + this seat's read of C-15.
4. **The L-5 rung disagreement (α MINOR / β MAJOR)** — mooted by the fold; both positions preserved. If the caret input is ever cured separately from the label Input, the site inherits D-9's MAJOR at the bank.
5. **Reader convergence as a risk surface** — the two readers agreed on nearly every fold and both BLOCKER demotions. The four most load-bearing agreed cells (the 8-integer set, the HEAD glass declaration, the forced-colors list, the light-arm identity) were re-measured by this seat with its own instruments — all held.
6. **Line drift** — zero drift observed at every caret-local site re-opened (the four files are byte-identical across all three refs); composable-tier anchors are origin/worktree lines and are labeled so throughout.

## Closing verdict

**ADJUDICATED — RED for the caret's seams, GREEN-hearted at the leaf: 25 rows stand (0 BLOCKER · 7 MAJOR · 13 MINOR · 5 INFO) with 11 whole-row folds + 4 partial arms discharged by reference to banked kf-KeyframeTimeline (M2/D-3/M8/M1/D-17/D-28/D-9/N-11/N-6/D-18/C-15), kf-CSSCodeEditor KF-CE-16, kf-CSSPasteDialog R-24, and census F-1 — zero re-bookings; 17 claims killed with re-executed refutations, among them one reader-vs-reader overrule settled by this seat's own execution (C-1: the export never contained "29%", the no-op commit is bit-identical, the arrow path breaks sole-writership) and one BLOCKER killed at every committed baseline (L-1); 10 missed rows adjudicated in, headed by the composition that promotes the rounding well to artifact data loss (MISS-β1) and the triple-private serializer that owns the float defect C-1 mis-attributed (MISS-β2, correcting banked KC-1's cure citation). The anchor truth is booked as a MAJOR process row: the audited tree is a HYBRID of `8281638c` and `81a56990`, the caret's four files anchor-safe, the composable tier origin-side — every consuming wave states its ref. Routings: the interaction/model rows to KF.W7 (the census names this cluster); the glass/token/a11y-name/register rows to KF.W6; naming+structure+serializer-publication to KF.W8; the type-gate to KF.W4 by fold; 12 residue rows to KF.W9/SS-13, headed by the single probe that decides two rows at once (`select()` focus). Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.
