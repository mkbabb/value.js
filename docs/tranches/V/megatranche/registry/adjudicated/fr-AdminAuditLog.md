claude-fable-5

# fr-AdminAuditLog — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject repo**: fourier-analysis HEAD `cd26c65` (= the F.W0 scope certified byte-identical by intake row **R4-9**, ADOPT-AS-FACT — relied upon, not re-verified) · **Corpus**: 3 challenge axes (`challenge-D-design.md` 27 defects / `challenge-L-library.md` 20 / `challenge-C-consumption.md` 17) against `web/src/components/visualization/gallery/AdminAuditLog.vue` (190 lines), re-read by two hostile re-readers whose returns this seat adjudicated.
**Workers adjudicated**: worker-1 (design-and-usage re-read) · worker-2 (library-and-code re-read). Both were attacked before either was adopted; every verdict-level disagreement was re-derived against the live tree with this seat's own instruments.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony)**: whole-file reads of AdminAuditLog.vue (190) and useOffsetPagination.ts (83); api.ts:45-263 + :640-673 (registry, coreFetch whole, adminFetch, listAuditLog); routers/admin.py:615-653; services/janitor.py:45-99; **services/database.py:135-152 (the index decider)**; stores/auth.ts:14-99 + gallery.ts:37-122 + repo grep `adminLogout` (3 hits, 1 call site); GalleryView.vue:30-34/:222-230/:373-390 + `tabpanel` grep → 0; AdminUserList.vue:70-74/:246-249/:277-281/:306-310/:352-381/:428-457; AdminFlaggedPanel.vue:50-68/:151-166; style.css:38-145 (root scale, tab-panel guard, `.cartoon-card` @utility, `--viz-amber` AA comment, `:focus-visible` block); installed glass-ui **4.0.0** (80 export keys; `./forms`→`dist/forms.d.ts` line 1 `export * from "./components/ui/input"`; `./badge` present; **`dist/styles/typography/semantic.css:213-220 `@utility text-admin-label`** + `scale.css:86 --type-admin-label: 0.625rem`; button d.ts variant/size unions verbatim; `dist/dark.js:17` auto→prefers-color-scheme; `offsets-sizing.css` `--control-floor`/`--ui-coarse-scale:1.5`/touch-target clamp + button cva `h-(--control-h-sm)` rungs + `cn` `["height",/^h-/]` bucket); producer glass-ui **7.0.0** (67 export keys; Button.vue:15-31 `emphasis`/`tone`/`iconOnly`, `ButtonSize = Extract<Size,"xs"|"sm"|"md"|"lg">`; axes.ts SIZES `:30`, TONES `:52`; `./forms` and `./badge` survive, `./pagination` does not); measured counts re-run: `abortInflight` **5 hits = export + 4 call sites (workspace.ts:90,140,202,270)**; zero-rule `<style scoped>` sweep → exactly **4** files, GalleryInfiniteGrid.vue has **no** style block; `-300` tone census → **8 sites, 5 in subject**; Button blast radius **37 files / 38 `size="icon"` / 124 `variant="`**; tailwind `^4.3.1` + `@import "tailwindcss"` (preflight `border: 0 solid` ⇒ bare `border` = `currentColor`); `autocapitalize|enterkeyhint|type="search"` repo-wide → 0; e2e `audit` grep → 4 prose-only hits; census: `emphasis|ButtonEmphasis|size="icon"` over `formation/fourier/` → **0**; lane-frontend.md:107/:213/:472-478 and CENSUS-2026-08-03.md:362 read.

## Scoreboard

**48 adjudicated rows (3 BLOCKER · 20 MAJOR · 20 MINOR · 5 INFO) · 13 killed claims · 15 missed rows adopted (11 as new rows, 4 as cures/mechanisms inside existing rows) · 8 superlatives · 9-item SS-13 residue.**

One-line verdict: the D axis's *un-remediated twin* reading is SUSTAINED — the file shares composable, skeleton, spinner, empty state and footer with `AdminUserList.vue` and carries none of the sibling's a11y pass; on top sit three blockers of its own class (a failed fetch rendered as an affirmative empty ledger; a 37-file producer break the census does not book; a default-theme contrast failure on 100 % of semantic branches). The single most repair-shaping fact in the corpus was found by neither original axis: **`database.py:146`'s compound index `[("action",1),("timestamp",-1)]` makes the regex-action cure both axes prescribed a performance regression wearing a correctness fix's name.**

**Status verbs**: every disposition below is **ADJUDICATED**, not VERIFIED — the X·F specs are still forming; routings are agglomeration input (M-25). Line anchors are HEAD `cd26c65`; re-resolve before executing any cure.

## Ruled disagreements (the seat's actual work)

| Locus | worker-1 | worker-2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-B2 severity** | BLOCKER sustained | demote to MAJOR ("BLOCKER overstates against the D-1 rendering blocker") | **BLOCKER SUSTAINED — worker-1** | The `-300` ink IS the action string itself (`:133-138`), not a decoration: at 1.36–1.81:1 over light `--card` on 10.4 px text, the row's entire semantic payload — *what was done* — is functionally unreadable for every light-OS operator on first paint (`dist/dark.js:17` seeds auto→OS). That is loss of the primary datum, not degradation. Worker-1's falsifier kill sharpens it: compositing red-500/10 over light `--card` moves L by 0.13 (not "<0.02"), so the true on-plate figure is **1.56:1**, *worse* than the 1.80 the axis reported. Worker-2's demotion preserved as dissent; both workers' arithmetic reproduces to ±0.02 and the cure ships at the current pin (Badge). |
| **D-B3 severity** | BLOCKER sustained | demote to MAJOR ("breaches no numbered SC — D itself concedes") | **RULED DOWN to MAJOR — worker-2** | The invisible chassis (1.01–1.07:1 both arms, my token walk: `--muted`=`--neutral-1` via color-radius.css:84, ramp values light-dark.css:84/:87) loses the authored composition but no content, no legibility, no SC. And the sharper reframe is worker-1's own missed M-2: the file hand-rolls the house `.cartoon-card` recipe (style.css:107-112, `border-color: var(--border); background: var(--card)`) with the wrong token — `AdminUserList.vue:361` uses `cartoon-card` on the *identical* admin row surface. A one-class cure for a composition defect is MAJOR, not BLOCKER. D's comparator row dies separately (K-3). |
| **L-7 severity + evidence** | MAJOR ("the ONLY delete is :64…") | MINOR (mechanism survives; axis's grep claim false — 4 real `abortInflight` call sites) | **MINOR — worker-2, with worker-2's own kill corrected** | My grep: 5 hits — export + workspace.ts:90/140/202/270. The axis's headline "never called with `abortInflight` by any consumer of it" is false as to the registry and carried the MAJOR; the mechanism (no `finally { inflight.delete }` in coreFetch :161-203 — my whole-function read) is true and bounded (settled controllers, per-distinct-key growth, admin-typed amplifier). **But worker-2's kill misquotes the axis by truncation**: the axis's parenthetical says "no call site *in this component*" (challenge-L-library.md:210), which is TRUE — worker-2 dropped the qualifier to strengthen its kill. The kill survives on the headline sentence alone; the truncation is recorded against worker-2's method. |
| **L-10 consequence** | sub-claim KILLED (bare `border` = `currentColor` ⇒ the raw buttons carry the file's only *visible* border, ≈5:1 via inherited `text-muted-foreground`) | CONFIRMED incl. the axis's "resolves to the Tailwind default" phrasing, undecided which default | **worker-1's kill ADOPTED** | Premise verified by this seat: web pins tailwind `^4.3.1` with `@import "tailwindcss"` (preflight `border: 0 solid` ⇒ border-color `currentColor`); wrapper `:166` sets `text-muted-foreground` (`--neutral-5` light `hsl(30 22% 40%)` ≈5:1 over `--card`). The inconsistency finding survives at MINOR inside AA-11; the implication that the raw controls render *worse* than the tokenised ones is backwards and must not steer the repair. Same mechanism kills D-B3's comparator row: `AdminUserList.vue:248` is a bare `border` width utility, not a `--border` instance (my read). |
| **D-B4 error count** | 3 breaking props at this file (`variant`×2, `size="icon"`×1; `size="sm"` survives — `ButtonSize` includes it) | "Both sites become unknown props" (repeats D's error inside its own C-S-3 kill) | **worker-1** | My read of producer Button.vue:15-31: `ButtonSize = Extract<Size,"xs"|"sm"|"md"|"lg">` — `sm` is a member. `:103` breaks on `variant` only; `:106` breaks on `variant` + `size="icon"`. The C-S-3 kill itself survives (3 lines ≠ 1 line; 1 of 37 files); its evidence cell is corrected. |
| **Destructure arity** | 10 of 13 (lists them) | "11 of the composable's 13 returns" | **10** — my count of `:15-26`: items,total,page,pageCount,loading,hasNext,hasPrev,loadPage,nextPage,prevPage. Worker-2's miscount recorded in the provenance cluster (K-13). |

## Adjudicated defect roster

Merged identities; original axis ids for life. Routing targets are the census lane taxonomy (F.W0 substrate · F.W1 tri-package uplift · F.W3/W4 shadow retirement + frontend audit · F.W5–W8 CRUD/provenance union · F.W9/W10), else NO-WAVE-OWNER for SS-3/SS-4 spec authoring.

### BLOCKER

- **AA-1 · D-B1 / L-1 / C·D-1 — ADJUDICATED BLOCKER.** Every fetch failure renders as an affirmative statement about the ledger. Mechanism (my whole-file reads): `useOffsetPagination.ts:31` declares `error`, `:47-48` catch writes ONLY `error` (never `items`/`total`), `:75` returns it; `:39` assigns `page.value` BEFORE the await at `:44`. Subject `:15-26` destructures 10 of 13 members — `error` absent; zero `error`/`toast` references file-wide. **C's arm is primary and the only fully correct rendering**: first-load failure → `:151-156` "No audit entries"; every LATER failure → the previous page's rows under a freshly advanced page number (`:175`). House idiom one directory-mate away (`AdminFlaggedPanel.vue:52,67` toast-on-load-failure). D's `Bearer null → 401 → ApiProblem` sub-arm is SEVERED (K-10): api.ts:127-129 throws a developer string *before* any request. Structural cure (worker-2's mechanism-miss, adopted): a request-generation token captured before `:39`, committing `page`/`items`/`total` together on the winning response — one cure for AA-1 and AA-7, in the shared composable. **→ F.W4** (composable + component), with the **F.W1 ordering constraint upheld**: the toast cure routes through `useToast.ts:3,9` whose `ToastVariant` is the census's booked [P1] 7.0.0 removal — cure `useToast` first or use an inline banner (D §6, verified against lane-frontend.md:477).
- **AA-2 · D-B4 — ADJUDICATED BLOCKER (census-extension row).** glass-ui 7.0.0 renames Button `variant`→`emphasis` and deletes `size="icon"` (producer Button.vue:15-31, axes.ts:30 — my reads); blast radius re-measured by this seat: **37 files / 38 / 124**, vs the census's entire booked break surface of ~15 files (lane-frontend.md:472-478); `emphasis|ButtonEmphasis|size="icon"` over `formation/fourier/` → **0 hits** (my grep). Corrections bound in: **3** breaking props at this file, not 4; 7.0.0 export map = **67** keys, not 76; `./button` survives so only props break. **Identity guard**: the producer-side rename is already known program law (value.js W44 Glass-7 adoption; several banked value.js rows use `emphasis`) — the NEW booking is the *fourier census gap*, not a re-derivation of the producer break. **→ F.W1** — must land in the break-surface table before F.W1 commits its atomic cure budget.
- **AA-3 · D-B2 — ADJUDICATED BLOCKER (ruled, see table).** All five `actionTone` inks fail WCAG 1.4.3 in the default light arm: 1.36–1.81:1 band (both workers reproduce to ±0.02; sharpest corrected figure red **1.56:1 on its actual plate**), on `text-[0.65rem]` = 10.4 px desktop — the ink is the action string itself. Dark arm passes (7.6–10.1:1): authored dark-only. Cure at the CURRENT pin: installed 4.0.0 `Badge` (`variant: …|destructive|success|warning|info`); the violet `batch` arm has no home in 7.0.0's 5-member TONES (axes.ts:52) → **ADOPTION-ASK rides the standing glass-ui BH relay** (owner edict), or `variant="outline"` + the `GalleryCard.vue:115-125` `.basis-tint` recipe meanwhile. **→ F.W4** (Badge adoption deletes `:67-81`), relay line for the batch tone.

### MAJOR

- **AA-4 · D-B3 (+ worker-1 M-2 as cure) — MAJOR (ruled down).** The whole chassis is drawn with the fill token `--muted` as a border at 30–40 % alpha → 1.01–1.07:1 in BOTH arms (sites `:87,:93,:100,:128`); provenance corrected: aliases live in `color-radius.css:84/:95`, ramp values in `light-dark.css:84/:87` (my reads). The cure is NOT `border-border` (still under the 3:1 non-text floor at 1.90:1): it is the house `@utility cartoon-card` (style.css:107-112) that `AdminUserList.vue:361` already applies to the identical row surface — one class, plate + border + shadow + hover restored. **→ F.W4.**
- **AA-5 · L-3 — MAJOR.** `actionTone` is blind to the entire `janitor:` namespace: the collection's SECOND writer (`janitor.py:59-99` inserts directly into `db.admin_audit`; no `log_audit` call) emits nine `janitor:*` actions (spot-verified `:133` hard_delete_visualizations, `:285` prune_audit) — none matches any arm of `:67-81`, so all nine (including the audit log pruning its own history) render in the benign sky default. Kills C·S-2/K-3 (K-6). Cure = a shared action taxonomy at the seam (also cures AA-24), **→ F.W5–W8** (cross-tier), display arm **→ F.W4**.
- **AA-6 · L-4 / C·D-2 — MAJOR, cure re-shaped by AA-23.** Server `admin.py:631-632` = bare equality on `action` (corrected lines, my read); target clause `:633-634` is the honest regex. Placeholder `:92` advertises prefixes; its own example `set_tier` matches zero rows forever (writers emit `set_tier:{tier}` etc. — verified `:185,:444,:497`). Composed with AA-1, "no matches"/"unreachable" are pixel-identical. **The regex-action cure BOTH axes prescribed is REJECTED** (see AA-23). Sound cures in order: (a) honest placeholder ("action (exact…)"), (b) `$in` over a generated taxonomy (cures AA-5/AA-24 at the same seam), (c) anchored case-SENSITIVE `^prefix` only. **→ F.W5** (contract co-sign) + placeholder honesty **→ F.W4**.
- **AA-7 · D-M3 / L-5 — MAJOR.** No applied-filter snapshot exists: `fetchFn` dereferences the live `v-model` refs at request time (`:32-33`), `applyFilters` (`:42-44`) snapshots nothing, `hasFilters` (`:52`) reads drafts — so Next ships unapplied filter text while `:39` already advanced `page` against the stale `pageCount` (worker-2's worked collapse: page=2 of a 1-page result, footer hidden by `:165`, empty state fires). Root cause is the SAME state-machine ordering as AA-1; one generation-token cure. Worker-2's correction adopted: "stranded" overstates — Apply/Clear recover via `loadPage(1)`. Sibling avoids by construction (`AdminUserList.vue:71-74` debounced watch). **→ F.W4** (composable).
- **AA-8 · L-6 / C·D-5 / C·D-6 — MAJOR.** The abort key embeds the full query path (api.ts:250 — C's `:229` citation corrected): identical keys abort-as-error (composable `:47-48` has no `isAbortError` — the predicate ships at api.ts:69 and is consumed at ~16 other sites, my grep 18 hits incl. definition) and its `finally` `:49-51` clears `loading` while the replacement flies → mid-load empty-state flash; differing keys never cancel → last-resolver-wins with `page` pre-advanced. Repair ordering (C's, upheld): the abort-discrimination fix must land BEFORE the AA-1 error surface, or every double-Apply toasts a bogus abort error. **→ F.W4** (composable + api.ts), shared with AdminUserList.
- **AA-9 · D-M1 (+ worker-1 M-3 as cure) — MAJOR.** Four unlabeled columns; wrapper `:124` is flex so each row's `grid-cols-[auto_auto_1fr_auto]` (`:128`) is its own formatting context — column edges step row to row; zero `role`/`aria` file-wide (my grep). The cheap house cure no axis named: BOTH siblings label the same shape with three attributes (`role="list"`/`aria-label` + `role="listitem"` — AdminUserList.vue:352-361, AdminFlaggedPanel.vue:156-165, my reads). `DataTable` (installed pin) remains the fuller seat; the 3-attribute cure does not compete with it. **→ F.W4.**
- **AA-10 · D-M2 / L-13 — MAJOR.** The log never answers *who*; the identity-gesturing column truncates the backend's own sentinel: `janitor.py:56` `_JANITOR_ACTOR = "system:janitor"` written into `ip_hash` at `:95`; `:146`'s unconditional `.slice(0,10)` renders `system:jan` — convicted by the producer's own comment (`:50-55`, "self-documenting in the viewer"). Display cure (sentinel-aware branch + legend) **→ F.W4**; the actor-field contract question **→ F.W5–W8**.
- **AA-11 · D-M4 / L-9 / C·D-10 / L-10-as-rescoped — MAJOR.** The pagination footer is a degraded copy of the sibling's: `<div>`+raw `<button>`s+plain `<span>` (`:164-184`) vs `<nav aria-label>` + glass Buttons + `aria-label`s + `aria-live` counter + the *recorded carry comment* (`AdminUserList.vue:428-457`, every cell re-verified by this seat). L-10's split-brain observation folds in at the corrected consequence (see ruled table). Corroborated: `./pagination` absent from BOTH export maps — the named carry has not landed in three majors; **F.W1 must not plan the pagination cure around it**. The raw buttons' missing `type` attr (worker-2 missed, INFO AA-48) discharges free with this cure. **→ F.W4** (port the sibling's footer; extraction wants AA-37's directory).
- **AA-12 · D-M5 — MAJOR.** The loading block is the siblings' block with `role="status"`/`aria-live`/`sr-only` deleted — 2-of-2 siblings carry the full treatment on a byte-identical spinner (AdminUserList.vue:277-280, AdminFlaggedPanel.vue:151-154, my reads); subject `:119-121` + zero a11y attrs file-wide. **→ F.W4.**
- **AA-13 · D-M6 — MAJOR.** Both filter inputs are labelled by `placeholder` alone (`:89-102`); the sibling's carries `aria-label="Search users"` (`:247`). The placeholder is also the field's only syntax documentation, destroyed on first keystroke — and per AA-6 the action placeholder's syntax claim is wrong anyway. **→ F.W4.**
- **AA-14 · D-M7 — MAJOR.** `focus:outline-none` deletes the indicator and replaces it with a ≈2.4:1 hairline on a 1.01:1 resting edge, keyed `focus:` not `focus-visible:` (`:93,:100`); house law verified: style.css:138-145 `:focus-visible` ring scoped to four link classes (none here), sibling uses `focus:ring-1 focus:ring-ring` (`:248`); the scoped block `:188-190` restores nothing. **→ F.W4.**
- **AA-15 · D-M8 (+ worker-1 M-1 as cure) — MAJOR.** `text-[0.65rem]` is an off-scale magic 10.4 px desktop / 11.7 px mobile (root inversion verified, style.css:40-50) on the two least-legible columns (`:134,:143`), compounding AA-3. The axis's "no rung exists" falsifier is DEAD (K-1): the installed pin ships **`@utility text-admin-label`** (semantic.css:213-220, `--type-admin-label: 0.625rem`, mono+uppercase+caps-tracking+500), live at 7 sites in 4 files incl. both siblings — the one-line cure, no uplift. **→ F.W4.**
- **AA-16 · D-M9 — MAJOR.** Zero responsive modifiers (`grep` → 0); the mechanism is static-certain (the `1fr` track collapses first via `truncate`'s overflow-hidden, then the space-bearing timestamp wraps against an unbreakable mono pill under `items-center`); the exact breakpoint is SS-13 residue. **→ F.W4.**
- **AA-17 · D-M10 / L-20 — MAJOR.** `v-if="loading"`/`v-else` (`:119/:124`) unmounts ~25 rows per page turn into a ≈56 px spinner with no `min-h`; the composable's single undifferentiated `loading` flag forecloses keep-previous-while-loading. `Skeleton` is a root re-export at the installed pin. Family defect (sibling identical). Magnitude SS-13. **→ F.W4.**
- **AA-18 · L-8 — MAJOR.** No teardown of any kind: bare `loadPage(1)` at setup (`:40`), `vue` import is `{ ref, computed }` only, composable registers no scope disposal; the unmount is real (`GalleryView.vue:385` `v-if` gate, my read). D-i3 folds in here. Cure belongs in the composable (`onScopeDispose` abort), which also feeds AA-27's reap. **→ F.W4.**
- **AA-19 · C·D-3 — MAJOR.** The action chip hand-rolls `glass-ui/badge`, present at the PINNED 4.0.0 (`./badge` in the 80-key map, my check) and imported twice in the same folder (`GalleryCard.vue:4` precedent with the documented `.basis-tint` recipe); survives at 7.0.0. Genuine gap: violet/batch has no TONES home (→ AA-3's relay line). C's census amendment upheld: the file-granular shadow table cannot see **fragment shadows** (this chip, the spinner, the raw footer) — carry the amendment. **→ F.W4** (Badge), census note → F.W3/W4.
- **AA-20 · C·D-4 / L-14 — MAJOR.** Five single-theme raw palette triples in an otherwise fully tokenised file; density re-measured by this seat: **8 repo `-300` tone sites, 5 owned here** (sole owner of violet/sky/emerald). The repo's own AA standard is written at style.css:113-118. Dies with AA-3's Badge cure. **C's F.W2 contradiction is upheld and carried**: the census scopes hand-rolled colour to `colors.ts` alone; `actionTone` is a second hand-rolled colour authority — **F.W2 must widen or exclude with reasons**. **→ F.W4** (cure) + **F.W2** (charter note).
- **AA-21 · worker-1 M-4 (missed by all three axes) — MAJOR.** iOS sentence-autocapitalisation silently defeats the exact-match action filter: bare `type="text"` inputs with zero `autocapitalize|autocorrect|spellcheck|inputmode|enterkeyhint` and no `<form>` (repo-wide 0, my grep); server equality carries no `$options:"i"` (`:631-632`) while the immune target clause does (`:633-634`). `delete` → `Delete` → zero rows → "No audit entries" + widening counsel: a silent false negative on a compliance surface, composing AA-6 and AA-33. UA half is SS-13. **→ F.W4** (attributes) + placeholder co-sign rides AA-6's F.W5 line.
- **AA-22 · worker-1 M-5 (missed by all three axes) — MAJOR.** `class="h-7"`/`h-7 w-7` on both Button sites (`:103,:106-110`) mechanically deletes glass-ui's WCAG-2.5.5 coarse-pointer clamp: the cva emits token rungs (`h-(--control-h-sm)`, my dist grep) that are `max(scaled, --control-floor)` clamps with the coarse block lifting scale to 1.5 and floor to the documented 44 px touch target (offsets-sizing.css:129-151, my read); `cn`'s `["height",/^h-/]` bucket is last-write-wins, so `h-7` pins 28 px on every pointer. Family defect (sibling `h-7 w-7` ×2, `h-6 w-6` ×3); 100 % of this file's Button sites. 2.5.8 AA still clears; the row is a producer-contract negation + 2.5.5 loss. **→ F.W4.**
- **AA-23 · worker-2 missed (read by neither axis) — MAJOR, repair-shaping.** `services/database.py:145-146` creates exactly two indexes on `admin_audit`: `[("timestamp",-1)]` and **`[("action",1),("timestamp",-1)]`** — the exact-match action filter is a deliberate index-driven design. A case-insensitive `$regex` (the cure C·D-2's "Note for F.W5" recommends and L-4's falsifier names) cannot use that index; shipping it converts an indexed equality into a collection scan on the one monotonically growing collection. **Binds AA-6's cure set.** **→ F.W5** (the 45/30/13 join must carry this constraint).

### MINOR

- **AA-24 · C·D-8.** `batch_users:delete` — the vocabulary's most destructive operation (verified writer `admin.py:497`) — falls past `:68`'s `startsWith("delete")` into violet `:77`, identical to `unsuspend`: severity inversion within the map. Dies with AA-3/AA-5's taxonomy. → F.W4.
- **AA-25 · L-12 / C·D-9.** UTC-sourced timestamps rendered host-local with no `timeZone`/`timeZoneName` and no ISO recovery (`:57-64`; titles go to target/hash only). C's K-1 kill ADOPTED as load-bearing: `database.py` constructs `tz_aware=True`, so there is NO parse shift — presentational only. → F.W4 (add `timeZoneName:"short"` or pin UTC).
- **AA-26 · L-16 / D-m5 / C·D-11.** `{{ total }} total` gated behind `v-if="pageCount > 1"` (`:165,:183`): every well-targeted (≤25-row) and every zero-row query hides the one number distinguishing complete from truncated. Inherited from the sibling; cure shared with AA-11. → F.W4.
- **AA-27 · L-7 (ruled MINOR, see table).** No completion reap in `coreFetch` (verified whole-function); growth per-distinct-key, amplified by the free-text target key; four explicit `abortInflight` reaps exist elsewhere. Cure: `finally { inflight.delete(key) }` guarded on controller identity + AA-18's unmount abort. → F.W4. Growth rate SS-13.
- **AA-28 · L-2-residual / C·D-7.** The `getAdminToken()!` BLOCKER is DEAD (K-5) — all three claimed null paths refuted against the stores by this seat (sole `adminLogout` call site is `gallery.ts:118` inside `deactivateAdmin`, which clears the mount gate synchronously; no storage listener; `adminMode` non-persisted). Residual: a real 11-site type-lie idiom — lane carry, → F.W4 (idiom row, not a component charge).
- **AA-29 · L-15 / D-i1 / C·D-16-corrected.** Rule-less `<style scoped>` stamps `data-v-*` on ~104 nodes/page for zero rules. My sweep: exactly **4** files at zero (C's five-file count dead, K-7 — GalleryInfiniteGrid has no style block at all). Bounded 4-file deletion. → F.W4.
- **AA-30 · L-17 / R5-7 — MINOR, discharge row.** 1 native `<div v-for>` loop, 0 component-callsite loops, ~104 rendered nodes invisible to a callsite-keyed deriver — **discharges CENSUS-2026-08-03.md:362's F.W4 instruction for this component** (fold by reference to R5-7, never re-booked as new law). Runtime corollary (25 unmemoised `toLocaleString` + tone dispatches per keystroke) certain in mechanism, SS-13 in magnitude. Row-component extraction cures both, and composes with AA-36. → F.W4.
- **AA-31 · C·D-12.** `after`/`before` implemented end-to-end (server `:623-624,:635-641`; transport `:655-656,:664-665` — my reads) and unreachable from any UI; sole caller passes 4 of 6 params. The cleanest R6-8 instance (fold by reference). → **F.W5** (45/30/13 join).
- **AA-32 · worker-2 missed.** The `target` filter — the honest one — drives an UNINDEXED case-insensitive regex plus a second full scan via `count_documents` per request (`:634,:644,:646-648`; only two indexes exist, AA-23). Bounded by the 90-day janitor prune; `re.escape` applied (no injection — positive recorded). → F.W5 (same join row as AA-23).
- **AA-33 · worker-1 M-6.** The empty-state HEADLINE never branches on filters (`:156` unconditional; only the `:157` subline gates on `hasFilters`): a zero-match query asserts "No audit entries" — a claim about the system of record — then counsels widening. Survives the AA-1 cure; half-blunts D's S-4. Cure: ternary on the applied-state signal AA-7 introduces. → F.W4.
- **AA-34 · worker-1 M-7.** The `v-if="hasFilters"` clear-X (`:106-115`) flips on character 1 inside a `flex-wrap` bar of two `flex-1 min-w-[10rem]` inputs — inserting a control mid-typing shrinks the field being typed in and can wrap a member. Reflow static-certain; wrap viewport SS-13. → F.W4.
- **AA-35 · worker-1 M-8.** No sticky filter bar and no internal scroll: the panel mounts inside GalleryView's `:220` scroller (my read) with no own `max-h`/`overflow`, so ~25 rows scroll the filter bar off-viewport while the sibling's control surface is `sticky top-2 z-10` (`:308`). Compounds AA-17. → F.W4.
- **AA-36 · worker-2 missed.** `items` is a deeply reactive `ref<T[]>` (`useOffsetPagination.ts:26`) for server-owned rows only ever wholesale-assigned (`:45,:64`) — `shallowRef` is a one-word cure paying at both consumers, and it strengthens AA-30's row extraction. → F.W4.
- **AA-37 · worker-2 missed.** Colocation: three admin console panels live under `components/visualization/gallery/`, a directory whose membership rule they do not satisfy — and four of the corpus's own cures (shared footer, shared loading block, a11y pass, row component) need a `components/admin/` that does not exist. Structural precondition. → F.W4.
- **AA-38 · worker-2 missed.** `defineAsyncComponent` bare (GalleryView.vue:31-33, my read): no `errorComponent`/`onError`, so a mid-deploy chunk-hash miss renders the audit tab as nothing — AA-1's epistemic failure one layer up, in exactly the long-lived-incident-tab scenario. Blunts the mount superlative to "exemplary except its error posture." → F.W4.
- **AA-39 · D-m1 / D-m2.** Decorative glyphs missing `aria-hidden` (`:88,:114,:155`; house applies it universally) and the clear button named by `title` alone (`:111`; house uses `aria-label`). Rides the AA-12/AA-13 a11y port. → F.W4.
- **AA-40 · D-m4.** Truncated target and sliced hash disclose on `title` hover only — mouse-privileged, unreachable by keyboard/touch (C·S-5's honest limit adopted). Cure trap upheld: `HoverCard` is REMOVED at 7.0.0 — route to Tooltip/copy affordance. → F.W4.
- **AA-41 · D-m6 + D-m7.** The only motion that runs here (`animate-spin` `:120`) is un-gated under reduced-motion, while the repo's sole authored reduce guard targets `[role="tabpanel"]` selectors that match nothing in GalleryView (grep → 0, my run) — the guard protects nothing on all four gallery tabs. EXTENDS census [P3] #9 (fold by reference, third site class). Not an SC failure (2.2.2 exemption stands). → F.W4.
- **AA-42 · D-m8.** Visual hierarchy inverts reading order: the operationally primary target sits at `text-foreground/80` below full ink while metadata outranks it. → F.W4 (rides the row redesign).
- **AA-43 · D-m9.** `disabled:opacity-30` → ≈1.49:1 (worker-1 recomputed exactly): exempt from 1.4.3 (kill upheld) but reads as *absent* rather than *unavailable*. Discharges with AA-11's glass Buttons. → F.W4.

### INFO

- **AA-44 · L-18.** Zero frontend tests; nine e2e specs never exercise the audit tab (4 prose-only `audit` hits, my grep); the backend pins its half (`test_janitor_audit.py`, verified present). One-sided seam — why AA-5/AA-6/AA-24 could open and stay open. → **F.W9/W10**.
- **AA-45 · L-19 / D-i2 / C·D-15.** The positional `:key` is forced by the contract (no identity on the wire — `AuditEntry` four fields; pydantic extra-ignore drops `_id`) and the hazard is unreachable (wholesale assignments only). Latent contract gap. **NO-WAVE-OWNER** (recorded for SS-3/SS-4).
- **AA-46 · C·D-13 / C·D-14.** Envelope `page`/`pages` discarded and re-derived client-side (agreeing today); page↔offset round-trip lossless. Redundancy records. **NO-WAVE-OWNER**.
- **AA-47 · C·D-17.** No runtime boundary validation, but pydantic guarantees the shape server-side (500, never a malformed 200); what survives is the *uneven* defensiveness (timestamp and target guarded; action and ip_hash not). **NO-WAVE-OWNER**.
- **AA-48 · worker-2 missed.** The two raw `<button>`s carry no `type` (default `submit`); inert today (no form ancestor), discharged free by AA-11's cure. Recorded as the same latent class as AA-45.

Census folds, not re-booked: the `lucide-vue-next` rename at `:8` is 1 of the census's 35 booked sites (D-i4); R4-9 relied upon; R5-7 discharged at AA-30; R6-8 instantiated at AA-31/AA-6; ToastVariant [P1] referenced at AA-1, not re-booked.

## Killed-claims register

- **K-1 · D-M8 falsifier** ("`0.65rem` matches no token rung — grepped style.css"): DEAD — the axis grepped the wrong surface. The installed pin ships `text-admin-label` (semantic.css:213-220; `--type-admin-label: 0.625rem` scale.css:86; my reads), consumed at 7 sites incl. both siblings. Defect stands (AA-15); the falsifier's absence-proof queried one file for a producer-owned token.
- **K-2 · D §6 item 7 + §7 kill row 2** ("`Input` unavoidable at both pins — do not file"): DEAD — `./forms` is in BOTH export maps and its d.ts line 1 re-exports `input` (my reads). C·K-2's disposition adopted for the correct reason: repo-wide non-adoption of `./forms` (grep → empty) = lane-census row, not a component charge. D's evidence must not be re-cited.
- **K-3 · D-B3 comparator row** (AdminUserList:248 as a `--border` instance at 1.90/2.6): DEAD twice — `:248` is a bare `border` width utility (currentColor under v4 preflight, premise verified: `^4.3.1` + `@import "tailwindcss"`); dark figure recomputes to ≈2.08, not 2.6. The row's substantive point survives on the light figure.
- **K-4 · D-B2 falsifier sub-claim** ("`bg-*-500/10` moves backdrop luminance <0.02"): DEAD — compositing moves L by 0.13; the true on-plate contrast is 1.56:1, LOWER than reported. The BLOCKER gets worse, not better.
- **K-5 · L-2 BLOCKER** (reachable null-token window): DEAD on all named mechanisms — sole `adminLogout` call site pairs with the mount-gate clear (`gallery.ts:117-121`); no `storage` listener (grep → 0); no expiry sweep; `adminMode` non-persisted; user `logout()` doesn't touch `adminToken` (all my reads). Residual type-lie at MINOR → AA-28. C·D-7's honest self-downgrade was the correct verdict all along.
- **K-6 · C·S-2 + C·K-3** ("`actionTone` exhaustive; the fallthrough is dead code"): DEAD — the enumeration method (`grep "log_audit("`) is structurally blind to the second writer that inlines `insert_one` (`janitor.py:92-98`, my read). All nine janitor actions land on the fallthrough. A superlative and a self-kill both withdrawn.
- **K-7 · C·D-16 count** ("5 gallery components… repo idiom"): DEAD — my sweep: exactly 4 files at zero; GalleryInfiniteGrid.vue has NO style block. Four files is bounded, not an idiom → the "out of scope" disposition flips (AA-29).
- **K-8 · L-10 consequence sub-claim** ("the raw buttons' border resolves worse than the tokens"): DEAD — currentColor via the wrapper's `text-muted-foreground` ≈5:1: the only visible border in the file. Inconsistency survives inside AA-11.
- **K-9 · D-B4 arithmetic** ("both sites become unknown props" / "76 keys"): CORRECTED — 3 breaking props (`size="sm"` survives 7.0.0); 67 keys. Verdict unchanged. Worker-2 repeated the same error inside its C·S-3 kill; corrected there too.
- **K-10 · D-B1 sub-arm** ("Bearer null → 401 → ApiProblem → swallowed"): DEAD — api.ts:127-129 throws a developer string BEFORE header assembly; no request, no 401, no ApiProblem. Matters for the cure: even a rendered `error` would surface prose no admin can act on.
- **K-11 · L-7 evidence + severity**: RESCOPED to MINOR (AA-27) — the axis's "never called with `abortInflight` by any consumer" is false (4 call sites, my grep); the reap-gap mechanism survives. **And worker-2's kill is itself corrected**: it truncated the axis's parenthetical ("no call site *in this component*" — true) to strengthen the kill. Kills must quote whole.
- **K-12 · C·S-3** ("uplift costs this file ONE line"): DEAD via AA-2 — a surviving subpath is not a surviving contract; 3 lines minimum, 1 of 37 files.
- **K-13 · Provenance cluster** (all three axes + both workers; none verdict-fatal, all repair-misrouting): `error` returned at `:75` (not :74/:71); `loading.value=false` at `:50`; destructure = **10** members (worker-2 said 11); composable returns 13 (L-11 said twelve); action filter at `admin.py:631-632` (not :637-638/:640-643); target regex `:633-634`; abort key at `api.ts:250` (not :229); `isAbortError` at `:69`; token aliases in `color-radius.css:84/:85/:95` not light-dark.css; SIZES `axes.ts:30` / TONES `:52`; 7.0.0 = 67 keys; `getAdminToken` at `auth.ts:96-98`; sibling `!` at `AdminUserList.vue:56`. Re-resolve every anchor before cures (the D-19 lesson).

## Missed-rows register (adopted; provenance = the worker that found it, premise re-verified by this seat)

Worker-1: **M-1** text-admin-label (→ AA-15 cure) · **M-2** cartoon-card (→ AA-4 cure) · **M-3** role=list 3-attribute cure (→ AA-9) · **M-4** autocapitalise × exact-match (→ AA-21) · **M-5** touch-floor strip (→ AA-22) · **M-6** empty-headline (→ AA-33) · **M-7** clear-X reflow (→ AA-34) · **M-8** sticky/scroll (→ AA-35).
Worker-2: compound index (→ AA-23, the corpus's most repair-shaping fact) · target-scan (→ AA-32) · deep-reactive items (→ AA-36) · state-machine unification of D-1/L-5 (→ AA-1/AA-7 cure shape) · colocation (→ AA-37) · async error posture (→ AA-38) · button `type` (→ AA-48).
Count: 15 — 11 as new rows, 4 as cures/mechanisms inside existing rows.

## Superlatives (with provenance; all re-verified)

- **S-1 · The NaN guard degrades to the source bytes** (`:55-56` `return iso`) — evidentiary truth over `"Invalid Date"`; the tree's ONLY date formatter (3 grep hits, all in-file). All three axes + both workers; the tree agrees.
- **S-2 · `font-mono … tabular-nums`** (`:130`) — the explicit `tabular-nums` is the single reason column 1 holds while AA-9's per-row tracks step. (D S-2.)
- **S-3 · `truncate` on the `1fr` track** (`:139`) is correctly composed (overflow-hidden collapses the automatic minimum; the ellipsis actually fires) — the D axis filed it as a defect and its own falsifier killed it into a superlative. Preserve through any cure.
- **S-4 · The fork comment** (`useOffsetPagination.ts:3-19`) is a reference-grade dependency-decision record; every load-bearing claim verified, and EXTENDED: `./pagination` absent from 4.0.0's 80 keys AND 7.0.0's 67 (my checks) — the retirement has held three majors; the sibling's named carry has still not landed (the fact F.W1 needs).
- **S-5 · The mount pattern** — async-chunked + double-gated for all three panels (GalleryView.vue:31-33/:375-387, my read) — deliberate house pattern; **qualified by AA-38**: exemplary except its error posture. (It is also what gives AA-18 its teeth.)
- **S-6 · Every truncation pairs the full value via `:title`** (`:139,:144`) — the right instinct on an audit surface, weakest satisfying mechanism (mouse-privileged; → AA-40).
- **S-7 · The backend narrates its own seam** — `janitor.py:50-56` + the `:59-84` docstring are model contract prose (sentinel rationale, peer-writer relation, crash-straddling emission gate); it is what makes AA-5/AA-10 provable. Neither original axis credited it; both workers did; adopted.
- **S-8 · Meta (worker-1's, adopted as method law)**: all three axes shipped applied falsifiers and preserved self-kills, which made this adjudication mechanical where it was cheap. The standing lesson from K-1/K-2/K-6: **an absence-proof must enumerate the surface, not query one name for it** — two kills were failures of the falsifier's search key (`./input` missed `./forms`; `log_audit(` missed the inlined second writer), and one kill of a kill (K-11) was a failure to quote the claim whole.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. AA-3/AA-4/AA-14/AA-43 contrast figures on a live viewport (token arithmetic reproduced statically to ±0.02 by both workers and this seat; real cascade/backdrop unmeasured) — axe/DevTools pass, both theme arms.
2. AA-16's timestamp-wrap breakpoint (estimate ~340–380 px; mechanism certain).
3. AA-17's collapse magnitude (~850 px estimate) and scroll-clamp/pointer displacement per page turn.
4. AA-8's mid-load empty-flash duration and the page-2/page-3 interleave window (state machine static-proven).
5. AA-27's `inflight` growth under a real narrowing session (heap snapshot; absence of reap static-certain).
6. AA-30's per-keystroke re-render milliseconds (25 `toLocaleString` + 25 tone dispatches).
7. AA-21's iOS autocapitalisation behaviour on this field (UA-dependent; server half static-certain) — device check.
8. AA-34's exact wrap viewport for the clear-X insertion.
9. Operator theme-arm distribution (the default-follows-OS mechanism static-proven from `dist/dark.js:17`); plus whether `vue-tsc` under 7.0.0 emits the projected ~160 errors (contract break certain; the count needs an install F.W1 will perform anyway).

## Closing verdict

**ADJUDICATED — the un-remediated twin, convicted on its own evidence and its siblings'.** 3 BLOCKERS sustained (AA-1 rendering-truth · AA-2 unbooked 37-file uplift break → F.W1 before any budget commits · AA-3 default-arm contrast on the semantic column); 20 MAJOR / 20 MINOR / 5 INFO routed — the overwhelming mass to **F.W4** (one a11y/idiom port from `AdminUserList.vue` + three current-pin glass adoptions: Badge, Skeleton, `text-admin-label`, plus `cartoon-card`), the cross-tier seam (exact-match filter, action taxonomy, dead date-range params, **the compound-index cure constraint AA-23 that neither axis could see**) to **F.W5–W8**, the F.W2 charter contradiction carried, the test seam to **F.W9/W10**, three latent records NO-WAVE-OWNER. 13 kills — including one kill-of-a-kill and both workers corrected on arithmetic they were surest of. Preserve S-1/S-2/S-3/S-6 through every cure; the file's four best lines must survive its forty-eight worst.

— end of apotheosis. The three challenge records and both worker returns remain immutable beside this file per E-1/E-3; this file is the session's only write.
