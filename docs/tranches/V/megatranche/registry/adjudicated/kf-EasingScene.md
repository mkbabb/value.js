claude-fable-5

# kf-EasingScene — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/scenes/easing/EasingScene.vue` (133 L) + its consumption closure (`useEasingDemo.ts`, `EasingSidebar.vue`, `PlaybackRibbon.vue`, the facility/surface chain) — reader-1 ("design-and-usage": 47 confirmation cells · 16 kills · 8 misses · 12 superlatives) and reader-2 ("library-and-code": 38 confirmation cells · 10 kills · 6 misses · 11 superlatives). All three challenge files consulted directly at the disputed cells (`audit/kf-components/EasingScene/challenge-{D-design,L-library,C-consumption}.md`).
**Sibling record**: `kf-EasingTarget.md` (banked 2026-08-08, this registry) adjudicated the SAME component family from its own corpus — a large fraction of this corpus's D-axis rows land on surfaces that record already owns. The identity guard below folds every such row by reference (KF-ET-1..37); **zero re-bookings**, and one **cross-registry correction against a banked row** (KF-ET-20) is recorded where this corpus's evidence is decisive.
**Method (incredulity)**: every reader-vs-reader head-on contradiction (five of them), every kill of a load-bearing cell, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption. **Zero browser tooling** (law); rulings are static reads + git archaeology + `node --input-type=module` executions against the **installed** `@mkbabb/value.js@4.0.0` and reads of the installed `@mkbabb/glass-ui@7.0.0` dist. Product source read-only. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only + node-against-dist)**: keyframes.js git triple (HEAD `8281638c`, `origin/master` `81a56990`, 252 dirty paths); **`git diff origin/master` per-file over the corpus-critical demo set → 0 diff-lines for ALL of** `EasingScene.vue`, `EasingTarget.vue`, `EasingSidebar.vue`, `useEasingDemo.ts`, `PlaybackRibbon.vue`, `AnimationVisualizer.vue`, `controlSurfaces.ts`, `scenePlaybackAdapters.ts`, `useSweepScene.ts`, `rafConstants.ts`; full read of EasingScene.vue (whole, 133 — the :23-27 `CONTROL_SURFACES.easing = ['easing']` comment, :45/:46, :57, the :59-69 "SAME component cube/amiga mount" prose, :70-92 quartet, :94-110 prop bag [five handlers, no `onScrubbed`], :112-132 expose block); useEasingDemo.ts:160-260 + :280-365 (`livePhaseValue = sweep.at(phase).p`; `onArm: startTime = now − livePhaseValue·2d`; `startTime` writes at exactly onArm + `reset`; unconditional `startLoop()`; `selectEasing`'s `[0,0,1,1]` reset; `previewAnim` built targetless and never `.play()`ed; `channels[0].progress: () => liveProgress()`; the `{immediate:true}` progress watch gated on `!playback.running`); useSweepScene.ts:77-122 (`startLoop` → `onArm()` → `loop`; visibility-pause re-arms through it under the FALSE ":the sweep resumes in phase with no jump" comment); useSceneMachine.ts:197-199 (`snapshot()` BEFORE `suspend()`) + :235-236 (PAUSE → suspend); PlaybackRibbon.vue:98-135/:181-203 (`const {animation, source} = defineProps`, both optional, "At least one is required" prose; `effectiveDuration = computed(() => animation?.options.duration ?? source?.duration ?? 1)`; `scrubTo`'s `rawT` un-reversal; `emit("scrubbed")` on every scrub); RibbonBar.vue:6-9 (`#controls-ribbon-target` under `v-show`, OUTSIDE the :13/:69/:108 branch chain) ; ChannelOptions.vue:377-399 + :428 (the teleported second PlaybackRibbon: `@scrubbed="wake"`, effective `currentT`, real `isAnimStarted`); ControlsPaneWrapper.vue:46-60/:207 (`controlHosts` = channels WITH animation → `:animation="host.animation"`); controlSurfaces.ts:95-120 (`selected?.animation ? [...BUILT_IN_SURFACES] : [...(selected?.surfaces ?? [])]` — and the light-channel arm that honors declared surfaces); App.vue:248-256 (`watchEffect → setActiveSurfaces`) + :203 (the false "easing scene → []" comment); **useKeyframesPaneReveal.ts:90-120 read whole — `scheduleIdleWarm()` unconditional at setup, `requestIdleCallback(warmKeyframes, {timeout: 4000})` — the ruling-3 decider**; ChannelControls.vue:79-80 (`@pointerenter/@focusin` warm accelerators), :130 (mount gate), :174-181 (the ungated `tabs-content` slot under the "scene gates its own body" contract comment), :194 (`:targets="animation.targets"`); play-lifecycle.ts:92-110 (`reverse()` shifts `startTime -= duration − 2·rawT`; `onStart` opens `reversed = false`) + :449 (`effectiveT`); animation.ts:95-96 (raw `reversed` setter); scene-facility/index.ts:35 (":Present ⇒ the triad is HONEST for this channel (it paints)"); SpringScene.vue:115-120 (the `scrubberPhase` cure verbatim); useThrottledReadout.ts:30-78 (`reconcile`/`reset` shipped, unused by easing); rafConstants.ts:15 (`PROGRESS_READOUT_HZ = 6`); numeric.ts:103-115 (identity easing default + auto-positions ⇒ the sweep IS the triangle); EasingSidebar.vue whole head + :80-140 (props snapshot; `steps:1/jump-start|jump-end` seeds; `name in bezierPresets`; hand-rolled `catalogueGap` + `syncGap`; the stale "4.0.1 EMIT-ONLY" comment; the false "wider (quart/quint)/differs (sine)" comment); sceneExposedApi.ts:33 (`isStarted?: boolean`); useSceneMachineShellBinding.ts:200-207/:264-265; liveProgress consumers **verified live**: AnimationControlsGroup.vue:259 + AnimationControlsGroup/useAnimationProgress.ts:24 (`clamp(ch.progress(), 0, 1)`); EasingTarget.css:29 (`bounce-in-ease-half`); font-roles.json (`"T.E6's 33 specimen-tile curve identifiers"` prose + `monoCeiling: 90`); EasingSidebar slider :59-61 (`min 300 / max 5000 / step 100`); node probes against installed value.js 4.0.0: `steppedEase(1,'jump-none') → step_count_invalid` (all other jump terms ok at 1); `bezierPresets` 30 keys incl. `smooth-step-3 [0.65,0,0.35,1]`; `cubicBezierToString(0.215,0.61,0.355,1) → "cubic-bezier(0.21, 0.61, 0.35, 1.00)"`; `parseTimingFunction`: `step-start`/`step-end`/`steps(2, jump-none)` OK, `ease-in-sine`/`ease-in-bounce`/`smooth-step-3`/`steps(1, jump-none)` ERR; installed glass-ui 7.0.0 dist/easing.js python re-extraction: `te(e,"modelValue")` + **ONE `{deep: !0, immediate: !0}` watch whose body seats mode, points (`N(0/1,…)`), steps (`Math.max(1, Math.min(12, round))`), and term (`I.includes(e.term)`)** — the L-M5 decider; `throw Error(\`${e}: ${t.error.code}\`)` unwrapper present; `terms: P` exported unfiltered; dist/chip-*.js carries the `role|aria-pressed|data-state` fallthrough-attr filter (the M-6 decider); `glass-chip.css` referenced by **nothing** in dist/ and `glass-chip` absent from `dist/styles/glass.css` (the KF-ET-4 orphan, re-verified).
**Substrate disclosure (SCH-1, inherited and applied)**: the working tree is dirty in 252 paths against HEAD `8281638c` — and the **entire demo-side read set of this corpus is byte-identical to `origin/master` `81a56990`** (per-file diffs above, all empty). Every demo row below is **frontier-durable by identity**. The `src/animation/` engine tree differs in LAYOUT at the frontier (worktree monolith `engine/play-lifecycle.ts` vs the frontier's `play-lifecycle/` split; `css/css-animation.ts` vs `css/animation.ts`) — engine line anchors in this corpus are worktree anchors and MUST be re-resolved at cure time (D-19); the cited mechanisms (`effectiveT`, `reverse()`, `onStart` reset) were verified at the worktree bytes.
**Law compliance**: product source read-only; no browser tooling; **sole write = this file.**

## Scoreboard

**45 adjudicated roster rows — 2 BLOCKER · 15 MAJOR · 19 MINOR · 9 INFO**, of which **17 are folds by reference to banked identities** (KF-CO-15 · KF-AV-1 · KF-APP-29 · SCH-1/KF-ET-3 · KF-CB-8/KF-ET-11 · KF-ET-1/-2 · KF-ET-6 · KF-ET-8 · KF-ET-10 · KF-ET-14 · KF-ET-16 · KF-ET-20 · KF-ET-22 · KF-ET-26 · KF-ET-27 · KF-ET-28/-30 · KF-ET-31 · KF-ET-34 · the banked SUP-3 ruling · kf-ChannelControls L-3 / kf-ControlsPaneWrapper M-4) — **zero re-bookings**. **18 killed-claims register entries** — 15 sustained, one reader kill **itself killed** (C-1's Monaco clause), and two reader confirmation cells **reversed** (C-10; C-2's "no transport" clause). **14 missed rows ADMITTED** (8 from reader-1, 6 from reader-2; three of them are registry-blind re-finds folded to banked identities) **+ 1 seat-added sharpening** (the D-B2 cure-lock: the triangle is non-invertible). **15 superlative cells sustained, 2 clause-corrected against the bank.** **7 adjudicator corrections against the readers, 1 against a banked row.**

Blocker-weight survivors: **KF-ES-1** — the resume clock is re-seeded with the sweep's OUTPUT where its PHASE belongs, so every resume, scrub-release, and tab-return teleports all 28 balls, under three comments asserting exactness — now **cure-locked** by this seat: the triangle has TWO preimages per output, so no inversion of `livePhaseValue` alone can be correct; the snapshot contract itself is under-determined. **KF-ES-2** — the facility declares a PAINTING channel over a targetless, never-played animation, so the scene derives four control surfaces instead of one, mounting a Monaco editor on an idle tick, an empty timeline, and two rival transports over the same clock.

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-2 / the `controls` surface transport** — head-on #1 | "WORSE than stated: on `controls` the slot is not rendered AT ALL … No fallback transport exists" | NARROWED: ChannelOptions teleports its OWN PlaybackRibbon into `#controls-ribbon-target`, which sits outside the branch chain | **Reader-2 WINS; reader-1's clause REVERSED** | RibbonBar.vue:6-9: the teleport target `div` rides `v-show="selectedControl === 'controls'"` at the TOP of the card, before and outside the `v-if` chain (:13/:69/:108); ChannelOptions.vue:377-399 teleports a fully-wired PlaybackRibbon into it when `active`. A transport IS present on `controls`. Sharper: reader-1's own C-13(1) kill had already found the ChannelOptions mount — its C-2 cell contradicts its own kill three rows down. The transport is genuinely deleted on exactly **two** surfaces (keyframes, timeline). Survivor booked as KF-ES-12; the dual-instance consequence as KF-ES-6. |
| **The ":SAME component cube/amiga mount" comment** — head-on #2 | "the seam comments remain false — no PlaybackRibbon mount exists under `demo/scenes/cube/` or `amiga/`" | "cube and amiga DO mount PlaybackRibbon, via the shared controls pane … the comment is substantially true" | **Reader-2 WINS** | EasingScene.vue:59-61 names the component, not a mount site. ChannelControls renders ChannelOptions for every painting channel (banked kf-ControlsPaneWrapper M-4 enumerates N=3 on cube/amiga), and ChannelOptions.vue:428/:377 imports and mounts PlaybackRibbon. Cube and amiga therefore mount it through the shared pane. Reader-1's residue clause is corrected; the kill of the challenge's *two-consumer census* stands (three importers). |
| **C-1's Monaco clause** — head-on #3 | KILLED the clause: "gated on `keyframesWarmed`, set by `@pointerenter/@focusin` — reachable, not automatic" | Confirmed "force-mounts the Monaco pane" | **Reader-1's KILL is itself KILLED; the challenge cell restored as rescoped** | My whole read of useKeyframesPaneReveal.ts:90-120: `scheduleIdleWarm()` runs **unconditionally at composable setup** — `requestIdleCallback(() => warmKeyframes(), { timeout: 4000 })` (with a timer fallback). The pointerenter/focusin bindings reader-1 cited are *accelerators*, one function below the idle warm it did not read. Monaco mounts automatically within ≤4 s of idle for any scene where `hasSurface('keyframes')` — which C-1 makes true for easing. Convergent with banked kf-ChannelControls L-3 (":scheduleIdleWarm() unconditional at setup … the mount gate consults `hasSurface` (scene-wide) and `keyframesWarmed`, never `active`"). Rescope: "force-mounted on entry" → "auto-mounted on the first idle tick"; the CWV framing tightens rather than relaxes. |
| **C-10 magnitude** — head-on #4 | KILLED: 2 of 28 tiles copy non-CSS, not "26 of ~31"; `step-start`/`step-end` are valid keywords | CONFIRMED as stated (":returns the bare name for every engine-named curve") | **Reader-1's KILL SUSTAINED; reader-2's cell REVERSED** | 23 of 28 names take the `isBezierEditable` branch (NAMED_EASING_BEZIER), `steps` takes its own, and the bare-name fallthrough is exactly {`smooth-step-3`, `ease-in-bounce`, `step-start`, `step-end`} — of which my `parseTimingFunction` probes pass the last two. 2 of 28. Convergent with the banked **KF-ET-2** figures; reader-2 endorsed the challenge's premise against its own probe set. Folds to KF-ET-1/-2 (KF-ES-30). |
| **D-m1 scope** — head-on #5 | Narrowed: ONE root-scoped property (`--ball-tone`); the other three sit on empty leaf spans | Confirmed "four unprefixed custom properties … inherit into the entire vendor subtree" — while its own proof cell concedes three are set on descendants | **Reader-1's NARROWING SUSTAINED; reader-2's claim-line corrected by its own evidence** | EasingTarget.css:1-8 sets `--ball-tone` on the Card root; `--rail-tint`/`--ball-size`/`--ball-glow` on `.tile-rail`/`.tile-ball` — childless spans. Glass-ui ships no `--ball-*`/`--rail-tint` (banked grep). Latent collision at 1/4 the stated radius; the counterweight is reader-1's own superlative (the seam consumed exactly as design-idioms.css documents). Booked INFO (KF-ES-37). |
| **C-8 severity** (steps(1, jump-none) picker throw) | BLOCKER | MAJOR (the axis's own grade) | **MAJOR at the consumer surface, GLASS-OWNED — with reader-1's BLOCKER preserved as dissent and a named escalation trigger** | The mechanism is complete at my instruments: `steppedEase(1,'jump-none') → step_count_invalid` (value.js is spec-correct — CSS requires ≥2 for jump-none); the picker's `throw Error(…)` unwrapper sits inside a computed feeding both render and emit; `terms` exported unfiltered; the demo seeds `steps: 1` for `step-start`/`step-end` (EasingSidebar :106-110). The ONE unwitnessed link: whether the term control offers `jump-none` while steps === 1 (SS-13 #1). If it does, this is a one-click crash of a default-mounted surface and the row escalates to BLOCKER. Per the ColorSpaceSelector D-10 / KF-ET-4 precedent the label follows the cure's owner: the picker must constrain its term×count domain (producer), with a demo-side seed-guard rider. |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** from the census taxonomy (`formation/keyframes/CENSUS-2026-08-03.md:179-206`: W0 Substrate Settle · W1 Mail Cure · W2 Parse Façade · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Tri-Fold Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit · W10 Fold Discharge & Close); behavioral repairs with no census owner are **NO-WAVE-OWNER (SS-1/SS-2 spec input)**; producer defects are **GLASS-OWNED** on the standing BH relay (owner edict 2026-07-12). Routings are bookings, not acts. Demo anchors are frontier-durable by identity; engine anchors are worktree-only (re-resolve, D-19).

### BLOCKER

- **KF-ES-1 · D-B2 (+ scrub-release and tab-return arms; + this seat's cure-lock) — `onArm` re-seeds the sweep clock with the sweep's OUTPUT, not its phase; every resume path teleports all 28 balls.** The sweep is the triangle by construction (numeric.ts:103 identity easing + auto-positions [0,50,100] ⇒ `at(φ).p = 2φ` for φ≤½), `livePhaseValue = sweep.at(phase).p` (useEasingDemo.ts:186) stores p, and `onArm` (:209) inverts it AS φ: pause at φ=0.25 (p=0.5) → resume at φ=0.5 → p=1.0, a half-rail jump on all 28. Reached on every re-arm: `useSweepScene.startLoop` (:82-87) fires `onArm` from machine PLAY, adapter `restore`, AND `useSceneVisibilityPause` (tab return) — the last under the verbatim false comment "the sweep resumes in phase with no jump" (:118-120). The scrub path writes `livePhaseValue = t` (a p-value) into the same inversion. Reader-2's fixed-point kill sustained: p=φ only at {0, 2/3}, not {0, ½}. **CURE-LOCK (this seat's addition, which neither reader stated)**: `tri(φ) = p` has TWO preimages (p/2 ascending, 1−p/2 descending) — no repair that keeps only `livePhaseValue` can be correct, and the ScenePlayback snapshot contract (`progress` = the p-value) is itself under-determined for an alternating sweep. The fix must persist the phase (or declare the ascending leg canonical in the contract), not "invert better." **BLOCKER (unanimous).** → **NO-WAVE-OWNER** (SS-1/SS-2), one clock-rebase law with KF-ES-9.
- **KF-ES-2 · C-1 — the facility declares a PAINTING channel (`animation: previewAnim`) over a targetless, never-played animation; the scene therefore derives the full built-in triad plus its facet — 4 surfaces where its own prose asserts 1.** `previewAnim` is constructed with no targets and `.play()` is never called (my :280-365 read; the only external touches are `.options.duration`/`.reversed`/`.t`); the contract comment (scene-facility/index.ts:35) says "Present ⇒ the triad is HONEST for this channel (it paints)" — the balls are painted by the painter registry, not by this animation. Derivation chain verified whole: controlSurfaces.ts:107-109 → App.vue:248-256 `watchEffect(setActiveSurfaces)`, no per-scene filter. Consequences, each verified: four dock tabs; ChannelControls mounted for the channel (ControlsPaneWrapper :207/:60); **Monaco auto-mounted on the first idle tick** (ruling #3 — folds to banked kf-ChannelControls L-3 / kf-ControlsPaneWrapper M-4); `KeyframeTimeline :targets="animation.targets"` = `[]` (ChannelControls :194); the second transport (KF-ES-6). Two false comments: EasingScene.vue:23-27 (cites a `CONTROL_SURFACES` table controlSurfaces.ts:4-6 records as DELETED) and App.vue:203. **Cure observation (not a prescription — the cure is adjudicated separately)**: the in-tree shape already exists — `surfacesFor` honors a light channel's declared `surfaces`, and PlaybackRibbon accepts a `source {progress, setProgress, duration}` — the channel already carries `progress`/`setProgress`. **BLOCKER (unanimous).** → **NO-WAVE-OWNER** (SS-1/SS-2: the facility-honesty contract); the Monaco/N-mount consequences ride the banked kf-CC/kf-CPW rows.

### MAJOR

- **KF-ES-3 · C-8 — a reachable `steps(1, jump-none)` throws inside the glass-ui EasingPicker the scene mounts by default** (ruled table: mechanism complete, one live link unwitnessed). Blast radius per reader-2, verified at the dist: the throwing computed feeds the picker's `value` computed AND its emit watcher — render and emit both fault. **MAJOR at the consumer surface; BLOCKER dissent + escalation trigger (SS-13 #1).** → **GLASS-OWNED** (BH relay: constrain the term×count domain or degrade the Result instead of throwing; value.js is CSS-spec-correct and needs no change) + NO-WAVE-OWNER rider (the demo's steps-1 seeds gain a guard until the producer cure lands).
- **KF-ES-4 · L-B1 = C-4 — FOLD by reference to banked KF-CO-15** (kf-ChannelOptions; root = PlaybackRibbon.vue:119-121's computed over a `markRaw` object's untracked `options.duration`, consumed as `:max` at :19), **EXTENDED with the easing instance, which is hotter than the banked one**: the writer here is a live 300–5000 ms slider (EasingSidebar :59-61) three inches from the rail, `applyDuration` mutates in place, and at 5000 ms the thumb pins at 30% of the rail while scrubs cap `progress` at 0.30. Both readers' permanence-kill is CONVERGENT with the banked §B correction (heals only on a surface bounce; permanent within an uninterrupted stay on the default `easing` surface). Zero re-booking; the easing instance rides as the fold's rider. → **KF-CO-15 (OPTIONS-UNIT)** + the reactive-`:max` repair in the shared component.
- **KF-ES-5 · D-B3 = L-M2 = C-6 + L-M1 + MISS-3 (admitted, verified) — the Reverse bundle: a live decoy that never touches the stage, two playheads that disagree under it, every scrub reflected across the midpoint, and a raw-setter idiom that the engine will silently reset.** (1) `EasingScene.vue:77-82` writes only `previewAnim.reversed`; the sweep and painters never read it (grep: 2 hits, both this file). (2) The visualizer paints `effectiveT` (= d−t under reverse) while `currentT` (:100) feeds raw `t` — mirrored playheads; the repo's own reference consumer pins the convention to EFFECTIVE (useAnimationSync :34/:45 → ChannelOptions :380). (3) The ribbon un-reverses on input (`rawT`, :183-185) and the scene re-normalizes raw (:72-75) — drop at 25%, thumb lands at 75%. (4) MISS-3: the published verb `reverse()` (play-lifecycle:95-102, worktree anchor) shifts `startTime -= duration − 2·rawT` to keep `effectiveT` continuous; the scene's raw `.reversed =` setter skips the shift, and `onStart` (:108) unconditionally clears the flag at play-start — the scene's transport state is engine-owned the moment the animation is ever played. **MAJOR.** → **NO-WAVE-OWNER** (one spec: either Reverse acts on the stage or it leaves the surface; the binding uses the verb, not the field).
- **KF-ES-6 · MISS-1 (reader-2, ADMITTED — verified whole by this seat) — under KF-ES-2 the scene runs TWO PlaybackRibbon instances over the SAME `previewAnim` with three contradictory prop conventions, switchable by a dock tab.** Instance A (easing surface, EasingScene :96-109): raw `t`, hardcoded `isAnimStarted: true`, no `scrubbed` handler. Instance B (controls surface, ChannelOptions :377-399, teleported to RibbonBar :7): effective `t`, real `started`, `@scrubbed="wake"` + wake-on-scrub-entry. Same clock, two scrub coordinate systems, two disabled-state semantics. All three axes and reader-1's C-2 cell missed the second instance; the banked kf-ChannelOptions record (L·M-3 proof) independently attests it. **MAJOR.** → **NO-WAVE-OWNER**; the reference instance (B) is the convention pin for the repair of A.
- **KF-ES-7 · L-M3 = C-5 — the transport thumb and visualizer twin are driven off the 6 Hz readout mirror while the 60 Hz channel sits wired to the DOCK only.** `PROGRESS_READOUT_HZ = 6` (rafConstants :15, verified); `progress` written only in `readout.maybeFlush` (:191-193); `currentT` (:100) and the `previewAnim.t` watch both sample it; the visualizer polls per-frame over the 6 Hz source (~167 ms steps). The kill-refined truth (both readers, convergent): `liveProgress` IS plumbed — `channels[0].progress: () => liveProgress()` (:355) feeds AnimationControlsGroup :259 + useAnimationProgress :24 (verified live) — the scene gives the 60 Hz channel to the dock and the 6 Hz mirror to its own transport. The cure is quoted verbatim in SpringScene :115-120 (`scrubberPhase`). Perceptibility → SS-13. **MAJOR.** → **NO-WAVE-OWNER**; cross-ref banked KF-AV-1 (same seam family, distinct defect).
- **KF-ES-8 · L-M8 + C-12 — pause-on-scrub freezes the ball for exactly the gesture it visualizes, and the seam's own wake cure is declared, documented keyboard-critical, and left unbound.** The visualizer-guard half (`guard: isPlaying || isDragging`, where `isDragging` is the ball's OWN drag) **FOLDS by reference to banked KF-AV-1** (BLOCKER there, with the playhead-overwrite amplification). The scene-side residue booked here: `onScrubbed` absent from the :96-109 bag (five handlers, verified); `scrubbed` fires on EVERY scrub incl. keyboard (`scrubTo` tail, verified) and the reference consumer wires it (ChannelOptions :391); a keyboard arrow during playback additionally round-trips to nothing — no pointerdown → no pause → the write is discarded (the `!playback.running` gate + the next readout flush). If the user was already paused, `wasPlayingBeforeScrub === false` and nothing resumes. **MAJOR.** → **NO-WAVE-OWNER** (bind the wake; admit keyboard scrub into the pause-latch path), riding KF-AV-1's guard cure.
- **KF-ES-9 · M-2 (reader-1, ADMITTED — verified at the bytes) — dragging the duration slider WHILE PLAYING teleports all 28 balls to an arbitrary phase.** `startTime` has exactly two writers (onArm :209, reset :234 — verified); the duration watch (:316-318) calls only `setDuration`; `phase = (now − startTime)/(2d) % 1` rescales discontinuously under the modulo of minutes of elapsed time. Worked example verified (30 s at 1500 → one 100 ms step → φ 0.0 → 0.375, p 0 → 0.75). `:step="100"` over 300–5000 = 47 discrete teleports across the control. A second, independent defect on the same control KF-ES-4 indicts. **MAJOR.** → **NO-WAVE-OWNER** — ONE clock-rebase law with KF-ES-1 (rebase `startTime` on every duration write, from the PHASE, which the cure-lock requires be stored).
- **KF-ES-10 · L-M5 — ~50 lines of remount-seed machinery rest on a "glass-ui 4.0.1 modelValue is EMIT-ONLY" claim the installed 7.0.0 falsifies.** Verified by this seat's python extraction of dist/easing.js: `useModel` + ONE `{deep, immediate}` watch whose body seats mode, points, steps, AND term (with the `q(J,e)` echo guard) — a full write-through v-model exists; the stated blocker is gone. Cure convergence adopted: migrate to a full `EasingPickerValue` `:model-value` binding (which the seat-verified watch fully honors), not a bare `:key` deletion. The stale vendor comment (EasingSidebar template :18-26) dies with it. **MAJOR.** → **NO-WAVE-OWNER** (EasingSidebar), one motion with KF-ES-23.
- **KF-ES-11 · C-3 — `tabsContent` is unconditional against the host's documented "the scene gates its own body on the active surface" contract**, so under KF-ES-2 the whole EasingSidebar stacks beneath ChannelOptions on the `controls` surface. Verified: EasingScene :57 ungated; ChannelControls :174-181 renders the slot ungated under the contract comment; CubeScene honours the contract; `isSingleSurfaceScene` requires `length===1` — false here. **MAJOR.** → **NO-WAVE-OWNER**, same spec as KF-ES-2 (surface honesty decides whether the gate or the derivation moves).
- **KF-ES-12 · C-2 as-narrowed (ruling #1) — `ribbonContent` returns null on the two surfaces where no other transport exists** (keyframes, timeline — the CSS/timeline button strips replace it), deleting play/pause/scrub/reverse there; on `controls` the teleported instance B covers (and contradicts — KF-ES-6). **MAJOR.** → **NO-WAVE-OWNER**, rides KF-ES-2's spec.
- **KF-ES-13 · L-M7 — EasingScene and SpringScene carry a byte-identical transport-arming quartet (`userReversed`/`onToggleReverse`/`wasPlayingBeforeScrub`/`onScrubStart`/`onScrubEnd`) that `useSceneTransport` was extracted to kill** — its own charter names the triplication. KF-ES-5/-7/-8's fixes must otherwise land twice. **MAJOR.** → **NO-WAVE-OWNER** (extend the seam; the reverse+scrub half joins the play/pause half).
- **KF-ES-14 · L-M4 — FOLD by reference to banked KF-ET-6** (NAMED_EASING_BEZIER is a strict 29-of-30 subset of `bezierPresets`; both clauses of its defending comment false; the `smooth-step-3` three-way contradiction). This corpus's probes are convergent with the bank (re-executed by this seat: 30 keys, sole delta smooth-step-3). Zero re-booking. → **KF.W4** with the owner-ruling flag banked there.
- **KF-ES-15 · L-M6 = C-15 = F-1 — FOLD by reference to census SCH-1 / banked KF-ET-3** (KF.W0 Substrate Settle). Both readers' worktree facts are right (deps = value.js only; lock has zero glass-ui); both repeat the "under `npm ci` this scene cannot resolve its first import" framing that the banked substrate ruling DATES: HEAD declares glass-ui 6.0.0 as a locked optionalDependency and **`origin/master` declares `@mkbabb/glass-ui: "7.0.0"` as an exact devDependency** — the frontier's install path produces exactly the measured producer. Fourth and fifth occurrences of frontier-blindness in the program (corrections register #5). This scene's exposure census (7 entry points across the closure) rides as the fold's rider. Zero re-booking.
- **KF-ES-16 · C-7 — FOLD by reference to banked KF-CB-8 / KF-ET-11** (nothing type-checks .vue; CI runs `check:lib` only; `*.vue` declared `DefineComponent<{},{},any>`). Riders booked here: the 10-key `h(PlaybackRibbon, …)` bag is unchecked (the enabling condition for KF-ES-4/-5/-18 and the L-M8 missing handler); `sceneExposedApi.ts:33` declares `isStarted?: boolean` while the scene exposes a `Ref` (works only unchecked); L-m10's detector gap (no `noUnusedLocals`, `lint` = `depcruise src`, no eslint) — the same class pt-leaf L-m1 booked. → **KF.W4**.
- **KF-ES-17 · D-M3 — the scene's only explanatory sentence renders as uppercase Fira Code at 0.1em caps tracking under a `data-register="code"` exemption prose does not qualify for.** Vendor utility verified (uppercase + `--type-tracking-caps`); the escape (`normal-case`) is in active use in the same demo (SharePopover, MbabbMenu, ChannelOptions). Count corrected: 21 sites, not 19. **MAJOR (both readers convergent, unrebutted).** → **NO-WAVE-OWNER** (EasingSidebar; one class).

### MINOR

- **KF-ES-18 · C-14 = L-m3 = D-m6 — `isStarted` is a write-only exposed ref (the shell writes it at useSceneMachineShellBinding :264-265, verified; nothing reads it) while :102 hardcodes `isAnimStarted: true` two lines away**, making the ribbon's disabled treatment dead for this scene — instance B passes the real thing. → **NO-WAVE-OWNER**, folds into KF-ES-6's convention repair.
- **KF-ES-19 · L-m5 + L-i3 — the documented pause-reconcile in `frame()` is unreachable on every real pause path** (RAFPlayback.stop is a synchronous cancel; useSceneMachine :197-199 snapshots BEFORE suspend — both verified), so the persisted snapshot is a ≤167 ms-stale mirror; and the extracted seam's own `reconcile`/`reset` (shipped, verified at useThrottledReadout :46/:48/:73/:78) sit unused while both are hand-rolled. One cure: call the seam's `reconcile` from the suspend path. → **NO-WAVE-OWNER**.
- **KF-ES-20 · C-11 = L-m6 — FOLD by reference to banked KF-ET-20, WITH A CORRECTION AGAINST THE BANKED ROW** (corrections register #7): the dead-export roster is **four** (`svgPath`, `comparisonCurves`, `currentFamily`, `currentEasingFn` — 0 consumers each, banked greps + this corpus concordant), **not five: `liveProgress` is LIVE** through the facility channel closure (`progress: () => liveProgress()` :355 → AnimationControlsGroup :259 + useAnimationProgress :24, verified by this seat) — grep-dead at the identifier, live at the data flow. `cssValue` is export-dead but internally load-bearing (:308 watch). KF-ET-20's "consume it or delete it" ruling for `currentFamily` unchanged. → **NO-WAVE-OWNER** hygiene, corrected roster.
- **KF-ES-21 · MISS-2 (reader-2, ADMITTED — verified) — `selectEasing` silently destroys authored bezier state**: any tile that is neither steps-family nor a NAMED key resets `bezierControlPoints` to `[0,0,1,1]` (:246-259, verified) — the user's dragged quad is gone on one click (`steps`/`step-start`/`step-end`/`ease-in-bounce` are the live triggers), with no second store and no undo; `[0,0,1,1]` is byte-identical to `linear`. → **NO-WAVE-OWNER** (rides banked KF-ET-5's selection-state spec).
- **KF-ES-22 · MISS-4 (reader-2, ADMITTED — verified) — PlaybackRibbon's "At least one is required" `animation`/`source` contract is prose-only**: both props optional (verified :100-110), no discriminated union, `?? 1` yields a silent 1 ms rail, the scrub silently drops with neither — and C-7 means nothing would catch the expressible-union fix anyway. → **NO-WAVE-OWNER** (shared component; type-level either-or).
- **KF-ES-23 · MISS-5 (reader-2, ADMITTED — verified) — `catalogueGap` is a hand-maintained ref mirroring a pure function of two computeds** (`syncGap` verified :131-138); the imperative mirror couples the caption to the picker seed in one callback and is what made KF-ET-6's contradiction authorable. `computed(() => …)` is a drop-in. → **NO-WAVE-OWNER**, one motion with KF-ES-10.
- **KF-ES-24 · D-M1 — the unit-square portrait in a ~3.97:1 letterbox under `preserveAspectRatio="none"`** — geometry re-derived by both readers (52 px stage · 33.28 px band · 132+ px tracks, worsening on `1fr`). **Adjudicated MINOR with both readers' + the axis's MAJOR preserved as dissent**, grounds: the distortion is uniform across all 28 (the comparative thesis survives), the mechanism is the banked P-7 superlative (deliberate, with `vector-effect` compensation), and the banked registry priced the same geometry family INFO (**KF-ET-36**, which this row extends: base ratio, not just variance). Canonical-shape legibility → SS-13 glance. → **NO-WAVE-OWNER** design question (stage height vs `1fr` tracks), one identity with KF-ET-36.
- **KF-ES-25 · D-M2(measurement) — FOLD by reference to banked KF-ET-14, WITH THE ORPHAN CORRECTION AGAINST BOTH READERS** (corrections #6): the 3.48:1/3.98:1 selected-label figures are computed over `glass-chip.css`'s accent-band rules — **a stylesheet nothing imports** (KF-ET-4, re-verified by this seat's grep of the installed dist). The shipped selected ground carries no band; the live figure is the readers' own ball-tone-over-card row (4.63/5.77 — exactly KF-ET-14's banked number, marginal at 4.5:1). The readers' band arithmetic is not wasted: it prices the POST-CURE state should the producer restore the import (it rides KF-ET-4's BH letter). Reader-1's `plus-lighter` flood caveat dissolves the same way (the flood never paints today). D-M2(cause)'s kill (own-`color` beats inherited `--accent-ink`) sustained AND superseded by the orphan. Zero re-booking.
- **KF-ES-26 · D-B1 + D-m3 — FOLD by reference to banked KF-ET-8** (idle sparkline/ticks 1.59/1.87, rail 1.23/1.33 — same numbers, re-derived independently by this corpus's readers; 1.4.11-arm MAJOR at the bank). The α-cure kill rides the killed register (#4). The resting-ground caveat from KF-ES-25's orphan correction applies here too (the "chip rest ground is `--card`" mechanism cites unloaded rules; the against-card baseline + SS-13 posture the bank took is the right one). Zero re-booking.
- **KF-ES-27 · D-M4(a11y half) — FOLD to banked KF-ET-8's aria-hidden clause** (the stage hidden while being the content; the 1.1.1 arm stays killed per the bank — the tile name is a complete text alternative). **The killed half SHARPENS banked KF-ET-31**: `CurveGroupItem.description` IS rendered — ChannelOptions.vue:267, in this scene's own dock under KF-ES-2 — so KF-ET-31's "no tooltip/aria consumer" holds only tile-side; the "authored and never rendered" framing is dead program-wide. Cross-registry sharpening recorded; zero re-booking.
- **KF-ES-28 · D-M5 + M-6 (reader-1's miss, ADMITTED — verified at the dist) — FOLD by reference to banked KF-ET-10** (28 loose toggles, hand-rolled single-select, no radiogroup), **STRENGTHENED**: the Chip strips `role|aria-pressed|data-state` from fallthrough attrs (filter verified in dist/chip-*.js), so the consumer-side ARIA remap is structurally unspellable — the cure MUST land producer-side (a selectable chip-group shape) or as the ToggleGroup reshell, exactly as KF-ET-10's KF.W6 routing already holds. A repair order listing this as a scene-side change is unexecutable. Zero re-booking.
- **KF-ES-29 · D-M6 — FOLD by reference to banked KF-ET-27** (BALL_SIZE triply encoded; the design-idioms `getComputedStyle` seam documented and never read — the sheet's contract names this file). Zero re-booking.
- **KF-ES-30 · C-9 + C-10 + M-1 — FOLD by reference to banked KF-ET-1 / KF-ET-2** (the literal fork BLOCKER + the cure-lock). Reader-1's "missed" M-1 (":the header literal is a different curve … none noticed") is the banked KF-ET-1, adjudicated in this registry the day before this corpus's return — a registry-blindness event, not a novel find (corrections #4); its Δ figures (0.037 max at 201 samples) are consistent with the banked 2001-sample table. C-9's toFixed(2) and both C-9 kills (reseat/compounding) are convergent with KF-ET-2's refuted-cure record. C-10's kill: ruling #4. Zero re-booking.
- **KF-ES-31 · M-3 (reader-1, ADMITTED) — `content-visibility: auto` on the 28 focusable chips can strip each tile's ONLY accessible name** (the stage is aria-hidden; the name sits inside the skippable subtree of the toggle button). Construction proved from source; engine semantics live-only. → **NO-WAVE-OWNER** + **SS-13** (shares the witness with banked KF-ET-26's residue #4).
- **KF-ES-32 · M-7 — FOLD by reference to banked KF-ET-26** (railWidth from `tileSnapshot[0]`, sticky zero collapse; the same self-contradicting index doctrine). Reader-1 re-found a banked row (registry-blindness, corrections #4). Zero re-booking.
- **KF-ES-33 · L-m8 + L-m9 — FOLD by reference to banked KF-ET-30 / KF-ET-28** (the async `wirePainter` posture bundle; the `?? ""` decoy that guarantees the throw it appears to prevent). Zero re-booking.
- **KF-ES-34 · D-m2 + D-m8 + D-m12 + D-i5 — FOLD by reference to banked KF-ET-31 / KF-ET-16 / KF-ET-22** (28 permanent `will-change` promotions incl. under PRM; no forced-colors arm demo-side [KF-CE-13 family — glass-chip's `prefers-contrast` arm is in the orphaned sheet, so even it does not load]; caps-tracking on lowercase + two trackings for one token; the dead `.literal-text` text-transform reset corroborating the two-utilities confusion). Zero re-booking.
- **KF-ES-35 · D-m5 + D-m9 + D-m4 — the header/sidebar residual set (new bookings on adjudicated-adjacent surfaces)**: exactly four specimens print the identical token twice in two typefaces beside a copy button (enumeration verified: `smooth-step-3`, `ease-in-bounce`, `step-start`, `step-end` — rides the KF-ET-1 literal-contract spec); the header literal snaps horizontally on every selection with no width reservation (the KF-ET-19 seam's second axis; magnitude → SS-13); `label="duration"` breaks the surface's sentence-case and the unit lives only in a hover tooltip (peers verified). **MINOR bundle.** → **NO-WAVE-OWNER**.
- **KF-ES-36 · D-m11 + M-5 — FOLD by reference to banked KF-ET-34, EXTENDED with two sites**: the "33 specimens" falsehood also lives in `font-roles.json`'s `_doc` prose (verified; the tree yields 28), and `EasingTarget.css:29` names a curve (`bounce-in-ease-half`) that exists nowhere — the identical stale name was fixed in EasingTarget.vue:217 one file over. The gate-input escalation stays killed (`monoCeiling: 90` is the consumed value — verified). Zero re-booking; the prose sweep (KF.W5) gains both sites.

### INFO

- **KF-ES-37 · D-m1 as-narrowed (ruling #5)** — one root-scoped unprefixed property (`--ball-tone`), three on childless leaf spans; no `--ball-*`/`--rail-tint` in the producer; the counterweight is the seam-consumed-as-documented superlative. → NO-WAVE-OWNER observation (namespace on next touch).
- **KF-ES-38 · D-m10 + M-8 — FOLD to the banked SUP-3 ruling** (kf-EasingTarget ruled table): the provide/prop split across the render-fn teleport is a forced, CORRECT boundary call; the defect residue is only that the 9-line comment above `tabsContent` never names the trap. Both this corpus's readers converged on exactly that disposition ("preserve the mechanism, add the comment"). Zero re-booking.
- **KF-ES-39 · MISS-6 (reader-2, ADMITTED) — EasingSidebar snapshots its reactive prop into a plain local at setup** (`const demo = props.demo`, verified) — harmless at HEAD (stable object), a staleness trap of the no-shadow-authority class, invisible under C-7. → NO-WAVE-OWNER.
- **KF-ES-40 · the dead-surface bundle** — `computed` imported and never used (:8); `const isPlaying = demo.isPlaying` never read (:45; SpringScene carries the twin); the `defineExpose` comment describing a `scenePlayback` key that is not exposed (:127-129 — the registration rides `facility.playback`); `SCENE_ID` as a one-use indirection (:18); two separate `@mkbabb/value.js/math` import statements in useEasingDemo (:2/:22). All verified. → NO-WAVE-OWNER hygiene, detectable only after KF-ES-16's toolchain rider.
- **KF-ES-41 · L-i1 — "zero rAF ticks at rest" is off by one tick**: `startLoop()` unconditional at composable init (verified); the first frame observes not-playing, writes `progress`, returns false. Comment truth; the perf substance survives. → NO-WAVE-OWNER (correct the :119-125 prose on next touch).
- **KF-ES-42 · L-i4 = C-13.2 — FOLD by reference to banked KF-APP-29** (`autoPlays` prose names easing as the `true` example; both producers `false`; the code is correct). Zero re-booking.
- **KF-ES-43 · D-i1 — the shell is a changelog: 58 comment lines to 56 code lines, documenting deletions** — and the three comments describing current behavior are the false ones (:23-27 → KF-ES-2; :79-80 → KF-ES-5; :119-125 → KF-ES-41; plus :127-129 → KF-ES-40). The oracular-comment pattern banked as KF-ET-37 recurs one file up. → NO-WAVE-OWNER.
- **KF-ES-44 · D-i4 + D-i6 — inert centering** (`items-center justify-center` around an `h-full w-full` child — verified both files) **and the rest state carries no in-scene invitation to press** (`autoPlays: false`; no play affordance in the target's template; at rest the drawer is the KF-ET-8/KF-ES-24 low-contrast field with 28 dots at x=0). → NO-WAVE-OWNER + SS-13 (dock-Play sufficiency).
- **KF-ES-45 · M-4 (reader-1, ADMITTED) — the focus ring's 8 px soft halo clips against the grid's 2 px padding on the leading row/column** (the residue of the sustained D-i7 kill: the hard 2 px ring clears exactly, with zero tolerance against any producer `--focus-ring-width` bump). → NO-WAVE-OWNER observation; rides KF.W6.

## Killed-claims register — 18 entries

1. **D-i3 "tautological guard"** — CONVERGENT KILL SUSTAINED: the surface set is 4-wide (KF-ES-2), `selectedControl` takes `keyframes`/`timeline`, the guard is live and its null arm is C-2's defect. D-i3 asserts the exact opposite of the corpus's own C-1/C-2.
2. **C-10 "not CSS for the majority (26 of ~31)"** — KILL SUSTAINED (ruling #4): 2 of 28; `step-start`/`step-end` parse (my probes); tile count is 28, not ~31. Reader-2's confirmation cell REVERSED. Convergent with banked KF-ET-2.
3. **D-M4(b) "descriptions consumed by NO template"** — KILL SUSTAINED: ChannelOptions.vue:267 renders `curveItem.description` — reachable in this very scene's dock under KF-ES-2. Sharpens banked KF-ET-31 (KF-ES-27).
4. **D-B1's cure arithmetic ("α is off by ~3×; land 0.70") and its max-ink figure (1.58)** — KILLS SUSTAINED (both readers convergent): black@0.22 over the light card = 1.69; 3:1 clears at α≈0.42 (fg ≈0.47/0.39 per theme); 0.70 = 8.23:1, over-inked by more than a stop. The measurement table survives whole (KF-ES-26); the prescription must not be quoted into a repair spec.
5. **D-M2(cause) "the demo throws away glass-ui's solved `--accent-ink`"** — KILL SUSTAINED (`.tile-name`'s own `color` beats the inherited ink in both states) AND SUPERSEDED: the "solved ink" lives in the orphaned glass-chip.css and never loads at all (KF-ES-25).
6. **D-S7's provenance ("18% headroom measured against ease-in-out-back, ~3× slack")** — KILL SUSTAINED: the worst rendered specimen is `ease-in-bounce` at 1.183 ⇒ 1.54× slack. Convergent with the banked L-7 superlative's own numbers (which praise the allowance at the correct margin).
7. **L-B1/C-4's "permanently, silently"** — KILL SUSTAINED: the ribbon remounts on a surface bounce (RibbonBar's v-if chain). Convergent with the banked KF-CO-15 §B correction — the two records now agree from two independent instances.
8. **L-m6/C-5's "`liveProgress`: external consumers 0 / dead"** — KILL SUSTAINED (both readers) and EXTENDED into a correction against banked KF-ET-20 (corrections #7): the channel closure is called per-frame by two dock consumers, verified live.
9. **D-i7 "focus ring clips ~2px"** — KILL SUSTAINED from source: no `outline-offset`; the 2 px hard ring clears the 2 px padding exactly. Residue (the 8 px glow) admitted as KF-ES-45.
10. **C-9's reseat/compounding clauses** — KILLS SUSTAINED: `cssValue` returns the NAME for every non-custom curve, and `previewAnim`'s compiled curve is sampled by nothing on screen. Convergent with the banked KF-ET-2 cure-lock.
11. **C-13(1) "imported by EasingScene and SpringScene only"** — KILL SUSTAINED (three importers; ChannelOptions :428). Reader-1's residue clause ("the cube/amiga comments remain false") REVERSED by ruling #2 — the comment is substantially true via the shared pane.
12. **D-m11's "stale gate input"** — KILL SUSTAINED: the gate consumes `monoCeiling: 90` (verified); the "33" is `_doc` prose drift. The staleness itself is booked (KF-ES-36).
13. **D-m1's four-property blast radius** — NARROWING SUSTAINED (ruling #5): one inheriting property, three on leaves.
14. **D-m7's family-wide `seedFor` null** — KILL SUSTAINED: `smooth-step-3` IS in `bezierPresets` (probe) and remounts the picker; the null branch is reached by exactly `ease-in-bounce` (1 of 28). The smooth-step-3 defect is KF-ET-6's, not this one's.
15. **Corpus provenance ("working tree dirty only in `.github/workflows/*`" / "no writes … HEAD 8281638c")** — KILL SUSTAINED AS DATED: 252 dirty paths incl. 8 in the read set — but every one of those files is byte-identical to `origin/master` (this seat's per-file diffs), so the corpus audited the FRONTIER's bytes and no verdict is voided. The provenance statement is false; the findings are frontier-durable. SCH-1 owns the reconciliation.
16. **D-B2's arithmetic aside ("p=φ except φ ∈ {0, ½}")** — KILL SUSTAINED: fixed points are {0, 2/3}; φ=½ maps to p=1. The defect broadens trivially; the verdict is untouched.
17. **C-2's "three surfaces / the ONLY transport unmounts"** — reader-2's NARROWING SUSTAINED, reader-1's "worse than stated — not rendered AT ALL, no fallback" REVERSED (ruling #1): two surfaces lose the transport; `controls` carries the teleported instance B.
18. **Reader-1's kill of C-1's Monaco clause ("reachable, not automatic — hover/focus only")** — THE KILL IS KILLED (ruling #3): `scheduleIdleWarm()` is unconditional at setup with a 4 s idle timeout; the hover bindings are accelerators. The challenge cell is restored as rescoped ("auto-mounted on the first idle tick") and folds to banked kf-ChannelControls L-3.

## Adjudicator corrections register

1. **Reader-1's C-2 cell** — "no fallback transport exists" contradicted by its own C-13(1) kill three rows later; reversed at the bytes (ruling #1). A return that kills a census in one cell must re-price every cell that assumed the census.
2. **Reader-1's C-1(monaco) kill** — killed a true claim by reading the accelerator bindings and not the composable one function above them (ruling #3). The banked kf-ChannelControls record had the mechanism; consulting the registry would have prevented the kill.
3. **Reader-2's C-10 confirmation** — endorsed the challenge's "majority" magnitude against its own probe set (ruling #4); and its C-13.1 kill's counter-claim ("the comment is substantially true") is adopted while reader-1's mirror-image residue clause is struck (ruling #2).
4. **Reader-1's M-1 and M-7 "misses"** — both are banked rows (KF-ET-1, KF-ET-26), adjudicated in this registry before this corpus's return; presented as novel. Registry-blindness, the same class kf-EasingTarget's correction #4 recorded. The finds are correct; the novelty framing is not.
5. **Both readers' frontier blindness** — neither ran a single `git diff origin/master`; both repeated the "phantom dep / npm ci fails" framing the SCH-1 bank dates, and both audited a "dirty" tree that is in fact the frontier's bytes on every file they read. Fourth and fifth occurrences in the program (kf-App, kf-CopyButton, kf-EasingTarget ×2 readers).
6. **Both readers' glass-chip arithmetic** — D-B1's resting-ground mechanism and D-M2's selected-band figures compute over `glass-chip.css` rules that the installed producer never loads (the banked KF-ET-4 orphan, re-verified). The shipped-truth figures are their own against-card rows; the band figures price the post-cure state (KF-ES-25/-26).
7. **Against banked KF-ET-20** (cross-registry, recorded here because this corpus's evidence is decisive; the banked file is dated evidence and is not rewritten): the five-member dead-export roster over-counts by one — `liveProgress` is live via the facility channel closure with two per-frame dock consumers (verified at AnimationControlsGroup :259 + useAnimationProgress :24). The corrected roster is four; `cssValue` is export-dead but internally load-bearing. Grep-identity and data-flow are different instruments; a dead-export claim must run both.

## Superlatives — sustained, with provenance

1. **L-S1 — the engine consumed in exactly its documented stateless mode** (`sweep.at(phase)`, zero-allocation path, value read immediately) — convergent with the banked kf-EasingTarget SUP; the recorded irony stands: the ping-pong-as-keyframes elegance is precisely what makes KF-ES-1 a teleport (the triangle owns the alternation the clock math forgot).
2. **L-S2 — the scene owns none of its lifecycle, and that is why it does not leak**: no onMounted/onUnmounted/rAF/listener in the shell; everything hazardous is structural in useSweepScene (bound loops, `onScopeDispose`, visibility pause), with the engine's arrow-class-field bind-proofing beneath (verified at playback.ts by the banked kf-AV seat).
3. **S-1 / S-2 / L-S4 / S-3-as-corrected — the EasingTarget craft set rides on the banked record**: half-pixel tick registration (KF-ET P-8), `vector-effect` under anisotropy (D-S-2), painter keyed by `data-curve` not index (L-S4), and the three-mechanism off-screen gate — **with reader-1's S-3 pre-arm clause STRUCK per banked KF-ET-23** (the 25% rootMargin cannot pre-arm through the FadingScroll clipper; the praise survives at two mechanisms plus intrinsic-size honesty, not three-plus-preroll).
4. **S-5 — the no-truncation promise kept by construction** (no ellipsis/clamp/fixed widths; `min-width: 0` where it must be) — sustained with the banked KF-ET-22 caveat (the inherited caps tracking is what breaks the promise at the 150 px floor).
5. **S-6 — reduced motion is reactive, not read-once** (re-wire on media-query flip; rest at `fn(1)`) — banked D-S-6; KF-ET-7's PRM-deletes-the-pedagogy remains the counterweight.
6. **SUP-1 — the light/heavy engine boundary honoured exactly** (static `NumericAnimation` from the light barrel; `CSSKeyframesAnimation` only through the dynamic accessor).
7. **SUP-2 — the value.js consumption is the model the glass-ui consumption is not**: subpath-only, real named exports, exact-pinned 4.0.0, reproducible under `npm ci` — the precise inverse of F-1 in the same tree.
8. **SUP-3 / L-S3 — the provide/prop split across the render-fn teleport is a forced correct boundary call** — the banked ruling adopted whole; both readers converged on it independently (KF-ES-38).
9. **Reader-2's find, ADOPTED — `usePainterRegistry` is a 20-line seam with nothing wrong in it**: paint-once-on-register, self-returning unregister, no reactive wrapper — consumed exactly correctly at both re-wire and dispose.
10. **Reader-2's find, ADOPTED — `useThrottledReadout` seeds `lastAt = -Infinity`** ("no phantom initial delay") and argues its clock-gated shape from the call sites — the correct common denominator; the blemish is KF-ES-19 (easing adopted half of it).
11. **Reader-1's find, ADOPTED — `timingCurveUtils`' single `requireEasing` funnel** over the Result API (three named helpers, source-tagged throws; glass-ui's bundle converged on the identical shape independently) — with the banked KF-ET-28 posture note (the funnel throws where the render path needed degrade).
12. **Reader-1's find, ADOPTED — the CSS half of the rail/ball seam consumed exactly as `design-idioms.css` documents** (all four drift-axis parameters, the 1 px rail a named delta with written reasoning) — the counterweight inside KF-ES-29/KF-ET-27's JS-half indictment.
13. **The reference transport exists and is right** (this seat's addition): ChannelOptions' teleported PlaybackRibbon — effective-t convention, real `started`, wake wired on every scrub entry — is the in-tree proof that every convention KF-ES-5/-6/-18 indicts was already solved one directory away.
14. **`useSweepScene`'s charter honesty**: the composable's docstring names the exact crash class it exists to make unrepeatable and delivers it (bound callbacks throughout) — the one false sentence in it (the "resumes in phase" clause) is KF-ES-1's, not the seam's.
15. **The corpus polices itself**: reader-1 killed six of its own axis's cells including the challenge's strongest rhetorical flourishes (C-10's "majority", D-B1's "×3"), and reader-2 narrowed its own side's C-2 — both returns are more trustworthy for it; this seat concurs with the sibling records that said the same of theirs.

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **KF-ES-3's escalation trigger** — set the picker to steps=1 (or click the `step-start` tile) and open the term control: is `jump-none` offered? If selecting it faults the render, the row escalates to BLOCKER; also decides L-i2's ordering question (does the picker fault before the demo's `stepOptions` ever updates).
2. **KF-ES-2's rendered consequence** — the four dock tabs, the ChannelOptions pane over a targetless animation, the empty `KeyframeTimeline`, and the idle-tick Monaco mount, on the easing scene as shipped.
3. **KF-ES-1's one-gesture witness** — play, pause mid-sweep (φ≈0.25), resume: all 28 balls jump half the rail together.
4. **KF-ES-6's witness** — bounce easing↔controls with Reverse on: two ribbons over one clock with mirrored thumbs.
5. **KF-ES-7's perceptibility** — the ~167 ms thumb/ball staircase at 1500 ms and 5000 ms.
6. **KF-ES-24's glance** — canonical-shape legibility of the 3.97:1 portraits at the 150 px floor and the widest track.
7. **KF-ES-31 + KF-ET-26 residue (shared witness)** — screen-reader enumeration of scrolled-away specimen buttons; `clientWidth` reads inside skipped tiles after scroll+resize.
8. **KF-ES-25's post-cure arm** — the selected-label composite (band + `plus-lighter` flood) in both themes, IF the producer restores the glass-chip import (rides KF-ET-4's BH letter).
9. **KF-ES-44's question** — whether the dock's Play carries a first impression the scene itself declines to make.
10. **KF-ES-35's magnitude** — the header literal's horizontal travel across the 28 selections at common breakpoints.

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c`, whose entire demo-side read set is byte-identical to `origin/master` `81a56990` (per-file diffs run by this seat — every row frontier-durable by identity; engine anchors worktree-only, re-resolve at cure). 45 roster rows — 2 BLOCKER (the resume clock fed the sweep's output where its phase belongs, now CURE-LOCKED by this seat's non-invertibility derivation: the snapshot contract itself is under-determined, so the fix must persist phase, not invert better; and the facility that declares a painting channel over a targetless never-played animation, quadrupling the scene's control surfaces and auto-mounting Monaco on an idle tick) · 15 MAJOR · 19 MINOR · 9 INFO, with 17 folds by reference into the banked registry (KF-CO-15, KF-AV-1, KF-APP-29, SCH-1, KF-CB-8, and twelve KF-ET identities) and ZERO re-bookings. 18 killed-claims entries: 15 kills sustained, one reader kill itself killed at the bytes (Monaco warms unconditionally on idle — the hover bindings reader-1 stopped at are accelerators), and two reader confirmations reversed (C-10's false majority; C-2's false no-transport). 14 misses admitted — the dual-ribbon contradiction, the duration-slider teleport, the authored-quad destruction, the prose-only transport contract chief among them — three of them registry-blind re-finds of banked rows, and one cross-registry correction issued the other way (banked KF-ET-20's dead-export roster shrinks by the live facility channel). The pattern of this component is now measured twice from two corpora: an exemplary hot path and seam discipline (fifteen sustained superlatives, most banked) wearing a transport and derivation layer whose every load-bearing comment is false — the clock comment, the surface comment, the reverse comment, the zero-tick comment, the resume-in-phase comment — and whose reference implementation, correct on all three disputed conventions, was mounted one directory away the whole time. Routings are M-25 agglomeration input: behavioral rows NO-WAVE-OWNER for SS-1/SS-2; the picker crash GLASS-OWNED on the BH relay with a named escalation trigger; structural folds to KF.W0/W4/W5/W6/W9; 10 witnesses to SS-13. Nothing here stamps a wave; X·KF executes on the owner's begin-word.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.
