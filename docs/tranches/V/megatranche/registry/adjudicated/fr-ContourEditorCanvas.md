claude-fable-5

# fr-ContourEditorCanvas — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-08 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourEditorCanvas.vue` (340 lines) + its drag/history/contourEditing/consumer chain.
**Substrate**: fourier HEAD `cd26c653` / re-pinned by this seat (`git log -1`) — identical to the fr-BasisCanvas pin. **Dirty-tree disclosure**: 28 dirty rows; **the subject file, both composables (`usePointDrag.ts`, `useContourHistory.ts`), `contourEditing.ts`, `ContourPreview.vue`, `useViewTransform.ts`, `colors.ts`, `golden-shimmer.ts` and the whole `api/` chain are CLEAN at HEAD**. Four cited consumers are drifted (`SliderControl.vue`, `EditorControlsDock.vue`, `VisualizationView.vue`, `BasisSelector.vue`) by equal-length in-place substitutions only (glass-ui renames: `MetricBadge :amount→:value`, `Slider variant glass-scrubber→standard`) — diffed hunk-by-hunk by this seat; **every line anchor in this record is stable across the drift** and quotes reflect the working tree SS-13 will see.
**Corpus**: ~79 accusation rows across three axes at `docs/tranches/V/megatranche/audit/fourier-components/ContourEditorCanvas/` — challenge-D (design, 876 lines, incl. its own §8 self-correction table), challenge-L (library, 266), challenge-C (consumption, 363).
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Returns arrived through the orchestration channel; every load-bearing or disputed cell was **re-derived by this seat against the tree with its own instruments** before adoption.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. Every kill was re-derived (a weak kill gets un-killed — none needed un-killing, but two reader cells died under this seat's own reads: K-18, K-19). Every missed find was re-proven before banking; one reader missed find was killed outright (K-18) and two were caught by the identity guard as already-banked fr-BasisCanvas rows (folded, never re-booked). All reader-vs-reader conflicts ruled with fresh reads (R-1..R-10).
**Arbiter's own instruments this session (all static + node; zero browser probes — probe parsimony; product source read-only)**: full reads of ContourEditorCanvas.vue (340), usePointDrag.ts (67), useContourHistory.ts (36), contourEditing.ts (222), ContourPreview.vue (62), useViewTransform.ts (77), EditorControlsDock.vue (230), colors.ts (whole), golden-shimmer.ts head, defaults.ts, index.html:1-35, App.vue:1-20; targeted reads of VisualizationView.vue (:70-100, :195-266, :388-450), FullscreenViewer.vue (:30-50, :100-135, :160-175), CanvasControlsDock.vue (:40-100), SliderControl.vue (:60-90), workspace.ts (:40-50, :90-110, :145-160, :262-278), api.ts (:290-300), useImageOverlay.ts (:10-18, :73-90), BasisCanvas.vue:521; api tree: routers/contours.py, services/image_storage.py:265-340 (hash + upsert verbatim), responses.py, dependencies.py:95-165, services/computation.py:60-96; census lane-frontend.md rows :118/:122/:213/:219/:241-252/:600-622; style.css:90-145; installed glass-ui 4.0.0 dist: `--card`/`--background` both arms, viz-token oklch forms, `--control-floor`, `--z-controls`/`--z-fullscreen`, dock.js collapseDelay defaults (hook 2500 / component 2e3); installed value.js 0.13.0 (version + export spot-checks incl. `parseCssColor` ABSENT / `parseCSSColor` present); node runs: exact sRGB contrast suite (no 8-bit rounding), target-size closed forms, **magnet negative-index crash simulation (throws at n=6/r=7/idx=0 and n=6/r=10/idx=3; clean at r=6)**, degenerate viewBox evaluation (`"Infinity Infinity -Infinity -Infinity"`), shimmer period (2π·200 ≈ 1257 ms); repo greps: `touch-action` (2, both paper/), editor terms in e2e (0 across 8 specs), `infinite` in visualization (3: one guarded), PRM in subject (0), `hsl(40 90% 55%` (8 + 1 in preview), `aria-|role=|focus` in subject (0), `zoom|wheel|pan(` in visualization (0), v-for total (33) + same-line (8, listed), value.js importers (4 files).

## Scoreboard

**47 rows ADJUDICATED-CONFIRMED (5 BLOCKER · 20 MAJOR · 18 MINOR · 4 INFO) · 19 KILLS in the killed register (incl. 2 minted by this seat against the readers) · 14 missed finds banked (12 rows + 2 identity folds; 1 reader missed find killed) · 9 superlatives with provenance.**

Blocker-weight survivors: **D/B-3·L-1·C-1** (window-scoped keydown destroys geometry from inside live text fields and swallows ⌘Z route-wide), **D/B-5·L-2·C-3** (fullscreen mounts a second unwired editor — two keymaps, two histories, silent edit loss), **D/B-6·L-3·C-4** (the primary operation never emits `stateChange` — Undo lies disabled, the green check lies saved), **D/B-2** (100% pointer-only grammar on the product's only direct-manipulation surface), **C-2** (`POST /api/contours` never derives `image_bounds` — the reference overlay dies on every genuine edit-save).

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing is agglomeration input (M-25); nothing here stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **D/B-1 severity** (8 amber literals sub-3:1 light) | BLOCKER | MAJOR | **MAJOR** | My exact-hsl node suite: opaque 1.810 / α.85 1.660 / α.6 1.433 / α.5 1.350 against light `--card` (lum 0.94212); dark passes 7.612/5.922. Light-arm-only failure with marks visible-if-faint; precedent fr-BasisCanvas D-7 (both-theme canvas text failures) = MAJOR. DU's BLOCKER preserved in DISSENT. Where the readers' figures diverged (DU 1.810 vs LC 1.802), **DU's derivation is the exact one** — LC composed from 8-bit-rounded channels. |
| R-2 | **D/B-4 severity** (≤9 px hit targets) | BLOCKER | MAJOR (SC 2.5.8 "essential" exception unaddressed) | **MAJOR** | Closed forms re-derived: outer = 2(3.5s+1.25) ≥ 24 ⇒ s ≥ 3.0714 ⇒ W ≥ 4088.7 px @ w=1024, 2395.7 @ w=600 — 24 px is unreachable at any realistic stage, and ~5-6 px targets at real widths are a hard usability defect regardless of citation. But a vertex handle whose screen position IS the datum has a live 2.5.8-essential/spacing argument neither the corpus nor DU addressed; the WCAG grade is a judgment call routed to UNPROVEN #6, so the row cannot carry BLOCKER on the citation alone. |
| R-3 | **D/M-8** (dblclick on a vertex splices a near-coincident duplicate) | CONFIRMED MAJOR (spec leg flagged) | UNPROVEN (engine-dependent) | **CONFIRMED MAJOR — DU wins, on spec grounds this seat supplies** | Wiring re-read: `@dblclick` unmodified on the `<svg>` (:237), `stopPropagation` on pointerdown only (usePointDrag.ts:17), no proximity test (:145-152), `nearestSegmentIndex` always returns `i+1`. The confounder both axes missed — `e.preventDefault()` on pointerdown (:16) — does not decide against the claim: per the Pointer Events compatibility mapping, canceling `pointerdown` suppresses compatibility **mouse** events; `click`/`dblclick`/`contextmenu` are device-independent activation events explicitly outside that suppression set. SS-13's dual-engine witness is demoted to belt-and-braces, not the decider. |
| R-4 | **L-8 mechanism** (per-move cost) | Confirmed wholesale | Confirmed substance; KILLED the event-rate arm | **LC wins (K-12); row stays MAJOR** | Current engines coalesce `pointermove` dispatch to ≤1/frame; `getCoalescedEvents()` is the opt-IN to the merged higher-rate samples, so its absence means fewer invocations, not 120 Hz+. The surviving substance is mine to restate: per FRAME, one forced layout (`getScreenCTM` inside `svgPoint`, hoistable because no pan/zoom exists), TWO O(n) spline rebuilds (editor :85 + preview via the exposed array), an O(n) bbox sweep in the preview (MM-7 — a THIRD sweep no axis counted), and n `<circle>` patches. |
| R-5 | **D/M-2·L-14 severity** (ungated `infinite` filter animation) | MAJOR | MINOR | **MAJOR** | `:hover`-gating is no mitigation on a work surface — during editing the pointer is over the SVG continuously, so the animation is effectively always-on during use; zero PRM in the file against the tree's own D.W4.c precedent (census PRM table re-read: 8 CSS reduce blocks, this file absent) and the installed producer ships no global PRM reset (fr-BasisCanvas instrument, re-cited). Consistency with fr-BasisCanvas D-8 (MAJOR). LC's MINOR preserved in DISSENT. MM-9 (the 5 px bloom vs ~1.8 px marks) rides this row. |
| R-6 | **D/M-1 severity** (invisible purposeless tab stop) | MAJOR | MINOR | **MINOR** | The keyboard capability loss is B-2's BLOCKER; this row's increment is the void stop + the FullscreenViewer trap landing on it (`[tabindex]:not([tabindex="-1"])` in FOCUSABLE, my read :37-46). Hygiene layer of an already-booked capability absence. |
| R-7 | **D/M-5 severity** (chassis divergence in the crossfade box) | MAJOR | (silent) | **MINOR** | BasisCanvas.vue:521 `cartoon-card` vs the bespoke shell (:285-297) verified; both `inset:0` in the same slot (VV:392-403, "no layout shift" comment read); FullscreenViewer normalizes both via `:deep()` in one mount only (:163-171). Same defect class fr-BasisCanvas R-5 ruled MINOR for D-12 (chrome composition, no capability loss). |
| R-8 | **L-9 severity** (`lostpointercapture` unhandled) | MAJOR | MINOR | **MINOR** | The absence is certain (grep 0 in both files; sole `dragging=false` at :58) but the entry is contingent on mid-drag element removal; the sharp consequence is booked separately at MAJOR (MM-2, the mid-drag-undo row) where the entry is one unguarded ⌘Z. |
| R-9 | **L-21 severity** (zero automated coverage) | MINOR | INFO | **MINOR** | 0 hits for editor terms across all 8 e2e specs, no vitest config, no test script (my greps). Load-bearing for this corpus: every cure needs a born-RED harness that does not exist; the repair wave must author it, so the absence is a defect of the surface, not a census note. |
| R-10 | **C-18** (getScreenCTM null → TypeError) vs D/i-2 (unguarded `!`s) | folded i-2 at MINOR | KILLED C-18's reachability | **LC's kill adopted (K-13); the `!`s ride the m-11/m-12/i-2 hygiene row** | `svgPoint` is reachable only from `onPointerMove` under `dragging === true`, which requires pointerdown on a rendered `<circle>`; Transition-leave keeps the element rendered; the `display:none` mobile case can't start a drag. Hygiene fact stays booked. |

**Numeric-precedence rule from R-1, applied corpus-wide**: where DU and LC disagree on a derived figure, this seat re-derived it; DU's arithmetic was exact in every tested case (1.810, 1.019 ring ratio, 5.3 px C-17 correction, calibrations 3.552→my 3.558 / 4.706→my 4.709 — the ±0.005 is my exact-hsl vs their 8-bit inputs, immaterial).

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · terminal disposition → route** (F.W* per the census lane taxonomy; routings are M-25 agglomeration input). Full proof cells live in the immutable challenge files; anchors re-verified by this seat unless marked (adopted).

### BLOCKER

- **D/B-3 · L-1 · C-1 — BLOCKER.** `window`-scoped keydown with no target/focus/mode guard: :209-215 binds window; :164-177 reads neither `e.target` nor `activeElement`; three props only (:18-22); the component is hidden-not-unmounted (`is-hidden` = opacity 0/z 0/pointer-events none, VV:404-409 — my read of the scoped block). Live editable fields are co-mounted IN the editing panel: `ContourSettings` renders inside the `isEditing` branch (VV:265) and each of its five `SliderControl`s (:230/:243/:269/:282/:295) hosts a real `<input :type="isNumericDisplay ? 'number' : 'text'">` (SliderControl.vue:71-79, re-read post-drift); `BasisSelector.vue:157/:184` add the non-editing pair. Backspace deletes a vertex from inside a text field; ⌘Z never reaches native undo. All 7 `deselect()` sites re-enumerated (:57,113,119,160,182,189,196) — none is a blur/mode watcher. **ADJUDICATED → F.W3/W4** (guard on target/mode; MM-8 rides: `preventDefault` at :166 fires before the :180 floor, so at n≤3 the keystroke is stolen with no action at all).
- **D/B-5 · L-2 · C-3 — BLOCKER.** FullscreenViewer.vue:115-120 mounts a second `<ContourEditorCanvas>` with three props and no ref/listeners; :130 gates the rail on `!isEditing`; saves read only the inline `editorRef` (VV:82-96, :204). Reachability confirmed by my read: CanvasControlsDock renders Edit AND Fullscreen OUTSIDE the `v-if="!isEditing"` template — fullscreen-while-editing is one click. Two window keymaps, two `useContourHistory` closures, edits in fullscreen silently discarded; `--z-controls: 20` vs `--z-fullscreen: 150` verified in the installed dist (save dock provably buried). **ADJUDICATED → F.W3/W4** (one instance or one shared state; note the toolbar-in-fullscreen cure lands in EditorControlsDock = the MM-11/F.W1 break surface — sequencing rider below).
- **D/B-6 · L-3 · C-4 — BLOCKER.** The drag path never emits: :42-47 destructures `onPointerUp` raw while `rawPointPointerDown` on the same line is wrapped at :154-157 precisely to add `emitState()`; :239-240 bind raw; usePointDrag.ts:54-60 calls only `onDragEnd` (= `pushHistory`, :46); `emit(` appears once in the file (:124). Consumer: `editorSaved` written only at VV:90 (clear)/:95 (set); dock `:disabled="!canUndo"` (:74). Nothing else catches the loss: workspace.ts:108 watches settings only; `_saveDraftNow` (:93-106) serializes `contour.value`, never the buffer. After the primary operation, Undo renders disabled and the green check asserts saved over unsaved geometry. **ADJUDICATED → F.W3/W4.** L-3's root-cause CURE is killed separately (K-14); the sound repair is DU's — wrap `onPointerUp` exactly as pointerdown is wrapped.
- **D/B-2 — BLOCKER.** 100% pointer-only grammar: the `<circle>` carries exactly one binding (`@pointerdown`, :277); zero `aria-`/`role=`/`keydown`-on-element in the file (my grep: 0); insert is `@dblclick`, move is pointer-capture drag, and the dock cannot rescue it — `canDelete` is `selectedIdx !== null` (:127), which only a pointer writes (usePointDrag.ts:18). SC 2.1.1's path-dependence exception does not apply (vertex placement is endpoint-dependent). **ADJUDICATED → F.W3/W4** (keyboard select/move/insert path + M-1's tab stop becomes real).
- **C-2 — BLOCKER.** POST never derives bounds: contours.py `store_contour_asset(xs, ys, req.image_slug, source="editor")` (no `image_bounds` arg — my read); the param defaults `None` and is written verbatim (image_storage.py:290/:317); responses.py echoes `asset.get("image_bounds")`; backfill is GET-only (dependencies.py:102-103, hardcoding resize 1024 at :151). Client: workspace.ts:272 installs the response → `imageOverlayRect` null (:99-108) → the `<image>` v-if drops (:246). The tree's own workaround comment sits on the LOAD path (workspace.ts:152-154, read). Refinement ratified (K-3's converse): the content-addressed hash means a no-op save re-hits the extraction doc WITH bounds — **the overlay dies exactly when the points changed**, i.e. on every genuine edit. LC's scope addition verified: useImageOverlay.ts:77-78 early-returns on null bounds, so the Canvas2D overlay dies at the same moment. **ADJUDICATED → F.W5–W8** (the persistence-contract seam owns it; derive bounds on POST or backfill on write).

### MAJOR

- **D/B-1 · C-7 · L-15(token arm) — MAJOR** (R-1). Eight `hsl(40 90% 55%)` literals (:261,307,308,315,319,328,329,333 — grep -c = 8) + a ninth at ContourPreview.vue:45, on a `var(--card)` surface, all below SC 1.4.11's 3:1 in light mode (my exact suite: 1.810/1.660/1.433/1.350; dark 7.612/5.922); routes around the repo's own D.W4.d `--viz-amber` carry (style.css, re-read: light `hsl(35 76% 35%)` = my 4.709 vs `--background`). Cure note with teeth: the CSS cure (`var(--viz-amber)`, hsl-authored locally, both arms tuned) is sound and lands in F.W3/W4; the JS cure (`VIZ_COLORS`) is UNSOUND until fr-BasisCanvas D-1 lands (the resolver greys oklch tokens) — cure adjudicated separately from defect. **ADJUDICATED → F.W3/W4**, token-substrate rider → F.W1.
- **D/B-4 · C-17 — MAJOR** (R-2). `r=3.5` data units + fixed 2.5 px stroke + ×1.3 fit ⇒ target ≤ ~6.2 px at a 700 px stage (w=1024); 24 px needs W ≥ 4089 px. Bounds re-derived by this seat; C-17's own "~8 px" killed (K-7). glass-ui's coarse-pointer floor (`--control-floor: var(--touch-target, 2.75rem)`, dist re-read) is opted out of by the bespoke surface. **ADJUDICATED → F.W3/W4** (screen-space handle radius); the 2.5.8-essential judgment → UNPROVEN #6.
- **L-4 · C-10 — MAJOR.** Magnet aliasing: no `n` term, no distinctness test (usePointDrag.ts:36-48, read); at `offset ≡ 0 (mod n)` the dragged vertex displaces itself — additive reads-of-self compose to `pt + 0.5·d` at n=3/r=3 (C-10 right; L-4's worked example killed, K-11). Domain verified: delete floor 3 (:180), simplify floor 6 (contourEditing.ts:99), dock slider max 10 with the sole clamp in the dock setter (:51). **ADJUDICATED → F.W3/W4** (bound offsets by ⌊(n-1)/2⌋ and dedupe).
- **MM-1 — MAJOR (missed find, reader-LC; CONFIRMED BY THIS SEAT'S SIMULATION).** The regime past aliasing: JS `%` is remainder, so `before = (idx - offset + n) % n` goes NEGATIVE for `offset > idx + n` — `points[-1]` is `undefined` → TypeError on every subsequent pointermove. My node run: n=6/r=7/idx=0 THROWS; n=6/r=10/idx=3 THROWS; n=6/r=6 clean. Reachable in-UI: ~23 Simplify presses walk 1024→6 (`toRemove = max(1, floor(n·0.2))`, my read of :102-103), dock permits radius 7-10. Aftermath ratified: the throw aborts before `dragPrevPt` advances (:51), so the stale delta base compounds while `dragging` stays true and pointerup still commits the corrupted array. **ADJUDICATED → F.W3/W4** (same cure as L-4).
- **L-5 — MAJOR.** Unbounded deep-reactive undo stack over a 1024-point default (all 36 lines read: no cap, slice+push, deep copies at 4 sites; `points` is `ref` not `shallowRef`; 4096 reachable). Census contradiction UPHELD by my read: lane-frontend.md:122 says "undo/redo ring" — there is no ring. **ADJUDICATED → F.W3/W4** (cap + structural sharing candidate); census correction rides MM-6.
- **L-6 · C-6 — MAJOR (consequence arm killed, K-19).** `defineExpose` publishes `points` and `magnetRadius` as a live read-WRITE channel (:217-227); VV:83-85 writes through the proxy; :262 hands the live array to `ContourPreview` — the exact idiom the same file's B.W2 comment records as retired (VV:75-78, read). The 0-vs-3 default contradiction (getter `?? 0` vs child `ref(3)`) is REAL as a latent two-sources-of-truth hazard but its claimed visible consequence is killed (K-19). **ADJUDICATED → F.W3/W4** (typed props/emits; the expose surface dies).
- **L-7(core) · D/M-6 · C-26 — MAJOR.** Frozen viewBox, no pan/zoom/fit anywhere (my grep: 0 in the directory), `overflow: hidden`, MARGIN 0.15 the entire headroom; pointer capture lets a drag continue arbitrarily far off-canvas and the default magnet drags six neighbours with it; sole recovery is `resetToExtraction` — which MM-5 proves is not what its name says after any save. The reuse-arm cure is killed (K-2). **ADJUDICATED → F.W3/W4.**
- **L-8 — MAJOR** (R-4, mechanism corrected). Per-frame forced layout + triple O(n) rebuild + n-circle patch, no hoist, no memo. The CTM hoist is free (no view transform exists). **ADJUDICATED → F.W3/W4** (with MM-7).
- **D/M-10 · C-8(main) · L-18 — MAJOR.** Save resets the editor: workspace.ts:272 `contour.value = markRaw(result)` (new identity on a `shallowRef`, :43) → identity watcher :72 → `initHistory` (bare reassignment) + `deselect` + fresh bounds + `emitState` → VV:90 clears `editorSaved`, racing :95. Reset-is-un-undoable rides (L-18). C-8's upsert sub-arm killed (K-3). MM-12 rides as INFO (the same save nulls `epicycleData`/`basesData` — :273-274 — correct invalidation, un-signposted cost). **ADJUDICATED → F.W3/W4** (equality-guarded re-init or buffer-preserving install).
- **D/M-8 · C-13 — MAJOR** (R-3 ruling). Double-click on an existing vertex splices a near-coincident duplicate; the zero-length-segment case is the very one `pointToSegmentDist` guards (:66, read). C-13's `.self` cure killed (K-15); DU's proximity-test cure is the sound one. **ADJUDICATED → F.W3/W4.**
- **D/M-9 — MAJOR.** Magnet ON by default (`ref(3)` "default on", :37-38), six neighbours per drag, zero canvas-side indication (grep: :37,38,44,226 — none renders); the only signal is a dock-tint two levels deep in a hover-transient surface (MM-4). **ADJUDICATED → F.W3/W4.**
- **D/M-2 · L-14 · C-23 — MAJOR** (R-5). `.editor-svg:hover .spline-path` runs `golden-shimmer 1.2s infinite` animating `drop-shadow` with zero PRM (grep 0), against 8 tree-wide reduce blocks and the D.W4.c precedent; directory census: 3 `infinite`, 1 guarded. MM-9 rides: the 5 px bloom (:329) is ~2.7× the ~1.84 px handle radius at a 700 px stage — the always-on motion actively occludes the targets it decorates. **ADJUDICATED → F.W3/W4.**
- **D/M-4 — MAJOR.** Selection — the single most important state in a point editor — is carried by a `var(--background)`-on-`var(--card)` cut-out ring: my exact ratios 1.018 light / ~1.36 dark (adopted for dark; token values re-read from dist both arms). **ADJUDICATED → F.W3/W4** (one-word fix + a real selection channel).
- **D/M-7 · L-16(arms 1-2) — MAJOR.** No empty/loading/error state: empty contour emits `viewBox="Infinity Infinity -Infinity -Infinity"` (my node run; D §8 C-2's corrected string — L re-shipped the uncorrected one, K-4); the 62-line sibling has both guards (`""` <3, `"0 0 1 1"` <2 — read); the overlay `<image>` has no error path and a fixed 0.28 alpha while the dock still reports it on. Zero-point API reachability → UNPROVEN #10. **ADJUDICATED → F.W3/W4.**
- **C-11 — MAJOR (identity-trimmed).** value.js pinned `^0.13.0` (four majors behind), consumed by ZERO of this component's chain while `lib/colors.ts` hand-rolls a lossy parser that cannot read the `oklch()` forms glass-ui ships. My verification: 0.13.0 installed; `parseCSSColor`/`mixColors`/`sampleColorRamp`/`timingFunctions` exported, `parseCssColor` absent (C's casing correct for this pin); importers = **4 files** (C's "5" wrong — DU right). Identity trims: the resolver/token seam IS fr-BasisCanvas D-1 (folded below); the pin-behind fact IS the F.W1 tri-package charter (fr-BasisCanvas C-8 identity — not re-booked). The residue booked HERE: this surface's substrate non-adoption. **ADJUDICATED → F.W1.**
- **FOLD — MAJOR · FOLD BY REFERENCE (identity guard; both readers re-booked it as a fresh missed find — caught).** "`VIZ_COLORS` resolves to `#888888` for fourier/chebyshev/legendre at runtime" is **fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER, adjudicated 2026-08-07, → F.W1)** — same `cssVarToHex` fallthrough (re-read whole by this seat: hex passthrough + 3 regexes + `#888888` at both exits), same oklch-only producer tokens (dist re-read), same App.vue:10-18 boot+observer. ONE identity. The NEW residue banked as a rider on that identity: **EditorControlsDock.vue:123** paints the magnet slider track from `VIZ_COLORS.fourier` — the exact control D/M-9 depends on goes grey; `:106`'s `text-viz-fourier` class-path is unaffected (bridge token, adopted). **→ F.W1 with fr-BasisCanvas D-1.**
- **MM-2 — MAJOR (missed find; DU's mid-drag-undo + LC's M-10 sparse-hole are ONE mechanism, merged).** `onKeyDown` has no `dragging` guard and `onPointerMove` has no index bound (:26 guard read): ⌘Z mid-drag replaces `points.value` under a live `dragStartIdx` — same-length: silent re-application over undone geometry with magnet propagation; shorter-by-≥2 (insert-chain undos): `points.value[dragStartIdx] = pt` extends the array with holes and `closedSplinePath` dereferences `undefined.x` inside a computed (:28-31, read) — render-time crash. One unguarded keystroke away because of B-3. **ADJUDICATED → F.W3/W4** (guard keydown on `dragging`; bound the index; L-9's `lostpointercapture` cure family).
- **MM-3 — MAJOR (missed find, reader-DU; re-proven).** Zero `touch-action` in the editor chain (my grep: 2 hits repo-wide, both paper/); `preventDefault` on pointerdown does not suppress touch pan/zoom; the viewport meta is zoomable (index.html:5, read) so **double-tap-to-zoom collides with the insert gesture**; the editor is phone-reachable (mobile Canvas tab, VV:181-186 read); glass-ui's coarse-pointer contract opted out of. Corollary ratified in source: `@pointercancel="onPointerUp"` → `onDragEnd` = `pushHistory` (:54-57) — **the aborted touch drag is COMMITTED to history**. **ADJUDICATED → F.W3/W4.**
- **MM-4 — MAJOR (missed find, reader-DU; re-proven).** The editor's only command surface is hover-transient: `:collapse-delay="2000" :start-collapsed="true"` (dock read post-drift), collapsed template = wand + count + Save; Undo/Redo/Smooth/Simplify/Delete/Magnet/Reset/overlays all behind a re-expand that requires leaving the work surface; the component itself renders zero text nodes. Compounds B-6 (the lying Undo is usually invisible) and M-9. glass dock timer verified in dist (hook default 2500; component `?? 2e3`; 2000 passed). **ADJUDICATED → F.W3/W4.**
- **MM-5 — MAJOR (missed find, reader-LC; re-proven).** "Reset to extraction" (tooltip re-read at dock :156) resets to `props.contour` — which after any save IS the edited server doc (workspace.ts:272); combined with the same save's history wipe, the extraction geometry becomes unrecoverable from inside the editor. The advertised escape hatch dies with the history. **ADJUDICATED → F.W3/W4** (with D/M-10).

### MINOR

- **L-9 — MINOR** (R-8). Capture on the `<circle>`, zero `lostpointercapture`/`releasePointerCapture` (grep 0), one reset path, index-keyed circles, listeners on the svg. → **F.W3/W4** (cure family shared with MM-2).
- **C-9 · L-12 — MINOR.** The overlay-resize derivation is a character-identical fork of `useImageOverlay.resizeFromBounds` incl. the `?? 768` (both read side-by-side); the composable's cache/crossOrigin/onerror/stale-key discipline has no counterpart in the SVG fork; alpha forked too (0.28 twice). → **F.W3/W4.**
- **C-21 — MINOR.** The `?? 768` fallback is triply dead (non-nullable store ref :46; 768 matches no default — defaults/api/dependencies all 1024, my reads; the selecting branch is exactly the no-render branch). → **F.W3/W4** (dies with C-9).
- **L-17 · C-14 — MINOR.** Delete enabled-and-inert at n≤3 (`canDelete` has no length term, :127 vs :180); Simplify pushes a no-op history entry at n≤6 (same-object return, :99/:187 read). → **F.W3/W4.**
- **D/m-1 · L-19 · C-15 · C-16 — MINOR.** Hover makes the point FAINTER (α .6→.5 = 1.433→1.350, my figures); half the transition is dead (`r 0.15s` with r never changed); `.selected` wins over `:hover` by source order at equal specificity (adopted). → **F.W3/W4.**
- **D/m-4 · L-20(collision arm) — MINOR.** `.editor-shell` names the wrapper (VV:208) AND the component root (:231) in one scoped namespace; masked today by `>`-anchored parent selectors; FullscreenViewer's `:deep(.editor-shell)` reaches the child root. Latent, correctly graded. → **F.W3/W4.**
- **D/M-1 · C-12(focus arm) — MINOR** (R-6). `tabindex="0"` + `outline:none` + zero focus styles (grep 0; the 4 global rings enumerate other classes — style.css re-read) + the fullscreen trap lands on it. Becomes real with B-2's cure. → **F.W3/W4.**
- **D/M-5 — MINOR** (R-7). Chassis divergence from the component it crossfades with in the identical `inset:0` box; author knowledge proven by the one-mount-only `:deep()` normalization. → **F.W3/W4.**
- **D/m-7 · L-21 · C-24 — MINOR** (R-9). Zero automated coverage of the interaction surface, its heap simplifier, its history, its coordinate flip. → **F.W3/W4** (the repair wave's born-RED harness is this row's cure).
- **D/m-11 · m-12 · i-2 — MINOR** (R-10 rider). No accessible name/`<title>`/`<desc>`, `pointCount` in no live region, zero `forced-colors`/`prefers-contrast` (adopted), two unguarded `!`s on the hot path (:134/:138 — real hygiene, unreachable crash per K-13). → **F.W3/W4.**
- **L-10 · D/m-3 · C-5 — MINOR.** `save` declared in `defineEmits` (:26) and never emitted (`emit(` once, :124); no `@save` bound to the component (VV:209-212 read); C-5's framing (vue-tsc happily typechecks a listener for a never-fired event) is the sharpest. → **F.W3/W4.**
- **L-11 · D/m-2 · C-19 · C-20 — MINOR.** `dragging` destructured unused (:42, 1 occurrence); `bounds` a pure alias (:75); `historyIndex` returned and never taken (my full-file read); no `noUnusedLocals` (adopted from both readers' tsconfig reads, consistent with fr-BasisCanvas's). → **F.W3/W4.**
- **L-13 + third-copy sharpening — MINOR (merged with DU-missed/LC-M-9).** Bounds+flip viewBox math duplicated in the preview WITH a divergent pad rule (x-extent 0.1 both axes, :26 — wrong in kind for tall contours); the 0.15 constant exists THREE times (subject :29, useViewTransform :58, vs preview's 0.1) — the defect is the missing shared exported constant, not "a magic number" (D/m-6's framing corrected). → **F.W3/W4** (home: `lib/contourEditing.ts`, already imported by all three neighbourhoods).
- **C-22 · C-25 · C-27 — MINOR.** Split dependency sourcing (prop slug vs store reach vs contour field, never cross-checked); `svgPoint` passed at :45 six lines above its hoisted `function` declaration (:133) — a `const` refactor is a TDZ throw at setup; mixed nullability idioms on the props (verified :18-22). → **F.W3/W4.**
- **MM-7 — MINOR (missed find, reader-LC; re-proven).** `ContourPreview` recomputes LIVE bounds per pointermove through the exposed array (:16-29 read) — the exact feedback-shape defect `stableBounds` was written to refuse, reintroduced one component away; also the third O(n) sweep L-8 undercounts. → **F.W3/W4** (with L-6's expose retirement).
- **MM-8 — MINOR (missed find, reader-LC; re-proven).** `preventDefault` before the no-op floor (:165-168 vs :180): at n≤3 the Backspace is swallowed with no compensating action — pure keystroke theft; no early returns between the three key branches. → **F.W3/W4** (rides B-3's cure).
- **MM-9 — MINOR (missed find, reader-DU; re-proven).** The shimmer's 5 px screen-space bloom vs ~1.84 px handles — occlusion, not just distraction. → **F.W3/W4** (rides D/M-2).
- **MM-6 — MINOR (census-corrections bundle; NO-WAVE-OWNER — SS-3/SS-4 spec-authoring input).** Verified verbatim by this seat: lane-frontend.md:118 "pan/zoom → screen transform" for a composable with neither (the row that propagated into L-7's killed cure and S-3's falsifier); :122 "undo/redo ring" for an unbounded stack; :601's style.css ranges drifted ~8 lines vs the live tree (shim/viz-amber/rings re-read); C's "30 siblings" denominator wrong (20 non-recursive / 32 recursive). Load-bearing: the forming specs will apply cures against these coordinates. **→ NO-WAVE-OWNER.**

### INFO

- **L-§4 / R5-7 — INFO · FOLD (census methodology, identity with intake R5-7).** The `<circle`/:268 + `v-for`/:269 split defeats a same-line deriver; totals verified (33 loops; same-line = **8**, not L's 5 — K-17); highest-cardinality interactive loop in the repo (1024 default/4096 max). Extends the R5-7 intake identity — folded, not re-booked. **→ NO-WAVE-OWNER.**
- **MM-11 — INFO (missed find, reader-DU; re-proven).** Two prescribed cures land inside the F.W1 break surface: EditorControlsDock.vue:4-5 imports `hover-popover` (REMOVED at glass-ui 5.0.0) and `metric-badge` (REMOVED at 7.0.0) — census rows :213/:219 re-read; CanvasControlsDock.vue:6 carries hover-popover too. Rendering the dock in fullscreen (B-5's cure) or editing `canDelete` (C-14's) enlarges the uplift transaction. **→ F.W1 sequencing rider.**
- **MM-12 — INFO (missed find, reader-LC; demoted by this seat).** Save also nulls `epicycleData`/`basesData` (workspace.ts:273-274, read) — this is CORRECT cache invalidation for changed geometry, not "a wider destructive act"; the booked residue is only the un-signposted recompute cost. → **F.W3/W4** rider on D/M-10.
- **FOLD — INFO · FOLD BY REFERENCE.** `golden-shimmer.ts:11`'s docstring misstates its own period (~200 ms claimed; 2π·200 ≈ 1257 ms — my node run) and range — **this IS fr-BasisCanvas M-doc(b)** (same file, same defect, → F.W3/W4 there). Its evidentiary role here: it manufactured L-15's killed period arm (K-10) — the second auditor misled by the same comment. Folded, not re-booked.

## Killed-claims register

- **K-1 · C-1 rider ("no text input in the left panel — latent") — KILLED** (both readers; ratified by my SliderControl.vue:71-79 + ContourSettings five-mount + VV:265 reads). The Backspace collision is live IN edit mode. C under-booked its own blocker; D/B-3's panel placement was also incomplete (BasisSelector only).
- **K-2 · L-7 reuse arm + S-3 falsifier — KILLED** (both; ratified by my whole-file read). `useViewTransform` is a fit-to-bbox projection keyed to epicycle/bases store data — no pan, no zoom, no gesture state, cannot see the editor's points; its margin is the SAME ×1.3 the editor already has. The census row it was inherited from is itself wrong (MM-6).
- **K-3 · C-8 upsert arm ("a different point_count can come back") — KILLED** (both; ratified against image_storage.py:269-283 read verbatim). Content-addressing on ordered pairs ⇒ hash hit = byte-identical points. The dedup's REAL consequence cuts the other way and is banked inside C-2 (no-op saves keep bounds; genuine edits lose them).
- **K-4 · L-16's viewBox string — KILLED** (both; my node run: `"Infinity Infinity -Infinity -Infinity"`). L re-committed the exact error D §8 C-2 had already self-corrected — citation-inheritance signature.
- **K-5 · L-16's smooth-NaN arm — KILLED** (both; my trace: empty in → `[]` out; the NaN centroid never touches a point).
- **K-6 · C-7's 1.91:1 stroke figure — KILLED** (both; my exact suite: 1.810). C-7's range should read 1.35–1.81. Verdict (all marks < 3:1) unaffected.
- **K-7 · C-17's "~8 px target" — KILLED** (DU; ratified: with C's own r ≈ 1.385, outer = 2(1.385+1.25) = **5.3 px** — worse than claimed; only D/B-4's closed form treats the geometry correctly).
- **K-8 · C-12's exclusivity + denominator — KILLED** (both; five zero-glass-ui files incl. interactive `ImageUpload`; 20/32 not 30 — D §8 C-1 had already retracted this in-corpus; C shipped it anyway).
- **K-9 · L-15's MutationObserver provenance — KILLED** (DU; my App.vue:10-18 read: the observer lives in App.vue, not colors.ts). Substance survives inside the FOLD row.
- **K-10 · L-15's "unrelated period" arm — KILLED** (LC; my node run: 1257 ms vs the CSS 1200 ms — a 4.7% divergence, nearly identical; the real divergence is the animated quantity). Upstream cause = the docstring (FOLD → fr-BasisCanvas M-doc(b)).
- **K-11 · L-4's worked example — KILLED** (LC, DU concurring; additive reads-of-self compose to `pt + 0.5·d`, contradicting L's own falsifier three sentences later; defect real and slightly worse).
- **K-12 · L-8's event-rate mechanism — KILLED** (LC; ratified as a platform fact: engines coalesce pointermove to frame rate; `getCoalescedEvents` absence means fewer samples seen, not more handler runs). DU confirmed the row without challenging this arm — recorded in DISSENT.
- **K-13 · C-18's getScreenCTM-null reachability — KILLED** (LC; ratified: `svgPoint` requires an in-progress drag on a rendered circle; Transition-leave keeps the element rendered). The `!`s stay booked as hygiene.
- **K-14 · L-3's root-cause cure (publish `computed` refs) — KILLED** (LC; ratified). It requires `defineExpose` — the channel L-6 demands retired, citing the same B.W2 precedent; L's own repair ordering lists both as step 1. Cure law: the defect is confirmed, its prescription is not.
- **K-15 · C-13's `.self` cure — KILLED** (from DU's missed-find analysis; ratified against the template: `.spline-path` is `fill="none"` stroked (:258-264) with no `pointer-events` override — under `visiblePainted` the stroke IS a hit target, so `.self` would break add-point-on-the-curve, the primary insertion gesture, while leaving empty-space insertion working).
- **K-16 · D/B-1's falsifier (c) ("defaults to light") — KILLED as written** (DU; my index.html:21-32 read: absent storage it follows `prefers-color-scheme` — OS default, not light). B-1 stands regardless.
- **K-17 · L-§4's same-line count (5) — KILLED** (DU; my grep: **8**, sites listed and matching DU's enumeration exactly). The structural point survives in the INFO fold.
- **K-18 · reader-LC's missed find M-7 ("held Backspace/⌘Z deep-copies ~245k point objects into the uncapped history") — KILLED BY THIS SEAT.** `deleteSelected` calls `deselect()` (:182), so a held Backspace's SECOND repeat fails the `selectedIdx !== null` guard (:165) — no chain of deletions, no chain of pushes, and no `preventDefault` on the repeats either. Held ⌘Z walks `undo()` (:15-19), which copies snapshots INTO `points` but never grows `history` — nothing is "retained". The surviving residue (no `e.repeat` filter exists) is an observation, not a row; key-repeat undo is ordinary editor behavior.
- **K-19 · L-6's consequence arm ("the dock paints the magnet glyph OFF while the editor runs at radius 3") — KILLED BY THIS SEAT (both readers asserted it).** Mount-order proof: the editor mounts under `v-if="store.contour"` (VV:208) the moment a contour exists; the dock renders only under `isEditing && store.contour` (VV:244), and `isEditing` can only become true via the Edit button, gated `v-if="hasContour"` (CanvasControlsDock, read) — strictly after the editor has mounted and `editorRef` is bound. No reachable order realizes the `?? 0`. The 0-vs-3 default CONTRADICTION stays booked inside L-6 as a latent hazard; the visible-defect claim does not.

## Superlatives — with provenance (L-18 runs both ways)

1. **The VW simplifier is first-rate and its subtle parts were verified, not trusted** (`contourEditing.ts:95-188`, read whole): indexed min-heap over parallel typed arrays, O(n) Floyd build, tombstone + `!alive` lazy-deletion pairing, the `reheapify` two-direction argument (checked: after `siftUp(k)`, `pos[heap[k]] === k`, and `siftDown(k)` re-settles the displaced parent), the `>= 3` floor, and a docstring that names the complexity it replaced AND declares its own deviation from strict VW with an argument. Both readers; this seat re-read every line.
2. **Centroid-preserving Laplacian smoothing** (:190-222) — capture/recompute/translate-back keeps repeated Smooth presses registered against the `image_bounds`-pinned overlay; the cancellation is exact by linearity. Both readers; my read.
3. **The `stableBounds` refusal, documented twice** (:49, :74) — the comment forecloses the rescale-under-cursor feedback loop a future "optimisation" would reintroduce; sole writer verified. All three axes + both readers; my read. **Debit attached**: MM-7 shows the sibling preview reintroduced exactly this through the exposed array — the praise and the violation are one lesson.
4. **The `<image>` counter-flip is provably general** (:252): `Y ↦ -Y + 2y + h` maps `[y, y+h]` onto itself un-mirrored for ANY y,h (my algebra); today's backend only emits the symmetric case where the term degenerates — a sign error would have been invisible until a cropped-extraction path shipped. Both readers; my check.
5. **The `image_bounds → resize` inversion is exact, not approximate** (:91-94 + computation.py:69-88, both read): on the dominant axis `int(dim·resize/dim) = resize` exactly; the `Math.round` is a no-op safety and the invariant is stated at the site. reader-LC; my reads.
6. **No aliasing anywhere in the history** (useContourHistory.ts:9/:18/:24/:28) — deep copies at all four sites, truncation before append; a shallow snapshot at any one would let the in-place drag mutation corrupt stored history. Both readers; my read.
7. **Path-correct magnet integration** (usePointDrag.ts:28-29/:31/:51) — deltas against `dragPrevPt` telescope to net displacement, path-independent; the naive origin-delta form overshoots by cumulative path length. The same loop contains the file's worst defect (MM-1); its most careful line sits four lines above it. Both readers; my read.
8. **`pointercancel` bound unasked** (:240) — the constellation paid for this lesson in value.js's ComponentSliders; recognising pointerup's insufficiency is the hard half. reader-LC. **Debit attached**: on touch it is the handler that FIRES, and it commits the aborted drag to history (MM-3) — the right binding wired to the wrong commit policy.
9. **The D axis's §8 self-correction table is the best artifact in this corpus and it was load-bearing**: two of its corrections (the viewBox second term; the zero-glass-ui enumeration) were independently re-committed by the L and C axes — the proof that an axis auditing its own prior text catches what sibling axes then re-ship. Both readers; the underlying facts re-verified by this seat (K-4, K-8). Measurement discipline corroborated: every D figure this seat re-derived landed within 0.5% (most exactly).

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **B-3 keystroke witness**: focus a `SliderControl` inline input in the editing panel, select a point, press Backspace → assert `pointCount` decremented and the field text unchanged. Wiring fully static.
2. **B-5 double-instance witness**: fullscreen while editing, one ⌘Z → both instances rewind; edit in fullscreen, close, save → fullscreen edits absent.
3. **C-2 witness — MOVE A POINT FIRST** (the K-3 dedup makes a no-op save pass spuriously): edit → save → overlay gone; reload → GET backfill restores.
4. **M-8 dual-engine witness** (belt-and-braces after the R-3 spec ruling): dblclick a vertex in Chromium AND WebKit → `pointCount` +1.
5. **The FOLD row's one-call confirmation**: `getComputedStyle(document.documentElement).getPropertyValue("--viz-fourier")` on /visualize — identical to fr-BasisCanvas UNPROVEN #1; here additionally: is the magnet slider track grey.
6. **B-4's rendered target size** at a real viewport, and the prior 2.5.8-essential/spacing judgment — owner or SS-13 settles the citation; the geometry is closed-form certain.
7. **M-10's `editorSaved` flicker ordering** (pre-flush watcher vs awaited promise) — the reset itself is static and certain.
8. **L-5 heap delta** at n=4096 · **L-8 per-FRAME cost** (per K-12, measure frames, not raw events) · **MM-1's post-throw aftermath trace**.
9. **MM-3 touch**: one-finger drag theft depends on ancestor scrollability; the double-tap-zoom arm is engine-certain given the zoomable viewport.
10. **M-7 zero-point reachability** from the extraction pipeline (`computation.py:78-81` forwards whatever `extract_contours` returns; no runtime validator found).
11. **MM-4 dock collapse behavior** under continuous editing traffic (props + timer static and verified).
12. **L-9/MM-2 stuck-drag reproduction**: drag → ⌘Z mid-drag → release off-svg → hover back; assert no-button drag resumption and/or the splinePath TypeError.

## DISSENT — preserved, not ruled away

1. **reader-DU severity dissents preserved**: B-1 and B-4 at BLOCKER (a light-OS user gets a near-invisible editing surface with sub-6px targets — DU argues the conjunction is capability-level); M-1 and M-5 at MAJOR. If SS-13's live pass shows the light-mode surface is unusable in ordinary conditions, R-1 re-opens.
2. **reader-LC severity dissents preserved**: D/M-2·L-14 at MINOR (hover-gated); L-21 at INFO. Ruled up at R-5/R-9.
3. **Reader lapses recorded (evidence about the fleet)**: DU confirmed L-8 without challenging the corpus's event-rate mechanism (LC killed it — K-12); DU's L-5 cell repeats the corpus's "only numeric literals are -1 and 1" verbatim (`0` appears at :10/:16/:29 — trivial, but verbatim-repetition-without-recount is the citation-inheritance signature this program tracks); LC's contrast figures inherited 8-bit rounding (1.802 vs the exact 1.810); LC's M-7 missed find did not walk its own mechanism past the first key repeat (K-18); BOTH readers asserted L-6's dock-paints-off consequence without a mount-order check (K-19).
4. **The identity-guard event**: both readers banked the `#888888` resolver defect as a fresh MAJOR missed find; it is fr-BasisCanvas's adjudicated BLOCKER (D-1/BC-1/C-1). The fold, not the re-book, is recorded — and the fleet lesson is that missed-find hunts must open with a sweep of the banked registry.
5. **Citation conventions**: C-8 cites workspace.ts:270-271 for the :272 install; C-14 cites dock :96 for :97; LC cites BasisSelector :158/:185 (attribute line) where DU cites :157/:184 (tag line). All facts identical; re-resolve anchors before executing any cure (the D-19 lesson), especially in the four drifted consumer files.

## Closing verdict

**ADJUDICATED — five blockers stand (B-3/L-1/C-1 · B-5/L-2/C-3 · B-6/L-3/C-4 · B-2 · C-2): the product's only direct-manipulation surface destroys geometry from text fields, forks itself unsaved in fullscreen, lies about undo-ability and saved-ness after its primary operation, admits no keyboard, and burns its reference overlay on every genuine save. 47 rows confirmed (5 BLOCKER · 20 MAJOR · 18 MINOR · 4 INFO), 19 kills banked (2 minted by this seat: the held-key row and the magnet-glyph consequence arm), 14 missed finds adopted after re-proof including a simulated hard crash (MM-1) — with two identity folds to fr-BasisCanvas (D-1 resolver, M-doc(b) docstring) and one to intake R5-7, never re-booked. Routings are M-25 agglomeration input: F.W3/W4 owns the interaction-truth/a11y/perf cluster (the bulk), F.W5–W8 owns the C-2 persistence seam, F.W1 owns the substrate rows (C-11, the D-1 fold rider, MM-11's removed-subpath sequencing), and the census-corrections bundle + v-for methodology ride NO-WAVE-OWNER for SS-3/SS-4. Nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
