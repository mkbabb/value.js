claude-fable-5

# fr-EqCoefficientsPanel — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/equation/EqCoefficientsPanel.vue` (17 lines, read whole) + its composition seams (CollapsibleSection · CoefficientsSpectrum · the cartoon-card shim · EquationView host · the glass-ui disclosure/card/configurator registers).
**Substrate**: fourier HEAD `cd26c65` — re-pinned by this seat (`git log -1`). **Dirty-tree disclosure**: 28 modified rows in the working tree (the uncommitted M.W1a bump: `web/package.json`, `web/package-lock.json`, EquationView.vue among them); **all five files this record anchors on — EqCoefficientsPanel.vue, CoefficientsSpectrum.vue, CollapsibleSection.vue, style.css, router/index.ts — are HEAD-clean** (my `git status --porcelain` per path). EquationView anchors were re-read against the WORKING tree. **Producer**: glass-ui **8.0.0** at HEAD `e73ec80c` (2026-08-09, "8.0.0 LIVE") — re-pinned by this seat; the bump commit `a8a6f66b` ("land BK #66 CLOSE — 8.0.0, the export surface cut whole") verified to exist; **70 export keys; `./metric-badge`, `./hover-card`, `./animated-digit` ABSENT; `./card` present** (my node run). The corpus (authored 2026-08-04) and both readers describe a 7.0.0 producer; every load-bearing producer claim in this record was re-run at `e73ec80c`.
**Corpus**: 3 challenge files (D design 403 · C consumption 366 · L library 252) at `docs/tranches/V/megatranche/audit/fourier-components/EqCoefficientsPanel/` — ~66 accusation rows, 3 BLOCKER claims (D/B-1, D/B-2, C-1), 5 D-axis corrections to its own prior pass.
**Readers adjudicated**: reader-DU (design-and-usage) · reader-LC-equivalent (library-and-code) — returns via the orchestration channel; challenge files immutable beside this record.
**Method (M-23/L-18)**: both readers were attacked before either was adopted. Every kill was re-derived (one reader kill is partially UN-KILLED — R-3; one is upheld on corrected grounds with its decisive instrument struck — R-2; one is extended by evidence the reader could not have had without the lockfile diff — R-4). **The identity guard was run over the whole `adjudicated/` registry before any row was booked, and it is the headline: the readers ran none.** The overwhelming mass of this corpus — including THREE of the readers' ten "missed finds" and both non-manifest BLOCKERs — was already adjudicated on 2026-08-08 in **fr-CoefficientsSpectrum**, **fr-CollapsibleSection**, and **fr-CoefficientsPanel**, plus the older manifest/uplift folds (fr-AnimationControls C-1 · fr-AppHeader FR-AH-22/FR-AH-33 · fr-App MG-β · fr-BasisSelector M-7/m-3/m-9/B-3 · fr-AdminAuditLog AA-2/K-13 · fr-AdminFlaggedPanel FR-AFP-18 · fr-CanvasOverlayButton FR-COB-10/FR-COB-19 · fr-ContourEditorCanvas MM-11 · census/intake R3-7a/R3-10/R6-8). One identity each, folded by reference, never re-booked.
**Arbiter's own instruments this session (all static + node, zero browser probes — probe parsimony; product source read-only; sole write = this file)**: registry identity sweep over `adjudicated/*.md` (`vaul-vue` → **∅ as a booked row**; `cartoon-card` → 9 files incl. fr-AdminAuditLog K-13's **21 sites / 14 files** measurement and AA-4's adopt-as-cure; `hover-card|metric-badge` → 7 files incl. FR-AH-22, FR-AFP-18, FR-COB-19; `simplify_series|truncate_by_budget` → **∅** — D/M-5 is genuinely new; `configurator-section-size` → ∅ — D/M-1 unbanked); fourier reads: EqCoefficientsPanel.vue whole (17); CoefficientsSpectrum.vue:60-168 (**row cells :85-104 = signed index + 2-dp amplitude ONLY; tooltip :105-120 = 4-dp amplitude + Phase + Relative + Re/Im — the L §0 kill decider**; `<component :is=…>` at **:132 exactly**; `.coeff-list-move { transition: transform 0.3s ease }` at :161-163; **the Show-more/less `<Tooltip><Button>` block :124-135 is the last child INSIDE the `:78` scroller, which closes at :136**; `@reference` :145; `expanded` ref implied :35); CollapsibleSection.vue:32-47 (trigger :36, title span :39 `cm-serif text-sm`, subtitle :40, `#actions` :43 — all anchors exact); EquationView.vue:1-15 (**:9-10 `@mkbabb/glass-ui/hover-card` + `/metric-badge` live**), :196-215 (**:213 `<EqCoefficientsPanel v-if="components.length">` inside `<Transition name="slide-down">` :212; the left column holds only FunctionInput + this panel**), :236-246 (**Recomputing :239-241 / Error :243-244 banners inside the RIGHT panel's `v-else-if="result"` block**), :360-376 (`.eq-panel-left-wrap::after` 2.5rem fade → `var(--background)` z-2, **`display:none` below 1024px** — a nuance no axis held; `.eq-panel-left` `@apply … overflow-y-auto`); router/index.ts:1-6 (**:2 root-barrel `from "@mkbabb/glass-ui"`**); style.css:98-112 (the shim + its stale "14 sites (13 files)" self-report); FunctionInput.vue:26-30 (**`nHarmonics` default 20 · `budget` default 10**) + :184 (`:max="100"`) + :217 (budget cap); simplification.py:15-31 (**truncate_by_budget keeps top-budget-by-amplitude then `# Re-sort by index for display`**); equations.py ~:91 `simplify_series(terms, req.budget, …)` / ~:126 `coefficients=[…result["terms"]]` whole; count battery: `cartoon-card` **21 sites / 14 .vue files** (re-run), `overscroll` in web/src **6**, `scrollbar-gutter` **1** (all in paper/); **manifest forensics — the R-4 decider**: `git diff web/package.json` (committed pins glass `^3.1.0` / keyframes `^2.2.0` / value.js `^0.10.0` → bumped to ^4.0.0/^4.3.0/^0.13.0 uncommitted), `git diff web/package-lock.json | grep vaul` → **the `node_modules/vaul-vue` entry (resolved 0.4.1 + integrity) exists at HEAD and is REMOVED by the working-tree regeneration**; current working lock's only `vaul` hit = the glass-ui peer-declaration line :347; `web/node_modules/vaul-vue` **present at 0.4.1** (from the committed-lock install); glass-ui producer at `e73ec80c`: node export census (70 keys, absences above), Card.vue:15-35 (**`cartoon`/`grid`/`metal`/`variant`/`dataHue`/`dataHueStrength` STRUCK — the docblock says so verbatim; `CardProps = SurfaceProps + {size, shadow, selected}`**), card/styles.css:39 (**`.card { border-radius: var(--radius-card) /* 16 */ }`** — the radius rides `.card`, not the decoration) + `cartoon-surface` :256-260 (position/border-width/box-shadow only), `grep -rn '@keyframes collapsible' src/` → **0** (disclosure-open/close live at disclosure.css:1/:13,:108), `font-serif-math` → **1 hit, the fallback consumer itself** (typography/utilities.css:78), `admin-label` in src/styles → **0**, sizing-config.css:40 **`--configurator-section-size: var(--type-heading) /* 25.9px */`**.

## Scoreboard

**43 adjudicated register rows: 16 BOOKED here (7 MAJOR · 5 MINOR · 4 INFO) + 27 identities FOLDED BY REFERENCE (2 of them BLOCKER-carrying at their banked homes) · 10 kills in the killed register (1 kill-of-a-reader's-kill; 1 reader kill upheld with its decisive instrument struck; 1 extended on fresh forensics) · 7 missed finds banked after re-proof (5 reader-sourced, 2 this seat's own) · 8 superlatives with provenance + 1 rescission of a reader's crown.**

Blocker weight lives at the banked homes, by reference: **fr-CollapsibleSection B-1** (the scoped `animation` shorthand names keyframes the producer deleted — still deleted at 8.0.0, my grep — and hangs reka's presence machine at the uplift; this panel is one of the five surfaces and the least-tested) and **fr-CoefficientsSpectrum B-1/B-2/B-3** (pointer-only physics under a false discharge docblock · sub-3:1 bars · rank-in-slice hue). The corpus's third BLOCKER (C-1, vaul-vue) is re-provenanced by this seat's lockfile forensics to a **working-tree-only F.W1 commit gate** — see R-4.

Status law: dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is M-25 agglomeration input; nothing here stamps a wave.

## Ruled adjudications (the incredulity seat's actual work)

| # | Question | Reader position | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| R-1 | **The identity guard** | Neither reader ran one; both banked "missed finds" and confirmed rows as new | **27 folds ruled; 3 of the readers' 10 missed finds are BANKED IDENTITIES** — missed-1 (Show-less below the fold) = fr-CoefficientsSpectrum M-7 = fr-CoefficientsPanel FR-CP-36; missed-2 (hue re-normalises on expand) = fr-CoefficientsSpectrum **B-3**; missed-3 (table-as-divs, columns unnamed) = fr-CoefficientsSpectrum M-9∘B-1; missed-5 (the second precept violation, bare `ease` at :161-163) is **already enumerated inside banked M-3** (":162 bare `ease`"); missed-9 (docblock false receipt) = banked B-1's standing correction obligation. | My registry sweep + the banked texts quoted at each fold row below. The readers' proofs are adopted as riders on the identities — the evidence was good; the booking was wrong. Fleet lesson recorded in DISSENT-1. |
| R-2 | **The C-14 kill and the EQC-L-16 crown** | Reader: C-14 "REFUTED at its load-bearing premise" by `grep -c animate-collapsible` → 0 over all 8 built stylesheets; EQC-L-16 crowned "the single highest-value row in the corpus" | **Kill UPHELD on corrected grounds; the decisive instrument is STRUCK; the crown is RESCINDED.** | The reader's grep ran over `web/dist/assets/` — the **stale artifact** fr-CollapsibleSection **K-4** killed as an evidence base (built Jun 12, five days before the installed glass-ui; `.gitignore:42`), and L's supporting premise ("no `@source`; Tailwind v4 never scans node_modules") was killed at fr-CollapsibleSection **K-5** (glass 4.0.0's own `dist/styles/index.css` ships the `@source "../*.js"` directive, and tw-animate-css ships the `--animate-collapsible-*` keys — both banked instrument cells). Whether the utilities emit in a CURRENT build is the banked **M-7 EMISSION HINGE, UNDECIDED in both directions**, gated on one `npm run build` + grep this seat also declined to run (it writes product `dist/`). C-14 stays dead anyway, on grounds that need no emission answer: its failure scenario ("an uplift *retunes* `animate-collapsible-down`; every other collapsible moves") is impossible because the producer **deletes** the grammar rather than retuning it (my 8.0.0 grep: 0 collapsible keyframes; disclosure-open/close the replacement) — that is fr-CollapsibleSection B-1's banked territory. EQC-L-16's protective instruction (do not delete `:60-65` in a "the library animates this" cleanup without settling emission) is real and survives — **as a rider on banked M-7 + B-1, not as a new pin**; its "decisive" grep proved nothing about the current tree. |
| R-3 | **The reader's EQC-L-08 kill** ("fixing the key at :82 alone changes nothing observable; the two must be sequenced, C-2 first") | Filed as a kill of L's causal attribution | **PARTIALLY UN-KILLED — the causal correction stands; the "nothing observable" clause is REFUTED by the banked registry.** | The Fragment-root limb is right (banked fr-CoefficientsSpectrum M-1: the FLIP pass never reaches key comparison), but keyed **remounting is renderer-level and Fragment-independent**: `:key="`${comp.index}-${i}`"` makes any reorder destroy and recreate every row, restarting every AnimatedDigit from 0 — exactly what banked **M-11** adjudicated ("any reorder remounts every row and every AnimatedDigit restarts"; same at FR-CP-30). Fixing the key alone is observably better today. The sequencing claim survives only for the move-CSS; the key fix needs no prerequisite. |
| R-4 | **C-1's provenance and severity** (vaul-vue) | Reader: mechanism CONFIRMED, BLOCKER-framing killed (repo-level; `router/index.ts:2` in every route's eager graph; panel neither owns nor cures) | **Reader's scoping kill ADOPTED and EXTENDED: the defect is WORKING-TREE-ONLY — today's CI is green on this axis.** | My lockfile forensics: the **committed** lock carries `node_modules/vaul-vue` @0.4.1 (the diff's `-` lines remove it); the working-tree M.W1a regeneration dropped the required peer (consistent with a `--legacy-peer-deps`-class regenerate under the banked ELSPROBLEMS peer state), and `web/node_modules/vaul-vue@0.4.1` survives from the committed install — which is why the tree runs. So "a clean CI checkout fails `npm ci && build` **today**" is FALSE (CI builds commits; the committed lock resolves); the failure fires **the day the M.W1a bump commits the regenerated lock**. Same provenance class as fr-CollapsibleSection F-1's rider ("true of the working tree and only the working tree"). Severity: MAJOR, F.W1-commit-conditional; home F.W0 manifest gate inside the F.W1 transaction. The reader's two sub-kills ratified: `@lucide/vue` is NOT a resolution edge (lucide inlined in the pin's dist), and the other lock-absent names (embla, perfect-freehand) are **optional** peers. |
| R-5 | **D/B-1's grade** (zero state coverage; reader CONFIRMED at BLOCKER) | BLOCKER | **MAJOR under the banked identity; the reader's and D's BLOCKER preserved as dissent.** | The identity is fr-CoefficientsSpectrum **M-5** (whose scenario (b) IS this row's sharpest limb: "failed RECOMPUTE — EquationView's `doCompute` never clears `result` — stale coefficients render as current") + **m-13** (the dead `emptyText` branch on this exact route, `EquationView:213` — banked with EqCoefficientsPanel.vue:14 named). Two prior seats graded the state-contract identity MAJOR, one via an explicit ruling (fr-CoefficientsPanel R-5: failure-path-conditional, unlike the at-rest BLOCKER precedents). This corpus adds composition riders (booked at the fold), not a new mechanism, so the banked grade holds. |
| R-6 | **The convergence recommendation** (C-5/EQC-L-02 → ConfiguratorLayer) vs missed-8 (the eager-mount cost) | Reader banked the cost as a missed find and demanded "both halves on the same line" | **ADOPTED — as a decision-input rider on the BANKED convergence row, not a new row.** | The convergence is banked at fr-CoefficientsPanel **FR-CP-33** ("one shared body, two disclosure chassis — converge") and fr-CollapsibleSection **i-4** (the Collapsible-vs-Accordion producer decision); the state-loss half is banked M-2/C-13-fold; the lazy-mount superlative is banked (fr-CoefficientsSpectrum sup-5, L/S-1). What was genuinely unwritten: converging eagerly constructs up to 41 rows + 41 animators + 41 tooltip roots per compute on the route that already carries KaTeX + ConvergencePlot, and cures the state-loss for free. Rider attached to FR-CP-33/i-4 below, with EQC-L-02's producer recursion-rationale quote and the L/X-2 unblocking (itself a banked identity — fr-BasisSelector B-3/R-2). |

## Verdict register — adjudicated defect roster

Routing law: **F.W0** substrate/manifest · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5–W8** CRUD/provenance union · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring. Line anchors are the trees pinned above; re-resolve before any cure (D-19).

### BOOKED — new rows (FR-EQC-1..16)

- **FR-EQC-1 = D/M-5 — MAJOR (genuinely new; registry sweep ∅).** The list is not the equation: the panel enumerates EVERY coefficient beside a formula rendered from a budget-truncated subset, and nothing on either surface says so. My chain: `EquationView.vue:59-67` maps `result.coefficients` whole; `equations.py` ~:126 serialises all `result["terms"]` while ~:91 renders `simplify_series(terms, req.budget, …)`; `truncate_by_budget` (simplification.py:15-31) keeps top-`budget` by amplitude; defaults `nHarmonics` 20 / `budget` 10 (FunctionInput.vue:28-29) → a default session shows a ~10-term formula, a `12 / 41` readout, and 41 rows — **three cardinalities, one label**, and the 4-dp values users copy are exactly the ones the formula may not contain. Cure is one word of copy + one mark (dim/rule below-budget rows); `budget` is already in scope at the host. **→ F.W3/W4.**
- **FR-EQC-2 = D/M-1 ∘ readers' missed-4 (folded as rider) — MAJOR (unbanked; my sweep ∅).** One heading, two routes, two type systems: `CollapsibleSection.vue:39` sets the title in `text-sm` (14px, off the glass ladder — Tailwind's own rung, bypassing the `--text-*` bridge) while the twin's ConfiguratorLayer header rides `--configurator-section-size` = 20.4px at the pin and **25.9px at the producer** (my 8.0.0 read of sizing-config.css:40 — `var(--type-heading)`): 1.46× today, **1.85× after F.W1 — inaction is a widening**. Rider (missed-4, re-proven by the reader): the subtree's seven type declarations are six Tailwind defaults + `text-admin-label`, and the seventh is the rung the producer deletes (fr-App MG-β) — post-uplift the panel's type is 100% off-ladder, so D/M-1's cure fixes one seventh unless the wave takes the inventory. **→ F.W3/W4** (join the section register; lands inside the FR-CP-33 convergence decision).
- **FR-EQC-3 = D/M-2 + C-5(shim limb) — MAJOR, booked at shim scope (21 sites / 14 files — my re-count; the panel's `:12` is one).** A glass-ui-first violation: `style.css:98-112` resurrects the C.W5-retired `.cartoon-card` as a local `@utility` while `./card` ships at the pin and at 8.0.0; the shim's own prose self-reports "14 sites (13 files)" — stale by 7 (**= fr-AdminAuditLog K-13's banked measurement, re-confirmed**). Cure is version-scoped and **the corpus's cure is now two majors stale** (K-7): at 8.0.0 `cartoon` is not a prop — `Card.vue:20-23` strikes `cartoon/grid/metal/variant/dataHue/dataHueStrength`; the form is `<Card class="cartoon-surface">`, and the radius arrives free because **`.card` carries `border-radius: var(--radius-card)`** (card/styles.css:39, my read). **Cross-row conflict this seat surfaces for the forming specs**: fr-AdminAuditLog **AA-4** prescribes ADOPTING `cartoon-card` as AdminAuditLog's chassis cure while this row retires the shim — the spec must order them (adopt-then-migrate, or route AA-4 straight to Card) or one wave will undo the other. **→ F.W1-sequenced F.W3** (the 21-site migration; census figure 21, not 14, not 25).
- **FR-EQC-4 = D/M-3 — MAJOR (shim recipe; dies in FR-EQC-3's cure).** The card frame renders at 0px radius against `--radius-card` = 1rem while five of its own children are pills (`rounded-full` at CoefficientsSpectrum :89/:91/:107; `rounded-control` on the trigger) — verified at both versions: pin `cartoon-surface` and producer :256-260 carry no `border-radius`; the emitted `.cartoon-card` rules carry none. Two radius languages in 17 lines. **→ F.W3/W4** (falls out of the Card adoption). 
- **FR-EQC-5 = D/M-4, scope-corrected per K-8 — MAJOR (shim recipe).** A hover lift + shadow bloom fires on the whole card — dead padding included — and drifts up-LEFT (`--lift-sm: -1px` on both axes) against its own down-left stamp shadow. Correction folded in: the card contains TWO controls (the trigger; the Show-more Button) plus 12–40 hover-reactive rows — the defect is precisely that the **container that is not itself a control** lifts. The producer already cured it (opt-in `.hover-lift`, pure vertical; `cartoon-surface` at 8.0.0 has no hover) — so F.W1 silently self-cures this site and silently strips the affordance from any genuinely interactive cartoon site among the 21 (FR-EQC-13 records the delta). **→ F.W3/W4** with the F.W1 note.
- **FR-EQC-6 = D/M-6 (composition limbs), evidence clause corrected per K-6 — MAJOR.** The wrapper is the element that nests the 300px port (banked fr-CoefficientsSpectrum M-6 owns the port itself) inside `.eq-panel-left`'s own scroller, and the column paints a desktop-only `::after` fade (EquationView :364-370, `display:none` <1024px — my read, a nuance no axis held) that washes the panel's last ~40px toward `var(--background)` — a column-level "more below" cue misattributing to the panel's scroller. D's "no overscroll anywhere" clause is DEAD (K-6: 6 `overscroll` + 1 `scrollbar-gutter` sites live, all in paper/) — restated SHARPER: the house owns both idioms and applied them to one route, so the cure here is a copy, not a design decision. **→ F.W3/W4** (contain + gutter + `./fading-scroll`, with the banked M-6 unit).
- **FR-EQC-7 = C-1, re-provenanced per R-4 — MAJOR (F.W1-commit-conditional).** The uncommitted M.W1a lock regeneration dropped `vaul-vue` — a REQUIRED glass-ui peer statically imported by the root barrel (`dist/glass-ui.js:60`) reachable through 7 barrel edges including `router/index.ts:2` (every route's eager graph — my read) and this panel's chrome chain (`CollapsibleSection.vue:2`) — while the **committed lock carries vaul-vue@0.4.1** (my diff forensics) and `web/node_modules` retains it. Today's CI: green. The bump commit as-staged: `npm ci` → build fails on the unresolved import, everywhere. **→ F.W0 manifest gate INSIDE the F.W1 transaction** (declare + lock vaul-vue, or land the census's barrel→subpath edge retirement first — `./collapsible` pulls no vaul-vue, C's own contrast cell). Riders: @lucide/vue non-edge (reader's kill, adopted); optional-peer absences benign.
- **FR-EQC-8 = C-11 — MINOR.** The coefficient crosses the seam as a positional `[re, im]` tuple through two independently written remaps (EquationView :60-67 collapses names → positions `as [number, number]`; api.ts :38-42 rebuilds the names) while a third shape reads the DTO directly at a third precision (useCoeffHover :86 `toFixed(3)` vs the tooltip's `toFixed(4)`). A silent re/im swap type-checks everywhere. Sibling clause of banked fr-CoefficientsSpectrum M-13 (the viz route's untyped dict) — same seam family, other route. **→ F.W5–W8** (one adapter, both directions; rides the R6-8 contract lesson with the C-10 ordering fold).
- **FR-EQC-9 = C-19 ∘ D/m-7 — MINOR.** `slide-down` is defined four times across four SFCs with divergent durations/offsets (EquationView :449-452 · VisualizationView · PaperView · ContourSettings), and the equation-route copy — the one wrapping THIS panel's mount at :212 — declares no PRM bracket; mitigation is the substrate allowlist by accident (a11y-overrides rewrites `transition-property`, dropping translate). fr-ContourSettings m-2 holds that file's instance; this row books the dedup + the EquationView instance. **→ F.W3/W4** (one recipe, one PRM intent, declared).
- **FR-EQC-10 = D/m-2 — MINOR (panel-local).** The closed card is 48px of which 28px is chrome (58%), with a 14px-above / 12px-below lean on the label — three authors' defaults stacked (the four-author ledger itself folds to fr-CollapsibleSection m-10). **→ F.W3/W4** (one rhythm owner; sequenced after the F.W1 disclosure-body register change, which moves the inner gutter 4px→16px — FR-EQC-13).
- **FR-EQC-11 = D/m-3 — MINOR.** No rule between header and body where both library registers ship one (`--configurator-divider-section`; the 8.0.0 disclosure register) — the panel gets neither because it is neither. Dies inside the FR-CP-33 convergence. **→ F.W3/W4.**
- **FR-EQC-12 = D/m-5 (promise limb) — MINOR.** `subtitle="Fourier spectrum"` (:13) promises the spectrum the panel then declines: the `#graph` slot is left empty here while the sibling fills it — the em-dash gloss restates the title and does no work. The colocation limb (FrequencyGraph misfiled) folds to banked m-14. **→ F.W3/W4** (a product decision — fill `#graph`, or re-word; not both, not neither).
- **FR-EQC-13 = D/i-4 — INFO (the uplift-delta ledger, for F.W1's diff review).** Four silent changes land inside this panel at the bump, none import-shaped: the false hover-lift disappears (improvement, FR-EQC-5); `role="region"` + `aria-labelledby` arrive (improvement; heading stays the consumer's — fr-CollapsibleSection M-1); **`tap-squish` press feedback drops from the trigger** (the 8.0.0 disclosure recipe carries radius/touch-action/focus only); the disclosure body register imposes 16px inline padding + `--type-small` against this consumer's 4px `pb-1`. **→ F.W1 notes** (the census's break surface is import-level; this is the CSS/prop arm's fourth independent request — D §0's own count).
- **FR-EQC-14 = D/i-1 + the reader's independent recomputation — INFO.** Border-vs-fill 1.90:1 light / 2.08:1 dark; muted register 5.11:1 / 5.68:1 — the reader reproduced D's arithmetic to 3-4 significant figures from the shipped literals, and the 1.90:1 figure is the SAME banked cell fr-AdminAuditLog AA-4 used to reject `border-border` as a cure. Adopted without re-running (two independent derivations agree; cross-banked). The 2px line is the panel's only edge cue given FR-EQC-4/-11. **→ F.W3/W4 rider.**
- **FR-EQC-15 = readers' missed-6, ratified and extended — INFO (method; NO-WAVE-OWNER).** The C axis's line citations into CoefficientsSpectrum.vue are systematically late (+5..+11, growing down the file) on a HEAD-clean file both other axes cite exactly — my own reads confirm 7 of the 10 disputed anchors (:132, :124-135, :145, :35, :78, :72-75, :94/:107) — and the same miscount produced the fabricated "R3-10 drift" (K-2). **Extension of fr-CoefficientsPanel FR-CP-45's law, with the polarity flipped**: there, D drifted and L/C were exact; here, C drifts and D/L are exact. The law must be per-corpus, never per-axis: verify anchors per document before quoting any of them into a wave spec. **→ NO-WAVE-OWNER (SS-3/SS-4).**
- **FR-EQC-16 = readers' missed-7, re-verified — INFO (substrate re-pin; extends banked fr-CoefficientsSpectrum M-16).** The producer moved 7.0.0 → **8.0.0** under the corpus (my pins: version 8.0.0, 70 keys, `a8a6f66b` exists, HEAD `e73ec80c` dated today). Every load-bearing 7.0.0 claim this record depends on was re-run at 8.0.0 and SURVIVES (collapsible keyframes still deleted; disclosure grammar the replacement; `./metric-badge`/`./hover-card`/`./animated-digit` still absent; `--font-serif-math` still unfilled; `text-admin-label` still gone; the configurator rung now `--type-heading`); one cure died (K-7). **F.W1 must re-pin to the ADOPTED commit before sizing** — the M-16 gate, third confirmation. **→ F.W1.**

### FOLDED BY REFERENCE (identity guard — banked rows, never re-booked; riders recorded here)

**The fr-CoefficientsSpectrum folds** (all banked 2026-08-08):
- **EQC-L-07 + missed-3(lexical limb) + missed-9 = fr-CoefficientsSpectrum B-1 (BLOCKER, banked).** Pointer-only tooltips under a docblock claiming the a11y gap discharged. My own reads settle the L §0 dispute the banked way: the row exposes index + 2-dp amplitude only; the tooltip holds 4-dp amplitude, Phase, Relative, Re/Im — three of four exist nowhere else (K-3). L's zero-BLOCKER stance was already overruled by the registry. Rider: the four column NAMES exist only inside that pointer-only tooltip, so the M-9 table-semantics repair and this row are one unit.
- **missed-2 = B-3 (BLOCKER, banked).** Hue = rank-in-current-slice; expand recolours every row. The reader's proof is exact and adds nothing the banked row lacks — B-3 already states "the same harmonic wears different colours collapsed (total=12) vs expanded (total=40)".
- **missed-1 = M-7 (= FR-CP-36).** The Show-more/less control is the last child INSIDE the `:78` scroller (my read: block :124-135, scroller closes :136) — banked, with the ~500px-below-the-fold expanded geometry. **New rider banked**: the reader's collapsed-state arithmetic (~276px of ~300px at 1rem root; OVER the cap at the 1.125rem sub-768px root) → SS-13.
- **missed-3 = M-9 (∘ B-1).** Data table as unlabelled divs, no role/name/header/live region; the bare `12 / 41` with no noun = banked M-9's "bare readout with no referent" + m-22.
- **D/B-1 = M-5 + m-13 (MAJOR per R-5; reader's BLOCKER dissent preserved).** Riders booked on the identity: (i) the equation route's banners render in the RIGHT panel's `v-else-if="result"` block (my :239-244 read) while the LEFT panel presents the prior run's 4-dp numbers with no cue; (ii) a zero-coefficient result does not show the declared empty copy — it UNMOUNTS the panel through `slide-down`, taking the user's open/expanded state with it (compounding banked M-2/M-6).
- **EQC-L-01 + C-12 = M-8** (the 40-cap vs the payload). Rider: the equation-route figures — default **41** (2·20+1, my FunctionInput read), slider max 100 → **201**, API `le=200` → **401**; the `40 / 41` readout is the sharpest witness. M-8's own worked figures (401/1001) are the viz route's; the wave sizes against both.
- **EQC-L-08 = M-11 + M-1** (with this seat's R-3 correction to the reader's kill). **EQC-L-12 / C-6 = M-10 (∘ B-3)** — the fork census + token bypass; the reader's confirmations corroborate, nothing new. **EQC-L-13 + missed-5 = M-3** — `transition-all` AND the bare `ease` at :161-163 are both already enumerated there (my read confirms both). **C-2 = M-1** (Fragment-rooted TransitionGroup children; rider: C-20's `defineOptions` name-breadcrumb INFO rides the warn-legibility note). **C-9 = m-2** (phantom tooltip, N≤5 reachable). **C-10 = m-6** (the unwritten descending-amplitude precondition; rider: `truncate_by_budget`'s `# Re-sort by index for display` idiom — my read — is one refactor from the response, C's sharpest cell). **EQC-L-03 = m-13** (dead `emptyText`, byte-identical default — banked with this file named). **EQC-L-17 + C-8 + D/m-5(colocation) = m-14** (FrequencyGraph misfiled; one consumer, cross-route import). **EQC-L-18 = M-15 + FR-CP-32** (the getComputedStyle-in-loop + deep watcher; the unused `onUnmounted` is FR-CP-32's banked cell). **EQC-L-19 + D/m-9 = i-2 + fr-CollapsibleSection m-8** (zero coverage; rider: no `equation-*.spec.ts` exists at all — the route's only touch is one collapsed whole-page baseline, which is why banked B-1's witness law demands the layer OPENED in an axe pass). **R3-10 at :132 = the census/intake fold (i-1)** — my read confirms :132 exactly; see K-2.

**The fr-CollapsibleSection folds** (all banked 2026-08-08):
- **D/B-2 = B-1 (BLOCKER, uplift-conditional, banked).** The unlayered scoped `animation` shorthand names `collapsible-open/close`, deleted at the producer (**still deleted at 8.0.0 — my grep**), consequence = the reka `unmountSuspended` hang on all five disclosure surfaces. This panel's stake, already banked there: least-tested surface; the deletion gate + five-surface close-completion witness ride F.W1. Rider from this corpus: D's "this is the panel's ONLY motion" sharpening (the mount `slide-down` is the host's and PRM-stripped).
- **EQC-L-04 = M-2** (unmountOnHide never forwarded; `expanded` destroyed on collapse; cure = `forceMount` — the forwarding fix is structurally impossible at the producer). **EQC-L-16 + C-14 residue = M-7 (emission hinge, UNDECIDED) + B-1 (deletion unconditional at the uplift)** — per R-2. **C-15 = M-4** (the class-attribute `closest` probe binds `<main>`; `.eq-panel-left` is `@apply`-inlined — my read confirms the literal-class absence). **EQC-L-10 = m-2** (the 250ms timer; N-queued, uncancelled). **EQC-L-11 = m-1** (`$el: any` laundering the dead fallback). **D/m-4 = m-9** (32px trigger; the floor is Accordion-only at the producer — thrice-banked provenance). **D/m-10 = M-7** (the stale `web/dist` — the SAME artifact the reader's C-14 grep trusted; one identity, one irony, recorded). **D/m-6 + C-13 = M-6** (uncontrolled, unpersisted `open` — banked with `EqCoefficientsPanel:13` as the named example). **D/m-1 = m-4 + FR-CP-12** (`#actions` empty/geometrically unusable; the count-in-header cure targets `./metric` post-uplift). **D/m-8 = m-10** (padding ledger). **C-16 = m-11** (untokenised 250/0.2s clock pair). **C-5/EQC-L-02 (convergence) = FR-CP-33 + i-4**, with the R-6 riders: the producer's ConfiguratorLayer recursion-free rationale (quoted by L, real in substance), the eager-mount cost (~41 rows/animators/tooltip roots per compute) vs the state-loss cure, and the X-2/L-X-2 unblocking. **D/m-11 = fr-CoefficientsSpectrum M-6's root-fork limb** (the 300px literal vs the 1.125rem root).

**The manifest / uplift / census folds** (older bankings):
- **C-4 = fr-AnimationControls C-1** (the value.js peer-range violation; G33 `npm ls` gate; fr-CollapsibleSection F-1's working-tree riders). The reader's honest runtime-scoping note (none of the value.js-importing chunks are reachable from this panel's four glass entries — install-time constraint only) adopted as a rider. Sixth independent reproduction; still one booking.
- **C-3 = fr-AppHeader FR-AH-33 + fr-BasisSelector m-9** (devDependencies manifest; lucide/reka/cva/clsx/tailwind-merge). → F.W0.
- **D/M-8 = the census break table (lane-frontend :470-481) + fr-AppHeader FR-AH-22 (`./hover-card`) + fr-AdminFlaggedPanel FR-AFP-18 / fr-AnimationControls L-13 (`./metric-badge`) + fr-CanvasOverlayButton FR-COB-19 (the same EquationView callsite, already banked as the single-file break-stack instance).** My 8.0.0 census re-confirms both subpaths absent. Rider booked on the identity: the host break MASKS this panel's silent CSS regression (banked B-1) — F.W1's witness ordering must cure the host imports FIRST, then run the five-surface close-completion witness, or the loud break hides the quiet one.
- **D/M-7 = fr-BasisSelector m-3 + fr-AppHeader's GLASS-RELAY row (via fr-CollapsibleSection F-3)** — `--font-serif-math` undefined at BOTH trees (my 8.0.0 grep: 1 hit, the fallback itself). Rider: D's collision framing (generic-serif title + system-sans subtitle + Fira Code numerals in one 48px card — the tightest three-register stack on the route).
- **D/i-3 = fr-App MG-β** (`text-admin-label` deleted; my 8.0.0 grep: 0). **D/i-5 roll-up**: (a) `variant="ghost"` = fr-BasisSelector M-7 + fr-AdminAuditLog AA-2 (with the banked K-4 correction: the break is LOUD in dev — Button's watchEffect voice); (b) = MG-β; (c) lucide ×2 = the census :105 fleet row; (d) = fr-CollapsibleSection B-1. Zero re-books.
- **D/m-12 + X-4/X-5 = fr-AdminAuditLog K-13** (21 sites / 14 files; the shim's "14" and the lane's "25" both wrong, in opposite directions — my re-count confirms 21/14). Census correction → **NO-WAVE-OWNER**, F.W1 sizing uses 21.
- **L/X-1 = fr-CoefficientsPanel FR-CP-44** (lane-frontend:443's `./metric-stack` mis-home — banked). Rider: L names ConfiguratorLayer as the true disclosure analogue; the reader's reconciliation adopted — D's separate `./metric` use stays right for the Metric `loading`/`aria-busy` contract (the M-5 cure seat). **L/X-2 = fr-BasisSelector B-3/R-2** (the stale `test.fixme` "omits inert" premise; `inert` live at the installed pin — banked twice already; no third booking).

## Killed-claims register

- **K-1 · C-14 — KILLED, grounds corrected (R-2).** The failure scenario is impossible (the producer deletes, not retunes — 8.0.0 verified); what survives of C-14 is only the `:57-59` prose contradiction, which rides fr-CollapsibleSection B-1's deletion edit. The READER's "REFUTED at its load-bearing premise" via the 8-stylesheet grep is STRUCK as evidence: it re-ran the stale-dist instrument fr-CollapsibleSection K-4 killed, atop the `@source` premise K-5 killed. The emission question stays UNDECIDED at banked M-7's pre-gate.
- **K-2 · C §4's R3-10 row ("live :137 … a 5-line drift consistent with edits since") — KILLED** (reader's kill, ratified by this seat's own read: the live line IS `:132`; the file is HEAD-clean). A self-manufactured drift booked against an ADOPT-AS-FACT intake row — the exact hazard FR-EQC-15 exists to prevent.
- **K-3 · L §0's zero-BLOCKER justification for EQC-L-07 ("every field it shows except phase is also on the row") — KILLED** (reader's kill, ratified by this seat's :85-104 vs :105-120 reads: three of four tooltip quantities exist nowhere else; the fourth is at double precision). The row's banked grade (fr-CoefficientsSpectrum B-1, BLOCKER) already reflected the truth.
- **K-4 · C-1's BLOCKER-of-this-component framing AND its "195 lock entries, vaul-vue ABSENT" cell as a statement about the shipping tree — KILLED/RESCOPED (R-4, this seat's forensics).** The committed lock carries vaul-vue@0.4.1; the absence is the uncommitted M.W1a regeneration's; today's CI is green; the panel neither owns nor cures the edge (`router/index.ts:2` fails identically with the panel deleted — reader's cell, my read). Mechanism survives whole as FR-EQC-7.
- **K-5 · C-5's "14 sites" and EQC-L-06's "25 sites" — both KILLED** (reader's kill, my re-count: 21/14; identity = fr-AdminAuditLog K-13). C repeated the shim's stale self-report; L repeated the census's raw string count (21 + 4 self-references).
- **K-6 · D/M-6's evidence clauses ("no overscroll-behavior anywhere … repo-wide grep → nothing"; "no scrollbar-gutter") — KILLED as stated, finding sharpened** (reader's kill, my re-runs: 6 + 1 live sites, all paper/). Scoped to the panel's closure the claims are true; repo-scoped they invert into "the house owns the idioms and applied them elsewhere" — a cheaper cure and a worse look.
- **K-7 · D/X-2's cure literal ("`<Card cartoon>` after the uplift … cartoon?: boolean at Card.vue:18,36") — KILLED at 8.0.0** (reader's kill, ratified by this seat's own Card.vue:15-35 read: `cartoon` STRUCK from the prop surface; `CardProps = SurfaceProps + {size, shadow, selected}`). The surviving half also re-verified: `<Card class="cartoon-surface">` + the radius free via `.card`'s `border-radius: var(--radius-card)` (styles.css:39). X-2's meta-point (cures are version-scoped; sequence after the bump) is vindicated by its own literal dying twice.
- **K-8 · D/M-4's "Nothing about the card is interactive … The only interactive thing inside is the trigger row" — KILLED as written** (reader's kill, ratified: the Show-more Button :124-135 and 12–40 tooltip rows are inside). Restated in FR-EQC-5 as "the card element itself is not a control"; the defect unmoved.
- **K-9 · The READER's EQC-L-08-kill clause ("fixing the key at :82 alone changes nothing observable") — KILLED by this seat (R-3).** Keyed remount + AnimatedDigit restart-from-0 is key-dependent and Fragment-independent — banked fr-CoefficientsSpectrum M-11 / FR-CP-30. The reader's causal correction for `.coeff-list-move` survives; the sequencing demand narrows to the move-CSS only.
- **K-10 · The READER's crown ("L/EQC-L-16 is the single most valuable row across all three challenges") — RESCINDED.** The row is a banked identity (fr-CollapsibleSection M-7's hinge + B-1's gate, adjudicated a day earlier with better instruments), and the reader's "decisive" re-test ran the killed stale-dist probe. The protective instruction survives at its banked home; crowns are not transferable to re-derivations that used dead evidence.

## Superlatives — with provenance (L-18 runs both ways)

1. **The shared-body extraction, thrice-crowned and still true** (all three axes + both readers + both banked sibling apotheoses; my reads concur — the subject's only route knowledge is two comments; props = `components` + `emptyText`). Preserved through every F.W3/W4 rework; the seam's flaw is under-specification (slot props, cap prop), never the seam.
2. **The wrapper contributes zero CSS** (D/S-6, reader-adopted; my whole-file read: no style block, 17 lines) — in a tree where a consumer scoped rule is the banked B-1 BLOCKER, the one card site on the route that cannot be a cascade defendant. Genuinely this panel's own virtue.
3. **The muted register clears AA in both arms from tokens alone** (D/S-2; reader independently recomputed 5.115/5.68 to 3+ s.f.; the arithmetic cross-checks the banked fr-CollapsibleSection S-2 cell). The panel never reaches for a colour; its only chromatic decision is the inherited one (banked M-10/B-3).
4. **The trigger inherits a conformant focus ring AND correct disclosure ARIA free, at both pins, by two different mechanisms** (D/S-3 + reader; consistent with my producer reads) — the reader went hunting a missing-`aria-expanded` defect and recorded the miss. Falsification done right.
5. **Lazy by construction — and the honest trade is now written on one line** (banked fr-CoefficientsSpectrum sup-5 + L/S-1; this corpus's contribution is R-6's rider putting the eager-mount cost BESIDE the convergence recommendation, which no prior document did).
6. **CORPUS · D's contrast arithmetic reproduces exactly** (reader's independent recomputation, luminances matching to 4 decimals) — an audit that publishes checkable numbers and survives the check. Adopted as FR-EQC-14's basis without a third derivation.
7. **CORPUS · All three axes publish their corpses** (D §6: 8 falsified candidates incl. the PRM-hover nuance; L §6: 8 incl. the two rAF/provider kills; C §5: 6) — and two of the kills (D's candidate 7, the 44px-floor false cure; D's candidate 4) are the exact nuances the banked registry independently confirmed. The discipline held even where the identity guard did not.
8. **READER · The lockfile-and-producer forensics posture** — the reader re-pinned the producer at 8.0.0, killed a two-majors-stale cure, and re-tested every load-bearing claim at the new HEAD rather than trusting the corpus's 7.0.0 cells. That is fr-CoefficientsSpectrum M-16's law practised unprompted; this seat's own forensics (R-4) only extended the same posture to the consumer's lockfile.
**Rescission**: the reader's EQC-L-16 crown (K-10).

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **THE EMISSION HINGE** (banked fr-CollapsibleSection M-7, re-armed here because the reader believed it settled): one CURRENT `npm run build` + grep for `animate-collapsible`/`transition-collapse` — decides today's deletability of `CollapsibleSection.vue:54-71` and retires the C-14/EQC-L-16 residue in one stroke. Not run by this seat (writes product `dist/`).
2. FR-EQC-7 end-to-end: `npm ci && npm run build` **at the staged M.W1a commit** (not today's HEAD — today is provably green from the committed lock). The build-failure terminal state is still unwitnessed.
3. missed-1's collapsed arm: whether the Show-more Button clears the 300px cap at the 1rem root (~276px derived — marginal) and overflows at the 1.125rem root — three-viewport measurement (the expanded ~500px-below-fold case is static-certain, banked).
4. FR-EQC-6: the double-scroller hand-off feel + whether the desktop-only column fade visibly washes the panel's tail rows at real breakpoints (the mechanism and the `display:none <1024px` gate are static — my reads).
5. FR-EQC-5: the felt 1px diagonal jitter on pointer entry to dead padding (geometry static; percept not).
6. FR-EQC-2/FR-EQC-10: whether the 1.46× heading disparity and the 58%-chrome closed card read as defects on screen (numbers exact; reading not).
7. Banked M-6's scrollbar question: overlay-vs-classic behaviour against the flush `w-16 tabular-nums` last cell at each supported width.
8. D/m-4's coarse-pointer clientHeight (32px derivation static; the floor's absence verified at both producer versions).
9. L/U-1: whether reka's Presence watchers approach Vue's 100-iteration cap for this single collapsible under throttle (EQC-L-02's consequence half; the divergence half is banked at FR-CP-33's rider).
10. L/U-2/U-4/U-5: the scroll-jump witness at the `<main>` mis-binding (banked M-4); the keyboard walk receipt for banked B-1; whether the committed `/equation` baselines contain even the panel HEADER (needs a live backend; the body's absence is proven).
11. The per-hue bar-contrast sweep against both theme arms (banked B-2's exactness rider) and EQC-L-09's reachability (`f(x) = 0` through `doCompute`'s guards — the NaN% arithmetic is banked m-3).

## DISSENT — preserved, not ruled away

1. **The readers ran no identity guard, and the cost was material**: three "missed finds" banked as new that the registry already held (one at BLOCKER), two BLOCKER confirmations re-argued from scratch for rows banked a day earlier, and a crown handed to a re-derivation of a banked hinge on dead evidence. Their falsification discipline was otherwise excellent — several kills of this corpus are the best cells in it (K-2, K-3, K-5, K-7). Fleet lesson, now thrice recorded (fr-BasisSelector D-1, fr-CoefficientsSpectrum D-1, here): **a re-reader's missed-find cell needs a registry sweep before it needs a proof.**
2. **Severity dissents preserved**: D's + the reader's BLOCKER on D/B-1 (R-5 — re-opens if SS-13 shows the stale-numbers-beside-an-error-banner state reachable in ordinary flows at material frequency); the reader's BLOCKER-severity endorsement of C-1 as a component row (R-4 — re-opens only if the M.W1a bump commits the lock unrepaired, at which point it is F.W1's gate failing, not this panel's).
3. **This seat's adopted-not-rerun cells, enumerated**: D's §3 token-literal contrast arithmetic (twice-derived by others, ≤0.06 agreement); the reader's per-file cartoon-card breakdown (EquationView 5 / FunctionInput 2 / AdminUserList 2 / VisualizationView 2); C's ci.yml/deploy-pages npm-ci cells; the reka `useForwardProps`/`Slot.js`/`TooltipTrigger` chains (banked at fr-CollapsibleSection/fr-CoefficientsSpectrum with whole-file reads); the tw-animate-css theme-key cell (banked instrument). Every other load-bearing cell was re-derived with the instruments listed in the header.
4. **A residue this corpus cannot settle**: whether the equation route SHOULD render the spectrum graph (FR-EQC-12's product decision) interacts with FR-EQC-1's honesty repair — a filled `#graph` would add a fourth surface to reconcile against the budget-truncated formula. The forming F.W4 spec should decide them together; neither axis nor reader made the connection.

## Closing verdict

**ADJUDICATED — a 17-line wrapper whose corpus is mostly other components' banked convictions wearing its name: 27 identities folded by reference (including both non-manifest BLOCKER claims and three of the readers' ten "missed finds" — the registry, not the readers, held them first), and 16 rows booked new. Of the new: the list-is-not-the-equation honesty MAJOR (FR-EQC-1 — three cardinalities under one label, the only defect authored in this file's own strings), the off-ladder heading that the 8.0.0 producer widens to 1.85× (FR-EQC-2), the 21-site cartoon-card shim retirement with its cure corrected to the 8.0.0 class form and an explicit cross-row conflict against fr-AdminAuditLog AA-4's adopt-as-cure (FR-EQC-3, with FR-EQC-4/5 dying inside it), the double-scroller composition with its evidence clause inverted into a cheaper cure (FR-EQC-6), and the vaul-vue lock gate re-provenanced by this seat's diff forensics from "CI fails today" to "the M.W1a bump commit fails the day it lands" (FR-EQC-7 → F.W0-inside-F.W1). Ten kills banked: the C-14 kill upheld on corrected grounds with the reader's stale-dist instrument struck, the fabricated R3-10 drift, the false tooltip-supplementarity clause, two wrong site-counts, two dead evidence clauses, one two-majors-stale cure literal, one scope over-reach — and one kill-of-a-reader's-kill (the key fix at :82 is observably valuable without C-2, per banked M-11). Routings are M-25 agglomeration input: F.W0 owns the manifest gates; F.W1 owns the deletion/break/re-pin cluster and the uplift-delta ledger; F.W3/W4 owns the honesty, register, shim-design, and composition mass plus the FR-CP-33 convergence decision now carrying its eager-mount cost on the same line; F.W5–W8 owns the tuple seam; the census corrections go NO-WAVE-OWNER. Nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
