**Served model id: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited (M-23/L-18 receipt, line 1 by commission).**

# shell-dock-genericactionbar — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Date**: 2026-08-08 · **HEAD**: `8d7e17fe` · branch `tranche-u` · subject `demo/shell/dock/layers/GenericActionBar.vue` (32 L, sha256 head `3f7a43b76dd1a14d`) + its command branch: `usePaneRouter.ts:24-58/:106-123/:186-230`, `App.vue:35-43/:83-137/:314-348`, `Dock.vue:9/:24-41/:152-190`.
**Corpus**: `audit/components/shell-dock-genericactionbar/` — challenge-C (3 rows) · challenge-D (2 rows + command-seat table) · challenge-L (3 rows), r2 governs; two candidates + `FINAL-SOL-AGGLOMERATION-2026-07-29.md`; corpus cut `e01d0065`, all cited files unchanged at HEAD (re-verified). All three challenge files are **UNWITNESSED-DIRECT** in `registry/HYDRATION-LEDGER.md:295-297` (`—` provenance, 0 evidence artifacts).
**Re-readers adjudicated**: design-and-usage (r-DU) and library-and-code (r-LC), both hostile, both challenged back. Every verdict-level disagreement between them, every kill, and every load-bearing miss was re-derived by this seat with its own reads (~40 file:line coordinates re-resolved; every one landed; zero browser probes — the whole corpus is decidable from source, which is itself the corpus's central fact).
**Adjacent adjudications governing shared surface (anti-rename identity guard)**: `Dock.md` (DELTA-1 · DELTA-5 · DELTA-13 C-25/C-27/C-35 · gate G-I · ruling R-8), `shell-dock-actionbutton.md` (ABN-2/3/7/16/19/21), `GenerateControls.md` (row 4 · row 15/B5 · ruling R-B). **Standing wave law**: `docs/tranches/X/waves/W4.md` unit **X.W4.d** (CC-043 — the typed `SceneActionSet`; `GenericActionBar.vue` is in its file bounds as *modify*; gates D1-D4, three RED of record) and **X-W5** unit a (`bindPane`, gate A3). The corpus's `V.U2`/`V.U3` routings are superseded V-era ids and are re-pointed below.

## Gestalt

The corpus's mechanism is entirely right and fully proven from source: a 32-line renderer maps untyped route descriptors onto handlers that optional-chain into pane-instance refs only desktop slots ever populate, so the shell manufactures enabled commands whose targets do not exist — and for Mix, commands that are never operative in ANY layout. Both re-readers hardened that proof, killed real errors in the corpus's cures, and surfaced the corpus's largest blind spot (the same ref bridge kills the PICKER's whole mobile command surface — the leg every cut retains). What neither reader caught, and what this seat adds: **the corpus's terminal DELETE, as a per-verb seat ruling, collides with standing canon and a standing adjudication** — `VISUAL-CONSTITUTION.md:194` + `DECISIONS.md` B-13 ("Regenerate, Save/Publish and Copy live in **one Dock control set**") and GenerateControls-adj ruling R-B (the PLATE cluster is the violation and retires INTO the dock set), while challenge-D:88-92 cites `CONVERGENCE-REAUDIT-2026-07-29.md:104-125` for the optional scene action role and skips the same diagram's Dock line ("Dock: global navigation, identity, status, **active-scene commands**"). The renderer and the untyped branch die regardless — X.W4.d orders exactly that collapse and owns the file. Which verbs each scene REGISTERS afterward is not settled by this corpus and is escalated, not silently ruled.

## Adjudicated defect roster

- **GAB-1 · BLOCKER — enabled generic actions on mobile terminate at null desktop-only pane refs; the renderer invokes unconditionally and no descriptor can ever be disabled** (C-GA-1, both readers CONFIRMED; static proof complete, the text-only ROOT receipt not load-bearing). GenericActionBar.vue:28 `@action="act.handler()"` unguarded; zero `disabled:` keys across usePaneRouter.ts:191-224; nine `paneRefs.X.value?.y?.()` closures (:196-198/:208-210/:220-222); the three refs `ref<any>(null)` (App.vue:317-319) written ONLY in `onDesktopLeftMount`/`onDesktopRightMount` (:323-332), bound only at :105/:131; the mobile PaneSlot (:83-91) passes no `:on-mount` — all re-read this session. **→ FOLD, identity preserved: ≡ Dock-adj DELTA-1 (D2-01 present-and-inert arm) ≡ ActionButton-adj ABN-2 ≡ X.W4.d gate D4 (RED of record, source-measured) + X-W5.a gate A3 (the parity cure).** This corpus adds the renderer-side limb (unconditional invocation, un-spellable disabled state) — carried into X.W4.d's "unavailable is surfaced, never silence" clause. Not re-booked.
- **GAB-2 · BLOCKER — shell presentation depends on untyped pane instances; layout decides whether an apparently valid command exists** (L-GA-1, both readers CONFIRMED). `handler: () => void` (usePaneRouter.ts:46), `PaneActionRefs { generate|gradient|mix: Ref<any> }` (:107-111), the contract imported backward into the dock layer (GenericActionBar.vue:4). Census note: r-DU said seven `any`s, r-LC enumerated nine spellings; **X.W4.d gate D2's scoped census (10 on the action path: App 6 · usePaneRouter 3 · Dock 1) is the count of record** — reader drift recorded, gate governs. **→ X.W4.d (gates D2/D3).**
- **GAB-3 · MAJOR — the command boundary cannot express whether an operation ran** (C-GA-2, CONFIRMED at the contract level; its cure part-killed, K-5). `() => void` at :46; the renderer discards the call expression (:28); the owners are async (GenerateControls.vue:106-108, GradientVisualizer.vue:127-129, MixPane.vue:49-55 — the true shell owner, per K-4). **→ X.W4.d** (the `SceneActionSet` models outcome/`unavailable`; D1/D4). Rider from K-5: deleting the boundary is necessary, not sufficient — the route-local copy seats must stop discarding `CopyResult` (GenerateControls :107/:112 already BANKED at GenerateControls-adj row 15/B5; GradientVisualizer :128 → GAB-18).
- **GAB-4 · MAJOR — Mix's descriptors are never operative in any layout** (C-GA-3, CONFIRMED with the framing corrected per K-2: not "can mask", ALWAYS masks). `actionBarContext` is a plain object literal (ColorPicker.vue:315-328) unconditionally exposed (:331-344); viewSchema.ts:142-151 pins mix `left: "color-picker"`; App.vue:38 is therefore non-null in every desktop Mix layout and Dock.vue:156 always wins; on mobile `mixPaneRef` is never written. Corollary verified: `clearSelection` (useMixingState.ts:113-117, exposed MixPane.vue:57) has exactly one caller (usePaneRouter.ts:220) — zero reachable callers in the tree. **→ X.W4.d D1** (the `v-if`/`v-else-if` priority is deleted by the single render path). The capability consequence is GAB-10.
- **GAB-5 · MAJOR — opening the branch withdraws the dock's navigation face to show verbs that already exist beside their subjects — CONFIRMED AS FACT, its per-verb DELETE ruling ESCALATED (K-8)** (D-GA-1). Duplication verified: Generate ×3 local (GenerateControls.vue:157-184), Gradient Copy CSS local (GradientVisualizer.vue:254-256), Mix primary + result Copy/Reset local (MixConfigBar.vue:162-170, MixResultDisplay.vue:119-143); r-LC's caveat verified — Gradient's `resetGradient`/`seedFromPalette` have NO route-local seat (sole invocation path usePaneRouter.ts:208/:210). The layer swap and producer hiding verified (Dock.vue:108-117/:153-158; glass layers.css `.dock-layer:not(.is-active)…visibility:hidden`). **Disposition: the renderer + untyped branch die at X.W4.d (its bounds already order it). The per-verb seat question — dock set vs route-local — is an OWNER/canon item at X-W10, with the VC:194 + B-13 + GenerateControls-adj R-B collision registered (see K-8). No seat may execute challenge-D's command-seat table for Generate while R-B's banked ruling stands un-overturned.**
- **GAB-6 · MAJOR — the opened group has no identity** (D-GA-2, both readers CONFIRMED). Root div :15 with no role/name; `DockActionBar.label` documented as the opener's label (usePaneRouter.ts:50-51) and consumed only at Dock.vue:187. **→ FOLD ≡ ActionButton-adj ABN-21** (group semantics on BOTH containers, already routed X.W4.d — the successor row states its group semantics). Sharpened by GAB-7.
- **GAB-7 · MAJOR — the contract types dynamism it structurally forbids** (r-LC missed find, ADOPTED after this seat's own derivation). All three producers wrap dependency-free literals in `computed()` (:195-199/:207-211/:219-223) — the getters construct closures without reading any reactive source (the `.value` reads live inside handler bodies, invoked outside tracking), so the computed can never re-evaluate and `Ref<DockAction[]>` is ceremony. Inert fields verified: `iconClass`/`disabled` never set (yet forwarded, GenericActionBar.vue:26-27), `accentColor` never set (Dock.vue:157/:188 always `safeAccent`), `label`/`icon` byte-equal to the opener's own fallbacks (`"Tools"`/`Paintbrush`, :186-187), `rotateOnClick` live 2-of-9. Six of ten interface fields can never change a rendered byte. **→ X.W4.d** — this is the structural root of GAB-3 and the sharpest justification of D1's falsifier ("fails if the collapse is faked by a union alias"); the fields land on the deleted-symbol list (`action-contract-diff.md`, W4 artefact 6).
- **GAB-8 · MAJOR — single-consumer duplicate indirection; the duplication with ActionToolbar is byte-level** (L-GA-2 CONFIRMED; the axis's falsification of Luna KILLED, K-7). One importer + one render site (Dock.vue:9/:157; not exported from dock/index.ts); root string `flex items-center justify-around flex-1` byte-identical to ActionToolbar.vue:2; same leaf, same `activeHover` mutex (:11/:29 vs :85 + five inline repeats). **→ X.W4.d** (the renderer is collapsed/deleted there). The shared zero-gap layout defect is **≡ ActionButton-adj ABN-19** (already booked, both consumers named, the 0 px/6 px cells = its U-3) — cross-cited, not re-booked; r-DU's "missed" spacing row folds to that identity.
- **GAB-9 · MAJOR — retaining the branch retains an extra consumer of the `demo/ui` forwarding layer** (L-GA-3, both readers CONFIRMED as the narrow ownership statement). ActionButton.vue:50-54 imports through the one-line `demo/ui/popover/index.ts` barrel while :55 uses the published `@mkbabb/glass-ui/dock` subpath; AUDIT-HANDOFF-2026-07-28.md §7 citation checks out. **→ X-W8** (the demo/ui subtraction surface, named in X.W4's own out-of-scope table); identity shared with the W-HYGIENE extirpation (ColorSpaceSelector-adj L-4 lineage).
- **GAB-10 · MAJOR — a Mix selection can never be emptied by any reachable control** (FINAL-correction-2 + r-LC missed find, merged; verified whole). `MIN_COLORS = 1` / `canRemoveColor` / `:disabled` (MixSourceSelector.vue:37/:39/:154) floor local removal at one; the only zero-path is `clearSelection`, never operative per GAB-4. A live product capability has no working seat. The C/D axes' contrary premise is KILLED (K-3). **→ NO-WAVE-OWNER** — no tranche-X wave names `MIN_COLORS`, `clearSelection`, or the Mix workbench controls (grep over `docs/tranches/X/` verified); the corpus's `V.U3` routing is superseded. Booked for the next formation boundary: either the `SceneActionSet` registers a working Clear or local removal reaches zero — one honest seat, not both, not neither.
- **GAB-11 · BLOCKER (already adjudicated — corpus gap, not a new row) — the same desktop-only bridge nulls `colorPickerRef` on mobile: the picker's action bar never renders on any phone, the mobile-edit layer's Save/Cancel dispatch to null, and palette colour editing is unreachable** (both readers' headline "missed" find; every limb re-verified: sole write App.vue:325 under :105; `whenColorPickerReady` 40×50 ms bounded give-up, usePaletteWiring.ts:33-58/:106-119; Dock.vue:72/:135-145; `hasAnyActionBar` false on every mobile route post-cut, so the Tools toggle collapses to `0fr`/opacity-0 there — ActionBarToggle.vue:115-128 read). **≡ Dock-adj DELTA-1's L-35 edit arm + gate G-I (RED of record) + ruling R-8 (the DockCommand registry cure; instance-ref command dispatch DIES) + X-W5.a.** The readers' genuinely new contribution is a **corpus-cut correction, hereby adopted**: challenge-C:118-119 / challenge-D:106-107 / challenge-L:129-130 ("retain the left mount callback only for `colorPickerRef`") would entrench a ruled-dead mechanism and contradicts R-8 — no execution seat may follow those steps as written. FOLD; not re-booked.
- **GAB-12 · MINOR — the prescribed terminal seats ratify the retired native-`title` register** (r-DU missed find, verified). GradientVisualizer.vue:254 and MixResultDisplay.vue:121-143 use `<DockControl compact title="…">` with no aria-label; DockControl's `__VLS_Props` declares no `title` (d.ts read — falls through as the UA tooltip attr); Dock.vue:140-142 records the register retired dock-wide. **→ X-W10** (register canon) with per-site notes: the Gradient seat sits in X-W6's file bounds and may move there; MixResultDisplay's Copy `:title` is also its only success confirmation channel for AT — cure together with its `copied` state.
- **GAB-13 · MINOR — the dock mirror is a latent tripwire against the repo's own shipped containment oracle** (r-DU missed find, verified). o20-generate-plate.spec.ts:36-40 asserts page-count ≡ plate-count for buttons named "Regenerate"; ActionButton :16 `:aria-label="title"` gives the dock mirror that exact name; it escapes today only because inactive layers are `visibility:hidden` (absent from the a11y tree); opening Tools on `/generate` yields 2-vs-1. **≡ GenerateControls-adj row 4** (the same vacuity mechanism, opposite emphasis) **+ its R-B carry** (the o20 containment law vs VC:194 collision, already registered) — FOLD; r-DU's Tools-open sharpening added to that record.
- **GAB-14 · MINOR — two competing `isDesktop` truths inside the dispatch chain** (both readers filed as missed; verified App.vue:310-312 compound vs Dock.vue:71 width-only). **≡ Dock-adj DELTA-5 — CONFIRMED and BUILT there** (structure (h) + gate G-L, collision frame `collision-1024x1366.png` committed). FOLD; not re-booked.
- **GAB-15 · MINOR — dead-symbol cluster in the FOLD comparand** (r-LC missed find; facts verified: `canProposeName` declared ActionToolbar.vue:72 and read nowhere in that SFC though passed at ActionBarLayer.vue:106; `clearHover` + `defineExpose` :87-91 zero callers; `colorInputRef`/`actionToolbarRef` declared ABL:28-29, bound :103/:116, read nowhere). **≡ ActionButton-adj ABN-16 + Dock-adj DELTA-13 C-35 — already booked.** r-LC's MAJOR severity is REFUSED on identity grounds (a re-reader may not re-grade an adjudicated identity by rediscovery). FOLD.
- **GAB-16 · MINOR — three-hop all-optional dispatch degrades every wiring regression to silence** (r-LC missed find, verified: GeneratePane.vue:22-26 / GradientPane.vue:11-15 forwarders whose second `?.` is unreachable-by-construction — both children render unconditionally). Subsumed by **X.W4.d D2** (the `?.()` deletion); the forwarding `defineExpose` relays land on the deleted-symbol list.
- **GAB-17 · MINOR — two implementations of the Mix result-copy derivation with divergent feedback postures** (r-LC missed find, verified; hidden behind the axis mis-citation killed at K-4). MixPane.vue:49-55 bare `writeClipboard`, no feedback; MixResultDisplay.vue:30-47 `useClipboard` with live `copied` state. **→ X.W4.d**: `copyResult`'s only caller is the shell dispatch — it dies with the nine dispatches; MixResultDisplay's honest seat remains the sole owner.
- **GAB-18 · MINOR — the route-local copy seats the cut names terminal discard the producer's discriminated `CopyResult`** (r-LC missed find, verified against `useClipboard.d.ts` and all named sites; 15 call sites in demo/, 9 explicit `void` discards, exactly one honest consumer). GenerateControls :107/:112 **≡ GenerateControls-adj row 15 — BANKED (B5)**, cross-cited; GradientVisualizer :128 → **X-W6 rider** (file in bounds); MixPane :54 dies with GAB-17. Census recorded so no site is lost.
- **GAB-19 · MINOR — Seed-from-palette is a second, independent false affordance, and its ruled deletion retires a working desktop capability without defect proof** (D-table-seed CONFIRMED + r-LC's INFO merged). GradientVisualizer.vue:110-116: silent `!pm` return, silent `< 2` return, silent first-palette choice — enabled-but-inert for a user with 0-1 saved palettes even where the ref IS populated; on desktop gradient the shell path WORKS today and the agglomeration deletes it on UX grounds alone (:47-48). **→ X-W6** (GradientVisualizer in bounds): redesign-with-explicit-chooser or delete is that wave's design decision; the working-capability retirement is named so the owner rules it, not a cut.
- **GAB-20 · INFO — the nine authored `description` strings are unreachable content on the one surface where this row is the only command face** (r-DU missed find). **≡ ActionButton-adj ABN-7** (description reaches no AT, no touch user, no disabled seat — already routed X.W4.d) — FOLD. r-DU's unproven touch limb is DISCHARGED STATICALLY by ABN-3's bundle read: on a coarse pointer the hover Popover renders reka's CLICK-semantics Popover, so a tap opens the card AND fires the action simultaneously — reachable, and worse than unreachable; live tap behaviour remains ABN-3's U-9.
- **GAB-21 · INFO — no CI project exercises the generic bar, the mobile action bar, or the mobile edit flow** (r-LC missed find, verified: smoke-mobile = 3 specs, Pixel 7; o15 runs at 1280×720 on the picker route; safari mix-flow drives the local button). The authored cures already exist as gates: **X.W4.d D4's `scene-action-contract.spec.ts` + X-W5.a A3 + Dock-adj G-I** — cross-booked as their motivating evidence, no new row.

Also of record: r-DU's FALSIFICATIONS row — the corpus's two falsifications of the prior corpus (no mobile mount guard exists; only three routes return descriptors) are themselves correct; verified (Dock.vue:153 carries no viewport predicate; usePaneRouter.ts:227 returns null otherwise).

## Killed-claims register

**Corpus claims killed (upheld or added by this seat):**

- **K-1 — "Binding browser receipt / binding root observation" (all three axes, challenge-C:18-24 · challenge-D:17-23 · challenge-L:17-23)**: KILLED as an evidentiary label. HYDRATION-LEDGER.md:295-297 marks all three files UNWITNESSED-DIRECT, `—` provenance, 0 artifacts; the agglomeration itself concedes the witness is text-only and the Luna browser attempt died at `net::ERR_CONNECTION_REFUSED` (:10-15). Three verbatim copies of one unwitnessed paragraph are one claim, not three receipts. The DEFECT survives — GAB-1 is fully proven from source — but the receipt is "source-corroborated hypothesis" wherever carried. [r-LC's kill, upheld; r-DU concurring.]
- **K-2 — C-GA-3's "can mask" / "changes between masked and visible-but-inert as layout changes" (challenge-C:66/:71-73)**: the hedge is STRUCK, the finding survives strengthened — masking is unconditional (ColorPicker.vue:315/:343 + viewSchema.ts:142-151), the descriptors never operative anywhere. The non-governing agglomeration states it correctly; the governing axis does not, and the weaker framing is what an execution seat would carry. [r-DU's kill and r-LC's confirm-with-strengthening, merged.]
- **K-3 — "individual removal already provides the honest operation" (challenge-C:104-107) / "Operand removal is already at each source" (challenge-D:44)**: KILLED against MixSourceSelector.vue:37/:39/:154 — per-item removal is hard-disabled at one remaining colour and can never reach zero. The corpus self-contradicts (the agglomeration's correction 2 states the refutation its own governing axes still prescribe). [r-LC's kill + r-DU's FINAL-correction-2, merged → GAB-10.]
- **K-4 — challenge-C:53-56 names `MixResultDisplay.vue:42-47` as the shell descriptor's Mix copy owner**: mis-citation — usePaneRouter.ts:222 dispatches `copyResult`, defined at MixPane.vue:49-55, a separate implementation. The conflation hid GAB-17. [r-LC.]
- **K-5 — "Minimal cure: delete the boundary. Route-local controls call the owning operation directly and can read its real state" (challenge-C:63-64)**: KILLED as a sufficiency claim. The capability is real; the practice is not — three of the four named seats `await writeClipboard(…)` and read nothing (GenerateControls :107/:112, GradientVisualizer :128, MixPane :54); only MixResultDisplay owns feedback. The cure-is-adjudicated-separately law applies (ColorSpaceSelector-adj addendum 2): the cut enumerates none of the outcome work its own C-GA-2 demands. [r-LC; → GAB-3 rider, GAB-18.]
- **K-6 — agglomeration :35-37, "remove … generic icon/label/accent fallbacks" in Dock.vue**: KILLED as written. Dock.vue:186-188's `??` right-hand operands are the ONLY icon/label/accent the SURVIVING picker toggle has, and ActionBarToggle.vue:38-47 declares all three as required props — the literal sentence strips the survivor, breaks typecheck three ways, and makes `Paintbrush` (Dock.vue:3) look dead when it must live. Correct cut: delete the `genericBar?.x ??` prefixes, keep the literals. The hedged axis texts are fine; the one document an execution seat would follow is the one that is wrong. [r-DU; verified against both files this session.]
- **K-7 — challenge-L:105-107, "Luna's 'second implementation of the same action bar' is false at the product-contract level"**: KILLED. Luna's claim was explicitly implementation-level and is byte-true at HEAD (identical root strings, same sole leaf, same `activeHover` protocol — GenericActionBar.vue:15/:11/:29 vs ActionToolbar.vue:2/:85 + five repeats); the same axis's own L-GA-2 restates it ("owns only a copied hover mutex and a v-for"). The product-role distinction (picker mode surface vs workbench mirror) is real and carries the FOLD rejection on its own; the FALSE label dies, and its cost was real — it orphaned the shared layout defect onto the survivor (caught at ABN-19). [both readers; r-LC's framing adopted.]
- **K-8 — this seat's own kill, caught by neither reader — challenge-D:88-92's canon citation is selective and the per-verb DELETE ruling is unsound as ruled law**: CONVERGENCE-REAUDIT-2026-07-29.md:104-125 does make the scene `action` role optional — and the SAME terminal diagram assigns the Dock "active-scene commands"; `VISUAL-CONSTITUTION.md:194` and DECISIONS.md B-13 bind Generate's three verbs into ONE Dock control set; GenerateControls-adj ruling R-B (a standing adjudication) ruled the PLATE cluster the violation and banked its retirement INTO the dock set. Challenge-D's command-seat table rules the exact inverse for the same three verbs. A corpus may argue canon change; it may not silently overrule it. The DELETE of the renderer + untyped branch stands (X.W4.d orders it independently); the per-verb seat table is DEMOTED from ruling to input and escalated (GAB-5 → X-W10 owner item).

**Reader claims killed or trimmed (the incredulity duty runs both ways):**

- **RK-1 — r-LC's MAJOR on the dead-symbol cluster**: severity REFUSED on identity — the facts are booked at ABN-16/C-35 in MINOR-grade folds; rediscovery does not re-grade an adjudicated identity (GAB-15).
- **RK-2 — both readers' framing of the `colorPickerRef` find as unowned ("no axis states it … all three then forbid fixing it")**: true of the AXES, false of the PROGRAM — Dock-adj DELTA-1/L-35/G-I/R-8 own every limb, with a RED gate of record. Filing it as a novel BLOCKER would have double-booked a built row. Demoted to corpus-gap + the adopted cut-correction (GAB-11).
- **RK-3 — r-DU's unproven "whether the hover Popover opens on touch at all"**: DISCHARGED — not live-unprovable; ABN-3's bundle read settles the mechanism statically (coarse pointer → click-semantics Popover). Removed from this record's residue; the live tap witness remains tracked at the sibling seat (U-9 there).
- **RK-4 — r-DU's "the Tools toggle, Back control and the whole action-bar DockLayer become desktop-only dead weight" (post-cut limb)**: TRIMMED, not killed — those controls remain fully live on desktop picker routes; the correct post-cut statement is that NO mobile route can ever render any action-bar affordance. The substance (mobile command death both before and after the cut) stands inside GAB-11.
- **RK-5 — census drift**: r-DU "seven `any`s", r-LC "seven" enumerating nine, X.W4.d D2 measuring ten on its scoped path. No verdict turns on it; the gate's scoped census governs; recorded so the next seat does not re-litigate arithmetic.

## Superlatives — preserved with provenance

- **Citation fidelity across ten days of drift**: both readers re-verified 20-30 coordinates each against HEAD `8d7e17fe` from a corpus cut at `e01d0065`; worst drift two lines (ActionButton.vue:48-55 → :50-55); exactly one true mis-citation in the whole corpus (challenge-C:55, K-4). This seat's ~40 spot re-reads concur: every coordinate landed.
- **Ownership discipline under a shared-leaf temptation** (corpus): challenge-C:58-62/:93-95 and challenge-L:78-81 refuse the ActionButton leaf defects by name and scope this file to "the mapping, hover mutex, and handler invocation" — verified correct, and exactly matching where the ABN roster later booked those leaves.
- **Motion hygiene by omission** (corpus): zero motion/PRM claims filed; the only motion carrier in reach is already ruled FALSE-for-PRM at motion-quarantine **M-32** (probe4 on the real `.action-icon`: `animation-duration 1e-05s`, iteration 1 — pulse and spin neutralised). No quarantined row carried, none smuggled. Per the commission's law this record carries none either.
- **The agglomeration corrects its own winning candidate** (corpus): Mix upgraded to never-operative; the MIN_COLORS floor found. Both verified true here — and both then survive as kills against the governing axes that failed to absorb them (K-2, K-3): self-correction recorded in the wrong document is only half-done.
- **Evidence honesty** (corpus): the agglomeration names its own witness text-only, names the dead Luna browser attempt, and makes durable evidence an acceptance item rather than laundering the receipt.
- **r-LC's zero-dependency-computed derivation (GAB-7)**: genuinely new above the corpus, statically airtight, and the best available justification of X.W4.d D1's anti-fake falsifier. The strongest single contribution of either reader.
- **r-DU's K-6 catch**: an execution-breaking error found in the exact document an execution seat would follow, proven against required-prop declarations — the incredulity chain doing precisely its job.
- **Live source worth protecting through the cuts** (r-LC, verified): usePaletteWiring.ts:33-58's bounded, named give-up (the one loud failure in the whole bridge — the reason the mobile death is diagnosable); PaneSlot.vue:12-23's in-situ post-mortem; useMixingState.ts:1-16's ONE-CLOCK law; MixResultDisplay.vue:30-47/:119-143 as the tree's only honest copy seat — the pattern every surviving copy verb should generalize.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1** — the ROOT 390×844 Reset witness (90°→337°→Reset→337° after 250 ms): text-only, no artifact, no verdict depends on it. Must be replaced by a durable capture at X.W4.d D4's MEASURE-AT-OPEN; never re-carried as "binding".
- **U-2** — whether the `action-bar` DockLayer is stretched by the producer grid at desktop widths or content-sized everywhere: decides which branch of the ABN-19 spacing defect renders (abutting seats vs untokenized count-dependent gap). The banked 32 px pitch settles only 390×844. Defect holds in either branch.
- **U-3** — KeepAlive round trip of the desktop generic refs (route away and back, then click Regenerate): Vue 3.5's setRef KEPT_ALIVE branch says re-capture SHOULD occur; one click confirms. Cheap; belongs in the D4 spec.
- **U-4** — the portrait-tablet band's action-bar face specifically: DELTA-5's `collision-1024x1366.png` witnesses the grammar collision; whether an edit-commit affordance exists AT ALL in that band remains unprobed (App mobile slots capture nothing while Dock suppresses the mobile-edit layer as "desktop").
- **U-5** — Tools-toggle absence on mobile picker-family routes: mechanism now read end-to-end (`:visible=false` → no `.is-visible` → `0fr` + opacity 0 + `tabindex="-1"`, ActionBarToggle.vue:92/:115-128) — DISCHARGED-STATIC; a single frame at 390×844 `/#/` closes it cosmetically.
- **Owner items, not probes**: the GAB-5/K-8 canon collision (dock set vs route-local seats, per verb); GAB-10's which-seat-becomes-the-honest-Clear; GAB-19's retire-or-redesign of Seed. r-LC's "FOLD-vs-DELETE not assessed" residue is hereby RESOLVED at the component level (X.W4.d collapse — neither candidate's disposition survives as written) and ESCALATED at the verb level.

## Closing verdict

**All eight corpus findings CONFIRMED (one reframed stronger, one demoted from ruling to input) + the agglomeration's second correction confirmed; 8 corpus claims KILLED (5 reader kills upheld, 1 merged framing strike, 1 mis-citation, 1 canon-collision kill of this seat's own); 13 reader-raised rows adopted, of which 6 fold to already-adjudicated identities (DELTA-1 · DELTA-5 · ABN-7 · ABN-16/C-35 · ABN-19 · ABN-21 · GC-row-4) and 2 land NO-WAVE-OWNER (GAB-10 whole; GAB-12's Mix half); 5 reader claims killed or trimmed back. Terminal disposition: the renderer and its untyped descriptor branch die inside X.W4.d's `SceneActionSet` collapse (gates D1-D4, file in bounds) with X-W5.a owning mobile parity and Dock-adj R-8 owning the registry cure — the corpus's naked DELETE and its retain-`colorPickerRef` cut steps are superseded as written; the per-verb seat table is escalated to the X-W10 canon lane under the standing VC:194/B-13/R-B collision. Nothing here stamps a wave: ADJUDICATED, not VERIFIED.**

— end of apotheosis. The challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3; dated evidence unrewritten.
