claude-fable-5

# fr-ContourSettings — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-08 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourSettings.vue` (470 lines: 187 script · 131 template · 148 style) + its store/loader/adapter/contract chain — the panel that is the app's sole compute orchestrator.
**Substrate**: fourier HEAD `cd26c653` — re-pinned by this seat (`git log -1` + porcelain). **Dirty-tree disclosure**: the subject file is CLEAN; its adapter `SliderControl.vue` is DRIFTED by exactly the 3-hunk `variant="glass-scrubber"→"standard"` rename (comment ×2 + the `variant=` attribute) — this seat re-ran `git diff` and reproduced the three hunks; all anchors in this record are working-tree (the state that ships) and stable across the drift. The live retint block is `SliderControl.vue:143-149` (the D axis's `:138-146` is stale by the drift).
**Corpus**: 66 accusation rows across three axes — challenge-D (design, 24 defects + 6 superlatives, opus-5[1m]), challenge-L (library, 17 + 3, opus-5[1m]), challenge-C (consumption, 25 + 6, opus-5[1m]) — at `docs/tranches/V/megatranche/audit/fourier-components/ContourSettings/`.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Returns arrived through the orchestration channel; the immutable challenge files sit beside this record; every kill, every promotion, and every reader-vs-reader conflict was **re-derived by this seat against the trees with its own instruments** before adoption.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. One reader cell died under this seat's reads (reader-DU's confirmation of the L-M3 orphan-listener leg — refuted at the installed Vue, see R-1/K-4); one UNPROVEN disposition was promoted to CONFIRMED after this seat closed the premises independently (R-2); two both-reader severities were overruled by banked identity law (R-7); one reader kill was trimmed as over-broad (R-4). Identity guard swept the banked registry first: **six rows fold by reference** (fr-BasisSelector B-2/B-3/m-10, fr-BasisCanvas D-1, fr-AnimationControls C-1, fr-AppHeader FR-AH-33) and are never re-booked.
**Arbiter's own instruments this session (all static, zero browser — probe parsimony; product source read-only)**: whole reads of ContourSettings.vue (470), stores/workspace.ts (471), useWorkspaceLoader.ts (136), SliderControl.vue (150 + the git diff), ui/tooltip/Tooltip.vue, lib/api-problem.ts, composables/useToast.ts, lib/defaults.ts, useViewState.ts; ranged reads VisualizationView.vue (:40-60/:85-100/:115-130/:160-175/:240-280), router/index.ts:36-120, lib/api.ts:170-200, lib/types.ts:29-42/:70-81; repo censuses: `store.reset()` callers = **1** (VisualizationView.vue:170, the `store.error && !store.imageSlug` arm); `extractContour|computeEpicycles|computeBases` callers = **ContourSettings.vue:126/:129/:130 only**; `saveContourPoints` consumers = **VisualizationView.vue:94 only**; `store.contour` watcher census (useImageOverlay/useViewState only — zero compute); e2e coverage grep (`Min Area|Max Contours|Smoothing|ML Threshold|Reset to defaults|retry`) = **0 hits**; `pageerror` in contour-extraction.spec.ts = **0**; visualization-ux.spec.ts:105-135 read (both fixme justifications verbatim). Installed-runtime extractions: **@vue 3.5.38** runtime-dom patchProp:735-738 (`isOn` → `if (!isModelListener(key)) patchEvent(...)`) + shared:24 (`isModelListener = key => key.startsWith("onUpdate:")`); runtime-core `callWithAsyncErrorHandling`:205-213 (`res.catch(err => handleError(...))` on promise-returning handlers); reka-ui TooltipTrigger.js:54 (`handlePointerDown` closes) + **:59-60 (`handlePointerMove(event){ if (event.pointerType === "touch") return; … }`)**; SelectRoot.js:19 + PopperRoot.js:7 both `inheritAttrs: false`; glass-ui dist/cn-DJXf4yaB.js read WHOLE (clsx + hand-rolled last-wins-per-`scope|group` resolver; ungrouped classes always survive; `text-dropdown` matches neither font-size list → `text-color` bucket; `rounded-pill` matches no group; `h-*` one group); SelectScrollDownButton chunk (`control-surface` trigger recipe; **`SelectValue` shipped in-chunk and unimported by the consumer**; the `flex flex-col gap-0.5 min-w-0` SelectItem column); tokens/offsets-sizing.css:151 (`--control-h-md: max(calc(2.5rem * var(--ui-scale)), var(--control-floor))`) + tokens/light-dark.css:17-22 (coarse-pointer `--ui-scale→1.5`, `--control-floor→var(--touch-target, 2.75rem)`); ConfiguratorRow.vue.d.ts (`canReset` + `description` both present at the pin); configurator chunk `inert` ×2; `grep -rl slider-scrub` over the dist → **empty**. Backend: image_storage.py:248-267 (**`extraction_cache_key` = a CLOSED 10-field `json.dumps` literal — `ml_threshold`/`ml_detail_threshold` ABSENT**); images.py:212-248 (cache short-circuit BEFORE `compute_contours`; the 422 with the actionable `detail`); **main.py:114 — the sole `@app.exception_handler(Exception)`; no HTTPException→problem+json handler**; contours/models.py:17-26 (7-member enum incl. `ADAPTIVE_THRESHOLD`); ml.py:128-137 (threshold ladder off both ml fields); isolation.py:40; processing.py:70-96 (`area_threshold = min_contour_area * image_area`; the `0.92·A` upper cut).

## Scoreboard

**48 adjudicated register rows (6 BLOCKER · 16 MAJOR · 18 MINOR · 8 INFO), covering 61-of-66 corpus rows confirmed in some form · 6 identities FOLDED BY REFERENCE (never re-booked) · 12 kills in the killed register · 16 missed finds banked (several composed into register rows) · 7 superlatives with provenance · 11 SS-13 probes.**

Blocker-weight survivors: **B-1** (the "Min Area %" slider drives a [0,1] area-fraction from a 0–20 step-0.5 axis — 40 of 41 detents manufacture an empty contour set and a 422), **B-2** (the retry banner — the panel's only recovery affordance — provably cannot render), **B-3** (the mount-time compute aborts a mid-flight load and persists defaults over the user's draft), **B-4** (the ML Threshold slider is inert after the first extraction — the server's cache key omits the two fields it is the sole producer of), **B-5** (the six local refs latch a store that navigation never clears — opening workspace B destroys B's saved settings with A's, PROMOTED from UNPROVEN by this seat), **B-6** (the panel's entire explanatory layer — the best microcopy in the tree — is unreachable by keyboard AND by touch).

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **L-M3's orphan-DOM-listener leg** ("two permanent addEventListener calls on ConfiguratorLayer's root div") | CONFIRMED — "Vue's patchEvent registers real addEventListener calls … on that div on every mount" | KILLED via `isModelListener` | **LC wins; DU's cell overruled — a reader error, recorded in DISSENT.** | My read of the installed Vue 3.5.38: runtime-dom.cjs.js:735-738 — `else if (isOn(key)) { if (!isModelListener(key)) { patchEvent(...) } }`, with `isModelListener = key => key.startsWith("onUpdate:")` (shared.cjs.js:24). `onUpdate:nHarmonics`/`onUpdate:nPoints` both match ⇒ `patchEvent` is never reached ⇒ **zero DOM listeners**. Only the runtime-cost leg dies; the contract half (v-model over zero `defineEmits`) survives intact and books as its own row, **M-16**. |
| R-2 | **C-6 / L-M1 reachability** (the stale-latch clobber) | UNPROVEN-NEEDS-LIVE (kept in the SS-13 list) | PROMOTED to CONFIRMED — the store is never cleared on navigation | **LC wins; promotion RATIFIED — every premise closed by this seat's own reads. Composed into B-5.** | (a) `store.reset()` has exactly ONE product caller — VisualizationView.vue:170, inside the `v-else-if="store.error && !store.imageSlug"` arm (my grep) — so `/w/A → /gallery` leaves `imageMeta`, `contourSettings`, `epicycleData` holding A (a Pinia store outlives route components). (b) On `/gallery → /w/B` the NEW VisualizationView's first render already has `hasImage === true` from A's stale `imageMeta` (:123), ContourSettings mounts and latches A's `store.contourSettings` (:33-39) BEFORE the parent's `onMounted` runs `loadWorkspace(B)` (useWorkspaceLoader.ts:24-31, my read). (c) `loadWorkspace(B)` replaces `contourSettings` with B's draft (:163-166) and assigns `epicycleData`/`basesData` **null** when B's draft carries no cache (:171-172/:177-178, same synchronous block as the `imageMeta` write) ⇒ the panel's `imageMeta` watcher fires with the guard OPEN ⇒ `runCompute()` writes A's six locals over B's store (:112-123) and `scheduleDraftSave` (workspace.ts:108) persists the loss. (d) The param-only second trigger is real: `/w/:imageSlug?` is ONE route record (router/index.ts, my read — with `alias: ["/visualize"]`), served by the loader's param watcher (:35-47) with NO remount. (e) The clincher this seat adds: the loader's own `{once:true}` watcher (:63-72) re-seeds ITS refs from the replaced store — the sibling composable honours the re-seed contract the panel omits. Precision trim on LC's "GUARANTEES": the loader's `:80-88` nHarmonics reset fires the debounced watcher only when A's value ≠ the 200 default (a same-value ref write is not a trigger). |
| R-3 | **C-4's third call site** (`@click="runCompute"` as an unhandledrejection source) | CONFIRMED at 3 sites | KILLED at the click site | **LC wins.** | My read of runtime-core `callWithAsyncErrorHandling`:205-213: a template handler's returned promise gets `res.catch(err => handleError(err, instance, type))` — the rejection lands in Vue's error channel, never `unhandledrejection`. The two WATCHER sites are genuinely unhandled (block-bodied arrows discard the promise; @vueuse's `createFilterWrapper` resolves on the callback's `undefined`). C-4 survives at 2-of-3 — and the third site is doubly moot (unreachable, B-2). |
| R-4 | **D-M3's disposition** (busy affordance) | CONFIRMED MAJOR (mechanism corrected) | KILLED "as stated" alongside D-M2/D-M4 | **Split — DU's substance wins, LC's trim adopted. RESCOPED-CONFIRMED at MAJOR (M-2).** | D-M3 has two halves. The banner-internal half (spinner/`:disabled` never painted *because the banner unmounts*) is dead — the banner never MOUNTS (B-2), so LC's kill holds there, and D's ":108 nulls it" mechanism was wrong anyway (DU's correction, ratified). The "wider" half is live and banner-independent, verified in my whole-file read: across the 1s debounce → `extractContour` → `allSettled` pipeline, the panel presents NO busy state — `grep -n computing` → :313/:314 only, both inside `v-if="store.error"` (:311); no `aria-busy`, no control disabling; `.retry-btn:disabled` (:450-453) is dead CSS. A panel with no Apply button that silently recomputes for seconds shows nothing. That is a live MAJOR. D-M2 and D-M4 die whole (K-1/K-2). |
| R-5 | **D-B2 / C-17 severity** (the tooltip layer) | BLOCKER | MAJOR | **BLOCKER (B-6) — DU wins, on DU's own missed-find.** | The touch kill upgrades the row: reka TooltipTrigger.js:59-60 returns on `pointerType === "touch"` and `handlePointerDown` (:54) CLOSES — so on coarse pointers **all six** tooltips (including the conformant Button-wrapped sixth) can never open, on a mobile surface the app ships first-class (VisualizationView `panel-inactive` arm; style.css mobile root). Five of six are also non-focusable-wrapped (Tooltip.vue:27 `as-child` over `div.slider-control`, SliderControl.vue:65 — no tabindex injected, my reads). The six parameters are opaque CV knobs and the tooltips are their ONLY documentation — the panel's single best asset (S-1) is delivered on a channel neither keyboard nor touch users can open. That is capability loss on two full input classes: BLOCKER. |
| R-6 | **D-i1 altitude** (the fixme'd a11y gate on a stale premise) | MAJOR | INFO | **Neither — IDENTITY FOLD.** | The `:133` keystone ("ContourSettings Configurator-open is a11y-clean") is one of the four fixmes already censused and banked inside **fr-BasisSelector B-3** (this seat's sibling record: `:110/:133/:192` + crud`:630`; un-fixme is a four-test operation, → F.W3/W4 there). My spot-read of visualization-ux.spec.ts:105-135 confirms the justification verbatim ("it omits `inert` … pending the glass-ui `inert` release + guarded `^2→^3` bump") against an installed 4.0.0 that emits `inert` (configurator chunk, ×2, my grep). One identity — folded, never re-booked; the stale-premise text correction is this component's rider on that row. |
| R-7 | **C-5 and C-13 severities** (both readers: MAJOR ×2) | MAJOR / MAJOR | MAJOR / MAJOR | **Both OVERRULED to identity folds — the banked altitude law wins over both readers.** | C-5 (value.js 0.13.0 vs glass-ui peer range) is **fr-AnimationControls C-1** — ruled there (R-14/K-13, third reproduction) "census §1 pins + G33, component-altitude INFO, **never re-book per component**", and re-affirmed at fr-AppHeader (fourth reproduction, demoted-to-fold with the @lucide/vue rider). C-13 (runtime packages in devDependencies) is **fr-AppHeader FR-AH-33** (→ F.W0 manifest hygiene). Two identities, two folds; the readers' fifth/sixth reproductions are corroboration, not new rows. Fleet lesson recorded in DISSENT: re-readers must sweep the adjudicated registry before scoring carried rows. |
| R-8 | **L-M2's disposition** (the compute key's missing success predicate) | Deferred whole to SS-13 | CONFIRMED MAJOR (static mechanism), interleave-only residue live | **LC wins — CONFIRMED (M-12), composed with LC-missed-3.** | The mechanism is source-closed, my reads: `allSettled` cannot reject; all three store actions return `void` and early-return on stale revisions (workspace.ts:251/:298/:327) — :133 stamps the key whether data landed or not. LC's missed sharpening is the better half and is RATIFIED: :133 re-reads the LIVE refs after the awaits, so a slider change made during flight is stamped as already-computed though the request carried the old values — the debounced callback then `nextKey === lastComputedKey`-drops the user's change (:144-145), silently, forever (the manual retry being B-2's dead banner). Only the full silent-wedge interleave stays SS-13. This also trims the corpus's C-S2 praise: post-await stamping is correct on the failure arm and WRONG on the interleave arm — snapshot-at-request is the one-line cure. |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor (re-verified by this seat unless marked adopted) · ADJUDICATED → route**. Full proof cells live in the challenge files and reader returns, incorporated by reference. Line anchors are working-tree at `cd26c653`; re-resolve before executing any cure (D-19).

### BLOCKER

- **B-1 = L-B1 / C-2 (∘ D-M9's magnitude half) — BLOCKER.** "Min Area %" drives a [0,1] fraction-of-image-area from a `0–20, step 0.5` axis (:271-274; write verbatim at :116; api.ts ships `{...settings}` unmodified). Backend semantics closed by my reads: models.py:194-195 ("fraction of total image area"), the [0,1] clamps (shared.py + models.py), processing.py:72 `area_threshold = min_contour_area * image_area` with :93 the drop filter and :94-95 the `0.92·A` upper cut, images.py:244-248 the 422 on empty. Every detent ≥1.0 (39 of 41) demands `area ≥ A ∧ area ≤ 0.92·A` — **empty by construction**; the 0.5 detent asks for half the image (SS-13 whether it 422s or merely decimates). The default `0.001` is off the step grid and renders "0.0" under a "%" label — a 100× unit lie in the dangerous direction. No `/100` rescale exists anywhere (L's 12-site grep, adopted; spot-confirmed). **ADJUDICATED → F.W3/W4** (unit-true control: fraction domain, percent display, sane step; the typed range-checked descriptor table for all six fields rides an **F.W5–W8 rider** — the contract seam owes client-derivable bounds). Born-RED fixture: drive Min Area to 0.5/1.0 in e2e and assert non-422 (today: 422 or silence).
- **B-2 = L-B2 / C-3 — BLOCKER.** The retry banner — ~60 lines of template/CSS/computed and the panel's only in-place recovery — can never render. Partition + ordering both closed by my reads: `useWorkspaceLoader.ts:125-133` (no `flush` ⇒ pre; registered in the ancestor's setup, lower uid) toasts and nulls `store.error` whenever `store.imageSlug` is truthy; `imageMeta` is never written without `imageSlug` in the same synchronous statement (workspace.ts:123-124/:148-149/:211-212; `reset()` :412-426 nulls both); the panel exists iff `imageMeta` (VV:123/:260/:270). ∴ the half where the banner could render is the half where the component does not exist. Aggravators: the layer is `:default-open="false"` (:190) with an `inert`+`aria-hidden` collapsed body (configurator chunk, my grep), and the panel itself races the same flag (`store.error = null` at :108 — m-8's mirror). **ADJUDICATED → F.W3/W4** (error-channel unification: ONE owner for `store.error`, toast or banner never both; riders that re-arm on cure: the ungated `v-if` over ten heterogeneous write sites (K-1 residue), the missing live-region (K-2 residue), the `.retry-btn` variant inversion (i-2), the `animate-spin` PRM gap (m-2 note)).
- **B-3 = L-B3 — BLOCKER.** The mount-time `runCompute()` (immediate watcher :152-169) reads `store.computing` but not `store.loading`; on the legacy `image_bounds`-null backfill branch (workspace.ts:154-161 — an awaited `getContour` BETWEEN the `imageMeta` write at :149 and the settings restore at :163) the freshly-mounted panel fires `runCompute` → `extractContour`'s synchronous `++revision` (:245) trips loadWorkspace's abort door (:160) → the draft's settings/epicycles/bases and the `_saveDraftNow` are all skipped, and the store's deep watcher (:108) persists the defaults the panel just wrote over the user's saved draft. Silent, durable, "my settings reset themselves." The miss is one identifier wide; the disease is architectural (the app's whole compute pipeline lives in a conditionally-mounted `out-in` leaf — L's closing diagnosis, ratified). **ADJUDICATED → F.W3/W4** (a `useContourCompute()`/store-action orchestrator owned at the route shell; the panel reduced to what it names itself: settings. M-9 and B-5 are the same repair unit).
- **B-4 = C-1 (∘ C-25/R6-8 fold) — BLOCKER.** The ML Threshold slider is inert after the first extraction: `extraction_cache_key` is a CLOSED 10-field `json.dumps` literal (image_storage.py:250-266, my read — `_v, image_sha256, strategy, resize, blur_sigma, n_classes, min_contour_length, min_contour_area, max_contours, smooth_contours, n_points`) that omits `ml_threshold`/`ml_detail_threshold`, the two fields this control is the sole producer of (:121-122); images.py:219-226 short-circuits on the hit BEFORE `compute_contours`. The fields materially drive extraction (ml.py:129-137 threshold ladder incl. the conditional rung; isolation.py:40 on the AUTO route — and `auto` is the shipped default, defaults.ts:4, so the control is live on every fresh workspace). The user drags 0.50→0.85, waits out the debounce, and receives the 0.50 contour with no signal. **ADJUDICATED → F.W5–W8** (the contract constraint C-25 states exactly: *an operation's cache identity must be a superset of the request fields the operation consumes* — the R6-8 seam made product-visible) **+ F.W3/W4** for the client-side regression fixture (assert `contour_hash` instability across an ML-threshold change — also the SS-13 probe).
- **B-5 = C-6 / L-M1 ∘ LC-missed-1 — BLOCKER (PROMOTED, R-2).** The six local refs latch `store.contourSettings` at setup and never re-seed (no store→local watcher in the file; the `:171-177` imageSlug watcher resets only guards), over a store that **navigation never clears** (`store.reset()`: one caller, the error arm). Two closed triggers, both this seat's re-derivation (R-2): the ORDINARY `/gallery → /w/B` path (stale `hasImage` mounts the panel on A's settings before `loadWorkspace(B)` runs; B's draft-without-cached-compute nulls `epicycleData` ⇒ the guard opens ⇒ A's settings are written over B's and persisted to B's IndexedDB row — **opening B destroys B**), and the param-only `/w/A → /w/B` transition (one route record; history back/forward; `uploadImage`'s own `router.push`), where the loader's nHarmonics reset (:80-88) arms the debounce against the stale locals. The loader's own `{once:true}` re-seed (:63-72) proves the missing contract was known one file away. **ADJUDICATED → F.W3/W4** (one repair unit with B-3/M-9: the orchestrator move plus a `store.applyContourSettings` action owning sync/write/draft-save — m-9's cure; a two-workspace round-trip e2e is the born-RED).
- **B-6 = D-B2 / C-17 ∘ DU-missed-3 — BLOCKER (R-5).** The explanatory layer does not exist for keyboard OR touch users: five of six tooltips hang on a non-focusable `<div>` (Tooltip.vue:27 `as-child` replaces reka's default button; SliderControl.vue:65 root, no tabindex; `aria-describedby` lands on the wrapper, not the focus-holding SliderRoot), and on coarse pointers **all six** are unopenable (TooltipTrigger.js:59-60 touch return + :54 pointerdown-closes, my read) — including the reset's, on the mobile arm the app ships first-class. The cure exists at the installed pin and is uplift-stable: `ConfiguratorRow.description` (d.ts verified at 4.0.0; 7.0.0 keeps it) — moving the four sentences to persistent descriptions also discharges 5 of R3-7a's 35-callsite F.W3 budget instead of migrating them. **ADJUDICATED → F.W3/W4** (fold into the R3-7a tooltip pass; do NOT port `as-child`-on-a-div verbatim — C-24's warning, ratified).

### FOLDED BY REFERENCE (identity guard — banked rows, never re-booked)

- **D-B1 (five `:color="VIZ_COLORS.amber"` bindings paint nothing) ∘ DU-missed-2 (BasisSelector's parallel retint copy) = fr-BasisSelector B-2** (the `--slider-scrub-*` family DEFINITION-ABSENT in glass-ui 4.0.0 — **fleet-scoped BLOCKER, 23 dead declarations / 7 files, → F.W1**, token-family acceptance gate). SliderControl's five declarations (:143-149, live anchors) and BasisSelector's four are already inside that 23-count; DU-missed-2 is therefore **already banked**, not a new find — the identity guard catch. Component-local content recorded here: the five callsites (:236/:249/:275/:288/:301), the compounding (the `standard` variant's thumb is invisible-by-design, so even the correctly-spelled token would tint nothing — slider/index.d.ts prose, adopted from three independent reads), and LC-missed-7's HEAD note (at HEAD the callsite passed `glass-scrubber`, not a 4.0.0 CVA member; the WT rename is paint-neutral — fr-BasisSelector K-2's cva extraction, one identity, fold).
- **C-11 (`lib/colors.ts` has no `oklch()` arm; 4 of 5 `--viz-*` tokens → `#888888`) = fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER → F.W1)** — one identity, banked with the canvas blast radius. At THIS component the local consequence is **REFUTED twice over** (K-8): the sole consumed arm (`amber`) resolves correctly via the style.css hsl carry, AND the binding it feeds is dead (D-B1 fold). Component-altitude INFO. Both readers' rulings (DU's consequence kill, LC's INFO rescope) are compatible and ratified.
- **C-5 (value.js 0.13.0 invalid vs glass-ui's peer range, ELSPROBLEMS) = fr-AnimationControls C-1** (census §1 pins + the G33 `npm ls` gate, → **F.W1**; "never re-book per component"). Fifth/sixth reproductions by the readers adopted as corroboration. Component-altitude INFO (R-7).
- **C-13 (lucide-vue-next + reka-ui + clsx + cva + tailwind-merge in devDependencies) = fr-AppHeader FR-AH-33** (→ **F.W0** manifest hygiene; the lucide rename itself F.W1). Component rider: :20 imports four lucide symbols at runtime (one of the censused 35 sites). DU's provenance corrections to the C axis (line numbers off; "nine" runtime deps → eleven) adopted.
- **D-i1 (the only a11y gate over this panel is `test.fixme` on a premise false at the installed pin) = fr-BasisSelector B-3's fixme census** (`:133` is this component's keystone; un-fixme = a four-test operation, → F.W3/W4 there; F.W0 substrate rider). This component's rider: the keystone's justification prose must be corrected to the 4.0.0 `inert` truth when re-armed — re-arming it is also how B-6/M-1/M-2 would have been CI-caught (all three axes said so; ratified).
- **DU-missed-1 (the reset affordance COPY-DUPLICATED byte-for-byte into BasisSelector) = fr-BasisSelector m-10** (zero-output diffs verified in that record: template :121-136 ≡ ContourSettings:191-206, CSS :295-313 ≡ :387-405; ≥2-consumer producer threshold met, → F.W3/W4 + the glass BH header-actions-slot relay) **+ fr-BasisSelector M-11** (the same pointer-events pseudo-disable mechanism, booked there for the sibling copy). Already banked at both halves — fold; this component's OWN copy books as M-1 below with the cross-reference. The lift-don't-patch-locally instruction is already in the banked rows.

### MAJOR

- **M-1 = D-M1 / L-m5 / C-16 — MAJOR.** The Reset control is pseudo-disabled in CSS only: no `disabled` binding (:195-204), `.is-default { opacity:.25; pointer-events:none }` (:399-402) — 1.40:1 at rest (both readers recomputed independently from the shipped tokens; within rounding of each other — adopted as twice-derived), the glass `focus-ring` dimmed inside the 25% box, keyboard-activatable while mouse-inert, and its own tooltip suppressed in exactly the state it would explain. DU's correction folded: the scoped `border:none; background:none` kills the **ghost hover wash** (not glass-wash — `ghost` is `bg-transparent … hover:bg-foreground/8`), and C-16's seven redundant declarations ride along. Cure: `:disabled="isDefault"` (the primitive forwards it) + delete the rule. Cross-ref fr-BasisSelector M-11 (the sibling copy — one lifted cure, two panels). **→ F.W3/W4.**
- **M-2 = D-M3-as-rescoped (R-4) — MAJOR.** No busy affordance anywhere: the only two `store.computing`-bound nodes live inside the never-mounted banner (:313-314 under :311); `.retry-btn:disabled` is dead CSS; across debounce + three requests the six controls stay live and nothing announces. 7.0.0's Button `loading` is the eventual seat (fr-BasisSelector M-7's improve-side note). **→ F.W3/W4.**
- **M-3 = D-M5 — MAJOR.** "Advanced" — the collapsible's only label and only control — rests at 2.50:1 (`color-mix(in srgb, var(--foreground) 40%, transparent)` :345, hover 60% → 4.53:1 :351): compliant only under the pointer, the state a keyboard user never reaches; `text-sm` normal-size ⇒ 4.5:1 due. Both readers reproduced D's arithmetic independently to three digits — adopted as thrice-derived; the glass-tint third digit is SS-13. **→ F.W3/W4.**
- **M-4 = D-M6-as-corrected (K-3) — MAJOR.** The literal `h-10` on SelectTrigger (:211) deletes `h-(--control-h-md)` under glass-ui's own cn (both match the `height` group; last wins — my resolver read) and thereby the WCAG-2.5.5 coarse-pointer clamp: 40px where the system produces 60px (floor 44px; offsets-sizing.css:151 + light-dark.css:17-22, my reads); on fine pointers the override is a pure no-op. THE MECHANISM AS FILED IS DEAD (K-3): `cn` is not tailwind-merge; `text-sm`-vs-`text-dropdown` and `rounded-lg`-vs-`rounded-pill` do NOT de-duplicate (different/no buckets — both ship, CSS emission order decides, SS-13) — F.W1/F.W3 must not carry D's five-row table as fact. **→ F.W3/W4** (delete the override band; the cn seam itself is m-17).
- **M-5 = D-M7 / C-8 — MAJOR.** Both option rows sit in the default slot ⇒ inside reka `SelectItemText` ⇒ the ~90-char description becomes each option's accessible name AND the typeahead corpus (SelectItem.js:100/:107-108, adopted from three reads); glass-ui ships the `description` slot beside it and `textValue`, both unused; the bare `<div>` (:219) collapses the shipped `gap-0.5`. Cure is uplift-stable (7.0.0 keeps both seams). **→ F.W3/W4.**
- **M-6 = D-M8 / C-12 — MAJOR.** Five of six controls bypass `ConfiguratorRow`; the panel hand-rolls `isDefault`+`resetDefaults` (15 hand-synced lines; a seventh knob silently breaks both) beside a subpath that ships the semantics twice (`useConfiguratorState.isDirty/resetCurrent` + `ConfiguratorRow.canReset/@reset` — d.ts verified at the pin). The :191-192 comment's premise is TRUE (single default slot — checked claim, S-6 credit) and its conclusion overshoots. Per-field resets also cure the all-or-nothing semantics and the per-field aria-naming (m-14). **→ F.W3/W4** (+ the banked glass BH header-actions relay, fr-BasisSelector m-10 — no local expansion meanwhile).
- **M-7 = D-M9 / L-m3 — MAJOR.** Two shipped defaults are off their own step grid (`0.001` vs step 0.5; `0.03` vs step 0.05) with strict-`===` `isDefault` — the pristine state is unrestorable by dragging, so M-1's dim-state is false almost always; typing restores Smoothing but NOT Max Contours (the "All" door, m-13). The unit-contradiction half lives in B-1. **→ F.W3/W4** (dies inside B-1's descriptor-table cure).
- **M-8 = L-M4 / C-4-as-trimmed (R-3) — MAJOR.** `runCompute` has no `catch`; the TWO watcher sites discard the promise (block-bodied arrows; @vueuse's wrapper resolves on `undefined`) ⇒ genuine `unhandledrejection` on every extraction failure — including B-1's manufactured 422 — invisible to the e2e's console-only filter (`pageerror` count 0, my grep). Re-entrancy unguarded: `computing` is depth-counted, overlapping calls collide on the per-key abort registry, and the aborted duplicate is paid in full server-side. The click site is Vue-caught (R-3) and unreachable (B-2). **→ F.W3/W4.**
- **M-9 = L-M5 — MAJOR.** The app's entire compute pipeline is owned by a `v-if`-gated leaf inside `<Transition mode="out-in">` (unmount/remount on every edit toggle; my census: :126/:129/:130 are the tree's only compute callers), and the pending 1s debounce survives unmount — @vueuse/shared 14.3.0's `debounceFilter` holds its `setTimeout` in a plain closure with zero `onScopeDispose` (both L and LC read the chain in full at the installed version; adopted as twice-derived) ⇒ a ghost `runCompute` from a dead instance with frozen props. The design decision is recorded at useWorkspaceLoader.ts:90-94 — the fix for the prior race moved the race. **→ F.W3/W4** (same repair unit as B-3/B-5: the orchestrator leaves the leaf).
- **M-10 = L-M6 / C-19 / D-m9 — MAJOR.** Two hand-maintained parallel `Record<string,string>`s stand in for the backend's 7-member `ContourStrategy` enum (models.py:17-26, my read — `ADAPTIVE_THRESHOLD` is real, implemented, and unrepresentable in the picker); the list iterates descriptions, names come from labels, the trigger from a third derivation; the wire type is bare `str` both sides, so the orphan value round-trips and cannot be re-selected once changed away. **→ F.W3/W4** (one typed vocabulary; the shared-enum contract itself is an **F.W5–W8 rider**).
- **M-11 = C-7 — MAJOR.** `max_contours: null` — the legal "All" the panel itself writes (:117) and renders (:289) — cannot survive a remount: three `?? … ?? 16` reads (:36/:65/:74) swallow the null and snap to 24, which is also the default, so `isDefault` then reports the destroyed choice as clean. The C axis alone filed it; both readers confirmed; the write-half honesty is S-5. **→ F.W3/W4** (explicit `=== undefined` reads; dies with B-5's sync cure).
- **M-12 = L-M2 ∘ LC-missed-3 (R-8) — MAJOR.** `lastComputedKey` records the scheduling fact, not the result fact (`allSettled` + void actions + revision early-returns), AND is stamped from LIVE refs after the awaits — an in-flight settings change is recorded as computed though the request never carried it, and the debounce then drops the user's change silently. One-line cure: snapshot the key at :112, assign the snapshot at :133; plus a success predicate. Interleave trace SS-13. **→ F.W3/W4.**
- **M-13 = LC-missed-2, RATIFIED + SHARPENED by this seat — MAJOR.** The hand-edit pipeline is a dead end that *destroys the edit on exit*: `saveContourPoints` (workspace.ts:263-283, sole consumer VV:94) nulls `epicycleData`/`basesData` and computes nothing; NOTHING in the tree recomputes from `store.contour` (my watcher census: overlay/view-state only; the deliberate watcher removal is documented at useWorkspaceLoader.ts:90-94); so the edit cannot be visualized — and **this seat's sharpening**: merely EXITING the editor remounts ContourSettings (the `out-in` swap), whose immediate watcher sees nulled data and auto-runs `runCompute` → `extractContour` → the server cache-hits the ORIGINAL extraction (editor-saved contours carry no `extraction_cache_key` — fr-BasisSelector M-14's verified premise) → `contour.value` is replaced. The user's edit is destroyed by closing the editor, zero settings touched. Sibling row: fr-BasisSelector M-14 (the settings-nudge trigger) — same provenance seam, two triggers, ONE cure. **→ F.W5–W8** (contour provenance/CRUD union: an editor-saved contour must be a first-class input to compute, and extraction must not silently overwrite `source="editor"` assets).
- **M-14 = LC-missed-4, RATIFIED — MAJOR.** Persist-before-verify: `:112-123` writes the store BEFORE the request; the store's deep watcher (workspace.ts:108) drafts to IndexedDB regardless of outcome; `loadWorkspace` restores verbatim. B-1's 422-producing configuration is therefore DURABLE — reload and the workspace 422s again, forever, with the last good contour still on canvas so nothing looks broken. **→ F.W3/W4** (persist on success, or persist-with-status; rides the B-2 error-channel item).
- **M-15 = DU-missed-4 ∘ D-m2 ∘ D-m3, RATIFIED — MAJOR.** The one actionable error string the backend authors is discarded by the client's own error contract: images.py:244-248 raises 422 with detail "No contours extracted — try lowering min area or changing strategy"; main.py has NO HTTPException handler (my read: `@app.exception_handler(Exception)` only) so FastAPI emits `{"detail": …}`; api-problem.ts:36-47 reads `title` (absent) → falls back to `response.statusText` → `super(title)` — the detail is parked on an unread field, `e.message` = "Unprocessable Entity", toasted as such (workspace.ts:256 → useToast). Under HTTP/2 `statusText` is `""` and `"" ?? x` does not fall through → `store.error = ""` → falsy → **no toast, no banner, only M-8's unhandled rejection: silence end-to-end** (transport arm SS-13; nginx terminator unknown). D-m2 (the "503" branch can never match a title) and D-m3 (double truncation, no title/expansion) fold in as sub-facts of the same seam. **→ F.W3/W4** (client: prefer `detail ?? title ?? statusText ?? fallback`) **+ F.W5–W8 rider** (server: a problem+json handler for HTTPException, so the RFC 7807 contract the client was built for is actually spoken).
- **M-16 = C-9 / L-M3(contract) / D-i2 — MAJOR (R-1).** Both mount sites bind `v-model:n-harmonics` / `v-model:n-points` (VV:260/:270, my read) over a component that declares props only — zero `defineEmits`, zero `emit(` (:26-29; my grep of the whole file) — a contract that advertises writeback the panel structurally cannot honour, while the sibling six lines away (BasisSelector:19-23) declares and emits the identical pair: a divergence from the component sharing the binding, not a house idiom, and it hides which child owns the two scalars. The orphan-DOM-listener consequence is DEAD (K-4); the contract defect is whole. Cure: props-only binding at the callsites, or real emits — decided WITH the B-3/B-5/M-9 orchestrator move so ownership is stated once. **→ F.W3/W4.**

### MINOR

- **m-1 = C-15 — MINOR.** The Advanced reveal is declared twice (glass `CollapsibleContent` hard-codes `animate-collapsible-up/down`, live via tw-animate-css; the scoped rules out-specify silently) and the provenance comment cites `@mkbabb/glass-ui/styles/animations.css` — not in the exports map (ERR_PACKAGE_PATH_NOT_EXPORTED for any future reader); the keyframes themselves are real and reachable transitively. → F.W3/W4 (comment correction is mandatory in any touching wave).
- **m-2 = D-m4 — MINOR.** PRM coverage inconsistent inside one file: the collapsible is gated (:370-375, the file's only PRM block), the `slide-down` banner transition is not (:455-469) — moot while the banner is dead (B-2), re-arms with its cure, as does `animate-spin`. → F.W3/W4.
- **m-3 = D-m5 — MINOR.** One control at three track widths (full / half / odd-tail full via :383-385); slider precision reads off track length. → F.W3/W4.
- **m-4 = D-m6 ∘ DU-missed-10 — MINOR (composed).** The row `<Label>` dangles (no `for` — the producer documents "No a11y for/id wiring"); the trigger renders its own `{{ strategyLabel }}` div instead of the shipped `SelectValue` (in-chunk, unimported — my grep), so the recipe's `[&>span]:line-clamp-1` never engages; and `aria-label="Contour extraction strategy"` (:211) wins the accessible-name computation, replacing the value-bearing name with a static string (whether ATs still surface the value: SS-13). WCAG 2.5.3 PASSES (substring) — D's refusal to over-claim, preserved. Cure: `aria-labelledby` onto the row label or LabeledField + `SelectValue`. → F.W3/W4.
- **m-5 = D-m7 — MINOR.** `advancedOpen` is instance-local and both mount arms are `out-in`-destroyed — every edit toggle silently re-collapses Advanced. → F.W3/W4.
- **m-6 = D-m8 — MINOR.** Scoped margins restate/fight the body's `space-y-2`; `.advanced-divider`'s 0.25rem is dead by margin collapse (LC's mechanism refinement adopted: it collapses through the borderless Collapsible root against the chassis's 0.5rem — max wins, conclusion unchanged). → F.W3/W4.
- **m-7 = C-21 — MINOR.** The suppress machinery (2 module `let`s + the :179-186 release watcher) releases itself one microtask after it arms — the release watcher's dep set is a superset of the armers'; the 1000ms debounce guarantees the flag is false when consulted. The real guard is the :145 key compare. Dead machinery; delete with M-12's cure. → F.W3/W4.
- **m-8 = L-m6 / C-22 — MINOR.** A leaf writes two globals directly (`store.error = null` :108; the settings object :112) where every other transport verb is an action — and the :108 write is the second racer on the exact flag whose race kills B-2's banner. `store.applyContourSettings()` is the shared cure seam (B-5/M-14). → F.W3/W4.
- **m-9 = L-m1 / C-10 — MINOR.** `smooth_contours: smoothContours.value as any` (:118) — the file's only type hole, on the one clamped field, both sides already `number`; a fossil of a former boolean shape. → F.W3/W4.
- **m-10 = L-m2 / C-20 — MINOR (D-m10 killed, K-7).** Six runtime-dead `?.`/`??` chains and a triplicated `?? 16` contradicting the declared 24 (defaults.ts:12, my read). Precision preserved from LC: the `??` on `max_contours` is TYPE-required (`number | null`, types.ts:38, my read) — dead by the literal's value, not by type; L's framing is the correct one. → F.W3/W4.
- **m-11 = L-m4 — MINOR.** Five inline `format-value` arrows land in the dynamic-props array (compiler output reproduced independently by the L axis AND reader-LC — adopted as twice-compiled); all five SliderControls re-render on every parent render while everything cacheable was cached. → F.W3/W4.
- **m-12 = C-14 / D-m1-substance — MINOR (warning half killed, K-6).** `class="w-full"` on `<Select>` is a dead attribute — silently: reka `SelectRoot` AND `PopperRoot` declare `inheritAttrs: false` (my reads), so the predicted dev warning does not exist and the dead line is strictly harder to notice. Pure deletion. → F.W3/W4.
- **m-13 = DU-missed-7 ≡ LC-missed-5 (independently found by both readers; mechanics verified by this seat) — MINOR.** "All" is a one-way door: at 0 the formatter returns a non-numeric string ⇒ SliderControl flips to `type="text"` and drops min/max/step (:58-61/:71-79, my read); any partial edit parses NaN ⇒ `clamp` returns `lo` = 0 ⇒ the model doesn't change ⇒ Vue never re-patches ⇒ the DOM keeps the garbled string and every keystroke is eaten; coarse pointers additionally swap virtual keyboards mid-field. A seam defect of this callsite's formatter contract. → F.W3/W4.
- **m-14 = DU-missed-5, RATIFIED — MINOR.** Two buttons with the identical accessible name "Reset to defaults" sit in the same rail simultaneously (:200 here; BasisSelector:130 — both panels in the v-else arm, my VV read), distinguishable only by position; the shipped `canReset` recipe names per-field. The a11y argument that actually forces M-6's cure. → F.W3/W4.
- **m-15 = DU-missed-8, RATIFIED in mechanism — MINOR.** Four declared glyph sizes across six affordances, and the two Button-hosted icons don't render as written: the CVA base's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` (0,2,1) outranks `.h-3\.5` (0,1,0) — authored 14px paints 16px fine / 24px coarse. Pixel confirm SS-13. → F.W3/W4.
- **m-16 = DU-missed-9, RATIFIED — MINOR.** Zero responsive rules in the file while the sibling panel in the same rail ships an explicit ≤639px block — and mobile is where the root font grows to 1.125rem inside the hard two-column `.advanced-grid`. Overflow-vs-crowding SS-13; the divergence is proven. → F.W3/W4.
- **m-17 = DU-missed-6, RATIFIED by this seat's whole read of the resolver — MINOR, GLASS-OWNED.** glass-ui's `cn` is clsx + a fixed regex group table whose buckets misclassify glass-ui's OWN utility vocabulary (`text-dropdown` → text-color; `rounded-pill` → no group; only true collisions like `h-*`/`w-*` de-duplicate) — every "just override the class" cure written against the uplift is silently mis-priced; the winner becomes CSS emission order. **→ glass BH relay** (standing owner edict — producer seam: either the table learns the producer's own registers or the docs state the contract), cross-cited to F.W1 so cures are written against the real merge semantics.
- **m-18 = L-i1 / C-18 — MINOR.** `ml_detail_threshold: mlThreshold.value * 0.6` (:122) — an invented, unnamed coupling that makes an independent contract field unsettable, overwrites any restored value on the next compute, and is absent from both `isDefault` and the compute key. Composes with B-4 (the same field is also uncached server-side). → F.W3/W4 (+ B-4's F.W5–W8 contract rider).

### INFO

- **i-1 = L-i2 — INFO.** Three of the 12-field settings atom are unexposed; Multi-threshold — an offered strategy — surfaces ZERO strategy-specific controls (`n_classes` is its only knob and is never bound in web/src). A product-surface decision needing an authored ruling, not an accident. → F.W3/W4 note + SS-3/SS-4 input.
- **i-2 = DU-missed-11, RATIFIED — INFO.** `.retry-btn` (:430-448) inverts the `variant="destructive"` it mounts — translucent outline where solid was requested, pill geometry deleted; unreachable today (B-2), re-arms the moment the banner is cured; the cure is one class deletion. → F.W3/W4 (rides B-2).
- **i-3 = DU-missed-12, RATIFIED — INFO.** The one panel owning the app's entire compute pipeline is the one panel that defaults COLLAPSED (`:default-open="false"` vs the sibling's `true`), its collapsed body inert — on a fresh workspace none of the six controls is reachable; three sit two disclosures deep. An IA choice worth a deliberate ruling. → **NO-WAVE-OWNER** (SS-3/SS-4 spec authoring; the F.W3/W4 pass should not silently flip it without the ruling).
- **i-4 = DU-missed-13, RATIFIED — INFO.** SliderControl ships a `subtitle` affordance (:27/:69, my read) built for exactly the unit-annotation gap M-7/B-1 diagnose, and none of the five callsites uses it — no value in the panel carries a unit. The cheapest half of the cure, one prop per callsite. → F.W3/W4.
- **i-5 = LC-missed-6, RATIFIED — INFO.** Stale cross-workspace paint: the same never-cleared store (B-5's root) renders A's contour/epicycles/settings for the whole duration of B's load; `store.loading` is held for the entire load and read NOWHERE in this panel or its mount guards — the same one-identifier miss as B-3, applied to the render gate. → F.W3/W4 (rides B-5).
- **i-6 = C-24 / R3-7a — INFO (census confirm).** Exactly 6 Tooltip callsites (:194/:229/:242/:268/:281/:294) — intake number exact, third and fourth independent recounts. The F.W3 budget stands; B-6 must ride the same pass.
- **i-7 = C-25 / R6-8 — INFO (fold).** B-4 is the live product-visible instance of the R6-8 operation↔client seam; the sharpened constraint ("cache identity ⊇ consumed request fields") is B-4's F.W5–W8 payload.
- **i-8 = L §5's derivation recommendation — INFO.** The R5-7/`NATIVE_TEMPLATE_LOOP` class does not bite here (the file's one `v-for` is a component callsite), but its dual does: the loop's SOURCE is an untyped literal disagreeing with the backend enum (M-10) — invisible to callsite-keyed and native-loop-keyed derivations alike. F.W4's audit needs a loop-source-provenance counter (literal vs typed domain). → **NO-WAVE-OWNER** (SS-3/SS-4 census methodology; cross-ref fr-BasisSelector i-2, same family).

## Killed-claims register

- **K-1 · D-M2 ("Retry renders for EVERY workspace error … wrong remediation for six of ten sources") — KILLED** (both readers; ratified by this seat's useWorkspaceLoader + co-assignment reads). The premise inverts: the banner renders for NO error (B-2). D never read useWorkspaceLoader.ts — absent from its §0 read set — and it is the one file that decides the row. Residue booked as a B-2 cure rider: the `v-if` is indeed ungated over ten heterogeneous write sites and must not be repaired as-is.
- **K-2 · D-M4 (the banner is silent to AT) — KILLED as a live row** (both readers; ratified). Nothing that never mounts can be un-announced. The app's actual error surface is the toast — and the real AT question there is M-15's discarded detail. Residue: a live-region lands with the B-2 cure.
- **K-3 · D-M6's mechanism ("cn is tailwind-merge, so every local class wins") + its `text-sm`/`rounded-lg` table rows + its falsifier — KILLED** (both readers; settled by this seat's whole read of cn-DJXf4yaB.js). Hand-rolled last-wins group resolver; the two cited rows don't de-duplicate at all (both classes ship; CSS order decides — SS-13); only the height/width rows collide. The touch-floor conclusion survives on the height group alone (M-4).
- **K-4 · L-M3's orphan-listener consequence — KILLED** (reader-LC; RATIFIED against reader-DU's confirmation by this seat's own runtime read — R-1). Vue 3.5.38 `patchProp` skips `isModelListener` keys; zero DOM listeners are attached. The v-model-without-emits CONTRACT half survives whole and is booked as **M-16**.
- **K-5 · C-4's third call site (`@click="runCompute"` → unhandledrejection) — KILLED** (reader-LC; ratified — runtime-core attaches `res.catch` to template-handler promises). Two of three sites survive (M-8); the third is also unreachable (B-2).
- **K-6 · C-14 / D-m1's "dev-mode Extraneous non-props attributes warning" — KILLED** (both readers; ratified — `inheritAttrs: false` at reka SelectRoot.js:19 AND PopperRoot.js:7 suppresses inheritance and the warning by design). The class is dropped SILENTLY (m-12). D also mis-located the mechanism (the attrs stop at SelectRoot, not the fragment).
- **K-7 · D-m10 ("a `??` that can never fire — `max_contours: number`") — KILLED** (both readers; ratified by my types.ts:38 read: `number | null`, so D's own falsifier is satisfied by the tree — the `??` is type-REQUIRED and dead only by the literal's value). The surviving residue (16-vs-24, triplicated) is m-10.
- **K-8 · C-11's component-local consequence ("remove the carry and every slider track goes grey") — KILLED** (reader-DU; ratified). The tracks are not painted from `VIZ_COLORS.amber` in any state — the projection targets the definition-absent `--slider-scrub-*` family (fr-BasisSelector B-2). Deleting the hsl carry changes nothing this component renders. The colors.ts measurement itself stands and is banked at fr-BasisCanvas D-1.
- **K-9 · challenge-C's template/style line anchors — KILLED as provenance** (both readers; ratified by my whole read: the file is 470 lines; C cites :492-506 among others — past EOF). Live anchors: banner :310-318, banner CSS :407-453, slide-down :455-469, `.reset-icon-btn` :387-405, `.is-default` :399-402, collapsible rules :361-375. C's script-region anchors are exact; the style half must be re-anchored before any F.W3/F.W4 edit. D and L verify to ±1 (D's sole slip: `:287` for the Max-Contours `:color`; live `:288`).
- **K-10 · C-17's "hover/touch-only" wording — KILLED** (reader-DU's missed-find, ratified by my TooltipTrigger read). Touch opens NOTHING — `handlePointerMove` returns on `pointerType === "touch"` and pointerdown closes. The corrected, worse fact is folded into B-6.
- **K-11 · D-S5's scope ("this component is OFF the entire named uplift break surface … entire F.W1 cost is line 20") — RESCOPED** (reader-DU; ratified via my useToast.ts read). True for the file's own import block; false for the feature: every failure this panel can produce routes store.error → useWorkspaceLoader → useToast.ts:3 `type ToastVariant` — the censused F.W1 break surface is one hop from this component's only error channel. The superlative survives with the caveat attached.
- **K-12 · Reader-DU's two self-falsified hypotheses — PRESERVED so they are never re-filed** (both re-derivable from the producer chunks; spot-consistent with my reads): (a) the Advanced trigger DOES have a focus indicator (`CollapsibleTrigger` merges `tap-squish focus-ring …` ahead of the consumer class); (b) the reset button does NOT undershoot 2.5.5 (the `icon` rung is a fixed `--control-h-md` box; the scoped `padding` is inert inside it — 40px fine / 60px coarse). Null results at full rigor.

## Superlatives — with provenance (L-18 runs both ways)

1. **S-1 · The best help microcopy in the visualization tree** (D-S1; both readers attacked it and failed; this seat concurs from the whole read). Every string names a concrete failure mode AND its direction of travel ("crank it up for furry subjects or noisy backgrounds"; "raise to drop grass, fences, and stray edges"); the siblings ship bare nouns. **Preserve the strings verbatim through the B-6 cure** — they become `ConfiguratorRow.description` bodies, not casualties. That the file's best asset rides its most broken channel is exactly why B-6 is a blocker and not a nit.
2. **S-2 · Motion posture, correct at the specificity level as well as the intent level** (all three axes + both readers; keyframes/tokens existence spot-verified). PRM gates both collapsible animations above every competing rule; named transition properties on canonical easing tokens; zero `transition: all`; the retirement of the hand-rolled keyframes is comment-documented and true (only the module SPECIFIER in the prose is unreachable — m-1).
3. **S-3 · Client and server independently converged on content-addressed compute identity** (L-S1/C-S2; both halves re-read by this seat) — `currentComputeKey()` normalises the 0→null sentinel INSIDE the key; the server keys extraction on `extraction_cache_key(sha256, cs)`. The same idea, no shared code — and the audit's two sharpest defects are exactly the two asymmetries between the halves (B-4: the server's key is two fields short; M-12: the client stamps its key from the wrong instant). The praise's "correctly scoped stamping" half is TRIMMED by M-12 — correct on failure, wrong under interleave.
4. **S-4 · Serial-then-parallel compute expressing the true dependency graph** (C-S4; store comments verified) — contour awaited first, the two independent computes `allSettled` so one basis can fail alone, split revision counters honouring the store's own documented race fix. The beneficiary does not re-break the fix.
5. **S-5 · The 0→null sentinel handled honestly on every WRITE path** (C-S6; :87/:117/:289 + shared.py:37-42) — which is precisely what makes M-11 (the read half) and m-13 (the input-seam half) the sharper findings rather than cheap ones.
6. **S-6 · Decision-recording comments that are CHECKED claims** (C-S3/D; producer surfaces re-verified) — :191-192's "no header-actions slot" is true against the d.ts; :354-360's keyframe claims are true against the dist. An author who reads the producer before working around it. Bounded credit per the fr-BasisSelector m-18 precedent: the checked premise carried an overshooting conclusion (M-6).
7. **S-7 · A self-cured a11y defect with its receipt left in the gate** (D-S4; spec prose read by this seat) — the axe `button-name` CRITICAL fixed at :211 and documented at visualization-ux.spec.ts:125-127 — with the m-4 caveat: the chosen instrument traded the trigger's value-bearing name for a static one. Also S-8 in brief: five glass subpaths, zero direct reka, zero shadcn copies, both local adapters documented (C-S1 — the census's "deepest, cleanest consumer" holds at this file), with K-11's ToastVariant caveat attached.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **The deployed transport arm of M-15** — HTTP/2 ⇒ `statusText === ""` ⇒ store.error `""` ⇒ total silence vs HTTP/1.1's "Unprocessable Entity". Probe: force the 422 (Min Area % → 0.5) against the deployed terminator; read store.error + toast.
2. **B-1's single 0.5 detent** — 422 outright vs decimation (the ≥1.0 band is closed by arithmetic; this detent depends on the image's area distribution).
3. **B-4's response identity** — assert `contour_hash` stability across an ML-threshold change on an `auto` workspace; the natural born-RED regression fixture for the cure.
4. **M-12's fully-silent wedge** — the revision-bump interleave that wedges the key without re-scheduling needs a live trace.
5. **B-2's single-frame paint** — the scheduler argument is closed statically (pre-flush parent watcher, lower uid); a one-frame flash is excluded to high confidence, belt-and-braces probe: force the 422 and assert `.retry-banner` never becomes visible.
6. **M-4's non-height rows** — `text-sm`-vs-`text-dropdown` and `rounded-lg`-vs-`rounded-pill` both ship; computed-style read decides which paints.
7. **M-3's third digit** — the `.configurator-layer` glass tint is unmodeled; the 2.0 gap will not close.
8. **The strategy dropdown's proportion** (D-i3) — six items × title + `max-w-[280px]` description vs the left rail; measure the popper.
9. **The `.advanced-grid` at ≤639px** (m-16) — overflow vs crowding at the 1.125rem root under coarse-pointer `--ui-scale` 1.5.
10. **m-4's AT half** — whether a screen reader still announces the collapsed combobox's selected value when `aria-label` supplies the name.
11. **m-15's pixel confirm** — the `--ui-glyph` override's rendered sizes (16/24px vs authored 14px).

## DISSENT — preserved, not ruled away

1. **A reader-cell death, recorded as evidence about the fleet** (R-1/K-4): reader-DU CONFIRMED L-M3's orphan-listener leg with an asserted mechanism ("Vue's patchEvent registers real addEventListener calls … hyphenated to `update:n-harmonics`") that is false at the installed runtime — the claim was inherited from the L axis's confident prose, not re-derived against `patchProp`. The same reader independently KILLED five other cells correctly, including two of its own hypotheses (K-12) — the error is citation-inheritance under load, the ColorSpaceSelector DISSENT-1 lesson in a new file. Detectable, again, because the refuting read is four lines long.
2. **Reader-DU's severities preserved where overruled**: C-23 at MAJOR ("the two covered controls are exactly the two cached-key fields — that is why C-1/C-2 shipped") — this seat folded the coverage claims into B-1/B-4 as born-RED riders and kept the row at the filed INFO; DU's causal framing is adopted verbatim into both blockers. D-i1 at MAJOR — folded instead (R-6); DU's argument ("the gate's re-arming is how three of these rows would have been CI-caught") survives inside the fold rider.
3. **Reader-LC's D-B2 at MAJOR** — overruled to BLOCKER (R-5) on the strength of DU's own touch missed-find; LC's narrower keyboard-only scoring is preserved here as the minority position.
4. **Both readers scored two already-banked identities as fresh MAJORs** (C-5, C-13 — R-7). Neither swept the adjudicated registry before scoring. Fleet lesson for M-25 agglomeration: the identity guard is part of the re-read brief, not only the adjudicator's; a banked row re-scored per-component is how roll-ups double-count.
5. **Precision trims on adopted missed-finds, preserved so the F-wave does not inherit over-claims**: LC's "GUARANTEES a runCompute ~1s later" holds only when the prior workspace's `n_harmonics` ≠ the 200 default (same-value ref writes don't trigger); LC's B-5 gallery chain requires B's draft to carry no cached compute data for the ZERO-ACTION arm (the one-nudge arm needs no such condition). Both trims weaken nothing load-bearing.
6. **This seat's own additions beyond the readers**: the exit-editor auto-destroy arm of M-13 (the remount's immediate watcher, not only the settings nudge, destroys the edit); the loader's `{once:true}` re-seed as B-5's sharpest contrast; the K-11 useToast one-hop re-derivation. Each is stated with its instrument above and is challengeable on the same terms as everything it judges.

**Closing verdict** — ADJUDICATED, DEFECTIVE-WITH-DISTINCTION: the best-written panel in the visualization tree (microcopy, motion, checked comments, content-addressed idempotence) is simultaneously the app's most dangerous component — the sole compute orchestrator, mounted transiently, whose every safety surface is dead (retry banner), silent (busy state, discarded 422 detail), unreachable (help layer, keyboard AND touch), or destructive (three independent paths — the backfill race, the never-cleared store, the editor exit — silently destroy user state and persist the loss); 48 rows adjudicated (6 BLOCKER · 16 MAJOR · 18 MINOR · 8 INFO), 6 identities folded by reference, 12 kills, 16 missed finds banked, routed to F.W3/W4 (the orchestrator extraction + error-channel unification + glass suffusion), F.W5–W8 (the cache-identity and contour-provenance contract seams), F.W1/F.W0 (folds), one glass BH relay (the cn group-table seam), and two NO-WAVE-OWNER census inputs — all routings agglomeration input for the forming X·F specs (M-25), nothing stamped.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3.
