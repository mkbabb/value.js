claude-fable-5

# pt-packrat-entry — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator)

**Seat**: incredulity/apotheosis · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · value.js branch `tranche-u` · parse-that main checkout **`ef10d5b`** (re-derived this session: `git rev-parse` → `ef10d5b`), `typescript/package.json:3` = 1.0.0, dist as built 2026-07-29 · read-only, main checkout only.
**Corpus adjudicated**: `audit/parse-that-modules/packrat-entry/challenge-L-library.md` (13 defects + 6 superlatives, 1 BLOCKER) and `challenge-C-consumption.md` (10 findings + 3 superlatives, 2 BLOCKERs), plus two independent reader returns — **reader-α** and **reader-β** — which disagreed at verdict level on **six rows** (C-3, E-B1, E-M5/C-6, C-4, C-7, C-9). All six were re-derived by this seat with its own instruments; both readers were attacked before either was adopted (one reader-α error found and corrected — see K3).
**Law**: no `memoize()`/`mergeMemos()` constructed; the one-way latch was not tripped. `PACKRAT_ARMED` grep over the three served artifacts → 0/0/0 (this seat's run). STOP-probe: `/Users/mkbabb/Programming/parse-that-css-totality-p2` — not entered, not created. Sole write: this file. **Dispositions are ADJUDICATED, not VERIFIED.**
**Arbiter's own instruments this session (all static reads + greps, zero arming, zero benches)**: `dist/parse.js` whole (4 import lines — the C-3 decider), `dist/packrat.js` whole (1 import line, `{m,i,k}`), `dist/core.js:1`, `dist/utils.js:1-40` (module-scope json/csv graph), `dist/parse.cjs:20-30` (the `whitespace` defineProperty getter), `package.json:1-56` numbered (type:module `:5`, sideEffects `:6`, exports `:7-33` = exactly 5 keys, files `:51-53`), `test/subpath-gate.mjs:25-60` (existsSync-only loop `:29-35`; sole packrat assertion `:53-55`), `ls dist` (57 entries, **zero** `.d.cts`/`.d.mts`), `packrat.ts` at `:88-100`, `:300-310`, `:325-360`, `:430-440`, `:262-292`, `:478-489`, `parser.ts:5-10` + `:40-50` (epoch call sites `:43`/`:47`), `core.ts:1-8`, `index.ts:1-14`, `packrat-entry.ts` whole, both chunks at `:673-684` (six module-global bindings each, ESM `:675-:680` / CJS `:676-:681`), `CHANGELOG.md:98-112` (r6 #6 + the WDM/LR provisional keep), `dist/packrat-entry.d.ts` whole (1 line, 3 value names, 0 types), facade sourcemaps (`parse.js.map` sources `[]`, `diagnostics.js.map` `[]`, `core.js.map` `[split.ts]`, `utils.js.map` `[json,csv,utils]`), byte sizes of all six dist files (158/40,576/3,516/1,109/1,336/2,039), repo-root `README.md:303-315` + dirty-hunk header (`@@ -344,7`) + `docs/api.md:99-107` + `typescript/README.md` non-existence, value.js `git grep 'from "@mkbabb/parse-that' 164343c1^ -- src/ demo/ test/` (**12** statements), subpath-specifier grep at `164343c1^` (NONE), HEAD deps (4.0.0, glass-ui+keyframes only), `W1.md:90-96` + `:424-462` (G-4, the 2026-08-03 55.6 ns RED baseline, and W1's own ratio law).

## Scoreboard

**20 CONFIRMED (0 BLOCKER · 4 MAJOR · 12 MINOR · 4 INFO) · 11 KILLED · 10 MISSED-BANKED (1 MAJOR among them) · 9 SUPERLATIVES · 6 MEASURE-AT-OPEN rows.**

**Both challenge BLOCKERs die.** C-1 survives at MAJOR as one identity with E-M1; C-2's BLOCKER-bearing cure arm and E-B1's entry-culpability argument are refuted at the bytes. What stands is a correctly-curated, zero-consumer, MAJOR-defective facade over a tier whose written retirement condition is met and priced. Nothing in this corpus blocks the X·P lane; the operative products are the refinement folds and W1's measure-at-open residue.

## Ruled disagreements (the seat's actual work)

| row | reader-α | reader-β | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-3 savings arm** | CONFIRMED whole (MINOR) | savings arm KILLED | **β wins — α corrected on the record** | `dist/parse.js` has **four** import lines: the chunk, the diagnostics chunk, `./core.js` (`containsDelimiter, splitBalanced`), and `./utils.js` (`csvParser, escapedString, jsonParser, numberParser, quotedString`); `dist/utils.js:3-37` constructs the entire json/csv parser graph at module scope; `dist/packrat.js` imports **only** `{m,i,k}` from the chunk. So `.` evaluates 48,576 B and the whole domain-parser construction; `/packrat` evaluates 44,250 B and none of it. "No measurable saving" is false absolutely; the identity arm (strict subset, `entry.memoize === root.memoize`, same chunk specifier) survives. C-3 stands at MINOR on its identity+frozen-key arms only. |
| **E-B1 grade** | MAJOR (down from BLOCKER) | MINOR (misattribution) | **MINOR at this module; package-scope MAJOR noted, NO-WAVE-OWNER** | The culpability premise ("impossible to consume /packrat without a second entry, so this file mandates the pairing") fails at `dist/parse.js`: the `.` row publishes `Parser` AND `memoize/mergeMemos/resetPackrat` from **one** chunk import (`P` and `m,i,k` in the same line-1 destructure) — a single-instance path exists, and it is the only path any consumer ever used (C-4). Deleting `packrat-entry.ts` leaves the hazard whole, because `.` itself offers `import`+`require` (`package.json:8-12`) over the same two chunks. The dual-instance FACT is byte-verified (both chunks carry six module-global bindings each; latches at ESM `:678` / CJS `:679`); the end-to-end PT-B1 revival remains a stated prediction → MEASURE-AT-OPEN. reader-α's own downgrade rationale ("amplification, not creation; realized blast radius nil") supports β's grade, not α's. |
| **C-6 ≡ E-M5 grade** | MINOR (zero drift in 7 weeks) | MAJOR | **MAJOR — β wins, with this seat's own linkage** | The gate hole is not unrealized: `subpath-gate.mjs:29-35` existsSync-only (never loads the `require` target, never reads the `types` file) is **exactly what concealed the FalseESM missed-MAJOR (M-1)** — a gate that loaded or typechecked the require condition would have caught it. A gate hole that has already hidden a confirmed MAJOR is not a zero-consequence hole. The header self-contradiction (`:5-8` claims manifest-tracking; `:25` hardcodes the set; `.` never checked) and the one-name `typeof` at `:53-55` are verified this session. α's no-drift point (C/S-3) survives as mitigation. |
| **C-4 grade** | INFO (fact, not defect) | MAJOR | **INFO — α wins, anti-double-count** | "Zero import sites ever" is the module's *exculpation* (it zeroes every hazard's blast radius) and the *evidence* on which C-3 (frozen key) and C-9 (retirement condition met) stand. Grading it MAJOR beside those two rows banks the same substance three times. Carried at INFO with **evidentiary weight: DECISIVE**, census corrected to **12** import statements (K11, re-run this seat). |
| **C-7 grade** | MINOR (down from MAJOR) | MAJOR | **MINOR — α wins** | The doc defect is real and verbatim-verified at repo root (`README.md:305-313` method-form example with no import line; `docs/api.md:101` zero-arg method heading between two real methods; specifier documented nowhere) — but its wrong shape fails loudly at first keystroke (`.memoize` is `undefined`), and the same zero-consumer fact that de-fanged C-5's hazard bounds this one's readership at zero. Becomes MAJOR the day a consumer exists. |
| **C-9 grade** | INFO | MINOR | **MINOR — β wins, narrowly** | Not merely information: the condition (`CHANGELOG.md:106-111`, read this session) is *measured-met* (bbnf-lang manifests parse-that-free) while the key continues to accrue semver weight under C-3. An authorized, priced, condition-satisfied decision left unexecuted is a live process defect, not a note. α's discretion point ("MAY retire, no deadline") is preserved in the row. |

## Adjudicated defect roster

Severities and dispositions are this seat's rulings. Route legend: **W0..W4** = an X·P wave owns the surface · **RF** = X·P REFINEMENT FOLD (the finding sharpens a spec) · **NWO** = NO-WAVE-OWNER (parse-that-side cure; no X·P wave owns that repo's manifest, gates, or docs).

| id | sev | claim (as adjudicated) | proof (verified anchors) | disposition |
|---|---|---|---|---|
| **C-1 ≡ E-M1** | **MAJOR** | ONE identity, two faces: the tier boundary this file names does not exist in the build — `./core`'s own header promise (`core.ts:3-5`) is broken by `dist/core.js:1` importing the packrat-named chunk — and the "thin" opt-in tier (`packrat-entry.ts:3`) is **91.1%** of the library by evaluated bytes (44,250/48,576, re-derived from `ls -la` this session), 100% of its import-time effects, under `sideEffects:false` (`package.json:6`) that `manifest-gate.mjs:37-41` asserts GREEN. Mechanism: `parser.ts:7` imports `packratEnter/packratExit`, consumed unconditionally at `:43`/`:47` inside `parseState`'s try/finally — unshakeable. Realized cost: bytes + 3 empty Maps + ~37 import-time Parsers; no wrong answers. Downgrade from BLOCKER stands: a packaging/documentation defect, not a correctness one. | dist reads + source reads, all this seat's own | NWO (chunk split / docstring cure); **RF**: r6 #6 framing (M-8) |
| **E-M2** | **MAJOR** | 2 of the tier's 3 names are one function — `memoize`/`mergeMemos` are one-line calls into `makeMemoized` differing only in a string (`packrat.ts:482-488`, read this session) whose only branching reader repo-wide is `parser.ts:488` (`=== "whitespace"`); the module concedes it (`:275-278`) while `A.md:348` sells them as distinct capabilities. Two-thirds of a three-name public tier is a printer label. | source read; both readers concur | NWO |
| **E-M3** | **MAJOR** | `resetPackrat` is one third of the tier, has no correct non-test call site, clears five bindings and never touches the latch (`:262-272`: early-return at `:266`; no `PACKRAT_ARMED = false` anywhere in source or either bundle), and the tier's unchecked `!` dereferences (`:396`,`:401`,`:455` per the sibling roster) make one reachable behaviour a raw TypeError out of `.parse()`. The arm-tax "reset that does not reset" receipt (139.3 ns) is inherited from O-15 and is stale → M-6. | source read; O-15 receipt flagged stale | NWO; ns figures → MEASURE-AT-OPEN |
| **C-6 ≡ E-M5** | **MAJOR** | The only gate naming this surface contradicts its own header (claims manifest-tracking `:5-8`, hardcodes the subpath set `:25`, never checks `.`), only `existsSync`s the `types`/`require` targets (`:29-35`), and asserts exactly one of three names (`:53-55`). Deleting 2 of 3 exports — or widening to `export *`, publishing the three hazardous epoch primitives — keeps `proof:subpath` GREEN. No test ever loads this module (`memoize.test.ts:1` imports the barrel). **Upgraded to MAJOR by this seat**: the existsSync-only hole is what concealed M-1. | gate read this session; ruled disagreement above | NWO |
| **C-3 (identity arm)** | MINOR | The subpath is a strict, identity-equal subset of the root barrel (`index.ts:8` byte-identical to `packrat-entry.ts:5`, verified; same chunk specifier both entries) at the price of a 1.x-frozen export key. **Savings arm KILLED (K3)** — the subpath in fact saves ~4.3 kB and the whole json/csv module-scope construction for plain-Node consumers; the redundancy claim survives only for bundler consumers. | dist reads this session | NWO (retirement pricing rides C-9) |
| **E-B1 (fact arm)** | MINOR | Dual publish ships two module instances — **six** duplicated module-global bindings per chunk (`MEMO`/`HEADS`/`LR_STACK`/`PACKRAT_ARMED`/`CURRENT_SRC`/`GROWING`, ESM `:675-:680`, CJS `:676-:681`, read this session), two latches, colliding `PARSER_ID` counters. A package-level property of the `.` row and the dual-publish decision, misfiled against a 5-line alias (ruled above). Cross-format PT-B1 revival = prediction. | chunk reads this session | NWO (package-scope MAJOR flagged to owner); **RF**: PT-03 restated per-format, six bindings (M-7); W1 rule → "one process AND one module format per cell" |
| **E-M4** | MINOR | The tier installs irreversible process-global state and publishes no way to query it (ARMED grep 0/0/0 over all three served artifacts, this seat's run). "G-4 unsatisfiable" arm KILLED (K7): `W1.md:434-459` (read this session) satisfies G-4 today via static dist-read + per-cell latch print. An API-observability gap, and the accessor must land in `packrat.ts` first — the entry can only re-export it. | W1 read + grep this session | NWO; **RF**: G-4 reading stands as written |
| **E-M6** | MINOR | 2 of 3 exports name `Parser<T>`, which the subpath does not export (`dist/packrat-entry.d.ts` = 1 line, 3 value names, 0 types — read this session). Harm framing KILLED (K8): the second import is forced by needing a `Parser` to *pass*, not to *name*; ergonomics, not correctness. | d.ts read this session | NWO |
| **E-m1 ≡ C-5 (surviving substance)** | MINOR | Basename collision: `vite.config.ts:18` maps entry key `packrat` → `packrat-entry.ts`, so `dist/packrat.js` (3 exports) sits beside `dist/packrat.d.ts` (6 declarations, dts of the *internals*), and `packrat-entry.d.ts:1`'s `from './packrat.js'` resolves to the internals' declarations under tsc while Node resolves the entry — type graph and runtime graph agree only by subset accident, unpinned (C-6). No exhibited resolver reaches the wrong pairing (K4); the concrete typing defect on this row is M-1. `files:["./dist"]` (`package.json:51-53`) ships the 6-declaration file regardless. | manifest + dist reads this session | NWO (one-line cure: rename the entry key) |
| **E-m2** | MINOR | No `default` condition (`:23-27` = exactly types/import/require), no `engines`, no `main`/`module`/`typesVersions`; legacy resolution fails hard, undeclared. Companion defect M-4 (no `./package.json` export). | package.json read this session | NWO |
| **C-8** | MINOR | Both served conditions ship empty-but-advertised sourcemaps; `packrat-entry.ts` appears in **zero** shipped `sources` arrays. **Scope made precise by this seat**: the empty-map artifact belongs to the *pure-facade* entries only — `parse.js.map` and `diagnostics.js.map` sources `[]`, while `core.js.map` (`split.ts`) and `utils.js.map` (`json/csv/utils.ts`) are non-empty (all four read this session). This is also what kills E-m3's shipping arm (K9). | map reads this session | NWO |
| **C-9** | MINOR | The tree's written retirement condition for the tier (`CHANGELOG.md:106-111`, verbatim this session: kept "pending the bbnf-lang LR-consumer question") is measured-met — bbnf-lang's manifests are parse-that-free — and unacted while the key accrues semver weight. Ruled MINOR over α's INFO (above). | CHANGELOG read; reader receipts | NWO — owner's begin-word decision; the corrected verdict is **RETIRE-AS-REDUNDANT** (see K5), never RETIRE-AS-CURE |
| **C-7** | MINOR | The only documentation of the served names documents a nonexistent instance-method API (`README.md:305-313`, `docs/api.md:101` — repo ROOT, both read this session; `Parser.prototype` has 25 own names, no `.memoize`), and the subpath specifier is documented nowhere outside the design record. Ruled MINOR over β's MAJOR (above). | doc reads this session | NWO |
| **E-m4** | MINOR | Rollup named the shared core chunk after this 5-line module; the corpus now cites `parser.ts` code as "packrat-entry" (O-15/PT-01's console.error = `parser.ts:67-69`, live chunk `:882` ESM / `:883` CJS). | both readers exact; chunk naming confirmed | **RF**: re-issue PT-01 cites as `chunk:882 (source: parser.ts:68)`; NWO for the chunkFileNames cure |
| **E-m5** | MINOR | `CURRENT_SRC: string \| undefined` receives coerced non-strings on the open `.parse()` boundary; consequence nil (only reads are the epoch save/restore). | sibling receipts; boundary verified via PT-07 row | NWO |
| **E-m6** | MINOR | "Inert until armed" is per-parse, not per-process: MEMO/HEADS/GROWING allocated at import (3 of the 4 import-time Maps). Prose-scope defect costing three empty Maps once; the per-parse claim itself is measured true (S-3). | chunk reads; reader P4/P5 | NWO |
| **C-4** | INFO | **Evidentiary weight: DECISIVE.** Zero import sites of the subpath, anywhere, ever; at `164343c1^` the consumer base was **12** root-barrel import statements (census re-run this seat — the challenge's 10-statement list omitted `test/parsing/diagnostics-sink.test.ts:2` and `test/tranche-f.test.ts:2`; K11), zero subpath specifiers; value.js, needing memoization, wrote its own bounded per-instance `memoize` (`164343c1^:src/utils.ts:116`); at HEAD the dependency is gone (4.0.0 deps = glass-ui + keyframes, verified) and `dist/subpaths/css.js` carries zero bytes of the tier. | git greps this session | drives C-3/C-9's disposition; itself NWO |
| **E-m3** | INFO | Stale coordination prose in source (`:4` "owned by A.W2 on its own branch" — non-denoting; with the false "thin", 2 of 5 lines wrong). Aggravating "shipped to consumers" arm dead (K9): the text reaches no shipped `sources` array. | entry + maps read this session | NWO |
| **L§9 PT-07** | INFO (corpus) | O-15's "5/5 non-string inputs throw" is refuted: the boundary is three-valued and coercion-dependent (throw / silent-reject / silent-accept-coerced), selected by the first leaf combinator and whether the coerced string matches. Reproduced 7/7 live by the L challenge; re-derived statically by reader-β from `leaf.ts:285/:297/:327/:337/:350`. **Strengthens** the letter's cure-above-parse-that conclusion. | both readers independent, concordant | **RF**: restate O-15 PT-07; feeds W2's boundary leg — a boundary this input-dependent cannot be pattern-matched from outside |
| **C-10** | INFO | Sibling packrat challenge's closing chronology corrected: the 1.0.0 freeze (2026-07-03) precedes the dependency removal (2026-07-17) and the band ruling (2026-07-27); "froze in defiance of a known ruling" must not propagate. | dated receipts, both readers | **RF**: harvest carries the corrected chronology |

## Killed-claims register

1. **K1 — C/C-2's cure arm, the BLOCKER's load-bearing premise** ("the entry withheld the two functions that would have made a lawful, parse-scoped memoizer expressible"). Refuted three ways, anchors read this session: (i) `parser.ts:43`/`:47` already invoke `packratEnter`/`packratExit` unconditionally in `parseState`'s try/finally — the scoping is automatic and complete; a consumer has no correct manual call site, and a mis-paired call is precisely the PT-Q1 corruption class. (ii) Publishing them cannot cure the objected-to cost because arming fires at CONSTRUCTION (`packrat.ts:290`, inside `makeMemoized`, comment "The latch never disarms" — read verbatim), before any parse. (iii) Challenge C's own §2 concedes it ("a parse-scoped wrapper over memoize still arms at construction... the wrapper's existence is the violation") — C-2 and C's §2 are mutually contradictory. The withholding is correct curation (S-1). Surviving arms (two arming names = one function; reset-no-disarm) distribute into E-M2/E-M3 at MAJOR.
2. **K2 — E-B1's BLOCKER grade + entry-culpability argument.** The single-instance consumption path exists through `.` (`dist/parse.js:1` destructures `P` and `m,i,k` from one chunk — this seat's read); the file mandates nothing; deleting it leaves the hazard whole; zero consumers ever. Fact survives at MINOR (roster); consequence is MEASURE-AT-OPEN.
3. **K3 — C-3's savings arm ("no measurable saving; a Node consumer evaluates the same 40,576 B either way") — and reader-α's wholesale confirmation of it.** Killed at this seat's own bytes: `dist/parse.js` additionally imports `core.js` and `utils.js`, and `dist/utils.js` constructs the full json/csv graph at module scope; `/packrat` evaluates none of that. The challenge's own falsifier ("a measured byte/evaluation difference between the two paths") is satisfied by its own artifacts. Reader-α confirmed C-3 whole at MINOR without catching this; corrected here.
4. **K4 — C/C-5's mechanism and MAJOR grade** (adjacency-resolver "green typecheck, load-time SyntaxError"). No resolver instantiated; every exports-honouring TS mode honours `exports.types`; node10 cannot reach the subpath (the doc concedes its own population away); the `.` row carries the same stem skew with no hazard. Cite drift compounds (`dist/packrat.d.ts:44,56` vs live `:46,:57`). Substance folds to E-m1 MINOR; the concrete defect the row reached for is M-1.
5. **K5 — C/§2's X·P verdict "RETIRE the key and the file, whole".** Killed on scope and inertness: every cited clause (G-10, K-6, O-8, `memoize = 0`) binds the *candidate value.js builds*, not a third party's export map — and the band ruled 24 days after the key froze (C-10); even granted authority, retiring the alias leaves the same three names published from `.` and the latch armable through them. The supportable verdict is **RETIRE-AS-REDUNDANT** (C-3/C-9), an owner decision.
6. **K6 — C/§2's "Not a K-8 row" exclusion.** Non-export ≠ non-reachability: `getCijKey` is called inside `recall` (`:306`) and `memoizeFn` (`:437`) — both read this session — inside the parser `memoize()` returns, so its RangeError (`:94-97`) escapes the public `.parse()` untyped. The exclusion would under-count the entry's typed-error posture; the finding it suppressed is banked as M-2.
7. **K7 — E-M4's "G-4 is unsatisfiable until this file gains a fourth export".** `W1.md:434-459` (read this session) satisfies G-4 today: the RED baseline reads the latch statically from the dist (`chunk :678,:722`) and the GREEN condition prints per-cell latch state from the installed dist — two routes, no fourth export. What survives is the MINOR observability gap.
8. **K8 — E-M6's harm framing** ("this defect is what forces every consumer into the pairing"). Circular: any consumer holding an argument for `memoize` has already imported `.` or `./core` to construct it; re-exporting the type removes an annotation inconvenience, never the pairing. E-B1's culpability leaned on this; both fall together.
9. **K9 — E-m3's aggravating arm** ("shipped to consumers via sourcemaps"). `dist/packrat.js.map` sources/sourcesContent are empty arrays; `packrat-entry.ts` is absent from every shipped `sources` array (chunk map = the seven core modules). The prose reaches no consumer; survives at INFO.
10. **K10 — the L challenge's citation layer.** Every package.json ordinal drifts (`./packrat` cited :20-28, live **:23-27**; `.` cited :9-13, live **:8-12**; sideEffects cited :5, live **:6**; `files` cited :60-62, live **:51-53** — all re-numbered this session); the subpath-gate assertion cited :44-46, live **:53-55**; manifest-gate cited :36-40, live :37-41; the "two console.error/console.log sites" claim lists three ordinals of which two (`:262`, `:1403`) are default parameter values, not call sites. Substance survives throughout; the L citation layer is unusable as issued and every quoted anchor must be re-resolved before the harvest carries it. The L challenge's **dist-chunk** ordinals, by contrast, are byte-exact in both bundles (re-verified this session), and the C challenge's cites are exact except `dist/packrat.d.ts:44,56` and its read-list placing `README.md`/`docs/api.md` under `typescript/` (both live at repo ROOT; `typescript/README.md` does not exist — checked).
11. **K11 — C/C-4's "names actually imported" enumeration.** The census is **12** statements, not 10 (re-run this seat at `164343c1^`): `test/parsing/diagnostics-sink.test.ts:2` and `test/tranche-f.test.ts:2` add `enableDiagnostics`/`disableDiagnostics`. The load-bearing claim — `memoize` never appears in a parse-that import line — survives all twelve.

## Superlatives (L-18 runs both ways — all upheld)

1. **S-1, upheld against C/C-2** — the narrowing is real and hazard-hiding: `packrat.ts` exports six names; the three epoch primitives are absent from both served namespaces and from the 34-name barrel; `export *` was one keystroke shorter and was not written. The fact challenge C graded a BLOCKER is, on the correct reading, this module's best decision — settled by two lines of `parser.ts` (`:43`,`:47`, this seat's read).
2. **S-2** — intra-format single-instance comes free from the chunking: `dist/packrat.js:1`, `core.js:1`, `parse.js:1`, `utils.js:2` all destructure the same chunk specifier (verified). Accidental, ungated, and the sole confinement of E-B1 to the cross-format case.
3. **S-3** — S.H1's no-op is a measurement that survives adversarial re-measurement: 0.0 packrat Maps/parse unarmed (reader probes, concordant); the guard is the first statement of `packratEnter` (chunk `:682`, read this session).
4. **S-4** — the tier is inert to import: the only `PACKRAT_ARMED = true` sits inside `makeMemoized`, reachable only by calling. This is what made a read-only audit of a one-way latch possible at all.
5. **S-5** — `growLR` converts unbounded recursion into a bounded strict-advance loop under two nesting levels of try/finally — the honest counterweight to every byte and latch complaint in the corpus.
6. **S-6 ≡ C/S-1** — five lines, one statement, `types`-first manifest row (`:24`), all three targets real, no `./dist/*` wildcard (the single thing keeping `dist/packrat.d.ts`'s six declarations unreachable). Wrong about WHAT it publishes, never about HOW.
7. **C/S-2** — the ownership boundary declared in source (`:4`) is discharged by the founding diff: `3b559a9` touched six paths, `packrat.ts` absent, zero deletions; `git log --follow` = one commit ever.
8. **C/S-3** — spec fidelity across a major, the only entry in its family: the founding message's three names are the served surface, same order, seven weeks and one breaking cut later — while `./core`'s founding spec is now false in two clauses. A promise kept perfectly to no one is still a promise kept perfectly.
9. **CORPUS CRAFT** — the L challenge's two-bundle dist table (7 rows, byte-exact both formats) and its PT-07 refutation, and the C challenge's C-4 consumption history, are the strongest contributions in either file; the C challenge's provenance ran ~60 cites with two drifts. Both seats declared and honoured the no-arming law; both ran the STOP probe; the forbidden root is absent (re-checked this session).

## MISSED — banked by this adjudication (10 rows)

| id | sev | claim | route |
|---|---|---|---|
| **M-1** | **MAJOR** | **The `require` condition is untypable — arethetypeswrong "masquerading as ESM", all five rows.** `package.json:5` `"type": "module"`; each row carries one top-level `types` (`.d.ts`, ESM-flavored by the package type) and no per-condition types; **zero** `.d.cts`/`.d.mts` among 57 dist entries (this seat's census). A CJS-mode node16/nodenext consumer resolves `./dist/packrat.cjs` and gets TS1479 while the runtime works. `subpath-gate.mjs:29-35` cannot catch it (existsSync only) — the concrete defect C-5 reached for speculatively, and the realized consequence of C-6's gate hole. | NWO (parse-that manifest; flagged to owner with C-6) |
| M-2 | MINOR | The tier's one raisable error escapes `.parse()` untyped: `memoize(p).parse(src)` can throw the raw RangeError from `packrat.ts:94-97` via internal `getCijKey` calls at `:306`/`:437` (read this session); `packrat-entry.d.ts` declares no error channel. A K-8-class row against this entry's surface (unblocks K6). | **RF**: W2 K-8 census gains the row |
| M-3 | MINOR | `recall()` returns two throwaway `MemoCell` literals never interned into MEMO — `:329` (epsilon contribution) and `:344` (head-active reject), contrast `MEMO.set(key,{ans})` at `:358` (all three read this session) — allocation pressure inside the grow loop the tier exists to accelerate. Quantifying requires arming. | W1 · MEASURE-AT-OPEN |
| M-4 | MINOR | No `"./package.json"` export among the exactly-five keys (`:7-33`, read this session): exports-aware resolvers answer ERR_PACKAGE_PATH_NOT_EXPORTED for the manifest itself, breaking ordinary manifest-reading tooling. | NWO |
| M-5 | MINOR | `sideEffects:false` is load-bearingly false: `whitespace` is an unassigned `export let` initialized by a module-scope `_initWhitespace()` call (`parser.ts:6` imports it; `:711` calls it), droppable under the declaration — and the shipped artifact itself testifies the binding is post-init mutable: `dist/parse.cjs:25-28` publishes `whitespace` through an `Object.defineProperty` getter while all sibling exports are plain assignments (read this session). `manifest-gate.mjs` asserts the false declaration GREEN. | NWO |
| M-6 | INFO | The arm-tax absolutes both challenges propagate (93.9/138.2/139.3, O-15 2026-07-27) are stale against the corpus's own instrument: `W1.md:437-446` (read this session) prints UNARMED median **55.6 ns/parse** (2026-08-03, same harness, different box-state) and already states the law — absolute ns is not portable; the bench reports ratios between interleaved cells in one process-set. The 1.47× ratio survives as a same-run comparison. Reader-α flagged the staleness; this seat adds that W1 self-corrected first — the residue rule below cites W1's own law, not a new one. | W1 · MEASURE-AT-OPEN |
| M-7 | INFO | The E-B1 duplication table is incomplete: **six** module-global bindings duplicate per bundle, not four — `LR_STACK` (ESM `:677`/CJS `:678`) and `CURRENT_SRC` (`:679`/`:680`) omitted (both read this session); both are PackratEpoch members. | **RF**: the re-issued PT-03 row carries all six bindings, stated per-format; W1 harness rule reads "one fresh process AND one module format per cell" |
| M-8 | INFO | `CHANGELOG.md:100-102` (read verbatim this session) ratifies the zone map and forbids splitting `parser.ts` **in the same sentence** — the ruling that makes the packrat zone nominal was enacted jointly with the zone map itself. Harvest framing: not "the build falsified a ruling" but "the ruling ratified its own falsification". | **RF** |
| M-9 | INFO | The two challenges grade the identical non-publication of the epoch primitives a SUPERLATIVE (L/S-1) and a BLOCKER (C/C-2) against the same commit with identical measurements, and neither names the conflict. Adjudicated for L/S-1 (K1); recorded so the harvest cannot bank both. | **RF** |
| M-10 | INFO | Provenance: both challenges assert "at ef10d5b" against a dirty working tree (README.md + ten rust/ files modified; untracked `docs/tranches/{B,T,U}/` — `git status` this session); the sole README hunk is `@@ -344,7` so every cited region is clean and no claim moves; C's read-list mislocates `README.md`/`docs/api.md` under `typescript/` (live at repo root). OP-7 receipts must record tree state. | **RF**: provenance hygiene note for the harvest |

## MEASURE-AT-OPEN residue (bench-dependent rows for the lane's W1 harness)

Rows this corpus could not measure under the no-arming law, or whose absolutes are non-portable. Each runs in **one fresh, deliberately-arming process per cell, one module format per cell** (the M-7 rule), against the dist as built at wave-open, anchors re-resolved first.

1. **E-B1 end-to-end** — the L challenge's mixed-format falsifier (ESM `memoize` node inside a CJS-driven graph); prediction `second === "hello"` (PT-B1 revived). If `"world"`, mechanism 1 is refuted and the package-scope row drops to the id-collision arm alone.
2. **M-3 magnitude** — allocation delta of the `:329`/`:344` throwaway literals over a grow-heavy armed corpus.
3. **Arm-tax re-baseline** — all absolute ns retired (93.9 is already contradicted by W1's own 55.6); re-measure and report **ratios between interleaved cells only**, per `W1.md:441-446`'s standing law. The 1.47× and 139.3-after-reset claims re-derive same-run.
4. **C-3/E-M1 percentages** — 91.1% and the 44,250/48,576 arithmetic re-derive against the wave-open dist (chunk hashes and ordinals drift on rebuild; today's `CS1td-8B`/`46NYx4_U` are 2026-07-29 artifacts).
5. **Import-time Parser census** — reconcile 37 (this corpus, both formats) vs 36 (`index/M-1`) once, against one build.
6. **PT-04 memoized frame-cost** — the L challenge's C-3 structural prediction (memoized ceiling strictly below 7,761) — binary-search in an arming cell.

## Closing verdict

**Both BLOCKERs are dead and no defect in this module blocks the X·P lane.** The five-line file is the best-curated entry in its family — real narrowing that hides the hazardous half of its source module (upheld over challenge C's central accusation), an ownership boundary discharged by its founding diff, spec fidelity across a major — publishing a surface nobody has ever imported, over a tier whose boundary the build never cut, guarded by a gate that cannot see any of it. The four surviving MAJORs (nominal tier boundary ≡ false "thin"; one-function-published-twice; the reset that neither resets nor disarms; the self-contradicting gate that concealed M-1) are all parse-that-side, all NO-WAVE-OWNER, and all subordinate to the one decision the tree already wrote for itself: the retirement condition of `CHANGELOG.md:106-111` is measured-met, priced at one major by C-3's own precedent, and correctly executed as **RETIRE-AS-REDUNDANT** — never as RETIRE-AS-CURE, because the barrel publishes the same three names and the latch arms through them regardless (K5). For X·P the operative products are the refinement folds: PT-03 restated as one latch **per bundle format** across **six** bindings; PT-07 restated three-valued (strengthening the cure-above-parse-that boundary leg); the K-8 census gaining the untyped RangeError row; the `memoize = 0` band gate gaining its third, packaging-shaped justification — the cheapest structural defence against every finding in this corpus remains the one the band already ruled. Six rows are banked MEASURE-AT-OPEN for W1's arming harness; every absolute nanosecond in this corpus is retired in favour of W1's own ratio law.

— end of apotheosis. Both challenge records and both reader returns remain immutable beside this file per E-1/E-3.
