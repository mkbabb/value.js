claude-fable-5

# kf-KeyboardShortcutsModal — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Anchors**: value.js @ `c654824e` (branch `tranche-u`) · keyframes.js @ `8281638c` (branch `master`, working tree = the audited tree, re-confirmed by this seat's own `git log -1`) · installed producer `node_modules/@mkbabb/glass-ui` 7.0.0 (dist is the contract of record) · reka-ui dist as installed.
**Corpus**: three axis challenges (D design · L library · C consumption, all authored by `claude-opus-5[1m]`) at `docs/tranches/V/megatranche/audit/kf-components/KeyboardShortcutsModal/`, re-read by two hostile re-readers (design-and-usage = **R-A**; library-and-code = **R-B**), both returns adjudicated here.
**Method (L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement — the registry-liveness rung (R-A BLOCKER vs R-B MAJOR), the L-1 rung (R-A BLOCKER vs R-B demote-and-fold), the D-1 grounding (R-A's SS-13-decider vs R-B's statically-certain trap proof), the C-8 rung, the C-7 rung, the D-7 rung — was re-derived against the tree by this seat with its own instruments. The three-axis convergent kill (the `--panel-max-h` "mobile panel" framing) was independently re-verified rather than trusted on reader agreement, precisely because convergence between two readers citing the same lines is where citation-inheritance hides. One identity collision neither reader caught is ruled by this seat (the S-9 register clash — see the corpus rows).
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony; product source READ-ONLY; this file is the only write)**: KeyboardShortcutsModal.vue read whole (69 lines); useControlsKeyboardShortcuts.ts:48-72 (all 18 registrations + the F.W14.S1 registry-discipline comment); EditorShell.vue:74-77/:104-110/:118-126/:186-192; shell/index.ts whole (**4 exports, no modal** — the K-6 decider); reka `FocusScope/FocusScope.js` (mount-focus block: `focusFirst(getTabbableCandidates(container))` with container-fallback only when focus did not move; `handleKeyDown`: Tab `preventDefault()` at `focusedElement === last`, Shift+Tab at `=== first`, loop refocus; `handleFocusIn/Out` refocus-on-escape; the Primitive rendering `tabindex: "-1"`) + `FocusScope/utils.js` `getTabbableCandidates` (**a property-read TreeWalker accepting only `node.tabIndex >= 0`**); glass-ui `keyboard.js` whole tail — alias table `:29-34` (`delete: ["backspace","delete"]`), matcher `:38-47` (consults aliases at `:45-46`), skip predicate `:48-52` (closed list), glyph mapper `:53-55` (Delete → one token `⌫`/`Del`; fall-through `e.trim()`), **the dispatcher `:67-73`: `g(n) ? [...e].reverse() : e` — LIFO for Escape ONLY, FIFO for every other key, first match returns**, `createGlobalState` window listeners + `labeled` filter `:74-85`, bare `Set.add` registerShortcut `:86-97`, the five-export surface `:104`; `dialog-BKSTfmIQ.js` — `K` base class at `:163` verbatim (**no max-height**), `scroll: { type: Boolean, default: !1 }` `:186-189`, the `Y` computed `:244`, the close-X class `:285` (`absolute right-(--overlay-pad-inline) top-(--overlay-pad-block)` — inside the content box), DialogHeader `:81` (`flex flex-col gap-y-1.5 text-center sm:text-left`), DialogTitle `:101` (`text-subheading leading-none tracking-tight`), DialogDescription `:124` (`text-sm text-muted-foreground`); `class-names-Cpy5eaBk.js:14-15` (both font-size groups; `grep 'leading|line-height|tracking'` → **no dedupe group** — the leading-none survival decider); shipped `scale.css` (`--type-admin-label: 0.625rem` fixed · `--type-small`/`--type-body` vw-clamped · `--type-subheading: 1.272rem`); `expandable-container.js` (**exactly one** labelled registration, `label: "Exit fullscreen"`); `fading-scroll` dist (`tabindex: "0"` unconditional; `role: "region"` when named); design-idioms.css:40-52 (the token + its "caps mobile panels" comment) + :222-231 (the `.status-badge` re-voice rule, verbatim); layout.css:5-14; DESIGN.md:24-30 (Mono-as-data + the named `proof:font-census` witness) + :132-138 (the U.B geometry-move prose); font-roles.json:68 (`kbd`) + :82 (`_monoContract` clause (d)); AnimationControlsGroup.vue:68-92 + AnimationControlsGroup.css:175-190/:215-226 (**the K-1 decider**); package.json scripts + devDeps (no `vue-tsc`, no `eslint`; `check` = plain `tsc --noEmit`; `lint` = `depcruise src`; proof scripts = `proof:publish` + `proof:owner-golden` only) + `node_modules/.bin` (neither binary); demo/env.d.ts whole (the `DefineComponent<{}, {}, any>` shim); ci.yml:4-8/:41-42/:60-75; tsconfig.lib.json:1-11 (the staged-Glass comment verbatim + `include: ["src/"]`); tsconfig.json:47; vite.config.ts:27-31; vitest.config.ts:35-52 (two projects, **no `@vitejs/plugin-vue`**) + `ls test/demo/instrument/` (8 populated test files); repo greps: `registerShortcut(` in demo → **19**; `--panel-max-h` consumers → exactly 2; `isMac|formatCombo\b|navigator.platform` in demo → 0; `DialogHeader` in demo → 1 file (this one); `kbd` in demo `*.css` → 0; `KeyboardShortcuts` in test/+scripts/ → 0; `glass-ui` in package.json/package-lock.json → 0/0; `ls node_modules/@mkbabb` → glass-ui + parse-that + value.js; `@mkbabb` lock entries → exactly 1 (`value.js`); `find -name 'vite.config*'` → root only (**no `demo/vite.config.ts`** — the K-8 decider); `useSpringMount-DrKU3wX2.js` imports (3, none keyframes.js) vs `useSpring-BCHxLjwv.js:3` (the actual engine importer — K-9); `useKeyboardShortcuts.d.ts:29-33` (**zero JSDoc on all five exports** — K-11); KF wave taxonomy read at `formation/keyframes/CENSUS-2026-08-03.md:177-206`; census rows at `lane-frontend.md:15` (F-1 RED) + `:610-612` ("F-1 first"). Sibling-dialog facts (the byte-identical `@interact-outside` toaster guard at CSSPasteDialog.vue/KeyframesAddDialog.vue; toastGuard.ts:26-28; the dist slot-before-close render order) are cited from the banked kf-CSSPasteDialog apotheosis, where that seat verified them verbatim — banked adjudicated records are citable; axis assertions are not.

## Scoreboard

**22 CONFIRMED (0 BLOCKER · 10 MAJOR · 12 MINOR) · 12 KILLED · 17 MISSED-then-adjudicated (15 reader rows + 2 seat-found corpus rows) · 14 INFO/fold-by-reference · 7 superlatives · 8 UNPROVEN-NEEDS-LIVE residue rows for SS-13.**

Both axis BLOCKERs fall off the BLOCKER rung, for different reasons: **D-2's chain is fully proven and demotes on the reversibility precedent** (the tree's own undo covers the destructive half; escalation condition named); **L-1 falls to the identity guard** — it is census F-1, already RED, already ruled "F-1 first", folded by reference and never re-booked per-component. **D-1 is re-grounded, not demoted**: its published proof (browser scroller policy) is killed, and the verdict survives on reka's focus trap — statically certain in every engine, verified by this seat, and it retires the SS-13 probe R-A had left as the decider.

The component **stays**. The cure spine is one composition change — **`FadingScroll axis="y"` as the named, tabbable scroll port + `scroll` on DialogContent (KF.W6)** — which retires R-2/R-3/R-4/R-22 and the RTL/gutter residue in one move, plus a **GLASS-OWNED relay** for the registry's missing modality affordance (whose cure-shape is constrained by the LIFO/FIFO asymmetry no axis saw — R-1).

## Ruled disagreements (the seat's actual work)

| id | R-A | R-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **R-1 rung** (D-2/L-2/C-1/C-3, one identity) | BLOCKER | MAJOR | **MAJOR-with-escalation — R-B wins the rung** | Chain re-verified end-to-end (dispatcher, skip list, reka's non-preventing Escape path, 18+1 registrations, unconditional sibling mount, five-export no-suspend surface). The rung follows the banked kf-CSSPasteDialog R-3 precedent: a silent but **reversible** destruction is MAJOR — the tree's own comment at useControlsKeyboardShortcuts.ts:66-69 states the destructive timeline ops "become reversible" via the registry-bound Mod+Z, and `Delete` routes `removeSelectedKeyframe` through exactly that history; Escape's `reset()` destroys transient playhead position, not persisted data. Escalation: an SS-13 witness of any destruction path behind the open modal with no live undo route re-promotes to BLOCKER. R-B's missed M3 is folded in as the **cure-shape law**: the dispatcher is LIFO for Escape only (`keyboard.js:67-73`, verified) — a demo-side later-registered guard can pre-empt Escape but can NEVER pre-empt Space/Delete/Mod+Z, which hold FIFO priority from AnimationControlsGroup setup forever. The destructive half is therefore producer-gated; the row is GLASS-OWNED with a KF.W6 consumer-wiring tail. |
| **L-1 rung + booking** (L-1/C-12 = census F-1) | BLOCKER; kills C's zero-blocker headline on it | MAJOR, then demoted by M5 (the tree documents the state in three places) | **FOLD BY REFERENCE — the identity guard decides, not a severity argument** | lane-frontend.md:15 books the fact RED; :610-612 rules "F-1 first — `npm ci` is currently broken." The banked kf-CSSPasteDialog apotheosis folded the identical fact by reference; one identity, never re-booked. R-B's M5 verified by this seat verbatim: tsconfig.lib.json:2-9 + ci.yml:5-7 + vite.config.ts:29 all document the missing manifest entry as a **deliberately staged repo state awaiting the named demo-consumer commit** — which un-makes the "undiscovered per-component blocker" framing entirely. Extensions recorded as riders on the F-1 fold, not as rows: (i) L-1's CSS-class contract (`.kbd`, `text-body/small/admin-label`, zero demo fallback — my grep) — real, but it bites only a *vendoring* remediation nobody prescribes; the census's manifest cure carries the stylesheet automatically; (ii) R-B's M8: `@mkbabb/parse-that` is a **second** undeclared, unlocked, non-transitive install (my `ls` + single lock entry) — F-1 is not a single-package finding; (iii) blast radius: the nightly `demo-correctness` job (ci.yml:60-75, `npm ci` → `gh-pages`) should be RED — CI-run-decidable only, residue #7. R-A's kill of C's headline survives in narrow form: C rating the same fact INFO that its sibling axis rates BLOCKER is a real corpus-calibration defect (process row), but the resolution is the fold, not a promotion. |
| **D-1 grounding** (D-1/L-3/C-2(c)) | MAJOR, decisive question left to SS-13 ("does Tab ever reach the port in Chromium ≥127?") | MAJOR, statically certain via the focus trap | **R-B wins — and the probe is FORECLOSED, verified by this seat** | My own reads: `getTabbableCandidates` accepts only `node.tabIndex >= 0` — a property-read walker blind to UA-implicit scroller focusability (which does not change the `tabIndex` IDL value; the HTML IDL default for a div is −1). And it never matters anyway: `handleKeyDown` fires **before** UA sequential navigation and `preventDefault()`s Tab whenever `focusedElement === last` (Shift+Tab at `=== first`); with the close-X as the sole candidate, first===last, so **every Tab press is cancelled and looped in every engine** — Chrome 127's feature can never surface inside this trap. `handleFocusIn` additionally yanks back any focus that escapes. R-A's unproven[0] ("SS-13: open the modal, Tab once…") is retired as a verdict input; the engine matrix survives only as corpus hygiene (residue #8). Held at MAJOR, not promoted: the SR virtual cursor reads overflowed content without DOM focus, so the denial is specific to sighted keyboard-only users — real, severe, but not total denial of the information. |
| **C-8 rung** (root-barrel Dialog import) | MINOR | MINOR | **FOLD — the seat overrules both readers on the banked precedent** | The kf-CSSPasteDialog apotheosis already ruled this exact fact-class: "C-9 root-barrel import: the demo's standing pattern (lane-frontend.md §3.2 — Dialog* drawn from the root barrel demo-wide) … Non-defect," with the subpath policy owned by the census §3.1 question. Identity guard: one ruling, never re-litigated per component. Both readers' MINOR preserved in DISSENT; C's own falsifier (EditorShell.vue:124 root-barrel `Button` → incremental cost zero, verified) is the substance of the banked ruling anyway. |
| **C-7/L-4 rung** (`:key="shortcut.raw"`) | INFO | MINOR | **INFO — R-A wins** | Bare `Set.add`, no dedupe, no id (verified). But: 19 live raws distinct; the double-mount path closed (keyed swap + synchronous `onScopeDispose`, both readers convergent); and R-A's extra mitigation verified by this seat — the only upstream second labelled `Escape` lives in `expandable-container.js` (exactly one labelled registration, my grep), unconsumed by the demo AND fullscreen-gated. Two gates out from live. One-line composite key noted for any wave already touching the file. |
| **D-7/L-9 rung** (no term↔definition semantics) | MINOR, while endorsing L-9's INFO | (not re-ruled) | **MINOR — with the tension named** | Fact verified (four nested divs, no association, no `role="group"`/`aria-labelledby`). The h2→h3 levels are right, so group boundaries survive AT heading navigation — that mitigation is why INFO tempts. Held at MINOR: in a 19-row reference whose entire payload is the pairing, losing the pairing in the reading order is a defect of function, not polish. The SR transcript (residue #4) is the final calibrator either way. |
| **S-9 identity** (C-9's "new shadow" id) | adopted as-minted | adopted as-minted | **COLLISION — seat's own ruling: renumber to S-10** | Neither reader noticed: the banked kf-CSSPasteDialog apotheosis already extended the census S-table with **S-9 = the bespoke Textarea shadow** (its R-5). This corpus mints "S-9" a second time for the `/command` family shadow. Two different findings, one id, in one registry. The `/command` extension enters the census as **S-10**, evaluate-not-replace posture intact. Booked as a corpus-integrity row for SS-1/SS-2 spec authoring: census extension ids must be allocated against the banked registry, not against the local corpus. |

## Adjudicated defect roster — 22 rows

Dispositions are **ADJUDICATED**, not VERIFIED — X·KF specs are forming; these routings are agglomeration input (M-25). Wave targets from `CENSUS-2026-08-03.md:177-206`. Line anchors are keyframes.js `8281638c` working tree; re-resolve before any cure (D-19).

### MAJOR (10)

- **R-1 — MAJOR (escalatable) · the whole 19-binding registry stays live behind the open modal** (D-2/L-2/C-1/C-3 merged — one identity, many manifestations; + R-B's M3 folded as cure-shape law). Escape rewinds and halts the animation then dismisses (`useControlsKeyboardShortcuts.ts:51`; reka's DismissableLayer neither stops nor prevents); Space toggles playback and, `preventDefault:true`, swallows the last scroll affordance `(:50)`; `Delete` destroys the selected keyframe `(:65)` — **and fires on Backspace too**, via the alias table (`keyboard.js:29-34`, `:45-46`, verified; R-B's M4/R-A's convergent missed row); Mod+Z/Mod+Shift+Z mutate the timeline `(:70-71)`; `[ ] 1 2 3 R Home End` all act behind the scrim; Mod+S raises the toast that feeds R-5. The registry's published surface has no suspend/scope/priority (five exports, `keyboard.js:104`, verified). **Cure-shape law (M3)**: LIFO-for-Escape-only means a demo-side guard can pre-empt Escape but never the FIFO-held destructive keys — only a producer affordance cures the destructive half. **→ GLASS-OWNED** (BH relay per the standing edict: a registry suspend/modality affordance; the relay must carry the LIFO/FIFO asymmetry so the API is designed against it) **+ KF.W6** consumer wiring on discharge. Escalation: SS-13 witness of an un-undoable destruction path behind the modal.
- **R-2 — MAJOR · the guaranteed-overflowing scroll port is keyboard-unreachable in every engine — proven by the focus trap, not by scroller policy** (D-1/L-3(b)/C-2(c)-conclusion re-grounded; R-A's missed trap-mechanism row and R-B's proof are the same identity, folded here). `:10` ships no tabindex/role/name; rows are div/h3/span/kbd only; content ≈864px vs a `60dvh` port → overflow on every viewport under 1440 CSS px (R-A's missed[5] threshold, re-derived arithmetic adopted; exact px = residue #2). The trap forecloses reach statically (ruled above). Arrows scroll nothing: the port is not an ancestor of the focused close-X, DialogContent is not a scroller (R-3), body is scroll-locked. Space/Home/End are swallowed (R-1). The shipped cure is verified sound end-to-end: `FadingScroll` renders `tabindex="0"` unconditionally (→ a real candidate for reka's `tabIndex >= 0` walker) + `role="region"` when named, already consumed at EasingTarget.vue. **→ KF.W6**.
- **R-3 — MAJOR · `scroll` declined; the dialog has no height bound of its own; the cure is `scroll` PLUS the inner port, never `scroll` alone** (D-6/C-2(b); the C-axis remedy killed at K-4). `K` base class at `dialog-BKSTfmIQ.js:163` verified: no max-height; `Y` at `:244` gates the clamp behind `scroll`, default `!1` `(:186-189)`. Below ~313 CSS px (arithmetic; residue #2) the centred box clips off both edges with no scroll owner — fails at the 400%-zoom SC 1.4.10 posture, passes 1.4.4 @200% (D's scope honesty adopted). `scroll` alone would scroll the DialogHeader away **and** the close-X with it (`:285` is `absolute` inside the same box — K-4). **→ KF.W6** (scroll + FadingScroll inner port, one composition).
- **R-4 — MAJOR · no scroll-boundary affordance; `pr-1` is a quarter-gutter; both shipped remedies unused** (D-5, minus the killed overlay-reflow clause K-12). At rest on overlay-scrollbar platforms the always-overflowing list just stops mid-row; `.scroll-gutter-stable` and `.fading-scroll--y` ship in the consumed stylesheet, zero demo hits (my grep); the demo owns `--mask-fade` and applies the same feather idiom at ControlsPaneWrapper.css. Same shape as census S-3/S-4/S-7 at utility granularity. **→ KF.W6** (FadingScroll carries the feather).
- **R-5 — MAJOR · missing the `@interact-outside` toaster guard both sibling dialogs carry — and the modal's own live Mod+S is what summons the toast** (C-4). 2-of-3 precedent + toastGuard.ts existence cited from the banked kf-CSSPasteDialog verification; `Mod+S → copyCSS → toast` chain live per R-1; the Toaster is an `<html>`-teleported singleton, so tapping the toast is interact-outside → the reference self-dismisses. One attribute, copied verbatim. **→ KF.W6**.
- **R-6 — MAJOR · typographic hierarchy inverted and the title demoted below the shipped default — the sole downward outlier of three sibling dialogs** (D-3). Verified from shipped tokens: `--type-admin-label` 0.625rem FIXED vs vw-clamped `--type-small` rows — 0.61× at 1440px and **widening with viewport**; `:5` overrides DialogTitle's `text-subheading` down to `text-body` (deterministic win — both in the cn font-size group, my read of `:15`). Sibling table verified in the corpus, unchallenged by either reader. Rider (R-A missed[6], verified): cn has **no line-height/tracking group**, so `leading-none tracking-tight` survive the demotion and `text-body`'s own leading competes on cascade order — latent, single-line-masked. **→ KF.W6**.
- **R-7 — MAJOR · demo-authored mono UI headings against the demo's own written Mono-as-data law; the named witness is dead; the tree's own cure was declined** (D-4, strengthened by R-A). DESIGN.md:24-30 states the law and names `proof:font-census`; only `proof:publish`/`proof:owner-golden` exist and `scripts/gates/` holds surface/+visual/ (verified). `text-admin-label` binds `--font-mono`; `h3`/`text-admin-label` appear in neither `monoAllowedSelectors` (15 entries, `kbd` yes) nor clause (d). The kill-shot (R-A, verified verbatim): design-idioms.css:222-231's `.status-badge` re-voices to `var(--font-text)` with a comment ruling exactly this case — "a status WORD is a UI label, not data" — and this file declines the fix the tree already wrote. **→ KF.W6** (class/voice fix) **+ NO-WAVE-OWNER rider** for SS-1/SS-2: the dead `proof:font-census` witness is a repo-process gap no forming wave names.
- **R-8 — MAJOR · keycaps are bare platform glyphs with no accessible text** (D-8). `formatComboParts` emits `⌘ ⇧ ⌥ ⌃ ␣ ⌫` (Mac) and arrows/`↵` everywhere (`keyboard.js:53-55`, verified); each is the sole text node of a `<kbd>` with no aria-label/title/sr-only twin. U+2318 = "place of interest sign." The demo rightly owns zero platform knowledge (superlative 4), so the durable cure is a producer accessible-name companion to `formatComboParts`. **→ KF.W6** (interim sr-only twin per row) **+ GLASS-OWNED rider** (the companion API rides the same BH relay as R-1). SR transcript = residue #4.
- **R-9 — MAJOR · nothing automated verifies this file — not types, not lint, not tests, not format** (R-B's M1, adopted with a carve-out). Verified fourfold: no `vue-tsc` anywhere (devDeps + `.bin`); `check` = plain `tsc --noEmit`, which never opens an SFC; env.d.ts shims every `*.vue` to `DefineComponent<{}, {}, any>` — the `defineModel` contract is erased at the consumer; CI runs only `check:lib` over `src/`; no ESLint exists (`lint` = depcruise). Carve-out (the M5 staging): the whole-project `check`'s demo half is documented as staged until the Glass dependency lands — but the vue-tsc absence, the any-shim, and the lint absence all **survive** F-1's restoration, so the row stands on its own. Every type-level argument in this corpus was made against a checker that never opens the file. **→ NO-WAVE-OWNER** (repo-scope substrate; no forming KF wave names demo typechecking — SS-1/SS-2 must seat it, post-F-1).
- **R-10 — MAJOR · the component's only logic is structurally unreachable by the repo's own populated demo-test lane** (R-B's M2 + C-14, merged). vitest.config.ts declares the `test/demo/**` project with **no Vue-SFC plugin** (verified); the lane is real (8 files in test/demo/instrument, my ls) and its convention is exactly the lift this file declined — useControlsKeyboardShortcuts.ts documents its own extraction as the colocated-composable seam. `groupedShortcuts` (`:57-67`) is the file's one testable unit and is locked inside the SFC. Cheap cure: extract `groupShortcuts()` beside the SFC, test it in the lane. Zero coverage + one-commit history (C-14) folds in. **→ KF.W8** (test-tree isomorphism / colocation settle).

### MINOR (12)

- **R-11 — MINOR · label↔keys pairing carries no semantics** (D-7/L-9; ruled above). No `<dl>`, no list roles, no association; heading levels correct (mitigation granted). Cheap: `role="group"` + `aria-labelledby` per group, or `<dl>` rows. **→ KF.W6**. Final calibration = residue #4.
- **R-12 — MINOR · the hover plate promises interactivity the rows do not have** (D-9, R-A's demotion adopted). `hover:bg-muted/50 transition-colors rounded-md` on rows with no click/role/tabindex/cursor (verified in the 69 lines); reading-position tracking is a defensible reading, which is what holds it under MAJOR. Either add the genuinely-good affordance (click-to-run) or strip to an honest treatment. **→ KF.W6**.
- **R-13 — MINOR · group headings hang 8px left of their rows, four times down one column** (D-10). `:12` no inline padding vs `:19` `px-2`; the caps tracking pushes the same direction. **→ KF.W6**.
- **R-14 — MINOR · multi-key combos lose their `+` off-Mac; `formatCombo`/`isMac` ship unused; `gap-0.5` off-rhythm** (D-11). `h(e)` encodes the platform join rule (`keyboard.js:60-62`, verified); demo usage of both helpers = 0 (my grep); `Ctrl Shift Z` renders as three floating caps on the platforms where `+` is the convention. **→ KF.W6**.
- **R-15 — MINOR · the `?` binding renders twice in one dialog; the literal bypasses the imported formatter; "panel" is the wrong noun; the copy is unactionable on touch** (C-5/L-6/D-13 + D-17 rider). `:7` hardcodes what the General group derives ≥34px below; agreement between the two paths is the accident of `e.trim()` fall-through. Touch entry is covered by the header button (verified rationale comment at EditorShell); the copy is not. One copy edit cures all four faces. **→ KF.W6**.
- **R-16 — MINOR · `{{ shortcut.options.label }}` rests on an undocumented runtime filter; the sibling optional field IS guarded** (C-6/L-8; L-8's JSDoc detail killed at K-11 — no export carries JSDoc, verified). `labeled` filter at `keyboard.js:83`; return type unnarrowed; `:61` guards `group`, `:22` does not guard `label` — the asymmetry is the tell. Durable cure is a producer type-narrowing. **→ GLASS-OWNED rider** on the R-1 relay **+ KF.W6** local guard meanwhile.
- **R-17 — MINOR · the Delete row under-reports its own binding: Backspace also destroys, the panel shows one chip** (R-A missed + R-B M4, independently convergent, verified by this seat at `keyboard.js:29-34`/`:45-46`/`:55`). A keyboard reference that under-reports a destructive binding is squarely this component's purpose; the formatting limit is producer-side, the manifestation is here. **→ KF.W6** (surface both keys) **+ GLASS-OWNED rider** (alias surfacing in the formatter API, same relay).
- **R-18 — MINOR · `text-muted-foreground` on DialogDescription restates the primitive's default** (C-13). `:124` verified; the `text-small` half is a real, winning override; the tone half silently pins the call site against a future producer re-tone. **→ KF.W6**.
- **R-19 — MINOR · the demo's only DialogHeader consumer: below 640px the title/description centre while all 19 rows stay left** (R-A missed[4], verified: `:81` ships `text-center sm:text-left`; `grep -rln DialogHeader demo/` → this file alone; both siblings compose Title/Description directly). Alignment-axis break exactly where the dialog is most cramped. Whether it reads as intentional mobile treatment = residue #6. **→ KF.W6**.
- **R-20 — MINOR · a 4-group port that always overflows ships no sticky group headings** (R-A missed[8]). Once a heading scrolls off, proximity is the only grouping signal left — and R-6 establishes the headings are already the weakest signal in the panel. `sticky top-0` + a plate, or FadingScroll's named-region + heading anchors. **→ KF.W6**.
- **R-21 — MINOR · the real `--panel-max-h` defect is at its DEFINITION: the doc comment is stale against BOTH consumers, and the token's home is disputed by the demo's own docs** (the relocation out of killed K-1 — R-A missed[0] + R-B M6, merged; verified by this seat: design-idioms.css:46 says "caps mobile panels"; consumer 1 is the expanded-timeline cell placed into the **desktop** rail/bottom grid cell (AnimationControlsGroup.css:219-222) with the mobile `position:fixed` variant at :179-184; consumer 2 is this centred modal; DESIGN.md:132-138 describes a U.B geometry move out of design-idioms.css that layout.css:9-11 re-asserts never landed). A token whose every call site contradicts its charter, in two documents that disagree about where it lives — the sharper worked example of the census's flat-namespace hazard: a **semantic** collision. **→ KF.W6** (the token-namespace audit names exactly this class).
- **R-22 — MINOR · the keyboard journey through a 19-row reference is Close → Tab → Close** (R-A missed[2]; premise verified: `focusFirst` lands on the sole candidate, the close-X; slot-before-close order cited from the banked kf-CSSPasteDialog dist read — immaterial here anyway, the slot holds zero tabbables). Opening a read-only reference onto its dismiss control with nothing else to Tab to is a distinct UX defect from R-2, cured by the same import: FadingScroll's `tabindex="0"` port precedes the close-X in DOM order, so `focusFirst` lands on the content. **→ KF.W6**.

### INFO / fold-by-reference (identity guard — never re-booked)

- **F-1 phantom dependency** (L-1/C-12): census `lane-frontend.md:15` RED, ruled "F-1 first" (:610-612); folded by reference exactly as the banked kf-CSSPasteDialog did. Riders recorded on the fold, not booked: the CSS-class contract edge (bites vendoring only); **M8 — `@mkbabb/parse-that` is a second phantom install** (verified), so F-1's cure must enumerate, not assume one package; the nightly blast radius (residue #7); the C-headline calibration inconsistency (process note for SS-1/SS-2: one fact rated INFO/BLOCKER across sibling axes of one corpus).
- **C-8 root-barrel import**: folded to the banked kf-CSSPasteDialog C-9 ruling (standing pattern; non-defect; subpath policy owned by census §3.1). Readers' MINOR in DISSENT.
- **C-7/L-4 `:key="shortcut.raw"`**: INFO latent (ruled above); composite key one-liner noted for any wave touching the file. **→ KF.W6** rider.
- **C-9 `/command` shadow → census S-10** (renumbered by this seat — see the ruled collision): evaluate-not-replace confirmed including the combobox-semantics and second-stylesheet reasons; record the decision so the census stops re-raising it. **→ KF.W6** decision record.
- **C-10 group order = registration order**: INFO; no ordering authority anywhere in the chain (verified: no sort; Map insertion). A one-line canonical group list rides any R-6 edit.
- **C-11 one required model, zero pass-through**: INFO, single-consumer facts; `required: true` is the correct posture for the sole host.
- **D-14 viewport-clamped type in a fixed `max-w-md` box**: INFO (both readers), rides the R-6 typography pass.
- **D-15 `pr-1` physical (RTL)**: INFO, tree-consistent, not charged; FadingScroll's RTL normalisation is a bonus of the R-2 cure.
- **D-16 empty branch**: RETIRED — the escape-route claim is killed (K-6, shell barrel verified) and the branch is unreachable in the only host that exists.
- **D-18 `.kbd` under forced-colors**: INFO, **GLASS-OWNED** — rides the same BH relay; forced-colors capture = residue #5.
- **M7 — the dialog graph pays for the engine and never runs it** (R-B, verified: `useSpring-BCHxLjwv.js:3` imports `SpringProgress`; `springPreset` never passed, so the spring mount is gated off). INFO with the dogfooding irony named; a `springPreset` pass is a one-token evaluation. **→ KF.W6** rider.
- **R-A missed[7] — `justify-between` with no gap floor and no `shrink-0`**: INFO; no shipped label reaches collision inside `max-w-md`; any longer future label collides rather than wraps. Rides any R-6/R-19 edit.
- **`unmountOnHide` not force-cast** (C's kill, R-B's reproduction): cleared — the closed dialog really unmounts; recorded so no future pass re-raises it.
- **value.js / R1 unreachability** (C-§1, R-B independently re-traced 62/11/1): CLEARED — the R1 crash class is not on this component's graph; same containment posture as the banked kf-CSSPasteDialog row. Do not re-file.

## Killed-claims register — 12 rows

- **K-1 · the "mobile panel" framing of `--panel-max-h`** (D-12 / L-5 / C-2(a) — all three axes, same error independently; both readers convergent; **re-verified by this seat** at AnimationControlsGroup.vue:80-88 + .css:179-184/:219-222). The token's second consumer is the expanded-timeline teleport cell, applied on BOTH breakpoints and placed into the **desktop** rail/bottom grid cell — not a mobile panel; L-5's own falsifier ("a consumer establishing it as a general cap") is met in substance by the consumer it mis-read. The token-coupling observation survives, relocated to the definition site as R-21.
- **K-2 · L-3(a)'s browser-support matrix, and with it D-1's published grounding** ("in Chromium and WebKit a scrollable div … is not keyboard-focusable; Firefox auto-assigns; the other two do not"). Inverted for the dominant engine (Chromium ≥127 ships keyboard-focusable scrollers; WebKit is the holdout) — as written, anyone checking the matrix refutes the BLOCKER. The verdict survives on the focus-trap proof (R-2), which is engine-independent and verified by this seat.
- **K-3 · C-2(c)'s mechanism** ("DialogContent … receives tabindex='-1' … and is focused on open; `scroll` makes the scroll owner the focused element"). `focusFirst` lands on the close-X (the sole `tabIndex >= 0` candidate); the container fallback fires only when focus did not move (verified). C-2(c) is also contradicted by C-3 in its own document. The remedy's conclusion survives by a different mechanism — DialogContent becomes the nearest scrollable **ancestor** of the focused close-X. Right answer, wrong proof.
- **K-4 · C's prescribed C-2 remedy** ("`scroll`, drop the inner div"). Structurally unsound: `scroll` puts the overflow on the content box, scrolling away the DialogHeader **and** the `absolute`-positioned close-X (`:285`, inside the same box) — the dialog's only visible dismiss control leaves the viewport. D-6 and L-5 independently stated the correct composition (`scroll` + inner port); the C axis prescribed the one variant its two peers ruled out. Adopted into R-3 as a binding cure constraint.
- **K-5 · C's zero-blocker headline clause** ("Nothing here fails a build"). Killed as stated — `npm ci` + the nightly `gh-pages` build cannot both succeed against an undeclared substrate (ci.yml:60-75 verified) — while R-A's implied per-component BLOCKER promotion falls to the identity guard (ruled above). What survives is the calibration-inconsistency process note on the F-1 fold.
- **K-6 · D-16's reachability claim** ("exported from `shell/index.ts`"). The barrel exports exactly EditorShell, EditorHeader, EditorStartScreen, SharePopover (read whole by this seat). The modal is private to shell/; the escape route does not exist.
- **K-7 · C-7's "plus two labelled expand/collapse bindings"**. `expandable-container.js` contains exactly one `registerShortcut` call and one shortcut label ("Exit fullscreen"), my grep. The core latency (a second labelled Escape upstream, unconsumed, fullscreen-gated) survives inside the INFO row.
- **K-8 · C-§1's provenance path** ("`demo/vite.config.ts:28-36`"). No such file — `find` returns the root `vite.config.ts` only (verified); the cited comment is real at the ROOT config's :27-31, which is also one of the M5 documentation sites. Content right, citation unresolvable.
- **K-9 · C-§1's engine importer** ("`useSpringMount-DrKU3wX2.js` … imports `@mkbabb/keyframes.js`"). Off by one hop: its imports are useSpring/springPresets/vue; the actual importer is `useSpring-BCHxLjwv.js:3` (both verified). Conclusion (engine in the dialog graph) right; module wrong.
- **K-10 · L-4/L-2(d)'s "20 call sites"**. My grep: exactly **19** (18 + 1); L's own group arithmetic sums to 19 against its own headline. Harmless to verdicts, fatal to the exhaustive-enumeration claim.
- **K-11 · L-8's JSDoc asymmetry** ("`useRegisteredShortcuts` is the one export with no JSDoc"). None of the five exports carries JSDoc (`useKeyboardShortcuts.d.ts:29-33`, read by this seat). The substantive contract point survives as R-16.
- **K-12 · D-5(2)'s overlay-scrollbar reflow mechanism**. Self-contradictory: overlay bars consume no layout box (nothing to reflow); on classic-bar platforms the always-overflowing list carries its bar from first paint (no materialisation moment). The quarter-gutter and unused `.scroll-gutter-stable` halves survive inside R-4.

## Superlatives — 7, with provenance

1. **The panel is a pure projection of the live registry — the canonical shortcuts-panel drift bug is designed out** (all three axes + both readers; re-verified). No shortcut table exists in the file; 19 of 19 rows derive from the registry; the upstream discipline is stated in prose at useControlsKeyboardShortcuts.ts:30-33 and was honoured by the later F.W14.S1 undo/redo pair (:66-69) — an invariant that survived a subsequent edit. (Precision note on R-A's "19 of 20": the census is 19 of 19 — R-B's count, mine.)
2. **Zero teardown surface — the component cannot leak** (both readers, verified). `:40` imports only `computed`; the window listeners live in glass-ui's detached `createGlobalState` scope, ownable and cancellable by no component; the closed dialog genuinely unmounts (the `unmountOnHide` kill reproduced). In the repo of the ~40-loop ungated-rAF epidemic, an empty teardown obligation is worth naming.
3. **`.kbd` passes two independent laws with one class** (D-S1/C-S+5, both readers). Consumed as-shipped (sole definition in the producer sheet, zero demo redefinitions — my grep) AND an enumerated `monoAllowedSelectors` member — the exact inverse of census S-1. The author knew the mono contract where it is hardest to notice, which is what throws R-7 into relief.
4. **`formatComboParts` over `formatCombo`, and zero platform knowledge demo-wide** (both readers, verified: `isMac|navigator.platform|⌘` in demo → 0). One `<kbd>` per chord token is the semantically correct HTML; every platform branch stays upstream. This is also why R-8/R-17's durable cures are producer-side — the demo is right not to own the mapping.
5. **Best-of-cohort two-way `open` contract at Goldilocks size** (both readers). `defineModel<boolean>('open', { required: true })`, no shadow ref, no read-after-write path — the project-memory `defineModel` trap has no entry point; both sibling dialogs do it worse.
6. **The corpus killed its own most seductive hypothesis three times independently** (R-A's corpus superlative, adopted). All three axes raised unlabelled-shortcuts→blank-rows, all three read `keyboard.js:83` and recorded the death rather than the temptation — triple-independent falsification, the strongest health signal in this slug.
7. **L's refusal to extend census F-4 to the scroll port is the reasoning that later refuted the C-axis cure** (R-A's corpus superlative, sharpened by the ruling). L-5 declined the pattern-match because the inner div buys a pinned header the shipped `scroll` posture cannot give — precisely the ground on which K-4 kills C's remedy. Discipline in one axis became the deciding evidence against another.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. **Space keydown `preventDefault` vs `<button>` activation per engine** (the C-3 half that decides whether the close-X is genuinely un-activatable by Space). Premise now source-certain (focus IS on the close-X); only engine behaviour remains. Probe: modal open, press Space on the focused close-X, record activation + play-state.
2. **The overflow and clip thresholds as measured, not derived** — one `scrollHeight` read on the `:10` element; a 400%-zoom capture on an 800px window; a landscape-phone frame at ~320 CSS px (the case that passes by ~2%). The signs of both inequalities are source-certain; the figures are clamp arithmetic.
3. **WCAG 1.4.3 on the 10px mono headings and the `.kbd` plate over glass** — undecidable statically (runtime-sampled `--glass-backdrop-luma` + `contrast-color` branch; all three axes rightly declined to fabricate a ratio). Measure both themes over light and dark backdrops.
4. **SR transcripts** — VoiceOver + NVDA on the bare glyph keycaps (R-8) and on the flat row structure (R-11); the R-11 rung finalises on this.
5. **`.kbd` under `@media (forced-colors: active)`** — whether the forced border alone keeps caps legible (D-18's INFO rating).
6. **The sub-640px centred header** (R-19) — defect or intentional mobile treatment; needs the rendered frame.
7. **Whether the nightly `demo-correctness` job is currently RED** (ci.yml:60-75 `npm ci` → `gh-pages` vs F-1) — decidable only by reading a CI run; the F-1 fold's blast-radius rider.
8. **Engine-matrix hygiene only, no verdict weight**: whether WebKit has shipped keyboard-focusable scrollers — immaterial here (the trap forecloses every engine) but the K-2 premise should be settled before any corpus reuses it. **R-A's Tab-reach probe is retired**: foreclosed statically by the trap (ruled above); do not spend a probe on it.

## DISSENT — preserved, not ruled away

1. **R-A's BLOCKER on R-1**: preserved with its promotion condition. The demotion is a reversibility-precedent ruling, not a mechanism dispute — if SS-13 witnesses a destruction path behind the modal with no live undo route (cold history, recording state, the Escape rewind judged data-loss in practice), R-1 re-promotes and R-A's rung is vindicated.
2. **R-A's BLOCKER on L-1**: falls to the identity guard, but its underlying instinct — that a corpus rating one fact INFO and BLOCKER across axes has a calibration defect — is adopted as a process row on the fold.
3. **Both readers' MINOR on C-8**: overruled by the banked precedent; preserved here. If SS-1/SS-2 ratify a subpath policy that re-opens the question, the readers' rung re-enters through that policy, not through this component.
4. **R-B's MINOR on C-7/L-4**: overruled to INFO on the two-gates-out mitigation; preserved. A third registration site or an `ExpandableContainer` adoption revives it instantly, with the composite-key one-liner as the standing cure.
5. **Reader method events, recorded as fleet evidence**: R-A left its verdict's decisive question to a live probe that its own co-reader — and this seat, from the same dist files R-A had open — settled statically; the lesson is that a trap's keydown handling must be read before browser policy is invoked. R-B, conversely, mis-graded nothing this session that this seat found — but neither reader caught the S-9 census-id collision, which argues that census extensions need a registry-allocation step, not reader vigilance.
6. **R-A's severity note on D-7/L-9** (endorsing INFO while filing MINOR): the internal tension is resolved MINOR here; if the SS-13 SR transcript shows the pairing conveyed acceptably by DOM order, L-9's INFO was right and R-11 demotes.

## Closing verdict

**ADJUDICATED — 22 confirmed (0 BLOCKER · 10 MAJOR · 12 MINOR; both axis BLOCKERs off the top rung — D-2 by the reversibility precedent with a named escalation, L-1 by the identity guard) · 12 killed (the three-axis `--panel-max-h` framing dead by this seat's own read; D-1 re-grounded on reka's focus trap, statically certain in every engine, its SS-13 decider retired) · 17 missed rows folded in · component STAYS; cure spine = FadingScroll-port + `scroll` composition (KF.W6, retires 4 rows) + the GLASS-OWNED registry-modality relay carrying the LIFO/FIFO cure-shape law, the label-narrowing, the alias-surfacing, the keycap accessible-name companion, and the `.kbd` forced-colors note · R-9 typecheck-void and the dead font-census witness = NO-WAVE-OWNER for SS-1/SS-2 · R-10 → KF.W8 · F-1 folded by reference with the parse-that second-phantom rider · the C-9 census extension renumbered S-10 against the banked registry.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/shell/KeyboardShortcutsModal.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **KeyboardShortcutsModal.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**CORRECTION (v) of the six named at G-0.9 — THE SEAT-RULED RENUMBER IS DEMOTED TO A CLAIM-INPUT** (R-5; E-3 addendum under the original id, never a rewrite). The cell at `:29` rules *"COLLISION — seat's own ruling: renumber to S-10"*. **That renumber is superseded by the KF.W0 C-17 single-motion mint; the slot id is whatever the mint assigns.** A claim is an INPUT to the mint and is never itself a slot, and no wave other than KF.W0 mints or renumbers a census slot.

**The seat's own booked lesson is thereby honoured rather than repeated**: *census extension ids must be allocated against the banked registry, not against the local corpus.* The renumber landed a SECOND "S-10" on top of `kf-KeyframesEditor:63`'s and `kf-SquareInstrument:78`'s — which is the exact mechanism C-17 exists to kill.

**R-7/R-9 provenance, corrected at the wave and recorded here**: `:43` is **R-7**, the dead-witness row (`DESIGN.md:24-30` states the Mono-as-data law and names `proof:font-census`); `:45` is **R-9**, the *"nothing automated verifies this file"* row, which routes to **KF.W4**. The two are separate rows and the dead-gate arm alone lands in this wave's roster.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

---

## CORRECTION (v), CONTINUED — THE SLOT THE MINT ASSIGNED · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e** · gate **G-0.9** · cross-unit discharge of **KF.W0.d residual 3**

Correction (v) above states *"the slot id is whatever the mint assigns"* and stops there, because a claim-input is not a slot. **The mint has since run, in the same wave, at the same clock.** The slot it assigned this record's `/command` family shadow is named here so the sentence terminates at a coordinate rather than at a promise:

> **`S-10` (this record's self-ruled renumber, `:29`) → the mint assigns `S-17`.**

**Mint of record** ⟨`grep -n 'S-17' docs/tranches/V/megatranche/formation/keyframes/lane-frontend.md`⟩ → `:918` `#### S-17 · the /command family shadowed — **MINT**`; the block's own re-key line `:955` reads **`S-10`** → {S-17 · S-18 · S-19 · S-20} ∪ {the struck duplicate → S-18}. Write-up at `docs/tranches/X/keyframes/W0/COUNTS-2026-09-17.md` §7 (row **S-17**, disposition *CLAIM-INPUT ONLY*), which names this E-3 addendum as `KF.W0.e`'s act so the obligation could not be lost between the two concurrent units.

**What this does NOT do.** It does not re-open the demotion, re-rule the collision, evaluate the shadow, or move the row's routing: the `/command` family shadow's evaluation remains **evaluate-not-replace → KF.W6**, exactly as banked at `:68`. Naming the assigned slot is bookkeeping on an id, not a disposition of a cure — and the id is the mint's, never this record's.

