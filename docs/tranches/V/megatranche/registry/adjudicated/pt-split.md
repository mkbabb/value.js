`claude-fable-5`

# pt-split — adjudicated apotheosis (M-23/L-18 fresh-Fable seat)

**Seat**: fresh Fable adjudicator (incredulity seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · **Subject**: `/Users/mkbabb/Programming/parse-that/typescript/src/parse/split.ts` (58 lines / 1,789 bytes / 2 exports) + its Rust twin `rust/parse_that/src/split.rs` (69 lines) · parse-that HEAD `ef10d5b` (main checkout, READ-ONLY, re-verified this session) · value.js working tree at `c654824e`, branch `tranche-u`.
**Corpus**: two challenges — axis **L** (`audit/parse-that-modules/split/challenge-L-library.md`, Opus 5 [1m]: 11 defects + 6 superlatives) and axis **C** (`.../challenge-C-consumption.md`, Opus 5 [1m]: 14 defects + 5 superlatives) — and two reader returns adjudicated: **reader-1** (slug `split`) and **reader-2** (slug `split-consumption-api-L18-hostile-reread`).
**Method (L-18/M-23)**: every reader-vs-reader disagreement was re-derived by this seat with its own instruments before either reader was adopted; suspiciously-agreed rows (the gorgeous kill, the CJS twin, the census kills) were independently re-measured, not spot-trusted. Dispositions below are **ADJUDICATED, not VERIFIED** — X·P execution remains blocked on the owner's begin-word; nothing here moves a verb.
**Writes by this seat**: this file only. `/Users/mkbabb/Programming/parse-that` read-only; the fresh root `/Users/mkbabb/Programming/parse-that-css-totality-p2` was neither created nor probed for creation.

**This seat's own instruments (all static + node byte-copies in the session scratchpad; zero imports of `@mkbabb/parse-that`, nothing armed):** full reads of `split.ts` (whole, 58), `split.rs` (whole, 69 — doctests `:19-41` = 4 `assert_eq!`; guard arm `:60`; slice `:61`; clamp `:59`; brackets `:58-59`), `core.ts` (whole), `utils-entry.ts` (whole), `balanced.rs:1-20,70-90` + `git ls-files` (COMMITTED), `proof-no-dead-combinator.mjs` (banned array, dispatch-only vacuity guard, `existsSync`-silent walk), `.github/workflows/ci.yml` (whole ts job), `value.js src/css/grammar.ts:60-130`, `stylesheet.ts:370-378,584-590`, `types.ts` (ParseResult), `css/index.ts:36-60`, `parsethat-surface-gaps.mjs:44-58`, `INBOX.md:77` (O-15 pin), `parser-band.md:116-120`, `X/parse-that/waves/{W0..W4}.md` headers + `W2.md` anchor lines (58, 132, 161-162, 193, 239-241, 253, 266-268, 281, 294, 313, 370). Repo facts re-measured: `src/parse/*.ts` = **14** files; `test/split.test.ts` = **13** `it()` (2+11, lines pasted); exports map = 5 keys, no `./split`; `proof:all` = **10** scripts (not C-6's 5 — all 10 checked, none names either export); `npm test` = bare `vitest run` (the `.mjs` gates never execute under it); `PACKRAT_ARMED` = 4 in `packrat-entry-CS1td-8B.js` **and 4 in `packrat-entry-46NYx4_U.cjs`**; `dist/core.js:1` ESM chunk import; `dist/core.cjs:3` **eager `require`**; `packrat-entry-CS1td-8B.js:1` imports 6 names from `diagnostics-DDazRHgl.js`, whose `:1`/`:50` are module-level `let diagnosticsEnabled` / `let collectedDiagnostics`; `console.error` at `:262` (default param) and `:882` (`:14` of the diagnostics chunk = the label-drop, `console.error` count 0); `lazy.ts` `export…from` = **0** (challenge table said 3); `splitTopLevel` = definition + **22** call sites, **8** of them `"space"` (grammar 181/471 · stylesheet 54 · timeline 23/38/71/72/75); parity-blind escape idiom at **five** sites (grammar `:71`,`:103`; stylesheet `:373` index-spelling, `:449`, `:586`); gorgeous evidence (`docs/benchmarks/2026-03-07-baseline.md:103-107`, `docs/publishing.md`, `rust/CLAUDE.md:147-148`, `CLAUDE.md:121`; `bbnf-lang/crates/gorgeous/Cargo.toml` v0.1.10 with `parse_that = "0.4"`; **zero** `to_doc`/`split_balanced` hits in today's `bbnf-lang/crates`). Dynamic battery (byte-copies of both implementations): the full non-string matrix incl. `["x",",","y"] → [["x"],["y"]]` (slow path), the surrogate split (`["a","\uDE00b"]`, wellFormed `[true,false]`, join===original **true**), the multi-char/empty/RegExp delimiter rows, bracket cross-cancel, unterminated quote, six conservation corpora, 10^6 nesting, **and the two decider probes**: `content: "a\\"; color: red` on `;` (PT 2 parts correct / VJ 1 part swallowed) and `[a,b], .c` on `,` (PT `["[a,b]"," .c"]` / VJ `["[a","b]",".c"]`).

## Scoreboard

**15 roster rows survive (CONFIRMED or RESCOPED) · 13 killed-claims register entries · 12 reader-recovered misses adopted (neither challenge filed them) · 8 superlatives upheld · 6 MEASURE-AT-OPEN residue rows.**

**No BLOCKER survives inside parse-that** — the two challenge BLOCKERs (L-D1/C-2, C-1) are capped **MAJOR** by their own corpus's zero-consumer census (ruling 1). The **blocker-weight survivor of the whole corpus is PT-SPLIT-14** — the value.js `splitTopLevel` escape swallow, the only defect here with a shipped consumer (`/css` at 4.0.0), measured live by this seat — and it is **X·V-owned, not X·P's** (W4: "This wave writes no value.js source bytes … X·V owns its bytes").

Honest axis tallies after adjudication (reader-1's reconciliation adopted): **axis L = 10 defects** (L-D11 struck as a null result; one row bench-dependent), **axis C = 13** (C-12 folded into the C-2 identity).

## Ruled disagreements (the incredulity seat's actual work)

| # | topic | reader-1 | reader-2 | RULING · deciding evidence (this seat's own) |
|---|---|---|---|---|
| 1 | Headline severity (C-1 · L-D1/C-2) | MAJOR, capped by the zero-consumer census | BLOCKER, as filed | **MAJOR — reader-1 wins.** A silent wrong answer no shipped code path reaches is a severity of the API surface, not of live behavior; the surface is headed for retirement (PT-SPLIT-5) and the corpus's own C-1 proves the blast radius. **Census-bounded rider**: the cap rests on the four-tree sweep, and the C-7 kill proves that sweep is structurally blind out-of-tree (gorgeous was real and invisible to it) — so the cap holds only together with PT-SPLIT-7's finding that the census mechanism must be repaired before retirement executes. |
| 2 | **C-5** (wrong tier — `./utils` "the correct home") | **DROPPED with no disposition** — absent from confirmed, killed, and missed | CONFIRMED MAJOR | **RESCOPED — neither reader is adopted.** My reads: `core.ts:4` names "the balanced-split helpers" **in the tier doc itself** — the placement is a documented decision, not drift; and `utils-entry.ts` is a **parser** tier (`jsonParser`, `csvParser`, `escapedString`/`quotedString`/`numberParser` are combinators) — a plain string function has no correct home among the five doors, which is *retire* evidence (C §6), not a relocation defect. The surviving content (2/18 = 11% inflation of the documented minimal tier by stdlib-replaceable helpers) folds into PT-SPLIT-5. Reader-1's silent drop is recorded as a reader defect: every row leaves with exactly one disposition. |
| 3 | C-10 severity (braces/comments) | INFO (contract scoped out) | MINOR (downstream needs both) | **INFO — reader-1 wins.** `split.ts:16-17` scopes the contract to `()[]`+quotes; the documented consumer is BBNF `@pretty split()` over **selector spans** (`rust/CLAUDE.md:147`), not stylesheet bodies. Reader-2 imported value.js's needs into a module documented for a different caller. |
| 4 | C-12 identity (empty delimiter) | folded into the C-2 domain row | separate MINOR row | **FOLDED — reader-1 wins.** The mechanism is `ch === delim` unsatisfiable for `delim.length !== 1`; length 0 and length ≥ 2 are the two ends of ONE domain defect (PT-SPLIT-1). L-D10 stays separate — arm-order shadowing is a different mechanism (ruling cross-referenced at PT-SPLIT-12). Anti-rename: one identity, one cure. |
| 5 | C-14 panic sub-claim | UNMEASURED (as filed) | source-certain by Rust spec | **Reader-2's upgrade ADOPTED.** My read of `split.rs:53-61`: every arm before `:60` matches only ASCII structural bytes or in-string guards, so a continuation-byte `delim` (e.g. `0xA9`) reaches the `:60` guard arm mid-character and `:61` slices `&text[start..i]` off a char boundary — a panic **by language specification**, no execution needed. The executable witness stays MEASURE-AT-OPEN (cargo writes into the read-only tree). Cite drift both readers caught is confirmed: the slice is `:61`, not C-14's `:62`. |
| 6 | C-3's falsifier cell ("value.js handles it") | REFUTED via missed #1 | ENDORSED in its C-3 row | **Reader-1 wins, measured by this seat.** `grammar.ts:71` tests one preceding character, not backslash **parity**. Byte-copy run: on `content: "a\\"; color: red` (a legal declaration whose string ends in an escaped backslash) VJ returns ONE part — the remainder swallowed — while the shipped `splitBalanced` returns the correct two. Neither implementation implements CSS §4.3.7; they fail on **complementary** escape inputs. Reader-2 re-measured only the case the challenge posed (`'It\'s, mine'`) and inherited the cell — a citation-inheritance event, recorded in DISSENT. |
| 7 | value.js `[]`-blindness — framing + severity | MINOR (a gap in C §2d's table) | MAJOR (live defect reachable from `parseStylesheet`) | **MAJOR — reader-2's framing wins.** My reads + run: `splitTopLevel` (`grammar.ts:63-87`) has **no** `[` branch anywhere; `stylesheet.ts:738` splits selector preludes on `,` beneath the published `parseStylesheet`; `[a,b], .c` splits inside the attribute selector. Reader-1's framing (the 11-shape table has no bracket row, so the head-to-head never scores the axis parse-that wins) is kept as the epistemic note on the same row (PT-SPLIT-15). |

## Adjudicated defect roster

Line anchors: parse-that `ef10d5b`, value.js tree at `c654824e` — re-resolve before any cure (the D-19 lesson). Dispositions: an **X·P wave id** where a W0..W4 unit/gate owns the surface · **X·P REFINEMENT FOLD** where the finding sharpens a ratified spec · **NO-WAVE-OWNER** otherwise. Nothing below authorizes execution.

- **PT-SPLIT-1 — MAJOR — the delimiter domain is a lie in the TS lowering** (L-D1/C-2 + C-12 folded + the surrogate rider). `split.ts:10-11` (`indexOf`, multi-char-aware) vs `:28`/`:50` (`text[i] === delim`, one UTF-16 unit): guard and consumer answer **oppositely** for every `delim.length !== 1` — measured: `('a||b||c','||')` guard true/unsplit; `('a, b, c',', ')` unsplit; `('abc','')` guard true/unsplit; coercion `('aundefinedb',undefined)` guard true/unsplit. **Surrogate rider (reader-1's miss, re-measured)**: `splitBalanced('a\u{1F600}b','\uD83D')` → `["a","\uDE00b"]` — a matching lone surrogate SPLITS INSIDE the astral pair and emits ill-formed UTF-16 (`isWellFormed` → `[true,false]`), which **holes both challenges' proposed cure**: "one code point" admits U+D83D; "one character" is ambiguous. The Rust twin types `delim: u8` (`split.rs:10,42`) — unrepresentable there. **Adjudicated cure spec: the domain is ONE NON-SURROGATE SCALAR VALUE.** → **X·P REFINEMENT FOLD** (W2 §3b wrap-obligation: domain narrowing; the EQ-1 specimen `("a::b","::")` rides the W2 `.g` seat per C §6's standing recommendation, which this seat adopts).
- **PT-SPLIT-2 — MAJOR — no escape state in the quote machine, both lowerings** (L-D2/C-3). `split.ts:30-37` has no `\` arm; `split.rs:55-57` identical; contradicts the module's own unconditional promise (`:16-17`) and the house idiom (`parsers/utils.ts:7-16`). Measured: the `'It\'s, mine'` split lands mid-string-token. The **strongest in-tree contradiction is the one neither challenge read**: `rust/parse_that/src/parsers/scan/balanced.rs:15` `pub escape: u8` with the `memchr2(quote, escape)` skip loop (`:74-86`) — the crate already wrote the cure, committed. → **X·P REFINEMENT FOLD** (W2 §3b wrap-obligation 2: CSS Syntax §4.3.7 escape state; **the wrap target is `balanced.rs`, not a fresh design** — the W2 `.g` seat is pointed at it, not asked to invent it).
- **PT-SPLIT-3 — MAJOR — no JS boundary, and the laundering is loop-wide, not fast-path-only** (L-D3/C-4 + reader-1's miss #3, re-measured). 4/5 non-strings die at a raw built-in `TypeError` (`:11` is the first touch; no `typeof` at `:10`,`:18`,`:20`); `[]` → `[[]]` through the fast path — and, the part both challenges' diagnosis omitted, **every loop operation is Array-polymorphic** (`.length` `:27`, `[i]` `:28`, `.slice` `:51,:56`, `.push`): measured `splitBalanced(["x",",","y"],",")` → `[["x"],["y"]]` — a genuinely *split* array-of-arrays from the **slow** path, typed `string[]` (`dist/split.d.ts:2`). A cure at the `return [text]` line alone — the fix either challenge's text implies — leaves this open; the cure is a named boundary invariant at entry (`typeof text === "string"`), per W2 §3.3 ("non-string inputs die at a named JS-boundary invariant **above** the algebra") and parser-band binding debt 4. → **X·P W3** (§6: the R1 throw class dead / typed totality at the boundary), citing W2 §3.3.
- **PT-SPLIT-4 — MAJOR — `core.ts:3-5`'s tier guarantee is false at the shipped bytes THREE ways** (L-D4 + reader-1's miss #4 + reader-2's M-3, all re-verified). (i) `dist/core.js:1` imports the 1,438-line packrat chunk (PACKRAT_ARMED ×4, five module-level containers, the `process.env` probe). (ii) That chunk's own `:1` imports 6 names from `diagnostics-DDazRHgl.js`, which carries `let diagnosticsEnabled = false` (`:1`) and `let collectedDiagnostics = []` (`:50`) — **the diagnostics accumulator, the second "never pulls" clause, is also false**; only the json/csv clause holds (`core.js` does not import `utils.js`). (iii) **The CJS door has no hedge at all**: `dist/core.cjs:3` is an eager, non-elidable `require("./packrat-entry-46NYx4_U.cjs")` (41 KB, PACKRAT_ARMED ×4) — `sideEffects:false` and tree-shaking have no purchase on a `require` graph, so L-D4's "a bundler may drop it" mitigation covers only one of the two shipped conditions. A `./core`-only consumer transitively evaluates **both** process-global latch modules the megatranche tracks. → **X·P W4** (packed-release admission: the successor's shipped entry graph must prove tier separation *at the bytes, under both `import` and `require` conditions*; this row is the reciprocity evidence for that predicate).
- **PT-SPLIT-5 — MAJOR — a dead, doubly semver-locked, ungated surface** (C-1 census + C-6 + the C-5 residue + L-D7 riding). Zero non-test consumers across the constellation's own four-tree definition (re-verified: repo-wide grep + value.js manifest/node_modules clean); meets `proof-no-dead-combinator.mjs:10-11`'s kill precept verbatim, escaping only because `banned` (`:29-32`) is a two-name literal; ships from **two** entry points (`index.ts:13`, `core.ts:26` → both dist pairs + the CJS mirrors) = two semver contracts for one function; **no gate pins either name** — and my check extends C-6: `proof:all` is **10** scripts (C tabled 5), none of the other five names them either. Sharpened split (reader-1, adopted): the **TS pair** has no named consumer anywhere; the **Rust pair** mirrors gorgeous — real, out-of-tree, benchmarked (killed-register #1), and retired from today's `bbnf-lang/crates`. `containsDelimiter` (L-D7: `includes` re-implemented with a silently-coercing RegExp arm, measured `('a,b',/,/)` → `false`) dies with the surface — the Rust guard's `memchr` justification has no TS analogue. → **X·P W4** (the retirement lands at the successor's release surface; the mechanism C §6 proposed is **broken as written** — see PT-SPLIT-7 before executing any deletion).
- **PT-SPLIT-6 — MAJOR — the AC-1 signature leak, already realized in the tree's only dual-target pair** (C-14, panic upgraded per ruling 5). `string`/UTF-16 units vs `u8`/bytes; `README.md:96` presents one matched row and omits the guard pair; the TS side answers inputs the Rust side cannot pose, and the Rust side **panics by spec** on a continuation-byte delimiter (`split.rs:60-61`, tail `:67`) where TS returns the input. W2 §3c predicted exactly this as AC-1 TAGLESS-TWIN failure (a)/K-2 — here it is, shipped, 58 lines, with an 11-row parallel oracle attached. → **X·P W2** (`.g` seat: the cheapest live AC-1 specimen; a dual-target equality probe fails EQ-1 on `("a::b","::")` before any candidate writes a line — a gate that can fail for its intended reason, L-19). Panic witness: MEASURE-AT-OPEN.
- **PT-SPLIT-7 — MAJOR — the gate infrastructure cannot prove retirement's own precondition** (reader-2's M-1 + M-2, both re-verified at the bytes). (i) **No `proof:*` script runs in CI at all**: `.github/workflows/ci.yml`'s typescript job is `npm ci` / `tsc --noEmit` / `npm test` / `npm run build`, and `npm test` is bare `vitest run` — the `.mjs` gate files in `test/` and `scripts/` never execute under it. Every surface invariant C-6 reasons about is locally-run ritual. (ii) **The cross-tree sweep is silently vacuous**: `proof-no-dead-combinator.mjs`'s `walk()` opens with `if (!existsSync(dir)) return acc;`, the CI checkout is single-repo, and the vacuity guard checks only that `dispatch` is defined — never that the sweep visited a nonzero file count. Executing C §6's "add two rows to `banned`" would delete a surface on the strength of a sweep that examined nothing — and that same sweep is **structurally blind out-of-tree by construction**, the exact blindness that produced the C-7 false kill. → **X·P REFINEMENT FOLD** (W4's release-condition predicate: any census gate the release cites must be CI-wired and must assert a nonzero visited-file count); the parse-that-main CI cure itself is **NO-WAVE-OWNER** (owner relay — X·P's waves own the fresh root, not main's workflows).
- **PT-SPLIT-8 — MINOR — one depth counter, no bracket-kind stack** (L-D8/C-9). `(`/`[` share `depth++` (`:41-44`), `)`/`]` share the clamp (`:45-48`); measured `'f(a],b)'` → split inside the call. Both lowerings identical (`split.rs:58-59`) — a specification defect the mirror faithfully reproduced. `balanced.rs:9,:11`'s separate `open`/`close` is the written cure. → **X·P REFINEMENT FOLD** (W2 §3b wrap-obligation 3: enumerate bracket families, never fold them).
- **PT-SPLIT-9 — MINOR — no failure channel, and the receiving type already exists, frozen** (L-D9/C-11 + reader-2's M-5, verified). An unterminated quote swallows the remainder (`:24/:34/:39/:56`; measured `'"a, b'` → one part) and `string[]` makes failure unrepresentable — while `ParseResult`'s `ok:false` arm with its **non-empty** `[ParseIssue, ...ParseIssue[]]` tuple is already exported on the frozen `/css` surface (`value.js src/css/types.ts`, W2:294 makes it load-bearing). The cure is not a design question; the type is waiting. → **X·P W3** (typed recovery over the closed union: the unterminated quote appends to `D`, per R-LAW-3/EQ-4).
- **PT-SPLIT-10 — MINOR — the differential corpus covers only the happy path** (C-13 + the doctest correction). TS: 13 `it()` (2 + 11, all delimiters `,`). Rust: 11 `#[test]` **plus 4 rustdoc doctests** (`split.rs:25,:28-31,:34-37,:40`, all `b','`) = 15 executable assertions neither challenge fully counted; `contains_delimiter` has **zero** Rust assertions of any kind. Zero rows for any measured defect class. → **X·P W1** (corpus + oracle seats: the defect-class rows of this roster enter the differential corpus; the guard export gets an oracle on both lowerings or dies un-oracled with PT-SPLIT-5).
- **PT-SPLIT-11 — INFO — a delimiter drawn from `"'()[]` can never split while the guard affirms it** (L-D10). Arm order (`:30/:41/:45` each `continue` before `:50`); measured both rows. Identical arm order in Rust — a genuine, nowhere-documented design boundary, mechanically distinct from PT-SPLIT-1 (ruling 4). → **X·P REFINEMENT FOLD** (the wrap spec writes the boundary down; the domain narrowing does not cure this — `"("` is a legal scalar).
- **PT-SPLIT-12 — INFO — no `{}`/comment awareness** (C-10, RULED INFO per ruling 3). Real behavior, measured; out of the module's own written contract and its documented caller's shape. → **X·P REFINEMENT FOLD** (the successor's token classes are W2 §3b's enumeration business; `balanced.rs:13,:17` already carries `quotes`/`terminators` fields).
- **PT-SPLIT-13 — INFO — the corpus never reconciles its own headline** (reader-1's miss #8, adopted as the tally row). Each challenge rates a silent-wrong-answer BLOCKER while the same corpus proves zero blast radius; C double-counts C-2/C-12; L counts its own refuted L-D11. Honest counts: **L = 10, C = 13, headline MAJOR** — and the only blocker-weight item is downstream (PT-SPLIT-14). → disposition: **this record** (the scoreboard above is the reconciliation).
- **PT-SPLIT-14 — MAJOR, blocker-weight (the only defect in this corpus with a shipped consumer) — value.js's escape check is backslash-parity-blind and swallows declaration bodies** (reader-1's miss #1, re-measured by this seat; ruling 6). `grammar.ts:71` tests `source[i-1] !== "\\"` — one character, not parity. Measured on the legal `content: "a\\"; color: red`: `splitTopLevel(·,";")` → **one part, remainder swallowed**; the retired-surface `splitBalanced` returns the correct two. Replicated at **five** sites (my census — reader-1 said four): `grammar.ts:71` (`splitTopLevel`, beneath `parseStylesheet`→`splitDeclarations` `stylesheet.ts:96` and all six `collect*`), `grammar.ts:103` (`splitValueTokens`), `stylesheet.ts:373` (`emptyComma`, `index` spelling), `stylesheet.ts:449`, `stylesheet.ts:586` (`topLevelColon`). Neither scanner family implements CSS §4.3.7 — the two codebases fail on complementary inputs. → **NO-WAVE-OWNER in X·P** — value.js source bytes are **X·V-owned** (W4 preamble; COHESION §2 routing law). **Loud relay flag**: this row must reach the X·V formation ledger; it is the corpus's only live-consumer defect and no X·P gate will ever witness it.
- **PT-SPLIT-15 — MAJOR — value.js's four scanners track no `[]` at all** (reader-1's miss #6 ≡ reader-2's M-4; ruling 7). No `[` branch in any of the five scan sites; measured `'[a,b], .c'` → VJ splits inside the attribute selector, PT does not; reachable from the published `parseStylesheet` (`stylesheet.ts:738` selector preludes). Epistemic note kept: C §2d's 11-shape table is paren-only, so the challenge's "10/11 DISAGREE, value.js mostly wins" ledger never posed either axis the published module wins (this row and the clamp, S-CLAMP) — the evidence C §6's RETIRE verdict most needed to weigh existed and went unmeasured. → **NO-WAVE-OWNER in X·P** (X·V relay, same flag as PT-SPLIT-14); the head-to-head correction FOLDs into W2's evidence record.

## Killed-claims register

Each entry re-verified by this seat unless marked reader-verified-and-spot-checked. None of these survives; several strengthen the corpus by dying.

1. **L-D5 headline / C-7 "the consumer never existed"** — KILLED. Both greps were extension/directory-blind to the `*.md` evidence: gorgeous is named and benchmarked in-tree (`docs/benchmarks/2026-03-07-baseline.md:103-107` — `@pretty selectorSpan split(",")`, `split_balanced()` memchr fast-path, `contains_delimiter()` "~95% of rules", `to_doc` 1,314 MB/s; `docs/publishing.md`; `rust/CLAUDE.md:147-148`; `CLAUDE.md:121`), and exists at `/Programming/bbnf-lang` (`gorgeous` 0.1.10, `parse_that = "0.4"` — my read). **Honest bound, verified**: `to_doc`/`split_balanced` have **zero** hits in today's `bbnf-lang/crates` — the consumer is retired/renamed, so exactly one C-7 disjunct survives ("removed without updating the doc"), refiled narrower inside PT-SPLIT-5: the TS pair consumer-less everywhere, the Rust pair's consumer real-but-lapsed.
2. **C-8's headline + falsifier** — KILLED twice without a bench. The title inverts its own table (the advice addresses the ABSENT case, where the guard *wins* 0.841×; the 1.298× loss is the case the advice doesn't address), and the "unmeasured common case" **is measured in-tree** (the ~95% single-selector figure, same file as #1) — the falsifier fired. Residue at INFO inside PT-SPLIT-5: `splitBalanced:20` already guards internally, so a doc-following caller double-scans. Ratios → MEASURE-AT-OPEN.
3. **C §5's "O-15/PT-07 contradicted in one row (4/5 not 5/5)"** — KILLED, category error. PT-07's 5/5 is `parseState` (verified at `parsethat-surface-gaps.mjs:52-56`); `splitBalanced` is a different function O-15 never probed. Extension, not contradiction — and under the epoch rule an asserted contradiction of a pinned authority must be exactly right. The false-correction is expunged from the record.
4. **C-2c/2d's `"space"` count of 5** — KILLED. Live count **8 of 22** (grammar 181/471, stylesheet 54, timeline 23/38/71/72/75 — my enumeration; a naive single-line grep undercounts because three sites nest `.replace()` parens). Runs *against* the challenge: 36% of the retrofit surface is structurally unrepresentable, strengthening its own "breaking redesign" verdict.
5. **C-2e's coordinates** — KILLED. The GUARD rows are `parsethat-surface-gaps.mjs:56-57`; `:47-52` is the LATCH region (verified). The routing substance (only the GUARD class reaches this module) survives; the coordinates would break any gate that consumed them.
6. **L's "12 cases" (four-to-five sites)** — KILLED. `test/split.test.ts` has **13** `it()` (lines 5,9,15,19,23,30,37,41,45,52,56,60,64 — pasted from my run). C-13 had it right. The substance (all delimiters `,`) survives.
7. **L-D6's arithmetic + equivalence claim** — KILLED as stated. "Five string comparisons" enumerates **7** in its own sentence and omits `:39` (worst path 8). And the `charCodeAt` variant is **not semantically equivalent in general**: reducing `delim` to one char code cannot reproduce `ch === delim`'s never-match for multi-unit delimiters — the equivalence held only on the single-char corpora tested, and the L-D6 cure is therefore **coupled to PT-SPLIT-1's domain narrowing** (you cannot land the scan rewrite first). Ratios → MEASURE-AT-OPEN.
8. **L §3's "no others anywhere in `dist/`"** — KILLED. `dist/packrat-entry-46NYx4_U.cjs` carries 4 more `PACKRAT_ARMED` (my grep) — the prose overreached its own falsifier's correct `dist/*.js` scope. The one-way-latch conclusion is unaffected; a CJS `./core` consumer pays the same tier (→ PT-SPLIT-4 iii).
9. **L §0's dependency census table** — KILLED as a table. The `export…from` column duplicates the `import` column (spot-verified: `lazy.ts` claimed 3, live **0**), 7 of 10 rows wrong; the glob is **14** files, not 10 (`core.ts`, `index.ts`, `packrat-entry.ts`, `utils-entry.ts` silently scoped out — my `ls`). The two-empty-edge-files conclusion survives under both columns; filed as a checkability defect.
10. **L-D11 in the §4 tally** — STRUCK. The challenge itself records the hypothesis refuted, then counts it among "defects total 11". A falsified hypothesis is a null result. Retained as a *method superlative* (below), struck from every count.
11. **C-5's "the correct home is `./utils`" arm** — KILLED by this seat (ruling 2 — a kill neither reader filed: one dropped the row, one confirmed it whole). `core.ts:4` documents the placement; `./utils` is a parser tier; no correct door exists — which is retirement evidence, not relocation evidence.
12. **Reader-2's C-3 falsifier cell "value.js `grammar.ts:71` handles it"** — KILLED as stated (ruling 6). It handles the posed case and swallows the complementary one; the cell was inherited from the challenge, not re-derived. The C-3 verdict itself survives untouched.
13. **Consolidated cite drift** (verdict-unaffecting, gate-breaking; every item line-checked): `split.rs` slice `:61` not `:62` · brackets `:58-59` not `:57-58` · clamp `:59` not `:60` · `banned` array `:29-32` not C §6's `:31-34` · VJ escape check `:71` not `:72` · C-7's "exactly one subdirectory" (`src/parse/parsers/` exists at depth 2) · C §0's `parsers/scan/**` gloss (only `decode.rs`/`number_simd.rs`/`quote_parity.rs` are modified there — `git status`, my run — which is precisely how the **committed** `balanced.rs` got skipped by every prior seat) · C-6's `proof:all` table lists 5 of **10** scripts (conclusion survives — I checked all 10) · O-15's PT-01 cites corrected per L §3 (label-drop at diagnostics-chunk `:14`, `console.error` at `:882` not `:881` — both re-verified here; the pinned row's substance holds).

## Superlatives — upheld, with this seat's re-measurements

1. **S-CONSERVATION** — `parts.join(delim) === text`, exactly, including on malformed input (six corpora re-run true; VJ's `splitTopLevel` false on all). **Qualified by the surrogate probe**: conservation is **code-unit-level, not well-formedness-level** (`["a","\uDE00b"].join("\uD83D")` === the original, with an ill-formed part) — state it as conservation of code units when it lifts into ALGEBRA.md. This is COMP-1/EQ-2 at split granularity, obtained by declining to be helpful, and it is the one thing the X·P algebra must not lose (C §6's KEEP, adopted).
2. **S-STACK** — flat loop, integer depth, no self-call; re-run at 10^6 nesting, no `RangeError`, no try/catch shield — the in-tree existence proof of parser-band binding debt 3 ("recursion bounded by construction, not by catch"; 128× PT-04's 7,761 ceiling). Honest bound kept: the probe proves scan stack behavior, not a split at depth. Cite as the G-11 pattern.
3. **S-PURITY** — the ONLY file in `src/parse` with an empty import set AND zero module-scope bindings (its pair `ansi.ts` carries the `process.env.NO_COLOR` probe) — O-8 satisfied vacuously; the anti-`PACKRAT_ARMED` in the same package that shipped the latch.
4. **S-CLAMP** — `Math.max(0, depth-1)` / `saturating_sub(1)` deliberately matched across lowerings; re-measured: one stray `)` permanently disables the VJ downstream (`')a,b'` → `[")a,b"]`) and does not disable the published module (`[")a","b"]`). On malformed input, the retired surface is the more total.
5. **S-BRACKETS** — the published module tracks `[]`; none of value.js's five scan sites does (re-measured, `'[a,b], .c'`). The second axis the head-to-head never posed. Together with S-CLAMP: two correctness axes favor the surface being retired — the wave inherits them as obligations on the successor, not as nostalgia.
6. **S-MIRROR** — a genuine two-language structural mirror (fast path / quote arms / skip / grouping / clamp / tail / docstring, row-by-row verified) + an 11-row 1:1 differential corpus **+ 4 rustdoc doctests** — the cheapest specimen for W2's AC-4 idiom, and the reason PT-SPLIT-2/8 are specification defects: the mirror worked; the original was flawed.
7. **S-SUCCESSOR** — `rust/parse_that/src/parsers/scan/balanced.rs`, 112 committed lines neither challenge read: separate `open`/`close`, `quotes`, `escape` + memchr2 skip loop, `terminators` — four of this roster's cures, already written, in-tree. The W2 `.g` seat wraps it; it does not design it.
8. **Method superlatives (the fleet, not the tree)**: L-D11's honest self-refutation retained as a null result; L §3's epoch-respecting corrections to the pinned O-15 (both re-verified exact here) — a correction offered upward at the correct altitude, moving no verb.

## MEASURE-AT-OPEN residue (for the W1 harness; no number below is a bar — parser-band HONESTY LAW)

| row | what re-runs | why it waits |
|---|---|---|
| R1 | L-D6's `text[i]` vs `charCodeAt` ratios (1.41×–1.47× two-byte; ~1.04× Latin-1) | N=1 unisolated box; **cure coupled to PT-SPLIT-1's domain narrowing** (killed #7) — measure only against the narrowed signature |
| R2 | L-D11's fast-path tax/benefit (±4–8%, sign flips) | recorded variance; re-run only under W1's interleaved three-leg cell (CC-095) if at all |
| R3 | C-8's guard-double-scan (1.298× / 0.841×) weighted by the in-tree 95:5 absent:present ratio | expected wash; only meaningful if the guard export survives PT-SPLIT-5, which it should not |
| R4 | the absent-delimiter fast path (131×–142× L-S3; ~12× C S-5) | the one number a successor must not regress; becomes a W1 bench leg |
| R5 | `split_balanced("é,a", 0xA9)` panic witness (PT-SPLIT-6) | source-certain by Rust spec (ruling 5); the executable witness needs one `cargo test` in the lawful W0-opened root — never in the read-only main checkout |
| R6 | the dual-target EQ-1 probe on `("a::b","::")` | runs the day the W2 `.g` harness exists; RED by construction until PT-SPLIT-1's narrowing lands |

## Closing verdict

**C §6's verdict — WRAP the contract, RETIRE the surface — is ADOPTED, with three corrections that change how it executes.** (1) The wrap target is not hypothetical: `balanced.rs` already carries the escape state, split bracket families, quote list, and terminator classes — W2 wraps a committed artifact. (2) The retirement mechanism as proposed is **unsound**: the census gate it leans on runs in no CI, silently sweeps nothing under a single-repo checkout, and is blind out-of-tree by construction — PT-SPLIT-7 must land in W4's release predicate before any `banned`-array deletion is trusted. (3) The severity landscape inverts the challenges' headline: nothing inside parse-that blocks anything (MAJOR, capped by the corpus's own zero-consumer census), while the two live correctness defects this corpus surfaced sit in the **downstream that re-authored the primitive worse** — value.js's parity-blind escape swallow and its bracket-blind scanners, both beneath the shipped `/css` surface, both X·V-owned, both flagged for relay because no X·P gate will ever witness them. The module itself remains what both challenges found despite themselves: the best-behaved 58 lines in the package — pure, flat, conservative, mirrored — whose conservation law and clamp semantics the successor algebra keeps, whose surface it retires, and whose type-level lessons (the `u8` the port dropped; the non-surrogate scalar neither doc reached) are now written where W2 will read them.

— end of apotheosis. Both challenge files and both reader returns remain immutable beside this record per E-1/E-3.
