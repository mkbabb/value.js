Served model: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited.

# fr-ImageUpload — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F (incredulity seat, not a transcriber) · **Date**: 2026-08-25 · fourier-analysis HEAD `cd26c65` · value.js branch `tranche-u`.
**Corpus**: three challenge axes (D-design 455 ln · C-consumption 404 ln · L-library 452 ln) against `web/src/components/visualization/ImageUpload.vue` (207 ln) + `composables/useImageUpload.ts` (106 ln), challenged by two hostile re-readers — **R1** (design-and-usage) and **R2** (library-and-code). Both re-reader returns were themselves attacked before adoption; every verdict-level R1↔R2 contradiction was re-derived against the tree with this seat's own instruments.
**This seat's own instruments (static + node only; zero browser probes — probe parsimony; product source READ-ONLY; this file the sole write)**: ImageUpload.vue whole (207 ln) · useImageUpload.ts whole (106 ln) · useImageOverlay.ts whole (**93 ln** — the L past-EOF cites re-confirmed dead) · workspace.ts:55-160 + reset() :412-426 · VisualizationView.vue:125-205, :255-275 · api.ts:50-62, :155-165, :255-296 · api-problem.ts:20-45 · lib/colors.ts:1-96 (rainbow = `STATIC`, frozen; `resolveVizColors()` re-resolves exactly 5 keys — rainbow NOT among them) · lib/defaults.ts:25-33 · router/index.ts:69 (`/w/:imageSlug?` — param change, no remount) · api/routers/images.py:61,99-100 (no `.svg`; 400 detail) · api/config.py:11 · **the PRM kill chain traced whole with my own reads**: fourier style.css:3 `@import "@mkbabb/glass-ui/styles"` → installed glass-ui **4.0.0** exports `./styles` = `dist/styles/index.css` → :165 `@import "./utilities.css"` → :19 `@import "./utilities/a11y-overrides.css"` → **a11y-overrides.css read verbatim**: `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important } … transition-property: opacity, color, background-color, border-color, box-shadow !important }` (+ the `[data-allow-motion]` absolute-override arm) · node read of installed glass-ui package.json (80 export keys; `./progress` `./toast` `./card` `./hover-card` `./hover-popover` all TRUE at 4.0.0) · greps re-run this seat: `store.error` → 7 hits/3 files incl. useWorkspaceLoader.ts:126,130; `abortInflight` → 4 call sites (:90,:140,:202,:270), `"uploadImage"` in NONE; `beginCompute()` → :243,:265,:287,:312 exactly; `image-file-input` in e2e/ → **15 hits / 6 files** (R2's denominator, not R1's 16); `dataTransfer|dragenter` in e2e/ → 0; `type="button"` in src/ → exactly 1 (ImageUpload.vue:94); `ls web/tsconfig*` → tsconfig.json only (**tsconfig.app.json does not exist**); `ConfiguratorLayer` → ContourSettings.vue:19/:190 · BasisSelector.vue:5/:120 (+ CoefficientsPanel per R1), ImageUpload absent · challenge-file falsifier passages read verbatim (D:40-58, D:228-252, C:70-80, C:195-205, L:36-48, L:202-232) · registry rows re-read at cited lines: fr-VisualizationView.md:23,:44,:47,:61,:69,:109,:110 · fr-ContourSettings.md:41,:72 · fr-ContourPreview.md rows 3/4/16/23/27/31 · fr-NotationPills.md:47 · fr-MorphShapePreview.md:9 (the type-button census AND the C-6/D-8 PRM kill chain, banked verbatim) · fr-GalleryDraftsSection.md:72 · fr-GalleryAdminBanner.md GAB-21.

## Scoreboard

**25 defect rows ADJUDICATED (1 BLOCKER · 10 MAJOR · 14 MINOR) + 9 FOLD-BY-REFERENCE to banked rows + 4 INFO · 15 killed claims · 8 UNPROVEN-NEEDS-LIVE.** Of the corpus's six filed BLOCKERs, **one survives** (B-1). Both re-readers were overruled at least once: R2's biggest confirmation (the PRM row) is KILLED against it; R1's biggest superlative (the FileReader lifecycle) is QUALIFIED against it.

Blocker-weight survivor: **B-1** — once an image exists, the panel's primary action (replace) is a bare `<div @click>` with no role, no name, no tabindex, no keydown; the real `<button>` is `v-else` and no longer renders. WCAG 2.1.1 + 4.1.2 Level A in the component's steady state.

## Ruled disagreements (this seat's actual work)

| # | Row | R1 | R2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **D:M-5 / C:C-10** (no PRM carve) | KILLED (KILL-2) | CONFIRMED MAJOR | **KILLED — R1 wins outright** | I traced the chain myself: style.css:3 → glass-ui 4.0.0 `./styles` → index.css:165 → utilities.css:19 → `utilities/a11y-overrides.css:6-16`, blanket `*:not([data-allow-motion])` with `!important` — `rainbow-slide` runs 0.01ms × 1 under PRM. R2's falsifier ("style.css read whole, 143 lines") never opened the imported package; D's own falsifier text (D:249-250) greps `web/src node_modules/tw-animate-css/dist` and omits the glass-ui package D cites elsewhere in the same document. Decisively: the SAME kill chain is already a **banked arbiter instrument** — fr-MorphShapePreview.md:9 records "the C-6/D-8 kill chain traced whole … a11y-overrides.css:6-16, PRM blanket read verbatim", and fr-ContourPreview row 23 already adjudicates the PRM interaction citing a11y-overrides.css:6-16. R2 confirmed a row the registry itself has twice killed. Residual survives as MISSED-4 (roster 24). |
| 2 | **D:U-3** (hover-lift not PRM-gated + BH relay) | KILLED (KILL-5) | CONFIRMED INFO, "legitimate relay" | **KILLED as filed — R1 wins** | (i) PRM leg dies with ruling 1 (the `translate` TRANSITION is clamped; the instant-1px-translate residual is banked verbatim at fr-ContourPreview row 23 → F.W1). (ii) The relay leg is a re-book of a **RETIRED ask**: fr-ContourPreview row 4 rules the cartoon-card upstream carry STALE with the producer's own Card.vue docblock endorsing consumer-written `cartoon-surface` — "Nothing to relay upstream; nothing to migrate." R2's "legitimate glass-ui BH-inbox relay" endorsement re-opens a closed ruling under REGISTRY-FIRST. |
| 3 | **S — FileReader lifecycle** | UPHELD ("no reader retained after load — :92 nulls it") | QUALIFIED (retained after load) | **QUALIFIED — R2 wins** | My read of useImageUpload.ts:82-84: `reader.onload` writes `preview.value` and does NOT null `activeReader`; the only nullers are onUnmounted:25 and clearPreview:92 — and :92 is inside `clearPreview`, unreachable on the dedup path (roster 12). Between a completed read and the next setPreview/clear/unmount, `preview.value` and `activeReader.result` hold the same data-URL string twice (~27 MB at the advertised ceiling). R1 answered the falsifier with a line from the wrong function. |
| 4 | **L-i1 cache step** (dedup re-upload) | key CHANGES `slug:<bounds>` → `slug:768` → MISS | key STABLE → HIT | **R2 wins on the mechanism; the provenance kill stands (both concur)** | useImageOverlay.ts is 93 lines — every L anchor :95+ is past EOF (provenance KILLED, both readers). The step: `resizeFromBounds` (:15-19) falls back to `store.contourSettings?.resize ?? 768`, and the module's own docstring (:13) states `max(w,h) == resize parameter` — so with settings unchanged since extraction (the common case) the fallback equals the bounds-derived number and the key is STABLE → the cache HITS the pre-regeneration bitmap, exactly when contour is nulled. R1's MISS arm holds only if the user changed `resize` in between. Outcome (stale overlay reachable) survives at corrected anchors → SS-13. |
| 5 | **L-M3/C-6 severity** (dedup-stranded preview) | MAJOR | MINOR per banked precedent | **MINOR — R2 wins; R1's MAJOR preserved in DISSENT** | fr-VisualizationView.md:23/:69 L-8: severity-follows-consequence — retention with marginal capability loss grades MINOR, the MAJOR already in dissent there. The stale-display half (regenerated thumbnail never shown) is intent-defeat of workspace.ts:119-121's own comment, but the pixels shown are the user's just-picked file — not a false state. |
| 6 | **C:C-11** (palette literals) | MAJOR incl. theming arm | MINOR, theming arm killed | **MINOR — R2 wins** | My colors.ts read: `rainbow` is `STATIC.rainbow`, a frozen `as const` literal; `resolveVizColors()` (:90-96) re-resolves exactly five keys — fourier/chebyshev/legendre/amber/green. The registry copy is exactly as theme-dead as the inlined one; R1's "participates in none of it" implies a reactivity the registry doesn't have. The DRY + grep-inventory-blindness claim survives; the same block carries roster 5's contrast failure. |
| 7 | **e2e denominator** | 16 call sites / 6 files | 15 / 6 | **15 / 6** — my grep. Both axes' originals (L "nine specs", C "three specs") stay killed. |
| 8 | **D:U-2 baseline** | Verified at `git show v7.0.0` (names its tag) | 7.0.0 sentence struck; adopted pin 4.0.0; FMD-11 says budget from 8.0.0 | **Conclusion UPHELD at every candidate baseline; the quarrel is F.W1's, not this file's** | ImageUpload imports zero glass-ui symbols (my read: vue, the store, the composable, api, 3 lucide icons) — break surface NIL at 4.0.0, 7.0.0, or 8.0.0. R1's row is the only axis row that names and shows its tag (BASELINE LAW compliant); R2's strike is right that the ADJUDICATION baseline is the installed 4.0.0 pin and that F.W1 must budget from the published artifact (fr-ContourPreview row 29). Both recorded; INFO. |
| 9 | **D:m-4 provenance** | proof cell: "tsconfig.app.json … (re-read)" | C-16's tsconfig.app.json = fabricated file | **R2's kill CONFIRMED — and R1 inherited the fabrication** | My `ls web/tsconfig*`: tsconfig.json + tsconfig.tsbuildinfo only. R1's proof cell repeats the C axis's nonexistent filename with a "(re-read)" stamp — a citation-inheritance event, recorded in DISSENT. Substance survives via the real web/tsconfig.json (strict, no noUnusedLocals). |

## Adjudicated defect roster

Dispositions are **ADJUDICATED**, never VERIFIED — X·F specs are forming; routings are agglomeration input (M-25). Line anchors are fourier HEAD `cd26c65`; re-resolve before curing (D-19).

| # | id | sev | claim (compressed) · proof anchor · disposition |
|---|---|---|---|
| 1 | **B-1** = D:B-3 · C:C-3 · L:L-M6 | **BLOCKER** | Populated-state replace control is a bare `<div @click>` — no role/tabindex/name/keydown; the real `<button>` (:92-94) is `v-else` and gone; input :120-127 is `class="hidden"`; the alternate canvas path is gated `!hasImage && !hasData` (VisualizationView.vue:198) i.e. unavailable exactly when an image exists. ImageUpload.vue:70-76. `grep -c aria-` → 0. WCAG 2.1.1/4.1.2 Level A. → **F.W3/W4** (upload cluster; with roster 10's chassis migration). |
| 2 | D:B-2 · C:C-1 | MAJOR | Strip advertises SVG (:109); API refuses it with 400 (images.py:61 no `.svg`, :99-100) and accepts 7 formats the copy omits; client gate passes it (useImageUpload.ts:4 `svg`, :9 `image/svg+xml` short-circuit; `accept="image/*"` :124) to a convincing local preview. The "silent" aggravator is dead (K-1: the 400 IS toasted). Enforcement sibling is banked (fold row F2). → **F.W3/W4**. |
| 3 | D:M-2 · C:C-4 · L:L-M7 | MAJOR | Sole busy affordance gated on `store.computing` (:115), which `uploadImage` never sets — my grep: `beginCompute()` at :243/:265/:287/:312 only; uploadImage writes `loading` (:112/:131). No `:disabled`, no aria-busy; second click re-opens the picker mid-flight. → **F.W3/W4** (cure = re-point the v-if — see superlative S-7: the flag it should read is the well-built one). |
| 4 | D:M-3 · C:C-9 | MAJOR | 47 lines of hand-rolled indeterminate progress, zero a11y channel (`grep -c aria-` → 0; no role/status/sr-only). `./progress` (variant `gradient`, `indeterminate`) is exported at the **installed 4.0.0 pin** — my node read. NOT an F.W1 item (K-12). → **F.W3/W4** (adopt `glass-ui/progress`; also closes roster 24). |
| 5 | D:M-4 | MAJOR | Rainbow bar fails WCAG 1.4.11: ALL SEVEN hardcoded stops < 3:1 against the `--muted` track (R1's extension adopted; R2's arithmetic correction adopted — #34d399 = 1.74:1 not 1.79:1; verdict unmoved). Zero `var()` in the gradient block (:148-157); theme-invariant literals. Dark-mode arm → SS-13. → **F.W3/W4** (dies with roster 4's Progress adoption; literals die with roster 13). |
| 6 | L:L-M4 | MAJOR | Scoped `.ring-dashed` writes bare `var(--tw-ring-inset)` (:179-180); Tailwind 4.3.1 registers `--tw-ring-inset` with NO initial-value (R2's compile probe + R1's dist extraction, concordant) → both ring customs IACVT → fall to registered `0 0 #0000` → box-shadow paints three transparent layers (NOT `none` — both readers' correction of L's mechanism adopted); unlayered scoped rule beats `@layer utilities`, so `ring-2 ring-primary ring-offset-2 ring-offset-card` (:55) are inert; only `outline: 2px dashed` renders. Kills D:m-12 (K-3). Pixel → SS-13. → **F.W3/W4**. |
| 7 | L:L-B1 · C:C-2(b) | MAJOR (demoted from BLOCKER) | Async callback through `(file: File) => void` (:14); invoked bare in non-async handlers (:38/:71 — Vue's callWithAsyncErrorHandling never engages); rejection floats (`grep unhandledrejection` → 0); workspace.ts:116-118 destroys contour/epicycles/bases BEFORE the await, catch (:127-129) never restores, rethrows. Split identity: preview shows file B, store still names A. R2's load-bearing addition ADOPTED: the pipeline does NOT self-heal — ContourSettings.vue:151-168's auto-compute watcher keys on `imageMeta` identity, untouched on failure. Demotion: the notification premise died (K-1). → **F.W3/W4** (upload cluster). |
| 8 | R2-missed-4 (NEW instance of the banked abort-identity class) | MAJOR | `"uploadImage"` absent from every `abortInflight` key list (my grep: :90/:140/:202/:270); `reset()` (:412-426) aborts nothing. Start an upload, navigate away or "Start fresh" (VisualizationView.vue:170 `store.reset(); router.push('/visualize')`): on resolve, workspace.ts:123-126 re-assigns imageSlug/imageMeta, `router.push('/w/<slug>')` and `_saveDraftNow()` — **hijacks an explicit user navigation and revives a reset session**. Class banked (GAB-21, AA-8/27, FR-AUL-3, R2-N7, intake R6-8); this instance registry-swept unbanked, booked NEW per the GAB-21 precedent. → **F.W4**. |
| 9 | R1-MISSED-1 | MAJOR | Every SUCCESSFUL upload blanks/collapses the card: the :20-23 watcher fires on the new slug → `clearPreview()` discards an already-decoded data URL; `hasPreview()` stays true via `imageMeta` (:124, next line) → :65 re-resolves to an **unloaded network URL**; the `<img>` reserves no box (:63-69) → card collapses and re-expands a round-trip later, reflowing the whole controls column; nothing remounts (router/index.ts:69 `/w/:imageSlug?` — my read; param change reuses the view). The discard is unforced; cure = hold preview until the thumbnail's `load`, or reserve from ImageMeta. Structural chain ADJUDICATED; paint receipt → SS-13 (R2's fence honoured). → **F.W3/W4**. |
| 10 | R1-MISSED-2 | MAJOR | ImageUpload is the LAST bespoke panel in a stack whose mount host declares the migration complete (VisualizationView.vue:188-192, my read: "the control panels (each now a ConfiguratorLayer) … bespoke cartoon-card panel backgrounds retire"); its three stack-mates comply (ContourSettings:190, BasisSelector:120, CoefficientsPanel); :44-48 hand-rolls the exact label/sub semantic ConfiguratorLayer exposes as props. Interlock with fr-ContourPreview row 16: adoption must CARRY the `<h3>` (ConfiguratorLayer supplies no heading) or the route drops to ZERO headings. → **F.W3** (ConfiguratorLayer adoption; carries rosters 14, 18). |
| 11 | R2-missed-1 (process) | MAJOR | **The corpus never swept the adjudicated registry**: zero `registry/adjudicated|fr-*` citations across all three axes (my grep of the challenge files → 0/0/0); ≥7 headline rows are re-books; two of six filed BLOCKERs were graded against banked demotions (fr-VisualizationView L-1; fr-ContourSettings B-2). Inflates the ledger, mis-grades severity. → **NO-WAVE-OWNER** (procedural — binds SS-3/SS-4 spec authoring and every future challenge lane: REGISTRY-FIRST is a precondition, not a courtesy). |
| 12 | L:L-M3 · C:C-6 | MINOR (R1's MAJOR in DISSENT) | Dedup re-upload: sha256-stable slug never fires the :20-23 watcher → multi-MB data URL stranded AND preferred over the REGENERATED thumbnail (:65), defeating workspace.ts:119-121's stated intent; compounded by `Cache-Control: public, max-age=86400` on an un-busted URL (api.ts:292-294). Severity per banked L-8 precedent (ruling 5). → **F.W3/W4** (cure = roster 22's objectURL + a load-keyed swap, with roster 9). |
| 13 | C:C-11 | MINOR | Six hexes byte-duplicate `lib/colors.ts` STATIC.rainbow (:150-156 ≡ colors.ts:13-16), invisible to any import-specifier-keyed F.W2 inventory (zero `@mkbabb`/value.js/keyframes imports in the SFC). Theming arm KILLED (ruling 6). → **F.W2 rider** (inventory-blindness note) + dies with roster 5. |
| 14 | D:m-8 | MINOR | The :44 `<h3>` is the reachable /w tree's ONLY heading (my corroboration: fr-ContourPreview row 16 banks the census); outline opens at level 3; axe page-has-heading-one fails. → **F.W4** (rides roster 10 with the carry-the-h3 interlock). |
| 15 | D:m-3 | MINOR | Dead `group` class ×2 (:53 AND :95 — R1's count adopted), zero `group-*` variants; consequence real at :53: cursor-pointer preview region with `bg-black/0` and no hover signal that the image is replaceable. → **F.W3/W4** hygiene. |
| 16 | D:m-4 · L:L-m1 · C:C-16 | MINOR | `computed` imported, never used (:2); `hasPreview` an un-memoised fn typed `boolean|string|null` (:25); no gate catches it — **web/tsconfig.json** (the real file; C-16's tsconfig.app.json provenance KILLED, K-6) has strict but not noUnusedLocals; no lint script. → **F.W3/W4** hygiene. |
| 17 | D:m-9 · C:C-17 | MINOR | `alt="Uploaded image"` constant while `ImageMeta.original_name` (required, types.ts:56) is in scope — the only human handle in a slug-addressed workspace. → **F.W3/W4**. |
| 18 | D:m-10 | MINOR | Decorative "— source input" span INSIDE the `<h3>` (:44-48) pollutes the accessible name — the exact label/sub pair ConfiguratorLayer expresses as props. → rides roster 10. |
| 19 | D:m-1 · D:m-2 | MINOR | Arithmetic: 6px bar in an 8px gutter overhanging the 12px type column by 4px/side (:135-138 vs :38); `mb-3` (12px) > card `py-2` (8px) — inner gap 1.5× the outer. → **F.W3/W4** (dies with roster 4). |
| 20 | D:m-7 · L:L-m6 | MINOR | No width/height/aspect-ratio/decoding on the `<img>` (:63-69) ⇒ cold-load CLS; the :132 comment advertises "no layout shift" for the bar 65 lines below. Happy-path manifestation is roster 9 (worse). → **F.W3/W4**. |
| 21 | C:C-15 | MINOR | No props/emits/expose; reads the global Pinia singleton; unmountable in a second context, untestable without a live store (vitest absent) — contrast every sibling in the same template. → **F.W3/W4** (input to roster 10). |
| 22 | R2-missed-2 | MINOR | `URL.createObjectURL`/`revokeObjectURL` is the primitive; `createObjectURL` appears NOWHERE in the product tree (my corroboration of R2's grep). readAsDataURL costs a main-thread base64 of ≤10 MB, holds ~1.37× as a string, decodes twice; `clearPreview` can only drop a reference — the advertised release contract doesn't exist. One line each way is the ACTUAL cure for rosters 12/23 and half of 9. → **F.W3/W4**. |
| 23 | R2-missed-3 | MINOR | `activeReader` never nulled after a SUCCESSFUL read (:82-84 — my read, ruling 3): preview + `activeReader.result` hold the same string twice until the next setPreview/clear/unmount; on the dedup path clearPreview is unreachable, so the double copy is what persists. → **F.W3/W4** (dies with roster 22). |
| 24 | R1-MISSED-4 | MINOR | The PRM residual that survives K-2 and is sharper than the claim both axes lost: under PRM the bar freezes at its 100% frame (blanket clamp, ruling 1) while the `<Transition>` still fades it in (opacity IS allow-listed) — a motionless decorative gradient with no name is the PRM+AT user's entire busy signal, composed with rosters 3 and 4. Correct routing = a non-motion channel (roster 4's Progress), NOT a new PRM carve (already present upstream). → **F.W3/W4**. |
| 25 | C:C-13 · C:C-14 | MINOR | Dead client trio (my re-grep: zero consumers of `computeSha256`/`checkImageHash`/`imageUrl` outside api.ts); `checkImageHash` additionally raw-`fetch`es outside the parametric core with a bare `throw`. → **F.W4** (delete or consume). |
| 26 | C:C-12 | INFO | `thumbnailUrl` is a template literal (api.ts:292-294) interpolated at :65 — the GET traverses none of coreFetch (no session token, no abort registry entry, no 429 retry, no ApiProblem, no ETag); `get_image_thumbnail` takes no auth dependency; R2's corrected consumer count adopted (5 un-clienting `<img src>` sites incl. useImageOverlay.ts:69). Identity: the incomplete **R6-8 join** — FOLD to the intake row, this instance recorded. → **F.W5-W8**. |
| 27 | L:L-i2 · D §7 | INFO | 15 e2e entries / 6 spec files all via `setInputFiles`; zero drag/reject/oversize/failure/keyboard coverage (my greps — ruling 7's denominator). → **F.W9/W10**. |
| 28 | D:U-2 | INFO | Tri-package-uplift break surface NIL: zero glass-ui imports; exposure = 3 lucide symbols + the cartoon-card shim. Ruling 8's baseline note attached (F.W1 budgets from the published artifact). → **F.W1** input. |
| 29 | R1-MISSED-5 | INFO | Positive census: :94 is the tree's ONLY `type="button"` (banked instrument, fr-MorphShapePreview.md:9; my grep concurs: 1 site) — alongside the sole `@error` handler, the propagation-candidate pattern. → **NO-WAVE-OWNER** (spec-authoring input: propagate, don't merely preserve). |

### Folds by reference (confirmed defects; identity lives in the banked row — never re-booked)

| id | folds to | note |
|---|---|---|
| F1 · L:L-B2 / C:C-7 / C:C-8 / D:M-1 | **fr-VisualizationView.md:44 L-1/MAJ-1** (MAJOR; BLOCKER demoted there) | Double `globalDrop` (overlay :147 → root :141), self-abort via the `uploadImage` key, double `invalidateInFlightComputation`. R1's K-9 ownership rescope ADOPTED: in the overlay branch ImageUpload's `handleDrop` never fires — the defect is VisualizationView's, fires viewport-wide, and the banked row already states the overlay→root mechanism. Residual ImageUpload-owned: two `useImageUpload()` instances (:12 / VisualizationView:41) own two dragCounters on one bubbling stream (D:U-4's framing). Cure = one `.stop`; count → SS-13 (banked probe). |
| F2 · D:M-7/L:L-M5/C:C-19 + D:M-8/L:L-M2/C:C-18 | **fr-VisualizationView.md:61 D-12** (MAJOR) | Rejected-file swallow (no `else`, :36-39/:69-72) + unenforced "≤ 10 MB" (no `file.size`; api/config.py:11 env-overridable) — the banked row already names ImageUpload.vue:109 and the composable. The one error class with genuinely zero channel (store never reached ⇒ K-1's toast can't cover it); remedy (`Toaster` app-wide) already mounted. |
| F3 · D:M-6/L:L-M1/C:C-5 | **fr-VisualizationView.md:109 M6** (MINOR) | Input never reset; banked row states verbatim "Sibling: handleFileSelect never resets at all. The reset belongs in finally." In-tree counter-example VisualizationView.vue:135 (my read). |
| F4 · L:L-i3 | **fr-ContourSettings.md:41 B-2** (BLOCKER, banked) | useWorkspaceLoader's pre-flush watcher nulls the field the retry banner renders from; ordering CLOSED there by seat reads. Paint receipt rides the banked probe. |
| F5 · R2-missed-7 | **fr-ContourSettings.md:72 M-15** (MAJOR, banked) | The 400's actionable `detail` is discarded (api-problem.ts:41 `title ?? statusText` — my read; all three upload-400s collapse to "Bad Request"); HTTP/2 `statusText:""` → `store.error=""` → falsy → NO toast (workspace.ts:128 `??` misses `""`). Recorded as the ImageUpload instance; transport arm rides the banked fr-ContourSettings:134 probe. |
| F6 · R1-MISSED-3 | **fr-NotationPills.md:47 FR-NP-8** (MAJOR class row) | No `@reference "tailwindcss"` in the scoped block; 4 `color-mix()` declarations ship unguarded — the banked row already names ImageUpload with the exact count. BOUNDS the corpus's "theme-correct by construction" superlative (S-4). → F.W4 via the banked row. |
| F7 · R2-missed-6 | **fr-VisualizationView.md:47 L-7** (MAJOR, banked) — increment | On a FIRST upload, :158's loading arm unmounts ImageUpload one tick after `setPreview` starts; its own onUnmounted aborts the read — the instant-preview affordance only ever paints on a REPLACE. Consequence-increment recorded on the banked row. |
| F8 · D:U-3 residual | **fr-ContourPreview row 23** (MINOR, banked → F.W1) | The instant-1px-translate-under-PRM residual — banked verbatim; ruling 2. |
| F9 · fr-VisualizationView.md:110 M7 | (banked MINOR) | No `dataTransfer.types` guard in the shared composable — banked from the host's side; NO ImageUpload axis found it (R2's observation, corroborated). Listed so the fold is bidirectional. |

## Killed-claims register

| id | claim | refutation (this seat's ruling) |
|---|---|---|
| K-1 · D:B-1 + C:C-2(a) | "error rendered nowhere" / "unrenderable" (both BLOCKERs) | useWorkspaceLoader.ts:124-132 toasts EXACTLY the replace case (`if (err && store.imageSlug)`), instantiated at VisualizationView.vue:50, `<Toaster/>` app-wide. My grep: `store.error` → 7 hits/3 files. Both axes' quoted greps are misreported (D:53-54 names 2 files; C's scoped grep misses two files INSIDE its own scope — both passages read verbatim this seat). L self-corrected in-file (:39-43) and shipped the surviving form; D and C shipped the refuted form as BLOCKERs. B-1's stale-preview half survives inside roster 7. |
| K-2 · D:M-5 + C:C-10 (and R2's confirmation) | infinite animation, no PRM carve | Ruling 1. The blanket reaches the element; the falsifier D wrote was satisfied by the package D omitted from its grep; R2 repeated the scoping error at this seat's level. Twice-banked kill chain (fr-MorphShapePreview.md:9, fr-ContourPreview row 23). Residual → roster 24. |
| K-3 · D:m-12 | "the rule works today … double ring draws" | Wrong proposition probed (property NAMES vs initial-VALUE). `--tw-ring-inset` is the one member registered without initial-value; bare `var()` → IACVT → registered initials `0 0 #0000` → nothing paints; the four utilities are inert, nothing double-draws. Concordant R1 dist-extraction + R2 compile probe; roster 6 stands. |
| K-4 · L:L-i1/L-B1 provenance | useImageOverlay.ts:95-118 anchors | File is 93 lines (my wc). All cited anchors past EOF. Mechanism corrected per ruling 4 (R2's stable-key derivation, via the module's own :13 docstring). Outcome survives at :6/:9/:31-34/:36-46 → SS-13. |
| K-5 · L:L-B1 mechanism (2) | "canvas keeps rendering the old image" | `drawImageOverlay` early-returns on `!bounds` (:77-78) and contour was nulled — the canvas renders NOTHING. Worse for the user, wrong as stated. |
| K-6 · C:C-16 provenance | "web/tsconfig.app.json … quoted whole" | The file does not exist (my ls). Substance survives via web/tsconfig.json. R1's D:m-4 cell inherited the fabricated name — DISSENT item 2. |
| K-7 · C:C-11 theming arm | "the inlined copy participates in none of [the dark-mode re-resolution]" | `rainbow` is STATIC and never re-resolved (my colors.ts read); the registry copy participates in nothing either. Ruling 6. |
| K-8 · L:S-1/C:S-1 falsifier + R1's S-2 form | "no reader retained after load (:92 nulls it)" | :92 is inside unreachable-on-dedup `clearPreview`; `onload` doesn't null. Ruling 3; roster 23. |
| K-9 · C:C-7 mechanism / L:L-B2 attribution | "every drop on the card runs the target handler then the ancestor's — two uploads, ImageUpload's fault" | In the near-certain overlay branch ImageUpload's handleDrop never runs; the double-fire is VisualizationView's overlay→root chain, viewport-wide — banked at fr-VisualizationView L-1. Ownership rescoped (F1); residual re-scoped to the two-instance dragCounter. |
| K-10 · D:U-1 count | "1 of the 14 shim sites" | Quoted the shim's own STALE comment as a census; live = 21 application occurrences/14 files, staleness itself banked (fr-ContourPreview row 27). Re-books a banked staleness AND propagates the stale number. |
| K-11 · D:U-3 (as filed) + R2's endorsement | hover-lift not PRM-gated + fresh BH relay | Ruling 2. PRM leg false (K-2's chain); residual banked (F8); relay ask RETIRED at fr-ContourPreview row 4. |
| K-12 · D:M-7/M-3 uplift framing (D:U-1 routing) | "./toast cures … glass-ui 7 exports ./toast"; Progress routed to F.W1 | Version dependency refuted: installed 4.0.0 exports `./toast` AND `./progress` AND `./card` (my node read, 80 keys); fourier consumes ./toast TODAY. Cures belong to F.W3/W4 component work; C:C-9's framing is the correct one and is folded. |
| K-13 · D:S-2 receipt | focus ring "byte-identical" to style.css:136-143 | Two declarations vs three (`border-radius: inherit` absent) — and the missing third is arguably a live gap: `.source-strip` is `rounded-md`, its outline squares off. Substance upheld (S-5). |
| K-14 · D:M-4 arithmetic | #34d399 = 1.79:1 vs track | 1.74:1 (R2's recompute; R1 concurs at 1.74). Isolated slip; verdict unmoved. Recorded to prevent re-litigation on the number. |
| K-15 · D:U-2's 7.0.0-absence sentence (R2's strike) | "./hover-card ./hover-popover absent at 7.0.0" as adjudication input | Both ARE present at the installed 4.0.0 (my node read); the 7.0.0 fact may be true at its tag but is not this adjudication's baseline, and F.W1 budgets from the published artifact (fr-ContourPreview row 29; FMD-11's 8.0.0 note). Conclusion of U-2 unaffected (ruling 8). |

## Superlatives (with provenance)

- **S-1 UPHELD — the tree's only broken-image posture.** `@error` exactly once in web/src (both readers re-ran; corroborated from the far side by fr-GalleryDraftsSection.md:72). Complete, not gestural: imgError ref, ImageOff fallback, TWO reset sites — cannot latch across images. Sharpened by roster 26: this handler is the ENTIRE error surface for the un-cliented thumbnail GET. Propagate outward (roster 29).
- **S-2 QUALIFIED (ruling 3) — the FileReader lifecycle is careful, not hole-free.** Three abort sites correctly `readyState`-guarded; but the reader IS retained after load (roster 23) and the read is aborted-before-use on every first upload (F7). R1's UPHELD form is overruled; the craftsmanship is real, the superlative overclaimed.
- **S-3 UPHELD — counter-based drag tracking is the right algorithm and says so** (:28-29 comment, clamp at `<= 0`, documented Safari re-arm). Banked corroboration fr-VisualizationView:99.
- **S-4 UPHELD BUT BOUNDED — the empty-state strip is 100% token-driven** (:190-206, zero literals — the counter-example that makes the seven hexes forty lines up indefensible, same author, same file). BOUND: per F6/FR-NP-8 the four color-mix declarations ship unguarded — "theme-correct by construction" overstates the mechanism.
- **S-5 UPHELD — reversible-decision provenance at the point of use** (:87-91, :186-189 record what was REJECTED and why; still true at HEAD — strip still v-else/flat, dashed reserved for the preview branch, canvas placeholder still the hero). Its tragedy stands: the hero it defers to IS the overlay that erases the deferral (F1). K-13's "byte-identical" correction attached.
- **S-6 UPHELD, denominator corrected — a data-testid with its rationale written in the consuming spec** (two hidden inputs on one route, genuinely). **15 call sites / 6 spec files** (ruling 7).
- **S-7 UPHELD (R1's own find, adopted) — the Transition is smooth BECAUSE the store flag is a depth counter** (workspace.ts:60-68, my read: reason in the comment; one fade per chained run across 4 call sites). Every axis cited :62-68 only to convict the wiring (roster 3 — correct); none noticed the flag it IS wired to is the well-built one. The fix is re-pointing the v-if, not rebuilding the affordance.
- **S-8 UPHELD (R2's) — the Safari empty-MIME fallback is real, scoped, documented** (:7-12, consulted only after `type` fails; bounded by server magic-byte re-validation). The `svg` membership is roster 2's problem; the mechanism is right.
- **S-9 UPHELD (R2's) — Goldilocks, measured**: 207 lines with the 106-line lifecycle extracted; contrast the 486-line host.

## UNPROVEN-NEEDS-LIVE (SS-13 inputs)

1. Drop hit-testing branch + POST count per gesture — run the **banked** probe (fr-VisualizationView.md:146 item 2), not a new one; pair with the D-30/D-31 flicker probe (:155).
2. `.ring-dashed` computed box-shadow on the preview wrapper while dragging — prediction: three transparent layers (`0 0 #0000` ×3), NOT `none`; only the dashed outline visible (roster 6).
3. Whether the z-50 overlay visibly occludes the panel's five drag treatments — static chain complete (`--z-overlay: 50`; `translate: 0` stacking context; nothing above `--z-controls`); paint is live.
4. Roster 9's paint: blank frame/collapse magnitude when `:src` swaps from data URL to unloaded `thumbnailUrl` (measure the reflow of the column below).
5. Stale overlay bitmap after a dedup re-upload of a re-processed asset — at the CORRECTED anchors (useImageOverlay.ts:15-19/:31-34/:41-46; ruling 4's stable-key form), plus a canvas read.
6. Retry-banner paint under a forced 503 — rides the banked fr-ContourSettings:134 item 1 probe (F4/F5 share it).
7. HTTP/2 deployed-transport arm: `statusText === ""` ⇒ zero-toast for a rejected upload (F5) — same probe, run once for both.
8. D:M-4 dark-mode arm: the seven theme-invariant stops against the dark track/card tokens (this record's derivations are light-theme only).

## DISSENT — preserved, not ruled away

1. **R1's MAJOR on the dedup-stranded preview (roster 12)** — held on the ground that defeating the store's own documented regeneration intent is capability loss, not waste. Overruled by the banked L-8 precedent (severity-follows-consequence); preserved here as the second dissent on the same family — if SS-13 item 5 shows a visibly wrong (pre-EXIF-transpose) bitmap presented as current, the family re-grades.
2. **Citation-inheritance event (evidence about the fleet).** The C axis fabricated `web/tsconfig.app.json` and claimed to quote it whole; R1's D:m-4 proof cell repeated the filename with a "(re-read)" stamp; the file does not exist (this seat's `ls`). Same class as the ColorSpaceSelector DISSENT item 1: a stale/false cell repeated without re-derivation is detectable, and detection is the point. R2 caught it; R1 propagated it while otherwise out-instrumenting R1's counterpart rows.
3. **R2's PRM confirmation (K-2)** — recorded as the sharper lesson because R2's return is otherwise the corpus's provenance benchmark: R2 read fourier's style.css whole and re-verified eight sibling PRM sites byte-exact, and still missed the imported package a banked instrument had already traced. Scope-of-the-cascade is part of any CSS claim; a falsifier over `web/src` alone cannot kill (or confirm) a cascade fact.
4. **The e2e denominator drift** (L "nine", C "three", R1 "16", R2 "15"; live 15/6) — four numbers for one grep across five documents. Recorded as the cheapest possible argument for pasting the command beside the count (the ColorSpaceSelector L-9.1 scope law, again).

## Closing verdict

**ADJUDICATED.** One BLOCKER survives of six filed (B-1, keyboard/AT-unreachable replace); 10 MAJOR + 13 MINOR booked with F-wave routings, 9 confirmed defects FOLD to banked rows rather than re-book, 15 claims killed — among them one entire axis-confirmed row (PRM) killed against re-reader-2 with a twice-banked chain, and one superlative (FileReader) qualified against re-reader-1. The component remains the tree's positive outlier on error posture and token discipline while carrying its worst defect in its most-used state. Routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
