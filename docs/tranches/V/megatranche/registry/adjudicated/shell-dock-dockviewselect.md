# shell-dock-dockviewselect — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · served model: `claude-fable-5` (Fable 5)

**Seat**: incredulity adjudicator over two hostile re-readers — reader-1 (design-and-usage) · reader-2 (library-and-code) — who each re-read the full challenge corpus. Model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **HEAD**: `c654824e` · branch `tranche-u` · **Subject**: `demo/shell/dock/DockViewSelect.vue` (162 lines) + its co-owner `demo/shell/dock/composables/useDockAdminMode.ts` (77 lines, read whole).
**Corpus (r2 governs)**: `audit/components/shell-dock-dockviewselect/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md` (all three read whole by this seat — 1005/847/1216 lines) + committed evidence `pi-D1-focus-ring-absent.png`, `pi-D1-focus-ring-control-sibling.png`, `pi-D3-selection-zero-delta.png`, `probe/probe-dvs*.mjs`, `probe/dock-focus.png`.
**Method (L-18)**: both reader returns were attacked before either was adopted. Every verdict-level disagreement (C:D-7 kill-vs-unproven · D:D-6 kill-vs-unproven · D:D-8 confirm-vs-unproven · L:L-18's module-drag cell inside reader-1's confirmed row · D:D-9 severity · ViewEntry severity · L:L-22 severity · update:open severity) was re-derived against the tree with this seat's own instruments. Two reader evidence cells are corrected by this seat (reader-1's gold token cells; reader-2's "no escape hatch" cell) and one reader-2 missed row is demoted under a standing Dock-adj ruling (R-5).
**Arbiter's own instruments (all static + node, zero browser — probe parsimony)**: full reads of DockViewSelect.vue (162) + useDockAdminMode.ts (77); targeted reads of Dock.vue:30-75/:160-180/:255-280, router/index.ts (whole), viewSchema.ts (`icon: Component` :75; all fourteen `accentHueShift` values; `isViewId` :236), useViewManager.ts:10-22/:33-50/:68-82 (shim comment, `router.isReady`, the `id === currentView` push guard), usePaletteWiring.ts:140-175 (watch (2) `immediate:true` vs watch (4) bare), useAtmosphereBoot.ts:53-57 (`import type`), useViewAccents.ts:1-45/:120-170 (the T-10 excise header + the `[safeAccentCss, surfaceL, shift]` writer watch), tsconfig.demo.json:38-52, foundation.css:50-110 (`@theme` gold block)/:181-200 (`@property --accent-view`)/:205-220 (Fraunces stack)/:258-266+:524-526 (`--focus-ring-color: var(--accent-live)` re-points), utils.css:150-205 (both gold/ramp recipes), animations.css:104-136 (vj-morph) via MQ §1, focus-ring.css (whole), palettes-ramp.ts:38-90 (both floors); **producer bytes**: sizing.css (`--dock-ring: var(--focus-ring-shadow)`; `--dock-touch-target/--touch-target: 2.75rem`), scale-paper.css (`--focus-ring-shadow` = a two-shadow LIST; `--gold` family in oklch; `--opacity-icon-muted: 0.8`), dock/styles/index.css (the `:focus-visible { box-shadow: var(--dock-ring); outline: none }` block + `[data-ring-yield]`), dock/styles/controls/triggers.css (trigger ink recipe, no type role), **touch-floor.css whole** (the coarse-pointer `::after` seat, no `pointer-events:none`), `_shared/menu.css` (`.glass-menu-row[data-highlighted]` rule EXISTS; `min-block-size: max(2rem, var(--touch-target, 2.75rem))`), menuRowClass-Nh7CtMON.js (whole — no checked branch; gutter `pl-7 pr-2` vs `px-2`), select-BcBAyLXA.js prop cells (`placeholder:{}` · `surface:{default:"glass"}` · `sideOffset:{default:0}` · `hideIndicator`), dock.js:22 (`SelectTrigger as ce` from **reka-ui**) + `line-clamp` census over dock dist = **0**, base-misc.css `.gold-shimmer` (gradient + `metal-shimmer-sweep var(--duration-shimmer) linear infinite`), scheme-motion.css (`--duration-panel: 0.55s`, `--duration-shimmer: 5s`); **reka bytes**: Select/utils.js `OPEN_KEYS` (open-only), SelectTrigger.js:43 (`useTypeahead()` **no callback**)/:71 (`dir` bound onto the button)/:99-100 (OPEN_KEYS → `handleOpen()` only), shared/useTypeahead.js (no-callback branch ends in `.focus()`), Select `activedescendant` census = **0**, SelectValue.js (`?.textContent ?? ""` → `.filter(Boolean)` → `props.placeholder`), SelectContentImpl.js:146-150 (`focusSelectedItem` on open) + :222-226 (first-valid-item fallback), SelectItem.js:109/:117 (`data-highlighted` driven by `onFocus`), shared/useDirection.js (ConfigProvider default `ref("ltr")`); **censuses re-run**: `RouterLink|router-link` in demo = 0 · `SelectSeparator` consumers = 0 · `hide-indicator` = 3 sites (ColorSpaceSelector + this file ×2) · `__admin_toggle__` = 2 · `ConfigProvider` in demo = 0 · `font-display` in shell+color-session = 17 lines / 8 files · root-barrel `@mkbabb/glass-ui` importers outside demo/ui = **18** · `gold-shimmer` sites (Dock seal :277, DVS :80/:134/:135, ProfileSection :96); **node contrast run** over both gold families (table in the missed-rows section).
**Registry cross-reads (anti-rename)**: `adjudicated/Dock.md` — **DELTA-4 (C-3-WIDENED · C-29)** = the blank nav label, BUILD with the union clause · **L-19** = the admin gold latch, BUILD G-C/G-D, **R-5** striking "two disagreeing predicates" as operative mechanism · **L-6** = SESSION_PORT_KEY, BUILD G-G, the cycle/bytes framings STRUCK · **DELTA-5** = the breakpoint-predicate family (G-L) · **DELTA-10 D2-16** = `hide-indicator` on BOTH blocks, BUILD G-N · **DELTA-11 D2-07** = RTL truth G-Q (wordmark/arrows — a *different* RTL defect than this corpus's) · **DELTA-13** D3-09 (the `index.ts ⇄ Dock.vue` cycle, BUILD) + C-26 (the mutex-fork BH relay) + L-36/R-21 (the 18 root-barrel imports, SPLIT) · G-D's gate text keys gold on `--dock-ring` (amendment flagged below); `adjudicated/ColorSpaceSelector.md` L-14 = the tsconfig `paths` drift (ONE identity with this corpus's L-16); `adjudicated/shell-dock-mobilemenudropdown.md` M-A (the cascade-layer law — the identity this seat's gold-token correction lands in) + K-1 (touch-floor precedent) + D-04/L-5/C-6 (the admin-gold treatment family, design loop); CARRY-CUT-LEDGER: CC-037 (X-W3 owns the unguarded `meta:{admin}` P0) · CC-047 (X-W4: SelectTrigger title register, producer variant REFUSED) · CC-078/CC-079/CC-083 (X-W8 owns demo/ui + SCCs + the Dock.md consumption) · CC-098 (X-W10 canon).
**Motion quarantine (M-xx law)**: `codex-provenance/motion-quarantine.md` carries **no row for this component** (grep over DockViewSelect/vj-morph/gold-shimmer/accent-view: §1 only); **no FALSE M-row is carried here**. Both served guards verified at §1 (animations.css `animation-iteration-count: 1 !important`; glass a11y-overrides `*:not([data-allow-motion])`). The one new motion observation (the 5s shimmer) is filed as INFO-not-PRM, consistent with the M-03/M-17 pattern (motion governed centrally by design).

## Scoreboard

**38 rows ADJUDICATED-CONFIRMED (3 BLOCKER · 11 MAJOR · 12 MINOR · 12 INFO) · 17 claims KILLED (14 axis kills sustained or issued + 3 reader-cell corrections by this seat) · 11 rows the axes MISSED, booked by the readers — ratified with two corrected evidence cells and one demotion.** Dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave.

Blocker-weight survivors: **DVS-1** (the inline `--dock-ring` colour-into-shadow-token write deletes the keyboard focus indicator on the product's primary navigation, and buys nothing — glass-ui has no resting ring), **DVS-2** (blank trigger + no-selected-option on 7 of 14 routes — ONE identity with Dock-adj DELTA-4, already BUILD'd), **DVS-3** (the raise-only admin latch leaks gold authorisation ink onto anonymous visitors — ONE identity with Dock-adj L-19, already BUILD'd). DVS-1 is this corpus's genuinely new blocker: it is in no prior register, and Dock-adj G-D's own gate text still treats `--dock-ring` as a gold *carrier* — that gate needs the amendment recorded below.

## Ruled disagreements (the seat's actual work)

| id | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C:D-7** arrow-keys-navigate-from-closed-trigger | KILLED from reka source | UNPROVEN-NEEDS-LIVE ("whether reka cycles the value is live-only") | **KILLED — reader-1 wins; statically decidable** | My reka read: `OPEN_KEYS = [" ","Enter","ArrowUp","ArrowDown"]` reaches only `handleOpen()` (SelectTrigger.js:99-100); the trigger's typeahead is `useTypeahead()` with **no callback** (:43), whose no-callback branch ends in `newItem.ref.focus()` — no `onValueChange` on any closed-trigger key path, and closed content mounts no items to focus. C's own uncontended probe (two ArrowDowns from Browse, `sel:"true"` stays on Browse) corroborates. The MAJOR's motivating hazard does not exist; C's first-pass "CONFIRMED-once" was the contended browser. |
| **D:D-6** 33.3px mobile trigger | KILLED via touch-floor.css | UNPROVEN-NEEDS-LIVE (rendered heights live-only) | **KILLED — reader-1 wins; the sibling K-1 precedent extends** | touch-floor.css read whole by this seat: `@media (pointer: coarse)` gives `:is(.dock-trigger, .dock-select-trigger, .dock-dropdown-trigger)::after` a centred `max(100%, var(--dock-touch-target, 2.75rem))` seat with **no** `pointer-events:none` (the decorative `::before` in the same file has it) — the operable target is ≥44×44 under any coarse pointer, statically. D measured the border box and inferred the target; PROPORTION §5.7 (which D itself quotes) is the law the producer implements. Only the visible-box aesthetics remain live-only, and they carry no floor. |
| **D:D-8** the `ltr` island in an `rtl` document | CONFIRMED MAJOR from source | UNPROVEN ("mechanism plausible; ancestor walk live-only") | **CONFIRMED MAJOR — reader-1 wins; the mechanism is source-certain** | useDirection.js: `injectConfigProviderContext({ dir: ref("ltr") })` → `dir?.value \|\| context.dir?.value \|\| "ltr"`; SelectTrigger.js:71 binds `dir: unref(rootContext)?.dir.value` **onto the button element**; DockViewSelect.vue:51-56 passes no `:dir`; `ConfigProvider` in demo = 0 (my grep). The trigger therefore carries `dir="ltr"` inside any rtl document by construction — the ancestor walk was corroboration, never the load-bearing cell. Canon: VISUAL-CONSTITUTION §6.1 (chrome/navigation follow the document). Cure at the right altitude: ONE app-level reka ConfigProvider, never a per-instance `:dir`. Distinct identity from Dock-adj DELTA-11 (wordmark bidi/arrow glyphs) — accretes a new arm to the same G-Q family, not a re-booking. |
| **L:L-18** "drags 31 modules to read one boolean" | carried inside its confirmed C:D-6 row | KILLED (Dock.vue already imports the module; delta = 0) | **KILL SUSTAINED — and it was already law** | Dock.vue:37 injects the same key from the same module and is this component's sole parent; the closure is paid before the child evaluates. Decisive: Dock-adj L-6 already **struck** the bundle-bytes framing ("the cost is OWNERSHIP and mountability/testability, never bytes — no close may cite"). Reader-1's confirmed row carries an uncitable cell; the row itself (dual predicate, wrong-direction edge, the `!` assert) survives at full weight and FOLDs to L-6/G-G. |
| **D:D-9** translucent menu surface | MAJOR | MINOR | **MINOR** | The producer rung exists and was never chosen (`surface:{default:"glass"}`, my read of the shipped props) — a one-word cure on a file X-W8 already edits; the canon cite (§2 specimen well; :19 "Glass earns its blur") makes the cure canon-hard but the measured harm (per-route substrate variance under the rows) is bounded and its legibility half sits in the UNPROVEN register (the trigger-label contrast row). The information-destroying half of the menu's failure is D2-16's, booked separately. |
| **ViewEntry erasure** (D:D-16/C:D-12/L:L-7/L-19) | MINOR | MAJOR | **MINOR** | Real erasure (my reads: `icon: unknown` + open index signature vs `icon: Component` at viewSchema.ts:75; the compiler proof L-19 ran is sound), but latent — no observed failure, `<component :is>` unchecked either way, and the systemic risk is already carried at MAJOR by C:D-13 (zero tests). Cure `{ id: ViewId } & PaneConfig` needs no cast — reader-2's own kill of L-7's cast-causation sub-claim (fresh object literals get implicit index signatures) is adopted, which matters because a cure written to "get past the signature" would keep the casts. |
| **L:L-22** zero link elements | INFO ("transposition against a shipped, owner-ruled shape") | MAJOR | **INFO at this component · named at the formation boundary at product scope** | The census is real (my grep: 0 RouterLink/router-link in demo) and the consequence set (nothing middle-clickable/⌘-clickable/copy-link-able, no link graph) is product-truth. But no component-local cure exists that is not a transposition of an owner-shaped composition (the Q5/T-10/R1/P4-R4 comment corpus shows the Select was ruled INTO this shape repeatedly), so severity attaches to the owning surface: recorded INFO here, and the anchor-desert question is flagged whole for the formation boundary (X-W5's one-route-one-scene family is the natural venue). Reader-2's MAJOR is sustained only at that scope. |
| **update:open triple declaration** (D:D-17/C:D-8/L:L-10) | INFO | MINOR | **MINOR** | Dead emit declaration + hand-wiring where `v-model:open` is the idiom + a misnamed `update:modelValue` channel (no `modelValue` prop exists — the value arrives as `currentView`): three spellings of one binding on the component's public contract. Contract-surface hygiene lands MINOR by house precedent. |
| **reader-2 missed-2** — seed predicate (`startsWith("admin-")`) vs watcher predicate (`adminViews.includes`) | (not filed) | MINOR missed row | **DEMOTED to INFO accretion under Dock-adj R-5** | The split is real (my read: useDockAdminMode.ts:32 vs :55-59) — but Dock-adj R-5 already **ruled it latent, not operative**, on O's cold-deep-link cell, and struck "two predicates" as citable mechanism for the latch. Reader-2's new consequence (an authed admin whose dock re-mounts on atmosphere/blob seeds `false` and loses the admin list) is a genuinely distinct failure direction, but its trigger set (dock re-mount / HMR re-setup / synchronous initial route) has no demonstrated production path — App mounts the dock once. Booked as an INFO accretion to the L-19 identity (the derivation cure kills both directions); NOT a new row, and NOT citable as the latch's mechanism. |
| **reader-1 missed-1/-6** — gold contrast + "metal token split" | filed on `#D4AF37`/`#F5E6A3` as the painting tokens | (not filed) | **CONFIRMED MAJOR — with the token cells CORRECTED by this seat** | Reader-1 read foundation.css:104-105 as live overrides. They are bytes, but they sit inside the Tailwind **`@theme` block** (my read of :96-105 context) — emitted into a cascade layer — while glass-ui's token `:root` rules arrive **unlayered** via the plain `@import`s at :56-57; unlayered beats layered, the exact M-A law the MobileMenuDropdown adjudication wrote. The empirical decider already existed in the corpus and neither reader connected it: **L-20's live cell measured the icon at `oklch(0.751 0.147 84.2)` = the producer `--gold`, and `goldToken: light-dark(oklch(0.751…), oklch(0.784…))`** — the producer family paints; the demo's hexes are (on this evidence) dead code that looks alive. The contrast row **survives a fortiori under both families** — my node run: producer gold Y=0.418 → 1.98:1 vs a `#FAEFE9`-class panel / 2.24:1 vs white (glyph, 3:1 floor); sweep stops gold-light 1.23:1 · gold 1.98:1 · gold-dark 3.63:1 — **no stop reaches 4.5:1**; the demo hexes give 1.86/1.11/3.63. Reader-1's "spends most of its cycle near 1.1:1" is retired as over-precise (the sweep is spatial, not temporal-uniform); the durable claim is *max over all stops < 4.5:1 on any plausible light panel*. The "split gradient authority" mechanism is REFUTED as stated and re-booked as the cascade-dead-override INFO row below. |

## Verdict register — adjudicated defect roster (id · severity · claim · proof · terminal disposition)

Line anchors are HEAD `c654824e`; re-resolve before any cure (D-19). Folds preserve original ids for life; accretions travel to their identities, never re-book them.

- **DVS-1 (D:D-1 ≡ C:D-15) — BLOCKER.** DockViewSelect.vue:70 writes a bare `<color>` into `--dock-ring` in **both** ternary arms; the producer declares it a shadow-list token (`--dock-ring: var(--focus-ring-shadow)`, sizing.css) consumed at exactly two `:focus-visible` box-shadows in all of glass-ui (my grep) — `box-shadow: oklch(…)` is invalid-at-computed-value-time → initial `none`, with `outline: none` already set in the same producer block. Net: **no focus indicator of any kind on the control the first Tab reaches**, every route, both schemes, masked only in the forced-colors matrix. The comment's "ring seam" (:60-65) does not exist — glass-ui 7.0.0 has **no resting ring**; the line buys nothing. Committed π: `pi-D1-focus-ring-absent.png` vs the control sibling; born-RED probe `probe/probe-dvs4.mjs`. The genuine `<color>` seam exists and the demo already uses it (`--focus-ring-color: var(--accent-live)`, foundation.css:264/:526), so a view-hued ring is a one-line consumer move at the declared type. Sharpenings this seat adds: (a) `[data-ring-yield]` exists in the dock CSS but only *suppresses* rings — reader-2's "no escape hatch" cell corrected, verdict unchanged; (b) **Dock-adj G-D's gate text keys admin gold on "`--dock-ring` resolving gold" — after this cure that probe arm must key on `.gold-shimmer-icon` alone; amendment flagged to the wave author.** **Disposition: X-W8 (MT-DOCK-LAYERS-1 — the wave already edits this file at G-C/G-D/G-N) as a NEW arm: delete the `--dock-ring` write; any per-view focus hue rides `--focus-ring-color`; the family law travels — a consumer may set a producer token only at the type the producer declared.**
- **DVS-2 (L:L-1/L-13 ≡ C:D-3 ≡ D:D-2) — BLOCKER, FOLD → Dock-adj DELTA-4 (BUILD, G-C/G-D + the union clause) → X-W8.** The trigger renders `""` and the listbox reports no selected option on 7 of 14 routes: `:model-value="currentView"` (14-valued, router-owned) vs `viewEntries` (7-valued, hand-partitioned, useDockAdminMode.ts:34-39); reka's SelectValue resolves an unmatched value to `?.textContent ?? ""` → `.filter(Boolean)` → `placeholder` default `""` (my read); `<SelectValue v-if="isDesktop" />` (:87) passes none. Tracked witness: `shots/safari-desktop-light/atmosphere.png` (gold glyph, no label, Login pill). Dock-adj DELTA-4 already BUILD'd this WITH the correct cure ("viewEntries is UNIONED unconditionally with the current view" — group derivation alone does not cure the anon-on-admin-route case). Accretions this corpus adds to that identity: the placeholder-refusal ruling (a placeholder fixes `triggerText` and leaves `anySelected:false` untouched — L-13); reader-2's missed icon/label owner-split row (below) proving the interim label repair is local; the killed activedescendant sub-claim must NOT travel (see kills).
- **DVS-3 (L:L-20 ≡ C:D-2) — BLOCKER, FOLD → Dock-adj L-19 (BUILD, G-C/G-D) → X-W8.** `isAdminMode` is a raise-only ref (:32 seed; :55-59 watch with no else, no auth consult; the only false-setters are auth-gated or need a true→false transition an anonymous visitor cannot produce); `adminViews` includes the public `atmosphere`/`blob`; consumed for gold at :70/:80. Verified whole by this seat; live cells and the five-step [false,true,TRUE,TRUE,TRUE] latch are Dock-adj's own G-D evidence. **R-5 governs**: the derivation cure kills it; "two disagreeing predicates" stays uncitable as mechanism. Accretion: reader-2's re-mount direction (ruled above, INFO).
- **DVS-4 (C:D-1 ≡ L:L-2 ≡ D:D-2-mech) — MAJOR, FOLD → the G-C derivation (Dock-adj L-19's cure surface) → X-W8.** Three hand-maintained enumerations disagree on "admin": router meta on exactly 5 (index.ts:30-34; atmosphere/blob bare at :28-29), the dock's 7+7 literals (useDockAdminMode.ts:26-27), and the schema putting atmosphere/blob on the primary 40° fan (280/320 at :177/:186 vs 0 for all five admin views — all fourteen values re-read by this seat). VISUAL-CONSTITUTION :58 names atmosphere/blob first-class member routes. The schema carries no grouping field — that missing column is why the dock re-enumerated; the derivation (one `group`/`audience` field, both halves computed) is the cure of record. Accretion: the fourth unguarded enumeration site — the bare `'palettes'` literal at :116 (missed row, below).
- **DVS-5 (C:D-14 ≡ L:L-3 + L:L-17) — MAJOR, FOLD → CC-037 (X-W3 — the K-25 route-closure P0).** `meta:{admin:true}` has zero readers (router registers only `installDocumentTitle`, my read — the file's own comment even calls itself "the canonical navigation-guard home"); the sole client eviction is a **non-immediate** watch (usePaletteWiring.ts:166-171, verified against the `immediate:true` watch twenty lines above) that cannot fire on a cold deep-link; the admin panes render whole to anonymous visitors and this component's `v-if` is the de-facto access control. Correctly routed to the router owner, not patched here. L-17's predicate note accretes.
- **DVS-6 (D:D-3-narrowed ≡ C:D-4-narrowed + D:D-20) — MAJOR, FOLD → Dock-adj DELTA-10 D2-16 (BUILD, G-N) → X-W8, with the narrowing as a binding accretion.** `hide-indicator` on every row (:97/:127) deletes the producer's sole selection affordance (menuRowClass has no checked branch; the gutter is the whole rung — my read), and the SFC comment's substitute channel is the FOCUS channel (`data-highlighted` driven by `onFocus`, SelectItem.js:109/:117) — "where am I" and "where is the cursor" collapse onto one signal that leaves the current row on the first arrow key. **The accretion the wave must carry: the D-3/C-D-4 "zero pixels at open" headline is DEAD (see kills) — reka seeds focus onto the selected row at open (`focusSelectedItem`, SelectContentImpl.js:146-150) and `menu.css` paints it; the cure target is the CONFLATION (VISUAL-CONSTITUTION :84 focus-distinct-from-selection), not a zero.** Second accretion: o14's "legend stays dead" oracle asserts the ABSENCE of an indicator inside options (C:D-13's mutation 3) — the suite currently PINS this defect and G-N must flip that assert, not merely add one. D-20's chromatic misdirection (the Palettes ramp reads as the selected row 13 routes out of 14) folds here as the compounding cell.
- **DVS-7 (D:D-5) — MAJOR → X-W10 (the closed type matrix, CC-098), mechanism note to X-W4/CC-047.** Fraunces on the trigger and every option: `text-small font-display` at :69/:91; `.font-display` resolves Fraunces by construction (foundation.css:218 + the :209-211 intent comment, my read) against VISUAL-CONSTITUTION :75's closed row ("control or label, **including dropdown options** — Plus Jakarta Sans, non-bold") — and the file's :99-109 comment fights for the law's WEIGHT clause while breaking its FAMILY clause. Scope corrected by this seat: 17 sites / 8 files across shell+color-session (D said 13) — a dock-wide voice, so the cure is a register family row, not a two-line patch. Mechanism (missed row, ratified below): the bare reka trigger carries NO producer type role (triggers.css sets geometry/colour only), so deleting `font-display` needs a consumer-composed type role — exactly CC-047's shape (the producer variant was REFUSED; the title goes through the type register in the trigger slot).
- **DVS-8 (D:D-4 ≡ C:D-17) — MAJOR, FOLD → Dock-adj DELTA-5/G-L (the breakpoint-predicate family) → X-W8, with the AX arm as its own accretion.** One `v-if="isDesktop"` (:87; Dock.vue:71 `useMediaQuery("(min-width: 1024px)")` — the exact width-only predicate DELTA-5 indicts) deletes the nav's identity below 1024px, which 200% zoom triggers as a device class; and because `SelectValue` is the node reka exposes the combobox value through, the v-if removes the **semantics**, not just pixels — the mobile AX node carries no value (structure source-certain; the CDP cell stays in UNPROVEN). Cure: mount the node always, hide pixels responsively at the trigger root. DELTA-4's route-parametrised label gate arm (C-29) covers the desktop half; the mobile mount-always arm accretes to G-L.
- **DVS-9 (D:D-8) — MAJOR (ruled above) → X-W8 as a NEW G-Q-family arm.** The whole widget is a `direction: ltr` island inside rtl documents, by reka's ConfigProvider default; no `:dir` anywhere. Cure: one app-level ConfigProvider bound to the document direction — the per-instance `:dir` anti-pattern is refused in the booking itself. RTL matrices are tracked product modes; DELTA-11's G-Q gains this arm without losing its own (distinct defects, one gate family).
- **DVS-10 (C:D-6 ≡ L:L-4) — MAJOR, FOLD → Dock-adj L-6 (BUILD, G-G port move) → X-W8.** The child re-injects SESSION_PORT_KEY (:8/:35) and gates the admin row on `pm.isAdminAuthenticated` ALONE (:122) while its parent computes the option list on `isAdminMode && isAdminAuthenticated` — two predicates for one menu, and the disagreement state is exactly DVS-2/DVS-3's. The five-file shell→palettes edge census is L-6's (re-confirmed twice at Dock-adj r3). **The module-weight cells (31 modules / "closure 33→2") are KILLED and were already struck at L-6 — no close may cite them.** L's whole-menu-model cure detail (rows + optional adminRow) also retires the `!` assert (C:D-12's crash arm).
- **DVS-11 (D:D-10 ≡ C:D-9 ≡ L:L-8) — MAJOR → X-W8 accretion (the wave's useDockAdminMode carve); the anchor-grammar question rides the formation boundary with L-22.** A command wears `role="option"`: `__admin_toggle__` duplicated as bare literals in two files (census = 2, mine), cast unvalidated into `router.push` (`switchView(id as ViewId)` :72) while `isViewId` (viewSchema.ts:236) sits unused on every write path; the row's `aria-selected` is permanently false; the label flips part of speech between modes; the mode switch is destructive to location (enter → always admin-users, leave → always picker). Correction adopted (reader-1): `useViewManager.ts:72-73` guards `id === currentView` so re-selection cannot push duplicate history — that sub-fault does not travel. Minimum cure at this boundary: shared constant + `if (!isViewId(id)) return;`; the real cure (a command is not an option) falls out of any anchor-grammar ruling.
- **DVS-12 (C:D-13) — MAJOR → X-W1 candidate re-gate (the AAP-34 precedent), with DELTA-4's C-29 arm already claiming the label assertion.** Zero tests target this component's behaviour (greps re-run by this seat: no test/e2e file names DockViewSelect/useDockAdminMode/viewEntries; every e2e touch is an anchor via the static aria-label); sibling dock modules have unit tests, so it is an omission, not a convention. The four named mutations stay reasoned-not-executed (UNPROVEN #7) — but mutation 3's discovery (o14 pins the ABSENCE of an indicator) is CONFIRMED from the spec text and folded into DVS-6's gate obligation.
- **DVS-13 (missed-1, reader-1; token cells corrected by this seat) — MAJOR → the convergent design loop (the MMD-adj D-04/D2-06 admin-treatment family) + the X-W10 contrast law.** The gold admin ink is the one chromatic exception in this file with NO contrast guard, and no candidate gold clears the floors on light panels: glyph ~1.9-2.2:1 against a 3:1 non-text floor; the `.gold-shimmer` sweep's best stop ~3.6-4.1:1 against a 4.5:1 text floor — under BOTH token families (producer oklch, which L-20's cell shows painting; and the demo's @theme hexes) — while the two sibling chromatic exceptions run through guards this repo wrote (useViewAccents ≥3:1; palettes-ramp 4.5:1 with a comment naming this exact menu). Population is not admin-only: the latch (DVS-3) puts the ~2:1 gold glyph on every anonymous visitor who deep-links `/#/atmosphere`. Dark scheme unaffected. Exact rendered ratios on the glass panel: UNPROVEN #1/#8.
- **DVS-14 (missed-2, reader-1) — MAJOR → X-W4 (name/state/value class) + the same design loop.** Admin mode is a colour-only state: `aria-label="Select view"` static across modes (:68); the only mode-dependent bindings in the file are two colour writes (:70/:80); no name change, no text, no ARIA state — against VISUAL-CONSTITUTION :83 ("states are never color-only"). Nuance added by this seat: the option set does swap textually between modes, but that signal is unannounced and requires opening the menu; at the closed trigger the entire authorisation signal is one 24px glyph tint — and DVS-1 has already deleted the other colour carrier (the ring never paints), so the "two signals" the design intended are in fact one, at ~2:1.
- **DVS-15 (D:D-9) — MINOR (ruled above) → X-W8 rider (one-word `surface` choice on a file the wave edits; canon VISUAL-CONSTITUTION §2/:19).**
- **DVS-16 (D:D-16 ≡ C:D-12 ≡ L:L-7/L-19) — MINOR (ruled above) → X-W8 rider on the carve: `ViewEntry` → `{ id: ViewId } & PaneConfig`, casts deleted (they were never forced — the kill travels), `currentIcon: Component`.**
- **DVS-17 (D:D-11 ≡ C:D-18 ≡ L:L-14) — MINOR → X-W8 rider; the zero-consumer SelectSeparator census accretes to CC-078.** A bare `<div>` separator (:123) inside `role="group"` inside `role="listbox"`, differing from the producer recipe in all three enumerated properties, while `SelectSeparator` sits re-exported and consumed by nobody (my census: 0); plus the danglings — `SelectGroup` with no `SelectLabel` emits an `aria-labelledby` that resolves to nothing, and the listbox itself is nameless (C:D-11). The lapse is proven two files away (MobileMenuDropdown uses the producer separator).
- **DVS-18 (C:D-16) — MINOR → X-W8 rider + a coordination close.** `[&>span]:line-clamp-none` (:69) cancels nothing: `DockTrigger for="select"` mounts **reka's bare SelectTrigger** (dock.js:22, my read) and no line-clamp exists anywhere in the dock dist (census 0, mine); the clamp lives only in glass-ui's styled SelectTrigger this file never mounts. The class is dead, the :57-59 comment is stale, and the five-tranche `clampLabel` ask **closes as "producer changed shape"** — withdrawn, not re-filed. The model correction of the corpus: two axes read the comment as evidence; the pass that read the shipped bytes won.
- **DVS-19 (D:D-17 ≡ C:D-8 ≡ L:L-10) — MINOR (ruled above) → X-W8 rider: delete the dead emit line, use `v-model:open`, rename the misnamed `update:modelValue` channel at the same touch.**
- **DVS-20 (L:L-5 + L:L-9 + L:L-15-as-structure) — MINOR, FOLD → CC-078 (demo/ui extirpation) and Dock-adj DELTA-13 D3-09 (the `index.ts ⇄ Dock.vue` cycle — ALREADY BUILD'd; not re-booked).** Two paths to one design system eight lines apart; 19 pure-forward barrels; the shipped `./select` subpath unused. L-15's severity is dead (see kills) — the structural argument needs no number.
- **DVS-21 (L:L-6, narrowed) — MINOR, FOLD → the W-HYGIENE alias-residue family.** The self-declared source-compat type re-export at useViewManager.ts:13-19 preserves compatibility with an import path W43/RF-15 deleted (vite.config.ts records it); 6 of 9 importers take the shim, including this component's composable (:5). The cost sub-claim is killed (below); what remains is a stale-rationale dual path, mechanical to retire.
- **DVS-22 (L:L-16) — MINOR, FOLD → the ColorSpaceSelector-adj L-14 identity (W-HYGIENE config parity) — accretion only, ONE identity, never re-booked.** All facts re-verified by this seat at HEAD: three `paths` targets nonexistent, the "CLOSED 8-key set" comment false by one, `./css`/`./value` unmapped. The born-RED generator-parity command already rides that fold.
- **DVS-23 (D:D-13) — MINOR → X-W8 rider.** The 12rem floor is 2.04× the widest row's ink; the producer's own floor is 8rem; the :89 comment asserts a need the rendering refutes and names a token (`--menu-min-w`) the producer does not use. PROPORTION :5/:73. Dies with the recomposition; the comment dies under the comment-truth law either way.
- **DVS-24 (D:D-15) — MINOR → X-W8 rider.** Icon and label on two clocks: `mode="out-in"` guarantees the old glyph fully leaves while `SelectValue` swaps instantly — the whole leave duration pairs the OLD glyph with the NEW word. The negatives re-verified: the vj-morph family is global, opacity/transform-only, PRM-neutralised by both served guards.
- **DVS-25 (missed-1, reader-2) — MINOR → X-W8 rider (comment correction mandatory) + UNPROVEN #9 for the visible magnitude.** The scoped `--accent-view` transition (:159-161, `var(--duration-panel)` = 0.55s, my read of scheme-motion.css) retargets on every COLOUR tick, not just view switches: the writer watch's first source is `safeAccentCss` — the live picked colour, rAF-coalesced during drags by the file's own header (useViewAccents.ts:135-150 + header, my reads) — so a drag restarts a 0.55s interpolation on the trigger subtree at drag cadence, and the inline `--dock-ring` write (until DVS-1 deletes it) is rewritten at the same cadence. The :152-158 comment frames the transition as the view-switch sweep only and declares the per-frame tax dead — dead at `:root`, alive at higher frequency on this element. All three axes sampled only view switches.
- **DVS-26 (missed-3, reader-2) — MINOR, accretion → Dock-adj DELTA-4.** The icon channel and label channel have different owners: Dock.vue:170 feeds `currentConfig.icon` (ungated, always correct) while the label is gated on option membership — the shipped failure frame shows a correct glyph beside an empty label, so the widget demonstrably holds the data it fails to display. Cure-sizing consequence for the union clause: the interim label repair is local to this file; no placeholder, no router needed.
- **DVS-27 (C:D-19) — INFO → X-W8 rider under the comment-truth law (Dock-adj D2-09's form).** The sweep is live but interpolates as a rectangular oklab chord (`syntax: "<color>"`, foundation.css:192-198, my read), not the hue arc the :155-158 comment claims; the guard covers endpoints only. Cure: animate an `<angle>` and compose, or correct the comment.
- **DVS-28 (D:D-19) — INFO → the design loop (W19 owns this witness).** The Palettes coordinate renders as a dark two-tone wash because the 4.5:1 text guard pins L≈0.47 — witness here, cause in palettes-ramp/useViewAccents; canon calls it "pastel-rainbow". Rendered values: UNPROVEN.
- **DVS-29 (L:L-12) — INFO → X-W8 rider (the schema doc block is corrected when the group field lands).** viewSchema's accentHueShift doc describes machinery useViewAccents' own header declares DEAD (both read verbatim by this seat).
- **DVS-30 (L:L-22) — INFO here (ruled above); the product-scope anchor question is flagged whole for the formation boundary.**
- **DVS-31 (L:L-24) — INFO, FOLD → Dock-adj DELTA-13 C-26 (the BH relay for the mutex fork rides the fond — already BUILD'd).** Reader-2's independent leak audit of the fork (benign close-during-swap path; teardown on every re-open + unmount) is adopted as corroboration.
- **DVS-32 (missed-3, reader-1) — INFO.** The `py-1.5` half of the per-instance override (:96/:126) is doubly dead: it duplicates the producer's value verbatim AND the row's block size is pinned by `min-block-size: max(2rem, var(--touch-target))` with flex centering (menu.css, my read) — only `px-2.5` (10px vs the producer's 8px) is live. Deleting both lines is safer than either axis implied. Rides DVS-6's edit.
- **DVS-33 (missed-4, reader-1) — INFO.** The bare `'palettes'` string at :116 is a FOURTH hand-maintained view-enumeration site that L-2's census missed — and it is the sole Dock-side enforcement of the constitution's two-coordinate law (:23): rename the view id and the coordinate vanishes with no type error and no test. Accretes to DVS-4's derivation.
- **DVS-34 (missed-5, reader-1) — INFO.** The mechanism connecting DVS-7 to DVS-18: the bare reka trigger receives NO producer type role, so the demo hand-supplied one and hand-supplied it wrong. This is why the Fraunces cure is a register/CC-047 row, not a class deletion.
- **DVS-35 (missed-6, reader-1 — RE-DERIVED by this seat) — INFO → the M-A cascade-layer identity (MMD-adj) + a BH relay question.** The demo's `@theme` gold overrides (foundation.css:104-105, `#D4AF37`/`#F5E6A3`) sit in a losing cascade layer against glass-ui's unlayered token `:root` — on L-20's measured cell the producer gold paints and the demo's hexes are dead bytes. Reader-1's "split gradient authority" mechanism does not survive; the ownership question (who mints the product's gold, and did anyone intend the #D4AF37 brand value?) does. Which token paints in every scheme/route: UNPROVEN #8.
- **DVS-36 (missed-7, reader-1) — INFO, explicitly NOT a PRM defect (no M-row; both served guards neutralise it — MQ §1).** `.gold-shimmer` is a 5s infinite animation on a nav label at default motion settings; the canon's five-second rule is scoped to ambient Aurora/Blob motion and does not reach a nav label — a canon-scope note for the owner. The ProfileSection:96 twin is already adjudicated at MMD-adj D-04 (where the inline colour makes the same animation invisible — zero-output compositor work).
- **DVS-37 (missed-4, reader-2) — INFO.** Dead public surface: `toggleAdminMode` is declared/returned but Dock.vue:44 destructures only `{ isAdminMode, viewEntries, onViewChange }` (my read) — the function is reachable solely through the sentinel string branch, so deleting the sentinel without re-exposing it orphans it; `usePopupMutex` declares/returns `current` with zero consumers (Dock.vue:66 destructures `{ isAnyOpen, popupModel }`). Rides DVS-11's carve.
- **DVS-38 (reader-2 missed-2, demoted — ruled above) — INFO accretion → Dock-adj L-19, R-5 language binding.**

## Killed-claims register (uncitable; refutations travel with the ids)

- **K-1 · D:D-6** (33.3px mobile trigger, MAJOR) — the producer ships the exact invisible seat on the exact selector: touch-floor.css `::after` at `max(100%, 2.75rem)` under `(pointer: coarse)`, no `pointer-events:none` (read whole by this seat). Same kill as MMD-adj K-1; challenge-C independently declined to flag it. The visible-box measurement survives only as rhythm trivia.
- **K-2 · C:D-7** (arrow keys on the closed trigger commit `router.push`, MAJOR) — reka's OPEN_KEYS open, never mutate; the trigger typeahead has no callback and ends in `.focus()`; C's own clean probe left the value unmoved. The architectural half (navigation should be anchors) survives inside DVS-30's boundary question — the hazard that carried the MAJOR does not exist. Reader-2's UNPROVEN listing is overruled: statically decidable.
- **K-3 · C:D-4 (mechanism half)** — "no select-option highlighted style anywhere in producer or consumer CSS" is false: `.glass-menu-row[data-highlighted]:not([data-disabled]) { background: var(--menu-row-bg); … }` in `_shared/menu.css` (read by this seat), reached via the styles index; C's grep was scoped to the compiled SFC surface, and C's own pass-2 measurement (`bg: oklab(0.9156 … / 0.52)`) contradicts its pass-1 grep. The SFC comment's "highlighted-on-open row" is accurate as written.
- **K-4 · D:D-3 (headline)** — "the open menu marks the current destination with zero pixels" is over-claimed: reka seeds focus onto the selected item at open (`focusSelectedItem`), which sets `data-highlighted`, which menu.css paints; on blank-label routes the fallback seeds the first valid row. The durable defect is the channel CONFLATION (DVS-6), and a cure aimed at "nothing paints" would aim at the wrong channel. D's zero-delta table describes a later state (pointer-open, cursor off the list) — that residue is UNPROVEN #2.
- **K-5 · D:D-12 row 1 / D:D-18's truncation row** — `[&>span]:line-clamp-none` fights nothing: the dock mounts reka's bare SelectTrigger (dock.js:22); `line-clamp` census over the dock dist = 0 (mine). The five-tranche `clampLabel` ask is MOOT for this call site and closes as producer-changed-shape (DVS-18) — it must not be re-filed.
- **K-6 · L:L-13 / C:D-5 (activedescendant sub-claims)** — reka's Select does not use the activedescendant pattern at all (census 0 over Select/*.js, mine); it moves real DOM focus. A null attribute is the expected value; keyboard navigation is seeded by real focus, not "from nothing". The blank-label and aria-selected halves of those rows stand inside DVS-2.
- **K-7 · C:D-5 (WCAG 2.5.3 Level-A citation + MAJOR)** — the visible "Home" is the combobox's VALUE, not its label; C's own AX dump shows name/role/value all present on desktop. Carrying a false Level-A citation would poison the a11y ledger; the voice-control friction survives as usability advice inside DVS-8's cure (name composition).
- **K-8 · D:D-14** (menu overlaps the reserved band, MINOR) — the cited laws govern the dock being clipped and scene displacement, not an overlay's proximity; the mechanism is the producer's `sideOffset:{default:0}` shared by every dock menu (verified in the shipped props). Downgraded to a producer/dock-register INFO; the 11.4px cell stays live-only.
- **K-9 · D:D-7 (MAJOR + the delete-the-mechanism cure)** — the cure re-litigates owner rulings recorded at the site (:46-47 "R1 SURVIVES"; useViewAccents' T-10 excise kept exactly this one chromatic point), and the constitutional hook fails on its own text (:178 constrains LABELS; the label here is neutral, only the glyph carries hue). The dark-mode near-white collapse survives as a MINOR perceptual observation for the design loop — magnitudes UNPROVEN #5.
- **K-10 · L:L-15 (the 66-module/224KB severity)** — the root barrel is imported directly by 18 demo files outside demo/ui (my count; identical to Dock-adj DELTA-13 L-36's census), so this barrel's routing adds zero modules and zero bytes to the graph; production tree-shakes `export *` besides. Matches Dock-adj R-21's severity bound ("never a shipped-bytes claim"). The correct-specifier point survives inside DVS-20.
- **K-11 · L:L-18 (the 31-module drag / "closure 33→2")** — killed by reader-2 and ALREADY STRUCK at Dock-adj L-6 ("the cost is OWNERSHIP … never bytes; no close may cite"). Reader-1 carried this cell inside its confirmed injection row — corrected here; the injection defect itself stands at full weight (DVS-10).
- **K-12 · L:L-7 (cast-causation sub-claim)** — the index signature does not force the `as ViewEntry` casts: fresh object literals get implicit index signatures and every member is assignable. The casts are ceremony; a cure written against L-7's causal story would keep them. L-19's compiler proof of the signature's harm is unaffected (DVS-16).
- **K-13 · L:L-6 (cost sub-claim)** — useAtmosphereBoot.ts:55 is `import type` (read by this seat), erased under verbatimModuleSyntax; no runtime edge to vue-router is created. The shim's staleness survives (DVS-21).
- **K-14 · challenge-L pass-1 line provenance** — pass-1 anchors drift 8-20 lines with no uniform delta while the same axis's later passes cite exactly; both readers killed it, reader-2's git-log proof (the file predates all three audits at `f2c8f565`) adopted. Every finding lands on the right construct at its true line; only pass-1's citations are uncitable.
- **K-15 · reader-1's gold token cells (NEW KILL by this seat)** — `#D4AF37`/`#F5E6A3` as the painting values, and the "split gradient authority" mechanism of missed-row-6: the overrides sit in a losing `@theme` cascade layer and the corpus's own L-20 cell measured the producer oklch painting. The contrast row survives on corrected values (DVS-13); the split-authority mechanism re-books as DVS-35. Also retired: "spends most of its animation cycle near 1.1:1" (the sweep is spatial).
- **K-16 · reader-2's "no escape hatch" cell on DVS-1 (NEW KILL by this seat)** — `[data-ring-yield]` exists in the shipped dock CSS; it suppresses rings rather than restoring them, so the verdict is untouched, but the cell as written would misinform a cure author scanning for producer seams.
- **K-17 · reader-2's missed-row-2 as a NEW booking (NEW DEMOTION by this seat)** — the seed/watcher predicate split was ruled LATENT and non-operative at Dock-adj R-5; it enters this record only as an INFO accretion to L-19 (DVS-38) and may not be cited as the latch's mechanism by any close.

## Superlatives (provenance preserved)

1. **challenge-C's second pass** — named its own contamination, built a private browser instead of reasoning around it (`probe/probe-dvs*.mjs`, checked in), then used clean data to correct itself twice: D-10's MINOR promoted to the D-15 BLOCKER, and the `clampLabel` ask retracted as moot. Its closing law — *a consumer may read producer tokens; it may not write them* — was arrived at by reading shipped bytes instead of the comment above the line. Both readers ranked it first; this seat concurs, and two of the corpus's kills exist only because that pass did the work.
2. **The bounded registered-property sweep** (DockViewSelect.vue:159-161 + foundation.css:181-200) — a real `<color>` syntax, `inherits: true`, a var()-free initial value, the transition scoped to the one painting element, and the whole-document invalidation tax it retires documented at both ends (the W3-7 form-A decision). Three axes attacked it and all three conceded; this seat's one addition is DVS-25 — a second *trigger* nobody anticipated, not a flaw in the design.
3. **The contrast discipline in the accent pipeline** — palettes-ramp.ts declares BOTH floors (4.5 text / 3 graphics) and reasons per consume-site, naming this exact dock menu; useViewAccents runs rotate → C-floor → gamut-map → L re-guard → WCAG. Two of the file's three chromatic exceptions go through guards — which is precisely what makes the unguarded gold (DVS-13) legible as an omission rather than a system failure.
4. **One watch, one writer** (useViewAccents.ts:135-168) — `--accent-view` and both ramp sets minted by a single watch against one certified referent, with the VJ-U-F26 one-referent cure and its measured numbers recorded inline. In a cone whose central disease is duplicated derived state, this module is the counterexample the cures should copy.
5. **The single-owner popup mutex** — Dock.vue calls `usePopupMutex` exactly once and says so; the consumer documents from its side that it must not (:27-33). The L axis raised the defineModel round-trip hazard this project's own memory warns about, measured it, disproved it, and confessed the instrument error found on the way (click vs pointerdown) — exemplary adversarial practice on both sides of the boundary.
6. **The producer's dock touch floor** (touch-floor.css) — PROPORTION §5.7 implemented once in the design system: an invisible coarse-pointer `::after` seat, deliberately NOT `pointer-events:none` while the decorative `::before` beside it is. It is the reason D:D-6 is a false defect here exactly as its twin was at MobileMenuDropdown.
7. **The load-bearing comment corpus** (:37-47, :57-65, :99-109, :121, :129-132, :148-161) — every block names the ruling that shaped it (T-10, Q5, P4-R4, S.W7-7, W3-7 §2 form A), which is why three audits could argue about intent instead of guessing it — and why the two comments that HAVE gone stale (the line-clamp claim, the view-switch-only sweep framing) were catchable at all. The tombstone at :148-151 pointing to the retired scoped twin is the de-duplication direction most codebases never achieve.
8. **The generated-vs-hand-written proof pair** — vite.config.ts derives the self-alias set from `package.json#exports` and is clean; the one hand-written twin of the same fact (tsconfig.demo.json) has drifted to three dead targets and a false comment. L-16 is the measured argument for generation over transcription, and it rides the standing W-HYGIENE fold.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs — task #4 feeds)

1. **The trigger label's rendered contrast** — producer ink is `color-mix(… var(--dock-fg-on-aurora, var(--foreground)) 80%, transparent)` over a translucent dock band on a live ambient field (triggers.css + `--opacity-icon-muted: 0.8`, my reads); not statically decidable. Feeds DVS-15's severity too.
2. **Pointer-open highlight seeding** — reka source says the selected row is seeded at open in both input modes; C's keyboard cell agrees, D's webkit pointer table and the banked `pi-D3-selection-zero-delta.png` show no highlighted row. One controlled re-measure with the pointer parked off the list decides whether K-4's kill covers pointer-open as well.
3. **L:L-21's intermittent deep-link redirect** (3 of 6 cold loads) — its own author labels the mechanism a hypothesis; the structural half is verified statically (four uncoordinated router-writer sites — useDockAdminMode.ts:44/:46/:72, Dock.vue:55-56, usePaletteWiring.ts:78/:109/:169, useColorUrl.ts:64 — with `router.isReady()` awaited in exactly one place). The race itself needs live work; it gates nothing here.
4. **C:D-17's mobile AX cell** — the structure is source-certain (the v-if removes the only value-carrying node); the CDP accessibility-tree claim is live-only.
5. **D:D-7's dark-mode collapse magnitudes** — the per-scheme resolved `--accent-view` values (seven near-whites spanning 0.092 OKLab) are runtime guard output; the surviving MINOR observation for the design loop needs the measured table re-taken at HEAD.
6. **D:D-14's 11.4px overlap** — `sideOffset: 0` is static; the overlap number is live-only and now belongs to the producer/dock register, not this component.
7. **C:D-13's four mutations** — greps re-run and confirmed by this seat; the "suite stays green" claims are reasoned, not executed. X-W1's born-RED authoring should execute mutation 3 first (it doubles as DVS-6's gate-flip proof).
8. **Which gold paints, per scheme and route** — this seat's cascade-layer derivation + L-20's single cell say the producer family; one cell per scheme closes it and settles DVS-35's relay question. The rendered gold-on-glass ratios for DVS-13 ride the same session.
9. **DVS-25's visible drag-lag magnitude** — the 0.55s retarget cadence is source-certain; whether the glyph visibly trails the picker during a real drag (and by how much) is a live measure.
10. **`(pointer: coarse)` fidelity in D's webkit mobile probe** — bears only on how D measured 33.3px, not on K-1's kill; recorded for instrument hygiene.

## Closing verdict

**DEFECTIVE — ADJUDICATED: 3 BLOCKER · 11 MAJOR · 12 MINOR · 12 INFO confirmed; 17 claims killed and uncitable; 11 axis-missed rows booked (two with this seat's corrected evidence cells, one demoted under Dock-adj R-5).** The component's through-line is one seam wearing many faces: the option set, the mode flag, the admin classification, and the gold ink are all hand-maintained copies of facts the route table and schema already own — and the one genuinely new blocker (DVS-1) is a consumer writing a colour into a producer shadow token on an assumption one grep of `node_modules` refutes. Terminal routing: the blank-label/latch/derivation/selection/injection/breakpoint/cycle/mutex families FOLD to their standing Dock-adj identities (DELTA-4, L-19+R-5, G-C, DELTA-10 D2-16, L-6, DELTA-5, DELTA-13) → **X-W8**, which also gains three new arms from this corpus (DVS-1 focus-ring, DVS-9 RTL ConfigProvider, the DVS-6 o14 gate-flip) plus the G-D gold-probe amendment; the router guard goes to **X-W3/CC-037**; the type-matrix and contrast-law rows to **X-W10** (mechanism via **X-W4/CC-047**); the colour-only-state row to **X-W4**; the test vacuum to **X-W1**; config/alias residue to **W-HYGIENE** (ColorSpaceSelector-adj L-14 identity); the admin-gold treatment decision to the **convergent design loop**; and the product-scope anchor-grammar question (DVS-30 ⊃ DVS-11) is named whole for the **formation boundary**. Dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this record.

— end of apotheosis. The r2 challenge corpus, both reader returns, and all committed evidence remain immutable beside this file per E-1/E-3.
