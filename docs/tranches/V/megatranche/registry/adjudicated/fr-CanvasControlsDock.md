claude-fable-5 (served model id)

# fr-CanvasControlsDock — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Subject**: `fourier-analysis/web/src/components/visualization/CanvasControlsDock.vue` (132 lines, read whole by this seat) + its consumer seam (`VisualizationView.vue`, dirty), the view-state composable, the two stores its emits reach, and the glass-ui dock/hover-popover substrate.
**Substrate (re-pinned by this seat)**: fourier `m/w1-bump-migration @ cd26c65` + the frozen M.W1a dirty WT (`git status --porcelain`: 28 modified rows incl. `VisualizationView.vue`, `EditorControlsDock.vue`, `AnimationControls.vue`; **the subject file is clean**) · value.js `tranche-u` · installed glass-ui **4.0.0** / reka-ui 2.9.10 / lucide-vue-next 1.0.0 / value.js 0.13.0 (invalid peer, reproduced) · producer glass-ui **7.0.0 @ `7d9abe08`** (read by this seat: 66 export keys, `./hover-popover` ABSENT, `./popover` present, `@lucide/vue ^1.16.0` peer, `dock/index.ts` exports `DockControl`/`DockTrigger`/`DockSeparator` and no `DockIconButton` — retiring reader-LC's provenance caveat).
**Corpus**: three axis challenges (`audit/fourier-components/CanvasControlsDock/challenge-{D-design 21+4, L-library 25+10 ⟨R2 consolidated⟩, C-consumption 28+6}.md`, all read whole by this seat) × two hostile re-reader returns (**reader-DU** design-and-usage · **reader-LC** library-and-code), received in-brief.
**Method (M-23/L-18)**: every verdict-level disagreement between the readers was re-derived against the tree before ruling; every load-bearing kill was re-attacked with this seat's own instruments; **one kill was minted by this seat against a reader cell (K-10), and two reader positions were overruled on seat evidence (K-4 against DU's L-19 booking, K-6 against DU's "all four arms" cell)**. Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; product source untouched)**: full read of CanvasControlsDock.vue (132); ranged reads VisualizationView.vue:70-137, :139-287, :229-339, :425-480; ContourSettings.vue:100-175; stores/workspace.ts:238-287; stores/gallery.ts:215-240; FullscreenViewer.vue:95-160; useViewState.ts:1-55 (whole); Tooltip.vue:25-40; App.vue:4,:23; AppHeader.vue `.header-inner` (height 3.5rem at :162-163); e2e/visualization-ux.spec.ts:195-235 (the inv-19 `test.fixme` RED-baseline prose read in place); e2e/gallery.spec.ts:104-120. Installed-dist reads: dock.js:583-601 (the whole props literal — **four `{type:Boolean}` no-default members enumerated: `fitContent`:585, `alwaysExpanded`:587, `startCollapsed`:594, `autoLuminance`:596**), :470 (`collapseDelay ?? 2e3`), :474 (the unreachable `?? !0`), :605 (`n.autoLuminance !== !1` gate), :680-731 (the full GlassDock render: `$attrs` on the inner `.glass-dock` :685, the `dock-inline` position ternary :699, expand triggers = mouseenter/focusin/touchstart-move-end/pointerdownCapture/clickCapture :705-713, `inert`-toggled layers :722-727, the summary layer = a bare `<div>` with `onClick` and no role/tabindex), :623-627 (rail-anchor `querySelector`), :1220-1226 (DockSeparator: `role="separator"`, `data-rail-anchor`); greps over dock.js: `keydown` → **0 hits**, `tabindex` → :1008 (a props-name string) + :1042 (DockLayer) only, `role:` → separator/group/tablist/tab only, nothing on the shell. Styles: dock-controls/icon-button.css:100-140 (the `:is(.is-active, .active, [aria-expanded="true"], [aria-pressed="true"])` cohort :109-115; `.dock-icon-button > svg { width: var(--dock-icon-glyph, 1.25rem) }` :132-135; the muted-glyph `color-mix(… var(--opacity-icon-muted) …)` dial :37-46); dock/layer-group.css:20-65 (separator recipe + vertical/grid arms, verbatim); dock/overflow.css:166-168 (`display:none` wrap arm) + :205-260 (the coarse `--dock-scale` AND `--dock-icon-glyph` re-declaration on `.glass-dock[data-density]`); utilities/a11y-overrides.css:1-32 (the PRM blanket + the `[data-allow-motion]` override, reachability sealed: style.css:3 → styles index → utilities.css:19); dock/shell.css:24 (`--dock-separator-height`), `contain: paint`; hover-popover.css:13-33 (the one-line-label panel register, verbatim); HoverPopover-Dpzwvc4t.js (`hoverOpenDelay: { default: 250 }`, `"avoid-collisions": !0` unconditional, `l.native` gate, `data-glass-dock-portal`, `keepDockOpen` ×3); HoverPopover.vue.d.ts (`v-model:open` defineModel at AB.W2 — the M-5 cure exists); reka HoverCardTrigger.js read whole (exactly `onPointerenter`/`onPointerleave` both `excludeTouch`-wrapped + `onFocus`/`onBlur`; attrs = `data-state` + `data-grace-area-trigger` — **no aria-expanded/haspopup/controls**); HoverCardContentImpl.js:139-140 (the unconditional `onMounted` `tabindex="-1"` strip); lucide Icon.js:41 (`aria-hidden` when no slot/a11y prop); dock styles grep `data-state="open"` → only `dock-select-trigger` + the dock-root `:has()` — **no `.dock-icon-button` open-paint arm**; dock/layers.css:279-306 (the `nth-child` stagger ladder + ×5 cap). Sibling/consumer greps: EditorControlsDock.vue (`:collapse-delay="2000"` at **:56** — a third L-axis line-drift instance; `aria-label` at :105/:121/:136; `Eye` trigger :137 + `Eye/EyeOff` :144; `span.dock-separator` :84/:131/:179; `pointCount` collapsed badge :61/:163); `npm ls @mkbabb/value.js` → invalid-peer ×3 (nth independent reproduction); fr-BasisCanvas.md grep (BC-10/C-9 carries the fullscreen-occlusion identity).

## Scoreboard

**43 rows ADJUDICATED-CONFIRMED (5 BLOCKER · 14 MAJOR · 14 MINOR · 10 INFO, counting the 10 missed rows banked) · 10 killed-register entries (1 seat-minted against a reader; 2 reader positions overruled on seat evidence) · full corpus coverage — every D/L/C row leaves with exactly one disposition.**

The five BLOCKERs: three corpus-unanimous (unnamed controls · keyboard-dead popover toggles · re-entrant Publish) and **two banked from reader misses that invert corpus credit**: the silent destructive contour re-extract on leaving edit mode (reader-LC's find, seat re-derived whole — it replaces the killed L-19), and the keyboard-sealed collapsed dock (reader-DU's find, seat re-derived whole — the corpus's own §6.3 read the proof and booked it as substrate credit).

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **L-19's verdict** | CONFIRMED BLOCKER with a falsifier correction (reload auto-recovers by re-extracting) | KILLED (permanence false; novelty false) + re-filed as the destructive-re-extract BLOCKER | **LC wins — K-4; the replacement BLOCKER is banked as M-1** | Seat re-derivation, every hop: VisualizationView.vue:254-276 is a KEYED `<Transition name="panel-swap" mode="out-in">` whose two branches (`key="editor-panel"` :255, `key="viz-panel"` :262) EACH mount their own `<ContourSettings v-if="hasImage">` (:260, :270) — flipping `isEditing` unmounts one instance and mounts a fresh one. ContourSettings.vue:152-169: the fresh instance's `watch(() => store.imageMeta, …, { immediate: true })` fires on mount; its guard at :164 (`!store.epicycleData && !store.basesData && !store.computing`) is EXACTLY the post-save state (workspace.ts:263-283: `contour = markRaw(result)`, `epicycleData = null`, `basesData = null`, `finally { endCompute() }`, no recompute) and is NOT gated by `suppressSettingsRecompute`. `runCompute()` (:104-137) synchronously runs `store.beginCompute()` → `computing` true → `hasData` truthy → the dock REMOUNTS after the ~0.2s `panel-swap` window (:430-432: `0.2s` each way) — so "zero controls forever" is FALSE. Then `:126 await store.extractContour()` → workspace.ts:252 `contour.value = markRaw(result)` + :253 `scheduleDraftSave()` — the MACHINE extraction overwrites the hand-edited contour AND persists the replacement, with no gesture, no confirmation, no undo. DU found the destroying watcher and applied it to RELOAD only, never re-walking the mount chain — the keyed-Transition remount makes the destruction automatic on the exit gesture itself. Novelty: e2e/visualization-ux.spec.ts:204-235 (`test.fixme` inv-19) books the store half ("saveContourPoints … launches no recompute … canvas stays blank", cure scheduled W3) — but it drives `window.__store` WITHOUT toggling `isEditing`, so the remount-triggered destruction is booked NOWHERE and invisible to the suite. |
| R-2 | **D-8's verdict** (side="top" never paints) | CONFIRMED MAJOR on corrected viewport arithmetic | proof KILLED, conclusion left open → SS-13 | **DU's verdict, LC's kill — both adopted: CONFIRMED MAJOR on seat-re-derived arithmetic; the axis's stage-relative proof stays killed (K-5)** | The collision boundary is the viewport (the panel is portaled to `<body>`), not `.canvas-stage`. Seat arithmetic: `.header-inner` height 3.5rem (AppHeader.vue:162-163) + `.viz-configurator` margin 0.5rem at ≥1024px (VisualizationView.vue:317-320) + anchor `top: 0.5rem` ≈ **72px** of clearance above the trigger — a quantity fixed by the header stack, NOT viewport-proportional, so no viewport height changes it. Panel: 2×2.5rem controls + `gap-1` 4px + `p-1` 8px + `padding-block: .375rem`×2 + `sideOffset: 6` ≈ **110px**. 72 < 110 on every fine-pointer open ⇒ the flip is unconditional at this layout; `[data-side]` readback → SS-13 as corroboration, not load-bearing. |
| R-3 | **C-6/L-4's blast radius** ("all four substrate arms silently replaced") | carried the axis cell ("all four are silently replaced") | wrap-arm sub-claim KILLED (cascade resolves per DECLARATION) | **LC wins — K-6** | The scoped block (:106-112) declares `width`/`height`/`margin`/`background`/`flex-shrink` and never `display`, `grid-column`, or `align-self`. Unlayered-beats-layered applies per declaration: overflow.css:166-168's `.dock-overflow-wrap .dock-separator { display: none }` still applies IN FULL; the grid arm's `grid-column: 1 / -1` + `align-self: center` survive (its width/height/margin do not); the vertical arm is genuinely neutralized (both `width` and `height` locally named). The headline (a consumer re-declaring a public substrate class) survives at MAJOR; the "every arm" framing dies. |
| R-4 | **autoLuminance missed-row severity** (the L-20 inverse) | MAJOR ("adaptive legibility silently dead across the whole app") | MINOR (generalized: the whole props object has no defaults path — one relay row) | **Merged as M-4: MAJOR at the producer, GLASS-RELAY; LC's generalization is the booked form; the relay letter MUST carry a consumer-impact warning** | Seat enumeration of dock.js:584-601: **four** `{ type: Boolean }` no-default members (`fitContent`, `alwaysExpanded`, `startCollapsed`, `autoLuminance`) in one literal; no `withDefaults`/`mergeDefaults` path exists, so NO Boolean prop on the dock surface can carry the positive default its docstring claims. The sting this seat adds: **"fixing" the producer default would REGRESS fourier** — if `autoLuminance` becomes truly default-ON, L-20's 4Hz forced-layout sampler goes LIVE over three canvas-stacked docks that today enjoy it being accidentally off. The relay must pair the default repair with either an explicit `:auto-luminance="false"` sweep at all three fourier mounts or a documented default-OFF. |
| R-5 | **The keyboard-sealed collapsed dock** (DU's missed BLOCKER; LC did not file it) | BLOCKER | (absent) | **BANKED as M-2, BLOCKER — seat re-derived whole** | dock.js:722-727: while collapsed, `dock-layer--full` carries `inert` (every DockIconButton out of the tab order and the a11y tree) and `dock-layer--summary` is a bare `<div>` with `onClick` — no `role`, no `tabindex`, no key handler. Expand triggers (:705-713) are mouseenter / focusin / touchstart / pointerdown-capture / click-capture; `grep keydown dock.js` → **0**; `onFocusin` is unreachable because no focusable descendant exists while collapsed and the shell itself is not focusable (tabindex grep: :1042 DockLayer only; :1008 is a props-name string). With `:start-collapsed="true"` (:41), no `always-expanded`, no `#persistent` slot, and the 2000ms idle-collapse, collapsed is the resting state — **a keyboard-only user can never open this dock; all seven controls, named or not, are unreachable** (WCAG 2.1.1 A). Severity note against the fr-AnimationControls D-9 precedent (MAJOR-until-live-keystroke): there, a partial keyboard path existed (arrow-nav to one registered item); here NO handler exists to fire — the negative is statically total, so BLOCKER is booked now and SS-13 is corroborative. The corpus inversion is recorded in DISSENT: challenge-D §6.3 READ the `inert` stamp and filed it as substrate credit; D-S2/L-S9/C-S1 celebrate the very prop that seals the resting state. |
| R-6 | **C-13's "live on any touch device" upgrade** | confirmed as booked (MAJOR) | UPGRADED — "observable today, not merely latent: any touch device" | **Cell KILLED — seat-minted K-10; the row survives MAJOR** | The coarse re-declaration (overflow.css: `--dock-scale` + `--dock-icon-glyph` on `.glass-dock[data-density]` under `@media (pointer: coarse)`) is real — but the glyph split can only RENDER where the popover OPENS, and this same corpus's D-2 proves the open path is touch-excluded (`excludeTouch` on both pointer handlers). On touch-primary iOS the panel never opens (Safari does not focus a `<button>` on tap); the split is live only where an open path exists (Android tap-focus, coarse-primary hybrids with attached pointers). The documented-channel severance stands; the "rendered today on ANY touch device" cell dies. |
| R-7 | **View-dot cluster severity** (D-17/L-5/L-11/C-21) | MINOR | MAJOR | **MINOR — DU wins; LC's MAJOR preserved in DISSENT** | Seat re-read useViewState.ts whole: `showGhost = ref(true)` unconditional (:19), the persistence writer emits `{editing, overlay, equation}` only (:43-49), `loadViewState`'s type has no ghost key (:6) — all facts unanimous. Severity follows consequence: a decorative pip that is on-by-default and an undisclosed persistence asymmetry inside one grouped affordance — incoherent, but no capability is lost and the popover (not the dot) is the control surface. |
| R-8 | **C-20** (Publish gate ≠ precondition) | down-rate to INFO ("practically unreachable") | kept inside its C-12/L-15 row | **INFO — DU wins** | DU's derivation adopted and spot-checked: every writer of `contour.value` (`extractContour` :242-252 requires `imageSlug`; `saveContourPoints` :264 requires `imageSlug`; the loaders set both) gates on or sets `imageSlug` first, so `hasContour ∧ ¬imageSlug` has no live constructor. The gate-is-not-precondition asymmetry is recorded; the SS-13 slot is demoted to opportunistic. |
| R-9 | **D-2's touch leg** | carried "touch-dead" unqualified | qualified: engine-dependent (Chrome/Android tap-focus opens via `onFocus`; iOS Safari does not) | **LC wins — K-9; the BLOCKER stands on the keyboard leg alone** | Seat-verified: HoverCardContentImpl.js:139-140 is an unconditional `onMounted` `tabindex="-1"` strip over `getTabbableNodes` (a `<button>` is tabIndex 0) — airtight, engine-independent. The trigger binds `onFocus` (seat read), so a tap that grants focus opens the card on some engines. "Touch-dead" is carried QUALIFIED; both engines → SS-13. |
| R-10 | **`.dock-separator` in the 4→7 removal budget** (C-4/D-12 framing) | KILLED the framing (category error) | confirmed the row with a provenance caveat (7.0.0 leg unverified) | **DU's kill ratified; LC's caveat retired by this seat's producer read** | Seat: installed 4.0.0 layer-group.css:23-62 is the live paint source for the class; producer 7.0.0 `dock/index.ts:32` still exports `DockSeparator` (whose compiled form renders `div.dock-separator`, dock.js:1222). The class is not version-gated; the defect (D-6/L-4/C-6) is fully live at HEAD and belongs on the today-fixable list, NOT in the F.W1 break table. The break table keeps exactly: `./hover-popover` (absent from the 66-key 7.0.0 exports — seat-verified), `DockIconButton` (no export at 7.0.0 — seat-verified), the lucide re-point (`@lucide/vue ^1.16.0` peer — seat-verified). |

**Citation-correction rulings inside agreed verdicts**: L-3's lucide mechanism cell (read `defaultAttributes.js` only) stays killed — Icon.js:41 stamps `aria-hidden="true"` at all seven sites (seat-verified), so the glyphs are affirmatively removed, not merely mute; the C axis's correction is the carried form (K-7). L-24's differentiator stays killed (K-3): dock.js:470 `collapseDelay ?? 2e3` makes :172's 2500ms unreachable from GlassDock; the effective delay is 2000ms, byte-identical to both siblings' explicit bindings (and L-24's `EditorControlsDock.vue:53` cite is live at **:56** — a third instance of the L-axis line drift, M-10). C-14 stays killed (K-2): a11y-overrides.css:6-16 + the seat-sealed import chain gates `animate-pulse` for free; the D axis killed it at §6.1 and the C axis published it anyway — the cross-axis reconciliation gap is DISSENT material, upheld by both readers.

## Adjudicated defect roster

Line anchors: subject file is committed-clean at `cd26c65`; `VisualizationView.vue` anchors are live-WT (dirty — the M-10 drift row is why every anchor here was re-resolved by this seat). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5–W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring.

### BLOCKER (5)

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **D-1 / L-3 / C-1** | Six of seven controls have no accessible name — :46 is the file's only `aria-label`; every glyph is `aria-hidden` (lucide Icon.js:41, no slot/a11y prop at any site); the Tooltip shim supplies `aria-describedby` only while open (a description, never a name). The in-repo idiom is the counter-proof (EditorControlsDock :105/:121/:136; AnimationControls ×3) and e2e/gallery.spec.ts:112-116 states the law ("keyed by aria-label — the dock idiom") then can only assert the one control that has one. | CanvasControlsDock.vue:46,:54,:59,:71,:77,:87,:93; Icon.js:41; Tooltip.vue:27-33; gallery.spec.ts:112-116 (all seat) | **F.W4** — six attributes land today. Rider on **R3-7a's F.W3** Tooltip migration: the X-11 census-contradiction re-scope ("re-point the import AND mint a naming leg") is ratified — the adapter is the surface that lets a nameless control look labelled. Fold: M-9 (the fullscreen exit button, same family). |
| **D-2 / C-2** | The two view toggles are keyboard-dead by construction: reka's hover-card content strips `tabindex` from every tabbable on mount (unconditional `onMounted`, seat-verified), no focus scope exists, and the trigger's pointer open-path is `excludeTouch`-wrapped. Touch leg QUALIFIED (K-9): iOS Safari never opens the panel; Android's tap-focus may. Repeats verbatim in EditorControlsDock (:103,:134) — four toggles, two docks, one root cause. The producer names the defect and cures it at the 5.0.0 Popover union ("coarse-pointer hover auto-promotes to tap-toggle; reka's excludeTouch leaves the hover root structurally dead on touch"). | HoverCardContentImpl.js:139-140; HoverCardTrigger.js (read whole); HoverPopover chunk `l.native` gate; CanvasControlsDock.vue:44,:51-64 (all seat) | **F.W1** — the Popover union structurally removes the interactive-content-in-a-hover-card shape; a mechanical rename would re-ship it, so the migration row must be marked primitive-class-change. Interim F.W4 option: `DockDropdownTrigger`/`Popover` exist at the installed 4.0.0. Both engines' touch legs → SS-13. |
| **D-4 / L-1 / C-3** | Publish is re-entrant on all three hops: the component receives `publishing` (:16) and spends it on paint only (no `:disabled` — grep 0); `handlePublish` has no re-entry guard (sole early return :107); `saveVisualization` is an unconditional POST minting a fresh slug per call (no idempotency key, `visualizationSlug` written :358, never read as guard); gallery.publish PATCHes each slug public. N clicks ⇒ N public gallery rows. The one-attribute cure is in-tree (sibling binds `:disabled`; glass ships the disabled paint). | CanvasControlsDock.vue:70-74; VisualizationView.vue:106-118; workspace.ts:344-365,:460; gallery.ts:219-235 (all seat) | **F.W4** — `:disabled="publishing"` + the parent guard today. **F.W5–W8 rider**: the server-side `(image_slug, contour_hash)` dedupe arm rides the R6-8 seam split (C-28 fold) — the client cure and the operation cure register a two-sided delta until that join is split. |
| **M-1** (banked; reader-LC) | **Exiting edit mode after a contour save SILENTLY RE-EXTRACTS the contour and destroys the user's hand edits — no gesture, no confirmation, no undo — and `scheduleDraftSave` persists the destruction.** The chain (seat re-derived whole, R-1): unguarded `toggleEdit` (:86-90, the only exit — EditorControlsDock ships none) → `isEditing` flip → the keyed `panel-swap` Transition remounts `ContourSettings` → its `immediate:true` imageMeta watcher's guard is exactly the post-save state → `runCompute()` → `extractContour()` overwrites `contour` and draft-saves it. This is the true row where the killed L-19 pointed; it inverts L-19's conclusion (the stage HEALS — by destroying the work). Invisible to the repo's own inv-19 fixme (drives `window.__store` without toggling `isEditing`). This component's share: it models `hasContour ∧ ¬hasData` explicitly (:14-15) and still emits a payload-free, unqualified toggle from inside it. | VisualizationView.vue:220,:254-276; ContourSettings.vue:104-137,:152-169; workspace.ts:241-261,:263-283; visualization-ux.spec.ts:204-235 (all seat) | **F.W4** — the auto-recompute seam the repo already schedules (inv-19's "W3" cure: a `ComputeBasesRequest` model + a `store.contour`-keyed watcher) MUST recompute FROM the saved contour, never re-extract over it; plus a component-local emit contract (`toggleEdit: [next: boolean]` or a `canExitEdit` gate). Live round-trip (perceptibility + operator recoverability of the overwritten contour) → SS-13. |
| **M-2** (banked; reader-DU) | **The collapsed dock has ZERO keyboard-focusable elements, so a keyboard-only user can never open it** — the full layer is `inert` while collapsed, the summary layer is a click-only bare `<div>`, the shell is unfocusable, and dock.js contains no key handler (grep 0) — so `onFocusin`, the only keyboard-relevant trigger, is unreachable. All seven controls, named or not, are unreachable from the resting state the consumer pins (`:start-collapsed="true"`, idle-collapse 2000ms). All three axes audited the accessible names of controls no keyboard user can arrive at; D §6.3 read the `inert` proof and booked it as credit. | dock.js:703-727 + the seat's keydown/tabindex/role greps; CanvasControlsDock.vue:41 | **GLASS-RELAY** (producer: the collapsed shell needs a keyboard affordance — a focusable summary with `Enter`/`Space` expand, or focus-visible auto-expand) **+ F.W4** (consumer decision meanwhile: focusable summary content — the sibling's collapsed Save button proves the slot accepts interactive content — or `always-expanded` on keyboard-primary media). Tab-walk corroboration → SS-13 (corroborative, not load-bearing — see R-5's precedent note). |

### MAJOR (14)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-3 / L-8 / C-8** | Five toggles take the class arm of the substrate's two-arm active contract and drop the semantic arm; `aria-pressed` is a byte-identical free substitute (`:is()` cohort seat-verified at icon-button.css:109-115; `aria-pressed="false"` matches nothing, so the off state is identical too). C-8's contradiction of fourier's own 2026-06-16 raw-findings.json:2968 ("is-active has NO backing paint") is UPHELD — the paint exists at installed 4.0.0. `CanvasOverlayButton.vue` (0 consumers) is the orphaned written spec of the dropped contract: delete AFTER restoration. | **F.W4** — five attributes today; at **F.W1** `DockControl`'s tri-state `active` (explicit `default: undefined` — the producer finally guarding the M-4 trap) replaces class+aria in one binding. |
| **D-6 / L-4 / C-6** | The scoped `.dock-separator` re-declares a public substrate class and overrides the shipped recipe (height decoupled from `--dock-h`, margin 3× tighter, tint off the `--surface-tint-15` rung and its dark-arm re-base, the vertical arm neutralized) — while `DockSeparator` ships in the barrel the file already imports, with `role="separator"` semantics a bare div forfeits. Blast radius CORRECTED per K-6: the wrap arm's `display:none` and the grid arm's placement declarations survive. Drift already visible: three divergent copies across two docks + glass. | **F.W4** — adopt `<DockSeparator />` (available at the INSTALLED 4.0.0 — R-10) and delete :106-112. Not an F.W1 row. |
| **D-5 / L-7 / C-7** (+ D-13/D-14 folded) | Two icon-size idioms with opposite cascade outcomes: five glyphs utility-pinned at 18px out of the `--dock-scale` ladder (the coarse-pointer re-declaration of BOTH `--dock-scale` and `--dock-icon-glyph` on `.glass-dock[data-density]` is seat-verified — the density chain does reach the glyph, so the pin is a real freeze: glyph/plate 0.385 vs the system's 0.50 on touch), while `:size="20"` is dead (presentation attributes lose to the `@layer components` rule). Four unrelated glyph sizes in one file (18/20-resolved/16/14). | **F.W4** — delete both idioms; the producer default is the intended value. Rendered split where an open path exists → SS-13 (K-10's scoping). |
| **D-12 / L-6 / C-4** | The 4→7 break density: `./hover-popover` dead at the bump (seat-verified absent from the 66-key exports), `DockIconButton` ×7 call sites definition-absent (seat-verified no export), 7 lucide symbols on the `@lucide/vue` re-point — three break axes inside 132 lines. The `.dock-separator` row is RE-CLASSED out of the break table per R-10 (today-live defect, not version-forced). | **F.W1** — the tri-package uplift owns the three real rows; D-2's primitive-class-change marking rides the same migration. |
| **L-2 / C-5** | `v-model:expanded` binds a prop no layer declares: the write leg is a permanent no-op (`DockProps` has no `expanded`; expansion is imperative) and the phantom attr lands literally on the substrate's own state element via `inheritAttrs:!1` + the `$attrs` spread (seat-verified dock.js:685) — `<div class="glass-dock … collapsed" expanded="false">`, a DOM-truth lie beside the state class, with `[expanded]` selectors matching in both states. No gate catches it (vue-tsc silent; `inheritAttrs:false` suppresses the extraneous-attrs warning). | **F.W4** — declare-and-honour `expanded` (wire to the exposed `expand()`/`collapse()`) or rename the event; the e2e locator idiom (`toHaveClass(/expanded/)`) is the one truth meanwhile. |
| **D-11 / L-17** | The component's only script logic (ref + watch + emit, :29-37) is inert at ≥1024px — `.dock-centered` is cancelled outright for anchor and modifier alike (seat-verified live :467-474 region) — and below 1024px it animates `left`/`right`/`transform`, two layout-triggering properties, over a canvas stage. The PRM half stays dead (the a11y-overrides blanket restricts transition-property, D's own §6.1-adjacent kill upheld). | **F.W4** — either honour the centring on desktop or delete the machinery; if kept, composited `transform` only. |
| **D-10 / C-12 / C-11** | No loading/empty/error vocabulary: `publishing` has no terminal state (gallery.publish try/catches internally and toasts — seat-verified :222-233 — so the parent's catch is unreachable and `finally` clears identically on both outcomes; no `aria-busy`/`aria-live`/result emit); and `hasData` is the wrong predicate for Equation — the panel needs `epicycleData` (:231) while the button enables on `basesData || computing` alone, painting `is-active` for a panel that does not exist. | **F.W4** — `hasEquationData` prop; publish result surfaced through a channel this component owns; disable vocabulary (glass ships the paint). Frequency of the `basesData`-only state → SS-13 opportunistic. |
| **D-7** | A tooltip-register panel used as a two-item control menu: the `.hover-popover-panel` register is a one-line label chassis (seat-verified verbatim: inline-flex, `padding-block .375rem`, `--type-small`, "single line of label text" docstring); paddings stack (panel + consumer `p-1`) into a value no author chose, and the consumer's `gap-1` (4px) undercuts the dock's own comfortable layer gap (6px) — the same two controls sit tighter in the popover than in the dock. | **F.W1** — dies with D-2's Popover union (a control-register surface); do not patch the register locally. |
| **C-13** | Portaling severs the documented dock-scoped custom-property channel (`--dock-active-*` cohort "scoped to a parent"; the coarse `--dock-scale`/`--dock-icon-glyph` re-declaration) for the two popover controls — they resolve at `:root` while their five siblings resolve at the dock. The portal stamps are click-away identity markers, not style scope. "Live on any touch device" KILLED (K-10) — live exactly where an open path exists. | **F.W1** with D-2 (the union re-derives the portal) + **GLASS-RELAY** rider: whether the dock's token scope should re-project onto owned portals is a producer design question the relay should carry. |
| **D-8** | `side="top"` can never paint: ~72px of header-fixed clearance vs a ~110px panel (seat arithmetic, R-2) with `avoid-collisions` hard-set — the popover flips below the dock on every open, and the inner Tooltips (defaulting `side ?? 'top'`, no side passed at :53/:58 while the file passes `side="bottom"` everywhere else) stack back up over the dock in reverse reading order. | **F.W4** — declare `side="bottom"` (+ the two inner tooltips) or fold into the D-2 rewrite; `[data-side]` readback → SS-13 corroborative. |
| **C-9** (+ L-13 folded) | The sibling dock implements the same affordances with a divergent contract on seven axes (emit name, prop optionality, trigger label, item order, feedback mechanism, separator host element, emit idiom) — all seven re-checked live by the readers, spot-checked by this seat (Eye/EyeOff :144, `span.dock-separator`, aria-labels). The two docks swap in place over one canvas on the `isEditing` flip. | **F.W4** — one shared contract (or one owning dock) for the ghost/overlay pair; pairs with M-8's icon vocabulary. |
| **C-10** | The W2.E comment claims the out-of-band coupling was removed; the reach-in was relocated one level down (`dockRef.value?.expanded` off the exposed surface), and glass 4.0.0's GlassDock declares NO emits — instance reach-in is the only observation path any consumer has; `?.` + `?? false` launders a renamed/dropped key into a legitimate-looking permanent `false`. | **F.W4** (correct the comment; keep the guard) + **GLASS-RELAY** (producer gap: a dock whose expansion is observable only by reach-in cannot be composed in-band — ask for an `update:expanded` emit or model). |
| **M-3** (banked; reader-LC) | In edit mode, Fullscreen (:91-96 — no `v-if`, outside the `!isEditing` template, live while editing) opens a SECOND, tool-less, save-less contour editor: FullscreenViewer mounts `ContourEditorCanvas` (isEditing && contour) with NO ref, NO `@state-change`, no save wiring (seat-verified :115-120), the controls row is `v-if="!isEditing"` (:130), and the parent's Save reads the INLINE instance's `editorRef` (:80,:92-96,:204). Every drag made in fullscreen is silently discarded on close; the layer's one usable control is the (unnamed, M-9) close button. | **F.W4** — wire the teleported editor to the save path or gate Fullscreen out of edit mode; pointer-handler reachability + focus-trap interference → SS-13. |
| **M-4** (banked; merged DU+LC) | `autoLuminance`'s documented default-ON never engages for ANY consumer — the same absent-Boolean trap the corpus proved on `startCollapsed`, two lines below it in the same props literal; generalized (LC): **no `withDefaults` path exists, so no Boolean prop on the dock surface (`fitContent`/`alwaysExpanded`/`startCollapsed`/`autoLuminance` — seat-enumerated) can carry a positive default**, and HoverPopover's `keepDockOpen`/`native` share the shape. The documented opt-out `:auto-luminance="false"` is a no-op distinction (both absent and false yield OFF). Retires L-20 rather than escalating it (K-1). | **GLASS-RELAY** — ONE letter with S-1's `startCollapsed` evidence and R-4's consumer-impact warning (a naive default repair would light the sampler over three fourier canvas docks); rides the standing BH inbox alongside M-2, L-23, and the C-10 emits gap. |

### MINOR (14)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-9 / C-15** | The collapsed summary advertises a Pencil the expanded dock may not have (Edit is `v-if="hasContour"`; the dock mounts during compute-before-contour via `hasData ∋ computing`), and `:start-collapsed="true"` makes the over-promising pill the first-time user's first state. | **F.W4** — with L-22 (make the slot reactive). |
| **D-17 / L-5 / L-11 / C-21** | The view-dot is lit by default (`showGhost = ref(true)` unconditional) so it indicates nothing on arrival; its two members have opposite persistence (overlay saved, ghost never — absent from writer and return type); one dot ORs two states and the disambiguation lives inside the keyboard-dead popover. LC's MAJOR preserved in DISSENT (R-7). | **F.W4** — persist ghost (or gate the dot on non-default state); `useViewState` is the seam. |
| **D-15 / L-21** | `.view-btn-wrap` (:114-119) is four dead declarations — every value is already the computed value from `.glass-material` + the icon-button base; the unlayered win changes nothing. Deletion is byte-identical. | **F.W4** — delete. |
| **L-23** | The reveal-stagger ladder is index-inclusive but role-blind (seat-verified `nth-child{2..5, n+6}` + the ×5 cap): the two hairlines eat beats 2 and 5 and the cap lands one control early. Honestly-owned upstream: `DockSeparator` also renders `div.dock-separator`, so the D-6 cure does not fix it; the sibling collapses the ladder the opposite way (single wrapper → one beat). | **GLASS-RELAY** — rides the M-4 letter (R5-7's dual: a deriver that counts nodes without knowing their roles). |
| **L-9** | The raw `<div>` separators carry no `data-rail-anchor`, so the dock's marker-keyed rail-seam derivation cannot see them — correctly latent (no `#rail` slot passed; dock.js:623-627 returns early, seat-verified). | **F.W4** — cured for free by the D-6 `DockSeparator` adoption; recorded so a future `#rail` does not land on a silent miss. |
| **D-16** | The state dot paints under the substrate's specular catch-light exactly on hover (the `::before` at `z-index:1` vs the dot's `z-index:auto`, host not a stacking context; rest intensity 0 — the composite occurs precisely when the control is being read). | **F.W4** — `z-index` in the same edit as D-6/D-15; perceptual magnitude → SS-13. |
| **D-18 / L-10** | Label register inconsistent across six labels (Title Case / sentence verb phrases / bare state nouns), and `toggleFullscreen` names a toggle the wiring never implements (`showFullscreen = true` only; the exit lives in FullscreenViewer). | **F.W4** — microcopy pass + rename the emit (`openFullscreen`) or honour the toggle. |
| **L-22** | The `#collapsed` slot is a static literal — none of the seven reactive props reaches the resting state; the sibling's collapsed slot (live `pointCount` badge + working Save) proves reactive/interactive content is idiomatic and cheap. Structural reason the view-dot fails in its second state. | **F.W4** — with D-9/C-15 and M-2's consumer arm (a focusable, truthful summary). |
| **L-24-residual** (re-scoped per K-3) | The publish-progress affordance lives only in the expanded layer while the dock idle-collapses at the resolved 2000ms and `handlePublish` awaits two sequential round-trips — EQUALLY true of all three docks (the "unlike both siblings" differentiator is dead). | **F.W4** — booked at the three-dock scope; collapse-vs-publish wall clock → SS-13 (re-scoped probe). |
| **L-25 / C-16 / D-19 / C-24** (+ L-16, C-19 folded) | Authoring residue: `hasData: boolean` fed by a non-boolean computed (`!!` at :217/:218, NONE at the load-bearing :210 mount predicate — the M-1 chain's gate); two emit idioms for one contract (typed `emit` ×1, `$emit` ×6); the mangled seven-symbol import line ending `Spline, }` in a repo with no formatter config (find → empty); `ref<InstanceType<…>>` where `useTemplateRef` is the stated 3.5 idiom. | **F.W4** sweep — one edit; the `!!`-at-:210 nit rides M-1's cure. |
| **M-5** (banked; reader-DU) | The one labelled control declares no `aria-expanded`/`aria-haspopup`: reka's hover-card trigger emits `data-state` only (seat read whole), and there is no `.dock-icon-button[data-state="open"]` paint arm (seat grep) while `[aria-expanded="true"]` is a first-class member of the active-paint cohort — so the open panel neither announces nor paints its trigger as open. `v-model:open` exists (AB.W2 defineModel, seat-verified), so `:aria-expanded="open"` buys both today. | **F.W4** — one binding. |
| **M-6** (banked; reader-DU) | Tooltip/popover cadence desync inside one dock: App.vue:23 sets the app TooltipProvider to 400ms while HoverPopover defaults `hoverOpenDelay: 250` (seat-verified), minted explicitly to match the tooltip cadence — so the heavier panel leads the lighter labels by 150ms across the cluster. | **F.W4** — pass `:hover-open-delay="400"` or align the provider; one prop. |
| **M-7** (banked; reader-DU) | The resting dock's entire affordance is two hard-coded opacity utilities (`opacity-70`/`opacity-40` on aria-hidden glyphs — the file's only opacities, in the resting face), bypassing the substrate's `--opacity-icon-muted` muted-glyph dial (seat-verified icon-button.css:37-46) and putting a 14px 40%-opacity glyph over live-canvas glass — the WCAG 1.4.11 shape. | **F.W4** — route through the token with the L-22 redesign; contrast decimal → SS-13. |
| **M-8** (banked; reader-DU) | Icon-vocabulary collision across the two swapping docks: `Eye` = "open the view menu" here, "open the overlay menu" AND "contour trace is on" in the sibling (seat-verified :137/:144), while the same trace toggle is `Spline` here. | **F.W4** — one glyph per meaning, with C-9. |

### INFO (10)

| id | claim | disposition |
|---|---|---|
| **D-20 / L-21-addendum** | The dot's halo clips under `contain: paint` below the comfortable density rung — correctly latent (`:41` passes no `density`). | **F.W4** latent note; explicitly NOT an SS-13 slot at HEAD (both readers concur). |
| **D-21 / C-17** | Latent adjacent-separator hole — the separators are unconditional while both enclosed controls are individually gated; unreachable only via the parent's own `v-if`. The component is not self-guarding. | **F.W4** note — gate the separators with their groups in the D-6 edit. |
| **C-20** | Publish gate (`hasContour`) ≠ precondition (`imageSlug && contour`): silent no-op click in a state with no live constructor (R-8). | **F.W4** note (INFO per R-8); opportunistic live check only. |
| **C-26** | No gate measures this dock's controls: gallery.spec asserts exactly the one named control; the axe keystones are fixme'd or exercise the other dock; the inactive layer is `visibility:hidden`/`inert`, which axe skips — six `button-name` criticals invisible to CI by construction. | **F.W4** — the gate re-enable rides the cure wave (sibling family of fr-AnimationControls C-29; distinct surface, not re-booked there). |
| **C-27** | Peer breach (`value.js@0.13.0 invalid: "^0.10.0 || ^0.11.0"`), reproduced by this seat (nth run). Component-altitude INFO. | fold → **F.W1** census §1 pins + G33 — ONE identity with fr-BasisCanvas C-8 / fr-AnimationControls C-1; never re-booked per component. |
| **C-25** | Zero value.js and zero keyframes.js consumption — this file's F.W2 migration cost is zero; its two `--viz-amber` CSS reads join the F.W2 colours denominator. | **NO-WAVE-OWNER** observation (spec-authoring input); the colors.ts identity stays with census W.L5. |
| **L-14 / C-23** | `dockExpanded` dangles across the dock's own unmount (setup-scoped watch emits no final `false`); the stale `dock-centered` window is real statically. | **F.W4** — `onScopeDispose` emit or parent reset; paint-window observability → SS-13. |
| **L-18** | This dock's `toggleFullscreen` is the sole entry into the occluded-inline-canvas redraw state (IntersectionObserver measures viewport intersection, never occlusion). | **fold by reference → fr-BasisCanvas BC-10/C-9** (banked MAJOR, F.W3/W4 there) — this record adds only the entry-point fact; not re-booked. |
| **M-9** (banked; reader-DU) | The state this dock's only-open emit creates has an unnamed exit: FullscreenViewer's close Button carries no `aria-label` and an aria-hidden glyph (seat-verified :110-112) — C-1's defect propagated into the destination. | **F.W4** — rides D-1's family into FullscreenViewer. |
| **M-10** (banked; reader-LC) | Systematic L-axis line-citation drift against the dirty `VisualizationView.vue` (live: AnimationControls :235-237, EditorControlsDock :238-248 vs L's :239-241/:243-251; plus `:collapse-delay` at EditorControlsDock **:56** vs L-24's ":53" — this seat's third instance) while D and C cite live-exact. No claim died from it; a corpus whose warrant is `file:line` receipts should not drift one axis against a dirty file. | **NO-WAVE-OWNER** — fleet-method evidence for SS-3/SS-4 (anchor-resolution discipline against dirty WTs). |

**Full-coverage note**: D-13/D-14 fold into the D-5 row; L-12/C-22 fold into the R3-7a identity guard; L-13 into C-9; L-15 into D-10/C-12 (+C-20); L-16/C-19 into the authoring row; C-18 into D-8 (+K-5); C-28 into D-4's F.W5–W8 rider; X-11 rides D-1's routing. Every corpus row leaves with exactly one disposition, original ids preserved.

## Killed-claims register (10)

| # | killed claim | refutation (adjudicated) |
|---|---|---|
| **K-1** | **L-20 (MAJOR)** — "the dock runs a 4Hz forced-layout luminance observer over the animating canvas"; §5.2b's three-dock census extension; the commissioned SS-13 frame-cost probe | Dead at the first link — the observer never starts. dock.js:596 declares `autoLuminance: { type: Boolean }` with no default; Vue coerces the absent Boolean-typed prop to `false`; :605's `n.autoLuminance !== !1` evaluates `false !== false` → `ye()` never invoked (both readers; re-sealed by this seat's :583-601 read). All three fourier docks omit the prop, so the census extension falls too. The refutation is self-inflicted: the same L axis proved this exact trap for `startCollapsed` at S-9, declared two lines above in the same literal, and reasoned from the .d.ts prose one row later. **The SS-13 frame-cost probe is STRUCK from the probe list, not scheduled.** What survives is the inverse, banked as M-4. |
| **K-2** | **C-14 (MINOR)** — "`animate-pulse` is ungated under `prefers-reduced-motion`" | glass ships the unlayered `!important` PRM blanket (a11y-overrides.css:6-16, `[data-allow-motion]` itself overridden at :26-30 — "accessibility is absolute"); reachability sealed end-to-end by this seat (style.css:3 → styles index → utilities.css:19). The D axis found and recorded this refutation at §6.1 BEFORE publishing; the C axis published the row anyway — the corpus contradicts itself and the tree sides with D. Both readers killed it; ratified. |
| **K-3** | **L-24's differentiator** — "unlike both siblings this dock inherits the substrate default (dock.js:172, 2500ms)" | dock.js:470 resolves `collapseDelay ?? 2e3` and GlassDock passes the resolved value into `useDockState` (:607) — the :172 default is structurally unreachable from any GlassDock. Effective delay = 2000ms, byte-identical to the siblings' explicit (and therefore redundant) bindings. L's own S-8 states "default 2000" — the document self-contradicts. Both readers; seat-sealed. Residual re-filed at the three-dock scope (roster row L-24-residual). |
| **K-4** | **L-19 (BLOCKER) as booked** — "the stage carries zero controls … `hasData` is false and stays false forever … the only recovery destroys the user's work"; and reader-DU's CONFIRMED-with-correction booking of the same row | Seat re-derivation (R-1): the keyed `panel-swap` remounts ContourSettings, whose `immediate:true` watcher fires `runCompute()` in exactly the post-save state — `beginCompute()` makes `hasData` truthy within the ~0.2s transition window, so nothing is permanent, and the "recovery" needs no user action at all. The falsifier was run with the wrong probe (`grep "() => store.contour"` cannot see an `imageMeta`-keyed rescue across a keyed-Transition remount). Novelty false: inv-19's fixme books the store half in the same file the axis read for C-26. **DU's confirm-with-correction is OVERRULED on seat evidence** — DU found the destroying watcher and applied it to reload only. The true defect is banked as M-1 (the conclusions invert: the stage heals; the work dies). |
| **K-5** | **D-8's stated proof** — "0.5rem of clearance vs a ≥2.5rem panel — decidable from source" (stage-relative) | The panel is portaled to `<body>`; floating-ui measures the viewport, not `.canvas-stage`'s inset. The conclusion survives on the seat's viewport arithmetic (R-2: ~72px header-fixed clearance vs ~110px panel), but the stage-relative template must not be reused for other floating surfaces in this corpus. Both readers agree the premise was wrong; DU's corrected arithmetic ratified with the seat's own derivation. |
| **K-6** | **C-6/L-4's "all four arms silently replaced" / "overflow-wrap suppression is dead"** (carried by reader-DU) | Cascade-layer precedence resolves per DECLARATION: the local block never names `display`/`grid-column`/`align-self`, so the wrap arm applies in full and the grid arm's placement survives (its geometry does not); only the vertical arm is fully neutralized. LC's kill ratified by this seat's reads of :106-112 + layer-group.css + overflow.css:166-168; **DU's cell corrected**. The row's headline survives (roster row D-6). |
| **K-7** | **L-3's lucide sub-reasoning** — "defaultAttributes.js … the svg contributes nothing to the name either" (mechanism) | Wrong file: Icon.js composes `...!slots.default && !hasA11yProp(props) && { "aria-hidden": "true" }` (seat-verified :41) — the seven glyphs are affirmatively REMOVED from the a11y tree, not merely mute. The C axis's correction is the carried form; conclusion strengthened. |
| **K-8** | **C-4/D-12's framing of `.dock-separator` as a 7.0.0 "definition-absent surface" / F.W1 break-budget row** | Category error: the class is the live paint source for the `DockSeparator` primitive at BOTH pins (installed 4.0.0 layer-group.css:23-62; producer 7.0.0 `dock/index.ts:32` still exports the primitive, compiled to `div.dock-separator` — both seat-verified). Nothing breaks at the bump; the real defect is today-live (D-6) and F.W1 must not budget it as a version-forced touch. DU's kill ratified; LC's provenance caveat retired by the seat's producer read. |
| **K-9** | **D-2's unqualified "touch-dead"** | Engine-dependent: the trigger binds `onFocus` (seat read), and Chrome/Android moves focus to a `<button>` on tap — opening the card — while iOS Safari does not. The BLOCKER stands on the unconditional keyboard leg (`onMounted` tabindex strip); the touch leg is carried QUALIFIED with both engines owed to SS-13. LC's qualification adopted. |
| **K-10** | **C-13's upgrade cell** (reader-LC) — "observable today, not merely latent … rendered today on ANY touch device" | **Seat-minted.** The coarse-media glyph split can only render where the popover OPENS, and this corpus's own D-2 proves the open path is `excludeTouch`-wrapped: on touch-primary iOS the panel never opens, so the split never renders there; it is live only via the Android tap-focus route or coarse-primary devices with attached pointers. One reader upgraded a row using a premise the same corpus's BLOCKER defeats — the same cross-row reconciliation failure shape as K-2. The row survives MAJOR on the channel severance. |

## Superlatives — with provenance (L-18 runs both ways)

1. **`:start-collapsed="true"` (:41) is load-bearing, and only measurement shows it** — all three axes and both readers independently; re-sealed by this seat (dock.js:594 `{type: Boolean}` no default; :474's `?? !0` unreachable; no `withDefaults` path in :583-601). Omitting the prop would mount the dock EXPANDED over the artwork. The corpus's best row — and, per M-2, the same prop pins the keyboard-sealed resting state: correct consumer engineering sitting on a producer a11y gap. Both facts ride the same relay letter.
2. **`keep-dock-open` (:44) is the one substrate contract this composition genuinely needed, and it took it** — the ref-counted hold + the `data-glass-dock-portal`/`-owner` click-away stamps (seat-verified in the chunk); without it the 2000ms idle-collapse retracts under an open popover. Both readers; contrast the producer's own named silent no-op sites.
3. **Correct altitude, proven by enumeration** — zero store/api/router/composable imports, zero `defineExpose`, zero prop mutation, zero lifecycle hooks/timers/observers (L-S2's grep → 0, re-run in spirit by this seat's read); all seven props consumed, six of seven emits payload-free; 132 lines against the parent's ~490. The defects are contract and substrate-boundary defects, not architecture defects — which is why five of the cures are DELETIONS (`DockSeparator`, `aria-pressed`, the glyph utilities, `:disabled`, the dead CSS block). Both readers, unanimous.
4. **The dock never re-renders from the animation clock** (reader-LC, adopted) — none of the seven props is clocked; combined with K-1, the steady-state cost over the 60fps instrument is the `backdrop-filter` alone. The render-path axis never credited it; LC checked it because L-20 claimed the opposite.
5. **`DockIconButton` merges the consumer `class` prop rather than dropping it** (reader-LC's negative-result check, adopted; `:is()` cohort seat-verified) — had the prop been swallowed, all five `is-active` bindings were dead and the component's state feedback a BLOCKER. Also the third independent refutation of fourier's own stale raw-findings.json:2968 cell ("is-active has NO backing paint") — false at installed 4.0.0; the finding must not be carried forward as written.
6. **`--viz-amber` is a real, contrast-adjudicated app token consumed in both arms** (reader-DU's sharpened form) — fill AND glow re-base on the dark arm; named precisely because fifteen lines above, the same author mis-based the separator on raw `--foreground` instead of the `--surface-tint-15` rung (D-6): one right, one wrong, in one 25-line style block.
7. **The collapse idiom is consumed, not hand-rolled** (C-S6; both readers) — `fit-content` + `:start-collapsed` + `#collapsed` is exactly the sanctioned surface; no invented width knob, no fight with the resize spring (contrast fr-AnimationControls D-1's `--animation-dock-max-width`). The slot's CONTENT is a separate defect (L-22/M-7); the mechanism choice is right.
8. **The corpus's self-kill discipline is real — and its reconciliation gap is now measured**: ten axis hypotheses died by their own falsifiers, and three of those negative results were load-bearing here (the PRM bracket, the `inert` stamp, GlassDock's touch handlers). But two of the three were mis-read as credit: §6.1's PRM kill was published anyway by the C axis (K-2), and §6.3's `inert` finding is the direct proof of the BLOCKER all three axes and one reader missed (M-2). The discipline finds the evidence; the fresh-instrument seat exists because the corpus does not always believe it.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **M-1** — the destructive re-extract round-trip: drive edit → drag points → Save → exit edit; confirm the contour visibly swaps back to the machine extraction, measure whether the compute spinner masks it, and check whether the server's contour asset retains the hand-edited version (operator recoverability). The chain is fully static; this is a data-loss claim and earns one live confirmation.
2. **M-2** — Tab-walk corroboration: from page top, the collapsed dock receives focus nowhere and never expands. Corroborative only (the static negative is total).
3. **D-2 / C-2 touch legs, both engines** — iOS Safari (expect: never opens) vs Chrome/Android tap-focus (expect: opens via `onFocus`). Do not carry "touch-dead" unqualified (K-9).
4. **D-8** — `[data-side]` readback on the opened popover at 1024px+ and a short viewport; corroborates the seat's 72-vs-110 arithmetic.
5. **M-3** — whether the teleported fullscreen editor's pointer handlers land under the fullscreen layout, and whether the focus trap interferes.
6. **D-4 / L-1 / C-3 server arm** — whether `POST /api/visualizations` dedupes on `(image_slug, contour_hash)`; client-side duplication is proven regardless.
7. **C-13 / D-5** — the rendered glyph split (in-dock ~1.17× vs portaled :root-resolved) on a device where an open path exists (K-10's scoping: Android or coarse-primary hybrid).
8. **M-7** — the `opacity-40` Pencil's composited contrast over `--glass-bg-dock` on a live canvas (1.4.11 decimal; direction is forced).
9. **M-6** — the 150ms popover-before-tooltip lead on a slow hover across the cluster (cheap to capture alongside 4).
10. **L-24-residual** — collapse-vs-publish wall clock, re-scoped to all three docks.
11. **L-14 / C-23** — whether a paint lands in the stale `dock-centered` window across the `v-if` remount.
12. **D-16** — the specular-over-dot perceptual magnitude on hover (stacking order is decided; the cost is not).
13. **STRUCK / DEMOTED**: L-20's frame-cost trace (K-1 — the observer never starts); D-20's density probe (latent, no `density` passed — not an SS-13 slot at HEAD); C-20 and C-11's state-frequency probes (opportunistic only, per R-8).

## Identity guards (folds by reference — never re-booked)

- **C-27 peer breach** = census §1 pins + G33 — ONE identity with **fr-BasisCanvas C-8** / **fr-AnimationControls C-1**; this record is another reproduction, not a booking.
- **R3-7a** (intake, CARRY→F.W3): the 6 Tooltip callsites — L-12/C-22 ride as riders; **X-11's naming-leg re-scope** (D-1's routing) amends the carry in place; identity stays with the intake.
- **R6-8** (intake, CARRY→F.W5): C-28 folds — this dock is the UI head of the non-isolable client↔operation seam; D-4's server-arm rider sequences after the join split.
- **R5-7** (intake): L-9 (undercount) + L-23 (overcount) are the class's two arms — scope note to F.W4's audit budget; identity stays with the intake; L-23's producer half rides the GLASS-RELAY.
- **fr-BasisCanvas BC-10/C-9** (banked MAJOR): absorbs L-18 whole (occlusion-blind IntersectionObserver + fullscreen dual-draw); this record adds only that CanvasControlsDock:93 is the sole entry into the state.
- **fr-AnimationControls** (banked): its K-11 already established CanvasControlsDock as the view-toggle owner behind the fullscreen backdrop — M-3 here is the edit-mode face of that family, a distinct identity (unwired second EDITOR vs undeclared props); its C-29 (fixme'd keystones) is the sibling of C-26 here — same F.W4 gate-re-enable family, distinct surfaces.
- **fourier inv-19** (`e2e/visualization-ux.spec.ts:204-235`): the repo's own booking of M-1's STORE half (no recompute after save; cure scheduled at fourier W3) — cited, not re-booked; the remount-triggered destruction is the new fact this corpus adds, and the W3 cure must be written against it (recompute FROM the saved contour, never re-extract).
- **fourier raw-findings.json:2968**: contradicted at installed 4.0.0 (superlative 5) — must not be carried forward as written.
- **GLASS-RELAY consolidation (owner edict — one letter, not five)**: (i) the Boolean-props no-defaults cohort (M-4 + S-1's `startCollapsed`, with R-4's consumer-impact warning); (ii) keyboard affordance for the collapsed shell (M-2 producer half); (iii) the role-blind stagger ladder (L-23) + the sibling's single-wrapper inverse; (iv) GlassDock's empty emits surface (C-10); (v) the portal token-scope question (C-13 rider). The 4.0.0 hover-popover touch exposure (D-2) is already cured at the 5.0.0+ union — the relay notes the exposure window only.

## DISSENT — preserved, not ruled away

1. **Reader-DU's L-19 booking, overruled but preserved** (K-4): DU independently found the destroying watcher — the correct load-bearing fact — and stopped at the reload application, confirming the axis's BLOCKER with its permanence claim intact. The failure shape is a falsifier corrected without re-walking the mount chain it feeds: one keyed-`<Transition>` read away from inverting the row, as LC did. Recorded as fleet evidence beside the citation-inheritance events of prior apotheoses — a corrected cell can still anchor an uncorrected conclusion.
2. **Reader-LC's MAJOR on the view-dot cluster** (R-7) preserved: "a grouped affordance whose two members silently disagree about persistence, surfaced by a dot that is on for every fresh session, is a trust defect, not an ornament defect." If SS-13's pass shows users toggling ghost off and losing it per reload as a repeated gesture, the severity re-opens.
3. **Reader-DU's component-grain MAJOR on autoLuminance** (R-4) preserved: the app-level framing ("the adaptive-legibility feature the token was minted for is silently dead over the exact live-canvas case") is true; this seat booked the generalized producer row because one relay item that retires L-20 AND fixes four props beats two rows that each fix one.
4. **M-2's severity vs the fr-AnimationControls D-9 precedent**: that record booked MAJOR-until-live-keystroke for a partial keyboard hole; this record books BLOCKER statically for a total one (no handler exists to fire). The distinction is argued at R-5; if the fleet wants a uniform severity floor for a11y rows pending live confirmation, this is the row to argue against — the seat holds that a statically-total negative needs no keystroke.
5. **Adopted-not-recomputed cells, disclosed**: the D-5 coarse-pointer decimals (1.17×/2.925rem/0.385) were adopted from the two readers' agreeing independent arithmetic and checked for direction only; the C-9 seven-axis table was spot-checked at three axes, not seven; the C-3 gallery/workspace store line-reads were re-read in ranges, not whole-file. Every OTHER load-bearing cell in this record carries a seat instrument.
6. **The K-2/K-10 shape, named for the fleet**: twice in one corpus, a row was published (C-14) or upgraded (C-13) on a premise a sibling row of the same corpus had already defeated. Cross-row reconciliation inside one axis pass is the cheapest audit step this program is not yet paying for; the SS-3/SS-4 spec authors should require each axis to grep its own kills before publishing.

## Closing verdict

**ADJUDICATED — DEFECTIVE, five BLOCKERs as booked: 43 rows confirmed (5 BLOCKER · 14 MAJOR · 14 MINOR · 10 INFO) including 10 missed rows banked (7 reader-DU · 4 reader-LC, one merged) · 10 kills registered (1 seat-minted against a reader upgrade, 2 reader positions overruled on seat evidence, 1 SS-13 probe struck) · the corpus's second BLOCKER (L-19) replaced by the data-loss row it was pointing at, and a keyboard-sealed resting state banked from evidence the corpus itself had already collected and mis-filed as credit · routings: 4×F.W1 · the R3-7a naming-leg rider at F.W3 · the balance F.W4 · 2 riders to F.W5–W8 · one consolidated five-item GLASS-RELAY letter · 3 NO-WAVE-OWNER records — all agglomeration input to the forming X·F specs (M-25), stamping nothing.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
