SEAT RECEIPT — served model id: `claude-fable-5` (Fable 5), FRESH FABLE ADJUDICATOR (M-23/L-18), observed in this seat's own system prompt, not inherited.

# PaletteCardSkeleton — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Date**: 2026-08-06 · **HEAD**: `3ecf0267` · branch `tranche-u` · **Subject**: `demo/palettes/browser/card/PaletteCardSkeleton.vue` (124 L) + its ink root (`demo/styles/utils.css` skeleton block), its twin (`ShadowPalette.vue`), and its three seats (`BrowsePane.vue:43-53/:125-131`, `ExtractWorkbench.vue:102-107`).
**Drift disclosure**: `git diff --stat c654824e..HEAD` over the cone (the subject + ShadowPalette + BrowsePane + ExtractWorkbench + utils.css + foundation.css + browse-loading.spec.ts) is **empty** — every line anchor below resolves at both the corpus HEAD and today's.
**Corpus**: `audit/components/PaletteCardSkeleton/challenge-{C,D,L}-*.md` (r2, all three seats Opus 5[1m], live-probed 2026-07-28). **Re-readers adjudicated**: design-and-usage (reader-1) · library-and-code (reader-2), both hostile, both static-only. Both were challenged back: one reader-1 confirmation cell is struck by reader-2's kill and the strike is **extended** (R-1), one reader-1 mechanism cell is struck by this seat's own bytes (R-2), reader-1's "irreconcilable" magnitude ruling is **superseded** (R-3), one reader-2 corroboration cell is struck (R-4), and the two readers' Extract-seat missed rows are reconciled from their mutual tension (R-5).
**Motion law**: `audit/codex-provenance/motion-quarantine.md`. The rows governing this exact producer primitive are **TRUE** rows — M-10 (reduce ELIMINATES the scan; the declaration is absent, not clamped), M-12 (inert props + the bundle's only `no-preference` gate), M-14 (inert attrs byte-exact) — and are carried as such. The M-03/M-11 pattern (a missing in-file PRM branch is not a defect under central governance) is applied at K-1. No FALSE row is carried anywhere in this record.
**Wave authority for routing**: tranche X ratified (`docs/tranches/X/waves/W0..W11`). **X.W7 owns every file in this cone by name** (W7.md §4 file list: PaletteCardSkeleton.vue, ShadowPalette.vue, BrowsePane.vue, ExtractWorkbench.vue all `modify`); X.W7.g owns the plate mass + the Extract caption + the o9 re-rule (G19/G20, k=16 height budget measured on BOTH components); X.W7.a's G3 (`strictTemplates: true`) makes the dead-attr class a compile error; X-W8 G-9 owns the dead Skeleton-axis class census (currently naming only AdminListSkeleton.vue — see PCS-1); X-W1 owns the browser-suite/oracle slate; X-W10 has **no skeleton row** (its canon binds as law, not as owner). Sibling adjudications banked in this registry and cross-booked here, never re-booked (anti-rename): **AdminListSkeleton A-2/A-3/A-4** (announcement chain house-wide · dead-attr class → X-W8 G-9 · dead ink seam split GLASS-OWNED + NO-WAVE-OWNER) and **PaletteCard PC-15/PC-18** (three-home shell drift → X.W7.c G11 · the ghosts model a nonexistent component → X.W7.c), both dated 2026-08-06.
**Arbiter's own instruments (all static + python/node over bytes; zero browser tooling per law — probe parsimony)**: full source reads of PaletteCardSkeleton.vue (whole, 124 L), ShadowPalette.vue (whole, 115 L), PaletteCard.vue:14-50/:90-110/:135-145/:190-200, BrowsePane.vue:40-60/:120-135/:200-210, ExtractWorkbench.vue:85-166, utils.css:30-90, foundation.css:665-700 (the WHCM roster); producer bytes: `Skeleton.vue.d.ts` (one prop), `glass-ui.css` skeleton run extracted whole at char 23444 (rules + `@media` arms pasted below) **with my own brace-depth probe: depth 0 at 23444 and exactly one `@layer components` block at char 2503 — the skeleton rules are UNLAYERED**; `styles/index.css` import list read whole (`@layer theme, base, components, utilities;` · `@import "../glass-ui.css"` with **no** `layer()` · `@import "./components.css" layer(components)`); `styles/components.css` head + my redeclaration census (`--radius:` 1 · `--radius-lg:` 1 · `--radius-sm:` 1 · `--radius-md:`/`--radius-badge:`/`--radius-card:`/`--radius-pill:`/`--radius-input:` **0 each**); `styles/theme/radius.css` `@theme` block; `WatercolorDot.vue.d.ts:30/:43` (`ghost` variant); glass-ui `package.json` version 7.0.0 vs `package.json:83` `^7.0.0`; repo censuses run fresh: `surface="glass"` = 14 lines / 6 files (PaletteCardSkeleton ×4 · AdminListSkeleton ×4 · AdminTagsPanel ×1 (v-for 5) · Markdown ×3 · PalettesPane ×1 + AdminUsersPanel ×1 = the two valid DialogContent receivers), `grep -rliI skeleton test/` = **0**, `ls demo/ui` = 19 dirs / `find demo/ui -name '*.vue'` = 0; e2e read whole: `browse-loading.spec.ts` (45 L, four assertions) + `fixtures/browse-palettes.ts` (`routeBrowsePalettesDelayed`, `PAGE1_COUNT = 50`); provenance: X-GU.md:61 (the 4.0.0 `surface="glass"`/`--skeleton-glass-bg` contract verbatim), N.W18.md:506 (U20a books the consume), N.W14.md:25/:33 (the shell-state ruling) + **N.W14.md:309** (Lane B books "the `--radius-input` 4-vs-8 drift DECIDED + documented" — never executed), CONTRIVANCE-REGISTER.md:27 (the false "valid" cell verbatim); DEFECT-LEDGER re-anchored **by content, never by line** (109,106 L today): AX CDP capture → **:24468**, Extract occupant trace `[[0,"ghost"],[2894,"card"]]` → **:13386**, the main-thread/isProcessing window → the C-14 row at ~:50686, WHCM inversion → **:6882/:6885**, the contradictory ink row → **:5603**, `field_1440 cards all {h:100}` → **:24972**, name-prohibited wrappers → **:23352/:26448**; my own arithmetic: hsl(38 26% 95%) → rgb(246,243,239) (the `--muted` light derivation that decides R-4).

## Scoreboard

**22 adjudicated rows CONFIRMED-family (1 BLOCKER · 11 MAJOR · 7 MINOR · 3 INFO), of which 4 are IDENTITY-FOLDS to rows already banked in this registry (PC-15, PC-18, A-2/A-3/A-4 — never re-booked) and 11 were MISSED by the three-axis corpus and adopted from the re-readers (plus one arbiter-found docblock contradiction and one stale-superlative relay) · 15 claims KILLED in the register (11 reader kills ratified, 2 of them extended; 1 magnitude conflict RULED; 3 new kills by this seat — one against each reader, one against the corpus's own banked ledger row) · 9 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

Blocker-weight headline: **the file is a design document about a renderer that does not exist.** Every prop and every custom property it writes addresses a producer API glass-ui 7.0.0 does not ship (`Skeleton.vue.d.ts` = `{ class }` — one prop; all five demo-minted seams = 0 hits in the whole dist); both `variant` registers are pixel-identical; the certified ink never reaches a pixel and paints inverted in both schemes; the stagger has zero phase by construction; and the one e2e oracle stays green when the entire template body is replaced by an empty marker div. This was a **ratified 4.0.0 producer contract** (X-GU.md:61, N.W18.md:506 — provenance verified by this seat) that a producer major dropped with no consumer sweep — the correct root, and the correct shape for the cure.

## Challenges to the readers (the incredulity seat's actual work)

- **R-1 — the "byte-identical stagger" is dead, and reader-1 inherited the corpse.** Reader-2 killed L-2's evidence row; my own source read sustains the kill byte-for-byte: ShadowPalette wraps **every** delay in `.toFixed(2)` and writes the real `animationDelay` property (:56, :63, :67, :77); the subject writes raw IEEE-754 template strings into the dead `--skeleton-shimmer-delay` (:50, :60, :66, :77 — C's live capture showed `"1.3399999999999999s"`). Same five coefficients, different formatter, different mechanism. **Extension**: reader-1's D-6 CONFIRMED cell repeats "the four stagger expressions are byte-identical (:50,60,66,77 vs ShadowPalette :56,63,67,77)" — a verbatim inheritance of the axis's false cell inside a hostile re-read. Recorded as a citation-inheritance event about the fleet (the ColorSpaceSelector DISSENT-1 precedent); reader-1's D-6 verdict survives on the other six drift rows.
- **R-2 — the radius mechanism: each reader holds half; reader-1's cell is struck, reader-2's model adopted whole.** My bytes: (a) `styles/index.css` imports `../glass-ui.css` with **no** `layer()`, and my brace-depth probe shows the skeleton rules sit at depth 0 outside the file's single `@layer components` block — so `.skeleton{border-radius:var(--radius-input)}` is UNLAYERED and beats every Tailwind utility (reader-1 right, and this is the D-3 cascade). (b) But reader-1's claim that `components.css` carries an "UNLAYERED `:root{--radius:0.25rem}`" is **false**: the import is `@import "./components.css" layer(components)` — layered, and that is exactly why it wins: `components` outranks `theme` in the declared order, so the producer's own compiled artifact clobbers `--radius`/`--radius-lg`/`--radius-sm` (my census: redeclared 1× each) while leaving `--radius-md`/`--radius-badge`/`--radius-card`/`--radius-pill` untouched (0 each) — predicting D-3's measured 4px/6px/9999px/16px table **exactly** (reader-2's missed #1, adopted at PCS-3). The conclusion (`--radius-input` = 4px app-wide) stands under both mechanisms; the relay letter must state the true one.
- **R-3 — the height magnitude is RULED, not irreconcilable.** Reader-1 filed the D-vs-C 120-vs-100 conflict as undecidable (K-7). Reader-2's arithmetic decides it: D-4's own component rows (strip 40 + metadata 56 + swatch absent) sum to 96 + 4px of 2px borders = **100px**, contradicting D's own 120 headline; C measured 100.00 with a coherent breakdown; DEFECT-LEDGER:24972 banks `field_1440 cards all {h:100}` from a third run; the skeleton's 150px closes exactly from source (40+40+68+2). Ruling: browse collapsed card = **100px, over-prediction +50.4%**, carried **PLAUSIBLE** (three concordant witnesses, no re-measure by this seat); the 120px/+25% figures are KILLED (K-7); L-3's 120 was a different route (`/#/palettes`) and survives only as a Library-route question (U-2).
- **R-4 — reader-2's own corroboration cell is struck.** Reader-2's C-2 row cites "DEFECT-LEDGER.md:5495/:5603/:19752 bank the same numbers from a third run." :5603 (re-read whole by this seat) banks the **opposite** for light: "light `--muted` = rgb(233,225,217) = `--well-bg` EXACTLY → 1.000:1 (invisible)". My derivation settles which is wrong: `--muted` light = `--neutral-1` = hsl(38 26% 95%) = **rgb(246,243,239)** ≠ rgb(233,225,217) — the ledger row measured the well twice (reader-1 caught this; reader-2 laundered the row into support). The dark cells of :5603 do corroborate. Partial-corroboration only; the :5603 light cell joins the relay list (PCS-18).
- **R-5 — the readers' Extract rows are in tension and are reconciled, not summed.** Reader-1's missed #1 assumes the skeleton mounts (and measures a ~22-24px caption collapse when it does); reader-2's missed #5 adopts the banked occupant trace saying it **never** mounts (`mode="out-in"` holds the entering branch until `isProcessing` has already flipped — DEFECT-LEDGER:13386, both engines). Both cannot fire on one frame. Reconciliation (PCS-12): the seat's comment ("same bones — a material change, not a layout jump"; "the caption carries the text for AT") is false in **both** branches — in the measured branch the KNOWN-IMMINENT register plus its announcement is dead code; in the slow-compute branch where it does mount, the swap deletes the plate's only readable status line and drops ~22-24px (the ghost branch alone wraps in `flex flex-col gap-2` + caption, ExtractWorkbench.vue:158-166; the skeleton branch is a bare component, :103-107 — my read).

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors re-resolve before any cure (D-19).

- **PCS-1 (D-1/C-1/L-1) · BLOCKER — every prop and custom property written to glass-ui's Skeleton addresses an API 7.0.0 does not ship; both registers are pixel-identical; ~22 of 66 code lines are inert.** Proof (mine): `Skeleton.vue.d.ts` = `{ class?: HTMLAttributes["class"] }`; compiled setup re-emits undeclared attrs to the DOM minus `role`/`aria-*`; dist-wide seam census 0/0/0/0/0 (`skeleton-glass-bg`, `skeleton-shimmer-delay`, `skeleton-shimmer-tint`, `pulse-aura-opacity-max`, `animate-ambient-pulse-easing`); the whole shipped rule set pasted in my instruments — no variant branch, no delay seam, no breath keyframe; consumer writes at :43/:57/:63/:74 (`surface`), :44/:58/:64/:75 (`:variant` — reader-2's off-by-one correction of L-1's ":45" sustained), :50/:60/:66/:77 (delay writes), :110-112, :120-123; `blockVariant` (:99) feeds only the dead binding. Provenance of the rot verified: X-GU.md:61 + N.W18.md:506 — a **ratified 4.0.0 contract**, un-swept at the 7.0.0 major. Blast radius (my census): 12 Skeleton sites / 4 files (+2 valid DialogContent receivers). Corrections travel with the row: 48 **elements** × 2 = **96** dead attributes on the browse wall (K-13); D's pasted evidence node is the Extract arm, not Browse (K-10); the consumer-side cure MUST ship its own WHCM + PRM arms (K-3) and the producer-side ask MUST NOT be `animation-delay` (K-4 — the seam cannot deliver the plate wave). **→ BUILD X-W7** (file owned by name; X.W7.a G3 `strictTemplates` makes the class compile-RED; the file rework deletes every dead construct) **+ cross-book X-W8 G-9** (the dead-Skeleton-axis class identity, = AdminListSkeleton **A-3**; G-9's census as written names only AdminListSkeleton.vue — the A-3 rider asked for AdminTagsPanel; this record adds **PaletteCardSkeleton ×4 and Markdown ×3** to the same widen note for the next conformance pass over W8; no re-spec here). The producer variant/seam question is ALREADY on the BH relay via **A-4's split** — cross-booked, not re-sent; PCS-5's topology correction rides that letter.
- **PCS-2 (D-2/C-2) · MAJOR — the certified `--skeleton-ink` never reaches a pixel; the bones paint `var(--muted)`, polarity INVERTED in both schemes.** Proof: structural (mine) — glass-ui hardcodes `background:var(--muted)`; `--muted` = `--neutral-1`, a fixed neutral constructed independently of `--well-bg` = `color-mix(in oklab, var(--card) 92%, var(--foreground) 8%)` (foundation.css:328), so no signed relation can hold; utils.css:36-43 names this component a consumer of the certified recipe (:57-61) — it is not one; both readers' token arithmetic concordant (light: muted rgb(246,243,239) LIGHTER than well rgb(233,225,217); dark: muted rgb(31,28,25) DARKER than well ≈ rgb(66,55,47)) and my own hsl derivation reproduces the light triple. The E1-R2 remediation (trigger 1.02:1) is live for ShadowPalette only. Exact painted values/contrasts: U-1. The **:5603 ledger correction** (R-4) travels with this row. **→ X.W7.g** (the wave touches both plates + o9; any surviving plate re-inks through the one live channel under X-W10 law) **+ cross-book A-4** (the utils.css false-prose half is A-4's NO-WAVE-OWNER row; the seam letter is A-4's relay — one identity).
- **PCS-3 (D-3 + reader-2 missed #1) · MAJOR — every declared corner radius loses to the producer, and the 4px value is itself a producer bug that clobbers the design system app-wide.** Consumer symptom (mine, R-2a): the unlayered `.skeleton{border-radius:var(--radius-input)}` beats `rounded-none`/`rounded-md`/`rounded-badge` unconditionally — the strip is five 4px pills with plate notches, the WatercolorDot ghost a hard square; the sibling's plain divs render what they declare (0px/9999px). Producer cause (mine, R-2b): `components.css` re-emits `--radius: 0.25rem`/`--radius-lg`/`--radius-sm` into `layer(components)`, outranking the `@theme` scale (`--radius: 0.625rem`) — so `--radius-input`/`--radius-button`/`--radius-lg` resolve wrong in **every consumer of glass-ui 7.0.0**; the untouched tokens (md 6px / badge 9999px / card 16px) are exactly D-3's "intended" column. Paper trail: N.W14.md:309 booked "the `--radius-input` 4-vs-8 drift DECIDED + documented" — never executed. **Producer half → GLASS-OWNED, standing BH relay** (a fond): components.css must not re-emit core scale tokens, or must import ahead of the scale — with the R-2 mechanism stated correctly. **Consumer half → X.W7.g** (the plate rework declares radii that can win, or stops declaring). Emission-step confirmation: U-8.
- **PCS-4 (D-4/C-3/L-3) · MAJOR — the ghost mispredicts the card it stands in for: a 68px swatch row the collapsed Browse card never renders, at the OTHER seat's 56px swatch size, +50.4% total height.** Proof (mine, static): PaletteCard renders `PaletteCardSwatches` under `v-if="expanded"`; BrowsePane passes `:expanded="pm.expandedId… === slug"` — false at mount — and no `swatch-class`, so the default `w-9 h-9 sm:w-10 sm:h-10` (40px) governs; the ghost hardcodes `w-12 h-12 sm:w-14 sm:h-14` (:76), byte-equal to ExtractWorkbench.vue:150's override; skeleton 150px closes from source. Magnitude RULED at R-3 (100px card, +50.4%, PLAUSIBLE → U-2). C-3's honest CLS negative (0.0000 in the 6-card fixture; `out-in` unmounts first) is preserved — the defect is **fidelity**, not measured shift. **IDENTITY-FOLD → PC-18 (X.W7.c)** — booked in this registry 2026-08-06 ("the loading ghosts model a component that does not exist"); this record adds the adjudicated magnitude and the phantom-row mechanism to that identity. The shell law is PC-15's G11 ("the skeleton mirrors the specimen, one shell source") — the N.W14 §A zero-drift ruling has a live X owner; no gap to flag.
- **PCS-5 (D-5) · MAJOR — the five-term stagger clock is inert: twelve blocks, one phase, structurally guaranteed.** Proof (mine): the four writes target a property with 0 dist hits; the producer's only animation declaration carries no `animation-delay`. Mechanism corrections bind: there is **no single bar** (K-5 — each block owns a private `::after` at `inset:0` translating ±110% of its own width), and therefore **no delay seam can produce the plate-crossing wave the file specifies** (K-4, promoted to a design fact): twelve phased local glints ≠ ":38 the plate develops left to right". Any revival requires a plate-level sweep element or a per-block property that composes (opacity — the sibling's mechanism), keyed on the **inline axis** (D-15's RTL constraint, carried as a revival constraint only, U-7). **→ dies with PCS-1 (X-W7); the topology correction rides A-4's BH relay letter.**
- **PCS-6 (D-6/L-2) · MAJOR — one plate, two hand-copies, drifted in every dimension that shows: ink polarity, radius, ink ladder (flat vs 100/60/40/30), strip separation (none vs `gap-px`), strip sizing mechanism (`width:100/count%` vs `flex-1 min-w-0`), and delay formatting (raw floats vs `.toFixed(2)`).** All divergences verified by this seat from source; the "byte-identical" evidence row is KILLED (R-1) and the .toFixed/width-mechanism rows (reader-2 missed #2/#3) are ADOPTED — the width one with its consequence: at non-divisor counts (3,6,7,9,11-14, all reachable from k 1..16) the rounded percentages under-sum and a sliver of plate shows at the strip's trailing edge; `flex-1` distributes exactly by construction. Both render from ExtractWorkbench's single `<Transition mode="out-in">`. **IDENTITY-FOLD → PC-15 (X.W7.c G11)**, the three-home shell drift row — this record adds the ghost-vs-ghost drift census to that identity.
- **PCS-7 (D-13/C-4/L-7) · MAJOR — four (six on load-more) identical `role="status"` regions, each with empty text and all-`aria-hidden` descendants; the hosts' own labels sit on name-prohibited generics.** Proof (mine): :35-36 on the root; BrowsePane `v-for="i in SKELETON_COUNT"` (4, :207) + `v-for="i in 2"` (:130); the producer hardcodes `aria-hidden="true"` and filters `role`/`aria-*` from consumer attrs — the announcement is structurally unreachable, not merely empty; wrappers :47/:126 are bare divs (ARIA 1.2 name-prohibited). Banked: DEFECT-LEDGER:24468 (CDP AX tree), :26448 (the generic-name row). The sibling states the correct split with its reason (ShadowPalette :36-40). Real-AT behaviour of inserted named regions: U-3-adjacent, but the cardinality + emptiness + zero completion signal are source-certain. **→ X-W7** (BrowsePane owner — the **M13** identity from the BrowsePane adjudication: one persistent polite region with real text, ghosts decorative) **+ cross-book AdminListSkeleton A-2** (the house-wide status-boundary/`aria-busy`/completion contract, NO-WAVE-OWNER at the formation boundary — this component's :23-31 doctrine comment is that row's third witness).
- **PCS-8 (D-8) · MAJOR — glass-ui ships, by name, the ghost of the exact species being ghosted (`WatercolorDot variant="ghost"`, d.ts :30/:43 — my read); the component hand-rolls a grey square whose radius it cannot even control.** Loading→loaded morphs a hard 4px square into an organic seeded blob; owner edict 4 binds. **→ X.W7.c under X-W10 law** (the ghost of a WatercolorDot is a ghost WatercolorDot, or the shell decides otherwise explicitly).
- **PCS-9 (D-9) · MAJOR — the metadata band is a false prediction: no `justify-between`, no `min-w-0`, no trailing action ghost, so the menu Button materialises from nothing at handoff.** Verified from source (:55 vs PaletteCard `px-3 py-2.5 flex items-center justify-between gap-2 min-w-0` + the icon-only `size="sm"` menu trigger). **→ folds into PC-18's identity (X.W7.c).**
- **PCS-10 (D-11 row 3) · MAJOR — `overflow-hidden` at plate level re-introduces the exact clip the loaded card documents as a rendering defect (S.W5-10/S-15-A), on the arm composited while animating, in both ghosts.** Verified: PaletteCard.vue:16-18 comment + :19 omission + the strip carrying its own corner radius; both ghosts carry the clip (:34; ShadowPalette :43). **Binding cure caveat (reader-2 missed #4, ADOPTED)**: the clip is load-bearing HERE — the ghost's `rounded-none` full-bleed strip has no own corner radius, so deleting the clip without moving it inward (strip carries `rounded-t-card`, the card's own solution) trades one defect for another. All three axes' cures are under-specified on this point. **→ X.W7.g** (both files; the caveat travels).
- **PCS-11 (C-7/D-14/L-8) · MAJOR — the only oracle over this component is vacuous: zero unit tests (my grep: 0), and the 45-line e2e spec asserts marker-visible / no-spinner / cards-arrive / no-console-error — the named empty-div mutation keeps all four green.** Read whole by this seat. This is the mechanism that let the file rot through a producer major under a hard, green CI typecheck (fallthrough attrs are always type-legal). Correction: the held-state fixture ALREADY EXISTS and this spec already uses it (`routeBrowsePalettesDelayed`, K-8) — the real gap is that the assertions are presence-only and the **visual matrix** never consumes the fixture (L-9 rescoped). **→ X-W1** (oracle slate: ink-certification, geometry-parity, one-region-with-text assertions — C-7's three cures adopted as the spec; plus the one-line `[surface],[variant]` DOM-attr sweep gate for the whole class) **+ partial cover noted**: X.W7 G3 (compile) and G20 (height) each catch a slice.
- **PCS-12 (reader-1 missed #1 ⊕ reader-2 missed #5, reconciled at R-5) · MAJOR — the Extract seat's contract is false in both branches: the comment promises an in-place material swap with AT text, but the measured branch never mounts the skeleton at all (out-in holds the entering branch past `isProcessing`'s window — DEFECT-LEDGER:13386, both engines), and the branch where it would mount deletes the caption + `gap-2` (~22-24px) and replaces readable idle text with a structurally-empty status region.** Consequences: the `shadow` register — the component's DEFAULT — has no seat observed to paint, so even a landed producer `breath` would light nothing; and the working state has strictly less AT-readable text than the idle state. Occupancy re-confirmation: U-3. **→ X.W7.g** (owns ExtractWorkbench's caption — which W7.g deletes "under every option" — plus both plates and the o9 re-rule; the transition-mode hold and the "what text survives while working" decision are flagged BY NAME for the wave open, since deleting the caption alone would cure the geometry limb by worsening the announcement limb).
- **PCS-13 (C-5) · MINOR — 48 concurrent infinite `will-change: transform` compositor animations for one wait (72 with load-more); the cap was placed on plates, not blocks-per-plate (BrowsePane's own :204-207 comment reasons the budget and caps the wrong dimension).** Count structural (4 × (2·count+2)); the bill: U-4. **→ X.W7.g G20** (the mass cut is the cure's chassis; the one-sweep-per-plate decision rides PCS-1's rework).
- **PCS-14 (C-6/D-12 + reader-1 missed #3) · MINOR — `count` is an unvalidated number driving TWO geometries with opposite scaling laws: a partition denominator in the strip (cells shrink) and a list length in the swatch row (rows grow), guarded only by a slider in another file.** C-6's Vue `renderList` analysis (no RangeError at `Infinity`) verified against the corpus; the coupling itself (reader-1's row) is the design error neither axis cure touches. D-12's k=16 slab numbers: U-6 (G20 measures exactly this at wave time). **→ X.W7.c/g** (the shell's per-row layout law dissolves the coupling; G20's k=16 budget is the gate).
- **PCS-15 (L-5 + reader-1 missed #6 + arbiter extension) · MINOR — the utils.css specimen/seam block feeds nothing, twice over, and its two docblocks contradict each other.** Mechanism 1 (L-5): `--skeleton-glass-bg`/`--skeleton-shimmer-tint` = 0 readers anywhere; `.specimen-seg`'s `@supports` relative-color branch reachable only via the dead `.skeleton-seg`. Mechanism 2 (missed #6): the carrying class is gated `variant === 'developing'` (:46), so the walk could never run at the Extract seat even with a live seam — and the `@supports` branch has never executed in any browser on any route. **Arbiter extension (mine)**: utils.css's walk docblock claims "the ShadowPalette true-empty ghost (STILL) reads it as fill" while the component's own :103-109 comment says that consumption died at T.W6.5/R12 — the component is right (ShadowPalette reads `--skeleton-ink` directly, my read of :103-114); two certification docblocks in one cone assert opposite consumer graphs. **→ cross-book A-4** (utils.css consumer half, NO-WAVE-OWNER at the formation boundary — no X wave's bounds include utils.css for this purpose; the component-side `:46`/`--i` writes die with PCS-1 in X-W7).
- **PCS-16 (L-6) · MINOR — the scoped block overrides two producer motion tokens that exist nowhere (0 dist hits, 0 demo hits outside this file), framed as "the published seams"; were it live it would be the edict-5 per-instance override.** **→ dies with PCS-1 (X-W7).**
- **PCS-17 (reader-1 missed #4) · MINOR — the handoff materialises a whole ELEMENT of the depth grammar, not merely a heavier token: PaletteCard ships `<span class="cartoon-cast" aria-hidden="true">` as a required child of `cartoon-surface` (:28-30, my read); the ghost has no such child and hand-rolls `border` + `shadow-cartoon-sm`.** All three axes compared tokens only. **→ X.W7.c** (PC-15's G11 owns the shell's depth grammar; the ghost mirrors it — noting PC-15 already discharged the sm-vs-none dueling-cure question in favour of the ratified G11 mechanism, so no shadow decision is made here).
- **PCS-18 (L-10 + R-4 + arbiter relay) · MINOR — three banked records assert falsehoods this corpus disproves, and must be RELAYED, never silently amended:** (a) CONTRIVANCE-REGISTER.md:27 "PaletteCardSkeleton ×4 → Skeleton (valid)" — falsified by the one-prop d.ts; the receiver check proved existence, not declaration; the adopted codemod population undercounts by ≥12 (+12 more counting `surface`). (b) DEFECT-LEDGER:5603's light cell ("--muted = --well-bg EXACTLY → 1.000:1") — falsified by my hsl derivation and both live seats; the row measured the well twice, and if consumed it hides the polarity inversion that is the real defect. (c) BrowsePane.md:117's superlative ("producer seams consumed rather than forked") — stale against the 7.0.0 truth this corpus established: the seams are dead; the praise survives only as praise of the *doctrine*. **→ registry hygiene at the next conformance pass; NO-WAVE** (this record is the relay's evidence).
- **PCS-19 (D-7, RESCOPED) · MINOR — the loading register has no slow arm, no stalled arm, and no designed route to failure (Extract's error line is a bare div vs Browse's `EmptyState variant="error"`), all source-certain; the two dramatic state rows ("ghosted 4s after decode", "never renders under a 517ms block") are UNSUBSTANTIATED AS FILED** — the cited ledger lines hold unrelated content; the never-renders limb survives via the relocated C-14 row (~:50686: `isProcessing` excludes the main-thread-blocking phase) and folds into PCS-12; the 4s-ghost row is unlocated by content anywhere (U-9). Severity drops MAJOR→MINOR with the evidence. **→ X-W7 flag-at-open** (the slow/stalled design decision belongs to the seat rework; no spec names it today).
- **PCS-20 (D-16/C-9/L-11, falsity limb only) · INFO — the file's prose asserts, in the present indicative, behaviour that has never executed; the sharpest false sentence is the one no axis quoted (reader-1 missed #5, ADOPTED): `:97-98` "The breath animation lives on each block, so its register needs no producer seam" — the only sentence claiming the dead register is SELF-sufficient, defeating even the check the other comments invite.** `wc -l` = 124 (L's 125 off by one, its interior cites correct). The ratio limb is KILLED (K-9 — house idiom). **→ dies with the PCS-1 rework (X-W7); the header collapses to the true design.**
- **PCS-21 (L-4, inventory limb) · INFO — demo/ui/ is 19 alias-only barrels, 0 .vue (my count), the skeleton's one import edge among them; the causal limb is killed (K-6).** The extirpation identity is ALREADY FOLDED (ColorSpaceSelector L-4 → W-HYGIENE; ApiOfflineChip AP-11 → X-W8) — **cross-book X-W8, never re-booked**; the input-subpath nuance (the barrels are the only record of which glass subpath a primitive lives on) travels with that identity.
- **PCS-22 (C-8 + reader-2 missed #6) · INFO — render-path nits: the boolean `:class` binding (:46); the numeric `--i` custom property (:49) written unconditionally in BOTH registers though its only (dead) reader is developing-gated — the dead-line inventory a cure executes against must include :49.** **→ dies with PCS-1 (X-W7).**

## Killed-claims register

1. **K-1 — D-10 ("in both accessibility environments the component degrades to… precisely the resting appearance of ShadowPalette; loading and empty become the same picture") — KILLED, both limbs.** Forced-colors: INVERTED — the producer ships a dedicated WHCM arm the subject inherits (`@media (forced-colors:active){.skeleton{opacity:.18;background:canvastext}…:after{display:none}}` — my extraction), while ShadowPalette has NO arm and its tier-1 roster covers only `.shadow-swatch` (foundation.css:679-694, my read) — three of its four species collapse to Canvas-on-Canvas (DEFECT-LEDGER:6882/:6885: "identical, contrast 1.00:1… The sibling PaletteCardSkeleton in the same Transition slot does not collapse"). In WHCM the loading register is the **survivor**. PRM: both go static but measurably different (radius, polarity, ladder, gap) — "the same picture" is false on D's own D-6 table. Surviving observation, recorded and de-fanged per motion law: under `reduce` the pending signal is shape-only because the producer ELIMINATES the scan — M-10/M-12 (TRUE) name this correct producer-owned behaviour, and the M-03/M-11 pattern bars filing the absent in-file PRM branch as a defect. ShadowPalette's WHCM collapse is that component's row, banked at :6882 — not re-booked here.
2. **K-2 — D-11 rows 1-2 (the cast shadow as THIS component's canon violation) — KILLED, misattribution sustained.** The cited clauses bind the entity Card; the loaded card is the heavier offender (`cartoon-surface` md + an explicit cast child). The class question is PC-15's, already discharged into X.W7.c G11's ratified mechanism — filing it here would manufacture a component finding from a route-level target. The elevation-step FACT survives inside PCS-6/PCS-17.
3. **K-3 — C-1/L-1's consumer-side cure as prescribed ("paint the bones the way the sibling does") — KILLED as unsound.** It trades two working accessibility arms (the producer's WHCM arm + the bundle's only PRM elimination gate) for none — the sibling's mechanism carries neither (bare `background:` rules that collapse in WHCM; unconditional `animate-pulse`). D's own §4 lists the producer arms under "what is actually sound" while §2/§6 hold up the sibling — a self-contradiction inside one report. Binding rider on PCS-1: any consumer cure ships its own `@media (forced-colors: active)` arm and PRM posture.
4. **K-4 — the producer-side cure all three axes prescribe (`animation-delay` seam) — KILLED: the seam cannot deliver the design.** Each block's `::after` is `inset:0` of its OWN box translating ±110% of its OWN width (my bytes); phased local glints can never compose into the plate-crossing wave the source specifies. The mechanism that CAN is a uniform per-block opacity swing (the sibling's) or one plate-level sweep element. The relay letter must ask for one of those. Promoted to a design fact at PCS-5.
5. **K-5 — D-5's mechanism cell ("a single 5-second bar sweeping all twelve boxes: strobing") — KILLED.** There is no single bar; twelve private in-block sheens run in phase. The zero-phase finding survives; the error matters because it is what makes K-4's wrong cure look right.
6. **K-6 — L-4's causal limb ("the barrel is the proximate cause of L-1") — KILLED.** X-GU.md:61 + N.W18.md:506 (verified) prove the author consumed a ratified producer contract at the 4.0.0 pin; the true mechanism is C-1's producer-major-no-sweep. Corroborated: Markdown + AdminListSkeleton write the same dead props through the same barrels with no demo CSS at all. The barrel INVENTORY survives (PCS-21).
7. **K-7 — the 120px / +25% magnitude (D-4, L-3) — KILLED; the conflict itself RULED at R-3.** D's own rows sum to 100; C measured 100.00; the ledger banks 100 twice (:24972, :19770-class). Real delta +50.4% (PLAUSIBLE, U-2). Reader-1's "irreconcilable, at most one is right" is superseded — the corpus could adjudicate itself, arithmetically.
8. **K-8 — L-9's cure ("the matrix needs a fetch-delay shim") — KILLED as prescribed.** `routeBrowsePalettesDelayed` exists and the browse-loading spec already consumes it (both read whole by this seat); D's §5 names it. The narrower true gap — the VISUAL matrix does not consume the fixture — survives inside PCS-11's X-W1 slate.
9. **K-9 — the comment-RATIO limb of D-16/C-9/L-11 — KILLED.** 43% prose is the house idiom (the sibling runs 34%; PaletteCard and ExtractWorkbench carry the same register); owner edict 1 concerns responsibility, and L's own negative row concedes "not a god module". The FALSITY limb survives (PCS-20). Stripping rationale comments is not licensed by this corpus.
10. **K-10 — D-1's pasted DOM node as Browse-attributed — KILLED (evidence-provenance only).** The node carries `variant="breath"` and lacks `specimen-seg skeleton-seg`; Browse passes `variant="developing"` (:51, :130 — my read) which renders `variant="shimmer"` + both classes. The node is the Extract arm or a manual mount; the finding survives intact on C's capture and the d.ts. Recorded beside D's own disclosed probe artifact (§5) as a provenance-hygiene datum.
11. **K-11 — five of D's DEFECT-LEDGER line citations (:7499, :10440, :18252, :10044, :18707) — KILLED as addresses; the evidence re-anchored by content where it exists.** Today: :7499 = SortableJS; :18252 = document outline; :10440/:10044/:18707 = separators/fences. Relocated by this seat: AX tree → :24468; occupant trace → :13386; the main-thread/isProcessing window → the C-14 row (~:50686). NOT located anywhere by content: the "plate ghosted 4s after a successful decode" row (U-9). Law restated: ledger citations re-anchor by content, never by line (the ledger is 109,106 lines and growing).
12. **K-12 — L-2's "byte-identical stagger expressions" row — KILLED (reader-2), and reader-1's D-6 repetition of it recorded as citation-inheritance (R-1).** The divergence (`.toFixed(2)` vs raw floats) is itself adopted evidence in PCS-6 — the drift the thesis predicted, refuting the thesis's own evidence cell.
13. **K-13 — C-1's "48 non-conforming attributes" arithmetic and L-1's `:45` line cite — KILLED as cells (reader-2, sustained by my reads: 48 elements × 2 attrs = 96; `:variant` sits at :44).** Neither moves a verdict; both would fail a mechanical re-proof.
14. **K-14 (NEW, this seat) — reader-1's D-3 mechanism cell ("components.css carries an UNLAYERED `:root{--radius:0.25rem}`… unlayered `:root` wins") — KILLED.** The import is `layer(components)` (my read of index.css); the clobber wins by layer ORDER over `theme`, not by unlayeredness. The conclusion (4px) stands; the mechanism in any relay letter must be reader-2's (R-2).
15. **K-15 (NEW, this seat) — reader-2's C-2 corroboration cell ("DEFECT-LEDGER :5495/:5603/:19752 bank the same numbers") — KILLED as written.** :5603's light cell asserts the two tokens IDENTICAL at 1.000:1 — a contradiction laundered into support (R-4); the dark cells corroborate. The ledger row itself is the wrong one (my derivation) and rides PCS-18's relay.

## Superlatives (with provenance)

- **demo/styles/utils.css:44-56 — the E1-R2 ink certification docblock** (both readers, concordant; construction re-derived by reader-1 and spot-checked by this seat's arithmetic): records the measured failure, diagnoses the root cause (ink not certified AGAINST its ground), re-mints the token as a bounded tone-step OF the ground so scheme-adaptivity falls out by construction. The recipe is correct; only its delivery channel is dead — when PCS-1 lands, the comment needs no rewriting. The tragedy row of this corpus.
- **PaletteCardSkeleton.vue:23-31 — the announcement-split doctrine** ("loading ≠ empty splits on ANNOUNCEMENT, not motion") (reader-1): the correct axis, stated sharper than most design systems manage; the cardinality is wrong (PCS-7) but the principle is why the sibling is `aria-hidden` and this root is not. AdminListSkeleton's adjudication independently flagged the same pair as "the house argues both sides on purpose" — the doctrine exists; the deviation is legible as a defect because of it.
- **ShadowPalette.vue:100-102 — ink and motion kept orthogonal** ("the ladder steps fade INTO the plate by a color-mix step (never element opacity — D6); the pulse's opacity swing is MOTION on top of the ink") (reader-1): the discipline that makes a per-block stagger compose into a plate-wide wave — the exact property the subject's translated-gradient mechanism lacks (K-4).
- **PaletteCard.vue:16-18 — the NO-overflow-hidden ruling** (reader-1): a hard-won rendering insight, correctly acted on in the card, and the ruling both ghosts break — which is what makes the break diagnosable (PCS-10).
- **foundation.css:665-696 — the two-tier forced-colors policy** (reader-1; roster read by this seat): for a color tool, blanket WHCM substitution destroys the product's content; the named color-display roster + UA-default chrome + restored `outline: Highlight` is a sophisticated answer — and the artifact that let D-10 be refuted statically (K-1).
- **The producer Skeleton's a11y hardening** (reader-2): hardcoded `aria-hidden="true"` plus active filtering of consumer `role`/`aria-*` — a producer that structurally forbids consumers from making decorative bones announce, which is why PCS-7's regions are provably empty rather than merely noisy; PRM-gated at `no-preference` (the bundle's only such gate, M-12) with real reduced-transparency and forced-colors arms.
- **challenge-C's epistemics** (both readers): the LAN-address defeat of `isLoopbackHost()` to reach the real transport path read-only; the measured-then-retracted CLS inference ("cls_since_skeleton: 0… reported as fidelity, not as a measured regression"); `Number.isInteger(Infinity)` checked against Vue's source. Paired with D's disclosed probe artifact ("recorded as a hypothesis, not a defect") — the corpus's hygiene standard, met unevenly elsewhere (K-10, K-11).
- **vite.config.ts:37-50 / eslint.config.js:221-250 / card barrels' named re-exports** (reader-2): structural enforcement over documented intent — machine-derived anchored self-aliases, a lint-enforced feature seam with a human-readable message, tree-shaking-aware named re-exports with the reason recorded. The posture whose absence at the Skeleton seam is exactly PCS-11.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1** — the exact painted colors/contrasts (light `rgb(246,243,239)` on `rgb(233,225,217)` @ 1.169:1; dark `rgb(31,28,25)` on `rgb(66,55,47)` @ 1.469:1). Polarity is structural (PCS-2); the numbers are two concordant live seats + partial ledger corroboration, re-run by no reader and not by this seat (probe parsimony).
- **U-2** — the collapsed loaded-card height at a stated viewport, both routes: RULED 100px on `/#/browse` (R-3); L-3's 120px on `/#/palettes` remains a Library-route question. One re-measure with the collapsed state pinned settles both, and no downstream figure may be carried as measured until it runs.
- **U-3** — Extract seat occupancy: DEFECT-LEDGER:13386 says the skeleton NEVER seats (`out-in` hold, both engines, throttled + unthrottled) — banked, not re-run. PCS-12's design ruling does not depend on which branch fires; the occupancy fact feeds the wave's transition-mode decision.
- **U-4** — the compositor BILL of 48 `will-change: transform` layers (72 load-more). The count is structural; whether it is material on target devices was measured by no seat.
- **U-5** — the literal computed values (`distinctAnimDelays: ["0s"]`, `distinctBackgrounds` length 1, `elementAnimationName: "none"`). Structurally certain from the producer CSS; the literals are the live seats' readings.
- **U-6** — D-12's k=16 slab (278px plate / 27.8px cells / 196px swatch rows), measured on ShadowPalette and transferred by class-identity; X.W7 G20 measures exactly this on BOTH components at wave time.
- **U-7** — D-15's RTL limb: sound reasoning, moot while both mechanisms are dead; carried solely as the revival constraint in PCS-5 (inline-axis keying), not as a live defect.
- **U-8** — whether Tailwind emits glass-ui's `@theme` radius block into `@layer theme` in THIS build (the one emission step of PCS-3's producer mechanism not directly in the bytes; the measured 4px/6px/9999px/16px quartet corroborates it exactly). One `getComputedStyle(document.documentElement).getPropertyValue('--radius')` settles it.
- **U-9** — the "plate ghosted 4s after a successful decode" ledger row: cited by D, unlocatable by content in the current 109,106-line ledger by either reader or this seat. The D-7 state row resting on it stays unsubstantiated; if the row exists under different words, it re-enters through PCS-19's wave-open flag.

## Closing verdict

**ADJUDICATED — BLOCKER SUSTAINED.** A 124-line file whose every seam addresses a producer that stopped listening at the 7.0.0 major: the two registers are one, the certified ink paints inverted in both schemes, the stagger has no phase, the radius intent loses to a producer artifact that is itself clobbering the design-system scale app-wide, and the single oracle cannot tell — while the ratified provenance (X-GU U20/U20a) proves this was once a real contract, not a fiction. Every row leaves with exactly one home: the dead-API class dies under X.W7's `strictTemplates` + the file rework with the X-W8 G-9 census widened by name; the geometry/twin/depth rows fold into the already-banked PC-15/PC-18 identities (X.W7.c G11 — the N.W14 shell ruling finally has an owner); the plate mass, ink, clip and Extract-seat contract land in X.W7.g with their binding caveats (move the clip inward; the caption's AT text must survive its own deletion); the announcement cardinality rides BrowsePane M13 (X-W7) with the house-wide contract cross-booked to AdminListSkeleton A-2 (NO-WAVE-OWNER, flagged); the oracle slate goes to X-W1; the producer radius clobber and the corrected motion-topology ask go out on the standing BH relay; and three banked records (CONTRIVANCE-REGISTER:27, DEFECT-LEDGER:5603, BrowsePane.md:117) are corrected by relay, never amended. 22 rows confirmed, 15 claims killed — three of them the re-readers' own — and 9 residue rows handed to the live pass. Nothing here stamps a wave; everything here is runnable by the session that does.

— end of apotheosis. The challenger corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
