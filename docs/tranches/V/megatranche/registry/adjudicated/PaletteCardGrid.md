# PaletteCardGrid — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (the incredulity seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt.
**Date**: 2026-08-04 · **HEAD**: `5f328fb8` · branch `tranche-u` · **Subject**: `demo/palettes/browser/card/PaletteCardGrid.vue` (53 L) + its two consumer seams.
**Corpus**: 3 r1 challenge files (C 6 rows · D 6 rows + state-matrix rider · L 5 rows = 17 accusations). **No r2 exists for any axis** — AXIS-PROMOTIONS governance does not apply; every row adjudicated directly against live source. Provenance: all three files REPORT-AUTHORED, evidence count **0** (HYDRATION-LEDGER.md:267-269, closure-manifest-2026-08-03) — thin batch, UNVERIFIED-UNTIL-RE-PROVEN. The corpus receipt is honest where its reasoning is not: SHA-256 `e4ee08d8…4d7107` re-hashed by this seat at HEAD — **byte-identical to the live file**; the 1-53 line range exact.
**Re-readers adjudicated**: R-DU (design-and-usage) · R-LC (library-and-code). Both were attacked before either was adopted; both verdict-level disagreements (C-6/D-6, cardRefs severity) ruled below; every load-bearing cell re-derived with this seat's own instruments.
**Arbiter's own instruments (all static; zero browser tooling — law + probe parsimony)**: whole-file reads of PaletteCardGrid.vue, EmptyState.vue, useFilteredList.ts; PalettesPane.vue:60-209; BrowsePane.vue:28-147; PaletteCard.vue:1-55; usePaletteStore.ts:40-174; usePalettePorts.ts:100-125; **the BLOCKER's full receipt chain re-walked**: `node_modules/@vueuse/integrations/dist/useSortable.js` whole (`defaultOptions.onUpdate` survives an `onEnd`-only options object; `moveArrayElement`'s `_valueIsRef=false` arm splices the SAME object synchronously, defers the re-insert to `nextTick`, never writes back) + `node_modules/sortablejs/modular/sortable.esm.js` `_onDrop` (`update` dispatched :2002-2007 BEFORE `end` :2022-2028, same synchronous frame); installed glass-ui 7.0.0 dist bytes: `components/card/styles.css` (`@utility cartoon-surface { position: relative; border-width: 2px; box-shadow: var(--shadow-cartoon-md) }`), `styles/tokens/shadow.css` (`--shadow-cartoon-md: -3px 3px 0 …, -5px 5px 0 …, -7px 7px 0 …` — LEFT+DOWN); demo/styles/foundation.css:119/:362-369/:483-487/:518-519 (only base+hover overridden to 8px RIGHT+down; `-md` stays producer-left; the containment-colocation tombstone at :485-487); eslint.config.js:220-256 (G-DEMO-3b object read whole); e2e/smoke/walk.spec.ts:71-72; e2e/smoke/oracles/o9-shadow-palette.spec.ts:180-215; censuses re-run: `role="listitem"` → **0 repo-wide**; `role="list"` → exactly PaletteCardGrid.vue:3 + the :7 comment; `emptyAction` → declaration only (:28-29); `PaletteCardGrid` consumers → exactly PalettesPane + BrowsePane (+2 barrels, 2 e2e); `reorder|sortable|drag-handle|dragTo` in e2e/ → **0**; `sticky` in demo/palettes/browser/ → **0**; `ls demo/@` → does not exist; **`.github/workflows/` = {ci.yml, deploy-pages.yml, release.yml}, zero `e2e|smoke|playwright` references in ci.yml** (settles R-DU residue 4 — the o9/walk oracles are NOT CI-hard); DEFECT-LEDGER corroborations spot-checked verbatim at ~:1752 (first-drag repro), ~:2058 (filtered-drag repro), :24288 (CDP AX probe: `childRoles:["article","article","article"], listitemChildren:0`, `AX listitem nodes anywhere: 0`), :24311 (green-keeping mutations incl. `role="list"→"grid"`) — 4 of 4 cited rows reproduced, remaining ledger cites adopted as corroboration; **adjudicated-registry identity sweep** (BrowsePane.md M9/M10/m2/R-1/K8/M13, EmptyState.md ES-1/ES-4/ES-9/ES-11/ES-16/ES-23/U-2, ActionFeedback.md AF-13, AdminPane.md L-11, ColorSpaceSelector.md L-5(a)) — a sweep NEITHER re-reader ran, and it re-homes five of their "missed" findings (anti-rename law).
**Motion/PRM**: motion-quarantine.md:275 lists PaletteCardGrid "C · D · L — no motion content"; no M-xx FALSE row applies to any verdict below; no verdict rests on a motion/PRM assertion.

## Scoreboard

**17 axis accusations: 5 CONFIRMED in some form (C-5, C-6≡D-6 as one rescoped identity, D-4, D-5-narrowed) · 12 KILLED.** Adjudicated roster: **13 rows — 1 BLOCKER · 4 MAJOR · 6 MINOR · 2 INFO** — of which **9 appear in no axis file** (2 wholly new to the program, 7 already booked at sibling adjudications or the ledger and cross-booked here, never re-booked). The gravest defect in the cluster was missed by all three axes AND walked past by R-DU, who cited its measured consequences as evidence for a kill and did not look up.

Blocker-weight survivor: **PG-1** — drag-to-reorder is functionally corrupt on the exact `$el`/useSortable seam the component's celebrated comment exists to protect: the move is double-applied against a detached computed snapshot, so the first drag after any wall render produces the wrong persisted order.

## Ruled disagreements (the incredulity seat's actual work)

| id | R-DU | R-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-6 / D-6** | KILLED ("two of D-6's three named casualties do not exist… hits zero of the live ones is not coverage") — then re-filed the caster clipping as its OWN missed MAJOR | CONFIRMED MAJOR, proven concrete (the caster amputation) | **CONFIRMED-RESCOPED — R-LC's verdict class wins; R-DU's casualty-roster refutation adopted as the rescope** | A row's identity is its mechanism, not its example list. The axis named the true mechanism — unconditional `contain: content` with unpriced paint consequences (PaletteCardGrid.vue:50-52) — and guessed wrong casualties (sticky → 0 hits in browser/; positioned affordances → all portaled: SwatchHoverMenu.vue Teleport-to-body + reka-ui PopoverPortal). Killing the row and re-raising the same mechanism's real casualty as one's own find is verdict-laundering; the correct verb is RESCOPE. The casualty is real and this seat re-derived it whole: `content` ⊇ `paint` clips the subtree at the padding edge; the grid has zero padding (:4) and `grid-cols-1` stretches every card flush to the clip boundary; PaletteCard.vue:19 `cartoon-surface` consumes `--shadow-cartoon-md` (−3/−5/−7 X, +3/+5/+7 Y, glass-ui dist bytes, my read); foundation.css:366-367/:518-519 re-point only `--shadow-cartoon`/`-hover` — `-md` keeps the producer LEFT+DOWN cast; 12px `gap-3` absorbs interior bottom legs only → every card loses its whole left leg, the last card both. **IDENTITY**: ≡ BrowsePane.md **M9 · D5-03** (already booked → X-W7, with the second clip ancestor `.pane-scroll-fade` this cluster's readers both missed). Cross-booked, not re-booked. |
| **cardRefs retention** | absent | missed-MAJOR ("unbounded retention sink… per-keystroke × page size") | **MINOR as booked (≡ BrowsePane m2 → X-W7.d); R-LC's severity dissent PRESERVED** | The identity already exists: BrowsePane.md m2 ("write-only registry… detached instances accumulate and operation verdicts can be delivered into unmounted cards, silently" ⊙ runtime-core:1762) → X-W7.d, and AXIS-PROMOTIONS-2026-08-03:23 merges ActionFeedback L-3+L-4 into the same repair. R-LC's mechanism re-verified by this seat (`el && (cardRefs[…] = el)` at PalettesPane.vue:84 / BrowsePane.vue:94 short-circuits Vue's unmount-null), and its growth-rate arithmetic is right — but the cure is identical at any severity (the registry dissolves when feedback leaves the card at X-W7.d), so re-grading a booked row buys nothing. Dissent recorded, identity kept. |
| **D-5 severity** | MAJOR (filtered-zero limb) | MAJOR (narrowed, cure rejected) | **CONFIRMED-NARROWED, carried at the booked grade: ≡ EmptyState.md ES-1, BLOCKER** | Both readers confirmed the same single limb and both correctly rejected the axis cure. The identity is already adjudicated at ES-1 ("the plate states a falsehood over a filtered-zero collection, and the contradiction is in the accessibility tree" — PalettesPane.vue:77 vs :79-80, BrowsePane.vue:83 vs :85-86, + AdminUsersPanel) → X-W7. One identity, one grade; this cluster adds the grid-side receipts and changes nothing. |

## Adjudicated defect roster

Line anchors are HEAD `5f328fb8`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

- **PG-1 · BLOCKER — drag-to-reorder double-applies the move against a detached array snapshot: the first drag after any wall render persists the wrong order.** (No axis row; R-LC's find; R-DU cited the measured consequences inside its L-5 kill and never booked them.) Chain re-derived whole by this seat: (1) PalettesPane.vue:183 hands `useSortable` the UNWRAPPED `pm.filteredSaved.value` — evaluated once at setup — with `onEnd` only; (2) the vendored default `onUpdate: (e) => moveArrayElement(list, …)` survives the spread (useSortable.js, my read) so BOTH handlers run; (3) sortablejs `_onDrop` dispatches `update` (:2002-2007) BEFORE `end` (:2022-2028) in one synchronous frame; (4) `moveArrayElement` with a non-ref list takes `array = toValue(list)` — the SAME object — and `splice(from,1)` runs synchronously (the re-insert deferred to `nextTick`, and `list.value = array` never runs); (5) the captured object is a computed's cached PLAIN array — usePaletteStore.ts:51-55 `savedPalettes = computed(() => getStore().value.palettes.filter(…))` (`.filter()` on a reactive array yields a non-reactive array) returned verbatim through useFilteredList.ts:10 when the query is empty — so the splice dirties nothing; (6) `onEnd` (PalettesPane.vue:190) re-reads `pm.filteredSaved.value` → the SAME mutated array, now missing the dragged palette → :191's `ids.splice(evt.oldIndex,1)` removes the WRONG palette → `reorderPalettes` receives a false order and its append loop (usePaletteStore.ts:163-165) tacks the never-mentioned dragged palette onto the END. Worked example verified: [A,B,C,D], drag A 0→2 ⇒ store [C,D,B,A]; correct [B,C,A,D]. The nextTick splice then lands in an orphan (the store replaced the array at :166). Live corroboration by another seat: DEFECT-LEDGER ~:1752 (seeded 4-palette repro, "correct from the 2nd [drag] onward") with a cure sketch this seat endorses (own the move in `onUpdate`, never split writers across `onUpdate`+`onEnd`). Zero test coverage: `reorder|sortable|drag-handle|dragTo` in e2e/ → 0. **→ X-W7.d** (owns PalettesPane.vue + usePaletteStore.ts by file bounds and "one mutation, one owner, one visible result" by goal; the defect is NOT yet named among W7.d's four DAG repairs — **named-addition rider at wave open**, and the W7.c/d card rebuild must not re-mount Sortable over a derived list).
- **PG-2 · MAJOR — a drag while a filter is active corrupts the order of the palettes the user cannot see.** `onEnd` enumerates only the PROJECTION (`filteredSaved`), and reorderPalettes' totalizing append (usePaletteStore.ts:157-165) moves every non-matching palette to the tail — a gesture over a projection silently rewrites the total order. Independent of PG-1 (survives any fix to the snapshot bug). Live corroboration: DEFECT-LEDGER ~:2058 (6-palette repro: "the 4 non-matching palettes have moved from positions 1-4 to 3-6 and the new order is persisted"; two cure shapes sketched — withdraw drag under filter, or translate the projection move into the base order). **→ X-W7.d**, same rider as PG-1.
- **PG-3 · MAJOR — `role="list"` is malformed in BOTH states: populated children are `role="article"`, and at true-empty the sole owned child is the plate's `role="status"`.** (D-4 CONFIRMED + the empty-state limb no axis saw.) PaletteCardGrid.vue:3; PaletteCard.vue:22; EmptyState.vue:28; `role="listitem"` → 0 repo-wide (my grep). ARIA 1.2 `list` Required Owned Elements = `listitem`; axe `aria-required-children` (serious). The :7-8 comment is wrong twice — `article` is not `listitem`, and neither `list` nor `article` is a landmark. Measured: DEFECT-LEDGER:24288 (`listitemChildren:0, setsize:null`), :16687 (`childRoles:["status"]` on /#/browse). Any fix to the populated state alone leaves the empty state violating. **IDENTITY**: ≡ EmptyState.md **ES-9** (booked → X-W7, already carrying both limbs). Cross-booked; AT-degradation mode → residue U-4.
- **PG-4 · MAJOR — the grid's containment amputates the card's signature cartoon caster: every card loses its left cast, the last card loses both; one wall, two shadow treatments.** (C-6/D-6 as rescoped above.) The two decisions are provably aware of each other and still collide: PaletteCard.vue:16-18 declines `overflow-hidden` explicitly to protect the caster; the grid clips it one level up (:50-52). **IDENTITY**: ≡ BrowsePane.md **M9** → **X-W7** (scope 7 / unit **X-W7.c** "shadow follows the rounded silhouette… at the card root", G11), with M9's rider intact: the cure must audit the SECOND paint-clip ancestor (`.pane-scroll-fade`, `contain: layout style paint`). Pixel delta → residue U-5 (corroboration only; the mechanism is spec-determined).
- **PG-5 · BLOCKER (as booked) — filtered-zero wears first-run copy at both call sites: N saved + non-matching query ⇒ header badge N, delete-all-N trigger live, and a plate saying "No saved palettes yet. Add colors above…".** (D-5 narrowed; the axis's four other states are pane-owned and REFUTED as grid obligations — BrowsePane.vue:43-54 skeletons / :61-78 error+Retry are siblings of the grid in one out-in Transition, exactly the altitude the cure belongs at.) PalettesPane.vue:77 vs :79-80; BrowsePane.vue:83 vs :85-86; usePalettePorts.ts:112-114 → useFilteredList.ts:10. Ledger: :4847, :5117. **IDENTITY**: ≡ EmptyState.md **ES-1** → **X-W7**. The cure is one honest branch of copy keyed on query/filter state at the two hosts — never a `CollectionSurfaceState` machine inside a 53-line leaf (both readers, ratified; KISS edict).
- **PG-6 · MINOR — the single-root invariant is load-bearing at an external `$el` seam and its only named guard is mislabeled; the seam is additionally untyped, so the regression is silent at compile time AND test time.** (C-5 CONFIRMED, sharpened by both readers; + R-LC's `defineExpose` limb folded in.) PalettesPane.vue:180-181 `(sortableGridRef.value as any)?.$el as HTMLElement | undefined` — the `as any` is unnecessary (`$el` is on ComponentPublicInstance) and the `as HTMLElement` unchecked (`$el` is typed `any`; a fragment root yields a Comment at runtime that still satisfies the annotation). walk.spec.ts:71-72's comment claims "the B.W1 single-root invariant" but asserts only `getByRole("list").last()).toBeVisible()` — insensitive to root arity; a leading sibling comment breaks `$el` and keeps it GREEN. A mislabeled gate is worse than an absent one: it retires the question. Corroborating: BrowsePane.md's P-1 decider compiled PaletteCardGrid **single-root in BOTH dev and prod** (the comment-inside-div trick works today), and DEFECT-LEDGER:24311 lists `role="list"→"grid"` among green-keeping mutations. Cure: `defineExpose` a typed root element (useTemplateRef) + a root-arity assertion **→ X-W7.c** (grid file bounds); the walk.spec comment correction + CI-hardening of the smoke project ride **X-W1** (re-gate; ledger 24311's standing ask — this seat verified ci.yml carries zero e2e steps today).
- **PG-7 · MINOR — the `emptyAction` CTA slot is plumbed end-to-end and consumed by nobody; the ratified ruling recorded in-file ("The CTA slot survives on the caption", :19-20) survived in the primitive and died at every call site — the invitation is a text-only dead end while the sibling error plate carries a real Retry.** `grep emptyAction` → declaration only (:28-29, my census); PalettesPane.vue:78-80 and BrowsePane.vue:84-86 pass text only; BrowsePane.vue:68-77 fills `#action` on the error arm. The `v-if="$slots.emptyAction"` guard is inert either way (EmptyState.vue:64 renders the slot unconditionally). **IDENTITY**: ≡ BrowsePane.md **M10 · D5-04** + EmptyState.md **ES-16** → **X-W7.g** (the CTA decision made explicitly, not dropped again). C-2/L-4 analysed this slot's typing and neither checked for a consumer — the corpus's cleanest inversion.
- **PG-8 · MINOR — `cardRefs` is a write-only registry in both grid consumers; unmounted card instances are retained and can silently receive operation verdicts.** (Ruled above.) **IDENTITY**: ≡ BrowsePane.md **m2** → **X-W7.d**; R-LC's severity dissent preserved in DISSENT.
- **PG-9 · MINOR — the list has no accessible name, none can be supplied (no prop, no attrs passthrough contract for it), and at 1440 two structurally identical unnamed lists mount side by side — while the decorative loading containers beside them carry `aria-label` on name-prohibited `generic` roles.** (R-DU's find; NEW at the list-name limb.) PaletteCardGrid.vue:2-6/:38-44 (my read: no `aria-label`, no prop); both consumers pass none; BrowsePane.vue:47/:128 label bare divs (ARIA 1.2 §5.2.8.4 name-prohibited). Measured: DEFECT-LEDGER:24288 AX list `name:""`; :5387 (dual 512px panes at 1440); :26448/:72660 (prohibited-name hits). The wrapper-div prohibited-name half already stands as a fragment inside BrowsePane.md **K8**; the list-name gap itself is unbooked — **→ X-W7** (the c/d rebuild names the collection at the grid/pane seam). Dual-AX-tree observation → residue U-3.
- **PG-10 · MINOR (cross-book only) — the G-DEMO-3b lint the barrel cites as "enforces it standing" is inert: dead `demo/@` globs, a ban pattern spelled in the deleted `@components` alias, and `demo/palettes/**` absent from the file glob.** Re-verified by this seat (eslint.config.js:232-239/:246-248; `ls demo/@` → absent). **IDENTITY**: fact-identical to ActionFeedback.md **AF-13** (→ **X-W8** dead-glob deletion + the re-point at `demo/palettes/**` = **NO-WAVE-OWNER named residue**), ≡ AdminPane.md L-11 ≡ ColorSpaceSelector.md L-5(a). One identity, three prior bookings — nothing new to book; recorded here so this cluster's citation of the seam inherits the truth.
- **PG-11 · INFO — the empty↔populated swap is the one unchoreographed state change on the Browse route and the one a search keystroke triggers most often: it pops inside the single `key="wall"` branch while the route's out-in Transition choreographs only boot/error.** (R-DU's find; NEW — distinct from BrowsePane M13/K7/R-1, checked.) BrowsePane.vue:40 vs PaletteCardGrid.vue:22 `v-if="empty"` (no Transition, no key change); the :30-39 comment disowns exactly this "hard v-if POP". Not a motion-quarantine row (PaletteCardGrid = "no motion content"; no M-xx carried). **→ X-W10** (canonical motion); perceptual severity → residue U-2.
- **PG-12 · INFO — `min-h-[120px]` is the only un-narrated decision in a file that narrates everything else; the arbitrary reach shadows an existing scale utility (`min-h-30` = 120px at default `--spacing`), the floor is inert in every state that matters, and its one active case (a single short card) is also the one state where the last card's bottom caster escapes PG-4's clip.** (R-DU's find, DEMOTED one notch by this seat: DESIGN.md:385's letter bans `[var(--…)]` reaches specifically — this is a numeric arbitrary value, so the house rule applies in spirit, not letter; the missing inline rationale is the substantive residue.) **→ X-W7.c** (the grid file is rebuilt there; the floor must be re-derived or deleted with a comment).

## Killed-claims register

Twelve axis rows die. Refutations re-verified by this seat where load-bearing; both readers' kills ratified — none was weak enough to revive.

- **C-1 ≡ D-1** ("`empty` can contradict the default slot / render both or neither") — **KILLED.** Both and only consumers derive `empty` from the array they iterate, in adjacent lines (PalettesPane.vue:77/:83; BrowsePane.vue:83/:93 — my reads). The contradiction has no live producer, and the prescribed cure is unsound: `$slots.default?.()` yields opaque VNode output (v-for Fragments, dev comment nodes); a leaf that guesses collection size from slot VNodes is a defect, not a closure. Residue: INFO-tier API hygiene, absorbed by the X-W7.c rebuild.
- **C-2 ≡ L-4** ("`$slots.emptyAction` checks existence, not actionability" / "couples rendering to slot presence rather than a typed action model") — **KILLED.** True about Vue, inert here: EmptyState.vue:26/:64 render `<slot name="action"/>` unconditionally, so the guard changes nothing; and the slot has ZERO consumers (my census). Two axes typed an API surface that has never been instantiated; the real defect on those lines is the inversion — PG-7.
- **C-3 ≡ D-2** ("optional text props → semantically empty plate / blank annotation fields, lost invitation hierarchy") — **KILLED.** `eyebrow` hard-defaults to `"· empty plate ·"` and `dots` to `true` (EmptyState.vue:90, withDefaults semantics); `hint` is v-if-guarded (:61); only the display line can blank and both live consumers supply it, with o9 asserting the exact strings on screen. "Blank annotation fields" (plural) is false at every reachable site.
- **C-4 ≡ D-3 ≡ L-2** ("arbitrary `gridClass` overrides columns/display/containment/visibility"; the D-3 state-matrix rider) — **KILLED.** (a) Containment cannot be overridden by a utility: the scoped rule compiles to `.palette-card-grid[data-v-hash]` — (0,2,0), unlayered — vs a (0,1,0) utility inside `@layer utilities`; unlayered wins regardless of specificity (layer-emission caveat → residue U-1). (b) Only one consumer passes `gridClass` at all, and it is an opacity dim, not a layout program (BrowsePane.vue:87-90; `duration-fast` resolves via the glass-ui bridge). (c) Card width/reading order are structurally guaranteed at `grid-cols-1` in a fixed ~512px pane column where multi-column would be wrong. (d) The rider's matrix is part fiction (no selection state, no permission-denied branch anywhere in the feature) and part correctly-homed-elsewhere (partial loading is a deliberate SIBLING below the grid, BrowsePane.vue:125-131); a 53-line leaf absorbing a ten-cell matrix is the exact god-module the standing edicts ban.
- **D-5's four non-filtered limbs** (loading / transport failure / permission-denied / first-run as GRID obligations, + the `CollectionSurfaceState` cure) — **KILLED as prescription** (the filtered-zero limb survives as PG-5). The three-state composition already ships one level up, with the error plate OUTSIDE the list and carrying the route's only real action (BrowsePane.vue:40-78) — the separation the axis demanded be built into the grid, at the altitude where pane policy belongs.
- **L-1** ("EmptyState reached by a deep relative path; no shared collection-state primitive") — **KILLED.** The path is the ratified house form (vite.config.ts W43/RF-15: demo aliases killed; `demo/shared/ui/` has no barrel — the relative path is the ONLY route); and EmptyState IS the shared primitive, already two-species (`empty`/`error`) with distinct roles and registers, consumed by 8 hosts. An audit reading a convention as a smell.
- **L-3** ("list semantics depend on undocumented child behavior") — **KILLED as stated.** The child contract is documented in-file (:7-8) and partly EXECUTABLE (o9 pins TRUE-EMPTY on both hosts). What the documentation says is invalid ARIA — that is PG-3's row, and mis-diagnosing an a11y violation as a documentation gap points the cure at the wrong file.
- **L-5** ("the component has no relation to the sortable/reorder library; the structural requirement is folklore") — **KILLED, the corpus's worst call, exactly backwards.** The relation is live and three lines long (PalettesPane.vue:133/:180-183, `handle: ".drag-handle"` rendered by the grid's own slot children at PaletteCard.vue:47-50); the "folklore" is correct Vue (fragment root → `$el` = first node); and the seam the axis declared nonexistent is where the cluster's only BLOCKER lives (PG-1/PG-2). The one surviving clause ("no typed adapter") is PG-6's row, stated better there.

## Superlatives (provenance preserved)

1. **The self-demonstrating comment** — PaletteCardGrid.vue:7-11: states the constraint, names the mechanism, names the remote consumer, and is PLACED as the div's first child — the precise hazard it warns about. Verified accurate against PalettesPane.vue:180-183; corroborated at the registry level: BrowsePane.md's P-1 decider compiled this component single-root in dev AND prod, and its ruling cites this comment as "the codebase knows the rule; EmptyState broke it." Both original axes read it and neither credited it; L-5 called it folklore. Its one fault: the invariant deserves `defineExpose`, not prose (PG-6).
2. **The design ruling is executable, not just recorded** — :13-20 (T.W6.5 Lane S R12) + EmptyState.vue:29-38 state matching halves across a file boundary without either being generated from the other, and o9-shadow-palette.spec.ts pins TRUE-EMPTY on BOTH hosts with `assertTrio` + `assertNoFillers` — negative assertions on an owner ruling. **This seat's correction**: the oracle is real but NOT a CI gate (ci.yml carries zero e2e steps — my grep); the superlative is about the artifact, not the enforcement. Enforcement is X-W1's row.
3. **The three-state composition lives at the right altitude** — BrowsePane.vue:40-78: loading/error/wall as sibling branches of one out-in Transition, error OUTSIDE the list with a real Retry, the grid a dumb leaf. Directly refutes the corpus's central prescription.
4. **`reorderPalettes` is written total, not partial** — usePaletteStore.ts:153-167: Map rebuild + append-the-absent with the reason inline. It is the only reason PG-1/PG-2 degrade to wrong-order rather than palette LOSS. Defensive in exactly the right place — and the same append loop is PG-2's mechanism: totality without a projection-aware caller is what moves the invisible palettes.
5. **Two characters of forward-compatibility** — :23 `col-span-full` on the empty plate: inert at `grid-cols-1`, correct the day `gridClass` grows columns — the one place the escape hatch is actually accounted for. Unremarked by all three axes, two of which attacked `gridClass`.
6. **The error plate's restraint** — EmptyState.vue:14-27: the error arm drops eyebrow, trio, and second invitation, keeps the machine truth in Fira and the only real action. Q6's ruling, structurally incapable of re-inviting. (≡ EmptyState.md superlative 1.)
7. **Size and provenance discipline** — 53 lines, one responsibility, zero script logic beyond `defineProps`, the containment rule colocated with its provenance (:48-49) and the vacated site left a tombstone (foundation.css:485-487, my read) — traceable from both ends.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1** — whether a `contain-*` utility through `gridClass` can defeat the scoped containment in the SHIPPED stylesheet (layer emission is a build fact). One `getComputedStyle(grid).contain` read with `grid-class="contain-none"` settles it. Does not move the C-4 kill (three other refutations carry it).
- **U-2** — perceptual severity of PG-11's empty↔populated pop at shipped frame rate vs SearchBar input latency. Absence of a Transition is proven statically; the pop's visibility is not.
- **U-3** — both unnamed `role="list"` regions simultaneously present in one accessibility tree at the dual-pane breakpoint (dual mount measured, ledger :5387; the single-tree observation not yet made).
- **U-4** — how NVDA/VoiceOver/Orca actually degrade PG-3's malformed list (silent list, list-of-0, or flat articles). Static violation certain; AT behavior needs a transcript. (Shares a session with EmptyState.md U-2.)
- **U-5** — pixel delta of PG-4's caster amputation with/without the rule, and whether the last row's missing bottom leg is the more legible of the two. Mechanism spec-certain; the delta is corroboration. (≡ BrowsePane.md U11.)
- **U-6** — the pressed-card `--cast-spread` growth vs the 12px gap: whether a pressed card's cast reaches under its neighbour and is occluded inside the containment's stacking context. Asserted as neither defect nor non-defect (R-LC's framing adopted).
- **U-7 (flag, not this cluster's ruling)** — the shadow direction split: demo overrides `--shadow-cartoon` to 8px RIGHT+down while `cartoon-surface` consumes the producer's LEFT+down `-md`. Whether intentional needs a design ruling; adjacent to PG-4, belongs to X-W7.c/X-W10's canon pass.
- ~~CI hardness of the o9/walk oracles~~ — **SETTLED STATICALLY by this seat**: not live-residue. ci.yml has zero e2e references; the ledger :24311 ask stands; X-W1 owns it.

## DISSENT — preserved, not ruled away

1. **R-DU's walk-past (recorded as evidence about the fleet).** R-DU's L-5 kill cites DEFECT-LEDGER :1752/:2058 — "first-drag ordering wrong, filtered drags corrupting unfiltered order" — as proof the seam EXISTS, in the same breath as declaring the axis's denial of the seam the corpus's worst call… and never booked the corruption itself. An adjudicator who touches a BLOCKER's measured receipts while arguing a different point and does not look up has reproduced the axes' central failure mode (three files largely ABOUT the empty state, none checking what the empty plate does to the list role). The registry sweep (this seat's) shows the same lesson generalizes: five of the two readers' nine "missed" findings were already adjudicated at sibling seats — a missed-finding claim is a claim about the PROGRAM's knowledge, not the corpus's, and must be checked against the adjudicated registry before it is minted. Proposed as an addendum-law candidate for the next formation boundary.
2. **R-LC's cardRefs severity dissent (preserved verbatim in substance)**: unbounded per-keystroke × page-size growth pinning whole component subtrees reads MAJOR, not m2's MINOR. This seat kept the booked grade because the cure is severity-invariant; if X-W7.d's inspector restructure slips a formation, the row revives at MAJOR with a heap-snapshot witness.
3. **R-DU's verdict-laundering on C-6/D-6 (ruled, position preserved)**: R-DU holds that a finding hedging with three plausible nouns and hitting zero live ones earns a kill plus a fresh find. This seat ruled the mechanism IS the row (RESCOPE, not KILL+re-mint) — the distinction matters because kill-counts are fleet-performance signals and a kill that re-files its own substance inflates both columns at once.
4. **Both readers' count discipline was independently verified and held**: every census this seat re-ran (listitem 0, emptyAction 2, e2e drag 0, sticky 0, consumers 2+2+2, `demo/@` absent, SHA byte-match) reproduced exactly. The corpus's receipts were honest; its reasoning was thin; the readers' receipts were honest AND their reasoning survived attack everywhere except the three ruled rows above.

## Closing verdict

**ADJUDICATED — RED AT THE SEAMS, NEAR-MODEL AT THE LEAF.** The 53-line component itself is close to exemplary (four of seven superlatives are its own lines); the corpus's SOURCE-RED verdicts aimed at the leaf and 12 of 17 accusations died there. The real red: **one BLOCKER (PG-1, first-drag order corruption) + one MAJOR (PG-2, filtered-drag corruption) living on the `$el` seam the component's best comment protects — found by no axis and one of two readers**; a both-states ARIA violation (PG-3 ≡ ES-9); the caster amputation (PG-4 ≡ M9); and the filtered-zero falsehood (PG-5 ≡ ES-1). Routing: **PG-1/PG-2/PG-8 → X-W7.d** (named-addition rider for the drag pair) · **PG-4/PG-6/PG-12 → X-W7.c** · **PG-7 → X-W7.g** · **PG-3/PG-5/PG-9 → X-W7** · **PG-10 → X-W8 + NO-WAVE-OWNER residue (as ruled at AF-13)** · **PG-11 → X-W10** · PG-6's gate-honesty rider **→ X-W1**. Seven live questions feed the visual-audit worklist (U-1..U-7). Dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave.

— end of apotheosis. The three r1 challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
