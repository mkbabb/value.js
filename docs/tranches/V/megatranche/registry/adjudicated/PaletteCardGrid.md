# PaletteCardGrid — adjudicated apotheosis, second pass (M-23/L-18, fresh-Fable adjudicator) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (the incredulity seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **HEAD**: `983967da` · branch `tranche-u` · **Subject**: `demo/palettes/browser/card/PaletteCardGrid.vue` (53 L) + its two consumer seams.
**SUPERSESSION (E-3-adjacent, declared)**: this record supersedes the same-day first-pass apotheosis previously at this path (HEAD `5f328fb8`/`95cfbb15` seat). That record was itself challenged this round — its PG-9 row is the one registry cell a re-reader killed. Its roster ids **PG-1..PG-13 are preserved for life** (anti-rename); every carried row below restates the prior substance and marks what this round changed. The prior record was not yet committed, so its text survives only inside this one — each carried row is therefore restated whole, not by pointer.
**Corpus**: 3 r1 challenge files (C 6 · D 6 + state-matrix rider · L 5 = 17 accusations), all REPORT-AUTHORED, evidence count 0 — UNVERIFIED-UNTIL-RE-PROVEN. Corpus receipt re-hashed by this seat at HEAD `983967da`: SHA-256 `e4ee08d8…4d7107` — **byte-identical to the live file**; the grid has not moved since the corpus was cut. **Anchor drift found by this seat**: `PaletteCard.vue` now lives at `browser/card/PaletteCard/PaletteCard.vue` (365 L); every reader line anchor into it still resolves (:19 class array, :22 role, :47-50 drag-handle verified at the new path).
**Re-readers adjudicated (this round)**: R2-DU (design-and-usage) · R2-LC (library-and-code). Both were attacked before either was adopted. Verdict-level rulings below: the BLOCKER asymmetry (R2-DU walked past it — the second consecutive design-side walk-past), the REG-PG-9 registry-cell kill (adopted, corroboration struck), the D-4 empty-limb double-mint (merged), the comment-archaeology row (killed as house convention), the published-tail limb (demoted to mechanism-only).
**Arbiter's own instruments (all static; zero browser tooling — law + probe parsimony)**: whole-file reads of PaletteCardGrid.vue, EmptyState.vue, useFilteredList.ts, PaletteCard/PaletteCard.vue (365 L, whole); PalettesPane.vue:1-212 (whole); BrowsePane.vue:36-150; usePaletteStore.ts:40-170; usePalettePorts.ts:100-124; **the PG-1 chain re-walked byte-level**: `node_modules/@vueuse/integrations/dist/useSortable.js` whole (`defaultOptions.onUpdate` survives an `onEnd`-only options spread; `moveArrayElement` `_valueIsRef=false` arm takes `array = toValue(list)` — the SAME object — splices synchronously, defers the re-insert to `nextTick`, never writes back; `insertNodeAt` indexes the element-only `children` collection) + `node_modules/sortablejs/modular/sortable.esm.js` `_onDrop` (`update` :2002-2007 dispatched BEFORE `end` :2023-2028, one synchronous frame); installed glass-ui 7.0.0 dist bytes: `components/card/styles.css` (`@utility cartoon-surface { position: relative; border-width: 2px; box-shadow: var(--shadow-cartoon-md) }`), `styles/tokens/shadow.css` (`--shadow-cartoon-md: -3px 3px 0 …, -5px 5px 0 …, -7px 7px 0 …` — LEFT+DOWN), `styles/glass/glass-atom.css` (`.cartoon-cast` travel/spread ride `--cartoon-press-t`; PRM zeroes `--motion-weight`), `styles/tokens/property-regs.css` (`--cartoon-press-t` @property, initial 0); demo/styles/foundation.css:119/:362-369/:486/:502/:518-519 (only `--shadow-cartoon`/`-hover` re-pointed to 8px RIGHT+down; `-md` stays producer-left; the colocation tombstone at :486); eslint.config.js:228-256 (G-DEMO-3b object whole: dead `demo/@` globs + the retired `@components/custom/palette-browser` ban pattern; `ls demo/@` → does not exist); `demo/palettes/browser/index.ts` docblock; e2e/smoke/walk.spec.ts:60-80; o9-shadow-palette.spec.ts:180-240; X/COHESION.md + X waves W7.md (units c–g whole, CC-069..CC-077) read for routing at the CURRENT authority (M-26); censuses re-run at HEAD: `role="listitem"` → **0** across demo/src/e2e; `emptyAction` → declaration only (PaletteCardGrid.vue:28-29); `gridClass|grid-class` → declaration + binding + ONE consumer (BrowsePane.vue:87); `PaletteCardGrid` consumers → exactly PalettesPane + BrowsePane (+2 barrels, +foundation.css tombstone, +2 e2e specs); `dragTo|sortable|drag-handle|reorder` in e2e/ → **0 files**; ci.yml `e2e|playwright|smoke` → **0**; importers of the top-level `browser/index.ts` barrel → **0 repo-wide** (both `from "./browser"` and `palettes/browser` spellings); `card-press-t` in glass-ui dist → **0 files**; `cartoon-press-t|card-press-t` in demo → only PaletteCard's own 3 lines; DEFECT-LEDGER live witnesses re-read verbatim (:466 filtered-zero probe; :1508/:1742/:3660 first-drag repros incl. a 3-context WebKit run; :2055/:3678 filtered-drag repros with pasted before/after orders); **adjudicated-registry identity sweep re-run**: BrowsePane.md M1/M9/M10/m2/K8/P-1-decider, EmptyState.md ES-1/ES-4/ES-9/ES-16/ES-36, ActionFeedback.md AF-13 — all cross-books below re-confirmed against the sibling files' own text.
**Motion/PRM**: motion-quarantine.md:275 lists PaletteCardGrid "C · D · L" — no motion content; **no M-xx FALSE row is carried by any verdict below**, and the one motion-adjacent row this round minted (PG-17) rests on producer bytes read by this seat, not on any quarantined cell.

## Scoreboard

**17 axis accusations: 5 CONFIRMED in some form (C-5, C-6≡D-6 as one rescoped identity, D-4 both-limbed, D-5-narrowed) · 12 KILLED — both readers' kills ratified, none weak enough to revive.** One registry cell of the superseded first-pass record killed on R2-LC's challenge (REG-PG-9's "none can be supplied" parenthetical) — adopted, row restated. Adjudicated roster: **17 rows — 1 BLOCKER · 4 MAJOR · 9 MINOR · 3 INFO** — of which **4 are new to the program this round** (PG-14..PG-17; PG-17 is this seat's own, found by no axis and neither reader) and the rest carry the prior roster's ids with this round's sharpenings folded in.

Blocker-weight survivor, unchanged and now thrice-witnessed: **PG-1** — drag-to-reorder double-applies the move against a detached computed snapshot; the first drag of a session persists the wrong order. Found by R2-LC; **walked past by R2-DU — the second consecutive design-side reader to touch this seam's receipts while arguing something else and not look up.**

## Ruled disagreements (the incredulity seat's actual work)

| id | R2-DU | R2-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **PG-1/PG-2 (the drag pair)** | ABSENT — confirmed C-5 by narrating "drag-reorder dies" under a *hypothetical* fragment regression, and cited the zero-e2e-reorder census, without asking what the uncovered live code does | BLOCKER + MAJOR, six-link chain | **CONFIRMED — BLOCKER + MAJOR.** | Chain re-derived whole by this seat from vendored bytes (instruments above): the `onEnd`-only options object does NOT displace the vendored default `onUpdate`; sortablejs fires `update` before `end` synchronously; `moveArrayElement` splices the SAME object PalettesPane.vue:183 captured at setup (`pm.filteredSaved.value` — a computed's cached plain array: usePaletteStore.ts:51-55 `.filter()` output, returned by identity through useFilteredList.ts:10 on an empty query); onEnd (:187-196) re-reads the now-n−1 array and splices by original DOM indices; reorderPalettes' append-the-absent tail (usePaletteStore.ts:161-165) tacks the dragged palette onto the END. Worked example re-verified: [A,B,C,D] drag A 0→2 ⇒ persists [C,D,B,A], correct [B,C,A,D]. LIVE corroboration re-read at the ledger, three independent witnesses: :1508 (`EXPECTED Bravo Charlie Alpha Delta / ACTUAL Charlie Delta Bravo Alpha`), :1742, :3660 (3 fresh WebKit contexts, `drag#2 → correct` — the exactly-once shape that evades manual testing). PG-2 likewise (:2055: 6-palette repro, non-matching palettes relocated and persisted; :3678). R2-DU's walk-past is booked in DISSENT as fleet evidence, not ruled away. |
| **REG-PG-9** (the prior record's PG-9 cell) | absent | KILLED the parenthetical: "none can be supplied" is false — fallthrough delivers `aria-label` today | **KILL ADOPTED · ROW RESTATED · one corroboration cell STRUCK.** | My whole-file read: PaletteCardGrid.vue has a single element root (:2-32) and no `defineOptions`/`inheritAttrs: false` anywhere in :35-45 — Vue's implicit attr fallthrough therefore delivers `aria-label` (and `role`, fallthrough winning in `mergeProps`) onto the root div with zero code change. `<PaletteCardGrid aria-label="Saved palettes">` names the list TODAY. The prior cell's "no attrs passthrough contract" was wrong as a mechanism claim; the GAP is real but is a two-attribute call-site omission. STRUCK: R2-LC's corroboration that the ledger's `role="list"→"grid"` green-keeping mutation "is only possible because fallthrough works" — a non-sequitur; that mutation (ledger :24311 family) edits the template attribute, proving nothing about fallthrough. The kill stands on Vue semantics alone. Restated as PG-9 below. |
| **D-4 empty-state limb** | booked the `list > status` shape as its OWN missed find | folded the same shape into D-4's confirmation as an extension | **ONE identity — merged into PG-3 (≡ EmptyState.md ES-9, which already carries both limbs).** | Both readers derived the same fact from the same lines (PaletteCardGrid.vue:21-31 + EmptyState.vue:28); minting it twice would inflate the missed column. ES-9's own text ("malformed in EVERY state, and the empty state makes EmptyState its only owned child") predates both — a missed-finding claim is a claim about the PROGRAM's knowledge (the prior record's law, re-ratified). |
| **C-6/D-6 casualty roster** | CONFIRMED with the cartoon-caster victim; both axes' named victims refuted | same, with the interior-bottom-legs-survive-in-gap nuance | **CONFIRMED-RESCOPED, no conflict — identity stays at BrowsePane.md M9.** | Re-derived from bytes (instruments): `contain: content` ⊇ paint clips the subtree at the padding edge; the grid has zero padding and `grid-cols-1` (:4) stretches every card flush; `cartoon-surface` consumes the producer's LEFT+DOWN `--shadow-cartoon-md` (foundation.css re-points only base/hover to RIGHT+down). One nuance this seat adds: the LAST card's bottom leg is clipped only when content fills past the `min-h-[120px]` floor — in the floor's one binding state (a single short card) the bottom leg escapes into the slack. The left-leg amputation is unconditional at both call sites. Both readers' sticky/positioned refutations verified (`sticky` → 0 hits in browser/; SwatchHoverMenu Teleports to body; the card menu rides reka-ui portals). |
| **Comment-archaeology row** (R2-DU missed #6: 20 of 53 lines are tranche provenance) | INFO find | absent (its superlative 6 PRAISES the same lines) | **KILLED as a defect — house convention; kernel preserved.** | The registry's own standing superlatives (this cluster's #7, BrowsePane.md S-3, ColorSpaceSelector's provenance discipline) treat in-source decision provenance with citations as a virtue of this codebase, and the tombstone pairing (grid :48-49 ↔ foundation.css:486) is exactly the traceability the program relies on to adjudicate at all — this file included. The surviving kernel: the one load-bearing comment (:7-11) is diluted by its neighbours, and its "landmark" clause is FALSE (PG-3's limb). The X-W7.c rebuild re-authors the file; no separate row. R2-DU's position preserved in DISSENT. |
| **PG-2's published-palettes limb** (R2-LC: every non-local palette relocates to the tail on EVERY reorder) | absent | asserted as part of PG-2 | **MECHANISM CONFIRMED · CONSEQUENCE DEMOTED to a noted limb.** | usePaletteStore.ts:57-59 `publishedPalettes` and :163-165's append verify the relocation mechanically. But no surface this seat traced ORDERS by the store's non-local tail (Browse renders the server list), so the user-visible consequence is unestablished — the limb rides inside PG-2 as mechanism, not as its own graded row. If a store-order-dependent non-local surface is ever found, the limb revives. |

## Adjudicated defect roster

Line anchors are HEAD `983967da`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Routing authority = `docs/tranches/X/COHESION.md` (M-26) + `X/waves/W7.md` (CC-077 folds this file into X.W7.c/d by name).

- **PG-1 · BLOCKER — drag-to-reorder double-applies the move against a detached array snapshot: the first drag of a session persists the wrong order.** (No axis row; R2-LC this round; live-witnessed thrice in the ledger.) Chain: (1) PalettesPane.vue:183 hands `useSortable` the UNWRAPPED `pm.filteredSaved.value`, evaluated once at setup, with `onEnd` only (:187-196); (2) the vendored default `onUpdate → moveArrayElement` survives the spread; (3) sortablejs `_onDrop` dispatches `update` before `end` in one synchronous frame; (4) `moveArrayElement` splices the SAME object synchronously (re-insert deferred to `nextTick`; no write-back for a non-ref list); (5) the object is the computed's cached PLAIN array (usePaletteStore.ts:51-55 `.filter()`; useFilteredList.ts:10 identity return on empty query) so the splice dirties nothing; (6) `onEnd` re-reads the mutated n−1 array, `ids.splice(evt.oldIndex,1)` removes the WRONG palette, and reorderPalettes' append tail (:161-165) moves the dragged palette to the END. [A,B,C,D] drag A 0→2 ⇒ [C,D,B,A]; correct [B,C,A,D]; drag #2 onward correct (the store's :166 array replacement recomputes the cache) — the exactly-once shape manual testing misses. Ledger live witnesses: :1508, :1742, :3660 (WebKit ×3). Coverage: `reorder|sortable|drag-handle|dragTo` in e2e/ → 0 files (my census, this HEAD). Cure shape endorsed: one owner for the move — either hand useSortable a writable ref/getter and delete the hand-rolled `onEnd`, or displace the default with an explicit no-op `onUpdate` (ledger :1520's invariant: nothing outside the store mutates a computed's cache). **→ X-W7.d** — W7.d owns PalettesPane.vue + usePaletteStore.ts by file list and "one mutation, one owner, one visible result" by goal, but its four NAMED DAG defects do not include this pair (re-verified against W7.md at this HEAD): **the named-addition rider stands — PG-1/PG-2 enter W7.d's defect list by name at wave open**, and the W7.c/d card rebuild must not re-mount Sortable over a derived list.
- **PG-2 · MAJOR — a drag while a filter is active silently rewrites the order of the palettes the user cannot see.** `onEnd` enumerates only the projection (`filteredSaved`, :190); reorderPalettes totalizes (:157-165), hoisting the visible ids and appending the rest — a gesture over a 2-of-6 projection relocates the 4 unseen palettes and persists it. Independent of PG-1 (survives any snapshot fix); no guard consults `searchQuery` (:187-196). Ledger live witnesses: :2055 (pasted before/after store orders), :3678. Noted limb (mechanism-only, ruled above): every non-local palette also relocates to the store tail on every reorder. **→ X-W7.d**, same named-addition rider.
- **PG-3 · MAJOR — `role="list"` is malformed in BOTH states: populated children are `role="article"`, and at true-empty the sole owned child is the plate's `role="status"`.** (D-4 CONFIRMED both readers; empty limb merged per ruling.) PaletteCardGrid.vue:3; PaletteCard/PaletteCard.vue:22; EmptyState.vue:28; `role="listitem"` → 0 repo-wide (my grep at this HEAD). ARIA 1.2 `list` Required Owned Elements = `listitem`; axe `aria-required-children` (serious). The :7-8 comment is wrong twice — `article` is not `listitem`, and neither `list` nor `article` is a landmark role (R2-DU's landmark-falsehood find folds here; the false premise is what licensed the structure). Any populated-state-only fix leaves the empty state violating. **IDENTITY**: ≡ EmptyState.md **ES-9** → **X-W7**. AT-degradation mode → residue U-4.
- **PG-4 · MAJOR — the grid's containment amputates the card's signature cartoon caster: every card loses its whole left leg at rest, at both call sites; the last card loses its bottom leg whenever content fills past the 120px floor.** (C-6/D-6 as rescoped.) PaletteCardGrid.vue:50-52 `contain: content` on a zero-padding single-column grid vs `cartoon-surface`'s −3/−5/−7px LEFT+DOWN stamp (producer bytes; foundation.css re-points only base/hover). The collision is knowing: PaletteCard :16-18 declines `overflow-hidden` explicitly to protect this caster; the grid clips it one level up. Interior bottom legs survive inside `gap-3`. This amputates a ratified register (demo/DESIGN.md cartoon-register law; the R4 row pins it to the card root). **IDENTITY**: ≡ BrowsePane.md **M9 · D5-03** → **X-W7 / X.W7.c** ("shadow follows the rounded silhouette… at the card root", G11), M9's rider intact: the cure must audit the SECOND paint-clip ancestor (`.pane-scroll-fade`, `contain: layout style paint`). Pixel delta → residue U-5.
- **PG-5 · MAJOR here (BLOCKER as booked at ES-1) — filtered-zero wears first-run copy at both call sites: N saved + a non-matching query ⇒ header badge "N", the delete-all-N trigger live, and a plate stating "No saved palettes yet. Add colors above, then save the set." — a falsehood with an unusable remedy, in the accessibility tree.** (D-5 CONFIRMED-NARROWED by both readers; the four other limbs killed below.) PalettesPane.vue:77 vs :79-80 (badge :19-25, delete-all :62-72); BrowsePane.vue:83 vs :84-86 ("No published palettes here yet. Publish one from My Palettes…" over a search/tag/color-filtered zero, :339-349 filters). Ledger :466 (live probe + screenshot). **IDENTITY**: ≡ EmptyState.md **ES-1** → **X-W7**. Cure = one honest branch of copy keyed on query/filter state at the two hosts — never a `CollectionSurfaceState` machine inside a 53-line leaf (both readers, both rounds, ratified; KISS edict).
- **PG-6 · MINOR — the single-root invariant is load-bearing at an external `$el` seam, untyped at both ends, and its only named guard is mislabeled.** (C-5 CONFIRMED both readers.) PalettesPane.vue:181 `(sortableGridRef.value as any)?.$el as HTMLElement | undefined` — the `as any` gratuitous, the `as HTMLElement` unchecked over `$el: any` (a fragment root's Comment node satisfies it silently); no `defineExpose` in the grid (:35-45). walk.spec.ts:71-72 claims "the B.W1 single-root invariant" but asserts only `getByRole("list").last()).toBeVisible()` — insensitive to root arity; the regression it names keeps it GREEN. And the oracle is not a gate: ci.yml carries zero e2e steps (my grep). Cure: `defineExpose` a typed root element + a root-arity assertion **→ X-W7.c**; the walk.spec comment correction + smoke CI-hardening **→ X-W1**.
- **PG-7 · MINOR — the `emptyAction` CTA slot is plumbed end-to-end and consumed by nobody; the in-file ruling ("The CTA slot survives on the caption", :19-20) survived in the primitive and died at every call site — the invitation is a text-only dead end while the sibling error plate carries a real Retry (BrowsePane.vue:68-77).** Census: declaration only (:28-29). The `v-if="$slots.emptyAction"` guard is inert either way (EmptyState.vue:64 renders `#action` unconditionally). **IDENTITY**: ≡ BrowsePane.md **M10 · D5-04** + EmptyState.md **ES-16** → **X-W7.g** (the CTA decision made explicitly, not dropped again). C-2/L-4 typed this slot's guard semantics and neither checked for a consumer — the corpus's cleanest inversion, both rounds.
- **PG-8 · MINOR — `cardRefs` is a write-only registry in both grid consumers; the `el &&` guard swallows Vue's unmount-`null`, detached card instances accumulate per filter keystroke, and async operation verdicts can be delivered into unmounted cards with no visible result.** PalettesPane.vue:84/:177 (+ the awaited `onPublish` verdict path :199-209); BrowsePane.vue:94/:209 + its four verdict paths. **IDENTITY**: ≡ BrowsePane.md **m2** → **X-W7.d** (dissolves when feedback leaves the card). The prior round's MAJOR-severity dissent stays preserved; this round's R2-LC filed MINOR — no live conflict remains.
- **PG-9 · MINOR (RESTATED this round; parenthetical struck per ruling) — the list has no accessible name at either call site — though plain attr fallthrough would deliver one today — while the decorative loading wrappers beside it carry `aria-label` on name-prohibited bare divs; at the dual-pane breakpoint two structurally identical anonymous lists mount side by side.** The gap is a two-attribute call-site omission (neither PalettesPane.vue:75-81 nor BrowsePane.vue:80-91 passes `aria-label`), NOT a missing mechanism — the grid is single-element-rooted with default `inheritAttrs`. Inversion limb: BrowsePane.vue:47/:128 label bare divs (ARIA name-prohibited `generic`); that half stands as a fragment inside BrowsePane.md **K8**. Positional-selector fragility corroborates (walk.spec.ts:71 must reach `.last()`). **→ X-W7** (the c/d rebuild names the collection at the seam). Dual-AX-tree observation → residue U-3.
- **PG-10 · MINOR — the feature's self-described "TOP-LEVEL SEAM" is a contract nobody consumes and nothing enforces: the top barrel has ZERO importers repo-wide, and the G-DEMO-3b lint its docblock cites as "enforces it standing" is inert.** Both limbs re-verified at this HEAD: (a) importer census of `./browser` / `palettes/browser` top-barrel specifiers → 0 (every consumer reaches a sub-barrel — which the docblock's own PI-6 rationale says is the tree-shake-honest reach, making the ~50-line surface dead by its own logic); (b) eslint.config.js:232-238 globs = `demo/color-picker/**` + dead `demo/@/**` (dir absent), ban pattern :246 spelled in the W43/RF-15-retired `@components` alias, `demo/palettes/**` in no glob. **IDENTITY (limb b)**: ≡ ActionFeedback.md **AF-13** ≡ AdminPane L-11 ≡ ColorSpaceSelector L-5(a) → dead-glob deletion **X-W8**; the live re-point at `demo/palettes/**` = **NO-WAVE-OWNER named residue** (as ruled at AF-13). **Limb (a) is new this round** (R2-LC): the barrel keep/delete decision + docblock truth-correction ride the same X-W8 seat, else the same NO-WAVE-OWNER residue.
- **PG-11 · INFO (carried; unchallenged this round) — the empty↔populated swap is the one unchoreographed state change on the Browse route and the one a search keystroke triggers most often: it pops inside the single `key="wall"` branch while the route's out-in Transition (BrowsePane.vue:40) choreographs only boot/error.** PaletteCardGrid.vue:22 `v-if="empty"`, no Transition, no key change; PalettesPane has no Transition at all around the grid. Not a motion-quarantine row (no M-xx carried). **→ X-W10**; perceptual severity → residue U-2.
- **PG-12 · INFO (carried + sharpened) — `min-h-[120px]` is the file's only un-narrated decision: inert in the empty state (the plate's own utilities compute ≈194px, so the floor never stabilises the skeleton→plate morph it could have served) and binding only under a single short card, where it opens ~30px of dead space — which is also the one state where the last card's caster escapes PG-4's clip.** (R2-DU's inertness arithmetic adopted; exact binding px stays residue U-6a — arithmetic over utilities, not a measurement.) **→ X-W7.c** (re-derive the floor or delete it with a comment).
- **PG-13 · MINOR (dev-only; carried) — PaletteCard, the grid's sole child and the very element Sortable drags, commits the root-leading-comment fragment hazard the grid's own comment warns about, on the one code path that rearranges the grid's DOM behind Vue's back.** Re-verified at the NEW path: PaletteCard/PaletteCard.vue:2-4 comment precedes the :5 root div (dev compiler → DEV_ROOT_FRAGMENT; prod strips comments — the BrowsePane.md P-1 decider's dual compile, not re-run here); `insertNodeAt` indexes the element-only `children` collection (vendored bytes, my read) so a dev drag re-inserts the card div across sibling anchors. **IDENTITY**: ≡ BrowsePane.md **M1**'s named latent limb → **X-W7.g** comment-move cure; the W7.c/d rebuild keeps the card root comment-free or PG-1's cure inherits a second dev-only failure mode. (R2-LC filed the runtime divergence as unproven-needs-live this round — that residue stays at U-7.)
- **PG-14 · MINOR (NEW this round — R2-LC) — the wall's loading and load-more phases hand-roll divergent copies of the grid's layout contract: BrowsePane.vue:46 and :127 re-declare `grid grid-cols-1 gap-3` on bare divs with no role, no 120px floor, no containment — so the out-in swap between skeleton and wall crosses a containment boundary and a min-height floor mid-morph, and the AX tree gains/loses a list.** Verified against both templates; checked NOT already booked at BrowsePane.md (its skeleton rows — D5-02, K8, K15 — are announcement/focus rows, none owns the layout-contract fork). A column change to the grid strands both copies. **→ X-W7.c** (the specimen/skeleton geometry rebuild; W7.g touches PaletteCardSkeleton in the same formation — the cure must land once, not twice).
- **PG-15 · MINOR (NEW this round — R2-LC) — `gridClass` is a redundant re-implementation of Vue's class fallthrough: the component is single-element-rooted with default `inheritAttrs`, so `class` already merges onto the root — the prop adds zero capability, creates two mechanisms with no defined precedence for one job, and is why the C-4/D-3/L-2 family's "bound the styling authority" prescriptions are unimplementable as written (deleting the prop cannot bound what fallthrough preserves).** Verified: no `defineOptions` in :35-45; the ONE consumer's opacity dim (BrowsePane.vue:87-90) works identically as plain `class`. Honest cure = delete the prop + document the class contract, or `inheritAttrs: false` with a deliberate surface. **→ X-W7.c** (the grid file is re-authored there). This row also hardens the C-4 kill: it is the finding the three axes' shared row was groping for.
- **PG-16 · INFO (NEW this round — R2-LC) — `message` is the one EmptyState prop with no default and no guard: an omitted `empty-text` renders a styled empty `<p>` (EmptyState.vue:58-60, unguarded, unlike `hint` at :61).** Unreachable today (both grid consumers supply it; o9 pins the strings) and NOT booked at EmptyState.md (ES-36 is the eyebrow's guard asymmetry — adjacent family, different prop; recorded here so the family closes together). One-line cure (`v-if="message || $slots.default"`). **→ X-W7.g** (owns EmptyState.vue's carve).
- **PG-17 · MINOR (NEW — this seat's own find; no axis, neither reader, no registry row) — the card's celebrated press-cast choreography is inert: PaletteCard drives `useLiquidPress({ pressVar: "--card-press-t" })` (:264) but the producer's `.cartoon-cast` travel/spread read `--cartoon-press-t` (glass-atom.css: `--cast-travel: calc(6px * var(--motion-weight) * var(--cartoon-press-t))`; @property-registered, initial 0) — and `card-press-t` has ZERO readers in the entire glass-ui dist and zero bridges in demo CSS (my censuses).** So the cast never travels or spreads on press; only the card's own useLiquidPress scale runs. The file asserts the wiring works twice (:28-29 "rides --card-press-t, PRM-zeroed"; :259-262 "writing --card-press-t for the caster travel/spread") — both comments false against producer bytes. Static mechanism certain; the visual delta (what a working lag-cast would have added) → residue U-9. Consumer misconfiguration, not a producer defect — no glass relay. **→ X-W7.c/d** (the specimen rebuild must wire `--cartoon-press-t` or drop the dead span; PaletteCard deletes at W7.d regardless — the finding's real payload is that the rebuild must not copy the dead wiring forward).

## Killed-claims register

Twelve axis rows die, all with both readers concurring and this seat's spot-checks holding. One registry cell dies with them (ruled above). None of the kills was weak enough to revive — this seat attacked each load-bearing refutation directly.

- **C-1 ≡ D-1** ("`empty` can contradict the default slot") — **KILLED.** Both and only consumers derive `empty` from the array they iterate, adjacent lines, same render (PalettesPane.vue:77/:83; BrowsePane.vue:83/:93 — my reads). No live producer; and the implied cure (derive count from `$slots.default?.()`) is unsound — v-for Fragments + dev comment nodes + PaletteCard's own dev fragment root make slot-VNode counting a NEW defect. Residue absorbed by the X-W7.c rebuild.
- **C-2 ≡ L-4** ("the `$slots.emptyAction` guard checks existence, not actionability") — **KILLED.** True of Vue, doubly inert here: EmptyState.vue:64 renders the action slot unconditionally with no fallback (an empty-rendering slot emits no flex item, no gap, no reserved box), and the slot has zero consumers. Two axes typed an API never instantiated; the real defect on those lines is PG-7.
- **C-3 ≡ D-2** ("optional text props → blank annotation fields, lost invitation hierarchy") — **KILLED.** `eyebrow` hard-defaults, `dots` defaults true (EmptyState.vue:90; defaults apply on explicit `undefined`); `hint` is v-if-guarded (:61); both live callers pass all three and o9 asserts the exact strings. The sole surviving grain — the unguarded `message` line — is PG-16, INFO, unreachable today. Note for the next formation: X.W7.g deletes the eyebrow prop AND element outright, mooting this family's substrate.
- **C-4 ≡ D-3 ≡ L-2** ("arbitrary `gridClass` overrides columns/display/containment/visibility"; "every responsive decision arrives through it"; the state-matrix rider) — **KILLED, four ways.** (a) Containment cannot be overridden by a utility: the scoped rule compiles unlayered (`.palette-card-grid[data-v-hash]`) vs Tailwind's `@layer utilities` — unlayered beats layered regardless of specificity (build-fact caveat → U-1). (b) The census: ONE consumer, passing an opacity dim, zero layout classes anywhere; D-3's "every responsive decision" is false in the strongest way — NO responsive decision arrives through it. (c) Reading order is DOM order, unconditionally. (d) The rider's matrix is part fiction (no selection state, no permission-denied branch in the feature — corroborated by BOTH rounds' searches; residue note retained) and part correctly-homed one altitude up. The true finding under this family is PG-15's redundancy — booked, and it makes the axes' prescriptions unimplementable as written.
- **D-5's four non-filtered limbs** (loading / transport failure / permission-denied / first-run as GRID obligations + the `CollectionSurfaceState` cure) — **KILLED as prescription** (filtered-zero survives as PG-5). The three-state composition ships one altitude up as sibling branches of one out-in Transition, error OUTSIDE the list with the route's only real action (BrowsePane.vue:40-78), paging as a deliberate sibling (:125-131). A presentational leaf whose owning pane holds the state machine is legitimate architecture; demanding the leaf absorb a ten-cell union is the god-module the edicts ban.
- **L-1** ("EmptyState reached by a deep relative path; no shared collection-state primitive") — **KILLED.** The relative path is the ratified house form (vite.config.ts W43/RF-15 comment, my read: demo aliases killed; `demo/shared/ui/` has no barrel); EmptyState IS the shared primitive — two-species, typed, role-differentiated, multiply consumed. A convention read as a smell.
- **L-3** ("list semantics depend on undocumented child behavior") — **KILLED as stated.** The child contract is documented in-file (:7-8) and partly executable (o9 pins TRUE-EMPTY on both hosts). What the documentation SAYS is invalid ARIA — that is PG-3, and aiming the cure at documentation aims at the wrong file.
- **L-5** ("no relation to the sortable library; the structural requirement is folklore") — **KILLED; the corpus's worst call, exactly backwards.** The relation is live and three lines long (PalettesPane.vue:133/:180-183; `handle: ".drag-handle"` rendered by the grid's slot children at PaletteCard/PaletteCard.vue:47-50); the "folklore" is correct Vue; and the seam the axis declared nonexistent is where the cluster's only BLOCKER lives. The one surviving clause (no typed adapter) is PG-6, stated better there.
- **REG-PG-9's parenthetical** (prior registry cell: "none can be supplied — no prop, no attrs passthrough contract") — **KILLED per ruling** (R2-LC; adopted with one corroboration cell struck). The row survives restated (PG-9): the gap is real, the mechanism claim was false.
- **R2-DU's comment-archaeology row** — **KILLED as a defect per ruling** (house convention; kernel folded into PG-3/X-W7.c; position preserved in DISSENT).

## Superlatives (provenance preserved; each re-verified in substance this round)

1. **The self-demonstrating comment** — PaletteCardGrid.vue:7-11: states the constraint, names the mechanism, names the remote consumer by file and API, and is PLACED as the div's first child — enacting the hazard it warns about. Verified against PalettesPane.vue:180-183; corroborated by the BrowsePane.md P-1 decider (single-root in dev AND prod). Its two faults are booked, not glossed: the "landmark" clause is false (PG-3) and the invariant deserves `defineExpose`, not prose (PG-6). L-5 called this folklore; it is the truest sentence in the file.
2. **The design ruling is executable, not just recorded** — :13-20 (T.W6.5 Lane S R12) + EmptyState.vue:29-38 state matching halves across a file boundary, and o9-shadow-palette.spec.ts pins TRUE-EMPTY on BOTH hosts with `assertTrio` + `assertNoFillers` — negative assertions on an owner ruling, with the Browse oracle routing a shape-correct empty API response so the right branch is deterministically on screen. Correction retained: the oracle is NOT a CI gate (ci.yml, zero e2e — re-verified this round); enforcement is X-W1's row.
3. **The three-state composition lives at the right altitude** — BrowsePane.vue:40-78: loading/error/wall as sibling branches of one out-in Transition, error OUTSIDE the list with a real Retry, the grid a dumb leaf. It directly refutes the corpus's central prescription — and both rounds' readers independently re-derived that refutation.
4. **`reorderPalettes` is written total, not partial** — usePaletteStore.ts:153-167: Map rebuild + append-the-absent with the reason inline. The only cause of PG-1/PG-2 degrading to wrong-ORDER rather than palette LOSS — and, honestly, the same append is PG-2's mechanism: totality without a projection-aware caller is what moves the invisible palettes.
5. **Two characters of accounted-for forward compatibility** — :23 `col-span-full` on the empty plate: inert at `grid-cols-1`, correct the day the grid grows columns — the one place the escape hatch three axis rows attacked is actually reasoned about.
6. **The error plate's restraint** — EmptyState.vue:14-27: the error arm drops eyebrow, trio, and second invitation; keeps the machine truth in Fira and the only real action. (≡ EmptyState.md superlative 1.)
7. **Size and provenance discipline** — 53 lines, one responsibility, zero script logic beyond `defineProps`; the containment rule colocated with provenance (:48-49) and the vacated site tombstoned (foundation.css:486, re-read this round) — traceable from both ends. Twelve of seventeen axis accusations aimed at this leaf and died there; every survivor lives at its seams.
8. **The producer's PRM cast-handling is elegant beyond compliance** (R2-DU's find, adopted with this seat's byte-verification and one sharp irony): glass-atom.css zeroes the INPUT (`--motion-weight: 0`) so `--cast-travel` collapses to 0 and `--cast-spread` to exactly 1 — the element lands in true rest geometry rather than freezing mid-pose. Verified byte-level. The irony is PG-17: at THIS consumer the cast never leaves rest geometry for anyone, PRM or not — the press variable is unwired. The superlative is the producer's; the consumer forfeited it.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1** — whether a `contain-*` utility through `gridClass`/fallthrough can defeat the scoped containment in the SHIPPED stylesheet (layer emission is a build fact; the kill stands on three other legs). One `getComputedStyle(grid).contain` read settles it.
- **U-2** — perceptual severity of PG-11's empty↔populated pop at shipped frame rate vs SearchBar input latency.
- **U-3** — both anonymous `role="list"` regions simultaneously present in one AX tree at the dual-pane breakpoint (single-tree observation not yet made).
- **U-4** — how NVDA/VoiceOver/Orca actually degrade PG-3's malformed list in each state (silent list, list-of-0, or flat articles). Shares a session with EmptyState.md U-2.
- **U-5** — pixel delta of PG-4's caster amputation with/without the rule; whether the left-leg or last-row loss is the more legible. (≡ BrowsePane.md U11.)
- **U-6** — (a) the exact px at which `min-h-[120px]` binds under one short card (PG-12's arithmetic is utility math, not measurement); (b) the pressed-card `--cast-spread` growth vs the 12px gap and the containment stacking context — now CONTINGENT on PG-17's cure (today the spread never grows; the question activates the day the press var is wired).
- **U-7** — whether PaletteCard's dev-mode fragment root actually desynchronises Vue's el/anchor bookkeeping under Sortable's element-only `insertNodeAt` on a live dev drag (PG-13's runtime limb; prod unaffected).
- **U-8 (NEW — R2-DU)** — whether the v-if'd `role="status"` plate is announced at all on a filter transition: PaletteCardGrid.vue:21-31 inserts the live region and its content in the same patch, and ARIA/APG guidance wants the region in the DOM before content changes — the filtered-zero plate may be SILENT for AT, compounding PG-5. Needs a screen-reader transcript.
- **U-9 (NEW — this seat)** — the visual delta PG-17 suppresses: what the lag-cast choreography adds at a working consumer (the producer's own `<Card surface="cartoon">`, if one exists live) vs this card's scale-only press today.
- **Standing note** (both rounds): permission-denied as an empty species remains NOT ASSESSED — no auth-gated empty path reaches this grid in any trace; the D-5 limb stays killed unless the API's 401/403 handling is shown to surface here.

## DISSENT — preserved, not ruled away

1. **The second walk-past (fleet evidence, now a pattern).** The superseded record's DISSENT #1 recorded that a design-side re-reader cited the drag corruption's ledger receipts inside an argument about something else and never booked the corruption. THIS ROUND'S R2-DU did it again in a new costume: it confirmed C-5 by narrating the death of drag-reorder under a hypothetical fragment regression, cited the zero-reorder-coverage census — and never asked what the uncovered, live, un-regressed code does on a real drag. Two rounds, two design-side seats, same blind spot: **coverage-absence was treated as the finding when it is the reason the finding was still available.** Re-proposed as an addendum-law candidate for the next formation boundary: any row asserting "zero coverage of X" must state what X does when exercised, or file it explicitly as unassessed.
2. **R2-DU's comment-archaeology position (killed above, preserved here)**: 20 of 53 lines are decision provenance unreadable without the tranche corpus, and the one load-bearing comment is diluted by them. This seat holds the convention is house law and the registry's own superlatives depend on it; if the owner ever rules in-source provenance out, this row revives as the cluster's exhibit.
3. **The prior round's cardRefs severity dissent (MAJOR vs m2's MINOR)** stays preserved by reference; this round's R2-LC independently filed MINOR, so no live disagreement remains — but the revival condition stands (heap-snapshot witness if X-W7.d slips a formation).
4. **Count discipline, both readers**: every census this seat re-ran reproduced exactly (listitem 0, emptyAction 2, gridClass 3+1-consumer, reorder-in-e2e 0, ci-e2e 0, barrel importers 0, `demo/@` absent, corpus SHA byte-identical, `card-press-t` 0 in dist). One reader corroboration cell was struck as a non-sequitur (REG-PG-9 ruling) and one consequence limb demoted (published-tail) — the receipts were honest everywhere; the reasoning needed the two corrections recorded above.

## Closing verdict

**ADJUDICATED — RED AT THE SEAMS, NEAR-MODEL AT THE LEAF (verdict unchanged by a second hostile round; the roster grew at the seams again).** The 53-line component remains close to exemplary — twelve of seventeen axis accusations died against it across two independent reader pairs. The red is where it was: **PG-1 (first-drag order corruption, BLOCKER, now thrice live-witnessed) + PG-2 (filtered-drag corruption) on the very `$el` seam the component's best comment protects — still absent from X.W7.d's named defect list, so the named-addition rider is this record's most load-bearing line**; the both-states ARIA violation (PG-3 ≡ ES-9); the caster amputation (PG-4 ≡ M9); the filtered-zero falsehood (PG-5 ≡ ES-1); and four new seam rows this round (PG-14 skeleton fork, PG-15 gridClass redundancy, PG-16 message guard, PG-17 the unwired press-cast — the last found by neither axis nor reader). Routing: **PG-1/PG-2/PG-8 → X-W7.d (named-addition rider) · PG-4/PG-6/PG-12/PG-14/PG-15/PG-17 → X-W7.c · PG-7/PG-13/PG-16 → X-W7.g · PG-3/PG-5/PG-9 → X-W7 · PG-10 → X-W8 + NO-WAVE-OWNER residue (per AF-13) · PG-11 → X-W10 · PG-6's gate-honesty rider → X-W1.** Nine live questions feed the visual-audit worklist (U-1..U-9). Dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave.

— end of apotheosis, second pass. The three r1 challenge files and both R2 re-reader returns remain immutable beside this record per E-1/E-3; the first-pass record is superseded BY this file and restated within it, per the supersession clause above.
