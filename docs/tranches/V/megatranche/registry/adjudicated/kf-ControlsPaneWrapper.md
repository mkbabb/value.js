# kf-ControlsPaneWrapper — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-07 · **Subject repo**: `/Users/mkbabb/Programming/keyframes.js` at HEAD `8281638c` (identical to the kf-AnimationControlsGroup adjudication HEAD — line anchors are directly comparable) · **Corpus**: the three challenge axes at `docs/tranches/V/megatranche/audit/kf-components/ControlsPaneWrapper/` (challenge-D-design.md, three merged passes, 34 defects; challenge-L-library.md; challenge-C-consumption.md), as re-read by two hostile re-readers — **RR-1 (design-and-usage)** and **RR-2 (library-and-code)** — whose returns this record adjudicates.
**Method (L-14/L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (D-B1 severity, D-M3 severity, D-M5 grade-vs-banked-identity, D-M11's count, D-m4's grade, the D-M7 kill, MISS-1's static decidability) was re-derived against the tree with this seat's own instruments. Two "net-new missed" claims filed by BOTH re-readers were checked against the banked registry first — one (`--rail-width` svi-vs-cqi) turned out to be **already banked** as the parent's D-7; the identity guard caught it (see DISSENT item 2).
**Arbiter's own instruments this session (all static + node; ZERO browser tooling — probe parsimony)**: full reads of ControlsPaneWrapper.vue (319 L), ControlsPaneWrapper.css (148 L), usePaneHover.ts (65 L), useControlsLayout.ts (92 L), usePaneRegister.ts (41 L), RibbonBar.vue (151 L); targeted reads of ChannelControls.vue (:3-4/:32/:98/:117-137/:150/:180-200/:234-253/:283-297/:377-389), AnimationControlsGroup.vue (:4/:19/:79-88) + .css (:7-8/:24-26/:51/:65-68/:114/:142/:183/:197-201), ChromeDock.vue:330-331, App.vue:9-16, EditorShell.vue:76, scenes.ts stage-mode census, useScrollFade.ts:60-100, useKeyframesPaneReveal.ts:95-105, layout.css:38-105, design-idioms.css:12-21/:45-64/:92-128, style.css:1-16/:210-231, KeyframesStringControls.vue + KeyframeTimeline.vue `defineExpose` wires; **installed producers**: glass-ui 7.0.0 `dist/styles/utilities/a11y-overrides.css` WHOLE (the D-M7 decider), `dist/components/drawer/styles.css` (inset token + snap-rule + handle recipe), `dist/drawer.js` (:7 SpringProgress; data-attr emissions; class merge; translateY paint; handle-only `pointerdown` binding), `dist/styles/tokens/sizing.css` + `light-dark.css` (coarse block), `dist/components/button/styles.css` (`@layer components` head byte; recipe; `[data-size="sm"]`), `dist/styles/typography/semantic.css` + `scale.css` (`text-body`, `--type-body`), `dist/styles/utilities/btn.css` (full `@utility` census: 8 utilities, no `btn-interactive`; `rainbow-vivid` stop list names indigo, never cyan); reka-ui `Dialog/DialogRoot.js:52`, `DialogContentImpl.js:50/:78`, `Dialog/utils.js:14-18`; `@vue/runtime-core` `setScopeId` :5656-5681 (the MISS-1 propagation decider); **executed oracles**: `@vue/compiler-sfc` `compileStyle({scoped:true})` over the live selectors (scope attr lands on the LAST compound only — third independent derivation), `compileTemplate` over the live SFC (slot objects: THREE `_: 3 /* FORWARDED */` + three STABLE — the MISS-3 and N-1-kill decider), node contrast battery (α 0.35 → **2.179**, 0.60 → **4.471**, 0.61 → 4.616, 0.62 → **4.768**; yellow `hsl(55 90% 55%)` Y = **0.73844** → **1.33:1**; red 0.2280/3.78, orange 0.3811/2.44, green 0.5033/1.90, blue 0.2530/3.47, violet 0.2766/3.21 — all reproducing the corpus to ≤1e-3 except the killed yellow cell); **executed greps (exit-checked)**: `@mkbabb/glass-ui` 0/0 in package.json + lock, installed 7.0.0; `"check"` = bare tsc ×2; `drawer-inset` = 1 demo hit (the :26 comment denying it exists); `btn-interactive` = **8 consumer lines / 6 files, 0 producers anywhere**; `scroll-fade` rules = exactly 1 (`.css:56`); stage-class families = 4 hits, 0 rules; `aria-expanded|aria-controls` = **0 demo-wide**; `forced-colors|prefers-contrast|prefers-reduced-transparency` = **0 demo-wide**; `group/controls` = 1 (the declaration); `--rainbow-indigo` = 0 in demo, `oklch(0.566 0.206 294.1)` in glass; max-height-transition = sole hit AnimationControlsGroup.vue:83 (the SIBLING); `--ui-scale|--control-floor|--control-h-sm|--touch-target` = 0 demo overrides; `--dock-menubar-reserve` = 5 hits (the fan-out); `isDesktop` = 4 hits all in its own file; PRM import chain `a11y-overrides.css → accessibility.css → styles/index.css → "@mkbabb/glass-ui/styles"` = style.css:3 (the kill-ratifying edge); registry cross-reads: kf-AnimationControlsGroup.md (D-1, D-3, D-5, D-7, D-8/C-7, L-4(a)/L-8/M-8, C-4, S-4 PRM superlative, M-6, M-9), kf-App.md KF-APP-4, kf-CSSCodeEditor.md KF-CE-16, lane-frontend.md §0 F-1/§5 S-1..S-8/§6.3/§6.5/§10, keyframes-truth.md:335 (BG-11 EXTERNAL-BLOCKED), ColorInput.md D-2 (the value.js-side `btn-interactive` family precedent — different repo, cross-referenced, never merged).
Status verbs per M-25: dispositions are **ADJUDICATED, not VERIFIED** — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets follow the census lane taxonomy as instantiated by the parent record (`lane-frontend.md` §10): **KF.W1-DEP** · **KF.W2-TABS** · **KF.W3-SHIM** · **KF.W4-PROSE** · **KF.W5-PARTIALS** · **KF.W6-TIMELINE** · **KF.W7-TOKENS**; the vue-tsc gate identity is banked at kf-App **KF-APP-4** (its KF.W4 gates lane) and folded there. Everything the census does not own is **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring.

## Scoreboard

**49 rows stand: 4 BLOCKER · 15 MAJOR · 20 MINOR · 4 INFO booked here, plus 6 FOLDS to banked identities (one of them blocker-weight at its bank). 11 claims KILLED with executed refutations (two kills ratified by this seat's own bytes, one kill-of-a-count corrected in both directions). 14 net-new re-reader finds adjudicated in — 4 of them MAJOR a11y/gesture defects every original axis missed. Identity guards held: census F-1, parent D-1/D-3/D-7/D-8·C-7/L-4(a), kf-App KF-APP-4 — zero re-bookings, and one double-filed "missed" find was caught as already banked.**

Blocker-weight center of mass: the mobile sheet — the ONLY control surface below 1024px — ships with its bottom ≥44px actionable band under the dock menubar on every session (D-B1), while the discharge lever `--drawer-inset-block-end` sits shipped and unread in the installed producer and two comments in the file assert it does not exist. The #1 repair every axis prescribed is **scope-dead as written** (MISS-1, adjudicated MAJOR here with this seat's own compiler + runtime receipts); the corrected form is one `:style` binding.

## Ruled disagreements (the seat's actual work)

| question | RR-1 (design-and-usage) | RR-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-B1 / N-2 / C-2 severity** | BLOCKER | MAJOR | **BLOCKER — RR-1 wins** | The sheet is the sole sub-1024px control surface; the occluded band is the normal-case home of the ribbon's primary actions (overflow case = the scroll extent's end is the occluded edge — RR-1's own mechanism kill sharpened, not weakened, this); the defect is the file's self-declared headline (born-RED BG-11 row, `.vue:22-26`), and the lever is shipped: my grep of `dist/components/drawer/styles.css` finds `--drawer-inset-block-end: 0px` + the `[data-glass-drawer-snap-points="true"][data-glass-drawer-direction="bottom"]` rule, and `drawer.js` emits both gating attrs on the class-merged element for exactly this consumer's config (`direction="bottom"` :120, 2-rung ladder :280). One demo hit for the token: the `.vue:26` comment claiming it does not exist. The 4-site stale fan-out is real (my grep: layout.css:78-88 "so the mobile sheet always clears the menubar", TransportDock.vue:260, AnimationControlsGroup.css:183, CubeScene.vue:270-275 — and 0 hits in ControlsPaneWrapper.css). Precedent: the parent booked D-1 BLOCKER for majority-scene damage to a first-class affordance; every-mobile-session occlusion of the only surface rates the same. RR-2's MAJOR is preserved as dissent. |
| **MISS-1 (RR-2): the prescribed cure is scope-dead** | Filed the same doubt as UNPROVEN | MAJOR, static-decidable | **ADOPTED at MAJOR — RR-2's static claim CONFIRMED by this seat's own two receipts** | (a) My `compileStyle({scoped:true})` run: a rule whose only selector is `.controls-drawer-content` emits `.controls-drawer-content[data-v-abc123]` — the attribute lands on the LAST compound, so the cure-as-written requires the scope attr ON the portalled `.glass-drawer`. (b) My read of `@vue/runtime-core` :5656-5681: `setScopeId` walks up ONLY while `vnode === parentComponent.subTree`; DrawerContent's element is a child of the portal's Teleport, not its subTree, so the chain breaks — the consumer's `data-v-*` cannot reach `.glass-drawer` by propagation, and scope ids are renderer-level, not `$attrs`. Corroboration RR-2 also saw: every existing `.controls-drawer-content` rule in the file uses it as an UN-attributed ancestor (:41/:47/:56) — the file has never once needed the attribute to land there. The sound form is `<DrawerContent :style="{ '--drawer-inset-block-end': … }">` (drawer.js merges `style`); a live attribute-list read stays SS-13 residue. This is the ColorSpaceSelector addendum's law instantiated: **the cure is adjudicated separately from the defect** — the corpus's #1-sequenced repair, in the exact form all three axes wrote it, would have joined the component's own dominant failure class (declarations that resolve to nothing). |
| **D-M7 kill (PRM coverage gap)** | KILLED | (did not adjudicate) | **KILL RATIFIED — this seat's own bytes** | The finding's falsifier was searched in the wrong directory. My read of `dist/styles/utilities/a11y-overrides.css`: `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important } … }`, reaching the demo via `accessibility.css → styles/index.css → "@mkbabb/glass-ui/styles"` = style.css:3 (my grep of each edge). `!important` producer declarations beat every author normal declaration regardless of layer, so ALL THREE of the file's opacity transitions clamp to 0.1s under PRM — the "0.3s in / 0.2s out still animates" coverage gap does not exist. Consistency check passed: the parent record's banked S-4 superlative rests on the same bytes. The inversion (the component's own PRM block is INERT under the same rule) is adopted as a roster row — see INFO. |
| **D-M5 grade vs the banked parent D-3** | MAJOR | MAJOR | **FOLD → parent D-3 (MINOR as rescoped there) — identity guard over both readers' grades** | The parent's D-3 row already books "one desktop/mobile boundary in three unit systems across five sites (four CSS + one JS)" — the one JS site being **this component's** `.vue:254` — with the rem-band branch UNPROVEN (its SS-13 #1) and the spec reading recorded. One identity, banked first at the parent. Both re-readers' own kills (the "fixed stage under a desktop rail" state cannot occur; the scrollbar instance dies on `html,body{overflow:hidden}` — my style.css:218-230 read) removed precisely the damage claims that would have re-graded it MAJOR. This record adds the riders the bank lacked: `usePaneRegister.ts:38` is a SIXTH site (second JS instance), and the wrapper's own `.css:40/:81` forks are named members. |
| **D-M3 / mi-5 / C-14 severity (0.48 prose vs 0.40 shipped)** | MAJOR | MINOR | **MINOR — RR-2 wins** | The SHIPPED value is the correct one by the file's own floor derivation (`.vue:270-274`: 0.48 would leave 0.41 < the quoted 0.45 unoccluded floor); only the prose is wrong (3 sites + the `proof:stage-visible` gate pointer, `.css:19-21`). Prose-drift with correct code is the parent's D-7 precedent — MINOR, prose-truth repair. RR-1's MAJOR preserved as dissent. |
| **D-M11 count** | "eight call sites … plus RibbonBar.vue:135" (= 9) | eight total incl. RibbonBar | **RR-2's census exact; RR-1 over by one** | My grep: exactly **8 lines / 6 files** — RibbonBar.vue:135, PlaybackRibbon.vue:55, CubeScene.vue:188/:193, SequenceTarget.vue:31, SpringScene.vue:167, SpringPhysicsFacet.vue:74/:105. Zero producers: btn.css's full `@utility` census is `scale-on-hover, transition-control, transition-disclosure, sheet-animate, table-cell, table-head, rainbow-vivid, rainbow-pastel`; the demo's own is `icon-{xs,sm,md,lg} + ppmycota-stroke`. The original axis's "six" undercount stays killed; the kill's replacement count is now pinned at 8. |
| **D-m4 / C-16 grade (stale provenance pins)** | MINOR | INFO | **MINOR — RR-1 wins** | Not mere comment rot: the "glass-ui 4.0.1"/"4.0.0" pins (`.vue:7/:197`) are the proximate mechanism of the BLOCKER — the consumer stopped re-reading the producer at 4.0.x and so never saw the 7.0.0 lever. The dogfood claim itself survives (drawer.js:7 is live — my read). Grade follows causal weight. |
| **`--rail-width` "missed" find (filed net-new by BOTH readers)** | filed as missed, MINOR | filed as MISS-5, MINOR | **FOLD → parent D-7 — already banked; NOT net-new; zero re-booking** | kf-AnimationControlsGroup.md D-7 books exactly this: `.css` rationale asserts `clamp(20rem, 26cqi, 30rem)` "tracking the work-area card"; shipped is `clamp(25rem, 33svi, 32rem)` (my design-idioms.css:47 read) — a viewport unit voiding the container-promotion rationale. Both readers derived it independently and correctly; neither checked the bank. This record adds only the wrapper-side rider: `.css:122-128` binds `.controls-content { width: var(--rail-width) }` and names it "the single width authority", so the pane's width is viewport-derived while its placement is container-derived — D-M5/parent-D-3's fork disease one layer down, filed at the D-7 identity. |

**Cure-level rulings inside agreed verdicts**: D-B3's repair magnitude is **α = 0.62**, not the corpus's 0.60 — my own battery: 0.60 → 4.471 (below the 4.5 floor it is prescribed for), 0.62 → 4.768; D-C3's correction is ratified over challenge-D-design.md:735. D-M10's cheaper repair is RR-1's missed find, adopted INTO the row: `isPaneIdle` already computes `idle && !isPaneHovered` (usePaneHover.ts:53) so the two classes are mutually exclusive in JS and the `:not(.controls-pane--hovered)` filter (.css:114) filters nothing — it exists only to manufacture the specificity that defeats `:focus-within`; deleting it restores the documented behavior by source order with zero new selectors (my compileStyle run confirms post-scope (0,3,0) vs (0,3,0), lift later). The D-B1 inset's token choice (`--dock-menubar-reserve` vs `--dock-band-reserve` vs the `-stable` peak sibling layout.css:90-98 whose own comment argues for it) is NOT adjudicable statically — SS-13 measures the dock tether's top edge at both detents first.

## Adjudicated defect roster

Line anchors are HEAD `8281638c`; re-resolve before executing any cure (D-19). Dispositions are ADJUDICATED (M-25); routings are agglomeration input for the forming X·KF specs.

### BLOCKER

- **D-B1 / N-2 / C-2 — ADJUDICATED BLOCKER (ruled) · the BG-11 occlusion's own named discharge lever ships in installed glass-ui 7.0.0, unconsumed; the file declares the gap structural; four cross-file claims are stale.** Lever: `:root{--drawer-inset-block-end:0px}` + the snap-points/bottom rule in `dist/components/drawer/styles.css`; both gating attrs emitted for this consumer (my drawer.js reads). Sole demo reference: the `.vue:26` comment asserting it does not exist. Stale fan-out (my grep, 4 sites) named above. Discharges keyframes-truth.md:335's BG-11 EXTERNAL-BLOCKED residue — the block is LIFTED; the born-RED row's dischargedBy condition (`--drawer-inset-block-end` publish) is MET and only consumer adoption remains. **Cure form corrected per MISS-1** (`:style` on `<DrawerContent>`, never the scoped-selector declaration); token choice → SS-13. Per D pass-3 sequencing, D-M12's detent budget recomputes in the same edit. Disposition: **NO-WAVE-OWNER** (SS-1/SS-2) **+ glass-ui BH relay note** (owner edict): BG-11 is discharged producer-side; the relay closes the loop, no producer work remains.
- **D-B2 + D-C1 — ADJUDICATED BLOCKER · the closed desktop rail is hidden by opacity alone — focusable, in the a11y tree, inside a 0px clipped track — and the focus-lift acts on the wrong element so nothing is ever revealed.** My reads: `.css:97-99` (`pointer-events:none` only) + `:104-107` (`opacity:0` on the INNER `.controls-pane`); zero `visibility|inert|display:none|content-visibility` in the pair; `:focus-within{opacity:1}` (:117-120) raises the already-1 WRAPPER, composite stays 1×0=0 (D-C1's correction ratified). The `:inert` primitive with the exact rationale sits one directory over (ChannelControls.vue:117-137 — my read: "not bare aria-hidden, which leaves focusable Monaco descendants in the tab order"). Compounds with the aria-expanded miss below: the void is unfocusable-looking, focusable, and unannounced. Disposition: **NO-WAVE-OWNER** (a11y spec row; pairs with the parent's D-5/M-6 a11y cluster).
- **D-B3 + D-C3 — ADJUDICATED BLOCKER · the 10s idle rest-dim ships body text at 2.18:1 under a "legibility-preserving" rationale, and the corpus's own prescribed repair magnitude lands under the AA floor.** My third-independent battery: α=0.35 → CR **2.179** (both readers reproduce); D-C3's bound argument (max ≈3.80:1 over all achromatic backdrops) is the load-bearing form; prescribe **0.62** (→4.768), not 0.60 (→4.471). Token: layout.css:41; consumer `.css:114-116`; `IDLE_MS=10_000` (usePaneHover.ts:7). The PRM block preserves the dim by design (`.css:139-148`) — the magnitude, not the mechanism, is the defect (see superlative S-5). One-token cure + docs. Disposition: **NO-WAVE-OWNER**; lands WITH D-M10's cascade fix (one batch, per D's sequencing as amended).
- **D-B4 — ADJUDICATED BLOCKER (headline RESTATED) · the Apply-CSS active state paints white body-scale text over the full-chroma seven-stop rainbow; every stop fails 4.5:1, three fail 3:1 — worst stop 1.33:1, NOT the corpus's 1.22:1.** My battery reproduces six of seven stops to 4 places and corrects yellow (Y 0.73844 → 1.33:1; the 0.8136 cell is arithmetic error — see killed register). Cascade verified: `rainbow-vivid`/`text-white` emit `@layer utilities`, glass recipes sit `@layer components` (my head-byte read) — the gradient and the white DO win. `.ribbon-apply--active` removes the border, the last non-color cue (RibbonBar.vue:148-150). Pairs with the state-legibility miss and D-m10 below; batch with D-M11 (both live in `RIBBON_BUTTON_CLASS`). Disposition: **NO-WAVE-OWNER**.

### MAJOR (booked here)

- **D-M1 / M-2 / C-3 — MAJOR · `isPanelTransitionDone` is a latch that clears but can never set: its sole setter waits on a `max-height` transitionend no element in the served cascade emits.** My exhaustive grep: the only max-height transition in the subtree is AnimationControlsGroup.vue:83 on `#timeline-expanded-target` — a SIBLING (transitionend bubbles up, never sideways). `.css:82-90` ships `max-height:none`; `.css:93-95` records the axis deleted; `.vue:296-298` still calls it "the max-height transitionend gate" — the file contradicts itself 200 lines apart. Session-scope phrasing bounded to the MOUNT (C-3's "session" kill, EditorShell:76 `:key`). Disposition: **NO-WAVE-OWNER** (one repair with D-M2; delete the latch or move the transition to the wrapper).
- **D-M2 / M-3 / C-4 — MAJOR · `paneScrollable` is inert on both arms: the mobile arm is overridden by the file's own unlayered scoped rule (`.css:40-46` unconditional `overflow-y:auto; touch-action:pan-y`), the desktop arm has no bounded ancestor to scroll against.** Layer mechanism third-derived by my compileStyle run (unlayered scoped beats `@layer utilities`; specificity (0,3,0) — the corpus's (0,4,0) cell is killed, verdict intact). Desktop: wrapper is auto-height (`items-start` parent, `max-height:none`, no stretch), content overflows the definite `.controls-layout` height into `html,body{overflow:hidden}` — unreachable by absence of a scroller, not by clipping (mechanism kill honored). The stated design (`.vue:295-298`) is correct and is exactly what the stylesheet overrides. Disposition: **NO-WAVE-OWNER** (with MISS-2's height-chain rider below; SS-13 measures whether the rail bites today).
- **D-M4 + mi-2 — MAJOR · the declared stage visual register paints nothing: `controls-pane--stage-*` and `controls-drawer--stage-*` are class families with zero rules anywhere (demo or producer dist), documented as a visual contract three times, with all three modes live inputs (my scenes.ts census: subject ×4, editor, storyboard); and `usePaneRegister` also returns a dead `isDesktop` (my grep: 4 hits, all its own file) — a 41-line wrapper whose live body is one `??`.** The PARENT-owned family `controls-layout--stage-*` is already banked (kf-AnimationControlsGroup D-8/C-7) — that member folds there, not re-booked; the two wrapper families + the dead media-query listener book here. Disposition: **NO-WAVE-OWNER** (implement the register or delete the driver + the prose; the `isDesktop` deletion also retires one of the three duplicate 1024px `useMediaQuery` instances). 
- **D-M8 — MAJOR · `PEEK_SNAP = 0.12` is a bare viewport fraction against fixed-px chrome: the residual band under the 44px handle swings 24.2px (568px vh) → 67.8px (932px), under the producer's own 54px coarse-pointer control floor on short viewports.** Terms all mine: handle `min-block-size: var(--touch-target, 2.75rem)`, `--touch-target: 2.75rem` unoverridden (0 demo hits), visible band = t·vh (drawer.js translateY paint). Understated if anything (the S-7 floor is 54px). Disposition: **NO-WAVE-OWNER**; recompute WITH the D-B1 inset (the denominator changes).
- **D-M9 / C-8 — MAJOR · the hand-rolled scroll fade styles one of its three emitted states, gated to mobile only — the "more below" cue is absent exactly at rest (`scrollTop === 0` can only emit `-bottom`, which has no rule), and the desktop rail styles none of three while paying the listener + ResizeObserver.** My reads: useScrollFade.ts computed (both/top/bottom); sole rule `.css:56` double-gated (`@media max-width:1023px` + mobile-only ancestor); the `.css:53` comment names `scroll-fade-y`, a class the composable cannot emit. glass-ui ships `/fading-scroll` and the demo consumes it elsewhere. Disposition: **NO-WAVE-OWNER** (adopt the producer primitive or complete the three rules; MISS-6's guard note applies).
- **D-M10 — MAJOR · the documented `:focus-within` instant lift is cascade-dead: the idle rule's `:not()` manufactures (0,3,0) against the lift's (0,2,0) (post-scope (0,4,0) vs (0,3,0) — my compileStyle run), and only the pointer path is protected, by a class focus never writes.** Contract asserted twice in prose (usePaneHover.ts:23, `.css:112-113`). **Adopted cure (RR-1's missed find, ratified): delete the redundant `:not(.controls-pane--hovered)`** — the classes are mutually exclusive in JS (usePaneHover.ts:53), the filter only exists to lose the fight; source order then restores the lift. Strictly smaller than the corpus's append-two-more-guards prescription. Disposition: **NO-WAVE-OWNER**; lands with D-B3.
- **D-M11 (+ the gap-1.5 rider) — MAJOR · `btn-interactive` is a phantom utility: 8 consumer lines / 6 files (my census, pinned), zero producers in the demo or anywhere in installed glass-ui.** With RR-1's fifth-token completion adopted: `RIBBON_BUTTON_CLASS`'s entire five-token string is dead (`btn-interactive`), unreachable (`h-8` vs `min-block-size` — D-m12), duplicative (`rounded-full` vs `--radius-pill`), or actively de-scaling (`text-body`, `gap-1.5` pinning 6px where the coarse recipe wants 9px) — the D S-7 reading (delete the string, don't strengthen it) is the ruled cure. **Cross-repo identity note**: the value.js demo has its own banked `btn-interactive` family (ColorInput.md D-2 et al., different repo, different consumers) — same disease, two trees, cross-referenced NEVER merged; the producer-side question (ship the atom or let both consumers delete) already rides the standing glass-ui BH relay from the value.js side. Disposition: **NO-WAVE-OWNER** (keyframes-side deletion; batch with D-B4).
- **D-M12 — MAJOR · at `EXPANDED_SUBJECT = 0.4`, ~49% of the mobile sheet is fixed chrome before one control draws: 44 (handle) + 24 (`p-3`) + 54 (coarse ribbon row) + 8 (`pb-2`) = 130px of 267px at 667px vh.** Every term re-derived from tokens by this seat; the coarse block is unopposed (0 demo overrides). Wrap-count residuals → SS-13. Disposition: **NO-WAVE-OWNER**; recompute with D-B1 (the inset shrinks the denominator further — the pass-3 sequencing amendment is ratified).
- **M-4 / C-7 — MAJOR · the host `v-for` mounts EVERY channel under `v-show` — N ChannelControls, N force-mounted Monaco panes, N timelines — defeating the child's own LCP remediation.** My reads: `.vue:45-49`; `scheduleIdleWarm()` unconditional at setup (useKeyframesPaneReveal.ts:102); force-mount predicate reads scene-wide state with no host parameter (ChannelControls.vue:130/:297); `defineAsyncComponent` pair :252-253 (module-deduped, instance-multiplied). N=3 on cube and amiga (banked at parent D-1's enumeration). Disposition: **NO-WAVE-OWNER** (mount policy: active-host `v-if` + keep-alive, or per-host warm gating); byte/latency magnitude → SS-13.
- **N-4 — MAJOR · the ribbon's eight actions render enabled on a store string, not target readiness — silent no-ops through the async-resolution window.** All nine expose wires verified LIVE by this seat (KeyframesStringControls :171-181, KeyframeTimeline :296-300) — so this is N-4's window, NOT a broken name (C-12's framing kill honored); the `?.()` chains convert the window into silence. Seam owned here (`:disabled="!activeKeyframesRef"` at `.vue:94-95`). Disposition: **NO-WAVE-OWNER**.
- **N-1 / C-5 — MAJOR · a four-level `#tabs-trigger` scoped-slot chain terminates in a child that declares no such slot (my grep: ChannelControls slots = :32/:180, both `tabs-content`), dead at both ends — this component publishes a scoped-slot contract (`selectedAnimation`, `isPlaying`) it structurally cannot honor.** Running-cost sub-claim killed (my compileTemplate run: `_withCtx` lazy closure, never invoked). Same dead-tabs-plumbing family as the banked parent C-4 (`extraTabs`, which this file also forwards at :65) = census S-2's sharp instance. Disposition: **FOLD → KF.W2-TABS / KF.W4-PROSE** at the parent C-4 / census S-2 identity (the chain deletes end-to-end in the tabs wave).
- **MISS-1 (RR-2, adopted) — MAJOR · the corpus's #1-sequenced repair is unsound as written** — ruled above; both static halves carry this seat's own receipts (compileStyle; `setScopeId` :5656-5681). Robust forms: `:style` on `<DrawerContent>` (mergeProps-carried), a global/`:deep()` rule, or `:root`. Rendered attribute list → SS-13 #1. Disposition: rides D-B1 (its cure clause).
- **MISSED (RR-1, adopted) — MAJOR · the desktop rail has no role, no accessible name, no landmark — the SAME body is a named `role="dialog"` on mobile and an anonymous `<div>` on desktop.** My greps: reka emits dialog + labelledby via DrawerTitle (`.vue:134`); the desktop wrapper (`.vue:142-157`) is a bare div; `role=` = 0 in the component cluster. `createReusableTemplate` shares everything except the one thing carrying semantics. Repair is one line (`role="region"` + `aria-label="Animation controls"` — the string DrawerTitle already carries). Disposition: **NO-WAVE-OWNER** (a11y spec row WITH parent D-5/M-6 — one spec surface, two components' edges).
- **MISSED (RR-1, adopted) — MAJOR · the controls-pane disclosure has no `aria-expanded`/`aria-controls` — zero of either in the ENTIRE demo (my grep) — and the toggle's accessible NAME mutates with state instead (`ChromeDock.vue:330-331`), breaking voice control (WCAG 2.5.3) and leaving AT stateless (4.1.2).** The disclosed region carries no `id` (that half of the repair lands in this file). Extends the parent D-5 grep (transport-scoped) demo-wide; the toggle lives in app/dock, outside the parent's booked sink row — a distinct edge on the same spec surface. Compounds D-B2 exactly. Disposition: **NO-WAVE-OWNER** (same a11y spec row).
- **MISSED (RR-1, adopted) — MAJOR · gesture inversion at the peek detent: the body is a scroll container with `touch-action: pan-y` at EVERY detent (`.css:45`) while the producer binds the drag exclusively to the 44px handle (my drawer.js read: pointer listeners attach to `handleEl` alone; handle is `touch-action:none`)** — so a swipe-up on the peek band scrolls a 24-36px sliver instead of expanding the sheet, inverting the bottom-sheet convention. The `touch-action: pan-y` is not inert intent (D-M2's framing); it is the declaration actively handing the expand gesture to the scroller. The stated design (`.vue:295-298` — scrollable at the OPEN detent only) is correct and overridden. Disposition: **NO-WAVE-OWNER** (one repair with D-M2's mobile arm); live gesture confirm → SS-13.

### FOLDS to banked identities (identity guard; zero re-booking)

- **B-1 / C-1 — FOLD → census F-1 (KF.W1-DEP), MAJOR at the bank.** Phantom `@mkbabb/glass-ui` (my greps: 0/0 manifests, 7.0.0 installed). Component rider the census could not see: `.vue:166` is the demo's SOLE `/drawer` consumer, statically on every scene's shell path — resolution failure deletes the entire mobile control surface at the root of the transport tree.
- **C-18 — FOLD → kf-AnimationControlsGroup D-1 (BLOCKER, banked by enumeration).** The N-teleports-one-sink stacking is D-1's identity; this record adds the wrapper-side mechanism cell (the `v-else` branch path, `active` spent only on the ribbon teleport) and corroborates the mount-all premise (M-4). Rendered stacking stays the parent's SS-13 item.
- **M-1 / C-6 / D-i3 — FOLD → parent L-4(a) + L-8 + M-8 (MINOR at the bank, corrected cure carried there).** `animControlRefs` writes with `if (el)` swallowing unmount-null; key-axis mismatch (`host.animation.id` loop vs `host.name` registry). Riders added here: RR-2's verified addendum that Vue's per-patch unset-old-then-set-new makes the guard currently load-bearing against churn (a naive guard-deletion is wrong; `else delete` is right because unset precedes set), and D-i3's design consequence — the mobile `v-if` / desktop `v-show` fork does not hand off across 1023/1024, it replaces (scroll, Monaco state, active tab discontinuity).
- **M-5 / C-9 — FOLD → kf-App KF-APP-4 (the KF.W4 gates lane), banked.** No `.vue` typechecked anywhere (my package.json read: `check` = bare tsc ×2). Riders this component contributes: four `any` props (`.vue:182/:194-196`), the SegmentedTabs→KfPillTabs field-loss bridge silently discarding `icon`/`tooltip` (C-10), and the `activeSnapPoint` v-model variance (C-11) — all first diagnostics for the banked gate's born-RED.
- **D-M5 — FOLD → parent D-3 (MINOR as rescoped)** — ruled above; riders: sixth site + the wrapper's CSS forks; rem-band branch stays the parent's SS-13 #1.
- **`--rail-width` axis-change (RR-1 missed + RR-2 MISS-5) — FOLD → parent D-7 (MINOR, banked)** — ruled above; wrapper rider: `.css:122-128`'s "single width authority" binding makes the pane width viewport-derived under container-derived placement.

### MINOR

- **D-M3 / mi-5 / C-14 — MINOR (ruled) · three prose sites + the named `proof:stage-visible` gate pointer state detent 0.48 against shipped `EXPANDED_SUBJECT = 0.4`; 0.48 would violate the file's own quoted 0.45 floor.** Prose-truth repair; the code is right. NO-WAVE-OWNER.
- **D-m12 — MINOR · `h-8` is structurally unreachable: `.button { min-block-size: var(--button-size) }` floors at 36px fine / 54px coarse; `height` vs `min-block-size` is no cascade contest.** Dies with D-M11's string deletion. NO-WAVE-OWNER.
- **D-m13 — MINOR · `text-body` + `icon-sm` decouple label and glyph from `--ui-scale`: utilities (later layer) beat the `@layer components` recipe; `--type-body` carries no scale term (my token reads); `"icon-sm"` passes the `:not([class*="size-"])` guard.** Dies with the same deletion. NO-WAVE-OWNER.
- **D-m3 — MINOR · FOLD → KF.W7-TOKENS (census §6.3; sibling of parent D-12).** `--controls-idle-opacity` — a WCAG-relevant magnitude — published unprefixed to the flat global namespace (layout.css:41), restated as a hardcoded fallback at its sole consumer (`.css:115`, the portalled-body divergence path), and mis-attributed to design-idioms.css by its own docblock (usePaneHover.ts:22 — my read); `@property --rail-width` is document-globally registered (design-idioms.css:58-64).
- **D-m4 / C-16 — MINOR (ruled) · provenance pinned to 4.0.x against installed 7.0.0; every dist line citation dead (`drawer.js:6`→:7; "drawer.css :53/:134" against a zero-newline minified file).** The stale pin is the BLOCKER's mechanism; the dogfood claim survives. Prose-truth; any wave touching the file corrects it. NO-WAVE-OWNER.
- **D-m5 / N-6 — MINOR · `group/controls` is a named Tailwind group with zero variant consumers (my grep: 1 hit, the declaration).** NO-WAVE-OWNER (hygiene).
- **D-m6 — MINOR · the prose sells "the exact peek/half/full" parity twice; two detents ship; the producer default had the missing rung.** The 0.12→0.62 editor fling crosses the whole readable stage with no rest. NO-WAVE-OWNER (detent design rides D-B1/D-M8's recompute).
- **D-m8 / mi-1 — MINOR · three homes: the SFC in `controls-pane/`, its three composables in a same-named sibling directory (`ControlsPaneWrapper/` — my ls), `useScrollFade` in a third (`transport/composables/`); imports go out-and-back-down; `usePaneRegister` calls itself "colocated".** NO-WAVE-OWNER (hygiene; the tree's own convention is `channel-controls/composables/`).
- **D-m9 — MINOR · zero forced-colors / prefers-contrast / prefers-reduced-transparency handling demo-wide (my grep: 0 across all three); both opacity mechanisms (0.35 dim, opacity-0 hide) survive forced-colors, which overrides colors, not opacity.** Sharpened: the producer models the axis (a11y-overrides.css ships a `forced-colors: active` block — my read), so the consumer opts out of an axis its design system honors. NO-WAVE-OWNER (a11y spec input).
- **D-m10 — MINOR · the demo overrides six of seven `rainbow-vivid` stops in hsl and leaves `--rainbow-indigo` at glass's oklch — inside a gradient it does not own; the demo's set defines cyan (unconsumed) and omits indigo (consumed).** The tell: indigo is the only stop clearing 4.5:1 in D-B4's table. Rides the D-B4 batch. NO-WAVE-OWNER.
- **D-m11 / N-3 / C-19 — MINOR · dangling `aria-describedby`: reka's `descriptionId ||=` overwrites the `""` seed and emits unconditionally; the warn gate's operands are both truthy (my DialogRoot/DialogContentImpl/utils reads); no `DrawerDescription` rendered though glass exports one on the same subpath.** Pass-1's recorded NON-defect (D-i0) stays overturned — with its method lesson (a recorded non-defect must carry the strongest falsifier, not the first sufficient one). NO-WAVE-OWNER (one-line cure either way: render a description or `:aria-describedby="undefined"`).
- **N-5 / mi-7 / C-15 / D-M6 — MINOR · the mobile peek reset is a setup-time one-shot writing a persisted store prop (`.vue:260-262`): it never refires on a viewport crossing (the fork is INSIDE the component; no `:key` boundary a resize crosses) and clobbers the stored open-fact across layouts.** NO-WAVE-OWNER (watch the boundary, or scope the reset to the mobile bucket).
- **mi-3 / mi-4 / mi-9 — MINOR (bundle) · one breakpoint declared three times in one subtree via `useMediaQuery` (`.vue:254`, useControlsLayout.ts:48, usePaneRegister.ts:38 — one of them dead); a verbatim six-event re-emit block duplicating the child's (`.vue:305-316`, only `layerConfigUpdate` adds information); a redundant `RibbonBar` `v-if` guard inside a body `showSheet` already gates.** NO-WAVE-OWNER (hygiene).
- **C-20 — MINOR · RibbonBar reaches the root barrel (`.vue:132`) where published `./button`/`./card` subpaths exist, 34 lines from the file that models the deep-import idiom.** NO-WAVE-OWNER (hygiene).
- **D-C2 — MINOR · S-3's premise is false: the sheet body carries no visible facet headings (my grep: one `<h3>`, in a drill-in); the `.vue:132-133` comment asserting otherwise is what stops a reviewer noticing; the only rest-state orientation cue is AT-only.** The self-correction's shape is right: keep the a11y praise, retract the visual-register premise. NO-WAVE-OWNER (rides the D-M4 register decision).
- **MISSED (RR-1, adopted) — MINOR · the idle rest-dim does not follow the timeline it drives: expanded, `KeyframeTimeline` teleports to a SIBLING cell (ChannelControls.vue:186 → AnimationControlsGroup.vue:79-88), so the rail dims to 0.35 while the instrument it commands stays at full opacity — and the `:focus-within` lift cannot see focus inside the teleported subtree.** Cross-references parent D-1's geometry. NO-WAVE-OWNER (the idle contract must name its subject; rides any D-B3 batch).
- **MISSED (RR-1, adopted) — MINOR · the Apply-CSS applied/unapplied distinction is carried by the POSITION of one palette (stroke vs background) with no second channel — no border, check, text change, or `aria-pressed` (my RibbonBar reads: :49-51/:55-62/:148-150); `border-color: transparent` deliberately removes the last edge cue.** WCAG 1.4.1; cheap fix serves D-B4 (keep the plate, add `aria-pressed` + an edge, drop `text-white`). NO-WAVE-OWNER.
- **MISS-2 (RR-2, adopted) — MINOR · the class-less `v-show` host div at `.vue:49` severs the flex height chain: as a default `flex: 0 1 auto` column item it is content-sized, so ChannelControls' `h-full` resolves against auto and its inner `flex-1 min-h-0` scrollers never bound.** The proximate, this-file-owned cause under D-M1/D-M2's symptom; bounding `.controls-pane` alone leaves the inner scroller unbounded because :49 sits between them — repair-order load-bearing. NO-WAVE-OWNER (rides the D-M2 repair).
- **MISS-3 (RR-2, adopted; premise re-executed) — MINOR · the forwarded scoped slots mark slot objects `_: 3 /* FORWARDED */` (my compileTemplate run: three of them), defeating slot stability — every wrapper re-render (hover linger, idle flip, scroll-fade threshold, paneScrollable) force-re-renders all N mounted ChannelControls subtrees, each retaining Monaco.** Scoping note ratified: deleting the dead `tabs-trigger` forward does NOT retire it (`tabs-content`/`ribbon-content` are legitimate); the cure is hoisting or `v-if="$slots.x"` guards. NO-WAVE-OWNER (efficiency; multiplies M-4).
- **MISS-4 (RR-2, adopted) — MINOR · `usePaneHover`'s entire output is unconsumed on mobile (the classes bind only on the desktop branch, `.vue:152-153`) yet the composable constructs `useIdle(10_000)`'s global window-listener set + the linger timer on every mobile mount — dead reactivity on the layout the file's own comments call LCP-sensitive.** NO-WAVE-OWNER (gate the composable on the branch).

### INFO

- **i-1 — INFO · the two halves disagree about whether the body is expensive: mobile `v-if` destroys what desktop `v-show` preserves, while the child documents the opposite discipline for its own Monaco pane.** Recorded beside M-4 (the mount-policy row owns the resolution).
- **D-i2 — INFO (promoted to this record's cascade law) · the tree has THREE tiers, not two: unlayered Vue-scoped CSS > `@layer utilities` > `@layer components` glass recipes.** The mirror halves decide five rows in this corpus at once (D-M2's override wins; D-m13/D-M11's utilities beat recipes; D-M7's kill via unlayered `!important`). Analysis row; no cure.
- **MISSED (RR-1, adopted) — INFO · the component's own PRM block (`.css:144-148`) is INERT: the producer's unlayered `*:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity … !important }` defeats the local `transition: none` under the same media condition (my bytes + import-chain reads).** Presence ≠ effect — strictly stronger than the killed D-M7's presence ≠ coverage, and census §6.5's inventory counts the block as one of the demo's 10. Honest disposition: delete the block + a comment delegating PRM wholesale to the producer (which the file already claims for the mobile half). NO-WAVE-OWNER (prose/dead-code hygiene; census §6.5 correction rider).
- **MISS-6 (RR-2, adopted) — INFO · guard note: `useScrollFade`'s `retrigger` hook is inert on the styled layout (fed the dead D-M1 latch) but the ResizeObserver path self-corrects, so the fade class derives correctly — the C-8 repair must not chase a phantom re-check bug on top of the real missing-rules bug.**

## Killed-claims register (executed refutations; nothing re-litigable without new evidence)

1. **D-M7 (whole finding) — KILLED, ratified by this seat's bytes.** "PRM guard covers one of three opacity transitions — the open/close cross-fade still animates (0.3s/0.2s)." The falsifier it stated was searched in `demo/styles/` + `dist/glass-ui.css`; the reset lives at `dist/styles/utilities/a11y-overrides.css` and reaches the demo through `styles/index.css` (style.css:3). All three transitions clamp to 0.1s under PRM. The coverage gap does not exist; the inversion is banked as the INFO row above.
2. **D-B4's yellow cell + the 1.22:1 headline — KILLED, third-derived.** `hsl(55 90% 55%)` → Y = 0.73844 (not 0.8136) → **1.33:1** (my battery; both readers agree to ±2e-4). The defect survives untouched — 1.33 is still the worst stop and still catastrophic — but the corpus quotes 1.22 three times including its verdict headline; downstream quotation must use 1.33.
3. **D-M5's stated in-band consequence — KILLED.** "A full-bleed fixed stage sits behind the desktop rail; no Drawer renders." The parent's mobile fork is `@media (max-width:1023px)` (same axis), not a container query — in the divergence band NEITHER fork applies and everything falls to auto-placement. The divergence is real (folded to parent D-3); the described damage state cannot occur.
4. **D-M5's scrollbar instance — KILLED.** `html, body { overflow: hidden }` in `@layer base` (my style.css:218-230 read) suppresses the document scroll port; the classic-scrollbar gutter the 1024-1039px argument needs never exists.
5. **D-B1's pass-2 ribbon-pinning mechanism — KILLED.** Nothing pins the ribbon to the sheet bottom: no `mt-auto`, no `justify-between`, no `flex-1` sibling (my SFC read; hosts are bare divs; `h-full` resolves to auto). In the non-overflow case the ribbon sits under the last host, clear of the dock band. The BLOCKER is unaffected (overflow is the normal case and the scroll extent's end IS the occluded edge), but a repair lane must not hunt a `mt-auto`.
6. **C-3's "for the rest of the session" — KILLED.** EditorShell.vue:76 `:key="superKey"` re-mounts per scene; the latch re-seeds from the stored open-fact. Damage is bounded by the MOUNT (M-2's phrasing is the correct one).
7. **C-3's clipping mechanism — KILLED.** Neither box has bounded height on desktop; `overflow: hidden` on an auto-height block clips nothing vertically — the harm survives by the height chain + the absent scroller (the roster row carries the corrected mechanism). A repair hunting `overflow` declarations fixes nothing.
8. **D-M2/M-3's specificity cell "(0,4,0) — both compounds carry the scope id" — KILLED, third-derived.** My compileStyle run: the attribute lands on the LAST compound only → (0,3,0). The verdict is unaffected (layer decides), but the same transform fact is what makes MISS-1 decisive — the corrected cell is load-bearing.
9. **N-1's running-cost sub-claim — KILLED, third-derived.** My compileTemplate run: the `tabs-trigger` forward is a `_withCtx` LAZY closure; with no receiving slot it is never invoked — cost is one property + one closure per render, not N vnode constructions. The dead-code MAJOR stands in full.
10. **C-12's live-break framing — RECORDED AS NOT-A-LIVE-BREAK.** All nine `defineExpose` wires resolve today (my greps of both targets). C-12 stays a correct LATENT type-safety observation folded into the KF-APP-4 riders; the live silence it fears is N-4's window, not a broken name.
11. **D-M11's "six consumers" — KILLED and re-pinned at 8** (my census; RR-1's "8 + RibbonBar" phrasing also corrected — RibbonBar.vue:135 is one OF the eight). Both omissions were primary-action surfaces (the playback CTA; the spring reseed).

## Superlatives (provenance preserved; L-18 runs both ways)

- **S-1 (all three axes + both readers; producer bytes re-read by this seat) · the Drawer adoption was correct and the producer is measurably better than the ~250L hand-roll it replaced**: keyboard-operable `role="slider"` handle with a real `:focus-visible` treatment on a 5px grip (`box-shadow: 0 0 0 3px var(--focus-ring-color)` — my styles read), 44px minimum seat, PRM-safe spring. A pointer-gesture hand-roll almost never ships keyboard operability; the deletion gained it.
- **S-2 (corpus + RR-2; re-read) · the dogfood survived the facade**: `drawer.js:7` imports keyframes' own `SpringProgress`; the sheet's paint is a pure function of that spring. The correct shape of an adopt-the-primitive migration — delete the duplicate mechanism, keep the dogfood.
- **S-3 (corpus + both readers) · `createReusableTemplate` is the right primitive used the right way**, and the failure distribution proves it: every layout-specific defect in this corpus lives in the forks; none lives in the shared body. The duplicate-`id` hazard for the two Teleport targets is impossible by construction.
- **S-4 (RR-1; token bytes re-read) · the F7 shadow clearance is tuned against the HARDER shadow**: 12px clears the `:has(:focus-visible)`-elevated `--shadow-cartoon-lg` (11px extent — my design-idioms.css:126-128 read) by exactly 1px, with correctly no `padding-top` (the stamp casts down-left). Someone measured the focus state, not the rest state.
- **S-5 (corpus + both readers) · `isPaneIdle = idle && !isPaneHovered` with the dock-hover union + 2s linger** — the pane can never rest-dim under a resting cursor or while the user works the dock that drives it. The WHEN/HOW-MUCH seam is stated and kept; the magnitude is the blocker, the mechanism is why D-B3 is a one-token fix.
- **S-6 (RR-2) · the `v-for` key `host.animation.id` is chosen against a genuinely hostile producer contract** (the facility getter rebuilds every channel object per read); it is the one choice surviving fresh-object-per-read semantics — and the instructive tension is that the ref registry two lines above keys on `name` (the folded L-4(a) defect).
- **S-7 (D pass-3 + both readers; every term re-derived by this seat) · the design system holds the touch floor AGAINST the call site**: two independent mechanisms (`--ui-scale` multiplier + `--control-floor` absolute) deliver 54px coarse targets while `h-8` pushes 32px, and `min-block-size` vs `height` cannot be lost to a cascade reshuffle. The correct reading of D-M11/D-m12: delete the fighting classes, never strengthen them.
- **S-8 (RR-1) · the D-B1 cure's value is already measured, monotonic, and cycle-free**: `--dock-band-reserve` folds a ResizeObserver fact into a token; the `-stable` sibling reads the only-ever-grows peak with its rationale written down (layout.css:90-98 — my read); `--dock-menubar-reserve` documents its own cycle-avoidance. Careful token engineering built for exactly BG-11's job, one declaration from discharging it.
- **S-9 (RR-2) · this node's keyframes.js consumption is 100% `import type`** (`.vue:161-163`; no value.js import at all) — the parser blast radius has no entry point in this component's import closure.
- **S-10 (RR-2 + corpus) · the ribbon teleport's `defer` is load-bearing and correct** (target declared after the loop; `defer` postpones resolution past the render pass), and its `v-if="active"` guard is precisely what the timeline teleport lacks (the folded C-18/D-1).
- **S-11 (corpus + RR-2) · `.ribbon-apply--active`'s comment retired an `!important` escape for a rule that wins on merit** — the only explicit cascade decision in this cluster, and it is right, three files from two rules that lose (by layer, by specificity). Keep in view during the D-B4 redesign.
- **Verified negative (RR-2, break attempt failed; also banked at the parent) · the transport axis is single-sourced**: the parent's validation `watchEffect` and this file's `controlHosts` compute the SAME `channels?.map(c => c.name) ?? Object.keys(…)` expression — the channel/group dual axis is expressed once and consumed identically at both ends; a whole desync class is closed by construction. Recorded so it is not re-raised.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs, ranked)

1. **The MISS-1 DOM check (decides the #1 repair's landing)**: inspect the portalled `.glass-drawer` in the mobile layout and report its full attribute list — expected: consumer class present, consumer `data-v-*` ABSENT (both static halves point to no) — and confirm `.controls-pane` inside the portal DOES carry the scope id (the M-3 falsifier (d); the createReusableTemplate reasoning is sound and worker-verified, one live confirmation wanted).
2. **The D-B1 inset token choice**: measure `[data-dock-tether="bottom"]`'s top edge against the sheet at both detents; candidates `--dock-menubar-reserve` (D), `--dock-band-reserve` (C), and the `-stable` peak sibling (this seat's note: its own comment argues it is the drawer-feed variant; CubeScene.vue:275 already computes `reserve − band` for a related job).
3. **C-18's rendered stacking** — rides the parent D-1's banked residue (N visible timelines in one sink on cube/spring when expanded).
4. **Whether the desktop rail's content exceeds the work-area height today** (M-3/C-3's bite; mechanism certain, magnitude layout-dependent).
5. **Monaco `editor.create` inside the N−1 `display:none` subtrees + the LCP/TBT magnitude** (M-4/C-7).
6. **D-M12's ribbon wrap counts** (the 49% single-row chrome floor is decided; glyph advances are not source-derivable).
7. **D-B4's rendered stop-under-glyph geometry** (per-stop luminances decided; worst-case sample point is a live read).
8. **The peek-band gesture inversion, felt** (the roster row's mechanism is static-certain; one touch confirm on a real device).
9. **M-1's key-churn rename scenario** (a host whose `name` changes while `animation.id` holds — neither confirmed nor killed by anyone; nothing structurally forbids a rekey).
10. **D-m7/D-E1 (the empty glass ribbon plate) — adjudication note: NEITHER re-reader confirmed the corpus's pass-3 MAJOR escalation; it stands UNRATIFIED.** The guard-absence half is source-proven per RR-2's cell (App.vue always supplies the template, so `$slots['ribbon-content']` cannot gate); the reachable (scene, selectedControl) set needs the scene machine walked or observed. Escalation neither adopted nor killed — this item is the decider.

## DISSENT — preserved, not ruled away

1. **RR-2's MAJOR on D-B1** (against this record's BLOCKER): "one repo fact, one declaration, occlusion partial at the expanded detent." Preserved; the ruling weighs surface-exclusivity (the only sub-1024 control surface) and the born-RED self-declaration.
2. **The identity-guard event (recorded as evidence about the fleet)**: BOTH re-readers filed the `--rail-width` svi-vs-cqi contradiction as a NET-NEW missed finding; it was banked eleven days earlier as the parent's D-7. Independent re-derivation is the system working; not checking the bank before claiming novelty is the same citation-hygiene failure class the ColorSpaceSelector DISSENT recorded from the other direction. The M-25 agglomeration must treat "missed" claims as identity-guard inputs, not automatic novelty.
3. **RR-1's MAJOR on D-M3** (against MINOR): "the top-of-file contract is the first thing a reader meets; a wrong headline detent poisons every downstream derivation." Preserved; the ruling follows the code-is-right precedent.
4. **Severity ordering (RR-1, implicit)**: D-B1 first and alone per the corpus sequencing — this record amends: D-B1 + MISS-1's corrected form + D-M12's recompute land as ONE edit (the pass-3 amendment ratified), and D-B3 + D-M10 as one batch at α = 0.62.
5. **Method lessons preserved verbatim in substance**: (a) D-i0 → D-m11 — a recorded NON-defect must carry the strongest falsifier available; (b) D-M7 — a falsifier STATED is not a falsifier RUN, and a falsifier run in the wrong directory is worse than none, because it stamps false confidence; (c) MISS-1 — a cure written in the corpus's own dominant failure mode (declarations that resolve to nothing) is detectable statically, and checking cures against the component's OWN failure taxonomy is cheap.

## Closing verdict

**ADJUDICATED at HEAD `8281638c` — RED.** 49 rows stand (4 BLOCKER · 15 MAJOR · 20 MINOR · 4 INFO booked + 6 folds to banked identities, one blocker-weight): the component is a correct, even praiseworthy adoption whose CONTRACT went stale in every direction the cascade can betray prose — the headline BLOCKER's discharge lever ships unread in the installed producer while two comments deny it exists; the #1 repair all three axes prescribed is scope-dead as written and is corrected here with compiler and runtime receipts; the rest-dim, the closed rail, and the Apply-CSS state each fail a WCAG floor by arithmetic this seat re-derived thrice-independently (with the corpus's own 1.22:1 headline killed at 1.33:1 and its α=0.60 cure killed below the AA floor it cites). 11 claims killed with executed refutations; 14 re-reader finds adjudicated in, four of them MAJOR a11y/gesture defects no original axis reached; identity guards held across census F-1, parent D-1/D-3/D-7/D-8·C-7/L-4(a), and kf-App KF-APP-4 with zero re-bookings — and one double-filed "novel" find caught as already banked. Routings are M-25 agglomeration input for the forming X·KF specs: KF.W1-DEP/W2-TABS/W7-TOKENS and the banked KF-APP-4 gates lane own their folds; everything behavioral is NO-WAVE-OWNER for SS-1/SS-2; ten residue items ride to SS-13, the MISS-1 DOM check first. Nothing here stamps a wave.

— end of apotheosis. The three challenge axes and both re-reader returns remain immutable beside this file per E-1/E-3.
