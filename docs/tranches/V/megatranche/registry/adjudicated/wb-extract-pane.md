# wb-extract-pane — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **HEAD**: `cae06414` · branch `tranche-u` · **Corpus**: three challenge axes against `demo/workbenches/extract/ExtractPane.vue` (37 lines) and its subtree — C-implementation (C-1..C-9 + X-1..X-5, authored at corpus HEAD `c654824e`), D-design (D-1..D-17), L-library (L-1..L-11). Provenance re-read by this seat at `registry/HYDRATION-LEDGER.md`: **C is UNWITNESSED-DIRECT, 0 payloads** (:301); D and L are EXISTS-ORIGINAL, 1 payload each (:195-196).
**Re-readers adjudicated**: design-and-usage · library-and-code (returns received merged). Both were attacked before either was adopted; every kill was re-derived against the tree, every confirm spot-audited at its sharpest line, and every reader-found miss re-walked in source by this seat.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling per the commission)**: full reads of ExtractPane.vue (37), ExtractWorkbench.vue (293), useExtractSession.ts (223), useImageQuantize.ts (161), quantize-worker.ts (44), ExtractControls.vue (151), ImageDropZone.vue (113); ImageEyedropper.vue targeted (:6-14, :230-248) + `grep -c 'role=|aria-modal|inert|tabindex'` → **0 across 298 lines**; PaneHeader.vue (:18-24, :43-58, :175-196); PaneSlot.vue (:12-24, :115-130); usePaneRouter.ts (:60-90, :135-145); App.vue (:85-112, :348-354); usePaletteWiring.ts (:34-58, :70-80); usePaletteStore.ts (:10-18, :60-95 — `createPalette` re-mints id + both timestamps); PaletteCardSkeleton.vue (:25-45 — `role="status"` + "Loading palette"); palettes/browser/card/index.ts (:1-6); e2e: **o18-contrast-census.spec.ts:1078-1140 read in full** (the C-9 decider), o9-shadow-palette.spec.ts:125-165, walk.spec.ts extract rows, `grep -rn setInputFiles e2e/` → **0**; `git log` dating o18's extract leg (`d000db30`, 2026-07-12 — **six days before the corpus HEAD**); repo censuses run by this seat: dead-API consumers of useImageQuantize (only useExtractSession, destructuring exactly `{palette, isProcessing, error, quantizeFromFile}`), `.plate-ink {` = **5 files**, `pane-scroll-fade` = **9 pane roots + the PaneHeader rule** (three drift variants, one more than the readers found), `inject(COLOR_TARGET_PORT_KEY|LIBRARY_PORT_KEY)` = **8 sites / two policies** (fleet count corrected below), `demo/ui/` = 19 dirs / 29 total lines / card barrel = root-barrel re-export / `ui/label`+`ui/switch` zero consumers / `ui/card` consumers = 12 files, `@mkbabb/value.js/{value,transform}` dogfood = **0 + 0** (`/css` 9, `/color` 23, `/quantize` 3), `grep -rn debounce src/` = **0** + utils.ts:8-20 comment read; package.json#exports = **7 keys, no root**; tsconfig.demo.json:36-52 (**3 phantom targets, `/css` AND `/value` omitted, "CLOSED 8-key set" comment false by one**); `ls dist/` = 3 chunks + subpaths (7 pairs; **no `dist/gh-pages/`**); installed glass-ui 7.0.0: DockControl.vue.d.ts (no `label`/`ariaLabel`; "Role rides the consumer"), instrument-chassis dist present, `grep InstrumentChassis demo/` = **0**; motion-quarantine.md M-32/M-33/M-34/**MQ-1** rows + adoption bar re-read; **X wave files W1/W2/W4/W5/W6/W7/W8/W9/W10 read for routing** — including **CC-108/A-17 (the InstrumentChassis is DEAD at the producer; X-W6 H1 is a no-copied-chassis gate)**, which neither reader surfaced and which re-rules D-1.

## RULING PIN (this seat's own derivation — neither reader made it)

**CC-108/A-17 forecloses the corpus's largest cure.** D-design's D-1 (BLOCKER as filed, MAJOR as the readers confirmed it) rests on "the right component is installed": glass-ui 7.0.0 ships `InstrumentChassis` with the exact prop contract `VISUAL-CONSTITUTION.md:47` names, and the whole D §5 transposition adopts it. The X formation has since **killed the component at the producer** (X-W6.md:17: "the producer's InstrumentChassis is dead… no chassis-shaped abstraction rebuilt on our side"; W6 H1 = `gate-no-chassis.mjs`, a subtraction gate whose falsifier is any NEW chassis-shaped housing; X-W5.md §3: "CC-108 makes the InstrumentChassis a fact, not a prohibition… the compositions in this wave are bespoke by construction"). Adopting the chassis today would fail the program's own gate direction. Consequences ripple through the corpus: D-1's cure is **foreclosed**, its severity was minted against a producer contract that no longer stands, and the canon row that binds Extract to `InstrumentChassis` is itself **stale** and must be corrected by the wave that owns the canon (X-W10). The measured geometry defects D-1 fathered (50/50 split, no protagonist, growing chassis) survive on their own evidence and route to the waves that own the scene. Second pin: **X-W5 scope item 5 marks the exact proportion numbers D-2 cites ("61.8033989% / 66.6666667%") as fabricated tolerance to be replaced** — D-2's direction (an empty companion never gets half the viewport) survives; its numeric law does not.

## Scoreboard

**28 adjudicated defect rows: 2 BLOCKER · 11 MAJOR · 13 MINOR · 2 INFO** — 20 corpus-claim rows confirmed (several rescoped/consolidated), 8 reader-found rows admitted after re-derivation (two of them corrected by this seat). **12 rows in the killed register**: the readers' 8 kills all UPHELD on re-derivation (three sharpened), plus 4 kills/corrections of this seat's own — two against reader CONFIRMs (D-1's cure-and-severity frame; the impure-computed consequence limbs) and two reachability/scope corrections inside reader-found misses. Blocker-weight cluster: **the session/quantize correctness family (XP-2/XP-3/XP-4/XP-23) has NO owning wave in tranche X** — the corpus's sharpest shipping lies live in two composables no wave's file bounds touch.

## Ruled disagreements and reader-challenges (the seat's actual work)

| dispute | reader position | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| D-1 (chassis) | CONFIRMED at MAJOR, "canon/producer-adoption divergence", adoption implied as the cure | **RESCOPED to MINOR — reader position killed in part (K-9)** | CC-108/A-17 (RULING PIN). The facts survive (zero demo imports — my grep; Card as page region; canon quoted verbatim-correctly by both axes) but the accusation's frame — "the right one is installed" — is dead: the producer retired the component and X-W6 H1 gates AGAINST rebuilding its shape. What remains is a **canon-staleness defect** (VISUAL-CONSTITUTION.md:47 names a killed producer component) plus the bespoke-composition obligation X-W5 already owns. |
| Impure computed (reader-found miss #4, MINOR) | "a saved palette's createdAt is the moment of the last invalidation… the value escapes via @save" | **DEMOTED to INFO — both named consequences refuted (K-10)** | usePaletteStore.ts:82-90: `createPalette(name, colors)` mints its **own** `crypto.randomUUID()` id and `const now = new Date().toISOString()` for both timestamps — the computed's clock values never reach the store. And `grep -rn createdAt demo/palettes/browser/card/` → zero renders in the card family (only AdminFlaggedPanel and VersionHistoryDrawer, both off this path). The impurity itself is real and stays on record; the `"__extracted__"` sentinel reads as intended in-place-patch identity for the single ephemeral result, not a defect. |
| Race reachability (reader-found miss #2, MAJOR) | "onReset and onFile both call runQuantize() with NO debounce, so Reset-during-processing or upload-during-processing overlaps deterministically" | **CONFIRMED at MAJOR — Reset arm KILLED, two live arms substituted (K-11)** | ExtractWorkbench.vue:70 passes `:disabled="session.isProcessing.value || cameraActive"` and ExtractControls.vue:84 gates Reset on `disabled || !hasImage` — **Reset cannot fire during processing**. The live arms, from my own reads: (a) ImageDropZone's `@drop` (:25, :94-100) has **no** disabled gate — dropping a second image mid-flight overlaps deterministically; (b) the **decode window**: `isProcessing` flips true only INSIDE `runQuantize` (useImageQuantize.ts:87), i.e. *after* `await createImageBitmap` — during a slow decode every control is still enabled and a debounced slider quantize can overlap the decode-triggered one. Mechanism as filed is exact: one `pendingResolve/pendingReject` slot (:51-52), overwritten unguarded (:89-91); first response resolves the SECOND caller's promise with the FIRST request's palette, flips `isProcessing` false mid-flight (skeleton vanishes early), strands the first promise forever. |
| Worker-poisoning scope (reader-found miss #3, MAJOR) | "after any worker-level failure (a `?worker` chunk that fails to load, **an uncaught throw inside quantize-worker.ts**, a Safari worker eviction) … posts into a dead worker" | **CONFIRMED at MAJOR — the in-worker-throw example KILLED (K-12)** | quantize-worker.ts:20-44 (read whole): the entire `onmessage` body is wrapped in `try/catch` that `postMessage({type:"error", error:String(err)})` — an uncaught throw inside the handler **cannot** reach `worker.onerror`; it returns through `onmessage`'s error branch, which the composable handles cleanly (:61-63). The poisoned-cache class narrows to genuine worker death: the `?worker` chunk failing to fetch/parse, or environment kill. For that class the finding is exact and severe: `getWorker()` (:54-56) returns the corpse forever, `onerror` (:69-75) never nulls `worker`, the only nulling site is the KeepAlive-dead `onBeforeUnmount` (:148-151), and the next `runQuantize` sets `isProcessing=true` into a void — **permanent skeleton, no error line, no deadline** (contrast usePaletteWiring.ts:34-58's bounded retry, verified). |
| C-9 census kill | killed the axis's "no e2e sets space= on /#/extract" + "o9 is the ENTIRE test surface" | **UPHELD, twice-sharpened** | o18-contrast-census.spec.ts:1088-1090 does `page.goto("/#/extract?space=lab&color=…")` and born-RED-gates two of THIS pane's surfaces (`[data-o18="extract-kc"] .slider-track` :1106/:1126, `[data-o18="extract-k-rail"]` :1133) — and `git log` dates that leg `d000db30` **2026-07-12, six days before the corpus HEAD** `c654824e` (2026-07-18): the pasted "no output" was false at the axis's own HEAD, not merely stale. Sharpening two: `e2e/smoke/walk.spec.ts:74-76,:102` now also visits Extract and asserts its heading — a second spec the census would have missed. The **core survives untouched**: `grep -rn setInputFiles e2e/` → 0; no gate has ever supplied this route a file; the `@pick`/`@add-color` seam is unasserted anywhere. |
| C-3 kill | killed "zero live regions in EVERY state including the 2.5s re-quantize" | **UPHELD** | PaletteCardSkeleton.vue:25-45 (my read): `role="status"` + `aria-label="Loading palette"`, with the comment stating the design rule ("loading ≠ empty splits on ANNOUNCEMENT… HERE work IS happening"); ExtractWorkbench.vue:103-107 renders it whenever `isProcessing`. The probe's `liveRegions: 0` for the in-flight arm can only have sampled after settle. The **narrower true claim** survives as XP-6: both terminal states are silent — the error line (:80-85) is a plain div, the developed arm (:108-157) carries nothing — which is the exact inversion of C-3's own cure ("terminal state only — never the in-flight skeleton"). |
| D-15 citation kill | killed "ExtractWorkbench.vue:36 — inject(CSS_COLOR_KEY, undefined)" | **UPHELD** | My read: :36 is `v-if="cameraActive"` in the camera viewfinder template; the defaulted inject is at :218. The C axis cites it correctly. Underlying asymmetry real; provenance wrong. |
| D-7 consequence kill | killed "Extract consequently LOSES the gap-4 its siblings have… rhythm therefore different" | **UPHELD** | GradientPane.vue:23-25 wraps only `<GradientVisualizer/>`; GeneratePane.vue:34-36 wraps only `<GenerateControls/>` (my reads) — a flex `gap-4` with one child contributes zero. Header→body spacing on all three panes is the same `pt-2`. The fallthrough-styling limb survives as XP-16. |
| L-1 limb kills (cost / cure / contract-count) | three limbs killed | **ALL UPHELD — cure kill sharpened, count corrected** | Cost: dev-server module lists and a static walker are not production facts; `dist/gh-pages/` is absent from the tree (my `ls`); App.vue:351→usePaletteWiring.ts:60 puts the facade in the eager graph regardless (verified). Cure: the keys.ts leaf cannot deliver the "NEVER palettes/…" lattice — three by-value edges verified (ExtractWorkbench.vue:196-200 pulls 3 members of `palettes/browser/card`; useExtractSession.ts:17-18 pulls `usePaletteStore` + `palettes/types`); **sharper**: X-W7's `PaletteSpecimen` props-only split (W7.md:206) is the program's own, deeper answer to the card-cluster edge — the reader's kill is the wave's premise. Count: two policies over three `COLOR_TARGET_PORT_KEY` sites, and the fleet split runs through `LIBRARY_PORT_KEY` too — my grep corrects the readers' cell: asserted at MixPane.vue:16, GeneratePane.vue:11, **PalettesPane.vue:164** (missed); bare at GradientVisualizer.vue:30, MixSourceSelector.vue:33. |
| D PRM negative-proof kill | killed via MQ-1 | **UPHELD — carryable, and already wave-owned** | motion-quarantine.md:131-161 (my read): MQ-1 is a **quarantine-authored NEW defect (HIGH)**, not an M-xx FALSE row — carryable under the motion law. M-34 SURVIVES (:125, :267). Blast radius names the nine `.pane-scroll-fade` panes; ExtractPane.vue:5 is one; PaneHeader.vue:177-194's `animation-timeline: --pane-scroll` animations ignore both guards' `animation-duration` clamp by construction (my read of the @supports block). **X-W10.md:202 already owns the cure** (wrap :178-193 in `@media (prefers-reduced-motion: no-preference)`). M-32 and M-33 (FALSE rows) are carried by no row of this record; C-8 CITES M-33's measurement in its own direction (no fade exists), which is compliance, not carriage. |

## Adjudicated defect roster

Line anchors are HEAD `cae06414` unless marked corpus-HEAD; re-resolve before executing any cure (the D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: a wave id means that wave's file bounds/scope own the surface today; **NO-WAVE-OWNER** rows are booked for the next formation boundary.

### BLOCKER

- **XP-1 — BLOCKER — C-1 (confirmed, reader-sharpened): the KeepAlive-cached routed unit declares no deactivation contract; every teardown in the subtree is written against unmount semantics and never runs.** Statically total: usePaneRouter.ts:73 `defineAsyncComponent(ExtractPane)` / :85-region `componentFor("extract")`; PaneSlot.vue:120-126 `<KeepAlive :max="max">` around `<component :is>`; ExtractPane.vue has zero lifecycle hooks (whole-file read). The four orphaned teardowns: ExtractWorkbench.vue:281 `onBeforeUnmount(stopCamera)` (:257-263 is the ONLY `track.stop()` path for the viewfinder stream), useImageQuantize.ts:148-151 (`worker?.terminate()`), useExtractSession.ts:194-196 (debounce timer), ImageEyedropper.vue:242-245 (window keydown + `sampler.dispose()`). Readers' two sharpenings verified: App.vue:97-100 comment + :110 `:max="6"` names exactly the six non-admin LEFT panes including extract — **the cache is sized never to evict this pane**, so the leak is session-permanent, not LRU-bounded (mobile slot `:max="9"`, :88); and a pane parked with the eyedropper open leaves its `window`-level keydown handler (ImageEyedropper.vue:237-240) consuming keys on every other route. The camera-stays-live transcript is UNWITNESSED (residue); the mechanism needs no probe. → **X-W5** (owner of App.vue/PaneSlot.vue/usePaneRouter.ts, the route-scene contract, and the KeepAlive-cache identity gate C5) — **ADDENDUM CANDIDATE**: no W5 scope item names deactivation teardown; propose the E-3 addendum with a born-RED gate (navigate away from an active camera/quantize → assert zero live MediaStreamTracks and a terminated or idle worker).
- **XP-2 — BLOCKER — C-2 (confirmed): an undecodable image leaves the previous image's palette on screen with no error and an unhandled rejection.** Every link statically verified: useImageQuantize.ts:105-108 awaits `imageFileToPixels` (createImageBitmap, :19 — the rejection site) BEFORE :86-87 arms `error=null; isProcessing=true` inside the inner `runQuantize`; useExtractSession.ts:153-157 calls `quantizeFromFile` un-awaited, un-caught; :164-168 paints `previewDataUrl` before any decode and never clears `palette` (shallowRef, useImageQuantize.ts:46); ExtractWorkbench.vue:108-109 then renders the `extracted` arm with the stale array while the dedicated error line (:80-85) stays dark because `quantizeError` (useExtractSession.ts:66-73) reads a `workerError` that was never set. ImageDropZone.vue:30 `accept="image/*"` and :97 `type.startsWith("image/")` both admit a corrupt `image/png`. The corrupt-PNG browser transcript is UNWITNESSED; the lie is provable from source alone. → **NO-WAVE-OWNER** — the session/quantize composables sit in no X wave's file bounds (X-W7 carves ExtractWorkbench.vue only). Booked as the head of the **extract-session correctness cluster** (with XP-3/XP-4/XP-23/XP-27) for the next formation boundary; the born-RED witness is XP-7's flow spec, arm (b).

### MAJOR

- **XP-3 — MAJOR — reader-found (miss #2), reachability corrected by this seat (K-11): single-slot pending-resolver race.** One `pendingResolve/pendingReject` pair per composable (useImageQuantize.ts:51-52), overwritten with no guard, id, or cancellation (:89-91); `onmessage` (:57-68) resolves the CURRENT resolver with WHICHEVER response arrives first, flips `isProcessing` false while a request is still in flight (skeleton disappears early, ExtractWorkbench.vue:103-107), paints the earlier request's palette under the later k until the second response lands, and strands the first caller's promise forever. Live overlap arms (this seat's): drop-a-second-image mid-flight (ImageDropZone drop path has no disabled gate) and the decode window (`isProcessing` arms only after `createImageBitmap` resolves, so controls stay enabled through a slow decode). Reset arm dead (ExtractControls.vue:84 disabled while processing). → **NO-WAVE-OWNER** (session cluster).
- **XP-4 — MAJOR — reader-found (miss #3), example corrected by this seat (K-12): a dead worker is never evicted; one worker death poisons every subsequent extraction into a permanent skeleton.** `getWorker()` caches unconditionally (:54-56); `onerror` (:69-75) records the error and never nulls `worker`; the only eviction is the never-firing `onBeforeUnmount` (:148-151, XP-1). Poison class = genuine worker death (`?worker` chunk fetch/parse failure, environment kill) — NOT in-handler throws, which quantize-worker.ts:20-44's own try/catch returns as `{type:"error"}` through the healthy path (this seat's read). After a death: next `runQuantize` sets `isProcessing=true` (:87) and posts into the corpse (:94-97); no response ever arrives; the skeleton spins forever with no error line and no deadline. → **NO-WAVE-OWNER** (session cluster).
- **XP-5 — MAJOR — C-4 h1 limb / D-9 (confirmed): the route's identity is an `<h3>` and the document has no `<h1>`.** ExtractPane.vue:7 → PaneHeader.vue:21 `<h3 class="pane-header-title font-display">` hardcoded for all nine panes; display-sized type at h3 rank; canon quotes verified verbatim (VISUAL-CONSTITUTION.md:85; PROPORTION-AUDIT.md:76); `main: 1` half sound (shell-owned). → **X-W5** — explicitly owned: scope item 1 "one visible route H1 inside `<main>`" + the fourteen-route H1 walk carve (W5.md:114-115).
- **XP-6 — MAJOR — C-3 as-rescoped by the readers' kill (K-2): the TERMINAL states announce nothing — and the shipped code announces exactly the state the axis's own cure forbids.** In-flight: `role="status"` skeleton (PaletteCardSkeleton.vue, verified). Terminal: the error line (ExtractWorkbench.vue:80-85) is a plain div; the developed result (:108-157) carries no region. An AT user hears "Loading palette" and then silence — success, failure, and "no colours found" are all indistinguishable from nothing. → **X-W5** (scope item 1 ships the shell-owned polite `role="status"` region — the substrate); the extract-terminal feed into it is an **addendum candidate** on the same wave.
- **XP-7 — MAJOR — C-9 core (confirmed as-rescoped; census limb killed, K-1): no gate has ever handed this route a file; the emit seam — 2 of the pane's 5 meaningful lines — is asserted by nothing.** `setInputFiles` = 0 across e2e/ (my grep); test/ touches only useImageSampler's pixel maths; o9 asserts the empty state; o18 asserts two control-contrast surfaces under `?space=lab`; walk.spec asserts the heading. Deleting `@pick`/`@add-color` (ExtractPane.vue:15-16) keeps every gate green. → **X-W1** (the re-gate program, CC-031's remit) — **addendum candidate**: one born-RED flow spec (`setInputFiles` a fixture PNG → palette develops; corrupt fixture → visible error + cleared palette [witnesses XP-2]; `?space=hex` sample → `#rrggbb`; navigate away → no live tracks, worker settled [witnesses XP-1]).
- **XP-8 — MAJOR — L-3/X-2 (confirmed): the producer owns no accessible-name prop, the demo runs two conventions, and this pane's three controls sit on the weaker one — plus the 12px slider thumbs.** DockControl.vue.d.ts (my read): props `{shape, compact, active, type, disabled, as, asChild, class}`, no `label`/`ariaLabel`, doc "Role rides the consumer". Workbench-side title-only DockControls = **11** (my count — ExtractControls.vue:41/:50/:85, ExtractWorkbench.vue:52, ImageEyedropper.vue ×3, GradientVisualizer ×1, MixResultDisplay ×3); shell-side uses `aria-label` (verified by grep direction; exact count of 8 not enumerated — residue). `title` is a last-resort accname source: weakly named, invisible to keyboard and touch. Thumb width (12×24 vs the 24px floor) is probe-measured — residue. → **X-W4** — the wave's own accname law (W4.md:358: `aria-label > … > title`, placeholder is NOT a name) and its DockControl remit own this; the `label` prop ask rides the standing glass-ui BH relay (owner fond).
- **XP-9 — MAJOR — X-1 (confirmed statically): the eyedropper overlay is not modal — no dialog role, no aria-modal, no inert, no focus management.** `grep -c 'role=|aria-modal|inert|tabindex'` over ImageEyedropper.vue = **0 in 298 lines** (this seat's run); the overlay is a plain `absolute inset-0 z-popover` div (:8); onMounted adds a window keydown and nothing manages focus (:237-240). The six-stops-behind-the-overlay Tab sequence is probe-only (residue); the absence of every modality primitive is source truth. → **X-W4** — ImageEyedropper.vue is in W4's file bounds, but marked "modify-carve — import + face only" (W4.md:166): the modality contract exceeds the declared carve — **addendum candidate** at the formation boundary.
- **XP-10 — MAJOR — X-3 (confirmed): one pane, two readouts, one space honoured.** Honoured path: ExtractPane.vue:14 → ExtractWorkbench.vue:176 → eyedropper/useImageSampler. Ignored path: useExtractSession.ts:54 `serializeCssColor(source.color)` — native oklch, no space argument, no precision budget; ExtractWorkbench.vue:137-141 renders it; :85-89 bakes the same strings into every saved swatch's `css`. `colorSpace` appears nowhere in useExtractSession.ts (my read). → **X-W7** (ExtractWorkbench.vue is in W7's modify-carve bounds and the dominance/PaletteCard surface is exactly what the specimen split re-carves); the `serializeCssColor` precision contract is library-side and rides **X-W9** awareness.
- **XP-11 — MAJOR — L-4 (confirmed): the 19-directory `demo/ui/*` shim; both spellings live inside this one subtree.** My census: 19 dirs, 18 one-line barrels + alert at 11 (29 lines total); card barrel re-exports the glass-ui ROOT barrel while 7.0.0 publishes `./card`; `ui/card` consumers 12 files vs 36 bare-glass-ui files; `ui/label` + `ui/switch` zero consumers; ExtractPane.vue:24 takes the shim while ExtractWorkbench.vue:187-188 takes `/dock` + `/dom`, and ExtractControls.vue:98-99 takes **both spellings on adjacent lines**. → **X-W8** — explicitly owned (W8.md:40: delete the 19 barrels, re-point 92 statements across 48 files; G-11/G-12 family).
- **XP-12 — MAJOR — reader-found (miss #7) / MQ-1 landing (K-8): this pane's header keeps its full scroll-driven spatial motion under reduced-motion.** MQ-1 (HIGH, quarantine-authored, carryable): both global guards clamp only `animation-duration`, which timeline-driven animations ignore; `pane-title-shrink` runs byte-identically under `reduce` (measured in the quarantine; mechanism confirmed from PaneHeader.vue:177-194 by this seat). ExtractPane.vue:5 is one of the nine hosts, and this corpus's D axis asserted the opposite as a NEGATIVE PROOF. → **X-W10** — explicitly owned (W10.md:202 wraps PaneHeader.vue:178-193 in `@media (prefers-reduced-motion: no-preference)`).
- **XP-13 — MAJOR — reader-found (miss #1, verified exactly): ~40 lines of dead public API in useImageQuantize, including a SECOND camera-capture implementation.** `quantizeFromCanvas` (:110-113), `quantizeFromCamera` (:115-146), `ImageQuantizeOptions.workerFactory` (:35-41) have zero consumers (my grep: the sole importer destructures `{palette, isProcessing, error, quantizeFromFile}`). `quantizeFromCamera` opens its own `getUserMedia` with the byte-identical constraints object of ExtractWorkbench.vue:242-248 — a second, unreachable camera-permission code path in the subtree whose own header comment (useExtractSession.ts:2-5) advertises the T20 collapse. `workerFactory`'s "tests… can swap a factory" docstring names tests that do not exist. → **X-W8 G-10 extension** (dead public surface is zero — census-extension addendum, the shell-dock precedent).

### MINOR

- **XP-14 — MINOR — C-5/L-2/D-14 (confirmed, consolidated): `DisplayColorSpace` re-declared four times over an exported canonical type; the prop contract erased at the router.** All four sites verified at HEAD (ExtractPane.vue:30, ExtractWorkbench.vue:202, useImageSampler.ts:21 exported, canonical demo/color-session/color-model.ts:29 with picker-color.ts:37 `PickerSpace = SpaceId` making them structurally identical); the seam: usePaneRouter.ts:64 `props: Record<string, unknown>` / :139 sole producer / PaneSlot.vue v-bind. Double default (ExtractPane.vue:32 + ExtractWorkbench.vue:204, both `"hex"`). The vue-tsc single-diagnostic repro is UNWITNESSED (residue); the seam's shape is source truth. → **X-W5** — scope item 1 makes `bindPane` "required and generic" on all three PaneSlot sites; the typed seam is that work, and the local type copies die when the canonical import lands with it.
- **XP-15 — MINOR — C-7/D-13 (confirmed): the root wrapper div is a no-op box** — `mx-auto` dead against `w-full`, `relative` shadowed by ExtractWorkbench's own relative root (the actual containing block for the eyedropper's `absolute inset-0`, verified from both templates), three classes byte-repeated on the Card; copied verbatim in GradientPane.vue:19 and GeneratePane.vue:30 (my reads). → **X-W5** (rider — dies in the one-mount/one-column re-carve).
- **XP-16 — MINOR — D-7 fallthrough limb (confirmed; consequence limb killed, K-4): the pane styles its child's root from outside.** ExtractPane.vue:12's `class="pb-4 px-4 sm:px-6 pt-2"` fuses onto ExtractWorkbench.vue:2 via single-root fallthrough; the siblings own the same padding in their own wrapper — so ExtractWorkbench has no intrinsic padding contract. → **X-W5** (rider on the scene re-carve; owner-edict-5 note travels with it).
- **XP-17 — MINOR — L-5/D-11 (confirmed, sharpened by this seat): `.pane-scroll-fade` is a stringly nine-site contract with THREE drift variants, not two.** Rule unscoped in PaneHeader.vue:54-58 with its own colocation apology (:43-49); nine roots verified; drift: Browse/Palettes/Admin/About add `mx-auto` on the Card, Mix puts `relative` on the Card, and **ConfigSliderPane.vue:106 applies the class to an inner `div` (not the Card root) with an added `scrollbar-thin`** — a third variant the readers did not count. demo/DESIGN.md's deliberate-colocation record is the fair counter and does not cure the drift. → **X-W10** (W10.md:73 moves the scroll-timeline declarations; PaneHeader.vue is in its bounds).
- **XP-18 — MINOR — C-8 (confirmed): the class name promises a fade its rule does not deliver, over content that does overflow.** PaneHeader.vue:55-57 is `contain` + `scroll-timeline`, nothing painterly — corroborated by pinned M-33 (mask-image none, measured; a FALSE row cited in its own direction, not carried). The 142px overflow figure is UNWITNESSED (residue). → **X-W10** (PaneHeader owner; rename-or-deliver decision).
- **XP-19 — MINOR — L-6 (confirmed): `.plate-ink` — a token by its own comments — declared five times identically.** Five files verified by grep (three in this subtree; EmptyState; ErrorBoundary), each `color: var(--ink-muted, var(--muted-foreground))`, each with the "certified de-emphasis rung" comment. → **X-W10** (unit .f owns foundation.css; one `@utility` home, producer-side option rides the BH relay).
- **XP-20 — MINOR — L-7/D-5 (confirmed): the `layout="split"` branch is dead — one caller, redundantly passing the default — and the session prose still claims two shells.** Sole consumer verified; four unreachable template sites + the prop union; useExtractSession.ts:4-5 "both shells now consume" vs one shell. → **X-W7** (ExtractWorkbench.vue modify-carve — the branch dies in the carve; the D-axis "split is the composition the canon asks for" garnish falls with CC-108/the D-1 rescope).
- **XP-21 — MINOR — L-8 (confirmed with the readers' corrections, re-verified): tsconfig.demo.json's map of the published surface is false.** My reads: exports = 7 keys, no root; paths block declares **three** phantom targets (`@mkbabb/value.js` → dist/index.d.ts, `/parsing`, `/units` — none exists; dist = 3 chunks + exactly 7 subpath pairs) and **omits two live keys** (`/css` — 9 demo files, two in this subtree — AND `/value`); the header comment asserts "a CLOSED 8-key set". Inert today (vue-tsc green via self-reference under bundler resolution). → **NO-WAVE-OWNER in X** — X-W8 explicitly declines it (W8.md:112: "tsconfig.demo.json path/export parity — L-22 ≡ W-HYGIENE, another owner") and no X wave file claims it; booked for the formation boundary with the generator-parity cure (paths generated from exports, as vite.config.ts already does).
- **XP-22 — MINOR — L-9 (confirmed): the debounce fork's rationale cites a root-barrel export that does not exist and a symbol gone from src/.** `grep debounce src/` = 0; no `"."` export/main/types (my reads); utils.ts:8-20 comment quotes verified; the file is on this pane's dependency path. → demo half **X-W8** (explicitly: W8.md:163 "demo/shared/utils.ts; re-home `debounce`"); the root-entry decision itself is **X-W9**'s (the 4.1 published-surface cut).
- **XP-23 — MINOR — reader-found (miss #6, verified): `readAsDataUrl` is a second unhandled-rejection site, before the decode.** :29-36 rejects with a ProgressEvent; :164-168 awaits with no catch; the bound handler chain (ExtractWorkbench.vue:24 → :234-237) is un-awaited — a FileReader failure is an unhandled rejection that leaves `lastFile` pointing at a file that never rendered (so the next slider move re-quantizes the failed file). Outside C-2's cure as written. → **NO-WAVE-OWNER** (session cluster).
- **XP-24 — MINOR — reader-found (miss #5, verified; gating rationale noted): the Reset control resets neither the image nor the palette.** onReset (:180-184) restores k=5/kC=0.5 and re-quantizes; never clears `lastFile`/`previewDataUrl`/`paletteName`/`palette`. The control (ExtractControls.vue:83-90, `title="Reset"`, RotateCcw) is gated `disabled || !hasImage` — functionally because re-quantize needs a file, but the surface reading stands: a slider-defaults button wearing a destructive name, and one of the three weakly-named controls of XP-8. → **NO-WAVE-OWNER** (nearest owners: X-W4's register sweep / X-W7's carve — neither's bounds hold ExtractControls' semantics; boundary booking).
- **XP-25 — MINOR — D-1 as-rescoped by this seat (K-9): the canon binds Extract's housing to a component the program has killed; the housing question must be re-derived bespoke.** Surviving facts: `<Card tier="resting">` as the page region (ExtractPane.vue:3-5) against VISUAL-CONSTITUTION.md:38/PROPORTION-AUDIT.md:66 (quotes verified); `InstrumentChassis` demo imports = 0. Dead frame: the adoption cure and the "installed contract" severity (CC-108/A-17; X-W6 H1 gates against chassis shapes). → **X-W10** (canon correction: VISUAL-CONSTITUTION.md:47's `InstrumentChassis` cell is stale against A-17) + **X-W5** (the bespoke scene composition, where D-2/D-4's geometry work already routes).
- **XP-26 — MINOR — D-3 disposition limb + D-12 (confirmed at the token chain; measurements residue): the pane inherits a REMOVE-disposed, direction-blind caster shadow purely by choosing Card.** foundation.css `--shadow-cartoon: 8px 8px 0 0 …` → `--shadow-card` project-wide override (readers verified; consistent with this seat's canon reads); PR-05 REMOVE and the no-drop-shadow veil rule quoted correctly; `8px 8px` is physical, so RTL mirrors the layout but not the shadow. Equal-rect/equal-shadow and RTL pixel tables are probe-only (residue). → **X-W10** (canonical design owns the shadow species and foundation.css).

### INFO

- **XP-27 — INFO (demoted from the readers' MINOR by this seat, K-10): `extractedPalette` is an impure computed — its getter mints wall-clock timestamps and a constant sentinel id.** useExtractSession.ts:75-99 verified. Both reader-named consequences refuted: `createPalette` re-mints id + timestamps (usePaletteStore.ts:82-90), and nothing in the card family renders `createdAt` (my grep). Residual substance: getter purity (invalidation-timing-dependent values) as hygiene; the `"__extracted__"` sentinel reads as intended stable identity for the single in-place-patched result. → **NO-WAVE-OWNER** (rider on the session cluster).
- **XP-28 — INFO — L-10 (confirmed, re-counted exactly): two of seven published subpaths — including `./value`, the namesake — have zero dogfood.** My counts: /color 23 · /css 9 · /quantize 3 · **/value 0 · /transform 0** across demo/test/e2e. → **X-W9** (the library apotheosis owns published-surface truth; W9's `./transform` totality work is the adjacent seat).

**Not carried (observation, not defect)**: C-6/D-15 — `inject(COLOR_TARGET_PORT_KEY)!` with no default has **no reachable failure path** (provider at the composition root on every in-app path; both axes concede; both self-label hypothesis). Under the ColorSpaceSelector precedent (L-5(b): the hard-assert as house contract) this is a two-policy house pattern, not a defect — fleet census corrected by this seat: asserted ×5 (ExtractPane:36, PalettesPane:164/:165, MixPane:16, GeneratePane:11), bare ×3 (ColorPicker:198, GradientVisualizer:30, MixSourceSelector:33). If a consumer ever mounts outside App.vue's provider, the row revives with a crash witness. Also not carried: D-16's "a state never captured has never been designed" as a defect of THIS component — it is a matrix gap, routed to residue → X-W1 goldens.

## Killed-claims register

- **K-1 — C-9 census limb ("no e2e sets space= on /#/extract"; "o9 is the ENTIRE test surface") — KILLED (readers' kill upheld, twice-sharpened by this seat).** o18-contrast-census.spec.ts:1088-1133 sets `space=lab` on the route and born-RED-gates two of this pane's own surfaces; that leg predates the corpus HEAD by six days (`d000db30` 2026-07-12); walk.spec.ts:74-76 is a second extract-visiting spec. The narrower truth carries at XP-7.
- **K-2 — C-3 as filed ("zero live regions in EVERY state including the re-quantize") — KILLED (upheld).** PaletteCardSkeleton.vue carries `role="status"` + "Loading palette" and renders whenever `isProcessing` (both files read by this seat). The shipped code announces exactly the state C-3's cure forbids and is silent on the terminal ones — carried at XP-6.
- **K-3 — D-15's citation ("ExtractWorkbench.vue:36 — the defaulted inject") — KILLED (upheld).** :36 is `v-if="cameraActive"`; the inject is :218. Asymmetry real, provenance wrong.
- **K-4 — D-7 consequence limb (the lost `gap-4` rhythm delta) — KILLED (upheld).** Both sibling wrappers hold exactly one flow child; `gap-4` contributes zero; all three panes' header→body gap is the same `pt-2`. Fallthrough limb carried at XP-16.
- **K-5 — L-1 cost limb (22 modules / 85KB runtime, 41 / 161KB compile as "this component's weight") — KILLED (upheld).** Dev-graph figures; no production bundle exists to measure (`dist/gh-pages/` absent at HEAD); the facade is eagerly booted regardless (App.vue:351 → usePaletteWiring.ts:60, verified). The COUPLING survives as a design fact inside the eager-boot story (X-W2's family), not as a byte cost.
- **K-6 — L-1 cure limb (a keys.ts leaf makes the "NEVER palettes/…" lattice reachable) — KILLED (upheld, sharpened).** Three by-value edges the cure never counts (card cluster ×3 members, usePaletteStore, palettes/types — all verified); X-W7's props-only `PaletteSpecimen` split is the program's own answer to the largest of them. The Symbol-placement idea survives only as a rider inside that larger move.
- **K-7 — L-1 contract-count limb ("one key, three contracts") — KILLED (upheld, count corrected).** Two policies over three `COLOR_TARGET_PORT_KEY` sites; the same two-policy split runs fleet-wide through `LIBRARY_PORT_KEY` (asserted ×3 — including PalettesPane.vue:164, which the readers' cell missed — bare ×2).
- **K-8 — D's PRM negative proof ("the scroll-driven arm… and the PRM path paint the same designed rest") — KILLED (upheld).** Refuted by pinned MQ-1 (HIGH, quarantine-authored NEW defect — carryable; not an M-xx FALSE row); the mechanism (timeline-driven animations ignore duration clamps) confirmed from source by this seat. Carried at XP-12, already X-W10-owned.
- **K-9 — this seat's kill of the readers' D-1 frame (CONFIRMED-MAJOR, adoption-shaped): the prescribed component is dead at the producer.** CC-108/A-17; X-W6 H1 is a no-copied-chassis gate; X-W5 declares its compositions bespoke by construction. Neither reader read the X wave files. Facts rescoped to XP-25 (MINOR, canon-staleness + bespoke obligation).
- **K-10 — this seat's kill of the impure-computed consequence limbs (reader miss #4): the store re-mints id and both timestamps; nothing renders `createdAt` in the card family.** Demoted to XP-27 (INFO).
- **K-11 — this seat's kill of the race's Reset-reachability arm (reader miss #2): Reset is disabled while processing** (`disabled = isProcessing || cameraActive` → `:disabled="disabled || !hasImage"`). Two live arms substituted (drop-mid-flight; the decode window). Row stands at MAJOR (XP-3).
- **K-12 — this seat's kill of the worker-poisoning "any worker error" example (reader miss #3): in-worker throws are caught by the worker's own try/catch and return as `{type:"error"}` through the healthy onmessage path.** The class narrows to genuine worker death; the row stands at MAJOR (XP-4).

**Motion-law compliance**: no motion-quarantine FALSE row is carried. M-32 touches a different component; M-33 is cited only in its own direction (the fade does not exist — XP-18's premise, not its contradiction); MQ-1 and M-34 are the carryable survivors and both ride X-W10.

## Superlatives — provenance verified by this seat

1. **A barrel that documents why it is NOT a star barrel**: demo/palettes/browser/card/index.ts:1-3 — "PI-6: never a star re-export — SFC scoped `<style>` is a side-effecting import; named re-exports let the bundler tree-shake unused members per consumer." Written at the exact site a future "simplification" would strike; ExtractWorkbench.vue:196-200 is a direct beneficiary (3 of 6 members).
2. **The serialization boundary is a dogfooded discriminated Result**: useExtractSession.ts:25-27 + :51-64 — `PalettePresentation = {ok:true; value} | {ok:false; error}`, `serialized.ok` actually checked, the failure folded into the visible error register (:66-73). The library's own `serializeCssColor` Result contract consumed correctly — the T.W1 keystone doing its job, rare in the corpus.
3. **A documented refusal of the library's own API**: useExtractSession.ts:7-10 — "dominant = max-population with a chroma tiebreak, derived from the RETURNED palette — never a second worker call; the library's `dominantColor()` re-quantizes and is the wrong tool." The tiebreak (:130-137) also handles the `"none"` channel sentinel explicitly. Choosing not to call your own library, and recording why, is a higher-order correctness act than any axis credited.
4. **A pre-refuted future regression**: PaneSlot.vue:12-24 — the full account of why the Transition is NOT `mode="out-in"` (the Vue 3.5 dev-only stranded-slot failure, the tranche reference, the DELTA path). Anyone who "cleans this up" re-reads their own refutation at the site.
5. **The module-level lazy singleton that names its own cured defect**: usePaletteStore.ts:10-18 — names the former per-call `useStorage` re-binding, its three call sites (useExtractSession among them), and the two reasons for laziness (SSR; Safari private browsing).
6. **Bounded liveness done right, one directory over**: usePaletteWiring.ts:34-58 — the unbounded 50ms poll replaced by a 40-attempt/~2s deadline with one warn. The exact discipline XP-4 shows the worker path lacks — the repo demonstrably knows how.
7. **Module hygiene across the subtree**: no file over 298 lines; every type-only import spelled `import type` (verified in every file this seat read); zero `@src` reaches; every `@mkbabb/value.js` specifier a real published subpath (/color, /css, /quantize — verified against exports and dist). An external consumer could write every one of these lines; L-8's drift is paperwork, not a break.
8. **The quantize worker's total error boundary**: quantize-worker.ts:20-44 — try/catch around the whole handler, a typed `{type:"error"}` response for both the Result-fail and the throw path. It is the reason K-12 exists: the worker is *better* defended than its consumer.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **The whole C transcript** (UNWITNESSED-DIRECT, 0 payloads — ledger:301; probes not in the tree): the camera-live-on-`#/mix`/`#/gradient` numbers, `{made:1, term:0}` worker counts, the corrupt-PNG byte-identical paneText, `client:772/scroll:914` (142px hidden), the vue-tsc single-diagnostic seam repro, the six-stop Tab sequence behind the eyedropper. **Every underlying mechanism is confirmed from source in this record**; only the numbers await a live witness — the XP-7 flow spec is designed to mint most of them.
2. **The D pixel corpus** — D-2 (50.00/50.00 with 199px gutters), D-3's equal-rect/equal-shadow table, D-4 (docScrollable false; 181px behind the inner scroller at 390×664 k=16 — note X-W5's block law is the routed cure), D-6 (578→706→774px growth under a slider), D-8 (plate C 0.0494 vs ambient C 0.1946 in dark), D-12's RTL table, D-17 (4.21:1 ghost caption — **moot if X-W7 lands: W7.md:65 deletes the caption**). Probe scripts `evidence/probe-geometry.mjs`/`probe-states.mjs` are **tracked**; the two PNG witnesses are **on disk but untracked** (this seat's `git ls-files` — correcting the readers' "committed probes" cell: scripts yes, PNGs no; a witness not committed does not exist).
3. **C-4's non-h1 limbs**: two unnamed scroll regions (structure confirmed — no role/aria-label on the Card, PaneHeader emits no id) and the Safari-vs-Chromium scroll-host focusability difference — live-only.
4. **X-5** (add-color has no mobile feedback): producer side verified (usePaletteWiring.ts:75-78 switches only when `right !== "palettes"`; extract's schema sets right=palettes) — whether the user sees nothing on a phone needs the live affordance; settles with the XP-7 flow spec.
5. **L-11** (gh-pages emits no app chunk): `dist/gh-pages/` absent at HEAD — unverifiable, not refuted; a §F carry against a gone artifact. Note X-W2.md:252 expects executable byte gates against `dist/gh-pages` at its own bench — the rebuild happens there.
6. **L-3's shell-side count** ("aria-label ×8"): direction and workbench side (11, exact) verified; the 8 not enumerated. Nothing depends on it.
7. **D-16**: `/extract` absent from all five special-state capture matrices (forced-colors genuinely unobserved; 200% must be real browser zoom) — input to X-W1's golden authoring, not a defect row here.
8. **Coverage note**: none of the 28 rows would be caught by today's suite except the two o18 contrast legs — the census that makes XP-7 the highest-leverage single artifact in this record.

## DISSENT / method notes — preserved, not ruled away

1. **The readers' round was strong**: all eight of their kills survived independent re-derivation — three came back sharper (K-1's six-day dating; K-6's convergence with X-W7; K-7's fleet correction) — and five of their eight misses survived attack, two with corrections. Their systematic weakness was the same as the corpus's: **nobody read the tranche-X wave files**, so a dead producer component was confirmed as a live cure (K-9) and two "deterministic" reachability claims were never checked against the disabled-state wiring (K-11) or the worker's own error boundary (K-12). The standing law extends: *an accusation's cure must be re-derived against the installed dependency tree AND the program's own rulings before it shapes a wave.*
2. **The C axis's honesty gradient, recorded**: its negative proofs (seven clean checks, the ValueUnit/PRM-rAF/defineModel hazard sweeps) and its self-labelled hypotheses (C-6, X-5) all held; its two census pastes were false at its own HEAD (K-1). An UNWITNESSED-DIRECT report whose mechanisms all verify but whose command outputs do not is exactly why the ledger class exists — mechanisms carried, transcripts quarantined.
3. **The D axis's inversion, named**: it filed the PRM negative proof (K-8) *and* the layout-property negative proof on the same header whose scroll-driven arm the quarantine had already convicted; and its three BLOCKERs all leaned on a producer contract (D-1) that the program had killed. Design canon cited without checking the canon's own supersession queue (W5 item 5, CC-108) produced the corpus's largest severity inflation.
4. **Unresolved severity instinct, preserved**: a case exists for XP-2 + XP-3 + XP-4 as one BLOCKER-class "the instrument lies under failure" identity rather than 1+2 rows — the readers filed them separately and this seat kept their grain so each mechanism keeps its own witness; the next formation may merge them under one unit without loss (ids preserved).
5. **What this seat could not do under the commission**: no browser, so no number in §residue moved from UNPROVEN to CONFIRMED here. Every disposition above is decidable from source + the tree, and was.

## Closing verdict

**ADJUDICATED RED — the corpus's direction confirmed, its two largest frames corrected.** 28 rows survive (2 BLOCKER · 11 MAJOR · 13 MINOR · 2 INFO): 20 corpus claims confirmed (D-1 rescoped against the program's own chassis-death ruling), 8 reader-found admissions (2 corrected in scope). 12 killed claims with written refutations — 8 reader kills upheld, 4 minted by this seat, two of those against reader confirms. Terminal dispositions: **7 rows to X-W5** (h1, announcement substrate, the typed pane seam, the wrapper/fallthrough riders, the KeepAlive contract as addendum, the bespoke-composition half of the housing), **3 to X-W4**, **2 to X-W7**, **3 to X-W8** (one a G-10 census extension), **2 to X-W9**, **5 to X-W10** (MQ-1 explicitly pre-owned at W10:202), **1 to X-W1** (the born-RED flow spec — the single highest-leverage artifact this record proposes), and **6 to NO-WAVE-OWNER**: the extract-session correctness cluster (XP-2/XP-3/XP-4/XP-23/XP-27) plus the tsconfig parity row (XP-21) that X-W8 explicitly declined — the sharpest shipping defects in this corpus live in two composables that no wave's file bounds touch, and that fact, not any single row, is this apotheosis's headline for the next formation boundary.

— end of apotheosis. The challenger record and both re-reader returns remain immutable beside this file per E-1/E-3.
