claude-fable-5 (served model id)

# fr-PaperView — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/paper/PaperView.vue` (685 L) + its closure — `MobileFloatingToc.vue` (397) · `PaperSidebar.vue` (283) · `PaperArticleWindow.vue` (212) · `PaperSearch.vue` (397) + `search/` (Input 69 · Dropdown 70 · Modal 124 · usePaperSearch 108 · index/helpers) · `useScrollNavigation.ts` (246 by `wc -l`; LC's "247" is the missing-trailing-newline off-by-one) · `paperTree.ts` (21).
**Substrate**: fourier `m/w1-bump-migration @ cd26c65` + the frozen M.W1a dirty WT (re-pinned by this seat: `git log -1` + porcelain; `PaperView.vue`, `MobileFloatingToc.vue`, `PaperSearchDropdown.vue` in the modified set — LC's 1/3/1-line diffs accepted; every row below is formally conditional on F.W0's land-or-abandon ruling) · installed glass-ui 4.0.0 / latex-paper 0.2.1 / value.js 0.13.0 / katex 0.17.0 · producer latex-paper read at `/Users/mkbabb/Programming/latex-paper` · producer glass-ui v7.0.0 evidence tag-pinned by both readers (`git show v7.0.0:…`), not re-run here.
**Corpus**: three axis challenges (`audit/fourier-components/PaperView/challenge-{D-design, C-consumption, L-library}.md`) × two hostile re-reader returns — **reader-DU** (design-and-usage) · **reader-LC** (library-and-code) — received in-brief.
**Method (M-23/L-18)**: every verdict-level disagreement between the readers was re-derived against the tree before ruling; every uncorroborated missed find was attacked before banking; **three kills were minted by this seat against the readers themselves** (K-15, K-16, K-17), one of them a kill-of-a-kill. Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony + task law; product source untouched)**: full read `useScrollNavigation.ts` (whole — the performScroll:174-179 bail, navigateTo:202-212 push-before-scroll, withOverlay:69-107 no in-flight guard, getScrollOffset:23-26); `PaperView.vue:40-685` (watcher :129-142 `{immediate:true}` + removeItem :137; restore :264-270; ⌘K :113-118; RO :220-230; teardown :291-297; template :300-412; scoped block :414-685 whole — three `@media` at :531/:540/:560, `.overlay-btn` :582-616 with box-shadow :597 and hover-without-background :607-612, `will-change` :513, track-no-background :474-483, overlay-page :636-644); `PaperSearchModal.vue` whole (Teleport :41, v-if-inside :44, global querySelector :32, hints = ↑↓/↵/esc only, zero ARIA, dead `emit` :12-14); `PaperSearchDropdown.vue:31-70` (two-root fragment: Transition + backdrop div; `defineExpose({resultsRef})` :30 with internal-only readers); `PaperSearchInput.vue:33-47` (single styled root; placeholder-as-name); `PaperSearch.vue:30-46` (Dropdown+Modal mounted unconditionally); `MobileFloatingToc.vue:107-136` (span-in-Button :123-125; bar classes) + `:190-250` (`.floating-toc` height:0; `.floating-toc-bar` declares **no border-radius**; search-btn 50% color-mix); `PaperSidebar.vue:38-45` (snapshot `sections:` beside getters) + `:188-196` (45% top-btn, 1.25rem box) + `:224-233` (`font-weight 0.15s` in the transition list). Greps: `<style` in search children → **0/0/0**; `aria-` across `components/paper/` → **exactly 1** (PaperSidebar:50); `⌘|Cmd|Ctrl+K` → **0**; `.ios` setter in web src → **0**. Installed glass dist: `tokens/color-radius.css:241-253` = exactly **13** stops `--section-color-0..12`; `utilities/base.css` `@layer components` at :43 + `.focus-ring:focus-visible{outline:none;…box-shadow:var(--focus-ring-shadow)}` :174-177. App `src/style.css:105-143`: cartoon-card shim; `--section-color-5` forked both themes; the D.W4.d `:focus-visible` allowlist = sidebar-link/floating-toc-item/callout-btn/gallery-card — **`.overlay-btn` absent**. Paper source: `grep -c '^\chapter{'` → **11**; `\section{Introduction}` at tex **:93** before the first `\chapter` at **:118**. Producer latex-paper: `sections.ts:36-41` `LEVEL_MAP {chapter:0, section:1,…}` + `:110-113` **`topLevel.push(bibliography)`**; `tracking/useTreeIndex.ts:27/:32` (the depth-refresh-only-at-0 parentId bug, line-exact); `html.ts:93-94` `labelKeyToAnchorId = key.replace(/:/g,"-")`; `tracking/useClickDelegate.ts` whole (**`e.preventDefault()` before resolve/scrollTo**; `onUnmounted` re-reads `options.container.value`); `useVirtualSectionWindow.ts:160-162` **`ensureTargetWindow`: `if (index == null) return`**. Installed d.ts: `useVirtualSectionWindow.d.ts:19` `activeId: ComputedRef<any>` verbatim. `e2e/paper-performance.spec.ts:39-48` ("97 → 110 → …" + `0.1.Introduction`). `web/package.json`: `katex ^0.17.0` + `@types/katex ^0.16.8` both present; `katex` ships `types/katex.d.ts`; `@types/katex` installed.

## Scoreboard

**80 rows ADJUDICATED-CONFIRMED (6 BLOCKER · 28 MAJOR · 30 MINOR · 16 INFO — counting the 13 missed rows banked, one of them a reader-missed BLOCKER this seat re-proved link-by-link) · 18 killed-register entries (3 minted by this seat against the readers, incl. one reversal of a reader's kill) · 1 reader miss folded to a banked identity without re-booking (vue-tsc RED = fr-AnimationControls M-15).**

The component is a paradox the record should state plainly: its reading-instrument core (the dual-path progress bar, the deferred-section recipe, the escaped `v-html` search sink, the virtual-window teardown discipline) is the best-engineered cluster in the fourier corpus — five superlatives survive hostile re-derivation, two of them producer-ratified — while its **navigation and search shells are broken at the feature level**: session restore provably dead 100% of the time, ~43% of in-paper cross-references silent no-ops that mint bogus Back-history, the search UI double-mounted and unstyled, and the one keyboard entry point case-sensitive and viewport-inert.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **Corpus denominators** (roots / flat / pages / colour-stop headroom) | killed L§11 with "~12 roots, ~97 flat"; endorsed D·m-10's "one stop of headroom" as **EXACT**; repeated "47-page" in its own M-4/MF-8 cells | **measured** by running the transform: **13 roots / 98 flat (13+51+34) / 374 search entries / totalPages 110**; killed m-10 AND C-20 — **zero headroom** | **LC wins outright — seat re-derived the root count from primary sources** | 11 `^\chapter` + the pre-chapter `\section{Introduction}` (tex:93 < :118) + the **bibliography root** `sections.ts:110-113` `topLevel.push(bibliography)` = **13**; installed dist ships exactly 13 stops (`--section-color-0..12`, :241-253). DU's arithmetic omitted the bibliography root at every site — including inside its own kill. K-16/K-17 minted; LC's missed MAJOR banked (MF-10); e2e's own "97 → 110" note corroborates 110 pages (seat-read). |
| R-2 | **The IO `display:none` premise** (desktop mount of MobileFloatingToc → the 8px anchor, B-2's desktop arm, L·D17) | KILLED D·B-2's hedge: "NOT AMBIGUOUS — edge-adjacency governs zero-AREA boxes, not absent ones" | preserved as "THE ONE PREMISE THAT SETTLES FOUR ROWS", decidable only by implementation source or one live probe; noted C-01 overreached | **LC wins — seat kills DU's kill (K-15)** | `getBoundingClientRect()` of a boxless target returns the zero rect at the origin, and the spec's isIntersecting clause ("intersect **or are edge-adjacent**, even if the intersection has zero area") does not draw DU's absent-vs-zero-area distinction — DU's ground proves the opposite of what it claims. The universal-implementation answer (false ⇒ desktop mounts the bar) is near-certain but is implementation truth, not spec derivation, and this seat has no browser by law. Premise = **SS-13 probe #1**; C-02/L·D25 books MAJOR conditional-high-confidence; **B-2 stands unconditionally on LC's mobile-scrolled arm** (at <1024px past the inline nav, both instances mount with no IO reasoning at all). The honesty gradient is preserved: D·B-2 hedged rightly, L·D17 hedged rightly, C-01 asserted wrongly (K-18), DU killed wrongly. |
| R-3 | **C-01b's denominator** (orphaned selector groups in `PaperSearch.vue`) | confirmed "26 of 41" as booked | killed the count: **~35 of 41** (fragment-root kills `.paper-search-results`/`-backdrop`/floating modifier; `<Transition>` classes land on the un-scoped teleported node, killing all 8 transition selectors; `-empty/-footer/-hint/kbd` are teleported descendants) | **LC wins** | Seat verified the two mechanisms LC's recount needs: all three children have **zero** `<style>` blocks (grep), and Dropdown's template root is a two-node fragment (Transition :35 + backdrop :65 — read). 26 is the certain floor, ~35 the honest figure; BLOCKER either way. |
| R-4 | **D·M-4 severity** (progress bar under PRM) | demoted to MINOR (L·D22's falsifier holds) | second half killed (track has no background — nothing "reads as broken"), first half survives | **MINOR — unanimous in substance** | Seat verified `.paper-progress-track` :474-483 declares no `background` and the bar rests at `scaleX(0)`. What survives: PRM users lose the sole continuous position affordance for a 110-page read (a state readout withheld, not an animation reduced). Both kills of the "visibly broken bar" + `visibility:hidden` cure ratified (K-2). |
| R-5 | **D·M-14's mechanism detail** (how the glass hover dies) | ":582-616 re-declares … plus `:hover` and `:active`" | the `hover:bg-(--glass-bg-resting)` arm is defeated by the unlayered **base** `background` at :592 — "there is no scoped `:hover` background rule" | **LC's route adopted** | Seat read :607-616: the scoped hover declares color/border-color/box-shadow/transform and **no background** — LC's sharper route is the true one; the verdict (unlayered scoped block outranks every layer; the component orders glass then overrides all five glass properties) is unanimous and stands MAJOR. |
| R-6 | **L·D7's altitude** (6 of 9 `v-for` hosts invisible to callsite-keyed budgets) | not returned | MAJOR, enumeration line-exact | **MINOR — seat demotes** | The enumeration is credible (and cheap to re-check), but the harm is audit-budget blindness, not a runtime defect, and the identity is a sharpening of the census F.W4 exhaustiveness row (`CENSUS-2026-08-03.md:362`), which keeps it. A budget note booked MAJOR against a component would double-charge the census. |
| R-7 | **LC's uncorroborated missed BLOCKER** (cross-ref / non-section search navigation = silent no-op) | absent from DU's return entirely | filed with a measured 71/164 refs + 66/374 search entries dead + navStack pollution | **BANKED BLOCKER — attacked first, then confirmed at every link with this seat's own instruments** | The chain re-proved from primary sources: `useClickDelegate` **`preventDefault()`s** then resolves `anchorId ?? elementId ?? sectionId` (producer read); `labelKeyToAnchorId` = colon→dash (`html.ts:93-94`); `performScroll:174-179` — `estimateAbsoluteTop` null for any non-flat-section id ⇒ `ensureTargetWindow(id); return;` with **no `getElementById` fallback on that path**; `ensureTargetWindow:160-162` bails on unknown ids (`itemIndex.get → if (index == null) return`) — and even a non-bailing mount would move no scroll. `navigateTo:202-212` pushes `activeId` **before** `performScroll`, so every dead click materialises a Back button for a jump that never happened (:399 `v-if="navStack.length > 0"`). The producer's own `useScrollTo` (getElementById + rAF retry) accepts any element id — the fork silently narrowed the domain: a regression, not a design. The 71/164 · 66/374 · 96/149 counts are LC-measured (transform run), adopted as reader-measured with the mechanism seat-proved; one live click on an `eq:` ref → SS-13 as confirmation, not as decider. |
| R-8 | **DU's uncorroborated missed MAJORs** (MF-1 focus ring · MF-2 combobox ARIA) | filed | absent from LC's return | **BOTH BANKED — seat-verified fully static** | MF-1: glass's ring is a **box-shadow** inside `@layer components` (`base.css:43` + `:174-177`, seat-read) with `outline:none` in the same rule; `.overlay-btn`'s unlayered scoped `box-shadow` (:597) overwrites it while the layered `outline:none` survives ⇒ no ring, no outline on the one history-navigation control; the app's own D.W4.d outline allowlist (style.css:136-143, seat-read) names four classes and omits `.overlay-btn`. Ratifies DU's kill of M-14's "focus-ring survives" clause (K-3). MF-2: one grep decides it — `aria-` across the whole paper directory hits exactly once (the sidebar nav label); the full arrow/Enter/Escape listbox model ships with zero roles, zero states, no labels, no dialog semantics. |

**Severity rulings inside agreed verdicts**: L·D27 stays MAJOR with its rationale **rewritten** — the modal hints are ↑↓/↵/esc only (seat-read :109-119; repo-wide ⌘/Cmd grep → 0), so ⌘K is *undiscoverable everywhere* rather than "advertised then inert" (K-8). L·D5/D30 books MAJOR as a pattern (two dead teardowns reading as live, one in the producer) with the harm honestly bounded — the elements the listeners ride are themselves being detached; LC's one-word cure (`onBeforeUnmounted` runs before ref-nulling) is adopted as the canonical fix over the cached-element idiom alone. L·D12's catch-scoping row carries the corollary both readers noted: its harm is nil **while D1 stands** (the wrapped `performScroll(saved)` is unreachable), and goes live the moment D1 is cured — the cure wave must fix both together.

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure. Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W9/W10** coverage · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **LATEX-RELAY** rides the standing latex-paper coordination mail · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring. ★ = banked missed row.

### BLOCKER (6)

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **★NAV-1** (LC miss, seat-proved) | Cross-reference and non-section search navigation are silent no-ops: `performScroll` accepts only flat-section ids; the two largest navigation producers hand it anchor ids (`eq-f1`, `thm-…`). Reader-measured: **71/164** `\ref`-family uses and **66/374** search entries dead; every dead click still `preventDefault()`s AND pushes a bogus navStack entry. The fork regressed the producer's id-domain (`useScrollTo` is getElementById-based). | useScrollNavigation.ts:174-179, :202-212; useClickDelegate (producer, whole); html.ts:93-94; ensureTargetWindow :160-162 — all seat-read | **F.W3** (de-fork to the producer's scroll domain, or widen `performScroll` with a getElementById arm) — the navStack push must move after a successful scroll. SS-13 confirms one click. |
| **L/D1** | Session scroll-restore is dead 100%: the `{immediate:true}` watcher runs during setup with `activeId` already = `flatSections[0].id` (library `watch(items,{immediate:true})→recalculate()` at construction; probe `0.2·innerHeight` cannot reach flat[0]'s 910px estimate below a 4550px viewport — LC measured; DU's 320px-floor derivation agrees directionally) and takes the `removeItem` branch; the `onMounted` reader always sees null; `catch{}` guarantees silence. | PaperView.vue:129-142 (:137), :264-270 — seat-read; both readers' independent chains | **F.W4** — persist on a debounced/`visibilitychange` write (cures L/D20 in the same edit) and never destroy the key before the restore reads it. |
| **D/B-1 = C-01b** | `PaperSearch.vue`'s scoped stylesheet is orphaned for **~35 of 41** groups (R-3): all three children ship zero `<style>` blocks and no scope-id reaches the teleported modal, the fragment-rooted dropdown, or the input chrome — the search UI renders unstyled. Consequence wording per DU's nick: below-the-fold and document-scrollable, visually "nothing happened". | seat greps + Dropdown :33-70 fragment root; Vue setScopeId single-root rule (both readers) | **F.W4** — move the styles into their owning SFCs (E-2 KISS); the painted landing spot → SS-13. |
| **D/B-2 = C-01 = L/D4** | One `usePaperSearch` state fans to two always-mounted `PaperSearch` subtrees; each teleports its own modal to `<body>` with `v-if` INSIDE the Teleport on the shared `isExpanded` — double overlay, double `<input>` racing `nextTick` focus, and a global `.search-modal-results` query resolving to whichever teleported first. **Mobile-scrolled arm unconditional**; desktop arm rides the R-2 premise. | PaperView.vue:111/:328/:347; PaperSearchModal.vue:41-44, :32 — seat-read; usePaperSearch.ts:22 | **F.W4** — one modal owner (mount at PaperView altitude), the two hosts keep only triggers. |
| **D/B-3** | glass-ui 7.0.0 removes `variant`/`size="icon"` from `ButtonProps` (tag-pinned by both readers independently, line-exact); 17 callsites in this closure break; naive migration inverts the design (default secondary/neutral = glass capsule where `emphasis="quiet"` is meant); the coarse-pointer 44px floor re-keys from `[data-size="icon"]` to `[data-control-target]` and detaches silently without `iconOnly`; `.overlay-btn` becomes a 32×40 oval under `min-block-size: var(--button-size)`. | `git show v7.0.0:src/components/button/{Button.vue,styles.css}` + `responsive.css:3-7` (both readers, agreeing line-exact); installed CVA read | **F.W1** — uplift-conditional BLOCKER: the migration map (variant→emphasis/tone/iconOnly) is agglomeration input; LC's citation correction K-14 rides (icon rules at v7 styles.css **:30/:35**, not :47-50). |
| **D/B-4** | The mobile search control is a click-only `<span>` nested inside the TOC `<Button>`: no keyboard path, no role, no name; `Enter` on the focused parent toggles the TOC instead; glyph at ~2:1. Accname-pollution sub-claim stays PLAUSIBLE (engine-variable), rows 1/2/4 stand alone. | MobileFloatingToc.vue:117, :123-125, :244 — seat-read | **F.W4** — a real `<Button>` sibling, not a nested span. WCAG 2.1.1 (A). |

### MAJOR (28)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **★MF-1** (DU miss, seat-proved) | The Back button has NO visible focus indicator: unlayered scoped `box-shadow` (:597) overwrites the layered focus-ring box-shadow while the layered `outline:none` survives; the app's own outline allowlist omits `.overlay-btn`. WCAG 2.4.7 AA on the sole history control. | **F.W4** — same edit as M-14/M-15; class = banked FR-COB-8 (fold by reference, new instance/new SC). SS-13's highest-value single probe. |
| **★MF-2** (DU miss, seat-proved) | Full combobox/listbox keyboard model with ZERO ARIA across four files (one grep: 1 `aria-` hit in the whole directory, the sidebar's); nameless input (placeholder-as-name), silent arrows, `is-selected` marked by an — orphaned — class, modal not a dialog. Composes with L/D27's stuck-open state. | **F.W4** — combobox/listbox/dialog semantics land with the B-2 re-home; no banked combobox row exists (registry checked by DU; disclosure rows ≠ this). |
| **★MF-10** (LC miss, seat-re-derived) | `--section-color-${si}` has **ZERO headroom**: 13 roots consume exactly 13 stops (R-1); the 14th `\chapter` makes `var(--section-color-13)` invalid-at-computed-value-time at four fallback-free sites; root identity unstable (bibliography injected at si=9); stop 5 already app-forked (style.css:119-131, seat-read). Supersedes D·m-10 (killed) and C-20's headroom cell (killed). | **F.W4** — `var(--section-color-${si % 13}, var(--foreground))` one-liner + a root-count assertion; GLASS-RELAY rider only if more stops are wanted upstream. |
| **D/M-1 = L/§6** | Both `scrollTo({behavior:"smooth"})` sites PRM-ungated; the census row crediting a "smooth-scroll opt-out" at :176 is CONTRADICTED (that matchMedia gates only the progress-bar listener — seat-read :173-182). | **F.W4** + census `lane-frontend.md:617` correction note (dated evidence stands; the correction is new record). |
| **D/M-2** | Four motion recipes, two spatial, zero `reduce` blocks; mitigation real but undeclared (glass a11y-overrides narrows `transition-property`, dropping `transform`). Enumeration corrected: **three** `@media` blocks (:531/:540/:560 — seat-verified; K-9). | **F.W4**. |
| **D/M-5** | `slide-down` is a no-op: percentage `translateY(-100%)` against `.floating-toc`'s `height: 0` root (seat-read :193-199). Only the co-declared opacity is visible. | **F.W4**. |
| **D/M-6** | Back button's accessible name is the history digit once the badge renders (name-from-content outranks `title`); below 2 it's "Back (1 in history)". | **F.W4** — `aria-label="Back"` + `aria-hidden` badge; same edit as ★NAV-1's navStack repair. |
| **D/M-7** | Light-mode `--primary` byte-identical to `--foreground` ⇒ `variant="link"` mobile TOC rows have zero link affordance (WCAG 1.4.1, always-on). Both readers verified both token arms. | **GLASS-RELAY** (producer token) + **F.W4** interim (underline). |
| **D/M-8** | Page readout at **2.88:1** (arithmetic reproduced independently by both readers to 3 decimals) vs a 4.5:1 floor; `--muted-foreground-strong` exists unused; "47-page" descriptor killed → 110. | **F.W4**; composited value under `.glass-wash` → SS-13. |
| **D/M-9** | Three more alpha-muted tokens below floors: sidebar-label 2.39:1, sidebar-top-btn 1.87:1 (seat-read the 45% site), floating-toc 50%, placeholder 45%. Consumer-override falsifier held. | **F.W4** — swap to the by-colour rungs. |
| **D/M-10** | Button-base `whitespace-nowrap` never reset in the 220px rail ⇒ unwrappable TOC titles + horizontal scrollbar; the author's own `overscroll-behavior-x: contain` self-corroborates. | **F.W4**. |
| **D/M-11** | 8 of 9 authored icon sizings dead under `[&_svg:not([class*=size-])]:size-(--ui-glyph)` at (0,2,1) vs (0,1,0); emission guaranteed by `@source "../*.js"`; producer documents the `size-*` escape verbatim. LC's v7 citation drift recorded (K-14). | **F.W4** — `size-*` utilities. |
| **D/M-12** | `pointer: coarse` inflates the `size="icon"` sidebar header to 44px (used-value clamp, uncontestable) with a 24px glyph in the author's 20px box, in a 220px column. | **F.W4**; painted break → SS-13. Same mechanism read in the rescue direction = superlative S-5. |
| **D/M-13** | Mobile TOC dropdown: trigger with zero ARIA (the MF-2 grep covers it), role-less panel, bare-div dismisser, no Tab containment over a frozen scroller. | **F.W4** — class banked at fr-CanvasControlsDock M-5 / fr-CoefficientsPanel FR-CP-39 / fr-GalleryDraftsSection M-2 (cited, not re-derived); this is a new instance. |
| **D/M-14 = C-04/C-05** | Orders `variant="glass" size="icon"` then overrides all five glass properties; mechanism = unlayered scoped block vs `@layer components` (LC's base-background route adopted, R-5); recurs at 4 sibling sites; `.overlay-page` uses `glass-wash` bare 200 lines away (C-04's sharpest form). | **F.W4** — pattern identity with banked fr-App MG-δ / fr-EasingPicker MISSED-A noted, instances filed here. |
| **D/M-15** | Every shadow re-types a token by hand; two sites byte-identical to `--shadow-cartoon`; the repo's own `cartoon-card` shim is the third spelling (seat-read). Cure table corrected: `--shadow-md`, never `--shadow-soft` (K-4). | **F.W4**. |
| **C-02 = L/D25** | `getScrollOffset()`'s global `.floating-toc-bar` query returns 8 (boxless offsetHeight 0 + 8), never the authored 16, on every desktop landing; propagates into all four position consumers and the correction loop converges on the wrong number (seat-read :23-26, :46, :59, :135, :198). | **F.W4** — conditional on R-2's premise at high confidence; measure once in SS-13. |
| **C-03** | glass-ui 4.0.0 peers `value.js ^0.10.0||^0.11.0` vs installed 0.13.0 — live breach, silent because optional; `@lucide/vue ^1.16.0` required-not-optional (feeds C-14). | **F.W1** — identity = census §1 pins (one identity with the fr-AnimationControls C-1 fold; never re-booked per component; this record adds the live-today framing). |
| **C-06** | latex-paper 0.2.1 ships `activeId/activeRootId: ComputedRef<any>` (seat-read d.ts:19); six typed contracts downstream vacuous; the repo's only automated gate proves nothing here. | **F.W1** rider + **LATEX-RELAY** (generic fix at `ref<T|null>`). |
| **C-07 = L/D28** (absorbs D·m-12) | Child writes the inline `overflow` **shorthand** onto the scroller PaperView owns, via a raw-element prop, against asymmetric authored axes; five other owners read that element blind. | **F.W4** — emit or a scroll-lock composable owned by the scroller's owner. |
| **C-08** | 50 `color-mix(in srgb…)` / 0 `in oklab` against the substrate's declared oklab standard + three dark-blind `hsl()` literals (simultaneously dead per B-1); F.W2's colors.ts work provably does not reach this subtree. | **F.W4**. |
| **L/D2** | `useScrollNavigation` owns rAF chains + two timers, exposes no dispose, has zero teardown; the correct pattern ships in the same package (seat corroborates via the `bindContainer` read). | **F.W4** — dispose handle + PaperView teardown. |
| **L/D3** | `withOverlay` non-reentrant (per-invocation `finished`, no in-flight guard — seat-read :69-107) × six user-reachable entry points into `navigateTo` (all six verified by both readers). | **F.W4** — serialize; visual thrash → SS-13. |
| **L/D5 + L/D30** | `disarmProgressFallback` and the producer `useClickDelegate` unbind are guaranteed no-ops: both re-read a template ref after Vue's synchronous unmount-path nulling (both readers re-derived in the installed runtime, agreeing line-exact; seat re-read the producer source today). Harm bounded (elements detached anyway); pattern MAJOR. | **F.W4** (consumer) + **LATEX-RELAY** (producer twin). Cure = `onBeforeUnmount` (LC) or the cached-element idiom (S9). |
| **L/D6** | Duplicate ResizeObserver + window-resize handler, both synchronous where the library's are rAF-coalesced, both writing the `leadingOffsetPx` ref the library already watches ⇒ up to 3 layout rebuilds/frame; LC's RO-loop-shape sharpening adopted. `updateSectionStartOffset` itself must stay. | **F.W4** — delete the redundant `recalculate()` calls. |
| **L/D8** | `:ref="(el) => bindSection(item.id, el)"` re-invoked unconditionally per patch (no equality guard in the installed runtime — both readers) and `measureSection` evaluates `el.offsetHeight` as the argument, before the dedupe ⇒ one forced layout read per visible section per patch. The guarded twin is 300 lines away (S1). | **F.W4** (consumer guard) + **LATEX-RELAY** (hoist the read behind the guard). |
| **L/D26** | Four whole-corpus traversals of a build-frozen constant, synchronous in setup, per mount, on the critical path; `scheduler.ts` exists and is unused here. Denominators corrected to **13/98/374** (R-1). Duration → SS-13. | **F.W4** — memoize at module scope (the data is frozen); defer the search index to first open. |
| **L/D27** | ⌘K: case-sensitive (`e.key==="k"`), inert below 1024px (seat re-walked LC's chain: at top-of-paper mobile only the `display:none` sidebar instance is mounted; `focus()` boxless = no-op), no dismiss path, no target guard. Rationale rewritten per K-8: undiscoverable everywhere. | **F.W4** — `e.code`/lowercase + window-level Escape + a visible hint (the modal already renders a kbd row to put it in). |

### MINOR (30)

| id | claim | disposition |
|---|---|---|
| **D/M-4** (demoted, R-4) | Under PRM the progress affordance is deleted, not reduced — both writers disarmed, terminal state the author's own `scaleX(0)`; nothing paints broken (kill K-2 holds). | **F.W4** — a PRM-visible static readout (pairs with MF-8). |
| **L/D20** (= D/M-3's surviving half) | Synchronous `sessionStorage.setItem` inside a pre-flush watcher per section crossing (98 boundaries, corrected). | **F.W4** — same edit as L/D1. |
| **L/D9** | `navigateBack`'s 500ms flag window is a clock, not a completion signal; drops pushes. | **F.W4** — dies with ★NAV-1's push-after-scroll repair. |
| **L/D10 = C-12** | Partial maps typed total (`noUncheckedIndexedAccess` absent), one unused type import (`PaperSectionData`, single occurrence); L's own falsifier killing 2/5 rows upheld (LC re-verified the vite-plugin's total pageMap). | **F.W4** hygiene. |
| **L/D11 = C-11** | Dead `treeIndex` prop (`Map<string, any>` at the seam, declared/passed/never read) + `currentSection`'s provably-identity index round-trip. | **F.W4**. |
| **L/D12** | The restore `catch{}` wraps `performScroll` too — too wide AND too narrow; harm nil while D1 stands, live the moment it's cured. | **F.W4** — same edit as L/D1, mandatory pairing. |
| **L/D15 = D/m-4 = C-09** | Three TOC implementations; the inline third renders roots-only, centres its rows under Button's `inline-flex justify-center`, and its `.mobile-toc-link` class is styled nowhere (repo grep = 1 hit, the application site). | **F.W4** — the inline copy's real job is IO sentinel; make that explicit or sentinel on the nav element alone. |
| **L/D29** | Producer `useTreeIndex` records the ROOT id as `parentId` for every depth≥2 node (seat-read :27/:32); masked by the `isDescendant` fallback; 34 depth-2 nodes live. Latent, correctly filed. | **LATEX-RELAY** — the same package's other walk gets it right (S9-adjacent contrast). |
| **L/D7** (demoted, R-6) | 6 of 9 `v-for` hosts invisible to callsite-keyed budgets. | fold → census F.W4 exhaustiveness row (identity stays census). |
| **D/m-1** | h1 ramp shrinks across md (40.5→54→52px against the root drop — LC recomputed). | **F.W4** sweep. |
| **D/m-5** | Declared 48rem article max unreachable (45.25rem effective — both recomputed). | **F.W4** sweep. |
| **D/m-6** | Mobile nav landmark unnamed (the MF-2 grep is the proof). | **F.W4** — same edit as MF-2. |
| **D/m-8** | Relative type (`@apply text-sm`) inside the fixed 16px badge ⇒ 15.75px at the mobile root. | **F.W4** sweep. |
| **D/m-9** | `text-rendering: optimizeLegibility` over a virtualised 98-section tree. | **F.W4**; cost → SS-13 instrumentation. |
| **D/m-13** | Edge fades double-attenuated (gradient stop × mask ramp over the same 2rem). | **F.W4** sweep. |
| **D/m-16 = L/D19 = C-17** | PRM sampled once at arm-time; no `change` listener. | **F.W4** — same edit as M-1/M-2. |
| **D/m-17** | `title` as sole label on four icon controls. | **F.W4** — same edit as MF-2/M-6. |
| **D/m-18** | Dropdown backdrop does not cover the bottom overlay (later sibling, equal z, same context — LC's sharpening: the panel itself sits under it too). | **F.W4**. |
| **D/m-14** (as corrected by K-10) | Easing holdouts: the true set is **5 `s ease` sites** (incl. PaperSearch:129, :360) + 1 token-less shorthand (:94) — six sites, two categories. | **F.W4** sweep. |
| **C-10** | `resolveFigure` called 7× per figure per render (both counted). | **F.W4** — hoist per-item. |
| **C-13** | One producer type reached by three specifier spellings. | **F.W4** hygiene; rides F.W1's manifest pass. |
| **C-14** | `lucide-vue-next` devDep with 6 runtime import sites; required peers (`@lucide/vue`, `reka-ui`) unlisted in the consumer manifest. | **F.W1**. |
| **C-15 + C-16** | `vendor-math` welds value.js (145 809 B, zero paper imports — LC measured the empty coupling set) to katex; `vendor-ui` names the barrel no site imports. | **F.W1** — decide with a fresh build (stale `web/dist` inadmissible, both readers agree). |
| **C-18 = L/D18** | `usePaperSearch` binds a debounced watcher with no scope disposal. | **F.W4** — same edit as B-2's re-home. |
| **★MF-3** (DU miss, seat-verified) | `.floating-toc-bar` (Button mode) computes `border-radius: 9999px` from `btn-pill` — no scoped radius contests it (seat-read :205-221) — while its search-mode `<div>` twin is square: one bar, two geometries. | **F.W4**; how loudly it reads → SS-13. |
| **★MF-4** (DU miss, seat-verified) | U+2131 ℱ as a display glyph in the sole `<h1>` (seat-read :356) + 4 sibling sites: "script capital F ourier" to AT; no text alternative. Registry: the glyph's FONT rows (FR-AH-2 cluster, fr-BasisSelector m-3) don't cover announcement — new mechanism, adjacent row cited. | **F.W4**. |
| **★MF-5** (DU miss, seat-verified) | iOS auto-zoom on the mobile search input: the sizing rules are all orphaned (B-1) so the input paints at UA ~13.3px, and the substrate's `.ios` guard never activates — fourier sets no `.ios` class (seat grep: 0). | **F.W4** — folds into B-1's blast radius; the mobile consequence named. |
| **★MF-6** (DU miss, seat-verified) | `.sidebar-link` transitions `font-weight` (seat-read :229-233) — reflow + synthesized-weight snapping against the remapped serif; exists only for full-motion users. | **F.W4** sweep. |
| **★MF-7** (DU miss, seat-verified) | Permanent `will-change: opacity` on a fixed full-viewport overlay (seat-read :513) — a standing compositor layer for far-jump-only chrome. Process finding preserved: L routed it to a sibling axis and no register received it. | **F.W4** — set/clear inside `withOverlay`. The referral-drop lesson → **NO-WAVE-OWNER** process note for SS-3/SS-4. |
| **★MF-11** (LC miss, seat-verified) | Dead API surface in the search subtree: two `defineEmits<{select}>` never emitted (both components call `search.selectResult` directly — seat-read), one `defineExpose({resultsRef})` with zero external consumers. Misleading contract on a 🟡 candidate-shadow family. | **F.W3** — clean before the `./search` diff. |

### INFO (16)

| id | claim | disposition |
|---|---|---|
| **D/i-1** | Zero automated coverage of the search surface; no vitest runner in web/. | **F.W9/W10** — coverage rides the B-1/B-2 cure wave. |
| **D/i-2 = S-1 adjacency** | v7.0.0 hoists exactly the consumer's two progress-bar declarations — producer ratified this consumer. | record only (see superlative S-1). |
| **L/D13 = D/i-3** | `computed(() => paperSections)` over a frozen module constant. | **F.W4** sweep. |
| **D/i-4** | `max-width: 100dvw` inert on the flex scroller. | **F.W4** sweep. |
| **D/i-5** | CVA `sm` arm trivia. | record only. |
| **C-19** | latex-paper peers `katex ^0.16` vs installed 0.17.0. | **F.W1** + **LATEX-RELAY**. |
| **C-20** (headroom cell excised by K-6) | `hsl(var(--section-heading))` invalid against glass's oklch token at six theme sites; latent behind the always-present `section-index`. | **LATEX-RELAY**. |
| **L/D14** | `_scrollTo` indirection — no real cycle exists (both agree). | **F.W4** sweep (inline it). |
| **L/D16** | `PAPER_CONTEXT` provided AND the same values prop-drilled. | **F.W4** — pick one transport. |
| **L/D17** | Desktop over-mount of MobileFloatingToc (the R-2 premise's face). | rides C-02's row; SS-13 #1. |
| **L/D21** | Producer `useKatex` cache keys omit the macro table; docblock promises an `inject` fallback the code lacks. | **LATEX-RELAY**. |
| **L/D23** | 685 lines, ~11 setup concerns. | **F.W4** — decomposition rides the cure edits, not a rewrite (E-2). |
| **L/D24** | The `onMounted → nextTick` body unguarded against unmount. | **F.W4** sweep. |
| **★MF-8** (DU miss, seat-verified) | The page readout — the product's only reading-location affordance — is unannounced (`no aria-live/status`) and uncopyable (`user-select:none`, seat-read :643); three independent exclusions from one affordance (with M-8 + M-4). | **F.W4** — `role="status"` + drop `user-select:none`; pairs with M-4's PRM readout. |
| **★MF-12** (LC miss, seat-verified) | Dual ambient type sources for katex: `@types/katex ^0.16.8` beside katex 0.17's own shipped types, no `types` array, `skipLibCheck` masking (seat-read package.json + both trees). | **F.W1** manifest pass. |
| **★MF-13** (LC miss, seat-verified) | `useSidebarState` handed a snapshot `sections:` value beside getters in both hosts (seat-read :38-45) — inert only because the source is build-frozen (L/D13's constant). | **F.W3** — one-line spelling fix during the sidebar work; the adoption itself stays exemplary (S-4). |

**Reader miss folded WITHOUT re-booking (identity guard)**: LC's "the repo's sole automated gate is RED at `PaperView.vue(12,8) TS2882` and both other axes reason about a gate that has never been green" = **fr-AnimationControls M-15 / R-4 / C-3**, already banked → **F.W0**. LC's fourth independent reproduction is corroboration; the one increment banked here is the corpus consequence — L·S4's "entire content surface is checked" superlative survives only as "tsc reports all diagnostics and found one", never as "the gate enforces"; D·B-3's "at least fails loudly" hazard cell inherits the same correction.

## Killed-claims register (18)

| # | killed claim | refutation (adjudicated) |
|---|---|---|
| K-1 | **D/M-3** — "session restore fires an unrequested animated scroll on page load" (MAJOR) | Dead on its premise: L/D1 (seat-verified) proves the setup-time `removeItem` branch is deterministic; `performScroll(saved)` is unreachable. Both readers killed; ratified. The corpus's clearest case of a D-axis row assuming a feature works because it reads as if it does. |
| K-2 | **D/M-4**'s "visibly broken zero-width bar" + `visibility:hidden` cure | The track declares no background (seat-read :474-483); a collapsed bar in a transparent 2px track paints nothing; the cure is a no-op. Both readers; ratified. First half survives at MINOR. |
| K-3 | **D/M-14**'s clause "what survives from glass is `focus-ring`, …" | The ring is a box-shadow in `@layer components`; the unlayered :597 shadow kills it while `outline:none` survives (seat-verified both halves). DU's kill ratified; consequence banked as ★MF-1. |
| K-4 | **D/M-15**'s `--shadow-soft` prescription | `--shadow-soft` is itself a pure-black literal (`:11`); only the elevation scale composes through `--shadow-color`. Both readers; ratified. Correct cure: `--shadow-md`. |
| K-5 | **L §11** "51 + 35 = 86 — a third independent line" (`\section ↦ root`) | `LEVEL_MAP` makes chapters roots (seat-read :36-41); the "exact agreement" was two wrong premises colliding. Both readers killed; **LC's corrected figures adopted (13/98)** — see K-16 for DU's own residual error. A false corroboration that inherited R1's error instead of testing it. |
| K-6 | **D/m-10** "one stop of headroom" + **C-20** "2 slots of headroom" | 13 roots (11 chapters + pre-chapter Introduction + injected bibliography — all three seat-verified in primary sources) against exactly 13 shipped stops = **zero**. LC's kill ratified with the seat's own derivation; replacement row banked ★MF-10. |
| K-7 | **D/m-11** "no empty state: `pg 1/0`, lone '.'" | Unreachable: the vite plugin throws on empty tocEntries / `totalPages <= 0` / tracked-section mismatch (LC's plugin read); the falsifier named the escape and didn't check it. Ratified. |
| K-8 | **L/D27**'s severity rationale "an *advertised* ⌘K affordance goes inert" | The modal hints are ↑↓/↵/esc only; repo-wide ⌘/Cmd/Ctrl+K grep → 0 (seat-run). ⌘K is undiscoverable everywhere — a different and arguably worse fact. DU's kill ratified; the row's four faults stand. |
| K-9 | **D/M-2**'s enumeration "no `@media` other than :531/:540" | Three: :531, :540, :560 (seat-read). Verdict unaffected; a repair pass from the cell would miss a block. DU's kill ratified. |
| K-10 | **D/m-14**'s holdout list (":130", ":94 as `s ease`", :360 omitted) | True set: 5 `s ease` hits incl. **:129** and **:360**, plus :94 as a *token-less shorthand* — six sites, two categories. DU's correction ratified (LC's grep agrees). |
| K-11 | **D/B-1**'s "content is unreachable" consequence wording | Teleport grows the document; `min-height` floors, nothing caps — below-the-fold and document-scrollable, indistinguishable from "nothing happened" but not unreachable. DU's nick ratified; mechanism untouched. |
| K-12 | **D/M-8 / C-17** descriptors "47-page" / "51-section, 11-chapter" | totalPages **110** (LC's parse; the e2e file's own growth note seat-read); structure 13/51/34/98. Every "47-page" scaling argument understates by 2.3×. LC's kill ratified — and DU repeated the stale descriptor twice (DISSENT §1). |
| K-13 | **C-01b**'s "26 of 41" | Undercount: the fragment-root and teleported-transition selector families are equally dead (seat-verified the two mechanisms) ⇒ ~35 of 41. LC's kill ratified; D/B-1's blast-radius paragraph was the more complete all along. |
| K-14 | **D/M-11**'s v7 citation "styles.css:47-50" | At the tag the icon rules sit at :30/:35; :47-50 is the secondary-emphasis rung — a HEAD-read attributed to the tag (the K-12/D-19 drift class). LC's kill ratified; the preserved-escape FACT stands. |
| K-15 | **reader-DU's kill of D/B-2's desktop-arm hedge** ("NOT AMBIGUOUS — edge-adjacency governs zero-area boxes, not absent ones") | **Seat-minted reversal** (R-2): a boxless target's getBoundingClientRect IS the zero rect at the origin, and the spec's edge-adjacency clause doesn't draw DU's distinction — the cited ground argues the wrong way. The conclusion is near-certain by universal implementation behavior, which is exactly what "decidable by one live probe, not by the spec" means. The hedge D·B-2 and L·D17 wrote was correct posture; C-01's flat assertion (K-18) and DU's flat kill are the same overreach in opposite costumes. |
| K-16 | **reader-DU's corrective arithmetic** "roots are therefore ~12, not 51, and the flat count is ~97, not 86" + its endorsement "D's 'one stop of headroom' is EXACT" | **Seat-minted.** DU's own kill of L§11 omitted the bibliography root that `sections.ts:110-113` injects (seat-read) — off by one on roots AND flat, and the "EXACT" endorsement certified a headroom of one where there is zero. A kill that replaces a wrong number with a nearly-right one is the citation-inheritance failure in its subtlest form: DU inherited D·m-10's estimate as the corrective. |
| K-17 | **R1 §7's useClickDelegate rejection** (already overturned by L's R2) | Ratified with fresh instruments: this seat read the producer source today — `onUnmounted` re-reads `options.container.value`, which the installed runtime nulls synchronously before the hook runs. Strike R1 §7; keep D30. |
| K-18 | **C-01's desktop-arm assertion** ("statically forced") | The mobile-scrolled arm is unconditional and carries the BLOCKER alone; the desktop arm was never static. Killed as an epistemic cell (LC's overreach note ratified); the row's verdict is unharmed. |

## Superlatives — with provenance (L-18 runs both ways)

1. **D·S-1 — the consumer shipped the producer's fix one major version early** (`transform-origin: 0 50%` + `scaleX(0)` unconditional at :485-491, seat-read; installed 4.0.0 guards both; v7.0.0 hoists exactly those two — both readers proved it at the tag). The strongest row in the corpus, and it survived a tag-pinned re-check by two hostile readers independently.
2. **The dual-path single-writer progress bar** (D·S-2/S-3 = C·S-1 = L·S2, three-way confirmed): the JS predicate is byte-identical to the recipe's `@supports`, the scroller binding sits on the recipe's own resolution element, the fallback is one passive rAF writing only `transform`. Four standard ways to get it wrong, none taken — and the file's defects ring it without ever entering it.
3. **The `content-visibility` recipe comment survives a substrate audit line-for-line** (D·S-2 = C·S-2): the producer's utility literally names "(fourier γ, …)" and makes the same `auto`-prefix measurement-safety argument; recipe upstream, tuning downstream, reasoning written down and true.
4. **The adapter was pushed INTO the producer** (C·S-3): both TOC hosts ride glass's `useSidebarState` via a `getChildren` the consumer landed upstream — two hosts, one implementation. The one rough edge left is the callsite's own reactivity spelling (★MF-13), which is why that row is INFO and not a defect of the adoption.
5. **L·S9 + L·S1 — the teardown/identity discipline that makes the failures provable**: `useVirtualSectionWindow`'s cached-container unbind and `registerWindowRoot`'s equality guard are the correct forms whose absent twins ARE D5/D30 and D8. A superlative that carries its own contrast case, twice.
6. **The `v-html` sink is closed on every branch** (L·S3): the query computes match indices only and is never emitted — injection structurally impossible, not merely avoided.
7. **D·S-5 — the recorded negative**: `.overlay-btn`'s 2rem looks like a 2.5.5 failure and is rescued by the coarse-pointer used-value clamp; the accusation was checked, disproved, and published as false — the discipline this whole registry runs on, and the same mechanism correctly re-used in the accusing direction at M-12.
8. **Contract-v2 resolution posture** (C·S-5): bare specifiers through the exports map everywhere, `alias: {"@"}` and nothing else — the reason F.W1 is a manifest change, not archaeology.
9. **The growth-tolerant e2e file** (L·S8): refuses to pin the page total and writes down why; LC's independent parse landed on the file's own current number (110). Validated by measurement, not by reading.
10. **Scroll containment complete and deliberate** (D·S-6): both scrollers + all three overlay scrollers contained; the rail cannot shift layout.
11. **Corpus-level**: L's R2 pass re-derived R1 before reading it and overturned exactly one of its predecessor's rejections with the predecessor's own evidence (D30) — and then failed on precisely the row it corroborated against itself (§11). LC repeated the shape at higher rigor: it MEASURED where every other seat estimated, and the two seat-kills against DU (K-15/K-16) both exist because estimation posing as correction is detectable.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **The R-2 premise** (probe #1, settles four rows): at ≥1024px, `document.querySelectorAll('.floating-toc-bar').length` — nonzero confirms the desktop over-mount, the 8px anchor (C-02), and B-2's desktop arm.
2. **★NAV-1 confirmation**: click one `eq:`-family cross-reference; observe no scroll + a Back button appearing; the mechanism is fully static, the click is the receipt.
3. **★MF-1**: focus the Back button by keyboard; read `getComputedStyle(el).boxShadow/.outlineStyle` under `:focus-visible`. Highest-value single probe (WCAG AA on a shipping control).
4. **D/B-1's painted result**: expand search; `getBoundingClientRect()` of `.search-modal-overlay` (below-fold hypothesis).
5. **B-2's double mount**: `<1024px`, scrolled, expanded: `document.body.querySelectorAll('.search-modal-overlay').length` + which input wins the focus race.
6. **D/M-8's composited contrast** under `.glass-wash` (arithmetic 2.88:1 is fixed; the glass moves it the wrong way — exact value live).
7. **D/M-12's painted coarse-pointer header** (44px button, 24px glyph, 220px rail) under emulation.
8. **D/M-13 Tab containment**: two Tab presses from the open dropdown; read `document.activeElement`.
9. **L/D26's duration + D/m-9's shaping cost**: `performance.mark` around :46/:111 and one section mount.
10. **L/D3's visual thrash**: two overlapping far jumps.
11. **★MF-3's severity**: mobile render of the pill-radius bar and the round↔square mode swap.
12. **C-15/C-16 byte figures**: a fresh `npm run build` (stale `web/dist` inadmissible — both readers, seat concurs from its 2026-06-12 date).

## Identity guards (folds by reference — never re-booked)

- **fr-AnimationControls M-15 / R-4 / C-3** (vue-tsc RED at PaperView.vue:12): LC's reproduction folds; only the S4/B-3 corpus-consequence increment is new record. → F.W0.
- **Census §1 pins + G33**: C-03 is that identity AMENDED (live-today framing added) — one identity with fr-AnimationControls C-1 / fr-BasisCanvas C-8. → F.W1.
- **Census FE lane-frontend.md:617**: CONTRADICTED by D/M-1=L/§6 — correction recorded here, dated census text immutable.
- **Census §3a / lane-frontend.md:438**: the `./search` candidate-shadow family owns the fork question; ★MF-11 rides it as a rider. → F.W3.
- **Census F.W4 exhaustiveness row (CENSUS-2026-08-03.md:362)**: L/D7 is its sharpening; identity stays census.
- **FR-COB-8** (unlayered-defeats-layered-a11y-resets, fr-CanvasOverlayButton.md:87; instances fr-App MG-δ, fr-EasingPicker MISSED-A): ★MF-1 files as a new instance by reference — new site, new property, new SC; never a re-book.
- **fr-CanvasControlsDock M-5 / fr-CoefficientsPanel FR-CP-39 / fr-GalleryDraftsSection M-2** (disclosure-ARIA class): D/M-13 is a new instance, cited not re-derived. ★MF-2's combobox class has no banked row — filed new.
- **FR-AH-2** (the ℱ MISS-FONT cluster) + fr-BasisSelector m-3/R-3: ★MF-4 is the same glyph, different mechanism (announcement) — filed new, adjacent row cited; the five-site count stays settled at R-3.
- **fr-AnimationControls K-9/K-15 precedent** (false-citation cells become gates): K-14's v7 line-drift and K-16's off-by-one are recorded under the same law.
- **CENSUS §9 item 2 / fr-AnimationControls M-15** (frozen dirty-WT provenance): the caveat is banked; this record restates it in the header, books nothing new.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance events (evidence about the fleet, not the tree).** (a) Reader-DU repeated the D axis's stale "47-page" descriptor inside two of its own cells (M-4, MF-8) in the same return where reader-LC measured 110 — same signature as the fr-AnimationControls K-15 dual event. (b) DU's K-16 failure is the subtler variant: its *correction* of L§11 inherited D·m-10's "~12 roots" estimate and certified it "EXACT". (c) LC's own citation nits (the "247-line" count; "dist/transform.js" where the dist ships chunks) are recorded here as trivia, not kills — but they are the same class at the lowest stakes. The lesson compounds across three adjudications now: numbers that were not re-measured are not evidence, however corrective their costume.
2. **The R-2 ruling's residual risk, stated against this seat**: if a live probe ever shows an engine reporting `isIntersecting: true` for the boxless sentinel, C-02/L·D25 and L·D17 evaporate and DU's kill was right for the wrong reason. The seat judges this vanishingly unlikely; it is recorded because the whole point of K-15 is that "vanishingly unlikely" is not "derived".
3. **Reader-LC's severity dissent preserved**: L/D7 at MAJOR (this seat: MINOR/census-fold) on the argument that a budget blind spot over the loop rendering the entire article body is how the next corpus-wide miss happens. If the F.W4 exhaustiveness sweep finds a real defect inside one of the six invisible hosts, LC's severity was right.
4. **Reader-DU's severity dissent preserved**: B-4 and MF-1 jointly argue the paper's mobile/keyboard chrome is BLOCKER-weight in aggregate even where each row books MAJOR; this seat kept the per-row grades and names the cluster instead (keyboard-and-AT-reachability: B-4, M-6, M-13, MF-1, MF-2, m-17, MF-8) so severity arithmetic does not hide it.
5. **This seat's own scope notes**: the ★NAV-1/L·D26/★MF-10 magnitudes (71/164, 66/374, 374 entries, 13/98/110) are LC's transform-run measurements — mechanisms seat-proved from primary sources, magnitudes adopted, not re-run; the v7.0.0 evidence is both readers' tag-pinned reads, agreeing line-exact against each other, not re-shown here; the Vue runtime internals (setRef/unmount ordering, setScopeId single-root walk) were accepted where both readers cited the same installed-file lines independently and the producer-source behavior this seat did read (useClickDelegate, ensureTargetWindow) matched their account exactly.

## Closing verdict

**ADJUDICATED — DEFECTIVE AT THE FEATURE LEVEL AROUND A SUPERLATIVE CORE: 80 rows confirmed (6 BLOCKER — one a reader-missed navigation BLOCKER this seat re-proved link-by-link · 28 MAJOR · 30 MINOR · 16 INFO, 13 missed rows banked) · 18 kills registered (3 seat-minted, including one reversal of a reader's kill and one kill of a reader's own correction) · 1 miss folded to a banked identity · routings: 1×F.W0 fold · 7×F.W1 · 4×F.W3 · the balance F.W4, with 2 GLASS-RELAY and 6 LATEX-RELAY riders, 1×F.W9/W10, and 1 NO-WAVE-OWNER process note — all agglomeration input to the forming X·F specs (M-25), stamping nothing.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
