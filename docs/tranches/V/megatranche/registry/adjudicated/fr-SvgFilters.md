served model id: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited. Fresh adjudicator seat (M-23/L-18); no evidence cell in this record is adopted from either re-reader where a disagreement or a load-bearing verdict existed — those were re-derived with this seat's own instruments.

# SvgFilters — adjudicated apotheosis (fresh-Fable incredulity seat)

**Date**: 2026-08-24 · **Subject tree**: fourier-analysis HEAD `cd26c65`, branch `m/w1-bump-migration`, 28 dirty paths (subject not among them) · **Registry tree**: value.js `tranche-u`.
**Corpus**: three challenge axes against `web/src/components/decorative/SvgFilters.vue` (178 lines, read whole by this seat) — challenge-D (design, 21 rows), challenge-L (library, 15 rows), challenge-C (consumption); all three axes self-receipt `claude-opus-5[1m]` on line 1. Two hostile re-readers (design-and-usage · library-and-code) challenged the full corpus; their returns are the input this seat adjudicates — and challenges back.
**Method**: every verdict-level disagreement between the two re-readers (9 ruled below) was re-derived against the trees with this seat's own instruments; suspiciously-agreed cells (the `^0.9.2` kill, the flat-black decode, the handmark filter string) were re-measured, not spot-trusted. **This seat's own instruments (all static + node + git-show; zero browser probes — probe parsimony)**: SvgFilters.vue whole (178); App.vue whole (32 — single static mount at :22, `paper-texture` at :24); installed `pencil-boil@0.4.1 src/vue.ts` whole (159 — PRM gate :129 before enrolment :134, unconditional `ensureScheduler()` :89 in the resume branch, tick :34-55 with no subscriber check, sole `maybeStopScheduler` call :141); pencil-boil manifest (`sideEffects: false`, source-published, barrel `index.ts:17` re-exports `./vue`); node run of all 16 base+offset IEEE-754 sums; node decode of all four installed glass-ui 4.0.0 paper data-URIs (paper.css ×2 byte-identical, scale-paper tokens ×2); handmark `types.d.ts` (HandShape union, boilFps 8 / boilFrames 3) + `dist/handmark.js` filter string (`fractalNoise`, `0.012·roughness`, `seed=`, region `-40%/-60%/180%/220%`, `color-interpolation-filters="sRGB"`); `git show v7.0.0:package.json` + `v8.0.0:package.json` in /Users/mkbabb/Programming/glass-ui (pencil-boil peer `^0.9.2` at 7.0.0 line 542 / devDep pin `0.9.2` :579; `^0.11.2` first at v8.0.0; `./handmark` at v7 :390 vs HEAD-tree :355, worktree version 8.0.0); `git show v7.0.0:src/styles/paper.css` ("disgusting metallic" TWICE :12, DEMOTED feTurbulence :3/:30/:85, `feFuncA slope='0.42' intercept='0.12'` in the relief URI :95, "screen lifts (NOT soft-light" :119); `git show v0.9.2:{package.json,src/vue.ts}` in /Users/mkbabb/Programming/pencil-boil (`hasActiveSubscriber()` guarding resume with verbatim "or never enrolled"; reactive-PRM MQL `change` listener; `useLineBoil(frameCount = 4, intervalMs = 125)` unchanged; `sideEffects: false` persists); live sweeps (`url(#` → 0 across src/+index.html+public/; pencil-boil imports = exactly 3; DESIGN.md = 33 lines, 0 lens hits; e2e = 8 specs; vitest absent); registry authorities re-read at the cited lines: fr-App.md:28/:46/:55/:64/:74/:81/:99/:123/:135/:140, fr-FourierShapeExtractor.md:98-100, fr-AppHeader.md:10/:116, fr-FourierMorphSvg.md:54, kf-HeroAurora.md:9.
**Environment disclosure**: `web/dist/` is stale (banked at fr-AppHeader.md:10 and fr-FourierShapeExtractor); no dist-derived cell carries weight anywhere in this record. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are forming; routings are agglomeration input (M-25).

## Scoreboard

**29 rows survive (6 FOLD-BY-REFERENCE · 23 component rows: 7 MAJOR · 6 MINOR · 10 INFO — D-12/L-13/C-m3 counted as its own INFO row, folded into L-6 as drift evidence; several rows RESCOPED in place) · 14 KILLED · 7 missed finds banked (2 MAJOR live, 1 MAJOR latent) · 12 SS-13 residue items.**

The component identity: a 178-line design declaration with zero design surface — banked before this corpus existed (fr-App.md:55, MAJOR, → F.W3/W4) and re-derived here by all three axes and both readers. The two rows of this corpus that matter beyond the delete are **producer-side**: pencil-boil 0.4.1's empty-loop resume (→ F.W1 at the **corrected** floor `^0.9.2`) and glass-ui 4.0.0's flat-black grain utilities (GLASS-OWNED, BH relay). Three identity-guard catches are this seat's own: L-8 + L-15 fold to banked fr-App.md:74; C-M4's census-contradiction half folds to banked fr-FSE.md:98; C-i2 folds to fr-AnimationControls C-1 via fr-FSE.md:99 — none of the three was folded by the reader that carried it.

## Ruled disagreements (the seat's actual work)

| id | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **R-1 · L-2 grade + blast radius** | MAJOR; separately books the post-deletion widening (its missed #3) as MAJOR | MINOR ("the resurrected loop iterates an empty Set and does zero DOM work"); books the false manifest as INFO, calls the surviving listener "benign in practice" | **MAJOR, one merged row** | Mechanism re-derived whole by this seat at 0.4.1: PRM return :129 precedes enrolment :134; resume branch :84-90 calls `ensureScheduler()` unconditionally; tick :34-55 re-requests on every path; sole disarm call site :141 inside `stop()`, unreachable here. Honest narrowing adopted from neither reader: the loop re-cancels on each tab-hide (:78-83) — "undisarmable **while visible**", not absolutely. The grade is MAJOR not for µs (SS-13) but because (a) the entry condition is the accessibility preference, and (b) the trap **survives the already-banked F.W3/W4 deletion and widens to every user**: `sideEffects:false` + barrel `index.ts:17` + 2 surviving import edges (svg-fourier.ts:11, FourierShapeExtractor.vue:144, my grep) mean the module-scope listener (:76-92) still installs — dev-certain (unbundled ESM), prod bundler-contingent (the manifest claim is false, so the outcome is the bundler's discretion). Reader-2's "benign" clause under-weighs its own confirmed mechanism: with zero subscribers ever, the first tab-return starts the chain for **everyone**. challenge-L §7 step 1 ("Discharges L-1 … L-15 in one commit") and challenge-C's "−2 RAF subscribers" blast radius are both corrected by this row. Cure verified at the correct floor: v0.9.2 guards resume with `hasActiveSubscriber()` ("or never enrolled" verbatim) AND adds the reactive-PRM MQL listener. |
| **R-2 · D-8** | CONFIRMED whole (MAJOR): region + frequency arms | Frequency arm KILLED (wrong comparator), region arm strengthened | **RESCOPED — reader-2 wins the kill; the region arm + the attachment-contract clause survive at MAJOR (latent)** | My handmark.js read: the producer's own **boil displacement** — the true analogue — is `fractalNoise` at `baseFrequency="0.012·roughness"`, numOctaves 2; local 0.015 is in-family, HIGHER if anything. D-8's "producer's own grain sits at 0.65 … three orders of magnitude closer to stroke scale" compares a background-tile grain frequency to a displacement frequency — category error; the arm dies as published. What survives, corroborated on evidence D never had: handmark's filter region is `-40%/-60%/180%/220%` against local `-5%/110%` — the producer budgets 6-12× the bleed for the same effect class, strengthening the 1.4px-bleed < 1.5px-displacement clipping arithmetic (which I accept as reader-verified against AppHeader/PaperView). D-8's richest clause — the same constants are right on the 200-unit viewBox attachment and wrong on the HTML button, and no attachment contract exists — is carried; the one-wavelength "coherent drift" observation folds into missed find M-3, where the sharper mechanism lives. |
| **R-3 · D-2's cure** | Confirms D-2 MAJOR, corrects D's "identical against BOTH artifacts" over-claim (C's precision adopted) | Same, plus missed find M-1: the prescribed cure is broken at the pin | **Both adopted; M-1 CONFIRMED by this seat's own decode + algebra** | My decode: both paper.css `@utility` URIs are byte-identical — `feTurbulence 0.65/4/stitch → saturate 0 → feBlend mode='multiply' in2='SourceGraphic'` applied to `<rect width='100%' height='100%' filter='url(#n)' opacity='1'/>` with **no `fill`** → SVG initial fill = black → premultiplied multiply with (cb=0, qb=1) gives cr=0, qr=1 for every pixel: **flat opaque black, zero texture**. The token URIs omit feBlend and bake at `opacity='0.04'`/`0.06` — which is why `.paper-texture` (App.vue:24) renders grain and the utilities cannot. D-2's duplication verdict survives (the recipe IS the producer family's, provenance comment included); D's cure ("use `@utility paper-grain-overlay`") prescribes a broken producer recipe. Static-certain; one screenshot rides SS-13 as relay evidence. |
| **R-4 · D-11 grade** | INFO (downgrade: identical 0.001 terminals read as a deliberate settle frame) | MINOR ("arithmetic certain") | **INFO** | Arithmetic is certain (both readers re-ran it; magnitudes uplift +0.001 on indices 1-6, index 7 unchanged) — but defect-hood is the unproven part: BOTH series terminating at exactly 0.001 is at least as consistent with a shared settle frame as with drift, and the percept ("stutter") is SS-13. An undocumented irregularity in the file's only spec-free constants: INFO. Reader-2's MINOR preserved as dissent. |
| **R-5 · L-1's fourth net** | KILLED (stale dist; banked twice) | Includes `grep -olF 'url(#' dist/assets/*` → 0 in its own confirmed proof cell | **Reader-1 wins; the net is void** | fr-AppHeader.md:10 (re-read): dist is 2026-06-12-stale, pre-bump; the STALE-DIST law is banked. A zero over stale bytes proves nothing about HEAD. L-1 stands on its three live nets (source sweep, index.html/public, full-history pickaxe — the last reproduced by both readers to the commit). Reader-2's use of the dist grep is recorded as a method lapse in K-14. |
| **R-6 · boil-can't-boil grade** | MAJOR (latent, "on the footing D-4 was graded") | MINOR | **MAJOR (latent)** — see missed find M-3 | The row is the decision input F.W3/W4's delete-vs-rewire ruling most needs: the animated register **cannot produce the register it names** (no `seed` variation anywhere — my whole-file read confirms `seed` absent from all four filters — plus the un-centred `type="turbulence"` DC bias), so REWIRE-as-is is not risky but pointless. Same footing as D-4: mechanism spec-certain, magnitude SS-13. Reader-2's MINOR preserved as dissent. |
| **R-7 · D-14** | Mechanism KILLED (single static mount; HMR replaces in place; producer ids sandboxed in their data-URI documents); INFO residue | CONFIRMED MINOR (un-namespaced document-global ids) | **Reader-1 wins — KILLED as a mechanism, INFO as convention** | My App.vue whole read: exactly one `<SvgFilters />`, static position, no v-if/v-for/`<component>`. Vue HMR re-renders in place. The hazard is counterfactual in this tree; the id-namespacing observation survives as INFO. D/S-4's "resilient under the duplicate-id hazard" premise falls with it (the scoped lookup stays correct practice — S-7). |
| **R-8 · D-9 vs S8** | Kills the frame-count half; notes the interval half (150/160/125, LCM 19.2s) "survives intact" as a defect | Kills the same half (K-11); credits the SAME 150/160 fact as deliberate de-sync craft (its S8) | **Split the fact**: the 10ms offset **between the two local clocks** is craft (S-8 stands); the shared departure from the producer's 125ms default is an unexplained deviation — D-9 RESCOPED to INFO | My reads: signature default is 4 frames (0.4.1 :110-112, unchanged at v0.9.2/v0.11.2), HandMark documents 3 — the producers disagree, so no house frame count exists (K-4). 125ms is genuinely stated twice; neither local clock uses it and no comment explains why. Both readers' positions are each half-right about the same two constants. |
| **R-9 · D-5 grade** | MAJOR (latent) | MINOR (latent) | **MINOR (latent)** | Certain and design-load-bearing (dual-theme app; producer inverts in three places; v7.0.0 writes the physics — "screen lifts (NOT soft-light, which collapses to identity on the near-black pole", my git-show), but it is one attribute on a dead surface whose weight is delete-evidence. Reader-1's MAJOR preserved as dissent; if F.W3/W4 rules REWIRE, this row re-grades MAJOR automatically. |

## Adjudicated defect roster

Folds first (identity guard — one identity, original ids for life, never re-booked):

- **D-1 / L-1 / C-B1 — FOLDS BY REFERENCE → banked fr-App.md:55** (L-5/C-5 dead-reference half, **MAJOR**, → **F.W3/W4**). All four filter ids orphaned; two consumers removed 65c1565 (2026-03-06, "Paper: CM font title (no boil)" in the body) / e9c7bc4 (2026-03-08); two born dead (pickaxe → 0 commits, all history). Re-derived by both readers and re-swept by this seat (`url(#` → 0). BLOCKER re-books refused (K-11); census correction stays 12 → **8** (K-12); the dist net struck (R-5). The delete-vs-rewire ruling this fold feeds now has the M-3/D-4/D-5/M-4 evidence set below: **the filters are not merely unwired — they are wrong**, which is this corpus's genuine contribution to the banked row.
- **D-6 / L-3 / C-m1 — FOLDS BY REFERENCE → banked fr-App.md:64** (m-1 primary clause, **MINOR**, → **F.W3/W4**), rescoped per L-15's correct narrowing: only the reverse flip (reduce enabled after enrolment) survives; the second-order clause is dead at banked fr-App.md:99 (K-1). Layer-2 is cured at the corrected F.W1 floor — v0.9.2's reactive-PRM MQL listener hard-stops on flip (my git-show).
- **D §3 U-2 — FOLDS BY REFERENCE → banked fr-App.md:46** (B-2, **MAJOR**, → **F.W1** with the P0 CSS-class-census gate). Re-proven at the tag by both readers per BASELINE LAW; nothing new to book. Rider from R-3: the B-2 cure decision must NOT prescribe the 4.0.0 `@utility` recipes (flat-black, M-1); the only working producer artifact at the pin is the token/`.paper-texture` form.
- **L-8 + L-15 — FOLD BY REFERENCE → banked fr-App.md:74** (L-8, **MINOR**, → **F.W3/W4**) — **this seat's identity catch**: the banked row already covers BOTH the per-frame `querySelector` ("re-runs querySelector every frame with the mount hook sitting right there") AND the redundant local PRM gates ("re-implements the PRM gate the producer owns at vue.ts:129, making the local :24/:36 guards unreachable"). Reader-2 confirmed both as fresh rows; neither reader folded them.
- **C-M4 (corpus-contradiction half) — FOLDS BY REFERENCE → banked fr-FSE.md:98** (L-i3/C-18.1, → **F.W1**): "pencil-boil = 3 import sites / 3 files; the '4' was a loose-grep comment artifact at SvgFilters.vue:19" — already banked, already routed. Reader-2 re-booked it fresh (MINOR); reader-1 cited the bank in passing. The symbol-count refinement (5 symbols) rides the banked row.
- **C-i2 — FOLDS BY REFERENCE → banked fr-AnimationControls C-1 via fr-FSE.md:99** (C-16, INFO-grade peer-graph inconsistency, → **F.W1**). Verified live by this seat (installed glass-ui 4.0.0 peers value.js `^0.10.0 || ^0.11.0`; 0.13.0 installed). This file holds no pin and exercises none.

Component rows (id · severity · claim · proof · terminal disposition):

- **L-2 (merged: + reader-1 missed #3 + reader-2 missed #5) — MAJOR.** pencil-boil 0.4.1's scheduler resurrects an empty rAF loop on tab-return whenever the subscriber set is empty — reachable today ONLY for PRM users (the a11y affordance is the entry condition), and **after the booked SvgFilters deletion, for every user**, because the visibilitychange listener is a module-scope side effect behind a false `sideEffects: false` manifest, still evaluated by the two surviving barrel imports (dev-certain; prod bundler-contingent — the celebrated visibility-pause itself ships at the bundler's discretion). Full mechanism re-derived by this seat (R-1); registry-swept unbanked by both readers, concordant. **→ F.W1**: the bump to the corrected floor `^0.9.2` cures both mechanisms upstream (guarded resume + reactive PRM, both verified at the tag by this seat). **Rider on F.W3/W4**: deleting SvgFilters does NOT discharge this row — challenge-L §7's and challenge-C's blast-radius claims are corrected here. The false-manifest note persists at 0.9.2 (`sideEffects: false` unchanged, my git-show) but becomes harmless-if-dropped once resume is guarded; one line in the F.W1 relay to the pencil-boil producer.
- **L-4 / D-15 / C-M3 — MAJOR.** Four unlogged silent returns (:24/:28/:36/:40), no vitest, zero e2e references (8 specs, my ls + both readers' greps), no DEV assert — and the failure mode demonstrably fired for five months (65c1565 deleted the last consumer; `applyBoilFrame` kept "succeeding"). The reduced-motion exit and the structurally-broken exit are indistinguishable to every observer. Adjacency, not identity: banked fr-App.md:81 (MG-θ) books the missing **gate**; this books the missing **observer**. **→ F.W3/W4** (and it is the proof-of-harm for lane-frontend:646's vitest P3, per challenge-L §6 row 8 — carried).
- **D-10 — MAJOR.** Three of four filter comments assert consumers that do not exist (:67 "main heading" — PaperView h1 carries no filter; :93-94 "dark mode toggle" — DarkModeToggle/FourierMorphSvg carry none; :148 "canvas/visualization area" — none exists), and the comments are the file's entire specification (DESIGN.md silent per D-21, e2e silent per L-4). Both readers confirmed against live reads. **→ F.W3/W4** (delete kills them; rewire must first make them true).
- **D-2 / C-M2 (duplication half) — MAJOR, cure struck.** `#paper-grain` (:129-145) is parameter-identical to the producer's grain family (0.65/4/stitch → saturate 0 → multiply), whose provenance comment names fourier as the origin (scale-paper.css:73-79, reader-verified verbatim). C's artifact precision adopted over D's conflation ("only the composite differs"); the feBlend operand-order difference is immaterial (multiply symmetric — reader-2's note, correct). **The axis's cure is dead at the pin (R-3/M-1)**. **→ F.W3/W4** (the duplication dies with the file; the register question routes to F.W1 alongside B-2).
- **D-4 — MAJOR (latent).** Both grain filters apply noise at full amplitude — no attenuation primitive anywhere in :121-175 (my read) — against a producer that attenuates three independent ways at the pin (token rect `opacity='0.04'`, utility `opacity: var(--glass-grain-opacity)` 0.025/0.045) and bakes it into the filter at the uplift baseline (`feFuncA slope='0.42' intercept='0.12'`, my git-show at v7.0.0:95). Magnitude/WCAG → SS-13. **→ F.W3/W4** (the sharpest single piece of delete-evidence: rewiring as-authored harms legibility).
- **D-8 — RESCOPED MAJOR (latent), per R-2.** Region arm survives: ±1.5px displacement vs a 1.4px bleed on the 28px masthead box clips at the region edge — now corroborated by the producer's own 6-12× larger boil regions (my handmark read). The attachment-contract clause survives: the identical constants are good on the 200-unit viewBox and wrong on the HTML button, and the component ships no contract. Frequency arm KILLED (K-2). **→ F.W3/W4**; CM-hairline break-up → SS-13.
- **D §3 U-3 — MAJOR.** The feTurbulence paper idiom was retired as primary at glass-ui 7.0.0 after two recorded owner rejections ("disgusting metallic" — verbatim at the tag, my git-show), with the root cause named (grey `saturate=0` tooth borrowing all warmth from the substrate); SvgFilters' grain filters are the retired recipe exactly — grey, warmthless, unattenuated. The design-conformance half of the uplift; unbanked (fr-App banks only the class-break half). **→ F.W1** (the uplift must not carry the twice-rejected register forward under any rewire ruling).
- **D-5 — MINOR (latent, per R-9).** `mode="multiply"` hardcoded on both grain filters in a first-class dual-theme app; producer inverts in three places and v7.0.0 writes the physics. Re-grades MAJOR automatically if F.W3/W4 rules REWIRE. **→ F.W3/W4.**
- **L-6 / D-16 — MINOR.** Six duplicated declarations realising one parameterised unit; the apply functions are identical modulo three tokens (reader-2's token diff, adopted); the pair has already drifted — **D-12 / L-13 / C-m3 (INFO) folds in as the drift evidence**: three inert `result` attributes, with `result="grained"` on canvas-grain's terminal primitive and not on its structural twin. **→ F.W3/W4** (dissolves on delete).
- **L-9 — MINOR.** The `as SVGFETurbulenceElement | null` casts are unchecked, unlock nothing (`setAttribute` is on `Element`; the members the cast suggests are readonly `SVGAnimatedNumber`), and actively mislead in the one file with no tests. Both readers concordant. **→ F.W3/W4.**
- **D-13 — RESCOPED MINOR.** Two texture scales (0.65/4 vs 0.8/3) where the design system defines one — RESCOPED by M-4: under `stitchTiles="stitch"` in objectBoundingBox geometry **neither constant is the delivered frequency**, so the two-materials argument is made against numbers the renderer does not use; what survives is "two authored scales, N delivered scales, zero tokens, zero notes". **→ F.W3/W4.**
- **D-21 — MINOR.** The design language of record (web/DESIGN.md, 33 lines — my wc) never mentions boil/grain/texture/filter/SvgFilters; the A.W2 abrogation wave swept styles/ not components/. Unbanked, correctly D's own. **→ F.W3/W4** (on delete, the doc needs no row; the sweep-gap observation is method).
- **L-11 / C-m5 / D-4 (alpha half) — MINOR.** `saturate` has an identity alpha row (SVG 1.1 §15.10), so turbulence's noisy alpha reaches `feBlend` unflattened; the producer sidesteps structurally (opaque rect). Spec-certain; per-pixel consequence → SS-13. **→ F.W3/W4.**
- **L-5 — RESCOPED INFO (method carry), per K-9.** The checkable half is exact (template :58-178: 16 native elements, 0 component callsites, 0 directives — reader-verified, spot-consistent with my read); the deriver-leaf half rests on a withdrawn artifact and cannot ground a component MAJOR. The extension is sound and carried: **F.W4 must derive `url(#id)` reference edges** for filter/mask/clipPath/gradient/pattern/marker — no import- or callsite-graph can represent them, which is the blindness this 178-line survivor proves. **→ F.W4 (method amendment).**
- **D-9 — RESCOPED INFO, per R-8.** Interval half only: both clocks deviate from the producer's stated 125ms with no comment; the frame-count half is dead (no house frame count exists — the producers disagree 4 vs 3, my reads at three tags). The 10ms mutual offset is credited at S-8, not booked here. **→ F.W3/W4.**
- **D-11 — INFO, per R-4.** Index-7 pattern break, arithmetic certain, intent undecidable statically (shared 0.001 terminals read as plausibly deliberate); percept → SS-13. **→ F.W3/W4.**
- **D-14 — INFO residue, per R-7.** Document-global un-namespaced ids as a convention note; the duplicate-mount mechanism is killed. **→ F.W3/W4.**
- **D-17 / L-10 / C-m2 — INFO.** The onMounted+rAF prime writes values byte-identical to the static markup on every mount (frame 0 offsets are 0; first tick cannot advance — reader-2's :44/:50 walk, consistent with my read). **→ F.W3/W4.**
- **D-18 / L-14 — INFO.** `frame % offsets.length` re-applies a modulus the producer guarantees with the identical divisor (:50 at 0.4.1, my read). **→ F.W3/W4.**
- **D-19 — INFO.** SVG defaults restated on two filters, omitted on two; the sibling declares `xmlns`, this file does not. **→ F.W3/W4.**
- **C-m4 — INFO.** An SSR guard (:8) in an SPA with no SSR path (reader-1's live ls; build is `vue-tsc -b && vite build`). **→ F.W3/W4.**
- **C-S3 — INFO (observation, carried).** The pencil-boil pin is the only `@mkbabb` dep in exact peer-compliance at HEAD (glass-ui 4.0.0 peers `^0.4.1`, installed 0.4.1 — my node read); the gap is forward-only. The forward target it was measured against is corrected by M-2. **→ F.W1** (context for the bump).

## Killed-claims register

1. **K-1 · the `^0.11.2`-at-7.0.0 version fact — REFUTED AT THE TAG (all three axes + the corpus + two banked rows).** My git-show: glass-ui v7.0.0 peers `"@mkbabb/pencil-boil": "^0.9.2"` (line 542; devDep pin `0.9.2` :579); `^0.11.2` first exists at v8.0.0. challenge-L §6 row 5 certified "Version facts exact"; challenge-D §3 U-1 and challenge-C-M4 carry the same cell; lane-frontend.md:60/:481/:636 and CENSUS-2026-08-03.md:105/:185/:220 are the source; all inherited without a git-show at the adopted tag — the K-12 stale-baseline defect reproduced across five documents. Distance is FIVE minors (0.4.1 → 0.9.2), not seven. Both readers killed this concordantly; this seat re-proved it. See M-2 for the banked-row consequences.
2. **K-2 · D-3's BLOCKER grade — its own falsifier is met.** HandMark is an ink-mark generator (`HandShape` union + prop surface, my read of the installed d.ts); its displacement filter warps the mark's own path, never host glyphs (dist/handmark.js, my grep: `feDisplacementMap in="SourceGraphic"` inside the mark's SVG). Per D-3's published rule, BLOCKER → MAJOR. Consequence: the D axis's 3 BLOCKERs reduce to 1 (D-1), itself banked at MAJOR — the axis tally "BLOCKER 3" nets to 0 new.
3. **K-3 · D's glass-ui "7.0.0" coordinates — STALE-BASELINE.** The /Users/mkbabb/Programming/glass-ui worktree is version 8.0.0 (my node read); `./handmark` sits at :390 at the v7.0.0 tag vs :355 in today's tree (my greps) — D's ":357" was a HEAD-tree read back-projected onto the 7.0.0 label. Substance re-proven at the tag survives; D's citations do not.
4. **K-4 · D-9's frame-count half.** `useLineBoil`'s signature default is **4** (:110-112 at 0.4.1, unchanged at v0.9.2/v0.11.2 — my reads); HandMark's `boilFrames` default is 3. The producers disagree; "the house tempo … stated twice" is true only of 125ms; "nearly triple the 3-frame default" has no stable denominator.
5. **K-5 · D-14's mechanism (and D/S-4's premise).** One static mount (App.vue whole, my read); Vue HMR replaces in place; producer ids are sandboxed inside their own data-URI documents. The duplicate-id hazard is unreachable in this tree.
6. **K-6 · L-1's fourth net.** The dist grep proves nothing (STALE-DIST law, banked twice). L-1 stands on its other three nets.
7. **K-7 · D/S-3 + C/S-2's numeric proof — FABRICATED EXAMPLE.** My node run: `0.015 + 0.002` is exactly `0.017`. Exactly TWO of sixteen sums differ from the quantised string: `0.015−0.001 → "0.013999999999999999"` and `0.02−0.002 → "0.018000000000000002"`. C's "several are not [exact]" over-counts (1 per filter); L's S-5 cites `0.014000000000000002` where the tree yields `…3999999999999999`; reader-1's "18-character attribute" is itself loose (the strings are 20 and 19 chars). The quantisation credit survives on the two real cases (S-3).
8. **K-8 · L-7's prescribed cure.** `:base-frequency` on a native SVG element writes the literal kebab attribute (Vue camelizes component props, never native attributes; SVG names are case-sensitive) — a silent no-op shipped into the one file with no tests. The correct SFC spelling is `:baseFrequency`. The row's line-count reasoning survives; the published cure would have been a regression.
9. **K-9 · L-5's MAJOR grade.** The load-bearing half is asserted from a withdrawn artifact (L's own falsifier concedes it); a component MAJOR cannot rest on an unfalsifiable inference. Demoted to method carry (roster row).
10. **K-10 · D/S-1's comparative ("more disciplined than the pinned producer").** The pinned producer's displacement filter — the analogous artifact — sets `color-interpolation-filters="sRGB"` (my handmark.js grep). sRGB is the house convention on this filter class, inherited not exceeded. The underlying credit survives (S-2).
11. **K-11 · the BLOCKER re-book of the dead-reference identity (all three axes' §1 headline).** Banked at a RULED severity (fr-App.md:55, MAJOR, adjudicated with the census correction). Severity re-books against ruled grades are refused; the identity FOLDS.
12. **K-12 · L §6 row 2's "12 → 11" SVG-surface arithmetic.** Superseded by the banked correction "12 → **8** live" (fr-App.md:55, re-read by this seat). L subtracted one from a denominator already found four-over.
13. **K-13 · C-M4's declared UNPROVEN ("pencil-boil 0.11.2 is not on disk").** The producer repo is on disk and was challenge-L's own comparator; the break degree at SvgFilters.vue:3/:20-21 is **ZERO** at either candidate floor (signature and destructured return byte-compatible at v0.9.2 and v0.11.2 — reader-2's git-shows, spot-confirmed at v0.9.2 by this seat). Decidable statically; not decided.
14. **K-14 · reader-level corrections (this seat's own).** (a) Reader-2 fresh-booked THREE banked identities — C-M4's census half (fr-FSE:98), L-8 and L-15 (fr-App:74) — the anti-rename law exists for exactly this. (b) Reader-2's L-1 proof cell leans on the void dist net (R-5). (c) Reader-2's "benign in practice" clause on the surviving listener contradicts its own confirmed L-2 mechanism (R-1). (d) Reader-1's "18-character" detail in an otherwise-correct superlative (K-7). None of these unseats the affected verdicts; all are recorded so the fleet's provenance stays honest.

## Missed finds — banked by this seat

- **M-1 — MAJOR, GLASS-OWNED (reader-2; CONFIRMED by this seat's decode + algebra).** glass-ui 4.0.0's `@utility paper-grain-overlay` and `@utility paper-underpaint` paint a **uniform opaque black tile** — the fill-less rect is black, and `feBlend multiply` against an opaque black SourceGraphic annihilates the noise (cr=0, qr=1 per pixel; my decode shows both URIs byte-identical). The working sibling proves the mechanism by contrast: the token URIs omit feBlend and render grey noise at 0.04/0.06 — which is why `.paper-texture` works and the utilities cannot. Consequences: D-2's cure and D §3 U-2's fallback both prescribe a broken recipe at the pin (R-3). **→ BH relay to the active glass-ui inbox per the standing owner edict** (with the one-screenshot SS-13 witness request), **+ F.W1 note** (the B-2 cure decision must route around the utilities). Registry-swept: unbanked (fr-App:11/:46 enumerate the roster only).
- **M-2 — MAJOR (both readers; re-proven at the tags by this seat).** The F.W1 pencil-boil target is **`^0.9.2`, not `^0.11.2`** — and the adjudicated registry itself carries the wrong figure: fr-FSE.md:100 (C-18.2) asserts 7.0.0 peers `^0.11.2` (true at 8.0.0, false at 7.0.0 — that seat confirmed HEAD and back-projected), while kf-HeroAurora.md:9 transcribes an installed 7.0.0's `^0.9.2` correctly, and fr-FourierMorphSvg.md:54 (FM-22) names "the F.W1 pencil-boil `0.4.1 → ^0.11.2` migration surface" — **three banked rows, one contradiction**. Constructive half: both cures the corpus wants (guarded resume, reactive PRM) land at v0.9.2 (my git-show), the signature is unchanged, so the bump is a no-break at this site and every "7 minors"-derived risk weight overstates. C-18.2's conclusion survives more tightly (producer 0.12.0 excluded). **→ F.W1** (correct the target; registry-correction note against fr-FSE:100 and FM-22 — dated evidence is never rewritten; this row is the correction of record).
- **M-3 — MAJOR (latent), per R-6 (both readers, merged; grades ruled).** The boil mechanism cannot produce a boil: all four `feTurbulence` leave `seed` at default (absent from the whole file — my read) and the only per-frame mutation rescales ONE fixed lattice (`baseFrequency` writes at :32/:44) — a breathing register, not a hand redrawing a line; compounded by `type="turbulence"` on both boil filters (:78/:105), whose non-centred output gives feDisplacementMap a permanent ~−0.6px DC bias that never animates away. The producer's answer is the inverse on both counts: `fractalNoise` (centred) + per-mark `seed` + N distinct frames (my handmark.js read). Neither axis read the two attributes that define the register. **→ F.W3/W4** (with D-4/D-5/M-4, the delete-evidence set); percept magnitude → SS-13.
- **M-4 — MINOR (latent) (both readers, merged).** `stitchTiles="stitch"` on both grain filters (:133/:161) is transplanted from the producer's fixed-raster tiles (60×60 / 200×200, where it is load-bearing) into objectBoundingBox per-element regions, where nothing tiles and the UA silently snaps the base frequency **per element size** — one material rendering at a different tooth on every surface, and neither authored constant is the delivered frequency (rescopes D-13). Spec mechanism certain (SVG 1.1 §15.20); visible spread → SS-13. **→ F.W3/W4.**
- **M-5 — MINOR (reader-1).** The file's only mechanism-explaining comment names the wrong actor (:93-94 "useLineBoil oscillates baseFrequency" — it returns `{currentFrame, start, stop}` and touches no attribute; the component oscillates, :35-45 — my reads at both ends), and :19 is a changelog line, not a contract — the same line whose loose grep produced the banked "4 sites" miscount (fr-FSE:98). D-10's species on the axis D-10 does not cover. **→ F.W3/W4.**
- **M-6 — INFO (reader-1).** No token seam: the register lives as ten hardcoded magic numbers unreachable from CSS, in an app whose producer expresses texture as tokens and whose DESIGN.md §Token Overrides is live and silent. The third face beside C-M3 (no JS contract) and D-21 (no doc row). **→ F.W3/W4** (rider; on delete it needs nothing — recorded for any rewire ruling).
- **M-7 — INFO, method (reader-2).** challenge-D and challenge-C cite the installed pencil-boil vue.ts with a systematic 2-5-line offset throughout (reader-2's ground-truth table, spot-confirmed by this seat at :96-99/:110-113/:114/:129); challenge-L's coordinates are byte-true at both ends. Two of three axes' library proof cells are not re-runnable as published. **→ NO-WAVE-OWNER** (fleet provenance note for the SS-3/SS-4 spec authoring).

## Superlatives — with provenance

- **S-1 (L/S-4 · D/S-2 · both readers · seat-confirmed in the whole-file read).** The defs host is inert on four axes at once (:59-65): `width="0" height="0"` + `position:absolute` + `pointer-events:none` + `aria-hidden="true"` — and decisively NOT `display:none`, the shortcut that breaks filter-reference resolution in several engines.
- **S-2 (three axes · both readers · corrected by K-10).** `color-interpolation-filters="sRGB"` on 4 of 4 filters (:75/:102/:127/:155) against a linearRGB default — including on the two filters never wired to anything: the house convention correctly inherited and uniformly applied on an unobserved surface. The "more disciplined than the producer" framing is dead; the discipline is real.
- **S-3 (three axes · corrected by K-7, seat's own run).** The `Math.round(·*10000)/10000` quantisation (:31/:43) IS load-bearing — on exactly two of sixteen sums, which is enough: without it those frames write 19-20-significant-digit attributes several times a second and defeat serialisation-keyed filter caching. The credit survives its fabricated headline examples.
- **S-4 (both readers).** The clock choice: two animations share pencil-boil's singleton rAF scheduler (one callback, visibility-paused, PRM-gated at the producer), with the rationale recorded at :19 — set against ConvergencePlot's ungated independent rAF (lane-frontend:644), this component chose the better clock, for an effect that draws nothing.
- **S-5 (reader-2).** The teardown contract is complete and correctly bound (`onUnmounted` registered synchronously in setup; last-out disarm at :141 → :63-73); the one narrowing stands — the :51 one-shot rAF handle is never cancelled, harmless only because the template ref nulls on unmount.
- **S-6 (D/S-6 · reader-2, recomputed).** Both filter regions are arithmetically symmetric on both axes (−5→+105, −10→+110); the common bleed-one-side-clip-the-other error is absent.
- **S-7 (D/S-4 · premise corrected per R-7).** The scoped `svgRef.value.querySelector` is the right form on its own terms — each instance addresses its own defs — even with the duplicate-id hazard killed.
- **S-8 (reader-2's S8 · ruled at R-8).** The 150/160ms pair is a deliberate de-sync — equal 8-frame cycles beating at 19.2s so two marks in one register never lock — the mark of someone who watched it run. Credited for the offset; the shared 125ms-default departure stays booked at D-9.
- **Corpus superlatives (adjudicated):** challenge-L is the only axis whose library coordinates are byte-true (M-7) and the only one that read the producer repo rather than the consumer's comment about it — the banked fr-App:123 (S-8) discipline, and it is what found L-2, the sharpest unbanked mechanism in the corpus. challenge-C's artifact separation in C-M2 ("only the composite differs") is what let the duplication verdict survive careful reading. challenge-D's D-8 is the corpus's only design constant judged against a measured target box — exemplary arithmetic even though its frequency comparator died. The git provenance work (L §1 / C §1) produced a dated death certificate for dead code — added 01eac1e/555cbd3, removed 65c1565/e9c7bc4, two ids born dead over all history — which is precisely what makes the delete-vs-rewire ruling decidable.

## UNPROVEN-NEEDS-LIVE — SS-13 residue (visual-audit inputs)

1. `#paper-grain` over a text block vs `--background`: rendered ΔL*/WCAG delta (D-4 magnitude; absence-of-attenuation is static-certain).
2. Computer Modern hairline fragmentation under ±1.5px at the 52px h1 (D-8 legibility arm).
3. Un-flattened turbulence alpha perceptibility at 0.65/0.8 (L-11/C-m5; graph semantics static-certain).
4. Breathing-vs-boil percept: baseFrequency-modulated vs seed-re-rolled displacement, two side-by-side renders (M-3 magnitude; the DC-bias mean per octave count is engine-dependent).
5. Per-element grain-frequency snap under `stitch` across two differently-sized surfaces (M-4 magnitude).
6. µs cost of the two `querySelector`+`setAttribute` pairs at ~13Hz (C-M1's declared live half).
7. **M-1 witness**: one screenshot of any element carrying `paper-grain-overlay`/`paper-underpaint` at 4.0.0 — flat vs grained settles it; attaches to the BH relay.
8. L-2's measured empty-loop cost (µs/frame, battery, compositor wakes) — reader-2's grade decider; the mechanism needs no browser.
9. D-11 percept: settle frame vs stutter.
10. B-2's perceptual weight — ALREADY BOOKED as fr-App.md:135 SS-13 item 9; pointer only, not re-booked.
11. **Bundler-contingent, not live-contingent**: whether `./vue` (listener + pause + trap) survives Rollup tree-shaking after the F.W3/W4 deletion — one `vite build` + chunk grep; no browser.
12. D-3 residual: whether HandMark's `shape:"path"` escape hatch can approximate a glyph boil by another route (unrun; would not restore the BLOCKER either way).

## Closing verdict

**ADJUDICATED.** The dead-filter identity stays where the registry put it — banked MAJOR at fr-App.md:55, all BLOCKER re-books refused — and this corpus's real yield is everything around it: the filters are proven **wrong, not merely unwired** (no boil mechanism, DC-biased, unattenuated, single-theme, size-dependent — the delete-evidence set for F.W3/W4), the F.W1 pencil-boil floor is **corrected to `^0.9.2` against three drifted documents and one banked contradiction**, pencil-boil 0.4.1's PRM-triggered empty-loop resume is booked MAJOR with its post-deletion widening (the booked remedy's blast radius was wrong), and glass-ui 4.0.0's grain utilities are proven to paint flat black (GLASS-OWNED, BH relay). 29 rows survive (6 folds by reference — three of them this seat's identity catches) · 14 killed · 7 missed finds banked · 12 SS-13 items. Original challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
