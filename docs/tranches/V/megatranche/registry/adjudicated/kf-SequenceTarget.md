Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SequenceTarget; receipt read from this seat's own system prompt, not inherited).

# kf-SequenceTarget — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), `origin/master` `81a56990`; **worktree ≡ origin/master on every `demo/scenes/sequence/` surface** (my `git diff origin/master --stat -- demo/scenes/sequence/` this session: empty); the two dirty rows are `package.json`/`package-lock.json`, and that mismatch is the corpus's sole BLOCKER (see K-1). · **Registry tree** value.js `tranche-u` · **Corpus**: two fresh hostile re-reader returns over `audit/kf-components/SequenceTarget/challenge-{D-design,L-library,C-consumption}.md` (all three axes `claude-opus-5[1m]`; D = 26 defects/1 BLOCKER, L = 14/1 BLOCKER, C = 13/1 BLOCKER) — **reader-A** (design-and-usage: 31 confirmed · 9 kills · 6 misses · 7 unproven · 7 superlatives) and **reader-B** (library-and-code: 36 confirmed · 8 kills · 5 misses · 10 unproven · 8 superlatives). Both returns consumed whole; all three challenge files re-read whole by this seat.

**Method (incredulity).** Every inter-reader disagreement, every kill, every claimed-new miss, and every cell where a verdict is load-bearing was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session (all read-only): `src/animation/engine/play-lifecycle.ts:360-372` (the THROW — decider for K-3); `git rev-parse` / `git show origin/master:package.json` (:77 glass-ui `7.0.0` EXACT; :37 the `check` script with `proof:structure`) / `git show origin/master:package-lock.json` (:19 glass-ui locked) / `git show HEAD:package.json` (glass-ui `6.0.0`, value.js `^3.1.0`) / `git status --short` / `git diff HEAD -- demo/scenes/sequence/SequenceTarget.vue` (the Glass-7 migration diff verbatim: `MetricBadge`→`Metric` with `label-position="inline"` + `color="var(--ball-tone,…)"` DROPPED; `variant="outline"`→`emphasis="secondary"` — deciders for ST-3/ST-10/K-1); SequenceTarget.vue :8-12 (the "master accent" comment), :94-110 (`class="seq-handle"` bare, the aria quintet), :161-171 + :203-218 (`rowEls: (HTMLElement|null)[]` + the `computed<HTMLElement|null>` — decider for ST-5), :245-251 (the typed egg); SequenceTarget.css :41-56 (plate + `--radius-card`), :155-176 (the focus rule + the **`proof:sequence-rows-draggable`** comment + `.reel-active`), transition census (`grep transition` → exactly :151); useSequenceDemo.ts :320-348 (`reseatRow`, no `isReeling` guard, the stale-product seek), :355-393 (`playReel` whole — 5 unretained `window.setTimeout`s, `let settled = 0` closure-local, `child.managed = false` → play → unconditional `= true` restore), :437-450 (`onScopeDispose` = `stopMirror() + sequence.stop()`); `orchestration/sequence/lifecycle.ts:55-83` (`play` sets `managed = true` per child; `stop` → `settleSequence`) + `transport.ts:265-280` (`settleSequence` clears `managed`, touches no child `_playback`) + `engine/animation.ts:125` (`managed: boolean = false` init); tsconfig.json (`noUncheckedIndexedAccess: true`, `include: ["src/","demo/"]`); repo greps: `sequence-rows-draggable` (CSS comment + docs prose ONLY — zero under `scripts/`, zero in any proof roster at either baseline), heading census (`<h1|<h2|<h3|role="region"` → EasingTarget/TimingFunctionPanel/EditorStartScreen/KeyboardShortcutsModal only), `grep focus demo/scenes/sequence/SequenceScrubber.vue` → **0** (decider for ST-1's relay limb); design-idioms.css :73-79 (the SINGLE-affordance `.focus-ring` contract verbatim) + :189-192 (`.readout-accent`); SequenceScrubber.vue :153-161 (`.scrub-ball { left: 0; margin-left: calc(var(--ball-size,36px)/-2) }` — centre-anchored, the N-4 corroborant); installed glass-ui 7.0.0: `utilities/a11y-overrides.css` whole head (the forced-colors focus list — ALL class-scoped: `.focus-ring/.interactive-item/.dock-*/.field-control/.input-pill`; the PRM allowlist), `components/button/styles.css` whole (`border: 0` base; `min-block-size: var(--button-size)` unconditional; `.button:not([data-tone="neutral"]) { border: 1px solid … }`; `[data-icon-only]`; `[data-loading] { cursor: progress }`), `button-B7c944jy.js` (python-extracted: `tone: { default: "neutral" }` · `emphasis: { default: "secondary" }` · `"data-tone": a.tone` · `"data-control-target": a.iconOnly ? "" : void 0` · `"aria-busy"/"data-loading": a.loading || void 0`), `components/button/Button.vue.d.ts` (`loading?`, `iconOnly?`, `tone?` — **no `variant`**), `components/metric/types.d.ts` whole (**no `color`, no `labelPosition`**; `MetricValue`/`MetricSize` as cited), `typography/utilities.css` (`text-mono-caption` carries `letter-spacing: var(--type-tracking-caps)` + uppercase), `tokens/scheme-motion.css` (`--type-tracking-caps: 0.1em` — the D-6 decider), `typography/scale.css` (caption clamp, admin-label 0.625rem, subheading 1.272rem, display-1/-3 clamps), `typography/semantic.css` (`text-caption` = body face + **italic**), `tokens/sizing.css` (`--control-h-md` = `max(calc(2.5rem*var(--ui-scale)), var(--control-floor))`; **zero** `pointer: coarse` — my `grep -c` = 0) + `tokens/light-dark.css` (the coarse arm: `--ui-scale: var(--ui-coarse-scale, 1.5); --control-floor: var(--touch-target, 2.75rem)`), `utilities/responsive.css` (`[data-control-target]` coarse floor), `theme/radius.css` (`--radius-card: var(--radius-2xl)`; `--radius-2xl: 1rem`), `tokens/{on-glass-fg,dark-arm,color-radius}.css` + `glass/ladder.css` (the `--muted-foreground → --on-glass-muted` remap and both arms' values); **this seat's own D-3 arithmetic** — node against the installed `@mkbabb/value.js` 4.0.0 dist (`parseCssColor` + `convertColor`, gamma-correct WCAG luminance, α-composite): light plate `mix(oklch(0.56 0.17 295) 5%, hsl(40 30% 98%))` → fg `hsl(30 26% 35%)` = **5.91:1 at α=1 → 3.82:1 at α=0.8 (3.92 at the 2% stop) — FAILS AA**; dark arm → **9.70:1 → 6.55:1 — PASSES**. Registry instruments: banked kf-SequencePlayhead r4 (whole, the scene home — N-1..N-20, K-1..K-33, ruling 1, SS-13 #1, §Pattern line 144), kf-SequenceAxis (SA-1/SA-5, the D-12 relay), kf-AnimationVisualizer KF-AV-15, kf-App KF-APP-4, kf-ControlsPaneWrapper D-M11, kf-CubeScene D-25/D-11/L-8-C-9, kf-CopyButton D-19-reversal + KF-CE-13 fold; novelty sweeps `reel-active|playReel` / `readout-accent` / `:loading` over all banked kf-*.md → **0 hits each**.

## Scoreboard

**52 adjudicated-confirmed identities — 41 rows booked here (12 MAJOR · 23 MINOR · 6 INFO) + 11 folds-by-identity to banked homes (independent confirmations recorded, zero re-bookings).** **Killed-claims register: 16 entries** — the corpus's ALL THREE BLOCKERS die (one falsified at every committed baseline, two as banked-grade re-contests), and the register catches both readers once each. **10 net-new rows admitted from the 11 filed misses (ST-1..ST-10) + 1 rider adopted into banked N-2 + 1 relay to the forthcoming kf-SequenceScrubber corpus** — the two readers' miss sets are fully disjoint (6 vs 5, zero overlap); every one re-derived at the bytes before admission. **9 superlatives** (one banked-idiom fold; one qualified by a new row). **11 UNPROVEN residues → SS-13.**

**Headline.** The corpus headlined three BLOCKERs and every one is dead: C-1's phantom-dependency claim is **falsified at every committed baseline** (origin/master declares glass-ui `7.0.0` exact and locked while the scene's own files are byte-identical to origin/master — the readers read a mutated manifest against unmutated sources, the classic one-baseline-short signature, and the census F-1 cell they folded had already been ruled FALSIFIED at kf-AmigaScene); D-1 and L-1 are the banked scene home's D-1/L-1/C-1 and N-2, where the BLOCKER grade was killed two generations ago (K-6) and the one-keypress `End` witness is already adopted. What survives instead is sharper than what was filed: a light-arm AA failure now pinned by **three concordant derivations including this seat's own product-converter run** (3.82:1 — and this is the component family whose ledger carries four wrong-byte events, so the third derivation is the story); an author-declared decorative overshoot cascade with **no PRM path** in a file that declares "MANDATORY PRM degrade" for its other decorative motion; a label column that **overflows at every viewport** once the utility's own `0.1em` caps-tracking is priced (the axis's "fits at 375" cell dead, and reader-B's re-derivation repeating the identical omission — caught here); and the Glass-6→7 migration seam yielding four of the ten admitted rows: a focus-ring fork justified by a **proof gate that exists nowhere on disk**, a `.reel-active` border half with **no border to color** (`border: 0` base + `data-tone="neutral"` default), a Metric that silently lost its master-tone binding while the comment three lines up still promises it, and a shipped `loading` prop whose one binding would close three filed defects at once.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave taxonomy (KF.W0/W4/W6/W9) with **NO-WAVE-OWNER** for the sequence scene-repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **C-1 BLOCKER (+ L-14's F-1 limb, D-16's F-1 rider): "glass-ui absent from package.json AND package-lock.json; the scene cannot build"** — both readers kill it | **KILL SUSTAINED (K-1) — falsified at every committed baseline; ≡ the already-FALSIFIED census F-1/SCH-1** | My git reads: `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` EXACT; `origin/master:package-lock.json:19` carries it resolved; installed = 7.0.0 = the pin; HEAD declares 6.0.0; the WORKTREE manifest alone omits it — and `git diff origin/master -- demo/scenes/sequence/` is EMPTY, so the corpus read origin/master content under a mutated manifest. The falsifier the axis itself named ("a glass-ui entry in package-lock.json") fires. Citation-inheritance event recorded: challenge-L:281's "I confirm F-1 against the tree" and challenge-C:36-52 both propagated a census RED cell ruled FALSIFIED at banked kf-AmigaScene (≡ SCH-1 → KF.W0 §B-12) days before either axis wrote. Surviving residue: C-8 (split specifiers, booked MINOR below). |
| 2 | **D-1 + L-1 filed as BLOCKERs; L-1 filed as a fresh booking** | **BOOKING/GRADE KILLS (K-2) ≡ banked D-1/L-1/C-1 (MAJOR, K-6) and N-2 (MAJOR, 5 derivations)** | The banked scene home's §Pattern line 144 names kf-SequenceTarget as a folding sibling by anticipation. Both mechanisms re-verified at the bytes this session (`gap: 0.5rem 0` at css:44; `reseatRow`'s in-place `entry.at` + the monotone `_duration`); both readers concordant on the folds; no new evidence accompanies either BLOCKER grade (the banked K-31 class governs). D-1's ruling-1 cure (`gap: 0.5rem var(--col-gap)`) carries; N-2's `End` witness was already adopted at the bank — never re-booked. |
| 3 | **L-2 grade: reader-A MINOR ("two-finger reachability is live-only") vs reader-B MAJOR ("novel wiring; family ≡ banked KF-AV-15")** | **MAJOR — B wins; A's caveat routed to SS-13** | Family consistency: banked KF-AV-15 (kf-AnimationVisualizer) books the IDENTICAL terminal consequence — `gestureSelectSuppression`'s module refcount stranded ≥1 → `user-select: none !important` on `html/body/*` for the session — at MAJOR, on the sibling `useDragCapture` seam. This is the `useDragScrub`-side instance and the demo's ONLY one-instance-many-press-targets wiring (Target:203-230; `dragging` a boolean at useDragScrub.ts:98; acquire unconditional :112-116; `pointerup` early-returns on `!dragging` :136-139). The code path is deterministic; only the two-thumb occurrence needs a device (SS-13). Cure is seam-level WITH KF-AV-15 (count or pointerId, both consumers), not a local patch. |
| 4 | **L-3/C-9 grade: reader-A MAJOR (10 function refs re-invoked per frame; contradicts a load-bearing invariant) vs reader-B MINOR (banked half folds; magnitude unproven)** | **MINOR — B wins; A's MAJOR preserved as dissent** | The prose-contradiction half is banked at MINOR (kf-SequencePlayhead D-5/L-8/C-4 + K-2 — the "no per-frame Vue work" clause is already dead there); grade-stability holds it. The net-new limb — both `demo.progress.value` unwraps in the PARENT's render scope (Target:21/:70) + Vue 3.5.35's unconditional function-ref invocation (runtime-core.cjs.js:1814-1815, verified by both readers against the installed dist) — is real but its COST is unproven, and a MAJOR cannot rest on an unproven magnitude. SS-13 carries the explicit kill condition: a render trace showing SequenceTarget re-rendering only on row mutation during playback kills the limb outright. |
| 5 | **D-6 arithmetic: reader-A prices the utility's caps-tracking (overflow at EVERY viewport) vs reader-B's 7×0.6em table (implies "fits" at 375)** | **A WINS — my own grep is the decider; B's cell carries the axis's own killed omission (K-10)** | Installed `typography/utilities.css`: `@utility text-mono-caption { … letter-spacing: var(--type-tracking-caps); text-transform: uppercase }`; `tokens/scheme-motion.css`: `--type-tracking-caps: 0.1em`. CSS letter-spacing lands on every glyph: 7 × 0.7em = 4.9em → **59.5px at 375px** (caption 12.15px) vs the fixed 52px track → +7.5px overflow at the NARROWEST viewport, ~70.5px at 1440. D's "break-even ~486px / fits (just)" cell is dead everywhere — and reader-B, who adjudicates inheritance in others, re-derived the table WITHOUT the tracking, reproducing the exact omission that killed the original cell. D-6 books MAJOR, strengthened. |
| 6 | **D-3 bytes: reader-A publishes 5.87→3.80 light / ≈6.8 dark; reader-B refuses all bytes (four wrong-byte events on this family) and routes numerics to SS-13** | **CONFIRMED MAJOR with this seat's OWN derivation — the wrong-byte curse is broken for this cell; B's refusal was right and is now moot** | My node run against the installed value.js converter (instruments above): light **5.91:1 at α=1 → 3.82:1 at α=0.8** (3.92 at the gradient's 2% stop) — FAILS AA at 12.15-14.38px/400 (no large-text exemption); dark **9.70 → 6.55:1 — PASSES**. Three concordant light-arm derivations now exist (the axis's 3.82, A's 3.80, mine); A's dark "≈6.8" is corrected to 6.55 (direction unchanged — recorded as a precision correction, not a kill, unlike banked K-30's materially-wrong byte). Residue narrowed to the two named movers (the `@container style(--glass-backdrop: light)` arm; `contrast-color()` support) + the composited backdrop — SS-13. |
| 7 | **D-7 grade: reader-A MINOR (scope broader here — the travellers carry state) vs reader-B INFO (≡ banked D-16, "MAJOR declined, K-31 class")** | **INFO FOLD — B wins on grade (banked D-16/KF-CE-13 is INFO); A's scope datum ADOPTED into the fold** | Grade-stability (banked K-31, third generation). But A's cell is right that this scene is the family's sharpest instance: the five travellers, rails, and playhead are `background-color`+`box-shadow` state carriers (all stripped in HCM) while the decorative washes are `background-image` (all kept) — adopted as the KF-CE-13 family's calibration datum. The interactive-focus HALF of the HCM story is NOT this row — it is net-new ST-1 (different mechanism: a forked ring, not decorative paint). |
| 8 | **The eleven filed misses (A×6, B×5 — fully disjoint sets)** | **10 ADMITTED (ST-1..ST-10) + 1 rider (B#5 → banked N-2) + 1 relay (A#1's scrubber limb → kf-SequenceScrubber)** | Every mechanism re-derived at the bytes this session (instruments above); registry novelty re-swept by this seat (`reel-active|playReel`/`readout-accent`/`:loading` → 0 banked hits; the forced-colors focus family checked against kf-CopyButton's KF-CB-32/KF-CE-13 and kf-CSSPasteDialog R-6 — different mechanisms, no prior row for a hand-reproduced ring losing the producer's HCM provision). Unlike the sibling generations' booking-kill epidemics (banked K-16/K-26), NONE of these eleven was pre-banked — the first corpus generation in this scene family to clear the sweep. Details per row below. |
| 9 | **L-11's kill (reader-B): the axis quoted `play-lifecycle.ts` as `warn(…)`; the live code THROWS** | **KILL SUSTAINED (K-3) — my read of :360-372: `throw new Error("Animation.play() called on a managed animation …")`** | The fabricated `warn` inverts the row's mechanism: `child.managed = false` is not warning-suppression but the call's mandatory precondition (without it `play()` rejects, and `void p.finally(…)` would surface an unhandled rejection — ST-8's posture). And `managed` IS Sequence-owned by protocol: `lifecycle.ts:57-61` sets it on play, `settleSequence` clears it, `animation.ts:125` inits false. The ownership concern survives as ST-7 (the RESTORE direction — the half the axis never opened). |
| 10 | **C-8 grade: reader-A's severity field says INFO while its own cell text says "the row is correctly MINOR"; reader-B MINOR** | **MINOR — the axis grade, B concordant; A's field/text self-contradiction recorded (K-15)** | The `/metric` subpath is FORCED (`Metric` absent from `dist/index.d.ts` — both readers' greps, concordant) which halves the row; the declined `./button`/`./card` subpaths keep it a real convention drift. No banked identity constrains it; the original C grade stands. |
| 11 | **Small-cell corrections both directions** | **RECORDED** | Against A: dark-arm 6.8 → 6.55 (ruling 6); the C-8 field slip (ruling 10). Against B: the D-6 tracking omission (ruling 5); B's D-11 "~30px band" inherited D's dead cell where A corrected it to ≥40px (the reel Button's `min-block-size` floor sets the band — adopted with C-2). Against A's coverage: no D-21 cell filed (B's adopted alone). Against B's coverage: no cell on D §4's two killed "checked and clean" rows beyond the button (A's outline-replacement kill stands alone and is decisive for ST-1). Credits: A's `data-control-target` limb (verified at the compiled chunk: emitted ONLY when `iconOnly`) and the fleet-precedent cure cite (AAP-19/AF-7/ATP-26/SFB-8); B's scrub-ball centre-anchor corroborant (Scrubber:155-161 — two anchoring conventions in one scene, adopted into the N-4 fold note); B's C-3 self-kill hunt (the lowercase-pathed `Slider` barrel re-export). |

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W4/W6/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the sequence scene-repair packet, extending the banked kf-SequencePlayhead packet). Anchors bind to worktree ≡ `origin/master`; re-resolve before any cure.

### Folds-by-identity (11 — independent confirmations recorded at the banked home, zero re-bookings)

- **D-1 (as filed here) ≡ banked kf-SequencePlayhead D-1/L-1/C-1** (MAJOR; BLOCKER killed at K-6; ruling-1 cure `gap: 0.5rem var(--col-gap)`; subgrid branch stays SS-13 #1 ONLY — K-29 is the standing warning against ruling it statically). Both readers concordant.
- **L-1 ≡ banked N-2** (MAJOR, 5 derivations; the `End` witness and ≈38% cell already adopted). **This corpus adds one rider (B miss #5, admitted): `sequence.seek(sequence.progress * sequence.duration)` at useSequenceDemo:346/:386 is algebraically `seek(_time)` routed through the very denominator `reseatRow` just staled — the expression's SHAPE hides the staleness at the call site.** Adopted into N-2's cell.
- **D-2/L-6 ≡ banked N-4** (MINOR; per-row offset table +12.8/+8.6/+4.5/+0.3/−3.8 already banked). New corroborant adopted: the sibling `.scrub-ball` is centre-anchored (`margin-left: calc(var(--ball-size)/-2)`, Scrubber:158) — two anchoring conventions ship in one scene.
- **D-12 ≡ banked D-2/L-4/C-5** (MINOR; the hand-copied `top`). B's direction-limb kill adopted (K-12): mobile error is 0.2rem the SAME way, never crossing.
- **D-10 ≡ banked N-14 + its M-6 rider** (MINOR; valuetext-in-ms cure BLOCKED on N-1's domain decision — the sequencing note carries). **New rider adopted:** no PageUp/PageDown against `ROW_AT_STEP = 40` over 1600ms (Target:232-243) — 40 presses to traverse, against APG's slider pattern.
- **L-4 ≡ banked KF-APP-4** (MAJOR → KF.W4, born-RED trivial; already folded by kf-CubeScene L-8/C-9 and kf-EasingTarget KF-ET-11). This corpus contributes the scene's FIRST live witness — ST-5 below — and both baseline cells corrected (K-11).
- **D-16/L-13/C-6 (`btn-interactive`) ≡ banked kf-ControlsPaneWrapper D-M11** (MAJOR, 8 lines/6 files census pinned, NO-WAVE-OWNER deletion) **+ kf-CubeScene D-11** (KF.W6 phantom-utility audit). The D-16 closing paragraph tying this to F-1 dies with K-1.
- **L-7/C-7 (`--stagger-max` write-only) ≡ banked N-5** (INFO). Sole hit = the write at Target:63; sibling `--scrub-dir` live.
- **D-7 (forced-colors decorative paint) ≡ banked D-16 / KF-CE-13 family** (INFO → KF.W9; ruling 7's scope datum adopted: this scene is the family's sharpest instance — the state carriers vanish, the washes survive). A's MINOR preserved as dissent.
- **D-20 (`will-change` on `.cascade-chase`) ≡ banked L-11/C-10** (INFO; the 8-promoted-layers census).
- **D-25 ≡ banked N-15 + D-13/L-12** (INFO; the `--seq-glow` duplicate — the bank deletes :216, D-25's inverse — and `grid-column: unset`). Both readers' complementary kills adopted (K-13): ONE redundancy, not two; and "inapplicable" is false — css-grid-1 §10.1 positions an abspos grid child against its grid area when placement is definite, so `grid-column: 2` on the playhead track is a candidate cure the axis's dismissal buried (ruling-1's gap cure stays preferred).

### MAJOR (12 booked)

- **D-3** — `.seq-row-at`'s `opacity: 0.8` on the AA-tuned `--on-glass-muted` token drops the row's `at:` value below AA in the light arm: **5.91:1 → 3.82:1** (3.92 at the 2% stop) at 12.15-14.38px/400 — no large-text exemption; dark arm **6.55:1 PASSES** (light-arm-only failure). Pinned by three concordant derivations incl. this seat's product-converter run (ruling 6) — the family's wrong-byte ledger does not touch this cell. The css:95-99 rationale refused an off-ladder font-size and then bought subordination with the one lever the token system cannot absorb. Composited-backdrop movers → SS-13. **NO-WAVE-OWNER (with D-14 — the same sub-line, weight and alpha cured as one decision).**
- **D-4** — `playReel()` — author-declared "pure delight", deliberately under-damped (`dampingFraction: 0.34`), user-retriggerable at two entry points (Target:34 button, :249 typed egg) — has **no prefers-reduced-motion path anywhere in its call chain**: children built without `respectReducedMotion` (default `false`, `constants/defaults.ts:87`); glass-ui's PRM blanket cannot reach engine-written inline styles; the same file declares "MANDATORY PRM degrade" for the boot (css:237-243) and useSequenceInstrument.ts:29-32 proves the idiom is known. Distinct from banked D-8/C-§5 (the playhead's progress-transform, ruled no-charge) — this is a separate, repeatable decorative motion. Both readers concordant MAJOR. **KF.W9 (PRM census repair — the lane-frontend §6.5 row is too generous for this scene, D's explicit corpus contradiction sustained) + the one-flag cure noted (`respectReducedMotion: true` at useSequenceDemo:128-132 or a `matchMedia` gate in `playReel`).**
- **D-5** — the scene title is a `<span>` (Target:14) where the sibling scene ships the identical class string on an `<h2>` (EasingTarget.vue:24); no heading, no `role="region"`, no `aria-label` anywhere in the mounted subtree (my heading census: four files, none an ancestor). A heading-navigation user cannot reach or identify the scene. **NO-WAVE-OWNER (one-element cure; scene-shell packet).**
- **D-6** — `--label-col: 3.25rem` (52px) is a fixed track that cannot hold its own default label: `@1040MS` = 7 glyphs × (0.6 + **0.1 tracking**)em = 4.9em ≈ **59.5px at 375 → 70.5px at 1440** — overflow at EVERY viewport, leftward (column flex, `align-items: flex-end`, no overflow/min-width guard), permanent after any `End` press (`@1600MS`). Ruling 5's arithmetic; the axis's break-even cell dead (K-10). **NO-WAVE-OWNER (geometry commit with the D-1 packet — the label column and `--col-gap` are one decision).**
- **D-8** — `overflow-hidden` + `max-h-full` on the Card, `shrink-0` on all three bands, no `min-h-0`, no scroll container (structure source-proven whole-file): at short viewports the excess is DISCARDED, and the last child is the master scrubber the card is built around. Magnitude (the ~476px floor vs a landscape phone) → SS-13. **NO-WAVE-OWNER.**
- **D-9** — the status badge is the only textual state signal, never announced (no `role="status"`/`aria-live` anywhere in the scene), **lies during the reel** (`playReel` pauses first ⇒ `isPlaying`/`isReversed` both false ⇒ "ready" renders for the ~1.3s cascade), and is a flat 10px (`--type-admin-label`). Natural end also flips playing→ready silently (`.finally` PAUSE, useSequenceDemo:231-239). Cure adjacency: ST-4's one binding supplies the announcement channel (`aria-busy`). **NO-WAVE-OWNER (state-signal spec with ST-2/ST-4/D-15).**
- **L-2** — ONE `useDragScrub` instance wired to FIVE press targets: a second concurrent pointerdown double-acquires the module-global suppression refcount and can only single-release (boolean liveness, no pointerId), stranding `body.is-dragging` → `user-select: none !important` document-wide for the session. **MAJOR (ruling 3); mechanism family ≡ banked KF-AV-15 — the seam-level cure (count/pointerId at `gestureSelectSuppression` + re-entrancy guard) covers both consumers; this row is the `useDragScrub` instance and the demo's only multi-target wiring.** Reachability → SS-13. **NO-WAVE-OWNER (seam-level, cross-referenced KF-AV-15).**
- **L-5** — the reel egg and the row handles are concurrently live: `playReel` guards only reel re-entry (:365); `reseatRow` runs mid-reel (guards index + `isPlaying` only, :325-327) and its `seek` → `applySequenceAt` iterates every entry with **no `managed` check** (transport.ts:33-37) — two writers on the same ball targets, and the reel's `finally` re-seeks against whatever the drag left. The settled-counter limb is killed (K-4); the typed egg fires even while a handle holds focus (useTypedTrigger skips only editable targets). **NO-WAVE-OWNER (gate the handles or the egg on `isReeling` — one guard, stated in the packet's gesture spec).**
- **C-2** — the Reel button re-implements `iconOnly` with `h-7 w-7 p-0` and loses to the primitive's own unconditional `min-block-size: var(--button-size)` floor: rendered **28 × 40px fine / 28 × 60px coarse** (`--control-h-md` = 40px; the coarse arm lives in `tokens/light-dark.css` — the axis's citation cell killed, K-5). **Extra limb verified at the compiled chunk: `data-control-target` is emitted ONLY when `iconOnly`, so the call site also forfeits the coarse-pointer `min-inline-size` floor — 28px wide on touch against the producer's own 44px floor.** The icon-sm limb is killed (K-6 — the utility is 1rem, not a numeric twin). Cure `size="xs" iconOnly` — the fleet-banked mechanism (AAP-19/AF-7/ATP-26/SFB-8). **NO-WAVE-OWNER (one-attribute cure; D-11's band cell corrected by it).**
- **C-3/D-26** — the scene carries a SECOND hand-rolled slider family: five `role="slider"` row handles with the complete Slider contract re-authored (aria quintet, pointer capture, arrow/Home/End ladder, thumb, own focus ring) — census S-4's shadow tally for this scene should read **6, not 1**. glass-ui `Slider` is root-barrel reachable (B's self-killed hypothesis — the lowercase re-export verified) and already imported thrice. Both readers concordant MAJOR; C's own evaluate-not-mechanical-swap caveat carries. **NO-WAVE-OWNER (census repair + the S-4 replacement wave scopes both families).**
- **C-5** — `<Metric>` adopted here (the demo's sole consumer) and hand-rolled 110 lines away in the sub-unit carved from this file: SequenceScrubber's eyebrow+timecode is `MetricProps` verbatim (`orientation: "stacked"` + the value slot express it; `.seq-timecode` re-authors `tnum` the primitive owns). An S-9 census candidate. Both readers concordant MAJOR. **NO-WAVE-OWNER (finish the adoption inside its own feature; ST-3 must land with it or the swap re-loses the tone).**
- **ST-1** *(NET-NEW — A miss #1, verified at all three links; ruling 8)* — **the five row sliders have NO keyboard focus indicator in forced-colors mode, because the scene forked the demo's self-declared SINGLE keyboard-focus affordance to satisfy a proof gate that does not exist.** (1) design-idioms.css:73-79 declares the `.focus-ring` contract ("the SINGLE keyboard-focus affordance") and the sibling drag rails obey it (SquareScene:46, SpringTarget:63, SpringHeatmap:30 — the banked kf-CopyButton adopter census, concordant). (2) Target:97 ships bare `class="seq-handle"`; css:160-167 re-authors the identical declarations locally, and its comment justifies the fork by "**the `proof:sequence-rows-draggable` static clause**" — my repo grep: that gate exists in the CSS comment and old tranche prose ONLY; the proof roster at BOTH baselines (`proof:structure`/`proof:publish`/`proof:owner-golden` at origin/master) has no such script. The contract was forked for a retired grep gate. (3) glass-ui's forced-colors provision is class-scoped (`.focus-ring:focus-visible, …` — my whole-head read; ≡ kf-CopyButton's substring-artifact reversal); HCM forces `box-shadow` to none, `.seq-handle`'s `outline: none` survives, nothing puts anything back — five keyboard-operable sliders, zero focus indication, WCAG 2.4.7/2.4.11. Cure is one word (`focus-ring` at .vue:97) + delete css:163-167 (its justification is void). **Second limb RELAYED to the forthcoming kf-SequenceScrubber corpus (true at the bytes, out of this file's scope): SequenceScrubber.vue contains zero `focus` hits in 162 lines — the master `role="slider"` has no focus indication in ANY mode.** Distinct from the D-7/KF-CE-13 fold (decorative paint) and from every banked focus row (novelty sweep, ruling 8). **MAJOR. NO-WAVE-OWNER (the one-word cure) + KF.W9 (the HCM-family datum; D §4's "outline replaced" cell is killed at K-8).**

### MINOR (23 booked)

- **L-3/C-9** *(ruling 4; A's MAJOR as dissent)* — the parent's render scope subscribes to `demo.progress` at :21/:70, so the five-row storyboard re-renders at rAF cadence, with 10 inline function refs re-allocated AND re-invoked per patch (runtime-core:1814-1815 unconditional — verified at the installed dist by both readers). Banked prose half ≡ D-5/L-8/C-4; the parent-scope + function-ref limb is this file's own. The sub-unit asymmetry is the cure's map: Scrubber injects (isolated ✓), Playhead takes a prop (forces the parent ✗), the Metric read moves with it. Render-trace kill condition → SS-13. **NO-WAVE-OWNER.**
- **L-8** — `.seq-root` is a dead class: sole repo hit is the markup at :2 (plus an unimplemented tranche-S proposal). **NO-WAVE-OWNER (dead-declaration sweep).**
- **L-9** — `ROW_TONES`/`rowEls`/`ballEls`/the mount loop/the `v-for` are five expressions of one cardinality with no compile-time coupling; `ROW_TONES[row.index]` is `string | undefined` under `noUncheckedIndexedAccess` and degrades silently to the master tone. L-4's fold is why nothing checks it; ST-5 is the same gate's live witness. **NO-WAVE-OWNER (tuple type, one line).**
- **L-10/C-4** — the view reaches past the composable's seam twice in `onMounted`: `childAnims[i]!.setTargets(el)` and the raw `demo.sequence.progress =` write — the one playhead write that neither re-syncs nor dispatches; the composable's own :439-442 comment names itself "belt-and-braces" for it (two writers acknowledged, a third guard added). Load-bearing (setTargets binds but does not paint) — the cure is a façade verb (`bindRowTargets`/`paintCurrent`), never deletion. **NO-WAVE-OWNER.**
- **C-8** *(ruling 10)* — root-barrel (`Button, Card`) and forced granular subpath (`/metric` — `Metric` genuinely absent from `dist/index.d.ts`) collide in one three-line block; half-forced, half convention drift. **NO-WAVE-OWNER (import-idiom sweep).**
- **C-10** — `.seq-ball` re-authors the promoted `.progress-ball` idiom's `box-shadow` (discarding the `0 2px` offset and the `--ball-glow` seam) in the scene whose sibling file documents the opposite discipline; the mitigation is real (`--ball-p`-scaled glow needs blur/spread parameters the idiom lacks) and unstated. Cure: extend the idiom (`--ball-blur`/`--ball-spread`), state the departure. **NO-WAVE-OWNER.**
- **C-11** — the entire inbound contract is `inject(SEQUENCE_DEMO_KEY)!` — no default, no throw — in a repo with a written injection-orphan post-mortem (CubeScene:41-44); the sibling sub-units carry compiler-checked props. Enforceability, not a live crash (SequenceScene provides unconditionally). **NO-WAVE-OWNER (guarded inject idiom).**
- **C-12** — pre-stringifying with `.toFixed(0)` disables `Metric`'s own non-finite coalescer (the string arm can never reach `Number.isFinite`); "NaN" would render in an xl poster rung. Unreachable today (clamps upstream) — but the bank's ruling 8 found four NaN-transparent clamps in this scene family, which is why the guard is worth restoring (pass the number). **NO-WAVE-OWNER.**
- **C-13** — the rect-ratio projector is byte-equivalent in both `useDragScrub` consumers in this scene; the composable's docstring names the shape twice and ships it zero times. Cure: a default projector on the seam. **NO-WAVE-OWNER.**
- **D-13** — the mobile `.seq-track { height: 1.5rem }` is inert for layout (row height = the ~34.3px label, `align-items: center`) and costs 8px of drag target (32→24px, exactly the 2.5.8 floor) — on the surface whose longest comment documents a prior target-size regression. **NO-WAVE-OWNER (mobile-pitch commit with D-8).**
- **D-14** — hierarchy inversion in the row label: the ordinal index (redundant with order and the tone spectrum) at 20.35px/600 vs the `at:` datum — the one number the row exists to expose — at 12-14px/400/0.8α. The exact mirror of banked kf-SequenceAxis **SA-1** one element over (the instrument louder than its data — same scene, same class). **NO-WAVE-OWNER (one decision with D-3).**
- **D-15** — `.reel-active` is documented as a pulse ("The reel button pulses while the egg runs") and is a static recolour — no animation, no transition, anywhere. Compounded by ST-2 (half of even the recolour is inert). **NO-WAVE-OWNER (with ST-2/ST-4).**
- **D-17** — the `@property --ball-p` rationale ("so the bloom INTERPOLATES between engine frames") is false as written: registration makes a property animatable, nothing transitions it (my transition census: exactly one, `.seq-handle::after` at :151). The real merits (`<number>`-typed `calc()` operands, `initial-value: 0`) are not what the comment claims. **This confirmation KILLS challenge-L's S-3 bullet 1 (K-9) — a cross-axis contradiction resolved in D's favor.** **NO-WAVE-OWNER (prose-truth sweep).**
- **D-18** *(scoped-escape half; census limb folds)* — `@property` is a selector-less at-rule Vue's scoped transform cannot qualify: `<style scoped src>` registers `--ball-p` DOCUMENT-globally (`inherits: true`, `<number>`). Blast radius nil today (zero outside-scene readers) — latent IACVT with no local cause for any future `.progress-ball` consumer. The sixteen-unprefixed-names census ≡ the banked scene token census (kf-SequenceAxis D-10/L-11 15/16 row) — folded, not re-booked. **NO-WAVE-OWNER (property-hygiene family).**
- **D-19** *(sibling-file scope — booked with relay)* — the scrubber eyebrow's comment specifies Fira Code letter-spaced caps; `text-caption` binds the body face AND `font-style: italic` (my read of installed semantic.css) — italics fighting uppercase at 0.18em tracking. The intended `text-mono-caption` is used seven lines away. **Relay to kf-SequenceScrubber with ST-1's limb. NO-WAVE-OWNER (one-class cure).**
- **D-21** *(B's cell adopted alone — A filed none, ruling 11)* — seven distinct vertical paddings and six type rungs across a three-band card, none in ratio, while css:95 invokes a φ-ladder to defend one font-size; the header band's height is set by C-2's unintended 40px button (D-11's "~30px band" cell corrected). **NO-WAVE-OWNER (rhythm decision, packet-spec).**
- **D-22** — `.seq-stage` paints an OPAQUE plate (color-mix of two opaques) over ~65% of a translucent Card and reaches for the page `--background` inside a `--card` surface — a measurably different paper inside its own host; `--surface-tint-*` is the register for a translucent inner plate. The containment idea is right (D-1's premise depends on it); the execution fights the demo's own reclaimed-translucency posture. **NO-WAVE-OWNER.**
- **ST-2** *(NET-NEW — A miss #2, verified at the compiled chunk)* — **half of `.reel-active` — the only feedback that the reel runs — is inert by construction**: glass-ui 7's Button base ships `border: 0`; the only restoring rule is `.button:not([data-tone="neutral"])`; the chunk's default is `tone: { default: "neutral" }` and the call site passes none — `border-color` colors a 0-width border. Provenance exact: the migration rewrote `variant="outline"` → `emphasis="secondary"` (my `git diff HEAD`; 7.0.0 has no `variant`) and left the stylesheet encoding the dead primitive's contract. Net with D-15 and D-9.2: a 1.3s state whose only signal is a `color` swap on a 28×40 icon button. **NO-WAVE-OWNER (dies with ST-4's binding).**
- **ST-3** *(NET-NEW — A miss #3, verified)* — **the Glass-7 Metric migration silently deleted the header readout's master-tone binding and the comment still promises it**: the diff drops `color="var(--ball-tone, var(--color-progress))"` + `label-position="inline"` (`MetricProps` has neither — my whole read); Target:9-11 still reads "master accent"; the house idiom `.readout-accent` (design-idioms:189-192) is used for the SAME scalar 110 lines away (Scrubber:16). One scalar, two colours in one card, and the comment asserts the opposite. All three axes missed it in different directions (C praised the adoption, L-4 called the props valid, D-11 audited only size). Cure: the `class`/value-slot binding `MetricTextProps` accepts. **NO-WAVE-OWNER (with C-5).**
- **ST-4** *(NET-NEW — A miss #4, verified)* — **Button's shipped `loading` prop is the exact contract `isReeling` needs and it is unbound**: the chunk emits `aria-busy`/`data-loading` and suppresses activation; the scene already owns the state (`demo.isReeling`) and already needs the suppression (`playReel` opens with a silent no-op guard). One binding — `:loading="demo.isReeling.value"` — supplies D-9.2's missing announcement, D-15's missing affordance, and makes the JS guard user-visible. **NO-WAVE-OWNER (the packet's cheapest cure; closes three rows).**
- **ST-5** *(NET-NEW — B miss #1, verified at the bytes)* — **a LIVE TypeScript error sits in the `<script setup>`**: `activeRowEl = computed<HTMLElement | null>(() => activeRow.value == null ? null : rowEls[activeRow.value])` with `rowEls: (HTMLElement | null)[]` under `noUncheckedIndexedAccess: true` → the getter returns `HTMLElement | null | undefined` against its declared `HTMLElement | null` — TS2322 the moment any gate parses the SFC. The sibling expression six lines down guards correctly (`if (i == null || !el)`), proving the author knew the shape at one site. Runtime-unaffected — a pure gate witness. **This kills L-4's "no live type error" parenthetical (K-14) and is the sequence scene's first live witness for banked KF-APP-4. → KF.W4 (witness) + NO-WAVE-OWNER (the one-guard fix).**
- **ST-6** *(NET-NEW — B miss #2, verified)* — **`playReel` schedules five `window.setTimeout`s that nothing retains, clears, or reaches from `onScopeDispose`**: a scene switch inside the ~360ms stagger fires `child.play()` AFTER teardown — fresh standalone `RAFPlayback` loops on detached targets, after `sequence.stop()` (which stops only `seq.playback` + settles; a child's OWN `_playback` is untouched — my lifecycle/transport reads). Falsifies the composable's :444-446 teardown claim on the one path that creates child-owned loops; bounded ~900ms/loop (hence MINOR). **Adjacency: ≡ mechanism of banked kf-SequenceAxis SA-5 (the `powerOn` timer) at the other site — route together as the boot/timer-hygiene pair (with N-17). NO-WAVE-OWNER.**
- **ST-7** *(NET-NEW — B miss #3, verified; the residue of K-3)* — **`playReel`'s `finally` writes `child.managed = true` UNCONDITIONALLY**, "restoring" a flag the Sequence only owns while playing/managed-paused (protocol verified: init `false`, `play()` sets, `settleSequence` clears, `reset()` stops). Fire the reel on a never-played or reset sequence and five children are left flagged as owned by an orchestrator that is not driving them — against an engine whose `play()` THROWS on the flag. Self-healing only because the sole direct-play caller clears it first: latent state corruption. Cure: capture-and-restore the prior value, or drive the overshoot through the Sequence with a swapped timing function and never touch the ownership bit. **NO-WAVE-OWNER (with L-5 — the reel's ownership story is one spec paragraph).**

### INFO (6 booked)

- **L-12** — three provenance comments assert a 24px handle against the shipped `width: 44px`, and the stylesheet contradicts itself nine lines apart (the S.G3 S4 widening rationale sits directly above the contradicted comment). **NO-WAVE-OWNER (prose sweep).**
- **D-23** *(prose half; the 4-rules-5-labels half ≡ banked N-16)* — the stylesheet promises a stage that "OWNS its time grid"; `.stage-field-x` dresses only the 17.6px ruler strip. **NO-WAVE-OWNER (axis half of the packet).**
- **D-24** — the 44px handle hit-box overhangs the label column at `at=0` and crosses the stage border at `at=STAGGER_MAX`; the focus ring paints on the oversized host (~7× the grip it indicates). The 44px choice is well-argued in situ; only the domain-end overhang is unhandled. **NO-WAVE-OWNER.**
- **ST-8** *(NET-NEW — B miss #4, verified)* — `void promise.finally(…)` at both engine-play call sites (useSequenceDemo:231, :379) is an unhandled-rejection posture, not error handling: `finally` re-raises, `void` discards, no `.catch` exists; the engine's throwing paths (the managed guard, `assertNoUnresolvedNamedSelector`) are real but unreachable in this scene today. **NO-WAVE-OWNER (error-posture note in the packet spec).**
- **ST-9** *(NET-NEW — A miss #5, verified at the token chain)* — concentric-radius break: `.seq-stage` wears `--radius-card` (= `--radius-2xl` = 16px) while sitting exactly 16px (`px-4`) inside the Card wearing the same token — the correct concentric inner radius is 0 (or `calc(var(--radius-card) - 1rem)`); the inner corners read fatter than their host's. The one geometric relationship no axis measured. **NO-WAVE-OWNER.**
- **ST-10** *(NET-NEW — A miss #6, verified at the chunk)* — `emphasis="secondary"` is the primitive's own default (`emphasis: { default: "secondary" }`) — a prop that reads as a deliberate choice and specifies nothing; it is the migration's REPLACEMENT for a real visual selection (`variant="outline"`), recorded as the fingerprint tying ST-2/ST-3/ST-10 to one mechanical rewrite — **the Glass-6→7 sweep over this file warrants a re-read, not three point fixes.** **NO-WAVE-OWNER (with the migration-seam trio).**

## Killed-claims register — 16 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **C-1 BLOCKER** ("glass-ui absent from package.json AND package-lock.json; on clean `npm ci` the scene cannot build") + L-14's F-1 limb ("I confirm F-1 against the tree") + D-16's F-1 rider | **KILLED at every committed baseline (ruling 1)** — origin/master declares 7.0.0 EXACT + locked = the installed version; the scene's sources are byte-identical to origin/master; the corpus read a mutated worktree manifest. ≡ census F-1/SCH-1 already ruled FALSIFIED (kf-AmigaScene → KF.W0 §B-12). The ledger's next **citation-inheritance event**: an unre-derived RED cell propagated into two fresh "confirmed against the tree" cells. C-16's dependent paragraph falls with it. |
| **K-2** | D-1 and L-1 filed as **BLOCKERs**; L-1 as a fresh booking | **BOOKING/GRADE KILLS (ruling 2)** ≡ banked K-6 (BLOCKER killed, two r4 readers concordant at MAJOR) + banked N-2 (the identical `End` witness adopted at r4). Grade-stability is the anti-rename law applied to severity — now a fourth generation. |
| **K-3** | **L-11's mechanism**: quotes `play-lifecycle.ts:364-366` as `warn("Animation.play() called on a managed animation …")` — "suppressing a warning by mutating an ownership flag" | **THE QUOTED LINE IS FABRICATED** — the live code THROWS (my read, :364-368). The mechanism inverts: the flip is the call's mandatory precondition; `managed` is protocol-owned by the Sequence (lifecycle/settle verified). The ownership concern survives in the RESTORE direction as **ST-7** — the half the axis never opened. |
| **K-4** | L-5's aggravator: "the settle counter is not reset on interruption" | `let settled = 0` is closure-local per invocation (my read, :371); the re-entry guard prevents a second concurrent counter; every child's `finally` runs. Limb dead; L-5's core stands. |
| **K-5** | C-2's citation cell: the coarse-pointer arm quoted at `tokens/sizing.css:1` | STALE-DIST discipline: my `grep -c 'pointer: coarse' sizing.css` = **0**; the arm lives in `tokens/light-dark.css` (`--ui-scale: var(--ui-coarse-scale, 1.5)`). Values survive (40/60px); the sourcing cell dies. |
| **K-6** | C-2 limb 3: "`icon-sm` (0.875rem) — numerically identical to `w-3.5`" | The demo utility is `@apply size-4` = **1rem** (≡ banked kf-CubeScene D-25's shipped-sheet extraction); 0.875rem is glass-ui's TOKEN, a different artifact; the numeric twin is `icon-xs`. Swapping in `icon-sm` would ENLARGE the glyph. Limbs 1-2 confirmed above. |
| **K-7** | D §4 "checked and clean": "the reel `Button` is `h-7 w-7` = **28 × 28px**" | The primitive's unconditional `min-block-size` floor renders it **28 × 40 fine / 28 × 60 coarse** (my base-rule read). The 2.5.8 conclusion survives; the measurement was wrong and propagated into D-11's band cell (corrected). Axis D did not read the primitive it was crediting. |
| **K-8** | D §4: "`outline: none` without replacement — none found; `.seq-handle:focus-visible` replaces the outline" + D-7's "glass-ui's forced-colors blocks cover focus rings" | **Both false in forced-colors** — the provision is class-scoped (`.focus-ring:focus-visible, …`; ≡ kf-CopyButton's substring-artifact reversal); `.seq-handle` wears none of the classes; HCM strips the box-shadow and keeps `outline: none`. The masked MAJOR is admitted as **ST-1**. A "clean" section's cell masking a MAJOR is this register's sharpest entry. |
| **K-9** | challenge-L **S-3 bullet 1** (superlative): "@property … so the browser can interpolate the engine's discrete per-frame writes" | Killed by axis D's own D-17 in the same corpus — registration ≠ interpolation; nothing targets `--ball-p` (my transition census: one rule, :151). L re-asserted the file's wrong comment as PRAISE. Cross-axis contradiction resolved for D-17; the idiom's real merits stand in superlative 2. |
| **K-10** | D-6's break-even cell ("~486px; 375px fits (just)") **+ reader-B's re-derived table carrying the same omission** | The utility itself applies `letter-spacing: var(--type-tracking-caps)` = **0.1em** (my greps): advance 0.7em/glyph → 59.5px at 375 vs the 52px track — no viewport fits (ruling 5). B killed nothing here but re-derived the axis's arithmetic shape without the tracking — the same class of inherited omission the reader polices elsewhere. D-6's substance strengthened. |
| **K-11** | L-4's quoted gate battery (`"check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json"`) | Dirty-worktree bytes: origin/master's `check` ends `&& npm run proof:structure` and the proof roster differs (my `git show`). Substance independently re-confirmed at origin/master (zero `vue-tsc`; CI runs `check:lib` alone) — partial kill, the row folds regardless. |
| **K-12** | D-12's mobile-direction limb ("the error becomes 0.2rem the OTHER way") | Same direction, smaller magnitude: rows start 2.5rem desktop / 2.2rem mobile vs the fixed 2rem top — the line never crosses (arithmetic re-run). The staleness claim folds to the bank intact. |
| **K-13** | D-25's "two dead declarations" framing + "`grid-column: unset` … **inapplicable**" | One redundancy (both resolve to 0; ≡ banked N-15, which deletes the OTHER copy — D-25 is the bank's inverse); and per css-grid-1 §10.1 `grid-column` genuinely applies to an abspos child of its grid-container CB — the dismissal buried the `grid-column: 2` candidate cure (ruling-1's gap cure stays preferred; the premise, not the preference, was wrong). |
| **K-14** | L-4's parenthetical: "the props all happen to be valid … NOT a live type error" | There IS one, in the surface all three axes read: **ST-5**'s TS2322 at `activeRowEl` (my derivation at the bytes). The axis affirmed the absence of the thing it failed to find. |
| **K-15** | Reader-A's C-8 severity field (INFO) vs its own cell text ("the row is correctly MINOR") | Internal contradiction; ruled MINOR (ruling 10) — the axis grade, B concordant. Recorded because a verdict whose text and field disagree is un-adoptable as filed. |
| **K-16** | Reader-A's D-7 re-grade (MINOR against the banked INFO) | **K-31 class, declined** — no new mechanism accompanies the re-grade; the scope datum is adopted into the fold WITHOUT moving the grade; A's dissent preserved. |

## Superlatives (provenance preserved; L-18 running the other way)

1. **`--row-start` — a temporal property made legible as space at zero per-frame cost** (Target:79-84; css:187-199): the stagger distribution reads as a diagonal cascade at t=0, before anything moves — one custom property per row, no JS, composing with the engine's `--ball-p` in a single `calc()`. Both readers; the best idea in the component. Its arithmetic defect is banked N-4 — a bug in the mapping, not the idea.
2. **The `@property` + `container-type` + `translateX(<cqw>)` engine-consumption idiom** ≡ banked kf-SequencePlayhead superlatives 1/5 — folded, not double-booked; **with D-17's correction riding beside it** (the idiom is right; its stated rationale is wrong — K-9).
3. **The J.WZ target relocation** — a tap-target regression diagnosed back to an ANIMATION-TARGET choice and fixed at that layer (`setTargets(ballEls[i])` preserving the keyframe, restoring geometry structurally), with the rationale recorded at the call site. Both readers re-derived both halves.
4. **Per-child `setTargets`, not the `Sequence`-level broadcast** — the right API choice against a real alternative (`Sequence.setTargets` fans one set to every child — exactly wrong for a storyboard). The call's LOCATION is L-10/C-4's defect; the call itself is right.
5. **inv ζ — four synchronised indicators, one clock, zero demo-owned rAF** (both readers' grep: none in the scene) — **now QUALIFIED by ST-6**: true of the SFC and the mirror, false at the one seam the SFC solely triggers (the reel's five timers can orphan child-owned loops past disposal). The superlative and its limit are one record.
6. **`useTypedTrigger`** — the textbook composable extraction (closure-local buffer, scope-bound teardown, editable-target skip, self-bounding `slice`); returns `void`, exposes nothing, ships a discoverable twin.
7. **`.status-badge` is justified bespoke, and the tree proves it** — the parameterised `--badge-*` seam with a written load-bearing AA derivation vs glass-ui Badge's fixed five-tone register; the correct 'keep, justified' with the argument at the idiom (C's S+3, checked in the negative direction).
8. **The pointer plumbing is careful in four places that each close a real bug class** (`touch-action` on the host; `pointer-events: none` on the grip; try/caught capture; `pointercancel` routed through the same end path) — the author saw L-2's failure class and closed one door of it; and `useDragScrub`'s one-honest-registration posture states its trade in the tree. (A's S-4/S-7, merged.)
9. **The corpus's kill sections and restraint survived hostile re-reading where spot-checked** (no transient-null ref window; no KeepAlive; valid Metric shapes; the C-12/C-8 self-limitations all check out) — with exactly the exceptions this register records (K-7/K-8/K-14: two "clean" cells masking a MAJOR and a live type error). The discipline's lesson from the sibling ledger sharpens: negative results must read the primitive too.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The three-rect probe** (`.seq-axis` / any `.seq-track` / `.seq-playhead-track` lefts) ≡ banked SS-13 #1 — settles D-1's subgrid branch (4.25/4.625/5rem); K-29 stands as the warning against ruling it statically; ruling-1's cure moots it.
2. **D-3's composited ratio** — my 3.82:1 is against the opaque plate; the `--glass-backdrop` container arm and `contrast-color()` support are the two live movers; direction cannot move (an α multiplier is a strict reduction).
3. **D-6's rendered spill** — `.seq-row-label` scrollWidth vs the 52px track at ≥375px; confirm leftward overflow, not clip.
4. **D-8's magnitude** — ≤400px-tall viewport: is the scrubber discarded (no scrollbar, no compression)?
5. **L-2's reachability** — two-thumb drag on rows 1/5 at 375px; check `document.body.classList` after both lifts.
6. **L-3/C-9's cost** — a render trace during playback; SequenceTarget rendering only on row mutation kills the limb outright.
7. **C-2's painted geometry** — the 28×40/60 lozenge against the caption-height badge in one flex row; plus the touch-width forfeit.
8. **ST-1's render** — the scene under `forced-colors: active` with a row handle keyboard-focused: the component's highest-value single capture (`grep -rn forced-colors demo/` = 0; the coverage hole is fleet-wide on record).
9. **D-13's computed row height at 375px** (label ≈34.3px vs the 24px track — confirm the inertness).
10. **D-24's ring straddle** at `at=0` / `at=STAGGER_MAX`.
11. **ST-6's teardown witness** — trigger the reel, switch scenes within ~360ms, observe orphaned `RAFPlayback` loops writing to detached targets after `sequence.stop()`.

## Identity guards (fold-by-reference — never re-booked)

- **The scene home is banked kf-SequencePlayhead** (its §Pattern line 144 named this record in advance): D-1 → banked D-1/L-1/C-1 · L-1 → N-2 (+ the seek-shape rider adopted) · D-2/L-6 → N-4 (+ the scrub-ball corroborant) · D-12 → D-2/L-4/C-5 · D-10 → N-14+rider (+ the PageUp/PageDown rider) · L-7/C-7 → N-5 · D-7 → D-16/KF-CE-13 · D-20 → L-11/C-10 · D-25 → N-15 + D-13/L-12 · D-23's label half → N-16 · D-18's census limb → the SA D-10/L-11 token census. Zero re-bookings.
- **L-4 ≡ KF-APP-4** (→ KF.W4); ST-5 is its first sequence-scene live witness, beside kf-CubeScene's five.
- **btn-interactive ≡ kf-ControlsPaneWrapper D-M11 + kf-CubeScene D-11**; the cross-repo value.js family stays cross-referenced, never merged; the producer-side question rides the standing glass-ui BH relay.
- **L-2's mechanism family ≡ KF-AV-15** — one seam-level cure, two composable-side instances (`useDragCapture` there, `useDragScrub` here); booked here because the wiring (one instance, five press targets) is this component's own.
- **C-1's corpse ≡ census F-1/SCH-1** (KF.W0 §B-12); the pattern ledger gains this corpus's inheritance event (K-1).
- **D-14 mirrors banked SA-1** (same scene, same class, one element over) — cross-referenced, separately booked (different element, different datum suppressed).
- **ST-6 ≡ SA-5's mechanism at the other site** — routed together (the boot/timer-hygiene pair with N-17).
- **ST-1's scrubber limb + D-19 are RELAYED to the forthcoming kf-SequenceScrubber corpus** (the SA D-12 relay idiom): the master slider has zero focus indication in any mode, and the eyebrow wears the italic body face.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the K-1 fold (census F-1/SCH-1, §B-12) — nothing new, one more inheritance event for the pattern ledger.
- **KF.W4**: the L-4 fold + **ST-5** (the live TS2322 witness; the born-RED is trivial — `vue-tsc --noEmit` exits non-zero on it today).
- **KF.W6**: the btn-interactive fold (phantom-utility audit, with the D-M11 deletion).
- **KF.W9**: **D-4** (the reel PRM gap — the §6.5 census row corrected for this scene) · the D-7/KF-CE-13 fold datum (this scene = the family's sharpest instance) · **ST-1's HCM-family datum** · the SS-13 list.
- **NO-WAVE-OWNER — the sequence scene-repair packet (SS-1/SS-2 authoring must home it, extending the banked packet):** the **instrument-truth folds** (D-1/N-2/N-4 confirmations + the seek-shape rider — the banked "one canonical domain" spec frame governs) · the **geometry commit** D-6 + D-13 + D-22 + ST-9 (+ the banked ruling-1 cure they ride) · the **state-signal spec** D-9 + D-15 + ST-2 + **ST-4 (the one-binding cure that closes three rows)** + ST-10 (the migration-seam re-read: ST-2/ST-3/ST-10 are one rewrite's three symptoms) · the **a11y cluster** D-5 (heading) + **ST-1 (the one-word focus-ring cure)** + the N-14 fold's riders (valuetext SEQUENCED AFTER N-1; PageUp/PageDown) · the **gesture/ownership spec** L-5 + ST-7 + ST-8 + **L-2 (seam-level with KF-AV-15)** + ST-6 (with SA-5/N-17) · the **consumption row** C-2 (one-attribute cure) + C-3/D-26 (the S-4 both-families scope) + C-5 + ST-3 (land together) + C-8/C-10/C-12/C-13 · the **render-scope row** L-3/C-9 (colocation cure; SS-13 #6 its kill switch) · the **seam-hygiene tail** L-8 · L-9 · L-10/C-4 · C-11 · D-17/D-18 (property-hygiene, with the banked family) · D-19 (relay) · D-21 · D-23/D-24/L-12 (prose/geometry riders).

## Verdict

**ADJUDICATED.** The corpus filed three BLOCKERs and this record buries all three — one falsified at every committed baseline it never read (the manifest was mutated; the sources were not; the census cell it folded was already ruled dead one directory away), two as grade re-contests against a bank that killed them generations ago. What the incredulity seat found instead: both readers' kill-work was sound where it counted (the fabricated `warn`, the tracking omission, the class-scoped HCM list, the stale citation cells — every load-bearing kill re-derived and sustained), both readers erred exactly once each in the direction this family's ledger predicts (A's dark-arm byte and severity-field slip; B re-deriving the axis's own killed arithmetic without its tracking term), and the eleven filed misses — for the first time in this scene family — were ALL genuinely new: ten rows admitted, one rider, one relay. The component itself is the same instrument its siblings described, seen from the storyboard: superbly conceived (a temporal property taught statically, one clock, careful pointers, a justified bespoke badge), arithmetically unclosed at every seam it shares (origins, gates, denominators — all banked, all folded), locally attentive to a11y and blind to it end-to-end (roles and rings everywhere; no heading, no announcement, no unit, no HCM, no PRM on the loudest motion) — and now carrying a fourth story the axes only grazed: a Glass-6→7 migration that mechanically rewrote its props and silently severed three contracts the comments still promise (the tone, the pulse, the outline), plus a focus-ring fork justified by a proof gate that no longer exists. The packet cure list is short and disproportionately cheap: one word (`focus-ring`), one binding (`:loading`), one attribute (`size="xs" iconOnly`), one class (the Metric tone), one guard clause (`isReeling`), five retained timer handles — and the banked one-line gap cure this scene inherits. Nothing here stamps a wave; every routing is M-25 agglomeration input for the forming X·KF specs. Original ids — D/L/C for life; **ST-1..ST-10 minted here for life**.

— end of apotheosis. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/sequence/SequenceTarget.css` · `demo/scenes/sequence/SequenceTarget.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SequenceTarget.css=IDENTICAL · SequenceTarget.vue=+5/-7**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

