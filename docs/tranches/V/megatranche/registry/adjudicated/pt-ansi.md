claude-fable-5

# pt-ansi — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Seat**: apotheosis, parse-that module `ansi` · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **parse-that HEAD**: `ef10d5b` (main checkout, branch `master` — the sole tree entered; `parse-that-css-totality-p2` never created) · **value.js HEAD**: `5e146edd`, branch `tranche-u`.
**Corpus**: `audit/parse-that-modules/ansi/challenge-L-library.md` (Opus 5 [1m]; 8 defects · 1 BLOCKER · 5 superlatives) + `challenge-C-consumption.md` (Opus 5 [1m]; 8 defects · 2 BLOCKERs · 4 superlatives) + the L-18 hostile re-read (Fable; 12 confirmed · 9 killed · 8 missed · 8 superlative rulings).
**Method**: both challenge files read whole by this seat; every killed claim and every reader-missed finding re-derived against the live tree with this seat's own instruments before adoption. The reader was then challenged back (incredulity seat): four corrections of degree filed below (§Seat corrections), zero verdict flips. Dispositions are **ADJUDICATED, not VERIFIED** — no bench was run, `enableDiagnostics()` never called, `makeMemoized` never reached, parse-that read-only throughout; my only write is this file.
**This seat's own instruments (all static, main checkout)**: `wc -l` ansi.ts (**17**) / debug.ts (**383**); full read of ansi.ts + debug.ts anchor regions (:1-31, :45-120, :121-260, :310-383); parser.ts :18/:25/:67-68/:690-696/:698-700/:711; state.ts :136-138; utils.ts :8/:12/:16; dist chunk `packrat-entry-CS1td-8B.js` — :28 sole `enabled =`, :29-37 uppercase `\x1B` table, `grep -c 'x1b'` → **0** vs `grep -oi` → **18**, `dim`/`formatDiagnostic` grep → absent, :881 guard / :882 emission, `wc -c` → **40,576**, region 28-285 → **8,517** bytes, 28-40 → **703**; chunk line 1 imports exactly {isDiagnosticsEnabled, mergeErrorState, reportUnclosedDelimiter, addSuggestion, collectDiagnostic, popLastDiagnostic}; `makeMemoized` :721 / `memoize` :844; `ls dist` full (7 declaration-only orphans); package.json exports (5 subpaths, no wildcard, `.`→types `dist/index.d.ts`); dist/{core,parse,packrat}.js:1 + utils.js:2 import the chunk; diagnostics.js:1 imports only the diagnostics chunk, `NO_COLOR` grep → 0; `grep -rn FORCE_COLOR src test scripts` → 0; `grep -rnE 'TERM\b|hasColors|colorDepth' src/` → 0; `addUnclosedDelimiterSuggestion` anywhere → 0; test/debug.test.ts :21-22 strip + six sites :168-:209, sole escape-byte mention in test/; test render-path census = {debug,print}.test.ts only; index.ts :10-12 scar; subpath-gate.mjs:25 four-subpath loop; dist-surface.test.ts:15; CHANGELOG `ansi` → 1 hit :117 (tr**ansi**tively); corpus anchors W1.md:483-492 (G-5 stderr-only), W2.md:266 (EQ-4), :281-285 (R-LAW-3), :720-726 (probe-defect rule); waves census `grep -rniE 'ansi|isTTY|NO_COLOR'` → 19 lines, substantive after excluding exp**ansi**on/tr**ansi** → **0**; value.js `dist/subpaths/css.js` → **43,973** bytes, dependencies parse-that-free; surface-gaps prober `grep -ci ansi` → 0; parser-band.md debt-3 located at **:118**; M-1/M-5/M-6 traces re-derived by hand from source (below).

## Scoreboard

**20-row roster: 12 CONFIRMED (from the two challenges, severities adjudicated) + 8 MISSED-ADOPTED (the reader's, all re-verified by this seat) · 9 KILLED sustained · 8 superlatives banked · 0 BLOCKERS SURVIVE ON `ansi`.**

Both challengers' BLOCKERs die as filed: L-D1 corrects down to MAJOR (the only library-chosen stdout sink is the opt-in debug tier); C-B1 is re-attributed off `ansi` entirely (the gate hole survives ansi's deletion — it is W1/W2 harness property, MAJOR there); C-B2 corrects to MAJOR (C's own concession: not a correctness bug; it blocks adoption-as-packaged, which is W4's surface). Sharpest live user-facing defect in the whole corpus: **M-1, which both challengers missed** — the caret drawn at the pre-truncation column against a re-windowed line.

## Adjudicated defect roster

Anchors are parse-that HEAD `ef10d5b`; re-resolve before executing any cure. Routing law: **W0..W4** = an X·P wave unit/gate owns the surface · **X·P REFINEMENT FOLD** = the finding sharpens a spec · **NO-WAVE-OWNER** = real defect, no X·P owner — recorded binding on the render-tier/2.0.0 relocation work so it inherits rather than rediscovers.

| # | id | sev | claim (adjudicated form) | proof (this seat's anchors) | disposition |
|---|---|---|---|---|---|
| 1 | L-D1 / C-D-4 | **MAJOR** | One stream oracle (stderr) governs colour for sinks on two streams; the public `Parser#debug()` default writes stdout; `ParserState#toString()` is ANSI-decorated, so `String(state)` and everything over it is TTY-dependent. | ansi.ts:5; parser.ts:68 (guard :67), :693 `console.log`; debug.ts:355 `console.error`; state.ts:136-138 → statePrint. All re-read live. `enabled===true` arm never observed in any session — witness is MEASURE-AT-OPEN row 3. | **W2** — R-LAW-3/EQ-4 own the shape: render path takes `{ color, sink }` as injected policy, ambient read retired (all four §3c candidates pre-declare the kill). The undeclared G-6 tension — is `String(state)` in the compared drop-in surface? — routes to the **X·P REFINEMENT FOLD** as a named rider. |
| 2 | L-D2 / C-D-3 | **MAJOR** | `enabled` is a zero-way latch: import-time `const`, unexported, no setter, no `FORCE_COLOR`; the coloured arm of all ten functions is uncovered and uncoverable; the suite hand-rolls its own strip. | ansi.ts:3-6 (no export); dist:28 sole write; FORCE_COLOR grep 0; test/debug.test.ts:21-22 + :168-:209; only escape-byte in test/ is the strip regex itself. | **W2** — dissolves under row 1's injected policy. Coverage of the coloured arm becomes possible and mandatory post-injection: a W1-harness witness rider (MEASURE-AT-OPEN row 3). |
| 3 | L-D3 | **MAJOR** | Ten escape constructors, zero sanitizer, in a render path that interpolates untrusted parse input between them; `statePrint`'s badge/header/body are diagnostics-ungated, so `parserDebug` reaches the echo with no flags. | debug.ts:58 split, :72/:89/:114 unfiltered, :14-31 pure slice; statePrint :141-:190 ungated body, extras gated :174; emission :377 + parser.ts:68; sole tree stripAnsi = test-local :21. | **NO-WAVE-OWNER** — EQ-4 quarantines rendering outside the algebra; no W0..W4 unit owns render-tier safety. Binding on any `./debug` relocation: ship a strip/escape covering **OSC (ESC ] … BEL/ST) + full CSI final-byte range**, not SGR-only (row 20). |
| 4 | L-D4 / C-D-5 | MINOR | `dim` is dead in the ship; `dist/ansi.d.ts` declares ten symbols with no `.js`; deep import typechecks green then throws `ERR_PACKAGE_PATH_NOT_EXPORTED` (exports-aware) / `MODULE_NOT_FOUND` (exports-blind). True orphan set = **{ansi, debug, lazy, leaf, parser, split, state}** — seven, per this seat's own `ls dist` (K7). | ansi.ts:9; debug.ts:203/:225 sole callers inside formatDiagnostic (:200), unreachable from any barrel; dist grep dim/formatDiagnostic → absent; exports map = 5 subpaths, no wildcard; subpath-gate.mjs:25 iterates runtime targets only. | **W4** — W4.b packed-release protocol / RC-P: dts-include narrowed or `@internal`-tagged; subpath gate extended to the declaration surface; the 7-orphan set is the acceptance list. |
| 5 | C-D-7 | MINOR | The ten symbols are de-facto private and de-jure typed-public at once — no `@internal`, no deprecation path, zero changelog rows ever. | ansi.ts:8-17 zero doc comments; CHANGELOG sole `ansi` hit = :117 substring. | **W4** — same RC-P unit as row 4; the tag-or-route decision is one edit. |
| 6 | L-D5 / C-D-8 | MINOR (latent) | SGR 22 cancels bold and dim alike, so same-category nesting loses the outer attribute; every live nesting is cross-category, so zero live impact. | ansi.ts:8-9 both close `\x1b[22m` (read whole by this seat); L's 12-site nesting enumeration verified by the reader, spot-held here (:78-79, :152-:156, :160). | **NO-WAVE-OWNER** — latent craft record; the only cure is a nesting-aware stack a 17-line module rightly declines. Revives if anyone writes `bold(dim(…))`. |
| 7 | L-D6 | MINOR | `enabled` unexported (consumer cannot ask "will this be coloured?"); caret math is UTF-16 code-unit based with no width primitive (CJK/tab/emoji misalignment). | greps 0 for export/stringWidth; debug.ts:83/:116 pads. NOTE: the dominant ASCII misalignment on the same two lines is row 13, filed separately — this row is the second-order case only. | **NO-WAVE-OWNER** — the export half dissolves under row 1's W2 injected policy; the width half is a render-tier record. |
| 8 | L-D7 | INFO | Header comment overstates: NO_COLOR respect is one-shot, TTY detected is stderr-only, FORCE_COLOR and TERM unmentioned because unimplemented. | ansi.ts:1 verbatim; rows 1/2/15 supply the limbs. | **NO-WAVE-OWNER** — dies with the injected-policy cure. |
| 9 | L-D8 | INFO | Background closers restore terminal default (49), not ambient background; no consumer establishes an ambient background. | ansi.ts:16-17; badge sites flat (:152-:156, :201). | **NO-WAVE-OWNER** — composition record, zero live impact. |
| 10 | C-B2 | **MAJOR** (corrected from BLOCKER; C's own :107 concession) | Un-shakeable Node-TTY payload rides 4 of 5 entries into a browser-only consume path where `enabled` is compile-time false, for zero consumed exports — pinned by class prototypes (`state.ts:136-138`, `parser.ts:690-700`), which `sideEffects:false` cannot reach. | Chunk carried by {core,parse,packrat}.js:1 + utils.js:2; diagnostics.js clean (NO_COLOR 0); escape count 18 = 9×2; value.js deps parse-that-free; css.js 43,973 bytes; region 28-285 = 8,517/40,576 = **21.0%** (this seat's own measures — C's 40,562 and "byte-parity" killed, K9). Minified figures NOT re-derived (MEASURE-AT-OPEN row 1). | **W4** — W4.a seam contract + W4.b/RC-P: prototype welds removed in favour of free functions, renderer re-homed behind a sixth `./debug` entry (2.0.0 precedent: the 1.0.0 `*Span` excision, index.ts:10-12). Acceptance probe rides row 14's corrected spelling. |
| 11 | C-D-6 | **MAJOR** | Tier inversion: the primitive tiers ship the renderer; `/diagnostics` — the tier that wants a formatter — does not. `core.ts:3-5` is false on **two of three** clauses: the core chunk carries the diagnostics accumulator (5 symbols, chunk line 1) AND the packrat tier (`makeMemoized` :721, `memoize` :844, `PACKRAT_ARMED` :678/:682/:714/:722) — only the json/csv clause survives. | All five entry rows re-verified by this seat; chunk imports and packrat lines pinned above. | **W4** — RC-P chunk-topology unit; any wave touching core.ts must correct the :3-5 comment to the shipped truth. |
| 12 | C-B1 (substance, re-attributed) | **MAJOR** as W1/W2 harness finding · INFO as an `ansi` finding | R-LAW-3's probe patches only `console.error`/`console.warn`; G-5 redirects and asserts stderr only. A lowering that prints via `Parser#debug()`'s `console.log` default passes both while printing on every corpus input. The hole survives ansi's deletion (K4) — it is a property of the gates' fd choice + parser.ts:693. W2.md:725 rules the class: "a probe that cannot fail for its intended reason is itself a defect." | W1.md:483-492 re-read (stderr-only, stdout unredirected and unread); W2.md:282/:722-725 re-read; parser.ts:693; `console.log` in W1/W2 → 0 hits (reader's grep, held). | **W1 (G-5)** — capture and assert BOTH descriptors, byte-empty each · **+ W2 (R-LAW-3)** — probe patches `console.log` as well. The strongest wave-owned row in this corpus. |
| 13 | M-1 (reader's, adopted **with scope corrected**) | **MAJOR** | The caret is drawn at the pre-truncation column against a line `summarizeLine` has re-windowed — **whenever the line exceeds 74 chars AND the failure column > 37** (i.e. `start > 0`); the `start === 0` branch aligns correctly, so "every failure display for a long line" (reader's phrasing) overstates. In the biting region the caret lands up to `start` cells right of the target, routinely past the rendered line's end. The common case for minified CSS/JSON single-line documents — precisely the X·P corpus shape. | debug.ts:72 truncates with `columnNum`; :83 pads with the ORIGINAL `columnNum`; summarizeLine :14-31: `start>0` → `"..." + slice(start,end)`, accused char now at `3+(columnNum-start)`. Traces re-derived by this seat: len 200/col 100 → rendered 80 chars, target idx 40, caret at 100 (60 right, 20 past end); len 100/col 90 → rendered 50, target 40, caret at 90. Both challenges inspected :83/:116 and missed it (L-D6 saw only display width; C never examined the caret). | **NO-WAVE-OWNER** — render tier. Recorded FIRST among the no-owner rows: any relocation wave cures it by shifting the caret by `start` (and marking the ellipsis offset), or the failure display keeps lying about where failures are. |
| 14 | M-2 (reader's, adopted) | **MAJOR** | C-B2's prescribed acceptance probe is case-blind and returns GREEN on a bundle containing the entire payload: source spells `\x1b` lowercase, Rollup ships `\x1B` uppercase. | Measured by this seat on the live artifact: `grep -c 'x1b'` → **0** (exit 1) while `grep -oi 'x1b' | wc -l` → **18** on the chunk carrying all nine arrows. challenge-C:105 prescribes the broken spelling; challenge-L:179 uses the working one — the two documents disagree and only one probe can fail. W2.md:725 rules the class. | **X·P REFINEMENT FOLD** — the adoption-wave acceptance spec is sharpened to `grep -ci 'x1b\['` or a raw U+001B byte match. Binds the value.js adoption wave's gate before it first runs. |
| 15 | M-3 (reader's, adopted) | MINOR | `enabled` ignores `TERM` and never consults Node's own capability API — SGR emitted into TTYs that cannot render it (`TERM=dumb`: Emacs shells, CI shells present real TTYs). `process.stderr.hasColors()` folds isTTY+TERM+FORCE_COLOR+depth in one zero-dep call, discharging row 2's missing limb simultaneously. | `grep -rnE 'TERM\b|hasColors|colorDepth' src/` → 0 (this seat). Both challenges bank NO_COLOR-to-spec as a superlative while the TTY half of the same convention is incomplete; neither says TERM once. | **NO-WAVE-OWNER** — dissolves under row 1's W2 injected policy; if an ambient default survives, `hasColors()` is the cure. |
| 16 | M-4 (reader's, adopted **with coupling corrected**) | MINOR | `debugDepth` is never restored on a throw — no try/finally around :358-:379 — so a throwing wrapped parser or logger leaves the module global permanently incremented and every later render mis-indents forever. CORRECTION (this seat): the R-LAW-3 probe patches `console.error`/`warn` only, and the sole public route `Parser#debug()` defaults to `console.log` — so the probe cannot trip this on the public path **until row 12's cure lands, at which point the patched `console.log` throw arms exactly this leak**. The two rows interlock: curing 12 makes 16 live. Direct `parserDebug` (in-tree lowering work) trips it today via the :355 `console.error` default. | debug.ts:12 global; :358 `debugDepth++` … :361 `parser.parser(state)` … :377 `logger(indented)` … :379 `debugDepth--`, no try (read whole by this seat); throw source :337 bare `Error` reachable when `recursivePrint`. | **NO-WAVE-OWNER** for the one-line try/finally cure; a W1 probe-suite rider so the purity probe's own throw does not corrupt later renders (MEASURE-AT-OPEN row 5). |
| 17 | M-5 (reader's, adopted) | MINOR | The two gutter-width formulas disagree — `addCursor` has no floor, `formatSecondarySpans` floors at 3 — and statePrint concatenates their output into one block, so secondary markers sit offset from the primary caret for any source under 100 lines. | debug.ts:65 → :49-51 `String(maxLine).length`; :110 `Math.max(…, 3)`; concatenation :182. All pinned by this seat. | **NO-WAVE-OWNER** — render tier; one formula, one constant. |
| 18 | M-6 (reader's, adopted) | MINOR (latent) | `parserPrint` treats `id === 0` as absent while `PARSER_ID` starts at 0: the lazy cycle-cut (:320-:326 `else return name`) is never taken for an id-0 resolved parser → unbounded recursion; and the top-level walk passes `id` undefined throughout (:345), so NO intermediate node is memoised — public `toString()` is super-linear on a shared-node DAG. Latent only because module-init parsers (parser.ts:711 `_initWhitespace()` + leaf.ts module-level) consume id 0 today; evaluation order is not a contract. | parser.ts:18/:25; debug.ts:257 guard, :320 `if (!id)`, :339 `if (id)` memo write, :345 top-level call — all pinned by this seat. Cross-ref: parser-band.md **:118** debt 3 ("Recursion bounded by construction, not by catch") — the same debt on the print path is unrecorded there (reader cited :114; that is the section heading — corrected). | **NO-WAVE-OWNER** — render tier; cure = truthy-id discipline (`id !== undefined`) + memoise the top-level walk. Recorded beside parser-band debt 3 as its print-path sibling. |
| 19 | M-7 (reader's, adopted) | INFO | `formatSecondarySpans` emits untruncated source lines — `MAX_LINE_WIDTH` unenforced on the secondary path; a one-line 1 MB document renders 74 chars primary, 1 MB secondary, same block. | debug.ts:114 raw `${lines[i]}` vs :72's summarizeLine route; pinned by this seat. | **NO-WAVE-OWNER** — render tier; same three lines as rows 13/17. |
| 20 | M-8 (reader's, adopted) | INFO | The suite's `stripAnsi` is SGR-only (`/\x1b\[[0-9;]*m/g`) and cannot strip the payloads row 3 warns about (`\x1b]0;…\x07` OSC-0, `\x1b[2J` CSI-J) — and it is the de-facto reference both challenges point downstream consumers at. | test/debug.test.ts:22 read by this seat; L:158's own injection specimens unmatched by it. | **NO-WAVE-OWNER** — rides row 3's strip-scope spec: any exported strip covers OSC + full CSI final-byte range. |

## Seat corrections (the incredulity seat's own findings — against the READER)

Four corrections of degree; zero verdict flips. The hostile re-read is otherwise exact at every byte this seat re-measured — an unusually clean record.

1. **M-1 scope** — "every failure display for a source line longer than 74 characters points at the wrong place" is overbroad: the `start === 0` branch (column ≤ 37) is correctly aligned; the defect bites exactly when `start > 0`. Adopted into row 13 as corrected. Severity unchanged (long-line failures at column ≤ 37 are the rare case for the minified corpus).
2. **M-4 coupling** — "running the mandated purity probe against any lowering that reaches `parserDebug` corrupts `debugDepth` on the first emission" holds only for **direct** `parserDebug` invocation (the :355 `console.error` default, unreachable from any barrel); the public `Parser#debug()` path defaults to `console.log`, which R-LAW-3's probe leaves unpatched — that unpatched path is row 12's own finding. The interlock (curing 12 arms 16) is this seat's addition and is now part of both rows.
3. **Measurement charity** — the reader called C's 8,512-byte region figure reproducing "within the newline-boundary convention"; the delta to this seat's 8,517 (`sed -n '28,285p' | wc -c`) is 5 bytes, which no newline convention explains. C's figure simply does not reproduce; the reader's own does. The 21.0% ratio conclusion is unaffected.
4. **Cite slip** — M-6 cites parser-band.md:114 for debt 3; live, :114 is the "WHAT CAND-O OWES CAND-F" heading and the debt-3 text is at **:118** (verified). Substance exact.

## Killed-claims register (all 9 sustained by this seat's own re-measures)

| id | killed claim | this seat's decider |
|---|---|---|
| K1 | C §2: "confirmed verbatim … `packrat-entry-CS1td-8B.js:881` is `console.error(this.state.toString())`" | `sed -n '876,886p'`: :881 = `if (isDiagnosticsEnabled()) {`, :882 = the emission. C restated INBOX O-15's off-by-one as independent verification; L:49 had already refined it correctly. Substance (PT-01 coupling) unaffected. |
| K2 | C header: "debug.ts, 340 lines" + the cite set :82/:84/:129/:131/:136/:138/:140/:265 | `wc -l` = **383**; every cited line re-read wrong (:82 `if (cursor) {`, :84 cursorStr, :265 `: "unknown";` — the real logger default is :355). C-D-4/C-D-8's substance survives at L's exact cites; C's provenance does not, and D-8's "verified line-by-line" claim rests on this set. |
| K3 | C §2: core bundle contains `addUnclosedDelimiterSuggestion` "verbatim, minified" | Symbol exists nowhere (grep src/dist/test → 0). Chunk line 1 imports `reportUnclosedDelimiter` — a different symbol — plus the three C did name. Conclusion survives (understated, see row 11); one of four named evidence pieces is fabricated. |
| K4 | C-B1's "why this is ansi's finding": the stream is load-bearing only because ansi made it so | Delete ansi.ts and a plain-text `console.log` lowering still passes G-5 (stderr-only redirect) and R-LAW-3 (error/warn-only patch) identically. The blindness is the gates' fd choice + parser.ts:693, independent of ansi.ts:5. Killed as an ansi BLOCKER; confirmed as row 12. |
| K5 | L §1: waves+parser-band grep for `ansi\|ANSI\|isTTY\|NO_COLOR` → "zero hits" | This seat: 19 case-insensitive lines (exp**ansi**on/tr**ansi**tively); substantive hits after exclusion → 0. The conclusion (virgin ground) is true; the stated grep result is false as written. |
| K6 | L §Target: "18 lines incl. trailing newline; the delta is the header comment" | `wc -l` → **17**, header comment inside them. Both the corrected count and its rationale wrong; the brief's ~17 was right; C:5 said 17 correctly. |
| K7 | L-D4: orphan set "debug, lazy, leaf, index" | `ls dist` whole: `index.d.ts` is the routed `types` target of `exports["."]` — not an orphan; `parser/split/state.d.ts` are orphans and went unnamed. True set = {ansi, debug, lazy, leaf, parser, split, state}, adopted in row 4. |
| K8 | L-S4: V8 script-context const-folding mechanism + factory refactor "strictly worse" | ESM module context, not script context; version-dependent, unmeasured, and the challenge's own law forbids the settling bench (MEASURE-AT-OPEN row 6). Factory's disabled arm compiles to the same `return s`; ten closures once at load, not per call. Shape claim survives as S4. |
| K9 | C-B2 provenance: chunk = 40,562 bytes; css.js 43,973 vs 43,972 = "byte-parity" | This seat: `wc -c` → **40,576** (neither convention yields 40,562); css.js → **43,973**, and one byte off is not parity — the stated ground for "measured against what ships" is unestablished. Ratio conclusion (21.0%) survives on this seat's numerators. |

## Superlatives (banked, L-18 both ways)

- **S1** — conjunction order makes the un-chained `process.env` read unreachable in shim environments; the `?.` on `stderr` is one character between `false` and a module-evaluation `TypeError` that would take down four of five subpaths in every browser bundle (the expression ships verbatim, dist:28 ESM / :29 CJS — this seat re-read both trees' spelling). `=== true` is the same discipline's second half.
- **S2** — `!process.env.NO_COLOR` is the current no-color.org rule on both edges implementations miss (`""` → colour stays on; `"0"` → off). Arithmetic on `!`, no run needed. Scope limit stands: opt-out half only — the TTY half ignores TERM (row 15).
- **S3** — the disabled arm returns the caller's string by reference: zero allocation on the path every piped/CI/vitest process actually takes. Verified in the shipped ternaries dist:29-37.
- **S4** — ten flat top-level arrows, one `(string) => string` signature, zero shared mutable state; monomorphic by construction. The V8 folding mechanism is killed (K8); the shape stands on the source alone.
- **S5** — attribute-specific closers (22/23/39/49), never `[0m` — verified absent from the source by this seat's read; it is what makes all four live cross-category nestings render correctly. The SGR-22 residual (row 6) is ECMA-48's, unfixable by closer choice.
- **S6** — perfect export/consumer parity at the source tier (10/10, one importer, one import statement, debug.ts:5); the sibling surface needed a permanent gate for its dead surface (index.ts:10-12 scar), ansi never did. Correction holds: 9/10 in the artifact — `dim` is dead in the ship.
- **S7** — genuine zero-import leaf; `sideEffects:false` honest for this file; and per-symbol shakeability is proven by the shipped artifact itself (Rollup already shook `dim` and `formatDiagnostic` — grep absent, this seat). The nine survivors are pinned by debug.ts and the class prototypes, not by anything ansi does. Relocation is cheap because the module is this clean.
- **Reader's LAW/STOP record** — clean, adopted: no forbidden root, no bench, nothing armed, prober read-not-run.

## MEASURE-AT-OPEN residue (bench-dependent rows for the lane's W1 harness)

1. **C-B2 minified figures** (3,509-byte ansi+debug region / 13,883 total / 25.3%) — esbuild re-bundle, not statically derivable; re-run at W1 open (or the adoption wave's open) with the command C pasted, before any byte figure is banked forward.
2. **Adoption acceptance probe** — build value.js `./css` against a parse-that substrate; probe with row 14's corrected spelling (`grep -ci 'x1b\['` or raw U+001B) → expect 0. Never run; C stated it as unrun and this record keeps it so.
3. **The `enabled === true` arm has never been observed in any session** (every seat's stderr was piped). One W1-adjacent cell under a real PTY witnesses both of row 1's redirection directions (`> file` polluted; `2> file` de-coloured) — currently provenance-only.
4. **`parsethat-surface-gaps.mjs` RED-7 figure** — the prober arms diagnostics (PT-01) and constructs memoized parsers (PT-03 one-way latch); statically unverifiable, run only in a disposable process. Its zero mention of ansi is verified statically (grep -ci → 0, this seat).
5. **Row 16 leak witness** — two-emission probe under a throwing logger, disposable process only (module-global `debugDepth` mutation); belongs beside R-LAW-3's probe in the W1 suite the day row 12's cure lands.
6. **K8's V8 question** — measure only if the factory refactor is ever re-proposed; not load-bearing for any row above.

## Closing verdict

**No blocker survives on `ansi`.** The 17 lines are the cleanest micro-craft in `src/parse/` — seven banked superlatives, several rarer than they read — and every severe finding in the corpus is a **seam** property: the policy seam (ambient stderr oracle vs two sinks — W2's injected-policy ruling owns the cure), the packaging seam (prototype-welded renderer in 4/5 entries, phantom typed surface, inverted tiers — W4's RC-P owns it), and the gate seam (stdout-blind purity probes — W1 G-5 + W2 R-LAW-3 own it, and it was never ansi's defect at all). One spec sharpening leaves as the REFINEMENT FOLD's (the case-correct acceptance probe, row 14, plus the G-6 `String(state)` rider from row 1). Ten rows have **no wave owner** because the algebra itself quarantines rendering as non-normative — they are recorded here, sharpest first (the caret mis-window, row 13, which both hostile challenges walked past while citing its exact lines), so the render-tier relocation inherits a complete debt roster instead of a rediscovery project. The two challenge documents earn opposite grades on the same evidence: L is nearly byte-exact with three self-downgrades that all held; C's substance is strong but its provenance failed five separate re-measures (K1, K2, K3, K9 twice) — the fleet lesson, matching the ColorSpaceSelector DISSENT precedent, is that verification claims inherit nothing from the conclusions they decorate.

— end of apotheosis. Both challenge records and the hostile re-read remain immutable beside this file per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.
