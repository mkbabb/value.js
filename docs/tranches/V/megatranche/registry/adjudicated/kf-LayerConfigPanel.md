Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component LayerConfigPanel)

# kf-LayerConfigPanel — adjudicated apotheosis

**Date** 2026-08-10 · **Corpus** `audit/kf-components/LayerConfigPanel/challenge-{L-library,C-consumption,D-design}.md` (211/241/314 lines, tree cited as keyframes.js `master` HEAD `8281638c`) · **Re-readers adjudicated**: design-and-usage (RR-1) + library-and-code (RR-2), both hostile — RR-1: 17 confirmation cells · 4 kills · 7 misses; RR-2: 21 confirmation cells · 4 kills · 7 misses. The two returns **disagree at verdict level on five rows** (the manifest regime, the blast radius, the D-S-E rename provenance, the L·M-3 zIndex leg, D-12's classing) — all five ruled below by this seat's own instruments.
**Registry tree**: value.js `tranche-u`. **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` — see the substrate ruling, which decides three of RR-2's kills and both readers' largest miss.

**Substrate ruling (this seat's own git, re-run this session — it reproduces the kf-ChannelOptions/kf-MatrixEditor trifecta verbatim and OVERTURNS RR-2's frontier-blind kills).** Local HEAD = the SACRED stale `8281638c`; `origin/master` = `81a56990` (**41 ahead**, my `rev-list --count`). Manifest trifecta, all three coordinates probed: worktree = **no glass-ui declaration at all** (uncommitted deletion; lock grep 0) · local HEAD = `optionalDependencies: {"@mkbabb/glass-ui": "6.0.0"}` (:70-72) · `origin/master` = `devDependencies: {"@mkbabb/glass-ui": "7.0.0"}` **exact** (:77), lock-resolved `https://registry.npmjs.org/@mkbabb/glass-ui/-/glass-ui-7.0.0.tgz` with integrity hash — **matching the installed 7.0.0** (my node read). And the component: `git diff origin/master --stat -- …/channel-controls/` returns **zero LayerConfigPanel lines** (sole diffs: TimingFunctionPanel.vue + its composable — the known EE-02 lag) — **the audited file is byte-identical worktree ↔ `origin/master`**, and `origin/master` carries `COMPOSITE_OPERATORS = ["replace","add","accumulate"]` while local HEAD still carries `BLEND_MODES = ["replace","add","weighted"]` (my `git show` at both coordinates). Consequences: the corpus audited **landed frontier content against exactly the glass version the frontier pins**; RR-2's three regime-based rulings — honest at local HEAD — each stopped **one baseline short**, the same error the kf-MatrixEditor seat recorded as a pattern ("a dirty-worktree claim without naming the comparison baseline is worthless in this repo"), now a **third occurrence at re-reader tier**.

**Method (incredulity).** Every verdict-level disagreement re-derived at the bytes; every kill re-executed at its decisive file; every filed miss checked against the registry before admission — which mattered: **the host component kf-ChannelOptions (2026-08-07, + addendum R2) had already banked the majority of this corpus**, and neither re-reader consulted it. Zero browser tooling; sole write = this file. **Instruments this session (all read-only)**: git substrate probes above incl. `git show` of the file at both coordinates; `LayerConfigPanel.vue` whole (92 L, worktree ≡ frontier); engine deciders `group.ts:155-235` (singleTarget predicate :159-161 + second recompute :201, zIndex-sorted `getEntries` :169-178, render routing :224-236), `entries.ts:68-105` (`if (!entry.layer.enabled) continue` at :73; `renderMultiTarget` :91-100 reads `entry.animation` only), `layer-api.ts:26-38` (`requireEntry` throw + bare `Object.assign(entry.layer, config); group.invalidateEntries()`), `weight.ts:1-22` (`isWeightBlend` replace-arm; `weightSpring?.value ?? weight`), `compositor.ts:100`/`soa.ts:70`/`waapi.ts:31` enabled/singleTarget reads (grep); demo chain `App.vue:216-220` (`shallowRef(markRaw(…))` + the MED-6 comment), `useSceneMachineShellBinding.ts:73-95` (markRaw ×3), `ControlsPaneWrapper.vue:200-230` (`controlHosts` computed over raw `.animations[…]?.layer`), `AnimationControlsGroup.vue:18-24` (`:blend-available="animationGroup.singleTarget"`), `useAnimationGroupActions.ts:38-48` (bare `getGroup().setLayerConfig`); `ChannelOptions.vue` :148-166 (live Tooltip idiom), :325-340 (class-toggle collapse), :355-370 (guarded mount), :548-585 (collapse CSS — no inert/aria-hidden/visibility, grep 0); `design-idioms.css:248-275` (§LABEL-subgrid whole — container 2-track, subgrid row, `.labeled-field-error` 1/-1 span); `style.css` import order (:3 glass, :14 idioms); installed glass-ui 7.0.0: `labeled-field/types.d.ts` (CommonProps interface :7-13, members :8-12), `input/types.d.ts` (**`type` union at :21** — ruling the readers' 21-vs-22 divergence), compiled `labeled-field.js` description placement (`j = {class:"labeled-field-copy"}` wrapping label + description — the cure-geometry decider), glass-ui.css labeled-field rules (**`grid-template-columns` count = 1, `[data-layout=horizontal]` only** — the D-15 decider), **`dist/styles/utilities/a11y-overrides.css` PRM block read whole** (`*:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important }` — the R1-miss-6 killer) **plus its reachability chain traced hostile** (`dist/styles/utilities.css` does NOT import it — the file rides `dist/styles/accessibility.css` line 1, the TAIL import of `dist/styles/index.css` = the `./styles` export, consumed at `demo/styles/style.css:3`); `animationDescriptions.ts:120-129` (map at :123-127, imports at :128-129, 129 L); keyframes.js's own `docs/tranches/V/AUDIT-REGISTRY.md:15` (FAM-01 RAIL verbatim) + `V.md:73-74,91-95` (W2 blocked, producer-gated) + commits `cda6a1a5`/`8281638c` messages; identity-guard reads: kf-ChannelOptions whole incl. addendum R2, kf-MatrixEditor whole, kf-CubeScene/kf-ChannelControls/kf-KeyframeCardList/kf-AnimationControlsGroup/kf-EasingSidebar/kf-ControlsPaneWrapper/kf-App targeted greps; registry-wide greps `setLayerConfig|layer-api|invalidateEntries|weightSpring` → **0 hits** (the LP-1 net-new decider) and `markRaw` (5 records, none owning the layer edge).

**Identity law applied throughout**: the LayerConfigPanel surface was substantially pre-banked at **kf-ChannelOptions** — KF-CO-1 (controlled-shut selects, `LayerConfigPanel.vue:11` named in its blast list, + GLASS-OWNED producer half), KF-CO-2/KF-ES-10 (the phantom-prop FAMILY, LayerConfigPanel's `tooltip` ×5 and `:descriptions` counted in the banked census: 4 files, tooltip ×13), KF-CO-5/-46 (collapsed rows keep the tab order; the inert cure-rider), KF-CO-7 ≡ KF-APP-4 (no vue-tsc), KF-CO-8 (the dead switch), KF-CO-35 (`parseInt||0` + `@change` divergence), KF-CO-37 (the stale `:deep` header comment), KF-CO-40/N-8 (bottom-of-file imports in animationDescriptions), KF-CO-47 (the tooltip→description cure-rider) — plus census F-1/SCH-1 (→ KF.W0 §B-12, FALSIFIED-AT-HEAD at kf-AmigaScene, re-ruled at kf-CubeScene/kf-MatrixEditor/kf-ChannelOptions), census S-9 (≡ kf-CubeScene L-5/C-6, the number-field shadow row), kf-CubeScene C-19a (subpath policy), kf-MatrixEditor K1 (the CI test-project hole). This record **never re-books** those identities; it attaches sharper component-scope evidence as riders and books only what is genuinely net-new.

## Scoreboard

**24 adjudicated roster rows — 3 BLOCKER (1 net-new) · 6 MAJOR (2 net-new) · 11 MINOR · 4 INFO — of which 9 are FOLD-BY-REFERENCE to banked identities.** **7 killed-claims register entries: 3 reader kills SUSTAINED (one discharging an SS-13 row) · 1 SUSTAINED-AS-CORRECTED · 2 RR-2 kills OVERTURNED/SPLIT at the frontier by this seat · 1 kill of RR-1's own miss (the PRM claim, dead at the vendor bytes).** **13 filed misses adjudicated: 7 ADMITTED (5 as riders) · 3 PRE-BANKED in the registry the readers did not read · 1 KILLED · 1 folded to census · 1 to the cite-drift register.** **9 superlatives sustained (one already banked at the host).** **6 UNPROVEN residues: 4 → SS-13, 1 rerouted build-level, 1 accepted-as-INFO.**

Headline: **LP-1 — axis L alone found that the panel has no write→render edge at all** (raw layer object off a `markRaw`'d, `shallowRef`-held group; the engine write is an in-place `Object.assign`; every glass primitive here resolves to reka's controlled mode) — net-new against the whole registry (my grep: no banked record owns the layer-config reactivity edge), BLOCKER sustained, and it **conditions the repair sequencing of the banked KF-CO-1/KF-CO-8 cures**: restoring the write legs without the render edge ships fresh staleness. The re-readers' most instructive failures are symmetric: RR-2 built a meticulous three-regime theory of the manifest and never probed `origin/master`, where the regime question dissolves; RR-1 filed two "no axis looked" misses that the host record had already banked — both seats read the tree and not the bank.

## The incredulity seat's own rulings

1. **RR-2's blast-radius kill ("broken on the author's machine only; CI installs 6.0.0 where the component is correct as authored") — OVERTURNED at the frontier.** `origin/master` pins devDep **7.0.0 exact**, registry-resolved with integrity hash (my `git show` of both manifest and lock); installed = pinned; `npm ci` from the frontier installs the exact tree every prop verdict was measured against. The four prop findings (LP-2/3/4/5) are **live breakage of any frontier build**, not machine-local artifacts. RR-2's producer-v6 facts (v6.0.0 declared `checked`/`isOpen`/`descriptions`/`tooltip`) are adopted as *history* — they date the breakage to the 6→7 API move — not as a severity cap. ≡ the kf-MatrixEditor K11 ruling shape, third occurrence.
2. **RR-2's F-1-premise kill — SUSTAINED-AS-CORRECTED.** The axes' shared "phantom dependency at HEAD `8281638c`" premise is false as stated (local HEAD carries the 6.0.0 optionalDep; only the uncommitted worktree deletes it) — but RR-2's replacement story ("regime 1: HEAD pins 6.0.0, the component WORKS, the findings evaporate") is equally false at the frontier. ONE identity: **census F-1/SCH-1, FALSIFIED-AT-HEAD**, ruled four times already (kf-AmigaScene, kf-CubeScene, kf-ChannelOptions, kf-MatrixEditor) → **FOLD KF.W0 §B-12**, never re-booked. RR-2's genuinely admitted rider: keyframes.js's own tranche V pre-booked this condition as **FAM-01 RAIL / H→V-01 / W2 producer-gated** (my read of AUDIT-REGISTRY.md:15 + V.md:74 confirms verbatim) — the worktree's deleted pin is a deliberate in-flight migration state, which corrects the axes' "nobody noticed for three majors" causal story.
3. **RR-2's D-S-E kill — SPLIT.** Its provenance mechanism ("the `BLEND_MODES`→`COMPOSITE_OPERATORS` rename is an UNCOMMITTED working-tree edit… the tree moved and has not been committed") is **overturned**: the rename is landed at `origin/master` (my `git show`); axis D's tree-state assertion was true of the frontier the corpus actually audited. But the kill's doc-defense half **stands on its own law**: `G.W17.md:18` is a dated wave record of the tranche-G file state (my read — it documents the "UI over a no-op" defect that wave found, citing the then-live `BLEND_MODES` at `:55`), and `U.C.md:752` records the U-era compat plan; **dated evidence is never rewritten**, so D's "the later lanes should stop citing BLEND_MODES / those docs are stale" indictment stays dead. Net: the docs need no correction; the rename is landed; only D's demand dies.
4. **The L·M-3 zIndex leg — RR-1's kill SUSTAINED at the engine bytes; RR-2's whole-row confirmation corrected.** `singleTarget` is `animations.every(a => a.targets[0] === animations[0]?.targets[0])` (group.ts:159-161) — false the moment ONE child differs, so a multi-target group may still contain target-sharing subsets. `renderMultiTarget` (entries.ts:91-100) iterates `getEntries()`, which returns entries **sorted by `layer.zIndex`** (group.ts:169-178, my read of both), and applies each child's frames in that order — last-writer-wins on any shared property is still zIndex-governed. "zIndex is inert on a multi-target group" over-claims; restated: **inert only under fully target-disjoint groups**. The `enabled` leg is airtight and books as LP-6 (waapi.ts:31 bails on `!group.singleTarget` before any layer read; all four `layer.enabled` reads are composite-path — my greps).
5. **D-15's cascade-fragility sub-claim — RR-1's kill SUSTAINED by this seat's own CSS read, and RR-2's UNPROVEN-NEEDS-LIVE tag DISCHARGED** (a kill that removes an SS-13 row). The installed glass-ui.css declares `grid-template-columns` for labeled-field **exactly once, under `[data-layout=horizontal]`** (unused here — my grep, count 1); the bare `.labeled-field[data-v]` rules carry only `min-inline-size:0; display:grid` + `gap`/`inline-size`. The demo's `.labeled-field-grid > .labeled-field` (design-idioms.css:262-267) sets `display:grid` (identical value) + `grid-template-columns:subgrid` + `grid-column:1/-1` + `align-items:center` — **no property on which the two rules disagree**, so import order and specificity cannot change a single computed value. The stated hazard is fictional; the surrounding INFO survives.
6. **RR-1's PRM miss — KILLED by this seat at the vendor bytes, reachability proven.** `dist/styles/utilities/a11y-overrides.css` ships a **global, unlayered, `!important`** PRM block: `*:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important }` — `grid-template-rows` is excluded from the property allow-list, so the `.panel-row` size morph is **removed** under PRM and only a 0.1 s opacity fade survives. And the file is IN the demo's cascade — a chain this seat traced hostile because the obvious road is a dead end: `dist/styles/utilities.css` does **not** import it; it rides `dist/styles/accessibility.css` (its first line), which is the tail import of `dist/styles/index.css` = the `./styles` export the demo consumes at `demo/styles/style.css:3`. RR-1 read scheme-motion.css, scheme-spring.css and transitions.css and missed the fourth file — the same allow-list two banked records already ruled on (the kf-ChannelOptions "PRM honest by delegation" superlative names it; kf-AnimationControlsGroup S-4 re-proved it). A registry read would have prevented the filing.
7. **RR-1's collapsed-pane focusability miss — mechanism CONFIRMED (my ChannelOptions reads: class-toggle collapse, no `v-if`, greps 0 for inert/aria-hidden/visibility) but PRE-BANKED ≡ KF-CO-5 (BLOCKER) with the KF-CO-46 cure-rider** — which moreover rules RR-1's proposed one-attribute `:inert` cure structurally incomplete (find-in-page residual; `content-visibility` incompatible with the 0fr↔1fr morph). Second registry-blindness event in one return.
8. **The S-9 collision (RR-2's miss, ADMITTED as a ruling).** Axis D's "propose S-9" for the hand-rolled number control is **convergent with the EXISTING census S-9** (≡ kf-CubeScene L-5/C-6, already consumed by the kf-MatrixEditor ME-18 fold) — no collision in substance, folds there. Axis C's "S-9 · consumed-but-mis-called" is a **genuinely distinct class** (right primitive, right subpath, stale prop vocabulary — type specimen this file) that must NOT take the occupied id: routed to **SS-1/SS-2 as a fresh-id census candidate** ("consumed-but-mis-called"; membership = the banked KF-CO-2/KF-ES-10 family census: LayerConfigPanel, ChannelOptions, EasingSidebar, SpringPhysicsFacet). Whichever id SS-1 mints, both challenge docs' "S-9" citations must be read through this ruling.
9. **D-11 — BOTH re-readers dropped it without adjudication; coverage closed by this seat.** `text-right` at :24 is a physical property in an otherwise logical-property context — trivially confirmed at the file, class-identical to the banked **KF-CO-32** (physical `text-right`/`ml-auto` in an RTL-ready vendor context). FOLD, MINOR-latent, inside LP-20. Recorded because a re-read that silently loses a row is exactly what re-reads exist to prevent.
10. **Cite-drift register (K9-style; no substance moved).** The `type` union sits at `input/types.d.ts:21` (**RR-1's count right; the axes' and RR-2's `:22` is the closing brace** — my sed); `LabeledFieldCommonProps` = interface :7-13, members :8-12 (RR-1 right; D's ":8-14" drifts by one); D-12's config lives at `.prettierrc.json`, not a package.json `"prettier"` key (both readers agree; the 8-line over-80 census reproduced byte-identically by both — 2(87) 4(84) 5(84) 32(82) 45(119) 57(82) 74(107) 77(94)); C's root-barrel import count 41 → RR-2 measures 44; C's optimizeDeps cite :365-372 → live :373-378; C's useAnimationGroupActions ":42-47" → live :41-46 (my read).

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** per the census wave sketch where a forming X·KF wave owns the surface; **OPTIONS-UNIT** = the proposed NO-WAVE-OWNER wave-surface banked at kf-ChannelOptions (SS-1/SS-2 spec authoring to consume, sequenced after KF.W4); dispositions are **ADJUDICATED, not VERIFIED**. Anchors bind to worktree ≡ `origin/master` content; re-anchor before any cure (KF.W0 law).

### BLOCKER

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **LP-1** | L·B-3 (+ RR-1 miss #5 / RR-2 miss #4 as rider) | **No write→render edge exists for this panel.** `layerConfig` is a plain object off a never-proxied graph: `App.vue:218` `shallowRef(markRaw(…))`, re-`markRaw`'d at every shell-binding assignment (useSceneMachineShellBinding.ts:75/:83/:92); `controlHosts` (ControlsPaneWrapper.vue:207-229) tracks only group identity, its `.layer` reads untracked; the engine write is `Object.assign(entry.layer, config); group.invalidateEntries()` (layer-api.ts:33-35) — `invalidateEntries` flips dirty flags only. Every controlled widget is frozen: the weight slider (reka controlled via `"model-value": [e.modelValue]`) cannot hold a new position **even for the user's own write**; the z-index `<Input>` escapes only by `useVModel {passive:!0}` accident. Sole incidental Vue dependency: `isOpen('blend')` called during render. **Rider (both readers' late finds, merged)**: `blendAvailable` = `animationGroup.singleTarget` (AnimationControlsGroup.vue:21) is the SAME untracked-read class, and `singleTarget` recomputes post-mount at group.ts:159/:201 — so **both operands** of the `:49` weight gate are stale-by-construction; the cure must cover the whole prop surface, not `layerConfig` alone | all links re-read by this seat this session; registry grep `setLayerConfig\|layer-api\|invalidateEntries` = 0 banked hits | **NET-NEW BANKED (axis L alone; both re-readers converged; neither D nor C mentions reactivity once). → OPTIONS-UNIT**, sequenced WITH the KF-CO-1/KF-CO-8 one-line repairs — the write legs must not be restored without the render edge. Class kin recorded, not folded: kf-ChannelControls L-16 ("live-but-untracked", milder) · KF-CO-15 (markRaw options polled). Witness → SS-13 #1 |
| **LP-2** | L·B-2 / C-2 (D-4's inverse) | The blend `<LabeledSelect>` can never open — `is-open` is not a 7.0.0 prop; the absent Boolean casts to `false`, is unconditionally forwarded, and pins reka's SelectRoot controlled-shut. `layerConfig.op` is unreachable from the UI | chain ≡ the banked KF-CO-1 derivation, re-executed in that record's addendum §A; C-2's jsdom probe corroborates | **FOLD ≡ KF-CO-1** (`LayerConfigPanel.vue:11` already named in its blast list; GLASS-OWNED producer half already on the BH relay). Rider banked (RR-2 miss #7): ChannelOptions uses `is-open` at TWO types in one file — booleans at :96/:120 (dead) and the function at :363 (live prop of this component) — the grep-camouflage that made the family look consistent |
| **LP-3** | D-2 / L·B-1 / C-1 | The `enabled` switch is dead in both directions: `:checked`/`@update:checked` unknown to the installed component; absent `modelValue` casts to `false` → controlled permanently OFF while the engine default is `enabled: true` (defaults.ts:91-96) | ≡ KF-CO-8's compiled-props derivation; the engine-default contrast is this corpus's contribution | **FOLD ≡ KF-CO-8** (OPTIONS-UNIT: `:model-value` + `@update:model-value`; BH-relay producer note already filed). SS-13 witness already booked there |

### MAJOR

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **LP-4** | D-1 / L·M-2 / C-4 | All five `tooltip="…"` props are dead against installed 7.0.0 — the component's entire explanatory layer renders nothing; the strings land as junk attributes on `.labeled-field` divs. Producer v6 declared and rendered the prop (axis L's `490cc46e` archaeology, adopted as dating evidence) | :15/:22/:36/:52/:63 re-read; `LabeledFieldCommonProps` :7-13 has no `tooltip`; `inheritAttrs` grep 0 | **FOLD ≡ KF-CO-2** via the KF-ES-10 census (4 files, `tooltip` ×13 — LayerConfigPanel's 5+0 already counted). **Cure-rider ADMITTED (RR-1 miss #1, geometry half net-new, sharpening the banked KF-CO-47)**: the compiled LabeledField renders `description` INSIDE `.labeled-field-copy` — **grid item #1, the shared `auto` label track of the §LABEL-subgrid idiom** — so the blind `tooltip`→`description` rename all three axes prescribe would pour 30-50-char strings into the uniform label column and squeeze every control in all four rows. Two in-tree sound cures exist: the explicit `<Tooltip><TooltipTrigger as-child>` idiom (ChannelOptions.vue:149-164, provider guaranteed — HEAD `8281638c` exists to mount it) or a demo span rule for `.labeled-field-description` matching the existing `.labeled-field-error` 1/-1 rule (design-idioms.css:268-270). Squeeze magnitude → SS-13 #2; per-field hover-vs-persistent decision = KF-CO-47's standing obligation |
| **LP-5** | D-3 / L·M-1 / C-3 | `:descriptions` is not a 7.0.0 prop; the three per-operator explanations never render; the object stringifies to `descriptions="[object Object]"` on the root div; `COMPOSITE_OPERATOR_DESCRIPTIONS` (animationDescriptions.ts:123-127, re-read) has exactly one, non-functioning consumer — dead code | grep census 3 hits (definition + :13 + :77); compiled item render is `{{item}}` only | **FOLD ≡ KF-CO-2** (the banked `:descriptions` ×3 count includes `LayerConfigPanel:13` per that record's killed-register #9). The dead-code fact is this corpus's rider; combined with LP-4, the blend row ships zero explanatory text at label, field and item level |
| **LP-6** | L·M-3 (enabled leg ONLY; zIndex leg killed — ruling 4) | The OD-U14 honesty fix (`cda6a1a5`, verified) covers 2 of 4 rows: on a multi-target group `renderMultiTarget` never touches `entry.layer` (entries.ts:91-100, my read), and even the WAAPI escape closes at waapi.ts:31 — so the `enabled` switch is exactly as inert as the blend selector the same commit made explicit, yet renders live and undimmed. Reachable today (SquareScene.vue:180, CopyButton.vue:96) | engine reads this session (roster header) | **NET-NEW MAJOR → OPTIONS-UNIT** (extend the `blendAvailable` disclosure posture to the enabled row, or author the divergence). L-ONLY find; neither sibling axis considered multi-target inertness |
| **LP-7** | D-14 / L·M-4 / C-5 | No gate can see any of the above: `check` is bare `tsc`, CI runs `check:lib`, vue-tsc exists nowhere; no test touches this component | ≡ the banked derivations; L·M-4's `wc -l` corroboration (LabeledField.vue.d.ts = 19 lines) re-used at LP-10 | **FOLD ≡ KF-CO-7 ≡ KF-APP-4 (→ KF.W4, booked once for the program).** **Rider ADMITTED (RR-2 miss #3, mechanism PRE-BANKED ≡ kf-MatrixEditor K1 + keyframes' own FAM-03/MR4)**: CI runs `test:lib` only and the vitest `library` project excludes `test/demo/**` — so C-5's prescribed cure ("a LayerConfigPanel.test.ts in the KfPillTabs idiom") produces a spec **no gate executes**; a `--project demo` CI step (= V.md's MR4) is a prerequisite of the cure, not an optional extra |
| **LP-8** | D-5 / C-7 (coercion limb = KF-CO-35) | The z-index row hand-rolls a number control (`type="number"` outside the declared union — **:21**, ruling 10; unbounded domain; `parseInt(…) \|\| 0` inventing data; blur-gated `@change` against live siblings) where 7.0.0 ships `/number-field` (73 exports; number-payload emit) | file re-read; dist listing per kf-MatrixEditor's corrected anchor (NumberField.vue.d.ts, no types.d.ts) | **FOLD, two banked identities**: the commit-contract/coercion limb ≡ **KF-CO-35**; the hand-rolled-where-`/number-field`-ships limb ≡ **census S-9** (≡ kf-CubeScene L-5/C-6; the ME-18 evaluate-not-mechanical-swap precedent → KF.W6). **S-9 id-collision ruling 8 applies** — D's "new" proposal converges on the existing row; C's distinct class goes to SS-1/SS-2 under a fresh id. Compounded by LP-1: the passive Input shows the typed text while the model holds the coerced value, with no re-render to correct it and no error surface (LP-11) |
| **LP-9** | D-6 | The blend row changes block-size (`--control-h-md` = 40px fine / 60px coarse vs a bare line box), inline alignment (`justify-between` vs `text-right`), and type scale (`.text-dropdown` scaled by `--ui-scale` vs unscaled `.text-small` → 67% of surrounding control text on every coarse pointer) when `blendAvailable` flips — three simultaneous discontinuities decided by a boolean outside the designer's control; no height floor exists on `.labeled-field-control` (RR-1's falsifier check, adopted) | token chain verified by both readers against the installed dist; direction static-certain | **NET-NEW MAJOR → OPTIONS-UNIT** (presentation leg: give the v-else arm the control-row box, or author the compact state). Composed magnitude → SS-13 #3 |

### MINOR

| id | merged ids | claim (adjudicated) | terminal disposition |
|---|---|---|---|
| **LP-10** | D-9 / L·m-1 / C-11 | Two false provenance citations in 92 lines: the header credits the deleted `.panel-content :deep(.labeled-field)` rule (tombstoned at ChannelOptions.vue:594-603; live mechanism = design-idioms.css §LABEL-subgrid) and :32-33 cites `LabeledField.vue.d.ts:19-26` — a 19-line file; the slot contract lives at `types.d.ts:18-26` | **FOLD ≡ KF-CO-37** (the `:deep` tombstone half, banked). Net-new riders attach: the d.ts mis-cite, and C-11's header enumeration ("blend / z-index / enabled") omitting the `weight` row :50 renders. Prose-truth; rides any wave touching the file |
| **LP-11** | D-7 | `:aria-errormessage="errorId"` is permanently inert — `errorId` resolves only when `invalid` AND an `#error` slot exist; the file binds neither; ARIA additionally requires `aria-invalid`. Glass 7's whole error apparatus (`invalid`, `errorLive`, `.labeled-field-error`) goes unreached — and LP-8's coercion is exactly the input this channel exists for | **NET-NEW → OPTIONS-UNIT** (lands with the KF-CO-35 coercion cure: validate, set `invalid`, feed `#error`) |
| **LP-12** | D-8 / L·m-4 | The terminal `<Separator class="my-1">` is non-decorative by default (`decorative:{default:!1}` → bare `role="separator"`, no accessible name) and separates content from nothing (last node of the last block; ChannelOptions closes immediately after). RR-1's half-width clearance adopted: design-idioms.css:271-273 spans it 1/-1, no layout defect | **NET-NEW → OPTIONS-UNIT** hygiene (delete, or `decorative` + host-owned placement). Class kin: kf-KeyframeCardList KC-19 (unnamed role=separator family) — cross-ref, distinct site |
| **LP-13** | D-10 / L·m-2 / C-9 | `v-if="layerConfig"` guards a prop declared required; the sole mount site already guards (ChannelOptions.vue:359); one level up the types are honest (`layerConfig?`). Either the guard is dead or the type lies — and the no-layer case renders literally nothing, inconsistent with the component's own designed-inert-state standard | **NET-NEW → OPTIONS-UNIT** (make the prop optional + author the empty state, or delete the guard) |
| **LP-14** | L·m-7 / C-12 | The degraded blend row leaves a dangling `label[for]` — LabeledField mints `${id}-control` unconditionally and the v-else slot contains no element carrying it; LabeledField is the wrong primitive for a non-field readout. C-12's cleared note stands (`text-right` takes effect; `.labeled-field-control` is `display:grid`) | **NET-NEW → OPTIONS-UNIT** (a readout row primitive, or bind the id onto the span's container semantics honestly) |
| **LP-15** | C-8 (+ L·m-3's narrowing half) | The `op` write is never narrowed: the compiled handler emits `String(e)`, `emit('update', {op: v})` carries `string` into a bare `Object.assign` (layer-api.ts:33, no validation), and an out-of-vocabulary operator falls into the compositor's replace/weight-blend arm rather than throwing — in a file whose sibling seam (`trySetOption`, ChannelOptions.vue:461-476) is explicitly fail-explicit | **NET-NEW → OPTIONS-UNIT** (narrow at the emit boundary — `satisfies`/type-guard —; engine-side validation is the engine lane's question, cross-ref KF-CO-3's guard-beside-unguarded class) |
| **LP-16** | L·m-6 / C-10 | The mutex is drilled in as two stringly-typed callback props (`isOpen`/`setOpen`, `'blend'` ×3 magic string) duplicating the component's own emit channel, while glass 7 ships both halves of `v-model:open` and the demo already uses them correctly (ChromeDock.vue). A function prop is precisely what made LP-2 silent — a prop name cannot be checked against a child that never declares it | **NET-NEW (design half) → OPTIONS-UNIT**, executed WITH KF-CO-1's consumer rewire (`:open` + `@update:open`; the callback pair and the magic string delete). RR-2 miss #7 rider recorded at LP-2 |
| **LP-17** | RR-1 miss #3 (ADMITTED) | Second OD-U14 violation axis: when `enabled` is false the layer contributes to nothing (entries.ts:73 excludes its keys from the stable union; compositor.ts:100/soa.ts:70/waapi.ts:39 all skip), yet `blend`/`weight`/`z-index` stay live and undimmed; glass 7's `disabled` channel (threaded into every wrapper, surfaced as `data-disabled`) is never touched. Unreachable today only because LP-3 pins the switch OFF — lands the moment KF-CO-8's cure does | **NET-NEW → OPTIONS-UNIT** (disable/dim siblings on `enabled: false`; same spec table as LP-6) |
| **LP-18** | RR-1 miss #4 (ADMITTED) | The weight slider — the one 0-1 quantity at step 0.01 where precision is the point — ships with no visible value: no readout, no `marks` (and vendor marks are `aria-hidden` anyway); reka supplies `aria-valuenow`, so the screen-reader user is told the number and the sighted user is not; the z-index row shows its exact integer one row up | **NET-NEW → OPTIONS-UNIT** (a readout in the label track or value affordance; the D-15 `marks` observation folds in as the weaker form) |
| **LP-19** | C-6 | `Separator` imported from the root barrel (:76) beside two subpath imports (:74-75); `./separator` exists (77 B vs 23,938 B); glass-ui absent from optimizeDeps | **FOLD ≡ kf-CubeScene C-19a class** (subpath-policy sweep; the ME-25 precedent). Dev-graph magnitude rerouted **build-level, NOT SS-13** |
| **LP-20** | L·m-5 · D-11 · D-12 | Hygiene tail, all verified: `<template v-if>` wrapping a single `<LabeledSlider>` (:49-59); `text-right` physical in a logical-property context (:24 — **the row both readers dropped**, ruling 9, ≡ KF-CO-32 class); 8 lines over the 80-col `.prettierrc.json` printWidth with :45 at 119 chars hiding LP-8's coercion (provenance corrected per ruling 10) | **NO-WAVE-OWNER** — rides any touching wave; D-12's axis-side citation error recorded in the cite-drift register |

### INFO

| id | merged ids | claim | disposition |
|---|---|---|---|
| **LP-21** | D-13 | One operator enumeration in three unenforced homes (untyped `as const` :79 · `Record<string,string>` :123 · the authoritative union types.ts:129) — agreeing today, nothing policing tomorrow; no runtime array exists in src/ to import | **OPTIONS-UNIT** (`satisfies readonly CompositeOperator[]` + key-constrained descriptions; trivial under the KF.W4 gate) |
| **LP-22** | L·i-1 | The demo's layer writer calls the throwing engine seam bare (`getGroup().setLayerConfig` — useAnimationGroupActions.ts:41-46, my read; `requireEntry` throws) while the sibling wraps every option setter in `trySetOption`. Unreachable today (layer derived from `animations[name]`) | **OPTIONS-UNIT** (one guard policy; cross-ref KF-CO-3) |
| **LP-23** | L·i-2 | 4 of 6 `AnimationLayerConfig` fields exposed; `properties` (honoured entries.ts:74-76) and `weightSpring` — "the flagship demo moment" — undemonstrated; and a live spring **silently shadows this panel's `weight` write** (weight.ts:17 `weightSpring?.value ?? weight`, my read — spring exempt even from the clamp) | **OPTIONS-UNIT spec input** (the shadow trap must be in the spec before any weight-UI repair) |
| **LP-24** | C-14 · RR-2 miss #6 | The value.js exposure is dev-graph only; **R1 (`parseCssColor` crash class) is NOT reachable from this component** — an explicit verified NEGATIVE, folded by reference onto the banked R1 identity so no later lane re-opens it. The `animationDescriptions.ts` bottom-of-file imports (:128-129, re-read) ≡ **KF-CO-40/N-8, banked** — RR-2 re-derived a banked row | negative recorded; fold held; tree-shake byte-count accepted as INFO, not proof |

## Killed-claims register — 7 entries

| # | killed claim | adjudication |
|---|---|---|
| K1 | **D-4** ("the read half is dead: `open` never bound → uncontrolled, never closes; two selects can sit open simultaneously", MAJOR) | **KILL SUSTAINED (both readers, convergent; mechanism ≡ KF-CO-1, re-executed in that record's addendum).** The absent Boolean casts to `false` and IS unconditionally bound and forwarded → controlled-SHUT: zero listboxes can exist, not two. D inverted the failure mode and under-graded by two steps; its own D-2 stated the cast rule one section earlier. D's severity table (2 BLOCKER) reads 3 corrected |
| K2 | **D-15 cascade-fragility sub-claim** ("wins solely by cascade order… any reordering silently reverts every panel row to stacked") | **KILL SUSTAINED — this seat's own CSS read (ruling 5)**: glass declares no `grid-template-columns` outside the unused `[data-layout=horizontal]`; zero conflicting properties; the hazard is fictional. RR-2's UNPROVEN tag DISCHARGED — one SS-13 row removed |
| K3 | **L·M-3 zIndex leg** ("zIndex is inert on a multi-target group — ordering is meaningless when each child paints its own target") | **KILL SUSTAINED at the engine bytes (ruling 4)**: `renderMultiTarget` iterates zIndex-sorted entries; `singleTarget === false` does not imply target-disjoint; restated "inert only under fully disjoint targets". RR-2's whole-row confirmation corrected; the enabled leg survives as LP-6 |
| K4 | **RR-2's kill of the shared F-1 premise** ("phantom dependency at HEAD") | **SUSTAINED-AS-CORRECTED (ruling 2)**: the axes' premise is false at local HEAD — but ≡ **census F-1/SCH-1, FALSIFIED-AT-HEAD**, ruled four times before this corpus filed it. FOLD → KF.W0 §B-12; the FAM-01/H→V-01 cross-repo rider banked; the three-regime replacement story dies at the frontier (K5) |
| K5 | **RR-2's blast-radius kill** ("live user-facing breakage NOT ESTABLISHED… broken on the author's machine only; CI compiles against 6.0.0 where the component is correct as authored") | **OVERTURNED — this seat's kill of the kill (ruling 1)**: `origin/master` pins devDep **7.0.0 exact = installed**, registry-resolved with integrity hash; the audited file is byte-identical to the frontier. The four prop findings are live breakage of any frontier build; RR-1's uncapped BLOCKER/MAJOR confirmations stand |
| K6 | **RR-2's D-S-E kill** ("the rename is an uncommitted working-tree edit; the docs are correct, the tree moved and has not been committed") | **SPLIT (ruling 3)**: provenance half OVERTURNED (the rename is landed at `origin/master` — my git show); doc-defense half SUSTAINED on its own law (G.W17/U.C are dated records, never rewritten — D's "stop citing" demand stays dead) |
| K7 | **RR-1's miss #6** ("the collapse animates unattenuated under `prefers-reduced-motion`; the audit's motion axis is otherwise empty") | **KILLED by this seat (ruling 6)**: the vendor's global PRM allow-list (`a11y-overrides.css`, `!important`, excludes `grid-template-rows`) removes the size morph; 0.1 s opacity fade is the authored PRM behavior — already banked TWICE as a superlative (KF-CO D·S-1, ACG S-4). A miss that was a registry re-litigation of a settled non-defect |

## Identity-guard folds (never re-booked)

- **LP-2 ≡ KF-CO-1** · **LP-3 ≡ KF-CO-8** · **LP-4/LP-5 ≡ KF-CO-2 (census extended at KF-ES-10; cure-riders on KF-CO-47)** · **LP-7 ≡ KF-CO-7 ≡ KF-APP-4 (→ KF.W4; CI-project rider ≡ kf-MatrixEditor K1 + FAM-03/MR4)** · **LP-8 ≡ KF-CO-35 + census S-9 (→ KF.W6 evaluation, ME-18 precedent)** · **LP-10 ≡ KF-CO-37** · **LP-19 ≡ kf-CubeScene C-19a class** · **LP-20's D-11 ≡ KF-CO-32 class** · **LP-24's imports-at-bottom ≡ KF-CO-40/N-8**.
- **RR-1 miss #2 ≡ KF-CO-5 + KF-CO-46** (collapsed-pane focusability; BLOCKER already banked, cure-rider already sharper than the reader's).
- **K4/K5's manifest schism ≡ census F-1/SCH-1 → KF.W0 §B-12**, with the FAM-01 RAIL / H→V-01 / W2-producer-gated cross-repo rider (keyframes.js's own V registry, verified verbatim).
- **LP-24's R1 negative** folds onto the banked value.js R1 identity as evidence, not a row.

## Superlatives — 9 sustained (provenance: corpus → readers → this seat)

1. **The weight-disclosure gate** (S-A/S-2/S-B, all three axes + both readers): `:49`'s `blendAvailable && op === 'replace'` mirrors `isWeightBlend` exactly — re-verified by this seat at weight.ts:4-6, and `resolveBlendWeight` is reached only from the replace arm. It survives the naive inversion that would have produced the opposite gate. **Caveat sustained (LP-1 rider): both operands are untracked reads** — the best line in the file is stale-by-construction.
2. **Honest degradation instead of a disabled control** (:19-27; `cda6a1a5` verified by this seat): the inert state SAYS WHY, derived correctly from `singleTarget`. Delivery currently broken by LP-4 (dead tooltip) and coverage is 2-of-4 rows (LP-6/LP-17) — an exemplary intent, half-applied.
3. **The z-index row's raw-slot wiring survived the API move end-to-end** (RR-2's S3): camelCase slot props match the destructure; `inheritAttrs:!1` + forwarded attrs land `:id`/`:aria-errormessage`/`@change` on the real element; the contract at `types.d.ts:18-26` counted line-for-line. The only row authored against the vocabulary the installed package actually has.
4. **Zero lifecycle surface** (S-E/S-3): 92 lines, no ref/computed/watch/lifecycle/timer/listener/style-block, zero minted custom properties, zero collision surface against the 98-property flat namespace — the counterexample to the demo's fork instinct. Every failure in this roster is a contract failure against a moved API; none is re-authorship.
5. **The library edge is exactly right and fully erased**: `import type { AnimationLayerConfig }` from the light barrel under `verbatimModuleSyntax` — zero runtime bytes toward the library it dogfoods; the value.js edge dev-only and R1-clean (LP-24's negative).
6. **The `.labeled-field-grid` subgrid idiom** — already banked as a superlative at kf-ChannelOptions (RR-A's, adopted there); this corpus's D-15 INFO surroundings (the φ-conjugate split, the inexpressible `auto` track) attach as riders. Cross-ref, not re-banked.
7. **Axis D's S-C contrast arithmetic** — reproduced independently by RR-1 to three significant figures (5.011/5.431), self-scoped floor claim, live term marked. The method template for computed a11y claims; backdrop term → SS-13 #4.
8. **Axis C's probe discipline** — jsdom mounts of the installed dist isolating cause to the prop name; polyfills disclosed; one candidate cleared by probe (`text-right` effective); one explicit NEGATIVE filed (C-14). Recording proven non-findings is rarer than findings.
9. **Axis L's citation discipline** — every `types.d.ts` cite verifies line-for-line under this seat's hostile count (:31, :28-36, :40-42, :18-26), in a corpus whose central finding is stale provenance; and L alone found LP-1 and LP-6, the two largest net-new rows. The axis that is precisely provenanced is the one that found what the others missed.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **LP-1 witness** — drag the weight slider / type into z-index: model updates, thumb/display do not (the cheapest confirmation of the frozen render edge). One probe corroborates KF-CO-1/KF-CO-8's already-booked witnesses — run them together.
2. **LP-4 cure geometry** — IF the description route is chosen: measure the label-column swell under `.labeled-field-grid` with a 50-char description in the copy cell (direction certain, magnitude live; the pane's resolved inline size + the 1fr min-content floor decide).
3. **LP-9 composed geometry** — the two blend-row arms' rendered block-size/baseline/type-size deltas, fine + coarse pointer (67% type-scale figure is token-derived; the composite is live).
4. **S-C's effective backdrop** under `backdrop-filter` on the glass surface (floor claim: both resolutions ≥5:1; exact figure live).
5. **D-5's coarse-pointer spinner sub-claim** — UA `type=number` spinner hit-target vs the 2.75rem floor: not derivable from any file in either tree (both readers concur).
6. **C-14's 318-byte tree-shake** — accepted as INFO on structural corroboration (`sideEffects:false`; no caller path); re-run only if a wave touches the reference-data module.

Rerouted, not SS-13: **LP-19's dev-graph magnitude** → build-level analysis (the C-19a precedent).

## Routing summary (M-25 agglomeration input)

- **KF.W0 §B-12**: the manifest schism (≡ census F-1/SCH-1, K4/K5) + the FAM-01/H→V-01 cross-repo rider — the reconciliation must land the frontier's 7.0.0 devDep, at which moment every prop verdict here is a verdict about the shipping build.
- **KF.W4**: LP-7 (≡ KF-APP-4, booked once) + the `--project demo` CI rider (≡ kf-MatrixEditor K1 / FAM-03 MR4) — the OPTIONS-UNIT sequences after this gate.
- **KF.W6**: LP-8's `/number-field` evaluation (≡ census S-9, ME-18 precedent).
- **glass-ui BH relay**: nothing new — the Boolean-cast producer pair (KF-CO-1/8) already rides; LP-4's description-geometry is consumer-side.
- **census SS-1/SS-2**: the "consumed-but-mis-called" class under a FRESH id (ruling 8 — the S-9 collision), type specimen this file, membership = the KF-ES-10 census.
- **OPTIONS-UNIT (NO-WAVE-OWNER, SS-1/SS-2 intake)**: LP-1 (sequenced WITH the KF-CO-1/8 rewires — write legs never restored without the render edge), LP-6/LP-17 (the honesty-coverage pair), LP-9, LP-11 (with KF-CO-35's cure), LP-13/14/15/16/18, LP-21/22/23, and the LP-20 hygiene tail.
- **SS-13/KF.W9**: the six-row list above.

## DISSENT — preserved, not ruled away

1. **RR-2's regime probes were honest work aimed one commit short.** Its local-HEAD facts (optionalDep 6.0.0; producer v6 declaring every prop this file passes) are all true and are adopted as the *dating* of the breakage — the component was correct as authored against 6.x and broke at the producer's 7.0.0 API move. What died was only the consequence clause. If the owner ever re-pins 6.x, RR-2's cap revives; nothing in the frontier's direction of travel suggests it.
2. **RR-1's grading of the missed-cure finding (MAJOR) vs its rider disposition here**: RR-1 filed the description-geometry collision as a standalone MAJOR miss; this seat banked it as a cure-rider on KF-CO-2/47 because the DEFECT identity (dead tooltips) is banked and the miss conditions its cure rather than adding a defect. If the SS-1 spec treats cure-riders as second-class, RR-1's severity instinct should be revisited — a cure that degrades the panel it repairs is wave-blocking information.
3. **Severity tension on LP-1 vs the banked KF-CO-1/8**: this record ranks the reactivity break as the corpus headline; a defensible alternative ranks the controlled-shut selects higher (visible, clickable, dead chrome vs a staleness only writes expose). The OPTIONS-UNIT cures both in one sequenced packet, so no ruling is required — recorded because the SS-13 witness order follows the ranking.
4. **Registry-blindness is now a measured re-reader failure mode, not an anecdote**: five of the thirteen filed misses (both readers) were pre-banked or pre-killed in records that name this component in their instrument lists. The kf-MatrixEditor closing verdict asked for a registry read before filing; neither reader did one. The fleet lesson: a re-reader packet should carry the banked kf-* roster for its component's HOST as mandatory context.

## Closing verdict

The two hostile re-readers split the frontier between them: RR-1's confirmations survive intact but two of its seven misses were already banked at the host record it never opened and a third died at a vendor file it never read; RR-2's three boldest rulings — the regime kill, the blast-radius cap, the rename-provenance kill — all stopped one baseline short of `origin/master`, where this seat's own git shows the audited file byte-identical to landed frontier content and glass-ui pinned at exactly the installed 7.0.0, reviving the four prop BLOCKERs RR-2 had capped and reproducing, for the third time in this program, the pattern the kf-MatrixEditor seat named. After identity-folding against the heavily pre-banked host surface (nine folds, zero re-bookings), the corpus's genuine net yield is precise and small: **LP-1, the absent write→render edge that axis L alone found and that now sequences the banked one-line repairs; LP-6, the multi-target honesty gap on the enabled row; LP-9, the two-armed row geometry; the description-cure geometry rider that stops the prescribed tooltip cure from squeezing the panel it repairs; and the S-9 collision ruling that keeps the census namespace sound.** Three reader kills sustained (one discharging an SS-13 row at the bytes), one kill overturned, one split, one reader miss killed, and the enabled/zIndex over-claim trimmed at the engine source. Nothing lost: head, tail, or interval — and nothing banked was booked twice.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; the X·KF specs consume this as agglomeration input (M-25); execution awaits the owner's begin-word.
