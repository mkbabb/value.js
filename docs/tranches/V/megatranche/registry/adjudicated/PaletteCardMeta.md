`claude-fable-5` — served model id (Fable 5), observed in this seat's own system prompt, not inherited; recorded per M-23/L-18 before any content.

# PaletteCardMeta — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator · **Date**: 2026-08-06 · **HEAD**: `3ea0144c` · branch `tranche-u`.
**Corpus**: 3 challenge files at `audit/components/PaletteCardMeta/` (C 6 findings · D 5 · L 5), all pinned SHA-256 `95dd9725de21…` — re-hashed by this seat against the live 64-line file: byte-identical. Provenance: HYDRATION-LEDGER.md:270-272 marks all three **REPORT-AUTHORED** (closure-manifest-2026-08-03) — a report corpus whose receipts are true even where its analysis is not.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement (6) was re-derived by this seat with its own instruments, and both readers' sole-BLOCKER/novel-MAJOR claims were re-traced end-to-end rather than spot-trusted.
**Arbiter's own instruments (all static, zero browser tooling per law)**: full reads of PaletteCardMeta.vue (64 L whole, re-hashed), PaletteCard.vue (364 L whole), MixSourceSelector.vue:1-80 + :235-274, AdminUsersPanel.vue:130-160 (+ grep census of its emit wiring), usePaletteStore.ts:78-157, usePalettePorts.ts (savedPalettes provenance), useBrowsePalettes.ts:100-145, demo/palettes/api/palettes.ts:130-152, demo/palettes/types.ts:16-110; api reads: format.ts (whole), schema.ts:28-57, service/votes.ts:30-75, service/crud.ts tags census (`:94`, `:184`), service/forks.ts forkOf census (`:82`); menu/popover reads: PaletteCardMenu.vue:75-100, TagEditPopover located at `browser/search/` (path drift vs the readers' citations — same file), AdminTagsPanel.vue:84-88; producer bytes: chip/chipVariants.d.ts SIZE+SHAPE ladders (the R-3 decider), typography/scale.css (`--type-micro: 0.6875rem` fixed; `--type-small: clamp(0.875rem, 0.8rem + 0.25vw, 1.25rem)`), typography/utilities.css (`text-mono-small`, `text-mono-micro`, `fira-code` @utility with liga/calt, `.tabular-nums`), @lucide/vue defaultAttributes.mjs (no `role`, no `aria-hidden`); demo bytes: foundation.css:121-129 (`--default-transition-duration: var(--duration-fast)` + the S.W3-5 comment), utils.css:9-11 (`.fira-code` fork); repo greps: `@vote` listeners = **exactly 1** (BrowsePane.vue:105), `bg-muted/60` in demo *.vue = **exactly 1** (this file's :39), `PaletteCardMeta` in test/+e2e/ = **0**; DEFECT-LEDGER rows 3505 · 4423 · 23622 · 23635 · 24209 · 24360 · 25129 · 26127 all re-read verbatim; motion-quarantine.md:107 (M-16 TRUE) + §7.1 (:276); prior adjudications PaletteCard.md (PC-6/PC-9/PC-11/PC-13/PC-22/PC-32/PC-35 identity guards), BrowsePane.md (M19/m5), AdminUsersPanel.md + AdminFlaggedPanel.md vote-mention census (0 / routing only); X wave specs W7.md:44-52/:246-262/:365-400/:430-440/:493-500.
**Wave authority for routing** (per PaletteCard.md's ratified map, re-read this session): tranche X `docs/tranches/X/waves/W0..W11`. **X.W7.f owns `PaletteCardMeta.vue` by name** (Display formatting facility; sub-gates G16/G17/G18); **X.W7.c** extracts the props-only `PaletteSpecimen` (G12: zero interactive descendants); **X.W7.d** moves vote/menu/versions/tags to the selected-entity inspector and deletes `PaletteCard.vue`; **X.W7 G8/G9/G10** already carry born-RED gates citing this file's :37/:39 by line; **X-W10** = design+motion canon; **X-W1** = re-gate; GLASS-OWNED rows leave by the standing BH relay. Line anchors are HEAD `3ea0144c`; re-resolve before any cure (D-19).
**Motion law**: the sole quarantine row touching this corpus is **M-16 = TRUE** (motion-quarantine.md:107) — carryable, and carried inside PM-6 with its producer-verified caveat (`transform` struck from the reduce property list ⇒ `active:scale-95` is PRM-safe by construction). No FALSE row is carried. §7.1 lists this component's C and D files fully cleared.

## Scoreboard

**21 CONFIRMED (9 MAJOR · 7 MINOR · 5 INFO) · 1 RESCOPED-CROSS-BOOKED (the DU "BLOCKER") · 8 KILLED (6 whole rows + 2 limbs) · 13 of the 21 confirmed rows were MISSED by all three axes and found by the re-readers · 5 UNPROVEN-NEEDS-LIVE residue rows.**

No BLOCKER survives **on this component's own roster** — the one filed (nested button-in-button) is real, measured, and already banked at PaletteCard.md **PC-11**, and its attribution to THIS component's vote button is refuted by the filer's own receipt (ruling R-1). Severity-weight survivors: the lying accessible name on the vote toggle (PM-8, ≡ banked M19 + two new limbs), the AdminUsersPanel dead vote button (PM-9), and the isLocal split-brain that renders another palette's provenance on local copies (PM-10).

## Ruled disagreements (the seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **R-1 · Mix nested button** | MISSED-filed as **BLOCKER**: "PaletteCardMeta.vue:43 renders a `<button>` inside MixSourceSelector's wrapper `<button>`" | Classified Mix "safe by locality, not by design" (inside its dead-button trace); filed nothing | **RESCOPED + CROSS-BOOKED ≡ PC-11 — both readers partly wrong** | The invalid content model is REAL and measured — ledger 24360: `nestedButtonInButton:2`. But the probe's own sample reads `innerInteractive:["BUTTON:Palette menu"]` — the MENU trigger, not the vote button. My trace: MixSourceSelector's list is `pm.savedPalettes` (usePalettePorts.ts:62 → usePaletteStore), where `isLocal: true` is minted at :91 and forced at :148 — so `v-if="!palette.isLocal"` (:44) fails and **the vote button never renders in Mix**. DU quoted the receipt that contradicts its own attribution — a citation-discipline event recorded in DISSENT. LC traced the locality correctly but dismissed the seam entirely, though the seam is live via the menu button. Identity: **≡ PaletteCard.md PC-11 (banked → X-W7.c)**; the vote limb is LATENT (joins the day any non-local palette feeds that list) and dies structurally at G12's "zero interactive descendants". Not re-booked. |
| **R-2 · C-2 duplicate-tags limb** | KILLED (authoring UI cannot mint; inert render; "a dev-mode console warning named as a product defect") | CONFIRMED MINOR (hand-crafted PATCH persists via crud.ts:184; no schema uniqueness) | **KILLED on the component; the write-path fact books as an API rider** | Both facts verified mine: schema.ts:35 carries no uniqueness refine; crud.ts:184 `$set.tags = body.tags` verbatim; TagEditPopover adds only on the unchecked→checked edge over the admin canonical list, so no authoring surface reaches the state. The rendered consequence at :36-40 is a keyed list of INERT static text — no state, no transition, no reorder — where a duplicate `:key` costs a dev-only warning. DU's kill stands for the component. LC's fact survives as a **schema-uniqueness rider** (`.refine`/Set-normalize on `tagsArraySchema`) → **X-W3 rider (API policy), else NO-WAVE-OWNER** (the AF-27 precedent: server-half unclaimed by name). |
| **R-3 · L-2's cure** | Geometry limb + one-line cure REFUTED (Chip's smallest rung larger on all three axes) | "The prescribed cure is structurally sound — I verified the producer actually ships it" | **DU wins — the cure is REFUTED; the mint charge stands** | My read of chipVariants.d.ts: `SIZE.sm = "gap-1 px-2.5 py-1 text-caption"`; the local pill (:39) is `px-1.5 py-0.5 text-micro` — smaller on inline pad, block pad, AND type. The only producer text-micro chip is `SHAPE.cell` — a flex-col cell, not a pill. LC verified *existence* (index.d.ts export, ChipMode union, `./chip` subpath) but never read the size ladder; a drop-in `<Chip size="sm">` would inflate exactly the row G9 proves starves the title to 0px. Per the ColorSpaceSelector addendum item 2 — **the cure is adjudicated separately from the defect** — the parallel-mint row survives (PM-7) and the producer ask (an `xs` rung, or `cell`'s micro type on a pill silhouette) rides the **glass BH relay**. |
| **R-4 · L-5 identity limb** | CONFIRMED MINOR ("the category channel … dies at the card") | REFUTED at the seam (`string[]` IS the wire contract; `Tag` is a separate admin catalogue joined by name) | **LC wins on the boundary; DU's fact survives demoted** | format.ts:24 declares `tags: string[]` and :72 passes verbatim — the component consumes exactly what the server emits; it cannot conjure `category` from strings. The `Tag {id,name,category}` entity (types.ts:96-101) is real and category IS load-bearing (AdminTagsPanel.vue:86 `groupedTags`), but the flattening happens at the API ENVELOPE, not the card. Demoted to an API-surface observation: **NO-WAVE-OWNER** at the formation boundary (enriching the envelope needs a new API surface + migration — not a component or wave-f fix). The "unbounded array" limb is killed by both readers and by schema.ts:35 (≤10 × ≤30 chars). |
| **R-5 · D-1** | Partial kill ("three, not four" — retains a three-way micro collision) | Full kill on the inverted premise + the inversion refiled as a missed find | **KILLED as filed; LC's inversion ≡ banked m5; DU's residue is design input, not a row** | The premise is false by my token reads: the vote count is `text-mono-small` → `--type-small` clamp (14→20px, FLUID) against `--type-micro` 0.6875rem (11px, FIXED) — the action is the MOST differentiated element in the row (size + hue + interactive surface + press + ring). BrowsePane.md **m5** already banked the true form ("four computed type sizes in one items-center row, the button's unauthored") → the inversion cross-books there (PM-12) with LC's sharpenings and this seat's addition: the producer already ships **`text-mono-micro`** — the size-preserving cure exists off the shelf, which neither reader saw. DU's three-way-collision residue folds into the W7.c/f row design as input. |
| **R-6 · Severity splits** (C-1, D-3, D-4, D-5, L-3+target-size, PM-10) | MAJOR / MAJOR / MAJOR / MAJOR / MAJOR+MINOR / MAJOR | MINOR / MINOR / MINOR / MINOR / MINOR+MAJOR / MINOR | **MAJOR across all six — DU's weighting adopted, LC's evidence often the sharper** | Principle: a row is MAJOR where a committed measurement exists for the harm (0px title — ledger 3505/4423; 23.6px target — ledger 23622) or where the surface states something FALSE (the split-brain provenance, the lying label). It stays MINOR/INFO where the harm is latent, idiomatic, or an observation. The target-size row folds INTO PM-6 (one identity, one cure — the producer atom seat) rather than standing alone: the ledger measured both under one line. |

## Adjudicated defect roster

Anchors at HEAD `3ea0144c`. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave; every cure is a requirement on the X.W7 successor surface, not a patch to this file (the PaletteCard.md routing law).

**MAJOR**

- **PM-1 (C-1/D-2, dedup) — `tags.slice(0, 3)` silently discards up to 7 legal tags with no overflow count, and the majority reader has NO path to the remainder.** Live :37; schema.ts:35 admits 10 on create (:45) and patch (:52). LC's partial refutation folds in as the precise scope: the only route to the full list is the menu's Edit Tags, gated `remote && isOwned` (PaletteCardMenu read) — the non-owner wall browser, the dominant case, has nothing. **→ X-W7 G8** (born-RED already, citing this line and this schema receipt verbatim; GREEN = all tags reachable or behind `+N`) + the W7 headline goal 5 names the cure. The `+N` problem is CLOSED-domain (≤10) — the corpus's `TagSummary` normalization cure stays refuted (killed register).
- **PM-2 (C-4) — the vote button has no pressed/busy/disabled state and can double-fire; the server toggle makes the race semantically destructive.** Live :43-54 (attributes = `class`/`:aria-label`/`@click.stop` only). Proved end-to-end by both readers, re-walked mine: useBrowsePalettes.ts:126-142 `onVote` holds no in-flight lock (its sibling `loadMore` DOES gate on `loadGeneration`, :104 — the pattern exists in the same file); palettes.ts:145 mints a fresh `idempotencyKey: crypto.randomUUID()` **per call**, defeating the idempotency layer by construction; votes.ts:39-70 is a genuine toggle (deleteOne → −1 / true-insert → +1), so two racing POSTs land on-then-off with the later response overwriting state (:133-136). The `aria-pressed` limb ≡ banked **M19** (cross-book, not re-booked). **→ X-W7.d** (the inspector vote seat: producer toggle semantics + an in-flight lock + a stable idempotency key per gesture).
- **PM-3 (D-3/L-4 + LC-missed aria-hidden, one identity) — fork/version meaning rides native `title` on non-interactive spans; the forkOf span carries ZERO text, so provenance does not exist for touch or AT.** Live :10/:19/:29 — three `:title`s as sole carriers; :7-13 contains only a `<GitFork>`; lucide's defaultAttributes (my read) ship no `role`/`aria-hidden`/`<title>`, so :12/:21/:31/:49 are nameless graphics — while the same card row already knows the convention (PaletteCard.vue:103 `aria-hidden="true"`). Producer cures exist (tooltip.d.ts; `.sr-only` registered). **→ X-W7.f** (the readout owns its carriers: text or sr-only, never bare `title`) under **X-W10** canon. AT-specific announcement behavior = residue U-3; the cure is correct under every AT behavior.
- **PM-4 (D-4) — a rejected vote is a silent no-op: the sole mutating action in the row is the single action wired past the card's own purpose-built feedback surface.** DU's relocation adopted as the sharpest form: ActionFeedback is mounted (PaletteCard.vue:123-128) and exposed (:244), consumed by five call sites for save/delete/rename — the vote path alone terminates in `console.warn("Failed to vote:", e)` (useBrowsePalettes.ts:139-141). LC's structural note folds in: the component's entire input is `palette` (:61) — it cannot express pending/error even if asked. This row is a named member of **X-W7 headline goal 3** (the 31-site `catch → console.warn` table). **→ X-W7 goal-3 + X-W7.d** (verdicts move to the inspector's announced feedback seat, per the banked AF-cluster ≡ PC-13 — the announcement half is NOT re-booked here).
- **PM-5 (D-5, mechanism confirmed / consequence corrected) — five `shrink-0` roots with no wrap/truncation policy injected into a no-wrap row whose only shrinkable member is the palette's NAME, measured at 0px on phones.** Mechanism mine: :9/:18/:28/:39/:45 all `shrink-0`, no `max-w`/`truncate`; PaletteCard.vue:43-44 no `flex-wrap`; the title's `line-clamp` collapses its `min-width:auto` to 0. The corpus's "collides with the vote control" consequence is KILLED (the vote button is `shrink-0` — it cannot be collided with; ledger 3505 `titleW:0, metaChildren:8`, 4423 `5.6px` chromium AND webkit). Identity: the starvation ≡ **PC-6 (banked)**; this row is the Meta-side limb G9 cites by line (:39). **→ X-W7 G9** (born-RED; "the P0 of the OM-16 census"; exact overflow px = MEASURE-AT-OPEN → residue U-2).
- **PM-6 (L-3 + both readers' missed target-size, one identity) — the vote button hand-owns color/pressed/focus/geometry where the producer atom is structural, and its measured box (47.2 × 23.6 px) fails WCAG 2.5.8 on every phone-width viewport.** Motion row **M-16 = TRUE** carried with its caveat (`active:scale-95` PRM-safe — `transform` struck from the reduce list). Live :45/:51/:53. The cure sits 40 lines away: PaletteCard.vue:96-104 consumes `<Button icon-only size="sm">` under the :93-95 comment "the sm square also cures the ~24px touch target" — this button is the surviving 4th copy of the retired recipe. Target size is MEASURED, not residue: ledger 23622 rendered the exact class string through the live cascade → 23.6px height (< 24), corroborated by LC's independent clamp-floor arithmetic (14px × 1.4 + 4px, clearing 24 only above ~596px viewport). Convergent double derivation, two methods, same figure. **→ X-W7.c G12** (the button leaves the specimen) **+ X-W7.d** (the rebuilt seat consumes the producer atom, inheriting name/hit-area/state structurally).
- **PM-8 (both readers' missed, convergent · ≡ banked M19 + two NEW limbs) — the accessible name is factually WRONG in the voted state, state-blind, and ungrammatical at n=1.** Live :46 `` `${palette.voteCount ?? 0} votes, click to vote` ``. (a) The endpoint is a toggle (votes.ts:39): activating a voted heart REMOVES the vote while the name promises the opposite — the name LIES about the action's direction. (b) State-blind: `voted` reaches only the visual fill (:51); a screen-reader user has no channel — this limb ≡ **M19 (banked → X-W7.d)**, not re-booked. (c) "1 votes", in the file that pluralizes correctly 27 lines up (:19) — the idiom was present and not applied. (d) "click to" is a pointer-specific instruction inside an accessible name. Both readers found this independently; all three axes read past the string while discussing its missing attributes. **→ X-W7.d** (the name derives from state) **+ X-W7.f G17** (count/grammar formatting; G17's `title`-retains-exact rule is the pattern).
- **PM-9 (LC missed) — the AdminUsersPanel vote button renders, takes focus, announces "N votes, click to vote", and does nothing: the emit dies unheard.** Verified whole-tree mine: `@vote` listeners in demo = **exactly 1** (BrowsePane.vue:105); AdminUsersPanel.vue:140-151 mounts PaletteCard with `@click/@feature/@admin-delete` only, over `pm.loadUserPalettes` (server-sourced → format.ts:88 `isLocal: false`) — so :44's guard passes and the button LIVES. Vue warns nothing for an unhandled typed emit. The other three listener-less hosts are safe **by locality, not by design** (LC's framing, adopted): Palettes/Mix/Extract feed local palettes. Disease sibling of banked **PC-22** (the 16-verb bus hosts wire partially); this concrete dead-control instance was unbanked anywhere (AdminUsersPanel.md vote census = 0). **→ X-W7.d** (vote leaves the card; per-host availability gating per PC-22's K-INV5 pattern).
- **PM-10 (DU missed; ledger-corroborated) — the kind-guard is applied to exactly ONE of five roots, so a saved copy of a remote palette permanently advertises provenance/version facts its own menu refuses to act on.** Live: :44 guards vote; :8/:17/:27/:37 unguarded. PaletteCardMenu gates Versions on `!palette.isLocal && versionCount > 1` — same field, same threshold, same `History` glyph, eleven lines away. The state is manufactured by the card family's own Save: usePaletteStore.ts:145-149 spreads the remote row verbatim under `isLocal: true` (my read — the K-PALID comment :140-144 documents the copy as "distinct" while its fields say otherwise). A local twin shows a "4 versions" chip with no route, frozen at absorption time — an affordance advertising a destination that does not exist, and numbers that are true of a DIFFERENT palette. Ledger 24209 filed it NEW; no adjudication had banked it. **→ X-W7.c** (the specimen states its kind honestly) **+ X-W7.d** (versions/provenance become inspector facts resolved live).

**MINOR**

- **PM-7 (L-2, role limb only) — the tag pill parallel-mints a producer primitive the demo already consumes elsewhere.** Chip is shipped (`./chip` subpath; `mode="static"` is the exact case) and consumed (EasingSpecimenStrip); :39 re-mints it at `rounded-full bg-muted/60 px-1.5 py-0.5 text-micro`. The geometry limb and the one-line cure are killed (R-3): the local pill is smaller than Chip's smallest rung on all three axes. **→ X-W7.c/f** (the chip row rebuilds on the producer primitive) **gated on the GLASS-OWNED producer ask (xs rung / pill-micro) → standing BH relay.**
- **PM-11 (both readers' missed, convergent) — two identical `GitFork` glyphs render adjacently with OPPOSITE meanings (is-a-fork vs has-forks), disambiguated only by whether a numeral follows.** :12 vs :21, same `w-3 h-3`; the v-ifs (:8, :17) are independent and jointly satisfiable — forks.ts:82 sets `forkOf` on a child while the parent's count increments, so any forked fork renders `⑂ ⑂3`. Direction — in vs out — is the one thing the glyph must carry and does not; the only disambiguator is the `title` PM-3 rules undependable. **→ X-W7.f** (provenance vocabulary: distinct glyphs or a worded readout).
- **PM-12 (LC missed · ≡ banked m5, sharpened) — three sibling counters, two type sizes, one FLUID against two FIXED.** Fork/version counts inherit `text-micro` (11px fixed) via bare `fira-code` spans (:22/:32); the vote count is `text-mono-small` (:53) — a clamp that grows 14→20px with viewport while its siblings stay 11px. The two spellings of mono in one file are the signature of drift, not emphasis. Identity ≡ **BrowsePane.md m5 (banked → X-W7 + X-W10)**, not re-booked; this record adds LC's fluid-vs-fixed mechanism and this seat's cure note: the producer ships **`text-mono-micro`** — an on-ladder, size-preserving fix neither reader saw.
- **PM-13 (DU missed · ≡ banked PC-35, sharpened) — `bg-muted/60` is a singleton mint of the guard-then-alpha pathology the F-4 sweep exists to kill, six lines under the parent's own comment retiring it.** My grep: **one** hit across demo *.vue — this :39. PaletteCard.vue:45-46: "the /40 post-hoc alpha dies — the muted token is the de-emphasis rung." Identity ≡ **PC-35 (banked → X-W7.c under X-W10 canon)** — PC-35's ten-site census already counts this literal; this record adds its singleton status (not a house idiom — DU deliberately declined to file `ring-ring/40` ×11 and `bg-accent/70` ×6, which ARE idioms; that discipline is adopted). Not re-booked.
- **PM-14 (LC missed) — the component is a rootless five-fragment with an unwritten layout contract: spacing, alignment, and overflow policy all live in the parent, and there is no element on which to hang a cure.** :1-55 has five sibling roots; every geometry fact rides PaletteCard.vue:44's `flex items-center gap-2 min-w-0`. Ledger 25129 names the same mechanism ("no element is ever assigned a column"). This is WHY PM-5 cannot be fixed inside the file — recorded so the W7.c rebuild assigns columns deliberately. **→ X-W7.c.**
- **PM-15 (LC missed) — zero test coverage on a network-mutating control whose server side IS pinned.** My grep: 0 references in test/ + e2e/; the toggle contract is asserted at palettes-votes-flags.test.ts on the API side only — pinned on one side, unpinned on the other. **→ X-W7 G8/G9/G10/G17** (the mounted N-fixture battery IS the coverage cure) **+ X-W1 rider** (the AP-4 precedent: gate-side coverage rides re-gate).
- **PM-16 (DU missed) — `voted` is the field that truly conflates unknown with false, and the collapse hides an identity-minting side effect.** format.ts:47/:89: `voted: votedSlugs ? … : undefined` — `undefined` is a deliberate third state ("we did not resolve this user's vote", :55-56). :51's binary ternary renders it as not-voted; the anonymous visitor and the confirmed non-voter get one hollow heart, and the click path silently mints a session (`session.ensureSession()`, useBrowsePalettes.ts:128) behind a control that looked stateless. This is the TRUE form of C-3's claim, aimed at the field the axis missed. **→ X-W7.d** (three-way state or an unresolved render; the silent session-mint is flagged as an **X-W3 policy note** — access policy owns whether identity creation may hide behind a heart).

**INFO**

- **PM-17 (L-1) — boundary census, accurate as filed**: two imports (:58-59), four spans + one raw button — the factual ground of PM-6/PM-7. No action; context row.
- **PM-18 (DU missed) — `duration-fast` (:45) restates the `@theme`-root default it was minted to make unnecessary** (foundation.css:121-129, S.W3-5: "no per-callsite modifier"). Harmless today; teaches the next author the modifier is required. **→ X-W10** (motion canon sweep).
- **PM-19 (LC missed) — `.fira-code` has two definitions: the demo fork (utils.css:9-11, font-family only, unlayered) shadows the producer @utility and drops `font-feature-settings: "liga","calt"` the day either changes.** Latent (current visible result likely identical — residue U-5). **→ X-W10** (type canon: one definition; the fork dies).
- **PM-20 (LC missed) — the mutating vote count lacks `tabular-nums`, so 9→10 reflows the button and nudges the whole `shrink-0` row.** The producer ships both cures (`.tabular-nums`; AnimatedDigit). **→ X-W7.f G17** (the compact-count formatter carries numeric stability).
- **PM-21 (LC missed) — the `vote` emit hands the parent back its own prop through two relay hops, for a consumer that reads only `.slug`.** Contract-altitude smell, not a bug; dies whole with **X-W7.d** (the vote seat stops being an emit chain). Recorded so the find is not lost.

**RESCOPED / CROSS-BOOKED**

- **PM-22 (DU missed-#1 "BLOCKER" as ruled in R-1) — Mix's nested interactive content model: real, measured (ledger 24360), owned by MixSourceSelector × PaletteCard(Menu), banked as PC-11 → X-W7.c.** This component's vote button is a LATENT second occupant only — it never renders on the reachable state (locals-only feed, verified). Corrected attribution recorded; not re-booked (anti-rename).

## Killed-claims register

- **C-2 — KILLED (both limbs, as a component defect).** Empty strings: schema.ts:35 `.min(1)` rejects on every write path; local palettes cannot acquire tags at all (the sole authoring surface is slug-keyed, remote+owned). Duplicates: no authoring surface can mint one; the render consequence is inert keyed text (dev-mode warning ≠ product defect). The genuine residue — no schema uniqueness, verified reachable via hand-crafted PATCH (crud.ts:184) — leaves as the R-2 rider: **X-W3 rider, else NO-WAVE-OWNER.**
- **C-3 — KILLED (unanimous, verified).** The `?? 0` at :46/:53 sits under `v-if="!palette.isLocal"` (:44); for that branch format.ts:25/:79 emits `voteCount: number` NON-optional, presence enforced by the startup probe (the file's own :6-12 records the Lane-D F1 default-excision precept). The coalesce is type-narrowing for the demo's local-accommodating optional, not semantics. The TRUE conflation is `voted` — refiled as PM-16 at the correct field.
- **C-5 — KILLED (unanimous).** (a) voted-without-count: unreachable (format emits both or neither). (b) Negative counts: unreachable (±1 `$inc` gated on the vote-document delete/true-insert signals — votes.ts:49/:63, my read). (c) "fork flags without companions" is the DOMAIN — `forkOf` with zero `forkCount` is simply an unforked fork, and :7-13 renders it correctly. The prescribed runtime invariant assertions in a stateless leaf would invert the repo's ratified layering (probe-at-startup, not compensate-at-render). The cure would make the code worse.
- **C-6 — KILLED (unanimous).** `@click.stop` on a NATIVE button covers Enter/Space identically (synthetic click) and correctly prevents card-expand — the one mechanism the axis fingered is the one that works. The real defects at that seam are the parent's pointer-only expand (≡ banked **PC-9**) and Mix's nesting (≡ banked **PC-11**, R-1) — neither is this component's row, both were already booked.
- **D-1 — KILLED as filed (R-5).** "Four classes at the same micro weight" is factually inverted: the action is the most-differentiated element in the row (fluid `text-mono-small` ≥27% larger, hue, surface, press, ring — my token reads). The truth it gestured at is banked as m5; the residue informs the rebuild.
- **D-5, consequence limb — KILLED.** "The row can collide with the vote control": the vote control is `shrink-0` and holds its box unconditionally; the measured casualty is the palette NAME at 0px (ledger 3505/4423). Filing it as a collision misdirects the cure toward spacing; the cure is column assignment (PM-5 → G9). Secondary: "long tags" is bounded (≤10 × ≤30).
- **L-2, geometry limb + one-line cure — KILLED (R-3).** The pill does not duplicate Chip's geometry — it is strictly SMALLER than Chip's smallest rung, so the drop-in cure would grow the starving row. Mint charge survives as PM-7; producer ask → relay.
- **L-5, as a component-boundary defect — KILLED (R-4).** `string[]` is the wire contract (format.ts:24); the taxonomy dies at the envelope, not the card. Identity observation → NO-WAVE-OWNER. The "unbounded array" limb killed on schema.ts:35 by both readers.

## Superlatives (provenance preserved)

1. **The one correct guard, for the right reason** — :44 `!palette.isLocal` on the vote button is the K-PALID domain invariant (a local palette has no server identity to vote on; types.ts:19-28; votePalette keys on slug alone) expressed at the render boundary, pre-empting a whole 404 class. That it is the ONLY guarded root is the file's central defect (PM-10) — but the one it got right, it got right for the right reason. [DU; verified]
2. **Voice discipline at the smallest scale** — :19 pluralizes "remix**es**" correctly AND chooses the product's word over the code's ("remix", not "fork") consistently across both provenance legs (:10). It is precisely what makes :46's "1 votes" indefensible rather than merely sloppy — the idiom was 27 lines up. [DU; corroborated LC]
3. **The right threshold, not the obvious one** — :27 `versionCount > 1` (every palette has one version; `> 0` would decorate every card) beside :17 `forkCount > 0` (zero forks IS the degenerate case). Two adjacent counters, two thresholds, each matching its semantics — thought, not drift. [both readers]
4. **The structural a11y instinct on a hand-rolled button** — :45's tokenized `focus-visible` ring and :46's existence-of-a-name are the parts hand-rolled buttons usually drop; PaletteCard.md R-5 counts this very label among the folder's four documented-rule sites. The name's CONTENT is wrong (PM-8); the instinct was right. [LC; PaletteCard.md corroboration mine]
5. **Correct, keyboard-inclusive event isolation** — the `.stop` guards a real conflict (card expand at :26) and covers Enter/Space by construction. [LC]
6. **Honest receipts under wrong verdicts** — the corpus hash-pins true bytes (re-verified this session), and the REPORT-AUTHORED rows are registered (HYDRATION-LEDGER:270-272): a corpus wrong in eight-plus places was auditable in one pass because its receipts are true. The provenance discipline outlived the analysis it carried. [DU; registry verification mine]
7. **Self-incriminating documentation** — :2-4 names the split's motive ("PP-8 cap cure"), handing ledger 23635 its primary source. Code that gives its auditor the indictment is rare and is why that finding is provable. [DU]
8. **The file does not lie about its complexity — and that is the corpus's method lesson.** 64 lines, zero state, zero lifecycle; nothing hidden. Yet of the rows that survive, the gravest (dead button, split-brain, lying label, nested seam, starved title) are visible only from OUTSIDE the file — in AdminUsersPanel, PaletteCardMenu, usePaletteStore, MixSourceSelector, and the parent's flex row. Three axes read 64 transparent lines and produced eight killable claims because they read ONLY those 64 lines. The component's honesty is real; the audit's blast radius was too small to use it. [both readers, adopted as this record's standing lesson]

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1** — contrast of `text-micro text-muted-foreground` on `bg-muted/60` over the card's `bg-well`, BOTH schemes, plus the `prefers-contrast: more` arm (the pill's alpha may sit outside the foundation.css contrast raise the way AP-16's chip did). 11px is below the large-text exemption; D asserted "low-contrast strip" and measured nothing; static resolution defeated both readers (color-mix over token layers). Sample computed styles at 390 and 1440, light/dark.
- **U-2** — tag-pill overflow geometry at 390×844 with 3 × 30-char tags: does content escape the card's painted bounds (the card deliberately carries no clip) or stop at the starved title? The number is already commissioned: **X-W7 G9's MEASURE-AT-OPEN** records it; do not duplicate the probe.
- **U-3** — AT announcement matrix for `title`-on-span + nameless SVG (VoiceOver/NVDA/JAWS). The PM-3 cure is correct under every outcome; this gates nothing.
- **U-4** — the C-4 double-click race's actual wire outcome (two POSTs landing out of order). Mechanism source-certain; network timing unwitnessed. A synthetic two-click probe at the inspector rebuild suffices.
- **U-5** — the served-CSSOM winner for the two `.fira-code` definitions (PM-19). Property-level analysis says visibly identical today; confirm before deleting the fork.
- *(Not residue: the 23.6px target height — ledger 23622 measured it through the live cascade; LC's clamp arithmetic is corroboration, not the primary.)*

**Scope note (recorded, not booked)**: D's demanded "no metadata" cell is vacuous by construction — all five roots are v-if-gated; the empty case renders zero DOM. A required-cell over-specification, not a false defect claim. [DU's framing, adopted]

## DISSENT — preserved, not ruled away

1. **Citation-discipline event (evidence about the fleet, not the tree)**: reader-DU attributed the Mix nested-button BLOCKER to this component's vote button while quoting ledger 24360, whose own `sample` names `innerInteractive:["BUTTON:Palette menu"]` — the receipt refuted the attribution in the same sentence that cited it. The verdict class survived (the seam is real, banked at PC-11); the attribution did not. Mirrors the ColorSpaceSelector DISSENT #1 lesson: a quoted receipt is not a read receipt.
2. **Reader-LC's locality framing adopted as standing law**: "safe by locality, not by design" — three of four listener-less PaletteCard hosts avoid the dead-vote-button defect only because their feeds happen to be local. Any wave that lets a remote palette reach those hosts revives PM-9 there; the X-W7.d per-host availability gate is the structural cure.
3. **Severity ordering (unresolved, preserved)**: DU would rank PM-10 (false facts) above PM-9 (dead control); LC the reverse ("a control that lies about existing beats facts that lie about a copy"). Both are MAJOR and both route to X-W7.d/c; no ruling required.

## Verdict

**SOURCE-RED ADJUDICATED — 21 rows confirmed (9 MAJOR · 7 MINOR · 5 INFO), 8 claims killed, 1 BLOCKER rescoped to its banked identity (PC-11); 13 of the survivors were invisible to all three axes.** The component is an honest, careful 64-line leaf — correct guard, correct thresholds, correct event isolation — whose serious defects live at its seams: a vote seat that lies to AT, dies unheard at one host, double-fires at another, misses the touch floor everywhere phones exist, and a provenance readout that goes stale the moment its own Save runs. Every surviving row lands on a surface a ratified X wave already owns by name (X-W7 G8/G9/G12/G17 born-RED · X.W7.c/d/f · X-W10 · X-W1 · one glass relay · two riders · one NO-WAVE-OWNER observation); nothing here stamps a wave, and the original challenger records remain immutable beside this file per E-1/E-3.

— end of apotheosis.
