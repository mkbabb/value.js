claude-fable-5 (served model id — read from this seat's own system prompt, not inherited)

# fr-UserSlugBar — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator)

**Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/gallery/UserSlugBar.vue` (168 lines, read whole by this seat) · fourier HEAD `cd26c65`, branch `m/w1-bump-migration`, tree DIRTY (the in-flight M-bump); **the subject file is CLEAN** (`git status --porcelain` on the path → empty). Installed producer `@mkbabb/glass-ui@4.0.0` (node-read of the installed manifest; **80-key export map, `./dom` / `./forms` / `./button` all live** — my node run).
**Date**: 2026-08-25 · **Corpus**: three challenge axes (D: B1-B4 + M1-M13 + m1-m8 + i1-i5 + §7 uplift table · L: 1-17 + S1-S3 · C: §0 + B1-B2 + M1-M5 + m1-m6 + i1-i3 + S1-S6) at `audit/fourier-components/UserSlugBar/`, challenged by two hostile re-readers (r1 = design-and-usage · r2 = library-and-code), both returns adjudicated here.
**Method (M-23/L-18)**: every reader-vs-reader disagreement, every kill of a load-bearing cell, and every missed find was re-derived against the trees by this seat with its own instruments before adoption. Zero browser probes — every ruling is bytes (read + grep + node + git). Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments**: subject whole (168 lines — the `loggingIn` decider, the `:121`/`:141` aria-label anchors, the `:135` sole keydown binding, the `:94/:106` host-level vs `:124` svg-level `size-5` split); installed `dist/button-BNDWhAZb.js` — CVA base at **:49** verbatim (`[&_svg:not([class*=size-])]:size-(--ui-glyph)` intact), `ghost:` at **:60**, `outline:` verbatim, **`icon: "h-(--control-h-md) w-(--control-h-md) p-0"` at :71**, `sm: "h-(--control-h-sm) rounded-pill px-3"`, `"data-size": r.size` binding; `dist/cn-DJXf4yaB.js` — **`["width",/^w-/]` · `["height",/^h-/]` · `["size",/^size-/]` three separate groups**, conflict key `${scope}|${group}` last-wins (the D-m4/D-M1 decider); `dist/styles/components.css` — **`grep -c "@layer"` = 0**, `.h-\(--control-h-md\)` and `.w-\(--control-h-md\)` and `.h-\(--control-h-sm\)` present precompiled, **`.size-4` the ONLY `.size-N` class shipped**; `dist/styles/index.css:201` imports components.css unconditionally, `:222` `@source "../*.js"` (the K-2 backstop, comment self-documenting); `dist/styles/tokens/offsets-sizing.css` — `--control-h-md: max(calc(2.5rem * var(--ui-scale)), var(--control-floor))`, `--ui-glyph: calc(1rem * var(--ui-scale))` with the `:not([class*=size-])` escape comment verbatim; `dist/styles/tokens/light-dark.css` — `@media (pointer: coarse) { --ui-scale: var(--ui-coarse-scale, 1.5); --control-floor: var(--touch-target, 2.75rem) }` verbatim; `dist/styles/utilities/a11y-overrides.css:116-123` — the `[data-size="icon"]` min-block/inline-size floor verbatim, its own comment "only RAISES — a larger explicit size is never lost"; `dist/dom.js` — **`export { … n as useClipboard … }` (the MPC-19 decider)**; `dist/composables/dom/useClipboard.d.ts` whole (`copied: Ref<boolean>` · `copy(): Promise<CopyResult>` · `onCopyError` "Surfaces the failure instead of swallowing it" — all verbatim); fourier: `AppHeader.vue:136` (`ml-auto … shrink-0` cluster) + `.header-inner` block + 640px arm verbatim; `App.vue:20-32` whole (`h-dvh flex flex-col … overflow-hidden` shell, header direct child, `<Toaster/>` outside); `style.css:41/:47` (18px root below 768px) + `:136-141` focus-visible allowlist (four classes, not this input); `stores/auth.ts:1-90` whole; `composables/useToast.ts` whole; `useSafeStorage.ts:13-27`; `lib/api.ts:52-71/:158-182/:468-480` (abort registry, 429 retry, `loginWithSlug` no-signal, dead `getMe`); `lib/types.ts:100-103` (`user_slug` NON-optional); `api/routers/sessions.py:56-99` (both `asyncio.sleep(0.2)` INSIDE login; **logout `:92-99` = no sleep, idempotent, `{"ok": True}` unconditional — the K3 decider**); `api/lib/crud/slugs.py:15/:40-42`; `e2e/gallery.spec.ts:66-82` verbatim (the whole body inside `if (await loginBtn.isVisible().catch(() => false))`); `playwright.config.ts:44-52` (**one project: chromium/Desktop Chrome**); original challenge filings re-read at the disputed cells (D-M5 `:233-249` — the original filing's guard-location claim is EXACT; D-M6 table; D-M10 `:332-345`; C-B1 falsifier `:77`; L-4 header `:102`); banked identities re-read in place: **fr-AppHeader FR-AH-10/FR-AH-16 + SS-13 item 8 (:168)** · **fr-EquationResult FR-EQR-1 (names `UserSlugBar.vue:23`)/-24/-25/-27** · **fr-MorphPhaseConfig L-D8 (:25) + MPC-19 (:65)** · **fr-EasingCurvePreview GEO-2 (:45) + the :34 symmetry note**.

## Scoreboard

**39 adjudicated defect rows (1 BLOCKER · 15 MAJOR · 17 MINOR · 6 INFO) · 16 killed claims ratified or minted · 9 reader-missed finds adopted (all seat-verified where load-bearing) · 4 registry corrections · 8 superlatives (two narrowed) · 10 UNPROVEN-NEEDS-LIVE → SS-13.**

The component's single shape: **a 168-line auth surface that is meticulous exactly where it does not matter (the trigger's responsive classes, a tokened reduced-motion-correct transition, a correct Pinia split) and absent exactly where it does — the sole credential is minted, discarded, and reported with the login path's byte-identical toast; the login form it opens carries zero responsive treatment into a `shrink-0` cluster inside an `overflow-hidden` shell; and every geometry number in the file is authored against sizes the design system silently refuses to paint.** The BLOCKER is the compound: on a coarse-pointer portrait phone, opening the login form clips the register affordance and the DarkModeToggle past the viewport with no scroll, no wrap, and no touch dismissal — the user is stuck in a state they cannot complete or leave.

## Ruled disagreements (the adjudication's actual work)

| topic | r1 (design-and-usage) | r2 (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-M1 / D-m4 — what size do the pill buttons paint?** | KILLS both: `size-5` loses to the CVA's own `h/w-(--control-h-md)` (three separate `cn` groups; unlayered precompiled beats fourier's `@layer utilities`); buttons paint 40/45/**67.5px coarse**; the a11y floor NEVER binds | CONFIRMS D-M1 via the `a11y-overrides.css` `min-*` floor lifting 20px→44px; folds to banked fr-AppHeader item 8 | **r1 wins on mechanism and magnitude — D-M1's floor mechanism is KILLED; the conclusion survives inside FR-USB-3** | All three steps byte-verified by this seat: (1) `cn-DJXf4yaB.js` — `width`/`height`/`size` are separate groups, so `size-5` never removes `h/w-(--control-h-md)` (icon variant, button chunk :71); (2) `components.css` ships `.h\(/w\(--control-h-md\)` precompiled with **zero `@layer`** while `.size-5` is absent (only `.size-4` ships) — fourier's JIT-minted `.size-5` sits in `@layer utilities` and loses unconditionally; (3) coarse tokens: `--ui-scale: 1.5` → `--control-h-md = max(3.75rem, 2.75rem) = 3.75rem` = **67.5px at the 18px root** > the 49.5px floor, so the floor is inert (its own comment: "only RAISES"). Banked corroboration: **FR-AH-16's own token walk landed on the same 3.75rem for the sibling nav trigger.** r2's fold instinct is still honored — the visual cost stays the banked SS-13 carry — but the banked item-8 wording needs the RC-1 correction below. |
| **D-B1 severity** | BLOCKER, magnitude larger than filed (~101px fine / ~200px coarse overflow at 375px) | MAJOR (silent regrade; arithmetic corrected upward — 3 header gaps not 1, break-even ≈467px fine) | **BLOCKER — r1 wins; r2's arithmetic adopted** | With FR-USB-3's corrected geometry, on a coarse phone the clipped tail (~200px) swallows the DarkModeToggle (67.5px), the dice/register Button (67.5px) and most of the submit Button. `App.vue:24`'s `overflow-hidden` shell clips — no scrollbar, no wrap (`shrink-0`, no `flex-wrap`), and per FR-USB-2 no touch dismissal. **Register is unreachable on an entire device class and the state is a trap.** That is blocker-weight; r2 offered no demotion argument. |
| **D-B4 severity** (generate discards the credential) | BLOCKER | MAJOR | **MAJOR — r2 wins, by r2's own K1 principle applied consistently** | The minted slug is persisted (`persistUser` before any toast), rendered at ≥640px, copyable at `:96`, and re-login-able after logout (`sessions.py` deletes only the session doc). Undiscoverable ≠ unreachable — the same edge-back-to-truth reasoning that demoted L-1 demotes this. The loss case requires the environment conjunction (private-browsing/quota `safeSetItem` swallow, `useSafeStorage.ts:13-19`) which both readers correctly hold UNPROVEN. r1's dissent preserved: on a sub-640px phone the conjunction is thinner than it looks (no visible identity + silent copy failure). |
| **D-M5's mechanism (K2)** | Confirmed with the cell "`loggingIn` is written (:35) and never read" | KILLS the N-concurrent-wins narrative via `abortable()`; notes `:142` IS guarded | **r2's kill RATIFIED — and BOTH readers are corrected: the original D filing was right where r1's paraphrase is false** | Subject bytes: `loggingIn` IS read — `:142` `:disabled="!canSubmit || loggingIn"` and `:151` `:disabled="loggingIn"`. The original D-M5 (`challenge-D-design.md:233-237`) says exactly that: "the in-flight guard is on the buttons but not on the key handler." r1 introduced a false cell; r2 half-inherited it ("never reads it") before contradicting itself. The surviving defect is L-3's shape: Enter-path only, second Enter self-aborts the first (`api.ts:54-59` synchronous predecessor abort), one client winner, spurious `AbortError` toast, server-side orphan + budget spend surviving as the real harms. |
| **D-M10 severity (K3)** | MAJOR ("guaranteed ≥200ms floor per sessions.py:74") | Kills the citation — `:74` is login's timing-mitigation sleep; logout `:92-99` has no sleep, is idempotent, returns `{"ok": True}` unconditionally | **r2's kill RATIFIED → MINOR** | This seat read `sessions.py:92-99` verbatim: `delete_one` on the header token, no error branch, no sleep. Double-fire harm = a second idempotent DELETE + a duplicate toast. The missing busy affordance is real and books at MINOR (FR-USB-18). |
| **e2e vacuity severity** | MAJOR | MINOR | **MAJOR — r1 wins** | `gallery.spec.ts:66-82` verbatim: the entire body sits inside `if (await loginBtn.isVisible().catch(() => false))` — the test passes green with zero assertions precisely when UserSlugBar fails to render, which is precisely the failure family this corpus files (D-B1 clipping the trigger, D-B3 churn, any mount error). Plus `playwright.config.ts:44-52` = one chromium/Desktop-Chrome project (no coarse pointer, no sub-640 viewport — D-B1/FR-USB-3 are outside the net by construction) and zero unit runner in `web/package.json`. A gate that goes green exactly when its subject breaks is worse than no gate: it will also mask this corpus's repairs. |
| **GEO-2's anchor (r1 MISSED-6)** | The banked `:124`→`:94/:106` "correction" is BACKWARDS — host-level `size-5` does not exempt the glyph; `:124`'s svg-level class is the only true escape site | (not contested) | **ADOPTED — registry correction RC-3** | The compiled selector is `<host-class> svg:not([class*=size-])` — the `:not` guards the **svg's own** class attribute. `:94`/`:106` put `size-5` on the `<Button>` host; the descendant `<Check>/<Copy>/<LogOut>` svgs still match and are forced to `--ui-glyph`. `:124`'s `<LogIn class="size-5 sm:size-3.5">` is a lucide component that forwards its class onto the svg root — the one genuine escape. A repair following GEO-2's banked anchor would not exempt the glyph. |
| **MPC-19 vs FR-EQR-24 (r2 K5)** | (not contested) | The registry carries a live contradiction: MPC-19 says "no subpath exporting `useClipboard` exists — producer-forced"; FR-EQR-24 says `./dom` is live | **r2 RATIFIED at the bytes — registry correction RC-2** | `dist/dom.js` line 6, read by this seat: `export { … n as useClipboard, … u as useUserInvalidAria, … }`; `./dom` in the 80-key map (node run). FR-EQR-24 is correct; MPC-19's attribution cell is stale, and the `./clipboard` BH-relay ask it filed should be withdrawn or restated — `./dom` already serves it. |

## Adjudicated defect roster

Line anchors are subject HEAD `cd26c65`; re-resolve before executing any cure (the D-19 lesson). Producer anchors are the INSTALLED 4.0.0 dist as corrected here (r2's K9 drift table adopted).

### BLOCKER

- **FR-USB-1 · D-B1 ⊕ r1-MISSED-2 (clip consequence) ⊕ r2-K8 (corrected arithmetic) — ADJUDICATED BLOCKER.** The logged-out login form (`:128-157`) carries zero responsive treatment (not one `sm:` class against the trigger's four) and a fixed `w-44` input into `AppHeader.vue:136`'s `shrink-0` cluster; `.header-inner` has no `flex-wrap`/`overflow`; `App.vue:24`'s `h-dvh … overflow-hidden` shell **clips** the excess — statically decided, no scrollbar, no squeeze (the UNPROVEN-NEEDS-LIVE framing on the failure MODE is killed; only the photograph remains). Corrected min-content (r2's three-gap count, this seat's ratification): break-even ≈467px fine / ≈485px+ coarse — past every phone in portrait. With FR-USB-3's real geometry the coarse-pointer clipped tail ≈200px at 375px: **the DarkModeToggle and the dice/register Button are fully off-viewport, most of submit with them** — the register path is dead on the whole device class, and per FR-USB-2 the state has no touch exit. r1-MISSED-2 folds in as the consequence clause (the shell fact is banked FR-AH-10; the consequence was booked nowhere). **→ F.W4** (responsive rewrite of the open-form state against PAINTED sizes) **+ SS-13** (375×667 + 430×932, logged out, form OPEN — the photograph only).

### MAJOR

- **FR-USB-2 · r1-MISSED-3 — ADJUDICATED MAJOR (new).** No dismissal affordance on touch at all: `showLogin` is lowered at exactly `:38` (login success), `:52` (generate success), `:75` (hardware Escape — which additionally requires focus inside the input, FR-USB-32). No close button, no outside-click, no `@blur`; zero listeners in the SFC. On touch the user who taps "Log in" and changes their mind has no way back short of completing an auth mutation or reloading — while FR-USB-1 holds the header clipped. Mechanism precedent banked at fr-PaperSearch D-7/L-7 (different component); new instance, new consequence, booked here. **→ F.W4.**
- **FR-USB-3 · r1-MISSED-1 (replaces killed D-M1/D-M12/D-m4/D-m6's shared premise) — ADJUDICATED MAJOR (new; the geometry-truth row).** All five host-level `size-N` overrides are INERT — `size-5` (`:94`,`:106`), `size-7` (`:140`,`:150`), `size-10`'s height leg (`:120`) — silently defeated by the CVA's own `h/w-(--control-h-*)` (three-step mechanism byte-verified by this seat, see the ruling table; halves banked at fr-ContourSettings K-3 + fr-CanvasOverlayButton R-1/FR-COB-8, corroborated by FR-AH-16). Painted: **40px desktop / 45px mobile-fine / 67.5px coarse** for `size="icon"`; `size="sm"` height 36/40.5/49.5px+. Every button in the file is 2-3× its authored size and the file's whole geometry story (including D-m7's 18px content column, which folds here) is written against numbers that never paint. No banked row applies the mechanism to the `size-*` group — that gap is what let one axis build five findings on a 20px premise. **→ F.W4** (geometry rewrite against painted sizes; note F.W1 deletes `size="icon"` as a type error anyway — D §7 U-1) **+ SS-13** (the FR-AH-16 shot now serves three rows). Carries RC-1.
- **FR-USB-4 · D-B4 / L-6 / C-B1 / C-M3 — ADJUDICATED MAJOR (r1's BLOCKER demoted, ruling table).** `handleGenerate` mints the application's sole credential and discards it: `register(): Promise<string>` returns `res.user_slug` (auth.ts); `:51` is a bare `await register();`; `:54`'s `toast("Logged in!", "success")` is byte-identical to `:40`. The affordance exists and is exercised elsewhere: `useToast.ts` `options.slug` → `` `${message} (${slug})` ``, used at `gallery.ts:230/:258`. Slug is the sole auth factor (no password/email/recovery, `sessions.py:56-81`). The private-browsing amplifier (void `safeSetItem`, refs set before persistence) is real statically, its firing UNPROVEN. Cure is one argument: `toast("Logged in!", "success", { slug })` + consuming register's return. **→ F.W4.**
- **FR-USB-5 · D-B2 ⊕ D-M9 ⊕ D-M4 — ADJUDICATED MAJOR (one element, one cure, three legs).** The credential field (`:134`) carries `outline-none` with only a border shift as focus replacement, and the numbers fail three ways — focused border vs fill **1.93:1** (SC 1.4.11), resting border vs fill **1.28:1** (re-introducing the failure glass-ui's own `.input-pill` annotation at `surfaces.css:199-212` diagnosed and fixed), placeholder **1.73-1.76:1** (SC 1.4.3, on the only visible statement of the required slug format). Contrast pipeline reproduced independently by BOTH readers to the hundredth (D 1.93/1.51/1.28 · r1 1.925/1.509/1.275 · r2 1.926/1.510/1.275) — figures of record. No global rescue: `style.css:136-141` allowlists four classes, not this one; the field carries neither `.focus-ring` nor `.input-pill`. Class-precedent fr-FunctionInput D-1/C-5 (banked); this instance books here with worse numbers. **→ F.W4** (adopt `./forms` Input / the producer's focus+border tokens — one element swap cures all three legs plus FR-USB-12).
- **FR-USB-6 · D-B3 — ADJUDICATED MAJOR (both readers' demotion from B ratified: degraded, not blocked).** Focus is destroyed on both open and close: `v-if="!showLogin"` (`:117`) / `v-else` (`:128`) are distinct vnodes; activating the trigger unmounts the focused element; zero `autofocus`/`ref`/`.focus(`/`nextTick` in the file (whole-read). Escape (`:74-77`) additionally discards typed text with no undo. **→ F.W4.**
- **FR-USB-7 · D-M5 / L-3 / C-B2 as rescoped by K2 — ADJUDICATED MAJOR.** The Enter path alone is unguarded: `onKeydown` (`:72-73`) calls `handleLogin()` unconditionally; `handleLogin` checks `canSubmit` only; the input has no `:disabled`; `slugInput` clears only after the await — while BOTH buttons are correctly guarded (`:142`, `:151`). A second Enter self-aborts the first through the shared per-key AbortController (`api.ts:54-59/:161`, `loginWithSlug` passes no signal), surfacing a spurious `AbortError` as a red auth toast beside a success toast; the aborted request may still have reached the server (session-doc orphan + a token of the 5/60s budget — UNPROVEN accounting). Window guaranteed ≥200ms (`sessions.py:67/:74`), extensible to ~60s via the 429 retry. **→ F.W4** (guard `loggingIn` in `handleLogin` + `isAbortError` in both catches) **+ F.W1** rider (7.0.0's `loading` + `aria-busy` arrive free — D §7c).
- **FR-USB-8 · L-4 — ADJUDICATED MAJOR (count corrected to 14 — r2/C exact, L's "12" and its own list disagree).** `isAbortError` is the repo posture at **14** consuming sites across 4 files (gallery ×4 · workspace ×7 · EquationPanel ×1 · EquationView ×2); `:41-43` and `:55-57` are the sole omission on exactly the two handlers wired to the self-abort of FR-USB-7. **→ F.W4** (same edit).
- **FR-USB-9 · L-5 / C-M1 — ADJUDICATED MAJOR.** `ApiProblem.detail` — the only actionable half of the server's error — is dropped: `.message === title` (`super(title)`), `:41` falls back to `response.statusText`, `detail` stored and never read (zero runtime `instanceof ApiProblem` repo-wide). `sessions.py` raises bare `HTTPException`s (FastAPI `{"detail": …}`, no `title`) → a mistyped slug reads **"Not Found"**. The `"" ?? fallback` hole at `:42`/`:56` is static (empty `statusText` under HTTP/2 → empty toast body — the transport arm UNPROVEN, SS-13). **→ F.W4** (one error-rendering helper: prefer `detail`, fall back non-empty).
- **FR-USB-10 · D-M2 / L-2 / C-M4 — ADJUDICATED MAJOR.** `copy()`'s `CopyResult` is discarded and `onCopyError` unset (`:23`, `:67-70` unawaited) — inverting the installed contract's own docblock ("the failure is REPORTED, never silently swallowed" — d.ts read verbatim by this seat), on the control that is the mobile-only path to the credential (FR-USB-13). On failure `copied` never flips: no icon change, no toast, nothing. **→ F.W4** (consume the result or pass `onCopyError` → toast) **+ F.W1** (the same `:23` destructure breaks at 7.0.0 — FR-USB-39/FR-EQR-1).
- **FR-USB-11 · D-M3 — ADJUDICATED MAJOR.** The copy confirmation is an icon swap and nothing else — no `aria-live`, no `role="status"`, no name change (`title="Copy slug"` static at `:95`); glass Button injects no live region (chunk read). Compounds with the 1.5s window. **→ F.W4** (rides FR-USB-10's cure — a toast or `sr-only` live region).
- **FR-USB-12 · D-M7 / C-M2 ⊕ D-M11 / C-M5 — ADJUDICATED MAJOR (merged: same field, same cure).** The credential field has no `autocomplete`/`name`/`id`/`pattern`/`aria-invalid`, no client-side shape validation (`canSubmit` = non-empty trim) and no retained error state, on a rate-limited path whose shape contract is published and trivially mirrorable (`SLUG_PATTERN = ^[a-z]+(-[a-z]+){3}$`, `slugs.py:15`). The cure ships at the pin behind `./forms` (`Input` declares `autocomplete/pattern/name/…`; `useUserInvalidAria` exported) — genuinely absent from the root barrel, so C-M2's discoverability-trap reading stands. **→ F.W4** (with FR-USB-5's element swap).
- **FR-USB-13 · D-M8 — ADJUDICATED MAJOR.** Below 640px the logged-in pill communicates no identity: `hidden sm:inline` on the abbreviation (`:90`), `title` unreachable on touch (`:87` — it does reach the accessibility tree, r1's partial-mitigation note adopted: desktop SR users can obtain the slug). `sm:` is a width breakpoint, not a pointer query. **→ F.W4.**
- **FR-USB-14 · D-M13 — ADJUDICATED MAJOR (arithmetic reproduced byte-exactly by both readers: min 17 / mean 26.26 / max 40 over `slug_words.json` × the 4-word contract).** `w-44` shows ≈18.8 mono characters of a credential whose mean is 26.3 — the user can never see their whole credential at any breakpoint. **→ F.W4** (rides FR-USB-5/12's field rework).
- **FR-USB-15 · L-1 / C-§0 / C-7 — ADJUDICATED MAJOR (r2's K1 demotion from BLOCKER ratified: the edge back to truth exists in this very file — logout → re-login with the still-rendered slug; what's missing is the signpost and the revalidation that would make it unnecessary).** `isLoggedIn` is a localStorage-presence check (`auth.ts:14/:21`); `getMe` has zero call sites; `grep "401"` over web/src → zero; `ensureUser` short-circuits on the stale value (`:76`). Server sessions expire at 30 days (`config.py:40`, `dependencies.py` 401). A >30-day identity renders authenticated forever until a mutation fails in a vocabulary the component doesn't speak (FR-USB-9). Defect authored in the store; the component is its sole renderer. **→ F.W5-W8** (session-truth cure: 401 handling + `getMe` revalidation at the store/api seam) **+ F.W4** rider (this component renders whatever truth the store then has).
- **FR-USB-16 · r1-MISSED-5 ≡ r2-MISSED-1 — ADJUDICATED MAJOR (r2's MINOR overruled, ruling table).** The component's only test is self-disabling (`gallery.spec.ts:66-82`: whole body inside `isVisible().catch(() => false)` — passes green with zero assertions exactly when the subject fails to render), the Playwright matrix is one Desktop-Chrome project (no coarse pointer, no mobile viewport — FR-USB-1/3 unreachable in CI by construction), and there is no unit runner in web/. Registry-swept: the vacuous-gate CLASS is banked elsewhere; the self-disabling-guard mechanism (`isVisible().catch`) is unbanked — books here. **→ F.W9/W10** (unguard the precondition — assert the trigger EXISTS; add a mobile/coarse project).

### MINOR

- **FR-USB-17 · D-M12-residue ⊕ D-M1-residue — MINOR.** What survives the two kills: the header slot still changes height between its own two auth states (`--control-h-sm` trigger ≈36px vs pill ≈46px at 16px root) and the pill/toggle pair diverge only on touch (toggle literals don't ride `--ui-scale` — the FR-AH-16 root cause, banked). **→ F.W4** (rides FR-AH-16's shared-token cure).
- **FR-USB-18 · D-M10 as demoted by K3 — MINOR.** No in-flight state or busy affordance on logout (`:62-65`, `:103-111` no `:disabled`); harm bounded — the DELETE is idempotent, unslept, `{"ok": True}` unconditional (seat-read). **→ F.W4 + F.W1** (`loading` prop).
- **FR-USB-19 · D-M6 / C-m3 — MINOR (D's MAJOR demoted — both readers agree; accname §5.2 makes `title` a valid name; the residue is consistency + touch/keyboard invisibility).** Three of five icon controls named by `title` alone, two by `aria-label`, in sixty lines. The R3-7a extension stands: a Tooltip-enumerated budget is structurally blind to these four raw `title=` sites. Anchor of record: `aria-label` at **`:121`** and `:141` (r1 right; the original D table's `:118` and r2's inheritance of it are CITE drift). **→ F.W4.**
- **FR-USB-20 · L-7 — MINOR.** `handleLogin`/`handleGenerate` are the same function twice (7 of 9 statements identical) — every repair in this corpus must be written twice until extracted. **→ F.W4.**
- **FR-USB-21 · L-8 / D-i5 — MINOR (count corrected UP: 34 `catch (e: any)` sites in web/src, not "≥20").** No lint gate exists anywhere in web/ (no script, no config, no dep); `vue-tsc` cannot see `: any`. **→ F.W0** (substrate: lint gate; the two local sites die in FR-USB-7/9's cure).
- **FR-USB-22 · L-9 / C-m6 — MINOR; FOLDS → banked FR-EQR-24 / fr-EquationView S-10 / prior art A5-05 (the subpath sweep).** `:4` subpath vs `:5` root barrel; `./dom` live at the pin (seat node-read). Bundle-weight arm correctly dead (`sideEffects`). Carries **RC-2** (the MPC-19 stale cell). **→ F.W0/W1** (rides the sweep).
- **FR-USB-23 · L-10 — MINOR.** Dead session subsystem (~17% of the store: `ensureSession`/`clearSession`/`userToken`-adjacent refs, zero external call sites — seat-corroborated greps) hiding a latent identity-mixing bootstrap (`logout` never calls `clearSession`; the `else if (sessionToken)` arm would re-attach an anonymous identity on reload if ever wired). **→ F.W5-W8** (session-subsystem decision rides the auth union).
- **FR-USB-24 · L-11 — MINOR.** The login path opts into the silent ≤60s 429 auto-retry (`retryOn429 ?? true`, two sleeps ≤30s) against a 5/60s budget with no feedback and no cancel; Escape doesn't abort (FR-USB-27). **→ F.W5-W8** (auth mutations pass `retryOn429: false` or surface the wait).
- **FR-USB-25 · L-12 ⊕ r2-K6's surviving half — MINOR.** `useToast` returns a dead `dismiss` (zero consumers) and drops `duration` (folds → banked MG-λ, fr-App.md:95); AND the local three-value vocabulary discards the producer's first-class `success`/`info` variants (5-member `ToastVariant`, Toaster paints the tones — banked FR-AUL-45 / FR-AFP-17, fold-by-reference): here `"Logged in!"` success and `"Logged out"` info render identically but for a hard-bound title string. **→ F.W4** (map success→success, info→info; one-table edit).
- **FR-USB-26 · L-13 — MINOR.** Filed under `visualization/gallery/`, consumed only by `layout/AppHeader.vue` beside its role-sibling `DarkModeToggle` (which lives in layout/); the census row inherits the misplacement. **→ F.W3** (structure/shadow sweep).
- **FR-USB-27 · C-m4 — MINOR.** Escape tears down the form without `abortInflight(["loginWithSlug"])` — the house cancel pattern at 4 workspace sites. **→ F.W4** (one line in the Escape arm).
- **FR-USB-28 · C-m5 — MINOR.** `text-green-500` raw palette literal at `:99` (paints — glass does not null the default palette; seat-trusts r1's dist grep); token-bridge discipline, repo-wide house pattern per C's own scope correction. **→ F.W4.**
- **FR-USB-29 · D-m1 / C-m2 — MINOR.** The doc example (`:27`, five-word slug) cannot occur under the server contract (four-key `_WORD_KEYS`, pattern forbids five — seat-read). Legacy-row arm UNPROVEN (login never re-validates shape). **→ F.W4** (fix the comment in any touching wave).
- **FR-USB-30 · D-m3 ⊕ r1-MISSED-6 — MINOR; the inert-`:size` half FOLDS → banked GEO-2 (which names this file).** Five `:size` props inert under the CVA svg rule (reaches consumers via the `@source "../*.js"` backstop — banked K-2, ratified; presentation attributes lose to CSS). NEW here: the GEO-2 anchor is BACKWARDS (ruling table) — carries **RC-3**. **→ F.W4** (the `[&_svg]` sweep GEO-2 already orders, with the corrected anchor).
- **FR-USB-31 · D-m7 — MINOR (corrected route ratified: `size-10`'s width leg lives because `size="sm"` has no `w-` twin; `px-3` survives sub-640; 18px content column vs 22.5px glyph).** Folds into FR-USB-3's geometry rewrite. **→ F.W4.**
- **FR-USB-32 · r1-MISSED-4 — MINOR (new instance; mechanism FOLDS → banked PSM-6 / fr-PaperSearch).** `@keydown` is bound to the `<input>` alone (`:135`); the two form Buttons are siblings, so Escape/Enter die the moment focus moves one tab stop — a keyboard user on Submit cannot abandon the form. **→ F.W4** (bind on the container, or a form + `@submit`, which also cures FR-USB-35's latency).
- **FR-USB-33 · D-m2 / C-m1 — MINOR (arithmetic reproduced exactly ×2: 128⁴ = 268,435,456 vs 251,712 initials = 1066.4× lossy).** The abbreviation is the only logged-in identity affordance and it is hidden below 640px. **→ F.W4** (rides FR-USB-13/14's identity cure).

### INFO

- **FR-USB-34 · D-i1 ⊕ r2-MISSED-3 ⊕ L-14 — INFO.** THREE dead guards inside the `v-if="isLoggedIn"` branch (`:29`, `:68`, `:87` — all dead because `isLoggedIn ≡ !!userSlug`), plus the `w[0]`-element-`undefined` type-truth (no `noUncheckedIndexedAccess`; `join` coerces — unreachable). Carries **RC-4**: banked FR-EQR-25 cites `:67-70` as the exemplar guard another component should adopt — the cure prescription there survives, but its provenance points at a provably dead line; re-point. **NO-WAVE-OWNER** (epistemic record + registry correction).
- **FR-USB-35 · L-15 — INFO.** All five Buttons render implicit `type="submit"` (`type: undefined` forwarded); latent (no `<form>` ancestor — seat-corroborated); becomes live the moment FR-USB-32's form cure lands — and is **cured free at 7.0.0** (`type: nativeButton ? (props.type ?? "button") : undefined`, r1's uplift note adopted). **→ F.W1.**
- **FR-USB-36 · r2-MISSED-2 — INFO (new).** `SessionResponse.user_slug` is non-optional (`types.ts:100-103`, both server producers always emit it) yet both store guards test `!res.user_slug` — dead branches whose developer-facing throw ("Server did not return a user slug") is wired verbatim into this component's user-facing toast via `e.message`. Either the type is wrong or the guards are; and the toast seam renders whichever. **→ F.W4** (rides FR-USB-9's error-rendering cure).
- **FR-USB-37 · C-i2 — INFO.** The bearer credential sits in a `title` attribute on a globally mounted element on every route (`:87`); localStorage already holds the token, so surface-without-worst-case. **NO-WAVE-OWNER** (record; FR-USB-13's cure will likely rework the attribute anyway).
- **FR-USB-38 · L-16 / L-17 / D-i2 / C-i3 — INFO (negative carries, all three correct-not-defects).** Zero value.js/keyframes/canvas/rAF/listener/timer contact — **do NOT book this component into the F.W2 migration surface** (C-i3's carry, ratified: the whole import surface is `:2-8`); the R5-7 loop membership is vacuous while its native-`<input>` superclass is live (a component-callsite-keyed derivation cannot see `:129-136`). **NO-WAVE-OWNER.**
- **FR-USB-39 · D §7 (U-1..U-4, 7b, 7c, 7d) ⊕ r2-K4's added row — INFO (the F.W1 uplift table, every original cell git-proven at the named 7.0.0 baseline `e286d992` by both readers).** `size="icon"` = hard vue-tsc break ×4 (`ButtonSize` has no `"icon"`); `variant` = silent visual break ×5 (no `variant` prop; `emphasis` default repaints); `ToastVariant` definition-absent at 7.0.0 src; the coarse floor re-keys to `[data-control-target]` stamped only on `iconOnly`; `loading`/`aria-busy` free. **EXTENDED BY ONE ROW (r2-K4, ratified via banked FR-EQR-1 which names `UserSlugBar.vue:23`)**: `copied` is deleted at ≥7.0.0 (`{ status, copy, invalidate }`) — the `:23` destructure yields `undefined` and `v-if="copied"` goes permanently false with no type error. §7d's "explicit non-findings" roll-call is therefore incomplete by one and MUST NOT be used to close this file's F.W1 budget. **→ F.W1.**

## Killed-claims register (16)

1. **D-m4** ("tailwind-merge strips the `h-(--control-h-*)` cohort; the component grows from nowhere") — **KILLED, ratified at the bytes and INVERTED**: `cn` is a bespoke resolver with `size`/`width`/`height` as separate groups; both class sets ship; unlayered precompiled beats fourier's layered JIT; every control rides `--ui-scale` — the exact opposite of the claim.
2. **D-M1-as-mechanism** (20px children lifted to 44-49.5px by the `min-*` floor) — **KILLED over r2's confirmation**: the CVA's own `h/w-(--control-h-md)` paints 67.5px coarse, above the floor, which never binds ("only RAISES", its own comment). Conclusion (header blowout) survives inside FR-USB-1/3; the banked SS-13 carry keeps its identity under RC-1.
3. **D-M12** (no shared optical scale, 1:1.57/1:1.83 ratios) — **KILLED, ratified**: its falsifier invoked tailwind-merge, which glass-ui does not call (banked dead at FR-NP-26); real ratios ≈1:0.96 logged-in (matched) and 1:1.22 logged-out. Residue → FR-USB-17.
4. **D-m6** (a cross-modifier conflict "tailwind-merge cannot resolve", UNPROVEN-NEEDS-LIVE) — **KILLED, ratified**: never a merge question (different groups AND different properties); the real contest is statically decided (unlayered wins height, `sm:size-auto` wins width); the live flag was mis-filed per banked K-6.
5. **D-m5** (consumer class "silently deletes the hover-colour transition the variant defines") — **KILLED, ratified**: the ghost variant declares no transition (`:60` verbatim); scope-keyed conflict resolution preserves both `hover:` legs. Residue (redundant resting-colour override) = INFO, absorbed into FR-USB-3's rewrite.
6. **D-B1's UNPROVEN arm** (which failure mode paints) — **KILLED as UNPROVEN**: statically decidable — `App.vue:24` clips. Only the photograph remains (SS-13).
7. **r2-K1** (L-1's BLOCKER framing: "no edge back to truth") — **RATIFIED**: the edge exists in this file (logout → re-login with the still-copyable slug); undiscoverable ≠ unreachable → MAJOR (FR-USB-15).
8. **r2-K2** (D-M5's "N concurrent winners, N tokens minted" client mechanism) — **RATIFIED**: `abortable()` aborts the predecessor synchronously; one client winner; server-side orphan limb survives. **Seat correction minted**: r1's proof cell "`loggingIn` is written (:35) and never read" is FALSE — read at `:142` and `:151`; the ORIGINAL D-M5 filing had the guard location exactly right. A re-reader introduced an error the filer did not make.
9. **r2-K3** (D-M10's "guaranteed ≥200ms floor" on logout) — **RATIFIED at `sessions.py:92-99`**: the sleep is login's; logout is unslept, idempotent, unconditional-ok → MINOR (FR-USB-18).
10. **r2-K4** (D-S5's "useClipboard is uplift-safe" superlative + §7d's closed roll-call) — **RATIFIED via banked FR-EQR-1**: an export subpath surviving is not a contract surviving; `copied` dies at 7.0.0 and this file is a named consumer. → FR-USB-39.
11. **r2-K5** (banked MPC-19's "no subpath exists — producer-forced") — **RATIFIED at `dist/dom.js` line 6** (seat's own read): `useClipboard` is `./dom`-exported at the pin. → RC-2.
12. **r2-K6** (C's S-4 "toast seam closed end-to-end") — **RATIFIED, half-kill**: the 5-member producer `ToastVariant` maps success/info onto `"default"`, discarding painted tones (banked FR-AUL-45/FR-AFP-17). Surviving half (Toaster mounted, App.vue:31) intact. → FR-USB-25.
13. **r2-K7** (the three axes' "1.5s reset faithfully preserved" superlative) — **RATIFIED as NARROWED via banked FR-EQR-27**: 2000 is the producer's worked example and the repo majority; UserSlugBar is the lone deviant; preserving a constant is not choosing the right one. → the F.W4 normalize-up sweep.
14. **r2-K8** (D-B1's one-gap arithmetic) — **RATIFIED**: four flex children → three gaps; min-content ≈444.75px, break-even ≈467px fine — the correction STRENGTHENS the finding. → FR-USB-1.
15. **r2-K9 ⊕ r1-MISSED-7** (the D axis's glass-dist anchor drift: `:53`→`:49`, `:65`→`:60`, five more) — **RATIFIED as CITE-class** (banked precedent fr-EasingCurvePreview CITE): every quoted byte exists, every fourier-side anchor exact; producer anchors in this record are the corrected ones.
16. **r2-K10** (L-4's "12 sites" / L-8's "≥20") — **RATIFIED**: 14 and 34; both corrections strengthen. Counts of record in FR-USB-8/21.

## Registry corrections (carried for the next formation)

- **RC-1 — fr-AppHeader.md:168 item 8** (SS-13 carry: "the coarse-pointer **44px lift** on its two `size="icon"` buttons"): the mechanism cell is stale — the operative rule is the CVA's own `h/w-(--control-h-md)` × coarse `--ui-scale: 1.5` = **3.75rem (67.5px @18px root)**; the 44px floor never binds. Harmonize to FR-AH-16's own token walk (same registry, same file, correct number). The carry's identity and its screenshot ask survive unchanged.
- **RC-2 — fr-MorphPhaseConfig.md:25/:65 (MPC-19 / L-D8)**: "glass-ui 4.0.0 ships no subpath exporting `useClipboard` — producer-forced" is FALSE at the installed pin (`dist/dom.js` exports it; `./dom` in the 80-key map — seat's node read). FR-EQR-24 is the correct cell. The `./clipboard`/`./composables` BH-relay ask should be withdrawn or restated: `./dom` already serves it.
- **RC-3 — fr-EasingCurvePreview.md GEO-2 (:45) + the :34 symmetry note**: the `:124`→`:94/:106` anchor "correction" is backwards for the escape-hatch claim. The compiled `:not([class*=size-])` guards the **svg's own** class; `:94/:106` are host-level (`<Button class="size-5 …">` — glyphs still forced to `--ui-glyph`); `:124` (`<LogIn class="size-5 sm:size-3.5">`, class forwarded onto the svg root) is the file's only true escape site — where the D axis originally put it. A repair following the banked anchor will not exempt the glyph.
- **RC-4 — fr-EquationResult.md FR-EQR-25**: its in-tree exemplar cite ("the four-character guard exists at `UserSlugBar:67-70`") points at a provably dead guard (sole caller inside the `v-if="isLoggedIn"` branch; `isLoggedIn ≡ !!userSlug`). EquationResult's own cure prescription survives; the provenance should be re-pointed or the exemplar marked dead-in-situ.

## Superlatives (provenance preserved; two narrowed)

1. **The scoped icon-swap transition composes exactly with the producer's reduced-motion guard with no local media query** (D-S1 · L-S3 · C-S3, both readers ratified at the bytes): `:163-167` transitions precisely `opacity` + `transform` on `var(--ease-standard)`; the a11y allowlist strips transform, keeps opacity — the swap degrades to a pure cross-fade for free. `transition: all` would have defeated it.
2. **The Pinia consumption is exactly right** (both readers): `storeToRefs` for state (`:11`), plain destructure for the setup-store's closure-bound actions (`:12`); `isLoggedIn` drives the top-level `v-if`, so a lost ref would freeze the component — the most commonly botched line in a Vue codebase, correct here.
3. **The zero-emits design is load-bearing correct** (C-S5, r2 re-proved): `GalleryView.vue:101-108` watches `isLoggedIn` in BOTH directions with a mount-time refetch closing the staleness window; an emit would be a second, weaker channel for a fact the store already broadcasts.
4. **`handleLogout`'s missing try/catch is correct by construction** (C-S6, r2 sharpened): the store's `logout` is total (its one throwing call self-caught; unconditional local clear) AND the server side is total (`{"ok": True}` even with no token — this seat re-read it). Nothing to catch; a catch would advertise an impossible failure.
5. **The `useClipboard` migration provenance comment (`:19-22`) is truthful at the commit level** (r2's git instrument: `-S "useClipboard"` → `4df1a06`, 21 lines in this file) — with r2's method note preserved: `-S "navigator.clipboard"` under-reports when the comment quotes the code it replaced. **NARROWED TWICE** (K4, K7): the field it destructures dies at 7.0.0 and the constant it preserved is the repo's lone deviation.
6. **The quantitative work in this corpus is the best-calibrated in the program** (r2's S-7, seat-concurred): four independent measurements (contrast triple, slug corpus, collision arithmetic, break-even) reproduced to the hundredth or byte-exactly across two readers plus the axes — adopt the figures, do not re-derive.
7. **D's §7 uplift table** — every cell git-provable at its NAMED baseline, and it refuses to pad the F.W1 budget (§7d) — the most valuable page in the corpus, **minus one row** (K4: incomplete by the `copied` break) and minus D-S5.
8. **Falsifier discipline in the honest direction** (all three axes): C self-killed three claims, L shipped a seven-row kill table, D recorded four downgrades — every self-kill checked out. Recording a dead claim with its cause of death is the behaviour L-18 exists to reward.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **FR-USB-1 photograph**: 375×667 + 430×932, logged out, login form OPEN — which controls are fully vs partially clipped (the clip itself is source-certain).
2. **FR-USB-3 painted geometry**: 768×1024 with `pointer: coarse` — computed header height both auth states; **the same shot banked at fr-AppHeader item 8 / FR-AH-16 now serves three rows** (with RC-1's corrected expectation: controls at 67.5px, header ≈91.5px below 640px).
3. **FR-USB-9 transport arm**: is the browser-facing hop HTTP/2 (empty `statusText` → empty toast body)? One live response header on a 404 login settles it.
4. **FR-USB-7 accounting**: whether the self-aborted first login had already spent a token of the 5/60s budget (network race).
5. **FR-USB-15 tail**: the exact 401 body/toast after a >30-day token; whether Mongo TTL-evicts before the `expires_at` filter matters.
6. **FR-USB-39 / U-2**: whether vue-tsc flags `variant="ghost"`/`"outline"` at 7.0.0 or passes them as fallthrough attrs (runtime repaint is source-certain either way).
7. **FR-USB-29 legacy arm**: whether any pre-contract five-word slug survives in `db.users` (DB read; login never re-validates shape).
8. **FR-USB-10 failure arm**: whether a real cell (non-secure origin / locked-down WebView) reaches the `exec-command`/`no-api` reasons.
9. **FR-USB-4 amplifier**: whether iOS Safari private browsing actually loses the freshly minted account (the `useSafeStorage` comment's own scenario).
10. **Declined-by-both-readers, offered not filed**: whether the 150ms transform transition inside the `backdrop-blur-md` sticky layer forces a re-composite of the blurred region on canvas-driven routes (one paint trace; rides FR-AH-10's booked decision).

## Closing verdict

**ADJUDICATED — 39 rows (1 BLOCKER · 15 MAJOR · 17 MINOR · 6 INFO), 16 kills ratified/minted, 9 missed finds booked, 4 registry corrections carried.** The BLOCKER is a compound no single axis filed whole: a zero-responsive login form inside a `shrink-0` cluster inside an `overflow-hidden` shell, magnified ~1.5× by a design system whose real control heights the file's five inert `size-N` overrides never touched, with no touch dismissal from the broken state — register dead on portrait phones, and the one e2e test that could have seen it is built to pass green when the component fails to render. The deepest adjudication work was protecting the corpus from its re-readers: one reader manufactured a false cell the original filer never wrote (`loggingIn` "never read"), the other confirmed a killed mechanism off a banked row whose own registry neighbor already held the correct number. Both re-reader returns were themselves adversarially re-derived here; nothing was adopted on agreement alone. Routings are M-25 agglomeration input for the forming X·F specs — nothing here stamps a wave.

— end of apotheosis. The original challenge axes and both re-reader returns remain immutable beside this file per E-1/E-3.
