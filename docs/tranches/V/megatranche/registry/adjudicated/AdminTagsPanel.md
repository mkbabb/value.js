# AdminTagsPanel — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed from this seat's own runtime, not inherited from the spawn label

**Seat**: the fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code), each of which re-read the full challenge corpus against the tree before returning.
**Date**: 2026-08-04 · **HEAD**: `03e4bfef` (branch `tranche-u`; both subject files clean in `git status`; every line anchor in this record re-resolved at this HEAD by this seat's own reads — AdminTagsPanel.vue 126 L whole, useAdminTags.ts 110 L whole).
**Corpus**: `audit/components/AdminTagsPanel/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md` + `evidence/D-tags-cascade-replica-1440-light.png` (the single evidence artifact — a synthetic DOM-injected replica whose 67-char input is refuted as unreachable, see K-2). **Provenance, verified by this seat**: NO `challenge-*-r2.md` exists (directory listed — r2 governs where it exists; here the three r1 files govern their own axes); all three are EXISTS-ORIGINAL at `registry/HYDRATION-LEDGER.md:36-38` (hashes sighted by this seat); `AXIS-PROMOTIONS-2026-08-03.md` carries **zero** AdminTagsPanel rows; `codex-provenance/motion-quarantine.md` carries **zero** AdminTagsPanel rows, so no M-xx FALSE row constrains a verdict here and no motion defect may be carried. The quarantine's **TRUE** rows M-12/M-14 (glass-7 Skeleton accepts only `class`; `surface`/`variant` inert, proved against AdminListSkeleton) are used as *evidence* for ATP-36 — permitted, they are rulings, not quarantined claims.
**Method (L-18)**: both re-reader returns were attacked before either was adopted. Every kill was re-derived against the tree or the installed producer dist by this seat's own instruments; every load-bearing confirmation was spot-verified at source; four severity settlements between the readers were ruled, two of the readers' shared settlements were **overruled** on registry-identity law, and one of reader-1's own correction cells was refuted (K-13). No browser was opened (probe parsimony): every ruling is decidable from source, installed producer bytes, the shipped API tree, and the prior-registry identities; pixel magnitudes that are not are named UNPROVEN-NEEDS-LIVE and become visual-audit inputs.

**Arbiter's own instruments this session (all static, each re-runnable)**: whole-file reads of AdminTagsPanel.vue + useAdminTags.ts; scoped reads of useTagEdit.ts:15-45, AdminUsersPanel.vue:33-41/:146-182/:278-292, AdminPane.vue:11-13/:117-131, EmptyState.vue:1-54/:95-105, viewSchema.ts:224-232, client.ts:148-156, App.vue:60-70 (X6 note) + ErrorBoundary/`:max` census (:50/:88/:107/:133), PaneSlot.vue (KeepAlive :120), api/src/modules/admin/{schema.ts:20-40, routes/tags.ts whole, service/tags.ts whole}, api/src/modules/color/repository/tag.ts:1-20, api/src/platform/db/db.ts:98 (`{ name: 1 }, { unique: true }`), demo/palettes/api/admin-colors.ts:58-82, types.ts:96-101, demo/styles/utils.css:36-66, demo/styles/foundation.css:56-57/:126-130, demo/color-picker/index.html:211-217, both e2e tag specs' assertion + comment blocks; producer bytes: `Button.vue.d.ts` (no `variant`; `ButtonSize` incl. `"xs"`; `iconOnly` documented), `button/styles.css` (`min-block-size: var(--button-size)`; `[data-size="xs"] { padding-inline: calc(0.5rem*var(--ui-scale)) }`), `Skeleton.vue.d.ts` (`__VLS_Props = { class? }` only), `badge/index.d.ts` (VARIANT/TONE/SIZE/SURFACE, no interactive mode), `chip/chipVariants.d.ts` (`sm: "gap-1 px-2.5 py-1 text-caption"`), chip chunk `dist/chip-DFZQr6rV.js` (base class `"glass-chip glass-capsule accent-tone … font-sans text-muted-foreground select-none"`; `type: "button"` ×2; removeLabel TypeError guard; **0** `.css` imports), `dist/styles/glass/glass-chip.css` (`.glass-chip__remove` base **1.25rem × 1.25rem**; the 44px `--touch-target` seat **only** inside `@media (pointer: coarse)`), typography `utilities.css` (`.section-label { @apply text-mono-caption; color: var(--muted-foreground) }` + the light-paper carve-out `:root:not(.dark) [data-paper-field] .section-label:not(--tinted)`), `dist/styles/index.css` first statement `@layer theme, base, components, utilities`; counting: `grep -rln glass-chip` over dist → **only** the chunk + the css file + chipVariants.d.ts (0 CSS importers; `dist/styles/glass.css` 0 hits, `dist/glass-ui.css` 0 hits); `catch (e: any)` census → **23 in demo/palettes/*.ts** (10/5/3/1/1/1/1/1 per file), 26 repo-wide; `ls demo/ui` → **19** dirs / **19** index.ts; `<div class="flex-1" />` → **6 sites** (Tags:8, Audit:**23**, Users:14, Flagged:8, MixSourceSelector:185, ImageEyedropper:40); `showConfirm(` in AdminUsersPanel → def :263 + **3** call sites (:289/:316/:327); `grep -rln 'useAdminTags\|AdminTagsPanel' test/` → **0**; usePalettePorts.ts = 275 L, `tags` member :230, provide :245, key :274; `tailwindcss: ^4.3.0` (package.json:109); prior-registry corroboration read: `admin.p1.md` rows A-1/A-2/A-3 (A-2 names `AdminTagsPanel.vue:87` at proxy 3.72:1), EasingSpecimenStrip.vue:14/:98 (the sole Chip consumer). Routing authorities read: X wave specs — W3.md:87/:267 (X.W3.6 fail-closed `meta.admin` guard + NotFoundPane), W4.md:28/:124/:171 (CC-040 sub-24 targets; AdminTagsPanel + EmptyState in modify lists; A3/CC-045), W5.md:8 (hard gate: pane axis absent from viewSchema), W7.md:62/:111-123/:235-242/:455-475 (AdminTagsPanel + useTagEdit + EmptyState modify; W7.e destructive seats G14/G15 **verbatim citing `AdminTagsPanel.vue:101`**; OM-15 eyebrow deletion), W8.md:40/:76/:276/:291 (19-barrel deletion; G-7 SCCs — AdminTagsPanel enumerated in the 7-node Admin/provider SCC), W6.md:430 (the glass I-9 Chip residual, glass-owned).

## Scoreboard

**47 rows CONFIRMED (3 BLOCKER · 22 MAJOR · 12 MINOR · 10 INFO) · 13 kills in the register (12 sustained from the readers, all re-derived; 1 NEW against reader-1's own correction cell) · 14 missed-by-the-axes findings adjudicated (readers' union; all confirmed, three folded into sibling cures) + 2 further observations by this seat · dispositions routed X-W3.6 ×2 · X-W4 ×3 · X-W5 ×2 · X-W7 ×19 · X-W8 ×4 · GLASS-OWNED ×1 · 8 NO-WAVE-OWNER rows (+2 partial).**

Blocker spine: one hover-invisible 16×16 glyph fires an unconfirmed, transaction-wrapped, **database-wide** cascade delete (ATP-1/ATP-2 compound — on a touch device the *only* interaction the delete button supports is being hit by accident), and the unauthenticated route asserts "No tags yet." as fact while handing over an enabled create button that does nothing (ATP-3). The server side of the same operation is exemplary (see Superlatives) — the asymmetry between a careful server and a careless client is the component's signature.

## Challenges to the re-readers (the incredulity seat's actual work)

| target | challenge | RULING |
|---|---|---|
| Both readers' settled MAJOR on D-19/C-1/L-1 (unauth = TRUE-EMPTY) | The registry already carries this exact class at BLOCKER twice: AdminUsersPanel **L-5** ("authority costumed as an empty roster" — THE blocker of that record) and AdminAuditPanel **AAP-3** ("the second member of AdminUsersPanel L-5's BLOCKER class … one identity, never re-booked"). One identity ⇒ one grade; the grade travels with the class, and the third member adds the enabled-but-dead create button (:42 tests strings + `creating`, never the token; createTagAction's own guard at useAdminTags.ts:67 makes the click a silent no-op). | **OVERRULED — RAISED to BLOCKER** (ATP-3). |
| Both readers' BLOCKER on D-3 (companion pane) | Same law, opposite direction: the viewSchema companion-pane surface is already booked at AdminAuditPanel **AAP-13 (MAJOR, FOLD → X-W5)** — the identical schema block, the identical canon sentence, one cure (X-W5's hard gate: "the physical left/right pane axis absent from `viewSchema.ts`"). A class booked MAJOR for one member cannot be BLOCKER for its sibling. | **DEMOTED to MAJOR** (ATP-4), identity preserved. |
| reader-1's D-15 raise (MINOR→MAJOR) vs reader-2's MINOR | AAP-19 (28×36) was demoted to MINOR because the deficit was 8px on ONE axis at fine pointer. Here: 16×16 re-derived arithmetically by both readers and this seat (12px glyph + p-0.5 = 2px ×2), under the 24px floor on BOTH axes, on a **destructive** control that is also invisible at rest (ATP-2), one per tag, unbounded count. Distinguishable from AAP-19 on every limb. | **reader-1 wins — MAJOR** (ATP-25). |
| reader-2's BLOCKER on C-2 vs reader-1's MAJOR | The identical mechanism (bare `res.json()` → reactive state, ErrorBoundary wrapping the whole `<main>`) is booked at AdminAuditPanel **AAP-10 (MAJOR, NO-WAVE-OWNER)**; the shipped DTO (service/tags.ts format()) is well-formed, so the trigger requires a non-conforming server. Identity law again. | **reader-1 wins — MAJOR** (ATP-13). |
| reader-1's confirmed D-9/L-15 limb "the in-file comment claims 'the ONE loading-ink register' while being the second one" vs reader-2's kill | This seat read `demo/styles/utils.css:38-64`: the ink register is ONE (`.skeleton-ink-register`) and its header comment enumerates "**the admin tag-chip shadows** (resting plate)" as a consumer by name; AdminTagsPanel.vue:53 applies that class. The comment at :49-50 is *accurate about the ink*; what is doubled is the SHAPE. | **reader-2's kill sustained** (K-9); ATP-16 survives on the shape limb only. |
| reader-1's C-9 MAJOR | AAP-21 (same announcement class, same suite) is MINOR; here as there, both plate outcomes ARE announced (EmptyState `role="alert"`/`role="status"` — read at :17/:29 by this seat); the silent arms are data-arrival + create/delete results, which ride ATP-9's result-surface cure. | **DEMOTED to MINOR** (ATP-27). |
| reader-1's D-11 MAJOR vs reader-2's MINOR (h-7 px-2) | reader-2's static derivation adopted and re-verified at the producer: `.button { min-block-size: var(--button-size) }` + `[data-size="sm"] { --button-size: var(--control-h-sm) }` clamps `h-7` away — the override is half-inert. AAP-19 identity = MINOR. | **reader-2 wins — MINOR** (ATP-26). |
| reader-2's promotion of L-3 (hypothesis → static fact) | Re-derived whole by this seat: useTagEdit.ts:34 `loadAllTags(force = false)` with TWO short-circuits (:35 `loaded`, :36 `allTags.length`), all three call sites zero-arg (BrowsePane.vue:223, TagEditPopover.vue:81/:85), and useAdminTags' create/delete never touch the sibling cache — the `force` escape hatch has never once been pulled. | **SUSTAINED — CONFIRMED MAJOR** (ATP-19), the dead `force` param folded in. |
| reader-1's D-17 correction cell ("the Audit spacer at AdminAuditPanel.vue:24") | Measured by this seat: AdminAuditPanel.vue:**23**. The corrector's correction is itself off by one (reader-2 and the AAP record both have :23). | **REFUTED as stated** (K-13, cosmetic). |
| Neither reader's D-18 disposition | X-W7.md:62 (read by this seat) already orders the OM-15 abrogation: "delete `EmptyState.vue`'s `eyebrow`" — the "· no tags minted ·" annotation this row litigates is scheduled to die formation-side. Missed by the axes AND by both readers; recorded so the next formation does not re-litigate a surface already owned. | ATP-39 **re-homed → X-W7 (OM-15 fold), INFO**. |

## Adjudicated defect roster

Line anchors are HEAD `03e4bfef`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Routing law: rows land on the X wave that owns the surface (`W7.md` + `W4.md` both carry `AdminTagsPanel.vue` as modify; `W7.md` also carries `useTagEdit.ts`, `AdminPane.vue`, `usePalettePorts.ts`, `EmptyState.vue` (carve); `W8.md` owns barrels/SCCs/dead-attr classes; `W3.md` §X.W3.6 owns route-closure auth; `W5.md` owns the pane axis). `useAdminTags.ts` appears in **no** X wave's file list — a named gap flagged inline wherever a cure needs it. Rows whose surface no X wave names are **NO-WAVE-OWNER**, owed to the next formation boundary.

### BLOCKERS

- **ATP-1 (D:D-1 / C:C-4 core) — one click of a hover-only glyph fires an unconfirmed, unbounded, cross-collection destructive write with no result surface.** Click path re-walked whole: AdminTagsPanel.vue:98-104 bare `<button>` → :101 `@click="tagsApi.deleteTag(tag.name)"` → useAdminTags.ts:87-96 (failure = `console.warn`) → api service/tags.ts `withTransaction` { `tags.deleteByName` ; `palettes.pullTagFromAll(name, session)` } — every palette in the DB, correctly and atomically. The sibling ships the full Dialog confirmation for narrower verbs (AdminUsersPanel.vue:157-181, "…and all associated data. This cannot be undone."), driven by showConfirm/onConfirm :263-292. Canon: VISUAL-CONSTITUTION §"dangerous confirmation". **Disposition: BUILD → X-W7** — W7.e's mechanism and gate **G14 already cite this exact line** ("`AdminTagsPanel.vue:101` calls `tagsApi.deleteTag(tag.name)` on click"): `dismiss="deliberate"` + `<Button tone="destructive">` at the four destructive admin seats, network-asserted.
- **ATP-2 (D:D-2) — the delete control is invisible at rest, its only reveal is hover-gated, and on touch the invisible box remains hit-testable.** :99 verbatim `opacity-0 transition-all group-hover:opacity-100 … focus-visible:opacity-100`. Mechanism static: tailwindcss `^4.3.0` (package.json:109) emits hover-family variants inside `@media (hover: hover)`; `opacity: 0` is paint-only (hit-testing intact, no pointer-events change); on `hover: none` the sole reveal is `focus-visible`, which a tap does not grant — so on touch the button can never be *seen*, only *hit*, and hitting it IS the unconfirmed cascade of ATP-1. The compound is the grade. Served-bytes wrapper + `elementFromPoint` at opacity 0: UNPROVEN-NEEDS-LIVE. **Disposition: BUILD → X-W7** (the G14 seat rework makes the affordance rest-visible and confirmed); the 24px floor arm coordinates with X-W4 CC-040.
- **ATP-3 (D:D-19 / C:C-1 / L:L-1, RAISED) — signed out, the panel asserts "No tags yet." as fact and hands over a live-looking create button that is dead.** useAdminTags.ts:49-51 `if (!token) return;` exits before touching loading/loadError/tags → the branch chain (:52/:69/:82) lands on TRUE-EMPTY, the register the codebase reserves for a verified-empty collection (EmptyState species law, :2-13); :42's `:disabled` never tests the token while createTagAction's :67 guard makes the enabled click a silent no-op. Third member of the "authority costumed as empty" BLOCKER class (AdminUsersPanel L-5 · AAP-3) — one identity, the grade travels (see Challenges). Class census (prior seats, re-affirmed): 21 `if (!token)` sites across 5 composables, 2 of them in this file. **Disposition: BUILD → X-W3.6** (fail-closed `router.beforeEach` on `meta.admin` + NotFoundPane, W3.md:87/:267; the per-method guards DELETE behind it).

### MAJOR

- **ATP-4 (D:D-3, DEMOTED — class identity) — all five admin routes still declare the `palettes` companion + right label + resulting mobile pane selector the canon orders removed.** viewSchema.ts:224-231 byte-verified for `admin-tags`; the class = AAP-13's surface. 36.4%/512px geometry live-only. **Disposition: FOLD → X-W5** (hard gate: the physical pane axis absent from `viewSchema.ts` — one deletion kills all five rows).
- **ATP-5 (D:D-4 / L:L-2, with M7 as a binding rider) — the tag chip hand-rolls a second implementation of the producer's `Chip mode="removable"`, copying the sm geometry verbatim (`gap-1 … px-2.5 py-1` = chipVariants sm byte-for-byte, my read) while discarding the required accessible name, the always-present remove button (`type="button"`, TypeError-guarded removeLabel), the disabled axis, and the coarse-pointer seat.** In-repo consumer exists (EasingSpecimenStrip.vue:14). **Two binding caveats travel with any cure**: (i) the transposition is BLOCKED-ON ATP-23 — the producer stylesheet is orphaned, so adopting Chip today lands a control with NO geometry on either pointer (this seat's dist greps; the D-15-cure kill K-3); (ii) M7 — Chip renders slot content `font-sans text-caption` while tag names are `/^[a-z0-9-]+$/` machine identifiers (schema.ts:28-32) whose jurisdiction is mono; the adoption must pin the mono register or it trades a duplication defect for a jurisdiction defect. **Disposition: BUILD → X-W7** (the seat rework; Chip adoption contingent on the relay), producer half rides ATP-23.
- **ATP-6 (D:D-5 / C:C-11 / L:L-12 + reader-1 M5) — the create row's proportion is inverted and it cannot wrap.** :26 `flex-1 min-w-0` (primary may collapse to zero) vs :34 hard `w-36`; the falsified honesty comment :17-19; and the one-token difference from the sibling all three axes held up as precedent — AdminAuditPanel.vue:4 carries `flex-wrap`, :16 here does not (M5, the cheapest structural cure, verified). Per-viewport widths live-only. **Disposition: BUILD → X-W7.**
- **ATP-7 (D:D-6) — type hierarchy inverted: the category heading is the smaller rung (caption vs small, ratio 0.877 re-derived from clamp() tokens by reader-1, arithmetic re-checked by this seat) in the same mono family as the values it heads.** :87 `section-label` heads :94 `text-mono-small`. **Disposition: BUILD → X-W7 cure under X-W10 law.**
- **ATP-8 (D:D-7 + reader-1 M4) — the heading rides the global `text-muted-foreground` rung already booked under the 4.5:1 floor (admin.p1.md A-2 names `AdminTagsPanel.vue:87`, proxy 3.72:1 light), and the extra utility class actively DEFEATS the producer's own light-paper remedy.** M4's mechanism verified whole by this seat: `.section-label`'s carve-out `:root:not(.dark) [data-paper-field] .section-label:not(…) { color: color-mix(in oklab, var(--neutral-5), var(--foreground) 22%) }` lives in `@layer components`; index.html:217 stamps `data-paper-field` on `<body>`; the declared layer order (`@layer theme, base, components, utilities` — dist/styles/index.css first statement) makes the Tailwind utility win regardless of specificity. Deleting one redundant class is the producer-respecting cure; whether it alone clears 4.5:1 is UNPROVEN-NEEDS-LIVE (residue #3). The certified `plate-ink` rung sits un-threaded two imports away (EmptyState.vue:102-104). **Disposition: BUILD → X-W7 under X-W10 law.**
- **ATP-9 (C:C-5 / D:D-8) — create and delete have no pending, no failure, and no result surface; only load has one; refresh speaks two grammars in one suite.** useAdminTags.ts:80-82/:93-95 = `console.warn` and nothing; the interface (:15-27) has no createError/deleteError/deleting; `creating` is spent solely on :42's disabled. Sibling contrast: AdminUsersPanel.vue:36-40 `:disabled="loading"` + `animate-spin` (read by this seat) vs :10-11 here (neither). **Disposition: BUILD → X-W7** (W7's own goal line: "every mutation has exactly one owner and a visible result"; G14's seats). **Named gap**: the state members land in `useAdminTags.ts`, absent from every X file list — W7 must adopt the file or this row degrades to NO-WAVE-OWNER at execution.
- **ATP-10 (reader-2 missed, CONFIRMED) — the most ordinary human input violates the server's published contract invisibly: `toLowerCase()` is mistaken for validation.** :70-74 sends `trim().toLowerCase()`; schema.ts:28-32 demands `/^[a-z0-9-]+$/` + max(30); typing "Deep Blue" → "deep blue" → 400 ValidationError → ApiProblem thrown (client.ts:153) → swallowed at :80-82. Enabled button, no message, inputs keep their text, nothing changes. No `pattern`, no `maxlength`, no client mirror. The corpus charged generic silent-failure (ATP-9) and never named the trigger a real admin hits on attempt one. **Disposition: BUILD → X-W7** (the create seat: mirror the constraint + surface `ApiProblem.detail`), same `useAdminTags.ts` gap flagged.
- **ATP-11 (C:C-6) — branch ordering hides a successfully created tag behind a stale load error.** Chain :52 → :69 → :82 → :85; loadError cleared only at :55 (successful load), never by create; the always-rendered form (:15-47) stays operable above the error plate, so the count increments and the inputs clear while the list can never render the row. Fully static. **Disposition: BUILD → X-W7** (the terminal-branch law — AAP-2's species, one identity).
- **ATP-12 (C:C-8) — the toolbar count asserts "0 tags" over the skeletons and over the error plate — the lie the parent's own A-3 ruling forbids in prose 30 lines away.** :5-7 unconditional; AdminPane.vue:117-131 re-read: the A-3 comment + `null`-while-loading cure applied for users/names, `default: return null` for tags — the one panel with neither fix nor badge tells the forbidden lie in its own toolbar. **Disposition: BUILD → X-W7** (folds into ATP-11's terminal-branch cure).
- **ATP-13 (C:C-2, +C:C-18 folded as its sub-case) — any shape drift in GET /admin/tags reaches reactive state unvalidated and detonates in an ErrorBoundary wrapping the entire main region.** client.ts:155 bare `return res.json()` (reader-1's off-by-one correction confirmed); App.vue:50/:140 boundary spans both panes. Blank-name rows (C-18) are reachable only via the same non-conforming server. RULED MAJOR (see Challenges; AAP-10 identity). **Disposition: NO-WAVE-OWNER** (`demo/platform/transport/` in no X file list — the AAP-10 row's own words), one class, never re-booked.
- **ATP-14 (C:C-3 + reader-2 missed-7) — no in-flight guard, no sequence number, no AbortController; last-to-resolve wins; and the sibling's one-prop guard (`:disabled="loading"`) sits three files away unadopted.** :49-63 re-read; :10 binds Refresh with no disabled. **Disposition: split** — the sequencing arm NO-WAVE-OWNER (AAP-11 identity, composable unowned); the `:disabled="loading"` + `animate-spin` interim rider rides X-W7's file touch.
- **ATP-15 (C:C-10 + reader-1 M8 / reader-2 missed-6, folded) — the only gates are two e2e specs that assert a request fired and nothing else, one authored AROUND the ATP-2 defect (`dispatchEvent("click")` past actionability), both justifying their selectors with a dual-mount the shell retired (X6, App.vue:61-70).** Assertion sets read verbatim by this seat (tag-create :39-40; tag-delete :49); `test/` census 0. **Disposition: NO-WAVE-OWNER as a row, with the AAP-12 binding rider: whichever wave cures ATP-1..ATP-3 lands the born-RED tag-route spec FIRST (E-1), seeded from C's mutation table; the stale X6 prose dies in the same edit.**
- **ATP-16 (D:D-9 / L:L-15, comment limb corrected per K-9) — the loading skeleton is not the silhouette of its settled state (zero heading shadows, one flat row of 5 decorative-width pills vs N labeled groups), and Tags is the only panel inlining its own shape instead of composing the shared atom.** Ink-register limb KILLED (utils.css names the tag-chip shadows as a consumer of the ONE register — this seat's read). **Disposition: BUILD → X-W7** (skeleton recomposition; ATP-36 dies in the same edit).
- **ATP-17 (D:D-10, structural limb only) — the five-route suite ships three list grammars and Tags has none: no filter, no pagination, no ordering control, no cap; the endpoint returns the whole collection** (`find().sort({name:1}).toArray()`, my read; SearchBar gated to users/names at AdminPane.vue:11-13, my read). The rendered-consequence limb is killed (K-2) and the magnitude is bounded by the server's 30-char cap — the residual mechanism (30 chars of Fira wraps at 320px) survives at half the claimed size. **Disposition: BUILD → X-W7.**
- **ATP-18 (D:D-12) — the error plate pours any exception string verbatim, centred, with no length budget and no disclosure** (:72 → EmptyState.vue:23-25 `max-w-[44ch] break-words`, no truncation — reader-1's citation corrections adopted; the detail rides `plate-ink`, so "full weight" was overstated). **Disposition: NO-WAVE-OWNER** (shared-atom contract, AAP-16 family) — with a named candidate: both X-W4 (:171) and X-W7 (:123) carve `EmptyState.vue` and either may adopt the budget.
- **ATP-19 (L:L-3, PROMOTED to static fact + reader-2's dead-`force` missed finding folded) — "Tag" has four client homes and two unreconciled caches: an admin tag write leaves TagEditPopover's catalog stale for the session, and the one reconciliation mechanism (`loadAllTags(force)`) has never been invoked with an argument.** Re-derived whole by this seat (see Challenges). **Disposition: BUILD → X-W7** — `useTagEdit.ts` is in W7's modify list (:118); the domain split owns the reconciliation.
- **ATP-20 (L:L-4, +C:C-19 folded) — ADMIN_PORT_KEY is a 34-member god port; this panel injects all of it (:122 `inject(ADMIN_PORT_KEY)!`, no guard) to read one member (`pm.tags`).** Port facts re-anchored (:230/:245/:274; 275 L). Identity: AdminUsersPanel L-2 / AAP-28, riding X-W8's `usePalettePorts.ts` carve. **Disposition: FOLD → X-W8.**
- **ATP-21 (L:L-5) — keys colocated with the provider (leaf imports the composition root) + the state layer type-imports the view layer, closing the Admin/provider SCC through this component's barrel.** W8.md:291 enumerates AdminTagsPanel in the 7-node SCC by name. **Disposition: FOLD → X-W8 G-7** (SCC → zero; keys to a keys module per the house idiom at color-session/keys.ts).
- **ATP-22 (L:L-6, count corrected 19-not-20 per K-10) — two live import paths to one design system; 19 pure alias barrels flatten the producer's subpath map.** The second-order cost is PROVEN by this corpus: the alias layer is why three axes read `chipVariants.d.ts` and none noticed the stylesheet never reaches the page (ATP-23), and why `variant="outline"` survived three hostile reads (ATP-24). **Disposition: FOLD → X-W8 scope 2** (19 barrels deleted, 92 imports re-pointed, 48 files).
- **ATP-23 (reader-1 M1, CONFIRMED by this seat's own dist greps) — GLASS-OWNED producer defect: glass-ui 7.0.0 ships Chip's entire stylesheet and never imports it.** `dist/styles/glass/glass-chip.css` exists with the full register (interactive/cell/remove/selected/PRM carve-out/coarse 44px seat); importers across `dist/styles/glass.css` (18 imports, chip absent), `dist/styles/index.css`, `dist/glass-ui.css`: **zero**; the chip chunk imports no CSS (grep 0); demo greps for `glass-chip`: 0. Consequences: the repo's sole Chip consumer (EasingSpecimenStrip.vue:98, `mode="selectable" shape="cell"`) renders with no cell geometry, and the corpus's headline prescribed cure (ATP-5) is structurally void until fixed. **Disposition: GLASS-OWNED → the standing BH relay** (owner edict; the batched BJ communique), filed beside the standing glass I-9 Chip residual (W6.md:430) as a NEW producer fact; consumer adoption of Chip anywhere in this repo is BLOCKED-ON the relay's discharge.
- **ATP-24 (reader-1 M2 / reader-2 convergent) — `variant="outline"` is a dead prop on every Button in this file (:10, :38, :75); glass Button 7.0.0 has no `variant` axis (d.ts re-read: emphasis/tone/size/iconOnly/loading only; chunk `variant` grep 0); all three render at default `emphasis="secondary"`.** Identity: AdminUsersPanel L-3 / AAP-7 / CC-081 — the fleet class (51/22 at `c654824e`), already RED-gated. **Disposition: FOLD → X-W8** (the class gate + barrel re-point kill it in one touch).
- **ATP-25 (D:D-15, defect limb; RULED MAJOR — see Challenges) — every delete target is 16×16 CSS px (12px glyph + 2px padding, derived statically), below SC 2.5.8's 24×24 floor on both axes, one per tag, unbounded in count, and invisible at rest (ATP-2 compound).** The prescribed Chip cure is killed (K-3: producer base = 20×20; 44px coarse-only; and the stylesheet is orphaned anyway). **Disposition: BUILD → X-W4** (CC-040 sub-24 family; the cure is local geometry ≥24×24 or the W7 seat rework, whichever lands first — cross-declared from both ends per the W4-D2 lesson).

### MINOR

- **ATP-26 (D:D-11 / C:C-12 / L:L-12 + reader-1 M6 / reader-2 convergent, folded) — `size="sm" class="h-7 px-2"` re-pins a height the producer clamps away (`min-block-size` wins; only px-2 takes) while the exact wanted rung ships unused: `size="xs"` (`--control-h-xs`, padding-inline 0.5rem = px-2, coarse floor preserved) + `iconOnly`.** Both readers' cure convergence adopted; K-8 kills the axes' new-producer-rung ask. Identity: AAP-19. **Disposition: FOLD → X-W4 gate A3 / CC-045** (`size="xs" iconOnly`, consumer one-liner ×2 here + siblings).
- **ATP-27 (C:C-9, DEMOTED — see Challenges) — the only status region rides the skeleton wrapper destroyed at the moment there is news; no aria-live for data-arrival or mutation results; the house idiom sits in PaginationBar.vue:17.** **Disposition: BUILD → X-W4** (semantic-controls law), announced-results arm rides ATP-9's surfaces.
- **ATP-28 (C:C-14, type limb; runtime limb killed K-7) — `groupedTags` published as writable `Ref` (:23) while assigned a readonly computed (:39); a consumer write is a silent no-op in prod (worse than the charged throw), and TS's readonly non-checking keeps it green.** **Disposition: NO-WAVE-OWNER** (one-line honesty fix in an unowned file).
- **ATP-29 (C:C-15, structural premises source-confirmed) — `onMounted` under KeepAlive `:max` (6 desktop-left / 9 mobile vs 11 left keys; PaneSlot.vue:120; App.vue's own admin-eviction design note) makes refresh-on-entry a function of LRU pressure; `onActivated` is the correct hook.** Per-path eviction outcomes live-only. **Disposition: rider on X-W7's file touch.**
- **ATP-30 (C:C-16 / L:L-14, counts corrected per K-11 — 23 `catch (e: any)` in demo/palettes, not 11) — error derivation re-implemented in `any` per composable, discarding the typed RFC 7807 `ApiProblem` the transport already throws; `.detail` (the field authored for display) read nowhere on this path.** **Disposition: NO-WAVE-OWNER** (AAP-16 family identity — the class cure is one narrowing helper at the transport seam).
- **ATP-31 (D:D-13) — `ml-0.5` physical margin on the file's one directional idiom; no `ms-`; parent `gap-1` already separates.** **Disposition: BUILD → X-W7** (dies with the ATP-1 seat rework).
- **ATP-32 (D:D-16) — three count grammars in one suite (header badge ×2, toolbar-left ×2, toolbar-right ×1) — all placements re-verified.** **Disposition: BUILD → X-W7** (one grammar decided with ATP-12).
- **ATP-33 (L:L-8) — one view identity, seven independent declaration sites, six invisible to the type system.** **Disposition: partial FOLD → X-W5** (the pane-axis members die there); the identity-unification remainder **NO-WAVE-OWNER**.
- **ATP-34 (L:L-9) — `meta: { admin: true }` is dead declaration: zero guards read it (grep re-run by prior seats; router = URL bus).** Pairs with ATP-3 — the ungated route is why the unauthenticated render is reachable. **Disposition: FOLD → X-W3.6** (the fail-closed guard makes the flag live; W3.md:87).
- **ATP-35 (L:L-10) — tsconfig.demo.json `paths` drift (3 phantom targets, 2 shipped subpaths missing).** Identity: ColorSpaceSelector L-14 / AAP-32 — the W-HYGIENE config-parity fold no X wave claims. **Disposition: NO-WAVE-OWNER, flagged again at the formation boundary.**
- **ATP-36 (reader-1 M3 / reader-2 convergent missed) — `surface="glass"` and `variant="breath"` on `<Skeleton>` (:60-61) are inert fallthrough attributes (d.ts: `class` only — re-read by this seat); identity = motion-quarantine rows M-12/M-14 (TRUE, AdminListSkeleton), the ruling every seat had available and none applied.** Second-order: the standing owner bracket admin.p1.md **A-1** asks breath-vs-shimmer **on this exact line range** — a question that cannot be answered while the prop is dead; A-1's "measured `animation:skeleton-breath-cycle 6s`" dates from the pre-7 producer. **Disposition: BUILD → X-W7** (the attrs die with ATP-16's recomposition); the A-1 bracket impossibility relayed to the owner's bracket roster.
- **ATP-37 (reader-2 missed, CONFIRMED) — the create row is a form with no form semantics and no keyboard submit: Enter is a no-op in both fields; the house idiom (`@keydown.enter`) exists at three cited sites.** No `<form>`, no `@submit`, no key handler anywhere in the file (my read). **Disposition: BUILD → X-W7** (the create seat), semantics arm coordinated with X-W4.

### INFO

- **ATP-38 (D:D-17, DEMOTED per K-12)** — the `flex-1` spacer is a consistent six-site suite idiom, not a KISS violation; the surviving observation is "spacer where `justify-between` would do". No disposition.
- **ATP-39 (D:D-18, re-homed — see Challenges)** — the specimen-plate trio + eyebrow borrowed for "no tags"; X-W7's OM-15 abrogation (:62) already deletes the eyebrow prop wholesale. **Disposition: FOLD → X-W7 (OM-15).**
- **ATP-40 (D:D-20 / C:C-17, + this seat's third site)** — `@click="tagsApi.loadTags"` (:10, reference) vs `:75` (call) — and :43's `@click="tagsApi.createTag"` is a third bare-reference site neither axis nor reader listed (inert: both fns take no params). Rider on X-W7's file touch.
- **ATP-41 (C:C-19)** — `inject(ADMIN_PORT_KEY)!` systemic across the cluster; folds into ATP-20's cure.
- **ATP-42 (reader-1 M9)** — POST returns a structurally narrower object than GET (`{id,name,category}` vs +`createdAt` — service re-read) and the client types both as `Tag` (createdAt optional, types.ts:96-101), appending narrow into wide. Latent; the one in-class instance originating from the product's own server. **Disposition: NO-WAVE-OWNER.**
- **ATP-43 (reader-1 M10)** — the delete `<button>` has no `type` (defaults submit); no ancestor form today; the producer sets `type="button"` explicitly (chunk re-read). Dies with the ATP-1 seat rework → X-W7 rider.
- **ATP-44 (L:L-16)** — 2,120 LOC of moderation console importing nothing from the library it demonstrates; context, correctly framed, no disposition.
- **ATP-45 (L:L-11)** — local `dist/subpaths/css.d.ts` drift vs published 4.0.0; on-disk fact confirmed, attribution to HEAD source requires a clean rebuild (residue #7); W-HYGIENE family.
- **ATP-46 (D:D-14, property-scope residue only; tokenization limb killed K-4)** — `transition-all` at :99 sweeps `active:scale-95`'s transform and anything added later; PRM-clean today (quarantine §1 Guard 2 rewrites the property list under reduce — cited as the ruling, not re-litigated). X-W7 rider: scope the property list.
- **ATP-47 (this seat's own observation)** — the Retry button (:75) carries `class="font-display"`, the per-instance type override AdminAuditPanel booked as AAP-26 with "the family repeats it at 5 sibling sites" — this is one of the five; folds into that family identity → X-W7 under X-W10 law.

## Killed-claims register

Twelve kills sustained from the re-readers (each re-derived by this seat at HEAD `03e4bfef`), one new against reader-1.

- **K-1 — C:C-7 (both limbs), KILLED.** (a) Re-create cannot return 200-with-row: `db.ts:98` unique index on `name` + service catch of Mongo `11000` → `ConflictError("Tag already exists")` (both read by this seat); the measured duplicate was C's own `page.route` mock. (b) Load order cannot disagree with create order: repository sorts `{name: 1}` and the schema's `[a-z0-9-]` alphabet makes byte order ≡ localeCompare. Residual (reader-2's fair statement adopted): two ordering policies in two places, MINOR-latent, no symptom.
- **K-2 — D:D-10's rendered-consequence limb ("no name-length cap"), KILLED.** schema.ts:27-34 `max(30)` + regex, enforced at routes/tags.ts:21-24 BEFORE the service (re-read whole). The 67-char/462px lozenge is a replica input the product rejects; the sole evidence PNG depicts it. The no-list-grammar limb survives as ATP-17.
- **K-3 — the D-15/D-2 "adopt Chip and five findings dissolve" cure, KILLED TWICE.** (i) Producer base remove geometry = **1.25rem × 1.25rem** (20×20 < the 24px floor); the 44px `--touch-target` seat lives ONLY under `@media (pointer: coarse)` (glass-chip.css read by this seat); SC 2.5.8 is not pointer-conditional. (ii) None of that stylesheet is served anyway (ATP-23). The defect rows survive; the transposition arithmetic does not.
- **K-4 — D:D-14's tokenization limb ("0.2s is the Tailwind default"), KILLED.** Tailwind v4's default is 150ms; the 0.2s IS the producer token, bound by hand at foundation.css:128 `--default-transition-duration: var(--duration-fast)` (read by this seat). Only the property-scope limb survives (ATP-46).
- **K-5 — C:C-13's Badge-`dismissible` cure, KILLED.** Badge's published surface is VARIANT/TONE/SIZE/SURFACE + class (index.d.ts read by this seat) — no interactive mode; growing one would mint a SECOND removable-pill primitive beside the shipped `Chip mode="removable"`, violating the reuse edict the row itself invokes and contradicting D-4/L-2.
- **K-6 — C:C-4's universal ("gates *every* destructive admin action behind a Glass-7 ConfirmDialog"), KILLED on both limbs.** No `ConfirmDialog` exists in glass 7 (the sibling's own :279-281 comment records the fold onto Dialog; the cluster is locally composed, re-read at :157-181); and the universal is false — AdminUsersPanel.vue:150 emits `adminDeleteUserPalette` with no confirmation, against exactly **3** showConfirm call sites (:289/:316/:327, my grep). D-1's narrower phrasing survives inside ATP-1.
- **K-7 — C:C-14's runtime consequence ("throws 'computed value is readonly'"), KILLED.** Vue's setter-less computed write path warns in dev and no-ops silently in prod — no throw. The (worse) silent-vanish consequence is recorded in ATP-28.
- **K-8 — D:D-11's cure ("the tighter rung belongs in glass-ui as a new size value"), KILLED.** `ButtonSize` already ships `"xs"` and `[data-size="xs"]` implements exactly the hand-written geometry, `iconOnly` included (d.ts + styles.css read by this seat). ATP-26 carries the real cure.
- **K-9 — reader-1's D-9/L-15 limb ("the comment claims 'the ONE loading-ink register'; there are two"), KILLED (reader-2's kill, ruled for).** utils.css:38-64 names `.skeleton-ink-register`'s consumers by hand, including "the admin tag-chip shadows"; :53 applies it. One ink, accurately named; the doubled thing is the SHAPE (ATP-16).
- **K-10 — L:L-6's counts ("20 barrel files"), CORRECTED.** 19 dirs / 19 index.ts (my `ls`). Defect untouched (ATP-22); cures sized in files must be sized correctly.
- **K-11 — L:L-14's count ("`catch (e: any)` ×11"), KILLED as stated.** My census: **23** in demo/palettes/*.ts (10/5/3/1/1/1/1/1), 26 repo-wide; the "11" was the `e?.message ??` count conflated in. The defect is worse than charged (ATP-30).
- **K-12 — D:D-17's canon citation (KISS edict vs a layout node), KILLED.** The edict governs new shared dirs/wrapper components; the spacer is a consistent **six-site** idiom (my grep, sites listed in Instruments). ATP-38 keeps the residual observation at INFO.
- **K-13 — NEW (this seat, against reader-1): the D-17 correction cell "the Audit spacer at AdminAuditPanel.vue:24".** Measured :**23**. The corrector inherited D's transposition style while fixing it; cosmetic, recorded under L-9 law (decorative counts and cites get re-measured, whoever writes them).

## Superlatives (provenance preserved)

- **The server side of the operation this UI mishandles is exemplary**: service/tags.ts wraps delete + cascade in one `withTransaction` and states the invariant in BOTH failure directions in prose, with the audit emit deliberately post-txn; schema.ts constrains the input space before the service is reachable — which is precisely what killed two of the corpus's most vivid evidence blocks (K-1, K-2). The client is careless exactly where the server is careful.
- **The `error ≠ empty` species law is real, structural, and documented** (EmptyState.vue:2-13, roles :17/:29, Q6 annotation-stripping) — and this corpus's blockers are all states that reach the *wrong species*, same as the audit panel's. The atom is better than its wiring.
- **`.skeleton-ink-register` carries its own falsification receipt** (utils.css:45-56: the prior recipe measured against its ground, failed ΔL 0.007, replaced by a by-construction tone-step) — a design token with a recorded null result, and the fact that killed a reader's limb (K-9).
- **The producer Chip is a textbook discriminated union** (`removeLabel` REQUIRED on the removable arm, `never`-typed elsewhere, runtime TypeError guard, PRM carve-out, coarse seat) — every a11y obligation the hand-rolled pill dropped is encoded in its types. That its stylesheet never ships (ATP-23) is a producer wiring defect, not a design one.
- **The prior-art trail predicted this component's defects before this formation existed**: admin.p1.md A-2 booked `:87`'s contrast coordinate, A-3 booked the count lie (cured next door, not here), A-1 bracketed the skeleton register — and the motion-quarantine's M-12/M-14 held the ATP-36 proof ready-made. The registry's cross-file machinery works; the gap was seats not consulting it.
- **Two hostile seats retracting their own first readings in writing** (challenge-D's transition-timing retraction; challenge-C's parallel note) remains the corpus's strongest quality signal, carried forward from the readers with concurrence.

## UNPROVEN-NEEDS-LIVE residue (the visual-audit inputs)

1. **ATP-2's served bytes**: the generated `@media (hover: hover)` wrapper around `.group-hover\:opacity-100` + `document.elementFromPoint` hit-test at opacity 0 (mechanism static from tailwindcss ^4.3.0; the dump is not re-derivable without the dev server).
2. **All live-DOM geometry**: D-3's pane rects/36.4%; D-5/C-11's per-viewport input widths + placeholder clip; D-11/C-12's 28×36 (and the coarse 28×54 analog); D-12's ~46% body share; D-13's RTL rects; ATP-17's residual 30-char wrap magnitude at 320px. (D-6's two font sizes and D-15's 16×16 are NOT residue — re-derived arithmetically.)
3. **ATP-8 post-cure ratio**: whether deleting `text-muted-foreground` alone (restoring the paper-field carve-out's `color-mix(--neutral-5, --foreground 22%)`) clears 4.5:1 over the live plate, or plate-ink threading is still required — the carve-out's rendered ratio has never been sampled. A-2's 3.72:1 is a light-only flat-body proxy by its own grading.
4. **ATP-23's live consequence**: EasingSpecimenStrip's Chip rendering without cell geometry — one screenshot for the BH relay letter.
5. **C-axis `page.route` probes** (C-1 zero-request capture, C-3 race inversion, C-6 pillsAfterCreate, C-18 blank pill): mechanisms all source-confirmed above; the runs are environment-bound.
6. **ATP-29**: which navigation sequences actually evict `admin-tags` (LRU state); the conditional character is proven, the per-path outcome is not.
7. **ATP-45**: the css.d.ts drift's attribution to HEAD source needs one clean `npm run build`.
8. **Motion: NOTHING.** Zero quarantine rows for this component; ATP-46's property-scope residue is ruled non-vestibular by the pinned Guard 2 and may not be carried as a PRM defect by any consuming wave.

## Closing verdict

**ADJUDICATED (never VERIFIED — no wave is stamped by this file): 47 rows CONFIRMED — 3 BLOCKERs (an invisible 16×16 glyph fires an unconfirmed database-wide cascade; the invisible box stays tappable on touch; the signed-out route asserts a fact it never asked the server and arms a dead create button) · 22 MAJOR · 12 MINOR · 10 INFO; 13 kills (12 sustained from the readers re-derived, 1 new against reader-1's own correction cell); two shared reader settlements OVERRULED on registry-identity law (ATP-3 raised to BLOCKER, ATP-4 demoted to MAJOR); dispositions routed X-W3.6 ×2 · X-W4 ×3 · X-W5 ×2 · X-W7 ×19 · X-W8 ×4 · GLASS-OWNED ×1 (the orphaned glass-chip.css relay) · 8 NO-WAVE-OWNER rows + 2 partial, with one named file-list gap (`useAdminTags.ts` in no X wave) flagged for the formation boundary; the r1 corpus governs (no r2, no promotions, no motion rows); 8 residue items feed the visual audit.** The component's signature is asymmetry: a transactionally exact, schema-guarded server operated by a client that confirms nothing, reports nothing, validates nothing, and cannot be seen. Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
