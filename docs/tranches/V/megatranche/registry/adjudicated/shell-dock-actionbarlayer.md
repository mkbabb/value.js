# shell-dock-actionbarlayer — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited)

**Component**: `demo/shell/dock/layers/ActionBarLayer.vue` (158 lines) + its swap cone (`ActionToolbar.vue`, `ColorInput.vue` bindings, `GenericActionBar.vue`, `Dock.vue:153-158`, `ActionBarToggle.vue`) against installed `@mkbabb/glass-ui@7.0.0` (plain dir, dist mtime Jul 17 21:10).
**Date**: 2026-08-07 · **HEAD**: `ee9474e4` · branch `tranche-u`.
**Corpus**: the r2 challenge trio (`audit/components/shell-dock-actionbarlayer/challenge-{C-implementation,D-design,L-library}.md`; r2 governs, both pass-1 files preserved beside) + two hostile re-reader returns — **R-DU** (design-and-usage) and **R-LC** (library-and-code) — adjudicated by this seat as the incredulity seat, not a transcriber.
**Law honored**: dated evidence unrewritten (this file is new); product source READ-ONLY; zero browser probes (probe parsimony — every ruling below is decided from source + the installed dist + git + repo greps; all pixel cells are carried labeled from the challengers' chromium/WebKit runs). **Motion-quarantine check**: `codex-provenance/motion-quarantine.md` carries **no M-xx row for this component**; the nearest, **M-32**, is `shell-dock-actiontoolbar`'s D-design:21 reduced-motion cell, ruled **FALSE** there — no row of this record carries it, and no close may resurrect it.

**Arbiter's own instruments this session (all receipts mine, none inherited)**: full source read of ActionBarLayer.vue (158 lines), Dock.vue (359 lines), GenericActionBar.vue (whole), ActionToolbar.vue :1-10/:63-92, ColorInput.vue :1-20/:60-100/:130-160/:215-295, ActionBarToggle.vue :86-96/:112-136, MobileMenuDropdown.vue :36-44, keys.ts :12-30, useColorNameResolution.ts :60-72, ColorPicker.vue :313-330, App.vue provide census (:38/:257/:270-271); producer reads: layers.css WHOLE, crossfade.css WHOLE, `controls/icon-button.css` WHOLE (NOTE: the corpus cites "icon-button.css" — the real path is `dist/components/dock/styles/controls/icon-button.css`; the quoted rules are verbatim-true there), density.css, overflow.css, tokens/sizing.css, DockControl.vue.d.ts, DockCrossfade.vue.d.ts, `dist/components/dock/index.d.ts`, `dist/dock.d.ts`; dist/dock.js byte-level: the DockCrossfade reserve implementation (running max over registered faces' `scrollWidth` → `{[min-inline-size]: peak}` + `--dock-crossfade-peak`), the focus-transfer guard (`!n||!r||!n.contains(r) ||` — the K-5 decider), the active-watch early-return, `grep -c useDockCrossfadeContext dist/dock.js` → **0**, `dock-layer dock-layer--full`/`--summary` emission → 1 each (live private machinery); repo censuses: bare `:hover` in demo scoped styles (**10 files** — the D-9/K-4 decider), `actionBar.`/`actionBarContext.` member reads (11 hits; the 3-dead-fields decider), `stroke-foreground` (exactly 2 files), `dock-layer-grid` (exactly 1 hit, :101), `clearHover` (declaration+expose only), `copyAndSetInputColor` (declaration+expose only), `defineEmits` in ColorInput (0), `submitProposedName` in test/+e2e/ (the spec's own comment only), `ls test/parsing` (`timeline` only), tsconfig.demo.json:36-50 + `ls dist/ dist/subpaths/` + `/css` used 10× vs `/value` 0×, `var(--dock-control-glyph-size)` consumption → **0 rules** (declared 5×, consumed never); registry cross-reads: **Dock.md** (L-20 ≡ MT-DOCK-LAYERS-1; R-17 GLASS-OWNED roving-rail precedent; CC-083), **ColorInput.md** (D-3, D-9, the MT-COLORINPUT-1 split, the cone includes this file), **ColorSpaceSelector.md** (L-14 ⇄ W-HYGIENE), **CARRY-CUT-LEDGER** (CC-080, CC-043, CC-083), **X wave files** W4/W5/W8/W10/W11 (X.W8.d G-8/G-10 own this surface; X.W4.a/.d own the semantic/contract half).

---

## Scoreboard

**26 CONFIRMED (2 BLOCKER at this surface + 1 BLOCKER cross-component) · 13 KILLED · 13 MISSED rows adjudicated in (2 of the readers' "missed" rows were registry-blindness — already booked at ColorInput.md) · 1 GLASS-OWNED · 11 UNPROVEN-NEEDS-LIVE cells routed to the visual-audit harness.**

The center of gravity: **one transposition — delete the `useLayerTransition` shim and mount the installed `<DockCrossfade>` (X-W8.d, G-8/G-10, ≡ CC-080 ≡ Dock.md L-20 ≡ MT-DOCK-LAYERS-1, I-18 DECIDED retire-clean-break) — discharges 14 of the 26 confirmed rows**, and the strongest argument for it appears in neither axis's cure list: a component swap ends teleported content by unmount; a class swap cannot (R-LC's inert/Teleport row, ratified below).

---

## Ruled reader-vs-reader disagreements (this seat's actual work)

| # | question | R-DU | R-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| 1 | **D-9 bare `:hover`** | KILLED (K-4: house norm, 6 files beyond the two named) | CONFIRMED MINOR ("census confirms only two demo files write a bare scoped `:hover`") | **R-DU WINS — D-9 KILLED** | My census: bare `:hover` in demo scoped styles in **10 files** — GradientEasingEditor (×2), EasingSpecimenStrip:212, ImageEyedropper:279, ConsoleRail:263, SpectrumCanvas (×2), PointerDebugOverlay:264, SearchFilterBar:248, ActionButton:117, ActionBarLayer:149. Exactly ONE demo file guards (`ColorSpaceSelector.vue:257-262`, verified inside `@media (hover: hover)`). R-LC's census cell is **false**; the "convention" is a single instance. If the unguarded register is a defect it is app-wide, and this file conforms to its own siblings — incl. the five ActionButtons whose bare `:hover` fires `scale(1.2)`, a strictly larger latch. Residue: INFO app-wide census, NO-WAVE-OWNER. |
| 2 | **L-4's pricing of `reserve="inline"`** ("+141 px at rest, on every route") | Adopted as governing ("L-4 is the only axis that prices the cure … that pricing is correct … and should govern") | KILLED as priced (inactive faces are `position:absolute` — no resting tax) | **R-LC WINS on scope; residual cost restated** | crossfade.css (my read): `.dock-crossfade > .dock-face { position:absolute; inset:0; opacity:0 }`, only `.is-active { position:relative; inline-size:max-content }`. dist/dock.js (my read): the peak is written as `min-inline-size` on the crossfade instance, INSIDE the action-bar face. An out-of-flow box contributes nothing to the dock plate's intrinsic width, so the reserve cannot widen the dock on the `main` layer — **not "at rest", not "on every route"**. The TRUE residual cost, which R-LC's kill under-states: the running max includes every registered face, so **while the Tools bar is open in `actions` mode, the plate carries the input face's peak** (≈+141 px, chromium cell, viewport-dependent). X-W8.d already prescribes `reserve="inline"`; this corrected price is the ruling record it executes under. |
| 3 | **K-3: the `⋮` glyph collision with MobileMenuDropdown** | KILLED (never co-visible; breakpoint argument from D-1's own mobile cell) | CONFIRMED inside C-7/D-10 ("the glyph collision is real too … in the same dock shell") | **R-DU WINS — KILL RATIFIED, and STRENGTHENED** | My static argument is breakpoint-independent: MobileMenuDropdown renders inside the `main` DockLayer (Dock.vue:207) and the `⋮` toggle inside the `action-bar` DockLayer (:153-156) — **mutually exclusive faces of one DockLayerGroup**. layers.css steps opacity with NO transition (`transition: visibility 0s linear` only), so the leaving face is `opacity:0` in one frame; no rendered frame carries both glyphs. R-DU's breakpoint argument (`lg:hidden` at MobileMenuDropdown:38, verified) also holds. D-10's surviving legs (no AT state; three glyph species; the 168-char ternary name) are unaffected. |
| 4 | **L-6 severity** (identity no-op `provide`) | MINOR ("a no-op, not a hazard") | MAJOR | **MINOR at this file; the knock-on is X-W4.d's** | Identity chain verified by me hop-by-hop: App.vue:257 `provide(COLOR_MODEL_KEY, pipeline)` → ColorPicker.vue:172 injects the same object → :321 places it in `actionBarContext` (:315-328) → App.vue:38 renders `<Dock>` inside App's provide scope → ActionBarLayer.vue:22 re-provides the identical object under the identical key; ColorInput.vue:155 resolves it with or without :22. A deletable line is MINOR. The real weight — `colorModel: UseColorPipelineReturn` living in the dock's presentation contract solely to feed a no-op — is contract debt and rides CC-043's collapse (X-W4.d). |
| 5 | **C-2's "the header comment is false" leg** | Killed via K-1 (composable half); kept "premise is false" in the confirmed banner | Killed the whole indictment leg | **R-LC's fuller kill ADOPTED; the banner corrected** | My runs: `dist/components/dock/index.d.ts` re-exports the crossfade context **types only** (`export type { DockFaceDescriptor, DockFaceRegistration, DockCrossfadeContext }`); `grep -c useDockCrossfadeContext dist/dock.js` = **0**; the runtime export list carries `DockCrossfade`/`DockLayer` and no crossfade-context value. The comment's one attacked claim — "offers no public composable successor" — is **TRUE on the installed bytes**. The retirement stands entirely on the COMPONENT half; no row of this record may carry "the comment's premise is false". |
| 6 | **D-1 scope** | Mechanism confirmed with two corrections (not this file's defect; not mobile-specific) | BLOCKER confirmed + a one-line containment filed as missed | **BOTH ADOPTED, merged** | My Dock.vue read: `:41` `hasAnyActionBar` has no breakpoint term; `:48` `toggleActionBar()` unguarded; `:49` reconciling watch not `{immediate:true}`; `:153` conditional face; `:108-117` dispatch writes `activeLayer="action-bar"` regardless. ActionBarToggle.vue:86-93: named, `aria-pressed`, `tabindex=-1` when hidden, **no `aria-hidden`/`disabled`**. dist/dock.js active-watch early-returns when the target host is absent → all faces non-active → crossfade.css hides every face. Reachable wherever `hasAnyActionBar` is false — mobile AND desktop routes whose pane publishes no bar. Containment is one predicate; the gestalt cure is the registry-as-single-source transposition. |

---

## Adjudicated defect roster

Every row: id · severity · claim · proof · **terminal disposition**. Line anchors are HEAD `ee9474e4`; re-resolve before any cure (the D-19 lesson). ⇄ marks a cross-booked identity — ONE identity, never re-booked (anti-rename).

### Blocker-weight

- **AB-1 · C-1/D-3/L-2 — CONFIRMED (BLOCKER) ⇄ Dock.md L-20 ≡ MT-DOCK-LAYERS-1 ≡ CC-080.** The local `useLayerTransition` shim renders **no crossfade at all** — the 260 ms window decorates a single-frame hard cut. PROOF (static, decisive without a browser — my layers.css read): the ONLY transition declared for `.dock-layer` is `transition: visibility 0s linear var(--duration-normal)`; `is-leaving`/`is-active` step `opacity` between hard state rules; the sole opacity ramp is gated on `.glass-dock[data-morphing]` (the dock's own morph), and the real crossfade lives on a different element and scalar (`.dock-crossfade[data-crossfading] > .dock-face` on `--dock-t`). ActionBarLayer.vue:62/:71-79 hold state no declaration reads. Three seats' frame samples agree (carried labeled). **DISPOSITION: X-W8.d (G-8) — the row is already the standing wave core (Dock.md L-20, harm clause struck there: the leaving box occludes nothing — `pointer-events:none` + `inert` + out-of-flow; the injury is motion truth + a ~40-line dead fork).**
- **AB-2 · C-4/L-1/D-15 — CONFIRMED (BLOCKER).** The consumer hand-binds glass-ui's **live private** `.dock-layer` shell-pane contract (`dock-layer`/`is-active`/`is-leaving`, :88-94 → :104/:117) onto two component roots nested inside a real producer layer, silently inheriting every descendant-scoped shell rule: layout (`display:flex; grid-area:1/1; gap`), `min-height: var(--dock-layer-height)`, `position:absolute; inset:0` stacking, and the morph-stagger `.glass-dock[data-morphing] .dock-layer.is-active > *` opacity/scale rule. PROOF: layers.css read whole (all rules `:where(.glass-dock,.dock-layer-group)`-descendant-scoped); dist/dock.js emits exactly two private `.dock-layer` nodes (`--full`/`--summary` — my grep, 1 each), confirming L pass-2's self-correction (live machinery, not orphaned CSS) against pass-1. The class string `"dock-layer"` is hand-authored exactly once in the repo — here. **DISPOSITION: X-W8.d (G-8); L's seven-row style-equivalence carry table (challenge-L-library.md:629-644) is the wave's risk register, EXTENDED by MISS-7 below (the nested clip-path row) and the `min-height` not-carried flag.**
- **AB-3 · D-1 — CONFIRMED (BLOCKER; cross-component — NOT a defect of this file).** Activating the Tools trigger while no action-bar context exists selects a face id that never registered; the producer's active-watch early-returns; every face de-activates; the dock collapses to an empty sliver with no recovery path. The trigger survives as a named, non-`aria-hidden`, programmatically-focusable button while visually void. PROOF (all static, my reads): Dock.vue:41/:48/:49 (watch fires only on CHANGE — never for a boot-false)/:108-117/:153/:182-183; ActionBarToggle.vue:86-93 + :115-135 (`0fr` + inner `overflow:hidden`); dist/dock.js early-return + crossfade.css all-faces-hidden. **Corrections adopted**: (a) the owning surface is `Dock.vue` + `ActionBarToggle.vue` — ActionBarLayer cannot cure it; (b) NOT mobile-specific — any route where `usePaneRouter` yields no bar and the picker is unmounted. **DISPOSITION: split — containment (ONE predicate: guard `toggleActionBar` on `hasAnyActionBar`, or `disabled`+`aria-hidden` the toggle when `!visible`) rides X-W4.d (Dock.vue is in its file bounds; CC-043's unit) and is independent of the transposition; the gestalt cure (the trigger renders from the crossfade's own `faces` registry — reachable via template ref `defineExpose({faces})`, NOT via the unpublished context; K-1's tail) accretes to X-W8.d. Collapse geometry numbers: UNPROVEN-NEEDS-LIVE.**

### Major

- **AB-4 · C-2/D-4 (retirement-due half) — CONFIRMED (MAJOR).** The CARRY-LEDGER §F retirement condition is met at HEAD: glass-ui 7.0.0 ships `DockCrossfade` + `DockLayer` publicly on the exact specifier :8 imports; `useLayerTransition` is absent from the dist; I-18 already ruled RETIRE clean-break. PROOF: my reads of `dist/components/dock/index.d.ts:3,5`, runtime export greps, `package.json` 7.0.0. The "comment is false" leg is DEAD (ruling 5). **DISPOSITION: X-W8.d (G-8) ≡ CC-080 — the born-RED zero-occurrence gate this seat's corpus was chartered to author already exists in that wave file.**
- **AB-5 · C-3/D-12/L-3 — CONFIRMED (MAJOR).** `.dock-layer-grid` (:101) matches **zero CSS rules anywhere** (my greps: 1 template hit repo-wide, 0 in the producer dist; the producer's real container is `.dock-layers`); the container is `display:block; position:static`, so the inactive face's borrowed `position:absolute; inset:0` resolves against `.dock-face` (crossfade.css `.is-active { position:relative }`) and overhangs its nominal container. Evidence correction carried: D's "two hits (:91 and :101)" census cell is FALSE (:91 is `dock-layer`; my grep returns one) — R-LC's kill ratified. Overhang magnitudes diverge across seats: UNPROVEN-NEEDS-LIVE as numbers; mechanism certain. **DISPOSITION: dies with the shim at X-W8.d (the `.dock-face` overlay is correctly parented by construction — carry-table row 4).**
- **AB-6 · C-5 — CONFIRMED (MAJOR) ⇄ ColorInput.md (MT-COLORINPUT-1).** "// Signal parent to exit propose mode" sits above no signal: ColorInput declares zero emits (my grep: `defineEmits` 0, `emit(` 0 over the whole SFC), the parent binds only `ref`/`v-bind`/`:edit-target`/`:propose-mode`/`class` (:115-121); a successful proposal leaves `propose` open with the field silently repopulated by a color string under `aria-label="Propose a color name"`. **DISPOSITION: MT-COLORINPUT-1 — the adjudicated ColorInput split (its cone names this file) creates `ProposeNameInput` with real submit semantics; the parent listener + the `propose → actions` edge land there, wired to this component's mode owner.**
- **AB-7 · C-6/D-11 — CONFIRMED (MAJOR).** `toolbarMode` caches a state whose entry gate can flip false beneath it; :37 is the only gate read and it is click-time-only; the file's only watch (:71) watches the shim's input; `canProposeName` is a live computed (useColorNameResolution.ts:66-70, my read) flipped by the very actions this toolbar hosts (`random()`/`reset()`, and C-5's successful proposal). :96 additionally publishes `toolbarMode` writable — a second ungated path (dead today per AB-16, but a contract statement). **DISPOSITION: the reconciliation watch rides MT-COLORINPUT-1's parent-wiring arm WITH AB-6 (one mechanism, two halves); the mode-machine's design residue (MISS-10, the ring topology) is recorded for the next formation boundary.**
- **AB-8 · C-7/D-10 (state leg) + MISS-3 — CONFIRMED (MAJOR).** A three-mode disclosure control exposes no state to AT — no `aria-expanded`, no `aria-pressed`, no `aria-controls`, only a mutating accessible name (:129-133) — while the producer ships the cure ONE PROP AWAY: `DockControl active?: boolean` "Stamps `aria-pressed` + `data-active`; the icon shape composes the `.glass-capsule` selected seat" (DockControl.vue.d.ts, my read), and the producer's ink register lights on the bare SELECTORS `[aria-expanded="true"]`/`[aria-pressed="true"]` (controls/icon-button.css `&:is(...)`, my read) — so the visible consequence (the toggle wears resting material while holding a mode open) is equally missing. The sibling models the semantic by hand (ActionBarToggle.vue:91) — the same miss in the other direction. **DISPOSITION: X-W4.a (shell semantics; the `active` prop is the one-prop cure).** K-3's collision leg is DEAD (ruling 3); the glyph-species/168-char-ternary legs are expression-shape residue riding the same touch.
- **AB-9 · C-8 — CONFIRMED (MAJOR).** The component's sole gate is vacuous AND cites a phantom test: `color-propose.spec.ts:22-25` claims unit coverage in `test/parsing/extract.test.ts`; my runs — `ls test/parsing/` → `timeline` only; `find test -iname '*extract*'` → nothing; `submitProposedName` appears in the test surface **only inside the sentence claiming it is covered**. Every surviving assertion reads the toggle's aria-label, computed from `toolbarMode`+`canProposeName` on a control outside the swap machinery. Mutation-list correction carried (R-LC's kill, ratified by my reads): mutation 5's failure mode is fictional — ColorInput is ALWAYS mounted (no `v-if`, class-only swap) and deleting :22 throws nothing (App's ambient provide resolves the same object); the mutation stays green trivially. **DISPOSITION: the false citation deletion + the cycle-invariant test (exactly one active sub-layer, `inert` on the other, propose→actions on success) ride X-W1 (gate truth) as a rider; the swap's rendered-motion truth is already X-W8.d G-8's arm.**
- **AB-10 · C-9/D-5/L-4 — CONFIRMED (MAJOR; pricing RESCOPED per ruling 2).** No size reserve: each sub-layer swap re-measures the fit-content dock in one frame and the control the user just clicked translates out from under the pointer (+98.5/+106.4/+141 px across three seats' cells — sign/mechanism certain, magnitudes UNPROVEN-NEEDS-LIVE). The producer implements `reserve="inline"` as a monotonic running max written as `min-inline-size` (dist/dock.js, my byte read). **DISPOSITION: X-W8.d G-8 (`reserve="inline"` is already the wave's prescribed mount) — executed under ruling 2's corrected price: no main-layer/resting tax; the actions run carries the input face's peak only while the bar is open.**
- **AB-11 · D-6 — CONFIRMED (MAJOR; prescribed cure REFUTED — K-5 ratified byte-level).** Two legs of one cycle obey two focus laws: entering `propose` focuses the field (ColorInput.vue:252-262, rAF focus — my read); entering `input` focuses nothing (`grep -c focus ActionBarLayer.vue` → 0). The transposition does NOT buy the fix: dist/dock.js transfers focus only when the dissolving face CONTAINS `document.activeElement` (`!n.contains(r)` guard — my byte read), and the driving toggle is a sibling of both faces. The instrument exists and is dead: `colorInputRef` (:28/:116) against ColorInput's exposed `focus()` (:283). **DISPOSITION: X-W8.d rider with MISS-12's sequencing tension recorded — the same wave's G-10 DELETES `colorInputRef` as dead; the wave must rule use-or-delete in one motion (focus the field on face activation inside the new mount, or bind and use the ref), never delete-then-need.**
- **AB-12 · L-5 + MISS-4 — CONFIRMED (MAJOR).** The outgoing face's portalled popover survives the class-only swap (the D-02 frame shows two portalled popovers superimposed at one anchor — both readers read the image; I carry it labeled); the held antidote (`clearHover`, exposed :91, handle bound :103) has **zero callers** (my grep). MISS-4 generalizes it and is ADOPTED at MAJOR: **`inert` is void across Teleport** — every `PopoverContent`/`TooltipContent` this layer opens is portalled to body, outside the inert subtree, so the face-level `inert`+`opacity:0` never reach them in either direction; the C and D negative records certifying the inert seam clean are WRONG for the half that leaves the subtree (killed-claims K-8-adjacent correction). This is the strongest argument for the transposition made anywhere in the corpus: **a component swap ends teleported content by unmount; a class swap cannot.** **DISPOSITION: X-W8.d G-8.** The refcount-leak consequence (`dock.release()` never firing): UNPROVEN-NEEDS-LIVE.
- **AB-13 · L-7 — CONFIRMED (MAJOR).** `ActionToolbar` is a hand-unrolled duplicate of its own sibling `GenericActionBar` (my reads: byte-identical root div, identical `activeHover` model; 92 lines + five re-declared emits forwarded straight back vs 28 lines over a typed `DockAction[]`), rendered as a mutual exclusion at Dock.vue:156-157. **DISPOSITION: X-W4.d (CC-043; gate D1 "the Dock renders one action path"). GenericActionBar is the proof the cure is a deletion, not a design.**
- **AB-14 · L-9 — CONFIRMED (MAJOR; count corrected).** The dock's action-bar contract has two homes and neither is the dock: `ActionBarContext` in a colour-domain key module (keys.ts:17-27 — **NINE fields**, my count; R-LC's K-12 correction of L's "8" adopted) and `DockActionBar` in the router module; this file reaches three directories up to learn a dock prop's shape. **DISPOSITION: X-W4.d (CC-043's `SceneActionSet` collapse), carrying MISS-6 (the collapse must not re-carry the three dead members).**

### Glass-owned

- **AB-15 · D-2 (design half) — CONFIRMED AS FACT · GLASS-OWNED ⇄ Dock.md R-17 (D3-03 identity, BANK-D4).** A six-control horizontal run implements no roving focus; VISUAL-CONSTITUTION.md:129 legislates it (verified verbatim by the challenger and both readers). But Dock.md R-17 already ruled the rail/roving machine **producer behaviour** — the owner's glass-first edict bars a demo keydown graft; the obligation stays OPEN against glass via the standing BH relay + BANK-D4 (`grep -rql 'rovingFocus\|RovingFocus\|useDockRail' node_modules/@mkbabb/glass-ui/dist/` — silent today). R-DU's cure-by-ConsoleRail-idiom is REFUTED as a booking (ConsoleRail's in-demo graft is the house's own recorded tension, not license). Not a value.js defect count. The ENGINE half (WebKit 0-of-6): UNPROVEN-NEEDS-LIVE.

### Minor

- **AB-16 · C-11/D-16/L-10/L-12 + C-14 — CONFIRMED (MINOR).** Every public surface is dead: three template refs (:28/:29/:83 — the third discarded by the shim itself at :67), `defineExpose({currentToggleIcon, toolbarMode, cycleToolbarMode})` (:96) against a ref-less mount (Dock.vue:156), `:can-propose-name` (:106) declared and never read (ActionToolbar.vue:72 — my grep), `clearHover` with no caller; two template-ref idioms coexist in 158 lines. **DISPOSITION: X-W8.d G-10 — WITH MISS-13: G-10's five enumerated sites do NOT include :96's defineExpose or :83; the gate needs the addendum or the wave under-deletes.**
- **AB-17 · C-10 — CONFIRMED (MINOR).** The 260 ms timer is never cleared on scope disposal (no `onScopeDispose`/`onUnmounted` — my grep) and the whole mode state is a local ref behind two stacked `v-if`s. **DISPOSITION: the timer dies with the shim (X-W8.d); the `toolbarMode`-discard-on-unmount is a state-lift question recorded for the formation boundary (no owner).** The observed mid-session unmount: UNPROVEN-NEEDS-LIVE.
- **AB-18 · C-12 — CONFIRMED (MINOR).** `void opts.containerEl` — signature parity with a producer composable that exists nowhere (my dist grep: 0), dragging `type Ref` + `useTemplateRef` into imports. Dies at X-W8.d. MISS-9 (the `Readonly<ShallowRef>` laundered into a writable `Ref` parameter — compiles because TS ignores readonly modifiers in property assignability) is its type-level shadow; both die together.
- **AB-19 · C-16/L-14 — CONFIRMED (MINOR).** `SUB_LAYER_CROSSFADE_MS = 260` is untokenized, off both scales (glass motion tokens and demo DESIGN.md:207-213's 100/200/300/450/550), and races the producer's 300 ms visibility hand-off it shadows; the same file's icon swap rides the fully-tokenized, PRM-guarded `vj-morph` family 70 lines below. Dies at X-W8.d. **No motion-quarantine row exists for this component; nothing here carries M-32 (the sibling's struck cell).**
- **AB-20 · L-6 — CONFIRMED (MINOR — ruling 4).** `provide(COLOR_MODEL_KEY, actionBar.colorModel)` (:22) is a provable identity no-op with a false comment ("so ColorInput works unchanged" — it resolves the same object from App regardless). **DISPOSITION: the line rides X-W8.d G-10's addendum as dead surface; the `colorModel` contract field dies at X-W4.d (CC-043).** R-LC's kill of the D negative record's "genuine re-scoping" absolution is RATIFIED (K-8) — a defence entered into a negative record forecloses the cure and is worse than a miss.
- **AB-21 · L-8 — CONFIRMED (MINOR) ⇄ ColorInput.md D-9.** `editTarget` is drilled App→Dock→ActionBarLayer→ColorInput and read by nobody at the end (ColorInput.vue:139 declaration only — already adjudicated at ColorInput.md D-9); `EDIT_TARGET_KEY` (keys.ts:15, provided App.vue:270) is the live DI home. Dock's own copy IS live (:72/:73/:136 — my read). **DISPOSITION: the dead leg of the chain dies with MT-COLORINPUT-1's split + X-W4.d's contract collapse; cross-booked, not re-booked.**
- **AB-22 · L-11 — CONFIRMED (MINOR; scope trimmed) ⇄ W-HYGIENE (ColorSpaceSelector.md L-14 identity — NOT re-booked).** tsconfig.demo.json:42-49 declares three unpublished specifiers pointing at absent files (bare root, `/parsing`, `/units` — my `ls`), omits published ones, and its comment ("the bare `.` root + the 7 subpath barrels … CLOSED 8-key set") is false. R-LC's K-13 trim ADOPTED and verified: `/css` used **10×** undeclared; `/value` used **0×** — only ONE undeclared subpath is live. Corroboration accretes to the standing W-HYGIENE row.
- **AB-23 · L-13 — CONFIRMED (MINOR).** The component decides two other components' layout/stacking/visibility/inertness by spreading producer class strings onto their single-root fallthrough (:104/:117); the producer's answer is a host (`DockLayer` renders content in a `.dock-face`). Dies at X-W8.d by construction.
- **AB-24 · C-17 — CONFIRMED (MINOR) ⇄ MT-COLORINPUT-1.** ColorInput re-enables hit-testing (`pointer-events-auto`, :7/:94) inside a box the producer hides with `pointer-events:none`; only the face `inert` masks it — and MISS-4 shows even that mask is void for the portalled half. Moot post-transposition (faces unmount); the utility itself is ColorInput-surface and rides that wave's rewrite.
- **AB-25 · D-8 (glyph leg) — CONFIRMED (MINOR; demoted from MAJOR, both corrections adopted).** `w-6 h-6` (24 px) beats the producer's 20 px `--dock-icon-glyph` (`.dock-icon-button > svg { width: var(--dock-icon-glyph, 1.25rem) }` — controls/icon-button.css, my read; md resolves 20 px; Tailwind utilities outrank `@layer components`). Corrections verified: `--dock-control-glyph-size` is declared 5× and **consumed by zero rules** (my grep — a dead producer token worth one BH relay line); the 24 px hardcode is the house norm at this seat (Back arrow, ActionBarToggle, all five ActionButtons). **DISPOSITION: a house-wide canon question → X-W10 (canonical design), with the dead-token note riding the standing glass BH relay.** The touch-target leg stays DEAD (K-2).

### Info

- **AB-26 · C-18 — CONFIRMED (INFO).** `canProposeName` consults only the custom-name registry (useColorNameResolution.ts:66-70, my read: `!findCustomName(...)` — no built-in table) while its gate's prose says "doesn't match a built-in or custom name" (spec :8-9). **DISPOSITION: the prose correction rides AB-9's X-W1 rider; whether built-ins SHOULD block proposing is a design question — NO-WAVE-OWNER, formation boundary.**
- **AB-27 · L-15 — CONFIRMED (INFO).** Directory inversion (two single-consumer components one level ABOVE their consumer), the dual import path (barrel vs direct `@mkbabb/glass-ui/dock`), and the real `Dock.vue → ./index.ts → Dock.vue` cycle. **DISPOSITION: X-W8 scope 6 (the Dock SCC closure) + X-W8.b — already owned; MISS-8 notes the transposition's import lands cleanly only after the barrel decision.**
- **AB-28 · D-17 — CONFIRMED (INFO; direction corrected).** `stroke-foreground` severs the `currentColor` chain; the asymmetry runs OPPOSITE to the charge (the five nearest siblings do the same; only the Back arrow differs — my 2-file census). The forced-colors paint stays a hypothesis (UNPROVEN-NEEDS-LIVE); the normal-mode consequence is MISS-2's row, which subsumes this.

---

## Killed-claims register

Each kill carries its refutation; none may be re-filed without new evidence.

- **K-1 — "`useDockCrossfadeContext()` is exported and documented" on `@mkbabb/glass-ui/dock` (C-2's evidence chain; D-4's truth-table row 1).** REFUTED against the installed bytes, re-pinned by this seat: the public dock entry re-exports the crossfade context's **types only**; the runtime export list carries no crossfade-context value; `grep -c useDockCrossfadeContext dist/dock.js` = 0; the internal `composables/index.d.ts` barrel C-2 grepped is never widened by the public surface; no `./dist/*` subpath is published. Consequence preserved: D-1's `faces` registry cure is reachable ONLY via a template ref on `<DockCrossfade>` (`defineExpose({faces})` — d.ts verified), never via the context. **The much-derided header comment is RIGHT on this exact point.**
- **K-2 — the 40×40 toggle violates the 44 px touch target and "is one of the eight smallTapTargets".** Category error + false attribution, corroborated by this seat: the 44 px token is a COARSE floor (`@media (pointer: coarse) { --dock-control-floor: var(--dock-touch-target, 2.75rem) }`, overflow.css — my read; the DockControl docstring itself says "≥44px on coarse via the density clamp; hit box ≠ paint box"); fine-pointer 40 px is the designed size; and the corpus's own C-seat measured the eight smallTapTargets elsewhere (largest 22 px — a 40 px control cannot be among them).
- **K-3 — the `⋮` collides with MobileMenuDropdown's `MoreVertical` "one glyph, one surface, two contradictory meanings".** KILLED, strengthened (ruling 3): the two glyphs live on mutually exclusive faces of one DockLayerGroup; opacity steps in one frame; no rendered frame carries both. R-LC's contrary confirmation is OVERRULED.
- **K-4 — "the only two demo files that write a bare `:hover` are ActionButton and this one" (D-9's convention claim, and with it D-9 itself).** KILLED (ruling 1): my census finds bare `:hover` in **10** demo files; the "convention" (the guard) exists in exactly one. R-LC's confirming census cell is FALSE. D-9 leaves the roster; residue = an app-wide INFO census, NO-WAVE-OWNER.
- **K-5 — "adopting `<DockCrossfade>` discharges D-6" (focus-transfer-on-dissolve inherited).** KILLED, ratified byte-level by this seat: the transfer fires only when the dissolving face contains `document.activeElement`; the driving toggle is a face-sibling; on the ordinary path the guard is false and nothing transfers. The peak-reserve half of the same sentence IS inherited. Wave-sequencing consequence recorded at AB-11/MISS-12.
- **K-6 — D-7's canon charge ("reduced motion resolves directly to the final geometry — VIOLATED").** KILLED by both readers, ratified: the element held 260 ms is `opacity:0`, `pointer-events:none`, `inert`, out-of-flow — no layout, paint, hit region, or a11y node; geometry resolves in ONE frame (which is precisely AB-1, and the two claims cannot both be true of one swap). VISUAL-CONSTITUTION.md:144 is met. Residue (the untokenized dead timer) is already charged at AB-19. No quarantine row exists to carry.
- **K-7 — L-4's pricing "`reserve="inline"` costs +141 px at rest, on every route" (and R-DU's adoption of it as governing).** KILLED on scope (ruling 2): inactive faces are out-of-flow; no resting/route-wide tax exists. Residual true cost restated at AB-10.
- **K-8 — the D negative record's absolution "the provide/inject seam is sound … a genuine re-scoping".** KILLED (R-LC, ratified by this seat's four-hop identity chain): there is no second model to re-scope from; :22 re-provides the identical object under the identical key.
- **K-9 — C-8 mutation 5's failure mode ("ColorInput throws on inject … only when the input layer renders").** KILLED on both limbs (R-LC, ratified): ColorInput is always mounted (class-only swap, no `v-if`), and deleting :22 throws nothing (ambient provide). The mutation stays green for a different reason than filed — the precision of C-8's mutation list is its force, so the cell is corrected here.
- **K-10 — D-12's census cell ("exactly two hits, both in this file, :91 and :101").** KILLED: `:91` is `dock-layer`, not `dock-layer-grid`; my grep returns one hit. The conclusion (zero matching rules) survives via C-3's accurate census.
- **K-11 — D-17's sibling-asymmetry direction ("its siblings do not do this").** HALF-KILLED: `stroke-foreground` = exactly 2 files, and one of them is the five ActionButtons rendered beside the toggle; the asymmetry is with the Back arrow alone.
- **K-12 — L-9's "8 fields".** Nine (keys.ts:17-27, my count). Trivial alone; the two miscounted fields are exactly MISS-6's dead pair.
- **K-13 — L-11's "omits the two subpaths that ARE published and ARE used".** Over-scoped by one: `/value` has zero uses (my census). Survives at reduced scope (AB-22).

---

## Missed register (rows the challenge corpus lacked; adjudicated in)

- **MISS-1 (R-DU) — CONFIRMED (MAJOR).** `gap-0` at :100 zeroes the dock's inter-control rhythm inside this run: the producer's `.dock-face-content` gap is 6 px, so the Back-side separator gets flex-gap + margin air while this run's separator/toggle get the margin alone — exactly half — and this, not an undersized seat, is the mechanism behind the focus-ring crowding D-14 mis-diagnosed (whose prescribed touch-target cure K-2 killed). An interval set to zero by an unexplained utility earns nothing (PROPORTION-AUDIT law). **DISPOSITION: X-W10 (the "uncited proportion constants" surface named in its own goal criterion) — rider; the ring-crossing subpixel claim stays UNPROVEN-NEEDS-LIVE.**
- **MISS-2 (R-DU) — CONFIRMED (MAJOR).** The toggle glyph opts out of `DockControl`'s own ink register by hardcoding `stroke` (:138), severing `color`-transition, hover ink, AND active/pressed ink from the glyph (controls/icon-button.css read verbatim by this seat — path corrected from the corpus's "icon-button.css"), then re-implements hover ink bespoke (:149-151 + the inline custom property :139) — two hover voices on one producer primitive, and the producer's selected ink dead. Unique at this seat: the five neighbouring ActionButtons carry the same class on a plain `<button>`, overriding nothing. Owner edicts (glass-first; root-level styling) both bite. Subsumes AB-28's mechanism with a normal-mode consequence. **DISPOSITION: X-W10 rider (one ink voice per primitive), executed WITH AB-8's `active` prop so the restored register has a state to show.**
- **MISS-3 (R-DU) — CONFIRMED (MAJOR), folded into AB-8.** `DockControl active` ships the full AT+material cure one prop away; and the producer's `:is([aria-pressed="true"], [aria-expanded="true"])` selector means even the ARIA alone would light the material (this seat's addition).
- **MISS-4 (R-LC) — CONFIRMED (MAJOR), folded into AB-12.** `inert` is void across Teleport; both negative records certified the opposite; the transposition is the only mechanism that ends portalled content.
- **MISS-5 (R-LC) — CONFIRMED (MAJOR), folded into AB-3.** D-1's one-line containment (guard the write / disable the toggle) is independent of, and sequenceable before, the transposition.
- **MISS-6 (R-LC) — CONFIRMED (MINOR).** `cssColorOpaque` and `formattedCurrentColor` are produced into `ActionBarContext` for consumers that do not exist (my 11-hit census: reads are `canProposeName` ×4, `isEditing`, `paletteActive`, `reset`/`copy`/`random`, and the :22 no-op; the tell — :105 feeds ActionToolbar the injected `safeAccent` while the context carries its own `cssColorOpaque` for the same prop). Three of nine members dead, not one. **DISPOSITION: X-W4.d — the SceneActionSet collapse must not re-carry them (gate D1's member count is the hook).**
- **MISS-7 (R-LC) — CONFIRMED (MINOR).** The prescribed cure nests `.dock-crossfade` inside `.dock-crossfade`, and crossfade.css's `[data-morphing] … > .dock-face-content { clip-path: inset(…) }` matches BOTH after the transposition — the same descendant-scoped shape that produced AB-2, co-directional and low-blast but absent from L's carry table. **DISPOSITION: X-W8.d carry-table addendum row.**
- **MISS-8 (R-LC) — CONFIRMED (INFO).** `demo/shell/dock/index.ts` does not re-export `DockCrossfade`; the transposition's import must not deepen the dual path. **Discharged by construction at X-W8.b/scope-6 (the barrel dies); recorded so the sequencing (b → d) is honored.**
- **MISS-9 (R-LC) — CONFIRMED (INFO).** The shim's parameter type launders `Readonly<ShallowRef<HTMLElement|null>>` into `Ref<HTMLElement|null>` — the file's one surviving type obligation is a false one. Dies at X-W8.d with AB-18.
- **MISS-10 (R-DU) — CONFIRMED (MINOR).** The mode machine is a one-directional ring with no `propose → input` edge (leaving propose always discards the field, two activations to return) and no glyph in the cycle says it is a ring; the propose leg is a mode nested inside a mode promoted into a glyph cycle that cannot express it. **DISPOSITION: design residue — NO-WAVE-OWNER, formation boundary (the MT-COLORINPUT-1 split will re-pose it with a real two-component seam).**
- **MISS-11 (R-DU) — CONFIRMED (INFO).** :126-128 justifies the accessible name as a test-harness affordance ("added aria-label so the … selectors … can drive the cycle") — the dependency inverted in-source; co-designed with the vacuous gate AB-9 reads. Prose fix rides AB-9's X-W1 rider.
- **MISS-12 (this seat) — NEW (sequencing hazard).** X-W8.d's G-10 deletes `colorInputRef` while AB-11's only in-file cure needs it (or its successor). The wave must rule **use-or-delete atomically**; a wave that deletes Monday and needs Tuesday re-files AB-11 as a regression. Recorded against the wave file, not patched into it (E-3).
- **MISS-13 (this seat) — NEW (gate gap).** G-10's five dead-surface sites omit ActionBarLayer:96's `defineExpose({currentToggleIcon, toolbarMode, cycleToolbarMode})` (mount is ref-less — dead, and a writable path into AB-7's state) and :83's `subLayerGridEl` (dies with the shim, but the gate should assert it). Addendum for X-W8.d's executor.
- **Registry-blindness events (recorded as evidence about the fleet, L-13 class)**: R-DU's "nameless propose send button" and R-LC's "copyAndSetInputColor is fully dead" are both TRUE and both **already adjudicated at ColorInput.md D-3 and D-9 respectively** (both branches nameless; the whole 5-member expose dead — that record's cone includes this file). Cross-booked, NOT counted as new rows; two hostile readers independently re-derived standing registry rows without checking the registry — the anti-rename discipline needs a lookup step, not just a naming rule.

---

## Superlatives (provenance preserved; adjudicated)

1. **The header comment (ActionBarLayer.vue:53-61) — RE-INSTATED as exemplary, with the indictment struck** (R-LC's superlative, ratified by this seat's own dist runs against R-DU's confirmed-row banner): nine lines naming the producer version, the retired symbol, the successor by name, the preserved contract, and its own outbound relay ask — and its one attacked claim is TRUE on the installed bytes. It is the reason the component was auditable: it told three seats where to look, and it was right.
2. **ActionBarToggle.vue's three-state presence machine + hand-written ARIA (:52-72/:86-93/:112-145)** (both readers): boot seats instantly, only genuine toggles animate, the rejected alternative recorded at the site with its cross-engine reason, the box model fixed through the producer's own token hook — "the file the subject should have been."
3. **Dock.vue:78-89 (U-F48)** (R-DU): three imperative watchers collapsed into one declarative disjunction with the behavioural-equivalence argument written down — the shape that makes AB-3's missing predicate legible as an omission.
4. **The icon swap (:134-141/:153-157)** (both readers): the sanctioned `vj-morph` family, tokenized, PRM-guarded, parameterising only geometry — cited by DESIGN.md:275 as the reference implementation — seventy lines below the house's worst motion idiom. The whole audit in one screen.
5. **`challenge-L-library.md:773-779` — pass 2 falsifies its own pass 1 and records the instrument error** (`"dock-layer"` never appears alone in `normalizeClass` output), RAISING the severity. Verified by this seat (1 hit each for the two private emissions). The single strongest act in the corpus, and the template for MISS-14's lesson.
6. **`challenge-L-library.md:629-644` — the carry table + the priced ruling refused-not-prescribed** (R-DU): costing the cure instead of naming it is the difference between an audit and a wish — even though the price itself fell to K-7, the FORM is the standard; the corrected price now sits beside it.
7. **`challenge-C-implementation.md:824-861` — the negative record**: twelve null results with commands, including correct not-applicable rulings on this repo's standing hazards. Silence made legible — with the one correction that its inert cell is wrong for portals (MISS-4), which is exactly why negative records must be attackable.
8. **`ActionBarLayer.vue:92` — the load-bearing `inert`** (R-DU): the single thing between the mis-parented leaving overlay and a live click-eater — correct by construction, and doomed to insufficiency by Teleport; both facts now recorded.

---

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs — task #4 feed)

Each cell is an input to the visual-audit harness (chromium primary; engines are separate cells per I-20); none may be cited as fact until re-measured.

1. **D-2 engine differential** (WebKit 0-of-6 vs Chromium 6-of-6 Tab reachability) — needs live; note it would indict every bare DockControl app-wide, and the design half is GLASS-OWNED regardless (AB-15).
2. **D-1 collapse geometry** (254.7×59.5 → 28.1×64.7; `--dock-expanded-px` 255→69; faces all-inert) — mechanism statically certain; numbers live. Correction pinned for the re-run: the clipping box is `.action-bar-toggle-inner` (`overflow:hidden`), not the slot.
3. **C-3/L-3 overhang figures** — three seats, three number sets; re-measure at a named viewport before quoting any.
4. **L-4/D-5/C-9 jump magnitudes** (+98.5/+106.4/+141 px) — sign/mechanism certain.
5. **D-13 optical rhythm** (28.5 vs 22.5 px end-gaps; 37.5 px pitch) — the `justify-around`-on-`flex-1` mechanism is sound; the asymmetry is below static resolution; note MISS-1's `gap-0` is the confounder to control.
6. **D-14 ring-crosses-separator** — crowding is real (frame read); the 1 px crossing is subpixel; re-measure after MISS-1's cure, not before.
7. **D-17 forced-colors paint** — mechanism confirmed at source; emulated media is not a forced palette (the Dock.md D3-11 lesson: WebKit forced-colors is a false green).
8. **C-10 mid-session unmount** (faces 4→3 between probes) — the gating that would produce it is code-certain; the event needs a trace.
9. **C-15/L-5 refcount-leak consequence** (stranded popover → `release()` never runs → hold leaks +1) — the stranding is evidenced (image + code); the leak needs the 5000 ms collapse-delay waited out.
10. **L-1 morph-stagger reachable path** — the selector match is statically certain; the reachable trigger (dock expand/collapse while the action bar is open) is asserted, not shown; one probe.
11. **D-8 rendered rects** (24 px glyph vs 20 px token at md) — token math certain; rendered boxes live.

---

## Closing verdict

**ADJUDICATED (nothing here stamps a wave): 26 CONFIRMED (AB-1..AB-14 substantive, AB-16..AB-28 hygiene/info; 3 BLOCKER-weight, one of them cross-component and cross-booked) · 1 GLASS-OWNED (AB-15 ⇄ Dock.md R-17/BANK-D4) · 13 KILLED with refutations pinned to the installed bytes · 13 MISSED rows adjudicated in (2 arbiter-original: the use-or-delete sequencing hazard and the G-10 enumeration gap) · 2 reader registry-blindness events recorded · 11 live cells routed to the visual harness. The component's fate is already correctly owned: X-W8.d (G-8/G-10 ≡ CC-080 ≡ Dock.md L-20/MT-DOCK-LAYERS-1) retires the shim against the shipped `<DockCrossfade>` and discharges AB-1/2/4/5/10/12/16/17/18/19/23 + MISS-7/8/9/13 in one transposition under ruling 2's corrected price; X-W4.a/.d (CC-043) take the semantic state, the contract collapse, the duplicate toolbar, and AB-3's one-predicate containment; MT-COLORINPUT-1 takes the propose seam; W-HYGIENE and X-W1 take the config and gate-truth riders; X-W10 takes the two design-canon rows (`gap-0`, the severed ink register) with one BH relay line for the dead `--dock-control-glyph-size` token. The header comment is exonerated; the readers killed each other's worst cells and this seat killed three more; the residue that needs a browser is enumerated and nothing else does.**

— end of apotheosis. The r2 challenge trio, both pass-1 files, and both re-reader returns remain immutable beside this file per E-1/E-3.

---

# ADDENDUM R3 (E-3 — appended, the body above unrewritten) · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt)

**Date**: 2026-08-07 · **HEAD at append**: `209f47c3` · branch `tranche-u`.
**Why this addendum exists**: the body above was banked as a write-then-die orphan (committed `08208bac`/`209f47c3`, complete-endings verified) against a FIRST-generation pair of re-reader returns. This seat, re-invoked with the same identity, received a SECOND-generation pair from the same two seats (R-DU′ design-and-usage · R-LC′ library-and-code) containing rows the body never ruled — and lacking rows the body cites (no `Readonly<ShallowRef>` row, no aria-label-prose row in this generation). Two returns, one corpus, drifting registers: every generation-2 delta is adjudicated here; nothing in the body is rewritten.
**Body re-verification (this seat's own instruments, all re-run before appending)**: K-1's dist bytes (public dock entry re-exports crossfade-context **types only**; `grep -c useDockCrossfadeContext dist/dock.js` → 0 — the header comment stays exonerated); AB-1's layers.css (visibility-only transition, verbatim); ruling 2's crossfade.css (inactive faces `position:absolute` — no resting reserve tax); ruling 1's census (bare scoped `:hover` in 10 demo files, exactly 1 guarded); AB-9's phantom test (`test/parsing/` = `timeline` only; `submitProposedName` exists only in the sentence claiming coverage); motion-quarantine (no row for this component; M-32 is `shell-dock-actiontoolbar`'s struck cell — nothing here carries it); routing (X waves W8.md §X.W8.d G-8/G-10 · CARRY-CUT-LEDGER CC-080/CC-043/CC-083 — all present, all as cited). **All held. The body stands whole.**

## Registry-blindness events three and four (the fleet lesson, again)

Both of R-LC′'s headline missed rows are TRUE, load-bearing — and **already adjudicated in this registry**. They are cross-booked, not re-booked (anti-rename):

- **The mobile `:on-mount` omission chain** (App.vue:83-91 mobile PaneSlot passes no `:on-mount` vs :105/:131 desktop; :323-325 the sole `colorPickerRef` writers; App.vue:38 `:action-bar` therefore permanently null on mobile; App.vue:41-42's `colorPickerRef?.commitEdit(); viewManager.mobilePaneIndex.value = 1` short-circuits the commit and switches the pane anyway) ⇄ **App.md C-2/D-1 (CONFIRMED · BLOCKER, one carry) ≡ Dock.md DELTA-1 (BUILD — the R-8 DockCommand-registry cure)**. Re-verified at source by this seat, every hop. Dock.md DELTA-1 additionally SETTLES R-LC′'s "strongly implied" reachability leg: on true mobile the edit journey dies EARLIER — `usePaletteWiring`'s `whenColorPickerReady` polls the desktop-only ref 40×50 ms and stops, so the `mobile-edit` DockLayer (Dock.vue:72) is unreachable code in the only grammar it exists for; the silent-discard handler is the same seam's second face and dies with R-8's registry. Not an unproven cell — a settled one.
- **The two divergent `isDesktop` predicates** (App.vue:310-312 `"(min-width: 1024px) and (min-aspect-ratio: 1.1)"` — whose own comment says the compound query exists "so they can never disagree" — vs Dock.vue:71 width-only `useMediaQuery`; divergence band ≥1024 px ∧ aspect < 1.1, iPad-portrait 1024×1366) ⇄ **Dock.md DELTA-5 (CONFIRMED — BUILD, structure (h) single-owner predicate + gate G-L; collision frame committed)**, with the zoom fork of the same predicate owned at App.md D-7. Re-verified at source by this seat.

With the body's two, that is FOUR registry-blindness events in one component's corpus: hostile readers keep re-deriving standing registry rows without a lookup step. The discipline needs the lookup, not more naming law.

## New confirmed rows (generation-2 deltas, each verified by this seat)

- **AB-29 · R-DU′ M-1 — CONFIRMED (MAJOR).** The cycle's `input → actions` escape (:40) is dead on virtually every color: its gate is `!canProposeName`, and `canProposeName` = `!findCustomName(...)` (AB-26's divergence carried to its UX consequence — true for all built-ins and every unnamed color). So "Close input" (:131) almost never renders, and leaving the input transits `propose` — which BLANKS the user's typed text (ColorInput.vue:258-260 `proposedName=""; innerText=""`, my read) and restores the MODEL's text on exit (:265), never the user's. The blanking writes are two of ColorInput.md D-7's six imperative `innerText` writes (⇄ booked; they die with that wave's v-model primitive). Booked HERE: the dead edge + the label economy — this completes MISS-10's ring into a measured topology (no `propose → input` edge, and no honest `input → actions` edge either). **DISPOSITION: MT-COLORINPUT-1 parent-wiring arm (with AB-6/AB-7); the built-ins design question stays AB-26's NO-WAVE-OWNER.**
- **AB-30 · R-DU′ M-4 — CONFIRMED (MAJOR).** The five primary actions are **32×32 on coarse pointers with no floor** — the real touch defect eight lines from K-2's killed charge. My reads: `.action-button-wrapper { width: 2rem; height: 2rem }` fixed (ActionButton.vue:105-111); the producer's coarse floor is scoped `.dock-icon-button:not(.dock-icon-button--compact):not(:where(.glass-dock *))` (touch-floor.css, verbatim) and the density floor feeds `--dock-control-size`, which a plain `<button>` never reads — so `--dock-touch-target` (2.75rem) cannot reach them by any route. Reachable cell: tablet-landscape (≥1024 w ∧ aspect ≥ 1.1 ∧ coarse) — per App.md C-2 the only coarse surface where this bar mounts at all. C-20 measured 32×32 and recorded PASS. **DISPOSITION: X-W4.d — the AB-13 collapse must seat actions on `DockControl` (or carry the coarse floor) as an explicit cure clause; canon half rides X-W10 with MISS-2/AB-25. Rendered coarse rects: UNPROVEN-NEEDS-LIVE (cell 12).**
- **AB-31 · R-DU′ M-2+M-3 — CONFIRMED (MINOR; extends MISS-2).** Three hover grammars in one ~184 px run: the producer glass seat (Back, toggle), the bespoke `scale(1.2)`+stroke latch (five actions, ActionButton.vue:117-120), and the toggle stacking a bespoke accent stroke ON TOP of its seat — plus the accent register bound to the 24 px glyph inside the 40 px seat, leaving an annulus where the seat lights and the accent does not (mechanism certain from the box sizes; magnitude UNPROVEN cell 13). **DISPOSITION: X-W10 with MISS-2 — one ink voice, one hover grammar per run.**
- **AB-32 · R-DU′ M-6 — CONFIRMED (MINOR; cure-regression rider).** The AB-13 collapse silently drops `activeStyle`: `DockAction` (usePaneRouter.ts:38-47, my read) has no such member; ActionToolbar.vue:46 feeds the palette-active stroke through it (→ ActionButton.vue:67/:30); Dock.vue:157's GenericActionBar path passes `:actions` + `:accent-color` only. Per the exemplar law (the cure is adjudicated separately from the defect): **X-W4.d's `SceneActionSet` must carry an active/selected member — which AB-8's `DockControl active` prop then renders for free — or the only palette-open indicator regresses under a green gate.**
- **AB-33 · R-LC′ — CONFIRMED (MINOR; extends AB-2/AB-5).** The borrowed `.dock-layer` yields a **hybrid box neither author specified**: ColorInput's root `grid grid-cols-1 gap-y-2 p-0 m-0` (:2, my read) out-cascades the producer's `@layer components` rules, so `display:grid` + row-gap 8 px (consumer) win while column-gap 6 px (producer) survives — exactly the `gap: 8px 6px` the L seat measured and mis-filed as inheritance; `grid-area:1/1` lands inert; which half wins depends on Tailwind's layer emission order — a second unversioned coupling stacked on AB-2's first. The min-width leg: the phantom container (:101 `flex-1`, no `min-w-0`) lacks the producer's `.dock-layers { min-width: 0 }`, which is why :120 hand-patches `min-w-0` onto ColorInput alone. **DISPOSITION: X-W8.d — two carry-table addendum rows beside MISS-7.**
- **AB-34 · R-LC′ — CONFIRMED (INFO; refines ruling 2).** The `reserve` running max is **session-history-dependent on a content-sized face**: the producer docstring pins "measure-once … a running max — NOT a per-swap FLIP" (body's byte read concurs), and the input face's width tracks `formattedCurrentColor`'s length, which varies by space and format — so under `reserve="inline"` the open bar's plate ratchets to the widest string yet displayed and never shrinks that session. Not a defeat of the transposition; **a ruling input recorded beside ruling 2's corrected price for X-W8.d's executor.**
- **AB-35 · R-DU′ M-7 — CONFIRMED (INFO).** `mode="out-in"` (:134) leaves the just-pressed 40 px seat glyphless for ~200 ms (`--duration-fast` leave — animations.css:111-116, my read) on every mode click, landing on the same beat as AB-10's plate translation. House-consistent idiom; recorded for X-W8.d's executor beside AB-10.

## Kill-register delta

- **K-14 — D-1's generation-1 mobile mechanism cell ("on mobile the Picker pane is not the mounted pane, so the ref is undefined") — KILLED** (R-LC′'s correction, ratified at source): the picker IS the mounted pane on mobile (App.vue:83-91 routes it through the mobile PaneSlot); the ref is never CAPTURED, not never mountable. Sharpens AB-3's correction (b): the mobile arm's cause is the registry-booked App.md C-2 omission; the general condition ("any route whose pane publishes no bar") stands unchanged.
- **Alignment notes (no new kills)**: both generations now kill D-9 — ruling 1's "R-LC CONFIRMED MINOR" cell was generation-1; generation-2 R-LC′ killed it with an 8-file census, still an undercount; this seat's 10-file census remains the count of record. R-DU′'s D-13 arithmetic reconstruction (`justify-around` distributes symmetrically and cannot produce the 6 px asymmetry; leading separator 6+6 px air vs trailing 0+6 px — 12−6 = the measured 6) is ADOPTED as MISS-1's mechanism proof, to the pixel against D-13's own coordinates. R-DU′'s D-14 clearance arithmetic (ring arc ≈826.5 vs separator 823.5-824.5, plus the 4 px `--dock-control-safe-inset`) is ADOPTED into UNPROVEN cell 6 as the prior: collision now UNLIKELY. R-DU′'s C-6 correction is ADOPTED: `random()`/`reset()` cannot trigger the stale-propose state (the toolbar is `inert` outside `actions`, :92) — AB-7's trigger list narrows to external color change + C-5's successful proposal. R-LC′'s L-5 probe-posture caveat is ADOPTED into AB-12's evidence note: the synthetic-pointer probe cannot distinguish "swap fails to dismiss" from "synthetic hover cannot be dismissed" — the row carries on the D-02 frame and the code-certain mechanism, never on that probe. R-LC′'s D-7 partial-preserve ("a JS `setTimeout` cannot be a reduced-motion arm") is already housed as AB-19's residue; K-6 stands; no quarantine row exists to carry.

## Superlative delta

9. **The clean library edge, independently re-derived by R-LC′ and ratified here**: every `@mkbabb/value.js` import across the demo resolves to a published `exports` key (`/color` ×25, `/css` ×10, `/math`, `/easing`, `/quantize`), zero `dist/` or `src/` reaches, zero deep imports — a real consumer could write every import this demo writes, which is the point of dogfooding a published package. The one boundary defect (AB-22) lives in program config, not in a single import statement — the correct shape for a codebase with this discipline.

## Closing verdict (R3 — supersedes the counts, not the rulings, of the body's verdict)

**ADJUDICATED (nothing here stamps a wave): body + addendum = 33 CONFIRMED (26 + AB-29..AB-35) · 14 KILLED (13 + K-14) · 1 GLASS-OWNED · 22 missed-register rows adjudicated across both generations · FOUR registry-blindness events recorded (App.md C-2/D-1 ≡ Dock.md DELTA-1; Dock.md DELTA-5; plus the body's two) · 13 UNPROVEN-NEEDS-LIVE cells (the body's 11, plus the tablet-landscape coarse rects and the hover annulus; the mobile edit-reachability question is SETTLED by Dock.md DELTA-1, not open). The center of gravity is unmoved: the X-W8.d transposition discharges the shim cluster whole; X-W4.a/.d take the semantic state, the contract collapse (now carrying AB-30's DockControl-seating clause and AB-32's active-member clause as explicit cure-regression guards), and AB-3's containment; MT-COLORINPUT-1 takes the propose seam with AB-29's dead-edge completing its brief; X-W10 takes the ink/hover canon with AB-31; the mobile chains stay where the registry already put them. The header comment remains exonerated; the readers' generations killed each other's worst cells; and the fleet's recurring failure is now named four times over — read the registry before re-deriving it.**

— end of addendum R3. The body above, the r2 challenge trio, both pass-1 files, and both generations of re-reader returns remain immutable per E-1/E-3.
