claude-fable-5 (served model id)

# fr-ConvergencePlot — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `fourier-analysis/web/src/components/equation/ConvergencePlot.vue` (410 lines) + its first-party library closure (`lib/{grid,hit-test,harmonics}.ts`, `composables/useCurveTransition.ts`, `@/lib/{golden-shimmer,colors}.ts`) — the `/equation` Canvas2D partial-sum convergence instrument, census row lane-frontend:131.
**Substrate**: fourier `cd26c65` + dirty WT (subject CLEAN; both children dirty per the challenges' §0 disclosures) · installed glass-ui **4.0.0** · installed value.js **0.13.0** · value.js producer **4.0.0** (seat-read: exactly 7 subpath export keys, no `"."`, no main/module/types fallback) · glass-ui producer **8.0.0** (seat re-pin this session: `package.json` 8.0.0; `slider/types.ts:10` `SliderVariant = "scrubber" | "spectrum"`; `types.ts:35` `valueText?: (value, index) => string`) — one major PAST the 7.0.0 the D axis audited (SR-1, fourth occurrence of the drift pattern).
**Corpus**: three axis challenges (`audit/fourier-components/ConvergencePlot/challenge-{D-design 27+5, L-library 28+3, C-consumption 24+4}.md`, all read whole by this seat) × two hostile re-reader returns (**R-D** design-and-usage · **R-L** library-and-code), delivered as two separable returns — so this record carries a reader-vs-reader ruled-disagreements table.
**Method (M-23/L-18)**: every reader-vs-reader verdict conflict was re-derived against the tree with this seat's own instruments before ruling; one reader CONFIRMATION was overruled as false (SC-1); one seat cell was minted that neither reader derived (SC-2); **twelve of the two readers' fifteen "missed" filings were caught by the identity guard as re-books of banked apotheosis rows** and folded, not re-booked (SC-3/SC-4) — the sibling records fr-ConvergenceTimeline and fr-ConvergenceLegend own most of this trio-spanning corpus's child-scoped rows. Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node; zero browser — probe parsimony; product source READ-ONLY; this file is the sole write)**: installed glass-ui dist CSS extraction — base `.slider-thumb[data-v-534634a7]{width:0;…opacity:0;…display:block}`, the visible-thumb rule scoped `.glass-slider[data-variant=spectrum] .slider-thumb[…]` (full-selector extraction), and `.slider-thumb.touch-hit-area[data-v-534634a7]:before{pointer-events:none}` — **the RD-1 decider**; `dist/styles/utilities/a11y-overrides.css:7-18` read verbatim (universal `*:not([data-allow-motion])` PRM reset: `animation-duration: .01ms !important` + `transition-property: opacity, color, background-color, border-color, box-shadow !important`) + the live import chain (`web/src/style.css:3` → `dist/styles/index.css` → `dist/styles/utilities.css:19`) — **the RD-2 decider**; `useEquationCache.ts` whole-head read (docblock "Session-storage cache"; `sessionStorage` at :27/:33/:38/:48; zero `localStorage`) — **the RD-3 decider**; `harmonics.ts:59-88` read whole (`if (globalEasedT <= start) return 0` with idx-0 `start = 0`; the three guarded divisors `total <= 0 → 1` / `Math.max(minWindow, 1/total)` / `Math.max(total-1, 1)`; `spectrumColor` constant-55%-L literal) + ConvergencePlot `:34` (`easedT = easeInOutSine(t)`), `:140-141` (cursors from `harmonicProgress`), `:205` (`if (cursors[hi] < 3) continue`), `:322-333` (mount order `draw(); t.value = 0; playing.value = true; startLoop()` and an `onUnmounted` that never writes `playing`) — **the M-D1 raters**; value.js producer greps (`sampleColorRamp|mixColorsN` → **0 hits in src/ AND dist/**; `easeInOutSine` live at `src/subpaths/easing.ts:16`; `lerp` live at `src/subpaths/math.ts:10`) — **the K-11/K-15 deciders**; `groupTrigHarmonics:21-49` read whole (a_n = crP+crN, b_n = −(ciP−ciN), amp = √(a²+b²) ⇒ 2·|c_k| for a Hermitian spectrum; the two in-loop `find`s; the `amp > 1e-14`-before-`slice` order); `useCurveTransition.ts` `:8` import + `:18` hand-rolled `lerp`; plot spot-reads `:53` PAD / `:93` `getContext("2d")!` / `:336-356` template (canvas classes, tooltip `:style` with two inline `getBoundingClientRect()` calls, `v-html`) / `:382-409` scoped CSS (min-height 200px; the tooltip block; the PRM tooltip gate); `:240-253` (`onCanvasMove` hit-tests against the **canvas** rect while `draw()`/tooltip use the **container** rect; hover repaint guarded `if (!playing.value)`; `onCanvasLeave` unconditional clear); `defineEmits` grep → **0**; `playing.value =` grep → exactly `:275` (toggle — the sole route to `false`, synchronously paired with `stopLoop()`) and `:324` (`true` at mount) — **the K-9 decider**; `FrequencyGraph.vue:185-196` (identical `shadow-[0_4px_12px_rgba(0,0,0,0.12)]` literal; `z-[var(--z-controls)]`; the `Math.max(tooltipPos.y-56, 4)` clamp) — **the K-3 / D-17-rescope decider**; `grid.ts:13-20` `niceStep` (negative range ⇒ `log10(negative)=NaN` ⇒ returns NaN) + `fillText` grep → 0 in grid.ts AND the subject; `FunctionInput.vue:44-60` (`parseDomainValue` accepts any finite, no `start < end` check; `onDomainInput` writes without emitting compute) — **the M-L1 raters**; `api/routers/equations.py:82-95` + `simplification.py:34-42` (`latex, energy = simplify_series(terms, req.budget, …)` — docstring "Truncate terms by budget": the `latex` field is the **truncated series render**, not the original f(x)) — **the K-13 decider**; `EquationView.vue:308-316` mount re-read (five bindings incl. `class="flex-1"` and live-ref `:domain`).

## Scoreboard

**62 rows ADJUDICATED-CONFIRMED (1 BLOCKER booked · 15 MAJOR booked · 10 rows folded to banked identities incl. the fr-ConvergenceTimeline BLOCKER family · 23 MINOR booked · 7 MINOR folded · 6 INFO) · 16 killed-claims register entries, ALL RATIFIED by this seat's own instruments (one of them simultaneously a false-confirmation strike against R-D) · 4 seat corrections + 1 seat demotion + 1 route re-pin minted against the readers · 9 superlatives ratified · 6 genuinely-new missed rows banked (of 15 filed — the other 9 were re-books of banked rows, caught by the identity guard).**

The sole surviving BLOCKER is **D-1**: the canvas palette is theme-blind by enumeration — `resolveVizColors()` assigns five keys and never `golden`, so the primary datum strokes a frozen `#f0b632` at **1.66:1** against the shipped-default light page, the reference curve at 1.43:1, the axes at 1.19:1 — the instrument's every meaningful mark is sub-threshold in the arm that ships by default, while the token-derived cure sits one property away and the legend already paints it (the key≠datum half is banked at fr-ConvergenceLegend C-2 — one cure join, cross-referenced, never double-cured). R-D's dark-plate correction is adopted: the plot composites over `--card`, not `--background`, so the axes fail **both** arms (1.38:1 dark). The deepest cure-shaping fact in the corpus is **M-D1** (R-D's one genuinely new missed row, seat-ratified): both prescribed reduced-motion cures freeze the plot at `t = 0` — the exact frame at which `harmonicProgress` returns 0 for every index, `cursors[hi] < 3` skips every harmonic stroke and hit region, and the sum collapses to the DC line — so the PRM gate must **seed the terminal frame (`t = 1`)**, not park the clock.

## Ruled reader-vs-reader disagreements (the incredulity seat's actual work)

| id | R-D (design-and-usage) | R-L (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **RD-1 · L-B1 path (b)** | CONFIRMS: the thumb is "an invisible, **enlarged** hit region" via `touch-hit-area`, so press+release-on-thumb is a third non-commit path | KILLS: zero-width thumb cannot be a pointer target; the halo is `pointer-events: none` | **R-L's kill RATIFIED; R-D's cell is a FALSE CONFIRMATION (SC-1)** | Seat extraction from the installed dist: base thumb `width:0; opacity:0; display:block`; the visible-thumb rule is `[data-variant=spectrum]`-scoped (this site runs `standard`); `.slider-thumb.touch-hit-area[…]:before{pointer-events:none}` — unlayered, higher-specificity, variant-unqualified. `event.target` can never be the thumb ⇒ path (b) does not exist. Matches the banked fr-ConvergenceTimeline K-4 trigger set exactly — triggers (a) zero-delta press at the playhead and (c) pointercancel survive; the BLOCKER family is unharmed. |
| **RD-2 · D-9 surface count** | 5 animated surfaces ungated (incl. `.icon-swap-*` and `.legend-entry`) | 3 ungated — the producer's universal PRM reset gates the two CSS surfaces | **R-L wins** | Seat read of `a11y-overrides.css:7-18` + the live import chain: `*:not([data-allow-motion])` snaps `animation-duration` to .01ms and forces `transition-property` to an opacity/color/bg/border/shadow whitelist — the icon-swap's `scale(0.7)` transition is stripped (transform not whitelisted); the legend's `background 0.1s` is whitelist-governed and was never a PRM harm. D-9's true residual scope = the **three JS surfaces** (playback rAF, transition rAF, the shimmer's `performance.now()` oscillation) — which CSS cannot reach. R-D's D-9 cell is premise-stale; R-D's M-D1 cure derivation survives independently and is adopted. |
| **RD-3 · L-M8/C-9 severity** | MAJOR (localStorage persistence aggravator) | MINOR (the store is sessionStorage — self-XSS only) | **MINOR — R-L wins** | Seat read of `useEquationCache.ts`: docblock "Session-storage cache"; `sessionStorage` ×4; `localStorage` ×0. Per-tab, per-origin, discarded on close — no cross-session plant. The inverted posture (`catch { return latex; }` → `v-html`) books on its own: a catch that converts "the sanitiser failed" into "bypass the sanitiser" is wrong at any severity, and the cure is one `escapeHtml`. |
| **RD-4 · L-B2 severity** | BLOCKER ("reachable because L-B1 holds") | MAJOR + kills falsifier (4) as vacuous | **MAJOR — R-L wins; falsifier-kill K-9 ratified on the seat's own grep** | `playing.value` is written exactly twice: `:275` (the only route to `false`, synchronously paired with `stopLoop()`) and `:324` (`true` at mount) — no false-transition bypasses `stopLoop`, so the "independent invariant break" arm is vacuous TODAY. The orphan trace requires L-B1's latch desync — a family already graded BLOCKER once at the banked fr-ConvergenceTimeline record; grading its amplifier BLOCKER again is the double-count R-D itself banned at D-2. Plus SC-2 (seat's own cell, below): the in-page double loop self-heals through one pause click; only unmount-with-`playing===true` is irrecoverable. |
| **RD-5 · D-2/C-14(b) severity** | MAJOR (same SC 1.4.11 family as D-1; both-BLOCKER double-counts one contrast gate) | BLOCKER (constant-L HSL cannot bracket 3:1 at both ends in ANY theme; failures invert between arms) | **MAJOR — R-D wins on grading; R-L's inversion argument survives as the BINDING CURE-SHAPE constraint** | The generator (`spectrumColor`, lib/harmonics.ts:86-87, seat-read) is already banked in the ramp identity (fr-BasisCanvas M-β1 / fr-CoefficientsPanel FR-CP-2, via the fr-ConvergenceLegend D-15/M-R4/C-15 folds) — the row FOLDS there, carrying two amendments: (i) tokenisation is NOT the cure — lightness must co-vary (R-L's argument, adopted as law for the identity); (ii) **K-11**: `sampleColorRamp`/`mixColorsN` are GONE at the value.js 4.0.0 target (seat grep: 0 hits in src+dist) — the banked identity's F.W2 `sampleColorRamp` adoption angle must re-route to `oklch`/`mixColors` (both live at 4.0.0). |
| **RD-6 · C-15 disposition** | KILLED (ignition unreachable: the sole callsite gates on an awaited-response `v-else-if`; a MINOR restatement of L-B2) | UNPROVEN, demoted MINOR-defensive (Vue chains `nextTick` on the same flush) | **Converged — killed as an independent MAJOR; its one-line cure (`playing.value = false` in `onUnmounted`) books inside L-B2's row and discharges both** | No live conflict; R-D's kill is the sharper form and R-L's reachability doubt agrees with it. |
| **RD-7 · L-M5 thrash mechanism** | Confirms the read-write-read layout thrash verbatim ("confirmed by the literal ordering") | missed[2]: same-value CSSOM writes short-circuit; the real per-frame forced layout is the reactive thumb inline-`left` write landing post-microtask, before the next frame's `getBoundingClientRect` | **R-L wins on mechanism; the row survives RESCOPED** | The engine-independent halves stand on spec alone: `canvas.width/height` assignment unconditionally runs set-bitmap-dimensions (context reset — which `:94-96` re-applies, the code's own admission) ⇒ `:101 clearRect` is dead; one `getBoundingClientRect` per frame at `:84` is literal. The "style write invalidates layout every frame" cell is engine-dependent (same-value CSSOM writes are commonly short-circuited) and unprovable statically; R-L's alternative chain (L-M4's `t` ref → fresh `tArr` array defeats the prop bailout → slider re-render → thumb inline style write → next frame's gBCR forces layout) is source-complete end to end. Magnitude of either → SS-13. |
| **RD-8 · C-24/L-i3 severity** | INFO (filed in the D-27·C-23·C-24 info row) | MAJOR ("zero unit coverage and no mechanism to acquire it; the pure math is welded to a ctx") | **INFO — precedent rules** | Both banked siblings graded the identical coverage row INFO (fr-ConvergenceTimeline L·D-14 row; fr-ConvergenceLegend D-L13), identity = the census vitest-absent row, folded. R-L's MAJOR is preserved in DISSENT-3 — its strongest cell (the weld is *why* L-M1 and L-B3 survived undetected) is adopted into L-M1's proof text. |

**Seat corrections, demotions, and the re-pin:**

- **SC-1 (against R-D — a FALSE CONFIRMATION killed).** R-D's L-B1 path (b) cell adopted the challenge's `touch-hit-area` framing without checking either the variant scoping of the visible-thumb rule or the halo's `pointer-events: none` (both one grep away in the same file R-D cites). A confirmation is a claim like any other — the fr-ConvergenceLegend SC-1 corollary, now with a confirmation-flavoured instance.
- **SC-2 (the seat's own cell — neither reader minted it).** L-B2's "uncancellable for the life of the page" holds ONLY post-unmount: in-page, after the double-start trace, one pause click kills both loops (`togglePlay` sets `playing=false` and cancels one handle; the other's pending tick dies on the `:60` early return — leaving only the stale-`rafId` wart). The irrecoverable arm is exactly `onUnmounted`'s failure to clear `playing`. This narrows the repro AND pins the cure: `playing.value = false` in `onUnmounted` is the kill switch for every variant of the orphan, including C-15's.
- **SC-3 (against R-D — identity-guard failures).** Five of R-D's seven missed rows are re-books of banked apotheosis rows: missed[2] (`aria-pressed` state layer) = the fr-ConvergenceTimeline L·D-3 disposition's own cure line; missed[3] (scrollable-region-focusable) = fr-ConvergenceLegend M-R2; missed[4] (legend intercepts pointer events + two event models) = fr-ConvergenceLegend D-L3 + D-L1/M-R13; missed[5] (dashed-dot literal) = fr-ConvergenceLegend D-6+D-L9+C-10.1; missed[6] (`.timeline-count` width) = fr-ConvergenceTimeline D·D-7 (R-D's ceiling arithmetic agrees with the banked desktop/mobile split). missed[7] (8.0.0 `valueText`) is an AMENDMENT to the banked fr-ConvergenceTimeline GLASS-RELAY, not a booking — seat re-pinned `types.ts:35`: the `aria-valuetext` relay ask is now SHIPPED upstream; the relay item converts from "request" to "adopt at F.W1". Only missed[1] is new (banked as M-D1).
- **SC-4 (against R-L — same class).** missed[5] (two event families, one hover state) = fr-ConvergenceLegend D-L1 (banked, killed-and-capped at its K-1); missed[6] (`standard` → `scrubber` second migration) = fr-ConvergenceTimeline K-8. Folded.
- **SD-1 (seat demotion): D-5, BLOCKER → MAJOR.** Both readers kept the D axis's BLOCKER. The corpus's own precedent rules the other way: fr-ConvergenceTimeline corrected the nameless-button BLOCKER to MAJOR ("a11y-critical, not functionally blocking"); fr-ConvergenceLegend demoted its Level-A a11y BLOCKER on "the information survives as text". D-5's own falsifier limit concedes the numbers survive elsewhere in the view (LaTeX series, coefficients panel, FrequencyGraph); the one genuinely unreachable datum — the per-harmonic 4-dp amplitude — is ALSO the number C-7 proves is wrong by 2×. Total-affordance-loss on the instrument, partial-information-loss on the view ⇒ MAJOR, with both readers' BLOCKER preserved in DISSENT-2.
- **SR-1 (route re-pin).** Producer at 8.0.0 re-pinned by this seat (version + `SliderVariant` + `valueText`). Ratifies both readers' substrate-drift kills (K-2) and the banked fr-ConvergenceTimeline SR-1/K-8 facts; fourth occurrence — the re-trigger belongs in the F.W1 charter, not here.

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W2** value.js migration · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring. Identity guards fold by reference, never re-book.

### BLOCKER (1 booked)

| id | claim (adjudicated form) | proof anchor (seat-verified where load-bearing) | disposition |
|---|---|---|---|
| **D-1** | The canvas palette is theme-blind by enumeration: `resolveVizColors()` (colors.ts:90-96) assigns exactly five keys — `golden` absent — while the resolver IS wired (mount + theme-toggle MutationObserver), so the sum curve strokes frozen `#f0b632` at globalAlpha 0.9 ⇒ **1.66:1** on the shipped-default light page; original `rgba(180,180,180,.55)` 1.43:1; axes 1.19:1 — every meaning-bearing mark under the SC 1.4.11 3:1 floor in the default arm. R-D's plate correction ADOPTED: the plot composites over `.cartoon-card` → `--card`, so dark-arm figures re-derive to sum 6.73:1 and **axes 1.38:1 — the axes fail BOTH arms**. The token-derived `--viz-amber` (already AA'd by the D.W4.d carry) sits one property away, and the legend already paints it — the key≠datum half is banked at **fr-ConvergenceLegend C-2** (introduced by `2e4a452`(d)); ONE mechanism family, two consequence rows, one cure join. | colors.ts:11-12/:83/:90-96 (both readers, enumeration agreed); golden-shimmer.ts:48-50; index.html light default; R-D's independent recomputation adopted as the sharper cell | **F.W4** — route `golden` through the resolver (or read `VIZ_COLORS.amber`) + grid/axes/original literals through tokens; the join with the legend swatch rides the fr-CL C-2 disposition (F.W2/W.L5 + F.W4) — cross-referenced, never double-cured. Composited-pixel corroboration → SS-13 (corroboration only; the arithmetic is closed). |

### MAJOR (15 booked)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **L-B2** *(RD-4: MAJOR)* | `startLoop()` unguarded; `tick`'s early return never nulls `rafId` (the `rafId !== null` predicate is not truthful); `onUnmounted` never clears `playing` — so the L-B1-ignited double-start trace yields an orphan rAF that **survives unmount** and re-arms forever, retaining the whole closure (props, transition, `cachedScreenCurves`). SC-2 narrows it: in-page the double loop self-heals via one pause; unmount-with-playing-true is the irrecoverable arm. The in-tree counter-example (`stores/animation.ts:52,:59-62`) carries all three missing safety properties. | **F.W4** — port the store's two guards + `playing.value = false` in `onUnmounted` (discharges C-15's arm too). Attribution per the banked fr-CT ruling: this is ConvergencePlot's own row. |
| **L-B3 + C-11** | The curve matrix (`:144`) and a complete second series evaluation (`fullSum`, `:159-163`, sole consumers two min/max calls) are rebuilt every frame though neither reads `t`/`easedT`/`cursors`; at the UI-reachable ceiling (N=100, FunctionInput:184) ≈200k trig calls + ≈51k allocations per frame; only the prefix reveal (`:211`) is animated. R-L's kill of L's BLOCKER rationale RATIFIED (the hidden-panel burn was self-refuted by L's own §7 F-4 — the zero-rect return spares the body). During the 500ms transition window `draw()` runs twice per frame (L §5.1). | **F.W4** — memoise on `[coefficients, nHarmonics, domain, transition.progress]`; MEASURE BEFORE the FourierField convergence study (census FE:432/:642 carry — else the study misattributes recomputation cost to Canvas2D). |
| **L-M1** | `snapshotForTransition` receives the NEW harmonics/DC as "previous" (computeds re-evaluated inside a pre-flush watcher after the source mutated); only `oldOy` is genuinely old ⇒ the harmonic and DC lerps are identities for all `tp`, `prevMinY/prevMaxY` derive from a hybrid state that never existed, the composable's docblock is false on three of four claims, and the `else { draw() }` arm is dead. The no-test weld (RD-8's adopted cell) is why this survived: there is no seam at which the identity-lerp could have been observed. | **F.W4** — pass `old[1]` (the watcher already holds it); a 6-line unit test on `snapshotForTransition` rides the same edit. |
| **L-M2** | `props.domain` feeds `omega`, `xGrid`, the closing sample, and `maxX` — and has NO watcher; the callsite binds live refs (`[domainStart, domainEnd]`) against last-successful-response data. R-L's sharpening ADOPTED: the divergence window is UNBOUNDED — `onDomainInput` writes the model and emits nothing (seat-read FunctionInput:57-60), so a typed domain edit renders wrong-frequency curves, a backwards closing segment, or a mirrored projection until the user presses Compute, which may be never. | **F.W4** — watch `domain` (+ `expression`); at the callsite, source `domain` from `result`. Pairs with M-L1 below. |
| **M-L1** *(missed, R-L — seat-ratified NEW)* | No domain validation anywhere in the chain: `parseDomainValue` accepts any finite pair with no `start < end` check (seat-read :44-60). Reversed domain ⇒ `maxX − minX < 0` mirrors every mark AND `niceStep(negative)` returns NaN (seat-derived: `log10(negative) = NaN`) so every vertical grid line silently vanishes; degenerate domain ⇒ `omega = Infinity` ⇒ NaN blank canvas. The contrast that makes it a code-quality row: every divisor in `lib/harmonics.ts` IS guarded (S-6) while the component's two are not. | **F.W4** — validate at FunctionInput + guard the two divisors; the reversed-domain live witness is confirmation theatre for the cured state → SS-13. |
| **L-M3 + D-26** | Neither rAF clock is visibility-gated; the mobile panel is `display:none` while mounted (`panel-inactive`, EquationView:219/:437-440, default `mobileView = "controls"`) so the clock, the per-frame layout read, and the reactive cascade run unseen; the repo's own reference-counted IntersectionObserver gate is documented eight lines into `stores/animation.ts`. Extends census lane-frontend:558-559/:624 with the off-screen dimension — this record is the component booking the fr-CL identity guards deferred here. | **F.W4** — IntersectionObserver + visibility gate, the store's shape (or `useRAFLoop`'s `pauseWhenHidden`, default-on). Identity = census ungated-clock family (FE §8), folded. |
| **D-9 + C-2** *(RD-2 rescoped)* | Reduced-motion is honoured for a 0.1s tooltip fade and ignored for the three JS surfaces CSS cannot reach: the autoplaying ping-pong rAF (2–12s sweeps, from mount, forever), the 500ms transition rAF, and the shimmer's `performance.now()` alpha/shadowBlur oscillation. Both named substrates (`useRAFLoop`/`useCanvas2D`, PRM default-ON) are installed and unreached — BUT see M-D1: naive adoption ships an empty plot. M-L7 (R-L, INFO, seat-ratified via the a11y-overrides read) rides here: the component's own `:405-409` PRM block is REDUNDANT with the producer's universal reset — its one act of compliance cost and bought nothing. | **F.W4** — gate the three JS clocks under the M-D1 cure shape. Identity = census FE §8 family + lane-frontend:624 (confirmed verbatim, folded). |
| **M-D1** *(missed, R-D — seat-ratified NEW; the cure constraint)* | Both prescribed PRM cures (gate `playing` at mount; adopt `useRAFLoop`/`useCanvas2D` park-one-frame) freeze the plot at `t = 0` — seat-derived: `easedT = easeInOutSine(0) = 0`; `harmonicProgress` returns 0 for every idx (idx-0 `start = 0`, `0 <= 0` — seat-read :72); all cursors = 0; `:205` skips EVERY harmonic stroke and hit region; `sumY` collapses to DC. The first painted frame at mount IS this frame (`draw()` precedes `playing = true` — seat-read :324), and the ping-pong returns to it each cycle. The correct cure seeds the TERMINAL frame (`t = 1`, where `:73` fires for every harmonic): a PRM user gets the converged state, not a blank grid whose only escape is a keyboard-dead scrubber. | MAJOR (cure-constraint) · **F.W4** — binds D-9/C-2's cure; also explains the pause-near-turnaround empty-plot corollary. |
| **L-M4 + C-12** | The frame clock is a Vue `ref` written 60×/s; `tArr`'s getter allocates a FRESH array per invalidation (R-L's sharpening — Vue's prop reference-equality bailout can never fire), re-rendering the reka Slider subtree every frame; `activeCount` is an O(N) computed per frame; the same file's OTHER clock is a non-reactive POJO (S-4) — two clocks, opposite answers. **Absorbs fr-ConvergenceTimeline M-5** (the same identity observed from the child seat; the mechanism — `t = ref(0)` — lives here, so the plot owns the row). M-L3 (R-L missed, seat-verified) rides: the parent's OWN render effect also depends on `t` (`:367`), so while hovered the template's two inline `getBoundingClientRect()` calls (`:350-351`, seat-read) run per frame. | **F.W4** — deliver timing by argument (the `useRAFLoop` idiom) or a non-reactive clock + explicit child sync; magnitude → SS-13 (one trace). |
| **L-M5 + C-10** *(RD-7 rescoped)* | `draw()` reassigns `canvas.width/height` every frame — spec-mandated bitmap realloc + full context reset (the `:94-96` re-application is the code's own admission) — making `:101 clearRect` dead code; plus one forced layout read per frame at `:84`. The thrash sub-mechanism is RESCOPED per RD-7: the provable per-frame forced layout runs through L-M4's thumb-style-write chain, not the same-value canvas style writes. M-L2 rides as the mechanism cell. | **F.W4** — size in the resize handler, draw in the draw path (`useCanvas2D` owns exactly this split); ResizeObserver-loop feedback → SS-13. |
| **L-M7 + C-8** | The "sum" and "original" tooltips are character-identical at the only callsite (the `?? "\\text{sum}"` fallback unreachable), the golden partial sum is labelled `f(x) =` ⟨the original⟩ — asserting the identity the plot exists to disprove — and the string is raw SymPy through KaTeX (`x*(pi - x)` renders as juxtaposed italics with a literal asterisk) while `latex`/`latex_sigma` arrive unconsumed on the same response. **K-13 binds the cure**: `latex` is the truncated-series render (seat-read equations.py:91 + simplify_series docstring), so routing it to the ORIGINAL curve would re-commit the same inversion — the original's LaTeX needs a new backend field. | **F.W4** — label honesty now (`latex_sigma` → sum; distinct original label); **F.W5** rider — `sp.latex(expr)` field on ComputeEquationResponse + C §6's field-level-gap counting rule. |
| **C-7** | Two contradictory `amplitude` conventions in one view, exactly 2× apart, both labelled `A`: the DTO ships `|c_n|`; `groupTrigHarmonics` discards `amplitude`+`phase` and re-derives `√(a²+b²) = 2|c_k|` under the same name (algebra seat-verified from :41-43); the tooltip says `A = 0.8106` while the notation-pill hover says `A_1 = 0.4053`; FrequencyGraph documents the DTO convention — 2 of 3 surfaces agree, the plot disagrees undisclosed. | **F.W5** — the intake **R6-8 mirror rider** (client shadows a server field with a same-named different-valued re-derivation; the shared-provenance contract needs the rule in BOTH directions) + **F.W4** rename-or-consume (`trigAmplitude`, or consume the DTO field). |
| **D-10** | The cursor tooltip has zero collision handling in either axis — raw offsets, `white-space: nowrap`, unbounded KaTeX width — clipped by the grid's `overflow: hidden`; the sibling clamps one axis and centres the other (seat-read FrequencyGraph:188/:191). Rider: this hand-rolled `v-html` tooltip is a **10th surface outside intake R3-7a's 35-callsite budget** — F.W3's sweep must count it, not merely mark the trio clean. | **F.W4** — clamp/flip (the sibling's pattern); escape threshold → SS-13. |
| **D-12** | A quantitative plot with adaptive tick machinery and no axis labels or tick values anywhere — `niceStep` computed, then lines only; `fillText` = 0 in grid.ts AND the subject (seat grep); user-set domain never surfaced; 8% y-headroom with no scale; 4-dp amplitude on hover. | **F.W4** — endpoint values + tick labels, the machinery already exists. |
| **D-5** *(SD-1: MAJOR)* | The instrument's canvas has zero accessible representation — no role, no label, no fallback content (self-closed), nothing focusable in the plot box; every information path is pointer-hover (12px hit regions); the mark→meaning mapping and the per-harmonic readout are keyboard/touch/AT-unreachable. The legend-rows half folds → **fr-ConvergenceLegend D-2** (banked, same cure pass). | **F.W4** — canvas `role="img"` + a data-driven `aria-label`/description + a focusable inspection path, sequenced with the fr-CL D-2/M-R2 pass; axe run on `/equation` → SS-13. |

**MAJOR folded to banked identities (10)** — original ids for life, never re-booked: **L-B1** (+ the parent's scrub-handler half) → fr-ConvergenceTimeline L·D-1+C·C-1 family (BLOCKER there; this record contributes the RD-1 path-(b) ruling ratifying the banked K-4 trigger set, and the producer-chain re-derivation both readers performed) · **D-4+C-5** (keyboard scrub clobbered; playing = arrival state) → fr-CT L·D-5 family (the enabling pairing defect F.W3; the amplifier pair books here at L-B2) · **D-3** (nameless transport) → fr-CT L·D-3 (banked MAJOR; the readers' BLOCKER labels die against the banked correction) · **D-8+C-6+L-m3** (ARIA trio on a roleless span; duplicated label; wrong quantity) → fr-CT L·D-4 (+ the SC-3 valueText amendment: the relay ask is now shipped at 8.0.0) · **D-13+C-13** (1.75rem floor destruction + wholesale paint override) → fr-CT D·D-5+C·C-5 / L·D-10 · **D §3 B-a** (Button prop-surface break behind a retained subpath, 35+ sites) → fr-CT D·D-2 (the F.W1 census-amendment row; both readers re-confirmed against the live producer) · **D-6** (legend occupies the plot; 12px gutter vs ≥100px overlay) → fr-CL D-L3 · **D-7** (glass-wash sub-perceptual rung) → fr-CL D-1+C-3 · **D-2+C-14(b)** → the ramp identity (fr-BasisCanvas M-β1 / fr-CoefficientsPanel FR-CP-2) with RD-5's two amendments · **L-M6+C-1** (three bare-root value.js specifiers; 4.0.0 has no `"."` — both ends seat-read; cure `@mkbabb/value.js/easing`, symbol survives at subpaths/easing.ts:16) → the banked census identity (CENSUS-2026-08-03:38 / lane-frontend:480), **F.W2**, latent at HEAD.

### MINOR (23 booked · 7 folded)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **L-M8 + C-9** *(RD-3: MINOR)* | `renderKatexInline`'s catch returns the raw interpolated user string into `v-html`; `throwOnError:false` scopes ParseError only. Threat model self-XSS (sessionStorage — seat-read). | **F.W4** — `catch { return escapeHtml(latex) }`; non-ParseError trigger → SS-13. |
| **L-m4** | Vacuous outer clamp — inner term ≥3000 ∀n, the 2s "floor" is a decoy. Kills the floor cell of D's S-5 (K-16); both readers agree. | **F.W4** — delete the dead `max`; keep the pedagogy comment (S-5). |
| **L-m5** | `animDuration` change mid-flight jumps `t` — `loopStartTime` rebased once, divided by the live duration each frame; the store captures `const dur` per loop. | **F.W4** — same edit as L-B2's guards. |
| **L-m6** | `clearShimmer(ctx)` at `:232` is a no-op inside save/restore (all three touched fields restored). | **F.W4** — delete or move outside the pair. |
| **L-m7** | Dead ternary at `:128` (guaranteed truthy by `:100`); its `:129` twin silences an `ox/oy` length mismatch into NaN screen coords instead of failing loudly. | **F.W4** — assert lengths; delete the dead branch. |
| **L-m9** *(reader-unattacked; seat spot-verified `:93`)* | `getContext("2d")!` re-asserted every frame with no context-lost posture — a null return throws inside the rAF callback, terminating the loop with no rafId reset and no error surface. | **F.W4** — hoist the context + a null/`contextlost` posture; flagged: neither reader attacked this row, seat verified the anchor only. |
| **L-m10** | `mousePos` written per mousemove before the hit test regardless of hover state; two template gBCR calls per render for one rect (rides L-M4's M-L3 cell). | **F.W4** — guard + cache the rect. |
| **L-m11** | `Math.sqrt` before the threshold compare; no squared-distance shortcut, no bbox reject, over ~17k points/mousemove at ceiling. | **F.W4** — squared-distance compare. |
| **L-m12** | `groupTrigHarmonics` O(n²) — two in-loop `find`s (seat-read :34-35); ~40k comparisons per response at 201 terms. Memoised per response ⇒ MINOR. | **F.W4** — one Map pass. |
| **L-m13** | `nHarmonics` = surviving-harmonic cap here (filter-before-slice, seat-read :44/:48) vs index bound on the wire; two denominators, one name. | **F.W4** naming + **F.W5** contract note. |
| **L-i2** | Double easing: `easedT = easeInOutSine(t)` fed into `harmonicProgress`, which applies `easeInOutSine` again (seat-read :75); docblock documents only the inner one. | **F.W4** — document or collapse to one explicit curve. |
| **C-17** | Zero emits (seat grep: `defineEmits` = 0) — `playing`/`t` private and auto-started; the parent cannot pause during its own `computing` window or on tab hide. The discipline exists one level down (the child's 4-event contract). | **F.W4** — expose a minimal playback contract; pairs with L-M3's gate. |
| **C-19** | Hand-rolled `lerp` eight lines below the value.js import (seat-read :8/:18); `lerp` live at 4.0.0 `./math` (seat-read subpaths/math.ts:10). | **F.W2** rider on the specifier edit — import from `@mkbabb/value.js/math`. |
| **C-20** | `{ deep: true }` over two 500-point arrays; the parent replaces `result` wholesale — reference equality suffices; deep also destabilises the `old` tuple L-M1's cure depends on. | **F.W4** — drop `deep`, same edit as L-M1. |
| **D-16** | Absolute px type (13/13/12px) under a responsive root — the trio is the route's only non-scaling text. | **F.W4** — one unit pass; identity = fr-CL D-3 + fr-CT D·D-11 (the sweep gains the plot site). |
| **D-17** *(rescoped per R-D's kill-cell)* | `--z-controls` on a tooltip where `--z-tooltip` exists — but the sibling does the same (seat-read FrequencyGraph:187): a directory-wide convention, wave-owned, not a file slip. | **F.W4** — one z-rung decision for the equation directory; adjacent fr-CL C-12. |
| **D-18** | `text-muted-foreground` on the canvas is inert — no draw call reads computed color; every stroke is a literal. | **F.W4** — delete, or wire via `resolveCanvasColor` with D-1's cure. |
| **D-19** *(canvas arm)* | The canvas sets no `cursor` despite 12px hit regions (sibling: `cursor-pointer`); the legend arms (`pointer-events-auto` no-op, `cursor-default` on interactive rows) fold → fr-CL C-11/D-2. | **F.W4** — one cursor declaration. |
| **D-20** *(surviving half after K-3)* | The tooltip's `92%` translucent `--popover` admits the moving curves beneath it (the sibling is flat `bg-popover`); the theme-blind shadow literal is a directory convention (killed as file-local), cure wave-owned. | **F.W4** — opaque surface; shadow literal → the directory pass. |
| **D-21 + C-21** | Comment provenance drift ×2: `:113` cites equations.py:61, live line 59; the ConvergenceTimeline docblock's `useOptionalDockContext` mechanism is doubly stale (component prop at 4.0.0; `keepDockOpen` deleted at 8.0.0 — per the banked K-8). | **F.W4** — symbol-anchored citations (the S-1 keep-rule); the Timeline cell rides fr-CT's record. |
| **D-22** *(reader-unattacked; seat spot-verified `:53`)* | PAD asymmetry corresponds to nothing in the render path — no labels to reserve for (D-12), minimum on the one side that needs reserve (D-6). | **F.W4** — rides D-12's label cure + the fr-CL D-L3 layout edit. |
| **D-23** *(reader-unattacked; seat spot-verified `:388-403`)* | Two inert tooltip declarations: Fira on KaTeX-owned content (reachable only via the L-M8 catch arm), and `tooltip-in`'s centre-origin scale on a cursor-anchored box. | **F.W4** — transform-origin + the L-M8 cure makes the font cell fully dead. |
| **M-L4** *(missed, R-L — seat-verified NEW)* | The hit test measures the CANVAS rect (`:243`) while `draw()` and the tooltip measure the CONTAINER rect (`:84`/`:350-351`) — coincident only because the container has no padding/border; the single most likely future edit silently mis-registers every hit region, with no type error and no test. Plus `cachedScreenCurves` staleness across early returns. | **F.W4** — one rect source; a regression test rides L-M1's new seam. |

**MINOR folded (7)**: **D-11+L-m2+C-16** (fragment root, class dropped, dev warning; R-D's kill of D's MAJOR grading ratified — K-4) → fr-CT M-1 · **D-14+C-3** (dead `--slider-scrub-track-height`, dead at 4.0.0 AND the current producer) → fr-CT L·D-6 + M-2 (the 23-declaration/7-file family) · **D-15** (Fira literals ×3) → fr-CL D-11 (the three-site sweep identity) · **L-m1+C-18** (`TrigHarmonic` declared twice, structurally joined) → fr-CL D-L7 — with the owner note: the defect's home is this component pair; the cure (one declaration) rides L-M1's edit · **L-m8** (legend rows that cannot highlight; `:205` skips stroke+hit region while the legend lists all N) → fr-CL M-R1 (MAJOR there — the legend-side booking owns it; the plot-side `continue` is the shared anchor) · **D-24** (glyph hygiene: `size-3` opt-out of `--ui-glyph`; no aria-hidden) → fr-CT D·D-13 with an `aria-hidden` rider · **L-i4** (`tArr` narrows a nullable emit) → fr-CT L·D-12.

### INFO (6)

| id | claim | disposition |
|---|---|---|
| **D-25** | `min-height: 200px` floor vs a 100-harmonic/102-row content ceiling — set independently. | **F.W4** — rides the fr-CL D-L3 layout ruling. |
| **D-27 + C-23** | The staged-reveal pedagogy dissolves at default N (~6.3 harmonics inside their windows simultaneously); `N=x/y` counts `progress > 0.5` — matching neither the sum (all N always in) nor the drawn set (`cursors ≥ 3`). | **F.W4** — a deliberate ruling on `sliceW` + one honest denominator. |
| **C-24 + L-i3** *(RD-8: INFO)* | Zero unit coverage, no runner, no seam; `/equation` e2e = one screenshot with swallowed navigation; the pure math welded inline in a 157-line `draw()` is WHY L-M1/L-B3 survived. | Identity = census vitest-absent row (folded); the seam-cut rides L-M1/L-B3's cures; `checkA11y(page, "equation")` + one functional spec → F.W4. |
| **C-22** | `@mkbabb/keyframes.js` declared, unconsumed by a surface owning two rAF tweens — F.W2 scope evidence, not a defect. | **F.W2** budget note (folded to the census keyframes cell). |
| **L §5.1** | Path B is TWO ungated clocks (playback + transition), overlapping 500ms per response — census cell low by one; repo total 4 animation clocks. | Census amendment rides this record; **F.W4**. |
| **L §5.3** | R5-7 second witness (the subtree's only `v-for` is native; glass-ui's component thumb loop in the same strip IS visible) + the NEW carry: native-loop counting must record the loop's BOUND (1..100 UI / le=200 wire), not merely its existence. | The witness folds → intake R5-7's standing carry (+1 already counted at fr-CL D-L5); **the bound-carry rider books here** → F.W4 denominator spec. |

## Killed-claims register — 16 entries, ALL RATIFIED

| # | killed claim | refutation (adjudicated) | seat instrument |
|---|---|---|---|
| K-1 | C-4 — "adopt `glass-ui/timeline` `GlassTimeline variant=\"scrubber\"`, the wrong primitive was chosen twice" | R-D's kill RATIFIED: the component is deleted at 8.0.0 and the producer RULED timelines `role="progressbar"`, never slider — "the commanding surface is `<Slider :marks>`", which is what ConvergenceTimeline is. Consistent with the banked fr-CT L·D-7/K-14 REJECTED-cure ruling. Survivor: the `marks` prop is the right expression of the harmonic entry points. | seat 8.0.0 re-pin + banked fr-CT SR-1 |
| K-2 | D §3 — "the Slider variant axis is byte-stable; `standard` survives the next pin" | Both readers killed it; identity = banked fr-CT K-8 (`SliderVariant = "scrubber" \| "spectrum"` at 8.0.0; the WT migration lands on a deleted name). Ratified on the seat's own types.ts read. | slider/types.ts:10 |
| K-3 | D-20's "the sibling composes the same surface from tokens" (shadow half) | Half-kill RATIFIED: FrequencyGraph carries the IDENTICAL `shadow-[0_4px_12px_rgba(0,0,0,0.12)]` literal (seat-read :190) — a house convention, cure wave-owned. The 92%-translucency half survives and books. | FrequencyGraph.vue:185-196 |
| K-4 | D-11's MAJOR grade for the fragment-root attr drop | RATIFIED — one callsite, class functionally re-supplied, cost = a dev warning; consistent with the banked fr-CT M-1 MINOR. | banked record + both readers' greps |
| K-5 | C-15 — "orphaned rAF from unmount inside the mount `nextTick`" as an independent MAJOR | RATIFIED per RD-6: ignition unreachable at the sole callsite (awaited-response `v-else-if`); the reachable orphan is L-B1's trace; the cure line books in L-B2. | EquationView gate + RD-6 |
| K-6 | C §0.2 — "cleanest-consumer claim contradicted at component level" | RATIFIED: a Canvas2D leaf with no chrome to import; its chrome children DO import glass-ui; the token register is 100% producer-sourced (S-5's A.W3.d verification). Counting imports per SFC makes a leaf renderer look like a defector. | both readers + S-5 anchors |
| K-7 | L-B1 path (b) — press+release on the thumb as a third non-commit path | RATIFIED per **RD-1** (the seat's own dist extraction); simultaneously strikes R-D's confirmation (SC-1). | glass-ui.css extraction |
| K-8 | L-B3's BLOCKER rationale ("hidden panel still burns 12M trig calls/s") | RATIFIED — self-refuted by L §7 F-4: the zero-rect return spares the body; survivors = scheduling, one layout read, the reactive cascade (booked at L-M3/L-M4). | ConvergencePlot :84-85 |
| K-9 | L-B2 falsifier (4) — "any playing→false transition not routing through stopLoop leaves a stale handle" | RATIFIED as vacuous: seat grep — `playing.value` is written at exactly `:275` (paired synchronously with `stopLoop`) and `:324` (true). No such transition exists today. | seat grep |
| K-10 | L-M8/C-9's localStorage persistence aggravator | RATIFIED per **RD-3**: sessionStorage ×4, localStorage ×0 (seat read). Threat = self-XSS; severity MINOR. | useEquationCache.ts |
| K-11 | C-14(a) — "`sampleColorRamp` survives at `@mkbabb/value.js/color`" | RATIFIED: seat grep of the value.js producer — `sampleColorRamp`/`mixColorsN` = 0 hits in src+dist; `color.ts` exports 22 symbols, neither among them. **AMENDS the banked ramp identity's F.W2 angle** (fr-CL C-15 recorded the installed-0.13.0 availability; the 4.0.0 target deletes it) — the cure builds from `oklch`/`mixColors`. | value.js src+dist greps |
| K-12 | C-6's "glass-ui forwards exactly four ARIA attributes" enumeration | RATIFIED: the installed chunk emits `aria-label` alone (both readers' greps agree; consistent with the banked fr-CT record) — a 7.0.0-source fact projected onto the 4.0.0 dist. The finding's load-bearing half is STRONGER at the pin. | banked + readers' greps |
| K-13 | C-8's cure ("route `result.latex` to the original curve") | RATIFIED: `latex` is the budget-TRUNCATED series render (seat-read equations.py:91 + the simplify_series docstring) — the prescribed routing would label the target with its own approximation, the exact inversion C-8 diagnoses. Cross-repo field required (F.W5). | api + simplification.py reads |
| K-14 | D-9's falsifier cell ("no global reduced-motion reset can capture these") | RATIFIED per **RD-2**: the universal `*:not([data-allow-motion])` reset is live in the cascade; two of D's five surfaces are producer-gated; the finding survives correctly scoped to the three JS clocks. | a11y-overrides.css + import chain |
| K-15 | D §3's "whether `easeInOutSine` survives value.js 4.x is UNPROVEN" | RATIFIED as decidable-and-decided: `src/subpaths/easing.ts:16` + the `./easing` export key (seat-read, home repo). The genuine hazard was always the specifier (L-M6). | value.js reads |
| K-16 | D S-5's "well-chosen 2s floor" superlative cell | RATIFIED: the inner term is ≥3000 ∀n — the floor is dead arithmetic (L-m4, both readers). The pedagogy COMMENT keeps its credit; the praised clamp contains a decoy. | :37-41 arithmetic |

## Superlatives — ratified with provenance (L-18 runs both ways)

- **S-1 · The endpoint-convention triptych** (all three axes + both readers; falsifier applied by every seat and survived) — two deliberately different x-grids with the reason in prose, cited to the producer file AND the paper section; `endpoint=False` is real at equations.py:59, so appending `domB` with `y = oy[0]` is the unique correct closure and the naive symmetry would be wrong. In a closure with zero unit tests, this comment does a test's job. Keep verbatim; anchor on the symbol (the :61→:59 drift is D-21's argument).
- **S-2 · The BLEND cursor feather** (R-D) — a clamped linear ramp that prevents a step discontinuity from sweeping across the one mark the user is watching; a considered answer to a problem the naive implementation doesn't know it has.
- **S-3 · The transition lerps the axis bounds, not just the data** (both readers) — nearly every hand-rolled plot jump-scales under a smooth data transition; doing the harder half is the mark of someone who watched their own animation. The irony filed at L-M1 stands: the bounds lerp is the only half of the snapshot still (half-)working.
- **S-4 · The transition clock is a non-reactive POJO** (both readers) — frame-rate mutation, zero reactive invalidation; the right answer to L-M4's question, already in a module the file imports — cheap to fix toward, hard to excuse away from.
- **S-5 · A.W3.d hygiene, executed twice with reasons recorded** (R-D; byte-verified) — the excised shadow `tooltip-in` keyframe with its resolution path checkable, and `transition: all` replaced by named properties on a tokenised curve. Sharpened, not softened, by M-L7: even the file's one PRM block turns out to be redundant with the producer's reset — the discipline was real; the coverage was not.
- **S-6 · Every divisor in `lib/harmonics.ts` is guarded** (R-L; seat-verified :64/:67-69/:86) — three NaN paths closed by construction in a file with no test net, by an author who walked the degenerate cases — the exact discipline the component omits at `2π/(domB−domA)` and `/(maxX−minX)`, which is what M-L1 is made of. The contrast is the finding.
- **S-7 · The three-file decomposition + extraction discipline** (all axes + both readers) — prop-in/event-out children with typed contracts, zero provide/inject, four real modules with no barrel ceremony, a cancellable rAF driver whose canceller the consumer actually stores and calls. The honest limit stands: the extraction stopped one file short of the math that carried the risk.
- **S-8 · `aria-label="Harmonics timeline"` is precisely the one attribute the producer forwards** — banked at fr-ConvergenceTimeline's superlative register; re-ratified here in passing (the contrast that makes the folded D-8/C-6 legible).
- **S-9 · `.timeline-count`'s tabular-nums + reserved box** (R-D) — banked at fr-ConvergenceTimeline (D·D-7 credits the instinct; the width defect is the banked row). R-D's independent re-derivation of both halves is recorded as corroboration, not a new row.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **D-1/D-2 composited-pixel corroboration** — arithmetic is seat/reader-closed; a screenshot adds confirmation, not evidence.
2. **D-7's worst-frame legend text contrast** over the animated strokes — rides the banked fr-CL SS-13 items (frame-dependent by construction).
3. **D-10's escape threshold** (viewport × expression length); the clamp absence is static.
4. **L-M5's ResizeObserver feedback loop** — both readers doubt reachability; layout-dependent.
5. **L-M8/C-9's non-ParseError trigger** (now MINOR; self-XSS only).
6. **L-M4/C-12 + absorbed fr-CT M-5 magnitude** — one performance trace on `/equation` (slider-subtree re-render rate + per-frame realloc cost + the RD-7 forced-layout chain).
7. **M-D1 confirmation theatre for the CURED state** (the `t = 1` PRM frame) and **M-L1's reversed-domain witness** — mechanisms need no proof; the cures will.
8. **Legend overflow at default N** and the **event-family dispatch probe** — banked fr-CL SS-13 items (the latter capped INFO/MINOR by fr-CL K-1); not re-opened here.

## Identity guards (anti-rename) — folds by reference

- The **scrub/transport family** (L-B1, D-4+C-5, D-3, D-8+C-6+L-m3, D-13+C-13, D-14+C-3, D-24, D §3 B-a, L-i4, the timeline-count and aria-pressed missed re-books) is banked at **fr-ConvergenceTimeline** (L·D-1/D-4/D-5/D-3, D·D-2/D-5/D-7/D-13, L·D-6+M-2, K-4/K-8, SR-1) — this record contributes the RD-1 path-(b) ruling and the SC-3 `valueText` relay amendment (ask → adopt-at-F.W1), nothing re-booked.
- The **legend family** (D-6, D-7, L-m8, D-19's legend arms, the scrollable-region/dashed-dot/event-family missed re-books) is banked at **fr-ConvergenceLegend** (D-L3, D-1+C-3, M-R1, M-R2, D-6+D-L9+C-10.1, D-L1/M-R13, C-11) — folds only.
- The **ramp identity** (fr-BasisCanvas M-β1 / fr-CoefficientsSpectrum B-3/M-10 / fr-CoefficientsPanel FR-CP-2) gains D-2+C-14(b)'s curve-stroke arm and TWO amendments: the lightness-co-variation cure-shape law (RD-5) and the K-11 `sampleColorRamp`-gone-at-4.0.0 re-route.
- The **value.js root-export break** stays ONE identity at CENSUS-2026-08-03:38 / lane-frontend:480 (**F.W2**; three of five sites in this closure; C-19's `lerp` and C-22's keyframes-budget cells ride the same wave).
- The **ungated-clock family** (census FE §8, lane-frontend:558-559/:624) receives its component booking HERE (L-M3, D-9+C-2, L §5.1's two-clock amendment) — exactly the deferral fr-ConvergenceLegend's guards recorded.
- **R6-8** gains the C-7 mirror rider (F.W5, both directions); **R5-7** keeps its standing carry (+the L §5.3 bound rider, new); **R3-7a** gains the D-10 tenth-surface rider; the **fragment-root** row stays fr-CT M-1; **vitest-absent** stays the census row.
- **fr-ConvergenceTimeline M-5 is ABSORBED** into L-M4+C-12 (one identity; the mechanism lives in this file) — recorded there and here so neither record re-books it.

## DISSENT — preserved, not ruled away

1. **The identity-guard failure is now systemic fleet evidence** (third consecutive record): 12 of the two readers' 15 missed filings were re-books of banked rows — R-D went 1-new-of-7, R-L 5-new-of-7 (two of them riders). Hostile re-readers attack the corpus in front of them and do not sweep the registry; the apotheosis seat exists partly for this, and the SS-3/SS-4 spec authoring should consider handing readers the registry index as challenge input.
2. **SD-1 (D-5 BLOCKER→MAJOR) is a seat judgment call against BOTH readers.** A forming X·F spec that grades zero-accessible-surface as first-class-blocking may restore BLOCKER; the mechanism cells need no re-derivation either way. The same applies to the readers' BLOCKER labels on D-3/D-4 folded rows, which die against banked corrections rather than against new evidence.
3. **R-L's MAJOR for the coverage row (RD-8)** is defensible — its causal cell (the weld is why L-M1/L-B3 survived) is adopted into the roster even though the grade follows precedent at INFO.
4. **R-D's severity architecture was the corpus's best** (the D-2 double-count demotion, the D-11 kill) and carried the corpus's one false confirmation (SC-1) — the citation-inheritance lesson now has confirmation-, correction-, and kill-flavoured instances across the three sibling records.
5. **Substrate drift, fourth occurrence**: every 8.0.0-derived figure here re-pins at F.W1 open; the standing re-trigger belongs in the F.W1 charter (the fr-CT K-8 / fr-CL SR-1 lesson, again).

---

**VERDICT — DEFECTIVE, ADJUDICATED**: 1 BLOCKER booked (the theme-blind canvas palette — the instrument's every meaning-bearing mark below the SC 1.4.11 floor in the shipped-default arm, cured one property away) + 15 MAJOR booked (led by the orphan-clock amplifier, the frame-invariant recompute, the identity-lerp'd transition, the unbounded domain divergence, and the M-D1 cure constraint that saves the PRM repair from shipping an empty plot) + 10 rows folded to banked identities incl. the fr-ConvergenceTimeline scrub BLOCKER family + 23 MINOR + 6 INFO across 62 rows; all 16 kills RATIFIED on this seat's own instruments, one reader confirmation struck as false (SC-1), one seat cell minted (SC-2), and 12 of 15 reader "missed" filings caught as registry re-books. The file's mathematics is genuinely careful — the endpoint triptych, the BLEND feather, the bounds lerp, the guarded harmonics divisors — and its defects concentrate in exactly the seams the extraction left inline: the two clocks, the one untested `draw()`, and a palette that never met its own resolver. One F.W4 pass (clocks + palette + a11y + honesty of labels), the F.W2 specifier edit, and the already-standing GLASS-RELAY dissolve the worst of it. Routings are M-25 agglomeration input; nothing here stamps a wave.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3. This document is the sole write.
