claude-fable-5

# fr-AdminUserList — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: `/Users/mkbabb/Programming/fourier-analysis` @ `cd26c65` (branch `m/w1-bump-migration`) — **audited bytes are the WORKING TREE**: `web/src/components/visualization/gallery/AdminUserList.vue` (529 lines, read whole this session, byte-consistent with all three challenges and both readers) carries exactly one uncommitted hunk over HEAD (`:461` `variant="opaque"` → `surface="opaque"`, part of the uncommitted 3.1→4.0 bump sweep that also holds the `^4.0.0` pins in `web/package.json`; committed HEAD still declares `^3.1.0`). Installed glass-ui = **4.0.0** (node_modules, re-read); producer glass-ui = **7.0.0** at `/Users/mkbabb/Programming/glass-ui`. **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two hostile re-reader returns (reader-DU = design-and-usage · reader-LC = library-and-code) over the full challenge corpus — `challenge-D-design.md` (D-01..D-32 + S-1..S-6, opus-5[1m]), `challenge-L-library.md` (L-1..L-23 + K-1..K-4 + S-1..S-6, opus-5[1m]), `challenge-C-consumption.md` (C-B1..B4 / C-M1..M7 / C-m1..m9 / C-i1..i4 + S1..S5, opus-5[1m]). All three challenge files were re-read **whole** by this seat.
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, and every net-new missed row was re-derived against the bytes by this seat with its own instruments before adoption. Suspiciously-agreed cells (the grep counts, the export-map enumerations, the compiled-CSS byte offsets) were independently re-measured, not spot-trusted. Product source READ-ONLY; **zero writes outside this file**; no browser tooling (probe parsimony — every ruling below is static: read + grep + node against installed dist). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25), never a wave stamp.
**This seat's own instruments (all read-only)**: full reads of `AdminUserList.vue` (529), `useOffsetPagination.ts` (83, whole), `useToast.ts` (whole); `api.ts` :40-75 (abort registry, `isAbortError`), :120-165 (coreFetch auth throw + `signal ?? abortable(abortKey)`), :245-256 (`adminFetch` — `coreFetch<T>(path, /* abortKey */ path, …)`), :560-650 (all five admin wrappers + the A.W5.c batch-repair comment); `api/routers/admin.py` :235-405 (list_users **both** pipelines · set_user_status incl. `sessions.delete_many` on suspend · delete_user return `{"ok","deleted_entries"}` · prune_empty_users unfiltered `$lookup` + `{"pruned"}`) and :450-501 (batch_users: three branches, one return `{"ok","affected"}`); `api/models/admin.py` :10-55 (required datetimes, `status: str = "active"`, `Literal` request models, `slugs … max_length=50`); `api/scripts/migrate_visualization.py` :380-400 (**`"status": "orphan-migrated"` upsert — the L-13 falsifier decider**); `api/services/janitor.py` :195-220 (fieldless-doc-skipping reap + the stale-user visualization **soft**-delete cascade); GalleryView.vue :31/:167/:175-179/:189-191/:204-205/:220/:309-313/:375-376; AdminAuditLog.vue :15-38 (the clone block) + `error` grep → 0; AdminFlaggedPanel.vue :137-142 + GalleryCard.vue :53-61 (the two `timeAgo` dialects, verbatim); export-map enumerations by node against **both** trees: glass-ui 4.0.0 = 80 keys (`./confirm-dialog` ✓, `./forms` ✓, `./tooltip` ✓, `./icon-tooltip` ✓, `./data-table` ✓, `./checkbox` ✗, `./pagination` ✗) and producer 7.0.0 = 67 keys (`./confirm-dialog` **✗**, `./forms` **✓**, `./tooltip` ✓, `./icon-tooltip` ✗, `./data-table` ✓, `./metric` ✓, `./chip` ✓, `./checkbox` ✗) + `find src -iname '*confirm*'` in the 7.0.0 source → **nothing** + `grep -rn ToastVariant src/` → **empty** + `src/index.ts:124` (root-barrel Checkbox at 7.0.0) + 7.0.0 peers (`@lucide/vue ^1.16.0`, `keyframes.js ^6.0.0`, `value.js ^4.0.0`); ConfirmDialog.vue.d.ts at the pin (`{title, description?, confirmLabel, destructive?, loading?}` + `open` model); Checkbox dist base class (`focus-ring peer relative touch-hit-area h-4 w-4 shrink-0 …`) + indeterminate fill + glyph-swap regexes ✓ + `boolean | "indeterminate"` d.ts ✓; a11y-overrides.css (`.input-pill` inside the forced-colors restore ✓ · PRM `animation-duration: 0.01ms !important` ✓ · `[data-size="icon"]` `min-block-size` touch floor ✓); tokens (`--type-admin-label: 0.625rem` "10px — fixed sub-control micro (NOT fluid)" ✓ · light `--card: hsl(36 48% 97%)` ✓ · `--z-popover:130/--z-modal:140/--z-toast:160` ✓ · coarse `--control-floor: var(--touch-target)` ✓); `forms.d.ts:1` = `export * from "./components/ui/input"`; tailwindcss **4.3.1** + preflight `border: 0 solid` (colorless) + built-CSS probes on `dist/assets/index-57FkGzlZ.css` (`.shadow-cartoon{` @ **129479** vs `.cartoon-card{` @ **109616** — shadow-cartoon wins by source order; `border-color:var(--border)` count = **2**; `.outline-hidden` **absent**; `.outline-none` unconditional; `.border` utility colorless); `dist/assets/AdminUserList-tn0RQdqM.css` = **0 bytes**, referenced by `dist/assets/GalleryView-B4SY1qTS.js` ✓; reka-ui deciders read directly: `Select/SelectContent.js` (closed-state slot **teleported into a detached fragment under SelectProvider** — items register while closed) + `Select/SelectValue.js` (`selectedLabel` resolves from `optionsSet` textContent; placeholder only when empty) + `Dialog/DialogContentImpl.js:53` (`if (getActiveElement() !== document.body) rootContext.triggerElement.value = getActiveElement()`) + `DialogContentModal.js:60` (`triggerElement.value?.focus()` on close); count battery (my own greps): `data-some` = **2** hits both in this file · `data-selected` = this file + GalleryCard only · `function timeAgo` = **5** · `aria-hidden="true"` in AUL = **11** · `toast(` in AUL = **11** · `text-xs|text-sm` in AUL = **13** · e2e specs = **8** · `admin` in e2e = **0** · vitest in package.json = **0** · eslint/oxlint/biome configs = **0** · `@admin_router.` decorators = **13** · sibling sizes 529/285/190.

## Scoreboard

**59 adjudicated roster rows — 1 BLOCKER · 26 MAJOR · 26 MINOR · 6 INFO.** **16 killed-claims register entries, all SUSTAINED** (one — C-i3 — ruled by this seat against reader-LC's own caution). **19 distinct missed rows ADMITTED** (12 from reader-DU, 9 from reader-LC, 2 overlaps merged) — five of them MAJOR, including three the entire six-document corpus walked past (the `./forms` Input cure, the tombstone-counting `entry_count`, the third `status` value live in the migration). **10 superlatives sustained** (one — C-S3's completeness claim — killed by its own falsifier). **5 adjudicator corrections against the readers.**

Blocker-weight survivor of record, converged on independently by all three axes and both readers and re-derived by this seat: **FR-AUL-1** — the batch selection outlives the result set it was made against (search/sort refilter on page 1 is an `Object.is`-guarded identity write to `page`, the sole invalidation edge), and the batch confirm never enumerates its targets, in front of a backend **hard** cascade (`flags → visualizations → sessions → users.delete_one`, no soft-delete, no restore). The repo convicts itself: GalleryView.vue:204-205 clears the structurally identical `selectedHashes` on **both** invalidating events.

## Ruled disagreements and corrections (the incredulity seat's own work)

1. **C-i3 (`<SelectValue />` with no placeholder) — the one head-on reader conflict. RULED for reader-DU; the row is KILLED, not residue.** Reader-DU refuted it as statically decidable; reader-LC kept it in the UNPROVEN/SS-13 residue ("not statically decidable"). This seat read the installed reka-ui 2.9.10 directly: `SelectContent.js` renders its closed state as `Teleport to fragment.value` (a detached fragment) wrapped in `SelectProvider`, so every `SelectItem` mounts and registers into `rootContext.optionsSet` while the popup has never opened; `SelectValue.js`'s `selectedLabel` computed resolves the model against that populated set and returns the item's `textContent`, falling to `placeholder` only when no label resolves. The closed trigger renders **"Newest"**. Statically settled; SS-13 keeps no probe for it.
2. **C-B4 (required datetimes 500 the whole page) — verdict harmonized at MAJOR.** Reader-LC confirmed at MAJOR; reader-DU filed mechanism-confirmed/incidence-unproven. Both are right and they compose: the absent defence is **proven on both sides** by this seat's own reads (`api/models/admin.py:18-19` required with no default vs the two defaulted soft fields; `admin.py` projects with bare inclusion flags and `$ifNull`-guards only `status`; the response model is constructed in the handler body → unhandled `ValidationError` → whole-page 500; `janitor.py` reaps via `find({"last_seen_at": {"$lt": …}})`, which **skips** fieldless docs forever). Production **incidence** cannot be settled from the tree (the only production write path sets both; the migration `$setOnInsert`s both) → the incidence question alone rides SS-13/live. Reader-LC's cell clause "the row renders `seen NaNm ago`" is **corrected**: the NaN render is unreachable through this server — the 500 arrives first, unconditionally (kill K4); the client's missing null guard is a defence-absence note, not a reachable render.
3. **Severity contests — five rulings.** (a) **D-06** (`role="toolbar"` without the APG contract): RULED **MINOR** (reader-DU's grade; reader-LC's MAJOR preserved as dissent) — all four controls remain fully reachable; the defect is a false affordance announcement with a free cure (`role="group"`), not an operational loss, and axe has no rule for it. (b) **D-12** (10 px master/detail checkbox misalignment): RULED **MINOR** (reader-DU; reader-LC's MAJOR as dissent) — a plain alignment break with no functional consequence. (c) **L-6** (benign aborts toasted red): RULED **MINOR** (reader-DU; reader-LC's MAJOR as dissent) — the toast lies about an operation that succeeded, but only on a same-path double-click, and the corrected trigger (ruling 4) bounds it. (d) **The four D-axis BLOCKER grades (D-01/D-02/D-03/D-05)**: both readers' downgrades to MAJOR SUSTAINED — forced-colors-only loss, state misreport beside a correct text count, speech-input-only failure, and two themed-surface contrast failures are each genuine MAJORs and none is an unshippable stop; the corpus's sole BLOCKER is FR-AUL-1, where all three axes converged independently. (e) **C-B2** ("un-harnessable by construction", BLOCKER): downgrade to MAJOR SUSTAINED — a props-less SFC mounts under `createTestingPinia` + a fetch double; the binding obstacle is that `web/` has no vitest at all, a one-line devDependency, not an architectural property.
4. **The L-6 trigger — reader-LC's kill of the challenge's stated triggers SUSTAINED at the bytes.** `:270` is `@click="askPrune"`, which only sets `pending` and opens the dialog (`:96-99`) — a double-click fires zero requests; and an aborted `loadPage()` rejects **inside** the composable, swallowed into the unread `error` (`useOffsetPagination.ts:47-48`), never reaching a toast. The genuinely one-click-reachable path — found by both readers independently, confirmed by this seat — is a double-click on a row's Suspend/Unsuspend button (`:393`/`:404`): no dialog, no `:disabled`, no busy flag; the constant path `/api/admin/users/{slug}/status` makes the second call abort the first via `api.ts` `abortable`, and `:186`/`:197` toast the DOMException message red for an operation that succeeded.
5. **The L-2/C-B3 mechanism — reader-LC's kill SUSTAINED; reader-DU corrected.** `useOffsetPagination.ts:45` assigns `items` only inside the `try`, so a failed **re**load leaves the previous page's rows mounted; "every failed load renders as No users found" (endorsed by reader-DU's cell) is false for every load after the first. The general case is **worse** and is admitted as FR-AUL-2's sharpened mechanism (reader-LC's M1): `:39` writes `page` **before** the fetch, so a failed `nextPage()` renders page *n*'s rows under the pager reading *n+1*, `error` unread, `loading` false — every per-row destructive control aimed at a row the pager says is elsewhere.
6. **Adjudicator's corrections register (against this corpus's readers)**: (1) reader-LC's C-i3 caution — over-conservative, corrected by this seat's reka-ui read (ruling 1); (2) reader-DU's bare-`loadPage()` anchors `:183/:194/:205` — one line off on three of four cells (actual `:184/:195/:206`, my read; reader-LC has them right); (3) reader-DU's D-08 endorsement of the "renders as No users found" general mechanism — corrected (ruling 5); (4) reader-LC's C-B4 NaN clause — corrected (ruling 2); (5) both readers' compiled-CSS byte offsets (129477/109614) differ from this seat's re-measurement (**129479/109616**, needle `.shadow-cartoon{`/`.cartoon-card{`) — a needle-spelling variance; the conclusion (shadow-cartoon wins deterministically) is identical and unaffected. Per the ColorSpaceSelector precedent these are fleet evidence, not tree evidence: the readers' disagreements sat exactly where a third instrument was needed.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **F.W0** substrate · **F.W1** tri-package uplift (glass-ui 4→7 ∧ keyframes 4.3→6 ∧ value.js 0.13→4.0, one atomic transaction) · **F.W3/W4** shadow retirement + per-component frontend audit · **F.W5–W8** CRUD/provenance union · **F.W9/W10** · **NO-WAVE-OWNER** where no forming wave owns the surface (SS-3/SS-4 spec authoring consumes it) · **GLASS-OWNED** where the cure lives in the producer (rides the standing BH/BI relay, owner edict). Line anchors are the working tree over `cd26c65`; re-resolve before any cure (D-19). Every routing is a booking, not an act.

### Blocker

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **FR-AUL-1** | D-04 / L-1 / C-B1 | Selection survives a search or sort refilter on page 1 (the default view): `watch(page, clearSelection)` at `:156` is the sole invalidation edge, and `loadPage(1)` from page 1 is an `Object.is`-guarded identity write (`useOffsetPagination.ts:39`). The batch cascade then fires at slugs the operator cannot see, behind a confirm that names **no** target (`:486-489` "the selected users") while the singular path names its exact victim in `font-mono` (`:477-481`) — the more dangerous action carries the weaker confirmation. Backend is a **hard** cascade with no restore (`admin.py:460-474`: flags → visualizations → sessions → `users.delete_one`; contrast the visualizations batch, which soft-deletes). | `:72-78` (both refilters → `loadPage(1)`) · `:106` (askBatch snapshots the stale set) · GalleryView.vue:204-205 (the sibling clears `selectedHashes` on **both** invalidating events — the repo convicts itself) — all re-read by this seat | **F.W4** — clear on `[searchQuery, sortMode]` (or in the fetchFn) **and** enumerate/preview the batch targets in the confirm; the snapshot-at-ask discipline (S-row) is preserved, it froze a set that was already wrong. FR-AUL-22 is the sibling edge the ranked cure does NOT close — booked separately |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **FR-AUL-2** | D-08 / L-2 / C-B3 + LC-miss-M1 | The composable's `error` is produced and never consumed — a failed list load is silent. **Sharpened mechanism (ruling 5)**: `page` is written before the fetch and `items` only on success, so a failed reload renders the **previous page's rows under the new page number** with no signal; "No users found" is only the first-load failure face. Family posture: AdminAuditLog omits `error` identically (my grep → 0) | `useOffsetPagination.ts:31/:39/:45/:47-50/:75` · `:43-54` ten-member destructure omits `error` · all five mutation paths toast, only the read path is silent | **F.W4** — consume `error` with a distinct failure state; re-clamp/restore `page` on failure (or write it only on success) |
| **FR-AUL-3** | L-3 / C-M1 (+ Map rider at INFO) | `adminFetch` keys the abort registry on the full query-bearing path (`api.ts` `coreFetch<T>(path, /* abortKey */ path, …)` — the comment shows the parameter was considered and the wrong value chosen), against ~20 stable-literal sibling call sites — the debounced search cannot cancel itself and **last-to-resolve paints**; per-operation cancellation silently degraded to per-URL for all **13** admin operations (decorator count re-run: 13). The Map-growth rider is INFO (ruling: reader-LC's demotion sustained — bounded by distinct debounced queries, tens of empty controllers, monotonic, immaterial) | `api.ts:250` region (my read) · `useOffsetPagination.ts:44-46` unconditional assignment, no in-flight token | **F.W5** — the R6-8 identity class (one leaf's identity derived from the other's shape), folded by reference to intake R6-8 (ADOPT-AS-FACT, carry → F.W5); cure = stable per-operation abort keys |
| **FR-AUL-4** | D-14 / L-4 / C-M5 | Four of five post-mutation reloads call bare `loadPage()`, which skips the clamp entirely (`if (p != null)` guard); server does not clamp either (`page: ge=1`, no upper bound, echoes the request). Render collision: "No users found" + pager "3 / 2" + "40 total" simultaneously. `performPrune` alone passes `loadPage(1)` — and correctly (see superlatives) | `:173/:184/:195/:206` bare vs `:217` (anchors re-derived by this seat — reader-DU's were one off) · `useOffsetPagination.ts:33/:39` · `admin.py` list_users (my read) | **F.W4** — re-clamp on the no-arg path (`watch(total)` or clamp-in-loadPage), preserving prune's deliberate reset |
| **FR-AUL-5** | D-02 / L-15 / C-m1 / C-m2 | The documented `data-some` indeterminate mechanism does not exist (my grep: exactly **2** hits, comment + binding; the sole CSS file and the empty scoped block carry no such selector), `data-selected` styles nothing on these rows (the only rule is scoped to `.gallery-card`), and the primitive ships indeterminate **whole** (fill + glyph swap + `boolean \| "indeterminate"` contract — all three re-verified against installed dist). At 5-of-20 the gate to a hard cascade renders fully unchecked and clicking it **adds the other 15** | `:282-295` · `:363` · glass-ui dist probes (this seat's regexes) | **F.W4** — pass `someOnPageSelected && !allOnPageSelected ? "indeterminate" : allOnPageSelected`; delete the dead attribute and the false comment; give selected rows a real style |
| **FR-AUL-6** | D-01 + DU-miss (the `./forms` Input) | `outline-none` + `focus:ring-1` annihilates the search field's focus indicator under forced-colors: `.outline-hidden` (the v4 safe variant, forced-colors escape) is **absent from the build** because unused; glass-ui's restore block is a closed nine-selector list and the raw `<input>` carries none; the Checkbox two elements away carries `focus-ring` natively. **The missed cure all three axes walked past**: glass-ui exports `./forms` → `Input` with base class `input-pill` — one of the nine restored selectors — **and `./forms` survives at producer 7.0.0** (this seat's enumeration of both maps). One import cures the forced-colors loss, the `focus:`-vs-`focus-visible:` aggravation, and part of the header height stagger, with zero uplift debt | `:243-249` · built CSS probes (`.outline-hidden` absent, `.outline-none` unconditional) · a11y-overrides forced-colors restore incl. `.input-pill` (regex ✓) · `forms.d.ts:1` | **F.W3** — shadow retirement: the raw input IS a shadow of a shipped, forward-safe primitive; swap to `Input` |
| **FR-AUL-7** | D-05 / C-m8 | Hard-coded Tailwind palette literals fail WCAG 1.4.3 in light mode on the **sole** "suspended" signifier (`bg-red-500/20` + `text-red-400` over light `--card: hsl(36 48% 97%)` ≈ 2.0:1 at the 10 px `text-admin-label` rung) and on the prune control (≈ 1.3:1) — reaching past the repo's own ratified `--viz-amber` axe-contrast carry (style.css:113-122) to literals ~2.5× worse. Both readers recomputed independently; directions and ~3× margins certain, exact composited ratios ride SS-13 (Tailwind v4 ships oklch; the composites assumed sRGB) | `:374-377` · `:267` · light `--card` re-read by this seat · scale.css admin-label re-read | **F.W4** (+SS-13 exact ratios) — route through semantic/repaired tokens; post-uplift the `./metric`/`./chip` homes exist (D-32) |
| **FR-AUL-8** | D-09 / L-19 / C-m5 / C-m4 | The "Loading overlay" is a full content swap (`:277 v-if="loading"` / `:353 v-if="!loading"` exact complements) on every 300 ms typing pause and every mutation, inside a scrolling ancestor — while the select-all header (`v-if="users.length"`, and `items` only assigned on success) and the batch toolbar (`v-if="selected.size"`) keep rendering stale chrome over the blank. `loading` is a boolean, not a counter, so concurrent loads (routine under FR-AUL-3) re-mount the list over stale items mid-request. `Skeleton` ships at the pin and survives 7.0.0 | `:277/:286/:305/:353` · `useOffsetPagination.ts:30/:40/:45/:50` · GalleryView.vue:220 | **F.W4** — dim-in-place/`aria-busy`/skeleton; token or counter for concurrency. Perceptual severity of the flash rides SS-13 |
| **FR-AUL-9** | D-10 + DU-miss/LC-miss-M6 (merged) | Search results are never announced — and **both** live regions are conditionally mounted with their content (the spinner's `role="status"` is itself inside `v-if="loading"`; the pager's `aria-live` span sits inside `<nav v-if="pageCount > 1">`, which unmounts exactly when the result set gets small), the announcement pattern most likely not to fire at all. `{{ total }}` — the one datum answering "did my search work?" — is behind the same gate; "No users found" is in no live region | `:277-280` · `:430-457` · `:422-425` | **F.W4** — one permanently-mounted `sr-only` live region outside both gates announces result counts and loading; discharges both halves |
| **FR-AUL-10** | D-30 | `ToastVariant` is definition-absent at glass-ui 7.0.0 (this seat's `grep -rn ToastVariant src/` over the producer → empty); `useToast.ts:4` imports the type and this file routes **11** static `toast(...)` sites through it (`:170` an unbounded per-error fan-out). With no vitest/no admin e2e, `vue-tsc` is the only catcher — loud, at least | useToast.ts:4/:9 · my count: 11 | **F.W1** — the tri-package uplift owns the break; rewrite the adapter (and fold FR-AUL-45's tone restoration into the same edit) |
| **FR-AUL-11** | C-M2 | `deleteAdminUser` declares `Promise<{ deleted; entries_deleted }>`; the router returns `{"ok": True, "deleted_entries": …}` — **neither declared key is ever emitted** (a transposition). Latent only because `:204` discards the result; `_json` is a bare Response, no transform (my read) | api.ts:594-603 · admin.py delete_user return (my read) | **F.W5** — wrapper types re-derived from the router (R6-8 class); the A.W5.c precedent shows the repair form |
| **FR-AUL-12** | C-M3 | `setAdminUserStatus` declares `Promise<{ slug; status }>`; the router returns `{"ok": True, "status": …}` — `slug` invented, `ok` dropped. The indictment lands: `api.ts:617-624` is a dated contract-repair record for the batch family, and the two singular wrappers twenty lines above were left declaring shapes the router never emits — in the same pass | api.ts:582-592 · admin.py set_user_status return (my read) | **F.W5** — same repair family as FR-AUL-11 |
| **FR-AUL-13** | C-M4 / L-11 / L-23 | `BatchResponse.errors` is never populated (my grep over admin.py: zero `"errors"`; all three branches converge on one return), so `:169-171` is dead and partial batch failure reports **green**; `affected` is `modified_count`, so unsuspending 20 already-active users toasts "Unsuspended 0 user(s)" as a success; `affected` is never compared to `slugs.length`. Duplicated verbatim at GalleryView.vue:189-191 — 2 of 2 batch consumers, written against a contract document rather than the router | admin.py:460-501 (my read) · types.ts BatchResponse optional `errors` | **F.W5** — either the router populates `errors`/`matched_count` or the contract drops them; the client compares `affected` vs `slugs.length` either way |
| **FR-AUL-14** | C-B4 (rescoped, ruling 2) | One user document missing `created_at`/`last_seen_at` 500s the entire admin list (required model fields, bare inclusion projection, handler-body construction, global exception handler), and the janitor's reap query **skips** fieldless docs so they never age out. Absent defence proven on both sides; production **incidence** UNPROVEN (the only production writer sets both) — the incidence question alone rides live | api/models/admin.py:18-21 · admin.py projection (`$ifNull` on `status` only) · janitor.py:204 (all my reads) | **F.W5** — `$ifNull` the projection or default the model fields; the client's null guard rides FR-AUL-17's timeAgo repair. Incidence → SS-13/live query |
| **FR-AUL-15** | L-5 / C-m6 | No unmount teardown under a mount pattern that guarantees unmount (`defineAsyncComponent` + bare `v-if`, no KeepAlive): the raw `searchTimer` outlives the component and fires `loadPage(1)` against a dead scope; `deactivateAdmin()` both unmounts and clears the token, so the orphan timer runs `getAdminToken()!` on null | `:2` (imports only `ref, computed, watch`) · `:41/:73-74` · GalleryView.vue:31/:375 · gallery.ts deactivateAdmin | **F.W4** — `onScopeDispose` clears the timer; abort in-flight on dispose |
| **FR-AUL-16** | L-10 / C-B2 / D-26 | The product's only cascade-delete surface has zero automated coverage of any kind: no vitest, no test script, **8** e2e specs with zero admin references (both counts re-run by this seat), no linter anywhere, `vue-tsc -b` the sole gate. "Un-harnessable by construction" killed (ruling 3e); the harness is unusually cheap here — the slug-bearing aria-labels are a ready-made selector vocabulary | web/package.json · e2e listing · config absence (my checks) | **NO-WAVE-OWNER** — SS-3/SS-4 spec authoring consumes this as the coverage-debt input; extend one axe keystone into the admin tab + a vitest devDependency |
| **FR-AUL-17** | D-15 / L-8 / C-m9 / L-22 | `timeAgo` is a fifth divergent copy in the worse of two dialects (no sub-minute floor → "joined 0m ago" beside a gallery tile reading "just now" on the same route; no cap → "730d ago" on the surface where join dates are cross-referenced; no negative guard; AdminFlaggedPanel's hardened `string \| null` signature never propagated — all five bodies re-read by this seat). The null/NaN arm is killed (K4) | `:223-231` · AdminFlaggedPanel.vue:137-138 · GalleryCard.vue:53-61 | **F.W3** — one shared util, one dialect, capped and guarded |
| **FR-AUL-18** | D-13 + DU-miss (layout shift) | The same hand-rolled batch toolbar exists twice in one view, divergent on **five** positioning decisions (top-2/bottom-2 · z-10/z-20 · inset · shadow-cartoon · count noun — GalleryView row re-read) **plus a sixth**: this one mounts in-flow between header and list, so the first selection pushes the just-clicked row out from under the pointer (sticky engages only on scroll), while the twin anchors bottom | `:304-349` vs GalleryView.vue:309-313 · `:235` flex stack | **F.W3** — one shared local toolbar component; D-17's surviving z-10/z-20 residue folds here |
| **FR-AUL-19** | D-11 | The control-height ladder is bypassed by literals — a 34/32/36 px stagger in one header row; six icon buttons at `h-6`/`h-7` overriding `size="icon"`'s token (with `icon-sm` byte-equivalent and unused); on coarse pointers the floor+`min-*-size` machinery turns each 24 px button into a 44 px box painted with a 14 px glyph, taken out of the truncating slug column. The challenge's own refutation of the naive 2.5.8 finding is correct and credited | `:248/:253/:264` · `:390/:401/:411/:343/:438/:449` · light-dark.css + a11y-overrides (my regex checks) | **F.W4** — `size` props, not literals. The coarse-pointer visual consequence rides SS-13 |
| **FR-AUL-20** | LC-miss-M2 (net-new, admitted) | `entry_count` counts soft-deleted tombstones: the `$lookup` labelled "Live-entry count" carries **no `deleted_at` filter** (my read), while `visualizations` is a soft-delete collection with a 30-day grace; `prune_empty_users` uses the **same** unfiltered lookup, so a user whose entries are all soft-deleted is NOT pruned — contradicting the control's own title and dialog copy — and the janitor **manufactures exactly this population** (it soft-deletes stale users' visualizations without touching the user row; my read of janitor.py:210-215). The whole corpus was silent here | admin.py lookup_stage + prune pipeline · janitor.py (all my reads) | **F.W5** — the join filters `deleted_at: None` (or the contract states tombstone-inclusive counting explicitly). Live magnitude → SS-13/live |
| **FR-AUL-21** | DU-miss (net-new) + L-13-falsifier | A third `status` value exists: `migrate_visualization.py` upserts `"status": "orphan-migrated"` (my read — the L-13 falsifier fired), the projection's `$ifNull` passes it through, `AdminUserInfo.status: str` accepts it, and the template branches only on `=== 'suspended'` — so an orphan-migrated user renders as a **healthy account with a Suspend affordance**, un-badged, on the panel whose purpose is separating real accounts from churn; the zero-entry cohort sits directly under the count-less prune control (FR-AUL-25) | migrate_visualization.py:385-399 · admin.py:279 · `:375/:387` | **F.W5** — the status domain becomes one closed set across writers, model, and wire (`Literal` + migration writer folded in); **F.W4 rider**: the template renders unknown statuses visibly, never as healthy |
| **FR-AUL-22** | LC-miss-M4 (net-new, admitted) | A single-row delete or suspend never clears that row's slug from `selected` (only the batch path and the X button clear — my read of all clear sites), so the toolbar reports a selection with zero checked boxes and a later batch ships the dead slug — `delete_one().deleted_count` contributes 0 and the admin gets "Deleted 0 user(s)" as a green success. **Survives FR-AUL-1's ranked cure** (no filter change is involved) — booked separately so the fix-by-watch does not close the ledger over it | `:172` vs `:179-210` (no `selected` touch) · admin.py batch delete | **F.W4** — mutations remove their victims from `selected` (or selection re-derives against the loaded page) |
| **FR-AUL-23** | DU-miss (net-new) | The search field's border renders in `currentColor` — a near-black hairline in light mode: Tailwind v4 preflight resets `border: 0 solid` with no color (my read of the installed preflight), the `.border` utility carries no color (built-CSS probe), and there is **no** base `border-color: var(--border)` rule anywhere in the stack (built-CSS count = **2**, both element-scoped). The only text input on the panel draws a near-black edge while every surface around it draws the muted token — the classic v3→v4 migration casualty; no axis caught it | `:248` · preflight.css · built-CSS probes (all this seat's) | **F.W4** — cured for free by FR-AUL-6's `Input` swap; independently, a base-layer border-color rule is an F.W0 substrate option |
| **FR-AUL-24** | DU-miss (net-new) | Every destructive action dumps keyboard focus to `<body>`: reka-ui captures the pre-open active element regardless of trigger and refocuses it on close (both lines read by this seat), but `confirmPending` closes the dialog, awaits the mutation, and the success path's `loadPage()` unmounts the entire `v-if="!loading"` list — including the row button focus was restored to. Fires on 100% of confirmed destructive paths (WCAG 2.4.3). The reader's own first hypothesis (no DialogTrigger ⇒ no restore) was refuted and the refutation is preserved | DialogContentImpl.js:53 + DialogContentModal.js:60 (my reads) · `:111-119` + `:173/:184/:195/:206` | **F.W4** — restore focus to a stable landmark (search field / list container) after mutation; the exact fall timing wants one SS-13 witness |
| **FR-AUL-25** | DU-miss (net-new) | The prune confirmation announces an unbounded permanent deletion **without a count** ("every user with zero gallery entries… irrevocable" — no number, no preview), though the magnitude is knowable pre-act (the router computes `empty_slugs` before deleting; my read) and arrives only after, in the toast. Its two dialog neighbours name a victim or a count; the highest-blast-radius control alone carries no quantity — compounded by FR-AUL-20 (the "zero entries" predicate is itself wrong) and FR-AUL-21 (the cohort is the orphan-migrated population) | `:482-484` · admin.py prune (my read) | **F.W5** — a count/dry-run endpoint or two-step confirm; **F.W4** carries the copy |
| **FR-AUL-26** | D-03 | WCAG 2.5.3 Label-in-Name (Level A) fails on the one irrevocable global operation: visible "Prune empty", `aria-label="Prune users with zero entries"`, `title="Remove users with 0 entries"` — three mutually exclusive strings; a speech-input user reading the button cannot activate it. Downgraded from BLOCKER (ruling 3d): pointer/keyboard unaffected | `:268-272` | **F.W4** — the aria-label contains (or becomes) the visible string |
| **FR-AUL-27** | L-7 | 529 LOC carrying ~7 concerns, top of its directory by ~1.9× (529/285/190 — re-measured), and one seam already cloned wrong: AdminAuditLog.vue:15-38 reproduces `:43-69` token-for-token (same ten-member destructure, same `getAdminToken()!`, same offset inversion, same bare `loadPage(1)` — re-read by this seat; reader-LC's anchor correction :15-38 confirmed); the multi-select block is GalleryView retyped; the five handlers differ only in the api call and two strings | whole file · AdminAuditLog.vue:15-38 | **F.W3** — extract `useAdminMultiSelect` + `useAdminAction`; each seam has a second consumer today |

### Minors

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **FR-AUL-28** | L-9 (rescoped) / C-M7-residue | Three hand-assemblies of one confirm structure (this file `:459-523`, GalleryView `:400-437`, AdminFlaggedPanel) — duplication stands at MINOR after both kills: the prescribed `ConfirmDialog` swap is dead (K1) and the "opposite sign" framing is dead (K2). Cure = a **fourier-local** shared component over `./dialog` (which survives 7.0.0), never the retired primitive | **F.W3** + BH relay (rerouted ask: "`./confirm-dialog` was retired at 7.0.0 with three hand-rolled consumers standing") |
| **FR-AUL-29** | L-6 (corrected trigger, ruling 4) | Benign aborts toast red on a double-clicked row Suspend/Unsuspend (no dialog, no disabled, no busy flag; constant path ⇒ second call aborts the first) while `isAbortError` guards a dozen-plus sibling sites (my grep: 17 references outside api.ts) and none of this file's five catches | **F.W4** — guard the catches; largely vanishes with FR-AUL-3's keying fix |
| **FR-AUL-30** | L-12 / C-M6 | Six `getAdminToken()!` assertions; the null path throws `"coreFetch: auth='admin' requires adminToken"` which `:175/:186/:197/:208/:219` render verbatim in an admin-facing toast; the correct guard exists on the identical batch flow at GalleryView.vue:**175-179** (anchor re-derived — the challenge's :173-177 drifted); token localStorage-backed while the mount gate is a non-persisted ref | **F.W4** |
| **FR-AUL-31** | L-13 / C-i2 | `status: string` wider than its domain (now proven three-membered — FR-AUL-21) and the sort seam type-unchecked end to end (reka `AcceptableValue` widens; `sort_map.get(sort, default)` silently falls back) | **F.W5** — one `Literal`/union across wire and client; the sort set closed on both ends |
| **FR-AUL-32** | L-14 + C-M7's surviving guard charge | No in-flight guard on any of the eight mutation controls; `confirmPending` nulls `pending` after the await, so a re-opened dialog's action is destroyed and the second confirm silently no-ops. The microtask double-fire arm is killed (K8); the Presence exit-window arm rides SS-13 | **F.W4** — a busy flag / disabled controls during the await |
| **FR-AUL-33** | D-06 (ruled MINOR; LC dissent MAJOR preserved) | `role="toolbar"` asserted with zero `tabindex`/`@keydown` in 529 lines and no producer primitive — announced affordance contradicts interaction; controls remain reachable | **F.W4** — `role="group"`, or a roving-tabindex composable |
| **FR-AUL-34** | D-07 | `role="list"` owns the non-`listitem` empty-state block (`:422-425` inside the container `:352-426`) — axe `aria-required-children`, manifest only when empty | **F.W4** — hoist as `v-else` sibling |
| **FR-AUL-35** | D-12 (ruled MINOR; LC dissent preserved) | Select-all checkbox 10 px out of the column it governs (`px-1` = 4 px vs 2 px border + `px-3` = 14 px), shifting its label off the slug column too | **F.W4** |
| **FR-AUL-36** | D-16 + LC-miss-M7 | The select-all control's visible label is overwritten by a count already rendered 14 lines below the moment the affordance becomes useful — and the span is not a `<label>` at all (no association, no hit area for a 16 px control); the row's `font-mono` slug is likewise inert | **F.W4** |
| **FR-AUL-37** | D-18 / L-16 (sharpened) | Two competing base box-shadow token families on one element, and the winner is **determined**: built-CSS source order (this seat's probe: `.shadow-cartoon{` @ 129479 > `.cartoon-card{` @ 109616) makes the toolbar render a different shadow family from every other `.cartoon-card` — then jump to `--shadow-cartoon-lg` on hover | **F.W4** — drop the stray utility |
| **FR-AUL-38** | D-19 | `class="h-4 w-4"`/`"h-4 w-4 shrink-0"` on `<Checkbox>` is byte-identical to the primitive's base (base class re-read: `… h-4 w-4 shrink-0 …`), twice — dead bytes that read as a consumer size decision | **F.W4** |
| **FR-AUL-39** | D-20 + DU/LC-miss-M8 (merged) | The empty `<style scoped>` stamps `data-v-*` on every element **and ships a 0-byte stylesheet the chunk still links** (my probe: `AdminUserList-tn0RQdqM.css` = 0 bytes, referenced from the GalleryView chunk) — one extra request carrying nothing; the only finding in this corpus verified against the deployed build | **F.W4** — delete the block |
| **FR-AUL-40** | D-21 + DU-miss (input hygiene) | No clear-search affordance though `X` is imported and used 100 lines away — and the field is `type="text"` with no `autocapitalize/autocorrect/spellcheck/inputmode` for a machine-generated lowercase-slug datum; `type="search"` alone UA-supplies the clear control D-21 proposes hand-building; glass-ui's `Input` exposes the attrs as props | **F.W4** — rides FR-AUL-6's swap |
| **FR-AUL-41** | D-22 | `title` + `aria-label` doubling on six controls with **divergent** strings — mouse and screen-reader users get different labels; `./tooltip` survives 7.0.0, `./icon-tooltip` does not (my enumeration) — reach for `Tooltip` | **F.W4** |
| **FR-AUL-42** | D-23 | Thirteen `text-xs`/`text-sm` literals (count re-run: 13) over the shipped semantic ladder, inconsistently within one component; the row's only quantitative payload sits at the 10 px "fixed sub-control micro (NOT fluid)" rung — the datum the `entries` sort exists to rank | **F.W4** |
| **FR-AUL-43** | D-24 | Hover-lift on rows that are not clickable (no `@click`/role/tabindex); under PRM the transition carve drops `translate` so the 1 px lift applies **discretely**. The challenge's refutation of the naive animate-spin finding is correct (PRM gate re-verified: `animation-duration: 0.01ms !important` global) and credited | **F.W4** |
| **FR-AUL-44** | D-25 + DU-miss (session destruction) | The risk ladder is inverted **and mis-measured**: reversible batch unsuspend gets the destructive modal while the singular suspend path fires from a 24 px icon with no confirmation — and `set_user_status` runs `sessions.delete_many` on suspend (my read), an irreversible act the batch copy states ("their active sessions shall be revoked") and the singular path performing the same act states nowhere | **F.W4** — copy + gating follow the real consequence |
| **FR-AUL-45** | LC-miss-M3 (net-new) | `useToast` flattens glass-ui's five-member `ToastVariant` onto two (`success → default`, `info → default` — my read of VARIANT_MAP), so all six success toasts render neutral while the installed Toaster maps distinct `feedback-tone-success/warning/info` classes (my grep of the dist Toaster) — the producer's semantic tone ladder is installed and unreachable | **F.W1** — fold into the FR-AUL-10 adapter rewrite |
| **FR-AUL-46** | LC-miss-M5 (net-new) | The client never enforces the server's `slugs … max_length=50` (model re-read; `askBatch` guards only non-empty), and FR-AUL-1's leak is the mechanism that exceeds it — three select-all-and-refilter cycles put 60 slugs in the set, the request 422s, and the toast shows the bare statusText (the ApiProblem-fallback clause is reader-derived, adopted; the cap and guard facts are this seat's) | **F.W5** — surface the cap in the shared contract; client-side chunk or block |
| **FR-AUL-47** | DU-miss (net-new) | The batch toolbar offers actions provably inapplicable to the current selection (Suspend/Unsuspend unconditionally enabled while `user.status` is in hand per row): 20 active users + Unsuspend → full destructive modal → green "Unsuspended 0 user(s)". The affordance defect upstream of FR-AUL-13's counting defect | **F.W4** |
| **FR-AUL-48** | DU-miss (net-new) | The 20-row page is 80+ sequential tab stops (4 focusable controls × 20 rows + chrome) with no grid semantics, no roving index, no skip mechanism, no heading or landmark — reaching the pager means traversing the page | **F.W4** — pairs with FR-AUL-33's cure |
| **FR-AUL-49** | C-m3 | `Checkbox` forced through the root barrel: no `./checkbox` subpath at 4.0.0 (80 keys) **or** 7.0.0 (67 keys; root-barrel export at src/index.ts:124 — both re-enumerated by this seat), so the relay ask is live post-uplift, not discharged by it | **GLASS-OWNED** — BH/BI relay: add `./checkbox` |
| **FR-AUL-50** | L-17 | Five `catch (e: any)`; `ApiProblem`'s `detail`/`type`/`status` all discarded (`super(title)` is the whole message); no linter anywhere to notice either | **F.W4** — typed catch helper; linter debt rides FR-AUL-16 |
| **FR-AUL-51** | L-18 / C-m7 | The response's authoritative `page`/`pages` are discarded and the page number re-derived by inverting the offset the composable computed one line earlier — pure ceremony caused by the `fetchFn(limit, offset)` signature | **F.W3** — composable seam fix alongside FR-AUL-52 |
| **FR-AUL-52** | L-20 | `useOffsetPagination` ships a 25%-dead export surface (`error`/`reset`/`pageSize`: zero consumers across both call sites) — fork residue, and `error`'s deadness is FR-AUL-2's mechanism | **F.W3** |
| **FR-AUL-53** | D-31 | Eight `lucide-vue-next` imports rename to `@lucide/vue` at the uplift (producer peer `^1.16.0` re-read) — mechanical | **F.W1** |

### Infos

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **FR-AUL-54** | L-21 | R5-7 applies with two refinements: the native `<div v-for>` loop body contains four component callsites (populated-but-mis-parented leaves read as covered — worse than empty), and there is **no `<li>` in the file**, so a `NATIVE_TEMPLATE_LOOP` cure keyed on element names re-inherits the blind spot | **REFINEMENT FOLD** by reference — identity = intake R5-7/R6-5 (ADOPT-AS-FACT, F.W4 carry); NOT re-booked; the refinement text rides that carry: *count native loops by DIRECTIVE, re-parent component callsites underneath them* |
| **FR-AUL-55** | C-i1 | value.js/keyframes consumption is zero here and correctly zero; value.js 0.13.0's exports map is a single root, so bare specifiers are producer-imposed | Scope fact for **F.W2** — the migration surface does not reach this file; recorded so it is not over-scoped |
| **FR-AUL-56** | C-i4 | `lucide-vue-next` and `reka-ui` in devDependencies while reached by shipped source — fatal under `npm ci --omit=dev` | **F.W0** substrate |
| **FR-AUL-57** | D-27 | `defineAsyncComponent` bare one-arg (re-read: GalleryView.vue:31) — no loading/error component on this component's mount boundary | **F.W4** (host-owned) |
| **FR-AUL-58** | D-28 | Root-size inversion (18 px mobile / 16 px desktop) renders the 10 px admin-label rung smallest on the densest context — app-wide | **F.W0** substrate |
| **FR-AUL-59** | LC-miss-M9 (net-new) | The `entries` sort orders `$match → $lookup → $addFields → $sort → $skip → $limit` — the cross-collection join runs over the **entire** matched user set per debounced request, O(users) not O(pageSize), while the other branch correctly joins after `$limit` (both pipelines read side-by-side by this seat). Also the slow path that makes FR-AUL-3's inversion most likely to fire | **F.W5** |

**Uplift improves (D-32, not a defect)**: at 7.0.0, `./metric` and `./chip` are the right homes for the suspended pill and `entry_count` datum, and `./data-table` **survives** with `responsive`/`cardBreakpoint` + empty/row-action slots — adopting it would retire FR-AUL-8/18/33/34/35/4's render halves wholesale. Whether the panel becomes a DataTable is an SS-3/SS-4 design ruling; recorded as the F.W1→W4 option, not booked as a cure.

## Killed-claims register — 16 entries, all SUSTAINED

1. **C-M7 as a verdict** ("swap in `@mkbabb/glass-ui/confirm-dialog`, available at the pin — no upgrade required"): the premise is true at 4.0.0 (d.ts re-read: `{title, description?, confirmLabel, destructive?, loading?}`), the prescription is structurally unsound — `./confirm-dialog` is **absent from producer 7.0.0's 67-key exports map and absent from its source tree entirely** (this seat's enumeration + `find` — gone, not de-exported). Adopting it manufactures a break row for F.W1 on a file whose four specifiers all survive the cull. Sub-kill: "an unhandled `pending.kind` renders an empty description body" — the five branches at `:477-497` exhaustively cover the union at `:83-87`. Surviving residue = FR-AUL-28. D's S-2 read the uplift correctly; C-M7 read the pin correctly and the uplift backwards.
2. **L-9's framing** ("the same shape as the retired `./pagination` but with the opposite sign — the primitive EXISTS and SHIPS TODAY"): identical sign — same retirement trajectory. Demoted MAJOR→MINOR; the BH relay ask rerouted (see FR-AUL-28).
3. **C-i3** ("whether the closed trigger renders the label is not statically decidable — flag for SS-13"): KILLED by this seat's own reka-ui read (ruling 1). The trigger renders "Newest"; SS-13 keeps no probe.
4. **D-15's null arm / C-B4's NaN arm** ("null `last_seen_at` renders `seen NaNm ago`" / "the client renders that 500 … as NaNm ago"): the two arms are mutually exclusive and the 500 arrives first, unconditionally — `last_seen_at: datetime` is required with no default and the response model is constructed in the handler (my reads). L's own K-3 had this right.
5. **L-13's falsifier** ("Two members, exactly — the server's only writers are set_user_status and batch_users"): FIRED — `migrate_visualization.py` upserts `"status": "orphan-migrated"` (my read), a third value by a third writer the census missed. The finding survives strengthened; the design consequence is net-new FR-AUL-21.
6. **L-2/C-B3's stated mechanism** ("`items` stays `[]`" / "every one of these renders as No users found"): false for every load after the first — `items` is assigned only on success and retains the previous page (my read). The finding survives and **worsens** (FR-AUL-2's sharpened mechanism); reader-DU's endorsement of the general claim corrected.
7. **L-6's stated triggers** ("a double-clicked Prune empty", "a mutation's loadPage() colliding … toasts"): `askPrune` only opens the dialog; an aborted `loadPage()` lands in the unread `error`, never a toast (my reads). Corrected trigger = the unguarded row Suspend/Unsuspend double-click — strictly more reachable; finding survives as FR-AUL-29.
8. **C-M7's microtask double-fire window** ("between the assignment and Vue's async DOM patch the confirm button is still live"): a second click is a task; Vue's flush is a microtask — no human double-click fits inside a microtask checkpoint. The real (different) window — reka-ui `Presence` keeping DialogContent hit-testable through its ~300 ms exit animation — is live-decidable only → UNPROVEN residue.
9. **D-02's grep arithmetic** ("`data-some` returns exactly THREE hits"): this seat's grep returns **2** (comment + binding) — the row's only quantitative claim about the mechanism's deadness, on what was filed as a BLOCKER. C-m1 stated it correctly. Finding unaffected (FR-AUL-5).
10. **D-26's supporting clauses** ("`web/e2e/` (9 specs)"; "D-02, D-06 and D-07 are all axe-detectable"): **8** specs (my `ls`); only D-07 is axe-detectable — axe has no roving-tabindex rule and cannot see an intended-but-unset indeterminate. The row's substance (zero automated gate) is confirmed (FR-AUL-16).
11. **D-17's mechanism** ("the ordering of the sticky toolbar against a portaled Toast, the Dialog scrim, or the dock is UNDEFINED by construction"): the ladder is concrete and two orders of magnitude above (`--z-popover:130/--z-modal:140/--z-toast:160`, re-read), the Dialog/Toaster are portaled out of the subtree, and `cartoon-surface` mints its own stacking context — the ordering is fully determined and correct. Surviving residue (the unmotivated z-10/z-20 twin disagreement) folds into FR-AUL-18.
12. **L-3's Map-growth rider** ("UNBOUNDED in the admin's input space"): mechanism real (`abortable` only `set`s), magnitude hyperbole — bounded by distinct **debounced** queries per session, tens of empty controllers, immaterial. Demoted to an INFO rider inside FR-AUL-3.
13. **C-B2's framing and grade** ("un-harnessable by construction … BLOCKER"): a props-less SFC mounts routinely under a testing pinia + fetch double; the obstacle is the absent vitest, one devDependency away. MAJOR, folded into FR-AUL-16.
14. **C-S3's superlative completeness claim** ("the ARIA surface is complete … I looked for … a `role=\"list\"` with non-`listitem` children — and found none"): its own third clause fired via D-07. The narrower substance (11/11 aria-hidden, slug-bearing labels) survives in the superlatives below.
15. **The four D-axis BLOCKER grades** (D-01/D-02/D-03/D-05): each kept as a finding, rejected as a BLOCKER (ruling 3d). Sole BLOCKER = FR-AUL-1.
16. **Anchor-precision register**: L-7's AdminAuditLog block is `:15-38` (not :14-40); C-M6's GalleryView guard is `:175-179` (not :173-177); reader-DU's bare-`loadPage()` anchors were one line off on three cells (actual `:184/:195/:206`); both readers' built-CSS byte offsets were 2 off this seat's needle (conclusion identical). Downstream consumers re-derive anchors rather than trust (D-19).

## Superlatives — sustained, with provenance

1. **The forward-correct confirm hand-roll (D·S-2)** — the single most valuable row in the corpus, now proven twice over by this seat's own instruments: `./confirm-dialog` present at the pin, absent from 7.0.0's exports **and source**. An axis that reasoned about the uplift rather than the pin, and was right; adopting the "obvious" primitive would have manufactured uplift debt on a file whose four specifiers all survive. The hand-roll's shape is also right: one discriminated `PendingAction` union, one dialog serving four flows, `dialogOpen` closed before the await.
2. **The fork-provenance record (`useOffsetPagination.ts:3-18`; D·S-5 / C·S1 / LC·S-a)** — names source version, path, retirement event, reason, authorizing clause, and pre-refutes the vueuse objection with the precise technical reason. Retirement re-verified by this seat at both 4.0.0 and 7.0.0 maps. The standard every fork should meet — even though the code beneath it carries FR-AUL-2/4/51/52.
3. **Icon-labelling discipline complete (D·S-3 / L·S-6)** — 11/11 `aria-hidden` (my count) and every icon-only control carries a **slug-bearing** accessible name, so a screen-reader user tabbing 80 controls always knows whose row they are on — precisely what FR-AUL-1's operator most needs. The sibling in the same view renders decorative icons with no `aria-hidden`; this file is strictly better. (C-S3's stronger completeness claim is killed — K14; this narrower one survives intact.)
4. **`surface="opaque"` — the new axis, right on the first try (D·S-1)** — and this seat adds the provenance the readers lacked: the cell is the file's **one uncommitted hunk**, part of the working-tree 3.1→4.0 bump sweep; committed HEAD still says `variant=`. Right call twice: the API rename and the design (a destructive confirm must not let the list it is about to mutate show through).
5. **Snapshot-at-ask (L·S-1)** — `askBatch` freezes `Array.from(selected.value)` into `pending`; nothing re-reads the set before confirm; GalleryView uses the identical idiom (`:167`, re-read). The correct discipline for a destructive confirm — and the irony is preserved: it froze a set that FR-AUL-1 had already made wrong.
6. **Severity-derived confirm variant (D·S-6 / LC·S-g)** — every irrevocable path red, the two reversible batch paths not; red keeps meaning something.
7. **`performPrune` alone resets to page 1 (L·S-4)** — the one mutation whose blast radius is the whole page space, and the only one that resets: simultaneously the strongest evidence FOR FR-AUL-4 (the author knew the rule) and a correct local decision a mechanical refactor must preserve.
8. **Copy-on-write set updates without exception (L·S-5)** — the explicit, refactor-proof idiom at all three mutation sites; not required under Vue 3.5's collection proxy, chosen anyway.
9. **The contract-repair record (`api.ts:617-624`; C·S4)** — names defect, disagreement, backend evidence with line numbers, ratifying authority, and follow-on migration; it is what makes FR-AUL-11/12 provable — and it convicts its own pass, which is exactly what a provenance record is for.
10. **Meta-superlatives, both readers** — reader-DU explicitly refuted two easy conformance findings (24 px targets; ungated animate-spin) with correct arithmetic (both re-verified here), and reader-LC killed four of its own challenge corpus's cells at the bytes (the ConfirmDialog cure, the z-ladder mechanism, the Map hyperbole, the "items stays []" mechanism) while stating the render-path negative explicitly rather than leaving silence. Auditors who decline defects the tree refuses are worth more than ones who bank thirty.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. **D-05 exact contrast ratios** — direction and ~3× margins proven by two independent recomputations (≈2.0:1 badge, ≈1.3:1 prune); exact composited values want a contrast checker (Tailwind v4 ships oklch; the composites assumed sRGB mixing). Light mode, `.cartoon-card` backdrop.
2. **D-11(c) coarse-pointer geometry** — the 80→140 px action-cluster growth and the slug-column theft are statically derived (floor + `min-*-size` re-verified); the visual consequence at a narrow admin viewport wants one live frame.
3. **D-29 hit-halo clearance** — the ~4 px select-all/first-row halo margin under the 44 px `touch-hit-area` `::before` is derived arithmetic; measure the two hit rects on a touch device.
4. **The Presence exit-window double-fire** (C-M7's surviving arm) — whether DialogContent remains hit-testable during its ~300 ms exit and can re-enter `confirmPending` with `pending` non-null is live-decidable only. Do not bank; FR-AUL-32's busy-flag cure closes it regardless.
5. **D-09 perceptual severity** — the swap is structural and certain; how badly the collapse thrashes scroll inside GalleryView's scroll container on each 300 ms pause is a live observation.
6. **C-B4 production incidence** — the absent defence is proven; whether any live user document lacks the datetimes only a production query settles.
7. **FR-AUL-20 live magnitude** — how many users currently show inflated `entry_count` or evade prune depends on the live soft-delete backlog inside the 30-day grace.
8. **FR-AUL-24 focus-fall witness** — the restore-target unmount is structural; one live frame confirming activeElement lands on `<body>` after a confirmed delete closes the row's evidence.
9. **The confirm-triplication home** — local shared component vs a producer replacement primitive is an owner ruling, not an audit verdict; routed to the BH/BI relay beside the still-live `./checkbox` gap (absent at both 4.0.0 and 7.0.0) and the tone-ladder note (FR-AUL-45).

*(C-i3 is deliberately absent from this list — killed at K3, statically settled.)*

## Corpus corrections (recorded against the census, not the tree)

- `lane-frontend.md:102`'s descriptor "Admin user table + role dialogs" is wrong on both nouns (no `<table>`; no role dialogs — the dialog is destructive-confirm; the status axis is suspension). Both challenges flagged it; adopted.
- The census shadow table's C-M7 row ("a fourth shadow … available at the pinned 4.0.0 — no upgrade required") is corrected by K1: the primitive dies at 7.0.0; the census was right to leave it unjoined.
- Intake identities folded by reference, never re-booked: **R5-7/R6-5** (FR-AUL-54 rides the F.W4 carry), **R6-8** (FR-AUL-3/11/12 are instances; carry → F.W5), **R3-7b/X-3** (13 admin ops, 0/45 security gate — corroborated at this consumer, unchanged).

## Verdict

**DEFECTIVE — ADJUDICATED.** 59 roster rows banked: **1 BLOCKER** (the stale-selection hard-cascade with an unenumerated confirm — the corpus's one convergent, three-axis result) · **26 MAJOR · 26 MINOR · 6 INFO**; 16 kills sustained (the loudest: the ConfirmDialog cure that would have shipped uplift debt under a green conscience, and a "not statically decidable" that was); 19 net-new missed rows admitted, five of them MAJOR, three of those untouched by all six prior documents (the `./forms` Input cure, the tombstone-counting `entry_count`+prune pair, the orphan-migrated third status). The type gate is green over all of it — every routing above is agglomeration input for the forming X·F specs (F.W0 ×2 · F.W1 ×4 · F.W3 ×8 · F.W4 ×30 · F.W5 ×12 · NO-WAVE-OWNER ×1 · GLASS-OWNED ×1 · refinement/scope folds ×2), ADJUDICATED, not VERIFIED; no wave is stamped by this record.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3.
