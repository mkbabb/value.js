# AdminNamesPanel — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · seat model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: incredulity/apotheosis (M-23/L-18) — a FRESH adjudicator over two hostile re-readers, not a transcriber of either.
**Date**: 2026-08-03 · **HEAD**: `c654824e` · branch `tranche-u` · **Subject**: `demo/palettes/browser/admin/AdminNamesPanel.vue` (152 lines) + its composable/atom/transport chain.
**Corpus (r2 governs)**: `audit/components/AdminNamesPanel/challenge-{D-design,C-implementation,L-library}.md` (18 + 16 + 19 accusations; all three axes Opus 5 [1m], declared) + probes `probe-C-{mount,queue}.test.ts`, `probe-C-error-state.png`.
**Re-readers adjudicated**: R-DU (design-and-usage) · R-LC (library-and-code). Both hostile, both partially wrong, both attacked here.
**Method**: every verdict-level disagreement between the re-readers was re-derived by this seat against the tree with its own instruments; the linchpin kill (C-1) was re-walked link by link in product source; the one BLOCKER escalation (C-5/L-10) was re-witnessed from the committed capture by this seat's own eyes; two reader-agreed kills (D-2 target-size, L-17) were spot-re-derived rather than trusted.
**Adjudicator's own instruments (all static + committed captures; ZERO browser probes — probe parsimony, and the seat law forbids them)**: full reads of AdminNamesPanel.vue (152), useColorNameQueue.ts (whole), AdminListItem.vue (whole), AdminListSkeleton.vue (whole), AdminPane.vue (whole), EmptyState.vue (whole); the C-1 chain — api/src/modules/admin/service/colors.ts:86-114, api/src/modules/color/repository/proposedName.ts:69-80, demo/platform/transport/client.ts:110-158 (+ :74 `assertApiAttemptAllowed()` inside `fetchWithRateLimitRetry`), useColorNameQueue.ts:68-79; availability.ts:50-196 (latch, cooldown 30_000, both constant messages); admin-colors.ts (whole); color-names.ts:1-45; api color model.ts:1-40 + service/proposals.ts:20-90 (the V·W45 item-3 attribution law); router/index.ts:25-40 + repo grep `beforeEach|beforeResolve|meta.admin` → 0; usePaletteWiring.ts:140-175 (watch 2 immediate + length-guard; watch 4 transition-only); viewSchema.ts:197-205; installed glass-ui 7.0.0 `dist/components/tabs/styles/segmented.css` (whole rule read: `font-weight: 500` five declarations before `font: inherit`; base `padding: 0.25rem 0.625rem` widened only at ≥640px; `.segmented-indicator` scale transition PRM-wrapped, `.segmented-indicator--js` translate/width/height transition NOT); index.html:1-12 (`dir="ltr"` + the U-F58 header comment); VISUAL-CONSTITUTION.md byte spot-checks :75/:76/:89/:101/:114/:154/:218 (all verbatim); e2e a11y-battery.ts target-size leg (`r.width < 24 || r.height < 24`, 2.5.8 AA); e2e fixtures admin-populated.ts:86-96 + color-approve.spec.ts:19; AdminFlaggedPanel.vue:60-68 + sibling inject/onMounted census; useColorNameResolution.ts:45-75; greps: `AdminListItem` consumers (1), `title=` in subject (0), `catch (e: any)` in composable (5), `contributor|proposerSlug` across demo+api. Committed captures read directly: `visual/shots/safari-desktop-light/admin-names.png` (this seat's own eyes: Login button + furnished console + "· QUEUE CLEAR ·"; the Pending pill overhanging its track; equal-weight companion; serif tab labels; five zero-statements) and `probe-C-error-state.png`.

## Scoreboard

**Corpus of 53 accusations → 60 adjudicated roster rows after identity-merges and 20 missed-finding promotions: 3 BLOCKER · 27 MAJOR · 24 MINOR · 6 INFO (of which 2 are identity/rescope records — D-14 already-booked, L-19 sibling witness — leaving 58 live defect rows) · 13 KILLED (register below) · 2 severity rulings (one escalation, one demotion) · 10 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

The gestalt from challenge-D survives adjudication intact and is the one sentence a fresh session should carry: **this was designed as a list of two strings, not as a review instrument** — enormous craft on the read path (min-w-0 chain, error≠empty split), nothing on the write path, and the ratified Admin·Names composition (full width, one row anatomy, pagination, dangerous confirmation, provenance) exists on paper and not in the tree.

## Ruled disagreements (the adjudication's actual work)

| id | R-DU | R-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-1** (double-click → duplicate keyed rows, BLOCKER) | CONFIRMED BLOCKER | KILLED (state limb unreachable) | **KILL SUSTAINED — R-LC wins.** Duplicate-row limb dead; ungated-button residual folds into D-1/C-4. | Re-walked all four links myself: (1) `approveColor` throws `NotFoundError("…already processed")` when `transitionStatus` misses (service/colors.ts:93-98); (2) `transitionStatus` is `updateOne({_id, status: from})`, atomic — of two concurrent POSTs exactly one matches `status:"proposed"` (proposedName.ts:69-80); (3) `adminRequest` throws `ApiProblem.from(res)` on any `!res.ok` (client.ts:152-153); (4) the append at useColorNameQueue.ts:75 sits INSIDE the `try`, after the await — the losing call rejects before it. `rejectColor`/`deleteColor` guarded identically (colors.ts:102-113, :80-83). The probe manufactured the result: probe-C-queue.test.ts:21-24 mocks `approveColorName` to ALWAYS resolve — it never models the 404 the real chain throws. R-DU carried the axis's mount-probe ("rows rendered for ONE unique id = 2"), which proves only that Vue renders two elements when HANDED two identical items. C's own BLOCKER ranking ("the only finding that corrupts state") does not stand. What survives: a double-click double-POSTs, the second 404s and dies in `console.warn` — that is D-1/C-4's silence plus a missing `:loading` (glass Button ships it), not a state-corruption class. |
| **C-5/L-10** severity | MAJOR | BLOCKER | **BLOCKER — R-LC's escalation adopted, with a stated cap.** | Witnessed by this seat from the committed capture (safari-desktop-light/admin-names.png): the dock shows **Login** (unauthenticated) beside a fully-furnished moderation console asserting "Pending · 0 / Approved · 0 / · QUEUE CLEAR · / No pending proposals." — a false moderation fact stated to any visitor. The guard is dead code, verified: router/index.ts:31-35 declares `meta: { admin: true }` on all five admin routes; repo grep for `beforeEach|beforeResolve|meta.admin` → zero hits; usePaletteWiring.ts watch(4) fires only on a transition to false, never on cold boot. **The cap (recorded so the row is not over-read)**: no data leaks and no capability is live — all five composable sites early-return without a token (:36, :52, :69, :82, :93), so the console is inert. The BLOCKER is the false-state assertion + a declared-but-unenforced authorization boundary one `beforeEach` away from being load-bearing. Identity guard: the cure is ALREADY RULED — the AdminUsersPanel apotheosis's AdminGate ("`getAdminToken()` a non-null contract inside the console; the unauthorized state stops being a state the panel models") owns it; the Names five early-returns delete under the same gate, never a second mechanism. |
| **D-14** (CSS literals not LTR-isolated) | KILLED (antecedent never holds) | CONFIRMED MINOR | **RESCOPED — R-DU's kill sustained in the component-defect sense; the fact survives as the already-booked book's input.** | My own reads: VISUAL-CONSTITUTION.md:154 conditions the rule on "inside RTL prose" — no RTL prose exists; index.html:11 hardcodes `dir="ltr"` under the U-F58 header comment recording the single-locale decision and the BOOKED per-component follow-ups (`docs/tranches/U/audit/w-a11y/rtl-logical-property-audit.md`). R-LC's source fact (zero bidi isolation repo-wide) is true and is exactly what that book exists to hold. Disposition: ALREADY-BOOKED rider, INFO at most, NOT carried into this component's cure ledger. R-LC's confirmation did not engage the antecedent; that is the error. |
| **L-17** (segmented strip mobile compaction) | KILLED ("no compaction" false) | CONFIRMED MINOR | **RESCOPED — both survive in the same weaker form; R-DU's kill of the strong claim verified.** | My own segmented.css read: base `.segmented-tab { padding: 0.25rem 0.625rem; font-size: 0.8125rem }` IS the compact rung, widened only inside `@media (min-width: 640px)`; the track trim follows the same ladder. So every consumer, including this panel, gets producer compaction below 640px — "two of three consumers never get it" is false. What survives: PaneSegmentedControl.vue carries a THIRD, tighter density step as a per-instance `:deep()` while its own comment claims root-level — a producer `density` axis question. GLASS-OWNED relay row, not a Names row. |
| **C-7** (Retry no-op under the latch) | UNPROVEN (didn't read availability.ts) | CONFIRMED MAJOR | **CONFIRMED — R-LC's derivation re-verified by this seat.** | client.ts:74 calls `assertApiAttemptAllowed()` inside `fetchWithRateLimitRetry` before ANY fetch; availability.ts:188-195 throws `DevMisconfigError` (misconfigured) or `ApiUnavailableError` (inside `RETRY_COOLDOWN_MS = 30_000`) — both messages constants, so the re-set `queueLoadError` is byte-identical to what is on screen. The `unavailable` branch is production-reachable off any single network failure. The microtask sub-claim ("skeleton never paints") stays UNPROVEN-NEEDS-LIVE. |
| **D-3 mechanism** (font-display → indicator miss) | KILLED | KILLED (with a control-case caveat) | **KILL SUSTAINED, R-LC's caveat adopted and sharpened by this seat's own eyes.** | The kill's logic holds: PaneSegmentedControl passes the identical `font-display` to the identical primitive and the axis itself measured that instance at zero delta — same class, same webfont, so the font cannot be the trigger and "delete the consumer override" would not move the miss. CAVEAT (mine, from the desktop capture): at 1440 the dock renders NO SegmentedTabs on this route (tag menu, Login, @mbabb only), so the axis's "Dock instance on the same page" 0-delta control cannot have come from this desktop composition — the control case is real but its cell is mislabeled. The sharpest surviving consumer delta is `w-full` (the only stretched instance in the tree, R-DU missed-row 9). Geometry limb CONFIRMED (I see the overhang in the committed capture); mechanism UNATTRIBUTED; relay to glass-ui with the axis's sound instruction: measure the BUTTON, not the track, and re-measure on `document.fonts.ready` + ResizeObserver. |
| **R-LC missed #3** (loader concurrency, filed MAJOR) | — | MAJOR | **CONFIRMED but DEMOTED → MINOR (adjudicator's ruling).** | The structure is real (no in-flight token, no AbortController; both loaders re-callable from Retry at any moment; completion-order wins). But the window is narrow three ways: usePaletteWiring watch(2) carries a length-guard (`if (…length === 0)`) on view entry, so re-entry does not re-fire; the latch throws synchronously for most rapid retries (no second request at all); and corruption requires overlapping in-flight loads WITH differing payloads. A stale-overwrite is possible, a routine hazard it is not. |
| **D-2 severity** (confirmation limb) | BLOCKER | MAJOR | **BLOCKER sustained — R-DU wins.** | Three grounds, all verified: the canon binds Admin to "dangerous confirmation" by name (VISUAL-CONSTITUTION.md:218, byte-checked); the sibling in the same directory ships the full Dialog confirm (AdminUsersPanel.vue:157-181 — the in-repo control case proving no producer dependency blocks the cure); and the mis-tap is not merely unconfirmed but UNOBSERVABLE after the fact (`status:"rejected"` is a real union member, the panel has two tabs, and the missed one-way-door row shows approve has no non-destructive remedy either). Irreversible + invisible-after + cure-in-reach = BLOCKER. The target-size limb of D-2 is killed separately (register below) and does not dilute this. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Wave owners per the CARRY-CUT-LEDGER X-map: X-W1 re-gate · X-W4 semantic controls · X-W5 one-route-one-scene · X-W7 palette domain/CRUD ownership (absorbs the ruled MT-AU1 console re-home identity) · X-W8 graph/config subtraction · X-W9 parser+library · X-W10 canonical design+motion. GLASS-OWNED rows leave by the standing BH relay (owner edict; INBOX I-20) under the masking-fallback ban. Line anchors are HEAD `c654824e`; re-resolve before executing any cure.

### BLOCKER

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **D-1 / C-4** (+ C-1 residual folded) | Approve/Reject/Delete failures are invisible — full error idiom for reads, none for writes; buttons ungated (`:disabled`/`:loading` = 0) so a double-click double-POSTs and the losing 404 dies in `console.warn`. | useColorNameQueue.ts:76-78/:87-89/:99-101 vs :24-25 + panel :30-41/:80-91; Retry re-runs LOADS only (AdminPane.vue:56-57). Cure premise corrected: glass-ui 7 ships the full `./toast` family + Button `loading`, and the demo owns `ActionFeedback` — the channel is unadopted, not nonexistent; canon still forbids the transient carrying the only truth (VISUAL-CONSTITUTION.md:101), so the durable per-row state is the cure's spine, producer primitives its voice. | **X-W7** |
| **C-5 / L-10** | Unauthenticated cold boot to `#/admin/names` renders the furnished console asserting "· QUEUE CLEAR ·" — a false moderation fact — over a dead `meta:{admin:true}` guard nothing reads; `if (!token) return` masks `unauthorized` as `empty` at five sites. Capped: capability inert, no data on the wire. | Ruled row above; capture witnessed by this seat. | **X-W7** — the ruled AdminGate identity (MT-AU1); the five early-returns DELETE under it, never re-check |
| **D-2** (confirmation limb) | Reject and Delete are irreversible, unconfirmed single-click commands with no undo and no post-fact observability, on the one Admin surface the canon names for dangerous confirmation — while the sibling panel ships the exact Dialog confirm. | Ruled row above; AdminNamesPanel.vue:66/:111 → immediate mutations; AdminUsersPanel.vue:157-181 control case. | **X-W7** (folds the one-way-door missed row: `rejectColorName` exists at admin-colors.ts:35-39 as the un-wired demote path) |

### MAJOR

| id | claim | proof anchor | disposition |
|---|---|---|---|
| **D-3** (geometry limb) | The selected SegmentedTabs indicator physically escapes its own track — confirmed qualitatively from two committed captures by this seat's own eyes; exact deltas UNPROVEN-NEEDS-LIVE; mechanism unattributed (font kill sustained; `w-full` the sharpest suspect). | Ruled row above; VISUAL-CONSTITUTION.md:89 (0.5px law, byte-checked). | **GLASS-OWNED → BH relay** (measure the button, not the track) + X-W1 capture obligation |
| **D-4** | The Palettes companion is retained at exactly equal weight on a route the canon orders to full width, companion 0. One-word cure: `right: null`. | viewSchema.ts:197-205 (read by this seat: `right: "palettes"`, `rightLabel: "Palettes"` — all three canon-deleted things in one 9-line object); capture witnessed. | **X-W5** |
| **D-1.3** | The retired mobile pane selector still ships on this route (`leftLabel`/`rightLabel` feed it). | Same viewSchema rows; VISUAL-CONSTITUTION.md:32 §3 law 6, :89 §4.2. | **X-W5** (dies in the same viewSchema edit as D-4) |
| **D-5** | "· QUEUE CLEAR ·" renders over a queue that is merely FILTERED — no-data and no-results conflated; no §5.1 status region announces the changed count. | AdminPane.vue:46-47 binds the filtered lists; useColorNameQueue.ts:27-33; panel :42/:92 are the only zero-length branches, both true-empty register. | **X-W7** |
| **D-6 / C-3 / L-11** | Tab labels count with no load/error guard — "Pending · 0" over skeletons, retained-length over an error plate — violating the A-3 law the same route's parent codifies in a comment 90 lines away; and the counted lists are the FILTERED page, so the number is "matches in the loaded page" presented as queue depth. | Panel :19-20; AdminPane.vue:118-128 (read whole by this seat; note the parent's own guard covers loading but NOT the error state — the badge lies there too, a sharpening neither axis filed). | **X-W7** (one count, one home: `Ref<number\|null>` from the envelope's `total`) |
| **D-7** (+ ADJ-1 precondition) | Actor, provenance and slot are fetched and discarded — the row renders name + literal only; the Approved tab records neither who nor when. Cure is BLOCKED by ADJ-1 until the DTO is re-derived against the wire. | color-names.ts:12-20 vs panel renders; VISUAL-CONSTITUTION.md:218 byte-checked. | **X-W7**, sequenced after ADJ-1 |
| **ADJ-1** (adjudicator's own — no axis and neither reader filed it) | **The demo DTO and the wire disagree on the provenance field's NAME.** Server DTOs carry `proposerSlug: string \| null` everywhere (admin ProposedNameDTO — service/colors.ts:21-29; public queries.ts:24/:54; propose response — proposals.ts:81); the demo type declares `contributor?: string` (color-names.ts:17), which NO endpoint ever returns; `proposeColorName` still POSTs a `contributor` body field the server explicitly ignores (proposals.ts:30-33 — "attribution is server-derived… NEVER a caller-supplied body field", V·W45 item 3). Blast radius beyond this panel: e2e fixtures type-check the phantom (`contributor: "azure-fox-01"`, admin-populated.ts:87-88 — a green fixture perpetuating the drift), and the dock's ColorInput tooltip `v-if="currentColorMeta.contributor"` (ColorInput.vue:48-51) can NEVER render from server data — a dead shipped feature. D-7's cure ("render contributor") would render `undefined`. | This seat's greps + reads listed in the header. | **X-W7** — re-derive `ProposedColorName` from the wire (`proposerSlug`, `createdAt`, `approvedAt: string \| null`, drop `contributor` + the dead body field), fixtures and ColorInput in the same edit |
| **D-8** | Control labels render in Fraunces via consumer `font-display` — mechanism now PROVEN statically: `.segmented-tab` declares `font: inherit` AFTER `font-weight: 500` (this seat's own read), so the consumer's family AND the shorthand's weight-reset both land; 12 admin-cluster sites. | Panel :17/:37/:87; segmented.css; VISUAL-CONSTITUTION.md:75 byte-checked. | **X-W10** (type-matrix sweep) + the producer dead-weight half → **BH relay** (M-DU10 below) |
| **D-9** | Four-sided box per row + swatch ring where the binding inventory grants one adjacent separator, none terminal. | AdminListItem.vue:11; panel :43/:46/:93/:96; OPTICAL-BENCH :83 verbatim per R-DU's byte-check. | **X-W7** (dies in the row-anatomy rebuild; canon authority X-W10) |
| **D-11 / C-9** (+ R-LC's six-site family folded) | Loading is unannounced at the wrapper (aria-label on a role-less div = ARIA `generic`) and announced three times by nested `role="status"` skeleton atoms; the same construct at SIX sites across the cluster with AdminTagsPanel:51-56 the sole correct instance — the cure is one atom edit (AdminListSkeleton drops per-instance `role="status"`) + wrapper roles, not a per-file patch. | Panel :26/:76; AdminListSkeleton.vue:10-12 (read by this seat); sibling census (this seat's grep). | **X-W4**; Skeleton-atom touch relays to glass-ui if the primitive moves |
| **D-12 / C-8** | An unnamed `role="group"` of toggle buttons with roving tabindex controlling zero panels, while the producer ships all three affordances (`semantics="tabs"`, `controls`, `ariaLabel`) unused. | Panel :14-22 passes none; SegmentedTabs.vue.d.ts quoted by both readers, consistent with the corpus. | **X-W4** |
| **C-2** (+ D-misc destructive-recipe folded) | All five `variant=` props are inert DOM attributes (glass 7 Button has no `variant` axis); Approve and Reject render as the same capsule; the quiet-destructive register lives as a five-class call-site string ×4+ sites. | Button.vue.d.ts:6-19; panel :37/:55/:61/:87/:106; `vue-tsc` exit 0 re-run by R-LC. | Subject's 5 sites → **X-W7** rebuild in producer axes (`emphasis`/`tone`/`iconOnly`/`loading`); fleet (66) + producer `inheritAttrs` allowlist = the ALREADY-RULED AdminUsersPanel L-3/MT-BTN1/MT-UI1 identities — ONE identity each, never re-booked here |
| **C-6 / L-9** | Raw transport `e.message` forwarded verbatim to user copy — the 484-char dev runbook fills the card; `string \| null` cannot express audience/category/retryability; and R-LC's missed #4 names the type-level root: five `catch (e: any)` discard the discriminated hierarchy (`DevMisconfigError`/`ApiUnavailableError`/`ApiProblem`) the transport ALREADY ships — the cure is `catch (e: unknown)` + `instanceof` dispatch, not a new union. | availability.ts:119-130 (read by this seat); useColorNameQueue.ts:43-45/:60-62; EmptyState.vue:23-25. | **X-W7** |
| **C-7** | Retry issues zero requests under the latch and re-renders byte-identical text into an already-present alert — no announcement, no observable transition. | Ruled row above. | **X-W7** (latch-honest retry: read `apiAvailability`, cooldown label or guaranteed observable transition) |
| **L-7 / D-10** | Pagination envelope discarded; silent truncation at 50; the atom sits wired in two sibling panels; search filters the loaded page only. D-10's unbounded-DOM mechanism is dead (register below) — the finding is the TRUNCATION. | admin-colors.ts:21-27/:41-50 (defaults read by this seat); useColorNameQueue.ts:40-41/:56-57. | **X-W7**, sequenced after M-LC1 (ownership convention) |
| **L-1** | The G-DEMO barrel-seam law is a dead letter for this file — globs aim at the deleted `demo/@` tree; `no-restricted-imports = None` on the subject (R-LC re-ran it). | eslint.config.js:232-239/:274-277; `ls demo/@` → absent. | IDENTITY: already ruled into the AdminUsersPanel wave's substrate item (globs re-pointed at the live tree) — **X-W8** umbrella; no second booking |
| **L-5** | `demo/ui/` is a 19-module pure re-export shim; this file uses the correct subpath idiom and the shim in adjacent lines (:124 vs :125); 6×/13× closure cost re-measured by R-LC. | Panel imports; demo/ui/button/index.ts. | Subject line dies in **X-W7** rebuild; the 19-module deletion = the standing **MT-UI1** identity under **X-W8** |
| **L-6** | The async quad-state ladder is transcribed twice in this file (32/44 lines identical) and six times across the cluster — the missing atom is the ladder, not the row. | R-LC's `comm -12` re-run; line census matches `wc -l`. | **X-W7**, SEQUENCED AFTER M-LC1 — grafting `AsyncList` onto a cluster with three load-ownership conventions is the contrivance the KISS edict bans |
| **L-8** | One global `searchQuery` ref serves five domains, v-model-bound in three panes — a moderation query leaks into the personal library. | usePalettePorts.ts:54 + the three bindings (R-LC census; binding at AdminPane.vue:13 read by this seat). | **X-W7** (per-corpus refs; the leak sequence itself stays needs-live) |
| **L-12** | Demo's declared value.js surface ≠ published exports (3 phantoms, 2 omissions, "CLOSED 8-key set" comment false by one). | R-LC static re-derivation; node experiment unrepeated here. | **IDENTITY GUARD — this is the ColorSpaceSelector adjudication's L-14, fact-identical, ALREADY FOLDed to W-HYGIENE config parity with a born-RED generator command. ONE identity; recorded here only so no Names wave re-books it.** |
| **C-14** | The gates are vacuous for this component: no component test anywhere in test/ or demo/test/, @vue/test-utils installed unused, and the hard typecheck passes with 66 inert `variant=` (exit 0 re-run by R-LC). "First mounts in history" limb killed (register below). | C's seven named green-staying mutations stand un-refuted. | **X-W1** (CI re-gate) + the G-17 mounts-precondition identity from the AdminUsersPanel record (vitest has no vue plugin — the specced unit gates were unbuildable; do not re-discover) |
| **M-DU1** (R-DU missed) | The judged NAME is typeset in the control/label register (`text-small` sans) while the sibling panel received the exact population-sweep cure (display voice, `text-subheading`) under a named wave that skipped this file — verified by this seat at AdminFlaggedPanel.vue:62-67. | Panel :50/:100; VISUAL-CONSTITUTION.md:72 vs :75. | **X-W7** (one class swap in the rebuild; canon X-W10) |
| **M-DU2** (R-DU missed) | The canon's "one review-row anatomy" exists as a component with exactly ONE consumer (this file) while its loading shadow is shared by four panels — AdminFlaggedPanel hand-transcribes the identical anatomy. | This seat's grep: `AdminListItem` consumers = AdminNamesPanel only (+ a comment). | **X-W7** |
| **M-DU3** (R-DU missed) | The queue has no list semantics (`role="list"`/listitem ×0 across all five panels) and no result-count status region, while both idioms ship in the same feature directory (PaletteCardGrid; PaginationBar.vue:17 `aria-live="polite"` count). | R-DU's greps, spot-consistent with this seat's reads. | **X-W4** |
| **M-DU4** (R-DU missed) | The color specimen is hand-rolled (`:style` backgroundColor on a bordered div) on the one surface whose job is judging color, while WatercolorDot is the repo's specimen face in 14 files including the EmptyState this panel renders — verified by this seat at EmptyState.vue:45-47. | Panel :46/:96. | **X-W7** (face) + **X-W9** (validity flag — see C-15/L-14) |
| **M-LC1** (R-LC missed) | Three contradictory data-ownership conventions coexist in one nine-file directory — three self-loading container panels (inject + onMounted, all paginated), one pure props/emits panel (this one, boot-loaded, unpaginated), one hybrid — and the split, not a missing atom, is the mechanism behind L-6 and L-7. Verified by this seat: Flagged/Tags/Audit `inject(ADMIN_PORT_KEY)` + `onMounted(load…)`; Names pure; Users props + inject at :234. | Sibling census in header. | **X-W7** — the convention is RULED FIRST (the MT-AU1 record already converges Users to inject-only; extend the ruling to the cluster), then L-6/L-7 land |
| **M-LC2** (R-LC missed) | The panel truncates the very string the moderator judges — `truncate`, no `title` (grep 0), no expand — against the repo's own 100-char fixture stressor; all three axes recorded truncation as a designed success. The min-w-0 chain cures LAYOUT; nobody priced what it costs a review instrument. | Panel :50; admin-populated.ts:90-95 (read by this seat). | **X-W7** |

### MINOR

| id | claim | disposition |
|---|---|---|
| **C-10** | A failed refresh hides rows still held in memory (error plate precedes the list; catch never clears the array) while the strip keeps counting them. Error used as a replacement for data, not a banner over it. | **X-W7** |
| **D-13 / C-11 / L-4** | `cssColorOpaque` — required, never read, prop-drilled OUT of an injection built to avoid prop-drilling. (C-11's "only reason AdminPane injects" parenthetical killed — register below.) | **X-W7** (two-line delete) |
| **D-15** | Spacing misses PR-35 at all three rungs (12 vs 24; 10 constant vs 16/12 responsive; 6 vs 8). | **X-W10** |
| **D-16 / C-16** | Both owned state changes un-transitioned (hard v-if cuts); the producer's `--js` indicator transitions `width`/`height` (statically confirmed by this seat). Reduced-motion limb SETTLED in the tree's favor by motion-quarantine §1's two-guard measurement (animations.css:184-193 + glass a11y-overrides.css:1 rewrite the transition-property list under `reduce`) — **no quarantine-FALSE row is carried into this record**; duration figure needs-live. Already booked as F034-b. | **X-W10**; producer PRM-gating inconsistency → BH relay (M-DU10) |
| **D-1.5** | The same zero stated FIVE times in one card (Badge, two tab labels, eyebrow, message) — counted by this seat in the capture; the axis undercounted by one. | **X-W7** (dies with the count/empty redesign) |
| **C-15 / L-14 / D-swatch** (merged) | An unparseable or unsupported user-submitted literal paints nothing yet stays approvable — indistinguishable from transparent; zero value.js imports in the file. Validation is GATED: project memory records R1 = live `parseCssColor("oklch()")` crash; the cure lands only behind the parser spec. | Face → **X-W7**; validation → **X-W9** (blocked on the V·π/R1 gate; `CSS.supports` is the interim honest mark if X-W7 lands first) |
| **L-2** | `palettes/browser/index.ts` — phantom public surface, zero importers (20 symbols, R-LC's arithmetic correction adopted). | **X-W8** (the MT-AU1 record already strips its five Admin exports) |
| **L-3a** | Two admin homes with a foreign feature (`browser/`) interposed. | IDENTITY: the MT-AU1 console re-home already rules this (browser/admin ceases to exist) — **X-W7**, no second booking |
| **L-3b** | `ProposedColorName` homed at its minority consumer (16/24 refs in palettes/), imported back up-and-across. | **X-W7**, same edit as ADJ-1 |
| **L-13** | HEAD's build emits 25 duplicate `_2` type declarations into the published `css` subpath under an unchanged 4.0.0 — artifact/repo divergence. | **X-W9** |
| **L-15 / D-17 / C-13** | `ref<string>` over a closed two-member union + catch-all `v-else` renders Approved for any invalid value, on a moderation surface. Free cure. | **X-W7** |
| **L-16 / C-12** | The `:options` literal re-allocates per render — the only such site; comparison limb corrected (register below: one sibling uses `computed`, the other a plain array — reference stability, not `computed`, is the invariant). | **X-W7** |
| **L-17** (as rescoped) | A third density step for the segmented strip lives per-instance as `:deep()` in one consumer; a producer `density` axis is the question. | **GLASS-OWNED → BH relay** |
| **L-18** | `EmptyState` is a two-species, owner-ruled design primitive (12 consumers, 4 areas) homed outside the design system; glass 7 ships no empty-state export. | **GLASS-OWNED → BH relay** (upstream as `@mkbabb/glass-ui/empty-state`) |
| **M-DU5** (R-DU missed) | The multi-line machine readout is centre-set (`text-center` container, ragged-both Fira) — machine truth needs a left rail; independent of shortening; reaches all 12 EmptyState consumers. Verified in EmptyState.vue:16/:23 by this seat. | **X-W7** edit riding the L-18 relay |
| **M-DU6** (R-DU missed) | The hard-coded headlines assert a network category the panel cannot know — a 401, a 500 and a CORS misconfig all render "unreachable". | **X-W7** (with C-6's typed dispatch) |
| **M-DU7** (R-DU missed) | Approve is a one-way door: the Approved tab's only action is destructive Delete (filters BOTH arrays); the demote path exists in the API and is unwired. | **X-W7** (folded into D-2's confirmation/undo design) |
| **M-DU8** (R-DU missed) | The skeleton promises a 56px action cluster; Pending delivers 66px, Approved 30px — every load ends in a lateral jump with no transition to absorb it. Verified: AdminListSkeleton.vue:18 `h-7 w-14`. | **X-W7** |
| **M-DU9** (R-DU missed) | This is the tree's only `w-full`-stretched SegmentedTabs (~228px per tab for ~110px of ink) — what makes the producer defect maximally visible, and the sharpest surviving D-3 suspect. | **X-W7** (composition); evidence rider on the D-3 relay |
| **M-DU10** (R-DU missed, producer) | Producer-internal, verified by this seat: `.segmented-tab`'s `font-weight: 500` is dead code (reset by the later `font: inherit` shorthand — the exact hook that lets any consumer rewrite a design-system control), and `.segmented-indicator`'s scale transition is PRM-wrapped while `.segmented-indicator--js`'s translate/width/height transition is not — producer gating inconsistency masked today only by app-level guards. No M-xx quarantine row covers either point; nothing FALSE is carried. | **GLASS-OWNED → BH relay** |
| **M-LC3** (R-LC missed, demoted) | Loaders have no in-flight guard/abort/sequencing — last-writer-wins on overlap; first `finally` clears the shared flag mid-flight. Demoted per the ruled row (narrow window; wiring length-guard; latch synchronicity). | **X-W7** |
| **M-LC4** (R-LC missed) | Five `catch (e: any)` — a deliberate strict-mode opt-out erasing the transport's typed hierarchy at the first touch. | **X-W7** (the C-6 cure's type-level half) |
| **M-LC6** (R-LC missed) | Two e2e flow specs mock `status: "pending"` — a value neither the demo union nor the server's `PROPOSED_NAME_STATUSES` can produce (verified: color-approve.spec.ts:19 vs color-names.ts:16 and api model.ts:9-10); untyped literals hide the drift. | **X-W1** (typed fixtures; note admin-populated.ts is typed and still perpetuates ADJ-1's phantom field — type the mocks against the RE-DERIVED DTO) |
| **M-LC7** (R-LC missed) | The optimistic approve fabricates a DTO the server never returns (`{...item, status:"approved"}`, no `approvedAt`, and — ADJ-1 — no provenance field the wire would recognize); `approveColorName: Promise<void>` makes the honest echo impossible without a second round trip. | **X-W7** (endpoint returns the updated DTO, or re-fetch on settle; precondition for D-7's Approved-tab provenance) |

### INFO

| id | claim | disposition |
|---|---|---|
| **D-18** | The tab is not addressable — resets on every sub-view switch; cannot deep-link Approved. | **X-W5** (routes own the state; also gives L-7's pagination a URL home) |
| **M-DU11** (R-DU missed) | Every measured a11y-battery violation on this route belongs to the companion the canon deletes (all four `smallTapTargets` = PaletteSlugBar) — `right: null` discharges four canon rows and the route's only battery findings in one word. | **X-W5** rider on D-4 |
| **M-DU12** (R-DU missed) | `AdminListItem`'s doc comment instructs the next author to commit a type-matrix violation (`text-caption` secondary) — its only consumer correctly uses `text-mono-small` per F-9; the comment predates the correction. | **X-W7** (comment corrected in the rebuild) |
| **D-misc-mobile-order** | Mobile source order inverted vs the decided composition (query before selector). | **X-W5** |
| **L-19** | The state layer types itself on a view component (`useAdminUsers.ts:14`) — the Names panel is CLEAN of it (credit recorded). | IDENTITY: already ruled deleted in the MT-AU1 wiring convergence; recorded here only as the sibling witness |
| **D-14** (as rescoped) | Zero bidi isolation repo-wide — TRUE at source, but the canon rule's antecedent ("inside RTL prose") never holds in the shipped single-locale product. | **ALREADY-BOOKED** → `docs/tranches/U/audit/w-a11y/rtl-logical-property-audit.md`; not a Names cure row |

## Killed-claims register (refutations preserved so nothing is re-discovered)

1. **C-1's duplicate-row/state-corruption limb (the corpus's #1-ranked BLOCKER) — KILLED.** Four-link chain re-derived by this seat (ruled row above): atomic `updateOne({_id, status: from})` → `NotFoundError` on the loser → `ApiProblem` thrown at `!res.ok` → the append inside the `try` never runs. The probe manufactured the result (probe-C-queue.test.ts:21-24 mocks the API to always resolve). Residual (ungated double-POST, silent 404) folded into D-1/C-4. R-DU's re-confirmation of the BLOCKER is overruled.
2. **D-10's unbounded-DOM mechanism — KILLED (inverted).** `getApprovedColorNamesAdmin(token, limit = 50, offset = 0)` — no-arg call sites take the defaults; the server returns ≤50. "No limit" at the call site means the opposite of what D-10 infers. The true finding is L-7's silent truncation; the two axes contradict each other on the same three lines and L-7 wins.
3. **D-2's target-size limb ("44px floor", "every row adds two smallTapTargets") — KILLED three ways.** No canon source for 44 (PROPORTION-AUDIT §5 law 7 says the opposite; 44 is WCAG 2.5.5 AAA); the repo's enforced floor is 24px (a11y-battery.ts `r.width < 24 || r.height < 24`, verified by this seat) which 28×30 clears; and REPORT.json's four rows are PaletteSlugBar's, so the arithmetic ("adds two more") counts nothing. The confirmation limb of D-2 is untouched by this kill.
4. **D-1/C-4's "no feedback primitive exists" premise — KILLED.** glass-ui 7 exports the complete `./toast` family (incl. `ToastAction`, the undo affordance) with zero demo consumers, and the demo owns `ActionFeedback`. The absence is un-adoption, which changes the cure (producer-first, edict 4); the core finding survives.
5. **D-3's mechanism + cure ("font-display's late metrics; delete the override") — KILLED, with this seat's caveat.** Identical class + font on the identical primitive measured landing exactly; deleting `font-display` is D-8's cure for D-8's reasons and would not move D-3 a pixel. Caveat recorded: at 1440 the dock renders no strip on this route (this seat's capture read), so the axis's "same page" control cell is mislabeled — the kill stands on class-parity logic; `w-full` is the surviving suspect; relay unattributed.
6. **C-9/C-7's "aria-live = 0 / a screen-reader user gets literally nothing" — KILLED.** The probe counted the attribute, not live semantics: EmptyState carries `role="alert"` (implicit assertive) and `role="status"` (implicit polite) — verified :17/:28 — and the skeleton atom is itself `role="status"`. The probe's own JSON refutes it internally (`alertLens: [37, 484]` beside `liveRegions: 0`). The true inverse defect is promoted: the error state announces a 484-char developer runbook ASSERTIVELY; the settled-rows branch alone announces nothing; a repeat Retry re-renders identical bytes in an existing alert so no re-announcement fires (C-7's real a11y half).
7. **L-17's "two of three consumers get no compaction" — KILLED.** The producer's BASE rung is the compact one (this seat's own segmented.css read); ≥640px widens. Survives only as the per-instance third density step (relay).
8. **D-14 as a component defect — KILLED/RESCOPED** (ruled row above): the canon antecedent never holds; `dir="ltr"` + U-F58 decision + booked follow-up audit. R-LC's MINOR confirmation overruled; the source fact rides the existing book.
9. **C-12's comparison limb ("both siblings use a computed") — KILLED** (R-LC): MixSourceSelector uses a plain setup-scope array; only PaneSegmentedControl uses `computed`. Reference stability is the invariant; the finding survives in L-16's correct form.
10. **C-11's parenthetical ("the only reason AdminPane injects CSS_COLOR_KEY") — KILLED.** Verified by this seat: the inject is setup-scope (AdminPane.vue:94) and the Users panel genuinely consumes it (:32 `:css-color`). The dead-prop finding is untouched.
11. **L-2's "22 named symbols" — corrected to 20** (R-LC recount). Immaterial to the zero-importers finding.
12. **C-14's "first component mounts in this repo's history" — KILLED** (R-LC): eight `mount(` probes pre-exist under audit/components/. The load-bearing halves (no tests in test/ or demo/test/; none for this component anywhere) stand.
13. **D-16's reduced-motion hedge ("unproven") — SETTLED, not unproven**: motion-quarantine §1's two-guard measurement strikes `width`/`height` from the transition set under `reduce`. No quarantine-FALSE row is carried by this record; the residual producer-gating inconsistency is M-DU10's relay, explicitly scoped as NOT an app-level PRM failure.

## Superlatives (provenance preserved)

1. **The `min-w-0` chain** — correctly diagnosed, cured at every link, honestly commented (mechanism + measured consequence, panel :2-12), and FENCED (`admin-populated.spec.ts` `toBeInViewport({ratio:1})` against a seeded 100-char stressor; `overflowX 0` in all four matrices). Both readers; the comment and fence read by this seat. The best engineering in the file.
2. **EmptyState's two-species split** — error≠empty separated at the ARIA layer (`role="alert"` vs `role="status"`), Q6 eyebrow correctly confined to TRUE EMPTY, the `--ink-muted` contrast cure documented with its measured failure (3.84:1). Read whole by this seat. It is also what makes the C-9 kill possible.
3. **Per-item interpolated accessible names on every icon action** (`Approve color name ${item.name}` …), each glyph `aria-hidden` — and the e2e selects by that exact name, so the a11y affordance and the test surface are one artifact. `namelessButtons: 0` all four matrices.
4. **F-8's quiet destructive** — red on hover AND `focus-visible`, so keyboard users get the danger signal; the decision is excellent even though its expression as a call-site string is C-2's defect.
5. **F-9's readout register** — the CSS literal set as `text-mono-small` Fira with the reason in the comment; the exact standard against which M-DU1's identity-rung miss is measured.
6. **`@mkbabb/glass-ui/tabs` at :124** — the correct subpath idiom demonstrated one line above the incorrect one, which is why L-5 is legible at all (R-LC).
7. **`AdminListItem`** — a textbook 24-line slot atom, zero data knowledge; the tragedy (M-DU2) is adoption, not design.
8. **CORPUS**: challenge-D's §2 state enumeration separates never-designed from never-captured and pays for its hypotheses with §6 reproductions; challenge-C §2 and challenge-L §4 publish negative results and correctly DISOWN the four smallTapTargets that would have inflated their own case — the single act of restraint that let D-2's target-size limb die cleanly; challenge-D's canon citations are byte-accurate (nine spot-checked by R-DU, six re-checked independently by this seat — every one verbatim). Where the corpus is wrong it is wrong in INFERENCE, never in transcription.

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

These are evidence obligations for the X-W1 instrument pass (chromium + the Safari cells, labeled per cell), not findings:

1. **D-3 exact deltas** (left +0.14 / right +1.07 / top +8.06 / bottom +15.95; indicator bottom − track bottom +11.95 at 1440×900) — overhang confirmed from committed captures; the pixels and the button-vs-track sizing mechanism need a live CSSOM. Ships with the BH relay.
2. **The populated row has never been photographed** — no capture in the entire matrix renders a single queue row, so the action cluster, hover/focus destructive register (D-1.6 dark wash), row rhythm and M-DU8's skeleton→row jump are all unwitnessed in both schemes. One seeded-fixture capture pair (light/dark) discharges five hypotheses at once.
3. **forced-colors / zoom-200 / keyboard-focus / RTL arms for this route** — verified hole: each shots/ arm holds 5 routes, `admin-names` absent from every one.
4. **D-16's `--tab-indicator-duration`** (the 440ms figure) — token unresolvable statically.
5. **C-7's microtask sub-claim** (skeleton never paints on a latched retry) — scheduler-interleaving dependent.
6. **C-6/L-9's rendered box metrics** (443 chars / 255px / 445px / 693px) — string + path confirmed at source; the geometry is not.
7. **D-4's millimetric geometry** (512/512/774) — equal weight is capture-certain; the numbers are the axis's live cell.
8. **D-1.4's acreage figure** (~225 display px) — mechanism source-visible (`h-full` card + top-packed plate), figure needs-live.
9. **L-8's rendered cross-pane leak sequence** — structurally certain from the single ref; the user-visible replay unwitnessed.
10. **D-1.6 dark-mode destructive wash at rest** — folded into #2's capture pair.

## Closing verdict

**DEFECTIVE — ADJUDICATED (not verified).** Three BLOCKERs stand — invisible write failures on the only commands the surface exists for (D-1/C-4), an unauthenticated furnished console asserting a false moderation fact over a dead route guard (C-5/L-10, escalated), and unconfirmed irreversible destruction with no undo and no post-fact observability (D-2, sustained) — while the corpus's own top-ranked BLOCKER (C-1's duplicate-row corruption) is KILLED by a four-link chain the probe never modeled, which is the whole argument for this seat's existence. 60 rows adjudicated (58 live defect rows): the write-path/domain mass to X-W7 (under the ruled MT-AU1 console-re-home and AdminGate identities, with M-LC1's ownership ruling and ADJ-1's wire-DTO re-derivation as stated preconditions), semantics to X-W4, route composition to X-W5, graph/config to X-W8 (two identity-guarded no-re-books), parser-gated validity to X-W9, canon/motion to X-W10, gate truth to X-W1; five producer rows leave by the standing glass-ui BH relay; zero rows NO-WAVE-OWNER. Dated evidence untouched; this file is new; the challenger corpus and both re-reader returns remain immutable beside it per E-1/E-3.

— end of apotheosis.
