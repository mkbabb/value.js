claude-fable-5

# fr-PaperArticleWindow — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/paper/PaperArticleWindow.vue` (212 lines, read whole this session) + its two consumed seams: `@mkbabb/latex-paper@0.2.1` (theme.css / PaperSection / PaperSectionBlocks / useVirtualSectionWindow, at the **shipped dist**, which is what runs) and `@mkbabb/glass-ui@4.0.0` (`.deferred-section`, tokens, a11y-overrides), plus `PaperView.vue` (sole caller), `useScrollNavigation.ts`, `figureDimensions.ts`, `style.css`, `router/index.ts`, `vite.config.ts`, and `paper/fourier_paper.tex`.
**Substrate disclosure**: fourier HEAD `cd26c65`; the subject file and `figureDimensions.ts` are **CLEAN at HEAD** (my `git status`); three paper siblings dirty (`MobileFloatingToc.vue`, `PaperView.vue`, `search/PaperSearchDropdown.vue`) — every anchor in this record re-resolved at the working tree this session. Producer repos both local: glass-ui at 8.0.0 (tags v7.0.0/v8.0.0 both read by this seat), latex-paper producer tree present (`/Users/mkbabb/Programming/latex-paper`); the **installed latex-paper ships no composable src** — only `src/vue/theme.css` + dist — so all virtualiser anchors in this record are dist anchors.
**Corpus adjudicated**: three challenge axes (D 23 defects/7 superlatives · L 18/4 · C 23/4, all `claude-opus-5[1m]`) already challenged by **two hostile re-readers** — **reader-DU** (design-and-usage: 24 confirmation cells · 10 kills · 7 missed finds · 9 superlatives · 6 unproven) and **reader-LC** (library-and-code: 28 confirmation cells · 8 kills · 4 missed finds · 9 superlatives · 6 unproven). Both returns arrived through the orchestration channel; the three immutable challenge files sit beside this record.
**Method (incredulity)**: every reader kill re-derived before sustaining (one single-reader kill — L-12 — was ratified only after this seat re-derived the containing-block relation itself); every admitted missed find re-proven at the bytes; every reader-vs-reader grade conflict ruled with fresh instruments; both rasters read directly by this seat. Zero browser probes (probe parsimony); product source read-only; **sole write = this file**.
**This seat's own instruments (all static + node, this session)**: PaperArticleWindow.vue whole (212 — all cited anchors land; note :193 carries a live `@apply`, so the `@reference` at :136 is load-bearing here); theme.css :1-24/:58-80/:198-218/:443-466/:515-523/:596-644 + `grep -c 'hsl(var(--'` → **42** + the var-free border-longhand sweep → exactly :22/:70/:71 (:70-71 = `.theorem-block`, un-paired, its rail survives via `--_section-color`); glass-ui 4.0.0 dist: utilities.css **exists, 19-line @import root** (the L-15 decider), utilities/base.css:460-485 (deferred-section + the `contentvisibilityautostatechange` prose), a11y-overrides.css:1-30 (the PRM allow-list, `transform` excluded), tokens color-radius/dark-arm (all complete colours), theme/radius.css (`--radius: 0.625rem`, `--radius-pill: 9999px`), scheme-motion (`--duration-fast`, `--ease-out-expo`), typography utilities (`cm-serif` :65, `fourier-f` :77), btn.css (`btn-interactive` :184, `.btn-pill` co-compose), glass/surfaces.css:60-64 (individual-transform doctrine verbatim); **producer at BOTH tags**: `git show v7.0.0:…color-radius.css` → `--card: hsl(30 85% 96%)`/`--border: var(--neutral-4)`/`--primary: hsl(24 10% 10%)` (:72/:95/:98) and `git show v8.0.0:…` → same shapes (:73/:96/:152), v7 base-misc.css carries `.deferred-section`; latex-paper dist/vue.js :688-702 (`buildSectionLayout` — **margin-blind border-box sums**, the K-16 decider), :789-790 (`SESSION_HEIGHT_CACHE` module scope), :822 (`getHeight` chain), :868-894 (`syncMeasuredHeight` no estimate guard / `measureSection` unconditional `offsetHeight`), :895-906 (warm ±2/+3), :941-942 (cache re-seed), :960-967 (teardown spares the cache), :968-971 (`visibleItems` slice), :1643 (**`_resolveDynamicComponent2(() => $setup.effectiveSlots.figure({…}))` — NOT `renderSlot`**, the K-13 decider), :1651-1661 (figcaption sibling, conditioned on `caption || number` only), :1760 (the callout slot rides the SAME fresh-arrow `:is` — seat extension), :1646 (`ctx.assetBase` consumed by the library's fallback img); chunk-5VAEDP55.js:620-627 (parser basenames + `.pdf`→`.png`); chunk-A7GY23HR.js paper-ref grep → **5 emitters, all `<a class="paper-ref" data-ref=…>` href-less** (the K-4 decider); producer clean.ts `replaceAccents` braced+unbraced (the K-5 decider); PaperSection.vue producer (h2/h3 flatten, `--_section-color` bind, single `<section>` root); `sourceLevel` grep → written at flatten :110, typed at output.ts:94, **read nowhere**; **SFC compiled with the tree's own @vue/compiler-sfc** → `PaperSection` `_: 2 /* DYNAMIC */` + `1032 /* PROPS, DYNAMIC_SLOTS */`, inner slot objects `_: 1 /* STABLE */`, fragment `128 /* KEYED_FRAGMENT */`, `ref_for: true` fresh arrow, **`resolveFigure(` × 7**; @vue/runtime-core 3.5.38 :4763-4765 (`patchFlag & 1024 → true`), :4781-4785 (`$stable` else-branch), :3288 (`slots._ === 1 ? 64 : -2`), :5457-5460 (**`patch()`** `patchFlag === -2 → optimized = false` — the correct site; :6665 is unmount), :1803-1818 (string/Ref-only old-ref teardown; function refs called unconditionally), :5544; **both rasters read at the pixels**: f21 (4276×1765) = three matplotlib panels on solid white, cyan traces, three ~200px insets (Fourier/Cauchy engravings + an NES R.O.B. product photo) well under 5% of plate; f20 (2600×2920) = panel (a) large Fourier engraving ≈¼ plate + three white-field line panels; on-disk 1.48MB/2.85MB; useScrollNavigation.ts whole head + :110-200 (`computeAbsoluteTop` prefers real geometry; `teleportTo` correction loop MAX 10/STABLE 2/6px — the K-6 decider); PaperView.vue :12/:22/:45/:59/:63/:77-97/:145-192/:330-335/:520-585/:610-616 (incl. `writeProgress` at ~:160 and the `NATIVE_SCROLL_TIMELINE || prm` early-return — the K-14 decider; `.overlay-btn:active` at :614-616; `usePaperSearch` live at :111); style.css :1-30/:88-100/:105-145 (PRM block = tabpanel only; the **unlayered** D.W4.d focus rule :136-143 with `border-radius: inherit`; `--viz-amber` overrides in complete-colour form; `find src -name '*.css'` → 1); figureDimensions.ts whole (**26 keys**, `TRANSCODED_FIGURES = new Set(Object.keys(…))` :52); censuses re-run: assets 28 png/26 avif/26 webp, unpaired = `fourier.png` + `maintainer-avatar.png` only; `\includegraphics` = 26; brace-anchored headings **11 + 51 + 31 = 93** (subsubsection 0; the readers' shared "35 subsections" cell is the un-anchored count — seat correction SC-2); **93 slugs slugified by this seat, `uniq -d` → EMPTY**; e2e: `MAX_MOUNTED_SECTIONS = 18` (:3), figure/callout/picture grep → only two `test.describe.configure` substring hits; print sweep: `media print|@page|beforeprint` → **0** in web/src, **0** files in glass-ui dist, no PDF in web/public; d.ts: PaperSectionBlocks `__VLS_Slots` → **0**, PaperSection → 2; peers `{katex:^0.16, vite:^6||^7}` vs installed **0.17.0 / 8.0.16**; **own WCAG instrument (node)**: border/card **1.904** light / **2.081** dark, tint/card **1.011**, dark fg/card **11.786** — reproducing reader-DU's and the D axis's figures to the third decimal and confirming LC's 11.79 correction; GalleryCardModal.vue :181-190/:246-256 (the twin + its false chassis comment); banked registry re-reads: fr-GalleryCardModal **GCM-23** (:69) + **K-5** (:121, names THIS component's bare router-link), fr-EquationModeToggle **FR-EMT-2** (:32, containment-clips-producer-outset class), intake **R5-7** (:125, ADOPT-AS-FACT).

## Scoreboard

**43 adjudicated roster rows — 1 BLOCKER · 15 MAJOR · 14 MINOR · 13 INFO**, of which **3 are identity-folds to banked rows** (GCM-23/K-5 · R5-7 · the census F.W1 lucide P0) and **8 are new rows admitted from the readers' missed finds** (all re-proven at the bytes by this seat; 2 further missed finds enter as cure-integrity kills). **17 killed-claims register entries** (15 reader kills sustained after re-derivation — including one single-reader kill ratified only after independent derivation — plus 2 cure-kills admitted from reader-LC's missed list) **+ 3 seat corrections against the readers' own cells**. **10 superlatives.** Reader coverage gap: corpus rows **D-i-4, D-i-5, C-i-1** adjudicated by neither reader — seat-checked and admitted/folded (R-9).

The component's centre of gravity: **a small, unusually careful authored surface composed onto two broken seams** — the latex-paper theme's token contract is dead against glass-ui's complete-colour canon (42 IACVT drops, the whole paper colour layer), and the virtualiser measures the very element `content-visibility` renders unmeasurable, laundering a 1200px fiction into a session-lifetime cache while a 24-line comment asserts the opposite. The B-2 family demotes to MAJOR on this seat's own read of the compensating navigation loop; what stays un-compensated (spacers, `scrollHeight`, thumb, progress) is real. The third axis of damage is compositional churn: the in-`v-for` slot declarations force a compiler-proven unmount/remount of every `<picture>` and callout on every scroll-frame patch.

Status law: dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing is M-25 agglomeration input; nothing here stamps a wave.

## Ruled disagreements and seat corrections (this seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **B-2/L-2/C-B-1 severity** (axes D+C filed BLOCKER) | MAJOR | MAJOR | **MAJOR — both readers' demotion sustained by fresh derivation.** | My whole read of useScrollNavigation.ts: `computeAbsoluteTop` prefers `getBoundingClientRect` whenever `document.getElementById(id)` resolves; `teleportTo` scrolls behind an overlay and corrects to 6px stability (MAX 10 frames). Navigation is compensated; the un-compensated residue (topSpacerPx/bottomSpacerPx, `scrollHeight`, scrollbar thumb, reading progress — both the JS fallback AND the native `scroll()` timeline, which reads the same falsified geometry) carries the MAJOR. |
| R-2 | **C-M-2/L-8 grade** (hasModernVariants) | MAJOR | MINOR | **MAJOR; LC's MINOR preserved as dissent.** | The invariant holds at HEAD by my own enumeration — but it is enforced by nothing (no generator, no manifest, no CI, zero e2e), drift is silent in BOTH directions, and the failure mode is the component's own documented no-fallback 404 trap compounded by PAW-5's deleted placeholder. A latent break with a permanent-broken-image outcome and zero guard sits above the MINOR line. |
| R-3 | **D-M-6/L-11 grade** (inter-section rhythm dead) | MAJOR | MINOR | **MAJOR.** | Document-wide: NO section in a 93-section treatise receives its authored 2rem separation — the primary reading surface's vertical rhythm, not a local nit. Compiled render confirms one `<PaperSection>` per wrapper; `contain`-implied containment blocks collapse both ways. LC dissent noted; LC's cure-kill (K-16) governs the repair. |
| R-4 | **D-M-5 measure arithmetic** (axis 688px · DU 644px · LC 684px) | 644 | 684 | **640px — seat's own figure, correcting all three.** | PaperView :332 `max-w-5xl px-2 sm:px-6` → 1024 − 48 = 976 inner; grid (:561-564, **inside `@media (min-width:1024px)`**) `220px minmax(0,48rem)` gap 2rem → column = min(768, 976−252) = **724** (the 48rem is never realised — DU right, LC wrong); `.paper-article` `border: 2px` + `box-sizing: border-box` + `padding: 2rem 2.5rem` (:523-534) → 724 − 4 − 80 = **640** (LC right about the border, DU omitted it). Direction and severity of D-M-5 unmoved; every "unused measure" cell re-derives from 640. |
| R-5 | **D-m-5 grade** (footer spacer) | INFO (magnitude killed) | MINOR | **INFO; LC's MINOR preserved as dissent.** | Seat re-run of DU's arithmetic: at 390×844/SAI-34, clearance = 67.5 (spacer) + 20 (article bottom pad <640px) = 87.5 vs overlay ≈90 → **~2.5px** deficit; zero at ≥640px (pad 32). The structural misses (no `env(safe-area-inset-bottom)` term, no coupling to `.paper-bottom-overlay`, `vh` where the repo uses `dvh` at style.css:21, and LC's double-count sharpening) are all proven from source and booked. |
| R-6 | **L-17 grade** (SESSION_HEIGHT_CACHE) | INFO | MINOR | **MINOR.** | The `onUnmounted` block (dist :960-967) clears every other resource and **deliberately spares the cache** — an authored teardown exclusion, not an omission; it is the vehicle of PAW-2's session durability and the key-space is bare slugs (PAW-38). |
| R-7 | **L-18 grade** (no srcset/sizes) | MINOR | INFO | **MINOR.** | On-disk facts, my `ls -la`: f20 **2.85MB**, f21 **1.48MB** — print rasters served into a 640px column (R-4) on every visit, format-negotiated but never width-negotiated. Transfer magnitude → SS-13; the defect does not need it. |
| R-8 | **L-12 kill** (sticky containing-block mechanism) — LC killed; DU never adjudicated | — | KILLED | **KILL RATIFIED by independent derivation (K-11).** | My PaperSection.vue read: the sticky headers are children of `<section class="paper-section">`, the nearest block container — THE constraint box; the interposed `.paper-window-section` is one level too far out, so L-12's own falsifier (`display: contents` would cure it) is false. The two-tier stack actually fails upstream: `flattenPaperSections` emits chapters and subsections as flat PEERS, so a chapter's `<section>` closes before its subsections begin. LC's surviving half (per-wrapper stacking contexts make the z-index 10/9 ordering inert) rides PAW-30. |
| R-9 | **Reader coverage of the corpus** (D-i-4, D-i-5, C-i-1 in neither return) | absent | absent | **Seat-checked all three.** | D-i-4 (empty state unreachable — build-time virtual module) admitted INFO as epistemic record; D-i-5 (the substrate's `contentvisibilityautostatechange` hook unwired) folded INTO PAW-2's cure clause, not a separate row; C-i-1 (lucide-vue-next rename, `:8`) admitted INFO as an identity-fold to the census F.W1 P0 (35-site migration) — never re-booked. |

**Seat corrections against the readers**: **SC-1** — the measure (R-4: DU forgot the 4px border; LC used the unrealised 768 column). **SC-2** — both readers' "35 subsections" cell: the brace-anchored line-start census is **31** (11+51+31 = 93, the total both correctly used elsewhere); "35" is the un-anchored count catching starred/optional-arg forms. The identical wrong cell in two independent returns is fleet evidence of a shared method default (un-anchored grep), recorded per the ColorSpaceSelector DISSENT-1 precedent. **SC-3** — reader-DU's virtualiser proof cells cite "producer src at the installed 0.2.1" with `src/vue/composables/…` paths; the installed package ships **no composable src** (my `ls`: `src/vue/` holds theme.css only) — those anchors come from the local producer repo. All of DU's dist cross-checks land, so no verdict moves; repair waves take virtualiser anchors from **dist/vue.js** (the running artifact) or this record.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

### BLOCKER

| id | claim (adjudicated) | proof | disposition |
|---|---|---|---|
| **PAW-1 · D-B-1/L-1** (family: D-M-1 figcaption ink, D-m-6 proof blocks, D-m-7 figure frames/placeholder, section-body tone; roots PAW-11, PAW-13, PAW-30) | latex-paper theme.css declares a shadcn bare-triplet token contract (header :1-9 verbatim); glass-ui 4.0.0 ships complete colours (`--card: hsl(36 48% 97%)`, `--border: var(--neutral-4)`, `--primary: hsl(24 10% 10%)`, `--section-heading: oklch(…)` — my token reads), fourier defines zero triplets (1 CSS file; its own `--viz-amber` overrides are complete-colour, proving the house convention) — so all **42** `hsl(var(--x))` declarations are IACVT-dropped: sticky chapter/sub headers ship with **no background** (:447/:459 — body copy scrolls visibly under them), figure borders vanish (shorthand → `border-style: none`; `.paper-figure`'s literal `background: white` survives, `.paper-portrait` gets NOTHING), figcaption falls to body ink (over-legible, hierarchy gone), proof rails/tints, the error placeholder, `.section-body`'s 90% tone, and every `.paper-ref` colour+underline. Version-independent: this seat verified the token shapes at glass-ui **v7.0.0 AND v8.0.0 tags** — **the tri-package uplift cures nothing here.** The six `var(--_section-color, …)` sites survive (PaperSection always binds it; 13-stop ramp vs 12 top-level entries). | theme header + 42-count + per-selector reads, all mine; token bytes at 4.0.0/v7/v8; `find src -name '*.css'` → 1; PaperView.vue:12 loads the theme | **→ F.W1 ledger rider** (the transaction as chartered is INSUFFICIENT for the paper route: latex-paper is a fourth package the constellation must reconcile — add the **LATEX-PAPER carry** to the transaction or seat it explicitly) **+ LATEX-PAPER carry** (rewrite the 42 sites to `var(--x)` — the sound limb; the consumer-alias limb is KILLED, K-9) **+ F.W3/W4** witness surface. Sequencing: PAW-30 must land in the SAME repair. |

### MAJOR

| id | claim | proof | disposition |
|---|---|---|---|
| **PAW-2 · D-B-2/L-2/C-B-1** (demoted BLOCKER→MAJOR, R-1; absorbs D-i-5) | Warm/overscan-mounted but never-painted `.deferred-section` wrappers report the 1200px `contain-intrinsic-size` estimate from `offsetHeight`; `syncMeasuredHeight` writes it un-guarded into `measuredHeights` AND the module-scope, never-invalidated `SESSION_HEIGHT_CACHE`, where it **outranks** the content-derived estimate (`getHeight` chain, dist :822) for the page session, re-seeded on every items-watch. The 24-line comment (:144-166) asserting the `auto` prefix neutralises this is false for the mounted-but-never-painted steady state the config itself produces (`overscanAfterPx: 720`, warm ±2/+3). No re-measure on paint; no `contentvisibilityautostatechange` listener in either tree (my grep → 0) — the hook glass-ui's own utility prose prescribes (base.css:471-476). Navigation is compensated (K-6); spacers/`scrollHeight`/thumb/progress are not. | dist :789-790/:822/:868-906/:941-942/:960-971, PaperView :77-85, component :74/:167 — all re-read by this seat | **→ F.W3/W4 + LATEX-PAPER carry**: gate `measureSection` on the CV-state event, or skip the write when `offsetHeight` equals the estimate, or move `.deferred-section` off the measured element. **COUPLING LAW (L-2/L-4)**: the per-patch ref re-read is currently the only repair mechanism — fix this BEFORE memoising the ref (PAW-4). |
| **PAW-3 · L-5** | Declaring `#figure`/`#callout` inside the `v-for` forces `DYNAMIC_SLOTS` on `PaperSection` (seat compile: `_: 2`, patchFlag **1032**) → `shouldUpdateComponent` returns true unconditionally (runtime :4763-4765) → `<slot/>` renders a BAIL fragment (`slots._ === 2` → `-2`, :3288) → `patch()` clears optimisation (:5457-5460) → `PaperSectionBlocks` re-renders (`$stable` absent, :4781-4785) → latex-paper's `:is="() => effectiveSlots.figure(…)"` mints a **fresh arrow as the vnode type** (dist :1643, and :1760 for the callout — seat extension) → `isSameVNodeType` false → every `<picture>`/`<img>` and callout `<router-link>` in the window is **unmounted and remounted on every patch**, i.e. every scroll frame (PAW-4c). Mechanism citations corrected (K-10). | compiled by this seat with the tree's own compiler; every runtime line re-read | **→ F.W3/W4**: extract `PaperWindowSection.vue` taking `:item` so the slots leave the `v-for` scope (compiler-verified cure shape — clears `hasDynamicSlots`); the extracted figure child is also PAW-5's and PAW-18's home |
| **PAW-4 · L-4/D-i-3** | (a) Vue calls function refs on **every** patch with no identity comparison (:1803-1818 — string/Ref old-refs only get teardown; my read), and the arrow is freshly allocated regardless (`ref_for: true`, seat compile); (b) the repeat path reads `el.offsetHeight` **synchronously mid-patch** (dist :884-886) — up to 18 forced layout flushes per patch (e2e budget :3); (c) `resolveSectionWindow` returns fresh objects, `visibleItems` is a fresh `.slice()` (dist :968-971) → the parent re-renders **every scroll rAF even when the window is identical**. `bindRoot` is identity-guarded upstream (PaperView :204-205); `bindSection` is not. | seat compile + runtime + dist reads | **→ F.W3/W4**, sequenced strictly AFTER PAW-2 (the coupling law) |
| **PAW-5 · L-3/C-M-1/D-i-1** | Supplying `#figure` takes latex-paper's first render branch and silently deletes the `@error → failedImages → .paper-figure-placeholder` recovery chain (dist :1643-1650, the three-way ternary read at the bytes); the override's `<img>` carries **zero listeners** (component :96-106; my grep `error` → 0) — in the same file whose own comment (:41-43) documents the `<picture>` no-404-fallback trap. Second-order: the placeholder itself is invisible until PAW-1 lands (theme :637-643 both IACVT). | dist + component reads, mine | **→ F.W3/W4** (the PAW-3 extraction is where `@error` belongs) |
| **PAW-6 · D-M-3/L-9/C-m-8** | The `filename.includes('portrait')` dark-inversion discriminator is **wrong in both directions on the only two figures where it decides anything** — verified at the pixels by this seat: f21 (matches → NOT inverted) is three matplotlib panels on solid white (insets <5%) → ships as a full-width white slab inside `hsl(24 8% 16%)`, borderless per PAW-1; f20 (no match → inverted) carries a large Fourier engraving (≈¼ plate) → photo negative. Dark mode is class-live (`useGlobalDark`). The C axis reasoned from the TeX caption; both readers and this seat read the rasters. | both rasters read by this seat; theme :598-613; component :102 | **→ F.W3/W4** — but the cure is CONSTRAINED by K-17: **no whole-figure boolean is correct for f20** (mixed panels); re-rasterise f20's panels separately or ship dark-mode variants and retire whole-figure `invert()` |
| **PAW-7 · D-M-2/C-m-12** (TeX-escape arm killed, K-5) | `:alt="figure.caption"` binds **un-rendered `$…$` math** (the visible caption goes through `ctx.renderTitle`'s KaTeX replace; the alt bypasses it — captions verified math-dense in the TeX) AND duplicates the visible `<figcaption>` verbatim (unconditional sibling, conditioned on `caption \|\| number` only — dist :1651-1661). Inherited from the library's own default `<img>` (dist :1647), but the slot override was the fix point. Accents are NOT in scope: `replaceAccents` normalises both forms upstream (K-5). | dist + tex reads, mine | **→ F.W3/W4** (`alt=""` beside the adjacent rendered caption, or a distinct short description) |
| **PAW-8 · D-M-4/C-M-3** | `.callout-btn` is a divergent twin: the identical CTA (ℱ + "Open Visualizer" + ArrowRight) authored twice with contradictory chassis under one shared class name — here a hand-rolled solid-primary pill on a bare `<router-link>` (:188-206, zero glass-ui imports), in GalleryCardModal a `<Button variant="outline" size="lg">` skin whose comment (:246-247, my read) is false of this definition — governed by one shared **unlayered global** focus rule (style.css:136-143). Six paper siblings import glass-ui Button; this file is the directory's sole hand-roller; `as-child` over the router-link is a drop-in. | component + twin + style.css reads, mine | **→ F.W3/W4** (glass Button `as-child`, thin skin — the R3-7a precedent shape); composes with PAW-22's banked GCM-23/K-5 cure and PAW-41's radius tokens |
| **PAW-9 · D-M-5** (arithmetic seat-corrected, R-4) | `style="max-height: 400px"` is an inline, cascade-unreachable absolute cap (compiled as a static style prop — outranks every author sheet absent `!important`) starving tall figures inside a **640px** measure: f01 → 280×400 (360px/56% unused), f20 → 356×400 (~170px per panel of a four-panel figure). The file's every other magic length is a documented token retune or a `clamp()`; this is the one unreachable number. | seat compile + R-4 geometry | **→ F.W3/W4** (`max-block-size: min(60svh, 34rem)` in the scoped block — cascade-reachable) |
| **PAW-10 · D-M-7** | `.interactive-callout` declares two emphasis devices, both sub-threshold — recomputed by this seat's own instrument: 25% `--muted` tint over `--card` = **1.011:1** (and inverts sign in dark: `--muted` L11 darker than `--card` L16 — the callout recedes), leaving the 1px hairline as sole boundary at **1.904:1** light / **2.081:1** dark, under the 3:1 floor. The paper's only interactive interruption. | seat WCAG instrument + token bytes | **→ F.W3/W4** |
| **PAW-11 · D-M-8** (premise corrected, K-4; root PAW-1) | Every `\ref`/`\eqref` renders as plain body text — colour and dashed underline both IACVT-dropped (shorthand → `border-bottom-style: none`); only `cursor: pointer` survives at rest. The refs are **href-less `<a>`** (5 emitters, my dist grep — NOT spans): role generic, not focusable, absent from the AT links list — while `useClickDelegate` keeps them live. Invisible-but-live affordance, unreachable to keyboard/AT. | theme :13-19 + dist emitters, mine | **→ F.W3/W4** (rides the PAW-1 cure for visuals; the focusability arm needs real `href`/tabindex semantics at the latex-paper emitter — LATEX-PAPER carry rider) |
| **PAW-12 · C-M-2/L-8/C-m-2** (R-2 ruling) | `hasModernVariants` answers "do I know the dimensions?", not "do transcodes exist" — `TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))` (:52, sole assignment), so the `if` branch's `dims?.` is dead and the `else` can never carry dimensions; two independent facts, one hand-typed literal, guarding a `<picture>` that cannot 404-fall-back, with **no generator/manifest/CI/e2e anywhere** (my sweeps). Invariant HOLDS at HEAD by seat enumeration (28/26/26; unpaired = `fourier.png`/`maintainer-avatar.png`; 26 = 26 map⇄TeX). | figureDimensions + asset census, mine | **→ F.W3/W4** (split the predicates) **+ F.W9/W10 rider** (build-time set-equality assertion) |
| **PAW-13 (DU missed find, admitted)** | Cross-refs are invisible at rest AND over-loud on hover: `.paper-ref:hover { border-bottom-style: solid }` is **var-free and survives** the IACVT collapse that killed the base — with width fallen to initial `medium` (≈3px) and color to `currentcolor`, every hovered ref gains a ~3px solid body-ink bar (authored: 1px dashed accent at 40%). Seat sweep proves uniqueness: the only var-free border longhand paired with an IACVT shorthand in all 644 lines (:22; :70-71 are `.theorem-block`'s, un-paired, rail survives). The transition covers `border-color` only → the bar snaps. All three axes stopped one rule short. | theme :13-24 + seat longhand sweep | **→ F.W3/W4** (dies with the PAW-1 cure; recorded so the repair's visual test asserts the HOVER state too) |
| **PAW-14 (DU missed find, admitted)** | The treatise cannot be printed or PDF-exported: **zero** `@media print`/`@page`/`beforeprint` in web/src AND in glass-ui's entire dist (my sweeps), no PDF affordance in web/public — Ctrl+P yields the ≤18 mounted sections plus two absolute-pixel spacer divs rendering thousands of px of blank pages (`content-visibility` is print-transparent per spec; the JS window is not — the spacers are real boxes). D's state-coverage sweep (i-4) enumerated empty/loading/error only; no axis ran a print row. | seat sweeps; component :64-68/:126-130; budget 18 vs 93 | **→ F.W3/W4** (print stylesheet that unwinds the window, or an explicit PDF affordance — formation decision) |
| **PAW-15 (DU missed find, admitted)** | ~81% of the treatise is absent from the accessibility tree and find-in-page at any moment (≤18 of 93 sections mounted; the rest **unmounted**, not CV-skipped — nullifying the S-3 credit all three axes gave the `auto` rung), with zero `aria-*` signposting (my read of all 212 lines: none; no `role="feed"`, no `aria-setsize`/`posinset`, no live region). A continuous SR read stops silently at the window edge. Partial mitigation, seat-verified: the Cmd+K full-corpus search (`usePaperSearch` live at PaperView :111) makes content discoverable; linear-read and Ctrl+F paths are not. | component + PaperView reads; budget arithmetic | **→ F.W3/W4** (signposting + a windowing-aware a11y posture); AT behaviour residue → SS-13 |
| **PAW-16 · D-M-6/L-11** (R-3 ruling) | The per-item wrapper makes every `.paper-section` its wrapper's only (hence last) child, so theme.css:200-202's `:not(:last-child) { margin-bottom: 2rem }` **never matches** — no section in the paper receives its inter-section rhythm; CV-implied layout containment additionally blocks margin-collapse. Seat compile confirms one `PaperSection` per keyed div. | theme + compile, mine | **→ F.W3/W4** — cure CONSTRAINED by K-16: **measurement-safe forms only** (`padding-bottom` on `.paper-window-section`, or `.paper-window-section > .paper-section { margin-bottom: 2rem }` — inside the measured border box); the axis's inter-wrapper margin is KILLED (≈2.9k px of un-modelled drift) |

### MINOR

| id | claim | disposition |
|---|---|---|
| **PAW-17 · C-m-3/L-6** | Second asset-base authority: `${import.meta.env.BASE_URL}assets/` re-derived (:19/:46/:52/:53) while `PaperContext.assetBase` is provided by its own parent (PaperView :45/:59/:63) and consumed by the library on the same render path (dist :1646). `inject(PAPER_CONTEXT)` is one line. LC's citation drift noted (assetBase is :59, not C's :56). | **→ F.W3/W4** |
| **PAW-18 · C-M-4/L-7/D-i-2** (C's MAJOR demoted by both readers, sustained) | `resolveFigure()` invoked **exactly 7×** per figure per render (seat compile count: 7), each running two regex replaces + Set.has + three concatenations + a fresh object; no CSE in the compiler; multiplied by PAW-4c's per-frame re-render and PAW-3's remounts. | **→ F.W3/W4** (one computed per figure in the PAW-3 extraction) |
| **PAW-19 · D-m-1/C-m-4/L-14-easing** | The one un-tokenised easing in `components/paper/`: `transition: transform 0.2s ease, box-shadow 0.2s ease` (:199) against shipped `--ease-out-expo`/`--ease-standard`/`--duration-fast` (my token reads), four sibling files migrated under the A.W3.d ledger id. | **→ F.W3/W4** |
| **PAW-20 · D-m-2** | The hand-rolled button ignores three shipped contracts: no `:active` (sibling `.overlay-btn` has one at PaperView :614-616 — my read; `.tap-squish`/`btn-interactive` shipped), no `(hover: hover)` guard (hover latches after tap), and the `transform` shorthand against the individual-transform doctrine (surfaces.css:60-64 verbatim — my read). | **→ F.W3/W4** (dies inside PAW-8's Button adoption) |
| **PAW-21 · D-m-3** | The ornamental ℱ (bare U+2131, :116) pollutes the accessible name ("ℱ Open Visualizer"); the ArrowRight is genuinely exempt (lucide auto-`aria-hidden` — reader-verified, consistent with my reads), making the untreated ℱ the sole pollutant. | **→ F.W3/W4** (`aria-hidden` on the span) |
| **PAW-22 · D-m-4/C-m-5 — FOLD → banked GCM-23 + K-5** | The unlayered global `.callout-btn:focus-visible` rule carries `border-radius: inherit` (resolves to the parent's 0.75rem, not the pill's 9999px — dead code or a keyboard-path radius pop, order-decided at a (0,2,0) tie) and splits the class's ownership across two files. Banked GCM-23 already books the rule-wide cure; banked K-5 already records that THIS component's bare router-link has no other indicator, so deletion must be site-scoped. Never re-booked. | **FOLD (F.W3/W4, inside the GCM-23 repair)** |
| **PAW-23 · D-m-8** | `{{ callout.text }}` (:111) is the one authored string on the surface bypassing the paper's renderer (everything else rides `renderTitle`/`v-html`); latent only because both configured callouts are math-free (vite.config :9-19, my read); the type is untyped-against-TeX. | **→ F.W3/W4** |
| **PAW-24 · C-m-1** | The `.pdf`→`.png` replace at :45 is dead — the parser already basenames + normalises (chunk :620-627, my read) — and the doc comment at :39 states the inverse of the installed contract. | **→ F.W3/W4** (delete + correct the comment) |
| **PAW-25 · C-m-6/7/9/10/11** | Five consumption seams, all LC-confirmed and spot-held by this seat: hardcoded "Open Visualizer" label vs a `{text, link}` model; unvalidated `:to` string with no router catch-all (`/visualize` IS live as an alias — :71, my read — so fragility only); five callback props / zero emits/expose; non-`readonly` `visibleItems` against the producer's readonly posture; `max-w-full rounded-lg` restating the theme layer (`rounded-lg` additionally loses the cascade at (0,1,0) vs (0,2,0) — inert). | **→ F.W3/W4** |
| **PAW-26 · L-10 (rescoped by K-12)** | `toHTMLElement`'s CPI arm is runtime-unreachable (seat compile: two native divs) and its `null` return is indistinguishable from legitimate unmount (`disconnectSection` deletes from `elementMap` only; `measuredHeights` keeps the stale height) — no warn, no distinction. But the TYPE is load-bearing (K-12): the inline arrow is contextually typed from Vue's ref signature, so narrowing would fail under `strictFunctionTypes`. The import is NOT dead code. | **→ F.W3/W4** (distinguish the two nulls; keep the type) |
| **PAW-27 · L-13** | Two independent estimators of section height disagreeing 3.75–5.5× at the floor: the flat `--deferred-section-size: 1200px` (:167, no measurement cited) vs `estimatePaperSectionHeight`'s content-derived per-section figure (floors 320/220 — my dist read); PAW-2 is the mechanism by which the flat one wins. The template already does per-item `:style` binding twice. | **→ F.W3/W4** (derive the token per-item, with PAW-2) |
| **PAW-28 · L-17** (R-6 ruling) | `SESSION_HEIGHT_CACHE`: module-global, keyed on bare slugified ids, written/read/re-seeded, and **deliberately excluded** from the teardown that clears everything else (dist :960-967, my read) — the session-durability vehicle of PAW-2 and a cross-consumer collision surface (with PAW-38). | **→ F.W3/W4 + LATEX-PAPER carry** (instance-scope or namespace the cache) |
| **PAW-29 · L-18** (R-7 ruling) | Format negotiation without width negotiation: no `srcset` width descriptors, no `sizes` — 4276px/2.85MB print rasters into a 640px column at every DPR. | **→ F.W3/W4** (width variants ride the PAW-12 generator) |
| **PAW-30 (both readers' missed find — admitted once)** | `.deferred-section`'s unconditional paint containment clips the sticky headers' designed **±1rem full-bleed** (theme :449-451/:461-463 negative margins into `.paper-article`'s gutters; the wrapper has no padding to absorb them — my reads). Latent ONLY because PAW-1 already dropped the header background — **the blocker's cure is what makes this visible**, so both must land in one repair or the header ships as a clipped plate with bare gutters. LC's registry cite honoured: same CLASS as banked FR-EMT-2 (containment clips producer-authored outset paint — re-read :32 this session), NEW instance with a distinct mechanism (negative-margin bleed under CV-implied containment) — cross-cited, not re-booked. R-8's surviving stacking-context half rides here. | **→ F.W3/W4, sequenced WITH PAW-1** |

### INFO

| id | claim | disposition |
|---|---|---|
| **PAW-31 · D-m-5** (R-5 ruling) | Footer spacer decoupled from the overlay it clears: no `env(safe-area-inset-bottom)` term, no coupling to `.paper-bottom-overlay`'s height, `vh` where the repo uses `dvh`, and the overlay double-counts SAI inside an already-inset body — magnitude seat-corrected to ~2.5px at 390×844, zero at ≥640px. | **→ F.W3/W4** (cheap: add the SAI term) |
| **PAW-32 · C-i-3** | Both slot contracts untyped: PaperSectionBlocks.vue.d.ts ships **no** `__VLS_Slots` (my grep: 0; sibling PaperSection has it) — `figure.*`/`callout.*` destructure implicit `any`; the file's largest unchecked surface; a producer gap. | **→ LATEX-PAPER carry** (emit slot types); NO-WAVE-OWNER locally |
| **PAW-33 · C-i-2** | Peer ranges silently violated: katex `^0.16` vs installed **0.17.0**, vite `^6\|\|^7` vs **8.0.16**, all `optional: true` (my reads) — no npm signal. | **→ F.W1 ledger** (pin-truth row) |
| **PAW-34 · C-i-5** | Zero e2e coverage of any surface this component owns — seat grep: the only "figure" hits in all 8 specs are `test.describe.configure` substrings. The `<picture>` guard, callout, spacer math, portrait dispatch: all unguarded. | **→ F.W9/W10** |
| **PAW-35 · C-i-4** | Comment overclaim (:144-146): `content-visibility` does not defer KaTeX **typesetting** — the JS half runs eagerly in the render path (`renderParagraph` via innerHTML bindings, dist :1660); only browser layout of the produced spans is deferred. | **→ F.W3/W4** (correct the comment in whatever wave touches the file) |
| **PAW-36 · L-16/C-§0 — FOLD → banked R5-7** | Line 71 is a native `<div v-for>` — the single largest instance multiplier in the tree and a second live witness for the banked native-loop blind spot (intake :125, re-read this session), differing in that its length is a runtime function of scroll state (budget 18). The increment carried: F.W4 needs a **multiplicity** model, not only loop-presence. Never re-booked. | **FOLD (F.W4)** |
| **PAW-37 · D-S-1/§6** | The load-bearing F.W1 planning fact, seat-verified at BOTH tags: zero glass-ui import surface here, all three consumed CSS surfaces survive at v7.0.0 (deferred-section byte-moved, cm-serif/fourier-f present), and `--card`/`--border`/`--primary` keep complete-colour shape at **v7.0.0 AND v8.0.0** — so the uplift does NOT cure PAW-1 at either candidate target. Unlike the GalleryCardModal record, **no disposition here flips on the 7-vs-8 decision.** | **→ F.W1 ledger** (planning input) |
| **PAW-38 (LC missed find, admitted)** | Section ids — the `:key`, the DOM `id`, `elementMap`/`measuredHeights`, and the never-cleared session cache — are all the same un-deduplicated title slug (sole assignment; no uniqueness registry): two identically-titled headings collide across four registries at once. Seat-verified LATENT: all 93 slugs unique today (my own slugify + `uniq -d` → empty); natural repeats are one edit away with zero test. L-17 books only the cross-consumer case; this intra-document case was unbooked by all three axes. | **→ LATEX-PAPER carry** (uniquify at the emitter); NO-WAVE-OWNER locally (latent, test-shaped) |
| **PAW-39 (DU missed find, admitted; census seat-corrected SC-2)** | Two outline levels flattened to one: every `depth > 0` section renders `<h3>` + `.section-header--sub` (PaperSection.vue, my read), so the paper's 51 `\section` and **31** `\subsection` nodes are indistinguishable to AT heading nav and park at the same `top: 3.5rem`; the flattener emits `sourceLevel` (:110) that the renderer never reads (my grep). Producer gap manifesting here (sole call site supplies `:depth`). | **→ LATEX-PAPER carry**; NO-WAVE-OWNER locally |
| **PAW-40 (DU missed find, admitted)** | Dark-mode `filter: invert(1) hue-rotate(180deg)` on `.paper-figure` also inverts the component's own `shadow-sm` → a white-10% halo (filter applies to the element's whole rendering incl. box-shadow, alpha preserved) — currently the ONLY edge treatment a dark figure has (PAW-1 dropped the border); `.paper-portrait` gets neither filter nor border → divergent unintended edge treatments. | **→ F.W3/W4** (rides PAW-6's inversion redesign) |
| **PAW-41 (DU missed find, admitted)** | Two off-token radii where the tokens ship: `9999px` (:197) vs `--radius-pill` (+ the `.btn-pill` recipe), `0.75rem` (:183) vs the `--radius-*` ladder (my token reads). The cheapest conformance step short of PAW-8, and the one that composes with banked GCM-23's rule-wide cure. | **→ F.W3/W4** (with PAW-8/PAW-22) |
| **PAW-42 · D-i-4** (coverage-gap admission, R-9) | No empty/loading/error state at component level — proven unreachable (build-time `virtual:paper-content`); admitted at corpus grade as an epistemic record. | **NO-WAVE-OWNER** (record) |
| **PAW-43 · C-i-1 — FOLD → census F.W1 P0** (coverage-gap admission, R-9) | `lucide-vue-next` import (:8) is one of the 35 census-booked rename sites in the F.W1 atomic transaction. Fold honoured; never re-booked. | **FOLD (F.W1)** |

## Killed-claims register — 17 entries (every kill re-derived before sustaining)

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **L-15** — "there is no longer a `styles/utilities.css` at 4.0.0; a reader following the comment finds nothing; a silent casualty of the major bump" | **SUSTAINED (both readers), re-derived**: my `ls` finds the file at BOTH `src/` and `dist/` — a 19-line thin @import root whose own header documents the carve, transitively containing the cited rule (utilities/base.css:477-480). The axis's own falsifier command refutes the axis. Residual = an INFO-grade citation-precision note, nothing more. |
| **K-2** | **C-i-6 + L-14's PRM arm** — "the callout hover animates transform with no PRM guard; the app's only PRM block covers a tabpanel" | **SUSTAINED (both readers), re-derived at the installed pin**: a11y-overrides.css:6-17 (my read) restricts `transition-property` under PRM to an allow-list **excluding `transform`**, `!important`, universal selector — the lift snaps, the shadow cross-fades. Loaded via style.css:3 → index.css → utilities.css → a11y-overrides (last). C's "only PRM block" cell is a scope error (grepped the app layer only). The D axis got this right (S-6). |
| **K-3** | **C-m-8's "right today" verdict** — "the heuristic is right today; f21 genuinely is a portrait set whose photographic insets should not be inverted; fragility only" | **SUSTAINED (DU), decided at the pixels by this seat**: f21 is ≥95% white-field matplotlib line art — withholding inversion to protect three ~200px insets ships a full-width white slab in dark mode; and f20 (no match) IS inverted with a quarter-plate engraving → negative. The C axis read the caption, not the bytes. PAW-6 wins the cross-axis contradiction. |
| **K-4** | **D-M-8's element premise** — "the elements are `<span>`s, so not in the tab order or links list" | **SUSTAINED (DU), re-derived**: my dist grep finds 5 emitters, all `<a class="paper-ref" data-ref=…>`. The a11y CONCLUSION survives on the corrected mechanism (href-less anchor → role generic, not focusable, no links-list entry). Recorded so a repair does not hunt for spans. |
| **K-5** | **D-M-2's TeX-escape arm** — "a screen-reader hears 'Fej backslash apostrophe brace e brace r'" | **SUSTAINED (DU), re-derived**: `replaceAccents` (producer clean.ts, my read) normalises braced AND unbraced accent forms before the caption reaches `PaperFigureData`; validate.ts additionally lints for unprocessed accents. The `$…$` math arm and the duplication arm of PAW-7 are untouched and stand. |
| **K-6** | **D-B-2/L-2's navigation-overshoot consequence** — "the scroll target overshoots by 800px — precisely the outcome the comment says is neutralised" | **SUSTAINED (both readers; the C axis ran this falsifier honestly), re-derived**: my whole read of useScrollNavigation.ts — `computeAbsoluteTop` prefers live geometry whenever the element resolves; `teleportTo` corrects behind an overlay to 6px stability. The un-compensated half (spacers/scrollHeight/thumb/progress) carries PAW-2 at MAJOR. This kill is why no BLOCKER survives in the B-2 family. |
| **K-7** | **D-M-5's measure arithmetic** — "content width 768 − 80 = 688px at ≥640px" + the derived unused-measure column | **SUSTAINED-AND-EXTENDED (seat, R-4)**: the 48rem column is never realised (976 − 252 = 724) and the axis's "≥640px" mis-states the 1024px media query; true measure = **640px** (724 − 80 − 4), correcting the axis AND both readers (DU 644: border omitted; LC 684: unrealised column). Direction/severity/cure of PAW-9 survive intact. |
| **K-8** | **D-m-5's ~22px overlap magnitude** | **SUSTAINED (DU), seat re-run**: the arithmetic omitted `.paper-article`'s own bottom padding below the spacer in the same box — deficit ≈2.5px at 390×844/SAI-34, zero at ≥640px. Structural facts booked at PAW-31; grade INFO (R-5). |
| **K-9** | **D-B-1's first-limb cure** — ":root triplet aliases (`--card: 36 48% 97%; …`) discharge B-1" | **SUSTAINED (DU), re-derived**: redefining the SAME token names as bare triplets breaks every complete-colour consumer in the document — PaperView :524 `background: var(--card)`, style.css cartoon-card :110-112, this component's :182/:184, and glass-ui's own recipes (my reads). Triplet aliases are sound only as NEW names with theme.css rewritten to consume them — which IS the second limb, an upstream change. The repair wave must not implement the literal snippet. |
| **K-10** | **L-5's step-1 mechanism citation** (the `scopes.vFor` branch of `buildSlots`) **+ its `:6665` BAIL citation** | **SUSTAINED (both readers), settled by this seat's own compile**: in module mode with prefixIdentifiers, `hasDynamicSlots = hasScopeRef(…)` — which is exactly why the inner `PaperSectionBlocks` slots compile `_: 1 /* STABLE */` while only `PaperSection` gets `_: 2`/1032 (my emitted output). And the de-optimisation site is `patch()` :5457-5460; :6665 is unmount. The OUTCOME is exactly as the axis claimed — PAW-3 stands on corrected provenance. |
| **K-11** | **L-12** — "the wrapper is the sticky containing block, so the two-tier header stack can never form; `display: contents` would cure it" | **SINGLE-READER KILL RATIFIED (LC; DU never adjudicated the row) after independent derivation (R-8)**: the sticky constraint box is `.paper-section` (the headers' own parent — my PaperSection.vue read), not the wrapper; removing the wrapper changes nothing. The stack fails upstream: the flattener emits chapters/subsections as flat peers, so the chapter's `<section>` closes first. The surviving stacking-context half rides PAW-30. The headline claim AND its cure ("remove the wrapper") are dead. |
| **K-12** | **L-10's type-deadness arm** — "the `ComponentPublicInstance` import at line 7 is dead" | **SUSTAINED (LC)**: the runtime arm IS unreachable (seat compile: two native divs), but the type is load-bearing — the inline ref arrow is contextually typed from Vue's `Element \| ComponentPublicInstance \| null` signature and checked contravariantly under `strictFunctionTypes`; deleting the import/narrowing the params breaks `vue-tsc`. PAW-26 carries the rescoped row. |
| **K-13** | **C-M-1's quoted evidence** — dist :1644-1649 rendered as `renderSlot(...)` | **SUSTAINED (LC), byte-checked**: the actual dist :1643 is `_createBlock2(_resolveDynamicComponent2(() => $setup.effectiveSlots.figure({…})), { key: 0 })`. The finding survives; the substituted quote is exactly the byte PAW-3's remount chain turns on — an axis that wrote `renderSlot` there could not have seen the fresh-arrow churn, which is why the corpus's deepest perf finding appeared on one axis only. Evidence-integrity kill. |
| **K-14** | **C-B-1's reading-progress citation** — "PaperView :186-192 computes the progress bar off that number" | **SUSTAINED (LC), re-read**: `writeProgress` sits at ~:160-168; :174-181 is the arm/disarm pair whose early-return (`NATIVE_SCROLL_TIMELINE \|\| prm`) means the cited listener never attaches on modern engines. The SYMPTOM survives via the native `scroll()` timeline reading the same falsified `scrollHeight`; the cited mechanism does not. Repair tests must witness the native path. |
| **K-15** | **D-S-2/D-M-1's cardinals** — "all 25 fNN assets are keyed" / "a treatise with 25 figures" + D-M-1's dark ratio 14.03:1 | **SUSTAINED (LC), re-counted and re-computed**: 26 map keys (two f14s), 26 `\includegraphics`, and my instrument gives dark `--foreground`/`--card` = **11.786:1** (LC's 11.79; the axis's 14.03 does not reproduce). Isolated slips — every other D-axis figure reproduced to the third decimal on my instrument. |
| **K-16** | **D-M-6's prescribed cure** — "`.paper-window-section + .paper-window-section { margin-top: 2rem }` — one line, the component owns the rhythm" | **CURE-KILL ADMITTED (LC missed find), re-derived**: `buildSectionLayout` (dist :688-702, my read) sums `offsetHeight` border boxes with zero gap allowance — an inter-wrapper margin is structurally invisible to the layout model → cumulative drift ≈ 92 gaps × 32px ≈ **2.9k px** by document end: the same corruption class as PAW-2, injected by the cure for PAW-16. The library's ORIGINAL rule was measurement-safe here for a non-obvious reason: the child margin sits INSIDE the wrapper and CV-implied containment stops it collapsing out, so `offsetHeight` counts it. Measurement-safe cures named at PAW-16. |
| **K-17** | **D-M-3's prescribed cure** — "an explicit per-figure `invertInDark: boolean` in figureDimensions.ts" | **CURE-KILL ADMITTED (LC missed find), decided at the pixels by this seat**: f20 is one PNG with four panels of two kinds — the engraving must not invert; the three white-field line panels must. No whole-figure boolean has a correct value; the flag cures f21 and leaves half of PAW-6 open. Remedy is upstream of the map: per-panel re-rasterisation or dark-mode asset variants. |

## Superlatives — 10 (provenance marked)

1. **The `<picture>` 404 reasoning is correct, documented, and its invariant survives audit** (all three axes + both readers + this seat's own enumeration: 28/26/26, the two unpaired PNGs are non-figures, 26 map keys = 26 `\includegraphics`). The comment states the exact semantic most `<picture>` adoptions get wrong, and the markup honours it.
2. **`:key="item.id"` is the one identity decision that had to be right, and is** (seat compile: `128 /* KEYED_FRAGMENT */`; an index key over the sliding slice would remount the whole article body, destroying KaTeX output and every CV last-remembered size, on every scroll step).
3. **`.deferred-section` is CONSUMED, not forked, retuned through the substrate's own documented knob** — one class, one token (:167), zero local `content-visibility` (my grep: 1 hit, in prose); glass-ui's rule even names fourier γ as the consumer it was written for (base.css:465, my read). PAW-2 attacks the reasoning ABOUT the recipe, never the adoption.
4. **The CLS kit is complete and correct for the non-obvious reason, on the authoritative element** — `width`/`height` on the `<img>` (not the sources) + theme's `height: auto` + `loading="lazy"`/`decoding="async"`; figureDimensions.ts exists because `PaperFigureData` genuinely omits dimensions (producer gap correctly diagnosed, minimal consumer remedy, 26/26, additive-degradation posture stated and true).
5. **Zero casts in a file whose job is DOM plumbing** — `toHTMLElement` narrows with real `instanceof`; the two non-null assertions are each `v-if`-guarded one line above; my grep `\bas [A-Z]|: any` → 0. And the imported type is load-bearing, not decoration (K-12).
6. **`<router-link>` for the navigation CTA** — real `<a href>`: middle-click, ⌘-click, new-tab, copy-link, links list all work; the divergent twin uses a click-handled Button for what is also navigation. This component is on the right side of the comparison its own PAW-8 loses.
7. **Reduced motion composes with the substrate by construction** — the component chose `transition` over an allow-listed property set rather than `animation` (which would escape the list); the lift snaps, the shadow cross-fades. Two axis rows filed this as a defect; both are killed (K-2).
8. **Where the component owns its ink, contrast clears AA/AAA in both schemes** — and the dark arm's near-black-on-lavender button label is a deliberate, correct ink flip. Only its SURFACE choices (PAW-10) are sub-threshold. The D axis's arithmetic reproduced to the third decimal on this seat's independent instrument at every correctly-inked row.
9. **Zero F.W1 break surface, proven at the tags, with the corollary that matters** — no glass-ui symbol imports at all; all three consumed CSS surfaces survive at v7.0.0; and because the token shapes survive at BOTH v7.0.0 and v8.0.0 (seat-verified), the uplift cannot cure PAW-1 — the single most valuable F.W1 planning fact in this corpus, and unlike the GalleryCardModal record, no disposition here flips on the 7-vs-8 target decision.
10. **Reader discipline this round** (seat's own): both readers read the rasters instead of the captions — the decisive act of the round; DU's kill arithmetic reproduced exactly under my independent instruments (with one border-width slip); LC's baseline extension to v8.0.0 and its two cure-kills (K-16, K-17) caught repair-corrupting errors the whole corpus missed; both held the identity guard with zero re-books.

## UNPROVEN-NEEDS-LIVE / NEEDS-BUILD — SS-13 visual-audit inputs

1. **PAW-2 engine detail** — whether Chromium's first lifecycle update locks a below-relevance-margin section BEFORE any paint (no last-remembered size → `offsetHeight` = 1200 even for near-viewport sections), and the live magnitude (how many of ≤18 mounted sections report 1200 at a given scroll position; px drift in `totalHeight`). Needs a live `.paper-window-section` + `offsetHeight` sweep on `/paper`. The mechanism does not depend on it (`warmTargetAfter: 3` proves it on its own arithmetic). → SS-13.
2. **PAW-22 order** — emitted CSS order between the unlayered global focus rule and the scoped `.callout-btn` at the (0,2,0) tie: dead code vs visible keyboard radius pop. The disjunction and the banked GCM-23 cure hold under both branches. → SS-13.
3. **PAW-31 magnitude** — rendered `.paper-bottom-overlay` height on a notched device (`env()` is device-reported); seat arithmetic says ~2.5px/zero. → SS-13.
4. **PAW-1 pixel grading** — which of the 42 drops is catastrophic vs flat. Witness set: a scrolled chapter header, a `.paper-figure` in both schemes, a `.paper-ref` at rest AND hovered (PAW-13's 3px bar), a proof block. → SS-13.
5. **PAW-30 magnitude** — the containment clip on the ±1rem header bleed, observable only AFTER the PAW-1 cure lands; spec/source-derived otherwise. → SS-13 (post-repair witness).
6. **PAW-15 AT residue** — whether VoiceOver/NVDA continuous read terminates at the window edge and how the spacer divs announce; DOM-absence is source-proved. → SS-13.
7. **PAW-3/PAW-4 cost** — visible flicker/re-decode from the per-patch remounts + the ~18 forced layouts per frame: one performance trace on `/paper` covers both (same scroll frame). → SS-13.
8. **`vue-tsc -b` clean run** — C-i-3 proves the slot props are implicit `any`; K-12 predicts the "dead import" cleanup breaks the build. One clean run in whatever wave touches the file (this seat is write-nothing). → NEEDS-BUILD, cheap.

## DISSENT — preserved, not ruled away

1. **Reader scorecard.** reader-DU: the round's sharpest missed find (PAW-13 — all three axes stopped one CSS rule short) and the raster-decided K-3; the D-B-1 cure-limb kill (K-9) is the record's best example of the "cure adjudicated separately" law firing. Debits: the 644px measure (border omitted, R-4), the shared "35 subsections" cell (SC-2), and "installed src" provenance wording on the virtualiser anchors (SC-3). reader-LC: the v8.0.0 baseline extension (PAW-37's second tag), both cure-kills (K-16's 2.9k-px drift derivation is the round's deepest single cell), the ratified L-12 kill, and the slug-collision find. Debits: the 684px measure (unrealised column, R-4), the same "35" cell, and two grade calls overturned (R-2, R-7). Both readers: zero identity-guard violations.
2. **Severity dissents carried**: LC's MINOR on PAW-12 and PAW-16; DU's INFO on PAW-28 and MINOR-boundary hesitation on PAW-31 (ruled INFO). A future seat re-grading PAW-15 upward should weigh that the a11y composite (PAW-15∘PAW-11∘PAW-7∘PAW-39) approaches blocker weight as a cluster without any single row carrying it — the GalleryCardModal R-1 precedent shape.
3. **Shared-cell event (fleet evidence)**: both independent readers produced the identical wrong "35 subsections" cardinal while both also carried the correct 93 total — evidence that un-anchored grep is a fleet-default instrument shape, not that either reader inherited from the other. The brace-anchored census (11/51/31) is this record's.
4. **Substrate note**: the subject file and figureDimensions.ts are clean at HEAD `cd26c65`; `PaperView.vue` (the sole caller, cited ~30 times here) is DIRTY — all its anchors are working-tree anchors re-resolved this session; a committed HEAD that differs shifts them (the D-19 lesson: re-resolve before any cure).
5. **This seat's scope notes**: adopted from reader verification without independent re-extraction — the lucide auto-`aria-hidden` line, Chromium's ~50%-viewport relevance margin (tagged UNPROVEN by all parties), the `dist/dark.js` class-toggle line, and DU's dark-arm button-label ratio (sanity-checked, not recomputed). Every verdict-deciding cell — the 42-count, both rasters, the compile flags, the runtime lines, the WCAG ratios, both producer tags, the slug census, the measure geometry — was re-derived this session with this seat's own instruments.

## Closing verdict

**ADJUDICATED — the corpus survives its second, adversarial reading re-shaped: the BLOCKER count falls from three axis-claimed to ONE (the latex-paper token-contract collapse, seat-verified version-invariant across glass-ui 4.0.0/v7.0.0/v8.0.0), the B-2 measurement family demotes to MAJOR on the compensated-navigation derivation, and the roster lands at 43 rows (1 BLOCKER · 15 MAJOR · 14 MINOR · 13 INFO; 3 identity-folds honoured, zero re-books). 17 kills sustained after independent re-derivation — including two cure-integrity kills (K-16's measurement-poisoning margin, K-17's impossible boolean) that would have shipped corrupted repairs under green reviews, and one arithmetic correction (640px) that re-inks every measure-derived cell in the corpus. 8 missed finds admitted at the bytes — the sharpest being the hover-state 3px bar all three axes stopped one rule short of, the unprintable-treatise state sweep, and the a11y-tree absence that nullifies the corpus's own S-3 credit. The component's authored surface earns 10 superlatives; its composed seams own the damage: PAW-1 routes to an F.W1 ledger rider + the LATEX-PAPER carry the tri-package transaction currently lacks, the virtualiser/slot/measurement cluster routes to F.W3/W4 under two sequencing laws (PAW-2 before PAW-4; PAW-30 with PAW-1), the producer gaps (slot types, slug uniqueness, sourceLevel, ref semantics) are named LATEX-PAPER carries, and coverage lands at F.W9/W10. Routings are M-25 agglomeration input; nothing here stamps a wave.**

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED.

---

# ROUND 2 — second adversarial re-read, adjudicated (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator, round 2 · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited (the file's line 1 stands — same served id, independently observed).
**Date**: 2026-08-25 · **E-3 posture**: the 2026-08-24 record above is dated evidence and is NOT rewritten. Where round 2 corrects a banked cell, the correction lives HERE and the banked bytes stand as the record of what the first seat held. Sole write = this file.
**Corpus adjudicated**: two hostile re-readers of the full challenge corpus AND the banked record — **reader-2DU** (design-and-usage: 28 confirmation cells · 13 kills · 3 missed finds · 11 superlatives · 9 unproven) and **reader-2LC** (library-and-code: 22 confirmation cells · 15 kills · 4 missed finds · 9 superlatives · 5 unproven). Both fold to banked PAW/K ids throughout; their confirmation cells re-derive banked rows and are adopted as corroboration, not re-adjudicated row-by-row here — this addendum rules only what is NEW, DISPUTED, or lands ON the banked record.
**Method (incredulity)**: every round-2 kill re-derived at the bytes before sustaining; every admitted missed find re-proven with this seat's own instruments; one reader kill extended at the tag the reader did not check (K-20); identity sweeps re-run before admitting any row. Zero browser probes (probe parsimony); product source read-only.
**Substrate re-checks (this seat's own, this session)**: fourier HEAD still `cd26c65`; `PaperView.vue` still DIRTY — but `git show HEAD:web/src/components/paper/PaperView.vue` reproduces the `.paper-article` rule :516-529 **byte-for-byte** and `git diff` shows the file's single hunk at ~:390 (elsewhere) — every PaperView anchor in this addendum is HEAD-stable for the rules it cites. The subject file and `figureDimensions.ts` remain clean.
**This seat's own instruments (all static + git + node greps, this session)**: PaperView.vue :301-302/:320-351/:379 (template chain `.paper-root > .paper-scroll > .paper-layout > .paper-grid > article.paper-article > PaperArticleWindow`), :416-422 (`.paper-root { position: relative }`), :425-445 (`::before` scrim — `position: absolute; top: 0; height: 2rem; z-index: var(--z-content)`, 55%→transparent-at-70% gradient), :458-465 (`.paper-scroll` = the real scroller, `overflow-y: auto; overflow-x: hidden`), :474-483 (`.paper-progress-track` sticky against `.paper-scroll` — a working sticky OUTSIDE the article, the control case), :516-529 (`.paper-article` — `overflow-x: hidden` at :527, no height/max-height), :553-565 (grid + the 1024px media query), :146/:251/:320 (`mobileTocVisible` IntersectionObserver v-if); theme.css (installed, `web/node_modules/@mkbabb/latex-paper/src/vue/theme.css`) :443-465 re-read at the bytes (`position: sticky; top: 0/3.5rem; z-index: 10/9; background: hsl(var(--card)); margin-left/right: -1rem; border-radius: var(--radius)`) + `grep scroll-margin` → :73/:197/:621; `grep -rn scroll-margin web/src` → **0**; `grep -rn scrollIntoView web/src` → exactly 3 (two search lists + CollapsibleSection — none a paper section/figure/math block); router/index.ts `grep scrollBehavior` → **0** (no fragment-navigation path consults scroll-margin); useScrollNavigation.ts :22-26 (`getScrollOffset` = `bar.offsetHeight + 8 : 16`) + :46 (`Math.max(0, rawTop - getScrollOffset())`); MobileFloatingToc.vue :107 (`class="floating-toc lg:hidden"`) + :193-199 (root `height: 0; overflow: visible`); latex-paper dist/vue.js :867-874 (**`syncMeasuredHeight` opens `if (measuredHeights.get(id) === normalized) return;` — the identity guard, K-19 decider**) + :383-415 (**`var cache = new Map()` at module scope :387; keys `` `i:${tex}` ``/`` `d:${tex}` `` — macros passed to `katex.renderToString` but NEVER keyed; `throwOnError: false` in BOTH branches**) + `grep 'cache\.delete\|cache\.clear\|cache\.size'` → **0 hits** (no eviction anywhere in dist/vue.js); `grep -rn useKatex web/src` → exactly 2 (import :3 + sole call PaperView :38, macro set :26-36 = spread + 8 authored macros); math census, my own greps: **1145 unique** `$…$` inline spans (reproducing reader-2LC EXACTLY) / **2025 occurrences** (exact) / display envs ≈266 by my coarse pattern (LC's 263 adopted, cardinal not load-bearing — SC-5); figureDimensions.ts :22 (`Record<string, readonly [number, number]>` — no `undefined` in the index signature) + :51-52 (`TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))`); PaperArticleWindow.vue :44-59 re-read (`const dims = FIGURE_DIMENSIONS[pngName]`; `dims?.[0]` in both arms) + :167 + :179-186; web/tsconfig.json read whole — `strict: true`, **no `noUncheckedIndexedAccess`**; web/package.json — scripts exactly `dev | build | preview | test:e2e | test:e2e:ui` (note `build` = `vue-tsc -b && vite build`), **no vitest/jest anywhere**, `ls vitest.config.*` → none, `find src -name '*.test.ts' -o -name '*.spec.ts'` → **0**; style.css :98-111 (the `cartoon-card` shim + its "14 application sites (13 files…)" comment) + :131-143 (the unlayered focus rule, comment naming PaperArticleWindow.vue); `grep -rln cartoon-card web/src` → style.css + **13 component files** (equation ×4, morph ×3, visualization ×6) and **ZERO under components/paper/**; glass-ui installed 4.0.0 a11y-overrides.css :115-122 (coarse-pointer floor = exactly `[data-size="icon"]`, `.expandable-container__trigger`, `.segmented-tabs__trigger`); **glass-ui v7.0.0 tag** (`git show` in the local producer repo): a11y-overrides has **NO `min-block-size` floor rule at all** — the only coarse-pointer treatment is the opt-in `@utility touch-hit-area` `::before` overlay composed by six form atoms, and :127-134 there is COMMENT prose inside that utility's header (the K-20 decider); registry identity sweeps re-run: `interactive-callout` → exactly 1 hit (PAW-10), `katex` in this record → PAW-7/PAW-33/PAW-35/superlatives (NO cache row — PAW-45/46 are novel), fr-FourierMorphDemo.md:77 **FMD-30** re-read (the `overflow-x: hidden`-mints-a-nested-scroll-container class — the PAW-44 cross-cite lands).

## Scoreboard (round 2)

**Roster 43 → 50** (1 BLOCKER · 16 MAJOR · 17 MINOR · 16 INFO): **7 new rows admitted** from the readers' missed finds (PAW-44..PAW-50), every one re-proven at the bytes by this seat. **Killed register 17 → 20** (K-18..K-20) — and for the first time a kill lands ON the banked record itself: **the banked BLOCKER's most vivid consequence arm is dead** (the sticky headers never stick, so nothing ever scrolled beneath them). **PAW-1 remains BLOCKER on its seven surviving arms** — the 42-drop token-contract collapse is untouched; what falls is one consequence row and the witness that would have chased it. Two sequencing laws added (LAW-3, LAW-4); the SS-13 witness list re-scoped; 4 superlatives added; 3 seat corrections against the round-2 readers.

## Ruled items (this seat's actual work, R2-1..R2-9)

| # | Item | reader-2DU | reader-2LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| **R2-1** | **The sticky stack is 100% inert** (DU missed find, MAJOR; + DU's kill of the D-B-1/L-1 scroll-under rows and the banked PAW-1 arm) | filed both | absent | **RATIFIED → new row PAW-44 + kill K-18.** | Re-derived end-to-end with my own instruments: `.paper-article` carries `overflow-x: hidden` (:527, **byte-identical at HEAD** — my `git show`/`git diff`); per CSS Overflow 3 §3 a `hidden` on one axis forces the other's computed `visible` → `auto`, so the article is a scroll container in BOTH axes; it declares no height/max-height and is a content-sized block (grid-item stretch is a no-op on the tallest item in an auto row), so its `scrollTop` is pinned at 0 for the life of the page; `position: sticky` resolves against the nearest ancestor scroll container's scrollport — which is now the article itself, not `.paper-scroll` — so the theme's sticky offsets (:443-465, re-read) never engage. Ancestor chain re-verified element by element (headers → `section.paper-section`, no overflow/contain at theme :196-202 → `.paper-window-section`, containment ≠ scrollport → `.paper-window-root` → article). Control case strengthening the mechanism: `.paper-progress-track` (:474-483) is sticky as a child of `.paper-scroll` and DOES work — stickiness dies exactly at the article boundary. This is the FIRST-order gate; banked K-11's flattener flat-peers finding is the SECOND (it kills only the two-TIER stacking once stickiness exists at all). |
| **R2-2** | **PAW-2's "no estimate guard" cell is incomplete** (LC's new kill, flagged "not in the banked register") | absent | filed | **SUSTAINED → K-19.** | My dist read :867-874: `syncMeasuredHeight` OPENS with `if (measuredHeights.get(id) === normalized) return;` — an **identity** guard preceding both `scheduleWarmRangeRelease()` and `scheduleRecalculate()`. It does not reject the 1200 estimate (PAW-2 stands whole), but it trims one consequence cell: an unchanged falsified 1200 re-read per patch does NOT re-arm the warm-release timer or the recalc — the poisoning is quiet, not self-amplifying — and :869 is the exact line where a skip-when-equal-to-the-estimate repair belongs. |
| **R2-3** | **The KaTeX memo pair** (LC missed finds 1+2) | absent | filed MINOR + INFO | **ADMITTED → PAW-45 (MINOR) + PAW-46 (INFO).** | All bytes mine: `var cache = new Map()` at dist module scope (:387); keyed `` `i:${tex}` ``/`` `d:${tex}` `` with the `macros` parameter passed to `katex.renderToString` but never entering the key; **zero eviction** (`cache.delete\|clear\|size` grep → 0 across dist/vue.js — contrast the virtualiser's own five-resource teardown at :960-967); `useKatex` has exactly one call site (PaperView :38) with a substantial macro set (:26-36), so the cross-serving collision is LATENT. Cardinality reproduced EXACTLY: 1145 unique inline spans / 2025 occurrences (my grep = LC's to the digit). Novelty sweep: this record's only cache row is PAW-28 — a different cache, different consequence. |
| **R2-4** | **Dead authored scroll clearance + scrim landing** (DU missed find 2) | filed MINOR | absent | **ADMITTED → PAW-47 (MINOR), with a seat-added coupling law (LAW-4).** | All premises re-verified mine: theme authors three clearances (:73/:197/:621 — 5rem/6rem `scroll-margin-top`); NOTHING consumes them (`scroll-margin` in web/src → 0; `scrollIntoView` → 3 hits, none on paper targets; router has NO `scrollBehavior` — my grep, closing the fragment-navigation loophole DU asserted but did not instrument); the app scrolls manually with `rawTop − getScrollOffset()` where `getScrollOffset()` = `.floating-toc-bar` offsetHeight + 8, else 16 (:22-26/:46) — at ≥lg the bar sits inside a `lg:hidden` root (:107) so both branches resolve to 8px or 16px against an authored 80/96px; the destination's first line lands inside the 32px `.paper-root::before` scrim (:425-445). Seat nuance the reader missed: the theme's 5rem was authored to clear the STICKY STACK that PAW-44 just proved dead — the two findings share one design decision (LAW-4). |
| **R2-5** | **`.interactive-callout` is the route's only hand-rolled card chassis** (DU missed find 3) | filed MINOR | absent | **ADMITTED → PAW-48 (MINOR).** | My census: `cartoon-card` consumed at style.css (+the shim :107-111 composing glass-ui `cartoon-surface` + `--border`/`--card`) and 13 component files across equation/morph/visualization — **zero under components/paper/**; the callout's own container `.paper-article` hand-rolls the same cartoon register at double weight (2px + `3px 3px 0` stamp, :522-525), so the treatise's one interactive interruption reads LESS present than the page it interrupts. Identity guard held: sole prior `interactive-callout` row is PAW-10 (contrast), and PAW-8 books the BUTTON's chassis — this is the CONTAINER's. Adopting the recipe supplies PAW-10's missing 2px boundary + stamp and retires PAW-20's `transform` shorthand via `cartoon-surface`'s individual-transform hover in the same edit. |
| **R2-6** | **`noUncheckedIndexedAccess` absent → resolveFigure's return type is false in one whole arm** (LC missed find 3) | absent | filed INFO | **ADMITTED → PAW-49 (INFO).** | My reads: tsconfig has `strict: true` and NO `noUncheckedIndexedAccess`; `FIGURE_DIMENSIONS` is `Record<string, readonly [number, number]>` (:22) so `dims` (:47) types non-nullable and `dims?.[0]` (:54-55/:58) narrows to plain `number`; but the `else` arm is reached only when the key is ABSENT (:52's Set-of-keys), so `dims` is `undefined` at runtime and width/height are `undefined` while the inferred return asserts `number`. Degrades correctly (Vue omits undefined attrs) — a type-truth defect and a SEQUENCING CONSTRAINT: PAW-12's predicate split is not type-safe until the flag lands or the return type is honest. |
| **R2-7** | **No unit harness in web/ at all** (LC missed find 4) | absent | filed INFO | **ADMITTED → PAW-50 (INFO).** | My reads: scripts are exactly `dev/build/preview/test:e2e/test:e2e:ui`; no vitest/jest in any dep block; no vitest.config; zero `*.test.ts`/`*.spec.ts` under src. PAW-12's booked F.W9/W10 set-equality rider has no home to live in — stand vitest up first or implement it as a Vite build plugin; and `resolveFigure` (pure, :44-59) is trapped in the SFC where no assertion can import it. Seat addition: `npm run build` already runs `vue-tsc -b` — the NEEDS-BUILD witness for PAW-49/K-12 exists TODAY even though the unit harness does not. |
| **R2-8** | **D-§6 uplift-divergence cell kill** (DU) | filed | absent | **SUSTAINED → K-20, with the decisive instrument added by this seat (SC-4).** | DU refuted at the ADOPTED pin (4.0.0 floor scoped to exactly three selectors, :115-122 — my read concurs). But the killed cell claimed 7.0.0 inheritance — so this seat checked the **v7.0.0 tag**: NO coarse-pointer min-size floor exists there at all; the only coarse-pointer treatment is the opt-in `@utility touch-hit-area` overlay composed by six form atoms (none of them Button), and the cell's cited :127-134 is comment prose inside that utility's header. False at both ends of the uplift. |
| **R2-9** | **K-2/S-7 phrasing** (LC sharpening vs DU's restatement) | restates "the hover lift snaps" | "over-claims by one word: there is a 1px instantaneous displacement" | **LC's precision adopted as a note on K-2/S-7.** | The PRM allow-list kills the TRANSITION, not the TRANSFORM — `translateY(-1px)` still applies, instantly. "Snaps" (banked S-7) is accurate; any repair note reading it as "no spatial motion" over-reads by 1px. No verdict moves. |

## New roster rows — PAW-44..PAW-50 (id · severity · claim · proof · terminal disposition)

| id | severity | claim (adjudicated) | proof | disposition |
|---|---|---|---|---|
| **PAW-44** (DU missed find; class cross-cite **FMD-30**) | **MAJOR** | The two-tier sticky header stack — the paper's only persistent "where am I" device — is **100% inert**: `.paper-article { overflow-x: hidden }` (PaperView :527, HEAD-stable) makes the article a scroll container in both axes (CSS Overflow 3 §3 computed-value rule); with no height constraint its `scrollTop` is permanently 0, so the theme's `position: sticky` headers (:443-465) never engage. ~24 lines of consumed theme CSS are dead; a 93-section treatise scrolls with no persistent heading in the reading column (position-tracking falls entirely to the sidebar + floating bar); and the banked record's most vivid PAW-1 arm cannot occur (K-18). Same CLASS as banked FMD-30 (`overflow-x: hidden` minting an unintended scroll container on a route root) — cross-cited, NEW instance with a distinct consequence (sticky death, not a nested scroller); no adjudicated row books `.paper-article`'s overflow. | R2-1 (chain, spec rule, HEAD bytes, the `.paper-progress-track` control case — all mine) | **→ F.W3/W4**, under **LAW-3**: candidate cheap cure is `overflow-x: clip` (clips without minting a scroll container) **but restoration MUST land WITH-or-AFTER the PAW-1 header-background cure + PAW-30's bleed handling** — restoring the scrollport first makes the killed see-through-header catastrophe REAL. The two-TIER stack additionally needs banked K-11's flattener nesting (LATEX-PAPER carry rider); restoring stickiness without it yields single-tier only. Whether the stack SHOULD return is a formation design decision — SS-13 witnesses first. |
| **PAW-45** (LC missed find) | **MINOR** | latex-paper ships a SECOND module-level, unbounded, never-evicted cache: the KaTeX memo (dist :387) retains the rendered HTML of every equation ever typeset for the page-session lifetime — surviving section unmount and route change — bounding the memory benefit of the very windowing this component exists to implement (~1145 unique inline + ~263 display spans, all mounted through this sole call path, vs ≤18 mounted sections). | R2-3 (bytes + zero-eviction sweep + exact cardinality reproduction, mine) | **→ LATEX-PAPER carry** (bound or instance-scope the memo), beside PAW-28; **NO-WAVE-OWNER locally**. Repairs must weigh superlative 13 (the memo is also what makes the PAW-3/4 churn survivable) — cap, don't delete. |
| **PAW-46** (LC missed find) | **INFO** | The same memo keys on the TeX string alone and ignores the `macros` argument the composable is parameterised by — two `useKatex()` instances with different macro sets would cross-serve rendered HTML. LATENT: exactly one call site today (PaperView :38, my grep), with a substantial macro set that makes the hazard real the day a second consumer appears. | R2-3 (key construction + sole-call-site grep, mine) | **→ LATEX-PAPER carry** (key on tex + macro identity, or move the Map inside `useKatex`), same repair as PAW-45; **NO-WAVE-OWNER locally** |
| **PAW-47** (DU missed find; class precedent PH-13 / Katex R18, INVERSE instance) | **MINOR** | The theme's authored anchor clearances are DEAD (three `scroll-margin-top` rules — 5rem/6rem — that nothing consults: no scrollIntoView on paper targets, no router `scrollBehavior`), and the app's substitute (`rawTop − getScrollOffset()`, offset = 8px or 16px on desktop, both an order of magnitude under the authored value) lands every ToC/cross-ref destination's first line inside the 32px top edge-fade scrim. Two clearance authorities, neither consuming the other. Value.js-side precedent is the same CLASS inverted (PH-13/R18: clearance absent; here it is authored upstream and never consumed) — cross-cited, not a re-book; consistent with banked K-6 (navigation is ACCURATE to its target; the TARGET is wrong). | R2-4 (all greps + both files, mine — incl. the router `scrollBehavior` → 0 check the reader asserted without instrumenting) | **→ F.W3/W4**, under **LAW-4**: one clearance authority — either `getScrollOffset()` reads the theme's constant, or `scroll-padding-block-start` on `.paper-scroll` with scrollIntoView navigation — and the CONSTANT is a dependent of the PAW-44 decision (stack restored → clearance ≥ stack height; stack retired → clear the 32px scrim and record the theme clearances as retired-with-rationale) |
| **PAW-48** (DU missed find) | **MINOR** | `.interactive-callout` is the paper route's only hand-rolled card chassis and it is WEAKER than the container it sits inside: the house `cartoon-card` recipe (style.css shim over glass-ui `cartoon-surface`) is consumed by 13 component files — zero under `components/paper/` — while the callout hand-rolls a 1px hairline + 1.011:1 tint + no shadow inside a `.paper-article` that itself hand-rolls the same register at 2px + offset stamp (:522-525). The treatise's one interactive interruption reads less present than the page it interrupts. Distinct from PAW-8 (button chassis) and PAW-10 (measured contrast); it SUPPLIES PAW-10's cure. | R2-5 (census + shim + container bytes, mine) | **→ F.W3/W4** (adopt `cartoon-card`/`cartoon-surface` on the callout — delivers PAW-10's 2px boundary + stamp, retires PAW-20's `transform` shorthand in the same edit; composes with PAW-8's Button adoption and PAW-41's radius tokens) |
| **PAW-49** (LC missed find; EXTENSION of PAW-12/PAW-26, new mechanism — not a re-book) | **INFO** | The compiler cannot police the hazard PAW-12 books: without `noUncheckedIndexedAccess`, `FIGURE_DIMENSIONS[pngName]` types non-optional, `dims?.[0]` narrows to `number`, and `resolveFigure`'s inferred return asserts `width: number` while the `else` arm ALWAYS yields `undefined` at runtime (reached only when the key is absent, by :52's construction). The author's defensive `?.` is erased by configuration; `vue-tsc` is green over a return type false in one whole arm. | R2-6 (tsconfig + types + arms, mine) | **→ F.W3/W4 rider on the PAW-12 cure** (the predicate split is not type-safe until the flag lands or the return is honestly `number \| undefined`) **+ F.W9/W10** (the flag itself is a repo-config decision) |
| **PAW-50** (LC missed find; EXTENSION of PAW-34 + PAW-12's rider) | **INFO** | web/ has NO unit-test harness at all (five scripts, no vitest/jest, zero test files) — the build-time set-equality assertion PAW-12's disposition books has no home: the rider must stand vitest up first or ship as a Vite plugin. `resolveFigure` is additionally trapped in the SFC, unimportable by any assertion; it belongs beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts`. Seat addition: `npm run build` already carries `vue-tsc -b`, so the PAW-49/K-12 type witnesses need no new harness. | R2-7 (package.json + finds, mine) | **→ F.W9/W10** (sequencing input: harness-before-rider; move `resolveFigure` to the lib in whatever wave executes PAW-12) |

## Killed-claims register — additions K-18..K-20 (register 17 → 20)

| # | killed claim | adjudication |
|---|---|---|
| **K-18** | **D-B-1/L-1's sticky-scroll consequence rows** ("body copy and KaTeX display blocks scroll visibly underneath the see-through heading for the entire treatise" — challenge-D :47-48 table row; challenge-L :51 row) **including the same arm inside banked PAW-1** ("(:447/:459 — body copy scrolls visibly under them)") and banked UNPROVEN-4's "scrolled chapter header" witness | **KILLED (reader-2DU find, seat-ratified at the bytes, R2-1)**: the headers never stick (PAW-44), so nothing can scroll beneath them — the :447/:459 background drops become LATENT (visible only if stickiness is restored; in normal flow a transparent header over the card ground is visually identical to an opaque `--card` one). The 42-drop mechanism and the other seven consequence arms are UNTOUCHED — **PAW-1 remains BLOCKER**. Re-grades banked **K-11**: its flattener flat-peers explanation was presented as THE reason the two-tier stack fails; it is the SECOND gate — the scrollport is the first, and a repair reworking the flattener (or wrapper) alone ships a no-op. INTERLOCK (LAW-3): a repair restoring the scrollport WITHOUT the PAW-1 background cure would make this killed arm REAL. |
| **K-19** | **Banked PAW-2's instrument cell "syncMeasuredHeight no estimate guard" as stated, and L-2's consequence-4** ("churn in these values also extends the forced window") | **TRIMMED (reader-2LC, seat-verified, R2-2)**: an IDENTITY guard exists at dist :869 and short-circuits before both schedule calls — true, it does not reject the estimate (PAW-2's mechanism whole), but steady-state re-reads of an unchanged 1200 re-arm NOTHING: the poisoning is quiet, not self-amplifying, and consequence-4 fires only while heights are actually changing. Repair surface sharpened: the skip-when-equals-estimate write belongs at :869, not at the axis's cited byte. |
| **K-20** | **D-§6's uplift-divergence cell** — the GalleryCardModal twin "will inherit … the coarse-pointer 44px floor (a11y-overrides.css:127-134)" from 7.0.0 | **KILLED (reader-2DU at the adopted pin; seat-extended at the claimed tag, R2-8/SC-4)**: at 4.0.0 the floor is scoped to exactly three selectors, none Button; at **v7.0.0 no floor rule exists at all** — the coarse-pointer treatment is the opt-in `touch-hit-area` utility composed by six form atoms, and the cited :127-134 is its comment prose. Not a row of this roster — a correction to a supporting cell of the **fr-GalleryCardModal F.W1 ledger**, recorded here because this corpus carried it. (The hand-rolled pill clears 44px on its own arithmetic regardless — DU's supplementary cell, seat-sanity-checked.) |

**Ratifications**: every banked kill the round-2 readers re-derived (DU: K-3/K-6/K-7/K-9/K-11/K-15/K-16/K-17 + the 640px measure; LC: K-1..K-7/K-11..K-17 + the 11.786 ratio) reproduced under their independent instruments, and every one this seat spot-checked lands. The readers' PAW-6 emphasis difference (DU: the f21 white-slab arm is the strong one; LC: f20 is 3-of-4 correct under inversion) is COMPLEMENTARY — both feed banked K-17's no-whole-figure-boolean constraint; both adopted.

## Banked-record re-scopes (by reference — the 2026-08-24 bytes stand)

1. **PAW-1**: consequence table re-inked — the sticky-scroll arm is K-18-dead; the :447/:459 drops re-file as LATENT-behind-PAW-44. Severity **BLOCKER sustained** on the seven live arms (figure borders, figcaption ink, `.paper-ref` colour+underline, proof rails, placeholder, `.section-body` tone, PAW-13's hover bar). Witness list per SS-13 revision below.
2. **PAW-2**: instrument cell corrected per K-19; mechanism, severity (MAJOR), coupling law, and disposition unchanged.
3. **PAW-30**: now DOUBLY latent — behind PAW-1's cure AND PAW-44's scrollport decision; its SS-13 witness is post-repair only. Sequencing unchanged (lands WITH PAW-1) with LAW-3 layered on top.
4. **K-11**: re-graded from "the reason the stack fails" to "the second of two gates" (see K-18). Its cure-kill force is unchanged.
5. **UNPROVEN-4 (banked)**: the "scrolled chapter header" witness is STRUCK until PAW-44 is decided (it presupposes engagement that does not occur).

**Sequencing laws added** — **LAW-3**: PAW-44 restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before. **LAW-4**: PAW-47's clearance constant is a dependent of the PAW-44 decision; one clearance authority only.

## Superlatives — additions 11..14 (provenance marked)

11. **The footer spacer's `clamp(3.5rem, 8vh, 5rem)` is the right shape** — bounded viewport-relative degradation (56→80px) where a raw `8vh` would scale linearly; the corpus criticised the row three times and credited it zero (reader-2DU; consistent with this record's banked reads). Missing only the SAI term (PAW-31).
12. **`throwOnError: false` at both KaTeX render sites is the substrate closing the error-posture hole the component leaves open** — sole mount point for the whole paper body, no `onErrorCaptured`, no try/catch; a malformed macro degrades to KaTeX's red-text fallback instead of blanking the app. Recorded as a COUPLING fact: a latex-paper change to that flag turns a content typo into a blank page (reader-2LC; seat byte-verified both branches).
13. **The KaTeX memo counterweight**: the cache filed as PAW-45 is simultaneously what makes the PAW-3/PAW-4 churn survivable — each of the 1145 unique spans typesets exactly once per session; every fresh-arrow remount's `innerHTML` is an O(1) hit thereafter. Both facts are true and the repair must weigh them together — cap or scope the cache, never delete it (reader-2LC; cardinality reproduced exactly by this seat).
14. **Round-2 reader discipline** (seat's own): both readers reproduced the banked instruments independently — the WCAG ratios to three decimals, the 42-count, the 640px measure, the raster reads — and EACH landed a kill on the record/corpus itself rather than only on the axes (DU on the banked BLOCKER's arm; LC on a banked instrument cell). The strongest evidence yet that the surviving cells are load-bearing.

## Seat corrections against the round-2 readers

- **SC-4** (reader-2DU): the D-§6 kill was refuted at the 4.0.0 pin only, while the killed cell claimed 7.0.0 inheritance — the decisive instrument (the v7.0.0 tag showing NO floor exists) was this seat's. Kill unchanged; provenance corrected.
- **SC-5** (reader-2LC): "263 display environments" — this seat's coarse pattern reads 266 (`\\[`-spacing false-positives one way, environment coverage the other); magnitude confirmed, cardinal not load-bearing, LC's figure adopted with the tolerance noted.
- **SC-6** (reader-2LC): the novelty sweep cell "grep katex → only PAW-33, PAW-35 and superlative 2" under-reports — the sweep also hits PAW-7's caption cell and superlative 2's KaTeX mention (my re-run). The novelty CONCLUSION is unchanged (no cache row exists); sweep-precision nit, recorded because sweeps are what the identity guard runs on.
- (Noted, not a correction: reader-2DU asserted "no fragment navigation" without instrumenting the router; this seat's `scrollBehavior` → 0 grep closed that loophole — the finding was right, the falsifier was unrun.)

## UNPROVEN-NEEDS-LIVE / NEEDS-BUILD — SS-13 inputs, revised

1. **PAW-44 observation — runs FIRST, before the PAW-1 repair is scoped**: on `/paper`, `getComputedStyle(document.querySelector('.paper-article')).overflowY === 'auto'`, and a scrolled chapter's `.section-header--chapter` translating with the flow instead of parking at `top: 0`. Decides how much of PAW-1's witness list survives and whether PAW-30 is reachable at all. Mechanism is spec+source-derived to this record's usual standard; the live check is confirmation, not derivation. → SS-13.
2. **PAW-47 branch magnitude**: whether desktop jumps resolve the 8px or 16px branch (IntersectionObserver-driven mount state of the `lg:hidden` bar at the jump instant). Both branches land inside the 32px scrim — a magnitude, not a mechanism. → SS-13.
3. **PAW-1 pixel grading — witness set REVISED**: STRIKE the scrolled-chapter-header witness (K-18); keep a `.paper-figure` in both schemes, a `.paper-ref` at rest AND hovered (PAW-13's 3px bar), a proof block, a figcaption. → SS-13.
4. **PAW-3/PAW-4/PAW-45 cost — one dark-mode scroll trace covers all three**: the per-patch remounts (incl. LC's sharpening: whether remounts abort in-flight decodes of the 2.85MB raster and defeat the compositor's `invert()` cache), the ≤18 forced layouts per frame, and the memo's session-resident footprint after a full read. → SS-13.
5. **NEEDS-BUILD (exists today)**: one `npm run build` (carries `vue-tsc -b`) witnesses PAW-49's type-lie inference and banked K-12's dead-import prediction. Cheap; no new harness required (PAW-50 notes the unit harness is what's missing, not this).
6. Banked items 1-3 and 5-7 (PAW-2 engine detail, PAW-22 order, PAW-31 magnitude, PAW-30 post-repair, PAW-15 AT residue) carry forward unchanged.

## DISSENT (round 2) — preserved, not ruled away

1. **Reader scorecard.** reader-2DU: the round's decisive find (PAW-44 — the first-order gate missed by all three axes, both round-1 readers, AND the banked seat, whose own K-11 substituted the second gate for it); the K-18/K-20 kills; the 640px measure independently re-derived to this record's corrected figure. Debits: refuted a 7.0.0 claim at the 4.0.0 pin (SC-4); asserted the router premise without the grep. reader-2LC: the only round-2 kill landing on a banked instrument cell (K-19); the KaTeX memo pair with exact cardinality; the two sequencing facts (PAW-49/50) that keep F.W9/W10 honest; the counterweight superlative — filing a defect and its own mitigation as one coupled record is the register at its best. Debits: SC-5, SC-6. Both readers: zero identity-guard violations — seat-swept and confirmed.
2. **Humility row (fleet evidence)**: the banked seat read theme :443-466 AND PaperView :520-585 in one session and did not connect :527 to the sticky rules — the connection required holding one CSS computed-value rule against two files simultaneously. Consequence-arm verification ("does the vivid arm's PRECONDITION hold?") belongs in the kill checklist beside the cure-adjudicated-separately law; K-18 is what it looks like when it's missing.
3. **Design dissent left open for the formation**: whether the sticky stack SHOULD return at all. DU's consequence (b) notes the column has other position-tracking (sidebar, floating bar, progress track — the last seat-verified working); a formation may legitimately retire the stack, cure PAW-1's other arms, and record theme :443-465 as dead-by-decision. LAW-3/LAW-4 are written to be valid under EITHER outcome.
4. **Banked grade dissents carry forward unchanged** (LC-r1's MINORs on PAW-12/PAW-16; the a11y-cluster escalation note on PAW-15).

## Closing verdict (round 2)

**ADJUDICATED — the record survives its third hostile reading larger and truer: roster 43 → 50 (1 BLOCKER · 16 MAJOR · 17 MINOR · 16 INFO), kills 17 → 20, and for the first time the kills reach the banked record itself — the BLOCKER's most vivid consequence arm falls to a scrollport derivation (the sticky stack never engaged: `overflow-x: hidden` at PaperView :527 makes `.paper-article` its headers' own dead scrollport), PAW-2's guard cell is trimmed at the byte, and a GalleryCardModal F.W1 ledger cell is corrected at both ends of the uplift. PAW-1 stands BLOCKER on seven arms; PAW-44 (the inert stack) enters at MAJOR under LAW-3 (restore only WITH the background cure — the killed catastrophe otherwise becomes real); the KaTeX memo pair, the dead clearance authority, and the cartoon-card gap enter with their cures constrained by the record's own counterweights; and two INFO rows keep the F.W9/W10 sequencing honest (harness before rider, flag before split). Seven admitted finds, three new kills, four superlatives, zero re-books — every verdict-deciding cell re-derived at the bytes by this seat this session. Routings remain M-25 agglomeration input; nothing here stamps a wave.**

— end of round-2 addendum. The banked apotheosis above, the three challenge files, and both round-2 reader returns remain immutable per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED.

---

# ROUND 3 — third adversarial re-read, adjudicated (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator, round 3 · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited (the file's line 1 stands — same served id, independently observed).
**Date**: 2026-08-25 · **E-3 posture**: the 2026-08-24 record and the round-2 addendum above are dated evidence and are NOT rewritten. Round 3 is the round in which kills land on a banked **instrument cell of the round-1 seat itself** (K-25) and on **two limbs of a banked disposition** (K-22/K-23). Corrections live HERE; the banked bytes stand. Sole write = this file.
**Corpus adjudicated**: two hostile re-readers of the full challenge corpus AND the two-round banked record — **reader-3DU** (design-and-usage: 13 confirmation cells · 7 kills/folds · 3 missed finds · 5 superlatives · 4 unproven) and **reader-3LC** (library-and-code: 12 confirmation cells · 5 kills/folds · 4 missed finds · 6 superlatives · 4 unproven). Confirmation cells that re-derive banked rows (PAW-1..PAW-8, PAW-12, PAW-16, PAW-18, PAW-24, PAW-33/34/37, PAW-44, K-1..K-7, K-13, K-16/K-17) are adopted as ratification, not re-adjudicated — this addendum rules only what is NEW, DISPUTED, or lands ON the record.
**Method (incredulity)**: every round-3 kill re-derived at the bytes before sustaining; every admitted missed find re-proven with this seat's own instruments; one reader kill re-scoped where the seat found a sound salvage the reader under-credited (K-22); one reader disposition clause corrected as over-claimed (SC-10); the challenge files' killed cells re-read verbatim at their own line numbers before any kill was accepted. Zero browser probes (probe parsimony); product source read-only.
**Substrate re-checks (this seat's own, this session)**: fourier HEAD still `cd26c65`; the tree is **in motion** — `git status --short` → **28** dirty rows at this seat's read (reader-3LC saw 25), and `git diff web/package.json` shows an uplift WIDER than the tri-package cell: `@mkbabb/glass-ui ^3.1.0→^4.0.0`, `keyframes.js ^2.2.0→^4.3.0`, `value.js ^0.10.0→^0.13.0`, `pencil-boil` pinned `^0.4.1`, plus the katex/vite/vue-router rows reader-3LC reported. The subject file remains clean; anchors are working-tree anchors (D-19: re-resolve before any cure).
**This seat's own instruments (all static + git greps, this session)**: theme.css `.theorem-block` base :66-83 read at the bytes (**`border-left-width: 3px` :70 / `border-left-style: solid` :71 — var-free longhands, NO `border-left-color` in the base rule**; `transition … border-color 0.3s ease` :75-ish rides along); variant colour rules :111-115 (`--theorem/--proposition/--corollary { border-left-color: hsl(var(--primary)) }`), :117-120 (`--definition/--example` → `hsl(var(--accent-pink))`), :122-125 (`--lemma/--aside` → `hsl(var(--muted-foreground))`); label rules :131-145 (same three, `color:`); `.theorem-type` :147-150 (`font-weight: 700; font-variant: small-caps` — the sole surviving discriminator); `grep -n '_section-color' theme.css` → **7 lines: :6 (header-comment prose) + 6 code hits :476/:481/:493/:498/:511/:512 — every code hit inside the `.section-header--*` rules, ZERO in `.theorem-block`** (the K-25 decider); TeX census, my own greps: `\begin{theorem}` 35 + `definition` 38 + `example` 14 + `aside` 4 + `corollary` 1 (+ lemma 0, proposition 0) = **92** theorem-family blocks, `\begin{proof}` 30 (the banked D-m-6 arm), `^\chapter{` → **11**; tokens re-read: `--primary` light `hsl(24 10% 10%)` (color-radius :98), dark `oklch(0.739 0.134 318.1)` (dark-arm :82), `--accent-pink` `oklch(0.613 0.197 353.8)` :256 / dark `oklch(0.683 0.131 354.7)` :109, `--section-color-0..12` = **13 stops** :241-253 (`--viz-amber: var(--section-color-5)` :266 corroborating the house convention); body-ink chain: `.section-body` theme :517 + style.css `@layer base` `body { @apply … text-foreground }` :17-21; dist/vue.js: **`containerResizeObserver` declared :811, constructed+observed :913-917 (callback = `scheduleRecalculate()` on the CONTAINER), disconnected :911/:965** (the K-21 decider); `syncMeasuredHeight` :867-874 (identity guard at :869, per K-19); `--_section-color` bind :1404 — **no fallback argument, no modulo**; `theorem-block--${$props.type}` emitted :1201; `figureId`/`anchorId` present (10 hits — the id substrate for PAW-52 leg 1); @vue/runtime-core **esm-bundler** :1818 `if (isFunction(ref)) { callWithErrorHandling(…) }` — BEFORE and OUTSIDE the `doSet` post-flush path, old-ref teardown string/isRef-only (the PAW-4b survival decider); component re-reads: `baseUrl = import.meta.env.BASE_URL` :19; `resolveFigure` :44-59 whole (pure over module constants; `dims?.[0]` both arms); `.paper-window-root { min-width: 0 }` :137-139; `.paper-window-section` `min-width: 0` :142 + token :167; `.paper-window-spacer` :170-172 (**`width: 100%` dead, but `pointer-events: none` beside it is LIVE** — seat scope-check on PAW-57); `.interactive-callout` :179-186 (`--border` :182, `--muted` mix :184); `.callout-btn` :188-201 — **NO border declaration anywhere in the rule**; token sites :195/:196/:200 + `:hover` :203-206 (**`var(--primary)` again at :205**); `grep -c 'hsl(var('` → **0**; `grep -c dark` → **0**; glass-ui a11y-overrides `@media (forced-colors: active)` ~:78-104: outline restore for TEN focus selectors + `border: 1px solid CanvasText` for `.hairline-accent, .glass-dock` ONLY — `.callout-btn` matches none; `as: { default: "button" }` present in dist (real-`<button>` default corroborated); style.css :1-3 (`@import "tailwindcss"` first) + `.katex-display` :64-69 verbatim (`overflow-x: auto; overflow-y: visible; padding: 0.75rem 0`); `grep -c '@layer' theme.css` → **0** (the K-27 decider — the whole 644-line theme is unlayered against Tailwind-v4-layered utilities); deep-link sweep: `grep -rnE 'location\.hash|route\.hash|\.hash\b|scrollBehavior|:target' web/src` → **0**, router = bare `createWebHistory` (~:37), position persistence = `sessionStorage` only (`SCROLL_POS_KEY` :65/:137-138/:266), e2e budget 18 (:3); challenge cells re-read verbatim before killing: challenge-D **:80** ("there is no `ResizeObserver`") + **:86** falsifier (c) + **:89** cure ("Two lines, no new dependency"); challenge-C **:25** (§0 inventory cell) + **:244** (§6 inner-wrapper limb); challenge-L :177-190 (L-8, `:53` cite — actual `:52`, the banked figure); registry novelty sweeps re-run over this whole record: `theorem-block` → exactly 2 hits, BOTH the PAW-13 cells K-25 kills; `forced-colors`/`math-inline`/`katex-display`/`deep-link`/`permalink` → **0**.

## Scoreboard (round 3)

**Roster 50 → 57** (1 BLOCKER · 18 MAJOR · 19 MINOR · 19 INFO): **7 new rows admitted** (PAW-51..PAW-57), every one re-proven at the bytes by this seat. **Killed register 20 → 27** (K-21..K-27) — and the kills now reach deepest yet: **K-25 kills a banked instrument cell of the round-1 seat** (the longhand-sweep's ":70-71 un-paired, rail survives via `--_section-color`" — false on both limbs, and the falsity suppressed a 92-block find for two rounds), and **K-22/K-23 strike two of banked PAW-2's three disposition limbs** (the literal CV-event two-liner and the inner-wrapper relocation), leaving the K-19-located write-suppression as the primary cure. PAW-1 gains a new ARM CLASS (wrong-colour, not absent — PAW-51). One sequencing law added (LAW-5); 5 superlatives added; 4 seat corrections against the round-3 readers. **PAW-4b survives its first serious kill attempt** (reader-3LC attacked it and refuted itself at the bytes — the strongest confirmation a row can get).

## Ruled items (this seat's actual work, R3-1..R3-6)

| # | Item | reader-3DU | reader-3LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| **R3-1** | **The theorem-taxonomy colour-erasure + the kill of the banked sweep cell** (LC missed find 1 + LC kill 1) | absent | filed MAJOR + kill | **ADMITTED → PAW-51 (MAJOR) + K-25.** | Re-derived whole at the bytes (instruments above): the base rule's two var-free longhands (:70-71) SURVIVE the IACVT collapse while all six companion colour rules (:111-125 rails, :131-145 labels) are IACVT and drop to initial `currentcolor` / inherited body ink; `--_section-color`'s six code hits are ALL in `.section-header--*` — the banked cell's "rail survives via `--_section-color`" is false, and its "un-paired" is an artifact of a same-rule pairing predicate that cannot see a pairing 40 lines away. Census 35+38+14+4+1=**92** reproduced exactly. This is the PAW-13 CLASS (survivor + dropped companion = wrong visual, not absent) at ~30× the instance count — the failure mode PAW-13's uniqueness cell asserted could not recur. |
| **R3-2** | **The PAW-2 cure-cluster kills** (DU kills 1-3: the "no RO" falsifier, the two-line CV-event cure, the inner-wrapper limb) | filed all three | absent | **K-21 SUSTAINED · K-22 SUSTAINED-AS-SCOPED with a seat salvage · K-23 SUSTAINED.** | K-21: `containerResizeObserver` exists at dist :811/:913-917/:965 — challenge-D :80/:86(c) is false at the bytes; the conclusion survives corrected (the RO observes the CONTAINER and its callback re-sums the already-poisoned cache via `buildSectionLayout`; nothing re-measures a section) — banked PAW-2's own cell ("no re-measure on paint; no CV-state listener") remains TRUE as worded, so this kill lands on the challenge, not the record. K-22: the two-line cure as prescribed (D :89 verbatim) is unsound — (a) the only per-element attachment point the component owns is the :73 function ref, invoked every patch (PAW-4a), and `addEventListener` with a per-invocation fresh arrow grows the listener set without bound on the scroll path; (b) the cure is ADDITIVE — it leaves the ref-driven unconditional `measureSection` in place, so the mount-time rAF write of 1200 lands regardless of event timing (the reader's event-never-fires sub-arm stays UNPROVEN and is not load-bearing). **Seat salvage the reader under-credits**: a SUBSTITUTIVE gate (the ref stops measuring; an identity-stable listener attached once per element with teardown, measuring only on `!skipped`) closes the headline case under EITHER event-timing branch — but that is not "two lines", and the simplest sound repair remains the write-side suppression at dist :869 (K-19's site). K-23: relocating `.deferred-section` to an inner wrapper is a structural no-op — the skipped inner box sizes to the 1200px substitute, the outer measured box's content height is the sum of its in-flow children, and `offsetHeight` on the outer still reports the estimate; no ancestor of a size-contained box escapes its substituted size. |
| **R3-3** | **Zero deep-link/citability surface** (DU missed find 1) | filed MAJOR | absent | **ADMITTED → PAW-52 (MAJOR) + LAW-5.** | All three legs re-derived mine: (1) the id substrate is complete (dist emits `id: $props.id` per section :1402-1406; `figureId`/`anchorId` live in dist); (2) NOTHING consumes a fragment — my sweep `location.hash|route.hash|\.hash|scrollBehavior|:target` over web/src → 0, router is bare `createWebHistory`; (3) nothing emits one — persistence is `sessionStorage` only (:65/:137-138/:266). The component is the BLOCKING mechanism (18-of-93 budget → ~81% of targets absent from the DOM at any instant, so even a supplied fragment finds nothing; any cure routes through `ensureTargetWindow`). Novelty verified: banked PAW-47 uses router-no-scrollBehavior only as evidence for dead clearances; PAW-15 books AT/find-in-page absence; PAW-38 books slug collisions; K-4 books href-less refs — no row books URL citability. In-app navigation working (K-6) is exactly what masks it. |
| **R3-4** | **Forced-colors chassis loss on the CTA** (DU missed find 2) | filed MINOR | absent | **ADMITTED → PAW-53 (MINOR), disposition seat-corrected (SC-10).** | The rule bytes: `.callout-btn` :188-201 declares NO border — silhouette rides `background` + `box-shadow` exclusively, both stripped under `forced-colors: active`; glass-ui's shipped rung matches none of its eleven selectors (outline restore is :focus-visible-only; border restore is `.hairline-accent, .glass-dock` only — my read); the twin survives as a real `<button>` (ButtonFace/ButtonText + UA border). Scope honest: focus outline survives (style.css :140 `outline` is forced-colors-durable) — resting-state loss only. **Correction**: "dies inside PAW-8's Button adoption" over-claims — the as-child cure keeps the `<a>` (banked superlative 6), and neither the solid chassis nor the rung follows automatically; the repair needs its own limb (a real border, or the glass-ui rung extended to the adopted class — GLASS-UI relay note). Rendered result → SS-13. |
| **R3-5** | **The `resolveFigure` identity kill and the cascade-layers kill** (LC kills 2-3) | absent | filed both | **K-26 SUSTAINED · K-27 SUSTAINED-AS-SHARPENED.** | K-26: `resolveFigure` :44-59 is pure over `baseUrl` (:19, a Vite build-time constant) + two module constants — the seven per-render objects are value-identical by construction, and every bound prop is a primitive (string/number), so `===` prop diffing sees equality regardless of object provenance: C-M-4's "different object identities → patcher sees fresh strings" carries no correctness hazard, and the count is 7 only for mapped figures (v-if compiles to a ternary → 5 on the unmapped path). PAW-18 stays a pure-waste MINOR; no repair may be sequenced as if data integrity depended on it. K-27: the decider for the inert `rounded-lg` is CASCADE LAYERS (theme.css: 0 `@layer` in 644 lines = unlayered; Tailwind v4 emits utilities into `@layer utilities`; unlayered normal beats layered normal at ANY specificity) — with the seat's precision note: the banked parenthetical's specificity comparison also holds for the selectors as they stand, so both mechanisms currently point the same way; the LAYER fact decides the counterfactual and bans the specificity lever as a repair. |
| **R3-6** | **The remaining three missed finds** (LC 2-4, DU 3) | filed 1 | filed 3 | **ADMITTED → PAW-54/PAW-55/PAW-56/PAW-57.** | Each verified whole with this seat's instruments (see the roster rows); the only scope adjustments: PAW-54's `.math-inline` anchor is :60-62 (SC-9), and PAW-57 must not touch `pointer-events: none` (:172), which is live and correct beside the dead `width: 100%`. |

## New roster rows — PAW-51..PAW-57 (id · severity · claim · proof · terminal disposition)

| id | severity | claim (adjudicated) | proof | disposition |
|---|---|---|---|---|
| **PAW-51** (LC missed find; PAW-13's class at ~30×; root = PAW-1; kills the banked sweep cell via K-25) | **MAJOR** | The treatise's theorem taxonomy is colour-erased across **92 blocks** — and unlike PAW-1's other arms the rail does not disappear, it ships in the WRONG colour: `.theorem-block`'s var-free `border-left-width: 3px; border-left-style: solid` (:70-71) SURVIVE while all three variant `border-left-color` rules (:111-125) and all three `.theorem-label` colour rules (:131-145) are IACVT → initial `currentcolor` → body ink. The three-way semantic colour system (theorem/proposition/corollary = primary · definition/example = accent-pink · lemma/aside = muted) collapses to ONE uniform body-ink rail + label in both schemes; light mode is accidentally forgiving (`--primary` ≈ near-black ≈ body ink → 36 of 92 near-correct; 56 visibly wrong), dark mode is total (authored lavender/pink/mid-grey all ship as body ink — 92 of 92 wrong, 92 of 92 semantically flattened). Sole surviving discriminator: the small-caps label WORD (:147-150). The 30 proof blocks correctly lose their rail entirely (banked D-m-6 arm — different failure shape, which is exactly why a proof-block witness cannot stand in for this one). | R3-1 (all bytes + census mine) | **→ F.W1 ledger rider + LATEX-PAPER carry — the SAME 42-site rewrite as PAW-1** (the six rules are among the 42); books NO new repair, but **MUST enter PAW-1's SS-13 witness set as its own witness**: one `theorem` + one `definition` + one `aside` side by side in BOTH schemes — three rails that must differ and will not. A figure or proof-block witness cannot show it (those arms go ABSENT; this one goes WRONG). |
| **PAW-52** (DU missed find; composes with PAW-38, PAW-47, banked K-4, PAW-15) | **MAJOR** | A public academic treatise with **zero URL addressability**: 93 sections, 26 figures and the numbered equations all carry stable DOM ids (producer substrate complete), and nothing in the app consumes OR emits a fragment — no `scrollBehavior`, no hash read, no copy-link affordance; position persistence is same-tab `sessionStorage` only. No section, theorem, figure or equation can be cited, bookmarked or shared by URL. The virtual window is the BLOCKING mechanism: 18-of-93 mounted → ~81% of fragment targets absent from the DOM at any instant, so even native UA fragment scrolling is structurally impossible without routing through `ensureTargetWindow`. In-app `\ref` navigation works (K-6) and masks the gap — a reader lands correctly and never notices the destination has no address. Composes: PAW-38 (ids are un-deduplicated slugs — must be unique before URLs point at them), banked K-4 (href-less `.paper-ref` emitters — "copy link address" fails from inside the document too), PAW-47/LAW-4 (a fragment landing needs the one clearance authority). | R3-3 (all sweeps mine) | **→ F.W3/W4** (router `scrollBehavior` + hash wiring through `ensureTargetWindow` + hash emission on active-section change — an app+component repair) **+ LATEX-PAPER carry riders** (PAW-38 slug uniquification; K-4 real `href`s), under **LAW-5** below. Whether equations get share affordances is a formation design decision; sections and figures are the floor. |
| **PAW-53** (DU missed find; a FOURTH missed interaction contract beside banked PAW-20's three) | **MINOR** | Under `forced-colors: active` the paper's only CTA loses its entire resting silhouette: `.callout-btn` carries its chassis exclusively in `background` (:196) + `box-shadow` (:200) — both stripped by forced colors — and declares **no border**, so the solid pill degrades to bare LinkText in the flow; `border-radius` on a fill-less, border-less box is invisible. The divergent twin survives by construction (real `<button>` → ButtonFace/ButtonText + UA border), making the two `.callout-btn` definitions diverge under WHC too. glass-ui's shipped forced-colors rung reaches neither: outline restore is focus-visible-only, border restore is `.hairline-accent, .glass-dock` only. Scope: resting-state loss only — the focus ring (style.css :140, `outline`) survives. | R3-4 (rule bytes + rung selectors mine) | **→ F.W3/W4 rider on PAW-8** — but NOT discharged by it automatically (SC-10): the adopted chassis needs a real border under WHC or the glass-ui rung extended to it (**GLASS-UI BH relay note** — the rung's enumerated-selector shape is the same gap-class K-20 documented at v7). Rendered result → SS-13. |
| **PAW-54** (LC missed find; cross-cites PAW-44 — same declaration, distinct mechanism+consequence) | **MINOR** | Inline math has **no horizontal escape**: `.math-inline { white-space: nowrap }` (theme :60-62) forbids wrapping; theme.css's only overflow declarations are :33/:34 (`.math-block`) and :238 (`.paper-code-pre`) — `.math-inline` gets nothing; and the enclosing `.paper-article`'s `overflow-x: hidden` (PaperView :527, the PAW-44 declaration) CLIPS on that axis with no scrollbar and no reachable scroll affordance. Any of the ~1145 unique inline spans wider than the 640px measure (K-7) is silently truncated. The tell: the author diagnosed horizontal overflow for `<pre>` at ≤640px (PaperView :546-549) and never revisited nowrap math. | R3-6 (grep census + bytes mine; anchor corrected SC-9) | **→ F.W3/W4**, rides the PAW-44 decision (LAW-3's scrollport question decides whether the x-axis clips or scrolls) **+ LATEX-PAPER carry note** (`.math-inline` needs an overflow posture of its own or permission to wrap at operator boundaries). Which spans actually exceed 640px → SS-13. |
| **PAW-55** (DU missed find; THIRD instance of the PAW-44/FMD-30 computed-value class on this route) | **INFO** | `.katex-display { overflow-x: auto; overflow-y: visible }` (style.css :64-69) — per CSS Overflow 3 §3 the authored `overflow-y: visible` computes to `auto`: a dead declaration, and every display-math box (~263-266 on the route, all entering through this component's sole `PaperSectionBlocks` call) is a block-axis scroll container clipping tall KaTeX ink at its padding box, with the undeclared 0.75rem block padding as the only headroom. Route sweep: three `overflow-<axis>`-with-partner-unset sites — :65-66 (this), PaperView :527 (PAW-44), PaperView :546-549 (`pre`, low consequence); `.paper-scroll` sets both axes and is correctly intentional. | R3-6 (bytes mine; :64-69 falls outside every range the prior rounds read) | **→ F.W3/W4** (declare the intended pair explicitly — `overflow-y: hidden` + sufficient padding, or `clip` with `overflow-clip-margin` — a one-rule fix in the file the repair already touches). Whether any specific equation clips → SS-13. |
| **PAW-56** (LC missed find; EXTENSION of PAW-1's ramp cell — new mechanism, not a re-book) | **INFO** | The `--section-color` ramp has exactly ONE spare stop and its overflow is silent and total: `PaperSection` binds `--_section-color: var(--section-color-${sectionIndex})` with **no fallback and no modulo** (dist :1404); 13 stops (:241-253) vs 12 top-level entries (11 chapters + the leading Introduction). A thirteenth entry makes `--_section-color` guaranteed-invalid → every `var(--_section-color, hsl(var(--section-heading)))` consumer substitutes the fallback → which is ITSELF IACVT (`--section-heading` is oklch) → all six heading-colour/gradient declarations (:476-:512) drop at once, that chapter's headings ship at body ink, and nothing signals. Banked PAW-1 records the 13-vs-12 margin as the reason the sites SURVIVE; the double-failure mechanism when they do not (guaranteed-invalid → fallback → second IACVT) was unbooked. | R3-6 (dist bind + stop census + chapter census mine) | **→ LATEX-PAPER carry** (modulo at the producer bind, or a fallback that is not var-shaped), riding the PAW-1 rewrite; **NO-WAVE-OWNER locally** (latent, margin-1) |
| **PAW-57** (LC missed find) | **INFO** | Three dead defensive declarations in the 78-line scoped block: `.paper-window-root { min-width: 0 }` (:137-139), `.paper-window-section { min-width: 0 }` (:142) and `.paper-window-spacer { width: 100% }` (:171) are all no-ops — `min-width: 0` differs from `auto` only for flex/grid ITEMS and both wrappers are plain in-flow block boxes (`.paper-article` declares no `display`); `width: 100%` on a margin-less border-less block ≡ `auto`. The live guards are elsewhere (`.paper-article` :521 and `.paper-grid` :559, genuine grid items). Cargo-culted guards flip from no-op to load-bearing the day someone adds `display: flex` to the root — unexercised code in a file whose every other length is documented. Seat scope-check: `pointer-events: none` (:172) beside the dead `width` is LIVE and must survive any cleanup. | R3-6 (bytes mine, both files) | **→ F.W3/W4** (delete-or-comment in whatever wave touches the file; zero behavior change today) |

## Killed-claims register — additions K-21..K-27 (register 20 → 27)

| # | killed claim | adjudication |
|---|---|---|
| **K-21** | **Challenge-D §B-2's "no ResizeObserver" prose (:80) and falsifier (c) (:86: "no RO … anywhere in either tree")** | **KILLED (reader-3DU, seat-verified at the bytes)**: `containerResizeObserver` is declared, constructed, `.observe(container)`'d and disconnected at dist :811/:913-917/:911/:965. The finding's CONCLUSION survives on the corrected mechanism — the RO observes the scroll CONTAINER and its callback (`scheduleRecalculate` → `buildSectionLayout`) re-sums the already-poisoned `measuredHeights`, never touching a section's `offsetHeight` — so "nothing re-measures a section" stands. Repair-integrity kill: an author following D's falsifier would add "the missing RO" as the cure; one exists and is provably insufficient — the repair must be per-section (or the write-side suppression). Banked PAW-2's own cell is correctly worded ("no re-measure on paint; no CV-state listener") and is NOT touched. |
| **K-22** | **Challenge-D §B-2's prescribed cure (:89, "Two lines, no new dependency") AND banked PAW-2's disposition limb 1 as read literally** ("gate `measureSection` on the CV-state event") | **CURE-KILL SUSTAINED-AS-SCOPED (reader-3DU + seat salvage, R3-2)**: as prescribed the cure is unsound twice over — the per-patch function-ref attachment point leaks a fresh listener per patch on exactly the path PAW-4 books, AND the cure is additive (the unconditional ref-driven measure keeps writing 1200 regardless of event timing; the event-never-fires-on-mount-skipped sub-arm stays UNPROVEN and is not load-bearing). **Banked limb 1 is RE-SCOPED, not struck**: a SUBSTITUTIVE gate (ref stops measuring; identity-stable per-element listener with teardown, measure on `!skipped` only) closes the headline case under either event-timing branch — legitimate, but a real lifecycle, not two lines. Primary cure of record = the write-side suppression at dist :869 (K-19's site). Same class as K-9/K-16/K-17: the cure adjudicated separately from the finding. |
| **K-23** | **Challenge-C §6's disposition limb (:244: "move `.deferred-section` off the measured element onto an inner wrapper") AND banked PAW-2's disposition limb 3** | **CURE-KILL SUSTAINED (reader-3DU, seat re-derived from the containment spec)**: structurally a no-op — the skipped inner box sizes to the `contain-intrinsic-size` substitute (1200px), the outer measured box's content height is the sum of its in-flow children (1200 + any un-deferred header), and `measureSection`'s `offsetHeight` on the outer box still reports the estimate; no ancestor of a size-contained box escapes its substituted size, and the whole section is what must be deferred. **Limb 3 is STRUCK from the banked PAW-2 disposition.** The two sound limbs: write-suppression (:869) or stop measuring skipped boxes at all. |
| **K-24** | **Challenge-C §0's consumption-inventory cell (:25: "3 utility classes, 0 component imports … + 5 theme custom properties (L182,184,195,196,200)")** | **KILLED (reader-3DU, seat-verified)**: the component consumes **4 distinct** glass-ui tokens across **6 sites** — `--border` :182, `--muted` :184, `--primary-foreground` :195, `--primary` :196/:200/**:205** (the hover box-shadow, omitted entirely). Low consequence, but §0 self-declares as ground truth and the F.W1/F.W2 blast-radius argument rests on it — the inventory must be exact. |
| **K-25** | **Banked PAW-13's uniqueness cell AND the round-1 seat's own instrument cell** ("the var-free border-longhand sweep → exactly :22/:70/:71 (:70-71 = `.theorem-block`, un-paired, its rail survives via `--_section-color`)") | **KILLED (reader-3LC, seat-verified at the bytes, R3-1) — the round's deepest kill, landing on the round-1 seat's own work**: (1) `--_section-color` never touches `.theorem-block` — all six code hits sit in the `.section-header--*` rules; (2) the pairing exists 40 lines away (:111-125 + :131-145, all IACVT), invisible to a same-rule pairing predicate. The suppressed consequence is PAW-51 (92 blocks). PAW-13's own finding (the hover bar) is untouched; its "only instance in 644 lines" claim and the :70-71 dismissal are dead. Fleet lesson beside the round-2 humility row: a sweep is only as good as its pairing predicate — survivor and companion can live in different rules. |
| **K-26** | **Challenge-C M-4's object-identity implication** ("`v-if` and the `:srcset` it guards operate on different object identities … Vue's patcher sees fresh strings each time" — a correctness framing) | **KILLED-AS-INERT (reader-3LC, seat-verified)**: `resolveFigure` is pure over a build-time constant — the seven objects are value-identical by construction, every bound prop is a primitive compared by value, the `v-if`/`:srcset` pair cannot disagree; and the count is 7 only on the mapped path (5 unmapped — the ternary never evaluates the `<source>` props). Banked PAW-18 stays a pure-waste MINOR; a repair must not be sequenced as if integrity depended on it. |
| **K-27** | **Banked PAW-25's cascade parenthetical** ("`rounded-lg` additionally loses the cascade at (0,1,0) vs (0,2,0) — inert") — the MECHANISM, not the conclusion | **SUSTAINED-AS-SHARPENED (reader-3LC + seat precision note)**: the decider is CASCADE LAYERS — theme.css is 100% unlayered (0 `@layer` in 644 lines) while Tailwind v4 emits utilities into `@layer utilities`, and unlayered normal declarations beat layered ones at ANY specificity — so the utility would lose even at (0,3,0) and the specificity lever is banned as a repair (no arbitrary-variant or selector-raising cure; only `!important`/unlayering could flip it, and none should). Seat note: the banked specificity comparison also holds for the selectors as written, so both mechanisms currently agree — the layer fact decides the counterfactual. Conclusion (inert) unchanged. |

**Ratifications and survivals**: reader-3LC ATTACKED PAW-4b ("synchronously mid-patch") on the queuePostRenderEffect hypothesis and refuted itself at the bytes — `isFunction(ref)` is called before and outside the `doSet` post-flush path (esm-bundler :1818, seat-re-read; the cjs :1814-1815 anchor of round 1 concurs): function refs run inside `patch()`, interleaved with PAW-3's DOM mutations, layout cache invalidated between reads. **PAW-4 survives its first serious kill attempt — recorded with the same weight as a kill.** Both readers independently re-derived PAW-44 on a moved working tree (`.paper-article` :527 HEAD-stable; the file's dirty hunk is elsewhere); reader-3LC re-derived PAW-13's mechanism, K-7's 640px, K-16's margin-blindness at the bytes; reader-3DU re-derived PAW-1 version-invariance at v7.0.0 (correcting D-§6's `--border` cite :96→:95), banked K-2's PRM fold at the installed pin (C's falsifier grepped one layer), and banked K-13's byte. The readers' PAW-6/K-3/K-17 raster conclusions were adopted from the three prior direct reads, honestly declared (probe parsimony) — acceptable under the banked provenance.

## Banked-record re-scopes (by reference — all prior bytes stand)

1. **PAW-2 disposition re-inked**: limb order inverted — **primary = write-side suppression at dist :869** (K-19's site); limb "gate on the CV-state event" survives only as the SUBSTITUTIVE design with real listener lifecycle (K-22); limb "move `.deferred-section` off the measured element" **STRUCK** (K-23). Mechanism, severity, coupling law (PAW-2 before PAW-4) all unchanged.
2. **PAW-1**: gains an ARM CLASS — the 42 drops now split **absent-visual arms** (borders, backgrounds, tints) vs **wrong-visual arms** (PAW-13's hover bar; PAW-51's 92 rails+labels), and the SS-13 witness set MUST cover both classes (a wrong-visual defect is invisible to an absence-shaped witness). BLOCKER unchanged.
3. **PAW-13**: finding intact; uniqueness cell and the :70-71 dismissal dead (K-25).
4. **PAW-25**: cascade cell mechanism corrected to layers (K-27); the repair note (no specificity lever) binds any cleanup.
5. **PAW-33**: EXTENDED at the working tree — the uncommitted manifest now DECLARES the peer violations (katex `^0.17`, vite `^8.0.16`) and the uplift is wider than any round recorded (glass-ui `^4.0.0`, keyframes `^4.3.0`, value.js `^0.13.0`, pencil-boil pinned — seat diff read); PAW-50's script-block stability re-confirmed. The F.W1 pin-truth row should re-snapshot at repair time (the tree is in motion — SC-8).
6. **PAW-14**: symptom RE-SCOPED (reader-3LC unproven 3, seat-adopted): the banked "thousands of px of blank spacer pages" presumes print paginates `.paper-scroll`'s overflow; engines that clip a scroll container to its scrollport on print would yield ~ONE viewport of the treatise — a worse symptom. Mechanism (zero print rules) unchanged; symptom branch → SS-13.
7. **LAW-5 (new)**: any PAW-52 deep-link cure routes through `ensureTargetWindow` (the window is the gatekeeper), lands under LAW-4's single clearance authority, and REQUIRES PAW-38's slug uniquification first — URLs must not be minted over colliding ids.

## Superlatives — additions 15..19 (provenance marked)

15. **The component is on the correct side of its own BLOCKER** (reader-3DU; seat greps concur): every colour it authors is complete-colour/`color-mix` form — `hsl(var(` × 0 in the file — in the exact convention whose absence upstream is PAW-1. The banked record used this fact as PAW-1 proof; it is also a positive, now credited.
16. **Zero dark-mode forks** (reader-3DU; seat grep `dark` → 0): no `.dark` arm, no `light-dark()`, no per-scheme literal — every surface derives from tokens, so both schemes come free with no second code path to drift, in a file that authors a full button chassis and callout container.
17. **The teardown is complete with ZERO lifecycle code** (reader-3LC; seat-checked against the null-ref path): Vue's unmount `null` flows `bindRoot(null)` → guarded early-return, and `bindSection(id, null)` → `disconnectSection` — every element handed to the library is handed back with no `onUnmounted`, no disposer, nothing to forget. PAW-26's complaint (the two nulls are indistinguishable) is orthogonal and stands.
18. **`resolveFigure`'s purity is what keeps PAW-18 a perf row instead of a correctness row, and the `v-if` is simultaneously its cost ceiling** (reader-3LC ×2, merged; seat-verified): value-identity by construction is why seven calls are waste rather than hazard (K-26), and the unmapped path pays 5, not 7 — the guard the corpus read purely as a correctness device also bounds the waste it books.
19. **`:key="item.id"` protects the measurement model, not just the KaTeX output** (reader-3LC extension of banked superlative 2; seat-adopted on the mechanism): a one-step window slide falls to the keyed diff's LIS branch with an already-increasing subsequence — surviving items are patched IN PLACE with no `insertBefore`, and a DOM move is precisely the remove-and-reinsert that would reset `content-visibility`'s last-remembered size (PAW-2's contested state). The key choice defends the exact invariant the roster's deepest MAJOR fights over.

## Seat corrections against the round-3 readers

- **SC-7** (reader-3LC): "`grep -n '_section-color'` … returns exactly six hits" — seven lines match; the :6 hit is header-comment prose. The six-CODE-hit conclusion (and K-25) unchanged; state the comment-exclusion beside the count.
- **SC-8** (reader-3LC): substrate motion — 28 dirty rows at this seat's read vs the reader's 25, and the manifest uplift is WIDER than the reader's cell (glass-ui/keyframes/value.js/pencil-boil rows too). PAW-33's extension is confirmed and larger; pin-truth must re-snapshot at repair time.
- **SC-9** (reader-3LC): `.math-inline` anchor :59-61 → :60-62 at this seat's read. One-line drift, verdict untouched.
- **SC-10** (reader-3DU): PAW-53's "dies inside PAW-8's Button adoption" over-claims — the as-child cure keeps the `<a>` and no shipped rung reaches it; the repair needs its own forced-colors limb (ruled R3-4). Mechanism find unaffected.

## UNPROVEN-NEEDS-LIVE / NEEDS-BUILD — SS-13 inputs, round-3 revision

1. **PAW-51 pixel grading — enters the PAW-1 witness set as its own row**: one `theorem` + one `definition` + one `aside` side by side, BOTH schemes (predicted: light 36/92 accidentally near-correct, dark 92/92 wrong). An absence-shaped witness cannot show a wrong-colour defect. → SS-13.
2. **PAW-53 rendered result**: `.callout-btn` under forced-colors emulation (mechanism spec+byte-derived; the painted degradation needs WHC). → SS-13.
3. **PAW-54 magnitude**: `scrollWidth` vs containing block over `document.querySelectorAll('.math-inline')` — which of ~1145 spans exceed the 640px measure. → SS-13, beside the PAW-44 observation (same declaration, runs first — banked revision item 1 stands).
4. **PAW-55 magnitude**: whether tall KaTeX constructs (`\overbrace`, stacked limits, tall delimiters) actually clip at the 0.75rem padding line. → SS-13.
5. **PAW-14 symptom branch**: one print-preview decides paginate-vs-clip (blank pages vs one viewport). → SS-13.
6. **K-22 engine sub-arm** (not load-bearing): whether `contentvisibilityautostatechange` fires for an element that mounts already-skipped. Recorded so the substitutive-gate salvage is designed against either answer. → SS-13.
7. Round-2 items 1-5 and the banked carries stand unchanged (PAW-44 observation FIRST; PAW-47 branch; the revised PAW-1 witness list now including item 1 above; the PAW-3/4/45 scroll trace; the `npm run build` type witness).

## DISSENT (round 3) — preserved, not ruled away

1. **Reader scorecard.** reader-3DU: the PAW-2 cure-cluster (K-21/K-22/K-23) is the round's best repair-integrity work — three separately-shaped kills on one family, each verified here at the bytes; PAW-52 is the round's largest affordance find (masked by working navigation, exactly the shape audits miss); honest UNPROVEN tagging on its own kill's weakest arm. Debits: the PAW-53 disposition over-claim (SC-10). reader-3LC: K-25 is the round's deepest single cell — the only round-3 kill landing on a banked seat-instrument, and the one that un-suppressed a 92-block find; the PAW-4b failed-kill attempt is the register's best example of L-18 running both ways (attack mounted, refuted at the bytes, filed as confirmation). Debits: SC-7/SC-8/SC-9, all instrument-precision nits. Both readers: zero identity-guard violations — seat-swept (novelty greps → 0 across all seven admissions).
2. **Humility row, compounding**: round 2 recorded that the round-1 seat held :527 and the sticky rules in one session without connecting them; round 3 records that the same seat's own SWEEP CELL asserted a pairing predicate ("in the same rule") as if it were the property itself ("un-paired"), and decorated it with a mechanism (`--_section-color`) belonging to a different selector family — and that cell then suppressed the taxonomy find through TWO adversarial rounds. Instrument cells need falsifiers too; a sweep's predicate is part of its claim.
3. **Severity dissent left open**: a future seat may argue PAW-52 (citability) up on product grounds — an academic paper whose every theorem is unlinkable arguably fails its genre — or down to MINOR as an unshipped-feature rather than a defect. It sits at MAJOR here because the id substrate EXISTS and is silently unreachable: this is a broken seam, not an unbuilt feature.
4. **Round-2 dissents carry forward unchanged** (the PAW-44 stack-retirement design question; the PAW-15 a11y-cluster escalation note; LC-r1's MINORs on PAW-12/PAW-16).

## Closing verdict (round 3)

**ADJUDICATED — the record survives its fourth hostile reading with its deepest corrections yet: roster 50 → 57 (1 BLOCKER · 18 MAJOR · 19 MINOR · 19 INFO), kills 20 → 27, and the kills now reach a banked seat-instrument (K-25 — the longhand-sweep cell whose false "un-paired" dismissal suppressed the 92-block theorem-taxonomy erasure for two rounds; PAW-51 books it as PAW-1's wrong-colour arm at ~30× PAW-13's instance count) and two limbs of a banked disposition (K-22/K-23 — the CV-event two-liner leaks on the corpus's own hot path and does not close the headline case; the inner-wrapper relocation is a containment no-op; the write-suppression at dist :869 is the cure of record). The corpus loses its "no ResizeObserver" premise at the bytes (K-21) but keeps the conclusion on a corrected mechanism; §0's ground-truth inventory is corrected (K-24); two banked mechanism cells are sharpened without moving their conclusions (K-26 purity, K-27 layers-not-specificity). Seven finds enter: the citability void (PAW-52 — complete id substrate, zero URL surface, the window as gatekeeper, LAW-5), the forced-colors chassis loss, the nowrap-math clip, the third computed-value-conversion instance, the ramp's margin-of-one, and the dead guards. PAW-4 survives its first genuine kill attempt at the bytes — the record's strongest confirmation event. Five superlatives enter, including the two the banked record used as evidence but never credited (right side of its own BLOCKER; zero dark forks). Routings remain M-25 agglomeration input; nothing here stamps a wave.**

— end of round-3 addendum. The banked apotheosis, both prior addenda, the three challenge files, and both round-3 reader returns remain immutable per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; execution awaits the owner's begin-word.
