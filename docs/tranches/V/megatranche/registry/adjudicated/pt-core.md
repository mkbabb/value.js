claude-fable-5

# pt-core — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Seat**: apotheosis, parse-that module `core` · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **parse-that HEAD**: `ef10d5b` (main checkout, branch `master`, dirty only in `rust/`/`.cargo/`/`README.md` — re-verified this session; `parse-that-css-totality-p2` never created) · **value.js HEAD**: `983967da`, branch `tranche-u`.
**Corpus**: `audit/parse-that-modules/core/challenge-L-library.md` (Opus 5 [1m]; 30 defects · 2 BLOCKER · 6 superlatives) + `challenge-C-consumption.md` (Opus 5 [1m]; 19 defects · 2 BLOCKER · 5½ superlatives · 1 self-withdrawn) + the L-18 hostile re-read (Fable; 36 confirmed · 17 killed · 10 missed · 12 superlative rulings).
**Method**: both challenge files read whole by this seat; every load-bearing verdict, every kill, and every reader-missed finding re-derived against the live tree and the shipped dist with this seat's own instruments before adoption. The reader was then challenged back (incredulity seat): **one kill REVERSED with a live wrong-answer witness (K17 → SF-1)**, one superlative halved, three precision notes filed (§Seat corrections). Dispositions are **ADJUDICATED, not VERIFIED** — no bench was run, `enableDiagnostics()`/`memoize()`/`mergeMemos()` never called (`PACKRAT_ARMED` and `diagnosticsEnabled` false in every process this seat opened), parse-that read-only throughout; my only write is this file.
**This seat's own instruments (fresh short-lived node processes against `dist/`, plus static reads, main checkout)**: core.ts whole (26 lines; **18 runtime + 3 type** exports); lazy.ts whole (43); split.ts spot; parser.ts :12-34/:40-80/:435-478/:478-522/:636-644/:688-711; state.ts :1-50/:85-189; leaf.ts :80-145/:275-305/:310-399; packrat.ts :70-100/:150-160/:212-232/:240-295/:345-355; utils.ts :20-60; package.json whole; tsconfig `experimentalDecorators` grep → 0; test/subpath-gate.mjs :25-58; test/manifest-gate.mjs :5/:23-47; probe file `prototypes/css-parser/parsethat-surface-gaps.mjs` :40-60; dist: `wc -c` core.js **1,336** / packrat chunk **40,576** / diagnostics chunk **3,516** / parse.js **1,109** / utils.js **2,039**; CJS twins packrat-entry-46NYx4_U.cjs **41,041** + diagnostics-DpUY87_d.cjs **3,787**; `ls dist/*.d.cts` → **0**; `find dist -name '*.map' | xargs cat | wc -c` → **278,661** vs all .js+.cjs **100,926**; `dist/parsers/` = {csv,json,index,utils}.d.ts+maps (declaration-only); dist cites diagnostics:14, chunk:881/:882, PACKRAT_ARMED 678/682/714/722, `_initWhitespace()` :1416, core.cjs:55-58 whitespace accessor — all byte-exact; censuses: `.flags` sole assignment parser.ts:496; `mergeErrorState(` parser.ts **15** sites (93 201 223 239 258 273 291 410 418 447 460 468 504 553 626) + leaf.ts **7** (16 54 69 142 291 303 360); `.call(` **3** sites (state.ts:93, parser.ts:493, test/debug.test.ts:115); `ParserFunction<` 32 occurrences / 26 `as`-casts; `Span` in parser.ts/leaf.ts bodies → 0 (2 dead type-only imports); jsonParser|csvParser|escapedString in chunk → 0; split.test 16 hits; sed regions 28-285 → **8,517** B / 661-849 → **5,412** B; API probes (all reproduced): whitespace EOF table, the trim `"ab"`/`"ab "` inversion, `parse("ab")` → `"ab"` with `isError true`, many(1)/many(0) `[]`-vs-`[]`, `ret!==p.state` with `expected` aliased, furthest −1/1, 200 ids per 100 trims, 18-name export list, `expected` undefined diag-off, ESM/CJS identity **4/4 false**, the trim name-sniff discard triple + NBSP divergence, dispatch é reject, inverted-range vacuum, hyphen-range ambiguity, **and the 257-entry mis-dispatch witness (SF-1)**; value.js side: `src/parsing` absent, `src/foundation/result.ts:1-6` Result/ok/err, css/index.ts 61 lines, package.json parse-that grep 0, root barrel index.ts:6 re-exports Suggestion/SecondarySpan/Diagnostic.

## Scoreboard

**48-row roster: 36 CONFIRMED (from the two challenges, severities adjudicated) + 10 MISSED-ADOPTED (the reader's, all re-verified by this seat) + 2 SEAT-FOUND (SF-1, SF-2) · 16 KILLED sustained + 1 KILL REVERSED (K17) · 12 superlatives banked (one halved) · 3 BLOCKERS SURVIVE.**

Unlike `pt-ansi`, the blockers here are real and die nowhere: **B-1/C-2** (a `/core` leaf rejects valid input at EOF — the trailing-space inversion reproduced by this seat), **B-2/C-14** (`.parse()` returns success-shaped values on failure; the type asserts the failure mode away), and **D-03/C-1** (the module's sole documented product — tier isolation — is false in the shipped artifact). The engine and its packaging fail on exactly the axis the 26-line barrel exists to certify. Calibration: C-19 (zero consumers today) keeps every consequence prospective — which is why this corpus exists now, before an X·P candidate inherits the substrate.

## Adjudicated defect roster

Anchors are parse-that HEAD `ef10d5b`; re-resolve before executing any cure (D-19 lesson). Routing law: **W1** = X·P harness/oracle/gates · **W2** = X·P algebra + candidate seats (AC-3 scan-union holds the sole parse-that write permission, divergences declared) · **W4** = X·P packed-release/RC-P packaging · **X·P REFINEMENT FOLD** = the finding sharpens a spec · **NO-WAVE-OWNER** = real defect, no X·P owner — recorded binding on any future parse-that source wave so it inherits rather than rediscovers.

| # | id | sev | claim (adjudicated form) | proof (this seat's anchors) | disposition |
|---|---|---|---|---|---|
| 1 | L:B-1 / C:C-2 | **BLOCKER** | `regex()` short-circuits at EOF before the pattern is consulted (leaf.ts:326-330), so every nullable pattern fails at end of input — including the `/core`-exported `whitespace` = `regex(/\s*/)` (leaf.ts:397, core.ts:24). The non-EOF path has a dedicated empty-match SUCCESS arm (leaf.ts:354-357): the function contradicts itself across the boundary. Masked only by `.trim()`'s name-sniff fast path (parser.ts:488 → trimStateWhitespace, correct at EOF). | This seat's probes, all reproduced: `whitespace.parseState('')` isError **true** / `('  ')` false / `('a')` false; `all(string a, whitespace)('a')` **true**; `string('ab').trim(regex(/\s*/))` on `'ab'` **true** but on `'ab '` **false** — appending a trailing space makes the parse succeed; default `.trim()` false (masked). New vs corpus (absent from O-15 and parser-band's 22-row ledger). | **W2** — AC-3 scan-union owns the leaf.ts repair, as a **declared divergence row** for `.h` (repairing it changes `whitespace` for every consumer — C's §3 filing adopted). **+ W1 rider**: the equivalence oracle's corpus gains inputs whose last token is a nullable regex — P-1's GREEN could not have contained one. |
| 2 | L:B-2 / C:C-14 | **BLOCKER** | `.parse()` returns a success-shaped value on failure — `parse(val): T`, no Result, no throw, no sentinel; failure arms write plausible values (`unsafeSetValue([])` parser.ts:555/:628). `parseState()` compounds it: returns a different object than `.state` (parser.ts:74 returns the original; :66 assigns errorState) while sharing `expected` by reference (:62). | Probes reproduced: `all(ws, string('ab'), ws).parse('ab')` → **`"ab"`** with `p.state.isError` true; `many(1).parse('z')` → `[]` isError true vs `many(0).parse('z')` → `[]` isError false — identical return, opposite verdict; `ret===p.state` false, `ret.expected===p.state.expected` **true**. Sharpens O-15 PT-07 correctly: plausible-value vs plausible-value, not undefined vs undefined. | **W2** — the frozen contract's failure arm (`ok:false` + non-empty diagnostics tuple, W2.md:292-295) makes the shape unrepresentable; `.parse()` must not survive any wrap. **+ REFINEMENT FOLD**: the value.js JS-boundary invariant asserts on `state.isError`, **never** on the return — an undefined-check passes all three rows above. `src/foundation/result.ts` is the typed home (row 43). |
| 3 | L:D-03 / C:C-1 | **BLOCKER** | The module's sole documented product — `core.ts:3-5`'s three denials — is false on two of three clauses in the shipped artifact: `dist/core.js:1` imports the whole packrat chunk, whose line 1 imports the whole diagnostics accumulator; only json/csv is truly absent (grep 0). Unshakeable by construction: `parseState` calls packratEnter/Exit every parse (parser.ts:43/:47); mergeErrorState reached from 15+7 sites (row 45); debug/print are class methods. | Byte closures re-derived exactly: `/core` = 1,336+40,576+3,516 = **45,428 B**; root = +1,109+2,039 = **48,576 B**; delta **3,148 B = 6.48%**; `/core` = **93.52%** of the root barrel. 16 of 18 runtime names re-exported from the packrat chunk. **Identity note**: pt-ansi row 11 recorded this same fact from the neighbor module at MAJOR — ONE defect, cross-booked, **primary here**: this module's 26 lines exist solely to make the claim. | **W4** — RC-P chunk topology; any wave touching core.ts corrects the :3-5 comment to shipped truth or makes it true. **+ W1/D-17 gate** (row 20): a module-graph assertion would have caught this the day core.ts was written. |
| 4 | L:D-04 / C:C-3 | **MAJOR** | The EOF arm of `regex()` never calls mergeErrorState, so `furthest` never advances and the rendered error sits at offset 0 (parser.ts:60 fallback). Two `/core` leaves, two incompatible failure protocols (string() always merges, leaf.ts:291/:303). | Probes: `all(string a, regex(/\d+/))` — `('a')` furthest **−1** vs `('ax')` furthest **1**: truncating one char moves the reported error from 1 to 0. Falsifier holds at source: utils.ts advances furthest UNCONDITIONALLY (re-read :29-33); only the label is diagnostics-gated. The commonest authoring error in the target domain (`oklch(0.5 0.1` truncated). | **W2** — same AC-3 repair surface as row 1; one cure, one declared divergence row. |
| 5 | M1 (reader) | **MAJOR** | **Dual-package hazard, live**: the exports map declares `import` AND `require` for all five subpaths (package.json:8-30); the build ships two complete module instances of everything (`packrat-entry-CS1td-8B.js` 40,576 B vs `packrat-entry-46NYx4_U.cjs` 41,041 B; diagnostics twins 3,516/3,787). Three process-global latches (`PACKRAT_ARMED`, `diagnosticsEnabled`, `PARSER_ID`) exist **twice**: `instanceof` unsound in a mixed graph; `enableDiagnostics()` in one format does not arm the other's leaves; the memo-key id budget is per-format. Neither challenge opened a `.cjs`; C's S-1 declared the hazard absent (K1). | This seat, one process: `esm.Parser === cjs.Parser` **false**; `ParserState` false; `esm.whitespace instanceof cjs.Parser` false; `new cjs.ParserState('x') instanceof esm.ParserState` false — **4/4**. Not gated: subpath-gate loads only the `import` targets (:39-43 area re-read; `require` targets existence-checked only). | **W4** — RC-P: single-format ship, or a cross-format identity/latch gate. The narrowed true half (identity WITHIN the ESM family) is row 44's INFO + gate rider. |
| 6 | M2 (reader) | **MAJOR** | `.trim(p)` **silently discards `p`** whenever `p.context?.name === "whitespace"`: the branch's closure calls trimStateWhitespace + inner and never references `parser` (parser.ts:498-512, re-read whole). So `.trim()` ≠ `.trim(whitespace)` semantics: the substituted charCode loop is ASCII-only (space + 9..13, leaf.ts:372-391) while `whitespace` = `/\s*/` includes NBSP/U+2000-200A/etc. Keyed on a mutable public string that is a sanctioned union member (`"whitespace"` ∈ parserNames, state.ts:158) — a booby-trapped, type-legal spelling. | Decisive triple reproduced: `regex(/x*/)` named `regex` → `.trim(xs)` on `'xxaxx'` **false** (argument honored); same pattern renamed via `createParserContext('whitespace')` → **true** (discarded); `.trim(xs,false)` → **false** (all() path honors it). NBSP divergence: `.trim()` default on NBSP+`a`+NBSP **true** vs `.trim(regex(/\s*/))` **false**. | **W2** — EQ-1 divergence source: the algebra wrap must not key behavior on a mutable name; whitespace policy becomes an explicit operator parameter. Also the reason K5 killed C-8's consequence clause in both directions. |
| 7 | C:C-4 (narrowed per K16) | **MAJOR** | `/core` is not closed under **freely-labelled** failure: mergeErrorState is off the surface (18-name list, verified), Parser has no `.label`, so parser-band binding debt 1 (the labelled zero-width reject) is undischargeable from the tier that already loads the 3,516-B diagnostics chunk. Narrowed: four `/core` leaves DO seed labels via mergeErrorState (string leaf.ts:291/:303 label `` `"${str}"` `` built :278; eof :16; dispatch :142; regex :360) — what is absent is a label **decoupled from a matcher**; and per row 8 the label is undefined at runtime anyway. | Export list Object.keys length **18**, `'mergeErrorState' in core` false (probe); parser.ts read whole — no label member; the cand-F reject() idiom must import from `/diagnostics` while combinators come from the barrel (probe file :11-13). | **W2** — the algebra's reject/label seam (parser-band debt 1); dischargeable only with row 8's cure. |
| 8 | C:C-5 | **MAJOR** | On `/core`, `expected` is **always** undefined — utils.ts:33/:38 gate the SEEDING itself on `diagnosticsEnabled` (re-read: both arms) — and `/core` exports no enable/disable. The only cure (arming from `/diagnostics`) buys parser.ts:67-69's unconditional `console.error` on every failed parse. | Probe: `regex(/[0-9]+/).parseState('').expected` === undefined. Same module instance reached from both subpaths (within ESM — row 44), so arming works and spews. R-LAW-3 ("diagnostics are values, never effects") is the violated law. | **W2** — R-LAW-3 owns the shape: diagnostics as injected policy/values. Mechanism row is 9; the two discharge together. |
| 9 | L:D-11 | **MAJOR** | PT-01 corroborated and sharpened: there is no label-without-console path because the gate lives inside mergeErrorState (seeding), not at the render site; parser.ts:67-69 fires unconditionally once armed. O-15's dist cite one line off (:881 guard, :882 emission — verified byte-exact by this seat). `Parser.debug` takes an injected logger (parser.ts:693); this site does not. | dist cites re-read: diagnostics-DDazRHgl.js:14 exact; chunk :881/:882 exact. | **W2** — R-LAW-3 (with row 8). An unwrapped `Parser` cannot pass R-LAW-3's own console-patching probe — C's strongest algebra sentence, adopted. |
| 10 | L:D-12 | **MAJOR** | PT-03 corroborated at the bytes and sharpened: the latch arms at wrapper **CONSTRUCTION** (packrat.ts:290, rationale :286-289 re-read), so it is process-global and grammar-agnostic — one import-time `memoize()` anywhere taxes every unrelated parse forever; `resetPackrat` never disarms (:262-272 re-read, early-returns on the latch it never clears). | Source + dist rows verified: src :156/:217/:266/:290, dist :678/:682/:714/:722, zero assignments back to false in either tree. The 1.47× figure is O-15 N=1 — **MEASURE-AT-OPEN row 1**, never re-derived by any core seat (latch hygiene). | **W2** — O-8 anti-latch: arming becomes a parse parameter. The retention corollary is row 30. |
| 11 | L:D-05 | **MAJOR** | Three public line/column methods, three conventions: `getLineNumber` 0-based, `getLineAndColumn().line` 1-based (the only documented one, state.ts:126), `getColumnNumber` disagrees with `.column` at a newline offset — and the `Math.max(0,…)` clamp (state.ts:116) exists only to convert the detectable negative into a plausible wrong answer. collectDiagnostic reimplements the convention a third time (utils.ts:107-110). Zero tests on all three. | state.ts:111-134 re-read whole; the five-row table verified by the reader, arithmetic re-derived by this seat from source (rows 1-2 by hand: `"a\nb"`@2 and `"ab\ncd"`@2). | **NO-WAVE-OWNER** — diagnostics/render-tier debt roster: one convention, one implementation, or delete two of three. |
| 12 | L:D-06 | **MAJOR** | `Parser.trim()` builds, configures, and discards a whole Parser per call (`flaggedParser` parser.ts:492-496, referenced nowhere else — grep verified), burning 2 PARSER_ID slots per call against the memo-key budget (MEMO_MAX_ID ≈ 2,097,151, packrat.ts:77, fail-loud :94). | Probe: 100 `.trim()` calls consumed exactly **200** ids (differenced by this seat). `.flags` sole assignment = :496 (census re-run after fixing my own filter — see instruments). | **NO-WAVE-OWNER** — dies with row 13's flag excision; the id-budget interplay is the reason it is MAJOR not MINOR. |
| 13 | L:D-07 | **MAJOR** | The entire flag machinery is unreachable: the only `.flags` assignment is on row 12's discarded parser, so `call()`'s post-fast-path body (parser.ts:441-477) is dead, `unsafeCall` (state.ts:92-94) has zero callers, and the `FLAG_EOF` branch (:466-476) is the **only** construction site of the public `Suggestion` variant `kind:"trailing-content"` (state.ts:26; census: sole hit parser.ts:470) — a consumer switching on `suggestion.kind` writes a permanently dead arm. | Censuses this seat's own: `.flags` writes = 1; `.call(` = 3 sites (K15's correction folded: parser.ts:493 is the second caller, inside the discarded closure); trailing-content = declaration + :470 only. Probe: flags === 0 on `.trim()`/`.eof()`/`.many(1)` results. | **NO-WAVE-OWNER** for the excision · **W4 note**: the stranded union member is public typed surface and must be excised or made constructible at the next semver cut. |
| 14 | L:D-08 | **MAJOR** | `ParserState.clone()` copies 5 of 8 fields, silently dropping expected/suggestions/secondarySpans (state.ts:101-109 vs :43-45) — contradicting the stated per-parse threading model (state.ts:37-42, utils.ts:20-26, both re-read). Sole production caller: packrat recall's grow-pass scratch (packrat.ts:351, verified in context). | Source re-read; the design-model contradiction is the falsifier's decider. | **NO-WAVE-OWNER** — copy all 8 or rename; binding on any packrat-touching wave. |
| 15 | L:D-09 / C:C-7 | **MAJOR** | `ParserFunction<T>`'s parameter is phantom — appears nowhere in the signature (parser.ts:14-16 re-read) — so all 26 `as ParserFunction<X>` casts assert nothing, on the library's only constructor parameter; `Parser<T = string>` compounds (unannotated = Parser<string>). | Counts this seat's own: 32 occurrences, 26 casts (K9's correction confirmed; L's 34 wrong). | **W2** — RETIRE: the algebra's function type carries `T` on the output position (`(s: ParserState<any>) => ParserState<T>` keeps the variance hatch). |
| 16 | L:D-10 / C:C-9 | **MAJOR** | The exported `lazy` decorator is legacy 3-arg form under a tsconfig with **no** experimentalDecorators (grep 0, this seat) and TS ^5.8 — unusable on the repo's own settings, throwing at class-definition time under TC39 shape; its JSDoc claims "then caches" while the body mints a fresh Parser + fresh cache per call (lazy.ts:37-42, read whole); `bind(target)` binds the prototype. Zero `@lazy` usages. The correct WeakMap reference sits ten lines above. | lazy.ts whole + tsconfig grep, this seat. `lazy.length === 3` (arity re-confirmed via source signature). | **W2** — RETIRE. |
| 17 | L:D-13 / C:C-17 | **MAJOR** | `dispatch()` silently drops every non-ASCII table key — `tbl[chars.charCodeAt(i)] = idx` (leaf.ts:122) is a silent OOB no-op past 127 — while the label built from `Object.keys` unconditionally (:128-132) still advertises them; the read side guards `ch < 128` (:137). Live the moment a CSS ident head-table is built (ident-start includes non-ASCII; W2 names head dispatch a shared-slice capability). The library's own fail-loud idiom for this hazard class is two files away (packrat.ts:94). | Probe: `dispatch({'é': string('état'), 'a': …}).parseState('état')` isError **true** on its own input. | **W2** — KEEP + widen: fail-loud at construction on unaddressable keys, before any head-table lands. Riders: rows 41 (range heuristic) and 42 (SF-1 intern overflow) ride the same cure. |
| 18 | L:D-14 / C:C-11 | **MAJOR** | No depth bound anywhere in the library — no counter, no maxDepth, no typed error (parser.ts/lazy.ts/leaf.ts full reads); both recursion primitives are arity 1 (`Parser.lazy`, `createLazyCached` — source signatures re-read), so parser-band debt 3 / W2's K-7 algebra-parameter bound has nowhere to go; the ceiling is a V8 frame-budget property, and a raw RangeError crossing the boundary is K-8's forbidden throw. The iOS-Safari precedent (ValueUnit ~294 frames) is the shape that passes CI and fails on a phone. | Source reads this seat's own. The NUMBER dispute is killed (K7): O-15's 7,761 and L's ≥9,000 are different constructions on different stacks — **neither may be pinned**. | **W2** — K-7: the bound is an algebra parameter. **+ REFINEMENT FOLD**: X·P.W1 **G-9 asserts the existence of a typed bound, never a constant** (MEASURE-AT-OPEN row 2). |
| 19 | L:D-15 | **MAJOR** | PT-07 boundary posture corroborated 5/5: `parseState` performs no runtime validation; null/undefined/42/{}/['a'] throw raw TypeErrors leaking internals (`state.src.charCodeAt`), no parser identity, no offset. | The five messages reproduced verbatim by the reader against dist; this seat spot-held 2 of 5 and endorses (mechanism certain from parser.ts:34/:52 + state.ts:47-53). | **REFINEMENT FOLD** — the named JS-boundary invariant ABOVE parse-that (O-15's posture, endorsed unchanged), widened per row 2 to assert on `state.isError`. |
| 20 | L:D-17 / C:C-12 | **MAJOR** | The only gate naming `/core` tests **resolution, not isolation**: three typeof checks over four subpaths (subpath-gate.mjs:25/:45-55, re-read); dist-surface derives its expectation FROM the source barrel; no golden export list anywhere — deleting 16 of 18 runtime exports leaves every gate green; a one-line module-graph assertion would have caught row 3 at build time. (value.js note held: `proof:*` is dead law in OUR tree — this observes parse-that's gates, prescribes nothing in value.js.) | Gate scripts re-read this seat; K12's corrections (4 subpaths, line numbers) folded. | **W4** — RC-P gate surface: module-graph assertion + golden export list + declaration-surface check (with rows 5/38/47). |
| 21 | C:C-6 | MINOR (re-rated from MAJOR, held) | The subpath split's payload premise is unearned: `/core` is 93.52% of the root barrel, saving 3,148 B; `dist/core.js` is a 1,336-B facade over one 40,576-B chunk. Accounting exact; no consumer harmed (row 43); dwarfed by row 40. | Byte closures re-derived by this seat (row 3). minify:false — ratio is a graph fact, absolutes move (MEASURE-AT-OPEN row 4). | **W4** — calibration input to RC-P, with row 40. |
| 22 | C:C-8 | MINOR (re-rated, held) | `createParserContext`/`ParserContext` exported against a frozen 29-name union (counted: 29, state.ts:141-171) with `parserNames` exported from neither barrel — a consumer cannot name a custom combinator. The stated consequence was backwards (K5): opting into the trim fast path is not blocked, it is a **booby trap** (row 6). | state.ts census + core.d.ts (6 content lines) + index.ts (14 lines) re-read. | **W2** — RETIRE from the algebra surface (provenance is `P`, an array, not a node field — W2.md:238). |
| 23 | C:C-13 / L:D-30 | MINOR | `sideEffects:false` (package.json:6 — L's cite right, C's off by one, K11) vs the required top-level `_initWhitespace()` (parser.ts:711; dist :1416 verified); manifest-gate Gate 2 hard-mandates the false declaration (:38-41 re-read). Currently harmless: modules-not-statements semantics + the CJS accessor (S11) close the breakage half as built. | All cites re-verified this seat. | **W4** — declare the file's effect honestly (`sideEffects` array) or hoist the init; the gate updates with it. |
| 24 | L:D-16 | MINOR (re-rated, held) | `mapState` mints a prototype-chained `Object.create(state)` view per invocation — the megamorphic-IC hazard lazy.ts:17 names — and has zero callers (census: :162/:168/:184/:185 + registry only, verified). Live defect = dead published surface; the cost is unmeasured. | Census this seat's own. | **NO-WAVE-OWNER** — folds into row 31's dead-surface retire list; cost question is MEASURE-AT-OPEN row 3. |
| 25 | L:D-18 | MINOR | Two throwaway arrays per ParserState construction (state.ts:44-45) and two more per furthest advance (utils.ts:34-35, re-read: **outside** the diagnostics gate that guards their only writers :52-59) — allocated to hold nothing on the shipping default, on the error path's highest-frequency call (15+7 sites, row 45). | utils.ts:20-60 re-read whole. | **NO-WAVE-OWNER** + MEASURE-AT-OPEN row 3. |
| 26 | L:D-19 (narrowed) | MINOR | HOLEY `new Array(n)` confined to `many(min>0)`/`sepBy(min>0)` and fuseAll's arity≥4 arm — the default min=0 arms use packed empty literals (parser.ts:526/:572 read: `est > 0 ? new Array(est) : []`); the unrolled arms are packed. Reader's narrowing adopted. | Source re-read. | **NO-WAVE-OWNER** + MEASURE-AT-OPEN row 3. |
| 27 | L:D-20 | MINOR | parser.ts:62-64 alias all three diagnostic arrays between the returned state and `p.state` — a consumer that dedupes one mutates the other; collectDiagnostic spreads correctly two files away (utils.ts:120-122). | Probe: aliasing reproduced (row 2's receipt). | **NO-WAVE-OWNER** — one cure with row 2's identity repair. |
| 28 | L:D-21 | MINOR | `regex()` force-rewrites every pattern sticky (leaf.ts:321-322), so `^`-anchored patterns never match at offset > 0 — silent, and the commonest porting idiom. | Source re-read; probe semantics held from the reader (mechanism certain: `y` without `m` anchors to input start). | **NO-WAVE-OWNER** — doc + a construction-time warning on `^`-source patterns; W1 corpus may include one ported-anchor row. |
| 29 | L:D-23 | MINOR (latent) | Custom-match arm reads the shared per-parser sticky's `lastIndex` AFTER invoking user code (leaf.ts:333-340 re-read) — re-entrant matchFunction corrupts offset arithmetic; no in-tree consumer passes one. | Source re-read. | **NO-WAVE-OWNER** — latent; snapshot lastIndex before the callback. |
| 30 | L:D-24 | MINOR | Packrat retention: cells written by the parse during which the latch first armed persist for process lifetime (enter returned null → exit(null) no-op; resetPackrat never disarms); soundness clearance re-endorsed — snapshot/replace ordering makes cross-input mis-restore unreachable (packrat.ts:216-249 re-read). | Source re-read. | **NO-WAVE-OWNER** — document resetPackrat; dissolves under row 10's O-8 cure. |
| 31 | L:D-25 / C:C-10 | MINOR | Dead public surface reachable from `/core`: save/restore/err, mapState, recover (sole hits = own definition :653-685, verified), spanToString/mergeSpans + line/col trio (zero tests); `getLazyParser`'s ONLY caller is debug.ts:318 — inside the tier core.ts denies pulling (grep this seat). getLazyParser is additionally an O-8 violation (process-global WeakMap; the correct closure-local sibling adjacent). | Censuses this seat's own. | **W4** — the retire list (with row 20's golden surface); getLazyParser also **W2** RETIRE. |
| 32 | L:D-26 | MINOR | `getLazyParser` tests `cached !== undefined` (lazy.ts:8-11) — an undefined-returning thunk is re-invoked forever; typed on bare `Function`. | lazy.ts read whole; mechanism certain, probe waived. | **W2** — rides row 31's RETIRE. |
| 33 | L:D-27 / C:C-16 | MINOR | The balanced-split helpers are BBNF format-time codegen support (split.ts:1-4 verbatim) inside "the zero-side-effect primitive set"; only two `/core` names not hoisted to the chunk (inlined dist/core.js:2-37; parse.js imports them back OUT); zero value.js references; 16 test hits (K10's count) — the only well-covered members, the tell. | Emission + greps re-verified. | **W4** — re-home (`/format` subpath or bbnf-lang's tree). |
| 34 | L:D-28 | MINOR | `splitBalanced` has no escape handling — a backslash-escaped quote closes the quoted section and the split lands inside the string (split.ts:30-37 logic re-read); flat quote nesting. Fine for its BBNF input; undocumented on a general-purpose surface. | Source re-read; trace endorsed. | **NO-WAVE-OWNER** — document the limitation where it lands after row 33. |
| 35 | L:D-29 (count corrected) | MINOR | `Parser.eof()` allocates 2 Parsers + 2 contexts and discards the context `skip()` just built (parser.ts:638-642 re-read: `this.skip(eof())` then `p.context =` overwrite). L's "three allocations" wrong; reader's correction held. | Source re-read. | **NO-WAVE-OWNER**. |
| 36 | C:C-18 (+ K13 cites) | MINOR | RED-7 instrument defects: probe row at **:48** can never go RED (`!ARM ? false : false` — verified in the file); header claims workspace-runnability but `@mkbabb/parse-that` is not in value.js's node_modules (verified: glass-ui/keyframes.js/value.js only) — unrunnable as documented. **Seat addition**: the probe also embeds a live hrtime bench (:41-43) that runs on EVERY invocation and arms the latch under `--arm` (:45) — running it at all violates the standing no-bench law; it may only ever run in a disposable process after repair. | Probe file :40-60 re-read this seat. | **X·P REFINEMENT FOLD** — repair rows 47/48 (with row 46) + re-home or delete the embedded bench before any RED-7 figure is banked. |
| 37 | M3 (reader) | MINOR | `Span`/`spanToString`/`mergeSpans` are orphans post-1.0.0-excision: zero producers/consumers in the library (census: declaration + helpers + 2 barrel re-exports + **2 dead type-only imports** parser.ts:2/leaf.ts:3, bodies clean — verified); three of 21 names carry a shape nothing carries. K3 killed the KEEP rationale (Span is start/end, not (offset,length); cannot carry `C`'s `kind`). | Span census this seat's own. | **W4** — dead imports + orphan trio to the retire/re-home list; the `C`-carrier decision is **W2**'s and starts from the triple, not this pair. |
| 38 | M4 (reader) | MINOR | No `.d.cts` anywhere (ls: 0); every `require` condition pairs a CJS implementation with an ESM-flavored `.d.ts` under `"type":"module"` — the FALSE_ESM/masquerading shape; TS node16 CJS consumers hit TS1479. No gate checks type/impl format pairing. No in-realm consumer affected today (value.js is ESM). | dist listing + package.json re-read. | **W4** — RC-P (with rows 5/20). |
| 39 | M5 (reader) | MINOR | No top-level `main`/`module`/`types`, no `./package.json` export — exports-map-only resolution (package.json read whole); classic-resolution consumers get bare ERR_MODULE_NOT_FOUND. | package.json whole, this seat. | **W4** — one-line fallbacks at the next cut, or a documented floor. |
| 40 | M6 (reader) | MINOR | **73% of the published payload is sourcemaps**: all maps 278,661 B vs all shipped code 100,926 B (2.76×), `files: ["./dist"]` + `sourcemap: true` — the single largest consumption lever on the package, untouched by both challenges' 3,148-B litigation. Reader's figure reproduces **only** via find across `dist/parsers/` (seat note 4) — which surfaced row 47. | `find dist -name '*.map'` … = 278,661 this seat. | **W4** — ship without maps or point at .map via files exclusion; calibrates row 21. |
| 41 | M7 (reader) | MINOR | `dispatch`'s 3-char range heuristic fires on ANY `x-y` key (no opt-in marker, leaf.ts:116); `'a-b'` is silently a range (hyphen itself never mapped — live for CSS custom-property/vendor-prefix head tables); an inverted range `'z-a'` maps NOTHING while the label advertises it. | Probes: `'z-a'` table rejects `'zz'` (true); `'a-b'` table rejects `'-x'` (true). | **W2** — rides row 17's construction-time validation (reject ambiguous/inverted keys loud). |
| 42 | **SF-1 (seat)** | MINOR (latent) — **K17 REVERSED** | `dispatch`'s interning is NOT bounded by the table width: `internParser` mints one index per distinct parser ENTRY (leaf.ts:104-111 — called for every entry before any key-write), so a table with >128 distinct parsers writes `ToInt8`-wrapped indices — entries 128..255 wrap negative (**silent reject on their own keys**), entries ≥256 wrap positive (**silent MIS-DISPATCH to the wrong parser**). C-17′'s withdrawal ("≤128 addressable slots ⇒ ≤128 indices") and the reader's K17 endorsement both rest on a false derivation: addressability bounds the KEYS, not the interned parser count. | This seat's live witnesses: 129-entry table, key `'a'` (entry idx 128) → isError **true** on its own input; 257-entry table with `parsers[0] = string('ab')`, target `string('abc')`, input `'abc'` → isError **false, value `"ab"`** — the wrong parser succeeded. 101-entry control clean. Latent: no realistic head table nears 128 distinct parsers — but it is the exact silent-alias class `getCijKey` fails loud on (packrat.ts:94), now the **third** silent-drop mode on one constructor (rows 17/41). | **W2** — one guard discharges all three: `internParser` throws RangeError past 127 (the getCijKey posture), alongside row 17's key validation. |
| 43 | C:C-19 | INFO | `/core` has zero consumers; every consequence above is prospective. value.js@4.0.0 deps: glass-ui + keyframes.js only; `parse-that` grep over src = 2 negative-asserting comments; the frozen 52-export `/css` surface is value.js's own grammar. Reader's addendum verified: `src/parsing` does NOT exist in 4.0.0; the consume edge is `src/css/**` + `src/foundation/result.ts:1-6` (Result/ok/err read by this seat) — **the typed home rows 2/19's invariant needs is already built and frozen**. | value.js-side probes this seat's own. | Calibration row — no owner; binds severity readings across the roster. |
| 44 | C:S-1 (surviving half) | INFO | Module-instance identity holds WITHIN the ESM family (all four .js entries share one chunk pair; one flag, one Parser, one latch) — real, valuable, and **ungated** (subpath-gate asserts nothing about identity). The headline ("the dual-package hazard does not exist here") is K1-killed by row 5. | Chunk-import reads + grep counts re-verified. | **W4** — identity gate rider, paired with row 5's disposition. |
| 45 | M9 (reader) | INFO | L's D-03 mergeErrorState census was short by four: parser.ts has **15** sites (incl. many() :553 and sepBy() :626 — the hot-path pair) + leaf.ts 7. Strengthens row 3's unshakeability. | Census re-run this seat: 93 201 223 239 258 273 291 410 418 447 460 468 504 553 626 / 16 54 69 142 291 303 360 — exact. | Roster hygiene — folded into row 3's proof. |
| 46 | M10 (reader) | INFO | `regex()`'s custom-match contract is undocumented: the callback receives null (must handle it, nothing says so); `match === ""` is a magic third outcome (zero-width success colliding with a real empty capture); the advance is the RAW match length regardless of the returned string (leaf.ts:335-343 re-read). | Source re-read. | **NO-WAVE-OWNER** — doc or narrow the parameter at the next cut. |
| 47 | **SF-2 (seat)** | INFO | `dist/parsers/{csv,json,index,utils}.d.ts` (+maps) ship in the payload — **declaration-only ghosts of the json/csv tier the `/core` docblock denies** — unresolvable through the exports map (no wildcard, no subpath): a phantom typed surface in the same class as pt-ansi's 7 top-level orphans, missed by both challenges and the reader. The runtime denial stays true (chunk grep 0); its types ship anyway. | `ls -R dist/parsers` this seat. | **W4** — same dts-include narrowing unit as pt-ansi row 4; add to that acceptance list. |
| 48 | C:C-15 (as re-rated by K4) | INFO (re-rated from MINOR, held) | Suggestion/SecondarySpan absent from `/core`'s type surface — but reachable from the ROOT barrel (index.ts:6, verified) and nameable via indexed access (`ParserState<T>['suggestions'][number]`); a completeness nit, not a blocked path. | index.ts read this seat. | **W4** — rides row 20's golden-surface work. |

## Seat corrections (the incredulity seat's own findings — against the READER)

1. **K17 REVERSED — the roster's one verdict flip, with a live witness.** The reader "independently re-verified" C-17′'s withdrawal by repeating its derivation: "internParser can mint at most 128 distinct indices (0..127)." False — interning is per table entry, key addressability bounds nothing about it. This seat's 257-entry probe produced a **silent wrong answer** (`isError false, value "ab"` from the wrong parser) and the 129-entry probe a silent self-reject. C-17′'s ORIGINAL hypothesis was correct; the withdrawal was the error; the reader re-endorsed the withdrawal without constructing the attack (C "built a 130-parser table" keyed on single chars — the one shape that cannot exceed 128 entries — and the reader inherited the conclusion). Filed as row 42; superlative S7 halved below. This is the citation-inheritance event of this corpus, matching the ColorSpaceSelector DISSENT precedent: a verification claim inherited nothing from the conclusion it decorated.
2. **S-2/S7 halved, not banked whole.** "Exactly minimal and provably total" survives only on the READ side (key domain closed at `ch < 128`, the −1 sentinel's sign load-bearing). The WRITE/intern side is unguarded ToInt8 wrapping — the same package that fails loud on memo-key aliasing (S1) wraps silently here.
3. **K16 phrasing nit.** string()'s label is `` `"${str}"` `` — the caller's own literal, quoted (leaf.ts:278) — not the fixed token `"str"` the reader's kill-text implies. Substance (four /core leaves seed labels) exact.
4. **M6 instrument precision.** The reader's 278,661 B reproduces only with `find` across subdirectories; a top-level `dist/*.map` glob gives 277,492 B (this seat's own first, wrong, instrument). The 1,169-B delta is `dist/parsers/` — whose existence is row 47, a finding the number was quietly standing on. Number exact; provenance now stated.
5. **Immaterial**: value.js `src/css/index.ts` is 61 physical lines (reader: 60); `result.ts`'s ok/err span :5-6 not :1-6's tail. Recorded only because provenance is this corpus's standard.

**Everything else re-measured was exact** — an unusually strong hostile re-read outside K17: byte closures (all five files to the byte), the `.flags`/`.call(`/mergeErrorState/Span/ParserFunction censuses, probe-file line cites (:47/:48), the dual-package 4/4, the trim-discard triple, dispatch é/range probes, dist cites (:14/:881/:882/:678-722/:1416), the CJS accessor (:55-58), gate shapes, tsconfig, and every value.js-side fact.

## Killed-claims register (16 sustained + 1 reversed)

Sustained as filed by the reader, each decider re-verified or re-derived by this seat where load-bearing: **K1** (C's S-1 dual-package headline — decider is row 5's 4/4 probe, mine); **K2** (C's 18+4/10-6-6 arithmetic — core.ts read: 3 types, 21 names); **K3** (Span KEEP rationale — start/end pair, no `kind`, zero producers; feeds row 37); **K4** (C-15 "only via /diagnostics" — index.ts:6 re-read, root-barrel path exists); **K5** (C-8's consequence backwards — my trim probes prove the booby-trap direction); **K6** (L's D-22 label-flags "defect" — the caller's own flags are the correct rendering); **K7** (L's PT-04 "CONTRADICTED in number" — different frames-per-depth, incomparable; disposition survives as row 18's FOLD); **K8** (L's 40,562/8,512/5,411 — mine: 40,576/8,517/5,412); **K9** (L's "34 sites" — 32/26); **K10** (13 → 16 test hits); **K11** (C's package.json:5 — :6); **K12** (C's five-subpath/two-symbol gate description — 4 subpaths, 3 checks); **K13** (C's :46 — :48, and the mirror defect is M8/row 36); **K14** (C's "worth 1.2 KB" — the true denial is utils.js 2,039 B); **K15** (L's "only caller of .call()" — 3 sites, census mine); **K16** (C-4 "cannot construct a labelled failure" — narrowed into row 7). **K17 REVERSED** — see Seat corrections 1 / row 42.

## Superlatives (banked, L-18 both ways)

- **S1 — getCijKey** (packrat.ts:70-100, every cite byte-exact this seat): hazard named with its history, bound derived from the float64 mantissa budget, RangeError at the boundary. Now sharpened by SF-1: the same package holds this standard at one aliasing boundary and not at the other two (dispatch keys, dispatch indices) — S1 is the measuring stick its own tree fails against.
- **S2 — the PT-Q5 RETRACT docblock** (leaf.ts:88-98, re-read verbatim): a shipped optimisation deleted for want of a real consumer, re-entry condition pre-stated against a named consumer's real grammar. The rarest discipline in a perf-oriented library.
- **S3 — the zero-allocation result discipline** is uniformly held: one state threaded through ~24 combinators, savedOffset backtracking, no per-step wrapper — and the consistency is the achievement.
- **S4 — two-level re-entrancy hardening** (parser.ts:43-48 try/finally + packrat.ts grow-loop finally), with the falsified prior design preserved in-tree as a warning (PT-Q1 note re-read).
- **S5 — both classic combinator footguns closed**: many()'s zero-width guard; sepBy()'s pre-separator checkpoint with the design boundary stated.
- **S6 — fuseAll's unrolling** targets a stated consumer shape ("the value.js hot shapes — 59 all() sites"), not a generic speedup claim.
- **S7 (halved)** — dispatch's read side is total and minimal (128-wide table, sign-bearing sentinel, `ch < 128` closure); the write side is row 42. Banked as a half.
- **S8 — the chain() falsy-seed cure**: wrong-answer bug fixed, dead parameter removed on measurement, landed in a major cut.
- **S9 — machine-enforced retirement hygiene**: fold row + version + finding + consumer count + gate name at every excision site; the no-dead-combinator gate proves its own grep machinery (vacuity guard) and sweeps sibling repos before permitting a delete.
- **S10 — the observable-truth standard**: proof-no-css-surface documents its own earlier unsoundness and observes the bundled dist's runtime keys. Both challenges adopted it; so did the reader; so did this seat.
- **S11 (reader's, verified)** — the CJS lowering emits `whitespace` as an accessor (dist/core.cjs:55-58, re-read verbatim), closing the one place the sideEffects hazard could have produced a real `undefined` under `require`.
- **S12 (reader's, verified)** — value.js's consume edge already owns the Result discipline the challenges demand of parse-that (`src/foundation/result.ts:1-6`, `ParseIssue`/`ParseResult` in the frozen 52): rows 2/19's invariant has a typed home one level above wherever a Parser would sit.

## MEASURE-AT-OPEN residue (bench-dependent rows for the lane's W1 harness)

1. **PT-03 armed-tax (1.47×)** — O-15 N=1, one machine; never re-derived by any core seat (latch hygiene forbids it outside a disposable process). Re-measure at W1 open before the figure is banked into any gate or cure justification (rows 10/30).
2. **PT-04 depth ceiling** — stack-budget property, not a library constant: 7,761 (O-15) and ≥9,000 (L) are both machine-facts of different constructions (K7). **G-9 asserts a typed bound exists, never a number** (row 18).
3. **V8 shape/allocation costs** — mapState's Object.create views (row 24), the per-construction/per-advance arrays (row 25), HOLEY arms (row 26): all unmeasured; no bench permitted here; harness rows if any cure is ever justified on speed rather than hygiene.
4. **Minified/gzip byte figures** — `minify:false` ships; the 93.52%/6.48% and 73%-maps ratios are graph facts, the absolutes move under a minifier (rows 21/40). Re-derive at the adoption or W4 wave's open with the byte closures in this file as the unminified baseline.
5. **RED-7 probe** — arms the latch under `--arm`, embeds a bench, has one never-RED and one never-GREEN row, and cannot run from the workspace its header names (rows 36 + M8). Repair first; run only in a disposable process; five of seven rows already reproduce statically in this corpus.
6. **Dual-format latch split witness** — row 5's behavioral corollary (enableDiagnostics in ESM leaves CJS parses unarmed) is derived by construction from the 4/4 identity probe; one disposable-process witness at W4 open **only if** the dual-format ship survives RC-P.

## Closing verdict

**Three blockers survive, and they compose into one sentence: the tier that exists to be the safe minimal substrate rejects valid input at end of input, reports success on failure, and ships the two subsystems its only docblock promises to exclude.** The roster routes cleanly: the engine repairs (rows 1/4, one leaf, one declared divergence) and the algebra retirements/wraps (rows 6-10, 15-18, 22, 41-42) are **W2**'s — with AC-3 holding the only pen that may touch parse-that source; the packaging truths (rows 3, 5, 20-21, 23, 31, 33, 37-40, 44, 47-48) are **W4/RC-P**'s, where the dual-package hazard the corpus certified as absent is in fact live at the condition boundary; two spec sharpenings leave as the **REFINEMENT FOLD**'s (the boundary invariant asserts `state.isError`, G-9 pins no depth constant, the RED-7 instrument is repaired before its figures are banked); and eleven rows have **no wave owner** and are recorded here, sharpest first (the three-convention line/column trio and the dead flag machinery with its stranded public union member), so any future parse-that source wave inherits a complete debt roster. The module's own craft is real — S1 through S6 are the work of a library that knows exactly how to name a hazard and gate it — which is precisely the indictment: getCijKey's fail-loud standard was available, in-tree, two files from every silent boundary on this surface. The fleet lesson repeats pt-ansi's, now with a wrong-answer witness attached: **C's S-1 superlative and the reader's K17 were both verification claims inherited rather than performed** — the first died on a `.cjs` file nobody opened, the second on a 257-entry table nobody built. Both challenges are otherwise strong and the hostile re-read is byte-exact almost everywhere it counted; the roster above is what survives all three, attacked.

— end of apotheosis. Both challenge records and the hostile re-read remain immutable beside this file per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.
