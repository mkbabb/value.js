claude-fable-5

# fr-ExportModal — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-10 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ExportModal.vue` (114 lines, read whole by this seat) + its consumer chain (`VisualizationView.vue`, `BasisCanvas.vue` export path, `AnimationControls.vue`, `FullscreenViewer.vue`) and its three glass-ui subpath dependencies.
**Substrate**: fourier HEAD `cd26c65` (2026-07-03) — re-pinned by this seat (`git log -1`), identical to the coordinate all three challenges and the fr-BasisCanvas apotheosis pin. **Dirty-tree disclosure**: 28 dirty rows; **the subject file and the whole `lib/canvas-drawing` subtree are CLEAN at HEAD**. `VisualizationView.vue` is drifted by exactly two equal-length glass-rename pairs (`UnderlineTabs`→`SegmentedTabs variant="underline"`, import + call site — my diff read); all anchors in this record are stable across the drift and were verified against the working tree. The four gallery dialog files carry 1–3-line rename pairs each; none is an adjudicated citation.
**Corpus**: three axes at `docs/tranches/V/megatranche/audit/fourier-components/ExportModal/` — challenge-D (design), challenge-L (library/code), challenge-C (consumption, incl. its §7 second-pass addendum and §7.0 anchor corrigendum).
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Returns arrived through the orchestration channel; every load-bearing or disputed cell was **re-derived by this seat with its own instruments** before adoption.
**Method (M-23/L-18)**: both readers were attacked before either was adopted. Every kill was re-derived (a weak kill gets un-killed); every missed find was re-proven before banking; every reader-vs-reader conflict was ruled with a fresh read. This seat demoted one composite both readers graded MAJOR (R-4), discharged an UNPROVEN hedge both readers left standing (R-6), and killed one stale limitation inside a reader's own missed find (K-14).
**Arbiter's own instruments this session (all static + node against installed dists; zero browser probes — probe parsimony; product source read-only)**: full reads of ExportModal.vue (114) and grid.ts (106); BasisCanvas.vue:460-516 (the whole export function); VisualizationView.vue:95-124/:233-238/:279-284; colors.ts `cssVarToHex` arm census (four forms + `#888888` fallback, no `oklch` arm); reka-ui `Switch/SwitchRoot.js` (`as: {default:"button"}` :53, `role:"switch"` :90, `aria-label` `$attrs` fallback :93); installed axe-core **4.11.4** `button-name` rule block read whole (`impact:'critical'`, `any: ['button-has-visible-text','aria-label','aria-labelledby',{title},'implicit-label','explicit-label','presentational-role']`) + `switch` role decl (`accessibleNameRequired: true, nameFromContent: true`); installed glass-ui **4.0.0** (`node`: 80 export keys, `./labeled-field` PRESENT with `LabeledSwitch.vue.d.ts` read, `./checkbox` ABSENT, `./button`/`./dialog`/`./switch` present); producer glass-ui **HEAD 8.0.0** (`node`: 70 keys; `./labeled-field`, `./button`, `./dialog`, `./switch` AND **`./checkbox`** all present); producer `src/components/dialog/styles.css` width-clamp/header/footer/✕ blocks + `DialogFooter.vue:9-12` confession read verbatim; devDeps census over glass-ui dist (`class-variance-authority` **8** files / `clsx` **1** / `tailwind-merge` **0** — third independent measurement); Dialog-mount census (`<Dialog[ >]` → **5 distinct files**; `DialogDescription` → exactly **3** files, GalleryCardModal not among them); web/package.json (`lucide-vue-next ^1.0.0` in devDependencies; glass-ui `^4.0.0`; value.js `^0.13.0`); `test-results/.last-run.json` (`"status": "failed"`); `DESIGN.md:31` + `lane-frontend.md:68-70` verbatim; registry fold-targets read at cited anchors (fr-BasisCanvas :27/:38/:43/:45-47/:59/:79/:99/:106/:117/:119 · fr-AdminAuditLog :36 · MigratePalettesDialog :77-80 · FlagReportDialog :42 · fr-EqCoefficientsPanel :53 · fr-ContourSettings :80 · fr-BasisSelector :26/:41/:69 · fr-AppHeader :48 · fr-EasingCurvePreview R-1/K-GEOM).

## Scoreboard

**44 rows ADJUDICATED-CONFIRMED (0 BLOCKER at this site · 8 MAJOR · 29 MINOR · 7 INFO — 13 of the 44 are identity-folds to banked rows, incl. one that folds into a banked BLOCKER) · 15 KILLS (3 minted by this seat) · 8 missed finds banked · 9 superlatives with provenance.**

The component's centre of gravity: **half its option surface is decorative** (folded to the banked fr-BasisCanvas export-truth cluster), **its two buttons sit on a deleted producer API** (folded to the banked AA-2 census BLOCKER), and **its four hand-rolled label rows re-implement a primitive the installed producer already ships** (`LabeledSwitch` — new, banked here). No new BLOCKER: every blocker-weight fact was already banked, and the identity guard held against three axes and one reader re-filing them.

Status law: dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25); nothing here stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **Grade of the dead-switch row** (D-B1/L-1/C-D-1) | BLOCKER, triple-witnessed | MAJOR — FOLD to banked fr-BasisCanvas.md:45; "filing BLOCKER a fourth time is a re-book at the wrong grade" | **LC wins — FOLD at MAJOR.** | fr-BasisCanvas.md:45 books `D-4/BC-2/C-2a — MAJOR (corpus BLOCKER demoted by both readers; ratified) → F.W3/W4` over the same emit/destructure pair AND already carries `FullscreenViewer.vue:138`'s bare `exportFrame()`. Identity guard: one identity, one grade — the bank's. The ExportModal-side increment survives inside the fold (the modal receives only `hasEpicycles` (:14-16) so it cannot self-gate; my read). |
| R-2 | **C/D-27 leg 2** ("theme-resolved strokes … dark ink on nothing") | CONFIRMED at MAJOR | KILLED — the PNG is theme-INVARIANT and internally split | **LC wins.** | My own `cssVarToHex` read: four match arms (`#`/`hsl()`/bare-triplet/`rgb()`) and `return "#888888"` — no `oklch` arm; the banked fr-BasisCanvas.md:38 BLOCKER (grey in BOTH themes) + :43 (module-eval frozen literals) make "theme-resolved" false. Leg 1 (no `fillRect` anywhere on the path) folds to banked D-24 (:79, MINOR). DU confirmed a cell that contradicts the registry it was required to sweep. |
| R-3 | **C-D-26's "by construction" cell** (axis glyphs outside the erase box) | CONFIRMED as filed, cell included | KILLED the cell — the "y" glyph is stage-width-dependent | **LC wins — ratified by this seat's own grid.ts read.** | `grid.ts:104` draws "y" at `(axisX + 10, 8 + arrowSize + 2)` = `(axisX+10, 18)`; `axisX` is `toScreen(0,0)[0]` (:50) — the screen x of data x=0, ≈ stage centre for a centred view, NOT width-anchored. y=18 < 100 always; whenever `axisX < 190` (any stage ≲380 CSS px, incl. the supported mobile canvas tab) the glyph lands INSIDE `clearRect(0,0,200,100)` and "Labels off" erases it. The vocabulary defect is therefore **non-deterministic across viewports** — worse than filed. |
| R-4 | **The unnamed-Switch composite** (L-6/D-m6/C-D-21/C-D-28) — grade and the "gate pincer" framing | MAJOR ("a green keystone proves nothing about this component's only controls") | MAJOR (static half) | **SEAT DEMOTES to MINOR; the pincer framing is half-killed (K-13).** | Three legs, all this seat's own: (1) reka `SwitchRoot` renders `as: "button"` (:53, my read) — a native `<button>` is an HTML **labelable element**, so the wrapping `<label>` is a valid host-language naming mechanism (accname step 2D): the four switches are named "Epicycles"/"Trace path"/"Grid lines"/"Labels" in any conformant engine. (2) Installed axe 4.11.4: `button-name` is `impact:'critical'` — it SURVIVES both keystones' serious/critical filters — it MATCHES these nodes (`switch` is `accessibleNameRequired`), and its `any` array contains `'implicit-label'`: the pass is a **true positive**, not blindness. Strip the label wrapper and the keystone goes red. Only the `aria-toggle-field-name` skip (namingMethods short-circuit) survives as genuine foreclosure. (3) The D axis itself filed D-m6 at MINOR with an explicit drop-on-live-names falsifier; the readers' MAJOR was a composite upgrade riding the framing killed here. Surviving content: the intra-tree idiom divergence (every other control names itself explicitly — D-m6 point 3, proven), reka's own name derivation declined (no `id`), the one-`as`-change fragility, and zero group semantics. Live-AT probe stays open with its direction REVERSED: expected named; **escalate** if an engine fails. |
| R-5 | **D-B2's novelty** (Button props deleted at producer) | MAJOR, filed as a fresh census contradiction ("add a `/button` row of ≥27 sites") | Substance confirmed; novelty and anchors killed — banked at fr-AdminAuditLog AA-2 | **LC wins — FOLD.** | fr-AdminAuditLog.md:36 verified by this seat: `AA-2 · D-B4 — ADJUDICATED BLOCKER (census-extension row)`, blast radius already re-measured at **37 files / 38 / 124**, census-gap probe already run → F.W1. DU's contradiction row is a re-book; its producer pin ("7.0.0") is stale (HEAD **8.0.0**, my `node` run — FR-EQC-16's re-pin gate, now at its fourth confirmation) and `Button.vue:15-30` no longer contains the interface at HEAD (K-10). The two ExportModal sites (:74-:75) are recorded as instances of AA-2's radius. |
| R-6 | **The D-m4/C-D-11 emission hedge** ("whether `[&_svg:not([class*=size-])]:size-(--ui-glyph)` is EMITTED is UNPROVEN-NEEDS-LIVE") | left standing | left standing | **SEAT DISCHARGES the hedge (K-15) — the rendering half is CONFIRMED, statically.** | The registry already closed this chain: fr-EasingCurvePreview R-1/K-GEOM ran the real `@tailwindcss/oxide` Scanner over `glass-ui/dist/*.js` (the exact dir the shipped `@source "../*.js"` targets), extracted the candidate, compiled it under fourier's installed tailwindcss 4.3.1 to `& svg:not([class*=size-]) { width/height: var(--ui-glyph) }`, and verified `style.css:3` imports the cascade through `@tailwindcss/postcss` — "K-GEOM is unconditional." The Jun-12 built artifact both readers cited proves nothing about the current pipeline. Consequence: the Download icon's `h-3.5 w-3.5` (:76) LOSES today — it paints at `--ui-glyph` (16 px fine-pointer; ~24–27 px coarse per the same banked row), and reader-DU's missed-5 (the ~2× coarse magnitude) folds to the same bank. A registry sweep would have found this; neither reader did (DISSENT §3). |
| R-7 | **Residual severity conflicts** (D-M4/L-9 always-open · D-M1 description · L-4 transparent-PNG · reader-DU's LabeledSwitch missed find) | MAJOR · MAJOR · (axes MAJOR) · MAJOR | MINOR · MINOR · INFO-per-bank · — | **MINOR · MINOR · FOLD at bank grade · MINOR.** | L-9: perceptual only, zero capability loss — the fr-BasisCanvas R-5 precedent; the `FullscreenViewer :visible` sibling inconsistency (VV:282, my read) rides the row. D-M1: role/title/trap all present; the absence is a dangling IDREF + missing prose — MINOR with DU's MAJOR preserved. L-4: banked BC-22 (INFO) + D-24 (MINOR alpha carry) rule; the reachability sharpening survives inside the fold. LabeledSwitch: the composite works and is likely named (R-4), so the re-implementation is design-system-first hygiene — MINOR per the fr-ContourSettings m-4 family precedent; DU's MAJOR preserved (DISSENT §2). |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · disposition → route.** Full proof cells live in the challenge files and reader returns, incorporated by reference; every anchor below was re-verified by this seat unless marked (adopted).

### Identity-folds — FOLD BY REFERENCE, never re-booked (identity guard)

- **D-B1 / L-1 / C-D-1 — MAJOR · FOLD → fr-BasisCanvas.md:45** (`D-4/BC-2/C-2a`, corpus BLOCKER demoted, ratified; R-1). `ExportModal.vue:37-38` emits `withEpicycles`/`withTrail`; `BasisCanvas.vue:466-469` destructures only `withGrid`/`withLabels` (both files, my reads). Increment recorded in the fold: the modal receives only `hasEpicycles` so it **cannot** self-gate, and the epicycle decision is re-derived at `BasisCanvas.vue:489` from props, never from the payload. → F.W3/W4 (export-truth).
- **L-5 / C-D-2b — MAJOR · FOLD → fr-BasisCanvas.md:46** (`BC-3/C-2b`: exportFrame is a drifted fork of drawFrame; image overlay silently exits every PNG; the structural cure is one pipeline). The second dialog-bypassing caller (`FullscreenViewer.vue:138`) is already inside the banked row. → F.W3/W4.
- **L-2 / D-4 / D-25 / C-D-4 — MAJOR · FOLD → fr-BasisCanvas.md:47** (`D-11/BC-4/C-12(vertical)`: `clearRect(0,0,200,100)` after the full draw over a never-filled canvas; erases unrelated art AND under-covers at 3 bases). My read confirms the C §7.0 corrigendum EXACT (`:498-500`, clearRect `:499`) and the L anchors off by 1–2 (K-5). The banked K-5 (fr-BasisCanvas.md:117) keeps the horizontal/fourth-row halves dead — no repair spec may inherit them. → F.W3/W4.
- **L-4 / C-D-3 — FOLD at bank grade → fr-BasisCanvas.md:99** (`BC-22 — INFO`, draw guarded / download unconditional) **+ :79** (`D-24 — MINOR`, alpha carry) (R-7). Increment: the full reachability chain re-verified by this seat — `VisualizationView.vue:121` (`hasData` includes `store.computing`), `:235-236` (overlay hosts AnimationControls), the menu item unconditional (adopted), so "Save PNG" during compute writes a fully transparent file while the on-screen canvas shows the placeholder. → F.W3/W4 rider.
- **C/D-27 (leg 1) — MINOR · FOLD → fr-BasisCanvas.md:79** (`D-24`: no opaque base before `toDataURL`; zero `fillRect` on the whole path — my grep-corroborated read of :472-513). Leg 2 KILLED (K-8/R-2). → F.W3/W4.
- **D-B2 — FOLD → fr-AdminAuditLog.md:36** (`AA-2 · D-B4 — ADJUDICATED BLOCKER`, census-extension row, F.W1) (R-5). At this site: both footer Buttons (`:74-:75`) carry `variant` + `size="default"` — legal at the installed 4.0.0 `ButtonVariants`, deleted at producer HEAD (`emphasis`/`tone`/`size: xs|sm|md|lg`). Producer HEAD is **8.0.0 / 70 keys / `./button` survives** (my node run) — the FR-EQC-16 re-pin gate, fourth confirmation. → F.W1.
- **C-D-6 — MAJOR · FOLD → fr-BasisCanvas.md:59** (`C-8`: glass-ui 4.0.0 peers value.js `^0.10||^0.11`, installed 0.13.0 — the F.W1 tri-package charter identity). C's scope honesty preserved: inert on this component's three import graphs. → F.W1.
- **D-M2 — MINOR at this site · FOLD → fr-AppHeader.md:48** (`FR-AH-2 · MISS-FONT`, blocker-weight cluster: `./styles/fonts` never imported, `--font-serif-math` declared nowhere — verified verbatim by this seat) — the five-way banked identity (+ fr-BasisSelector.md:69 m-3 · fr-CollapsibleSection.md:44 F-3 · fr-ContourPreview D:M-4 · fr-EqCoefficientsPanel D/M-7). The DialogTitle's `cm-serif` (:51) is the 27th consuming site; the narrow local cure is superseded by the banked root cause. → F.W1 token parity + F.W4 wiring per the banked row.
- **L-8 / D-i4 / C-D-2 — MINOR · FOLD → fr-BasisCanvas R-3 (:27) + K-7 (:119)** (`BC-13`: the untyped `Record<string, boolean>` bag, exactly 3 sites, named 0 times; the interface-cure claim already killed — no TS unread-member diagnostic, neither unused flag set). Increment adopted from reader-LC: `VisualizationView.vue:68`'s typed `InstanceType` ref means the bag is the ONLY hole in the chain. Note: LC's register citation ":82" is off — BC-13 is ruled at R-3; :82 is BC-14. → F.W3/W4.
- **L-16 — INFO · FOLD → fr-BasisCanvas.md:106** (`L §R5-7` census generalisation UPHELD): 0 `v-for`; four byte-identical hand-unrolled rows invisible to both census families; the mechanism that let the dead keys ship unnoticed. The data-shaping repair rides M-2. → NO-WAVE-OWNER (SS-3/SS-4 spec-authoring input).
- **Dialog width full-bleed + header alignment fork (inside M-3) — producer-owned · FOLD → MigratePalettesDialog.md:78** (D-8#1 → **A-21 GLASS-RELAY**: `w-full max-w-lg`, no inline inset, `DialogContentProps` exposes no width axis) **+ :79 / FlagReportDialog.md:42** (D-8#3 / A-11: `text-center sm:text-left` producer-authored three-way mobile fork). ExportModal passes no class to DialogHeader (:50) and is the only Dialog passing no width class — both facts land on the banked producer rows. See M-3 for the two increments this seat banked. → F.W1.
- **D-m4 / C-D-11 — MINOR · rendering half CONFIRMED via FOLD → fr-EasingCurvePreview R-1/K-GEOM** (R-6). The conformance half (declining the documented `size-*` escape hatch at `:76`) is unconditional and ExportModal-owned; the authored 14 px never paints — 16 px fine / ~24–27 px coarse per the banked chain. Reader-DU's missed-5 (the doc quoted only the fine-pointer arm) folds to the same bank. → F.W3/W4 (one-token cure: `size-3.5`, or accept the glyph token).
- **C-D-29 — INFO · FOLD → fr-BasisSelector.md:26 (R-2) + :41 (B-3)** (the `test.fixme` rationale cites a two-majors-stale pin; already load-bearing there — ux:150, this component's keystone, is the SOLE live axe keystone). → F.W3/W4 rider.

### MAJOR (new at this site)

- **L-3 / D-M5 / C-D-24 — MAJOR.** Zero error posture and zero confirmation on the export path: `doExport` closes the modal unconditionally after an optional-chained call (`VisualizationView.vue:99-102`, my read); `exportFrame` returns void with a silent early return (`BasisCanvas.vue:462-463`) and `:506-512` fires outside the data guard; the cure is declined in the same file — `useToast` imported and used for the publish path only (VV:13/:35/:111/:114, adopted). A silent no-op is indistinguishable from success. **ADJUDICATED → F.W3/W4** (export-truth: make `exportFrame` return an outcome; toast both arms; the banked BC-12 try/finally rides the same item).
- **D-M3 — MAJOR (substrate-owned; improves free at uplift).** The always-present ✕ is a 16×16 target at `opacity-70` with no padding, no min-size, no `data-size` — the coarse floor (`[data-size="icon"], .expandable-container__trigger, .segmented-tabs__trigger`) does not reach it (adopted, byte-verified by both readers). The producer's cure is live at HEAD: `dialog/styles.css` "THE ✕. 44×44 hit box … Rest ink is `--foreground` at ≥3:1 — the shipped `opacity-70` compounded every state into the same wash" (my verbatim read). **ADJUDICATED → F.W1** (free at the uplift; nothing to author locally).
- **L §X-A — MAJOR (census correction).** `lane-frontend.md:70`'s "Dead devDeps" row is wrong on the inference for two of three packages: my own census over the installed glass-ui dist — `class-variance-authority` imported by **8** chunks, `clsx` by **1** (`cn-DJXf4yaB.js`, imported by ALL THREE primitives this file uses), `tailwind-merge` by **0**. Both live packages are glass-ui peerDependencies resolved from the app's node_modules; acting on the row (or `DESIGN.md:32`'s "Remove unused CVA dependency") breaks the Vite build of `ExportModal.vue:3-11` specifically. Third independent measurement, three-way agreement. **ADJUDICATED → F.W1** (correct the census row + re-scope DESIGN.md:32 to `tailwind-merge` only, BEFORE any dependency sweep; L's F.W4 sweep rider noted).

### MINOR (new at this site)

- **D-M1-as-rescoped / L-10 / C-D-8 — MINOR** (R-7; headline killed, K-1). No `DialogDescription`: reka mints and binds `descriptionId` unconditionally, so a dangling `aria-describedby` ships in prod and the dev warning fires on every open; no prose anywhere states the output format. Axe files a dangling IDREF as `incomplete` — invisible to harnesses reading `violations` only. → F.W3/W4. (GalleryCardModal — no Description AND no Title — is the strictly-worse outlier; its home is its own record.)
- **L-6 / D-m6 / C-D-21 / C-D-28 — MINOR (SEAT-DEMOTED, R-4).** No EXPLICIT accessible name on any Switch (no `id`/`for`/`aria-label` — `grep "aria-"` → 0, my read); reka's own `[for]`-derivation declined; the sole implicit-idiom divergence in a tree that names every other control explicitly; zero group semantics. Names are expected to resolve via the implicit `<label>` on a native labelable `<button>` (R-4); the `aria-toggle-field-name` skip is real but `button-name` guards the floor at `critical`. Cure = M-2's `LabeledSwitch`/`LabeledField`, not four hand-written `aria-label`s. → F.W3/W4. Readers' MAJOR preserved (DISSENT §2).
- **L-9 / D-M4 / C-D-5 — MINOR** (R-7). `:open="true"` (:46) + parent `v-if` (VV:281): `useVModel` goes non-passive, `data-state` is permanently `"open"`, and the `popover-animate` closed arm + scrim fade are dead CSS on 100% of dismissals; the sibling `FullscreenViewer` uses `:visible` (VV:282, my read) — the file is not internally consistent. → F.W3/W4 (adopt the `computed({get:()=>true,set})` + `v-model:open` idiom or lift the flag; pick ONE idiom per C-D-18).
- **D-m1 — MINOR.** Footer buttons flush at <640 px (`flex-col-reverse` + `sm:gap-x-2` — column-gap on the cross axis); producer confession + cure verbatim at HEAD (`DialogFooter.vue:9-12`, `styles.css` footer block — my reads: "a real 0.00 gap … It is a row at EVERY viewport now"). → F.W1 (free at uplift).
- **D-m2-as-corrected — MINOR (broader than filed; escape cell killed, K-9).** Adjacent `touch-hit-area` halos overlap by exactly **0.125rem at every root size** under a coarse pointer — halo 2.75rem vs pitch 2.625rem (row 2.5rem from `text-base` + 2×0.5rem padding (:100), gap 0.125rem (:93)); every term is rem-derived, so the overlap is scale-invariant and fires **on every phone**, the regime the axis disclaimed. Registry-checked (reader-DU): seven banked `touch-hit-area` rows, none books adjacent-halo collision — new. → F.W3/W4 (pitch ≥ halo: `gap: 0.25rem` or row padding 0.625rem; or M-2's row anatomy).
- **D-m3 — MINOR.** Option labels at 28 px between a 24 px title edge and a 24 px button rail — a third left edge from no token (header/footer recipes declare no padding — extracted, adopted; `:100`'s `0.25rem` inline pad is the culprit, my read). → F.W3/W4.
- **D-m5 — MINOR.** `.option-row` teaches by `:hover` only; `grep "focus"` → 0 in the whole file (my read). One `:focus-within` rule restores keyboard parity. → F.W3/W4.
- **D-m7 / L-18 (+ M-6 folded in) — MINOR.** `showClose` left at default while the component composes its own Cancel: four dismissal paths on two code paths; and the ✕ is DOM-last but visually-first, so tab order runs …Cancel → Save PNG → ✕ (render-function read, adopted). Binding `:show-close="false"` — the decision the producer d.ts documents as the consumer's to make — resolves both. → F.W3/W4.
- **D-m8 / C-D-12 (+ M-8 folded in) — MINOR** (clipping killed, K-4). `min-width: 300px` (:87) is inert at ≥300 px viewports and ExportModal is the tree's WIDEST dialog purely by passing nothing (4 of 5 peers pass a narrower class — census adopted, 5-site list verified). Reader-LC's narrowing banked: `.option-list`/`.option-row`/`.option-label` are slot content compiled in the parent scope and carry `data-v-*` natively through the Teleport; only `.export-dialog` (a class on a child's fragment root) is at risk. Correct edit is deletion either way. → F.W3/W4.
- **D-m9 — MINOR** (K-3 kills the line-height leg; K-11 corrects the attribution). `text-lg` (:51) deterministically strips the recipe's `text-subheading` in glass-ui's **hand-rolled** merger (both regex entries share the `"font-size"` group key; last-wins — mechanism byte-verified by both readers): `text-wrap: balance` and the `--type-subheading` binding are lost; `font-semibold` restates the 600 the token carried. → F.W3/W4 (drop the raw utilities; `cm-serif` rides D-M2's fold).
- **D-m10 — MINOR.** Six user-facing strings, three grammatical registers; "Trace path" matches neither the code noun (`trail`) nor its siblings; format/resolution/instant-capture stated nowhere but the button. → F.W3/W4 (one register; name the artifact).
- **C-D-10 — MINOR.** `:107`'s `color-mix(in srgb, var(--muted) 50%, transparent)` is byte-identical to the shipped `--muted-medium` token (adopted; 0 consumers of the token tree-wide, 6 more raw sites — one instance of a tree-wide bypass). → F.W3/W4.
- **C-D-13 — MINOR.** `surface` left at the `glass` default while all four peers pass `surface="opaque"` — and this is the only dialog over the animated canvas (census adopted). Legibility UNPROVEN; the inconsistency is source-certain. → F.W3/W4.
- **C-D-14 — MINOR.** Emphasis hierarchy inverted: opaque `outline` Cancel beside translucent `default` primary; `solid` unused; peers use `ghost` for dismissive actions (cva body adopted). → F.W1 (decided during the AA-2 emphasis/tone migration, per-site).
- **L-12 / C-D-15 — MINOR.** Restated cva defaults are not inert — they materialise `data-variant="default"`/`data-size="default"` attributes the omitting peers lack, and become load-bearing breaks at the uplift. → F.W1 (dies in the AA-2 migration).
- **L-11 / C-D-9 — MINOR.** `aria-modal` asserted at `:29` and `:47` and echoed at `visualization-crud.spec.ts:655`, rendered by neither dependency (0 occurrences in either dist — adopted, twice-measured); modality is real via `useHideOthers`. Three comment corrections ride whatever wave touches these files. → F.W3/W4.
- **L-14 / C-D-19 — MINOR.** Four `ref(true)` + remount-per-open = preferences reset every open, in a view that persists every sibling preference (localStorage + debounced workspace round-trip — VV:37-38/:52-63, adopted). Sequenced AFTER the key set settles (the fold's cure). → F.W3/W4.
- **C-D-20 — MINOR.** `DESIGN.md:31` still books this component's Teleport→Dialog migration as open — both halves stale (verbatim re-read by this seat; the two live `<Teleport>`s are PaperSearchModal + FullscreenViewer). A live mis-signal for F-planning. → F.W3/W4 (doc hygiene; DESIGN.md:32's re-scope rides X-A).
- **C-D-17 / L-15 — MINOR.** `lucide-vue-next` imported at module scope in shipped code (:12) from **devDependencies** (my package.json read); glass-ui peers and vendors the different `@lucide/vue`, so two lucide runtimes render inside this one dialog. 1 of the census's 35 rename sites (lane-frontend.md:68, verified). → F.W1.
- **C-D-18 — MINOR.** Three spellings of the always-open Dialog idiom and two `defineEmits` forms across adjacent files (adopted; census verified in part via my GalleryCardModal greps). One idiom, chosen once. → F.W3/W4.
- **C-D-26-as-worsened — MINOR** (R-3). The two text-bearing switches do not partition the canvas text: the axis glyphs are drawn by `drawGrid` (gated by `withGrid`), and whether "Labels off" erases the "y" glyph depends on stage width — the vocabulary is wrong in both directions AND non-deterministic across viewports (my grid.ts read). → F.W3/W4 (export-truth vocabulary; the option set is re-derived with the fold's key-set cure).
- **M-1 — MINOR (missed find, reader-LC; re-proven by this seat).** The "Trace path" row is missing the `v-if="hasEpicycles"` guard its twin carries, though the trail has the identical precondition: `:55` gates Epicycles and `:37` re-masks its emit; `:59-62`/`:38` do neither for Trace path — my read of both files confirms the trail draws only under `fourier-epicycles` (BasisCanvas :144-145 inside `drawEpicycleFrame`; :329-330 under `:319 if (hasEpicyclesMode && …)`). On a chebyshev/legendre-only workspace the modal offers a switch for an artifact that cannot exist while hiding its sibling under the same condition. Narrows superlative S-2. → F.W3/W4.
- **M-2 — MINOR (missed find, reader-DU; verified by this seat at BOTH pins; grade per R-7).** glass-ui ships `./labeled-field` at the installed 4.0.0 (80-key map, `LabeledSwitch.vue.d.ts` props `{checked, label, tooltip, labelClass?, required?, hideLabel?}` + `update:checked` — my node run and d.ts read) and it survives to producer 8.0.0 (my run). The four hand-rolled `label > span + Switch` rows re-implement exactly this primitive — the one that owns the `for`/`id` wiring all three axes prescribe by hand. Closes the whole L-6 cluster with library wiring, re-enables reka's `[for]` name derivation, and retires the hand-unroll (L-16). Constellation precedent: keyframes' LayerConfigPanel imports it (adopted). → F.W3/W4. DU's MAJOR preserved (DISSENT §2).
- **M-4 — MINOR (missed find, reader-DU; limitation corrected by this seat, K-14).** `role="switch"` is the wrong semantic for a deferred-commit option set (nothing changes until "Save PNG"): ARIA/APG reserve `switch` for immediate effect; eight corpus rows take the role as given, none questions it; and the four controls have no group boundary (no fieldset/legend/`role="group"`). DU's "no `./checkbox` subpath — producer relay only" is stale: **producer 8.0.0 exports `./checkbox`** (my node run), so the cure is a consumer edit sequenced after F.W1, no relay needed. → F.W3/W4 (after F.W1).

### INFO (new at this site)

- **C-D-22 — INFO.** Zero coupling to the 45-operation API surface; the exported artifact has no server counterpart — **R6-8 is empty by construction for this leaf** (counts re-derived by reader-LC; `createSnapshot` is a parameter-record alias, not a render). → NO-WAVE-OWNER (census/spec input).
- **C-D-23 — INFO.** The last recorded run of this component's a11y keystone died at affordance DISCOVERY (`waiting for getByText('Export')`), before `checkA11y` — `.last-run.json` `"status": "failed"` re-read by this seat. And it is the SOLE live axe keystone (fold: C-D-29). → F.W3/W4 rider (e2e repair; a green run would still not see the four switches' semantics — but see R-4: it DOES guard their names).
- **L-17 / D-i3 — INFO.** `@reference "tailwindcss"` (:85) pulled in to serve one `@apply text-base` (:111) beside raw CSS — 35-of-66-SFC house idiom (counts adopted). → F.W3/W4 rider.
- **M-3 — INFO (missed find, reader-DU; both underlying defects FOLD, increments banked; producer facts verified by this seat).** The D-axis §7 uplift ledger omits two more free producer improvements on this file: the width clamp (`inline-size: min(100% - 2*var(--space-section), 32rem)`) and the header `text-align: start` — both verbatim at producer HEAD (my reads), both banked defects (see the fold row). The two increments: (a) the rows belong in ExportModal's F.W1 ledger; (b) **the A-21 GLASS-RELAY producer ask is SATISFIED at 8.0.0 — the discharge is recorded here first**; the relay row can close at the uplift. → F.W1.

## Killed-claims register

- **K-1 · D-M1's headline + census — KILLED** (both readers; ratified by this seat's own census: `<Dialog[ >]` spans **5 files**, not 4; `DialogDescription` in exactly 3; GalleryCardModal has neither Description NOR Title — a second, strictly worse outlier the D table omitted). The underlying absence survives at MINOR.
- **K-2 · D-m6 point 2** ("`role="switch"` is nameFrom: author — contents prohibited") — **KILLED** (reader-DU; byte-ratified by this seat: installed axe 4.11.4 declares `switch … nameFromContent: true`, matching ARIA 1.2). The point was stated backwards; C had it right — a corpus self-contradiction resolved against D.
- **K-3 · D-m9's `--type-leading-body` leg — KILLED** (reader-DU; the recipe's own `leading-none` ships in the same class string and already overrides it at every call site — nothing was live to lose). Two legs survive.
- **K-4 · D-m8's "buttons clip" consequence — KILLED** (reader-DU; at 280 px the 300 px plate overflows 10 px of its own padding per side; the `p-6` content box spans x 14…266 — no control clips). The inert-or-harmful verdict and the deletion cure are unchanged.
- **K-5 · reader-uncorrected L-axis anchor drift — KILLED as citations** (reader-DU; ratified by this seat's BasisCanvas read: the `showLabels` block is `:498-500` with `clearRect` at `:499`, `getContext` at `:475`, `toDataURL` at `:506`; the C §7.0 corrigendum is exact). Every underlying claim survives; carries must land on the corrigendum's anchors.
- **K-6 · C/D-21's label anchors — KILLED as citations** (reader-DU; my read: openers at `:55/:59/:63/:67`; `:58/:62/:66` are closers). Claim survives.
- **K-7 · D-B1's "a shared `ExportOptions` interface would have made this a build error" — KILLED** (reader-LC; the banked fr-BasisCanvas K-7 ratified: TypeScript has no unread-member diagnostic; neither unused flag is set). L's narrower excess-property variant *enforces* the fix, it does not *detect* the bug; C's honest phrasing is the version to carry.
- **K-8 · C/D-27 leg 2** ("theme-resolved strokes … dark ink on nothing") — **KILLED** (reader-LC; R-2; my own `cssVarToHex` read + the banked D-1/D-2: the export is grey-poisoned and theme-INVARIANT, with frozen literals split against live greys).
- **K-9 · D-m2's phone-escape cell — KILLED** (both readers, convergent; the cell compared a rem-scaled pitch to a hard-coded 44 px; `--touch-target` is rem too — overlap is 0.125rem at every root, and the disclaimed regime is the certain one).
- **K-10 · D-B2's novelty + anchors — KILLED** (reader-LC; R-5; AA-2 verified by this seat; producer pin stale at 8.0.0; `Button.vue:15-30` no longer contains the interface; `tone?: Tone` is narrowed to `ButtonTone`). Substance folded, not lost.
- **K-11 · D-m9/S-6's twMerge attribution — KILLED** (reader-LC; glass-ui imports `tailwind-merge` in **0** dist files — my third-party count; `cn` is clsx + a hand-rolled ~60-entry group table, last-wins). The determinism mechanism survives; S-6's falsifier ("remove `subheading` from the regex") would reason about a library not in the tree.
- **K-12 · C-D-26's "by construction" cell — KILLED** (reader-LC; **ratified by this seat's own grid.ts read** — R-3: the "y" glyph is `(axisX+10, 18)` with `axisX` view-dependent; erased at stage ≲380 px). The finding worsens: viewport-non-deterministic.
- **K-13 · the "both axe rules foreclosed / a green keystone proves nothing about the only controls" framing — HALF-KILLED BY THIS SEAT** (R-4). `button-name` is `impact:'critical'`, matches these nodes, and passes truthfully via `'implicit-label'` on a native labelable `<button>` — a live detector, not a blind spot; strip the label and the keystone reds. Only the `aria-toggle-field-name` naming-methods skip survives as foreclosure. Drives the L-6 demotion.
- **K-14 · M-4's "no `./checkbox` — producer relay, not consumer edit" — KILLED BY THIS SEAT.** Producer HEAD 8.0.0 exports `./checkbox` (my node run against `/Users/mkbabb/Programming/glass-ui`). The cure is a consumer edit sequenced after F.W1; no relay.
- **K-15 · both readers' "emission half UNPROVEN-NEEDS-LIVE" hedge (D-m4/C-D-11) — DISCHARGED BY THIS SEAT** (R-6). fr-EasingCurvePreview R-1/K-GEOM closed the delivery chain statically and unconditionally; the rendering half is CONFIRMED without a build. The hedge was an inherited corpus cell that a registry sweep retires.

## Superlatives — with provenance (L-18 runs both ways)

1. **The dialog stays modal across a three-file Boolean-cast footgun** (reader-LC S-1, re-derivation adopted; corroborated by C's K-6): glass-ui's shim declares `modal: {type: Boolean}` with no default — Vue casting would materialise `modal === false` and demote reka to the non-modal content path — but `useForwardProps` forwards only defined keys, so reka's `default: true` stands and `useHideOthers` is genuinely reached. One keystroke (`:modal="true"` nowhere) from breaking for every consumer — worth the standing glass-ui BH relay.
2. **The emit re-gates against the prop, not the hidden control** (all three axes + both readers; `:37` re-read by this seat): a `v-if`-hidden row's ref stays `true`, and the author closed that at the emit boundary — exactly the bug class the folded MAJOR is, done right. **Narrowed by M-1**: the discipline was applied to one of the two rows that needed it, not to the class.
3. **Zero-leak by construction** (both readers): 43 script lines, no lifecycle hooks, no watchers, no timers, no listeners, no `defineExpose`; every subscription in the mounted subtree is reka's and cleans itself up. The contrast sits one file away on the render path this component drives (BasisCanvas's observer/credit teardown).
4. **Flawless subpath addressing that survives a version the corpus did not know about** (both readers; both pins verified by this seat): three declared subpath imports, zero dist reach-through, zero root barrel, zero direct reka, zero shadcn shadow — and `./button`/`./dialog`/`./switch` all survive the 80→70-key cut at 8.0.0. The minimal `<Switch v-model>` binding surface is likewise uplift-immune. **Narrowed by M-2**: the imports are clean; the composite is bespoke.
5. **The PRM bracket reaches an inline style** (reader-DU's extension, adopted): the a11y-overrides clamp (`!important`, safe-listed transitions) beats the SwitchThumb's non-important inline `transition: translate …` — the thumb snaps under PRM with zero local code. A grep for the local PRM idiom would file a false positive here.
6. **The `onOpenChange` comment is real engineering prose with one false clause** (both readers): it names the owning primitive and enumerates the three `@update:open(false)` sources exhaustively; its `aria-modal` clause is corpus-killed (L-11) and has propagated into the e2e comment. Correct the attribute, keep the prose habit — name `useHideOthers` in its place.
7. **The historical HIGH is genuinely discharged, and the discharge explains the blind spot** (both readers): the 2026-05-26 L5 audit's single HIGH a11y gap (hand-rolled Teleport modal, 147 lines) is now 114 lines with zero owned modal machinery — and every prior audit examined the chassis, none opened the contract, which is how a 50%-decorative option surface survived two tranches.
8. **The producer's hand-rolled merger makes semantic-token overrides deterministic** (D S-6, attribution corrected per K-11): two `"font-size"`-keyed entries collide `text-lg` with `text-subheading` in one group, last-wins — a clean, legible override rather than a build-order cascade race. Does not excuse D-m9; explains why it is deterministic.
9. **C's §7.0 anchor corrigendum is the falsifiable form of self-audit** (both readers; ratified by this seat's own BasisCanvas read — all four corrected tail anchors exact, all nine ExportModal head anchors exact). The L axis's equivalent drift went uncorrected (K-5). Re-measuring your own citations before a carry lands on them is the practice this registry should copy — and this seat extends the same demand to *registry* sweeps (K-15).

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **Real-AT names of the four switches** (Chromium AX tree + VoiceOver/NVDA). Direction REVERSED by R-4: expected "Epicycles"/"Trace path"/"Grid lines"/"Labels" via the implicit label; **escalate L-6 back to MAJOR** if any engine returns unnamed. The M-2 cure is right either way.
2. **Rendered face of the DialogTitle's `cm-serif`** — a whole-tree probe per the banked FR-AH-2, not an ExportModal one.
3. **Frame-level exit capture** — record dismissal ≥60 fps via Esc/backdrop/✕/Cancel; confirm no `data-state="closed"` frame paints for plate or scrim (source chain proven).
4. **Coarse-pointer halo rects** for adjacent `touch-hit-area` `::before` boxes at both root sizes — confirmation only; the rem arithmetic is closed at every root (K-9).
5. **Download icon computed size** — corroboration only after K-15: expect `--ui-glyph` (16 px fine / ~24–27 px coarse), not 14 px.
6. **`.export-dialog` scope-attribute survival** through the Teleport (narrowed to this ONE rule by M-8's slot-content analysis); deletion is correct either way.
7. **Sampled contrast**: the `opacity-70` ✕ (pre-uplift) and `.option-row:hover`'s 50% muted wash over the glass scrim, both themes; plus C-D-13's `glass` surface legibility over the animated canvas.
8. **`document.activeElement` after Cancel / Esc / ✕ / Save** — the structural half (every close routes through the parent-`v-if` unmount path) is proven; the focus destination is not decidable statically.
9. **Width of the `store.computing` race window** (the code path is certain; the exposure is timing-dependent).
10. **Surviving glyph band below y=100 at three active bases** (queued at fr-BasisCanvas UNPROVEN-5) **+ the narrow-stage "y"-glyph erase** (R-3's sharpening: probe at stage <380 px with Labels off).
11. **Export-affordance reachability at desktop** — the C-D-23 rerun settles the keystone's evidentiary status and the discovery-path question together.
12. **Whether ATs materially mis-report the staged options as immediately-effective** given `role="switch"` (M-4's announced consequence).

## DISSENT — preserved, not ruled away

1. **Severity dissents preserved.** All three axes and reader-DU graded the dead-switch row BLOCKER (this record folds it at the bank's MAJOR — if the F.W3/W4 spec finds the fold's grade under-weighted for a control surface that lies to the user, the re-grade argument starts from DU's cells). DU's MAJOR on D-M1, D-M4, D-M2-at-this-site, the L-6 composite, and M-2 are preserved against this seat's MINOR rulings (R-4/R-7); if SS-13 §1 returns an unnamed engine, L-6 re-opens at MAJOR by the recorded trigger.
2. **Reader scorecard, both directions.** reader-DU: three cells died under registry or fresh reads it should have caught — the C/D-27 leg-2 confirmation contradicted banked D-1/D-2 (R-2), the C-D-26 "by construction" adoption died under one grid.ts read (R-3), and D-B2 was re-filed as novel over a banked BLOCKER (R-5). Beside these sat genuinely excellent original work: the LabeledSwitch find (M-2), the A-21 discharge (M-3), and the D-m2 scale-invariance correction. reader-LC: citation slip (BC-13 "register :82"), and it left the same emission hedge standing as DU — but its six kills all survived this seat's attack, two of them (K-8, K-12) against its sibling's confirmations.
3. **The inherited-hedge event (evidence about the fleet).** Both readers carried the corpus's "emission UNPROVEN-NEEDS-LIVE" cell forward verbatim when the registry had already closed the chain (fr-EasingCurvePreview R-1, banked before this corpus was read). Same signature as the ColorSpaceSelector/fr-BasisCanvas citation-inheritance events, in hedge form: an unproven marker is also a claim, and it also demands re-derivation — against the registry, not just the tree.
4. **This seat's own scope notes.** (a) The R-4 demotion rests on HTML-AAM labelable-element law plus the installed axe rule composition (both verified here) — not on a live AT transcript; the probe stays open with its direction stated. (b) VisualizationView anchors were verified against the dirty working tree (two equal-length rename pairs); a committed-tree re-read should accompany any carry that lands there. (c) Adopted-not-re-read cells are marked "(adopted)" throughout; every verdict-deciding cell was re-derived.

## Closing verdict

**ADJUDICATED — no new BLOCKER: the identity guard held against three axes and one reader re-filing banked blocker-weight facts; 44 rows confirmed (8 MAJOR · 29 MINOR · 7 INFO, 13 identity-folds incl. into the AA-2 census BLOCKER), 15 kills banked (3 minted by this seat: the axe-pincer half-kill that demotes L-6, the stale checkbox-relay limitation, and the discharge of an emission hedge the registry had already closed), 8 missed finds adopted after re-proof (LabeledSwitch is the structural cure for the whole naming cluster; the Trace-path guard asymmetry narrows the corpus's best superlative); routings are M-25 agglomeration input — F.W1 owns the button-prop/census/font/lucide/peer substrate cluster and collects three free producer improvements plus the recorded A-21 discharge, F.W3/W4 owns the export-truth and dialog-anatomy cluster with the fr-BasisCanvas folds, NO-WAVE-OWNER carries the two census generalisations; nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
