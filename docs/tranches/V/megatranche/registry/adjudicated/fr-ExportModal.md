claude-fable-5

# fr-ExportModal — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ExportModal.vue` (114 lines, read whole again this session) + its consumer chain and three glass-ui subpath dependencies · **Substrate**: fourier HEAD `cd26c65` re-pinned this session (`git log -1`); the subject file is CLEAN (`git status --porcelain` on it → empty). `VisualizationView.vue` anchors drift ±1 from some reader cites (the r1-disclosed rename-pair drift); every cited VV fact re-read at the working tree: `useToast` :13/:35, `doExport` :98-103 (optional-chain then unconditional close), `hasData` :122 (includes `store.computing`), `hasEpicycles` :123, `:active-bases` to BasisCanvas :199, `<ExportModal v-if>` :281, `<FullscreenViewer :visible>` :282.
**Corpus adjudicated (r2)**: two fresh hostile re-reader returns over the full three-axis challenge corpus AND the r1 bank — **reader-DU** (design-and-usage: 26 confirmation cells · 10 kill-register rows, 3 minted · 5 missed finds M-α..M-ε · 7 superlatives · 10 unproven) and **reader-LC** (library-and-code: 7 confirmation cells · 4 kills · 3 missed finds LC-1..LC-3 · 6 superlatives · 5 unproven). Corpus cells at every disputed anchor spot-checked against the challenge files by this seat (challenge-D :243/:270-275/:456-458/:686-690 · challenge-L :135-138 · challenge-C :455-458 — all quoted faithfully by the readers).
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-10 r1 apotheosis at this same path (git history holds it; this corpus's reader citations of `fr-ExportModal.md:NN` resolve against the **r1 revision**). r1's 44-row roster, 15 kills (K-1..K-15), 9 superlatives, and 12 UNPROVEN items are **preserved-by-carriage under their original ids**; r1's verdicts are corrected in **zero** places and r1's *prescribed cures* in exactly **two** (M-α: the D-m7 `:show-close="false"` spelling; LC-3: the M-2 `LabeledSwitch` anatomy) — both cure-integrity corrections, the ColorSpaceSelector addendum-2 law ("the cure is adjudicated separately from the defect") firing twice against the same bank.
**Producer drift disclosure**: glass-ui HEAD moved since the readers' session (`4a86570b` → `558c3fa3`, version still **8.0.0**). Every producer-HEAD fact this record relies on was **re-verified at `558c3fa3` by this seat**: `showClose` grep over src → 0; `dismiss: "free"|"deliberate"|"locked"` with the "old boolean close knob folds into it" docblock; `closeVisible = dismiss === "free"` (:147); `surface?: Surface` survives; `spring`/`scrimAnimation` absent from `DialogContentProps`; 70 export keys with `./button`/`./dialog`/`./switch`/`./labeled-field`/`./checkbox` all present; all five named dialog symbols exported; width clamp + gutter docblock (styles.css :14-27); title √φ block with **zero** `font-weight` in the whole file; ✕ 44×44 block (:95-118) with the `opacity-70` confession; `--space-section` 2rem/1.25rem (sizing.css :123/:322); `index.css:254` plain (unlayered) `@import` of the dialog lane.
**Method (incredulity)**: every kill was re-derived before sustaining (a weak kill gets un-killed); every missed find re-proven at the bytes before admission; every reader-vs-reader and reader-vs-bank conflict ruled with fresh instruments. Two of reader-DU's increments **failed** re-derivation and are killed by this seat (K-22, K-23); all three of reader-LC's missed finds and all five of reader-DU's missed-find *mechanisms* survived. Zero browser probes (probe parsimony); product source read-only; **sole write = this file**.
**This seat's own instruments (all static/node, this session)**: ExportModal.vue whole; BasisCanvas.vue :460-516 (export tail: destructure :466-469, `getContext("2d")!` :475 / `setTransform` :476, guard :484, `hasEpic` :489, `clearRect(0,0,200,100)` :498-500, restore-before-`toDataURL` :504/:506, `removeChild` :512); VisualizationView regions above; `visualization-ux.spec.ts` :26-40 (unscoped AxeBuilder, violations-only, serious/critical, wcag2a/2aa/21a/21aa) + :150-165 (keystone awaits `[role="dialog"]` visible then `checkA11y`); `visualization-crud.spec.ts` :84-90 (same unscoped helper) + :616-664 (the fixme rationale verbatim + the ExportModal keystone running `establishSession`+`openWorkspace` first); installed **axe-core 4.11.4**: `isModalOpen` (visible `dialog, [role=dialog], [aria-modal=true]` → cached true), `aria-hidden-focus` rule block (`impact:'serious'`, `tags:[…'wcag2a','wcag412'…]`, `all:['focusable-modal-open','focusable-disabled','focusable-not-tabbable']`), `focusableModalOpenEvaluate` (`return void 0` under open modal → **incomplete**), the other two evaluates (`return true` under open modal), `button-name` rule + `noExplicitNameRequired` body (role `switch` → `accessibleNameRequired` → matches), `aria-toggle-field-name` (`none: ['no-implicit-explicit-label']` PRESENT — the corpus dropped it), `landmark-one-main`/`page-has-heading-one` both `best-practice`-tagged; **reka-ui 2.9.10**: `DialogContentModal.js` :48 `useHideOthers` + :57-62 `onCloseAutoFocus` (guarded-then-prevent + `triggerElement?.focus()`), `DialogContentImpl.js` :41/:68 (`openAutoFocus` emit), :50/:78 (`descriptionId` minted + bound), `FocusScope.js` :90-104 (`focusFirst(getTabbableCandidates(container))` unless mount event prevented), `FocusScope/utils.js` :40-45 (TreeWalker DOM order, `tabIndex >= 0`); **aria-hidden 1.2.6** `hideOthers` = `applyAttributeToOthers(…, 'aria-hidden')` (:124-135), `inertOthers`/`suppressOthers` shipped and uncalled by reka; installed **glass-ui 4.0.0**: `LabeledSwitch.vue.d.ts` whole (`tooltip: string` REQUIRED, `update:checked`), `labeled-field.js` (LabeledField label-FIRST + IconTooltip route on truthy tooltip + slot `{errorId, controlId, labelledBy}`; LabeledSwitch = `<div class="flex items-center">` + `<Switch :id>`), `base.css` :79-97 (`.labeled-field { display:flex; flex-direction:column; inline-size:100% }` + the width-contract docblock) and :62-70 (`.labeled-field-label … cursor: help`), `DialogContent-DDE6pQBU.js` (`showClose: {type:Boolean, default:!0}`; base `C = "fixed left-1/2 top-1/2 z-modal grid w-full max-w-lg gap-4 p-6"`; render slot-FIRST/✕-LAST with the ✕ class string verbatim; `surface: {default:"glass"}`; `spring`+`scrimAnimation` present at the pin), `typography/scale.css` :110-114 (`--type-body: clamp(1rem, 0.92rem + 0.27vw, 1.375rem)`), `typography/utilities.css` :60-68 (`@utility cm-serif { font-family: var(--font-serif-math, serif) }`), `theme/bridges.css` :61-70 (`--font-serif: var(--font-stack-text)` — "the library carries no display-serif voice"), `scheme-motion.css` :43 (`--font-stack-text: "Plus Jakarta Sans", …`), `--font-serif-math` declared **nowhere** (grep 0, dist styles + fourier src); **tailwindcss 4.3.1**: `preflight.css` :73-81 (`h1..h6 { font-size: inherit; font-weight: inherit }`), `dist/lib.js` static `hover` variant registration (`&:hover` wrapped in `@media (hover: hover)` — byte-read); fourier `package.json` :14 (`@mkbabb/glass-ui: ^4.0.0`) / :35 (`lucide-vue-next` in devDependencies); fourier `style.css` :1-3 (cascade imports) / :13-15 (`@theme --font-sans: "Computer Modern Serif"…`) / :17-21 (`html, body { @apply … font-serif }` — **`font-serif`, not `font-sans`**) / :40-50 (mobile-first root 1.125rem→1rem); registry sweeps: `aria-hidden-focus|isModalOpen|focusable-modal-open` → 6 files, none books the suppression mechanism, `fr-CoefficientsSpectrum.md` :82/:117 books the opposite direction; fold targets re-read at `fr-DarkModeToggle.md:102` (N-16), `fr-AdminFlaggedPanel.md:91` (FR-AFP-37), r1 whole.

## Scoreboard

**52 adjudicated roster rows — 0 BLOCKER · 11 MAJOR · 34 MINOR · 7 INFO** (r1's 44 identities carried whole, none re-graded; **8 new rows admitted** from this corpus's missed finds: 3 MAJOR from reader-LC, 5 MINOR from reader-DU). **23 killed-claims register entries** (r1's 15 sustained untouched; **8 new this round — 6 sustained reader kills, 2 minted by this seat against reader-DU's own increments**). **14 superlatives** (r1's 9 sustained; 5 admitted from this corpus). **2 corrections against r1's own prescribed cures** (M-α, LC-3) — the identity guard held in r1; the cure-compatibility check did not, twice, and this round adds it to the standing law.

The round's centre of gravity is **gate integrity**: the tree's sole live axe keystone is silently narrowed by its own subject (LC-1 — opening ExportModal converts `aria-hidden-focus` page-wide from violation to `incomplete`, and both harnesses read `violations` only), its neighbouring `test.fixme` justifies itself with a compensating-control claim that is structurally false (LC-2), and the two banked cures for this component's naming and dismissal clusters were both authored against APIs or anatomies that do not survive contact (LC-3, M-α). No new BLOCKER; no banked verdict moved.

Status law: dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25); nothing here stamps a wave.

## Ruled disagreements and corrections (the r2 seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R2-1 | **Grounds for killing D-M3's WCAG 2.5.8 headline** | Equivalent exception (Cancel, ≥40 px, same function) | Spacing exception (24 px circle intersects nothing) + wcag2.2-outside-tag-scope + "every pointer type" category error | **BOTH SUSTAINED — one kill, two independent normative grounds (K-16).** | Corpus quote verified at challenge-D:270-275. Cancel's floor: `size="default"` → `--control-h-md` ≥ 40 px (r1-verified chain). The ✕ at `right-4 top-4` in a `p-6` plate: the 24 px circle extends 4 px past the 16×16 glyph; nearest interactive nodes are tens of px away. Keystone tags are `wcag2a/2aa/21a/21aa` (my spec read) — a WCAG **2.2** SC is outside them by construction. The banked D-M3 geometry MAJOR (r1, F.W1-free) is untouched; DU's irony rider recorded: the ✕ is rescued by the very Cancel D-m7 keeps, so no cure may delete Cancel while the ✕ is undersized (moot after F.W1's 44×44 ✕). DU's sharpening banked into the row: `hover:opacity-100` is capability-gated by tailwind's static `hover` variant (byte-read), so on coarse pointers the glyph **never** recovers from 70 % opacity. |
| R2-2 | **R-4 leg 1** (implicit `<label>` names a `<button role="switch">`) | takes the bank's demotion as given | contests leg 1: HTML-AAM's button accname enumerates aria-labelledby → aria-label → subtree → title, **not** `<label>`; "labelable" ≠ "named by its label" | **LC's caveat ADOPTED; grade unchanged (MINOR + recorded trigger); the SS-13 probe is re-classed LOAD-BEARING, not corroborative.** | This seat re-derived leg 2 to the matcher body (`noExplicitNameRequired`: explicit role `switch` → `accessibleNameRequired` → matches; `any` carries `'implicit-label'`) — axe **does** guard these names at `critical` and the keystone reds if the `<label>` wrapper is stripped. But axe's `implicit-label` passing is axe's *model* of the platform, and LC is right that the HTML-AAM leg is contested; if any real engine returns unnamed, L-6 re-opens at MAJOR by the r1 trigger. LC's failed attack on leg 2 is recorded as a negative-result superlative. |
| R2-3 | **Grade of the M-2 cure correction** (LC-3) | — | MAJOR | **MAJOR SUSTAINED (spec-integrity class).** | The banked M-2 row says "closes the whole L-6 cluster with library wiring" and cites only the `.d.ts` prop list. This seat read the *implementation*: `.labeled-field` is `flex-direction: column; inline-size: 100%` (base.css :94-96, with the width contract authored in prose), the label renders FIRST, `tooltip: string` is REQUIRED, truthy tooltip re-routes the label through `IconTooltip`, the label carries `cursor: help`, and the emit is `update:checked`. Adopting M-2 as banked converts four ~40 px space-between rows into stacked full-width blocks — an unapproved redesign of the primary control surface. M-2 stays MINOR as a cure *option*; LC-3 is the MAJOR rider that any F.W3/W4 spec must consume with it: accept the anatomy explicitly, or take the narrower `id`/`for` local wiring (zero layout delta, same accname mechanism — `LabeledField`'s own slot contract proves the mechanism). Precedent: FM-12 / FR-AH-24 cure-completeness law. |
| R2-4 | **reader-DU's Computer-Modern premise** (rides the D-M2 increment and the M-ε cure caveat) | "style.css applies `--font-sans` (Computer Modern) to `html, body`, so `.cm-serif` actively DOWNGRADES the title from the inherited brand face" | — | **CORRECTED BY THIS SEAT — both dependent cells killed (K-22, K-23); no banked verdict moves.** | `html, body` applies **`font-serif`** (style.css :19, my read), not the remapped `--font-sans`. `--font-serif` resolves through glass-ui's bridge to `var(--font-stack-text)` = the Plus Jakarta text register (bridges.css :68 → scheme-motion.css :43) — and with the banked FR-AH-2 fact (fonts never imported) the rendered body face is the system fallback. Under **no** resolution is the body Computer Modern, so `.cm-serif` cannot "downgrade from" it. What survives: the banked FR-AH-2 fold unchanged (`.cm-serif` → undeclared `--font-serif-math` → generic `serif` → platform Times), and DU's sharpened SS-13 expectation survives with corrected faces — the title (Times) and plate (system/Plus-Jakarta register) still diverge within one screenshot. |
| R2-5 | **Precision notes, both directions** | r1-line cites drift ±2 (":93" for M-3 at :91; ":71" for the D-m8 deletion quote at :69) | "`preventDefault()` UNCONDITIONALLY" — actually guarded by `if (!event.defaultPrevented)` (DialogContentModal :58), effectively-always in this tree | **Recorded; zero verdicts affected.** | Both readers' substantive chains re-derived clean at the bytes. The r1-revision line drift is absorbed by the supersession note above (r1 anchors live in git history). |

## New roster rows admitted this round — id · severity · claim · proof · terminal disposition

| id | sev | claim (adjudicated) | proof (this seat's own unless marked) | terminal disposition |
|---|---|---|---|---|
| **LC-1** | **MAJOR** | **The tree's sole live axe keystone is silently narrowed by its own subject.** Opening ExportModal makes axe's `isModalOpen()` true (visible `[role=dialog]` — the keystone *asserts* it before `checkA11y`), which flips all three checks of `aria-hidden-focus` (`serious`, `wcag2a` — inside both keystone filters): `focusable-modal-open` returns `undefined` → the node lands in **`incomplete`**, and the other two return `true` — page-wide, because neither helper carries `.include()`/`.exclude()`. Both helpers read `results.violations` only. The suppressed condition is REAL and created by the subject: reka's `useHideOthers` calls `aria-hidden`'s `hideOthers` (attribute `'aria-hidden'` only; `inertOthers`/`suppressOthers` shipped, uncalled), stamping `aria-hidden="true"` over the app root and every focusable workspace control while the dialog is open — the textbook `aria-hidden-focus` condition. Precision bound: the other two `passForModal` consumers (`landmark-one-main`, `page-has-heading-one`) are `best-practice`-tagged, outside the filter — `aria-hidden-focus` is the **sole** in-scope suppressed rule. | Every link re-derived by this seat at the installed bytes: ux spec :26-40/:150-165, crud spec :84-90/:640-660; axe 4.11.4 `isModalOpen` + the three evaluates (verbatim above); reka `DialogContentModal.js:48`; `aria-hidden@1.2.6` :124-135. Registry-cleared: 6 files mention the rule, none books the suppression; fr-CoefficientsSpectrum :82/:117 books the opposite direction. | **→ F.W3/W4** (e2e repair rider on the C-D-23/C-D-29 keystone cluster): scope `checkA11y` with `.include()` per surface, and assert `results.incomplete` for the modal-suppressed rules; the corroboration run (how many nodes land incomplete) is SS-13 #13. |
| **LC-2** | **MAJOR** | **A suppressed keystone is justified by a compensating control that provably lacks the claimed property.** The `test.fixme` rationale (crud :616-630) fails on all three clauses at the installed pin: (a) "glass-ui omits `inert`" — dead, FOLD → banked fr-CoefficientsSpectrum K-13 (:117: ConfiguratorLayer carries `inert` WITH `aria-hidden`); (b) "consumes the PUBLISHED `^2.0.0`" — stale, FOLD → banked C-D-29 (package.json :14 = `^4.0.0`, my read); (c) NEW and load-bearing: "the `ExportModal` keystones below still enforce a11y on surfaces without the collapsed-region defect" is **structurally false** — the ExportModal keystone (crud :641-660) runs `establishSession` + `openWorkspace` BEFORE opening the dialog, so its unscoped `checkA11y` evaluates the very collapsed ConfiguratorLayers the fixme excludes, over a strict DOM superset of the suppressed test's page. The two adjacent tests assert contradictory things about the same DOM and coexist only via LC-1's suppression mechanism. | Spec regions read verbatim this session; fold targets re-read at their banked anchors. | **→ F.W3/W4**: re-run the fixme'd workspace-default keystone at the installed pin; banked K-13 predicts green → delete the fixme AND its rationale rather than porting either forward. Cheapest item in this component's queue (SS-13 #14). |
| **LC-3** | **MAJOR** | **The banked M-2 cure (`LabeledSwitch`) is not drop-in** — column-stacked full-width anatomy, label-first, REQUIRED `tooltip`, `cursor: help`, `v-model:checked`; adopting it as prescribed redesigns the option list (≈2× vertical extent, right-aligned switch rail deleted) with no axis or ruling having proposed that. The naming *mechanism* the bank credits is genuinely sound (`LabeledField` mints `controlId` → `<label for>` + `<Switch id>` re-enables reka's derivation); it is the **anatomy** the prescription does not disclose. | Ruled at R2-3; implementation + CSS read verbatim by this seat at the installed 4.0.0. | **→ F.W3/W4 rider ON M-2** (which stays MINOR): accept the anatomy change explicitly or take locally-authored `id`/`for` pairs keeping `.option-row`. Side-by-side anatomy delta → SS-13 #15. |
| **M-α** | MINOR | **r1's D-m7 cure is deleted at the producer and mis-sequenced by the bank**: `:show-close="false"` (r1 → F.W3/W4) does not exist at 8.0.0 — `showClose` is GONE from the whole source tree, folded into `dismiss` (`"deliberate"` = no ✕, Esc + outside kept — the D-m7 intent exactly) — while the prop's deletion lands at **F.W1**. Executed in the banked order the cure fails `vue-tsc` or falls through as a dead DOM attribute with the ✕ back on screen: a silent regression of the exact defect it closes. Producer fact FOLDS → banked FR-AFP-37 (fr-AdminFlaggedPanel :91); the ExportModal-specific increment is the bank's sequencing contradiction. Rider (verified at HEAD): `spring`/`scrimAnimation` are also gone from `DialogContentProps`, so challenge-D's U-7 "STABLE" row is wrong about the producer on three of four props. | `grep -rn showClose src/` → 0 at `558c3fa3`; `DialogContent.vue` :18-47 dismiss grammar + :147 `closeVisible`; installed 4.0.0 still ships `showClose {default:!0}` (my node slice). | **→ F.W3/W4, re-spelled `dismiss="deliberate"` and sequenced WITH/AFTER F.W1** — this row supersedes r1's D-m7 cure cell (D-m7's defect row itself is unchanged). |
| **M-β** | MINOR | **D-m9's mechanism dies at F.W1 and its banked cure inverts**: producer HEAD moves the DialogTitle recipe out of `cn()` (`DialogTitle.vue` renders `cn(props.class)` — no default utilities) into an UNLAYERED `:where([data-slot="dialog-title"])` rule (plain `@import` at index.css :254; tailwind compiles `text-lg` into `@layer utilities`; unlayered beats layered) that declares **no font-weight** — and preflight resets `h2` to `font-weight: inherit`. Consequences: (1) `text-lg` goes INERT at the uplift (a third free producer improvement for this file — the title lands on the fluid ramp); (2) `font-semibold` flips from redundant to the SOLE weight source — executing r1's "drop the raw utilities" cure after F.W1 ships a 400-weight title. Producer-owned rider for the standing glass-ui BH relay: every 8.0.0 consumer passing no weight inherits 400 where 4.0.0 gave 600. | All five links verified this session at `558c3fa3` + installed dists (title block :66-73, `font-weight` grep 0, preflight :73-81, unlayered import). At the 4.0.0 pin D-m9 stands exactly as banked (r1-verified: `grep 'dialog-title' dist/styles/` → 0 — the merge is the whole story TODAY). | **→ F.W1 ledger rider** (extends r1's M-3 uplift ledger, which books only the width clamp + header alignment) **+ glass-ui BH relay note** (owner edict — producer-owned regression surface). Post-uplift typography probe → SS-13 #16. |
| **M-γ** | MINOR | **`min-width: 300px` inverts from inert to actively harmful at F.W1**: today the base is `w-full max-w-lg` with no inset (my 4.0.0 read) so the rule binds only under 300 px viewports (r1's "inert" verdict right at the pin); at HEAD the plate becomes `inline-size: min(100% − 2·var(--space-section), 32rem)` with `--space-section` 1.25rem at ≤768 px — the docblock names hand-rolled consumer gutters as the motive, and ExportModal is one of them. Arithmetic: the 300 px floor binds below **340 px**; at 320 px it halves the producer's 20 px gutter guarantee; below 300 px it overflows. Deletion is a sequenced *prerequisite* of the free improvement, not a tidy-up. Conditional on UNPROVEN-6 (`.export-dialog` scope-attribute survival) — direction unchanged either way, priority raised. | Clamp + docblock + token values verified at `558c3fa3` (styles.css :14-27, sizing.css :123/:322); installed base string re-read. | **→ F.W3/W4** (inside the banked D-m8 deletion row, now sequenced before/with F.W1's uplift); raises UNPROVEN-6's stakes (SS-13 #6). |
| **M-δ** | MINOR | **Initial focus lands on a state-dependent toggle**: no `@open-auto-focus` handler (:46 binds `:open` + `@update:open` only), so reka's FocusScope runs `focusFirst(getTabbableCandidates(container))` in DOM order; the ✕ renders AFTER the slot (4.0.0 render, my read) so the first tabbable is the first `<Switch>` — and `v-if="hasEpicycles"` (:55) decides whether that is "Epicycles" or "Trace path". The open-then-Space habit mutates a *different option* depending on workspace state; the landing is under-signalled (D-m5's missing row focus style); compounds L-6 (the first thing announced is the control whose name is the open probe). Not in any axis, either reader's confirmations, or the r1 bank (which carries only D-m5 and UNPROVEN-8 after-close). | FocusScope :90-104 + utils :40-45 + DialogContentImpl :68 + the 4.0.0 slot-first render — all my reads this session. | **→ F.W3/W4** (one line: `@open-auto-focus.prevent` + explicit target, or the producer's dismissal/primary convention). Landing-element probe in BOTH workspace states → SS-13 #17. |
| **M-ε** | MINOR | **The plate's primary text is the only type in the dialog off the fluid ramp**: `.option-label { @apply text-base }` hard-codes 1 rem against `--type-body: clamp(1rem, 0.92rem+0.27vw, 1.375rem)` (≈18.6 px at 1440 — labels ~14 % under the system's body rung, to 27 % at the ceiling); at F.W1 the title becomes `calc(var(--type-body) × 1.272)` (the producer's authored √φ interval) while the labels stay fixed, so the interval holds at exactly one viewport and drifts everywhere else. The honest edit (`font-size: var(--type-body); line-height: var(--type-leading-body)`) also retires the sole `@apply` that L-17's `@reference` machinery serves, collapsing that INFO. Cure-rationale corrected per K-23: the reason not to take the full `text-body` utility is face-coupling while the tree's font story (FR-AH-2) is unresolved — not brand-face preservation. | Clamp + title calc + utility bodies read this session; ExportModal :110-113. | **→ F.W3/W4** (with the D-m2 pitch cure — the same rows). Ratio-at-three-widths probe → SS-13 #16. |
| *(fold)* | MINOR | **N-16 instance — the sticky hover latch**: `.option-row:hover` (:106-108) is the ONLY hover in the plate not capability-gated (every design-system `hover:` compiles to `@media (hover: hover)` — byte-verified at tailwind 4.3.1's static variant registration), so it alone fires and latches on touch. ExportModal-specific increment: the latched wash is the sole row-as-target affordance (D-m5) and reads as *selected* — the exact semantic the Switch owns; N-16's own instance (a scale transform) carries no state semantics. DU's tree-wide re-measure adopted: 49 raw `:hover` across 32 SFCs, 0 guards. | FOLD BY REFERENCE → `fr-DarkModeToggle.md:102` (N-16, tree-wide pattern candidate) — **not a new row**; increment banked inside the fold. | → F.W4 per the banked N-16 route; UA latch probe → SS-13 #18. |

## r1 roster — carried whole (44 rows, original ids, grades and routes unchanged)

Re-confirmations from this corpus are noted in brackets; rows this round touched carry a pointer to the new material. Full proof cells live in the r1 revision (git history) and the challenge files, incorporated by reference.

**Identity-folds (13)**: D-B1/L-1/C-D-1 → fr-BasisCanvas :45, MAJOR [re-confirmed by BOTH readers; LC's increment banked: `hasEpicycles` (VV :123) and BasisCanvas :489 are the SAME derivation over `:active-bases` (:199), so no second divergence hides behind the fold — the payload gap is the whole gap; DU's BLOCKER dissent again preserved] · L-5/C-D-2b → fr-BasisCanvas :46 · L-2/D-4/D-25/C-D-4 → fr-BasisCanvas :47 · L-4/C-D-3 → fr-BasisCanvas :99 + :79 at bank grade [both readers re-confirmed the reachability chain; LC's bytes re-walked by this seat: the guard wraps only the painting, the download tail :506-512 sits outside, `removeChild` present] · C/D-27 leg 1 → fr-BasisCanvas :79 · D-B2 → fr-AdminAuditLog :36 (AA-2 BLOCKER, census-extension; both footer Buttons :74-75 are instances) [DU's fifth stale-pin confirmation superseded by this seat's sixth at `558c3fa3`] · C-D-6 → fr-BasisCanvas :59 · D-M2 → fr-AppHeader :48 (FR-AH-2 five-way MISS-FONT identity; `.cm-serif` chain re-verified this session: undeclared `--font-serif-math` → generic serif; DU's downgrade increment killed, K-22) · L-8/D-i4/C-D-2 → fr-BasisCanvas R-3/K-7 · L-16 → fr-BasisCanvas :106, NO-WAVE-OWNER · dialog width/header fork (inside M-3) → MigratePalettesDialog :78-79 + FlagReportDialog :42 · D-m4/C-D-11 → fr-EasingCurvePreview R-1/K-GEOM (K-15 discharge stands; conformance half ExportModal-owned, `h-3.5 w-3.5` contains no `size-` substring — LC re-verified the cva byte) · C-D-29 → fr-BasisSelector :26/:41 [now also load-bearing inside LC-2].

**MAJOR (r1: 3 new-at-site)**: L-3/D-M5/C-D-24 — zero error posture on the export path, silent no-op ≡ success (`doExport` closes unconditionally after an optional chain; `useToast` used for publish only — VV :98-103/:111/:114 re-read this session) → F.W3/W4 [LC killed the corpus's non-null-assertion mechanism cell, K-19; the MAJOR is untouched] · D-M3 — the 16×16 `opacity-70` ✕ outside every coarse floor; free at F.W1 (producer 44×44 verified at `558c3fa3`) [WCAG headline now formally killed, K-16; coarse-hover-never-recovers sharpening banked] · L §X-A — devDeps census correction (cva 8 / clsx 1 / tailwind-merge 0) → F.W1.

**MINOR (r1: 24)**: D-M1-as-rescoped/L-10/C-D-8 (no `DialogDescription`; dangling `aria-describedby` — reka minting re-verified at DialogContentImpl :50/:78) · L-6/D-m6/C-D-21/C-D-28 (unnamed-Switch composite, SEAT-DEMOTED at r1 R-4; leg 1 now CONTESTED per R2-2 — probe load-bearing, trigger unchanged) · L-9/D-M4/C-D-5 (always-open `:open="true"` + parent `v-if`; exit choreography dead on 100 % of dismissals; `FullscreenViewer :visible` sibling inconsistency re-read at VV :282) · D-m1 (footer flush <640 px; producer cure verbatim at HEAD) · D-m2-as-corrected (halo overlap 0.125rem at every root — K-9; LC re-ran the axis's own nominated falsifier: `--touch-target: 2.75rem` is the sole declaration, offsets-sizing :461) · D-m3 (third left edge; the plate-inset CLAUSE killed this round, K-17 — the 28-px headline and the banked cure survive, softened to "token the 4 px or adopt the producer row anatomy") · D-m5 (`:hover`-only teaching; `grep focus` → 0 re-run this session) · D-m7/L-18 (four dismissal paths, ✕ DOM-last/visually-first; tab order …Cancel → Save PNG → ✕ re-derived by DU from the render fn, ratified by this seat's slot-first read; **cure superseded by M-α**) · D-m8/C-D-12 (min-width 300px inert-at-pin, tree's widest dialog by omission; **harm window widens at F.W1 per M-γ**) · D-m9 (deterministic `text-subheading` strip in the hand-rolled merger — stands at the pin; **mechanism dies at F.W1 per M-β**) · D-m10 (six strings, three registers; "Trace path" matches nothing) · C-D-10 (hover wash byte-identical to `--muted-medium`) · C-D-13 (`glass` default while four peers pass `opaque`, over the animated canvas; `surface` survives at HEAD — cure uplift-safe, re-verified) · C-D-14 (emphasis inverted: opaque Cancel, translucent primary, `solid` unused — cva bodies re-read by LC) · L-12/C-D-15 (restated cva defaults materialise `data-variant`/`data-size` — LC completed the derivation: attributes bind from RAW props, class string identical either way; dies inside AA-2 at F.W1) · L-11/C-D-9 (`aria-modal` asserted ×3, rendered by nobody — reka renders role/describedby/labelledby/data-state only) · L-14/C-D-19 (four `ref(true)` + remount-per-open discards preferences in a view that persists every sibling) · C-D-20 (DESIGN.md:31 stale Teleport row) · C-D-17/L-15 (lucide from devDependencies; two lucide runtimes in one dialog — manifest re-read :35) · C-D-18 (three dialog idioms, two emit forms) · C-D-26-as-worsened (axis glyphs ride `withGrid`; "y"-erase viewport-dependent — C §7.0 anchors re-verified EXACT at :498-500) · M-1 (Trace-path row missing its twin's `v-if` guard) · M-2 (LabeledSwitch as the naming cure — **now rides LC-3's anatomy rider**) · M-4 (`role="switch"` wrong for deferred-commit; `./checkbox` consumer edit after F.W1, K-14).

**INFO (r1: 4)**: C-D-22 (zero API coupling — R6-8 empty for this leaf) · C-D-23 (last keystone run died at affordance discovery; the SOLE live axe keystone — **now compounded by LC-1**) · L-17/D-i3 (`@reference` serving one `@apply` — **collapses with M-ε's cure**) · M-3 (uplift ledger: width clamp + header alignment + A-21 discharge recorded — **extended by M-β's third free improvement**).

## Killed-claims register — 23 entries (r1 K-1..K-15 sustained untouched; 8 new)

| # | killed claim | adjudication |
|---|---|---|
| K-16 | D-M3's "**WCAG 2.5.8 … AA, fails at every pointer type**" headline (challenge-D :243/:272; adopted into the r1 row prose) | **KILLED on two independent grounds (R2-1)** — DU's Equivalent exception (Cancel ≥40 px, same function, same page) and LC's Spacing exception (24 px circle intersects no other target), plus LC's scope facts: 2.5.8 is WCAG **2.2** (outside the keystones' tags by construction) and has no pointer-type qualifier. Geometry MAJOR, `opacity-70`, and the F.W1-free routing all survive; a repair spec inheriting the conformance claim would book a defect that does not exist. DU's irony rider recorded (Cancel is the rescue — don't delete it first). |
| K-17 | D-m3's clause "the plate … stops 4 px short of the content edge rather than bleeding to it" (challenge-D :456-458) | **KILLED (reader-DU; seat-ratified by CSS derivation)** — `.option-list` is a grid item (default `justify-items: stretch`) and `.option-row` a column-flex item (default cross-axis stretch): the row's border box spans the full 24 px content width, the `0.5rem 0.25rem` padding is INSIDE the painted plate, the hover wash bleeds to the edge, only the label TEXT is inset 4 px. The same axis needs the row full-width in D-m5 and D-m2 — corpus self-contradiction recorded. The 28-px third-edge headline survives; the cure softens from "delete the 4 px" to "token it or adopt the producer row anatomy". |
| K-18 | D-i1's failure scenario "adjacent hover plates read as a single mis-rendered block" (challenge-D :686-690) | **KILLED (reader-DU; seat-ratified)** — `.option-row` has no resting background, no `:focus-within`/`:active`/`[data-state]` styling; a single pointer hovers one row at a time, so two plates can never co-paint, and at rest the 2 px gap separates nothing visible. The 8:1 ratio and the pitch consequence survive inside D-m2-as-corrected — which is why r1 could drop D-i1 as a standalone row without loss. |
| K-19 | L-3's mechanism cell "an unguarded non-null assertion … **both can fail**" (challenge-L :135-138) | **KILLED (reader-LC; seat-ratified at the bytes)** — the `!` (:475) is erased at compile time and cannot fail; a null ctx would TypeError one line later at `setTransform` (:476). Anchors also drift (:477/:505 vs the corrigendum-exact :475/:506). L-3's substance (no try/catch, no outcome, no toast; download outside the guard) is untouched at its banked MAJOR. |
| K-20 | C-D-28's rule transcription (challenge-C :455-458) — "its `any` array contains no label-based check at all" | **KILLED as citation (reader-LC; seat-verified)** — the installed rule also carries `none: ['no-implicit-explicit-label']`, a precisely label-aware check family the axis silently dropped. Verdict unaffected (the rule still never matches a native `button` via `no-naming-method-matches`); the second corpus cell quoting a rule with an array omitted — F.W3/W4 specs must re-derive a11y arguments from `axe.js`, never from the challenge text. |
| K-21 | The residual "green keystones are structurally incapable of failing on this" framing (challenge-L :236-240 / challenge-C :448-467) | **KILL COMPLETED (reader-LC; matcher body re-derived by this seat)** — r1's K-13 half-kill asserted the match; this round derives it: `noExplicitNameRequired` → explicit role `switch` → `accessibleNameRequired: true` → the rule matches, evaluates at `critical`/`wcag2a`, and passes via a true-positive `'implicit-label'`; delete the `<label>` wrapper and the keystone reds. Only the `aria-toggle-field-name` skip survives as genuine foreclosure. (That axe's model may diverge from HTML-AAM is R2-2's separate, preserved contest.) |
| K-22 | reader-DU's D-M2 increment: "`.cm-serif` … actively DOWNGRADES [the title] from the inherited brand face" | **MINTED BY THIS SEAT (R2-4)** — the body rides `font-serif`, whose bridge resolves to the Plus Jakarta text register, never the `@theme --font-sans` remap; the body is not Computer Modern under any live resolution (and per banked FR-AH-2, likely the system fallback). The banked fold and the within-one-screenshot divergence expectation survive with corrected faces; "removing the class is strictly better" falls to UNPROVEN. |
| K-23 | reader-DU's M-ε cure caveat: "`text-body` … would move the labels off the brand face" | **MINTED BY THIS SEAT (R2-4)** — same false premise: `text-body` binds `--font-text` = the SAME register the body already resolves to. The narrow font-size/line-height edit remains right for a different reason (don't couple the label face to a utility while FR-AH-2 is open). |

**Ratifications recorded, not re-numbered**: DU independently re-derived K-9 (rem-invariance, running the falsifier), K-1 (census), K-4 (no clipping), K-15's delivery precondition (style.css :1-3 cascade), K-8 (theme-invariant grey poisoning), K-10 (AA-2 novelty), and re-fired the axis's own F-3 kill at the byte (`lucide` Icon.js `aria-hidden: "true"`). LC re-derived S-1/K-6 (third derivation of the forwarder key-set filter) and attacked R-4 leg 2 expecting to re-open L-6 — the attack failed and is banked as a negative result. LC's claimed `--touch-target` grep (sole declaration, offsets-sizing :461) closes the falsifier the corpus nominated and nobody had run.

## Superlatives — 14 (r1's 9 sustained; 5 admitted this round, provenance marked)

r1 S-1..S-9 carried whole (modal Boolean-cast survival · emit re-gates the prop (narrowed by M-1) · zero-leak by construction · uplift-immune subpath addressing · PRM bracket reaches the inline style · the `onOpenChange` prose (one false clause) · the discharged historical HIGH · the deterministic merger · C's §7.0 corrigendum as falsifiable self-audit). New:

10. **The forwarder discipline is correct TWICE** (reader-LC, adopted; mechanism identical to the r1-verified S-1): the Switch's twelve defaultless `{type: Boolean}` props are equally defused by `useForwardProps`'s defined-key filter, because :57/:61/:65/:69 bind nothing but `v-model` — the minimal binding surface is doing defensive work, not just minimising migration cost.
11. **`touch-hit-area` survives its own nominated falsifier with a divergence-rationale** (reader-LC; the `--touch-target: 2.75rem` sole-declaration grep run at last): the utility's docblock argues size-agnostic centred min-size against its own in-house symmetric-inset precedent, names its six consumers, and is rem-derived throughout — the best-argued utility in the substrate, and the same rem-derivation is what makes D-m2's overlap scale-invariant.
12. **`LabeledField`'s slot contract is a careful accname design** (reader-LC; implementation read by this seat): three minted ids exposed as `{errorId, controlId, labelledBy}`, four wrappers choosing four correct and DIFFERENT naming strategies — exactly what the four hand-rolled rows are missing, and why M-2 is right in mechanism even where LC-3 shows it wrong in anatomy.
13. **The export path's ctx swap restores BEFORE `toDataURL`** (reader-LC; :504/:506 re-read by this seat): the one operation with a documented throw surface cannot strand the live surface on a detached buffer — and the synthetic anchor is fully cleaned up (`removeChild` :512, data: URL so nothing to revoke). Two standard canvas-export defects hunted and refuted with the same rigor as the ones found.
14. **`font-semibold` is an accidental immunity** (reader-DU; chain re-verified by this seat, and it SURVIVES K-22): the one class D-m9 calls redundant is exactly what keeps this consumer's title at 600 after the 8.0.0 uplift, where the producer's title lane declares no weight and preflight resets `h2` to inherit. Redundant at the pin, load-bearing after it — the M-β relay rider in miniature.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (r1 #1-12 carried; renumbered additions)

r1 #1-12 stand as written (with #1's direction note amended per R2-2: the real-AT accname probe is now **load-bearing**, not corroborative — HTML-AAM's button computation is contested even though axe's floor is proven; probe the ON-OPEN control's name first per M-δ; #6's stakes raised per M-γ). New:

13. **`results.incomplete` census for the ExportModal keystone** (LC-1 corroboration): one run printing `incomplete` beside `violations` makes the gate-integrity defect measurable and becomes the repair's regression assertion. The suppression itself is proven statically.
14. **The fixme'd workspace-default keystone re-run at the installed 4.0.0 pin** (LC-2): banked K-13 predicts green → delete the fixme and rationale. Cheapest item in the queue.
15. **Side-by-side anatomy delta if M-2 is adopted as prescribed** (LC-3): the column-vs-row change is source-certain; the perceptual verdict on the redesigned option list is not.
16. **Post-uplift typography of the plate** (M-β + M-ε): `text-lg` inert against the unlayered title rule; computed title weight 600 *only because* `font-semibold` is present; title↔label ratio at three widths witnessing the √φ drift.
17. **`document.activeElement` immediately after open, BOTH workspace states** (M-δ): with and without `fourier-epicycles`, witnessing the landing target move. (r1 #8's after-close probe narrows per LC: FocusScope's body fallback is provably skipped — `onCloseAutoFocus` prevents then focuses `triggerElement`; only the trigger's attachment across the DropdownMenu interleaving is live-only.)
18. **Sticky-hover latch on a real touch UA** (N-16 fold): the wash persists after tap while every design-system hover in the same plate stays dark — mechanism proven statically, latch is UA behaviour.

## DISSENT — preserved, not ruled away

1. **Reader scorecard, both directions.** reader-DU: two increments died at this seat's font-resolution read (K-22, K-23 — the same false premise inherited into two cells; the reader read `style.css:13-20` but not the `font-serif` → bridge → token chain underneath it), and its r1-line cites drift ±2; beside that sat this round's densest original work — M-α's cure-sequencing catch against the bank, M-β's two-consequence uplift derivation, M-δ (a find three axes, two prior readers, and the r1 seat all missed on a 114-line file), and three clean minted kills. reader-LC: one precision slip ("UNCONDITIONALLY"), and its three missed finds — all MAJOR, all sustained — are the round's centre of gravity; its negative-result discipline (recording the failed R-4 attack) is the register's model. Both readers held the identity guard perfectly: zero re-books across 21 banked fold targets.
2. **The bank's own lesson (fleet evidence).** r1's identity guard was airtight while both of its *prescribed cures* under correction this round (D-m7's `:show-close="false"`, M-2's `LabeledSwitch`) were authored from `.d.ts` surfaces without reading the implementation or the producer's forward state — the ColorSpaceSelector addendum-2 law ("a proposedCure inherits no evidential weight from its defect's evidence") firing twice against one record, same seat, same day. Every F.W3/W4 carry out of this file now re-verifies its cure against BOTH pins before landing.
3. **Severity dissents carried forward.** All r1 dissents stand (the axes' BLOCKER on the dead-switch fold; DU-r1's MAJORs against r1's MINORs; the R-4 escalation trigger). This round adds: reader-LC's MAJOR grades on LC-1/LC-2/LC-3 were sustained by this seat, but a future seat that finds LC-1 over-graded should note the bound recorded here — the suppression manufactures a false green only for `aria-hidden-focus`; every other in-scope rule still fires.
4. **This seat's scope notes.** (a) The producer moved under this adjudication (`4a86570b` → `558c3fa3`); all load-bearing HEAD facts were re-verified at the new pin, and any F.W1 spec must re-pin again (FR-EQC-16's gate, now at its sixth confirmation). (b) The WCAG-exception kills (K-16) are normative readings of SC 2.5.8's published exceptions, not live measurements; if the F.W3/W4 spec targets WCAG 2.2 conformance, both exceptions should be re-argued against the final layout. (c) Adopted-not-re-read cells are marked (adopted); every verdict-deciding cell was re-derived this session.

## Closing verdict

**ADJUDICATED — r2 seals the record against a second, adversarial corpus with zero banked verdicts moved and two banked CURES corrected: 52 roster rows (0 BLOCKER · 11 MAJOR · 34 MINOR · 7 INFO; r1's 44 carried whole under original ids), 23 kills (8 new — the WCAG 2.5.8 headline dead on two independent normative grounds; two of reader-DU's own increments killed by this seat at the font-resolution bytes), 8 missed finds admitted after re-proof. The round's real product is gate integrity: LC-1/LC-2 establish that the tree's sole live axe keystone cannot see the one serious rule its own subject suppresses and that the neighbouring fixme's compensating-control rationale is structurally false — both MAJOR, both routed to F.W3/W4's e2e repair with their probes banked; M-α/M-β/M-γ establish that three banked cures or verdicts invert at the F.W1 uplift and are now sequenced against it; LC-3 makes the M-2 naming cure honest about its anatomy. Routings remain M-25 agglomeration input: F.W1 owns the substrate/uplift cluster (now with M-β's relay rider and M-γ's deletion prerequisite), F.W3/W4 owns export-truth, dialog anatomy, naming, focus, and the keystone repair, NO-WAVE-OWNER carries the census generalisations; nothing here stamps a wave.**

— end of apotheosis r2. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; the r1 record is superseded-by-carriage, not erased (git history holds its bytes; its every surviving row is present above under its original id); dispositions are ADJUDICATED, not VERIFIED.
