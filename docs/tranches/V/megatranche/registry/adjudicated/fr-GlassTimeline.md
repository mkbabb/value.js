claude-fable-5 (served model id)

# fr-GlassTimeline — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-25 · **Subject**: `fourier-analysis/web/src/components/visualization/GlassTimeline.vue` (127 lines, re-read whole this session) — the dock scrub bar, the tree's hardest shadow (census §3a HARD).
**Substrate**: fourier live WT (the subject dirty; my `git diff` = exactly 2 lines, both `glass-scrubber → standard`) · installed glass-ui **4.0.0** (my `package.json` read) / reka-ui 2.9.10 · producer at `/Users/mkbabb/Programming/glass-ui`, working tree **8.0.0** @ HEAD, all producer facts git-pinned this session.
**Corpus adjudicated (r2)**: two fresh reader returns over the full challenge corpus (`challenge-{D-design 541, L-library 304, C-consumption 864}.md`) **and the banked r1 record** — **reader-γ** (design-and-usage, slug `GlassTimeline`: 12 confirmations · 4 kills · 3 misses · 6 superlatives · 9 unproven) and **reader-δ** (library-and-code, slug `fr-GlassTimeline`: 14 confirmations · 12 kills · 3 misses · 9 superlatives · 9 unproven).
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-24 r1 apotheosis at this same path (readers α/β). The r1 roster — 32 rows, 18 kills, 12 superlatives — is **carried whole below under its original ids** (anti-rename); r1 rulings stand except where this corpus's evidence corrects them in **exactly two registry cells**: the D-8/D-9/D-10/L-m5 "five of five" count (→ FOUR, rK-20) and the header's `v7.0.0 = 51cfdfaf` cite (→ tag object `62ec299a` → commit `4ab12128`, RC-2). Where r1 content is restated it was re-verified at the bytes unless marked *(r1-carried)*.
**Method (M-23/L-18)**: every verdict-level disagreement between the readers, every kill of a load-bearing cell, and every reader-missed row was re-derived against the trees by this seat before adoption; one reader-missed MAJOR was **killed as a re-book of a banked registry row** (rK-24 — the identity guard doing its job against a reader, not for one). Zero browser probes (probe parsimony); product source READ-ONLY; **sole write = this file**. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing is agglomeration input (M-25), stamping nothing.
**This seat's own instruments (all static + git, read-only)**: whole reads of GlassTimeline.vue (127) and stores/animation.ts (146) — every cited anchor checked (:16-18 trio prose, :24-28, :35-41, :43-57, :49-51, :60-64 no `aria-hidden`/no `id`, :62, :67, :71-74, :80, :90-100, :102-105, :107-118, :112-114 kept tokens, :124-126; store :55/:63/:69, :114 bare ref, :116-126, :128-130, :132-135, :138-143, :43+:41-42 refcount rationale). Producer git: `git rev-parse v7.0.0` = tag `62ec299a` → commit `4ab12128` (and `51cfdfaf` = a docs(BK/coordination) commit — RC-2 decided); `v7.0.0:src/components/slider/types.ts:5` = `"standard" | "spectrum"`; `v7.0.0:Slider.vue:18-19` `withDefaults(… variant: "standard")`; **`v7.0.0:Slider.vue:69-78` read verbatim — "a Vue `@pointerdown` template binding arrives as `$attrs.onPointerdown` and is DROPPED across the Slot/forwardRef boundary … vue-tsc + units pass; only a real drag catches it (the canonical binding-verification class). A native listener on the resolved host is immune."** — and `:119-124` the `@touchstart` chain "dropped across the same reka forwarding boundary"; HEAD `package.json` 8.0.0, `Slider.vue:26` `variant: "scrubber"`, `types.ts:10` scrubber union. Installed 4.0.0: `--slider-scrub` grep → **0**; `--popover-offset` grep → **0 in the package AND 0 in web/src** (the rK-20 phantom-token decider); byte extractions `.timeline-caret[data-v-a206e2d2]` (bare `bottom:calc(100% + 6px)`; `transition: … var(--duration-fast) var(--ease-standard)`), `.caret-value[data-v-a206e2d2]` (**TEN declarations** — reader-γ's nine→ten correction verified — keeping SIX token refs incl. `--border`), `.glass-track[data-v-a206e2d2]` (`height:24px` AND **`cursor:pointer`** — the CU-1 producer half); CVA base string (zero `cursor-*`); `cursor` in the slider chunk → **0**; cursor rules touching any slider element in `glass-ui.css` → **0**; dock cursor census → exactly `.collapsed`/`.always-expanded`/`.dock-layer-group` (none live on an expanded plain dock); Slider emit surface = `"update:modelValue"` + `valueCommit` ONLY (d.ts read — `pointerdown` is undeclared fallthrough). Consumer: `style.css:40-50` (mobile root **1.125rem/1.75rem**, ≥768px → 1rem — both readers' both-roots arithmetic re-derived: required ~36px vs ~16px desktop, ~39.5px vs ~18px mobile; the px-literal 6 does not scale); `AnimationControls.vue:147` `.play-btn{cursor:pointer}`; ConvergenceTimeline.vue = **146 lines**, `:71 variant="standard"`, `:75-77` ARIA spray, `:136` dead token; `variant="standard"` blast radius re-run = **14 hits / 7 files**. Registry: fr-MorphPhaseConfig **MPC-21** read (the rK-24 decider — "deleting the prop at all 9 sites is a no-op today and correct at 8.0.0 … MINOR / repair-value AMENDMENT · F.W1", both baselines verified at its pass 2); registry-wide cursor sweep over `fr-*.md` → no row books a cursor claim on any slider/track/scrub surface (CU-1 novelty holds; fr-ConvergenceTimeline's `cursor-pointer` cell is a Button survivor, different element); fr-AnimationControls M-3/D-7 read (AX-1 distinct-face check). Adopted spot-held, not re-derived end-to-end: reka SliderRoot/SliderImpl internals (r1 + both readers concordant), reader-δ's headless four-level render (its ORDER result independently derived by this seat from Vue `cloneVNode`/`mergeProps` semantics: element-own handlers first, fallthrough after ⇒ `[rekaImpl, consumer, snapshot]`), the C axis's §0a git archaeology.

## Scoreboard

**35 rows ADJUDICATED-CONFIRMED (6 MAJOR · 19 MINOR · 10 INFO)** — r1's 32 carried whole + **3 admissions** from this corpus's misses (PD-1 MAJOR · CU-1 MINOR · AX-1 MINOR); 2 further misses folded as sharpenings (D-1 both-roots; DB-1 into D-11/L-m2); 1 reader-missed MAJOR **killed as a re-book** (rK-24). **24 killed-register entries**: r1's 18 all SUSTAINED (0 revived — both fresh readers independently ratified K-1/K-5/K-6/K-8/K-9/K-10/K-11/K-12/K-15/K-16 with their own instruments) + **6 new** (incl. one registry-cell correction against r1's own roster and one kill of a reader's MAJOR). **15 superlatives** (r1's 12 sustained · 3 new · one MORE inversion: C·S-1 joins L-S4/C·S-5). **One SS-13 probe DISCHARGED STATICALLY** (the WCAG 2.5.8 question — rK-19); three probes added. **0 BLOCKERs as booked.**

The r2 corpus's real finds: **PD-1** — the scrub session's sole opener rides an undeclared fallthrough channel the producer's own v7.0.0 docblock declares DEAD ("dropped across the Slot/forwardRef boundary … only a real drag catches it"), which is simultaneously the strongest silent-break candidate on the F.W1 uplift and the missing rationale for r1's DH-1 cure precedent — and the **accessibility inversion** (AX-1): the caret is a visual duplicate NOT hidden from AT while the value it duplicates is NOT exposed on the control — the exact inverse of the axis's "decorative by construction", flipping the cure from one attribute to two-that-must-land-together.

## Ruled disagreements and corrections (the incredulity seat's r2 work)

| # | Dispute | reader-γ | reader-δ | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R2-1 | **D-3's WCAG half** | KILLED statically — 2.5.8's Spacing exception is met by construction; discharges SS-13 probe #5 | left in unproven ("whether the exception rescues the 20px track") | **γ SUSTAINED — probe DISCHARGED** | The SC's exception places a 24px circle **centred on the undersized target's bounding box** — the target is the whole `w-full`/`flex:1 1 0` Slider root, so the circle sits at the track's midpoint, half a track-width from any neighbour; the 8px `gap-2` constrains edge distance, which the exception never uses. Robust under SW-1 starvation (30px track ⇒ centre ≥23px from the play button > 12px radius; the play button at 48×40 is not itself undersized). Statically decidable; no live probe needed. SURVIVORS unchanged inside D-3: the real 4px regression vs the retired 24px recipe, and 2.5.5 (AAA) unmet with the coarse-pointer halo `pointer-events:none`. |
| R2-2 | **reader-δ missed[0]** ("`variant="standard"` costs nothing to cure — nobody asked what the attribute is worth", MAJOR) | folded the deletion remedy to banked fr-MorphPhaseConfig MPC-21 in its own D-2 kill | filed as a MAJOR novel miss amending BR-1 | **γ WINS — killed as a RE-BOOK (rK-24)** | fr-MorphPhaseConfig **MPC-21** (read this session) already banks the identical row: "deleting the prop at all 9 sites is a no-op today and correct at 8.0.0; strictly better than every axis's rename — MINOR / repair-value AMENDMENT · F.W1", with both baselines verified at its pass 2 (4.0.0 `?? "standard"` + empty variant classes; v8 `:not([data-variant=spectrum])`). δ's "nobody asked" is false against the registry. What SURVIVES of δ's cell: the no-op observation on the WT's own edit (→ rK-23) and the BR-1 cross-ref to MPC-21 (booked in the INFO table). δ's re-pin caveat (scrubber = rename, not re-pointed recipe) is already carried by MPC-16/MPC-21 pass-2 verification. |
| R2-3 | **C-9/L-i4 grade** | MINOR (unargued) | MAJOR | **MAJOR SUSTAINED (r1 + δ); γ corrected** | The cost is realized, not hypothetical: the verbatim 146-line sibling fork (my `wc -l` + reads of :71/:75-77/:136) exists BECAUSE the component has no contract; the producer's dispatcher declares the exact four bindings the file synthesises. A paid architectural price does not grade MINOR; γ offered no demotion argument. |
| R2-4 | **D-4/C-15 confirmability** | confirmed statically (`8t − 4` px; padding-box vs content-box) | live-only (unproven list) | **static geometry STANDS (r1's MINOR unchanged)** | Percentage `left` resolves on `.timeline-row`'s padding box (`padding: 0 0.25rem; position:relative` — :84-85 re-read) while the Slider fills the content box as an in-flow flex child; `t·W − (4 + t·(W−8)) = 8t − 4`. Producer-identical (my extraction). Magnitude/perception stays SS-13. |
| R2-5 | **rK-23 scope** (δ: the WT's completed half-migration "changed nothing") | — | "the praised edit was a no-op" | **SUSTAINED-AS-NARROWED (adjudicator's correction against δ)** | RUNTIME/pixel no-op, TYPE-LEVEL necessary: at the 4.0.0 pin `variant="glass-scrubber"` is a TS2322 (union = `"standard" | "spectrum"`, my git-pinned read) and no rule keys on `data-variant=standard`, so the rename cured the compile while changing zero pixels — and left all four visual drifts (dead token, docblock paint claim, comment↔rule mismatch, retired 24px height) behind. C·S-1's inversion lands on the narrowed form. |
| R2-6 | **S-alpha precision** (γ: "the ONE token pair the sweep did not touch") | filed as new superlative | — | **ADOPTED-AS-CORRECTED** | The fork's `.caret-value` keeps **three** token refs, not one pair: `--popover-foreground` (:112), `--popover` (:113), **`--border` (:114)** — my byte extraction of the producer rule confirms all three survive from its six. The substance sharpens: the sweep spared exactly the scheme-paired declarations whose literalization would break the dark arm, and regressed only the geometry/motion legs (one of which, the shadow, went theme-blind — already booked in D-8/9/10). |
| R2-7 | **Merge order on the `@pointerdown` channel** (C axis: "`mergeProps` puts the `$attrs` handler first") | — | KILLED by headless render: `[rekaImpl, consumer, snapshot]` | **δ SUSTAINED — and independently derived** | Vue semantics decide without a browser: fallthrough attrs merge onto the rendered root via `cloneVNode(root, fallthroughAttrs)` ⇒ element-own handlers FIRST, fallthrough after. Consistent with r1's K-6 (reka's snapshot fires after the press has moved the model). Load-bearing consequence booked in L-B1's cell: on every press `handleSlideStart → seek()` executes BEFORE `startScrub()/stopRAF()` — a consumer cure on this channel can never pre-empt reka. C·S-3's survival intact (its refusal-to-accept-the-tidy-answer posture held against a producer docblock saying the opposite). |

**Adjudicator's corrections register (against this corpus's readers)**: (1) γ's C-9 MINOR grade — corrected (R2-3); (2) δ's missed[0] MAJOR — killed as a re-book of MPC-21 (R2-2/rK-24); (3) δ's "changed nothing" — narrowed (R2-5); (4) γ's S-alpha "one pair" — three survivors (R2-6); (5) γ's C-5 nine→**ten** producer declarations — verified and adopted; (6) reader-γ's registry catch against r1's own header (`v7.0.0` ≠ `51cfdfaf`) — verified by my git and booked RC-2. Per the ColorSpaceSelector precedent these are fleet evidence: both readers' instruments otherwise re-derived clean, and their disagreements sat exactly where a third instrument was needed.

## Adjudicated defect roster

Line anchors re-verified live this session; re-resolve before any cure (D-19). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5–W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring (none needed here). Banked sibling records fr-AnimationControls / fr-ConvergenceTimeline / fr-BasisCanvas / fr-MorphPhaseConfig own the fork-family and variant-remedy identities; folds below never re-book them.

### MAJOR (6)

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **D-1** *(r1-carried, AMENDED both-roots)* | The caret — the file's sole stated reason to exist (:10-11) — is clipped by the dock's unconditional `contain: paint`: ~36px required vs ~16px available at the 16px desktop root, **and ~39.5px vs ~18px at the shipped 1.125rem mobile root** (style.css:41-43, my read; `@apply text-base` under bare `@reference` scales the line box to 27px while the 6px offset, a px literal, does not) — a ~2.2× deficit at BOTH roots; no viewport rescues it (δ's hardening + γ's arithmetic, both re-derived). `overflow-y:visible` cannot lift paint containment (css-contain-2 §3.2); `overflow-clip-margin` grep 0; the producer's rail-extend.css testimony stands; F.W3 adoption does not cure (v7.0.0 ScrubberTimeline carries the identical caret). | GlassTimeline.vue:90-118; shell.css:96; style.css:40-50 (this seat) | **GLASS-RELAY** + **F.W3 rider** *(r1)*. Severed-pixel count at BOTH roots → SS-13 #2. |
| **SW-1** *(r1-carried)* | Dead consumer width rule ⇒ the shrink-wrapped dock may starve the scrub track to a sliver; the banked fr-AnimationControls D-1 cure is a CAP where this needs a FLOOR; qualifies superlative D-S1. Untouched by this corpus except ratification (both readers keep it probe #1). | r1 anchors | **F.W4** (one identity with banked D-1, amended cap→floor). SS-13 #1 — still the single highest-value probe. |
| **L-B1 / C-1** *(r1-carried + rK-27 order note)* | Scrub opens on unconditional `@pointerdown` (:49-51/:73), closes only on reka's `hasChanged`-gated `valueCommit` (:53-57/:74); no `pointercancel`/`lostpointercapture` in SliderImpl; deterministic freeze per banked K-4 (null-delta press; pointercancel); NOT permanent (`watch(speed)` :138-143 + next value-changing commit — re-verified whole this session). **r2 addition (R2-7)**: handler order on the span is `[rekaImpl, consumer, snapshot]` — reka seeks BEFORE the consumer's `startScrub` on every press; no consumer cure on this channel can pre-empt reka, which sharpens why the durable cure is PD-1's native-listener shape or an upstream session pair. | GlassTimeline.vue:43-57,:73-74; animation.ts:114-135 (re-read whole) | **F.W3** — FOLD: banked fr-AnimationControls L-1/L-6 + fr-ConvergenceTimeline L·D-1/L·D-2/K-4 *(r1)*. Freeze/cancel probes → SS-13 #6. |
| **L-M2 / C-2 / D-7** *(r1-carried)* | Keyboard seeks never open a session — reka routes arrow/Home/End through `updateValues({commit:true})`; the rAF's once-anchored `startTime` (:63, re-verified) rewrites `t` next frame; playing is the arrival state. Both readers ratified with their own SliderRoot reads. | r1 anchors + animation.ts:63-69 (this seat) | **F.W3** — FOLD: banked fr-AnimationControls L-3 + fr-ConvergenceTimeline L·D-5(a) *(r1)*. |
| **C-9 / L-m3 / L-i4** *(r1-carried; grade re-affirmed R2-3)* | No `modelValue`, no emits, a hard `useAnimationStore()` bind (:24-28) — and the tree paid with the verbatim **146-line** sibling fork (my `wc -l`; same latch/adapter/dead-token). K-16's rider stands: the corpus's prescribed `ScrubberTimeline` import is unexported at both layers; the reachable dispatcher route is dead at 8.0.0 (RB-1). | GlassTimeline.vue:24-28; ConvergenceTimeline.vue:36-56/:136 | **F.W3** — FOLD: banked fr-AnimationControls C-4/M-1/M-14/L-6 + census §3a + fr-ConvergenceTimeline L·D-7 *(r1)*. |
| **PD-1** *(NEW — reader-δ missed[1], verified verbatim at the producer bytes)* | **The scrub session's sole opener rides a channel the producer itself documents as dead.** `@pointerdown` (:73) is undeclared (emit surface = `"update:modelValue"` + `valueCommit` only, my d.ts read) and survives only by attribute fallthrough across four component boundaries — and `v7.0.0:Slider.vue:69-78` states in terms: *"a Vue `@pointerdown` template binding arrives as `$attrs.onPointerdown` and is DROPPED across the Slot/forwardRef boundary … vue-tsc + units pass; only a real drag catches it (the canonical binding-verification class). A native listener on the resolved host is immune"* — with `:119-124` saying the same of `@touchstart`. The producer ABANDONED the channel already at the 4.0.0 pin (`useDockHold` attaches native listeners on the resolved host). At the installed pin the binding DOES land (δ's headless render; order per R2-7), so the row is a **contract/route defect wearing a time bomb**: EITHER the binding drops at the uplift target's reka (⇒ the only scrub opener silently dies on F.W1, invisible to vue-tsc — the producer's own named failure class) OR the producer docblock is false at its own pin (⇒ a producer-doc defect owed the relay). One of the two is true; the F.W1 probe decides which. Also the missing RATIONALE for r1's DH-1: `useDockHold` is the cure precedent not merely for its teardown but for its **channel** — and the banked K-14/SR-1 cure route ("re-derive the session pairing on the Slider's own pointer events") prescribes three MORE template bindings on exactly this channel unless re-shaped. | v7.0.0:Slider.vue:69-78/:119-124 (git-pinned, this seat); Slider.vue.d.ts emit surface; GlassTimeline.vue:49-51/:73 | **F.W1** — binding-verification probe at the new pin (cheapest form: assert `anim.scrubbing === true` mid-drag) becomes a charter obligation, per the producer's own statement that no static gate can see this class. **F.W3** — cure-shape rider amending banked DH-1/K-14/SR-1: native-listeners-on-resolved-host or an upstream session-pair prop, never template bindings on the forwarding chain. **GLASS-RELAY** — the docblock-vs-render contradiction at reka 2.9.10 is producer information either way. |

### MINOR (19)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-3 / C-4 / L-m1** *(r1-carried; WCAG half now rK-19)* | `--slider-scrub-track-height` exists at NO version (my whole-package grep: 0); track renders at the md 20px, 4px under the retired 24px recipe (both byte-verified). The 2.5.8 question is DISCHARGED (rK-19): the exception is met statically; survivors = the 4px regression + 2.5.5 AAA with the halo `pointer-events:none`. | **F.W3** — FOLD: banked fr-AnimationControls C-5/K-16, fr-ConvergenceTimeline M-2/K-6 (23 decl/7 files), K-2 specificity, K-11 remedy *(r1)*. SS-13 probe #5 RETIRED. |
| **D-4 / C-15** *(r1-carried; R2-4)* | Caret mis-registered `8t − 4` px against the fill edge (±4.5 at the mobile root) — padding-box vs content-box; producer-identical; statically certain, magnitude live. | **GLASS-RELAY** + F.W3 note *(r1)*. Offsets → SS-13 #3. |
| **D-5** *(r1-carried; cure REWRITTEN per AX-1/rK-21)* | No `aria-valuetext` — AT hears the private 0..100 integer. The caret is no substitute because UNASSOCIATED (no `id`/`aria-describedby`), **not** because AT-invisible — the axis's "decorative by construction" mechanism is killed (rK-21). The cure is now the **two-attribute law** (see AX-1): `aria-valuetext` on the control AND `aria-hidden` on the caret, in one edit. | FOLD to the banked GLASS-RELAY three-item ask, now four-item (AX-1) *(r1 + r2)*. |
| **D-6** *(r1-carried)* | Caret reveals on `:hover` + `[data-held]` only — no `:focus-within` arm anywhere (γ's whole-scope scan: the sole producer `:focus-within` rule is a track ring, never caret opacity — adopted, spot-held); keyboard drives an invisible value. | **F.W3** + **GLASS-RELAY** *(r1)*. |
| **D-12 / L-M4 / C-7** *(r1-carried)* | `[data-held]` is the dock's refcounted hold, not this slider's drag state — wrong predicate, de-facto element-local at this mount (K-10); the shipped element-local `data-touch-active` unused. Both readers ratified K-10's unreachability census. | **F.W3**; negative probe → SS-13 #4 *(r1)*. |
| **D-8 / D-9 / D-10 / L-m5** *(r1-carried, **CORRECTED by rK-20**)* | The fork de-tokenised **FOUR of the producer's byte-identical rule's paints** — `--type-small`→`text-base` (:110), `--radius-sm`→`0.25rem` (:115), `--shadow-sm`→theme-blind rgba (:116), `--duration-fast var(--ease-standard)`→`0.2s ease` (:96). The r1 cell's fifth leg (the bare `6px`) is STRUCK: producer-identical at the bytes, and the axis's named alternative `--popover-offset` exists nowhere (rK-20). K-17's anchor fix stands. | **F.W3** (block deletes with the fork); transition leg folds to banked fr-AnimationControls M-7 *(r1)*. |
| **D-11 / L-m2** *(r1-carried, AMENDED by DB-1 — reader-γ missed #3, verified at :16-18)* | Docblock stale/wrong ×4 *(r1)* — **plus the r2 amendment: :16-18 states the freeze bug as the file's CONTRACT** ("a `valueCommit` trio … pointerdown opens scrub, drag emits seek, pointerup closes scrub" — three ways wrong: only one of the three is `valueCommit`; reka's `valueCommit` is `hasChanged`-gated, not a pointerup event; nothing in the file binds `pointerup` at all — all re-verified at my whole read). Same identity family as banked L-m6 (the false-symmetry name); its value is SEQUENCING: F.W3 must rewrite :16-18 in the same cut as the pairing fix or the next reader re-derives the bug from the comment. | **F.W3** *(r1 + r2 law)*. |
| **L-M3 / C-3 / D-13** *(r1-carried, R-1)* | Two clocks, one marker: caret x raw `anim.t` (:62), caret text `easedT`-derived; max divergence 10.5pp; mode-dependent. δ's severity dissent stays preserved (r1 DISSENT-2). | **F.W3/W4** — NEW FACE of the banked caret-clock family *(r1)*. |
| **L-M1 / C-13** *(r1-carried)* | Fresh array per rAF write; five-deep subtree re-renders at frame rate (and :62 reads `anim.t` directly, so the parent effect re-runs regardless — δ's sharpening, verified at my read). | **F.W3** — FOLD: banked M-11/M-12 + fr-ConvergenceTimeline M-5 *(r1)*. |
| **L-B2** *(r1-carried)* | Dual mount, zero lifecycle, one bare store boolean + two private mirrors — beside the store's own refcount exemplar (:41-44/:99-106, re-read). | **F.W3** *(r1, K-7 reachability)*. |
| **L-m6** *(r1-carried)* | `onValueCommitStart` names an emit that does not exist; dead one-line delegation; fork-local regression vs the sibling. | **F.W3** *(r1)*. |
| **DT-1** *(r1-carried)* | `class="timeline-slider"` + :124-126 = dead triple; the conduit has never carried anything; inverts L-S4/C·S-5. | **F.W3** — 3-line delete *(r1)*. |
| **DH-1** *(r1-carried; rationale now supplied by PD-1)* | The unconditional teardown the scrub session lacks already executes on the same element (`useDockHold`: window pointerup+pointercancel+unmount); the affordance and the state actively disagree on the freeze path. **r2**: its cure-precedent status is because of its CHANNEL (native, resolved host), per PD-1. | **F.W3** *(r1 + r2)*. |
| **LF-1** *(r1-carried)* | Inline `left` % written every frame vs the producer's own "NEVER `style.left`" rule. | **GLASS-RELAY** + **F.W3** *(r1)*. |
| **TS-1** *(r1-carried)* | Fullscreen-instance sequencing rider (latched scrub × never-precomputed canvas ⇒ ≤601 `evaluateFourier`/frame); banked BC-20/M-β3 + L-1 are one edit. | **F.W3/W4** *(r1)*. |
| **C-6** *(r1-carried, EXTENDED by CU-1)* | The docblock credits paints `standard` does not ship; the migration deleted the hover-handle affordance — **and (CU-1) a second one, the cursor**. At rest during playback the only playhead indicator is the fill edge. | **F.W3** — FOLD *(r1)*; regression read → SS-13 #9. |
| **C-10 / D-17** *(r1-carried)* | `aria-label` lands twice; role-less host span carries a prohibited name. | **F.W4** + **GLASS-RELAY** *(r1)*. |
| **CU-1** *(NEW — reader-γ missed #1, verified at the bytes)* | **The migration deleted the pointer-cursor affordance and nothing restored it at any level**: the retired recipe declared `cursor:pointer` on `.glass-track` (my byte extraction); the replacement declares none — zero `cursor-*` in the CVA base, zero `cursor` in the slider chunk, zero slider-touching cursor rules in `glass-ui.css`, and the dock's three cursor declarations live only on `.collapsed`/`.always-expanded`/`.dock-layer-group`, none of which applies to this expanded plain dock (all my greps). The subject styles no cursor either (:81-88). So the dock's primary drag surface renders the default arrow while the play button eight lines from the mount declares `cursor:pointer` (AnimationControls.vue:147, my read) and the tree's own drag exemplar declares grab/grabbing. Registry-swept clean (my sweep: no fr-* books a cursor row on a scrub surface); the challenge corpus never uses the word. | **GLASS-RELAY** (producer-owned deletion — rides the same letter as D-1/D-4/C-6's affordance family) + **F.W3 rider** (the re-derivation must not land a cursor-less drag surface). One-glance confirm → SS-13 #8. |
| **AX-1** *(NEW — reader-γ missed #2, verified at my whole read; kills the D-5-clause-3 mechanism, rK-21)* | **The accessibility posture is exactly inverted**: the caret (:60-64) carries NONE of the four AT-removal mechanisms (`display:none`/`visibility:hidden`/`aria-hidden`/`inert` — `opacity:0`, `pointer-events:none`, `user-select:none` are all purely presentational), so its live text is exposed to AT as an **unassociated text node mutating ~5×/s** at the default duration, adjacent to but disconnected from the slider — in the expanded state, i.e. precisely when the control is operable (collapsed, the layer is `inert` — banked fact, honest scoping). Meanwhile the value it duplicates is NOT exposed on the control (no `aria-valuetext` — D-5). **The two-attribute law**: `aria-hidden="true"` on `.timeline-caret` AND `aria-valuetext` on the control must land in ONE edit — either alone leaves the readout absent from the a11y tree or duplicated as noise. Distinct face from banked fr-AnimationControls M-3 (visual exposure at rest) and D-7 (the collapsed mini-progress divs — different element); registry-swept (γ's sweep + my spot-checks). | **F.W3** + **GLASS-RELAY amendment** — the banked three-item ask becomes four items with the same-edit law. Engine caveat → SS-13 #7 (AX-tree dump; `opacity:0` exposure varies per engine, but the DOM-level absence of all four mechanisms is static and proven). |

### INFO (10)

| id | claim | disposition |
|---|---|---|
| **C-8 / C-11 / C-12 / C-14 / L-i1 / D-16** *(r1-carried)* | Small-surface cluster (quantization premise, restated defaults, triple clamp, `label` contract, emit hole). | FOLD: banked fr-AnimationControls C-6 + fr-ConvergenceTimeline C·C-7/L·D-12 — **F.W3** *(r1)*. |
| **C-16** *(r1-carried, R-3)* | Unscoped `.timeline-row` ancestor compound; three producer variants emit the class. | **F.W3** — delete the block *(r1)*. |
| **Z-1** *(r1-carried)* | `--z-popover` structurally inert inside the dock's double stacking context; grounds K-15. | **F.W3** *(r1)*. |
| **RC-1** *(r1-carried)* | No `event.button` filter — right/middle press seeks + parks; glitch door, not freeze door. | **F.W3**; divergence → SS-13 *(r1)*. |
| **EO-1** *(r1-carried)* | Caret unclamped at track ends; overhangs neighbours. | **GLASS-RELAY** rider on D-4 *(r1)*. |
| **C-18 / C-20** *(r1-carried)* | Zero direct value.js/keyframes consumption; census pin reading corrected at r1. | FOLD: banked C-1/R-14 + census §1 + G33 — **F.W1** *(r1)*. |
| **C-19** *(r1-carried; K-12 cell)* | No automated assertion of any tier (my r1-concordant grep: 0). | FOLD: census vitest-absent row — **F.W4** *(r1)*. |
| **D-14** *(r1-carried)* | `@reference "tailwindcss"` cannot see the project `@theme`; benign at this pin only because `--text-base` coincides — but it is what makes D-1's line-box arithmetic resolve to the stock 1.5 pair (r2 note: the both-roots hardening rides on this). | FOLD by reference: the banked dead-`@reference` cohort *(r1)*. |
| **RB-1** *(r1-carried)* | Banked K-14/SR-1 route authorities mis-baselined (v8 facts at a 7.0.0 label); route conclusion survives. | **F.W1 charter note** *(r1)*; both r2 readers re-ratified with their own git reads. |
| **BR-1** *(r1-carried, CROSS-REF added)* | `variant="standard"` = 14 raw hits = 9 attribute callsites / 7 files + 5 prose (re-run this session: 14/7 ✓). **r2**: the REMEDY is banked at fr-MorphPhaseConfig **MPC-21** (delete, don't rename — no-op at 4.0.0, correct at 8.0.0, pre-clears the TS2322 surface); this row carries only the measurement. | **F.W1** — budget cell + MPC-21 pointer *(r1 + r2)*. |

## Killed-claims register — 24 entries

**K-1 .. K-18** *(r1, ALL SUSTAINED, none revived)*: D-2's BLOCKER baseline-false at v7.0.0 (K-1 — both r2 readers re-killed with their own `git show`, δ adding the tag-object correction RC-2); the three other mis-baselined D-axis uplift cells (K-2/K-3/K-4); L-B1 permanence + BLOCKER (K-5); the drag-back scenario (K-6); L-B2's reachability (K-7); the trail-cost inversion (K-8 — δ re-derived whole); the touch-gate mechanism (K-9); the named spurious-reveal instances (K-10 — δ re-censused keepOpen); C-4's remedy absolutism (K-11 — δ re-proved the inline-style channel with a render); the e2e grep cell (K-12 — δ re-ran: 0); the 6px-vs-42px ledger cell (K-13); C-16-at-MINOR (K-14); C·S-4/D-15's escape-hatch rationale (K-15 — the caret cannot escape what clips it); the `ScrubberTimeline` import (K-16 — δ re-verified at the export map); L-m5's anchor (K-17); the banked K-14/SR-1 baseline (K-18 = RB-1).

| # | killed claim | refutation (r2, this seat's adjudication) |
|---|---|---|
| **rK-19** | **D-3's WCAG half**: "20px < 24px ⇒ fails 2.5.8; the spacing exception is not obviously available given the 8px gap-2 clearance … had the token been spelled correctly the control would have passed" | KILLED STATICALLY (R2-1, reader-γ sustained). The exception's 24px circle is centred on the undersized target's **bounding box** — the whole `w-full` Slider root — so it sits half a track-width from any neighbour; edge clearance is not the exception's metric; robust even under SW-1 starvation. 2.5.8 is MET; **SS-13 probe #5 discharged** — the first probe this record retires without a browser. Survivors stay booked in D-3 (4px regression; 2.5.5 AAA). |
| **rK-20** | **D-10's `6px` leg** ("token-bypassing literal; `--popover-offset` bypassed") — **and r1's own roster cell** D-8/D-9/D-10/L-m5 ("de-tokenised five of five … plus the bare `6px` offset") | Both readers convergent; decided at MY bytes: the producer's own `.timeline-caret[data-v-a206e2d2]` carries `bottom:calc(100% + 6px)` as a bare literal — a de-tokenisation charge cannot rest on a declaration the producer never tokenised — and `--popover-offset` has **0 occurrences in the installed package AND 0 in web/src** (it is a value.js-demo token; prescribing it injects a phantom into an F.W3 cure). **REGISTRY CORRECTION applied in place**: the roster row now reads FOUR legs, all four re-verified. |
| **rK-21** | **D-5 clause 3's mechanism**: "the caret is `pointer-events:none`, `opacity:0` … DECORATIVE TO AT BY CONSTRUCTION" | KILLED (reader-γ, verified at my whole read): none of the three properties removes an element from the a11y tree; :60-64 carries none of the four mechanisms that do. The conclusion inverts into AX-1 — the caret IS exposed (as unassociated mutating noise) while the value is NOT — and the cure flips from one attribute to the two-attribute same-edit law. Engine-variance caveat honestly carried (SS-13 #7). D-5's core (no `aria-valuetext`) stands untouched. |
| **rK-22** | **C axis's merge-order mechanism**: "`mergeProps` puts the `$attrs` handler first" (defending the landing of the consumer binding) | KILLED (reader-δ's render, independently derived by this seat from `cloneVNode` semantics — R2-7): the order is `[rekaImpl, consumer, snapshot]`. The defended CONCLUSION (the binding lands at this pin) SURVIVES; the corrected mechanism is load-bearing: reka's seek precedes `startScrub` on every press, so no consumer-channel cure can pre-empt reka — booked into L-B1 and PD-1's cure shape. Also independently re-derives r1 K-6's snapshot-third order. |
| **rK-23** | **C·S-1 (superlative)**: the in-flight `glass-scrubber → standard` rename shows "the migration author read the variant docs" | INVERTED-AS-NARROWED (R2-5): the WT's completed half-migration is a **pixel no-op** (no rule keys on `data-variant=standard`; both CVA arms empty) though type-level necessary at the pin — and the four real drifts sat unfixed in the same edit window. The praise pointed at the one edit that changed nothing visible. C·S-1 joins L-S4/C·S-5 in the inverted column — the corpus's second superlative pair killed by rows nobody filed. |
| **rK-24** | **reader-δ missed[0] (MAJOR)**: "`variant="standard"` costs nothing to cure … nobody asked what the attribute is worth" | KILLED AS A RE-BOOK (R2-2): banked fr-MorphPhaseConfig **MPC-21** already holds the identical row — delete-don't-rename, no-op at both baselines, MINOR, F.W1 — verified at both ends at its own pass 2. The registry asked and answered. Survivors distributed: the WT-no-op observation → rK-23; the measurement cross-ref → BR-1. δ's MAJOR framing preserved in DISSENT-5. |

**RC-2 (registry-cell correction, non-kill)**: r1's header cell "`v7.0.0` tag = `51cfdfaf`" is wrong — `v7.0.0` is tag object `62ec299a` dereferencing to commit `4ab12128`; `51cfdfaf` is a `docs(BK/coordination)` commit (reader-γ's catch, my git this session). r1's producer facts are unaffected (its reads used `git show v7.0.0:` which dereferences correctly); the cite alone is corrected.

## Superlatives — with provenance (L-18 runs both ways)

r1's twelve are SUSTAINED as written (D-1's rail-extend corroboration; C's §0a archaeology; C-5's byte proof — now with γ's nine→ten declaration correction; L-S3/C·S-6 `pointer-events:none` load-bearing (independently re-derived by both r2 readers: without it the caret would swallow the press at the exact x where a press cannot commit); L-S2 round-trip stability; C·S-2 typed-dock migration; C·S-3 (STRENGTHENED — δ re-attacked it with a running render under a harder instrument and its refusal-to-accept-the-tidy-answer held against a producer docblock asserting the opposite); D-S2; D-S1-qualified-by-SW-1; the L-S4/C·S-5 inversion; the `visibleCanvases` exemplar (re-read whole this session, :41-44 rationale verbatim); corpus fold discipline). New this round:

13. **The producer's v7.0.0 Slider.vue docblock is the most useful cure-shaping artifact in either tree, and no prior seat had opened it** (reader-δ) — it names its own failure class in the abstract ("vue-tsc + units pass; only a real drag catches it — the canonical binding-verification class"), records the device-proven fix, and states the structural mechanism. A producer writing down the class of bug that silently defeats its own gates is rarer than the bug. Verified verbatim at `v7.0.0:Slider.vue:69-78` by this seat; it is PD-1's spine and DH-1's missing rationale.
14. **S-alpha (corrected, R2-6)** — the de-tokenisation sweep spared exactly the THREE scheme-paired declarations whose literalization would have broken the dark arm (`--popover-foreground`/`--popover`/`--border`, :112-114) and regressed only geometry/motion legs. Token targets verified scheme-paired by γ at the producer's color-radius/light-dark/dark-arm files (adopted, spot-held).
15. **S-beta** (reader-γ) — ARIA restraint with a live counter-example one file away: the subject passes `aria-label` alone and lets reka mint the value trio on the real `role="slider"` element, while the sibling fork sprays `:aria-valuenow/min/max` onto the role-less host span (ConvergenceTimeline.vue:75-77, my read) — two conflicting value sets in one widget. Two forks of one primitive; only this one got it right.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (r2 list of record)

1. **SW-1** (highest-value): `.glass-dock` + `.timeline-row` rendered widths, both mounts, expanded, 1440px + 375px — decides starvation, banked D-1 cap→floor, D-S1's fate.
2. **D-1**: severed-pixel count of the caret on hover, both mounts, **at BOTH roots** (≥768px and <768px — the deficit differs, r2 amendment); check partial survival before absolutist phrasing.
3. **D-4/C-15 + EO-1**: caret-vs-fill offset at t=0/0.5/1 (−4/0/+4 px; ±4.5 mobile) + endpoint overhang.
4. **D-12 NEGATIVE**: focus ⋮, open Speed select, hover Tooltip — caret stays hidden.
5. ~~WCAG 2.5.8 spacing exception~~ — **DISCHARGED STATICALLY (rK-19)**; retained as a struck row so no later seat re-opens it.
6. **L-B1**: K-4-narrowed freeze repro (pre-press-value release; pointercancel) + pause-glyph/rainbow over frozen `t`; iOS pointercancel reachability under `touch-none`.
7. **AX-1** *(new)*: AX-tree dump of the expanded dock — the caret text as an unassociated mutating static-text node; absent while collapsed (`inert`); per-engine `opacity:0` exposure noted.
8. **CU-1** *(new)*: one glance — default arrow over the expanded scrub track; no UA/`touch-none` interaction supplies an affordance.
9. **C-6 + CU-1 residue**: whether no-thumb + no-caret-at-rest + no cursor reads as one discoverability regression against the retired recipe — one screenshot, not three.
10. **PD-1** *(new — F.W1, not SS-13's cosmetic lane)*: post-uplift, a real drag must be observed to fire `anim.startScrub()` (`anim.scrubbing === true` mid-drag) — the producer states no static gate can see this class.
11. **L-M1/M-11/M-12** profiler magnitudes; **RC-1** right-click divergence; **L-M2** End-key chord visibility *(r1-carried)*.
12. **Incidental-reveal noise** (γ): viewport-bottom crossing expands the dock (2000ms delay) while `.timeline-row:hover` flashes the caret with no authored reveal delay — motion-quality, live-only.

## Identity guards (folds by reference — never re-booked)

All eight r1 guards carried verbatim (scrub-pairing family → banked fr-AnimationControls L-1/L-3/L-6/M-1/K-4/K-5 + fr-ConvergenceTimeline L·D-1/L·D-2/L·D-5(a)/K-4; dead-token family; fork/no-contract family as re-baselined by RB-1; caret-clock family; render/trail cost; `@reference` cohort + vitest-absent + census pins; padding-box class first fourier instance; the consolidated GLASS-RELAY letter). r2 additions:
- **The variant-remedy identity is fr-MorphPhaseConfig MPC-21** — one identity; BR-1 carries only the measurement here; no seat re-books the delete-don't-rename cure (rK-24 is the enforcement event).
- The GLASS-RELAY letter gains: CU-1 (the cursor deletion, riding the C-6 affordance family), the AX-1 four-item/same-edit amendment, and PD-1's docblock-vs-render contradiction.
- AX-1 vs banked fr-AnimationControls M-3/D-7: three faces, one element family — visual exposure (M-3, banked), collapsed divs (D-7, banked), AT-tree structure (AX-1, booked HERE once).

## DISSENT — preserved, not ruled away

1-4. *(r1-carried)*: the mis-baselined instrument pattern (now with both r2 readers re-deriving it independently — the fleet lesson compounds); reader-β's L-M3 MAJOR; reader-α's D-1 aggravation conditional on SW-1; r1's scope notes.
5. **reader-δ's missed[0] MAJOR framing preserved**: even folded to MPC-21, the F.W1-budget consequence is real — the feared 9-site break-surface is a 9-site deletion, and the banked D·D-2/K-8 budget rows should carry MPC-21's pointer (they now do, via BR-1). If F.W1's charter prices the variant row as a rename, this dissent revives it.
6. **The PD-1 disjunction is deliberately unresolved**: the producer docblock says the channel drops; δ's render at reka 2.9.10 says it lands. This seat declines to rule which is true at the F.W1 target — the row is booked as the disjunction, and probe #10 is its decider. Ruling it statically in either direction would repeat the exact mis-baselining pattern this registry has now killed five rows for.
7. **reader-γ's C-9 MINOR** is recorded as unargued grade drift, not a reasoned dissent — corrected without preserved standing (R2-3).

## Closing verdict

**ADJUDICATED r2 — DEFECTIVE, zero BLOCKERs as booked: 35 rows confirmed (6 MAJOR · 19 MINOR · 10 INFO) — r1's roster carried whole under original ids + 3 admissions (PD-1 · CU-1 · AX-1) + 2 folded sharpenings · 24-kill register (r1's 18 sustained with none revived; 6 new, incl. one registry correction against r1's own roster cell and one kill of a reader's MAJOR as a re-book of banked MPC-21) · 2 registry-cell corrections (rK-20 four-legs; RC-2 tag cite) · one SS-13 probe discharged statically (rK-19) · routings unchanged in shape: the fork family and its riders to F.W3 (this file and its 146-line twin die in one unconditional-session re-derivation, now with the PD-1 cure-shape law: native listeners on the resolved host, never template bindings on the forwarding chain), SW-1 + residue to F.W4, PD-1's binding-verification probe to F.W1's charter, one consolidated GLASS-RELAY letter (containment · caret geometry · focus-reveal · `style.left` · the cursor deletion · the four-item a11y ask with the same-edit law · the docblock contradiction) · 0 NO-WAVE-OWNER. The r2 delta in one sentence: the component's sole scrub opener rides a channel its producer documents as dead and its only uplift gate cannot test, and its accessibility story is not absence but inversion — the caret shouts to AT what the slider refuses to say.**

— end of apotheosis r2. The three challenge files and both r2 reader returns remain immutable beside this record per E-1/E-3; the r1 record is superseded-by-carriage, not erased — its every surviving row is present above under its original id; dispositions are ADJUDICATED, not VERIFIED.
