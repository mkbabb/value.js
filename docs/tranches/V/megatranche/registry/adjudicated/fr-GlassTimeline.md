claude-fable-5 (served model id)

# fr-GlassTimeline — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/visualization/GlassTimeline.vue` (127 lines) — the dock scrub bar, the tree's hardest shadow (census §3a HARD).
**Substrate**: fourier `m/w1-bump-migration @ cd26c65` + the frozen dirty WT (the subject is one of the dirty paths — the C axis's §0a provenance) · installed glass-ui 4.0.0 / keyframes.js 4.3.0 / value.js 0.13.0 / reka-ui 2.9.10 · producer glass-ui read at `/Users/mkbabb/Programming/glass-ui` (working tree **8.0.0** @ HEAD; `v7.0.0` tag = `51cfdfaf`, both git-read by this seat).
**Corpus**: three axis challenges (`audit/fourier-components/GlassTimeline/challenge-{D-design 17+4, L-library 16+5, C-consumption 21+6}.md`, all read whole by this seat) × two hostile re-reader returns (**reader-α**, **reader-β**), received in-brief.
**Method (M-23/L-18)**: every verdict-level disagreement between the readers was re-derived against the trees before ruling; every load-bearing kill was re-attacked; the two readers' one genuine conflict (the D-12 spurious-reveal reachability) was settled by this seat's own census. Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; product source untouched)**: full read of GlassTimeline.vue (127). Producer git: `v7.0.0:src/components/slider/types.ts` (line 5 = `"standard" | "spectrum"`; `marks` already present), `51cfdfaf:package.json` = 7.0.0, `ls-tree v7.0.0 slider/` = 3 files (**no styles.css**), `v7.0.0:timeline/index.ts` = `export { GlassTimeline }`, `v8.0.0:timeline/index.ts` = `export { Timeline }`, `ls-tree v7.0.0 timeline/` = the full 10-file family, `git log -S valueText` → `9b4c15d8` (the 8.0.0 slider lane), HEAD types.ts:10 = the scrubber union at 8.0.0; `v7.0.0:ScrubberTimeline.vue` — `:aria-valuetext="label || undefined"` on the `role="slider"` track (:212-215), `@pointerup` **and** `@pointercancel` → `onTrackUp`, the docblock's "written to `transform: translateX()` (NEVER `style.left` — Safari composites transform, not left)" and "`opacity:0`-until-hover is RETIRED" (bead), the caret `v-if="label"` with the same `bottom: calc(100% + 6px)` / `left: modelValue*100+'%'` / two hover arms. Installed 4.0.0: shell.css:96 `contain: paint` unconditional + the BA.W-DOCK-GEOMETRY "UNEDITED" comment (:83-96), :114-115 `white-space: nowrap` + `max-inline-size`, :130 `backdrop-filter`, :161 `overflow-y: visible`; rail-extend.css:1-16 (the R4-1 "could NEVER paint past that box" testimony, verbatim); overflow.css:96-99 ("the dock ALREADY shrink-wraps to its content's intrinsic width"); layers.css:196-224 (`.dock-layer` `min-height: 2.5rem`, the max-content morph comment) + :315-323/:360-368 (`width:100%` rules FILL the dock's width, none creates one); density.css comfortable `0.375rem`; `grep overflow-clip-margin` → **0**; `grep slider-scrub` → **0**; **keepOpen census**: 3 chunks (`slider-DQ95MET2.js` ×1 = useDockHold; `dock.js` ×5 occurrences of which exactly ONE is a call — `r?.keepOpen()` inside the component rendering `class:["dock-layer-group",…]`; `HoverPopover-Dpzwvc4t.js` ×1); `dropdown-menu-BJ7E9js_.js` keepOpen → **0**; useDockHold extracted whole (window `pointerup`+`pointercancel` add/remove + onBeforeUnmount release); CVA base `"glass-slider focus-ring relative flex w-full touch-none select-none …"`; timeline.js tail `export { oe as GlassTimeline }` only + `index.d.ts` GlassTimeline-only + the dispatcher forwarding `onScrubStart`/`onScrubEnd` + the `e.label ?` caret guard + `.timeline-row` emitted by **three** variants (bare, `timeline-continuous`, `timeline-segmented`); `.glass-slider[data-size=md]` 1.25rem; `.slider-thumb` width:0/opacity:0; the producer `.caret-value[data-v-a206e2d2]` five-token rule + `.glass-track` height:24px, byte-extracted; scheme-motion.css:343-344 `--z-tooltip:120`/`--z-popover:130`; GlassTimeline.vue.d.ts (dispatcher doc; `label` = "Tooltip caret text (scrubber variant only)"; the `role=progressbar` fragment belongs to the CONTINUOUS variant's rail). Consumer: AnimationControls.vue :55-115 (Tooltip is the LOCAL `@/components/ui/tooltip` shim — not HoverPopover; DockDropdownTrigger; the `gap-2 w-full` row; play-btn 3rem×2.5rem) + :134-136 (the dead width rule) + :190 (`.mini-progress{width:3rem}`); VisualizationView.vue:235-236/:282/:412-423 (`.controls-overlay` flex justify-center); FullscreenViewer.vue:107/:130-135/:208-217 (`.fs-controls` flex justify-center; `max-width="60rem"` feeding the dead prop); animation.ts :20-30/:51-75/:84-88/:96-143 whole-cited-range reads; trail.ts:40-72; ConvergenceTimeline.vue:36-56/:71/:130-140; useWorkspaceLoader.ts:116-118 (`anim.play()` ×2); reka SliderImpl.js handler census (exactly 4; `button` grep → 0), SliderRoot.js :58/:106/:115-118/:127-129/:160/:165-175. Greps: `variant="standard"` **14 hits / 7 files**; `timeline-slider` **2 hits, both the subject**; e2e `timeline|scrub` → **0** (`slider` → 4); `@reference` → 36 × `"tailwindcss"`; value.js exports = `"."` only; web scripts carry no unit runner. Arithmetic re-run: max |easeInOutSine(t) − t| = **0.10538 at t ≈ 0.2196** (≈42px on a 400px track).

## Scoreboard

**32 rows ADJUDICATED-CONFIRMED (5 MAJOR · 17 MINOR · 10 INFO, counting the 9 reader-missed rows banked) · 18 killed-register entries (incl. one kill of a banked registry cell) · 0 BLOCKERs survive as booked.**

The corpus's re-book fraction is the highest yet adjudicated (~26 of 54 axis rows are re-books of fr-AnimationControls / fr-ConvergenceTimeline / fr-BasisCanvas identities — expected: the banked fr-AnimationControls corpus already audited this exact file as a child). What is genuinely NEW and survives: **D-1** (the caret — the file's sole differentiating surface — cannot paint past the dock's `contain: paint`) and **SW-1** (with the banked D-1 width rule dead, the shrink-wrapped dock may starve the scrub track to a sliver — and the banked cure is a CAP where this needs a FLOOR). Both are one live probe from decisive.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-α | reader-β | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **L-M3/C-3/D-13 novelty + severity** (raw-`t` caret position vs `easedT` caret text) | MINOR; "BANKED: fr-AnimationControls M-13 + L-4/C-11" | MAJOR; "genuinely new — the banked L-4/C-11 row is a different claim" | **MINOR, booked as a NEW FACE amending the banked caret-clock family** | β is right on novelty: banked L-4/C-11 is `Math.round` vs floor-and-lerp (two `easedT` consumers), banked M-13 is caret-label vs CANVAS ("different quantities, no legend") — neither states the intra-marker position-vs-text split, which is multi-basis-mode-only and raw-vs-eased. α is right on severity: the banked R-13 demotion logic (caret `opacity:0` at rest, hover/scrub-only exposure — M-3) applies identically, and D-1 clips the marker besides. My arithmetic: max divergence 0.10538 at t≈0.2196 (α's cell exact; β's 0.1036@0.25 is a non-max sample). |
| R-2 | **D-12/L-M4/C-7 spurious-reveal reachability** | "the live false-positive reveal remains live-only" (unproven, not refuted) | "UNREACHABLE at this mount" — killed the named instances | **β RATIFIED by this seat's own census** | `grep keepOpen dist/*.js` → 3 chunks; dock.js holds exactly ONE call, inside the component rendering `class:["dock-layer-group",…]` — which AnimationControls never instantiates (bare `<GlassDock>` + `#collapsed`/default slots, my template read); its Tooltip is the LOCAL shim, not HoverPopover; the dropdown-menu chunk calls keepOpen **zero** times. At this mount the only holder is the subject's own `<Slider>` — `[data-held]` is de-facto element-local. The row survives at MINOR on semantics alone (dock-global predicate; the shipped element-local `data-touch-active` unused, ×2 in the chunk); SS-13 confirms the NEGATIVE. |
| R-3 | **C-16 severity** (unscoped `.timeline-row` ancestor compound) | INFO — "F.W3 REPLACES this component, it does not nest the fork inside it"; matched declaration benign | MINOR — "latent collision the moment F.W3 mounts the real primitive above this subtree" | **INFO — α wins; β's wave action adopted** | The hazard needs a producer `.timeline-row` ANCESTOR of the fork's own caret while the fork still exists — only a partial-migration interleaving produces it, and the matched declaration is `opacity:1` on hover. Sharpened by my dist read: THREE producer variants emit `.timeline-row` (scrubber, continuous, segmented), not C's two. Action for both readers: DELETE the block at F.W3, never migrate it. |
| R-4 | **L-B1 render-path cost** | not addressed | KILLED the "O(1) becomes O(N) permanently" narrative via banked BC-20 | **β RATIFIED** | My trail.ts:40-72 read: the `else` path is `push` with NO eviction and the loop-back predicate `t < lastT − 0.01` never fires at any selectable speed (Δt/frame ≤ 0.0033 at the fastest regime) — so the UN-latched path grows unboundedly while the latched path CAPS at `endIdx+1 ≤ 1201` and clears per frame. The true cost is the `preX === null` fallback (≤601 `evaluateFourier`/frame) = the fullscreen/never-precomputed instance (banked fr-BasisCanvas M-β3). The freeze verdict is untouched; the cost story is replaced (TS-1). |
| R-5 | **e2e coverage cell** | "my run → 0 hits" (stronger than C's cell) | (same direction) | **α RATIFIED — my run** | `grep -rn -i "timeline\|scrub" web/e2e/` → **0**; C's cited 4 hits match the pattern `slider` (my run: 4), not the quoted command. Finding stronger, cell dead (K-12). |
| R-6 | **D-1 grade** (axis: BLOCKER) | MAJOR | MAJOR | **MAJOR ratified** | Both readers' budget arithmetic independently re-derived and mine agrees: ~16px available (6px `--dock-padding-block` + 10px structural centring slack off the 40px `min-height` layer) vs 36px required (6+24+4+2). Demotion holds on banked M-3: the caret is `opacity:0` at rest, so the loss is confined to the hover/scrub reveal — and D's own BLOCKER framing ("the file's reason to exist never renders") is better served by the shadow-retirement route than by a grade. |

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5–W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring. The banked sibling records **fr-AnimationControls** and **fr-ConvergenceTimeline** own the fork-family identities; folds below never re-book them.

### MAJOR (5)

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **D-1** (both readers; NEW) | The caret — the file's sole stated reason to exist (:10-11) — is clipped by the dock's unconditional `contain: paint`: `.timeline-caret` paints upward out of `.timeline-row` (:90-92) needing ~36px of stack (6 offset + 24 `text-base` line box + 4 padding + 2 border) against ~16px inside the dock's padding box (6px `--dock-padding-block` + 10px structural slack — `.dock-layer` `min-height: 2.5rem` makes the slack play-button-independent, β's hardening). `overflow-y: visible` does not lift paint containment (css-contain-2 §3.2); no `overflow-clip-margin` anywhere in the package (my grep: 0). The producer's own rail-extend.css:3-14 is independent testimony: an abspos dock CHILD "could NEVER paint past that box" — an audited regression the producer cured only by re-parenting OUTSIDE the dock. F.W3 adoption does NOT cure it: the v7.0.0 ScrubberTimeline carries the identical `bottom: calc(100% + 6px)` caret (my git read). Demoted from the axis BLOCKER per banked M-3 (caret invisible at rest). | GlassTimeline.vue:90-118; shell.css:96/:83-96; rail-extend.css:1-16; layers.css:221-224; density.css:82-85 (all this seat) | **GLASS-RELAY** (the caret needs a portal/teleport or an in-row readout — producer-owned at every pin) + **F.W3 rider**: the re-derivation must not land a known-clipped caret with a clean diff. Severed-pixel count → SS-13. |
| **SW-1** (reader-α missed[0]; NEW, amends banked fr-AnimationControls D-1) | With the consumer's only width rule dead (banked D-1: `.animation-dock:where(.expanded)` matches nothing), the dock's width authority is its own shrink-wrap — overflow.css:96-99 says so in prose — and this component contributes ~0 to that intrinsic width: `flex: 1 1 0; min-width: 0` (:82-83) with every inner width a percentage (`w-full` CVA base; `.slider-track{width:100%}`). Both wrappers are `display:flex; justify-content:center` with the dock an auto-width item (my reads). The grid-stacked collapsed layer contributes ~150px (`.mini-progress` width:3rem + badge + mini play), the full layer's fixed members ~190px — so the scrub track may render at tens of pixels instead of the intended `min(960px, 100dvw − 1rem)`. CONSEQUENCE: the banked D-1 cure (rename to `--dock-max-inline-size`) sets a **cap** — it cannot restore a starved track; the fix must give the dock a definite width or the row a `flex-basis`/`min-inline-size` **floor**. Also qualifies superlative D-S1: under a fit-content dock the praised flex hygiene is what starves the control. If the live probe confirms, the banked D-1 demotion rationale ("a dock that still renders, functions") weakens and that row's severity re-opens. | AnimationControls.vue:134-136/:190; overflow.css:96-99; layers.css:196-224/:315-323; VisualizationView.vue:412-423; FullscreenViewer.vue:208-217; CVA base (all this seat) | **F.W4** — same edit as banked D-1 (one identity, AMENDED cap→floor). Rendered widths at both mounts = **SS-13's single highest-value probe** for this component. |
| **L-B1 / C-1** (freeze family) | Scrub opens on unconditional `@pointerdown` (:49-51/:73), closes only on reka's `hasChanged`-gated `valueCommit` (:53-57/:74; SliderRoot.js:117-118, my read); SliderImpl carries exactly 4 handlers — no `pointercancel`/`lostpointercapture` (my census). Deterministic triggers per banked K-4: (a) press+release inside ±half a 1% step of the PRE-press playhead value; (b) pointercancel. End state: rAF parked, `playing===true`, pause glyph + rainbow assert motion, `anim.scrubbing` latched into the trail's reset branch. NOT permanent (banked K-4: next value-changing drag or speed change self-heals — `watch(speed)` re-verified at animation.ts:138-143). | GlassTimeline.vue:43-57,:73-74; animation.ts:114-135; SliderRoot/SliderImpl (this seat) | **F.W3** — FOLD: one identity with banked **fr-AnimationControls L-1/L-6** + **fr-ConvergenceTimeline L·D-1/L·D-2/K-4** (the two verbatim forks die in ONE unconditional-session re-derivation incl. `pointercancel`). This corpus contributes DH-1 (the in-primitive cure precedent) and the affordance/state disagreement it exposes. Freeze + cancel probes → SS-13 (banked list). |
| **L-M2 / C-2 / D-7** (keyboard clobber) | Keyboard seeks never open a session: reka routes arrow/Home/End through `updateValues({commit:true})` (SliderRoot.js:165-175, my read) — no pointer path; `startScrub` has exactly one reachable caller (:46, from `@pointerdown` only); the rAF's once-anchored `startTime` (animation.ts:63) rewrites `t` next frame. Playing is the arrival state (useWorkspaceLoader.ts:116-118, my read). The producer's scrubber keyboard handler emits no bracket either — the seam is the store's pointer-shaped scrub contract, so F.W3 alone does not discharge this (C's honest scoping, ratified). | GlassTimeline.vue:49-57; SliderRoot.js; animation.ts:63-69 (this seat) | **F.W3** — FOLD: banked **fr-AnimationControls L-3** (which already mandates the explicit keyboard-seek acceptance test) + **fr-ConvergenceTimeline L·D-5(a)**. |
| **C-9 / L-m3 / L-i4** (no contract; the paid price) | No `modelValue`, no emits, a hard `useAnimationStore()` bind (:24-28) — and the tree paid with the verbatim 146-line sibling fork (ConvergenceTimeline.vue:36-56, my side-by-side read: same latch, same adapter, same guards, same dead token at :136, differing by exactly one dead indirection — L-m6). The producer's dispatcher declares the exact four bindings the file synthesises and forwards `scrubStart`/`scrubEnd` (my compiled-tail read). | GlassTimeline.vue:24-28; ConvergenceTimeline.vue:36-56; timeline.js (this seat) | **F.W3** — FOLD: banked **fr-AnimationControls C-4/M-1/M-14/L-6** + census §3a HARD-shadow + **fr-ConvergenceTimeline L·D-7**. Rider from K-16: the corpus's prescribed `ScrubberTimeline` import is UNREACHABLE (unexported at both layers) — the only reachable 4.0.0 route is the dispatcher, itself dead at 8.0.0; the durable cure remains banked K-14/SR-1's session-pair-on-`<Slider>` (as re-baselined by RB-1 below). |

### MINOR (17)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-3 / C-4 / L-m1** | `--slider-scrub-track-height` exists in NO glass-ui version (my grep: 0 whole-package); the track renders at the md default 20px, 4px under the retired recipe's 24px; the consumer's scoped class (0,2,0) loses to the producer's (0,3,0), both unlayered. | **F.W3** — FOLD: banked **fr-AnimationControls C-5/K-16**, AMENDED at fr-ConvergenceTimeline **M-2/K-6** to 23 declarations / 7 files; specificity ruling = banked **K-2**. The WCAG 2.5.8 spacing-exception question → SS-13. Remedy notes: K-11 corrects the "24px inexpressible" absolutism (inline custom property or `!important` reaches it); DT-1 supplies the cheaper edit for THIS site. |
| **D-4 / C-15** (NEW; inherited) | Caret mis-registered against the fill edge by `8t − 4` px (±4.5px at the 1.125rem mobile root): percentage `left` resolves on `.timeline-row`'s padding box (:84-85) while the Slider fills the content box (`w-full`; zero-width thumb ⇒ `thumbAlignment:"contain"` degenerates to zero inset). Producer-identical at 4.0.0 and v7.0.0 (my reads) — C's decline-to-score-the-fork is correct. No fourier instance banked (the padding-box class is banked only for value.js GradientStopEditor L-3 / MiniColorPicker MCP-26 — different product). | **GLASS-RELAY** (with EO-1 as the same missing geometry contract) + F.W3 note. Rendered offsets → SS-13. |
| **D-5** | No `aria-valuetext`: AT hears the private 0..100 adapter integer, never `t = 0.53` / `N = 7`; the caret is no substitute (pointer-events:none, opacity:0, no association); the 4.0.0 Slider cannot forward it (only `aria-label` is hoisted to the thumb — a consumer-authored valuetext lands on the role-less root). | FOLD to the banked **GLASS-RELAY three-item ask** (fr-ConvergenceTimeline L·D-4/M-3/SR-1: session pair · name prop · `aria-valuetext`). K-3 corrects the route: `valueText` is an 8.0.0 prop; the v7.0.0 cure was ScrubberTimeline's own `:aria-valuetext` (my git read). |
| **D-6** | The caret reveals on `:hover` + dock-`[data-held]` only — no `:focus-within` arm; `useDockHold` binds no focus event (my extraction); keyboard operation drives an invisible value. Producer-identical at both pins, while the v7.0.0 file's own docblock retires hover-gating for the BEAD "because the affordance must be present during keyboard/touch scrub" and leaves the caret hover-gated (my read). | **F.W3** (the re-derivation must decide an at-rest, focus-reachable readout — extends banked **fr-AnimationControls M-3**'s at-rest/touch face with the keyboard face) + **GLASS-RELAY**. |
| **D-12 / L-M4 / C-7** (rescoped per R-2) | `[data-held]` is the dock's reference-counted hold, not this slider's drag state — the WRONG PREDICATE, even though at this mount it is de-facto element-local (my keepOpen census; the named spurious-reveal instances are killed, K-10). The element-local hook the producer ships on the same root (`data-touch-active`, ×2 in the chunk) is unused; the producer's own retired arm was element-local (`:has(.glass-track:active)`). Stands in recorded tension with banked fr-AnimationControls superlative 7 ("a good coupling") — both true at different altitudes: documented hook, wrong semantics. | **F.W3** — dies with the fork; the negative probe (focus ⋮ / open Select / hover Tooltip ⇒ caret stays hidden) → SS-13. |
| **D-8 / D-9 / D-10 / L-m5** | The fork de-tokenised five of five paints of the producer's byte-identical rule (my byte-extraction of `.caret-value[data-v-a206e2d2]`): `--type-small`→`text-base` (18px at the mobile root vs the ~15.75px clamp floor), `--radius-sm`→`0.25rem` (4.5px vs 4px mobile), `--shadow-sm`→theme-blind `rgba(0,0,0,.1)`, `--duration-fast var(--ease-standard)`→`0.2s ease`, plus the bare `6px` offset. | **F.W3** (the block deletes with the fork) — transition leg FOLDS to banked **fr-AnimationControls M-7**; radius/shadow/type/offset legs are the AMENDMENT this record banks. K-17 fixes L-m5's `:111` anchor (radius is at :115). |
| **D-11 / L-m2** | Docblock stale/wrong ×4 (":13 v1.8.x" vs installed 4.0.0; `glass-scrubber` prose vs `standard` at :67; ":10-11 only divergent surface" false — `label` is a first-class upstream prop, my d.ts read) and the one live CSS comment (:120-123, a RETINT) describes a rule (:124-126) whose sole declaration is a height. | **F.W3** — partially banked (fr-AnimationControls superlative-5 exceptions; fr-ConvergenceTimeline M-4 owns the twin's inverted shadow-direction sentence); the comment↔rule referent mismatch is this record's addition. |
| **L-M3 / C-3 / D-13** (per R-1) | One composite marker, two clocks: caret x from raw `anim.t` (:62), caret text from `easedT`-derived `N` (AnimationControls.vue:44-54) on the default `sine` easing — max divergence 10.5pp of track (≈42px @400px, my arithmetic); the canvas agrees with the text, not the position; epicycle-only mode is self-consistent, so the incoherence is mode-dependent. | **F.W3/W4** — NEW FACE, booked as an AMENDMENT to the banked caret-clock family (fr-AnimationControls **M-13** fold-rider on fr-BasisCanvas **D-9/BC-8**; L-4/C-11 adjacent). Position-or-label must be decided in the same edit as that family. |
| **L-M1 / C-13** | The v-model getter mints a fresh array per rAF write — the five-deep Slider subtree re-renders ~60×/s for a value changing ~5×/s, including behind the collapsed `inert` layer. | **F.W3** — FOLD: banked **fr-AnimationControls M-11** (+M-12; fr-ConvergenceTimeline M-5 for the twin). Magnitude → SS-13 (banked). |
| **L-B2** (mechanism; grade per K-7) | Zero lifecycle hooks; two instances live at once (VisualizationView.vue:236 independent of FullscreenViewer.vue:131 — my reads) co-owning one bare `anim.scrubbing` with two private mirrors — while the same store solves the same dual-mount problem seventy lines up with the `visibleCanvases` reference count (animation.ts:96-106, my read). | **F.W3** — the re-derivation gives the scrub axis one owner (counter or store-derived), per the in-file model. Reachability of the mid-scrub unmount is narrowed by K-7 (pointer capture swallows the backdrop click; Esc-only). |
| **L-m6** (NEW) | `onPointerDown` is a dead one-line delegation and `onValueCommitStart` names an emit that does not exist (`emits: ["update:modelValue","valueCommit"]`) — the name asserts the exact symmetry L-B1 disproves; the sibling fork inlines the same guard with no indirection, so the indirection is a fork-local regression. | **F.W3** — dies with the fork; recorded so the naming trap is not re-authored. |
| **DT-1** (reader-β missed; NEW) | `class="timeline-slider"` (:72) + its rule (:124-126) are DEAD CODE as a triple: the class has exactly two references in the tree (my grep — the attribute and the selector), and the rule's sole declaration is the never-read token — the conduit has carried nothing since `4df1a06` (2026-05-16). Inverts superlatives L-S4/C·S-5 ("the channel is exemplary"): the channel has never delivered. | **F.W3** — a strictly behaviour-preserving 3-line delete; AMENDS the banked C-5 family with the cheaper per-site edit. |
| **DH-1** (reader-β missed; NEW) | The unconditional teardown the scrub session lacks is ALREADY EXECUTING on the same element inside the same primitive: `useDockHold` releases on window `pointerup` AND `pointercancel` AND unmount (my extraction, verbatim). Two consequences owed the wave: (1) on the freeze path `[data-held]` correctly FALLS at pointerup while `anim.scrubbing` stays latched — the affordance and the state actively disagree, part of why the freeze is invisible; (2) the F.W3 cure needs no new producer API and is version-stable (the 8.0.0 Slider still ships useDockHold). | **F.W3** — cure input for the banked L-1 re-derivation. |
| **LF-1** (reader-α missed; NEW; inherited) | The caret position is an inline `left` percentage written every rAF frame (:62) — a layout-inducing property the producer's own v7.0.0 scrubber docblock forbids by name ("NEVER `style.left` — Safari composites transform, not left", my git read) while its own caret still uses `left`. Distinct from banked M-11 (array identity) and M-12 (vnode churn): neither names the property choice. | **GLASS-RELAY** (the producer holds itself to a rule it breaks on the caret) + **F.W3** (the replacement binds transform). |
| **TS-1** (reader-β missed; synthesis) | The one regime where L-B1's cost claim is true is the FULLSCREEN instance: latched `scrubbing` ⇒ reset branch every frame; the fullscreen BasisCanvas never precomputes (banked M-β3) ⇒ the `preX===null` fallback runs ≤601 `evaluateFourier(components,·)`/frame at 60Hz — on the surface opened specifically to look at it. Conditional on the mount, not permanent, not universal (R-4). | **F.W3/W4** — a SEQUENCING rider: banked fr-BasisCanvas **BC-20**, **M-β3** and the L-1 pairing cure are one edit, not three. No new identity. |
| **C-6** | The docblock credits `standard` with "3-layer track + thumb + halo" it does not ship (thumb width:0/opacity:0; halo spectrum-gated — my byte reads); the migration deleted the visible-handle affordance the retired recipe painted on hover; at rest during playback the only playhead indicator is the fill edge. | **F.W3** — FOLD: banked **fr-AnimationControls K-5** + superlative-5's named exception; the removed-affordance framing is the surviving sharpening. On-screen regression read → SS-13. |
| **C-10 / D-17** | `aria-label="Timeline"` lands twice — once on reka's role-less host span where ARIA prohibits a name (no `inheritAttrs:false` on the glass Slider; SliderImpl sets no role — prior seats' chain, spot-held). C's MAJOR over-graded (the name still lands on the real slider); D's INFO under-graded (`aria-prohibited-attr` is serious). | **F.W4** + **GLASS-RELAY** — FOLD: banked **fr-ConvergenceTimeline L·D-4** (whose twin sprays the whole ARIA trio). |

### INFO (10)

| id | claim | disposition |
|---|---|---|
| **C-8 / C-11 / C-12 / C-14 / L-i1 / D-16** | The small-surface cluster: ×100/step-1 quantization justified by a false premise (reka's `step` is a `Number` with decimal handling — my SliderRoot read; the producer scrubber takes a raw float); `:min/:max/:step` restate reka defaults; the clamp runs three times; `label` required + unguarded where the producer's is optional + `v-if`-guarded (my `e.label ?` read); the `number[] | undefined` emit hole is unreachable under controlled `useVModel`. | FOLD: banked **fr-AnimationControls C-6** (+ fr-ConvergenceTimeline C·C-7's threshold table) · **fr-ConvergenceTimeline L·D-12** for the type hole. All die with the fork at **F.W3**. |
| **C-16** (per R-3) | Vue scopes only the last compound: `.timeline-row` in the two descendant rules is unscoped, and THREE shipped producer variants emit that class (my dist read). | **F.W3** — delete the block (the real reason is C-5, not collision risk). |
| **Z-1** (reader-α missed) | `z-index: var(--z-popover)` (:97) is structurally inert: the dock mints a stacking context twice over (`contain: paint` per css-contain-2 §3.2 + `backdrop-filter`, shell.css:96/:130 — my reads), so 130-vs-120 orders nothing outside it. Falsifies C·S-4's load-bearing clause ("the caret escapes the dock's stacking context" — it cannot; D-1 is the proof it cannot even paint) and empties D-15's stakes; D-15 survives on vocabulary alone. | **F.W3** — token swap if any block survives; else dies with the file. |
| **RC-1** (reader-β missed) | Neither the consumer handler nor reka filters `event.button` (my grep: 0 in SliderImpl) — a right/middle/barrel press on the track seeks AND parks the clock before the context menu opens; the session self-closes on the release, so it is a glitch door, not a freeze door. | **F.W3** — re-derivation input (filter primary button); browser divergence → SS-13. |
| **EO-1** (reader-β missed; inherited) | The caret is unclamped at the track ends: `translateX(-50%)` at t≈0/1 hangs the badge over the play button / speed select (`gap-2` = 8px clearance). Producer-identical. | **GLASS-RELAY** rider on D-4 (one geometry contract, one letter). |
| **C-18 / C-20** | Zero direct value.js/keyframes consumption; the transitive edge terminates in the `easedT` axis this file does not read; the census's "bare specifiers 4.0.0 no longer exports" present-tense reading is wrong at the pin (my export-map read: `"."` only at 0.13.0 — F.W2 is a consequence of F.W1). | FOLD: banked **fr-AnimationControls C-1/R-14** + census §1 pins + G33 — one identity, F.W1; never re-booked per component. |
| **C-19** | The surface has no automated assertion of any kind (no unit runner; e2e grep → 0, K-12's corrected cell). | FOLD: the census vitest-absent row (banked at fr-ConvergenceTimeline L·D-14 / fr-AnimationControls C-29). **F.W4** gate rides the cure wave. |
| **D-14** | `@reference "tailwindcss"` ×36 — scoped `@apply` cannot see the project `@theme`. | FOLD by reference: the banked dead-`@reference` cohort (fr-EditorControlsDock C-23; fr-EasingPicker L/i-3; fr-EquationResult FR-EQR-28; fr-CoefficientsSpectrum i-5(d)). D's "worth booking at F.W1" would double-book. |
| **RB-1** (both readers, converging; REGISTRY CORRECTION) | The banked route authorities are mis-baselined: fr-AnimationControls **K-14** ("producer 7.0.0 exports `Timeline` only — role=progressbar") and fr-ConvergenceTimeline **SR-1**'s repetition are v8.0.0 facts wearing a 7.0.0 label — my own git reads: `v7.0.0:timeline/index.ts` exports **GlassTimeline** with the full four-variant family on disk; the `Timeline`-only collapse first appears at v8.0.0; the `role=progressbar` fragment belongs to the CONTINUOUS variant's rail at 4.0.0/7.0.0 (my d.ts read). The route CONCLUSION (session pair on `<Slider>`'s own pointer events) survives at 8.0.0 unchanged. | **F.W1 charter note** — re-pin all producer facts at wave open (the fr-ConvergenceTimeline DISSENT-4 clause, now with a demonstrated miss in two banked records). |
| **BR-1** (D-2's surviving measurement) | `variant="standard"` blast radius = 14 raw hits = **9 attribute callsites across 7 files** + 5 prose (my grep reproduces both readers and the D axis's list exactly) — corrects the banked fr-ConvergenceTimeline D·D-2/K-8 "+14 sites" cell by the 5 prose hits. | **F.W1** — budget correction on the banked break-surface row. |

## Killed-claims register (18)

| # | killed claim | refutation (adjudicated) |
|---|---|---|
| K-1 | **D-2 (BLOCKER)**: "`variant="standard"` is definition-absent at glass 7.0.0 (`types.ts:10`) — a NEW break-surface row" | BASELINE-FALSE at the challenge's own named baseline AND a re-book. My git reads: `v7.0.0:src/components/slider/types.ts` line 5 = `export type SliderVariant = "standard" | "spectrum"` (identical at 51cfdfaf = 7.0.0); the quoted `:10` two-member scrubber union is the 8.0.0 working tree (`9b4c15d8`). The K-12-class error inverted — an 8.0.0 fact asserted at a 7.0.0 label. And the 8.0.0 break is ALREADY BANKED, correctly baselined, at fr-ConvergenceTimeline **D·D-2/K-8**. Survivor: the measurement (BR-1). Both readers killed identically; ratified with my own instruments. |
| K-2 | **D-3's uplift note**: "7.0.0 adds a coarse-pointer floor at `slider/styles.css:85-93`" | `src/components/slider/styles.css` DOES NOT EXIST at v7.0.0 — my `ls-tree`: Slider.vue, index.ts, types.ts only. The quoted block is the 8.0.0 working tree. The core (token dead ⇒ 20px) is confirmed; the uplift half would mis-budget F.W1 at the wrong version. |
| K-3 | **D-5's uplift cite**: "7.0.0 ships `valueText` at `types.ts:30-35`" | `valueText` is absent from the v7.0.0 interface (my read, ends at `motion?`); `git log -S valueText` → introduced by `9b4c15d8` with the 8.0.0 slider lane. The correct v7.0.0 route is ScrubberTimeline's own `:aria-valuetext="label || undefined"` on its `role="slider"` track (my read, :212-215). |
| K-4 | **D-17's uplift anchor**: "dev-time accessible-name warning at `Slider.vue:229-244`" | Reader-α's kill, adopted: substance true, anchor is the 373-line 8.0.0 file; at v7.0.0 the warning sits at :187-188 of a 651-line file. Fourth row from one mis-baselined instrument pass (K-1..K-4 share one signature: the D axis read producer HEAD and labelled it 7.0.0). |
| K-5 | **L-B1/C-1 permanence + BLOCKER** ("stays true forever"; "the traced curve can never accumulate again for the life of the page") | Banked **fr-AnimationControls K-4**, re-verified: the next value-changing drag re-enters `onValueCommit` with the stale flag and calls `endScrub()` → `startLoop()`; `watch(speed)` also restarts (animation.ts:138-143, my read). C-1 contradicts itself — its own Recovery paragraph names the healing gesture. BLOCKER → MAJOR, and the MAJOR itself folds to the banked identity. |
| K-6 | **C-1 failure scenario B**: "drag out to 40%, drag back to the origin step, release" freezes; "a *common* gesture" | The deciding comparison is against the PRE-press value: reka's snapshot (SliderRoot.js:160) fires after the press has already moved the model, and controlled-mode `useVModel` reads the un-updated prop synchronously — so returning to the press point COMMITS; only a release at the pre-press value fails. Adjudicated identically at banked fr-ConvergenceTimeline **K-4** (the full merge-order trace). A repair wave sizing frequency from scenario B would over-budget. |
| K-7 | **L-B2 (BLOCKER)**: "unmount mid-scrub … Esc, backdrop click … an ordinary user action" with "no local state left to recover from" | Reachability collapses: SliderImpl calls `setPointerCapture` on pointerdown, so a live scrub swallows the backdrop click and the edit-mode toggle; only Esc-while-dragging survives (two-handed) and lands in the K-4 self-heal regime; the inline instance is NOT unmounted when fullscreen closes, so a surviving instance can clear the flag; the file holds no listener/timer/rAF of its own (the identical shape banked as fr-ConvergenceTimeline **S-2**, a superlative). MINOR, mechanism kept. |
| K-8 | **L-B1's cost narrative**: "an O(1)-per-frame path becomes O(N·components) per frame, permanently … up to 1201 pushes per frame" | The comparison baseline is inverted (R-4, my trail.ts read): the "O(1)" else-path is unbounded append with an unreachable loop-back predicate (banked fr-BasisCanvas **BC-20**), while the latched path caps at ≤1201 and clears per frame — heap-better after one cycle. The true cost is the fullscreen `preX===null` fallback (banked **M-β3**) — booked as TS-1. |
| K-9 | **L-B1 door-2 touch mechanism**: "useTouchGate deliberately allows vertical touch-scroll to escape … an escaping touch is precisely a pointercancel" | Two kills from the installed bytes (reader-β, spot-held by this seat): the CVA base carries `touch-none` (my extraction — the compositor cannot start a scroll from the slider), and the gate's escape path `preventDefault()`s the touchstart, cancelling scrolling. The door survives via other pointercancel sources (banked fr-ConvergenceTimeline **L·D-2**); the named mechanism is dead and its touch reachability was overstated. |
| K-10 | **L-M4/D-12's named spurious-reveal instances**: "a focus-guarded dock member … `DockDropdownTrigger` … `SpeedSelect`, the `DropdownMenu`, or any sibling acquiring `keepOpen()`" | This seat's own census (R-2): exactly 3 chunks touch keepOpen; dock.js's sole CALL is inside DockLayerGroup (unmounted here); the dropdown-menu chunk calls it zero times; AnimationControls' Tooltip is the local shim, not HoverPopover. At this mount the only holder is the subject's own Slider — the spurious reveal is UNREACHABLE, not merely unproven. C-7's narrower predicate claim survives untouched. |
| K-11 | **C-4's remedy absolutism**: "24px is inexpressible through the producer's API at all" | The scoped-class channel is dead ((0,2,0) vs (0,3,0) — banked K-2), but an inline `:style="{'--slider-track-height':'24px'}"` falls through `$attrs` onto the very element carrying the producer rule, and an inline declaration beats every selector; `!important` in the scoped block also works. The remedy space is not empty; only the channel used is. The same absolutism in the banked fr-BasisSelector m-21 cell is FLAGGED for correction, not re-booked. |
| K-12 | **C-19's measurement cell**: "`grep -rn -i "timeline\|scrub" web/e2e/` → 4 hits" | My run of the quoted command → **0**; the cited lines match `slider` (my run: 4), a different pattern. The finding gets stronger; a wave re-running the quoted grep as an oracle would mis-read its own baseline. |
| K-13 | **C-3's §0 ledger cell**: the axes diverge "6 px apart on screen" | C's own §4 computes ≈42px @400px, and my independent derivation agrees (max \|easeInOutSine(t)−t\| = 0.10538 at t≈0.2196 ⇒ ≈42.2px). The summary cell is off ~7× — and it is the cell a skimming reader would carry into a severity decision. |
| K-14 | **C-16 at MINOR**: "a latent collision the moment F.W3 lands the real primitive" | R-3: the hazard needs the producer's `.timeline-row` as an ANCESTOR of the fork's own scoped caret — F.W3 REPLACES the fork; only a contrived interleaving matches, and the matched declaration (`opacity:1` on hover) is benign. INFO; the real reason to delete the block is C-5. |
| K-15 | **C·S-4 (superlative rationale) + D-15's stakes**: "the one place a literal would have been most dangerous — the caret escapes the dock's stacking context" | The caret CANNOT escape: `contain: paint` forces a stacking context (css-contain-2 §3.2) and `backdrop-filter` mints a second (shell.css:96/:130, my reads). `--z-popover` orders the caret only against its dock siblings; the same containment grounds D-1's clip — the corpus praised as an escape hatch the property that severs the element. D-15 survives on vocabulary alone (Z-1). |
| K-16 | **L-i4/C-9's prescribed import**: "4.0.0 already exports `./timeline` → `{GlassTimeline, ContinuousTimeline, ScrubberTimeline, SegmentedTimeline}` … adopt `ScrubberTimeline`" | STALE-DIST/export-map trap, re-verified by this seat: `dist/timeline.js` ends `export { oe as GlassTimeline };` and `index.d.ts` re-exports GlassTimeline + three types ONLY. `ScrubberTimeline` is unexported at both layers; the `.vue.d.ts` mirrors are compiler artifacts. The reachable route is the dispatcher (`variant` defaults `scrubber`, forwards the four bindings — my compiled-tail read), itself dead at 8.0.0 (RB-1). Both axes read the mirrors and never re-verified the export map — the exact trap the fleet law names. |
| K-17 | **L-m5's anchor**: "`.caret-value` radius hardcoded at `:111`" | Live :111 is `font-weight: 500`; the radius literal is at :115 (D-10 anchors it correctly). Citation drift in an otherwise declaration-for-declaration-verified table. |
| K-18 | **Banked cells** — fr-AnimationControls **K-14** / fr-ConvergenceTimeline **SR-1**: "producer 7.0.0 exports `Timeline` only — role=progressbar" | Killed against the REGISTRY, both readers converging and this seat's own git reads deciding: v7.0.0 exports `GlassTimeline` (scrubber-default dispatcher; ScrubberTimeline with `@pointercancel` AND `:aria-valuetext` intact); `Timeline`-only is v8.0.0. Route conclusions unaffected at 8.0.0; every F.W3 budget line citing "7.0.0" producer facts re-pins first. Booked as RB-1. |

## Superlatives — with provenance (L-18 runs both ways)

1. **D-1 is the corpus's real find, and the producer convicts itself** — derived by the D axis from the shipped stylesheet alone; independently corroborated by rail-extend.css:3-14 (an audited R4-1 regression note stating a dock child "could NEVER paint past that box"), which reader-α found and the axis did not cite. Two banked adjudications read this file closely and neither reached the containment. Reader-β's budget hardening (`min-height: 2.5rem` makes the slack structural) held under my read.
2. **C's §0a provenance page** — the subject's uncommitted diff is exactly the two `glass-scrubber → standard` lines inside the same WT that bumps the three package pins: the uplift's own edit window is where the drift entered. Byte-verified by reader-α; the sharpest archaeology in the corpus and the reason C-4/C-6/D-11 are cheap to cure.
3. **C-5's byte-level fork proof** — the census's "partial re-fork" hedge retired at the bytes (my own extraction of the `data-v-a206e2d2` block agrees: six identical `.timeline-row` declarations, identical caret markup down to `fira-code`, exactly the five token regressions).
4. **L-S3 / C·S-6** — `pointer-events: none` on the caret is load-bearing: without it the caret would swallow the press at the one x-position where a press cannot commit, making the corpus's own headline freeze trivially reproducible. Reached independently by two axes; retained from the producer while three sibling declarations were not.
5. **L-S2 / banked S-1** — the two-way `computed` is round-trip stable for all 101 reachable integers with no watch pair, no ref mirror, no nextTick dance — the exact `shallowRef`/`defineModel` stale-read class this constellation's memory records, sidestepped structurally.
6. **C·S-2** — the CR-2 typed-dock migration is real and complete end-to-end (typed context token; `keepDockOpen` default true; the string keys reduced to two prose comments) — the one migration in the file carried through entirely, and the axis proved it rather than asserting it.
7. **C·S-3** — the axis falsified its own tidiest explanation (a swallowed `@pointerdown`) before filing its BLOCKER. The L-18 posture, performed.
8. **D-S2** — the state-coverage finding the D axis expected and could not file: both mounts gate the dock on data presence. Caveat carried from banked fr-AnimationControls D-12/C-20: `hasData` includes `|| store.computing`, so the parent CAN mount over null payloads — right about this component, qualified one level up.
9. **D-S1, QUALIFIED by SW-1** — `flex: 1 1 0; min-width: 0` is exactly right under a definite-width dock and is what starves the control under a shrink-to-fit one. The superlative and the MAJOR are the same two declarations; which one is true is SW-1's live probe.
10. **INVERTED: L-S4 / C·S-5** — "the override channel is exemplary" praises a conduit (`class="timeline-slider"` → the dead token) that has never carried anything (DT-1). Recorded as the corpus's one superlative pair killed by a missed row.
11. **The store's `visibleCanvases` reference count** (animation.ts:96-106) with its written dual-mount rationale is the correct pattern seventy lines above the bare `scrubbing` flag that is not — the cure lives in the same file as the defect (L-B2's booking).
12. **Corpus fold discipline above fleet baseline** — all three axes refused to re-file the census HARD-shadow row and said so in their openings; where the corpus failed it failed on BASELINE (four D-axis rows from one mis-labelled instrument pass) and NOVELTY (~26/54 re-books), not on rigor. Both readers independently converged on the RB-1 registry correction from opposite directions — the strongest single output of the two-reader design in this corpus.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **SW-1** (highest-value probe): `getComputedStyle($('.glass-dock')).width` + the rendered `.timeline-row` width at BOTH mounts, expanded, at 1440px and 375px. Decides simultaneously: track starvation, sufficiency of banked D-1's cure, D-S1's fate, and whether banked D-1's severity re-opens.
2. **D-1**: severed-pixel count of the caret at both mounts (hover reveal); check for partial survival before accepting absolutist phrasing (the fr-EquationModeToggle precedent).
3. **D-4/C-15 + EO-1**: caret-vs-fill offset at t=0/0.5/1 (predicted −4/0/+4 px; ±4.5 below 768px) and the endpoint overhang over the neighbours.
4. **D-12 NEGATIVE probe**: focus the ⋮ trigger, open the Speed select, hover a Tooltip — the caret must STAY hidden (this seat rules the reveal unreachable; the probe confirms the negative).
5. **D-3**: whether WCAG 2.5.8's spacing exception rescues the 20px track given the 8px `gap-2` clearance.
6. **L-B1**: the banked K-4-narrowed freeze repro (press+release at the pre-press playhead; pointercancel) + pause-glyph/rainbow asserting motion over a frozen `t` — already an SS-13 item at fr-AnimationControls; plus iOS pointercancel reachability given `touch-none`.
7. **L-M1/M-11/M-12**: profiler magnitudes (banked list), behind the collapsed `inert` layer and with the dual mount live.
8. **RC-1**: right-click seek — does the context menu open over a moved playhead; Chrome/Safari capture-release divergence.
9. **C-6**: whether no-thumb/no-caret-at-rest reads as a regression against the retired glass-track recipe.
10. **L-M2**: visibility of the End-key chord across the figure (given banked BC-20 the trail already holds the full curve).

## Identity guards (folds by reference — never re-booked)

- The scrub-pairing family (L-B1/C-1, keyboard L-M2/C-2/D-7, teardown L-B2) is ONE identity with banked **fr-AnimationControls L-1/L-3/L-6/M-1/K-4/K-5** + **fr-ConvergenceTimeline L·D-1/L·D-2/L·D-5(a)/K-4**; this record contributes DH-1, TS-1, RC-1 and the affordance/state disagreement.
- The dead-token family is banked **fr-AnimationControls C-5/K-16**, amended at **fr-ConvergenceTimeline M-2/K-6** (23/7) and ruled at **K-2**; this record adds DT-1's dead-triple and K-11's remedy correction.
- The fork/no-contract family is banked **fr-AnimationControls C-4/M-1/M-14** + census §3a; the route authority is banked **K-14/SR-1** as RE-BASELINED by RB-1.
- The caret-clock family is banked **fr-AnimationControls M-13/L-4/C-11** folding to **fr-BasisCanvas D-9/BC-8**; L-M3 is its new intra-marker face.
- Render-cost rows fold to banked **M-11/M-12** (+ fr-ConvergenceTimeline M-5); trail-cost to **fr-BasisCanvas BC-20/M-β3** (TS-1 sequences them).
- `@reference` cohort, vitest-absent row, value.js pins/ELSPROBLEMS (census §1 + G33, fr-BasisCanvas C-8), R6-8 and R3-7a stay with their banked/intake identities.
- The padding-box-vs-border-box class is banked for value.js only (GradientStopEditor L-3, MiniColorPicker MCP-26) — D-4 is its first fourier instance, booked here once, GLASS-RELAY.
- GLASS-RELAY consolidation: ONE letter — the three banked items (session pair · accessible-name prop · `aria-valuetext`, per fr-ConvergenceTimeline SR-1 re-baselined) + D-1 (caret containment), D-4/EO-1 (caret geometry contract), D-6 (focus-reveal arm), LF-1 (`style.left` vs the producer's own rule), C-28-class residue as already banked.

## DISSENT — preserved, not ruled away

1. **The mis-baselined instrument pass is now a three-record pattern** (evidence about the fleet): the D axis produced four rows (D-2, D-3-uplift, D-5-uplift, D-17-anchor) reading producer HEAD as "7.0.0" — the same signature as the banked K-8 event, now inverted; and two BANKED records (K-14, SR-1) carry the same class of error in the other direction. Producer facts must be git-pinned (`git show vX:`) at read time, never read from the working tree under a version label. RB-1 carries the operational consequence to F.W1.
2. **Reader-β's severity dissent preserved**: L-M3 at MAJOR ("a positional readout whose text and position disagree by a tenth of the track is the component lying about its one datum"). This seat booked MINOR solely on the banked R-13 exposure logic (hover-only reveal, D-1 clip). If SS-13 shows the caret is in practice revealed for whole scrub sessions, the severity re-opens with the family.
3. **Reader-α's D-1 aggravation preserved**: if SW-1's probe shows the track starved AND the caret clipped, the component's two visible surfaces are both broken at once and the file-level verdict hardens from "defective shadow" to "non-functional at both mounts" — blocker-weight at the F.W3 route even with no single row graded BLOCKER.
4. **This seat's scope notes**: the reka chain cells (merge-order, controlled-mode snapshot, thumb-zero-width) were adopted from the banked fr-ConvergenceTimeline seat derivations and both readers' agreeing reads, spot-held but not re-derived end-to-end here; the C-axis git history cells (§0a's five commits) are the axis's archaeology, uncontested by both readers and not re-walked.

## Closing verdict

**ADJUDICATED — DEFECTIVE, zero BLOCKERs as booked: 32 rows confirmed (5 MAJOR · 17 MINOR · 10 INFO) including 9 reader-missed rows banked · 18 kills registered (4 from one mis-baselined D-axis instrument pass; 1 against banked registry cells, corrected as RB-1) · re-book fraction ~26/54 folded to banked identities without double-booking · routings: 2×F.W1 correction rows · the fork family and its riders to **F.W3** (where this file and its ConvergenceTimeline twin die in one unconditional-session re-derivation) · SW-1 + the residue to **F.W4** · one consolidated **GLASS-RELAY** letter (containment, caret geometry, focus-reveal, `style.left`, the banked three-item ask) · 0 NO-WAVE-OWNER — all agglomeration input to the forming X·F specs (M-25), stamping nothing. The two facts that outlive the shadow's retirement: the producer's dock clips what its own slider's consumers most need to show (D-1), and the dock's shrink-wrap may be starving the control the wave is about to re-derive (SW-1 — one measurement decides).**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
