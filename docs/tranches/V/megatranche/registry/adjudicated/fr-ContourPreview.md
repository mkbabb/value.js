claude-fable-5 (served model id, observed in this seat's own system prompt — recorded against L-11/L-14)

# fr-ContourPreview — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator · **Date**: 2026-08-08 (session opened under the 2026-07-27 formation clock) · **Component**: `fourier-analysis/web/src/components/visualization/ContourPreview.vue` (62 lines, byte-identical at fourier HEAD `cd26c653`).
**Corpus adjudicated**: three challenge axes (`challenge-D-design.md` 26 defects / 7 superlatives · `challenge-C-consumption.md` 11 / 4 · `challenge-L-library.md` 13 / 4, all read whole) + two hostile re-reader returns (design-and-usage · library-and-code), each of which challenged the full corpus before this seat challenged both of them.
**Method (L-18 turned on the re-readers)**: every verdict-level disagreement between the two returns was re-derived by this seat against the live trees with its own instruments; agreements were spot-audited where load-bearing. This seat's own reads: ContourPreview.vue (whole), CollapsibleSection.vue (whole), FourierMorphSvg.vue (whole, 41 lines), useViewState.ts (whole), VisualizationView.vue:190-279 + the `panel-swap` CSS at :430-432, usePointDrag.ts:20-55, ContourSettings.vue import/template/keyframe greps, the heading grep over `components/visualization/`, `api/routers/contours.py:15-30` + `api/models/assets.py:88,102` + `api/services/image_storage.py:315`; glass-ui producer: `git show 4ef35e8d:src/components/_shared/disclosure/disclosure.css` (whole head + the `.disclosure-content` rules at both pins), `axes.ts:15-35` @4ef35e8d, `Card.vue:1-40` @HEAD `7d9abe08`, `CollapsibleContent.vue` role/aria grep, `Collapsible.vue:64`, `git rev-list --count 4ef35e8d..HEAD` → 80. Plus two re-derivations by arithmetic: the 400×40 `meet` fit (decides KILL-2) and the out-in mount-order chain (decides KILL-10). Zero browser probes (probe parsimony); product source read-only; this file is the seat's only write.

## Scoreboard

**42 ADJUDICATED-CONFIRMED rows — 1 BLOCKER · 15 MAJOR · 20 MINOR · 6 INFO** (2 rows CONFIRMED-AS-REWRITTEN with their prescribed cures struck) · **11 KILLED claims** (refutations below, three of them killing one re-reader against the other) · **14 missed rows admitted** (5 folded into existing identities) · **12 superlatives endorsed** · **14 UNPROVEN-NEEDS-LIVE residues queued for SS-13**.

Blocker survivor: **B-1** — the component's only drawing, the contour stroke `hsl(40 90% 55% / 0.85)`, composites to **1.660:1** against the light `--card` (SC 1.4.11 bar: 3:1) while the repo's own remediated `--viz-amber` measures 4.625:1. The arithmetic now has **five** independent reproductions to three digits, authenticated by the repo's own published figure at `style.css:113-118`.

## Ruled disagreements (the seat's actual work)

The two re-readers disagreed at verdict level in four places. Each was re-derived here from the live trees; neither return was trusted on any contested cell.

| conflict | re-reader 1 (design-and-usage) | re-reader 2 (library-and-code) | RULING · deciding evidence (this seat's own) |
|---|---|---|---|
| **M-8 restore-path reachability** | CONFIRMED — "on the restore pass both subtrees are created together and `editorRef` is null", restoring D §6 C-3 against the earlier draft's kill | KILLED — the `<Transition mode="out-in">` defers the mount past the leave; the undefined-props vnode is discarded unmounted | **R2 WINS — the branch is KILLED.** My reads: the editor mounts in the `#stage` slot under `v-if="store.contour"` (VisualizationView.vue:203), OUTSIDE the aside's `<Transition name="panel-swap" mode="out-in">` (:254); the leave is real (`:430` `transition: opacity 0.2s …, transform 0.2s …`) and the outgoing `v-else key="viz-panel"` branch (:262) is live on the restore pass. useViewState.ts:24-35 flips `isEditing` in the tick `store.contour` first lands (verified verbatim), so the parent's next render mounts the editor in `#stage` (assigning `editorRef` during that patch) while the Transition holds the editor-panel branch behind the leaving viz-panel. When `afterLeave` forces the re-render, the slot re-evaluates and the ContourPreview that actually MOUNTS carries the real array. The undefined-props vnode is created and thrown away pre-mount, so `validateProps` never sees it — and per KILL-1 no warning could fire even if it did. **M-8 survives on its durable half only** (no loading/computing/error arms over a store that publishes all three). |
| **M-9's FourierMorphSvg sharpening** ("a `d`-accepting primitive existed and needed no extension") | CONFIRMED as a sharpening of C:D-5 | KILLED — substitution destroys two verified superlatives | **R2 WINS — the sharpening is KILLED.** My whole-file read of FourierMorphSvg.vue (41 lines, not the D-lane's 39): NO `vector-effect="non-scaling-stroke"` (`stroke-width` default 3 renders in DATA units — the exact failure S-1 credits this component for avoiding), NO `scale(1,-1)` group, NO `preserveAspectRatio`, `overflow: visible` (:38-39), and `viewBox` is a consumer prop (:24,:29) — every line of the bbox+pad+flip derivation would survive at the callsite. "Needed no extension" is false on three counts. **M-9 stands on its PathPreview leg alone.** |
| **m-9's mechanism** (R1: producer keyframes found under new names + an unlayered-suppression MAJOR; R2: grep for old names → zero, mechanism narrative intact) | Killed the D-lane's mechanism AND added the suppression finding | Confirmed m-9 as written (did not find disclosure.css) | **R1 WINS — on evidence this seat re-took itself.** `git show 4ef35e8d:src/components/_shared/disclosure/disclosure.css`: `@keyframes disclosure-open` (line 1) / `disclosure-close` (:14) interpolating `block-size: 0 ↔ var(--disclosure-content-size)`, with `.disclosure-content[data-state="open"] { animation-name: disclosure-open }` INSIDE `@layer components` (:107-113 @HEAD), `--disclosure-content-size: var(--reka-collapsible-content-height)` bound for the collapsible arm (:99-101), and its own PRM arm (:120-123 `animation-duration: 0.01ms`). Stamped by `CollapsibleContent.vue:49` `cn('disclosure-content', props.class)`, shipped by `Collapsible.vue:64` `<style src="../_shared/disclosure/disclosure.css">`. The D-lane's own falsifier ("find the keyframes under a new name") is SATISFIED; its grid-template-rows narrative belongs to ConfiguratorLayer alone. And the suppression is structurally sound: fourier's `CollapsibleSection.vue:60-65` writes the `animation:` SHORTHAND (resets `animation-name`) from an UNLAYERED scoped block onto the same element (the consumer's `collapsible-content` class merges with `disclosure-content`; the parent's scoped attribute lands on the child root) — unlayered author declarations beat `@layer components` regardless of specificity, so post-uplift the consumer's dead name wins over a working substrate reveal. See roster row 15. |
| **M-3's disposition** (R1: confirm availability, kill the projection; R2: kill the whole finding as inverted) | Availability confirmed + missed-row for the cure inversion | Full kill: "the shim is the forward-compatible expression" | **MERGED — CONFIRMED-AS-REWRITTEN, R2's inversion adopted as the row's content.** My reads: at 4.0.0 `CardSurface = "glass" | "cartoon" | "veil"` ships (both returns verified it; not re-taken). At producer `4ef35e8d`, `axes.ts` `SURFACES = ["glass", "veil", "opaque"]` and `SURFACE_TIERS = ["wash","quiet","resting","floating","overlay"]` — BOTH halves of the D-lane's prescribed `<Card tier="opaque" surface="cartoon">` are struck (re-read by this seat). At current HEAD `7d9abe08` the `cartoon` prop is struck too, and Card.vue's docblock says why in terms that settle the routing: *"`cartoon` / `grid` / `metal` … are struck: three of them wrote a class a consumer can write (`cartoon-surface`, `paper-grid`)"*. The shim (`@apply cartoon-surface`) is the producer-ENDORSED, version-stable expression. See roster row 4 and KILL-6. |

Smaller ruled cells: R2's L-10 correction (the timer does NOT arm on first open — `open = ref(props.defaultOpen)` starts `true`, the watcher is not `immediate`; verified from my CollapsibleSection read) is adopted into row 25. R2's B-1 soft-leg (the editor simultaneously renders the same contour on stage, so "the only rendering" is loose) is adopted as a bound on B-1's conformance prose — it does not move the ratio, and the editor's rendering uses the same failing literal at width 3, so conformance fails everywhere regardless. R2's resolution of L-8's write-path leg is VERIFIED by this seat (`save_contour` at contours.py:22-24 pulls `xs`/`ys` off `dict[str, Any]` with no cardinality check; `image_storage.py:315` computes `point_count: len(xs)` only) — the mismatched-array route is open at the boundary; folded into row 29.

## Adjudicated defect roster

Line anchors are fourier HEAD `cd26c653` / glass-ui@4.0.0 installed / producer `4ef35e8d` (cited HEAD) and `7d9abe08` (current HEAD, 80 commits on, both reading `version: 7.0.0`). Dispositions are **ADJUDICATED**, not VERIFIED — routes are agglomeration input for the forming X·F specs (M-25). The census taxonomy anchors: F.W0 substrate · F.W1 tri-package uplift · F.W2 colors-migration surface (present in the census's own routing though absent from the compressed anchor list; SS-3/SS-4 to confirm the slot) · F.W3/W4 shadow retirement + frontend audit · F.W5–W8 CRUD/provenance union · F.W9/W10 gates/close.

| # | id | sev | claim (adjudicated) | proof anchor | disposition |
|---|---|---|---|---|---|
| 1 | D:B-1 · C:D-1(a) · L:L-9 | **BLOCKER** | Stroke `hsl(40 90% 55% / 0.85)` composites to **1.660:1** vs light `--card` (`hsl(36 48% 97%)`, token chain unbroken — zero consumer override); `--viz-amber` cures to 4.625:1 at both pins. Five independent reproductions to three digits; the SC 1.4.11 prose is bounded per R2's soft-leg (the on-stage editor renders the same contour — with the same failing literal). | ContourPreview.vue:45 · style.css:107-111,113-127 · glass-ui color-radius.css:72 / dark-arm.css:64 | ADJUDICATED → **F.W2** (`stroke="var(--viz-amber)"`, alpha via `stroke-opacity`; with row 22's token mint) |
| 2 | D:M-1 | MAJOR | Entire informational payload is an unnamed graphic: no role/name/`<title>`/`<desc>` and no `aria-hidden` — no decision in either direction; the axe gate (`svg-img-alt` keys on `role="img"`) is structurally blind to it. | ContourPreview.vue:36-50 · both returns' repo greps EMPTY | ADJUDICATED → **F.W4** (blocker-adjacent; pair with an explicit assertion — no gate can regress-test it) |
| 3 | D:M-2(b)(c) + C:D-9 rider | MAJOR | Wrong hierarchy register (14px/600 header 12px above a sanctioned 20.4px/600 ConfiguratorLayer sibling) + disclosure state uncontrollable/unforwarded/unpersisted (`open = ref(props.defaultOpen)` snapshots; no `defineExpose`, no model passthrough; `:default-open="true"` restates the default). The (a) recursion-hazard leg is KILLED (KILL-5). | CollapsibleSection.vue:14,39 · ContourSettings.vue:190 · glass-ui configurator.css + offsets-sizing.css:499-500 | ADJUDICATED → **F.W3** (`<ConfiguratorLayer>` adoption at this site) |
| 4 | D:M-3 **as rewritten** | MAJOR | The booked `cartoon-card` upstream carry is STALE — at 4.0.0 the producer already ships the surface, and at 7.0.0 the producer's docblock ENDORSES the consumer-written `cartoon-surface` class, which is exactly what the shim `@apply`s. The shim is the version-stable form; the D-lane's `<Card tier="opaque" surface="cartoon">` migration is a 21-site typecheck break at the uplift target (KILL-6). Nothing to relay upstream; nothing to migrate to `<Card>`. | style.css:98-111 · axes.ts:22,26 @4ef35e8d (this seat's re-read) · Card.vue docblock @7d9abe08 | ADJUDICATED → **F.W3** (re-scope the BH relay: send `--viz-amber` only, RETIRE the cartoon-card ask) + **F.W1 rider** (do NOT migrate; correct the shim's stale comment per row 27) |
| 5 | D:M-4 | MAJOR | `--font-serif-math` is defined in NONE of the three trees (consumer zero; installed dist one hit = the consumption; producer @7d9abe08 one hit = the consumption) — the title falls to UA generic serif beside a Plus Jakarta Sans subtitle 6px away; the brand's Computer Modern fork is stranded on `--font-sans` while the shell applies `font-serif`. One `:root` declaration cures 17 files. | CollapsibleSection.vue:39-40 · glass-ui typography/utilities.css:65-67 · bridges.css:68 · style.css:13-20 | ADJUDICATED → **F.W4** (consumer declaration + the `font-sans`/`font-serif` shell decision) |
| 6 | D:M-5 · C:D-3 · L:L-1 | MAJOR | Re-frames on every pointermove against the sibling's commented stable-bounds policy on the same array — 7 index writes per move at default `magnetRadius` 3 (verified from my usePointDrag read). SC 2.3.3 rider stands. | ContourPreview.vue:16-29 · ContourEditorCanvas.vue:49-50,74-82 · usePointDrag.ts:31,36-48 | ADJUDICATED → **F.W4** (adopt `stableBounds`; adoption, not invention) |
| 7 | D:M-6 · C:D-6 · L:L-2 (tall-contour half) | MAJOR | X-derived pad spent on both axes: on tall contours the vertical gutter collapses; past h ≳ 15.8·w the extreme vertices clip against the SVG root's default `overflow: hidden`. The mirror (wide) case is KILLED (KILL-2, this seat's own arithmetic) and the gutter-table's attribution rescoped (KILL-3): the X-derived pad is INERT on every wide contour and bites only when h > w. | ContourPreview.vue:26,28,38,57-59 · ContourEditorCanvas.vue:29,79-80 | ADJUDICATED → **F.W4** (per-axis pad via the row-40 `contourBounds` extraction; row 42's `overflow: visible` is the immediate palliative) |
| 8 | D:M-7 | MAJOR | Hard-coded 160×160px in a 320-440px column (fills 60.6/41.7/37.7% of available width), the closure's only px literal, in a repo whose root font steps the WRONG direction (1.125rem mobile → 1rem desktop, so the graphic reads smaller-vs-label on mobile); both in-tree preview primitives take a size knob. | ContourPreview.vue:57-61 · VisualizationView.vue:322-329,355,366-368 · style.css:40-50 | ADJUDICATED → **F.W4** (one owner, `rem`/`%`/`clamp()`) |
| 9 | D:M-8 (durable half) | MAJOR | Zero empty/loading/error coverage over a store publishing `loading`/`computing`/`error` (depth-counted); the card always emits `<path d="">` into an always-open section; house idioms + installed `Skeleton` exist unused. The restore-path undefined-render branch is KILLED (KILL-10, this seat's mount-order derivation). | ContourPreview.vue:12,18,42-48 · stores/workspace.ts:49-51,60-68 | ADJUDICATED → **F.W4** (`emptyText` idiom + `Skeleton`; SS-13 measures the extract-window duration) |
| 10 | D:M-9 (PathPreview leg) · C:D-5 | MAJOR | The framing/colour/sizing/guard decisions exist correctly in the sanctioned `ui/PathPreview.vue` and were re-derived here weaker on every axis; honest bound kept (not a drop-in — the Catmull-Rom spline needs a `d` path). The FourierMorphSvg "needed no extension" sharpening is KILLED (KILL-9). | PathPreview.vue:14-17,29-33,56,60 · lane-frontend.md:444 | ADJUDICATED → **F.W4** (component-convergence; with row 40) |
| 11 | C:D-2 (type-null half) · L:L-6 | MAJOR | `points: Point2D[] | undefined` compiles to `{ type: null, required: true }` — runtime validation permanently OFF; reads stricter, is strictly weaker than `points?:`; the repo's only prop-declaration union of this form; invisible to `vue-tsc`, the sole gate. The warning sub-claim is KILLED (KILL-1). Fix is one character. | ContourPreview.vue:7 · both returns' independent compiler receipts (one empirical) | ADJUDICATED → **F.W4** (`?:`; pairs with row 13's F.W5 seam change per the L-lane carry) |
| 12 | L:L-4 · C:D-4 | MAJOR | Byte-identical Catmull-Rom `d` built TWICE per mutation (same array, same omitted arg) at full double precision (~100+KB at n=1024 default) plus a second O(n) bbox pass, per pointermove, into a 160px box; scheduler exists unapplied; the sanctioned sibling rounds (`.toFixed(4)`). | ContourEditorCanvas.vue:85 · ContourPreview.vue:13,16-29 · contourEditing.ts:39 · defaults.ts:8 | ADJUDICATED → **F.W4** (+ SS-13 magnitude; with row 41's tracking amplifier) |
| 13 | L:L-5 (+ exercised-pattern rider) | MAJOR | Prop sourced through the sibling's `defineExpose`d MUTABLE ref (the undo ring's subject) across a v-if boundary; the channel is read-write (`proxyRefs` set-trap) and the tree ALREADY writes through it one key over — `magnetRadius` via the `computed` setter at VisualizationView.vue:82-85 (both re-readers verified; corroborated by my read of the `:magnet-radius`/`@update:magnet-radius` bindings at :242,:246). `getPoints()` is exposed two lines earlier. Live pattern, not latent. | ContourEditorCanvas.vue:205-207,217-227 · VisualizationView.vue:82-85,257 | ADJUDICATED → **F.W5** (readonly/`getPoints` seam; a TWO-member change — `points` AND `magnetRadius`) |
| 14 | C:D-1(b) | MAJOR | The literal is unreachable by the repo's own resolver: `cssVarToHex` has no slash-alpha arm (falls to the `#888888` sentinel), so it sits OUTSIDE the 117-LOC surface F.W2 migrates — the migration cannot sweep it as collateral. | colors.ts:22-54 (live lines; C's cites drifted — row 37) | ADJUDICATED → **F.W2** (with row 1) |
| 15 | D:m-9 **as rewritten** (+ R1 missed: suppression) | MAJOR | At 7.0.0 the consumer's `animation:` shorthand does not merely go inert (the old keyframe names are gone) — it SUPPRESSES the substrate's working `disclosure-open`/`disclosure-close` reveal, because the unlayered scoped shorthand beats `@layer components` and resets `animation-name` (mechanism re-verified by this seat at both producer pins; the D-lane's grid-rows narrative is KILLED, KILL-4). Blast radius 2 files: CollapsibleSection.vue:54-71 AND ContourSettings.vue:361-374 (my grep: `:365,:368` consume the same names). Cure is DELETION of both scoped blocks — disclosure.css supplies the PRM arm too. No CSS gate can catch it. | CollapsibleSection.vue:54-71 · ContourSettings.vue:355-374 · disclosure.css @4ef35e8d/:107-113 @HEAD · CollapsibleContent.vue:49 · Collapsible.vue:64 | ADJUDICATED → **F.W1** (delete both blocks WITH the uplift; Tailwind-v4 layer handling in the consumer build = SS-13 residue) |
| 16 | MISSED (R1) — heading semantics | MAJOR | The edit-mode aside has ZERO heading landmarks: the wrapper renders the title as a `<span>` in the exact register ImageUpload.vue:44 renders as an `<h3>` (my grep: that h3 is the visualization tree's ONLY heading, and it lives in the `v-else` panel). ConfiguratorLayer adoption does NOT close this (its surface is button/expanded/controls, no heading). | CollapsibleSection.vue:39 vs ImageUpload.vue:44 · VisualizationView.vue:255-263 | ADJUDICATED → **F.W4** (wrap the title in `<h3>` inside the trigger; version-independent) |
| 17 | D:m-1 · L:L-3 · C:D-6(latent) | MINOR | No degenerate-extent guard: zero X extent emits viewBox width 0 (spec: disables rendering of the element); two of three siblings guard (`|| 1` — ContourEditorCanvas.vue:67 is the L-lane's exact cite; D and C both mis-cite). | ContourPreview.vue:26,28 · ContourEditorCanvas.vue:67 · PathPreview.vue:29-30 | ADJUDICATED → **F.W4** (rides row 40) |
| 18 | D:m-2 · C:D-7 · L:L-7 | MINOR | Guard skew (<3 path / <2 viewBox) frames a box for a path it decided to leave empty at n=2, while the library handles n=2; unreachable through the editor (delete floors at 3, simplify at `max(4, …)`). | ContourPreview.vue:12,18 · contourEditing.ts:22-24,103 · ContourEditorCanvas.vue:180 | ADJUDICATED → **F.W4** |
| 19 | D:m-3 | MINOR | Fallback viewBox `"0 0 1 1"` frames the UNflipped half-plane against the file's own convention two lines later; harmless only because two bugs cancel. Convention-correct: `"0 -1 1 1"`. | ContourPreview.vue:18 vs :27-28,41 | ADJUDICATED → **F.W4** |
| 20 | D:m-4 | MINOR | Three padding owners across two files, none tokenized — a change to any silently re-tunes the frame (the 14/20/20 asymmetry itself is a superlative, S-5). | ContourPreview.vue:33,35 · CollapsibleSection.vue:36,46 | ADJUDICATED → **F.W4** |
| 21 | D:m-5 | MINOR | Two non-scaling (= device-px) stroke widths for one contour — preview 2 vs editor 3 across a ~4× smaller box — with no binding, token, or ratio. | ContourPreview.vue:46-47 · ContourEditorCanvas.vue:262-263 | ADJUDICATED → **F.W4** (+ SS-13 perception) |
| 22 | D:m-8 · L:L-9(system) | MINOR | The brand amber is an inline presentation attribute at 9 sites / 2 files / five alphas, zero declarations — invisible to CSS-only token audits and on the weakest cascade rung; the semantic home ships at both pins. | 9-site grep (both returns re-ran) · color-radius.css:266 · style.css:119-127 | ADJUDICATED → **F.W2** (mint `--contour-stroke` once, cure ten sites with row 1) |
| 23 | D:m-7 | MINOR | Display-only card carries 4.0.0's `cartoon-surface` hover-lift (`&:hover:not(:disabled)` always true on a `<div>`); under PRM the transition dies but the 1px translate lands instantly; 7.0.0's recipe is three declarations, no hover — F.W1 is a net cure landing a knowing 21-site visual delta. | glass-ui cards.css:33-49 · a11y-overrides.css:6-16 · producer card/styles.css:256-260 @7d9abe08 | ADJUDICATED → **F.W1** (land knowingly; SS-13 before/after) |
| 24 | D:m-10 (conclusion) | MINOR | Sole tab stop wears no design-system focus recipe (`.focus-ring`/`.interactive-item` both unapplied; fourier's own D.W4.d enumeration omits `.collapsible-trigger`); the UA ring renders — vocabulary inconsistency, not absent focus. The supporting 3-rule enumeration is KILLED (KILL-7: 18 rules, none matching). | CollapsibleSection.vue:36 · glass-ui base.css:174-178,205-208 · style.css:136-143 | ADJUDICATED → **F.W3** (rides row 3's ConfiguratorLayer adoption — the producer trigger carries `:focus-visible` box-shadow, my disclosure.css read :45-47) |
| 25 | D:m-11 · L:L-10 (as corrected) | MINOR | Uncancelled 250ms timer per re-open firing an un-PRM-guarded `scrollIntoView({behavior:"smooth"})` through an `any`-typed `$el`. Corrections adopted: does NOT arm on first open (R2 — `open` seeds `true`, watcher non-immediate; my read); blast radius is 2 other files / 3 callsites, NOT ContourSettings (KILL-8). | CollapsibleSection.vue:14-30,66-70 | ADJUDICATED → **F.W4** (matchMedia guard + handle/`onUnmounted`) + BH relay note (the `Collapsible` root-element assumption is producer-side, per the L-lane carry) |
| 26 | C:D-8 | MINOR | The closure's one glass-ui seam imports from the ROOT barrel (also the tree's only single-quoted glass-ui specifier) while siblings use `/collapsible`; cost nil under the vendor-ui chunk; the barrel survives the uplift. | CollapsibleSection.vue:2 vs ContourSettings.vue:8-12 | ADJUDICATED → **F.W4** (discipline sweep) |
| 27 | C:D-10 · L:L-13 | MINOR | The shim's own comment is stale by +7 sites/+1 file (live: 21 application occurrences / 14 files; both re-readers re-ran the greps; lane-frontend's 25 is the raw denominator — both right under their own denominator). | style.css:101-102 | ADJUDICATED → **F.W1 rider** (correct with row 4's comment refresh) |
| 28 | D:i-3 · L:L-8 (write-path leg RESOLVED) | MINOR | One non-finite coordinate blanks the preview silently (invalid viewBox is IGNORED by UAs); `noUncheckedIndexedAccess` absent; and the write path is OPEN — verified by this seat: `save_contour` takes `xs`/`ys` off `dict[str, Any]` with zero cardinality validation, `point_count: len(xs)` only. | ContourPreview.vue:19-28 · contourEditing.ts:8 · web/tsconfig.json · contours.py:22-24 · assets.py:102 · image_storage.py:315 | ADJUDICATED → **F.W4** (client finite screen, rides row 40) + **F.W5-W8** (boundary cardinality validation) |
| 29 | MISSED (R1) — producer drift | MINOR | The producer tree moved 80 commits past the corpus's cited HEAD while both commits read `version: 7.0.0` (my `git rev-list` → 80; HEAD `7d9abe08`); substantive drift already inside this component's ledger (relocations + the `cartoon` prop strike). Every §7 uplift row needs a pin to the PUBLISHED artifact before F.W1 executes. | glass-ui git log · Card.vue @7d9abe08 | ADJUDICATED → **F.W1** (pin before the transaction) |
| 30 | MISSED (R1) — unbooked F.W1 ledger rows | MINOR | 7.0.0's CollapsibleContent adds `role="region"` + `:aria-labelledby` + stable id (my grep: :47-48) — a NEW guarantee, not marginal (4.0.0's region has no role/name); AND `.disclosure-content` imposes `color: var(--muted-foreground-strong)` + `--type-small` typography on every collapsible body (my disclosure.css read :92-94) — inert for this SVG, a live re-render for the wrapper's other consumers (EqCoefficientsPanel, FunctionInput ×2). Neither is on the D-lane's ledger. | producer CollapsibleContent.vue:47-48 · disclosure.css:90-94 | ADJUDICATED → **F.W1** (ledger rows) |
| 31 | MISSED (both) — C-lane provenance drift | MINOR | The C-axis's file:line cites are systematically drifted against the substrate it certifies (my own full read confirms: stroke :45 not :44, cartoon-card :33 not :32, `preserveAspectRatio` :38 not :53, flip :41 not :55, vector-effect :47 not :48, plus the cross-file floors); its Axis header also attributes working-tree pins to HEAD. The fold must re-resolve every C-lane citation before acting (the D-19 lesson). L-axis cites are byte-exact throughout — the accuracy benchmark. | this seat's ContourPreview read vs challenge-C cites | ADJUDICATED → **NO-WAVE-OWNER** (procedural — binds SS-3/SS-4 spec authoring) |
| 32 | MISSED (R1) — default-state inversion | MINOR | The aside opens with the decorative 160px void EXPANDED (`:default-open="true"`) above the settings COLLAPSED (`:default-open="false"` — my ContourSettings grep :190): on every edit-mode entry the least informative element dominates the opening composition (compounds rows 8 and 9). | ContourPreview.vue:34 vs ContourSettings.vue:190 | ADJUDICATED → **F.W4** (SS-3 owns the composition decision) |
| 33 | MISSED (R2) — bbox verbatim clone | MINOR | The bounding-box loop is a whitespace-identical clone of ContourEditorCanvas.vue:60-66 — the literal copy-paste 200 lines away that all three axes missed while arguing duplication against different-algorithm primitives; the cleanest evidence for the `contourBounds` extraction. | ContourPreview.vue:19-25 ≡ ContourEditorCanvas.vue:60-66 | ADJUDICATED → **F.W4** (row 40's extraction) |
| 34 | MISSED (R2) — magnet double-write | MINOR | `usePointDrag`'s magnet loop double-writes colliding indices whenever n ≤ 2·radius+1 (reachable: default radius 3, delete floors at 3, simplify descends to 6) — at n=3 the dragged anchor itself is displaced ahead of the pointer. Verified by this seat's read: `before`/`after` carry no distinctness check (:36-48). Adjacent-scope (the file is cited by two axes), silent corruption. | usePointDrag.ts:36-48 · ContourEditorCanvas.vue:38,180 · contourEditing.ts:99-103 | ADJUDICATED → **F.W4** (adjacent-scope rider; SS-13 for the visible geometry) |
| 35 | MISSED (R2) — deep-reactive tracking amplifier | MINOR | Both computeds iterate the DEEP-reactive proxy (~12k `track()` calls per recompute at n=1024, doubled by the editor's twin computed), per pointermove; `toRaw`/`shallowRef` unused in the subtree — the cost multiplier neither lane priced into row 12. | ContourEditorCanvas.vue:34 · ContourPreview.vue:13,20 · contourEditing.ts:27-39 | ADJUDICATED → **F.W4** (+ SS-13 magnitude) |
| 36 | D:i-2 · L:L-11 | INFO | `closedSplinePath(points, tension = 0.5)` advertises a dead knob (`factor = 1/6` hardcoded = tension/3 at the default — inlined, never removed); the two renditions agree by accident, not contract. | contourEditing.ts:20,33 | ADJUDICATED → **F.W4** |
| 37 | D:i-5 | INFO | The closure is NOT zero-line under F.W1: `CollapsibleSection.vue:4` is 1 of the census's ×35 `lucide-vue-next → @lucide/vue` renames (verified in my read). Sharpens C:S-3's honest self-scoping. | CollapsibleSection.vue:4 · CENSUS-2026-08-03.md:105 | ADJUDICATED → **F.W1** (fold by reference to the census row — not re-booked) |
| 38 | C:D-11 · L:L-12 | INFO | Zero automated coverage at every level; the only gate (`vue-tsc -b`) is precisely the one row 11's defect is invisible to; the least-tested highest-risk pure code (the simplify heap) sits in the import. | web/package.json:6-12 | ADJUDICATED → **F.W9/W10** (test-net gate) |
| 39 | MISSED (R2) — dead branch | INFO | `simplifyClosedPoints`'s `if (!alive[idx]) continue;` (contourEditing.ts:172) is unreachable — each index is extracted exactly once (R2's full heap re-derivation, which ALSO certifies the rest of the heap CORRECT — the more useful result). | contourEditing.ts:117,166-180 | ADJUDICATED → **NO-WAVE-OWNER** (comment/cleanup if row 40's wave touches the file; the heap-correct certification is the record) |
| 40 | L §5 carry — `contourBounds` extraction | (cure row) | Extract `contourBounds(points, margin)` (per-axis pad + `|| 1` + finite screen) into `lib/contourEditing.ts`; rewire ContourPreview/ContourEditorCanvas/PathPreview. Closes rows 7, 17, 28-client, 33 in one move; row 33 proves the callsites already agree character-for-character. | challenge-L §5 · row 33 | ADJUDICATED → **F.W4** (the framing cluster's single cure) |
| 41 | MISSED (R2) — grading consistency | INFO | The corpus files one default-restatement as a defect (`:default-open="true"`) and another as a superlative (`preserveAspectRatio="xMidYMid meet"` — the SVG spec's own default): both are documentation-of-intent or both are noise. S-3's credit is demoted accordingly (agreement-with-the-spec-default, credited as legibility, not choice). | ContourPreview.vue:34,38 | ADJUDICATED → **NO-WAVE-OWNER** (binds SS-3/SS-4 register) |
| 42 | MISSED (R1) — `overflow: visible` palliative | INFO | A one-declaration cure for row 7's clip half exists with in-repo precedent — verified in my FourierMorphSvg read (:38-39 `display: block; overflow: visible`); neutralizes the clip at the installed pin with zero geometry change. No lane named it. | FourierMorphSvg.vue:37-40 vs ContourPreview.vue:57-61 | ADJUDICATED → **F.W4** (immediate palliative; row 40 is the real cure) |
| — | D:m-6 | MINOR | Previews the path, not the editing surface: `showImageOverlay` changes the editor and not the thing labelled "Preview"; takes only `points`. (Counted in the 20 MINOR.) | ContourPreview.vue:7 · VisualizationView.vue:205 | ADJUDICATED → **F.W4** (accept "shape only" + re-word the deck, or pass the toggle through — SS-3 decides) |

## Killed-claims register

| # | claim (id · holder) | refutation |
|---|---|---|
| KILL-1 | C:D-2's warning sub-claim — "*every entry into edit mode emits `[Vue warn] Missing required prop` and burns one wasted empty render*" (also SS-13 queue item 2) | Killed by BOTH re-readers, once empirically: `validateProps` warns only when the KEY is absent (`isAbsent = !camelizePropsKey.includes(key)`), and `:points="editorRef?.points"` always carries the key with value `undefined`; R2's live probe against the installed runtime produced ZERO warnings for the key-present case. Reachability half also dead: the toggle path has `editorRef` populated (editor mounts under `v-if="store.contour"`, class-hidden), and the restore path is KILL-10. **DROP SS-13 probe #2 — it cannot fire.** |
| KILL-2 | L:L-2's mirror case — "*400×40 → pad = 40 = 100% vertical margin — the shape renders at a third the size it should*" | Killed by R1, re-derived by THIS SEAT: when the fit is width-limited, `s = 160/(w+2p)` on both schemes (X-derived padY=40: viewBox 480×120, s=0.3333; per-axis padY=4: viewBox 480×48, s=0.3333 — SAME scale); the vertical gap resolves to `(160−s·h)/2 + s·padY` whose total is padY-independent under `meet`'s letterbox. Delta = 0.00px on every measure. The X-derived pad is inert on every wide contour. |
| KILL-3 | D:M-6's gutter-table attribution — "*the framing swings 3.5× to 11×*" as evidence of the padding defect | Killed by R1 (arithmetic exact, attribution not): under the lane's own prescribed cure, the three wide/square rows — including the one the lane re-derived by hand as its receipt — are IDENTICAL (Δ 0.00px); only the tall rows move (+6.06/+8.33px). Three of five rows demonstrate `meet` letterboxing in a square viewport, which survives the cure. Matters because the F.W4 fix budget was being sized off the table. |
| KILL-4 | D:m-9's mechanism + falsifier — "*the producer moved disclosure reveals to grid-template-rows; the only related addition is the transition-disclosure caret register*" | Killed by R1, RE-VERIFIED BY THIS SEAT at both producer pins: `disclosure.css` ships `@keyframes disclosure-open`/`-close` (block-size interpolation off `--reka-collapsible-content-height`), applied via `.disclosure-content[data-state=…] { animation-name: … }` in `@layer components`, stamped by CollapsibleContent.vue:49, shipped by Collapsible.vue:64, with its own PRM arm. The lane's own falsifier is satisfied; it grepped for the old name and never searched for the replacement it asked for. The OUTCOME survives, for a worse reason → roster row 15. |
| KILL-5 | D:M-2(a) — the reka-`Collapsible`-inside-`Configurator` recursion-hazard framing | Killed by both re-readers, corroborated by my ContourSettings grep: the docblock describes ConfiguratorLayer's OWN former reveal; and ContourSettings — the very component held up as the sanctioned pattern — renders the reka triple (:255-307) INSIDE its own ConfiguratorLayer (:190) inside the same `<Configurator>`, one level deeper, shipping without a reported loop. The prescribed cure removes one such node and leaves another twelve pixels away. (b)/(c) stand → roster row 3. |
| KILL-6 | D:M-3's F.W1 projection + prescribed cure — "*F.W1 does not change the picture … `<Card tier="opaque" surface="cartoon">` is the shipped expression*", routed as the F.W4 21-site migration | Killed by both re-readers, decisive cells re-read by THIS SEAT: at `4ef35e8d` `SURFACES` has no `"cartoon"` and `SURFACE_TIERS` no `"opaque"` — both halves of the cure are unspellable at the uplift target; at `7d9abe08` even the interim `cartoon` prop is struck, with the docblock endorsing the consumer-written `cartoon-surface` class. Executing the migration as routed lands 21 non-compiling values across 14 files in a repo whose only gate is `vue-tsc`. The conclusion INVERTS → roster row 4. |
| KILL-7 | D:m-10's bounding enumeration — "*the only `outline: none` rules at 4.0.0 are `.popover-content` and the two focus recipes*" | Killed by R1's grep: EIGHTEEN such rules ship at 4.0.0, not three. None matches this button, so the operative conclusion (UA ring renders; the defect is vocabulary) survives — the enumeration offered as the finding's bound was off by 6×. |
| KILL-8 | L:L-10's blast radius — "*3 other consumers (ContourSettings, EqCoefficientsPanel, FunctionInput)*" | Killed by R1's import grep, corroborated by my read: ContourSettings does NOT import CollapsibleSection — it imports the reka triple directly (:8-12) and hand-rolls its own disclosure. Correct radius: 2 other files / 3 other callsites (FunctionInput ×2). |
| KILL-9 | D:M-9's FourierMorphSvg sharpening — "*a `d`-accepting component already existed and needed no extension*" (ENDORSED by re-reader 1, killed by re-reader 2) | RULED KILLED on this seat's whole-file read: FourierMorphSvg has no `vector-effect` (stroke in DATA units — the exact S-1 failure), no Y-flip, no `preserveAspectRatio`, `overflow: visible`, and a consumer-supplied viewBox — the substitution destroys two verified superlatives and relocates every line of the derivation to the callsite. "Needed no extension" is false on three counts. |
| KILL-10 | D §6 C-3 / M-8 branch 2 — the restored-edit-mode cold load as a reachable undefined-points render (RE-CONFIRMED by re-reader 1, killed by re-reader 2) | RULED KILLED on this seat's own template + transition derivation: the premise is right (useViewState flips `isEditing` the tick `store.contour` lands) but the consequence dies at `<Transition name="panel-swap" mode="out-in">` (:254, real 0.2s leave at :430, live outgoing `v-else` branch at :262) — the editor mounts in `#stage` (outside the Transition) and assigns `editorRef` during the same patch in which the aside's new child is deferred behind the leave; the `afterLeave` re-render re-evaluates the slot, so the ContourPreview that MOUNTS carries the real array. The undefined-props vnode is discarded pre-mount; `validateProps` never sees it (and KILL-1 holds regardless). |
| KILL-11 | L:L-10's arming claim — "*the timer arms on the very first open*" | Killed by R2, verified by my read: `open = ref(props.defaultOpen)` seeds `true` and the watcher is not `immediate`, so a timer requires a user close→open cycle. Folded into roster row 25. |

## Superlatives (endorsed, with provenance)

1. **`vector-effect="non-scaling-stroke"` (ContourPreview.vue:47)** — D:S-1/C:S-2/L:S-2, found independently by all three lanes, endorsed by both re-readers: the path is emitted in unbounded contour-data space then `meet`-fitted into 160px; a plain width would vary by an order of magnitude per upload. KILL-9 makes this credit load-bearing. Honest limit kept by everyone: it cannot rescue B-1.
2. **The Y-flip is algebraically exact on both halves and documented in one line (:27-28, :41)** — D:S-2/C:S-1/L:S-1; both re-readers re-worked the interval arithmetic (content [−maxY, −minY]; viewBox symmetric, exactly `pad` each side). L's observation is the sharpest: a vertically symmetric contour renders identically under a sign error — the class of bug that survives casual testing.
3. **`preserveAspectRatio="xMidYMid meet"` — unanimous across four of four SVG fitting surfaces** (the fourth, EasingCurvePreview, counted by re-reader 1) — endorsed WITH the row-41 demotion: the value is the SVG spec default, so the credit is intent-legibility, not choice.
4. **Scoped-style restraint (D:S-4)** — four lines, one class, zero `!important`, zero `:deep()`, in a host carrying a 198-line scoped block; this is precisely why rows 3/4's migrations are cheap.
5. **The optical frame (D:S-5)** — 20/20/20px on three sides, 14px to the caption: correct typographic grouping, `rem`-derived so it rides the root-size step; and the lane's willingness to split its predecessor's finding into a surviving ownership half (row 20) and a refuted aesthetic half is what L-18 asks for.
6. **The reduced-motion floor is doubly inherited (D:S-6)** — local `animation: none` + the producer blanket; no ungated CSS animation in the closure, and the lane names its own two live gaps (rows 25, 23) rather than claiming a sweep. Post-F.W1, roster row 15's deletion hands the PRM arm to disclosure.css.
7. **Zero break-surface members (D:S-7/C:S-3), with the self-inflicted i-5 downgrade** — the closure's glass-ui surface is the Collapsible triple (in neither ADDED-14 nor REMOVED-21) plus a shim resolving at both pins; one lucide rename line is the whole F.W1 cost. The correction habit is what makes the ledger credible.
8. **Zero teardown surface, argued structurally (L:S-3)** — no listener/timer/rAF/observer/context in 62 lines, sited correctly against the census's canvas-third leak concentration; the one leak in the closure is the imported wrapper's (row 25), and the lane separated them.
9. **Guard-before-reduce + a bbox loop with no argument-count ceiling (L:S-4)** — the `Infinity` seeds can never escape; the explicit single-pass `for…of` vs the sibling's spread, booked honestly as latent superiority.
10. **METHOD — the D-lane's §5 F-1 self-kill**: suspected the caret snaps off the producer's own docblock, then refuted itself against fourier's compiled bundle (`transition-property: transform, translate, scale, rotate`); both re-readers re-ran it in the exact cited output file. Killing a finding the producer's prose appears to endorse is the hardest kind.
11. **METHOD — the contrast arithmetic**: FIVE independent reproductions to three digits (earlier draft, C, D, both re-readers), authenticated by the repo's own published figure. One recorded prose slip (the "linear sRGB" label on a gamma-composite number; linear would give 1.614) — the arithmetic is the browser-correct one.
12. **METHOD — the L-axis's citation discipline**: byte-exact file:line on every checked fourier path, including the three lines both other lanes got wrong (:67, :180, :29); its contra-findings block (the `proxyRefs` reactivity chain, do-not-split, the trailing `Z`) is the cleanest L-18-inward in the corpus. Adopted as the accuracy benchmark row 31 measures the C-axis against.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **B-1** — computed runtime `--card` under the real cascade + rendered-stroke probe (static math: 1.660 light / 5.922 dark; no consumer override exists).
2. **M-4** — the two computed `font-family` values side by side (UA serif vs Plus Jakarta Sans, 6px apart).
3. **Row 6** — perceived viewBox-swim amplitude dragging a bbox-defining point (must drag an extremum; a mid-contour drag reveals nothing).
4. **Row 7** — visible stroke clip at extreme aspect (10×400 → 0.398 CSS px top gap vs a 1px stroke half-width; UA rounding/AA could mask).
5. **Rows 12+35** — measured per-pointermove task duration at n=1024 incl. the ~12k-track amplifier; 50ms long-task / 200ms INP thresholds.
6. **Row 8** — the rendered gutter (52/112/132px per side).
7. **Row 4 residual** — whether the fourier shim renders identically once `cartoon-surface` loses its hover-lift at F.W1 (the 21-site / 14-file visual delta; `<Card>` substitutability is MOOT per KILL-6).
8. **Row 15 residual** — the unlayered-beats-layered suppression confirmed against a BUILT post-uplift bundle (Tailwind v4's layer emission for Vue scoped blocks) before the deletion is authored.
9. **Row 23** — PRM instant-translate and the hover-lift delta before/after frames.
10. **Row 25** — whether the aside overflows its scroll parent at a real viewport (the condition gating the smooth scroll).
11. **Row 17** — UA behavior on a zero-width viewBox (blank vs intrinsic-size fallback).
12. **Row 34** — the visible geometry corruption at n ≤ 7 with default magnet radius (the collision arithmetic is proven; what a user sees is not).
13. **Row 21** — perceived weight ratio, 2px non-scaling in 160px vs 3px full-height.
14. **M-2(a) residual** — the recursion reproduction is best read ALREADY-DISCONFIRMED-IN-PRODUCTION (ContourSettings ships the composition); a Lighthouse cold-load probe closes it. **DROPPED from the queue: the C:D-2 warning probe (KILL-1 — it cannot fire).**

## Identity guards (fold by reference — never re-booked)

- **R5-7** (native-loop deriver blindness): the `ContourEditorCanvas.vue:269` `<circle v-for>` rider and the L-lane's *dual* (attribute-encoded geometry invisible to element-keyed evidence) fold to the existing census carry → F.W4. Referenced, not re-booked.
- **R6-8** (operation↔client leaf coupling): CONFIRMED-BY-ABSENCE here (whole import closure read by two seats; contourEditing.ts imports nothing). Corroboration only.
- **`--viz-amber` + `cartoon-card` carries** (`lane-frontend.md §9 item 8`): ONE identity each — rows 1/22 amend the first (the cure never reached a literal), row 4 RETIRES the second's producer-ask half per KILL-6. Amended by reference.
- **lucide ×35 rename** (CENSUS-2026-08-03.md:105): row 37 confirms this closure owns 1 of 35 — the census row keeps the identity.
- **PathPreview "genuinely bespoke — no flag"** (lane-frontend.md:444): agreed and extended by row 10 on the inverse axis; not contradicted.
- **The `cartoon-card` site-count** (lane-frontend.md:380 "25"): row 27 reconciles both denominators; no new row.

## Closing verdict

**ADJUDICATED — DEFECTIVE, ONE BLOCKER, RICHLY CURABLE, AND THE CORPUS'S TWO PRESCRIBED FLAGSHIP CURES WERE BOTH WRONG.** The component draws its only object at 1.660:1 without a name, inside a chassis that hand-restates the design system at every layer — yet the geometry core (flip, fit, non-scaling stroke, guard-before-reduce) is exactly right, and the two most expensive corrections this adjudication delivers run AGAINST the challenge corpus, not with it: the `<Card tier="opaque" surface="cartoon">` migration would have shipped a 21-site typecheck break at the uplift target (the shim it replaces is the producer-endorsed stable form), and the "restore-path blank render" driving an SS-13 probe cannot occur (out-in defers the mount past the ref assignment; the warning cannot fire at a key-present callsite). Fifteen of forty-two rows route to F.W4, three to F.W1 (one of which — the disclosure-motion deletion — must land WITH the uplift or the substrate's working reveal ships suppressed), two to F.W2, three to F.W3, two to F.W5(-W8), one to F.W9/W10, four NO-WAVE-OWNER for SS-3/SS-4; every row keeps its original id for life.

— end of apotheosis. The three challenge axes and both re-reader returns remain immutable beside this file per E-1/E-3; dated evidence was not rewritten.
