# PaletteCardMenu — adjudicated apotheosis · seat model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited — recorded per M-23/L-18)

**Seat**: fresh-Fable adjudicator (the incredulity seat) over two hostile re-readers.
**Date**: 2026-08-06 · **HEAD at this seat's reads**: `3ecf0267` (branch `tranche-u`; the corpus was authored 2026-07-28/29 against `c654824e` ancestry — every anchor below was re-resolved by this seat against the working tree, and my own reads are the anchor authority).
**Corpus**: 15 challenge files (C/D/L axes × pass-1..4 + the three governing pass-5 files) against `demo/palettes/browser/card/PaletteCard/PaletteCardMenu.vue` (229 L) + its card/pane/export/transport cone. All three governing axes are **EXISTS-ORIGINAL** per `registry/HYDRATION-LEDGER.md:94-96` (self-authored, 5 passes each) — the r2 pass-5 files (`challenge-C-implementation.md` · `challenge-D-design.md` · `challenge-L-library.md`) govern, exactly as the brief states.
**Readers adjudicated**: reader-1 (design-and-usage) · reader-2 (library-and-code). Both were attacked before either was adopted. The one verdict-level CONFLICT between them (reader-1 confirmed a row whose proof asserts "the admin path is confirmed"; reader-2 killed exactly that premise) was re-derived end-to-end by this seat and ruled for reader-2. Every kill was re-litigated; two kills required this seat's own instruments to close (the L-8 arithmetic; the P5-3 line-clamp mechanism), and one reader-1 missed-find rested on a class-co-location premise neither reader had proven — this seat extracted the producer's class stack from the shipped chunk and proved it.
**Motion law (M-xx)**: `audit/codex-provenance/motion-quarantine.md` has **no FALSE row against this component** (its subject set is the 46 REPORT-AUTHORED files; this corpus is EXISTS-ORIGINAL). Adjacent row M-16 (PaletteCardMeta vote press) is TRUE-and-motion-safe and is not carried as a defect. The one motion-adjacent kill below (K-4) *relies on* guard-1's `transition-property` rewrite, which this seat re-read in `a11y-overrides.css:1` — `scale` is excluded from the reduced-motion transition list, so the producer press register is PRM-safe as the kill claims.

**Arbiter's own instruments this session (all static + node; zero browser probes — probe parsimony; product source read-only):** full source read of PaletteCardMenu.vue (whole, 229 L) and PaletteCard.vue (whole, 365 L); `demo/palettes/{export.ts (whole), usePaletteExport.ts:1-12, usePaletteActions.ts:31-60, usePaletteStore.ts:118-152, useAdminUsers.ts:110-132, utils.ts:15-32, types.ts:35-52, usePalettePorts.ts:1-22/:125-137, BrowsePane.vue:92-96+cardRefs census, PalettesPane.vue:84/:101-121, browser/admin/AdminUsersPanel.vue:138-152 + showConfirm census (:263/:289/:316/:327), browser/status/{index.ts:1-8, ApiOfflineChip.vue:33-40}, browser/card/PaletteCard/PaletteCardMeta.vue:24-34}`; `demo/workbenches/mix/MixSourceSelector.vue:244-270`; `demo/platform/transport/availability.ts:36-46/:160-196`; installed glass-ui 7.0.0 dist: glass-ui.css rule extraction (`.dropdown-menu__item,.dropdown-menu__sub-trigger` unlayered `color:inherit`; `.dropdown-menu__label` unlayered 600/muted/secondary-size; `.dropdown-menu__content` max-height+scroll vs `.dropdown-menu__sub-content` `overflow:hidden` no max-height; `.dropdown-menu__shortcut`; **exactly one `@layer` at-rule in the whole file**), `components/_shared/menu.css` (`@layer components` glass-menu-row highlight `color: var(--accent-foreground)` + 44px touch floor), `styles/utilities/base.css` (`.interactive-item` `:hover`/`:focus-visible{box-shadow:var(--focus-ring-shadow)}`/`:active{scale:…}`), `styles/utilities/a11y-overrides.css` (forced-colors `.interactive-item:focus-visible` outline; PRM transition-property rewrite), typography (`@utility text-mono-caption` = uppercase; `@utility text-caption` = italic; `@utility fira-code` = liga/calt; `text-subheading` 600), tokens (`--type-tracking-{wide 0.025em, wider 0.05em, caps 0.1em}`; `--overlay-min-width: 8rem`; `--overlay-max-block: 60vh`), **class-stack extraction from `dist/dropdown-menu-BlbnvMaZ.js` → `"dropdown-menu__item interactive-item glass-menu-row"` on one element (×3 occurrences)**, `dist/components/dropdown-menu/` ships CheckboxItem/Group/RadioGroup/Shortcut d.ts; reka-ui dist: `Menu/MenuSubTrigger.js:131-132` (`if (props.disabled || event.defaultPrevented) return;`), `Menu/MenuItem.js:63-64`, `Menu/MenuContentImpl.js` searchRef = exactly {136 declare, 218 reset, 251 expose}, `shared/useTypeahead.js:6` separate `refAutoReset`, `Menu/utils.js:64` `pointerType === "mouse"`, `DropdownMenu/DropdownMenuRoot.js:26-31` `modal … default: true`, `Menu/MenuPortal.js:1/:28` unconditional Teleport vs `Menu/MenuContent.js:117` Presence; **re-executed `probe-L5-lattice.mjs` → TOTAL VIOLATIONS: 28**, row-identical to the corpus; eslint.config.js:228-298 dead-glob read + `ls demo/@` → No such file or directory; `docs/tranches/V/ARCHITECTURE.md:49-58` fence verbatim; VISUAL-CONSTITUTION.md:62/:72/:76/:84/:102 + PROPORTION-AUDIT.md:77-78 verbatim; `package.json#exports` = 7 keys no root, `ls dist/index.d.ts` → missing, `git check-ignore dist` → `.gitignore:17`, installed `@mkbabb/value.js` 4.0.0; my own element census of the subject file: 16 `DropdownMenuItem` + 1 `DropdownMenuSubTrigger` = 17, `gap-2 cursor-pointer` on 12, the five export items `cursor-pointer` alone; SESSION_PORT_KEY shell-import census = exactly 5; Checkbox/Group/Shortcut demo census = barrel-only; SubTrigger demo census = this file only; `demo/palettes/export/*.ts` = 914 lines, sole external consumer `demo/test/export/byte-exact.test.ts`.

## Scoreboard

**45 CONFIRMED (7 blocker-class · 30 major · 8 minor/info; 3 cross-booked identities inside those, anti-rename) · 15 KILLED · 14 reader-missed finds adopted (all inside the 45) · 11 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

Blocker-weight survivor: **AP-1 (D-1)** — the component *as a category* is constitutionally abrogated: VISUAL-CONSTITUTION.md:102 (verified verbatim by this seat) forbids the card body from owning an action menu and assigns rename/lifecycle/export to the selected inspector; this file is that menu, and **X-W7 already owns the transposition by name** (W7.md:100 `PaletteCardMenu.vue | modify-carve`; W7.md:222-225 "rename, menu, feedback, versions, tags, export, publication, vote, feature, and delete move to the selected-entity inspector"). Most rows below are therefore adjudicated *into a wave that intends to dissolve their host* — the roster's job is to guarantee nothing dies unrecorded and every mechanism that outlives the host (producer collisions, export truth, graph law) keeps its own disposition.

**X-map** (per the CARRY-CUT-LEDGER and the ApiOfflineChip precedent): X-W1 re-gate (CI/e2e/deploy truth) · X-W7 palette specimen + domain split (owns this file, PaletteCard.vue, PaletteCardMeta.vue, BrowsePane/PalettesPane, `export.ts` delete + certified-serializer cutover + one-slugify + mutation-confirmation + admin-menu strip, all BY NAME) · X-W8 shadcn/graph subtraction (lattice, barrels, module homes) · X-W10 canonical design + motion (type registers, tokens). GLASS-OWNED rows leave by the standing BH relay (owner edict). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors re-resolve before any cure (D-19).

## Ruled disagreements and seat-level corrections (the actual work)

1. **The admin-delete conflict — ruled for reader-2; reader-1's confirmed row AMENDED.** Reader-1 confirmed P4-2/C2-14 (owner Delete unconfirmed) with the proof clause "the admin path is confirmed"; reader-2 killed that clause. This seat re-traced the chain with its own reads: PaletteCardMenu.vue:163-169 `Delete (admin)` (no `:disabled`, no latch, no confirm) → PaletteCard.vue:300 `adminDelete: () => emit("adminDelete", props.palette)` — an unguarded re-emit (`:301` is the Q1 *comment*, not a dialog) → AdminUsersPanel.vue:150 `@admin-delete="emit('adminDeleteUserPalette', $event, user.slug)"` → useAdminUsers.ts `onAdminDeleteUserPalette` = `getAdminToken(); if (!token) return; await deletePaletteAdmin(token, palette.slug)` — **no dialog anywhere**. AdminUsersPanel's `showConfirm` (:263) has exactly three call sites (:289 prune, :316 delete-all-of-user, :327 delete-user); none is the card menu. Ruling: **both destructive paths are unconfirmed** (AP-6); the corpus's thrice-repeated "the admin branch is already confirmed" (C-file:279, pass-2 C2-14, pass-4 P4-4) is killed (K-10), and any repair scoped to "the owner branch is the outlier" ships a server-side, cross-user, un-undoable hard delete unchanged.
2. **L-8 arithmetic — ruled for reader-2 on this seat's own count.** The subject file has 16 `DropdownMenuItem` + 1 `DropdownMenuSubTrigger` = 17 such elements; `gap-2 cursor-pointer` sits on 12; the five export items (:113/:116/:119/:123/:126) carry `cursor-pointer` alone — *deliberately*, they render no icon. "11 of 11 = 100% = root styling, move `gap-2` to the producer root" is wrong twice and its cure would smear icon-gap over every iconless item in the constellation (K-12). The edict-5 residue (per-instance styling + two inline `style=` attributes) survives inside AP-10/AP-16.
3. **P5-3's kill — ruled for reader-1 on this seat's own read.** PaletteCard.vue:55 is `line-clamp-2 sm:line-clamp-1` — a *vertical* clip under `-webkit-box`, for which `scrollWidth − clientWidth` is structurally zero. The pass-5 probe that reported the card name "0 px clipped / max-width: none" could not see the truncation its own witness sentence describes ("Muted Terracotta an…"). K-1 stands; D-10's computed-style half (menu header 14.384 px vs card 20.352 px) survives into AP-36.
4. **Reader-1 missed #6 (the producer's own highlight ink is dead) — CONFIRMED by this seat's own class-stack extraction, the one premise neither reader had proven.** The mechanism requires `.dropdown-menu__item` and `.glass-menu-row` to co-locate on one element. My extraction from the shipped `dist/dropdown-menu-BlbnvMaZ.js`: `"dropdown-menu__item interactive-item glass-menu-row"` — three occurrences, one class string. With glass-ui.css carrying exactly one `@layer` at-rule, the unlayered `color:inherit` beats `@layer components`' `color: var(--accent-foreground)` on every highlighted row. AP-37 is real, producer-on-producer, and is the missing mechanism behind what D-12's probe saw (adopted; rides the BH relay beside AP-3).
5. **C2-2's scope — reader-2's correction folded into the BLOCKER row.** Mouse hover opens the submenu (reka `onPointermove`, gated `pointerType === "mouse"` — my read); the dead paths are click and tap. "Kills the submenu on pointer input" (C-file:77, pass-4:64) would mislead a summary-scoped repair; AP-2 carries the precise reach.
6. **Identity guards (anti-rename), three rows.** (a) L-20's tsconfig phantom-paths facts — root `dist/index.d.ts` missing, `/parsing`+`/units` phantoms, live `./css`+`./value` omitted, the "CLOSED 8-key set" comment false by one against 7 exports keys (all re-verified by this seat) — are **fact-identical to ColorSpaceSelector L-14**, already FOLDed to W-HYGIENE config parity with a born-RED generator command: CROSS-BOOKED, never re-booked (AP-22). (b) The dead eslint globs (`demo/@/**` objects; `ls demo/@` → nothing — my read) are **ColorSpaceSelector L-5(a)**: cross-booked inside AP-18, whose *new* fact (the ratified ARCHITECTURE.md lattice + the 28-violation census) keeps its own life. (c) L-12's chip/lamp seats are already adjudicated in ApiOfflineChip AP-1/AP-12 + MT-DOCK-LAYERS-1: AP-30 cross-books and adds only the menu's seat, which dies with AP-8.
7. **Severity rulings.** P5-7 ("Saved!" reported for a silent store no-op — dedup-return and slug-guard fall-through re-read by this seat) is raised INFO→MINOR: a false success statement on a user gesture is a UX lie, and X-W7's "every mutation … a visible result" gate owns it by name (AP-44). P4-1's BLOCKER rank is *not* adopted — the geometry is source-confirmed (AP-27) but the routine-mis-aim frequency is unmeasured (U-4), exactly as reader-1 held. The `unlisted` defect family (AP-9) carries the latency rider both readers converged on: the arm is unreachable through the shipped write paths, so the severity is latent-MAJOR, and this sentence is the governing-doc statement that was missing.

## Adjudicated defect roster

Anchors: subject file = PaletteCardMenu.vue unless prefixed. "MY" marks this seat's own re-derivation; unmarked cells adopt a reader's verification that this seat spot-checked without full re-execution.

### Blocker-class

| id | claim | proof | disposition |
|---|---|---|---|
| **AP-1** (D-1; PA law 12) | The card action menu is constitutionally abrogated: the card body may own no action menu; rename/lifecycle/export live in the selected inspector. This file is that menu, mounted in the card at PaletteCard.vue:83-106. | MY: VISUAL-CONSTITUTION.md:102 + PROPORTION-AUDIT.md:77 law 12 verbatim; live mount read. | **X-W7** — the wave's own §4 mechanism (W7.md:222-225) IS the cure; every row below that names this file dies with the transposition unless separately routed. |
| **AP-2** (C-1/C2-2/C3-7, scope per K-11) | `@click.prevent` on the sub-trigger (:108) sets `defaultPrevented` before reka's own click handler, which early-returns on it — click and tap activation of the Export submenu are dead; touch has no hover path, so all five formats are unreachable on touch. Hover (mouse) and keyboard survive. The modifier carries no handler and no comment; PaletteCard.vue:82 already stops `click` on the wrapper. | MY: :108 verbatim; reka MenuSubTrigger.js:131-132 early return; utils.js:64 mouse-only pointer gate; SubTrigger census = this file only. | **X-W7** (the submenu dies into the inspector; until then the cure is a one-token deletion). |
| **AP-3** (D-2) | The destructive ink is inert: unlayered `.dropdown-menu__item{color:inherit}` defeats `@layer utilities` `.text-destructive` at :135 and :164 — Delete renders in Rename's ink (measured ΔRGB 0-1, pass-1/pass-3). | MY: the unlayered rule extracted from glass-ui.css; **one** `@layer` at-rule in the file; class stack proven (disagreement #4). | **GLASS-OWNED → BH relay** (the `variant="destructive"` seam + the layer-discipline defect) · consumer arm X-W10. Rides the same relay as AP-37. |
| **AP-4** (L-1/C2-3-chain) | The five Export items ship the LEGACY serializers; the 914-line certified byte-exact set (`export/` — RFC 8785, digest, correct identifier grammar) has zero production consumers. | MY: usePaletteExport.ts:2-9 imports `"./export"`; 914-line `wc`; sole external consumer = byte-exact.test.ts. | **X-W7.b by name** (W7.md:42-43/:190-200: delete `export.ts`, cut over to `export/`, one slugify). |
| **AP-5** (D-axis P5-2) | Three scaling authorities in one panel — rem chassis (`w-48`, :7), fluid row type, and the identity pinned to an absolute `max-w-[180px]` (:9) that never moves: text-only resize enlarges the panel and shrinks the visible share of the name. WCAG 1.4.4 limb stands. | MY: :7/:9 verbatim; token arithmetic adopted (reader-1 re-derived the pass-5 ladder from `--overlay-*` tokens I verified). | **X-W7** carve (the header dies with the inspector move) — the `max-w-[180px]` deletion stands alone as the interim cure; its claimed repair of P5-3 is killed (K-5). |
| **AP-6** (P4-2/C2-14 owner limb + reader-2 missed #1 admin limb; premise amended per ruling #1) | **Both** destructive verbs are unconfirmed, un-undoable, unannounced: owner Delete (:136) → PaletteCard.vue:296 → `onDelete` (`if (palette.id == null) return; deps.deletePalette(id)` — MY read, no guard); admin Delete (:163-169) → the traced chain in ruling #1 ending in an immediate remote cross-user hard delete. The same pane confirms its bulk twin (PalettesPane.vue:101-121 Dialog — MY read). One verb, three standards of care, the eleven-per-screen instance and the cross-user instance both unguarded. | MY: full chain, both limbs. | **X-W7** (W7.md:36 confirmation · :54-57 one-owner-one-result + admin-menu strip). The corpus's "owner branch is the outlier" scoping is DEAD (K-10) — the wave must confirm both limbs or strip the admin item per :55. |
| **AP-7** (L-30/L-10/C2-1) | The menu renders on hosts that bind none of its emits: MixSourceSelector wraps the whole card in `<button type="button">` with two props and zero listeners — 17 menu verbs render and every one is swallowed at PaletteCard.vue:315-316; the wrapper also violates the card's own :2-4 nested-controls contract. | MY: MixSourceSelector.vue:246-268 verbatim; the swallow path. | **X-W7** item 4 — the props-only `PaletteSpecimen` is the named cure; the menu never mounts on non-interactive hosts again. |

### Major

| id | claim | proof | disposition |
|---|---|---|---|
| AP-8 (C2-4/L-17/D-4) | `apiOffline = availability === "unavailable"` (:217) tests one member of a four-union; `misconfigured` leaves Publish enabled while every transport call throws `DevMisconfigError` first — the file's own K-INV5 comments (:24-26, :214-215) declare the contract it fails; sibling ApiOfflineChip derives both states (MY read :36-37). | MY: :217; availability.ts:40-44 union, :188-195 throw order, :168-170 no-relabel. | **X-W7** (one-line predicate cure in the carve; the doomed-set is `{unavailable, misconfigured}`). |
| AP-9 (C2-5/C-3/L-14 + C3-5; latent per ruling #7) | `isPublic = visibility !== "private"` (:222) renders `unlisted` as public and offers the verb that destroys the middle state; the emit contract is two-valued (PaletteCard.vue:212); the wire-typed `published` (`⟺ visibility === "public"`, types.ts:43-49) has zero readers. LATENT: every shipped write path pins visibility, so the arm is currently unreachable — severity is latent, recorded here as governing text. | MY: :222; types.ts:39-49; PaletteCard.vue:212. | **X-W7** (domain split owns the visibility vocabulary). |
| AP-10 (C3-1/D-17) | The K-INV5 small-caps register has never rendered: `text-mono-caption`'s `text-transform: uppercase` consumes the lowercase glyphs before `font-variant: small-caps` (:37-39, :57-59 inline style) can synthesize; three comment blocks name a register that never painted. px measurements → U-8. | MY: the `@utility` verbatim (uppercase in-block); :37/:57 verbatim. | **X-W10** (the register spells uppercase, never synthetic small-caps — the ApiOfflineChip AP-14 companion; that row also proved the shipped Fira Code has no `smcp`). |
| AP-11 (C3-3/D-11 + reader-1 missed #1, folded — same mechanism, two surfaces) | `text-caption` contributes only `font-style: italic` where it was reached for size: (a) the Export sub-content (:112) renders italic while its parent is upright — the item recipe re-declares size, so italic is the only survivor; (b) **the corpus's blind spot**: the Versions count span (:101) takes the utility IN FULL — italic Plus Jakarta at caption size, a third type voice in a mono upright panel, violating PA:78 law 13 ("value … Fira Code" — MY verbatim); no descendant `.dropdown-menu__*` rule reaches the span (MY enumeration). All three axes tabulated :101's class string; none read it. | MY: `@utility text-caption` italic; :101/:112; rule enumeration. | **X-W10**. |
| AP-12 (C-axis P5-1) | Split-brain version predicate: the card's History chip (PaletteCardMeta.vue:26-33, no kind guard — MY read) and the menu's Versions item (:94, `!palette.isLocal &&`) gate the same field on different predicates; the separating state is manufactured by this menu's own Save (`{...palette, id: …, isLocal: true}` — MY read of usePaletteStore.ts:144-149: `versionCount`/`tier`/`slug` all survive the spread). | MY: all three anchors. | **X-W7** (one derivation per fact; the meta row is in the W7 file table). |
| AP-13 (L-22) | `paletteKind` is a derived prop — a total pure function of `palette` (utils.ts:22-31), computed by the sole call site (PaletteCard.vue:225), passed alongside its input, then bypassed by the receiver at :94 (`!palette.isLocal` = the same predicate re-derived). | MY: all anchors. | **X-W7** carve. |
| AP-14 (C-axis P5-2) | `d` focuses Delete via typeahead, Space activates it — reka's Space-vs-typeahead guard reads `contentContext.searchRef`, which is declared, reset in blur, exposed, and **never assigned** (the live buffer is a separate `refAutoReset` in useTypeahead.js); `isTypingAhead` is permanently false. Live 5/5 sweep → U-9. Zero `text-value` attributes in the file. | MY: MenuItem.js:63-64; MenuContentImpl.js searchRef = {136, 218, 251}; useTypeahead.js:6. | Consumer cure = AP-6's confirmation (X-W7). Library half = **UPSTREAM (reka-ui)** — recorded for the relay/issue queue; not a wave of ours. |
| AP-15 (P3-7/P2-7 + reader-1 missed #7, folded) | The visibility toggle has no programmatic state (state = adjacent text span, `aria-checked: null`), while the producer ships and the barrel re-exports `DropdownMenuCheckboxItem`/`Group`/`Shortcut` with zero consumers (MY census: barrel-only; RadioItem correctly used in sibling UserSortMenu.vue:22 — MY read). The fold: two in-file comments *are the defect's authors* — :42-47 pre-emptively bans the checkbox primitive by fiat; :100 cites an "exception" with no authority, exempting the span AP-11 convicts. | MY: censuses, d.ts presence, comments verbatim. | **X-W7** carve (the toggle's terminal home is the inspector) + **X-W10** (machine-readable state law). Unaccountable-policy comments die with the file. |
| AP-16 (D-7 + reader-1 missed #2, folded) | The producer's `.dropdown-menu__shortcut` (0.1em, auto-margin, 0.6 opacity — MY read) is re-implemented three ways in one file with no two recipes agreeing (:37, :57, :101), and both tracking utilities INVERT the register they claim: `tracking-wide` = 0.025em (4× reduction of `--type-tracking-caps` 0.1em) at :37/:57; **the missed site**: `tracking-wider` = 0.05em (2× reduction) + a no-op `uppercase` re-declaration at :155 — D-7's table never reached the Admin label. | MY: tokens verbatim from scheme-motion.css; all three anchors. | **X-W10**. |
| AP-17 (P3-8 + reader-1 missed #8, folded) | `w-48` pins a fixed width on variable-length user data — the only DropdownMenuContent consumer to do so — while the app minted `--menu-min-w: 11rem`/`min-w-menu` (foundation.css:118/:445 — MY read) AND the producer already ships `--overlay-min-width: 8rem` on the panel class (MY read): three width authorities, and the corpus's cure consumes the middle one. `--overlay-min-width` appears nowhere in fifteen challenge files. | MY: all three token homes. | **X-W10** (one width authority; consume or relay the producer token) + BH note. |
| AP-18 (L-26 + L-2 mechanism; globs = CSS1 L-5(a) identity, cross-booked) | The ratified import lattice (ARCHITECTURE.md:49-58, verbatim — MY read) is enforced by nothing: the boundary rules are keyed to `demo/@/**` (dead — MY `ls`) and a dead `@components` alias; the census probe **re-executes at this HEAD to the identical 28 violations** (MY run). Both readers' kills of the prescribed lint cure stand (K-6): two of four objects fail today and the block is blind to 7 of the 28. | MY: probe re-run; eslint.config.js:228-298; fence verbatim. | **X-W8** (graph law; the lint encoding must be re-authored, not the corpus's block). Dead-glob identity cross-booked to ColorSpaceSelector L-5(a). |
| AP-19 (L-27) | `SESSION_PORT_KEY` has the wrong home: exactly 5 shell modules value-import it through the palettes feature (MY census), and 5 of its 7 members are `platform/auth` re-exports (MY read of usePalettePorts.ts:5-7/:127-135). | MY: censuses. | **X-W8**. |
| AP-20 (L-29 + C-6) | Two features reach app-root boot (picker→color-picker/boot ×2 — in MY probe re-run), and pass-4's "no upward import" negative is false: usePalettePorts.ts:19 (type) + ColorPicker.vue:130 (VALUE import of an injection key, non-erasable) both exist. | MY: probe rows; :19 read. | **X-W8**. |
| AP-21 (L-19) | The demo-dogfood trust boundary is a gitignored local build not byte-equal to published 4.0.0 (the `_2` rollup-dts collision artifact in local `dist/subpaths/css.d.ts`). | MY: `git check-ignore` → `.gitignore:17`; installed 4.0.0; artifact diff adopted from reader-2. | **X-W1 rider** (deploy-truth family), else NO-WAVE-OWNER at the formation boundary. |
| AP-22 (L-20/L-5) | tsconfig.demo.json declares a root + `/parsing` + `/units` that don't exist and omits live `./css`/`./value`; the comment claims "a CLOSED 8-key set" against 7 keys. | MY: paths block read; exports = 7 keys; `ls dist/index.d.ts` missing. | **CROSS-BOOKED → ColorSpaceSelector L-14** (W-HYGIENE config parity, born-RED generator command lives there). Anti-rename; no new booking. |
| AP-23 (L-21 + reader-2 missed #3, folded) | Two slugify algorithms for one concept: store (NFKD fold) vs export.ts:9-11 (ASCII-only) — `Café Noir` → `cafe-noir` vs `caf-noir.json`; the fold: an all-non-ASCII name yields an EMPTY stem → `.json`/`.css`/`.tailwind.ts` dotfiles, `--palette--0` malformed property namespace, `""`-keyed Tailwind theme (MY recomputation from the read source); api schema imposes no charset. The correct discipline exists unshipped in `export/canonical.ts`. | MY: export.ts whole. | **X-W7.b by name** (one slugify, the Unicode-normalising one; ASCII duplicate deletes). |
| AP-24 (L-23) | Hand-rolled open model over a v-model-capable primitive (`menuOpen` prop + `updateOpen` emit vs the producer's declared `update:open`), and the guard it feeds is dead: PaletteCard.vue:317 `if (action !== "rename") menuOpen.value = false` — but `startRenaming()`'s first statement (:281) is `menuOpen.value = false`; both branches set the same value and the :291-292 comment describes behavior the code contradicts. | MY: both anchors. | **X-W7** carve. |
| AP-25 (C2-3) | Export writes attacker-controlled markup to disk: export.ts:68 `fill="${c.css}"` and :74 `${palette.name}` raw into SVG (re-serialized for PNG at :88); api schema caps length only. Downloaded-byte witness → U-10. | MY: export.ts verbatim; schema adopted. | **X-W7.b** (the certified serializers are the cure; the wave's failure-disposition table (§8) owns the rest). |
| AP-26 (P5-4) | Publish on a saved-from-remote copy sends the ORIGINAL owner's slug (the Save spread carries `slug` verbatim — MY read; `onPublish` forwards `slug: palette.slug` — MY read of usePaletteActions.ts:48-52) to a unique-indexed collection. Server leg → U-5. | MY: hops 2-4 client-side. | **X-W7.b** (one slug + surfaced export/CRUD failure, both named in the wave). |
| AP-27 (P4-2-modal + P4-1 geometry; severity limb → U-4) | Eleven per-row menus each open `modal: true` (reka default — MY read: `default: true`; `:modal` passed nowhere in demo — census adopted) with no side/align/collision offsets, over a repeated-row list whose covered controls include Delete; the enabling levers are all source-confirmed, the mis-aim frequency is not. | MY: DropdownMenuRoot.js:26-31; :2/:7. | **X-W7** (menus die with the inspector; any surviving menu decides `:modal` explicitly). |
| AP-28 (P4-3) | Two items ship the identical label `Publish` + `Globe` glyph for different operations (:33-34 mints a new remote palette; :54-55 flips visibility on an existing one). | MY: both anchors + PaletteCard.vue:295/:302. | **X-W7**. |
| AP-29 (L-11) | `isOwned` has ONE derivation site (BrowsePane.vue:99) and AdminUsersPanel omits it on remote palettes, so the admin's card menus render Report (`!isOwned`, :144) and suppress the owner set — mis-rendered gates on the moderation surface. | MY: AdminUsersPanel mount read (props: `is-admin show-slug`, no `is-owned`); :210 optional prop. | **X-W7** (:55 "strip user actions out of the Admin menu" is the named cure). |
| AP-30 (L-12) | The degraded-backend register has three homes with three domains (lamp: 4-state; chip: 2-state; this menu: 1-state). | MY: all three reads. | **CROSS-BOOKED** — lamp/chip seats live in ApiOfflineChip AP-1/AP-12 + MT-DOCK-LAYERS-1; the menu seat dies with AP-8 (X-W7). |
| AP-31 (P4-4/C2-13) | Pressing the menu trigger drives the whole card's press choreography: PaletteCard.vue:24 binds `press.handlers` at the root, :82 stops `click` but not `pointerdown`, and the producer handler set includes `onPointerdown` — the entity animates while its child control is operated, against PA law 12. PRM-safe (guard-1 excludes `scale` — MY read), so no motion-quarantine conflict. | MY: :24/:82/:263-267; a11y-overrides.css:1. | **X-W7** (law-12 seat: the card root becomes noninteractive; activation moves to the one named button). |
| AP-32 (L-3/C-5) | `action: [action: string]` (:225) erases a 17-verb vocabulary against an 18-key handler record; the dead `copyAll` key (PaletteCard.vue:294 — its only repo hit, MY re-check of the readers' grep against the read source) proves the erasure bites, and misses fail silent (:315-316). | MY: emit + record read. | **X-W7** carve (the inspector's action surface is typed by construction). |
| AP-33 (P5-5 structural half; arithmetic killed K-2) | Two incompatible annotation grammars co-render in one `ml-auto` column on any owned remote palette with >1 version: the state span (mono-caption, 0.55 opacity, :57-59) beside the count span (italic text-caption, muted ink, :101). | MY: both spans; co-render condition via utils.ts:23. | **X-W10** (one annotation grammar; AP-16's shortcut primitive is the vehicle). |
| AP-34 (reader-1 missed #3, adopted) | One palette identity, two truncation grammars 40 px apart: the card clips vertically and responsively (`line-clamp-2 sm:line-clamp-1` + `:title` recovery — MY read of PaletteCard.vue:55-57); the menu clips horizontally and unconditionally (`truncate max-w-[180px]`, no title). The mismatch is also what broke P5-3's oracle (K-1). PA §5 one-anatomy law governs. | MY: both mechanisms. | **X-W7** carve + **X-W10**. |
| AP-35 (reader-1 missed #4, adopted) | The Export submenu has no overflow policy at all: the producer gives the parent panel `max-height: var(--overlay-max-block)` + `overflow-y: auto` and gives `.dropdown-menu__sub-content` `overflow: hidden` with NO max-height — it can neither scroll nor bound itself; the consumer adds nothing (:112). Rendered consequence at the 400% arm → U-2. | MY: both producer rules extracted verbatim. | **GLASS-OWNED → BH relay** (sub-content parity with content) · consumer arm X-W7. |
| AP-36 (reader-1 missed #5, adopted) | The identity header's two headline numbers (0.707× the constitutional rung; muted ink; weight 600 defeating `font-bold`) are PRODUCER defaults of `.dropdown-menu__label` (unlayered — MY read: `color:var(--muted-foreground); font-size:var(--dropdown-text-secondary); font-weight:600`), so every consumer-side cure the D axis prescribed is inert; the true consumer defect is primitive selection — a menu-label primitive carrying an entity identity that VC:72 assigns `--type-subheading`/Fraunces (MY verbatim). | MY: rule + canon. | **X-W7** carve (the identity moves to the inspector at its constitutional rung); corrects the D-10/P5-3/P5-7 cure sections. |
| AP-37 (reader-1 missed #6, adopted per ruling #4) | The producer's own two-channel highlight collapses to background-only: `@layer components` `.glass-menu-row[data-highlighted]{…color:var(--accent-foreground)}` loses to unlayered `.dropdown-menu__item{color:inherit}` on the same element (class co-location proven by MY chunk extraction). The unexplained mechanism behind D-12's measurement, turned on the producer itself. | MY: both rules + class stack. | **GLASS-OWNED → BH relay** (one relay with AP-3: the layer-discipline defect has two victims, consumer ink and producer ink). |

### Minor / Info

| id | sev | claim | disposition |
|---|---|---|---|
| AP-38 (reader-2 missed #2) | MINOR | `cardRefs` retains unmounted card instances: `:ref="(el) => el && (cardRefs[…] = el)"` short-circuits the null unmount call in BOTH panes (BrowsePane.vue:94/:209; PalettesPane.vue:84 — MY reads); every narrowing filter and every load-more page strands full card+menu subtrees in a reactive record. | **X-W7** (both panes in the wave's file table). |
| AP-39 (reader-2 missed #4) | MINOR | `downloadExport` uses a never-appended anchor and revokes the object URL synchronously in the same task as `click()` (export.ts:121-132 — MY read) — the sole exit for all five formats, zero e2e coverage. | **X-W7.b**. |
| AP-40 (reader-2 missed #5) | INFO | A palette name containing `\n` escapes the `//` comment into executable TypeScript in the downloaded `.tailwind.ts` (export.ts:55 — MY read; api `.trim()` strips ends only). C2-3's family on a code artifact. | **X-W7.b** (dies with AP-4's cutover). |
| AP-41 (C3-6) | MINOR | demo `.fira-code` (utils.css:9-11 — MY read) is a strict subset of the producer `@utility fira-code` (drops liga/calt declaration but same family; the utility's feature-settings still apply to the shared class name) — the fork changes nothing and is dead. | **X-W10** hygiene. |
| AP-42 (L-18) | MINOR | Transport DI at leaf altitude: :216 `useApiClient()` throws without a provider — the menu cannot mount in isolation; every other input is a prop. | **X-W7** carve (the inspector reads availability at pane altitude). |
| AP-43 (L-24) | MINOR | 21-file flat feature root; `types.ts` spans four domains incl. generic HTTP envelopes; `export.ts` name-collides with `export/` by design. | **X-W7** ("Domain Split" is the wave's name). |
| AP-44 (P5-7 C-axis; INFO→MINOR per ruling #7) | MINOR | Save reports "Saved!" unconditionally while `addPublishedPalette` has two silent no-op paths (dedup move-to-front `return`; slug-guard fall-through — MY read of usePaletteStore.ts:121-150; BrowsePane.vue:226-232 checks nothing). | **X-W7** item 8 (visible-result gate). |
| AP-45 (reader-2 missed #6) | INFO | `browser/status/index.ts:3-6` documents its own migration via two module specifiers that no longer resolve (`@components/…`, `@lib/…` — MY read; aliases dead per AP-18's mechanism). | **CROSS-BOOK → ApiOfflineChip AP-11** (same file, X-W8); new fact noted there. |

## Killed-claims register

| # | killed claim | refutation (authority) |
|---|---|---|
| K-1 | P5-3's quantitative basis — "the card's name span is unclipped (0 px) at 16px AND 32px root", hence the menu header is "strictly worse in every arm measured" | The card clips VERTICALLY: `line-clamp-2 sm:line-clamp-1` (PaletteCard.vue:55 — MY read); under `-webkit-box`, `scrollWidth == clientWidth` for any length, so the probe was structurally blind; the corpus's own D-10 and pass-5 witness sentences describe the ellipsis the table denies. Qualitative core survives into AP-36. (reader-1; ruled #3) |
| K-2 | P5-5's emphasis arithmetic — "the version count is 1.8× more prominent", from "text-muted-foreground measured inert" | The cited pass-1 cell measured the Report ITEM (:145), eaten by the unlayered item rule; :101's span is a CHILD the item rule never matches (glass-ui ships zero descendant `.dropdown-menu__*` selectors — reader-1's enumeration, spot-checked by MY rule extraction); the ratio compares an alpha to a color token. Structural half = AP-33. (reader-1) |
| K-3 | D-12 as written — "hover and focus are literally the same declaration; no second channel" | The items carry `.interactive-item` (measured classList; MY class-stack extraction), and the producer ships three DISTINCT state declarations — `:hover` background, `:focus-visible` `box-shadow: var(--focus-ring-shadow)`, `:active` scale — plus a forced-colors outline (all MY reads); nothing unlayered competes for `box-shadow`. Whether `:focus-visible` matches under roving focus is a DIFFERENT question → U-3. (reader-1) |
| K-4 | pass-1 state-coverage row "active/pressed: none — no `:active` treatment anywhere; measured no transform" | `.interactive-item:active { scale: var(--scale-press-sm) }` (MY read); `scale` is not `transform` — the identical instrument blindness P4-4 later proved decisive. PRM-safe: guard-1's reduced-motion transition list excludes `scale` (MY read of a11y-overrides.css:1). (reader-1) |
| K-5 | P5-7's causal claim — deleting `max-w-[180px]` "repairs P5-2 AND P5-3" | P5-3's headline is a FONT-SIZE deficit set by the unlayered producer label rule (MY read: `font-size: var(--dropdown-text-secondary)`, weight 600); `max-width` cannot move a font size. The deletion stands for P5-2 only; the mis-scoped cure section is corrected by AP-36. (reader-1) |
| K-6 | L-26's cure adoptability — "three of four objects pass today … adoptable immediately as a ratchet" (and its "retires 8, quarantines 20") | usePalettePorts.ts:19 imports `../shell/useViewManager` in the `palettes` object's own ban set (MY read); core ESLint `no-restricted-imports` has no `allowTypeImports`; the `scenes` row contradicts its own census; the four-object block is blind to 7 of the 28 violations in MY re-executed census. Finding CONFIRMED (AP-18); the cure as written cannot detect a quarter of what it claims. (both readers, merged) |
| K-7 | C2-15's scroll limb — "the lock is a no-op and the list scrolls freely under an open menu" (and its verbatim carry in P4-2 symptom 4) | Falsified inside the corpus by a better instrument: programmatic `scrollTop` bypasses hit-testing; the real wheel-gesture re-measure recorded 400 px closed / **0 px open**. The narrowed truth (lock on `<body>`, scroller an inner div) survives in the governing C file. (reader-1) |
| K-8 | P5-1's interim cure — "the clip may not land on a row boundary" | Unimplementable (a function of viewport, root size, and conditional rows the component doesn't control) and it prescribes misaligning rows to manufacture a cue; the overflow is producer-owned (`.dropdown-menu__content` max-height+scroll — MY read), so the affordance is a glass-ui relay, not consumer arithmetic. Finding (43% hidden, unsignalled) stands. (reader-1) |
| K-9 | D-8's mechanism — "`ml-auto` is a physical margin; the annotation lands wrong in RTL" carried by pass-5's blanket "those 61 findings stand" | Retracted by the corpus's own pass-4: no ConfigProvider exists, reka's portalled content keeps `dir="ltr"` under `<html dir="rtl">`, so logical properties change nothing there; the conclusion survives as hygiene, the mechanism does not, and the blanket re-assertion must carry the amendment. (reader-1) |
| K-10 | "The admin Delete path is already confirmed" — stated three times across the C axis, incl. "the owner branch is the outlier" | FALSE; the cited PaletteCard.vue:301 is a comment; the traced chain ends in an immediate `deletePaletteAdmin` with no dialog (MY end-to-end re-trace, ruling #1). The remediation scoped on this premise ships a cross-user hard delete unguarded. → AP-6. (reader-2) |
| K-11 | C2-2 restated as killing the submenu "on pointer input" | Mouse hover opens it on every measurement in the corpus and via reka's `onPointermove` (mouse-gated — MY read); the dead paths are click/tap, fatal only where hover doesn't exist (touch). The BLOCKER stands at its true scope (AP-2). (reader-2) |
| K-12 | L-8 — "`gap-2 cursor-pointer` on 11 of 11 items = 100% = misfiled root styling; move it into the producer item root" | MY count: 17 elements, the recipe on 12; the five export items omit `gap-2` deliberately (no icons). The transposition would smear icon-gap over every iconless item in the constellation. Edict-5 residue survives in AP-10/AP-16. (reader-2; ruled #2) |
| K-13 | L-15's 234 kB root-barrel byte headline ("the chunk leaves the graph") | A Vite dev pre-bundle, deliberately un-treeshaken; the corpus's own pass-4 built both entries: 20,333 B vs 20,337 B — the subpath build is LARGER; 36 modules reach the root regardless. The finding survives on structure only, and pass-5 correctly makes no byte claim. (reader-2) |
| K-14 | pass-1's negative "menu content mount cost — PASS (items don't mount while closed)" | Right measurement, wrong axis: DOM nodes vs component instances. `MenuPortal` is an unconditional Teleport; only the inner content sits behind `Presence` (MY reads of both dist files) — the portal + wrappers instantiate per closed menu, which is what P5-3 later measured. The PASS verdict is struck; the numbers → U-7. (reader-2) |
| K-15 | pass-4's negative "feature → shell direction — no upward import" | usePalettePorts.ts:19 (type) and ColorPicker.vue:130 (VALUE import, non-erasable) both exist and both appear in MY probe re-run. A component-cone observation generalized to the tree. → AP-20. (reader-2) |

## Superlatives — provenance preserved

1. **PRODUCER** — the 44 px touch floor is free and correct: `.glass-menu-row { min-block-size: max(2rem, var(--touch-target, 2.75rem)) }` (MY read); every pass measured items at 44 px with a consumer that wrote nothing. (both readers)
2. **PRODUCER** — the accessibility-media arms are honored at the material: contrast-more blur collapse, reduced-transparency opaque surface, forced-colors Canvas/CanvasText + the `.interactive-item:focus-visible` Highlight outline (static half MY-verified in a11y-overrides.css). Pass-3's own sentence deserves the carry: "Every accessibility failure in this report is consumer-side." (reader-1)
3. **COMPONENT** — the DI seam at :214-217 is right and self-documenting (injected api-client seam, wave-cited); four seats certified it independently; the predicate ABOVE it is wrong (AP-8), the seam is why the fix is one line. (reader-1)
4. **COMPONENT** — :219-222 is a model domain comment: it explains a defaulting decision (why absent visibility on a browse row means public) instead of asserting policy. The register the file's other comments should have used. (reader-1)
5. **CORPUS** — three costly self-corrections, all in the honest direction: the dark-arm sampler retraction that made the defect worse (11→0 delta); the discarded RTL arm measured against a not-yet-existing root; the two-pass bundle argument settled by building both (and retiring its own axis's headline). (reader-1)
6. **CORPUS** — C-axis P5-2 is the best forensics in fifteen files: reads the guard, finds the never-assigned ref, then designs a delay-sweep whose FLATNESS is the proof. Re-verified independently by BOTH readers and by this seat (searchRef = three lines, MY grep). (both readers + MY confirmation)
7. **LIBRARY** — `availability.ts` `assertApiAttemptAllowed`/`markApiUnreachable` (MY read) is the model the consumers fail: discriminates `misconfigured` FIRST, throws typed, refuses to mislabel. Every AP-8-family defect is a consumer reading this module with `===` on one member. (reader-2)
8. **LIBRARY** — `getPaletteKind` (utils.ts:22-31, MY read): total, pure, dependency-free discriminator with the invariant documented inline — exactly the shape the subject file's three hand-rolled predicates each fail to take. (reader-2)
9. **LIBRARY** — `demo/palettes/export/` (914 L): the certified set is markedly better than what ships — RFC 8785, digest, and `canonical.ts`'s no-slugifier-needed discipline, whose absence produces AP-23. Its only defect is that nothing imports it. (reader-2)
10. **CORPUS INSTRUMENT** — `probe-L5-lattice.mjs`: deterministic, source-only, and it re-executed at THIS seat's HEAD to the identical 28-violation census — the one instrument in the record a hostile reader can confirm in a single command, and this seat did. (reader-2 + MY re-run)

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1** — P5-1's 400% arm is the substitution VC:62 forbids by name (MY verbatim); the height-limb argument for "optimistic bound" is accepted, but the arm is unmeasured as canon defines it across the whole mega-tranche. Needs: live routed page at actual browser zoom.
- **U-2** — Export sub-content clip at a 225 px-tall viewport: `overflow: hidden`, no max-height (structural half MY-proven from producer bytes, AP-35); no pass ever opened the submenu at the 400% arm.
- **U-3** — the D-12 residual: does `:focus-visible` match under reka's roving focus (focus moved from a keydown handler), i.e. does `--focus-ring-shadow` ever paint in this menu? One probe: `matches(':focus-visible')` + computed `box-shadow` after ArrowDown. If it fails, the defect is reka/glass focus interop — a different owner than D-12 named.
- **U-4** — P4-1's severity: the covered-trigger mis-aim frequency (geometry sound, AP-27; BLOCKER rank unsupported).
- **U-5** — P5-4's server leg (unique-slug 409 on Publish of a saved-from-remote copy); hops 1-3 proven, hop 4 inference, honestly labeled by its own seat.
- **U-6** — the `remote && isOwned` arm has never been produced by the shipped system (injected markup / stubbed feed only); the entire visibility defect set (AP-9 family) is latent, and this record is the governing statement of that fact.
- **U-7** — P5-3's instance census numbers (13 menus/row; 4932 nodes; 110.6 MB; 110 ms/keystroke at N=200); the structural half is static-proven (K-14).
- **U-8** — C3-1's 55.266/39.094 px widths (mechanism MY-proven at source).
- **U-9** — P5-2's 5/5 destruction sweep (dead guard MY-proven at source, which is why the live result is unsurprising).
- **U-10** — C2-3's actual downloaded SVG bytes (interpolation + schema MY-proven).
- **U-11** — every D-axis pixel figure not re-derived here: the 400% panel clip (43.4% hidden), the text-resize widths (319→569 px), the occlusion intervals, the 178-vs-180 px pin inertness. Chromium-cell, browser-only.

## Closing verdict

The corpus is CONFIRMED in the main and the component is terminally shaped: a constitutionally forbidden surface (AP-1) whose producer seam is sound, whose every load-bearing predicate is one comparison too narrow (AP-8/AP-9/AP-13), whose destructive verbs — owner AND admin, the corpus's own "confirmed" claim killed — are unguarded (AP-6), whose export path ships the worse of two implementations it contains (AP-4), and whose type registers assert three laws the pixels never obeyed (AP-10/AP-11/AP-16). Fifteen kills hold against the corpus, three of them repairing the corpus's own remediation scoping (K-5/K-6/K-10) — the incredulity that mattered most was aimed at the cures, not the defects. Routing is total: X-W7 owns the file and its cone by name, X-W8 the graph law, X-W10 the type canon, the BH relay the two producer-side cascade collisions (AP-3/AP-35/AP-37), reka upstream the dead guard, with two cross-books honoring anti-rename and one X-W1 rider; **zero rows route NO-WAVE-OWNER**. Eleven residue rows are the visual-audit inputs. ADJUDICATED, not VERIFIED — no wave is stamped by this record.

— end of apotheosis. The challenger corpus and both reader returns remain immutable beside this file per E-1/E-3.
