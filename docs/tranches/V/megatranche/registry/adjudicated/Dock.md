# Dock — adjudicated (M-12 tri-fold apotheosis)

**Component**: shell-dock-dock (`demo/shell/dock/` + its chrome-ink seam in `demo/color-session/`)
**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own runtime, not inherited from the spawn label (L-11).
**Workers**: worker-F (`claude-fable-5`) · worker-O (`claude-opus-5[1m]`). Both attacked before either was adopted (L-14).
**HEAD at adjudication**: `9bcd5d91` (matches worker-O's declared HEAD; re-resolve every line anchor before executing).
**DELTA r3 (2026-07-28)**: the re-run shell band's fresh corpus (six seats, 58 rows, 8 filed BLOCKER — the root's 30-row/7-BLOCKER window = seats 6+8+11 of `harvest/area-shell.json`, adjudicated here inside the full six-seat band) is adjudicated in **§6** and MERGED into this document in place. Adjudicator: worker-F, model receipt `claude-fable-5` (observed in this seat's own runtime — L-11); r3 HEAD `7775473b`; every §6 verdict re-derived against that tree with this seat's own reads/greps/dist-censuses (probe parsimony: zero fresh browser probes needed — all challenger live cells carried LABELED, every operative mechanism decided from source/dist). Prior rulings §1–§5 are PRESERVED; overturns are explicit in §6.3 only.

**Method (L-14)**: refutation-first. Every one of the seven rows was re-derived against the tree by this seat with its own instruments — no worker cell was trusted un-re-derived. The workers' six substantive disagreements were each ruled with deciding evidence (below). Where they agreed suspiciously well (the 65/23 module histogram, the plate alphas, the layers.css transition list), the agreement was itself audited: worker-O declared it wrote its own probes without reading the challenger's (addendum D), and this seat re-verified the underlying mechanisms statically, so the agreement is convergence on source truth, not echo.

**Arbiter's own instruments this session (all receipts mine)**: full read of `useDockAdminMode.ts` (all 77 lines) / `useContrastSafeColor.ts:140-230` / `ActionBarLayer.vue:40-99` / `ColorInput.vue:60-88` / `usePalettePorts.ts:43-110` + the `sessionPort` literal (:127-135) + Port-4/5 + provides (:242-246) + type derivation (:262-271) / `ProfileSection.vue` pill blocks (:54-70, :108-143) + chrome consumer (:15/:28-29) / `Dock.vue:143-144/:154/:277`; **selector census over the installed glass-ui 7.0.0 dist dock CSS** (shell.css: `.glass-dock` gets box-shadow/layout, NO background; morph.css/dock.css: every dock-surface background rule paints `.dock-plate`; layers.css: `.dock-layer { transition: visibility 0s linear var(--duration-normal) }` — the ONLY transition; `.is-leaving { opacity: 0; visibility: visible; pointer-events: none }` + `:not(.is-active) { position: absolute; inset: 0 }`; the only opacity ramps live behind `.dock-crossfade[data-crossfading] > .dock-face` and `[data-morphing]` contexts); **dist export census**: `grep -rl 'DockIconButton\|DockSelectTrigger\|DockDropdownTrigger' node_modules/@mkbabb/glass-ui/dist/` → **ZERO files**; `dock/index.d.ts` exports `DockControl` + `DockTrigger` (+ `DockCrossfade`, whose `.vue.d.ts` I read: `active: string`, `reserve?: 'block'|'inline'`, spring-driven opacity overlap, measure-once peak reserve, focus transfer); **my own TypeScript-resolver run** (`ts.resolveModuleName`, moduleResolution Bundler, paths ABSENT): bare `@mkbabb/value.js` → UNRESOLVED, `/css` `/value` `/color` → LOCAL `dist/subpaths/*.d.ts` via package self-reference; `tsconfig.demo.json:35-49` (8 hand-written keys) + `package.json#exports` (7 keys, no `.`) + `ls dist/` (no index.*; subpaths = color/css/easing/math/quantize/transform/value only); greps: `SESSION_PORT_KEY` (5 dock value-import sites + definition, exact), `pm\.` member usage across the five dock files (isAdminAuthenticated/userSlug/userLogout/onRegenerateSlug/onSlugSwitch), `providePalettePorts` boot call (usePaletteWiring.ts:60), router guards (`beforeEach|beforeEnter` → 0 in demo), `/atmosphere` `/blob` plain routes (router index.ts:29-30), `<h1` in demo/**/*.vue → 0 files, palettes→shell runtime edges → 0, `group` in viewSchema.ts → 0 (PaneConfig :69, VIEW_MAP :104); registry cross-reads: App.md:28 (D-4 visible-H1 canon-hard, sr-only struck) /:127 (G6 route-H1) /:146 (**MT-DOCK-LAYERS-1** minted, void-opts gate), ColorInput.md:45 (D-3) /:55+:144 (**the D-13 verbatim-carry hedge naming MT-DOCK-LAYERS-1**) /:92/:98 (G5/G11), ColorSpaceSelector.md:25/:47 (L-14 → W-HYGIENE config parity), ROOT-FINDINGS.md:1242 (the stale primitive names); artifacts on disk: `audit/probes/owner-marked-shadows.mjs` + `audit/visual/owner-marked/OM-1-dock-item-shadows.png` both present. **Zero browser probes needed** — every verdict decided from source, dist, git, the registry, and the compiler; both workers' live chromium cells are carried labeled where cited.

---

## 1 · Final verdicts

**5 CONFIRMED (L-18, L-19, L-20, L-6, L-7) · 2 RESCOPED (L-21, L-22) · 0 REFUTED · 0 UNVERIFIABLE-HERE · 0 GLASS-OWNED rows** (two glass-relayed ASKS ride the standing BH fond; no corpus row is glass-owned).

**r3 DELTA tallies (§6, 58 fresh rows): 45 CONFIRMED · 12 RESCOPED · 1 REFUTED (D3-07) · 0 UNVERIFIABLE-HERE · 0 GLASS-OWNED.** Of the 8 filed BLOCKERs, 7 CONFIRMED (L-30 · D2-01 · D2-02 · D2-03 · L-35 · L-1-rep · D3-01) and 1 RESCOPED-TRUE (L-23-rep → W-HYGIENE with a G-G rider here). Merged document totals: 66 rows adjudicated → 50 CONFIRMED · 14 RESCOPED · 1 REFUTED · 1 replication-accretion pair counted once each (L-6-rep, L-30b).

| Row | worker-F | worker-O | **Final** | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L-18** chrome ink probe dead | CONFIRMED | CONFIRMED | **CONFIRMED — BUILD here** | `useContrastSafeColor.ts:151-153` reads `getComputedStyle(document.querySelector('.glass-dock')).backgroundColor`; :215-217 returns `undefined` on alpha 0. My dist census: NO rule anywhere in glass-ui 7.0.0 or demo CSS paints `.glass-dock`'s own background — every dock-surface rule paints `.dock-plate` (morph.css `.glass-dock:not(.vertical) .dock-plate { background: color-mix(…) }`, `.vertical`, `[data-held]`, `.collapsed:hover` variants — all `.dock-plate`). An unstyled element's computed backgroundColor is `rgba(0,0,0,0)` by spec — statically certain. So `resolveLiveTint('chrome')` is undefined on EVERY call and the static arm serves (ink.ts:118-122 fallthrough `case "floating": case "chrome"`). Both workers' independent live cells agree (plate α 0.5392 light / 0.5776 dark; O's model-vs-truth deltas +0.0816..+0.2250 light / −0.0685..−0.2348 dark — opposite signs per scheme), carried labeled. Sole consumer `ProfileSection.vue:28` `useSafeAccentFn("chrome")` → `triggerInk` = colour AND borderColor of the Profile/Login pill. The e2e census (o18-contrast-census.spec.ts) never asserts the probe returned anything — blind by construction. Reproduction: load `/#/`, evaluate `getComputedStyle(document.querySelector('.glass-dock')).backgroundColor` → `rgba(0, 0, 0, 0)`, both schemes. Silent since the W44 Glass 7 adoption. |
| **L-19** admin gold latch | CONFIRMED | CONFIRMED | **CONFIRMED — BUILD here; O's mechanism narrowing adopted** | `useDockAdminMode.ts:32` seeds `ref(currentView.startsWith("admin-"))`; :55-59 is the only maintainer — `if (adminViews.includes(view)) isAdminMode.value = true` with **no falsifying arm** (my read of all 77 lines: the only clearing writers are the toggle :41-48 and the logout watch :62-64). `atmosphere`/`blob` are in `adminViews` (:27) but not prefix-matched — latent predicate disagreement. Router: `/atmosphere` `/blob` plain routes (index.ts:29-30), **zero** `beforeEach`/`beforeEnter` anywhere in demo (my grep) — any anonymous user typing `/#/atmosphere` latches gold (`Dock.vue:277` `gold-shimmer-icon` on the seal; `DockViewSelect.vue:70/:80/:83` gold ring + loss of the per-view accent) for the session. **Mechanism ruling (O wins)**: the missing falsifying arm ALONE is operative — O's cold-deep-link cell (fresh context → `/#/atmosphere` still golds, because the seed evaluates on the `picker` fallback before route resolution and the watch then fires) shows the seed/watch predicate split is latent, not causal. Verdict unweakened; the cure (derivation) kills both. AUTH-GATED SUB-ARM (`viewEntries` :34-39 collapsing to the seven admin views on a user route) — **UNVERIFIABLE-HERE** (needs `isAdminAuthenticated` true; dev :9000 is API-LESS): certified by SOURCE reading only, asserted at vitest level with the flag stubbed, labeled. Reproduction: `/#/` → `/#/atmosphere` → back() → gold still on `/#/` (O's five-step cell [false,true,TRUE,TRUE,TRUE], adopted labeled; mechanism statically certain). |
| **L-20** crossfade shim is a one-frame cut | CONFIRMED | RESCOPED (fold to MT-DOCK-LAYERS-1; harm clause struck) | **CONFIRMED — BUILD here (the wave IS MT-DOCK-LAYERS-1, so the two dispositions converge); O's harm-clause strike ADOPTED** | `ActionBarLayer.vue:53-82`: local `useLayerTransition`, `SUB_LAYER_CROSSFADE_MS = 260`, `void opts.containerEl; // signature parity with the retired producer composable` at :67, `subLayerProps` hand-binding `.dock-layer`/`is-active`/`is-leaving` (:88-94). My layers.css read: `.dock-layer` transitions ONLY `visibility 0s linear var(--duration-normal)` — no opacity transition exists outside `[data-crossfading] > .dock-face` (DockCrossfade-only) and `[data-morphing]` (dock-morph-only) — so `is-leaving`'s `opacity: 0` lands in one frame and the 260 ms timer times a window in which nothing animates. Both workers' phase samples agree (transitionProperty `visibility`/0s at every phase), labeled. **HARM CLAUSE STRUCK (O wins, source-verified)**: "a transparent inert box held over the live controls" overstates — layers.css gives `is-leaving` `pointer-events: none` and `:not(.is-active)` `position: absolute; inset: 0`, plus the template's `inert` (:92): it occludes nothing; the injury is motion truth + a ~40-line dead fork of a retired producer concept. Successor real: `dist/components/dock/index.d.ts:5` exports `DockCrossfade`; its contract (my read) is `active: string` + `reserve?: 'block'\|'inline'` (inline = peak-WIDTH reserve — exactly this horizontal control run) + focus transfer. Identity: this row IS the standing **MT-DOCK-LAYERS-1** core (App.md:39 L-14 rescoped the subject here; the :146 void-opts gate RED today at :67 — my grep). |
| **L-21** no document H1 | RESCOPED → MT-APP-1 | RESCOPED → MT-APP-1 | **RESCOPED → MT-APP-1 (App-adj D-4 H1 arm) — unanimous, arbiter-verified** | Fact re-verified: `grep -rl '<h1' demo/**/*.vue` → **0 files** (my run). Already CONFIRMED-AND-BUILT at App.md:28/:81 (visible-H1 canon-hard §4.1/§5.1) with gate G6 (App.md:127: all fourteen routes, `h1===1 && main===1 && h1.text===VIEW_MAP[view].label`, VISIBLE — RED today). The filing's own cure text concedes the fix cannot land in Dock.vue (the dock is the `<nav>` landmark). **The filing's `sr-only` cure clause stands STRUCK** (App.md:28 already ruled visible-H1; uncitable by any close). Not re-booked (L-5 anti-rename); this corpus is a third independent confirmation accreting to the booked row. The Dock wave carries NO obligation here beyond not claiming it. |
| **L-22** tsconfig paths drift | RESCOPED → W-HYGIENE (generator-parity cure) | RESCOPED → W-HYGIENE (cure superseded to DELETE — all 8 entries proven inert) | **RESCOPED → W-HYGIENE config parity (ColorSpaceSelector-adj L-14 identity) — cure shape: O wins, DELETE** | Facts re-measured by me: tsconfig.demo.json:35-49 hand-writes 8 `@mkbabb/value.js*` keys; bare → `./dist/index.d.ts` (**no such file** — my ls); `/parsing` + `/units` declared with no export and no dist target; `/value` + `/css` shipped+exported but omitted; exports = exactly 7 keys, no `.`; the block's "CLOSED 8-key set" comment false by one. **Deciding run (mine, not inherited)**: `ts.resolveModuleName` under Bundler resolution with the paths ABSENT — bare `@mkbabb/value.js` → **UNRESOLVED** (refutes the filing's "second instance" consequence AND O's own first hypothesis of binding the installed 4.0.0); `/css` `/value` `/color` → **LOCAL `dist/subpaths/*.d.ts` via package self-reference**. All eight entries are INERT; the right cure is DELETE the block (L-8 — remove the second declaration and drift becomes unrepresentable), superseding the generate-parity shape ColorSpaceSelector.md:47 booked. The supersession evidence travels to W-HYGIENE as an addendum (booking unchanged, id preserved — this is that identity's FOURTH+FIFTH independent confirmation). INFO severity stands: no live break. No Dock locus; nothing enters this wave. |
| **L-6** shell imports its identity from a feature | CONFIRMED | CONFIRMED | **CONFIRMED — BUILD here; O's two sub-claim refutations adopted; F's cure-detail shape wins** | My grep: `SESSION_PORT_KEY` value-imported by EXACTLY the five dock files (Dock.vue:18/:37, DockViewSelect.vue:8/:35, SlugEditLayer.vue:5/:8, ProfileSection.vue:14/:42, MobileMenuDropdown.vue:13/:33); defined+provided in `demo/palettes/usePalettePorts.ts` (:271/:242), whose top-level pulls the whole palettes feature graph (usePaletteStore, useBrowsePalettes, useAdminUsers, useColorNameQueue, useSlugMigration, usePaletteActions, audit/flagged/tags/versions/tagEdit — my read of :43-110). The 65-module/23-palettes histogram is both workers' independently-convergent cell (O wrote its own walker), carried labeled. **REFUTED sub-claims (O, arbiter-verified)**: (a) no palettes↔shell CYCLE — palettes→shell runtime edges = **0** (my grep; the one hit is `import type { ViewId }`, erased); (b) the bundle framing — `providePalettePorts` is called from the boot path (usePaletteWiring.ts:60, my grep), so the cost is OWNERSHIP and mountability/testability, never bytes. No close may cite either refuted mechanism. **Cure-detail ruling (F wins)**: the sessionPort literal (:127-135, my read) = `{ isAdminAuthenticated, userSlug, userLogout, ensureUser, ensureSession, onRegenerateSlug: migration.onRegenerateSlug, onSlugSwitch: migration.onSlugSwitch }` — the last two are slug-migration ACTIONS the dock actually calls (SlugEditLayer:54/:107, ProfileSection:87, MobileMenuDropdown:61 — my grep), so O's "pure identity payload assembled in platform/auth" under-specifies and would strand three consumers or drag slug-migration into platform. The platform module owns the KEY + the port TYPE; the composition root injects the implementation; the dock depends on platform only. |
| **L-7** nameless send button | CONFIRMED (fold) | CONFIRMED (fold) | **CONFIRMED — FOLD → MT-COLORINPUT-1 (D-3 ≡ MT-F005 ≡ L-7, ONE identity), id preserved; this wave asserts nothing about it** | `ColorInput.vue:67-82` re-read: both branches `<button class="send-btn btn-interactive">`, SVG-only child, no aria-label/text/title/type (my read). Contrast: every `DockControl` use in the dock subtree carries aria-label (Dock.vue:143/:144/:154 — my read) and ActionButton binds `:aria-label="title"`. Already BUILT at ColorInput.md:45 (D-3) with gates G5 (:92) + G11(a) (:98, nameless-on-`/#/` = 0, today 1). Both workers' censuses stand as third+fourth confirmations of the 1-nameless-button cell. A second cure site here would manufacture the dual path — not built here. Doctrinal consistency: the primitive-adoption doctrine curing it there is the SAME doctrine as G-H's pill cure here; the two waves pull one direction. |

## 2 · Ruled disagreements (the tri-fold's substance)

| # | Subject | worker-F | worker-O | **Ruling** | Deciding evidence |
|---|---|---|---|---|---|
| R-1 | **Wave identity** | ONE wave: extend the standing MT-DOCK-LAYERS-1 to the full dock corpus | TWO waves: mint MT-DOCK-1; hand the crossfade gate to a separately-standing MT-DOCK-LAYERS-1 | **F wins — ONE wave, the standing name MT-DOCK-LAYERS-1, scope extended** | (a) L-5 anti-rename: MT-DOCK-LAYERS-1 is already minted at App.md:146 against this exact component tree; a second dock wave name overlapping it manufactures the dual path. (b) **Decisive**: ColorInput.md:55 + :144 hedge names MT-DOCK-LAYERS-1 as the mandatory verbatim carrier of D-13's 390×844 reachability gate — worker-O's return never mentions D-13 at all, so under O's plan the hedge silently fires and the row bounces back to ColorInput unhandled; F's G-F discharges it explicitly. (c) O's own L-20 hedge ("if MT-DOCK-LAYERS-1 does not carry it verbatim, L-20 returns") resolves trivially when the wave IS MT-DOCK-LAYERS-1. O's scope-creep concern is real and preserved in DISSENT. |
| R-2 | **MT-F026 primitive names** | `DockIconButton`/`DockSelectTrigger`/`DockDropdownTrigger` | `DockControl`/`DockTrigger` (old names are CSS classes, not exports) | **O wins — F's wave text named nonexistent components** | My dist grep: `DockIconButton\|DockSelectTrigger\|DockDropdownTrigger` → **ZERO files** in glass-ui 7.0.0 dist; `dock/index.d.ts` exports `DockControl` + `DockTrigger`; the old names survive only as emitted CSS classes (`.dock-icon-button`, `.dock-select-trigger`, `.dock-dropdown-trigger` — I saw them in material.css/controls CSS). ROOT-FINDINGS.md:1242 and the memory note `glass_ui_dock_components.md` are STALE (the second near-false-cure from memory-inherited names this formation, cf. MT-F007/MT-F017). MT-F026's substance and gate are untouched; only the cure sentence's names correct. |
| R-3 | **L-22 cure shape** | Generate the paths from exports (generator-parity) | DELETE the block — all 8 entries proven inert | **O wins** | My own resolver run (paths absent): bare → UNRESOLVED; exported subpaths → local dist via self-reference. Deletion makes the drift unrepresentable (L-8); generation would maintain a mirror nothing needs. Evidence handed to W-HYGIENE; booking and id unchanged. |
| R-4 | **L-20 harm clause** | "260ms timer holds a transparent inert box" (over-read as occlusion) | Struck: the leaving face is `pointer-events: none` + `position: absolute` + `inert` — occludes nothing | **O wins — struck** | layers.css (my read): `.dock-layer.is-leaving { opacity: 0; visibility: visible; pointer-events: none }`; `:not(.is-active) { position: absolute; inset: 0 }`; template `inert` at ActionBarLayer.vue:92. The verdict stands at full strength on motion truth + dead code; no close may cite occlusion harm. |
| R-5 | **L-19 operative mechanism** | Two disagreeing predicates (seed vs watch) | The missing falsifying arm alone; the predicate split is latent (cold deep-link still golds) | **O wins on mechanism; verdict identical** | :55-59 has no else — sufficient alone; O's cold-deep-link cell shows gold appears even when the seed's predicate never fires true (route resolves after seed). The derivation cure kills both. A close citing "two predicates" as the cause would be citing a non-operative mechanism. |
| R-6 | **L-6 cure detail** | Platform owns KEY + port TYPE; composition root injects the implementation | Move the identity-payload assembly itself into `demo/platform/auth/session.ts` | **F wins** | The port is NOT pure identity: `onRegenerateSlug`/`onSlugSwitch` are slug-migration actions (sessionPort literal :127-135, consumer greps — mine). O's shape strands three dock consumers or drags a palettes concern into platform. Injection keeps the boundary honest and the payload byte-identical. |
| R-7 | **L-18 cure architecture** (recorded — the workers' proposals differ though verdicts agree) | Re-point the query at `.dock-plate` + loud dev alpha-0 assert + BANK-D1 | `paintedTintOf(root)` paint-order walk shared with the veil branch + TYPE-required tint for chrome (static rung removed) | **O's architecture wins, F's loud-fault survives inside it** | O's shape makes the silent-fallback CLASS unrepresentable (no chrome rung to fall back to — a compile error at the call site, a loud typed fault at runtime), and generalizes the walk the veil branch already wrote (:170-206, my read) instead of adding a second class-name literal that the next producer paint change re-breaks (G-E3 guards this). F's dissent (b) conceded its own re-point was "another line of the same wrong kind". BANK-D1 identical in both — kept. The root query pins `nav .glass-dock` (the shell dock), which structurally moots the two-instance ordering hazard — F's BANK-D2 RETIRED on that ground (see carries). |

---

## 3 · THE WAVE — MT-DOCK-LAYERS-1 (the standing name of App.md:146, scope extended by the r2 Dock corpus; born-RED) — **r3 NOTE: this section is EXTENDED, not replaced, by §6.5 (structure (f)-(j), gates G-J..G-Q, amendments to G-B/G-C/G-G/G-I, banks D4/D5). §6.5's G-I seam supersedes the MT-APP-1-first text below (R-8). Read §3 and §6.5 as ONE wave.**

**Title**: The dock shell tells the truth — DockCrossfade adoption, admin identity derived not latched, a chrome referent that reads the surface that paints, the session port re-homed behind a platform key, chrome-tier pill primitives.

**Structure (L-8 — three defect classes made unrepresentable; gates only where structure cannot reach)**:
(a) **The silent-fallback class dies by TYPE**: `resolveSurfaceLightness` requires a `tint: SurfaceTint` for `surface: "chrome"` (the `case "chrome"` arm leaves the ink.ts:118-122 static fallthrough); "the live probe broke and the static model quietly served" becomes unspellable — compile error at the one call site, loud typed fault at runtime. The DOM read collapses to ONE `paintedTintOf(root)` paint-order walk (root-then-descendants, first alpha>0 layer composited) shared by the chrome AND veil branches — one instrument, no per-surface class-name literal; the chrome root pins `nav .glass-dock` (the shell dock, disambiguated from ConfigSliderPane.vue:164's second instance by structure, not document order).
(b) **The latch class dies by DERIVATION**: `PaneConfig` (viewSchema.ts:69) gains a REQUIRED `group: "user" | "admin"`; all fourteen VIEW_MAP rows must declare it (today all fourteen omit it — the introduction is compiler-RED); `atmosphere`/`blob` declare `admin` (preserving the S.W5-12 intended membership). `useDockAdminMode` reduces to `computed(() => VIEW_MAP[currentView.value].group === "admin")` + `viewEntries` filtering VIEW_MAP by group; the ref (:32), the maintaining watch (:55-59), the logout watch (:62-64, dies with the ref it cleared), and BOTH hardcoded ViewId arrays (:26-27) die. `toggleAdminMode` keeps its real job (it navigates) — the same transposition useViewManager already applied to mobilePaneIndex.
(c) **The dependency inversion dies by MODULE BOUNDARY**: NEW `demo/platform/auth/session.ts` owns `SESSION_PORT_KEY` + the `SessionPort` TYPE (`{ isAdminAuthenticated, userSlug, userLogout, ensureUser, ensureSession, onRegenerateSlug, onSlugSwitch }` — byte-identical member surface); the composition root (usePaletteWiring's providePalettePorts call path) INJECTS the implementation, since `onRegenerateSlug`/`onSlugSwitch` are slug-migration actions that stay palettes-owned (R-6); `usePalettePorts.ts` consumes the key like any consumer; the five dock files re-point to `platform/auth`. No compatibility re-export (no-backwards-compat law).
(d) **The crossfade cannot be styled out of the producer's private stylesheet because the producer component owns it**: ActionBarLayer's local `useLayerTransition` + 260 ms timer + `subLayerProps` + hand-bound producer classes (~40 lines, :53-94) are DELETED; `<DockCrossfade :active="activeSubLayer" reserve="inline">` wraps the two `DockLayer` faces (spring overlap, peak-width reserve, focus transfer arrive instead of being approximated).
(e) **Pills**: ProfileSection's hand-rolled pills (Login :111-120, authed Profile :54-69, @mbabb :129-142) route through the producer's REAL exports — `DockControl` (tab shape) for the standalone pills, `DockTrigger for="dropdown"` inside the two DropdownMenuTriggers (MT-F026 cure #1, names corrected per R-2; available today — DockControl/DockTrigger measure zero cast layers on the live tree, O's cell). The in-chrome capsule elevation-variant ask stays GLASS-RELAYED (standing BH/BI fond); NO demo box-shadow patch may satisfy the gate (masking-fallback ban).

**Scope (exact files)**: `demo/shell/dock/layers/ActionBarLayer.vue` · `demo/shell/dock/composables/useDockAdminMode.ts` · `demo/shell/viewSchema.ts` · `demo/color-session/useContrastSafeColor.ts` (:148-221 — `paintedTintOf` unification) · `demo/color-session/ink.ts` (:118-122 — chrome arm out of the static ladder; signature narrowed) · NEW `demo/platform/auth/session.ts` · `demo/palettes/usePalettePorts.ts` · the five dock consumer files · `demo/shell/dock/menus/ProfileSection.vue` · NEW `test/dock-admin-mode.test.ts`. READ-ONLY trees untouched; `vnext/` Codex-owned; execution awaits the formation's begin-word.

### Gates (every one RED today; failing input named; gate scripts + witnesses enter git in the wave's FIRST commit — a witness with no git object does not exist)

- **G-A (carried VERBATIM from App.md:146 — the wave's original gate)**: `test "$(grep -c 'void opts\.' demo/shell/dock/layers/ActionBarLayer.vue)" = 0` — RED: line 67 exists (arbiter re-verified).
- **G-B (crossfade — structure + rendered motion)**: `test "$(grep -c '<DockCrossfade' demo/shell/dock/layers/ActionBarLayer.vue)" -ge 1 && test "$(grep -cE 'useLayerTransition|SUB_LAYER_CROSSFADE_MS' demo/shell/dock/layers/ActionBarLayer.vue)" = 0 && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/sublayer-crossfade.mjs` (chromium :9000, `/#/`, drive `[aria-label="Toggle action bar"]` → `[aria-label="Open color input"]`; assert a non-zero opacity transition-duration (or `[data-crossfading]` ancestor) on both faces at +60 ms AND no frame with both faces at opacity extremes). RED: 0 template uses, 5 shim hits, and layers.css transitions ONLY visibility — the one-frame step is structural (both workers' phase cells: transitionProperty `visibility`, 0s, all phases).
- **G-C (latch — structure)**: `test "$(grep -c 'const isAdminMode = ref(' demo/shell/dock/composables/useDockAdminMode.ts)" = 0 && test "$(grep -cE 'const (userViews|adminViews)' demo/shell/dock/composables/useDockAdminMode.ts)" = 0 && npx vue-tsc -p tsconfig.demo.json --noEmit && npx vitest run test/dock-admin-mode.test.ts` — RED: seed at :32 (1), arrays at :26-27 (2); the vitest spec (authored in-wave, INVERTED from the latch: switchView('atmosphere') → switchView('picker') ⇒ `isAdminMode.value === false`; authed arm with `isAdminAuthenticated` stubbed true on 'palettes' ⇒ the seven USER views listed, labeled API-LESS) fails today — :55-59 has no falsifying arm. tsc goes RED the instant `group` is required while any VIEW_MAP row omits it (today: all fourteen).
- **G-D (latch — product)**: `node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/admin-latch.mjs` — chromium 1280×900, dev :9000; drive `/#/` → `#/atmosphere` → back() → `#/palettes` → `#/`; assert gold (`.gold-shimmer-icon` on `.view-select-trigger svg` / `--dock-ring` resolving gold) ABSENT on every non-admin step AND PRESENT on a cold deep-link to `/#/atmosphere` (membership preserved, only the latch dies). RED: O's measured five-step cell [false, true, **TRUE, TRUE, TRUE**].
- **G-E (chrome — structure)**: `test "$(grep -c 'case \"chrome\"' demo/color-session/ink.ts)" = 0 && test "$(grep -c 'querySelector<HTMLElement>(\".glass-dock\")' demo/color-session/useContrastSafeColor.ts)" = 0 && npx vue-tsc -p tsconfig.demo.json --noEmit` — RED: ink.ts:119 fallthrough exists (1); useContrastSafeColor.ts:152 literal exists (1); tsc RED once the tint parameter is required while any call site omits it.
- **G-F (chrome — product)**: `node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/chrome-tint.mjs` — chromium, dev :9000, `/#/`, dock expanded, BOTH schemes (the referent error inverts sign between them — a one-scheme witness reads half the defect as absent); assert the resolved chrome tint has alpha > 0 AND |referentL − measuredBandL| ≤ 0.02 OKLab-L (band composited from the dock's own painted layer). RED: `.glass-dock` computes `rgba(0,0,0,0)` both schemes (statically certain — zero paint rules target it), `resolveLiveTint('chrome')` undefined every call; O's deltas at ambient 0.75: light +0.1207, dark −0.1895.
- **G-G (shell edge — source + graph)**: `test "$(grep -rnE "from ['\"](\.\./)+palettes/" demo/shell/ | grep -v 'import type' | wc -l | tr -d ' ')" = 0 && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/dock-graph.mjs demo/shell/dock/Dock.vue` (the walker excludes `import type`/all-type clauses as erased; exits 1 unless hist['demo/palettes'] === 0; header states honestly: this measures OWNERSHIP and mountability, never bytes — providePalettePorts is boot-path regardless). RED: 5 value-import files today (arbiter-verified list); both workers' independent walks: 65 modules, demo/palettes = 23.
- **G-H (pills — MT-F026's own gate, verbatim, not re-minted)**: `node docs/tranches/V/megatranche/audit/probes/owner-marked-shadows.mjs` (the ROOT's existing probe, on disk — arbiter-verified; 1440×900, `/#/`, dock hovered): every focusable control inside `.glass-dock` has ZERO non-inset box-shadow layers. RED: Login + @mbabb pills at 2 cast layers each (`0px 8px 24px` + ring) over a plate that casts none (O's independent cell agreeing with the root's OM-1). The authed Profile pill shares the class chain statically — **UNVERIFIABLE-HERE** live (needs userSlug → API), cured by construction with its siblings, labeled. GREEN only via producer primitives — no demo box-shadow override (masking-fallback ban).
- **G-I (mobile reachability — ColorInput-adj D-13 hedge, carried VERBATIM per ColorInput.md:55/:144)**: `node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/dock-mobile-colorentry.mjs` — chromium 390×844 (isMobile+hasTouch), `/#/`: a free-text colour entry is reachable. RED: the mobile Dock receives `:action-bar` null (App.vue's desktop-only `onDesktopLeftMount` is the sole colorPickerRef writer) so the action-bar layer never mounts ColorInput. SEAM STATED HONESTLY: the null-ref CURE lands in MT-APP-1 (bindPane on all three PaneSlot sites); this gate is the dock-side witness the hedge demanded — if MT-APP-1 lands first it turns GREEN and stands as the regression guard; if neither lands, D-13 returns to ColorInput per the hedge.

### π-obligations
Chromium cells: 1440×900 (G-H), 1280×900 (G-B/G-D/G-F), 390×844 (G-I only). Routes: `http://localhost:9000/#/` hash-only (MT-F006), plus `/#/atmosphere` (G-D trace) — never path URLs. Both colour schemes for G-F. Selectors pinned: `nav .glass-dock` · `.glass-dock .dock-plate` · `.dock-layer-grid > *` / the two `.dock-layers` children · `[aria-label="Toggle action bar"]` · `[aria-label="Open color input"]` · `.view-select-trigger svg` · `.dock-seal` `.gold-shimmer-icon` · `[data-o18="profile-trigger"]` · the Login/@mbabb pills. Committed captures force-added past `.gitignore` `*.png` in the wave's FIRST commit into `docs/tranches/V/megatranche/audit/components/shell-dock-dock/witness/`: before/after pairs {crossfade-60ms, admin-latch-return, chrome-ink-light, chrome-ink-dark, pill-elevation-1440} + each gate's machine-readable oracle output; the existing `audit/visual/owner-marked/OM-1-dock-item-shadows.png` is ADOPTED as G-H's BEFORE frame. ENV (L-12): dev :9000 is API-LESS — the ONE data-coupled arm (authed viewEntries) is vitest-stubbed and labeled; playwright-webkit and safari-app are SEPARATE cells (I-20), nothing here claims Safari; the dock-morph SIGABRT arm (`--dock-expand-t` under morph) belongs to the Phase-D safari-real matrix, not this wave; the dev server is structurally blind to bundle truth — no perf/bundle claim is made.

### Delta obligations
(1) Crossfade: leaving-face opacity 1→0 in one frame (transitionProperty `visibility`/0s) → mid-swap overlap strictly in (0,1) under the producer spring, same gesture. (2) Admin ink: five-step trace [false,true,TRUE,TRUE,TRUE] → [false,true,false,false,false]; cold deep-link `/#/atmosphere` stays true; authed vitest arm: user views listed on 'palettes'. (3) Chrome referent: `resolveLiveTint('chrome')` undefined every call → defined tint within 0.02 OKLab-L of the band, both schemes; the ProfileSection trigger's rendered ratio recorded on both sides (the claim is CORRECTNESS of the referent, not a bigger number). (4) Dependency: shell→palettes value-import files 5 → 0; Dock.vue graph demo/palettes 23 → 0 (total 65 → ~42). (5) Pills: 2 controls × 2 cast layers → 0, all controls, before/after against OM-1. (6) Schema: VIEW_MAP rows without a partition 14 → 0; hardcoded ViewId arrays 2 → 0. (7) Mobile: no reachable free-text entry at 390×844 → reachable (or D-13 returns per the hedge).

### Carries (every inherited row leaves with exactly ONE disposition; original ids for life — L-5)
- **BUILD here**: L-18 (G-E/G-F + the BH-relayed token ask), L-19 (G-C/G-D), L-20 (G-A/G-B — THE original MT-DOCK-LAYERS-1 core), L-6 (G-G, strictly the port move), MT-F026 adoption half (G-H; owner-marked, root-established, not re-litigated; names corrected per R-2), D-13's hedge gate (G-I, carried verbatim — the ColorInput.md:144 demand discharged).
- **FOLD, identity preserved, NOT re-booked**: L-7 → MT-COLORINPUT-1 (D-3/G5/G11a own the cure) · L-21 → MT-APP-1 (D-4 H1 arm, G6; sr-only clause struck and uncitable) · L-22 → W-HYGIENE config parity (ColorSpaceSelector-adj L-14 identity; **cure superseded to DELETE** — the arbiter's resolver proof travels with it).
- **RETIRED**: L-18's document-order sub-hypothesis (F's BANK-D2) — the cure pins `nav .glass-dock` structurally, and both instances measure alpha 0 today anyway (O's cell), so no ordering-sensitive read survives the cure to protect. STRUCK AND UNCITABLE by any close: L-20's occlusion-harm clause (R-4); L-19's "two disagreeing predicates" as operative mechanism (R-5); L-6's "cycle" and bundle-bytes framings; L-22's "second instance" consequence; L-21's sr-only cure.
- **BANKS (L-4 — runnable, swept at every wave open)**: **BANK-D1** (chrome tint token): `grep -rq 'glass-tint-chrome\|useDockSurfaceTint\|surfaceTint' node_modules/@mkbabb/glass-ui/dist/` — silent today (both workers checked; DockContext carries no tint); the day it hits, `paintedTintOf`'s chrome use is DELETED for a token read (the ask rides the standing BH/BI relay — an ask, never a local patch). **BANK-D3** (glass major): `node -e "process.exit(require('./node_modules/@mkbabb/glass-ui/package.json').version.startsWith('7.')?1:0)"` exits 1 today; on Glass 8 re-derive the plate recipe, the DockCrossfade contract, and the capsule elevation; the spectrum prefix seam stays GLASS-OWNED (8.0.0), never re-booked here.

### Completable alone
**YES (L-1)**: were this the only wave ever executed — the latch state stops existing (uncurable by any other wave), the layer swap becomes a real crossfade using a component already installed (no producer dependency), the chrome referent's silent-serving class dies by type and the certification walks the surface that paints, the shell stops importing 23 palette modules to render a Login button, and the owner-marked pill shadows die via primitives available today. No sibling dependency: G-I is a carried hedge that MT-APP-1 may green first (then it is the regression witness); the token ask is banked, not blocking; no API, no Safari cell, no bundle claim. Six of nine gate arms run this minute as grep/tsc/node against the tree; the three probe gates' RED cells are pasted with statically-certain mechanisms.

---

## 4 · Addendum clause (E-3 — appended, never rewriting the worker records)

**MT-DOCK-A**:
1. **IDENTITY GUARDS**: the wave keeps the standing name MT-DOCK-LAYERS-1 (App.md:146; scope extended; G-A carried verbatim). L-7 ≡ MT-F005 ≡ ColorInput-adj D-3 is ONE identity cured at ColorInput — any close row claiming this wave cured the nameless button is false. L-21 ≡ App-adj D-4 H1 arm (MT-APP-1; VISIBLE per §5.1). L-22 ≡ W-HYGIENE config parity (ColorSpaceSelector-adj L-14), now at five independent confirmations — corroboration accretes, never re-books; its cure shape is superseded to DELETE by the arbiter's resolver proof (R-3).
2. **NAME CORRECTION RECEIPT (R-2)**: glass-ui 7.0.0 exports `DockControl` + `DockTrigger`; `DockIconButton`/`DockSelectTrigger`/`DockDropdownTrigger` are emitted CSS class names only (arbiter's dist grep: zero export hits). ROOT-FINDINGS.md:1242 and the memory reference `glass_ui_dock_components.md` are STALE and must be re-keyed before any seat cites them — the second memory-inherited near-false-cure this formation (cf. MT-F007/MT-F017). MT-F026's substance, gate, and owner-marked status are untouched.
3. **INSTRUMENT NOTES for future seats**: "which node does the glass paint?" is decidable by a selector census over the producer's dist CSS with zero browser (no rule paints `.glass-dock`'s own background; every dock-surface rule paints `.dock-plate`; an unstyled element's computed backgroundColor is transparent by spec) — a live rgba read corroborates but cannot outrank it. "Can the shim ever animate?" was decided by reading layers.css's transition property list. "Are the paths entries load-bearing?" was decided by running the TypeScript resolver with the entries absent (byte-identical resolution; package self-reference covers the exported subpaths; the bare specifier resolves to nothing anywhere).
4. **THE HEDGES DISCHARGED**: ColorInput-adj D-13's verbatim-carry demand → G-I (with the MT-APP-1 seam stated). Worker-O's L-20 hedge ("if MT-DOCK-LAYERS-1 does not carry the rendered-motion gate, L-20 returns to MT-DOCK-1") → G-B carries it; MT-DOCK-1 is never minted.
5. **THE CHROME CURE IS NOT the masking fallback the filing feared**: the defect is the SILENCE; the cure removes the fallback by type, generalizes an existing instrument, ships a loud fault path, and banks the producer-token deletion (BANK-D1). The next producer paint change turns G-F RED instead of un-wiring silently.
6. **I-20 STANDS**: every browser gate here is a chromium cell; the dock-morph SIGABRT arm belongs to the Phase-D safari-real matrix; nothing in this record claims real Safari or playwright-webkit.
7. **UNVERIFIABLE-HERE ledger (L-12)**: L-19's authed `viewEntries` collapse (vitest-stubbed, labeled) · MT-F026's authed Profile pill (static class chain, cured by construction) · any data-backed dock state. Dev :9000 is API-LESS; a blank data surface is an environment fact, not a defect confirmation.

## 5 · DISSENT (preserved verbatim where unresolved)

1. **Wave-shape counter-position (worker-O, overruled at R-1 but preserved)**: "A component seat that harvests the whole document rather than its own surface inflates the defect count… the honest headline for this component is FOUR rows (L-18, L-19, L-20, L-6), of which one (L-20) already had an owner too — so THREE are genuinely new. I record this because 'seven defects' will otherwise enter a close row unchallenged." — The arbiter adopts the count discipline (this document's headline is 5 CONFIRMED / 2 RESCOPED with three folds to standing owners) while overruling the two-wave shape. Worker-F's own dissent (a) concedes the split is viable: "a reasonable arbiter could split G-E (the session-port re-home, ~7 files) into its own micro-wave… but the split is viable and I record it." If execution finds the extended wave too large, the sanctioned split line is the port move (G-G) — under the SAME wave name, sequenced commits, never a second name.
2. **Worker-O on the filings' reliability, verbatim (binding caution for any close)**: "these filings are right about WHAT is broken and unreliable about WHY… If a close cites those mechanisms it will be citing three things that are false." (L-19 predicates / L-6 cycle+bytes / L-22 second-instance — all struck above.)
3. **Worker-F's pure-bank alternative on L-18, verbatim**: "An arbiter preferring the pure-bank shape (static model serves, loudly logged, no re-point) has a defensible position — but silent serving must die either way." Overruled in favor of O's type-kill (R-7); recorded because a future producer-token landing (BANK-D1) converges all three positions.
4. **L-6 boundary (worker-O's soft dissent, verbatim)**: "A reasonable arbiter could rule the opposite way and give the whole port to MT-APP-PORTS-1… If the arbiter moves it, the hedge is the ColorInput precedent — the gate text travels verbatim, and if MT-APP-PORTS-1 does not carry it, the row returns here." The arbiter keeps the port move HERE (completable-alone; the eager-boot-graph row keeps its distinct MT-APP-PORTS-1 identity untouched) and adopts the hedge text as standing law should a future formation re-home it.
5. **Unresolved measurement debt, declared**: the live cells adopted labeled (plate alphas, five-step latch trace, four-phase transition samples, 13-control shadow table, 65/23 histogram) were each measured by BOTH workers independently (O with its own instruments, addendum D) and every underlying mechanism was re-derived statically to certainty by this seat — but no cell was re-measured a THIRD time by the arbiter (probe parsimony). The gate scripts re-measure all of them at execution; any divergence is a gate event, not a silent drift.

---

# §6 · DELTA r3 (2026-07-28) — the re-run shell band, adjudicated and merged

**Seat**: worker-F, single adjudicator (the r3 delta brief; NOT a tri-fold — recorded per L-14's corollary: this round's rows had one challenger fleet and one adjudicator, so agreement-independence arguments from §2 do not extend here). Model receipt: `claude-fable-5`, observed in this seat's own runtime. **HEAD**: `7775473b`. Method: refutation-first (L-2/L-9/L-10) — every row below was re-derived against the tree with this seat's own instruments before any challenger cell was believed; the challenger's live cells (all playwright-WebKit or Chromium, per row) are carried LABELED where the static mechanism was decided to certainty; I-20 respected — nothing below claims real Safari.

**This seat's own instruments (receipts mine, this session)**: full reads of `Dock.vue` (all 359 lines), `SlugEditLayer.vue` (all 119), `useSlugMigration.ts` (all 126), `useDockAdminMode.ts:20-66`, `ActionBarToggle.vue:40-160`, `PaneSegmentedControl.vue`, `dock/index.ts`, `DockViewSelect.vue:85-130`, `status-lamp.ts:40-55` + `DockStatusLamp.vue:1-35`, `App.vue:30-45/:78-135/:305-335`, `usePaneRouter.ts:185-225`, `ColorPicker.vue:265-345`, `usePaletteWiring.ts:30-125`, `router/index.ts:20-36`, `api-problem.ts:22-33`, `usePopupMutex.ts:1-8`, eslint.config.js glob census, `test/picker-blob-config.test.ts:1-20`, vitest include; **dist censuses (mine)**: `dock.js` focus-repair `w()` body (`!n||!r||!n.contains(r)||(await I(),(_(t)??document.body).focus?.())` — the outgoing-face-contains-activeElement refocus, verbatim), `De()` body (a function getter returning null yields null with NO document-convention fallback; the wrapper at :715-716 always hands a getter), sampler branch `:215-216` (`source-unavailable` exactly when live && !canvas), `backgroundCanvas` prop present at `:710`, exports map (`./dom` and **`./dropdown-menu` BOTH EXIST** — decisive for L-36 and D3-06), no rail/roving export (decisive for BANK-D4); greps: `PaletteSlugBar` render sites (0 templates; 2 barrel lines + 1 type import), `slugBarRef` (never template-bound), `TopDock` (1 stale comment), root-barrel `@mkbabb/glass-ui` imports outside demo/ui (18), `demo/@` (`No such file or directory`) vs the eslint G-DEMO globs (:235-238/:275-276 all `demo/@/...`), `test/` files importing `../demo/` (10), `:deep(` census (≈15), `meta: { admin: true }` (exactly the 5 admin routes), `EDIT_TARGET_KEY` provided at App.vue:270, e2e/test hits for `SlugEditLayer|Switch to slug` (**0**), the r1 banner's phantom Pass-2 (`C-15..C-22` exist at lines 4-5/:561 only), `git ls-files` on the three r3 frames (**UNTRACKED** — L-7 obligation below).

## 6.1 · Corpus map

Six fresh seats in `registry/harvest/area-shell.json` (run `wf_e28d617f-9eb`) target this component: seat 5 (CHALLENGE-C pass 3, C-23..C-30), seat 6 (CHALLENGE-L, L-30..L-34+L-30b), seat 8 (CHALLENGE-D r2', D2-01..D2-16), seat 11 (CHALLENGE-L, L-35..L-39 + L-1/L-23/L-6 blind replications), seat 15 (CHALLENGE-C pass 4, C-31..C-38), seat 16 (CHALLENGE-D r3, D3-01..D3-11). All six carry Opus-5 receipts (`claude-opus-5[1m]`). Seat 0 replicates the r2 corpus (§1) verbatim and is not re-adjudicated. Owner marks MT-F026 / OM-1 / OM-7 stand RULED — not re-litigated here (G-H untouched).

## 6.2 · Verdicts (58 rows, clustered by identity; every member id listed, original ids for life — L-5)

| Cluster | Member ids | Verdict | Deciding evidence (this seat's own unless labeled) |
|---|---|---|---|
| **DELTA-1 · the command seam** (the dock owns no command surface; every dock action dispatches through DESKTOP-ONLY instance refs) | **L-35** (BLOCKER) · **L-1-rep** (BLOCKER) · **D2-01** (BLOCKER) · **D2-02** (BLOCKER) · **D3-01** (BLOCKER, mechanism half) · L-30b (partial) | **CONFIRMED — BUILD here (the unified registry cure; R-8 rules the MT-APP-1 seam)** | Statically certain, re-derived whole: App.vue:83-91 mobile PaneSlot has NO `:on-mount` vs :105/:131 desktop (my read); `onDesktopLeftMount`/`onDesktopRightMount` are the SOLE writers of colorPickerRef/generatePaneRef/gradientPaneRef/mixPaneRef (:317-332); App.vue:38 `:action-bar="colorPickerRef?.actionBarContext ?? null"`; usePaneRouter builds the generic bars from VIEW METADATA with handlers `paneRefs.<view>.value?.<cmd>?.()` (:193-220, my read) — so on mobile the gradient/generate/mix bars RENDER (metadata is ref-independent) and every handler is a silent no-op (`?.` masks the null): D2-01's present-and-inert arm is worse than absence and is structural, not incidental. L-35's edit arm: ColorPicker.vue:271 `editTarget` private ref, writers reachable only via defineExpose (:331-343); usePaletteWiring:37-55 `whenColorPickerReady` polls the desktop-only ref 40×50ms then `console.warn`s and STOPS — on mobile the `mobile-edit` DockLayer (Dock.vue:72 `!isDesktop && !!editTarget`, :135-145) is unreachable code in the only grammar it exists for. Challenger cells carried labeled: clipboard sentinel untouched on mobile /#/gradient "Copy CSS" (desktop writes the gradient string); button inventory at 390 lacks all six picker actions; dock face count 3-vs-4. D3-01's 200%-zoom arm (720×900 ≡ 1440@200%): the same hole hits a ZOOMED DESKTOP user — carried labeled, gate arm added (G-I). |
| **DELTA-2 · login truth** (a failed login is completely silent, three independent swallows deep) | **L-30** (BLOCKER) · C-23 · L-31 · C-24 · C-36 | **CONFIRMED — BUILD here** | All three swallows re-derived to certainty: (1) SlugEditLayer.vue:54 `pm.onSlugSwitch(...)` NOT awaited inside `try` while useSlugMigration.ts:51 declares it `async` — the catch at :57 is unreachable for every network failure, :55-56 close the layer synchronously, and `finally` clears `slugSwitching` in the same tick (the :97 spinner is unreachable UI); (2) the callee catches everything itself (:74-88) and dispatches into `slugBarRef.value?.setError(...)` — `slugBarRef` (:30) is NEVER template-bound (my grep) and `PaletteSlugBar.vue` is rendered by NOTHING (0 template sites; 2 barrel re-exports + 1 `import type`) — L-31's "the DEAD copy owns the error channel" exact; (3) `slugError` is written 7× (:13/:18/:43/:49/:59-62) and rendered ZERO times (template :75-119, my read). And :59-62 is a verbatim resurrection of the S.W2 W2-6-condemned substring sniff — `ApiProblem` does `super(title)` (api-problem.ts:31, my read), so `e.message` can never contain "409"/"404"/"429"; the cure comment sits 30 lines away in useSlugMigration.ts:78-82. C-24: the SlugEditLayer↔PaletteSlugBar parser clone confirmed same-shape. C-36: e2e/test hits for the whole login surface = **0** (my grep); the o15 oracle's prose exclusion carries it. Challenger live cell (WebKit, :9000): submit → layer closes inert, input cleared, `errorish:[]`, clean console — carried labeled; the API-LESS dev server GUARANTEES the failure, so the reproduction is an environment FEATURE here (L-12 inverted). |
| **DELTA-3 · the dead lint lattice** | **L-23-rep** (BLOCKER) | **RESCOPED-TRUE → W-HYGIENE (config-parity family, the L-22 identity class) with a G-G RIDER built here** | Re-derived: eslint.config.js:235-238/:275-276 scope every G-DEMO boundary object to `demo/@/...`; `ls demo/@` → No such file or directory (my run). The rules read as enforcement and enforce nothing — the same class as L-22's tsconfig paths (config drift made unrepresentable by deletion or re-point). The repo-wide re-point belongs to W-HYGIENE (one owner for the class); but the ONE boundary this wave itself creates (shell must not import palettes) lands HERE as eslint structure alongside G-G, WITH a meta-check that the glob matches ≥1 file (a guard that matches zero files is the disease, not the cure). |
| **DELTA-4 · the nav label goes blank** (the controlled select's model-value leaves its own option set) | **C-3-WIDENED** · C-29 | **CONFIRMED — BUILD here (extends G-C/G-D)** | Statically certain from my useDockAdminMode read: `viewEntries` returns `adminViews` ONLY when `isAdminMode && isAdminAuthenticated` (:34-39), else `userViews` (7 entries) — so for ANY anonymous visitor on blob/atmosphere/admin-* the current view is absent from the option set and `<SelectValue/>` resolves nothing: blank primary navigation on 7 of 15 routes. The r2 G-C cure (group derivation) does NOT alone cure this — an anon user on an admin-group route still has the view filtered out — so the derivation gains the challenger's clause: viewEntries is UNIONED unconditionally with the current view. C-29 (test truth): deleting `<SelectValue v-if="isDesktop"/>` keeps the ENTIRE e2e suite green (locators use the aria-label; `openView` can only reach views already in the list) — the route-parametrised label assertion becomes a gate arm. |
| **DELTA-5 · four predicates, one boundary** (tablet-portrait renders both grammars at once) | **D2-03** (BLOCKER) · D3-04 | **CONFIRMED — BUILD here (new structure (h) + G-L)** | Statically certain: App.vue:305-312 `useBreakpoint("(min-width:1024px) and (min-aspect-ratio:1.1)")` — whose OWN comment says the compound query exists "so they can never disagree" — vs Dock.vue:71 `useMediaQuery("(min-width: 1024px)")` width-only (my reads), plus the `lg:` display utilities in ProfileSection:48/:127, MobileMenuDropdown:38, ActionBarToggle, plus PaneSegmentedControl.vue's 639px fork. At 1024×1366 the predicates disagree BY CONSTRUCTION (aspect 0.75 < 1.1: App says mobile, Dock.vue:71 says desktop). Challenger cell carried labeled: dataLayout='mobile' + mobile switcher AND desktop profile both rendered, frame `collision-1024x1366.png`. shell.css's stamp-law comment (the challenger's cite) makes this a violation of the repo's own recorded decision, not just of taste. |
| **DELTA-6 · the slug layer misses the floor and has no name** | **D2-04** · C-33 · D3-05 | **CONFIRMED — BUILD here (G-M) + BANK-D5 producer ask; r1's "MT-F004 not real" ruling is OVERTURNED (§6.3)** | Mechanism re-derived from producer source: dist touch-floor.css exempts `.dock-icon-button--compact` AND everything inside `.glass-dock` from the 2.75rem coarse-pointer floor — SlugEditLayer's three `compact` DockControls (:91-118, my read) hit BOTH exclusions, and the demo never re-supplies geometry through the `--dock-compact-control-*` token seam that its OWN sibling already uses (ActionBarToggle.vue:155 `--dock-compact-control-padding: 0.5rem 0.75rem` — T-36, my read). The input (:81-87) has placeholder only, no label/aria-label, fixed `w-40`. Challenger activated-state cells carried labeled: 22-23px controls, 160×20-23 input, placeholder clipped. r1 measured only the at-rest INERT state — the overturn is recorded, the at-rest harness rows stay geometry-only artifacts (both r3 D seats' negativeProofs agree). The §7 Account-Dialog re-housing (D2-04's root cure) is the VISUAL-CONSTITUTION's own end state and stays owned by the constitution's W-map — THIS wave lands the lawful interim (floor via the producer's own token seam + a real name), which the Dialog move later deletes wholesale. D3-05's PaneSegmentedControl 26px rows die with W47 (R-9). Producer hit-seat token ask → BANK-D5. |
| **DELTA-7 · the layer machine loses focus and says nothing** | **C-31** · C-34 · C-35 | **CONFIRMED — BUILD here (G-K) + BH relay** | The producer mechanism verified in dist by this seat: `w()` refocuses the incoming face (or body) whenever the OUTGOING face contains `document.activeElement`, after `nextTick` — unconditional, no `[autofocus]` preference (verbatim body quoted in the instruments block). SlugEditLayer:20-22's nextTick focus therefore loses the race (challenger's instrumented trace: +193.9ms input.focus → +201.6ms face.focus, final activeElement `div.dock-face[tabindex="-1"]`, typed text dropped — carried labeled; the mobile portaled-menu path unaffected, also labeled). C-34: the only live region in the band is the DEV-gated lamp (`isDev` at DockStatusLamp.vue:32, my read) — production has ZERO live regions; layer swaps, linkCopied, login outcomes and view switches are all unannounced, and focus parks on an unnamed role-less div 3/3 (labeled). C-35: `colorInputRef`/`actionToolbarRef` declared (:28-29) + bound (:103/:116) + read NOWHERE (my grep; defineExpose :96 excludes them); ColorInput's exposed `focus()` (:282) has no caller. Cure shape: the layer declares its entry-focus target and the machine honours it — the producer half (prefer `[autofocus]`/focusTarget inside the INCOMING face) rides the standing BH fond; the demo half (drive focus off the became-active edge, after the repair; one dock-owned polite region) is BUILD here. No demo patch may fight the producer's repair with timers — the interim is the post-repair edge, stated. |
| **DELTA-8 · the backdrop sampler has never succeeded** | **C-32** (+ C-38 hazard) | **CONFIRMED — BUILD here (G-P) + BH relay** | Statically certain from my dist read: Dock.vue:132 binds NO `:background-canvas` (my read of the full tag); dist :715-716 always hands the sampler a getter; `De()` (body quoted above) returns null for a null-yielding getter WITHOUT falling through to the `[data-glass-field-canvas]` document convention — while the is-live predicate (:176) DOES consult the convention. So live=true + source=null → `source-unavailable` re-stamped forever (:215-216), `--glass-backdrop-luma` unset, tint pinned at its 4% floor. Challenger cells (3/3 routes, 42 idle mutations/4s at ~3.5Hz) carried labeled. Cure: thread the canvas the App already owns (App.vue:9 ref → prop/inject into Dock.vue's GlassDock — the `backgroundCanvas` prop exists at dist :710, my read); the producer fallthrough (getter-null → document convention) is a BH-relayed ask, never a demo shim. C-38 (the 300×150 WebGL2 backing store) stays a LABELED HYPOTHESIS rescoped to the atmosphere seat and SEQUENCED before G-P's `sampled` assertion — the gate asserts "not source-unavailable"; if the cure trades it for `sample-unavailable`, that is the atmosphere row firing, not this gate passing. |
| **DELTA-9 · the pill jumps** | **D2-10** · D2-11 | **CONFIRMED — BUILD here (extends G-B)** | Statically certain: Dock.vue:132 `:fit-content="true"` over four DockLayers of very different intrinsic widths; crossfade.css cross-fades OPACITY only (r2 §2's layers/crossfade census, re-confirmed) — the box change is instantaneous by construction. Challenger cell: main 471.5w → slug-edit 309.0w, Δx +81.2 unanimated (labeled). D2-11: `.action-bar-toggle-slot.is-live` transitions `grid-template-columns` (my read of :120-128) — the one layout-animated property in the dock, on a duration/ease register while the seal rides a spring register. Both fold into G-B's rendered-motion product gate: no single-frame box step ≥20px on a layer swap; the slot animates transform/opacity or not at all. |
| **DELTA-10 · register & composition truth** | **D2-16** · D3-06 · D2-08 · D2-09 | **CONFIRMED — BUILD here (G-M/G-N/G-O)** | D2-16: `hide-indicator` on BOTH SelectItem blocks (DockViewSelect :96/:127, my read) + the P4-R4 weight-marker retirement comment — selection has NO visual marking (challenger's both-scheme cell: 7 options fontWeight 400, transparent bg, only aria-selected — labeled); §4.2's ColorSpaceSelector rule (delete hide-indicator, consume the producer indicator gutter) is the SAME canon, unapplied here — canon-hard cure. D3-06: MobileMenuDropdown composes from `../../../ui/dropdown-menu` (demo-local, my read) — and **glass-ui 7.0.0 ships `./dropdown-menu` in its exports map (my census)**, so the producer-tier move is executable TODAY, not banked; the wrapped GitHub row (55px vs 44 siblings) + read-through material cells carried labeled (frame `state-mobilemenu.png`). D2-08: separators at ProfileSection:49 AND :123 + ActionBarToggle's (my reads) — 3 visible lines for 4 self-seated capsules; PR-05's terminal verb (labeled cite) → separator ownership moves to the layer composition, main-layer count 0. D2-09: five operable rungs (22/28/32/40/44, labeled) and ActionBarToggle.vue:150-153's comment claims "the box lands at the sibling controls' 2.5rem height" against a measured 32px — the comment is corrected or deleted (a comment that states a measurement must be true or not exist). |
| **DELTA-11 · RTL truth** | **D2-07** | **CONFIRMED — BUILD here (G-Q arm)** | Static: ProfileSection:140 `@mbabb` bare text, no `dir`/bidi isolation (my read) — a neutral-then-Latin run reorders to `mbabb@` in RTL paragraph direction by UAX#9, statically certain; the rendered frame (rtl-desktop/picker.png) carried labeled. The two directional affordances are physical arrow glyphs (ActionBarToggle's ArrowRight :103, Dock.vue:154's ArrowLeft — my reads) with no logical flip. Cure: `dir="ltr"`/`unicode-bidi: isolate` on the wordmark + slug pill; ONE logical chevron primitive for the enter/exit motif (two instances that must always agree = one component). |
| **DELTA-12 · the partition's third home** | **L-34** | **CONFIRMED — BUILD here (extends structure (b))** | Re-derived: router/index.ts:29-34 stamps `meta: { admin: true }` on exactly the FIVE `/admin/*` routes (atmosphere/blob unmarked — my read); useDockAdminMode:27 says SEVEN; viewSchema's hue law classes the same five at 0°. Three owners, tally 5/5/7, live semantic disagreement. The r2 structure (b) (`group` on PaneConfig) gains the router derivation: `meta.admin` derived from `VIEW_MAP[id].group`, hand literals → 0 (G-C grep arm). |
| **DELTA-13 · hygiene cluster** (one sequenced commit, grep-gated in G-Q) | **C-25** · **C-26** · **C-27** · **C-28** · **D2-13** · **D2-14** · **D2-15** · **D3-09** · **D3-10** · **L-36** · **L-39** | **CONFIRMED (each fact re-verified this session) — BUILD here** | C-25: Dock.vue:87/:89 watches non-immediate (my read); mount-time predicate never applied to the producer's ref-counted hold; survivable only via the producer clamp (labeled) — `{immediate:true}` + the derivation. C-26: usePopupMutex fork header (:1-2, my read) has NO BH relay filed — relay rides the fond; the useLayerTransition shim dies at G-B. C-27: uncancelled one-shots verified (Dock.vue:105, ColorInput:256/:262, ActionButton:95, ABL:70-76 cleared on re-entry not unmount) vs the correct idiom one directory away (PaneSlot:89-108) — harm bounded (post-unmount ref writes are inert in Vue), the asymmetry real. C-28: the `slotLive` early return (my read of :50-68) makes the watch's re-arm dead; the invariant held today by the `transitionend` side channel alone (challenger measured recovery, labeled) — drop the early return. D2-13: crossfade keeps inactive faces opacity-0 (not visibility-hidden) so nav.innerText carries three invisible layers incl. the bare `&rarr;` at :137 (no aria-hidden, my read) and the live specimen string — aria-hidden the connector; the leak documented for every text-metric consumer. D2-14: `role:"alert"` static-from-first-paint (status-lamp.ts:47-52, my read) → `role="status"`; dev-only, INFO. D2-15: `ref` idiom at :60 vs useTemplateRef at :74 + the two `?? null` computeds (:39-40) — modernise + delete. D3-09: Dock.vue:4 imports from `"./"` while index.ts:5 exports Dock.vue — a real module cycle for five producer symbols (my reads; L-30b's walker found it the tree's ONLY runtime cycle, labeled) — import from `@mkbabb/glass-ui/dock`, barrel exports only Dock. D3-10: two-root SFC (:128/:293, my read); the lamp re-homes to the band (App.vue's nav), its documented positioning context. L-36: 18 root-barrel `@mkbabb/glass-ui` imports outside demo/ui, all `./dom` symbols (my grep; `./dom` in the exports map, my census) — mechanical rewrite. L-39: the load-bearing contract comment names `TopDock`, which does not exist (my grep) — rewritten in the registry commit to state the NEW contract. |
| **DELTA-14 · folds & rescopes** (true rows owned elsewhere; identities preserved, NOT re-booked) | **D2-05**, **D3-08** → MT-COLORINPUT-1 · **L-32**, **L-37** → W-HYGIENE · **L-38** → W47 + BH + W-HYGIENE (doc line) · **L-33** → palettes seat · **D3-11** → the visual-audit harness (Phase-D chromium re-run) · **C-38** → atmosphere seat · **C-30** → registry maintenance (L-13) · **D2-06**, **D3-02** → the convergent design loop (task #7) | **RESCOPED (all facts verified or carried labeled)** | D2-05/D3-08 ≡ MT-F005 ≡ L-7 ≡ ColorInput-adj D-3 — ONE identity, already BUILT at ColorInput (r2 §1); these are its fifth+sixth confirmations; the propose/submit-fold detail accretes to that row. L-32 (10 of 19 `test/` files import `../demo/` — my count) and L-37 (`picker-blob-config.test.ts` greps three demo SFCs from the LIBRARY vitest root via `process.cwd()` — my read; the owner-retired grep-invariant idiom RESURRECTED, cf. the 2026-06-02 feedback) → W-HYGIENE, with the L-37 note flagged to the owner-feedback ledger. L-38: the `:deep(.segmented-tab)` density pierce (my read) — the control dies at W47 (R-9); the density-token ask rides BH; DESIGN.md:381's false-by-≈14 census line → W-HYGIENE doc fix. L-33: palettes dual-home, out of subject, the seat itself assigns the palettes owner. D3-11: forced-colors is a WebKit false green (the harness cannot produce the state it names) — re-run under `ENGINE=chromium` belongs to the visual-audit owner; until then the row reads UNTESTED, and no close may cite the forced-colors matrix for this component. C-30: the r1 challenge file's banner promises a Pass-2 (§6, C-15..C-22, incl. a claimed BLOCKER C-15) whose body is NOT in the committed file (my grep: banner + one dangling forward-ref only) — per L-13 the root attempts journal recovery BEFORE any re-run; until then C-15..C-22 are uncitable and the banner correction is a registry-maintenance action. D2-06 (dark-scheme accent chroma 0.188→0.021; Login/@mbabb visually identical in dark; default-route `--accent-view` ≡ `--accent-live` — cells carried labeled): the MEASURED degeneracy is real, but the filed cure ("delete accentHueShift and the token family") would re-litigate Q5/W7-4/T-28 owner canon without owner authority — the row goes to the convergent design loop WITH its cells; the auth-primacy half is adjacent to G-H's producer-primitive direction and no close may cite THIS wave for it. D3-02 (the wax seal DOES render; collapse is pointer-history-gated; two resting geometries): the facts overturn r1/r2's "seal never observed" claim (§6.3) and the first-ever collapsed-seal π frame is ADOPTED as an obligation below — but the seal is owner-ratified canon (W7-1/T-37/T-28, the 40-line law block in Dock.vue itself) and no ruled law bans a hover-armed collapse; the one-resting-state design question goes to the design loop with the cells. |
| **DELTA-15 · refuted** | **D3-07** | **REFUTED as filed** | The citation fails on its own text: VISUAL-CONSTITUTION "Shell, dock, and header" (my read) says "**Direct route changes** keep its full navigation identity" — opening the Tools LAYER is not a route change; the band reservation holds (72px constant in both geometries — the same seat's OWN negativeProof cell); and "the only exit is one unlabelled ←" is false — Dock.vue:154 carries `aria-label="Back"` (my read). The residual (seven adjacent icon-only controls at two rungs) is a PR-07/PR-06 density observation for the design loop, already partly covered by DELTA-10's rung gate; no constitutional breach is shown. No close may cite D3-07's "persistent chrome stops being persistent" clause. |
| **DELTA-16 · replications accreting** | **L-6-rep** · **L-30b** | **CONFIRMED — accretion only, no new booking** | L-6-rep independently re-measured the five shell value-import sites (identical list to my r2 grep) — a third/fourth confirmation of the r2 L-6 ruling (G-G unchanged; R-6's cure detail stands). L-30b re-confirmed L-1/L-11/L-6/L-9/L-23/L-8/L-7 with two measurements agreeing to the digit — its 65-module/251KB closure and 39/157KB-without-session-port cells are ADOPTED (labeled) as G-G's delta baseline refinement. |
| **DELTA-17 · negative proofs & rail** | **C-37** · **D3-03** | **C-37 CONFIRMED as the negative-proof ledger; D3-03 CONFIRMED → BH ask + BANK-D4** | C-37's six measured absences (parseCssColor crash class does not reach the dock — 8 malformed `?color=` values, 0 page errors; active-layer v-if teardown recovers; zero getImageData across 10 colour ticks; DockControl forwards `type`; `.is-active` hits a real producer rule; the App transition listeners are guarded) are RECORDED so no later seat re-spends the probes; (1)+(2) become the parser-band regression floor if its cure lands. D3-03: §5.2 NAMES this component ("horizontal Dock/rail roving focus") and Dock.vue binds zero keydown handling (my read; challenger's ArrowRight/Home cells labeled) — but the cure is rail behaviour and belongs to the producer (the seat's own edict-4 argument); my dist grep finds NO rail/roving export in glass 7.0.0 → the ask rides the standing BH fond and **BANK-D4** carries the runnable re-trigger; no demo keydown graft may pre-empt the producer rail (masking-fallback ban). The constitution obligation stays OPEN against the producer, recorded. |

## 6.3 · Overturns and registry corrections (every one explicit; none touches a §1 ruling)

1. **r1 "MT-F004 is not real" — DEAD** (overturned by D2-04/C-33/D3-05): r1 measured only the at-rest INERT slug layer; the ACTIVATED layer's three 22-23px controls and unnamed 160px input are real WCAG 2.2 SC 2.5.8/3.3.2 failures. The at-rest harness rows remain geometry-only artifacts (that half of r1 stands).
2. **r1 "the dock is keyboard-unreachable on Safari" BLOCKER — DEAD** (overturned by D2-12): the macOS Full-Keyboard-Access default excludes bare buttons site-wide; Chromium tab order is complete and correct. The surviving MAJOR observation (no §5.2 roving rail) is DELTA-17/D3-03's row.
3. **r1/r2 "the wax seal has never been observed rendering" — DEAD** (overturned by D3-02): the seal renders; its gate is pointer history (collapse arms on pointerenter/leave; `:always-expanded="!isDesktop"` keeps mobile expanded — my read of :132). The collapsed-seal π frame is adopted below — the first this component has ever had.
4. **The r1 challenge file's Pass-2 banner (C-15..C-22) — UNCITABLE** until recovered (C-30, L-13): the body is absent from the committed file; the root attempts journal recovery before any re-run; no close may cite C-15..C-22.
5. **No §1-§5 ruling is overturned.** The r2 verdicts, the R-1..R-7 rulings, and the owner marks (MT-F026/OM-1/OM-7) stand exactly as written.

## 6.4 · Cross-wave rulings (the seams, so no close double-claims)

- **R-8 — the registry owns the command seam; bindPane narrows**: the provide/inject **DockCommand registry** cure appears independently in both corpora (the earlier L-pass's L-1 `commands.ts` and this band's D2-01/D2-02/D3-01/L-35 cures) and lands in THIS wave, ONE wave, end-to-end: panes provide a typed command surface (and the picker's `actionBarContext` + `editTarget` write move onto the already-provided injection cells — `EDIT_TARGET_KEY` is provided at App.vue:270 TODAY, my grep); the dock/usePaneRouter inject; the `?? null` prop-hops, `whenColorPickerReady`'s startEdit path and the 2s poll, and every action-dispatch `?.()` on a paneRef DIE. **MT-APP-1's `bindPane` (App.md:104) is hereby NARROWED to the non-command instance uses** (C-3's ref-identity cure; `applyExternalColor`/`commitEdit`/`cancelEdit` plumbing) — it may not resurrect command dispatch through instance refs, and this wave may not claim C-3. G-I's r2 seam text ("the null-ref CURE lands in MT-APP-1") is SUPERSEDED: G-I's cure now lands HERE and the gate no longer waits on any sibling.
- **R-9 — W47 keeps PaneSegmentedControl's death**: CARRY-LEDGER:22 already owns `PaneSegmentedControl 1→0` ("it lives ALIVE at demo/shell/ for exactly this gate"). D3-01's collision geometry (elementFromPoint at the Tools centre → `DIV.segmented-indicator` at three viewports, labeled) and L-38's `:deep` census ACCRETE to W47 as evidence; this wave guarantees only that the Tools capability EXISTS at every grammar (G-I), which stands whether W47 has landed or not. No close may claim this wave retired the switcher.
- **R-10 — the §7 Account end state**: the slug/auth flow living inside the navigation pill contradicts the constitution's Account ruling (a modal side Dialog opened FROM the dock). The Dialog move is the constitution's own W-map's; THIS wave lands the interim floor+name cure (G-M), which the move later deletes. Stated so neither wave re-books the other.
- **R-11 — design-policy rows go to the design loop, with their cells**: D2-06 and D3-02 (§6.2 DELTA-14). A named, in-formation owner (task #7) — not a deferral; any close citing THIS wave for either is false.
- **R-12 — the r3 witness frames are untracked**: `collision-1024x1366.png`, `state-mobilemenu.png`, `state-tools-desktop.png` exist on disk in this component's audit dir but are NOT in git (`git ls-files` — my run; `.gitignore:34 *.png`). Per L-7 they are force-added in the wave's FIRST commit or the claims they witness are re-captured by the gate scripts.

## 6.5 · THE WAVE, UNIFIED — MT-DOCK-LAYERS-1 (same standing name; r3 scope merged; still ONE wave, born-RED)

**Title (extended)**: The dock shell tells the truth — DockCrossfade adoption, admin identity derived not latched, a chrome referent that reads the surface that paints, the session port re-homed behind a platform key, chrome-tier pill primitives — **and the dock now OWNS a command surface (provide/inject registry), a truthful login (typed result, rendered error), one layout predicate, declared entry focus with announcement, a live backdrop referent, and register/composition truth.**

**Structure additions (L-8 — classes made unrepresentable; letters continue §3's (a)-(e))**:
(f) **The desktop-only capability class dies by INVERSION**: a typed `DockCommand` registry (provide/inject; `demo/shell/dock/commands.ts` owns the KEY + types) — panes PROVIDE their command set; the dock and usePaneRouter INJECT; a view providing no commands renders no Tools trigger; a provided command is live in whichever slot mounted it, BY CONSTRUCTION. `editTarget`'s write path moves onto the provided `EDIT_TARGET_KEY` cell. Deletes: the `?? null` action-bar hops (App.vue:38 + the generic hop), `whenColorPickerReady`'s startEdit path + 2s poll, every action-dispatch `?.()` on a paneRef, and the "exposed, not provided" contract comment (rewritten naming the new contract — L-39). Present-and-inert becomes unspellable: an absent capability is ABSENT.
(g) **The silent-login class dies by CONTRACT**: `onSlugSwitch` RETURNS its outcome (`Promise<Result<void, ApiProblem>>`-shaped) instead of dispatching into a foreign instance ref; SlugEditLayer AWAITS it and RENDERS the failure (`role="status"`); branches on `ApiProblem.status`, never message substrings (the S.W2 W2-6 ruling, at last obeyed at both sites); `PaletteSlugBar.vue` + both barrel lines + `slugBarRef` + `setError` DELETED (−243 lines; the dead twin cannot own the error channel if it does not exist).
(h) **The predicate pile dies by SINGLE OWNER**: the dock consumes the App-owned layout truth (the `[data-layout]` stamp / one injected `useLayoutMode()`); Dock.vue:71's width-only `useMediaQuery` and every `lg:`/`sm:` display fork in the dock subtree DELETED (PaneSegmentedControl's 639px fork dies with the control at W47 — seam stated).
(i) **Entry focus + announcement become DECLARED**: each layer names its entry-focus target, honoured on the became-active edge (after the producer repair — the producer-side `[autofocus]` preference is the BH ask); ONE dock-owned polite live region announces layer/login/copy outcomes in production.
(j) **The backdrop referent becomes REAL**: the atmosphere canvas the App already owns is threaded to `GlassDock`'s `backgroundCanvas` prop (exists in dist :710); the sampler stops failing by construction; the producer's convention-fallthrough fix is a BH ask, never a demo shim.

**Scope additions (exact files, beyond §3's)**: NEW `demo/shell/dock/commands.ts` · `demo/shell/usePaneRouter.ts` (bars carry provided commands) · `demo/color-picker/App.vue` (the two `?? null` hops; the canvas thread; lamp re-home) · `demo/color-picker/composables/usePaletteWiring.ts` (startEdit path retired) · `demo/picker/ColorPicker.vue` (provides its command set; the :311 comment) · `demo/palettes/useSlugMigration.ts` + DELETE `demo/palettes/browser/slug/PaletteSlugBar.vue` (+2 barrel lines) · `demo/shell/dock/DockViewSelect.vue` · `demo/shell/dock/menus/MobileMenuDropdown.vue` + `menus/ProfileSection.vue` (dropdown-menu tier + bidi + separators) · `demo/shell/dock/DockStatusLamp.vue` + `status-lamp.ts` (role) · `demo/shell/dock/ActionBarToggle.vue` (slot motion, comment truth, chevron) · `demo/shell/dock/ColorInput.vue` (focus edge; one-shot handles) · eslint.config.js (the shell→palettes ban, physical paths, ≥1-file meta-check) · NEW `test/` (or `demo/test/`) specs: `dock-slug-login.test.ts`, the label-per-route arm in `dock-admin-mode.test.ts`.

### Gates added or AMENDED (all RED today; failing input named; §3's G-A..G-H otherwise unchanged)

- **G-B (AMENDED — adds DELTA-9)**: the crossfade probe additionally asserts NO single-frame `.glass-dock` box step ≥20px across ANY layer swap (main↔slug-edit measured Δw −162.5px instantaneous — RED), and `.action-bar-toggle-slot` animates transform/opacity only or not at all (grid-template-columns transition present at ActionBarToggle — RED).
- **G-C (AMENDED — adds DELTA-4/DELTA-12)**: + `viewEntries` UNIONS the current view (vitest: anon, currentView='blob' ⇒ entries contain blob — RED: userViews-only, my derivation) · + trigger-label arm: anon cold-load each of the 15 routes ⇒ view-select accessible text === `VIEW_MAP[view].label` (RED on 7 routes — blob/atmosphere/5×admin, challenger cell + static certainty) · + `grep -c 'meta: { admin: true }' demo/color-picker/router/index.ts` = 0 (derived from `group`) — RED: 5 literals (my grep).
- **G-G (AMENDED — L-23 rider)**: + eslint `no-restricted-imports` ban `demo/shell/** → demo/palettes/**` pointed at PHYSICAL paths, with a meta-check (script or test) asserting each boundary glob matches ≥1 file — RED: today's guards all target the nonexistent `demo/@` (my ls+grep).
- **G-I (AMENDED — re-seamed by R-8 + D3-01's zoom arm)**: the registry cure lands HERE; the gate no longer waits on MT-APP-1. + a second arm at 720×900 (≡1440@200% zoom): a free-text colour entry AND the six picker actions reachable — RED (challenger's hit-test: elementFromPoint at the Tools centre → segmented-indicator; slot 0px/opacity 0/tabindex −1).
- **G-J (login truth — structure)**: `test "$(grep -c 'includes(\"409\")' demo/shell/dock/layers/SlugEditLayer.vue)" = 0 && test ! -f demo/palettes/browser/slug/PaletteSlugBar.vue && test "$(grep -rc 'slugBarRef' demo/palettes/useSlugMigration.ts)" = 0 && npx vitest run test/dock-slug-login.test.ts` — the spec (authored in-wave): stub the session port with a rejecting `onSlugSwitch` (typed `ApiProblem` 404) ⇒ the layer RENDERS "Slug not found." in a `role="status"` node and STAYS OPEN. RED today: sniff present (:59), file present, 6 slugBarRef refs, no spec exists.
- **G-J′ (login truth — product)**: `node .../gates/slug-login-error.mjs` — chromium, :9000 (API-LESS — the failure is GUARANTEED here, the environment is the fixture), desktop `/#/`: click Login → type `zzzz-zzzz-zzzz-zzzz` → Enter ⇒ visible error text inside the layer, layer still open, input not cleared. RED today (challenger's WebKit cell: closes inert, `errorish:[]`; mechanism statically certain).
- **G-K (entry focus + announcement)**: `node .../gates/dock-entry-focus.mjs` — chromium, desktop `/#/`: (i) click Login ⇒ `document.activeElement` IS the slug input (RED: producer `w()` refocuses the face — dist-verified + challenger trace); (ii) Tools → "Open color input" ⇒ the colour field holds focus (RED: focus never moves, typed text dropped — labeled); (iii) the built PROD bundle's dock contains ≥1 `[role=status]`/`aria-live=polite` region (RED: the only live region is dev-gated — static). ENV: arm (iii) runs against the BUILT artifact (L-12 — the dev server cannot witness it).
- **G-L (one layout predicate)**: `test "$(grep -c 'useMediaQuery' demo/shell/dock/Dock.vue)" = 0 && node .../gates/dock-layout-grammar.mjs` — chromium 1024×1366 `/#/`: NOT (mobile pane switcher visible AND desktop profile block visible). RED today: grep=1 (Dock.vue:71, my read); the dual grammar measured (labeled, frame committed by this wave).
- **G-M (register floor + composition)**: `node .../gates/dock-register-floor.mjs` — chromium, slug layer ACTIVATED (both grammars): every operable control ≥24×24 CSS px AND ≥ the producer touch floor under `(pointer:coarse)` via the `--dock-compact-control-*` seam, the slug input HAS an accessible name (RED: 22-23px ×3 + placeholder-only, labeled + static), main-layer visible separators = 0 (RED: 3, sources my read), and `grep -c "2.5rem height" demo/shell/dock/ActionBarToggle.vue` = 0-or-true (RED: false comment present, measured 32px).
- **G-N (selected state)**: `test "$(grep -c 'hide-indicator' demo/shell/dock/DockViewSelect.vue)" = 0 && node .../gates/view-menu-selected.mjs` (open menu, both schemes: the aria-selected option shows a nonzero producer-indicator/visual delta vs siblings). RED: 2 grep hits (my read); measured uniform (labeled).
- **G-O (producer menu tier)**: `test "$(grep -c 'ui/dropdown-menu' demo/shell/dock/menus/MobileMenuDropdown.vue demo/shell/dock/menus/ProfileSection.vue | awk -F: '{s+=$2} END {print s}')" = 0` (imports move to `@mkbabb/glass-ui/dropdown-menu` — EXISTS in the dist exports map, my census) `&& node .../gates/mobile-menu-rows.mjs` (390×844: no wrapped row — every menu row height within 2px of the modal rung). RED: demo/ui imports present (my read); GitHub row 55px vs 44 (labeled, frame committed).
- **G-P (backdrop referent)**: `node .../gates/dock-backdrop.mjs` — chromium `/#/` + one other route, post-settle: `.glass-dock[data-backdrop-sample-state]` ≠ `unavailable`-with-`reason=source-unavailable`, and `--glass-backdrop-luma` is SET. RED today (statically certain: no `:background-canvas` bound + `De()` getter-null returns null; measured 3/3 routes, ~3.5Hz re-stamp, labeled). The `sampled`-vs-`sample-unavailable` distinction belongs to the atmosphere row (C-38) — this gate may NOT be greened by trading reasons.
- **G-Q (hygiene greps, one command)**: `bash .../gates/dock-hygiene.sh` asserting ALL of: bidi isolation present on `@mbabb` + the slug pill (RED: absent) · `grep -c 'from "\./"' demo/shell/dock/Dock.vue` = 0 (RED: line 4) · lamp `role:"status"` (RED: `alert` at status-lamp.ts:47) · `aria-hidden` on the `&rarr;` connector (RED: absent at Dock.vue:137) · dock-subtree root-barrel `@mkbabb/glass-ui` imports = 0 (RED: 2 — ColorInput:117, SlugEditLayer:6) · `grep -c 'TopDock' demo/picker/ColorPicker.vue` = 0 (RED: 1) · both hold watches `{ immediate: true }` (RED: absent at Dock.vue:87/:89) · `DockStatusLamp` mounted from the band, Dock.vue single-root (RED: :293 sibling root) · the ActionBarToggle `slotLive` early return gone (RED: present).

### π obligations (additions)
The three r3 frames FORCE-ADDED in the wave's first commit (R-12): `collision-1024x1366.png` · `state-mobilemenu.png` · `state-tools-desktop.png` (adopted as BEFORE frames for G-L/G-O/G-B). NEW committed captures: the collapsed WAX SEAL at rest (the component's FIRST-EVER seal frame — D3-02's adoption), the ACTIVATED slug layer both schemes (G-J′/G-M before/after), the RTL band frame (G-Q), the 720×900 zoom arm (G-I). Selector pins add: `input[placeholder^="enter slug"]` · `[role=status]` in the band · `.glass-dock[data-backdrop-sample-state]` · `.dock-mobile-panes` · the menu rows. ENV: all chromium cells; :9000 API-LESS — for G-J′ the missing API is the FIXTURE (a guaranteed-fail login), stated honestly; G-K arm (iii) runs against the BUILT bundle; challenger WebKit cells stay labeled corroboration, never the gate's engine (I-20).

### Delta obligations (additions)
(8) Login: submit fails → [layer closes, no message, console clean] ⇒ [layer open, rendered `role=status` error, spinner visible during flight]. (9) Mobile commands: gradient "Copy CSS" clipboard [SENTINEL unchanged] ⇒ [gradient string]; dock face count mobile 3 ⇒ 4 with a live mobile-edit path; the 2s poll warn ⇒ gone. (10) Tablet-portrait 1024×1366: [switcher AND profile] ⇒ exactly one grammar. (11) Focus: click Login activeElement [div.dock-face] ⇒ [the slug input]; typed text [dropped] ⇒ [held]. (12) Backdrop: [`unavailable/source-unavailable`, luma unset, 42 mutations/4s] ⇒ [state set, luma set, idle mutations ~0]. (13) Selected state: [7 visually identical options] ⇒ [aria-selected row carries the producer marker]. (14) Slug controls: [22-23px, nameless input] ⇒ [≥24px/floor via token seam, named input]. (15) Layer swap: [Δw −162.5px one frame] ⇒ [animated or held]. (16) G-G refinement: Dock.vue closure [65 modules/251KB, palettes 23] ⇒ [~39/157KB, palettes 0] (L-30b's cells adopted as baseline).

### Carries (additions; every row exactly ONE disposition)
- **BUILD here**: L-35 · L-1-rep · D2-01 · D2-02 · D3-01 (G-I/G-L arms + registry, per R-8/R-9) · L-30 ≡ C-23 · L-31 ≡ C-24 · C-36 (G-J/G-J′) · C-3-WIDENED · C-29 (G-C arms) · D2-03 ≡ D3-04 (G-L) · D2-04 ≡ C-33 ≡ D3-05 (G-M; producer ask → BANK-D5) · C-31 · C-34 · C-35 (G-K + BH ask) · C-32 (G-P + BH ask) · D2-10 · D2-11 (G-B) · D2-16 (G-N) · D3-06 (G-O) · D2-08 · D2-09 (G-M) · D2-07 (G-Q) · L-34 (G-C) · C-25 · C-26 · C-27 · C-28 · D2-13 · D2-14 · D2-15 · D3-09 · D3-10 · L-36 · L-39 (G-Q hygiene commit).
- **FOLD, identity preserved**: D2-05/D3-08 → MT-COLORINPUT-1 (the MT-F005 identity's fifth+sixth confirmations) · L-32/L-37 → W-HYGIENE · L-38 → W47 + BH ask + W-HYGIENE doc line · L-33 → palettes seat · D3-11 → visual-audit harness · C-38 → atmosphere seat (sequenced before G-P's `sampled` claim) · C-30 → registry maintenance (L-13 recovery first) · D2-06/D3-02 → the convergent design loop, cells attached (R-11) · L-23-rep → W-HYGIENE (the G-G rider built here).
- **RETIRED / STRUCK AND UNCITABLE**: D3-07's "persistent chrome stops being persistent" clause (REFUTED §6.2) · r1's "MT-F004 not real", r1's Safari-keyboard BLOCKER, r1/r2's "seal never renders" (§6.3 overturns) · the r1 banner's C-15..C-22 until recovered · G-I's superseded r2 seam text ("the cure lands in MT-APP-1").
- **BANKS (additions; runnable, swept at wave open)**: **BANK-D4** (producer rail): `grep -rql 'rovingFocus\|RovingFocus\|useDockRail' node_modules/@mkbabb/glass-ui/dist/` — silent today (my run); on hit, adopt + mint the arrow-nav gate (D3-03's obligation stays OPEN against the producer via the BH fond). **BANK-D5** (compact hit-seat token): `grep -rq 'compact-hit\|hit-seat\|--dock-compact-control-seat' node_modules/@mkbabb/glass-ui/dist/` — silent today; on hit, the G-M demo tokens collapse onto the producer seat.

### Completable alone (re-answered under the merged scope — L-1)
**YES — with the sanctioned sequencing made explicit.** Every added arm is demo-side executable today (the dropdown-menu tier EXISTS in dist; the backgroundCanvas prop EXISTS; the token seam EXISTS; the registry is plain provide/inject; R-8 removed the MT-APP-1 wait), or BANKED with a runnable trigger (rail, hit-seat, chrome token, glass major). Nothing waits on the API, on Safari, or on a sibling wave. The wave is now large enough that §5-DISSENT-1's sanctioned split line is RE-AFFIRMED and EXTENDED: execution lands it as sequenced commits under the ONE name — (1) command registry + login truth (the two BLOCKER clusters), (2) the r2 §3 core (crossfade/latch/chrome/port), (3) layout+register+focus+backdrop, (4) hygiene — each commit independently green-able, never a second wave name. Ten of the ~17 gate arms run this minute as grep/tsc/vitest against the tree; every probe arm's RED cell is pasted with a statically-certain mechanism.

## 6.6 · Addendum (E-3 — appended, never rewriting)

**MT-DOCK-B**:
1. **STALE-NAME RECEIPTS (r3)**: glass-ui 7.0.0 DOES ship `./dropdown-menu` (my exports-map census) — any seat still treating the producer menu tier as absent is working from stale memory; and `./dom` carries `writeClipboard` — the 18 root-barrel imports are all narrowable today. The `TopDock` comment (ColorPicker.vue:311) is the third stale-name instance this formation (cf. MT-DOCK-A §2).
2. **INSTRUMENT NOTES for future seats**: "can the catch ever fire?" is decidable by reading ONE `await` (SlugEditLayer:54) — no browser needed. "Does the producer steal the focus?" is decidable from dist (`w()`'s contains-check body) plus one labeled trace. "Is the sampler broken by convention or by wiring?" is decidable from `De()`'s getter branch. "Is the option set a superset of the reachable views?" is decidable from `viewEntries`'s two-arm computed. The r3 corpus needed ZERO fresh probes from this seat.
3. **METHOD WARNINGS carried from the challengers (binding on gate authors)**: a dock/router probe reusing one MCP page across navigations manufactures a phantom route-hijack (seat 5's negativeProof); WebKit keyboard cells are meaningless without FKA (D2-12); the at-rest harness rows for inert layers are geometry-only artifacts — gates measure the ACTIVATED state.
4. **THE ENVIRONMENT AS FIXTURE**: the API-LESS dev server makes every login attempt fail deterministically — G-J′ USES this (L-12 stated as a feature, once). The same server can never witness the PROD live-region arm (G-K iii runs against the built bundle) nor any bundle claim.
5. **COUNT DISCIPLINE (§5-DISSENT-1 extended)**: the honest r3 headline is FIVE new defect CLASSES (command seam, login truth, predicate pile, focus/announcement, backdrop referent) + one canon-hard register cluster — not "58 defects"; 13 of the 58 rows are replications/accretions of already-ruled identities and 12 are re-homed. A close quoting raw row counts without this paragraph is misquoting the record.

## 6.7 · Dissent additions (preserved for the record)

6. **This seat's solo-adjudication caveat**: r3 had no second worker and no arbiter above this seat (the delta brief's shape). Every CONFIRMED above rests on this seat's own re-derivation plus ONE challenger's cells; where a cell could not be statically forced (the five-rung table, the 55px menu row, the dark-scheme accent values, the focus-race ordering), it is carried LABELED and the gate scripts re-measure at execution — any divergence is a gate event. A future tri-fold pass over §6 is sanctioned and would be the first to satisfy L-14 for this corpus.
7. **The D2-06 tension, recorded verbatim in spirit**: the challenger's "stop encoding view identity as hue" collides with Q5/W7-4/T-28 owner canon; this seat declined to adjudicate owner canon (R-11). If the design loop rules FOR the deletion, G-N's producer-indicator cure and the seal-ink derivation survive unchanged (neither depends on accentHueShift); nothing in this wave pre-judges the ruling.
8. **L-23's split disposition risk**: building the shell→palettes eslint ban here while W-HYGIENE owns the lattice re-point creates two eslint-touching commits. Accepted deliberately (the ban is this wave's OWN structure for G-G; a wave that creates a boundary without its guard repeats the L-23 disease); the W-HYGIENE fold notes the rider so neither doubles the other.

---

# §7 · ARBITER APOTHEOSIS — the r3 tri-fold ruled (2026-07-28) · **BINDING LAYER**

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own runtime, not inherited from the spawn label (L-11). **HEAD at arbitration**: `7775473b`; `git diff --stat 9bcd5d91 HEAD -- demo/ src/ test/ e2e/` re-run EMPTY by me — every line anchor in §1–§6 still resolves.
**Workers over the r3 corpus (M-12 tri-fold)**: worker-F (`claude-fable-5`) merged in place as **§6** above; worker-O (`claude-opus-5[1m]`) wrote aside at `registry/adjudicated/delta-r3/Dock.worker-O.md` (deliberately, to avoid racing the concurrent worker — the correct L-14 discipline; that file is a worker record and stays byte-intact). Both were attacked before either was adopted. **This §7 is the binding reading**: where §6 and the worker-O record disagree with §7, §7 governs; §6's text is PRESERVED as the worker record it is (E-3 — addendum, never rewrite).

**Method (L-14, refutation-first)**: every disagreement between the workers was re-derived against the tree with this seat's own instruments; agreed verdicts were spot-checked at source (§7.3). Arbiter's own receipts this session: full reads of `Dock.vue` (359 lines), `SlugEditLayer.vue` (119), `useSlugMigration.ts` (126), `useDockAdminMode.ts` (77), `ActionBarToggle.vue` (159), `dock/index.ts` (5); targeted reads of `App.vue:25-139/:300-335`, `usePaneRouter.ts:185-225`, `usePaletteWiring.ts:30-60` + the sole external `onStartEdit` call site (:117), `ColorPicker.vue:260-345`, `router/index.ts:20-36`, `DockViewSelect.vue` (hide-indicator ×2 at :97/:127, `SelectValue` :87, the Q5-RULED/"R1 SURVIVES" law block :40-48), `ProfileSection.vue:46-52/:120-128/:138-142`, `MobileMenuDropdown.vue:5-14/:36-40`, `status-lamp.ts:44-58`, `usePopupMutex.ts:1-4`, `PaneSegmentedControl.vue:42-55`, `usePalettePorts.ts:125-137`, VISUAL-CONSTITUTION.md `:80-91/:126-132/:175-182/:218-226`, CARRY-LEDGER.md:22; greps/censuses re-run: `void opts\.` (=1, :67), shell→palettes value imports (=5, exact file:line match), root-barrel imports outside demo/ui (=18), `TopDock` (=1 comment), `PaletteSlugBar` (=4 refs, 0 template mounts), `slugBarRef` (never template-bound), `demo/@` (ENOENT) vs the eslint G-DEMO globs, `meta:{admin:true}` (=5), `group` in viewSchema.ts (=0), e2e/test hits for the login surface (=0), `test/*.ts` (=19) with `../demo/` importers (=10), the C-30 banner gap (banner + dangling ref only; zero `## 6` headings), `git ls-files` over shots + components PNGs (=0) and `sha256` in the dock audit docs (=0), megatranche `.gitignore:10`; dist censuses: glass-ui **7.0.0** exports `./dropdown-menu` ✓ `./dom` ✓ `./dock` ✓. **One fresh probe, because two workers directly conflicted on an empirical claim** (D3-11): playwright two-engine cell, `forcedColors:'active'` → `matchMedia('(forced-colors: active)').matches` = **true in BOTH webkit and chromium**, `(forced-colors: none)` false in both (command preserved in R-14 below; a fresh session can re-run it verbatim).

## 7.1 · Binding tallies (supersede §1's r3 line and any §6.2 label they correct)

**r3 delta corpus (58 filed rows; each id counted once at its primary surviving disposition): 43 CONFIRMED · 12 RESCOPED · 2 REFUTED (D3-07, D3-11) · 1 GLASS-OWNED (D3-03) · 0 primarily UNVERIFIABLE-HERE** (two *arms* are UNVERIFIABLE-HERE inside confirmed rows: D3-06's two visual claims and D2-07's rendered RTL frame — citation-gated on G-R). Changes from §6's self-tally (45/12/1/0/0): **D3-11 RESCOPED→REFUTED** (R-14), **D3-05 CONFIRMED→RESCOPED** (R-16), **D3-03 CONFIRMED→GLASS-OWNED** (R-17). D3-01 stays counted CONFIRMED via its capability half, but its collision framing is REFUTED AND STRUCK (R-13). Merged document totals: 65 distinct rows (7 r2 + 58 r3) → **48 CONFIRMED · 14 RESCOPED · 2 REFUTED · 1 GLASS-OWNED**. Of the 8 filed r3 BLOCKERs: **2 survive at BLOCKER** (the command seam DELTA-1; the silent login DELTA-2), 1 survives in its capability half only (D3-01), 1 downgrades to MAJOR (D2-03, R-19), 1 is RESCOPED-TRUE lint config (L-23-rep), and the severity-inflation pattern worker-O names in his dissent is ADOPTED as a formation finding: the challengers were right about WHAT far more often than about WHY. The brief's "30 accusations (7 BLOCKER)" is the union of seats 6+8+11 only; no close may quote "30" or "58" without §6.6.5's count-discipline clause. Corrected count: the anonymous-nav row is **7 of 14** views (userViews 7 + adminViews 7 = the whole VIEW_MAP; worker-F's "7 of 15" was a denominator error, worker-O's 14 verified by me against `useDockAdminMode.ts:26-27`).

## 7.2 · Ruled disagreements (R-13..R-23; every ruling re-derived by this seat)

| # | Subject | worker-F | worker-O | **Ruling** | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| **R-13** | **D3-01's collision framing** ("the retired switcher is painted on top of the Tools control; 100% overlap of a live control") | CONFIRMED as composite; collision cells adopted | REFUTED — the control is clipped, unpainted, unfocusable; the rect is a phantom | **O wins — the collision framing is REFUTED AND STRUCK; the capability half survives inside DELTA-1; the retirement half stays W47's (R-9)** | Source, my read: `.action-bar-toggle-slot` computes `grid-template-columns: 0fr; opacity: 0` when not `.is-visible` (ActionBarToggle.vue:115-119); `.action-bar-toggle-inner` carries `overflow: hidden` (:130) released only at `.is-visible.is-settled` (:143); the control takes `tabindex="-1"` when `visible` is false (:92). A clipped child reports its UNCLIPPED `getBoundingClientRect` — the probe read a phantom rect. Nothing is painted over anything; `elementFromPoint` returning the segmented indicator is the clip working, not occlusion. O's residual ADOPTED at MINOR into the hygiene commit: a zero-width presence slot whose child overflows its clip manufactures phantom geometry for every rect-reading probe, and becomes real the day the clip lifts. Worker-F's genuinely new 200%-zoom arm (720×900 ≡ 1440@200%) survives independently as G-I's second arm. STRUCK AND UNCITABLE: "painted directly on top of", "two siblings laid out on top of each other", "100% overlap of a live control". |
| **R-14** | **D3-11** (forced-colors matrix row) | RESCOPED — "a WebKit render that cannot produce the state it names; the row reads UNTESTED"; chromium re-run ordered | REFUTED — both engines DO set the media query | **O wins — REFUTED; the ordered chromium re-run is UNNECESSARY and §6.2 DELTA-14's D3-11 sentence is superseded** | My own two-engine cell this session (fresh contexts, `forcedColors:'active'`): webkit `(forced-colors: active).matches` = **true**, chromium **true**; `(forced-colors: none)` false in both. Reproduction a fresh session can run: `node -e "const{webkit,chromium}=require('playwright');(async()=>{for(const[n,e]of[['webkit',webkit],['chromium',chromium]]){const b=await e.launch();const c=await b.newContext({forcedColors:'active'});const p=await c.newPage();await p.setContent('<body>x</body>');console.log(n,await p.evaluate(()=>matchMedia('(forced-colors: active)').matches));await b.close();}})()"`. D3-11's mechanism ("WebKit cannot emulate forced-colors → a green row for an unrun test") is FALSE, and with it the cure. O's residual is RECORDED as the surviving substance: emulating the query evidences the app's OWN forced-colors CSS (foundation.css U-F57 two-tier policy), never UA palette forcing — any future forced-colors claim must state which of the two it means. |
| **R-15** | **D3-07** (opening Tools "deletes the dock's navigation identity" — constitution breach) | REFUTED as filed | CONFIRMED at MINOR-with-a-design-question, gate carried | **F wins — REFUTED as filed; no gate; O's factual arms accrete elsewhere** | VC:178, my read: "**Direct route changes** keep its full navigation identity" — the clause's subject is route changes; opening a dock LAYER is not one, so the citation fails on its own text. `Dock.vue:154` carries `aria-label="Back"` (the "unlabelled ←" clause false — both workers agree). O's true observation (the main layer's four seats are absent while Tools is open — template-certain, :153-158 vs :161-226) is real COMPOSITION material, not a canon breach: it accretes to D2-10's box-truth gate (the same fit-content mechanism, verified: 471→309/326 is one phenomenon) and to the convergent design loop (task #7). No wave row; "persistent chrome stops being persistent" stays STRUCK AND UNCITABLE. |
| **R-16** | **D3-05** (mobile dock "below the 44px touch floor" = WCAG failure) | CONFIRMED — demo token cure in G-M + BANK-D5 | RESCOPED — clears WCAG 2.2 SC 2.5.8 (24px AA); 44 is house canon; producer ask only | **O wins — RESCOPED; no WCAG claim may be cited for the at-rest mobile controls** | SC 2.5.8's AA floor is 24×24 CSS px; the measured at-rest band (26–33px, both workers' cells agreeing) CLEARS it. The genuinely sub-24 controls (three 22×22) belong to the ACTIVATED slug layer and are already booked at C-33≡D2-04 (DELTA-6, G-M) — double-booking them here would inflate the count. The 26px rung's source (PaneSegmentedControl's 639px fork, my read :46-51) dies with W47's owned retirement (R-9). Residual: a MINOR house-canon (PROPORTION-AUDIT/HIG 44px) row + the producer hit-seat token ask — **BANK-D5 kept**, per-site padding bumps stay banned (PR-12). G-M's scope is corrected accordingly (§7.4). |
| **R-17** | **D3-03's verdict label** (no roving rail) | CONFIRMED (cure = producer; BH ask + BANK-D4) | GLASS-OWNED | **O's label wins — GLASS-OWNED; dispositions were already identical** | The rail is GlassDock/DockLayerGroup behaviour; VC:129 names the component but the owner's glass-first edict bars a demo keydown graft (both workers agree). Verdict label matters for closes: a GLASS-OWNED row is not a value.js defect count. Per the coordination law this row cites **INBOX I-20** (playwright-webkit ≠ real Safari; the challenger's keyboard cells are chromium) and rides the standing BH/BI relay fond + BANK-D4's runnable re-trigger. The constitution obligation stays OPEN against the producer. Tab order itself is sound (worker-O's chromium cell; consistent with D2-12). |
| **R-18** | **D2-06** (view-hue identity degenerate in dark; delete the accent family?) | RESCOPED → convergent design loop with cells (R-11) | CONFIRMED MAJOR — "the wave finishes the argument" | **F wins — RESCOPED to the design loop; O's measurements ADOPTED as the row's cells** | `DockViewSelect.vue:40-48`, my read: the per-row hue legend was excised for near-zero discriminative information **with the trigger/seal hue naming explicitly kept as the ruled counterweight — "Q5 RULED", "(R1 SURVIVES)"** — and Dock.vue:163-166 carries W7-4 ("ONE dock voice") as owner canon. Deleting `accentHueShift`/the `--accent-view` family is therefore an OWNER call, not an adjudicator's; an arbiter confirming it as a defect-with-cure would re-litigate Q5/W7-4/T-28 without authority. O's cells (dark C 0.188→0.021 at identical hue; `--accent-view` ≡ `--accent-live` bit-identical on the default route — reproduced to the digit in two independent runs) travel WITH the row to task #7; if the loop rules FOR deletion, G-N and the seal-ink derivation survive unchanged. No close may cite this wave for it. |
| **R-19** | **D2-03≡D3-04 severity** | BLOCKER | MAJOR | **O wins — MAJOR; finding and G-L untouched** | Both grammars render and every control in both remains operable at 1024×1366 (both workers' cells; nothing unreachable). BLOCKER is reserved for capability loss (DELTA-1) and silent data-loss-class failures (DELTA-2). The predicate pile stays fully CONFIRMED with structure (h) + G-L. |
| **R-20** | **D3-06's two visual halves** (wrapped 55px GitHub row; read-through material) | CONFIRMED whole | boundary CONFIRMED; visual halves UNVERIFIABLE-HERE (untracked, unhashed frame — L-7) | **SPLIT — O's discipline wins on the visual halves; F's BUILD wins on the cure** | My runs: `git ls-files` over the shots dir and `audit/components/**/*.png` = **0 paths**; `grep sha256` over the dock audit docs = **0**; megatranche `.gitignore:10` ignores component PNGs — per L-7 the cited frame does not exist as a witness, so the two visual claims are **UNVERIFIABLE-HERE (missing arm: a committed, hashed `state-mobilemenu.png`)** and citation-gated on G-R. The boundary half is source-certain (both menus import `../../../ui/dropdown-menu` — my reads of MobileMenuDropdown:7-11 and ProfileSection:10-12) and the cure is executable TODAY (`./dropdown-menu` in the installed 7.0.0 exports map — my census), so **G-O stands born-RED** with its structure arm; its row-geometry product arm re-measures fresh at execution rather than trusting the unwitnessed frame. |
| **R-21** | **L-36's home** (18 root-barrel imports) | rewrite lands in this wave's hygiene commit | whole row → W-HYGIENE ("not half-fixed here") | **SPLIT** | The TWO dock sites (ColorInput.vue:117, SlugEditLayer.vue:6 — files this wave already edits) ride the G-Q hygiene commit; the 16-site repo-wide sweep goes to W-HYGIENE with the id preserved. Neither doubles the other: the W-HYGIENE fold notes the two-site head start. Severity bound re-affirmed: module-graph/dev-transform + surface-contract cost only (`sideEffects ["*.css"]`) — never a shipped-bytes claim. |
| **R-22** | **Gate letters** — the two records mint COLLIDING letters (F: G-J=login, G-K=focus, G-N=selected-state, G-Q=hygiene · O: G-J=registry, G-K=nav-names, G-L=login, G-Q=witnesses) | — | — | **CANONICAL TABLE FIXED: §6.5's lettering (worker-F's, already in this registry) governs — G-A..G-H r2-carried · G-I mobile reachability+zoom · G-J/G-J′ login · G-K focus+announcement · G-L one predicate · G-M register floor (scope per R-16) · G-N selected state · G-O producer menu tier · G-P backdrop · G-Q hygiene — PLUS G-R minted (§7.4)** | Same-letter collisions across records poison closes (a "G-J GREEN" claim would be ambiguous). §6.5 is the standing registry text, so its letters win by seniority (L-5's spirit); worker-O's gates map onto them 1:1 (his G-J→G-I+structure(f) · G-K→G-C/G-D label arms + G-N · G-L→G-J/G-J′ · G-M→G-M · G-N→G-L · G-O→G-P · G-P→G-K · G-Q→**G-R**, the only gate §6.5 lacked). Every worker-O gate amendment survives the renaming; none is dropped. |
| **R-23** | **Witnesses & priorities** worker-O additions absent from §6.5 | π-obligation prose only | a runnable witness GATE + a commit-priority ruling + BANK-D6 | **O's three additions ADOPTED** | (i) **G-R (the witnesses exist — L-7)**: every frame cited by name in this document is either `git ls-files`-tracked under `audit/components/shell-dock-dock/witness/` or has its sha256 recorded in this committed file — RED today (my runs: 0 tracked, 0 sha256); worker-O's loop-over-cited-frames command adopted verbatim from his record. (ii) **Commit priority**: if only ONE commit of the sequenced set ever lands it MUST be commit 1 (registry + login truth) — the only cure no other wave can reach; four separately-filed BLOCKERs collapse into it. (iii) **BANK-D6** (producer backdrop fallthrough): runnable — assert `De()`-equivalent falls through to the `[data-glass-field-canvas]` document convention on a null-yielding getter in the installed dist; silent today; on hit, the G-P producer ask is discharged and only the demo threading remains. |

## 7.3 · Spot-checks of suspicious agreement (all held)

Where the workers agreed to the digit, the agreement was audited rather than trusted: (1) **DELTA-2's three swallows** — my full reads reproduce every line (:54 un-awaited inside try / :57-62 unreachable substring sniff vs the correct typed branching stranded at useSlugMigration:83-87 / `slugError` written 7× rendered 0× / `PaletteSlugBar` 4 refs 0 mounts). (2) **The command seam** — App.vue:83-91 mobile PaneSlot bare of `:on-mount` vs :105/:131; the nine `paneRefs.<view>.value?.<cmd>?.()` handlers; the 40×50ms poll + warn; `EDIT_TARGET_KEY` provided at App.vue:270. (3) **C-3's mechanism** — useDockAdminMode:34-39 two-arm computed, userViews omitting 7 of 14. (4) **L-23** — `demo/@` ENOENT vs four dead globs. (5) **L-34** — exactly 5 `meta:{admin:true}` literals. (6) **D2-16** — hide-indicator ×2 + P4-R4 comment. (7) **C-30** — banner + dangling ref, zero `## 6` headings. (8) **dist truth** — 7.0.0 ships `./dropdown-menu`/`./dom`/`./dock`. (9) **G-A** — `void opts.` = 1 at :67, RED. (10) **The false 2.5rem comment** — ActionBarToggle:147-153 verbatim. No echo detected; both workers' convergent numbers trace to source truth.

## 7.4 · THE WAVE — binding corrections to §3+§6.5 (read §3, §6.5 and THIS section as ONE spec; §7.4 wins conflicts)

**MT-DOCK-LAYERS-1**, one standing name, born-RED, four sequenced independently-green-able commits (registry+login · r2 core · layout+register+focus+backdrop · hygiene), **commit-1 priority per R-23(ii)**. Corrections to §6.5: (a) **G-M's scope narrows per R-16** — its floor arm governs the ACTIVATED slug layer (the three 22×22 controls + the nameless input, via the producer's `--dock-compact-control-*` seam + a real accessible name) and does NOT assert 44px against the at-rest mobile band; the house-canon 44px question rides BANK-D5. (b) **G-I's collision arm re-worded per R-13** — it asserts capability PRESENCE at 390×844 and 720×900 (≡1440@200%), never occlusion; the phantom-rect residual joins the G-Q hygiene script (assert the presence slot's child reports zero-area geometry while clipped, or the clip is lifted with the slot). (c) **G-R ADDED per R-23(i)**, RED today. (d) **BANK-D6 ADDED** beside D1/D3/D4/D5. (e) The carries table's `D3-11 → visual-audit harness` line is DEAD (R-14 — refuted, nothing to re-run); `D3-05` moves from the DELTA-6 BUILD list to RESCOPED (BANK-D5 + W47); `D3-03` re-labels GLASS-OWNED citing INBOX I-20. (f) All §6.5 gate commands, structures (f)-(j), delta obligations (8)-(16), banks D4/D5, and the R-8..R-12 seam rulings stand otherwise unchanged. Every gate remains RED today by this seat's own re-runs or the workers' agreeing cells carried labeled; no deferral exists anywhere in the spec ("next tranche decides" appears nowhere; every surviving defect maps to a gate or a named in-formation owner).

## 7.5 · Addendum clause — **MT-DOCK-C** (E-3; appended, never rewriting §6.6's MT-DOCK-B or the worker records)

1. **THE TRI-FOLD'S YIELD, stated for the close**: over 58 rows the two workers disagreed on verdict LABEL eleven times and on operative MECHANISM three times (collision, forced-colors, the D3-07 canon read); every mechanism disagreement was resolved by source or by one probe, and in each case the worker who had RE-DERIVED rather than ADOPTED won — worker-O twice (R-13, R-14), worker-F once (R-15/R-18 on canon-authority grounds). The lesson is the formation's own L-2 restated: adoption of a challenger's framing without re-derivation is where false mechanisms enter the registry.
2. **UNVERIFIABLE-HERE LEDGER (L-12), consolidated**: D3-06's two visual halves and D2-07's rendered RTL frame (missing arm: committed+hashed witness — G-R); the authed `viewEntries` collapse and authed Profile pill (missing arm: a session against a live API; dev :9000 is API-LESS); the production live-region absence (source-certain from `status-lamp.ts:48`; missing arm: a built bundle — G-K(iii) runs against it); D3-02's collapsed-seal keyboard reachability (declared unmeasured by worker-O — the first execution session measures it before accepting the rescope); C-31's producer line attribution (dist-verified mechanism, instrumented trace carried labeled).
3. **STRUCK AND UNCITABLE, consolidated** (additions to §6.5's list): D3-01's occlusion vocabulary (R-13) · D3-11's engine mechanism and its ordered re-run (R-14) · any WCAG citation against the at-rest mobile dock controls (R-16) · "7 of 15" as the anonymous-nav denominator (it is 7 of 14) · any close counting D3-03 as a value.js defect (GLASS-OWNED, I-20).
4. **MODEL RECEIPTS, chain complete (M-12)**: workers `claude-fable-5` + `claude-opus-5[1m]`, arbiter `claude-fable-5`, all observed in-runtime and declared at their returns' heads; the six r3 challenger seats all carried `claude-opus-5[1m]` receipts (§6.1). No seat's tier was inherited from a spawn label.
5. **PROBE PARSIMONY RECEIPT**: this arbitration spent exactly ONE browser-engine probe (R-14's two-engine cell, ~10s) — every other ruling was decided from source, dist, git, and the constitution. The probe was spent only because two workers directly contradicted each other on an empirical claim, which is the one circumstance where a probe is cheaper than an argument.

## 7.6 · DISSENT (preserved verbatim in substance; unresolved by design)

1. **Worker-O on wave size** (his dissent 1, standing): sixteen-plus gates across the sequenced commits "sits at the outer edge of what L-1 permits given a measured 38% delivery rate and a standing formation at 2% executed"; he restates his overruled r2 split argument in its sanctioned form — split at the commit boundary, never a second name, commit 1 first. The arbiter AFFIRMS the sanctioned form and preserves the tension: if execution stalls mid-wave, the close records which commits landed and the rest re-enter as the SAME wave resumed, not a new identity.
2. **Worker-O on the housing question** (his dissent 2, standing): a reasonable arbiter could move the slug/auth surface to the Account Dialog NOW (VC:222 canon), deleting the slug layer, the 22px controls, the nameless input and the 162px jump in one move, instead of the interim G-M cure + W23 fold this document rules (R-10). This arbiter holds the R-10 line — W23 owns the composition per the constitution's own W-map — but records that the opposite ruling is coherent; the hedge stands (G-M's arms travel to W23 verbatim if the Dialog lands first; if W23 does not carry them, the rows return here).
3. **Worker-O on D3-02 under-calling** (his dissent 3, standing verbatim in substance): the collapsed 56px seal's keyboard reachability and accessible name were never measured; if it is unreachable/unnamed, a mouse user's dock becomes an unlabelled circle hiding all four navigation seats — a real defect, not a design decision. The first execution session MUST measure this before accepting the design-loop rescope; the arbiter binds that ordering.
4. **Worker-F's solo-round caveat** (§6.7 item 6) is DISCHARGED for this corpus: this §7 is the tri-fold pass his §6 sanctioned, and L-14 is now satisfied over the r3 delta.
5. **The arbiter's own dissent**: R-18 (D2-06 to the design loop) is the ruling this seat is least certain of. Worker-O's argument — that DockViewSelect's own excision comment condemns the surviving trigger/seal hue mechanism by the deleted legend's exact rationale — is strong, and "Q5 RULED / R1 SURVIVES" may record a narrower ruling than this seat read it as (the comment's "R1" may be the legend review, not a standing defense of the trigger hue). If the design loop finds the owner never ruled the TRIGGER hue specifically, D2-06 returns as a CONFIRMED MAJOR with worker-O's cells and the wave gains its arm. Recorded so the loop knows exactly what to check: the Q5 ruling's actual scope, in the T-round record.
