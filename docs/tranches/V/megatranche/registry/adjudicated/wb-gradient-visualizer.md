# wb-gradient-visualizer — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-10 · **HEAD**: `54a97489` · branch `tranche-u` · **Corpus**: `demo/workbenches/gradient/` whole (GradientVisualizer.vue 279 · GradientStopEditor.vue 392 · GradientCodeEditor.vue 116 · GradientEasingEditor.vue 295 · easing/{EasingAuthoringStage.vue 116, EasingSpecimenStrip.vue 215, easingCatalogue.ts 230, useSpecimenRows.ts 74} · composables/{gradientParse.ts 301, useGradientCSS.ts 334, useGradientInterpolation.ts 56, useGradientModel.ts 193} · GradientPane.vue 29 — 2,630 lines) plus the contract surfaces it rides (App.vue boundary seat · usePaneRouter · usePalettePorts · color-session adapters · src/easing.ts · src/color/operations.ts · src/foundation/math.ts). Challenge corpus as found in the component directory: **12 files across three axes** — C-implementation (r4 governs, + r2 corrections in-corpus), D-design (17:36 r-final governs), L-library (r4 governs, + r2/r3/pass-1) — with `evidence/` and `probes/` beside them.
**Re-readers adjudicated**: design-and-usage (11 confirmed · 10 killed · 5 missed · 6 unproven · 8 superlatives) · library-and-code (22 confirmed · 7 killed · 6 missed · 7 unproven · 7 superlatives). Both were attacked before adoption: every kill re-derived against the tree, every promoted miss re-walked in source with this seat's own instruments, one reader consequence-limb killed by this seat against installed framework bytes (K-18).
**Subject drift**: `git diff --stat c654824e HEAD -- demo/workbenches/gradient/ demo/color-picker/App.vue demo/shell/usePaneRouter.ts demo/palettes/usePalettePorts.ts src/easing.ts src/color/operations.ts src/foundation/math.ts eslint.config.js tsconfig.demo.json package.json test/gradient-v4-consume.test.ts test/gradient-parse.test.ts demo/styles/utils.css` → **empty**. Every corpus and reader line anchor remains valid at HEAD.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling)**: full reads of all 13 subject files + GradientPane.vue + PaneHeader.vue:15-25 + App.vue:20-58 + usePalettePorts.ts:1-22/:243/:272 + usePaneRouter.ts:69-78/:105-112/:205-213 + useGradientInterpolation.ts whole + EasingAuthoringStage.vue:1-60; library reads: src/easing.ts:40-66 (**quart ×3 + quint ×3 present in `PRESETS`; back family y-coords −0.55…1.55**), :116-129 (`CubicBezier` validates x1/x2 ONLY), src/color/operations.ts (**`color_missing_channel` for non-hue `none`; `color_missing_alpha` for `/ none`; `color_progress_out_of_range` guard in both `interpolateHue` and `mixColors`**), src/foundation/math.ts:113-119 (`toFixed(2)`); test reads: test/gradient-v4-consume.test.ts whole (**`@src/subpaths/css` beside demo-dist imports; `easingFnOf({css})` exercises all four `timingFunctionValue` branches + `linearStops`; `SPECIMEN_TILES.length > 20`**), test/math.test.ts:405-436 (2dp law pinned ×6), test/v4-c1.test.ts:334 (`cubicBezierToString` in the pinned `/math` surface literal); installed glass-ui 7.0.0 probed by node: `fading-scroll-DhxXIhm2.js` (**unconditional `tabindex: "0"`; `role: o.value ? "region" : void 0`**), `components/labeled-field/types.d.ts` (**`LabeledFieldProps` whole incl. the `controlLabelable:false` reka-Slider-span door**), `components/_shared/selection.d.ts` (**`SelectionValue = string | number`**) vs `components/select/index.d.ts` (**not re-exported**) vs root `index.d.ts` (**0 hits**), `components/dock/DockControl.vue.d.ts` (**`grep -c title` → 0**; `__VLS_Props` = shape/compact/active/type/disabled/as/asChild/class), producer package.json exports (**`./select ./slider ./dock ./dom ./chip ./fading-scroll ./labeled-field ./easing` all first-class**), `glass-ui.css` slider focus rules (**`:not([data-variant=spectrum]):focus-within .slider-track{box-shadow:var(--focus-ring-shadow)}` — standard HAS a branded focus indicator; `outline:none` lives ONLY behind the spectrum thumb gate**); `@vue/runtime-core` dist bytes for the K-18 decider (**`ErrorCodes.ASYNC_COMPONENT_LOADER = 13`; `handleError` walks parent `errorCaptured` hooks (`cur.ec`) for EVERY type; the async loader's no-`onError` branch rethrows into the chain that calls it**); repo censuses: `LIBRARY_PORT_KEY` injections = **5** (Mix/Generate/Palettes with `!`, GradientVisualizer:30 + MixSourceSelector:33 without), `ls demo/ui` = 19 barrels (select+slider from the ROOT barrel, read), `demo/@` absent + eslint.config.js:204-208 (src-only live rule) + :232-238 (dead `demo/@` globs beside live `demo/color-picker` globs) + :274-276 (fully dead composables object) — **no object selects `demo/workbenches/**`**; tsconfig.demo.json:42-49 (8 hand-written paths: phantom `.`→`dist/index.d.ts` + `/parsing` + `/units`; `/css` + `/value` omitted; "CLOSED 8-key set" comment false by one) vs `package.json#exports` = 7 keys vs `ls dist/subpaths` (no dist/index.d.ts); demo/styles/utils.css:8-18 (**:13 is a COMMENT in `.section-subtitle`'s docstring; the real `.section-label` recipe is glass-ui `typography/utilities.css` incl. the `[data-paper-field]` light-scheme override — read**); PaneHeader.vue:21 `<h3 class="pane-header-title font-display">`; hand-executed derivations: the R4-2 relocation (`green -20%` in a 3-stop list → prev=0, next=100, span=2 → **50** — the corpus's pasted value reproduced by this seat), the quart/quint drop arithmetic (30 bezier presets + 3 steps = 33 tiles; 6 filtered = **27 rendered**, and 27 > the gate's 20), and the chip-expander geometry (below); motion-quarantine.md M-17 + §cascade + §5 S-1/S-2 re-read; **X wave files W0/W1/W3/W4/W5/W6/W8/W10 read for routing**: X-W6 §4 bounds hold the gradient triad + GradientPane + composables + easingCatalogue + EasingAuthoringStage (verified row-by-row), X-W4 gates D1/D2/D4 name `usePaneRouter`'s `any` refs and optional-chained dispatches by line, X-W5 bounds hold App.vue + PaneSlot + usePaneRouter, X-W8 owns the 19-barrel extirpation + the eslint carve and **declines the tsconfig parity row at W8.md:112**, X-W10 bounds hold PaneHeader.vue.

## RULING PINS (this seat's own derivations)

1. **X-W6's bounds table carries two errata and one hole, and the hole sits under both BLOCKERs.** (a) The bounds row spells `GradientVisualizer/easing/GradientEasingEditor.vue`; the live file is `GradientVisualizer/GradientEasingEditor.vue` (the `easing/` dir holds the stage, strip, catalogue and useSpecimenRows) — a unit string-matching its bounds would halt on a phantom path. (b) **`easing/useSpecimenRows.ts` is in NO bounds table anywhere in tranche X**, yet it is a live render-throw path for both BLOCKERs (`interpolateStopColors` at :53-59 throws on `!mixed.ok`) and a mandatory touch of X-W6.c's "one `sampleAt`" consolidation — the cure cannot complete without editing it; §3a's expansion trigger fires on day one unless the bounds are amended at wave open. (c) **`GradientCodeEditor.vue` and `EasingSpecimenStrip.vue` are outside every X bounds table** (this seat's grep across W0–W11): every row landing in those two files is NO-WAVE-OWNER regardless of how gradient-shaped it looks.
2. **The two BLOCKERs are one disease in two organs: the validity oracle is never the render predicate.** Organ 1: `CubicBezier` legitimately admits y∉[0,1] (src/easing.ts:123 checks x only; back-family y-coords −0.55…1.55 read by this seat) while `mixColors` legitimately rejects progress∉[0,1] (operations.ts guard read) — and the demo forwards eased t straight in. Organ 2: `parseCssColor(token).ok` (gradientParse.ts:92-94) accepts CSS Color 4 `none` channels that `convertColor`/`mixColors` then reject (`color_missing_channel` / `color_missing_alpha`, bytes read; hue-`none` alone is tolerated — operations.ts's hueIndex carve-out). Both detonate in Vue computeds. **The throw-door census is SEVEN, not five**: `coalescedCSS` / `railRampCSS` (useGradientCSS.ts:325/:327 → :202-208), `parseColorIn` inside the same walk (:192-193), `ghostColor` (GradientStopEditor via the `colorAt` prop → GradientVisualizer.vue:64-88, three throw statements), `useSpecimenRows` ink (:53-59), **and `openIntervalRamp`** (GradientEasingEditor.vue:63-67 → `serializeIntervalRamp` :253 → the same :206-208 throw) — reader-2's found sixth door, which is the one wired to the accordion row that is **open by default** (`openInterval = ref(0)`, :61). The C axis's "a try/catch at any of the five would leave four open" arithmetic is off by one and its cure-sizing inherits the error.
3. **The D/F-1 cure transposition is dead; the sound cure is already spelled in X-W6's own gates.** Reader-1's kill re-derived by this seat: gradientParse.ts:295-298 re-seeds EVERY parsed interval to `linearInterval()`, so the "serialize the coalesced form, the panel round-trips itself" cure provably cannot round-trip (parsing its own output yields N−1 linear intervals), and the baked string is a 32-sub-stop machine artifact in an authoring editor. The surviving cure = express the model completely (the ` in <space> <hue> hue` clause — X-W6.c names it) AND stop re-seeding intervals when stop topology is unchanged — which is exactly X-W6.a's gate a1 ("an authored easing never migrates"). The defect stands BLOCKER; the corpus's prescribed fix would have shipped a second one.
4. **K-18 (arbiter kill, framework bytes): the "import-time throw no ErrorBoundary can catch" limb is not supportable.** Reader-2's missed #3 claims the module-eval throw in `easingCatalogue.ts:198` (`buildFamilies()` at top level; `easingValue` throws on any future preset with x∉[0,1]) "surfaces as a chunk-load failure … App.vue:50's boundary never sees it". This seat read the installed `@vue/runtime-core` bytes: a loader rejection with no `onError` rethrows into the chain that calls `handleError(err, instance, 13 /* ASYNC_COMPONENT_LOADER */)`, and `handleError` walks parent `errorCaptured` hooks for **every** error type — the same walk that feeds ErrorBoundary.vue. The latent contract-assumption row survives at MINOR (the demo silently assumes every future `bezierPresets` member keeps x∈[0,1] — a guarantee value.js does not make, and its y-coords already break the analogous assumption); the uncatchable-consequence limb dies.
5. **The remove chip's coarse-pointer hit expander owns a 6px × 44px band of the rail, and no axis ever hit-tested chip against rail.** This seat's geometry from source: chip seat `top-11` (44px) + `h-6` → visible box y=44→68, centre y=56 (GradientStopEditor.vue:288); coarse expander `var(--touch-target, 2.75rem)` = 44px centred (:385-391) → y=34→78; the rail is `h-10` → y=0→40 (:203). Overlap band y=34→40 under the selected handle. The chip is a z-20 SIBLING of the rail (:284 outside the rail div, :288 `z-20`) in the same stacking context as the handles' z-10, so the band hit-tests to the chip, events never bubble through the rail, and the rail's add-guard (`closest("[data-stop-id]")`, :87) cannot see it — a coarse-pointer tap aimed at the rail's bottom edge beneath the selected handle fires `removeStop` (:297). Fine pointers exempt (expander = 24px = the chip box). The same geometry proves reader-1's fourth miss: the stop-editor root's only flow child is the 40px rail, so the chip (y=44→68) straddles the `<hr>` its parent column places at y=60 (`gap-5`, GradientVisualizer.vue:135/:148) by 8px whenever a stop is selected with 3+ stops.
6. **Identity guards (anti-rename).** The dead-lint-lattice row ≡ ColorSpaceSelector L-5(a) ≡ MX-25 — booked to the X-W0/X-W1 fold at the wb-extract-controls apotheosis; the tsconfig-paths row ≡ ColorSpaceSelector L-14 ≡ XP-21 ≡ MX-20 — **banked NO-WAVE-OWNER (X-W8 declined it by name, W8.md:112)**; the 19-barrel row is X-W8's own G-1/G-2 subject. All three are cross-referenced here with this component's sharpenings, never re-booked.

## Scoreboard

**40 adjudicated defect rows: 3 BLOCKER · 21 MAJOR · 14 MINOR · 2 INFO** — all 33 reader-confirmed rows survive re-derivation (several rescoped in place), all 11 reader-found misses admitted (one with its consequence limb killed, one merged as a sharpening). **18 killed claims**: all 17 reader kills UPHELD on this seat's re-derivation, plus 1 minted by this seat against a reader's own promoted miss (K-18, PIN 4). Headline: **a first-class instrument whose catalogue sells three curves that crash it, whose CSS panel certifies success while destroying the very easing the pane exists to author, whose validity oracle accepts colors its renderer cannot draw — seven throw doors under one app-wide boundary — and whose owning wave's bounds table misspells one cure file and omits two more.**

## Ruled disagreements and reader-challenges (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **L4-7 "sole outlier" census** | L axis: 4 injection sites, this component the sole unasserted one; reader-2: killed (5 sites, MixSourceSelector also bare) | **kill CONFIRMED by this seat's own grep** | `grep -rn LIBRARY_PORT_KEY demo/` → exactly 5: MixPane:16 `!` · GeneratePane:11 `!` · PalettesPane:164 `!` · GradientVisualizer:30 bare · **MixSourceSelector:33 bare**. Both bare sites are inside X-W6 §4 bounds (rows verified), so the cure covers both in one wave. |
| **"Copy CSS is nameless"** | D axis + L r2/r4: nameless button, REPORT-corroborated; reader-2: killed via C-r2's CDP AX read | **kill UPHELD, twice-instrumented** | C-r2:46-63 pasted `{"name":"Copy CSS","nameFrom":"attribute=title"}` + 0 empty-name buttons in `<main>`; this seat adds the mechanism: DockControl.vue.d.ts has **no** `title` prop (grep 0) — the name is an undeclared attribute fallthrough that accname step F resolves. Survives as GV-30 (MINOR): named only by `title` — invisible to sighted keyboard users, suppressed by some AT. |
| **Stop-handle tap targets** | D §5 PR-12 + L4-6 heading: 20×20, "every control under the minimum"; readers: both killed | **kills UPHELD; the NEW overlap geometry booked instead** | GradientStopEditor.vue:361-391 read whole: always-on centred `::before`, `max(1.5rem,100%)` fine / `var(--touch-target, 2.75rem)` coarse, `pointer-events` auto, absolute (mount box held — why `getBoundingClientRect` reads 20×20). C-r2:88-113 hit-tested it live and retired the rows a day before the L r4 re-assertion. The real defect neither axis found is the chip-vs-rail overlap — PIN 5, GV-22. |
| **D/F-5 reduced-motion inversion** | D axis: MAJOR product-wide inversion; reader-1: killed via motion-quarantine | **kill LAWFUL and UPHELD (M-17 class)** | motion-quarantine §cascade (re-read): under `reduce`, Guard 2 rewrites the transition property list to colour/opacity legs only — `transform`/`max-height`/`border-radius`/`filter` are deleted, not shortened; the 250 count is elements carrying a duration, none spatial. M-17 rules the same claim-shape FALSE; S-1/S-2 record the interaction as a superlative. No FALSE quarantine row is carried anywhere in this record. This component's own subtree ships a correct PRM branch (EasingSpecimenStrip.vue:74, read). |
| **easingFnOf parse tail** | L r4 + r1 L-4: dead code, delete the `Partial<>` and the tail; C R4-4: coverage defect, reachable only from a test; reader-2: killed the delete-cure | **kill UPHELD — C's framing adopted** | test/gradient-v4-consume.test.ts:39-53 read by this seat: `{css:"ease-in-out"}` / `cubic-bezier` / `steps(4, jump-end)` / `linear(0, 0.25 50%, 1)` exercise all four `timingFunctionValue` branches + the 26-line `linearStops` arithmetic, assert `0.125` and the throw path. Deleting the tail breaks 6 assertions. Booked GV-20: a gate whose subject the product never executes is a coverage defect with a different cure. |
| **cubicBezierToString retirement cost** | L r4: "costs nothing but the export line and its test block"; reader-2: killed (semver) | **kill UPHELD** | `./math` is a key of the published exports of value 4.0.0 (immutable); test/v4-c1.test.ts:334 pins the surface as a sorted literal; test/math.test.ts pins the 2dp law ×6 (read). Removing a named export from a shipped subpath is a major-version decision — GV-12 routes as a coordination row, not a patch. |
| **R2-1 "three copies"** | L r2: three independent implementations, "all three delete"; reader-2: killed (two callers of one helper) | **kill UPHELD** | GradientVisualizer.vue:79 and useSpecimenRows.ts:53 both call `interpolateStopColors`; the only mix-unwraps in the gradient tree are useGradientCSS.ts:202-208 and useGradientInterpolation.ts:36-37 — matching r3's own six-site census. Crash count unaffected; cure sizing corrected. |
| **Module-eval throw catchability** | reader-2 missed #3: "no ErrorBoundary can catch it"; this seat: challenged | **K-18 — consequence limb KILLED, row survives MINOR** | PIN 4; runtime-core bytes pasted in the instruments line. |
| **Dead-lint severity** | reader-2: BLOCKER; sibling records: identity cross-ref | **booked at the banked identity, dissent preserved** | The gate fact is program-wide (no `no-restricted-imports` object reaches `demo/workbenches/**` — this seat's read of :204-276); the identity is ColorSpaceSelector L-5(a) ≡ MX-25, already folded. Reader-2's sharpening stands: the census shows the demo's heaviest cross-boundary edge (GV-5) walked straight through the unguarded seam. |
| **R4-2's relocation numbers** | reader-1: flagged as the one MAJOR whose numbers nobody replicated | **replicated by this seat, statically** | Hand-execution of gradientParse.ts:266-279 on `red, green -20%, blue`: green pushed at −20 → auto-fill sees `<0` → prev=0 (red, :266), next=100 (blue, :267-269), span=2 → **50**. And `-0 < 0` is false in JS, so `-0%` pins to 0 while `-0.0001%` relocates to the run midpoint. Removed from residue. |
| **Reader anchor errata** | — | corrected in place | Reader-2's own kill of L's citation drift confirmed: the `v-for :key` is GradientStopEditor.vue:**231**; the second minter opens at gradientParse.ts:**31**. Neither moves a verdict. |

## Adjudicated defect roster

Line anchors are HEAD `54a97489`; re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: an X-wave id means that wave's §4 bounds own the surface today (verified row-by-row by this seat); **NO-WAVE-OWNER** rows are booked for the next formation boundary; GLASS-OWNED rows leave by the standing BH relay.

### BLOCKER

- **GV-1 — L/R2-1 ≡ R2-1: selecting a `back`-family specimen tile — first-class, always-rendered, one click from landing — throws `color_progress_out_of_range` from render computeds and erases both panes.** Chain verified whole by this seat: src/easing.ts ships the three back quads with y ∈ {−0.55…1.55}; `CubicBezier` (:116-129) validates x only, so the evaluator legitimately leaves [0,1]; easingCatalogue.ts:174/:191 renders them as pressable Chips (`back` ∈ FAMILY_ORDER); tile → `updateInterval` stores `fn` verbatim (useGradientModel.ts:134-140); every sampler forwards `easing(t)` into `mixColors`, which rejects progress∉[0,1] (operations.ts guard, bytes read). **Seven throw doors (PIN 2), including the default-open accordion row's.** Blast radius = both panes + all in-pane state, dock survives (App.vue:23-50 read: nav outside `<main>`, one ErrorBoundary inside — the whole-app limb killed below). The eased-ramp concept has no library owner: both contracts are individually correct; their composition is published nowhere. → consumer cure (clamp/total sampling + one `sampleAt`): **X-W6.a/.c** — with the PIN 1(b) bounds amendment (useSpecimenRows.ts) as a precondition; catalogue exposure: **X-W6.d**; the library-capability question (an eased-mix that owns its domain): **X-W9 coordination note, else NO-WAVE-OWNER**.
- **GV-2 — C/R4-1 ≡ R4-1: the validity oracle is not the render predicate — a spec-valid `none` channel parses `ok:true`, `parseVerdict` clears, and the pane dies from the same seven doors.** gradientParse.ts:92-94 validates with `parseCssColor(token).ok`; render resolves through `parseColorIn`/`mixColors`, which throw `color_missing_channel` / `color_missing_alpha` (operations.ts read; hue-`none` alone tolerated — the asymmetry is real and located). GradientVisualizer.vue:107 asserts success (`parseVerdict = null`) while the surface unmounts. Independent of GV-1 and GV-3; the only closing cure is making the render operation the oracle (or a total render). → **X-W6.a** (gradientParse owned) **+ X-W6.c** (the sample law), same PIN 1(b) precondition.
- **GV-3 — D/F-1: one keystroke in the CSS panel silently destroys every authored easing curve.** All four links live: GradientCodeEditor.vue:55-62 debounces every input 500ms and emits the whole text → GradientVisualizer.vue:102-108 applies unconditionally, no announcement → useGradientModel.ts:158-167 swaps `intervals` wholesale → gradientParse.ts:295-298 re-seeds every interval linear. The panel cannot show what it destroys (`simpleCSS` carries no easing — serializeGradient :147-162). No undo, no confirm; D's live reproduction (a single trailing space → `easingSurvived: false`) stands. Aggravator (C-r2, verified): the debounce is never `.cancel()`ed and panes are KeepAlive'd — a pending parse fires into the cached tree the user left. **Cure per PIN 3** (the corpus's coalesced-form transposition is killed). → model half (topology-stable intervals + complete serialization): **X-W6.a gate a1 + X-W6.c**; editor-surface half (announce/confirm, cancel-on-deactivate): **NO-WAVE-OWNER** (GradientCodeEditor.vue in no bounds — PIN 1(c)).

### MAJOR

- **GV-4 — r1 L-1: no import-boundary lint reaches this workbench; the demo-side restriction objects select a deleted tree.** This seat's read: live rule = src/ only (:204-208); :232-238 mixes dead `demo/@` globs with live `demo/color-picker` globs; :274-276 fully dead; no object selects `demo/workbenches/**`; `demo/@` absent. **Identity = ColorSpaceSelector L-5(a) ≡ MX-25 — cross-referenced to the X-W0/X-W1 dead-lint-lattice fold + X-W8.b's eslint carve, not re-booked.** Reader-2's BLOCKER dissent preserved (PIN 6 / table).
- **GV-5 — r1 L-2: one VALUE import (`LIBRARY_PORT_KEY`, GradientVisualizer.vue:27) drags the auth/transport closure into a gradient ramp editor.** Payload verified: usePalettePorts.ts:1-19 imports the three auth composables + 12 palette composables; the key declared :272, provided :243. The 72-module delta is inherited measurement (residue); the edge and payload are this seat's own reads. Census correction rides (both bare-inject sites W6-owned — table). r3's caveat stands: panes are code-split and ports provided in the main chunk, so no chunk-weight consequence. → **X-W6.a/.b** (move `seedFromPalette` to the pane seam or type the port; cover BOTH bare sites) + graph identity **X-W8**.
- **GV-6 — R2-2: the single ErrorBoundary wraps the whole pane grid — any one pane's render throw is a two-pane outage.** App.vue:47/:50/:140 read: one boundary inside `<main>` around the grid; `demo/shell/PaneSlot.vue` is the unused correct altitude. Both GV-1/GV-2 land under it. → **X-W5** (App.vue + PaneSlot in its bounds); the boundary's console-invisibility is **MX-7's identity** (ErrorBoundary.vue, NO-WAVE-OWNER) — cross-referenced.
- **GV-7 — D/F-6: reset does not reset.** GradientVisualizer.vue:118-125 touches stops/type/direction/space/hue/verdict, never `intervals`, never `selectedStopId` (:51); the only interval sync is the stop-count watcher (useGradientModel.ts:89-100) — 2→2 never fires; `setStopsFromColors` mints fresh ids, so the selection points at a destroyed stop. → **X-W6.a** (stop-owned easing makes reset total by construction; the second selection channel dies by name).
- **GV-8 — D/F-2 + D/F-9: the CSS dialect cannot express space/hue/easing, and the rejection blames the colour.** `serializeGradient` emits type/direction/raw stops only (:147-162); no `in <space>` production exists — `linear-gradient(in oklch shorter hue, …)` falls through parsePreamble (:139-154) into the stop loop and rejects at :236 with exactly `unparseable color "in"` (path re-walked by this seat). A pane whose largest heading is *Interpolation* rejects the standard CSS that expresses it. → **X-W6.c** (the ` in <space>` clause is named in its scope) + **X-W6.a** (parser production + honest reason).
- **GV-9 — R2-4: tsconfig.demo.json drifts five rows from `package.json#exports`, and the demo typecheck is structurally blind to it.** Re-verified at HEAD (instruments line): phantom `.`/`parsing`/`units` targets, `/css` + `/value` omitted while shipped and load-bearing here (useGradientCSS.ts:25-29, gradientParse.ts:21), the "CLOSED 8-key set" comment false by one. **Identity = ColorSpaceSelector L-14 ≡ XP-21 ≡ MX-20 — banked NO-WAVE-OWNER (X-W8 declined, W8.md:112); cross-referenced with this component's `/css` sharpening, not re-booked.**
- **GV-10 — R2-5: the Select emit is typed with reka's wider `AcceptableValue` because glass-ui does not export `SelectionValue`.** Producer facts this seat's own (instruments line): declared in `_shared/selection.d.ts`, absent from the select subpath index and the root. Three downcasts at :164/:181/:198. → producer export: **GLASS-OWNED → BH relay**; consumer downcast deletion: **X-W6.b rider**, discharging when the relay lands.
- **GV-11 — R2-6 + reader-2 missed #6 (merged): the "preserve public API surface" re-export block preserves a surface nobody consumes AND omits the one serializer with a live cross-module consumer.** useGradientModel.ts:19-29 re-exports 9 names; 7 have zero importers through the barrel (censuses corroborated); `serializeIntervalRamp` — the one with a real external importer (GradientEasingEditor.vue:31) — is not in it. A stale snapshot, not a facade. → **X-W6.c** ("the seven dead re-export doors deleted" — named in scope).
- **GV-12 — L4-1: `/math` publishes a bezier→CSS serializer that is unused, `toFixed(2)`-lossy, a fourth format, with a test pinning the wrong law.** src/foundation/math.ts:113-119 + the zero-product-consumer grep + the 2dp/3dp law split (easingCatalogue.ts:48-51 hand-mirrors glass-ui's `+n.toFixed(3)` across the boundary) all verified. Retirement is a **semver decision** (kill upheld — table). → **X-W9 coordination (published-surface change), else NO-WAVE-OWNER**; the demo-side literal-law duplication folds into X-W6.c's one-dialect work.
- **GV-13 — L4-4: seven import cycles inside the feature, all closing through `import type`; four modules import the orchestrator to fetch leaf domain types.** All four back-edges + both forward VALUE edges verified in source. Invisible to runtime signals by construction. → **X-W6.c** (leaf `model/types.ts` lands first, breaks both cycle edges — named).
- **GV-14 — L3-1: one `Result`, six unwrap sites, two contradictory failure policies, no owner.** The six-site census re-confirmed via the two gradient-tree unwraps (this seat) + the r3 census (corroborated); `sample.ts`'s `return null` is the masking outlier; `color-utils.ts` is the natural home. → gradient half: **X-W6.c**; the cross-feature one-home fold (mix/ink/sample/palettes sites): **NO-WAVE-OWNER rider** (sibling identity: MX-15 for the mix twins).
- **GV-15 — L3-4: `npm test` has no `pretest`, and the gradient consume test runs `src/` and `dist/` copies of the library in one process.** package.json scripts + vitest alias facts verified; test/gradient-v4-consume.test.ts:2 (`@src/subpaths/css`) beside demo-dist imports read by this seat. → **X-W1** (owns the gate slate).
- **GV-16 — r1 L-11: the shell reaches the gradient model through `Ref<any>` + optional-call chains with a rename across expose hops.** usePaneRouter.ts interface (`gradient: Ref<any>`) + the three `?.` dispatches (:205-213 region) read; GradientVisualizer exposes `resetGradient`, GradientPane re-exposes `reset` (:11-15). → **X-W4** — its gates D1/D2/D4 name these exact lines and count these exact `any`s (W4.md:405-408, read); mobile mount-parity limb is **X-W5**'s (bindPane), per W4's own gate note.
- **GV-17 — r1 L-6 / L4-3: nineteen pure alias barrels over glass-ui, all routing through the root barrel while the producer publishes per-component subpaths.** 19 dirs + select/slider root-barrel one-liners + the producer exports map — all this seat's own reads. → **X-W8** (owns it by name; its G-1/G-2 baselines are this row).
- **GV-18 — R4-2: the parser's `-1` position sentinel collides with an authored negative percentage — silent relocation, discontinuous at `-0%`.** Mechanism AND values now arbiter-derived (table): `green -20%` → 50; `-0%` → 0. The module's own header (:5-10) states the opposing doctrine. → **X-W6.a** (gradientParse owned; make the sentinel unrepresentable — `position?: number` or a tagged token).
- **GV-19 — L4-6 (library half) + the D-axis boundary rows: four hand-composed label+control pairs where the producer ships `LabeledField`, three per-instance `h-9` overrides, and a `grid-cols-3` with no responsive prefix at every viewport.** LabeledFieldProps read whole by this seat, including the `controlLabelable:false` door for reka's Slider span root — the exact threading being re-authored — and `LabeledSelectProps.items: readonly string[]`'s inability to carry per-item descriptions (why the hand-rolled `#description` exists — the caveat travels with any cure). → **X-W6.b**.
- **GV-20 — r1 L-4 ≡ L3-2 ≡ R4-4: `easingFnOf`'s `Partial<>` optionality is unreachable from the app — every in-app producer supplies `fn`; the 50-line parse tail is exercised only by the library's own test.** All app call-sites + producers verified. Booked as C frames it: a coverage defect (the delete-cure killed — table). → **X-W6.c** (the sampler reshape decides the seam) + **X-W1** (what the consume test is FOR gets stated in the gate slate).
- **GV-21 — D/F-10 ≡ C-15: `aria-invalid="true"` with no `aria-describedby`/`aria-errormessage` to its own verdict.** GradientCodeEditor.vue:92 vs :107-114 (verdict has `role="status"`, no `id`) — read. The user hears *invalid*, never *why* (WCAG 3.3.1). → **NO-WAVE-OWNER** (PIN 1(c) — the file is in no bounds table).
- **GV-22 — reader-1 missed #1 (PIN 5): on coarse pointers, a tap in the rail's bottom 6px band beneath the selected handle removes the stop instead of grabbing or minting.** Geometry this seat's own; fires in the normal 3+-stop authoring state; destructive, unconfirmed, no undo. → **X-W6.b** (the seat recomposition re-homes removal: "one removal owner with an explicit disabled state and its reason" — named in scope).
- **GV-23 — reader-2 missed #1: the ENTIRE quart and quint families — 6 of 30 shipped presets — are silently dropped by `FAMILY_ORDER`, while the module header swears the catalogue "IS value.js `bezierPresets` … never a second mint".** Verified end-to-end by this seat: presets present in src/easing.ts; `familyLabelFor` derives `quart`/`quint`; :191 filters them out; 27 of 33 tiles render; the gate (`> 20`) cannot see it; the corpus's own live tile enumeration (L-r2:94-96) corroborates by absence. It IS a second mint, lossy by 20%. → **X-W6.d** (easingCatalogue owned).
- **GV-24 — reader-2 missed #2: the SIXTH render-throw door (`serializeIntervalRamp` via `openIntervalRamp`) — the one wired to the row open by default — appears zero times in the twelve-file corpus.** Verified: useGradientCSS.ts:232-254 → :253 → :206-208; GradientEasingEditor.vue:61/:63-67. Sharpen: with `ghostColor`'s three visualizer throws the full door census is seven (PIN 2). → **X-W6.c/.d** — subject to the PIN 1(a) path erratum being corrected at wave open.

### MINOR

- **GV-25 — D/F-12: one 20px interval does all the spacing work; grouping is carried by three `<hr>`s instead of proximity.** `gap-5` at :135 is the file's only rhythm token, uniform across all ten body children — read. → **X-W6.b under X-W10 law**.
- **GV-26 — D/D-18: no h1/h2 on the route; the pane title and its three subsections are four h3 peers.** PaneHeader.vue:21 + GradientVisualizer :149/:241/:253 — read. Cure at PaneHeader (9 panes inherit). → **X-W10 rider** (owns PaneHeader.vue by bounds, W10.md:112 — but its spec touches scroll-timeline only; **named gap**: the heading-rank cure must be adopted there or this degrades to NO-WAVE-OWNER at execution).
- **GV-27 — L/R2-10 ≡ R2-10: dead cross-boundary DI edge at the mount point.** GradientPane.vue:6/:8 `inject(CSS_COLOR_KEY)!`, identifier used nowhere — read whole (29 lines). The `!` also converts missing-provider into silent undefined. → **X-W6.a** ("the dead injection" — named in scope).
- **GV-28 — D/F-3(b), narrow limb: the UA outline suppression is variant-gated in the producer, so a `standard` slider keeps the UA outline on a zero-width thumb box.** This seat's own read of glass-ui.css confirms BOTH halves: the `outline:none` lives only behind the spectrum gate, AND standard has its own branded focus indicator (`:not([data-variant=spectrum]):focus-within .slider-track` ring) — which is why the wide limbs died. → **GLASS-OWNED → BH relay** (one-line producer fix; no local `:deep`).
- **GV-29 — L4-5 ≡ R2-8 ≡ R4-3: two stop-ID minters with byte-identical format in two modules, and every handle press writes the selection twice through a `defineModel` nothing external binds.** Minters (useGradientModel.ts:66-69 / gradientParse.ts:31-34), the double write (GradientStopEditor.vue:129-130 → the v-model at :140 AND the emit at :144), and the binding-less mount (GradientPane.vue:25) all read. The collision itself remains unreproduced (residue). → **X-W6.a** ("the second selection channel" — named in scope).
- **GV-30 — L4-2 mechanism (post-R2-C1 downgrade): the Copy control's only name is an undeclared `title` fallthrough, and the demo speaks three naming conventions for one control species.** DockControl d.ts grep = 0 `title` (this seat); the three-convention census corroborated. → **X-W6.b rider** + **X-W4 coordination** (the semantic-controls register owns the convention).
- **GV-31 — r1 L-12: `railRampCSS` recomputes on `direction`, which `serializeRailRamp` provably never reads.** One invalidation cell for two domains — read (:266-275 vs modelState). Costs are inherited measurements (residue). → **X-W6.c**.
- **GV-32 — reader-1 missed #2: `touch-none` on the full-width rail refuses vertical pane scrolling for any touch that begins on it.** :203 read; gestures are clientX-only (:75-81/:136-146), so `touch-action: pan-y` restores the scroll at zero cost. → **X-W6.a** (gesture core).
- **GV-33 — reader-1 missed #3: the resting/selected handle ring is single-vocabulary white over the stop's own fill, while the file's focus ring documents the dual-contrast requirement 90 lines below.** :235-247 vs :328-349 — read; ghost twin repeats at :216. WCAG 1.4.11 on the protagonist's only handles. → **X-W6.a** — **already programmed by name** ("interim dual-contrast resting/selected ring generalized from the file's own recipe").
- **GV-34 — reader-1 missed #4 (PIN 5): the remove chip straddles the section divider by 8px whenever a stop is selected with 3+ stops.** Geometry this seat's own. → **X-W6.b** (seat recomposition re-homes the chip).
- **GV-35 — reader-1 missed #5: the specimen strip's scroll port is a nameless, roleless tab stop; the producer ships the prop that would name it.** FadingScroll bytes: unconditional `tabindex:"0"`, `role` only with `ariaLabel`/`ariaLabelledby` (this seat); EasingSpecimenStrip:84 passes neither, naming an inner non-focusable div instead (:86-90). Only the open row's strip is an effective tab stop (v-show hides the rest). → **NO-WAVE-OWNER** (PIN 1(c) — the strip is in no bounds table; the cure is one prop + one deletion).
- **GV-36 — reader-2 missed #3, as corrected by K-18: the catalogue is built by a side-effecting call at module evaluation whose builder throws on any preset the `CubicBezier` contract rejects — a latent import-time detonation resting on a guarantee value.js does not make.** easingCatalogue.ts:198/:98-104 + src/easing.ts:123 read. The uncatchable-consequence limb is killed (PIN 4); the latent contract assumption survives. → **X-W6.d** (build lazily or make the builder total).
- **GV-37 — reader-2 missed #4: `tuneOpen` is disclosure state keyed by positional index, accumulate-only, never pruned — remove stops then re-add and interval 3 reappears with its authoring canvas already disclosed.** :84-88 spread-accumulate + the end-truncating length sync (useGradientModel.ts:96-98) read. Same identity-vs-index disease the file already knows about (`:key="stop.id"`). → **X-W6.d** (with the PIN 1(a) path erratum).
- **GV-38 — D/F-11, narrow limb (the wide limb killed): the Delete/Backspace removal refusal at the 2-stop floor returns without any feedback, and the absence of the chip is never explained.** :158-161/:180-182 read; the chip's `v-if` absence IS a real (silent) expression — the surviving charge is the keyboard path + the unexplained refusal. → **X-W6.b** ("one removal owner with an explicit disabled state and its reason" — programmed).

### INFO

- **GV-39 — R2-11: two glass-ui clipboard idioms one file apart, both from the root barrel while `./dom` publishes them; the same file mixes root-barrel and subpath resolution styles.** :12-13 + GradientEasingEditor:29 — read. → **X-W8** (the root-barrel extirpation re-points these with the other 92).
- **GV-40 — reader-2 missed #5: the syntax-highlight fallback returns the caller's string unescaped into an `innerHTML` sink, guarded only by an upstream validator in a different module.** GradientCodeEditor.vue:43-53 read; unreachable-by-markup today (stop literals survive `parseCssColor`), which is why INFO and not a security row. → **NO-WAVE-OWNER** (PIN 1(c)).

## Killed-claims register

All 17 reader kills re-derived and UPHELD; K-18 minted by this seat. No motion-quarantine FALSE row is carried anywhere above (M-17 cited as the class precedent, per the kill of D/F-5).

1. **D/F-16 + D/D-19 (clip-past-viewport)** — KILLED (reader-1, upheld): the strip is its own scroll port (`FadingScroll axis="x"`, `.fading-scroll--x{overflow-x:auto}`, `width:max-content` :129, the reveal writes `port.scrollBy` :50-80 — all re-read); `overflowX 0` measures the document; the `bleeding` predicate is a known-bad probe (recorded in-corpus a day before the re-file, L-r2:591-596).
2. **D §5 PR-12 (20×20 seats)** — KILLED (reader-1, upheld): the always-on `::before` inflator (:361-391, re-read) + C-r2's live hit-test (R2-C2) — the REPORT rows are artifacts; the mount box is deliberately held.
3. **D/F-5 (reduced-motion inverted product-wide, MAJOR)** — KILLED (reader-1, upheld, lawful): Guard 2 deletes spatial properties from the transition set rather than shortening them (quarantine §cascade re-read); M-17 rules the claim-shape FALSE; S-1/S-2 record it as a superlative; this subtree ships its own correct PRM branch (:74).
4. **D/F-3 wide limbs (0-width thumb = 2.5.8 failure; cure: declare `variant="spectrum"`)** — KILLED (reader-1, upheld + sharpened by this seat's CSS read): `standard` is a designed thumbless register WITH its own branded focus indicator (the `:focus-within` track ring — pasted in instruments); the slider's pointer target is the track; the spectrum cure would import the wrong control language. Only GV-28's one-line producer nit survives.
5. **D/F-4 provenance limb (`.section-label` recipe at utils.css:13)** — KILLED (reader-1, upheld by this seat's read): :13 is a comment inside `.section-subtitle`'s docstring; the recipe is the producer's (`typography/utilities.css`, with a `[data-paper-field]` override the row never mentions). The measured ratios stay in residue; the cure address was wrong.
6. **D/F-11 wide limb ("the chip does not change")** — KILLED (reader-1, upheld): the chip is absent from the DOM at the floor (`v-if="selectedStop && removable"` :285, `removable = stops.length > 2` :66). The narrow limb survives as GV-38.
7. **D/F-1's cure (serialize the coalesced form; "the panel round-trips itself")** — KILLED (reader-1, upheld — PIN 3): gradientParse re-seeds linear; the coalesced form provably cannot round-trip; 2115-byte machine artifact in an authoring editor.
8. **L/R2-1 blast-radius limb ("the whole application is a Try-again pill; dock gone")** — KILLED (reader-1, upheld by this seat's App.vue read): the dock band is outside `<main>`; the boundary replaces exactly its own slot. The accurate radius (both panes + state) is GV-1's.
9. **D/F-18 cure (tokenize a transition for the render tile)** — KILLED (reader-1, upheld): `transition: all 0s` is every element's initial value (proves nothing), and `linear-gradient()`→`radial-gradient()` is a discrete image swap at 50% — a transition would DELAY the snap, not smooth it.
10. **D/F-15 phrasing ("no consumer anywhere")** — KILLED (reader-1, upheld): :140/:144 consume it in-file; the accurate charge (unbound PUBLIC model that should be a local ref) is GV-29's.
11. **L4-7 "sole outlier"** — KILLED (reader-2, upheld by this seat's own grep): five injection sites, MixSourceSelector.vue:33 is a second bare one with the same masking shape.
12. **L4-6 tap-target heading ("every interactive control under the coarse minimum")** — KILLED (reader-2, upheld): refuted by the live source AND an in-corpus measurement the axis never read (C-r2 R2-C2); conflates 2.5.8 AA (24px) with the 44px coarse referent.
13. **L4-2/R2-9 "nameless" consequence** — KILLED (reader-2, upheld): the REPORT's predicate never consults `title`; Chrome's AX tree names the control (`nameFrom: attribute=title`, C-r2 R2-C1); the two nameless-by-text buttons are outside `<main>`. Survives only as GV-30's weak-name MINOR.
14. **r4 §4.2 / r1 L-4 cure ("the parse tail is dead code — delete it")** — KILLED (reader-2, upheld by this seat's test read): six assertions in the library's own suite exercise all four branches; the C axis's coverage framing is adopted (GV-20).
15. **L4-1 cost clause ("retiring costs nothing")** — KILLED (reader-2, upheld): breaks the pinned `/math` surface literal (v4-c1:334) and removes a named export from published 4.0.0 — a semver decision the same finding had already conceded eleven lines earlier.
16. **R2-1 "three copies … all three delete"** — KILLED (reader-2, upheld): B and C are two callers of one shared helper; two unwrap implementations exist in the gradient tree, not three; C is one midpoint sample, not a walk.
17. **L4-5/L4-1 citation anchors (:237, :30-33)** — KILLED (reader-2, upheld): live lines are :231 and :31-34; tree unmoved since `f2c8f565`; report error, not drift.
18. **K-18 (arbiter, NEW) — reader-2 missed #3's consequence limb ("an import-time throw that no ErrorBoundary can catch")** — the installed runtime-core bytes show the async loader's rejection reaching `handleError(err, instance, 13)` whose parent-walk runs `errorCaptured` hooks for every type; the boundary very likely DOES see it as an in-place fallback. The latent-throw row survives (GV-36); the categorical uncatchability does not. Live confirmation of the fallback UX = residue R-11.

## Superlatives — preserved with provenance (readers' union, re-verified in source by this seat)

1. **The paint stacks are material contracts, not shorthand assemblies** (both readers): `.gradient-rail` (GradientStopEditor.vue:305-326) and `.gradient-render-tile` (GradientVisualizer.vue:266-278) pin origin/clip to border-box, no-repeat, full-box sizing over the alpha-checker — and the comments preserve the exact cascade artifact cured (the ramp tiling into the 1px border ring, each border column showing the OPPOSITE terminal). One recipe, one home, the bug kept as its own rationale.
2. **The focus affordance system** (GradientStopEditor.vue:328-359, both readers): the material lift hoisted off the inline style specifically so the ring COMPOSES; dual-contrast ring (1px dark under 3px light) surviving over ANY stop fill; a `forced-colors` outline fallback because WHCM strips box-shadow. Three failure modes anticipated in nine lines; unremarked by all three axes.
3. **The always-on hit inflator** (:361-391, both readers): `max(1.5rem,100%)` fine / `--touch-target` coarse, absolute (mount box held), pointer-events real, parented to the button so the bar's add-guard still reads handle-adjacent taps as grabs. Verified live in-corpus (C-r2) — and it is the byte-level refutation of the corpus's most-repeated tap-target row. Its one genuine hole is the chip-band overlap this record books (GV-22) — a seat-position defect, not an inflator defect.
4. **The add affordance argues for itself** (reader-1): the dashed ghost is filled with the exact colour a click will mint, from the same easing resolver the render uses (:211-226 ← GradientVisualizer:64-88); "the affordance replaces the instruction line" — and the code earns the comment.
5. **The editor truce** (GradientCodeEditor.vue:34-79, reader-1): no rewrite while focused, no caret theft, failed parses keep the authored text verbatim beside a loud verdict, canonical form settles on clean blur. WIP-preservation as written policy.
6. **The `scrollIntoView` refusal** (EasingSpecimenStrip.vue:37-80, both readers): the comment names the measured harm (the ~95px pane yank), the hand-rolled nearest-edge scroll touches only the one port the strip owns, and it carries a real PRM branch (:74) — in a fleet where the quarantine found twelve of thirteen PRM raisers unchecked.
7. **The sparkline ink floor** (:180-191, reader-1): 65% `color-mix` ink with the 45%→~2.6:1 WCAG 1.4.11 failure measured and recorded in the comment — a non-text contrast floor applied to a decorative-looking glyph.
8. **`gradientParse.ts` is the best module in the feature** (both readers): library oracles for every token class, textual segmentation with the WHY written down, authored literals preserved, model-or-reject with one-line human reasons, explicit refusals for everything unrepresentable. Its two real flaws (GV-2's oracle choice, GV-18's sentinel) are type-domain slips inside a disciplined design.
9. **One glyph painter** (easingCatalogue.ts:58-74 + `.head-glyph`/`.tile-glyph` `overflow: visible`, reader-2): the library's own callable sampled into a unit box, serving tiles and head glyphs, with overshoot drawing PAST the box — the author understood the exact [0,1] excursion that is GV-1's crash and handled it correctly in the SVG path while missing it in the colour path.
10. **Package hygiene + generated aliases** (reader-2, facts re-verified): subpath-only exports (no `.` root — honoured by zero bare-specifier imports in demo), `sideEffects:false`, and vite.config.ts GENERATING its self-aliases from the exports map — the exact discipline whose absence in tsconfig.demo.json/vitest.config.ts makes GV-9 and GV-15 self-evident. The `linearInterval()`↔catalogue byte-identity (`+(0).toFixed(3)` → `0`) also holds — checked, not assumed.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-1** — D/F-4's composited contrast ratios for `.section-label`/h3 over the live aurora (4.31/3.57 · 4.03/3.53): recipe confirmed producer-owned; rendered ratios not statically decidable; the light-scheme `[data-paper-field]` override must be in the measurement frame.
- **R-2** — D/F-7's route proportions (pane 512px/36.4%, empty companion 50.0%, 183px gutters): shell-owned; the attribution half (one flat column, no stage/inspector regions, `InstrumentChassis` unconsumed) IS static and stands.
- **R-3** — D/E-6 RTL mirroring: the rail is pure `clientX` arithmetic with no `dir` handling (:55-81 read) — plausible enough to owe a run; never re-verified by any pass.
- **R-4** — D/F-14's 12-stop degradation numbers (body 720→1321.8px; easing 51.9%; code well 477/190): mechanism static (fixed `h-10` rail vs growing accordion), ratios live-only.
- **R-5** — D/D-15 forced-colors composite: the stop editor ships a real WHCM block (:353-359); the render tile ships none; whether the composite reads correctly in a real WHCM run is untested (the tranche's keyboard-focus capture is empty).
- **R-6** — L4-6's phone-layout clipping percentages (34%/44%/44% at 390×844): structural cause static (`grid-cols-3` no responsive prefix beside the `w-20` tile), ratios live-only.
- **R-7** — L4-3's shipped-bytes closure claim (66 modules/224KB vs 11/18.6KB): self-labelled HYPOTHESIS; the structural all-19-through-the-root fact is verified.
- **R-8** — r1 L-2's exact 72-module delta (107→35): edge and payload verified by this seat; the count is inherited (twice-replicated in-corpus, never by this seat).
- **R-9** — R2-1's live blast-radius frames (bodyText 611→141; "Try again" pill) and R4-1's caret-edit runs: mechanisms fully confirmed offline; frames not re-executed here. Includes the two self-labelled HYPOTHESIS exposure vectors (drag-to-overshoot; an uncontrolled `initialPreset` boot) — the second is masked today by EasingAuthoringStage's controlled binding (read).
- **R-10** — L4-5(a)'s stop-ID collision: needs equal counter values in the same millisecond across two minters; latent, unreproduced.
- **R-11** — the GV-36/K-18 fallback UX: whether the async-chunk eval throw lands as the ErrorBoundary's announced fallback (this seat's bytes-derivation) or as an unbounded failure — one deliberate-throw run decides it.
- **R-12** — r1 L-12's per-tick costs (0.041ms/call; 0.265/0.449ms per modelState tick): coupling confirmed structurally; magnitudes inherited.

## Closing verdict

**ADJUDICATED — BLOCKERS SUSTAINED (3).** The instrument's own catalogue sells three curves that crash it through seven throw doors under one app-wide boundary (GV-1, with GV-24's door the corpus never counted); its parse oracle certifies colors its renderer cannot draw while the surface announces success (GV-2); and its CSS panel destroys the pane's namesake work on a debounced keystroke, with the corpus's own cure killed and the sound one already spelled in X-W6's gates (GV-3, PIN 3). The triad cures are X-W6's by name — **contingent on three bounds corrections recorded here (PIN 1): the misspelled GradientEasingEditor path, the unowned `useSpecimenRows.ts` under both BLOCKERs, and the fully unowned GradientCodeEditor/EasingSpecimenStrip pair whose rows (GV-3's editor half, GV-21, GV-35, GV-40) go NO-WAVE-OWNER at the formation boundary.** Routing: X-W6 ×22 (.a/.b/.c/.d) · X-W8 ×3 (+2 identity cross-books) · X-W4 ×1(+1 coordination) · X-W5 ×1 · X-W1 ×2 · X-W10 ×2 · X-W9 coordination ×2 · GLASS-OWNED ×2 (BH relay) · NO-WAVE-OWNER ×5 (+3 rider halves). 40 rows confirmed, 18 claims killed (one the readers' own promoted miss), 12 residue items handed to the live pass, 10 superlatives preserved with provenance. Nothing here stamps a wave; everything here is runnable by the session that does.

— end of apotheosis. The challenger corpus and both reader returns remain immutable beside this file per E-1/E-3.
