# wb-gradient-visualizer — adjudicated apotheosis r2 (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator, second seating (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **HEAD**: `9c779130` · branch `tranche-u` · **Supersedes in place**: the 2026-08-10 r1 apotheosis at this path (HEAD `54a97489`). The r1 record's rulings are CONSUMED WHOLE — its PINs re-verified, its GV-1..GV-40 ids preserved for life (anti-rename) — and this r2 record repairs its one material defect: **two corpus-carried STANDS rows (C-5, C-6+R3-5) were absent from the r1 roster** although r4 §6 carries both; both readers flagged the omission independently and both rows are admitted below (GV-41, GV-42). One r1 row is retired on a reader's argument this seat ratified (GV-28, PIN 3).
**Corpus**: `demo/workbenches/gradient/` whole (13 files, 2,601 lines re-counted this seating) + the contract surfaces it rides. Challenge corpus: 12 files across C/D/L axes (r2 evidence cells govern where the axes conflict; r4 finals + r2/r3 corrections in-corpus) with `evidence/` and `probes/` beside them.
**Re-readers adjudicated (this seating)**: design-and-usage (28 confirmed · 14 killed · 8 missed · 9 unproven · 11 superlatives) · library-and-code (29 confirmed · 4 killed · 5 missed · 9 unproven · 8 superlatives). Both were attacked before adoption: every new kill re-derived against the tree or the installed producer, every promoted miss re-walked in source with this seat's own instruments, **one reader widening killed in half by this seat's own dist run (K-19)** and **one reader miss demoted after this seat found the gesture operable (K-20)**.
**Subject drift**: `git diff --stat 54a97489 HEAD -- demo/workbenches/gradient/ demo/color-picker/App.vue demo/shell/usePaneRouter.ts demo/palettes/ src/easing.ts src/color/operations.ts src/foundation/math.ts eslint.config.js tsconfig.demo.json package.json test/ demo/shared/utils.ts demo/color-session/picker-color.ts docs/tranches/X/waves/W6.md` → **empty**. Every r1, corpus, and reader line anchor remains valid at this HEAD.
**Arbiter's own instruments this seating (static + node against dist; zero browser tooling — probe parsimony)**: full re-reads of GradientVisualizer.vue (whole, 279), useGradientCSS.ts (whole, 334), useGradientModel.ts (whole, 193), gradientParse.ts:130-301, GradientStopEditor.vue:60-234, GradientCodeEditor.vue (whole, 116), GradientEasingEditor.vue:55-184, EasingSpecimenStrip.vue:33-46/:80-133, easingCatalogue.ts:28-36/:125-203, useSpecimenRows.ts (whole, 74), GradientPane.vue (whole, 29); **node against the built dist**: `CubicBezier(...bezierPresets[n])` sampled at 101 points for all three back presets — `ease-in-back fn(0.5)=−0.0636 (min −0.0969)`, `ease-out-back fn(0.5)=1.0676 (max 1.0868)`, `ease-in-out-back fn(0.5)=0.6067 (min −0.0927 / max 1.0927)` — and `mixColors(c0,c1,p,{space:'oklch'})` → `ok:false / color_progress_out_of_range` at p=1.0676 and p=−0.0636, `ok:true` at 0.5; preset census by node: **30 bezier presets, quart×3 + quint×3 present** → 27 of 33 tiles render under `FAMILY_ORDER` (:174); installed producer bytes: `chip-DFZQr6rV.js` (**`mode:"selectable"` renders reka `Toggle`, `aria-pressed`, `focus-ring` — all grep-verified**), `useEasingPicker.d.ts` (**`initialPreset?: string` documented "(default `\"ease-out-back\"`)"; `JumpTerm = (typeof jumpTerms)[number]` derived FROM `@mkbabb/value.js/easing`**); repo censuses re-run: `LIBRARY_PORT_KEY` bare injects = 2 (GradientVisualizer.vue:30 + MixSourceSelector.vue:33, the latter read whole in context), `package.json#exports` = 7 keys **no `.` root** + `grep 'export function debounce' src/` → 0 (the L-15 decider), `usePaletteStore.ts:51-59` (savedPalettes = `isLocal && id != null`; publishedPalettes separate; **no trash/deletedAt/isDeleted token in the file**), picker-color.ts:37 (`PickerSpace = SpaceId` identity), usePaneRouter.ts:208-210 (`?.` dispatch trio), X-W6 spec re-read whole at this HEAD (bounds table :49-120 — the phantom `easing/GradientEasingEditor.vue` path at :59 **still stands**; GradientCodeEditor.vue, EasingSpecimenStrip.vue, useSpecimenRows.ts **still in no bounds table**; gates a1/a2/a7/b1-b4/c1-c4/d1-d2/e1-e2/j1 re-read for routing).

## RULING PINS (this seat's own derivations, this seating)

1. **The r1 record is adopted as substrate, not as gospel.** All six r1 PINs re-verified at this HEAD (the two-blockers-one-disease frame, the seven-door census including `openIntervalRamp`, the PIN-5 chip-band geometry, the K-18 runtime-core kill, the bounds errata, the identity guards). Its roster carries forward as GV-1..GV-40 minus GV-28 (PIN 3), with GV-1 sharpened (PIN 2) and GV-39 re-severed (PIN 6). Its scoreboard claim of full reader-coverage was **false by two rows** — C-5 and C-6+R3-5, both r4-STANDS, both absent. Repaired here.
2. **K-19 — reader-2's R2-1 widening is half right, and this seat's own run splits it.** The reader's kill of the r2 axis's ease-out-back scoping claims "the specimen-row ink path throws for ALL THREE back presets." This seat's dist run: `ease-out-back fn(0.5)=1.0676` → `color_progress_out_of_range` at the `useSpecimenRows.ts:52-59` door — **CONFIRMED**, and it is the producer's documented `initialPreset` default (`useEasingPicker.d.ts`, read), so the widening's headline stands. But `ease-in-out-back fn(0.5)=0.6067 ∈ [0,1]` — **that preset does NOT throw at the fn(0.5) ink door**. It detonates through the multi-sample walk doors instead (min −0.0927 / max 1.0927 across 101 samples → `sampleCoalescedStops`' 32-step walk crosses the overshoot region). GV-1's blast mechanics are corrected to: all three presets crash the pane; **two of three** also crash the specimen-ink door at fn(0.5); ease-out-back's unmarked row in the r2 axis's table was the reader's genuine find.
3. **GV-28 is RETIRED — reader-1's argument, ratified by this seat.** The r1 record kept a narrow producer-relay limb: the UA outline suppression is spectrum-gated, so a `standard` slider's thumb keeps the UA outline. Reader-1's kill: the thumb is `opacity: 0` (producer bytes, both records) — CSS opacity composites the ENTIRE element including its outline at zero alpha, so the unsuppressed outline is computed but never painted, in any state, on any platform. A defect with no expressible consequence in any reachable state is not a defect; the BH-relay row is withdrawn with this rationale. The `standard` register's own branded focus indicator (the `:focus-within` track ring, producer bytes re-read at r1) remains the working affordance.
4. **K-20 — reader-1's touch-add miss is NARROWED: the gesture is operable, the charge is discoverability.** The reader claims "the pane's primary creative act has no discoverable expression on touch." This seat's read of the commit path: `onBarPointerDown` (:85-89) arms `pendingAdd` on ANY pointer type; `onBarPointerUp` (:101-110) commits on an unmoved press; and because the rail is `touch-none` (:203), a touch tap reliably delivers `pointerup`, not a scroll-cancel — **tap-to-add WORKS on touch**. What is true: the ghost preview is hover-only (:91-99, cleared :112-115), `cursor-copy` is fine-pointer-only, the file's own no-instruction rationale is hover-premised, and the minted stop is BY DESIGN invisible at mint (GradientVisualizer.vue:59-62 — it takes the ramp's own colour). Surviving charge: the primary creative act is unsignified and unpreviewable on coarse pointers. Demoted MAJOR→MINOR (GV-71). The reader's other coarse-pointer find (`touch-none` eats pane scroll) is GV-32, already carried.
5. **C-5 is classed BLOCKER, and the wave already agrees.** Reader-1 rates the add/remove interval re-hosting BLOCKER; this seat re-derived the mechanism whole (useGradientModel.ts:89-100 syncs `intervals` by LENGTH alone — push linear at the END, truncate from the END; `addStop` :116-120 sorts the new stop into the MIDDLE; `removeStop` :122-125 filters anywhere) and rules BLOCKER by consistency: GV-3 (one keystroke destroys every curve) is a BLOCKER for silent destruction; C-5 is silent CORRUPTION on the pane's most common gesture — authored work re-attached to intervals the author never bound it to, undetectable at the moment it happens. X-W6.a's gate a1 already pastes this exact repro as its RED state ("a `steps()` authored on 50→100 re-attaches to 25→50 after a 25% insert") — the wave knew; the r1 roster did not.
6. **GV-39 re-severed INFO→MINOR; reader-2's MAJOR declined.** The clipboard-idiom split is hygiene (X-W8's extirpation), but the user-facing half — `copyCSS` (GradientVisualizer.vue:127-129) awaits `writeClipboard` with no try, no status, no tick, while `GradientEasingEditor.vue:94-103/:184` renders a confirmation tick for the SAME verb one file away — is a real divergent-feedback defect on a first-class dock action (D/D-9's half). MINOR: the action works; only its confirmation is absent. Reader-2's MAJOR conflates the fleet-wide census with this pane's harm.
7. **Identity guards (anti-rename), this seating's additions.** D/F-8 ("one package two ways") and reader-2's missed three-registers census (13 statements / 3 disciplines / 8 already correct) are **sharpenings of GV-17**, not new rows — the count now lives at the feature scope and travels with X-W8's row. D/F-15's three-channels observation is a **sharpening of GV-29** (the unbound public model + dual write already booked; the bare `inject(LIBRARY_PORT_KEY)` is GV-5/GV-60's). D/F-14's mechanism (scale-invariant protagonist) folds into **GV-49** as provenance. Reader-2's missed barrel-omission (serializeIntervalRamp absent from the "preserve public API surface" block while being the ONLY serializer with a live external importer — GradientEasingEditor.vue:31) was already merged into **GV-11** at r1 and is re-verified here (useGradientModel.ts:22-27 re-read).

## Scoreboard

**72 adjudicated defect rows: 4 BLOCKER · 34 MAJOR · 31 INFO-and-MINOR (28 MINOR · 3 INFO)** — 39 rows carried from r1 (all re-anchored, zero drift), 33 rows new this seating (every reader-confirmed row admitted or merged with its identity named; 10 reader misses promoted to rows, 2 misses merged as sharpenings, 1 miss narrowed by K-20). **25 killed claims**: r1's 18 carried, 5 new reader kills upheld (one retiring a roster row), 2 seat-minted against the readers themselves (K-19, K-20). Headline: **the instrument's catalogue sells three curves that crash it through seven throw doors — one of them the producer's own default preset — its CSS panel certifies success while destroying the pane's namesake work, its two commonest gestures (add a stop, remove a stop) silently re-host every authored curve, and its position writer emits CSS its own parser refuses. The cure surface is X-W6's by name, contingent on the three bounds corrections this record re-pins.**

## Ruled disagreements and reader-challenges (this seating's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **C-5 severity** | reader-1: BLOCKER; r1 roster: absent; corpus r4: STANDS unclassed | **BLOCKER — admitted as GV-41 (PIN 5)** | Mechanism re-derived whole from useGradientModel.ts; X-W6 gate a1's own RED text is this row's repro. |
| **C-6 severity** | corpus: BLOCKER; reader-1: MAJOR ("unrecoverable only until the user drags back") | **MAJOR — reader-1's demotion upheld** | The state is reachable in one gesture but fully recoverable by the inverse gesture; no work is destroyed. The emit-what-the-parser-rejects half is gate a1/a2's subject; the ghost-colour half is colorAtPosition:67's first-branch short-circuit (read). |
| **R2-1 widening scope** | reader-2: all three back presets throw at the specimen-ink door; r2 axis table: left ease-out-back unmarked | **SPLIT — K-19 (PIN 2)** | This seat's own dist run: in-back −0.0636 THROWS · out-back 1.0676 THROWS (and is the producer's `initialPreset` default) · in-out-back 0.5→0.6067 DOES NOT at that door (detonates via the 32-step walks instead). |
| **GV-28's survival** | r1 seat: keep narrow producer nit; reader-1: kill (zero-alpha box paints no outline) | **RETIRED — reader-1 wins (PIN 3)** | `opacity:0` composites the element whole, outline included; no reachable state paints it. |
| **Touch add** | reader-1 missed #3: "no discoverable expression on touch", MAJOR | **NARROWED — K-20 (PIN 4), MINOR** | Tap-to-add operable by this seat's read of the commit path + `touch-none`'s guarantee of `pointerup`; the surviving charge is signification, not capability. |
| **Copy-CSS feedback severity** | reader-2: MAJOR (r1 L-9/R2-11); r1 roster: INFO (GV-39) | **MINOR — both corrected (PIN 6)** | The divergent-feedback half is real and user-facing; the idiom census is X-W8 hygiene. |
| **`r1 L-1` enumeration** | L axis pass-1: "the globs match zero files"; reader-2: killed the enumeration, kept the conclusion | **kill UPHELD** | r1's own instruments line already recorded the correction (dead `demo/@` globs sit BESIDE live `demo/color-picker` globs in the :232-238 object; the live rules ban specifier shapes W43 deleted, so they are unfireable rather than unscoped). GV-4's verdict unmoved: no rule of any kind reaches `demo/workbenches/**`. |
| **L4-7 "sole outlier"** | L axis: sole bare inject; both readers: killed (5 sites, 2 bare) | **kill RE-UPHELD, this seat's own read** | MixSourceSelector.vue:33 read in context this seating: bare inject, guarded `pm?.savedPalettes.value ?? []` at :34 — a second masking-shape site, both inside X-W6 §4 bounds. |
| **D/F-17 owner-state limb** | D axis: seeds from "possibly a trashed palette"; reader-1: killed | **kill UPHELD, this seat's own grep** | usePaletteStore.ts:51-59 re-read: savedPalettes = `isLocal && id != null`; `grep trash\|deletedAt\|isDeleted` → 0. Index 0 can be stale, never trashed. The three-silent-no-ops core survives as GV-60. |
| **C-7 categorical phrasing** | C axis: "no keyboard equivalent anywhere" (2.1.1 categorical); reader-1: killed the phrasing, kept the charge | **kill UPHELD** | GradientCodeEditor.vue:86-101 is a focusable `role="textbox"` wired to `applyCSS` — typing a stop into the CSS adds it by keyboard alone. The surviving charge (no DIRECT affordance; no slider semantics on handles) is GV-43, and X-W6 gate b1's RED text already states it precisely ("no keyboard path to CREATE a stop exists at all" — scoped to the rail's own grammar). |
| **D/F-2's cure** | D axis: "one string that carries every field"; reader-1: killed the easing half | **kill UPHELD** | CSS Images 4 has no per-interval `<easing-function>` production; `GradientInterval` carries `{mode, css, fn, points, steps, term}` (useGradientModel.ts:49 → EasingPickerValue). The ` in <space> <hue> hue` half survives and is X-W6.c's named clause; easing needs a non-CSS home — GV-8's cure note now says so. |
| **Debounce-fork rationale** | L axis r1 L-15; reader-2: confirmed | **CONFIRMED, this seat's own greps** | demo/shared/utils.ts:8-21 re-read: the comment asserts a root-barrel export "stands for external consumers" — `package.json#exports` has no `.` key (this seat's node run) and `grep 'export function debounce' src/` → 0. The fork's stated licence is false at HEAD; GradientCodeEditor.vue:3 is a live consumer. |

## Adjudicated defect roster

Line anchors are HEAD `9c779130` (≡ `54a97489` for every subject file — drift empty); re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: an X-wave id means that wave's §4 bounds own the surface today (re-verified this seating); **NO-WAVE-OWNER** rows are booked for the next formation boundary; GLASS-OWNED rows leave by the standing BH relay. The three X-W6 bounds corrections (r1 PIN 1, re-verified: the phantom `easing/GradientEasingEditor.vue` path at W6.md:59; unowned `useSpecimenRows.ts` under both original BLOCKERs; fully unowned `GradientCodeEditor.vue` + `EasingSpecimenStrip.vue`) remain **preconditions** on every X-W6 route below.

### BLOCKER

- **GV-1 — L/R2-1 ≡ R2-1 (carried, sharpened by PIN 2/K-19): selecting a `back`-family specimen tile throws `color_progress_out_of_range` from render computeds and erases both panes.** Chain re-verified whole this seating, including this seat's own dist measurements of all three presets. Sharpened: **ease-out-back — the producer's documented `initialPreset` default — throws at the specimen-ink door too** (fn(0.5)=1.0676); ease-in-out-back crashes only via the multi-sample walks (fn(0.5)=0.6067 in-range). Seven throw doors (r1 PIN 2) re-confirmed: `coalescedCSS`/`railRampCSS` (useGradientCSS.ts:325/:327→:206-208), `parseColorIn` in the same walk, `colorAtPosition`'s three throws (GradientVisualizer.vue:66/:77/:87), `useSpecimenRows` ink (:53-59), `openIntervalRamp` (GradientEasingEditor.vue:63-67→:253→:206-208 — wired to the row open by default, :61). → **X-W6.a/.c** (+ bounds precondition); catalogue exposure **X-W6.d**; the eased-mix library-capability question **X-W9 coordination, else NO-WAVE-OWNER**.
- **GV-2 — C/R4-1 (carried): the validity oracle is not the render predicate — spec-valid `none` channels parse `ok:true`, `parseVerdict` clears, the pane dies from the same doors.** Re-anchored; unchanged. → **X-W6.a + X-W6.c**, same precondition.
- **GV-3 — D/F-1 (carried): one debounced keystroke in the CSS panel silently destroys every authored easing curve.** All four links re-read this seating (GradientCodeEditor.vue:55-62 → GradientVisualizer.vue:102-108 → useGradientModel.ts:158-168 → gradientParse.ts:292-298 unconditional `linearInterval()` re-seed). Reader-1's sharpening admitted: **the destruction requires no semantic edit — re-typing the panel's own byte-identical text destroys the curves**, because :295-298 is unconditional and the panel binds `simpleCSS`, which cannot carry easing (serializeGradient :147-162). The r1 cure ruling (PIN 3 of r1: coalesced-form transposition dead; stop-owned easing + complete serialization = X-W6.a gate a1 + .c) stands. → model half **X-W6.a/.c**; editor-surface half (announce/confirm/cancel-on-deactivate) **NO-WAVE-OWNER** (GradientCodeEditor.vue unowned).
- **GV-41 — C-5 (NEW this record; r4 STANDS; absent from r1 — PIN 5): adding or removing a stop silently re-hosts every authored easing curve onto a different interval.** This seat's own derivation: `intervals` is index-keyed and reconciled by LENGTH alone (useGradientModel.ts:89-100 — push `linearInterval()` at the END while short, truncate from the END while long); `addStop` (:116-120) sorts the new stop into the MIDDLE, so interval[i]'s authored curve now governs a pair of stops the author never bound it to and the appended linear lands on the LAST interval; `removeStop` (:122-125) filters anywhere while the truncation eats the END, so removing the FIRST stop re-hosts every curve one interval leftward and discards the last. Silent, unannounced, no undo, fires on the pane's most common gesture (a rail tap). X-W6 gate a1's RED text is this row's repro verbatim. → **X-W6.a** (stop-owned easing + the watcher's deletion are its named mechanism).

### MAJOR

**Carried from r1, re-anchored, zero drift — routes unchanged:** GV-4 (no import lint reaches the workbench; identity ColorSpaceSelector L-5(a) ≡ MX-25 → X-W0/X-W1 fold + X-W8.b; the reader's enumeration-kill folded, see table) · GV-5 (LIBRARY_PORT_KEY drags the auth/transport closure; both bare sites W6-owned → X-W6.a/.b + X-W8) · GV-6 (single ErrorBoundary = two-pane outage → X-W5; MX-7 cross-ref) · GV-7 (reset does not reset → X-W6.a) · GV-8 (the CSS dialect cannot express space/hue/easing and blames the colour → X-W6.c + .a; **cure note amended per the D/F-2-cure kill: the ` in <space> <hue> hue` clause carries space+hue; easing can NEVER ride the CSS string and needs its own persisted home**) · GV-9 (tsconfig↔exports five-row drift; identity L-14 ≡ XP-21 ≡ MX-20, banked NO-WAVE-OWNER — X-W8 declined by name) · GV-10 (`SelectionValue` unexportable → GLASS-OWNED BH relay + X-W6.b rider) · GV-11 (the re-export block preserves a surface nobody consumes AND omits the one live serializer → X-W6.c; re-verified :22-27) · GV-12 (`/math` bezier serializer: unused, 2dp-lossy, test-pinned wrong law → X-W9 coordination else NO-WAVE-OWNER) · GV-13 (seven type-only import cycles; orchestrator-as-type-registry → X-W6.c) · GV-14 (one Result, six unwrap sites, two policies → gradient half X-W6.c; cross-feature fold NO-WAVE-OWNER rider) · GV-15 (no pretest; src+dist in one process → X-W1) · GV-16 (`Ref<any>` + `?.` chains + the reset/resetGradient rename → X-W4; mobile limb X-W5) · GV-17 (19 alias barrels through the root barrel → X-W8; **sharpened per PIN 7: three import registers across 13 statements in this one feature, 8 already correct — the cure is finishing a migration, not starting one**) · GV-18 (the `-1` sentinel vs authored negatives; `-0%` discontinuity → X-W6.a) · GV-19 (hand-composed label+control pairs where LabeledField ships; `h-9`×3; `grid-cols-3` no responsive prefix → X-W6.b) · GV-20 (`easingFnOf`'s parse tail reachable only from the library's test = coverage defect → X-W6.c + X-W1) · GV-21 (`aria-invalid` with no describedby/errormessage → NO-WAVE-OWNER, file unowned) · GV-22 (coarse-pointer chip band overlap: a rail-bottom tap removes → X-W6.b; gates a9/a10 name it) · GV-23 (quart+quint families silently dropped, 27 of 33 tiles — re-counted by this seat's node census → X-W6.d) · GV-24 (the sixth throw door `openIntervalRamp`, corpus-absent → X-W6.c/.d).

**New this seating:**

- **GV-42 — C-6 + R3-5 (r4 STANDS; absent from r1): the drag/keyboard position writer drops the ordering invariant — the component emits CSS its own parser rejects, the rail paints a clamped slab, and the add-ghost previews a colour that matches nothing.** Re-derived: `updateStop` (useGradientModel.ts:127-131) maps a raw position with NO re-sort while `addStop` sorts (:118) and `gradientParse.ts:284-290` REJECTS non-monotonic input — three writers, three policies. Consequences from source: serializeGradient emits array order (:157-159) → the visible CSS is a string the app's own parser refuses; `sampleCoalescedStops` computes a negative `posRange` (:195-200) → descending sub-stop positions that CSS clamps to a hard edge; `colorAtPosition`'s first branch (:67) short-circuits on `list[0]!.position` → once list[0] is the right-most stop, the ghost reports the wrong terminal colour. Severity MAJOR (reader-1's demotion upheld — recoverable by the inverse gesture). → **X-W6.a** (gates a1/a2 are this row's commands; the normalise-on-write mutator is its named cure).
- **GV-43 — C-7 (narrowed per the phrasing kill): the rail is a bare `<div>` with no role, no name, no tab stop; its primary creative act has no DIRECT keyboard affordance; the handles carry no value semantics.** GradientStopEditor.vue:200-210 re-read (data-testid only); `onHandleKeydown` :173-187 offers nudge/delete/deselect, never insert; handles are buttons named `Gradient stop at N%` with no `role=slider`/`aria-valuenow`/`aria-valuetext`, so an arrow nudge changes only the accessible name. The contenteditable CSS well IS a keyboard route (the kill), so the charge is a missing equivalent affordance + absent semantics, not 2.1.1-categorical. → **X-W6.b** (gate b1 verbatim).
- **GV-44 — C/R2-N1: a cancelled pointer gesture COMMITS the add.** GradientStopEditor.vue:208 binds `@pointercancel="onBarPointerUp"`, and :101-110 is the committing handler — cancel, the one event that means "this gesture did not happen," is wired to the same evaluator as the gesture that did. → **X-W6.a** (gate a7's post-cure clause names it: "a `pointercancel` in the dead-zone must mint NOTHING").
- **GV-45 — D/D-4 ≡ C-9: the Direction axis stays live and operable for `radial`, where it changes nothing, and silently means "start angle" for `conic`.** GradientVisualizer.vue:229-233 unconditional, no `disabled` branch; both serializers push direction only for linear≠180 and conic (useGradientCSS.ts:151-155, :285-289) — re-read this seating; the model's own comment says "ignored for radial" (useGradientModel.ts:55). A control that is a no-op in one of three modes and mislabeled in another. → **X-W6.b**.
- **GV-46 — C-10: the Hue control is inert in four of the nine offered spaces, with no disabled state and no explanation.** `{space, hue}` threading re-verified (useGradientCSS.ts:202-205; useGradientInterpolation.ts:36); the Select at :196-211 fully live in oklab/lab/rgb/xyz where no hue channel exists. Same silent-no-op family as GV-45. → **X-W6.b**; the space-metadata half (an `interpolatable`/cylindrical flag) rides the X-W6.f catalog + the c4 derivation rider.
- **GV-47 — D/D-12 + F-7 (attribution half): one flat `flex flex-col gap-5` column of ten siblings — no stage, no inspector, no action region — so the canon's golden split has nothing to proportion.** :135 re-read; the canon citation (OPTICAL-BENCH-COMPOSITIONS.md:44 — P122 golden, regions "preview; stop seats; easing/code/action") is reader-verified and seat-accepted; `InstrumentChassis` unconsumed is CC-108's fact-to-preserve-by-subtraction. The route-proportion numbers stay residue (R-2). → **X-W6.j** (gate j1: Gradient owns its stage/inspector/action contract as a routed scene) **+ X-W6.b**.
- **GV-48 — D/D-11 + D-13 + D-14 (+ D/F-14 mechanism, merged per PIN 7): protagonist inversion — the meniscus rail is a fixed 40px at any content volume while every support grows unbounded; the support tile wears the protagonist's own material lift (`--shadow-sm` byte-identical at GradientVisualizer.vue:277 and GradientStopEditor.vue:325); at the default model the tile repaints the very ramp the rail paints 90px above it (coalescedCSS ≡ serializeRailRamp's 90deg normalization at linear/90°).** All three legs re-anchored; the degradation ratios stay residue (R-4). → **X-W6.b** (the seat recomposition "frees the meniscus") **+ .j**.
- **GV-49 — C-13 (re-severed MAJOR, reader-1's argument adopted): selection is a public model that leads nowhere — no affordance in the app can edit a stop's colour.** Census re-verified: `selectedStopId` exists at :51/:140/:144 only; `updateStop` accepts `{cssColor}` and its ONE call site passes `{position}` (GradientVisualizer.vue:94-96); the dock offers reset/copy/seed only (usePaneRouter.ts:208-210, re-read); the injected session colour is dropped at the mount (GradientPane.vue:8, re-read whole). The instrument's headline verb is "Build gradients" (GradientPane.vue:21) and the only colour-authoring route is the panel that destroys the easing (GV-3). → **X-W6.b** (the selected-stop inspector is its named home; gate b3's numeric entry generalizes to colour entry).
- **GV-50 — R2-3: 635 lines of framework-free, library-grade gradient code live in demo/, and the library's own suite already votes them library code.** `grep -rli gradient src/` → 0; gradientParse.ts 301 L zero-Vue; 10 test files under test/ import demo paths (reader-verified; spot-confirmed via gradient-parse/gradient-v4-consume imports at r1). A promotion decision on the published surface. → **X-W9 coordination, else NO-WAVE-OWNER** (X-W6 may not touch `src/**` by its own Do-NOT-touch row).
- **GV-51 — r1 L-3: the value.js `/css` ↔ `/easing` seam is missing both round-trip halves, and this tree owns both re-implementations.** No `serializeTimingFunction` anywhere in src/ (reader census, r1-corroborated); half A (AST→callable incl. the linear() position-fill) is useGradientCSS.ts:80-117; half B (value→literal) is easingCatalogue.ts:38-56's byte-mirror of glass-ui's private `toFixed(3)` mint with no test binding the two. Corrected by L4-1: `/math` DOES ship a bezier serializer — the wrong one (GV-12). → library halves **X-W9 coordination else NO-WAVE-OWNER**; the demo-side one-dialect fold **X-W6.c** (with GV-12's literal-law work).
- **GV-52 — reader-2 missed #1 (promoted): the specimen strip's stated contract — "rides the SAME sampling law … shows the interval exactly as the gradient will render it" — is false for every model with more than two stops.** This seat re-read both sides: the budget is per-RAMP (`stepsPerInterval = max(2, round(32/(stops.length−1)))`, useGradientCSS.ts:180-183) while `serializeIntervalRamp` (:232-254) rebuilds a TWO-stop sub-model → the strip always samples at 32 while the live render samples the same interval at 32/(N−1) — 16 at N=3, 3 at N=12. The docstring making the equality claim sits at :227-231, three lines above the code that breaks it. Sharpest witness: `steps(4)` at 12 stops — the row previews a staircase the render cannot paint. Nothing in test/ exercises `serializeIntervalRamp` (reader grep, unchallenged). → **X-W6.c** (gate c2's "one sampling law" equivalence is this row's oracle; the one-`sampleAt` cure closes it by construction).
- **GV-53 — reader-1 missed #1 (promoted): the open easing row is a ~28-stop keyboard tunnel — 27 independently-focusable `aria-pressed` toggle chips with no roving tabindex and no single-select semantics, plus the scroll port's own unconditional tab stop, seated between the accordion head and the CSS panel.** Producer bytes verified by this seat: `mode:"selectable"` renders reka `Toggle` (real button, `aria-pressed`, `focus-ring`) — `chip-DFZQr6rV.js` grepped; 27 tiles re-counted by node census (30 presets − 6 filtered + 3 steps); FadingScroll's unconditional `tabindex:"0"` is r1-instrument fact; the container is `role="group"` (:88), not a radiogroup, and `onTileToggle` (:33-35) ignores un-press — exactly-one-selection modeled as 27 independent toggles. No corpus file mentions the strip's focus order (reader grep, spot-confirmed). The exact rendered tab-stop count wants one keyboard run (residue R-13). → **NO-WAVE-OWNER** (EasingSpecimenStrip.vue in no bounds table — the GV-35 precondition), GV-35 cross-ref.

### MINOR

**Carried from r1, re-anchored — routes unchanged:** GV-25 (one `gap-5` does all rhythm work → X-W6.b under X-W10 law) · GV-26 (four peer h3s, no h1/h2 → X-W10 rider, named gap stands) · GV-27 (dead `CSS_COLOR_KEY` inject at the mount → X-W6.a) · GV-29 (two byte-identical ID minters + the double selection write + the unbound public model → X-W6.a; **sharpened per PIN 7 with D/F-15's three-channels observation**) · GV-30 (Copy control named only by undeclared `title` fallthrough → X-W6.b rider + X-W4 coordination) · GV-31 (railRampCSS recomputes on direction it provably ignores → X-W6.c) · GV-32 (`touch-none` on the full-width rail eats vertical pane scroll; `pan-y` free — gestures are clientX-only :75-81/:136-146, re-read → X-W6.a) · GV-33 (single-vocabulary handle ring vs the file's own dual-contrast recipe → X-W6.a, programmed by name) · GV-34 (the remove chip straddles the section divider by 8px → X-W6.b; gate a10) · GV-35 (the strip's scroll port is a nameless, roleless tab stop; the producer ships the naming prop → NO-WAVE-OWNER) · GV-36 (module-eval catalogue build resting on a contract value.js does not make; K-18's consequence-kill stands → X-W6.d) · GV-37 (`tuneOpen` accumulate-only positional disclosure state → X-W6.d) · GV-38 (the keyboard removal refusal at the floor returns in silence → X-W6.b; gate b4) · **GV-39 (re-severed INFO→MINOR per PIN 6: two clipboard idioms one file apart AND the dock Copy has no result state while the sibling control ticks → X-W8 idiom half; X-W6.b feedback half)**.

**New this seating:**

- **GV-54 — D/D-6: three raw `<hr class="border-border">` where the binding composition sets the Gradient row's divider count to `none` ("meniscus, stop seats and code/action interval carry grouping").** :148/:240/:251 re-read; canon cite reader-verified (OPTICAL-BENCH-COMPOSITIONS.md:78); `demo/ui/separator` re-exports the producer Separator and is unused here. Rides GV-25's rhythm cure — the hrs are load-bearing precisely because one gap token serves both intervals. → **X-W6.b**.
- **GV-55 — L3-5: one paint recipe in two scoped homes (`.gradient-render-tile` :271-278 / `.gradient-rail` :317-326, five byte-identical declarations); three inline `background` shorthand assemblies the tree's own comment forbids (GradientVisualizer.vue:270 "Never a per-callsite `background` shorthand assembly" vs GradientStopEditor.vue:219-221 + GradientEasingEditor.vue:154); the raw-hr species beside an unused Separator.** → **X-W6.c** (the two-site paint-stack dedup rider is named in its mechanism) **+ .b**.
- **GV-56 — D/F-13: the `Interpolation` heading is wrong for half its contents (Type and Direction are geometry — they land in the function name and the leading angle) while the interpolation axis with the largest authoring surface (easing) sits under a different heading.** :149 vs :163/:229 vs :241 re-read. → **X-W6.b** (the heading outline is .b's named subject).
- **GV-57 — D/F-3 surviving limbs + D/E-2: the Direction slider is adopted as a bare primitive — no `aria-valuetext` (the value is a unit-bearing angle), no numeric entry, no reserved width for the live readout (its left edge walks 0°→360° inside the `justify-between` row).** :229-233 re-read; the three killed limbs (0-area target, double focus ring, spectrum-variant cure) stay killed. → **X-W6.b** (rides GV-19's LabeledField adoption).
- **GV-58 — D/E-7: the render tile's accessible name is a constant that never names what it paints.** :219-225 re-read: `role="img"` + literal `aria-label="Gradient render with type and direction applied"` beside a `:style` that changes with every edit — invariant across type, angle, and colour. → **X-W6.b**.
- **GV-59 — reader-1 missed #5 (promoted): six radius vocabularies inside one pane, two of them card-scale on card-scale surfaces one column apart.** Re-verified this seating: `--radius-pill` (rail :318) · `rounded-full` (handle/chip/ghost :216 et al.) · `rounded-card` (tile :223; interval rows GradientEasingEditor.vue:114) · `rounded-md` (interval ramp :153; readout rail :176) · `--radius-input` (rail buttons :274) · `rounded-lg` (code well GradientCodeEditor.vue:93). The tile and the code well — both card-scale, same column — take `rounded-card` and `rounded-lg`. → easing-panel half **X-W6.d** (gate d1 is this row's oracle); pane-surface half **X-W6.b** rider.
- **GV-60 — D/F-17 core ≡ L4-7 surviving half: `Seed from palette` is a named dock command with an unnamed subject and three silent failure paths.** :110-116 re-read: `if (!pm) return` · `savedPalettes.value[0]` (index 0 of an undifferentiated list, no chooser, no name shown) · `if (colors && colors.length >= 2)` — three states where a named action does nothing and says nothing; the whole dispatch path is `?.`-masked end to end (GradientPane.vue:11-15 + usePaneRouter.ts:208-210). Owner-state limb stays killed (no trash state exists — this seat's grep). → **X-W6.b** (+ the GV-16/X-W4 port typing removes the masking).
- **GV-61 — r1 L-15: the demo's forked debounce justifies itself with a rationale that is false at HEAD** — "the library's root-barrel export stands for external consumers" (demo/shared/utils.ts:8-21) against a 7-key exports map with no `.` root and no `debounce` anywhere in src/ (both this seat's own runs). The comment is load-bearing (it is the fork's stated licence) and GradientCodeEditor.vue:3 consumes the fork. → **NO-WAVE-OWNER** (demo/shared/utils.ts in no X bounds table).
- **GV-62 — r1 L-13: two bare `<button class="rail-btn">` with a locally-authored ghost-icon-button recipe (GradientEasingEditor.vue:178-197 + the scoped :269-294 block) — a third implementation of the icon-button concept inside one directory** (beside the DockControl at GradientVisualizer.vue:254 and the specimen Chip). → **X-W6.d** (the one-input-register goal; gate d2's field-primitive decision governs the readout row these buttons sit in).
- **GV-63 — r1 L-16: the pane takes a string-ref + `InstanceType` template ref (GradientPane.vue:9/:25) while both sibling components use `useTemplateRef`.** Dissolves under GV-16's typed-port cure. → **X-W4 rider** (dies with the `Ref<any>` chain), X-W6.a adjacency noted.
- **GV-64 — L3-3: `PickerSpace` is an identity alias of `SpaceId` (picker-color.ts:37, re-read) that this component imports across a feature boundary on the line after importing its sibling type correctly from the published surface** (GradientVisualizer.vue:25-26). → consumer edge **X-W6.c** (the leaf `model/types.ts` homes the model's space type); the alias's retirement **NO-WAVE-OWNER** (picker-color.ts unowned by X-W6).
- **GV-65 — reader-2 missed #2 (promoted): the radial round-trip silently zeroes an authored direction.** This seat re-read both halves: serializeGradient pushes an angle only for linear≠180 and conic (:151-155) — the radial branch emits nothing — while the parser initialises `direction = type === "linear" ? 180 : 0` (:214) and radial's preamble can only reject or fall through (:175-183). So with type=radial and Direction at 270, ANY debounced keystroke in the CSS panel writes `direction = 0` through applyCSS (:164) and the readout jumps. Lossless for linear and conic; radial is the asymmetric hole. Mitigating: direction is render-inert for radial (GV-45) — the loss is authored UI state, not paint. → **X-W6.a** (parser) **+ .c** (serializer); note the deeper cure is GV-45's (retire or re-scope the control for radial).
- **GV-66 — reader-1 missed #7 (promoted): the accordion's default-open contract silently dies — `openInterval` is a positional index never reconciled with the interval count.** :61 `ref(0)`, :144 index match, re-read; intervals shrink from the END (useGradientModel.ts:96-98), so open interval 2 → remove two stops → no row matches and every row renders collapsed with nothing explaining why. Same identity-vs-index disease as GV-37; the file already knows the cure (`:key="stop.id"`). → **X-W6.d** (path-erratum precondition).
- **GV-67 — reader-1 missed #8 (promoted): a circular quantity modeled as a clamped linear range with duplicate endpoints.** :232 `:min="0" :max="360" :step="1"` — 0deg and 360deg are one rendering (both serializers), so Home and End land on the same gradient, the D-axis's §5.2 "MET" is met vacuously, and dragging past either end stops dead where a circular control wraps. → **X-W6.b**.
- **GV-68 — reader-1 missed #4 (promoted): every interval row MOUNTS a full 27-tile specimen strip behind `v-show`** (GradientEasingEditor.vue:143-146 — the ramp at :152 is `v-if`, the strip at :161 is not) — hidden catalogue twins scale with stop count (~11 hidden strips × 27 chips × 49-point SVG paths at the D-axis's own 12-stop state), and EasingSpecimenStrip.vue:39-41's comment records the twins as a known condition without ever costing them. DOM-cost sizing wants one measured run (residue R-14). → **X-W6.d**.
- **GV-69 — reader-1 missed #6 (promoted): `break-all` on the CSS code well breaks tokens mid-identifier on the one surface whose job is to be read as code** (GradientCodeEditor.vue:93 `whitespace-pre-wrap break-all` — `oklch(0.7`/`5 0.15 145)` at the box edge), in a file otherwise fastidious about code legibility (:104-106). `overflow-wrap: anywhere` is the same protection without mid-token breaks. → **NO-WAVE-OWNER** (file unowned).
- **GV-70 — reader-1 missed #3, as narrowed by K-20 (PIN 4): the add affordance is unsignified and unpreviewable on coarse pointers.** Tap-to-add is operable (this seat's derivation); but the ghost is hover-only, `cursor-copy` is fine-pointer-only, the file's no-instruction rationale is hover-premised, and the minted stop takes the ramp's own colour — deliberately invisible at mint — so a touch user acts blind and confirms only by the appearance of a handle. Pairs with GV-32 (the same rail eats their scroll). → **X-W6.b** (gate b2's add-path recomposition).
- **GV-71 — r1 L-9's fleet half, retained at the identity split PIN 6 defines**: the three-idiom clipboard census (26 `writeClipboard` / 3 `useClipboard` files / 1 bare `navigator.clipboard`) travels with **X-W8**'s root-barrel extirpation; this pane's feedback divergence is GV-39's. *(Bookkeeping row — no new defect beyond GV-39; recorded so the fleet census is not silently dropped at the split.)*

### INFO

- **GV-40 — carried: the syntax-highlight fallback returns the caller's string unescaped into an `innerHTML` sink** (GradientCodeEditor.vue:43-53, re-read; unreachable-by-markup today). → **NO-WAVE-OWNER**.
- **GV-72 — reader-2 missed #5 (promoted): `easingCatalogue.ts:136` casts `term as JumpPosition` across a producer boundary that needs no cast** — glass-ui DERIVES `JumpTerm` from value.js's own `jumpTerms` (`useEasingPicker.d.ts` lines read by this seat: `import { jumpTerms } from "@mkbabb/value.js/easing"` → `type JumpTerm = (typeof jumpTerms)[number]`), so the types are identical by construction and the cast's only effect is to silence the exact divergence alarm this tree wants (the R2-5 failure mode in the opposite direction). One-line deletion. → **X-W6.d**.
- **GV-73 — D/F-18, observation only: the render tile is the one surface with no motion register and the most violent change** (a type switch replaces the whole painted field in one frame while every control around it eases). The prescribed cure stays killed (`--tile-render` is unregistered → discrete interpolation; gradient-type changes are discrete under CSS Images — a transition would DELAY the snap). Recorded as a register gap for the wave that owns gradient motion. → **X-W6.e rider**, under the H2 motion-quarantine law (no motion assertion ships unquarantined).

## Killed-claims register

r1's 18 kills carried intact (renumbered K-1..K-18 there; not restated — the r1 register's text remains authoritative for them beside this file's supersession note). **No motion-quarantine FALSE row is carried anywhere in this record** — the D/F-5 reduced-motion inversion stays killed under the M-17 class precedent, re-affirmed this seating.

New this seating:

19. **D/F-2's cure, easing limb ("one string that carries every field the model has")** — KILLED (reader-1, upheld): CSS gradient grammar has no per-interval timing-function production; `GradientInterval` is the whole EasingPickerValue payload. The ` in <space> <hue> hue` half survives as GV-8's cure; easing needs a non-CSS persisted home — a different cure with a different shape.
20. **D/F-17 owner-state limb (seeding from "possibly a trashed palette")** — KILLED (reader-1, upheld by this seat's own grep): no trash state exists on the port (`usePaletteStore.ts:51-59`; zero hits for trash/deletedAt/isDeleted). Index 0 can be stale, never trashed. GV-60 carries the surviving core.
21. **C-7's categorical phrasing ("no keyboard equivalent anywhere" — WCAG 2.1.1 Level A)** — KILLED (reader-1, upheld): the contenteditable textbox is a keyboard route to every model mutation, including add. GV-43 carries the honest charge.
22. **r1 L-1's enumeration ("the globs match zero files")** — KILLED (reader-2, upheld): 2 of the :232-238 object's 6 globs select the live `demo/color-picker` tree; the rules are unfireable (they ban specifier shapes W43 deleted), not unscoped. GV-4's conclusion unmoved.
23. **GV-28 (the r1 roster's own producer-relay nit: unsuppressed UA outline on the standard slider thumb)** — RETIRED (reader-1's argument, ratified — PIN 3): an `opacity:0` element composites outline and all at zero alpha; no reachable state paints it; the BH relay is not sent.
24. **K-19 (seat-minted): reader-2's "the specimen-ink door throws for ALL THREE back presets"** — KILLED IN HALF by this seat's own dist run: ease-in-out-back's fn(0.5)=0.6067 is in-range; that preset crashes only through the multi-sample walk doors. The ease-out-back half (1.0676, and the producer's default preset) is CONFIRMED and sharpens GV-1.
25. **K-20 (seat-minted): reader-1's "the pane's primary creative act has no discoverable expression on touch"** — NARROWED: tap-to-add is operable on touch by construction (`touch-none` guarantees `pointerup`; the commit handler is pointer-type-blind). The discoverability charge survives as GV-70 at MINOR.

## Superlatives — preserved with provenance (both readers' union, re-verified where newly claimed)

r1's ten stand (the material-contract paint stacks · the three-failure-mode focus system · the always-on hit inflator · the self-arguing add ghost · the editor truce · the `scrollIntoView` refusal · the sparkline ink floor · `gradientParse.ts` as the feature's best module · the one glyph painter with overshoot honoured · package hygiene + generated aliases). Added this seating:

11. **`applyCSS` is the correct shape for a parse boundary, and both sides of the invariant are implemented** (reader-2): the whole model swaps in or NOTHING changes, with the dead field-by-field partial apply named in the comment (useGradientModel.ts:151-157) — and its UI counterpart (the editor truce, GradientCodeEditor.vue:34-40/:68-79) enforces the same WIP-preservation law from the other side. Two components, one invariant. (That this correct boundary still destroys easing is GV-3's charge against what the model persists, not against the atomicity.)
12. **The byte-identity law is stated, motivated, and honoured** (reader-2, facts re-verified at r1): `linearInterval()`'s literal is byte-identical to a `:preset="linear"` picker emission (useGradientCSS.ts:44-62), generalized across the catalogue with the producer's formatting rule quoted — a consumer that identifies a cross-package identity requirement and then actually satisfies it, even though the invariant's rightful home is a published serializer (GV-12/GV-51).
13. **100% type-only cycle discipline** (reader-2): every one of the feature's seven import cycles closes through `import type` under verbatimModuleSyntax — which is exactly what keeps GV-13 a direction defect instead of a TDZ hazard. Five independent passes re-verified it; this seat re-confirmed the two forward VALUE edges.
14. **The readout rail's well is a contrast fix with its own receipt** (reader-1): GradientEasingEditor.vue:167-176 records the measured ~2.7:1 failure, the named precedent, and a cure that does two jobs — measured failure, cited precedent, honest comment.
15. **The Select composition is complete where it counts** (reader-1): `aria-label` on all three triggers plus real per-item `#description` slots inside the dropdowns, with the :159-161 comment recording WHY the per-select subtitle rows were excised. The reasoning is right even though the grid it lives in still clips (GV-19).
16. **No god module — and every defect on this axis is a boundary or ownership defect, never a size defect** (reader-2 + r3/r4/D independently): largest file 392 L, subject 279 L; the R.W4/S.W5 decompositions held.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

r1's R-1..R-12 carried unchanged (contrast ratios over the live aurora · route proportions · RTL mirroring · 12-stop degradation numbers · WHCM composite · phone-clip percentages · shipped-bytes closure · the 72-module delta count · the R2-1 blast frames + the two HYPOTHESIS exposure vectors (drag-to-overshoot; the uncontrolled `initialPreset` boot — **now sharpened by this seat's measurement that the default preset's fn(0.5) throws**, so the masked-today verdict rests entirely on EasingAuthoringStage's controlled binding) · the ID-collision hypothesis · the GV-36 fallback UX · the per-tick cost figures). Added:

- **R-13** — GV-53's rendered tab-stop count: the Chip→Toggle mapping and the port's `tabindex:"0"` are byte-verified; whether the producer applies any runtime roving behaviour wants one keyboard run.
- **R-14** — GV-68's hidden-twin cost at 12 stops: mount counts derivable from source (this seat: 27 tiles × N−1 rows, one strip visible); the DOM-node/layout cost wants one measured run before it is sized as a performance charge.
- **R-15** — D/D-3's tile aspect spread (0.747@1440 / 0.657@390 / 0.549@320): structural cause static and admitted into GV-19's frame (the tile sets width and `row-span-2` but no height/aspect-ratio, :223); the numbers are live-only.

## Closing verdict

**ADJUDICATED — BLOCKERS SUSTAINED (4).** The catalogue sells three curves that crash the instrument through seven throw doors — one of them the producer's own default preset, by this seat's own measurement (GV-1/K-19); the parse oracle certifies colours the renderer cannot draw (GV-2); the CSS panel destroys the pane's namesake work on a byte-identical keystroke (GV-3); and the pane's two commonest gestures silently re-host every authored curve (GV-41 — the row the r1 roster lost and both readers found). The r1 apotheosis is superseded in place: its 40 rows carry (one retired — GV-28), 33 rows are added, and its three X-W6 bounds preconditions are re-pinned unchanged at a wave spec that has not moved since `c654824e`. Routing: **X-W6 ×47** (.a ×14 · .b ×19 incl. riders · .c ×12 · .d ×9 · .e ×1 · .j ×2 — rows may carry two unit routes) · **X-W8 ×4** (+2 identity cross-books) · **X-W4 ×2** (+2 coordination/riders) · **X-W5 ×1** (+1 limb) · **X-W1 ×2** · **X-W10 ×2** · **X-W9 coordination ×4** · **GLASS-OWNED ×1** (GV-10; GV-28's relay withdrawn) · **NO-WAVE-OWNER ×9** (+3 rider halves) — the NO-WAVE-OWNER set is dominated by the two files (GradientCodeEditor.vue, EasingSpecimenStrip.vue) and one composable (useSpecimenRows.ts, under two BLOCKERs) that no wave in tranche X owns, which remains this component's loudest formation-boundary fact. 72 rows adjudicated, 25 claims killed (two of them the readers' own), 15 residue items to the live pass, 16 superlatives preserved. Nothing here stamps a wave; everything here is runnable by the session that does.

— end of apotheosis r2. The challenger corpus, both r2-seating reader returns, and the superseded r1 record's text remain immutable beside this file per E-1/E-3.
