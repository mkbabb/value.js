claude-fable-5 (served model id)

# fr-ConvergenceTimeline — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `fourier-analysis/web/src/components/equation/convergence/ConvergenceTimeline.vue` (146 lines; read whole by this seat) — the `/equation` transport chassis: play button + `<Slider variant="standard">` scrubber + `N=…` count.
**Substrate**: fourier `cd26c65` + dirty WT (re-pinned by this seat: `git log -1` + porcelain; the subject is `M` among the modified rows, matching the challenges' declared substrate) · installed glass-ui **4.0.0** (re-pinned: `package.json` version + the `./timeline` export key read directly) · reka-ui 2.9.x (source SFCs under `node_modules/reka-ui/src/Slider/` read whole) · producer glass-ui working tree at **8.0.0** (`e73ec80c`, "8.0.0 LIVE" — one full major PAST the 7.0.0 the D axis audited).
**Corpus**: three axis challenges (`audit/fourier-components/ConvergenceTimeline/challenge-{L-library 14+4, C-consumption 15+6, D-design 19+3}.md`, all read whole by this seat) × two hostile re-reader returns (design-and-usage · library-and-code), **delivered pre-merged as one consolidated return** — the two seats' cells are not separable in delivery, so this record contains no reader-vs-reader ruled-disagreements table; the incredulity work below attacks the merged corpus row by row instead.
**Method (M-23/L-18)**: every load-bearing verdict and every kill in the merged return was re-attacked against the tree before adoption. **All 10 kills survived this seat's attack and are RATIFIED**; two cell-level corrections were minted against the merged corpus (SC-1, SC-2), one severity was demoted against it (D·D-6, MAJOR→MINOR), and one cure route was sharpened by a producer fact one major newer than any axis read (SR-1). Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; product source untouched; this file is the sole write)**: full read of the subject; reka source reads — `SliderRoot.vue` (`useVModel` `passive:(props.modelValue===undefined)` ⇒ **controlled mode under a bound v-model**; `handleSlideEnd`'s bare `if (hasChanged)`; `updateValues` comparing `String(nextValues) !== String(modelValue.value)` and committing keyboard steps via `{commit:true}`; the template's `v-bind="$attrs"` FIRST, own `@pointerdown` snapshot LATER), `SliderImpl.vue` (**exactly** `@keydown/@pointerdown/@pointermove/@pointerup` — no pointercancel/lostpointercapture; `slideEnd` gated on `hasPointerCapture`), `SliderThumbImpl.vue` (`role="slider"`, own `aria-valuenow/min/max` off rootContext 0..100, `:aria-label="$attrs['aria-label'] || label"`), `Slot.ts` (`mergeProps(attrs, child.props)`) + `Primitive.ts` (`h(props.as, attrs, …)`); the full pointerdown merge-order derivation (below); installed dist reads — `slider-DQ95MET2.js` (`inheritAttrs` grep → 0; the single `"aria-label": n.$attrs["aria-label"] ?? void 0` hoist; the compiled `m(t.modelValue` thumb renderList; `disabled:{type:Boolean}`; `useDockHold`'s `!i() || a || !n || (n.keepOpen(), a = !0)` / `!a || !n || (n.release(), a = !1)` guards), `button-BNDWhAZb.js` (glass variant string with the `aria-pressed:bg-[color-mix(…)]` paint; `icon: "h-(--control-h-md) …"`; `"icon-sm": "h-(--control-h-xs) …"`; the `[&_svg:not([class*=size-])]:size-(--ui-glyph)` guard), `timeline.js` (literal `"aria-label": "Timeline"`; `class:"glass-track"/"glass-fill"/"glass-thumb"` LIVE; `onPointerup: f` AND `onPointercancel: f`; `role:"slider"` + `aria-valuemax:"1"` on the track), `ScrubberTimeline.vue.d.ts` (props = `modelValue?` + `label?` "Tooltip caret text" ONLY), `glass-ui.css` (`--slider-track-height` reads ×2; `.glass-slider[data-size=md][data-v-534634a7]` = (0,3,0); `.slider-range` ships `-webkit-backdrop-filter` **only**; `@layer` count = 0), `styles/utilities/base.css` (`@layer components` opens at :43; the six-leg `.tap-squish` list + `:active` + PRM reset), `styles/tokens/scheme-motion.css` (`--duration-fast: 0.2s` :67; `--font-stack-mono` :46; `--ease-standard` :216), `offsets-sizing.css:148-152` + `light-dark.css` coarse block + the `--touch-target` AQ.W3 §7 comment naming `Button size="icon"`; fourier tree reads — ConvergencePlot.vue `startLoop` (no guard; tick exits only on `!playing`), `stopLoop`, scrub handlers, `activeCount` (`> 0.5` threshold), `animDuration` (2 000–12 000 ms), `draw()` (`getBoundingClientRect` + `canvas.width/height` reassign per call), `onMounted` autostart, `onUnmounted` never clearing `playing`, the **two-root fragment template** with zero `inheritAttrs`, EquationView's `<ConvergencePlot class="flex-1">` callsite + its 5-prop surface (no `computing`/`error`), `style.css` `html{font-size:1.125rem}` → `1rem @ ≥768px`, AnimationControls.vue:66-90 (**native** `<button>` ×2, both with `:aria-label` + `<Tooltip>`, byte-identical FA paths), harmonics.ts (`amp > 1e-14`; `sliceW/availableRange/start` formula), FunctionInput.vue:184 (`:min="1" :max="100"`), ConvergenceLegend.vue:17 `v-if` + :90 13px, `tsconfig.json` (`strict:true`, `noEmit:true`, no composite/incremental), `index.html:19` → `public/fonts.css` self-hosted Fira Code, vite.config.ts PostCSS = `[@tailwindcss/postcss]` only; measurements run by this seat — `--slider-scrub` family = **23 declarations / 7 files** (per-file: BasisSelector 4 · EditorControlsDock 4 · GlassTimeline 1 · SliderControl 5 · ConvergenceTimeline 1 · MorphPhaseConfig 4 · HarmonicLevelGrid 4), `icon-swap` = 4 files, the FA path = 2 files, `lucide-vue-next` = 35 src files **against a devDependencies-only entry** (dependencies block re-read: no lucide), `size="icon"` = 38 / `icon-sm` = 0 / `variant="standard"` = 14, `goto(` census (`/visualize` ×**13** — SC-1), `/equation` in e2e = `visual-baseline.spec.ts:34` alone (body re-read: `.catch(() => {})` + timeout-1200 + screenshot + overflow gate, zero interaction), the C-7 threshold table **re-derived by an independent node run** (total=20 → minΔt 0.025136, 0/19 pairs under step; 49 → 0.009949, 6/48; 100 → 0.004823, **99/99**), producer 8.0.0 reads — `slider/types.ts:10` `SliderVariant = "scrubber" | "spectrum"` vs `git show v7.0.0:` `"standard" | "spectrum"`; `Button.vue` `emphasis/tone/size/iconOnly` with `ButtonSize = xs|sm|md|lg` and no `variant`; `button/index.ts` no alias; `timeline/index.ts` exports `Timeline` ONLY; `slider/Slider.vue` emits `valueCommit` and **no scrubStart/scrubEnd/pointercancel**.

## Scoreboard

**32 rows ADJUDICATED-CONFIRMED (1 BLOCKER · 8 MAJOR · 17 MINOR · 6 INFO, counting the 6 missed rows banked) · 10 killed-register entries, ALL RATIFIED by this seat's independent re-derivation · 2 seat cell-corrections + 1 seat demotion + 1 seat route-sharpening minted against the merged return · 9 superlatives ratified.**

The BLOCKER stands on this seat's own full re-derivation of the reka chain: **L·D-1 + C·C-1** — `scrub-start` fires without a matching `scrub-end`; the parent's rAF clock is stranded while the UI still reads "playing", and the stuck latch then poisons every subsequent gesture. The merged return's narrowed trigger set is CONFIRMED mechanism-for-mechanism (the controlled-mode pre-press snapshot; see K-4). The deepest structural fact in the corpus is the merged return's missed[4], ratified here: **the fork direction is inverted in the file's own header** — `glass-track`/`glass-fill`/`glass-thumb` are the LIVE class names of the producer's shipped ScrubberTimeline at the installed 4.0.0, so the P.W5 migration moved OFF the producer's correct component onto a lower-level primitive and thereby *created* the BLOCKER.

## The incredulity seat's own work (what was attacked, and what it yielded)

Every verdict below that matters was re-derived, not transcribed. The five decisive seat derivations:

1. **The pointerdown merge-order chain, end to end (decides C·C-9's upgrade AND K-4's mechanism).** SliderRoot's template spreads `v-bind="$attrs"` *before* its own `@pointerdown` snapshot ⇒ `mergeProps($attrs, {onPointerdown: snapshot})` = `[consumer, snapshot]`. That array falls through SliderHorizontal onto SliderImpl, whose template already binds its own `@pointerdown` on the Primitive tag; Vue's `renderComponentRoot` clones the root with fallthrough attrs via `mergeProps(vnode.props, fallthroughAttrs)` — existing first ⇒ `[implOwn, consumer, snapshot]`. Primitive declares only `as/asChild`, so the array lands in its `attrs` and `h(props.as, attrs, …)` attaches it whole to the real `<span>`. Vue invokes in order: **implOwn (slideStart → updateValues → update:modelValue → the consumer's `tArr` setter → `scrub-move`) → consumer (`scrub-start`) → snapshot.** C·C-9's "scrub-move can precede scrub-start" is therefore source-decided, not live-only — the upgrade is RATIFIED. And killed[10] is RATIFIED on the same trace: the handler provably reaches the DOM under the pin; SS-13 register item 2 stays struck.
2. **The controlled-mode snapshot (decides K-4, the C-1 trigger narrowing).** `useVModel(props,'modelValue',emits,{passive:(props.modelValue===undefined) as false})` — the consumer always binds `v-model`, so passive is `false` and `currentModelValue` reads the **prop**, which does not update synchronously during the emit cascade (prop propagation awaits the parent re-render). The snapshot, firing third, therefore captures the **pre-press** value even though the press has already moved the model. Releasing at the press point commits (`prevValue ≠ nextValue`); only a release at the pre-press value fails to. The kill of C-1's "drag-out-and-return by construction" trigger is RATIFIED; the BLOCKER's surviving deterministic triggers are (a) press+release inside the ±0.5-step band at the playhead and (b) pointercancel (L·D-2, whose handler-absence I verified as exactly four bindings in SliderImpl).
3. **The C-7 table, independently.** A node run from `harmonics.ts`'s own formula + `acos(1-2e)/π` inversion, written without reading C's table: 0.004823 / 99-of-99 at total=100; 0.009949 / 6-of-48 at 49; 0/19 at the default 20. Reproduces to the 4th significant figure (SC-2 records the one 5th-decimal nit). CONFIRMED at the demoted MINOR.
4. **The producer at 8.0.0 (SR-1, route-sharpening the whole F.W3 cure).** `SliderVariant` is now `"scrubber" | "spectrum"` — `standard` is GONE (14 more fourier sites join the F.W1 break surface, exactly as the merged return's killed[8] recorded as substrate drift) — while `timeline/` still exports the progressbar-only `Timeline` and the 8.0.0 Slider still declares **no scrub-session pair and no pointercancel**. Consequence: L·§5.1's "adopt `<GlassTimeline variant="scrubber">`" cure is **doubly stale** (7.0.0 killed the component per the banked fr-AnimationControls K-14; 8.0.0 renames the Slider variant out from under `standard` too), and the durable cure remains fr-AnimationControls K-14's: re-derive the unconditional session pairing on the Slider's own pointer events including `pointercancel`, or land the scrub-session contract upstream — a **GLASS-RELAY** ask, now three items (session pair on Slider-scrubber · an accessible-name prop · `aria-valuetext`), not L's one.
5. **The orphan-token family, re-counted.** `grep -rn -- '--slider-scrub' src/` → 23 declarations / 7 files, per-file distribution matching missed[2] exactly; the live producer knobs `--slider-range-bg`/`--slider-thumb-bg` verified in `glass-ui.css`. L's "six" and C's "3 lines" both under-count — killed[6] RATIFIED, and the banked fr-AnimationControls C-5 row ("3 writer sites") is hereby **AMENDED at the scope where the count lives** (the ColorSpaceSelector L-9.1 scope law): the F.W3 sweep budget is 23/7, and at five of those sites the deadness silently drops a per-instance `--track-color` tint to `var(--primary)`.

**Seat corrections and demotions minted against the merged return:**

- **SC-1 (cell correction, trivial)**: the e2e route census cell reads "`/visualize` ×12"; this seat's grep counts **×13**. The claim shape (zero functional `/equation` coverage; the sole visit is the screenshot sweep with swallowed navigation) is unaffected.
- **SC-2 (cell correction, 5th decimal)**: at total=20 the minimum adjacent-threshold Δt is 0.025136 by direct discrete difference (merged return: 0.025127; C: 0.02514). No comparison against the 0.01 step changes. Recorded only so the corpus carries the seat's own instrument.
- **SD-1 (severity demotion)**: **D·D-6 hover-dead-while-playing, MAJOR → MINOR.** The mechanism is CONFIRMED verbatim from this seat's read of the subject (equal (0,3,0) specificity, source order, `.is-playing` re-setting both `:hover` channels plus `border-color`, autostart making playing the arrival state). But no capability is lost: `cursor-pointer`, the focus ring, and `active:scale` all survive, the control stays discoverable, and the compounding aggravators (the 100 ms glyph blank, the 28 px target) are separately booked rows. A total loss of one secondary affordance channel on a still-operable control is MINOR by this corpus's own grading (its variant-nominal siblings sit at MINOR). The merged return's MAJOR is preserved in provenance.
- **SR-1 (route sharpening)**: item 4 above; folds into the L·D-7 and D·D-2 dispositions.

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring. Identity guards (folds by reference, never re-booked) are stated inline; the banked sibling record **fr-AnimationControls** already owns the fork-family identities (its L-1/L-3/L-6/C-4/C-5/M-1/K-14).

### BLOCKER (1)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **L·D-1 + C·C-1** | `scrub-start` (unconditional native `@pointerdown`, :46-50/:80) has no dual: `scrub-end` rides only reka's `hasChanged`-gated `valueCommit` (:52-56/:81; SliderRoot `handleSlideEnd`). Deterministic freeze triggers, narrowed per K-4: (a) press+release inside the ±0.5-step band at the playhead (step=1 of 0..100 ≈ 1% of track); (b) pointercancel (L·D-2). End state: rAF cancelled, `playing===true`, pause glyph + `.is-playing` still assert motion, and the stuck `:47` latch suppresses every subsequent `scrub-start` — the scrubber is inoperative until a value-changing drag completes. Second-order: the desynced latch enables the orphan-rAF path (L·D-5 scenario B). | subject :36-56/:80-81; SliderRoot (controlled `useVModel`, bare `if (hasChanged)`, snapshot-third merge order — seat derivations 1-2); ConvergencePlot :282-291/:57-76 (sole `startLoop` callers re-verified) | **F.W3** — identity SHARED with banked fr-AnimationControls L-1/L-6 (the two verbatim forks die in ONE unconditional-session re-derivation incl. `pointercancel`); this record adds the narrowed trigger set + the K-4 mechanism + SR-1's 8.0.0 route facts. Freeze-gesture + cancel probes → SS-13. |

### MAJOR (8)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **L·D-2** | No `pointercancel`/`lostpointercapture` anywhere in the chain (SliderImpl = exactly 4 handlers, my read; the only `pointercancel` in the glass chunk is `useDockHold`'s window cleanup, emitting nothing) and the subject adds none. A UA-cancelled touch ⇒ no `pointerup` ⇒ no `slideEnd` (gated on `hasPointerCapture`) ⇒ the D-1 end state, deterministic, not self-healing. The cleanest path into the BLOCKER. | SliderImpl.vue template (whole, my read); subject :80-81 | **F.W3** — dies in the same re-derivation; the value.js `useSliderTouchGates.ts` precedent (same primitive, cured downstream) recorded as pattern, not re-booked. Touch-gate first-tap aggravator → SS-13. |
| **L·D-3 + C·C-2 + D·D-1** | The play/pause `<Button>` has no accessible name and no `aria-pressed` — accname computes empty (children = two bare `<svg>`; producer contributes only `data-*`/`type`/`disabled`/`class`, my chunk read); the glass variant's own `aria-pressed:bg-[…]` paint (verified in the cva string) is declined for a hand-rolled `.is-playing`. It is also the WCAG 2.2.2 mitigation lane-frontend:624 leans on, over an autostarting clock. In-tree precedent carries both `:aria-label` and `<Tooltip>` — with the K-7 correction: those are NATIVE buttons on a different 48×40 chassis (my read of AnimationControls :66-90), so copy the label idiom, not "the identical control". | subject :61-66; button-BNDWhAZb.js; AnimationControls.vue:66-90 | **F.W4** — one state-bound `:aria-label` + `:aria-pressed="playing"` (then delete `.is-playing` for the variant's paint) available under the pin today; the R3-7a tooltip budget gains this site (C §6, ratified). Severity MAJOR (a11y-critical, not functionally blocking — the D-axis BLOCKER label is corrected as the merged return ruled). |
| **L·D-4 + C·C-3 + D·D-3** | The ARIA value trio + a duplicate `aria-label` fall through to reka's role-less `<span data-slider-impl>` (glass Slider: no `inheritAttrs:false`, hoists ONLY `aria-label` to thumbs — both facts re-verified in the chunk); the real `role="slider"` thumb announces the 0..100 t-axis (my SliderThumbImpl read), never N-of-total; `:aria-valuemax="totalHarmonics"` evaluates 0 when `trigHarmonics` is empty. axe `aria-prohibited-attr`/`aria-allowed-attr` shapes source-decided; rule firing → SS-13. | subject :75-78; slider-DQ95MET2.js; SliderThumbImpl.vue | **F.W4** (delete the trio; `aria-live="polite"` on the `:85` count span is the under-pin cure) + **GLASS-RELAY** — the M-3-corrected TWO-item ask (accessible-name prop AND `aria-valuetext`), now phrased against the 8.0.0 Slider-scrubber per SR-1. |
| **L·D-5 + C·C-4 + D·D-9** | (a) Keyboard scrubbing is inert while playing: reka commits Home/End/arrow/Page steps via `updateValues({commit:true})` with no pointer event (my SliderRoot read); `scrub-start` never fires, `stopLoop` never runs, the next tick recomputes `t` from `loopStartTime` and discards the keystroke — and playing is the ARRIVAL state (autostart re-verified). (b) From the D-1 stuck state, the desynced latch emits `scrub-end` with no `scrub-start`; `onScrubEnd` calls the guard-less `startLoop` (re-verified: no `rafId` check), orphaning chain #1, and `onUnmounted` never clears `playing` (re-verified) so the orphan re-schedules past unmount. | subject :52-56; SliderRoot keyboard path; ConvergencePlot :57-70/:274-291/:324-333 | **F.W3** for the enabling pairing defect (with an explicit keyboard-seek acceptance test, per the banked fr-AnimationControls L-3); the amplifier pair (unguarded `startLoop`, uncleared `playing`) is ConvergencePlot's own row → **F.W4**, attribution exactly as L filed it. |
| **L·D-7** | Undeclared shadow, direction-corrected by M-4: `./timeline` is a real installed-4.0.0 export whose `ScrubberTimeline` ships the strictly better contract — unconditional `onPointerup`+`onPointercancel` scrubEnd, keyboard step, `role="slider"` on the track (all re-verified in `timeline.js`), float 0..1 axis — and the subject + its verbatim twin `GlassTimeline.vue` (latch/adapter/dead-token table spot-verified by this seat) hand-roll it wrong twice. `grep glass-ui/timeline src/` → 0. The census undercount (10 shadow components, 2 call sites for the F.W3 budget) stands. | timeline.js + d.ts; subject :36-56/:69-82; GlassTimeline.vue:28-42/:125 | **F.W3** — identity = the census HARD-shadow row + banked fr-AnimationControls C-4/M-1 (folded, not re-booked). **The prescribed 4.0.0 adoption cure is REJECTED by this seat (SR-1)**: stale twice over (7.0.0 progressbar ruling K-14; 8.0.0 variant rename), and it would regress the accessible name (M-3's hardcoded `"Timeline"`, re-verified as a literal). Cure = unconditional session pair on the Slider composition + the three-item GLASS-RELAY. |
| **D·D-2** | `variant="glass"` and `size="icon"` are definition-absent at the producer working tree (my 8.0.0 read: `emphasis/tone/size/iconOnly/loading`, `ButtonSize = xs|sm|md|lg`, no alias in index.ts) — a hard `vue-tsc` break behind a RETAINED `./button` subpath, invisible to the export-map census method. Counts reproduce exactly on my tree: `size="icon"` ×38, `icon-sm` ×0, `variant="glass"` ×12 in the histogram. **Aggravated by killed[8]'s ratified drift**: 8.0.0 drops `SliderVariant "standard"` too — +14 sites (subject :71 among them) in a class the D-axis §7 ledger marked safe at 7.0.0. | producer src (this seat); web/src greps (this seat) | **F.W1** — census amendment: a new break-surface row class, *prop-surface breaks behind retained subpaths*, budgeted at 38+14 sites minimum plus the ≤101 variant upper bound; not a live defect under the pin. |
| **D·D-4** | The `:114-118` transition SHORTHAND truncates the producer's six-leg `.tap-squish` list (re-read whole at base.css: bg/border/shadow/color/opacity on `--ease-standard` + `scale` on `--spring-smooth`) while `:active { scale: var(--scale-press) }` still applies ⇒ the press snaps and the focus-ring shadow hard-cuts; duration literal 0.15s vs `--duration-fast: 0.2s` (both re-verified). Mechanism STRENGTHENED as the merged return ruled and this seat confirmed: base.css opens `@layer components` at :43 while the SFC scoped rule is UNLAYERED — unlayered normal declarations beat every layered one regardless of specificity, so D's specificity arithmetic was the weaker of two sufficient mechanisms. | subject :114-118; base.css :43/:258-278; scheme-motion.css:67 | **F.W4** — longhand pair or delete the block and let `.tap-squish` own it; refutes C·C-10's supporting cell (K-3). |
| **D·D-5 + C·C-5** | Literal `1.75rem` geometry defeats both producer knobs (`--ui-scale` coarse ×1.5 and the `--control-floor`→`--touch-target` 44px clamp — token block + AQ.W3 §7 comment re-read verbatim, and the comment names `Button size="icon"` as the retired-patch consumer class). `size="icon-sm"` = `h-(--control-h-xs)` exists in the pinned cva (re-verified) and is byte-identical on fine pointers. Arbitration ratified: C's arithmetic wins — fourier's root is 1.125rem below 768px (re-verified at style.css), so the phone figure is 31.5px against a 49.5px floor. | subject :108-109; offsets-sizing.css:148-152/:452-462; light-dark.css coarse block | **F.W4** — `size="icon-sm"` + delete :108-109 under the pin; **F.W1 rider** — 7.0.0+ `iconOnly`/`data-control-target` is the post-uplift seat. Rendered px at a real coarse viewport → SS-13. |

### MINOR (14)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D·D-6** *(seat-demoted from MAJOR — SD-1)* | `.play-btn.is-playing` (written after `:hover`, equal (0,3,0)) re-sets both hover channels plus `border-color` ⇒ zero hover delta in the autostart arrival state; the state ladder mixes `--background`- and `--foreground`-tinted families. Mechanism CONFIRMED from this seat's own read; consequence is affordance polish, not capability loss. | **F.W4** — cure with the D-15/C-6 variant decision (one paint owner). Perceptual half → SS-13. |
| **C·C-7** | The ×100 step-1 adaptation under-resolves the harmonic axis: **seat-reproduced** table (0.004823 min-Δt / 99-of-99 pairs under step at total=100; 6/48 at 49; 0/19 at the default 20). Scrub granularity on a time axis, not a break; the naive `:max="1000"` cure would ×10 the announcement rate D·D-9 flags — the cure is adjudicated separately from the defect (the ColorSpaceSelector addendum-2 law). | **F.W3** — dissolves if the re-derivation adopts the float 0..1 axis (the producer scrubber's own contract); else F.W4 with an explicit announcement-rate decision. |
| **D·D-8 + C·C-12** | Rendered unconditionally while the same-folder sibling guards (`v-if="harmonics.length"`, re-verified); `totalHarmonics===0` reachable via the `1e-14` amplitude filter (re-verified); `computing`/`error` exist at EquationView (re-verified) and cannot reach the timeline through ConvergencePlot's 5-prop surface (callsite re-read); both `disabled` props exist in the pinned API (chunk-verified) and are unused. | **F.W4** — empty/loading/disabled decision at the ConvergencePlot seam. |
| **L·D-6 + D·D-10 + C·C-8** | `--slider-scrub-track-height` never existed in glass-ui (dist grep 0, seat-run); the real knob `--slider-track-height` is already 20px at `size=md` ⇒ deadness only at this site, zero visual delta — L's honest limit ratified, L's specificity arithmetic ((0,3,0) producer vs (0,2,0) consumer, both unlayered) ratified over C's false tie (K-2). | **F.W3** — the sweep, at the **amended 23-declaration / 7-file budget** (seat count; identity = banked fr-AnimationControls C-5, AMENDED not re-booked; see M-2 for the five tint-dropping sites). |
| **L·D-10 + D·D-15 + C·C-6** | `variant="glass"` bought then overridden away on every material channel; survivors = `btn-pill`/`focus-ring`/`cursor-pointer`/`active:scale` + the truncated `tap-squish`. Strengthened by the layer argument (unlayered SFC beats layered Tailwind utilities categorically). Standing value.js feedback class (glass-ui-first-class / root-styling). The no-autoprefixer rider re-verified (PostCSS = tailwind only). | **F.W4** — variant-or-paint, not both; + **GLASS-RELAY** for the producer's own webkit-only `.slider-range` backdrop-filter gap (K-5's discovery). |
| **L·D-11 + D·D-12 + C·C-10 (PRM half)** | Zero `prefers-reduced-motion` posture while animating `scale(0.7)`, against both neighbours AND the producer's own `.tap-squish` PRM reset (re-verified); the prop contract has no channel for motion suppression and the parent autostarts, so the cure is a CONTRACT change, not a media query — L's coupling reading ratified as the strongest. | **F.W4** — identity = census FE §8 ungated-clock family (folded; same fold the banked fr-AnimationControls D-8/C-19 used). |
| **L·D-8 + C·C-11** | Verbatim FA path duplication (2 files / 3 copies each, seat-counted) against 35 lucide files; `icon-swap` duplicated across 4 files (seat-counted) with divergent durations (0.1s here vs 0.15s at the twin); `.play-btn` name reuse across incompatible chassis. | **F.W4** — one icon home (lucide or a shared module); glyph-family decision rides the same edit as D·D-13. |
| **L·D-9** | The file breaks its own inline A.W3.d law two rules later: `:114` carries the annotation + token; `:142` writes raw `ease` + raw 0.1s; the twin's copy carries both annotation AND token. Seat-verified from the subject read. | **F.W4** — same edit as D·D-4. |
| **L·D-12 + C·C-14** | Latent wrong-type hole: producer emit is `number[] | undefined`, `tArr` is `computed<number[]>`; `arr[0] ?? 0` guards the element, not the array. Unreachable in the current call graph (reka's sole writer assigns arrays; controlled mode, both seat-verified). The C-vs-L method contradiction is resolved by K-9 (my tsconfig read: `noEmit:true`, no composite — the check was always read-only-safe). | **F.W3** — the adapter dies with the fork; if the Slider composition is kept, widen the setter parameter — F.W4. |
| **L·D-13 + D·D-14** | The `scrubbing` latch exists only to de-duplicate `scrub-start` and is precisely what converts one missed commit into permanent desync (the producer's scrubber needs no latch — end signal unconditional); `.timeline-track-wrap` is one bare DOM node inside an already-flex parent (Slider accepts `class`, d.ts-verified); the 0.125rem inset is off-scale. The `mt-2` complaint has the M-1 structural cause. | **F.W3** (latch dies with the pairing re-derivation) + **F.W4** (wrapper + spacing, sequenced after M-1's fragment-root collapse). |
| **D·D-13** | `mode="out-in"` blanks the glyph ~100 ms per toggle (briefly a contentless button, compounding D-1's namelessness); 320 vs 384 viewBoxes at forced `size-3` ⇒ 7.5px vs 9.0px rendered widths, a 20% optical-weight delta; `size-3` deliberately opts out of `--ui-glyph` (the `:not([class*=size-])` guard, chunk-verified). | **F.W4** — crossfade + optically compensated glyphs, same edit as L·D-8. Perceived blank → SS-13. |
| **D·D-11** | `font-family: "Fira Code", monospace` drops two canonical fallbacks (`--font-stack-mono` re-verified at scheme-motion.css:46); 12px against the sibling/parent 13px (both re-verified) — three readouts, two sizes, the odd one on the transport's own baseline row. | **F.W4**. |
| **C·C-9** *(upgraded from C's UNPROVEN to source-decided — seat derivation 1)* | On a value-changing press the invocation order is provably implOwn → consumer → snapshot: `scrub-move` fires **before** `scrub-start`. Benign today (parent's move handler is `t.value = newT; draw()`), but the docstring's implied `start → move* → end` contract is not the enforced one. | **F.W3** — dies when start/end derive from one source. |
| **D·D-7** *(the finding survives its falsifier's death — K-1)* | The 3.5rem count column under-sizes its own domain — but on DESKTOP only (root 16px ≥768px: `N=10/100` = 57.6px > 56px; below 768px the column is 63px and only `N=100/100` overflows). Fira Code premise HOLDS (self-hosted @font-face, seat-verified), so the 0.6em advance arithmetic stands. Silent flex widening steals track width; the falsifier's "no html font-size rule" is FALSE (K-1). | **F.W4** — `4.5rem` or left-align with a stable gutter; failure MODE → SS-13 with the desktop/mobile split. |

### INFO (3)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **C·C-13** | `t: number` with no domain contract; `[0..1]` lives in prose; the clamp guards only inbound. | **F.W3** — the re-derived contract states units; no `withDefaults` correctly absent. |
| **L·D-14 + D·D-19 + C·C-15** | `/equation` has zero functional and zero axe coverage — vitest absent (scripts re-read); the sole visit is the screenshot sweep with `.catch(() => {})` (body re-read); both axe helpers drive `/visualize`/`/v/{slug}` only; and even with coverage, D-5/C-5 would escape (axe target-size = 2.5.8 AA 24px; the 44px floor is 2.5.5 AAA, outside the tag set) — C's rider ratified. SC-1's ×13 correction applies to the census cell. | **F.W4** — `checkA11y(page, "equation")` + one functional scrub spec gate the rest; identity = the census vitest-absent row (folded). |
| **L·§3** | R5-7 NOT APPLICABLE (`v-for` grep → 0, seat-run); the one loop in the rendered subtree is producer-internal (`m(t.modelValue` renderList, chunk-verified) — a third blindness class (producer-internal loops behind a component boundary), harmless here (one element ⇒ one thumb), systematically undercounting wherever a producer fans out over a consumer array. | **F.W4** rider on the mounted-instance denominator; no census correction owed on this file's account. |

## Missed rows — banked (all six seat-verified before banking)

| id | claim | seat verification | severity · disposition |
|---|---|---|---|
| **M-1** | ConvergencePlot is a FRAGMENT-ROOT component (two root nodes, no `inheritAttrs:false` — template re-read whole), so EquationView's `class="flex-1"` is silently dropped with a dev warning on every `/equation` mount; visually inert only because `.convergence-container` already carries `flex-1`. This is the structural cause of the `mt-2` symptom all three axes filed against the child — the plot and timeline are sibling roots, so the parent cannot own the spacing without collapsing its root first. | template :336-376 + callsite :308-315, both re-read | MINOR · **F.W4** — collapse the fragment root, then move the margin; sequencing binds L·D-13/D·D-14's cure. |
| **M-2** | The `--slider-scrub-*` orphan family silently drops the per-instance track tint at FIVE other sliders (`--slider-scrub-range-bg`/`-hover`/`-thumb-bg`/`-hover` set from `--track-color`; the live knobs are `--slider-range-bg`/`--slider-thumb-bg` — both re-verified in glass-ui.css) — every colour-coded slider paints `var(--primary)` instead of its assigned tint. Family = 23 declarations / 7 files (seat-recounted, distribution identical). | seat greps + glass-ui.css reads | MINOR · **F.W3** — AMENDS banked fr-AnimationControls C-5 at the true scope (L-9.1 scope law); ~4× L's prescribed budget. |
| **M-3** | The L·D-7 adoption cure carries an ARIA regression: ScrubberTimeline HARDCODES `aria-label: "Timeline"` (literal, chunk-verified) and its d.ts declares only `modelValue?` + `label?` ("Tooltip caret text", not a name). Adopting as prescribed swaps a wrong-target label for a wrong-VALUE one. | timeline.js + ScrubberTimeline.vue.d.ts, both re-read | MINOR · **GLASS-RELAY** — the ask is per SR-1 three items against the 8.0.0 Slider-scrubber: session pair · name prop/`aria-labelledby` · `aria-valuetext`. |
| **M-4** | The header comment inverts the shadow relation: `glass-track`/`glass-fill`/`glass-thumb` are the LIVE class names of the producer's shipped ScrubberTimeline at 4.0.0 (all three verified as `class:` literals in timeline.js) — the "166 LOC shadow recipe" was a fork of the producer's canonical scrubber, and P.W5 migrated OFF the correct component onto a lower-level primitive, creating L·D-1/D-2. Corrects lane-frontend:382's "prose comments only" at its root, and partially corrects L·S-4/D·S-1/C·§6, which accepted the header's framing. | timeline.js class literals; subject :5-7 | INFO · **F.W3** — the re-derivation corrects the header; census cell correction rides the F.W3 record. |
| **M-5** | While playing, the whole reka Slider subtree re-renders 8–50×/s (fresh `[Math.round(t*100)]` array per tick; `animDuration` 2 000–12 000 ms — both re-verified) atop a per-frame `getBoundingClientRect` + full canvas bitmap reallocation in the parent (`draw()` re-read: unconditional `canvas.width/height` reassign). The corpus costed only the AT announcement rate. | subject :38-39; ConvergencePlot :37-41/:80-92 | INFO · **F.W4** — magnitude → SS-13 (one trace on `/equation`). |
| **M-6** | `lucide-vue-next` sits in devDependencies (`^1.0.0`, re-verified; the dependencies block holds no lucide) while imported from `src/` at 35 files — `npm ci --omit=dev` builds cannot resolve. The tight claim is lucide alone (reka/cva/clsx/tailwind-merge are peer/transitive concerns). | package.json + seat grep | INFO · **F.W0** — packaging correctness before F.W1 opens. |

## Killed-claims register — 10 entries, ALL RATIFIED by this seat

Each kill was independently attacked; the deciding instrument is this seat's own where listed.

| # | killed claim | refutation (ratified) | seat instrument |
|---|---|---|---|
| K-1 | D·D-7's falsifier "no html/:root font-size rule in style.css" | style.css:40-49 sets `html{font-size:1.125rem}` → `1rem @ ≥768px` — root is 18px BELOW 768 and 16px at/above: the exact opposite. The DEFECT survives desktop-only at MINOR (roster row); the Fira premise holds (self-hosted @font-face verified). | my style.css + fonts.css reads |
| K-2 | C·C-8's "specificity tie, source order decides" repair cell | No tie: `.glass-slider[data-size=md][data-v-534634a7]` = (0,3,0) vs consumer (0,2,0); both unlayered (`@layer` grep → 0 in glass-ui.css). L counted correctly; C's cell would mislead a repair wave. | my glass-ui.css extraction |
| K-3 | C·C-10's "`--duration-fast` ≡ 150ms" supporting fact | `--duration-fast: 0.2s` (scheme-motion.css:67, my read); the 150ms was a `var()` FALLBACK literal. The consumer's 0.15s runs 25% fast — D·D-4's version stands, C's evidence dies. | my token read |
| K-4 | C·C-1's "drag-out-and-return fails to commit by construction" trigger | The snapshot fires THIRD in the merged array but reads the **pre-press** prop (controlled-mode `useVModel` — stale synchronously), so returning to the press point DOES commit; only a release at the pre-press value fails. BLOCKER unharmed; trigger frequency was overstated and no repair wave should size from it. | seat derivations 1-2 (full merge-order + controlled-mode trace) |
| K-5 | D·D-15's "glass ships the backdrop-filter pair" rider evidence | `.slider-range` ships `-webkit-backdrop-filter` ONLY (rule extracted whole by this seat) — a producer gap worth its own relay. D-15's conclusion survives sharpened: Safari<18 loses the button's unprefixed blur while the slider keeps its prefixed one; no-autoprefixer confirmed (vite PostCSS re-read). | my glass-ui.css + vite.config.ts reads |
| K-6 | L·D-6's "six orphaned declarations" cure scope + C·C-8's "exactly 3 lines" measurement | Both under-count: the family is **23 declarations / 7 files** (my recount, per-file distribution verified) and five sites drop live tint (M-2). The F.W3 budget is ~4× L's. | my repo grep |
| K-7 | L·D-3's "identical control one directory away" precedent cell | AnimationControls' two play buttons are NATIVE `<button>` elements on a different 48×40 chassis (my read) — "identical glyph + identical labeling opportunity" is the true form; a cure copying "the identical control" would import the wrong chassis. Severity unaffected. | my AnimationControls read |
| K-8 | D·D-2 §7's "Slider `variant="standard"` → retained \| safe" ledger row | True at 7.0.0 (my `git show v7.0.0:` read), **stale at the 8.0.0 working tree**: `SliderVariant = "scrubber" \| "spectrum"` (types.ts:10, my read) — +14 sites join the F.W1 break surface. Substrate drift, not authoring error; F.W1 must not budget from the 7.0.0 snapshot. | my producer-tree + git reads |
| K-9 | C·C-14's "not run — `vue-tsc -b` writes build info" abstention (vs L·D-12's run) | Unnecessary: `noEmit:true`, no composite/incremental (my tsconfig read) — `--noEmit` writes nothing; the claim was statically decidable anyway and both axes derived it correctly. The corpus carries no UNPROVEN marker a sibling axis already discharged. | my tsconfig read |
| K-10 | D·D-9's uplift-risk half / SS-13 item 2 ("does `@pointerdown` reach the DOM?") | DISCHARGED STATICALLY: the full merge chain (no `inheritAttrs:false` at glass → SliderRoot `$attrs` spread → default fallthrough ×2 → Slot `mergeProps` concatenation → `h(as, attrs)`) lands the whole handler array on the real `<span>` — re-derived end-to-end by this seat. The producer's 7.0.0 warning describes its OWN re-architected Slider, not the pinned chain. No live drag test owed. | seat derivation 1 |

## Superlatives — ratified with provenance (L-18 runs both ways)

- **S-1 · `tArr` (:38-44) is the component's best code** (D·S-2 + C·S-6, merged-return S-1 — ratified). Getter/setter `computed` over the prop, no shadow state; idempotent on all 101 reachable integers; `arr[0] ?? 0` + the clamp make it total; the getter cannot re-enter the setter (reka writes `modelValue` only from `updateValues` — my SliderRoot read). Sidesteps the `defineModel` async-round-trip trap value.js's own memory records.
- **S-2 · Zero teardown surface, zero teardown leaks** (L·S-2 — ratified). No listener/timer/observer/rAF of its own; the producer components own symmetric cleanup (chunk-verified `onBeforeUnmount` + `useDockHold`'s window pair). The post-unmount orphan rAF in L·D-5 is NOT this file's leak — it holds no resource that could leak.
- **S-3 · The dock-context header sentence is a comment doing real work** (L·S-4 + C·S-4 — ratified; the `useDockHold` null-guards re-extracted verbatim by this seat). Three independent falsification attempts (both axes + the merged return) came back empty; so does this seat's. *Bounded by M-4*: the same header's shadow-direction sentence is inverted — credit the dock sentence, correct the migration sentence.
- **S-4 · `class="size-3"` honored by producer design** (C·S-5 — ratified; the `:not([class*=size-])` guard re-extracted). The model refutation-superlative: a hunted defect the tree disproved, with the exact guard quoted.
- **S-5 · D·S-3's contrast work computes rather than asserts** — ratified with the producer's own annotations re-read by this seat: token comments "WCAG AA: 5.21:1" (color-radius.css:45) and "7.64:1" (dark-arm.css:47) bracket D's independently derived 5.19/7.72. Honest caveat list preserved; composited-through-blur residual → SS-13.
- **S-6 · C·C-7's threshold arithmetic is the corpus's best quantitative claim** — ratified by this seat's OWN node run, derived from the source formula before consulting C's table: 0.004823 / 6-of-48 / 99-of-99 reproduce (SC-2's 5th-decimal nit recorded).
- **S-7 · A precisely typed, one-directional emit surface** (L·S-3 — ratified). Modern tuple-form `defineEmits` with a named payload; all four emits consumed, none dead, none undeclared. Whatever is wrong with the scrub machine is a WIRING defect, not a state-management one — which is exactly why one re-derivation dissolves four findings.
- **S-8 · Goldilocks module** (L·S-1 — ratified). 146 lines / 57 script, one job, 4 props / 4 emits, zero lifecycle hooks, zero store reach — against a 410-line parent. The extraction cut the boundary in the right place; the defects live in the seam it left, not in the split.
- **S-9 · Subpath-correct dependency granularity** (C·S-3, with C·S-1/S-2 on the same basis — ratified). `/button` + `/slider` are real keys in the 80-key exports map; zero value.js, zero API/store coupling — the F.W2 value.js cut and R6-8's entanglement have no purchase here.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. axe rule firing on `/equation` (`button-name` critical; `aria-prohibited-attr`/`aria-allowed-attr` serious) — DOM shapes source-decided above; repro = `checkA11y(page, "equation")`, the helper exists twice already.
2. ~~The `@pointerdown` fallthrough~~ — **DISCHARGED** (K-10, seat derivation 1). No live test owed.
3. `aria-valuenow` announcement behaviour on the focused thumb during playback (8–50 changes/s is confirmed arithmetic; coalescing vs storm is AT-dependent).
4. D·D-7's failure MODE (silent flex widening vs clipping) — with K-1's desktop/mobile split: repro at ≥768px, `N=10/100`.
5. Perceptual halves only: the 60%→85% hover step in light theme (now MINOR per SD-1) · the ~100 ms out-in glyph blank · Safari<18 blur asymmetry (`.play-btn` unprefixed vs `.slider-range` prefixed-only).
6. The touch-gate first-tap-swallow frequency on a real device (code paths proven; user-visible rate not).
7. M-5's magnitude — one performance trace on `/equation` (Slider subtree re-render rate + per-frame canvas reallocation cost).
8. The D-1/D-2 freeze gestures as live confirmation theatre for the F.W3 acceptance tests (the mechanism needs no proof; the CURED state will).

## Identity guards (anti-rename) — folds by reference

- The scrub-pairing family (L·D-1/D-2/D-5-enabling, C·C-1/C-4) is ONE identity with banked **fr-AnimationControls L-1/L-3/L-6/C-4/M-1** — the two verbatim forks die in one F.W3 re-derivation; this record contributes the narrowed triggers, the K-4 mechanism, and SR-1's 8.0.0 route facts. Never re-booked per fork.
- The dead-token family is banked **fr-AnimationControls C-5**, AMENDED here to 23/7 with the tint-drop consequence (M-2) — one identity at the corrected scope.
- The 4.0.0-adoption route question is banked **fr-AnimationControls K-14/R-10** — extended by SR-1 (8.0.0: Slider gains `scrubber`, loses `standard`; timeline stays progressbar; still no session pair upstream).
- Reduced-motion/ungated-clock rows fold to the census **FE §8** family; coverage rows fold to the census **vitest-absent** row; the tooltip-name gap extends intake **R3-7a**'s budget by this site; **R5-7** is N/A here by measurement and **R6-8** is unreachable by construction (S-9).
- lane-frontend cell corrections riding F.W3: `:382` (prose-only → live producer CSS one import away, M-4) · `:446` shadow aggregate (+1 component, L·D-7) · the goto census (×13, SC-1).

## DISSENT — preserved, not ruled away

1. **The merged delivery is itself a method note.** Two hostile re-readers were commissioned; one consolidated return arrived. Per-seat attribution is unrecoverable, so the L-14 suspicious-agreement check between the two readers could not be run — this seat compensated by re-deriving every load-bearing cell against the tree directly (ten kills, five derivations, three census recounts). Where the merged return and this seat agree, the agreement is instrument-level, not citation-level; the two corrected cells (SC-1, SC-2) and the demotion (SD-1) are offered as the non-inheritance proof the format demands.
2. **Severity ordering, unresolved and preserved**: a reasonable seat would rank L·D-3+C·C-2+D·D-1 (the nameless transport over an autostarting clock) above L·D-2 within the MAJORs — the a11y row is the one failing users today on every visit, while the cancel path needs an interrupted gesture. No ruling required; both ride F.W3/F.W4 adjacent edits.
3. **SD-1 is the seat's judgment call, not a fact correction.** The merged return's MAJOR for D·D-6 is defensible (primary control, arrival state, whole affordance channel); the demotion rests on capability-preserved grading consistency with the corpus's own MINORs. A forming X·F spec that grades affordance channels as first-class may restore MAJOR; the mechanism cells need no re-derivation either way.
4. **The 8.0.0 producer facts (K-8, SR-1) are substrate drift recorded against a moving target**: the fourier pin is 4.0.0 and nothing is broken today; every 8.0.0-derived budget number in this record must be re-pinned at F.W1 open (the producer has shipped two majors during this audit's lifetime — the BANK-2-style re-trigger belongs in the F.W1 charter, not here).

---

**VERDICT — DEFECTIVE, ADJUDICATED**: 1 BLOCKER (the scrub contract does not close — this seat's own full-chain re-derivation) + 8 MAJOR + 17 MINOR + 6 INFO survive across 32 rows (6 of them corpus misses banked after seat verification); all 10 kills RATIFIED; the file's header inverts its own shadow provenance (the producer's scrubber was the original, and the migration off it is the BLOCKER's origin); the component's own state-bridge, teardown discipline, and module proportions are genuinely exemplary — the defects live in the seam, and one F.W3 re-derivation with a three-item GLASS-RELAY dissolves the worst six at once. Routings are M-25 agglomeration input; nothing here stamps a wave.

— end of apotheosis. The three challenge files and the merged re-reader return remain immutable beside this record per E-1/E-3. This document is the sole write.
