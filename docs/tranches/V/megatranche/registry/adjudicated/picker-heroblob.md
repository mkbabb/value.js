**Served model: `claude-fable-5` (Fable 5)** — observed in this seat's own system prompt, not inherited; fresh adjudicator per M-23/L-18.

# picker-heroblob — adjudicated apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter/apotheosis for component `picker-heroblob` (`demo/picker/visual/HeroBlob.vue` + its seat/fixture/boot chain).
**Date**: 2026-08-07 · **HEAD**: `ee9474e4` · branch `tranche-u` · **Corpus**: challenge-C (13 rows + negatives, Opus 5), challenge-D (17 rows, Opus 5), challenge-L (15 rows, Opus 5) under `audit/components/picker-heroblob/` — r2 governs; the three axis files ARE the r2 corpus (no earlier revision exists in the directory).
**Re-readers adjudicated**: design-and-usage and library-and-code — both returns attacked before either was adopted; every verdict-level disagreement re-derived by this seat with its own instruments.
**Law compliance**: `codex-provenance/motion-quarantine.md` swept — its 34 M-xx rows cover the 46 REPORT-AUTHORED files; **zero** M-xx rows touch this corpus's three axis files (grep `heroblob|HeroBlob` over the quarantine → 0), so no FALSE motion row can be carried here by construction. The corpus's own PRM claims are the *favorable* kind (D's negative #3: PRM honest, one static frame) and are not quarantined rows. **Status verbs: every disposition below is ADJUDICATED, not VERIFIED — nothing here stamps a wave.**

**Arbiter's own instruments this session (all static + node against the installed dist; zero browser probes — probe parsimony; product source READ-ONLY):** full reads of `HeroBlob.vue` (317 lines), `e2e/smoke/oracles/o12-blob-seat.spec.ts` (whole — the five probe points and both assertions), `e2e/smoke/fixtures/blob-timing.ts` (whole), `demo/picker/seat.css` (whole), `test/picker-blob-config.test.ts` (:1-45), `useAtmosphere.ts` (:30-40, :60-110, :280-406), `useOverture.ts` (:100-160), `ColorPicker.vue` (:88-96, :150-165), `ConsoleRail.vue` (:125-140), `keys.ts` (:1-20), `useContrastSafeColor.ts` (:133-146, :205-215, :340-350), `useMarkdownColors.ts` inject sites, `overture.css` (:160-178); **dist reads**: `blob.js` setup body whole (:1550-1650 — the M-1 decider), `createCanvasLifecycle-*.js` (:105-160 + IO handler :72-74 — the M-4 decider), `color.wgsl-Ct5o9dpf.js` context-loss handlers (:56-64), blob.js import list (:1-8); **node runs**: `deriveBlobPalette('lab(92% 88.8 20)')` across 5 option arms (ceil=seedC / 0.16 / omitted / bodyLightness 0.65 / lightnessFloor 0.15 — all byte-reproduced), `oklch(0.5 0.23 30)` → meanC 0.1688, both `cssToOklch` throw classes, `BLOB_HERO`/`BLOB_CONFIG_DEFAULTS` atom census via import; **esbuild in scratchpad**: tree-shaken two-symbol bundle, `/blob` = **77,452 B** (vue external) vs `/blob-config` = **1,667 B**; **greps** (all repo-scope): `rimColor` demo → 0, `forced-colors` picker+boot → 0, `instrument-title-gap` demo → 0, `rendererStatus` demo → 0, `bumpProbeEpochOnMount` → ConsoleRail/useViewAccents/internal only, `blob-timing` importers → **4** files, `<Blob\b` → 1 component site, `deriveBlobPalette(` → exactly 2, `0\.76` picker → 4 restatements 0 derivations, `@keyframes` css census → 2 files, `update:paused` in blob.js → exactly 1 (the emits array; `emit("update` → 0); `REPORT.json` re-read directly (row 0 canvas 1 + `WebGL: context lost.` + settle 18905; the `canvas.goo-blob-canvas` bleed rows on BOTH mobile matrices at `/#/`, `/#/blob`, AND `/#/does-not-exist`); `package.json#exports` (7 keys, no root) vs `tsconfig.demo.json:42-49` (8 keys incl. root/parsing/units) vs `ls dist/subpaths` (no parsing/units) vs `diff -q` tarball-css.d.ts (differ TODAY); glass-ui `package.json` carries both `./blob` and `./blob-config`; the C-13 flip algebra re-derived by hand on both sides (`plateL > 0.83` / `plateL < 0.17`, flipped |push| = 0.15+|δ|, |push| < 0.30 strictly).

**Cross-registry identity guards consulted (anti-rename)**: `registry/adjudicated/BlobPane.md` (rows 1/2/3/11/13/21 share this surface), `registry/adjudicated/ColorSpaceSelector.md` (L-14 = this corpus's L-7), `registry/CARRY-CUT-LEDGER.md` (CC-035, CC-036, CC-057, **CC-061** = MT-F032 "HeroBlob does not carry the current color's vibrancy" — the pre-existing ledger identity of this corpus's central BLOCKER).

## Scoreboard

**29 roster rows survive: 1 BLOCKER · 14 MAJOR · 10 MINOR · 4 INFO. 13 claims/limbs KILLED with written refutations (one killed by this seat against both readers). 9 reader-missed rows adopted (one of them killed). 0 NO-WAVE-OWNER — every surviving row has an X-wave or relay owner.**

Blocker-weight survivor: **C-1/D-1/L-2** — the component's stated purpose ("the picked colour made flesh") is arithmetically not delivered: the shipped derive+floor path yields max ramp C 0.0879 against seed C 0.2725 (32%; mean 13%), two of four stops white, while the producer's shipped `bodyLightness` option returns the full seed chroma at the same body lightness — byte-reproduced by this seat in five option arms. The ledger already carries this identity as **CC-061 → X-W6**.

## Ruled disagreements (the tri-fold's actual work)

| id | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-5 / L-8 consequence** | KILLED the vacuous-gate limb (probe 5 superset); MINOR drift | CONFIRMED MAJOR ("the oracle passes by construction") | **Reader-1 wins — consequence KILLED, row demoted to MINOR** | My read of `o12-blob-seat.spec.ts:111-137`: five probes, the fifth at `[cx, wrapper.y + 2]`; assertion = `closest(".glass-dock, nav")` membership. Bead top sits at `wrapper.y + (0.5 − 0.325)·fp ≈ +19.7 px`; probe 5 sits at +2 px — **strictly above the true arc for any bodyRadius ≤ 0.48**. The dock is a band above the card; a dock reaching the bead's topmost arc necessarily covers +2 px first, so probe 5 dominates every arc probe on the only axis where occlusion is geometrically possible. Shrinking r from 0.325·fp to 0.26·fp cannot flip the verdict; the undersized diagonals are dead weight, not vacuity. Reader-2's "all land inside the true silhouette" is false for probe 5 — it lands *outside/above* it. |
| **L-1 severity** | BLOCKER (esbuild 159,329 B) | MAJOR (production-chunk landing unproven) | **MAJOR — reader-2's grade; reader-1's mechanism adopted with a corrected number** | My own esbuild run (treeShaking, esm, browser, **vue external**): `/blob` two-symbol bundle = **77,452 B** vs `/blob-config` = **1,667 B**. Reader-1's 159 KB included bundled vue runtime — inflated, but the load-bearing fact stands: the barrel does not shake, and Rollup places a module statically imported by an eager module (useAtmosphere.ts:36, re-read) in the eager chunk regardless of HeroBlob's dynamic import. IDENTITY: this is **BlobPane row 21 (L-5 there), routed BUILD → X-W2.a** — one identity, not re-booked. That record's "bundle cost measured zero" cell survives as *total-bytes* truth (the engine ships either way) but its "boundary, not size" framing must now carry the placement half: ~77 KB of engine parses on the boot path, proven static. Severity dispute recorded against BlobPane's MINOR: this seat grades the identity MAJOR on the strengthened evidence; the X-W2 wave inherits both cells. |
| **C-13 severity** | MINOR | MAJOR | **MINOR** | Algebra re-derived by hand and exact on both sides (`plateL > 0.83` positive, `plateL < 0.17` negative; flipped push 0.15+\|δ\| with no re-test; per-stop clamp silent). But the row has no user-visible consequence independent of C-1 — the transposition (`bodyLightness` inside the derive) deletes the whole function. A mechanism row folded into the cure it motivates is MINOR. |
| **D-4 / C-8 severity** | MAJOR | MINOR | **MINOR** | My five-arm node run: ceil=seedC, ceil=0.16, and ceil-omitted are **byte-identical** (the option is inert, not merely non-binding), and the comment's named case `oklch(0.5 0.23 30)` derives meanC 0.1688, not 0.23. Real, false-comment-bearing — but the rendered consequence is already charged in full to C-1; this is the same wound's paperwork. Dies with the same cut. |
| **C-3 severity** | MAJOR (C-3/L-6/D-6 merged) | BLOCKER (C-3) | **MAJOR** | Chain re-walked at HEAD (useAtmosphere:291-325 latch feed → useOverture:106-116 `if (open && !b2.value)` latch-once → :152+ b4 → ColorPicker:94). Real, observed once (REPORT row 0: canvas 1, `WebGL: context lost.`, settle 18905), trigger unpinned (three injection strategies failed in Chromium — reader-C's own honest record). The lost surface is an `aria-hidden` ornament; the session degrades, the product functions. BLOCKER over-grades an ornament-tier absence; the coupling + the zero-consumer `rendererStatus` seam is a MAJOR with a shipped-seam cure. |
| **C-7 disposition** | filed UNPROVEN (disjunct unresolved) | CONFIRMED MAJOR | **CONFIRMED MAJOR on the premise; the branch goes to residue** | The premise is statically certain from my reads: no pointer handler in the SFC, `pressLabel` omitted so blob.js renders the hit layer conditionally (`f.value ? button : …`, re-read), root `pointer-events-none` (:10) — and O-12·3 (:139-160) demands a ≥6/255 frame diff on hover. A gate asserting a response the component *cannot produce* is defective in both branches (RED, or green off the 1.6×-overscan composite artifact). Which branch holds is live-only — residue item 6. The defect books now; the branch resolution cannot block it. |
| **D-15** | CONFIRMED MINOR (band-arm law) | KILLED (the law scopes the footprint formula) + replacement M-1 | **RESCOPED — reader-2's kill of the law limb adopted; the row survives on the arithmetic** | seat.css:14-16 re-read: "ONE cqi FORMULA at every viewport (no band arms): fp = clamp(…)" — the law legislates the *footprint*, and :86 explicitly styles the min-height "a clamp, never a band arm". The quality rung changes no layout box. What survives is sharper: the proxy is provably wrong (container-scaled footprint vs viewport-scaled quality — identical 73 px painted body gets `full` at 1440 and `half` at 390; DPR misfits both ways) **and** frozen at mount (M-1). |
| **M-1 vs the BlobPane record** | (absent from reader-1) | NEW MAJOR: producer snapshots the config at setup; only three colour atoms live | **CONFIRMED — this seat re-derived it against dist; it SHARPENS BlobPane NEW-B1, does not contradict it** | My read of blob.js:1559-1650: `s = t.config ?? a` snapshots once; the live re-read `c = () => t.config ?? a ?? s` appears in exactly TWO places — `M()` (token resolve) and the deep-watch source over `paletteStops/satelliteColors/rimColor`. The renderer (`We({…, config: s})`), satellites (`xe(s, …)`), hit radius, and the dpr policy (`:1475 l.quality === "half"`) all bind the snapshot. So: geometry/surface/quality atoms delivered via fresh prop objects **never reach the renderer after mount**; membrane/satellites/interaction stay live only as shared reactive-proxy references (they can't mint a wake, but in-loop reads see writes); the three colour atoms are the only fully-live seam. HeroBlob:62-66's "liveConfig() re-reads the prop… exactly the wake-repaint shape" is true **only** for the colour atoms. BlobPane NEW-B1's 13-of-31 claim is the proxy-side half of the same fact; this row supplies the snapshot-side half. User-reachable: the `<lg` half-res quality rung never re-arms across a 1024 px resize/rotate/zoom. |
| **M-4 (reader-2's missed row)** | — | MINOR: emerge re-measure silently skipped when off-screen-parked; backing "stays frozen" | **KILLED IN CONSEQUENCE by this seat** | My read of createCanvasLifecycle: the skip at that instant is real (`D("manual")` resizes only when the token set empties), **but** the IO re-entry handler (:72-74) calls `e.resume("off-screen-io"), e.isArmed() && e.hasHooks() && e.resize(), e.wake()` — **`resize()` fires explicitly on re-entry regardless of the token set**. The 0.35× backing persists only while the ornament is off-screen (invisible), and is re-measured the moment it can be seen. Mechanism-observation grade only; the stated terminal consequence ("frozen, with no warn, in exactly the case it was written for") is unreachable. |
| **D-8 severity** | MAJOR (D-8/D-9 merged; G unwired is the sharpest limb) | MINOR | **MAJOR** | The canon clause is verbatim ("no Blob-derived minimum block size enters it", VISUAL-CONSTITUTION:182) against a live `min-height: calc(0.76·fp − 0.75rem)` binding at two of three arms — and my grep confirms **`--instrument-title-gap` has zero hits in demo/**: the constitution's G, the basis of the whole §3.2 inequality, has no live referent. A canon law with no measurable referent plus a binding violation of its sibling clause is MAJOR. |
| **L-15 / C-4 severity** | MAJOR (formation-level output) | INFO | **MAJOR** | Three independent rigs triangulate (264 ms unthrottled / 928 ms / 1056-1108 ms medians at 4×) and all locate the cost in the ONE indivisible engine-init task the demo's own forensic names (useOverture:120-146, re-read). CC-036 (X-W2) currently writes its gate as "p75 LCP + JS-eager gzip"; this corpus proves an LCP-only born-RED would be born un-actionable and closeable-without-cure. A correction that re-aims a ledgered wave's gate axis is MAJOR, not INFO. |

## Adjudicated defect roster

Severity is this seat's final grade. Disposition names the X wave that owns the surface (per `CARRY-CUT-LEDGER.md` §1.D/§1.H and the BlobPane record's ownership table: **X-W6** owns `BlobPane.vue` + `HeroBlob.vue` + the picker stage incl. `useAtmosphere.ts`; **X-W2** owns the boot boundary; **X-W8** owns config/barrel subtraction; **X-W10** owns canon), an identity-fold into an already-adjudicated row, or the standing glass-ui BH relay. Line anchors are HEAD `ee9474e4`; re-resolve before executing any cure (D-19).

| # | ids | sev | claim (adjudicated form) | proof (arbiter-verified) | disposition |
|---|---|---|---|---|---|
| 1 | **C-1 / D-1 / L-2** | **BLOCKER** | The ink floor is bought by destroying the picked chroma: `floorStops` shifts L *after* the derive has gamut-mapped, so the bead is never the picked colour; the shipped `bodyLightness` option is the cure; and the whole correction is open-loop — computed against a modelled plate, never compared to the rendered frame (`settledFrame` unread; `rendererStatus` grep → 0). | My node run, byte-exact: shipped path → stops L{.8683,.9283,.98,.98} C{.0879,.0388,.0097,.0099}, meanC 0.0366, two stops white, hex `#ffbde0 #ffdde5 #fff6f6 #fff6f4`; `bodyLightness:0.65` → C{.2725,.1975,.1364,.0958}, hex `#f60bb2 #ff6198 #ff8e93 #ffb19e`. Source: HeroBlob:97-111/:117-130. Producer doc verbatim (index.d.ts). Pixel-side numbers (rendered \|ΔL\| ≈ 0.017 vs the 0.15 contract, two independent samplers) are corroboration → residue 1. | **BUILD → X-W6** — this is ledger row **CC-061** (MT-F032) with its mechanism now proven; rides CC-057's stage transposition. The plate-referent dispute (see residue 1) must be settled before any G1/G2/HB-3-shaped gate is written. |
| 2 | C-2 / L-3 | MAJOR | The floor never re-derives on a scheme flip or ambient drift — `isDark`/`inkAmbient` are read inside a watch callback whose only source is `cssColorOpaqueFrame`; the referent is scheme-dependent at three levels (`probeEpoch`, dark-class cache key, ambient). | Structurally certain from my full read: floorStops' only caller is reseedHeroStops (:117-130); its only callers are the :132 watch and onActivated (:246-250); neither signal is a source anywhere in 317 lines. Reader-C's live flip witness (stops byte-identical across a dark toggle) corroborates. | **BUILD → X-W6** — the ramp becomes a `computed`; dies with row 1's transposition. |
| 3 | C-13 | MINOR | The flip branch reduces to `plateL > 0.83` (light) / `plateL < 0.17` (dark) — every light-scheme plate always *inverts* the ramp; the flipped push is strictly larger (0.15+\|δ\|) and is committed without re-testing headroom; the clamp truncates silently. | Re-derived by hand this session; exact. | **FOLD into row 1's cut → X-W6** (the function deletes). |
| 4 | D-4 / C-8 | MINOR | `chromaCeiling: Math.max(0.16, seed.C)` is fully inert (ceil=seedC ≡ ceil=0.16 ≡ omitted, byte-identical) and the W6-4 comment's named case is false (`oklch(0.5 0.23 30)` → meanC 0.1688, not 0.23). | My five-arm node run. HeroBlob:73-78/:124. | **FOLD into row 1's cut → X-W6**; the false comment corrects in the same edit. |
| 5 | L-5-RULING-1 | MAJOR | The `lightnessFloor` booking at :90-92 is **undischargeable as written**: the producer knob is an absolute deepest-stop floor, INK_FLOOR is a bead↔plate separation; the 0.15 coincidence hid it. A false carry must be re-specified, never rolled forward. | My node run: `lightnessFloor:0.15` returns stops byte-identical to the no-option call on the light seed — the knob cannot produce separation. types.d.ts semantics confirm. | **BUILD → X-W6 wave authoring** — the wave must carry the RULING (producer `contrastAgainstL`-shaped option via **BH relay**, or abrogate the bead↔plate floor); the stale booking comment dies either way. |
| 6 | C-9 / L-5 / D-13 / D-10 | MAJOR | Three shipped producer seams are hand-reimplemented while the comments still call them "BOOKED" (settled/settledFrame vs the wall-clock park; BLOB_HERO vs the hand-inlined register — **seven atoms diverge**, re-verified by import: HERO {.22,4,.1,.3,.04,smoothK .06,fissionAmp 0} vs demo {.325,3,.09,.4,.03,.05,.6}; LIGHTNESS_FLOOR_* vs local INK_FLOOR); two idle machines (2000 ms wall clock vs the engine's quiescence read) drive one mood surface. | BLOB_HERO/DEFAULTS atom census via import this session; HeroBlob:90-92/:148-149/:160-177/:206-230; blob.js expose block re-read. | **BUILD → X-W2 (CC-035: the settled-seam park + `/blob-config`) + X-W6 (the register transposition)**. The BlobPane record's **K-11 cure-kill is honored**: do NOT adopt BLOB_HERO whole — push the demo's ruled deltas INTO a named preset first (BH relay carries the amendment; L's RULING-2). |
| 7 | **M-1** (reader-2 missed; arbiter-verified) | MAJOR | The config prop is a FALSE LIVE SEAM: the producer snapshots the object at setup; only `paletteStops/satelliteColors/rimColor` re-read live (`c()` used exactly twice); geometry/surface/**quality** are pinned at mount — the `<lg` half-res rung never re-arms across a viewport change, and BlobPane's sliders are half-dead on the hero (the proxy-passed keys live in-loop, the re-materialised keys dead). HeroBlob:62-66 asserts the opposite. | This seat's own dist derivation (ruled disagreement above). | **BUILD → X-W6** (sharpens BlobPane rows 1/3 — same surface, same cut) **+ BH relay** (the producer's liveConfig scope deserves a documented contract: which atoms are live). |
| 8 | L-1 | MAJOR | The async split is defeated: eager `useAtmosphere.ts:36` (+ `BlobPane.vue`) imports the blob barrel for two constants shipped in the 245 B `/blob-config`; the barrel retains ~77 KB tree-shaken (proven static, environment-independent); Rollup semantics place it in the eager chunk. | My esbuild run 77,452 B vs 1,667 B; import sites re-read; glass-ui exports map re-read. | **IDENTITY-FOLD → BlobPane row 21 = CC-035 → BUILD X-W2.a** (the wave names these exact repoints). Severity raised MINOR→MAJOR on the new evidence; recorded, not re-booked. |
| 9 | C-3 / L-6 / D-6 | MAJOR | The ornament's mount is gated on the *atmosphere* canvas's WebGL success through a latch-once b2 that never re-opens; a pre-b2 loss deletes the hero for the session (observed: REPORT row 0 — canvas 1, `WebGL: context lost.`, settle 18905, empty seat by eye); zero demo consumers of the live `rendererStatus` seam; `defineAsyncComponent` carries no error/loading state; PR-15 names the empty seat non-satisfying. | Chain re-walked at HEAD; REPORT.json re-read directly; `rendererStatus` grep → 0 (mine). Two sub-claims KILLED (see register): the producer HAS a loss path (substrate chunk) and DOES handle offscreen. | **BUILD → X-W2** (the b4 beat's predicate — gate on paint-completion, not the field's GPU arrival) **+ X-W6** (the designed absent/error state; the atmosphere's NO-FIELD HONEST TERMINAL two files away is the template). Trigger unpinned → residue 7; the coupling itself is the booked defect. |
| 10 | D-3 | MAJOR | Ambient motion cannot terminate within canon's five seconds — source arithmetic alone breaches it (BLOB_IDLE_MS 2000 + SLEEPY_POSE_MS 3300 = 5300 ms, armed at mount by the `{immediate:true}` watch) — and no pause control exists (`:paused` one-way, root inert), so **both** branches of VISUAL-CONSTITUTION §6's law fail; every KeepAlive return re-arms the full cycle. | HeroBlob:211-212/:279/:246-250 re-read; the canon clause verbatim; fixture mirrors the constants. Rendered timeline (7.0-8.1 s) → residue 3. | **BUILD → X-W2 (CC-035's settled-seam park restores the tight runway) + X-W6** (wire `v-model:paused` to the shipped `DockBackgroundToggle` — the producer doc names this exact shape); canon echo **X-W10**. |
| 11 | C-6 | MAJOR | The only unit test is a regex over the raw SFC text: mutating `:config="heroConfig"` → `appBlobConfig` (one template token) deletes the register, the fission arming, the quality rung, and the derived palette while every assertion stays green (the literals survive as dead code); `floorStops`/`INK_FLOOR`/`heroStops` have zero coverage anywhere. | test/picker-blob-config.test.ts read whole this session; the mutation argument checked against the assertions line by line; coverage greps mine. | **BUILD → X-W6** — identity with BlobPane row 11 (BPC-9): the o27 scene-contract battery + the pinned-literals re-aim in the same cut as the register transposition. |
| 12 | C-7 | MAJOR | O-12·3 asserts a hover response the component cannot produce: no pointer host exists (pressLabel omitted → no hit layer; root `pointer-events-none`; producer binds pointer listeners to the host element only), yet the oracle demands a ≥6/255 frame diff on hover. Either currently RED or green off the overscan-composite artifact — defective in both branches. | My reads of the spec (:139-160), HeroBlob:10, blob.js hit-layer conditional. | **BUILD → X-W6** (oracle re-derivation in the row-11 battery: the metric must read the bead, not the composite — or the oracle retires with a written rationale if the hover beat is ruled out-of-design). Branch resolution → residue 6. |
| 13 | C-5 / L-8 + M-5 | MINOR | `BEAD_RATIO` 0.52 encodes the retired bodyRadius 0.26 against the live 0.325 (two of three mirrors updated; the anti-skew fixture itself skewed), and the fixture's header advertises three importing specs while **four** import it. The vacuous-gate consequence is KILLED (probe-5 geometry — see the ruled disagreement); the drift and the header are real. | fixture:56-57 + header, HeroBlob:163, seat.css:10, 4-file import grep — all mine. | **BUILD → X-W6** — the fixture derives from the named register (row 6's cut); no bead/park constant typed twice; the header count corrects in the same edit. |
| 14 | L-7 | MAJOR | tsconfig.demo.json's paths ≠ package.json#exports: 3 phantom keys (root, /parsing, /units — no d.ts exists), 2 real keys missing (/css, /value), the "CLOSED 8-key set" comment false by one — and /css is live in HeroBlob's own transitive chain while typechecking against a registry tarball that **already differs** from the local build. | All re-verified this session: exports 7 keys; paths block read; `ls dist/subpaths`; `diff -q` → differ; the ink.ts chain re-read. | **IDENTITY-FOLD → ColorSpaceSelector L-14** (one identity, already folded to the config-parity row with F's born-RED generator command) **→ X-W8** (CC-007's config/graph-subtraction wave). The /css-live + tarball-differs-today facts adopted as strengthening evidence into that identity. |
| 15 | D-8 / D-9 + the 0.76 constant | MAJOR | A Blob-derived vertical minimum still exists (`min-height: calc(0.76·fp − 0.75rem)`, binding at 390 and 200% zoom) against canon's verbatim ban; `--blob-seat: 0` leaves PR-03 unimplemented; **`--instrument-title-gap` has zero live referents** so the §3.2 inequality is unmeasurable; and 0.76 itself is an underived magic ratio — the sheet derives every other number from one closed identity and this one from nothing (4 restatements, 0 derivations, my grep; the ≈0.65·fp+12px near-coincidence diverges 7 px at the 11rem arm). D-9's emerge/arm inversion critique stands on source (a lifecycle seam used as a resize API); its 537 ms attribution stays inference → residue 4. | seat.css:47/:82/:88-89 re-read; canon clauses re-read verbatim; greps mine. The binding px measurements are the axis's live evidence → residue (corroboration only). | **BUILD → X-W6** (the seat's layout tax dies with the stage transposition; the reservation must derive or die) **+ canon echo X-W10** (G must gain a live referent or the law must be re-authored). |
| 16 | D-7 / L-13 | MINOR | The blob canvas is the only element bleeding past the mobile viewport — and the attribution is corrected: the same row fires on `/#/blob` and `/#/does-not-exist` too (re-verified directly from REPORT.json), so it is the producer's 1.6× overscan box in general, not the picker seat's placement; a seat-scoped gate would aim at the wrong owner. `overflowX` 0 — clipped by an ancestor, not scrolled. | REPORT.json rows 30/38/44/45/53/59 re-read by this seat (desktop matrices bleed MathML only — a different component's row). | **BUILD → X-W6** (the seat's clip/containment contract states the viewport edge) **+ BH relay** (whether the overscan box should self-contain is a producer question). |
| 17 | D-11 | MINOR | forced-colors is undesigned for the seat: zero rules reach it (my grep), and canvas content is exempt by spec, so the WebGL bead opts out of the user's palette. The rendered-dominance claim is not statically decidable → residue 5 (the tracked WebKit shot is a disqualified witness). | Grep mine; spec exemption standard. | **BUILD → X-W6** (one `@media (forced-colors: active)` decision for the seat: suppress or flatten); canon echo X-W10. |
| 18 | D-12 | MINOR | The seat positions with physical properties (`top`/`right`/`padding-right`) against canon's logical-direction law; the two currently agree by accident — fixing either alone runs the label under the bead. | seat.css:52-53/:88 re-read; §6.1 clause verbatim. | **BUILD → X-W6** (`inset-inline-end` / `padding-inline-end`, one paired edit). |
| 19 | C-10 / D-14 / L-9 (family-rescoped) | MINOR | Two masking fallbacks — the bare `catch {}` over the component's only colour-truth source (against the producer's explicit no-catch-to-default contract; the `// throws iff un-parseable` comment false on two counts — my node run shows `color_non_opaque` and `color_context_required` both throw) and the fabricated 0.5 ambient. **RESCOPED to the family**: the identical catch+comment sits at useAtmosphere:398-400 (and the throw-free read :80-84), and the nullable inject is the established picker idiom (ConsoleRail byte-equivalent) — a HeroBlob-only cure leaves the defect standing. The "deviant vs loud law" framing is KILLED (see register). | All sites re-read this session; both throw classes reproduced. Current wiring safe (`cssColorOpaque` = withAlpha(…,1)) — latent, not live. | **BUILD → X-W6** (owns both files): one family decision — either the throws surface (debug rail) or the swallow is documented as policy at every site; the false comments correct regardless. |
| 20 | C-12 / L-10 | INFO | HeroBlob (and useMarkdownColors — a family) folds `resolveSurfaceLightnessLive` into its own computation without the documented mount bump; its first probe fires pre-mount from the `{immediate:true}` watch. Both escalations KILLED (resting tier reads a body-attached probe — my read; the "ONLY consumer" limb false). | Grep + useContrastSafeColor:135-145/:205-215 re-read. | **RIDER → X-W6**: the `useSurfaceLightness(surface)` composable cure (self-registering bump) is adopted as the shape *if* the wave touches this seam anyway; no independent gate — the design degrades safely for this tier. |
| 21 | L-12 | MINOR | A string-literal contract crosses the feature→boot boundary with a silent bare-return guard (`animationName !== "blob-emerge"`), and `overture.css` is the only `@keyframes` file outside `demo/styles/` (census: exactly 2). A rename in either file silently disables the R2 re-measure. | HeroBlob:311-312, overture.css:167-178, census grep — mine. | **BUILD → X-W6** (the emerge/arm inversion in row 15's cure deletes onEmergeEnd entirely — the preferred cut; else one exported name constant + the keyframe moved, never deleted, per edict 6). |
| 22 | L-14 | INFO | The `Blob` import shadows the DOM global; `InstanceType<typeof Blob>` silently reinterprets if the import is ever removed. Producer naming. | HeroBlob:34/:58. | **BH RELAY** (naming row; no demo action). |
| 23 | L-15 / C-4 / D-§0 | MAJOR | The Q14/CH-4 framing gates the wrong quantity: LCP does not reproduce anywhere near 5141 ms on three independent rigs (264 / 928 / 1056-1108 ms medians); the real, reproducible cost is the ONE indivisible engine-init long task (TBT / longest-task axis) the demo's own forensic already names. An LCP-only born-RED is born un-actionable and closeable-without-cure. | Tri-rig triangulation adopted (all three axes agree against the standing record); useOverture:120-146 re-read; CC-036's current gate text re-read. | **BUILD → X-W2 (CC-036)** — this record is a direct input to CC-036's mandated re-measure: the gate must carry the TBT/longest-task axis (born-RED ≈ +797 ms Δ at 4× / 397 ms task at 1×) beside or instead of LCP-p75; the engine-init cost is a **producer** row (shader precompile / init chunking) and rides the BH relay. |
| 24 | M-2 (+ D-16, + reader-1's missed #1, + missed #5) | MAJOR | The palette is derived TWICE per rAF-coalesced frame: the eager boot derive (`useAtmosphere:389-403`, ceil 0.16) writes stops that no `<Blob>` ever renders (HeroBlob is the sole mount — census 1 — and overrides at :171), and because :171 spreads `appBlobConfig.color`, the dead write invalidates `heroConfig` a SECOND time per frame — inside the 12 ms budget the ink-floor comment invokes. Rider (missed #5): the condemned 0.16 register survives as the app-level default any future consumer inherits. | `deriveBlobPalette(` census = 2, `<Blob` census = 1, both chains re-read — mine. | **IDENTITY-FOLD → BlobPane row 13 (L-4 there) → BUILD X-W6** — one identity (anti-rename); the per-frame double-invalidation consequence and the 0.16-register rider adopted as new evidence into it. One derive owner; the floor applied once. |
| 25 | L-4 | MAJOR | One lightness-separation kernel, two character-identical demo homes (verified line-by-line: HeroBlob:105-110 ≡ useAtmosphere:101-108 modulo the constant), the copy self-named in prose; and the "applied uniformly so the spread survives" comment is falsified by the per-stop clamp — reader-2's corrected arithmetic adopted (the original +0.30 illustration killed: \|push\| < 0.30 strictly). | Both copies re-read; the replacement example re-computed by hand (stops [0.5,0.7,0.9,0.95], plate 0.65 → push +0.0375, top clips, achieved mean-Δ 0.148 < 0.15). | **BUILD → X-W6**: HeroBlob's copy deletes with row 1's transposition, leaving ONE demo home. The "lift `separateLightness` into value.js/color" cure is **NOT adopted as a gate** — the producer's `clampLightnessFloor` is a semantically different floor (absolute vs relative), and consolidating two concepts sharing a noun is the exact contrivance the ColorSpaceSelector L-15 ruling killed; recorded as an option for the library band, no obligation. |
| 26 | D-15 (rescoped) + reader-1's missed #2 | MINOR | The quality proxy is provably wrong on its own terms (container-scaled footprint, viewport-scaled rung: identical 73 px painted body → `full` at 1440, `half` at 390; DPR-3 tablet ≥1024 gets `full`, DPR-1 desktop at 1023 gets `half`) — and frozen at mount besides (row 7). The band-arm-law limb is KILLED. | Arithmetic re-checked against seat.css:43 + HeroBlob:71/:177. | **BUILD → X-W6** — the rung derives from device scale (DPR/fill-rate), not viewport width, and rides row 7's live-seam cure. |
| 27 | M-3 (reader-2 missed) | MINOR | `surface.rimColor` is an unowned colour atom: every hero frame paints the producer's fixed warm-brown `#8c694e` Fresnel rim (strength .32) over an arbitrary picked hue — and it is one of only THREE atoms the producer keeps live post-mount (row 7), i.e. a one-line seam nobody claimed. All three axes measured bead chroma and none looked at the rim. | `rimColor` demo grep = 0 (mine); preset value re-read via import; liveness from the M-1 derivation. | **BUILD → X-W6** (derive the rim from the seed alongside the ramp) **+ BH question** (whether a hue-neutral default rim is intended at hero scale). |
| 28 | M-6 (reader-2 missed) | INFO | `heroConfig` hands a hybrid object across the package boundary — half plain snapshots, half live reactive proxies, decided by which keys the consumer happened to override; the declared `BlobConfig` type cannot express the difference. | Follows directly from the row-7 derivation + HeroBlob:152-178. | **FOLD → X-W6** (dies with row 7's contract cure). |
| 29 | D-17 | INFO | The file is a ledger, not a component: ~180 of 317 lines are comment; six machines, three self-declared interims whose replacements shipped. Not an edict-1 god-module, but the accumulated-interim shape. | Full read. | **FOLD → X-W6** — the rows above delete roughly half the file; no separate action. |

## Killed-claims register (refutations preserved; nothing re-bookable without new evidence)

1. **D-2 as filed (BLOCKER — "a parked blob still costs two-thirds of the route's frame rate; the park is a freeze-frame, not a suspension")** — KILLED, both readers concurring, sustained by this seat on source: `createCanvasLifecycle` `E()` cancels the rAF on the first suspend token (re-read); `shouldContinue()` (:1399) self-parks on `mood.isSettled() ∧ pointer.isAtRest() ∧ satellites.isQuiescent()` and arms a setTimeout wake, not a rAF; probe C5 measured **0 GL draws** across 2.5 s and three further park cycles inside D-2's own window, beside a live instrument (53 draws on a colour change). "The work continues" is false; §6's "parked means no animation work" is satisfied on the GPU axis. The fps delta (112.8→40.1) is a real observation with **no owner** — its arms differ by the whole module (canvas compositing layer, contact-shadow gradient, emerge pose), and an uncapped headless rAF read is not a frame rate. → residue 2. Gate G3 as written may not enter any wave.
2. **D-2's cure ("gate the mount on intersection") and D-6's offscreen row ("no IntersectionObserver anywhere")** — KILLED: the producer ships it on by default (`composeIntersectionPark: !0`, rootMargin 200 px; suspend/resume under the named `"off-screen-io"` token — my read of blob.js:1479-1480 + the lifecycle IO handler). The grep was demo-scoped; the seam is producer-owned and already active.
3. **C-3's sub-claim "there is no context-loss path anywhere in the blob chain"** — KILLED: the grep was scoped to one file. `webglcontextlost`/`restored` handlers live in the shared substrate chunk `color.wgsl-Ct5o9dpf.js` (:61-64, my read), which blob.js imports (line 6), reporting through `onContextStateChange`/`onContextError` into `rendererStatus`. The real defect is the *consumer* ignoring a shipped status channel — a smaller, differently-cured claim, booked as roster row 9.
4. **L-11 ("one-way `:paused` drops producer-originated `update:paused` on the floor")** — KILLED: `update:paused` occurs exactly ONCE in dist/blob.js — inside the `emits` array; `emit("update` → 0 occurrences (my greps). The channel is declared and never driven; the suspend-reason Set means prop and imperative pause bind the same seam by construction. `v-model:paused` remains a harmless one-line improvement (adopted into row 10's cure); the stated failure is unreachable in 7.0.0. Producer gap → BH relay note.
5. **L-9 / C-10.2's framing ("HeroBlob is the deviant consumer of a loud-fail key")** — KILLED on two counts, both re-verified: ConsoleRail.vue:128/:138 uses the byte-equivalent nullable form (2 of 4 consumers; the picker-subtree idiom), and the quoted loud-fail law lives on `useSafeAccentFn`'s docblock (useContrastSafeColor:342-343), not on the key — keys.ts:10-14 carries no such law. The fallback survives as the family-scoped row 19.
6. **L-10's escalations ("can poison the shared cache"; "the ONLY live-probe consumer that skips the bump")** — KILLED: the `resting` tier resolves through a probe div appended to `document.body` (useContrastSafeColor:135-145, :205-215 — my read), a root-context read valid pre-mount; the epoch protects the DOM-querying `chrome`/`veil` branches HeroBlob never touches; and useMarkdownColors shares the omission, contradicting "only". Survives as INFO row 20.
7. **L-5 / C-9's sub-claim "`lightnessFloor` — never passed"** — KILLED as stated: it rides the config prop on every frame (`BLOB_CONFIG_DEFAULTS.color.lightnessFloor = 0.15` through the `...appBlobConfig.color` spread) and is inert where it lands (types.d.ts: read only through `deriveBlobPalette`). Same defect, corrected statement: the cure is "pass it to the derive" — folded into rows 1/5.
8. **C-5 / L-8's consequence ("O-12·2 never touches the arc / passes by construction")** — KILLED by this seat's own geometry (ruled disagreement above): probe 5 at `[cx, wrapper.y+2]` is a strict superset of every arc probe on the only occludable axis, for ANY value of BEAD_RATIO. The drift is real and MINOR (row 13).
9. **L-4's worked illustration (push = +0.30)** — KILLED: from `push = dir·(0.15 − dir·δ)` with \|δ\| < 0.15, \|push\| < 0.30 strictly; the example sits on the open boundary. Reader-2's replacement arithmetic verified by hand and adopted (row 25); the underlying clamp-truncation defect stands.
10. **D-15's band-arm-law limb** — KILLED: seat.css:14-16 legislates the *footprint* formula ("ONE cqi FORMULA"), and :86 pre-answers the charge ("a clamp, never a band arm"); the quality rung changes no layout box. Survives rescoped as row 26.
11. **M-4's terminal consequence (reader-2's missed row: "the R2 re-measure can be skipped silently in exactly the case it was written for… stays frozen")** — **KILLED BY THIS SEAT** (new): the IO re-entry handler calls `e.resize()` explicitly regardless of the suspend-token set (createCanvasLifecycle IO handler, line 74, my read), so the 0.35× backing persists only while off-screen — invisible — and re-measures the moment the ornament can be seen. The token-churn observation is real and harmless; no roster row.
12. **D-1 §3's modelled push (−0.2377 from plateL 0.8515) as a measurement** — QUARANTINED as evidence-class: the live tree back-solves push −0.1808 / plateL ≈ 0.908 (probe C3's stop read), and four plate referents now circulate (0.8079 probed / 0.8392 sampled / 0.8515 modelled / ≈0.908 back-solved). The §3 numbers are a model; they must not enter a wave spec as measured. The conclusion (row 1) is unaffected.
13. **The Q14 "LCP 5141 ms" as an inheritable born-RED** — KILLED as a gate number (not as history): unreproducible on three rigs; whatever produced it predates v4/glass-7/alias-death (CC-036 already says so). Superseded by row 23's axis correction.

## Superlatives (with provenance; preserved so the wave does not flatten them)

- **PRODUCER — the suspend-reason Set** (`createCanvasLifecycle`: `new Set()` of NAMED tokens; resume-by-name; PRM through one shared matchMedia singleton). It is why HeroBlob's PRM comment (:52-54) is *true rather than wishful*: `:paused=false` can never clear a PRM or off-screen park. Reader-1 set out to falsify the comment and found it engineered true; this seat's read concurs — and the same Set is what makes kills 1, 4, and 11 decidable.
- **PRODUCER — the no-silent-defaults config throw** (blob.js: "no blob config… There is no silent defaults synthesis", naming both valid wirings and the escape hatch). Reader-2's find; re-read here. The exact opposite of the masking-fallback disease this corpus books elsewhere.
- **PRODUCER — the self-parking render loop**: `shouldContinue()` consults a three-way quiescence read and arms a setTimeout for the *next scheduled event*, clamped [16 ms, 30 s]. The loop costs nothing between beats. The demo built a 5.3-second stopwatch on top of it (row 6).
- **DEMO — useOverture:120-146, the B2/B4 de-coincidence**: waits on `getAnimations()` STATE (never a timer), rIC with a 500 ms ceiling, immediate fall-through when nothing runs, time-driven-finite-only (the WebKit ScrollTimeline lesson cited in place), PRM synchronous. Re-read whole by this seat; the best boot sequencing in the graph — and its own forensic paragraph is what row 23 stands on.
- **DEMO — useContrastSafeColor:52-79, the probe epoch**: names the detached-mount hazard precisely, encodes it as ordinary reactivity, records the earned catch with a number. It is also *why* row 20 is INFO — the design degrades safely for the tier HeroBlob uses.
- **DEMO — the NO-FIELD HONEST TERMINAL** (useAtmosphere:283-310): loss → persisted ground, restore-once, second-loss rests for the session. Two files from HeroBlob, and precisely the state design row 9 says the blob is missing. The template is in-repo.
- **DEMO — seat.css:1-41's size identity** ("the factors cancel, so nominal body px = 2·bodyRadius·footprint") plus its honest self-amendment (the W4.5 "the BOOT path was demo-root after all" correction). One closed identity made every downstream number checkable — which is the only reason the BEAD_RATIO skew (row 13) was detectable, and what convicts the underived 0.76 beside it (row 15).
- **E2E — `seatFootprintPx()`** (blob-timing.ts:47-51), consumed by the mobile presence spec: the geometry gate computes its bounds from the same clamp formula at the test's own viewport. The fixture's thesis is validated by its own single exception: the one constant restated by hand is the one that skewed.
- **AUDIT — challenge-C's C-13 reduction** (meanL cancels; the flip is `plateL > 0.83`): re-derived exactly by this seat; it converts a two-branch heuristic into a decidable statement about the light scheme. The same report's plain reproduction-failure disclosures (C-3's "the trigger… is not yet pinned") are the posture that made its other rows cheap to verify.
- **AUDIT — vite.config.ts's exports-derived aliases** (reader-2's find, re-confirmed): the repo already proved the one-source-of-truth idiom that row 14's cure asks tsconfig to adopt.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs; none may enter a wave as measured)

1. **The rendered bead/plate pixel numbers and the plate referent.** Input-side derive: byte-certain (this seat). Rendered \|ΔL\| (0.0156/0.0168/0.0174), bead mean C (0.0446/0.0461/0.0047), and reader-L's disc-vs-annulus sampling all require re-sampling composited frames. Critically, **four plate-L referents circulate** (0.8079 probed · 0.8392 sampled · 0.8515 modelled · ≈0.908 back-solved from the live push) — the audit must rule which referent is authoritative before any G1/G2/HB-3-shaped gate is authored (row 1's rider).
2. **The idle-fps delta and TBT medians with attribution** (40.1 vs 112.8 at 1×; 661 vs 318 ms at 4×). Mechanism killed (register item 1); the delta is real and unowned. Needs GL draw counting + a control arm that removes only the render loop, and a compositing-cost hypothesis test (alpha canvas layer + `::before` radial gradient).
3. **D-3's rendered park timeline** (last change 7000 ms / first frozen 8111 ms) and the KeepAlive re-arm witness (14 distinct hashes over 4 s). The 5300 ms source arithmetic already convicts (row 10); only the rendered witness is owed.
4. **D-9's 537 ms long-task attribution** to the emerge re-measure (self-labelled one-arm-deep inference; the resize-on-resume mechanism itself is source-confirmed).
5. **D-11's forced-colors pixel claim** ("the single most chromatic object on a monochrome page") — Chromium witness required; the tracked WebKit shot is disqualified by the report's own note.
6. **C-7's branch**: whether O-12·3 is currently RED or green off the composite artifact — run the spec and inspect what the frame diff reads (row 12 books the defect either way).
7. **C-3's Safari trigger**: the coupling is read-certain and the observed instance real (REPORT row 0); three Chromium injection strategies failed. File the coupling, hunt the trigger live.
8. **L-1's production-chunk landing**: the static case is proven (unshakeable barrel + Rollup placement semantics + my 77,452 B measurement); the gh-pages build witness (which chunk, how many gzip bytes eager) is still owed to X-W2's born-RED.
9. **The 404/blob-route bleed attribution** (row 16): confirm the same picker-KeepAlive mount produces the `/#/does-not-exist` and `/#/blob` bleed rows, so the gate is written against the box's true owner.

## Addendum (E-3 — beside the challenge records, never rewriting them)

1. **Identity guards enforced (anti-rename)**: C-1/D-1/L-2 = **CC-061**; L-1 = **BlobPane row 21 / CC-035**; M-2-family = **BlobPane row 13**; C-6 = **BlobPane row 11's** battery; C-9/L-5's register half = **BlobPane row 1** (K-11 cure-kill honored — never adopt BLOB_HERO whole); L-7 = **ColorSpaceSelector L-14**. One identity each; this record adds evidence, never a second name.
2. **The scope law applied** (ColorSpaceSelector addendum item 1): the catch/inject/kernel rows were each charged by an axis to this one component; the counts live at family scope (3 catches, 2+2 injects, 2 kernel homes) and are booked there (row 19, row 25).
3. **The cure is adjudicated separately from the defect** (same addendum, item 2, decisive again here): D-2's cure was already shipped by the producer; L-4's value.js lift was a two-concepts-one-noun consolidation; C-1's as-filed "adopt BLOB_HERO" cure was killed at the BlobPane sitting. Three true-ish defects arrived carrying cures that would have shipped regressions or contrivance under green gates.
4. **BH relay rows** (owner edict — every producer-facing item rides the standing glass-ui relay, one letter): the `contrastAgainstL`/post-shading floor option (row 5 / L's RULING-1); the BLOB_HERO amendment carrying the demo's ruled deltas (row 6 / RULING-2); the liveConfig atom-liveness contract (row 7); the engine-init task size (row 23); the overscan box's viewport containment (row 16); `update:paused` declared-never-emitted (kill 4); the `Blob` name shadow (row 22); the hero-scale rim default (row 27).
5. **Evidence portability**: the derive specimens in this record are byte-pinned to installed glass-ui 7.0.0 at HEAD `ee9474e4` and were re-reproduced by this seat; my esbuild figure (77,452 B, vue-external) supersedes reader-1's 159,329 B as the citable form. Re-resolve every line anchor before executing any cure (D-19).

## Closing verdict

**DEFECTIVE — ADJUDICATED.** The corpus survives contact with a hostile arbiter at **1 BLOCKER + 14 MAJOR + 10 MINOR + 4 INFO** across 29 rows, with 13 claims or limbs killed in writing — including one BLOCKER (D-2), one reader-missed row killed against both readers (M-4), and one gate-consequence killed by geometry (C-5/L-8). The component's disease is one shape repeated six ways: a consumer computing open-loop against models of a producer that already ships the closed-loop seam (`bodyLightness`, `settled`/`settledFrame`, `rendererStatus`, the preset, the pause seam, the quiescence park) — so the terminal cure is one transposition (CC-061/CC-057 at **X-W6**, boot halves at **X-W2**, config parity at **X-W8**, canon echoes at **X-W10**), not twenty-nine patches. Nothing here stamps a wave; every disposition awaits its owning wave's own born-RED.

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
