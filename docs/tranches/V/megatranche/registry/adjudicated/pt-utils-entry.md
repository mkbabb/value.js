claude-fable-5

# pt-utils-entry — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ master `ef10d5b` (re-verified this session; `typescript/` has **zero** dirty rows) · `typescript/src/parse/utils-entry.ts` = 14 lines, the `@mkbabb/parse-that/utils` subpath entry, published 1.0.0 · **Registry tree**: value.js `tranche-u` @ `3ecf0267` (this session's own resolve — see ruling 5 on why this number is recorded AND expected to decay).
**Corpus adjudicated**: two fresh reader returns over the module's two challenge files — **reader-α** (29 confirmation cells · 8 kills · 8 misses · 12 superlatives; the return that measured the fixture re-segmentation, the FLAG dead code, and the 4-RegExp count) and **reader-β** (28 confirmation cells · 7 kills · 5 misses · 7 superlatives; the return that killed X-3 and C§0 and ran the C§4 W2-citation sweep). Both challenge files (`audit/parse-that-modules/utils-entry/challenge-L-library.md`, 427 lines; `challenge-C-consumption.md`, 571 lines) were re-read **whole** by this seat.
**Method (incredulity)**: every cross-reader disagreement and every kill of a load-bearing cell was re-derived against the bytes by this seat with its own instruments before adoption; four suspiciously-agreed cells (the D-3 row-2 kill, the C-4 falsifier kill, the invalid-vector 7/7, the valid-corpus 33/33) were independently re-measured, not spot-trusted — all four reproduced. Nothing in parse-that was armed (**no `enableDiagnostics`, no `memoize` in any probe process**; the latch was read only as static bytes); every runtime probe imported the **vendored npm 1.0.0 tarball** inside value.js (`prototypes/css-parser/node_modules/@mkbabb/parse-that/dist`), never the read-only checkout — legitimized by this seat's own 8/8 sha256 identity proof (ruling 5). Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments**: sha256 over 8 dist artifacts (checkout vs vendored tarball — all IDENTICAL: utils.js, utils.cjs, utils.d.ts, utils-entry.d.ts, diagnostics-DDazRHgl.js, packrat-entry-CS1td-8B.js, packrat-entry-46NYx4_U.cjs, parsers/utils.d.ts); `import.meta.resolve` from the RED-7 probe's own directory (root/`/diagnostics`/`/utils` **all resolve**; `dist/utils.js` deep and `./package.json` both `ERR_PACKAGE_PATH_NOT_EXPORTED`); `wc -l` over the full import set (186 · 52 · 20 · 23 · 8 · 14 · 711 · 399 · 189 · 43 · 26); INBOX.md O-15 row verbatim; the probe's `:47`/`:48` latch rows verbatim; `manifest-gate.mjs:37-41` Gate 2 verbatim; `dist-surface.test.ts:18-35` extractor **run by this seat** over both barrels (29/29; the five `parsers/*` names in NEITHER set); `package.json:1-53` whole (5 doors, no `"./*"`, no `"./package.json"`, `"./utils".types → ./dist/utils-entry.d.ts` at `:29`, `sideEffects:false` at `:6`, ten proofs at `:49`); source whole-reads: utils-entry.ts, parsers/{index,utils,csv,json}.ts, index.ts, utils.ts:145-186, parser.ts `:77-79`/`:189-210`/`:460-478`/`:480-520`/`:634-642`/`:700-711`; dist bytes: sizes of all 14 dist files (closure arithmetic re-derived: `/utils` ESM 2,039+3,516+40,576 = **46,131** · root **48,576** · `/core` **45,428** · `/packrat` **44,250** · `/diagnostics` **3,798** · CJS `/utils` **47,293**), `dist/utils.js:1-2` import lines + `b as skipWhitespace` export tail + `Parser.lazy` at `:11`/`:14`, utils-entry.d.ts whole (5 lines, `:1` re-exports `'./utils.js'`), utils.d.ts census (**17 names** across 16 export statements), parsers/utils.d.ts whole, core.d.ts:1-6, index.d.ts:10; greps: `trailing-content` = exactly 2 hits (`state.ts:26` kind + `parser.ts:470` sole producer), `flaggedParser` = exactly 2 (`:492`, `:496`); runtime probes (non-arming, vendored dist): `quotedString("]")` on `'"x"'`/`"]x]"`/`"]]"`, `quotedString()` on `'"abc'`, csvParser on the two-row quoted input / `"a,b\nc,d"` / `"a,,b"` / **the real `data/csv/data.csv`** (5 lines → 4 rows, isError=false, offset 1409/1409, fieldCounts **[10, 2, 24, 27]**), export-key sets + ESM identity + ESM↔CJS split, `escapedString().parse("\\u0041")` = `"u"` vs `jsonParser` = `"A"`, `numberParser().parse("1e400")` = Infinity, `new String("42")` → 42, RegExp-construction Proxy (quotedString **4** · escapedString **2** · numberParser **1**), valid.jsonl 33/33 differential vs `JSON.parse` (0 diffs, 33/33 fully consumed), invalid.jsonl **7/7** non-undefined residue with isError=true; consumer receipts: value.js `package.json` dependencies = `{glass-ui, keyframes.js}`, `node_modules/@mkbabb` = 3 dirs, keyframes `leaves.ts:9-10` "parse-that-FREE" verbatim, `denominator.test.ts:80-87` dependency pin verbatim; cite-drift deciders: W2.md:247 (`span-into-C`) and parser-band.md:138 (DISSENT header) / :141 (non-finite bullet); `proof-no-css-surface.mjs:6-14` OBSERVABLE-TRUTH NOTE verbatim.
**Law compliance**: `/Users/mkbabb/Programming/parse-that` read-only, main checkout only, zero writes; no fresh root created; **sole repository write = this file** (probe scripts ran from the session scratchpad and touched no repository).

## Scoreboard

**31 adjudicated roster rows — 4 BLOCKER · 10 MAJOR · 9 MINOR · 8 INFO.** **13 killed-claims register entries, all SUSTAINED** — including one ruling (R1) that overturns a reader's confirmation cell at the resolver, and one (R5) where the killer's own replacement receipt is corrected. **12 missed rows ADMITTED** (8 from reader-α, 5 from reader-β, one shared) — two become the deciding halves of roster rows (the gate-coverage MAJOR; the sideEffects gate-lock), the rest sharpen existing cells. **13 superlatives sustained.** **4 adjudicator corrections against the readers.**

Blocker-weight survivors: **PT-UE-1** — `csvParser` re-segments the repository's own fixture into rows of 10/2/24/27 fields with `isError=false` and `offset=len` (**no return-path signal exists**; the parser-band's own prescribed idiom is measured insufficient) — and **PT-UE-2** — `escapedString`/`quotedString` destroy every semantic escape while the correct decoder ships 30 lines away on the same subpath. Realized in-constellation impact today is **zero at every hop** (PT-UE-8: zero consumers anywhere, gated as an executable assertion in the consumer tree), so every blocker is a defect of the **published 1.0.0 contract** and of curation — the entry file itself is the best-behaved thing in the corpus (superlative 1).

## The incredulity seat's own work — ruled disagreements and corrections

1. **X-3 — the head-on contradiction. RULED for reader-β; reader-α's confirmation cell CORRECTED.** Reader-α confirmed C·X-3 ("the RED-7 probe cannot run in the value.js workspace — parse-that is not installed"); reader-β killed it. At this seat's own instruments: `@mkbabb/parse-that@1.0.0` **is** installed at `prototypes/css-parser/node_modules/` and `import.meta.resolve` **from the probe script's own directory** resolves all three of its import specifiers (root → `dist/parse.js`, `/diagnostics` → `dist/diagnostics.js`; `/utils` resolves too). Node's ESM resolver walks up from the **script's** URL, not the invoker's cwd — the C-doc (and reader-α behind it) conflated "absent from the value.js root `node_modules`" with "unresolvable by the script". The C-doc's own falsifier is satisfied. **Kill SUSTAINED**; convergent with the pt-leaf record's C-i2 ("the prototype-local install is shasum-identical — recorded so no later reader voids the corpus on a false alarm"), which makes reader-α's confirmation a *repeat* of an alarm the registry had already disarmed. X-4 (no RED-7 **row** touches `/utils`) survives independently and is confirmed.
2. **X-1 vs D-22 — RULED for reader-β; reader-α's merged cell partially corrected.** INBOX O-15 verbatim: *"`label` is a no-op unless diagnostics are armed, and arming couples an unconditional `console.error` (`dist/diagnostics-DDazRHgl.js:14` + `dist/packrat-entry-*.js:881`) — the two are reachable only together."* Two cites for the two halves of one coupled sentence: `:14` **is** the label-gating line (`state.expected = diagnosticsEnabled && label ? [label] : void 0;` — byte-exact) and is correctly paired with the no-op half; only `:881` drifted (+1: `:881` = the `isDiagnosticsEnabled()` guard, `:882` = the `console.error` — same split the pt-leaf record ruled at PT-LEAF-23). C's X-1 ("the cite is half wrong… its first cite should read `:882`") misreads the pairing, and its correction as written would delete a correct cite. The L-doc's D-22 states the same facts correctly. **X-1 killed; D-22 confirmed** (PT-UE-27). Reader-α's merged "D-22/X-1" cell endorsed C's framing ("the diagnostics chunk carries no console call at all" — true, but O-15 never said it did) without the pairing check.
3. **C-6 severity — RULED for reader-β's kill; reader-α's MAJOR corrected; row re-graded MAJOR→INFO.** The collision is real (dist/utils.js exports 7; dist/utils.d.ts declares **17 names** — this seat's census) — but every exploitation path the challenge enumerates is dead at the bytes: (a) deep import refused — **this seat's own run**: `import.meta.resolve("@mkbabb/parse-that/dist/utils.js")` → `ERR_PACKAGE_PATH_NOT_EXPORTED` (no `"./*"` pattern in the 5-door exports map); (b) node10/classic resolution finds no `utils.*` at the package root — it gets nothing, not wrong types; (c) every resolver that can reach the subpath reads the exports map, which carries the `:29` types steer. And the "remove the collision" cure is structurally unavailable: `dist/utils-entry.d.ts:1` re-exports from `'./utils.js'`, so the `/utils` type entry *requires* the colliding basename to exist. A latent naming hazard with zero demonstrated failure path prices as INFO in this register (the C-14 class), not MAJOR (PT-UE-24).
4. **C-9 severity split (α MINOR · β MAJOR) — RULED MAJOR, by merger with reader-β's decisive missed row; the C-doc's rationale killed.** The "only door to escapedString/quotedString/csvParser" justification is refuted by the same document's own C-1 (all three ship from the root barrel; this seat's set-difference run: `/utils \ root` = ∅). What makes the row MAJOR is the **two-sided** hole reader-β found and this seat reproduced with the gate's own extractor: `dist-surface.test.ts`'s braces-only regex (`/export\s*\{([^}]*)\}/g`) cannot see `export *` (src `index.ts:14` / dist `index.d.ts:10`), so it compares 29 names against 29 with all five `parsers/*` exports **in neither set** — the exact 8-of-15 drift class the gate's own header says it was born to bite, invisible in the one place it can recur. Combined with subpath-gate's existsSync-only treatment of `/utils`, the subpath's runtime surface has **zero gate coverage in either direction** (PT-UE-12). The condemning adjacency: `proof-no-css-surface.mjs:6-14` documents this *exact* `export *` blindness as an OBSERVABLE-TRUTH NOTE and fixes it for the other gate — the lesson was written down and applied once.
5. **C§0 substrate receipt — reader-β's kill SUSTAINED AND EXTENDED against the killer.** (a) The provenance hedge dissolves harder than claimed: this seat measured **8/8** dist artifacts sha256-identical between `typescript/dist` and the vendored npm 1.0.0 tarball (β checked 13 by its own account; the union is now byte-certain) — every `[dist]` claim in both challenges is on the exact bytes consumers receive, and the OP-7 "the dist may not be master" caveat is void. (b) The stale-receipt half cuts both ways: the challenge's consumer-root receipt (`95cfbb15`) was stale, **and reader-β's replacement (`3ea0144c`) is itself already stale** — value.js HEAD at this seat's resolve is `3ecf0267`. On an active tranche an absolute consumer-root receipt decays in days. **Register law**: consumer-root receipts are re-resolved at use and recorded per-record; no seat "corrects" one absolute number into another and calls it repaired.
6. **D-15/C-13 severity split (α MINOR · β INFO) — RULED MINOR.** Two byte-identical shippable copies of the number regex that can silently diverge (json.ts:21 does not call `numberParser()`) is a maintenance defect over and above the measured `Infinity` posture; the band connection is real on both readers' cites once reconciled — parser-band.md:138 is the `## DISSENT (preserved, unresolved)` **header** and `:141` the non-finite **bullet** (this seat's read; α cited the section truthfully, β's drift note is the precise coordinate).
7. **Suspiciously-agreed kill cells — re-measured, all reproduce.** `quotedString("]").parseState('"x"')` → value `undefined`, isError **true**, offset 0/3, and `.parse('"x"')` → `undefined` (NOT `[]`/isError=false as challenge-L:110 printed); `"]x]"` → `[]` only WITH isError=true; `"]]"` → `""` (a string) isError=false — no input yields an array with isError=false, `map`'s join running on every non-error path. The two-row quoted CSV input → 2 rows, isError=false, 25/25 (C-4's "no concept of a row" falsifier satisfied by this seat's own run). Also re-measured rather than trusted: fixture fieldCounts **[10, 2, 24, 27]** (α's re-segmentation sharpening is real — both challenges reported only rows 0-1); RegExp constructions **4/2/1**; valid 33/33 (0 value diffs, 0 partial consumes); invalid **7/7** satisfying the suite's only assertion.

**Adjudicator's corrections register**: (1) reader-α's X-3 confirmation — corrected (ruling 1); (2) reader-α's C-6 MAJOR — corrected (ruling 3); (3) reader-α's D-22/X-1 merged cell — pairing check added (ruling 2); (4) reader-β's replacement consumer-root receipt — corrected as itself stale (ruling 5). Per the ColorSpaceSelector precedent these are fleet evidence: both readers' instruments were otherwise re-derived clean, and their disagreements sat exactly where the truth needed a third instrument.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **W0..W4** where an X·P wave owns the surface (W0 pause/fresh-root · W1 harness/corpus/bench · W2 dual-target algebra · W3 totality/recovery · W4 PLAW-BIND/release) · **REFINEMENT FOLD** where the finding sharpens a wave's spec · **NO-WAVE-OWNER** where no X·P wave owns it (incumbent parse-that repair; evidence rides the standing O-15 letter channel, owner-held, **semver-priced per the pt-leaf PT-LEAF-27 law**). This entry sits **outside the adjudicated CSS band** (verified: no wave spec or parser-band row names any of the seven exports), so the only W2 claims on it are the C§4 KEEP/WRAP/RETIRE verdicts, which reader-β verified cite-for-cite and this seat sustains: **scanners WRAP** (W2.md:188-191; behind the span-into-`C` operator at W2.md:**247**, never called direct), **all five showcase parsers RETIRE** (outside the 52-export closed universe). X·P executes nothing until the owner's begin-word; every routing is a booking, not an act. Line anchors are `ef10d5b`; re-resolve before any cure (D-19).

### Blockers

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-UE-1** | L·D-1 / C·C-4-as-rescoped + α-miss | `csvParser` silently corrupts real CSV and reports unqualified success — **no return-path signal exists** (value well-formed, isError=false, offset===len), and the corruption is a **re-segmentation, not a truncation**: the repo's own `data/csv/data.csv` (5 lines × 10 columns) → 4 rows of **[10, 2, 24, 27]** fields, values landing under arbitrary source columns (this seat's run). C-4's "no concept of a row" half is KILLED (K1): fully-quoted input row-splits correctly, so the defect is one character class (`[^,]+` spanning `\n`) + the inexpressible RFC 4180 `""` + `sepBy`'s silent break on the empty field (`parser.ts:605→608`, no isError) | csv.ts:11-15/:17/:20; parser.ts:538/:605; fixture measured; minimal rows `"a,,b"`→`[["a"]]` off 1/4 re-verified | **W2** RETIRE sustained (outside the universe; C-4 rider) · **REFINEMENT FOLD → W1**: the parser-band idiom ("entry via `parseState` + `isError`, never `parse()` truthiness") is **measured insufficient** — the boundary law gains `offset === src.length` AND the note that even that passes this case · **NO-WAVE-OWNER** incumbent repair rides O-15, priced **2.0.0** (changes accepted-input values) |
| **PT-UE-2** | L·D-2 / C·C-2 | `escapedString`/`quotedString` do not unescape: `.skip()` keeps the LEFT value so the `\uXXXX` payload is annihilated (`"\\u0041"`→`"u"`, `'"a\\u0041b"'`→`"aub"` — re-verified); only escapes whose letter equals their value survive. The correct decoder ships 30 lines away on the same subpath (json.ts:22-24 → `"aAb"`, re-verified) — two escape semantics, one 7-export surface, both public at 1.0.0 | parsers/utils.ts:8-11 (`string("u").skip(regex(...))`; `.map(([, esc]) => esc)`); parser.ts:198 `state.ok(value1)` | **W2** RETIRE sustained (EQ-1 rider: a wrong-`V` injector is invisible to K-1, lethal to the wave) · **NO-WAVE-OWNER** incumbent repair (adopt the in-file json.ts model) rides O-15, priced **2.0.0** (value-changing) |
| **PT-UE-3** | L·D-3 (headline; row 2 killed K2) | `.parse()` publishes parse residue as the result and violates the declared type doing so: `parse()` never consults isError (parser.ts:77-79); the error view built at :60-66 is discarded; `"["`→`[]`, `"[1,2"`→`[1,2]`, `'{"a":1,"a":2'`→ the pre-`fromEntries` pair array — all isError=true. Type violation on the primary path: `quotedString().parseState('"abc')` → value `["abc"]` (array) vs declared `Parser<string>` — **error-path only** (K2: no isError=false array exists; `map` joins every non-error path — this seat's runs). **Identity guard**: the mechanism is pt-leaf **PT-LEAF-16** — one identity; this row books the *subpath-surface instance* (the published `.d.ts` lie + the D-4 interaction), not a second mechanism | parser.ts:77-79/:51-75; wrap :416-423 leaves state.value; dist/parsers/utils.d.ts:5 | **REFINEMENT FOLD → W1** (shared with PT-LEAF-16): entry via `parseState` + isError only; `.parse()` truthiness banned with this row as a second written reason · incumbent cure ordering: **must land before or with any `.eof()` fix** (PT-UE-6's rider) |
| **PT-UE-4** | L·D-4 | Six of seven exports untested; the seventh's only assertion (`not.toBeUndefined()`) is satisfied by **7/7** of the repo's own invalid vectors (re-measured); `test/csv.test.ts:5-16` re-declares the grammar byte-identically and tests the **copy**, its multi-row cases all fully-quoted (the class that works), its fixture case asserting only `length > 0` — satisfied by the 4-row mangling. The suite is GREEN on a file it corrupts | grep census re-run: csvParser/quotedString/escapedString/numberParser/skipWhitespace/skipBlockComments = 0 test refs; json-vectors.test.ts:23/:28-29; invalid.jsonl 7/7 this seat's run | **W1** — the oracle-suite successor owns the class (differential vs `JSON.parse` + invalid.jsonl wiring) · **NO-WAVE-OWNER** incumbent test repair rides O-15 (**1.1.0-eligible**: test-only) |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-UE-5** | L·D-5 / C·C-7 | `/utils` is not self-sufficient: its two headline functions take `ParserState<unknown>` and the subpath exports **no** state type, no class, no factory (`"ParserState" in utils === false`); the argument is unconstructible from a literal (8 own props + 13 methods) though the functions duck-type on `{src, offset}` at runtime. The consumer must open a second subpath to use the first | utils.ts:159/:168; dist/utils.d.ts:35/:38; dist/utils-entry.d.ts (5 lines); dist/core.d.ts:2-3 | **NO-WAVE-OWNER** — parameter narrowed to `{ src: string; offset: number }` (caller-safe widening of acceptance, **1.1.0-eligible**), rides O-15 · **W2 rider**: the WRAP seat inherits the two-field shape, never the 21-member class |
| **PT-UE-6** | L·D-6 + α-misses (FLAG correction; eof-ordering) | Neither domain parser terminates at EOF — silent prefix acceptance (`"1 garbage"`→1 isError=false; docstring json.ts:51 claims trailing handling). **Correction to the challenge's proof cell**: the FLAG_EOF machinery it credits (parser.ts:22, :466-476) is **unreachable dead code** — `flaggedParser` is built at :492-496 and discarded (this seat's grep: exactly 2 hits; identity: pt-leaf **PT-LEAF-20**/K4), and the public `"trailing-content"` Suggestion kind has exactly ONE producer, :470, which cannot execute. The live cure is `Parser.eof()` = `this.skip(eof())` (:638-642) — **which does not cure PT-UE-3**: skip's error arm keeps `state.value`, so `.parse()` still returns residue; D-3 must be repaired first or the EOF fix is invisible at the public surface (α's ordering rider, source-verified) | json.ts:52; csv.ts:20; parser.ts:466-476/:492-496/:638-642; state.ts:26 | **W2** — EOF is a total-consumption judgment in the algebra (pt-leaf L-m2 identity) · **NO-WAVE-OWNER** incumbent `.eof()` rides O-15, priced **2.0.0** (acceptance narrowing) with the ordering rider attached |
| **PT-UE-7** | L·D-7 / C·C-3 | `quotedString(quote)` splices caller input raw into a RegExp character class: `"\\"` throws `SyntaxError` at construction; `"]"` degenerates the grammar (`[^]` swallows the class) and yields `[]` from a declared `Parser<string>` (error-path, per K2); `""`/`"^"`/`"-"` silently degenerate or pass by accident; nothing validates, nothing documents | parsers/utils.ts:15-16; dist/parsers/utils.d.ts:5 | **W2** RETIRE sustained · **NO-WAVE-OWNER** construction-time validation rides O-15 (**1.1.0-eligible**: converts undefined behavior to a loud throw) |
| **PT-UE-8** | L·D-8 / C·C-10 | Zero consumers repo-wide and constellation-wide, and the justifying provenance note (utils.ts:150-156: "the primitives … value.js's canonical CSS grammar drives its hot paths with") is **false at today's tree**: value.js deps = `{glass-ui, keyframes.js}` (re-verified), zero parse-that imports, the CSS grammar uses `/\s/`+`.trim()` and hand-rolled its own comment skipper (stylesheet.ts:437-439); keyframes records itself "parse-that-FREE" (leaves.ts:9-10, re-read); parse-that's own tree has no internal caller; the megatranche prototypes import root/`/core`/`/diagnostics`, never `/utils`. The package's own precept (proof-no-dead-combinator.mjs:10-12) names the disposition and its sweep already spans the right trees | this seat's grep + dependency re-verification; denominator.test.ts:80-87 pins the zero-dep fact as an executable assertion | **NO-WAVE-OWNER** — comment truth repair **1.1.0-eligible**; export/subpath retirement **2.0.0**, owner-carried recommendation only (C§4's constraint sustained: no seat may write parse-that) |
| **PT-UE-9** | L·D-9 / C·C-5-as-rescoped + α-miss | `/utils` statically loads the diagnostics chunk AND the packrat-latch chunk (46,131 B ESM / 47,293 B CJS — closure arithmetic re-derived by this seat) — but the **BLOCKER attribution and the absolutes are killed** (K5/K6): the packrat chunk itself imports the diagnostics chunk, so every subpath inherits both; `/utils` is 2,445 B **narrower** than root and its marginal cost over `/core` is **703 B**; `core.ts:4-5`'s promise is false in BOTH of its first two clauses for `/core` itself. A **family-level chunking defect** `/utils` inherits: causes = vite.config.ts (five entries, no manualChunks) + `src/parse/utils.ts` fusing the diagnostics accumulator (:6-148) with the byte scanners (:150-186) — which is exactly why the two scanners arrive as `b`/`s` **from the diagnostics chunk** (dist/utils.js:1 + export tail, re-read) | dist sizes re-measured (14 files); dist/utils.js:1-2; core.ts:4-5; json.ts:5/csv.ts:5/parsers/utils.ts:4 barrel imports vs core.ts:7-25 | **NO-WAVE-OWNER** incumbent two-part cure (manualChunks + split utils.ts's two unrelated halves) rides O-15 (**1.1.0-eligible**: internal) · **REFINEMENT FOLD → W0/W4**: tier promises asserted over **import graphs**, never file existence (identity: pt-leaf PT-LEAF-17's W4 rider) |
| **PT-UE-10** | C·C-1 | The subpath adds **zero** surface: all 7 exports already on root `.` (set difference ∅, ESM identity holds — re-measured), while adding a permanent 1.0.0 exports-map commitment whose removal is a MAJOR bump. Unlike `/core` (narrowing), `/diagnostics` (grouping), `/packrat` (arming door), `/utils` has no tier-defining property | this seat's key-set run (7 vs 34, diff ∅, `root.jsonParser === utils.jsonParser` true); index.ts:5/:14; package.json:28-32 | **NO-WAVE-OWNER** — owner-carried retirement recommendation (**2.0.0**), bundled with PT-UE-8's |
| **PT-UE-11** | C·C-8 | The `/utils` **type** graph imports the root barrel: dist/parsers/{utils,json,csv}.d.ts all open with `import { Parser } from '../index.js'` (re-read), and dist/index.d.ts:10 `export *` makes the declaration graph circular — a TS consumer importing only `/utils` carries the whole root declaration surface. Contrast dist/core.d.ts:1-6 (leaves only, re-read) | source twins parsers/*.ts:4-5; dist bytes re-read | **NO-WAVE-OWNER** — three-line `../index.js` → `../core.js` rewire rides O-15 (**1.1.0-eligible**) · **W2 rider**: in-package barrel self-imports banned (adjacent to pt-leaf PT-LEAF-14's cycle) |
| **PT-UE-12** | C·C-9 / L·D-19 + **β-miss (deciding half)** | The `/utils` runtime surface has **zero gate coverage in either direction** (ruling 4): subpath-gate existsSync-onlys `/utils` (imports and asserts only `/core`+`/packrat`; "4 subpaths resolve" overstates); AND dist-surface.test.ts's braces-only extractor cannot see `export *`, so the five `parsers/*` names are in **neither** compared set (29/29 — re-run by this seat with the gate's own function) — the gate's own born-red drift class can recur invisibly here; manifest-gate reads `exports["."].types` only. Emptying utils-entry.ts to `export {}` keeps every gate GREEN. The C-doc's "only door" rationale is KILLED (K8). The repo documented this exact blindness in proof-no-css-surface.mjs:6-14 and cured it there only | subpath-gate.mjs:25-60 re-read; dist-surface.test.ts:18-35 re-run; package.json:49 | **NO-WAVE-OWNER** incumbent gate repair rides O-15 (**1.1.0-eligible**) · **W4 rider**: release gates enforce standing properties over real module graphs (shared identity with pt-leaf PT-LEAF-18/-20's rider) |
| **PT-UE-13** | L·D-10 | `mergeErrorState` allocates two provably-unreachable arrays per furthest-advance on the **accept** path with diagnostics off — `suggestions`/`secondarySpans` assigned unconditionally (utils.ts:34-35) while their only writers are flag-gated; reachable-only-via-`recover()` confirmed. **Identity guard**: the mechanism is pt-leaf **PT-LEAF-23** — this row books the *jsonParser-path instance* with reader-α's independent scaling measurement (~87-128 arrays/KB, linear; challenge's 98.7-125.4 same class) | utils.ts:32-35/:51-61; α's counting-accessor runs adopted as corroboration | **W2** (via PT-LEAF-23: diagnostics are values, allocation-free when disarmed) · incumbent one-line guard already rides O-15 · magnitudes **MEASURE-AT-OPEN #2** |
| **PT-UE-14** | L·D-11 + α-miss (failure class) | The recursion ceiling on the shipped `jsonParser` is a JIT-state-dependent **range** far below PT-04's 7,761 (α cold ~1.49k, warm 2.5-4.1k; challenge cold 1,493; **the crisp constant is killed both times** — K9), the throw escapes `.parse()` uncaught, and at the cold boundary the class itself flips: α measured `SyntaxError: Invalid regular expression: … Stack overflow` (V8's lazy regex compile failing under exhausted stack, naming the library's own numeral pattern as invalid) at the boundary depths and `RangeError` above — so a consumer's `instanceof RangeError` guard is unsound and the error text points at a grammar bug that does not exist. That makes **four** mutually inconsistent bad-input postures on one surface (silent wrong value · residue+isError · raw TypeError · RangeError-or-SyntaxError) | json.ts:25/:28 two lazy back-edges; parser.ts:702-707 arity 1; no catch on the path | **W2** — bounded back-edge is an algebra parameter (W2.md:250); the band's "stack ceiling becomes ordinary `ok:false`" debt cited · **NO-WAVE-OWNER** O-15 evidence-packet rider: PT-04's number must be quoted as a sample, never a property · all numerals **MEASURE-AT-OPEN #1** |

### Minors (9) and info (8)

| id | claim (adjudicated, compressed) | disposition |
|---|---|---|
| **PT-UE-15** | L·D-12 + α/β-miss (the gate-lock): `sideEffects: false` is untrue (`parser.ts:711` bare `_initWhitespace()`, load-bearing for both `.trim()`-terminated parsers) — AND `manifest-gate.mjs:37-41` **hard-fails on any other value** (re-read: `if (pkg.sideEffects !== false) … failed = true`), so the honest manifest is unshippable without editing the gate; proof:manifest runs first in proof:all. Currently unexploitable (single-chunk build carries used bindings), hence MINOR; α's MAJOR preserved as dissent | NO-WAVE-OWNER — gate+manifest repaired together, rides O-15; adjacency: pt-leaf PT-LEAF-14 owns the `_initWhitespace` mechanism |
| **PT-UE-16** | L·D-13/D-14-as-rescoped: utils-entry.ts:7-14 duplicates parsers/index.ts:5-8 (same four statements, different specifier paths — α's precision) with nothing enforcing lockstep; csv.ts:5 + parsers/utils.ts:4 carry dead `Parser` imports. The claimed build cost is KILLED (K10): the bundled `P as Parser` is live via `Parser.lazy` at dist/utils.js:11/:14 regardless — source hygiene only | NO-WAVE-OWNER (**1.1.0-eligible**) |
| **PT-UE-17** | L·D-15 / C·C-13 (ruled MINOR, ruling 6): byte-identical number regex at json.ts:21 and parsers/utils.ts:22, both bare `Number`; `numberParser().parse("1e400")` → `Infinity`, no diagnostic (re-verified). Band dissent coordinates reconciled: parser-band.md:138 header / :141 bullet | NO-WAVE-OWNER dedupe (**1.1.0-eligible**) · non-finite posture owned by the band's D-journal law (W2.md:301) |
| **PT-UE-18** | L·D-16: `jsonParser` is `Parser<any>` (eslint-disabled at json.ts:39-40, emitted in the `.d.ts`); the exported `JsonValue` — the subpath's ONLY type export — is connected to nothing. The `any` is load-bearing for the init cycle; the cure is a declared binding type | NO-WAVE-OWNER — types-only change, semver class priced by the owner (an `any`→`JsonValue` narrowing can break downstream compiles) |
| **PT-UE-19** | L·D-17 / C·C-11 + α-miss: two API shapes on one surface (module-eval singletons vs per-call factories) with no stated rule; the factory cost is **4 RegExp compilations per `quotedString()` call** (this seat's Proxy probe: 4 · escapedString 2 · numberParser 1 — leaf.ts's sticky clone fires per regex() leaf), not the "one recompile" both challenges wrote | NO-WAVE-OWNER — parameterless factories become consts (**1.1.0-eligible**, jsonNumber is the in-file model) |
| **PT-UE-20** | L·D-18: RFC 4180 unimplemented (`""` inexpressible by the ≥1-char `[^"]+` arm) and no dialect declared anywhere on the surface — the fixture itself uses `""` throughout | NO-WAVE-OWNER doc truth (**1.1.0-eligible**); the repair rides PT-UE-1's 2.0.0 |
| **PT-UE-21** | β-miss: the dual-package split forks the **diagnostics module globals** too (utils.js imports `diagnostics-DDazRHgl.js`; utils.cjs requires `diagnostics-DpUY87_d.cjs` — two instances of `diagnosticsEnabled`/`collectedDiagnostics`), so `enableDiagnostics()` from one format silently no-ops for the other format's parsers — and `/diagnostics` is a published subpath whose entire contract is those globals. Construction argument (nothing armed); the instance split measured (ESM ≠ CJS across all singletons — re-verified). The L-doc drafted and retracted the adjacent claim; the consumer is constructible | NO-WAVE-OWNER · W4 rider: dual-package resolution matrix (identity: pt-leaf C-miss-5's rider). MINOR (latent, mixed-graph-conditional — the C-14 class, sharpened) |
| **PT-UE-22** | α-miss: `skipWhitespace`'s `charCodeAt(i) <= 32` swallows NUL, VT, and every C0 control — CSS Syntax §4.2 whitespace is newline/tab/space only. The one behavioral defect in the pair both challenges nominate for KEEP/WRAP, filed by neither as a numbered row (C carries it as seat-prose; L's S-1 poses the wrong falsifier). **Identity**: an instance of pt-leaf **PT-LEAF-7**'s three-alphabet family; the WRAP table row the AC-3 seat inherits (W2.md:355-359 predicted failure (a)) | W2 — trivia class fixed by css-syntax citation; the row lands in the AC-3 class table (booked, not executed) |
| **PT-UE-23** | C·C-12: showcase/demo code semver-locked at 1.0.0 in direct contradiction of the package's own dead-export precept, which no gate applies to entry surface (ten proofs, none reds on a zero-consumer entry export — re-verified at package.json:49); the record shows the rule applied once (the 15 `*Span` builders excised for exactly this) | W4 rider (standing-property gates, shared with PT-UE-12) · NO-WAVE-OWNER |
| **PT-UE-24** (INFO) | C·C-6 **re-graded MAJOR→INFO** (ruling 3): dist/utils.js (7 exports) and dist/utils.d.ts (17 names — this seat's census) are adjacent files describing different modules; every enumerated exploitation path is dead at the bytes (deep import refused, node10 gets nothing, the `:29` types steer works) and the colliding basename is structurally required by utils-entry.d.ts:1. A latent naming hazard, deliberately routed around (superlative 11); the `/packrat` twin makes it a class | recorded; any future exports-map or tooling change re-prices it |
| **PT-UE-25** (INFO) | L·D-20-as-corrected: non-string handling is four-way non-uniform — jsonParser/csvParser/quotedString/escapedString 7/7 throw, `numberParser` 5/7 with `{}`/`["a"]` → silent `undefined`, and `new String("42")` **silently succeeds** (re-verified) — falsifying the corpus row's "5/5 throw" generalization; the "three un-unifiable messages" count is KILLED (K11): five-to-six distinct message strings, attribution input-dependent. **Identity**: pt-leaf PT-LEAF-5 owns the boundary law; this row extends its probe matrix to the five callables | REFINEMENT FOLD → W1 boundary corpus (positional `typeof` at entry, never throw-inference) |
| **PT-UE-26** (INFO) | L·D-21: `/utils` shares the one-way `PACKRAT_ARMED` binding with `/packrat` (four latch sites byte-exact, no disarm; arming at construction; epoch opened on every parseState) — pays for a capability it cannot decline, undocumented at the entry. 1.47× figure carried from O-15 as N=1, NOT re-measured (nothing armed) | frame of record · O-15 channel · MEASURE-AT-OPEN #3 |
| **PT-UE-27** (INFO) | L·D-22 + the X-1 ruling (ruling 2): O-15 PT-01's second cite drifted +1 (`:881` guard / `:882` call — re-read); the `:14` cite is **exact and correctly paired** with the label-no-op half; X-1's proposed correction would have introduced an error. Rider so the evidence packet attached to the standing 1.1.0 ask is not quoted stale | NO-WAVE-OWNER — one-coordinate correction rides the O-15 evidence channel |
| **PT-UE-28** (INFO) | C·X-2: the RED-7 probe's latch row is vacuous — `row(!ARM ? false : false, …)` at `:48` can never increment red (re-read; `:47` is the separate under-`--arm` disarm row); the C-doc's own ledger cites `:47` where its body correctly says `:48` (self-drift, recorded) | NO-WAVE-OWNER — value.js-owned instrument repair; MEASURE-AT-OPEN #4 carries the rule |
| **PT-UE-29** (INFO) | C·X-4: no RED-7 row touches `/utils` — the corpus's only surface instrument has never measured this subpath (probe imports root + `/diagnostics` only; row map verified). **X-3 is dead** (ruling 1): the probe RESOLVES from its own directory; "never measured" is a fact about the rows, not about runnability | frame of record for PT-UE-12's coverage hole |
| **PT-UE-30** (INFO) | β-miss: `"./package.json"` absent from the exports map — `import.meta.resolve("@mkbabb/parse-that/package.json")` → `ERR_PACKAGE_PATH_NOT_EXPORTED` (this seat's own run). **Identity**: an instance of pt-leaf **C-miss-4/5's** manifest row — folded there, recorded here | NO-WAVE-OWNER (non-breaking manifest add, 1.1.0-eligible), via the existing pt-leaf booking |
| **PT-UE-31** (INFO) | Cite-drift bundle inside the challenge corpus (β-miss + K12): every L-doc header line count is +1 (this seat's `wc -l`: 186/52/20/23/8/14/711/399/189/43 — the C-doc's counts exact); challenge-C:459's `W2.md:250` should be `:247` (span-into-`C` — re-read); `:558`'s `parser-band.md:138` is the DISSENT header, the bullet at `:141`; `:72`'s `core.ts:7-27` overruns a 26-line file; the C-doc ledger's X-2 `:47` contradicts its own body's `:48`; challenge-L:203's sweep cite `:62-67` is `:63-68` | recorded so no downstream reader re-quotes without re-reading; no substantive claim moved |

## Killed-claims register — 13 entries, all SUSTAINED

| # | killed claim | adjudication |
|---|---|---|
| K1 | C-4: "The parser has no concept of a row… *Falsifier*: any two-line input for which csvParser.parse returns two rows. None exists" | **KILL SUSTAINED, re-run by this seat**: `'"name","age"\r\n"John","30"'` → 2 rows, isError=false, 25/25. Row production exists (many() + newline-consuming trim), defeated only by the `[^,]+` arm. The L-doc retracted this same claim from its own prior draft (challenge-L:388); the C-doc carried it forward. C-4's substance lives at PT-UE-1; its excise-weight disposition does not |
| K2 | L·D-3 table row 2: `quotedString("]").parse('"x"') → [] (an ARRAY) isError=false` | **KILL SUSTAINED, re-run by this seat**: → `undefined`, isError **true**, offset 0/3 (wrap's opening `string("]")` fails before any value). The array leak exists only on error paths; no isError=false array is constructible (map joins every non-error path; `"]]"` → `""`). D-3's headline and primary exhibit survive intact |
| K3 | C·X-3: "the RED-7 probe is currently unrunnable in this workspace (`ERR_MODULE_NOT_FOUND`)" — CONFIRMED by reader-α, KILLED by reader-β | **KILL SUSTAINED at the resolver (ruling 1)** — the prototype-local install exists and the probe's own falsifier command resolves. Reader-α's confirmation corrected; convergent with the registry's standing C-i2 disarm |
| K4 | C·X-1: "O-15 PT-01's line cite is half wrong… its first cite should read `:882`" | **KILL SUSTAINED at INBOX verbatim (ruling 2)** — `:14` is the label-no-op half's correct cite ("the two are reachable only together" pairs them); only `:881→:882` drifted. X-1's correction as written would delete a correct cite. D-22 (the L-doc's version) confirmed at PT-UE-27 |
| K5 | C-5: "There is no consumer for whom `/utils` is a narrower import than `.`" | **KILL SUSTAINED at the bytes** — closure arithmetic re-derived: `/utils` 46,131 B vs root 48,576 B (omits parse.js + core.js entirely; 2,445 B narrower). Trivial narrowing — which is the honest finding — but the absolute is false as stated |
| K6 | C-5's BLOCKER rating as a defect **of** this subpath (the ~66× framing) | **RE-GRADE SUSTAINED (both readers convergent)** — the packrat chunk itself imports the diagnostics chunk, so every subpath pays both; marginal cost of `/utils` over `/core` = 703 B; `core.ts:4-5` is false in both clauses for `/core` itself. Family-level chunking defect, inherited not caused → MAJOR at PT-UE-9 |
| K7 | C-6's hazard mechanism ("any tool falling back to `.js`→`.d.ts` adjacency gets the wrong 17-export surface") | **KILL SUSTAINED + re-run**: deep import `ERR_PACKAGE_PATH_NOT_EXPORTED` (this seat); node10 finds nothing; exports-aware resolvers read the `:29` steer; utils-entry.d.ts:1 requires the colliding basename. Collision real, hazard undemonstrated → INFO at PT-UE-24; reader-α's MAJOR corrected |
| K8 | C-9's rationale: "`/utils` is the **only door** to escapedString/quotedString/csvParser" | **KILL SUSTAINED** — refuted by the same document's C-1 and this seat's set-difference run (all three on root `.`; dist/parse.js:4 imports them). The coverage finding survives at MAJOR via the merger with the dist-surface blindness (ruling 4) |
| K9 | L·D-11's crisp cold boundary ("deepest OK = 1,493; RangeError at 1,494") | **KILL SUSTAINED (reader-α, not reproducible: throws at 1,492 AND 1,493, and as SyntaxError not RangeError)** — which *strengthens* the doc's own thesis: the ceiling is a range, the class is boundary-dependent, no numeral enters the ledger as a constant. All numerals MEASURE-AT-OPEN #1 |
| K10 | L·D-14's emit consequence ("under verbatimModuleSyntax these are emitted as real value imports") | **KILL SUSTAINED at the bytes** — the bundle's `P as Parser` is live via `Parser.lazy` at dist/utils.js:11/:14 (json.ts genuinely uses it), so the unused specifiers in csv.ts/parsers/utils.ts cost nothing shipped. Source hygiene survives at PT-UE-16 |
| K11 | L·D-20's "three un-unifiable messages" | **UNDERCOUNT SUSTAINED (reader-α)** — five-to-six distinct message strings across the surface; attribution input-dependent, not one-per-export. Substance confirmed and worse at PT-UE-25 |
| K12 | Challenge-L's read-whole manifest line counts (`:7`) | **KILL SUSTAINED by this seat's `wc -l`** — every figure uniformly +1; the C-doc's counts exact. Folded into PT-UE-31's drift bundle |
| K13 | C§0's substrate receipt (consumer root `95cfbb15`) and OP-7 provenance hedge | **KILL SUSTAINED AND EXTENDED (ruling 5)** — the hedge dissolves (8/8 sha256-identical to the published tarball, this seat's own hashes: every `[dist]` claim sits on the exact bytes consumers receive); the receipt was stale — and the killer's replacement (`3ea0144c`) is now stale too (HEAD `3ecf0267` at this seat's resolve). Receipt law recorded in ruling 5 |

## Missed rows — 12 admitted, all placed

From **reader-α**: the FLAG/`trailing-content` dead-code correction (→ PT-UE-6, identity PT-LEAF-20) · the manifest-gate lock (→ PT-UE-15, shared with β) · the SyntaxError-at-ceiling class flip (→ PT-UE-14 + MEASURE-AT-OPEN #1) · the family-level tier arithmetic (→ PT-UE-9) · the 4-RegExp count (→ PT-UE-19, re-measured exactly) · the `<= 32` predicate (→ PT-UE-22, the corpus's only unnumbered behavioral defect in the KEEP/WRAP pair) · the fixture re-segmentation (→ PT-UE-1, re-measured: [10, 2, 24, 27]) · the eof-ordering rider (→ PT-UE-6/PT-UE-3). From **reader-β**: the dist-surface `export *` blindness (→ PT-UE-12's deciding half, re-run by this seat) · the diagnostics-globals dual-package fork (→ PT-UE-21) · the manifest-gate lock (shared) · `"./package.json"` unexported (→ PT-UE-30, re-run; identity pt-leaf) · the cite-drift bundle (→ PT-UE-31, spot-verified at W2.md:247 and parser-band.md:138/:141).

## Superlatives — 13 sustained (L-18 runs both ways)

1. **`utils-entry.ts` itself is close to flawless** (C·S-1, both readers): 14 lines, zero runtime logic, `export type` split for verbatimModuleSyntax, `.js` extensions throughout, no `export *`. Not one confirmed defect lives in the file — the correct failure distribution for a barrel, stated before 31 rows against what it re-exports.
2. **The two byte-scanners are the best code the entry touches** (S-1): single monotone loops, `state.offset` the only write, zero allocation on any path, memchr-style `indexOf("*/")`; unterminated comment stops at the comment's **start** (re-read at utils.ts:179).
3. **The JSON grammar is value-correct on the entire valid corpus** — re-measured by this seat: 33/33 vs native `JSON.parse`, 0 value diffs, 33/33 fully consumed. Every BLOCKER on this entry is failure-posture, curation, or packaging — not one production rule is wrong.
4. **`Object.fromEntries` closes the prototype-pollution class** (S-3): CreateDataPropertyOrThrow semantics; strictly safer than the repo's other JSON grammar.
5. **`dispatch()` used textbook-correctly** (S-4): shared-slot interning for `"t"`/`"f"`, the `"0-9"` range key, construction-time label, bounds-checked guard (json.ts:40-49 re-read).
6. **`jsonString`'s two-mode map** (S-5): one `indexOf` fast path, spec-correct native decode otherwise — the in-file model the PT-UE-2 repair should have reused.
7. **`many()`'s zero-width guard** (S-6): `csvParser.parse("")` terminates as `[]`; the identical guard at parser.ts:605 is simultaneously one of PT-UE-1's mechanisms — a virtue in the outer loop, silent data loss in the inner.
8. **The latch design is honest and its documentation survives adversarial reading** (S-7): `if (!PACKRAT_ARMED) return null` first statement, symmetric guard on reset, construction-time arming explained correctly — a deliberate documented soundness trade, undocumented only at the entry that pays for it.
9. **`skipBlockComments`'s unterminated-comment posture is the totality-correct one** (C·S-2): stop-at-start vs value.js's twin failing — under K-8, parse-that's is the shape that composes.
10. **The scanners are O-8-clean by construction** (C·S-3): only the passed state, in a file that also holds the diagnostics globals — a line deliberately held; the single reason WRAP, not RETIRE.
11. **The basename collision was SEEN and routed around** (C·S-4): the `:29` types steer + the `*-entry.ts` convention, load-bearing not merely defensive (utils-entry.d.ts:1 depends on the colliding file existing) — and it is what reduces C-6 to INFO.
12. **The gate authors were more honest than their gates** (α-NEW + β): subpath-gate's success message names exactly what it asserted; proof-no-css-surface carries an OBSERVABLE-TRUTH NOTE explaining why its own obvious implementation was unsound (re-read verbatim); dist-surface documents the drift defect it was born to bite. Three gates documenting their own blind spots — one of which (PT-UE-12) then bit through the documented-but-uncured spot.
13. **The constellation's zero-dependency fact is an executable assertion, not prose** (β): `denominator.test.ts:80-87` pins value.js's dependency set and the parse-that absence, keyed to decree I-11 §2 (re-read verbatim) — the routing law is gated.

## MEASURE-AT-OPEN residue — bench-dependent rows for the lane's W1 harness

Inherited law: one fresh process per cell; nothing armed; every probe disposable-PID; **no probe numeral is portable** (the pt-leaf K18 law).

1. **Recursion ceiling + failure class (PT-UE-14)** — cold/warm depth bisect on the lane's pinned node, recording the **error class at each boundary depth** (SyntaxError band vs RangeError band), through the shipped `jsonParser`, per fresh process. The W1 oracle asserts the class set and the escape path, never a depth constant; PT-04's 7,761 is quoted as a sample.
2. **mergeErrorState allocation counts (PT-UE-13)** — mechanism source-certain (identity PT-LEAF-23); magnitudes (both seats' ~87-128/KB) re-measured on the W1 rig's accept-path leg with counting accessors.
3. **Latch cost (PT-UE-26)** — O-15's 93.9→138.2 ns (1.47×) stays N=1 until W1 arms the latch **in a disposable cell only**; `/utils`-path cost measured through `jsonParser.parseState` with and without a foreign `memoize()` in-process.
4. **Instrument repair rows (PT-UE-28/29)** — the RED-7 probe gains a real latch predicate (the `:48` constant-false row) and, if the corpus wants this subpath measured, an `/utils` import; until then "never measured" stands as a row-level fact.
5. **Chunk-name re-derivation rule** — every dist byte cite re-derives `diagnostics-*`/`packrat-entry-*` filenames from `dist/utils.js:1-2`'s import specifiers at the moment of use (identity: pt-leaf #11); the 8/8 sha identity recorded here (ruling 5) voids on the next `vite build`.
6. **Boundary probe matrix (PT-UE-25)** — the 7-input × 5-callable grid plus `new String` rows enters W1's boundary corpus with expected postures per cell (throw-message strings recorded but not asserted verbatim — they are engine text).
7. **CSV fixture signature (PT-UE-1)** — `data/csv/data.csv` → fieldCounts `[10, 2, 24, 27]` is the born-RED witness for any repair; a cure is proven by 5 rows × 10 fields with the `""` cells decoded, not by `length > 0`.
8. **Receipt law (ruling 5)** — consumer-root HEADs recorded per-record at time of use; three receipts in this corpus's chain (`95cfbb15`, `3ea0144c`, `3ecf0267`) were each true briefly, which is the point.

## Closing verdict

**DEFECTIVE-BY-CURATION, sealed against a second adversarial corpus.** Both fresh reader returns survive the incredulity seat with corrections that sharpen rather than reverse — but this corpus contained one head-on contradiction (X-3: confirmed by one reader, killed by the other) that had to be **ruled at the resolver**, and it fell against the reader who repeated a workspace-level alarm the registry had already disarmed: the fleet lesson is that "not installed here" claims must name the resolution base, because Node resolves from the script, not the seat. The other cross-reader splits (X-1's cite pairing, C-6's hazard, C-9's rationale and grade, C§0's receipts) all resolved the same way — the reader who re-derived at the bytes beat the reader who inherited the challenge's framing — and one killer was itself corrected (its replacement receipt was stale within days; receipts are re-resolved, not replaced).

The module's adjudicated shape: **a 14-line barrel that is itself the best-audited file in the corpus, publishing seven exports of which five are wrong at the value level, unsafe at the boundary, or dead by the package's own precept — and two byte-scanners that are genuinely excellent, consumed by no one, justified by a provenance note that is false at today's tree.** No signal exists on the sharpest failure (PT-UE-1's isError=false/offset=len corruption — the row that defeats the parser-band's prescribed idiom and must widen W1's boundary law); the correct escape decoder ships 30 lines from the broken one; the test suite is GREEN on a file the parser corrupts; and the subpath's runtime surface has zero gate coverage in either direction, in a repository that documented exactly this blindness for a sibling gate. Routing: **W2** holds only what the C§4 verdicts already ruled (scanners WRAP behind span-into-`C` with the `<= 32` predicate as an inherited class-table row; all five showcases RETIRE) plus the algebra-parameter and totality laws this entry evidences; **REFINEMENT FOLD** sharpens W1 (the `.parse()` ban's second reason, the widened entry idiom, the boundary matrix, the oracle-suite successor) and W0/W4 (import-graph-asserted tier promises, standing-property release gates, the dual-package matrix); **NO-WAVE-OWNER** carries every incumbent repair on the owner-held O-15 channel, each priced by semver class per the PT-LEAF-27 law — the value-changing cures (CSV, escapes, EOF) are 2.0.0 events that must not ride the standing 1.1.0 ask, and the 1.1.0-eligible set (parameter widening, barrel rewires, gate repairs, comment truth, consts, chunking) is listed row by row. Eight dynamic rows are banked to MEASURE-AT-OPEN. Nothing lost: head, tail, or interval.

— end of apotheosis. Both challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; execution awaits the owner's begin-word.
