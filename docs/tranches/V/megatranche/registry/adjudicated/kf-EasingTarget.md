claude-fable-5

# kf-EasingTarget — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/scenes/easing/EasingTarget.vue` (335 L) + `EasingTarget.css` (193 L) — reader-1 ("design-and-usage": 28 confirmation cells · 7 kills · 8 misses · 9 superlatives) and reader-2 ("library-and-code": 34 confirmation cells · 4 kills · 5 misses · 14 superlatives). All three challenge files re-read **whole** by this seat (`audit/kf-components/EasingTarget/challenge-{D-design,L-library,C-consumption}.md`, 267/574/544 L, all opus-5[1m]; the L file is its own rev 2, the C file its own two-pass).
**Method (incredulity)**: every reader-vs-reader head-on contradiction (rovingFocus; the header collapse), every kill of a load-bearing cell, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption. **Zero browser tooling** (law); rulings are static reads + git archaeology + `node` executions of the **installed** `@mkbabb/value.js@4.0.0` and reads of the installed `@mkbabb/glass-ui@7.0.0` and `reka-ui` dists — product source read-only; the irreducibly visual residue rides to SS-13/KF.W9. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only + node-against-dist)**: keyframes.js git reads (HEAD `8281638c`; merge-base `a59d3a22`; `origin/master` `81a56990`, 41 ahead; HEAD/worktree/frontier `package.json` triple; `git diff --stat origin/master -- demo/scenes/easing/ demo/utils/reference-data/` → **empty**); `reka-ui/dist/shared/useForwardProps.js` read whole (the R-1 decider: forwards only `Object.keys({...defaultProps, ...preservedProps})`); `reka-ui/dist/ToggleGroup/ToggleGroupRoot.js:14-40` (`rovingFocus`/`loop` `default: true`); glass-ui `dist/toggle-group-BWHkiLdP.js` import line (`useForwardProps`/`useForwardPropsEmits` **from reka-ui**) + its `rovingFocus: { type: Boolean` with no default; EasingTarget.vue `:14-45` (Transition wraps ONLY the `<h2>`; `.specimen-literal` + CopyButton outside), `:330-335` (**`<style scoped src="./EasingTarget.css">`** — reader-1's scoped-compilation premise exact); EasingTarget.css `:20-50` (the leave/enter transitions animate `opacity`+`transform` only, 140 ms, PRM-suppressed) and `:160-193` (selected-name rule `:177-180` vs `.specimen-tile:hover .tile-name` `:191-193` — both (0,3,0), later wins); EasingSidebar.vue `:80-140` (seedFor's `name in bezierPresets`; syncGap's `!isBezierEditable`; the BG-8 caption verbatim at `:42-49`) + `:165-211` (`quadEq` 0.0005, `nameForQuad` over NAMED_EASING_BEZIER only, `onPickerChange` fall-through to `updateBezierPoints`); useEasingDemo.ts `:36` (`currentEasingName = ref("ease")`), `:64` (`progress = ref(0)`), `:73-76`, `:78-118` (`currentEasingFn`/`cssValue`/`svgPath`/`currentFamily` — `cssValue` returns the bare NAME for every non-`cubic-bezier`/non-`steps` curve, byte-read), `:161` (`livePhaseValue = 0`), `:246-271`, `:325-341` (the `{ immediate: true }` progress watch — the K9 decider), `:380-395` (export block); node probes against installed value.js 4.0.0: `bezierPresets["smooth-step-3"]` → `[0.65, 0, 0.35, 1]`; `parseTimingFunction` → **FAIL** for `ease-in-bounce`, `smooth-step-3`, `ease-in-sine`, `ease-in-out-circ` · **OK** for `step-start`, `step-end`, `steps(4, jump-end)`, `ease`, `linear`, `ease-in-out`; dead-export greps over demo/+test/ excluding the composable: `currentEasingFn`/`svgPath`/`comparisonCurves`/`currentFamily`/`liveProgress` → **0 consumers each**; easingGroups.ts `:27-45` (Standard = linear/ease/ease-in/ease-out/ease-in-out); glass-ui re-greps: `grep -rln "glass-chip.css"` package-wide → **no output**; `grep -c glass-chip dist/styles/glass.css` → 0; `grep -o "Object.keys(N)" dist/easing.js` → hit (the picker's preset list IS `bezierPresets`); `forced-colors` in `dist/styles/glass/glass-chip.css` → 0; `glass-chip|glass-capsule` in `dist/styles/glass/a11y-fallback.css` → 0; registry identity greps (kf-App SCH-1 ruling; kf-CopyButton KF-CB-3/-5/-8/-14 and KF-CE-13; kf-ChannelControls N-2 — FadingScroll banked there as *adoption*, never as the tabindex defect).
**Substrate disclosure — the four-layer ruling, inherited and SHARPENED (this seat's git reads + the banked kf-App/kf-CopyButton precedent)**: local HEAD `8281638c` is the stale divergent fork (1 ahead of `a59d3a22`, 41 behind `origin/master` `81a56990` — census **SCH-1**, banked at kf-App ruling 1). HEAD declares glass-ui `optionalDependencies: 6.0.0` (locked, 3 entries); the worktree deletes it; **the frontier declares `@mkbabb/glass-ui: "7.0.0"` as an exact devDependency** — so every glass-ui-7.0.0 cell in this corpus was measured against **the frontier's declared producer**, not a phantom. The sharpening this component adds: `git diff origin/master` over `demo/scenes/easing/` **and** `demo/utils/reference-data/` is **EMPTY** — the six dirty files the axes audited (`EasingTarget.vue`, `EasingSidebar.vue`, `useEasingDemo.ts`, `easingGroups.ts`, `animationDescriptions.ts`, `timingCurveUtils.ts`) are **byte-identical to the frontier**. Unlike kf-CopyButton (where the frontier had silently repaired the blocker), there is **no frontier repair here: every adjudicated row below is frontier-durable by identity.** All line anchors are worktree-at-`8281638c` = `origin/master` bytes; re-resolve before any cure (D-19).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file.**

## Scoreboard

**37 adjudicated roster rows — 1 BLOCKER · 13 MAJOR · 17 MINOR · 6 INFO**, of which **5 are folds by reference to banked identities (census SCH-1 · KF-CB-3 · KF-CB-5/-14 · KF-CB-8 · KF-CE-13) — zero re-bookings**. **10 killed-claims register entries, all SUSTAINED** — two of them rulings on head-on reader-vs-reader contradictions, both decided at the bytes for the kill. **13 missed rows ADMITTED** (8 from reader-1, 5 from reader-2; two are one identity, folded to SCH-1) — chief among them reader-2's cure-lock (the corpus-unanimous repair for the BLOCKER converts a fidelity defect into a validity defect), reader-1's hover-erases-selection and preset-pick-empties-selection, and the 140 ms header desync that replaces the killed collapse claim. **6 adjudicator corrections against the readers**, including one reversed confirmation cell (reader-2's rovingFocus mechanism) and one row both readers passed over in silence (C-4), restored by this seat. **14 superlative cells sustained, 1 clause-corrected.**

Blocker-weight survivor: **KF-ET-1** — the copyable literal is not the curve on screen for 17 of 28 specimens (to 0.158 of travel), under prose promising "COMPLETE re-parseable" in three places — now **cure-locked**: the fix all three axes prescribed is adjudicated REFUTED (it would make 20 of 28 copies invalid CSS), so the repair must be specified, not patched.

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-M-3(c) rovingFocus** — the head-on contradiction | KILLED: `useForwardProps` forwards only defaulted-or-passed keys; reka's own `default: true` governs | CONFIRMED MAJOR: "Vue resolves absent Boolean to `false`, so reka receives explicit `rovingFocus: false`"; blast radius "every ToggleGroup consumer" | **Reader-1's KILL SUSTAINED; reader-2's cell REVERSED** | My reads: glass-ui `toggle-group-BWHkiLdP.js` imports `useForwardProps`/`useForwardPropsEmits` **from reka-ui**; reka's implementation builds its key set from `{...defaultProps, ...preservedProps}` — `defaultProps` = declared non-undefined defaults (glass-ui's compiled `rovingFocus: { type: Boolean }` has NONE), `preservedProps` = keys in `vm.vnode.props` (EasingTarget passes `type`/`model-value`/`aria-label`/handler only). `rovingFocus`/`loop` never enter the forwarded set; `ToggleGroupRoot` then applies its OWN `default: true` (ToggleGroupRoot.js:16-38). The dist-vs-source `withDefaults` divergence is real and **behaviourally null**. Consequences: the filter keeps roving focus; the "41 tab stops" arithmetic falls to ~32; reader-2's blast-radius extension falls with the cell. SS-13 probe #4 (tab the pills) retained as the one-keystroke live witness. |
| **D-M-5 header collapse** — the second head-on | KILLED on Vue `out-in` semantics: the outgoing `<h2>` stays mounted through the 140 ms leave (opacity/transform only — layout-inert), and `remove()` + the replacing render run in the same synchronous `afterLeave`; no painted frame lacks the heading | CONFIRMED MINOR ("mechanism decidable; magnitude live") | **Reader-1's KILL SUSTAINED; reader-2's cell CORRECTED** | My reads: EasingTarget.vue:23-30 (`<Transition name="specimen-name" mode="out-in">` wrapping only the keyed `<h2>`), EasingTarget.css:33-47 (leave/enter = `opacity` + `translateY(±0.1-0.15em)` transitions, 140 ms — neither affects layout), PRM arm suppresses it. Vue's `out-in` triggers the insertion from the leave's `afterLeave` synchronously; there is no reserved-height requirement because there is no frame without a header. The real transient defect at this seam is reader-1's replacement miss — admitted as **KF-ET-19** (the 140 ms name↔literal desync, byte-verified: the literal + armed CopyButton sit OUTSIDE the Transition and re-render on the selection tick). |
| **F-1 framing / substrate** (reader-1 miss M7 INFO vs reader-2 miss M-2 MAJOR — same fact, different frames) | "in-flight uncommitted change; blast radius = red CI job" | "uncommitted-work regression; every 7.0.0 claim validated against an artifact no install path produces" | **ONE identity → census SCH-1 → FOLD to KF.W0; both frames DATED by the frontier** | My git triple confirms both readers' HEAD facts (optionalDep 6.0.0 + lock entries — reader-2's "npm ci fails" arm was already corrected at kf-CopyButton ruling 2: it silently *downgrades*). The extension neither reader ran: `origin/master` declares **7.0.0 exact devDep** (the SCH-1 cure, banked), so reader-2's "no install path produces 7.0.0" is FALSE at the frontier — the frontier's install path produces exactly it, which is what makes every glass-ui cell in this corpus *evidence about the shipping producer* rather than about an accident. And the easing chain itself is **byte-identical to the frontier** (empty diff), so nothing in this corpus is voided or repaired by the reconciliation. Zero re-booking. |
| **D-B-2/D-16 severity** (glass-chip orphan) | MAJOR | BLOCKER | **MAJOR at the consumer surface — GLASS-OWNED routing; reader-2's BLOCKER preserved as dissent** | Both readers' package-wide greps re-run by this seat (`glass-chip.css` imported by nothing; `glass.css` imports 18 siblings, chip absent; the demo's sole entry is `exports["./styles"]`). Selection stays legible via the component's own two rules (EasingTarget.css:173-180) — the L axis's stated reason for MAJOR, adopted. The cure is producer-side (one `@import` in glass.css) — per the ColorSpaceSelector D-10 precedent the label follows the cure's owner: **GLASS-OWNED**, rides the standing BH relay. The row's blast radius (every Chip consumer in every repo) belongs to the relay letter, not to this component's grade. |
| **L·D-20 exemplar** (paused-entry origin flash) | confirmed the mechanism incl. the paused-entry exemplar | K-3: the exemplar cannot occur — at entry the live phase IS 0, so the skipped register-paint would have written the identical transform | **Reader-2's KILL SUSTAINED; reader-1's cell NARROWED** | My reads: `progress = ref(0)` (:64), `livePhaseValue = 0` (:161), and the `watch(progress, …, { immediate: true })` at :325-341 runs at construction with `playback.running` false → `livePhaseValue = 0` + repaint. The void-register-paint mechanism is CONFIRMED (IO delivers as a task; `visibleStages` empty at :310); the observable case is a **filter change while paused at a scrubbed phase ≠ 0**, not scene entry. |
| **D-M-1/C-1/L·D-2 tallies** | 17 of 28 (killed "18"); 15 (killed "16 of 23") | 17 of 28; 15 | **CONVERGENT — ADOPTED** | Both readers independently caught the L table's double-counted `ease-in-out-back` and C-2's internal 16-vs-15 contradiction. Magnitudes reproduced by both against installed value.js at 2001 samples; the ranking table is trusted on double-reproduction. Corpus figures of record: **17 of 28 tiles Δ > 0** (7 registry-order + rounding; 10 rounding-only); **15 of 23** quads round, each by exactly 0.005. |
| **C-2 BLOCKER escalation** (paste round-trip) | KILLED: no paste surface exists (picker = drag/preset/copy only) | KILLED: tag census of the whole picker chunk — zero `input`/`textarea`; `nameForQuad` reachable only from the picker's own emissions | **CONVERGENT KILL SUSTAINED** | Both refutations are independent and mechanism-distinct; my read of `onPickerChange` (:183-210) confirms `v.points` originates in the picker's own state. Survivor = the L-grade MINOR (lossy formatter + false prose + two co-rendered copyable precisions), folded into KF-ET-1. |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** named from the census taxonomy (`formation/keyframes/CENSUS-2026-08-03.md:179-206`: W0 Substrate Settle · W1 Mail Cure · W2 Parse Façade · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Tri-Fold Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit · W10 Fold Discharge & Close); behavioral repairs with no census owner are **NO-WAVE-OWNER (SS-1/SS-2 spec input)**; producer defects are **GLASS-OWNED**, riding the standing glass-ui BH relay (owner edict 2026-07-12). Routings are bookings, not acts.

### BLOCKER

- **KF-ET-1 · D-M-1 = C-1 = L·D-2 (+ D-15 and C-2-as-rescoped folded) — the header literal is not the curve on screen, and its copy affordance exports the divergence.** `literal` (EasingTarget.vue:209-220) takes the `isBezierEditable` branch for 23 of 28 specimen names (useEasingDemo.ts:73-76; NAMED_EASING_BEZIER carries 23 of 28) while tile, sparkline and engine all run value.js's analytic-registry-first `easing()` — **17 of 28 tiles** copy a curve with Δ > 0 against the one painted (max 0.158 end-to-end, `ease-in-out-circ`; 7 via registry order + rounding, 10 via `cubicBezierToString`'s `toFixed(2)` alone — 15 of 23 quads round, each by exactly 0.005, 10× the sidebar's own `quadEq` tolerance). The authorizing comment's own example (`ease-in-out-sine`, :217-219) is caught four lines above the line it annotates; three prose sites claim "COMPLETE re-parseable / never truncated". Both readers reproduced every magnitude independently; tallies corrected (17, not 18; 15, not 16). Frontier-durable by identity. **BLOCKER (unanimous).** → **NO-WAVE-OWNER** (SS-1/SS-2), **CURE-LOCKED by KF-ET-2**: the repair must emit the exact bezier at full precision where a faithful one exists and disclose or disable the copy where none does (`cssTwinFor`'s own doctrine, src/animation/easing.ts:41-50) — never "bind `cssValue`" (refuted below) and never a silent approximation. Rider letters → KF.W5: value.js needs a lossless timing-function serializer twin for `parseTimingFunction`, and `easing()`'s analytic-first resolution order documented in the `.d.ts` (cross-referenced to the V·π parser-proof round-trip concern).

### MAJOR

- **KF-ET-2 · reader-2 miss M-1, ADMITTED and re-verified by this seat — the shipped copy affordance already exports non-CSS, and the corpus-unanimous cure would multiply it by ten.** My probes against value.js's own grammar: `parseTimingFunction("ease-in-bounce")` and `("smooth-step-3")` **FAIL** — the two name-branch specimens that print bare names print strings that are not CSS `<easing-function>`s, under the ":38 Copy easing literal" label (silently `ease` on paste). All three axes scored these two "honest/exact". The cure every axis prescribed ("delete `literal`; bind `demo.cssValue`") is **REFUTED as a discharge of C-2 and adjudicated harmful**: `cssValue` (useEasingDemo.ts:93-104, byte-read) returns the bare NAME for everything except `cubic-bezier`/`steps`, so adopting it makes **20 of 28** specimens copy invalid CSS (survivors: linear, ease, ease-in, ease-out, ease-in-out, step-start, step-end, steps(n,term)) — and its `cubic-bezier` branch is the same `toFixed(2)` formatter, so the user-authored-drag loss survives too (reader-2's K-4, sustained). Correction against reader-2: the `return name` branch is reached by **4** specimens, not 5 — `steps` takes its own branch (a slip inherited from the L table's "honest" row grouping). **MAJOR (reader-2's BLOCKER preserved as dissent — its grade priced the repair-order kill, which this seat books as the cure-lock instead).** → **NO-WAVE-OWNER**, one spec with KF-ET-1; the pt-leaf/ColorSpaceSelector law restated for the record: *the cure is adjudicated separately from the defect, and a proposedCure inherits no evidential weight from its defect's evidence.*
- **KF-ET-3 · D-1 = C-14 = census F-1 (identity fold — never re-booked).** The phantom glass-ui dependency, TRUE of the audited worktree (my greps concur), FALSE-as-framed at HEAD (optionalDep 6.0.0, locked), **CURED at the frontier** (7.0.0 exact devDep). ONE identity with census **SCH-1** (kf-App ruling 1; kf-CopyButton ruling table). → **FOLD by reference to SCH-1 → KF.W0** (Substrate Settle): the reconciliation's one obligation from this corpus is that the in-flight migration land the frontier's exact-devDep form, not the worktree's undeclared form. This component's exposure facts (4 specifiers here + 3 in the sidebar; sole consumer of `/fading-scroll` + `/toggle-group`) ride as the fold's rider.
- **KF-ET-4 · D-B-2 = D-16 = C-21(a) — glass-ui 7.0.0 ships `glass-chip.css` orphaned from its own style graph; the selected-state wash this component's CSS documents never paints, and the 28 cells lose `--radius-card` for `--radius-pill` (9999px).** Re-verified by this seat package-wide (`glass-chip.css` imported by nothing; `glass.css` imports 18 siblings, chip absent; the demo loads only `exports["./styles"]`). Lost with it: the accent-band wash + edge + ink (`accent-tone.css` defines channels nobody reads), the `::after` flood + press-pop, the coarse-pointer 2.75rem floor, the `prefers-contrast: more` arm. EasingTarget.css:169-172's "the wash … The specimen ADDS" is false — :173-180 is the ENTIRE selected state. **MAJOR at the consumer surface (ruled; reader-2's BLOCKER as dissent).** → **GLASS-OWNED** — BH relay letter (a producer packaging defect degrading every Chip consumer in every repo; the fix is one `@import`); consumer obligations booked here: any wave touching this file corrects the :169-172 comment (and its "primary 15%" figure — the vendor truth is `max(18%, floor+10%)`, and the rule does not load); SS-13/KF.W9 witnesses the rendered pill-corner/no-wash state (one glance decides). The 7.0.0 measured IS the frontier's declared producer (substrate ruling), so this is not contingent evidence.
- **KF-ET-5 · D-M-2 = D-17 = C-22 + reader-1 miss M2 (ADMITTED, byte-verified) — "a curve is always selected" (:202-206) is false by THREE reachable paths, and the exported repair is ignored.** Path A: filter away from the selection (`familyFilter` written only by `onFamilyChange`; zero pressed tiles). Path B: any picker drag → `updateBezierPoints` → `currentEasingName = "cubic-bezier"` (:268-270), whose `Custom` family :158 deletes from the specimen set. **Path C (the admitted miss, sharpest)**: the picker's own preset list is literally `Object.keys(bezierPresets)` (my grep of dist/easing.js) — 30 names including `smooth-step-3`; `nameForQuad` searches only NAMED_EASING_BEZIER (29, no smooth-step-3; nearest neighbour `ease-in-out-cubic` differs by 0.005 > the 0.0005 tolerance), so picking the NAMED, catalogued, tile-bearing `smooth-step-3` falls through to Path B and **empties the gallery selection** while its own tile sits in the Cubic family. The repair seam exists and has zero readers: `currentFamily` computed at useEasingDemo.ts:116-118, exported at :388 (D's ":387" corrected by reader-2), **0 consumers repo-wide** (my grep — it is also one of KF-ET-20's five dead exports). **MAJOR.** → **NO-WAVE-OWNER** (SS-1/SS-2: the selection-follows or selection-absent state must be designed, not asserted).
- **KF-ET-6 · C-5 + reader-2 miss M-4 (ADMITTED, byte-verified) — `NAMED_EASING_BEZIER` is a byte-exact strict subset of value.js's published `bezierPresets`, its defending comment is false on both halves, and the one-key divergence renders a false caption over a picker that disproves it.** Probe convergent across both readers (30 vs 29 keys; sole delta `smooth-step-3`; zero value differences; EasingSidebar.vue:86-89's "wider (quart/quint)" and "differs on some quads (sine)" both false). The consequence chain verified by this seat: select the `smooth-step-3` tile → `seedFor` (`name in bezierPresets` TRUE, :112) seeds the picker with smooth-step-3's own preset, while `syncGap` (`!isBezierEditable`, :132-137) sets `catalogueGap` → the :42-49 caption renders "*smooth-step-3 is engine-native — editing here authors a custom cubic-bezier*" directly above a picker displaying that preset. `ease-in-bounce`, absent from BOTH tables, is captioned correctly — the misfire is exactly one tile, which is why every axis missed it. **MAJOR.** → **KF.W4** (K-Quartet easing hygiene) with the owner-ruling flag C-5 asked for: repointing at `bezierPresets` flips `smooth-step-3`'s class (its literal becomes a bezier) — a behaviour decision, not a rename.
- **KF-ET-7 · D-M-4 — reduced motion deletes the pedagogy rather than calming it, and leaves the transport a dead control.** Under PRM no painter registers (:304-307 before :310); `paintRestState` pins every ball at `fn(1)` — **= 1.000000 for all 28 specimens** (both readers executed; steps clamp included), so the comparative read (:8-10, the component's stated thesis) is deleted, not reduced, while the mounted PlaybackRibbon scrubber routes `progress` → `repaintDots()` → an empty painter set: the scrubber slides, nothing moves — explicitly-requested motion conflated with automatic motion. **MAJOR.** → **NO-WAVE-OWNER** (SS-1/SS-2: a PRM rest-state that preserves the comparison — e.g. rest at a mid-phase per-curve x — and a scrub path that repaints under PRM).
- **KF-ET-8 · D-B-1 as-rescoped (both readers convergent) — the graphic the component exists to show sits below the 3:1 non-text floor while `aria-hidden`.** Idle sparkline/ticks 1.59:1 light / 1.87 dark; rail 1.23/1.33 (reader-2 recomputed the 1.585 independently from shipped tokens); :94 hides the stage from AT while :8-10/:262-263/css:118-119 assert it is the content. Reader-1's kill of the 1.1.1 second horn SUSTAINED (`.tile-name` is a live-text complete alternative for a portrait of a *named* curve — D's own D-S-6 concedes it): **BLOCKER→MAJOR, narrowed to the 1.4.11 arm**, strongest under PRM where the sparklines are declared the only preview (compounding KF-ET-7). The selected-sparkline dark arm is CONTESTED (reader-1's recomputation 3.31:1 vs the D transcript 2.71:1 — opposite sides of the floor) → SS-13. **MAJOR.** → **NO-WAVE-OWNER** + **KF.W9** (the contrast reads).
- **KF-ET-9 · D-M-3(a) = C-17 — both `FadingScroll` mounts are unconditionally focusable with no role and no accessible name.** Compiled render fn: `tabindex: "0"` literal, `role` gated on `ariaLabel|ariaLabelledby`; :42 and :76 pass neither; the one label the component owns sits on the inner non-focusable grid (:80-81). Not previously banked (kf-ChannelControls N-2 banked FadingScroll *adoption*, not this defect). **MAJOR.** → **NO-WAVE-OWNER** consumer-side (one `aria-label` per mount, already written one element away) + **GLASS-OWNED rider** on the BH relay: unconditional focusability with label-gated role makes the default mount a nameless tab stop — gate the tabindex on naming or document the pairing as required (pairs with the standing BG-12 FadingScroll letter).
- **KF-ET-10 · C-4 = D-M-3(b), RESTORED BY THIS SEAT — neither reader adjudicated it (a corpus-coverage correction, register #6).** 28 independent `Chip mode="selectable"` toggles + a hand-rolled single-select invariant (:201-206) where `ToggleGroup type="single"` is used 30 lines up in the same file; 28 tab stops vs 1 + arrows; the `as-child` compose path exists on the installed 7.0.0 (C's probe). The ARIA-conformance arm stays dead (C's own N3, re-verified by reader-1: reka renders the same `role="group"` + `aria-pressed` shape); the keyboard burden survives, now atop the ~32-stop corrected total (rovingFocus ruling). **MAJOR.** → **KF.W6** (the chip-group pairing rides the Glass Suffusion decision + the BH letter for a first-class selectable chip group); interim NO-WAVE-OWNER note: any reshell must keep real `<button>`s (the KF-CB-7/S-2 constraint class).
- **KF-ET-11 · C-16 — nothing type-checks this file.** CI runs `check:lib` only; `check` is plain tsc (cannot parse .vue); vue-tsc absent — every glass-ui prop/emit contract in this template unverified (C-18 is the live instance). **ONE identity with the banked KF-CB-8** (merge-path gate topology; nightly-only demo battery). → **FOLD by reference to KF-CB-8 → KF.W4**; this file's exposure rides as the fold's rider.
- **KF-ET-12 · C-15 — overshoot balls are clipped by the tile's own paint containment; the sparkline was sized for overshoot (18%, exact — L-7), the rail was not.** `content-visibility: auto` (css:105) imposes paint containment unconditionally; `tileBallXAt` does not clamp; measured extremes reproduced by both readers (ease-in-bounce **1.1827**, ease-in-back **−0.0969**, ease-out-back 1.0869); the `px-2` cell geometry holds (chipVariants + the class-names `padding-x` group). At the 150px floor the ball leaves the padding box almost entirely at the bounce peak. **MAJOR.** → **NO-WAVE-OWNER** (clamp, allowance, or `overflow-clip-margin` — the sparkline's own solved half is the template); pixel extent → SS-13/KF.W9.
- **KF-ET-13 · D-18 — zero test coverage for this component, in a tree with a per-scene test idiom** (10 scene specs, no easing; the sole grep hit is the catalogue data test). The cost is measured: KF-ET-1 and KF-ET-5 each die to a five-line assertion in a file that already imports EASING_GROUPS. **MAJOR.** → **NO-WAVE-OWNER** (the SS-1/SS-2 spec's acceptance tests) with the cure-lock rider: the once-obvious oracle `literal === demo.cssValue` is **void** (KF-ET-2) — the test must assert the post-ruling contract (parseable-or-disclosed), not the refuted identity.
- **KF-ET-14 · D-M-6 — the entire selected state is one colour channel at ≤4.63:1 light plus a 12px weight bump** (given KF-ET-4 the wash never paints; :173-180 is everything; WCAG 1.4.1's exact target). The 4.63 is against `--card`, the optimistic baseline; the true `.glass-capsule` composited plate is lighter → SS-13 decides the AA arm. **MAJOR (contingent: partially discharged if the producer restores the wash — the 1.4.1 single-channel point survives even then).** → **NO-WAVE-OWNER** + **KF.W9**.

### MINOR

- **KF-ET-15 · D-M-7 = C-11 — the pressed, violet-inked `steps` tile races a 4-step staircase under a header reading live `steps(n, term)`** (:175-178 pins; `getCurvePath` memoises `generateStepSVGPath(4)` permanently; the picker's steps mode writes `stepOptions` reachably; the "4" itself is duplicated across the module boundary — reader-2's note, same class as the BALL_SIZE triplication). Documented at :172-174; declaring a divergence does not remove it, but the in-file concession + 2-of-28 scope prices it **MINOR (D's and reader-1's MAJOR preserved as dissent)**. → **NO-WAVE-OWNER**, one spec with KF-ET-1 (the literal contract decides both).
- **KF-ET-16 · D-M-8 as-corrected — no forced-colors arm in the demo; the chip/capsule family is absent from glass's WHC skin; and (convergent correction, re-verified by my grep) glass-chip.css has NO forced-colors block anywhere — the finding strengthened, the parenthetical struck.** SVG `stroke` is not a forced property, so the sparkline's authored ink survives into a forced palette. → **FOLD by reference to KF-CE-13** (demo-wide forced-colors row) → **KF.W9**; the producer half (chip family into a11y-fallback's selector list) rides KF-ET-4's BH letter.
- **KF-ET-17 · D-M-9 — the copy affordance is a 16×16 CSS-px target** (css:68-75 + CopyButton's `p-0 m-0`), compounded by the orphaned coarse-pointer floor. **ONE identity with the banked KF-CB-5/KF-CB-14 family** (EasingTarget.css:68-74 is already cited in KF-CB-5's proof as the consumer prosthesis). → **FOLD by reference → KF.W6**; the SC 2.5.8 spacing-exception question rides SS-13.
- **KF-ET-18 · reader-1 miss M1, ADMITTED — verified whole by this seat: hovering the SELECTED tile erases its selected ink.** `.specimen-tile[data-state="on"] .tile-name` (:177-180) and `.specimen-tile:hover .tile-name` (:191-193) are both (0,3,0); the SFC attaches the sheet via `<style scoped src>` (:335 — reader-1's compilation premise exact, one attribute appended to each subject equally); the later hover rule wins and repaints the pressed name to `--foreground`, destroying the colour half of the only selection cue KF-ET-14 leaves. Only `font-weight` and the sparkline stroke (no hover competitor) survive the hover. All three axes read this sheet whole; none ordered the two rules. **MINOR.** → **NO-WAVE-OWNER** (one selector reorder or `:not(:hover)`/specificity bump; rides KF-ET-14's spec).
- **KF-ET-19 · reader-1 miss M3, ADMITTED — the header is internally inconsistent for 140 ms on every selection.** The `<h2>` (inside the out-in Transition) holds the OUTGOING name through the leave while `.literal-text` and the armed CopyButton (outside it, :31-39) already carry the INCOMING literal — the same "the header shows something that is not the thing" class as KF-ET-1, at the interaction moment; the true defect at the seam where D-M-5's collapse died (kill register #2). **MINOR.** → **NO-WAVE-OWNER** (transition the `.gallery-id` pair together, or key the span with the h2).
- **KF-ET-20 · reader-2 miss M-3, ADMITTED — verified by this seat's greps: `useEasingDemo` exports five members with zero consumers repo-wide** (`currentEasingFn`, `svgPath`, `comparisonCurves`, `currentFamily`, `liveProgress` — 0 hits each outside the composable), three of them residue of surfaces the file's own comments record as DELETED; three imports feed only dead code; the 24-member context is read at 6 members by this component. `currentFamily`'s double status noted: dead export AND KF-ET-5's ignored repair — consume it or delete it, decided together. **MINOR.** → **NO-WAVE-OWNER** hygiene (the no-legacy-beside-the-replacement doctrine the file itself invokes).
- **KF-ET-21 · reader-1 miss M4, ADMITTED — 28 concurrent `backdrop-filter` glass plates inside a `mask-image` scroller**: every tile carries `.glass-capsule` (loaded; `--glass-blur-floating` + 3-part shadow) — a floating-tier choice for a 28-cell grid when `glass-quiet`/`glass-resting` exist and `Chip` exposes `surface`/`tone` seams; materially heavier than the 28 `will-change` balls all three axes flagged. Bounded, not removed, by `content-visibility`. **MINOR; magnitude → SS-13.** → **KF.W6** (the tier decision is a Glass Suffusion call).
- **KF-ET-22 · D-m-1 + reader-1 miss M5 (ADMITTED) — the caps-register tracking survives on lowercase identifiers AND is what breaks the file's own no-truncation promise.** `.tile-name` resets `text-transform` only; `--type-tracking-caps: 0.1em` remains; reader-1's arithmetic (134px content box; ≈12.15px caption; +1.2px/char) puts `ease-in-out-cubic` at/past the line box at the 150px floor, where `overflow-wrap: anywhere` (css:186) breaks MID-IDENTIFIER — against :74-75's and css:182's explicit promises. Deleting the inherited tracking buys ≈19px. **MINOR; exact wrap point → SS-13.** → **NO-WAVE-OWNER** (one-line `letter-spacing` reset).
- **KF-ET-23 · D-19 = C-9 — `rootMargin: "25% 0px"` cannot pre-arm the drawer**: the `.fading-scroll--y` intermediate clipper (verified in the loaded closure) clips before the margin applies to the root rect only; no tile is ever pre-armed; FadingScroll exposes no scroll port (no `defineExpose`). **MINOR.** → **NO-WAVE-OWNER** (drop the margin and own the snap, or root on the scroller) + the BH-relay scroll-port ask (rides KF-ET-9's letter).
- **KF-ET-24 · D-20 as-rescoped (kill register #9) — the documented "paints once on register" writes nothing** (`visibleStages` cleared at :282, refilled only by the async IO task; the :256 gate skips all 28 on the register pass), and the comment's paused-entry exemplar CANNOT occur (entry phase = 0 by construction); observable only on a filter change while paused mid-scrub. **MINOR.** → **NO-WAVE-OWNER** (seed the set optimistically or paint unconditionally on register).
- **KF-ET-25 · D-11 — CopyButton's `isCopied` write-once latch.** **ONE identity with the banked KF-CB-3** (kf-CopyButton roster; the file is byte-identical at the frontier per that record). Reader-1's sharpening rides as context: glass-ui's own EasingPicker one panel away runs the full idle/pending/copied/failed machine with a 1400 ms reset. → **FOLD by reference to KF-CB-3**; zero re-booking.
- **KF-ET-26 · D-12 = C-8 — `railWidth` is sourced from `tileSnapshot[0]`, an index the same function declares unordered (:283-284 vs :270/:302), and a zero measure is sticky for every ball** (the RO re-reads the same entry; `maxX ≤ 0` parks all 28 at the origin). The doctrine self-contradiction is CONFIRMED static; the `content-visibility` freeze half is UNPROVEN → SS-13. **MINOR.** → **NO-WAVE-OWNER** (measure from the RO entry's contentRect / gridEl).
- **KF-ET-27 · D-m-2 = D-6 = C-10 = C-19 — `BALL_SIZE` encoded three times across two languages, and `design-idioms.css:162` documents a `getComputedStyle` seam this component never uses** (zero hits in scenes/easing; the shared fallback is 36px against a rail computed for 14). The idiom sheet carries a false contract naming this exact file. **MINOR.** → **NO-WAVE-OWNER** (one home for 14px + fix the sheet); C's proposed **S-10 census arm** over design-idioms.css (glass overlap + contract truth) is ADOPTED as a KF.W5 input.
- **KF-ET-28 · D-3 = C-6 — value.js's Result API reaches the render path through a throwing adapter with no boundary**, latent by measurement (25/25 names resolve; the catalogue test REDs a typo before a browser can) — the L re-grade to MINOR is CORRECT and its ground verified (easing-catalog.test.ts enumerates every item + asserts `easing(name).ok`). The `?? ""` decoy at :288 guarantees the throw it appears to prevent, after the painter is already retired. **MINOR.** → **NO-WAVE-OWNER** (degrade-don't-detonate posture; skip-not-throw on missing attribute).
- **KF-ET-29 · D-4 = C-7 — `SpecimenCurve.fn` is allocated per tile per filter change and never read**; the painter re-derives from `el.dataset.curve`; caching asymmetric (`getCurvePath` memoised, `namedEasing` not). **MINOR.** → **NO-WAVE-OWNER** hygiene (delete the field, keep the data-curve keying — L-3/P-5 is correct and must survive the fix).
- **KF-ET-30 · D-7 + D-8 + D-10 + D-5 — the painter-seam posture bundle**: the IO is built and 28 stages observed before the PRM early-return (dead callback under PRM); `wirePainter` is async with three promise-dropping call sites (rejection visible, not swallowed — the Monaco guard preventDefaults only its own signature) and torn state on a mid-body throw; `railWidth` is a `ref` no reactive consumer reads (a live trap in a deliberately off-graph section); the `currentEasingName` watch (:324-329) is a provable no-op writing byte-identical transforms. **MINOR (bundle).** → **NO-WAVE-OWNER** hygiene, one pass with KF-ET-24/-26.
- **KF-ET-31 · D-m-5 / D-m-6 / D-m-8 / D-m-9 / D-m-10 / D-m-11 — the residual D-minor set, spot-verified by reader-1 and adopted**: paint containment clips the ball's glow (the tile's own shadow survives); 28 unreleased `will-change` promotions; every curve's authored `description` dropped with no tooltip/aria consumer; the `Bounce` filter yields a comparison set of one; filter changes fire no AT announcement (`role="group"` with no live region); the off-scale raw-value set beside the Tailwind scale. **MINOR (bundle).** → **NO-WAVE-OWNER** (SS-1/SS-2 sweep); the will-change/layer magnitude rides SS-13 with KF-ET-21.

### INFO

- **KF-ET-32 · C-18 + C-20 + C-12/D-21 + C-13 — the consumption-surface bundle**: `ToggleValue` (:164) mirrors reka's `AcceptableValue` and has drifted (adds `boolean`, omits `bigint`; glass-ui re-exports no alias — 0 hits — so no supported spelling exists: **BH-relay letter**, re-export the payload type); the 41.6 KB `/css` module (exporting `parseCssColor`) enters the static graph for two frozen tables (link, not call — N1 keeps this component **clean** on the R1 map, a KF.W3 scoping input); the sole keyframes.js import is an erased type and the wrong library's (structurally identical; the coverage hole is the library's — no synchronous name→fn surface: **KF.W5 letter**, expose the registry off `loadAnimationEngine()`); `inject(EASING_DEMO_KEY)!` is the file's entire contract (house idiom; posture-only). **INFO/MINOR (bundle).** → **KF.W5** letters + NO-WAVE-OWNER observations.
- **KF-ET-33 · C-21(b) + reader-1 miss M6 (ADMITTED) — the stale "ToggleChip" vendor name has TWO sites, not one**: EasingTarget.css:169 and the drawer comment at EasingTarget.vue:70; glass-ui 7.0.0 exports `Chip` only. Census **S-2 class, EXTENDED with the second site — never re-booked as a new defect**. The related API point (first-class `tone` prop unused in favour of `[data-state]` descendant reaches) rides KF-ET-4/KF-ET-14's repair. **INFO.** → **KF.W5** prose sweep.
- **KF-ET-34 · D-m-3 = D-14 — two false load-bearing comments** ("33 rails" vs 28; "primary 15%" vs the vendor's 22/25% — in a rule that does not load). **INFO.** → corrected by whichever wave touches the file first (obligation attached to KF-ET-4).
- **KF-ET-35 · reader-2 miss M-5, ADMITTED — `animationDescriptions.ts` hygiene**: two unexported dead `const` tables (:108/:116, 0 references) and both import statements at :128-129, after 127 lines that consume them — every axis cited this file without reading past :51. **INFO.** → **KF.W5** sweep.
- **KF-ET-36 · reader-1 miss M8, ADMITTED — the sparkline's horizontal exaggeration varies ~2× across the auto-fill range** (fixed 3.25rem stage under 150→~300px tracks; `preserveAspectRatio="none"` stretches 4:1→9:1), flattening portraits toward the diagonal exactly where there is most room — the fragility inside the P-7 praise. **INFO.** → NO-WAVE-OWNER design observation; optional SS-13 glance.
- **KF-ET-37 · D-i-1..4 — the register bundle**: the oracular comment voice whose five falsified claims this roster enumerates (the prose is the only place the invariants are recorded, and its confidence anti-correlates with accuracy); `<h2>` as a promoted readout in the heading tree; `contain-intrinsic-size` drift (never-rendered rows only); physical-axis race vs logical grid (RTL latent, no `dir` surface). **INFO.** → NO-WAVE-OWNER observations.

## Killed-claims register — 10 entries, all SUSTAINED

1. **D-M-3(c) — "the filter's roving focus is off in the shipped artifact" (and the "41 tab stops" arithmetic).** KILL SUSTAINED at the bytes by this seat (ruled table); reader-2's MAJOR confirmation REVERSED — reka's `useForwardProps` forwards only defaulted-or-passed keys; `rovingFocus` never reaches `ToggleGroupRoot`, whose own `default: true` governs. The dist-vs-source divergence is real and behaviourally null. ~32 stops, not 41; reader-2's every-consumer blast radius falls with it.
2. **D-M-5 — "the header collapses ~7.6-19.4px on every selection".** KILL SUSTAINED (reader-1): under `out-in` the outgoing `<h2>` stays mounted through a layout-inert 140 ms leave, and removal + replacement render run in one synchronous `afterLeave` — no painted frame lacks the heading. Reader-2's MINOR confirmation corrected. The seam's real transient defect is KF-ET-19 (desync, not collapse).
3. **C-2's BLOCKER escalation — "paste the copied literal back into the scene's picker".** CONVERGENT KILL SUSTAINED: the picker has no paste surface (zero input/textarea; `nameForQuad` reachable only from the picker's own emissions, re-read by this seat at :183-210). Survivor = the lossy-formatter MINOR, folded into KF-ET-1; C-2's core facts (toFixed(2); three false prose sites; two co-rendered copyable precisions) all survive.
4. **C-2 §Repair-order — "C-3 is the single edit that discharges C-1 AND C-2".** KILL SUSTAINED (reader-2) and EXTENDED by this seat's probes into the cure-lock (KF-ET-2): `cssValue` makes 20 of 28 copies invalid CSS and keeps the 2-dp loss on authored drags. All three axes prescribed the refuted cure; only C claimed it discharged both.
5. **L·D-2's "18 of 28".** CONVERGENT KILL SUSTAINED — 17; the L table double-counts `ease-in-out-back` (named row 0.0101 AND the ten-name residual row). Every magnitude otherwise reproduces; the L doc's second arithmetic slip in the same finding (rev 2 had already corrected "25"→23).
6. **C-2's "16 of 23 lossy".** CONVERGENT — 15, which C-2 itself states four paragraphs later; the internal contradiction resolved to 15 by both readers' probes.
7. **D-B-1's second horn — "aria-hiding additionally fails 1.1.1".** RESCOPE SUSTAINED (reader-1): each tile's accessible name is the curve's canonical identifier as live text — a complete text alternative for a portrait OF a named curve (D-S-6's own concession). BLOCKER→MAJOR; the 1.4.11 arm survives, sharpest under PRM; the selected-sparkline dark arm is contested (3.31 vs 2.71 across 3:1) → SS-13.
8. **D-M-8's parenthetical — "the chip's forced-colors provisions live in the unreachable glass-chip.css".** CONVERGENT CORRECTION SUSTAINED and re-verified by my grep: glass-chip.css has NO forced-colors block (it carries PRM/coarse-pointer/prefers-contrast only); nothing forced-colors-aware for the chip family exists anywhere in the package — the substantive finding strengthened.
9. **L·D-20's exemplar — "every ball sits at translateX(0) for a frame on paused entry".** KILL SUSTAINED (reader-2's K-3) at this seat's bytes: `progress` seeds 0 and the `{ immediate: true }` watch sets `livePhaseValue = 0` at construction, so the skipped register-paint would have written the identical transform. Mechanism confirmed; observable case = filter change while paused at phase ≠ 0. Reader-1's confirming cell narrowed.
10. **F-1's "declared nowhere / no install path produces 7.0.0" framing.** KILLED AS FRAMED by both readers' own misses and settled by this seat's frontier probe: HEAD declares optionalDep 6.0.0 (locked); `origin/master` declares **7.0.0 exact devDep**; the audited easing chain is **byte-identical to the frontier**. ONE identity = census SCH-1 (kf-App ruling 1) → KF.W0; the phantom is worktree-only and the 7.0.0 evidence base is the frontier's declared producer.

## Adjudicator corrections register (against this corpus's readers)

1. **Reader-2's D-M-3(c) confirmation cell** — "Vue resolves an absent Boolean to `false`, so reka receives explicit `rovingFocus: false` … spreads the whole resolved prop bag": false at the forwarding bytes (reka's `useForwardProps` forwards a computed KEY SET, not the resolved bag). The one reversed confirmation in this adjudication.
2. **Reader-2's D-M-5 confirmation** — "mechanism decidable" endorsed a mechanism that is statically refutable from Vue's out-in semantics + the layout-inert transition properties it had itself read.
3. **Reader-2's M-1 count** — "the `return name` branch is reached by exactly 5 specimens": 4 (`steps` takes its own branch at :211-213); a slip inherited from the L table's "honest" row grouping. Substance unaffected (the 2 invalid and the 20-under-cure figures re-verified by this seat).
4. **Reader-2's M-2 frame** — "an artifact that no install path in this repo produces": dated at the frontier (7.0.0 exact devDep); and the kf-App/kf-CopyButton records that had already banked the substrate shape were in the registry this return was written into (the same not-consulting-the-registry class kf-CopyButton's correction #4 recorded against its reader).
5. **Both readers' frontier blindness** — reader-1 probed no git layer at all; reader-2 established the HEAD layer but not `origin/master`. The governing simplification of this corpus — the easing chain is byte-identical to the frontier, so nothing is voided and nothing was silently repaired — was one `git diff origin/master` away. Third occurrence of this class in the program (kf-App, kf-CopyButton).
6. **Both readers' C-4 silence** — neither return confirms, kills, nor mentions C-4/D-M-3(b) (the 28-loose-toggles MAJOR); the corpus's own probes stand unchallenged and the row is restored by this seat as KF-ET-10. A hostile re-read that adjudicates 90% of a corpus leaves the remaining 10% *unaudited, not endorsed* — recorded as register law.

## Superlatives — sustained, with provenance (L-18 runs both ways)

1. **P-1/L-1 · the shared-clock painter** (all three axes + both readers; hunted independently by both for a layout read / non-transform write / reactive write / DOM query — none exists): one phase, pre-built snapshot, IO gate, `style.transform` only, cached railWidth, static glow. "All balls depart together" is true by construction. Sustained with D-20's narrowing (the register-paint clause died; the hot path did not).
2. **P-2 · the `.progress-ball` idiom leaves `transform` unclaimed BY DESIGN** — `margin-top` centring on the ball, `translateY(-50%)` on the rail that nothing paints; the asymmetry is the proof of intent, and one tidy-up would drop 28 balls out of their rails.
3. **P-8/L-8/D-S-1 · tick geometry exact to the half-pixel, derived from the right quantity** (recomputed by both readers: 14/2 = 7 = 6.5 + 0.5; maxX + 7 = W − 7) — the class of thing that is almost always off by BALL_SIZE/2. Also KF-ET-27's hostage: exactness maintained by three uncoupled literals.
4. **L-7 · the 18% sparkline headroom is a SIZED allowance** — max overshoot 1.1827 → 11.7% needed against 18% granted, both ends, ~35% margin (re-executed by both readers). Also what convicts KF-ET-12: one half of the tile was sized for overshoot and the other was not.
5. **D-S-2 · the idle label ink rides the vendor's on-glass register** through the LOADED ladder re-point — 8.19:1/9.07:1, reproduced independently — the one ink path the component did NOT override, and the one that passes comfortably.
6. **D-S-3 · the `family-row` fix has exactly the right posture** — bug diagnosed, cure removes the overflow condition rather than `:deep()`-ing the vendor root, strip-posture ask relayed as BG-12. Six lines, three usual failure modes avoided.
7. **`test/demo/reference-data/easing-catalog.test.ts` is a real data gate** (both readers verified independently): enumerates every EASING_GROUPS item, asserts non-empty prose + `easing(name).ok` + pins three curves to value.js canonical exports by identity — the single gate between a catalogue typo and a white screen in a demo nothing type-checks, and the ground of KF-ET-28's MINOR re-grade.
8. **P-9 · portraits memoised at module scope** — 28×33 samples once per session, surviving remount; the right altitude for page-constant data.
9. **P-7 · the sparkline is dimensionless by construction** — unit viewBox + `preserveAspectRatio="none"` + `vector-effect="non-scaling-stroke"`; both halves necessary, both present (KF-ET-36 records the one fragility inside the praise).
10. **L-4 · the controlled-toggle invariant is right against reka's real semantics** — `:model-value` always boolean, never `undefined`, so `useVModel`'s passive flag resolves false and ignoring the `false` emit is safe, not a desync (reader-2's re-verification adopted; seeded `"ease"` closes the first-render hole).
11. **P-6/L-2 · the vendor boundary is clean at every layer** — zero reka imports, zero local vendoring, subpath-only value.js, the emit typed structurally (the mechanism's rot is KF-ET-32's row; the intent is census F-6's GREEN).
12. **Reader-2's unrecorded find, ADOPTED · `namedEasing`'s step special-case is load-bearing** — `easing("step-start"/"step-end"/"steps"/"cubic-bezier")` all return `easing_name_unknown` from value.js 4.0.0 (probed); timingCurveUtils.ts:43-44's two lines are what keep three of 28 specimens from taking the scene down at render.
13. **D-S-5 · the design refuses to differentiate the ball** ("the race reads only when every runner is equal") — sound judgement; KF-ET-14 is an execution failure on the channel this judgement correctly chose.
14. **CORPUS · the documents police themselves and say so** — the L rev 2 corrects its own P-1/P-3, closes its self-declared unchecked falsifier arms, and re-grades D-3 against new evidence, every change marked; the C axis records pass 1's probe killing pass 2's stronger ARIA claim (N3, re-verified through reader-1). Both are more trustworthy for it; both readers independently said so and this seat concurs.

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **KF-ET-4's rendered consequence** — do the 28 cells read pill-cornered (9999px clamped ≈49px) with no wash and no press flood on the pressed tile? One glance decides the whole producer finding.
2. **KF-ET-14's light-arm AA question** — computed background of `.specimen-tile[data-state="on"]` vs computed `.tile-name` color on the real composited `.glass-capsule` plate (the 4.63:1 is against `--card`, the optimistic baseline).
3. **KF-ET-8's contested row** — the SELECTED sparkline's dark arm, where reader-1's model (3.31:1) and the D transcript (2.71:1) fall on opposite sides of the 3:1 floor; plus the idle 1.59:1 confirmation read.
4. **KF-ET-26/12's freeze half** — does `clientWidth` inside a `content-visibility: auto`-skipped tile return 0 in the target engines? If yes: scroll, resize, and every ball parks at the origin permanently.
5. **KF-ET-12's pixel extent** — how much of the 14px ball survives at ease-in-bounce's 1.1827 peak and ease-in-back's −0.0969 dip at the 150px floor.
6. **The rovingFocus one-keystroke witness** — tab the family filter: one stop confirms this seat's ruling; ten would revive D-M-3(c) against the forwarding bytes (it should not).
7. **KF-ET-24's origin flash** — on a filter change while paused at a scrubbed phase ≠ 0 (NOT on entry — kill #9).
8. **KF-ET-21's magnitude** — composited layer count / GPU cost of 28 backdrop-filter capsules + 28 will-change balls inside a mask-image scroller, at rest.
9. **KF-ET-22's wrap point** — does `ease-in-out-cubic` break mid-identifier at the 150px floor with the 0.1em tracking in force (real Fira Code metrics)?
10. **KF-ET-17's spacing exception** — does a 24px circle centred on `.literal-copy` intersect any other target's box at every breakpoint (marginal on the wrapped narrow header)?
11. **KF-ET-16's rendered forced-colors outcome** — including whether the sparkline's authored `stroke` paints near-black over a forced-black canvas.
12. **KF-ET-7's PRM witness** — all 28 balls at one identical x, and the scrubber sliding with nothing moving.

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c`, whose easing chain is byte-identical to `origin/master` `81a56990` — the governing substrate fact this seat adds: unlike kf-CopyButton, NOTHING here was silently repaired upstream; every row is frontier-durable by identity, and every glass-ui 7.0.0 cell was measured against the frontier's declared producer (census SCH-1 fold). 37 roster rows — 1 BLOCKER (the literal fork, now CURE-LOCKED: the corpus-unanimous repair is refuted by this seat's own parser probes — 20 of 28 copies would go invalid — so the fix must be specified against `cssTwinFor`'s doctrine, not patched) · 13 MAJOR · 17 MINOR · 6 INFO, with 5 folds by reference (SCH-1, KF-CB-3, KF-CB-5/-14, KF-CB-8, KF-CE-13) and zero re-bookings. 10 kills sustained — including both head-on reader contradictions, each ruled at the bytes for the kill (the filter KEEPS its roving focus; the header does NOT collapse), with one reader confirmation reversed outright. 13 misses admitted — the cure-lock, the hover that erases the selection it decorates, the catalogued curve whose picker pick empties the gallery, the 140 ms header desync, the five dead exports. The component is the best-engineered hot path in the demo tree wearing a derived-data layer that lies about itself: the painter, geometry, memoisation and vendor boundary are genuinely exemplary, and every blocker-weight failure is in what the prose PROMISES — a copyable literal that is neither the curve on screen nor, twice, CSS at all; a selection invariant false three ways with its exported repair unread; a selected state whose vendor half never loads and whose surviving half a hover erases. Routings are M-25 agglomeration input: behavioral rows NO-WAVE-OWNER for SS-1/SS-2; producer rows GLASS-OWNED on the BH relay (the glass-chip orphan, the FadingScroll tabindex/port, the AcceptableValue re-export); structural folds to KF.W0/W4/W5/W6/W9; 12 visual rows to SS-13/KF.W9. Nothing here stamps a wave; X·KF executes on the owner's begin-word.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/easing/EasingTarget.css` · `demo/scenes/easing/EasingTarget.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **EasingTarget.css=IDENTICAL · EasingTarget.vue=+41/-16**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · G-KFW9-13) — **KF-ET-8**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here. **Nothing above this line is
rewritten** — no edit, no re-grade, no renumber, no deletion, no new id (E-1 immutable bytes / E-3
addenda-not-patch). This addendum records a MEASUREMENT STATE, not a grade: **the banked severity is
unchanged and stays exactly where the record put it.***

- **armed trigger** — register row **11** of thirteen, `evidence/W9/ESCALATION-REGISTER.json` (id as banked there: `KF-ET-8 (contested dark arm)`); ceiling if it fires: **MAJOR**; roster cell(s): `—`.
- **measurement named by the trigger** — the **dark-arm** contrast numeral, **RE-DERIVED AT CAPTURE** (KF-SKEL-22 — no corpus numeral is the figure of record).
- **measured value** — **NONE. The trigger did not run.**
- **resulting severity** — **PROVISIONAL (unrun)**. The banked grade stands; it is neither inherited as confirmed nor moved.
- **exact precondition** — one dark-arm capture of the selected sparkline over the true plate, with the numeral re-derived from the delivered pixels. **The DARK arm specifically** — a light-arm reading does not decide it, and none of this wave's 7 shots is a dark-arm contrast plate. the `safari-app/desktop` cell was open and productive at `.d` (4 sessions, OP-4 taken, 3 shots) and **closed mid-seat**: session creation began returning *"You must enable 'Allow remote automation'…"*; `safaridriver --enable` → **`Password:Password is not valid`** (an interactive admin authorization no seat here can supply); the running Safari is the **owner's** (pid 23725, started 16:23:59, 6 windows) and was not quit. **Precondition: 'Allow Remote Automation' re-enabled by a hand that can answer an admin prompt, or the owner's Safari restarted.** the `chromium` and `chromium/emulated-forced-colors` cells DID open at `.d` (8 rows, 2 + 2 captures), so this trigger is **unrun, not foreclosed** there: it needs a pass that mounts the subject. kf-EasingTarget's **KF-ET-24 is a SEPARATE row and a named TRAP** (NEGATIVE-REGISTER T-3) — not conflated here, and not re-probed.
- **substrate of this wave** — keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD; frontier moved to `3e81f500`, the pin is its ancestor, **no re-pin on a seat's authority**). Served bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448`, built in the §0m.2 capture clone and **re-hashed at this seat, double-run identical**. Capture: **none for this row**; shot-sha256: **none**.
- **G-KFW9-13 reading** — this row closes the gate's *"unrun triggers reported PROVISIONAL at close"* arm, **not** its *"measured value written back"* arm. The gate stays **RED**.
