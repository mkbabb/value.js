claude-fable-5 (served model id — read from this seat's own system prompt, not inherited)

# fr-EquationResult — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator)

**Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/equation/EquationResult.vue` (101 lines, read whole by this seat) · fourier HEAD `cd26c65`, branch `m/w1-bump-migration`, tree **DIRTY** (26 files — the M-bump in flight: the committed manifest still pins `@mkbabb/glass-ui ^3.1.0`; the dirty manifest moves it to `^4.0.0` + `katex ^0.17.0` + `vue-router ^5.1.0`, my `git diff`). **The subject file is CLEAN**; its consumer `EquationView.vue`'s dirty delta is four in-place line-swaps (`UnderlineTabs`→`SegmentedTabs variant="underline"`, `glass-elevated`→`glass-floating`, `MetricBadge amount`→`value`) leaving every anchor cited below stable — re-diffed by this seat. Installed producer `@mkbabb/glass-ui@4.0.0` (80-key export map; `./dom`, `./fading-scroll`, `./toast`, `./icon-tooltip` all live — my fs read). Producer working tree **8.0.0** at `558c3fa3` — note it has moved AGAIN past the `4a86570b` both readers pinned; version and tag `v8.0.0` unchanged.
**Date**: 2026-08-12 · **Corpus**: three challenge axes (D 22 rows · L B1-B2+M1-M5+m1-m6+i1-i3 · C 19 rows) at `audit/fourier-components/EquationResult/`, challenged by two hostile re-readers (design-and-usage · library-and-code), both returns adjudicated here.
**Method (M-23/L-18)**: every reader-vs-reader disagreement, every kill of a load-bearing cell, and every NEW missed find was re-derived against the trees by this seat with its own instruments before adoption. Zero browser probes — every ruling is bytes (read + grep + git + node execution). Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments**: subject whole (101 lines — every C-axis line-drift claim decided at the bytes); EquationView anchors `:37-39/:43/:49-51/:221/:229/:237/:239-242/:255/:386-390/:426-434` re-read; installed `dist/composables/dom/useClipboard.d.ts` whole (`copied: Ref<boolean>` · `onCopyError` · 3-member `CopyFailureReason` · the `resetMs: 2000` worked example VERBATIM); producer `git show v7.0.0:src/composables/dom/useClipboard.ts` (`status`, no `copied`; `CopyFailureReason` **already 2-member at v7**; `timeoutMs` **already present at v7** — 3 hits) + HEAD identical; producer `Button.vue:127-146` DEV retired-prop diagnostic read verbatim at `558c3fa3` (`console.error`, `"variant" in attrs` + `size === "icon"` legs); installed `dist/styles/utilities/a11y-overrides.css:6-31` read verbatim (universal `*:not([data-allow-motion])` PRM reset; `transition-property` allowlist EXCLUDES `transform`); **KaTeX executed, not just read**: `node --input-type=module` against the installed `katex@0.17.0` — `renderToString` of a sigma-form `\htmlClass` string with `{displayMode, throwOnError:false, trust:true}` emits `console.warn("LaTeX-incompatible input and strict mode is set to 'warn': HTML extension is disabled on strict mode [htmlExtension]")` while rendering `eq-coeff` intact, and `strict: (code)=>code==="htmlExtension"?"ignore":"warn"` silences it with the class intact (the M-2 decider, run twice); katex.mjs `SETTINGS_SCHEMA.strict` (enum-first, no default key) + `reportNonstrict:304-321` + the `:11199` htmlExtension report; `latex_rendering.py` htmlClass census — **6 hits, 4 emission sites, all four inside the `*_sigma` renderers**, expanded renderers clean (the C-2/M-1 decider); `web/public/_headers:1-11` rationale block + `:64` CSP verbatim; `nginx/fourier.conf:25-29` (five headers, no CSP); `playwright.config.ts` whole (3-env matrix; NO webServer block; prod cell "RED-with-cause"); `scripts/e2e.sh:102` (`npx vite web` — the DEV server, `import.meta.env.DEV === true` under e2e); e2e console-gate census — **FOUR gates, not the readers' three**: `visualization-crud.spec.ts:439-482/:612` (filters favicon/ERR_CONNECTION_REFUSED/429/404/412/401), `workspace-flow.spec.ts:175-202`, `contour-extraction.spec.ts:139-166`, **`gallery.spec.ts:118-135`** — no filter excludes `[glass-ui]`; `web/tsconfig*` ls → `tsconfig.json` + `tsconfig.tsbuildinfo` ONLY (no `tsconfig.app.json`), `strictTemplates|vueCompilerOptions` grep → 0, `moduleResolution: "bundler"` at `:5`; an independently written OKLCH→linear-sRGB→WCAG pipeline (third derivation: **2.101 / 2.175 / 6.559 / 8.510** — D-9's four cells to three decimals; this seat's FIRST run double-linearized Ottosson's already-linear output and read 3.76/6.56-inverted — caught by calibrating against the banked figures, corrected, recorded); tokens verbatim (`--color-green-500` theme.css:75, `--card` color-radius.css:72 / dark-arm.css:64, `--success` :273/:144); repo counts re-run on the dirty tree — `variant="` = **124**, `size="icon"` = **38 / 21 files** (unchanged by the in-flight bump), `useClipboard` consumers = **3** (subject `:15` 2000 · `useMorphConfig.ts:58` 2000 · `gallery/UserSlugBar.vue:23` **1500**), `@reference`-with-no-`@apply`/`theme(` = **9 of 35, D-19's nine names one-for-one**; siblings re-read (AnimationControls `:195-197` A.W3.d comment + tokened icon-swap + `scale(0.7)`; ConvergenceTimeline `:140-143` 0.1s raw ease; EquationPanel `:28-34` identical options triple + `text-red-400` fallback + `:87` `aria-label`; `useCoeffHover.ts:99-102` triple + `""` fallback; `ConvergencePlot.vue:255-258` NO trust; `useEquationCache.ts:28/:39` unvalidated `JSON.parse`, sessionStorage ×4); `katex.min.css` `.katex-display` rules extracted verbatim; `visual-baseline.spec.ts:31-38` PAGES table (`/equation` = one screenshot slug); e2e grep `Copy LaTeX|eq-coeff|EquationResult` → **0**; lucide (`package.json:35` devDependencies · `vite.config.ts:55` vendor-ui); banked identities re-read in place: fr-EquationView **D·D-B1/K-7/K-8/RD-3, D·D-M1, I-3, S-5, S-10, C·D-03 residue**, fr-ConvergencePlot **RD-3/K-10**, fr-AdminAuditLog **AA-2**, fr-EquationPanel **item 8 (:90)**, fr-CanvasOverlayButton **FR-COB-18/22**, fr-ConvergenceLegend **D-7 (seat-demoted MINOR)**, fr-EquationModeToggle **FR-EMT-22**.

## Scoreboard

**33 adjudicated defect rows (0 new BLOCKER — the one blocker-weight identity FOLDS → banked AA-2 · 11 MAJOR · 15 MINOR · 7 INFO) · 12 killed claims ratified/minted · 6 reader-missed finds adopted (all re-proven or corrected here) · 7 superlatives · 12 UNPROVEN-NEEDS-LIVE → SS-13.**

The file's single shape: **a 33-script-line component that consumes the right producer surfaces by name (`useClipboard`, `--z-controls`, the Button) and then discards each contract's substance — the copy result dropped, the confirmation field about to stop existing, the copied string unportable in the app's default mode, the geometry authored against a fine-pointer fixed-root mental model the tree does not supply.** No new blocker: the worst identity (the `variant`/`size="icon"`/`copied` uplift-break triple) is a banked census row extended by one symbol family, and the two genuinely new MAJORs are seam facts (the DEV diagnostic × console-gated e2e; the default-mode clipboard payload) that no axis and no bank had connected.

## Ruled disagreements (the adjudication's actual work)

| topic | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **The trust row's booking** | No confirmed trust row — kills C-2 and L-B1, folds the mechanism to banked I-3/RD-3 | CONFIRMS "L-B1/C-2 mechanism" as its own MAJOR row | **Reader-1 wins — FOLD, never re-book; MINOR residue** | fr-EquationView **I-3** already adjudicates the blanket-vs-handler question with the exact cure ("a trust HANDLER whitelisting `\htmlClass` + the class set"), and fr-ConvergencePlot **RD-3/K-10** already fixes the threat model at self-XSS/MINOR (sessionStorage ×4, re-read). Booking the mechanism again at MAJOR is the re-book the identity guard exists to stop. What is genuinely unbanked is the *vehicle* (FR-EQR-17) and the two consequences riding the same option object (FR-EQR-4, FR-EQR-19). |
| **L-M3/C-9 severity** (quadruplicated render) | MINOR ("the dedup increment") | MAJOR ("the single edit that lands I-3 + escapeHtml + M-1 + M-2 at one site") | **MINOR — the cure is adjudicated separately from the defect** | The ColorSpaceSelector addendum item 2 is law: a cure inherits no weight from the defects it would carry. The defect is duplication (4 sites, 3 byte-identical option objects, 3 divergent fallbacks — all re-read); its severity is MINOR by every banked dedup precedent. The vehicle status books as a ROUTING fact: F.W4's cures for FR-EQR-4/-15/-16/-19 MUST land through the one `renderLatex()` home, or four waves touch four sites. DISSENT-1. |
| **D-8/C-5 severity** (inert `h-4.5`) | MAJOR | MINOR (heavily banked class: GEO-2 · fr-EasingPicker · FR-COB-18 · fr-EquationPanel D-19+C-13) | **MINOR — reader-2 wins** | The rendered outcome is CORRECT (the token wins and paints `--ui-glyph` at the house 0.40 ratio); the harm is dead code plus a trap for a mechanical "fix" (`size-4.5` would pin 20.25px in a 67.5px coarse box). A defect whose live paint is right and whose danger is a hypothetical wrong cure is MINOR; D-8's "delete, do not rename" travels as the cure-constraint. |
| **D-10 severity** (stale-copy window) | MAJOR | not filed | **MINOR — this seat demotes** | My read of `EquationView.vue:237-242`: the `Recomputing…` banner is a SIBLING in the same `v-else-if="result"` block — the staleness IS communicated at card level, visibly, during the whole window. The leaf's residue is real (no `disabled`, no `aria-busy` — the announcement never reaches AT) but bounded by recompute latency and cured by one prop at the pin. Reader-1's MAJOR preserved in DISSENT-2; escalates if SS-13 shows habitual copy-during-recompute. |
| **L-B1's kill shape** (CSP) | "deployment-conditional — NEITHER axis fully right"; owner ruling needed | "premise false for the deploy-of-record" (_headers:4-10) | **Kill stands in BOTH forms; reader-1's statement is the honest residue** | `_headers:1-11` (my read): CF-Pages is named deploy-of-record and the docker nginx governs the SEPARATE `api.fourier.babb.dev` origin — reader-2's premise-kill is sound for the target of record. But `playwright.config.ts` marks prod "currently GH-Pages 404 … RED-with-cause" and keeps a `host` cell at `:8100` — the shipping target is genuinely unsettled, so the honest terminal statement is deployment-conditional + the owner ruling already filed in unproven. BLOCKER dies either way (banked RD-3/K-10 fixes the surviving threat at MINOR). |
| **D-9 arithmetic** | 2.10 / 2.17 / 6.56 / 8.51 (exact) | ≈2.08 / ≈6.4 ("within rounding") | **Reader-1's figures are the figures of record** | This seat's third derivation: **2.101 / 2.175 / 6.559 / 8.510** — reader-1 and the D axis to three decimals; reader-2's pipeline is loose (same side of every threshold, so no verdict changes, but the record keeps the exact cells). Method note, recorded against this seat too: my own first run double-linearized (Ottosson's matrices emit LINEAR sRGB; gamma-decoding them again gave 3.76) — caught only because the banked figures existed to calibrate against. Independent recomputation without a calibration target is not an instrument. |
| **Reader-1 missed-4's dating** ("three 8.0.0 deltas") | files `CopyFailureReason` narrowing + `timeoutMs` as 8.0.0 deltas vs the 4.0.0 contract | (not contested) | **RESCOPED — two of three legs mis-dated; K-12** | `git show v7.0.0:src/composables/dom/useClipboard.ts` (my run): `CopyFailureReason = "clipboard-api" | "no-api"` at `:5` and `timeoutMs` present (3 hits) **at v7.0.0 already**. The consumer consequence (the exec-command fallback is gone at ANY uplift target, so the unreported-failure rate rises exactly where FR-EQR-5 says nothing is reported) survives untouched; the "8.0.0 delta" attribution does not. The version fact itself folds → FR-EMT-22. |
| **C-10** (inert `@types/katex`) | KILLS the provenance — `tsconfig.app.json` does not exist | CONFIRMS at INFO — **citing `tsconfig.app.json:5` again** | **Row survives at INFO re-pinned to `tsconfig.json:5`; a citation-inheritance event is MINTED** | My ls: `web/tsconfig.json` + `tsconfig.tsbuildinfo` only. Reader-2 repeated the phantom file in its own confirmed row's proof cell — inheriting C's citation rather than re-deriving it — while reader-1 killed the same cell. Recorded in DISSENT-4 as evidence about the fleet (the ColorSpaceSelector DISSENT-1 class, second occurrence in this program). |
| **Reader-1 missed-1's count** (console-error gates) | "three green specs go RED" | (not contested) | **ADOPTED and SHARPENED to FOUR** | My census: `gallery.spec.ts:118-135` is a fourth zero-console-error gate (filters favicon/404/ERR_CONNECTION_REFUSED/429 — no `[glass-ui]` carve) on a route dense with `variant=` Buttons. And `scripts/e2e.sh:102` launches the DEV server, so `import.meta.env.DEV` is true under the whole e2e matrix — the branch is live exactly where the gates are. |

## Adjudicated defect roster

Line anchors are working-tree at HEAD `cd26c65` + the in-flight M-bump dirty set; re-resolve before any cure (D-19). Identity guard applied throughout: banked identities are FOLDED by reference, never re-booked.

**MAJOR**

- **FR-EQR-1 = D-3(iii) — MAJOR (new; the one unbanked leg of the uplift triple).** `const { copied, copy } = useClipboard({ resetMs: 2000 })` (`:15`) destructures a field the producer deleted: installed 4.0.0 d.ts returns `{ copied: Ref<boolean>; copy }` (my whole-file read); producer at v7.0.0 AND 8.0.0 HEAD returns `{ status: ComputedRef<ClipboardStatus>; copy; invalidate }` — `copied` grep → 0 (my `git show`). `v-if="copied"` (`:46`) goes permanently false at the uplift: the confirmation state silently disappears, no type error under this repo's config (no `strictTemplates` — my grep), no runtime warning. Three consumers, all destructure `copied` (my grep): `EquationResult.vue:15` · `UserSlugBar.vue:23` · `useMorphConfig.ts:58`. Registry-swept by both readers, ratified here: unbanked — extends AA-2's uplift budget by a third symbol family. **→ F.W1** (the `copied` → `status` triple migrates in the same change as the bump).
- **FR-EQR-2 = D-3(i)(ii) — FOLD → fr-AdminAuditLog AA-2 (ADJUDICATED BLOCKER census-extension, banked; never re-booked). Instance MAJOR.** `variant="glass"` + `size="icon"` (`:39-40`) are definition-absent at producer v7 and v8 (`ButtonProps` = emphasis/tone/size/iconOnly/loading — reader reads, ratified by the producer's own retired-prop diagnostic naming both); my dirty-tree counts reproduce the banked instrument exactly (124 / 38 / 21 files). fr-EquationView's fold instruction already names `EquationResult:39-40` among its nine prop sites. The half-visibility consequence rides K-6: `variant` fails silently (no `strictTemplates`), `size="icon"` is a checked prop-value — only the SMALLER half of the 162-attribute surface is compiler-visible. **→ F.W1** (rides AA-2).
- **FR-EQR-3 = reader-1 missed-1, SHARPENED — MAJOR (new consequence folding onto AA-2, not a re-book).** The producer's DEV-mode retired-prop watcher (`Button.vue:131-146` at `558c3fa3`, read verbatim: `console.error("[glass-ui] <Button> was given …")`, fired per mounted Button with `variant` in attrs or `size==="icon"`) meets **four** e2e gates asserting zero console errors (census above) whose filter lists pass `[glass-ui]` through, on a matrix that runs against the DEV server (`e2e.sh:102`, my read). `EquationResult.vue:39-40` is a two-prop instance emitting the fully-populated "X and Y" form. Consequence: the F.W1 bump and the 162-site prop rewrite must land in the SAME change, or four green specs go red with the redness attributed to the wrong cause. **→ F.W1** (sequencing constraint on AA-2's atomic cure budget).
- **FR-EQR-4 = reader-2 missed M-1 — MAJOR (new).** The component's sole affordance emits invalid LaTeX in the app's default mode: `eqMode` defaults `"sigma"` (`EquationView.vue:43`), `activeLatex` prefers the sigma form (`:49-51`), and `copyLatex()` (`:30-32`) copies it raw — a string embedding `\htmlClass{eq-coeff …}`, which only KaTeX-with-trust consumes (`Undefined control sequence` everywhere else). The asymmetry that makes it a defect, re-verified by this seat's census: all four `\htmlClass` emission sites live in the `*_sigma` renderers (`latex_rendering.py:175/:176/:216/:248`); the expanded renderers are clean — the app copies portable LaTeX in the non-default mode and unportable LaTeX in the default one. Registry-swept: unbanked. **→ F.W4** (a `plainLatex()` strip beside the FR-EQR-17 singleton) **or F.W5** (the hooks as a separate wire field — filed beside R6-8; the forming spec chooses the seam).
- **FR-EQR-5 = D-6 / L-M2 / C-1 — MAJOR.** The copy outcome is colour+glyph only and the composable's reported failure is re-swallowed: `copy(props.latex);` (`:31`) — no await/then/void; only `{ resetMs: 2000 }` passed; the installed d.ts's `onCopyError` ("Surfaces the failure instead of swallowing it" — my read) declined; no `aria-live`/`sr-only` anywhere in the file. Systemic ×3 (all three callsites discard — my grep). Aggravated at the uplift target, correctly dated by this seat (K-12): the exec-command fallback is gone at ≥7.0.0, so a failure is MORE likely and still unreported, and `timeoutMs` (≥7.0.0) adds a hang mode the callsite would also discard. Cure in hand at the pin: `./toast` + `./icon-tooltip` live in the 80-key map (my read). **→ F.W4** (one shared `copyOrToast`, not three).
- **FR-EQR-6 = D-5 / L-m5 / C-8 — MAJOR.** `.eq-scroll-region` (`:37`) is a scroll container with no `tabindex`, no `role`, no accessible name — zero `aria|role|tabindex` tokens in the file (my whole read); KaTeX emits span trees, so no focusable descendant exists. Test net: `/equation` appears in exactly ONE spec as an assertion-free screenshot slug (`visual-baseline.spec.ts:35`, my read); the sole axe seat visits `/visualize` only. Cohort: fr-CoefficientsSpectrum M-6 same class. **→ F.W4** (tabindex + `aria-label` on the region) **+ F.W9/W10** (an axe/behavioural seat on `/equation`).
- **FR-EQR-7 = D-9 — MAJOR (instance) + producer carry.** `text-green-500` on the success glyph vs light `--card`: **2.101** (this seat's third derivation; three pipelines now agree to three decimals) — under the 3:1 WCAG 1.4.11 floor; the design system's own light-arm `--success` ALSO fails at **2.175**, so the cure is a darker light-arm success rung or a non-colour confirmation (which FR-EQR-5 independently demands). Dark arms pass (6.559 / 8.510). Registry-swept: `green-500` unbanked. **→ F.W4** (instance) **+ GLASS-RELAY carry** (the light-arm `--success` rung is producer-owned — rides the standing glass-ui BH relay per owner edict; NO-WAVE-OWNER inside fourier's F-lattice).
- **FR-EQR-8 = D-1 — MAJOR (mechanism; attribution and one-declaration cure KILLED — K-7).** A centred, `white-space: nowrap` display box inside `overflow-x: auto` puts the inline-start overhang outside the scrollable overflow region (CSS Overflow §3; `scrollLeft` floors at 0) — the leading terms of an over-wide equation are unreachable with zero cue. Operative centring is the VENDOR sheet (`.katex-display{…text-align:center}` + `.katex-display>.katex{display:block;text-align:center;white-space:nowrap}` — my verbatim extraction), which the component's `:deep` override (`:71-75`) does not touch; deleting `:63` alone is a no-op. Cure of record: make `.eq-scroll-region` a flex container with `justify-content: safe center` (KaTeX's internal centring goes inert on a shrink-to-fit flex item; `safe` degrades to start on overflow) + mind `align-items: stretch` vs the `min-height: 4.5rem` floor. Overflow onset → SS-13, riding the banked fr-EquationPanel item-8 witness (one witness, not two). **→ F.W4.**
- **FR-EQR-9 = D-2 / L §7 H-3 / D-17(geometry) — FOLD → fr-EquationView D·D-B1 (banked; BLOCKER→MAJOR via K-8; fine arm corrected via RD-3).** Coarse-pointer 16px overlap (60px controls; copy 8→68, info 52→112), info z-30 over copy z-20, deterministic because BOTH controls consume band tokens; 44px retained = the touch floor (the K-8 demotion). All numbers re-derived twice by the readers and standing in the bank; nothing new books here. The cure is banked verbatim: "delete the constant; one flex row `top-2 right-2 gap-2`, one owner". **→ F.W4** (rides D·D-B1).
- **FR-EQR-10 = D-4 / L-M5 — FOLD → fr-EquationView D·D-M1 (banked MAJOR).** The `:60` "no vertical clip" comment is false: `.eq-card { height: 10rem; overflow: hidden }` (`EquationView.vue:386-390`, my read) is the real clip; the region's computed `overflow-y` is `auto` (Overflow §3.4 fixup); descendants' `overflow: visible !important` cannot undo an ancestor scrollport. Verbatim the banked row, plus unremediated prior art B4-01. Truncation threshold → SS-13 (banked there). **→ F.W4** (rides D·D-M1; the comment corrects in the same edit).
- **FR-EQR-11 = D-7 — FOLD → fr-EquationView D·D-B1 (vertical-incursion limb: 16px fine / 36px coarse, banked figures) — MAJOR-fold.** `padding: 2rem 1rem 1rem` (`:64`) reserves neither the 48px (fine) nor 68px (coarse) the corner-anchored `.copy-pos` occupies; the control does not scroll with content. The one increment: the padding-reservation cure (`calc(0.5rem + var(--control-h-md) + 0.5rem)`) is the only prescription in the corpus that scales with the token — it rides D·D-B1's one-flex-row cure or replaces it. Glyph-occlusion px figures stay CAPPED by banked K-7 (expression-width-dependent) → SS-13. **→ F.W4.**

**MINOR**

- **FR-EQR-12 = reader-1 missed-2 — MINOR (new; adopted).** Symmetric 16px inline padding + symmetric centring inside grossly asymmetric chrome: left = `.eq-mode-anchor` at `left-2` hosting the ~100px two-button pill (`EquationModeToggle` buttons `padding: 0 0.6rem; height: 2.25rem` — my re-read of the dirty file), right = 48/68px copy button — the geometric centre is displaced from the free band's optical centre, opposite the wider cluster. Registry-swept (D·D-B1 books the right edge + toggle height; FR-EMT-13 the height delta; K-7 the cap): the LEFT-edge footprint and centring asymmetry are the unbanked increment — exactly the leg D-7's top/right table omits. **→ F.W4** (closes free if D·D-B1's one-flex-row cure spans both edges); painted witness → SS-13.
- **FR-EQR-13 = D-10 — MINOR (demoted; ruling above).** No loading/staleness treatment on the leaf: no `disabled`, no `aria-busy`; the copy affordance stays live over a stale equation during recompute, and the card-level `Recomputing…` banner never reaches AT. `disabled` closes it at the pin; 8.0.0's `loading` (aria-busy + activation guard, reader-read, producer-confirmed by this seat's Button read) closes it better at F.W1. **→ F.W4** (`:disabled` today) with the F.W1 upgrade noted.
- **FR-EQR-14 = D-8 / C-5 — MINOR (ruled above); FOLD → fr-CanvasOverlayButton FR-COB-18 (glyph-register sweep) + fr-EquationPanel D-19+C-13.** `h-4.5 w-4.5` (`:46-47`) is inert against the cva's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` guard at (0,2,1); the cure-constraint is D-8's own: **delete, do not rename** — `size-4.5` would pin 20.25px in a 67.5px coarse box (0.30 vs the house 0.40). **→ F.W3/W4** (rides the FR-COB-18 sweep). Composited size → SS-13 (the shipped dist sheet is stale and cannot corroborate — C-5's honest cell, preserved).
- **FR-EQR-15 = the trust posture — FOLD → fr-EquationView I-3 (cure-constraint) + fr-ConvergencePlot RD-3/K-10 (threat model, MINOR) — MINOR.** `trust: true` (`:23`) is LOAD-BEARING (four backend `\htmlClass` emission sites, my census — C-2's delete-cure killed at K-3) and blanket where a predicate suffices; the banked cure is the handler whitelisting `\htmlClass` (+ L's byte-identity probe). Books here only as the instance the FR-EQR-17 singleton must carry. **→ F.W4** (via the singleton; binds the C·D-03-residue cure per I-3).
- **FR-EQR-16 = L-M1 / C-3 — FOLD → fr-EquationView C·D-03 residue (banked MINOR; both axes' MAJOR grades exceed the bank and are corrected).** The `catch` interpolates `props.latex` unescaped into the same `v-html` sink (`:25-27` → `:37`); reachable (non-ParseError rethrow; unvalidated `JSON.parse` rehydrate — `useEquationCache.ts:28/:39`, my read); threat = self-XSS (sessionStorage ×4). **→ F.W4** (`escapeHtml` in both catches, via the singleton).
- **FR-EQR-17 = L-M3 / C-9 — MINOR (defect) + DESIGNATED CURE VEHICLE (routing fact; ruling above).** Four render sites, three byte-identical option triples (subject `:20-24` · EquationPanel `:28-32` · useCoeffHover `:99-101` — all re-read), a fourth omitting `trust` (ConvergencePlot `:255-258`, re-read), three divergent fallbacks (`<code>` / `text-red-400` span / `""`), and `web/src/lib/equation/` exists owning none of it. Unbanked (both readers' registry sweeps, ratified). The one `renderLatex()` home carries: the I-3 trust handler (FR-EQR-15), `escapeHtml` (FR-EQR-16), the strict silencer (FR-EQR-19), and `plainLatex()` (FR-EQR-4). Prior art: the 2026-05-19 refinement assay recommended exactly this. **→ F.W4** (the four rows above MUST route through it).
- **FR-EQR-18 = L-M4 / D-17(contract half) — MINOR; FOLD → fr-EquationView I-3 + S-5 (the "declares nothing" framing killed by reader-2, ratified — the producing side has the docstring).** The `v-html` subtree is a four-file two-language contract (`latex_rendering.py` mints · `EquationView.vue:414-423` styles · `useCoeffHover.ts:29` hit-tests) that the DOM-owning component never names. Residue = one comment at `:23` naming `\htmlClass` load-bearing. **→ F.W4** (the comment lands with the singleton).
- **FR-EQR-19 = reader-2 missed M-2 — MINOR (new; CONFIRMED BY EXECUTION, this seat).** KaTeX `strict` unset defaults `"warn"` (schema enum-first, no default key — my read of the settings machinery) and the `\html*` handler calls `reportNonstrict("htmlExtension", …)` unconditionally BEFORE the trust check (`katex.mjs:11199`) — so the app's own deliberate feature logs `console.warn` per occurrence per render in the default mode. **Witnessed live in node by this seat**: the warn fires with `trust: true` and `eq-coeff` intact; `strict: (code) => code === "htmlExtension" ? "ignore" : "warn"` silences it with output intact. L saw these warns on its own probe run and suppressed them without filing (reader-2's catch, preserved). Note: warns do NOT trip the four console-ERROR e2e gates (msg.type() === "error" — my reads) — FR-EQR-3 and this row are different channels. **→ F.W4** (one property on the singleton's option object).
- **FR-EQR-20 = D-11 / D-12 — MINOR.** The authored +28.6% type ramp (`1.4em`→`1.8em` at 768px, `:78/:84`) delivers +14.3% because the root font drops 18px→16px at the identical breakpoint (`style.css:41/:46-49`, my read); no intermediate font-size intervenes (reader-1's gap-closure, ratified); the chrome-to-ink proportion inverts ~29% across the breakpoint. **→ F.W4.**
- **FR-EQR-21 = D-14 / L-m6 — MINOR (reader-1's sharpening adopted).** `:97` = 0.15s / raw `ease` / exit `scale(0.8)` vs AnimationControls `:196-197` (0.15s / `var(--ease-standard)` / 0.7, under the live A.W3.d ratifying comment — my re-read) and ConvergenceTimeline `:140-143` (0.1s / raw ease / 0.7): this file is the SOLE exit-scale outlier against two agreeing siblings, and one of three durations for one transition name. Prior art E5-11 names `:97`, still unremediated. **→ F.W3/W4** (the motion-vocabulary sweep).
- **FR-EQR-22 = D-15 — MINOR.** The `!important` at `:74` is unearned — the scoped `(0,3,0)` selector beats both competitors unaided (`style.css:64-69` at (0,1,0); vendor sheet sets NO overflow — my extraction); `:79`'s `overflow: visible` restates the initial value. **→ F.W4** (sweep; the `:70-75` block otherwise keeps its superlative).
- **FR-EQR-23 = D-16 — MINOR; cure FOLDS → fr-ConvergenceLegend D-7 (banked, seat-demoted MINOR there — grades now consistent).** `scrollbar-width: thin` is the entire overflow affordance on a centred region that (per FR-EQR-8) overhangs BOTH edges with zero cue; `./fading-scroll` is live at the installed pin (my exports read + dist ls). **→ F.W4.**
- **FR-EQR-24 = L-m2 / C-4 — MINOR; the nick is banked (fr-EquationView S-10).** Root-barrel `useClipboard` import (`:4`) one line below a subpath import (`:3`); `./dom` live in the 80-key map (my read); prior art A5-05 names all three sites; C-4's bundle-cost downgrade sound (`sideEffects` + vendor-ui chunk). **→ F.W0/W1** (rides the subpath sweep).
- **FR-EQR-25 = D-22 / L-m1 / C-12-as-narrowed — MINOR.** The click path is unguarded where the render path short-circuits (`:18` vs `:30-32`, no `:disabled`): `copy("")` flashes a green Check for a copy of nothing. Reachability is the NARROW rehydrate case only (C-12's first-load scenario is killed — K-2); the four-character guard exists in-tree (`UserSlugBar:67-70`). **→ F.W4** (`:disabled="!latex"` — one binding, also FR-EQR-13's arm).
- **FR-EQR-26 = L-m4 / C-13 — MINOR.** The icon-only Button is named by `title` alone (`:42`) — last-resort accname source, unreachable on touch/keyboard — against the same-repo, same-primitive sibling pattern (`EquationPanel.vue:87` `aria-label`, my read); the `copied` flip is unannounced (rides FR-EQR-5's live-region cure). `./icon-tooltip` live at the pin. **→ F.W4.**

**INFO**

- **FR-EQR-27 = D-18 / C-15 + reader-1 missed-3 (polarity correction ADOPTED) — INFO.** `resetMs: 2000` is producer-SANCTIONED (the installed d.ts's own second worked example, verbatim: "Custom reset window (e.g. for paired toast feedback): `useClipboard({ resetMs: 2000 })`" — my read) and this file is the repo MAJORITY (2000/2000/1500 — my grep): `UserSlugBar` is the lone deviant, not this file. D-18/C-15's polarity inverted as filed; genuine residue = untokenised value. **→ F.W4** (normalise UP to 2000; the sweep moves UserSlugBar).
- **FR-EQR-28 = D-19 / L-m3 — INFO.** Dead `@reference "tailwindcss"` (`:54`): the scoped block is plain CSS + two `:deep()` (my read). Cohort census REPRODUCED name-for-name by this seat: **9 of 35**, D-19's nine exactly. FOLDS → fr-CoefficientsSpectrum i-5(d) + fr-EditorControlsDock C-23 as the sweep's banked precedent. **→ F.W4** (cohort sweep, one row).
- **FR-EQR-29 = D-21 / C-11 — INFO; FOLD → FR-AH-33 · fr-BasisSelector m-9 · fr-CollapsibleSection F-2 · FR-AFP-54 (banked ×4).** `lucide-vue-next` devDependency (`package.json:35`, unchanged by the in-flight bump — my read) imported by runtime source (`:5`) and named in the production `vendor-ui` chunk (`vite.config.ts:55`). **→ F.W0** (manifest hygiene; natural moment = the `@lucide/vue` rename row).
- **FR-EQR-30 = C-10 — INFO (re-pinned by this seat; ruling above).** `@types/katex@^0.16.8` inert against self-declaring `katex@0.17.0` under `tsconfig.json:5` `moduleResolution: "bundler"` — the FACT survives; every `tsconfig.app.json` cite is dead (no such file — my ls). **→ F.W0** (delete the dead types row; F.W0 consumes THIS pin, not C's).
- **FR-EQR-31 = L-i3 / C-14 / D-20 — INFO (scope law: the repo-wide coverage gap is the census carry; this file's share books here).** Zero behavioural coverage by construction: no vitest anywhere, `/equation` = one screenshot, and the trust config / fallback branch / discarded promise / `.eq-coeff` contract are all unguarded (my e2e grep → 0). D-20's archaeology-comment half rides FR-EQR-27's correction (the "why 2000ms" answer sits ten lines below the default in the consumed d.ts). **→ F.W9/W10.**
- **FR-EQR-32 = C-§0.4 — INFO (adopted; reader-2's chain re-walked at the endpoints).** The operation record for `/api/equations/{compute,simplify}` carries no sink class for its string-typed response fields — a client-side sink change (the trust flip) is invisible to operation-side conformance. Non-overlap with R6-8's C31 stated and correct. **→ F.W5** (rides R6-8 as the second seam instance).
- **FR-EQR-33 = substrate — INFO; PARTIAL FOLD → FR-EMT-22 (producer 8.0.0, banked) + this seat's additions.** (a) Producer HEAD has moved AGAIN (`558c3fa3`; both readers pinned `4a86570b`; version/tag still 8.0.0) — the fifth-plus occurrence of declared-pin drift; F.W1 re-derives at the ADOPTED pin, whatever it is by then. (b) The `CopyFailureReason` narrowing + `timeoutMs` are **v7.0.0 facts** (K-12), so they bind at ANY ≥7 target. (c) NEW, this seat: the 4.0.0 "installed pin" the whole corpus audits is itself an UNCOMMITTED row of the in-flight `m/w1-bump-migration` dirty set (committed manifest = `^3.1.0`) — the branch must land-or-lose it, else every "at the pin" statement in this corpus describes a tree that never existed on any commit. **→ F.W0/F.W1** (land the bump; re-pin at adoption).

## Killed-claims register (all ratified or minted by this seat's own instruments)

1. **K-1 · D-13 + C-7 (PRM leg) — KILLED, ratified at the bytes.** The installed 4.0.0 ships the near-universal PRM reset at `dist/styles/utilities/a11y-overrides.css:6-16` (read verbatim this session): `transition-property` forced to an opacity/color/backgrounds/box-shadow allowlist EXCLUDING `transform` — under PRM the `scale(0.8)` snaps and the opacity fade shortens to 0.1s, precisely the behaviour both axes said was missing. Delivery chain live (`style.css:3` → `./styles` → `utilities.css` import). D cited the file's line 6 and stopped one declaration short; C's grep missed the depth-2 partial. The token-vocabulary half survives as FR-EQR-21.
2. **K-2 · C-12's first-load scenario — KILLED, ratified.** `EquationResult` mounts only inside `<template v-else-if="result">` (`EquationView.vue:237`, my read); on a true first load `result` is null and the `:221` spinner branch renders — no empty box, no copy button, no clipboard destruction. C-12's falsifier read the wrong nesting level. The surviving narrow rehydrate path books at FR-EQR-25.
3. **K-3 · C-2's grep and delete-`trust` cure — KILLED, ratified.** C's over-escaped grep returned a false zero; the correct grep returns **6 hits / 4 emission sites** (my run: `:159/:174/:175/:176/:216/:248`), all in the sigma renderers the DEFAULT mode displays. `trust: false` silently kills the coefficient-hover feature two files away — the exact severance banked I-3 exists to prevent. The narrow-predicate handler (L's probe C, byte-identical output) is the cure of record.
4. **K-4 · L-B1's "no CSP" premise and BLOCKER grade — KILLED in both legs, ratified (ruling above).** `web/public/_headers:64` ships the full policy (read verbatim); the file's own rationale block names CF-Pages the deploy-of-record and the nginx a separate API origin. The surviving `javascript:` href leg survives for C's `'unsafe-inline'` reason, at the banked RD-3/K-10 severity (MINOR, self-XSS). Deployment-target ambiguity → unproven (owner ruling).
5. **K-5 · L §7 H-3's self-falsification — ITSELF FALSIFIED, ratified.** L computed the copy/info geometry at the fine pointer only and never applied the coarse block it had read: at `pointer: coarse`, `--control-h-md` = 60px → 16px overlap, z-30 info over z-20 copy — the banked D·D-B1 numbers (re-read in place this session). A falsification must re-derive under every media condition the library declares. Books at FR-EQR-9.
6. **K-6 · D-3's build-gate rebuttal (`variant` leg) — KILLED, ratified.** No `strictTemplates` anywhere (`web/tsconfig*` = one 20-line file; my ls + grep → 0): `variant="glass"` falls through SILENTLY at build AND runtime — the exact mechanism D-3 dismissed. Banked precedent FR-EMT-8/FR-EMT's K-6 class. The `size="icon"` leg survives (checked prop value). Consequence booked inside FR-EQR-2.
7. **K-7 · D-1's "KISS, one declaration" cure — KILLED, ratified.** Deleting `:63` cures nothing: the vendor sheet still centres both boxes (my verbatim extraction; the `:deep` override resets margin/padding/overflow, NOT text-align). Only the flex + `safe center` half works, by a mechanism D-1 never states. Any wave applying the one-declaration half ships a no-op and closes the row. Cure of record in FR-EQR-8.
8. **K-8 · C-10's provenance — KILLED and re-pinned; a citation-inheritance event MINTED.** `tsconfig.app.json` does not exist (my ls). Reader-1 killed it; reader-2 REPEATED it in its own confirmed C-10 proof cell — inheritance, not derivation (DISSENT-4). The fact survives at FR-EQR-30 pinned to `tsconfig.json:5`.
9. **K-9 · L-H1's "the 7.0.0 break surface for this file is empty" — KILLED, ratified.** L tested specifier resolution, not contract survival: three definition-absences live in the 101 lines (`variant`, `size="icon"`, `copied` — all re-verified at v7 by this seat's git show). A false negative filed as a self-correction receipt is worse than a plain miss.
10. **K-10 · C's whole-document provenance — KILLED, ratified at the bytes.** Substrate id wrong (`c654824e` vs the real `cd26c65`) and every script/style anchor displaced ~2 lines against the live file (my whole read: useClipboard **:15**, catch **:25-27**, copyLatex **:30-32**, title **:42**, scroll region **:61-68**…). C's claims mostly true; its addresses unverifiable-as-written. F.W4 re-pins from D/L/this record, never from C.
11. **K-11 · D-2's falsifier (b) ("`.eq-result-root` is a static … block child") — KILLED, ratified.** `:56-58` reads `position: relative` (my read): `.copy-pos` resolves against `.eq-result-root`, `.info-anchor` against `.eq-card` — the arithmetic survives (unpadded full-width chain) but the stated mechanism is wrong in the one finding graded BLOCKER on geometric proof. Grade already banked down (K-8 → MAJOR).
12. **K-12 · reader-1 missed-4's "8.0.0 deltas" (legs a) — KILLED AS DATED, minted by this seat.** `git show v7.0.0:src/composables/dom/useClipboard.ts`: the 2-member `CopyFailureReason` AND `timeoutMs` are already there. The consumer consequences survive (booked in FR-EQR-5/FR-EQR-33); the delta attribution does not. Precision law: a substrate correction must itself be pinned to the tag it names.

## Superlatives (provenance attached; L-18 runs both ways)

1. **D-9's contrast table** — now a FOUR-way convergence (D · reader-1 · reader-2-loosely · this seat: 2.101/2.175/6.559/8.510), each derivation from nothing but the cited token literals. The framing is the rarer virtue: proving the obvious cure (`--success`) also fails converts a consumer nit into a producer indictment. And the method lesson cuts at this seat too: my own first run double-linearized and was caught only by calibration — a refutable number needs a published predecessor to be refutable against.
2. **Reader-2's M-1 (the clipboard payload)** — the corpus's best single find: three axes audited the render consumer of `\htmlClass` exhaustively and NOBODY followed the same string down the copy path, where it turns the component's one job inside out in the shipped default mode. Registry-swept, wire-walked end to end, and the asymmetry (expanded clean, sigma dirty) is what elevates it from wire fact to defect.
3. **L's §7 falsified-hypotheses ledger** — the correct FORM even carrying two wrong entries (H-3 killed at K-5, H-1 killed at K-9): publishing the kill evidence is what let both readers and this seat find the errors in a single read. Propagates with the amendment: falsifications re-derive under every declared media condition, and an "empty break surface" claim is a contract-survival claim, not a resolution claim.
4. **The `useClipboard` migration + provenance comment (`:12-15`)** — FOLD → banked fr-EquationView S-10 ("the single place the pattern ran the right direction"), now narrowed FOUR ways by this corpus: the destructured field is the one the uplift deletes (FR-EQR-1), the import is the root barrel (FR-EQR-24), the comment records the adopted option but not the declined one (`onCopyError`, six lines above `resetMs` in the same d.ts — FR-EQR-5), and the 2000ms it chose is producer-sanctioned all along (FR-EQR-27). C-5's standing practice adopted: migration comments record what was DECLINED.
5. **The `:deep(.katex-display)` block (`:70-75`)** — a three-sheet cascade collision (app sheet × vendor sheet × scoped block) found, defeated at the narrowest possible scope, with the reason written down. Narrowed twice and honestly by the corpus itself: the `!important` is unearned (FR-EQR-22) and the sweep stopped one declaration short of `text-align` — the declaration that causes FR-EQR-8.
6. **Delegation over the wholesale-replaced subtree + the load-bearing `computed`** (reader-2, re-verified): `useCoeffHover` hit-tests with one `closest(".eq-coeff")` storing a string kind, never a node — nothing survives a `v-html` swap to leak; and `:17`'s computed keeps the KaTeX parse off the parent's `mousemove`-driven re-render path (`EquationView.vue:252`, my read). Zero teardown surface in 33 script lines — the one delegated timer is producer-disposed.
7. **`z-index: var(--z-controls)` (`:90`)** — real cross-repo token consumption whose diagnostic value the D axis itself named: because BOTH colliding controls consume band tokens, the D·D-B1 overlap is deterministic and was provable from source without a browser. Correct token, uncoordinated band.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **FR-EQR-8**: overflow onset — whether/when the rendered equation exceeds the region's inline size, and `scrollLeft`-at-0 vs the `.katex-display` left edge. RIDES the banked fr-EquationPanel item-8 witness (one witness, not two).
2. **FR-EQR-9**: painted coarse+fine witness of the copy/info overlap and which control takes the tap in the 16-18px band — already the D·D-B1 deferred item.
3. **FR-EQR-10**: the vertical truncation threshold inside the 96px usable budget — banked at D·D-M1.
4. **FR-EQR-11**: px-of-glyphs behind the control band — CAPPED by banked K-7 (expression-width-dependent).
5. **FR-EQR-14**: `getComputedStyle(svg)` on the rendered Check/Copy (the shipped dist sheet is stale, Jun-12-era, `ui-glyph` grep → 0 — cannot corroborate statically).
6. **FR-EQR-7**: composited contrast of the Check through the `glass-wash` plate — `--card` is the floor, so 2.101 is the ceiling case; the painted ratio can only be worse.
7. **FR-EQR-12**: rendered boxes of the toggle pill and copy button against the centred equation's inline extent, fine + coarse (the ~100px toggle width is derived, not measured).
8. **L-i1/L-i2**: the `v-html` parse/layout cost of the 58kB / 1336-span expanded form on the reactive-flush path beside ConvergencePlot's rAF.
9. **FR-EQR-1/-2**: the exact `vue-tsc` diagnostic enumeration at the adopted pin (fr-EquationView open item 9: a scratch typecheck at F.W1, not a browser).
10. **K-4 residue**: WHICH deployment target is authoritative for the CSP question — `_headers` (CF-Pages) vs `nginx/fourier.conf` (the host cell) vs the RED prod cell. An OWNER RULING, not a probe, closes this.
11. **FR-EQR-4 residue**: whether `@mkbabb/latex-paper` (the paper route) strips `\htmlClass` downstream — outside this seat's closure; narrows blast radius to the clipboard alone if so.
12. **FR-EQR-3 witness**: one dev-server run of the four gated specs at the adopted pin, before-and-with the prop rewrite — the born-RED demonstration the F.W1 spec should paste.

## DISSENT — preserved, not ruled away

1. **Reader-2's MAJOR on the render singleton (FR-EQR-17).** Its position: the vehicle that carries four adjudicated cures outranks a dedup MINOR. Ruled down under the cure-is-not-the-defect law — but if the forming F.W4 spec fails to route FR-EQR-4/-15/-16/-19 through one home and instead patches four sites, reader-2's severity was the truer reading and the row escalates at that wave's open.
2. **Reader-1's MAJOR on the stale-copy window (FR-EQR-13).** Demoted on the banner-communicates rationale; preserved because the banner is visual-only. If SS-13 shows recompute latencies long enough that copy-during-recompute is habitual, or an AT transcript shows zero staleness signal, the MAJOR revives.
3. **Reader-1's MAJOR on the inert glyph classes (FR-EQR-14)** vs this seat's MINOR — preserved; reader-1's grade priced the mechanical-sweep hazard, which the cure-constraint now carries instead.
4. **The citation-inheritance event (K-8).** Reader-2's C-10 cell repeated a phantom file reader-1 had killed in the same corpus — the second recorded occurrence of the class this program first logged at the ColorSpaceSelector DISSENT-1. Evidence about the fleet, not the tree: confirmation cells inherit citations unless the seat re-runs the ls.
5. **The two readers' L-B1 kill shapes** — reader-2's premise-kill (deploy-of-record) vs reader-1's deployment-conditional narrowing. Ruled for reader-1's statement as the honest residue; if the owner rules CF-Pages the sole shipping target, reader-2's cleaner form was right all along.
6. **This seat's own recorded instrument error** — the double-linearization in the first contrast run (superlative 1). Kept in the record because the correction path (calibrate against banked figures before trusting a new instrument) is the method the next seat should inherit.

## Closing verdict

**DEFECTIVE — ADJUDICATED at 0 new BLOCKER · 11 MAJOR · 15 MINOR · 7 INFO, with 12 killed claims and 6 adopted misses, all re-derived; the blocker-weight identity folds to banked AA-2 rather than re-booking.** The terminal dispositions concentrate on four seams: **F.W1** — the bump, the 162-site prop rewrite, and the `copied`→`status` triple land as ONE change or four green console-gates go red for the wrong reason (FR-EQR-1/-2/-3, with FR-EQR-33's land-or-lose substrate note); **F.W4** — one `renderLatex()` singleton carries the trust handler, the escape, the strict silencer, and `plainLatex()` so the component's sole affordance stops handing out KaTeX-only LaTeX in the default mode (FR-EQR-4/-15/-16/-17/-19), while the geometry rows ride the banked D·D-B1/D·D-M1 cures and the a11y quartet (FR-EQR-5/-6/-25/-26) closes on primitives live at the installed pin; **F.W5** — the sink-class seam rides R6-8 (FR-EQR-32, with FR-EQR-4's wire-field alternative); **F.W9/W10** — `/equation` gets its first assertion. One producer carry leaves the repo entirely: the light-arm `--success` rung fails its own card at 2.175:1 and rides the standing glass-ui BH relay. Everything else is census truth, and the census's deepest lesson this round is about the auditors: two kills died of unread files, one confirmation inherited a phantom citation, one self-correction was itself wrong, and this seat's own first instrument mismeasured — every one caught only because somebody else's exact number was already on the record.

— end of apotheosis. The three challenge axes and both re-reader returns remain immutable beside this file per E-1/E-3.
