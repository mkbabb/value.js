claude-fable-5

# fr-PaperSearchDropdown — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-25 · **Subject tree**: `/Users/mkbabb/Programming/fourier-analysis` @ `cd26c65`, working tree 28 dirty paths (re-counted); the ONE dirty line touching this component is itself adjudicated below (C:S-2) · installed `@mkbabb/glass-ui` **4.0.0** (re-read from `web/node_modules`), vue **3.5.38** · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the component's three challenge files — reader-1 (14 confirmations · 10 kills · 2 misses · 6 superlatives · 7 unproven) and reader-2 (29 confirmation cells · 11 kills · 4 misses · 6 superlatives · 9 unproven). Challenge files (`audit/fourier-components/PaperSearchDropdown/challenge-D-design.md` 47K, `challenge-L-library.md` 45K, `challenge-C-consumption.md` 38K) spot-read by this seat at every disputed row (D:B-2 + falsifier, D:B-3 falsifier, D:M-6b, D:I-2 cells :243/:252, D:S-4, D:§Posture line 8, C:C-2 falsifier, C:C-3 + falsifier, C:C-6 table, C:C-7 enumeration, C:Receipt-3, C:C-10 worked example, L:L-2 falsifiers a/b, L:L-8/L-9 latency limbs) — the readers' representations of the corpus are accurate everywhere I checked.
**Method**: neither reader was adopted before being attacked. The one verdict-level disagreement (the B-2/L-3/C-3 consequence limb: reader-1 KILLED with replacement, reader-2 CONFIRMED latent-BLOCKER-on-repair) was re-derived by this seat element-by-element; both load-bearing NEW MAJORs (reader-1's containing-block capture, reader-2's type-weight flood) were re-verified end-to-end before admission — the flood by this seat's own node run of the transcribed scorer, the capture by reading host, rung, tokens, spec-precept and the producer's own logged casualty. Killers' cells were re-derived too: one of reader-2's kill cells dies here.
**This seat's own instruments (all read-only, zero browser probes — probe parsimony)**: full reads of `PaperSearchDropdown.vue` (whole, 70), `PaperSearch.vue` (whole, 397 — the scoped block :41-397), `usePaperSearch.ts` (whole, 108+type), `searchHelpers.ts` (whole, 74), `paperSearchIndex.ts` (whole, 402); `MobileFloatingToc.vue:100-129` + `:200-224`; `PaperSidebar.vue:45-54` + `:130-169`; `PaperView.vue:105-124` + `:320-349`; installed glass-ui 4.0.0 dist — `styles/glass/ladder.css` (`.glass-resting` rung with `backdrop-filter: var(--glass-blur-resting)`; `.glass-floating` rung `--glass-backdrop`/`position:relative`/`background`/`backdrop-filter`/`border`/three-part `box-shadow`; the **unconditional** `:where(.glass-floating, .glass-overlay)` `contrast-color(var(--card))` ink re-point incl. `--muted-foreground`), `styles/tokens/glass.css:45,:76` (`blur(calc(10px * var(--glass-level))) saturate(1.05)`), `styles/tokens/scheme-motion.css:337` (`--z-bar: 30`), `styles/tokens/color-radius.css:45/:85/:89` (`--neutral-5` + AA annotation; `--muted-foreground-strong: var(--neutral-6)`), `styles/tokens/dark-arm.css:82` (`--primary: oklch(0.739 0.134 318.1)` — the M-6b decider), `styles/transitions.css:83-97` + PRM carve `:231/:249`, `dist/button-BNDWhAZb.js` (`defaultVariants {variant:"default", size:"default"}`; `size.default = "h-(--control-h-md) px-4 py-2 has-[>svg]:px-3"`; `ghost: "bg-transparent text-foreground/70 …"`; `btn-pill` in base), `styles/tokens/offsets-sizing.css:146-151` (`--control-floor` coarse lift), exports map (80 keys, `./search` present), `search/FuzzySearch.vue.d.ts` (`variant?: "sidebar" | "floating"`), `useFuzzySearch.d.ts` (`debounceMs`/`maxResults`), `fuzzySearchIndex.d.ts` docblock **byte-compared** against `paperSearchIndex.ts:64/:68` (`cat -v`: producer ASCII `->`/`-0.1`, local U+2192/U+2212 — the C-6 glyph decider); `@vue/runtime-core/dist/runtime-core.cjs.js:5687` read verbatim (**no** `DEV &&`); `@mkbabb/latex-paper` dist `ContentBlock = string | MathBlockData | … | ProofBlock` (no null — the FR-PSD-NULL gate); glass-ui repo git-shows at **v7.0.0**: `Button.vue:15-31` (emphasis/tone/size/iconOnly; `ButtonSize` excludes `icon`), `searchVariants.ts` (`inline|bare|floating`), `scheme-spring.css` `--ease-standard` **:191** / `--ease-out-expo` **:194**, `scheme-motion.css:216` `--z-bar: 30`, `ladder.css:139` `.glass-floating`; `docs/precepts/instructions/LESSONS-LEARNED.md` 2026-06-10 block + `docs/tranches/AY/audit/ANIM-MATRIX.md` D3 (the ToastViewport casualty) read whole; a **node run** of the scorer transcribed verbatim from `:70-127` (the flood decider: `'pro'`→type `proof` = 29.8×10 = **298.0**; `'pro'`→label `convergence properties` = 18.1×12 = **217.2**; `'sec'`→`section` = **296.0**; stable-sort tie → insertion order; also `'co'` ⊂ `'section'` score 1.5 — reader-1's decoy-leak lesson reproduced); repo greps re-run: click-outside/`@blur` in `components/paper/` = **0**, barrel importers = **0**, tailwind `preflight.css` sole `mark` hit = the datalist-marker **comment** at :353, `glass-elevated` in installed dist = **0**, `web/e2e` = 8 spec files with `search` only in `gallery.spec.ts`; registry identity sweeps: `containing block` absent from every fr-PaperSearch*/fr-MobileFloatingToc/fr-PaperSidebar/fr-PaperView row (the 7 fr-* hits — fr-App MG-ε, fr-GalleryCardModal GCM-14, etc. — are different elements and mechanisms, read to confirm); no banked row carries the `_lc.type`-weight flood.
**Identity posture**: this component sits one file below the banked **fr-PaperSearch** r2 apotheosis, which already owns most of this family's defects. The guard is therefore FOLD-heavy by construction: **21 of 34 roster rows fold by reference, zero re-books**; the record's value is the 13 rows the bank does not have — two of them MAJOR mechanisms no axis and no banked row states.

## Scoreboard

**34 adjudicated roster rows — 1 BLOCKER · 11 MAJOR · 13 MINOR · 9 INFO; 21 FOLD BY REFERENCE (banked ids kept for life), 13 NEW to the registry.** **17 killed-register entries** — including one RULED kill that **overturns reader-2's confirmation** (the B-2/L-3/C-3 consequence limb) and one **kill of a killer's cell** (reader-2's `:150/:151` anchor "correction" fails this seat's re-read — the D axis's cite was exact). **6 missed rows ADMITTED** (2 from reader-1 + 4 from reader-2 — **the missed sets are again fully disjoint**, the same fleet fact fr-PaperSearch's correction 4 recorded). **8 superlatives sustained.**

Blocker-weight survivor: **B-1** (D:B-1/L:L-1/C:C-1), the two-root-Fragment scope kill — re-proved by both readers with the repo's own toolchain (`sfc.parse` styles:0, `compileTemplate` → `_Fragment` patchFlag 64 `STABLE_FRAGMENT`, 64 & 2048 = 0) and pinned by this seat's verbatim read of the installed gate at `runtime-core.cjs.js:5687`. It FOLDS into banked fr-PaperSearch D-1/L-1/C-B1 — this file is the CAUSE (the discretionary root arity), the bank owns the identity. The sharpest new fact in this corpus: **the naive cure's replacement regression was mis-stated by all three axes**, and the correct statement (FR-PSD-CB) is the producer's own named glass-first trap, committed by the one component in the family that does not portal its fixed overlay.

## Ruled disagreements and corrections (the incredulity seat's actual work)

| # | row | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **D:B-2 / L:L-3 / C:C-3 consequence limb** ("post-cure, the backdrop swallows every result click") | KILLED; replacement filed as missed find (containing-block capture) | CONFIRMED — "LATENT, BLOCKER-on-repair"; click trace listed as its own UNPROVEN | **KILLED — reader-1 sustained, reader-2 overturned; corrected statement admitted as FR-PSD-CB** | The z-tie is real (PaperSearch.vue:108 vs :195, both `var(--z-bar)`; `--z-bar: 30` at installed scheme-motion.css:337 — all re-read). But the floating host is `MobileFloatingToc.vue:110` `.floating-toc-bar--search glass-resting` (`position:relative; z-index:2` at :205-221, my read) and `glass-resting` carries `backdrop-filter: var(--glass-blur-resting)` (ladder rung + tokens :45/:76, my reads) — a non-`none` computed value, hence a **containing block for `position:fixed` descendants** (Filter Effects L2; glass-ui's own precept, LESSONS-LEARNED 2026-06-10, read whole: "ANY fixed overlay mounted inside a glass surface silently re-anchors … MUST teleport"; casualty ANIM-MATRIX D3, ToastViewport → ToastPortal). Post-cure the backdrop's `inset:0` resolves to the ~48px bar's padding box: it covers the INPUT (tap-in-field fires `close()`, which also wipes the query at usePaperSearch.ts:48-53) and overlaps only the bar's bottom-padding strip of a panel anchored `top:100%` (:219) — it cannot swallow the list. C-3's falsifier is **doubly refuted**: it names the ancestor class and discharges it as "impossible, since a transform/filter ancestor captures fixed descendants too" — wrong operative direction: capture SHRINKS the fixed box; the `absolute` plate anchors to the NEARER positioned `.paper-search` (:43-45) regardless. The three axes' repair would raise the backdrop's z-index and ship a broken dismiss shim. Engine spread (Safari/Gecko) → UNPROVEN-NEEDS-LIVE. |
| 2 | **D:I-2 grade** (glass 4→7 Button `variant`→`emphasis` + no `icon` size) | MAJOR ("largest uplift fact in the census") | INFO ("adopted pin is ^4.0.0; F.W1 budget input") | **MAJOR — as a formation-census gap, booked at the scope where the count lives (tree-wide → F.W1)** | Both readers reproduced all counts EXACTLY (35 files / 99 `<Button` / ghost 49 / outline 30 / glass 12 / `size="icon"` 38) and git-show-proved the producer at the tag; this seat re-proved `Button.vue:15-31` at v7.0.0 (no `variant`, `ButtonSize` excludes `icon`). Nothing breaks at the ADOPTED pin — but `lane-frontend §5`'s "rows that hit fourier TODAY" table omits an ~87-site hard typecheck break, and a forming F.W1 spec consuming that table under-budgets by an order of magnitude. Per the scope law (ColorSpaceSelector addendum 1): the row books tree-wide at F.W1; this component contributes exactly one site (`:44`). Two cite kills ride the row (K12, K13). |
| 3 | **D:B-3's eight `color-mix` cells** | Falsifier-kill: the 2 in-plate cells are engine-conditional (unconditional `contrast-color` ink re-point) | Flat 8-site confirmation, arithmetic re-derived | **Reader-1's qualification ADOPTED; fold to banked D-14 with a rider the bank lacks** | My ladder read confirms the **unconditional** `:where(.glass-floating, .glass-overlay)` arm re-points `--muted-foreground` to `contrast-color(var(--card))`; nothing in `web/src` re-declares it. So `.paper-search-badge` (:146) and `.paper-search-number` (:171) — inside the plate — are engine-conditional; the other six sites stand as computed. Both readers' arithmetic is concordant to 3 s.f. (2.78/2.39/3.93/3.05 — badge/number/dark-badge/definition) and is sustained. Precedent: fr-MobileFloatingToc M-4/K-11, the identical arm ruled engine-conditional MAJOR. Banked fr-PaperSearch D-14 books all eight flat — **this rider corrects the banked row's scope without re-booking it**. |
| 4 | **D:M-5 / C:C-13 grade** (highlight from live query vs debounced rows) | MINOR + a LIVENESS addition (no preflight `mark` reset) | "MINOR-to-MAJOR (amplitude unproven)" | **MINOR, LIVE — with reader-2's render-cost limb admitted as the second limb of the same row** | Mechanism certain (`:58` reads `search.query.value`; results from `debouncedQuery`, 120ms), amplitude unmeasured, cure one word (plus a one-line export — `debouncedQuery` is NOT returned, my read of :94-105). Liveness re-verified by this seat: tailwind preflight's only `mark` hit is a comment (:353) → UA `Mark`/`MarkText` renders today, flicker visible now, not latent behind B-1. Certain-mechanism/unmeasured-cost/trivial-cure books MINOR (fr-PaperSearch ruling-7 discipline). |
| 5 | **D:M-7 grade** (material plate = scrollport; the `::before` catch-light scrolls away) | (not adjudicated) | MAJOR, LATENT | **MINOR, LATENT** | Mechanism certain (`glass-floating` on the same div PaperSearch.vue:110 makes `overflow-y:auto`; producer `::before` `position:absolute; inset:0` — reader-2's material.css read, corroborated by this seat's rung read), visual magnitude unmeasured (SS-13), cure trivial (separate the material plate from the scroll layer). Same ruling-7 discipline as row 4. |
| 6 | **Reader-2's anchor "correction" on D:M-2** ("D cites :151, live is :150") | (n/a) | Filed inside its D-provenance kill | **REJECTED — killer's cell killed; the provenance kill itself survives on other rows** | This seat's read: `.sidebar-nav {` opens at **PaperSidebar.vue:151**, exactly D's cite; reader-2's own cells cite ":150-165" and are the ones off by one. The D-axis drift kill (K8) is sustained on the rows I re-verified myself (S-4's `:333/:341/:350` vs live `:335/:350`; B-2's backdrop ":62-67" vs live `:65-69`) — but its cell census must be consumed knowing one entry is inverted. Killers' cells get re-derived too (fr-PaperSearch ruling-2 precedent). |

**Adjudicator corrections against the readers (4).** (1) Reader-2 re-confirmed the B-2/L-3/C-3 consequence from the tie + tree order without inspecting the floating host — the exact one-element-short failure it convicted the C axis of in its own C-2 kill; overturned (ruling 1). (2) Reader-2's `:150/:151` counter-cell is itself wrong (ruling 6). (3) Reader-1 graded the e2e-gap row MAJOR; the bank books the family test-gap cell INFO-within-MINOR (fr-PaperSearch L-11/L-18/C-N7/C-N8) — identity guard keeps the banked grade; folded, not re-graded. (4) The two readers' missed sets are again fully disjoint (2+4, zero overlap) — recorded as fleet evidence that single hostile re-readers still under-cover; and only reader-1 flagged that ALL THREE challenge axes missed the banked **FR-PS-NAV** BLOCKER (the dead click at the centre of this very surface — `fr-PaperSearch.md`, decided statically), while praising the `_lc.number` ×18 weight that ranks exactly the dead results first. Folded by reference, never re-filed; recorded as the correct measure of this corpus's ceiling.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Dispositions are **ADJUDICATED, not VERIFIED** (M-25); routes name the forming X·F wave surface as agglomeration input. Line anchors are working-tree @ `cd26c65`; re-resolve before any cure (the D-19 lesson — doubly so here, given K8).

### Blocker (1)

- **B-1 = D:B-1 / L:L-1 / C:C-1 — BLOCKER · FOLDS BY REFERENCE into banked fr-PaperSearch D-1/L-1/C-B1 (+ D-3 for this surface).** No `<style>` in any child SFC; this file's template is a two-root Fragment (`<Transition>` :35 + backdrop div :65 — my whole-file read), so Vue 3.5.38's `setScopeId` never stamps it and every `.paper-search-results/-result/-badge/-number/-label/-backdrop` rule in PaperSearch.vue:103-196 is dead. Both readers re-proved it independently with the toolchain (compileTemplate → `STABLE_FRAGMENT` 64; 64 & 2048 = 0); this seat read the installed gate verbatim (`runtime-core.cjs.js:5687`, no `DEV &&` — see K11) and the negative control (PaperSearchInput single-root wrap :47-56 survives). New cell this record adds to the bank: the CAUSE is this file's discretionary root arity, and the cure that re-homes styles into this child must land WITH the FR-PSD-CB, FR-PSD-BASE and M-2-rider hazards below — the repair-hazard group now has SIX pre-booked members. **Route F.W3/W4** (bank's route, one atomic cluster).

### Majors (11)

- **FR-PSD-CB — MAJOR, LATENT-on-repair (NEW — reader-1's missed find, ruled #1; every link re-read by this seat).** The dismiss backdrop (`:65-69`, `position:fixed; inset:0` once B-1 is cured) is captured by the glass search bar's `backdrop-filter` containing block (`MobileFloatingToc.vue:110` + ladder `glass-resting` rung + tokens :45/:76): `inset:0` resolves to the ~48px bar's padding box, so (i) the click-away shim covers nothing outside the bar; (ii) it covers the INPUT — tapping the field fires `close()` and wipes the query; (iii) it overlaps only ~the top 10px of the panel, which is why the axes' "un-selectable" consequence is false. The producer names this exact trap and its cure in its own precepts (teleport to `<body>`) and this family already applies it one file over (`PaperSearchModal.vue:41`). Riders, folded not re-booked: the backdrop is a SIBLING of the `<Transition>`, so it unmounts on the dismissal tick while the panel fades (fr-MobileFloatingToc **m-11** class); and the panel's `z-index: var(--z-bar)` is inert against anything outside its host in BOTH variants (`.floating-toc-bar` `z-index:2`; `.paper-sidebar` `position:sticky` :141 — my reads) — the one elevation token spent buys nothing. Not banked (registry sweep re-run by this seat). Engine spread → SS-13. **Route F.W3/W4, gated into the B-1 repair cluster: the cure is Teleport, never z-index surgery.**
- **FR-PSD-TYPE — MAJOR, LIVE (NEW — reader-2's missed find; re-executed by this seat's node run).** `_lc.type` — a non-user-visible internal enum, byte-identical across every entry of a type — is weighted **10**, above `rawTex` (6) and `plainText` (3) (`paperSearchIndex.ts:171-177`). Any query fuzzy-matching a type name returns a content-blind, document-ordered slab of every entry of that type (`'sec'`→`section` = 296.0 for ALL sections, exactly tied; stable sort → build order; `slice(0,30)`), and can OUTRANK genuine in-label hits (`'pro'`: every proof scores 298.0 vs 217.2 for a real title match — **+37.2%**, my run reproducing reader-2's cells exactly). Compounds banked FR-PS-FTS: with rawTex/plain structurally unreachable, the surviving ranking axis is number(18) > label(12) > **type(10)**. Registry sweep: not in any banked row (the ranker-hygiene row L-10/L-13/L-17 and MISS-B4 are different mechanisms). The visible badge (`TYPE_LABELS[r.type]`, :50-52) makes the flood look plausible rather than broken. **Route F.W3/W4** (scorer spec, sequenced with the FTS cure and the MISS-B3 producer-adoption decision; whether the producer's 3-field copy shares the flood is a **glass-ui BH relay** question, checked before the local delta is authored).
- **D:I-2 — MAJOR (formation-census gap; ruled #2).** The glass 4→7 Button surface (`emphasis`/`tone`/`iconOnly`; no `variant`, no `icon` size — git-show-proved at v7.0.0 by both readers and this seat) breaks ~87 sites tree-wide that `lane-frontend §5`'s break table omits; all twelve counts reproduce. This component contributes one site (`:44`). Cite kills K12/K13 ride the row. **Route F.W1** (uplift budget); the component-local cell rides F.W3/W4's shadow retirement (the row may DELETE before F.W1 executes — the census must budget for both orders.)
- **L:L-2 / C:C-10 — MAJOR · FOLDS into banked fr-PaperSearch L-2/C-M4 (ruling 3 + K13).** Truncated-cache prefix narrowing (`:220-228` vs `:239-240`, re-read); reader-1's from-scratch re-execution against the unmodified module is admitted as the strongest witness yet (COLD `'co'` → `['X']`; after `'c'` → `[]`), including its published false start (decoys leaking through `_lc.type` — which is FR-PSD-TYPE's mechanism seen from the side). C-10's flagship `"thm 3"` scenario is killed (K2); L's BLOCKER grade is killed (K4). The banked MAJOR stands. **F.W3/W4** (cured mechanically by producer adoption).
- **D:M-2 — MAJOR, LATENT-on-repair · FOLDS into banked fr-PaperSearch D-3, with a post-cure rider the bank lacks.** Today's limb is banked (in-flow rows inside the sidebar's scroll panel). The rider: once B-1 revives `position:absolute; max-height:50vh`, the panel becomes a positioned descendant of `overflow-y:auto` `.sidebar-nav` (:151-166 — D's cite exact, per ruling 6): clipped at the rounded border, contributing its overflow to the ToC's scrollable height, 50vh unhonoured. Repair-hazard group member. **F.W3/W4.**
- **L:L-5 — MAJOR · FOLDS into banked fr-PaperSearch L-5.** Below 1024px the sidebar instance renders all 30 rows into `display:none` (`PaperView.vue:335` no `v-if`; `PaperSidebar.vue:132-140` — my reads), and ⌘/Ctrl-K `preventDefault`s the browser then no-ops. Reader-2's mechanism refinement adopted: `open()`'s only observable effect routes to a `focus()` on an element inside `display:none` — a spec no-op; the mobile host's `searchActive` is unreachable from `open()`. **F.W3/W4.**
- **D:M-3 / L:L-4 — MAJOR · FOLDS into banked fr-PaperSearch D-7/L-7.** No outside-dismissal of any kind in the sidebar variant (backdrop gated `variant === 'floating'` :66; zero click-outside/`@blur` listeners — grep re-run by this seat, 0 hits; `@focus` raises `isOpen`, nothing lowers it). **F.W3/W4.**
- **D:M-1 / C:C-11 — MAJOR · FOLDS into banked fr-PaperSearch D-9/D-10/D-11/C-M7/C-B3/C-M1.** Zero AT surface; 30 real `<button>` tab stops; selection is an invisible-inaudible class. **F.W3/W4** (adopt `./dialog`; combobox semantics in the spec).
- **D:B-3 — MAJOR, LATENT · FOLDS into banked fr-PaperSearch D-14, carrying ruling 3's in-plate qualification as a rider on the banked row.** Eight `color-mix(in srgb, var(--muted-foreground) N%, transparent)` dilutions of an AA-annotated token; both readers' independent recomputations concordant to 3 s.f. with D's cells; the producer's own written refusal of the idiom (`--muted-foreground-strong … By-colour neutral step, not alpha` — re-read at color-radius.css:89) stands beside it. Two in-plate cells (badge :146, number :171) engine-conditional per the unconditional `contrast-color` arm; six flat. BLOCKER re-filing killed (K4). **F.W1 token re-ink + F.W3/W4 repair-hazard gate** (bank's route).
- **C:C-6 — MAJOR (disposition) · FOLDS into banked fr-PaperSearch MISS-B3.** The installed 4.0.0 `./search` is the same module upstreamed — ten-member state identity, same 120/30 defaults, same bonus table (glyph-identity cell killed, K10), the `variant?: "sidebar" | "floating"` union re-read by this seat — imported zero times. The bank adds what neither reader could know from d.ts alone: the producer copy already fixes L-2 and L-4. **F.W3/W4 decision row + BH relay** (bank's route).
- **C:C-7 — MAJOR · FOLDS into banked fr-PaperSearch D-4/D-5.** The CONTENT-WIDTH pill consumed as 30 full-width truncating list rows (producer contract quoted verbatim and correctly). The six-override enumeration cell is killed (K14): five, and the un-enumerated `h-(--control-h-md)` is the load-bearing one — see FR-PSD-BASE. **F.W3/W4.**

### Minors (13)

- **D:M-5 / C:C-13 (+ reader-2's render limb) — MINOR, LIVE (ruled #4; NEW row — the bank holds the UA-mark fact as D-8, not this consequence).** Limb 1: marks drop wholesale for 120ms after each keystroke (`:58` live query vs debounced rows; `searchHelpers.ts:49`), visible TODAY (preflight has no `mark` reset — this seat's grep). Limb 2: because `:58` reads undebounced `query` inside the render function, every keystroke re-renders all 30 rows and re-runs 30×tokens fuzzy passes + 30 `[...text]` allocations while `results` is unchanged — the debounce is bypassed for the more expensive half of the work it exists to bound (×2 instances below `lg`, per banked L-5). One cure: export `debouncedQuery`, pass it. **F.W3/W4.**
- **FR-PSD-BASE — MINOR, LATENT-on-repair (NEW — reader-1's missed find; premises re-read by this seat).** `.paper-search-result` (:118-130) declares `align-items: baseline` and **no height**; the row is a `<Button>` with no `size`, so the CVA default `h-(--control-h-md)` (40px fine / 44px coarse — dist reads) applies before AND after the B-1 cure. Post-cure the authored compact geometry never takes effect: a ~40px box holds a ~20px baseline-anchored row (~14px dead band under every label), and the 50vh panel seats ~12 rows instead of ~16. The inversion neither axis states: the un-overridden height is the only thing keeping coarse-pointer rows at the WCAG 2.5.5 44px floor — a repair that forces `height:auto` to honour the authored padding introduces a tap-target failure. Third member of the repair-hazard group (banked D-4 + MISS-A5). Magnitudes at live `--ui-scale` → SS-13. **F.W3/W4.**
- **FR-PSD-PURE — MINOR (NEW — reader-2's missed find; verified against my whole-file reads).** `results = computed(() => searchIndex(index, debouncedQuery.value, 30))` wraps a side-effecting function: the cache's ONLY clear paths (`:205`, `:214`) run inside a lazily-evaluated getter whose forcing subscriber (`watch(results, …)` :39-41) dies with the component scope — `close()` schedules a debounced empty-query write that can land on a stopped scope, so `_cache.clear()` never runs (this CAUSES the banked L-4/C-M5 retention, not merely coincides with it); and a cache hit keyed on `q` alone (`:210-211`, `index` never consulted) returns a value the computed's tracked deps did not produce. The module docblock's "no Vue reactivity" purity claim is falsified in the host, not just the cache. New limb ON banked L-4/C-M5. **F.W3/W4** (dies with producer adoption).
- **D:M-7 — MINOR, LATENT (ruled #5).** The glass tier and the scrollport are one element: `glass-floating`'s absolutely-positioned specular `::before` scrolls away with the content once `overflow-y:auto` revives. Magnitude SS-13. **F.W3/W4** (the repair splits plate from scroll layer — same edit family as FR-PSD-BASE).
- **D:M-6a — MINOR, LATENT · FOLDS into banked fr-PaperSearch MISS-A5 + D-17.** Unlayered scoped `border: 1.5px`/literal-black `box-shadow` (:112/:114) beat the rung's tokenized rim/lift in `@layer components`; no dark arm. **F.W3/W4.**
- **L:L-14 / C:C-22 — MINOR (NEW).** `TYPE_LABELS: Record<string, string>` (searchHelpers.ts:8) erases the 13-key union, typing `TYPE_LABELS[r.type]` as `string` and making the `?? r.type` guard (:51) typed-dead; `bibliography` is a declared union member (+label) no builder branch ever emits (this seat's whole-file read of the emission sites: section/equation/theorem-union/figure/code/proof only). **F.W3/W4.**
- **L-hygiene cluster (L-20 / L-21 / L-24 + L-19 residue) — MINOR (aggregate).** `let _cache` never reassigned (`const` correct); `.is-selected` round-tripped through the DOM (:46 → :24) for state Vue owns; `resultLabel` shows raw LaTeX with UTF-16 slices at six sites; `escapeHtml` (element-content-correct in place) is attribute-unsafe under a public name whose only export surface is the dead barrel — the name premise killed (K15), the residue rides the fleet dead-barrel class (banked MISS-B5). **F.W3/W4.**
- **L:L-8 / D:N-3 — MINOR, LIVE · FOLDS into banked fr-PaperSearch MISS-A4 (+ MISS-A3 for liveness).** `@mouseenter` (:48) writes the `selectedIndex` the arrows drive and the watcher observes; NOT latent — `scrollIntoView({block:'nearest'})` walks to the scrollable `.sidebar-nav` today (reader-2's kill K6, banked as MISS-A3). **F.W3/W4.**
- **L:L-6 / C:C-9 — MINOR-booked-as-banked · FOLDS into banked fr-PaperSearch L-4/C-M5 (MAJOR there for the leak limb; the magnitude limb dies here, K5).** Module-global cache, no owner/disposal/index identity; the "multi-megabyte" spread is refuted — `{...entry}` is a shallow spread, `_lc` and strings shared by reference (`:234`, my read). Severity rests on no-teardown + index-blind key, per the bank. **F.W3/W4.**
- **L:L-7 / C:N6 — MINOR · FOLDS into banked fr-PaperSearch L-8/C-N6.** Undisposed debounce timer; zero `onScopeDispose` family-wide. **F.W3/W4.**
- **L:L-10 / C:C-12 / C:C-14 — MINOR · FOLDS into banked fr-PaperSearch L-9/C-M6.** Per-row render-time `highlightFuzzy` with the shipped `matches` field read by nothing (root cause `:185` — label-only, `[]` otherwise). **F.W3/W4.**
- **L:L-11 / D:N-1 / C:C-20 — MINOR · FOLDS into banked fr-PaperSearch L-11/L-18/C-N7/C-N8.** The result row, import triple, dead emit and scroll watcher duplicated into PaperSearchModal (watchers partition `isExpanded` exactly — see superlative 6). **F.W3/W4.**
- **L:L-17 / C:C-17 — MINOR · FOLDS into banked fr-PaperSearch L-6 (ruling 7).** Index-suffixed `:key` (:43) defeats keyed reuse; motive = the real `code`/`proof` `id: sectionId` collisions at **:335/:350** (this seat's anchors — both readers now agree; D's S-4 superlative on the same construct is killed, K7). **F.W3/W4.**

### Info (9)

- **D:I-1 — INFO (NEW).** The Transition name `search-dropdown` (:35) forfeits glass-ui's shipped, PRM-carved `dropdown` transition (dist transitions.css:83-97 + carve :231/:249 — re-read by this seat); a rename restores motion, tokens and PRM handling while B-1 keeps the local rules dead. **F.W3/W4.**
- **C:S-2 — INFO (substrate provenance).** The sole working-tree change to this file is the correct repair (`glass-elevated` → `glass-floating`, one line — my diff); `glass-elevated` has ZERO occurrences in the installed dist (my grep) — **HEAD paints a class that does not exist**. Recorded for F.W0's substrate ledger; the dirty line must not be lost in any tree reset. **F.W0.**
- **FR-PSD-NULL — INFO (NEW — reader-2's missed find; gate verified).** `isMathBlock` (`:285-287`) guards `typeof block === "object" && "tex" in block` — `typeof null === "object"`, so a null content entry throws in `buildSearchIndex`, synchronously in `PaperView` setup, no error boundary → dead route. Unreachable at the pin (`ContentBlock` admits no null — this seat's d.ts read); the same author guards correctly 21 lines up (`:264` `b && "tex" in b`). Boundary-hygiene note. **F.W3/W4.**
- **L:L-12 / C:C-8 — INFO · FOLDS into banked fr-PaperSearch D-28/L-12/C-M2.** Dead `select` emit ×2, dead `defineExpose({resultsRef})`, unused `props` binding; the MAJOR/MINOR re-filings are killed (K17). **F.W3/W4.**
- **L:L-16 — INFO, LATENT · FOLDS into banked fr-PaperSearch L-19 — NO-WAVE-OWNER.** Code-unit/code-point mismatch; 0 non-BMP code points in the live corpus (reader-1's byte figure corrected by reader-2: 244,267, stale by 284 — the substantive falsifier holds). Re-measure on any paper swap.
- **L:§6 — INFO (corpus hygiene; also superlative 5).** Adjudicated intake row R5-7 proven NOT to apply on two independent grounds (component-loop callsite kind; no native `<li v-for>` in the ancestry — sibling-above-`<ol>` verified by reader-2), with the INVERSE blind spot (3 native `<span>`s per iteration incl. the `v-html` sink) booked. Recorded so F.W4 cannot mis-tag the component.
- **C:C-23 / C:C-24 / L:§5 — INFO · FOLDS into banked fr-PaperSearch C-Z1/C-Z2.** Load-bearing zeros: sole producer import `glass-ui/button`; zero value.js/keyframes/canvas/WebGL/API contact; data via the build-time virtual module. **F.W5-W8 = no rows.**
- **Archaeology — INFO · corroborates banked D-1's provenance.** `6bbb291` (631-line monolith, markup+style colocated) → `ffba307` (three children split out, stylesheet left behind, 395 lines) — both readers re-ran every cell independently with matching results. The defect has a commit of birth.
- **e2e/test gap — INFO-within · FOLDS into banked fr-PaperSearch L-11/L-18/C-N7/C-N8 (correction 3).** 8 Playwright spec files / 29 `test(` (both counting units correct — no conflict), `search` only in gallery.spec.ts, no unit runner, no lint config; sole static gate `vue-tsc -b`. The defect set has shipped since 2026-05-26 with no gate capable of seeing it — the FACT is blocker-adjacent context for every LIVE row above; the BOOKING stays with the bank.

## Killed-claims register — 17 entries

- **K1 · D:B-2 / L:L-3 / C:C-3 consequence limb — RULED KILLED (overturns reader-2; ruling 1).** "Every result click dismisses instead of selects" does not survive the floating host's containing block; the tie and the repair-hazard identity survive inside FR-PSD-CB's corrected statement. C-3's falsifier named the right class and discharged it with inverted reasoning; D's falsifier and C's C-2 grep both stopped one element/file short of the host.
- **K2 · C:C-10 worked example — SUSTAINED (reader-2; :203 re-read).** `"thm "` is never a cache key (trim precedes read/write); multi-token queries re-scan the full index. The flagship scenario is the one case the mechanism cannot produce.
- **K3 · D:M-6b `--primary` dark cell — SUSTAINED (reader-2; token re-read by this seat).** Dark `--primary` is `oklch(0.739 0.134 318.1)` (dark-arm.css:82) — a light lavender; reader-2's recomputation lands ~4.83:1 PASSING. D resolved the light arm's token in the dark cell; its falsifier grepped only the consumer's `.dark` blocks. The row's other two cells (definition 3.05, equation 2.28 dark) reproduce and survive; one leg dies. (Reader-2's path cite doubled a `tokens/` segment; value verified at the real path.)
- **K4 · L:L-2 and D:B-3 BLOCKER grades — SUSTAINED (both readers).** Both mechanisms are already RULED at MAJOR(-LATENT) on the same lines by the banked fr-PaperSearch adjudication (ruling 3 + roster D-14); re-filing at BLOCKER against a standing ruling is a severity regression, not new evidence.
- **K5 · L:L-6 / C:C-9 magnitude limb — SUSTAINED (reader-2; `:234` re-read).** Shallow spread; `_lc` and strings shared by reference; "multi-megabyte ceiling" and "30×~1KB retained" both overstate by the string-sharing factor.
- **K6 · L:L-8 / L:L-9 "latent behind L-1" limbs — SUSTAINED (reader-2).** `scrollIntoView({block:'nearest'})` scrolls the nearest scrollable ANCESTOR; `.sidebar-nav` is scrollable today — both findings are LIVE (banked MISS-A3). The L axis had both halves and drew the wrong latency conclusion.
- **K7 · D:S-4 as a superlative — SUSTAINED (reader-2; bank ruling 7).** The composite `:key` is a workaround banked as a MINOR DEFECT; awarding it excellence while the root cause (build-time unique ids) goes unbooked is the failure mode L-18 exists to catch.
- **K8 · D-axis "Every claim carries severity · file:line · falsifier" — SUSTAINED (reader-2), with ONE cell of the kill itself killed (ruling 6).** Script-block and cross-file anchors drift 1–4 lines throughout (this seat re-verified S-4's `:333/:341/:350` → live `:335/:350`, and B-2's `:62-67` → live `:65-69`); but reader-2's `:151→:150` entry is inverted — D was exact there. C's and L's citations resolve exactly everywhere this seat spot-checked. All D claims survive; all D anchors must be re-resolved before cure.
- **K9 · C:C-2 falsifier enumeration — SUSTAINED (both readers, convergent).** The `glass-floating` grep omitted the ladder rung — the very rule the sibling axis builds D:M-6a on. Conclusion ("nothing positional") survives; a falsifier discharged by an incomplete grep is not discharged. Second one-file-short discharge in the same corpus (with K1).
- **K10 · C:C-6 "character-identical" — SUSTAINED (both readers; byte-compared by this seat with `cat -v`).** Producer ASCII `->`/`-0.1` vs local U+2192/U+2212. Substance unimpeachable; the one word carrying the evidentiary force is false.
- **K11 · C:Receipt-3 — SUSTAINED (reader-1; `:5687` read verbatim by this seat).** No `DEV &&` in the installed runtime (0 occurrences file-wide); the dev-gating lives in the COMPILER's patchFlag emission. A paraphrase inside a fenced "Receipt" presented as engine source is a provenance defect; the conclusion is unaffected.
- **K12 · D:I-2 ease-token cite — SUSTAINED (reader-1; git-shown by this seat).** `--ease-standard`/`--ease-out-expo` live at v7.0.0 scheme-spring.css **:191/:194**, not :158/:161 (prose in the motion-register table). Fact true, cite points at commentary — the K-12-class failure the fleet minted a rule for.
- **K13 · D:I-2 variant-union attribution — SUSTAINED (reader-2; git-shown by this seat).** v7.0.0 `searchVariants.ts` is THREE-valued (`inline|bare|floating`, no `sidebar`) — D quotes it correctly then calls it "the same two-variant chrome axis"; the exact `"sidebar" | "floating"` match exists at the INSTALLED 4.0.0 `FuzzySearch.vue.d.ts` (C's citation, re-read by this seat). BASELINE LAW: the adopted pin decides; C-6 is the correct statement of the shadow.
- **K14 · C:C-7 six-override enumeration — SUSTAINED (reader-1; `.paper-search-result` re-read).** Five, not six: no `height`/`min-height` of any kind is declared, so `h-(--control-h-md)` is never overridden — and that un-enumerated cell is the one that outlives the repair (FR-PSD-BASE). Also `white-space: nowrap` on the LABEL is the same value, not an override.
- **K15 · L:L-19 public-name premise — SUSTAINED (reader-1; grep re-run by this seat: barrel importers = 0).** No public name to trap; the 4-line barrel is dead code already banked in the fleet dead-barrel class (MISS-B5). Residue folded into the hygiene cluster.
- **K16 · L:L-2 falsifier-(a) "always" cell — SUSTAINED (reader-2; :28-37 re-read).** `searchIndex` is reached only through the debounced computed; a first character typed-through within 120ms is never cached. Mechanism stands; "always"/"any two-character query" do not.
- **K17 · L:L-12 / C:C-8 severity — SUSTAINED (reader-1).** The dead-API cluster is banked at INFO (D-28/L-12/C-M2); two call sites, both in one read file, do not cost a debugging session.

## Superlatives — 8 sustained (L-18 runs both ways)

1. **The `v-html` sink survives its SEVENTH independent attack.** Three axes, two prior hostile readers + the banked adjudicator, and now two re-readers — all from the attacker's side: four escaping exits, `&`-first, query never interpolated, element-content sink making the `"`/`'` omission correct. This seat re-read all four exits. Still the strongest convergent correctness result in the fourier corpus.
2. **D's WCAG arithmetic is reproducible by construction** — both readers independently recomputed disjoint cell sets from installed tokens (sRGB alpha-compositing of `color-mix(…, transparent)` + relative luminance) and landed every recomputed cell to 3 s.f. (2.780/2.393/3.929/3.054). The one cell that died (K3) died to token RESOLUTION, not arithmetic — precision and provenance are different disciplines, and this corpus proves you can ace one and fail the other in the same table.
3. **D's uplift census: twelve counts, twelve exact reproductions** — by two readers independently, including the two disambiguation rows D volunteered against itself (Slider `variant="standard"` ×14, SegmentedTabs `variant="underline"` ×3) — feeding the largest uplift fact in the fourier census from outside the design axis's own remit. Its two dead cite cells (K12/K13) do not touch the counts.
4. **The commit archaeology was done twice, independently, with matching results** (631-line monolith at `6bbb291`, grep -c 3 at `ffba307^`, 395 lines after) — dating the BLOCKER to its commit of birth is the single most useful thing an audit hands a repair wave.
5. **L's §A re-execution discipline** — the recall probe re-run from scratch against the unmodified module, reproducing the shape exactly, INCLUDING its own published false start (`'co'` ⊂ `'section'` through `_lc.type`, score 1.5 — reproduced by this seat's node run). "I executed it and here is where I first got it wrong" is worth more than any clean assertion; and the accident is FR-PSD-TYPE's mechanism, witnessed before anyone named it.
6. **The partitioned scroll watchers + the instance-scoped query, closed on a fourth side.** `:22`'s early-return partitions `isExpanded` exactly against the Modal's `:30`; `:24`'s `resultsRef.value?.querySelector` is better than its sibling's document-global reach AND better than the producer's own regressed FuzzySearch (banked superlative 6). `scrollIntoView({block:"nearest"})` with no `behavior` remains the minimum-movement PRM-safe pair.
7. **C's and L's citation exactness** — every citation this seat spot-checked (~15 across seven files and the installed dist, including the awkward multi-line rules) resolves exactly; that is the baseline D's script-block anchors miss, and the contrast is itself the lesson (K8).
8. **Three axes reached B-1 from three genuinely different directions** (orphaned stylesheet / root arity / consumption seam), each carrying the negative control — the single-root Input forty lines from the two-root Dropdown, positive and negative case in one feature. And all three published their falsifier corpses (D §F retiring four of its own claims; L-16 measuring its own finding down; C's S-1/S-3 "tried and could not"). The discipline held everywhere except at the two mechanisms that required stepping one element outside the component — which is exactly why this seat exists.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. **FR-PSD-CB engine spread**: the `backdrop-filter` fixed-containing-block capture on Safari/Gecko (spec, producer precept and Blink concordant; capture wanted). Note for the probe: the a11y arms do NOT release it (`prefers-reduced-transparency`/`prefers-contrast` only set `--glass-level` → `blur(0px)` is still non-`none`); only the `@supports not` fallback would. 2. The post-cure click trace, filmed: backdrop box vs panel vs input in the floating variant (decides the FR-PSD-CB consequence geometry cell-by-cell). 3. Which of D:B-3's two in-plate cells fail on the live engine (`contrast-color()` support switch; sequenced with fr-MobileFloatingToc's SS-13 #2). 4. Today's unstyled pill stack: wrap vs one-per-line (C-2's 30×40px=1200px arithmetic is UNPROVEN as stated — `inline-flex` atoms in a block container total (line count × line box), not (rows × height)). 5. M-5 flicker amplitude per keystroke and the render-limb's wall-clock (30–60 rows). 6. `mouseenter`-on-scroll dispatch (engine/compositor-dependent; closes banked MISS-A4's loop). 7. ToC scroll amplitude per ArrowDown (banked MISS-A3). 8. FR-PSD-BASE dead-band and D:I-3 baseline magnitudes at live `--ui-scale`. 9. D:M-7's specular-scroll magnitude. 10. Dark-mode `<mark>` once the `:deep` rule revives — photograph, don't reason. 11. L-23's index-build ms. 12. Whether banked MISS-A7 (build unrunnable: object `manualChunks` under vite ^8 rolldown) still holds — the reason none of these axes could produce rendered-DOM falsifiers; **F.W0 re-checks before any SS-13 pass is scheduled.**

## Closing verdict

**DEFECTIVE BY INHERITANCE, WITH TWO NEW MECHANISMS OF ITS OWN.** This file is where the family's banked blocker is CAUSED (the two-root fragment that orphans 43 of 48 rules) and where its dead-click results render — both already owned by fr-PaperSearch, folded here without re-booking. What this adjudication adds to the registry: **FR-PSD-CB** — the producer's own named glass-first × fixed-overlay trap, committed by the one component in the family that doesn't portal, which invalidates the consequence all three axes filed and redirects the repair from z-index surgery to Teleport; **FR-PSD-TYPE** — an internal enum as the third-strongest ranking signal, returning content-blind slabs that outrank real hits by 37%, reproduced in this seat's own node run; plus the F.W1 census gap (~87-site Button break omitted from lane-frontend §5), the baseline/height repair inversion, the impure-computed cache-clear scope hazard, and the debounce-bypassed render limb. Roster: **1 BLOCKER · 11 MAJOR · 13 MINOR · 9 INFO** (21 folds, 13 new); kills: **17**, one overturning a reader's confirmation and one overturning a reader's kill cell; routes: overwhelmingly **F.W3/W4** (one atomic repair cluster around B-1, now with SIX pre-booked hazards: FR-PSD-CB, M-2's clip rider, FR-PSD-BASE, MISS-A5, D-14's ghost inversion, D-16's ring — the first three from this record), **F.W1** owning the census gap and the token re-ink, **F.W0** owning the substrate cells (C:S-2's dirty-line preservation; MISS-A7 re-check), **F.W5-W8 empty** (measured zeros), one **NO-WAVE-OWNER** bank (L-16 → banked L-19), and two **glass-ui BH relay** riders (does the producer's 3-field scorer share the type-flood; the already-banked shared-scorer letter). The corpus's ceiling is recorded honestly: arithmetically immaculate on three axes, and every axis missed the dead click (banked FR-PS-NAV), the containing block one element up, and the enum flooding its own ranker.

— end of apotheosis r2. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED (M-25); the routings are agglomeration input to the forming X·F specs, and execution awaits the owner's begin-word.
