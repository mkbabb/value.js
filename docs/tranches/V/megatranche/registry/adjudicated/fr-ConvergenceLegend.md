claude-fable-5 (served model id)

# fr-ConvergenceLegend — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `fourier-analysis/web/src/components/equation/convergence/ConvergenceLegend.vue` (97 lines; read whole by this seat) — the `/equation` curve legend: Sum + f(x) reference rows, a hairline divider, and one native `v-for` over `TrigHarmonic[]`, painted as a `glass-wash` overlay on the ConvergencePlot canvas.
**Substrate**: fourier `cd26c65` + dirty WT (re-pinned by this seat: `git log -1` + porcelain; the subject is `M`, its whole working-tree diff = the one-hunk `glass-subtle → glass-wash` rename at `:17`, re-diffed by this seat) · installed glass-ui **4.0.0** (80 export keys; `./fading-scroll` present — seat-read) · installed value.js **0.13.0** (exports `['.']` — seat-read) · value.js producer **4.0.0** (7 subpath keys, no `"."` — seat-read) · glass-ui producer at **8.0.0** `e73ec80c` ("8.0.0 LIVE") — **one major PAST the 7.0.0 the D axis audited** (SR-1).
**Corpus**: three axis challenges (`audit/fourier-components/ConvergenceLegend/challenge-{D-design 19+4·U1-U4, L-library 14+4, C-consumption 15+5}.md`, all read whole by this seat) × two hostile re-reader returns (reader-1 design-and-usage · reader-2 library-and-code), delivered as two separable JSON returns — so this record DOES carry a reader-vs-reader ruled-disagreements table.
**Method (M-23/L-18)**: every reader-vs-reader disagreement was re-derived against the tree by this seat's own instruments before ruling; four seat corrections were minted AGAINST the readers (SC-1..SC-4, including one false correction by reader-1 and one arithmetic-independence claim that was premise-inheritance); two reader "missed" rows were caught by the identity guard as ALREADY BANKED apotheosis rows and folded, not re-booked. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node; zero browser — probe parsimony; product source READ-ONLY; this file is the sole write)**: whole-file reads of the subject and `lib/harmonics.ts`; ConvergencePlot.vue :1-30 (script head incl. `:5` bare-root value.js import) / :30-110 (`startLoop` no-guard, `draw()` unconditional `canvas.width/height` at :88-89, early returns :82/:85/:100) / :140-334 (cursors :141, `:205 if (cursors[hi] < 3) continue;`, strokes :188-235, `:235` hit-region push, `:240-253` guarded canvas path + `mousePos` writes :244/:251, tooltip :260-270, `:276` togglePlay reset, **`:295-296` legend handlers — BOTH wrapped `if (!playing.value)`, the D-L6 decider**, `:298-304` activeCount, :308-333 lifecycle incl. `:324` autostart) / :336-410 (**two-root fragment template**; canvas :338-343 bare; legend mount :357-362 — no `activeCount` prop; `.convergence-container` :382-385; tooltip block + PRM :388-409); EquationView.vue :28/:43-45/:52-54 (`autoHarmonics = ref(true)`; `vizHarmonics = min(effectiveN, nHarmonics)`) + :308-315 (`<ConvergencePlot class="flex-1">` inside `flex-1 min-h-0`); FunctionInput.vue:183-186 (`:min="1" :max="100"`); useCurveTransition.ts:10-15 (byte-identical `TrigHarmonic`) + :57; CoefficientsSpectrum.vue:35-50 (`slice(0, expanded ? 40 : 12)`; private `spectrumColor` copy); simplification.py:66-84 (`minimum: 3`; returns it on `total_energy <= 0`); **api/models/equations.py:20 `le=200` vs visualization.py:126/:187/:281 `le=4096` — the C-6 cardinality decider**; style.css :40-49 (root bump) / :112-127 (the `--viz-amber` carry text, "against `--background`") / :129-144 (`:focus-visible` list — no `.legend-entry`); colors.ts :10-14/:77-96 (`STATIC.golden "#f0b632"`; `resolveVizColors` assigns exactly five, never `.golden`); golden-shimmer.ts:52; **`git show 2e4a452` read whole — the 3+/3- diff IS the `#f0b632 → var(--viz-amber)` swap on the golden dot+label, and mechanism (d)'s two enumerations (9 hex hardcodes · 11 Tailwind alpha-modifier sites) are disjoint from `rgba(180,180,180,0.6)`, while (e) authors the canonical ARIA pattern**; installed glass-ui 4.0.0 reads — ladder.css `:6 @layer components` / `:37-42` wash rung / `:134-151` `@container style(--glass-backdrop: light)` bucket (includes wash, never fires: `grep glass-backdrop web/src` → ∅) / `:200-204` `:where(.glass-floating,.glass-overlay)` unconditional muted lift (wash EXCLUDED) / **`:216-219` content-tier tint floor — `--glass-tint-source: var(--glass-tint-ink)` at `--glass-tint-strength-floor`** / `:336+` grain `::after`; glass.css `:22` 0.30α / `:43` 1px wash blur / `:137` bg mix / **`:292 --glass-tint-ink: var(--foreground)`** / `:321` floor 4%; dark-arm.css `:196` 0.38 / `:202` grain 0.045 / `:243` floor 12%; material.css specular family (`--specular-x: var(--mouse-x, 50%)`; rest 0; `:hover` 0.1/0.08; transition track) + property-regs.css:152-161; scheme-motion.css `:46` mono stack / `:67 --duration-fast: 0.2s` / `:335-336` z 10/20; radius.css **`:16 --radius: 0.625rem` / `:18 --radius-sm: 4px` / `:19 --radius-md: 6px`** (the SC-1 decider); a11y-fallback.css three escapes; producer 8.0.0 reads — ladder.css `:61` `.glass-wash` live, `:183/:203` wash in the on-glass-muted `:where()` list, `:207 --muted-foreground: var(--on-glass-muted)`, `:196` the "collapses (1.15-3.29:1 measured in dark)" comment; tokens/glass.css `:85` wash blur **10px**; radius.css `:107 --radius: 0.625rem`; seat-run censuses — `spectrumColor` = **15 lines / 8 files / FOUR definitions** (transforms.ts:3 gamma; harmonics.ts:81; FrequencyGraph.vue:42; CoefficientsSpectrum.vue:47), `overscroll` = **6 declarations / 4 files** (all `paper/`), `pointerenter|pointerleave` = the subject's 4 bindings ONLY tree-wide, `role=|aria-|tabindex` over the subject+parent = 0, `CurveKey` = ∅, `vitest` in web/package.json = 0, `scrollbar-width: thin` = **6 sites / 2 themed**, checkA11y = 7 sites all `/visualize`+`/v/{slug}`, `/equation` in e2e = visual-baseline.spec.ts:34 alone, `legend` in e2e = 0 (two `chebyshev-and-legendre` TOC ids only), sampleColorRamp present in installed 0.13.0 (d.ts + dist); **the seat's node run `scratchpad/contrast-tinted.mjs` — the K-9 decider — computing every contested light-mode ratio on BOTH the untinted plate (D's §0 model) and the TRUE 4%-oklab-ink-tinted plate (`--glass-tint-ink` = `--foreground`, exact not approximated)**; fold-anchor verification — CENSUS-2026-08-03.md:38 · lane-frontend.md:480/:558-559/:604/:624 · intake R5-7/R6-5/R6-8 (`lane-fourier-r3-r6.md:125/:139/:142`) all read at the cited lines.

## Scoreboard

**48 rows ADJUDICATED-CONFIRMED (0 BLOCKER · 9 MAJOR booked + 2 MAJOR folded to banked identities · 19 MINOR · 20 INFO — counting the 14 unique missed rows banked/folded) · 12 killed-claims register entries (11 kills RATIFIED; 1 reader confirmation OVERRULED into the register) · 4 seat corrections minted against the readers + 1 route-sharpening (SR-1, producer 8.0.0) · 9 superlatives ratified.**

**All four axis BLOCKERs die as BLOCKERs and survive as MAJOR-or-less**, each on grounds this seat re-derived: D-1 (wrong rung — real, but text remains readable and three a11y escapes cure it for capability-users), D-2 (Level-A a11y — the information survives as text; the affordance does not), C-1 (latent at HEAD; the cure is one line in `harmonics.ts`, not here; identity = the banked census row), C-2 (fidelity, not identification — one uniquely-labelled gold row). The deepest single fact in the corpus is provenance, ratified from the commit itself: **`2e4a452` mechanism (d) INTRODUCED the C-2 swatch↔curve break in this file (3+/3-, the exact hex→token swap) in the same transaction whose mechanism (e) authored the canonical ARIA pattern this file still lacks** — the file was open, edited for colour, and skipped for keyboard.

## The incredulity seat's own work — ruled reader-vs-reader disagreements

| id | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **D §0 arithmetic (K-9)** | "I re-ran D's arithmetic independently and reproduced its table to 2dp" — confirms D-1/D-5/D-15 on those numbers | KILLED: "computed against a plate the tree does not paint" — ladder.css:216-219 tint floor omitted | **K-9 RATIFIED as premise correction; the light-arm CONCLUSIONS survive it** | This seat's node run on the TRUE plate (`--glass-tint-ink` = `var(--foreground)`, glass.css:292 — exact): the 4% oklab ink tint moves every light ratio ~2-3% (muted over hue-0 α.55 curve 2.89→2.80; over golden sum 3.61→3.51; amber 2.61→2.53; failing dots 1.41/1.50/1.38/1.76→1.38/1.46/1.34/1.72; veil 1.126 unchanged to 3dp). No light-mode pass/fail flips. Dark (12% floor) remains SS-13. R1's "reproduced to 2dp" was reproduction FROM D's premises — premise-inheritance, not independent verification (SC-2). |
| **D-L6 / C-13 (hover-sweep cost)** | KILLED as MAJOR: guards `if (!playing.value)` on both legend handlers ⇒ zero extra draws in the default autoplay state; parent's realloc already booked | CONFIRMED MAJOR "at every cited line" — while quoting the guard itself | **R1's kill WINS; R2's MAJOR is overruled into the killed register (K-12)** | Seat read of `:295-296`: both handlers ARE `if (!playing.value) draw()`; `:324` autostarts `playing = true` ⇒ default-state legend emits cost zero draws. Paused-state residue = 2 draws where 1 would do, on the parent's own unconditional-resize defect (`:88-89`, banked at lane-frontend:558-559 — seat-verified anchor). Legend-side residue booked MINOR: the unguarded leave-then-enter emit pair, cured by M7's contract collapse. |
| **D-L1 (pointer/mouse race)** | KILLED structurally: every intra-legend crossing is pointer-family-only; the overlay's 8/12px padding puts the canvas `mouseleave` strictly earlier on any normal approach; blast radius caps at the FIRST row entered direct from canvas in one pointermove | UNPROVEN: "the UA ordering decides MAJOR vs BLOCKER; I decline to guess" | **R1's kill RATIFIED; R2's escalation range is DISSOLVED — the probe now decides INFO vs MINOR only** | Seat verification: `:50 padding: 8px 12px` on `.legend-overlay` (no handler) + per-row `px-2`; row-to-row = `pointerleave`/`pointerenter` pairs with no canvas event; canvas `@mouseleave` (`:342`) fires once per transit. Worst case = one un-highlighted first row until the next row boundary. Residue INFO: the subject is the tree's ONLY pointer-family boundary site (seat grep) vs six mouse-family surfaces — a cross-family protocol inconsistency with pen/touch asymmetry. |
| **D-7 (scroll well)** | MAJOR | MINOR — the ~6-of-102-rows figure rests on `min-height: 200px`, a FLOOR; real height comes from `flex-1` | **MINOR — R2 wins** | Seat reads: EquationView :308 wraps the plot in `flex-1 min-h-0` and passes `class="flex-1"`; default rows = `min(effectiveN, 20)` with `effectiveN` floored at 3 (simplification.py:69) — typically single-digit. The affordance absence (no fade/count/collapse; `./fading-scroll` in the installed exports map with `axis?: "x"|"y"`, imported ∅ — all seat-verified) is static and books; the magnitude goes to SS-13. R1's MAJOR preserved in DISSENT. |
| **D-5 (hover veil)** | MAJOR (sole state indicator, 1.13:1, byte-identical to divider) | MINOR | **MINOR — R2 wins** | The row veil is the SECONDARY feedback channel; the primary is the canvas re-stroke (α0.55→1.0, 2.5→3.5px, `:207-208`), which is strong and immediate. Capability preserved ⇒ MINOR by this corpus's grading (the fr-ConvergenceTimeline SD-1 precedent). The veil's imperceptibility is seat-verified (1.126:1 on the true plate) and compounds ONLY where the canvas gives no feedback — which is missed row M1, booked separately at MAJOR. |
| **D-15 (swatch discs)** | MAJOR (table reproduced) | ratios UNPROVEN (§0 plate error) | **MAJOR — R1's severity survives on this seat's OWN numbers** | Seat node run on the true tinted plate: hues 171/129/86/43 = 1.38/1.46/1.34/1.72 — all under 3:1 by margins no plate correction can close; the disc is the harmonic row's only distinguishing mark (`:72-76`, no ring/border — seat-read). Light arm decided statically; dark → SS-13. |
| **D-8 (amplitude discarded)** | MAJOR | MINOR (missing feature, not broken contract) | **MINOR — R2 wins** | The contract renders `k` correctly; magnitude is enhancement. The data being one file away (`:267 A = ….toFixed(4)`) and the tabular idiom at ConvergenceTimeline:103 make the cure cheap — but cheap-to-fix is not severity. |
| **D-L4 / C-7 (stringly keys)** | MINOR (no live symptom; index space provably dense) | MAJOR (twelve untyped edges + no gate) | **MINOR — R1 wins, on precedent** | Banked precedent fr-ConvergenceTimeline L·D-12: a latent type hole with no reachable symptom grades MINOR. Seat re-verified density: `harmonics.ts` Set-dedupe → ascending sort → head slice ⇒ `k` strictly monotone in `i`; both surfaces iterate the same array. The R6-8-echo arm (interaction identity derived from parent iteration order; `parseInt(h.slice(2))` re-derives the join at read time) keeps C-7's **F.W5 carry**. |
| **D-L3 (occlusion)** | MAJOR with premise corrected (default ≠ pinned; `vizHarmonics = min(effectiveN, nHarmonics)`) | occlusion real, magnitude UNPROVEN | **MAJOR, magnitude → SS-13** | Mechanism static and seat-verified: the overlay is an input-consuming box (z 10) over the canvas — the plot's ONLY curve-picking surface (`:240-247`) — with `max-height: calc(100% - 16px)` authorising full-height growth; the k=1 extremum lands at the right domain edge inside the legend's x-band (PAD.right=12, `toScreen` :178-181). C-11's inertness note binds the cure: deleting `pointer-events-auto` un-occludes nothing; only layout does. |

**Seat corrections minted against the readers:**

- **SC-1 (against reader-1 — a FALSE correction killed).** R1's provenance kill charges D with radius-token line drift, "cited :16/:18/:19 (actual :17/:20/:21)". This seat's read of the installed `dist/styles/theme/radius.css`: `--radius` **:16**, `--radius-sm` **:18**, `--radius-md` **:19**. **D's original citations were right; R1's corrections are wrong.** R1's parallel corrections that this seat spot-checked (`@layer components` at ladder.css **:6** not :7; wash declarations :37-41 not :39-45) are right — the kill survives with this cell excised. A "correction" is a claim like any other; it inherits no immunity from being filed inside a kill.
- **SC-2 (against reader-1 — independence mislabeled).** "Reproduced to 2dp" from D's own token table is faithful re-computation of D's premises, not independent verification — K-9 shows the premise omitted the tint axis. The conclusions happen to survive (this seat's run), but the method claim was wrong, and had the tint been 30% rather than 4% it would have flipped results R1 had already "confirmed".
- **SC-3 (against reader-2, trivial)**: the C-14 cell "applied five times elsewhere" — the tree carries **6** `overscroll` declarations / 4 files (R1's count was exact).
- **SC-4 (against reader-1, scope)**: the scrollbar-theming missed row says "two of the four `scrollbar-width: thin` sites theme the thumb" — the tree has **6** such sites (EquationPanel inline, GalleryFeaturedCarousel, EquationResult, FrequencyGraph, ConvergenceLegend, HarmonicLevelGrid), 2 themed. The finding survives at the corrected scope (the ColorSpaceSelector L-9.1 scope law).
- **SR-1 (route sharpening — producer drift)**: the glass-ui producer is at **8.0.0** (`e73ec80c`), one major past D's U-3 audit. This seat re-derived U-3 at 8.0.0: `.glass-wash` is IN the `--on-glass-muted` lift list (ladder.css:203/:207), the collapse comment survives (:196), wash blur is **10px** (glass.css:85), `--radius` unchanged (radius.css:107). **U-3's land-now-re-measure-after-F.W1 sequencing stands, re-pinned to 8.0.0**; every F.W1 budget must re-pin at wave open (the fr-ConvergenceTimeline K-8 lesson, second occurrence).

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W1** tri-package uplift · **F.W2** value.js migration · **F.W3/W4** shadow retirement + frontend audit · **F.W5** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring. Identity guards fold by reference, never re-book.

### BLOCKER (0)

None survive. The four axis BLOCKERs are each demoted with written rationale (scoreboard ¶2); no crash path, no leak, no data loss, no unguarded I/O exists in this file — the component's entire script is two macro statements.

### MAJOR (9 booked + 2 folded)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **D-1 + C-3** | `.glass-wash` is the wrong ladder rung for an overlay on a live saturated canvas: 30%α plate, 1px blur, and the one gap where NEITHER 4.0.0 remedy reaches (`:where(.glass-floating,.glass-overlay)` muted-lift excludes wash; the `@container --glass-backdrop` bucket includes wash but never fires — `grep glass-backdrop web/src` → ∅). Light-arm figures now SEAT-DECIDED on the true tinted plate: `--muted-foreground` 13px = 2.80:1 over a hue-0 rest-alpha curve, 3.51:1 over the always-on golden sum — every case below 4.5:1. The repo's own convention corroborates: every other floating-overlay-over-content uses `glass-floating`; every other `glass-wash` site is in-flow — the legend is the sole wash-as-overlay. | subject :17/:89-90; ladder.css :200-204/:134-151/:216-219; seat node run; convention census (reader-1, spot-verified) | **F.W4** — rung change (`glass-floating` carries the unconditional lift today), then **RE-MEASURE post-F.W1** per U-3/SR-1 (8.0.0 wash gains the on-glass-muted lift + 10px blur; reverting to wash is a legitimate outcome). Dark-arm ratios + duty cycle → SS-13. |
| **D-2 + C-8** | Three real cross-component interaction sites are bare `<div>`s — no role/tabindex/focus/keydown/aria, and `cursor-default` actively denies the affordance — while the interaction is functional (writes `hoveredCurve`, re-strokes three curve classes, gates the tooltip). SC 2.1.1 + 4.1.2 (A). Provenance seat-ratified from `2e4a452`: mechanism (e) wrote the canonical `role="button" tabindex="0" :aria-label @keydown.enter.space.prevent` pattern while (d) edited THIS file for colour only. Zero axe coverage reaches `/equation` (7 checkA11y sites, all elsewhere — seat grep). The `:focus-visible` global ring list (style.css:129-144) names four classes and omits `.legend-entry`. | subject :18-35/:58; git show 2e4a452; e2e greps | **F.W4** — lift the 2e4a452(e) pattern verbatim + extend the ring list + `checkA11y(page, "equation")`; MUST sequence with the scrollable-region row below (its axe rule fires first). |
| **C-2** | The Sum swatch/label are `var(--viz-amber)` (hsl(35 76% 35%) light) while the sum curve strokes `VIZ_COLORS.golden` = `#f0b632` in both themes — `resolveVizColors()` assigns five keys, never `.golden` (seat-read). A dark-brown key for a bright-gold referent in light mode. **INTRODUCED by `2e4a452`(d)** — the 3+/3- diff is exactly this swap; before it the swatch was byte-identical to the stroke. One token doing two incompatible jobs (AA ink vs identity paint). | subject :78/:94; colors.ts:12/:83/:90-96; golden-shimmer.ts:52; the commit diff | **F.W2/W.L5** — the join must be made (the swatch and the shimmer pipeline reunite on one source), NOT independent migration: W.L5 ACT(2) deletes `hexToRgba`, a live dependency of golden-shimmer. Consumer-side render fix rides **F.W4**. |
| **D-9 + D-L2** | `v-if="harmonics.length"` gates the WHOLE overlay including the two rows naming curves the plot draws unconditionally (`draw()` early-returns never reference `totalH` — seat-read :82/:85/:100; at `totalH=0` the sum degenerates to the DC line). Reachable via the `amp > 1e-14` filter on a constant `f` while the plot still mounts (`compute_effective_n` floors at 3; slider floors at 1). Two labelled-nowhere curves whose hover keys stay live in the hit-test. | subject :17-27; ConvergencePlot :188-235; harmonics.ts:44; simplification.py:69-81 | **F.W4** — gate the divider + loop only; reference rows render whenever the plot mounts. |
| **D-L3** | A `pointer-events`-live overlay authorised to consume full plot height sits on the plot's ONLY hit-test surface; curves beneath it are unhittable and the tooltip is unreachable there. Premises corrected both ways (not pinned at default; `pointer-events-auto` is inert so removing it cures nothing — layout is the cure). | subject :46-54; ConvergencePlot :240-247; EquationView :52-54 | **F.W4** — layout cure (reserve a band / collapse affordance / move scroll+tier per D-L10's wrapper split). Occlusion fraction + rendered boxes at 1280×800 → SS-13. |
| **D-15** | Four of eight spectrum discs fall below 3:1 against their own plate (seat-run on the TRUE plate: 1.34-1.72 for hues 86-171-129-43), and the 10px disc carries no ring/border — it is the only thing distinguishing one `n=` row from another. Generator-level defect surfaced at this site. | subject :36/:72-76; harmonics.ts:86-87; seat node run | **F.W3/W4** — an arm of the banked ramp identity (fr-BasisCanvas M-β1 / fr-CoefficientsPanel FR-CP-2): the OKLCH/`sampleColorRamp` re-home carries lightness co-variation + a disc ring here. Dark arm → SS-13. |
| **M-R1 (missed, BOTH readers independently — merged)** | The legend does not participate in the progressive-reveal narrative: `draw()` skips harmonics whose cursor < 3 (`:205 continue` — before the hover read AND the hit-region push), while the legend renders all N rows from t=0. At t=0 (reachable by construction: mount `:324`, togglePlay reset `:276`, ping-pong loop) 100% of harmonic rows are hover-inert; at eT=0.5, N=20, rows 13-19 are. The parent already computes `activeCount` (`:298-304`) and passes it to the Timeline (`:369`, "N=3/20") but NOT to the Legend — the transport and the legend tell contradictory stories on the same screen. One prop + one `:class` cures it. | ConvergencePlot :205/:298-304/:357-362/:369; harmonics.ts:66-76 — all seat-read | MAJOR · **F.W4**. Duty cycle (time parked at low t) → SS-13. The convergence of two independent hostile readers on this row is itself evidence of its reality. |
| **M-R2 (missed, reader-1)** | The scroll well is an axe `scrollable-region-focusable` violation (wcag2a, serious) whenever content overflows: a scrollable region with zero focusable descendants and no `tabindex` — keyboard users cannot scroll the legend AT ALL, a strictly larger failure than the missing hover path. **Seat correction: "guaranteed" → CONDITIONAL on overflow at scan time** (single-digit default rows may not overflow; the rule fires when they do). Sequencing is the sharp part: the moment D-2's own repair adds `/equation` to checkA11y, this rule fails the gate before any hover finding. | subject :47-48/:17-39; visualization-ux.spec.ts:29 tag set — seat-read | MAJOR · **F.W4** — `tabindex="0"` on the well (or focusable rows per D-2's cure), sequenced WITH D-2. |
| **M-R3 (missed, reader-1)** | SC 1.4.1 Use of Color (A): the harmonic-row↔curve association is carried by hue alone — solid disc, identical typography, no pattern/label link — and the only non-colour disambiguator (the KaTeX tooltip) is pointer-only (D-10). The two REFERENCE rows are redundantly encoded (glow `:79`, dashed ring `:83`) — the failure is confined to the harmonic stack, and the cure pattern already exists twice in-file. | subject :36-37/:79/:83; ConvergencePlot :207/:260-270 | MAJOR · **F.W4** — rides the D-2 a11y pass + the ramp identity's redundant-encoding arm. |
| **M-R4 (missed, reader-1) — FOLDED** | Hue step-size collapse: adjacent swatches differ by 300/(N−1)° — ~15.8° at N=20, **~3.0° at N=100** — far below JND for a 10px disc through a translucent plate. Combined with D-15 + D-7 the legend is decorative above N≈25. | harmonics.ts:86; FunctionInput :184 | MAJOR · **FOLD → banked fr-BasisCanvas M-β1 / fr-CoefficientsPanel FR-CP-2** as a new quantitative arm of the ramp identity; never re-booked. |
| **M-R5 (missed, BOTH readers — FOLDED, identity guard)** | Same-`n`-different-colour across co-mounted components: the legend colours grouped `|n|` (DC dropped, ascending, denominator ≤ min(effectiveN, 20)) while `CoefficientsSpectrum` (same `/equation` view) runs a PRIVATE byte-identical `spectrumColor` over the raw signed slice (`slice(0, expanded ? 40 : 12)` — seat-read) — different index space AND denominator; plus the gamma-warped fourth fork in `visualization/`. **Both readers filed this as a NEW missed find; the identity guard catches it as ALREADY BANKED**: fr-BasisCanvas M-β1 (four forks, one ramp home), fr-CoefficientsSpectrum B-3/M-10, fr-CoefficientsPanel FR-CP-2 (denominator divergence). | seat grep: 15 lines / 8 files / 4 definitions | MAJOR · **FOLD → M-β1/FR-CP-2** with the legend-pair rider (index-space mismatch: grouped-|n| vs raw-signed-incl-0). This record's contribution is the rider, not a booking. |

### MINOR (19)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-7** *(seat-demoted from R1's MAJOR)* | Up to 102 rows in a scroll well with no fade edge, no count, no top-N collapse; `./fading-scroll` (axis="y") sits in the installed exports map, imported zero times. Magnitude figures rest on a height FLOOR, not the shipped height. | **F.W4** — FadingScroll + count affordance; visible-row measurement → SS-13. |
| **D-8** *(seat-demoted)* | `amplitude` arrives in the prop and is discarded; rows are ordered by the least informative key. The data is rendered one file away (`:267`) and the tabular idiom exists at the sibling (`ConvergenceTimeline:103`). | **F.W4** — amplitude encoding (dot area or `A=` column), same edit as D-7. |
| **D-5** *(seat-demoted)* | The row's hover veil computes 1.126:1 (seat-verified, tint-invariant) and is byte-identical to the divider ink — the two are indistinguishable. Secondary channel; the canvas re-stroke is primary. | **F.W4** — one contrast pass with D-6 + M-R8. |
| **D-6 + D-L9 + C-10.1** | `rgba(180,180,180,0.6)` — the file's only literal colour: no token, no dark arm, ~1.5:1 light; third non-agreeing spelling of the f(x) identity (0.55 rest / 0.85 hover at the plot). The 2px dash on a 10px `border-box` circle renders as specks. Token GAP (no `--viz-` neutral exists) — the fix mints upstream. | **F.W4** + the C-2 join (one neutral identity source). |
| **D-L4 + C-7** *(ruled MINOR)* | The curve-key protocol (`"sum" | "original" | h-${number}`) is `string` across six edges in three modules incl. a hand-rolled `parseInt(h.slice(2))` parser; no gate can catch a rename. Latent — index space provably dense (seat-verified). | **F.W4** — `CurveKey` union + `harmonicKey(i)` in `lib/harmonics.ts`; the R6-8-echo arm CARRIES → **F.W5** (child identity derived from parent iteration order, the join re-derived at read time). |
| **D-L6/C-13 residue** *(post-K-12)* | The legend emits unguarded and identity-free: leave-then-enter per crossing, no `key === current` check, against the parent's own guarded canvas path (`:246`). In the paused state this doubles draws; the heavy cost is the parent's banked unconditional-resize. | **F.W4** — M7's contract collapse (`hover: [key: string | null]`) halves emits and deletes the intermediate null state; parent cost stays at its own row (lane-frontend:558-559 → the ConvergencePlot adjudication). |
| **C-5** | The 4.0.0 specular contract fires on `:hover` but is never wired (`grep useSpecularTracking|mouse-x web/src` → ∅): the gleam wakes at 0.1 and pins at 50%/50% — a static centred blob with an interpolation track that can never fire. Magnitude sub-decidable statically. | **F.W4** (wire or suppress) + **GLASS-RELAY**: the library's "an unwired rung is clean" dormancy guarantee is defeated by its own `:hover` arm for any hoverable rung — relay per the standing BH edict. |
| **D-3** | Two unit systems in one 53-line stylesheet, split 6-rem/8-px, torn by the app's own sub-768px root bump (1.125rem — seat-read): gaps/pads inflate 12.5% on the phone while type/swatch/corners freeze; the `top-2`/`calc(100% - 16px)` symmetry breaks 9/7. | **F.W4** — one unit pass. Mobile REACHABILITY of the legend (the `mobileView`/`isDesktop` gate) → SS-13 first. |
| **D-4** | Nested-radius inversion: child `.rounded` = `var(--radius)` = 10px inside a parent 8px corner with 8/12px padding (concentric wants 0). Surfaces on hover. `--radius-sm: 4px` is the nearest rung. Neither heals nor worsens across the uplift (radius identical at 8.0.0 — seat-read). | **F.W4** — `rounded-sm` + the D-13 literal in one pass. |
| **D-L11 + C-4** | The `--viz-amber` AA carry names `--background` as its denominator; at this site the backdrop is a tinted translucent plate over painted canvas. Seat-run: amber = 4.56:1 over page-only (carry holds) but 3.17:1 over the golden sum and 2.53:1 over a hue-0 curve — 13px/600 is normal text; the 4.5:1 floor is missed in light. | **FOLD → the held carry at lane-frontend:604** as a second denominator (never a new carry). Dark → SS-13. |
| **C-12** | `z-index: var(--z-content)` (10) files interactive floating chrome in the substrate band; nine comparable overlays use `--z-controls` (20), two use content. The ordering WITHIN the band (legend under tooltip) is right and token-expressed. | **F.W4** — one token swap. |
| **C-14** | No `overscroll-behavior: contain` on the scroll well; the house convention exists at 6 declarations / 4 files (all `paper/` — seat count) and this container departs from it, chaining wheel/touch to the page under the pointer. | **F.W4** — one declaration. |
| **D-10** | Hovering the legend never yields the KaTeX tooltip (`mousePos` written only by `onCanvasMove` — seat-verified :244/:251); the labelled, discoverable surface is the less informative one, order-independently. | **F.W4** — either seed `mousePos`-equivalent positioning from the row, or surface `A=` in-row (D-8's cure covers it). |
| **D-14 + D-L8 + C-10.3** | The legend swatch draws at α=1 while the plot strokes the same harmonic at α=0.55 at rest — the key matches only when already hovered. Defensible legibility choice on a 10px disc; undocumented. | **F.W4** — either document or encode rest-alpha; rides the ramp arm. |
| **D-11** | `font-family: "Fira Code", monospace` drops the metric-matched `"Fira Code Fallback"` swap face the system ships (`--font-stack-mono`, bridged to `--font-mono`); the app declares no Fallback face (seat-checked reader evidence). Three-site sweep: legend :88, ConvergenceTimeline :97, ConvergencePlot :390. | **F.W4** — `var(--font-mono)` sweep; the Timeline site is already booked at fr-ConvergenceTimeline D·D-11 — ONE sweep identity, this row adds the two remaining sites. |
| **C-15** | The swatch ramp is hand-rolled constant-nominal-L HSL while `sampleColorRamp` is live in the INSTALLED 0.13.0 (seat-verified in d.ts + dist; the HEAD commit message itself carries "sampleColorRamp M.W7 book dischargeable-on-adopt"). | **FOLD → the ramp identity (M-β1/FR-CP-2)**, which already adopted the F.W2 `sampleColorRamp` angle; this row is its legend-site instance. |
| **D-L10 + C-6.1** | Glass tier and scroll container share one box: the material's abspos `::before` (specular) and `::after` (grain) ride the scrolled content and de-laminate from the plate; the host-level plate stays put. Mechanism fully static; visible consequence near-nil at grain 0.025/0.045 + specular 0-at-rest. | **F.W4** — tier on a non-scrolling shell, scroll on an inner wrapper (the same edit is D-L3's layout carrier). Perceptibility → SS-13. |
| **M-R6 (missed, reader-2 — seat-confirmed)** | `.legend-divider` (height 1px, no `flex-shrink: 0`, no content ⇒ automatic minimum 0) is the column's ONLY shrinkable item: when the well overflows, the flex algorithm collapses it to 0 while its 3px margins survive — the IA's one structural separator vanishes exactly when the list is long. The author knew the idiom (`shrink-0` on `.legend-dot`, :73). One token cures it. | **F.W4** — rides D-5's contrast pass. (Mooted-in-practice by M-R8's finding that the divider is sub-perceptual anyway — both cures are one edit.) |
| **M-R7 (missed, reader-2 — seat-confirmed)** | The reverse channel has no scroll-into-view: a canvas hover marks `is-hovered` on a row that may be scrolled out of the well — the component has no ref, no watch, no script beyond two macros — so the highlight silently lands off-screen. | **F.W4** — a `watch` + `scrollIntoView({block:"nearest"})` on the hovered key, or accept and document. |

### INFO (20)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-12 + C-9** | `transition: background 0.1s` — shorthand, raw literal, implicit `ease` — in the one file the A.W3.d motion sweep missed; the sibling carries the sweep's own annotation. C-9's `--duration-fast ≡ 150ms` cell is KILLED (0.2s at scheme-motion.css:67 — seat grep; 150ms is the `var()` fallback literal). | **F.W4** one-liner. |
| **D-13** | Hardcoded 8px corner vs the sibling tooltip's `rounded-md` (6px) in the same container; drifts on any `--radius-md` retune. Radius genuinely the consumer's (wash rung declares no geometry — seat-read :37-41). | **F.W4** — rides D-4. |
| **D-16 residue** | `select-none` on the label is redundant (ancestor `:383` already suppresses); the cost claim is killed (K-5). | **F.W4** — delete or leave; the copyability decision is the PARENT's. |
| **D-17** | No PRM block — correctly NOT a WCAG defect (100ms background fade); the real reduced-motion gap is the parent's ungated rAF, already booked (lane-frontend:624 — seat-verified anchor). | Consistency note; no separate booking. |
| **D-18** | No list semantics for up to 102 items; the canvas has no role/label/fallback/`aria-describedby` while this component holds its only textual description. | **F.W4** — `role="list"`/`listitem` rides D-2; the canvas half is the parent's row. |
| **D-19** | Vue scoped styles are unlayered; glass-ui's ladder opens `@layer components` (ladder.css:6 — seat-verified) — unlayered wins categorically, which is WHY every px literal above accumulated with no diagnostic (and why `.legend-overlay`'s `position:absolute` correctly beats the rung's `relative`). | **NO-WAVE-OWNER** — the durable fix is a lint rule (unlayered-scoped-vs-system), a spec-authoring input for SS-3/SS-4, not a per-component edit. |
| **D-L1 residue** *(post-kill)* | The subject is the tree's only pointer-family boundary site (4 bindings) against six mouse-family surfaces — a cross-family protocol inconsistency with pen/touch asymmetry; blast radius capped at first-row-highlight per K-1. | **F.W4** — unify family in the M7 contract edit; probe demoted to INFO-vs-MINOR → SS-13 (optional). |
| **D-L5** | R5-7 class, second true instance: the entire variable surface is one native `v-for`, zero component instances — instance-keyed derivations read 0 of ≤102 rows. | **FOLD → intake R5-7's standing F.W4 carry** (+1 instance; the carry is already booked — never re-booked). |
| **D-L7** | `TrigHarmonic` declared twice in the feature folder (harmonics.ts:10-15 ≡ useCurveTransition.ts:10-15 — seat-read); the parent's call compiles on structural identity. Not this file's defect. | **F.W4** hygiene — single declaration; INFO at this seat. |
| **D-L12** | `:key="h.k"` vs index identity — the axis's own falsifier killed it (k strictly monotone in i — seat re-derived) and REPORTED the non-survival. No defect at HEAD; latent under future TransitionGroup. | Preserved as method + latency note. |
| **D-L13** | Zero behavioural coverage: no vitest (seat grep), `/equation` reached only by the screenshot sweep — which would BASELINE the occlusion. | **F.W4** — one functional scrub/hover spec + the checkA11y addition; identity = census vitest-absent row (folded). |
| **D-L14** | 3× template triplication; extraction correctly DECLINED under the repo's KISS law; a local `computed` rows array is the cheapest carrier for the D-L4 fix. | Rides the D-L4 edit. |
| **C-1 + U-2** | The sole import edge transitively carries the value.js root-export break (`harmonics.ts:5` bare root; producer 4.0.0 has no `"."` — both ends seat-read). Latent at HEAD (installed 0.13.0 exports `['.']`). | **FOLD → CENSUS-2026-08-03:38 + lane-frontend:480** (banked; `ConvergencePlot.vue:5` — the legend's only mount — is IN the five-site inventory, seat-verified, so no undercount). Cure = one line at harmonics.ts:5, **F.W2**; C-1's module-split suggestion (grouping·progress·colour) rides the same wave as hygiene. |
| **C-11** | `pointer-events-auto` is vestigial — no ancestor sets `none` (seat-checked); load-bearing AGAINST D-L3's framing (removing it un-occludes nothing). | **F.W4** — delete in the layout edit. |
| **M-R8 (missed, reader-1)** | The divider measures 1.126:1 (same mix as the veil — seat-run): the IA's one structural separator does not render on any display. Corrects D's S-3 cell ("a whisper, not a rule" — it is neither). | Rides D-5's pass. |
| **M-R9 (missed, reader-1, seat-corrected scope)** | `scrollbar-width: thin` with no `scrollbar-color`/`::-webkit-scrollbar` theming — 2 of **6** sites theme the thumb (SC-4); this is the only one on glass over animated canvas. | **F.W4** cosmetic. |
| **M-R10 (missed, reader-1)** | Zero file-level documentation where both neighbours carry rationale docblocks — and the undocumented choices are exactly the contested ones (`:key` identity, α=1 swatch, amber-for-golden, the rung). | **F.W4** — a four-line header docblock in whichever wave touches the file first. |
| **M-R11 (missed, reader-1 — seat-DEMOTED with mechanism correction)** | Touch-scroll spurious highlight. **Seat correction: the highlight is NOT "held for the entire gesture"** — when the UA claims the drag as a scroll it fires `pointercancel` then `pointerout`/`pointerleave` (Pointer Events §compat), so the row's `@pointerleave` clears it: the artefact is a transient flash + one paused-state draw pair per scroll attempt, not a held selection. | INFO · **F.W4** — dissolves under the M7 contract edit; reachability on phone → SS-13. |
| **M-R12 (missed, reader-2 — seat-demoted from MINOR)** | Every hover boundary re-renders the whole legend subtree (fresh `:class`/`:style` object + `spectrumColor` string per row per render; 2 renders per crossing). ~1-2k vnode constructions per sweep — well under frame budget; the load-bearing cost is the parent's banked realloc. | INFO · halved for free by M7; memoise the ramp on `harmonics.length` in the ramp re-home. |
| **M-R13 (missed, reader-2 — the cure carrier)** | `leave: []` is identity-free — the parent cannot distinguish row-transition from legend-exit, which is the STRUCTURAL cause of the null-then-set double write; a guard cannot help because leave carries no key. `hover: [key: string | null]` collapses the pair, removes the intermediate null, halves draws, and closes the D-L1 residue window order-independently. | **F.W4** — THE contract edit the D-L6 residue, D-L1 residue, M-R11 and M-R12 all ride. |
| **M-R14 (missed, reader-2 — FOLDED, identity guard)** | `class="flex-1"` on `<ConvergencePlot>` is dropped by Vue's fragment-root rule (two root nodes — seat-read :336-375); dev-warns per mount; inert only because `.convergence-container` re-declares it. Filed by R2 as new; **ALREADY BANKED at fr-ConvergenceTimeline M-1**. | **FOLD → fr-ConvergenceTimeline M-1**; this record adds one rider: the readers' occlusion arithmetic reasoned from `min-height` precisely because the REAL height mechanism is the scoped flex-1, not the dropped call-site one. |

## Killed-claims register — 12 entries

| # | killed claim | refutation (adjudicated) | seat instrument |
|---|---|---|---|
| K-1 | D-L1's BLOCKER-escalation branch ("legend hover dead on arrival for mouse users") | Structural kill RATIFIED: intra-legend crossings are canvas-free pointer pairs; the overlay's 8/12px padding orders `mouseleave` strictly before any row `pointerenter` on normal approach; worst case = the first row of a single-frame direct entry. Probe decides INFO vs MINOR only. R2's UNPROVEN-at-MAJOR/BLOCKER framing is dissolved. | subject :50; ConvergencePlot :342; seat pointer-family census |
| K-2 | C-6.2's "material pseudos paint above the text" as a component defect | True CSS, wrong ledger: uniform producer behaviour on every glass tier (twelve-selector rule — seat-read), caused by no consumer act, cured by no consumer edit; specular is 0 at rest. GLASS-RELAY item, not a subject row. | material.css :65-96 |
| K-3 | C-6's "harmonic count ranges to 4096" | Cross-domain citation: 4096 is the VISUALIZATION pipeline (visualization.py:126/:187/:281); the equation pipeline caps `le=200` (equations.py:20) and the UI at 100 (FunctionInput:184), sliced by `groupTrigHarmonics`. Worst case 102 rows. | seat reads of both model files |
| K-4 | D-L3's "pinned at max-height in the DEFAULT configuration" (594px from N=20) | `autoHarmonics = ref(true)` ⇒ `vizHarmonics = min(effectiveN, nHarmonics)`; `effectiveN` floors at 3 and is typically single-digit. Occlusion survives at high N; the default-config framing does not. | EquationView :43/:52-54; simplification.py |
| K-5 | D-16's "costs the ability to copy a harmonic index" | Self-defeating: the ancestor `:383` already applies `select-none`; deleting the label's copy restores nothing. Residue = redundant declaration (INFO). | ConvergencePlot :383 |
| K-6 | D-6's "contradicts the honesty bar of `2e4a452`" | Misreads the commit: sweep (i) = 9 `#f0b632` hex hardcodes, sweep (ii) = 11 Tailwind `text-muted-foreground/NN` alpha-modifier utilities. `rgba(180,180,180,0.6)` is in neither enumeration; the 3+/3- diff touches only the golden pair. The defect stands on its own arithmetic; the rhetoric dies. | git show 2e4a452 (message + diff, whole) |
| K-7 | C-1's BLOCKER severity + "a specifier-grep F.W2 inventory UNDERCOUNTS affected components" | The legend's only mount is `ConvergencePlot.vue:5` — itself in the five-site inventory (lane-frontend:480, seat-verified), so the specifier grep already books the failing subtree; the break is latent at HEAD; the cure is one line in harmonics.ts. INFO at this component, MAJOR at the specifier — already banked. | ConvergencePlot :5; installed+producer manifests; lane-frontend:480 |
| K-8 | D S-1's falsifier cell ("`grep spectrumColor` → three sites; no duplicated hue formula; drift structurally impossible") | THE FALSIFIER FIRES: 15 lines / 8 files / FOUR definitions incl. a gamma-divergent fork and two byte-identical private copies (seat grep). The narrow legend↔plot superlative survives; the universal claim is dead — and the fork census is ALREADY BANKED (fr-BasisCanvas M-β1), which neither reader's "missed" filing noticed. | seat grep |
| K-9 | D §0's composite-plate table as cited (and every downstream published ratio) | The shipped wash plate is `color-mix(in oklab, bg-wash, ink 4%)` light / 12% dark (ladder.css:216-219 + glass.css:321 + dark-arm.css:243 — all seat-read); D's model omits the tint axis. RATIFIED as premise correction — **with the seat's quantitative closure: in light the correction is ~2-3% relative and flips NO conclusion** (`--glass-tint-ink` = `--foreground` exactly, glass.css:292). Dark-arm ratios remain unproven → SS-13. | seat node run `contrast-tinted.mjs` |
| K-10 | D's axis-wide line-citation integrity claim ("every input token cited to file:line…") | Ratified with ONE cell excised: D's anchors into ConvergencePlot/ConvergenceTimeline are systematically off by −6..+10 against a tree that is NOT the problem (both readers + this seat's spot checks agree) — but R1's radius.css "corrections" inside this kill are THEMSELVES wrong (SC-1): D's :16/:18/:19 were right. Findings survive on content; anchors do not; F.W4's repair ledger must re-resolve every D anchor. | seat reads (radius.css, ladder.css :6, wash :37-41) |
| K-11 | L's D-L8 dismissal ("static amber dot approximating a shimmer = design decision, not counted") | The axis found the file's sharpest fidelity defect and declined to count it on a mis-diagnosis: not shimmer-vs-dot but two different COLOURS from two never-joined token families, introduced by the very commit the D axis lionised. C caught it (C-2); D missed it; L saw and waved. Killed as a dismissal; C-2 books at MAJOR. | colors.ts; the commit diff |
| K-12 | R2's D-L6/C-13 confirmation AT MAJOR (44 emits → 44 reallocations → ~1.3M trig evals "checks out") | OVERRULED: R2's own quoted evidence contains the `if (!playing.value)` guard; with `:324` autostart the default state costs zero legend-triggered draws, and the paused-state cost is 2-for-1 on a defect owned and banked by the parent. Mechanism cells adopted; severity + attribution die. Residue booked MINOR (the emit contract). | ConvergencePlot :295-296/:324 — seat read |

## Superlatives — ratified with provenance (L-18 runs both ways)

- **S-1 · Honestly stateless** (D S-4 / L S-1/S-2 / both readers — ratified). The script is two macro statements; zero refs, watchers, lifecycle, listeners, timers. Every pixel is a pure function of `(harmonics, hoveredCurve)`; the teardown class is ABSENT, not handled — against a parent dismantling four resources. The correct disposition of all ~48 rows is repair-in-place, never rewrite.
- **S-2 · DTO-free props contract** (C SUP-1 — ratified). `TrigHarmonic[]` (domain view-model), never `FourierTermDTO`; the wire shape collapses once in `harmonics.ts:21-49` and never crosses. The exact inverse of intake R6-8's seam failure — this leaf already has the shape R6-8 demands.
- **S-3 · Zero component imports; the glass coupling is one class, live at BOTH ends of the hop** (L S-3 / C SUP-2 — ratified and extended by this seat: `.glass-wash` verified live at installed 4.0.0 ladder.css:37 AND at producer **8.0.0** ladder.css:61 — one major further than either reader checked). The F.W1 break surface costs this file zero lines.
- **S-4 · Riding the ladder rung buys three capability escapes free** (D S-2 — ratified; all three blocks seat-read in a11y-fallback.css). Under `prefers-reduced-transparency` the plate goes opaque `--card` — CURING D-1 outright for those users — while the sibling's hand-rolled glass gets none of the three. Right structural call, wrong rung on the right ladder.
- **S-5 · The 3.1→4.0 rename is minimal and exact** (C SUP-3 — ratified against the seat's own `git diff`: one hunk, `glass-subtle → glass-wash`, zero stragglers tree-wide).
- **S-6 · IA correctly ranked, over-design declined** (D S-3 — ratified WITH two corrections: C-12 shows the z-BAND is wrong though the intra-band order is right; M-R8 shows the "whisper" divider does not render at all). Exactly one element carries emphasis; the colour-every-label temptation was one line away and refused.
- **S-7 · `:key="h.k"` uses the domain identity** (C SUP-4 — ratified) — the in-file proof the correct identity was AVAILABLE, which is what makes the index-keyed hover protocol a defect of choice; and **L's D-L12 self-kill** (reporting a claim that failed its own falsifier, reasoning shown — seat re-derived, correct) is the corpus's best epistemic discipline, preserved as method.
- **S-8 · `shrink-0` on `.legend-dot`** (reader-2 — ratified at :73): the one flex child whose fixed box needed protection got it — which converts M-R6 from unknown-unknown to plain omission: the same token was needed 7 lines later and not written.
- **S-9 · The two reference rows are redundantly encoded** (reader-1 — ratified: glow :79, dashed ring :83): the 1.4.1 cure pattern exists twice inside the file that fails it; the harmonic stack needs only what Sum and f(x) already have.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **Dark-arm contrast ratios** for D-1 / D-15 / D-5 / D-L11 — the 12% tint floor is material there; light arm is seat-decided static (K-9 closure).
2. **Occlusion fraction + rendered boxes** at 1280×800 (D-L3 / D-7): overlay bbox vs canvas, visible-row count, real container height (the floor-vs-flex-1 correction).
3. **Duty cycle** of curve-under-legend overlap (D-1) and of the inert-row state (M-R1 — time parked at low `t` while paused).
4. **Mobile reachability**: whether the legend renders at all under 1024px (`mobileView` toggle + `isDesktop` gate, EquationView :44-45) — gates D-3's phone consequences and M-R11's touch class entirely.
5. **Perceptual halves only**: the pinned 0.1-opacity specular blob on hover (C-5) · the grain/specular scroll-away seam (D-L10) · the sub-3:1 discs as rendered through real compositing (D-15 confirmation theatre for the cured state).
6. **The D-L1 dispatch-order probe** — demoted: decides INFO vs MINOR only (K-1's cap); optional.
7. **Post-F.W1 re-measure** of `.glass-wash` under producer 8.0.0's on-glass-muted lift + 10px blur over a hue-0 curve — decides rung-revert (U-3/SR-1); do not assume either direction.
8. axe run on `/equation` — `scrollable-region-focusable` (fires iff overflowed) before the hover rules (M-R2's sequencing).

## Identity guards (anti-rename) — folds by reference

- The **ramp identity** (four `spectrumColor` forks / one ramp home / theming / `sampleColorRamp`) is banked at **fr-BasisCanvas M-β1** + **fr-CoefficientsSpectrum B-3/M-10** + **fr-CoefficientsPanel FR-CP-2** — this record FOLDS M-R4 (step-size collapse), M-R5 (cross-component divergence rider), C-15 (legend-site instance), and D-15's generator half into it. One identity, never re-booked per surface.
- The **fragment-root/class-drop** row is banked at **fr-ConvergenceTimeline M-1** — M-R14 folds with one rider.
- The **value.js root-export break** is banked at **CENSUS-2026-08-03:38 / lane-frontend:480** — C-1/U-2 fold; the inventory-undercount corollary is killed (K-7).
- The **`--viz-amber` carry** stays ONE carry at **lane-frontend:604**, gaining the second denominator (D-L11/C-4).
- The **mono-stack sweep** identity includes fr-ConvergenceTimeline D·D-11's site — one three-site sweep, not three bookings.
- **R5-7** keeps its standing F.W4 carry (+1 instance, D-L5); **R6-8**'s lesson generalises via C-7's F.W5 carry; the parent's unconditional-resize + ungated-rAF stay the PARENT's rows (**lane-frontend:558-559/:624**) for the ConvergencePlot adjudication — nothing of theirs is booked here.
- **GLASS-RELAY** (single relay, three items, per the standing BH edict): (1) the `:hover::before` specular arm defeats the documented unwired-surface dormancy guarantee (C-5); (2) no documented contract for a glass-tier host that is also a scroll container (C-6.1/D-L10); (3) rung guidance for consumer overlays over an unknown CANVAS backdrop + the `--viz-amber` second-denominator note (with lane-frontend:604).

## DISSENT — preserved, not ruled away

1. **Reader-1's MAJOR grades for D-5 / D-7 / D-8 are defensible** under a grading that ranks the legend's fitness-for-purpose (discriminating N series) as first-class rather than capability-preserving polish. This seat demoted all three on the corpus's own precedent (capability preserved ⇒ MINOR); a forming X·F spec that grades instrument-legibility first-class may restore them. The mechanism cells need no re-derivation either way.
2. **R2's D-L6 MAJOR (K-12) is the seat's closest call**: if "paused inspection" is the legend's design-basis state (it is the state in which a user READS a legend, as C-13 argued), the 2× draw on every crossing is a first-class cost. It is overruled on attribution (the expensive half is the parent's banked defect), not on reality.
3. **Both readers failed the identity guard on the fork census** (M-R5/M2) and R2 failed it on the fragment root (M-R14) — filing banked rows as new missed finds. Recorded as fleet evidence, not tree evidence: hostile re-readers attack the corpus in front of them and do not sweep the registry; the apotheosis seat exists partly for this.
4. **R1's kill-craft is the corpus's strongest and carries its one false cell** (SC-1): the radius line "corrections" were wrong while the kill they decorated was right. Corrections inherit no immunity; the citation-inheritance lesson (ColorSpaceSelector DISSENT-1) now has a corrections-flavoured instance.
5. **The 8.0.0 producer facts (SR-1) are substrate drift against a moving target** — the pin is 4.0.0 and nothing breaks today; every uplift-derived figure here must be re-pinned at F.W1 open (third occurrence of this pattern; the re-trigger belongs in the F.W1 charter).

---

**VERDICT — DEFECTIVE, ADJUDICATED**: 0 BLOCKER (all four axis BLOCKERs demoted with written rationale) · 9 MAJOR booked + 2 MAJOR folded to banked identities · 19 MINOR · 20 INFO across 48 confirmed rows (14 unique missed rows banked or folded, one with a seat mechanism-correction); 11 kills RATIFIED + 1 reader confirmation overruled; 4 seat corrections minted against the readers, including one false correction and one premise-inherited "independent" verification; the light-mode contrast arm is now decided statically on the true tinted plate by this seat's own instrument, the dark arm rides SS-13. The file is honestly stateless, DTO-free, and import-clean — its defects are one wrong rung, one severed colour identity, one missing ARIA pass, and one ramp that is already owned by a banked identity; one F.W4 pass plus the F.W2 colour-join and the single three-item GLASS-RELAY dissolve the worst of it. Routings are M-25 agglomeration input; nothing here stamps a wave.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3. This document is the sole write.
