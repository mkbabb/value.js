claude-fable-5 (served model id)

# fr-VisualizationView — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: apotheosis adjudicator r2 · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-25 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/VisualizationView.vue` (486 lines, working tree; still ` M` dirty — the two-hunk `UnderlineTabs → SegmentedTabs` rename) at fourier HEAD `cd26c653` on `m/w1-bump-migration` (re-verified this session; `web/package.json` also dirty) · installed `@mkbabb/glass-ui` **4.0.0** (node-read this session) · registry tree value.js `tranche-u` @ `8c66314a`.
**Corpus adjudicated (r2)**: two fresh hostile re-reader returns — **reader-A** (design-and-usage: 19 confirmation cells · 8 kills · 4 misses · 7 superlatives) and **reader-B** (library-and-code: 31 confirmation cells · 8 kills · 4 misses · 9 superlatives) — over the same three axis challenges (D-design · L-library · C-consumption), adjudicated against each other, against the tree, and against the r1 record at this path.
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-25-a.m. r1 apotheosis at this same path, which adjudicated a *different* reader pair. The r1 roster (56 register rows · 11 kills · 10 missed finds M1–M10 · 13 SS-13 probes) is **carried whole below under its original ids** (anti-rename); r1 rulings stand except where this corpus's evidence corrects them in **exactly two places** (corrections C-1 and C-2 below: the dropped L-27 row, and the D-18 ease-token provenance cell). Where r1 content is restated it was re-verified, not copied, except where marked *(r1-carried)*.
**Duplication disclosure**: reader-A detected the r1 record mid-read and filed a process note declaring it did NOT transcribe — every cell re-derived with its own instruments. This seat tested the claim: reader-A's two refinements (the both-ways D-18 provenance, the unlayered-cascade MAJ-4 mechanism) are facts the r1 record does not contain and could not be inherited from it; its concurring cells reproduce banked figures (1.77/2.71) from stated independent arithmetic. Non-inheritance accepted as demonstrated. Reader-B cited the r1 record openly and by line, which is the fold discipline working as designed, not inheritance.
**Method (incredulity)**: every inter-reader divergence, every r1-touching claim, and every NEW missed find was re-derived against the bytes by this seat before adoption; suspiciously-agreed cells (the D-10 border-none chain, the D-13 tag refutation, the 1.77/2.71 recompute) were checked for citation-inheritance and cleared — both readers' D-10 derivations state independent instrument paths and now make the r1 K2 ruling **thrice-derived**. Zero browser probes (probe parsimony); product source READ-ONLY; **sole write = this file**. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are forming; routings are M-25 agglomeration input; nothing here stamps a wave.
**This seat's own instruments (all static, read-only, this session)**: full subject read (486 — verifying `:145-153` overlay-under-`<Transition name="fade">`, `:220` bare toggle, `:230-232` EquationPanel fade, `:235/:238` twin `.controls-overlay` blocks with NO Transition, `:402` `var(--duration-mid, 0.24s) ease` (the file's only bare `ease`), `:415-416/:414` base insets, `:421` `overflow: visible`, `:426-428/:483` dead rules, `:439-440` opacity-only fade recipe, `:450-458` dock anchor, `:461-474` dock-centered, `:482-485` the 900px block); `style.css:40-50` (html `1.125rem/1.75rem` → 768px `1rem/1.5rem`), `:83-85` (`[data-state="active"][role="tabpanel"]`), `:107-111` (`@utility cartoon-card` → `var(--card)`); `useImageUpload.ts:28-45` (`handleDrop` sets `isDragging=false` at `:34` BEFORE processing — the fade-leave trigger); `gallery.ts:195-236` (`recordView`'s own comment: "GET … increments the view counter server-side"; `publish`'s `etags.get(slug) ?? (await api.getVisualization(slug)).etag`; `resetAndFetch` inside publish; the `_imageSlug` doc); `workspace.ts:340-470` (`saveVisualization` writes `visualizationSlug/ETag` at `:358-359` and reads `animationSettings.value` at `:351-356`; `setVisibility` `:373-389` ETag replay; `reset()` `:412-426`; the 24-member export block); **`api/routers/visualizations.py:255-271`** — the `$inc: {views: 1}` is unconditional after the not-found gates (the owner/visibility checks gate visibility only; even the owner's draft GET increments); `CanvasControlsDock.vue:40-100` (Sigma inside the `v-if="!isEditing"` template; publish Tooltip gated `hasContour` only, `is-active` paint-only, zero `disabled`; Fullscreen outside the arm); `EquationPanel.vue:117-125` (`.eq-panel` absolute, `top: 3.5rem; left: 0.5rem`); `placeholder.ts:25-36/:33/:54-60` (box `min(280,w*0.6)×100`; stroke 0.25 at `:33`; fill/font/msg `:55-60`); installed glass dist: `scheme-motion.css:216-219` (`--ease-standard/--ease-in/--ease-out-expo` **defined here too**) + `:336/:341` (`--z-controls: 20` / `--z-overlay: 50`), `bridges.css:320-330` (`--ease-spring*`, `--ease-standard`, `--ease-out-expo`, `--ease-apple`, `--ease-spring`), `--duration-mid` grep over dist → **0**, `--ease-apple-spring` → **0**; **zero-consumer censuses over `web/src` excluding the store file**: `setVisibility` **0** · `loadVisualization` **0** · `loadSnapshot` **0** · `invalidateInFlightComputation` **0** · `visualizationETag` **0** · `defaultContourSettings`/`defaultAnimationSettings` **0** (consumers import `lib/defaults` directly) · `visualizationSlug` 0 store-reads (hits are comments + draftStorage field names) · `.revision` 0 outside stores · `deleteVisualization` — the store action 0, `gallery.ts:168` calls `api.deleteVisualization` RAW (stranding the store's slug/ETag); **compute-action census**: `store.extractContour|computeEpicycles|computeBases` = exactly **3** call sites, all `ContourSettings.vue:126/:129/:130` (inside `runCompute`); challenge files read verbatim at every killed/corrected region (D `:150-165`, `:193-199`, `:375-385`; C `:160-180`; L `:375-385`); registry: r1 record whole; `fr-ImageUpload.md:69` (F1) + `:91` (K-9) — **created 2026-08-25 10:33, AFTER reader-B's sweep**; `fr-GalleryCard.md:55` (the "views" hit is an a11y-name row, not this defect); `fr-ContourPreview.md` row 8 (the root-font step already in registry awareness); `fr-BasisCanvas.md:55` (BC-9/C-6/D-20 — fields, not timing); adjudicated-tree sweeps: `setVisibility` → **0 files**, publish-path view increment → **0 bookings**.

## Scoreboard

**62 adjudicated register rows — r1's 56 carried under original ids + L-27 READMITTED (r1 roster drop, correction C-1) + 5 NEW rows admitted from this corpus's misses (2 MAJOR: VV-R2-A the phantom-view publish increment · VV-R2-B the dead converged-CRUD arm; 3 MINOR: VV-R2-C the fade-leave pointer swallow · VV-R2-D the four-surface gesture timing · VV-R2-E the debounced-mirror publish race).** Severity distribution: 5 blocker-weight (all banked folds, unchanged) · 24 MAJOR · 29 MINOR · 4 INFO. **11 killed-claims register entries, all SUSTAINED — several now thrice-derived** (both fresh readers independently re-killed D-10(b), the D-13 cost clause, D-27, D-23, and the L-5 arm count with their own instruments). **8 missed finds adjudicated: 5 admitted (above), 2 folded as sharpenings into carried rows (the root-font seam → the D-17 cluster; `overflow: visible` → the dead-CSS census), 1 folded by reference (B's M-C → fr-ImageUpload.md F1/K-9 — a registry-motion event, not a reader error).** **2 corrections against the r1 record · 2 corrections against this corpus's readers.** 16 UNPROVEN-NEEDS-LIVE probes for SS-13.

The component's blocker weight remains entirely **banked-and-folded** (contour destruction · armed editor keymap ×2 · data-dead `/v/` · non-idempotent publish · the 50-vs-200 default). What this corpus adds is a **second data-integrity axis on the publish path** (every publish falsifies its own public view counter; the payload it persists is a 500ms-stale mirror) and the **structural frame** that BLK-1 + BLK-2 + both new publish rows are ONE adoption gap: the store grew the correct converged-CRUD surface and its sole route shell never wired any of it.

## Corrections to the r1 record (exactly two — E-3 discipline, named and bounded)

- **C-1 · L-27 was silently dropped from the r1 roster.** Reader-B's catch, verified by this seat's full read of the r1 record: the r1 MINOR band runs L-25, L-26, then jumps to the D-band; L-28 was folded into D-12 but L-27 (filed at challenge-L:382, read verbatim this session) received no disposition — neither booked, killed, nor folded. **READMITTED below in reader-B's narrowed form.** The omission class matters beyond the row: a zero-drop roster is the registry's core warranty, and the drop was found only because a second reader diffed the roster against the challenge's own row list — a cheap, mechanical check this seat commends to the SS-3/SS-4 spec authors as a standing gate (challenge-row census vs roster census, every apotheosis).
- **C-2 · The r1 D-17/D-18/MIN-2 provenance cell is half-wrong in the other direction.** r1 adopted: "the ease tokens live in `theme/bridges.css`, not `scheme-motion.css:336,341` (those are the z-index tokens)." Both fresh readers independently found, and this seat's grep confirms at the installed bytes: `--ease-standard`/`--ease-in`/`--ease-out-expo` are defined in **BOTH** `dist/styles/tokens/scheme-motion.css:216-219` **AND** `dist/styles/theme/bridges.css:325-330`. D-18's original cite (`:336/:341`) named the z-index rungs — that half of the r1 cell stands — but the "not scheme-motion.css" clause is false as written. Corrected cell: *the ease tokens are twice-defined (scheme-motion `:216-219`, bridges `:325-330`); `:336/:341` are `--z-controls: 20` / `--z-overlay: 50`; `--duration-mid` and `--ease-apple-spring` exist at NEITHER location nor anywhere in the dist (grep 0 ×2, re-run this session).* No verdict moves; the cell is now byte-true in both directions.

## Ruled items — the r2 seat's actual work

1. **B's M-C ("a third double-fire topology, registry swept clean") — FOLD, not admission; the sweep was true when run.** Reader-B's sweep cell states "there is no fr-ImageUpload.md in the adjudicated registry." There is now: `fr-ImageUpload.md` landed 2026-08-25 10:33 — after the reader's sweep window — and its fold row F1 books exactly this topology ("two `useImageUpload()` instances (`:12` / VisualizationView:41) own two dragCounters on one bubbling stream … cure = one `.stop`; count → SS-13") and its K-9 rules the branch question M-C poses ("in the near-certain overlay branch ImageUpload's handleDrop never runs; the double-fire is VisualizationView's overlay→root chain"). **RULING: M-C FOLDS BY REFERENCE → fr-ImageUpload.md F1 + K-9.** The one increment fr-ImageUpload does not spell out — the parent overlay also intercepts subsequent `dragleave` events, so the child's `dragCounter` can latch above zero in branch (b) — is recorded at the fold as a rider on F1's SS-13 probe. Recorded as a **registry-motion event**: a sweep is a timestamped instrument, and the identity guard must be re-run at adjudication time, not inherited from the reader.
2. **B's L-2 cure-incompleteness increment — ADOPTED after seat re-census; it changes the shape of the banked cure.** The banked L-2 chain (fr-CanvasControlsDock M-1 + fr-ContourSettings B-3/B-5/M-9) prescribes stopping the `out-in` remount of `<ContourSettings>`. Reader-B's census, re-run by this seat: `store.extractContour` / `store.computeEpicycles` / `store.computeBases` have exactly **3 call sites in the whole tree — ContourSettings.vue:126/:129/:130, all inside `runCompute`, which always re-extracts first**. So a remount-only cure leaves `epicycleData`/`basesData` null with no non-destructive recompute path, and the transport dock (`v-if="hasData && !isEditing"`, `:235`) never returns. **The orchestrator move (compute pipeline hoisted out of the `out-in` leaf) is REQUIRED, not preferred** — the banked rows' B-3/B-5/M-9 framing already points there; this census closes the escape hatch. Sharpens the L-2 fold row below.
3. **A's MAJ-4 cascade mechanism — challenge-C corrected; conclusion intact.** Challenge-C:176 (read verbatim) claims the host's `grid-template-columns` "wins only by scoped-attribute specificity `(0,2,0)` over Tailwind's `(0,1,0)`". Reader-A's correction is right: Vue injects `<style scoped>` **unlayered**, and per CSS Cascade 5 unlayered author declarations outrank `@layer utilities` regardless of specificity — the host's rule would win at ANY specificity. Same conclusion (the bypass is an accident of cascade position, not a contract), sturdier mechanism; recorded so the F.W1 deletion of the workaround CSS is reasoned from the true cascade.
4. **A's four-surface timing find — ADMITTED with one seat correction (correction R-1 against the reader).** The four decoupled clocks on `@toggle-edit` verify at the bytes (roster row VV-R2-D). But the clause "the surface nearest the cursor is the one that hard-cuts", attributed to `:235/:238`, misplaces the geometry: the pointer sits on the **top** dock (`.controls-dock-anchor`, top-right), and what hard-cuts under it is `CanvasControlsDock`'s OWN internal `<template v-if="!isEditing">` arm (seat read `:42-83` — View options + Publish + Equation + two separators vanish instantly), while `:235/:238` is the **bottom** band's 0ms swap. The claim survives — strengthened, even: counting the top dock's internal swap there are **five** surfaces on one gesture — but the row is booked with the corrected geometry.
5. **The D-10 conflict is now closed three ways.** r1 ruled the two prior readers' mutually incompatible D-10 refutations at the banked border-none chain (fr-CanvasOverlayButton FR-COB-7(b)/R-2). Both fresh readers of THIS corpus independently re-derived the same chain (tailwind-merge group drops → `.btn-pill`'s `@apply … border-none` → utilities-layer `border-2` sets `border-style: var(--tw-border-style)` = `none` → zero pixels) and reader-A adds the sharpest phrasing yet: *the ratio never paints — the border has no style*. K2 stands thrice-derived; the one-line SS-13 probe (`getComputedStyle($btn).borderStyle`) remains the closer.
6. **A's K10 qualification — kill sustained, agency nuance recorded.** The `:336-343` comment's mechanics are correct (re-read again this session), but reader-A is right that its opening clause misassigns agency: glass never "drops its desktop grid" — the producer's mobile template is `grid-cols-1` + row-minmax, and it is the host's own `:334` `display: flex` that converts the component off grid. "One false comment in the file, not two" (K10) holds; the surviving comment is *mechanically right, agentially loose*.
7. **B's L-14 character note — adopted into the banked fold cell.** `updateHoverScale` self-terminates at `useCanvasHover.ts:54-58` (`|diff| < 0.002`) — a bounded tween, not a persistent clock; only the shimmer loop sustains a 60fps full-instrument redraw. The banked BC-10/C-9 fold keeps its count (the loops exist, ungated); its *character* cell is corrected so the cure wave doesn't over-engineer the tween.
8. **B's producer-8.0.0 re-verification — banked as a strengthener on K3/K8.** At producer HEAD 8.0.0 (`8a96868d`): `./metric-badge`, `./hover-popover`, `./hover-card` absent from the export map; `ToastVariant` grep-absent from `src/`; `ButtonProps` = `emphasis|tone|size|iconOnly|loading`, no `variant`. The census-break facts survive the version move — the folds to FR-AUL-10 and AA-2 are version-robust, while K3's tag-discipline note (drift claims re-derived at tags only) is re-affirmed.

**Corrections register against this corpus's readers**: (R-1) reader-A's "nearest surface" geometry — corrected on adoption (ruling 4). (R-2) reader-B's M-C registry-sweep cell — superseded by registry motion, not error (ruling 1). Neither reader inherited a false cell from the challenges or from r1 that this seat could detect; both independently corrected D's figures (1.77/2.71, ~40.5px) in agreement with the banked values — the desirable failure mode of L-18 run twice.

## Adjudicated defect roster

Dispositions are ADJUDICATED. Routing: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5–W8** CRUD/provenance union · **F.W9/W10** close · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring or the glass BH relay. Line anchors are the working tree at `cd26c653`; re-resolve before curing (D-19).

### Blocker-weight — all FOLD BY REFERENCE (identity guard; never re-book) *(r1-carried)*

- **L-2 — BLOCKER · FOLD → fr-CanvasControlsDock.md M-1 + fr-ContourSettings.md B-3/B-5/M-9.** Hand-edited contours destroyed on leaving edit mode: `<ContourSettings>` in BOTH `mode="out-in"` branches (`:260`/`:270` under `:254`); every toggle remounts the sole owner of auto-compute, whose `immediate` watcher finds the post-save state (both data refs nulled) and `runCompute()` → `extractContour()` overwrites the user's contour. **r2 sharpening (ruling 2): the orchestrator move is REQUIRED — the 3-call-site compute census closes the remount-only cure.** → **F.W3/W4**, urgency rider (data-loss class). SS-13 #1.
- **L-3 — BLOCKER ×2 · FOLD → fr-ContourEditorCanvas.md D/B-3 + D/B-5.** (a) Window keydown armed outside edit mode (editor mounted on `v-if="store.contour"` `:203`, hidden by CSS only `:404-409`; unconditional `preventDefault` on ⌘Z/⌘⇧Z/Backspace). (b) `FullscreenViewer.vue:115-120` mounts a second editor with no ref and no `@state-change` — fullscreen edits unpersistable; two keymaps, one ⌘Z undoes twice (Fullscreen button verified OUTSIDE the `!isEditing` arm this session). → **F.W3/W4** (banked; F.W1 sequencing rider).
- **BLK-1 — BLOCKER · FOLD → fr-GalleryCardModal.md GCM-1.** `/v/:visualizationSlug` is data-dead: `loadVisualization`/`loadSnapshot` zero call sites (re-grepped this session: 0), loader reads `imageSlug` only, warm sessions navigated OFF the route, View-Transition opened around a route rendering nothing. → **F.W4** + F.W5–W8 rider. **See VV-R2-B for the structural frame.**
- **BLK-2 — BLOCKER · FOLD → fr-CanvasControlsDock.md D-4/L-1/C-3.** Publish non-idempotent, non-atomic: no re-entry guard, `publishing` paint-only (dock `disabled` grep 0, re-verified), `saveVisualization` POSTs unconditionally, writes `visualizationSlug/ETag` this component never reads. → **F.W4** + the R6-8 dedupe seam. **See VV-R2-A/VV-R2-E for the two new publish-path arms and VV-R2-B for the frame.**
- **L-10 — harmonics arm BLOCKER · FOLD → fr-BasisSelector.md B-1** (private `DEFAULTS.nHarmonics = 50` vs canonical 200). nPoints-literal arm MINOR (four copies of `1024`). → **F.W3/W4** (rides B-1).

### MAJOR — carried *(r1; sharpened where marked)* and new *(VV-R2-\*)*

- **L-1 / MAJ-1 — MAJOR** *(r1-carried; both fresh readers concur on the demotion — sha256 dedup converges the asset)*. One drag-drop fires `globalDrop` twice (root `:141` + descendant overlay `:146-147`, no `.stop`, no `stopPropagation` in `useImageUpload.ts`). → **F.W3/W4**; count → SS-13. The sibling two-instance topology is fr-ImageUpload F1's (ruling 1).
- **L-4 / FM-1 — MAJOR · FOLD → fr-FullscreenViewer.md FM-1** *(r1-carried)*. Fullscreen ghost/overlay toggles provably dead (`AnimationControls` declares neither emit; listeners fall to `$attrs` on GlassDock's root). → **F.W4**.
- **L-6 — MAJOR** *(r1-carried)*. Failed first upload replaces the whole app with an unclearable error screen (the sole clearer's guard `err && store.imageSlug` false by construction; the only exit is destructive `reset()`). → **F.W3/W4** error-posture cluster.
- **L-7 — MAJOR** *(r1-carried)*. `loading` is a plain boolean; first upload unmounts the entire workspace for the POST while the depth-counted `computing` pattern sits ~45 lines above. → **F.W3/W4**.
- **L-9 / MAJ-2 — MAJOR · FOLD → fr-BasisCanvas.md D-4/BC-2/C-2a** *(r1-carried)*. Two of four export toggles inert; the subject is the `Record<string, boolean>` launderer (`:99`). → **F.W3/W4**.
- **L-11 / MIN-9 — MAJOR · FOLD → fr-SpeedSelect.md SS-C-8** *(r1-carried)*. `{once:true}` seeders never re-arm on in-place `/w/:a → /w/:b`; the debounced writer can burn the one-shot. → **F.W4**.
- **L-13 / MAJ-3 / MAJ-4 / D-13(bypass) / D-2 — MAJOR composite** *(r1-carried; both cost clauses stay killed — K3/K4)*. The Configurator ships `asideWidth` + the documented cascade pair; the host hand-rolls the track list at three breakpoints (`:317-330`), converts the component off grid (`:334`), patches producer-internal `.configurator-stage` via `:deep()` (`:344-347`). **r2 note (ruling 3): the override wins because scoped styles are UNLAYERED over `@layer utilities` — not by specificity; and (ruling 6) the `:336-343` comment is mechanically right, agentially loose.** → **F.W1 + F.W3/W4 joint** (the uplift deletes ~14 lines of workaround CSS) + the two standing glass BH relays. Mobile geometry → SS-13.
- **L-14 — MAJOR · FOLD → fr-BasisCanvas.md BC-10/C-9** *(r1-carried; r2 character note per ruling 7: one persistent clock — the shimmer loop — plus one bounded self-terminating tween)*. → **F.W3/W4**.
- **MAJ-5 — MAJOR · FOLD → fr-ContourSettings.md M-16** *(r1-carried)*. `v-model` over zero `defineEmits`, twice; same-tag idiom split at `:265-267`. → **F.W3/W4**.
- **MAJ-6 (+ D-3/D-4 folded arms) — MAJOR, uplift-forward** *(r1-carried; strengthened by ruling 8's 8.0.0 re-verification)*. The closure carries the majority of the glass 4→7 break surface; `ToastVariant` → fr-AdminUserList FR-AUL-10; Button `variant`/`size="icon"` → fr-AdminAuditLog AA-2 (never re-book); Tooltip addition + at-pin peer corrections booked here. → **F.W1**.
- **MAJ-7 — MAJOR** *(r1-carried)*. One failed save, two toasts; the component discards the diagnostic it holds and volunteers a retired noun. → **F.W3/W4**.
- **D-1 — MAJOR · FOLD → fr-BasisCanvas.md D-7** *(r1-carried; the banked 1.77/2.71 figures now reproduced by BOTH fresh readers' independent arithmetic — D's 1.63/3.30 stay dead)*. Illegible raster hero text, zero ARIA. → **F.W3/W4**.
- **D-5 / MIN-13 — MAJOR** *(r1-carried)*. Canvas click-to-upload role-less/name-less/keyboard-dead; only upload affordance on the mobile Canvas tab. → **F.W3/W4**.
- **D-6 — MAJOR** *(r1-carried)*. `hasData` ORs `computing`; transport dock mounts over "Computing...". → **F.W3/W4**.
- **D-7 — MAJOR** *(r1-carried)*. Recompute has no on-stage busy signal. → **F.W3/W4**.
- **D-8 — MAJOR** *(r1-carried)*. Route-to-route loads render no loading state. → **F.W3/W4**.
- **D-9 — MAJOR** *(r1-carried)*. Sole recovery destructive, unconfirmed, tooltip-described only. → **F.W3/W4**.
- **D-12 (+ L-28 arm) — MAJOR** *(r1-carried)*. Rejected drops silently swallowed; "PNG/JPG/SVG ≤ 10 MB" unenforced; canvas-click path unvalidated; `handleFileSelect` received and discarded. → **F.W3/W4**.
- **D-15 — MAJOR (+ M1)** *(r1-carried)*. The `:188-193` comment is false — four surviving cartoon-card sites including the subject's own `:163`. → **F.W3/W4** + F.W1 rider.
- **D-19 — MAJOR** *(r1-carried)*. Zero ARIA in 486 lines; PRM-frozen spinner leaves unannounced text as the only signal. → **F.W3/W4**.
- **VV-R2-A — MAJOR · NEW (reader-B miss 1, chain verified whole by this seat).** **Every publish silently inflates its own visualization's public view counter by one.** `handlePublish` `:112` → `gallery.publish` → `gallery.ts` `const etag = etags.get(slug) ?? (await api.getVisualization(slug)).etag`: a freshly minted draft's ETag lives in `workspace.visualizationETag` (`workspace.ts:359`), a different store, so the map ALWAYS misses and the GET fires — and `api/routers/visualizations.py:268-270` runs `$inc: {views: 1}` unconditionally (this seat's read: the owner/visibility gates above it gate NOT-FOUND only; even the owner's draft GET increments). The entity is born at `views: 1` before any third party sees it. The store's own `recordView` comment documents GET-as-increment as the *deliberate* tracking mechanism, so the publish-path GET is an accidental self-count, not policy. The gratuitous part: `workspace.setVisibility(visibility)` (`:373-389`) performs exactly the ETag-guarded PATCH the publish path needs, replaying the validator `saveVisualization` captured two statements earlier — and it has **zero callers repo-wide** (seat grep). Registry-novel (adjudicated-tree sweeps: `setVisibility` 0 files; no publish-path view-increment booking; fr-GalleryCard's "views" row is an a11y-name row). **→ F.W4** (publish adopts `store.setVisibility("public")`, dropping one round trip and the phantom view; rides BLK-2's repair unit) **+ F.W5–W8 rider** (view-count provenance is a persistence-contract concern). Observable → SS-13.
- **VV-R2-B — MAJOR · NEW (reader-B miss 2, census verified 10/10 by this seat).** **The workspace store's entire converged-CRUD arm is dead surface, and this component — the sole route shell over `/w/` and `/v/` — is the owner that never adopted it; BLK-1 and BLK-2 are one adoption gap, not two bugs.** Ten of the 24 exported members have zero consumers outside the store file (all re-grepped this session): `visualizationSlug` · `visualizationETag` · `revision` · `loadVisualization` · `loadSnapshot` · `setVisibility` · `deleteVisualization` · `invalidateInFlightComputation` · `defaultContourSettings` · `defaultAnimationSettings`. `gallery.ts:168` even bypasses the store's `deleteVisualization` to call the API raw, stranding the slug/ETag state maintained for exactly that call. Four of the ten are precisely the surface a `/v/` route shell owns. Reading the cluster as one gap changes the cure: **a single wiring unit at this component** (route → `loadVisualization`; publish → `setVisibility`; delete → the store action), not scattered repairs. Registry frame-row: does not re-book BLK-1/BLK-2 (folded above) — it owns their JOINT identity and the cure shape. **→ F.W4** (the wiring unit) **+ F.W5–W8** (the CRUD-CONTRACT §1 adoption is the union tranche's own acceptance surface).
- **D-11 — MINOR · FOLD → fr-EquationView.md M-TL** *(r1-carried; both fresh readers independently confirmed the producer bytes and the kf-KfPillTabs:85 grade)*. Orphan tablist; tree-wide-dead `[role="tabpanel"]` animation extension stands. → **F.W4** + F.W1 benefit.

### MINOR — carried *(r1)*, readmitted, and new

- **L-5(a)** *(r1-carried)* — the one genuinely orphaned rejection (drop path). → F.W3/W4.
- **L-8** *(r1-carried; reader-2-of-r1's MAJOR still in DISSENT)* — multi-MB preview retained, zero consumers. → F.W3/W4.
- **L-12 / MIN-1 · FOLD → fr-CanvasControlsDock.md R-7** *(r1-carried)* — `showGhost` the one non-persisted flag. → F.W4.
- **L-15 / MIN-12** *(r1-carried)* — `hasData` typed `… | boolean`, coerced at 1 of 4 consumers. → F.W4.
- **L-16** *(r1-carried)* — saved badge flips on mere selection. → F.W4.
- **L-17 / D-16 / MIN-3** *(r1-carried; **r2 sharpening — A's miss 4 folded in**)* — dead CSS census now **FIVE** declarations: three `.expand-pop-*` (`:426-428`, consuming the never-existed `--ease-apple-spring` — dist grep 0, re-run), `.viz-grid` (`:483`), **and `.controls-overlay { overflow: visible }` (`:421`) — a no-op restating the initial value whose evident intent two ancestors structurally defeat (`.viz-panel-right { overflow: hidden }` `:379`; the producer stage cell's `overflow-hidden`), i.e. simultaneously inert AND a false escape-permitted signal.** (A's secondary `.viz-panel-left-wrap { position: relative }` observation preserved un-asserted — a child SFC could depend on it.) → F.W4.
- **L-18 · FOLD → fr-BasisCanvas.md D-23/BC-21/C-14** *(r1-carried)*. → F.W3/W4.
- **L-19** *(r1-carried)* — scope-name collisions held apart only by the `>` combinator. → F.W4.
- **L-20** *(r1-carried)* — redundant `&& !isDesktop`; breakpoint duplicated across two languages. → F.W4.
- **L-21 / MIN-6** *(r1-carried)* — the `:185` cast launders SegmentedTabs' non-generic `string` model. → F.W4 + glass BH relay.
- **L-22** *(r1-carried)* — guarded read, bare `setItem` in a watcher. → F.W4.
- **L-23** *(r1-carried)* — editor-state shape declared three times. → F.W4.
- **L-24** *(r1-carried)* — `reset()` omits the revision bump/`abortInflight` (re-read `:412-426` this session: confirmed, neither present). → F.W3/W4.
- **L-25 / MIN-7** *(r1-carried)* — publish triggers `resetAndFetch()` into a store this route renders nothing from (re-read at `gallery.ts:231-232`). → F.W4.
- **L-26** *(r1-carried)* — `handlePublish`'s catch dead on BOTH limbs; "Publish failed" unreachable. → F.W3/W4.
- **L-27 — MINOR · READMITTED (correction C-1), narrowed per reader-B.** `CanvasControlsDock` is told `:show-equation="showEquation"` unconditionally (`:216`) while the panel renders only under `showEquation && store.epicycleData && !isEditing` (`:231`). Narrowing seat-verified: the `!isEditing` leg is moot — the Sigma button lives inside the dock's `v-if="!isEditing"` template (seat read `:42-83`), absent while editing. The LIVE divergence is the `store.epicycleData` leg: during a compute `hasData` is true via `computing` (`:121`), the dock mounts, Sigma renders `is-active` off a persisted `showEquation`, and no panel appears — the dock reads "equation ON" over nothing. Adjacent to D-6's busy-conflation but a distinct surface (state/UI divergence, not affordance-over-empty-canvas). **→ F.W3/W4** (busy-signal cluster; the cure passes the same conjunction or derives dock state from panel state).
- **D-17 / D-18 / MIN-2 — MINOR hygiene cluster** *(r1-carried; **r2: provenance cell corrected per C-2, and A's miss 3 folded in as the census sharpening**)*. Three breakpoint systems in the scoped block (900 / 1023-1024 / 1280-1536) **plus the uncounted fourth: `style.css:40-50`'s 768px root-font seam rescales every rem in the block by 12.5%** — `.controls-overlay`'s inline insets walk 9px → 8px → 6px → 6px across two independent seams while `.controls-dock-anchor` walks 9 → 8 → 8 → 8 (agreement below 900px, divergence above — D-17 found the divergence, attributed all of it to the 900px block); the aside band alone is written in px (`:322/:326/:329`) and does not participate in the rescale, so the file's spatial rhythm and its width band move on different clocks; the same seam supplies D-25's true 40.5px. Cross-refs: the seam itself is registry-known as arithmetic input (fr-ContourPreview row 8's "root font steps the WRONG direction"; fr-PaperSearchModal PSM-19/K-5) — what is new here is D-17's census shortfall for THIS file. `--duration-mid` self-guards while the five real-token uses go unguarded; the crossfade runs the 0.24s literal + the file's only bare `ease`. → F.W4.
- **D-20** *(r1-carried)* — `break-all` over English error prose. → F.W4.
- **D-21** *(r1-carried)* — Esc-to-close needs focus nothing performs. → F.W4.
- **D-22 (surviving half)** *(r1-carried)* — `:is-saved` has no failure arm. → F.W3/W4.
- **D-24** *(r1-carried)* — `panel-swap` `out-in` double-reflows the mobile column; the correct technique is 130 lines away. → F.W4. **See VV-R2-D for the composite-gesture booking.**
- **D-25** *(r1-carried; both fresh readers re-derived ~40.5px independently — margin to the 44px AAA floor is 3.5px, material to any padding-based cure)*. → F.W4 + producer note.
- **D-26** *(r1-carried; underline indicator element never rendered — anchor-positioning-only by construction)*. → NO-WAVE-OWNER (producer, glass BH relay); engine matrix → SS-13.
- **D-28** *(r1-carried; A's cite correction adopted: the 0.25-alpha stroke is `placeholder.ts:33`, not `:32` — seat-verified)* — dashed 280×100 box vs whole-stage click zone. → F.W4.
- **D-14 / MIN-4 / MIN-5 / MIN-10 / MIN-11 — vocabulary/contract cluster** *(r1-carried)* — three names for one affordance; retired `snapshot` noun; dead `_imageSlug` positional (doc re-read this session — "kept so the pre-existing call site need not change"; `:112` the sole caller); FullscreenViewer mounted on loading/error screens; phantom `save` emit. Noun + positional → **F.W5**; rest → F.W4.
- **MIN-8** *(r1-carried)* — keyframes.js inert across THIS closure; tree-wide import exists at `useFourierMorph.ts:14`. → F.W1.
- **§4 — method row + bypass** *(r1-carried)* — producer-materialised loop sites; `responsive` bypass. Method → **NO-WAVE-OWNER (SS-3/SS-4)**; bypass → F.W1/F.W4.
- **VV-R2-C — MINOR · NEW (reader-A miss 1, mechanism verified whole by this seat).** **The full-viewport drag overlay stays hit-testable for the entire ~200ms fade-leave, silently discarding the first post-drop (and post-drag-abort) click anywhere in the app.** Seat-verified: `:145-153` mounts the plate under `<Transition name="fade">`; the recipe (`:439-440`) transitions opacity ONLY; the file's four `pointer-events` declarations (`:401/:408/:420/:423`) all belong to other constructions; `handleDrop` sets `isDragging = false` (`useImageUpload.ts:34`) at the drop instant, so from that moment the invisible plate — `position: fixed; inset: 0`, z 50, only `@drop`/`@dragover.prevent` handlers — eats clicks until leave completes. Under PRM the window shrinks to glass's forced 0.1s, not zero. The same recipe at `:230` dead-zones the stage's top-LEFT corner (~`top: 3.5rem; left: 0.5rem`, seat-read `EquationPanel.vue:117-125`) for 200ms on every equation-panel close. This is exactly the fault S-1 praises the file for avoiding at `:404-409` — the standard was chosen in one place and not carried to the other. Registry-novel for hit-testing (banked D-30/D-31 book announcement, flicker topology, contrast — not this). **→ F.W3/W4** (one `pointer-events: none` on the leave phase, or lockstep discipline per S-1); click-loss frequency → SS-13.
- **VV-R2-D — MINOR · NEW (reader-A miss 2, adopted per ruling 4 with corrected geometry).** **One click on `@toggle-edit` (`:220`, a bare boolean flip) drives FIVE surfaces at four durations spanning 0–400ms with nothing sequencing them**: (a) the canvas↔editor crossfade at 240ms — itself the accidental literal fallback of the nonexistent `--duration-mid`, paired with the file's only bare `ease`; (b) EquationPanel's fade at 200ms (when mounted); (c) the BOTTOM dock band `:235`→`:238` — same box, no `<Transition>`, 0ms; (d) **the TOP dock's own internal `v-if="!isEditing"` arm swap — 0ms, and THIS is the surface under the user's pointer** (seat correction R-1); (e) the aside's `panel-swap out-in` at 400ms total — the slowest surface farthest from the gesture. Increment, not re-book: D-24 books the reflow cost in isolation, MIN-8-cluster the token absence, S-1 the crossfade in isolation; no row reads the gesture as one event. **→ F.W4** (motion-coherence unit: one gesture, one timing authority; pairs with D-24's cure). Perceived shape → SS-13 (pairs with the banked M2 probe).
- **VV-R2-E — MINOR · NEW (reader-B miss 4, both write paths verified by this seat).** **Publish persists a 500ms-stale animation mirror with no flush.** The canvas renders `activeBases` synchronously (`:199`, updated at `:267`); `store.animationSettings` is written only by the debounced watcher (`:53-65`, `{ debounce: 500 }`, also covering `anim.easing`/`anim.speed`); `saveVisualization` reads the STORE mirror — `active_bases` at `workspace.ts:351-353` (with the `["fourier-epicycles"]` fallback) and the whole `toRaw(animationSettings.value)` at `:356`. Toggle a basis (or easing, or speed) and publish inside the window: the saved entity replays a frame the author never saw. The dock's publish button is live throughout (gates on `hasContour` only, seat-verified). Adjacent to banked BC-9/C-6/D-20 (WHICH fields round-trip; unit divergence) but distinct in trigger — WHEN the mirror is written relative to the publish read. **→ F.W4** (flush the debounced writer — or read the live refs — in `handlePublish` before `createSnapshot`; rides BLK-2's repair unit). Interleave → SS-13.

### INFO *(r1-carried)*

- **L-29** — `onCanvasClick` re-enters via the bubbling synthetic click; terminates on an unstated HTML invariant. → F.W4.
- **D-29 / INF-1** — subject uncommitted mid-uplift; `style.css:81-83` still cites the retired `UnderlineTabs` by name (re-read this session); zero props/emits, no runner. → F.W1 (land-or-abandon).
- **D-30 / D-31** — drag overlay unannounced + structurally flicker-prone; contrast unguaranteeable over user imagery. → F.W4; flicker → SS-13. (Hit-testing is VV-R2-C's, distinct.)
- **INF-3** — saved-checkmark rests on pre-flush watcher ordering. → SS-13.

## Missed finds — r1's ten carried, this corpus's eight adjudicated

**r1-carried (M1–M10, dispositions unchanged)**: M1 (`:163` cartoon-card falsifies `:188-193` — rides D-15) · M2 (dock-anchor `left/right: auto` discrete-transition jump-then-slide — F.W4; SS-13) · M3 (mobile opens on Controls; FOLD fr-EquationView D-D-M2) · M4 (tab-strip 3× margin misalignment — F.W4) · M5 (transparent-PNG export during compute — F.W3/W4; SS-13) · M6 (file input never re-armed on failure — F.W3/W4) · M7 (no drag-type filter; any in-page drag raises the modal plate — F.W3/W4) · M8 (cosmetic change → double-structuredClone full-workspace persist — F.W4; SS-13) · M9 (`exportFrame` ctx swap without `try/finally` — F.W3/W4) · M10 (`activeBases` born aliased to Pinia state — F.W4).

**This corpus**: A-1 → **VV-R2-C** (admitted) · A-2 → **VV-R2-D** (admitted, corrected) · A-3 → folded into the D-17 cluster (sharpening) · A-4 → folded into the L-17 dead-CSS census (sharpening) · B-1 → **VV-R2-A** (admitted) · B-2 → **VV-R2-B** (admitted) · B-3 → **FOLD → fr-ImageUpload.md F1/K-9** (ruling 1; dragCounter-latch rider recorded there-adjacent) · B-4 → **VV-R2-E** (admitted).

## Killed-claims register — 11 entries, all SUSTAINED (several thrice-derived this corpus)

1. **K1 · D-10(b)** (".btn-pill undefined → square"). FALSE at the installed bytes — `surfaces.css` `.btn-pill { … border-radius: var(--radius-pill) }` in `@layer components`; import chain sealed. **Re-killed independently by BOTH fresh readers with their own greps**; D's grep read the definition's neighbourhood. *(r1-carried, thrice-derived.)*
2. **K2 · D-10(a)** (1.14:1 border / "41% worse"). The ratio never paints — computed `border-style: none` via the banked FR-COB-7(b)/R-2 chain; the r1 two-reader mechanism conflict is now closed three ways (ruling 5). Surviving truth: the sole recovery affordance has NO boundary at all, producer-owned. SS-13 one-liner stands.
3. **K3 · D-13 cost clause / U-6** ("7.0.0 retunes the band to 300/400"). FALSE at the v7.0.0 tag — byte-identical 280/360; the cited `:207-212` are data-gallery pins. Tree-as-tag error. **r2 strengthener (ruling 8): the ADOPTED-pin facts and the census-break folds re-verified at producer 8.0.0** — drift claims die at tags; census facts survive them.
4. **K4 · L-13 cost #1** ("discards `--configurator-stage-min` at ≥1024px"). KILLED — a fabricated hybrid of the mobile row template and the desktop column template (whose stage track is `minmax(0,1fr)`, no floor to discard). Re-killed by reader-B's own dist extraction. *(r1-carried, thrice-derived.)*
5. **K5 · D-27** (mono status line as outlier). KILLED BY ITS OWN FALSIFIER — `fira-code` IS the repo's status register (seat grep decisive in r1; reader-A independently reproduced the grep this corpus). ASCII-ellipsis residue at INFO.
6. **K6 · D-23** ("the catch can only fire for createSnapshot"). Premise wrong in the same direction as its conclusion — dead on BOTH limbs; L-26 is the correct row. Re-derived by reader-B. *(r1-carried, thrice-derived.)*
7. **K7 · L-5(b,c)** ("three unhandled rejections"). Two of three killed — Vue's `callWithAsyncErrorHandling` covers `@change`/`@save`; correct count 1 (the bare async arrow invoked from non-async `handleDrop`). Re-derived by reader-B. *(r1-carried.)*
8. **K8 · D-3/D-4 as component-BLOCKER bookings.** RE-BOOKS — ToastVariant banked at FR-AUL-10; Button `variant` at AA-2 with its explicit never-re-book guard; D's 27/35 grep is the superseded undercount. Both fresh readers enforced the guard unprompted; facts re-verified at 8.0.0 (ruling 8). *(r1-carried.)*
9. **K9 · §4's "two option rows mount and re-key per render."** WRONG VERB — keyed diff patches in place; cost is re-render + diff. Re-derived by reader-B at the dist. *(r1-carried.)*
10. **K10 · the `:336-343` comment as "the worst comment".** KILLED — the comment names the host's own `:334` conversion and describes consequences correctly. **r2 qualification (ruling 6): the opening clause does misassign agency** (glass swaps grid-rows; the host converts to flex) — one false comment in the file (`:188-193`), not two, and the surviving comment is mechanically right, agentially loose. *(r1-carried, qualified.)*
11. **K11 · S-2/SUP-1's PRM-inheritance framing as fresh discovery.** Advisory fold (fr-FrequencyGraph K-5 precedent); the superlative stands on merits, presents by reference. Reader-A's S-2 this corpus correctly verified the producer bytes rather than inheriting — the behaviour K11 exists to reward. *(r1-carried.)*

## Superlatives — with provenance (L-18 runs both ways)

- **The canvas crossfade (`:390-409`)** — the file's best construction, now confirmed by four readers across two corpora: absolute-positioned pair with `z-index` AND `pointer-events` switched in lockstep with opacity. **r2 sharpening: the file now carries TWO in-file counter-examples proving the standard was chosen, not stumbled into — `panel-swap` (D-24) and the fade recipe itself (VV-R2-C), the latter the exact click-eater fault S-1 avoids, 30 lines below it.**
- **The View-Transitions gate (`:302-316`)** — `no-preference` nested inside `@supports`; the repo's only INVERTED PRM guard; comment names artifact, mechanism, double floor, uniqueness precondition; engine detection imported, not re-forked. *(Both corpora.)*
- **`workspace.ts`'s stale-response protocol** — three split revision counters with the WHY written, a guard at every async landing point, `abortInflight` on every load path; sole omission `reset()` (L-24). *(Re-walked by reader-B this corpus.)* — and now the sharpest irony in the record: the store that got cancel-and-discard right grew a complete converged-CRUD arm nobody wired (VV-R2-B).
- **The depth-counted `computing` flag** — the correct pattern forty lines above the `loading` boolean that lacks it; the defect is an unfinished generalisation and the cure wave should be told exactly that.
- **The IO visibility credit** — reference-counted, floored, de-duped, release-only-if-held, documented no-IO fallback; airtight counter, under-reporting sensor (bounded by banked BC-10).
- **`useCanvasSetup`** — refuses zero rects; absolute `setTransform` cannot accumulate DPR.
- **FullscreenViewer's focus discipline** — real Tab cycle, autofocus, restoration, matched Escape teardown. **r2 addition (reader-B, adopted): the file contains BOTH modal postures and the delegated one is correct by construction** — `ExportModal` hands dialog semantics wholesale to reka via glass's Dialog and says so in its own comment, while `FullscreenViewer` hand-rolls the same guarantees well; the delegated one is the one that cannot rot. Worth naming because F.W1 touches both.
- **The stylesheet audits itself** — wave markers + rationale on every non-obvious rule; three r1 kills and two r2 verifications were decidable in minutes because the comments name the producer facts to check. One comment is false (`:188-193`); one is mechanically right but agentially loose (`:336-343`, ruling 6).
- **SUP-6** — the `--animation-dock-max-width` → typed `maxWidth` prop retirement, recorded at both ends: the in-tree precedent the Configurator bypass falls short of.
- **SUP-5** — zero reka-ui, zero shadcn copies, zero bespoke buttons; one Tooltip callsite on the one control needing it.
- **Preserved kills worth keeping** — D's own §6 self-kills, above all K-2's stale-dist self-detection (the Jun-12 artifact voided as 4.0.0 evidence): published negative results the next auditor doesn't re-derive. Both fresh readers honoured it by reading glass's shipped CSS, never the stale consumer bundle.

## DISSENT — preserved, not ruled away

1. **L-8 severity** (r1-carried): reader-2-of-r1's MAJOR on the retained multi-MB preview stands recorded against the ruled MINOR (severity-follows-consequence precedent). fr-ImageUpload's roster has since booked the same family with its own dissent — the two records agree on grade and both carry the dissent.
2. **D-19 grade** (r1-carried): reader-2-of-r1's MINOR recorded against the ruled MAJOR.
3. **Reader-A's agency qualification on K10** — adopted into the kill (ruling 6) but noted here too: if a future reader re-litigates the `:336-343` comment, the ruling is *kill sustained, one clause loose*, not *comment clean*.
4. **This corpus produced no verdict-level reader-vs-reader contradiction** — unlike the pt-leaf r2, the two returns partitioned the surface (design-and-usage vs library-and-code) with overlapping confirmations that agreed everywhere they met, including on figures (1.77/2.71, 40.5px). The seat's work concentrated instead on reader-vs-r1 (C-1, C-2), reader-vs-challenge (rulings 3, 4), and reader-vs-registry-motion (ruling 1). Recorded because suspicious agreement is itself evidence: spot-inheritance checks were run (method header) and cleared.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (priority order)

1. **L-2 mount ordering** under `mode="out-in"` — save an edited contour, toggle edit off, diff the contour. The corpus's highest-value probe. *(r1 #1.)*
2. **L-1 request count** — one drag-drop, network panel open: `POST /api/images` count + `extractContour` aborts. *(r1 #2; fr-ImageUpload F1's branch-resolution probe pairs here.)*
3. **MAJ-3/D-2 rendered mobile geometry** at <1024px, `mobileView==='controls'`, no image: `flexGrow` of the aside; stage rect height. *(r1 #3.)*
4. **VV-R2-A observable** — publish a fresh visualization; read the gallery card's view count (derivation: born at 1). Server `$inc` and the always-miss `etags.get` are source-certain; the rendered figure is not.
5. **D-10 truth** — `getComputedStyle` on the `:168` Button: `borderStyle` (derived `none`) + `borderRadius` (derived pill). One line closes the thrice-derived chain. *(r1 #4.)*
6. **VV-R2-C click loss** — drop an image, immediately click the harmonics slider: is the first click discarded inside the ~200ms window (0.1s under PRM)? Grades the row if habitual-click loss proves worse than MINOR.
7. **M5 artifact** — export during compute; inspect the PNG (derived: fully transparent). *(r1 #5.)*
8. **VV-R2-E interleave** — toggle a basis, publish inside 500ms, re-open the saved entity: `active_bases` vs the on-screen frame.
9. **D-1 composited canvas pixel read** — both themes; source arithmetic complete at the banked 1.77/2.71. *(r1 #6.)*
10. **D-25 measured `.segmented-tab` box** at 375px (derived ~40.5px; kill ≥44, AAA-only ≥24). *(r1 #7.)*
11. **D-26 engine matrix** — `CSS.supports("position-anchor","--x")` per target engine. *(r1 #8.)*
12. **M2 + VV-R2-D perceived shape** — the dock-anchor jump-then-slide AND the five-surface toggle-edit gesture in one recording; both land on the same control. *(r1 #9, paired.)*
13. **M8 wall-clock** — the double-structuredClone draft save at shipped defaults. *(r1 #10.)*
14. **D-30 flicker** — descendant-overlay topology vs the depth counter. *(r1 #11.)*
15. **INF-2 duplicate `view-transition-name`** during `/w/` ↔ `/v/` — moot until BLK-1/GCM-1 is cured. *(r1 #12.)*
16. **INF-3 flush-ordering dependence** of the saved checkmark (+ the SS-C-8 ordering witness). *(r1 #13.)*

## Closing verdict

**ADJUDICATED r2 — supersedes-by-carriage.** Both fresh reader returns survive the incredulity seat with **zero overturned verdicts and two corrections against the r1 record itself**: the roster drop of L-27 (readmitted narrowed — and the challenge-census-vs-roster-census diff that caught it is commended to SS-3/SS-4 as a standing gate) and the half-wrong D-18 ease-token provenance cell (now byte-true in both directions: twice-defined tokens, z-index rungs at `:336/:341`, `--duration-mid`/`--ease-apple-spring` nowhere). The register grows 56 → 62: alongside the readmission, five new rows are admitted after seat re-derivation — **the phantom-view publish increment (VV-R2-A)** and **the dead converged-CRUD arm (VV-R2-B)** at MAJOR, the second reframing BLK-1/BLK-2/VV-R2-A/VV-R2-E as ONE adoption gap curable as a single wiring unit at this component; the **fade-leave pointer swallow (VV-R2-C)**, the **five-surface gesture timing (VV-R2-D**, geometry corrected on adoption**)**, and the **debounced-mirror publish race (VV-R2-E)** at MINOR. Two further misses fold as sharpenings (the 768px root-font seam into D-17's census; `overflow: visible` into the dead-CSS census, now five declarations) and one folds by reference to the same-day fr-ImageUpload record — a registry-motion event proving the identity guard must be re-run at adjudication time. All 11 kills sustain, five of them now thrice-derived by independent instruments; the D-10 border-none chain is settled three ways pending its one-line SS-13 closer. Routings remain M-25 agglomeration input: **F.W1** the uplift leverage + bypass-CSS deletion (with the unlayered-cascade mechanism now correctly stated); **F.W3/W4** the upload/error/busy/a11y clusters, the banked repair units (L-2's cure now REQUIRED to be the orchestrator move — the 3-call-site census closes the remount-only escape), and VV-R2-C; **F.W4** the hygiene band, the publish wiring unit (VV-R2-A/-B/-E riding BLK-2's repair), and the motion-coherence unit (VV-R2-D); **F.W5–W8** the vocabulary/provenance riders + the CRUD-CONTRACT adoption acceptance; **NO-WAVE-OWNER** the §4 method row and the census-gate recommendation (SS-3/SS-4) and the producer rows (glass BH relay). Nothing lost: head, tail, or interval — including r1's, whose every surviving row is present above under its original id.

— end of apotheosis r2. The three challenge files and both r2 reader returns remain immutable beside this record per E-1/E-3; the r1 record's content is superseded-by-carriage, not erased; dispositions are ADJUDICATED, not VERIFIED; the forming X·F specs consume these routings, and nothing here stamps a wave.
