claude-fable-5 (served model id)

# fr-VisualizationView — adjudicated apotheosis (M-23/L-18, fresh-Fable seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt.
**Date**: 2026-08-25 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/VisualizationView.vue` (486 lines, working tree; ` M` dirty — the two-hunk `UnderlineTabs → SegmentedTabs` rename) at fourier HEAD `cd26c653` on `m/w1-bump-migration` · installed `@mkbabb/glass-ui` **4.0.0**.
**Corpus**: three axis challenges (D-design 31 rows · L-library 30 rows · C-consumption 26 rows) re-read whole by this seat, plus two hostile re-reader returns (design-and-usage · library-and-code) adjudicated against each other and against the tree.
**Method**: incredulity seat, not transcriber. Every inter-reader disagreement was re-derived with this seat's own instruments; suspiciously-agreed kills were re-attacked; both readers' D-10 refutations — which are **mutually incompatible** — were resolved at the bytes. Identity guard enforced against the 59-file fr-* registry (banked rows verified by grep this session, cited by file:line below).
**Arbiter's own instruments (all static, read-only, no browser — probe parsimony)**: full read of the subject (486 lines); `useImageUpload.ts` whole (106); `useViewState.ts` whole; `workspace.ts` :78-135, :263-284, :344-366, :410-428, :455-461; `gallery.ts` :214-236; `BasisCanvas.vue` :455-516; `useWorkspaceLoader.ts` :120-134; `AnimationControls.vue` grep (`:120` unguarded `@select="emit('exportFrame')"`; zero `disabled`); repo-wide `fira-code` grep (the D-27 decider); installed glass `dist/styles/glass/surfaces.css` :1-168 (`.btn-pill` inside `@layer components`, `@apply … border-none`, `border-radius: var(--radius-pill)`); `draftStorage.ts:52` (`.put`); `useFourierMorph.ts:14` (keyframes import); registry verifications: fr-CanvasOverlayButton R-2/FR-COB-7, fr-CanvasControlsDock R-7/K-4/M-1-chain, fr-ContourSettings B-3/B-5/M-9/M-16/R-1/R-2, fr-ContourEditorCanvas D/B-3 + D/B-5, fr-GalleryCardModal GCM-1, fr-AdminAuditLog AA-2, fr-AdminUserList FR-AUL-10/FR-AUL-45, fr-EquationView M-TL, fr-BasisSelector B-1, fr-SpeedSelect SS-C-8, fr-BasisCanvas D-4/D-7/BC-10/D-23, fr-FullscreenViewer FM-1.

## Scoreboard

**56 adjudicated register rows (5 blocker-weight — ALL folded by reference to banked rows · 22 MAJOR · 25 MINOR · 4 INFO) · 11 killed-claims register entries · 10 missed finds adopted · 13 UNPROVEN-NEEDS-LIVE probes for SS-13.**

The component's blocker weight is entirely **banked elsewhere and folded here** (contour destruction on edit-toggle · the always-armed editor keymap + the unwired fullscreen editor · the data-dead `/v/` route · non-idempotent publish · the 50-vs-200 default). What is genuinely **this file's own** concentrates in the upload/error/busy posture (a failed first upload replaces the whole app; route-to-route loads and recomputes have zero feedback; the sole recovery is destructive) and in the Configurator producer-API bypass. Two of the corpus's four filed BLOCKERs did not survive at blocker grade in this seat's own name (L-1 demoted; D-2 demoted); the other filed blocker-weight rows survive at full strength **in their banked homes**.

## Ruled disagreements (this seat's actual work)

| topic | reader 1 (design-and-usage) | reader 2 (library-and-code) | RULING | deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-27** (mono status line) | **KILLED** by its own falsifier — fira-code IS the repo's status register | CONFIRMED inside the MINOR hygiene cluster ("fira-code … elsewhere on data") | **KILLED — reader 1 wins; reader 2's cluster arm OVERRULED** | This seat's repo grep: `fira-code` on PROSE at `EquationView.vue:224` ("Computing…"), `:232` (error), `:241` ("Recomputing…"), `ImageUpload.vue:61` ("Image unavailable"), `ContourSettings.vue:312` (retry-msg), `PaperSearchModal.vue:110/:113/:116` (hints), and the subject's own `:165`. `:158` is consistency, not deviation. Surviving residue at INFO only: the in-repo ellipsis split (`…` at EquationView vs `...` at `:158` and `placeholder.ts`). |
| **D-10(a)** — what the "Start fresh" button's boundary actually paints | KILLED: **no border paints** — `.btn-pill`'s `@apply border-none` → `border-style: none`; folds to banked FR-COB-7(b) | KILLED: **a border paints at `--input`, 1.90:1** — cn keeps both color utilities; unlayered `.border-input` wins border-COLOR | **Both kills of D's figures SUSTAINED; reader 1's mechanism WINS; reader 2's 1.90:1 painted-border claim is ITSELF killed** | The banked chain (fr-CanvasOverlayButton.md:25 R-2, re-verified at the installed bytes this session): `surfaces.css` `.btn-pill { @apply … border-none }` in `@layer components` → `--tw-border-style: none; border-style: none`. The host's `border-2` (utilities layer, beats components) sets `border-style: var(--tw-border-style)` — which the same element's `.btn-pill` has set to `none`; Tailwind's `@property --tw-border-style` initial `solid` yields only when the cascade doesn't set it, and here it does. Reader 2 adjudicated border-COLOR and never resolved border-STYLE — color is moot on a zero-style box ("zero pixels, fleet-wide", FR-COB-7 R-2, banked). The true state is worse than D filed: the sole recovery affordance has **no boundary at all** (fill `bg-background` vs `--card` ≈ 1.02-1.03:1) — for a banked **producer** reason, not the host's alpha choice. One-line SS-13 probe closes it (below). |
| **L-8 severity** (retained multi-MB preview data URL, zero consumers) | MINOR | MAJOR | **MINOR — reader 2's MAJOR preserved in DISSENT** | Precedent: fr-CanvasControlsDock.md:28 R-7, severity-follows-consequence — waste with no capability loss grades MINOR. Facts unanimous and seat-verified (`useImageUpload.ts:37` setPreview-before-onFile; `:75-86` readAsDataURL; subject destructures neither `preview` nor `clearPreview` at `:41`). |
| **L-5 arm count** (unhandled rejections: 3 or 1) | did not book | 2 of 3 KILLED — Vue's `callWithAsyncErrorHandling` catches async DOM/emit handlers | **1 — reader 2 wins** | Vue 3.5 wraps `@change` (`:201`) and `@save` (`:247`) handlers and `.catch`es returned promises into the app error channel. Only the drop path is genuinely orphaned: the async arrow at `:41-42` is invoked bare at `useImageUpload.ts:38` from inside NON-async `handleDrop`, which returns `undefined`. D-22's "unhandled" half dies with this; its no-failure-arm half survives (seat-verified `saveContourPoints` re-throws at `workspace.ts:278-280`). |
| **D-2/MAJ-3 grade** (mobile grid destruction) | MAJOR (bypass composite) | MAJOR (D's BLOCKER demoted — "no capability is lost") | **MAJOR** | The aside remains reachable and scrollable; the harm is first-run layout inversion, not capability loss. Rendered geometry stays UNPROVEN-NEEDS-LIVE. |
| **the `:336-343` comment** ("worst comment" per D; correctly-scoped per reader 2's K-8) | S-5 kept D's "the comment is FALSE" cell | KILLED D's characterisation | **KILL SUSTAINED — reader 2 wins** | Seat read: "When the Configurator drops its desktop grid for the mobile flex column …" — "the mobile flex column" names the host's own `:334` rule two lines above; every subsequent clause is a correct consequence description. The file carries **one** false comment (`:188-193`, D-15 + missed find M1), not two. |
| **D-19 grade** | MAJOR (zero-ARIA composite) | MINOR (clustered) | **MAJOR — with reader 2's MINOR in DISSENT** | Seat grep: `aria-|role=|tabindex` → **zero hits in all 486 lines**, on a route shell whose error arm (L-6) replaces the entire application unannounced, whose spinner is PRM-frozen (`a11y-overrides.css:6-10`), and whose only remaining signal is unannounced text. The composite consequence carries it. |
| **D-12/D-14 grades** | D-12 MAJOR · D-14 MAJOR | both clustered MINOR | **D-12 MAJOR · D-14 MINOR** | D-12: silent rejection of the product's primary ingestion act plus an advertised "≤ 10 MB" promise no code keeps (seat read of `useImageUpload.ts` whole: no `else`, no size logic) — consequence-bearing. D-14: register drift with no capability loss (the click-omission consequence is D-5's row) — MINOR, reader 1's MAJOR noted. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED**, never VERIFIED — X·F specs are forming; these routings are M-25 agglomeration input. Line anchors are the working tree at `cd26c653`; re-resolve before curing.

### Blocker-weight — all FOLD BY REFERENCE (identity guard; never re-book)

- **L-2 — BLOCKER · FOLD → fr-CanvasControlsDock.md M-1 (seat-re-derived there at R-1/K-4) + fr-ContourSettings.md B-3/B-5/M-9.** Hand-edited contours silently destroyed on leaving edit mode: `<ContourSettings>` in BOTH `mode="out-in"` branches (`:260`/`:270` under `:254`), every toggle remounts the sole owner of auto-compute, whose `immediate` watcher (`ContourSettings.vue:152-169`) finds the post-save state (`saveContourPoints` nulls both data refs, `workspace.ts:272-274` — seat-read) and `runCompute()` → `extractContour()` overwrites the user's contour. The banked rows add what neither axis had: the server CACHE-HITS the original extraction, the dock remounts after ~0.2s ("the stage heals; the **work** dies" — K-4), and the cure is the B-3/B-5/M-9 orchestrator move (compute pipeline leaves the `out-in` leaf). **→ F.W3/W4** (banked routing) with an urgency rider: data-loss class, should not queue behind the uplift. SS-13's single highest-value probe (below).
- **L-3 — BLOCKER ×2 · FOLD → fr-ContourEditorCanvas.md D/B-3·L-1·C-1 + D/B-5·L-2·C-3.** (a) Window-scoped keydown armed outside edit mode — editor mounted on `v-if="store.contour"` (`:203`), hidden only by CSS (`:404-409`); unconditional `preventDefault` steals ⌘Z from live number inputs; `:220` toggles with no deselect. (b) `FullscreenViewer.vue:115-120` mounts a second editor with no ref and no `@state-change`, so `onEditorSave` (`:92-96`, reads only `editorRef`) can never persist fullscreen edits; two keymaps, one ⌘Z undoes twice. **→ F.W3/W4** (banked; the toolbar-in-fullscreen cure rides the F.W1 break surface — sequencing rider recorded there).
- **BLK-1 — BLOCKER · FOLD → fr-GalleryCardModal.md GCM-1** (`loadVisualization` zero call sites; `/v/` pushes = 0 — banked instruments re-run by reader 2 this corpus, ratified). Route-level extension CONFIRMED as the increment: `useWorkspaceLoader.ts:24-31` reads `imageSlug` exclusively, so a warm session is navigated OFF `/v/` and a cold one renders the empty workspace; `router/index.ts:20-26` still opens a View-Transition around a route that renders nothing. **→ F.W4** (banked: emit `entry.slug`, route `/v/`, point the loader at `loadVisualization`) **+ F.W5–W8 rider** (the no-provenance save).
- **BLK-2 — BLOCKER · FOLD → fr-CanvasControlsDock.md D-4/L-1/C-3** ("N clicks ⇒ N public gallery rows"). Seat re-verified: `handlePublish` (`:106-118`) has no re-entry guard; `publishing` is paint-only (dock has zero `disabled`); `saveVisualization` POSTs unconditionally and writes `visualizationSlug`/`ETag` (`workspace.ts:358-359`) this component never reads; `workspace.ts:456-460` books the debt against this exact call site. **→ F.W4** with the server-side `(image_slug, contour_hash)` dedupe riding the **R6-8** seam (banked routing; C's F.W5 framing preserved as the admission-horizon context).
- **L-10 — harmonics arm BLOCKER · FOLD → fr-BasisSelector.md B-1** (private `DEFAULTS.nHarmonics = 50` vs canonical 200; reset = 4× fidelity cut + armed recompute; trimmed per its K-5). The subject is the hoist site (`:50`, `:265-267`, `:270`). **nPoints arm NEW — MINOR**: the bare literal `1024` at `useWorkspaceLoader.ts:19` AND `:69` (plus `BasisSelector.vue:29/:33`) instead of `CONTOUR_DEFAULTS.n_points` — four copies of one constant, two in one file. **→ F.W3/W4** (rides the B-1 repair unit).

### MAJOR

- **L-1 / MAJ-1 — MAJOR (L's BLOCKER demoted, both readers concur) · NEW.** One drag-drop fires `globalDrop` twice: `@drop` on the root (`:141`) AND on its own descendant overlay (`:146-147`), no `.stop`, no `stopPropagation` (`useImageUpload.ts:31-40`, seat-read whole). Bubbling is synchronous; both runs call `store.uploadImage`. Demotion rationale (reader 2, adopted): server sha256 dedup converges the asset; the harm is a duplicated POST, a manufactured mid-pipeline abort (`invalidateInFlightComputation`, `workspace.ts:115` — the exact ERR_ABORTED the loader comment exists to kill), a duplicate `router.push`, two `_saveDraftNow`. Registry-swept novel. **→ F.W3/W4** (one `.stop` or one `stopPropagation`); request count → SS-13.
- **L-4 / FM-1 — MAJOR · FOLD → fr-AnimationControls D-4/L-2/C-3 via fr-FullscreenViewer.md FM-1** ("the 4-line FullscreenViewer repair"). Fullscreen ghost/overlay toggles provably dead: `AnimationControls` declares `{activeBases?, maxWidth?}` + `exportFrame` only; `FullscreenViewer.vue:136-137`'s listeners fall to `$attrs`; the subject's `:285` handlers are dead. **→ F.W4** (banked).
- **L-6 — MAJOR · NEW.** A failed first-session upload replaces the entire application with "Could not load workspace": `uploadImage` sets `error` and leaves `imageSlug` null (seat-read `workspace.ts:111-133`: assignment at `:123` after the await, `throw e` at `:129`), so `:162`'s arm wins; the only clearer (`useWorkspaceLoader.ts:129-133`, guard `err && store.imageSlug` — seat-read) is false by construction; the only exit destroys the session (`store.reset()`). The copy lies twice (loading arm says "Loading workspace..." during an upload). **→ F.W3/W4**, error-posture cluster.
- **L-7 — MAJOR · NEW.** `store.loading` is a plain boolean (seat-read) while `_computeDepth`/`beginCompute`/`endCompute` sit ~45 lines above; on a first upload `:156`'s arm unmounts Configurator + stage + `ImageUpload` (with its in-flight FileReader) + the IO credit for the whole POST. The cure shape exists in the same file (S-6). **→ F.W3/W4**, same cluster.
- **L-9 / MAJ-2 — MAJOR · FOLD → fr-BasisCanvas.md D-4/BC-2/C-2a** (corpus BLOCKER already demoted there). Two of four export toggles inert; seat re-verified the destructure at `BasisCanvas.vue:466-469`; the subject is the `Record<string, boolean>` launderer (`:99`). **→ F.W3/W4** (the banked export-truth work item).
- **L-11 / MIN-9 — MAJOR · FOLD → fr-SpeedSelect.md SS-C-8** (+ fr-ContourSettings R-2/B-5 legs). `{once:true}` seeds never re-arm on in-place `/w/:a → /w/:b`; the debounced writer (`:53-65`) can burn the one-shot seeder. **→ F.W4** (banked; ordering witness → SS-13).
- **L-13 / MAJ-3 / MAJ-4 / D-13(bypass) / D-2 — MAJOR composite · the bypass mechanism is the durable finding; both named COSTS killed (K3, K4).** `Configurator` ships `asideWidth?: string | readonly [min,max]` + the documented `--configurator-aside-min/max` cascade pair; the host passes only `scroll-mode`, hand-rolls the full track list at three breakpoints (`:317-330`), converts the component off grid (`:334`), and patches producer-internal `.configurator-stage` with `:deep()` (`:344-347`) — three documented workaround scars for one typed prop. The mobile flex conversion inverts which child absorbs slack (first-run Controls tab: upload card under a tall empty stage — rendered geometry UNPROVEN-NEEDS-LIVE). **→ F.W1 + F.W3/W4 joint** (the uplift is the moment to DELETE ~14 lines of workaround CSS, not re-port it) **+ two glass-ui BH relays owed under standing law**: the Configurator mobile stage-fill gap and the `SegmentedTabs` non-generic model (MIN-6). SUP-6 is the in-tree precedent for the cure.
- **L-14 — MAJOR · FOLD → fr-BasisCanvas.md BC-10/C-9 + D-8/BC-25** (verbatim: the two `useCanvasHover` rAF loops consult neither PRM nor the visibility credit; the touch-pin arm is the K-11-preserved indefinite regime). Filing as new would be a re-book. The corpus-amendment VALUE (CENSUS §3a undercounts the viz path's rAF loops 2→4) is preserved as the banked rows' scope. **→ F.W3/W4** (banked).
- **MAJ-5 — MAJOR · FOLD → fr-ContourSettings.md M-16** (v-model over zero `defineEmits`, twice — `:260`, `:270`; the orphan-DOM-listener leg is separately DEAD there via `isModelListener`, R-1). Same-tag idiom split (`:265-267`: three models, two idioms) survives as this file's note. **→ F.W3/W4** (decided with the orchestrator move).
- **MAJ-6 (+ D-3, D-4 as folded arms) — MAJOR, uplift-forward.** The closure carries the majority of the glass 4→7 break surface. **Identity guard enforced**: the `ToastVariant` arm **FOLDS → fr-AdminUserList.md FR-AUL-10** (+ FR-AUL-45's adapter-rewrite rider); the `Button variant`/`size="icon"` arm **FOLDS → fr-AdminAuditLog.md AA-2** (ADJUDICATED BLOCKER census-extension, blast radius 37/38/124, explicit "never re-book" guard — D's 27/35 same-line grep is the superseded undercount). What is NEW here: the per-closure leverage figure (this one route shell discharges the majority of the break sites + both HARD shadows), D-4's genuinely-new **Tooltip** addition (1 callsite, `:166`, rides R3-7a), and reader 2's seat correction adopted: the lucide (`@lucide/vue ^1.16.0`) and value.js (`^0.10.0 || ^0.11.0` vs installed 0.13.0) peer obligations exist **at the ADOPTED 4.0.0 pin**, not first at the uplift. **→ F.W1** (this closure as F.W1's first cut).
- **MAJ-7 — MAJOR · NEW.** One failed save, two toasts: `saveVisualization` swallows to `error.value` + `null` (seat-read `:344-365`); the subject toasts the generic "Could not create snapshot" (`:111`) while its own installed loader watcher toasts the real message. The component discards the diagnostic it holds. **→ F.W3/W4**, error-posture cluster.
- **D-1 — MAJOR · FOLD → fr-BasisCanvas.md D-7** (banked ratios ~1.77:1 light / ~2.71:1 dark — both readers' recomputes reproduce them; **D's 1.63:1 and 3.30:1 are both wrong** and the registry's figures stand). Raster/AT arms hold (`BasisCanvas.vue:518-527` bare canvas, zero ARIA in the subject). D's "single instruction a first-run user receives" framing also false (ImageUpload.vue:109 is a second instruction). **→ F.W3/W4** (banked).
- **D-5 / MIN-13 — MAJOR · NEW.** The canvas click-to-upload affordance is role-less, name-less, keyboard-dead (`:198` plain div, handler `:127-131`, `cursor:pointer` only at `:477-479`; seat grep: zero `aria-|role=|tabindex` file-wide) — and on the mobile Canvas tab it is the ONLY upload affordance (the real `<button>` is `display:none`d with the aside). **→ F.W3/W4**, a11y cluster.
- **D-6 — MAJOR · NEW.** `hasData` ORs in `store.computing` (`:121`), so the transport dock mounts over an empty canvas painting "Computing..." during the first compute; `AnimationControls` has zero `disabled` (seat grep). The type arm is L-15's. **→ F.W3/W4**, busy-signal cluster.
- **D-7 — MAJOR · NEW.** A recompute has no busy signal anywhere on the stage: `runComputeEpicycles` never nulls `epicycleData` before awaiting, so the stale frame keeps painting; the sole busy affordance lives in the aside — `display:none` on the mobile Canvas tab. Distinct surface from banked fr-ContourSettings M-2. **→ F.W3/W4**, same cluster.
- **D-8 — MAJOR · NEW.** Route-to-route loads have no loading state: the spinner guard is `store.loading && !store.imageSlug` (`:156`) and `imageSlug` still holds the previous slug for the whole fetch. Exactly three arms in the chain. **→ F.W3/W4**, same cluster.
- **D-9 — MAJOR · NEW.** The error state's sole recovery is destructive, unconfirmed, mis-described: `:170` `store.reset(); router.push('/visualize')` wipes eleven refs (seat-read `:412-426`); no Retry for what are overwhelmingly transient fetch errors; the Tooltip is a description not a name, and unreachable on touch. **→ F.W3/W4**, error-posture cluster.
- **D-12 (+ L-28 as the validation-posture arm) — MAJOR (ruled above).** Rejected drops silently swallowed (no `else` — seat-read); the "PNG/JPG/SVG ≤ 10 MB" promise is unenforced (zero size logic in the file; `IMAGE_EXTENSIONS` also admits gif/bmp/webp/tiff); the canvas-click path runs no validation at all (`accept` is a picker hint). `handleFileSelect` — received and discarded by the subject — is the in-file cure. **→ F.W3/W4**, upload cluster.
- **D-15 — MAJOR · NEW (+ missed find M1 as the increment).** The SFC's load-bearing comment at `:188-193` ("the bespoke `cartoon-card` panel backgrounds retire…") is false: four in-view sites survive — `BasisCanvas.vue:521`, `ImageUpload.vue:38`, `ContourPreview.vue:33`, **and the subject's own `:163`** (seat-verified in the template read; the error card is the only cartoon-card this file authors, contradicting the comment by the same hand — reader 1's missed find, adopted; D-15's own enumeration and falsifier both omit it, so an uplift sweep driven by D's list would skip the subject itself). Stage double-border + clip confirmed. Prose that lies is worse than absent prose. **→ F.W3/W4** with an F.W1 rider (the uplift must not trust `:188-193`).
- **D-19 — MAJOR (ruled above) · NEW.** Zero ARIA in 486 lines: loading and error swaps carry no `role="status"`/`alert`/`aria-live`; under PRM the frozen spinner leaves unannounced text as the only signal. **→ F.W3/W4**, a11y cluster.
- **D-11 — MINOR · FOLD → fr-EquationView.md M-TL** (same producer, same variant, graded MINOR per the ratified kf-KfPillTabs:85 ruling: `aria-controls` is SUPPORTED, not required, on `role=tab`; D's MAJOR is a re-grade of a banked ruling — declined). **Extension CONFIRMED**: `style.css:79-85`'s `[role="tabpanel"]` entry animation is dead TREE-WIDE (no element in any of its three named consumers carries the pair; `dist/tabs.js` emits no `data-state`) — extends the banked fr-AdminAuditLog dead-rule row. 7.0.0's `semantics`/`ariaLabel`/`controls` additions confirmed at the tag; the default preserves `underline → tabs`, so the uplift alone does not cure. **→ F.W4** + F.W1 benefit.

### MINOR

- **L-5(a) — MINOR.** The one genuine orphaned rejection (drop path; ruled above). → F.W3/W4 error cluster.
- **L-8 — MINOR (ruled above; reader 2's MAJOR in DISSENT).** Multi-MB base64 preview read and retained per drop, zero consumers; the sibling `ImageUpload.vue` proves the clear-contract exists. → F.W3/W4.
- **L-12 / MIN-1 — MINOR · FOLD → fr-CanvasControlsDock.md R-7 + D-17/L-5/L-11/C-21** (already adjudicated MINOR; "no capability is lost"; LC's MAJOR lives in that record's DISSENT). `showGhost` is the one non-persisted flag (seat-read `useViewState.ts`: shape `{editing?,overlay?,equation?}`, `ref(true)` unconditional, writer omits ghost). → F.W4 (banked).
- **L-15 / MIN-12 / D-6(b) — MINOR.** `hasData` typed `EpicycleData | AnimationData | boolean`; coerced at exactly one of four consumers. → F.W4 hygiene.
- **L-16 — MINOR.** The saved badge flips to unsaved on mere selection/deselection (`:87-90` + `ContourEditorCanvas` emitState on pointer-down/bg-click); the opposite direction is banked at fr-EditorControlsDock R-1 — this false-negative direction is new. → F.W4.
- **L-17 / D-16 / MIN-3 — MINOR.** Dead CSS: three `.expand-pop-*` rules + `.viz-grid` (seat template read confirms live names are `fade`/`panel-swap`/`slide-down` only), and `.expand-pop-enter-active` consumes `--ease-apple-spring`, defined at neither glass version (both readers' token sweeps concur: glass ships `--ease-apple` and the `--ease-spring*` family, never the hybrid). → F.W4.
- **L-18 — MINOR · FOLD → fr-BasisCanvas.md D-23/BC-21/C-14** (three exposed members, one consumer; `drawImageOverlay` structurally uncallable; `anim` re-exports the store through a handle — the retired idiom). → F.W3/W4 (banked).
- **L-19 — MINOR (reader 2's sharpening adopted).** `canvas-container`/`editor-shell` name collisions across the parent/child boundary; under SFC scoping the child ROOT receives the parent scope id, so only the `>` combinator (`:392-393`, `:404-405`) keeps `position:absolute; inset:0`/`opacity:0` from cross-applying. → F.W4.
- **L-20 — MINOR.** `&& !isDesktop` redundant (the consuming rule exists only under `max-width:1023px`); breakpoint duplicated across two languages at two values with a fractional-viewport sliver. → F.W4.
- **L-21 / MIN-6 — MINOR.** The `:185` `as 'controls' | 'canvas'` cast launders `SegmentedTabs`' `string` model — a workaround for a producer typing gap (non-generic model, `.d.ts` verified by both readers) → F.W4 + the glass BH relay (with MAJ-3's).
- **L-22 — MINOR.** `useViewState`: guarded read, bare `setItem` in a watcher (seat-read `:43-49`). → F.W4.
- **L-23 — MINOR.** Editor-state shape declared three times, handler typed off the local ref. → F.W4.
- **L-24 — MINOR (reader 2's reachability narrowing adopted).** `reset()` omits the revision bump/`abortInflight` every other mutator performs (seat-read: `:412-426` touches neither); sole caller sits behind the loading arm's pre-emption, so the hazard needs two concurrent loads with the first failed. → F.W3/W4 error cluster.
- **L-25 / MIN-7 — MINOR.** Publish triggers `resetAndFetch()` into a store this route renders nothing from (seat-read `gallery.ts:231`). → F.W4.
- **L-26 — MINOR.** `handlePublish`'s catch is fully dead — BOTH callees swallow (seat-read: `saveVisualization` returns null, never throws; `gallery.publish` catches and toasts). "Publish failed" is unreachable. D-23's narrower one-limb derivation is killed (K6); L-26 is the correct row. → F.W3/W4 error cluster.
- **D-17 / D-18 / MIN-2 — MINOR hygiene cluster (D-27 REMOVED — killed).** Three breakpoint systems (the ownerless 900px seam re-rhythms `.controls-overlay` against the dock anchor between 900-1023px); raw literals with split token-fallback discipline (only `--duration-mid` guards itself — and `--duration-mid` does not exist at either glass version; the crossfade runs on the 0.24s literal + bare `ease`); provenance correction adopted: the ease tokens live in `theme/bridges.css`, not `scheme-motion.css:336,341` (those are the z-index tokens). → F.W4.
- **D-20 — MINOR.** `break-all` over ordinary English error prose; `break-words` is the requirement. → F.W4.
- **D-21 — MINOR.** `EquationPanel`'s Esc-to-close needs programmatic focus that nothing performs (zero `.focus()` in either file). → F.W4.
- **D-22 (surviving half) — MINOR.** `EditorControlsDock`'s `:is-saved` has no failure arm — a failed save is indistinguishable from an untouched one (the "unhandled rejection" half died with L-5(b,c), K7; `saveContourPoints`' rethrow is seat-verified). → F.W3/W4 error cluster.
- **D-24 — MINOR.** `panel-swap` `out-in` empties the aside ~200ms and double-reflows the mobile column per edit toggle — the correct technique is 130 lines down at `:390-409`. → F.W4.
- **D-25 — MINOR (reader 1's arithmetic correction adopted: ~40.5px, not D's 36px — still <44, >24, so AAA-only).** Glass's coarse-pointer floor covers `.segmented-tabs__trigger` (the Select), not `.segmented-tab`; the host doesn't pass `responsive`. → F.W4 (host half) + producer note.
- **D-26 — MINOR, STRONGER THAN FILED (reader 1's find adopted).** For `underline` the indicator element is not even rendered (`V.value ? m("",!0) : …`); the mark is purely the anchor-positioned `::before`. No JS fallback by construction. → NO-WAVE-OWNER (producer) via the glass BH relay; engine matrix → SS-13.
- **D-28 — MINOR.** The dashed 280×100 box implies a target the actual click zone exceeds by an order of magnitude. → F.W4.
- **D-14 / MIN-4 / MIN-5 / MIN-10 / MIN-11 — MINOR vocabulary/contract cluster (D-14 ruled MINOR above).** Three names for one affordance, the hero omitting the click it implements; the retired `snapshot` noun in user copy (`:110-111`) against CRUD-CONTRACT-1; the dead `_imageSlug` positional kept alive by this sole call site (seat-read `gallery.ts:214-219`); `FullscreenViewer` mounted unconditionally incl. on loading/error screens; `ContourEditorCanvas`'s phantom `save` emit. Noun + positional → **F.W5** (vocabulary co-sign); rest → F.W4.
- **MIN-8 — MINOR (reader 2's scope note adopted).** keyframes.js inert across THIS closure while its `~4.0.0` tilde locks the tri-package deadlock — but the tree DOES import it at `useFourierMorph.ts:14` (seat-verified); never carry MIN-8 forward as repo-wide ABSENT. → F.W1.
- **§4 — MINOR method row + MINOR bypass.** The producer-boundary variant of R5-7 loop blindness (0 authored `v-for`, 2 producer-materialised rows via the inline `:options` literal at `:183`) — restated per K9 in terms of authored-vs-materialised loop SITES, not mounts. The `SegmentedTabs.responsive` bypass (hand-rolled mobile switch) confirmed at the pin. Method row → **NO-WAVE-OWNER (SS-3/SS-4 spec authoring: the mounted-instance denominator needs a rule for consumer-fed producer loops)**; bypass → F.W1/F.W4 with L-20.

### INFO

- **L-29** — `onCanvasClick` re-enters via the bubbling synthetic click; terminates only on HTML's click-in-progress flag; unstated invariant. → F.W4.
- **D-29 / INF-1** — the subject is uncommitted mid-uplift (two-hunk rename, correct at 4.0.0; `style.css:81` still names the retired `UnderlineTabs`); zero props/emits — no seam to test, no runner exists. → F.W1 (land-or-abandon) / the census's test-floor decision.
- **D-30 / D-31** — drag overlay unannounced + structurally flicker-prone (descendant topology; depth-counter + Safari re-arm mitigations seat-verified in `useImageUpload.ts`); contrast not guaranteeable over user imagery. Reader 1's adjacent containing-block hypothesis checked-and-killed (`.paper-texture` declares background properties only). → F.W4; flicker → SS-13.
- **INF-3** — the saved-checkmark rests on pre-flush watcher ordering (correct today by ordering, not design). → SS-13.

## Missed finds — adopted after seat re-proof

1. **M1 (reader 1) — MINOR.** The subject's OWN `:163` cartoon-card falsifies the `:188-193` comment 25 lines below it; D-15's enumeration AND falsifier omit it. Seat-verified in the template read. Rides D-15's cure. (Chassis context: `.cartoon-card` is a fourier-local resurrection of a class glass deleted at C.W5 — `style.css:98-111` shim.)
2. **M2 (reader 1) — MINOR.** The `.controls-dock-anchor` transition is inert in two of three named properties by construction: at rest `left` computes to `auto` (only `top`/`right` set, `:450-453`); `.dock-centered` sets `left:50%; right:auto` (`:461-465`); per CSS Transitions L1, `auto` is discrete for `left`/`right`, so both legs snap and only `transform` animates — jump-then-slide, on a transition A.W3.d tuned the easing of (`:455-458`, seat-verified). Not banked (registry sweep negative). → F.W4; perceived shape → SS-13.
3. **M3 (reader 1) — MINOR · FOLD → fr-EquationView.md D-D-M2** (mobile opens on Controls; RD-5 ruled MINOR — grade held). Increment: here the hidden content is the app's ONLY first-run instruction, and `ImageUpload.vue:87-91` designates it the hero in prose. Compounds D-1/D-5. → F.W4 (banked home).
4. **M4 (reader 1) — MINOR.** The mobile tab strip's `px-3` (12px) misaligns 3× against the Configurator's `margin: 0.25rem` (4px) directly beneath (`:181` vs `:296-300`, seat-verified), an opaque `bg-background` band abutting a `glass-floating` plate with no divider. Not banked; none of the three axes reached the cross-element relation. → F.W4.
5. **M5 (reader 2) — MINOR.** Exporting during a compute downloads a fully transparent PNG: `hasData` ORs `computing` → dock mounts; `AnimationControls.vue:120` menu item has no guard (seat-verified); `exportFrame` guards only its DRAW block (`if (data || basesData)`, `:484`) while `toDataURL` + anchor click run unconditionally (`:506-512`, seat-read). No banked export row books the no-data path (registry sweep). → F.W3/W4 (rides the banked export-truth item); artifact → SS-13.
6. **M6 (reader 2) — MINOR.** `onCanvasFileSelect` never re-arms the input on failure: `uploadImage` re-throws (seat-verified `:127-129`), so `:135`'s reset is unreachable; re-selecting the SAME file fires no `change`. Live on second-and-later uploads (imageSlug set → no L-6 takeover). Sibling: `handleFileSelect` never resets at all. The reset belongs in `finally`. → F.W3/W4 upload cluster.
7. **M7 (reader 2) — MINOR.** No drag-type filter: `handleDragOver`/`handleDragEnter` `preventDefault` unconditionally and never inspect `dataTransfer.types` (seat-read whole — `dataTransfer` appears only in `handleDrop`), so ANY in-page drag (text selection, an `<img>`) raises the full-viewport modal plate, and D-12's silent swallow then dismisses it. One `types.includes("Files")` guard is the whole cure. → F.W3/W4 upload cluster.
8. **M8 (reader 2) — MINOR.** A cosmetic playback change persists the ENTIRE workspace payload, structure-cloned twice on the main thread, ~1.5s later: `:53-65` watcher → `workspace.ts:108` deep watch → `scheduleDraftSave` (1000ms) → `_saveDraftNow`'s `structuredClone` of `{…epicycleData, basesData…}` (seat-read `:93-106`) → `draftStorage.ts:52` `.put` (second internal clone). `basesData.partial_sums` is megabytes at shipped defaults. Distinct from banked M-β4 (dead field), MISSED-E (race), M-14 (persist-before-verify) — the trigger-frequency×payload cost is this seat's booking. → F.W4; wall-clock → SS-13.
9. **M9 (reader 2) — MINOR.** `exportFrame` swaps the LIVE surface's ctx with no `try/finally` (`:479-480` … `:504`, seat-read): one throw inside the draw block leaves the instrument permanently painting into a discarded offscreen bitmap, silently. The draw block indexes unchecked server payloads (the banked FR-CP-16 enforcement gap). Two-line `finally` fix; durable fix is BC-3's one-pipeline cure. → F.W3/W4 (rides the export-truth item).
10. **M10 (reader 2) — INFO.** `activeBases` is born an alias of Pinia state (`:45-47`: `ref()` over the store's reactive array returns it unchanged), so until the first writer replaces the settings object, an in-place mutation of the view ref would write persisted store state. Latent — all four writers reassign (seat-spot-checked `:59`'s spread). One-line copy makes ownership explicit. → F.W4.

## Killed-claims register

1. **K1 · D-10(b)** ("`.btn-pill` undefined in glass 4.0.0's shipped CSS … renders square"). **FALSE at the installed bytes** — `dist/styles/glass/surfaces.css` `.btn-pill { … border-radius: var(--radius-pill); … }` inside `@layer components` (file header confirms the layer; seat-read :115-168); `--radius-pill: 9999px`. Import chain sealed (`style.css:3` → `index.css` → `glass.css` → `glass/surfaces.css`). D's grep read the definition's neighbourhood and not the definition. The 7.0.0 half is also mis-cited (tag ships `border-radius: var(--radius-pill)`).
2. **K2 · D-10(a)** (1.14:1 border, "41% worse"). **KILLED — and reader 2's replacement mechanism (a `--input` border at 1.90:1) is ITSELF KILLED** (ruled above): the banked FR-COB-7(b)/R-2 border-none chain resolves computed `border-style: none` regardless of which border-COLOR utility survives cn or wins the cascade — zero pixels. **FOLD → fr-CanvasOverlayButton.md FR-COB-7(b)**, one identity, never re-booked. Reader 2's cn-group and unlayered-`.border-input` facts are preserved as method notes (true, moot). The surviving truth — the sole recovery affordance has NO boundary, producer-owned — rides the banked FR-COB-28 relay packet. SS-13 one-liner: `getComputedStyle` on the `:168` Button's `borderStyle`.
3. **K3 · D-13 cost clause / U-6** ("7.0.0 retunes the band to 300/400; the bypass suppresses the retune"). **FALSE at the v7.0.0 TAG** — the desktop rule is byte-identical 280/360 defaults; the cited `:207-212` are `data-gallery` grid pins. A K-1-class tree-as-tag error (challenge authored inside the window the producer working tree self-reported 7.0.0). Twice-derived (both readers), sustained. The bypass survives; the MAJOR-elevating clause does not. BASELINE LAW note: producer HEAD is now 8.0.0 — re-derive at tags.
4. **K4 · L-13 cost #1** ("the raw grid-template-columns discards `--configurator-stage-min` at ≥1024px"). **KILLED** — the quoted producer grid is a fabricated hybrid of the MOBILE row template (which carries the 18rem stage floor) and the DESKTOP column template (whose stage track is `minmax(0,1fr)` — no floor to discard). Twice-derived, sustained.
5. **K5 · D-27** (mono status line as outlier). **KILLED BY ITS OWN STATED FALSIFIER** — seat grep decisive (ruled above); reader 2's contrary cluster arm overruled. ASCII-ellipsis residue survives at INFO inside D-29's hygiene.
6. **K6 · D-23** ("the catch can only fire for `createSnapshot`"). **KILLED — premise wrong in the same direction as its conclusion**: `createSnapshot` = `saveVisualization`, which cannot throw either (seat-read; only pre-`try` statement is the guard). The catch is dead on BOTH limbs — L-26's framing, confirmed. Recorded so no cure keeps the catch for a caller that cannot use it.
7. **K7 · L-5(b,c)** ("three unhandled rejections"). **TWO OF THREE KILLED** — `@change` and `@save` handlers ride Vue's `callWithAsyncErrorHandling` promise-catch into the app error channel. Correct count: 1 (ruled above). Disposes of D-22's "unhandled" half.
8. **K8 · D-3 and D-4 as component-BLOCKER bookings.** **RE-BOOKS — fold, never re-book**: ToastVariant is banked at fr-AdminUserList FR-AUL-10 (standing disposition recorded at fr-AdminAuditLog); the Button `variant` break is the banked AA-2 census-extension with its explicit identity guard (fr-BasisSelector M-7 fleet identity; FR-COB-2 "never re-booked"). The facts are CONFIRMED at the tags; the new content (closure leverage, the Tooltip addition, the at-pin peer corrections) is booked inside MAJ-6.
9. **K9 · §4's "two option rows mount and re-key on every parent render."** **WRONG VERB** — a fresh array identity re-runs the child render and the keyed diff; Vue patches DOM in place (`key: e.value` in the dist). Cost = re-render + diff, no mount-denominator change. The identity-churn, hoist cure, and `responsive` bypass survive separately.
10. **K10 · D-2/S-5's characterisation of the `:336-343` comment as false/"the worst comment."** **KILLED** (ruled above) — the comment names the host's own conversion two lines up and correctly describes its consequences. The self-inflicted-collapse diagnosis (MAJ-3/D-2) is untouched.
11. **K11 · S-2/SUP-1's PRM-inheritance framing as a fresh discovery — advisory fold.** True but banked as a known reader-failure class (fr-FrequencyGraph K-5: glass's universal PRM rule as "the inherited-falsifier failure"). The superlative stands on merits; presents by reference, not as novel.

## Superlatives — with provenance (L-18 runs both ways)

- **The canvas crossfade (`:390-409`) — the file's best construction** (D's S-1 = both readers, seat-verified): absolute-positioned pair, `z-index` AND `pointer-events` switched in lockstep with opacity — the faded layer is never an invisible click-eater. The in-file counter-example (`panel-swap`, D-24) proves the standard was chosen, not stumbled into.
- **The View-Transitions gate (`:302-316`)** (D S-3 = C SUP-1, seat-read): `no-preference` nested inside `@supports`, the repo's only inverted PRM gate — opts INTO motion for the unobjecting rather than patching motion out; the comment names artifact, mechanism, double floor, and uniqueness precondition; engine detection imported from the producer, not re-forked.
- **`workspace.ts`'s stale-response protocol** (L S-1, both readers re-walked): three split revision counters with the WHY recorded, a guard at every async landing point, `abortInflight` on every load path. Sole omission = `reset()` (L-24, reachability narrowed).
- **The depth-counted `computing` flag** (L S-6): the correct pattern, present and working, forty-odd lines above the `loading` flag that lacks it — the defect is an unfinished generalisation, and the cure wave should be told exactly that.
- **The IO visibility credit is leak-free by construction** (L S-3): reference-counted, floor at zero, de-duped acquire, release-only-if-held, documented no-IO fallback — and it is why the banked panel-inactive clock-parking defect (fr-ConvergencePlot) does NOT reproduce here. Bounded by banked BC-10: airtight counter, under-reporting sensor.
- **`useCanvasSetup` refuses zero rects and cannot accumulate DPR** (L S-2): the two commonest Canvas2D bugs foreclosed in ~10 lines; the collapse the host hit anyway was at the layout layer (MAJ-3), not here.
- **FullscreenViewer's focus discipline** (D S-4, seat-corroborated by reader 2): real Tab cycle in the Teleport target, autofocus on open, focus restoration on close, matched Escape teardown. Sharpens L-4: the one well-behaved modal is the one with dead toggle wiring.
- **The stylesheet audits itself** (S-5, all three axes + both readers): wave markers + rationale on every non-obvious rule (`B.W2`/`B.W2.a`/`B.W4`/`A.W3.d`/`I.ε`/the flex post-mortem). Three kills in this record were decidable in minutes because the comments name the producer facts to check. **One** comment is false (`:188-193`, D-15/M1) — the ruling that it is one, not two, is K10.
- **SUP-6 (C)**: the `--animation-dock-max-width` custom-property contract retired for a typed `maxWidth` prop — the in-tree precedent proving the house knows the standard MAJ-3/MAJ-4 fall short of; the Configurator-bypass cure has a model 200 lines away.
- **SUP-5 (C)**: zero reka-ui, zero shadcn copies, zero bespoke buttons; one Tooltip callsite on the one control needing it (R3-7a exact); the F.W3 tooltip migration is a one-line change here.
- **Preserved kills worth keeping** (reader 1): D's K-1 (publish DOES confirm success — `gallery.ts:231` `toast("Published!", …)`, seat-verified) and K-2 (the `@source/node_modules` self-refutation — exemplary adversarial hygiene; the Jun-12 build artifact is void as 4.0.0 evidence, a caveat this record's own K1/K2 evidence respects by reading glass's shipped CSS, not the stale consumer bundle).

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (priority order)

1. **L-2 mount ordering** under `mode="out-in"` (the banked M-1 chain's one live cell): save an edited contour, toggle edit off, diff the contour. The corpus's highest-value probe.
2. **L-1 request count**: one drag-drop with the network panel open — count `POST /api/images` and `extractContour` aborts.
3. **MAJ-3/D-2 rendered mobile geometry** at <1024px, `mobileView==='controls'`, no image: `getComputedStyle($('.configurator-aside')).flexGrow`; `$('.configurator-stage').getBoundingClientRect().height`.
4. **D-10 truth**: `getComputedStyle` on the `:168` Button — `borderStyle` (this seat derives `none`) and `borderRadius` (this seat derives the pill). One line settles the two-reader mechanism conflict.
5. **M5 artifact**: begin a compute, Export → Save PNG from the transport overflow, inspect the file (derivation says fully transparent).
6. **D-1 composited canvas pixel read** (canvas is opaque to axe — why this survived); source arithmetic is complete at the banked 1.77/2.71.
7. **D-25 measured `.segmented-tab` box** at 375px (seat arithmetic ~40.5px; kill at ≥44, keep AAA-only at ≥24).
8. **D-26 engine matrix**: `CSS.supports("position-anchor","--x")` per target engine.
9. **M2 perceived shape** of the dock-anchor jump-then-slide (interpolability is source-decided; the artifact's look is not).
10. **M8 wall-clock** of the double-structuredClone draft save at shipped defaults (change easing once with a computed workspace loaded; profile the ~1.5s-later task).
11. **D-30 flicker** — whether the descendant-overlay topology fires visibly despite the depth counter.
12. **INF-2 duplicate `view-transition-name`** during `/w/` ↔ `/v/` (moot until BLK-1/GCM-1 is cured).
13. **INF-3 flush-ordering dependence** of the saved checkmark (plus the SS-C-8 ordering witness from the banked row).

## Closing verdict

**ADJUDICATED — the blocker weight is entirely banked-and-folded (contour destruction · armed editor keymap ×2 · data-dead `/v/` · non-idempotent publish · the 50-vs-200 default), with this file's own new severity concentrated in nine MAJORs of upload/error/busy posture plus the Configurator producer-API bypass; 56 register rows (5 blocker-weight folds · 22 MAJOR · 25 MINOR · 4 INFO); 11 kills banked — including the resolution of the two re-readers' mutually incompatible D-10 refutations at the banked border-none chain, and the D-27 conflict settled by this seat's own grep; 10 missed finds adopted after seat re-proof (2 of them the readers' catches of each other's blind spots: the subject's own `:163` cartoon-card and the transparent-PNG export); 13 SS-13 probes queued in priority order; routings are M-25 agglomeration input for the forming X·F specs — F.W1 owns the uplift leverage (this closure as first cut) + the bypass-CSS deletion moment, F.W3/W4 owns the upload/error/busy/a11y/export clusters and the banked repair units, F.W4 owns the hygiene band, F.W5–W8 owns the noun/positional vocabulary + the provenance riders, and the §4 method row + producer-owned rows go to SS-3/SS-4 and the glass BH relay with NO-WAVE-OWNER; nothing here stamps a wave.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
