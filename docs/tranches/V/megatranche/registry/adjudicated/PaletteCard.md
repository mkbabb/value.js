SEAT RECEIPT — served model id: `claude-fable-5` (Fable 5), FRESH FABLE ADJUDICATOR (M-23/L-18), observed in this seat's own system prompt, not inherited.

# PaletteCard — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Date**: 2026-08-06 · **HEAD**: `1caf0f11` · branch `tranche-u` · **Subject**: `demo/palettes/browser/card/PaletteCard/` (six SFCs, 876 L) + the card/ siblings it is inseparable from (`SwatchHoverMenu.vue`, `PaletteColorStrip.vue`, `PaletteCardSkeleton.vue`, `ShadowPalette.vue`, `composables/` ×4).
**Drift disclosure**: `git diff --stat c654824e..HEAD` over the cone (`demo/palettes/browser/card/ demo/styles/ demo/workbenches/ demo/color-session/ demo/ui/ tsconfig.demo.json`) touches only `demo/palettes/api/*` + `useAdminUsers.ts` — **the cone is byte-identical to the corpus HEAD**; every line anchor below resolves at both.
**Corpus**: `challenge-C-implementation.md` (pass 4, Opus 5[1m]) · `challenge-D-design.md` (pass 3, Opus 5[1m]) · `challenge-L-library.md` (pass 5, Opus 5[1m]); prior passes preserved beside them; ~40 probe scripts/results + `evidence/`. The current (latest-pass) files govern.
**Re-readers adjudicated**: design-and-usage · library-and-code. Both were challenged back: one of reader-1's confirmations is RESCOPED by reader-2's kill (R-1), one of reader-1's kills is itself extended (R-2), one reader-2 superlative is demoted against reader-1's missed row (R-3), and two superlative limbs — one from each reader — are struck (R-4, R-5).
**Motion law**: `audit/codex-provenance/motion-quarantine.md` — FALSE rows may not be carried. Rows M-03, M-07, M-11, M-17, M-26, M-29, M-31, M-32 re-read this session; each is the "no per-component PRM design" claim shape ruled FALSE against the two-guard central architecture (Guard 2 = glass-ui `a11y-overrides.css`, sha `511e861a…`, re-read byte-for-byte this session).
**Wave authority for routing**: tranche X ratified — `docs/tranches/X/waves/W0..W11`. **X.W7** (Palette Specimen and Domain Split) owns `demo/palettes` whole: X.W7.c extracts a props-only `PaletteSpecimen` (zero CRUD imports, zero nested interactive controls; shadow-at-root + hover register = **G11**; CC-069 maps owner mark MT-F036 → BUILD X.W7.c); **X.W7.d moves rename/menu/feedback/versions/tags/export/publication/vote/feature/delete to the selected-entity inspector and DELETES `PaletteCard.vue`**; X.W7.f owns display formatting incl. `PaletteCardMeta.vue`; CC-077 folds this registry file into X.W7.c/d at wave open. Every component-local cure below is therefore a requirement on the SUCCESSOR surface, not a patch to this file.
**Arbiter's own instruments (all static; zero browser tooling per law)**: full reads of PaletteCard.vue (364 L whole), SwatchHoverMenu.vue, PaletteColorStrip.vue, PaletteCardSwatches.vue, PaletteCardMeta.vue, PaletteRenameInput.vue, useHeightTransition.ts, useHoverPopover.ts, useLeaveTimer.ts; ActionFeedback.vue ARIA/timer greps; PaletteCardMenu.vue emit census (17 verbs, no `copyAll`); host reads PalettesPane.vue:82-97 · BrowsePane.vue:92-107 (+15-listener count) · AdminUsersPanel.vue:138-150 · ExtractWorkbench.vue:143-155 · MixSourceSelector.vue:244-268; producer bytes: `useLiquidPress-BOxuDkKa.js` pressStyle + handler bag (the R-1 decider), `watercolor-dot.js` (`inheritAttrs:!1`, span, `aria-hidden`, `pointerEvents:"none"`, class `watercolor-swatch` ×2), `components/card/styles.css` (cartoon-surface = 3 declarations; `--card-pad-inline` `.card`-scoped; `[data-variant="selection"][data-selected="true"]` register), `styles/glass.css` import list (18 sheets; glass-atom.css absent, liquid-enter.css present), `a11y-overrides.css` whole, `base-misc.css` `.gold-shimmer` block, `composables/dom/useClipboard.d.ts` (CopyResult); demo bytes: foundation.css:104-105 gold pins, :91-92 `@source` pair (+`ls color-picker` → No such file / `demo/color-picker` exists), :653-700 + :793-841 COLOR-SURFACE ROSTER (both media blocks), useContrastSafeColor.ts:60-78/:340-362; greps: `copyAll` = 1 hit repo-wide (the definition), `floating-panel` = 0 CSS rules anywhere incl. glass dist, `<PaletteCard` = exactly 5 hosts (GenerateControls.vue = 0), `card-press-t` = 0 hits outside PaletteCard.vue, `find demo -iname '*PaletteDialog*'` = 0; tsconfig.demo.json:37-50 vs `package.json#exports` (7 keys, no `.`) vs `ls dist/ dist/subpaths/` (no index.d.ts; no parsing/units barrels; value/css shipped); the malformed audit path probed with `find -type f` + `git ls-files` + HYDRATION-LEDGER.md read (the R-4 decider); motion-quarantine rows; X.W7/W8/W9/W10 wave specs; prior adjudications PaletteCardGrid.md, ActionFeedback.md, ColorSpaceSelector.md, GenerateControls.md (identity guards + the R-C collision).

## Scoreboard

**42 adjudicated rows CONFIRMED-family (8 BLOCKER · 14 MAJOR · 16 MINOR · 4 INFO), of which 5 are IDENTITY-FOLDS to rows already banked in this registry (never re-booked) and 17 were MISSED by the challenge corpus and adopted from the re-readers (+1 arbiter-found coordination collision) · 16 claims KILLED/DEMOTED (13 reader kills ratified or extended + 3 superlative limbs struck by this seat) · 12 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

Blocker-weight headline: **the card's entire subject is unreachable in every register.** The colour strip is `aria-hidden` and dies in WHCM/print (PC-7); the expanded faces are `aria-hidden` spans with `pointer-events:none` whose `tag="button"`/`aria-label`/click are producer-dropped on BOTH the hover and the touch path (PC-3); the hover action panel positions itself with inline coordinates no stylesheet can seat (PC-2); the one bulk-copy verb is dispatch-registered and emitted by nobody (PC-8); and under PRM the expand/collapse Transition never completes at all (PC-1). A palette card from which no colour can be perceived by AT, copied by any input path, or (under PRM) even collapsed.

## Ruled disagreements (the incredulity seat's actual work)

| id | design-and-usage (reader 1) | library-and-code (reader 2) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **R-1 · the press spring** | CONFIRMED D3-4/C4-10 whole, incl. "the `--card-press-t` spring is dead… a property with no readers"; cures incl. delete `useLiquidPress` | K-1: the spring is LIVE — pressStyle emits inline `scale`; only the custom-property limb is unread | **reader 2 WINS; D3-4/C4-10 RESCOPED** | My byte read of `useLiquidPress-BOxuDkKa.js`: `e > f && (o.scale = \`${i} ${a}\`)` with PaletteCard's `shrinkDepth: 0.02` → an inline `scale ≈ 0.98…/0.98…` paints on every press via `:style="press.pressStyle.value"` (PaletteCard.vue:25); the handler bag is `{onPointerdown(button 0), onPointerup, onPointercancel, onPointerleave, onPointerenter(buttons&1), onKeydown(Enter/Space), onKeyup, onBlur}`. Surviving limbs: zero `:hover/:active/:focus-visible` rules reach the root (cartoon-surface = 3 static declarations, my bytes); `.cartoon-cast` matches nothing (R-1b below); `--card-press-t` has 0 readers (my grep); the :7-18 template comment stays false in every clause but "2px border" — the squash that DOES paint comes from JS, not the atom it credits. Both prescribed cures REFUTED: deleting `pressVar` leaves the default `--press-t` still scaling; deleting the composable removes real shipped behaviour. **Consequence adopted**: reader 2's missed container-press row is REAL (PC-20). Every probe cited for "dead" measured HOVER, never a press — recorded in DISSENT as a method lesson. |
| **R-1b · `.cartoon-cast` provenance** | Kill C4-11a: premise false — liquid-enter.css declares it, loaded but scoped | L-44 CONFIRMED same correction | **UNANIMOUS after correction; ratified** | My read of `dist/styles/glass.css`: 18 imports, `glass-atom.css` absent, `liquid-enter.css` present; the reachable rules are all `.liquid-enter.is-cel > .cartoon-cast` and the card root carries neither class. The span is inert because the loaded rule does not MATCH, not because the sheet is missing — which is exactly why the earlier "restore the import" cure would have shipped an unscoped duplicate of a producer-private child. C4-11a's cell dies; the inert-span conclusion survives inside PC-4/PC-16. |
| **R-2 · forced-colors cure routing** | D3-1 CONFIRMED; killed the BJ-routing for the strip limb but KEPT "the expanded WatercolorDot faces inherit the same fate… a producer ask" | K-2: BOTH limbs demo-owned — the roster already covers `.watercolor-swatch`; the generic `[data-color-surface]` hook has zero consumers | **reader 2 WINS WHOLE; reader 1's partial kill was itself too narrow** | My reads: foundation.css:653-700 tier-1 roster contains `.watercolor-swatch` (forced-color-adjust: none) and :793-841 repeats it under `@media print` with `print-color-adjust: exact`; `watercolor-dot.js` emits the class unconditionally (2 byte hits; corroborated by GenerateControls.md R-C's dist render-fn read). So the expanded faces are ALREADY protected; the sole gap is the compact strip — `PaletteColorStrip.vue:13-23` paints bare `div.shrink-0` with inline backgroundColor, member of NO roster, in a folder with zero `print` hits. D3-1 RESCOPED to PC-7 (strip-only, demo-side, one attribute/selector wide, WHCM **and** print). **Arbiter's own coordination find**: GenerateControls.md R-C ruled `[data-color-surface]` "a dead selector, also deletable" — if PC-7's cure makes the strip that hook's first consumer, it supersedes the deletable ruling; X.W7.c must reconcile (roster-class membership for the new specimen strip, or an explicit revival of the hook — never both waves acting blind). |
| **R-3 · the touch Popover path** | MISSED row: `as-child` + `inheritAttrs:!1` makes the reka Popover trigger structurally inoperative; the :38-39 "accessible route" comment describes a route that does not exist | Superlative #7 praises the same two-path split as "deliberate and correctly branched" | **reader 1's find ADOPTED (PC-3); reader 2's superlative DEMOTED to the gate only** | SwatchHoverMenu.vue:13-21: `<PopoverTrigger as-child>` over a component child; reka's slot-merge delivers the trigger's onClick/data-state/aria-expanded/ref as component props → non-declared → `$attrs`; the producer d.ts declares `{color, variant, animate, cycleDuration, range, seed}` only and the compiled dot forwards class+style alone onto a span that is `aria-hidden` with `pointerEvents:"none"` (my bytes). Nothing operable can reach the DOM. The branching hygiene at useHoverPopover.ts:26-27 (media query AND pointerType) remains praiseworthy AS A GATE; what it defers to is dead. One honesty note: the merge semantics are byte-derived, not live-tapped — the live tap is residue row U-10. |
| **R-4 · the malformed pass-2 path (K-7)** | Superlative #6 praises the L-axis's housekeeping ("empty directory… receipt proving no content was lost") | K-7: FALSE against the shipped tree — the path exists, holds a 20,018-byte CHALLENGE-L report, git-tracked, ledger-banked | **reader 2 CONFIRMED by this seat's own probe; reader 1's superlative limb STRUCK** | My `find` on `audit/components/PaletteCard/'challenge-L-library.md (pass 2; …)'`: ONE regular file, 20,018 bytes; `git ls-files` matches it; HYDRATION-LEDGER.md banks it as EXISTS-ORIGINAL, sha `49f2ef62…`. challenge-L-library.md:41-45's "zero files… Removed. No report content existed in it" does not describe the tree. Reader 2's timestamp caveat preserved (a post-deletion re-hydration may have recreated it) — either way a canonical report sits at a malformed path. **Registry repair item (NO-WAVE-OWNER, registry not product): re-home the file to a well-formed name, reconcile the ledger row, and correct the pass-5 housekeeping note by addendum (E-3), never by rewrite.** |
| **R-5 · "PaletteRenameInput has no defects"** | MISSED row: the Check/X buttons have no accessible name — in the folder that documents the opposite rule 4 times | Superlative #6: "the one file in the folder with no defects found by any seat including this one" | **reader 1's find ADOPTED (PC-31); reader 2's superlative claim FALSE as stated, demoted to scoped praise** | My read of PaletteRenameInput.vue:18-30: `<button type="submit">`+`<Check>` and `<button type="button">`+`<XIcon>` — no aria-label, no title, no text node; four sibling buttons in the same folder carry explicit `:aria-label` WITH W5-a11y comments naming the rule (PaletteCardSwatches.vue:12-19 and the three action buttons; PaletteCardMeta.vue vote; PaletteCard.vue:100). The file's genuine virtues (submit policy, focus-and-select, escape handling) stand in SUPERLATIVES with the asterisk. |
| **R-6 · the emit census (L-46 / K-3)** | CONFIRMED L-46 as filed (six hosts, Mix 1/16, mean 5.2) | K-3: the table invented a host and miscounted two rows | **reader 2 WINS on the numbers; the finding survives WORSE** | My greps/reads: `<PaletteCard` in exactly FIVE files — GenerateControls.vue contains none (grep -c = 0; it renders PaletteColorStrip only); MixSourceSelector.vue:264-267 wires ZERO listeners (the click is the wrapper's); PalettesPane wires 6; BrowsePane 15 (my count of the listener block); AdminUsersPanel 3; Extract 4. Corrected census **15/6/3/4/0 over five hosts**. A user can open the menu on a Mix card, pick Delete, and the `if (!fn) return`-less dispatcher emits into zero handlers — silence by construction (PaletteCard.vue:315-318 masks only UNKNOWN verbs; known verbs emit to nobody). |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Original corpus ids ride for life; PC-ids are this record's spine. Line anchors resolve at HEAD `1caf0f11` (= corpus cone bytes).

### BLOCKERs

- **PC-1 · BLOCKER — under `prefers-reduced-motion` the expand/collapse Transition never completes and the swatch subtree is never unmounted.** (C4-1; both readers CONFIRMED; three legs re-derived by this seat: useHeightTransition.ts:35-39/:66-70 gate the only `done()` on `propertyName === "height"`; a11y-overrides.css rewrites the PRM transition set to five colour properties `!important` — height absent — beating the inline `style.transition` at :30/:61; 2-arity JS hooks mean Vue waits on `done()` forever, and the opacity transitionend that DOES fire returns early without removing the listener.) Node-retention arithmetic is residue U-1. **Rider (PC-37 folds here)**: the `<Transition>` at PaletteCard.vue:131-138 has neither `name` nor `:css="false"`, so Vue also stamps undefined `v-enter-*`/`v-leave-*` classes and runs nextFrame+reflow machinery around hooks that already force three reflows of their own (:32, :56, :63) — pass-4's own capture shows `"overflow-hidden v-enter-"` on the panel. **→ X-W7.c/d** (the Transition dies with the card; the successor's disclosure motion must be PRM-safe by construction) **+ X-W10 rider** (the motion canon owns the law: a JS-completed transition may never gate `done()` on a property the central PRM policy strikes). Cite M-03/M-07 posture: the 0.1 s colour floor itself is correct central governance, NOT part of this defect.
- **PC-2 · BLOCKER — `.floating-panel` is declared in no stylesheet; the teleported hover action panel is `position: static` and its inline top/left are inert.** (C4-12; both readers; my grep: zero rule declarations across demo/src/e2e and glass dist — the three non-usage hits are comments/prose, so DESIGN.md:273's producer-provenance claim is stale too.) The panel is the ONLY hover-device route to per-colour Add/Edit/Copy (SwatchHoverMenu.vue:37-51). Rendered geometry (the −720 px rect) is residue U-2. **→ X-W7.c** (the hover path dies with the specimen split; swatch actions move to the inspector at W7.d).
- **PC-3 · BLOCKER — the swatch is not a control on ANY input path.** (D3-2/C4-13 + reader-1's missed popover row, merged; R-3 ruled.) Producer bytes: no `tag` prop in glass-ui 7.0.0 WatercolorDot; `inheritAttrs:!1` forwards class+style only; the span ships `aria-hidden="true"` + `pointerEvents:"none"`. So SwatchHoverMenu's `tag="button"`, `:aria-label`, `@click.stop` (:17-18, :32-35) all drop on the hover path, AND the reka `PopoverTrigger as-child` merge drops on the touch path (R-3) — the :38-39 comment "the reka-ui Popover (touch path) is the accessible route" is false. The compact strip is `aria-hidden` by the consumer's own hand (PaletteColorStrip.vue:3-4). Causation identity: ≡ GenerateControls.md R-D — **consumer debt, not a glass seam** (the producer never promised `tag`; its own defaults are right, see SUPERLATIVES). **→ X-W7.c** (PaletteSpecimen: zero nested interactive controls; a NAMED activation seat) **+ X-W7.d** (per-colour actions move to the inspector). The 6-site dead-`tag` census rides the standing glass relay as migration input only (GenerateControls.md R-D booking; not re-booked here).
- **PC-4 · BLOCKER (RESCOPED per R-1) — the card root has zero hover/focus feedback and a false choreography comment; the press register is live but landlocked.** (D3-4/C4-10 as rescoped.) Confirmed limbs: no `:hover/:active/:focus-visible` rule matches the root anywhere (producer atom static; no `hover:`/`group-hover:` consumer in the folder); `.cartoon-cast` (:30) matches nothing (R-1b); `--card-press-t` has zero readers; the :7-18 comment describes translate/shadow-bezier/:active-squash choreography that exists in no loaded rule. Killed limb: "the spring is dead" — the inline `scale` squash paints on every press (my bytes). **→ X-W7.c G11** (hover register = designed elevation/material shift at the root; CC-069/MT-F036 BUILD).
- **PC-5 · BLOCKER — `contain: content` on the zero-padding grid paint-clips the card's −3/−5/−7 px cartoon caster; every card loses its left facets, the last its bottom.** (D3-3; both readers; mechanism source-certain, corner witness residue U-4.) **IDENTITY ≡ PaletteCardGrid.md PG-4 ≡ BrowsePane.md M9·D5-03 — already banked → X-W7.c G11**, with PG-4's rider intact (audit the second clip ancestor `.pane-scroll-fade`). Not re-booked; recorded here so the card-side citation trail is complete.
- **PC-6 · BLOCKER — at 390 px the palette name renders at 0 px while the row reserves two line boxes for it.** (D3-5; reader 1's sharpened mechanism ADOPTED: `line-clamp-2 sm:line-clamp-1` (:55) sets `overflow: hidden`, which resolves the flex item's `min-width: auto` to 0 — every sibling in the row is `shrink-0`, so the title is the only crushable member and it crushes to nothing.) Pixels are residue U-5. **→ X-W7.c** — the wave mechanism ALREADY names the cure verbatim (`min-w-0 overflow-hidden` + declared collapse priority + `+N` overflow chip; the A-20 ruled interim).
- **PC-7 · BLOCKER (RESCOPED per R-2) — the compact colour strip is absent from the demo's own COLOR-SURFACE ROSTER: five declared colours collapse to one system colour in WHCM and are dropped for ink economy in print.** (D3-1 as rescoped + reader-2's missed print limb, adopted.) The expanded faces are already covered (`.watercolor-swatch`, both rosters, producer-emitted class); the strip is the whole gap; `[data-color-surface]` has zero consumers. Cure is one demo-side declaration; **NO producer ask** — the corpus's BJ routing is retired. **→ X-W7.c** (the rebuilt specimen strip joins the roster) **with the R-2 reconciliation rider against GenerateControls.md R-C's "deletable hook" ruling.** WHCM/print pixels residue U-3.
- **PC-8 · BLOCKER (AGGREGATE; arbiter-framed from reader parts) — the card ships no functioning colour-copy path in any register.** `copyAll` is dispatch-registered (PaletteCard.vue:294) and emitted by NOBODY — my grep: one hit repo-wide, the definition; PaletteCardMenu emits 17 verbs, never `copyAll`. The per-swatch Copy button lives inside the unpositioned panel (PC-2) behind an inert trigger (PC-3). The sole working copy control is the slug chip (PaletteCardSwatches.vue:13-19) — which copies the slug, not a colour, and only where `show-slug` is passed. A colour tool whose palette card cannot surrender a colour. **→ X-W7.c/d** (the inspector owns copy verbs; the export/copy surface must be reachability-gated at wave open).

### MAJORs

- **PC-9 · MAJOR (RESCOPED per R-6/K-5) — the card's primary action is pointer-only: no tab stop, no button semantics, no keyboard activation.** (C4-3.) `role="article"` + `cursor-pointer` + `@click` (:19-26), no `tabindex`/`aria-expanded`. The "no @keydown" support sentence is KILLED — `v-bind="press.handlers"` (:24) installs onKeydown/onKeyup/onBlur — but they drive only the spring, and with tabIndex −1 no keystroke can reach the element anyway. **→ X-W7.c** (named activation seat) / **X-W7.d** (expansion becomes inspector selection).
- **PC-10 · MAJOR — `role="list"` owns zero `listitem`s in both states.** (C4-2.) **IDENTITY ≡ PaletteCardGrid.md PG-3 ≡ EmptyState.md ES-9 — already banked → X-W7.** Not re-booked.
- **PC-11 · MAJOR — Mix nests the card's interactive content inside a wrapper `<button>` and forks selection as a 75 % opacity dim over a live chromatic field.** (D3-9/C4-4; census corrected per R-6: Mix wires ZERO of 16 emits.) The producer already ships `[data-variant="selection"][data-selected="true"]` (my bytes). **→ X-W7.c** (specimen adoption at the Mix consumer; selection state via the producer register at the host seat).
- **PC-12 · MAJOR (MERGED) — the title span is a click dead-zone at three of five hosts and a rename hijack on coarse pointers at the other two.** (D3-7 + reader-1's missed `@click.stop` row, adopted.) `.stop` at :58 kills propagation unconditionally (withModifiers stops before the guard runs); `editableName` is passed only by PalettesPane and ExtractWorkbench — at Browse/Admin/Mix the largest target in the row does nothing, and at Mix it defeats the wrapper's own selection toggle. Where editable, a tap renames instead of expanding (probe-D5g: `swatchCount: 0` + the rename row's pencil/check/x buttons mounted) with the sole affordance a hover-only dashed underline. **→ X-W7.d** (rename moves to the inspector; the title stops being a secret control).
- **PC-13 · MAJOR — every async verdict is unannounced; ActionFeedback has no live region.** (C4-5.) **IDENTITY ≡ ActionFeedback.md AF-1 (banked 2026-08-03, with the AF-2/3/5 cluster) → X-W7.** Not re-booked; this corpus adds only the card-side reach map (:123-128, defineExpose :244, cardRefs at both panes).
- **PC-14 · MAJOR — the `aside` layout arm is unreachable dead surface, and it drags PaletteColorStrip's `orientation` prop down with it.** (D3-8; both readers; exhaustive consumer chain re-checked at the hosts: ExtractPane hard-codes `layout="column"`, the sole `:layout` ternary can never yield `'aside'`.) **→ X-W7.c** (the specimen rebuild deletes the arm; W8's dead-surface gate is the backstop).
- **PC-15 · MAJOR — the "one shared shell" has three hand-typed homes already drifted: card = `cartoon-surface` (md shadow / 2 px border, producer bytes), skeleton + ShadowPalette = `border … shadow-cartoon-sm` (1 px), against DESIGN.md:98's "PaletteCard (+skeleton — one shared shell)" and utils.css's "the chip-scale elevation PaletteCard rides".** (L-43.) **The corpus's dueling-cures INFO row (reader 1 missed #10) is CONFIRMED as an observation and DISCHARGED as an owner-ruling need**: VISUAL-CONSTITUTION:186's no-shadow reading and DESIGN.md's sm-rung reading are both superseded by the RATIFIED X.W7.c mechanism — "Shadow follows the rounded silhouette at the card root" (G11; CC-069 maps MT-F036 → BUILD there). Neither a `shadow: false` patch nor a `.well-card` sm mint may land ahead of it. **→ X-W7.c G11.**
- **PC-16 · MAJOR — `.cartoon-cast` has two producer homes; the loaded one is descendant-scoped and unreachable from this root; the hand-authored span is inert markup.** (L-44 + C4-11a as corrected, R-1b.) **→ X-W7.c** (the span deletes with the rebuild; if G11 wants the cel cast, the correct vehicle is the producer's own `liquid-enter is-cel` contract or a glass-relay ask — never a local unscoped duplicate).
- **PC-17 · MAJOR — tsconfig.demo.json's map of the published surface is 3/8 phantom (bare root, /parsing, /units → missing d.ts) and omits two shipped keys (/value, /css); its header comment states the falsehood as a "CLOSED 8-key set".** (L-45; my `ls dist/` + exports read.) **IDENTITY ≡ ColorSpaceSelector.md L-14 (booked to config parity with a born-RED generator command) → X-W9** (the public-surface law / 4.1 cut). Not re-booked; this corpus adds the header-comment falsehood as a correction obligation on whichever wave touches the file.
- **PC-18 · MAJOR — the loading ghosts model a component that does not exist: a 56 px swatch row the collapsed card never renders, `overflow-hidden` the card explicitly refuses, sm where the card wears md.** (D3-6, minus the CLS narrative — see KILLED #6.) **→ X-W7.c** (the skeleton mirrors the specimen, one shell source; PC-15's G11 governs the shell).
- **PC-19 · MAJOR [MISS-adopted, reader 1] — the Featured badge's gold shimmer cannot reach text contrast in light mode, and the producer's PRM gate parks the gradient at its least legible stop permanently for exactly the users who asked for less motion.** Mechanism byte-certain (my reads): `.gold-shimmer` = 5-stop gradient, `background-size: 250% 100%`, clip-text, `color: transparent`; the sweep lives ONLY under `no-preference`, so under reduce `background-position` rests at `0% 0` — the near-white `--color-gold-light` (#F5E6A3, pinned scheme-invariant at foundation.css:104-105 while `--color-gold-dark` stays producer-owned: a chimeric ramp) sits mid-label over the light `bg-well` ground. The scoped border (`--color-gold`) shares the fault. The exact ratios are residue U-6; the conclusion (no stop reaches 4.5:1 on light well) is robust. D-design.md:1028-1029 lists this code under "genuinely right" — struck. **→ X-W7.c** (the badge rides the specimen) **+ X-W10** (the metal register's PRM resting frame is a canon decision: a shimmer must PARK legibly).
- **PC-20 · MAJOR [MISS-adopted, reader 2] — the press register is bound to the article CONTAINER, so every descendant interaction squashes the whole card: Space/Enter typed in the rename input, pointerdown on the menu/vote/copy/swatch buttons, and pointerenter with a held button during a sortable drag.** Wiring byte-certain (R-1's handler bag; pointerdown/keydown bubble; the folder's four `.stop` modifiers stop `click` only; `@keydown.escape.stop` stops Escape only). Visible amplitude (~2 %) is residue U-7. **→ X-W7.c G11 / X-W7.d** — the press register belongs on the named activation seat, not the container.
- **PC-21 · MAJOR [MISS-adopted, reader 2] — all four clipboard writes discard the producer's discriminated `CopyResult`; copy verbs confirm nothing and swallow every failure while the component owns a purpose-built verdict surface it never uses for them.** (`writeClipboard(text): Promise<CopyResult>` d.ts read; `void writeClipboard(…)` at PaletteCard.vue:294/:333, bare call at PaletteCardSwatches.vue:16, useSwatchActions.ts; ActionFeedback + showFeedback sit idle beside them.) **→ X-W7.d** (verdicts move to the inspector's feedback seat, which the AF-cluster adjudication already requires to be announced and identity-keyed).
- **PC-22 · MAJOR (RESCOPED per R-6) — the emit surface is a 16-verb bus that the average host wires 5.6/16, with corrected census 15/6/3/4/0 over FIVE hosts (the sixth was invented), and a Mix menu whose every verb emits into silence.** (L-46 as corrected by K-3.) **→ X-W7.d** (menu verbs become the inspector's, availability-gated per host capability — the K-INV5 pattern already in PaletteCardMenu is the model).

### MINORs

- **PC-23 · MINOR — no timer in the folder is disposed** (C4-9; zero `onUnmounted/onBeforeUnmount/onScopeDispose` across all ten files; useLeaveTimer per-card at :255, ActionFeedback's 2500 ms emit-after-unmount). The ActionFeedback limb is banked at ActionFeedback.md; the useLeaveTimer/useHoverPopover limb is card-owned. **→ X-W7.c/d.**
- **PC-24 · MINOR — `px-3` (12 px) hand-codes what the producer canon binds at 16 px; `--card-pad-inline` is unreachable because the root is a bare div, not `.card`** (D3-16; my bytes: the variable and its five derived relations live only inside `.card`/`.card[data-size]`). **→ X-W7.c.**
- **PC-25 · MINOR (RESCOPED per K-4/K-6) — `EMPTY_PALETTE_SWATCH` is unreachable at every call site (all five hosts pass non-nullish `cssColor`; Mix's `''` defeats `??`), and the "designed empty-state swatch" comment describes a childless flex band nothing designed. BUT the `useSafeAccentFn("well")` limb is NOT dead: it bumps the global probeEpoch on every mount and its `inject(INK_AMBIENT_KEY)!` makes the card unmountable outside the boot tree; and `show-slug` is live at TWO sites (Browse + AdminUsersPanel), not one.** (C4-7/D3-17 as corrected.) **→ X-W7.c** (the specimen decides the zero-colour state explicitly).
- **PC-26 · MINOR — the drag handle is a 16 px unnamed `<svg>` with no keyboard reorder path, bound from the pane's options bag as a bare class-string contract** (C4-8/D3-10). The sortable data-integrity rows are already banked (PaletteCardGrid.md PG-1/PG-2 → X-W7.d, named-addition rider). **→ X-W7.c/d.**
- **PC-27 · MINOR — the disclosure exposes no state to AT: no `aria-expanded`, no `aria-pressed` anywhere in the file** (D3-19). Dies with the disclosure itself. **→ X-W7.d.**
- **PC-28 · MINOR — a dead `@source "../../color-picker/**"` governs (vacuously) the JIT emission of every utility in the cone; the path resolves to a nonexistent `<repo>/color-picker`, masked by the `../**` sibling, while the file's header sells the explicit scan as CI-assertable coverage** (L-48; my `ls`). **→ X-W10** (the AF-24 precedent: canon fold of style-authority debris).
- **PC-29 · MINOR — the strip's `rounded-t-card` arc exceeds the card's inner arc by exactly one 2 px border-width; the concentricity is broken by construction because the card refuses a root clip and the strip clips itself** (D3-14; arithmetic source-certain, pixels residue U-5). **→ X-W7.c G11** (silhouette law).
- **PC-30 · MINOR — one shared `expandedId` ref carries two disjoint key spaces: PalettesPane keys `palette.id`, Browse/Admin key `palette.slug`, while the composable's own writers use the id axis** (L-47; my host reads + usePaletteActions.ts:24-29). **→ X-W7.d** (selection identity becomes the inspector's, one key law).
- **PC-31 · MINOR [MISS-adopted, reader 1; R-5 ruled] — the rename confirm/cancel buttons announce as bare "button" in the folder that documents the icon-button naming rule four times.** **→ X-W7.d** (the rename seat rebuild).
- **PC-32 · MINOR [MISS-adopted, reader 1] — the colour-count badge is an unlabelled bare numeral, and it is the only quantitative fact about the specimen that reaches AT** (:72-74; the strip and faces are both aria-hidden — PC-3/PC-7). The AT transcript of a five-colour palette is "Palette: … · Featured · 5 · Palette menu". **→ X-W7.c + X-W7.f** (W7.f owns count formatting at PaletteCardMeta; the label rides the same edit).
- **PC-33 · MINOR [MISS-adopted, reader 1] — `:title` is the card's only truncation recovery and it is unavailable precisely where truncation is total (coarse pointers at 390 px, PC-6); the recovery and the failure are disjoint by construction.** **→ X-W7.c.**
- **PC-34 · MINOR [MISS-adopted, reader 1] — Extract's card is unconditional `cursor-pointer` over `@click="() => {}"`: a false affordance with no hover register to contradict it (PC-4).** (My ExtractWorkbench.vue:152 read.) **→ X-W7.c/d.**
- **PC-35 · MINOR [MISS-adopted, reader 1] — the folder's own retired-idiom edict (":45-46 the /40 post-hoc alpha dies") is contradicted at ten-plus sites in the same six files** (`border-border/15`, `bg-muted/60`, `active:bg-accent/70` ×7, `hover:bg-accent/50`, `ring-ring/40` ×7, `opacity-55` ×2 — my reads corroborate the census). Same prose-vs-tree disease as PC-4's comment and PC-10's grid comment. **→ X-W7.c** authors the successor under **X-W10** canon.
- **PC-36 · MINOR [MISS-adopted, reader 2] — the dispatcher's rename special case is a no-op wearing an explanatory comment: `startRenaming()`'s first statement closes the menu the guard claimed to hold open** (:291-292 vs :281/:317; my read). **→ X-W7.d** (the dispatcher dies).
- **PC-37 · MINOR [MISS-adopted, reader 2] — the unnamed `<Transition>` runs Vue's CSS machinery and stamps undefined `v-enter-*` classes per toggle.** Folded as PC-1's rider; counted here so the find is not lost. **→ X-W7.c.**
- **PC-38 · MINOR [MISS-adopted, reader 2] — the default swatch-size utility string is triplicated (prop default :197, JSDoc :194, SwatchHoverMenu default :82) with no shared source, sixteen lines from a comment naming the exact discipline (`PANEL_LAYOUT`) it breaks.** **→ X-W7.c.**

### INFO

- **PC-39 · INFO [MISS-adopted, reader 2] — the cone's comments cite `PaletteDialog.vue` (with a line number) as a live consumer; no such file exists** (my find; constants.ts:6, useHoverPopover.ts:8, DESIGN.md:386). **→ X-W7.c/d** (dies with the rebuild); the DESIGN.md citations ride X-W10's canon pass.
- **PC-40 · INFO — the dueling-cures collision (VC no-shadow vs DESIGN.md sm-rung) — CONFIRMED as a corpus observation, DISCHARGED by ratification** (ruled inside PC-15; X.W7.c G11 is the standing answer). **No owner ruling required any longer.**
- **PC-41 · INFO — `demo/ui/*` forwarding shims: 19 dirs / 92 import statements / this card walks both roads five lines apart** (L-49). **IDENTITY ≡ the W-HYGIENE demo/ui extirpation (ColorPicker L-6 lineage) → X-W8**, whose hard gate names these exact numbers. Not re-booked.
- **PC-42 · INFO (arbiter's own, R-2/R-4 by-products) — two cross-record coordination items**: (a) the `[data-color-surface]` deletable-vs-first-consumer collision between GenerateControls.md R-C and PC-7's cure (X.W7.c must reconcile); (b) the malformed pass-2 path registry repair (**NO-WAVE-OWNER** — registry hygiene at the next formation boundary, per R-4).

## Killed-claims register (refutations preserved; nothing ruled away silently)

1. **D3-20a** ("the card still carries a 0.1 s transition… unowned… a utility nobody in this folder chose") — **KILLED under binding quarantine law**: the claim shape is the motion-quarantine FALSE family (M-03, M-07, M-11, M-17, M-26, M-29, M-31, M-32 — all re-read this session); the 0.1 s floor carries colour legs only, every spatial property struck from the set — central governance, correct by design. The C-axis's own measurement shows the card animates NOTHING at no-preference, so PRM adds a colour floor rather than shortening a motion. The true second limb is PC-1.
2. **C4-11a** ("glass-atom.css — the only sheet declaring `.cartoon-cast` — is not in the loaded cascade") — **premise KILLED** (R-1b): liquid-enter.css is loaded and declares it four times, scoped; the span is inert by non-match, not absence — the distinction that killed the "restore the import" cure.
3. **D3-18a** ("17 emits") — **KILLED**: 16 (my read of :200-218); third wrong denominator in the corpus (18 → 17 → 16). Substance (god-module bus) unaffected → PC-22.
4. **D3-1-cure** (both forced-colors limbs routed to BJ) — **KILLED WHOLE per R-2**, extending reader 1's own partial kill: the strip is demo-owned AND the expanded faces are already roster-covered; zero producer round-trips required.
5. **D3-17b** ("slug chip renders only on /#/browse") — **KILLED**: AdminUsersPanel.vue:147 also passes `show-slug` (my read).
6. **D3-6-cls** ("100 px of layout collapses under the user's scroll position") — **KILLED**: two below-fold ghosts replaced by a full page of rows is growth, not collapse; no CLS/anchor/page-size was ever measured. Geometry drift survives as PC-18.
7. **K-1** (reader 2, ratified R-1): the "dead spring" limb of D3-4/C4-10 and both its cures — **KILLED**; press paints inline `scale`; only `--card-press-t` is unread.
8. **K-2** (reader 2, ratified R-2): the producer-ask limb of D3-1 — **KILLED**; `.watercolor-swatch` is tier-1 in both rosters; the hook has zero consumers.
9. **K-3** (reader 2, ratified R-6): L-46's census — **KILLED AS PUBLISHED** (invented host GenerateControls; Mix 1/16 → 0/16; PalettesPane 7 → 6); corrected census 15/6/3/4/0, finding survives worse.
10. **K-4** (reader 2, ratified): C4-7's "useSafeAccentFn dead for every local card" limb — **KILLED**; `bumpProbeEpochOnMount()` is a global side effect on EVERY mount and `inject(INK_AMBIENT_KEY)!` is a mount precondition (my :340-362/:60-78 reads).
11. **K-5** (reader 2, ratified): C4-3's "no @keydown" sentence — **KILLED**; `press.handlers` installs live keydown/keyup/blur. The finding survives (PC-9); the hidden real defect it masked is PC-20.
12. **K-6** (reader 2, ratified): D3-18's "17 emits / 6 refs" and D3-17's browse-only scoping — **KILLED**; 16 emits, 5 refs, two show-slug sites.
13. **K-7** (reader 2, CONFIRMED by this seat's own probe, R-4): challenge-L-library.md:41-45's "empty… removed… no content existed" — **FALSE against the shipped tree**; a 20,018-byte git-tracked, ledger-banked CHALLENGE-L report sits at the malformed path. Timestamp caveat preserved.
14. **Reader 2's superlative #7** (the two-path split "deliberate and correctly branched") — **DEMOTED per R-3**: the gate is real hygiene; the route it defers to is structurally inoperative (PC-3).
15. **Reader 2's superlative #6 claim** ("no defects found by any seat including this one" for PaletteRenameInput) — **FALSE per R-5** (PC-31); demoted to scoped praise. The D-axis's "genuinely right" cell for the same file (:1019-1021) falls with it.
16. **Reader 1's superlative #6 limb** (the L-axis housekeeping "receipt proving no content was lost") — **STRUCK per R-4/K-7**. The C-axis's negative-results hygiene (its own killed hypotheses logged at :449-478) keeps its praise.

## Superlatives (provenance preserved; asterisks are load-bearing)

1. **PaletteColorStrip.vue:50-71** — the weighted-segment computation (explicit `weights` → the colours' own `weight` fields → equal; 8 % named floor; renormalised against `flooredTotal` so percentages still sum after flooring — the step everyone forgets). Both readers independently; this seat re-read it whole. The one place the folder treats the specimen as data. (X.W7.c's mechanism keeps the weighting and retires only the `Math.max(…, 0.5)` clip floor.)
2. **PaletteCardMenu.vue K-INV5 pattern** (:27-40, :48-60) — the tripped availability latch DISABLES the doomed verb and names the degraded state in-register through an injected client seam, annotating current visibility beside the flip verb. Better interaction design than most of the product; the model for PC-22's cure.
3. **card/index.ts:1-9** — named-only re-exports with the bundler rationale written at the decision point (SFC scoped-style side effects vs star re-exports).
4. **PaletteRenameInput.vue:48-65*** — submit policy treats unchanged/whitespace as cancel so `rename` is always a real mutation; `useTemplateRef` + focus-and-select on mount; `@keydown.escape.stop`. *Asterisk: PC-31 (unnamed confirm/cancel buttons) — "zero-defect" claims about it are dead (R-5).
5. **useHoverPopover.ts:26-27*** — the hover gate keys on BOTH the media query and the event's `pointerType`, so a hybrid device's finger tap never opens the hover panel. *Asterisk: R-3 — the touch route it defers to does not function; praise is for the gate alone.
6. **SwatchHoverMenu.vue:64-66** — `PANEL_LAYOUT` extracted with its anti-drift rationale inline, applied to both branches. (And sixteen lines later the file breaks the same discipline on `sizeClass` — PC-38. Both facts stand.)
7. **The producer's WatercolorDot defaults** — per-instance namespaced SVG filter ids, `respectReducedMotion`, `pauseWhenHidden`, unconditional `watercolor-swatch` roster class. On every axis the consumer cannot reach, the producer is right by default — which sharpens the consumer-debt ruling in PC-3.
8. **useContrastSafeColor.ts probeEpoch** (:56-78) — a detached-subtree cache-poisoning bug solved with ordinary reactivity, the measured failure recorded in the comment, `getCurrentInstance()` guarding the no-component case. (Also the mechanism that killed K-4's "dead code" claim — the virtue and the side effect are the same bytes.)
9. **The C-axis's negative-results log** (challenge-C-implementation.md:449-478) — four hypotheses its own probes killed, published so later passes don't re-spend them, plus an apparatus correction invalidating part of its own prior fixture. Self-correction with receipts. (The L-axis's equivalent claim did not survive R-4.)

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **U-1** — PC-1's retention arithmetic (607→1628 nodes, 6 zero-height panels, ~8500-node Browse wall) and the DERIVED tab-order consequence (retained `Copy slug` button). Mechanism source-certain; counts are probe-C4e's alone.
- **U-2** — PC-2's rendered geometry ({x:−720, w:1440, inViewport:false}) — where a static full-width block lands in THIS body flow.
- **U-3** — PC-7's pixels: WHCM five-colours→white witness and the print render (nobody has ever printed this card; zero print probes exist in the folder).
- **U-4** — PC-5's A/B corner witness (p3-corner-clip-ON/OFF.png) vs the owner's OM-12 closeup.
- **U-5** — the geometry family: D3-5's 0 px/61 px at 390; D3-6's 150 vs 100 px ghost; D3-11's single-column field ratios (never confirmed by any pass — geometry claims only); D3-12's dark ΔL 0.0053; D3-13's 71.9 px expand; D3-14's 2.0 px corner delta; D3-7's iPhone-14 tap replay on the SUCCESSOR surface.
- **U-6** — PC-19's exact contrast ratios (~1.03:1 / ~1.6:1 / ~3:1) — stops, gate, pins and type size are byte-certain; the ratios convert a prior pass's measured ground by hand.
- **U-7** — PC-20's visible amplitude: one live Space keypress in the rename input while watching the card's transform.
- **U-8** — the hover-fanout hypothesis (fresh `:floating-style` object identity re-rendering all N children; premise real at PaletteCardSwatches.vue:31, cost unmeasured).
- **U-9** — pass-2 **C-3**, the live `parseCssColor` throw taking Browse down — STILL OPEN after four passes and both readers; needs the authed e2e fixture, not another static pass. One datum against catastrophe: ink.ts's certifyAccentInk returns input unchanged on a falsy parse, so THIS component's ink path degrades silently. **Priority live probe.**
- **U-10** — R-3's live tap: one coarse-pointer tap on the Popover trigger to witness the as-child drop end-to-end.
- **U-11** — the press-handler blur/keyup interplay during drag (PC-20's pointerenter-with-held-button leg) on a real sortable drag.
- **U-12** — PC-15/PC-29's shell drift and corner delta as rendered pixels (feeds G11's before-frames).

## DISSENT — preserved, not ruled away

1. **The hover-for-press substitution (method lesson, recorded as evidence about the fleet).** Three challenge axes AND reader 1 concluded "the press spring is dead" from probes that only ever HOVERED (`evidence/L5-results.json .D_hover` `changedKeys:[]`; pass-4's Playwright `.hover()`); the L-axis's own probe log row even recorded the emitted `scale` while its prose asserted the opposite. Reader 2's byte read of the producer bundle settled it in one line. The lesson generalizes the MT-F019 signature-before-output failure: a probe must exercise the interaction the claim names, and a probe log that contradicts its own prose is itself a finding.
2. **Reader 2's K-7 timestamp caveat, preserved verbatim in substance**: the malformed-path artifact's mtime post-dates the pass-5 report, so a subsequent hydration may have recreated what pass-5 genuinely deleted — the ruling convicts the claim against the SHIPPED tree, not necessarily the seat's honesty at write time. The registry repair (PC-42b) settles it either way.
3. **Reader 1's D3-1-cure kill was directionally right and under-scoped**: it correctly pulled the strip limb back from the BJ relay but retained the producer-ask limb the roster already covers (R-2). Preserved because it shows a kill inherits the same re-derivation duty as a confirmation — half a kill can misroute a repair as surely as no kill.
4. **Severity posture**: neither reader contested another's severity grades; this seat kept reader gradings except where a rescope changed the mechanism's reach (PC-4, PC-22). No unresolved severity dissent remains.

## VERDICT

**ADJUDICATED — GUILTY AS RESCOPED, WITH THE CORPUS ITSELF CORRECTED ON SIX CELLS.** 42 rows survive (8 BLOCKER · 14 MAJOR · 16 MINOR · 4 INFO): the card's specimen is unreachable in every register (AT, WHCM, print, hover, touch, keyboard, copy), its disclosure hangs forever under PRM, and its interaction chrome is a comment describing a world the cascade does not contain — while the one live register the corpus had (the press spring) was the one thing all three axes swore was dead. 16 claims are killed or demoted with written refutations, including three superlative limbs and one quarantine-law violation; 5 rows are identity-folded to already-banked adjudications (PG-3, PG-4, AF-1, CSS-L-14, W-HYGIENE/demo-ui) and never re-booked. Every cure routes to ratified tranche X — **X-W7.c/d carry the component whole (PaletteCard.vue is scheduled for deletion; the requirements bind its successor), X-W7.f the counts, X-W8/X-W9/X-W10 the folded hygiene/canon rows** — plus one registry repair at NO-WAVE-OWNER. Twelve residue rows are the visual audit's input deck, U-9 first. Original challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
