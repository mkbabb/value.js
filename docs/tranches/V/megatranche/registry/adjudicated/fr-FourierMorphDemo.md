claude-fable-5 (served model id)

# fr-FourierMorphDemo — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **Subject**: `fourier-analysis/web/src/components/morph/FourierMorphDemo.vue` (330 lines; read whole by this seat) — the `/morph` route page — with its three children `MorphShapePreview.vue` (175, read whole), `MorphPhaseConfig.vue` (212, read whole), `HarmonicLevelGrid.vue` (286, read whole), and the composable pair `useFourierMorph.ts` (230, read whole) + `useMorphConfig.ts` (97, read whole) + `lib/easings.ts` (127, read whole).
**Substrate**: fourier `cd26c65` + dirty WT (28 porcelain rows — the M.W1a pin-bump family; every subject file clean) · installed glass-ui **4.0.0** · keyframes.js **4.3.0** · value.js **0.13.0** (all three pins node-read by this seat) · glass-ui producer at **8.0.0**, HEAD `dc4267fc` (moved since fr-FourierMorphSvg's `074a3d0e`; this seat's producer reads are at `dc4267fc`) — the corpus's "7.0.0" framing re-killed per banked SR-2/K-15, now for the **third** corpus.
**Corpus**: three axis challenges (`audit/fourier-components/FourierMorphDemo/challenge-{D-design,L-library,C-consumption}.md`, all opened and read at the disagreement points by this seat) × two hostile re-reader returns (reader-1 design-and-usage, slug `FourierMorphDemo` · reader-2 library-and-code, slug `FourierMorphDemo`).
**Method (M-23/L-18)**: every reader-vs-reader disagreement and every load-bearing verdict was re-derived against the tree with this seat's own instruments before ruling. One reader confirmation is REJECTED outright by this seat's own node run (m-5 — the first reader-kill-of-a-reader-confirmation in this folder family); one reader omission is ruled an error (D-4); five severity conflicts are ruled; both readers' registry folds all held under re-check. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node/grep only; zero browser probes — probe parsimony; product source READ-ONLY; this file the sole write)**: whole reads of the seven subject files above; the tree's own 24-sample `easingCurvePath` map run against installed value.js 0.13.0 (**the m-5 decider**: ease-out-back sampled v-max 1.08679 → minY **0.611**; ease-in-back maxY **19.546**; ease-in-out-back **0.519/19.478**; v(0.5)=1.06755 byte-matching reader-1's/C-8's cell); installed glass-ui probes — version 4.0.0, `grep -rn slider-scrub dist/` → **0**, `dist/styles/cards.css:33-49` `@utility cartoon-surface` read verbatim (rest `box-shadow: var(--shadow-cartoon-md)` · `translate: 0` · `transition: translate …, box-shadow …` · hover `translate: var(--lift-sm)` + `--shadow-cartoon-lg` — the D-21 + missed-hover decider), `dist/styles/theme/bridges.css:61-68` (`--font-serif: var(--font-stack-text)`) + `scheme-motion.css:43` (Plus Jakarta = **a sans** — the S-10-kill decider); producer-HEAD probes — package 8.0.0, `src/components/slider/types.ts:10` `SliderVariant = "scrubber" | "spectrum"` (no `"standard"`), slider token census `grep -rhoE -- '--slider-[a-z0-9-]+' src/components/slider/` → exactly **{range-bg, range-origin, target-floor, thumb-size, touch-target, track-height}** (no `--slider-thumb-bg`, no `--slider-track-bg` — the D-2-cure decider, byte-matching reader-2), `Button.vue:39-40` (`ButtonEmphasis`/`ButtonSize`, no `variant`) + `:127-140` the DEV `console.error` retirement warning ("exists and paints nothing… `variant` split into `emphasis`" — the D-3-silent-arm decider); keyframes probes — pin 4.3.0, `stop()` resolve-contract at d.ts:526 (`Animation`) AND :2195 (`NumericAnimation` — "The play promise resolves immediately", **the L-05-scope-kill decider**), `grep -c "as NumericAnimation" dist/keyframes.js` → 1 root export, `grep -c "@mkbabb/value.js" dist/keyframes.js` → **0**; dead-surface census — `updateField` 2 hits (decl :64 + return :92), `getIdlePoints` 2 hits (:110/:227), `morph.config` readers in FourierMorphDemo.vue → **0**; style.css:6/:14 (`--font-sans` = Computer Modern) + :20 (`@apply font-serif` on body) + :41/:47 (18px→16px root step); fourier + producer git pins; registry identity sweeps — targeted reads of fr-FourierMorphSvg.md (FM-1..FM-19 incl. FM-3's corrected cadence, FM-11's demotion + dissent, FM-12's rider text, K-2/K-8/K-11/K-15, SC-1), fr-AppHeader.md (FR-AH-2/-7/-8/-9/-11/-20/-21/-24/-31/-37/-53), fr-DarkModeToggle.md (N-4/N-12/N-14/N-15, M-3, M-8, K-2, SR-2, superlative 3 verbatim — "Three clamps in the right order… cannot escape the level table"), fr-BasisSelector.md (B-2/M-7/R-7/K-1), fr-EasingCurvePreview.md (WRONGPAD/K-8/BARREL/FORK/K-GEOM), fr-ConvergenceTimeline.md (K-8), fr-BasisCanvas.md (D-1), fr-App.md (B-1); `grep -ln computePreviewLevels adjudicated/*.md` → fr-FourierMorphSvg.md only, and only as the L-13 instance-count instrument — the missed-row identity guards hold.

## Scoreboard

**35 corpus rows ADJUDICATED-CONFIRMED (4 BLOCKER-tier, of which 2 are identity folds · 17 MAJOR · 14 MINOR; 13 of the 35 are folds-by-reference to banked rows) · 13 killed-claims register entries (11 reader kills RATIFIED — one decided by this seat's own node run — plus 1 reader confirmation REJECTED by this seat and 1 reader omission RULED an error) · 6 reader-missed finds adjudicated and booked (3 MAJOR · 3 MINOR, all seat-verified where load-bearing) · 10 superlatives ratified (2 corpus-level) · 2 bank corrections minted (R-7 leg (b); fr-DarkModeToggle superlative 3's scope).**

The component-owned headline is **FMD-1** — the harmonic-level strip ships two phantom levels (75, 100) the decomposition does not contain: three byte-identical tiles under three labels, a half-inert High range, a lying readout, and a frozen settle tail — the only BLOCKER-weight defect here that no banked row owns. The uplift headline is **FMD-11** — at producer 8.0.0 this component loses BOTH its Button prop axis (3 sites) and its Slider `variant="standard"` (3 sites), invisibly to every import-level sweep. The folder's kill rate under hostile re-read is low and every failure this seat found in the corpus is inference or provenance — not one arithmetic error survived to this record uncorrected.

## The incredulity seat's own work — ruled disagreements

| topic | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **m-5** (easing preview clips the back lobes) | CONFIRMED · MINOR | KILLED (magnitude) | **KILLED — reader-2 wins; reader-1's confirmation REJECTED** | This seat's node run of the tree's own `easingCurvePath` map (`y = 18 − v·16`, 24 steps) against installed value.js 0.13.0: sampled extrema **0.611 / 19.546 / 0.519–19.478** — the polyline centerline stays inside the 0–20 box with ≥0.45 units of headroom; only the 1.5-wide stroke's outer half pokes out by 0.139–0.296 units ≈ ≤0.3 CSS px at the 40×20 render. The lobe — "the entire semantic difference" the D axis said is lost — renders in full. Reader-1's own cell self-contradicts: it quotes v=1.06755 → y≈0.92, stroke edge +0.17, i.e. **no clip at all** at its own sample, then stamps CONFIRMED. Already banked at correct magnitude as fr-EasingCurvePreview **WRONGPAD** (with K-8's pad-exceeds-excursion arithmetic); folds there. The surviving residue — `.easing-preview` lacks the `overflow: visible` both sibling SVGs set (FourierMorphSvg.vue:36-40, `.grid-svg` :261-265) — is an INFO-weight consistency note inside FMD-32. |
| **D-4** (duration slider starved at 640-767 px) | CONFIRMED · BLOCKER | not booked (endorsed the arithmetic in its own UNPROVEN list, then omitted the row) | **CONFIRMED BLOCKER — reader-1 wins; reader-2's omission ruled an error** | Seat re-derivation from live literals: `.duration-row` flex with nowrap label + fixed 3.5rem input + `flex: 1 1 0%` slider (MorphPhaseConfig.vue:142-159, :167-168, :205-206) inside a 3-column grid (FourierMorphDemo.vue:264-269) at the 18px root (style.css:41). At 640-767 px the incompressible furniture (~172 px) EXCEEDS the card content (~132-152 px); flex items cannot shrink below min-content, so the slider's grown width is **0** and the row overflows the card. A 0-width track on the no-visible-thumb pull-the-TRACK idiom carrying 76 steps is no control. Statically proven; only the exact rendered pixels are SS-13. A page-primary control unusable across a whole viewport band is BLOCKER-weight. |
| **L-01/L-14/C-4 severity** (stop() resolves → Reset/unmount RESUME the coroutine) | BLOCKER | MAJOR (fold → FR-AH-9/FR-AH-21) | **MAJOR — reader-2 wins on booking discipline** | The mechanism is banked: FR-AH-9 (anti-teardown, stop-resolves, epoch-token cure — explicitly naming `/morph` as the LIVE site) + FR-AH-21 (setShape/setLevel race). A fold carries the bank's identity; re-rating a banked mechanism BLOCKER at a second site is the anti-rename failure. The component-NEW arm — `handleReset` unguarded (:175-178, vs guards at :128/:138) and no `:disabled` at :75 — books here at MAJOR (already recorded new at fr-FourierMorphSvg's L-01 fold row). Reader-1's BLOCKER preserved as dissent: the user-visible failure (Reset silently does not happen; the shape reset away from finishes rendering) is severe, but severity attaches to the family's one cure, not per-site. |
| **D-3 severity** (Button `variant`/`size="default"` gone at destination) | BLOCKER | MAJOR (inside its combined glass-contract row) | **MAJOR at F.W1 — with reader-1's BLOCKER dissent recorded** | Seat-read at HEAD `dc4267fc`: `ButtonEmphasis`/`ButtonSize`, no `variant` (Button.vue:39-40) — the break is real and invisible to import greps. But it manifests ONLY at the F.W1 uplift; at the installed 4.0.0 pin all three sites are fully functional (13-variant/6-size CVA in `dist/components/ui/button/index.d.ts:4-5`, seat-adopted from both readers), and the producer now emits a per-instance DEV `console.error` naming the exact mapping (:127-140). An uplift-day break with a voice at the destination books MAJOR on the F.W1 surface, not BLOCKER on today's tree. |
| **D-9/L-09 severity** (reduced-motion declined; false scheduler comment) | MAJOR (leg 1) + MINOR (leg 2) | MINOR (fold → FR-AH-31 + FM-11) | **MINOR-as-banked — reader-2 wins** | Both legs are banked with adjudicated severities: FR-AH-31 (PRM, MINOR with rationale) and FM-11 (the false `scheduler.ts:13-15` comment, MAJOR→MINOR demotion with the MAJOR dissent recorded). The D axis's correct NARROWING (glass's a11y blanket gates CSS motion — `a11y-overrides.css:6-16` property list omits `transform`/`translate` — so only the JS geometry writes are ungated) is adopted into the fold. This corpus + reader-1 constitute the **third** MAJOR dissent on this family; recorded, not ruled away. |
| **L-12/C-13 severity** (`as any` at the JSON boundary + 225 KB retention) | MAJOR | MINOR (fold → N-14/N-15/FR-AH-1) | **MINOR-as-banked — reader-2 wins** | Banked N-14 names FourierMorphDemo.vue:99-100 by line and rules the cast FORCED (TypeScript never infers tuples from JSON modules — fr-FourierMorphSvg K-11); N-15 owns the silent-degradation chain; FR-AH-1 owns the payload story. The surviving defect is `any`'s over-breadth, cured by N-14's narrower assertion — a MINOR at its banked home. C-13's own cure is killed separately (register, K-3). |
| **D-16 / D-22 severity** (15px glyphs; seven literal transitions) | MAJOR both | (not separately contested) | **DEMOTED to MINOR, both — seat's own ruling** | Both are real and mechanically proven (seat re-read :280-284 and all seven transition literals) but are token-hygiene defects: no capability loss, no truth defect, each a one-edit cure inside the same FR-AH-11 cascade cleanup. MAJOR is reserved on this roster for rows with a user-visible or structural consequence. |

## Adjudicated defect roster

Line anchors are fourier `cd26c65`; re-resolve before executing any cure. Folds carry the banked row's identity — original ids for life; nothing here re-books a banked defect.

### BLOCKER tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **FMD-1 · D-5 ⊕ L-03 ⊕ C-6** | The strip ships two phantom levels: `computePreviewLevels` hard-codes `[1,2,3,5,8,12,18,25,35,50,75,100]` and takes NO shape argument (useMorphConfig.ts:27-39, seat-read); both shipped shapes stop at 50 (`levels=[1..50]`, `n_harmonics=50`, reader-2's execution: `d(75)===d(50)===d(100)` TRUE, 57,029 chars each); `interpolateAtHarmonicLevel` clamps silently (svg-fourier.ts:130-131). Four consequences, all statically derived and concordant across all three axes: three byte-identical tiles labelled n=50/75/100 on first paint; the High control's 51-100 range inert (`max="100"` at :36/:44); clicking n=100 lights n=50 while the chip reads n=100 (the `nearestActiveLevel` domain crossing, FourierMorphDemo.vue:60-61/:115-120/:143-146/:172); a frozen tail = 52.6% of settle-in once highLevel>50. A truth defect on the product's own thesis (more harmonics ⇒ more fidelity), visible with no interaction. Registry-clean: the only banked touch of `computePreviewLevels` is fr-FourierMorphSvg L-13's instance count — **the one component-owned BLOCKER in this folder**. | **BLOCKER · F.W4** — derive the ladder from `shape.data.levels` (or filter candidates against it) and cap the High range at the decomposition's own maximum; the phantom-tile pixel identity → SS-13. |
| **FMD-2 · D-1 ⊕ C-10 ⊕ L-31** | The `/morph` route's primary and only control has an EMPTY accessible name — `<button class="morph-button cartoon-card">` whose sole child is a bare SVG (MorphShapePreview.vue:4-10; FourierMorphSvg.vue read whole by the readers, aria grep 0 re-run by both). Three axes re-book it under three ids. | **FOLD → banked fr-FourierMorphSvg FM-1 (BLOCKER, F.W4; leaf half → fr-DarkModeToggle N-4).** No re-book; the cure (leaf `title?` prop + consumer names the button) is FM-1's. |
| **FMD-3 · D-2 ⊕ C-1** | The entire per-slider colour system is dead CSS: 8 `--slider-scrub-*` declarations across MorphPhaseConfig.vue:205-211 + HarmonicLevelGrid.vue:208-214 (both seat-read), fed by `slider-color` props at FourierMorphDemo.vue:31/:41/:51 and `VIZ_COLORS` at HarmonicLevelGrid.vue:25/:48 — and `slider-scrub` has **0 hits** in the installed 4.0.0 dist (seat re-run) and 0 at producer HEAD. Five sliders, zero tint delivered, live today. | **FOLD → banked fr-BasisSelector B-2 (fleet row: 23 declarations / 7 files) · F.W1.** Cure as corrected at the bank AND re-corrected here: rename ONLY the range pair to `--slider-range-bg`; DELETE the thumb pair — this seat's producer-HEAD census confirms `--slider-thumb-bg`/`--slider-track-bg` do not exist at 8.0.0. **Bank correction minted**: B-2/R-7's leg (b) ("no `--slider-thumb-bg` at 7.0.0 at all") is false at the v7.0.0 tag and true only at 8.0.0 — B-2's corrected cure stands on leg (a) alone (reader-1's finding, ratified). |
| **FMD-4 · D-4** | The duration slider is starved to ≈0 px at 640-767 px: incompressible row furniture (~172 px of nowrap label + fixed input + gaps) exceeds the one-third-column card content (~132-152 px), and the producer ships no track floor (`.slider-track{width:100%;flex-grow:1}`, no min-inline-size). At ≥960 the track is ~90-95 px for 76 steps on the pull-the-track idiom. Ruled above against reader-2's omission. | **BLOCKER · F.W4** — restructure the row (label above, or the grid to 2/1 columns earlier, or adopt `LabeledSlider` whose producer layout owns this); exact rendered widths at 640/700/768/960 → SS-13. |

### MAJOR tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **FMD-5 · L-01 ⊕ L-14 ⊕ C-4** | keyframes' `stop()` RESOLVES the pending `play()` (d.ts:526, seat-read), so `onUnmounted(() => stopAnim())` (:215) and unguarded Reset both ADVANCE the three-await coroutine (:170-208) they mean to kill; no disposal flag exists (readers' grep, 0). Reset mid-morph: `setShape`'s write is clobbered by the resumed coroutine; the shape the user reset away from finishes rendering. | **MAJOR (ruled above).** Mechanism **FOLD → FR-AH-9** (epoch/cancellation token — names `/morph` as live site) **+ FR-AH-21**; the component-new arm (guard `handleReset`, `:disabled` on :75 — and fold L-27's three-postures observation into the same edit) books here · **F.W4**. |
| **FMD-6 · D-8 ⊕ L-02** | The `isAnimating` guard has a hole exactly the width of the dynamic `import()`: first `phase` write at :169 sits after `await getAnimationCtor()` at :149, and there is no loading state anywhere (no spinner, no `aria-busy`, no dimming — three templates, seat-read). Mechanism CORRECTED per the killed register (K-1): the failure is orphaned-handle overwrite, not stop-races. | **MAJOR · FOLD → fr-DarkModeToggle M-3·L-4 (the FR-AH-8/-9/-20/-21 epoch family's fourth arm) · F.W4** — the epoch token closes the hole; add a busy affordance for the cold-cache window. |
| **FMD-7 · L-04 ⊕ C-5** | Three copies of one config joined by a pre-flush watcher: reactive copy #1 (:107) → setup-time spread #2 (:108) → composable ref #3 (useFourierMorph.ts:77), bridged by `syncWith`'s default-`'pre'` watcher (useMorphConfig.ts:79-83, no `flush` — seat-read). `handleReset` mutates #1 synchronously then `setShape` reads #3 (still stale, :92); `updateConfig` (:106-108) is a bare `Object.assign` re-deriving nothing; `morph.config` has **0 readers** in the page (seat grep). Reset drives the preview to the value the user just discarded and the chip lies until the next interaction. | **MAJOR · F.W4** — one config home (banked FM-13 books the adjacent mid-flight-mutation half; the Reset stale-read is NEW here). Cure: pass the reactive config itself (or `flush: 'sync'` + derived-state reconciliation in `updateConfig`) — one truth, not three. |
| **FMD-8 · L-05 ⊕ C-8 (observations)** | `createTweenAnimation` (:122-143) uses a full CSS `Animation` + dummy `{v:"0px"}` frame as a bare stopwatch, recovering `t` via `a.options.duration` — while the PINNED keyframes 4.3.0 root-exports `NumericAnimation` (seat-verified: 1 hit in the root bundle, which imports value.js **0** times), synchronous, value.js-free, rAF-driven, with `timingFunction` + `respectReducedMotion`. C-8's phase-2 clamp asymmetry stands: :175/:204 clamp geometry, :189 feeds raw back-easing `t` (overshoots measured by this seat) into unclamping `lerpPoints`. | **MAJOR · rider → banked fr-DarkModeToggle M-8·C-4** (the NumericAnimation identification is the sharper third cure). Cure caveats carried: string easing names throw in `NumericAnimationOptions.timingFunction` — pass the repo's own synchronous `getEasingFn`; and the swap does NOT cure FMD-5 (K-2). C-8's own cure killed (K-11). **F.W4**. |
| **FMD-9 · L-06 ⊕ L-11 ⊕ C-11 ⊕ D-10 (engine arm)** | One transient chunk-load failure poisons `enginePromise` forever (:38-44 — memo assigned before settlement, no `.catch`, no reset; no try/catch at any consumer) and surfaces as an unhandled rejection — while `isMoon` flips BEFORE the await (:132 vs :134), so the chip and all 12 grid thumbnails snap to the destination at t=0 and are never rolled back; the grid pays a 12-cell rebuild inside the click handler. | **MAJOR · FOLD → FR-AH-8 (memo poisoning + flag-before-await) + FR-AH-20 (post-:169 throw bricks the guard; try/finally cure) · F.W4.** Rider adopted (reader-2): FR-AH-20's open "reachable throw" question has an answer here — `getEasingFn`'s soft-fall (easings.ts:65-67) throws inside the rAF callback only if `linear` itself drops, and per banked fr-EasingPicker the F.W1 value.js bump drops the whole `timingFunctions` table at once. |
| **FMD-10 · L-07 ⊕ C-7 ⊕ i-3** | `getPath` is an uncached plain function inside the 12-cell `v-for` (HarmonicLevelGrid.vue:129-132/:55/:68, seat-read), re-run on every `activeLevel` crossing; `interpolateAtHarmonicLevel` allocates fresh arrays every call. | **FOLD → banked fr-FourierMorphSvg FM-3 (MAJOR, F.W4)** — which owns the CORRECTED cadence (~168 rebuilds ≈ 9.5 MB/toggle + the unguarded slider write path :64-65); the axes' "≥12 / ≈8 MiB" is a floor, killed as a citable figure (K-12). |
| **FMD-11 · D-3 ⊕ the killed §6 slider ledger row** | The uplift break surface: 3 Button sites (`variant="default"/"outline"` + `size="default"`, FourierMorphDemo.vue:71/:75, HarmonicLevelGrid.vue:57-58) lose their prop axis at 8.0.0 (`ButtonEmphasis`/`ButtonSize`, no `variant` — seat-read at `dc4267fc`), AND 3 Slider sites (`variant="standard"`, MorphPhaseConfig.vue:23, HarmonicLevelGrid.vue:19/:42 → five rendered sliders) lose their variant value (`SliderVariant = "scrubber" | "spectrum"` — seat-read; second rename of the same axis, making the "glass-scrubber" fossil comments accidentally correct again). Neither greps as a removed subpath. | **MAJOR · F.W1** (ruled above; reader-1's BLOCKER dissent recorded). Slider leg **FOLDS → fr-ConvergenceTimeline K-8 / fr-BasisSelector M-7** (the 8.0.0 break-surface identity). F.W1 must budget from 8.0.0, never the 7.0.0 snapshot; the exact consumer-side failure mode (vue-tsc error vs DOM fallthrough) → SS-13/F.W1 rehearsal. |
| **FMD-12 · D-13** | The inverse of the census's break table: four glass surfaces hand-rolled whose seats ship at the CURRENT pin — 8 `.info-chip`s, 5 label+control rows, 5 `<input type=number>` + ~20 duplicated lines of spin-button CSS (MorphPhaseConfig.vue:167-186 ≈ HarmonicLevelGrid.vue:180-200, seat-read), a hand-rolled overflow strip — with `./labeled-field ./number-field ./fading-scroll ./badge ./metric-badge` all present in the installed 4.0.0 export map (reader-verified both ends). The component imports NONE of the census's break subpaths, so F.W1's grep will not find these. | **MAJOR · F.W3** (shadow retirement). Chip-arm cure caution **FOLDS → banked FM-14**: `./metric-badge` is GONE at 8.0.0 — target `./chip`. |
| **FMD-13 · D-14 ⊕ C-9** | Three glass `Button`s re-skinned to the metal by unlayered scoped CSS (FourierMorphDemo.vue:286-329, HarmonicLevelGrid.vue:228-259, seat-read); glass ships its component sheet layer-free while SFC scoped output is unlayered → the overrides win regardless of specificity, and the emitted `data-variant`/`data-size` lie. No scoped rule declares a focus treatment (survivor claim held). | **MAJOR · FOLD (cascade decision) → FR-AH-11 · F.W4** — delete the overrides or move deliberate divergence into producer variants (`feedback_glass_ui_first_class`). |
| **FMD-14 · D-11** | Eight controls named only by inert `<label>` elements (no `for`/`id`/wrapping — MorphPhaseConfig.vue:8/:35 ×3, HarmonicLevelGrid.vue:7/:30, seat-read); the `LabeledField` cure ships AT THE PIN (installed 4.0.0 exports `./labeled-field`; `LabeledSlider` takes a scalar `modelValue: number`, which also retires all three array adapters). | **MAJOR · F.W4** (naming is live-today a11y), adoption coordinated with **F.W1**: `LabeledSelect` is REMOVED at 8.0.0 — the Easing leg lands as `LabeledField` + `<Select>`. |
| **FMD-15 · D-12** | Three sliders carry one byte-identical accessible name (`aria-label="Duration (ms)"` hard-coded at MorphPhaseConfig.vue:27, mounted ×3 as Settle Out / Morph / Settle In) and `.config-card` is a bare `<div>` — no `role="group"`, no `aria-labelledby` to its own `<h3>`. | **MAJOR · F.W4** — interpolate the card title into the label; group the card. (Correctly narrows the corpus's own S-2.) |
| **FMD-16 · D-15** | `.grid-cell` keeps the recipe's fixed `h-(--control-h-md)` while stacking ~2× that in content (48/64 px SVG + label inside a `size="default"` Button; :228-240 declares no height — seat-read); the mismatch grows with `--ui-scale` on coarse pointers. | **MAJOR · F.W4**, conformance half proven; the emission half UNPROVEN — decidable browserlessly by the banked fr-EasingCurvePreview **K-GEOM** instrument (oxide over the build). |
| **FMD-17 · D-17 ⊕ C-2** | `cssVarToHex` has no `oklch()`/`light-dark()` arm → both harmonic sliders tint `#888888` (colors.ts four arms + two `#888888` returns; producer ships the token as oklch inside light-dark(); the style.css hsl re-declaration of `--viz-amber` is the in-tree control). | **FOLD → banked fr-BasisCanvas D-1 / fr-App B-1, with banked K-12 attached** (HEAD greys BOTH themes) · **F.W4**. Moot for these two call sites if FMD-3's dead channel is deleted first — sequence the cures. |
| **FMD-18 · D-18 ⊕ D-6 ⊕ D-19 (the grid-affordance suite)** | The strip clips its own affordances at the top edge (one-sided `padding-bottom` under `overflow-y: hidden`, :218-226 — seat-read — against two-sided outward ring/lift/focus decorations); `.is-bound` overpaints `.active` at equal specificity in the deterministic boot state (both (0,2,0), later wins, :251-259; boot = highLevel=50 = an is-bound tile); and the bound colour `#60a5fa` measures 2.45:1 (< 3:1, WCAG 1.4.11), while the same copy-pasted input focuses `#60a5fa` here and conformant `var(--accent-red)` in the sibling. | **MAJOR · F.W4** — add the top gutter, order/scope the state classes, tokenize the bound colour. Clipped-pixel count + overpaint perceptibility → SS-13. |
| **FMD-19 · D-19 ⊕ D-7 ⊕ D-20 ⊕ m-9 (the contrast suite)** | Every contrast figure reproduced to the hundredth by BOTH readers independently (chips 3.43/3.91 light, 5.71/4.94 dark; neutral 16.21/14.74; #60a5fa 2.45/2.40; borders 1.36/1.28; `--primary` vs `--muted` 15.81/6.93; etc.), with exactly one trivial drift (dark `.num-input` border 1.40, not 1.53 — verdict unaffected). | **MAJOR · F.W4** — the failing pairs (chip text on light, borders, #60a5fa) ride the token cleanup; the suite is this corpus's most reliable evidence (superlative S-9). |
| **FMD-20 · C-3** | The documented value.js lazy boundary is false — `vendor-math` rides the eager bundle on every route via the static `easings.ts` edge (main.ts → App → AppHeader → DarkModeToggle → useFourierMorph → `@/lib/easings` → `@mkbabb/value.js`), and keyframes' own deps carry value.js anyway. The prose at :33-36 is wrong about its own subject. | **FOLD → banked FR-AH-7 · F.W4** — fix the one static edge, keep the boundary, correct the comment. |
| **FMD-21 · C-§6** | Corpus contradiction, stands: `class-variance-authority` is NOT a removable dead devDep — 18 dist files import it, `slider/index.d.ts:1` is a type-level hard edge reachable from MorphPhaseConfig.vue:73, so removing it breaks `vue-tsc -b`. The census's measurement (0 src imports) is true; its disposition is false. | **MAJOR · census erratum consumed by F.W1** (the uplift budget must carry CVA until the type edge dies with the 8.0.0 slider). |

### MINOR tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **FMD-22 · D-9 ⊕ L-09** | Reduced-motion declined (`respectReducedMotion` omitted at :127-133 — a documented opt-in default-false; `prefersReducedMotion()` exists in-tree at router/index.ts:15-17) + the false `scheduler.ts:13-15` off-screen-gate comment. D-axis narrowing adopted: glass's CSS blanket gates transitions; only the JS geometry writes are ungated. | **MINOR-as-banked (ruled above) · FOLD → FR-AH-31 + FM-11 · F.W4.** Third MAJOR dissent recorded. |
| **FMD-23 · D-10 ⊕ L-10 (clipboard arm)** | `useClipboard({resetMs:2000})` declines `onCopyError`; `copy()`'s `Promise<CopyResult>` floated bare in the template (:71); the producer documents the error contract explicitly ("REPORTED, never silently swallowed"); `useToast` exists and `<Toaster/>` is mounted, unused here. Registry-clean (bank has only migration credits). | **MINOR · F.W4** — wire `onCopyError` → toast; one option + one line. |
| **FMD-24 · L-12 ⊕ C-13 ⊕ i-2 ⊕ L-13** | Two `as any` casts at the JSON seam (:99-100) suppress genuine tuple mismatches (`domain` AND `coefficient` ×101 — the C axis missed the second) and disable the only structural check; `FourierShape.data` retains the ~225 KB graph for exactly two `.levels` reads repo-wide (seat-adopted greps). | **MINOR-as-banked (ruled above) · FOLD → N-14 (cast FORCED; cure = narrow typed assertion) + N-15 + FR-AH-1 · F.W4.** C-13's cure killed (K-3). |
| **FMD-25 · L-15 ⊕ L-16 ⊕ m-15** | The clamp funnel's three edges: no-op clamps desync the input's DOM (`:value`+`@change`, no force-write); two of three emitters lost `Math.round` (typing 3.7 → a tile labelled `n=3.7` via computePreviewLevels); `Number("") || 1` silently rewrites an emptied field to 6 or 1. All three arithmetics re-derived from live source by reader-1; seat re-read the emitters. | **MINOR · F.W4** — round in both grid emitters, force-write on no-op clamp, handle empty-string explicitly. Unbanked; books here. |
| **FMD-26 · L-17 ⊕ L-18 ⊕ L-19 ⊕ L-30** | Dead surfaces, all seat-re-verified: `getIdlePoints` (2 hits), `updateField` (2 hits, carries the module's only `as any`), the `closed:false` branch + its `@mkbabb/pencil-boil` import (no caller passes arg 2 — a dead branch holding a live census uplift row), `xyToPoints` over-export. | **FOLD → FR-AH-37 (deletion batch — updateField already added there) + FR-AH-53 (pencil-boil, deletable outright) · F.W4.** |
| **FMD-27 · D-21** | `.morph-button`'s scoped `transition` shorthand (:97) omits `translate` → the cartoon lift snaps while border/shadow ease; the two sibling `.cartoon-card` hosts are the in-file control (no transition declared, lift intact). Seat re-read cards.css:33-49 — mechanism exact. | **FOLD → FR-AH-11 new-site rider at the banked INFO magnitude · F.W4.** The D axis's MAJOR is over-rated against the bank (both readers concordant; ratified). |
| **FMD-28 · D-22** | Seven `transition` declarations, zero motion tokens — every duration a literal (.1/.15/.2s), every easing `ease` or bare, three different hover durations on one page, while the producer's tokens are observably live in the same bundle. | **MINOR (demoted, ruled above) · F.W4** — tokenize alongside FMD-13's override cleanup. |
| **FMD-29 · m-7** | Two breakpoint systems 128 px apart: all 14 component queries at 640 px vs the app root-font step at 768 px — so the title renders 36 px at 640-767 and SHRINKS to 32 px at ≥768 (arithmetic from live literals, seat re-read :216/:224 + style.css:41/:47). | **MINOR · F.W4** — align on one seam (the ratio itself is sound — superlative S-7). |
| **FMD-30 · m-14 ⊕ m-18 ⊕ m-12 ⊕ i-4** | Layout micro-quartet: the chip column breathes as one (stretch + nowrap → all four chips resize with the widest as `phase` runs); dead `justify-content: center` on a fit-content flex item; `overflow-x: hidden` minting a nested scroll container on the route root; two no-op declarations. All mechanically sound (reader-1's derivations, spot-checked). | **MINOR · F.W4.** |
| **FMD-31 · m-1 ⊕ i-1 ⊕ L-26** | Heading skip h1→h3 (no h2 anywhere on the route — grep 0); `class="grid"` collides with the Tailwind utility and survives only because scoped CSS is unlayered (one layer directive from flipping); `/morph`'s entire automated gate is one screenshot row (no click, no slider, no axe). | **MINOR · F.W4** (heading + class rename); the test-posture arm folds → banked FM-10's e2e row and feeds SS-13's probe list. |
| **FMD-32 · C-12 ⊕ i-5 ⊕ L-24** | A fifth easing-curve-preview fork: MorphPhaseConfig inlines its own 40×20 sampler while `easings.ts` carries TWO samplers nine lines apart (one cached, one not) and producer `./easing` ships at 8.0.0. The axes' fork-count disagreement is definitional, not factual. | **FOLD → fr-EasingCurvePreview BARREL + FORK · F.W3.** Census carry-6 correction rides: the morph subtree's share was uncounted. The m-5 residue (`.easing-preview` lacks `overflow: visible`) lands as an INFO note inside this cure. |
| **FMD-33 · C-14 ⊕ C-16 ⊕ C-18 ⊕ C-19** | Seam-quality quartet: `phase: string` where `MorphPhase` exists (and the child keys CSS off the value); `deep: true` + getter-spread = untracked-by-depth + never-reference-equal source; the slider adapters' setter type diverges from the producer's `number[] | undefined` emit (persists at HEAD); the `onMounted` deferral buys nothing and costs one `d=""` render. | **MINOR · F.W4.** |
| **FMD-34 · L-20 ⊕ L-21 ⊕ L-22 ⊕ L-23 ⊕ L-27** | Residue batch: the current shape derived three ways in 80 lines (the `:124` hardcoded `sunShape` a latent bug); three hand-unrolled `MorphPhaseConfig` blocks + six manual write-backs (R5-7 inverted); the four info chips duplicated byte-identically with both copies always in the DOM (**FOLD → banked FM-16**); the easing catalog reachable via three import paths with a state composable acting as a lib barrel (**FOLD → fr-EasingCurvePreview BARREL**); three different busy postures (joins FMD-5's cure). | **MINOR · F.W4**, folds as marked. |
| **FMD-35 · D-16** | `.btn-icon` pins the lucide glyphs at 15 px — not a rem multiple, not 4-aligned, not a token — while the button obeys the coarse-pointer floor; the mismatch grows with `--ui-scale`. | **MINOR (demoted, ruled above) · F.W4** — delete the rule; the recipe's `size-(--ui-glyph)` arbitrary variant already owns glyph sizing. |

### New finds — reader-missed rows, adjudicated and booked

| id | finder | claim · seat verification | terminal disposition |
|---|---|---|---|
| **FMD-N1** | reader-1 | The 12-tile strip REFLOWS mid-drag: `computePreviewLevels` injects the live bounds into the ladder (:35-36, seat-read), so dragging Low across a non-candidate value (e.g. 5→4) inserts/removes a tile and shifts every tile rightward ~70 px, oscillating 12↔13 for the whole gesture — while the user is dragging the control that scrubs it; each oscillation is also a full grid rebuild (the trigger FM-3 prices but does not attribute). reka's SliderRoot writes continuously through the drag. Identity-guarded: seat grep confirms `computePreviewLevels` appears in the bank only as FM-13's L-13 instance count. | **MAJOR · F.W4** — freeze the ladder during drag (or render the bounds as markers, not tiles); perceived magnitude → SS-13. |
| **FMD-N2** | reader-1 | The active-tile ring can never reach full strength during the animation it exists to track: its 150 ms transition (:239, seat-read) is 6-15× the dwell between `activeLevel` crossings (six crossings quantised into the default 150 ms settle-out → dwells of 50/33/24/20/13/10 ms); the PRM blanket rewrite (0.1 s) is still 4-10× dwell. An independent fourth leg on the affordance FMD-1 makes unreachable, D-6 overpaints, and FMD-18 clips. | **MINOR · F.W4** — drop or shorten the transition on `.grid-cell.active`; smear perceptibility → SS-13. |
| **FMD-N3** | reader-1 | Hovering the primary control DELETES the design system's signature offset shadow: scoped `.morph-button:hover` (:108-112, seat-read) declares `box-shadow: 0 0 0 3px <halo>`, and box-shadow is a single property in unlayered scoped CSS — so `cartoon-surface`'s hover stamp (`--shadow-cartoon-lg`, a −6px 4px offset — cards.css:33-49 seat-read verbatim) is REPLACED by a flat centred halo while the card still lifts. The two sibling `.cartoon-card` hosts keep the md→lg growth (in-file control). A DIFFERENT property from the banked translate-timing leg; D-21's own narration ("the shadow eases… following") is the one thing the shadow does not do. | **MAJOR, riding FR-AH-11's single cascade decision at elevated weight — not a silent INFO fold** (reader-1's booking argument ratified) · **F.W4**. |
| **FMD-N4** | reader-2 | The public `harmonicLevel` readout escapes `[lowLevel, highLevel]` under the three `back` easings: `:174`/`:203` write the raw eased level, the clamps at `:175`/`:204` protect only the geometry (seat-read — anchors exact). Reader-2's sweep: chip reads n≈1.09 (below lowLevel AND below two shipped levels) under Back Out, n≈54.4 (above highLevel AND above n_harmonics) under Back In. DIRECTLY falsifies C-8's "phases 1 and 3 are safe" clause — all three phases leak, two through the chip. **Bank correction minted**: fr-DarkModeToggle superlative 3 ("cannot escape the level table") is true of the GEOMETRY only; the reported state escapes. | **MINOR · F.W4** — clamp at the write, not one line later (rides banked FM-12's cure idiom). Chip-flash perceptibility → SS-13. |
| **FMD-N5** | reader-2 | Every morph silently discards the harmonic level the user just selected: `morphTo` destructures only `config.value` (:151-160) and never reads `harmonicLevel`, so phase 1's first tick snaps the preview from the clicked level (e.g. n=3, a first-class interaction — twelve real Buttons) to `highLevel` in one frame before degradation begins (seat-read; :171-175 at t≈0 → level≈highLevel). On a page whose subtitle is "Tune the morph transition," the selected truncation is discarded at t=0, undocumented. Registry-clean (FM-13 books the adjacent config-mutation row, not the start state). | **MINOR · F.W4** — seed phase 1 from `harmonicLevel.value`, or document reset-to-high as deliberate. Two lines. |
| **FMD-N6** | reader-2 | The RESIDENT cost is unbooked: ~667.6 KiB of live `d`-attribute text mounted at all times on `/morph` (12 × ~57 K chars, reader-2's execution over the shipped JSON; ~114 KiB of it the three phantom tiles), independent of any animation — where banked N-12 and FM-3 both scope the churn to TRANSIENT windows. The tree's own prior art (`PathPreview.vue:39` `.toFixed(4)`) applied at 2 dp cuts a path 57,002→20,323 chars (−64%). | **MINOR · F.W4 — rider on FM-3 + N-12 under the FR-AH-24 cure-completeness precedent**: the memo (FM-3's cure) and precision rounding must land together — the memo alone leaves all 667 KiB resident. Heap witness → SS-13. |

## Killed-claims register

Refutations ruled by this seat; original ids for life. A killed claim is not re-mintable without new evidence.

1. **K-1 · L-02/D-8's cited mechanism** ("call B's `stopAnim()` resolves call A's phase-1 promise" on cold cache) — **KILLED, both readers concordant, seat-verified from source**: both calls park at `:149` before the first `currentAnim` assignment (`:171`; initialiser null at `:87`; `stopAnim` guarded at `:116`), so B's stop is a no-op. The real failure is WORSE: B overwrites the single handle, orphaning A's Animation beyond the reach of `stopAnim`/`onUnmounted`/Reset — two rAF loops to completion. The conclusion (two concurrent chains, guard reopens, `isMoon` nets to zero) survives on corrected causality. **Third minting of this exact kill** (banked fr-DarkModeToggle K-2, fr-FourierMorphSvg K-2) — the wrong causality must not travel into a repair ticket.
2. **K-2 · L-05's scope claim** ("delete the unnecessary await and one blocker plus two majors evaporate") — **KILLED**: `NumericAnimation.stop()` documents the IDENTICAL resolve-on-stop semantics (d.ts:2195, seat-read: "The play promise resolves immediately"), so L-01/L-14 survive the primitive swap untouched; they need FR-AH-9's epoch token. The challenge's own §2 falsifier cites the same line — a self-refuting cell inside one document.
3. **K-3 · C-13's prescribed cure** (`as unknown as` / widen `domain`) — **KILLED**: a double assertion performs NO structural check (strictly weaker than a direct `as T`), and the `coefficient: [number, number]` tuple (×101/file) blocks assignability independently of `domain`. Banked K-11 already ruled SOME assertion mandatory; the surviving defect is `any`'s over-breadth (N-14's cure text).
4. **K-4 · m-5's magnitude** ("the overshoot lobes — the entire semantic difference — clip") — **KILLED by this seat's own node run** (ruled-disagreements table): sampled extrema 0.611/19.546/0.519-19.478 all inside the box; ≤0.3 px of outer stroke edge clips; the lobes render in full. Reader-1's CONFIRMED is REJECTED — its own quoted arithmetic contradicts its verdict. Folds → banked fr-EasingCurvePreview **WRONGPAD** (correct magnitude) + K-8.
5. **K-5 · L-28** ("a coincident-point `Z` is a no-op closepath") — **KILLED** (reader-2, ratified): closepath converts the seam from two line-CAPS to a line-JOIN; with `stroke-linejoin="round"` at width 4.5 that is exactly the artifact S-2 credits the function for avoiding. Banked FM-K-8 already treats `" Z"` as load-bearing. L-28 undercuts its own corpus's S-2.
6. **K-6 · L-25's reachability** (0-duration → NaN path) — **KILLED** (reader-2, ratified): its own falsifier is met — every live write path clamps (≥50 with round; bounds arithmetic), the second consumer never calls `updateConfig`. Re-filed at INFO as a hardening ask (clamp inside `updateConfig`), not a defect.
7. **K-7 · The D axis's folder-wide "producer HEAD 7.0.0" pin, D-2's uplift arm, and C-1's thumb-half cure** — **KILLED, seat-verified**: producer is 8.0.0 (`dc4267fc`); the 8.0.0 slider token census contains NO `--slider-thumb-bg`/`--slider-track-bg` (seat run, six names exactly), so "7.0.0 still reads the pair" and the promised `--slider-thumb-hover-ring-color` improvement are both false at the tree anyone will uplift onto; the rename cure survives for the range pair only. **Third corpus carrying the stale pin** (banked SR-2, K-15) — and the D axis wrote the both-pins law it broke. D-3's substance survives 8.0.0 verbatim.
8. **K-8 · The D §6 ledger row "`./slider` survives; `SliderVariant = "standard"|"spectrum"` unchanged"** — **KILLED as stale-at-destination** (correct at the v7.0.0 tag it names — which also refutes banked M-7's "DOES NOT EXIST at 7.0.0" leg): 8.0.0 reads `"scrubber" | "spectrum"` (seat-read), putting this component's 3 sites on the F.W1 break surface. Folds → fr-ConvergenceTimeline K-8 / fr-BasisSelector M-7; booked as FMD-11's slider leg.
9. **K-9 · D S-10's superlative** ("a genuine two-voice typographic system — Serif for prose") — **KILLED, seat-verified**: `--font-serif` is glass's bridge alias to `--font-stack-text` = Plus Jakarta Sans, **a sans** (bridges.css:68 + scheme-motion.css:43, seat-read); fourier binds Computer Modern to `--font-sans` only (style.css:14) with zero application sites, while body applies `font-serif` (:20). The four morph `var(--font-serif)` declarations resolve to the sans register. Banked FR-AH-2/FR-AH-11 own the identity; D-4's "Computer Modern" attribution inherits the error (its arithmetic is robust to the swap; the row survives as FMD-4).
10. **K-10 · D-3's "the producer doc that would have caught it is silent" arm** — **KILLED, seat-verified**: `Button.vue:127-140` ships a DEV-only `console.error` triggered by exactly this component's call-site shapes, with the mapping printed (landed 2026-08-08, AFTER the challenge was written — the axis was right when it wrote and is stale now). The MIGRATION.md per-prop mapping gap survives inside FMD-11.
11. **K-11 · C-8's prescribed cure** (pass the easing to `addFrame`'s per-frame `timingFunction`) — **KILLED** (reader-1, ratified): the per-frame timing function shapes the interpolated VARS, not the `t` the callback consumes (`TransformFunction<V> = (v, t) => void`; the engine passes absolute elapsed time); consuming an engine-applied easing would require reading the dummy `ValueUnit` across the very boundary the design defers. The sound cure is L-05's `NumericAnimation` (FMD-8).
12. **K-12 · L-07's "≥12 re-renders / ≈8 MiB" figures and L §1's "the two worst faults are not in the .vue file" framing** — **KILLED as citable figures**: banked FM-3 owns the corrected cadence (~168 rebuilds ≈ 9.5 MB + the unguarded slider path), and the folder's only unbanked BLOCKER (FMD-1) lives precisely in the demo closure the framing deprioritises.
13. **K-13 · D's "the axe keystone spec navigates only /visualize (1 spec)" cell** — **KILLED**: two AxeBuilder specs exist; banked FM-1 carries the correct "2 of 8, both /visualize" census. The substantive point (zero axe coverage of `/morph`) survives inside FMD-2's fold.

## Superlatives — ratified, with provenance

1. **Frame-adjacency snapping, and the `low < high` invariant provably cannot collapse** (FourierMorphDemo.vue:148-169, seat re-read). Index-distance decision with numeric fallback; both readers independently case-analysed collapse-impossibility over the non-uniform ladder (and it survives even FMD-25's fractional-level breach — the argument never assumes integrality). Minted independently by D (S-5) and L (S-1); the strongest single piece of thinking in the subtree.
2. **The closed-curve seam is right twice over** (svg-fourier.ts:54-72): modular indexing on all four control points AND the load-bearing `" Z"` converting caps to a join — complementary, not redundant (the K-5 kill strengthens this row). Concordant with banked fr-DarkModeToggle superlative 2.
3. **One clamp funnel, two input paths** — every bound write routes through one validator; the slider adapters route their setters back through it; zero prop mutations (readers' falsifier greps, ratified). Narrowed by FMD-25, never contradicted.
4. **`lerpPoints`' `Math.min(a.length, b.length)`** structurally closes the index-out-of-bounds class at exactly the cross-shape call site where it would live — with the honest caveat that silent truncation is the same degrade-don't-throw posture N-15 prices as a defect one layer up.
5. **The dynamic-boundary instinct is right** and the memo is correctly constructed synchronously; the boundary is real (8 dynamic imports, seat-adopted). Defeated by one static sibling edge (FMD-20/FR-AH-7) — keep the boundary, fix the edge.
6. **`useClipboard` adopted with a dated receipt** (useMorphConfig.ts:55-58; `setTimeout` count 0) and still on the producer root barrel at HEAD — the migration ran the right direction and left evidence. Concordant with fr-EquationView S-10.
7. **One breakpoint, applied whole** — all 14 media queries step padding, gap, type and SVG size together at a coherent ratio; FMD-29 faults the CHOICE of seam against the app's 768 px root step, not the ratio.
8. **Every child is genuinely presentational** — `defineProps`+`defineEmits` only, one owner for config state, three identical contracts for three `MorphPhaseConfig` instances; and **every slider is accessibly named with naming that survives the uplift** (aria-label forwarded onto the thumb at the pin; hardened + DEV-warned at HEAD — reader-1's both-pins verification).
9. **Corpus superlative — the D axis's contrast work**: ~18 ratios reproduced to the hundredth by two independent re-derivations (one trivial drift). The most reliable evidence in the folder.
10. **Corpus superlative — stated falsifiers and self-kills**: D §5 records its own killed superlatives; D §00 corrects two pass-1 errors in the consumer's favour; L §6 reports its negative R5-7 result as the finding; C marks its own falsifier partially met and downgrades. Measurement fidelity is extraordinary across all three axes — every kill in this record is inference, provenance, or magnitude, not arithmetic. (The mirror image: reader-1 confirmed a row its own arithmetic refutes — m-5 — which is why the incredulity seat re-runs the numbers.)

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

Static work is exhausted on all of these; each names its probe.

1. **FMD-1** — rendered identity of the three phantom tiles (d-string identity proven by `===`; pixels not observed). Load `/morph`, compare the trailing three.
2. **FMD-4** — `getComputedStyle` width of `.duration-slider-track` at 640/700/768/960; whether ≤767 renders as row overflow or label clipping.
3. **FMD-6** — double-click window width in practice: Slow-3G, two clicks inside the chunk fetch, assert `isMoon` and the drawn path disagree (also witnesses K-1's orphaned-loop mechanism).
4. **FMD-10/N6** — frame-time/INP across one toggle + a heap snapshot on `/morph` (resident ~667.6 KiB witness); pair with the FR-AH-1 boot trace already queued.
5. **FMD-16** — whether fourier's build emits `h-(--control-h-md)`: browserless, the banked K-GEOM instrument (`npm run build` + oxide/grep over the chunks). No browser needed; listed here only because it is undecided.
6. **FMD-18** — clipped pixel count at the strip's top edge; is-bound-over-active perceptibility at 48/64 px.
7. **FMD-N1/N2** — drag Low across 4-8 at 1280 px and record the strip (reflow magnitude); sample `.grid-cell.active`'s computed border-color mid-phase (ring smear).
8. **FMD-N4** — whether the chip's n≈1/n≈54 excursion under back easings is noticed at 150 ms phase durations.
9. **FMD-33 (C-19 arm)** — whether the `d=""` first frame is perceptible (unlikely — onMounted runs pre-paint; the wasted render is certain).
10. **FMD-11** — the consumer-side failure mode at 8.0.0: install + `vue-tsc -b` (F.W1 rehearsal; at minimum 14 DEV console.errors on this route).
11. **Provenance rider** — every producer-8.0.0 fact in this record is a working-tree read at `dc4267fc`, not a registry read; all such cells re-baseline at F.W1 exactly as banked SR-2/FM-2 order.

## Verdict

**ADJUDICATED (M-25 agglomeration input — stamps nothing): 35 corpus rows confirmed (4 BLOCKER-tier: FMD-1 phantom-ladder NEW + FMD-4 starved-slider NEW + 2 folds; 17 MAJOR; 14 MINOR; 13 folds-by-reference with identities preserved) · 13 killed claims (incl. one reader confirmation rejected by this seat's own instruments and one reader omission ruled an error) · 6 reader-missed rows booked (3 MAJOR / 3 MINOR, seat-verified) · 2 bank corrections minted (B-2/R-7 leg (b); fr-DarkModeToggle superlative 3's geometry-only scope) · 10 superlatives ratified.** The page is a thin, largely well-mannered shell whose two worst truths are its own: a teaching surface that renders three identical glyphs as three further steps of Fourier convergence, and a primary tuning control that arithmetic starves to zero width across an entire viewport band — while everything animation-lifecycle-shaped folds into the already-banked FR-AH-8/-9/-20/-21 epoch family, and its glass seams (Button axis, Slider variant, dead slider tokens, CVA) queue on F.W1's break surface at the 8.0.0 truth, not the 7.0.0 snapshot.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
