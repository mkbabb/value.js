claude-fable-5 (served model id)

# fr-EditorControlsDock — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `fourier-analysis/web/src/components/visualization/EditorControlsDock.vue` (230 lines, read whole by this seat: script 1–53, template 55–173, style 175–230) + its consumer seam (`VisualizationView.vue`, dirty), the editor chain (`ContourEditorCanvas.vue`, `usePointDrag.ts`, `useContourHistory.ts` — all read by this seat), and the glass-ui dock/hover-popover/slider substrate.
**Substrate (re-pinned by this seat)**: fourier `m/w1-bump-migration @ cd26c65`, dirty WT (27 ` M` rows; the subject IS dirty — `git diff` re-run by this seat: exactly two lines, `:113` `:amount=`→`:value=`, `:117` `variant="glass-scrubber"`→`variant="standard"`) · installed glass-ui **4.0.0** (verified) · **producer glass-ui `package.json` reads `8.0.0` TODAY (seat-verified)** — the corpus's 7.0.0 pin AND the fr-CanvasControlsDock apotheosis's `7.0.0 @ 7d9abe08` pin (2026-08-08) are both stale; dated evidence stands, this is a new dated fact. value.js registry HEAD `c654824e`, branch `tranche-u`.
**Corpus**: three axis challenges (`audit/fourier-components/EditorControlsDock/challenge-{D-design 28+7, L-library 15+8, C-consumption 25+7+4§R}.md`, all read whole by this seat) × two hostile re-reader returns (**reader-DU** design-and-usage · **reader-LC** library-and-code), received in-brief.
**Method (M-23/L-18)**: every verdict-level disagreement between the readers was re-derived against the tree before ruling; every load-bearing kill was re-attacked with this seat's own instruments. **Two reader positions were overruled on seat + banked-registry evidence (K-1 against DU's L-1b confirmation, K-2 against DU's C-9 confirmation), one reader praise-cell was corrected by ruling (K-3 against LC's spectrum cell), and two reader missed-finds were killed by the identity guard as re-books of banked fr-ContourEditorCanvas rows (K-11, K-12)** — the exact fleet lesson that record's DISSENT §4 predicted. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; product source untouched)**: full reads of EditorControlsDock.vue (230), usePointDrag.ts (67), useContourHistory.ts (36); ranged reads ContourEditorCanvas.vue:18-23,:36-75,:110-203; VisualizationView.vue:70-129,:195-259,:404-424; FullscreenViewer.vue:110-136; CanvasControlsDock.vue:44-62,:95-112. Installed-dist byte extractions: `--slider-*` census (exactly ten names; `grep -rc slider-scrub dist` → 0), slider cva base (`"glass-slider focus-ring relative flex w-full …"` — `w-full` IS in the base), `HoverPopover` props (`keepDockOpen:{type:Boolean,default:!1}`, `native:{type:Boolean,default:!1}`), `Slider` props (`keepDockOpen:{…,default:!0}`), slider `index.d.ts:10-36` (standard = invisible thumb; **spectrum = "a transparent range"** — the K-3 decider), dock.js: the two layers' complementary `inert: !K(S) || void 0` / `inert: K(S) || void 0`, the summary layer as a bare `div` with `onClick`, `function N(){ s() || (w(), l.value === "collapsed" && (l.value = "hover", S())) }` (focusin→expand), the leave/focusout scheduler `M(e)` with its `relatedTarget`-containment guards falling through to `D()`, `grep -c keydown dock.js` → **0**; DockSeparator runtime (`div.dock-separator` + `role="separator"` + `data-orientation`); `dock/index.d.ts` exports incl. **DockLayerGroup + DockLayer**; layer-group.css:35-41 (`margin: 0 0.375rem`, `--surface-tint-15`) + `.dock-spacer{@apply flex-1}` + `.dock-layer-group` gap token; shell.css:24 + offsets-sizing.css:210/:239 (`--dock-h` = `calc(2.5rem + 0.75rem + 3px)`; referenced exactly twice in styles — the K-4 decider); touch-floor.css:57-66 (`.glass-dock.vertical`-scoped, verbatim closing comment — the K-6 decider); icon-button.css:80-96 (`--btn-hover-color`, press darken), :107-115 (`:is(.is-active,…,[aria-pressed="true"])` cohort), :129-136 (`> svg` glyph rule + the "size class still WINS" doctrine); tokens color-radius.css:40,:58,:85,:140,:144,:263,:273 + dark-arm.css:144,:256,:260. reka-ui: HoverCardContentImpl.js:139-140 (unconditional onMounted `tabindex="-1"` strip), HoverCardTrigger.js:46-49 (`excludeTouch` on both pointer handlers; bare `onFocus`/`onBlur`), utils.js `excludeTouch` (verbatim), TooltipTrigger.js:39-70 (`pointerType === "touch"` return; `handlePointerDown`/`handleFocus`). Consumer/API: contours.py:18-26 (bare handler, no `dependencies=`), api.ts:98-99/:138-139 (Idempotency-Key declared) vs :316-324 (never passed), colors.ts four-branch fallthrough → `#888888`; `showGhost` consumer census (6 files); ContourEditorCanvas props = `contour/imageSlug/showImageOverlay` — **no `showGhost`**; web/package.json scripts (no vitest; playwright only, 8 specs); `grep -rl "Magnet\|EditorControls\|Save contour\|dock-badge" e2e/*.spec.ts` → **0**. Producer: package.json 8.0.0; `dock/index.ts` exports DockControl/DockSeparator, no DockIconButton; `ls src/components` → `metric/`, `popover/`, no metric-badge/hover-popover.

## Scoreboard

**45 rows leave with a disposition: 38 ADJUDICATED-CONFIRMED booked here (2 BLOCKER · 12 MAJOR · 19 MINOR · 5 INFO) + 7 identity FOLDS (3 BLOCKER-weight · 3 MAJOR · 1 INFO) — full corpus coverage, every D/L/C row and every reader row dispositioned exactly once · 12 killed-register entries (2 reader confirmations overruled on seat evidence, 1 reader cell corrected by ruling, 2 reader missed-finds killed as registry re-books) · 10 missed rows banked (with 4 more absorbed as riders and 2 as folds).**

The BLOCKER surface: **C-3** (ten of twelve controls nameless — including both Saves — by a perfectly consistent tooltip-XOR-label rule, which on touch means NO label for anyone) and **B-2, the banked reader-DU find this corpus missed whole: the collapsed dock's Save button is the surface's only keyboard entry, and focusing it expands the dock, which `inert`s the summary layer out from under the focused element — keyboard entry DESTROYS focus and the dock re-collapses in 2000 ms.** Three further BLOCKER-weight rows fold to banked identities (keyboard/touch-dead popovers → fr-CanvasControlsDock D-2/C-2; the stale push-mirror → fr-ContourEditorCanvas D/B-6·L-3·C-4; the `#888888` resolver → fr-BasisCanvas D-1/BC-1/C-1).

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **L-1's save-lie leg (L-1b)** | CONFIRMED BLOCKER ("post-save drags leave the green Check standing over unsaved edits") | KILLED (pointer-down emits first) | **LC wins — K-1; DU OVERRULED on seat evidence** | Seat re-derivation, every hop: `usePointDrag.ts:15-23` `onPointPointerDown` writes `selectedIdx`/`dragStartIdx`/`dragPrevPt`/`dragging` and captures the pointer — **it mutates no coordinate**; the first mutation is `points.value[dragStartIdx] = pt` at `:31`, inside `onPointerMove`. `ContourEditorCanvas.vue:154-157` wraps it: `rawPointPointerDown(idx, e); emitState();` — the emit fires at pointer-DOWN, strictly before any drag pixel. `VisualizationView.vue:87-90` sets `editorSaved.value = false` on EVERY `stateChange`. So on the deterministic drag path the Check un-greens at pointer-down; no plain-path window exists in which green coexists with unsaved edits. LC's "no window" is itself one notch overclaimed: the residual lie window is the save-await race (a drag begun during the in-flight `await saveContourPoints` is re-greened when `:95` lands after the pointer-down emit cleared it) — which is **fr-ContourEditorCanvas D/M-10's banked race**, not a new row. Consequence for the registry: the banked fr-ContourEditorCanvas B-6 phrasing "the green check asserts saved over unsaved geometry" must be read as race-reachable-only (via its own M-10), not drag-reachable — a refinement, recorded in IDENTITY GUARDS. L-1's canUndo/canRedo leg survives whole, PLUS LC's extension (seat-verified from `useContourHistory.ts:10`): drag-end `pushHistory` truncates the redo tail with no emit, so after undo→drag the **Redo button stays enabled over a handler that returns at `:22`** — enabled-control-over-no-op, the L-5 shape in the opposite direction. |
| R-2 | **C-9** (magnet round-trip snaps to 0) | CONFIRMED MAJOR | KILLED (premise unreachable) → INFO | **LC wins — K-2; DU OVERRULED — and the kill was already banked** | Seat mount-predicate reads: `ContourEditorCanvas` mounts under `v-if="store.contour"` (VV:203); `EditorControlsDock` under `v-if="isEditing && store.contour"` (VV:238) — a strict subset; both unmount together. The dock cannot be interactive with `editorRef` null, so the `?? 0` getter fallback and silent setter guard are dead branches. This is byte-for-byte **fr-ContourEditorCanvas K-19** (both ITS readers asserted the same consequence; that seat killed it on the same mount-order proof) — the identity guard decides before the re-derivation does. The surviving architecture note (one datum crossing by `defineExpose` reach-in one way, typed emit the other) is **already banked** as fr-ContourEditorCanvas L-6·C-6 (MAJOR, → F.W3/W4). FOLD; nothing new to book. |
| R-3 | **C-15** (adopt the `spectrum` variant) | KILLED (cure structurally unsound) | carried as "verbatim what this component wants" | **DU wins — K-3; LC's cell CORRECTED** | Seat read of the shipped docstring, `dist/components/ui/slider/index.d.ts` §spectrum: "a consumer-supplied `--slider-track-bg` linear-gradient (the LCH/hue ramp), **a transparent range**, and a THIN VISIBLE squircle thumb". The magnet control's entire readout is the FILLED range of a 0–10 magnitude; spectrum deletes the fill and demands a gradient this control does not have. Adopting it is a regression under a green diff. The correct cure family is D-2/C-1's (`--slider-range-bg` on `standard`, or whole-unit deletion — see the roster). C-15 is retired, not scheduled. |
| R-4 | **C-3 severity** | BLOCKER | MAJOR | **BLOCKER — DU wins** | Precedent: fr-CanvasControlsDock D-1/L-3/C-1 (same tooltip-shim family, six unnamed controls) is banked BLOCKER. Here the census is worse (10 of 12; both Save buttons — the only persisting action — in the unnamed ten; seat-confirmed the 1:1 XOR mapping from the whole-file read: `aria-label` at :105/:121/:136 only, `<Tooltip` at exactly the other ten), and DU's banked touch rider makes it universal, not AT-only: reka's TooltipTrigger returns on `pointerType === "touch"` (seat-verified :60) and suppresses focus-open during pointer-down, so on the mobile-first surface ten controls have NO label for ANY user. Same family as the sibling BLOCKER, distinct surface — booked here (the R3-7a/X-11 identity keeps the shim-migration leg). |
| R-5 | **D-1/D-11 booking** (keyboard/touch-dead popovers) | BLOCKER, booked fresh | MAJOR, booked fresh | **FOLD — neither reader swept the registry** | fr-CanvasControlsDock D-2/C-2 (banked BLOCKER, → F.W1 Popover union) already names THIS file: "Repeats verbatim in EditorControlsDock (:103,:134) — four toggles, two docks, one root cause." One identity. This corpus adds three facts the banked row lacks, carried as riders: (a) `magnetRadius`'s sole writer is this dock (VV:82-85 fed by :246), so the magnet is keyboard-unreachable APPLICATION-WIDE, not just dock-locally; (b) DU's characterization correction — HoverPopover is NOT a bare reka root: it ships a `native` arm (`interestfor`/`popover="hint"`), `default: !1` (seat byte-verified), unpassed here, so the reka arm is live; (c) the interim cure is real at the installed pin (`open` is a `defineModel` — `v-model:open` + a trigger `@click` survives the F.W1 migration unchanged). |
| R-6 | **D-4 severity** (spacer inert under `fit-content`) | MAJOR | MINOR (mechanism corrected: grid max-content, not flex) | **MAJOR — DU's grade, LC's mechanism** | LC's sharper chain adopted (seat-corroborated: `.dock-layers` is grid, both layers in one cell, column sizes to the widest layer's max-content, so the row's `w-full` resolves to its own max-content and free space = 0). Severity follows the fr-CanvasControlsDock D-11/L-17 precedent (authored intent structurally cancelled = MAJOR): the ONE proportional gesture in a twelve-control strip — the status+commit cluster pushed right — never paints, and the layout reads as an undifferentiated strip. The FLIP-morph transient → SS-13. |
| R-7 | **The DU-MAJOR / LC-MINOR cluster** (D-7 · D-8 · D-9 · D-10 · D-13) | MAJOR ×5 | MINOR ×5 | **MAJOR for D-7/D-8/D-9/D-10 · MINOR for D-13** | Sibling-record consistency governs: fr-CanvasControlsDock banked the identical defect classes at MAJOR — D-3/L-8/C-8 (class-arm-only state, `aria-pressed` free: = D-8/C-12 here, and D-7's hue-only magnet is the same missing-channel family at Level A), D-6/L-4/C-6 (scoped `.dock-separator` shadowing the shipped primitive: = D-9/L-8 here, plus the role forfeit), D-5/L-7/C-7 (two icon-size idioms, dead `:size`: = D-10/L-4 here, worsened by the cross-dock scale split banked as a rider). D-13 (press-darken lost on `.is-save`) lands with the M-7/D-16 cosmetic-register precedent at MINOR; DU's MAJOR preserved in DISSENT. |
| R-8 | **L-2/C-2 booking** (VIZ_COLORS → `#888888`) | BLOCKER, booked fresh | MAJOR here / BLOCKER at repo scope, booked fresh | **FOLD — the identity guard again; BOTH readers re-derived a banked row without sweeping the registry** | This is **fr-BasisCanvas D-1/BC-1/C-1** (BLOCKER, adjudicated 2026-08-07, → F.W1), and the EditorControlsDock:123 magnet-track instance is ALREADY BANKED as a rider on that identity at fr-ContourEditorCanvas ("FOLD — MAJOR" row). Both readers' re-verifications are adopted as corroboration (LC's masking analysis is the sharpest: the grey feeds only dead names here, so fixing L-3 alone ships a grey slider — that same-commit coupling survives for the OTHER six `--slider-scrub` files). The genuinely new fact this corpus adds rides the MAJ-1 roster row: the whole consuming unit is deletable (K-8), which for THIS file dissolves the coupling instead of discharging it. |

**Cell corrections inside agreed verdicts**: D-5/D-6's falsifier premise — "the dock floats … with `autoLuminance` default-true" — is repeated by the D axis AND both readers' unproven cells, and it is DEAD: fr-CanvasControlsDock M-4/K-1 (banked) proved no Boolean prop on the dock surface carries a positive default, so the luminance sampler never engages for any consumer. The SS-13 composite probe survives (static glass plate over canvas), but its adaptive premise is a citation-inheritance event against a banked kill — recorded in DISSENT. D-2's provenance cell ("the sweep touched `:116` and `:113`") — LC's kill ratified by this seat's own `git diff`: the two lines are `:113` and `:117` (K-10). D-27's mechanism — DU's simpler own-element-beats-inherited derivation is the carried form.

## Adjudicated defect roster

Line anchors: subject working-tree (dirty; the two-line rename diff re-run by this seat). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5–W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring.

### BLOCKER (2 booked · 3 folds)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **C-3** (+ DU touch rider + DU XOR rider) | 10 of 12 DockIconButtons have no accessible name — the author's rule was tooltip-XOR-label, stated with perfect consistency: the two `aria-label`ed buttons (:105,:136) are exactly the two without a Tooltip; the ten tooltipped are exactly the ten without a label; both Save buttons (:63,:166) are in the unnamed ten; every glyph is `aria-hidden` (lucide stamps it absent a11y props/slot). The tooltip supplies `aria-describedby` only-while-open — a description, never a name. **Touch rider (banked, DU)**: reka's tooltip is touch-excluded (seat-verified `pointerType === "touch"` return + pointer-down focus suppression), so on the mobile-first surface (18px root <768px) the ten controls have no label AT ALL, for everyone. **XOR mirror rider (banked, DU)**: the two labelled popover triggers are the two with no hover text for sighted pointer users — and the magnet trigger's only remaining signal is D-7's hue. | **F.W4** — both channels on all twelve, today. The shim-migration leg keeps its **R3-7a/X-11** identity (this file = 10 of the 35 Tooltip callsites, the largest single consumer) → **F.W3**. Family of fr-CanvasControlsDock D-1/L-3/C-1 — distinct surface, booked here. |
| **B-2** (banked missed find, reader-DU; seat re-derived whole) | **Keyboard entry into the dock destroys focus.** The `#collapsed` slot hosts a real focusable `<DockIconButton>` (Save, :58-69) — the ONLY keyboard entry point, since the full layer is `inert` while collapsed (dock.js: `inert: !K(S) || void 0`) and the shell has no key handler (`grep keydown` → 0). Tabbing to it fires `focusin` → `N()` expands (`"collapsed" → "hover"`, seat-extracted verbatim) → the next patch stamps `inert` on `.dock-layer--summary` (seat-extracted: `inert: K(S) || void 0`) **while the Save inside it is focused** → HTML's inert unfocusing steps blur it, focus falls to the body → `focusout` with null `relatedTarget` passes `M(e)`'s containment guards → `D()` schedules the 2000 ms collapse. Net: expand → focus thrown to the top of the document → re-collapse before a realistic tab-back. Sibling proof it is a COMPONENT-triggered producer trap: CanvasControlsDock's `#collapsed` holds only two non-focusable glyphs (seat-read :98-101) and has no such trap. WCAG 2.1.1 + 2.4.3, Level A. **The cross-record sting (this seat's own): fr-CanvasControlsDock M-2's booked consumer cure for the keyboard-sealed dock — "focusable summary content — the sibling's collapsed Save button proves the slot accepts interactive content" — is DEFEATED by this row.** The two records' cures must be co-designed or both docks lose. | **GLASS-RELAY** — amends item (ii) of the fr-CanvasControlsDock consolidated letter: the collapsed-shell keyboard affordance must ALSO not inert the summary out from under a focused child (move focus into the expanded layer on expand, or suppress the inert flip while summary holds focus) + **F.W4** consumer arm (interim: `always-expanded` while editing, or accept the focus hand-off once the producer ships it). Focus-landing per engine + the 2000 ms tab-back race → SS-13 (corroborative; the destruction chain is static). Absorbs **M-9** (DU's INFO: the collapsed Save is simultaneously the file's best interaction decision — S-4's credit — and this BLOCKER's root; any fix must keep both). |
| **FOLD** — D-1 · D-11 · D-25 · L-15 | Keyboard-inoperable + touch-dead popovers (tabindex strip on mount · trigger closes on blur · `excludeTouch` on the only pointer path · no `aria-expanded`/`aria-haspopup`) = **fr-CanvasControlsDock D-2/C-2 (banked BLOCKER)**, which pre-names :103/:134. Riders added by this corpus (R-5): magnet keyboard-unreachable app-wide; the `native` arm correction; the `v-model:open` + `@click` interim cure at the installed pin. | **→ F.W1** with the banked row (Popover union, primitive-class-change marking); interim F.W4 option recorded. Touch legs both engines → SS-13 (K-9 discipline inherited from the sibling record: never carry "touch-dead" unqualified). |
| **FOLD** — L-1 (canUndo/canRedo leg + LC's redo-truncation extension) | The drag path never emits `stateChange`; the dock's three state props are a push-mirror sampled at pointer-down = **fr-ContourEditorCanvas D/B-6·L-3·C-4 (banked BLOCKER)**. This record adds: the redo-truncation arm (R-1) and the refinement that the banked "green check lies" leg is race-only (= that record's own D/M-10). The save-lie leg as charged here is killed (K-1). | **→ F.W3/W4** with the banked row (wrap `onPointerUp` as pointerdown is wrapped). |
| **FOLD** — L-2 · C-2 | `VIZ_COLORS.* → #888888` (four-branch `cssVarToHex` vs oklch/light-dark tokens) = **fr-BasisCanvas D-1/BC-1/C-1 (banked BLOCKER)**; the `:123` magnet-track rider is already banked at fr-ContourEditorCanvas. | **→ F.W1** with the banked identity. The same-commit C-1⊕C-2 coupling survives for the six OTHER `--slider-scrub` files; for this file the MAJ-1 deletion disposition dissolves it. |

### MAJOR (12 booked · 3 folds)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-2 / L-3 / C-1** (+ C-13 + C-22 riders; K-8 extension) | The magnet retint block is 100% dead: `--slider-scrub-*` is not a token namespace at 4.0.0 (seat census: exactly ten `--slider-*` names, zero `scrub`, no `-hover` variant of anything); two of the four are DOUBLY dead (standard's thumb paints `width:0; opacity:0` by contract — C-13); and per K-8 even `width:100%` is a no-op (`w-full` is in the slider's cva base, seat byte-verified). **The entire unit — the `:222-229` rule, the `magnet-slider-track` class (:122), the `:style` `--track-color` binding (:123), the `VIZ_COLORS` import edge (:8), and the two stale `glass-scrubber` comments (:48,:222 — C-22) — is one deletable block with zero visual delta today.** The 3.1→4.0 sweep renamed the variant prop and not its CSS-variable surface: a zero-diff-footprint dead-code class the lane census ("prose comments only", lane-frontend.md:382) cannot see. | **F.W4** — cure decision belongs to the forming spec: EITHER delete the unit whole, OR realize the authored intent in pure CSS (`--slider-range-bg: color-mix(in srgb, var(--viz-fourier) 30%, transparent)` — which sidesteps BOTH the resolver grey and the JS import, and must answer the R3-6 register question when chosen). The 23-occurrence / 7-file epidemic → F.W4 sweep row; the lane-frontend.md:382 census correction → NO-WAVE-OWNER bundle. |
| **D-4** | `.dock-spacer` is structurally inert under `fit-content` (LC's grid-max-content mechanism adopted; seat-verified the `.controls-overlay` flex-center host at VV:412-422): the sole authored proportional gesture — status+commit pushed right — never paints. | **F.W4** (with D-9's separator edit); the ~320 ms morph transient → SS-13. |
| **D-5** | `.dock-badge` ≈ 3.32:1 light (both readers independently reproduced 3.322; token values seat-verified) — WCAG 1.4.3 AA fail on the dock's only text; the shipped cure is pre-measured in-token (`--muted-foreground`, annotated 5.21:1 at color-radius.css:45, seat-read). | **F.W4** — one token substitution. Composite vs the painted plate → SS-13, WITHOUT the dead autoLuminance premise (cell correction above). |
| **D-6** | The saved-Check ≈ 2.21:1 light vs page and **1.97:1 vs its own `.is-save` plate** (DU's understatement catch adopted) — WCAG 1.4.11 fail on the surface's only success signal. In-repo cure precedent verbatim at style.css:113-127 (the D.W4.d `--viz-amber` darken). | **F.W4** local carry + **GLASS-RELAY** rider: a `--success` light-arm contrast carry belongs in the producer, same class as the amber one. |
| **D-7** | Magnet ON/OFF is hue-alone (`:106` — the only delta; static `aria-label`; no `.is-active`, no `aria-pressed`) — WCAG 1.4.1 Level A; internally inconsistent with the two `.is-active` toggles twenty lines below. | **F.W4** with D-8 (one state vocabulary). |
| **D-8 / C-12** | The overlay toggles carry visual state with no `aria-pressed`, while the library's active cohort already pays `[aria-pressed="true"]` (seat-verified :109) — the semantic is one attribute away, after which `.is-active` deletes. | **F.W4** now; at **F.W1** `DockControl`'s tri-state `active` (stamps `aria-pressed`) replaces both. |
| **D-9 / L-8** (+ D-26; K-4 kills the height row) | The scoped `.dock-separator` shadows `<DockSeparator>` — shipped in the barrel this file already imports at 4.0.0 — dropping the library's `0 0.375rem` margin, re-basing the tint off `--surface-tint-15` (and its dark-arm re-base, seat-verified dark-arm.css:260), and forfeiting `role="separator"` + `data-orientation` (seat-extracted from the compiled primitive). Drift realised: the CanvasControlsDock copy carries `margin: 0 0.125rem` (seat-read :106-112) — two docks, one stage, two hairline rhythms. The height/scale table is killed arithmetic (K-4). `.dock-spacer{flex:1}` (D-26) is byte-equivalent redundancy — delete, don't fix. | **F.W4** — adopt the primitive at the INSTALLED pin (not an F.W1 row; the class is live paint at both pins, per the sibling record's K-8 precedent). |
| **D-10 / L-4** (+ DU's cross-dock scale-split rider, banked) | 14 of 15 `:size` bindings are inert (presentation attributes lose to the `@layer components` `> svg` rule; the library documents "an explicit lucide size **class** still WINS" — seat-read :129-136); only :60's Wand2, outside any DockIconButton, paints. **Rider**: the sibling dock uses the class idiom that works (`h-4.5 w-4.5`, seat-read), so the two concurrently-mounted docks paint a hard-18px top rail against a 20px/≈26px-coarse bottom rail — the author demonstrably knew the correct idiom in the file next door. | **F.W4** — delete the `:size`s; the producer default is the intended value. The coarse ≈26px decimal is direction-certain, factor-unverified (LC could not locate the 0.78 `--dock-local-scale` literal; seat did not pursue) → SS-13. |
| **D-12 / C-4 / C-5** | Async network commit with no pending state, no double-submit guard, and a reachable silent unhandled rejection: `:63`/`:166` carry no `:disabled` while three siblings do; `onEditorSave` has no catch over a store that rethrows; the abort registry (`abortable("saveContour")`, seat-read) turns a double-click into an AbortError whose message is suppressed but whose rethrow still escapes — no user signal at all. Two-state `isSaved` for a four-state reality. | **F.W4** — `:disabled`/pending + a result channel (store already exposes `computing`/`error`). Contrast recorded: NOT the sibling's publish-BLOCKER — the write is content-addressed and abort-keyed, so the data effect is bounded. **F.W5–W8 rider**: C-7's idempotency channel at the same seam. |
| **C-6 / C-7** | The dock's only API seam is an unauthenticated write with a client that always authenticates (bare `save_contour(req)` — no Depends/Header/Request, router without `dependencies=`, seat-read) and that declines the `Idempotency-Key` channel its own client core declares (:98-99/:138-139 declared, :316-324 unpassed — seat-read) on the one non-idempotent-shaped call it can double-fire. The asymmetry, not an exploit, is the defect (content-addressed store honestly noted). | **F.W5–W8** — instantiates intake **R3-7b** (security 0-of-45) at this operation; identity stays with the intake; the seam is R6-8-clean (this component holds no client↔operation join — see superlatives). |
| **L-7** (+ M-10 pin correction) | Three of four glass import edges are definition-absent in the producer's shipping majors (seat-verified at 8.0.0: dock/index.ts has DockControl/DockSeparator and no DockIconButton; `src/components` has `metric/`+`popover/`, no metric-badge/hover-popover); the migration budget is CALLSITES, not imports: **12 `<DockIconButton` + 2 `<HoverPopover` + 1 `<MetricBadge` in 118 template lines** — the densest single-file dock-surface cost. **The uplift target is a 4→8 hop, not 4→7**: every producer `file:line` in this corpus and the CENSUS/lane rows citing 7.0.0 must be re-keyed before scheduling. | **F.W1** — with the fr-ContourEditorCanvas MM-11 sequencing rider (cures that render this dock elsewhere enlarge the transaction) and the fr-CanvasControlsDock D-12 sibling row. Pin correction also → **F.W0** substrate note. |
| **M-2** (banked missed find, reader-LC; seat re-derived) | **The "Contour trace" toggle is invisible-in-effect for the dock's entire lifetime**: `ContourEditorCanvas` declares NO `showGhost` prop and renders no ghost path (seat-read props :18-22); the flag's only consumers are the inline BasisCanvas (opacity-0 via `is-hidden` exactly while this dock exists), FullscreenViewer's BasisCanvas arm (`v-else` of `isEditing` — unmounted while editing, seat-read :115-127), and the SIBLING dock's 6px view-dot pip (the sole visible delta, seat-read :48). Its neighbour in the same popover, four lines apart with identical markup, is fully live (`showImageOverlay` IS a consumed editor prop). LC's "changes only a fully transparent surface" corrected to: the effect is a pip on the other dock plus a deferred state change that materialises only on edit-exit. | **F.W4** — either wire the ghost into the editor canvas (the honest reading of "Contour trace" in an editing context) or remove the toggle from this dock. Live confirm (nothing visibly changes but the pip) → SS-13. |
| **FOLD** — L-6 | The 60 fps epicycle pipeline behind the `opacity:0` canvas for this dock's whole lifetime = a third arm of **fr-BasisCanvas BC-10/C-9 (banked MAJOR: the IO sensor cannot report non-geometric hiding; scroll-away + fullscreen arms)**. This corpus adds the edit-mode arm: the dock's mount predicate (VV:238) is the exact complement of the canvas-hide predicate (VV:198), so the dock's presence is a proof-carrying signal the clock should park — and LC's strengthening (seat-verified VV:235): `AnimationControls` unmounts on `isEditing`, so the only pause control vanishes exactly when the hidden burn begins. | **→ F.W3/W4** with the banked identity; magnitude rides its existing SS-13 slot. |
| **FOLD** — C-10 · C-11 (+ DU's banked glyph/order + missing-pip missed rows) | The two concurrently-mounted docks diverge on the same contract = **fr-CanvasControlsDock C-9 (banked MAJOR: seven-axis divergence — emit name, prop optionality, item order, feedback mechanism, …)** + **M-8** (cross-dock icon vocabulary) + the **D-17/L-5/L-11/C-21 view-dot cluster** there. This corpus adds the editor-side anchors: `toggleOverlay` vs `toggleImageOverlay` (byte-identical parents, seat-read VV:222/:245), `showGhost?` optional-here/required-there, Eye/EyeOff-then-Image vs ImageIcon-then-Spline (glyphs AND order swapped on one screen, seat-read), and the trigger that drops the sibling's state pip — **entering edit mode HIDES the overlay state the user could previously see**, while the dock that owns the editing session conceals it. | **→ F.W4** with the banked C-9/M-8 identities (one contract, one glyph-per-meaning, one trigger anatomy); the pip precedent is 90 lines away. |
| **FOLD** — L-5 | `canDelete` true at the 3-point floor over a handler guarded by a different predicate = **fr-ContourEditorCanvas L-17·C-14 (banked MINOR)**. This corpus adds the reachability proof (simplify floors at 4 — `Math.max(4, …)` — one delete lands 3; the enabled Trash then no-ops silently with a point selected), which sharpens but does not re-book. Both readers' MAJOR preserved in DISSENT against the banked grade. | **→ F.W3/W4** with the banked row. |

### MINOR (19)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-3 + D-13 + D-14** | The unlayered scoped register cluster: brand-hue interactive retints against the producer's R3-6 doctrine (now SINGLY-stated — K-6 killed the touch-floor witness, which is `.glass-dock.vertical`-scoped, seat-read verbatim); the unlayered `.is-save` background kills the layered `&:active` press-darken (seat-read icon-button.css:93-96 — scale survives, darken dies) on the commit control; `:204` is character-identical to `--surface-tint-6` and forfeits its dark-arm re-base (seat-verified :140/:256). | **F.W4** — one edit: token rungs (`--surface-tint-6`, `--dock-control-hover-bg`), `:not(:active)` scoping, doctrine question answered in the MAJ-1 cure. DU's MAJOR on D-13 → DISSENT. Press-darken computed read → SS-13. |
| **D-15 / D-16 / C-18** | Two numeral faces for two small counts (badge inherits Plus-Jakarta serif-slot 16-18px; MetricBadge `sm` = Fira Code 11px); the label row hand-rolls MetricBadge's shipped `label`/`labelPosition="inline"` contract; `color`/`unit` unconsumed while 8 lines of bespoke tinting sit below. | **F.W4** — use the producer's own affordance. |
| **D-17** | MetricBadge hardcodes `cursor-pointer` + hover/press scale on a non-focusable readout div — a false affordance 4px from a real control (dead `focus-visible:` utilities noted by DU). | **F.W4** consumer cure (scale knobs to 1, `cursor-default`) + **GLASS-RELAY** note: the hardcoded affordance class is a producer wart. |
| **D-18** | `gap-2` fixed 0.5rem vs the family's `--dock-layer-gap` (0.375rem × `--dock-scale`) — 33% wide at 1×, frozen on touch. | **F.W4** — discharged for free by INFO-3's `DockLayerGroup` adoption. |
| **D-19 / L-11 / C-16** | `@mousedown.stop @pointerdown.stop` — inert against capture-phase dock listeners, doubled (one gesture, two compat events), and superseded twice (Slider `keepDockOpen` default TRUE, seat byte-verified; portal stamps handle click-away). | **F.W4** — delete both. |
| **C-8 / L-10 / D-20 / C-25** | The magnet-domain contract row: `[0,10]` stated three times across two files plus reka's own clamp, no shared constant, no test to catch a half-landed retune; the model clamps on write only (raw prop passthrough on read); `arr[0] ?? 0` maps a malformed payload to the destructive default (magnet OFF) instead of the prior value. | **F.W4** — one exported constant + symmetric guard. (The domain facts corroborate fr-ContourEditorCanvas L-4·C-10's magnet rows; the shared-constant defect is component-side and new.) |
| **D-21** | "1 pts" — no pluralisation, unexpanded abbreviation, and it is the accessible reading too. Two occurrences, one string. | **F.W4** microcopy. |
| **D-22** | Tooltips say "Undo"/"Redo" while the editor binds ⌘Z/⌘⇧Z/⌘Y on window; both richer-content affordances (shim `#content` slot, popover kbd-hints) exist unused. | **F.W4**. |
| **D-23 / C-19** | Tooltips die on exactly the disabled buttons (native `disabled` via UNTYPED `$attrs` fallthrough — invisible to vue-tsc, breaks under `as="a"`/`as-child`; no pointer/focus events on native-disabled controls). | **F.W4** now; **F.W1** `DockControl` ships `aria-disabled`-not-native (producer cure verified in the corpus; line numbers now 8.0.0-stale). |
| **D-24** | Twelve controls, no group name, no role; `aria-label`/`role="toolbar"` fall through `$attrs` to the state element (DU verified the render merges `$attrs` despite `inheritAttrs:false`). | **F.W4** — one attribute (+ note it lands beside B-2's cure, which changes what the group contains at rest). |
| **D-27** | `text-viz-fourier` sets `color` on the `<svg>` itself, so magnet-ON is the one glyph that never brightens on hover (DU's own-element mechanism adopted). | **F.W4** — dies with D-7/D-8's state-vocabulary cure. |
| **C-14 / L-9** (+ LC's side/align rider) | No-op default restatements as future pins: `variant="standard"`, `:collapse-delay="2000"`, `:start-collapsed="true"` (both compiled defaults seat-corroborated via the sibling record), and `side="top" align="center"` on both popovers (LC's banked extension — compiled defaults verbatim). K-7 kills the "shared idiom with CanvasControlsDock" parenthetical (it passes no `collapse-delay`). | **F.W4** — delete the restatements; `keep-dock-open` and `fit-content` are the props carrying intent. |
| **L-12 / C-24** | The Save control + badge duplicated across collapsed/expanded slots, drifted (`@click.stop` vs `@click`, 18 vs 20): maintenance-only — the sibling record's superlative holds here too (the `inert` layer flip means never two live copies in the a11y tree), and the `.stop` asymmetry is CORRECT (DU proved it against the compiled bubble-phase expand listener). | **F.W4** — dedup to one sub-template; keep the `.stop`. |
| **C-20** | `valueCommit` unconsumed while two sibling sliders consume it; every drag frame round-trips the four-hop chain. | **F.W4** — cheap; adopt the in-repo idiom. |
| **M-3** (banked missed find, reader-LC; seat corroborated) | `onEditorStateChange` conflates "state changed" with "contour dirty": pure SELECTION events (pointer-down select, background-click deselect — both emit, neither mutates geometry; seat-read :154-162) flip the green Check to unsaved over byte-identical persisted work. The inverse of the killed L-1b, and the true defect on that control. The zero-cost discriminant exists: `historyIndex` vs a saved-at index. | **F.W4** — rides the L-1 fold's cure (the emit payload carries no dirtiness signal; the parent invents it from the channel's firing). |
| **M-4** (banked missed find, reader-LC; seat verified) | Zero automated coverage of this dock or its editor chain: no vitest anywhere in the web workspace (scripts seat-read), 8 playwright specs with zero editor/magnet/save assertions (seat grep → 0 files). Both the L-1 fold and M-2 would be caught by one spec. | **F.W4** — the repair wave's born-RED harness; family of fr-CanvasControlsDock C-26 / fr-ContourEditorCanvas L-21 (distinct surface, same law). |
| **M-5** (banked missed find, merged DU+LC) | In-file icon semantics collide twice: `Wand2` is the surface's collapsed identity mark (:60 — the ONE live `:size`) AND the Smooth action (:88), one gesture apart; `Eye` is the static overlay trigger (:137) AND the ghost-ON state glyph (:144), so the trigger paints identically in all four overlay states. (`import { Image }` shadowing the global — C-17 — rides here; cross-dock vocabulary is the C-9/M-8 fold.) | **F.W4** — one glyph per meaning. |
| **M-6** (banked missed find, reader-DU; seat corroborated) | Duplicate accessible name in one subtree: trigger (:105) and Slider (:121) are both "Magnet radius" — indistinguishable disclosure vs value in an AT control list; the visible "Magnet" text (:112) could label the slider via `aria-labelledby` (or D-16's cure lands it). | **F.W4**. |
| **M-7** (banked missed find, merged DU+LC; seat corroborated from the whole-file read) | `.is-save.saved`'s unlayered `color` beats the layered hover `color: var(--btn-hover-color)` unconditionally — in the saved state the plate retints toward `--viz-fourier` on hover while the glyph stays `--success` green: a half-hover no one authored, on the control users hover to confirm. One control, two hover registers, keyed by a boolean the CSS ignores. | **F.W4** — dies with the D-3 cluster edit. Quick visual → SS-13. |

### INFO (5 booked · 1 fold)

| id | claim | disposition |
|---|---|---|
| **C-21** | Three accent classes, three naming conventions (`.is-amber`≈token · `.is-sky`→basis token · `.is-rose`→pink token) in three adjacent lines. | **F.W4** — rename in the D-3 cluster edit. |
| **C-23** | `@reference "tailwindcss"` instead of the project entry — the app `@theme` and glass's `@theme inline` bridge are invisible to any future `@apply` here; no live consequence today. | **F.W4** note. |
| **INFO-3** (banked missed find, reader-DU; seat verified exports + gap token) | `DockLayerGroup`/`DockLayer` ship at 4.0.0 unconsumed (`dock/index.d.ts:2-3`, seat-read) with `.dock-layer-group`'s own `gap: var(--dock-layer-group-gap, var(--dock-layer-gap, …))` — the grouping primitive whose adoption discharges D-18 and D-26 in the same edit that D-9 adopts `DockSeparator`. | **F.W4** — the separator cure should take the whole family, not the hairline alone. |
| **M-10** (banked missed find, both readers; **seat-verified**) | Producer glass-ui is **8.0.0**; the corpus, the CENSUS, lane-frontend.md's break-surface rows, AND the fr-CanvasControlsDock apotheosis's producer pin are stale. All producer `file:line` cites in this corpus verified in SUBSTANCE (DockControl tri-state, Popover coarse degradation, definition-absences) but every anchor needs re-resolution at 8.0.0 before any F.W1 scheduling; the atomic tri-package transaction re-costs against the 4→8 hop. | **F.W0/F.W1** substrate correction — one row, cited by MAJ-11. |
| **C-22** | Two stale `glass-scrubber` comments — absorbed into MAJ-1's cure (they name the dead block; they die with it). | rides **MAJ-1**. |
| **FOLD** — L-14 | `pointCount` mirrors a native `<circle v-for>` loop by hand at nine emit sites = the **intake R5-7** identity (already extended at fr-ContourEditorCanvas L-§4). LC's discipline note adopted: the COUNT is currently total-correct (all five length-changing sites emit — seat-corroborated); only the history predicates desync (the L-1 fold). | **→ NO-WAVE-OWNER** with the intake identity. |

**Full-coverage note**: D-26 rides D-9; D-28 → K-5; C-9 → K-2 + the fr-ContourEditorCanvas L-6·C-6 fold; C-13/C-22 ride MAJ-1; C-15 → K-3; C-17 rides M-5 + the C-9 fold; C-25 rides the magnet-domain row; L-13/C-10/C-11 ride the C-9 fold; L-15/D-25 ride the D-1 fold; the D axis's §5 cleared premises and the C axis's §R refutations are adopted as recorded (R-4's scope-boundary mechanism — custom properties inherit through Vue scoping — is load-bearing for MAJ-1's cure shape). Every corpus row leaves with exactly one disposition, original ids preserved.

## Killed-claims register (12)

| # | killed claim | refutation (adjudicated) |
|---|---|---|
| **K-1** | **L-1b (the save-lie leg)** — "post-save drags leave the green Check standing over unsaved edits"; and **reader-DU's confirmation of it inside a BLOCKER verdict** | Seat re-derivation (R-1): `rawPointPointerDown` mutates no coordinate; the wrapper emits at pointer-DOWN; `onEditorStateChange` clears `editorSaved` unconditionally — the Check un-greens strictly before the first drag pixel. The only lie window is the save-await race = fr-ContourEditorCanvas D/M-10, already banked. LC's kill RATIFIED with that one-notch qualification ("no window" → "no drag-path window"); DU OVERRULED. The canUndo/canRedo leg and the redo-truncation extension survive into the L-1 fold. |
| **K-2** | **C-9** — "the `update:magnetRadius` round-trip can silently no-op and snap the slider to 0 mid-drag"; and **reader-DU's MAJOR confirmation** | Mount predicates seat-read: the dock's `v-if` is a strict subset of the editor's; `editorRef` is populated whenever the dock is interactive; both unmount together. Identical to the ALREADY-BANKED fr-ContourEditorCanvas K-19 (which killed the same consequence against both of ITS readers). The expose-channel architecture note is banked there as L-6·C-6. DU OVERRULED; nothing new to book. |
| **K-3** | **C-15** — "the producer ships a variant purpose-built for exactly this need — `spectrum` — and it is unconsumed"; and **reader-LC's cell carrying it as "verbatim what this component wants"** | The shipped docstring (seat-read): spectrum has **"a transparent range"** over a consumer-supplied gradient track — it deletes the filled-range readout that IS this control's value display and demands a gradient the control doesn't have. DU's kill ratified; the cure-adjudicated-separately law (ColorSpaceSelector addendum §2) applies: retire, don't schedule. |
| **K-4** | **D-9's height/scale table** — "library 1.25rem vs component 1.5rem = +20%, desyncs at any `--ui-scale` ≠ 1" | Seat token walk: `--dock-separator-height = calc(var(--dock-h, --size-icon-btn) × 0.5)`; `--dock-h = calc(2.5rem + 0.75rem + 3px)` ⇒ ≈1.72rem ≈ 27.5px — the local 1.5rem is ~13% SHORTER, not 20% taller; and `--dock-h` is referenced exactly twice in the whole styles tree (definition + shell.css:24) — it does not ride `--dock-scale`, so the claimed desync mechanism does not exist for this token. The row's margin/tint/role/drift arms survive (roster MAJ-7); the "42% narrower break" derived figure is unreliable. Both readers; seat numbers. |
| **K-5** | **D-28** — "at `pointCount === 0`, Smooth/Simplify/Reset stay enabled and no-op — the state the state-machine forgot" | The state is unreachable: delete floors at `length <= 3` (seat-read :180), simplify at `Math.max(4, …)`/`<=6` early-return; the dock mounts only after `initFromContour` emitted a populated state; and Reset at 0 would NOT no-op (it re-reads the contour). The reachable version is the L-5 fold (3-point floor), which D-28 does not name. Both readers; ratified. |
| **K-6** | **D-3's "twice-stated" citation leg** — the touch-floor.css second witness | Seat-read verbatim: the block is scoped `.glass-dock.vertical` and its own comment closes "a horizontal dock or the layer-switcher rail are untouched." This dock is horizontal. D-3 stands on the offsets-sizing R3-6 witness alone — singly-stated, weight reduced accordingly (roster MIN-1). DU's kill ratified. |
| **K-7** | **L-9's parenthetical** — "Compare CanvasControlsDock.vue:41, which uses the same pair — a shared idiom worth retiring at both sites" | CanvasControlsDock passes `fit-content :start-collapsed="true"` and NO `collapse-delay` (DU's read; corroborated by fr-CanvasControlsDock's whole-file seat read and its K-3, which turned on exactly this fact). Single-site fix, not a cross-file idiom. DU's kill ratified. |
| **K-8** | **L-3's carve-out** — "only `width: 100%` survives" | The slider's cva base is `"… flex w-full touch-none …"` (seat byte-verified) and the consumer class merges alongside it — `width:100%` is a no-op restatement. All five declarations are dead ⇒ the rule + class + `:style` + import edge are ONE deletable unit (the disposition that reshapes MAJ-1 and dissolves the C-1⊕C-2 coupling for this file). Both readers' extension; ratified. |
| **K-9** | **The corpus-wide producer pin (7.0.0)** and every producer `file:line` keyed to it | Producer `package.json` reads **8.0.0** (seat-verified today). All substance cures re-verified by the readers at the moved HEAD (this seat corroborated the export/dir shape); all anchors stale; F.W1 is a 4→8 hop. No finding dies; every producer citation must be re-resolved (the D-19 lesson, again). |
| **K-10** | **D-2's provenance cell** — "the sweep touched `:116` and `:113`" | Seat's own `git diff`: the two lines are `:113` and `:117`. Trivial in consequence; non-trivial in a corpus whose warrant is `file:line` receipts. LC's kill ratified. |
| **K-11** | **Reader-LC's missed find** — "the undo history is unbounded — no depth cap, O(edits × points) retained" | **Identity-guard kill: this is fr-ContourEditorCanvas L-5, banked MAJOR (→ F.W3/W4) on 2026-08-08** — same composable, same 36-line read, same no-cap/slice+push facts, including the census "ring" correction. A correct derivation of an already-adjudicated row is a re-book, not a find. Folded; reader lapse recorded in DISSENT (that record's own DISSENT §4 names this exact failure). |
| **K-12** | **Reader-LC's missed find** — "`dragging` destructured unused; `historyIndex` returned with zero consumers" | **Identity-guard kill: fr-ContourEditorCanvas L-11·D/m-2·C-19·C-20, banked MINOR** — same greps, same lines. Folded; same lapse class as K-11. |

## Superlatives — with provenance (L-18 runs both ways)

1. **`keep-dock-open` on both popovers is load-bearing, and the file reads two OPPOSITE library defaults correctly** — HoverPopover `default: !1`, Slider `default: !0` (seat byte-verified both); passed exactly where required (:103,:134), omitted exactly where supplied (:115-126). Without it the 2000 ms collapse eats the panel mid-scrub. All three axes + both readers + this seat.
2. **The collapsed Save (S-4) is a genuinely considered affordance — and the root of B-2.** The one action that persists work survives the auto-collapse; the `@click.stop`/`@click` asymmetry is CORRECT against the compiled bubble-phase expand listener (DU proved it; adopted). The design decision and the BLOCKER are one object; the roster binds them (M-9 rides B-2) so no naive fix loses either.
3. **Token-seam theming with zero escape hatches** — `grep -c '!important\|:deep(' → 0` (seat-run in the whole-file read); every retint rides a declared producer seam (`--btn-hover-color`, the `--slider-*` namespace, an inline custom property). The failure is VOCABULARY (four dead names), not method — C's §R R-4 (properties inherit through Vue scoping) makes the cure a rename or a delete, never an architecture change. Both readers.
4. **The producer routes the Slider's `aria-label` to the actual `role="slider"` widget** (LC's recorded self-refutation: the compiled render forwards `$attrs["aria-label"]` onto `SliderThumb`) — in a file where ten controls are nameless, the one label with a non-obvious correct path got it right, by the producer's hand.
5. **`text-viz-fourier` (:106) is the control group for the whole L-2 lesson** — the only Tailwind viz-bridge consumption in the 66-SFC tree, delivering the true `#d73523` through CSS while the JS path four lines below would deliver grey. Both readers recomputed the oklch→sRGB independently and byte-agreed.
6. **`font-variant-numeric: tabular-nums` on the one number that mutates per gesture, and "Delete point" exactly true** (singular label, single-selection predicate — seat-corroborated `canDelete: selectedIdx !== null`). The unglamorous details, present unprompted. Recorded independently of D-5's contrast failure and the L-5 fold's boundary lie on the same two surfaces.
7. **The sibling dock proves the author knew the working glyph-size idiom** (`h-4.5 w-4.5` classes vs this file's dead `:size` — the library documents class-wins in-band). Same directory, same wave, same hand: MAJ-8 is a lapse, not ignorance — mechanical to discharge. Reader-DU.
8. **The `emitState()` discipline is total for the COUNT** — all five length-changing sites emit (seat-corroborated), so `pointCount` is never wrong; only the history predicates desync. The corpus row (L-14) that declined to inflate an architectural note into a live bug is credited by name. Reader-LC + the L axis itself.
9. **Zero component-authored motion on a surface that is nothing but motion** (S-1) — no transition/animation/keyframes in the scoped block (seat-corroborated); every moving part rides the library's PRM-gated substrate. The D axis.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **B-2** — the focus-destruction round-trip: Tab to the collapsed Save; assert focus lands at body after the expand-flip (per-engine), and whether Shift-Tab can re-enter the expanded row inside the 2000 ms window. The destruction chain is static; the "unreachable in practice" framing is the part to confirm.
2. **D-5 / D-6 / M-7** — computed colours of `.dock-badge`, `.is-save.saved svg`, and the saved-hover plate against their PAINTED backdrops. **Without the autoLuminance premise** (dead per fr-CanvasControlsDock M-4/K-1); the page-bound figures (3.32 / 2.21 / 1.97) are the recorded bounds.
3. **D-1 fold touch legs, both engines** — iOS Safari (expect: never opens) vs Android tap-focus (expect: opens via bare `onFocus`); inherited K-9 discipline: never "touch-dead" unqualified.
4. **D-4** — `.dock-spacer` width at rest (expect 0) and mid-FLIP-morph (the `--dock-morph-size` window).
5. **MIN-1 (D-13 leg)** — hold pointer-down on Save; read computed `background-color` (expect the unlayered base fill, not `--dock-control-press-bg`).
6. **M-2** — toggle "Contour trace" while editing; assert nothing visibly changes except the sibling dock's pip; then exit edit and observe the deferred flip.
7. **MAJ-8 rider** — the rendered cross-dock glyph split on a coarse-pointer device (the ≈26px factor is direction-certain, decimal-unverified).
8. **L-2 fold serialization** — rides fr-BasisCanvas UNPROVEN #1 (one `getPropertyValue` call); not duplicated here.
9. **L-6 fold magnitude** — rides fr-BasisCanvas BC-10's existing frame-cost slot; add the edit-mode arm (drag a point while the hidden pipeline runs).
10. **STRUCK**: D-28's empty-state probe (K-5 — the state is unreachable); C-9's mid-drag snap probe (K-2 — the premise is unreachable); any spectrum-variant trial (K-3).

## Identity guards (folds by reference — never re-booked)

- **L-1 → fr-ContourEditorCanvas D/B-6·L-3·C-4** (BLOCKER): + redo-truncation extension; + the REFINEMENT that its "green check asserts saved" leg is reachable only via its own D/M-10 race, not the plain drag path (K-1) — the forming F.W3/W4 spec should state the cure against the pointer-down-emit fact.
- **C-9 → fr-ContourEditorCanvas L-6·C-6 + K-19** (the kill was already banked; K-2 here is its second independent derivation).
- **L-5 → fr-ContourEditorCanvas L-17·C-14** (MINOR there; reachability sharpened here; severity dissent preserved).
- **L-2/C-2 → fr-BasisCanvas D-1/BC-1/C-1** (BLOCKER; the `:123` rider pre-banked at fr-ContourEditorCanvas; MAJ-1's deletion disposition dissolves the same-commit coupling for THIS file only).
- **L-6 → fr-BasisCanvas BC-10/C-9** (edit-mode arm + the AnimationControls-unmount strengthening added).
- **D-1/D-11/D-25/L-15 → fr-CanvasControlsDock D-2/C-2** (which pre-named :103/:134; riders per R-5).
- **C-10/C-11/C-17-cross-dock + DU's glyph/order/pip missed rows → fr-CanvasControlsDock C-9 + M-8 + the D-17 view-dot cluster** (editor-side anchors added; the pip-concealment consequence is the new fact).
- **L-14 → intake R5-7** (via fr-ContourEditorCanvas L-§4).
- **C-6 → intake R3-7b** (instantiated at `POST /api/contours`); **C-7** rides the same F.W5–W8 seam; the component itself is the R6-8 POSITIVE example (S-7 — no client↔operation join held).
- **C-3's shim leg → intake R3-7a + X-11** (this file = the largest Tooltip-callsite consumer; the naming leg rides the amended carry, per the fr-CanvasControlsDock D-1 routing).
- **L-7 ↔ fr-CanvasControlsDock D-12/L-6/C-4 + fr-ContourEditorCanvas MM-11**: sibling F.W1 rows; this file's callsite-density row is its own distinct surface, booked at MAJ-11 with the 4→8 correction.
- **B-2 ↔ fr-CanvasControlsDock M-2**: same relay letter, opposite faces (sealed-with-no-entry vs entry-that-self-destructs); **this record AMENDS that letter's item (ii) and its booked consumer cure** — the "focusable summary content" prescription is defeated by the inert flip unless the producer half lands with it.
- **M-4 (coverage) ↔ fr-CanvasControlsDock C-26 / fr-ContourEditorCanvas L-21**: same law, distinct surface — booked, not re-booked.
- **GLASS-RELAY consolidation (one letter, not many)**: (i) the B-2 inert-under-focus amendment to the collapsed-shell keyboard item; (ii) the `--success` light-arm contrast carry (D-6); (iii) the MetricBadge `cursor-pointer` false-affordance wart (D-17). All ride the standing fr-CanvasControlsDock letter; the 4.0.0 hover-popover exposure remains cured at the Popover union (noted there).

## DISSENT — preserved, not ruled away

1. **Reader-DU's L-1b and C-9 confirmations, overruled but preserved** (K-1, K-2): DU independently verified real component facts (the unconditional `editorSaved` reset; the guarded setter with the `?? 0` read) and anchored uncorrected conclusions on them without walking the pointer-down emit and the mount predicates one hop further. C-9 is the sharper lesson: the kill DU missed was already banked (fr-ContourEditorCanvas K-19) — confirmation hunts, like missed-find hunts, must open with a registry sweep.
2. **Reader-LC's registry lapses** (K-11, K-12): two correct derivations of banked rows shipped as fresh missed finds — the exact failure fr-ContourEditorCanvas's DISSENT §4 recorded the day before. Symmetric with §1: one reader under-swept the registry for kills, the other for finds.
3. **Severity dissents preserved**: DU's MAJOR on D-13 (press-darken on the commit control); both readers' MAJOR on the L-5 fold (vs the banked MINOR); LC's MINOR grades on D-7/D-8/D-9/D-10 (vs this seat's sibling-consistency MAJORs); DU's BLOCKER framing of the whole D-1 fold (vs the banked identity's own BLOCKER, which absorbs it). If SS-13's pass shows the 3-point Trash no-op or the press-feedback loss as repeated live confusion, the respective grades re-open.
4. **The autoLuminance citation-inheritance event**: the D axis wrote "autoLuminance default-true" into D-5's falsifier; BOTH readers repeated it into their unproven cells; the premise was killed at fr-CanvasControlsDock (M-4/K-1) the day before this corpus was re-read. Three independent seats carried a dead premise into probe framing — the same cross-record reconciliation gap the fleet keeps paying for (SS-3/SS-4 input).
5. **Adopted-not-recomputed cells, disclosed**: the D-5/D-6/D-7 contrast decimals (both readers' independent arithmetic byte-agreed; seat verified tokens + direction only); the C-3 lucide `aria-hidden` mechanism (corpus + sibling-record instrument; seat spot-checked the TooltipTrigger touch return only); the DockControl/Popover producer cure prose (corpus-verified at 7.0.0, readers re-verified shape at 8.0.0; seat verified exports/dirs only). Every other load-bearing cell in this record carries a seat instrument.
6. **LC's "no window" overclaim inside a correct kill** (K-1): the save-await race window exists and is banked elsewhere. A kill can be right and still need a boundary — recorded so the forming spec writes the cure against the race too.

## Closing verdict

**ADJUDICATED — DEFECTIVE, with the BLOCKER surface split between the booked and the banked: 38 rows confirmed in this record (2 BLOCKER · 12 MAJOR · 19 MINOR · 5 INFO) + 7 identity folds (3 of BLOCKER weight) for full 45-row corpus coverage · 12 kills registered — two reader confirmations overruled on seat evidence (one against a kill already banked in the registry), one reader praise-cell corrected by ruling, two reader missed-finds killed as registry re-books · 10 missed rows banked (headlined by reader-DU's focus-destruction BLOCKER, which inverts fr-CanvasControlsDock M-2's booked consumer cure, and reader-LC's invisible-in-effect ghost toggle) · producer pin corrected to 8.0.0 against the whole corpus and the sibling apotheosis · routings: 2×F.W1 + the F.W0 pin row · the C-3 naming leg on R3-7a/X-11 at F.W3 · the balance F.W4 · C-6/C-7 + the D-12 idempotency rider at F.W5–W8 · one three-item GLASS-RELAY amendment to the standing letter · 2 NO-WAVE-OWNER records — all agglomeration input to the forming X·F specs (M-25), stamping nothing.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
