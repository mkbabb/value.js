# PaletteColorStrip — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: adjudicator-F (incredulity seat over two hostile re-readers).
**Date**: 2026-08-06 · **HEAD**: `3ecf0267` · branch `tranche-u` · **Subject**: `demo/palettes/browser/card/PaletteColorStrip.vue` (72 L) + its 3-consumer / 2-import closure.
**Corpus (r2 governs)**: `audit/components/PaletteColorStrip/challenge-{C-implementation,D-design,L-library}.md` — all three EXISTS-ORIGINAL in `registry/HYDRATION-LEDGER.md:103-105`; 4 probe artifacts beside them. Motion-quarantine bar: `grep PaletteColorStrip motion-quarantine.md` → 0 rows (my run) — no M-xx FALSE row constrains any claim here.
**Readers adjudicated**: reader-A (design-and-usage) · reader-B (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement (L-3, L-7 limb 2, D1 grade, D8, D9, D10-vs-constitution, C-3 reachability, L-9 grade) was re-derived against the tree by this seat with its own instruments.
**Arbiter's own instruments (all static + node against the working tree; ZERO browser tooling per law)**: full read of PaletteColorStrip.vue (whole); PaletteCard.vue:1-60; foundation.css:636-700 + :790-841 (both roster arms, whole); VISUAL-CONSTITUTION.md:1-20/:50-58/:184-196; N/audit/lanes/C5.md:118-135/:243-249; api/src/modules/palette/hash.ts:1-55 (whole) + schema.ts:25-35; useExtractSession.ts:80-92/:140-152; ExtractWorkbench.vue:115-130; GenerateControls.vue:50-58; GeneratePane.vue:14-19; mix.ts:140-144; MixPane.vue:40-44; CurrentPaletteEditor.vue:242-246 + `:disabled` grep; export.ts:15-19; usePaletteStore.ts:1-26; demo/color-picker/index.html:1-14; tsconfig.demo.json:30-50 + `package.json#exports` (7 keys, no `.`) + `ls dist/ dist/subpaths/`; **the L-3 decider**: `npx tsc -p tsconfig.demo.json --noEmit --traceResolution` → `@mkbabb/value.js/css` resolves to `/Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts` Package ID `@4.0.0` (×3 shown, my run); **the arithmetic decider**: the `segmentPcts` body re-run verbatim in node — 75.0000/6.2500×4 at n=5 · min 3.889 at [900,10×15] · 25.466 at n=16/.41 · the full ceiling table 75.76/64.10/53.19/51.02/45.45/39.68/20.33 · partial [0.3,0×4]→75.76/6.06×4 · [-1,1,1]→7.407/46.296² · Infinity→NaN-vector · `NaN>0`=false · `JSON.parse("1e999")`=Infinity; greps: `:weights|weights=` in demo Vue → 0, `ColorStrip|WEIGHT_FLOOR|segmentPct` in test/ e2e/ demo/test/ → exit 1, `EmptyPaletteMark` → 0, `ratio-phi` (demo + glass dist) → 0, `data-color-surface` → exactly the 2 CSS rule bodies, `layout="split"` → 0, `.preview-chip` defined at PreviewStrip.vue:56 AND PreviewRamp.vue:40, `data-slot` on skeleton+shadow and NOT on the strip; **my own eyes on `probe-states-4up.png`** (the D2-kill decider). Sibling apotheoses read for identity guards: PaletteCard.md (PC-3/PC-7/PC-14/PC-29/PC-32 + superlative 1), GenerateControls.md (R-C/R-D/R-G + row 6), ColorSpaceSelector.md (L-5(a)/L-14 identities); X wave specs W7 (whole of .c/.d), W8 (:42/:112/:250), W6/W10 headers.
**Environment disclosure**: no dev server touched. Every ruling below is decidable from source + node; the readers' and challengers' live cells are adopted as corroboration only and quarantined into the UNPROVEN residue where load-bearing.

## Scoreboard

**31 rows CONFIRMED (1 BLOCKER-class FOLD · 10 MAJOR · 12 MINOR · 8 INFO) · 10 kills sustained in the register (every kill re-derived by this seat; one reader-confirmation cell overturned by my own trace) · 12 missed-by-the-axes findings adopted (readers' union, all re-verified; five folded into sibling rows) · dispositions: X-W7.c ×22 · X-W7 export unit ×1 · X-W8 ×1 · PC-7/PC-14/PC-29 identity FOLDs ×3 (inside the W7.c count) · GenerateControls#6 BANKED identity ×1 · NO-WAVE-OWNER ×6 (one with the W-HYGIENE identity preserved).**

Headline: **the component's single documented invariant — the "8% legibility floor" — is arithmetically false on the shipped path, and a sibling apotheosis praised the exact line that breaks it.** `PaletteCard.md` Superlative 1 lauds `:66-67`'s renormalisation as "the step everyone forgets"; this corpus proves, and this seat re-derived to four decimals, that the renormalisation is the step that **undoes the floor** (delivered minimum `0.08/flooredTotal`, 6.25% at the shipped k=5, 3.889% at k=16). The correction is recorded in COORDINATION below. Second headline: the strip is absent from **both arms** of the demo's own color-surface roster (WHCM *and* print) — already booked whole as **PC-7 (BLOCKER)**; this docket adds only facts, never a second booking.

## Ruled disagreements (the adjudication's actual work)

| id | reader-A | reader-B | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **L-3** | CONFIRMED MAJOR ("demo typechecks against a different library"; scope-corrected off this docket) | KILLED (traceResolution: package self-name resolution → this checkout's dist) | **KILLED — B wins; A's confirmation cell overturned** | My own trace under the real config: `@mkbabb/value.js/css` → `/Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts` Package ID `@mkbabb/value.js/dist/subpaths/css.d.ts@4.0.0`. The missing `paths` key does not mean node_modules; it means TS walks up to the root package.json (`name: "@mkbabb/value.js"`) and honours its own exports map. Type and runtime land on the SAME artifact; the node_modules byte-diff A verified is real and irrelevant. Survivor: the dead config rows (PCS-30) — fact-identical to ColorSpaceSelector L-14 (W-HYGIENE config parity, born-RED generator command); **identity guard: never re-booked under a second name**, and X-W8.md:112 explicitly declines it ("L-22 ≡ W-HYGIENE, another owner") → NO-WAVE-OWNER with the identity preserved. |
| **L-7 limb 2** | CONFIRMED ("the comment is false — the strip does not clip its own corners") | KILLED ("overshoots; the comment is accurate about WHERE the clip happens") | **KILLED — B wins** | My reads: PaletteCard.vue:16-18 says "the strip clips its OWN corners below — an interior clip keeps normal AA", and the `rounded-t-card` class falls through onto the strip's own root (:36), which carries `overflow-hidden` (PaletteColorStrip.vue:7). The strip element does clip its own corners; the comment describes the clip's LOCATION (the S-15-A AA rationale) and is accurate as written. The true defect is unowned SHAPE (limb 1, PCS-6): the radius is authored at three call sites and MixSourceSelector.vue:199-203 re-introduces the banned ancestor clip. Calling the comment "false" invites deleting a correct statement. |
| **D1 grade** | BLOCKER (as filed) | MAJOR (name+count survive; state unwitnessed) | **BLOCKER-class, resolved BY IDENTITY — neither reader's framing** | PaletteCard.md **PC-7** already books this exact surface — "the compact colour strip is absent from the demo's own COLOR-SURFACE ROSTER … WHCM **and** print" — at **BLOCKER**, routed X-W7.c, with reader-B's print limb already adopted there. The AdminTagsPanel precedent cuts both ways: a class booked BLOCKER at its home cannot be independently re-graded MAJOR for a sibling docket. PCS-2 is a FOLD; the grade lives at PC-7. My own reads confirm both arms: forced-colors roster = 15 selectors (:679-694), print roster = 8 (:830-841), the strip matches neither, and the ":659-663 declared ONCE" claim is false by 7 selectors of drift. |
| **D8** | UNPROVEN ("a design judgment, not a claim against bytes") | CONFIRMED INFO (statically decidable; quarantine-checked) | **CONFIRMED INFO — B wins** | The file has no `<style>` block and no transition/animation utility anywhere in :1-25 (my whole-file read) — `animationName: none` follows from bytes, no browser needed. Whether the absence is a *violation* stays a design judgment, and the row is graded as a recorded absence, not an edict breach. Quarantine law satisfied by my own grep: zero M-xx rows name this component; all three challenge files are EXISTS-ORIGINAL. → X-W7.c under X-W10 law (motion decisions are W10's canon). |
| **C-3 reachability** | "latent-by-luck — only the dead prop reaches it" | localStorage vector: `JSON.parse('{"weight":1e999}')` → Infinity | **B's strengthening ADOPTED** | My run: `JSON.parse("1e999")` = Infinity; usePaletteStore.ts:19-26 rehydrates palettes through a serializer that validates `version` and nothing else, and the strip's `own` channel (:53) reads `c.weight` straight off store palettes. The blank-strip path is reachable from tampered client storage, not only from the caller-less prop. Adversarial-only → MINOR stands (PCS-15). |
| **D9** | provenance limb KILLED (cites the dead `export/` module); substance INFO on the live export.ts path | UNPROVEN + demoted: `index.html` hard-pins `dir="ltr"` as deliberate booked readiness | **BOTH adopted, merged** | My read of demo/color-picker/index.html:1-11: `dir="ltr"` pinned with the U-F58 comment naming per-component physical→logical conversions as BOOKED follow-ups (`docs/tranches/U/audit/w-a11y/rtl-logical-property-audit.md`). The mechanism (bare flex row reverses under rtl; export emits ascending-index) is sound and the provenance limb is killed (K-8); no direction switch exists → PCS-31, INFO, NO-WAVE-OWNER on the booked RTL ledger. |
| **D10 vs constitution status** | missed-#6: "the constitution specifies a Card the tree does not implement; five D findings are 'not yet built' reported as 'unmet'" | D10 CONFIRMED MINOR on the verbatim §Generate clause | **BOTH right, reconciled** | My reads: VISUAL-CONSTITUTION.md:54 mandates the quiet opaque `sm` tuple "no cartoon"; the shipped card carries `cartoon-surface` citing the ratified T.W5-R4/T-14/D7 register (PaletteCard.vue:6-19). The constitution is the **binding forward canon** — the X waves build to it (X-W7.c's PaletteSpecimen implements §3.1/§5 verbatim) — so constitution-grounded rows are **build-forward obligations, not repair defects**; A's re-framing is adopted as a disposition note and demotes nothing. D10 itself stands (the §Generate "not a flat strip plus unrelated buttons" clause read verbatim by this seat): Generate limb ≡ GenerateControls.md row 6 TRIPLE-DEPICTION (BANKED/W25 — pinned file; identity preserved, not re-booked); Mix limb → PCS-19. |
| **D2 visual limb** | KILLED | KILLED | **KILL SUSTAINED by this seat's own eyes** | I read `probe-states-4up.png`: row 3's leading band spans ~a quarter of the strip; row 4's spans one sixteenth — plainly different at a glance, exactly as the axis's own arithmetic (25.47% vs 6.25%) predicts. The compression finding is fully confirmed (PCS-1); the "indistinguishable / identical to the fallback" rhetoric that elevated D2 to "strongest single defect" is false. |
| **L-9 grade** | MINOR | MAJOR | **MAJOR — B wins** | Two strip species imported into ONE file and rendered on one screen (GenerateControls.vue:16/:20, :135/:245/:274) with incompatible input types, geometry and truncation policy, while `color-chips/index.ts:2-4` claims "ONE focused common module … never a per-pane copy" — a false uniqueness claim plus a live same-screen duality, and `.preview-chip` is defined twice divergently (my grep: PreviewStrip.vue:56 `inline-flex` vs PreviewRamp.vue:40 `inline-block`). |

## Adjudicated defect roster

Line anchors are HEAD `3ecf0267`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

### BLOCKER-class (fold)

- **PCS-2 (D:D1 + readers' print limb) — the strip is absent from BOTH arms of the demo's own COLOR-SURFACE ROSTER: WHCM substitutes its backgrounds and print drops them for ink economy, on the product's primary entity, whose collapsed card has no other color surface (`PaletteCardSwatches` is `v-if="expanded"`, and the strip is aria-hidden — both channels fail together).** My reads: roster intent :653-657 verbatim; forced-colors arm :679-694 (15 selectors); print arm :830-841 (8 selectors); strip emits only `overflow-hidden flex h-10 w-full` + `shrink-0 h-full` — member of neither; no `.swatch-row` ancestor. The ":659-663 one roster, declared ONCE" claim is false — the two copies have drifted by 7 selectors, of which `canvas`, `.gamut-overlay`, `[data-glass-field-canvas]`, `.readout-rail`, `.glass-slider[data-variant="spectrum"] .slider-range` are dropped from print and NOT covered by print's `display:none` block (:801-807). **Disposition: FOLD → PC-7 (grade lives there: BLOCKER) → X-W7.c**, carrying two riders: (i) the **R-2/R-C reconciliation** — GenerateControls.md R-C ruled `[data-color-surface]` a deletable dead selector (zero consumers, my grep confirms) while PC-7's cure makes the rebuilt strip its first consumer; X-W7.c must pick ONE (hook revival or roster-class membership), never both waves acting blind; (ii) the **print box-shadow rider** — `@media print * { box-shadow: none !important }` (:814-819, my read) kills any inset-hairline boundary cure in the print register, so PCS-5's cure and this row's cure must land as one decision.

### MAJOR

- **PCS-1 (C:C-1 / D:D2 arithmetic / L:L-10) — the documented "8% legibility floor" is not a floor: clamp-then-renormalise undoes the clamp; the delivered minimum is `0.08/flooredTotal` and shrinks as n grows; dominance is compressed (41% drawn as 25.47%; hard ceiling 45.45% at Extract's own k-max 16, 20.33% at the API's n=50).** Every published figure across all three axes re-derived by this seat in node to four decimals (see instruments). Reader-B's strengthening ADOPTED and re-verified: ExtractWorkbench.vue:119-127 renders `Math.round(dominantShare*100)}% of the image` — the RAW share (useExtractSession.ts:145-149) — immediately above the card whose strip renders the floored share: two computations of one quantity, adjacent on one screen, guaranteed to disagree. Correct shape is water-filling (exact, idempotent, loud when `n·floor > 1`) or floor deletion + honest truncation (the PreviewStrip law). **Disposition: BUILD → X-W7.c — with a NAMED GAP**: W7.c's mechanism (:210-212) names only the `Math.max(…, 0.5)` clip floor and "flex-basis proportional to weight"; the WEIGHT_FLOOR renormalisation defect is in no X file's mechanism text. W7.c must adopt this row explicitly (its `n-fixtures` gate is the natural home) or it degrades to NO-WAVE-OWNER at execution.
- **PCS-3 (C:C-6) — the wire schema silently discards `weight`: a saved extracted palette loses the population story the component exists to draw, falsifying `types.ts:5-11` and the strip's own :40-42 prose the moment the user presses Save.** My read: api/src/modules/palette/schema.ts:27-31 — `{css, name?, position}`, no `weight`; zod strips unknown keys, no 400, no log. Sole producer useExtractSession.ts:88. The honest cure is a decision, not a patch: (a) `weight` joins `colorEntrySchema` (and the hash-canonicalisation question of PCS-11 must then be answered), or (b) `weight` becomes an extract-session transient and the type stops claiming it rides ON the palette. **Disposition: NO-WAVE-OWNER** — no X wave touches `api/src/modules/palette/schema.ts`; named candidates: the client half rides X-W7.c/d, the schema decision needs an owner at the next formation boundary.
- **PCS-4 (C:C-4) — vacuous gate: zero tests anywhere reference the component, and replacing :50-71 with `colors.map(() => 100/n)` deletes WEIGHT_FLOOR, the weighted path and the whole S.W5-6/F7/T19 deliverable while vitest, playwright, vue-tsc and eslint stay green.** My grep: exit 1 across test/ e2e/ demo/test/. The arithmetic is a pure function trapped in an SFC computed — which is exactly why PCS-1 shipped. **Disposition: BUILD → X-W7.c** (authors `demo/test/palettes/n-fixtures/`; G8-G12) — with the PCS-22 rider: the corpus's own proposed e2e oracle selects `div[aria-hidden="true"][role="presentation"]`, which C-12's cure breaks; the stable hook lands first.
- **PCS-5 (D:D4 + the UNFILED N-tranche F-4 limb, adopted from reader-A's miss) — no boundary of any kind: (i) n colors render as fewer than n bands beside a badge asserting n (5→2 in the axis's own probe, re-seen by this seat); (ii) the strip collapses into the card ground for dark-dominant palettes — the actual `N/audit/lanes/C5.md:123-131` [P1] filing ("a single near-black bar … indistinguishable from the card chrome"), which D4 quoted with an ellipsis that deleted its subject, so no V axis ever filed it.** My read of C5.md confirms both limbs and the :246 cure order, never executed. The registers already exist in-tree: PreviewStrip's container inset ring (`inset 0 0 0 1px` @12% foreground) answers limb ii; ShadowPalette.vue:46-51's designed `gap-px` boundary law ("keeps the segmentation legible AT REST") answers limb i — the loading ghost has the law the loaded strip lacks. **Disposition: BUILD → X-W7.c**, coupled to PCS-2's print rider (box-shadow dies in print — the seam must survive both registers).
- **PCS-6 (D:D5 / L:L-7 limb 1 / C:C-9) — the corner law is authored at three call sites by three mechanisms, one of which (MixSourceSelector.vue:199-203's ancestor `rounded-card overflow-hidden` wrapper) re-introduces the exact S.W5-10/S-15-A banned pattern, because the law survives only as prose in a sibling consumer (PaletteCard.vue:16-18).** The comment itself is accurate (see ruled disagreement — L-7 limb 2 killed); the defect is that the strip owns the clip but not the shape. The concentricity arc limb (radius = outer 16px at a 2px/1px inset → ~0.83px/~0.4px proud arc) is **≡ PC-29** — identity preserved, pixels in residue U-3. **Disposition: BUILD → X-W7.c G11** (silhouette law: the component owns radius as `calc(card-radius − border-width)` logic in ONE home; the Mix wrapper's clip deletes).
- **PCS-7 (L:L-6 + reader-B's readonly-props miss folded) — the prop type is the palette DOMAIN type: the strip reads exactly `css` and `weight`, yet `position` is REQUIRED, so GenerateControls.vue:55-56 mints fabricated positions and imports `palettes/types` across an area boundary to paint a colored bar.** GenerateControls.md R-G's sharpening adopted verbatim: the fabrication is *type-mandated*; "retype on the move". PreviewStrip's `stops: readonly string[]` is the in-tree contrast; the mutable `PaletteColor[]`/`number[]` prop surface (vs both siblings' `readonly`) rides the same retype. **Disposition: BUILD → X-W7.c** (PaletteSpecimen is props-only; the paint type is `readonly {css, weight?}[]` at most).
- **PCS-8 (L:L-8) — `demo/palettes/export.ts` (live, 132 L) vs `demo/palettes/export/` (ratified byte-exact set, 12 modules): the contract half is reachable only from `demo/test/export/byte-exact.test.ts`; both host panes of the strip download through the legacy module. CI proves a module users never run.** Scope [A], outside the strip's import closure — booked at area scope per the L-9.1 scope law. **Disposition: BUILD → X-W7 export unit** (W7.md:42 + :92-96 + :194-200: `export.ts` delete, `usePaletteExport` routed onto the certified serializers).
- **PCS-9 (L:L-2 + reader-B's +gradient count and homing rider) — the `palettes` area has no seam: no `demo/palettes/index.ts` exists while `browser/index.ts:1` calls itself "the mega-feature's TOP-LEVEL SEAM"; SIX areas (workbenches/{mix,generate,extract,gradient}, shell/dock, color-picker) raw-reach `palettes/` internals, `shell/dock → palettes/usePalettePorts` being a shell→feature inversion.** Homing rider (reader-B miss, adopted): the strip itself is reached by three different specifiers (raw intra-cluster, card sub-barrel ×2, browser re-export) — a shared presentational primitive wearing a feature-internal address. **Disposition: NO-WAVE-OWNER** — no X wave names the area seam; candidates: X-W8 (graph subtraction) for the lint re-pointing, the seam construction needs an owner at the formation boundary.
- **PCS-10 (L:L-9) — five parallel implementations of "render a palette compactly" with incompatible truncation policies; two imported into ONE file and rendered on one screen; `.preview-chip` defined twice, divergently, under a directory whose index claims uniqueness.** Grade ruled MAJOR (see disagreements). **Disposition: BUILD → X-W7.c for the strip's own consolidation** (W7.c's mechanism already names the PreviewStrip mask-fade / MixResultDisplay gradient idioms as the over-threshold arm); the chip-family unification residue (PreviewStrip/PreviewRamp/dot-rows) is NO-WAVE-OWNER, named so it is not lost.
- **PCS-11 (readers' position miss, union of both — the strongest new find of this adjudication) — `PaletteColor.position` is a REQUIRED, undocumented field with two incompatible live encodings, and the server folds it into BOTH identity hashes.** All verified by this seat: Producer A (integer ordinal ×5 sites — GenerateControls.vue:56, GeneratePane.vue:17, MixPane.vue:42, mix.ts:142, CurrentPaletteEditor.vue:244); Producer B (normalized fraction — useExtractSession.ts:87 `i / max(1, len-1)`); both pass `z.number().finite()`; hash.ts:11-13 rounds position into `computeContentHash` (version/fork identity) and :46 keys `computeAtomSetHash` (dedup fingerprint) by it, while :20-24's own doc mandates the property both producers break ("position is the atom KEY (its stable identity in the ladder) … a re-order does not [change it]" — but both derive position from array index, so a reorder rewrites every key and the documented order-independence cannot manifest). The same 5 colors saved from Extract vs Generate hash differently; the live exporter (export.ts:17) ships the ambiguity verbatim to the user while the ratified serializers mint 1-based positional ids and ignore the field. **Disposition: NO-WAVE-OWNER** — the contract decision (what `position` MEANS) is server-owned and unnamed in every X file list; client-half candidates X-W7.c/d. Must not be silently absorbed into PCS-3's cure: adding `weight` to the schema without ruling `position` compounds the identity ambiguity.

### MINOR

- **PCS-12 (C:C-5 / L:L-4 / D:D2 dead-arm) — the `weights` prop is dead public surface: zero consumers (my grep), a six-line doc, a per-recompute branch, and a permanently unreachable tier-1 of the documented precedence.** → X-W7.c (dies in the specimen rebuild).
- **PCS-13 (L:L-1, conclusion only) — the G-DEMO boundary `browser/index.ts:9-12` cites as "enforced standing" is inert: every ban pattern keys on the `@components` alias killed at W43/RF-15 (tsconfig.demo.json:32-34, my read), so no specifier can match; the `demo/@/**` globs are additionally dead.** Glob limb killed (K-10). No strip consumer violates the seam today (reader-A verified; adopted). ≡ ColorSpaceSelector L-5(a) — identity preserved. **→ X-W8** (W8.md:42/:250 deletes the six dead globs; the re-pointing rides the same carve).
- **PCS-14 (C:C-7 geometry / L:L-5) — the 40px strip geometry has four literal homes (`h-10` ×3 + `w-10`), and the loading ghost's segment mechanics genuinely diverge (ShadowPalette `gap-px` + `flex-1` vs seamless percentage widths): segments shift at the loaded handoff; no token binds them, no test asserts agreement.** The a11y sub-limb is killed (K-9). → X-W7.c.
- **PCS-15 (C:C-3 + C:C-8, merged; reader-B's storage vector adopted) — the guard-then-swallow ladder: wrong-length `weights` silently ignored; one NaN silently reverts to equal (the population story vanishes without signal); one Infinity blanks the ENTIRE strip (NaN% vector, my run) — reachable from tampered localStorage (`JSON.parse("1e999")` → Infinity, my run; the store serializer validates only `version`); a negative weight still draws 7.4% of the bar; `backgroundColor` unvalidated against a schema that never checks css-ness. The finding is the asymmetry: mix.ts:46-55 writes the exact four-guard ladder for the identical quantity, forty lines away.** → X-W7.c (one `toShares()` boundary; the css-validation half coordinates with the parser program's oracle).
- **PCS-16 (C:C-2, deadness limb) — the entire `orientation="vertical"` branch has never executed: the sole `:layout` ternary requires `layout === 'split'`, which no shipped mount produces (`layout="split"` grep → 0; ExtractPane.vue:13 hard-codes `column`, my read). Roughly half the rendering surface ships, typechecks, and has never painted.** ≡ **PC-14** — FOLD, identity preserved → X-W7.c (W8's dead-surface gate is the backstop). The 0px rendering limb stays UNPROVEN (residue U-1) with reader-B's counter-mechanism recorded: a percentage height against an indefinite flex container resolves as `auto`, which is precisely the condition `align-self: stretch` needs — the live answer could be the OPPOSITE of the axis's synthetic probe.
- **PCS-17 (D:D3, as rescoped by the kills) — the zero-color palette renders a 40px transparent void; the host mints `EMPTY_PALETTE_SWATCH` (PaletteCard.vue:220-223) and routes it away from the one surface that shows swatches. Reachability is bounded: API `.min(1)`; the local save is double-guarded (CurrentPaletteEditor :138 `:disabled` + the pre-emit return, my read + reader-A's); no card-level color delete exists. A contract gap, not a state users hit (K-4); the "reuse EmptyPaletteMark, zero new primitives" cure is killed (K-5 — the mark does not exist; my grep).** → X-W7.c (the specimen's empty arm is a designed state per the forward canon).
- **PCS-18 (D:D6, observation core only) — one raw isotropic Tailwind literal (40px) serves as both band thickness and rail width, bound to no token and no ratio, while the far smaller sibling carries `2.618rem /* φ² × 1em (F7) */` with provenance.** Both quantitative support limbs killed (K-6: `--ratio-phi` does not exist anywhere — my grep; the `C = 16px` anchor is the constitution's forward spec, not the live card's `px-3` = 12px). The live share-of-host swings (33%/21%/13%) are residue U-4. → X-W7.c.
- **PCS-19 (D:D10) — the strip renders inside the two workbench compositions the forward canon rules it out of (§Generate: "not a flat strip plus unrelated buttons", read verbatim by this seat), duplicating the WatercolorDot specimen on the same plate.** Generate limb **≡ GenerateControls.md row 6 TRIPLE-DEPICTION — BANKED/W25** (pinned file; identity preserved, NOT re-booked). Mix limb (MixSourceSelector.vue:203 + its :210-220 dot row) → **X-W7.c with a NAMED GAP**: MixSourceSelector.vue appears in no W7 file list; the strip's call-site migration must adopt it or this limb degrades to NO-WAVE-OWNER at execution.
- **PCS-20 (C:C-14) — the population story has no non-visual equivalent anywhere: the strip is the app's sole rendering of `weight` and it is aria-hidden; the card's accessible name is name + bare count. Hiding a decorative rail is right; hiding the sole rendering of a data dimension was never stated as a decision.** Adjacent to (not identical with) PC-32's unlabelled-count row. → X-W7.c (+ the X-W7.f count-formatting seat).
- **PCS-21 (readers' partial-weights miss, both limbs merged) — the gate is `some`, the coercion is `?? 0`: one weighted entry switches the whole strip into the weighted branch, where every weight-LESS color is floored to WEIGHT_FLOOR and reads as "small but measured" rather than "unknown" — my run: `[0.3, 0×4]` → 75.76/6.06×4, a fabricated population relation. Unreachable today (the sole producer writes all-or-none behind `total > 0`), but representable by the type the component accepts.** → X-W7.c (gate becomes `every` + an explicit unknown-weight arm, or the type forbids partial weighting).
- **PCS-22 (reader-B miss) — no stable hook: the ghost siblings carry `data-slot` (skeleton :33, shadow :42 — my grep) and the strip carries nothing, which is why the corpus's own proposed e2e oracle selects by ARIA attributes that C-12's own cure removes — two cures in one corpus that collide, noticed by neither axis.** → X-W7.c (the specimen lands `data-slot` before any oracle or ARIA cleanup).
- **PCS-23 (C:C-10 / L:L-11 / D:D11.2) — `Math.max(100/n, 0.5)` is dead for every reachable n (API max 50, extract 16, generate 12) and incorrect where it would fire: past n=200 the un-renormalised sum exceeds 100% and `shrink-0` + `overflow-hidden` silently clip trailing colors.** The one row an X mechanism already names verbatim (W7.md:210 "delete the `Math.max(…, 0.5)` clip-inducing floor"). → X-W7.c.

### INFO

- **PCS-24 (C:C-12 / D:D11.1)** — `role="presentation"` beside `aria-hidden="true"` is inert; reader-A's strengthening adopted: ARIA mandates the role be IGNORED on an element carrying a global aria-* attribute — spec-mandated noise, not mere redundancy. Keep aria-hidden, drop the role (after PCS-22's hook lands). → X-W7.c.
- **PCS-25 (C:C-13 / D:D8 tail)** — `:key="i"` index keys on a list the product intends to reorder/animate; benign today, forecloses TransitionGroup and mis-animates the moment a color transition lands. → X-W7.c.
- **PCS-26 (C:C-15 / L:L-12)** — `weights = undefined` destructure default is a no-op; dies with PCS-12. → X-W7.c.
- **PCS-27 (D:D8)** — zero motion: no style block, no transition class; the strip hard-cuts on every Regenerate while its plate-mate rides the register. A recorded absence, not an adjudicated violation; no M-xx quarantine row applies (my grep). → X-W7.c under X-W10 law.
- **PCS-28 (D:D11.3)** — six lines of tranche provenance in a prop doc for a facility with no caller; dies with PCS-12. → X-W7.c.
- **PCS-29 (reader-A miss)** — the third dead guard between the two the axes catalogued: `segmentPcts[i] ?? 0` (:20-21) can never be nullish — `segmentPcts` maps `colors` itself, so inside `v-for="(color, i) in colors"` the index always exists. Verified against my whole-file read. → X-W7.c.
- **PCS-30 (L:L-3 residue)** — tsconfig.demo.json:37-50 carries three entries resolving to nothing (`.` → absent `dist/index.d.ts`; `/parsing`, `/units` → nowhere) and omits `/css`/`/value` (covered by self-name resolution — my trace); the block's "CLOSED 8-key set" comment is false (7 keys, no `.`). **≡ ColorSpaceSelector L-14 / W-HYGIENE config parity — ONE identity, already folded there with a born-RED generator command; X-W8.md:112 explicitly declines it.** → NO-WAVE-OWNER, identity preserved.
- **PCS-31 (D:D9 residue)** — on-screen strip (document-direction flex row) vs downloaded SVG (unconditionally ascending-index) would mirror under RTL; mechanism sound, exposure nil: `index.html` hard-pins `lang="en" dir="ltr"` as deliberate U-F58 readiness with per-component conversions BOOKED (my read). → NO-WAVE-OWNER, rides the existing `docs/tranches/U/audit/w-a11y/rtl-logical-property-audit.md` ledger; the `rounded-l-card` physical-radius arm dies with PCS-16's dead branch.

## Killed-claims register (each kill re-derived by this seat)

1. **K-1 · D2 headline** — "the weighted render is visually indistinguishable from the unweighted one / identical to the fallback it was built to replace" (challenge-D-design.md:132, :177-179). REFUTED by the axis's own artifact and its own arithmetic three lines above the claim; re-seen by this seat in `probe-states-4up.png` (row 3's leading band ≈4× its neighbours). The compression is real (PCS-1); the erasure rhetoric is false, and it is the sentence that made D2 "the strongest single defect".
2. **K-2 · L-3 core** — "the demo typechecks against a different library than it runs" (challenge-L-library.md:134-199, :599-602 — the axis's own strongest-defect claim). REFUTED by this seat's own `--traceResolution` run: package self-name resolution lands types AND runtime on this checkout's `dist/subpaths/css.d.ts`. The node_modules byte-diff is real and never consulted. Residue survives as PCS-30 (INFO, W-HYGIENE identity). Reader-A's CONFIRMED-MAJOR cell is overturned with it.
3. **K-3 · L-7 limb 2** — "the comment is false about this component: the strip does not clip its own corners". OVERSHOOTS: the fall-through radius lands on the strip's own root, which carries `overflow-hidden` — the comment describes the clip's location (the S-15-A AA law) accurately. The true defect is unowned shape (PCS-6). Killing a correct comment invites deleting hard-won knowledge.
4. **K-4 · D3 reachability limb** — "this is not a theoretical state". The argument rests on a PROSE comment; the save path is double-guarded (CurrentPaletteEditor :138 + the pre-emit return), the API is `.min(1)`, and no card-level color-delete affordance exists. C-11's honest "hypothesis as to reachability" was right; D's escalation was not. (Generate transient-mount revisit-trigger in residue U-5.)
5. **K-5 · D3 cure limb** — "reuse the established `EmptyPaletteMark` … zero new primitives". The mark does not exist (my grep: 0 hits in demo/); it is named only prescriptively in VISUAL-CONSTITUTION.md:186. The live empty mark is EmptyState.vue's 24-44px ghost-dot trio, which cannot be "reused at strip scale" without being a new primitive.
6. **K-6 · D6 support limbs** — `--ratio-phi` exists nowhere (my grep across demo/ + glass dist; the producer's phi tokens are `--space-phi-*`/`--spacing-phi-*`/`--sqrt-phi`), and the `C = 16px` anchor is the constitution's forward spec while the live card's metadata row is `px-3` = 12px with no `--card-pad-*` reference. The observation core survives (PCS-18); the published rungs do not.
7. **K-7 · D4/D5 cure provenance** — "lift `.preview-chip` into a shared glass-ui strip primitive … reuse an existing component-type name, `Strip`". Glass-ui 7.0.0 has no Strip/band/color-band export (reader-A's 67-subpath enumeration, consistent with this program's producer reads at GenerateControls/PaletteCard); PreviewStrip is a demo component. L-9 §4.2's `./color-band` proposal is honest about minting; D's edict-3 framing has no referent.
8. **K-8 · D9 export-contract provenance** — cites `PALETTE-CONTRACT` Appendix W51 §6 / the `export/` svg module, which L-8 itself proves is dead in production; the SVG users download comes from legacy `export.ts:67-68`. Substance survives at PCS-31 on the live path; the citation was to a module no user runs.
9. **K-9 · C-7 a11y sub-limb** — "the three siblings do not even agree on whether a colour strip is decorative". The skeleton's root carries `role="status"` + `aria-label="Loading palette"` with its rationale written at :28-31 — a deliberate, correct, DIFFERENT posture for a different state, not an omission. A manufactured inconsistency bundled with a genuine geometry finding.
10. **K-10 · L-1 glob limb** — "the file globs match nothing". False for the live `demo/color-picker/**` globs (the tree exists and is populated; reader-B's `--print-config` on App.vue shows the rule LIVE there); the axis generalised from a sample of two files that sit outside the globs. The conclusion survives on the alias-death reason alone (PCS-13).

## Missed-by-the-axes findings (readers' union — 12 adopted, all re-verified by this seat)

1. Print-arm roster absence + the false "declared ONCE" invariant (both readers) → folded into PCS-2 (≡ PC-7, which had already adopted it).
2. `[data-color-surface]` has zero consumers repo-wide — D1's own prescribed cure hook has never been exercised (reader-A) → PCS-2's R-C reconciliation rider.
3. `PaletteColor.position` dual semantics; reader-B's server-hash deepening makes it PCS-11 (MAJOR, the adjudication's strongest new find).
4. Partial-weight fabrication: `some` gate (reader-A) + `?? 0` conflation (reader-B), one mechanism → PCS-21, with my own numeric witness.
5. The N-tranche C5.md F-4 dark-slab limb, quoted into D4 with a subject-deleting ellipsis and never filed by any V axis (reader-A; ellipsis verified against C5.md by this seat) → PCS-5 limb ii.
6. Constitution-status disposition note (reader-A): constitution-grounded rows are build-forward, not repair — ADOPTED as a re-framing (see ruled disagreements); demotes nothing, redirects everything to X-W7.c's build.
7. The third dead guard `segmentPcts[i] ?? 0` (reader-A) → PCS-29.
8. `data-slot` absence + the C-12×C-4 cure collision (reader-B) → PCS-22.
9. Mutable prop types vs the siblings' `readonly` convention (reader-B) → PCS-7 rider.
10. Triple-specifier homing of a three-area component inside one area's card sub-cluster (reader-B) → PCS-9 rider.
11. The dominantShare adjacency — raw % beside floored strip on one screen (reader-B; re-read by this seat at ExtractWorkbench.vue:119-127) → PCS-1.
12. The localStorage `1e999` → Infinity vector past the version-only serializer (reader-B; my JSON.parse + store read) → PCS-15.

## Superlatives (provenance preserved)

1. **The corpus's central convergence**: three axes reached the clamp-then-renormalise defect from three genuinely different framings (broken idiom / dominance collapse / untrue name), and every published figure — 6.25, 3.889, 25.47, and the seven-entry ceiling table — reproduces EXACTLY under this seat's own verbatim re-run. Triple-independent triangulation with zero numerical drift is the strongest evidentiary shape in the program.
2. **demo/color-session/color-chips/PreviewRamp.vue:26** — the one-element `linear-gradient` idiom: n DOM nodes → 1, overflow unrepresentable by construction, interpolation free. The correct primitive for this concept already exists in-tree, upstream of every defect filed against the strip (reader-B's elevation from cure to excellence, adopted).
3. **demo/color-session/color-chips/PreviewStrip.vue:11-26, :72-75** — a NAMED taste knob (`STRIP_SEGMENT_CAP = 7`), honest mask-fade truncation, and a written record of the rejected alternative. Exactly the discipline `WEIGHT_FLOOR` lacks: a constant whose name is true of its behaviour.
4. **demo/palettes/browser/card/ShadowPalette.vue:46-51** — the loading ghost reasons about legibility in both motion states and ships `gap-px` for the at-rest case: the imagined strip has a boundary law the real one lacks — the sharpest single evidence for PCS-5, found by neither D nor C.
5. **api/src/modules/palette/hash.ts:19-31** — a hash function that documents its own equivalence relation ("position is the atom KEY, not part of what the atom IS"). The server half is impeccable; that its client-side premise is unmet is PCS-11, not a blemish on this prose.
6. **demo/styles/foundation.css:636-700 + :793-841** — the two-tier, two-modality color-surface policy with a stated principle, a `:where()` specificity discipline, and an open membership hook. The architecture is right; only the membership list and the declared-once claim failed.
7. **demo/palettes/mix.ts:46-55** — the four-guard validation ladder with specific thrown messages: this repo already knows the correct idiom for exactly the strip's input, forty lines away.
8. **demo/palettes/browser/card/PaletteCard/PaletteCard.vue:13-18** — the S.W5-10/S-15-A interior-clip rendering law, hard-won and written down; its only defect is living in a consumer instead of the component (and this adjudication kills the claim that the comment is false).
9. **challenge-C-implementation.md §3** — fourteen hazard classes recorded CLEAN with evidence, including the load-bearing Vue 3.5.35 check; reachability labelled honestly at C-10/C-11. It is what keeps a DEFECTIVE verdict from reading as a shotgun.
10. **vite.config.ts's generated self-alias set** — the reason the runtime surface cannot drift, and (per my L-3 kill) the type half in fact arrives at the same artifact by self-name resolution: both halves agree today, by two different mechanisms.
11. **Restraint under DEFECTIVE**: both the D axis (6×76.33 vs 458px — "not a real defect") and the C axis (gaps [0,0,0,0] at two widths) measured the naive sub-pixel worry and declined to file it.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs — task #4 feed)

- **U-1** — C-2's 0px-tall vertical strip. Mechanism CONTESTED between the readers (explicit-height-defeats-stretch vs percentage-resolves-auto-so-stretch-applies); the axis measured a synthetic probe, not the real aside card. Decide on the real node before the dead branch deletes, so the deletion rationale is recorded true.
- **U-2** — WHCM + print rendered pixels of a collapsed palette card (shared with PC-7's residue; the tracked forced-colors matrix contains zero card captures — the coverage hole itself is recorded in challenge-D §0).
- **U-3** — corner concentricity pixel reads (~0.83px card / ~0.4px plate; shared with PC-29's residue). Static arithmetic certain; the crescent's visibility is the live question.
- **U-4** — D6's strip-share-of-host measurements (33.3% / 21.3% / ~13%).
- **U-5** — whether the Generate plate can transiently mount `palette: []` (the sole trigger that would revive D3's reachability; trace `useGeneratePalette`'s initial state and the count slider's minimum).
- **U-6** — C-8(3)'s invalid-css → transparent-segment CSSOM behaviour (certain from spec; unmeasured by this seat).
- **U-7** — the pasted live-DOM probe outputs in D1/C-1/C-6 (roster match false; 75/6.25×4; equal 16.6667×6). Each FOLLOWS from bytes this seat verified independently; the measurements themselves were not re-run (probe parsimony).
- **U-8** — the RTL x-flip measurement (mechanism certain; exposure nil while `dir="ltr"` is pinned).

## COORDINATION (cross-apotheosis corrections — recorded so no sibling record misleads a future seat)

1. **PaletteCard.md Superlative 1 is WRONG about the arithmetic it praises.** It lauds PaletteColorStrip.vue:50-71's renormalisation ("the step everyone forgets") and its parenthetical says X.W7.c "keeps the weighting and retires only the `Math.max(…, 0.5)` clip floor". This corpus proves, and this seat re-derived, that the renormalisation DESTROYS the documented floor (PCS-1). The dated record is not rewritten (E-1/E-3); this entry stands beside it, and any wave consuming PaletteCard.md's superlative list must read this correction first. This is a citation-inheritance hazard of exactly the ColorSpaceSelector-DISSENT-1 species: a praised cell repeated without re-derivation.
2. **The `[data-color-surface]` three-way**: GenerateControls.md R-C ruled it deletable (dead selector); PaletteCard.md R-2 flagged the collision; this record confirms zero consumers AND that it is the exact cure hook for PCS-2. X-W7.c must make ONE ruling (first consumer, or roster-class membership + deletion) — three adjudications now point at the same line of foundation.css.
3. **Named gaps for the formation**: (i) PCS-1's WEIGHT_FLOOR cure is in no X mechanism text — W7.c must adopt it explicitly; (ii) MixSourceSelector.vue is in no W7 file list while PCS-19's Mix limb and PCS-6's ancestor-clip deletion both require touching it; (iii) PCS-3 (schema `weight`), PCS-9 (area seam), PCS-11 (`position` contract) have NO wave owner and are the boundary rows the next formation must home.

## Verdict

**DEFECTIVE — adjudicated.** 31 rows confirmed (1 BLOCKER-class fold to PC-7 · 10 MAJOR · 12 MINOR · 8 INFO), 10 corpus claims killed, 12 axis misses adopted. The component's one documented invariant is arithmetically false on the shipped path and re-derived so by this seat; its only face is missing from both arms of the roster that exists to protect it; its weight contract dies at the wire; its `position` requirement exports an undocumented ambiguity into the server's identity hashes; and nothing in the repo would notice any of it. Twenty-two rows die in one build (X-W7.c's PaletteSpecimen); three contract rows await an owner at the next formation boundary. These dispositions are ADJUDICATED, not VERIFIED — X-W11 stamps, not this file.

— end of apotheosis. The challenger corpus and both reader returns remain immutable beside this file per E-1/E-3.
