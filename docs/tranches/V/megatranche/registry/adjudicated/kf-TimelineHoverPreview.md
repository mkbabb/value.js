claude-fable-5

# kf-TimelineHoverPreview — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Anchors**: keyframes.js worktree (the substrate of record — per banked kf-App ruling 1's witness law, `origin/master` or later; the local HEAD `8281638c` is a 41-commit-stale fork and is DISQUALIFIED as a witness substrate, re-confirmed by this seat's own `git rev-list --count HEAD..origin/master` → 41) · registry branch `tranche-u` (value.js) · installed producers: `@mkbabb/glass-ui` 7.0.0 dist · `@mkbabb/value.js` 4.0.0 · `reka-ui` dist · `tailwindcss` **4.3.0** (manifest ^4.3.0, installed 4.3.0 — this seat's read; version-exact match to the banked SwatchHoverMenu emission-order instrument).
**Corpus**: three axis challenges (D-1..D-17 · L-D1..L-D15 · C-1..C-10, all `claude-opus-5[1m]`) at `docs/tranches/V/megatranche/audit/kf-components/TimelineHoverPreview/`, re-read by two hostile re-readers — **RR-A** (design-and-usage) and **RR-B** (library-and-code) — both returns adjudicated here, attacked before either was adopted.
**Method (L-18/M-23)**: every verdict-level reader disagreement (the D-2 confirm-vs-kill, the ghost-`{}` frequency, the D-13 stacking arm, the C-5 / L-D7 / D-3 / D-6 rungs, RR-B's S-b vs RR-A's MISSED-2 collision, the MISSED-5 decidability dispute, the C-7 export status, the L-D2 proof status) was re-derived against the tree with this seat's own instruments. Product source READ-ONLY; **no browser tooling** (law); zero writes outside this file; the irreducibly visual residue rides SS-13.
**Arbiter's own instruments (all static reads + git; zero product writes)**: TimelineHoverPreview.vue read WHOLE (38 L, verbatim); TimelineTrack.vue :62-122 (the mount + marker), :145-200 (`getGhostStyle` :151-158, `onTrackPointerDown` :168-172, `onMarkerPointerDown` :192-196 — the D-2 decider's first hop); KeyframeTimeline.vue :80-134 (`@select` :85, the Label input :106, **CSSCodeEditor mount :123-127** — the D-2 decider) and :213-267 (`previewCache`/`previewLoading` reactive decls :217-218, `onDiamondHover` :220-233 with the guard/`finally` shape — the D-13 decider; `selectedKeyframeCSS` :239-244 — full-fidelity, every value un-truncated; `onKeyframeCSSChange` :246-265); snapshotCapture.ts :8-29 (the `""`/`none`/`auto` filter — the ghost-`{}` decider); timelineTypes.ts WHOLE (worktree carries `selector: KeyframeSelector` + the value.js `/css` type import; **`git show 8281638c:…/timelineTypes.ts` carries NEITHER** — the C-5 substrate decider); tsconfig.json :7-9 (`strict` / `noUncheckedIndexedAccess` **:8** / `exactOptionalPropertyTypes` **:9** — the S+4-kill decider, and the citation correction to L-D2's ":9-10"); useTimelineBuild.ts :85-117 (`scrubAndCapture`: per-call `prevT`, `catch { return null }` — confirming the banked KEEP-catch correction and the L-5 fold); src/animation/compile/adapter.ts :179-196 (`formatSelectorPercent` emits named selectors as literal text under the "surface them as their literal name" comment — the C-5 reachability decider); demo/utils/keyframeSelector.ts WHOLE (`selectorText` :7-12 unused here; `selectorPercent`→`namedSelectorToFraction`); src/animation/internal/scroll-phases.ts :11-16 (entry 0–0.25 / cover 0.25–0.75 / contain 0.375–0.625 / exit 0.75–1 — the collision arithmetic re-derived: `entry 100%`=`cover 0%`=0.25→"25%"; `contain 0%`=`cover 25%`=0.375→`Math.round(37.5)`=38); **reka** `Tooltip/TooltipContentImpl.js` :80-140 read verbatim — `const ariaLabel = computed(() => props.ariaLabel || currentElement.value?.textContent)` at :87, the `VisuallyHidden {id: contentId, role:"tooltip"}` rendering `toDisplayString(ariaLabel.value)`, the `onMounted` scroll-close firing only when `target?.contains(trigger)` (the wheel-does-not-dismiss decider), `--reka-tooltip-content-available-height` published on the content style — the MISSED-1 decider, whole; **installed glass 7.0.0**: `dist/styles/fonts.css` @font-face enumeration → **exactly 4 faces, ALL `font-style: normal`** (Jakarta 200-800 ×2, Fira Code 300-700 ×2; `grep -c "font-style: italic"` → **0** — the MISSED-2 decider), `typography/scale.css` (`--type-admin-label: 0.625rem`; `--type-small: clamp(0.875rem, 0.8rem + 0.25vw, 1.25rem)`), `tokens/scheme-motion.css` (`--type-leading-small: 1.4`; `--type-tracking-caps: 0.1em`), `typography/utilities.css` `@utility text-mono-small` (mono family + `--type-small` + `--type-leading-small`, **no text-transform** — the MISSED-3 arithmetic re-derived: 19.6→28 px rows in the 96 px box = 4.9→3.4 visible vs today's ~9), `dist/components/timeline/index.d.ts` = **two lines** (`GlassTimeline` + 3 types; ContinuousMarkers NOT exported — the C-7 decider), `dist/tooltip-OxciiZm6.js` `overflow-hidden` present (the L-D10 decider); demo/styles/style.css :90-100 (`:root { … font-synthesis: none; }` OUTSIDE @layer, rationale comment verbatim); demo/scenes/spring/StartingStyleTarget.vue **:73** verbatim (`text-mono-small capitalize` + `data-register="code"` — the D-1 sub-claim decider); demo-wide `italic` census on .vue class attributes → exactly **2** (KeyframeCard.vue:29, THP.vue:24); font-roles.json parsed by this seat — the 15 `monoAllowedSelectors` verbatim + `_monoContract` T.D4 text; **git**: `origin/master:package.json:77` `"@mkbabb/glass-ui": "7.0.0"`, worktree diff removing HEAD's 6.0.0 optionalDep (the L-D11-kill decider, folding to banked kf-App ruling 1 / census SCH-1).

## Scoreboard

**26 ADJUDICATED-CONFIRMED rows booked here (0 BLOCKER · 4 MAJOR · 15 MINOR · 7 INFO) · 13 whole-row/partial-arm FOLDS by reference to banked kf-KeyframeTimeline / kf-App.skeleton / kf-CSSCodeEditor / census identities + 1 killed-identity fold (SCH-1) — identity guard, zero re-bookings · 10 KILLED with re-executed refutations (one BLOCKER demotion-to-kill, one axis-superlative kill, one reader-superlative arm killed by the opposing reader and settled here, one "undecidable" claim decided by a banked instrument) · 8 net-new missed rows adjudicated IN · 11 UNPROVEN-NEEDS-LIVE residue rows to SS-13.**

Zero BLOCKERs. Both axis-filed BLOCKER rungs die: D-1's folds to the banked M7 MAJOR (the program's BLOCKER law), and D-2's is KILLED outright on its own stated falsifier — the full-fidelity, copyable recovery path ships one selection away in the same cluster. The heaviest local truth is a pairing no single row states: **the component's only textual ground truth lies three ways at once** (uppercased by the banked M7 identity, truncated without in-tooltip recovery, and announced to AT as a frozen unpunctuated run-on that excludes the image's celebrated alt — MISSED-1), while **the ghost fallback misrepresents four ways** (invisible at `opacity: 0`, silently clipped on translate, frame-shrunk to 19.2 px when a transform survives, and plate-overridden to a 1 px hairline on transparent targets). The instrument's inspector is the cluster's least honest surface.

Status verbs per M-25: dispositions are **ADJUDICATED**, never VERIFIED — X·KF specs are forming; routings are agglomeration input. Wave targets from the census KF.W0–W10 taxonomy. No row here is NO-WAVE-OWNER: this leaf is inside the census-named subject of **KF.W7 · Timeline Evaluate**, with typography/token rows to KF.W6, the skeleton fold to KF.W5, the type-gate arm to KF.W4 by fold, and the visual horizon to KF.W9/SS-13.

## Ruled disagreements (the seat's actual work)

| id | RR-A (design-and-usage) | RR-B (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-2 / L-D9** | CONFIRMED MAJOR ("visual data loss", BLOCKER demoted) | KILLED as filed; survives narrowed MINOR | **KILL ADOPTED — RR-B wins; survives MINOR, narrowed** | The axis's own falsifier ("killed only by finding a title/tooltip/expand/copy path in the tree") is satisfied by MY reads: `onMarkerPointerDown` :192-196 emits `select` → `selectedKeyframeId` (:85) → CSSCodeEditor mounts at :123-127 with `selectedKeyframeCSS` (:239-244) — every value in full, in Monaco at 250 px, wrapped/selectable/copyable. No data is lost and no capability is absent (the program's BLOCKER law). RR-A confirmed the row without ever engaging the recovery path. The ~29-chars-per-row geometry survives as the MINOR's substance. |
| **L-D4 / C-4(b) / D-7(3) frequency** | CONFIRMED MAJOR (the narrowed `{}` case) | Frequency KILLED: structurally impossible on the capture path | **RR-B wins — narrowed to the authored-vars path, MINOR** | My snapshotCapture :16-21 + getGhostStyle :151-158 reads: `getPropertyValue("opacity")` is always a non-empty string ("1" at rest) and `background-color` always resolves (`rgba(0, 0, 0, 0)` when transparent); neither is ever `""`/`none`/`auto`; both are in the 17-set and both are ghost keys — so every snapshot-produced keyframe carries ≥2 ghostable keys and `{}` is unreachable there. Reachable ONLY via `importCSSToTimeline`/the typed editor. Both axes' concrete scenarios say "captured" and are dead as stated. Corroborates banked kf-KeyframeTimeline K-7 and supplies the mechanism that seat did not state. |
| **D-13 stacking arm** | Confirmed with the cached-image+Capturing stack | Arm KILLED: unreachable | **RR-B wins — arm killed** | My :220-233 read: the guard returns early on `previewCache[kf.id] || previewLoading[kf.id]`, so loading can only be true while the cache is falsy; the cache write (:226) and `previewLoading[kf.id] = false` (:231 `finally`) execute in the same synchronous tail after the await — Vue flushes both together; image-present + loading-true never renders for one keyframe. The live-region arm folds to banked D-22; the residual (placement + ASCII `...`) books INFO. |
| **C-5 rung** | MAJOR ("C-5's rung is right; L-D13's self-demotion void") | MINOR | **MINOR — RR-B wins, on the banked ladder** | Reachability is SETTLED in C-5's favour by my own adapter.ts :179-196 read (named selectors emitted as literal text; L-D13's stated falsifier is dead), and the collision arithmetic re-derives exactly (25/25; 38/38 with a 0.5 pp mis-statement). But banked **N-6** — two keyframes silently MERGING in the built animation AND the exported CSS — sits at MINOR; a display-only collision cannot out-rank an output-corrupting one without inverting the ladder. Substrate note: the row is VACUOUS at the disqualified stale HEAD (`git show` — no `selector` field) and REAL at the worktree/origin-master substrate of record. RR-A's MAJOR preserved in DISSENT with its reopening condition. |
| **L-D7 rung** | MAJOR | MINOR ("no runnable gate → contract debt") | **MAJOR — RR-A wins, on the banked family rung** | The identity family is booked per-component at MAJOR twice: kf-MbabbMenu **MM-9** ("ADMITTED (MM-9, MAJOR) — the manifest is explicit and the gate is prose-only") and kf-KeyboardShortcutsModal **R-7** (MAJOR; the dead `proof:font-census` witness carries R-7's NO-WAVE-OWNER rider). RR-B's demotion logic — gate-absence softens the breach — was already rejected by MM-9's own ruling text. My font-roles.json parse confirms the 15 selectors and the sibling-not-descendant geometry (:17 vs :20 are both children of :2). |
| **D-3 / L-D6 / C-6 rung** | MAJOR | MINOR | **MINOR — RR-B wins** | The overflow mechanism is confirmed (10 px rows, 96 px box, ~10-11 surviving rows) and SHARPENED by my own TooltipContentImpl read: the scroll-close listener fires only when `target?.contains(trigger)`, so wheeling the portalled list does NOT dismiss — the pointer path is fully functional. The hidden rows are keyboard-unreachable, but the same data is fully readable in the Monaco surface (the D-2 decider), so the marginal harm is the last rows of a list with a complete elsewhere-home. The keyboard-modality compound (selection mutates — banked M3) is the reopening condition, preserved in DISSENT. |
| **D-6 rung** | MINOR (per the banked M9 rung) | MAJOR label on the fold | **MINOR — a fold keeps the banked rung** | D-6 ≡ banked kf-KeyframeTimeline **M9**, booked MINOR. RR-B correctly folds the identity and incorrectly re-rungs it; an identity fold cannot carry a different severity than its bank without re-adjudicating the bank, which nothing in this corpus does. |
| **MISSED-2 vs RR-B's S-b** | Filed MISSED-2 (italic paints upright) | S-b credits "differentiation is italic … verified" | **MISSED-2 wins — settled on this seat's own bytes** | My fonts.css enumeration: exactly 4 @font-face blocks, all `font-style: normal`, zero italic faces for any family; demo `:root { font-synthesis: none }` outside @layer (my style.css :90-100 read); per CSS Fonts 4 §5 the matched family is not skipped for a missing style and synthesis is forbidden — `italic` at :24 paints upright. The exact chain is BANKED at kf-EditorStartScreen **KF-EST-2** (where it REVERSED a kill). RR-B's S-b arm dies; the exhaustiveness arm of the same superlative survives. A reader-vs-reader collision settled by a third instrument. |
| **MISSED-5 decidability** | "which padding paints is not source-decidable" | Winner decided; FOLDS to banked TEP-12/MCP-31 family | **RR-B wins — decided, by a banked instrument that is version-exact here** | The banked SwatchHoverMenu seat EXECUTED glass's own merger (`p-`/`px-`/`py-` = three independent conflict groups — nothing displaced) and read tailwindcss **4.3.0** registration order `p`@106031 < `px`@106047 < `py`@106071 — the longhands emit later and win. keyframes.js installs tailwindcss **4.3.0 exactly** (my read), so the instrument transfers without a version caveat: glass's 8/10.18 px overlay padding paints; the consumer's `p-2` at TimelineTrack:86 is DEAD. The ambiguity-at-the-call-site observation survives as an INFO rider; D-11/D-12/D-2's ~206 px content-width geometry is FIRMED. |
| **C-7 export status** | ContinuousMarkers NOT exported; seam via GlassTimeline's forwarded slot | Cites ContinuousMarkers.vue.d.ts:31 | **RR-A's correction confirmed; no substantive conflict** | My read: `dist/components/timeline/index.d.ts` is two lines — `GlassTimeline` + 3 types. The d.ts FILE exists in dist (RR-B's cite) but is not re-exported (RR-A's point); the seam is reachable only through GlassTimeline's forwarded `popoverContent`. Folds to banked C-15 unchanged. |
| **L-D2 proof status** | UNPROVEN (Vue-aware widening undecidable) | CONFIRMED at the flags, demoted MINOR | **CONFIRMED-AT-THE-FLAGS, MINOR — RR-B's booking adopted, RR-A's caveat attached** | My tsconfig read: `noUncheckedIndexedAccess` at **:8**, `exactOptionalPropertyTypes` at **:9** (correcting L-D2's ":9-10"). At the flags the declaration does not admit an explicitly-present `undefined` and the sole call site passes exactly that. Whether a Vue-aware checker would FIRE it is undecidable in a tree with no vue-tsc (the KF-CE-16 fold) — that question rides the KF.W4 gate, not argument. Zero runtime consequence; one-token cure. |
| **D-5 rung** | CONFIRMED MAJOR (with recomputation + sharpening) | UNPROVEN-NEEDS-LIVE | **MINOR booked + SS-13 — a third position, on the banked ladder** | The ordering argument is structural (a 70 %-alpha ink is pulled toward the plate and cannot out-rank an opaque promoted ink), RR-A's re-derivation agreement is exceptional (light 6.05 vs the axis's 6.06; dark 7.22 EXACT — the KF-SKEL-22 discipline, passed), and D-5 explicitly refuses a WCAG claim (all four ratios clear AA). A hierarchy-only defect books at the banked **M10** rung — MINOR — not the axis's MAJOR. RR-A's genuine sharpening is adopted into the row: on the `@container style(--glass-backdrop: light)` and `contrast-color()` arms the LIGHT column inverts too, so the inversion is not dark-only; which arm is live is runtime luma → SS-13. |

## Adjudicated defect roster — 26 rows booked

Ids are the corpus's own, for life; net-new rows keep their finder's designation. Line anchors are the worktree (= origin/master shape for every file this seat opened); re-resolve before any cure (D-19). Every routing is M-25 agglomeration input for the forming X·KF specs.

### MAJOR (4)

- **MISSED-1 (RR-A net-new; decider re-executed by this seat) — the accessible description reka builds for this tooltip EXCLUDES the alt all three axes filed as their headline superlative, and what it announces is an unpunctuated run-on frozen at first mount.** My verbatim TooltipContentImpl.js read: `:87` `computed(() => props.ariaLabel || currentElement.value?.textContent)`; the VisuallyHidden carrying `{id: contentId, role: "tooltip"}` renders only that string — it is what the trigger's `aria-describedby` resolves to. (1) `Node.textContent` excludes `<img alt>`: THP.vue:8 contributes NOTHING to the announcement. (2) Block `<div>` boundaries contribute no separator: the announced string is caption + every declaration row concatenated. (3) `.textContent` is an untracked DOM read — the computed captures once after mount and never re-derives on the ghost→image or Capturing→settled swap. COMPOUNDING: on the focus path the `<img>` never renders at all (banked D-10 fold) and the ghost div has no text/role/name — an AT user's ENTIRE preview is this one frozen run-on. The published escape hatch exists: `TooltipContent` accepts `ariaLabel` (`:16`), unpassed at TimelineTrack:86. Registry-swept (RR-A) — not banked anywhere. **→ KF.W7** (pass a composed `ariaLabel` at the mount; joint with the D-10 fold — the focus path's cure and this row's cure are one seam); per-AT utterance → SS-13.
- **D-7 (re-scoped by this adjudication; cluster head of the ghost-fidelity family) — the ghost fallback misrepresents in the cases that matter most**: (1) `opacity` copied straight through (TimelineTrack:154; "0" survives the capture filter as a truthy string — my read), so the world's most common authored keyframe renders a completely invisible 64 px box that still occupies the guard (`Object.keys > 0` true) and the layout; (2) `scale(0.3) ${computed matrix}` (:155) applies the translate inside the scaled frame — a 300 px authored translate displaces the 64 px swatch 90 px, **silently clipped** by the tooltip's own `overflow-hidden` (the escape arm is killed; my dist grep) — a wrong-but-invisible preview, not a spill; (3) the `{}`-nothing arm survives only at the narrowed authored-vars scope (ruled table). Family siblings booked below: MISSED-4 (the frame itself shrinks) and GHOST-PLATE (the plate dies). value.js ships `decompose.ts` for exactly the scale-only treatment (axis-L's cure note, adopted as spec input). **→ KF.W7**.
- **D-4 — the component overrides the tooltip's own declared body size DOWNWARD to a fixed 10 px at `line-height: 1` for a ~10-row scrolling list**: the surface declares `--tooltip-text: var(--type-caption)` = clamp 12→16 px (offsets.css + scale.css, producer verbatim); `text-admin-label` forces 0.625 rem fixed, zero leading, uppercase, 0.1 em tracking — −16.7 % at the clamp floor, −37.5 % at the ceiling, and the gap widens on large viewports because the override is not fluid. The prescribed cure is real (`text-mono-small`, non-uppercasing — my utilities.css read) **but collides with D-3: see MISSED-3, booked so no wave ships the swap without resizing the box in the same commit.** **→ KF.W6** (one typography cure family with the banked M7 fold + L-D7 + MISSED-2/-3).
- **L-D7 — `"Capturing..."` at :17 is a demo-authored mono UI label, a ruled T.D4 breach, and a SIBLING of the `data-register="code"` div so no allowlist selector reaches it** (my font-roles.json parse: 15 selectors; `:17` matches none; `[data-register='code'] *` cannot reach a sibling; `_monoContract` closes "A new demo-authored mono UI label reds the census"). Family rung per banked MM-9 + R-7 (ruled table). The dead-witness datum (`proof:font-census` unwired; the census probe cannot sample a closed tooltip) rides R-7's NO-WAVE-OWNER rider and the KF.W4 gates row — NOT re-booked here. **→ KF.W6** (voice fix: a text-face status line, or the Skeleton fold's cure absorbs the string entirely).

### MINOR (15)

- **D-2/L-D9 (narrowed per the ruled table) — in-tooltip truncation with no `title`, no wrap, no copy affordance**, at ~29 chars/row (206 px content ÷ ~7 px/char at the 10 px rung + caps tracking) against 35-char background-color rows and 80+-char matrices; the full-fidelity home one selection away is the reason this is MINOR, not the reason it is nothing — the hover surface exists to spare that click. **→ KF.W7** (`:title` or wrap; joint with D-4's re-rung).
- **D-3/L-D6/C-6 (ruled MINOR) — a `max-h-24 overflow-y-auto` scroller inside portalled `role="tooltip"`**: overflow ordinary (~10-11 surviving rows vs 9 visible), keyboard-unreachable, APG-hostile either horn; wheel does NOT dismiss (my scroll-close read). **→ KF.W7**; the Chrome ≥127 focusable-scroller wording → SS-13.
- **C-5 (ruled MINOR; reachability settled by this seat) — the `KeyframeSelector` discriminant is dropped at :3/:8**: named scroll phases display a percent the author never wrote; `entry 100%` and `cover 0%` both caption "25%"; `contain 0%`/`cover 25%` both caption "38%" (mis-stating position by 0.5 pp); `selectorText` sits one call away, already consumed by timelineEngine. Substrate: real at origin/master, vacuous at the disqualified stale HEAD. **→ KF.W7** (render the authored form, resolved percent secondary).
- **L-D4/C-4(b) (narrowed) — the `// KEEP:` comment's documented fallback is false on the authored-vars path**: an imported/editor-authored keyframe with no ghostable property renders NOTHING with no explanation, beside a file that renders an explicit empty state for the vars list 8 lines away. Capture-path claims dead (ruled table). Corroborates banked K-7. **→ KF.W7** (a terminal `v-else` state).
- **L-D2 (ruled) — `previewSrc?: string`/`loading?: boolean` do not admit the explicitly-present `undefined` the only call site passes**, under `exactOptionalPropertyTypes` (:9). One-token cure; fire-question rides the KF.W4 gate. This row is the banked KF-CE-16 fold's cleanest witness. **→ KF.W4 rider** (booked here as the witness; the gate is the fold's).
- **L-D8/C-4(a) — `ghostStyle` is a required prop that is a pure function of prop #1**, derived in a geometry component with no other use for it, required while its two siblings are optional. The withdrawn perf arm stays withdrawn (the slot is `withCtx`-lazy — axis-L's own graveyard, re-verified by both readers). **→ KF.W7** (derive locally; delete the prop).
- **D-5 (ruled MINOR + SS-13) — the authored prop/value two-tone is perceptually null in light and INVERTED in dark** because `.glass-floating` promotes `--muted-foreground`; the distinction is carried entirely by the literal `:`; RR-A's sharpening adopted (the light column inverts too on the `@container`/`contrast-color` arms — the inversion is not dark-only). No WCAG breach claimed; glass credited for the promotion that prevents one. **→ KF.W6**; painted ratios + which-arm-is-live → SS-13.
- **D-11 — one undifferentiated 6 px gap for a four-tier stack, tighter than the frame that holds it** — geometry now FIRM (the p-2 question is decided: glass's 8/10.18 px φ-stepped padding paints). **→ KF.W6**.
- **D-12 — the alignment axis changes mid-stack with no rule** (`items-center` vs the full-width left code block); the 144 px media narrower than its 206 px annotation, ~31 px dead gutters. **→ KF.W7**; exact gutter → SS-13.
- **D-15 — no height cap or aspect-ratio on the capture while the cheap element beside it IS capped**; reka publishes `--reka-tooltip-content-available-height` on the content style (my read) and glass's TooltipContent consumes none of it. **→ KF.W7**; tall-capture consequence → SS-13.
- **C-9 — zero test coverage**: no test names this SFC or either cache prop (both readers' greps; timeline-undo.test.ts exercises the same state without this surface). **→ KF.W7** (locks the cures above).
- **MISSED-2 (RR-A net-new; decider re-executed by this seat) — `italic` at :24 paints UPRIGHT**: zero italic faces in the installed producer (my 4-face enumeration), demo `font-synthesis: none` at `:root`, so the empty state's ONLY differentiator is dead — NO PROPERTIES renders 10 px uppercase 0.1 em-tracked, indistinguishable from the declaration rows it stands in for. Banked mechanism (KF-EST-2), new face + new site; `italic` exists on exactly 2 demo elements. Kills RR-B's S-b arm (ruled table) and refutes axis-D's S-2 differentiator clause at its stated ground. **→ KF.W6**; screenshot → SS-13.
- **MISSED-3 (RR-A net-new; arithmetic re-derived by this seat) — the corpus's two headline cures collide**: swapping to `text-mono-small` makes rows 19.6→28 px (`--type-small` 14→20 px × leading 1.4 — my token reads), cutting the 96 px scroller from ~9 visible rows to 4.9→3.4 — D-4's cure materially worsens D-3, and the two rows are adjacent in the same axis document. The banked N-10 cure-collision law's typography twin. A repair must resize or drop `max-h-24` in the same commit. **→ KF.W6/W7 spec input** (booked so no wave ships the regression).
- **MISSED-4 (RR-A net-new) — `scale(0.3)` shrinks the ghost's own FRAME, not just its payload**: the transform is written onto the bordered element itself (:155 → :12-16), so when a transform was captured the swatch renders 19.2 px with a 0.3 px border at 30 % alpha and 1.2 px radius — a FOURTH envelope on top of the banked M9 three — while the layout slot stays 64 px (transforms do not affect layout), and the border goes sub-pixel exactly when a near-transparent background makes it the only legibility aid. Both axes read the line; neither noticed the frame scales with the content. **→ KF.W7** (decompose; apply scale to a wrapper, not the plate).
- **GHOST-PLATE (RR-B net-new) — the ghost's neutral plate is dead on every capture-path keyframe and its ground is painted transparent by the very value it displays**: `:style` always carries `backgroundColor` on the capture path (my snapshotCapture + getGhostStyle reads — `background-color` never filters out), and inline style beats the `bg-muted/30` utility at :14 unconditionally; for a transparent target the inline value is `rgba(0, 0, 0, 0)` and the entire branch is a 1 px 30 %-alpha hairline on a glass plate — the branch the keyboard path ALWAYS receives (D-10 fold), strictly worse than the opacity-0 arm because it needs no authored keyframe. Registry-swept (RR-B): not banked. **→ KF.W7** (the ghost redesign owns it with D-7/MISSED-4).

### INFO (7)

- **D-16 — no `dir="ltr"` on the code register**; latent (demo-wide RTL grep 0); promotion condition correctly stated by the axis. **→ KF.W9 record**.
- **D-17 — three unnamed alpha one-offs** (`/30` ×2, `/70`) where design-idioms.css is an owned, documented vocabulary; the `/70` is the one that turned out load-bearing and wrong (D-5). **→ KF.W6**.
- **L-D14 — `Math.round(keyframe.percent)` duplicated at :3 and :8** — noting the corpus tension: the same duplication is celebrated by three superlatives as drift-proof single-sourcing; it is one expression written twice, not one source read twice. **→ KF.W7 hygiene**.
- **L-D15 — no `@error` on the `<img>`**; once `previewSrc` is truthy the ghost branch is dead for that render; a decode failure paints the broken-image glyph with no fallback. **→ KF.W7**; frequency → SS-13.
- **MISSED-6 (RR-A net-new) — `overflow-y-auto` alone coerces :20 into a scroll container on BOTH axes** (CSS Overflow 3), benign today (`truncate` clips x) but the mechanism behind D-3's focusable-scroller horn — and the file silently disagrees with the mixed-pair discipline its own cluster banks as a superlative (kf-KeyframeTimeline S-3, one file up). **→ KF.W7 note**.
- **REGISTER-LAUNDER (RR-B net-new) — the T.D4 descendant wildcard launders UI prose**: `:24`'s "No properties" is census-legal purely by descent from `[data-register='code']` while `_monoContract`'s own clause forbids exactly this content — the gate CAN see this selector and still admits what the contract bans; the mirror of C-S+1's "the census gate cannot see text-transform". **→ KF.W4** census-gate design input (rides R-7's witness rider; not a re-book of L-D7).
- **P2-DEAD (resolved form of RR-A's MISSED-5) — `class="p-2"` at TimelineTrack:86 is dead weight**: glass's own merger buckets `p-`/`px-`/`py-` as three groups and tailwind 4.3.0 emits the longhands later (banked TEP-12/MCP-31/SwatchHoverMenu instrument, version-exact here) — the author's padding never paints. The producer cn-group-table seam itself is BANKED and already on the glass-ui BH relay; not re-filed. **→ KF.W7 hygiene** (delete the token).

## FOLDS — identity guard (anti-rename; zero re-bookings)

- **D-1/C-1 (uppercase + caps tracking on the CSS-literal dump) ≡ banked kf-KeyframeTimeline M7** (MAJOR, → KF.W7; the `data-register` falsifier run by that seat). Both readers agree; both BLOCKER rungs die on the fold. This corpus CONTRIBUTES: the census corrections (15 `text-admin-label` occurrences / 9 files, not 16; THP:20 the sole co-occurrence with `data-register="code"`; 8 such .vue sites; 5 `normal-case` sites, not 3), the StartingStyleTarget sub-claim kill, and two cure guards — `normal-case` must pair `tracking-normal` (banked MM-29) and the `text-mono-small` swap must resize the box (MISSED-3).
- **D-14 / C-1's rider (`font-mono` dead weight, 4-site idiom) ≡ banked D-18** (MINOR, KF.W6 — the bank names THP:20 explicitly).
- **D-6 (three envelopes, no reserved box, popper jump) ≡ banked M9** (MINOR — the fold keeps the banked rung; ruled table). The decode-reflow arm rides M9's residue.
- **D-8 / L-D1 / C-3 (cache never invalidated; retry-forever; stale PNG beside live vars) ≡ banked D-4/L-4/C-5** (MAJOR, KF.W7). Contributions: RR-B's provenance correction (one of L-D1's three mutation sites, `updateKeyframeProperty`, is DEAD — zero callers, itself banked as L-13; the live routes are `moveKeyframe` + `onKeyframeCSSChange`) and my scrubAndCapture read re-confirming the banked KEEP-catch mechanism (`catch { return null }` — only `toDataURL`'s SecurityError reaches :228).
- **L-D5 (concurrent scrub/restore crossing) ≡ banked L-5** (MAJOR, KF.W7). Citation corrections recorded: the emit is TimelineTrack:83 (not :84); the function spans useTimelineBuild:89-117.
- **D-9 / C-2 (capture armed on mouseenter; tooltip opens on focus) ≡ banked D-10** (MAJOR, KF.W7). C-2's contract sharpening — the leaf declares no emits and cannot request its own content — rides as the cure's spec input, joint with MISSED-1.
- **D-10 (forced-colors: the demo-authored swatch unhandled; glass handles the surface) ≡ banked D-21** (MINOR, KF.W9).
- **C-7 (the glass hover-payload seam; segment geometry not adoptable) ≡ banked C-15** (MINOR, KF.W7 evaluation input) — with RR-A's export-status correction adopted (my two-line index.d.ts read; HoverPopover deck-private).
- **C-8's Skeleton half ≡ banked kf-App.skeleton KF-SKEL-16** (KF.W5 — the bank cites THP:17-18 by name); the geometry-jump half ≡ M9.
- **L-D12 / C-10 (easing dead; label write-only) ≡ banked N-2 (MAJOR) + N-9 (MINOR)** (KF.W7). C-10's sharpening — the label is USER-AUTHORED and the panel that exists to identify the keyframe shows a number instead of the typed name — is recorded as the strongest statement of the banked row, agglomeration color for the W7 spec.
- **L-D3 (no .vue file typechecked anywhere) ≡ banked kf-CSSCodeEditor KF-CE-16** (KF.W4, "the sequencing head of the whole record"). Filing it as a component MAJOR would be a re-booking; the component-specific contribution is that L-D2 is its witness.
- **D-13's live-region arm ≡ banked D-22** (MINOR, KF.W7 — the bank names `Capturing...`); the stacking arm is KILLED (ruled table); the residual placement + ASCII-ellipsis note is absorbed into the KF.W7 status-line cure with L-D7.
- **MISSED-5's producer seam (glass cn padding groups) ≡ banked TEP-12 / MCP-31 / SwatchHoverMenu instrument family** (glass-ui BH relay, standing) — winner decided, demo residue booked as P2-DEAD above.
- **L-D11 (phantom dep) → KILLED; identity ≡ census SCH-1 / banked kf-App ruling 1** (see the killed register).

## Killed-claims register — 10 rows, refutations re-executed

1. **K-1 · L-D11 (+ the F-1 inheritance in D §0 and C §4's "nothing here is reproducible on a clean npm ci")** — KILLED on baseline law: `origin/master:package.json:77` declares `"@mkbabb/glass-ui": "7.0.0"` as an exact devDep matching the installed producer (my git read); only the uncommitted worktree strips it; HEAD carried 6.0.0. Identity ≡ census SCH-1 / kf-App ruling 1 — banked, never re-booked. The three typography facts inside L-D11 survive; the dependency claim and its every downstream caveat die with it.
2. **K-2 · D-2's BLOCKER ("no recovery path whatsoever")** — KILLED on the axis's own falsifier by my reads of the selection chain (:192-196 → :85 → :123-127) and `selectedKeyframeCSS` (:239-244): a full-fidelity, wrapped, copyable Monaco surface one click from the tooltip. Survives narrowed MINOR.
3. **K-3 · L-D10's escape arm ("nothing to clip the escapee")** — KILLED: TooltipContent itself carries `overflow-hidden` (my dist grep); a statically-positioned transformed descendant is clipped by an overflow-hidden ancestor. The corpus contained its own refutation in axis-D's Not-Claimed block and no seat connected them until the re-read. The inner arm survives inside D-7 as a silent clip.
4. **K-4 · L-D4/C-4/D-7(3)'s frequency ("entirely ordinary … captured")** — KILLED structurally: `opacity` and `background-color` ALWAYS survive the capture filter (my snapshotCapture + getGhostStyle reads), so `{}` is unreachable on the capture path. Survives only at the authored-vars scope. Corroborates banked K-7 with the mechanism that seat lacked.
5. **K-5 · C's superlative S+4 ("the optional-prop typing is earned … exactly correct")** — KILLED: the certification reads tsconfig:8 and stops on the line that reverses it — `exactOptionalPropertyTypes` at :9 (my read). S+4 and L-D2 are mutually exclusive claims about two adjacent flags, an intra-corpus C-vs-L contradiction neither axis flagged; the tsconfig decides for L-D2.
6. **K-6 · D-13's stacking arm ("a finished preview with Capturing… underneath")** — KILLED as unreachable (ruled table; my :220-233 read).
7. **K-7 · D-1's scope sub-claim ("the other mono/data leaves use non-uppercasing rungs — StartingStyleTarget.vue:73")** — KILLED as stated: :73 composes `capitalize` onto a `data-register="code"` leaf (my verbatim read) — the rung is innocent, the call site is not, and the "this file is the only one" contrast fails in the form stated. D-1's headline is unaffected (it folds to M7 regardless).
8. **K-8 · C-1's second prescribed cure (`text-admin-label normal-case`)** — KILLED as structurally unsound: `normal-case` cancels `text-transform` only; `--type-tracking-caps: 0.1em` rides onto the literal — the exact composition banked as kf-MbabbMenu MM-29. Any cure pairs `normal-case` with `tracking-normal`, or takes the `text-mono-small` route with MISSED-3's box-resize guard.
9. **K-9 · RR-B's superlative S-b, italic arm ("the differentiation is italic … verified")** — KILLED on this seat's own font-face enumeration + the demo's `font-synthesis: none` (ruled table; the banked KF-EST-2 chain). A reader-vs-reader collision settled by a third instrument; the exhaustiveness arm of the same superlative survives untouched.
10. **K-10 · MISSED-5's decidability arm ("which one paints is not source-decidable")** — KILLED by a banked instrument that is version-exact for this tree (tailwindcss 4.3.0 registration order; the executed glass merger): the longhands win, `p-2` is dead. The observation that the call site cannot SHOW this survives as the P2-DEAD INFO row.

**Citation corrections (conclusions unaffected; these documents present as provenance-exact)**: L-D2's ":9-10" → tsconfig :8-9 (my read); L-D5's ":84" → :83, span :89-117; D-1's "16 call-sites" → 15 across 9 files; C-1's "three sites compose normal-case" → 5; RR-B's D-6 severity label vs the banked M9 rung (ruled); C-7's ContinuousMarkers framing (ruled).

## Superlatives — provenance preserved (L-18 runs both ways)

- **S-1 · the derived alt** (:8) — all three axes independently, both readers, re-verified here: accessible name and visible caption computed from ONE expression; cannot desynchronise under refactor. **BOUNDED HARD by MISSED-1**: the string the corpus celebrates loudest is excluded from the tooltip's own announced description — the aria-describedby mechanism never speaks it. The craft is real; the celebrated consequence is smaller than every prior seat believed.
- **S-2 · the exhaustive empty state** (:24) — guarded on the exact complement of the `v-for`'s expression; can neither double-render nor gap; reachable; honestly worded. The italic-differentiator clause is DEAD (MISSED-2, K-9); the exhaustiveness stands.
- **S-3/S+1 · the percent readout** (:3) — `text-mono-caption font-semibold tabular-nums`: pre-authorised by `_monoContract` clause (b) + the `[class*='tabular-nums']` selector; tabular figures are load-bearing in the shrink-to-fit envelope; and `font-semibold` (600) is a REAL instance — Fira Code ships `300 700` variable (my read) under the same `font-synthesis: none` that kills MISSED-2's italic. The author reached for the one pre-authorised mono rung at a weight the face actually ships.
- **S+3/S-d · the cluster's only clean external boundary** — zero `@mkbabb` imports in the SFC; the entire closure is one erased `import type`, one hop, honouring the published surface in a cluster banked for reaching into library-private `src/` (C-12/KF-CE-12 family) — and structural immunity to the R1 crash class by construction.
- **L-S1/S-c · zero teardown surface** — one type import, one `defineProps`; no lifecycle, watch, refs, listeners, timers, or imperative DOM — in the ONE component of the cluster that mounts/unmounts per tooltip open. The contrast is provenance-backed: the composable one layer up has no unmount guard on an in-flight capture (banked L-20/L-21). The leaf is clean; its owner is not.
- **D-S4 · zero authored motion, zero authored CSS** — a PRM claim it never has to honour, in a cluster whose parent had to delete a hand-rolled PRM-less transition and whose replacement is itself banked-dead (D-1/L-1/C-3 at the bank). This component never made the mistake, so it never inherited the bad repair either.
- **S-5 + C-S+1 · the `data-register="code"` discipline and its limit** — one of exactly 8 such .vue sites, on a genuine code leaf, self-documented as T.D4 clause (c) requires; the census would read it legal and it IS legal — and C-S+1 states the resulting tension better than any sentence in the corpus: the SELECTOR contract is satisfied while the REGISTER SEMANTICS it encodes is violated by the inherited uppercase; the census gate cannot see `text-transform`. That sentence, plus RR-B's REGISTER-LAUNDER mirror (a selector the gate CAN see admitting content the contract forbids), is the KF.W4 census-gate design brief in two lines.
- **Corpus method, three instances** — axis-L's §5 Non-claims register (three candidates published WITH their refutations; all three kills re-ran clean through both readers, and it stopped RR-B from re-manufacturing the slot-laziness hypothesis); axis-D's Not-Claimed block (which contained the refutation of axis-L's L-D10 escape arm — written the same week, in the same folder, unconnected until the re-read); and RR-A's D-5 recomputation agreeing with the axis to 0.01 in one cell and exactly in the other (the KF-SKEL-22 "re-derive, never quote" discipline, passed in the one row where a flashier claim was available and refused). **Fleet law proposed by RR-A, adopted here: Not-Claimed/Non-claims blocks are cross-read BEFORE adjudication, not after.**

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (route with KF.W9)

1. **D-5's painted ratios + which ladder arm is live** — the plate is `color-mix` under `backdrop-filter` and `--glass-backdrop` is runtime luma-sampled; decides "null" vs "also inverted in light". Read computed `color` on the :22 span vs its :20 container, both themes.
2. **M7/D-1's rendered face + MISSED-2's upright empty state** — source-certain by cascade + CSS Fonts 4 + the banked KF-EST-2 precedent; only a screenshot closes them.
3. **L-D5's observed interleaving** — carried at the bank's residue #5, not double-booked; the absence of serialization is proven static.
4. **D-7(2)/MISSED-4's rendered clip** — how much of the displaced, frame-shrunk ghost survives `overflow-hidden` at shipped scale.
5. **D-15's tall-capture consequence** — clip vs viewport overflow; target-dependent.
6. **D-6/M9's decode reflow magnitude** and whether it makes the tooltip un-hoverable (WCAG 1.4.13) — rides the banked M9 residue.
7. **D-10/D-21's forced-colors rendering** — what the ghost conveys under the system palette.
8. **D-12's exact gutters and whether `max-w-56` binds across markers** — geometry firmed statically (P2-DEAD); the binding is live-only.
9. **MISSED-1's exact per-AT utterance** — the mechanism (textContent-derived, alt-excluded, frozen post-mount) is source-certain; row-boundary handling varies by AT.
10. **D-3's Chrome ≥127 focusable-scroller horn** — decides wording only (unreachable vs APG violation); the defect survives either way.
11. **L-D15's decode-failure frequency** — multi-MB base64 data URLs at scale 0.5 over large targets; the missing handler is static.

## DISSENT — preserved, not ruled away

1. **RR-A's MAJOR rungs on D-2, D-3, C-5, and D-5** — preserved with reopening conditions: D-2 reverts if the Monaco surface ever detaches from the timeline card (the recovery path is the demotion's whole ground); D-3 reverts for the keyboard modality if KF.W7 rules non-destructive keyboard selection in (today the recovery path costs a mutation — banked M3); C-5 reverts the day scroll-phase authoring becomes a first-class flow (the adapter comment's "aren't YET wired" is a dated promise); D-5's MAJOR stands ready if SS-13 measures the inversion as legibility-breaking on the real plate.
2. **RR-B's demotion logic on L-D7** ("no runnable gate → contract debt") — preserved as a position, overruled by the banked family rung (MM-9's ruling text rejects exactly this softening). If the owner ever rules status text a data register, the row dies for the family, not per-component.
3. **Reader collisions as fleet evidence** — this corpus produced THREE instrument-settleable contradictions that no axis flagged: C-S+4 vs L-D2 (two adjacent tsconfig lines), axis-L's L-D10 vs axis-D's own Not-Claimed block (the same two lines, opposite conclusions), and RR-B's S-b vs RR-A's MISSED-2 (the italic that does not exist). All three were settled here on third instruments. Joins the ColorSpaceSelector citation-inheritance event and kf-KeyframeTimeline DISSENT-4: high-confidence prose is not provenance.
4. **The substrate schism, again** — every seat in this corpus (both axes, both readers, this one) measured against the installed 7.0.0 producer while the local HEAD is a disqualified 41-commit-stale fork; C-5's field does not even EXIST at that HEAD. kf-App ruling 1's witness law (origin/master or later) held this record together; any future wave touching this cluster re-resolves anchors against the frontier first (D-19).
5. **Zero line drift** observed at every site this seat re-opened against the worktree.

## Closing verdict

**ADJUDICATED — RED at the keyframes worktree (origin/master substrate; the stale local HEAD disqualified per the banked witness law): 26 rows booked (0 BLOCKER · 4 MAJOR · 15 MINOR · 7 INFO) with 13 folds + 1 killed-identity fold discharged by reference to banked kf-KeyframeTimeline (M7/M9/D-4·L-4·C-5/L-5/D-10/D-18/D-21/D-22/C-15/N-2/N-9), kf-App.skeleton KF-SKEL-16, kf-CSSCodeEditor KF-CE-16, census SCH-1, and the TEP-12/MCP-31 producer-merger family — zero re-bookings; 10 claims killed with re-executed refutations (both axis BLOCKERs dead — one by fold, one on its own falsifier; one axis superlative and one reader-superlative arm killed on this seat's instruments; one "undecidable" decided by a version-exact banked instrument); 8 net-new missed rows booked, two of them (MISSED-1's frozen alt-excluding description, GHOST-PLATE's transparent hairline) landing precisely on the keyboard path the banked D-10 already convicts. The component remains the cluster's best-built leaf — pure signature, zero teardown, clean boundary, R1-immune — while its two output channels are its least honest: the textual ground truth is uppercased (banked), truncated (MINOR, recovered elsewhere), and announced to AT as a frozen run-on (MAJOR, new); the visual fallback misrepresents four independent ways (MAJOR head + two MINOR siblings). Routings: the bulk to KF.W7 (the census names this cluster); the typography/token cure family — the M7 fold, D-4, L-D7, MISSED-2, MISSED-3, D-5, D-11, D-17 — to KF.W6 with the cure-collision guard booked so no wave fixes the rung and ships a 3-row scroller; C-8 to KF.W5 by fold; the type-gate witness and the census-gate design input to KF.W4; 11 residue rows to KF.W9/SS-13. Sequencing: the KF.W4 vue-tsc gate precedes verification of L-D2's cure; the ghost redesign (D-7 + MISSED-4 + GHOST-PLATE + L-D8) and the a11y seam (MISSED-1 + the D-10 fold) are each ONE design problem and must be spec'd as such. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.
