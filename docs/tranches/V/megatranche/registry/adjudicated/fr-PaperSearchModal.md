# fr-PaperSearchModal — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat) · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited)

**Date**: 2026-08-24 · **Registry HEAD**: value.js `8a174321` (branch `tranche-u`) · **Subject tree**: fourier-analysis `cd26c65` (branch `m/w1-bump-migration`) · **Installed producer**: glass-ui **4.0.0** (`web/node_modules/@mkbabb/glass-ui/package.json`, re-read this seat; committed HEAD lockfile pins ^3.1.0 — the adopted baseline is the installed 4.0.0, and every dist-artifact claim below is labeled by which baseline it reads) · **Component**: `web/src/components/paper/search/PaperSearchModal.vue` (124 lines, read whole this seat).
**Corpus**: 3 challenge axes at `docs/tranches/V/megatranche/audit/fourier-components/PaperSearchModal/` (challenge-D 289 L · challenge-L 169 L · challenge-C 303 L, immutable per E-1/E-3) · **Re-readers adjudicated**: design-and-usage (return 1) · library-and-code (return 2), both hostile full-corpus re-reads.
**Method (M-23/L-18)**: every verdict-level disagreement between the two re-readers was re-derived against the tree with this seat's own instruments before ruling; every load-bearing kill was re-attacked; every new-MAJOR miss was independently re-proven (two by execution). **One re-reader verdict is OVERRULED on seat evidence (return 2's D-18 CONFIRMED-MAJOR, killed at K-1), one re-reader confirmation cell is corrected twice over (return 2's "ModalOverlay … verified present in the installed exports map", K-4), one re-reader kill is itself killed (return 2's K-10 rule-block arithmetic — the seat's recount vindicates the C axis, R-5), and one seat-minted kill lands against an arm both readers left unruled (L-7's prop-drilling arm, dead on the C axis's own cleared probe, K-11).** Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**Seat instruments (all static + node against installed dist; zero browser probes — probe parsimony)**: subject whole; `PaperSearch.vue` whole (397 L — the style owner; rule blocks enumerated one by one); `usePaperSearch.ts` whole (108 L); `paperSearchIndex.ts` whole (401 L); `searchHelpers.ts` whole (74 L); `PaperSearchInput.vue` whole (69 L); `PaperSearchDropdown.vue` whole (70 L); `search/index.ts` (4 L); `web/src/style.css` whole (143 L — the 4-class `:focus-visible` enumeration :136-143, the tab-panel PRM block :92-96, the D.W4.d scope-hash comment :129-135, the responsive root :40-50); producer: `dist/styles/utilities/a11y-overrides.css:1-31` (the universal PRM reset — the D-18 decider), `dist/styles/index.css`→`utilities.css:19` import chain, `dist/dialog.d.ts` + `dist/components/ui/dialog/index.d.ts` (nine names — the ModalOverlay decider) + `grep ModalOverlay dist/index.d.ts` → no hit + `find` → `_shared/ModalOverlay.vue.d.ts` only, `dist/search.js` (penalty formula :31 · full-list cache :110 · cache-hit re-slice :85 · `class:"kbd"` ×4 :321-326 · `type:"button"` ×5 · separator set · 36rem/12vh/70vh geometry · "No results" · onScopeDispose+clearSearchCache · WeakMap :79), `dist/button-BNDWhAZb.js` (`[&_svg:not([class*=size-])]:size-(--ui-glyph)` + `as: { default: "button" }` + `type:{}` no-default, byte-extracted), `composables/types.d.ts` (FuzzySearchState member order), `useFuzzySearch.d.ts` (120/30 defaults), `styles/utilities/base.css:525-539` (`.kbd`), `typography/scale.css:87` (`--type-micro: 0.6875rem`); shipped artifact: `dist/assets/index-57FkGzlZ.css` byte-extraction of the universal PRM block AND the `.kbd` atom; `--ui-glyph`/`--control-h-md` grep over all of `web/dist` → **0 files each** (the artifact is a 3.1.0-era build); pin provenance `git show HEAD:web/package.json` (^3.1.0) vs working tree (^4.0.0) vs installed (4.0.0); consumers: `PaperView.vue` :99-118 (navigateTo + the ⌘K handler) / :171 (`progressRaf = requestAnimationFrame` — the K-12 decider) / :271-275 (window keydown) / :317-336 (both mounts), `MobileFloatingToc.vue` :35-59 (the house focus-restore + scroll-lock idiom) / :86-96 / :105-112, `PaperSidebar.vue` :48-52 / :130-140; censuses re-run: `usePaperSearch(` 1 call site · `<PaperSearch ` 2 mounts · `.matches` readers 4-internal-only · `entry.depth`/`.sectionId` field readers 0 · PRM files 12 · `aria-label` 60 / `title=` 46 · `<form` 0 · e2e 8 specs / 0 coverage · barrel consumers 0; **node execution** of faithful transcriptions of `fuzzyMatch` + `highlightFuzzy` (the PSM-14/PSM-28 deciders — outputs pasted in their rows); banked-registry identity sweeps: fr-FullscreenViewer (K-2 PRM substrate · FV-12 · FV-27), fr-BasisSelector M-3, fr-ContourEditorCanvas D/M-1, kf-KeyboardShortcutsModal superlative-3, fr-AnimationControls K-15, intake `lane-fourier-r3-r6.md` R3-11/X-6 rows read verbatim; `grep -rln 'PaperSearchModal|search-modal' registry/adjudicated/` → only FV-27's census row — **nothing here is a re-book**.

## Scoreboard

**42 adjudicated-confirmed rows (4 BLOCKERS · 10 MAJOR · 19 MINOR · 9 INFO) · 12 claims KILLED (one re-reader verdict overruled, one re-reader confirmation cell corrected, one re-reader kill killed, two seat-minted) · 11 corpus misses banked (4 from return 1, 7 from return 2) + 1 seat sharpening · 11 UNPROVEN-NEEDS-LIVE residues → SS-13.**

The component in one sentence: a command palette whose entire authored visual contract is dead CSS orphaned two commits after birth, which mounts twice against one shared flag below 1024px, hand-rolls a dialog with zero dialog semantics beside a producer that ships the whole thing — dialog, search, scorer, strings, `.kbd` keycaps — at the installed pin, and whose fork diverged from that producer by exactly one `.slice()` placement that silently amputates the search space; yet its internal escaping, scroll containment, watcher complementarity, and layer discipline are genuinely expert, and its corpus produced the single best re-rooting in the fourier fleet (C-1's six-lane contradiction).

## Ruled disagreements (the seat's actual work)

| # | Question | return 1 (design-and-usage) | return 2 (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|---|
| R-1 | **D-18** — does the modal transition lack a `prefers-reduced-motion` guard? | KILLED ("dies on its own falsifier"; producer universal reset; MINOR residue) | **CONFIRMED MAJOR** ("no ancestor reset covers this") | **KILLED — return 1 wins; return 2 OVERRULED** | `a11y-overrides.css:6-17`, read this seat: `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important; animation-duration: 0.01ms !important } }` — `transform` is globally removed from the transitioned set, so the scale/translate legs (PaperSearch.vue:364-368/:374-378) never tween under PRM. Reachability: `src/style.css:3` `@import "@mkbabb/glass-ui/styles"` → `index.css` → `utilities.css:19` → a11y-overrides. **And not 4.0.0-only**: this seat byte-extracted the identical block from the SHIPPED `dist/assets/index-57FkGzlZ.css` (`@media(prefers-reduced-motion:reduce){:not([data-allow-motion]){transition-property:opacity,color,background-color,border-color,box-shadow!important;…}}`) — a first grep truncated at the artifact's *named-class* PRM blocks and nearly misled this seat; the full extraction settles it at both baselines. Return 2's cell counted 12 guard-carrying files and quoted the tab-panel-scoped `style.css:92` block but never opened the producer sheet — the exact falsifier-failure class fr-FullscreenViewer **K-2** already banked against this same repo's corpus. Residue re-booked PSM-32 (MINOR): the enter-from `scale(0.96) translateY(-8px)` frame still *applies* statically and snaps — the banked FV-12 shape. |
| R-2 | **C-9's export limb / return 2's D-2 cell** — is `ModalOverlay` "reachable through `./dialog`" at the installed pin? | KILLED (nine names, none ModalOverlay; the cure seat must be named `DialogContent`) | CONFIRMED — "`ModalOverlay-CkPRRgl_.js` … verified present in the installed dist and exports map" (asserted in TWO confirmed cells) | **KILLED — return 1 wins; return 2's confirmation cell CORRECTED** | Seat reads: `dist/dialog.d.ts` = `export * from "./components/ui/dialog"`; that barrel publishes exactly **nine** names (Dialog, DialogClose, DialogTrigger, DialogHeader, DialogTitle, DialogDescription, DialogContent, DialogScrollContent, DialogFooter) — no ModalOverlay; `grep ModalOverlay dist/index.d.ts` → exit 1; the component exists only at `dist/components/ui/_shared/ModalOverlay.vue.d.ts`, an internal partial composed by DialogContent, with **no public specifier**. The chunk's existence (`ModalOverlay-CkPRRgl_.js`) is real and is what fooled return 2 — a chunk on disk is not an export. A repair wave writing `import { ModalOverlay } from "@mkbabb/glass-ui/dialog"` does not resolve. Cure seat: **DialogContent** (+ DialogTitle for the accessible name). The surrounding C-9 finding — the modal opts out of the depth ladder its own sibling opts into (`PaperSearchDropdown.vue:39 glass-floating`, seat-read) — SURVIVES at PSM-38. |
| R-3 | **D-14's `[LIVE]` tag** — do the icons render at `--ui-glyph` today? | CONFIRMED MINOR (with a stale-dist aside) | Mechanism confirmed; `[LIVE]` KILLED — LATENT-on-next-build (its K-5) | **Return 2 wins — LATENT-at-the-adopted-pin** | Seat greps: `--ui-glyph` and `--control-h-md` appear in **0 files** under `web/dist` — the shipped artifact is a 3.1.0-era build (2026-06-12) in which `.h-3\.5` is unopposed and the icons DO render at 14px. Pin provenance seat-verified: HEAD `web/package.json` ^3.1.0 / working tree ^4.0.0 / installed 4.0.0. The mechanism is certain at the adopted pin (guard byte-extracted from `button-BNDWhAZb.js`; `h-3.5` contains no `size-` substring; specificity (0,2,1) > (0,1,0), same layer) — it becomes live on the first 4.0.0 build. Booked PSM-24 with the baseline label; re-measure post-F.W1 (SS-13 #6). |
| R-4 | **D-6 clause (a)** — is the stationary-cursor hijack source-certain? | Confirmed clause (a) whole as source-certain | Split (its K-9): movement-hijack certain; stationary half UA-dependent | **Return 2 wins — split adopted** | `@mouseenter` (subject :90) writes the arrow-keys' state with no movement guard — pointer *movement* across a re-rendering list hijacks keyboard selection, source-certain (PSM-10, MAJOR). Whether a UA re-dispatches `mouseenter` under a *motionless* pointer when the DOM mutates or `scrollIntoView` moves rows is engine re-hit-testing — the same UA question D-6(b) itself tags UNPROVEN. Both halves → SS-13 #3. |
| R-5 | **Rule-block arithmetic** — return 2's K-10 charged C-1 (48/5/43) and L-1 (~4/~40) with miscounts, asserting **49/5/44** | (not addressed) | "My enumeration … gives 49 rule blocks … 44 dead" | **SEAT-MINTED KILL of the kill (K-10): the count is 48/5/43 — C-1 was right** | This seat enumerated every block of `PaperSearch.vue:41-397` from its own whole-file read: 40 selector blocks (:43,:47,:58,:62,:69,:80,:84,:97,:103,:118,:132,:137,:150,:158,:163,:168,:174,:185,:192,:199,:204,:211,:215,:225,:232,:239,:252,:266,:274,:281,:292,:296,:304,:308,:313,:317,:321,:328,:336,:344) + 8 transition blocks (:359,:364,:370,:374,:380,:384,:389,:393) = **48**, of which **5 live** (:43, :47, :58, :199, :204 — PaperSearch's own root, the Input child's single-element root, and their variants) and **43 dead**. Return 2's charge against L-1's "~4 of ~40" stands (L enumerates 5 live in its own scope note, then reports 4); its "correction" of C-1 is itself off by one. Verdict unmoved at any count; F.W3/W4's mechanical rule-move takes THIS enumerated set. |
| R-6 | **D-13's residue severity** — ⌘K opens only the dropdown; the footer omits the entry key | re-file as INFO | "surviving true kernel, at MINOR" | **INFO — return 1 wins; return 2's dissent recorded** | The modal is fully keyboard-operable (seat-verified: `PaperSearchInput.vue:47-57` is a native-`<button>` glass Button, Tab+Enter path complete) and nothing advertises a modal hotkey that fails — the residue is an ergonomics/teaching gap on a surface shaped like a command palette, not a broken contract. PSM-42. If a future spec declares ⌘K *should* target the palette, the row upgrades there, not here. |
| R-7 | **L-7's prop-drilling arm** — "a composable's return bag prop-drilled through four boundaries" (filed inside a MAJOR) | (dead-emit half confirmed; drilling arm unruled) | (dead-emit half confirmed; drilling arm unruled) | **SEAT-MINTED KILL (K-11) — dead on the C axis's own cleared probe** | challenge-C ran this exact falsifier and published the result both re-readers skipped past: glass-ui's own `FuzzySearch.vue` declares the identical contract — `props: { state: FuzzySearchState }` with `e.state.selectedIndex.value = n` child-side mutation (seat re-verified in `search.js`). The state-bag-as-prop idiom is producer-sanctioned; the four-hop transport is an observation (provide/inject exists one level up at `PaperView.vue:63`), not a chargeable defect. L-7's surviving half is the dead emit (PSM-16, MINOR). |

**Also ruled inside agreed verdicts**: C-1's contradiction of the six 2026-06-17 M-critique lanes is RATIFIED with the seat's own mechanism walk (`PaperSearchInput.vue:35` single-element root → `.paper-search-input-wrap[data-v-a7d16b8c]` PAINTS while `.paper-search-input { outline: none }` (:69-78, a descendant rule) never matches → the UA ring survives: square, offset, outside the rounded wrap; the A4-03 verdict is self-refuting on its own terms) — recorded as S-1, the corpus's best work. And two rows neither re-reader adjudicated at all are dispositioned by this seat so nothing is dropped: **D-15** (prose) FOLDS into PSM-8 (the idle-state cure re-words "No results") and PSM-37 (the esc register rides the legend correction); **C-11** (`v-html` vs `useTextHighlight`) FOLDS into PSM-11 + PSM-9 (the re-run cost and its producer cure are one identity).

## Adjudicated defect roster

Line anchors are fourier-analysis `cd26c65`; re-resolve before any cure (D-19). Every fold cites its banked identity; **no fold is re-booked**.

### BLOCKERS

- **PSM-1 = D-1 · L-1 · C-1 — ADJUDICATED BLOCKER.** Every CSS rule governing the component is orphaned by Vue scoping; the modal ships as an unstyled block appended to `<body>`. Subject has zero `<style>` blocks; all 43 dead rules live in `PaperSearch.vue:41` `<style scoped>` (seat count **48 blocks / 5 live / 43 dead**, R-5); the scope id attaches to PaperSearch alone (`data-v-a7d16b8c` ×1 in the shipped JS, emitted overlay props carry no scope attribute — both readers, artifact-verified); the `setScopeId` root-inheritance gate cannot fire because the subject's subTree is the Teleport vnode (:41). Provenance: a refactor casualty at `ffba307` (the pre-split monolith had Teleport + styles in one SFC). Two limbs of D-1's consequence paragraph are KILLED and ride below (K-5 causation, K-6 cross-baseline render predictions); the BLOCKER stands on the scope-id proof alone. The sibling damage (Input's descendants, Dropdown's fragment root inheriting nothing) is one repair, not three. **→ F.W3/W4** (the fork-retirement/colocation cure; L §6's colocation order stands, WITH the PSM-13 rider mandatory in the same commit). Rendered appearance → SS-13 #1.
- **PSM-2 = D-2 · C-3 · L-4(scope)/L-14(container)/L-16 + D-4 — ADJUDICATED BLOCKER.** Not a dialog: zero `role=`/`aria-` tokens across the whole search family (seat grep), no accessible name, no focus trap, no focus restore, no `inert`/`aria-hidden` background, no scroll lock (L-16 folds here — one identity), no combobox/listbox/`aria-activedescendant` semantics for the virtual selection it implements (D-4 folds here), Escape input-bound only. The house idiom exists two files away and was not applied (`MobileFloatingToc.vue:37-59` — focus restore + scroll lock + unmount restore, seat-read). The producer remedy at the installed pin is `@mkbabb/glass-ui/dialog` → **DialogContent + DialogTitle** (NINE public names; **never `ModalOverlay`** — R-2). Precedent: fr-FullscreenViewer FV-2 books the same hand-rolled-modal shape on this repo's other Teleport — same cure family, different component, no fold. **→ F.W3/W4**.
- **PSM-3 = L-2 · C-2 — ADJUDICATED BLOCKER.** Below 1024px two PaperSearchModal instances mount simultaneously against one shared `isExpanded`: one `usePaperSearch()` (PaperView.vue:111, seat grep = sole site), two unconditional `<PaperSearch>` hosts (PaperSidebar.vue:51 inside a CSS-only-hidden aside :132-140; MobileFloatingToc.vue:111 under `searchActive` inside `lg:hidden`), each rendering one modal that Teleports out of the hidden subtree. Both `nextTick` autofocuses race; `document.querySelector` (subject :32) deterministically drives the *sidebar's* invisible instance (body-append order). **Identity guard**: intake **R3-11/X-6** adjudicated the Codex registry-JOIN defect (same record twice) and ADOPT-AS-FACT'd the two Teleport sites; fr-FullscreenViewer **FV-27** ratified the census. This row is the *tree's own* duplicate — extends, never overwrites; F.W4 must not "correct" the instance count to 1 by fiat (both axes' rider, ratified). **→ F.W3/W4** (gate the sidebar mount on the breakpoint its CSS already uses, or hoist the modal to one app-level seat — the Dialog chassis makes the latter natural). Capture → SS-13 #2.
- **PSM-4 = D-3 — ADJUDICATED BLOCKER (LATENT — armed by PSM-1's cure).** The modal's four non-result chrome surfaces fail WCAG 1.4.3/1.4.11 by ~2.4× in both themes: `--muted-foreground` diluted to 45%/50%/40% via `color-mix(…, transparent)` composites to **1.88:1** (hint, light) / **2.39:1** (dark) / 2.04:1 (empty) / 1.74:1 (placeholder). Both readers independently reproduced the arithmetic to the digit, each first calibrating against the producer's own inline comments (5.21/7.64 undiluted) — the method self-validates (S-2). House floor precedent: `style.css:113-122` darkens `--viz-amber` at 3.54:1. **Must land in the same wave as PSM-1 or the cure ships a WCAG regression** (challenge-D's repair-order constraint, both readers, ratified). **→ F.W3/W4** (same-wave rider).

### MAJOR

- **PSM-5 = L-8 · C-5 — ADJUDICATED MAJOR.** The prefix-narrowing cache stores the TRUNCATED top-30 (`paperSearchIndex.ts:238-240` — sort, slice, `_cache.set(q, results)`), then `:221-227` narrows the next keystroke's candidates from it: a row falling out of a prefix's top-30 is permanently unreachable down that keystroke path, and typing vs pasting the same query returns different result sets. Decisive producer diff seat-verified in bytes: `search.js:110` caches the FULL scored array and slices only the return; `:85` re-slices on cache hit. The fork diverged by one `.slice()` placement. **→ F.W3/W4** (dies with fork retirement; PSM-30 is its exported-contract rider).
- **PSM-6 = D-5 · L-4 — ADJUDICATED MAJOR.** `@keydown="search.onKeydown"` is bound to exactly one node (the input, :59); every result row and both header actions are native tabbable `<button>`s (chunk-verified `as:{default:"button"}`); the moment focus lands on any of up to 32 of them, ↑/↓/Enter/Escape all die while the footer keeps advertising them. Sole document-level keydown is ⌘K (PaperView.vue:113-118/:273, seat-read). Distinct mechanism from PSM-2, same chassis cure. **→ F.W3/W4**.
- **PSM-7 = L-6 (navigation half) — ADJUDICATED MAJOR.** `SearchEntry.id` is not unique — code (:335) and proof (:350) entries take `id: sectionId` unconditionally (equations/theorems/figures fall back at :301/:314/:325) — so `selectResult → navigateTo(r.id)` sends the second proof/code block in a section to the section heading, indistinguishable from the first. The `:key` sub-claim is DEMOTED to PSM-39 (return 2's K-2, adopted): index-spliced keys are positional, but the rows are stateless and no stable identity *exists* to key on — the cure mints one at `buildSearchIndex`, then deletes the index term. C-S5's celebration of this key is KILLED (K-3). **→ F.W3/W4**.
- **PSM-8 = D-7 · L-11 (+ D-15 fold) — ADJUDICATED MAJOR.** No idle state: an empty query paints "No results" (`searchIndex` returns `[]` for `""` :204-207; the subject's only alternative branch is `v-else` :104-106); reachable because `canExpand` gates only entry. No busy affordance across the 120ms debounce, no error branch. D-15's prose corrections (query echo, placeholder, Esc register) ride this cure. **→ F.W3/W4**.
- **PSM-9 = D-9 · C-4 — ADJUDICATED MAJOR (census escalation 🟡→🔴 HARD, both readers sustained, seat re-verified at the pin).** `@mkbabb/glass-ui/search` at the INSTALLED 4.0.0 is a generalisation of this exact fork: same seven-line scoring docstring (ASCII-modulo — K-7's trim holds), same separator set, same 120/30 defaults (d.ts, seat-read), same ten state members (**different order** — C-4's "same order" is FALSE, K-7), prop-for-prop host component, byte-identical geometry/strings/`<kbd>` legend/row-key shape, plus what the fork lacks: `useTextHighlight`, `clearSearchCache`+`onScopeDispose`, WeakMap-per-index caching (:79), `type:"button"` ×5, reka Dialog + Popover. `./search` imported **0** times. Corpus corroboration: the 2026-06-17 D4-05 row said "unadopted", six weeks unactioned then, ~fourteen now. **→ F.W3/W4** (this IS the shadow-retirement lane; adoptable at the installed pin, no tri-package wait) — with the PSM-14 caveat: adoption must NOT be booked as fixing the scorer.
- **PSM-10 = D-6(a-movement) — ADJUDICATED MAJOR (R-4 split).** `@mouseenter` (subject :90) writes `selectedIndex` with no movement guard while the list re-renders on every keystroke and `selectedIndex` reset (usePaperSearch.ts:35-41) — pointer travel over the list hijacks keyboard selection. Stationary half + scrollIntoView feedback loop → SS-13 #3. **→ F.W3/W4**.
- **PSM-11 = L-5 (+ C-11 fold) — ADJUDICATED MAJOR.** `SearchResult.matches` is computed, spread onto every result (:234) and read by nobody (seat grep: 4 hits, all internal producers); the highlighter instead re-runs the full fuzzy match + per-character three-regex escaping inline in the `v-for` body on every render, re-entered per `mouseenter`. Root cause ratified: `:185` indexes the LOWER-CASED label while `resultLabel` returns original case or a rawTex/plainText slice — the indices genuinely misalign; the re-run is a correct workaround for a broken contract never deleted. Producer cure (`useTextHighlight`, CSS Custom Highlight API) rides PSM-9. Cost in ms → SS-13 #4. **→ F.W3/W4**.
- **PSM-12 = D-19 — ADJUDICATED MAJOR (LATENT).** Dark-mode figure/ground collapse: panel fill = the page token (:259), hardcoded black shadows invisible over `hsl(24 9% 4%)` (:260-262), scrim = `--background`@55% over itself (:247); `grep '\.dark'` → 0. What remains is the 1.5px `--border` hairline at **2.82:1** (return 1's measurement, adopted) — below the 3:1 SC 1.4.11 floor, converting the qualitative claim into a named WCAG failure. **→ F.W3/W4** (same wave as PSM-1).
- **PSM-13 — MISS (return 1), seat-verified — ADJUDICATED MAJOR. The prescribed cure ships a WCAG 2.4.7 regression.** `.search-modal-input { … outline: none … }` (PaperSearch.vue:281-290, `:285` — seat-read) is inert today only because PSM-1 kills it. The moment the repair colocates the block — exactly what L §6 prescribes — the modal's primary control loses its focus indicator with no replacement: seat grep of the file finds no `:focus`/`:focus-visible` for `.search-modal-input` (the only focus rule is the SIBLING's `:focus-within` at :58), and the global fallback (`style.css:136-143`) enumerates exactly four other classes. The result rows keep glass Button's `focus-ring`; post-cure the one control the user types into would be the only ringless one. Absent from every axis's activated-by-the-cure ledger. Banked shape precedents (NOT the same defect — new booking is proper): fr-BasisSelector **M-3**, fr-ContourEditorCanvas **D/M-1**; the novelty here is the ordering — the first row in the registry where the *repair* arms the defect. **→ F.W3/W4 — MANDATORY rider inside the PSM-1 commit.**
- **PSM-14 — MISS (return 2), seat-CONFIRMED BY EXECUTION — ADJUDICATED MAJOR. Full-text search is functionally dead for short queries, and the prescribed adoption does not fix it.** `fuzzyMatch`'s length penalty (`:124` `score -= (tLen − pLen) × 0.1`) scales with the FIELD while bonuses scale with the PATTERN; `scoreEntry` drops any entry with `best <= 0` (:189). This seat ran a faithful transcription over a 458-char lowercase body: `"fo"` → **−31.6**, `"conv"` → **−36.4**, `"fourier"` (an exact word-boundary occurrence, mid-body) → **−1.1**, `"convergence"` → +6.3 — an exact hit of the word "fourier" in a Fourier paper's body text scores negative and the entry is DROPPED when no other field matches; the drop-logic branch was asserted in the same run. Break-even is text-length-dependent (≈1 pattern char per ~60-70 field chars), so the 300-500-char `plainText` slices — the bulk of the index and of `_cache`'s retained bytes (PSM-22) — are inert for nearly all real queries, and unsliced `rawTex` (weight 6) with it. **Joint consumer+producer defect**: the installed producer carries the IDENTICAL formula (`search.js:31`, seat-extracted) — adopting FuzzySearch must NOT be booked as fixing this. **→ GLASS-RELAY** (per the standing BH-relay edict) **+ F.W3/W4** (the adoption wave carries the caveat).

### MINOR

- **PSM-15 = D-11 · L-3 · C-6.** `document.querySelector(".search-modal-results")` (:32) where the sibling uses a template ref (`PaperSearchDropdown.vue:16/:24` — seat-read; C's `:18/:26` cites do not resolve, K-8); the subject's own `modalInputRef` through the same Teleport is the in-file disproof. Independent of PSM-1; becomes *wrong* under PSM-3. **→ F.W3/W4**.
- **PSM-16 = D-8 · L-7(surviving half) · L-13 · C-7.** `defineEmits<{ select }>` bound, never invoked, no listener (seat-read; duplicated in the Dropdown); nothing in the ladder sees it (tsconfig `strict` only — seat-read; no eslint config). The drilling arm is K-11. **→ F.W3/W4**.
- **PSM-17 = L-15 · C-10.** `TYPE_LABELS: Record<string,string>` defeats exhaustiveness over the closed 13-member union; `"bibliography"` is declared and never produced (seat grep: 0 push sites); the `thm.type as` cast is provably unnecessary today (latex-paper's 7-member union is a strict subset) and erases the future cross-package tripwire; `?? r.type` is compile-dead without `noUncheckedIndexedAccess`. **→ F.W3/W4**.
- **PSM-18 = D-12.** 20 lines of result-row markup duplicated verbatim between modal (:83-102) and dropdown (:41-60), already drifted via the `.search-modal-result` re-tunes (:308-319). Dies with PSM-9's adoption. **→ F.W3/W4**.
- **PSM-19 = D-21 + D-22 + D-23 (LATENT).** One box, three metric defects, all seat-re-derived: the dominant vertical axis misaligned 0.125rem (header/footer inset 0.875 vs row 0.375+0.625=1.0); the panel hardcodes `0.75rem`/`1.5px` outside the `--radius` series its children derive from, with 1.5px panel vs 1px divider hairlines unruled; keycaps at `0.6rem` = 9.6px desktop (root 1rem ≥768px — seat-read style.css:45-50) in 1.25×1.125rem boxes at PSM-4's 1.88:1. **→ F.W3/W4** (largely dissolved by PSM-25's `.kbd` + the chassis).
- **PSM-20 = D-24 · C-12 (LATENT).** Three hardcoded HSL literals with no dark arm (:158-161, :163-166, :186 — D's cites exact; C's `:151-160` does not resolve, K-8), one rule block from the correct `color-mix(var(--primary))` idiom (:150-156). House precedent style.css:113-127. **→ F.W3/W4**.
- **PSM-21 = D-20 (trimmed to MINOR, both readers).** `vh` (:246, :254) vs `dvw` (:253) viewport-unit incoherence on one box; the failure mode is narrower than filed (12vh+70vh leaves 18vh; the keyboard case that closes the gap isn't fully rescued by `dvh` either). **→ F.W3/W4**.
- **PSM-22 = L-9 + L-10 + L-12.** Module-global query-keyed cache, never index-keyed, never disposed (:196 et al. — producer contrast: WeakMap + `clearSearchCache` + `onScopeDispose`, seat-verified); debounce timer never cleared on scope dispose (no `onScopeDispose` in the composable — seat-read whole); `@click.stop` (:48) dead beside `@click.self` (:46). **→ F.W3/W4**.
- **PSM-23 = D-10 · L-14(title limb).** `title=` where the house uses `aria-label=` on two icon-only buttons (:66, :75); seat census **60** `aria-label` / **46** `title=` (return 1's recount confirmed against D's 42); keyboard-unreachable, touch-absent — and touch is this surface's primary path. **→ F.W3/W4**.
- **PSM-24 = D-14 (LATENT-at-the-adopted-pin, R-3).** `h-3.5 w-3.5` inert against the button chunk's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` guard (byte-extracted; producer prose in `offsets-sizing.css:172-178` names `size-*` as the only escape) — both intended icon registers (:68/:77 vs Input's `h-3 w-3`) collapse to one output on the first 4.0.0 build. **→ F.W1** (re-measure post-uplift, SS-13 #6).
- **PSM-25 — MISS (return 1), seat-verified.** glass-ui ships a `.kbd` atom — a global utility, no import needed (`base.css:525-539`, seat-read; `--type-micro: 0.6875rem` = 11px; **present in the shipped artifact**, seat-extracted) — and the producer's own FuzzySearch puts `class:"kbd"` on these EXACT four legend keycaps (`search.js:321-326`, seat-verified). fourier's four `<kbd>`s (the repo's only four) carry no class and hand-roll strictly worse values on every axis (9.6px vs 11px; 1.125rem box vs 1.5rem floor; 3px vs `--radius-sm`; diluted vs solid tokens). Precedent: kf-KeyboardShortcutsModal banks consuming `.kbd` as-shipped as a *superlative* (row seat-read). Three-attribute adopt-today fix, independent of full fork retirement. **→ F.W3/W4**.
- **PSM-26 — MISS (return 1) + SEAT SHARPENING.** Two visually identical adjacent ghost icon buttons carry opposite data semantics: Collapse (:61-69) preserves the query; Close (:70-78) destroys it (`close()` wipes `query` — no undo), distinguished only by a `title` (PSM-23) at an icon size that renders identically (PSM-24). **Seat sharpening — the legend lies about Escape**: `onKeydown`'s Escape branch (usePaperSearch.ts:83-90, seat-read) COLLAPSES when expanded (`isExpanded = false`, query kept) and never `close()`s — so "esc close" (:117) is false as authored; esc actually agrees with the backdrop (collapse), and only X destroys. Neither reader stated it. Extends C-14 (PSM-37). **→ F.W3/W4** (the Dialog single-`open` model collapses the trio).
- **PSM-27 — MISS (return 2), seat-verified from source.** `close()` is not synchronous: it clears `query` but `debouncedQuery` (and hence `results`) lags 120ms and `_cache` clears only on the next empty-query call — reopening within the window (⌘K, or the input's `@focus`) paints the previous search's rows under an empty input. Producer clears all four in one tick (seat-read `search.js` close path). **→ F.W3/W4**.
- **PSM-28 — MISS (return 2), seat-CONFIRMED BY EXECUTION.** `highlightFuzzy` applies UTF-16 code-unit indices (from `fuzzyMatch` over `text.toLowerCase()`) to a CODE-POINT array (`[...text]`, :52) — this seat's run of the exact source: `highlightFuzzy("𝔽ourier basis","basis")` → `𝔽ourier b<mark>asis</mark>` (off-by-one) and `highlightFuzzy("İstanbul set","set")` → `İs<mark>t</mark>anbul se<mark>t</mark>` (catastrophic). Third-order, also reproduced: `resultLabel`'s code-unit `.slice(0,120)` can emit a lone surrogate (`\ud835`) that `escapeHtml` passes to the sink. Mathematical alphanumerics are plausible in this repo's labels. Bounds S-9 honestly: the sink is SAFE but not CORRECT. **→ F.W3/W4** (the producer's `useTextHighlight` sidesteps string splicing entirely; verify ITS Unicode handling in the relay).
- **PSM-29 — MISS (return 2), seat-verified.** `SearchEntry.sectionId` and `.depth` are write-only payload: populated at every push site, threaded through both walker signatures, read by nothing (seat greps: the only `sectionId` hit outside the module is an unrelated `labelMap` field; no `entry.depth` reader) — shipped on every spread-copied result and retained in the cache; `sectionId` also means two different things at section vs content-block sites (:376/:381 vs :389). **→ F.W3/W4** (the SearchableItem projection deletes both).
- **PSM-30 — MISS (return 2), seat-verified.** `searchIndex`'s exported `maxResults` parameter is inert on the cache-hit path (`:210-211` returns the write-time-sliced array unre-sliced; cache keyed on `q` alone) — an exported-surface contract violation (`index.ts:2` publishes it). Producer re-slices per call (`search.js:85`). Rider on PSM-5. **→ F.W3/W4**.
- **PSM-31 — MISS (return 2), seat-verified.** The index is built eagerly and synchronously in the composable's setup body (`usePaperSearch.ts:18`) on every PaperView mount, search opened or not — five `toLowerCase()` per entry over ≤500-char slices; the producer defers via `computed`. The route carries a dedicated performance spec (`e2e/paper-performance.spec.ts`). **→ F.W3/W4**.
- **PSM-32 = D-18 residue (R-1).** Under PRM the enter-from `scale(0.96) translateY(-8px)` frame still applies statically, then snaps — a one-frame spatial offset paint. Banked shape: fr-FullscreenViewer **FV-12** (same substrate, different property mechanics). Perceptibility → SS-13 #11. **→ F.W3/W4** (dies with the chassis).
- **PSM-38 = C-9 (surviving finding, R-2).** The modal opts out of the glass depth ladder its own sibling opts into (`glass-floating` on the dropdown :39; `glass-resting`/`glass-wash` in the hosts) — hand-rolled scrim/shadow literals in its place (dead today per PSM-1). Cure seat: **DialogContent** (R-2). **→ F.W3/W4**.

### INFO

- **PSM-33 = C-13.** `lucide-vue-next` imported at runtime from **devDependencies** (Dockerfile bare `npm ci` keeps it latent); rides the F.W1 `@lucide/vue` rename ×35 with the manifest-section move. **→ F.W1**.
- **PSM-34 = C-8.** No `type="button"` on any Button (chunk-verified: `as` has a default, `type` does not → HTML `submit`); no `<form>` in the tree (seat grep 0) — hygiene against the producer's own ×5 idiom (seat-verified; return 1's trim ratified: those five are native buttons in FuzzySearch, not glass `<Button>`s). **→ F.W3/W4**.
- **PSM-35 = D-16.** Equation rows display raw TeX hard-cut at 120 code units mid-token (`searchHelpers.ts:26`) — plus PSM-28's lone-surrogate rider on the same slice. **→ F.W3/W4**.
- **PSM-36 = D-17.** Negative result, ratified: the file's only glass-ui import is `./button` (survives 4→7); the census's named break surface touches this file zero times; sole uplift exposure = the lucide rename. **→ F.W1**.
- **PSM-37 = C-14 (+ D-15's register limb + PSM-26's seat sharpening).** Contract asymmetries: backdrop AND Escape collapse while the legend teaches "esc close" and only X closes; no `variant` prop so the modal cannot self-arbitrate PSM-3's duplicate; the barrel excludes the SFCs. **→ F.W3/W4**.
- **PSM-39 = L-6 (`:key` half, demoted per K-2).** Index-spliced keys are positional (destroy/recreate on every reorder) but the rows are stateless and no stable identity exists until PSM-7's cure mints one — cost, not correctness. **→ F.W3/W4** (rides PSM-7).
- **PSM-40 — MISS (return 1), seat-verified.** The enter transition's root and child disagree on duration: overlay `transition: opacity 0.2s ease` (:359-361) vs child transform `0.25s var(--ease-out-expo)` (:364-368) — Vue's `whenTransitionEnds` times out on the overlay's 200ms and the `enter-active` removal cancels the child transform at ~80% time (≈99.6% progress under the expo curve — sub-pixel snap, honestly graded INFO). The leave pair matches (0.15/0.15, :370-378), proving the author knew to match them. Bounds S-14. **→ F.W3/W4**.
- **PSM-41 — MISS (return 2), seat-verified.** `search/index.ts` has ZERO consumers repo-wide (seat grep — every import into the directory is a deep path); the fictional boundary also publishes `escapeHtml` — attribute-unsafe (`"`/`'` omitted) — as general-purpose public API. **→ F.W3/W4**.
- **PSM-42 = D-13 residue (R-6).** ⌘K reaches only the dropdown (`open()` sets `isOpen`, never `isExpanded`); the footer omits the key that got the user there. Return 2's MINOR dissent recorded; upgrades only if a spec declares the palette hotkey-owned. **→ F.W3/W4**.

## Killed-claims register

| K | Claim (origin · filed weight) | Refutation (deciding evidence) |
|---|---|---|
| K-1 | **D-18 as filed** (challenge-D LATENT-MAJOR "no PRM guard") **and return 2's CONFIRMED-MAJOR** | R-1. The producer's universal PRM reset (`a11y-overrides.css:6-17`) removes `transform` from the transitioned set on `*:not([data-allow-motion])`, reaches fourier via `style.css:3`, and is byte-present in the SHIPPED artifact (seat-extracted after a truncated first grep nearly misled this seat). Fleet precedent on this exact repo: fr-FullscreenViewer **K-2**. Residue → PSM-32. Return 2 OVERRULED — it named the 12-file contrast and the tab-panel block but never opened the producer sheet. |
| K-2 | **D-13** (MINOR — "a keyboard-only user cannot reach the expanded view at all") | Both returns, ratified with seat reads: the expand affordance is a native-`<button>` glass Button (`PaperSearchInput.vue:47-57`; chunk `as:{default:"button"}`), Tab-reachable, Enter/Space-activated; the path input→type→Tab→Enter is complete. Also internally contradicts D-5, which *requires* the rows to be tabbable. Residue → PSM-42 (INFO, R-6). |
| K-3 | **C-S5** (superlative — "the row `:key` is exactly right") | Both returns, ratified: a symptom container mistaken for a design — C found the id collision (:335/:350) and praised the workaround while missing the navigation defect it implies (PSM-7). Positional keys are a band-aid; the cure is a minted unique id, then the index term deletes. |
| K-4 | **C-9's export limb** ("glass-ui 4.0.0 ships `ModalOverlay` … reachable through `./dialog`") + D-2's repetition + **return 2's TWO confirmation cells** ("verified present in the installed exports map") | R-2. Nine public dialog names, none ModalOverlay; no hit in `dist/index.d.ts`; the component is an internal `_shared/` partial. A chunk on disk is not an export. The repair seat must be named **DialogContent** or the wave writes an unresolvable import. Surviving depth-ladder finding → PSM-38. |
| K-5 | **D-1's icon-button causal aside** ("60px … BECAUSE `.paper-search-action-btn{padding:.15rem}` is dead") | Return 1's kill, ratified: the scoped rule declares padding, never height; `size="icon"` sizes via `--control-h-md` (a different property) and the coarse floor rides `a11y-overrides` regardless — curing PSM-1 does not shrink these buttons. Secondary slip: on the coarse arm where `--ui-scale` is 1.5, fourier's root is 1.125rem → ~67.5px, not 60px. Core BLOCKER untouched. |
| K-6 | **D-1's/D-14's `[LIVE]` render predictions read from the 4.0.0 pin** ("content-width centred `.btn-pill` rows", "`--ui-glyph` icons") | Return 2's cross-baseline law, ratified with the seat's own grep: `--ui-glyph`/`--control-h-md` occur in **0 files** of `web/dist`; the only build that exists is 3.1.0-era (HEAD lockfile ^3.1.0, seat-verified). The scope-id proof is baseline-invariant and stands; the pill/glyph renders are predictions about a build that does not exist — re-measure after F.W1 (SS-13 #6). |
| K-7 | **C-4's "same ten members, same names, same order"** | Seat-read `composables/types.d.ts`: producer declares `query, results, selectedIndex, isOpen, isExpanded, onKeydown, selectResult, toggleExpanded, close, open`; the fork returns `query, results, isOpen, isExpanded, selectedIndex, selectResult, close, open, toggleExpanded, onKeydown`. Same members, different order. D-9's "byte-equivalent" docstring is likewise ASCII-modulo (`→`/`−` vs `->`/`-`). Neither trim touches the PSM-9 escalation. |
| K-8 | **C axis provenance (systemic)** — file:line cites into every non-subject file | Both returns, seat spot-confirmed on three deciders: `<style scoped>` is at **:41** (C: :39-386); `_cache` at **:196** (C: :186); Dropdown `resultsRef`/query at **:16/:24** (C: :18/:26); badge literals at **:158-166** (C: :151-160). C's substance survives everywhere this seat checked; its checkability does not — F-wave repairs re-anchor from L or D (whose cites this seat verified line-exact throughout, sole drift L-6's ±1-2). |
| K-9 | **D-6(a)'s "source-certain" tag on the stationary half** | R-4. `mouseenter` under a motionless pointer on DOM mutation is engine re-hit-testing, not a source property — the same UA class D-6 itself queues as (b). Movement half CONFIRMED (PSM-10); stationary half → SS-13 #3. |
| K-10 | **Return 2's own K-10** ("49 rule blocks … 44 dead"; C-1's dead count "one short") — **seat-minted kill of a kill** | R-5. The seat's block-by-block enumeration of PaperSearch.vue:41-397 = **48 / 5 live / 43 dead** — exactly C-1's figures. The charge against L-1's "~4 of ~40" stands; the charge against C-1 is withdrawn by arithmetic. A reader that miscounts while correcting a count is the citation-inheritance lesson (fr-AnimationControls K-15) in a new costume. |
| K-11 | **L-7's prop-drilling arm** ("a composable's return bag prop-drilled through four boundaries" as MAJOR substance) — **seat-minted** | R-7. The C axis's cleared probe, which both re-readers skipped: the producer's own FuzzySearch declares the identical `state`-bag prop and mutates it child-side (`search.js`, seat-verified) — producer-sanctioned idiom, not a fourier deviation. Dead-emit half survives at PSM-16. |
| K-12 | **L-17's falsifier ("the paper route has no canvas and no rAF clock")** — rAF half | Return 2's K-4, ratified with seat read: `PaperView.vue:171` `progressRaf = requestAnimationFrame(writeProgress)` (+ the scroll-correction loop in `useScrollNavigation`). Canvas half TRUE. The failed falsifier makes L-17's latent blur-cost INFO **stronger**: a full-viewport `backdrop-filter: blur(6px)` would composite against live rAF writers. → SS-13 #7. |

## Misses banked (beyond the challenge corpus)

Return 1: the cure-armed `outline:none` 2.4.7 regression (→PSM-13, the corpus's most consequential miss — every axis prescribed the colocation and none asked what goes live), the `.kbd` atom (→PSM-25), the enter root/child duration mismatch inside a praised block (→PSM-40), the collapse/close destructive asymmetry (→PSM-26). Return 2: the length-penalty scoring kill (→PSM-14, seat-confirmed by execution, joint producer defect), the Unicode highlight misalignment (→PSM-28, seat-confirmed by execution), dead `sectionId`/`depth` payload (→PSM-29), `maxResults` inert on cache-hit (→PSM-30), the 120ms stale-close window (→PSM-27), the eager index build (→PSM-31), the dead barrel + `escapeHtml` API (→PSM-41). Seat: **Escape collapses and never closes** — the footer's "esc close" is false as authored (usePaperSearch.ts:83-90 → PSM-26/PSM-37); the K-10 recount (R-5); and the notice that both re-readers silently dropped D-15 and C-11 without disposition (both FOLDED by this seat so nothing is lost).

## Superlatives — with provenance

- **S-1 (both returns' best-work pick; seat-ratified end-to-end)** — C-1's contradiction of six prior fourier M-critique lanes (A2-08 · A4-03 critical · CHR-07 · B1-PAPER-03 · B6H-01 · C2-R1), all of which recorded the live "blue square offset focus ring" and mis-rooted it as an unadopted Input primitive. True root: scope-id unreachability — the wrap (child ROOT) paints rounded and bordered while the descendant `outline:none` never matches, leaving the UA's square ring. The A4 verdict is self-refuting on its own terms, and C's rider is the difference between a repair and a cover-up: adopting `.input-pill` would have MASKED the defect while 42 other rules stayed dead.
- **S-2 (return 2's method note, ratified)** — D-3's contrast instrument was calibrated against the producer's own published numbers (5.21/7.64) before being pointed at the diluted surfaces; both re-readers then reproduced the dilutions independently to the digit. The correct epistemic move, rare in this fleet.
- **S-3 (return 1, ratified with the K-6 caveat)** — D-1's four-instrument close (compiler, bundler, runtime, shipped artifact — `data-v-a7d16b8c` ×1, attached to the parent alone) plus the `ffba307^` provenance dig proving refactor casualty, not original sin. The render-consequence paragraph is the part that died (K-5/K-6); the proof core is exemplary.
- **S-4 (challenge-L §6, both returns, ratified)** — the standing gate lesson: `vue-tsc` + `vite build` cannot observe a scoped-CSS boundary violation and e2e has zero coverage of this surface (seat re-verified: 8 specs / 0 hits); the greppable check — no `<style scoped>` block may declare a class appearing exclusively in a CHILD component's template — would have caught this at `ffba307` and generalises to every future split. **The most reusable sentence in the corpus; F.W3/W4 spec-authoring input.**
- **S-5 (return 1, ratified)** — D-14's specificity read corroborated by the producer's own prose (`offsets-sizing.css:172-178` names `size-*` as the sole escape hatch): two intentionally-differentiated icon registers collapsing to byte-identical output is a finding no gate can produce.
- **S-6 (both returns, seat-extended)** — L's and D's provenance discipline (line-exact everywhere this seat checked, ~30 spot-verifications across two returns plus this seat's own) against C's systemic drift (K-8) — and the corpus's falsifier discipline is again what made it cheap to adjudicate: K-1 and K-2 were obtained by running the corpus's own named falsifiers against files their authors never opened.
- **S-7 (in the component; D-S2 · both returns, ratified)** — textbook scroll containment: `overflow:hidden` on the panel + `flex:1; min-height:0; overflow-y:auto; overscroll-behavior:contain` on the results — the `min-height:0` defeats the flex min-content floor, and `overscroll-behavior` is load-bearing precisely BECAUSE there is no scroll lock (PSM-2).
- **S-8 (in the component; L-S2, ratified)** — the two scroll-into-view guards are exact logical complements (`!isExpanded` return in the modal :30 vs `isExpanded` return in the dropdown :22): across two components sharing one `selectedIndex`, every state is covered exactly once.
- **S-9 (in the component; D-S7/L-S1/C-S4, ratified WITH the PSM-28 bound)** — the `v-html` sink escapes on all four return paths and the input is defended twice (`stripHtml` at index time); the highest-risk string in the app is SAFE by construction. Safe is not correct: the indices it marks are wrong under non-BMP input (seat-executed). Both facts now stand together.
- **S-10 (in the component; D-S1, ratified)** — `scrollIntoView({ block: "nearest" })`: the correct, less obvious choice; `"center"`/`"smooth"` would recentre per keystroke and fight PRM.
- **S-11 (in the component; L-S3/L-S4/C-S1/C-S2, ratified)** — pure-core discipline that holds under grep (`paperSearchIndex.ts` imports no Vue at all; the header comment is TRUE — noteworthy against the fleet's stale-comment findings class), zero canvas/rAF/observer/timer of its own in a tree censused for two ungated rAF clocks, zero value.js/keyframes/API coupling — bounding the tri-package atomic transaction to ONE leg (glass-ui) for this whole family.
- **S-12 (in the component; C-S3, ratified)** — `fira-code` resolves to the producer's global utility and is today the only class on the component that paints — the one styling decision that happened to be scope-proof.
- **S-13 (in the component; D-S4/S-5/S-6, ratified)** — `@click.self` dismissal without a backdrop element (smaller and more correct than the sibling's extra div + four-clause `v-if`), `dvw` with an honest gutter, top-anchored panel that doesn't jump with result count.
- **S-14 (in the component; D-S3, ratified BOUNDED by PSM-40)** — asymmetric token-native motion (faster, shallower exit; all three easing tokens resolve at the pin, seat-corroborated by both returns) — praised by the corpus without noticing the root/child duration incoherence inside the same block; the superlative and its bound now travel together.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **PSM-1 rendered truth** — the unstyled teleported subtree's appearance and document position (below the fold? overlapping? scroll-reachable?). The selector mismatch is statically closed; the pixels are a capture.
2. **PSM-3 dual-overlay capture** — <1024px, reach `searchActive ∧ !mobileTocVisible ∧ isExpanded`; photograph both `.search-modal-overlay` subtrees in `<body>`, the focus race between the two autofocus `nextTick`s, and the misdirected scroll-into-view.
3. **PSM-10 both halves** — whether a UA re-dispatches `mouseenter` under a stationary pointer on (a) list re-render and (b) programmatic `scrollIntoView` (R-4).
4. **PSM-11 cost** — ms per render of the 30-row re-scan + per-character escaping (the deadness and re-evaluation are static facts; the milliseconds are not).
5. **PSM-5 live specimen** — a type-vs-paste divergence for a concrete query against the live index (deterministic from source; the specimen is index-dependent).
6. **PSM-24 / K-6 post-uplift re-measure** — the first 4.0.0 build's actual render of the icon sizes and result-row pills; the only existing artifact is the 3.1.0-era build and must not be quoted either way (STALE-DIST law).
7. **K-12-sharpened blur cost** — `backdrop-filter: blur(6px)` full-viewport over a route running the `progressRaf` writer + the scroll-correction loop, once PSM-1's cure makes it paint.
8. **The sibling's DEV_ROOT_FRAGMENT question** (C's queue item 4) — dev/prod scope asymmetry on PaperSearchDropdown's two-node fragment root; immaterial to the subject, material to whoever executes the colocation.
9. **PSM-14 live confirmation** — a body-text-only term absent from results in the running app (the scorer arithmetic is seat-executed; one witness closes it).
10. **IME composition** on the hand-rolled `:value`/`@input` binding (return 2's recorded-not-filed; not derivable from source).
11. **PSM-32 perceptibility** — the one-frame enter-from offset paint before the PRM snap (FV-12's witness shape).

## Closing verdict

**GRAVE, TWICE OVER, WITH AN EXPERT'S FINGERPRINTS INSIDE THE WRECK.** Four blockers — a stylesheet that cannot reach its markup, a dialog with no dialog in it, a modal that mounts twice against one flag, and a contrast failure armed the moment the first blocker is cured — sit on a 124-line component whose producer ships the entire capability, dialog to keycap, at the installed pin; the fork's sharpest divergences from that producer are one `.slice()` above a `.set()` (PSM-5) and one shared scoring formula whose defect the adoption will NOT fix (PSM-14 — the relay letter matters as much as the migration). The corpus was strong — C-1's six-lane re-rooting is the best single finding in the fourier fleet — and the two hostile re-readers were strong too, yet the seat still found both fallible in the directions that matter: one confirmed a claim whose falsifier lives in a file it never opened (K-1), one asserted an export that does not exist into two confirmed cells (K-4), one killed a correct count with an incorrect one (K-10), and both walked past an arm their own corpus had already cleared (K-11). Routing: the fork-retirement/chassis/colocation cluster with its two mandatory same-commit riders (PSM-4, PSM-13) **→ F.W3/W4**; the lucide rename + manifest move and the post-uplift icon re-measure **→ F.W1**; the scorer formula **→ GLASS-RELAY** (joint defect, standing BH edict); the R3-11 registry-join stays with its intake owner at F.W4, extended and not overwritten. No row here is NO-WAVE-OWNER. Nothing here stamps a wave — agglomeration input under M-25.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
