claude-fable-5

# fr-AdminFlaggedPanel — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: `/Users/mkbabb/Programming/fourier-analysis` @ `cd26c65` (re-verified this session; **28 dirty rows** incl. the subject — see ruling 8/K8: the working tree carries exactly one uncommitted hunk in this file, `:265` `variant="opaque"` → `surface="opaque"`, re-diffed by this seat) · subject `web/src/components/visualization/gallery/AdminFlaggedPanel.vue` (286 lines, read whole) · **Registry tree**: value.js `tranche-u`.
**Name-collision guard**: the banked `adjudicated/AdminFlaggedPanel.md` (2026-08-03) is the **value.js demo** component `demo/palettes/browser/admin/AdminFlaggedPanel.vue` — a different product, a different tree, zero shared rows. This record is the **fourier-analysis** component under the `fr-` prefix; no fold in either direction.
**Corpus adjudicated**: two hostile re-reader returns (reader-α = design-and-usage: 34 confirmation cells · 8 kills · 7 misses · 7 superlatives; reader-β = library-and-code: 36 confirmation cells · 11 kills · 7 misses · 10 superlatives) over the component's three challenge files, all three read **whole** by this seat: `challenge-D-design.md` (365 L, Opus 5 [1m], 22 rows) · `challenge-L-library.md` (230 L, Opus 5 [1m], 27 rows) · `challenge-C-consumption.md` (545 L, Opus 5 [1m], 20 rows). The readers' remits differ by design (α design-heavy, β library-heavy), so one-sided coverage of a row is remit, not negligence — noted where it matters.
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, every severity contest, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption. One reader kill **failed re-derivation and is itself killed** (ruling 1); one reader "correction" of a challenge was corrected back (ruling 3); one silently-demoted grade was restored (ruling 10). Contrast arithmetic is adopted only where double-derived (challenge + at least one reader concur to ~0.1); no colorimetry was re-run by this seat. Zero browser probes (probe parsimony; every ruling below is decidable from source + installed dist + producer src). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings are agglomeration input (M-25), and nothing here stamps a wave.
**This seat's own instruments (all read-only; sole write = this file)**: full read of `AdminFlaggedPanel.vue` (286 L — every cited anchor checked: `:37/:44-56/:58-71/:73/:84-99/:101-125/:127-146/:151-153/:156-235/:241-261/:264-279/:283-285`); `git log`/`git status`/`git diff` on fourier (the K8 decider: the sole hunk is `:265` variant→surface; HEAD `cd26c65` carries `variant="opaque"`); **empty-scoped-style enumeration re-run repo-wide** (awk block-body test after stripping `@reference`+comments → **exactly 4**: this file, AdminAuditLog, AdminUserList, GalleryDraftsSection; the three paper files reader-β added all carry real rules — ruling 1); flag-route grep (`admin.py:509` GET + `:590` DELETE, **only**), `flags.insert|replace|update` grep (5 hits, all tests/migration), `FlagRequest` grep (declaration only), `grep -ic flag GalleryCard.vue` → **0**, `grep -rli flag web/src` → **11 files**; `web/e2e/` ls → **8 specs**, `admin|flag` grep → 0; PRM grep → **17 raw hits**; `function timeAgo` grep → **5**; success-toast grep → **3** (`:93/:105/:120`); `api/routers/admin.py:160-190/:193-231/:509-585/:585-612` read (soft-delete default, hard arm, `if not flagged` short-circuit, `{"liked_ips": 0}` projection, hash-scoped `delete_many`, `dismissed: 0` idempotence, `log_audit` at set_tier/delete:hard/delete:noop/delete/dismiss_flags); `api/routers/images.py` route census (**7 routes, zero DELETE**, `:132` blob GET) + `visualizations.py:430` restore route; `web/src/lib/api.ts:61/:215-254/:278-290/:518-532` (the abort-seam decider: `apiFetch` takes an explicit semantic `abortKey`, `adminFetch` hard-codes `coreFetch<T>(path, /* abortKey */ path, …)` at `:250`; `abortInflight` used only with semantic names, `workspace.ts:90/:140/:202/:270`); `stores/gallery.ts:138-160` (guards + `resetAndFetch` twins); `composables/useToast.ts:1-19` (VARIANT_MAP success≡info≡"default"); `composables/useOffsetPagination.ts` error refs `:31/:41/:48/:75`; `lib/types.ts:96/:139-169` (GalleryTier closed; `tier: string | null` at the FlaggedVisualization body — the β-miss decider); `web/package.json` whole (bands + scripts); `GalleryCardModal.vue:28-36/:68-80` (the D-S-6 kill decider: D.W4.c comment + real `<Dialog v-model:open>` + `DialogContent surface="opaque"`); installed glass-ui **4.0.0**: `package.json` (peers `@mkbabb/value.js ^0.10||^0.11`, `@lucide/vue ^1.16.0`; exports incl. `./badge` + `./confirm-dialog`), `dist/components/ui/badge/index.d.ts:1-8` (variant incl. `destructive`, size incl. `sm`), `dist/components/ui/dialog/DialogContent.vue.d.ts` (`surface` `:12`, `showClose` `:53`/defaults `:74-75`, and the `:11` comment naming the **retired binary `variant: glass|opaque`** — the K8 mechanism), `dist/button-BNDWhAZb.js:67-71` CVA (`icon: "h-(--control-h-md) w-(--control-h-md) p-0"`, `icon-sm: "h-(--control-h-xs) w-(--control-h-xs) p-0"` — the ruling-4 decider), `dist/styles/tokens/offsets-sizing.css:148-152` (`--control-floor: 0px`; `--control-h-xs: max(calc(1.75rem·scale), floor)`; md = 2.5rem) + `~:461` (`--touch-target: 2.75rem /* 44px */`), `dist/styles/tokens/light-dark.css:17-21` (`@media (pointer: coarse)` → `--ui-scale: 1.5`, `--control-floor: var(--touch-target)`), **this seat's own node run of the bundled merger** (`dist/cn-DJXf4yaB.js` export `t`): `cn('h-(--control-h-md) w-(--control-h-md) p-0', 'h-7 w-7 …')` → `p-0 h-7 w-7 …` — **both control-height vars stripped** (the ruling-5 decider); `node_modules/tailwindcss/theme.css:13-15` (`--color-red-300: oklch(80.8% 0.114 19.571)` — the K2 decider); producer glass-ui **7.0.0** src: `button/Button.vue` (`variant` grep → **0**; `ButtonSize = Extract<Size,"xs"|"sm"|"md"|"lg">` `:16`; `loading?: boolean` `:27`), toast `ToastVariant` grep → **0**, `dialog/DialogContent.vue` (`dismiss?: DialogDismiss` `:39`, default `"free"` `:47`, `closeVisible = dismiss === "free"` `:147` — the K3 decider), `_shared/axes.ts:22/:52-53` (SURFACES incl. `opaque`; TONES); `web/tsconfig*` + `vite.config.ts` `strictTemplates` grep → **0** (the ruling-3 decider).

## Scoreboard

**57 adjudicated roster rows — 4 BLOCKER · 20 MAJOR · 24 MINOR · 9 INFO.** **15 killed-claims register entries: 14 SUSTAINED, 1 REFUTED by this seat** (a reader kill killed — reader-β's L-19 enumeration). **14 missed rows ADMITTED** (7 per reader; two become new MAJORs, both mechanism-re-executed by this seat). **11 superlatives sustained** (three of them new from reader-β, all re-verified at the bytes; two qualified). **8 adjudicator corrections against the readers**, including one restored severity (C-D-2 back to BLOCKER), two demotions (L-2, D-3 → MAJOR), and one config-contingent "strengthening" struck (ruling 3).

Blocker-weight survivors of record: **FR-AFP-1** (the moderation queue has **no producer** — nothing in production writes `db.flags`; the panel's only production-reachable state is its empty state), **FR-AFP-2** (all six `<Button>` callsites break at the 4→7 uplift — three hard `vue-tsc` errors + six silent `variant` fallthroughs, incl. the confirm dialog's Delete degrading to look identical to Cancel — a **sixth break class the census does not name**), **FR-AFP-3** (the panel is unshipped in light mode; the card outline fails 1.4.11 in **both** themes), **FR-AFP-4** (no action the panel offers takes the reported artifact offline: no image DELETE exists in the 45-op surface, the blob is served unauthenticated with `Cache-Control: public`, and every blob GET bumps the janitor's recency predicate). Realized production impact of the moderation-flow defects is **zero today** — FR-AFP-1 gates them all — which is precisely why the roster's routing splits between the uplift wave (live at F.W1) and the CRUD/provenance union (live the day a producer lands).

## Ruled disagreements and corrections — the incredulity seat's own work

1. **L-19 enumeration — reader-β's kill is KILLED; reader-α's confirmation stands EXACT.** β killed the L-challenge's "exactly four files, all in this directory" with "seven SFCs carry a `<style scoped>` whose only content is `@reference "tailwindcss"`", naming `MobileFloatingToc.vue:191`, `PaperSearch.vue:41`, `PaperSidebar.vue:130`. This seat re-ran the enumeration with a block-body test (strip `@reference` + comments, require empty remainder): **exactly 4 hits**, all in the gallery directory. All three paper files carry real rules immediately after `@reference` (`.floating-toc{…}`, `.paper-search{…}`, `.paper-sidebar{…}` — read directly). β's instrument matched files *containing* the `@reference` line without testing that the block was otherwise empty — a grep-shape error on a "repo-wide the pattern appears in exactly N files" claim. **FR-AFP-39 stands at 4 with directory-locality intact; β's kill is REFUTED and recorded as fleet evidence** (an enumeration kill must re-run the *predicate*, not a substring).
2. **L-2 severity — RULED for reader-β (MAJOR); reader-α's BLOCKER was internally inconsistent.** α graded L-2 BLOCKER on the composition "wrong label × irreversibility" while simultaneously confirming C:D-5 ("the dialog asserts irrevocability over a **restorable** operation") — the two cells cannot both hold. This seat re-read the deciders: `api.ts:522-526` `hard = false` default, the component's `:92` two-arg call, `admin.py:196` `Query(default=False)`, `:215-222` the only hard arm (UI-unreachable), the `soft_delete` call, and `visualizations.py:430` POST restore. The delete is a 30-day-restorable soft delete; the irreversibility premise is false. **FR-AFP-8 = MAJOR** (the label bug is real and one token to cure), and the *sharper* composed defect is C-D-5's: the copy promises a permanence the operation does not deliver, on a moderation surface — **FR-AFP-9**.
3. **D-1's break mechanism — reader-α's account ADOPTED; reader-β's "the finding is stronger than stated" cell CORRECTED.** β asserted "excess-property checking applies to template prop object literals, so `variant=` is **also** a vue-tsc error at 7.0.0, not the silent fallthrough D-1 describes." That is config-contingent: without `vueCompilerOptions.strictTemplates`, vue-language-tools accepts undeclared template props as fallthrough attrs. This seat's grep of `web/tsconfig*` + `vite.config.ts` finds **no `strictTemplates`** — so at this tree's config the six `variant=` uses are **silent** (incl. `:276`'s destructive→neutral degradation, the worst one) and only the three `size="icon"` uses (a *declared* prop with a narrow union) hard-error. α/challenge-D's split — 3 hard + 6 silent — is the correct budget for F.W1. β's verdict is unaffected; its mechanism cell is corrected.
4. **The `icon-sm` cure — reader-α wins; the cure half of reader-β's C-D-14 kill is CORRECTED.** β: "nothing in the shipped .d.ts or CVA output establishes that `icon-sm` resolves to the h-7 w-7 box… the prescribed swap is unverified." At the bytes: `dist/button-BNDWhAZb.js` CVA maps `icon-sm → h-(--control-h-xs) w-(--control-h-xs) p-0`, and `--control-h-xs = max(calc(1.75rem × var(--ui-scale)), var(--control-floor))` (`offsets-sizing.css:149`) = **28px fine-pointer ≡ h-7 exactly**, re-entering the 44px coarse clamp. The swap is verified — *as the incumbent (pre-uplift) spelling*; β's version-context point survives intact: F.W1's destination is glass 7's `iconOnly` + a real size rung, so `icon-sm` is a today-cure, not the uplift cure. β's other two C-D-14 kill halves (THREE not six `size="icon"` sites; `#fca5a5` is the v3 hex — the installed v4.3.1 declares `oklch(80.8% 0.114 19.571)`, re-read at `theme.css:13`) are SUSTAINED.
5. **The touch-floor missed MAJOR (α) — ADMITTED, mechanism RE-EXECUTED by this seat.** My own node run of the bundled merger: `cn('h-(--control-h-md) w-(--control-h-md) p-0', 'h-7 w-7 …')` → `p-0 h-7 w-7 …` — both control-height variables **removed** by tailwind-merge. Those variables are the entire WCAG-2.5.5 contract: `light-dark.css:17-21` lifts `--ui-scale` to 1.5 and `--control-floor` to `--touch-target` = 44px under `pointer: coarse`, and the library's own comment (`offsets-sizing.css:145-147`) names the clamp seam. So `size="icon"` delivers 40px fine / 60px coarse; the `:201/:211/:221` overrides pin all three row actions — including irreversible Delete — at **28px in both pointer classes** (AdminUserList's at 24px, below even WCAG 2.2 §2.5.8). Neither D-18 (geometry drift) nor C-D-14 (redundancy) reached the accessibility consequence. **FR-AFP-24, MAJOR.** Counterweight adopted as superlative: the load-more Button (`size="sm"`, no h-* override) keeps `h-(--control-h-sm)` and is touch-conformant **by omission** — the file contains its own proof the override is unnecessary.
6. **The abort-seam missed MAJOR (β) — ADMITTED, verified at the seam.** `apiFetch` (`api.ts:215`) takes an explicit semantic `abortKey` and its call sites pass names (`"uploadImage"` `:278`, `"getImageMeta"` `:285`); `adminFetch` (`:245-254`) has **no key parameter** and hard-codes the URL as identity (`:250`, comment verbatim `/* abortKey */ path`); `abortInflight(keys)` (`:61`) — the repo's only explicit cancel API — is called only with semantic names (`workspace.ts:90/:140/:202/:270`). Consequence: every admin operation is structurally uncancellable-by-name, and the L-5/L-11 self-abort/race family is a **seam property, not a component accident**; the one-parameter seam cure is where C-D-11's own "durable fix is at the seam" reasoning leads. Extends **R6-8** (operation identity must not embed request identity) by reference — fold, never re-book. **FR-AFP-13, MAJOR.**
7. **D-9's toast counts — reader-β's kill SUSTAINED; reader-α's cell corrected.** This seat's grep: exactly **3** success toasts (`:93/:105/:120`), 8 total. α's confirmed claim text repeats the challenge's "its 4 success toasts" while α's own proof cell enumerates the three — a citation-inheritance-shaped slip (the same class pt-leaf r2 recorded twice), verdict unaffected, corrected.
8. **Pin provenance (α's K-8) — SUSTAINED at the tree; reader-β's D-S5-confirmed superlative cell inherits the correction.** `git diff` on fourier: `AdminFlaggedPanel.vue` is one of **28 dirty rows**, with exactly one hunk — `:265` `- variant="opaque"` / `+ surface="opaque"`. At HEAD `cd26c65` the file says `variant="opaque"`, and `variant` is **not** a 4.0.0 DialogContent prop (the installed d.ts's own `:11` comment names the "retired binary `variant: glass|opaque` (clean break)") — so at the audited pin the prop falls through as a stray attribute and `surface` takes its `"glass"` default: **a translucent destructive confirm at the very coordinate the corpus praises for opting out of glass**. Every "survives 4→7 untouched" superlative (D:S-5, C:S-2's surviving half, β's D-S5 cell) describes the *working tree*, not the pin. The one-hunk fix must be understood as **uncommitted** by any F.W1 planner.
9. **D-S-6's comparison (β's kill) — SUSTAINED at the bytes; DA-design-A3 #5 is NOT still live.** `GalleryCardModal.vue:31-35` carries the D.W4.c migration comment and `:69-72` is a real `<Dialog v-model:open>` + `DialogContent surface="opaque"`; the `<div>` at `:75` is a layout child *inside* DialogContent. Challenge-D re-asserted a prior-audit row without re-verifying, in the one superlative whose force is the comparison. The superlative's primary half (this panel's confirm is a real focus-trapped Dialog) survives unharmed.
10. **C-D-2 severity — RESTORED to BLOCKER against reader-β's silent MAJOR.** β confirmed everything in C-D-2 and *added* the sharpest mechanism (every anonymous blob GET ends in `touch_document`, so the janitor's `last_accessed_at` predicate never reaps actively-requested content) — then graded the row **below** the challenge and below its own added evidence, with no demotion rationale. This seat re-verified the skeleton: `images.py` = 7 routes, **zero DELETE**; `:132` blob GET with no `Depends` and `Cache-Control: public, max-age=86400`; restore exists for the visualization but nothing exists for the image. The public-blob/janitor half is live **today**, independent of FR-AFP-1's producer gap. **FR-AFP-4 = BLOCKER**; α's grade sustained, β's dissent recorded.
11. **D-3/L-7 severity — RULED MAJOR (β's grade); α's BLOCKER preserved as dissent.** The v-if/v-else subtree swap and the absent restoration are source-certain; but BLOCKER in this corpus is reserved for rows that invalidate the surface wholesale, the cure is in-file (`loadingMore` pattern, thirty lines above), and live reachability is zero until FR-AFP-1's producer exists. α's operability argument (WCAG 2.4.3; single-use-per-keyboard-session) is preserved inside the row.
12. **L-1's evidence enumerations — conclusion CONFIRMED by this seat's own greps; β's enumeration kill SUSTAINED.** Two flag routes only; zero production inserts; `FlagRequest` orphaned; `GalleryCard.vue` flag hits = **0** (α's K-4 against D-10's phantom "raise-a-flag affordance" also sustained); but `grep -rli flag web/src` = **11 files** (all boolean-sense/comment/type-surface), not the L-challenge's promised two — a reviewer re-running the stated command finds eleven and may wrongly discount a true BLOCKER. Enumeration corrected; the row is untouchable.

**Adjudicator's corrections register (against this corpus's readers)**: (1) β's L-19 kill — refuted, ruling 1; (2) β's C-D-14 icon-sm-cure half — refuted, ruling 4; (3) β's D-1 excess-property cell — corrected, ruling 3; (4) α's D-9 "4 success" claim text — corrected, ruling 7; (5) α's L-2 BLOCKER — demoted, ruling 2; (6) α's D-3 BLOCKER — demoted, ruling 11; (7) β's C-D-2 MAJOR — restored to BLOCKER, ruling 10; (8) β's devDependencies MINOR — re-graded INFO (the claimed failure mode, "a production install cannot build the app", is a pipeline that does not exist: `npm ci --omit=dev` also omits vite itself; the surviving substance is band inconsistency + unhonored peers, real but harm-nil in a private bundled app).

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5-W8** CRUD/provenance union · **F.W9/W10** · **NO-WAVE-OWNER** for the SS-3/SS-4 spec authoring to consume. Line anchors are the working tree at `cd26c65`+1 hunk; re-resolve before any cure (D-19). Identity guards: rows extending census/intake identities (R3-7a, R3-7c, R5-7, R6-6, R6-8, census FE §5, DA-design-A3 #5/#7/#8, lane-crud `POST /:slug/flag`) **fold by reference — never re-booked**.

### Blockers

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **FR-AFP-1** | L-1 / C-D-1 | The moderation queue has **no producer**: nothing in production writes `db.flags` — two admin routes (read + delete) are the collection's entire live surface, `FlagRequest` is declared and referenced nowhere, no client posts a flag, no UI renders a report affordance. The panel's only production-reachable state is `"No flagged content"`; ~110 of 286 lines dead by reachability | this seat's greps: routes `admin.py:509/:590` only; inserts = 4 test fixtures + 1 `update_many` rename; `FlagRequest` = `api/models/admin.py:57` declaration only; `GalleryCard.vue` flag hits 0 | **F.W5-W8** — the union decides: build the user half (`POST /visualizations/{slug}/flags`, the mirror of lane-crud:336-339's value.js booking) or retire the admin half whole. Extends **R3-7c inverted** (a client+2 endpoints+tab+route branch with no writer) — fold by reference; the gap ledger carries both directions |
| **FR-AFP-2** | D-1 (+K1 = C:S-2 killed) | All six `<Button>` callsites break at glass 4→7: three `size="icon"` are **hard vue-tsc errors** (declared prop, narrow union: `ButtonSize` excludes "icon"), six `variant=` are **silent fallthroughs** at this tree's config (no `strictTemplates` — ruling 3), the worst being `:276` where the confirm dialog's Delete degrades to default emphasis/tone — **visually identical to Cancel** on the one irreversible control. C's "one-line exposure" is a false superlative (nine breaking prop sites, not one); this is a **sixth census break class** (Button re-grammar), unnamed in FE §5 | producer `Button.vue:15-31` (`variant` grep 0; `ButtonSize` `:16`); installed 4.0.0 `index.d.ts:4-6` declares both; sites `:199/:209/:219/:247/:275/:276` + `:200/:210/:220`; this seat's strictTemplates grep → 0 | **F.W1** — mechanical re-grammar (`ghost`→`emphasis="quiet"`, `destructive`→`emphasis="primary" tone="destructive"`, `size="icon"`→`iconOnly`+rung); F.W1 must open with a `<Button` census across all 66 SFCs before sizing (the challenge's own recommendation, sustained) |
| **FR-AFP-3** | D-2 / C-D-14-as-killed | The panel is unshipped in light mode: hardcoded Tailwind reds (`:166/:172/:176/:191`, zero `dark:` prefixes, zero tokens) measure ~1.3–2.6:1 against the light theme the app ships as **fallback default**, and the card outline (`border-red-500/20`) fails 1.4.11 in **both** themes (1.33 light / 1.20 dark). Double-derived: α reproduced the challenge to 2dp on 3 of 4 rows (pill row ~1.31 vs doc 1.39 — compositing-order nuance, immaterial); β independently ≈1.32/≈8.85 | sites re-read in source; tokens `color-radius.css:40/:57`, `dark-arm.css:42`; `theme.css:13-15` v4 oklch (K2); reachability `index.html` light-fallback + `GalleryView.vue:380` admin-only gate | **F.W1** — re-ink on `--destructive`/`tone` + `--border`; **with FR-AFP-46's cure split** (`--destructive` alone fails normal-text at 4.38:1 light — pill fill + glyph on the token, text on `--muted-foreground-strong`/`--foreground`); **K6's correction**: `<Badge variant="destructive" size="sm">` is installed **today** at 4.0.0 — the worst-contrast row needs no uplift to cure |
| **FR-AFP-4** | C-D-2 (+β's janitor sharpening; severity restored, ruling 10) | No action this panel offers takes the reported artifact offline: delete is a soft-delete of the *visualization*; the 45-op surface contains **no image DELETE**; the blob is served unauthenticated with `Cache-Control: public, max-age=86400`; and every blob GET ends in `touch_document`, so the janitor's recency predicate is bumped by the very fetches that keep reported content alive. The panel prints that handle as the row title and inside the confirm copy | `images.py` 7-route census (this seat), `:132` no-Depends blob GET; `api.ts:522-532` `hard=false`; `admin.py:224` soft arm; β's `dependencies.py` touch + `janitor.py:167-169` predicate (adopted) | **F.W5-W8** — image-remediation contract (delete/quarantine + blob visibility gate + janitor predicate); the scope-discipline note survives (soft-delete does de-list from browse) |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **FR-AFP-5** | D-3 / L-7 (MAJOR per ruling 11, α's BLOCKER dissent preserved) | Every mutation sets `loading` and the spinner/list are v-if/v-else **siblings**, so the whole `role="list"` subtree — incl. the just-activated Button — unmounts; no restoration anywhere (sole vue import is `ref`); the Dialog path is worse (reka returns focus to a trigger `reload()` then destroys). Correct shape is 30 lines above (`loadingMore`) | `:45/:94/:106/:121`; `:151` v-if / `:156` v-else; `GalleryView.vue:380-382` bare v-if, no KeepAlive | **F.W3/W4** — refreshing-not-destroying reload + focus restoration; focus destination residue → SS-13 |
| **FR-AFP-6** | D-4 / L-9 / C-D-10 | The error state IS the empty state: a failed load toasts transiently, `flaggedEntries` stays `[]`, and `:232-235` paints "No flagged content" — a false all-clear on a safety surface; no `error` ref exists; the only retry control is `v-if="hasMore"`, false after a failed first load | `:51-54/:232-235/:32`; the repo's own correct seat `useOffsetPagination.ts:31/:48/:75` (re-read) | **F.W3/W4** — explicit error + retry branch |
| **FR-AFP-7** | L-3 / L-4 | Flags are keyed by non-unique `content_hash`, so one flagged artefact fans out to N rows each claiming the full count — and slug-labelled "Dismiss flags" is hash-scoped `delete_many`, silently clearing every sibling | `admin.py:530-548` $group / `:553` $in / `:607` delete_many (re-read); `database.py:98` non-unique (readers concordant) | **F.W5-W8** — flag identity moves to the entity (or dismiss scopes to slug); contract-level |
| **FR-AFP-8** | L-2 / C-D-3 (MAJOR per ruling 2) | The destructive confirm names `image_slug` (asset FK, shared by every remix) while deleting `slug` — violating the identity law the file's own header states at `:18-23`; every human-readable surface renders the FK | `:224/:270/:92`; remix inheritance (C, adopted); header law re-read | **F.W5-W8** identity surface, with a **one-token F.W3/W4 rider** (pass `item.slug` as label) |
| **FR-AFP-9** | C-D-5 | The dialog asserts irrevocability over a 30-day-restorable soft delete; the purpose-built `?hard=true` grace-bypass has zero callers repo-wide | `api.ts:518-532` (docstring re-read verbatim); `admin.py:196`; `visualizations.py:430` restore (this seat) | **F.W5-W8** — the wave decides the hard-arm's caller + truthful copy (copy itself also in FR-AFP-35) |
| **FR-AFP-10** | C-D-4 (β only — α coverage gap by remit; verified by this seat) | "Mark acceptable (save)" never dequeues: `set_tier` writes `{tier, updated_at}` only, the flagged listing has **no tier predicate**, and the in-file comment `:112-115` claims the opposite. The one non-destructive triage action is a no-op against its own queue | `admin.py:160-190` re-read (tier+updated_at only); listing predicates re-read (`not_deleted_filter` + `$in` only) | **F.W5-W8** — tier↔flag semantics decision; the false comment dies with the cure |
| **FR-AFP-11** | C-D-6 (+FR-AFP-57 fold) | Duplicates the gallery store's `setTier`/`deleteEntry` verbatim minus `resetAndFetch` **and** minus the token guard, so the gallery cache goes stale after every moderation; the copy even generalised (`handleSetTier` typed over full GalleryTier, one call site, one literal) where the store action already exists | `stores/gallery.ts:138-160` re-read (guards + resetAndFetch + same toast strings); `:84-99/:116-125/:204` | **F.W3/W4** — delegate to the store (or a shared invalidation channel); the private copies delete |
| **FR-AFP-12** | L-5 / C-D-7 | Five unconditional `catch (e:any) → error toast` with zero `isAbortError` guards, in a repo that guards at 15 sites; `adminFetch`'s path-as-key makes same-path self-abort real (tab toggle remount; double-click) | `:51/:66/:95/:107/:122`; `api.ts:69/:250` re-read; guarded twins `gallery.ts:78/:99/:174/:185` | **F.W3/W4** — guard at the catch sites; the seam-level cure is FR-AFP-13's. Both challenges' quoted abort strings quarantined (K15) |
| **FR-AFP-13** | NEW (β-miss 1, ruling 6) | The api seam runs **two abort-key conventions** and `adminFetch` is the odd one out: it hard-codes the request URL as operation identity, so no admin operation can be cancelled by name via `abortInflight`, and the L-5/L-11 family is a seam property, not a component accident | `api.ts:215/:245-254/:61` + call-site census (this seat) | **F.W3/W4** — one `abortKey` parameter on `adminFetch`; extends **R6-8** by reference |
| **FR-AFP-14** | L-11 / C-D-8 | `reload()`/`loadMore()` have distinct abort keys, no generation token; an interleaved mutation lets a stale page-2 push onto a fresh page-1 array and clobber the cursor; duplicate `:key` reachable via post-deletion cursor shift (β's two-mechanism reconciliation adopted: the cursors.py exclusivity kill and the duplicate-key consequence are both right) | `api.ts:542-548`; `:48-50/:59/:63-65` | **F.W3/W4** — generation token or shared key; composes with FR-AFP-48 |
| **FR-AFP-15** | D-8 / L-8 / C-D-9 | Every mutation discards all accumulated cursor pages (`:48` reassign after `fetchFlagged(null)`), contradicting the file's own header promise (`:28-29`); in-place splice model exists at `gallery.ts:170-171` | source re-read | **F.W3/W4** — splice-in-place; **FR-AFP-56 rider binds here**: the `:key="i"` flag sub-loop (`:187-188`) is benign only under wholesale remount and will mis-patch the moment this cure lands — land the natural key (`(content_hash, reporter_slug)` is unique) in the same change |
| **FR-AFP-16** | D-6 / L-14 / C-D-12b | Three of four mutations have no in-flight guard and no disabled state while the correct pattern sits ten lines above; double-click fires duplicate mutations + concurrent reloads whose completion order decides the array | `:59/:250` vs `:84/:101/:116`, buttons `:198-227/:276` | **F.W3/W4** guards; the one-word cure primitive (`Button loading`, producer `Button.vue:27`, re-read) lands with **F.W1** |
| **FR-AFP-17** | D-9 (count corrected, ruling 7) | The panel's 8 toasts ride the census-named `ToastVariant` hard break (producer grep 0, re-run by this seat), AND its **3** success toasts are already indistinguishable from info today (`VARIANT_MAP` success≡info≡"default", re-read) — no positive-outcome signal on the moderation surface; the 7.0.0 cure is an improvement (`tone:"success"`), not a rename | `useToast.ts:1-13` re-read; producer toast surface re-read | **F.W1** — `Record<ToastType, Tone>` with success→"success". Census FE §5 identity — fold by reference |
| **FR-AFP-18** | D-10 (falsifier prose killed, K4) | The moderation queue has no size anywhere in the product: no total in the envelope, no `count_documents`, no `AdminStats.flagged`, six vanity MetricBadges and zero pressure metrics, bare tab label | types/envelope + banner cites (readers concordant); K4: the "raise-a-flag affordance" counter never existed | contract → **F.W5-W8**; render seat → **F.W1** (metric-badge→metric migration) |
| **FR-AFP-19** | D-5 (folds DA-design-A3 #7, still live — identity fold, not re-book) | Destructive intent is colour-on-hover only: all three actions rest at `text-muted-foreground`, Delete 4px from confirm-less Dismiss, no `focus-visible:` intent counterpart anywhere in the file. α's calibration keeps it honest: the CVA base's focus ring **survives** the merge (verified via the same bundled-cn instrument as ruling 5), so the defect is the missing intent signal, not a missing ring | `:197/:201/:211/:221`; focus-visible grep 0 | **F.W1** (rest-state tint via tone) + F.W3/W4 focus-visible rider |
| **FR-AFP-20** | D-7 | `opacity-60` on `text-muted-foreground` fails AA in both themes (3.38 dark / 2.34 light — α reproduced exactly) on the reporter+time provenance line; the alpha-mute is the exact anti-pattern `--muted-foreground-strong` exists to prevent, per the token's own canon comment | `:189/:193`; `color-radius.css:45/:84-89`, `dark-arm.css:47` | **F.W1** — swap to the strong rung |
| **FR-AFP-21** | L-10 (β/L only — α remit gap) | The panel that moderates images renders no image — the only gallery surface holding `image_slug` and printing it as text; `thumbnailUrl` takes exactly the field in hand; five sibling surfaces render the asset | `:175`; `api.ts:~292` (imageUrl/thumbnailUrl adjacency sighted by this seat); comparanda per L's table | **F.W3/W4** — render the evidence being adjudicated |
| **FR-AFP-22** | C-D-11 / L-17 | The RFC 7807 typed error surface (`ApiProblem` with `type/status/detail/is()`) is built then discarded at all five catch sites — `.message` carries only the title, never the server's rich `detail`; no lint gate exists anywhere in `web/` to notice `catch (e:any)` | `api-problem.ts`; `web/package.json` scripts re-read (no lint, no vitest) | **F.W3/W4** — typed catch posture at the seam (repo-wide pattern; this file is where an admin's only failure feedback lives) |
| **FR-AFP-23** | C-D-12 | The confirm dialog closes **before** the request (`:86` precedes `:92`), so a slow/failed delete is silent and the row looks unactioned; no `:disabled`, no loading | `:84-99/:275-276` | **F.W3/W4** — await-then-close + in-flight guard; the 7.0.0 primitive shape is FR-AFP-37's |
| **FR-AFP-24** | NEW (α-miss 1, ruling 5) | The `h-7 w-7` overrides strip glass-ui's control-height variables and thereby **defeat the WCAG-2.5.5 coarse-pointer touch floor**: 28×28 in both pointer classes where the system delivers 40/60 with a 44px clamp — on all three row actions incl. irreversible Delete; AdminUserList pins 24px, below §2.5.8 | this seat's own cn run + token/coarse-block reads (instruments header) | **F.W1** — `iconOnly` + rung, **no class override**; incumbent spelling `size="icon-sm"` bare is exact (ruling 4) |

### Minors (24) and info (9)

| id | claim (adjudicated, compressed) | disposition |
|---|---|---|
| FR-AFP-25 · L-6/D-22/C-D-16 (MINOR per contest ruling; β's MAJOR preserved as dissent) | `getAdminToken()!` ×4 vs `string|null`; on null the operator toast carries the internal string `coreFetch: auth='admin' requires adminToken`; `:73` bare setup-scope fetch. Reachability genuinely narrow: `auth.ts` registers no storage listener, so a cross-tab logout never nulls **this** tab's in-memory ref — no live path produces the null today; the defect is the silenced assertion + the internal string | **F.W3/W4** — guard-and-return posture (the store's own twins) |
| FR-AFP-26 · D-11 (folds DA-A3 #8, still live — identity fold) | Reviewer's decision data at the system's smallest rung — 10px desktop (`--type-admin-label` fixed non-fluid), three type sizes in one 20px row | **F.W3/W4** re-rung (reason/detail → text-xs) |
| FR-AFP-27 · D-13 | `border-l border-muted` nesting rule at 1.13/1.01:1 — a FILL token painted as a border; literally invisible in light | **F.W1** — `--border` |
| FR-AFP-28 · D-18 (+α's hostile counter-hypothesis record adopted: the 4.0.0 CVA's `--ui-glyph` descendant rule is **emitted in no shipped stylesheet** and no `@source` reaches node_modules, so authored h-* glyph sizes are what render — the table stands) | Three icon-in-button geometries across three sibling tabs; the byte-identical pill recipe carries two different reds (red-300 here, red-400 in AdminUserList) | **F.W1** — geometry collapses free with FR-AFP-24's cure; the ui-glyph packaging question rides the **glass-ui BH relay** (producer-owned; owner edict) |
| FR-AFP-29 · D-12 (headline killed K5: 14 real PRM sites, not 18; this seat's raw count 17) | Two `animate-spin`s with no reduced-motion branch; nothing in any imported sheet reaches `.animate-spin` under PRM; MINOR is the right grade (WCAG 2.2.2 busy-indicator exemption, the challenge arguing against itself honestly) | **F.W3/W4** — `motion-reduce:animate-none`; built-sheet grep residue → SS-13 |
| FR-AFP-30 · D-14/L-18 | `role="list"` owns a non-listitem child — and composed with FR-AFP-1 the illegal child is the **only** child production ever renders | **F.W3/W4** — move the empty state out of the list container |
| FR-AFP-31 · D-15/L-15/C-D-17 | `reasonLabel(reason: string)` widens the closed `FlagReason` union; a drifted server vocabulary ships the raw wire token into the decision slot; sole callsite → zero-blast-radius narrowing | **F.W3/W4** |
| FR-AFP-32 · D-16/L-13/C-D-15 | `timeAgo`: fifth verbatim copy in one directory, two dialects ("0m ago" ×2 vs "just now" ×3 — this seat's grep: exactly 5), no clamp, no seconds bucket, no `<time>`/absolute, no NaN guard against the non-ISO `default=str` wire format (the sibling AdminAuditLog guards) | **F.W3/W4** — one shared formatter; JSC acceptance residue → SS-13 |
| FR-AFP-33 · D-17/L-20/L-22 | Flag list unbounded on both ends (server `$push` no `$slice`; client no cap/collapse) and the aggregate is unscoped — a bare `$group` over the whole collection per page request. Counterweight (β superlative, verified): the `if not flagged` short-circuit means the empty case — production's only case — skips the join entirely | **F.W5-W8** ($slice + $match/index); client collapse (`./expandable-container` installed) **F.W1/W3** |
| FR-AFP-34 · D-19 (+FR-AFP-53 fold) | Truncated slug identity with no recovery; native `title` (touch-inert) instead of the app's 34-callsite/9-consumer Tooltip idiom; `title` and `aria-label` are separately-authored strings that already diverge on all three actions | **F.W3** — rides **R3-7a**'s title→Tooltip migration, fold by reference; a Tooltip callsite collapses the two strings into one source |
| FR-AFP-35 · D-20/C-D-19 | Prose: statutory register ("shall"/"irrevocable" — the latter also **false**, FR-AFP-9), three names for one object, "Dismissed 1 flags"/"Dismissed 0 flags" reachable while `:177` pluralises correctly eleven lines away | **F.W3/W4** copy pass (truthfulness clause owned by FR-AFP-9's wave) |
| FR-AFP-36 · L-16/C-D-20/L-21 | Contract-twin drift both directions (`image_slug: string|null` vs required server field → five dead `?? item.slug` branches; emitted `content_hash` omitted client-side, so the actual join key is invisible) + three dead server models encoding the retired offset contract (`FlagRequest`/`FlaggedEntryInfo` with rotted `user_slug`/`FlaggedListResponse`) + no `response_model` anywhere to check any of it | **F.W5-W8** — one generated (or checked) shape; dead models delete |
| FR-AFP-37 · C-D-13 (defect confirmed; cure re-derived per K3) | Hand-rolled destructive confirm where the pinned producer ships `./confirm-dialog` (0 imports repo-wide), leaving a stray top-right X beside Cancel at the pin (`showClose` defaults true, d.ts re-read). **The prescribed cure is version-stale**: at 7.0.0 `showClose` is GONE — the spelling is `dismiss="deliberate"` (producer `DialogContent.vue:39/:47/:147`, re-read by this seat) | **F.W1** — Dialog preset with `dismiss="deliberate"` + loading guard |
| FR-AFP-38 · L-12′ (headline killed K11 — the "bypasses the seat" framing is refuted: `useOffsetPagination.fetchFn` presupposes a `total` this endpoint never emits; the refusal was correct and is superlative S-5) | Surviving core: the cursor loop is hand-rolled here AND in `stores/gallery.ts` and no `useCursorPagination` exists | **F.W3/W4** — extract the composable |
| FR-AFP-39 · L-19 (enumeration RULED at 4, ruling 1) | Empty `<style scoped>` (only `@reference`) stamps `data-v-*` on ~40 elements for zero rules — exactly 4 files, all in this directory | **F.W3/W4** hygiene sweep (4 files, not 7 — β's wider scope refuted) |
| FR-AFP-40 · L-23 | `timeAgo` is render-frozen — no reactive clock; a panel left open says "3m ago" forever | folds into FR-AFP-32's shared formatter |
| FR-AFP-41 · L-24 | `pendingDelete` never cleared on Cancel/Escape — state leak in a destructive-confirm path (no functional consequence today; `askDelete` overwrites) | **F.W3/W4** — clear on close |
| FR-AFP-42 · C-D-18 (spec count corrected K7: **8** e2e specs, this seat's ls) | Zero props/zero emits/setup-top-level fetch — the seam that makes FR-AFP-11 inevitable and the component untestable in isolation; no e2e opens the tab | **F.W3/W4** seam; test seat → F.W9/W10 |
| FR-AFP-43 · NEW (α-miss 2) | The celebrated `role="status" aria-live="polite"` region is **created by v-if simultaneously with its content** (`:151-153`, `loading` inits false), so polite announcement is unreliable; the loaded/empty ends of the cycle carry no live semantics at all — an AT user gets no load-cycle feedback but the toast. Qualifies both axes' S-1 praise | **F.W3/W4** — persistent region whose text swaps; AT-behaviour residue → SS-13 |
| FR-AFP-44 · NEW (α-miss 4) | The empty state — production's **only** state per FR-AFP-1 — is carried by a glyph at `opacity-30` (1.48 light/1.70 dark), the same alpha-mute anti-pattern FR-AFP-20 indicts at 60%, applied harder, unmeasured by any axis (correctly `aria-hidden`, so not a strict WCAG failure — a legibility/canon defect) | **F.W1** — by-colour rung, not alpha |
| FR-AFP-45 · NEW (α-miss 5) | Icon semantics contradict effects: `XCircle` (reject glyph) = benign dismiss tinted green; `Star` = tier-raise tinted blue; no visible text label at any breakpoint; and the hover tints α measured are sub-3:1 in light (green-400 ≈1.59 — the worst figure in the file, on rows D-2 listed but never measured) | **F.W3/W4** — icon+label re-semantics; composes with FR-AFP-19 |
| FR-AFP-46 · NEW (α-miss 3) | FR-AFP-3's prescribed cure is insufficient at the worst rung: `--destructive` ≈4.38:1 on the composited light card — passes 1.4.11, fails 1.4.3 normal-text (10px pill count, reason labels). Cure must split: token for fill/glyph, `--muted-foreground-strong`/`--foreground` (or the Badge's owned pairing) for text | **F.W1** rider on FR-AFP-3 |
| FR-AFP-47 · NEW (β-miss 2) | A second closed-union widening one line from the field all three axes cite: `FlaggedVisualization.tier: string | null` (`types.ts` body, re-read) where `GalleryTier` is closed on both wire sides, already imported by this component (`:15`), rendered `:181` — passed over three times | **F.W3/W4** — same cure as FR-AFP-31, same file |
| FR-AFP-48 · NEW (β-miss 3) | "Load more" is gated on `hasMore` only — the `<nav>` is a **sibling** of the v-if/v-else pair, so during every mutation-triggered reload a spinner and an ENABLED Load-more (carrying the pre-mutation cursor) render simultaneously; the FR-AFP-14 corruption is reachable by ordinary clicking. Two-token interim cure (`hasMore && !loading`) vs the generation-token full cure | **F.W3/W4** — binds to FR-AFP-14 |
| FR-AFP-49 · D-21/L-26 (INFO) | Zero automated coverage of any kind: no test script, no vitest dep, 8 e2e specs with zero admin/flag hits — every row above incl. all four blockers is invisible to every gate the repo runs (vue-tsc sees exactly FR-AFP-2's `size="icon"` + FR-AFP-17's `ToastVariant`) | **F.W9/W10** — census risk #10 instance, fold by reference |
| FR-AFP-50 · L-25 (INFO) | Both of this component's loops are native-`<div>`-hosted — invisible to a component-callsite-keyed deriver; extends **R5-7/R6-6** by reference (fold, never re-book); the 33-site classification is live-reproducible, the 16-vs-17 registry reconciliation is not from here | **F.W4** board carry; registry half → UNPROVEN residue |
| FR-AFP-51 · L-27 (INFO) | The census's ToastVariant break is booked against the uplift TARGET, not the current tree — at installed 4.0.0 the symbol resolves; correct in context, recorded so no reader mistakes tense | record only — NO-WAVE-OWNER |
| FR-AFP-52 · NEW (α-miss 6, INFO) | The D (design) axis never absorbs FR-AFP-1, so its severity ladder is calibrated for a surface that renders one state — its latent rows (D-4, D-10, D-17) are graded as live while the two findings that fire on every production render (role=list, the opacity-30 glyph) sit at MINOR/unmeasured. A corpus-composition observation for the SS-3/SS-4 spec authors, not a defect in any row | NO-WAVE-OWNER — spec-authoring input |
| FR-AFP-53 · NEW (α-miss 7, INFO) | `title` vs `aria-label` divergence on all three actions (benign content, separately authored, can drift) | folded into FR-AFP-34 |
| FR-AFP-54 · NEW (β-miss 4, re-graded MINOR→INFO per corrections register #8) | Runtime-imported deps in devDependencies (lucide-vue-next + glass-ui's declared peers reka-ui/cva/clsx) while vue/pinia/glass-ui sit in dependencies — band inconsistency, real; the claimed "production install cannot build" failure mode is a pipeline that does not exist | **F.W0** substrate — manifest banding |
| FR-AFP-55 · NEW (β-miss 5, INFO) | The pinned substrate is outside its own peer range **today**: installed glass-ui@4.0.0 peer-declares `@mkbabb/value.js ^0.10||^0.11` vs the app's `^0.13.0` pin, and `@lucide/vue ^1.16` vs imported `lucide-vue-next` (both verified at the manifest by this seat) — the "clean 4.0.0 pin" premise of the tri-bump sizing understates the start state; component exposure nil (zero value.js imports here) | **F.W0** substrate — recorded into F.W1's sizing |
| FR-AFP-56 · NEW (β-miss 6, INFO) | The flag sub-loop keys on array index (`:187-188`) — benign only under wholesale remount; the splice cure all three axes prescribe **activates** it; a stable natural key exists (`(content_hash, reporter_slug)` unique) | rider bound into FR-AFP-15 |
| FR-AFP-57 · NEW (β-miss 7, INFO) | `handleSetTier` is dead generality — typed over the full union, one call site, one literal, duplicating a store action with the same signature plus the invalidation it drops | folded into FR-AFP-11 (its sharper form) |

## Killed-claims register — 15 entries: 14 SUSTAINED · 1 REFUTED by this seat

| # | killed claim | adjudication |
|---|---|---|
| K1 | C:S-2 + C §3: "this file's whole 4→7 uplift exposure is one line (`:276`)" | **SUSTAINED** (both readers convergent; producer re-read by this seat: `variant` absent, `ButtonSize` excludes "icon") — nine breaking prop sites; the corpus's most consequential error (would under-size F.W1). C's §3 table also omits its own inherited ToastVariant break |
| K2 | C:D-14: "six `size=\"icon\"` buttons"; "`#fca5a5` ⇒ ≈1.90:1 vs white"; (β's addition) "icon-sm unverified" | **TWO HALVES SUSTAINED** (three sites, this seat's read; v4 oklch at `theme.css:13`, re-read) · **ONE HALF REFUTED** — `icon-sm` IS established at the CVA bytes = 28px fine-pointer ≡ h-7 + coarse clamp (ruling 4). C-14's load-bearing contribution (the primitive exists at the pin) survives |
| K3 | C:D-13's cure "`:show-close=\"false\"`" | **SUSTAINED** — `showClose` is gone at 7.0.0; the spelling is `dismiss="deliberate"` (producer `:39/:47/:147`, re-read by this seat). Defect stands (FR-AFP-37) |
| K4 | D:D-10's falsifier: "GalleryCard's raise-a-flag affordance" | **SUSTAINED** — `GalleryCard.vue` flag hits = **0** (this seat's grep); no flag-raising UI exists anywhere in web/src; the phantom would, if believed, kill FR-AFP-1. D-10's core unaffected |
| K5 | D:D-12: "18 PRM references" | **SUSTAINED** — 17 raw (this seat's count), 3 in comments → 14 real; internally inconsistent with its own body ("13 other sites"). Substance separately confirmed |
| K6 | D §7 ledger: pill cure "—" at 4.0.0, "improve" only at 7.0.0 | **SUSTAINED** — `./badge` in the 4.0.0 exports keyset with `variant: destructive` + `size: sm` (d.ts re-read by this seat): the file's worst-contrast row is curable **today**; deferring it to F.W1 is a scheduling error |
| K7 | C:D-18: "web/e2e/ has 7 specs" | **SUSTAINED** — 8 (this seat's ls); zero-admin-hits half confirmed |
| K8 | D header pin + D:S-5 + C:S-2's "survives 4→7 untouched" | **SUSTAINED** (ruling 8) — the file is NOT at the pin; the one dirty hunk IS `:265` variant→surface; at `cd26c65`, `variant="opaque"` is a stray attribute and the destructive confirm renders on the default glass surface. β's D-S5-CONFIRMED cell inherits the correction |
| K9 | D:S-6's comparison: "GalleryCardModal … a hand-rolled roleless `<div>`, DA-design-A3 #5 still live" | **SUSTAINED** (β's kill; deciders re-read by this seat, ruling 9) — it is a real glass Dialog since D.W4.c; the prior-audit row was re-asserted without re-verification. Primary half of the superlative survives |
| K10 | L-2's BLOCKER premise / D-20's "irrevocable is accurate": the delete is irreversible | **SUSTAINED** (ruling 2) — soft-by-default + restore route, verified end-to-end; the sharper defect is the inverse (the copy promises permanence the operation lacks, FR-AFP-9) |
| K11 | L-12's headline: "bypasses the repo's pagination seat" | **SUSTAINED** — `useOffsetPagination` presupposes a server `total` this endpoint never emits; the refusal is superlative S-5; the narrow duplication residue survives as FR-AFP-38 |
| K12 | **reader-β's OWN kill** of L-19's enumeration ("seven SFCs, three outside the directory") | **REFUTED BY THIS SEAT** (ruling 1) — the three paper files carry real rules; the predicate re-run yields exactly 4. The only register entry where a reader's kill, not a challenge's claim, dies |
| K13 | L-1's evidence enumeration: "the only 'flag' hits outside this file are two" | **SUSTAINED** — 11 files match the stated command (this seat's grep), all benign; the conclusion is untouched, the enumeration was irreproducible as written |
| K14 | D:D-9: "its FOUR success toasts" | **SUSTAINED** — three (`:93/:105/:120`, this seat's grep); α's claim cell repeated the four while its proof enumerated three (ruling 7) |
| K15 | L-5's vs C:D-7's quoted abort strings ("The user aborted a request." vs "signal is aborted without reason") | **SUSTAINED** — mutually exclusive and neither statically decidable: `abortable()` calls `.abort()` with no reason, so the DOMException message is engine-defined. Mechanism confirmed; literals quarantined → SS-13 |

## Superlatives — 11 sustained (L-18 runs both ways; two qualified)

1. **Entity-interpolated accessible names on every control; ten of ten glyphs `aria-hidden`** — re-verified against the sibling by this seat: `AdminAuditLog.vue` carries **0** `aria-` attributes (grep). "Delete entry sunflower-3f2, button", not "button, button, button". *Qualified twice*: the labels inherit FR-AFP-8's identity defect in content, and the live-region half of the old praise is FR-AFP-43's defect — the spinner region is v-if-created with its content. (D:S-1/S-3 · C:S-1 · β cells, merged.)
2. **`min-w-0` on the flex child (`:169`)** — the most-omitted flex idiom, correct first try; the reason `truncate` ellipses instead of blowing the row out.
3. **The append path (`:58-71`) is the in-file cure for three of the worst findings** — re-entry guard, separate non-swapping flag, `.push`, `:disabled`, textual busy state. The correct pattern and the wrong pattern live thirty lines apart.
4. **The load-more Button is touch-conformant by omission** (α's S-D, adopted; consistent with this seat's CVA read: `sm → h-(--control-h-sm)`, no override) — the file contains its own proof that FR-AFP-24's override is unnecessary.
5. **The cursor-envelope migration is complete client-side and the shared offset composable was correctly REFUSED** — reusing it would have required fabricating a `total` the server never sends; the refusal is documented in situ (`:238-240`). The client finished a migration its own server left half-done (dead offset models still standing, FR-AFP-36).
6. **`surface="opaque"` on a destructive confirm is the right design judgment** — the content you are about to destroy is not legible through the dialog asking if you are sure — ***qualified by K8***: true only of the working tree; at the audited pin the prop is `variant=` and inert.
7. **`text-admin-label` consumes the real semantic typography utility** where the sibling hard-codes `text-[0.65rem]` — coexists with FR-AFP-26's correct complaint that 10px is the wrong *rung* for decision data.
8. **NEW (β, verified at the bytes): privacy-by-projection** — `db.visualizations.find(query, {"liked_ips": 0})` in the flagged listing (re-read); the operator surface never receives IP material it has no use for, and `_public_doc` strips the same field elsewhere: a rule, not an accident.
9. **NEW (β, verified): every remedy is self-recording, one tab away** — five distinct `log_audit` actions incl. the idempotent no-op (`set_tier:{tier}`, `delete:hard`, `delete:noop`, `delete`, `dismiss_flags`, all re-read), surfacing in the sibling AdminAuditLog tab: a closed loop.
10. **NEW (β, verified): the endpoint's hot path is its correct path** — `if not flagged: return` short-circuits before building the `$in`, so the empty-queue case — production's only case per FR-AFP-1 — skips the unbounded join FR-AFP-33 correctly faults.
11. **The admin token is read fresh at each callsite, never captured at setup** — four for four; orthogonal to FR-AFP-25, which faults the `!`, not the freshness. **Method note held with it**: all three challenge axes ran and recorded their own falsifiers (L killed six of its own hypotheses; D argued against its own D-12), and the corpus's error rate concentrated in secondary enumerations and comparanda, not primary mechanisms — the same distribution this adjudication found in the readers.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **Lived severity of FR-AFP-3 in light mode** — ratios are triple-derived (challenge + both readers, ±0.08); whether ~1.3:1 reads illegible or merely faint wants one light-theme screenshot.
2. **FR-AFP-43's AT behaviour** — the DOM-insertion mechanism is source-certain; whether NVDA/JAWS/VoiceOver actually suppress the announcement is AT-dependent. One screen-reader trace.
3. **FR-AFP-5's focus destination** — unmount certain; that `activeElement` lands on `<body>` (vs reka retargeting) wants one keyboard trace; the Dialog path (close → return-focus → reload destroys the trigger) is the interesting one.
4. **FR-AFP-33 at scale** — the 60/500-flag card geometry and card-vs-page scroll need a seeded fixture, which per FR-AFP-1 **cannot be produced through any product path** (fixture required).
5. **Dialog overflow at `max-w-sm`** with an unhyphenated `font-mono` slug and no `break-all` at `:270` — slug-length distribution unknown.
6. **JSC/Safari acceptance** of the space-separated 6-digit-fraction datetime (`json.dumps(default=str)`); V8 measured; a rejection renders "NaNm ago" per row + per flag.
7. **The literal abort-toast string** (K15) — one console trace on the double-click path; mechanism needs no live check.
8. **The unscoped `$group`'s magnitude** — shape source-certain, wall-clock not static; moot until a producer exists.
9. **L-25's 16-vs-17 reconciliation** against the R6 DERIVED-REGISTRIES artifact (unreadable from here); the live 33-site classification is reproducible, the registry half is not.
10. **Built-stylesheet PRM grep** (FR-AFP-29) and **the `--ui-glyph` packaging question** (FR-AFP-28 — producer gap or intent; emitted in no shipped sheet, established; the *why* belongs to the glass-ui BH relay).

## Closing verdict

Both hostile re-reader returns **survive the incredulity seat, but asymmetrically**: reader-α's cells were exact wherever this seat re-derived them (its one slip a claim-text count its own proof contradicted), and both of its admitted MAJOR misses re-executed cleanly on this seat's own instruments; reader-β contributed the corpus's three best new superlatives and the seam-level abort finding — and also the corpus's one **killed kill** (L-19's enumeration, refuted by re-running the predicate rather than the substring), one config-contingent "strengthening" struck, one cure-kill half refuted at the CVA bytes, and one silent demotion reversed. The subject's adjudicated shape: **a genuinely well-crafted accessibility-and-cursor skeleton wrapped around a moderation surface that cannot moderate** — no producer writes its queue (FR-AFP-1), no reachable action removes the reported artifact (FR-AFP-4), its entire palette is one-theme raw Tailwind about to be structurally invalidated by an uplift that also breaks all six of its Buttons (FR-AFP-2/-3), and its identity, permanence, and dequeue semantics each say something the operations do not do (FR-AFP-7/-8/-9/-10). Routing: **F.W1** owns the nine-site Button re-grammar, the re-ink with its split cure, ToastVariant, the touch-floor restoration, and the dialog-preset spelling; **F.W3/W4** owns the reload/focus/error/abort/store-delegation family and the hygiene tail; **F.W5-W8** owns the producer decision, the artifact-remediation contract, content-hash identity, queue-size, and the twin-drift; **F.W0** the manifest bands; **F.W9/W10** the zero-coverage gate; four rows fold by reference into standing census/intake identities (R3-7a/R3-7c/R5-7/R6-8) and two INFO rows stand as spec-authoring input with NO-WAVE-OWNER. Nothing lost: head, tail, or interval — every challenge row, every reader kill, every miss, and both readers' dissents leave under exactly one disposition.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; the X·F specs are still forming and consume this roster as agglomeration input (M-25); execution awaits the owner's begin-word.
