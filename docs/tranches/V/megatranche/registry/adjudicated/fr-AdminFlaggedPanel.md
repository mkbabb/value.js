claude-fable-5

# fr-AdminFlaggedPanel — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: `/Users/mkbabb/Programming/fourier-analysis` @ HEAD `cd26c65` (re-verified this session; 28 dirty rows incl. the subject — the subject's sole uncommitted hunk is `:265` `variant="opaque"` → `surface="opaque"`, re-diffed by this seat TODAY: still uncommitted) · subject `web/src/components/visualization/gallery/AdminFlaggedPanel.vue` (285 L, read whole) · **Registry tree**: value.js `tranche-u`.
**Name-collision guard**: the banked `adjudicated/AdminFlaggedPanel.md` (2026-08-03) is the **value.js demo** component `demo/palettes/browser/admin/AdminFlaggedPanel.vue` — different product, different tree, zero shared rows; no fold in either direction.
**Supersession**: this record REPLACES the same-path draft of 2026-08-07T13:33, which adjudicated a **different re-reader pair** (its β killed L-19 claiming seven files; this corpus's β confirms L-19 at four — incompatible returns, so a different run). The registry directory is untracked, so overwrite would silently destroy that draft's unique rows; per zero-drop they are **carried below with attribution** (§Supersession register), and its two rulings this seat REFUTED at the bytes (its FR-AFP-24, FR-AFP-29) are retired there with rationale. FR-AFP ids are kept stable wherever the defect is the same; new rows take ids 58+.
**Corpus adjudicated**: two hostile re-reader returns — **reader-α** (design-and-usage: 29 confirmation cells · 5 kills · 8 misses · 9 superlatives · 9 unproven) and **reader-β** (library-and-code: 47 confirmation cells · 8 kills · 9 misses · 9 superlatives · 9 unproven) — over the component's three challenge files, all read **whole** by this seat: `challenge-D-design.md` (365 L, Opus 5 [1m], 22 rows) · `challenge-L-library.md` (229 L, Opus 5 [1m], 27 rows) · `challenge-C-consumption.md` (544 L, Opus 5 [1m], 20 rows; its internal ids are also "D-n" — cited here as C:D-n).
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, every severity contest, and every NEW missed row was re-derived against the bytes before adoption. The corpus's one head-on reader contradiction (D-12) was settled by this seat at the source sheet AND the compiled sheet — **reader-β's confirmation is refuted** (ruling 1). One reader cure was refuted at the same file that proves its defect (ruling 3). Zero browser probes (probe parsimony); every ruling is decidable from source + installed dist + producer src + git. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings are agglomeration input (M-25); nothing here stamps a wave.
**This seat's own instruments (all read-only; sole write = this file)**: full read of `AdminFlaggedPanel.vue` (285 L — every cited anchor checked); fourier `git log`/`status`/`show HEAD:` (the pin decider: HEAD carries `variant="opaque"` at `:265`); installed glass-ui **4.0.0**: `dist/styles/utilities/a11y-overrides.css` read at `:1-40` and `:75-125` (**the D-12/D-5 decider**: blanket PRM `*:not([data-allow-motion]) { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important }` + a second arm re-applying the kill to `[data-allow-motion]` ("accessibility is absolute") + `@media (pointer: coarse) { [data-size="icon"] … min-block-size/min-inline-size: var(--touch-target, 2.75rem) }` with the comment "min-block/inline-size only RAISES"), `dist/button-*.js` (`"data-size": r.size` reflection; CVA `icon`/`icon-sm` → `h-(--control-h-md/xs)`; base carries `focus-ring` + `text-[length:var(--control-text)]`; `ghost` sets `text-foreground/70`), `dist/components/ui/toast/use-toast.d.ts:2` (**the D-9 decider**: `ToastVariant = 'default'|'destructive'|'success'|'warning'|'info'` AT THE PIN), `DialogFooter-*.js` (`flex flex-col-reverse sm:flex-row sm:justify-end sm:gap-x-2` — no col-direction gap), `dist/styles/theme/radius.css` (`--radius-lg` = 10px vs `--radius-card` = `--radius-2xl` = 16px), `badge/index.d.ts` (variant incl. `destructive`, size `sm|md|lg`), exports keyset by node (`./card` ✓ `./badge` ✓ `./expandable-container` ✓ `./confirm-dialog` ✓ **`./chip` ✗**), `package.json` peers (`@mkbabb/value.js ^0.10||^0.11`, `@lucide/vue ^1.16`); **compiled sheet** `web/dist/assets/index-57FkGzlZ.css` (contains the blanket `:not([data-allow-motion])` PRM rule AND the `[data-size=icon]` coarse floor — both greps this seat's); producer glass-ui **7.0.0** src: `button/Button.vue` (`variant` gone; `ButtonSize = Extract<Size,"xs"|"sm"|"md"|"lg">` `:16`; `loading?` `:27`; `:data-control-target="iconOnly ? '' : undefined"` `:92`), `styles/utilities/responsive.css:3-8` (`[data-control-target]` coarse floor), `dialog/DialogContent.vue` (`dismiss?: DialogDismiss` `:39`, default `"free"` `:47`, `closeVisible = dismiss === "free"` `:147`; `showClose` gone); tailwindcss 4.3.1 `theme.css:13-15` (red-300 = `oklch(80.8% 0.114 19.571)`); `api/routers/admin.py` `:85-96` (`_json` = `json.dumps(default=str)`) `:160-190` (set_tier: `$set {tier, updated_at}` only, unconditional, audited) `:505-612` read whole (unbounded `$push` $group · `if not flagged` short-circuit · `{"liked_ips": 0}` projection · `content_hash` emitted / `image_slug` via `doc.get` · hash-scoped `delete_many` · `dismissed: 0` reachable) `:652` (`AuditListResponse(...)` — the dual-serialization decider); `api/lib/crud/softdelete.py:1-45` (docstring: "the cron handles hard-delete cascade via `pinned_cron.cron_prune`"; `soft_delete` "Does NOT cascade") + `pinned_cron.py` flags grep → **0** + `services/janitor.py` (grace hard-delete `delete_many({"deleted_at": {"$lt": grace_cutoff}})` with **no flags cascade**; the only flags cascade is reporter-keyed stale-user) — **the β-miss-1 deciders**; `web/src/lib/api.ts` `:45-75` (inflight Map; `abortable` never deletes; `abortInflight` `:61-66` = the sole deleter) `:161` (`signal ?? abortable(abortKey)`) `:245-254` (`coreFetch<T>(path, /* abortKey */ path, …)` verbatim) `:506-561` (all four flag wrappers; `hard = false`); `useToast.ts:1-19` (VARIANT_MAP success≡info≡"default"); `types.ts:96/:137-170` (GalleryTier closed; `tier: string | null`; FlaggedVisualization omits `content_hash`); `api-problem.ts:19-30` (`super(title)` — message = title only); `useOffsetPagination.ts` error at `:31/:48/:75`; `index.html:21-32` (**light is the fallback default** — `.dark` added only on saved-dark or OS-dark); `GalleryView.vue:32/:57/:205/:380-382`; sibling bytes: `AdminAuditLog.vue:54-56` (NaN guard) `:87` (`rounded-lg`) `:119-121` (roleless spinner), `AdminUserList.vue:223/:376/:390` (timeAgo · `text-red-400` pill · `h-6 w-6`), `GalleryCard.vue:53-56/:73-77/:147-153/:226-236` (**the tier-language decider**: "just now" dialect · `data-tier` · Crown/Bookmark chips on `--tier-featured/saved` · border+glow), `GalleryCardModal.vue:85-91` (capitalize tier badge), `GalleryCard.vue` flag grep → **0**; censuses re-run: `function timeAgo` → **5** · zero-rule `<style scoped>` sweep → **exactly 4**, all gallery dir · gallery-dir `focus-visible` → GalleryCard only · e2e = **8 specs**, `admin|flag` grep → **0** · `web/package.json` (no test script, no vitest; `lucide-vue-next` in devDependencies). Registry cross-reads for identity guards: `fr-AdminAuditLog.md` (**AA-2** Button-re-grammar census extension 37 files/38/124 · **AA-8** abort-key seam · **AA-27** inflight no-reap · **AA-28** `getAdminToken()!` idiom · **AA-29** 4-file scoped sweep · **AA-44** zero coverage) and `fr-AdminUserList.md` (**FR-AUL-3** R6-8 abort-identity class · **FR-AUL-7** light-mode red literals family · **FR-AUL-10** ToastVariant · **FR-AUL-16** coverage · **FR-AUL-17** timeAgo family · **FR-AUL-19** icon-ladder bypass incl. the coarse-floor mechanics · **FR-AUL-24** focus-to-body family — whose instruments independently corroborate the a11y-overrides PRM + touch-floor bytes).

## Scoreboard

**60 adjudicated roster rows — 4 BLOCKER · 22 MAJOR · 25 MINOR · 9 INFO.** **15 killed-register entries**, including the corpus's one **reader-cell kill** (reader-β's D-12 confirmation, refuted at the bytes) and two **supersession refutations** against the same-path prior draft. **17 reader-missed rows adjudicated**: 16 admitted (4 as new MAJORs, all mechanism-re-executed by this seat), 1 resolved to an already-ruled identity (β-miss-3 → AA-27, the anti-rename law). **10 superlatives sustained** (two qualified — one by the pin provenance, one by the live-region defect). **6 adjudicator corrections against the readers**, 2 against the challenge corpus, 2 against the superseded draft.

Blocker-weight survivors of record: **FR-AFP-1** (the moderation queue has **no producer** — nothing in production writes `db.flags`; production's only reachable state is the empty state), **FR-AFP-2** (all six `<Button>` callsites break at glass 4→7 — three hard `vue-tsc` errors + six silent `variant` fallthroughs, the worst being the confirm dialog's Delete degrading to look identical to Cancel; the Button re-grammar break class is the banked **AA-2** census-extension identity, folded by reference), **FR-AFP-3** (the panel is unshipped in light mode — the app's fallback **default** theme per `index.html`; the card outline fails 1.4.11 in **both** themes), **FR-AFP-4** (no action the panel offers takes the reported artifact offline: zero image DELETE in the 45-op surface, unauthenticated public-cached blob GET, and every blob GET bumps the janitor's recency predicate). Roster-wide conditional, ruled once (ruling 5): severities grade the **populated surface**; FR-AFP-1 gates live reachability of the whole moderation band, and F.W5-W8 must rule producer-or-retire before F.W1 sizes off these grades.

## Ruled disagreements and corrections — the incredulity seat's own work

1. **D-12 — the corpus's one head-on reader contradiction, RULED FOR READER-α at the bytes; reader-β's confirmation is REFUTED.** α killed D-12 citing a blanket PRM reset in glass 4.0.0 `a11y-overrides.css:6-16`; β confirmed D-12 asserting "there is no global `* { animation-duration }` reset in the imported sheets." This seat read the file: the blanket reset EXISTS exactly as α quotes (`*:not([data-allow-motion]) { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important }`, plus a second arm re-applying the kill to `[data-allow-motion]` elements — "accessibility is absolute"), it is imported via `utilities.css` ← the `styles` export ← `style.css:3`, and it is **present in the compiled sheet** `index-57FkGzlZ.css` (`:not([data-allow-motion]){…animation-duration:.01ms!important…}` — this seat's grep). β read `transitions.css` (whose PRM bracket does enumerate named classes — that half of β's cell is true) and stopped one file short, then repeated challenge-D's own "no global reset anywhere in the imported sheets" — a citation-inheritance event of the class the ColorSpaceSelector record predicted and pt-leaf recorded. **Consequences, all adjudicated here**: D-12 is KILLED as filed (K6); α's missed row M-3 — under PRM the panel's only visual loading affordance freezes into a static ¾ ring — is ADMITTED (FR-AFP-58); α's partial refutation of D-5's touch sub-claim is SUSTAINED (ruling 2); two rulings of the superseded draft flip (K14/K15). Two banked sibling adjudications' instruments (fr-AdminUserList, fr-AdminAuditLog) independently corroborate both a11y-overrides facts.
2. **The coarse-pointer touch floor — reader-α's account VERIFIED end-to-end; the superseded draft's contrary MAJOR retired.** `a11y-overrides.css:113-118` raises `[data-size="icon"]` to `min-block/inline-size: var(--touch-target, 2.75rem)` under `pointer: coarse`; the rule keys on the **attribute** Button reflects (`"data-size": r.size`, dist chunk), not on a class, so tailwind-merge stripping the CVA `h-(--control-h-*)` classes (real — the draft's cn run was correct) is irrelevant to the floor: `min-*-size` "only RAISES" over the `h-7 w-7` used size, and the rule survives into the compiled sheet. So the three row actions are **28px fine-pointer (passes 2.5.8's 24px) / 44px coarse (passes 2.5.5)** — α's D-5 correction stands, and α's D-1 CARRY is verified at the 7.0.0 bytes too: the uplift cure `iconOnly` preserves the floor because 7.0.0 re-keys it to `[data-control-target]` (`responsive.css:3-8`), which `Button.vue:92` sets exactly when `iconOnly`. FR-AUL-19's banked mechanics agree. The superseded draft's FR-AFP-24 ("floor defeated in both pointer classes", MAJOR) is REFUTED and retired (K14).
3. **α's M-3 cure is REFUTED by the same file that proves its defect — the cure-is-adjudicated-separately law (ColorSpaceSelector addendum §2).** α proposed `[data-allow-motion]` on the spinner "paired with a slow non-spatial pulse." The second PRM arm (`a11y-overrides.css:~26-34`) re-applies `animation-duration: 0.01ms` to `[data-allow-motion]` elements precisely so no element can opt out — the escape hatch α reached for is welded shut by design. The only viable cure is the textual one the file already ships on its other path (`:259` "Loading…"): a visible text/skeleton busy state. FR-AFP-58 books the defect with the corrected cure.
4. **D-9's second half — reader-β's kill SUSTAINED; reader-α's cell inherits the correction.** Installed 4.0.0 `use-toast.d.ts:2`: `ToastVariant = 'default' | 'destructive' | 'success' | 'warning' | 'info'` — `success` exists **at the pin**; `useToast.ts:9-13` simply declines it (success→"default"). D-9's falsifier fired on its own terms. α confirmed D-9 wholesale including its "the cure is an IMPROVEMENT (`tone: "success"`), not a rename" framing — corrected: the success-face cure needs **no uplift**; deferring it to F.W1 is a scheduling error. The first half (success≡info today; no positive-outcome signal on the moderation surface) is confirmed and books as FR-AFP-17.
5. **The producer conditional — reader-β's board flag ADOPTED as a roster-wide ruling; the L-1/L-2 dual-BLOCKER incoherence resolved by it.** β is right that the L axis cannot carry two BLOCKERs when its first proves the second unreachable, and right that D-2/D-3 silently inherit the same conditionality. Ruling: severities in this roster grade the populated surface; the conditional is stated ONCE here, not per-row; L-2 is MAJOR (β's demotion sustained, K10), FR-AFP-1 is the band's sole reachability gate, and the F.W5-W8 producer-or-retire decision must precede F.W1's sizing where grades are load-bearing.
6. **D-3/L-7 severity — RULED MAJOR (β's grade); α's BLOCKER preserved as dissent.** The v-if/v-else subtree destruction and absent restoration are source-certain (sole vue import is `ref`; no nextTick/focus call; `GalleryView.vue:380-382` bare v-if, no KeepAlive), and α's operability argument (single-use-per-keyboard-session, WCAG 2.4.3) is real — but the cure is in-file thirty lines above (`loadingMore`), reachability is zero until FR-AFP-1's producer exists, and the sibling family row (FR-AUL-24) is banked MAJOR. Consistency wins.
7. **D-2 severity — RULED BLOCKER-class (α's grade); β's MAJOR preserved as dissent.** Deciders re-read by this seat: light is the shipped fallback **default** (`index.html:21-32` adds `.dark` only on saved-dark or OS-dark — α's strengthening, verified); the card outline fails 1.4.11 in BOTH themes; the failing pill is the queue's ranking datum. Arithmetic is adopted as double-derived (challenge + α to 2dp on 3 of 4 rows; β independently ≈1.38/1.33 on the headline pair; the pill-row delta 1.31-vs-1.39 is compositing-order, verdict-immaterial — K13). Tailwind v4 oklch red-300 re-read at `theme.css:13` (K2's hex correction stands).
8. **Reader-β's miss-3 (inflight registry never pruned) — resolved to the ALREADY-RULED AA-27, the anti-rename law.** β filed it as net-new ("all three axes read `abortable` and none noticed"); true of this corpus's axes, but the identity is banked at fr-AdminAuditLog **AA-27** (ruled MINOR, cure `finally { inflight.delete }` + unmount abort, → F.W4). Cross-booked, never re-booked; this file's NEW fact folds in as the amplifier: the cursor loop mints a **never-repeating key per page** (`?limit=20&cursor=<opaque>`), so growth here is unbounded per-session, not per-vocabulary. The seam-level two-conventions row is likewise the banked **FR-AUL-3 / AA-8 / R6-8** identity — fold by reference (FR-AFP-12/-14 book only this component's instances).
9. **β's miss-1 (orphan flag accumulation) — ADMITTED, MAJOR, mechanism re-executed.** `softdelete.py:3-5` promises "the cron handles hard-delete cascade via `pinned_cron.cron_prune`" — `pinned_cron.py` contains **zero** flags references (this seat's grep); the janitor's grace hard-delete (`delete_many({"deleted_at": {"$lt": grace_cutoff}})`) cascades nothing; the only flags cascade anywhere is reporter-keyed (stale users). So the UI's own Delete → soft-delete → janitor hard-delete pipeline strands every flag forever, feeding the unbounded `$group` (L-22's permanent-growth term) — and the module docstring is false for this band. FR-AFP-66.
10. **β's miss-2 (reload() swallows its own errors) — ADMITTED, MAJOR, re-derived by construction.** `:46-55` handles and discards every error and never rethrows, so `await reload()` at `:94/:106/:121` cannot reject — the handlers' catches are structurally dead for refresh failures, and each handler toasts SUCCESS **before** reloading. Composite verified against the source: DELETE succeeds → "Entry deleted" → reload 500s → reload's own error toast → `flaggedEntries` never reassigned → **the deleted row remains rendered with live buttons under a success toast**. The reason FR-AFP-6 and FR-AFP-16 compose into a wrong-state list. FR-AFP-67.
11. **α's miss-1 (the tier renders as a raw wire token) — ADMITTED, MAJOR, deciders re-read.** `:181` `{{ item.tier ?? "normal" }}` — lowercase, unglyphed, 10px muted — while the product's tier design language is real and adjacent: `GalleryCard.vue:147-153` Crown/Bookmark chips on `--tier-featured/--tier-saved`, `:75` `data-tier`, `:226-236` border+glow; `GalleryCardModal.vue:85-91` a `capitalize` badge. The type layer widened identically to `reasonLabel`: `GalleryTier` closed at `types.ts:96` and imported at `:15`, yet `FlaggedVisualization.tier: string | null`. The MAJOR-making composition: per FR-AFP-10 the Save action does not dequeue, so after Star the **only** on-screen change is that one 10px word — a sub-perceptual confirmation for the panel's sole non-destructive remedy. FR-AFP-59 (the widening limb absorbs the superseded draft's FR-AFP-47).
12. **α's miss-2 (mobile confirm renders Delete ABOVE Cancel, flush) — ADMITTED, MAJOR, byte-verified.** DialogFooter's shipped class string is `flex flex-col-reverse sm:flex-row sm:justify-end sm:gap-x-2` (dist chunk, this seat) — DOM order Cancel(`:275`)→Delete(`:276`) becomes visual order Delete-on-top under 640px, with **no** col-direction gap declared, composing with the confirmed stray close-X (FR-AFP-37): X · DELETE · Cancel, the irreversible action first-read and thumb-nearest. Flushness-in-situ → SS-13. FR-AFP-60.
13. **Pin provenance — re-verified TODAY and held against both readers' cells.** Both readers cite `:265` as `surface="opaque"` and neither ran git: at HEAD `cd26c65` the file says `variant="opaque"` — not a 4.0.0 DialogContent prop (the d.ts's own comment names the retired binary) — so at the audited pin the prop is a stray attribute and the destructive confirm renders on the default **glass** surface. The working-tree hunk is real and STILL uncommitted. Every "survives 4→7 untouched" superlative describes the working tree; F.W1 planners must treat the one-hunk fix as unlanded. (Carried from the superseded draft's K8; independently re-derived.)
14. **Smaller corrections.** (a) challenge-D §7's pill-cure home "`./chip` / `./badge`": `./chip` is **absent** from the 4.0.0 exports keyset (this seat's node run; present at 7.0.0 per fr-AdminUserList) — the at-pin home is `./badge` alone, which α's miss-4 correctly routes (K12). (b) α's D-9 claim text repeats the challenge's "four success toasts"; the file has **three** (`:93/:105/:120`) — verdict unaffected. (c) Tooltip census: α says 34+shim=35, β says 35-across-9 exact (CoefficientsSpectrum 1 vs 2) — ±1, both land the R3-7a figure, immaterial (K13). (d) β's C-D-18 cell corrected C's "7 specs" to 8 — this seat's ls concurs (K11).

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5-W8** CRUD/provenance union · **F.W9/W10** · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring. Line anchors = working tree at `cd26c65`+1 hunk; re-resolve before any cure (the D-19 lesson). Identity guards: rows extending banked identities (**AA-2, AA-8, AA-27, AA-28, AA-29, AA-44, FR-AUL-3, FR-AUL-7, FR-AUL-10, FR-AUL-16, FR-AUL-17, FR-AUL-19, FR-AUL-24**; census FE §5, risk #10; intake **R3-7a, R3-7c, R5-7, R6-6, R6-8**; prior-audit DA-design-A3 #7/#8; lane-crud `POST /:slug/flag`) **fold by reference — never re-booked**.

### Blockers

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **FR-AFP-1** | L-1 / C:D-1 | The moderation queue has **no producer**: two admin routes (GET `/flagged`, DELETE `/{slug}/flags`) are the collection's entire live surface; every `db.flags` write repo-wide is a test fixture or a `$rename` migration; `FlagRequest` is declared and referenced nowhere; no client posts a flag; no UI renders a report affordance (`GalleryCard.vue` flag grep → 0, this seat). Production's only reachable state is `:232-235` "No flagged content"; ~110 of 285 lines dead by reachability | β's route/insert census verified against `admin.py:509/:590`; `FlagRequest` at `models/admin.py:57` | **F.W5-W8** — build the user half (`POST /visualizations/{slug}/flags`, the mirror of lane-crud:336-339's value.js booking) or retire the admin half whole. Extends **R3-7c inverted** — fold by reference; the gap ledger carries both directions |
| **FR-AFP-2** | D-1 (+K1) | All six `<Button>` callsites break at glass 4→7: three `size="icon"` (`:200/:210/:220`) are **hard vue-tsc errors** (declared prop; `ButtonSize` excludes "icon" at producer `:16`); six `variant=` (`:199/:209/:219/:247/:275/:276`) are **silent fallthroughs** (no `strictTemplates`; corroborated in-tree by the already-fallthrough `title` attrs building green) — worst at `:276`, where the confirm's Delete degrades to default emphasis/tone, **visually identical to Cancel** on the one irreversible control. Cure is a three-axis re-grammar, not a rename (K1) | producer `Button.vue:15-31` re-read; installed `index.d.ts` declares both; α's fallthrough-mechanism corroboration adopted | **F.W1** — mechanical re-grammar (`ghost`→`emphasis="quiet"`, `destructive`→`emphasis="primary" tone="destructive"`, `size="icon"`→`iconOnly`+rung, floor-preserving per ruling 2). The census-gap identity (sixth break class; 37 files/38/124 blast radius) is **banked at AA-2 — fold by reference**, book only this file's nine prop sites |
| **FR-AFP-3** | D-2 / C:D-14 (severity per ruling 7; β's MAJOR as dissent) | The panel is unshipped in light mode — the shipped fallback **default** (`index.html:21-32`): hardcoded Tailwind reds (`:166/:172/:176/:191`, zero `dark:`, zero tokens) measure ~1.3–2.6:1 against the resolved light tokens (pill ≈1.31–1.39 — the queue's ranking datum), and the card outline fails 1.4.11 in **both** themes (1.33 light / 1.20 dark). Double-derived arithmetic adopted (ruling 7); v4 oklch red-300 re-read (K2) | sites re-read in source; `theme.css:13-15`; glass token decls per challenge, α/β concordant | **F.W1** re-ink on `--destructive`/tone + `--border` — **with FR-AFP-46's cure-split rider** (token for fill/glyph, strong-rung for text) — **and the FR-AFP-61 correction: `<Badge variant="destructive" size="sm">` is installed TODAY at the pin**, so the worst row needs no uplift; sibling family = FR-AUL-7 |
| **FR-AFP-4** | C:D-2 (+β's janitor sharpening) | No action this panel offers takes the reported artifact offline: Delete is a soft-delete of the *visualization*; `images.py` = 7 ops, **zero DELETE**; the blob GET (`:132`) has no `Depends` and ships `Cache-Control: public, max-age=86400`; and every anonymous blob GET ends in `touch_document`, bumping the janitor's recency predicate — actively-fetched reported content is precisely what never reaps. The panel prints that handle as the row title and inside the confirm copy | β's four-limb trace adopted; hard-arm zero-callers verified at `api.ts:518-532` (`hard = false`, no third arg at `:92`) | **F.W5-W8** — image remediation contract (delete/quarantine + blob visibility gate + janitor predicate). C's scope discipline preserved: soft-delete does de-list from browse |

### Majors

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **FR-AFP-5** | D-3 / L-7 (MAJOR per ruling 6; α's BLOCKER dissent preserved) | Every mutation sets `loading` and the spinner/list are v-if/v-else siblings (`:151/:156`), destroying the whole `role="list"` subtree incl. the just-activated Button; no restoration exists (sole vue import `ref`); the Dialog path is worse (reka restores to a trigger `reload()` then destroys). The correct shape is 30 lines above (`loadingMore`) | **F.W3/W4** — refreshing-not-destroying reload + focus restoration; destination residue → SS-13; family = FR-AUL-24 |
| **FR-AFP-6** | D-4 / L-9 / C:D-10 | The error state IS the empty state: failed load → transient toast → `:232-235` "No flagged content" — a false all-clear on a safety surface; no `error` ref; the only retry control is `v-if="hasMore"`, false after a failed first load; the correct shape ships in-repo (`useOffsetPagination.ts:31/:48/:75`, re-read) | **F.W3/W4** — explicit error + retry branch |
| **FR-AFP-7** | L-3 / L-4 (β/L only — α remit) | Flags key on non-unique `content_hash`: one artefact fans out to N rows each claiming the full count (3 rows × "3 flags" = apparent nine), and slug-labelled "Dismiss flags" is hash-scoped `delete_many` (`:607` region, re-read) silently clearing every sibling; `content_hash` is documented "never identity" | **F.W5-W8** — flag identity moves to the entity or dismiss scopes to slug; contract-level |
| **FR-AFP-8** | L-2 / C:D-3 (MAJOR per ruling 5/K10) | The destructive confirm names `image_slug` (asset FK, shared by every remix — `VisualizationRemix` declares no `image_slug`) while deleting `slug`, violating the identity law the file's own header states at `:18-23`; every human-readable surface renders the FK | **F.W5-W8** identity surface, with a **one-token F.W3/W4 rider** (pass `item.slug` as the label) |
| **FR-AFP-9** | C:D-5 | The dialog asserts irrevocability over a 30-day-restorable soft delete (restore route live at `visualizations.py:430`); soft-delete does NOT cascade flags, so a restored row returns to the queue still flagged; the purpose-built `?hard=true` grace-bypass has **zero callers** repo-wide | **F.W5-W8** — hard-arm caller decision + truthful copy (copy diction rides FR-AFP-35) |
| **FR-AFP-10** | C:D-4 (β only — α remit; re-verified) | "Mark acceptable (save)" never dequeues: `set_tier` writes `{tier, updated_at}` only (re-read `:160-190`); the flagged listing has **no tier predicate**; the in-file comment `:112-115` claims the opposite. The one non-destructive triage action is a no-op against its own queue. β's own kill of its 428-escalation hypothesis (If-Match `*` accepted) preserved | **F.W5-W8** — tier↔flag semantics decision; the false comment dies with the cure |
| **FR-AFP-11** | C:D-6 (+FR-AFP-57 fold) | Duplicates the gallery store's `deleteEntry`/`setTier` verbatim minus `resetAndFetch` (and minus the store's token guard), so `gallery.entries` keeps the deleted row and stale tier after moderation; the sole `activeTab` watcher clears selection only (`:205`, re-read) | **F.W3/W4** — delegate to the store or a shared invalidation channel |
| **FR-AFP-12** | L-5 / C:D-7 | Five unconditional `catch (e:any) → error toast` (`:51/:66/:95/:107/:122`) with zero `isAbortError` guards in a repo that guards at ~15 sites; `adminFetch`'s path-as-key (`:250`, verbatim re-read) makes same-path self-abort real (tab-toggle remount `:73`; double-click). Quoted abort literals quarantined (engine-defined) → SS-13 | **F.W3/W4** — guard the catches; the seam identity is **banked FR-AUL-3 / AA-8 (R6-8 class) — fold by reference** |
| **FR-AFP-14** | L-11 / C:D-8 (+FR-AFP-48 fold) | `reload()`/`loadMore()` have distinct abort keys (cursor in the query = the key), no generation token: a stale page-2 `push` lands on a fresh page-1 array and clobbers the cursor; duplicate `:key` reachable via post-deletion cursor shift. **FR-AFP-48 (superseded-draft carry, re-verified)**: the load-more `<nav>` is a SIBLING of the v-if/v-else pair, so during every mutation reload an ENABLED Load-more carrying the pre-mutation cursor renders beside the spinner — the race is reachable by ordinary clicking | **F.W3/W4** — generation token or shared key; interim `hasMore && !loading` gate |
| **FR-AFP-15** | D-8 / L-8 / C:D-9 (+FR-AFP-56 rider) | Every mutation discards all accumulated cursor pages (`:48` reassign after `fetchFlagged(null)`), contradicting the file's own header (`:28-29`); splice model exists at `gallery.ts:170-171`. Rider: the `:key="i"` flag sub-loop (`:188`) is benign only under wholesale remount — land the natural key (`(content_hash, reporter_slug)` is the unique compound) in the same change | **F.W3/W4** — splice-in-place + natural key |
| **FR-AFP-16** | D-6 / L-14 (MAJOR — β's composition grade adopted) | Three of four mutations have no in-flight guard and no `:disabled` while the correct pattern sits ten lines above (`:59/:250`); double-click fires duplicate mutations AND self-aborts into a spurious error toast (composes with FR-AFP-12) | **F.W3/W4** guards; the one-word primitive (`Button loading`, producer `:27`) lands with **F.W1** |
| **FR-AFP-17** | D-9 first half (+ruling 4's cure correction) | The panel's 3 success toasts (`:93/:105/:120` — count per ruling 14b) are indistinguishable from info TODAY (`VARIANT_MAP` success≡info≡"default", re-read): no positive-outcome signal on the moderation surface. **The cure is available AT THE PIN** (`ToastVariant` includes `'success'` at 4.0.0 — ruling 4); the `ToastVariant` 7.0.0 removal itself is the census FE §5 / **FR-AUL-10** identity — fold by reference | map success→"success" **now** (F.W3/W4-eligible); adapter rewrite rides **F.W1** |
| **FR-AFP-18** | D-10 (falsifier killed K4) | The moderation queue has no size anywhere in the product: no total in the envelope (no `count_documents`; `has_more` via limit+1), no `AdminStats.flagged`, six vanity MetricBadges and zero pressure metrics, bare tab label | contract → **F.W5-W8**; render seat → **F.W1** (metric-badge→metric migration) |
| **FR-AFP-19** | D-5 (folds DA-design-A3 #7, still live at identical lines — identity fold) | Destructive intent is colour-on-hover only: all three actions rest at `text-muted-foreground`, Delete 4px from confirm-less Dismiss, zero `focus-visible:` intent styling in the file (dir census: GalleryCard only). α's two corrections bound in (ruling 2): the CVA base's `focus-ring` + forced-colors outline mean the missing thing is the SEMANTIC differentiation, not the indicator; and touch targets are 44px via the coarse floor — the 28px figure is fine-pointer-only (K5) | **F.W1** rest-state tone + **F.W3/W4** focus-visible rider |
| **FR-AFP-20** | D-7 | `opacity-60` on `text-muted-foreground` (`:193`) fails AA in both themes (3.38 dark / 2.34 light — α reproduced exactly) on the reporter+time provenance line; the alpha-mute is the anti-pattern `--muted-foreground-strong` was minted to prevent, per the token's own comment | **F.W1** — the strong rung |
| **FR-AFP-21** | L-10 (β/L only) | The panel that moderates images renders no image — the only gallery surface holding `image_slug` that prints it as text; `thumbnailUrl` takes exactly the field in hand; five sibling surfaces render the asset; the viz-render-path non-defect (no canvas contact) recorded | **F.W3/W4** — render the evidence being adjudicated |
| **FR-AFP-22** | C:D-11 / L-17 | The RFC 7807 surface is built then discarded at all five catches: `ApiProblem`'s `super(title)` (re-read) means `.message` never carries the server's rich `detail`/`type`; `catch (e:any)` ×5 with no lint gate anywhere in `web/` | **F.W3/W4** — typed catch posture at the seam |
| **FR-AFP-23** | C:D-12 | The confirm dialog closes **before** the request (`:86` precedes `:92`): a slow delete invites a second Delete on the same row; a failed delete toasts against nothing; no `:disabled`, no loading state | **F.W3/W4** — await-then-close + guard; primitive shape per FR-AFP-37 |
| **FR-AFP-59** | NEW (α-miss-1, ruling 11; absorbs draft FR-AFP-47) | The tier — the exact state the Save button mutates — renders as a raw lowercase wire token in the 10px muted meta line (`:181`), bypassing the product's real tier design language (Crown/Bookmark chips, `data-tier`, `--tier-*` tokens — all verified at the sibling bytes); `FlaggedVisualization.tier: string | null` where `GalleryTier` is closed on both wire sides. Composed with FR-AFP-10, the tier word is the ONLY visible change after the panel's sole non-destructive remedy | **F.W3/W4** — reuse the sibling `data-tier` recipe; narrow the type with FR-AFP-31's cure |
| **FR-AFP-60** | NEW (α-miss-2, ruling 12) | Under 640px the destructive confirm renders **Delete above Cancel, flush**: DialogFooter is `flex-col-reverse` with only `sm:gap-x-2` (dist bytes, this seat), DOM order `:275/:276`; composed with FR-AFP-37's stray X the mobile read order is X · DELETE · Cancel | **F.W3/W4** — col-direction gap + order fix + `dismiss`-guarded preset (FR-AFP-37); flushness witness → SS-13 |
| **FR-AFP-66** | NEW (β-miss-1, ruling 9) | Orphan flag accumulation: soft-delete does not cascade flags, the janitor's grace hard-delete cascades nothing, `pinned_cron` (which the softdelete docstring names as the cascade owner) contains zero flags code — so the panel's own Delete → grace pipeline strands flags forever, permanently inflating the unscoped `$group` every page request runs; the module docstring is false for this band; the existing orphan-flag test exercises only the reporter-keyed stale-user cascade | **F.W5-W8** — content_hash-keyed cascade at grace hard-delete (+ docstring correction); composes with FR-AFP-4/-33 |
| **FR-AFP-67** | NEW (β-miss-2, ruling 10) | `reload()` swallows its own errors, so the three mutation handlers' catches are structurally dead for refresh failures — and each toasts SUCCESS before reloading: a successful mutation + failed refresh leaves the actioned row rendered and re-clickable under a success toast beside an error toast about the same action | **F.W3/W4** — rethrow-or-return from reload (one error channel per action); binds to FR-AFP-6's error state |

### Minors

| id | claim (adjudicated, compressed) | disposition |
|---|---|---|
| FR-AFP-25 · L-6 / D-22 / C:D-16 (MINOR — β's grade, = the banked AA-28 rescope) | `getAdminToken()!` ×4 vs `string\|null`; on null the operator toast carries the internal `coreFetch: auth='admin' requires adminToken`; `:73` bare setup-scope fetch; reachability narrow (no storage listener → no live cross-tab null path) | **F.W3/W4** guard posture; idiom identity = **AA-28, fold by reference** |
| FR-AFP-26 · D-11 (folds DA-design-A3 #8, still live — identity fold) | Decision data at the system's smallest rung — 10px desktop (`--type-admin-label` fixed non-fluid); three type sizes in one 20px row | **F.W3/W4** re-rung |
| FR-AFP-27 · D-13 | `border-l border-muted` nesting rule at 1.13/1.01:1 — a FILL token painted as a border; even `--border` would sit under 3:1 on this fill (α's reference figures), so the cure exceeds a token swap | **F.W1** |
| FR-AFP-28 · D-18 (+α's exact re-measures) | Three icon-in-button geometries across the three sibling tabs (0.571/0.500/0.583) and the byte-identical pill recipe in two reds (red-300 here `:176`, red-400 AdminUserList `:376`); every `h-*` an override against `size="icon"` | **F.W1** — collapses free with the `iconOnly` re-grammar; ladder identity = **FR-AUL-19, fold by reference** |
| FR-AFP-30 · D-14 / L-18 | `role="list"` owns a non-listitem child (`:232-235` a direct sibling of the listitems) — and per FR-AFP-1 the illegal child is the list's ONLY child in every production render | **F.W3/W4** — move the empty state out of the container |
| FR-AFP-31 · D-15 / L-15 / C:D-17 | `reasonLabel(reason: string)` widens the closed `FlagReason` union over `Record<string,string>` + `?? reason` — a drifted vocabulary ships the raw wire token into the decision slot; sole callsite, zero-blast-radius narrowing; no lint gate exists to notice | **F.W3/W4** (same edit narrows FR-AFP-59's tier) |
| FR-AFP-32 · D-16 / L-13 / C:D-15 (+FR-AFP-40 fold; +FR-AFP-69 = β-miss-5 extension) | `timeAgo`: fifth copy in one directory, two dialects ("0m ago" ×2 vs "just now" ×3 — grep re-run: exactly 5), no clamp, no `<time>`/absolute, render-frozen (no reactive clock), no NaN guard against the non-ISO wire. **β-miss-5 admitted as the sharper server fact**: ONE router serializes the same concept two ways — flagged via `_json`/`default=str` (space-separated, non-ECMA) while audit returns `AuditListResponse` (ISO with `T`) — and the NaN guard sits in the consumer that gets the conformant payload (`AdminAuditLog.vue:54-56`) while this file, on the non-conformant one, has none | **F.W3** — one shared formatter (**= FR-AUL-17, fold by reference**); serializer unification → **F.W5-W8**; JSC acceptance → SS-13 |
| FR-AFP-33 · D-17 / L-20 / L-22 | Flag list unbounded on both ends (server `$push` no `$slice`; client no cap/collapse — `./expandable-container` installed) and the aggregate is a bare `$group` over the whole collection per page request (O(pages × \|flags\|), permanent-growth term = FR-AFP-66). Counterweight preserved: `if not flagged` short-circuits production's only case | server → **F.W5-W8**; client collapse → **F.W1/W3**; magnitude + β's sort-index note → SS-13 |
| FR-AFP-34 · D-19 (+α-miss-7 = draft FR-AFP-53, unfolded with new limbs) | Truncated slug identity with no recovery; native `title` (touch-inert) instead of the app's ~35-callsite/9-consumer Tooltip idiom; **new limbs (α, verified at source)**: `title`+`aria-label` on the same controls duplicates the SR announcement (title → accessible description), the visible string is not contained in the accessible name (2.5.3-adjacent, qualifier noted), and "(save tier)" leaks wire vocabulary into the accessible name | **F.W3** — rides **R3-7a**'s title→Tooltip migration, fold by reference; one label source, no `title`, drop "(save tier)" |
| FR-AFP-35 · D-20 / C:D-19 | Prose: statutory register ("shall"/"irrevocable" — the latter also FALSE, FR-AFP-9), three names for one object, "Dismissed 1 flags" / reachable "Dismissed 0 flags" while `:177` pluralises correctly eleven lines away | **F.W3/W4** copy pass (truth clause owned by FR-AFP-9's wave) |
| FR-AFP-36 · L-16 / L-21 / C:D-20 (second half of L-16 demoted per K9) | Contract-twin drift: emitted `content_hash` omitted client-side (the actual join key invisible to the component) + three dead server models encoding the retired contract (`FlagRequest` / rotted `user_slug` `FlaggedEntryInfo` / offset `FlaggedListResponse`), no `response_model` anywhere to check. The "five dead `?? item.slug` branches" limb is NOT PROVEN — `admin.py` emits `doc.get("image_slug")` unvalidated, so the fallbacks are defensive (K9) | **F.W5-W8** — one generated/checked shape; dead models delete |
| FR-AFP-37 · C:D-13 (cure re-spelled at 7.0.0 — carried K3, re-verified at producer bytes) | Hand-rolled destructive confirm while the pin exports `./confirm-dialog` (0 imports repo-wide; `AdminUserList` hand-rolls a second); the hand-roll omits `:show-close="false"` so a stray X rides beside Cancel (`showClose` default true at the pin). At 7.0.0 `showClose` is GONE — the spelling is `dismiss="deliberate"` (`DialogContent.vue:39/:47/:147`, this seat) | **F.W1** — Dialog preset with `dismiss` guard + loading; forward-compat nuance keeps it MINOR |
| FR-AFP-38 · L-12 (framing killed K8; core survives) | No shared CURSOR composable: the loop is hand-rolled here and again in `stores/gallery.ts`; the refusal of `useOffsetPagination` was CORRECT (it presupposes a `total` this endpoint never emits — superlative S-8) | **F.W3/W4** — extract `useCursorPagination` |
| FR-AFP-39 · L-19 (both readers concordant at 4; sweep re-run by this seat) | Zero-rule `<style scoped>` (only `@reference`) stamps `data-v-*` on ~40 elements — exactly 4 files, all this directory | **F.W3/W4** — **= AA-29, fold by reference** |
| FR-AFP-41 · L-24 (+FR-AFP-68 = β-miss-4, the consequence, re-derived by construction) | `pendingDelete` is not cleared on Cancel/Escape — and the leak is REACHABLE: open a second row's confirm during an in-flight delete (list stays mounted; no disabled), the first handler's late `:98` nulls the target under the open dialog → empty entity name + a silent no-op Delete (`:87` early-return) | **F.W3/W4** — clear on close; null-target guard must not silently close |
| FR-AFP-42 · C:D-18 (spec count corrected K11: **8**) | Zero props / zero emits / setup-top-level fetch in a `defineAsyncComponent` — the seam that makes FR-AFP-11 inevitable and the component untestable in isolation; no e2e opens the tab | **F.W3/W4** seam; test seat → F.W9/W10 |
| FR-AFP-43 · β-miss-6 (= draft FR-AFP-43 — convergent across both reader pairs; id retained) | The celebrated `role="status" aria-live="polite"` region is v-if-created WITH its content (and `reload()` at `:73` makes it part of the first paint), the classic unreliable-announcement pattern; qualifies the S-1 superlative's absolute form — the comparative (vs AdminAuditLog's roleless spinner) stands | **F.W3/W4** — persistent region, swapped text; AT trace → SS-13 |
| FR-AFP-44 · superseded-draft carry (α′-miss, attributed; `:233` re-sighted) | The empty state — production's only state — is carried by a glyph at `opacity-30` (≈1.48/1.70 per the draft's arithmetic, unrecomputed), the same alpha-mute anti-pattern as FR-AFP-20 applied harder; `aria-hidden`, so a canon/legibility defect not a strict WCAG one | **F.W1** — by-colour rung |
| FR-AFP-45 · superseded-draft carry (α′-miss; hover recipes re-sighted at `:201/:211/:221`) | Icon semantics contradict effects: `XCircle` (a reject glyph) is the benign dismiss tinted green; `Star` is a tier-raise tinted blue; no visible text label at any breakpoint | **F.W3/W4** — icon+label re-semantics; composes with FR-AFP-19 |
| FR-AFP-46 · superseded-draft carry (α′-miss) | FR-AFP-3's token cure is insufficient at the worst rung: `--destructive` ≈4.38:1 on the composited light card — passes 1.4.11, fails 1.4.3 for the 10px text; cure must split (token fill/glyph, strong-rung text, or the Badge's owned pairing) | **F.W1** rider on FR-AFP-3 |
| FR-AFP-58 · NEW (α-miss-3, rulings 1+3 — replaces the killed D-12 with its inversion) | Under PRM the panel has NO visible busy indicator: the blanket reset (byte-certain, in source and compiled sheet) freezes `:152`'s spinner into a static ¾ ring — the only visual loading signal, firing after EVERY mutation; SR users are covered, sighted PRM users are not; the load-more path is immune (`:259` swaps text). **α's `[data-allow-motion]` cure REFUTED** (ruling 3) — the cure is textual/skeleton | **F.W3/W4** — visible text/skeleton busy state; perception witness → SS-13 |
| FR-AFP-61 · NEW (α-miss-4; exports keyset re-run by this seat; +ruling 14a) | The flag pill and the moderation card are bespoke re-implementations of `./badge` and `./card`, both exported AT THE PIN (`./chip` is NOT — challenge-D's §7 home corrected); `<Badge variant="destructive" size="sm">` resolves the worst-contrast row TODAY, so booking that cure under the F.W1 uplift is a scheduling error; extends lane-frontend §4's bespoke-shadow list (+2, after C:D-13's +1) | **F.W3** shadow retirement (available now) / **F.W1** for the tone re-grammar |
| FR-AFP-62 · NEW (α-miss-5; radius tokens re-read) | `rounded-lg` resolves 10px against the semantic `--radius-card` 16px (glass re-declares the scale; the class name reads correct) — the flagged card sits 6px squarer than every glass Card around it; same substitution at `AdminAuditLog.vue:87` (re-sighted) → two-file sweep | **F.W3/W4** — `rounded-card` |
| FR-AFP-63 · NEW (α-miss-6; statically certain — no wrap utility exists in the file) | Reporter free text (`flag.detail`, adversarially controlled) renders unbounded with no `break-words`/`line-clamp` inside the one flex child explicitly allowed to shrink (`min-w-0`); the file is careful about exactly this one element short (`:175` truncates) | **F.W3/W4** — wrap control; overflow witness → SS-13 (fixture-gated per FR-AFP-1) |
| FR-AFP-70 · NEW (β-miss-7) | `handleSetTier` is hardwired to `'saved'` with no inverse — the panel writes one of three tiers and cannot undo its own write; a repeat click is a real PUT + audit row + success toast + full pagination reset for a no-op. Composed with FR-AFP-10, an unrecoverable one-way write whose only visible effect is FR-AFP-59's 10px word | **F.W5-W8** semantics (with FR-AFP-10); interim: reflect-and-disable when already saved |

### Info

| id | claim | disposition |
|---|---|---|
| FR-AFP-49 · D-21 / L-26 | Zero automated coverage of any kind (no test script, no vitest, 8 e2e specs, zero admin/flag hits) — every row above incl. all four blockers is invisible to every gate the repo runs | census risk #10 — **= AA-44 / FR-AUL-16, fold by reference**; → F.W9/W10 + SS-3/SS-4 input |
| FR-AFP-50 · L-25 | Both loops are native-`<div>`-hosted, invisible to a component-callsite-keyed deriver; 33-site denominator re-derived by β; extends **R5-7/R6-6 — fold by reference**; the 16-vs-17 registry reconciliation stays artifact-unreadable | F.W4 board carry; registry half → residue |
| FR-AFP-51 · L-27 | The census's ToastVariant break is an uplift-target break, not present-tense — at the pin the symbol resolves; tense clarification only | record — NO-WAVE-OWNER |
| FR-AFP-52 · superseded-draft carry + β's board flag (ruling 5) | The D axis grades a surface FR-AFP-1 proves unreachable; the corpus never states its own conditional — a corpus-composition observation for the spec authors | NO-WAVE-OWNER — SS-3/SS-4 input |
| FR-AFP-54 · superseded-draft carry (re-verified: `lucide-vue-next` in devDependencies, runtime-imported `:16`) | Runtime deps in devDependencies; band inconsistency, harm-nil in a bundled app | **F.W0** manifest banding |
| FR-AFP-55 · superseded-draft carry (peers re-read this session) | Installed glass-ui 4.0.0 peer-declares `@mkbabb/value.js ^0.10\|\|^0.11` vs the app's newer pin, and `@lucide/vue ^1.16` vs imported `lucide-vue-next` — the "clean pin" premise of tri-bump sizing understates the start state | **F.W0** — into F.W1's sizing |
| FR-AFP-56 · β-miss-8 (id retained; convergent across both reader pairs) | The flag sub-loop keys on array index; a stable natural key exists — rider bound into FR-AFP-15 | rider |
| FR-AFP-65 · NEW (α-miss-8; CVA bytes re-read) | Two dead utilities on the pagination wrapper (`text-xs text-muted-foreground` — the Button sets its own size via `--control-text` and colour via ghost) and a `<nav>` landmark minted for a control that does not navigate; inconsistent with the sibling's bare-button pager | **F.W3/W4** hygiene with FR-AFP-42's seam |
| FR-AFP-71 · β-miss-9 | The contract twins are god-modules (api.ts 672 L / types.ts 391 L, wc re-run; no codegen) — the structural cause of FR-AFP-36's drift; three hand-maintained copies of the flagged shape, one already rotted | **F.W5-W8** — named as the drift's cause; module split is the union's call |

## Killed-claims register — 15 entries

| # | claim | adjudication |
|---|---|---|
| K1 | C:S-2 + C §3: "this file's whole 4→7 uplift exposure is ONE LINE (`:276`)" | **SUSTAINED** (both readers convergent; producer re-read) — nine breaking prop sites; the corpus's most consequential error (would under-size F.W1); C's §3 table contradicts its own §1 |
| K2 | C:D-14 sub-claims: "six `size=\"icon\"` buttons"; `#fca5a5` (v3 hex) arithmetic | **SUSTAINED** — exactly three (`:198-207/:208-217/:218-227`, source re-read); installed v4.3.1 declares oklch (`theme.css:13`, re-read); direction unchanged |
| K3 | D:S-3 "ten glyphs" / C:S-1 "8 icons" | **SUSTAINED** (α) — six icon elements, all `aria-hidden` (source read); the superlative itself is true and kept; the two axes' counts disagree with each other |
| K4 | D:D-10's falsifier: "GalleryCard's raise-a-flag affordance" | **SUSTAINED** (β) — `GalleryCard.vue` flag hits = **0** (this seat's grep); the phantom would, if believed, falsify FR-AFP-1; D-10's core unaffected |
| K5 | D:D-5's "28px targets… touch users never see it" (geometry half) | **SUSTAINED-AS-SCOPED** (α) — 28px is fine-pointer-only; the coarse floor delivers 44px via `[data-size=icon]` `min-*-size` (bytes + compiled sheet, ruling 2); the COLOUR half of D-5 stands untouched |
| K6 | D:D-12 as filed ("no global PRM reset anywhere in the imported sheets") | **KILLED** — α's kill SUSTAINED at source AND compiled sheet; **reader-β's confirmation REFUTED by this seat** (ruling 1) — the corpus's one reader-cell kill, recorded as fleet evidence of citation-inheritance (β repeated the challenge's falsified cell). Replacement defect = FR-AFP-58 |
| K7 | D:D-9's "tone: success exists at 7.0.0 and does not exist at 4.0.0" | **SUSTAINED** (β, on D-9's own falsifier) — `use-toast.d.ts:2` at the pin includes `'success'`; fixable now; α's "improvement not a rename" cell inherits the correction (ruling 4) |
| K8 | L-12's framing: "bypasses the repo's pagination seat" | **SUSTAINED** (β) — `useOffsetPagination` demands a `total` the endpoint never emits; the refusal is superlative S-8; residue survives as FR-AFP-38. The two axes' contradiction (L-12 vs C:S-4) resolved for C |
| K9 | L-16's "five dead `?? item.slug` branches" | **SUSTAINED** (β) — `admin.py` emits `doc.get("image_slug")` through an unvalidated hand-built body with no `response_model` (re-read); migration writers exist; defensive, not dead. L-16's first half (content_hash omitted) confirmed in FR-AFP-36 |
| K10 | L-1 + L-2 both graded BLOCKER | **SUSTAINED** (β) — internally incoherent as graded; resolved by ruling 5's roster-wide conditional; L-2 = MAJOR |
| K11 | C:D-18's "web/e2e/ has 7 specs" | **SUSTAINED** (β) — 8 (this seat's ls); zero-coverage conclusion unaffected |
| K12 | challenge-D §7 pill-cure home "`./chip` / `./badge`" at the pin | **CORRECTED BY THIS SEAT** — `./chip` is absent from the 4.0.0 exports keyset (node run; present at 7.0.0); the at-pin home is `./badge` alone (FR-AFP-61) |
| K13 | Provenance cluster (none verdict-fatal) | **RECORDED** — α's D-9 claim-text "four success toasts" vs its own proof's three; census `useToast.ts:3,9` vs live `:4/:9`; Tooltip census 34+1 vs 35 (±1, both land R3-7a's figure); α's pill compositing order vs the challenge's (1.31 vs 1.39, verdict-identical). Re-resolve anchors before cures |
| K14 | **Superseded draft's FR-AFP-24**: "the `h-7 w-7` overrides defeat the WCAG-2.5.5 coarse floor — 28px in both pointer classes" | **REFUTED BY THIS SEAT** (ruling 2) — the floor keys on the reflected `data-size` attribute via `min-*-size`, unstrippable by class merge, present in the compiled sheet; corroborated by banked FR-AUL-19. Surviving residue: the merge does strip the CVA height vars (true, floor-independent) |
| K15 | **Superseded draft's FR-AFP-29**: D-12 confirmed ("nothing in any imported sheet reaches `.animate-spin` under PRM") | **REFUTED BY THIS SEAT** (ruling 1) — the blanket reset reaches everything; the draft's own sibling instruments (fr-AdminUserList) already recorded the rule. Replaced by K6 + FR-AFP-58 |

## Superlatives — 10 sustained, with provenance (L-18 runs both ways; two qualified)

1. **Entity-interpolated accessible names on every control; all six glyphs `aria-hidden`** (D:S-3 · C:S-1 · both readers, counts corrected per K3) — "Delete entry sunflower-3f2, button", not "button ×3"; measured against `AdminAuditLog.vue`'s zero aria attributes. *Qualified twice*: the labels inherit FR-AFP-8's identity bug in content, and the live-region half of the praise is FR-AFP-43's defect.
2. **`min-w-0` on the flex child (`:169`)** (D:S-2 · both readers) — the reason `truncate` works at all; the most-omitted flex idiom, correct first try.
3. **The append path (`:58-71`) is the in-file cure for three of the file's worst findings** (D:S-4) — re-entry guard, non-swapping flag, `.push`, `:disabled`, textual busy state; the correct and the wrong pattern thirty lines apart.
4. **`surface="opaque"` on a destructive confirm is the right design judgment and the one glass API here that survives 4→7 verbatim** (D:S-5 · C:S-2's surviving half · both readers) — ***qualified by ruling 13***: true of the working tree only; at HEAD the prop is `variant="opaque"`, inert, and the confirm renders on the default glass surface. The one-hunk fix is still uncommitted.
5. **The native `confirm()` was genuinely supplanted with a real focus-trapped Dialog** (D:S-6) — the modal quality the rest of the roster's dialog rows refine rather than refute.
6. **`text-admin-label` consumes the real semantic typography utility** (C:S-3 · β) where the sibling hard-codes `text-[0.65rem]` — the rung is wrong (FR-AFP-26); reaching for the token was right.
7. **The cursor-envelope migration is complete client-side and the offset composable was correctly REFUSED** (L:S-1 · C:S-4 · both readers — the finding that kills L-12's framing); the client finished a migration its server left half-done (dead offset models, FR-AFP-36).
8. **The admin token is read fresh at each callsite, never captured at setup** (L:S-4) — four for four; orthogonal to FR-AFP-25's `!`.
9. **Corpus method, both readers concordant and spot-checked by this seat**: challenge-D's contrast arithmetic reproduces from first principles to ~2dp (α rebuilt ten of eleven values; β independently rebuilt the headline pair); the prior-art fold (DA-design-A3 #7/#8 "still live at identical lines") is verbatim-true; challenge-L's §4 published six of its own killed hypotheses — two re-verified by β. The corpus's errors concentrate in secondary enumerations and comparanda, never primary mechanisms — the same distribution this adjudication found in the readers.
10. **Family coherence** (α, net-new): the three admin panels share a byte-identical container recipe (`flex flex-col gap-3 px-4 py-2` — re-sighted at all three), so the tab strip never jumps; given how much else diverges (three geometries, two reds, two timeAgo dialects), the shared shell is a preserved decision worth protecting through F.W1.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **Lived severity of FR-AFP-3 in light mode** — ratios triple-derived; whether ~1.3:1 reads illegible or merely faint wants one light-theme screenshot.
2. **FR-AFP-5's focus destination** — unmount certain; `<body>` vs reka retargeting wants one keyboard trace (the Dialog close→restore→destroy path is the interesting one).
3. **FR-AFP-60's flushness** — class string + DOM order source-certain; whether an ancestor supplies incidental separation wants one <640px screenshot.
4. **FR-AFP-58's perception** — the frozen ring is byte-certain; that it reads as "nothing is happening" is a perception claim; one PRM-emulated mutation-path screenshot.
5. **FR-AFP-43's AT behaviour** — one screen-reader trace decides the S-1 absolute form.
6. **FR-AFP-33/-63 at scale** — 60-flag geometry and unbroken-token overflow need a seeded fixture, which per FR-AFP-1 **no product path can produce** (fixture required).
7. **Dialog slug overflow** — `max-w-sm` + `font-mono` + no `break-all` at `:270`; slug-length distribution unknown.
8. **JSC/Safari acceptance** of the space-separated `default=str` datetime (V8 measured by the C author); rejection renders "NaNm ago" per row + per flag.
9. **The literal abort-toast strings** (L-5 vs C:D-7 quote different engine strings — mutually exclusive, engine-defined; mechanism needs no live check).
10. **The unscoped `$group`'s magnitude** (+ β's no-supporting-sort-index observation) — shape source-certain; moot until a producer exists.
11. **L-25's 16-vs-17 registry reconciliation** — the R6 artifact remains unreadable from this seat, exactly as the axis records; the live 33-site classification is reproducible.

## Closing verdict

Both re-reader returns survive the incredulity seat, **asymmetrically and in the opposite direction from the superseded draft**: reader-α's substrate account — the one place the two readers flatly contradict each other — is byte-exact (the blanket PRM reset and the coarse-pointer floor both exist, in source and in the compiled sheet), its five kills all sustain, and its eight misses all admit, two as MAJORs; reader-β contributed the corpus's widest verification sweep, its eight kills all sustain, and its nine misses land seven bookings plus one already-ruled identity — but its D-12 confirmation is this record's one **refuted reader cell**, a citation-inheritance event (it repeated the challenge's falsified "no global reset" sentence one file short of the truth). The subject's adjudicated shape is unchanged in outline and sharpened in mechanism: **a genuinely well-crafted accessibility-and-cursor skeleton wrapped around a moderation surface that cannot moderate** — no producer writes its queue (FR-AFP-1), no reachable action removes the reported artifact (FR-AFP-4), its palette is one-theme raw Tailwind about to be structurally invalidated by the uplift that also breaks all six of its Buttons (FR-AFP-2/-3), its identity, permanence, dequeue, and tier semantics each state something the operations do not do (FR-AFP-7/-8/-9/-10/-70), and its refresh path can leave a deleted row on screen under a success toast (FR-AFP-67). Routing: **F.W1** owns the nine-site Button re-grammar (under AA-2's census identity), the re-ink with its split cure, the toast-tone adapter, the dialog preset, and the geometry collapse; **F.W3/W4** owns the reload/focus/error/abort/store-delegation family, the tier render, the PRM busy state, and the hygiene tail; **F.W5-W8** owns the producer decision, artifact remediation, content-hash identity, orphan-flag cascade, queue size, serializer unification, and the twin drift; **F.W0** the manifest rows; **F.W9/W10** the coverage gate; six rows fold by reference into banked sibling/census identities and two INFO rows stand as spec-authoring input. Nothing lost — head, tail, or interval: every challenge row, every reader kill and miss, both readers' dissents, and every unique row of the superseded same-path draft leaves under exactly one disposition.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; the superseded draft's content is preserved here by carry (§Supersession entries K14/K15, FR-AFP-44/-45/-46/-48/-52/-54/-55/-57 folds) because the registry path is untracked and holds no git history; dispositions are ADJUDICATED, not VERIFIED; the X·F specs are still forming and consume this roster as agglomeration input (M-25); execution awaits the owner's begin-word.
