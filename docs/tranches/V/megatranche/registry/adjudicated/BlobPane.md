Served model id: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited (M-23/L-18 receipt).

# BlobPane — adjudicated apotheosis (fresh Fable adjudicator over two hostile re-readers)

**Seat**: fresh-Fable adjudicator · **Date**: 2026-08-04 · **HEAD at adjudication**: `2e334231` (branch `tranche-u`; the D axis recorded `f36f780c` at its own run — the tree moves under the formation; every line anchor below re-verified at `2e334231`).
**Corpus**: `audit/components/BlobPane/challenge-{D-design,C-implementation,L-library}.md` — three r1 seats, all Opus 5 [1m] by their own receipts. **No r2 file exists for any axis** (directory enumerated by this seat); the tasking's "r2 governs" therefore resolves to: the three r1 files are the corpus of record, weighted per HYDRATION-LEDGER.md:51-53 (all three EXISTS-ORIGINAL, harvest `area-scenes.json` — witnessed challenge seats, not the REPORT-AUTHORED Codex batch).
**Re-readers adjudicated**: reader-A (design-and-usage) · reader-B (library-and-code), both hostile, both returned confirmed/killed/missed/unproven registers. This seat attacked both before adopting either.
**Adjacent adjudication of record**: `registry/adjudicated/ConfigSliderPane.md` (2026-07-27, MT-CSP-1 + ADD-CSP clauses 1-13). A large fraction of this corpus is fact-identical to rows already adjudicated there; identity guards below prevent every re-book (anti-rename law).

**Arbiter's own instruments this session (static + node against the installed dist + stored-frame vision; zero browser probes — probe parsimony, and every ruling below is decidable without one):** full reads of `BlobPane.vue` (130 lines), `ConfigSliderPane.vue` (253), `HeroBlob.vue` (318); `viewSchema.ts:85-190`, `useViewManager.ts:59-70`, `usePaneRouter.ts:172-188`, `PaneHeader.vue:40-98` + the `animation-timeline` grep (3 consumers at :180/:186/:191), `foundation.css:345-356/:678-700/:740-750`; the full atmosphere chain (`App.vue:280-292` → `useColorPipeline.ts:279` → `useAtmosphereFrameCoalesce.ts:1-22` → `useAtmosphereBoot.ts:55-66` → `useAtmosphere.ts:108-118/:378-405`); node against `@mkbabb/glass-ui@7.0.0` `./blob-config`: full 48-leaf census re-derived (39 numeric; the 8/9 unreachable sets match all three axes element-for-element), all-31 bracket check (0 violations) **and an all-31 step-grid check (exactly ONE off-grid default: `geometry.satelliteRadius` 0.082, (0.082−0.02)/0.005 = 12.4)**, `BLOB_HERO` read whole (bodyRadius **0.22**, orbitRadius 0.3, satelliteRadius 0.1, eccentricity 0.04, fissionAmp **0**, paletteStops = the canned browns); dist d.ts reads: `ConfiguratorRow.vue.d.ts:38-50` (the LabeledField directive verbatim), `ConfiguratorLayer.vue.d.ts:1-29`, `blob/types.d.ts` (:129 `lightnessFloor?`, the stretch demotion, :207-222 all-required `BlobConfig`, `morphT: number`); dist CSS: spectrum recipes (`.slider-track{…background:var(--slider-track-bg,var(--secondary))}` / `.slider-range{…background:0 0}`), `typography/utilities.css` (`.section-label{@apply text-mono-caption;…}`), `configurator-M5OaIlJd.js` (`name` → `text-micro font-mono text-muted-foreground/70`; **zero** `for:` bindings); greps: `noteBlobActivity` = exactly 3 call sites, `<Blob\b` = 1 component hit, `BLOB_HERO` in repo = 0, `#/blob` in e2e = 0, `BlobPane` in test/+e2e/ = 0 files, `deriveBlobPalette` = exactly 2 call sites; `test/picker-blob-config.test.ts:20-32` (the four pinned literals); `motion-quarantine.md` M-21/M-33/M-34 read in place; X-ledger sweep (`CARRY-CUT-LEDGER.md` §1.G-H, X-W2/W5/W6/W8 file tables); **stored-frame vision reads** of `shots/safari-desktop-light/blob.png` and `shots/forced-colors-desktop/blob.png` side by side (the deciding read for ruling 2).

## Scoreboard

**24 roster rows adjudicated: 14 CONFIRMED-HERE (6 of them newly minted from the re-readers' missed registers, arbiter-verified) · 10 IDENTITY-FOLDS to the ConfigSliderPane adjudication (confirmed defects, booked once, never re-booked) · 13 KILLED claims registered with refutations · 0 UNVERIFIABLE.**

Blocker-weight survivor: **the D-3/BPC-1/L-1 + BPC-2 + reactivity-split family** — the console has no causal link to its subject by THREE independent mechanisms (four atoms literal-overridden; config writes absent from the wake set; 13 of 31 sliders structurally unable to mint the wake-shape prop object), on a route whose only rendered blob is an aria-hidden ornament of a different pane (D-2), while the one blob test in the suite pins the override literals (BPC-9).

**Formation flag (this seat's own find):** the ConfigSliderPane adjudication's wave **MT-CSP-1 is absent from the X carry-cut ledger** — no CC row folds it (the only ledger touch on the file is CC-105's `:202` ink-muted repin bank at X-W8), and `demo/scenes/ConfigSliderPane.vue` appears in **no** X wave's file-ownership table. Every disposition below that lands on that surface therefore routes "MT-CSP-1 (adjudicated) — **NO-X-WAVE-OWNER**, flag to the next formation boundary." Rows on surfaces X waves do own route to those waves.

## Ruled disagreements (this seat's actual work)

| # | Question | reader-A | reader-B | RULING · deciding evidence (arbiter's own) |
|---|---|---|---|---|
| 1 | **BPC-1 "root 2" (propagation)** — which side of `heroConfig` propagates? | listed the tension as unproven | KILLED C's version as "exactly inverted"; re-minted the inversion as a missed MAJOR | **B wins, on this seat's own derivation.** `HeroBlob.vue:152-178`: the nested spreads `...appBlobConfig.{geometry,surface,color}` (:161/:170/:171) read every leaf → tracked deps → a write mints a fresh prop object (the "wake-repaint shape" the file's own :63-66 contract names). The top-level `...appBlobConfig` (:153) reads `membrane`/`satellites`/`interaction` at KEY level only → their leaves are untracked → writes mint nothing. Untracked slider set = Membrane 7 + Pointer 4 + Satellites 2 = **13 of 31** (`tempo`, a top-level leaf, IS tracked by the spread). C's claim ("the snapshot side is dead, the proxy side works") states the true mechanism backwards. B's kill CONFIRMED; B's re-mint CONFIRMED as roster row 3. |
| 2 | **`shots/forced-colors-desktop/blob.png`** — genuine WHCM capture (A: "textbook, and it CONTRADICTS D-6's consequence limb") or invalid frame to be struck (D:45-49)? | kill D's strike; use the frame against D-6 | did not use the frame | **BOTH over-ruled; the frame is a VOID forced-colors witness — neither strike-as-fabricated nor contradiction-evidence.** This seat read both frames: relative to `safari-desktop-light/blob.png` the frame IS materially stripped (card tints flattened to cream, glass gone, hard borders minted on both footer buttons + the dock) — so D's "not a forced-colors capture / full chromatic palette" overreached; the surviving chroma is roster-exempt or ambient. But the frame's tracks render DARK on cream — irreconcilable with D's probe-8 chromium measurement (track = Canvas white) — and the frame carries **no engine/emulation label and no `matchMedia` receipt**. Under the adjudicated two-channel law (ADD-CSP clause 4; CSP G4: "a WebKit forced-colors frame is void, I-20/L-12") an unlabeled frame with unproven emulation truth is inadmissible in EITHER direction. A's "materially contradicts D-6's own consequence limb" is therefore STRUCK as load-bearing; D-6's consequence limb returns to UNPROVEN-NEEDS-LIVE (residue item 1). The frame is retained as an unlabeled-cell capture and must be re-taken under G4 discipline. |
| 3 | **BPC-6 kill grounds** — is the coarse-pointer block "block-only" (C), or does it "lift the target to 44px" (A)? | kill via operable-rail + `inset-inline:0` extension | kill via SC 2.5.8 spacing exception | **The kill stands; BOTH characterizations of the coarse block are corrected by the adjudicated record.** CSP C-7 (arbiter-verified there): the demo `::before` is **INERT** — the producer's own `@media(pointer:coarse){[data-control-target]{min-block-size:var(--touch-target,2.75rem)}}` rung already delivers 44px everywhere, and the demo block deletes with its comment (ADD-CSP clause 13). So A's "lifts it further" credits dead code, and C's "block-size only" mis-reads dead code. The SC-framing kill itself is triple-precedented (CSP C-8/D-5 ruling + ADD-CSP clause 7 "NAME THE FLOOR"): 24px-circle spacing exception passes on B's row-pitch arithmetic; the rail is the operable target (432×24). No SC 2.5.8 row is booked; the house-floor row lives at CSP G5b. |
| 4 | **D-7's cure** — reserve "max legal ch" per definition? | KILLED (retired mechanism, `readoutReservation.ts`, M-21) | KILLED (same, plus the NORMATIVE misquote) | **Kill CONFIRMED — with a governance note.** M-21 (TRUE, superlative-aware) and `readoutReservation.ts:5-13` ("no per-cell `ch` reservation returns") verified in place. The governing cure is CSP G7 (step-derived precision + declared unit + reserved width, ≤0.5px swing); this seat records that G7's reservation must be implemented at the rung M-21 sanctions (per-definition, worst-case-of-its-OWN-format, no blank-air pooling) — the two records are compatible; a wave that re-mints the retired per-cell mechanism fails M-21's precedent even while satisfying G7's number. |
| 5 | **LabeledField (A's missed M1)** vs the adjudicated CSP C-6 cure | new MAJOR: "wrong producer primitive, against an explicit producer directive" | (not raised) | **CONFIRMED as a sharpening, routed as an E-3 addendum — not a re-book.** The directive is verbatim in the installed dist (`ConfiguratorRow.vue.d.ts:38-50`: "**No a11y for/id wiring** … use LabeledField directly for an accessible form control, including inside a Configurator"), `./labeled-field` is a published export, and the compiled row binds no `for` (0 hits, this seat's grep) — all arbiter-verified. The SYMPTOM (31 orphan labels, zero grouping) is CSP C-6, already CONFIRMED and gated (G3 + RELAY B-3). M1 adds the producer's own contra-indication and the sanctioned alternative primitive; it rides beside CSP G3 as addendum ADD-BP-1 below, and it is the true root of D-9's duplicate-name mechanism (the hand-duplicated `aria-label`) and of D-8-limb-2/M4 (the live value jammed into the mono TOKEN rung `name`). |
| 6 | **MT-CSP-1's home** | routed rows to "MT-CSP-1" | routed rows to "MT-CSP-1" | **Both readers routed correctly but neither noticed the wave is ORPHANED**: absent from `CARRY-CUT-LEDGER.md` (this seat's sweep — only CC-105 touches the file, banked on the Glass-8 repin) and from every X wave file table. Recorded as the formation flag above; all CSP-surface dispositions below carry NO-X-WAVE-OWNER. |

## Adjudicated defect roster

Severity is this seat's final grade. Disposition names the wave that owns the surface (X-W2/X-W5/X-W6/X-W8), the adjudicated CSP gate where the identity already lives, or NO-WAVE-OWNER. **Status verbs: ADJUDICATED, not VERIFIED — nothing here stamps a wave.**

| # | id(s) | sev | claim (final form) | proof (arbiter-anchored) | terminal disposition |
|---|---|---|---|---|---|
| 1 | D-3(a) / BPC-1 / L-1 | **BLOCKER** | Four of five Geometry sliders cannot reach the renderer: the only `<Blob>` literal-overrides `bodyRadius/orbitRadius/satelliteRadius/eccentricity` after spreading the injected config; the prop wins the producer's `config ?? injectedConfig` seam. | `HeroBlob.vue:160-166` (+ :16 `:config="heroConfig"`); `BlobPane.vue:58,60,61,62`; `<Blob` census = 1. Arithmetic — no probe needed. The pane prints `0.220` while the render draws `0.325` (both stored frames). | **BUILD → X-W6** (owns `BlobPane.vue` + `HeroBlob.vue`; CC-057 stage ownership). The as-filed `BLOB_HERO`-adoption cure is KILLED (K-11); the correct transposition pushes the demo's three deltas INTO a named preset first. `test/picker-blob-config.test.ts:25-31` is the adverse oracle and re-aims in the same cut (ADD-CSP clause 10). |
| 2 | D-3(b) / BPC-2 | **BLOCKER** | After the 5.3s park, no config write wakes the renderer: `noteBlobActivity()` has exactly 3 callers (`onActivated`, the colour watch, the save watch) — a blob-config write is not activity. | `HeroBlob.vue:211-212/:216/:248/:267/:290` (this seat's grep). Mechanism fully static; the byte-identical-frame evidence is live-only corroboration. | **BUILD → X-W2.b** (the `settled`-seam park replacement — spec'd verbatim) **+ X-W6** (a stage the pane owns needs no ornament-park at all). |
| 3 | NEW-B1 (reader-B missed, arbiter-confirmed) | **MAJOR** | `heroConfig` is heterogeneous: geometry/surface/color leaves are tracked (fresh prop object per write) while membrane/satellites/interaction pass at key-level only — **13 of 31 sliders can never mint the wake-repaint prop shape** the file's own contract (:63-66) depends on. | Ruling 1 above — this seat's own dependency derivation over `HeroBlob.vue:152-178`. | **BUILD → X-W6** (dies with the stage transposition); recorded as the structural half of row 2. Pixel consequence = residue item 3. |
| 4 | D-2 | **BLOCKER** | The Blob workbench owns no preview; the sole rendered blob is `aria-hidden` ornament of the Picker pane; canon requires preview-larger-than-form. | `BlobPane.vue:122-130` renders only the console; `HeroBlob.vue:10` `pointer-events-none aria-hidden`; `viewSchema.ts:179-187`; VISUAL-CONSTITUTION §3 law 3 / §7 quoted exactly by D and re-read here. 35.2:1 figure = residue item 4. | **BUILD → X-W6** (CC-057: "persistent Blob/Atmosphere preview" — the named row). |
| 5 | D-1 / BPC-3 / L-6-mobile | **MAJOR** (D's BLOCKER demoted — one tap, one schema line) | `/#/blob` below lg and at 200% zoom opens on the Picker: the `blob` entry omits `defaultPaneIndex` while its shape-identical siblings (`palettes`:122, `mix`:150) set `1`. | `viewSchema.ts:96/:179-187`; `useViewManager.ts:65` `?? 0`; `usePaneRouter.ts:177-184`. Fully static. | **BUILD → X-W5** (owns `viewSchema.ts`; the left/right/defaultPaneIndex axis is deleted for `regions[]` there — the omission dies with the field). |
| 6 | D-4 / L-2 | **MAJOR** | 31 controls in 7 flat, non-collapsible, preset-less sections while the installed producer ships Configurator/ConfiguratorLayer/useConfiguratorState (the Layer docblock names the blob split verbatim). | `BlobPane.vue:54-119` (31 = 5+7+5+7+4+2+1, re-counted); `ConfiguratorLayer.vue.d.ts:1-29` read here. | **FOLD**: disclosure/composition → **X-W6** scene contract + CSP D-9/G8 `rank`; L-2's "delete ConfigSliderPane, adopt the machine whole" is RESCOPED by the adjudicated CSP C-9 ruling (the state machine is preset-shaped and was REFUSED; per-row `canReset` adopted). |
| 7 | D-5 limbs 1-2 | **MAJOR** | The certified de-emphasis TEXT ink is the pane's dominant painted mass (both schemes), and the Color section's chromatic axes are grey while the Picker's ramps are spectral. | `ConfigSliderPane.vue:202` — the repo's only background use of `--ink-muted` (all other consumers `color:`); both stored frames re-read by this seat. | **IDENTITY-FOLD → CSP C-3/D-3/D-4/D-14 = G-PAINT** (the single cut: retire `variant="spectrum"` in the console, feed the painted range, delete :202/:204-206, re-aim o18:1161-1211 same cut). Surface = ConfigSliderPane → **NO-X-WAVE-OWNER** (formation flag). |
| 8 | D-6 (mechanism limb) | **MINOR** | The WHCM roster protects `.slider-range` — permanently unpaintable under the spectrum recipe (`background:0 0`) — and omits `.slider-track`, the only painted element. | `foundation.css:685-696` + this seat's dist recipe reads. Source-decidable. | **IDENTITY-FOLD → CSP D-1 (rescoped) + G-PAINT** (retiring the variant removes `data-variant="spectrum"` from the console, un-aiming the roster line); slider forced-colors arm stays a glass RELAY (I-20). Consequence limb = residue 1. |
| 9 | D-12 / BPC-12 (+ BPC-4) | **MAJOR** | 17 of 48 axes unreachable — incl. `morphT` (the sole flat↔dressed axis), `surface.lit` (the boolean the section is named for), both enums — and the guard makes `color.lightnessFloor` a compile error (`BlobConfig[A][K] extends number` sees `number|undefined`). | This seat's own node census: 48/39/8+9, matching all three axes exactly; `types.d.ts:129/:207-222`; the slot mechanism exists and AuroraPane uses it. | **BUILD → X-W6** (sections + enum defs through the slot / G8 enum-def species). The guard mechanics (BPC-4's `NonNullable` cure, BPC-5's literal bypass, L-10's double casts, L-9's SFC types) **IDENTITY-FOLD → CSP C-5/D-16/G8** — the typed lens deletes `NumericAtomPath` + comment wholesale. |
| 10 | BPC-8 | **MAJOR** | `interaction.stretch` shipped as a top-level slider over exactly the 0→1.5 domain the producer measured null and expressly demoted ("the demo no longer surfaces it as a top-level slider"). | `types.d.ts` stretch docblock read here; `BlobPane.vue:104`. | **BUILD → X-W6** (one-line section edit; the demotion honored). |
| 11 | BPC-9 | **MAJOR** | Vacuous gate: `#/blob` appears nowhere in e2e, no test mounts BlobPane, and the one blob test regex-pins the four override literals; `SECTIONS = []` renders green. | This seat's greps (0 hits / 0 files); `test/picker-blob-config.test.ts:25-31` read; `ConfigSliderPane.vue:119/:163` v-ifs. | **BUILD → X-W6** (`o27-scene-contracts.spec.ts` — the born-RED pixel-delta oracle belongs to the scene-contract battery; the pinned-literals test re-aims with row 1's cut). |
| 12 | L-3 / BPC-11 (clobber limb) | **MAJOR** | Reset shallow-clobbers `color.paletteStops` (externally owned, declared off-limits by the pane's own header) back to the canned browns until the next colour change. | `ConfigSliderPane.vue:92-94`; defaults read by this seat (`["#b5947f","#d4b27d","#dad6b1"]`); the writer is `useAtmosphere.ts:389-403`'s colour-keyed watch. Masked today only by row 1's override — two defects cancelling. | **IDENTITY-FOLD → CSP C-1 = G1** (declared-key reset). NO-X-WAVE-OWNER flag. Coalescing limb KILLED (K-8). |
| 13 | L-4 | **MAJOR** | Two `deriveBlobPalette` owners; the boot derive's output is never rendered (HeroBlob:171 overrides it; the :79 seed is superseded same-tick by the `{immediate:true}` watch). | This seat's grep: exactly 2 call sites; chain read confirms both triggers are the SAME rAF-coalesced signal. | **BUILD → X-W6** (owns `useAtmosphere.ts`; one owner, ink-floor applied once — lands with row 1's transposition). |
| 14 | L-6 (+ L-13) | **MAJOR** | Config created in boot, provided under a producer symbol, written in scenes, overridden+rendered in picker — no module named for the concept; `inject(BLOB_CONFIG_KEY)!` stands in for a guarantee the graph does not make. | Six operations across three areas, all anchors re-read. | **BUILD → X-W6** (+ X-W5 for the route half). |
| 15 | D-9 / BPC-7 | **MINOR** (aggravated by row 5's addendum root) | Two pairs of sliders share an accessible name across different domains/ranges ("Noise Freq"/"Noise Speed" ×2); the name is the bare `aria-label` string because the label element wires nothing. | `BlobPane.vue:71-72/:83-84`; `ConfigSliderPane.vue:145`. Static. | **BUILD → X-W6** (BlobPane owns the strings) under **CSP G3**'s grouping cure + ADD-BP-1 (LabeledField/section-qualified names). |
| 16 | D-7 (surviving residue) / D-11 / D-10 | **MINOR** | `fmt` swings the readout's KIND 1↔5 chars mid-drag; units smuggled into two labels ("Merge (ms)"); three bipolar axes render with no origin affordance. | `ConfigSliderPane.vue:84-86`; `BlobPane.vue:111-112/:81-82/:102`. | **IDENTITY-FOLD → CSP D-11 = G7** (step-derived precision + `unit` + reservation per ruling 4) **+ G8 schema** (direction axis). NO-X-WAVE-OWNER flag; BlobPane's label strings X-W6. |
| 17 | D-8 limb 2 + A-missed-M4 | **MINOR** | The live value is the smallest, faintest type in the pane (11px producer `text-micro` via the `name` TOKEN rung) and forms no column — each number's x-origin is a function of its label length. | Dist read: `name` renders `text-micro font-mono text-muted-foreground/70`; both stored frames show the ragged origins. Root = the value living in a token-reference slot (ADD-BP-1). | **FOLD → CSP G7 + ADD-BP-1**. NO-X-WAVE-OWNER flag. |
| 18 | D-8 size-rung residue + A-missed-M5 | **MINOR** | `.config-section-title` re-rolls the producer's `.section-label` recipe byte-for-byte EXCEPT the size — `--type-small` (= the row label's rung) instead of `--type-caption` — so no rung separates furniture from content. | `ConfigSliderPane.vue:237-243` vs this seat's dist read of `.section-label`/`text-mono-caption`. Family limb killed (K-6). | **IDENTITY-FOLD → CSP D-7 = G10a** (one recipe per role) with the `.section-label` fact as new evidence; canon echo X-W10. NO-X-WAVE-OWNER flag. |
| 19 | D-13 / D-14 / BPC-10 | **MINOR** | Copy JSON discards its `{ok,reason}` Result with no result surface (real on LAN/insecure origins); Reset is destructive, unconfirmed, un-undoable, rendered at identical weight to the benign verb. | `ConfigSliderPane.vue:88-94/:163-174`; unhandled-rejection limb killed (K-9). | **IDENTITY-FOLD → CSP C-4/D-10** (status region + confirm grammar; `useClipboard` status shipped/unused). NO-X-WAVE-OWNER flag. |
| 20 | D-16 | **MINOR** | Nine boundary lines against a canon inventory of zero (7 section hairlines + well border + action-bar rule). | `ConfigSliderPane.vue:232-235/:250`; `foundation.css:350-354/:743-748` — all re-read. | **IDENTITY-FOLD → CSP D-6 = G6** (deletion) **+ CSP D-8/BANK B-4** (the well border). NO-X-WAVE-OWNER flag. |
| 21 | L-5 | **MINOR** | Config constants + type reached through the 103KB WebGL barrel; the producer's 245B `./blob-config` has zero consumers. Bundle cost measured zero (L's own honest negative) — a boundary defect, not a size defect. | Import sites re-read; exports map read here. | **BUILD → X-W2.a** — the wave names these exact two repoints. |
| 22 | L-7 | **MINOR** | 19 `demo/ui/*` alias barrels; three parallel import idioms; ConfigSliderPane uses four idioms in eight lines. | L's counts re-spot-checked at CSP adjudication; unchanged. | **BUILD → X-W8** (the 19-barrel subtraction is that wave's hard gate). |
| 23 | L-8 | **MINOR** | Three `:deep()` reaches into ConfiguratorRow internals while the producer ships the `size` rung + `CONFIGURATOR_SIZE_KEY` door; the `--slider-track-bg` feed is the correct pattern by contrast (and still dies in G-PAINT). | `ConfigSliderPane.vue:204/:215/:219-222`. | **IDENTITY-FOLD → CSP D-12 = G5a** (pass the producer size; delete clamp + coarse block per clause 13). Note CC-105 (X-W8, banked) also names `:202` — the G-PAINT cut supersedes; the bank must not resurrect it. NO-X-WAVE-OWNER flag. |
| 24 | NEW-B2 + NEW-B3 + NEW-B4 + A-missed M6/M7/M8/M9 + D-15 + INFO residue | **MINOR/INFO** | (a) `satelliteRadius` default 0.082 is the ONLY off-grid default of 31 — first interaction makes the shipped default unreachable except via Reset (arbiter-confirmed by the all-31 census); (b) `BlobPane.vue:9` attributes the palette watch to App.vue — false (it lives in `useAtmosphere.ts:389`, this seat's grep); (c) the `readonly unknown[]` guard arm is dead (no array-typed top-level member) and the OUTER `-?` is defended by a stale cause (`morphT` is required at 7.0.0); (d) a 5-value discrete axis drawn as a continuous rail, no enum/boolean vocabulary; (e) 31 raw shader-uniform labels with `sub`/`description` shipped and unconsumable; (f) the satellite domain split across two sections (the "Satellites" section omits count/size/orbit); (g) the pane description restates four section titles; (h) 31 unskippable tab stops before the action pair. | All anchors verified this session. | (a),(d),(e),(f),(g) **BUILD → X-W6** (schema + sections + description bytes); (b),(c) die with row 9's G8 deletion + row 13's cut — doc-truth per ADD-CSP clause 12; (h) **FOLD → CSP G3** + D-9/band disclosure. |

## Killed-claims register (refutations verified by this seat)

| # | killed claim | axis:anchor | refutation |
|---|---|---|---|
| K-1 | D-5 limb 3 — "the declared variant is defeated; edict-5 violation" | D:294-302 | `--slider-track-bg` is the producer's own declared fallback slot (`var(--slider-track-bg,var(--secondary))`, dist read); the transparent range is the producer's unconditional spectrum recipe (`background:0 0`). A token handoff through a published hook engages no edict. The true defect is the WRONG VARIANT on 31 non-chromatic axes — adjudicated at CSP G-PAINT. |
| K-2 | D cure #8 — "delete the `--slider-track-bg` feed; let spectrum paint" | D:755-760 | Regressive twice over: the fallback is `var(--secondary)` = the measured born-RED O-18 defect (~1.09:1/1.26:1, WCAG 1.4.11), and spectrum's range paints NOTHING by recipe — no value signal returns, D-6's roster entry still protects a phantom. The live lock is `o18-contrast-census.spec.ts:1161` (read here). The adjudicated cure is the opposite move (G-PAINT). |
| K-3 | D-7 limb 1 + the NORMATIVE quotation | D:347-367 | The law's scope qualifier "**at display scale**" is elided by D's ellipsis (`demo/DESIGN.md:64-65`); an 11px `text-micro` readout is its antithesis. And the same law: "Fira Code is tabular by default" — `font-variant-numeric: normal` on a mono face is not a violation; the measured swing is char-COUNT, which tabular figures cannot address. |
| K-4 | D-7 cure — per-definition "max legal `ch`" reservation | D:751-752 | Prescribes the RETIRED mechanism: `readoutReservation.ts:5-13` + motion-quarantine M-21 (TRUE, deliberate trade) — "no per-cell `ch` reservation returns." Ruling 4 above records how CSP G7 satisfies the goal without the dead mechanism. |
| K-5 | BPC-6 — "31 SC 2.5.8 failures; no pointer-type exemption" | C:239-258 | The 12×24 rect is the thumb GLYPH; the operable target is the full rail (reka `SliderImpl` root pointerdown → slide, spectrum track 24px tall × row width), and the Spacing exception passes on ≥32px row pitch with one target per row. Triple-precedented (CSP C-8/D-5 ruling; ADD-CSP clause 7). Coarse-block characterizations on BOTH sides corrected per ruling 3. |
| K-6 | D-8 limb 1 — "second undeclared type-jurisdiction exception (mono family)" | D:407-412 | The constitution's own value row admits `mono-caption` for captions, and the producer SHIPS the mono uppercase eyebrow as `.section-label` (dist read this session). The surviving defect is the size RUNG — roster row 18. |
| K-7 | `.pane-scroll-fade` = a fade to keep (D:246-247, L:191) or to delete (L-12 cure, D cure step 2) | both axes | The class sets exactly `contain: layout style paint` + `scroll-timeline: --pane-scroll block` (`PaneHeader.vue:54-57`, read here) and hosts THREE live `animation-timeline` consumers (:180/:186/:191). No fade exists (the only mask is the header veil's feather, :91-96); deleting the class silently kills three animations. Precedent: motion-quarantine M-33 (FALSE, same misread) + M-34 (the timeline is load-bearing). The truth is roster row 24's overflow-affordance gap + CSP D-17's doc-truth row. |
| K-8 | BPC-11 coalescing limb — "the derive runs on every colour change, not the rAF-coalesced ref" | C:365-367 | The cited line is a PARAMETER NAME. This seat walked the chain: `App.vue:285-289` passes `cssColorOpaqueFrame` (= `useAtmosphereFrameCoalesce(cssColorOpaque)`, `useColorPipeline.ts:279`) into `useAtmosphereBoot` → `useAtmosphere`; both files label the param rAF-COALESCED; the coalescer's own doc names the blob-palette derive as a protected consumer. L-4's characterization is the correct one. |
| K-9 | D-13's unhandled-rejection limb | D:540-541 | `writeClipboard` returns a Result and never throws (producer source; CSP D-10's identical refuted clause, both workers + arbiter there). The surviving truth — result discarded, no surface — is roster row 19. |
| K-10 | BPC-1 "root 2" — "the snapshot side is dead; the proxy side propagates" | C:90-95 | Exactly inverted — ruling 1 (this seat's own dependency derivation). The correct statement is roster row 3. |
| K-11 | The `BLOB_HERO`-adoption cure (all three axes' shared prescription) | C:97-105, L:145-165, D:718-722 | Adopting the shipped preset whole regresses three separately-witnessed decisions, read by this seat from the installed dist: `BLOB_HERO.geometry.bodyRadius = 0.22` (vs the W29-pinned 0.325, `test/picker-blob-config.test.ts:26-27`), `surface.fissionAmp = 0` (vs the WR-2/T-49c owner-ruled arming, `HeroBlob.vue:134-150`), `color.paletteStops` = the canned browns (vs the live derived ramp). The DEFECT (a preset authored as literals in a leaf; two divergent hero registers) stands at full weight; the cure as-written does not. |
| K-12 | L's "35 sliders" (three load-bearing uses) | L:45-46/:359-361/:541-542 | BlobPane declares 31 (5+7+5+7+4+2+1, re-counted from source); 35 is the route total including the picker's 4 channels — which makes L-6's mobile sentence self-refuting (on mobile the picker's 4 ARE rendered). |
| K-13 | L-1 summary-table limb "+ fissionAmp inert" | L:614 | No `surface.fissionAmp` slider exists (Lit Glass = 7 other atoms; fissionAmp is in the unreachable-8) — the row conflates L-1 with its opposite (D-12). |

## Superlatives (protect through every rewrite; provenance = this seat's re-verification)

1. **`floorStops()`** — `HeroBlob.vue:97-111`: closed-form two-branch ΔL ink floor with the budget constraint stated in source ("CLOSED-FORM inside the 12ms drag headroom … an iterative solve here is forbidden"), headroom flip at :107-108, and the producer replacement booked with its sizing bracket (:89-92). Design intent enforced as arithmetic; all three axes passed it without comment.
2. **`reseedHeroStops()`** — `HeroBlob.vue:117-132`: ONE named re-seed shared by the live watch and the KeepAlive re-activation, with the invariant in the comment ("the wake path can never diverge from the live path"). The exact discipline the rest of the subtree lacks.
3. **The action bar lifted out of the scroll region** (`ConfigSliderPane.vue:159-163`, W6-6) — the reason the 31st row is reachable — and the coarse tap-zone INTENT (:218-229, extend the zone, not the row); the block is inert today (ruling 3) but the shape is the right one.
4. **The `--slider-track-bg` token handoff** (`:190-202`) — a WCAG 1.4.11 defect cured through the producer's published hook, with measurement, rationale and its born-RED gate recorded in the comment and LANDED (`o18:1161`). It still dies in G-PAINT — but as a superseded correct move, not a mistake.
5. **The `defaultPaneIndex` mechanism** (`viewSchema.ts:88-96` + `useViewManager.ts:59-70`) — schema as single writer, view-TAGGED override so one view's toggle cannot leak. A one-line omission inside a sound mechanism is the best kind of blocker to inherit.
6. **`readPath`'s null-guard** (`:57-64`) and **BlobPane's import hygiene** (:11-15 — clean value/type separation, zero private-source reaches) — re-verified.
7. **The corpus's negative-proof discipline**: L-5/L-7's byte-identical bundle probes published AGAINST the axis's own thesis; D's self-strike of its own supplied frame; BPC-13's checked-and-sound register. This posture is why most of what survived here survived.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

1. **D-6 consequence limb** (WHCM: track vs well, thumb shadow) — single-engine, and the stored frame is a VOID cell (ruling 2). Re-measure chromium-only under CSP G4 discipline: `matchMedia` asserted first, a same-register control element, and **settled reads** (ADD-CSP clause 1 — the `.glass-liquid-fill` background transition can report from-values).
2. **Every pixel-delta figure**: BPC-probe11's table, D-3's phase-2/3 deltas, the byte-identical park PNGs. Mechanisms are source-confirmed (rows 1-3); the numbers are live-only. Open anomaly: `satelliteCount` 3→0 reading 5px — a tracked, prop-minting atom — points at probes run against an already-parked loop; and BPC-1's "producer uniform re-pack covers smoothK and not warpAmp/noiseAmp" needs an engine read. Neither axis resolved it.
3. **The 13-untracked-slider pixel consequence** (row 3): needs a live wake-shape probe (write a membrane leaf on a parked blob; assert no repaint; write a color leaf; assert repaint).
4. **Live-only magnitudes**: D-2's 35.2:1 / 1,141,739px² rects; D-4's scrollH 2611 / 3.76 screens; D-5's measured track/well colours; D-8's 16.4/16.4/11px computed ladder; BPC-6's 12×24 thumb rects. Structural claims all confirmed from source + stored frames; figures unwitnessed by this seat.
5. **Focus visibility** (D's state matrix, declined-to-file): whether the 30%-alpha ring clears 2.4.11/1.4.11 against the painted track in either scheme — producer-token-driven, JS-stamped ink; not statically decidable. Measure, don't carry.
6. **`/#/blob` 200%-zoom arm** and the CSS-viewport threshold — mechanism static (row 5), threshold live-only.

## Addendum (E-3 — beside the immutable records, rewriting nothing)

- **ADD-BP-1 (LabeledField)**: any wave executing CSP G3/RELAY B-3 on this surface must adjudicate the producer's own directive (`ConfiguratorRow.vue.d.ts:38-50`) — ConfiguratorRow is contra-indicated for accessible form controls; `./labeled-field` is published for exactly this; the live value does not belong in the mono TOKEN rung `name`. Rows 15/17 are downstream of this one choice.
- **ADD-BP-2 (routing flag)**: MT-CSP-1 is orphaned from the X ledger (ruled disagreement 6). The next formation boundary must either mint its CC row (folding it to the wave that takes `demo/scenes/ConfigSliderPane.vue`) or supersede it explicitly. Until then, every NO-X-WAVE-OWNER row above is homeless by ledger, not by adjudication.
- **ADD-BP-3 (void-cell law, restated)**: an unlabeled forced-colors frame proves nothing in either direction (ruling 2). Re-capture under G4 or strike from evidence use; the frame stays on disk as an unlabeled-cell record.
- **Dissent preserved**: reader-A and reader-B contradict on the forced-colors frame's evidentiary force; both positions are recorded in ruling 2 and neither is adopted. Reader-A's re-attribution of D-8 limb 2 (producer-baked 11px) and reader-B's tension note on probe11's satelliteCount row are both preserved in the residue rather than harmonised.

## Closing verdict

**DEFECTIVE — BLOCKER sustained on the causal-link family (rows 1-4: overridden atoms, excluded wake set, split reactivity, absent stage), with the route default (row 5) one schema line away and the entire paint/typography/action band already adjudicated once at ConfigSliderPane and never to be booked twice; 13 claims killed, six new rows minted from the hostile re-read, and one orphaned wave flagged to the formation.** Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.

## Addendum 2 (E-3 — second seat, appended 2026-08-04; the record above is untouched)

**Seat receipt**: a second fresh Fable adjudicator (`claude-fable-5`, observed in this seat's own system prompt, not inherited — M-23/L-18) was dispatched against this component after the first seat's structured return was lost to a wall-kill; the record above was found landed at `e5c85303` (whose commit message names this apotheosis) and already consumed downstream (`docs/tranches/X/COHESION.md:105` carries the MT-CSP-1 orphan row citing this file). It is treated as dated evidence — attacked and re-verified, never rewritten.

**Independent re-verification (this seat's own instruments, all re-run this session; zero browser probes).** The heroConfig override + reactivity split re-derived from `HeroBlob.vue:152-178` (untracked census re-summed: Membrane 7 + Pointer 4 + Satellites 2 = 13; `tempo` tracked by the top-level spread); `noteBlobActivity` re-grepped = definition :216 + exactly 3 call sites (:248/:267/:290); `viewSchema.ts:179-187` omission vs the `defaultPaneIndex: 1` siblings (:122/:150) re-read, with `useViewManager.ts:65` `?? 0` and `usePaneRouter.ts` mobile computed; the 31-slider census re-counted from `BlobPane.vue:54-119` (5+7+5+7+4+2+1); `#/blob` in e2e = 0, `BlobPane` in test/+e2e/ = 0 files, `<Blob` component census = 1 (`HeroBlob.vue:13`), `BLOB_HERO` consumers = 0 — all re-grepped; the producer census re-run via node against the installed `@mkbabb/glass-ui/blob-config` (48 leaves / 39 numeric / `satelliteRadius` 0.082 → grid 12.4, the sole off-grid default / `BLOB_HERO` = 0.22/0.3/0.1/0.04, `fissionAmp` 0, canned-brown stops / defaults `morphT` 1, `lit` true, `quality` "full") — K-11's regression grounds re-proven; `test/picker-blob-config.test.ts:20-32`'s four regex pins re-read; the `ConfiguratorRow.vue.d.ts` LabeledField directive re-read verbatim in dist (ADD-BP-1's premise); `ConfigSliderPane.vue`'s `fmt` (:84-86), shallow reset (:92-94), `variant="spectrum"` (:146), `--slider-track-bg` (:202), `:deep` ink (:204-206), section header/title (:232-243), action-bar rule (:245-251) all re-read; K-8's coalescing chain re-walked (`useColorPipeline.ts:279` → `App.vue:251/:287` → `useAtmosphereBoot.ts:62`, the rAF-COALESCED label in place); K-13 re-proven (Lit Glass = 7 atoms, no `fissionAmp` slider); motion-quarantine re-read in place — M-21 TRUE (superlative-aware) carried correctly at ruling 4/K-4, M-33 FALSE cited ONLY inside kill K-7 (no FALSE row is carried as a defect above — quarantine law satisfied), M-34 TRUE; CC-105 + the MT-CSP-1 absence from `registry/CARRY-CUT-LEDGER.md` re-grepped (0 hits — the formation flag stands); the CSP anchors (G-PAINT, C-6/G3, C-7 inert coarse block, D-11/G7, C-2/G2, D-8/B-4, G4's two-channel law) re-read in `ConfigSliderPane.md`. **Ruling 2's deciding vision read was independently re-performed on both frames by this seat**: confirmed, with the mechanism sharpened — the frame is the media-query-fires / UA-substitution-absent half-state (the demo's own `@media (forced-colors: active)` chrome tier visibly applied: card tints flattened to cream, hard borders minted on both footer buttons, the dock, and the Login pill — while NO Canvas substitution occurred anywhere: tracks author-dark, ambient field and Lab ramps fully chromatic). An emulation-flag capture, not a WHCM rendering; VOID in both directions stands (ADD-BP-3 unchanged). **Every spot-check landed; zero corrections to the scoreboard, roster, kills, superlatives, residue, or dispositions.**

**Attribution reconciliation (recorded, not ruled).** The design-and-usage re-reader return as handed to THIS seat differs from the reader-position CELLS in rulings 1-3 above: that return killed BPC-1 mechanism 2 outright (the same inversion argument the table credits to reader-B), killed BPC-6 via the SC 2.5.8 spacing exception (also credited to reader-B), and explicitly declined to open the forced-colors frame ("I did not open the frame") — it did not advance the frame against D-6 as ruling 2's reader-A cell states. Either the returns were revised between dispatches or the cells transposed the seats; the returns were in-prompt artifacts and are not on disk to settle it (this seat searched). The RULINGS are attribution-independent — each was re-derived by the first seat with its own instruments and re-verified by this one — so the verdict register is unaffected; the reader-position cells alone are hereby marked unreliable as a map of who said what.

**ADD-BP-4 (admin-classification cross-cite — the one in-hand missed row the record above does not name).** The design-and-usage reader's missed MAJOR — `useDockAdminMode.ts:27` seats `"atmosphere", "blob"` in `adminViews`, against VISUAL-CONSTITUTION §3.1's member-route inventory (":58 — `/atmosphere`, `/blob` are members", re-read by this seat) and the schema's own `accentHueShift: 320` ninth-primary-hue turn — is fact-identical to the **Dock adjudication's L-19 / structure (b) / DELTA-12 family** (`registry/adjudicated/Dock.md`: the gold latch with no falsifying arm; the required `group: "user" | "admin"` derivation; the 7/5/5 three-owner partition disagreement with router `meta.admin` and the hue law) — ONE identity, already adjudicated at the surface that owns it; not re-booked here (anti-rename). Two things ARE adopted into this record: (i) **the causal link to roster row 11** — the admin classification is why `o7-card-census.spec.ts:38-43` excludes ConfigSliderPane ("session-gated … admin-mode views") and why no user-mode census reaches this pane; BPC-9's vacuous gate is a classification CONSEQUENCE before it is a coverage oversight, and X-W6's o27 scene-contract oracle must not inherit the same exclusion; (ii) **a canon tension flagged to the owner at the next formation boundary**: Dock structure (b) rules atmosphere/blob "declare `admin` (preserving the S.W5-12 intended membership)" while VISUAL-CONSTITUTION §3.1 binds both as first-class members with named compositions — the `group` field cannot land until one authority is ruled primary. (The reader's other unmatched missed rows resolve without new bookings: Card-count = CSP D-8, CONFIRMED there with BANK B-4; aria-valuetext = CSP C-2 RESCOPED → G2 + RELAY B-3, which already carries the richer fact — the unconsumed in-repo `useSliderAnnouncements.ts` formatter.)

— end of addendum 2. The scoreboard, roster, kills, superlatives, residue, and closing verdict above stand exactly as landed at `e5c85303`; dispositions remain ADJUDICATED, not VERIFIED.
