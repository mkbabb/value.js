# picker-debugeventlog — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · served model: `claude-fable-5`

**Seat**: fresh Fable adjudicator (incredulity seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt — recorded against L-11/L-14.
**Date**: 2026-08-07 · **HEAD**: `a5ed40e0` · branch `tranche-u` · **Corpus**: 16 accusations (C-1..C-6, D-1..D-6, L-1..L-4) against `demo/picker/visual/DebugEventLog.vue` (136 lines, SHA-256 `61b8aaee…f6b3f` — re-hashed by this seat, byte-identical to the corpus pin), plus 12 reader-missed claims and 3 verdict-level challenges.
**Readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both hostile re-reads of the same three REPORT-AUTHORED axes (HYDRATION-LEDGER.md:286-288 — 0 evidence artifacts each; no r2 exists on any axis of THIS component; the sibling `picker-pointerdebugoverlay` HAS a D-r2 with a live evidence tree, used here as relay only).
**Method**: both readers were attacked before either was adopted. Every verdict-level disagreement (D-5, D-3, C-5, L-3, the salience-severity split, K-VERDICT's calibration) was re-derived against the tree with this seat's own instruments. Suspiciously-agreed rows (the 19-string vocabulary, the classifier simulation, the fall-through set, the wrap arithmetic's worst-case specimen) were independently re-enumerated, not spot-trusted — and two shared errors were caught (see CORRECTIONS).
**Arbiter's own instruments (all static; zero browser probes — probe parsimony)**: whole-file reads of DebugEventLog.vue (136), PointerDebugOverlay.vue (286, SHA re-pinned `5529d0…c56a27`), usePointerDebug.ts (281), useSliderTouchGates.ts (133); SpectrumCanvas.vue debug call-site grep (7 sites); `logEvent(` repo grep (8 call sites, ALL labelled — no native `e.type` enters the vocabulary); PICKER_CHANNELS key census (picker-color.ts:52-70 — keys are 1–2 letters plus `kelvin`; `alpha` added in the gate cluster); own replay of `eventClass` (:35-43) over all 19 live strings; foundation.css:665-698 forced-colors two-tier read (U-F57/BR-5); `--z-debug` definition grep (zero hits outside audit prose); test/e2e coverage grep (`DebugEventLog|eventClass|debug-event|PointerDebug` → 0 hits); WCAG contrast derivations for #666/#777 on near-black; sibling r2 relay verification (challenge-D-design-r2.md:47-73, :99-103, :134, :166-172, :206-207, :223); CARRY-CUT-LEDGER routing read (CC-081, CC-027, §2 X-W8 row).
**Motion/PRM (M-29 quarantine law)**: `picker-debugeventlog` sits in motion-quarantine.md §7.4 "No motion content" for C·D·L (:281); the only two keyword hits (:300-301) are excluded perf prose (§7.5). **No motion or PRM claim is carried in this record.**

## Scoreboard

**9 ADJUDICATED-CONFIRMED (1 MAJOR · 6 MINOR · 2 INFO) · 8 KILLED · 7 reader-missed identities adopted · 2 shared reader errors corrected · all confirmed rows route to X-W8 (CC-081).**

Top survivor: **ADJ-1** — the severity palette anti-correlates with diagnostic severity for the instrument's three alarm-class strings: the recovery event shouts, the leak signature whispers.

## Ruled disagreements (the seat's actual work)

| row | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-5** | CONFIRMED MAJOR ("understated") | KILLED (mis-homed no-op; self-defeating cure) | **KILLED — LC wins** | DU's MAJOR is carried almost entirely by the parent-owned scroll trap (`.debug-scroll` `overflow-y:auto; pointer-events:none`, PointerDebugOverlay.vue:232-237) — that is the SIBLING seat's D-N3, live-measured there (scrollTop pinned 0, 2586px/91.4% hidden), and may not be double-booked here. The child-owned half (selection blocked) is the component's design invariant S1, stated at :2/:60, corroborated by the sibling seat's own superlative, with Copy JSON (overlay :55-57 → :82-107) as the sanctioned inspection path. `pointer-events` is inherited: the child's :61-67 declarations are no-ops TODAY under the parent's :151/:236 — LC's mechanism verified. But see ADJ-9's KEEP ruling: they stop being no-ops the day the sibling's ratified cure lands. |
| **D-3** | rescoped to a tokenization finding | KILLED (thin residue) | **KILLED as authored; token residue folds to ADJ-9** | Forced-colors is NOT unowned: foundation.css:665-698 is an explicit two-tier policy (my read) — color-DISPLAY surfaces pin `forced-color-adjust:none`, CHROME stays UA `auto`; the overlay is tier-2 by deliberate omission. Color-vision loss is bounded by S2 (type renders as literal text at :15). `CAP` (:17) is word-not-color — the correct idiom, not a defect. DU's surviving atom (0 tokens in the child vs 5 `var()` in the parent) is real (my read) but is hygiene, not a11y — INFO, folded to ADJ-9. |
| **C-5** | KILLED | CONFIRMED INFO (asymmetry) | **KILLED — DU wins** | All 7 live `extra` producers enumerated by this seat: 3 compile-time constants, 2 literal-bounded, 2 scaling with concurrent-pointer count (digitizer-capped), interpolated elements already 40-char-capped by describeElement (:31). `flex-wrap:wrap` (:74) degrades gracefully. A type-level unboundedness with no reachable bad input is the C-4 pattern: a cure for a condition the tree cannot produce. The clip-asymmetry (`.debug-extra` :109-111 lacks `.debug-tgt`'s overflow treatment) is recorded as an observation inside K-3, not a roster row. |
| **L-3** | mostly confirmed INFO | KILLED as prescribed | **KILLED as prescribed — LC wins; concrete residue folds to ADJ-9** | Consumer count = 1 (overlay :44/:70, my grep). A shared diagnostics trace/table primitive for a single dev-gated consumer is KISS-banned contrivance (feedback_kiss_no_contrivance; feedback_glass_ui_first_class — a pointer-diagnostics table is not a glass-ui primitive), and importing shared interactive machinery into the measured subtree violates the instrument invariant (S1/K-6). Copy/export is owned at the CORRECT altitude — the parent, which alone sees gauges/UA/screen. The true, cheap version of L-3 is the located verbatim duplication → ADJ-9. |
| **salience severity** | MAJOR | MINOR (dev-only cap) | **MAJOR — DU wins, on a tiebreaker LC's own relay supplies** | LC's cap premise ("a URL-gated developer instrument that never renders for a user") is WEAKENED by the sibling's live production proof: `isDebugEnabled()` (:34-39) is a substring gate, `?nodebug=1` contains `debug=1`, and the sibling r2 (:134) rendered the overlay LIVE on `https://color.babb.dev/?nodebug=1`. The surface is reachable in production. Within the instrument, the palette inverts its sole purpose in both directions (ADJ-1); S2's text redundancy caps it below BLOCKER, the leaky gate lifts it above LC's MINOR. |

## CORRECTIONS — two shared reader errors, caught by re-enumeration

1. **The fall-through count is FOUR, not three.** Both readers state exactly three live strings return `""` from `eventClass`. My replay of :35-43 over all 19: `ORPHAN_POINTERS`, `spec:gate-block`, `sl:*:gate-block` **and `spec:move`** fall through. `spec:move` un-tinted is benign (routine traffic should not shout), so ADJ-1's substance is unchanged — but "exactly three" is a miscount, and identical miscounts in both "independent" readers is a citation-inheritance odor (the ColorSpaceSelector DISSENT-1 pattern), recorded here as fleet evidence.
2. **`sl:lightness:cancel-release` (27 ch) is a fabricated specimen.** Both readers cite it as the live worst-case type string. PICKER_CHANNELS keys (picker-color.ts:52-70) are 1–2 letters (`r,g,b,h,s,l,v,w,a,c,x,y,z,i,ct,cp,jz,az,bz`) plus `kelvin`; the gate cluster adds `alpha`. The live worst case is **`sl:kelvin:cancel-release` (24 ch)**. ADJ-5's structural claim survives with corrected magnitude; the identical invented channel name in both returns is the same inheritance odor.

## Adjudicated defect roster

Line anchors are HEAD `a5ed40e0`; re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

- **ADJ-1 — MAJOR (instrument-scoped) · merged C-3 + L-2 + missed-salience (both readers).** The untyped `type: string` contract (usePointerDebug.ts:3-10) plus the substring classifier (DebugEventLog.vue:35-43) misroutes 3 of the 19 live vocabulary strings, anti-correlating visual severity with diagnostic severity in BOTH directions: `UNFREEZE` (:200 — the recovery signal) matches `includes("FREEZE")` by containment and paints alarm-red `rgba(255,40,40,0.2)` (:113-115), pixel-identical to `FREEZE_DETECTED`; `ORPHAN_POINTERS` (:222 — the literal iOS pointer-leak signature this instrument exists to catch) and both `gate-block` strings fall through unstyled, while routine `spec:down`/`spec:up` get tints. Proof: this seat's own vocabulary harvest (8 labelled `logEvent`/`log` producer files, zero unlabelled call sites) + classifier replay. C-3's stated mechanisms (multi-token order-dependence, case loss) have ZERO live instances — confirmed by all three seats independently; do not close against an order test. L-2 is the root cause; C-3 the symptom; the missed-salience find the consequence — **ONE identity**. Cure shape: severity as data (typed kind on the event, or an exact-match table), receipted by the five-line table test over the closed 19-string vocabulary (reader-LC's missed #6, folded here — current coverage: 0 hits in test/ + e2e/, my grep). **Route: X-W8** (CC-081 homes the PointerDebugOverlay-cluster cures).
- **ADJ-2 — MINOR · merged D-2 + missed unit-divergence (reader-LC).** The clock is unlabelled AND the two output surfaces disagree by 1000×: the UI renders seconds `(evt.ts / 1000).toFixed(2)` (:14, no unit, no column header), the export writes raw milliseconds `t: +e.ts.toFixed(1)` (overlay :96, bare key). The same event reads `0.42` on screen and `424.3` in the copied JSON — the eyeball-then-export correlation workflow silently breaks. Both halves verified by this seat's reads. C-4's "clock validation" framing stays dead (K-2). **Route: X-W8.**
- **ADJ-3 — MINOR · merged D-4 + L-4 (one identity: L-4 root, D-4 symptom).** Retention/ordering/dropped semantics are absent from the boundary: `Events ({{ events.length }})` (:5) is the RETAINED count; eviction is silent (`splice` at usePointerDebug.ts:74-76 against MAX_EVENTS=80 at :20, no dropped/total field in either interface at :3-18); ordering inverted at :33 with no label. Once saturated the counter freezes at 80 forever, and the evidence destroyed first is precisely the causal window before a `FREEZE_DETECTED` (which fires only after 2500 ms of silence, :21/:242-252). LC's narrowing adopted: absent from the TYPE, present in the module — the cure is a type/label lift, not an architecture. **Route: X-W8.**
- **ADJ-4 — MINOR · missed by all three axes; sibling relay, picked up here (the relay reader-DU flagged as dropped).** Two text colors fail WCAG AA at 10px: `.debug-ts` `#666` (:80-83) and `.debug-tgt` `#777` (:101-107), sibling-measured live at **3.47:1** and **4.45:1** (picker-pointerdebugoverlay/challenge-D-design-r2.md:166-172, ":172 — numbers relayed there"). This seat's own static derivation sharpens the relay: against pure black #666 ceilings at ~3.66:1 — **fails under every reachable backdrop** — while #777 computes ~4.69:1, so `.debug-tgt`'s failure is backdrop-dependent (the 0.92-alpha blur lightens the effective ground); the sibling's live 4.45 governs the live tree. These are the two columns ADJ-2 and ADJ-7 independently identify as hardest to read — the defect compounds. **Route: X-W8** (CC-081 names this cluster's a11y explicitly).
- **ADJ-5 — MINOR · merged missed column-budget rows (both readers), magnitude corrected.** Budget is allocated inversely to diagnostic value: `.debug-etype` has `min-width:60px` and NO cap (:85-89) while `.debug-tgt` is hard-capped at 120px (:101-107). Fixed minima 40+60+24+120 = 244px + three 4px gaps = 256px against ~258px of content (280 − 2px borders − 20px padding); any `CAP` or `extra` span forces `flex-wrap:wrap` (:74) — and every alarm row carries an `extra` (FREEZE_DETECTED, ORPHAN_POINTERS, cancel-release), so the diagnostic rows are the ones that wrap and eat the already-scarce viewport. Corrected worst live specimen: `sl:kelvin:cancel-release` (24 ch), not the readers' invented 27-ch string (CORRECTIONS #2). Exact wrap threshold UNPROVEN-NEEDS-LIVE (residue R-2). **Route: X-W8.**
- **ADJ-6 — MINOR · C-2 (both readers agree; severity as re-bounded).** `:key="i"` over the reversed array (:9-10, :33): every push shifts every reversed index, so Vue in-place-patches all ~80 rows instead of prepending one. No observable consequence TODAY (no transitions, no selection, no per-row state; subtree inert) — the SOURCE-RED urgency is not earned — but it becomes a real defect the moment any ratified cure adds row interactivity or motion. Cure: a monotonic event id minted in the producer's `log()` (:57-77), the only constructor. **Route: X-W8** (rides the ADJ-1/ADJ-3 producer touch).
- **ADJ-7 — MINOR · D-6 fact confirmed, cure refuted.** Double truncation: the producer clips targets to 40 chars (describeElement :31), then CSS clips ~40 to ~20 visible (`max-width:120px` at 10px mono, :101-107) — `div.slider-track.is-active` renders `div.slider-track.is-…`. The prescribed `title` cure is REFUTED by the corpus's own D-5: a native tooltip needs hit-testing and the subtree is inert — a `title` here can never render on any input modality. The recoverable path exists (Copy JSON exports the untruncated 40-char `tgt`, overlay :99); the cure is layout budget (with ADJ-5), never interactivity. **Route: X-W8.**
- **ADJ-8 — INFO · merged D-1 residue + C-6 (one a11y-semantics identity).** Six unlabelled sibling `<span>`s in a wrapping flex row (:14-19, :69-78), no header row, no `role="log"`/list/table semantics, no heading element beyond the styled div (:4-6). Adopt log semantics; **explicitly DECLINE any live-region announcement strategy** (both readers concur; a feed that can burst during a drag is AT spam — the finding closes by declining, not inventing). D-1's "no copyable path" clause is dead (K-4 territory: Copy JSON exists). **Route: X-W8.**
- **ADJ-9 — INFO · merged CSS-hygiene identities (missed by all three axes; located by both readers) + this seat's KEEP ruling.** (a) `.debug-section` (:47-49) and `.debug-section-title` (:51-58) are byte-duplicated from the parent (:196-207); under Vue scoped-CSS the parent's scope id stamps the child's ROOT (:3), so the child's `.debug-section` copy is redundant TODAY — while `.debug-section-title`, a descendant, genuinely needs its copy; a cleanup must not delete the wrong one. (b) `.debug-event` re-declares `font-family/font-size/line-height` (:75-77) inherited verbatim from `.debug-overlay` (parent :143-145). (c) Zero design tokens in the child beside five `var()` uses in the parent (D-3 residue; token half eligible to fold into X-W10's design law). (d) **KEEP ruling — the cure-conflict resolution (reader-DU's missed #4, ruled here)**: the two `pointer-events:none` declarations (:61-67) are no-ops under today's ancestors, **but they are the designated landing mechanism for the sibling's ratified D-N3 cure** ("move `pointer-events:none` off `.debug-scroll` ONTO the rows inside DebugEventLog", sibling r2:207) — the sibling's posture WINS over D-1/D-5's selection demand, rows stay inert, selection closes against Copy JSON. Deleting :61-67 as "dead code" before D-N3 lands would re-open the touch-stealing hole the file exists to prevent. **Route: X-W8**, sequenced with the sibling's scrollport cure.

## Killed-claims register

- **K-1 — C-1 (perf of `[...events].reverse()`) — KILLED, unanimous, refutation verified.** Self-neutering ("bounded O(80)" in its own text); the computed invalidates only on `events` mutation — the 500 ms gauge ticks don't reach it (same array proxy through :44; props unchanged); the only high-rate source is producer-sampled at 3% (`Math.random() < 0.03`, SpectrumCanvas :163) and slider moves never log (useSliderTouchGates :69-71). The closure demand to profile "the maximum retained 80-event rate" profiles a rate the tree cannot reach — descoped, not scheduled.
- **K-2 — C-4 (clock validation) — KILLED, unanimous.** `ts` has exactly one producer (`performance.now()`, :66, sole constructor `log()`); finite, millisecond, single-origin by specification. No NaN/Infinity/mixed-clock path exists. The one true atom (unlabelled rendering) lives in ADJ-2. A guard for a state the platform cannot produce.
- **K-3 — C-5 (unbounded `extra`) — KILLED as ruled** (see Ruled disagreements). Observation preserved without a row: `.debug-extra` lacks the overflow treatment `.debug-tgt` has; if a future producer ever interpolates content-controlled strings into `extra`, this revives with a witness.
- **K-4 — D-5 (pointer-events as defect) — KILLED as ruled.** Mis-homed (inherited no-ops; the posture is the parent's), self-defeating (the exemption is the instrument's reason to exist — S1), and internally contradictory with D-6's hover cure. Residues homed: the scroll trap = sibling-owned D-N3 (never re-booked here); the declarations = ADJ-9(d) with the KEEP ruling; selection = Copy JSON (S5).
- **K-5 — D-3 (forced-colors/color-vision unowned) — KILLED as ruled.** The two-tier policy exists and governs by deliberate omission (foundation.css:665-698, my read); severity is redundantly text-encoded (S2); `CAP` is the correct word-not-color idiom (S3). Token residue → ADJ-9(c). Rendered WHCM outcome = residue R-1.
- **K-6 — L-1 (hand-authored presentation as defect) — KILLED, unanimous.** Polarity backwards: zero coupling is a correctness requirement for an instrument measuring pointer capture — shared interactive machinery would drag listeners and capture surface into the measured subtree. Stated in the file's own comments (:2, :60).
- **K-7 — L-3's prescription (shared diagnostics primitive + normalized snapshot type) — KILLED as ruled.** One consumer; KISS/glass-ui-first edicts; wrong altitude for copy/export (parent owns the facts the export needs). The located duplication residue → ADJ-9(a-b); the contract gap → ADJ-3.
- **K-8 — the triple SOURCE-RED verdicts — KILLED as calibrated, with this seat's amendment.** All three axes are REPORT-AUTHORED (HYDRATION-LEDGER:286-288, 0 evidence artifacts; unverified-until-re-proven per AXIS-PROMOTIONS law; no r2 on any axis). On re-proof: 6 of 16 accusations killed outright, 3 more lose their cures, survivors top at one instrument-scoped MAJOR. AMENDMENT against reader-LC: the "never renders for a user" premise is refuted by the sibling's live production witness (`?nodebug=1` renders the overlay on color.babb.dev — the substring gate is leaky), so the correct re-verdict is **instrument-AMBER**, not the two-full-steps demotion LC argued. Neither reading sustains RED.

## Superlatives — with provenance

- **S1 — the non-perturbation discipline** (`pointer-events:none` subtree, :2/:60-67; parent re-enables only header + actions). The design axis read the component's central insight as its principal defect. Provenance: both readers independently; the sibling seat's own superlative list; this seat's read of all three comment sites.
- **S2 — severity is redundantly encoded**: every row carries its meaning as literal text (:15) beside the tint (:113-135) — the property whose absence D-3 assumed, and the reason color-stripping loses nothing. Provenance: both readers; verified in template by this seat.
- **S3 — `CAP` renders as a word, not a color dot** (:17) — survives WHCM substitution intact. Provenance: reader-DU; verified.
- **S4 — the naive reverse is correct-by-context**: someone did the rate analysis UPSTREAM — 3% sampling on the only firehose (SpectrumCanvas :163), no per-move slider logging, edge-triggered freeze logging (:243). Cheap code made safe by a producer-side decision. Provenance: both readers; call sites re-verified by this seat. Caveat preserved (reader-LC): random sampling can drop the one move that mattered, undocumented at the call site.
- **S5 — the Copy JSON escape hatch** (overlay :55-57 → :82-107, `execCommand` fallback :116-127): one-tap structured export of all six facts per event plus UA/screen/DPR/gauges/frozen — strictly better than drag-selecting 10px text on the target platform, and the fact that kills D-1's copy clause. Provenance: both readers; verified whole by this seat. (Its `t` unit divergence is ADJ-2.)
- **S6 — `describeElement()` normalization** (usePointerDebug.ts:23-32): `instanceof` guard, `"??"` sentinel, the SVG `SVGAnimatedString` hazard handled explicitly, hard 40-char cap — the reason C-5 collapsed. Provenance: both readers; verified.
- **S7 — complete listener teardown, capture-matched** (:256-264): all six document-level capture listeners removed with `{capture:true}` — the half of the removal match key most cleanup code botches — plus the interval cleared. Provenance: reader-LC; re-verified line-by-line by this seat.
- **S8 — the instrument refuses to observe itself**: `isDebugOverlay()` (:149-152) heads all six global handlers (:160/:181/:195/:205/:214/:218), so touching the panel never pollutes the log or clears a freeze under measurement. Provenance: reader-LC; all six call sites re-verified by this seat.
- **S9 — bounded in-place retention** (push + splice against MAX_EVENTS=80, :73-76): no growth, no churn, no timer — the contract that left C-1 nothing to land on. Provenance: reader-LC; verified.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-1** — the rendered WHCM outcome: tier-2-by-omission is inferred from foundation.css (zero `.debug-*` selectors in any forced-colors block); that the six tints drop while the type text survives is UNPROVEN-NEEDS-LIVE. Information loss bounded regardless (S2).
- **R-2** — ADJ-5's magnitude: the wrap threshold and visible-row count at 390×844 with the corrected worst specimen `sl:kelvin:cancel-release` (24 ch). The structural over-budget is proven from source; the pixel arithmetic assumes ~0.6em mono advance.
- **R-3** — iOS Safari behavior of the inert scrollport (sibling's chromium-cell measurement relayed; iOS is the instrument's target platform). Sibling-owned surface; recorded here only because it halves this leaf's effective viewport (with ADJ-5's wrapping).
- **R-4** — a live BEFORE witness of ADJ-1's tints during an actual freeze/orphan repro (the static replay is decisive; the witness belongs to X-W8's delta obligations, not to this record's proof).
- **DISCHARGED, not carried**: reader-LC's residue "is the overlay reachable in the deployed build at all" — answered LIVE by the sibling r2 (:134): production RENDERS it at `?nodebug=1` with `--z-debug: 99999` resolved. The working-tree `--z-debug` orphaning (zero definitions, my grep; sole consumer overlay :136; regression from `c84504d3`) is the sibling's D-N1 BLOCKER — cross-referenced so no finding here is re-measured against an occluded surface, never re-booked.

## Method notes / dissent preserved

1. Both readers cite axis-doc line numbers that do not exist in the tracked challenge files (e.g. "challenge-D-design.md:19/:20" for D-5; the file's findings sit at :11-16) — content-accurate, anchor-wrong, in the same direction in both returns. With CORRECTIONS #1-2, that is three same-shaped coincidences across two nominally independent seats; recorded as fleet-level citation-inheritance evidence (the ColorSpaceSelector DISSENT-1 class), no verdict weight taken or given.
2. Reader-DU's severity instinct (MAJOR for the salience inversion) and reader-LC's calibration instinct (the dev-gate cap) were BOTH half right; the ruling needed a fact neither reader connected — the leaky substring gate proven live by the sibling. Preserved as the seat's argument for cross-component relay reads at adjudication time.
3. Reader-LC's C-2 non-re-render mechanism (same array proxy → child skips re-render on gauge ticks) is adopted as corroboration of K-1 but was not independently instrumented here (static Vue semantics only) — labelled so.

## Closing verdict

**ADJUDICATED: instrument-AMBER at HEAD `a5ed40e0` — the triple SOURCE-RED is refuted as calibrated (K-8); 9 rows survive (ADJ-1 MAJOR: the severity palette inverts the instrument's purpose for its three alarm strings; 6 MINOR; 2 INFO), every one routed to X-W8 per CC-081 with the classifier cure receipted by a five-line table test; 8 claims are killed with written refutations; the component's core disciplines — non-perturbation, redundant encoding, upstream sampling, self-exclusion — are superlatives the axes misread as defects. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.**

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3.
