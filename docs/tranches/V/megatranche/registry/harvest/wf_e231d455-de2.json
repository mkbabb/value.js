{
 "runId": "wf_e231d455-de2",
 "workflow": "wf_e231d455-de2",
 "resultCount": 4,
 "results": [
  {
   "agentId": "a30dc2c82e210ddfc",
   "key": "v2:46479a155648ca097ac5cb782168f93ba69b80efba99eafd51ffc8cdbe782f2c",
   "result": {
    "modelObserved": "Fable 5 — exact model id `claude-fable-5`, observed directly by this seat from its own system prompt (not inherited from the spawn label). Session 2026-07-28, HEAD c654824e, node/darwin arm64. Seat = worker-F per the M-12 tri-fold (Fable seats are by design; the all-Opus mark governs mechanical/challenge seats).",
    "slug": "wb-gradient-stopeditor-r3-delta",
    "verdicts": [
     {
      "id": "C1 + D2-03 + Lr2·L-1 (≡ r1 L-1/D-2)",
      "verdict": "CONFIRMED",
      "note": "The three-owner ordering hole, asserted by a third and fourth independent seat with convergent digits. Re-derived at source today: updateStop (useGradientModel.ts:127-131) has no ordering policy while addStop (:118) sorts and gradientParse.ts:284-290 rejects; this seat's own vite-node parse-probe run TODAY re-confirmed the REJECT on the exact descending string. BLOCKER stands; gates G1/G2 unchanged from r1. The clamp-vs-sort cure fork is ruled in dissent: the gates assert the invariant (non-decreasing + round-trip), not the policy."
     },
     {
      "id": "C2 (passes 1+2)",
      "verdict": "CONFIRMED",
      "note": "Index-keyed intervals + tail-only length watcher (:89-100) vs sorted insert (:118) re-verified at the line — inserting a stop silently re-pairs every downstream authored easing. Already folded into MT-GRADSTOP-1 as the r1 G2c splice-at-insert amendment; the fresh cure (easing hung on the stop that opens it, deleting the parallel array + all three 'interval N is missing' throws) is structurally superior and BOOKED as W-GRADSTOP-3's model shape with the supersession recorded. BLOCKER."
     },
     {
      "id": "C3 (≡ r1 L-9/D-11)",
      "verdict": "CONFIRMED",
      "note": "Double dispatch per drag frame — third measurement (21/21 bar-branch fires), mechanism source-certain (:91-99 false 'fallback' comment; capture retargets but does not stop bubbling). Fold; G6 unchanged; the r1 Addendum §8 cost-claim correction binds C3's prose too (2× emit + 2× stops.map, ONE serializer flush)."
     },
     {
      "id": "C4",
      "verdict": "CONFIRMED",
      "note": "Test truth, statically decided by this seat today: grep .github/workflows → Playwright wired to NO CI job; o21-gradient-rail.spec.ts:135/:163 assert gradient-ruler-cap/gradient-rung and gradient.spec.ts:57/:112 assert the Perceived-space plate — all 0 matches in demo/+src/, so ≥4 assertions target retired DOM and the gate is RED at HEAD by construction. The 5-of-13 count stays attributed to the challenger's 4.2m run. The HANDLE_HALF=0 vacuity mutation is adopted as GRADSTOP-A §13's canonical witness. Spec re-anchoring = core-wave scope 6 (amended); CI wiring BUILDs at W-TOOLCHAIN. MAJOR."
     },
     {
      "id": "C5 + Lr2·L-2 + D2-01-root (≡ r1 L-2 = MT-F001 = π-R1)",
      "verdict": "RESCOPED",
      "rescopedTo": "Parser band — registry/adjudicated/parser-band.md, ruled winner cand-O (totality: 0/172 throws vs published 102/172); = MT-F001 (ROOT-FINDINGS.md:11) = π row R1, pinned/immutable. MT-F024 recorded as the parser-band alias of the same crash (L-5 anti-rename; MT-F001 primary).",
      "note": "Re-thrown TODAY by this seat's own vite-node run: all 8 empty-argument colour functions throw 'Cannot read properties of undefined (reading replace)' from src/css/grammar.ts:181, and parseGradientCSS('linear-gradient(90deg, oklch(), blue)') throws through the isColorToken total-oracle assumption at gradientParse.ts:93. The cure is cand-O totality in the library — and all THREE fresh seats independently refuse the local try/catch (masking-fallback ban), converging with the r1 ruling. The runnable bank (r1 BANK 1) is the sole coupling; when it exits 0 the code-editor arm is re-witnessed (a typed oklch() must yield a verdict, not a throw)."
     },
     {
      "id": "D2-01 (consequence arm)",
      "verdict": "CONFIRMED",
      "note": "Eight typed characters DO silently annihilate the route's entire working surface — probe7: stops 2→0, bar/tile gone, verdict null, editorText empty, pageErrors [] (the throw rides a watcher-scheduled debounce, so the Vue boundary catches it with ZERO console evidence — why the visual audit's pageErr-0 telemetry is structurally blind to this class). BLOCKER stands. But the 'and dock' arm is REFUTED by the shell read: App.vue puts nav+Dock BEFORE <main> (:47) and the ErrorBoundary (:50) inside it — the shell survives, and 'destroys the whole application' may not be cited (GRADSTOP-A §10). No local guard exists or is permitted; upstream totality makes applyCSS's atomic contract true as written."
     },
     {
      "id": "C6 + C-7 + D2-06 (≡ r1 D-7 + D-8 rail arm)",
      "verdict": "CONFIRMED",
      "note": "Third/fourth independent keyboard-and-role census, identical numbers: keyboard grammar is a third of the §5.2 spec (:173-187 — no Home/End/Up/Down/PageUp/Space), role null, no aria-value*, no ordinal, no aria-live, no focus restoration after remove, rail unnamed and keyboard-unreachable for add (WCAG 2.1.1/2.4.3/4.1.2). Fold into r1 D-7 → W-GRADSTOP-2, unchanged."
     },
     {
      "id": "C7 + C13 + D2-02 (≡ r1 D-1/L-3)",
      "verdict": "CONFIRMED",
      "note": "The dual-axis family with fresh instruments: the linear pixel-error table (-10.5..+10.0px, zero at 50%) and the two-denominator measurement (getPosition over the 304px border box vs handleLeft over the 302px padding box). Joins G3/G4's evidence; Δhue ≈2.9° near terminals recorded. Severity stays MAJOR per the r1 arbiter's ruling — D2-02's BLOCKER grade joins the preserved dissent."
     },
     {
      "id": "C8 + Lr2·L-5 (≡ r1 L-10/D-12)",
      "verdict": "CONFIRMED",
      "note": "Dual selection channel unchanged (delete the emit at :23/:130 and the @select at GradientVisualizer.vue:144; demote the unbound outer defineModel :51). Pass-2's mechanism correction ADOPTED: because the parent DOES bind v-model:selected-id, vue 3.5.35's useModel takes the parent path — a same-tick read-after-write of selectedId IS stale, and today's code survives only by ordering luck (wasSelected captured at :123 before the write at :129). The armed hazard is recorded; the cure (one channel) already removes it."
     },
     {
      "id": "C9 + D2-16 style arm",
      "verdict": "CONFIRMED",
      "note": "Hover modelled in the reactivity system because the inline transform shadows every hover: utility — the file documents its own trap twice (:30-33, :255-261) and re-enters it for transition. Source-certain, no probe needed. Cure BUILDs into the core wave: inline style carries DATA only (--stop-pos/--stop-color); geometry+state move to the scoped sheet under :hover/[data-selected]; hoveredId, handleScale and two listeners per handle are deleted. MINOR."
     },
     {
      "id": "C10 (passes 1+2)",
      "verdict": "CONFIRMED",
      "note": "All four facts re-verified at source today: unreachable var() fallbacks (:318 --radius-pill, :388 --touch-target — both resolve on :root), the dead cssColorOpaque inject (GradientPane.vue:8, never referenced), the no-op colorAt = undefined default (:6), and the :76 'if (!barRef.value) return 0' — a null guard answering an unanswerable question with a DESTRUCTIVE value (silent warp-to-0%). All four die in the core wave. INFO."
     },
     {
      "id": "C11",
      "verdict": "CONFIRMED",
      "note": "Grab-offset teleport — INDEPENDENTLY REPRODUCED by this seat today (gate-gesture.mjs arm A, pasted): press 8px right of the 0% handle's centre, travel 1px → handle travelled 10.11px, label 0%→2% (challenger's live 10.12px). Mechanism source-certain: onHandlePointerDown records no grabDx; :145 emits absolute getPosition(e); the 4px dead-zone the file computes (:138-144) is wired only to the deselect decision, never to the emit. Cure (grabDx + dead-zone-gated first emit + cached rect) BUILDs into the core wave and halves C3's forced-layout cost. Graded MAJOR (precision-destructive, data-recoverable); challenger's BLOCKER preserved in dissent."
     },
     {
      "id": "C12",
      "verdict": "CONFIRMED",
      "note": "Pointer selection never focuses the handle — REPRODUCED by this seat today (gate-gesture.mjs arm B): real mouse click → activeElement BODY, 2×ArrowRight had no effect. e.preventDefault() at :120 suppresses the focus default; every line of the keyboard handler (:173-187) assumes focus follows selection; the :169-172 docstring describes a state the pointer path can never reach; the entire :328-359 focus-ring system is pointer-unreachable. Cure: drop preventDefault (select-none touch-none + pointer capture already suppress the defaults that mattered). MAJOR, core wave, gate G9."
     },
     {
      "id": "C14",
      "verdict": "CONFIRMED",
      "note": "UPGRADED from latent-hypothesis to REPRODUCED: this seat's gate-gesture.mjs arm C minted a stop with a right-click on the bare rail ON DEMAND (2→3, chromium cell) — the pointerdown(button=2) arms pendingAdd (no e.button predicate anywhere, :85-89), the delivered pointerup consumes it. The challenger's honest HYPOTHESIS label is superseded by reproduction. ARBITER SHARPENING (new, source-certain): @pointercancel='onBarPointerUp' (:208) routes CANCELLATION through the add-consuming path — a cancelled touch gesture inside the 4px dead-zone also mints. Cure: e.button!==0 early-return at both pointerdowns + a cancel path that ONLY disarms. MAJOR, gate G10, GRADSTOP-A §12."
     },
     {
      "id": "R1-C1 (correction of r1-corpus L-8(b))",
      "verdict": "CONFIRMED",
      "note": "Verified by this seat's own instrument run today: npx tsc --traceResolution resolves @mkbabb/value.js/css to /Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts (repo dist, no node_modules segment) — the 'two value.js's in one build' mechanism is a measured FALSE POSITIVE; the installed copy is keyframes.js's transitive dependency, consulted by nothing. CONSEQUENCE: W-TOOLCHAIN-ONE-ARTEFACT re-scoped — the type/runtime-split arm is dead and may not be cited (GRADSTOP-A §9); the r1 arbiter's OWN §0 stale-server third-artifact proof is UNAFFECTED and survives as the wave's exhibit. Cure re-ruled per R1-C1: DELETE the value.js paths block entirely (exports self-resolution serves types; vite generates runtime aliases from the same map); no workspace link."
     },
     {
      "id": "Lr2·L-3 (≡ r1 L-13)",
      "verdict": "GLASS-OWNED",
      "note": "Same ruling as r1 L-13: the bespoke multi-thumb slider's substance sits in our tree but the cure (variant='rail'/SliderThumb slot) is a producer change — glass-ui is not ours, no local patch may be proposed, and a wave gated on a sibling repo shipping is not completable (L-1). Disposition unchanged: relay to the standing glass BH inbox fond + runnable banks 2/3; the a11y/interaction contract lands locally NOW via D-7 in W-GRADSTOP-2."
     },
     {
      "id": "Lr2·L-4 (≡ r1 L-5)",
      "verdict": "CONFIRMED",
      "note": "Dead re-export barrels re-confirmed (the 7-name grep loop: 0 non-comment consumers; the 3-hop constants chain vs MixConfigBar's 1 hop). Fold into the r1 L-5 identity → W-GRADSTOP-3, after L-7's leaf lands."
     },
     {
      "id": "Lr2·L-6 (≡ r1 L-6)",
      "verdict": "RESCOPED",
      "rescopedTo": "W-HYGIENE demo/ui extirpation — the ColorPicker adjudication's L-6 identity (90-site extirpation), already booked; never re-booked (anti-rename).",
      "note": "Facts unanimous and unchanged from r1 (19 content-free alias shims; GradientStopEditor itself imports nothing from demo/ui). One identity, one booking."
     },
     {
      "id": "Lr2·L-7 (≡ r1 L-7)",
      "verdict": "CONFIRMED",
      "note": "Types homed in the state factory — both type-only cycles re-verified at the same import sites (model:15-16 value-out; css:31-34 + parse:23-27 type-back; GradientStopEditor.vue:4 hangs on the apex). Leaf model/types.ts remains W-GRADSTOP-3's opening move."
     },
     {
      "id": "Lr2·L-8",
      "verdict": "CONFIRMED",
      "note": "tsconfig paths drift re-measured today, slightly STRONGER than claimed: exports = 7 keys no '.'; the paths block maps root + /parsing + /units at THREE missing files (dist/index.d.ts, dist/subpaths/parsing.d.ts, dist/subpaths/units.d.ts — all ls-verified absent); /css and /value have NO entry and resolve correctly via exports — the proof the block is unnecessary rather than incomplete. Cure = R1-C1's deletion. FOLD → W-HYGIENE config parity (ColorSpaceSelector L-14 identity, cross-referenced)."
     },
     {
      "id": "R2-A1",
      "verdict": "CONFIRMED",
      "note": "The minimum-2-stops rule implemented twice — component :66 (removable computed) vs model :122-123 (removeStop floor), re-verified. Two homes agree today by coincidence. Cure: the model exposes canRemove; the chip consumes it as state. Core-wave amendment (both lines are already in the wave's scope). MINOR."
     },
     {
      "id": "R2-A3",
      "verdict": "CONFIRMED",
      "note": "The self-described MATERIAL CONTRACT written out in four files — re-verified today: GradientStopEditor :317-326, GradientVisualizer :271-278, GradientEasingEditor ×2 (:154/:246), ComponentSliders.vue:191-199 inline. One recipe, one home → W-GRADSTOP-3 as a demo-owned surface class NOW; the glass-variant arm rides the existing L-13 relay (waiting on a producer to deduplicate our own copies would be a deferral wearing the boundary's name — dissent §r3-6). INFO."
     },
     {
      "id": "D2-04 (≡ r1 D-5 + measured arm)",
      "verdict": "CONFIRMED",
      "note": "The r1-confirmed tautology now carries MEASURED teeth: ring:fill 1.00:1/1.03:1 on the white ramp (handles literally invisible — a data-dependent TOTAL operability loss, hover ghost included) and 6/10 rainbow hues below the WCAG 1.4.11 3:1 floor against the sole boundary (border-white/80, both schemes, :235-240 re-verified). Severity RAISED to BLOCKER for the class. INTERIM cure added to the core wave: generalize the file's own dual-contrast focus recipe (:342-349) to the resting/selected boundary — gate G11 born-RED on the white-ramp input. Constitutional home (seat rail + WatercolorDot faces) stays W-GRADSTOP-2."
     },
     {
      "id": "D2-05 (≡ r1 D-3)",
      "verdict": "CONFIRMED",
      "note": "The touch-delete overlap with its band now measured: handle zone 198.7..242.7 ∩ chip zone 234.7..278.7 = 8px INSIDE the rail, chip wins by DOM order + z-20; coarse pseudo measured 44×44. Joins G5's evidence; playwright-webkit cell, never safari-app (I-20). Fold."
     },
     {
      "id": "D2-07 (≡ r1 D-8)",
      "verdict": "CONFIRMED",
      "note": "Add starvation with the modality arm sharpened: the ONLY add affordance is a hover ghost (nonexistent on touch), the bar is a bare unnamed div (role/tabindex/label null), each stop sterilises 44px (13.6% of the mobile rail) with zero feedback, and the :35-41 'self-evident, no instruction line needed' comment is measured false for half the input modalities. Fold → W-GRADSTOP-2."
     },
     {
      "id": "D2-08 (≡ r1 D-9)",
      "verdict": "CONFIRMED",
      "note": "Selection has no payload — re-greped: selectedStopId = exactly 3 lines, all writes, no reader; nothing in the product edits a stop's colour (select→tune→commit has no tune); the dead CSS_COLOR_KEY inject re-verified at GradientPane.vue:8. Inspector lands in W-GRADSTOP-2; the dead inject dies in the core wave. Fold."
     },
     {
      "id": "D2-09 (≡ r1 D-4)",
      "verdict": "CONFIRMED",
      "note": "Chip reserves zero geometry — same class arithmetic as r1 (28px borrow across the parent's gap-5, painting over the next section's hr). G7 unchanged. Fold."
     },
     {
      "id": "D2-10",
      "verdict": "CONFIRMED",
      "note": "Three destructive removal species re-verified at source (:164-167 contextmenu, :180-182 Delete/Backspace, :284-300 chip), no confirmation or undo anywhere on the route, and the 2-stop floor expressed as ABSENCE (v-if) plus a silent no-op with no status — PR-06 + the explicit-disabled-state law. BUILD → W-GRADSTOP-2 (one removal owner in the inspector + explicit disabled state); the contextmenu species' deletion rides the core wave beside C14's cure. MAJOR."
     },
     {
      "id": "D2-11",
      "verdict": "CONFIRMED",
      "note": "The protagonist is 40px tall — facts re-verified (rail h-10 at :203; render tile w-20 sm:w-24 at GradientVisualizer.vue:223) against PR-09's 19-22rem protagonist canon. Adopted with the challenger's own insight: the 40px stage is WHY the phantom targets, the exiled chip and the invisible bands exist. BUILD → W-GRADSTOP-2, whose stage/seat recomposition is the cure. MAJOR."
     },
     {
      "id": "D2-12 (≡ r1 D-13, UPGRADED)",
      "verdict": "CONFIRMED",
      "note": "The r1 latent-no-gate ruling is SUPERSEDED by reproduction: at rootFS 20px (an ordinary WCAG 1.4.4 setting) the handle grows to 25px while the JS inset stays 10 — the 0% handle protrudes +1.50px past the rail edge, the exact condition the :49-53 'end-handle truce' comment claims fixed (probe 2 block 5, before/after pasted). G12 is now a REAL born-RED gate (GRADSTOP-A §11); the row rides the one-custom-property axis cure. Identity preserved: D-13 = D2-12."
     },
     {
      "id": "D2-13",
      "verdict": "CONFIRMED",
      "note": "The only unnamed region on the route is the protagonist — re-verified in the visualizer template: h3 at :149 (Interpolation), :241 (Easing), :253 (CSS); the stop editor rendered bare at :136-145; bar role/name null. BUILD → W-GRADSTOP-2 (named region lands with D-7's group/name work). MINOR."
     },
     {
      "id": "D2-14",
      "verdict": "CONFIRMED",
      "note": "No visible value, ever — re-verified: the position exists only in the whole-percent aria-label (:234) and the CSS string far below; no numeric entry anywhere in the tree; a drag to 42% has no feedback. BUILD → W-GRADSTOP-2 (reserved live value + numeric twin, per the producer axis composition's own spec). MINOR."
     },
     {
      "id": "D2-15",
      "verdict": "CONFIRMED",
      "note": "Crowding law: arithmetic re-derived — (324−20)/9 = 33.8px centre gaps < the 44px coarse targets at 10 stops on mobile, so neighbours become mutually untappable. The COINCIDENT-POSITION arm is RESCOPED in place, not adopted as a model defect: equal positions are a hard stop — a legitimate CSS feature the parser correctly admits (non-decreasing) — so the defect is the EDITOR's missing disambiguation affordance, not the model permitting the value. Both arms → W-GRADSTOP-2 (the seat rail gives every stop a reachable seat regardless of rail position). MINOR."
     },
     {
      "id": "D2-16 (≡ r1 D-6)",
      "verdict": "CONFIRMED",
      "note": "WatercolorDot species violation re-confirmed (64 uses across 19 demo files, 0 in gradient; §7/§4.2/P051 name the site); the 40-line inline :style object arm folds into C9's data-only-inline cure. Fold → W-GRADSTOP-2 (the face is a CONSUME of the published @mkbabb/glass-ui/watercolor-dot subpath — buildable, not producer-gated)."
     },
     {
      "id": "D2-17",
      "verdict": "UNVERIFIABLE-HERE",
      "note": "The rendered forced-colors collapse is the challenger's own labelled hypothesis and cannot be witnessed in this environment: the shipped forced-colors matrix row runs WebKit, which ignores the emulation (the shot proves nothing — an environment fact, honestly recorded, not a defect confirmation), and no Chromium forced-colors cell exists. The SOURCE arm is confirmed: selection cue = border-alpha + scale only; :353-359 covers focus-visible only; no aria state. The aria-state arm folds into D-7 (W-GRADSTOP-2); the harness re-pin (ENGINE=chromium for the forced-colors row) belongs to the visual-audit band and is cross-referenced, not re-booked here."
     }
    ],
    "waveSpec": {
     "waveId": "MT-GRADSTOP-1 (r3-unified)",
     "title": "The model owns the ordinal AND the pairing, the rail owns one axis, no tap that grabs may delete, no button but the primary may mint, and no grab may teleport — GradientStopEditor's ordering/geometry/gesture core, unified r1+r3",
     "bornRed": true,
     "gates": [
      {
       "gate": "G1 — THE MODEL CANNOT SERIALIZE WHAT ITS PARSER REJECTS + AUTHORED EASINGS DO NOT MIGRATE",
       "command": "npx vite-node docs/tranches/V/megatranche/adjudication/worker-F/tmp/repro-order.ts AND npx vite-node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-order.mts (end-state: npx vitest run test/gradient-order-invariant.test.ts)",
       "redToday": true,
       "whatWouldMakeItFail": "stops [0,50,100], first stop → 70: serialized descending → REJECT 'stop positions must be non-decreasing' (re-confirmed by this seat's parse-probe run 2026-07-28); AND a cubic-bezier authored on pair a→c re-attaches to a→b after a 50% insert (index-keyed intervals)"
      },
      {
       "gate": "G2 — LIVE ROUND-TRIP: leftmost handle dragged past a neighbour, readout re-fed, verdict null",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p4.mjs (T10; end-state: new e2e -g 'neighbour-crossing drag round-trips')",
       "redToday": true,
       "whatWouldMakeItFail": "fresh #/gradient, add at 50%, drag first handle to ~80% — painted ramp descends and re-feeding the CSS yields the rejection string"
      },
      {
       "gate": "G3 — ONE AXIS: |handleCenterX(p) − rampX(p)| ≤ 1px for p ∈ {0,50,100}",
       "command": "node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-axis.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "default 2-stop model at 1440×900: handle-0% centre 235 vs ramp-0% 224 → 11px = 2.38% (3.40% at iPhone-14 W=324; the D2-02 pixel table −10.5..+10.0px is the full curve)"
      },
      {
       "gate": "G4 — THE MAPS ARE INVERSE: forward(inverse(p)) = p ± 0.05 at p ∈ {0,100}, read from style.left never aria-label",
       "command": "node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-axis.mjs (G1b arm)",
       "redToday": true,
       "whatWouldMakeItFail": "forward(inverse(0)) = 0.226 — a click at the 0%-handle's own painted pixel mints 0.2% (border-box forward vs padding-box inverse, C13's 304px vs 302px denominators)"
      },
      {
       "gate": "G5 — NO TAP THAT GRABS MAY DELETE (coarse cell): destructive and non-destructive hit regions disjoint",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p4.mjs (T11), playwright-webkit iPhone-14",
       "redToday": true,
       "whatWouldMakeItFail": "3 stops, select mid, tap handle-centre+20px → count 3→2 silently; the D2-05 measured overlap band 234.7..242.7 sits INSIDE the rail and is unpainted"
      },
      {
       "gate": "G6 — ONE GESTURE, ONE OWNER: one pointermove on a captured handle = exactly one position write",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p3.mjs (T9)",
       "redToday": true,
       "whatWouldMakeItFail": "one bubbling pointermove during a drag reaches bar 1 + handle 1 = two emissions/rebuilds per move (the :93 'fallback' comment is the always-path)"
      },
      {
       "gate": "G7 — THE CHIP RESERVES ITS OWN GROUND: chip's border box intersects no sibling rule",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p2.mjs (T2)",
       "redToday": true,
       "whatWouldMakeItFail": "select any stop on desktop: chip paints y 244.7→268.7 across the section hr at 260.7; elementFromPoint at the rule's y returns BUTTON.rail-remove-chip"
      },
      {
       "gate": "G8 — A GRAB IS NOT A TELEPORT: first emit after an off-centre grab moves the handle ≤ the dead-zone",
       "command": "node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-gesture.mjs (arm A)",
       "redToday": true,
       "whatWouldMakeItFail": "press handle-centre+8px then travel 1px → handle travelled 10.11px, label 0%→2% (this seat's own run 2026-07-28, pasted in the gate file; exit 1)"
      },
      {
       "gate": "G9 — POINTER SELECT FOCUSES: after a real mouse click on a handle, document.activeElement IS the handle and ArrowRight moves it",
       "command": "node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-gesture.mjs (arm B)",
       "redToday": true,
       "whatWouldMakeItFail": "today: activeElement = BODY and 2×ArrowRight has zero effect (e.preventDefault at :120 suppresses the focus default; the whole keyboard contract is dead after mouse select)"
      },
      {
       "gate": "G10 — ONLY THE PRIMARY BUTTON ADDS; CANCEL ONLY DISARMS",
       "command": "node docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-gesture.mjs (arm C)",
       "redToday": true,
       "whatWouldMakeItFail": "today: a right-click on the bare rail MINTS a stop, 2→3 (reproduced on demand by this seat — the C14 hypothesis upgraded); post-cure the same input and a pointercancel inside the dead-zone must mint nothing"
      },
      {
       "gate": "G11 — CONTRAST FLOOR: resting/selected handle boundary:fill ≥ 3:1 at every stop of the white-ramp input",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-D-probe4.mjs + the committed pixel-buffer contrast method",
       "redToday": true,
       "whatWouldMakeItFail": "linear-gradient(90deg, #ffffff 0%, #fafafa 100%) → ring:fill 1.00:1/1.03:1 — handles invisible (evidence/D-03-white-ramp-handles.png); 6/10 rainbow hues below 3:1"
      },
      {
       "gate": "G12 — TYPE-SCALE TRIPWIRE (D-13/D2-12 upgraded): at rootFS 20px the 0% handle stays inside the rail",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-D-probe2.mjs (block 5)",
       "redToday": true,
       "whatWouldMakeItFail": "document.documentElement.style.fontSize='20px' → handleW 25, overhang +1.50px past the rail's rounded end — the exact condition the :49-53 comment claims fixed"
      },
      {
       "gate": "G13 — TEST TRUTH: zero assertions in the two gradient spec files target retired DOM, and the amended files pass",
       "command": "npx playwright test e2e/smoke/views/gradient.spec.ts e2e/smoke/oracles/o21-gradient-rail.spec.ts --project=smoke --reporter=line",
       "redToday": true,
       "whatWouldMakeItFail": "today: o21:135/:163 assert gradient-ruler-cap/gradient-rung (0 matches in demo/+src/) and gradient.spec:57/:112 assert the retired Perceived-space plate; challenger's run 5 failed / 8 passed; and per GRADSTOP-A §13, HANDLE_HALF=0 keeping the suite green proves vacuity"
      }
     ],
     "scope": "Unified r1 §3 scope 1-6 PLUS the r3 arms, 4 demo files + 1 test + 1 e2e amendment + committed probes: (1) useGradientModel.ts — updateStop clamps into [prev,next] (model-owned ordering); addStop splices its interval at the insertion index (pairing); canRemove exposed (R2-A1); setStopsFromColors validates via the parseCssColor oracle (D-10 arm). (2) GradientStopEditor.vue — ONE axis (ramp painted on the handle track, one map over one box via clamp+scale from @mkbabb/value.js/math); bar's draggingId branch DELETED + stopPropagation on the handle; select emit DELETED; colorAt REQUIRED; chip geometry reserved; alpha-checker ground on its own layer; grabDx + dead-zone-gated first emit + cached rect (C11); e.preventDefault removed from pointerdown (C12); e.button!==0 guard at both pointerdowns + cancel path that only disarms + contextmenu removal species deleted (C14/D2-10); data-only inline style, geometry/state in the scoped sheet (C9); interim dual-contrast resting/selected ring generalized from :342-349 (D2-04); var() fallbacks and the :76 warp-to-0 guard deleted (C10). (3) GradientVisualizer.vue — @select dropped; unbound selectedStopId defineModel demoted to a plain ref. (4) GradientPane.vue — dead cssColorOpaque injection deleted. (5) test/gradient-order-invariant.test.ts NEW — both ordering arms. (6) e2e amendment — ADD neighbour-crossing drag + round-trip verdict + coarse tap-delete assertions AND re-anchor the four dead-DOM assertions (C4). Probes committed: gate-gesture.mjs (new, run RED today, output pasted in-file).",
     "structure": "L-8 over gates: clamp-in-the-sole-mutator makes non-monotonic stops unwritable; splice-at-insert makes easing migration unrepresentable by insertion; the button predicate + cancel-only-disarm make the phantom-mint class unrepresentable; grabDx makes the teleport unrepresentable (the emit cannot express an ungated absolute); deleting preventDefault makes focus follow the platform — no code left to get wrong; data-only inline style makes the hover-shadowing class unrepresentable; canRemove gives the 2-stop floor one home; deletions kill the drag double-path, the second selection channel, the masking fallback, the dead injection, and the contextmenu species outright. Gates persist only where no type can span two languages (CSS paint vs JS map: G3/G4/G12), two hit regions (G5), contrast pixels (G11), the round-trip algebra (G1/G2), gesture totality (G8-G10), and the e2e cell (G13).",
     "carries": "BUILD (this wave): L-1+D-2+C1+D2-03+Lr2·L-1 (G1/G2 incl. pairing) · D-1+L-3+C7+C13+D2-02 (G3/G4) · D-3+D2-05 geometric arm (G5) · D-4+D2-09 (G7) · L-9+D-11+C3 (G6) · D-12+L-10+C8+Lr2·L-5 (deletion) · L-11 (required prop) · L-12 (clamp/scale) · D-13+D2-12 (axis-cure rider, G12 now a REAL gate) · D-10's two cure arms · D-9's dead-inject line · C9, C10, C11 (G8), C12 (G9), C14 (G10), R2-A1, D2-04's interim arm (G11), C4's spec re-anchoring (G13). BUILD → W-GRADSTOP-2 (§7 recomposition: seat rail + inspector + WatercolorDot faces + full keyboard grammar): D-5+D2-04 constitutional home, D-6+D2-16, D-7+C6+C-7+D2-06 (+D2-17's aria arm), D-8+D2-07, D-9 inspector+D2-08, D-3's constitutional home, D2-10, D2-11, D2-13, D2-14, D2-15. BUILD → W-GRADSTOP-3 (one sampling law, one literal dialect, leaf types, branded OrderedStops): L-4, L-5+Lr2·L-4, L-7+Lr2·L-7, L-14 dialect arm, C2's stop-owned easing (supersedes the splice), R2-A3, and the clamp-vs-sort reorder question reopened there. BUILD → W-TOOLCHAIN: C4's CI wiring; W-TOOLCHAIN-ONE-ARTEFACT re-scoped per R1-C1 (stale-serve vintage only, no workspace link). FOLD by identity: L-6+Lr2·L-6 → W-HYGIENE demo/ui (ColorPicker L-6) · L-8 parity sub-arm+Lr2·L-8 → W-HYGIENE config parity (ColorSpaceSelector L-14; cure = deletion per R1-C1) · L-2+C5+Lr2·L-2+D2-01-root → parser band MT-F001 = π-R1 (pinned; cand-O totality; MT-F024 alias recorded). GLASS-ROUTED: L-13+Lr2·L-3, D-14 (relay + banks; a11y lands via D-7). RETIRE: nothing whole — the dead clauses (L-8(b) two-value.js; D2-01's dock arm; L-14 precision-at-HEAD; L-9/D-11 cost prose) die inside surviving rows with refutations recorded. BANKS (runnable, swept at every wave open): 1) π-R1/MT-F001 exit-1 probe (this seat re-confirmed THROWS today) — on exit 0, re-witness the code-editor arm; 2) glass rail-variant grep; 3) glass-8 version tripwire — all three verbatim in r1 §3.",
     "piObligations": "MATRIX — chromium 1440×900 (:9000, DOM-behaviour only) + playwright-webkit iPhone-14 (the coarse cell); safari-app is a SEPARATE cell (I-20), nothing transfers; the audit safari-* shot dirs are webkit captures cited as webkit only. ROUTE — #/gradient (hash URL, fully client-seeded; every gate data-free — no blank-data-surface trap; the dev server is API-LESS and that is an environment fact). SELECTORS — [data-testid=gradient-stop-bar], button[data-stop-id][aria-label^='Gradient stop at'], .rail-remove-chip[aria-label='Remove selected stop'], [aria-label='Gradient CSS']. STALE-SERVER LAW (r1 §0/§7, re-affirmed; R1-C1 does NOT touch it): literal/serializer claims run headless vite-node against the tree or a freshly restarted server; :9000 was proven serving a stale serializer 2026-07-27; scripts/dev/dev.sh stays untouched (unowned dirty row). COMMITTED CAPTURES (force-added past .gitignore *.png): witness/{before,after}-{cross-drag-rail-1440, axis-overlay-1440, touch-delete-iphone14, chip-hr-1440, grab-teleport-1440, white-ramp-contrast-1440, rootfs20-overhang-1440}.png — a witness not in the wave's own commit does not exist. NEW TELEMETRY LAW (GRADSTOP-A §10): the D2-01 class is invisible to pageError/console counts (watcher-scheduled debounce → boundary catches silently); route-health witnesses are DOM facts, never error channels.",
     "deltaObligations": "D1 round-trip: before = the REJECT string on G1/G2's own outputs / after = re-parse ok + verdict null, same commands. D2 axis: before 235 vs 224 (11px) / after ≤1px. D3 touch delete: before 3→2 / after 3→3. D4 dispatch: before 2 writes per move / after 1. D5 chip: before hr under chip / after no intersection. D6 pairing: before easing migrates on insert / after pair-stable. D7 grab: before 10.11px teleport / after ≤ dead-zone, same gate-gesture.mjs. D8 focus: before BODY + dead keys / after BUTTON + moving, same command. D9 buttons: before right-click mints 2→3 / after no mint, same command. D10 contrast: before 1.00:1 / after ≥3:1 on the same white-ramp input. D11 type-scale: before +1.50px overhang at rootFS 20 / after ≤0, same probe block. D12 test truth: before 4 dead-DOM assertions + 5 failing / after 0 + green, same playwright command. EXPLICITLY NOT CLAIMED: any drag-frame serialization cost delta (r1 Addendum §8 — measured 1× per move already; no close may claim a win it did not measure).",
     "completableAlone": "YES — if this were the only wave that ever executed: the model could no longer serialize CSS its own parser rejects, an authored easing could no longer silently migrate, a touch user could no longer destroy a stop by grabbing it, no mouse button but the primary could mint a stop, a cancelled gesture could mint nothing, a grab could no longer teleport a stop, a mouse user would reach the full keyboard contract, the handles could not be invisible on light ramps, the rail would tell one geometric truth its own doc-comment claims at every root font size, the component's behavioural gate would stop asserting DOM retired in July, and seven dual paths/dead surfaces would be deleted. Zero capability lost (the contextmenu removal retires with chip + Delete surviving), no new surface, no dependency on the parser band landing, any barrel deletion, any toolchain change, or any sibling repo. Four demo files + one test + one e2e amendment + committed probes = one session at the 38%-landing discipline (L-1)."
    },
    "addendumClause": "GRADSTOP-A §9-§13 (E-3: appended to the standing gradient authorities; r1 §1-§8 untouched and still binding). §9 RESOLUTION-TRUTH — the 'two value.js's in one build' mechanism (r1-corpus L-8(b)) is a measured false positive (this seat's traceResolution run: @mkbabb/value.js/css → the repo's own dist/subpaths/css.d.ts, no node_modules segment) and may not be cited by any wave or close; the tsconfig value.js paths block is cured by DELETION, not repair (exports self-resolution is both proof and mechanism — /css and /value already resolve with no entry); the r1 §0/§7 stale-server law is UNAFFECTED. §10 SHELL-SURVIVAL — 'route annihilation' means the pane surface inside <main> (both panes, all model state, silently); nav/dock survive (App.vue:47 vs :50) and 'destroys the whole application/dock' may not be cited; telemetry law: the throw rides a watcher-scheduled debounce so the boundary catches it with ZERO console/page errors — error-count probes are structurally blind to this class, the witness is the DOM. §11 LATENT-ROW UPGRADE — D-13/D2-12: the r1 'no gate of its own' discipline retires the moment a reproduction exists; the rootFS-20px reproduction (+1.50px overhang) makes G12 a real born-RED gate — the r1 principle (a gate authored green is vacuous) is exactly why the upgrade is legitimate, because this gate is authored RED. §12 GESTURE-TOTALITY — every pointer entry point states its button predicate and its cancel disposition; a cancel path routed through a commit path (today @pointercancel='onBarPointerUp' can MINT) is the C14 disease; cancel handlers may only disarm; reproduced witness gate-gesture.mjs G10 (right-click minted 2→3, this seat, 2026-07-28). §13 VACUOUS-SUITE — 'the suite is green' is not evidence for this component while the HANDLE_HALF=0 mutation (deleting the entire inset-track geometry) would keep it green; a gradient e2e close must state which assertion catches that mutation; today the suite is not even green — 4 assertions target DOM retired in a68ecdc1 and Playwright is wired to no CI job.",
    "dissent": "1) C11 severity — challenger: BLOCKER ('relocates the stop by the whole grab offset'); this seat: MAJOR (precision-destructive, data-recoverable; the BLOCKERs are the ordering family, the parser family, D-3's silent delete, and D2-04's invisibility class). Both grades carried; the challenger's preserved as the losing position. 2) D2-02/D2-03 BLOCKER grades — the fresh D seat re-asserts BLOCKER for the axis-skew substance the r1 arbiter graded MAJOR (D-1); nothing new decides it; the r1 ruling stands, the fresh grade joins the preserved dissent. 3) Clamp vs sort — C1 pass 1 verbatim: 'Clamping the drag to (prev, next) is cheaper and strictly worse — it makes stops un-reorderable.' RULED: clamp remains the core wave's interim law (the gates assert the INVARIANT — non-decreasing + round-trip — not the policy, so either cure passes them); the reorder-gesture question is REOPENED at W-GRADSTOP-3 where C2's stop-owned easing removes the hazard clamp exists to avoid. The challenger's 'strictly worse' stands as the strongest argument that W-GRADSTOP-3's shape, not the interim, is the final law. 4) C2's stop-owned easing vs r1's splice — structurally superior (deletes the parallel array, the watcher, three throws) vs smaller-and-completable-now; ruled splice-in-core / stop-owned-booked-at-W3 with the supersession recorded; the pairing gate is policy-neutral either way. 5) D2-01's dock arm — the challenger overclaimed ('dock unmounted'); refuted by the shell read; severity retained on the surviving substance; recorded because the overclaim is the L-9 class: a probe measuring bodyLen without reading the shell. 6) R2-A3 cure direction — challenger: move the paint recipe into glass; this seat: the four copies are ours to deduplicate NOW as a demo-owned class; waiting on a producer variant to fix our own duplication is a deferral wearing the boundary's name; the glass-variant arm rides the existing L-13 relay regardless. 7) C14's earlier negative — the challenger's clean-state right-click did NOT mint in their run while this seat's chromium run minted deterministically; the cells likely differ in pointerup delivery for button-2; both records kept, the reproduction (with its exact command) governs."
   }
  },
  {
   "agentId": "a9072a5bc1690ce86",
   "key": "v2:5c086cf9393e99934ad1bd33ddb41483c4507a2410114802dae2bc2129cb2af8",
   "result": {
    "modelObserved": "Opus 5 — exact model id `claude-opus-5[1m]` (1M-context seat). Declared at spawn as worker-O; receipt is mine by self-observation, not inherited (L-11).",
    "slug": "GradientStopEditor",
    "verdicts": [
     {
      "id": "C1 (challenge-C, BLOCKER — drag/shift-arrow inverts stop order)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-1 / D-2 (registry/adjudicated/GradientStopEditor.md §2) — books ONCE, no new row",
      "note": "Facts fully re-derived by me today at HEAD 7775473b. Source-certain: useGradientModel.ts:127-131 `updateStop` is a bare map with no ordering policy, :118 `addStop` sorts, gradientParse.ts:284-290 rejects `stops[j].position < stops[j-1].position` — three write paths, three policies, and the mutator the component drives on every pointermove (:145) and arrow (:179) is the one with none. Live (my own chromium run, WBGSE-O-r3-crossdrag-forcedcolors.mjs): add at 50%, drag the first handle right → labels [\"Gradient stop at 84%\",\"…50%\",\"…100%\"], CSS readout `linear-gradient(90deg, oklch(0.75 0.15 145) 83.8%, oklch(70% 0.165 205deg) 50%, oklch(0.65 0.18 265) 100%)`, computed `background-image` head descends 83.8% → 81.69% → 79.58%. Feeding that exact string back: `npx vite-node …/evidence/parse-probe.ts` → `reject: stop positions must be non-decreasing`. The accusation is true in every particular; its IDENTITY is r1's L-1/D-2 and it may not be re-booked under a C-number (L-5 anti-rename). Its delta contribution is the DRAG arm's live numbers (r1's live arm came from a keyboard reproduction) and the observation that equality is deliberately admitted by the parser (`<`, not `<=`), which is what makes the cure a SORT and not a separation law."
     },
     {
      "id": "C11 (challenge-C pass-2, BLOCKER — grab-offset teleport)",
      "verdict": "CONFIRMED",
      "note": "NEW identity, not in the r1 corpus, and it survives incredulity by measurement. Source: onHandlePointerDown (:119-134) records `x`,`y`,`wasSelected`,`moved` but NO grab offset; onHandlePointerMove (:136-146) emits `getPosition(e)` — the raw pointer position — unconditionally, and the 4px dead-zone the same function computes (:138-144) feeds only the re-tap-to-deselect decision at :151. My live run (WBGSE-O-r3-gestures.mjs, chromium 1440×900): press 8px right of the 0% handle's centre (inside its own 24px fine hit pseudo, :374-384), move the pointer exactly 1px → handle centre 235.00 → 245.11 (10.11px of travel for 1px of pointer travel), aria-label `Gradient stop at 0%` → `Gradient stop at 2%`. The challenger measured 10.12px; I measure 10.11px independently. The defect is a direct consequence of the hit-inflation the U-F27 ruling landed: the bigger the advertised grab zone, the bigger the teleport it invites. Cure is local and one-line-ish (record `grabDx` at pointerdown, cache the bar rect, gate the first emit on the dead-zone that already exists) and it also retires half of C3's per-move forced layout. BUILD in the core wave, gate G4."
     },
     {
      "id": "C2 (challenge-C, BLOCKER — adding a stop re-attaches every authored easing)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-2 (its insert-pairing arm, already adopted as the r1 core amendment 'G2c')",
      "note": "Structurally certain and re-verified at the line: `intervals` is an index-keyed parallel array (useGradientModel.ts:83) whose only synchroniser is `watch(() => stops.value.length)` (:89-100), which pushes/truncates at the TAIL, while `addStop` (:116-119) splices in SORTED position. Insert anywhere but the end and every downstream authored curve re-pairs; remove does the mirror. I did not re-run the easing-picker UI leg (the structural argument is total and the r1 arbiter already live-verified it), so I claim it as source-certain, not as my own live reproduction. Identity is r1's D-2; the delta contribution is the CURE: pass-2's 'hang the interval on the stop that opens it' is the same cure three independent seats (C-band, D2-band, and r1's O) converged on, and it is the PRECONDITION that makes a sort-on-move safe — which is why I rule it into the core wave rather than treating it as a second amendment (see Dissent §1)."
     },
     {
      "id": "C12 (challenge-C pass-2, MAJOR — pointer selection never focuses the handle)",
      "verdict": "CONFIRMED",
      "note": "NEW identity. Mechanism source-certain: `e.preventDefault()` at GradientStopEditor.vue:120 suppresses the compatibility mouse events, and focus is the default action of mousedown — so the button never becomes activeElement on a pointer path, while every line of onHandleKeydown (:173-187) and the docstring at :169-172 ('Escape clears selection while the handle keeps focus') assume it does. My live run (chromium): a real `mouse.click` on the 0% handle leaves `document.activeElement = BODY`, the selection nevertheless takes (computed transform mid-ladder, matrix scale 1.165 → the 1.25 selected rung), and two subsequent ArrowRight presses change nothing (`labels_after == labels_before`). Independent of the challenger's counters — I measured the consequence, not the event tally. Scope honestly: the keyboard grammar is reachable by Tab (I focused via `.focus()` and the arrows worked), so the defect is 'select with the mouse, then the keyboard is dead until you Tab away and back', not 'the keyboard never works'. The cure is a DELETION (`select-none touch-none` at :203 and pointer capture at :133 already do the work preventDefault was hired for), which makes it core-wave material: gate G5."
     },
     {
      "id": "C3 (challenge-C, MAJOR — every drag pointermove emits update:position twice)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-9 / D-11 (double dispatch), cost clause STRUCK per r1 Addendum §8",
      "note": "Re-derived structurally: pointer capture retargets the event to the handle but does not stop it bubbling, and there is no `stopPropagation` in onHandlePointerMove (:136-146), so the bar's `if (draggingId.value) { emit(...); return; }` branch (:91-96) fires on the same event. The comment at :93 calling it a 'fallback path' is false — it is the always-path. I did not re-run the counter probe: two prior seats measured 21/21 and 21/22 on 20-step drags and the source admits it. The identity is r1's L-9/D-11; the standing cost correction binds (no close may cite 'two 32-sample re-serialisations per frame' — measured 1× per move). Delta contribution: pass-2's `serialize-cost.ts` figure (0.223 ms per invalidation at 3 stops) is a per-invalidation cost, NOT a per-frame delta, and may not be quoted as one."
     },
     {
      "id": "C4 (challenge-C, MAJOR — the only behavioural gate is RED at HEAD, asserts dead DOM, and Playwright is not in CI)",
      "verdict": "CONFIRMED",
      "note": "NEW identity and the single most load-bearing row for wave design, so I ran all three arms myself. (1) `npx playwright test e2e/smoke/views/gradient.spec.ts e2e/smoke/oracles/o21-gradient-rail.spec.ts --project=smoke` → **5 failed / 8 passed (3.6m)**, byte-consistent with pass-1: o21:62 'terminal truth: each rail edge paints ITS OWN stop's color', o21:129 'ruler grammar: two terminal caps', views:40 'zero console errors', views:106 'selecting a stop pins the envelope plate', views:270 'easing row … steps mode lands in the literal' (expected `steps(4, end)`, received `steps(4, jump-end)`). Scope each honestly: views:40 fails on the API-less dev cell's own `value.js dev is MISCONFIGURED` console error (ENVIRONMENT, not a component defect — my probe captured the same 4 messages); views:270 is a stale expectation against a producer's literal; the two oracles fail against DOM that no longer exists — `grep -rn 'gradient-ruler-cap|gradient-rung' e2e/ demo/ src/` returns ONLY the two spec lines (135, 163). (2) `grep -rln playwright .github/workflows/` → no matches across ci.yml, deploy-pages.yml, release.yml. (3) The vitest side is emptier than claimed: `grep -rln 'useGradientModel' test/` returns exactly one file, `test/gradient-v4-consume.test.ts:10`, and it is a TYPE-only import — no test in the repo calls addStop/updateStop/removeStop. The mutation proof therefore holds by construction: delete `.sort()` from useGradientModel.ts:118 and every assertion in the repository stays green. The drag leg at views:157-175 is vacuous as charged: `mid.hover()` grabs the exact centre (so C11 can never fire), drags the MIDDLE handle of [0,50,100] to 75% (never crossing a neighbour), and asserts only `expect(pct).toBeGreaterThan(60)`."
     },
     {
      "id": "C5 (challenge-C, MAJOR — the parse path THROWS on the 8 empty-argument colour functions)",
      "verdict": "RESCOPED",
      "rescopedTo": "MT-F001, enlarged and superseded by MT-F024 (ROOT-FINDINGS.md:972) = π row R1; cure owned by the parser band (cand-O totality, registry/adjudicated/parser-band.md)",
      "note": "Re-run today, my own invocation: `npx vite-node …/evidence/parse-probe.ts` → all eight of oklch/rgb/hsl/lab/lch/oklab/hwb/color throw `TypeError: Cannot read properties of undefined (reading 'replace')`, and `parseGradientCSS('linear-gradient(90deg, oklch(), blue)')` throws the same. Root confirmed at the line: src/css/grammar.ts:181 `splitTopLevel(slash[0]!.replace(/,/g,\" \"), \"space\")` — a non-null assertion over `splitTopLevel(\"\", \"/\")` which returns []. Consumer contract confirmed: gradientParse.ts:16-17 names parseCssColor 'the validity oracle; the demo never hand-validates a color' and isColorToken (:92-94) calls it as a total predicate. The row is PINNED (epoch rule): it is not re-booked here, not renamed, and NO local guard may be proposed — the challenger's own cure text says the same ('a try/catch in onParseCSS would be a masking fallback'), and it is right. The gradient wave carries it as a runnable bank only."
     },
     {
      "id": "C6 (challenge-C, MAJOR — accessibility as decoration: no keyboard add, slider behaviour on button semantics, 20×20 targets)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identities D-7 (keyboard/state) + D-8 (add starvation) → W-GRADSTOP-2",
      "note": "Every arm re-measured by me on the live route: the handle's complete attribute set is `data-stop-id`, `type=button`, `aria-label=\"Gradient stop at N%\"` — no role, no aria-valuenow/min/max/valuetext, no aria-pressed|current|selected; the rail is `{role:null, tabindex:null, aria-label:null, tag:DIV, cursor:copy}`; removeStop (:158-162) restores no focus. The '20×20 in all 4 Safari matrices' arm is a WEBKIT cell and stays labelled as such (I-20) — I did not and may not infer safari-app from it. Identity is r1's D-7/D-8; the genuinely new arm is that there is no keyboard path to CREATE a stop at all (the only mint gesture is a pointer click on a role-less div), which folds into D2-13's naming cure and W-GRADSTOP-2's inspector."
     },
     {
      "id": "C13 (challenge-C pass-2, MINOR — one axis, two denominators: 304 vs 302)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-3 (forward map over the border box vs inverse map over the padding box)",
      "note": "Re-derived arithmetically and confirmed by my own DOM measurement. `.gradient-rail` carries `border: 1px` (:319); `getPosition` (:75-81) works off `getBoundingClientRect()` = the BORDER box, span = width − 20; `handleLeft` (:55-57) is `calc(10px + (100% − 20px)·p/100)` where `left`'s percentage resolves against the PADDING box = border box − 2px. At my measured desktop (barX 224, width 462): getPosition's track is 442px anchored at x=234, handleLeft's track is 440px anchored at x=235 — a 1px origin offset and a 2px length offset. The challenger's 324/322 → 304/302 pair is the same fact at the mobile width and I confirm railW=324 in my mobile cell. Books once, as r1's L-3."
     },
     {
      "id": "C7 (challenge-C, MINOR — handle track and ramp paint do not share an axis; the file's headline invariant is false)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-1 (ruled MAJOR at r1) — same identity as D2-02",
      "note": "My direct DOM measurement (not a PNG sample): barX=224, width=462, `background-origin: border-box`, `background-size: 100% 100%`, handle centres at x=235.0 and x=675.0. So the ramp paints its 0% at 224 and its 100% at 686 while the handles' 0%/100% sit at 235/675 — ±11.0px at both terminals, 0px at p=50 (455 vs 455), linear in between. The docblock at :8-13 ('handles, add-ghost and ramp share one axis by construction'), echoed at :211-213 and :262-265, is false under the geometric reading. Books once as D-1; my numbers supersede both the challenger's ±10.5/±10.0 PNG table and the r1 record's 11px estimate — they agree, and 11.0 is the citable figure."
     },
     {
      "id": "C8 (challenge-C pass-2, MINOR — dual selection write path + the defineModel stale-read hazard is ARMED)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identities L-10 / D-12 (dual channel + unbound public model)",
      "note": "Facts re-verified: :129-130 writes `selectedId.value` AND emits `select`; GradientVisualizer.vue:140 binds `v-model:selected-id` and :144 wires `@select` to the same ref; `grep -rn selectedStopId demo/` returns exactly three lines (51 declaration, 140 write, 144 write) and no read. Pass-2's contribution is a MECHANISM CORRECTION to pass-1 and it survives: because the parent DOES bind the model, Vue's `useModel` setter does not update the child's local value synchronously, so the hazard the repo's standing law #7 describes is armed — it is unexercised today only because `wasSelected` is captured at :123 before the write at :129. I record it as a checked negative (latent), exactly as the challenger did, and note that the core wave's deletion of the emit + demotion of the unbound model retires it structurally rather than by luck of ordering."
     },
     {
      "id": "C9 (challenge-C pass-2, MINOR — hover modelled as reactive JS because the inline transform shadows the hover: utility)",
      "verdict": "CONFIRMED",
      "note": "NEW identity (r1 booked the hand-rolled-mechanics species at L-13/D-14 but never this row). Source-certain and self-documenting: the file states the defect twice in its own comments (:30-33 'the inline transform shadows any hover: class utility, so hover must be modeled here'; :255-261 'the hover:scale-110 utility was DEAD'), and then re-enters the same trap for `transition` at :263. Live cost: `hoveredId` ref (:33) + `handleScale` (:60-63) + two listeners per handle (:269-270) + a per-instance style string per stop. This is the repo's own feedback_root_styling / no-per-instance-override law violated by the component that documents it. Disposition: BUILD → W-GRADSTOP-2 (the seat/style contract owns it; per-stop DATA becomes custom properties and the scale ladder becomes `:hover`/`[data-selected]` rules), NOT the core wave — it changes the visual/motion contract and belongs with the recomposition."
     },
     {
      "id": "C14 (challenge-C pass-2, MINOR — no e.button guard on the add gesture)",
      "verdict": "CONFIRMED",
      "note": "CONFIRMED AND STRENGTHENED — the challenger filed the observable arm as a non-reproducible HYPOTHESIS ('an earlier session where a right-press preceded it DID mint one; not reproducible on demand'); I reproduced it deterministically on the first attempt. My live run (chromium 1440×900, clean reload): stop count 2 → middle-click on the bare rail at 40% → **3** → right-click on the bare rail at 60% → **4**. Source: both pointerdown handlers (:85-89 bar, :119 handle) inspect `e.target` and coordinates and never `e.button`/`e.buttons`; `pendingAdd` is a bare latch disarmed only at :101-115 (pointerup/pointerleave) with no `contextmenu` or `pointercancel` arm. Cell discipline: this is the chromium-headless cell — on a real desktop a native context menu may consume the right-button pointerup, so the RIGHT-button arm is UNVERIFIABLE-HERE beyond headless; the MIDDLE-button arm needs no such caveat and is sufficient to make the gate RED. The juxtaposition is the finding: the same secondary button that MINTS a stop on the rail DESTROYS one on a handle (:164-167), neither guarded, neither undoable. Core wave, gate G6."
     },
     {
      "id": "C10 (challenge-C, INFO — masking fallbacks, a dead inject, a no-op prop default, a null guard that warps to 0%)",
      "verdict": "CONFIRMED",
      "note": "All four arms verified. (a) `var(--radius-pill, 9999px)` (:318) and `var(--touch-target, 2.75rem)` (:388) are dead fallbacks — my mobile cell measures the coarse pseudo at exactly 44px×44px, i.e. the token resolves, so the fallback can never paint (and duplicates the token's value anyway). (b) `GradientPane.vue:8` injects `CSS_COLOR_KEY` and the identifier occurs exactly once in the file (`grep -c` = 1) — a dead injection, the select→tune→commit grammar broken at the far end too. (c) `:6` destructures `colorAt = undefined` on an already-optional prop whose sole call site always passes it (GradientVisualizer.vue:139). (d) `:76` `if (!barRef.value) return 0` answers an unanswerable question with a destructive value (position 0). Each is a pure deletion; (b) and (c) are already in the r1 core scope, (a) and (d) join them. INFO grade stands."
     },
     {
      "id": "Lr2-1 (challenge-L r2, BLOCKER — 'the stop-ordering invariant is homeless')",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-1 (same number, same identity — the only r2 L-number that collides benignly)",
      "note": "Same row as C1/D2-03/r1 L-1, independently reproduced by this seat via the KEYBOARD path (10× shift+ArrowRight then 10× shift+ArrowLeft → labels [100%, 0%]). I verify the mechanism and note the r2 seat's cure is the one point of substantive disagreement in the whole delta: it says 'do NOT add .sort() to updateStop — that makes a fourth policy holder' and proposes transposing to a multi-thumb glass-ui/reka Slider (whose `getNextSortedValues` already owns the invariant). That cure is GLASS-OWNED (see Lr2-3) and cannot gate a completable wave. The ruling I take: ordering is owned by the COLLECTION in our model (one mutator that normalises), which is neither a fourth policy holder nor a producer dependency — it is the deletion of two of the three existing policies."
     },
     {
      "id": "Lr2-2 (challenge-L r2, BLOCKER — parseGradientCSS is documented total and is not)",
      "verdict": "RESCOPED",
      "rescopedTo": "MT-F001 → MT-F024 = π row R1 (parser band, cand-O totality) — pinned, immutable, never re-booked",
      "note": "Duplicate of C5 / r1 L-2 / D2-01's parse arm, with the sharpest framing of the four: the /css subpath publishes a `ParseResult`-typed API — a TYPE-LEVEL promise of totality — and breaks it at runtime, so the consumer's 'model-or-reject, never a partial' docblock (gradientParse.ts:5-6) rests on a guarantee never given. Re-run today: throws on `oklch()`, `rgb()` and inside `parseGradientCSS`. The seat's cure discipline is correct and binding (fix `parseFunctionalColor` to `failure(source)` on an empty body plus a property test over {fn}×{\"\",\" \",\"/\",\",,\"}; explicitly NO try/catch in isColorToken). Cross-cite only: the parser band already ruled cand-O the winner on exactly this totality class (0/172 failures vs the published parser's crash)."
     },
     {
      "id": "R1-C1 (challenge-L r2, MAJOR — CORRECTION: the prior seat's L-8(b) 'two value.js's in one build' is a FALSE POSITIVE)",
      "verdict": "CONFIRMED",
      "note": "The correction is RIGHT and I verified it independently, because it impeaches a pinned r1 ruling. `npx tsc --noEmit -p tsconfig.demo.json --traceResolution` resolves `@mkbabb/value.js/css` to `/Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts` with Package ID `@mkbabb/value.js/dist/subpaths/css.d.ts@4.0.0` — **no node_modules segment anywhere**, for every one of the demo's import sites. The `Package ID` string is TypeScript's package IDENTITY (name/path@version) for whichever package owns the file, and the repo's own package.json declares `@mkbabb/value.js@4.0.0` — reading it as a node_modules provenance marker is the misreading that produced L-8(b). I additionally confirmed the installed copy's provenance: `node_modules/@mkbabb/value.js@4.0.0` exists solely as a transitive dependency of `@mkbabb/keyframes.js@6.0.0` (its package.json:69 pins `\"@mkbabb/value.js\": \"4.0.0\"`), and nothing in the demo graph consults it. Note the sharper corollary I confirmed: `/css` and `/value` have NO `paths` entry at all and resolve correctly through package.json#exports — proof the paths block is UNNECESSARY, not incomplete. CONSEQUENCE (E-3, addendum not patch): the r1 apotheosis's L-8(b) clause and the phrase 'two byte-different builds in one app' must be WITHDRAWN, and the W-TOOLCHAIN-ONE-ARTEFACT cure re-derived — the honest cure is DELETING the tsconfig paths mappings, not workspace-linking. The r1 §0 'stale dev server' fact is a different claim and is untouched by this correction."
     },
     {
      "id": "Lr2-3 (challenge-L r2, MAJOR — wrong owner: a bespoke multi-thumb slider hand-rolled beside the published primitive)",
      "verdict": "GLASS-OWNED",
      "rescopedTo": "r1 identities L-13 + D-14 (GLASS-ROUTED: relay to the standing glass BH inbox fond + runnable bank)",
      "note": "Facts re-verified: `demo/ui/slider/index.ts` is one line (`export { Slider } from \"@mkbabb/glass-ui\";`), GradientVisualizer.vue:10 already imports it 126 lines above the rail that does not use it, glass-ui's slider types already declare `modelValue?: number[]` and `minStepsBetweenThumbs` (N-thumb by construction), and the component's own comments cite the producer vocabulary (:198 'the glass-ui slider-track rounding register (T-46)', :316 `--radius-pill`). The cure — a `variant=\"stops\"` with #thumb/#ghost slots — is a PRODUCER change: glass-ui is not ours, no local patch may be proposed, and a wave gated on a sibling repo shipping is not individually completable (L-1). Disposition unchanged from r1: relay + bank; the accessibility CONTRACT the composition would supply lands locally NOW via D-7/W-GRADSTOP-2 (a11y does not wait on a sibling repo). Its 'ordering becomes structurally unrepresentable' argument is real but may not be used to block the local cure — a wave that waits for it lands nothing."
     },
     {
      "id": "Lr2-4 (challenge-L r2, MAJOR — dead re-export barrels kept as back-compat shims)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-5 → W-GRADSTOP-3",
      "note": "Enumerated myself over demo/, test/, e2e/: `serializeRailRamp` (one hit, a doc-comment), `GradientParseResult`, `ParsedGradientModel` — ZERO real consumers through any door; `serializeGradient`, `serializeCoalescedGradient`, `linearInterval`, `parseGradientCSS` — imported from their real homes by the two test files; the ONLY live pass-through is `INTERPOLATION_SPACES`/`HUE_INTERPOLATION_METHODS` at GradientVisualizer.vue:19-20, which travel two stacked re-exports from demo/color-session/color-space-meta.ts while the sibling MixConfigBar.vue:18 imports the same two constants from the real home in one hop. The block's own header ('Re-exports (preserve public API surface)') names the shim it is. Books once as r1's L-5; one-line cure, zero behaviour delta."
     },
     {
      "id": "Lr2-5 (challenge-L r2, MAJOR — selection transported twice on one gesture)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identities L-10 / D-12 (= C8) — one identity, three seat numbers",
      "note": "Verified at the lines (:129-130 write-then-emit; GradientVisualizer.vue:140 + :144 consume both). The r2 seat's sharpening is correct and worth carrying into the cure text: the two channels have ALREADY diverged — removeStop (:158-162) and onHandlePointerUp (:148-156) write `selectedId` with no matching emit, so a parent listening on `@select` silently misses deselection. Pure deletion; already in the core wave scope."
     },
     {
      "id": "Lr2-6 (challenge-L r2, MINOR — demo/ui is a 19-module content-free alias barrel)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-6, already FOLDED to W-HYGIENE demo/ui extirpation (ColorPicker adjudication's L-6, 90-site identity)",
      "note": "Re-counted: 19 directories under demo/ui/, 18 of them a single one-line re-export (the exception is alert/index.ts at 11 lines). GradientVisualizer.vue reaches the design system through three doors in one file (:9 Select via shim, :10 Slider via shim, :12 writeClipboard from the package root, :13 DockControl from the /dock subpath). GradientStopEditor.vue itself imports NOTHING from demo/ui — its only imports are vue, @lucide/vue and one type — so the row is not this component's to build. Books at the demo-wide identity; the gradient tree's three callsites may ride any wave already touching those files."
     },
     {
      "id": "Lr2-7 (challenge-L r2, MINOR — direction of dependency inverted: leaf imports the state factory for a pure type)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-7 → W-GRADSTOP-3 (leaf model/types.ts is that wave's first landing)",
      "note": "Verified: GradientStopEditor.vue:4 imports `type { GradientStop }` from ../composables/useGradientModel, whose module (useGradientModel.ts:33-62 declares the types; :10-17 imports vue, @mkbabb/value.js/color, @mkbabb/glass-ui/easing, picker-color and three sibling composables; :73-193 builds the reactive state) is the orchestrator. Type-only and erased by verbatimModuleSyntax, so it is a declaration-graph disease rather than a runtime hazard — but it is what denies an ordered-stops type a home, which is why it precedes the L-5 deletion."
     },
     {
      "id": "Lr2-8 (challenge-L r2, MINOR — tsconfig.demo.json paths has drifted from package.json#exports)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-8(a) → W-HYGIENE config parity (ColorSpaceSelector L-14 identity); the CURE is corrected by R1-C1",
      "note": "Re-measured: `Object.keys(package.json#exports)` = ./color ./value ./css ./easing ./math ./transform ./quantize — SEVEN keys and no '.' root; tsconfig.demo.json:42 maps `@mkbabb/value.js` → ./dist/index.d.ts (`ls dist/index.d.ts` → No such file), :44 maps /parsing and :48 maps /units to subpaths that exist in neither the exports map nor dist/subpaths/ (which holds color, css, easing, math, quantize, transform, value). The block's own comment calls it 'a CLOSED 8-key set' — it is 7, and the two subpaths with no entry resolve correctly anyway. Three dead mappings; the honest cure is deletion of the whole @mkbabb/value.js paths block, per R1-C1, not repair."
     },
     {
      "id": "R2-A1 (challenge-L r2, MINOR — the 'minimum 2 stops' policy is implemented twice)",
      "verdict": "CONFIRMED",
      "note": "NEW identity, source-certain: GradientStopEditor.vue:66 `const removable = computed(() => stops.length > 2)` and useGradientModel.ts:122-123 `if (stops.value.length <= 2) return`. Two homes for one rule; they agree today. The real cost is not the duplication but its EXPRESSION: at the floor the component expresses unavailability by ABSENCE (`v-if` the chip away, :285) and the model expresses it by silence (an early return), so pressing Delete at two stops does nothing and says nothing — which is precisely D2-10's disabled-state arm. Disposition: FOLD with D2-10 into W-GRADSTOP-2 (the model exposes `canRemove(id)`; the inspector renders it as an explicit disabled state carrying its reason)."
     },
     {
      "id": "R2-A3 (challenge-L r2, INFO — the 'owned paint stack' is copy-pasted four times / six declarations)",
      "verdict": "RESCOPED",
      "rescopedTo": "the real duplication is TWO sites, not four/six — count corrected per L-9 and re-scoped to a W-GRADSTOP-3 style-contract rider",
      "note": "I read all four cited sites. GradientStopEditor.vue:317-326 (.gradient-rail) and GradientVisualizer.vue:265-278 (.gradient-render-tile) ARE the same recipe (background: <layer>, var(--alpha-checker) + origin/clip/repeat/size 100% 100%,16px 16px + --shadow-sm) — genuine copy-paste, and the second file's comment even says 'the rail's material contract, same shape'. The other two are NOT: GradientEasingEditor.vue:238-247 (.specimen-dot) is a different recipe (a 10px dot, `background-size: cover, 6px 6px`), and ComponentSliders.vue:191-199 is an inline shorthand for the alpha row's ramp. So 'six identical declarations, four sites' is wrong; the defect is one duplicated surface recipe across two files. It survives at INFO with the corrected count, and the correction matters because the proposed cure (push the recipe into a glass-ui variant) was sized off the inflated count."
     },
     {
      "id": "D2-01 (challenge-D r2, BLOCKER — eight typed characters annihilate the route)",
      "verdict": "CONFIRMED",
      "rescopedTo": "blast-radius arm CONFIRMED with the 'dock' clause struck; parse arm folds to MT-F024/π R1",
      "note": "Reproduced myself (WBGSE-O-r3-gestures.mjs, chromium 1440×900, clean reload): typing `linear-gradient(90deg, oklch() 0%, red 100%)` into the CSS field takes the census from {stops:2, bar:true, tile:true, paneCount:2, bodyLen:649} to {stops:0, bar:false, tile:false, **paneCount:0**, bodyLen:142} with ZERO page errors and zero new console errors. Two corrections to the accusation, both material. (1) The claim 'the dock is unmounted' is FALSE: `<nav>` survives with all 16 dock buttons and `<main>` still exists — what dies is everything inside the ErrorBoundary at App.vue:50-140, i.e. the ENTIRE two-pane grid, taking the unrelated My Palettes companion pane down with the gradient. That is still a BLOCKER and arguably a worse framing: one bad character in one workbench's text field destroys a sibling instrument's state. (2) The mechanism is now source-certain and explains the silence: the throw is raised inside the debounce timer but travels through `emit(\"parse\", text)` (GradientCodeEditor.vue:55-57), and Vue routes component-event handlers through callWithAsyncErrorHandling — so it lands in ErrorBoundary.vue's `onErrorCaptured`, which sets `caught` and returns false (halting propagation), which is exactly why `page.on('pageerror')` sees nothing and why the visual audit's 'pageErr 0 / consoleErr 0' row (audit/visual/REPORT.md:140) is telemetry-blind rather than wrong. Disposition: the PARSE arm folds to MT-F024 = π R1 (pinned; cand-O totality; no local try/catch — masking-fallback ban, and the challenger's own cure text agrees); the BLAST-RADIUS arm (boundary granularity: one boundary for two independent panes) is cross-cited to the App-shell identity, not re-booked here; the rail's missing `model-unavailable` face is a W-GRADSTOP-2 item. The wave carries it as a runnable bank + a delta obligation, never as a local guard."
     },
     {
      "id": "D2-02 (challenge-D r2, BLOCKER — ramp and handles ride two axes; pixel table)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-1 (= C7 = L-3's geometric twin); severity stays MAJOR per the r1 ruling, pixel table corrected",
      "note": "The fact is confirmed and I measured it more precisely than the challenger: their table (-10.5 … +10.0 px, sampled from a 924px-device PNG) is 0.5–1.0px optimistic; the DOM says ±11.0px exactly (handle centres 235.0/675.0 against a ramp painting 0% at 224 and 100% at 686), linear, zero at p=50, = 2.38% of a 462px rail and 3.4% at the 324px mobile rail I measured. The BLOCKER grade re-opens a severity the r1 arbiter already RULED (MAJOR, on the ground that nothing is destroyed or lost); re-grading on the same facts is re-litigation, so MAJOR stands and the challenger's BLOCKER is preserved in Dissent. Delta contribution kept: the committed element screenshot D-03-ten-hue-rings.png and the fill-vs-ramp colour sample at p=0 ((232,49,49) handle vs (235,85,36) ramp) make the skew visible rather than merely arithmetic."
     },
     {
      "id": "D2-03 (challenge-D r2, BLOCKER — the drag gesture has no ordering law)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity L-1 / D-2 (= C1 = Lr2-1)",
      "note": "Live-reproduced by me with numbers matching the challenger's to the digit: labels [84%, 50%, 100%], `left` values [calc(83.8% − 6.76px), calc(50% + 0px), calc(100% − 10px)], CSS readout descending, computed rail background-image descending. Its distinct contribution over C1 is the CURE FRAMING, which I adopt: ordering belongs to the collection, every mutation returns a normalised list with re-derived pairing, AND the editor preserves handle identity across a crossing so the grabbed handle stays grabbed — which is why sort (not clamp) is the correct cure once intervals are stop-owned. See Dissent §1."
     },
     {
      "id": "D2-04 (challenge-D r2, BLOCKER — stop swatch contrast reaches 1.00:1; 6 of 10 hues below 3:1)",
      "verdict": "CONFIRMED",
      "note": "NEW measured identity (r1's D-5 held the tautology/figure-ground argument; nobody had measured it). I recomputed WCAG contrast myself in-page (browser-resolved oklch → sRGB, ring = white composited at the authored 0.8 alpha over the fill, per :235 `border-2 border-white/80` and :247's fill): on a constant-lightness ten-hue ramp `oklch(0.7 0.2 h)` the ring:fill ratios are 2.38, 2.37, 2.25, 2.17, 2.09, 1.87, 1.92, 2.28, 2.43, 2.39 — **ten of ten below the 3:1 non-text floor**, not six; and on a #ffffff→#fafafa ramp the ring:fill ratio is exactly **1.00** (fill:ground 1.04), i.e. the control is literally invisible, which is what the committed D-03-white-ramp-handles.png shows. The class is confirmed and stronger than charged; the challenger's '6 of 10' is scoped to THEIR ramp and must be cited that way (L-9). The file already owns the cure recipe for focus only (:342-349's dual-contrast inner-dark/outer-light ring). Disposition: BUILD → W-GRADSTOP-2, born-RED gate: min ring:fill ≥ 3:1 over a ten-hue sweep (RED today at 1.87)."
     },
     {
      "id": "D2-05 (challenge-D r2, MAJOR — coarse handle and chip hit zones overlap; the chip wins)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-3 (CONFIRMED BLOCKER, coarse cell only)",
      "note": "Geometry re-derived and partly re-measured: my mobile cell (390×844, hasTouch) confirms `(pointer: coarse)` matches and the handle's ::before computes to exactly 44px×44px (:385-391), the chip carries the same inflation and sits at `top-11` with `z-20` (:288) — later in DOM and higher in stacking, so it wins the hit test in the overlap band. The r1 ruling stands: CONFIRMED at BLOCKER in the coarse cell, refuted at fine (where the pseudo is max(1.5rem,100%)=24px and there is no overlap), evidence cell playwright-webkit/iPhone-14 — never safari-app (I-20). D2-05's contribution is the arithmetic (handle zone 198.7–242.7 vs chip zone 234.7–278.7 → an 8px destructive band inside the rail) which makes the r1 gate G9's threshold derivable rather than hardcoded."
     },
     {
      "id": "D2-06 (challenge-D r2, MAJOR — a third of the keyboard grammar; no accessible state)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-7 → W-GRADSTOP-2",
      "note": "Re-measured by me with the handle genuinely focused (I used `.focus()` so the C12 confound could not mask the result): Home, End, ArrowUp, ArrowDown, Enter and Space each leave both aria-labels and the stop count unchanged; the handle's whole attribute set is data-stop-id/type/aria-label. Canon verified at the line — VISUAL-CONSTITUTION.md:127 is the gradient-stop-position binding row and :104 binds the domain-neutral axis composition to the producer Slider. Books as r1's D-7."
     },
     {
      "id": "D2-07 (challenge-D r2, MAJOR — the add gesture is hover-only, unnamed, keyboard-unreachable, and starves as the instrument fills)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-8 (starvation) + D-7 (naming/keyboard) → W-GRADSTOP-2",
      "note": "Both arms verified. Naming: my census gives the bar `{role:null, tabindex:null, aria-label:null, tag:DIV, cursor:copy}` — the mint gesture lives on an anonymous div and its only affordance is a hover ghost that a touch pointer cannot perceive. Starvation: the mechanism is the :87 guard (`target.closest(\"[data-stop-id]\")`) reading any hit inside a handle's inflated pseudo as a grab, so each stop sterilises 44px of a 324px mobile rail (13.6%) against the ADD gesture with no feedback in the masked band. The r1 narrowing stands: the masking is documented and rationale-bearing (:361-373) — the defect is the absence of any alternative add path plus zero feedback, not the guard."
     },
     {
      "id": "D2-08 (challenge-D r2, MAJOR — selection has no payload; nothing can edit a stop's colour)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-9 → W-GRADSTOP-2 (inspector); the dead injection line dies in the core wave",
      "note": "Re-verified end to end: `grep -rn selectedStopId demo/` = three lines, zero reads; GradientVisualizer's template has type/space/hue, easing and CSS regions and no stop inspector; GradientPane.vue:8 injects CSS_COLOR_KEY and never uses it (occurrence count 1). So the component named StopEditor edits position and existence only — select→tune→commit (VISUAL-CONSTITUTION.md:96) has no tune, at both ends of the wire. The challenger's either/or (give selection a payload, or delete selection) is the right shape; W-GRADSTOP-2 takes the first branch."
     },
     {
      "id": "D2-09 (challenge-D r2, MAJOR — the remove chip reserves no geometry and wins hit-tests on the next section's rule)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-4",
      "note": "Class arithmetic re-derived from the source: the chip is `absolute w-6 h-6 top-11 z-20` (:288) on a `relative` root whose only in-flow child is the 40px rail — it occupies y 44→68 of a 40px box, a 28px borrow that crosses the sibling `<hr>` (GradientVisualizer.vue:148). Both prior seats measured chip 244.7–268.7 against hr at 260.7 with `elementFromPoint` at the rule returning the chip. Canon verified: OPTICAL-BENCH-COMPOSITIONS.md:78 gives Gradient boundaries `[]` / reserve `none` while the route renders three `<hr>` rules. Books as r1's D-4 (gate retained)."
     },
     {
      "id": "D2-10 (challenge-D r2, MAJOR — three destructive removal species, no undo, and a silent no-op at the 2-stop floor)",
      "verdict": "CONFIRMED",
      "note": "NEW identity. Three species verified at the lines: contextmenu on a handle (:164-167), Delete/Backspace (:180-182), and the chip (:284-300). `grep -rn 'undo|confirm(' demo/workbenches/gradient/` returns nothing — no confirmation, no undo anywhere on the route. At the floor, `removable` (:66) merely v-ifs the chip away and removeStop (useGradientModel.ts:122-123) returns silently: pressing Delete at two stops does nothing and announces nothing, which VISUAL-CONSTITUTION.md:83 (explicit disabled states) forbids; PROPORTION-AUDIT.md:50 (PR-06) names the duplicated-action-species disease with 'one action/selection owner' as its cure — both citations verified verbatim at those lines. My own C14 measurement supplies the sharpest exhibit: the secondary mouse button MINTS a stop on the rail and DESTROYS one on a handle, undoably in neither direction. → W-GRADSTOP-2 (one removal owner in the inspector + an explicit disabled state carrying its reason), folding R2-A1."
     },
     {
      "id": "D2-11 (challenge-D r2, MAJOR — the protagonist is 40px tall)",
      "verdict": "RESCOPED",
      "rescopedTo": "PR-09 (PROPORTION-AUDIT.md:53, 'Gradient/Easing protagonist subordinated', Primary W27) + OPTICAL-BENCH-COMPOSITIONS.md:44 — a composition-band identity, cross-cited, NOT a GradientStopEditor row",
      "note": "Measurements confirmed (rail 462×40 desktop, 324×40 mobile; render tile `w-20 sm:w-24` at GradientVisualizer.vue:223) and both canon citations verified verbatim at the cited lines — PR-09 prescribes 'one 19–22rem protagonist; support subordinate', OPTICAL-BENCH:44 gives Gradient a P122 golden 61.8% meniscus/preview. But the row is not this component's to build: the height is set by the pane's composition and the 50/50 companion split is the shell's. Re-booking it under a D2-number would be the rename disease (L-5). It rides the design band's PR-09 identity; the gradient waves must not silently pre-empt it (and the r1 core cure is deliberately geometry-neutral so that a later enlargement does not invalidate it)."
     },
     {
      "id": "D2-12 (challenge-D r2, MINOR — HANDLE_HALF=10 duplicates a rem-derived size; the handle overhangs at 20px root font)",
      "verdict": "CONFIRMED",
      "rescopedTo": "r1 identity D-13, UPGRADED from LATENT (no gate) to REPRODUCIBLE (gains gate G7)",
      "note": "This is the delta's cleanest single win. r1 ruled D-13 CONFIRMED-as-LATENT with explicitly NO gate of its own because no reproduction existed ('a row with no reproduction carries no gate'). D2-12 supplies one and I reproduced it live: at the default 16px root the handle is 20px and its left edge sits 1.00px INSIDE the rail's border box; setting `document.documentElement.style.fontSize='20px'` — an ordinary WCAG 1.4.4 text-size condition — grows `w-5` to 25px while the JS inset stays 10px, and the 0% handle's left edge lands at 100.5 against a rail left edge of 102.0, i.e. **1.50px OUTSIDE** — the exact condition the file's 'end-handle truce' comment (:49-53) claims to have fixed. Four declarations of one geometric fact (:53 JS, :235 w-5/h-5, :288 top-11, :380-390 max(1.5rem,100%)/--touch-target) across two languages, with nothing keeping them in agreement. Core wave (the axis cure derives the half-handle from the rendered box or one custom property), gate G7."
     },
     {
      "id": "D2-13 (challenge-D r2, MINOR — the protagonist is the only unnamed region on the route)",
      "verdict": "CONFIRMED",
      "note": "NEW identity, verified with a correction that strengthens it. My live heading census inside main is [H3 'Gradient', H3 'Interpolation', H3 'Easing', H3 'CSS', H3 'My Palettes'] — so there IS a 'Gradient' h3, but it is the PANE title (PaneHeader.vue:21, rendered by GradientPane.vue), not a name for the instrument: the rail itself is `{role:null, tabindex:null, aria-label:null}` and is rendered bare at GradientVisualizer.vue:136-145 between the pane header and the first titled support section. Two defects fall out, not one: the protagonist has no accessible name or group, AND the pane's own title sits at the same h3 rung as its subsections, so the outline is flat. → W-GRADSTOP-2 (named region with a heading at the right rung, or all four lose their headings together per OPTICAL-BENCH:78's `[]`/`none` boundary law)."
     },
     {
      "id": "D2-14 (challenge-D r2, MINOR — no visible value during a drag, no numeric entry)",
      "verdict": "RESCOPED",
      "rescopedTo": "PR-07 'invisible drag state' (PROPORTION-AUDIT.md:51 — verified verbatim), already flagged open inside r1's D-9 → W-GRADSTOP-2 inspector",
      "note": "True and source-certain: the position appears only in the rounded aria-label (:234) and in the CSS block far below; there is no numeric field anywhere in GradientVisualizer's template. VISUAL-CONSTITUTION.md:104 (verified) specifies the axis composition as 'label, unit, reserved live value, optional numeric entry' — three of the four are absent. It is not a new identity: PR-07 already carries it and r1's D-9 already named PR-07 open at this site, so it folds rather than books. Its cure is the same inspector row that discharges D2-08, D2-10 and D2-06's numeric arm — one cure, four rows, which is the argument for building the inspector rather than four affordances."
     },
     {
      "id": "D2-15 (challenge-D r2, MINOR — crowding has no law; coincident stops become unreachable)",
      "verdict": "RESCOPED",
      "rescopedTo": "the measurement CONFIRMED, the PROPOSED CURE REFUTED — surviving arm is coarse-cell disambiguation → W-GRADSTOP-2",
      "note": "Measurements re-derived by me: a ten-stop model on the 462px desktop rail gives centre-to-centre gaps of 48.4px, and the mobile rail measures 324px → (324−2−20)/9 = 33.6px against a coarse hit pseudo I measured at exactly 44px — neighbours are mutually untappable on touch, confirmed. But the proposed cure ('a separation law owned by the collection: minimum ordinal spacing') is WRONG and must not enter a wave: CSS hard stops REQUIRE coincident positions (`red 50%, blue 50%`), and the demo's own parser deliberately admits equality — gradientParse.ts:284-290 rejects only `position < previous`, never `<=`. A minimum-separation law would make hard-stop gradients unauthorable, i.e. delete a capability to fix a hit-test problem. What survives is the disambiguation arm (fan-out / overflow treatment / a seat large enough to disambiguate, or selection-driven z-ordering), which belongs to the recomposition. Recorded because a cure that deletes a CSS capability is exactly the kind of accusation that becomes a defect if adopted unexamined."
     },
     {
      "id": "D2-16 (challenge-D r2, INFO — hand-rolled circle where the canon names a WatercolorDot face inside a seat)",
      "verdict": "RESCOPED",
      "rescopedTo": "r1 identity D-6 (a CONSUME of the published @mkbabb/glass-ui/watercolor-dot — BUILDs locally) → W-GRADSTOP-2",
      "note": "Verified: `grep -rn WatercolorDot demo/ | wc -l` → 64 across 19 files, zero in GradientStopEditor.vue; the handle is `linear-gradient(c, c)` over the alpha checker (:247); the canon says it plainly at VISUAL-CONSTITUTION.md:206 (verified verbatim: 'Each WatercolorDot stop is a face inside an enclosing geometric button/seat; that seat alone carries selection, focus, drag and accessible state') and OPTICAL-BENCH:108 names the site (P051). The r1 ownership check is what makes it buildable and it still holds: watercolor-dot is a PUBLISHED subpath, so this is a consume, not a producer change — which is exactly where it parts company with Lr2-3/L-13. The inline-40-line-:style arm folds into C9."
     },
     {
      "id": "D2-17 (challenge-D r2, INFO — in forced-colors the selection delta collapses; the matrix is blind because it runs WebKit)",
      "verdict": "REFUTED",
      "note": "REFUTED at BOTH arms, by measurement, and this is the one accusation I would not have believed without probing. (a) The harness arm: I launched playwright-webkit AND chromium with `forcedColors: \"active\"` — both report `matchMedia('(forced-colors: active)').matches === true`. The shipped matrix is NOT structurally blind to the mode, so states.mjs:14/:24 does not need re-pinning for this reason. (b) The mechanism arm is false at the source: `.rail-handle` is explicitly enrolled in the U-F57 tier-1 roster at demo/styles/foundation.css:695 (`forced-color-adjust: none`, the two-tier WHCM policy that keeps colour-display surfaces coloured), so border-color is NOT forced. Live confirmation in both engines under forcedColors:active: the handle's computed border stays `oklab(1 0 0 / 0.8)` and `forced-color-adjust` computes to `none`. The premise 'border-color is forced, so selection survives as a 5px diameter change' is therefore untrue in any engine that implements forced colors, and the row's own reproduction field already conceded 'NONE — hypothesis by construction'. What survives is not this row: selection carries no ARIA state, which is r1's D-5 (corrected clause: role/name/state explicit) and is already booked. Recorded as a REFUTATION with its evidence so it cannot re-enter under another number."
     }
    ],
    "waveSpec": {
     "waveId": "MT-GRADSTOP-1 (r3 delta-unified — same identity as the r1 core wave; extended, never renamed)",
     "title": "One collection owns the ordinal and the easing; one gesture owns the grab; one box owns the axis — GradientStopEditor's model + gesture core",
     "bornRed": true,
     "scope": "6 files, all ours, no producer dependency, no library fix required. (1) demo/workbenches/gradient/composables/useGradientModel.ts — the easing moves ONTO the stop that opens its interval (`GradientStop { id, cssColor, position, easing }`; the last stop's easing is ignored), which deletes the `intervals` parallel array, its length-keyed watcher (:89-100) and the three 'Gradient interval N is missing' throws (useGradientCSS.ts:189, :240, GradientVisualizer.vue:77); with pairing made identity-carrying, ONE mutator `setStopPosition(id, position)` normalises the collection (map, then sort by position, stable for equal positions so CSS hard stops survive) and `updateStop`/`addStop`'s private sort are deleted — three ordering policies become one. gradientParse.ts:284-290 survives as the foreign-input tripwire only. (2) GradientStopEditor.vue — `grabDx` recorded at pointerdown against a bar rect cached at pointerdown (kills the teleport AND the per-move forced layout), the first emit gated on the dead-zone the file already computes (:138-144); `e.preventDefault()` deleted from onHandlePointerDown (:120) so a pointer select focuses the handle (`select-none touch-none` at :203 and pointer capture at :133 already own text-selection and drag); `if (e.button !== 0) return;` at the top of both pointerdown handlers plus pendingAdd disarm on `pointercancel`/`contextmenu`; ONE axis — the ramp paints on the handle track (`background-position: var(--rail-inset) 0; background-size: calc(100% - 2*var(--rail-inset)) 100%`) and `getPosition`/`handleLeft` derive from ONE map over ONE box with the half-handle read from the rendered box (or one custom property both languages consume), using `clamp`+`scale` from @mkbabb/value.js/math (the dogfood the demo exists for); `onHandlePointerMove` gains `stopPropagation` and the bar's draggingId branch (:91-96) plus its false comment (:93) are DELETED; the `select` emit (:23, :130) is DELETED; `colorAt` becomes required and the `!barRef → 0` guard becomes an early throw; the remove chip's ground is reserved by the root and its coarse hit region is derived to be disjoint from the handle's. (3) GradientVisualizer.vue — drops `@select` (:144), demotes the unbound `selectedStopId` defineModel (:51) to a plain ref, follows the stop-owned easing through :64-88 and :118-125, and validates at `setStopsFromColors` (the model boundary, same oracle law as gradientParse). (4) GradientPane.vue — the dead `cssColorOpaque` injection (:8) is deleted. (5) test/gradient-order-invariant.test.ts NEW — the first behavioural test the model has ever had. (6) e2e/smoke/views/gradient.spec.ts — ADD (never replace) a neighbour-crossing drag with a round-trip assertion and a coarse-cell tap-at-centre+20px count assertion.",
     "structure": "Structure over gates wherever the defect class can be made unrepresentable (L-8). Easing hung on its opening stop makes migration-on-insert unrepresentable — there is no index to mis-key. One normalising mutator makes a non-monotonic model unrepresentable by mutation while KEEPING the drag-across-a-neighbour capability (equal positions stay legal, so hard stops survive). A grab offset recorded at pointerdown makes the teleport unrepresentable — the emitted position is a function of pointer displacement, not pointer absolute. Deletion does the rest: the drag double-path, the second selection channel, the unbound public model, the masking prop default, the dead injection and the `!barRef → 0` guard cease to exist. Gates persist only where no type can span two languages (CSS paint vs JS map — G3/G7), two hit regions (G9/G10), the pointer-button space (G6) or the round-trip algebra (G1/G2, the standing tripwire). NOT in this wave and deliberately so: the seat/inspector recomposition (W-GRADSTOP-2), the leaf types and re-export deletions (W-GRADSTOP-3), the parser totality (parser band, pinned), the glass rail variant (relay), PR-09's enlargement (design band).",
     "gates": [
      {
       "gate": "G1 — THE MODEL CANNOT SERIALIZE WHAT ITS OWN PARSER REJECTS, AND AN AUTHORED EASING NEVER MIGRATES",
       "command": "npx vite-node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/parse-probe.ts  (end state: npx vitest run test/gradient-order-invariant.test.ts)",
       "redToday": true,
       "whatWouldMakeItFail": "The string the drag itself produced — `linear-gradient(90deg, oklch(0.75 0.15 145) 83.8%, oklch(70% 0.165 205deg) 50%, oklch(0.65 0.18 265) 100%)` — fed back to the app's own parser returns `reject: stop positions must be non-decreasing (hard-stop reordering isn't modeled)`. My run today, verbatim. Second arm: with stops [0,50,100] and a `steps(4, jump-end)` authored on the 50→100 pair, inserting at 25% re-attaches that curve to 25→50 (index-keyed intervals, useGradientModel.ts:89-100 grows the TAIL while :118 inserts in sorted position)."
      },
      {
       "gate": "G2 — LIVE CROSSING ROUND-TRIP: the leftmost handle dragged past its neighbour, readout re-fed, verdict null",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-crossdrag-forcedcolors.mjs  (first block; end state: e2e -g \"neighbour-crossing drag round-trips\")",
       "redToday": true,
       "whatWouldMakeItFail": "Fresh #/gradient, click the rail at 50%, drag the first handle to ~84%: DOM/model order becomes [\"Gradient stop at 84%\",\"…50%\",\"…100%\"], the emitted CSS descends (83.8%, 50%, 100%), and the computed rail background-image descends 83.8 → 81.69 → 79.58. My own chromium run today."
      },
      {
       "gate": "G3 — ONE AXIS: |handleCentreX(p) − rampX(p)| ≤ 1px for p ∈ {0, 50, 100}",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-gestures.mjs  (block B geometry)",
       "redToday": true,
       "whatWouldMakeItFail": "Default 2-stop model at 1440×900: bar x=224 w=462 with background-origin border-box and size 100% 100%, handle centres at 235.0 and 675.0 → the ramp paints 0% at 224 and 100% at 686, so the error is +11.0px at p=0 and −11.0px at p=100 (2.38% of the rail; 3.4% at the 324px mobile rail), exactly 0 only at p=50."
      },
      {
       "gate": "G4 — A GRAB IS NOT A TELEPORT: pressing anywhere inside a handle's advertised hit region and moving 1px moves the stop by ≤ 1px",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-gestures.mjs  (block C11)",
       "redToday": true,
       "whatWouldMakeItFail": "Press 8px right of the 0% handle's centre (inside its own 24px fine hit pseudo) and move the pointer exactly 1px: the handle centre goes 235.00 → 245.11 — 10.11px of travel for 1px of input — and the accessible name goes `Gradient stop at 0%` → `Gradient stop at 2%`. My run today."
      },
      {
       "gate": "G5 — A POINTER SELECT LEAVES THE HANDLE FOCUSED: after a real mouse click on a handle, document.activeElement is that button and ArrowRight moves it",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-gestures.mjs  (block C12)",
       "redToday": true,
       "whatWouldMakeItFail": "A real mouse.click on the 0% handle: selection takes (computed transform climbs the scale ladder) but `document.activeElement` is `BODY`, and two subsequent ArrowRight presses leave both aria-labels unchanged. `e.preventDefault()` at :120 suppresses the compatibility mousedown whose default action is focus."
      },
      {
       "gate": "G6 — ONLY THE PRIMARY BUTTON MINTS: middle-click and right-click on the bare rail leave the stop count unchanged",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-gestures.mjs  (block C14)",
       "redToday": true,
       "whatWouldMakeItFail": "Clean 2-stop route: middle-click the bare rail at 40% → 3 stops; right-click the bare rail at 60% → 4 stops. (chromium-headless cell; the middle-button arm needs no caveat, the right-button arm may be swallowed by a native context menu on a real desktop.) Neither pointerdown handler inspects e.button (:85-89, :119) and pendingAdd is disarmed only on pointerup/pointerleave (:101-115)."
      },
      {
       "gate": "G7 — ONE HANDLE BOX AT ANY TYPE SCALE: at root font-size 20px the 0% handle's border box is contained by the rail's border box",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-gestures.mjs  (block D2-12)",
       "redToday": true,
       "whatWouldMakeItFail": "`document.documentElement.style.fontSize='20px'` (an ordinary WCAG 1.4.4 setting): the handle grows 20px → 25px because `w-5` is rem-derived while `HANDLE_HALF = 10` (:53) is a JS literal, and the 0% handle's left edge lands at 100.5 against a rail left edge of 102.0 — 1.50px OUTSIDE (it is 1.00px inside at the default 16px). Measured by me today; this is the reproduction r1's D-13 lacked."
      },
      {
       "gate": "G8 — ONE GESTURE, ONE OWNER: one pointermove on a captured handle produces exactly ONE position write",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p3.mjs  (T9)",
       "redToday": true,
       "whatWouldMakeItFail": "During a captured handle drag every bubbling pointermove runs BOTH the handle handler (:136-146) and the bar's draggingId branch (:91-96) — two emits and two eager `stops.map` rebuilds per event (prior seats measured 21/21 and 21/22 over 20-step drags). The comment at :93 calling it a 'fallback path' is false. NOTE (binding): no close may claim a serialisation-cost win here — measured at 1 ramp write per move (r1 Addendum §8)."
      },
      {
       "gate": "G9 — NO TAP THAT GRABS MAY DELETE: in the coarse cell, no point inside a handle's advertised hit region removes a stop",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p4.mjs  (T11, playwright-webkit iPhone-14)",
       "redToday": true,
       "whatWouldMakeItFail": "3 stops, select the middle one, tap at its centre +20px (still inside its own 44px coarse pseudo, which I measured at exactly 44×44 on a 390×844 touch context): the stop count goes 3 → 2, silently, with no undo. The chip's 44px zone (top-11, z-20, later in DOM) overlaps the handle's by 8px and wins the hit test; the destructive band 14–22px is UNPAINTED."
      },
      {
       "gate": "G10 — THE CHIP RESERVES ITS OWN GROUND: the chip's border box intersects no sibling rule and the root's layout box contains its footprint",
       "command": "node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p2.mjs  (T2)",
       "redToday": true,
       "whatWouldMakeItFail": "Select any stop at 1440×900: the chip paints y 244.7→268.7 on a root whose in-flow content ends at 240.7, crossing the next section's hr at 260.7, and elementFromPoint at the rule's y returns BUTTON.rail-remove-chip. A 28px borrow from a 40px box."
      },
      {
       "gate": "G11 — THE MODEL IS COVERED AT ALL: the ordering and pairing invariants have an executable owner",
       "command": "npx vitest run test/gradient-order-invariant.test.ts  (RED: no such file) and, as the vacuity proof, `grep -rln useGradientModel test/` → one file, type-import only",
       "redToday": true,
       "whatWouldMakeItFail": "Today the only reference to useGradientModel in test/ is a TYPE import at test/gradient-v4-consume.test.ts:10 — no test calls addStop/updateStop/removeStop, so deleting `.sort()` from useGradientModel.ts:118 leaves every assertion in the repository green. The e2e side is no better: 5 of 13 gradient specs FAIL at HEAD (my run: o21:62, o21:129, views:40, views:106, views:270 — 3.6m), Playwright appears in none of the three GitHub workflows, the two oracles assert test-ids (`gradient-ruler-cap`, `gradient-rung`) that exist nowhere in demo/ or src/, and the one drag assertion hovers the exact handle centre (so G4 can never fire) and checks only `pct > 60`."
      }
     ],
     "piObligations": "MATRIX — chromium 1440×900 against the dev server at :9000 (all of G1–G8, G11) plus playwright-webkit iPhone-14 for the coarse cell (G9) and a 390×844 touch context for the crowding/pseudo measurements. safari-app is a SEPARATE cell (I-20): nothing in this wave is claimed for it, and the audit's `safari-*` shot directories are webkit captures cited as webkit only. ROUTE — `#/gradient` (hash URL; fully client-seeded, so every gate is data-free and no gate can be faked green by the API-less dev cell — the blank-data-surface trap does not apply here). SELECTORS — `[data-testid=gradient-stop-bar]`, `button[data-stop-id][aria-label^=\"Gradient stop at\"]`, `.rail-remove-chip[aria-label=\"Remove selected stop\"]`, `[contenteditable=true]` for the CSS field. PROBES BANKED (durable, this seat's, committed with the wave): audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-gestures.mjs, WBGSE-O-r3-contrast-crowding.mjs, WBGSE-O-r3-crossdrag-forcedcolors.mjs. COMMITTED CAPTURES (force-added past .gitignore's `*.png`, or the claim is not made): witness/{before,after}-{cross-drag-rail-1440, axis-overlay-1440, grab-offset-1440, touch-delete-iphone14, chip-hr-1440, typescale-20px-1440}.png. STALE-SERVER LAW (r1 Addendum §7, still binding): any literal/serializer claim runs headless vite-node against the tree, never against a dev server of unknown boot vintage; `scripts/dev/dev.sh` is an unowned dirty row and stays untouched.",
     "deltaObligations": "(D1) round-trip — before: the REJECT string on G1/G2's own outputs; after: re-parse ok and the verdict null, same commands. (D2) axis — before: 11.0px at both terminals; after: ≤1px at p ∈ {0,50,100}, same block. (D3) grab — before: 10.11px of handle travel for 1px of pointer travel; after: ≤1px, same block. (D4) focus — before: activeElement BODY after a real click, arrows dead; after: the handle is activeElement and ArrowRight moves it. (D5) buttons — before: 2 → 3 → 4 on middle/right click; after: 2 → 2 → 2. (D6) type scale — before: +1.50px overhang at rootFS 20px; after: contained at 16px and 20px. (D7) dispatch — before: 2 writes per move; after: 1. (D8) touch delete — before: 3 → 2; after: 3 → 3. (D9) chip — before: hr under the chip; after: no intersection. (D10) pairing — before: an authored steps() migrates on insert; after: pair-stable. EXPLICITLY NOT CLAIMED: any drag-frame serialisation-cost win (measured 1× per move already); any safari-app property; any bundle or performance property (this wave's witness is a dev server and is structurally blind to both — L-12).",
     "carries": "BUILD (this wave, ids for life): L-1 + D-2 (= C1, C2, Lr2-1, D2-03) with the cure RE-RULED to normalise-on-write plus stop-owned easing (see Dissent §1); D-1 + L-3 (= C7, C13, D2-02) at MAJOR; C11 (new); C12 (new); C14 (new); D2-12 (= D-13, upgraded LATENT → gated); D-3's geometric arm (= D2-05); D-4 (= D2-09); L-9 + D-11 (= C3) with the cost clause struck; L-10 + D-12 (= C8, Lr2-5) by deletion; L-11 (required prop); L-12 (clamp/scale from /math); C10's four deletions; D-9's dead-injection line (= D2-08's far end); D-10's two cure arms (checker on its own layer; validation at setStopsFromColors — the end-to-end witness stays UNVERIFIABLE-HERE until an API-backed cell exists); C4 + G11 (the model's first behavioural test + the e2e amendment; wiring Playwright into ci.yml rides W-HYGIENE, not this wave, so that this wave stays completable alone).  BUILD → W-GRADSTOP-2 'the §7 recomposition: seat rail + selected-stop inspector + WatercolorDot faces + full keyboard grammar' (D-5, D-6 = D2-16, D-7 = C6/D2-06, D-8 = D2-07, D-9's inspector = D2-08, D-3's constitutional home, plus the delta's new rows D2-04 with its own born-RED contrast gate (min ring:fill ≥ 3:1 over a ten-hue sweep; RED today at 1.87, and 1.00 on a white ramp), D2-10 + R2-A1 (one removal owner + explicit disabled state), D2-13 (named region), D2-14 (PR-07 numeric/live value), D2-15's surviving disambiguation arm, C9 (hover into the sheet), D2-01's `model-unavailable` face).  BUILD → W-GRADSTOP-3 'one sampling law, one literal dialect, leaf types' (L-4, L-5 = Lr2-4, L-7 = Lr2-7, L-14's dialect arm, R2-A3's corrected two-site paint-stack rider).  FOLD by identity: L-2 = C5 = Lr2-2 = D2-01's parse arm → MT-F001, enlarged as MT-F024 = π row R1 (pinned, immutable, cure owned by the parser band's cand-O totality; no local try/catch — masking-fallback ban). L-6 = Lr2-6 → W-HYGIENE demo/ui extirpation (ColorPicker's L-6 identity). D2-11 → PR-09 (design band, Primary W27). D2-01's blast-radius arm → the App-shell boundary-granularity identity (one ErrorBoundary for two independent panes), cross-cited not re-booked.  RESCOPED-BOOK: L-8 = Lr2-8 → W-HYGIENE config parity, with its cure CORRECTED by R1-C1 (delete the tsconfig paths block; do NOT workspace-link) and its L-8(b) clause WITHDRAWN.  GLASS-ROUTED: L-13 + D-14 = Lr2-3 → the standing glass BH inbox fond + banks; the spectrum blur(7px) prefix-trap seam stays glass-owned and is not re-booked anywhere in this wave.  RETIRE: D2-17, refuted at both arms with the evidence recorded so it cannot re-enter under another number; D2-15's separation-law cure, refuted as a capability deletion.  BANKS (runnable, swept at every wave open): (1) `node --input-type=module -e \"const {parseCssColor}=await import('./dist/subpaths/css.js'); try{parseCssColor('oklch()');process.exit(0)}catch{process.exit(1)}\"` — exits 1 today; when it exits 0, re-witness the code-editor arm (typing `linear-gradient(90deg, oklch(), blue)` must yield a verdict, not an unmounted pane) and gradientParse.ts:16-17's oracle claim becomes true as written. (2) `node -e \"const s=require('fs').readFileSync(require.resolve('@mkbabb/glass-ui/package.json').replace('package.json','dist/slider.d.ts'),'utf8'); process.exit(/variant.*rail|variant.*stops|SliderThumb.*slot/.test(s)?0:1)\"` — exit 0 fires the L-13/D-14 consume. (3) `node -e \"process.exit(require('./node_modules/@mkbabb/glass-ui/package.json').version.startsWith('8')?1:0)\"` — exits 1 when glass 8 lands.",
     "completableAlone": "YES. If this were the only wave that ever executed: the model could no longer serialize CSS its own parser rejects; an authored easing could no longer silently migrate onto a different pair of stops; a grab could no longer teleport a stop by the width of its own hit target; a mouse user would keep the keyboard grammar the file documents; a stray middle- or right-click could no longer mint a stop; the rail would tell the one geometric truth its own docblock claims, at any text-size setting; a touch user could no longer destroy a stop by grabbing it; and six dual paths, dead surfaces and masking defaults would cease to exist — with the model's first behavioural test standing behind all of it. Zero capability is lost (hard stops and drag-reordering both survive by construction), no new surface is added, and there is no dependency on the library fix, on glass-ui, on the barrel deletions, on the toolchain, or on the composition band. Six files, one new test, one e2e amendment: one session at the 38%-landing discipline this formation was sized against."
    },
    "addendumClause": "GRADSTOP-A2 (E-3: appended to the standing gradient authorities and to the r1 apotheosis, never rewriting either). §1 ORDERING-CURE CLAUSE — the ordering invariant is discharged by NORMALISE-ON-WRITE plus STOP-OWNED EASING, not by clamping the mutator: clamping a position into (prev, next) makes a drag-across-a-neighbour silently impossible, which converts a data defect into a capability deletion with no feedback (the D2-07 disease), while equality must remain legal because CSS hard stops require coincident positions and gradientParse.ts:284-290 deliberately admits them (`<`, never `<=`). Sorting is only safe once the interval hangs on the stop that opens it — so C2/D2-03's pairing cure is a PRECONDITION of the ordering cure, not an amendment to it, and no gradient close may land one without the other. §2 SEPARATION-LAW BAN — no wave may adopt a minimum-ordinal-spacing rule for stops (D2-15's proposed cure): it would make hard-stop gradients unauthorable. Crowding is cured by disambiguation (seat, fan-out, overflow), never by forbidding coincidence. §3 FORCED-COLORS CLAUSE — `.rail-handle` is enrolled in the U-F57 tier-1 roster at demo/styles/foundation.css:695 (`forced-color-adjust: none`), so no gradient row may assert that the handle's colour-coded state 'collapses in forced colors'; and playwright-webkit DOES honour `forcedColors: active` (measured, both engines), so the forced-colors matrix row may not be dismissed as engine-blind. §4 RESOLUTION-PROVENANCE CLAUSE (withdrawal) — the r1 apotheosis's L-8(b) claim that 'the demo type-resolves against the installed node_modules package while it runtime-resolves against the working tree — two byte-different builds in one app' is WITHDRAWN as a false positive: `tsc --traceResolution` resolves every `@mkbabb/value.js/*` specifier to the repo's own `dist/` with no node_modules segment, and TypeScript's `Package ID` is package identity, not provenance. No close may cite the two-builds phrase; the W-TOOLCHAIN cure is re-derived as deleting the tsconfig paths block. The separate 'stale dev server' fact (r1 §0) is untouched and still binds. §5 COUNT-SCOPING CLAUSE — three delta counts were measured wrong and may only be cited as corrected: the axis skew is ±11.0px measured from the DOM (not ±10.5 from a PNG); the swatch ring:fill contrast is ramp-specific (on a constant-lightness ten-hue ramp I measure 10 of 10 below 3:1 at 1.87–2.43, and exactly 1.00 on a white ramp — 'six of ten' belongs to its own ramp); the duplicated paint stack is TWO sites (.gradient-rail, .gradient-render-tile), not four sites/six declarations. §6 BLAST-RADIUS CLAUSE — a throw in one workbench's parse path unmounts BOTH panes because a single ErrorBoundary (App.vue:50-140) wraps the whole two-pane grid; the nav/dock survives. Any claim of 'the whole application dies' overstates it, and any claim that the route is healthy because pageErr/consoleErr are 0 (audit/visual/REPORT.md:140) is telemetry-blind — Vue's onErrorCaptured swallows the throw before it can reach either channel.",
    "dissent": "§1 SORT vs CLAMP (against the r1 arbiter's ruling, the one substantive disagreement in this delta). The r1 apotheosis ruled the cure as 'updateStop position writes clamp into [prev.position, next.position]'. I rule against it on new evidence: three independent r3 seats (C-band pass-1 and pass-2, D2-band) converged on SORT with preserved handle identity, and the reason is a capability, not a taste — under clamp a user dragging the leftmost handle to the right simply stops at the neighbour, silently, which is the same species of unexplained refusal as the add-gesture starvation the same apotheosis books as a defect (D-8). Clamp was chosen at r1 because index-keyed intervals make re-sorting unsafe; once the easing hangs on its opening stop (C2's cure, which r1 already adopted as an amendment), that objection dissolves and sort becomes the strictly better cure. The r2 L-band seat's alternative ('do not add .sort() — transpose to the producer slider') is refused on L-1 grounds: a wave gated on glass-ui shipping a variant is not individually completable. §2 D2-02 SEVERITY. The D2 seat grades the axis skew BLOCKER; the r1 arbiter ruled MAJOR and I decline to re-open it on identical facts — but I record that my measurement made it worse than the record says (±11.0px, not ±10.5) and that the challenger's grade is preserved here as the losing position, not as a settled one. §3 C14 CELL LIMIT. My deterministic reproduction of the button-guard defect is chromium-headless; the right-button arm may behave differently on a real desktop where a native context menu can consume the pointerup, and I did not test macOS Safari (I-20 forbids inferring it). The middle-button arm carries the gate on its own. §4 D2-01 CURE TENSION. The challenger's cure ('make applyCSS result-typed and throw-free') is one edit away from a masking fallback: if it lands as a try/catch around a throwing library it violates the standing ban and leaves every other parseCssColor caller exposed. I route the parse arm to the parser band unchanged and let the wave carry only a bank — which means the route stays annihilable by eight characters until that band lands. I record that as an accepted, named risk rather than pretending the gradient wave can cure it. §5 WHAT I COULD NOT SETTLE. The end-to-end setStopsFromColors write path (r1 D-10) still needs an API-backed cell; this dev server is API-less and a blank data surface is an environment fact, not a confirmation. §6 METHOD NOTE, recorded because it nearly cost a refutation: my first crossing-drag run moved the handle only to 36.8% — short of the neighbour — and produced a perfectly ordered model and a null verdict. Had I stopped there I would have reported D2-03 as REFUTED. A gate that does not attempt the crossing passes with the defect fully present, which is precisely the vacuity the existing e2e drag leg embodies."
   }
  },
  {
   "agentId": "a2e6a5c94ac5ac9e6",
   "key": "v2:0fe34c161fd6df8e8016b487932644c6c72d41b8733bfc6f8ba9dd6318e9723d",
   "result": {
    "modelObserved": "claude-fable-5 (Fable 5) — observed in this seat's own runtime, not inherited from the spawn label (L-11)",
    "slug": "dock-r3-delta-adjudication-merged",
    "verdicts": [
     {
      "id": "L-35",
      "verdict": "CONFIRMED",
      "note": "BLOCKER stands, fully re-derived statically: ColorPicker.vue:271 editTarget is a private ref whose writers are reachable only via defineExpose (:331-343); usePaletteWiring:37-55 whenColorPickerReady polls the DESKTOP-ONLY colorPickerRef 40x50ms then console.warns and stops; App.vue:83-91 mobile PaneSlot passes no :on-mount (my read) so the ref is structurally null on mobile forever; Dock.vue:72/:135-145 mobile-edit layer is therefore unreachable in the only grammar it exists for. Cure BUILD here: the DockCommand provide/inject registry + moving the editTarget write onto EDIT_TARGET_KEY (provided at App.vue:270 TODAY, my grep). Cross-wave ruling R-8: MT-APP-1's bindPane narrows to non-command instance uses."
     },
     {
      "id": "L-36",
      "verdict": "CONFIRMED",
      "note": "18 root-barrel @mkbabb/glass-ui imports outside demo/ui, all ./dom symbols (my grep, count matches the seat); ./dom exists in the dist exports map (my census). Mechanical rewrite lands in the G-Q hygiene commit; the two dock sites are ColorInput.vue:117 and SlugEditLayer.vue:6 (my reads)."
     },
     {
      "id": "L-37",
      "verdict": "RESCOPED",
      "rescopedTo": "W-HYGIENE",
      "note": "Verified: test/picker-blob-config.test.ts reads three demo SFCs via readFileSync + process.cwd() from the LIBRARY vitest root (my read of :1-20; vitest include spans test/ + demo/test/) and asserts regex morphology — the owner-retired grep-invariant idiom (2026-06-02 feedback) resurrected in the wrong test home. True, but repo-hygiene not dock behaviour; flagged to the owner-feedback ledger in the fold."
     },
     {
      "id": "L-38",
      "verdict": "RESCOPED",
      "rescopedTo": "W47 (control death, CARRY-LEDGER:22) + BH density-token ask + W-HYGIENE (DESIGN.md:381 doc line)",
      "note": "Facts verified: PaneSegmentedControl.vue:46-51 pierces the producer-internal .segmented-tab via :deep under a root-variant comment (my read); my :deep census ≈15 across demo SFCs. The control is ALIVE-for-the-W47-gate by owner design — this wave may not kill it (R-9); the density variant belongs in glass SegmentedTabs (BH fond); the false DESIGN.md census line is a W-HYGIENE doc fix."
     },
     {
      "id": "L-39",
      "verdict": "CONFIRMED",
      "note": "grep TopDock demo/ → exactly ColorPicker.vue:311, a load-bearing contract comment naming a component that no longer exists (my run). Rewritten in the registry commit to state the NEW contract (panes provide commands; the dock injects) — G-Q arm."
     },
     {
      "id": "L-1 (replicated blind)",
      "verdict": "CONFIRMED",
      "note": "BLOCKER replication of the command-seam identity, independently measured by the seat before reading prior reports (4-vs-3 dock faces, slot class, tabindex -1 — labeled) and re-derived statically by me (App.vue:38 ?? null hop; desktop-only mount callbacks; usePaneRouter paneRefs.<view>.value?.<cmd>?.() handlers, my reads). One identity with L-35/D2-01/D2-02; ONE wave owns the registry cure (R-8)."
     },
     {
      "id": "L-23 (replicated blind)",
      "verdict": "RESCOPED",
      "rescopedTo": "W-HYGIENE (config-parity family, the L-22 class) with a G-G rider BUILT here",
      "note": "BLOCKER-true, re-derived: eslint.config.js:235-238/:275-276 scope every G-DEMO boundary to demo/@/... and `ls demo/@` → No such file or directory (my runs) — enforcement that enforces nothing. The repo-wide re-point goes to W-HYGIENE; the ONE boundary this wave creates (shell must not import palettes) lands HERE as eslint structure beside G-G with a ≥1-file-match meta-check."
     },
     {
      "id": "L-6 (replicated blind)",
      "verdict": "CONFIRMED",
      "note": "Replication accretion only: the five shell value-import sites of SESSION_PORT_KEY match my r2 grep exactly; the r2 L-6 ruling (G-G, R-6 cure detail) is unchanged — this is its third/fourth independent confirmation. No new booking."
     },
     {
      "id": "L-30",
      "verdict": "CONFIRMED",
      "note": "THE BLOCKER of the band, all three swallows re-derived to certainty: (1) SlugEditLayer:54 un-awaited async inside try (useSlugMigration:51 is async) — the catch :57 is unreachable and :55-56 close the layer synchronously; (2) the callee catches into slugBarRef.value?.setError on a NEVER-template-bound ref whose component PaletteSlugBar is rendered by NOTHING (my greps: 0 template sites, 2 barrel lines, 1 type import); (3) slugError written 7x, rendered 0x (my full read). Plus the S.W2 W2-6-condemned substring sniff resurrected at :59-62 against ApiProblem's super(title) (:31, my read). Challenger live cell labeled; the API-LESS dev server guarantees the failure — G-J/G-J′ born-RED. Cure: typed returned result, awaited, rendered role=status; PaletteSlugBar + slugBarRef + setError deleted."
     },
     {
      "id": "L-31",
      "verdict": "CONFIRMED",
      "note": "The dead twin owns the error channel: PaletteSlugBar.vue exposes setError and is mounted by nothing (my enumeration — 2 barrel re-exports only); SlugEditLayer exposes no setError, so the two do not share an interface. Deleted (−243 lines + 2 barrel lines) in the same G-J commit as L-30."
     },
     {
      "id": "L-32",
      "verdict": "RESCOPED",
      "rescopedTo": "W-HYGIENE",
      "note": "Verified: 10 of the test/ files import ../demo/ (my count = the seat's); demo/test/ also exists. True dual-home for tests; repo-hygiene owner, not the dock wave."
     },
     {
      "id": "L-34",
      "verdict": "CONFIRMED",
      "note": "Three-owner partition disagreement re-derived: router/index.ts:29-34 meta:{admin:true} on exactly the 5 admin routes with atmosphere/blob unmarked (my read); useDockAdminMode:27 counts 7; the schema's hue law classes 5. BUILD: extends r2 structure (b) — router derives meta.admin from VIEW_MAP group, hand literals → 0 (G-C grep arm)."
     },
     {
      "id": "L-33",
      "verdict": "RESCOPED",
      "rescopedTo": "palettes seat",
      "note": "Out of this component's subject by the seat's own filing; the export.ts / export/ / usePaletteExport.ts dual-home is the palettes owner's cure. Kept as systemic-mechanism corroboration for the lattice-guard argument (L-23)."
     },
     {
      "id": "L-30b",
      "verdict": "CONFIRMED",
      "note": "Replication bundle accreting to already-ruled identities (L-1/L-6/L-23/L-7 et al), two measurements agreeing to the digit with prior passes. Its 65-module/251KB vs 39/157KB-without-session-port closure cells are ADOPTED labeled as G-G's delta baseline. No new booking."
     },
     {
      "id": "C-23",
      "verdict": "CONFIRMED",
      "note": "Identical identity to L-30 (the id folds; L-30 holds the booking as the fullest filing). Every cited line re-verified by my full reads of SlugEditLayer.vue and useSlugMigration.ts; the unreachable spinner (finally clears slugSwitching in the same tick) confirmed by derivation."
     },
     {
      "id": "C-3-WIDENED",
      "verdict": "CONFIRMED",
      "note": "Statically certain from my useDockAdminMode read: viewEntries returns userViews for every anonymous visitor, so on blob/atmosphere/admin-* the controlled Select's model-value leaves its own option set and SelectValue renders nothing — blank primary navigation on 7 of 15 routes. The r2 group-derivation cure alone does NOT fix the anon arm — G-C gains the challenger's union-current-view clause + a per-route label assertion."
     },
     {
      "id": "C-24",
      "verdict": "CONFIRMED",
      "note": "Verbatim credential-parser clone confirmed (looksLikeSlug/normalizeTokenInput identical shapes in SlugEditLayer:25-37, my read, vs the orphaned PaletteSlugBar); the dead twin dies with L-31; the helpers hoist beside the port."
     },
     {
      "id": "C-25",
      "verdict": "CONFIRMED",
      "note": "Dock.vue:87/:89 watches non-immediate (my read) — the mount-time predicate value is never applied to the producer's ref-counted hold; survivable today only via the producer clamp (labeled). Hypothesis branch honestly labeled by the seat (no current App state mounts with editTarget non-null). { immediate: true } lands in G-Q hygiene."
     },
     {
      "id": "C-26",
      "verdict": "CONFIRMED",
      "note": "usePopupMutex fork header carries no BH relay (my read of :1-2 — violates the standing relay fond); the ActionBarLayer shim (which HAS the note) dies whole at G-B. Relay both to glass via the fond; no local API change needed meanwhile."
     },
     {
      "id": "C-27",
      "verdict": "CONFIRMED",
      "note": "All cited uncancelled one-shot sites verified (Dock.vue:105, ColorInput:256/:262, ActionButton:95, ABL:70-76 cleared on re-entry not unmount) against the correct idiom in PaneSlot:89-108 and usePopupMutex:82. Harm bounded (post-unmount ref writes are inert in Vue) — hygiene commit, one owned-handle idiom."
     },
     {
      "id": "C-28",
      "verdict": "CONFIRMED",
      "note": "Latent, measured-negative-at-HEAD (the transitionend side channel restores is-settled — challenger cell labeled): the slotLive early return (my read of ActionBarToggle:50-68) makes the watch's re-arm dead code, so the O-15b invariant is held by an unacknowledged DOM event. Drop the early return — G-Q."
     },
     {
      "id": "C-29",
      "verdict": "CONFIRMED",
      "note": "Test-truth: every e2e locator resolves the view control by aria-label and nothing asserts its rendered text (the seat's locator inventory; consistent with my C-36 zero-grep on the login surface). The route-parametrised label assertion becomes G-C's product arm — it kills the C-3 FAMILY, not the instance."
     },
     {
      "id": "C-30",
      "verdict": "RESCOPED",
      "rescopedTo": "registry maintenance (L-13 journal recovery first)",
      "note": "Verified myself: the challenge file's banner promises Pass 2 (§6, C-15..C-22, incl. a claimed BLOCKER C-15) and the body is absent — grep finds the banner (:4-5), the in-file annotation (:15) and one dangling forward-ref (:561) only. C-15..C-22 are UNCITABLE until recovered; the root attempts journal recovery before any re-run."
     },
     {
      "id": "D2-01",
      "verdict": "CONFIRMED",
      "note": "BLOCKER: the present-and-inert arm re-derived statically — usePaneRouter builds gradient/generate/mix bars from VIEW METADATA (ref-independent, my read of :193-220) while every handler optional-chains a desktop-only paneRef, so on mobile the bar RENDERS and every action silently no-ops. Clipboard-sentinel cell labeled. Cure = the DockCommand registry (structure f); an absent capability becomes ABSENT by construction."
     },
     {
      "id": "D2-02",
      "verdict": "CONFIRMED",
      "note": "BLOCKER: the picker's six actions absent from the whole mobile document — App.vue:38's ?? null masks 'the mobile slot never registered' as 'this view has no actions' (my read). Same identity family as L-1/D-13; G-I re-seamed (R-8) so the cure lands HERE, no longer waiting on MT-APP-1."
     },
     {
      "id": "D2-03",
      "verdict": "CONFIRMED",
      "note": "BLOCKER: predicates disagree BY CONSTRUCTION — App.vue:305-312 compound width+aspect query (whose own comment says it exists so the witnesses can never disagree) vs Dock.vue:71 width-only useMediaQuery (my reads) + the lg: forks + the 639px fork. At 1024x1366 aspect 0.75 → App says mobile, Dock says desktop; dual-grammar cell + frame labeled. BUILD: structure (h) + G-L."
     },
     {
      "id": "D2-04",
      "verdict": "CONFIRMED",
      "note": "OVERTURNS r1's 'MT-F004 not real' (recorded §6.3): the ACTIVATED slug layer's three 22x22 controls + unnamed 160px input are real SC 2.5.8/3.3.2 failures; r1 measured only the inert rest state. Mechanism = C-33's (producer compact opt-out + unset token seam). Interim cure BUILT here (G-M); the §7 Account-Dialog re-housing stays the constitution's W-map end state (R-10)."
     },
     {
      "id": "D2-05",
      "verdict": "RESCOPED",
      "rescopedTo": "MT-COLORINPUT-1 (the MT-F005 ≡ L-7 ≡ D-3 identity)",
      "note": "Fifth confirmation of the ONE nameless-send-btn identity already BUILT at ColorInput (r2 §1 L-7 ruling unchanged); the propose/submit-fold detail accretes to that row. Any close claiming this wave cured it is false."
     },
     {
      "id": "D2-06",
      "verdict": "RESCOPED",
      "rescopedTo": "the convergent design loop (task #7), cells attached (R-11)",
      "note": "The measured degeneracy is real (dark --accent-view C 0.188→0.021; Login/@mbabb identical dark slabs; default-route --accent-view ≡ --accent-live — cells carried labeled), but the filed cure (delete accentHueShift + the token family) would re-litigate Q5/W7-4/T-28 owner canon without owner authority. Goes to the design loop with its cells; the auth-primacy half is adjacent to G-H's producer-primitive direction; no close may cite this wave for it."
     },
     {
      "id": "D2-07",
      "verdict": "CONFIRMED",
      "note": "Both arms verified: ProfileSection:140 bare @mbabb with no bidi isolation (statically certain reorder to mbabb@ under RTL by UAX#9; frame labeled); physical ArrowRight/ArrowLeft glyphs for a logical enter/exit motif (ActionBarToggle:103, Dock.vue:154 — my reads). BUILD: G-Q (dir=ltr/unicode-bidi isolate + ONE logical chevron primitive)."
     },
     {
      "id": "D2-08",
      "verdict": "CONFIRMED",
      "note": "Sources verified: DockSeparator at ProfileSection:49 AND :123 plus ActionBarToggle's (my reads) — 3 visible lines between 4 self-seated capsules, the n−1 maximum reached because no one owns the row. PR-05 canon (labeled cite): separator ownership moves to the layer composition; main-layer count 0 — G-M arm."
     },
     {
      "id": "D2-09",
      "verdict": "CONFIRMED",
      "note": "The five-rung table carried labeled; the FALSE COMMENT verified myself — ActionBarToggle:150-153 claims 'the box lands at the sibling controls' 2.5rem height' against a measured 32px. One density decision + the comment corrected or deleted (a comment stating a measurement must be true or not exist) — G-M."
     },
     {
      "id": "D2-10",
      "verdict": "CONFIRMED",
      "note": "Statically certain: :fit-content=true (Dock.vue:132, my read) over four layers of very different widths + opacity-only crossfade (r2 census re-confirmed) = instantaneous box snap; the 471→309px cell labeled. BUILD: G-B amended — no single-frame box step ≥20px on any layer swap."
     },
     {
      "id": "D2-11",
      "verdict": "CONFIRMED",
      "note": "The one layout-animated property in the dock verified: .action-bar-toggle-slot.is-live transitions grid-template-columns on a duration/ease register while the seal rides a spring (my read of :120-128). Folds into G-B's amended motion arm (transform/opacity or nothing)."
     },
     {
      "id": "D2-12",
      "verdict": "CONFIRMED",
      "note": "As a CORRECTION: r1's Safari-keyboard BLOCKER is a known harness false-signal class (macOS FKA default; the repo's own states.mjs header warns it) — overturn recorded §6.3. The surviving MAJOR (no §5.2 roving rail) is D3-03's row → BH ask + BANK-D4."
     },
     {
      "id": "D2-13",
      "verdict": "CONFIRMED",
      "note": "Inactive faces are opacity-0 (not visibility-hidden) so nav innerText/textContent carry three invisible layers incl. the bare &rarr; at Dock.vue:137 with no aria-hidden (my read) and the live specimen string. AT protected by inert; every text-metric consumer is not. G-Q: aria-hidden the connector; leak documented."
     },
     {
      "id": "D2-14",
      "verdict": "CONFIRMED",
      "note": "role:'alert' static-from-first-paint verified at status-lamp.ts:47-52 (my read); dev-gated so prod unaffected — INFO. G-Q: role='status'; the infinite pulse is PRM-guarded (seat's own note) and bounded at execution's discretion."
     },
     {
      "id": "D2-15",
      "verdict": "CONFIRMED",
      "note": "Both idioms fourteen lines apart verified (ref at :60 vs useTemplateRef at :74) + the two ?? null computeds (:39-40, my reads). G-Q hygiene; the slugEditRef proxy chain dies further under G-K's declared-entry-focus cure."
     },
     {
      "id": "D2-16",
      "verdict": "CONFIRMED",
      "note": "hide-indicator on BOTH SelectItem blocks verified (DockViewSelect :96/:127, my read) + the P4-R4 weight-retirement comment — selection has no visual marking (both-scheme uniform cell labeled) and the one chromatic row (Palettes, Q5) impersonates it. §4.2's ColorSpaceSelector rule is the SAME canon unapplied — G-N: delete hide-indicator, consume the producer indicator gutter."
     },
     {
      "id": "C-31",
      "verdict": "CONFIRMED",
      "note": "The producer mechanism verified in dist by me: w() refocuses the incoming face/body whenever the OUTGOING face contains activeElement, after nextTick, unconditionally — SlugEditLayer:20-22's focus deterministically loses (challenger's instrumented trace labeled; mobile portaled path unaffected, labeled). BUILD G-K + BH relay (producer prefers [autofocus] in the incoming face); demo interim = focus on the became-active edge AFTER the repair, stated as such — not a timer fight."
     },
     {
      "id": "C-32",
      "verdict": "CONFIRMED",
      "note": "Statically certain from my dist reads: Dock.vue:132 binds no :background-canvas; the wrapper always hands De() a getter; De() returns null for a null-yielding getter WITHOUT the document-convention fallback (body quoted in §6) while the is-live predicate DOES consult the convention → source-unavailable forever, tint pinned at the 4% floor (3/3-route cells + 3.5Hz re-stamp labeled). BUILD G-P: thread the App-owned canvas (backgroundCanvas prop exists, dist:710); the fallthrough fix is a BH ask, never a demo shim."
     },
     {
      "id": "C-33",
      "verdict": "CONFIRMED",
      "note": "Mechanism named and verified from producer source: touch-floor.css exempts compact AND .glass-dock descendants; the three compact DockControls (SlugEditLayer:91-118) hit both exclusions and the demo never re-supplies geometry through the --dock-compact-control-* seam its OWN sibling uses (ActionBarToggle:155, T-36 — my reads); the input is placeholder-only w-40. G-M cure via the producer's own token seam + a real name; producer hit-seat ask → BANK-D5."
     },
     {
      "id": "C-34",
      "verdict": "CONFIRMED",
      "note": "Production has ZERO live regions (the only one is the isDev-gated lamp — my read of DockStatusLamp:32) and every layer change parks focus on an unnamed role-less div[tabindex=-1] (3/3 cells labeled). BUILD G-K: one dock-owned polite region + declared entry-focus per layer; arm (iii) gated against the BUILT bundle (L-12)."
     },
     {
      "id": "C-35",
      "verdict": "CONFIRMED",
      "note": "Write-only refs verified by my grep (colorInputRef/actionToolbarRef declared :28-29, bound :103/:116, read nowhere; defineExpose :96 excludes them); ColorInput's exposed focus() has no caller. Dies with G-B's shim deletion + G-K's entry-focus edge; the open-color-input focus arm is G-K (ii), RED today (labeled cell)."
     },
     {
      "id": "C-36",
      "verdict": "CONFIRMED",
      "note": "Zero e2e/test hits for the entire login surface (my grep: SlugEditLayer|Switch to slug → 0) with the exclusion stated in the o15 oracle's prose. The G-J vitest spec + G-J′ live arm close four findings' regression surface (C-31/C-33/C-23/C-36) in one spec, as the seat proposed."
     },
     {
      "id": "C-37",
      "verdict": "CONFIRMED",
      "note": "Recorded as the negative-proof ledger (six measured absences: parseCssColor crash class does not reach the dock; active-layer teardown recovers; zero getImageData across colour ticks; DockControl forwards type; .is-active hits a real producer rule; App's transition listeners guarded) so no later seat re-spends the probes; arms (1)(2) become the parser-band regression floor. No wave obligation."
     },
     {
      "id": "C-38",
      "verdict": "RESCOPED",
      "rescopedTo": "atmosphere seat (labeled hypothesis), SEQUENCED before G-P's sampled claim",
      "note": "The 300x150 WebGL2 backing-store measurement is real (labeled); the defect interpretation is explicitly unestablished by the seat itself. G-P asserts 'not source-unavailable' only — trading it for sample-unavailable is the atmosphere row firing, not this gate passing."
     },
     {
      "id": "D3-01",
      "verdict": "CONFIRMED",
      "note": "BLOCKER: a composite of two booked identities plus new evidence — the capability hole (the command-seam cluster, BUILD here per R-8) painted over by the W47-owned retired switcher (R-9: PaneSegmentedControl stays ALIVE for W47's own gate, CARRY-LEDGER:22 — this wave may not kill it and no close may claim it did). NEW and adopted: the 200%-zoom arm (720x900 ≡ 1440@200% — a zoomed DESKTOP user loses all six picker actions) → G-I's second arm; the collision geometry cells + the three frames accrete (frames currently UNTRACKED — force-added per R-12/L-7)."
     },
     {
      "id": "D3-02",
      "verdict": "RESCOPED",
      "rescopedTo": "the convergent design loop (task #7), cells attached; the D-22 overturn recorded §6.3; the first-ever collapsed-seal π frame ADOPTED as a wave obligation",
      "note": "The facts are real and OVERTURN r1/r2's 'seal never observed' claim (collapse is pointer-history-armed; :always-expanded=!isDesktop keeps mobile expanded — my read of :132). But the seal is owner-ratified canon (W7-1/T-37/T-28, the law block in Dock.vue itself) and no ruled law bans a hover-armed collapse — the one-resting-state question is a design-policy call, not a defect this wave may adjudicate."
     },
     {
      "id": "D3-03",
      "verdict": "CONFIRMED",
      "note": "§5.2 names this component ('horizontal Dock/rail roving focus') and Dock.vue binds zero keydown handling (my read; ArrowRight/Home cells labeled). The cure is producer rail behaviour (the seat's own edict-4 argument); my dist grep finds NO rail/roving export in glass 7.0.0 → BH-relayed ask + BANK-D4 with a runnable re-trigger; no demo keydown graft may pre-empt it (masking-fallback ban). The constitution obligation stays OPEN against the producer, recorded."
     },
     {
      "id": "D3-04",
      "verdict": "CONFIRMED",
      "note": "Same identity as D2-03 (the predicate pile), independently measured; the shell.css stamp-law comment makes it a violation of the repo's own recorded decision. ONE booking — G-L + structure (h)."
     },
     {
      "id": "D3-05",
      "verdict": "CONFIRMED",
      "note": "The mobile floor miss confirmed via the same producer-mechanism chain as C-33 (compact register supplies no hit-seat); the 26px rows come from PaneSegmentedControl's 639px fork, which dies with the control at W47 (seam stated, R-9). Demo token cure G-M; producer hit-seat ask BANK-D5 — a per-site padding bump is banned by PR-12 (the seat's own cite)."
     },
     {
      "id": "D3-06",
      "verdict": "CONFIRMED",
      "note": "Decisive new fact from MY dist census: glass-ui 7.0.0 ships ./dropdown-menu in its exports map — the producer menu tier is available TODAY, so the cure is BUILD (G-O), not a bank. MobileMenuDropdown/ProfileSection compose from demo-local ../../../ui/dropdown-menu (my reads); the wrapped 55px GitHub row + read-through material cells carried labeled (frame committed by the wave)."
     },
     {
      "id": "D3-07",
      "verdict": "REFUTED",
      "note": "Fails on its own citation: the constitution's clause governs DIRECT ROUTE CHANGES ('Direct route changes keep its full navigation identity' — my read) and opening the Tools LAYER is not a route change; the band reservation holds (72px constant — the same seat's own negativeProof cell); and 'the only exit is one unlabelled ←' is false — Dock.vue:154 carries aria-label=Back (my read). The residual icon-only-density observation is PR-07/PR-06 material for the design loop, partly covered by G-M's rung arm. The 'persistent chrome stops being persistent' clause is STRUCK and uncitable."
     },
     {
      "id": "D3-08",
      "verdict": "RESCOPED",
      "rescopedTo": "MT-COLORINPUT-1 (the MT-F005 identity)",
      "note": "Sixth confirmation of the one nameless send-btn, correctly self-identified by the seat as MT-F005's exact pattern. Cured at ColorInput (D-3/G5/G11a); accretes, never re-books."
     },
     {
      "id": "D3-09",
      "verdict": "CONFIRMED",
      "note": "The cycle verified by my reads: Dock.vue:4 imports GlassDock/DockLayerGroup/DockLayer from './' while index.ts:5 exports Dock.vue — index.ts→Dock.vue→index.ts, for five symbols all from one producer package (L-30b's walker found it the tree's ONLY runtime cycle, labeled). G-Q: import from @mkbabb/glass-ui/dock; the barrel exports only Dock."
     },
     {
      "id": "D3-10",
      "verdict": "CONFIRMED",
      "note": "Two-root SFC verified (:128 wrapper + :293 DockStatusLamp sibling — my read); the lamp's documented positioning context is already the band (its own comment) so the re-home to App.vue's nav is the ownership-honest move; dev-gated so prod unaffected; the mobile clipping cell labeled. G-Q hygiene."
     },
     {
      "id": "D3-11",
      "verdict": "RESCOPED",
      "rescopedTo": "the visual-audit harness owner (Phase-D ENGINE=chromium re-run, task #4)",
      "note": "The forced-colors capture is a WebKit render that cannot produce the state it names — a green row for an unrun test. Until the chromium re-run, the row reads UNTESTED and no close may cite the forced-colors matrix for this component."
     }
    ],
    "waveSpec": {
     "waveId": "MT-DOCK-LAYERS-1",
     "title": "The dock shell tells the truth — DockCrossfade adoption, admin identity derived not latched, a chrome referent that reads the surface that paints, the session port re-homed behind a platform key, chrome-tier pill primitives — and (r3 merge) a provide/inject DockCommand surface, a truthful login with a rendered error, one layout predicate, declared entry focus + announcement, a live backdrop referent, and register/composition truth. ONE wave (the standing App.md:146 name; r2 §3 + r3 §6.5 read as one spec).",
     "bornRed": true,
     "scope": "r2 scope (Dock.md §3) PLUS: NEW demo/shell/dock/commands.ts · demo/shell/usePaneRouter.ts · demo/color-picker/App.vue (?? null hops, canvas thread, lamp re-home) · usePaletteWiring.ts (startEdit poll retired) · demo/picker/ColorPicker.vue (provides commands; :311 comment) · demo/palettes/useSlugMigration.ts + DELETE PaletteSlugBar.vue (+2 barrel lines) · DockViewSelect.vue · menus/MobileMenuDropdown.vue + menus/ProfileSection.vue · DockStatusLamp.vue + status-lamp.ts · ActionBarToggle.vue · ColorInput.vue · eslint.config.js (shell→palettes ban, physical paths, ≥1-file meta-check) · NEW specs dock-slug-login.test.ts + label-per-route arm. READ-ONLY trees untouched; execution awaits the formation's begin-word.",
     "structure": "r2 (a)-(e) unchanged, PLUS: (f) desktop-only capability dies by INVERSION — typed DockCommand registry, panes provide / dock+usePaneRouter inject, editTarget write moves onto the provided EDIT_TARGET_KEY cell (provided at App.vue:270 today); deletes the ?? null hops, the 2s poll, every action-dispatch ?.() — present-and-inert becomes unspellable. (g) silent login dies by CONTRACT — onSlugSwitch RETURNS a typed result, awaited and RENDERED (role=status); status-branching not message-sniffing; PaletteSlugBar/slugBarRef/setError deleted. (h) the predicate pile dies by SINGLE OWNER — dock consumes the App-owned [data-layout] truth; Dock.vue:71 + the lg:/sm: display forks deleted. (i) entry focus + announcement DECLARED — per-layer entry target honoured on the became-active edge (producer [autofocus] preference = BH ask); one dock-owned polite region in prod. (j) the backdrop referent becomes REAL — the App-owned atmosphere canvas threaded to GlassDock's backgroundCanvas prop (exists in dist :710); producer convention-fallthrough = BH ask, never a demo shim.",
     "gates": [
      {
       "gate": "G-A (carried verbatim, App.md:146)",
       "command": "test \"$(grep -c 'void opts\\.' demo/shell/dock/layers/ActionBarLayer.vue)\" = 0",
       "redToday": true,
       "whatWouldMakeItFail": "ActionBarLayer.vue:67 `void opts.containerEl` exists (re-verified this session)"
      },
      {
       "gate": "G-B (crossfade + AMENDED box/motion truth)",
       "command": "grep arms (DockCrossfade present; useLayerTransition/SUB_LAYER_CROSSFADE_MS = 0) && node .../gates/sublayer-crossfade.mjs — plus: no single-frame .glass-dock box step ≥20px across ANY layer swap; .action-bar-toggle-slot animates transform/opacity only",
       "redToday": true,
       "whatWouldMakeItFail": "0 DockCrossfade uses, 5 shim hits; main→slug-edit measured Δw −162.5px in one frame (:fit-content over unequal layers + opacity-only crossfade — statically certain); grid-template-columns transition present in ActionBarToggle"
      },
      {
       "gate": "G-C (latch structure + AMENDED viewEntries/label/router arms)",
       "command": "greps (no isAdminMode ref seed; no hand ViewId arrays; `meta: { admin: true }` literals = 0) && npx vue-tsc -p tsconfig.demo.json --noEmit && npx vitest run test/dock-admin-mode.test.ts (adds: anon currentView='blob' ⇒ entries CONTAIN blob)",
       "redToday": true,
       "whatWouldMakeItFail": "seed at :32, arrays at :26-27, 5 meta literals in router/index.ts (my grep); anon-on-blob viewEntries excludes the current view (my derivation) — SelectValue blank on 7 of 15 routes"
      },
      {
       "gate": "G-D (latch product + label arm)",
       "command": "node .../gates/admin-latch.mjs — the r2 five-step gold trace PLUS: per-route anon cold-load asserts view-select accessible text === VIEW_MAP[view].label",
       "redToday": true,
       "whatWouldMakeItFail": "gold latch [false,true,TRUE,TRUE,TRUE] (labeled r2 cell); blank trigger text on blob/atmosphere/admin-* (labeled + statically certain)"
      },
      {
       "gate": "G-E/G-F (chrome referent — unchanged from r2)",
       "command": "as Dock.md §3",
       "redToday": true,
       "whatWouldMakeItFail": ".glass-dock computes rgba(0,0,0,0) both schemes; resolveLiveTint('chrome') undefined every call"
      },
      {
       "gate": "G-G (shell edge + L-23 rider)",
       "command": "grep shell→palettes value-imports = 0 && node .../gates/dock-graph.mjs && NEW: eslint no-restricted-imports ban demo/shell/**→demo/palettes/** at PHYSICAL paths with a ≥1-file glob meta-check",
       "redToday": true,
       "whatWouldMakeItFail": "5 value-import files today; every existing G-DEMO glob targets the nonexistent demo/@ (my ls+grep) — enforcement that enforces nothing"
      },
      {
       "gate": "G-H (pills — owner-ruled MT-F026/OM-1, NOT re-litigated)",
       "command": "node docs/tranches/V/megatranche/audit/probes/owner-marked-shadows.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Login + @mbabb pills at 2 cast layers each (r2 cells; unchanged)"
      },
      {
       "gate": "G-I (mobile reachability — RE-SEAMED per R-8 + zoom arm)",
       "command": "node .../gates/dock-mobile-colorentry.mjs at 390x844 AND 720x900 (≡1440@200% zoom): a free-text colour entry AND the six picker actions reachable; the registry cure lands HERE — no MT-APP-1 wait",
       "redToday": true,
       "whatWouldMakeItFail": "mobile slot never registers capability (no :on-mount — my read); 720x900 hit-test returns segmented-indicator at the Tools centre, slot 0px/opacity 0/tabindex −1 (labeled)"
      },
      {
       "gate": "G-J (login truth — structure)",
       "command": "test $(grep -c 'includes(\"409\")' SlugEditLayer.vue) = 0 && test ! -f demo/palettes/browser/slug/PaletteSlugBar.vue && grep slugBarRef useSlugMigration.ts = 0 && npx vitest run test/dock-slug-login.test.ts (stubbed rejecting port ⇒ rendered role=status error, layer stays open)",
       "redToday": true,
       "whatWouldMakeItFail": "the substring sniff at :59; the dead twin exists; 6 slugBarRef refs; no spec exists (all my greps)"
      },
      {
       "gate": "G-J′ (login truth — product)",
       "command": "node .../gates/slug-login-error.mjs — chromium :9000 (API-LESS = the guaranteed-fail fixture): Login → zzzz-zzzz-zzzz-zzzz → Enter ⇒ visible error in the layer, layer open, input kept",
       "redToday": true,
       "whatWouldMakeItFail": "today the layer closes inert with errorish:[] and a clean console (labeled cell; all three swallows statically certain)"
      },
      {
       "gate": "G-K (entry focus + announcement)",
       "command": "node .../gates/dock-entry-focus.mjs — (i) click Login ⇒ activeElement IS the slug input; (ii) Open color input ⇒ field holds focus; (iii) the BUILT bundle's dock contains ≥1 polite live region",
       "redToday": true,
       "whatWouldMakeItFail": "(i) producer w() refocuses the face (dist-verified + labeled trace); (ii) focus never moves, typed text dropped (labeled); (iii) the only live region is isDev-gated (my read)"
      },
      {
       "gate": "G-L (one layout predicate)",
       "command": "test $(grep -c useMediaQuery demo/shell/dock/Dock.vue) = 0 && node .../gates/dock-layout-grammar.mjs at 1024x1366: NOT (mobile switcher AND desktop profile both visible)",
       "redToday": true,
       "whatWouldMakeItFail": "Dock.vue:71 width-only query (my read) disagrees with App's width+aspect predicate by construction; dual grammar measured (labeled, frame committed)"
      },
      {
       "gate": "G-M (register floor + composition)",
       "command": "node .../gates/dock-register-floor.mjs — ACTIVATED slug layer: every control ≥24px (and ≥ the producer floor under pointer:coarse via the --dock-compact-control-* seam), input has an accessible name; main-layer visible separators = 0; the false '2.5rem height' comment gone-or-true",
       "redToday": true,
       "whatWouldMakeItFail": "22-23px ×3 + placeholder-only input (labeled + producer-source-certain); 3 visible separators (sources my read); the comment claims 40px against a measured 32"
      },
      {
       "gate": "G-N (selected state)",
       "command": "test $(grep -c hide-indicator demo/shell/dock/DockViewSelect.vue) = 0 && node .../gates/view-menu-selected.mjs (both schemes: aria-selected row shows a nonzero producer-indicator delta)",
       "redToday": true,
       "whatWouldMakeItFail": "2 hide-indicator occurrences (my read); all 7 options measured visually identical (labeled) — §4.2 canon unapplied"
      },
      {
       "gate": "G-O (producer menu tier)",
       "command": "grep 'ui/dropdown-menu' in the two menus = 0 (→ @mkbabb/glass-ui/dropdown-menu — EXISTS in dist exports, my census) && node .../gates/mobile-menu-rows.mjs (390: no wrapped row; uniform row rung)",
       "redToday": true,
       "whatWouldMakeItFail": "demo/ui imports present (my reads); GitHub row 55px vs 44 siblings + read-through material (labeled, frame committed)"
      },
      {
       "gate": "G-P (backdrop referent)",
       "command": "node .../gates/dock-backdrop.mjs — post-settle: data-backdrop-sample-state ≠ unavailable/source-unavailable AND --glass-backdrop-luma set; may NOT be greened by trading reasons (C-38 → atmosphere row)",
       "redToday": true,
       "whatWouldMakeItFail": "no :background-canvas bound (my read) + De() getter-null returns null without the document fallback (dist-verified) → source-unavailable re-stamped ~3.5Hz forever (labeled 3/3 routes)"
      },
      {
       "gate": "G-Q (hygiene, one script)",
       "command": "bash .../gates/dock-hygiene.sh — bidi isolation on @mbabb + slug pill · no `from \"./\"` in Dock.vue · lamp role=status · aria-hidden on the &rarr; connector · dock root-barrel glass imports = 0 · TopDock comment gone · both hold watches immediate · single-root Dock.vue (lamp in the band) · slotLive early return gone",
       "redToday": true,
       "whatWouldMakeItFail": "every arm RED by my reads: no bidi attrs; Dock.vue:4; role alert at status-lamp.ts:47; bare span :137; 2 root-barrel imports; ColorPicker.vue:311; watches :87/:89 non-immediate; sibling root :293; early return present"
      }
     ],
     "carries": "BUILD here: L-35 · L-1-rep · D2-01 · D2-02 · D3-01 (registry + G-I/G-L; W47 keeps the switcher's death — R-9) · L-30≡C-23 · L-31≡C-24 · C-36 · C-3-WIDENED · C-29 · D2-03≡D3-04 · D2-04≡C-33≡D3-05 · C-31 · C-34 · C-35 · C-32 · D2-10 · D2-11 · D2-16 · D3-06 · D2-08 · D2-09 · D2-07 · L-34 · the eleven-row hygiene cluster (C-25/C-26/C-27/C-28/D2-13/D2-14/D2-15/D3-09/D3-10/L-36/L-39) — plus all r2 BUILD rows unchanged. FOLD: D2-05/D3-08→MT-COLORINPUT-1 · L-32/L-37→W-HYGIENE · L-38→W47+BH+W-HYGIENE · L-33→palettes · D3-11→visual harness · C-38→atmosphere (sequenced before G-P's sampled claim) · C-30→registry maintenance (L-13 recovery) · D2-06/D3-02→the convergent design loop with cells (R-11) · L-23-rep→W-HYGIENE (G-G rider here) — plus r2 folds unchanged. RETIRED/STRUCK: D3-07's persistence clause; r1's 'MT-F004 not real'; r1's Safari-keyboard BLOCKER; r1/r2's 'seal never renders'; the r1 banner's C-15..C-22 until recovered; G-I's superseded MT-APP-1-first seam text. BANKS: D1 (chrome token) + D3 (glass major) kept; NEW D4 (producer rail: grep dist for rovingFocus/RovingFocus/useDockRail — silent today) + D5 (compact hit-seat token: grep dist for compact-hit/hit-seat/--dock-compact-control-seat — silent today).",
     "piObligations": "r2 π unchanged, PLUS: the three r3 frames FORCE-ADDED in the first commit (collision-1024x1366.png · state-mobilemenu.png · state-tools-desktop.png — currently UNTRACKED, my git ls-files run; L-7); NEW committed captures: the collapsed WAX SEAL at rest (the component's first-ever seal frame — D3-02 adoption), the ACTIVATED slug layer both schemes, the RTL band frame, the 720x900 zoom arm. All gate cells chromium; :9000 is API-LESS — for G-J′ the missing API is the FIXTURE (guaranteed-fail login), stated once; G-K (iii) runs against the BUILT bundle (the dev server is structurally blind to it); challenger WebKit cells stay labeled corroboration, never the gate engine (I-20); nothing claims real Safari.",
     "deltaObligations": "r2 (1)-(7) unchanged, PLUS: (8) failed login [layer closes silent] ⇒ [layer open, rendered role=status error, spinner during flight]. (9) mobile gradient Copy CSS clipboard [SENTINEL unchanged] ⇒ [gradient string]; dock faces 3 ⇒ 4 with a live mobile-edit path; the 2s-poll warn ⇒ gone. (10) 1024x1366 [both grammars] ⇒ [exactly one]. (11) Login click activeElement [div.dock-face] ⇒ [the slug input]; typed text [dropped] ⇒ [held]. (12) backdrop [source-unavailable, luma unset, ~42 idle mutations/4s] ⇒ [state set, luma set, idle ~0]. (13) view menu [7 identical options] ⇒ [producer marker on the aria-selected row]. (14) slug controls [22-23px, nameless] ⇒ [≥floor via token seam, named]. (15) layer swap [Δw −162.5px one frame] ⇒ [animated or held]. (16) Dock.vue closure [65 modules/251KB, palettes 23] ⇒ [~39/157KB, palettes 0] (L-30b baseline adopted).",
     "completableAlone": "YES (L-1, re-answered under the merged scope): every added arm is demo-side executable today — the ./dropdown-menu tier and backgroundCanvas prop EXIST in the installed dist (my censuses), the token seam exists (T-36 precedent), the registry is plain provide/inject, and R-8 removed the MT-APP-1 wait — or BANKED with a runnable trigger (rail D4, hit-seat D5, chrome token D1, glass major D3). Nothing waits on the API, Safari, or a sibling wave. Execution lands FOUR sequenced commits under the ONE name (registry+login · the r2 core · layout+register+focus+backdrop · hygiene), each independently green-able — §5-DISSENT-1's sanctioned split, extended, never a second wave name. Ten of ~17 gate arms run this minute as grep/tsc/vitest; every probe arm's RED cell is pasted with a statically-certain mechanism."
    },
    "addendumClause": "MT-DOCK-B (E-3, appended to Dock.md §6.6 — never rewriting MT-DOCK-A): (1) STALE-NAME RECEIPTS — glass-ui 7.0.0 DOES ship ./dropdown-menu and ./dom (this seat's exports-map census): any seat treating the producer menu tier as absent, or the root barrel as the only clipboard path, is working from stale memory; ColorPicker.vue:311's TopDock is the formation's third stale-name instance. (2) INSTRUMENT NOTES — 'can the catch fire?' is one await read (SlugEditLayer:54); 'does the producer steal focus?' is the dist w() contains-check body; 'convention or wiring?' is De()'s getter branch; 'can the model-value leave the option set?' is viewEntries' two-arm computed — the whole r3 corpus needed ZERO fresh browser probes from this seat. (3) METHOD WARNINGS carried and binding on gate authors: one reused MCP page manufactures phantom route hijacks; WebKit keyboard cells are meaningless without FKA; inert at-rest rows are geometry-only — gates measure the ACTIVATED state. (4) THE ENVIRONMENT AS FIXTURE — the API-LESS dev server makes G-J′'s login failure deterministic (L-12 stated as a feature exactly once); the same server can never witness the prod live-region arm or any bundle claim. (5) COUNT DISCIPLINE — the honest r3 headline is FIVE new defect classes (command seam · login truth · predicate pile · focus/announcement · backdrop referent) plus one canon-hard register cluster; 13 of 58 rows are replications/accretions of ruled identities and 12 are re-homed; a close quoting raw counts without this clause misquotes the record. (6) OVERTURN LEDGER — r1's 'MT-F004 not real', r1's Safari-keyboard BLOCKER, and r1/r2's 'seal never renders' are DEAD (§6.3); the r1 banner's C-15..C-22 are UNCITABLE until journal-recovered (L-13); NO §1-§5 ruling of this document is overturned, and the owner marks MT-F026/OM-1/OM-7 stand un-relitigated.",
    "dissent": "(1) Solo-seat caveat (L-14): r3 ran one adjudicator over one challenger fleet — no second worker, no arbiter above this seat. Every CONFIRMED rests on my own re-derivation plus one fleet's cells; the cells I could not force statically (the five-rung table, the 55px menu row, the dark-accent values, the focus-race ordering, the collision hit-tests) are carried LABELED and the gate scripts re-measure at execution — divergence is a gate event, not silent drift. A future tri-fold pass over §6 is sanctioned and would be the first to satisfy L-14 for this corpus. (2) The D2-06 tension: 'stop encoding view identity as hue' collides with Q5/W7-4/T-28 owner canon; I declined to adjudicate owner canon and routed the row to the design loop with its cells (R-11). If the loop rules FOR deletion, G-N and the seal-ink derivation survive unchanged — nothing in this wave pre-judges it. (3) L-23's split disposition: building the shell→palettes eslint ban here while W-HYGIENE owns the lattice re-point creates two eslint-touching commits — accepted deliberately (a wave that creates a boundary without its guard repeats the L-23 disease); the W-HYGIENE fold notes the rider so neither doubles the other. (4) Wave size: the merged MT-DOCK-LAYERS-1 is at the outer edge of L-1's one-session completability; the sanctioned remedy is the four sequenced commits under ONE name (§6.5), and if execution still finds it too large, the split line is commit (1) registry+login first — the two BLOCKER classes — never a second wave name. (5) The 'present-and-inert' severity judgment (D2-01 worse than absence) is this seat's reading of the masking-fallback edict; a dissenting reader could rank D2-02's absence arm higher — both are cured by the same structure (f), so the ranking is moot at execution."
   }
  },
  {
   "agentId": "a338267d46d76c74c",
   "key": "v2:94aa79dd7814e538493a3ee69185bf3119720f1b28e5952533c91077b1063d1f",
   "result": {
    "modelObserved": "claude-opus-5[1m] — Opus 5, 1M-context variant. Observed in this seat's own runtime and declared at spawn; not inherited from a harness label, not relabeled (L-11).",
    "slug": "dock-r3-delta-adjudication-worker-O",
    "verdicts": [
     {
      "id": "L-35 ≡ L-1(replicated) ≡ D2-01 ≡ D2-02 ≡ D3-01(capability half) ≡ L-30b(action-bar half) — the dock owns no command surface",
      "verdict": "CONFIRMED",
      "note": "Five seats filed this; I re-derived it without a browser and then measured it. Source trace: demo/color-picker/App.vue:83-93 (mobile <PaneSlot>) passes NO :on-mount while :105 and :131 do; :323-332 are the sole writers of colorPickerRef/generatePaneRef/gradientPaneRef/mixPaneRef; usePaneRouter.ts:196,197,198,208,209,210,220,221,222 are NINE dock actions of the form paneRefs.<view>.value?.<cmd>?.(); App.vue:38 is :action-bar=\"colorPickerRef?.actionBarContext ?? null\". The terminal proof is a single-writer trace, not a probe: `grep -rn onStartEdit demo/` outside ColorPicker.vue returns EXACTLY ONE call site, usePaletteWiring.ts:117, inside whenColorPickerReady, which polls the permanently-null colorPickerRef 40x50ms (:37) then console.warns (:49-53) — so editTarget can never become non-null on mobile, so Dock.vue:72 mobileEditActive is permanently false, so the whole mobile-edit DockLayer (Dock.vue:135-145) is DEAD CODE in the only grammar it renders in. My live cell (chromium, fresh contexts, /#/): action-region census Copy color/Random color/Open color input = false at 390x844, 720x900 and 1024x1366, true at 1440x900; dock faces 3 mobile vs 4 desktop. The `?.` and `?? null` are the masking-fallback the owner's edict 2 forbids, at the highest-cost site: an action that can be absent is present, named, focusable and inert. Severity BLOCKER upheld. This is the row that forces DELTA-1: the r2 apotheosis handed the CURE to MT-APP-1 as `bindPane on all three PaneSlot sites` and kept only a witness gate (G-I) here — that shape is a masking fix (it binds the ref channel on mobile and leaves present-and-inert representable), so the cure shape is upgraded to the provide/inject command registry both corpora independently proposed and its home moves into this wave; G-I is discharged by the stronger G-J."
     },
     {
      "id": "D3-01 — \"the retired PaneSegmentedControl is painted on top of the Tools control\" (collision framing)",
      "verdict": "REFUTED",
      "note": "I reproduced the challenger's OBSERVATION exactly and it does not mean what the row says. My cell: document.elementFromPoint() at the Tools button's own centre returns DIV.segmented-indicator.glass-capsule at 390x844, 720x900 and 1024x1366 (hitIsTools=false), and at 1024x1366 the panes box (x398.4 w141.1) covers the tools box (x409.4 w100.8) completely. But the mechanism is the inverse of occlusion: .action-bar-toggle-slot computes grid-template-columns 0px, opacity 0, rect width 0, and .action-bar-toggle-inner carries overflow:hidden (ActionBarToggle.vue:131; the release rule at :143 needs .is-visible.is-settled, absent). The Tools control's border box merely overflows a zero-width clip — it is NOT painted (opacity 0 + clipped), NOT hit-testable (elementFromPoint proves the clip removes it from hit order, which is the correct behaviour for a hidden control) and carries tabindex=\"-1\" on all three mobile viewports. Nothing is laid out on top of anything; a clipped child still reports an unclipped getBoundingClientRect, and the probe read that rect. STRUCK AND UNCITABLE by any close: \"painted directly on top of\", \"two siblings laid out on top of each other\", \"100% overlap of a live control\". The BLOCKER's real substance is entirely in its other half (capability absence — see the command-registry row) plus the W47 carry. Residual kept at MINOR and carried into the wave's hygiene commit: a zero-width presence slot whose child overflows its clip produces a phantom rect over a live control — it manufactured this BLOCKER and becomes a real defect the day the clip is lifted."
     },
     {
      "id": "D3-01 — the constitution-retired PaneSegmentedControl is still mounted",
      "verdict": "RESCOPED",
      "rescopedTo": "W47 (F1 shell & scene) — the booked `PaneSegmentedControl 1→0` gate, id preserved",
      "note": "The canon citation is real and I verified it: VISUAL-CONSTITUTION.md:89 reads \"V retires the global Dock `PaneSegmentedControl` and left/right view state\", and CARRY-LEDGER.md:22 already books it as a W47 gate with the note \"it lives ALIVE at demo/shell/ for exactly this gate\". `grep -rn PaneSegmentedControl demo/` excluding the file itself = 4 references (Dock.vue:15 import, :198 mount, and the shell barrel). So this is an INHERITED BOOKED row with a named owner and a runnable 1→0 gate, not a new finding — re-booking it here would be exactly the anti-rename violation L-5 exists to stop. It travels to W47 with its evidence (including L-38's :deep(.segmented-tab) piercing, which dies with it) and this wave asserts nothing about it beyond not mounting a replacement."
     },
     {
      "id": "C-3-WIDENED — the primary navigation control renders with NO name on 7 of 14 views for any unauthenticated visitor",
      "verdict": "CONFIRMED",
      "note": "The single most valuable row of the delta round, and NEW: it has no owner in the r2 apotheosis. My own measurement, one fresh browser context per route, unauthenticated, 3.2s settle, reading the accessible text of nav.dock-band [aria-label=\"Select view\"]: /#/ → \"Home\", /#/gradient → \"Gradient\", /#/blob → \"\", /#/atmosphere → \"\", /#/admin/users → \"\" (gold=1 on all three blanks). Mechanism source-certain: useDockAdminMode.ts:34-39 returns adminViews only when isAdminMode && isAdminAuthenticated; userViews (:26) omits atmosphere, blob and all five admin-*; DockViewSelect.vue:52 feeds that set as a CONTROLLED :model-value=\"currentView\", so <SelectValue/> (:87) resolves nothing and reka reports no aria-selected on any option. The deeper invariant broken is that a controlled select's model-value can leave its own option set — 7 of the 14 VIEW_MAP entries. Load-bearing consequence for the formation: the r2 wave's structure (b) as written (\"viewEntries filtering VIEW_MAP by group\") does NOT cure this — a user-group filter still excludes blob, so the blank label survives the r2 cure. Hence DELTA-2: the option set must be the group filter UNIONED unconditionally with the current view, and the wave gains gate G-K asserting the rendered name equals VIEW_MAP[id].label on all fourteen routes. Severity MAJOR (the product's primary navigation is anonymous on half its routes); not BLOCKER, because navigation remains operable."
     },
     {
      "id": "L-30 ≡ C-23 — a failed login is completely silent (three independent swallows)",
      "verdict": "CONFIRMED",
      "note": "Confirmed statically to certainty and then reproduced live with my own instruments. (1) SlugEditLayer.vue:39 declares onSlugSubmit async but contains NO await: :54 calls pm.onSlugSwitch(...) un-awaited inside a try, so the catch at :57-62 is unreachable for the async rejection, and :55-56 clear the input and close the layer synchronously before the network call resolves; slugSwitching flips true(:42)→false(:64) inside one tick, so the <Loader2 v-if=\"slugSwitching\"> at :97 is unreachable UI. (2) It would not matter: useSlugMigration.ts:51 catches everything itself and dispatches onto slugBarRef.value?.setError (:84-87) where `grep -rn slugBarRef demo/` returns ONLY the declaration (:30), the four uses and the return (:121) — no ref= binding exists anywhere. (3) slugError is assigned at seven sites (:18,:43,:49,:59,:60,:61,:62) and the template at :75-119 contains no reference to it — I read the whole template. Live (chromium 1440x900): fill 'aaaa-bbbb-cccc-dddd', click [aria-label=\"Switch to slug\"] → inputValue \"\", layer inert:true aria-hidden:\"true\", spinnerPresent false, errorish [] (an exhaustive leaf-text scan of the whole document for fail/error/not found/too many/already signed/invalid), Login still present, console tail carries only the two boot lines. ENV honesty (L-12): the dev server is API-LESS and that is WHY the login fails — the defect is that the failure is SILENT, and all three swallows are source-certain independent of the API. Canon: VISUAL-CONSTITUTION.md:83 \"Selected, failed, pending, withdrawn and disabled states are never color-only. Role, accessible name, state/value and associated error/status are explicit.\" Also confirmed: the :48-52 'Already signed in.' branch writes an error and returns without closing, leaving a form that has silently refused. BLOCKER upheld."
     },
     {
      "id": "L-31 ≡ C-24 — the slug bar exists twice and the DEAD copy owns the error channel",
      "verdict": "CONFIRMED",
      "note": "`grep -rn PaletteSlugBar demo/` returns exactly four hits: useSlugMigration.ts:6 (a TYPE-only import), :30 (the ref's type parameter), and two barrel re-exports (browser/slug/index.ts:3, browser/index.ts:44). `grep -rn '<PaletteSlugBar' demo/ --include='*.vue'` = 0 — no template in the repository renders it. So the only component in the tree that owns a setError surface is mounted by nothing, and the composable's four setError dispatches are guaranteed no-ops. The live surface, SlugEditLayer.vue, defineExposes {onStartSlugEdit, onCopySlug, slugSwitching} (:72) with no setError — the two do not even share an interface. C-24's additional finding is also confirmed by reading both files: looksLikeSlug and normalizeTokenInput (including the ADMIN_TOKEN= assignment strip and the quote strip) are duplicated between SlugEditLayer.vue:25-37 and PaletteSlugBar.vue — two independent parsers of the same user credential, one of them unreachable. This is not a separate defect from L-30 so much as the reason L-30's third swallow exists; both cure in one commit (typed return kills the instance-ref channel, deletion kills the twin)."
     },
     {
      "id": "C-31 — the login field's focus is revoked ~0.3ms after it is set; typed keystrokes are dropped",
      "verdict": "CONFIRMED",
      "note": "Reproduced independently with my own probe, no challenger script read. Chromium 1440x900, click the Login pill, wait 900ms: document.activeElement is DIV.dock-face.is-active with tabIndex -1, inputIsActive false; then typing 20 characters leaves the input value \"\". The challenger's producer trace (dist/dock.js:892-896 — the layer focus-repair `!n||!r||!n.contains(r)||(await nextTick(),(_(t)??document.body).focus?.())`) is a coherent mechanism for what I measured: two nextTick-scheduled focus calls race and the producer's runs second whenever the OUTGOING face contains activeElement, which the desktop Login button does. I did not re-instrument the focus stack, so I adopt the producer line-number attribution as CARRIED-LABELED and rest the verdict on my own end-state measurement plus the demo-side fact that SlugEditLayer.vue:20-22's nextTick focus is therefore dead code at runtime. Harm is real but recoverable (a user who clicks the field can type; the challenger measured one Tab reaching it), so MAJOR not BLOCKER. Cure boundary: the fix is declarative entry-focus on the layer, NOT a second hand-poke that would re-race — the producer half (prefer an explicit target inside the incoming face) rides the standing BH/BI relay as an ASK, never a local patch."
     },
     {
      "id": "C-33 ≡ D2-04 — three 22x22 operable controls and an unnamed 160x23 input in the slug layer",
      "verdict": "CONFIRMED",
      "note": "Measured by me in the OPEN, non-inert state at 1440x900 after clicking Login: [Switch to slug 22x22], [Generate new slug 22x22], [Cancel 22x22], input 160x23 with ariaLabel null, id null, labels 0 — placeholder-only. That is a genuine WCAG 2.2 SC 2.5.8 (Target Size Minimum, 24x24 AA) failure on three controls, plus SC 3.3.2 and 4.1.2 on the input; the spacing exception does not save them (the controls sit ~1.5px apart, so no 24px undershoot circle is disjoint). This also settles r1's MT-F004 ruling: r1 measured the at-rest INERT state and called the row a measurement artifact — the challenger's correction is right and I verified the distinction myself (my at-rest 390/720 census shows these controls absent from the non-inert set; they appear only once the layer is activated). Mechanism: SlugEditLayer.vue:91-118 passes `compact` to DockControl (the producer's documented opt-out from the coarse-pointer touch floor) without re-supplying geometry through the --dock-compact-control-* token seam that the sibling ActionBarToggle.vue:154-158 already uses. CURE RULING between the two filings: D2-04's housing cure wins on canon — VISUAL-CONSTITUTION.md:222 is explicit that \"Account is one modal side Dialog opened from the Dock, not a route chassis or second main\" — but that composition is W23's subject, so this wave cures the NAME and the SIZE (a real accessible name; controls at >=24px through the producer's own token hook, root-level not per-instance) and FOLDS the housing to W23 with the row identity preserved and a runnable re-trigger."
     },
     {
      "id": "C-32 — the dock's live backdrop sampler has never once succeeded",
      "verdict": "CONFIRMED",
      "note": "Independently measured on 4/4 viewports in my own run: .glass-dock carries data-backdrop-sample-state=\"unavailable\", data-backdrop-sample-source=\"canvas\", data-backdrop-sample-reason=\"source-unavailable\"; --glass-backdrop-luma resolves to \"0\"; --glass-tint-strength resolves to clamp(4%, calc(4% + (20% - 4%) * max(0, (0 - 0.6)/(1 - 0.6))), 20%) — i.e. pinned at the 4% floor, so the producer's adaptive-legibility tint (AA 20%) can never engage. A MutationObserver over nav.dock-band for 4s idle recorded 33 attribute mutations, exactly 11 each of the three sample attributes and nothing else — an infinite failing retry writing to persistent chrome at ~2.75Hz per attribute (the challenger measured 14 each; scoped to my cell it is 11, and the difference is timing, not disagreement). Ownership split, stated so the glass boundary is not crossed: the DEMO-side half is ours and shippable — Dock.vue:132 passes no :background-canvas although App.vue:9-16 already owns the canvas and stamps it data-glass-field-canvas; the PRODUCER-side half (the resolver early-returning on a truthy getter so the documented data-attribute fallback is unreachable) is an ASK on the standing BH/BI relay, never a local shim. Sequencing note adopted from C-38: the gate must assert the sampler reaches \"sampled\", not merely that a canvas was threaded, or it trades source-unavailable for sample-unavailable."
     },
     {
      "id": "C-38 — the atmosphere field canvas has a 300x150 UA-default backing store on a webgl2 context",
      "verdict": "RESCOPED",
      "rescopedTo": "a sequencing precondition inside C-32's gate; the atmosphere-canvas question itself belongs to the scenes/atmosphere seat",
      "note": "The challenger filed this honestly as a labeled hypothesis about the atmosphere, not a finding about the dock, and I keep that framing. I did not re-measure the canvas backing store this pass. Its only load on this wave is real and is adopted: a WebGL2 surface without preserveDrawingBuffer reads back blank, so G-O must assert the terminal state data-backdrop-sample-state===\"sampled\" with a non-zero luma rather than asserting the prop was threaded. No row enters the wave; no defect is claimed."
     },
     {
      "id": "C-34 — the dock's layer machine is silent to assistive tech; production has no live region",
      "verdict": "CONFIRMED",
      "note": "Verified in two halves. Live: my probe's landing after opening the login layer is DIV.dock-face.is-active with tabIndex -1 and no accessible name — an unnamed, role-less div absorbs focus on the layer swap (the same landing the challenger measured three times out of three). Live-region census of nav.dock-band on all four viewports returns exactly one element: role=\"alert\" carrying \"dev misconfigured — run `npm run dev`\". Source: status-lamp.ts:46-49 `if (!isDev) return null; // dev-gated — the lamp ships dark in production`, so a PRODUCTION build's band contains ZERO live regions — that half is source-certain, not measured (I did not build). Consequence: the layer swap, the linkCopied flip, the login result and the view switch are all unannounced, against VISUAL-CONSTITUTION.md:83's requirement that state and associated error/status be explicit. This is the same root as C-31 and C-35: the dock's layer machine owns neither focus nor announcement and leaves both to the producer's default face-focus."
     },
     {
      "id": "D3-03 — no roving-focus rail (no ArrowLeft/Right, no Home/End, movement not separated from activation)",
      "verdict": "GLASS-OWNED",
      "note": "Measured by me: focus [aria-label=\"Select view\"], press ArrowRight → activeElement unchanged; press Home → activeElement unchanged. Dock.vue binds no keydown handler anywhere (I read all 359 lines). The canon citation is real — VISUAL-CONSTITUTION.md:129 names this component explicitly: \"horizontal Dock/rail roving focus | Right moves to the visual-right item ... Home=first semantic item, End=last; activation is separate from movement\". But rail behaviour is a property of GlassDock/DockLayerGroup, i.e. the design system, and the owner's glass-first edict forbids grafting a keydown handler into Dock.vue to simulate it — that would be exactly the demo-local re-derivation this formation keeps striking. Disposition: relay ask on the standing BH/BI fond plus BANK-D4 with a runnable re-trigger; NOT a value.js wave row, and the wave may not claim it. Note the tab order itself is sound in chromium (all four controls, visual order), so nothing is unreachable today."
     },
     {
      "id": "D2-12 — r1's 'the dock is keyboard-unreachable on Safari' BLOCKER is a harness false signal",
      "verdict": "CONFIRMED",
      "note": "I accept this correction and re-derived both halves. The harness documents the mechanism against itself: audit/visual/states.mjs:7-9 warns that \"keyboard rows are meaningless in WebKit alone — macOS ships Full Keyboard Access OFF, so run ENGINE=chromium before believing any focus gap\", and states.mjs:14 defaults ENGINE to webkit. My chromium cell finds all four dock controls in the sequential focus order in visual order. Per I-20 this is a statement about playwright-webkit ONLY and infers nothing about real Safari 26.4, which remains a separate evidence cell nobody has run for this question. The r1 BLOCKER is REFUTED as filed; its surviving substance is D3-03 (the missing rail), which is GLASS-OWNED. Recorded because a false BLOCKER left standing in a registry is worse than a missing MINOR."
     },
     {
      "id": "D2-06 — dark mode annihilates the dock accent (C 0.188 → 0.021) and --accent-view ≡ --accent-live on the default route",
      "verdict": "CONFIRMED",
      "note": "Reproduced to the digit in my own two-context run at 1440x900 on /#/: light --accent-view = oklch(0.471189 0.188448 9.83402); dark --accent-view = oklch(0.958322 0.0210531 9.83402) — chroma down 88.8%, L up to 0.958. And light --accent-live = oklch(47.118925176164% 0.188447570516 9.83402284231deg), i.e. bit-identical in L, C and h to --accent-view (viewSchema.ts gives picker accentHueShift 0), so the 'two dock voices' design is degenerate exactly on the route a first-time visitor meets. The consequence the filing draws is sound and is corroborated by the component's own record: DockViewSelect.vue:40-47 excised the per-row hue legend for carrying 'near-zero discriminative information' and then kept the identical mechanism on the trigger, ring and seal ('R1 SURVIVES'), so the survivor now carries strictly less information than the deleted one. MAJOR upheld. The cure direction (identity by glyph, not by hue) is already argued inside Dock.vue:255-268; the wave finishes that argument rather than inventing a new one."
     },
     {
      "id": "D2-16 — the open view menu has zero visual selected-state marking; options render in a display face",
      "verdict": "CONFIRMED",
      "note": "My own cell, view menu opened at 1440x900: all seven options report fontWeight \"400\", backgroundColor \"rgba(0, 0, 0, 0)\" and one <svg> child each (the view icon, not an indicator); only Home carries aria-selected \"true\". So selection is expressed to AT and to nobody else — not colour-only, which the canon at least contemplates, but NOTHING, which VISUAL-CONSTITUTION.md:83 forbids outright. Source: DockViewSelect.vue:97 and :127 pass hide-indicator on every SelectItem, stripping the producer's indicator gutter, while :99-109 records that the font-semibold marker was deliberately retired at P4-R4. Second arm also confirmed by measurement: computed fontFamily on every option is Fraunces, a display face, against §4's rule that controls and labels INCLUDING dropdown options use the text face non-bold. Cure is the ColorSpaceSelector precedent already ruled elsewhere in this registry — delete hide-indicator and consume the producer indicator, which is scheme-, monochrome- and forced-colors-safe by construction where no consumer-side marker can be."
     },
     {
      "id": "D2-03 ≡ D3-04 — three disagreeing responsive predicates; at 1024x1366 the dock renders the mobile and desktop grammars at once",
      "verdict": "CONFIRMED",
      "note": "Measured by me at 1024x1366 in a fresh context: app-layout[data-layout]=\"mobile\" (App.vue:311's aspect-aware `(min-width:1024px) and (min-aspect-ratio:1.1)`), .dock-mobile-panes display block with Picker/About tabs rendered at x398 w141, AND Login (83x28) and @mbabb (73x28) both present — with NO overflow Menu in the non-inert button set and no Tools. Predicate census, all read by me: Dock.vue:71 useMediaQuery(\"(min-width: 1024px)\") width-only; demo/styles/shell.css [data-layout] width AND aspect; ProfileSection.vue:48/:127 `hidden lg:flex` and MobileMenuDropdown.vue:38 `lg:hidden` width-only; PaneSegmentedControl.vue:46 `@media (max-width: 639px)`. The shell's own stylesheet states the stamp exists precisely so the witnesses \"can never disagree\" and warns that a media wrapper would re-introduce the width-only disagreement — Dock.vue:71 re-introduces exactly that predicate, and it drives mobileEditActive, :always-expanded, the SelectValue visibility and the Tools label. Severity: the BLOCKER framing is overstated (nothing is unreachable and the page functions), so RESCOPED to MAJOR while the finding itself stands. One injected layout truth, consumed everywhere, deletes the class."
     },
     {
      "id": "L-34 — the admin partition has a THIRD home and the three owners disagree 5 / 5 / 7",
      "verdict": "CONFIRMED",
      "note": "Verified by reading all three: demo/color-picker/router/index.ts:30-34 carries meta:{admin:true} on exactly the five /admin/* routes and NOT on /atmosphere or /blob; demo/shell/viewSchema.ts assigns accentHueShift 280 and 320 to atmosphere and blob (the primary 40-degree fan) and 0 to the five admin rows, i.e. it classifies the same five as admin; useDockAdminMode.ts:27 alone lists seven, including atmosphere and blob. The route table is additionally a sixth hand-maintained copy of the ViewId enumeration. INFO severity is right — nothing breaks today — but the row is load-bearing for the wave because it names an owner the r2 cure text did not: structure (b) must also derive the router's meta.admin from PaneConfig.group, or the fourth copy simply outlives the three it replaced. Folded into structure (b), id preserved, not separately booked."
     },
     {
      "id": "C-35 ≡ C-26 — ActionBarLayer's two template refs are write-only, ColorInput's exposed focus() has no caller, and a retired-API shim threads a parameter it voids",
      "verdict": "CONFIRMED",
      "note": "Read the whole file. ActionBarLayer.vue:28-29 declare colorInputRef and actionToolbarRef; :103 and :116 bind them; nothing anywhere reads either — `grep -n 'colorInputRef\\|actionToolbarRef'` returns exactly those four lines. ColorInput.vue:282-288 defineExposes focus:() => inputColorRef.value?.focus() and `grep -rn` finds no caller in the repository; ColorInput.vue:262 already does the right thing (rAF focus) for the propose-mode swap, so the actions→input swap is the one path that never got it — meaning 'Open color input' leaves focus on the toggle and typed text goes nowhere, the same family as C-31. C-26's half is likewise confirmed by reading: usePopupMutex.ts:1-2 declares itself a local fork of a composable glass-ui retired, with NO BH/BI relay note filed (contrast ActionBarLayer.vue:60-61, which does file one), and ActionBarLayer.vue:67 `void opts.containerEl; // signature parity with the retired producer composable` threads a parameter from a useTemplateRef (:83), through the call (:86), into a template binding (:101), and discards it. These fold into the r2 L-20 crossfade row rather than standing alone: DockCrossfade adoption deletes the shim, its voided parameter, both write-only refs and the hand-bound producer classes in one commit, and the entry-focus contract lands with it."
     },
     {
      "id": "D2-10 — a dock layer swap jolts the pill 81px sideways and shrinks it 162px, unanimated",
      "verdict": "CONFIRMED",
      "note": "My own measurement at 1440x900: main layer .glass-dock {x 484.3, w 471.5, h 62}; after clicking Login, {x 565.5, w 309.0, h 62} — Δx +81.2, Δw −162.5, height unchanged. Mechanism as filed and verified in source: Dock.vue:132 instantiates GlassDock with :fit-content=\"true\" while hosting four DockLayers of very different intrinsic widths as coincident grid siblings, so the dock's inline size is a property of whichever layer is active rather than of the dock. The producer's crossfade animates opacity only, so the box change is a step. Reads directly against VISUAL-CONSTITUTION §3 law 4 (the dock owns a reserved band whose states do not move the scene) — and note the band floor DOES hold (the challenger's own negative proof, which I did not re-run), so the injury is the pill's own jump, not a scene shift. MAJOR upheld; the cure is a dock-owned inline size (max-content over the coincident faces) or the producer's morph spring, not a per-layer width patch."
     },
     {
      "id": "D2-08 — three visible dividers for four visible seats",
      "verdict": "CONFIRMED",
      "note": "Measured: at 1440x900 the nav subtree carries 7 separators, 3 of them rendered and non-inert — a line between every adjacent pair of the four capsule seats, which is the maximum possible duplication (n−1). Sources verified: ProfileSection.vue:49 AND :123 place a separator on both sides of the auth block; ActionBarToggle.vue:82 folds one into the Tools slot. The mechanism named in the filing is the honest one: each dock sub-component ships its own DockSeparator as local furniture, so the count is a sum of independent decisions and no one owns the row. Against PROPORTION-AUDIT §5 law 4 (a divider is retained only when grouping would be ambiguous without it; spacing plus material already expressing the boundary makes the line duplicative). Severity rescoped MAJOR→MINOR: it is a real composition defect with a one-line cure and no functional or accessibility consequence. Separator ownership moves to the layer composition; a leaf cannot know whether its own boundary is ambiguous."
     },
     {
      "id": "D2-09 — five operable height rungs in one dock, and a source comment that states a false measurement",
      "verdict": "CONFIRMED",
      "note": "Measured at 1440x900 main layer: Select view 32, Toggle action bar 32, Login 28, @mbabb 28. Measured in other layers this pass: the three slug controls 22. The challenger's 40/44 rungs (Back, Save edit, Open color input, mobile Back) I did not re-measure and carry labeled. The comment under test is ActionBarToggle.vue:147-153, which asserts the T-36 padding makes \"the box land at the sibling controls' 2.5rem height\" — 2.5rem is 40px and the measured height is 32px, so the parenthetical is wrong by 8px even though the box does land at its siblings' actual height. Severity rescoped MAJOR→MINOR: the rung pile is a composition row, not a break, and the comment is a documentation defect — but a load-bearing one under this formation's laws, because a comment that states a measurement is a claim, and an untrue claim in source is how the next seat inherits a false premise (cf. L-10). Cure: one dock control rung declared once as a producer token and consumed by every seat; delete the assertion rather than correcting it."
     },
     {
      "id": "D2-13 — rendered text from three inactive dock layers is present in the nav's innerText, including a bare arrow",
      "verdict": "CONFIRMED",
      "note": "My own read at 1440x900: nav.dock-band innerText = \"→ | Home | Tools | Login | @mbabb | dev misconfigured — run `npm run dev`\" — the leading arrow is Dock.vue:137's bare <span>&rarr;</span> inside the NON-ACTIVE mobile-edit layer. At 390x844 the same leak reads \"→ | Picker | About\". Mechanism verified: the producer's crossfade gives inactive faces opacity 0 and pointer-events none, not visibility hidden or display none, and innerText excludes display:none but includes opacity:0 content. AT is protected (the faces are inert — the challenger's negative proof, which my inert-filtering census corroborates), so the harm is not to users: it is to every text-based probe, crawler, snapshot test and bodyTextLength metric this formation runs, which is exactly how a shell text metric moves while nothing visible changes. MINOR, and it pairs with the mobile-edit layer's deadness — the leaked arrow belongs to a layer that can never activate."
     },
     {
      "id": "D2-14 — a role=\"alert\" node is mounted in the nav band from first paint with an infinite pulse",
      "verdict": "CONFIRMED",
      "note": "Verified at source and live. status-lamp.ts:50-56 returns {variant:\"misconfigured\", role:\"alert\", label:\"dev misconfigured — run `npm run dev`\"}; DockStatusLamp.vue:13-21 renders it v-if=\"lamp\" with :role=\"lamp.role\"; my live-region census finds exactly that node present at rest on all four viewports. role=\"alert\" is an assertive live region for state that CHANGES, so a node present at first paint fires an interruption on every load. Two honesty caveats that lower the severity to INFO, both source-verified by me: the lamp is dev-gated (status-lamp.ts:46 `if (!isDev) return null`), so production ships nothing; and the pulse is wrapped in @media (prefers-reduced-motion: no-preference). The correct cure is role=\"status\" for a lamp that exists from first paint — the file's own §38 comment already says \"the a11y role IS the register\", which makes this a one-word fix consistent with its own doctrine."
     },
     {
      "id": "D2-11 — the dock's one layout-forcing animation is the one that fires on boot",
      "verdict": "CONFIRMED",
      "note": "Source-verified: ActionBarToggle.vue:120-124 transitions grid-template-columns (a layout-animated property) plus opacity on the presence slot, and the component's own header at :12-16 records that this slot 'visibly GREW the pill on EVERY load' before the .is-live gate was added. The gate suppresses the boot growth but not the layout work on genuine toggles. Second arm also true: everything else in the dock is transform/opacity only and rides --spring-snappy, so the component speaks two motion registers at once (duration/ease on the slot, spring on the seal and settle), against VISUAL-CONSTITUTION §6's one-producer-spring rule. MINOR. Cure is either scaleX+opacity on a fixed-basis box or no animation at all — a dock control appearing is not a spatial-continuity event."
     },
     {
      "id": "D2-15 — two template-ref idioms fourteen lines apart, and two computeds that only re-spell `?? null`",
      "verdict": "CONFIRMED",
      "note": "Read directly: Dock.vue:74 uses useTemplateRef<InstanceType<typeof GlassDock>>('dockRef') (the Vue 3.5 idiom the owner's edict 7 requires) while :60 uses ref<InstanceType<typeof SlugEditLayer>|null>(null) for the same job; :39-40 wrap two props that are already typed `| null | undefined` (:31) in computeds that add nothing but a name. MINOR, pure hygiene — but the filing's deeper observation is the one worth keeping: slugEditRef exists only so Dock can proxy two child methods (onStartSlugEdit at :61, onCopySlug at :62) from two different menus through a template ref, which is prop/event/ref triangulation for one action and the same anti-pattern the command-registry row kills at App scope. It dies with the login-surface commit rather than needing its own."
     },
     {
      "id": "D2-07 — RTL: the @mbabb wordmark bidi-reorders and the two directional affordances do not mirror",
      "verdict": "RESCOPED",
      "rescopedTo": "MINOR, and folded into the wave's hygiene commit as a logical-property pass; the rendered-frame arm is unwitnessed (DELTA-7)",
      "note": "The mechanism is certain without a probe. ProfileSection.vue:140 renders the literal '@mbabb' with no dir=\"ltr\" and no unicode-bidi isolation (I read the block); under UAX#9 a leading neutral in an RTL paragraph takes the paragraph direction, so it renders 'mbabb@'. The second arm is equally source-certain: ActionBarToggle.vue:103 draws ArrowRight and Dock.vue:154 ArrowLeft as PHYSICAL glyphs for a LOGICAL meaning (forward into the layer / back out), so the enter-exit motif inverts wholesale under dir=rtl. Canon citation verified: VISUAL-CONSTITUTION §6.1 requires LTR-isolated spans for handles and slugs and logical inline direction for chrome. Severity rescoped MAJOR→MINOR on one honest ground: the product ships no RTL locale, so the harm is latent and the row is canon compliance, not a live break. The rendered evidence (shots/rtl-desktop/picker.png) is untracked and unhashed, so I rest the verdict on source plus spec, not on the frame."
     },
     {
      "id": "D3-02 — the dock has two resting geometries for one idle page, selected by pointer history",
      "verdict": "RESCOPED",
      "rescopedTo": "a design decision needing one ruling plus the π frame the tranche has never captured; the collapsed-rest reachability arm is declared unmeasured",
      "note": "I reproduced the mechanism myself at 1440x900. After 9s with the pointer never entering the page: .glass-dock is `expanded`, 471x62. Reload, one pointer-in/pointer-out cycle, 7s: `collapsed`, 56x56, .dock-seal present. So the producer's collapse timer is armed by pointer interaction, not elapsed time (Dock.vue:132 :collapse-delay=\"5000\", :always-expanded=\"!isDesktop\" — mobile never collapses). This OVERTURNS a factual claim two prior rounds carried ('the wax seal has never been observed rendering') but overturns no ruling in the r2 apotheosis, which never adopted it — recorded in DELTA-3 exactly so the correction is not lost again. Why RESCOPED rather than CONFIRMED as a defect: a collapse-on-idle-after-interaction dock is a defensible producer idiom, and the seal is the most heavily ruled composition in this component's record (Dock.vue:229-268 carries three owner rulings). What is actionable and what the wave takes: one π obligation (the collapsed rest has never been committed as a frame in any tranche) and one open question I did NOT measure and will not assert — whether the collapsed 56px seal is keyboard-reachable and named. Declared, not assumed."
     },
     {
      "id": "D3-05 — every operable control in the mobile dock is below the 44px touch floor",
      "verdict": "RESCOPED",
      "rescopedTo": "a producer hit-seat ASK on the standing BH/BI relay + a MINOR canon row; NOT a WCAG failure",
      "note": "Measurements confirmed and slightly different from the filing's. Mine at 390x844 at rest: Select view 33 high, Toggle action bar 33 (invisible/clipped), Picker 26, About 26, Menu 33 — the filing's 26–33 band is right. But the verdict's framing must be scoped: WCAG 2.2 SC 2.5.8 sets the AA floor at 24x24 CSS px, which every one of these clears; 44px is PROPORTION-AUDIT canon and Apple HIG, i.e. a house standard, not a conformance failure. The genuinely sub-24px controls in this component are the slug layer's three 22x22 buttons, and those are booked under C-33 where they belong. The 26px rung's source is verified (PaneSegmentedControl.vue:46-51's max-width:639px block), and it dies with W47's retirement. The residual cure is correctly the producer's: a hit-seat token on the compact register (target at the floor, optics unchanged), because a per-site padding bump is exactly what PR-12 forbids. Ask, bank, no local patch."
     },
     {
      "id": "D3-06 — the mobile overflow menu is composed from a demo-local primitive, with a wrapped row and see-through material",
      "verdict": "RESCOPED",
      "rescopedTo": "boundary half CONFIRMED into this wave's menu commit; the two visual halves UNVERIFIABLE-HERE this pass",
      "note": "The design-system-boundary half is source-certain and I verified it: MobileMenuDropdown.vue:7-11 imports DropdownMenu/DropdownMenuContent/DropdownMenuItem/DropdownMenuSeparator/DropdownMenuLabel from ../../../ui/dropdown-menu — a demo-local primitive — while importing DockTrigger from the producer on the very next line. Under the owner's glass-first edict the row layout, the control type family and the material tier should resolve from ONE producer decision, and the filing is right that all three are re-derived locally. The two VISUAL claims (the GitHub row wrapping its glyph above its label at 55px against 44px siblings; the picker's numerals legible through the option copy) rest entirely on state-mobilemenu.png, which `git ls-files` shows is untracked and for which no sha256 is recorded anywhere in the committed record — per L-7 that witness does not exist, and I did not re-capture it. So those halves are UNVERIFIABLE-HERE and may not be cited by any close until the frame is committed with its hash (gate G-Q)."
     },
     {
      "id": "D3-07 — opening the action bar deletes the dock's navigation identity",
      "verdict": "CONFIRMED",
      "note": "True by construction and I verified it in the template rather than by probe: DockLayerGroup swaps whole layers, and the action-bar layer (Dock.vue:153-158) contains a Back control, a separator and the ActionBarLayer/GenericActionBar contents — Select view, Login and @mbabb live only in the main layer (:161-226) and are therefore gone while Tools is open. Canon citation verified: VISUAL-CONSTITUTION.md:178 — \"The dock is its own top band, fully visible, focusable... Direct route changes keep its full navigation identity.\" The challenger's own geometry (471→326px, re-centred) is consistent with my measured 471.5→309 for the slug layer, i.e. the same fit-content mechanism as D2-10. ONE CLAUSE STRUCK: \"the only exit is one unlabelled ←\" is false — Dock.vue:154 carries aria-label=\"Back\"; it is visually unlabelled, which is a different and lesser claim. Severity rescoped MAJOR→MINOR-with-a-design-question: the cure (render the action bar in the scene's action region and keep the four navigation seats always) is a composition decision at chassis altitude, so the wave carries the row and its gate but the region move folds to the chassis owner if it exceeds one commit."
     },
     {
      "id": "D3-09 — one producer package reached by two import paths on consecutive lines, and the indirect path closes a module cycle",
      "verdict": "CONFIRMED",
      "note": "Read both files: Dock.vue:4 imports GlassDock, DockLayerGroup, DockLayer from \"./\" while Dock.vue:5 imports DockControl, DockSeparator from \"@mkbabb/glass-ui/dock\"; demo/shell/dock/index.ts (5 lines total) re-exports the first three from the same producer subpath and also exports Dock.vue itself — so index.ts → Dock.vue → index.ts is a genuine runtime cycle, for five symbols that all originate in one package. An independent seat's module walker independently reported it as the tree's ONLY runtime cycle across 250 demo files (carried labeled; I did not re-run the walker). MINOR, one-line cure: import all five from the producer and let the barrel export only the app-specific Dock. Worth keeping because a barrel that both re-exports a producer and exports its own consumer is the structural shape the whole formation keeps finding under different names."
     },
     {
      "id": "D3-10 — Dock.vue is a two-root SFC whose second root is band chrome",
      "verdict": "CONFIRMED",
      "note": "Read the template: Dock.vue:128 opens the dock wrapper and closes at :285, then :293 mounts <DockStatusLamp /> as a SIBLING root — a multi-root SFC, which disables attribute fallthrough. The lamp is dev-gated (status-lamp.ts:46) and renders live in my cells at 1440 (present in navInnerText) and at 390 (present in the live-region census). Its own comment (Dock.vue:287-292) states that the .dock-band is its positioning context — i.e. the file itself documents that the lamp belongs to the band, not to the dock, and then mounts it from the dock anyway. MINOR; the cure is to mount it from App.vue's <nav class=\"dock-band\"> and let Dock.vue return one root, at which point the lamp's mobile clipping becomes the band's problem, solved once, at the right altitude. I did not re-verify the clipped-at-390 rendering claim."
     },
     {
      "id": "D3-11 — forced-colors is a false green because WebKit cannot emulate it",
      "verdict": "REFUTED",
      "note": "Falsified by my own two-engine cell, ten seconds of work: Playwright 1.60 launched with forcedColors:\"active\" yields matchMedia(\"(forced-colors: active)\").matches === TRUE in BOTH webkit and chromium (and (forced-colors: none) false in both). So the media query fired, and the app's own forced-colors CSS — foundation.css:665-678's two-tier U-F57 policy and GradientStopEditor.vue:353 — DID apply during the capture. The row's mechanism (\"WebKit does not emulate forced-colors\", \"a green row for an unrun test\") is false, and with it the proposed cure (re-run under chromium) is unnecessary. STRUCK AND UNCITABLE. The narrower residual, recorded rather than booked: emulating the query is not the same as a UA forcing a system palette, so a capture evidences the app's own forced-colors rules and nothing about UA colour forcing — any future claim must say which of the two it means. This row is the delta round's clearest reminder that a plausible mechanism written next to a real screenshot is still an accusation, not a finding."
     },
     {
      "id": "D3-08 ≡ D2-05 — MT-F005's single desktop-only nameless button, enumerated",
      "verdict": "CONFIRMED",
      "rescopedTo": "MT-COLORINPUT-1 (D-3 ≡ MT-F005 ≡ r2 L-7) — already built there; NOT re-booked",
      "note": "My census confirms the cell precisely: inside nav.dock-band the nameless-button count is exactly 1 at 1440x900 — {cls:\"send-btn btn-interactive\", 24x24, visibility:\"hidden\"} — and 0 at 390, 720 and 1024x1366. Source: ColorInput.vue:69 and :78, two <button class=\"send-btn btn-interactive\"> branches, neither carrying aria-label, text or title. Both filings' route-pattern analysis (nameless=1 exactly on the VIEW_MAP entries whose left pane is color-picker) follows from the same fact and needs no separate booking. The r2 apotheosis already FOLDED this identity to MT-COLORINPUT-1 with gates G5 and G11(a); this is now its fifth and sixth independent confirmation. Corroboration accretes; it does not re-book. Any close claiming THIS wave cured the nameless button would be false. Note also D2-05's honest correction to the 'zero on mobile' half: on mobile the colour-input face is never provided at all, so the button is not hidden — it is never rendered, which is the command-registry row, not this one."
     },
     {
      "id": "L-23 (replicated blind) — the demo's import-boundary lint guards match zero files",
      "verdict": "CONFIRMED",
      "rescopedTo": "W-HYGIENE for the full re-point; ONLY the shell→palettes guard enters this wave (as structure, per L-8)",
      "note": "Verified by running it: `ls -d demo/@` → No such file or directory, and eslint.config.js scopes its demo no-restricted-imports objects to demo/@/composables/**, demo/@/components/**, demo/@/lib/** (three dead globs) plus demo/color-picker/**, which matches exactly two files (App.vue, ErrorBoundary.vue), neither importing palette-browser. The banned patterns themselves reference @components/custom/... aliases that no longer resolve. So G-DEMO-1, G-DEMO-3a and G-DEMO-3b have enforced nothing since the W43 tree move — rules that read as enforcement and enforce nothing, which is the exact disease MT-F022 and the vacuous-gate census name. BLOCKER severity is over-scaled for a lint config (nothing is broken at runtime), so the row is rescoped in severity while fully confirmed in fact. Its load on THIS wave is real and adopted: the honest way to keep the r2 L-6 cure from re-inverting is a physical-tree layer guard banning demo/shell/** from importing demo/palettes/**, which makes the defect unrepresentable rather than gated (L-8). That one guard lands here; the other six globs travel to W-HYGIENE with the id preserved."
     },
     {
      "id": "L-36 — two dock files (and 16 others) import the 200-symbol root barrel for helpers published on a 12-symbol subpath",
      "verdict": "CONFIRMED",
      "rescopedTo": "W-HYGIENE (mechanical 18-specifier rewrite); the two dock sites ride along",
      "note": "Counted myself, scoped to source: `grep -rn 'from \"@mkbabb/glass-ui\"' demo/ --include='*.vue' --include='*.ts'` = 37 hits, of which 18 sit outside demo/ui/ — and I enumerated all 18: they are writeClipboard (x12), useClipboard (x3) and useTouchGate (x3), every one of them a ./dom export. The two dock sites are ColorInput.vue:117 and layers/SlugEditLayer.vue:6, sitting in a subtree that otherwise uses /dock (x5), /watercolor-dot, /dark and /tabs — so the inconsistency is internal to the same files' neighbours. The filing's own severity bound is correct and I keep it: glass-ui declares sideEffects [\"*.css\"], so this is a module-graph and dev-transform cost plus a public-surface-contract claim, NOT a shipped-bytes claim, and no close may frame it as one. Mechanical, repo-wide, and not a dock defect — it goes to W-HYGIENE whole rather than being half-fixed here."
     },
     {
      "id": "L-37 — the published library's vitest root contains the owner-retired grep-over-source idiom, reaching into three demo SFCs",
      "verdict": "CONFIRMED",
      "rescopedTo": "W-HYGIENE / test-homes; carried here only as a hazard note on the dock's own files",
      "note": "Verified: vitest.config.ts includes test/** and demo/test/**; `grep -rln readFileSync test/` returns exactly one file, test/picker-blob-config.test.ts, whose first 20 lines readFileSync three SFCs — demo/picker/visual/HeroBlob.vue, demo/picker/ColorPicker.vue and demo/shell/dock/ActionToolbar.vue — via path.resolve(process.cwd(), ...). This is the idiom the owner deleted on 2026-06-02 as 'overfit junk' (project memory, feedback-proof-idiom-retired), re-authored inside the LIBRARY suite, asserting on DEMO presentation, with cwd-relative paths that ENOENT if vitest runs from anywhere but the repo root. Its bearing on this wave is direct and worth stating: one of the three files it greps is a dock file, so a dock refactor can redden the library suite for reasons that have nothing to do with the library. That is a hazard note for execution, not a row this wave cures."
     },
     {
      "id": "L-38 — a passthrough wrapper whose only substance is a :deep() into a producer-internal class, and a normative DESIGN.md rule that is false by 14",
      "verdict": "CONFIRMED",
      "rescopedTo": "the wrapper's :deep() dies with W47's PaneSegmentedControl retirement; the DESIGN.md re-derivation → W-HYGIENE",
      "note": "Both halves verified, and the challenger's census beat my first one — worth recording as a method note. My crude grep counted 15 across 6 files including PaletteCard.vue; reading the hits showed PaletteCard's two are BOTH comments (:66 and :347, the latter documenting the survivor's REMOVAL), so the live count is 14 across 5 SFCs and PaletteCard is absent, exactly as filed. DESIGN.md:381 states PaletteCard's `.featured-badge :deep(svg)` is 'the post-D.W4 Lane A survivor... no further :deep() reaches into reka-ui markup' — false on both clauses. The dock-adjacent instance is PaneSegmentedControl.vue:46-51, whose @media (max-width:639px) block pierces the producer's .segmented-tab under a comment declaring the variant is applied at the ROOT and 'never per-instance'. That contradiction dies with W47's 1→0; the catalog statement must be re-derived from a measurement rather than a remembered census (L-10)."
     },
     {
      "id": "L-39 — the only written record of the exposure-not-provision decision names a component that no longer exists",
      "verdict": "CONFIRMED",
      "note": "Verified: ColorPicker.vue:311 reads '// --- Action bar context for TopDock (exposed, not provided — TopDock is a sibling) ---' and `grep -rn TopDock demo/` returns comment hits only; no TopDock file exists — the component has been demo/shell/dock/Dock.vue since the March restructure. INFO severity, but the row is precisely aimed: this comment is the ONLY place the architectural decision that causes the command-registry defect is written down, so a reader who greps for the rationale lands on a name with no file. It is rewritten in the same commit that lands the registry, stating the new contract (panes register commands; the dock injects a registry) — not deleted, because the decision's history is what stops it being re-made."
     },
     {
      "id": "L-32 — the demo's tests have two homes and the library's vitest root reaches into demo/",
      "verdict": "CONFIRMED",
      "rescopedTo": "W-HYGIENE / test-homes (same identity as L-37)",
      "note": "Counted: `ls test/*.ts | wc -l` = 19 and `grep -rln '\\.\\./demo/' test/ | wc -l` = 10, while demo/test/ already exists with three files (export/byte-exact, glass/aurora-bracket, glass/aurora-motion). One of the ten is test/status-lamp.test.ts, which tests the demo shell module demo/shell/dock/status-lamp.ts from the library root. The structural irony the filing names is real: eslint.config.js:195-218 enforces inv-K-1 (src/ never imports glass-ui) for the library program, while the library's own test root imports the demo, which imports glass-ui — so the boundary the lint protects in one program is crossed in the other. MINOR, out of this component's locus, mechanical to fix (move ten files, add a vitest project entry)."
     },
     {
      "id": "L-33 — palette export has two homes (a flat module and a 12-module directory)",
      "verdict": "RESCOPED",
      "rescopedTo": "the palettes seat; cited here only as evidence that the dual-home mechanism is systemic",
      "note": "Verified by listing: demo/palettes/export.ts (132 lines: exportAsJSON / ExportResult / slugify) coexists with demo/palettes/export/ (bytes, canonical, css, digest, json, png, reload, rfc8785, serializers, svg, tailwind, types) plus demo/palettes/usePaletteExport.ts (27 lines). The seat filed it explicitly out of its own subject, which is the correct discipline, and its purpose here is argumentative rather than accusatory: it is the fifth instance of the same dual-home shape this corpus found (the menu, the action bar, the view registry, the slug bar, the export), which is the strongest available argument that the cure is a physical-tree layer guard (L-23) rather than five per-case fixes. No dock locus; nothing enters this wave."
     },
     {
      "id": "C-27 — six uncancelled rAF/timer handles across the dock subtree, one directory from the correct idiom",
      "verdict": "CONFIRMED",
      "note": "Enumerated myself with one grep over demo/shell/dock/ and demo/shell/: Dock.vue:105 (rAF, fires on every view change), ActionBarToggle.vue:60-61 (double rAF), ColorInput.vue:256 (setTimeout 300ms) and :262 (rAF), ActionButton.vue:95 (setTimeout 400ms), layers/ActionBarLayer.vue:76 (setTimeout cleared on re-entry at :75 but not on unmount) — none storing a cancellable handle for teardown. The contrast the filing draws is exact and verified: demo/shell/PaneSlot.vue:95 stores its rAF handle and cancels it, and demo/shell/dock/composables/usePopupMutex.ts:82 does onUnmounted(clearSwapTimer). So the idiom is known in this very area and simply not applied in the SFCs. Severity MINOR and honestly bounded by the filing itself: a post-unmount ref write is inert in Vue, so there is no user-visible harm today — this is the constellation-wide PRM-RAF pattern recorded so it does not become one. Cure is one small owned composable, not additions to a god module."
     },
     {
      "id": "C-25 — the dock's keep-open watches are not immediate, so a mount-time predicate is never applied",
      "verdict": "CONFIRMED",
      "note": "Verified at source: Dock.vue:87 watch(shouldKeepOpen, ...) and :89 watch(anyEditActive, ...), neither with {immediate:true}, while the comment at :78-85 asserts the one-predicate collapse is 'behaviour-equivalent (held ⇔ any flag true)' — true only over edges observed AFTER mount, which the code does not say. The unpaired release survives only because the producer clamps its ref-count at zero. I accept the filing's own honest scoping: there is NO reachable arm today, because the App shell cannot produce editTarget non-null at Dock mount (and the command-registry row proves it cannot produce it at all on mobile). So this is a latent structural asymmetry plus a comment that overstates an equivalence — the same class as D2-09's false measurement. Fix is two words plus deriving the predicate; it rides the wave's hygiene commit."
     },
     {
      "id": "C-28 — ActionBarToggle's slotLive early return makes the watch's re-arm dead code",
      "verdict": "CONFIRMED",
      "note": "Verified at source: ActionBarToggle.vue:59 `if (slotLive.value) return;` short-circuits after the first visible===true, while :56 clears settled on every false edge — so on a later false→true edge the watch never restores settled, and the only restorer is the @transitionend.self handler at :69-72. The state machine's stated owner is not its actual owner. The filing's own measurement is NEGATIVE at HEAD (the transitionend path restores is-settled on every re-entry, which it verified across four hash navigations) and I accept that; I did not re-run it. So this is INFO/latent, filed correctly: the day the grid-template-columns transition becomes a no-op (equal endpoints, transition:none, a content-visibility skip) the clip pins on and the o15-dock-register oracle starts failing far from ActionBarToggle.vue:57. Cure is to drop the early return so the watch owns both edges and transitionend becomes confirmation."
     },
     {
      "id": "C-36 ≡ C-29 — nothing in the repository ever opens the slug-edit layer, and the e2e suite asserts supplied names rather than rendered ones",
      "verdict": "CONFIRMED",
      "note": "I ran the census myself: `grep -rn 'SlugEditLayer|Switch to slug|Generate new slug|enter slug or token' e2e/ test/ demo/test/` returns ZERO hits, and `grep -rln 'useDockAdminMode|usePopupMutex|shell/dock' test/ demo/test/` returns only status-lamp.test.ts and picker-blob-config.test.ts, neither of which constructs Dock.vue. So the entire login surface — C-31's dead focus, C-33's four sub-24px targets, L-30's triple-swallowed error — sits behind a documented gate exclusion, and the oracle that owns the dock asserts a SOURCE property by grep instead of rendering the surface. C-29's companion claim (deleting DockViewSelect.vue:87's <SelectValue/> keeps the whole suite green, because every locator resolves the control by its explicitly-supplied aria-label and openView() can only click options already in the list) I did NOT re-run as a mutation; its locator inventory is consistent with what I read, and the C-3-WIDENED blank label is direct evidence that the suite cannot see a missing rendered name. Labeled accordingly: the mutation arm is carried, not re-derived. Both rows collapse to one obligation — the wave's specs must RENDER the surfaces they assert."
     },
     {
      "id": "C-30 — this corpus's own report banner advertises eight findings the committed file does not contain",
      "verdict": "CONFIRMED",
      "rescopedTo": "the registry owner (the root), not a code row",
      "note": "Verified with the two stated greps against audit/components/shell-dock-dock/challenge-C-implementation.md: the strings C-15..C-22 appear only in the banner at :5 and in a dangling forward reference at :561 ('OVERTURNED BY PASS 2 — see §6 · C-19'), and there is no ## 6 heading anywhere in the file. So a downstream fold trusting the banner's tally would count eight findings nobody can read, including a claimed BLOCKER and a claimed overturn of §3.4. The pass-3 seat handled this correctly — it filed the gap rather than inventing or renumbering another seat's findings, and recorded its own reasoning at :1027-1060. Disposition: this is registry integrity, not a defect in the dock; it goes to the root with the id preserved, and no close may cite C-15..C-22 or the §3.4 overturn until the body is recovered or the banner corrected."
     },
     {
      "id": "C-37 — six suspicions measured and killed (negative proofs)",
      "verdict": "CONFIRMED",
      "note": "Not a defect and not treated as one — recorded because negative proofs are the scarcest artifact in this formation and re-spending the probes is pure waste. The seat measured and killed: (1) the R1 parseCssColor crash class does not reach the dock (8 malformed ?color= values, fresh context each, zero page errors, dock and main present every row); (2) tearing down the action-bar layer while ACTIVE recovers to main with no errors and restores on return; (3) zero getImageData calls across 10 discrete colour changes, so the useContrastSafeColor ink probe is properly cached; (4) DockControl really does forward type, so SlugEditLayer.vue:93's type=\"submit\" is a real submit; (5) ActionBarToggle's hand-rolled .is-active does hit a real producer rule; (6) App.vue:32-33's subtree-wide transitionend/animationend capture handlers are guarded so the dock-settle beat cannot re-trigger boot work. I re-derived none of these and adopt them all as CARRIED-LABELED negative evidence, kept as the regression floor — (1) and (2) in particular become the parser band's landing checks."
     },
     {
      "id": "L-6 (replicated blind) — identity/session lives inside the palettes feature and the shell imports it on every route",
      "verdict": "CONFIRMED",
      "rescopedTo": "the r2 L-6 booking (unchanged); this is its fourth independent confirmation, NOT a re-booking",
      "note": "Re-measured with the r2 gate command verbatim: `grep -rnE \"from ['\\\"](\\.\\./)+palettes/\" demo/shell/ | grep -v 'import type' | wc -l` = 5, the same five files (Dock.vue:18, DockViewSelect.vue:8, layers/SlugEditLayer.vue:5, menus/MobileMenuDropdown.vue:13, menus/ProfileSection.vue:14). The replication adds one sharpening worth keeping: Dock.vue:44 consumes exactly ONE member of that port, pm.isAdminAuthenticated, whose true origin is demo/platform/auth/useAdminAuth.ts (wired at usePalettePorts.ts:57) — so the shell pulls a feature graph to learn one boolean whose home is already the platform. The r2 cure-detail ruling stands unchanged and is re-verified: the sessionPort literal at usePalettePorts.ts:127-135 includes onRegenerateSlug and onSlugSwitch, which are slug-migration ACTIONS the dock actually calls, so the platform module owns the KEY and the TYPE while the composition root injects the implementation. Corroboration accretes; the booking and id are untouched."
     }
    ],
    "waveSpec": {
     "waveId": "MT-DOCK-LAYERS-1",
     "title": "The dock tells the truth — one command surface (no instance refs), a navigation that names itself, a login that reports its outcome, one layout predicate, a chrome referent that reads the surface that paints, and the crossfade the producer already ships",
     "bornRed": true,
     "scope": "UNIFIED, one name (L-5 anti-rename): the standing MT-DOCK-LAYERS-1 minted at App.md:146, scope extended once by the r2 Dock corpus and once here by the r3 delta. Exact files — demo/shell/dock/Dock.vue · demo/shell/dock/DockViewSelect.vue · demo/shell/dock/ActionBarToggle.vue · demo/shell/dock/layers/ActionBarLayer.vue · demo/shell/dock/layers/SlugEditLayer.vue · demo/shell/dock/menus/ProfileSection.vue · demo/shell/dock/menus/MobileMenuDropdown.vue · demo/shell/dock/composables/useDockAdminMode.ts · demo/shell/dock/index.ts · NEW demo/shell/dock/commands.ts · demo/shell/viewSchema.ts · demo/shell/usePaneRouter.ts · demo/color-picker/App.vue (the ref-channel deletions only) · demo/color-picker/composables/usePaletteWiring.ts · demo/color-picker/router/index.ts (meta.admin derivation) · demo/color-session/useContrastSafeColor.ts (:148-221) · demo/color-session/ink.ts (:118-122) · demo/palettes/useSlugMigration.ts · demo/palettes/usePalettePorts.ts · DELETE demo/palettes/browser/slug/PaletteSlugBar.vue + its two barrel lines · NEW demo/platform/auth/session.ts · NEW test/dock-admin-mode.test.ts + demo/test/dock/*.spec — plus eslint.config.js for ONE guard only. READ-ONLY trees untouched. FIVE SEQUENCED COMMITS, each closeable: (1) the command registry + the ref-channel deletion; (2) the navigation partition (group on PaneConfig, router derivation, union-with-current, selected marking); (3) the login surface (typed outcome, rendered error, name + target size, dead-twin deletion); (4) the chrome/crossfade/session-port trio carried verbatim from r2; (5) hygiene (one layout predicate, separators, rungs, roots, cycle, leaked text, lamp role, stale comment).",
     "structure": "Six defect CLASSES are made unrepresentable; gates exist only where structure cannot reach (L-8). (a) PRESENT-AND-INERT DIES BY REGISTRY: NEW demo/shell/dock/commands.ts owns a DockCommandRegistry keyed by ViewId; panes provide() their command set on mount and the dock injects it. `paneRefs`, colorPickerRef/generatePaneRef/gradientPaneRef/mixPaneRef, onDesktopLeftMount/onDesktopRightMount, whenColorPickerReady's 40x50ms poll and every `?.()` on a handler are DELETED; a view that provides no commands renders no Tools trigger, so 'an action can be absent yet rendered' becomes unspellable. editTarget moves onto the already-declared EDIT_TARGET_KEY cell (color-session/keys.ts:15, provided at App.vue:270, injected at useSwatchActions.ts:23), so onStartEdit is a plain write on injected state and the mobile-edit layer stops being dead. (b) THE LATCH AND THE ANONYMOUS NAV DIE BY DERIVATION: PaneConfig (viewSchema.ts:70) gains a REQUIRED group:'user'|'admin' (compiler-RED the instant it is added — all fourteen rows omit it today); the router derives meta.admin from it (killing the 5/5/7 disagreement L-34 measured); useDockAdminMode reduces to a computed over (currentView, auth) and viewEntries = VIEW_MAP filtered by group UNIONED UNCONDITIONALLY WITH THE CURRENT VIEW — the union is the DELTA-2 amendment without which the blank trigger survives the cure. The ref at :32, the maintaining watch at :55-59, the logout watch and BOTH hardcoded ViewId arrays die. (c) THE SILENT FAILURE DIES BY TYPE: onSlugSwitch returns Promise<Result<void, ApiProblem>> instead of dispatching onto a component instance; SlugEditLayer awaits it and renders the outcome in a role=status region; slugBarRef, setError and PaletteSlugBar.vue (243 lines, mounted by nothing) cease to exist as concepts. Awaiting makes it observable, returning kills the instance-ref channel, rendering kills the dead state. (d) THE PREDICATE PILE DIES BY ONE OWNER: one injected useLayoutMode() derived from the [data-layout] stamp (width AND aspect); Dock.vue:71's width-only useMediaQuery and every lg:/sm: display utility in the dock subtree are deleted. (e) THE SILENT-FALLBACK CLASS DIES BY TYPE (r2, unchanged): resolveSurfaceLightness requires a tint for surface:'chrome'; the DOM read collapses to one paintedTintOf(root) paint-order walk pinned at nav .glass-dock. (f) THE DEPENDENCY INVERSION DIES BY MODULE BOUNDARY (r2, unchanged) plus ONE eslint guard re-pointed at the physical tree banning demo/shell/** from importing demo/palettes/** — structure, so the cure cannot silently re-invert. The crossfade is not structure but SUBSTITUTION: the ~40-line local useLayerTransition shim, its voided containerEl, its 260ms timer and its hand-bound producer classes are deleted for <DockCrossfade :active reserve=\"inline\">, which is installed today.",
     "gates": [
      {
       "gate": "G-A (carried VERBATIM from App.md:146 — the wave's original gate)",
       "command": "test \"$(grep -c 'void opts\\.' demo/shell/dock/layers/ActionBarLayer.vue)\" = 0",
       "redToday": true,
       "whatWouldMakeItFail": "ActionBarLayer.vue:67 `void opts.containerEl; // signature parity with the retired producer composable` — I re-ran the command at HEAD 66f3ca89: output 1, gate RED."
      },
      {
       "gate": "G-B (crossfade — structure + rendered motion; carried from r2)",
       "command": "test \"$(grep -c '<DockCrossfade' demo/shell/dock/layers/ActionBarLayer.vue)\" -ge 1 && test \"$(grep -cE 'useLayerTransition|SUB_LAYER_CROSSFADE_MS' demo/shell/dock/layers/ActionBarLayer.vue)\" = 0 && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/sublayer-crossfade.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Re-run today: 0 DockCrossfade uses, 5 shim hits. Driving [aria-label=\"Toggle action bar\"] then [aria-label=\"Open color input\"] and sampling both faces at +60ms yields transitionProperty `visibility` at 0s — layers.css transitions ONLY visibility, so is-leaving's opacity:0 lands in one frame and the 260ms timer times a window in which nothing animates."
      },
      {
       "gate": "G-C (partition — structure; carried from r2, AMENDED per DELTA-2 to require the union)",
       "command": "test \"$(grep -c 'const isAdminMode = ref(' demo/shell/dock/composables/useDockAdminMode.ts)\" = 0 && test \"$(grep -cE 'const (userViews|adminViews)' demo/shell/dock/composables/useDockAdminMode.ts)\" = 0 && npx vue-tsc -p tsconfig.demo.json --noEmit && npx vitest run test/dock-admin-mode.test.ts",
       "redToday": true,
       "whatWouldMakeItFail": "Re-run today: the seed ref at :32 → 1, the two hardcoded ViewId arrays at :26-27 → 2. The in-wave vitest spec is INVERTED from the latch (switchView('atmosphere') then switchView('picker') ⇒ isAdminMode === false) and additionally asserts viewEntries CONTAINS currentView for all fourteen ids — both fail today because :55-59 has no falsifying arm and userViews omits 7 of 14. vue-tsc goes RED the instant `group` is required while any VIEW_MAP row omits it (today: all fourteen)."
      },
      {
       "gate": "G-D (latch — product; carried from r2)",
       "command": "node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/admin-latch.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "My cell today, one fresh context per route: gold-shimmer count in nav = 1 on cold /#/blob, /#/atmosphere and /#/admin/users; the gate additionally drives /#/ → /#/atmosphere → back() → /#/ and asserts gold ABSENT on every non-admin step, which the r2 five-step trace [false,true,TRUE,TRUE,TRUE] fails. Membership must be PRESERVED (cold deep link to an admin view still golds); only the latch dies."
      },
      {
       "gate": "G-E (chrome — structure; carried from r2)",
       "command": "test \"$(grep -c 'case \\\"chrome\\\"' demo/color-session/ink.ts)\" = 0 && test \"$(grep -c 'querySelector<HTMLElement>(\\\".glass-dock\\\")' demo/color-session/useContrastSafeColor.ts)\" = 0 && npx vue-tsc -p tsconfig.demo.json --noEmit",
       "redToday": true,
       "whatWouldMakeItFail": "Re-run today: ink.ts's chrome fallthrough → 1; the .glass-dock query literal in useContrastSafeColor.ts → 1. tsc goes RED once the tint parameter is required while any call site omits it."
      },
      {
       "gate": "G-F (chrome — product; carried from r2, BOTH schemes)",
       "command": "node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/chrome-tint.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "My own two-scheme cell today: getComputedStyle(document.querySelector('.glass-dock')).backgroundColor = rgba(0, 0, 0, 0) in BOTH light and dark, while .dock-plate paints color(srgb 0.931227 0.845921 0.816039 / 0.5392) light and color(srgb 0.390298 0.334077 0.29024 / 0.5776) dark. resolveLiveTint('chrome') is therefore undefined on every call and the static ladder serves silently. A one-scheme witness reads half the defect as absent — the referent error inverts sign between schemes."
      },
      {
       "gate": "G-G (shell edge — source + graph; carried from r2, now with the lint guard)",
       "command": "test \"$(grep -rnE \"from ['\\\"](\\.\\./)+palettes/\" demo/shell/ | grep -v 'import type' | wc -l | tr -d ' ')\" = 0 && npx eslint demo/shell --max-warnings=0 && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/dock-graph.mjs demo/shell/dock/Dock.vue",
       "redToday": true,
       "whatWouldMakeItFail": "Re-run today: 5 value-import files (Dock.vue:18, DockViewSelect.vue:8, SlugEditLayer.vue:5, MobileMenuDropdown.vue:13, ProfileSection.vue:14). The eslint arm is RED-by-absence today — `ls -d demo/@` errors, so the three demo boundary globs match zero files and the new shell→palettes guard does not exist yet. The walker excludes `import type` as erased and states honestly that it measures OWNERSHIP and mountability, never bytes."
      },
      {
       "gate": "G-H (pills — MT-F026's own gate, verbatim, not re-minted; owner-marked, not re-litigated)",
       "command": "node docs/tranches/V/megatranche/audit/probes/owner-marked-shadows.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Login and @mbabb each cast 2 non-inset box-shadow layers over a plate that casts none (owner mark OM-1, on disk at audit/visual/owner-marked/OM-1-dock-item-shadows.png). GREEN only through producer primitives (DockControl / DockTrigger for=\"dropdown\" — the names corrected at r2's R-2; DockIconButton/DockSelectTrigger/DockDropdownTrigger do not exist in glass-ui 7.0.0). NO demo box-shadow override may satisfy this gate (masking-fallback ban)."
      },
      {
       "gate": "G-J (command registry — structure + product; SUPERSEDES and DISCHARGES r2's G-I)",
       "command": "test \"$(grep -cE 'paneRefs|colorPickerRef|generatePaneRef|gradientPaneRef|mixPaneRef|onDesktopLeftMount|onDesktopRightMount|whenColorPickerReady' demo/color-picker/App.vue demo/shell/usePaneRouter.ts demo/color-picker/composables/usePaletteWiring.ts)\" = 0 && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/dock-commands.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Static arm RED today: nine `paneRefs.<view>.value?.<cmd>?.()` handlers at usePaneRouter.ts:196-222, four instance refs at App.vue:317-319/223, two mount callbacks at :323-332, one 40x50ms poll at usePaletteWiring.ts:37-55. Product arm, the input that makes it RED: at 390x844 on /#/gradient, seed the clipboard with a sentinel, open Tools, click the control named exactly 'Copy CSS' — the clipboard is unchanged with zero page errors, while the same click at 1440x900 writes the gradient CSS. Second arm at 390/720/1024x1366 on /#/: the accessible-name inventory of the whole document contains no Copy color / Random color / Reset color / Open color input (all four true at 1440). Third arm: with a palette swatch edit dispatched at 390x844, assert the mobile-edit layer mounts within 500ms — today it never mounts and a console warning lands at ~2.05s."
      },
      {
       "gate": "G-K (the navigation names itself — the DELTA-2 amendment's own gate)",
       "command": "node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/nav-names-itself.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "One fresh, UNAUTHENTICATED context per ViewId, cold hash load, 3.2s settle; assert (i) the accessible text of nav.dock-band [aria-label=\"Select view\"] equals VIEW_MAP[id].label, (ii) the open listbox contains the current view with aria-selected=\"true\", (iii) exactly one option carries a visible selected marker. My measurement today: /#/ 'Home' and /#/gradient 'Gradient' pass, but /#/blob, /#/atmosphere and /#/admin/users all return the empty string — 7 of 14 views are anonymous. Arm (iii) is RED on every route: all seven options compute fontWeight 400 and backgroundColor rgba(0,0,0,0) with one svg child each (the view icon, not an indicator), because DockViewSelect.vue:97/:127 pass hide-indicator."
      },
      {
       "gate": "G-L (the login reports its outcome — structure + product)",
       "command": "test \"$(grep -rc 'slugBarRef' demo/palettes/useSlugMigration.ts)\" = 0 && test \"$(grep -rn 'PaletteSlugBar' demo/ | wc -l | tr -d ' ')\" = 0 && npx vitest run demo/test/dock/slug-outcome.test.ts && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/login-outcome.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Static arm RED: slugBarRef appears 6 times in useSlugMigration.ts and PaletteSlugBar is referenced 4 times while rendered by zero templates. Product arm, the exact input: open the dock login layer, fill 'aaaa-bbbb-cccc-dddd', submit — today the layer closes inert with aria-hidden=\"true\", the input is cleared, an exhaustive leaf-text scan for /fail|error|not found|too many|already signed|invalid/i returns [], no spinner ever renders, and the console carries nothing from the attempt (my measured cell). GREEN requires: the layer STAYS OPEN, a role=status node renders the ApiProblem's message, and the vitest spec proves onSlugSwitch RETURNS its outcome rather than dispatching it."
      },
      {
       "gate": "G-M (the login surface is named and reachable)",
       "command": "node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/login-a11y.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "With the layer ACTIVATED (the r1 error was measuring the at-rest inert state), assert every operable control computes >= 24x24 CSS px and the text field has a non-placeholder accessible name, and that document.activeElement is the field after the layer becomes active. My measured cell today: Switch to slug 22x22, Generate new slug 22x22, Cancel 22x22 (WCAG 2.2 SC 2.5.8 AA), input 160x23 with ariaLabel null / id null / labels 0 (SC 3.3.2, 4.1.2), and after clicking Login the activeElement is DIV.dock-face.is-active[tabindex=-1] with 20 typed characters dropped. Cure through the producer's own --dock-compact-control-* token hook at the layer root — never a per-instance padding bump (PR-12)."
      },
      {
       "gate": "G-N (one layout predicate)",
       "command": "test \"$(grep -c 'useMediaQuery' demo/shell/dock/Dock.vue)\" = 0 && test \"$(grep -rcE 'lg:hidden|hidden lg:|sm:|max-width: *639' demo/shell/dock/ demo/shell/PaneSegmentedControl.vue | awk -F: '{s+=$2} END {print s}')\" = 0 && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/one-grammar.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Static arm RED: Dock.vue:71 useMediaQuery(\"(min-width: 1024px)\") plus the lg:/639px forks in ProfileSection.vue:48/:127, MobileMenuDropdown.vue:38, ActionBarToggle.vue:82/:103 and PaneSegmentedControl.vue:46. Product arm, the input that makes it RED: load /#/ at 1024x1366 (iPad Pro portrait) and assert exactly one grammar renders. Today: data-layout=\"mobile\" while .dock-mobile-panes is display:block (Picker/About at 141x39) AND the desktop Login (83x28) and @mbabb (73x28) both render AND no overflow Menu exists — mobile and desktop grammars simultaneously, measured by me."
      },
      {
       "gate": "G-O (the backdrop sampler actually samples)",
       "command": "node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/backdrop-sampled.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Assert, on /#/ and two other routes: .glass-dock's data-backdrop-sample-state === 'sampled', --glass-backdrop-luma resolves non-zero, and a 4s idle MutationObserver on nav.dock-band records ZERO backdrop-attribute rewrites. My measured cell today on 4/4 viewports: state 'unavailable', source 'canvas', reason 'source-unavailable', luma '0', --glass-tint-strength pinned at its 4% floor, and 33 attribute mutations in 4s idle (11 per attribute) — an infinite failing retry on persistent chrome. The demo-side cure is threading App.vue's already-stamped canvas as :background-canvas; the producer's resolver fallthrough is a BH/BI ASK and NO local luminance shim may satisfy this gate. Per C-38, asserting 'sampled' rather than 'a canvas was passed' is mandatory — a WebGL2 surface without preserveDrawingBuffer would merely trade source-unavailable for sample-unavailable."
      },
      {
       "gate": "G-P (the layer machine owns focus and announcement)",
       "command": "node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/layer-focus-announce.mjs && npm run build && node docs/tranches/V/megatranche/audit/components/shell-dock-dock/gates/prod-live-region.mjs",
       "redToday": true,
       "whatWouldMakeItFail": "Arm 1: after each layer swap (Tools, Back, Login, Open color input) assert document.activeElement is a NAMED control inside the incoming face. Today it is an unnamed div.dock-face[tabindex=-1] on every one — measured by me for the Login swap, and ColorInput's exposed focus() has no caller in the repository, so the actions→input swap never hands over either. Arm 2 runs against the BUILT artifact (L-12: the dev server is structurally blind to the dev-gate) and asserts a polite live region exists in the band: today status-lamp.ts:46 returns null when !isDev, so a production band contains ZERO live regions and every layer swap, copy-flip and login result is unannounced."
      },
      {
       "gate": "G-Q (the witnesses exist — L-7, closing this corpus's own debt)",
       "command": "bash -c 'for f in $(sed -n \"s/.*witness\\/\\([a-z0-9.-]*\\.png\\).*/\\1/p\" docs/tranches/V/megatranche/registry/adjudicated/Dock.md | sort -u); do git ls-files --error-unmatch docs/tranches/V/megatranche/audit/components/shell-dock-dock/witness/$f >/dev/null || exit 1; grep -q \"$(shasum -a 256 docs/tranches/V/megatranche/audit/components/shell-dock-dock/witness/$f | cut -c1-16)\" docs/tranches/V/megatranche/registry/adjudicated/Dock.md || exit 1; done'",
       "redToday": true,
       "whatWouldMakeItFail": "`git ls-files docs/tranches/V/megatranche/audit/visual/shots/` returns ZERO paths and megatranche/.gitignore:10 ignores audit/components/**/*.png, so the three frames this corpus cites by name (collision-1024x1366.png, state-mobilemenu.png, state-tools-desktop.png) are untracked — and `grep -rn sha256 audit/components/shell-dock-dock/*.md` returns NOTHING, so the ignore policy's own escape clause ('every cited shot's sha256 is recorded in the committed registry docs') is unmet. Every visual-only claim in this corpus is therefore currently uncorroborable. GREEN requires each cited witness force-added past the ignore into the wave's FIRST commit, or its sha256 recorded in the committed apotheosis."
      }
     ],
     "carries": "BUILD HERE (12 identities, ids for life): L-35 ≡ L-1 ≡ D2-01 ≡ D2-02 ≡ D3-01(capability) [G-J] · C-3-WIDENED [G-K] · L-30 ≡ C-23 and L-31 ≡ C-24 [G-L] · C-31 and C-33 ≡ D2-04 [G-M] · C-32 [G-O] · C-34 [G-P] · D2-03 ≡ D3-04 [G-N] · D2-16 [G-K arm iii] · L-18 [G-E/G-F] · L-19 + L-34 [G-C/G-D] · L-20 + C-35 + C-26 [G-A/G-B] · L-6 + L-23(the ONE shell→palettes guard) [G-G] · MT-F026 [G-H]. FOLDED INTO THE SAME COMMITS, no separate gate: D2-06, D2-08, D2-09, D2-10, D2-11, D2-13, D2-14, D2-15, D3-07, D3-09, D3-10, D2-07, C-25, C-27, C-28, L-39. FOLD ELSEWHERE, identity preserved, NOT re-booked: D3-08 ≡ D2-05 ≡ L-7 ≡ MT-F005 → MT-COLORINPUT-1 (D-3; this wave asserts nothing about it) · L-21 → MT-APP-1 (D-4 H1 arm; h1=0 re-measured on five routes) · L-22 → W-HYGIENE config parity (now six confirmations; cure superseded to DELETE) · D3-01(retirement) + L-38(the wrapper's :deep) → W47's booked PaneSegmentedControl 1→0 · C-33's HOUSING half → W23 (VISUAL-CONSTITUTION:222 rules Account is a modal side Dialog opened FROM the dock; this wave cures the name, the size and the outcome, never the housing) · L-36, L-37, L-32, L-23(the other six globs), L-38(DESIGN.md:381) → W-HYGIENE · L-33 → the palettes seat · C-30 → the registry owner (root) · D3-05 and D3-03 → the standing BH/BI glass relay. RETIRED / STRUCK AND UNCITABLE: D3-01's collision framing ('painted on top of', '100% overlap of a live control' — the control is clipped, opacity 0 and tabindex -1) · D3-11's forced-colors mechanism (both engines DO set the media query — my two-engine cell) · the r1 Safari-keyboard BLOCKER (harness FKA default, per states.mjs:7-9 and D2-12) · D3-07's 'one unlabelled ←' (Dock.vue:154 carries aria-label='Back') · and the r2 strikes carried forward unchanged (L-20's occlusion harm, L-19's 'two predicates' as operative mechanism, L-6's cycle and bundle-bytes framings, L-22's 'second instance', L-21's sr-only cure). BANKS, each a runnable command (L-4): BANK-D1 chrome tint token `grep -rq 'glass-tint-chrome|useDockSurfaceTint|surfaceTint' node_modules/@mkbabb/glass-ui/dist/` (silent today) · BANK-D3 glass major `node -e \"process.exit(require('./node_modules/@mkbabb/glass-ui/package.json').version.startsWith('7.')?1:0)\"` (exits 1 today) · BANK-D4 roving rail `grep -rq 'roving|ArrowRight' node_modules/@mkbabb/glass-ui/dist/dock.js` (D3-03 lands the day it hits) · BANK-D5 hit-seat token `grep -rq 'dock-touch-target|dock-hit-seat' node_modules/@mkbabb/glass-ui/dist/components/dock/styles/` (D3-05) · BANK-D6 backdrop fallthrough `node -e \"…assert De() falls through to the document convention…\"` (C-32's producer half). Every bank is swept by RUNNING it at each wave open.",
     "piObligations": "Chromium cells only (I-20 — nothing here claims real Safari or playwright-webkit; the dock-morph SIGABRT arm stays in the Phase-D safari-real matrix). Matrices: 390x844 (isMobile+hasTouch) · 720x900 (= 1440 at 200% browser zoom, the reflow arm) · 1024x1366 (the predicate-disagreement arm — G-N is blind without it) · 1440x900 · BOTH colour schemes for G-F and D2-06 (the referent error inverts sign between them). Routes: http://localhost:9000/#/ hash-only, never path URLs (MT-F006), plus /#/gradient (G-J clipboard sentinel), /#/blob, /#/atmosphere, /#/admin/users (G-K blank-name arm), each in a FRESH unauthenticated context. Selectors pinned: nav .glass-dock · .glass-dock .dock-plate · .action-bar-toggle-slot · [aria-label=\"Toggle action bar\"] · [aria-label=\"Open color input\"] · [aria-label=\"Select view\"] · [role=option] · .dock-mobile-panes · .dock-seal .gold-shimmer-icon · input[placeholder^=\"enter slug\"] · [aria-label=\"Switch to slug\"] · the Login and @mbabb pills. COMMITTED captures, force-added past the repo-root *.png ignore AND megatranche/.gitignore:10 into audit/components/shell-dock-dock/witness/ in the wave's FIRST commit, each with its sha256 written into the apotheosis (G-Q): before/after pairs {mobile-actions-390, tablet-grammar-1024x1366, nav-name-blob, login-error-rendered, login-targets-1440, crossfade-60ms, admin-latch-return, chrome-ink-light, chrome-ink-dark, pill-elevation-1440} plus the collapsed-seal frame the tranche has NEVER captured (DELTA-3), plus each gate's machine-readable oracle output. OM-1-dock-item-shadows.png is ADOPTED as G-H's BEFORE frame. ENV (L-12): the dev server at :9000 is API-LESS — the authed viewEntries arm and the authed Profile pill are vitest-stubbed and LABELED, never claimed as measured; a blank data surface is an environment fact, not a defect confirmation. G-P's second arm and any bundle claim run against the BUILT artifact, never the dev server.",
     "deltaObligations": "(1) Command surface: mobile clipboard sentinel unchanged after 'Copy CSS' → gradient CSS written, same gesture, 390x844; action-region inventory at 390/720/1024 from 0 of 4 named controls → 4 of 4; dock faces 3 → 4; the mobile-edit layer from never-mounting to mounting within 500ms of a swatch edit; console 'gave up waiting for the color picker' from present at ~2.05s to absent. (2) Navigation names: trigger accessible text on /#/blob, /#/atmosphere, /#/admin/users from \"\" → \"Blob\", \"Atmosphere\", \"Users\"; views whose current entry is missing from the listbox 7 → 0; options carrying a visible selected marker 0 → 1. (3) Login outcome: {inputValue:\"\", layerInert:true, errorish:[], spinner:absent} → {layer open, one role=status node carrying the ApiProblem title, input preserved}; slugBarRef references 6 → 0; PaletteSlugBar references 4 → 0 (243 lines deleted). (4) Login surface: three controls 22x22 → >=24x24; input accessible name absent → present; activeElement after opening from div.dock-face[tabindex=-1] → the field; 20 typed characters dropped → 20 kept. (5) Layout: at 1024x1366, {data-layout mobile + panes shown + Login + @mbabb + no Menu} → exactly one grammar; dock-subtree width-only predicates 4 → 0. (6) Backdrop: state unavailable/luma 0/tint at the 4% floor with 33 idle mutations per 4s → state sampled/luma non-zero/0 idle mutations. (7) Admin ink: five-step trace [false,true,TRUE,TRUE,TRUE] → [false,true,false,false,false] with cold /#/atmosphere still true; VIEW_MAP rows without a group 14 → 0; hardcoded ViewId arrays 2 → 0; admin-partition owners disagreeing 5/5/7 → one derivation. (8) Chrome referent: resolveLiveTint('chrome') undefined every call → defined within 0.02 OKLab-L of the composited band, both schemes. (9) Crossfade: leaving-face opacity 1→0 in one frame (transitionProperty `visibility`, 0s) → overlap strictly in (0,1) under the producer spring. (10) Dependency: shell→palettes value-import files 5 → 0; Dock.vue graph demo/palettes 23 → 0; eslint guards matching zero files 3 → 0 (with a meta-test asserting each guard matches a non-zero file count). (11) Geometry: layer-swap Δx +81.2px / Δw −162.5px → 0/0 or a spring-driven motion. (12) Chrome hygiene: visible separators 3 → 0; nav innerText leaked inactive-layer text present → absent; Dock.vue roots 2 → 1; runtime cycles in demo/ 1 → 0; lamp role alert → status. (13) Witnesses: cited frames tracked by git 0 → all; sha256s recorded 0 → all.",
     "completableAlone": "YES, with one honest qualification stated rather than hidden. Were this the only wave ever to execute: the dock's actions would work on a phone (nine handlers, three views, one dead layer), the primary navigation would name itself on all fourteen routes, a failed login would say so, the login controls would be reachable and named, the chrome would read the surface that paints, the layer swap would be a motion, the shell would stop importing 23 palette modules to render a Login button, and the owner-marked pill shadows would die through primitives installed today. No sibling wave is a precondition: r2's G-I hedge is DISCHARGED by G-J (the stronger gate) rather than deferred; MT-APP-1 keeps only the boot/H1 rows; the producer asks (roving rail, hit-seat, backdrop fallthrough) are BANKED with runnable re-triggers, never blocking; no API, no Safari cell, no bundle claim. Eight of sixteen gate arms run this minute as grep/tsc/vitest against the tree, and every probe gate's RED cell above was MEASURED BY ME today, not inherited. THE QUALIFICATION (L-1, and I would rather state it than have a close discover it): sixteen gates across five commits is at the outer edge of what the measured 38% delivery rate supports. The wave is therefore authored as FIVE SEQUENCED, INDEPENDENTLY CLOSEABLE COMMITS, and the sanctioned split line — should execution measure it too large — is the commit boundary, UNDER THE SAME NAME, never a second name (L-5). Commit 1 (the command registry) is the one that must land: it is the only cure no other wave can reach, and four other rows collapse into it."
    },
    "addendumClause": "MT-DOCK-B (E-3 — appended, never rewriting the r2 record or the worker records). 1. WHERE MY MERGE LIVES. The brief asked for an in-place merge into registry/adjudicated/Dock.md. I did NOT overwrite it, deliberately: L-14 gives agglomeration to arbiter-F, worker-F is executing this same brief concurrently, and an in-place write by either worker races the other and pre-empts the arbiter. My merge-ready sections — the amended §1 tallies, the seven-item DELTA section, the row map and the instrument notes — are at docs/tranches/V/megatranche/registry/adjudicated/delta-r3/Dock.worker-O.md, written to be transplanted verbatim. The r2 apotheosis is byte-intact. 2. THE COUNT, SCOPED (L-9). The brief's '30 accusations (7 BLOCKER)' is exactly the union of three of the six Dock.vue-subject seats (a1d2b8ed + ae695a68 + ab862155); the D3-* and C-* seats the brief also names by row sit outside it. The r3 corpus is 58 rows across six seats, which collapse to 34 distinct claims once aliases are folded, of which 9 already have owners. No close may say 'thirty defects in the Dock' or 'fifty-eight': the honest headline is 24 CONFIRMED / 3 REFUTED / 6 RESCOPED / 1 GLASS-OWNED new claims, of which twelve are built here. 3. NO SOURCE DRIFT. git diff --stat 9bcd5d91 HEAD -- demo/ src/ test/ e2e/ is EMPTY, so every r2 line anchor still resolves and every r2 gate was re-run RED by me today at 66f3ca89 — the r2 rulings stand on unchanged ground and this delta is not an artifact of a moved tree. 4. THE ONE RULING THAT CHANGES A PRIOR DISPOSITION (DELTA-1). r2 sent the null-ref CURE to MT-APP-1 as 'bindPane on all three PaneSlot sites' and kept only a witness gate here. Five independent r3 seats plus my own single-writer trace show that shape is a masking fix — it binds the ref channel on mobile and leaves present-and-inert fully representable, against L-8 and the owner's no-masking-fallback edict. The cure shape is upgraded to the provide/inject registry and re-homed HERE; MT-APP-1 keeps its own identity untouched; G-I is discharged by G-J. This is a SCOPE and CURE-SHAPE overturn, not a verdict overturn: r2's L-20/L-18/L-19/L-6 verdicts are all re-confirmed. 5. THE ONE AMENDMENT WITHOUT WHICH THE r2 CURE LEAVES A DEFECT STANDING (DELTA-2). Structure (b) as written ('viewEntries filtering VIEW_MAP by group') does not cure C-3-WIDENED — a user-group filter still excludes blob, so the trigger stays anonymous. The option set must be the group filter UNIONED UNCONDITIONALLY WITH THE CURRENT VIEW. Any close citing structure (b) as sufficient would be citing a cure that leaves 7 of 14 routes with a nameless primary navigation control. 6. THE TWO REFUTATIONS THAT MATTER MOST. D3-01's collision framing dies on measurement (the Tools control is clipped to zero width, opacity 0, tabindex -1 and out of hit order — nothing is painted over anything; the phantom rect is what a clipped child reports), and D3-11's mechanism dies on a ten-second two-engine cell (Playwright 1.60 sets forced-colors: active in BOTH webkit and chromium). Both were filed as evidenced findings with real numbers attached; both are accusations that do not survive re-derivation, which is precisely the seat's purpose. 7. THE FORMATION'S OWN DEBT (DELTA-7, and it is ours, not the challengers'). git ls-files over audit/visual/shots/ returns ZERO, and megatranche/.gitignore:10 ignores audit/components/**/*.png. That ignore is deliberate policy whose escape clause is 'every cited shot's sha256 is recorded in the committed registry docs' — and grep -rn sha256 audit/components/shell-dock-dock/*.md returns NOTHING. So every visual-only claim in this corpus is uncorroborable by any future reader today. Rows resting solely on such a frame (D3-06's wrapped row and read-through material, D2-07's rendered RTL band) are marked UNVERIFIABLE-HERE rather than adopted, and G-Q closes the hole. 8. UNVERIFIABLE-HERE LEDGER (L-12). The dev server is API-LESS: the authed viewEntries collapse, the authed Profile pill, and every data-backed dock state are stubbed and labeled, never claimed as measured. The production live-region absence (C-34) is source-certain, not measured — I did not build. C-31's producer line attribution, C-29's SelectValue mutation, C-28's transitionend recovery, C-37's six negative proofs, D3-02's collapsed-seal keyboard reachability and C-38's canvas backing store are CARRIED-LABELED or DECLARED UNMEASURED; none of them carries a verdict I did not earn. 9. GLASS BOUNDARY, HELD. Three producer asks ride the standing BH/BI fond with runnable banks — the roving-focus rail (D3-03, GLASS-OWNED), the compact hit-seat token (D3-05), and the backdrop resolver's fallthrough (C-32's producer half). None may be satisfied by a demo-local patch, no keydown handler may be grafted into Dock.vue, no demo box-shadow may satisfy G-H, and no local luminance shim may satisfy G-O. The spectrum prefix seam remains glass-owned and is not re-booked anywhere in this record. 10. OWNER MARKS. MT-F026 / OM-1 / OM-7 are ruled and are NOT re-litigated here; G-H carries MT-F026's own gate verbatim with only the r2 R-2 name correction (glass-ui 7.0.0 exports DockControl and DockTrigger; DockIconButton / DockSelectTrigger / DockDropdownTrigger are emitted CSS class names only, and ROOT-FINDINGS.md:1242 plus the memory reference glass_ui_dock_components.md remain STALE).",
    "dissent": "1. ON WAVE SIZE (my own dissent from the shape I was asked to author, recorded so it is not discovered at execution). The brief directs ONE unified wave, and I delivered one — but sixteen gates across five commits sits at the outer edge of what L-1 permits given a measured 38% delivery rate and a standing formation at 2% executed. The r2 apotheosis already recorded worker-O (me, in the prior round) arguing for a split and being overruled at R-1, and the r3 evidence makes the wave LARGER, not smaller. I therefore restate the position in its weaker, sanctioned form rather than re-litigating: the wave keeps ONE name, and the split line — if execution measures it too large — is the commit boundary, never a second identifier. If only one commit ever lands, it must be commit 1 (the command registry): four separately-filed BLOCKERs collapse into it, it is the only cure no other wave can reach, and every other commit is closeable behind it. 2. ON THE HOUSING QUESTION I DECLINED TO RULE. D2-04 argues the whole slug/auth flow should leave the band for the Account Dialog, and VISUAL-CONSTITUTION:222 backs it as canon-hard ('Account is one modal side Dialog opened from the Dock, not a route chassis or second main'). C-33 argues instead for supplying the producer's compact token in place. I ruled that this wave cures the outcome, the name and the target size and FOLDS the housing to W23 — but a reasonable arbiter could rule the opposite way and move the surface now, which would delete the slug layer, the three 22px controls, the unnamed input and the 162px dock jump in one move. If the arbiter moves it, the hedge is the ColorInput precedent: G-L and G-M travel verbatim to W23, and if W23 does not carry them, the rows return here. 3. ON D3-02, WHERE I MAY BE UNDER-CALLING. I rescoped 'the dock has two resting geometries selected by input device' to a design decision plus a π obligation, because collapse-on-idle-after-interaction is a defensible producer idiom and the seal is the most heavily ruled composition in this component's record. But I did NOT measure whether the collapsed 56px seal is keyboard-reachable or carries an accessible name, and if it is not, then a mouse user's dock silently becomes an unlabelled circle that hides all four navigation controls — which would be a real defect, not a design decision. I record the gap rather than guessing at it; the first execution session should measure it before accepting my rescope. 4. ON SEVERITY INFLATION ACROSS THE CORPUS. Four of the seven BLOCKERs in this delta do not survive as BLOCKERs: D3-01's collision half is refuted outright, L-23 is a lint-config row with no runtime arm, D2-03's tablet grammar is a real MAJOR but nothing is unreachable, and C-3-WIDENED is a MAJOR rather than a BLOCKER because navigation still operates. Two DO survive at full strength (the command surface, the silent login) and one is upheld only in its capability half. I record this because 'seven BLOCKERs against the Dock' will otherwise enter a close row unchallenged, and because the pattern — a true observation wearing a false mechanism and an inflated severity — is the same pattern MT-F022 and this formation's own seven manufactured findings describe. The challengers were right about WHAT far more often than about WHY, and a close that cites their mechanisms will cite three things that are false (D3-01's occlusion, D3-11's engine claim, and the r1 Safari keyboard BLOCKER)."
   }
  }
 ]
}