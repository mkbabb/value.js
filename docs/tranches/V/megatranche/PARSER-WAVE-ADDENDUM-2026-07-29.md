# PARSER MEGA-TRANCHE — CSS TOTALITY FORMATION ADDENDUM

**Date:** 2026-07-29
**Authority:** owner instructions, `docs/precepts/`, and
`PARSER-IN-FLIGHT-AUDIT-2026-07-29.md`
**Status:** `SUPERSEDED_REAUTHOR_REQUIRED`
**Canonicalization blocker:** SK-V26 W3 must reach semantic parity and freeze;
the active task must then assign this program either a post-W3 SK-V26 addendum
identity or the next unused parser tranche identity.

This addendum is fully bounded so the parser session can adjudicate and
canonicalize it without another research cycle. It is not authority to edit the
active W3 worktree, select its held-out grammar, expose held-out inputs, or start
W4.

> **PASS 1 SUPERSESSION — 2026-07-29.** This addendum's generated token-event
> tape as parser input and its P5 token plane conflict with the owner's later
> source-direct, scannerless combinator acceptance and Value ADDENDA-07/08.
> They are rejected and must not execute. Preserve only the CSS Syntax, WPT,
> recovery, span, consumer, and performance research. Durable authority is
> parse-that-css-totality `273133b3d88f`: R/E/V/K `KILL`, S/D inactive, repair
> the incumbent source-direct closure runtime. Luna M1 is `059e129`; M2 is
> active. The replacement formation has no BBNF-owned or generated CSS parser:
> parse-that owns reusable runtime/combinators/spans/recovery, and Value owns
> the sole CSS grammar, consumer, and UI.

## 1. Thesis

One Value-owned, source-direct combinator vertical replaces the three
handwritten value.js structural scanners. It consumes only reusable parse-that
runtime, span, and recovery primitives proven by the consumer. Normative CSS,
draft CSS modules, and value.js additions remain separate profiles. No token
tape, tokenizer array, generated CSS parser, or second runtime is production
input.

## 2. Goal criterion

This program succeeds when current value.js parser consumers run directly from
source through idiomatic parse-that combinators with normative recovery and
structured spans, the replaced handwritten scanners are deleted, and
equivalent end-to-end correctness/performance receipts close.

## 3. Invariants

1. No active W3 source is widened before its existing freeze.
2. No parser dispatches on `CSS`, grammar filenames, rule names, property names,
   or extension identity inside a generic backend.
3. One source-direct CSS grammar; no duplicate scanner, token tape, or
   token-object runtime.
4. BBNF receives a runtime receipt only after W3 freeze and creates no CSS
   parser, grammar registry, or generated CSS artifact.
5. CSS recovery is grammar/algorithm data carried by events, never a transform
   name switch in parse-that.
6. Normative CSS, draft profiles, and value.js extensions have separate
   denominators and manifests.
7. Lossless spans are source offsets with a declared UTF-8/UTF-16 crossing
   policy; downstream code does not invent positions.
8. Ordinary invalid input returns an explicit result and never throws.
9. No alias, shim, dual export, old-parser fallback, or compatibility barrel
   survives cutover.
10. Substrate and a real consumer land in the same wave.
11. Every wave has a maximum three implementation agents; design synthesis uses
    GPT Sol xhigh + GPT Luna xhigh proposals followed by GPT Sol xhigh
    adjudication. Historical model receipts stay literal.
12. The in-app Browser is required for every value.js consumer wave.

## 4. File-bound roots

All relative file bounds below resolve against exactly one named repository:

| Prefix in this addendum | Repository root at execution |
|---|---|
| `restart/**`, `crates/**`, `grammars/**`, `tests/**` | a fresh post-W3 BBNF sibling worktree; never the active `/Users/mkbabb/Programming/bbnf-lang-skv26` worktree |
| `rust/**` | a fresh post-W3 parse-that sibling worktree; never the admitted `/Users/mkbabb/Programming/parse-that-skv26` worktree |
| `src/**`, `test/**`, `docs/**`, `package.json` | the value.js worktree assigned by the active owner after canonicalization |

`<canonical>` is a required future tranche identity, not a literal directory to
create. No file bound authorizes edits before P0 assigns fresh worktrees and
records their exact branches and starting commits.

The inherited benchmark constitution also uses `P1`, `P2`, `P3`, `P4`, `P6`,
and `P7` as semantic-plane names. This addendum always writes those as
`benchmark-plane P<n>`; an unqualified `P<n>` means the wave defined below.

## 5. Wave table

| Wave | Name | Goal | Agents | Opens after | Closes on | Status |
|---|---|---|---:|---|---|---|
| P0 | Truth and corpus seal | freeze semantic planes, modules, consumers, corpora, and controls | 3 serial/parallel | W3 freeze | hashed manifests and A/A plan | planned/blocked |
| P1 | Source graph and diagnostic carrier | make modular syntax and precise events expressible without CSS special cases | 3 serial | P0 | JSON/Sheets/W3 parity plus modular control | planned |
| P2 | CSS Syntax event authority | generate full preprocessing/token/component/generic-rule behavior | 3 parallel then serial | P1 | reference/WPT differential and regen | planned |
| P3 | Typed module profiles | consume syntax events for the exact value.js CSS surface and extensions | 3 parallel | P2 | profile denominator green | planned |
| P4 | Consumer clean break | transpose all 52 `/css` exports and delete structural scanners | 3 serial/parallel | P3 | consumer crater, browser journeys, deletion | planned |
| P5 | Equivalent-plane profiling | profile benchmark-plane P1/P2/P4/P7 and admit only end-to-end wins | 3 parallel then serial | P4 | raw paired receipts and profiles | planned |
| P6 | Totality close | delete residue and reconcile every promise, export, doc, and artifact | 3 serial | P5 | seven audit lanes and final receipt | planned |

No wave may be reported merely as `P<n>`; use its number and name together.

## 6. P0 - Truth and corpus seal

**Name:** P0 - Truth and corpus seal
**Opens after:** admitted W3 semantic parity, regeneration freeze, and held-out
selection rules have completed without exposure to this program
**Agents:** three; proposal seats may run in parallel, adjudication is serial
**Hard gate:** every semantic plane, module/profile, consumer, corpus object,
competitor, and decision statistic is hash-sealed before candidate measurement
**Status:** planned/blocked on active W3

### Goal criterion

P0 succeeds if later implementation and profiling can neither change the
claimed language nor choose favorable inputs after observing results.

### Scope

1. Freeze CSS Syntax algorithms and exact spec revisions.
2. Freeze module/profile rows as `REQUIRED`, `OPAQUE_PRESERVE`,
   `CONTEXT_REQUIRED`, or `OUT_OF_SCOPE`.
3. Freeze the value.js extension namespace and collision law.
4. Map all 52 `/css` exports and every demo consumer to terminal BUILD/FOLD/
   RETIRE rows.
5. Pin WPT paths/commit and local generated/fuzz seeds.
6. Define benchmark-plane P1/P2/P3/P4/P6/P7 output and checksum promises.
7. Define A/A calibration, paired estimator, multiplicity family, stopping rule,
   promotion floor, and regression budget before candidate timing.

### Triumvirate dispatch

Dispatch research + plan augment + redress if the module matrix gains more than
two profiles, a consumer cannot be assigned a terminal row, a proposed baseline
cannot match the semantic plane, or the same corpus/hash failure recurs three
times.

### File bounds

| File | Access |
|---|---|
| `restart/skinny/tranches/<canonical>/CSS-SPEC-MANIFEST.tsv` | create |
| `restart/skinny/tranches/<canonical>/CSS-CONSUMER-MATRIX.tsv` | create |
| `restart/skinny/tranches/<canonical>/CSS-CORPUS-MANIFEST.tsv` | create |
| `restart/skinny/tranches/<canonical>/CSS-BENCH-DECISION.json` | create |
| `docs/tranches/V/megatranche/registry/parser-terminal-dispositions.tsv` | create |

Do not touch BBNF compiler source, parse-that source, value.js product source, or
the SK-V26 held-out pool.

### Disjointness and worktrees

Proposal agents write separate temporary research packets; only the Sol xhigh
adjudicator writes the five canonical files. No shared modify path exists.

### Agent units

#### P0.a CSS authority proposal

- **Goal:** identify the minimal normative syntax floor and exact module-profile
  boundaries.
- **Mechanism:** GPT Sol xhigh reads specifications, WPT metadata, and current
  consumers.
- **Files:** temporary read-only packet.
- **Sub-gate:** every row has a spec URL/version, semantic plane, and explicit
  disposition.

#### P0.b Consumer and corpus proposal

- **Goal:** produce a mechanical exact export/consumer/corpus inventory.
- **Mechanism:** GPT Luna xhigh reads exports, call sites, tests, WPT paths, and
  hashes; no design decisions.
- **Files:** temporary read-only packet.
- **Sub-gate:** zero unmatched `/css` exports, consumers, or corpus objects.

#### P0.c Truth seal adjudication

- **Goal:** select one bounded denominator and benchmark decision contract.
- **Mechanism:** GPT Sol xhigh challenges both proposals and writes the canonical
  manifests.
- **Files:** the five bounded files above.
- **Sub-gate:** schema checks, hashes, and manual adversarial review all pass.

### Hard gate

1. Schema validators parse every row and reject duplicate IDs.
2. `git ls-files` plus export-AST inspection returns zero unmatched public
   surfaces.
3. Every corpus object resolves and matches SHA-256 in a clean checkout.
4. A/A decision JSON is accepted before any A/B run.
5. `git diff --check` passes on the wave slice.

### Format and lint cadence

Run TSV/JSON schema validation after each integration batch and at close; run
`git diff --check` at close.

### Verification artefacts

The five canonical manifests plus proposal hashes and an adjudication receipt.

### Commit plan

`docs(parser-p0): seal CSS authority, consumers, corpora, and benchmark decision`
with a body naming the freeze identities and routed exclusions.

### Dependencies

- **Depends on:** SK-V26 W3 - Post-freeze generality proof.
- **Blocks:** P1 - Source graph and diagnostic carrier.

### Archaeology

V·π and ADDENDA-08 changed denominators during implementation and called
color-shaped probes “CSS.” P0 prevents both recurrences.

## 7. P1 - Source graph and diagnostic carrier

**Name:** P1 - Source graph and diagnostic carrier
**Opens after:** P0 truth seal
**Agents:** three serial
**Hard gate:** action-free modular sources compile deterministically and emit
structured spans without changing admitted JSON, Sheets, or W3 behavior
**Status:** planned

### Goal criterion

P1 succeeds if CSS Syntax can be expressed as small colocated modules and
diagnostics can cross parse-that without a grammar-specific backend or duplicate
source bytes.

### Scope

1. Extend the grammar-instance boundary with a deterministic source graph or
   canonical source bundle.
2. Give every module logical identity, content hash, and source-map span.
3. Extend the event/failure carrier with structured code/span/expected/actual
   facts required by current value.js `ParseIssue`.
4. Preserve transaction rollback, progress, bounded depth/work/events, and
   NoSink behavior.
5. Carry legacy JSON, Sheets, and W3 public witnesses unchanged.
6. Add one non-CSS modular control so the mechanism cannot specialize to CSS.

### Triumvirate dispatch

Trigger if changes expand beyond the named compiler/parse-that files by more
than two paths, ABI evolution cannot preserve existing controls, deterministic
hashes diverge twice, or a third diagnostic carrier redesign is proposed.

### File bounds

| File | Access |
|---|---|
| `crates/core/src/compiler/source.rs` | modify-carve |
| `crates/core/src/compiler/instance.rs` | modify-carve |
| `crates/core/src/compiler/frontend.rs` | modify-carve |
| `crates/core/src/compiler/event.rs` | modify-carve |
| `crates/core/src/compiler/replay.rs` | modify-carve |
| `crates/core/tests/css_source_graph.rs` | create |
| `rust/parse_that/src/outcome.rs` | modify-carve |
| `rust/parse_that/src/run.rs` | modify-carve |
| `rust/parse_that/tests/kernel/diagnostic_laws.rs` | create |

Do not touch legacy `grammar/css/l4/**`, value.js source, W3 frozen evidence, or
backend language-name dispatch.

### Disjointness and worktrees

All three units are serial because compiler and parse-that ABI changes cross a
single boundary. Use fresh sibling worktrees and unique
`CARGO_TARGET_DIR=<worktree>/target/parser-p1`; never profile in an active W3
worktree.

### Agent units

#### P1.a Boundary design

- **Goal:** freeze the smallest general source-graph and diagnostic schema.
- **Mechanism:** Sol + Luna proposals, final Sol adjudication.
- **Files:** addendum/schema packet only.
- **Sub-gate:** JSON, Sheets, CSS, and non-CSS control can all be represented
  without optional grammar identity fields.

#### P1.b parse-that carrier

- **Goal:** carry exact failure/event facts transactionally with bounded cost.
- **Mechanism:** implement ABI evolution and law tests.
- **Files:** parse-that bounds only.
- **Sub-gate:** all kernel/allocation/rollback laws and diagnostic adversaries
  pass.

#### P1.c BBNF source graph

- **Goal:** compile linked action-free modules to one deterministic replay.
- **Mechanism:** implement source graph, source map, replay, and controls.
- **Files:** BBNF bounds only.
- **Sub-gate:** two empty output directories regenerate byte-identically; all
  pre-existing public witnesses are event-identical.

### Hard gate

1. `cargo test --workspace --all-features --all-targets` passes in both repos.
2. Strict Clippy, rustdoc, formatting, package, and external bridge gates pass.
3. Two clean module orderings produce identical revision and generated bytes.
4. Span tests cover preprocessing offset changes, UTF-8, EOF, rollback, and
   recovery.
5. No CSS/JSON/Sheets name appears in generic source or dispatch.

### Format and lint cadence

`cargo fmt --all --check` and strict Clippy after each serial unit and at close;
full workspace tests at close.

### Verification artefacts

Source-graph schema, ABI receipt, event-equivalence JSON, regen hashes, package
receipts, and crater manifest.

### Commit plan

1. `feat(parse-that-diagnostics): carry structured transactional failure spans`
2. `feat(bbnf-source-graph): compile deterministic action-free grammar modules`
3. `test(parser-p1): prove module order, span, and incumbent parity`

Each broad/ABI commit has a body with rationale, evidence, and remaining scope.

### Dependencies

- **Depends on:** P0 - Truth and corpus seal.
- **Blocks:** P2 - CSS Syntax event authority.

### Archaeology

Historical `grammar/css/l4` depends on imports and host actions while W3 admits
one source and no actions. P1 adds the general missing organ rather than
resurrecting the old host-action path.

## 8. P2 - CSS Syntax event authority

**Name:** P2 - CSS Syntax event authority
**Opens after:** P1 source graph and diagnostics
**Agents:** three parallel on disjoint grammar/corpus/adapter bounds, then serial
adjudication
**Hard gate:** generated scalar P2 output matches the frozen reference and pinned
CSS Syntax corpus with byte-identical regeneration
**Status:** planned

### Goal criterion

P2 succeeds if preprocessing, every token class, component values, generic rules,
declarations, and recovery have one generated lossless event authority.

### Scope

1. Author action-free modules under `grammar/css/totality/syntax/**`.
2. Generate the scalar Rust event parser through the P1 source graph.
3. Normalize event identities and lossless raw spans.
4. Port frozen reference cases and pinned WPT-derived cases.
5. Add differential, metamorphic, fuzz, resource, depth, and recovery progress
   tests.
6. Delete no value.js consumer yet.

### Triumvirate dispatch

Trigger if grammar expansion exceeds the sealed token/algorithm manifest, two
reference disagreements lack a spec-section ruling, generated source requires a
host action, or a third recovery-progress failure appears.

### File bounds

| File | Access |
|---|---|
| `grammar/css/totality/syntax/*.bbnf` | create |
| `grammar/css/totality/css-syntax.instance.json` | create |
| `crates/core/tests/css_syntax_totality.rs` | create |
| `crates/core/tests/fixtures/css-syntax/**` | create |
| `crates/core/benches/css/syntax_p2.rs` | create |
| `restart/skinny/tranches/<canonical>/artifacts/CSS-SYNTAX-REGEN.json` | create |

Do not touch typed property modules, value.js source, legacy CSS grammar, or
generic compiler/runtime code.

### Disjointness and worktrees

Grammar, corpus, and differential-adapter units use three sibling worktrees with
unique targets. The adjudicator integrates serially after each unit commits.

### Agent units

#### P2.a Syntax grammar

- **Goal:** express the frozen syntax algorithms as compact action-free modules.
- **Mechanism:** author preprocessing/token/component/rule modules against the
  reference event vocabulary.
- **Files:** grammar and instance bounds only.
- **Sub-gate:** every manifest row resolves to a rule/event or explicit opaque/
  out-of-scope disposition.

#### P2.b Conformance corpus

- **Goal:** produce independent expected event/diagnostic fixtures.
- **Mechanism:** import pinned WPT-derived rows and generated adversaries without
  observing candidate output.
- **Files:** fixtures only.
- **Sub-gate:** hashes/spec sections resolve; zero expected file is candidate-
  generated.

#### P2.c Differential and regeneration proof

- **Goal:** prove generated/reference equivalence and deterministic output.
- **Mechanism:** event normalizer, lossless replay, fuzz/metamorphic laws, and
  two-directory regeneration.
- **Files:** test, bench harness, and receipt bounds.
- **Sub-gate:** zero unexplained differential; limits and recovery always make
  progress.

### Hard gate

1. Frozen positive, negative, recovery, full-consumption, EOF, tail, and
   alignment families pass.
2. Normalized event and diagnostic checksums match the independent reference.
3. Exact raw replay matches source under the declared preprocessing policy.
4. Property/fuzz runs produce no throw, hang, unbounded growth, or zero-progress
   recovery.
5. Clean regenerations are byte-identical and contain no language-name dispatch.

### Format and lint cadence

Run grammar format/schema and targeted Rust tests after each integrated module;
full format/Clippy/workspace suite at close.

### Verification artefacts

Corpus manifest, differential JSON, fuzz seed/crash corpus, regen receipt,
generated-size report, and preliminary non-promotional P2 profile.

### Commit plan

1. `feat(css-syntax): generate lossless preprocessing and token events`
2. `feat(css-syntax): add component values, generic rules, and recovery`
3. `test(css-syntax): bind reference, WPT-derived, fuzz, and regeneration proof`

### Dependencies

- **Depends on:** P1 - Source graph and diagnostic carrier.
- **Blocks:** P3 - Typed module profiles.

### Archaeology

ADDENDA-08 rejected any token stream, leading consumers back to repeated string
splitting. P2 permits one generated event contract and prohibits a second
scanner runtime.

## 9. P3 - Typed module profiles

**Name:** P3 - Typed module profiles
**Opens after:** P2 syntax authority
**Agents:** three parallel, disjoint by module
**Hard gate:** every sealed value.js profile row is typed, explicitly context-
required/opaque, or retired, with no syntax-layer mutation
**Status:** planned

### Goal criterion

P3 succeeds if current value.js color, value, image/gradient, easing, animation,
timeline, property, scope, nesting, and custom-function syntax consumes the
common event floor without a universal AST or property-name backend switch.

### Scope

1. Implement the normative Color/Values profile.
2. Implement gradients/easing/animation/timeline profiles.
3. Implement generic stylesheet/at-rule/profile boundaries.
4. Implement the named value.js extension profile.
5. Preserve context-required constructs as typed unresolved values, not syntax
   failures.
6. Generate exact profile coverage receipts.

### Triumvirate dispatch

Trigger on any syntax-layer edit, profile overlap across agent file bounds, more
than two unsealed module additions, a third typed AST redesign, or an unresolved
normative-vs-extension name collision.

### File bounds

| File | Access |
|---|---|
| `grammar/css/totality/modules/color*.bbnf` | create |
| `grammar/css/totality/modules/values*.bbnf` | create |
| `grammar/css/totality/modules/images*.bbnf` | create |
| `grammar/css/totality/modules/animations*.bbnf` | create |
| `grammar/css/totality/modules/timelines*.bbnf` | create |
| `grammar/css/totality/modules/rules*.bbnf` | create |
| `grammar/css/totality/extensions/value-js*.bbnf` | create |
| `crates/core/tests/css_profiles_*.rs` | create |

Do not modify P2 syntax modules or generic compiler/runtime source.

### Disjointness and worktrees

Color/Values, motion/images, and rules/extensions agents own disjoint grammar and
test files in sibling worktrees. One adjudicator integrates in dependency order.

### Agent units

#### P3.a Color and values profile

- **Goal:** cover sealed Color/Values rows with exact unresolved-context policy.
- **Mechanism:** small event-to-typed reducers over generated profile rules.
- **Files:** color/values grammar and tests.
- **Sub-gate:** every profile row and invalid boundary passes independent
  fixtures; project functions remain excluded.

#### P3.b Motion and image profile

- **Goal:** cover gradients, easing, keyframes, animation, and timeline syntax.
- **Mechanism:** colocated reducers with no browser scheduling/layout semantics.
- **Files:** images/animations/timelines grammar and tests.
- **Sub-gate:** all current Gradient and motion consumer cases are represented
  without context fabrication.

#### P3.c Rule and extension profile

- **Goal:** preserve generic rule structure and type only sealed rule/extension
  surfaces.
- **Mechanism:** rule-profile reducers plus a separately named `value-js`
  extension namespace.
- **Files:** rules/extensions grammar and tests.
- **Sub-gate:** normative, draft, opaque, and extension denominators remain
  disjoint and sum to the sealed total.

### Hard gate

1. Profile manifest has no unresolved row.
2. Reference/differential cases pass for every typed row.
3. Syntax-event checksums remain identical to P2.
4. Context-required values survive with source spans and do not become invalid.
5. No extension appears in normative CSS conformance counts.

### Format and lint cadence

Grammar formatting/schema and targeted module tests after each batch; full
compiler/workspace gates at close.

### Verification artefacts

Per-profile coverage JSON, typed-output checksums, extension manifest, and
unchanged P2 event receipt.

### Commit plan

1. `feat(css-color-values): generate typed normative value profiles`
2. `feat(css-motion): generate gradient, easing, animation, and timeline profiles`
3. `feat(css-rules): add generic rule profiles and isolated value-js extensions`

### Dependencies

- **Depends on:** P2 - CSS Syntax event authority.
- **Blocks:** P4 - Consumer clean break.

### Archaeology

The old l4 grammar combines broad fallback rules, host actions, backend
discriminants, and property syntax. P3 retains useful language research but not
that execution architecture.

## 10. P4 - Consumer clean break

**Name:** P4 - Consumer clean break
**Opens after:** P3 typed profiles
**Agents:** three, one substrate adapter then two disjoint consumer cuts
**Hard gate:** all `/css` exports and demo consumers use the generated authority;
the old structural scanners and fallbacks are deleted
**Status:** planned

### Goal criterion

P4 succeeds if value.js exposes a smaller failure-explicit CSS API whose current
consumers are green and whose invalid inputs, recovery, spans, and extension
profiles are browser-verified.

### Scope

1. Add one value.js adapter from generated events/typed outputs to public result
   types.
2. Transpose color/value/gradient/easing consumers.
3. Transpose stylesheet/animation/timeline/property consumers.
4. Exercise every affected demo route in the in-app Browser.
5. Delete `splitTopLevel`, `splitValueTokens`, block scanners, and unreachable
   public exports in the same cut.
6. Delete obsolete tests only when the replacement matrix cites stronger cases.

### Triumvirate dispatch

Trigger if an export lacks a consumer/disposition, deletion requires a shim, a
browser failure cannot be reproduced in a unit test, file bounds widen beyond
two paths per unit, or the same adapter defect returns after two repairs.

### File bounds

| File | Access |
|---|---|
| `src/css/generated-adapter.ts` | create |
| `src/css/grammar.ts` | modify-carve/delete when empty |
| `src/css/stylesheet.ts` | modify-carve/delete when empty |
| `src/css/timeline.ts` | modify-carve |
| `src/css/syntax.ts` | modify-carve |
| `src/css/types.ts` | modify-carve |
| `src/css/index.ts` | modify-carve |
| `src/subpaths/css.ts` | modify-carve |
| `test/parser-totality-*.test.ts` | create |
| exact demo call-site paths from `CSS-CONSUMER-MATRIX.tsv` | modify-carve |

Do not modify BBNF/parse-that compiler source or glass-ui.

### Disjointness and worktrees

The adapter lands first. Color/value and stylesheet/motion consumer units then
use separate sibling worktrees and non-overlapping files resolved by the P0
matrix. Shared barrel/type changes remain orchestrator-owned and serial.

### Agent units

#### P4.a Public adapter

- **Goal:** expose generated outcomes as minimal immutable `ParseResult` values.
- **Mechanism:** one adapter; no generated-AST clone or hidden fallback.
- **Files:** adapter, types, and orchestrator-owned barrels.
- **Sub-gate:** ordinary invalid inputs never throw; exact spans survive.

#### P4.b Color and value consumers

- **Goal:** move color/value/gradient/easing consumers and remove their old
  structural scans.
- **Mechanism:** use typed profiles and preserve extension/context distinctions.
- **Files:** matrix-owned color/value call sites and tests.
- **Sub-gate:** unit/e2e/browser matrices pass; old helper references are absent
  because the files are deleted, not because grep is the runtime proof.

#### P4.c Stylesheet and motion consumers

- **Goal:** move stylesheet/keyframe/timeline/property consumers with normative
  recovery.
- **Mechanism:** generic rule events plus typed profile reducers.
- **Files:** matrix-owned stylesheet/motion call sites and tests.
- **Sub-gate:** invalid declaration recovery, nested rules, custom-property
  blocks, at-rule preservation, and current routes pass.

### Hard gate

1. Build, typecheck, lint, unit, and e2e tests pass.
2. Export and consumer matrices have terminal receipts.
3. The four empty-color crash probes return structured failures.
4. Bad-declaration recovery and custom-property brace-block probes pass.
5. In-app Browser journeys cover Gradient, animation/timeline, palette color
   entry, and any parse echo/error UI at desktop and narrow widths.
6. Replacement TSV proves deleted symbols, consumers, new owners, tests, and
   commits; no alias/shim/fallback remains.

### Format and lint cadence

Run formatter/lint/typecheck after each integration batch; targeted tests during
development and the full suite before close.

### Verification artefacts

Replacement TSV, export crater, test logs, browser screenshots/console receipt,
bundle/API-size diff, and deletion diff.

### Commit plan

1. `feat(css-adapter): expose generated lossless and typed parse outcomes`
2. `refactor(css-color-values): cut consumers to generated profiles`
3. `refactor(css-stylesheet-motion): cut consumers to generated rule profiles`
4. `refactor(css-parser): delete handwritten structural scanners and fallbacks`

Broad/deletion commits require bodies.

### Dependencies

- **Depends on:** P3 - Typed module profiles.
- **Blocks:** P5 - Equivalent-plane profiling.

### Archaeology

Value 2.0.1 consumed parse-that; Value 4.0 removed it and rebuilt handwritten
splitting. P4 is a clean break, not a reintroduction of the old API shape.

## 11. P5 - Equivalent-plane profiling

**Name:** P5 - Equivalent-plane profiling
**Opens after:** P4 consumer clean break
**Agents:** three parallel harness/profile lanes, then serial adjudication
**Hard gate:** raw paired receipts support PROMOTE/BANK/REJECT for every
candidate; no unequal-work speed claim survives
**Status:** planned

### Goal criterion

P5 succeeds if the program knows where time, memory, allocations, branches,
binary size, and compile time go and admits only optimizations that move
representative end-to-end families without regressions.

### Scope

1. Run benchmark-plane P1, P2, P4, P6, and P7 equivalent-work rows.
2. Run A/A calibration before A/B.
3. Compare generated scalar P2 to the frozen reference and a proven-equivalent
   cssparser adapter.
4. Compare typed/transform rows to current value.js and Lightning CSS only where
   retained work is identical.
5. Capture CPU, allocation, retained-memory, binary/generated-size, and compile
   profiles.
6. Evaluate scalar classifier and NEON only at the predeclared activation row.
7. Record PROMOTE/BANK/REJECT and delete rejected candidate code.

### Triumvirate dispatch

Trigger if A/A noise invalidates the decision budget, equivalent checksums
diverge, a profile identifies a different hot mechanism than planned, any
non-pathological family regresses above the valid budget, or three measurement
reruns fail environmental controls.

### File bounds

| File | Access |
|---|---|
| `crates/core/benches/css/totality_*.rs` | create/modify-carve |
| `crates/core/benches/css/adapters/*.rs` | create |
| `restart/skinny/tranches/<canonical>/artifacts/CSS-PROFILE-*.json` | create |
| `restart/skinny/tranches/<canonical>/artifacts/CSS-CHECKASM.json` | create |
| `docs/tranches/V/megatranche/prototypes/css-parser/bench/**` | modify-carve |

Do not change parser semantics, grammars, or product consumers in response to a
timing result. A semantic change returns to its owning prior wave.

### Disjointness and worktrees

Benchmark-plane P2, benchmark-plane P4/P7, and profile/checkasm agents own
separate harness/adapters. Run in fresh worktrees with unique targets and
profile directories outside active worktrees. Adjudication is serial.

### Agent units

#### P5.a Token/event plane

- **Goal:** measure equivalent full-consumption benchmark-plane P1/P2 behavior.
- **Mechanism:** paired interleaving, checksums, cssparser adapter, corpus axes.
- **Files:** benchmark-plane P1/P2 benches and raw receipt.
- **Sub-gate:** semantic/error equivalence and calibrated confidence report.

#### P5.b Typed/transform plane

- **Goal:** measure equivalent benchmark-plane P4/P6/P7 consumers.
- **Mechanism:** current/published controls with identical decoding, retention,
  transform, and serialization.
- **Files:** typed/transform benches and raw receipt.
- **Sub-gate:** every row names retained work and rejects incomparable controls.

#### P5.c Profiles and adjudication

- **Goal:** select the smallest proved implementation.
- **Mechanism:** CPU/allocation/checkasm profiles plus simultaneous-interval
  decision rules.
- **Files:** profile/checkasm/decision receipts.
- **Sub-gate:** every candidate is PROMOTE, BANK with trigger, or REJECT with code
  removed.

### Hard gate

1. A/A receipt precedes A/B and establishes valid noise floors.
2. At least 40 paired samples per sealed family or the predeclared larger
   stopping rule completes.
3. Raw samples, hashes, environment, compiler flags, checksums, allocations,
   retained bytes, size, and compile time are present.
4. Promotion meets confidence, practical floor, representative-family, and
   regression gates.
5. CPU profiles resolve to symbols; rejected optimized code is deleted.

### Format and lint cadence

Format/lint harnesses after each integration; schema-validate every raw receipt;
rerun correctness before and after profiling.

### Verification artefacts

A/A, raw paired samples, summary intervals, CPU profiles, allocation profiles,
checkasm/differential receipts, environment manifest, and candidate
dispositions.

### Commit plan

1. `bench(css-p2): add equivalent token-event controls and sealed corpora`
2. `bench(css-typed): add equivalent typed and transform controls`
3. `perf(css-parser): promote only adjudicated end-to-end kernels`
4. `docs(css-profile): record raw receipts and rejected candidates`

Every benchmark/profile/performance commit has a body.

### Dependencies

- **Depends on:** P4 - Consumer clean break.
- **Blocks:** P6 - Totality close.

### Archaeology

Prior CSS benches were sequential, small, sometimes dependent on ignored
fixtures, and often compared unlike retained work. P5 applies the existing
benchmark constitution rather than inventing another gate.

## 12. P6 - Totality close

**Name:** P6 - Totality close
**Opens after:** P5 performance adjudication
**Agents:** three serial close/audit units; read-only close lanes follow the
precepts
**Hard gate:** every promise and residue is terminal, all close lanes pass, and
the final document cites commits and artifacts
**Status:** planned

### Goal criterion

P6 succeeds if the parser gestalt is smaller, all current consumers are proven,
no legacy execution path remains, and a future session can reproduce every
claim from committed artifacts.

### Scope

1. Reconcile spec/profile/consumer/corpus/benchmark manifests.
2. Delete legacy CSS grammar/runtime/bench artifacts that have terminal
   replacements.
3. Archive historical research without leaving it as authority.
4. Run alpha/beta/gamma/delta/epsilon/pi/iota close lanes.
5. Update parser, value.js, BBNF, parse-that, demo, and tranche docs.
6. Produce `FINAL.md`, exact resumption state, and cross-repo receipts.

### Triumvirate dispatch

Trigger if any residue has no terminal disposition, close-lane findings expand
source bounds beyond two paths, a third post-close repair is needed, or
cross-repo commit identities diverge.

### File bounds

| File | Access |
|---|---|
| legacy paths named `RETIRE` in replacement manifests | delete |
| parser/value.js/BBNF/parse-that public documentation named by consumer matrix | modify-carve |
| `restart/skinny/tranches/<canonical>/FINAL.md` | create |
| `restart/skinny/tranches/<canonical>/PROGRESS.md` | modify |
| `docs/tranches/V/megatranche/PARSER-RESUME-HANDOFF-*.md` | modify-carve |

Do not delete historical evidence without an archive pointer and terminal row.

### Disjointness and worktrees

Deletion, documentation reconciliation, and audit are serial against an
integrated clean candidate. Read-only audit lanes may run concurrently under the
precepts and do not write shared files.

### Agent units

#### P6.a Residue subtraction

- **Goal:** leave one execution path and no unconsumed parser surface.
- **Mechanism:** replacement matrix, consumer crater, package/tree inspection,
  then exact deletion.
- **Files:** terminal `RETIRE` paths only.
- **Sub-gate:** no current consumer resolves to deleted or legacy paths; package
  contents and generated output contain one authority.

#### P6.b Documentation reconciliation

- **Goal:** make every public and tranche claim match landed behavior and
  evidence.
- **Mechanism:** update docs from receipts, not memory.
- **Files:** bounded docs only.
- **Sub-gate:** no “full CSS,” SOTA, or completion claim exceeds its semantic
  plane/profile.

#### P6.c Close adjudication

- **Goal:** decide whether goal and completion criteria both hold.
- **Mechanism:** Sol xhigh aggregates all audit lanes and writes the final.
- **Files:** final/progress/handoff.
- **Sub-gate:** every finding lands, retires, or archives permanently; no silent
  deferral.

### Hard gate

1. Full BBNF, parse-that, and value.js build/lint/test/package/e2e gates pass.
2. In-app Browser journeys and console/network receipts pass.
3. Regen and profile receipts reproduce from exact commits.
4. Consumer/replacement/profile ledgers contain zero open row.
5. Seven close lanes and iota reflog/stash/precept integrity checks pass.
6. `FINAL.md` reconciles the goal criterion and cites exact commits/artifacts.

### Format and lint cadence

Run repository format/lint/test commands after deletion integration and before
final; docs checks plus `git diff --check` at close.

### Verification artefacts

Final/progress docs, close-lane reports, browser receipt, package crater,
replacement ledger, profile packet, reflog/stash integrity receipt, and exact
cross-repo commits.

### Commit plan

1. `refactor(css-parser): remove replaced grammar, runtime, and benchmark residue`
2. `docs(parser-totality): reconcile public contracts and evidence`
3. `docs(parser-totality): close tranche with audit and replay receipts`

Deletion/status commits require bodies.

### Dependencies

- **Depends on:** P5 - Equivalent-plane profiling.
- **Blocks:** parser totality release and any SOTA/equivalence claim.

### Archaeology

Earlier tranches treated handoffs and ledgers as completion. P6 closes only on
product bytes, consumer behavior, profiles, deletion, and reproducible receipts.

## 13. Program completion criterion

The canonical parser tranche may close only when:

1. P0–P6 goals and hard gates all hold.
2. W3 remains historically reproducible at its frozen identity.
3. Full CSS claims resolve to the syntax floor plus exact profile matrix.
4. All 52 current `/css` exports and every live consumer have terminal rows.
5. The four reproduced crashes, recovery defect, and custom-property block defect
   are covered by generated-path tests.
6. Handwritten structural scanners and old execution paths are deleted.
7. Benchmark-plane P2/P4/P7 profiles contain equivalent-work raw receipts and
   honest decisions.
8. No extension contaminates normative conformance counts.
9. No alias, shim, fallback, unconsumed substrate, or open brittleness window
   remains.
10. The final in-app Browser, package, regen, close-audit, and integrity receipts
    are committed and replayable.

## 14. Explicit exclusions

- DOM layout, selector matching, cascade, inheritance, computed values, painting,
  animation scheduling, and browser API effects are consumer/runtime semantics,
  not parser-close evidence.
- Browser support is not the normative oracle.
- The TypeScript reference prototype is not production.
- W4 scanner/NEON work is not implicitly authorized by this addendum.
- No held-out SK-V26 grammar or result may be exposed to P0–P6 before the active
  W3 selection protocol permits it.
