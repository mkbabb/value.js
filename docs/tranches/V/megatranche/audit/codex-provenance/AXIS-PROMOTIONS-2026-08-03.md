# AXIS PROMOTIONS — the r2 re-runs govern (provenance ledger row 9c)

**Rule.** Canonical Codex-authored files are NEVER rewritten (C-02 discipline: receipts literal,
corrections visible as separate artifacts). Where an r2 file exists, **the r2 file governs** the
axis for adjudication and wave authoring; the canonical file remains as dated evidence of its era.
The hash-banked canonical path is unchanged, so the completeness validator stays GREEN and honest.

## Promoted 2026-08-03 (commissioned re-runs, live-stack workflow challenges)

| component | axis | r2 file | why it governs |
|---|---|---|---|
| `ActionFeedback` | L | `challenge-L-library-r2.md` | r1's RED grade survives; **none of its reasons do** (motion claim false 6/6 tokens live + PRM clamp 0.57s→0.1s; "local duplicate" is a repo-wide singleton; cited prop `type` does not exist — it is `variant`; prescribed toast contract structurally wrong, Toaster is viewport-anchored). Four new MAJORs, three live-only. |
| `picker-pointerdebugoverlay` | D | `challenge-D-design-r2.md` | r1's D#4 reduced-motion claim proven false (M-28/M-29); re-challenged in the live browser incl. the a11y/clipboard cluster. |
| `picker-componentsliders-consolerail` | D | `challenge-D-design-r2.md` | r1 rested on the cross-product matrix class retired under L-19; re-challenged against the live component. |

## Also present (earlier band deploys, same governing rule)

`wb-gradient-easingeditor` D/L/C · `wb-gradient-visualizer` L/C · `wb-gradient-stopeditor` L ·
`wb-gradient-easingauthoringstage` D.

## Headline carries into tranche X

- **ActionFeedback L-3 + L-4 are ONE repair** (X-W7): the `cardRefs` imperative registry has no
  teardown contract — Vue's null-on-unmount is swallowed by the `el &&` guard, so entries are
  retained forever and a verdict addressed to a dead card **throws nothing, renders nothing, logs
  nothing** (measured: 2 entries vs 1 mounted card; user's publish/save/delete outcome vanishes);
  and the three-prop surface carries no verdict identity, so a second verdict inherits the first's
  clock and lives 701ms of its designed 2500ms. Cure: hoist an identity-carrying verdict object
  into pane state — deletes both registries, both `(el: any)` casts, the `defineExpose`, the leak,
  the silent-loss path and the clock bug.
- **ActionFeedback L-1** (X-W8/X-W10): tone-axis fork — success rung hand-rolled from Tailwind
  literals while the error rung one line away matches `--destructive` byte-exactly; `--color-success`
  bridge existed and was available. Violates the sealed axis three ways (prop name, vocabulary,
  rendering).
- **ActionFeedback L-2** (X-W0/X-W1): the G-DEMO-3b eslint boundary that `browser/index.ts:6-7`
  claims "enforces it standing" is **wholly inert** — globs name the deleted `demo/@` tree, the
  banned specifier has 0 live imports, `no-restricted-imports` resolves UNSET across the whole
  palettes tree. A dead gate asserting live enforcement: L-19's exact species.
- **MQ-1 (HIGH, new)** from the quarantine sweep → X-W6 motion-sourced gates.

## Motion quarantine — QUARANTINE LIFTED, with corrections

`motion-quarantine.md`: 46 files swept, **34 motion assertions adjudicated across 16 files —
14 TRUE · 15 FALSE · 5 UNPROVEN**, 1 new defect (MQ-1 HIGH), 4 superlatives. Of 13 reduced-motion
raisers **exactly one** checked the gating; `demo/styles/animations.css` is cited **zero** times in
all 46 files. The prior pass's 3 statically-suspected false claims are all **probe-confirmed**, and
4 more false claims plus MQ-1 were found beyond it. Four re-runnable probes under
`evidence-motion-quarantine/`, each measured at both `no-preference` and `reduce`.

**Consequence:** the blanket quarantine on the 46 files' motion content is LIFTED and replaced by
this per-assertion adjudication. Wave authors cite `motion-quarantine.md` row IDs (M-xx); the 8
substantive FALSE rows may not be carried as defects; the 5 UNPROVEN rows need measurement before
any gate binds to them.
