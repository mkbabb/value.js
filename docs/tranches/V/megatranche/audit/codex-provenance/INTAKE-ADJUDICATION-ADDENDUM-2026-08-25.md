SERVED MODEL: claude-opus-5[1m]

# INTAKE-ADJUDICATION ADDENDUM — 2026-08-25 — the NEXT-COMMUNIQUE amendment and the Codex-residue terminal verbs

⟨**ADDENDUM-BESIDE (E-3).** The dated `INTAKE-ADJUDICATION-2026-08-03.md` is **never rewritten**: not
one byte of it is edited by this file, and the receipt (`git diff --stat` → empty, double-run) is
published in the execution record, outside this write. **Authority**: `docs/tranches/X/fourier/waves/F-W10.md`
§1a row 7 (*"CREATE BESIDE (E-3 shape) — the NEXT-COMMUNIQUE amendment (G-F10-9). The dated intake bytes
never rewritten"*) · §2.4's **NEXT-COMMUNIQUE-CONTRADICTION** row · §2.3's **CODEX-R3–R6 residue** row ·
§3 gates **G-F10-8** and **G-F10-9**. **Seat**: X.F.W10.b, under the owner's begin-word of 2026-09-17
(`docs/tranches/X/COHESION.md` §0j). **Two dates, both stated rather than smoothed**: the FILENAME carries
**2026-08-25** because F-W10 §1a fixes the path so no seat invents a second channel; every ACT below was
performed on **2026-09-20** and every measurement is this seat's own, double-run. **This addendum rules
nothing the owner ruled** — OG-F1 and OG-F2/OG-V2 are RULED at COHESION §0j.D and are cited by id below,
never re-opened (F.W0 rules; F.W10 verifies).⟩

---

## §A What this addendum amends — and what it does not

**Amends**, by standing beside them:

1. the dated intake's `### NEXT-COMMUNIQUE` declaration at its own `:221` — amended in **reading**, not in
   bytes: both statements stand dated (§B);
2. the dated intake's `### F.W0 · re-ground` carry block at its own `:168–174` — each Codex-residue row
   named there receives a **terminal verb** (§C).

**Does not amend, and does not touch**: the intake's totals (§1), its AGREE/CONFLICT register (§2), any
other `### <wave>` carry block of its §3, its §4 owner-ruling table, its §5 closing statement, or the
`## KF.W0 EXECUTION OUTCOME — APPENDED 2026-09-17` section at its tail. **Does not book work in any tree.**
**Does not cure one producer row consumer-side** — the standing owner edict, and G-F10-9's own falsifier.
**Does not re-enumerate another holder's roster**: where a roster belongs to another register or another
wave, it is **cited by holder**, because a second enumeration is a rival operand — the defect R2-6b and
R4-3 convict, and the one this sub-tranche has been repaired for three times.

---

## §B The NEXT-COMMUNIQUE amendment — both statements stand dated

### §B.1 The dated bytes, quoted by command, and the amended reading

⟨cmd⟩ (this seat, read-only) `sed -n '221,225p' INTAKE-ADJUDICATION-2026-08-03.md` →

> ### NEXT-COMMUNIQUE
> **EMPTY.** Zero producer-facing asks survived adjudication in any lane; the glass lane states it
> explicitly (no `FOR-NEXT-BATCHED-COMMUNIQUE` row issues). Every surviving action is value-side
> hygiene, wave input, or already glass-owned and self-cured.

**The amendment, in one sentence: that statement is TRUE of the corpus it adjudicated and FALSE of the
registry that followed it, and neither half is withdrawn.**

- **TRUE, dated 2026-08-03, of the Codex intake corpus.** The intake adjudicated the Codex lanes — 364
  claims across the R3–R6, B-lineage and V-lineage packets — and out of *that* corpus no producer-facing
  ask survived. Nothing below convicts it; it is not re-graded, and its bytes are not touched.
- **FALSE, dated from the 66-record adjudicated fourier registry onward**, which was authored after the
  intake and carries live producer-facing asks in two distinct producer trees. The channel is **not
  empty**. Every surviving ask sits in **EXACTLY ONE** register, and the two registers are disjoint by
  measurement (§B.4), not by assertion.

**Register assignment is all this addendum performs.** It moves no row, re-books no id, and writes no
producer tree: **glass-ui is READ-ONLY always** (F-W10 §1c), and producer rows ride their register, never
a frontend hack.

| # | register | what routes there | who holds it |
|---|---|---|---|
| 1 | **SS-6** — the standing glass-ui BH relay; live inbox path `glass-ui/docs/tranches/BK/coordination/` (BJ retained as the historical record) | the **seventeen** glass arms enumerated at §B.2, one row each | accretion into `COHESION.md` §4a is **F.W10 unit `c`'s** act (G-F10-9's register limb); the head arm is already **dispatched and receipted** at O-20 row **A-1** |
| 2 | **LATEX-PAPER-RELAY** — the register F-W10 §2.6 stands up (`docs/tranches/X/fourier/LATEX-PAPER-RELAY.md`) | the §2.6 carry roster **whole**, cited by holder and deliberately **not re-enumerated here** | **F.W10 unit `d`** (G-F10-5); at this seat's clock the file does not yet exist, and that is stated at §E rather than stamped |

### §B.2 Register 1 — **GLASS → SS-6**, seventeen arms, one register each

**Transcribed from F-W10 §2.4's own NEXT-COMMUNIQUE row** (the wording is the spec's, not this seat's;
ids keep their banked identity for life and not one is re-booked, re-graded or renamed here). The roster
is published as an **enumeration**, because the enumeration is the falsifier; **this file publishes no
count of itself** — the settled count is measured from outside, in the execution record (self-count law).

| # | arm (banked id, kept) | the ask, in the spec's own words | register | state at this seat's clock |
|---|---|---|---|---|
| 1 | **FR-NP-32 ≡ fr-PaperSidebar M1** (cite-both, never substitute) | *"the corrupt dist, BLOCKER, at the TOP of the FR-COB-28 packet per its banked disposition"* | **SS-6** | **DISPATCHED AND RECEIPTED** — O-20 row **A-1**, `glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md`; receipt READ at §D row 1, never asserted |
| 2 | **FR-COB-7** | *"the rest of the FR-COB-28 packet"* (§2.5a) | **SS-6** | accretes at §4a (unit `c`) |
| 3 | **FR-COB-8** | idem | **SS-6** | accretes at §4a (unit `c`) |
| 4 | **FR-COB-23** | idem | **SS-6** | accretes at §4a (unit `c`) |
| 5 | **FR-COB-26** | idem | **SS-6** | accretes at §4a (unit `c`) |
| 6 | **HLG-32** | *"folded to their producer classes"* — folded, **never re-booked** | **SS-6** | accretes at §4a (unit `c`) |
| 7 | **FR-MSP-12** | *"folded to their producer classes"* | **SS-6** | accretes at §4a (unit `c`) |
| 8 | **FR-NP-11** | named in the packet roster | **SS-6** | accretes at §4a (unit `c`) |
| 9 | **D-26** | named in the packet roster | **SS-6** | accretes at §4a (unit `c`) |
| 10 | **L-10/D-M4** | *"ramp headroom + rebaseline, 4 failing light stops incl. 3.58:1 at stop 11"* | **SS-6** | accretes at §4a (unit `c`) |
| 11 | **GAB-2** | *"ask (a) tier-tint home"* | **SS-6** | accretes at §4a (unit `c`) |
| 12 | **GAB-9** | *"ask (b) 3.26:1 stacked-badge label rung"* | **SS-6** | accretes at §4a (unit `c`) |
| 13 | **AA-3** | *"violet `batch` tone, no home in the 5-member TONES"* | **SS-6** | accretes at §4a (unit `c`) |
| 14 | **FR-EQR** (light `--success`) | *"light `--success` 2.175:1"* | **SS-6** | accretes at §4a (unit `c`) |
| 15 | **PaperView D/M-7** | *"light `--primary` ≡ `--foreground`"* | **SS-6** | accretes at §4a (unit `c`) |
| 16 | **fr-AppHeader** (two limbs, ONE arm as the spec words it) | *"cm-serif undeclared variable + light `--tier-featured`"* | **SS-6** | accretes at §4a (unit `c`) |
| 17 | **FR-GIG-5** | *"observer-root fix"* | **SS-6** | accretes at §4a (unit `c`) |

**NO GLASS ROW IS EVER CURED CONSUMER-SIDE** — the standing owner edict, restated at the register that
carries these rows, and G-F10-9's explicit falsifier. Nothing in this addendum prescribes a consumer-side
repair for any of the seventeen; each routes, and the cure is the producer's.

### §B.3 Register 2 — **LATEX-PAPER → the §2.6 register**, cited by holder

The latex-paper asks route to the register `F-W10 §2.6` stands up — `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md`,
bounds row §1a #2, gate **G-F10-5**, authored by F.W10 unit `d`. **Its roster is §2.6's, whole.** This
addendum **does not restate it**: a roster restated in a second file is a rival operand that will drift
from its holder, and the anchor law (SC-3 — every anchor from `dist/vue.js`, never producer-src) belongs
to the register, not to this note. What this addendum settles is only the **register**: the latex-paper
carries have exactly one, and it is not SS-6.

### §B.4 "EXACTLY ONE register" — the disjointness receipt, measured

⟨cmd⟩ this seat, 2026-09-20, read-only: the §2.6 region of `F-W10.md` was extracted whole
(`awk 'NR>=367 && NR<=388'`; the heading `### §2.6 …` re-resolved at **:367** and `### §2.7` at **:389**
before the extraction — anchors verified at true bytes, FR-CP-45's law) and each of the seventeen arms of
§B.2 was probed against it. **Total hits = 0**, run twice, identical both runs. No arm of register 1
appears in register 2's roster; no ask is double-registered.

**Disclosed against this receipt's own interest — one homonym, found by the probe and not hidden.** The
bare token `L-10` returns **1** hit inside §2.6, and it is **not** arm 10: the hit is *"PaperSidebar L-10 ≡
★MF-10"*, the consumer-side fallback-less `var(--section-color-${si})` row named inside §2.6's **C-20**
cell — a different id sharing a short spelling. The disambiguated probe `grep -c -- 'L-10/D-M4'` over the
same region returns **0**. R-5's rule is exactly this: an unqualified short token is not an identity.

### §B.5 What is NOT a producer ask — named, so the silence is not read as a drop

The amendment above is about **producer-facing** asks. Three classes of surviving action are *not*
producer asks and route by their own law; they are named here so no reader takes "two registers" to mean
"everything lives in two registers":

- **fourier-tree acts** — asked, never made (COMMISSION §2, their sub-session). The four ADOPTION-ASKS,
  the FN charter and the 2.0.0 note are answered in the letter whose path F-W10 §1a row 5 fixes, by
  F.W10 unit `e`; **no third channel is invented, here or anywhere.**
- **the keyframes half of ASK-5** — KF's mail seat; **declared ∦, not absorbed** (F-W10 §5), so no id is
  double-homed.
- **value-side hygiene and wave input** — the F.W0 / F.W1 / F.W3 / F.W4 / F.W9 work in their own specs.
  The intake's own closing clause (*"value-side hygiene, wave input"*) survives whole and untouched.

---

## §C The Codex residue — five rows carried to F.W0, five terminal verbs

**Operand**: the dated intake's `### F.W0 · re-ground` block (`:168–174`) and the frozen lane intake it
summarises, `intakes/lane-fourier-r3-r6.md` (immutable evidence; quoted, never edited). **Law**: OG-F1 is
RULED **FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE** and OG-F2/OG-V2 **CODEX-ERA-SPECIFIC** at
COHESION **§0j.D** — cited by id, **never re-ruled here** (*"G-F10-8 closes on the charter line"*, §0j.D's
own words). **A terminal verb is a disposition with a falsifier, not a deferral wearing a verb**; each row
below carries both, and **none books work in any tree**.

| row(s) | banked wording (quoted by command from `intakes/lane-fourier-r3-r6.md`, this seat, read-only) | grade at intake | **TERMINAL VERB** |
|---|---|---|---|
| **R3-9** | *"No authored auditor, product module, API, service, Browser, Safari, Simulator, Docker, package, or parser code was imported or executed."* — the intake's carry: *"record, never cite as method guarantee"* | UNPROVEN (an unfalsifiable negative from a frozen artifact) | **RECORD-ONLY — NEVER AN OPERAND.** It is already binding in exactly this form at F-W10 §Binding law (*"an assertion of non-execution is recorded, never cited as a method guarantee"*). No wave books work on it; it enters no denominator and no gate. **Falsifier: any seat citing R3-9 to establish that nothing was executed.** |
| **R3-16 + R4-11 + R5-9 + R6-10** (one board row, four restatements — never four rows) | *"Fourier formation remains 14/14 waves, 72/72 units, and 158/158 terminal rows. P29 remains 0/137 COMPLETE. … all five slots remain null."* · lane row **X-7**: *"AGREE on the wave count. The '72 units / 158 terminal rows / P29 0/137 / slots 0/5' superstructure is Codex-internal with no tree counterpart — does not transfer"* | UNPROVEN (partly TRUE) | **SPLIT BY LIMB, and the split IS the disposition object the intake named.** (a) the **14-wave board is TRUE and is fourier's own** — re-measured live at this seat (§C.2) → **CITE-THE-HOLDER: fourier `docs/tranches/M/M.md`, their sub-session (COMMISSION §2); value.js books nothing on it and no F wave adopts it.** (b) the **72/158/P29/slot superstructure** → **EXCLUDED-WITH-REASON: Codex-internal, no tree counterpart, does not transfer** (F-W10 §5 carries the same exclusion). **Falsifier: any value-side figure derived from the superstructure; any F wave booking work on fourier's M board.** |
| **R4-12** | *"a read-only census command inadvertently materialized `/tmp/fourier-r4-files.sha256` outside the authorized writer root … one regular mode-`0644`, nlink-1 file, 74,507 bytes and 339 lines, SHA-256 `e70543907d…`. It is preserved without cleanup."* | TRUE at the lane's clock (2026-08-03) | **MOOT — KILL-WITH-RATIONALE.** The artifact is **GONE**: ⟨cmd⟩ `ls -la /tmp/fourier-r4-files.sha256` → *"No such file or directory"*, run twice at this seat. The banked *"74,507 B still on disk"* is **stale and is named stale**, not quietly dropped. The intake's standing prohibition — *"consume none of its rows"* — is **DISCHARGED AND MEASURED**, not asserted: receipt at §C.1. **Falsifier: any live figure in the formation traceable to a row of that file.** |
| **R5-3** | *"Terminal receipt SHA is `7c1b1d59…`. No regular-file mtime follows it."* — the lane's carry: *"an unverified pin; re-hash if the coordinate is ever cited, or drop it"* | UNPROVEN (not re-hashed before Codex-root access withdrawal) | **DROP — the second disjunct, taken.** Under OG-F1's FREEZE-WITH-ADOPTION the R-coordinates are frozen and every measurement is live-reproduced, so the coordinate is **never cited for a live count**; the re-hash disjunct has no consumer and its root is gone by the lane's own record (*"Operation not permitted"*). Receipt at §C.1: the pin token appears in **five PRIOR** value-side files (enumerated there — two 2026-08-02 FOURIER-R5 letters, the 2026-08-02 audit matrix, the pinned `apotheosis/pi/HANDOFF.md`, and the lane intake that graded it) and in **ZERO** files under `docs/tranches/X/`; this addendum's own naming of it makes a sixth, and that is disclosed at §C.1 rather than folded into the figure. **Falsifier: a live count resting on `7c1b1d59…`; a re-hash claim without the root.** |
| **the Codex worktree `~/.codex/worktrees/d0be/fourier-analysis`** (the intake's own observation: *"still registered — F.W0 should disposition it"*) | re-measured at this seat, 2026-09-20: ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis worktree list` → `/Users/mkbabb/.codex/worktrees/d0be/fourier-analysis  cd26c65 (detached HEAD)`; the directory exists | still registered | **FROZEN-AND-RETAINED AS A RECORD — NO WAVE ACT, and the reasons are three, each checkable.** (i) `~/.codex/**` is **explicitly NOT in bounds** (F-W10 §1c) — no F wave may touch it, so a wave verb that moved it would be an out-of-bounds write. (ii) OG-F1 ruled **FREEZE-WITH-ADOPTION**: the coordinate's substrate is a **record**, and §0j.D adds that *"the `FOURIER-R*` value-side files are records"* — the same class. (iii) **Nothing in the formation reads from it**: every fourier measurement in this sub-tranche is taken at the live tree (this seat's at HEAD `cef242d`), never at `cd26c65`. Pruning it is an **OWNER ACT**, available at any moment, blocking nothing and booked by no wave — named here so the silence is not read as an oversight. **Falsifier: any seat measuring a figure from that path and citing it as live.** |

### §C.1 The consume-nothing receipt — R4-12 and R5-3, measured rather than asserted

All probes this seat, 2026-09-20, in `/Users/mkbabb/Programming/value.js`, **run twice with identical
output both runs**. The distinction they keep is the operative one: **naming a residue in order to
disposition it is not consuming it**; consuming it would mean a live figure resting on its contents.

| probe | reading | what it settles |
|---|---|---|
| `grep -rl 'e70543907d' docs/ \| wc -l` | **4** | R4-12's sha is *named* in four files: the 2026-08-02 `FOURIER-R4-TERMINAL-SOURCE-RED-INTAKE`, the 2026-08-02 `CONSTELLATION-REMAINING-AUDIT-MATRIX` json, the lane intake that graded it — **and this addendum, which names it to kill it** |
| `grep -rl 'e70543907d' docs/tranches/X/ \| wc -l` | **0** | **no row of R4-12 is consumed by the live formation**: the X tranche — every F spec, every conformance check, every execution record — rests on it nowhere |
| `grep -rl 'fourier-r4-files' docs/tranches/X/ \| wc -l` | **13** | thirteen X-side files *name the residue*; each names it to disposition it (the gate text, the check files, F.W0's and F.W10's records). **Naming ≠ consuming**, and the sharper probe above is the one that would catch a consumption |
| `grep -rl '7c1b1d59' docs/ \| wc -l` | **6** | R5-3's pin is *named* in six files: `V/apotheosis/pi/HANDOFF.md` · `V/megatranche/coordination/CONSTELLATION-REMAINING-AUDIT-MATRIX-2026-08-02.json` · `…/FOURIER-R5-TERMINAL-SOURCE-RED-INTAKE-2026-08-02.md` · `…/FOURIER-R5-TERMINAL-INTEGRATION-AUDIT-2026-08-02.md` · `audit/codex-provenance/intakes/lane-fourier-r3-r6.md` — **and this addendum** |
| `grep -rl '7c1b1d59' docs/tranches/X/ \| wc -l` | **0** | **the DROP costs the formation nothing**: no X-tranche byte cites the pin, so dropping it removes no evidence from any live claim |

**Disclosed against this receipt's own interest (self-count law).** Both `grep -rl … docs/` figures moved
by **exactly one** while this file was being written, because this file quotes both tokens: the readings
before this addendum existed were **3** and **5**, and the settled readings are **4** and **6**. The
figures are published in their settled form with the writing file named inside each, rather than published
in the flattering pre-write form — and the two load-bearing legs (`docs/tranches/X/` → **0** and **0**) are
**untouched by this write**, since this file does not live under `docs/tranches/X/`.

### §C.2 The 14-wave board — measured live, cited to its holder, booked by nobody

⟨cmd⟩ this seat, read-only in `/Users/mkbabb/Programming/fourier-analysis` at HEAD **`cef242d`**, run
twice, identical both runs: `grep -cE '^\| *\*{0,2}M\.W[0-9]+' docs/tranches/M/M.md` → **14** board rows ·
`grep -oE 'M\.W[0-9]+' docs/tranches/M/M.md | sort -u | wc -l` → **14** distinct wave tokens (`M.W0 …
M.W13`). The lane intake's X-7 row — *"'14 waves' is TRUE and maps to a real board"* — **reproduces at the
live tree**, three sub-tranches after it was written. It is fourier's board, in fourier's tree, under
COMMISSION §2; **value.js cites it and books nothing on it**, and no F wave adopts it.

### §C.3 The two owner-gated legs — DISCHARGED BY RULING, verified, not re-ruled

**OG-F1** → **FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE** and **OG-F2 / OG-V2** → **CODEX-ERA-SPECIFIC**,
both at `docs/tranches/X/COHESION.md` **§0j.D**, dated 2026-09-17 under the begin-word's delegation. This
addendum **verifies they are ruled and cites them by id**; it does not restate their reasoning as its own,
does not re-open them, and does not presume any ruling the owner has not given. The consequences §0j.D
itself states are the ones used above: the R-coordinate rows become **records**, the `FOURIER-R*`
value-side files are **records**, **GAB-13 discharges to a disclosure line** (given at §D row 2), and
*"G-F10-8 closes on the charter line"* — which is **not this unit's act**: the corrected §6.7 charter line
lands in `formation/fourier/CENSUS-ADDENDUM-2026-08-25.md`, authored by **F.W10 unit `a`**, and is verified
from outside in the execution record. **The dissent OG-V2 carries is preserved, not resolved away**: the
Codex chain violated its own prohibitions (XF-7, KF X-7); §0j.D's ruling is that those prohibitions died
with M-15's abrogation, while the *finding* — the absence, TRUE — stays adopted.

---

## §D The F.W0 substrate, VERIFIED at the bytes (F-W10 §2.3 · gate G-F10-11) — F.W0 rules, F.W10 verifies

**Recorded here because this addendum is the F-side surface where the Codex-era block is closed, and the
substrate rows sit in the same §2.3 table.** Every reading is this seat's own, 2026-09-20, **read-only**,
**run twice with identical output**, taken in `/Users/mkbabb/Programming/fourier-analysis` at HEAD
**`cef242d`**. **Not one of these cures is F.W10's**: each is a predecessor wave's landed act, and each is
named with its author so the close cannot bank a predecessor's work as this wave's.

| §2.3 row | born state (spec, 2026-08-28) | THIS SEAT, measured | verdict |
|---|---|---|---|
| **FR-NP-32 (≡ fr-PaperSidebar M1)** — **cite-both, never substitute**; the banked ids are original for life | glass-ui **4.0.0** `dist/styles/index.css`: `postcss.parse` → **35** top-level nodes, **ZERO** valid `@source`, garbage `` source` `` at-rule at `:203–222`; `vite dev`/`vite build` RED at the `^4.0.0` pin | at the **8.0.0** pin (COHESION §0i.3's election, `v8.0.0` @ `17a11bc5`): ⟨cmd⟩ `postcss.parse(node_modules/@mkbabb/glass-ui/dist/styles/index.css)` → **PARSE OK · 40 top-level nodes** (twice); valid `@source` at-rules → **1**, params `"../*.js"`; ⟨cmd⟩ `grep -c '@source'` → **1** · `grep -c 'source\`'` → **0**. Installed version **8.0.0** against `web/package.json` `"@mkbabb/glass-ui": "^8.0.0"` | **GREEN — by F.W1's atomic tri-package transaction**, verified here. The dist **parses**; the syntax error is gone with the pin. **The relay leg was already discharged and is READ, not asserted**: ⟨cmd⟩ on `glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md` → `grep -c 'FR-NP-32'` **1** · `grep -c '@source'` **1** · row **A-1** opens *"Corrupt published stylesheet (BLOCKER, fourier F.W0 pre-gate)"* |
| **GAB-13** — the audited artifact was the **DIRTY WORKTREE** (28 modified rows incl. `web/package.json`) | `git status --porcelain \| wc -l` → **exactly 28** | ⟨cmd⟩ `git status --porcelain \| wc -l` → **1** (twice), the single row being `?? .worktrees/` — this sub-tranche's own wave worktrees, **not a source edit** | **GREEN — the WT diff LANDED.** **And this line is GAB-13's disclosure line**, which is all OG-F1 leaves it owing (COHESION §0j.D: *"GAB-13 discharges to a disclosure line"*): *the 2026-08-03 audit measured a dirty worktree and said so; under WORKTREE-AS-BASELINE that was the lawful artifact; the diff has since landed, and at HEAD `cef242d` the committed tree holds the audited bytes, `@mkbabb/glass-ui` at `^8.0.0`.* DU's INFO grading, preserved verbatim in the spec, was right in the end — recorded, not re-graded |
| **F8-REACH-01 / F8-REACH-02** (`InfoCard.vue` N.C11 · `CanvasOverlayButton.vue` N.C39) — **HOLD IS RED by the source's own law** | both un-dispositioned | ⟨cmd⟩ `find web/src -name 'InfoCard.vue' -o -name 'CanvasOverlayButton.vue' \| wc -l` → **0** (twice). The verb is **G-10's `DELETE`** (COHESION §0j.D, *"in one breath"*), and it **executed in ONE commit**: ⟨cmd⟩ `git show --name-status 5842377` → `D web/src/components/equation/InfoCard.vue` · `D web/src/components/visualization/CanvasOverlayButton.vue`, in the same commit as the `aria-pressed` lift (`M CanvasControlsDock.vue` · `M EditorControlsDock.vue` · `M ConvergenceTimeline.vue` · `M docs/tranches/F/SUBSTRATE-LEDGER.md`), message *"refactor(F.W0): F8-REACH ruled — aria-pressed lift + rm CanvasOverlayButton"* | **GREEN — DELETE executed, NO HOLD SURVIVES.** No import survives either: ⟨cmd⟩ `grep -rn "from .*CanvasOverlayButton\|from .*InfoCard" web/src` → **0**. One residual token exists and is disclosed rather than swept: `web/src/style.css:349` names `fr-CanvasOverlayButton` inside a comment authored by **X.F.W3 `.e`** — a citation of the *record id*, not an import or a mount |
| **FR-COB-20** (`fr-CanvasOverlayButton:74`) — *"interaction contract entirely implicit; dies with the file; the tree favours DELETE"*; gated on F8-REACH-02 | HOLD un-dispositioned | its **entire claim on this wave** is the verification above — F-W10 §2.5's own words: *"F.W10 verifies no HOLD survives to close (that verification is this wave's entire claim — not a re-booking)"* | **VERIFIED — the file is gone, the verb was DELETE, no HOLD survives.** The id keeps its banked identity; **nothing is re-booked, re-graded or renamed**, and no work is manufactured by the disposition |

---

## §E Residuals, and what this addendum does NOT claim

1. **The latex register does not yet exist at this seat's clock.** ⟨cmd⟩ `ls docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` → absent. §B.3 settles the **register assignment**; the register's **existence** is gate **G-F10-5**, F.W10 unit `d`'s act. Stated, not stamped — an assignment to a register that must still be stood up is an honest half, and calling it whole would be the deferral-wearing-a-verb this wave exists to abolish.
2. **The SS-6 accretion of the seventeen arms into `COHESION.md` §4a is unit `c`'s act**, not this one's (G-F10-9's register limb). This addendum assigns; `c` accretes; **O-20 already carries the head arm and is never re-sent** — the F-side boundary reconciles against that dispatched packet.
3. **The §6.7 charter line is unit `a`'s landing**, in `formation/fourier/CENSUS-ADDENDUM-2026-08-25.md`. G-F10-8's charter limb is verified from outside, in the execution record; this addendum does not re-author it and does not quote the superseded wording as if it were live.
4. **The Codex worktree stays registered.** Its verb is RETAIN-AS-RECORD (§C), the prune is an owner act that blocks nothing, and `~/.codex/**` is out of bounds for every F wave.
5. **Two self-counting figures caught themselves mid-write** and are published in their settled form with this file named inside each (§C.1), rather than in the flattering pre-write form.
6. **No escalation.** Every act landed inside this unit's writable set; the specified cure was possible at the bytes and was performed as specified; no producer tree, no fourier tree and no registry record was written.
