SERVED MODEL: claude-opus-5[1m]

# SS-13 CAPTURE RECEIPT — X.KF.W9's surface receipt to KF.W10

**Seat** `.e` · **2026-09-17** · **Created under** `docs/tranches/X/keyframes/waves/KF-W9.md` §Bounds
**`:77`** (cited in the dispatch as `:81`; `:81` is §Disjointness — the drift is recorded in §5 and the
INTENT taken at the true bytes) · **create (`.e`, serial)**.

> ## THIS FILE RULES NOTHING.
> **OD-V3** and **OD-V5** are **OWNER-GATED** at KF.W10's own owner block — ⟨`grep -n 'PACKET-FIRST'
> KF-W10.md`⟩ → `:519` *"**PACKET-FIRST (OD-V3)**: KF.W9/SS-13 produces the 390+1280 captures against
> real Glass 7 **before** the ask is assembled. **Never proxied.**"* COHESION **§0j.C** holds the same
> line from the other end: the transport-home ruling is *"taken by the orchestrator **from the OD-V3
> packet** at KF.W10 `.g`'s sitting"*, and **OD-V5 stays DEFERRED** pending glass's dock mark.
> **This wave PRODUCES and RULES NOTHING.** Where a half is missing, the row below says **INCOMPLETE**
> and states the exact precondition; it does not say "sufficient", "equivalent" or "close enough".

**Substrate stamp carried by every row in this file** (each figure re-measured at this seat, double-run):

```
repo          keyframes.js
substrateRef  55e9bf0d                       COHESION §0m.2 HOLD — "no re-pin on a seat's authority"
substrateSha  55e9bf0d2391bbc6d9871bb3f0555a6225daae92
frontier      3e81f500                       10 commits past the pin; the pin is its ANCESTOR
bundleSha256  1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448   MEASURED, double-run
clone         /Users/mkbabb/Programming/keyframes-w9-capture   §0m.2 shape (b); clean at the pin,
                                                               no commit, no push, NOT the sacred checkout
origin        http://127.0.0.1:9123          (`.d`'s serving origin for the pinned bundle)
```

---

## 1 · THE FOUR INDEXED ITEMS — each with what exists, what is missing, and the exact precondition

### (a) THE OD-V3 CAPTURE PACKET — **INCOMPLETE · 0 of 16 required cells**

**What OD-V3 requires**, read verbatim at the pin ⟨`git show 55e9bf0d:docs/tranches/V/OWNER-DECISIONS.md
| sed -n '7p'`⟩ → *"**THE TRANSPORT-HOME RULING IS HELD FOR CAPTURE REVIEW.** … fresh captures of both
transport homes — in-panel card and floating pill — on all four duplicating scenes at **390 AND 1280**,
real Glass 7 …"*, and identically at KF-W10 **`:68`** (OP-4): *"both transport homes (in-panel card,
floating pill) on all four duplicating scenes at **390 AND 1280**, real Glass 7, both options mocked or
annotated"*.

| axis | required | produced |
|---|---:|---:|
| scenes (`cube` · `amiga` · `square` · `easing`) | 4 | **0** |
| transport homes (in-panel card · floating pill) | 2 | **0** |
| viewports (390 · 1280) | 2 | **0** |
| **cells (4 × 2 × 2)** | **16** | **0** |

**THE DISCRIMINATOR IS HONOURED, AND IT IS THE REASON THE ROW READS ZERO.** §Sequencing S-8 (iii-a):
*"a capture of one home, or of either home at one viewport, does not satisfy it."* This wave holds **7
shots**, **three of which were taken on `/#/easing`** — an OD-V3 scene — in the `safari-app/desktop` and
`chromium/emulated-forced-colors` cells at **1280×848** (§2). **NOT ONE OF THEM IS COUNTED HERE.** They
are targeted focus-state captures of a ribbon control; none was framed to show both transport homes,
none has a 390 twin, and none covers the other three scenes. **Filing them against OD-V3 would be
exactly the substitution the DISCRIMINATOR exists to forbid**, and it is not done — not partially, not
"pending", not as a first instalment.

**EXACT PRECONDITION** (the shape COHESION §0j.C **KF-ODV3** authorizes KF.W10 `.g` to close
`complete_with_misses` on, citing this row):

1. **A cell that can render at 390.** The iOS cells are **UNREACHABLE-IN-CELL** — ⟨`xcrun devicectl
   list devices`⟩ → `No devices found.` (re-run at this seat, double-run) · ⟨`safaridriver --help |
   grep -ci simulator`⟩ → **0** · `safari:useSimulator` refused by the platform (`.c`, double-run
   byte-identical) — **RULED** at COHESION **§0m.2**. A 390-wide *desktop* window is a **different
   cell**, lawful only if labelled `safari-app/desktop`, and it is a **wave-level call no seat here
   took** (`.c` residual 4).
2. **A cell that stays open.** `safari-app/desktop` **closed mid-seat** at `.d`: session creation began
   returning *"You must enable 'Allow remote automation'…"*; ⟨`safaridriver --enable`⟩ →
   **`Password:Password is not valid`** (interactive admin); the running Safari is the **owner's**
   (pid 23725, 6 windows) and was not quit. **Precondition: "Allow Remote Automation" re-enabled by a
   hand that can answer an admin prompt, or the owner's Safari restarted.**
3. **Real Glass 7 under it** — satisfied: the served bundle is built in the §0m.2 clone at the pin and
   its `bundleSha256` is measured above, not asserted. **This limb is NOT the blocker.**

**OWNER**: the orchestrator, at **KF.W10 `.g`** — the ruling, and the `complete_with_misses` decision,
are both that sitting's. **Not this wave's, at any strength.**

### (b) THE OD-V5 390 AT-REST OBSERVATION — **NOT OBSERVED · nothing ruled, and nothing may be**

**What it is**: S-8 (iii-b) — the at-rest state at 390, *"recorded **either way**"*. That phrasing is
the whole mechanism: recording it either way is what lets KF.W10's row close `complete_with_misses`
**without proxying an owner**.

**Produced**: **nothing.** Same bound as (a)-1: no cell on this host renders a real 390 Safari surface.

**And the ruling is not this wave's under any outcome.** ⟨`git show 55e9bf0d:…/OWNER-DECISIONS.md |
sed -n '9p'`⟩ → *"**THE AT-REST REOPEN QUESTION IS DEFERRED PENDING GLASS'S DOCK MARK.**"* COHESION
§0j.C: *"**OD-V5 stays DEFERRED** pending glass's dock mark (standing edict: glass rows and OD-V5 route
to SS-6, never demo-side hacks); the `complete_with_misses` shape is authorized in advance."*
**ZERO glass-ui bytes were read or written by this wave** — the producer boundary is SS-6's, not a
capture seat's.

**OWNER**: glass-ui (the dock mark) → the SS-6 relay → KF.W10 `.g`. **Three hands, none of them here.**

### (c) THE GLASS §4 DOCK-CONTRACT RE-VERIFY (**CH2-02 ×4**) — **INCOMPLETE · 0 of 4 marks**

**The contract, quoted whole from a dated authority at a pinned ref** ⟨`git grep -n 'CH2-02' 55e9bf0d --
docs/tranches/V/FOLD-FORWARD.md`⟩ → **`:34`** *"**Glass §4 dock contract** (PEEK is NOT native; binary
FSM + opacity crossfade; manual-mode suppression): our dock consumption re-verifies against THIS
shipped contract — subsumes the folded CH2-02 ×4 (BG-5, GU-1, GU-2, subject-legible)."*

| mark | produced | why not |
|---|---|---|
| **BG-5** | **none** | no dock-state capture in any cell |
| **GU-1** | **none** | idem |
| **GU-2** | **none** | idem |
| **subject-legible** | **none** | idem |

**What exists near it and is NOT offered as it**: `.c`'s S-6 ChromeDock touch/menu session (M-5/C-6
witness · the two kf-MbabbMenu M-4 MUST-CARRY riders · MM-4's computed-style falsifier) reads **NOT
RUN**, and `ChromeDock.vue` sits inside the 23-file pin-to-frontier demo delta `.c` measured. **No dock
frame was photographed by any seat of this wave.**

**EXACT PRECONDITION**: one dock-state capture pass in an open cell against the pinned bundle,
exercising the binary FSM and the opacity crossfade with manual-mode suppression on — i.e. (a)-2's
re-authorized cell, plus a pass that mounts the dock. **The sibling half is KF.W6's TransportDock
§Bounds rows** (KF-W10 **`:214`** names the two anchors as a pair); **this receipt carries only this
end, and declares the other end rather than speaking for it.**

**OWNER**: KF.W9's own capture band for this half (re-dispatchable the moment a cell opens); **KF.W6**
for the TransportDock half.

### (d) THE V-A95 AURORA SURFACE VERIFY — **NOT PRODUCED**

**Anchor**: **`KF-W10.md` §B-1** — the row KF.W10 routes here, re-resolved at this seat ⟨`grep -n
'surface verify' KF-W10.md`⟩ → **`:212`**, *"aurora reverse-drag black slab (V-A95) … **STANDING-CARRIED**
— glass-owned (born-RED BJ wave **GF-AURORA**); rides the **SUCCESSOR SS-6 batch** … surface verify
**KF.W9**"* (a dated reading of 2026-09-17; **the anchor is the receipt, the offset is not**).

**Produced**: **nothing.** No aurora surface was captured, at any viewport, in any cell.

**And note what the row already says about itself**: the **defect** is **glass-owned** and rides the
successor SS-6 batch. **Only the surface-verify leg is KF.W9's**, and it is unproduced. **Nothing here
touches the glass row, its vehicle, or its verb** — this wave FEEDS the SS-6 relay with captures and
does not send it (§Carry: *"the letter waits on them"*).

**EXACT PRECONDITION**: an open cell (a)-2 plus a reverse-drag pass on the aurora surface at the pinned
bundle. **OWNER**: KF.W9's capture band; the defect itself is **glass-ui's**, via SS-6.

---

## 2 · WHAT *DOES* EXIST — the whole capture inventory, indexed as the §Bounds row asks

**Self-counted from the settled bytes at this seat, double-run**: ⟨`ls -1 …/safari-real/ | grep -cE
'^(hcm|at)-.*\.png$'`⟩ → **7** · ⟨each PNG re-hashed and compared to its `.sha256` sidecar⟩ →
**7 of 7 agree**. Every shot is **force-added** (the tree carries
`megatranche/.gitignore:11 audit/visual/safari-real/*.png` — the exact L-7 mechanism this wave's
born-RED witness convicts).

| # | capture path (under `…/audit/visual/safari-real/`) | sha256 | cell | substrate |
|---|---|---|---|---|
| 1 | `hcm-safari-app-desktop-easing-ribbon-default.png` | `631c27a9a8851d20f559001d5ca269d9de89f7983f5c4d6e277e44421f43c538` | `safari-app/desktop` | `55e9bf0d` |
| 2 | `hcm-safari-app-desktop-easing-focusring-default.png` | `9c357c5f352c647b6277c3fcc6a51101a4b6f82f90caa2cd19eecebda9820356` | `safari-app/desktop` | `55e9bf0d` |
| 3 | `hcm-safari-app-desktop-sequence-slider-default.png` | `ed0e28d46e55dc28208744f39481a5f0af4ec2c1ecaf0d5d39c06ef871936105` | `safari-app/desktop` | `55e9bf0d` |
| 4 | `hcm-chromium-emulated-forced-colors-easing-ribbon.png` | `bb2434b5c7bb9e17f29736ef8246b422e013ca7f3222af2b9dfd20bdcc1bdd88` | `chromium/emulated-forced-colors` | `55e9bf0d` |
| 5 | `hcm-chromium-emulated-forced-colors-sequence-slider.png` | `32c0504df7aa370a08a28e53e3e2da5faf26deb8b2c11a8cbd27268c72d1f36e` | `chromium/emulated-forced-colors` | `55e9bf0d` |
| 6 | `at-chromium-spring-heatmap-application.png` | `67e4ca6205e7a18e81fd4dffa2af4679116dda1e7a53c3989f06da830c50549a` | `chromium` | `55e9bf0d` |
| 7 | `at-chromium-amiga-bare-canvas-subject.png` | `e4937bdfe60c2cfc4b16e4e24d7d11f7c92ec622f7cc0fe5b52edb27b8dc3c4d` | `chromium` | `55e9bf0d` |

Every row also carries the measured `bundleSha256` above. **No capture is labelled `windows/real-HCM`
and none is labelled `webkit-engine`** — a label a run did not earn is the I-20 failure, not a shortcut.

**MEMBERSHIP, STATED SO IT CANNOT BE MISREAD: 0 of these 7 is a member of (a), (b), (c) or (d).**
Three are on an OD-V3 *scene* (`/#/easing`) and in an OD-V3-eligible *cell*, and are still **not** OD-V3
members, because OD-V3 is a packet with a shape and these are single-control focus captures at one
viewport. **The nearest miss is named rather than quietly counted.**

---

## 3 · THE SIX S-10 ANCHORS, RE-RESOLVED AT THIS SEAT (2026-09-17)

S-10's SHADOW block hands the RECONCILE seat one act: *"re-resolve them at close and re-issue col C if
they moved."* This seat consumes those anchors, so it re-ran all six against `KF-W10.md`'s current
bytes — **every command's whole output printed, nothing inherited from col A, col C, a check, a ruling
or a certificate.**

```
                                                                      col C (PASS-7)   col D (THIS SEAT)
(A1) §State "Opens after"   grep -n 'X.KF.W9' KF-W10.md             → :22*           → :22*
(A2) OP-4                   grep -n 'OP-4' KF-W10.md                → :68* :232 :530 :537 :541
                                                                                     → :68* :232 :530 :537 :541
(A3) PACKET-FIRST law       grep -n 'PACKET-FIRST' KF-W10.md        → :68 :84 :519* :537
                                                                                     → :68 :84 :519* :537
(A4) §D cross-edge row      grep -n 'D. → KF.W9 (Safari Visual…'    → :553*          → :553*
(A5) §B-1 (V-A95 aurora)    grep -n 'surface verify' KF-W10.md      → :212*          → :212*
(A6) §B-3 (Glass §4 dock)   grep -n 'CH2-02' KF-W10.md              → :203 :214* :232 :537
                                                                                     → :203 :214* :232 :537
(*) marks the anchor's OWN line.
RESULT: 6 of 6 anchors RESOLVE · 6 of 6 quoted subjects are byte-exact · 0 of 6 OFFSETS MOVED since col C.
        col C is therefore NOT re-issued — the SHADOW's condition ("if they moved") is measured FALSE.
VERIFIER OF RECORD FOR col D: THIS SEAT, 2026-09-17, by the six commands above. Nothing is delegated.
```

---

## 4 · RESIDUALS, EACH WITH A NAMED OWNER

1. **(a) OD-V3's 16 cells — 0 produced.** OWNER: **the orchestrator at KF.W10 `.g`** for the ruling and
   the `complete_with_misses` call; **a re-authorized capture cell** for the packet itself.
   Precondition: §1(a), verbatim.
2. **(b) OD-V5's 390 at-rest — not observed, nothing ruled.** OWNER: **glass-ui → SS-6 → KF.W10 `.g`**.
   **DEFERRED by ruling; not re-opened here.**
3. **(c) Glass §4 dock contract — 0 of 4 marks.** OWNER: **KF.W9's capture band** (this end) and
   **KF.W6's TransportDock §Bounds rows** (the sibling end, declared not spoken for).
4. **(d) V-A95 aurora surface verify — not produced.** OWNER: **KF.W9's capture band** for the verify
   leg; the **glass-owned defect** rides the successor SS-6 batch, untouched.
5. **The `safari-app/desktop` cell's re-authorization** is the single precondition that unblocks the
   largest share of 1–4 at once. OWNER: **a hand that can answer an interactive admin prompt** (or the
   owner's Safari restarted). Declared host-state change left in place by `.d`, with its revert:
   `com.apple.Safari AllowRemoteAutomation = 1` — revert with ⟨`defaults delete com.apple.Safari
   AllowRemoteAutomation`⟩.
6. **The capture clone `/Users/mkbabb/Programming/keyframes-w9-capture`** is left at the pin, clean,
   built and hashed ⟨`rev-parse HEAD`⟩ → `55e9bf0d…` · ⟨`status --porcelain | wc -l`⟩ → **0**, both
   re-run at this seat — so a re-authorized seat re-serves the **same** bytes without rebuilding.
   OWNER: the next capture seat.

---

## 5 · Anchor drift, recorded

The dispatch cited this file's own §Bounds row at **`:81`**; at the true bytes ⟨`grep -n
'SS-13-CAPTURE-RECEIPT.md` … ' KF-W9.md`⟩ the row is **`:77`**, and `:81` is **§Disjointness**. The
adjudicated-record CELL SPLIT row was cited at **`:70`**; it is **`:69`**, and `:70` is the adjacent
census row. §Sequencing S-10/S-10.1 were cited at **`:145-161`**; they are **`:256`** and **`:308`**
(`:145-161` is §Carry D/E/F). **S-11 `:309` and S-14 `:315` DO fall inside the cited `:306-315`, and
§H's two registers resolve exactly at `:207-219`.** In every case the SUBJECT is identical at both
readings and only the offset drifted; each was re-resolved **by §-heading and id, which is the sole
anchor (R2-7)**, and recorded rather than worked around. `.c` recorded the same §Sequencing drift
independently — two seats, one reading.
