# parse-that B / P-B1 root verification

**Date:** 2026-07-29  
**Mode:** tranche development only  
**Terminal:** **ACCEPT canonical formation; KEEP execution RED / NO RELEASE**

## Immutable coordinate

- repository:
  `/Users/mkbabb/Programming/parse-that-css-totality`
- branch: `codex/css-totality-combinators-20260729`
- accepted authority commit:
  `302c623b4ffddf76bc4ee042cad1d3c5e739ef9d`
- accepted-M2 control evidence commit:
  `c480578a894dce1b2cda4e3293cfebe8ceca50dd`
- corrected S/recovery evidence commit:
  `2fc18dc`
- pre-P-B1 authority commit:
  `f3ed3b6`

`302c623` is the immutable P-B1 authority coordinate. Subsequent private
prototype work may dirty the live research checkout and does not silently
change this coordinate.

## Independent canon check

Root read the canonical `B.md`, `PROGRESS.md`, `FINAL.md`, `waves/W0.md` and
`coordination/CONSTELLATION.md` directly and confirmed:

1. the order is private convergence plus formal source freeze;
2. one immutable unpublished candidate pack is produced before either
   consumer receipt;
3. Value CSS and parse-that `jsonParser` consume the exact same SHA;
4. equivalent-output, deletion and formal consumer proof follow consumption;
5. two clean audits precede release;
6. parse-that releases before Value's released-coordinate rebind;
7. BBNF receives one ABI receipt only after Value W3 and creates no CSS
   parser;
8. one serial integration writer is preserved while bounded disjoint
   research, prototype, baseline, candidate-consumer and hostile-critic
   worktrees remain permissible;
9. overlapping concurrent writers, raw-source aliases, token/scanner planes,
   fallback runtimes and domain-surface widening remain forbidden.

This closes root finding P-B1 without creating a second parser plan.

## Executed verification

From `typescript/`:

- `npm test -- --reporter=dot`:
  14/14 test files and 134/134 tests passed;
- `npm run build`: Vite build and declaration generation passed;
- `npx tsc --noEmit --project tsconfig.json`: passed;
- repository `git diff --check`: passed at the P-B1 boundary.

The owner receipt additionally reports its authority grep and diff check
green. The root build did not supply formal performance admission and does
not turn a private prototype into a released product.

## Performance truth

The corrected 96-name S-family evidence remains below the binding threshold:

| plane | exact-bootstrap CI-low / observed corrected value |
|---|---:|
| corrected matched `Parser.parseState` boundary | 9.033× |
| corrected immutable result | 4.804× |
| accepted-M2 matched boundary | 8.179× |
| accepted-M2 raw internal | 8.698× |
| accepted-M2 immutable result | 4.420× |
| first corrected recovery matched/result sample | 9.365× / 4.828× |

The full-denominator terminal signal remains useful mechanism evidence, but
cannot waive the frozen smaller scale or immutable-result plane. No point
estimate substitutes for the formal exact-bootstrap CI-low.

## Remaining release boundary

Canonical formation is accepted; execution convergence is not. B.W0 remains
open on recovery/result composition, bounded recursion, unordered composition,
Value's full scannerless CSS/WPT/browser consumer, the same-primitive JSON
consumer, net deletion, every-scale/result-plane formal ≥10×, sealed
CPU/allocation/deopt/GC evidence, `/utils` two-consumer proof or prune, one
immutable pack, two clean audits, release, Value rebind and the post-W3 BBNF
receipt.

No Value, Keyframes, Fourier or BBNF product source was edited.
