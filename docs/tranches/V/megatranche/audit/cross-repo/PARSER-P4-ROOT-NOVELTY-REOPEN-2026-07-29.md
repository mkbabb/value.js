# Parser P4 root novelty reopen

Date: 2026-07-29  
Altitude: tranche-development research and isolated prototyping  
Product execution credit: **zero**  
Release status: **NO RELEASE**

## User ruling

The user explicitly rejected terminal failure and ordered continued iteration
to perfection, particularly for parse-that and the full CSS parser that drives
Value and Keyframes.

This reopens parser research without changing any acceptance law:

- no threshold waiver;
- no scanner, tokenizer, token-event tape, or generated grammar;
- no VM/interpreter, fallback runtime, dual path, compatibility shim, or alias;
- no recognizer-only comparison against a full result;
- no consumer-specialized engine;
- exact-bootstrap CI-low `>=10x` on every unchanged whole-product binding scale
  and state/value/immutable-result plane;
- exact failure, recovery, diagnostic, span, cold/build-first,
  alternating-shape, memory, IC, deopt, and GC evidence;
- net KISS deletion and one successor runtime.

## Relationship to P1–P3

The feasibility ruling at
`PARSER-P3-ROOT-FEASIBILITY-RULING-2026-07-29.md`, SHA-256
`d03d042286d0bad27e3666f3c08e83a393aaa86928e6273c05ac85ac51bfed67`,
remains immutable negative evidence. Its cost lower bound and terminal
dispositions must be challenged, not forgotten.

P4 may not rename, recombine, or repay the killed graph/table/compiler,
source-leaf, callback, ASCII, public-sticky, balanced-fusion, unordered S/D,
direct-closure, parser-result, packrat, or `/utils` families. A P4 mechanism
must be mechanistically distinct and explain exactly where the missing
`6.56x` to `9.38x` whole-product gain comes from.

## Ownership

- **parse-that:** generic scannerless combinators/runtime, numeric UTF-16 spans,
  scalar transactions, recovery evidence, result/fault primitives.
- **Value:** the sole full CSS Syntax grammar, typed CSS/domain results,
  recovery policy, lossless syntax projection, canonical inverses, and UI.
- **Keyframes:** consumes Value's CSS/path/timing/animation surfaces and deletes
  its displaced grammar, scanner, and serializer forks.
- **BBNF:** receives one parse-that ABI handoff only after released Value rebind
  and `V.L6.css-path-abi-freeze`; it creates no CSS parser.

No P4 prototype may move CSS grammar or domain types into parse-that.

## Full CSS denominator

The Value-owned prototype must bind a current primary-source denominator for:

- CSS Syntax stylesheet, qualified rule, at-rule, declaration, component
  value, function, and simple block;
- comments, escapes, whitespace, bad strings, bad URLs, EOF, Unicode and lone
  surrogate behavior;
- unknown at-rules and declarations, custom properties, nested blocks and
  source-preserving recovery;
- exact UTF-16 spans and immutable diagnostics;
- syntactic losslessness separated from typed semantic projection;
- Keyframes-required keyframes selectors, animation declarations, timing
  functions, transforms, motion path, SVG path, and canonical inverse
  boundaries;
- current CSS Syntax/Webref/WPT identities and an explicit standard/extension
  denominator.

Keyframes does not receive a string escape, compatibility export, local parser,
local scanner, or second serializer.

## Sol–Luna–Sol loop

### P4-Sol architecture

- Task: `019fb12b-ca9c-7603-aa05-7ca653ef1e47`
- Model: GPT-5.6 Sol, xhigh
- Output:
  `/Users/mkbabb/Documents/Codex/2026-07-29/parser-p4-sol-architecture/outputs`
- Role: independently falsify the P3 lower bound, inspect current V8/TypeScript
  opportunities, author at least three genuinely distinct mechanisms, choose
  one candidate or minimal pair, and specify the Luna and Value/Keyframes
  contracts.

### P4-Luna prototypes

- Task: `019fb12c-2624-7732-909a-e1e87ce7ef24`
- Model: GPT-5.6 Luna, xhigh
- Output:
  `/Users/mkbabb/Documents/Codex/2026-07-29/parser-p4-luna-prototypes/outputs`
- Role: independently implement and measure at least three small isolated
  mechanisms against live M2 `jsonParser` and a Value-owned full-CSS-shaped
  product, preserving raw failures and iterating rather than stopping at the
  first killed spike.

### P4-Sol adjudication

A fresh Sol xhigh task is dispatched only after both sealed predecessors land.
It must:

1. rehash both predecessor corpora;
2. reject family renaming and disguised scanner/compiler/VM paths;
3. replay equal-product correctness and performance independently;
4. choose or compose only mechanisms supported by measurements;
5. author the next bounded iteration if no candidate yet clears every point;
6. grant no formation, product, execution, candidate-pack, or release credit.

P4 continues through successive sealed iterations until one exact candidate
survives or the user explicitly changes the governing law. Negative iterations
are retained as evidence and pruned from positive authority.

## Admission boundary

P4 research does not itself satisfy parser formation. The ordered boundary
remains:

```text
P1/P2/P3 negative evidence
  -> P4 surviving generic mechanism
  -> isolated every-scale/every-plane >=10x proof
  -> formation Clean A
  -> genuinely later formation Clean B
  -> parser formation admission
```

Only after admission may the private freeze, immutable candidate pack,
Value-plus-`jsonParser` exact-SHA receipts, execution clean pair, release,
Value rebind, ABI freeze, and BBNF handoff begin.
