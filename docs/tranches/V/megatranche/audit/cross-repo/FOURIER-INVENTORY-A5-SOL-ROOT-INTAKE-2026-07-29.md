# Fourier inventory A5 fresh-Sol root intake

Date: 2026-07-29  
Phase: constellation megatranche development  
Terminal: **AMEND — COMPLETE-NODE PUBLICATION IDENTITY REQUIRED**  
N integration, admission, execution, product, visual, browser, release,
package-deploy, and tuple credit: **zero**

## Sealed critic

Fresh Sol task:

`019fb13e-1c2b-7693-80da-2c3a558947a2`

Output root:

`/Users/mkbabb/Documents/Codex/2026-07-29/fourier-sol-inventory-a5-adjudication/outputs`

The root contains exactly six regular files, no directory, symlink, or special
node. All five entries in `checksums.sha256` independently verify:

| Artifact | SHA-256 |
|---|---|
| `SOL-INVENTORY-A5-ADJUDICATION.md` | `7c57efaf9688c6cbaad64901947738474651cb7f1f10dfed9165bcada4f82275` |
| `FINDINGS.json` | `156300117050207d21245f107308934219612bd4b37864dad5ecd01433473301` |
| `MUTANT-RESULTS.json` | `20e9b102bc0219c6d5d496dc9f5aff6f47a528ab4fcb89c7553807de154e7bab` |
| `REPLAY-RECEIPT.json` | `439e6b7744e7595b7a543cb0a071d308b077cdf6a131f4f336daeccb0321e169` |
| `INTEGRITY-RECEIPT.json` | `60d5647240102180f5a2bb00e6038d1d2151f3dc13b6188aafcc8afdd6e95229` |
| `checksums.sha256` | `fda3c12568f04250efafebadd8d36bcc5773e7c917329f5cfff584c099ac7051` |

Two earlier app-level system errors interrupted prose closure after the
disposable replay. The same critic and same output root resumed; completed
evidence was not repaid. Its final six-file seal records unrun optional cells
as `NOT-PROVEN`, removes all scratch, and claims no inferred green.

## A5 pointer authority

The immutable A5 pointer is:

| Field | Value |
|---|---|
| `LAST-GOOD.json` SHA-256 | `8bd9b5396ecc73ad181fbc8eab187eabe52686d23391d7c86dd51dd99bb28450` |
| promotion ordinal | `12` |
| selected candidate/file-tree ID | `a3878ab7e140183b8a62730225d8ef188ecc09a5ca7398e1f50ffbe727a6f5d2` |
| candidate manifest | `4af902126902af87c17998b98769dc4fdd4837f27f5292620c0b055108ad3f3d` |
| generator | `25741dfd4a816d419820b4f62b82dfa0280711cc5b1e343e2d3c2834184488ec` |
| semantic authority | `984947a9051133f7eb959e2d04f31204699b7aeb76e611fbcd0d160088b6353e` |
| source identity | `e3e225bca366a340f33b1bffe7da4eb9a59e6655df94511e3aa108363ff7010a` |
| client disposition | `8c085d2b464afb92e8ac0bd184ea8b3dd11706b1ffdd37c6eda482efcce61b7c` |

The stale `8c7fdcee…` Luna prose coordinate is not an authority. Pointer
selection, the 90/90 regular-file manifest, four top checksums, and three
no-write replays are valid. They establish the selected file payload, not a
complete physical tree.

## Reusable semantic evidence

Fresh Sol independently reconciled the following A5 prototype facts:

| Surface | Reconciled evidence |
|---|---|
| governed denominator | 560 regular files; path, size, and SHA sets match live source |
| typed DAG | 1,227 nodes / 2,005 edges = 1,735 semantic + 270 membership |
| terminal source nodes | 218 typed terminals; no generic padding |
| component/workflow | 66 live stable components; 396 cells remain RED-unwitnessed |
| routes | 9 |
| mounted API operations | 45 |
| current client templates | 32 |
| lifetimes | 36; 17 paired, 14 contextual one-shot, 5 RED live leaks |
| persistence | 10 production / 2 legacy input / 1 migration metadata / 1 test-only |
| parents/handoffs | 30 canonical parent edges + 6 unit handoffs |
| producer state | current locked 0.13/4/4.3; future 4/7/6 remains RED |

These are formation evidence only. They are carried byte-for-byte into A6 and
must not be recounted or regenerated there.

## Terminal defect

A5's publication identity is file-only:

- `walk()` traverses directories but returns only non-directory paths;
- `treeDigest()` hashes those file rows;
- `validateCandidate()` compares the regular-file manifest with the same
  file-only walk.

Fresh Sol reproduced these own-reason false-greens while pointer, candidate
ID, semantic/source/client authorities, and 1,227/2,005 graph stayed
unchanged:

1. an added empty directory;
2. renaming that directory;
3. two nested directory-only nodes;
4. a nonempty `.pytest_cache` subtree;
5. a nested `certified-<64hex>/empty-node` recurrence.

The cache control increased the physical copy to 92 files and 154
directories while A5 still returned its original identity. A5's physical
candidate happened to contain 91 files, 150 directories, and no forbidden
node when observed, but the pointer cannot prove that fact.

Terminal dispositions:

| Evidence or claim | Disposition |
|---|---|
| selected pointer and regular-file bytes | **KEEP** |
| live-derived semantic maps | **KEEP** as prototype evidence |
| directory/cache false-greens | **FOLD** into one publication defect |
| complete-node identity | **SPLIT** into A6 only |
| “full tree digest” and “zero forbidden nodes proven by identity” | **PRUNE** |
| A5 CLEAN/PASS/admission | **PRUNE** |
| product, N, package, visual, execution credit | **PRUNE** |

## A6 release condition

The same Luna inventory task
`019fafcc-b871-7ba2-895a-4462dcd4cb0f` is continuing exactly once into:

`/Users/mkbabb/Documents/Codex/2026-07-29/fourier-luna-inventory-reformation-a6/outputs`

A6 is publication-only. It must:

1. copy every carried semantic artifact byte-for-byte;
2. enumerate every directory, regular file, symlink, and special node with
   normalized paths and exact kind-specific fields;
3. reject every symlink, special, forbidden cache, nested candidate,
   `certified-*`, `LAST-GOOD`, manifest, and checksum recurrence;
4. bind complete-node and regular-file manifests, counts, hashes, generator,
   and carried authorities in one sole pointer;
5. use only explicitly enumerated self-reference exclusions whose file hashes
   are separately pointer-bound;
6. prove at least 16 uncapped valid promotions;
7. inject every publication-write failure and preserve the prior pointer/tree;
8. replay all 46 existing controls and close the seven A5 `NOT-PROVEN` cells
   with targeted validators, not a semantic rebuild;
9. remain `REJECT-AS-PROTOTYPE` until a new independent Sol xhigh critic
   returns CLEAN.

No canonical N file may consume A6 before that fresh Sol close.
