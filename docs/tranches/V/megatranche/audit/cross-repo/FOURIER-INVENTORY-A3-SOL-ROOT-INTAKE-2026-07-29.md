# Fourier inventory A3 fresh-Sol root intake

Date: 2026-07-29

Status: **A3 REJECTED — A4 REQUIRED**

Altitude: tranche development only. A3 supplies no canonical-N, product,
visual, Browser, frontend-design, workflow-execution, repository-formation,
root-DAG satisfaction, or execution credit.

## Exact subject

- Fourier lane:
  `/Users/mkbabb/.codex/worktrees/d0be/fourier-analysis`
- Fourier HEAD:
  `cd26c6533adc32dfe1453d74117d3cb73b89ea16`
- A3 Luna root:
  `/Users/mkbabb/Documents/Codex/2026-07-29/fourier-luna-inventory-reformation-a3/outputs`
- A3 fresh-Sol root:
  `/Users/mkbabb/Documents/Codex/2026-07-29/fourier-sol-inventory-a3-adjudication/outputs`
- Fresh-Sol task:
  `019fb0ab-20e6-74e1-87ed-52f20de953d2`

The initial same-head main-checkout confusion was corrected before evidence
formation. All N, source, census, mutation, and preservation results use the
d0be lane. The main checkout supplies only materialized precepts.

## Immutable fresh-Sol receipt

| Artefact | SHA-256 |
|---|---|
| `REPORT.md` | `6fedb272e1fed85b1ff5cf00a7f857ae8cd1c2b91e3d5b0890af51da501aff66` |
| `FINDINGS.json` | `5a40a98e4efbaa769b636e1a7b5fc5f827395a72c97ff747ef87258c09d6f176` |
| `MUTANTS.json` | `ee8b4ca6182e69e106fb0487388b89c08c5bb4ef50cdb76439c8369846636438` |
| `REPLAY.json` | `648bc656daa239b59bdf6c61a2f208674b387040883060c347c527a2d8e25414` |
| `RECEIPT.md` | `34814c56ac8f8277962018833217eb3a135c1a16880b14e0b3d8fbc429d77f7c` |
| `checksums.sha256` | `65c919ecaea9a8789e9149fa1622e97c0ac86dd4d36a952cc076b4ad1c5049e7` |

All six listed files verify. The output contains six regular files, no
subdirectories, and no symlinks. Output-tree SHA-256:

`db60b3b1775f631dff928f34c15c5fbf5184a81ccbe656ab0f22ad4ebd3a50b2`.

## A3 candidate integrity that survives

The following establishes reusable package/seal mechanics only:

- candidate/tree:
  `6a65fe3b20c866048ed362412aa433477b896829f2d194bddeacd5a9dca73414`;
- generator:
  `9baf5c774554496216bc823802cb199d82453ec07d55aefc600b165cb60963eb`;
- candidate manifest:
  `1b0570ab6e772759d4a0831ff56b89d28507184cfd3f577cf474021b0c74743b`;
- sole pointer:
  `4523afdf3d3025e2611f624a4893f3471bfed3699aaf68b44a92caeeb3ec5485`;
- 123/123 candidate-manifest entries and 4/4 top projections verify;
- three no-write runs reproduce authority hash
  `7192f467a79513aa75c46cc926595cd629c9244cbc870072bab569872d132c33`;
- pointer-field, candidate closure, byte-corruption, and interrupted
  publication controls preserve the prior pointer/tree.

These facts do not make the authority hash semantically sound.

## Reusable independent census

| Surface | Independent count | Disposition |
|---|---:|---|
| live Vue files | 66 | KEEP |
| AST-derived router mounts | 7 | KEEP |
| unique mounted operations | 45 | KEEP, but remove fixed-count forcing |
| canonical N parent edges | 30 | KEEP |
| math functional files + package markers | 57 + 4 | KEEP |
| current persistence identities | 14 | KEEP as census |
| persistence partition | 10 production / 2 legacy-input / 1 migration-metadata / 1 test-only | KEEP as census |
| workflow cells | 66 × 6 = 396 | KEEP `RED-unwitnessed`; zero execution credit |
| App `MutationObserver` | one unpaired live leak | KEEP RED |

## Terminal falsifiers

### Closed source denominator

A3 claims 389 relevant files. The independent broad denominator is 554:
515 product/test/style/asset/config/deploy/package/math files plus 39
canonical N files. A3 omits 165 paths.

Even if a later written policy excludes archival binary assets, at least 20
definite package/config/deploy/public paths remain omitted, including root
`package-lock.json`, `web/Dockerfile`, `web/public/_headers`,
`web/public/_redirects`, `web/public/favicon.svg`, and
`web/public/robots.txt`.

An added `web/public/*.svg` path remains false-green. A4 must derive a closed
denominator by rule and reject additions; it may not hard-code the observed
count.

### Typed graph

The claimed `856 nodes / 1746 edges` is rejected:

- 175 `finite-static-disposition` edges attach otherwise isolated files to
  one fabricated static terminal;
- all 152 live `from/import fourier_analysis...` statements produce zero
  Python import edges;
- live CSS imports are incomplete;
- removing padding leaves at most 855 current nodes / 1571 current edges
  before adding the omitted denominator.

A4 must emit real typed import, style, test, route, state, worker, package,
persistence, migration, receiver, and parent edges. Root/unit membership
edges and static terminal padding never count as semantic degree. Final
counts are derived outcomes, not targets.

### Mutation soundness

A3 reports 28/28 own-reason mutants, but 16 are synthetic
`pureFailure` constructions. The fresh hostile suite observes:

- 36 controls expected RED;
- 17 returned RED;
- 19 remained false-green;
- 14 failed for their own reason;
- 3 failed generically or through forced historical checks.

Actual third successive promotion is accepted, so the nth-promotion law is
unproved. A4 must run source/output/publication mutations through the real
mechanism in isolated fixtures and require the named failure reason.

### Consumer, owner, and topology truth

A3 remains false-green for:

- absolute and aliased dangling `fourier_analysis` imports;
- extra route aliases and route parameter-shape drift;
- comment-only URL-helper consumption;
- `getattr` persistence access;
- injection key/value drift;
- duplicate C02/C39 owner clauses;
- timer callback and unreachable cleanup-scope drift;
- generated clients and `SharedWorker` additions inside existing modules;
- SVG/asset denominator additions;
- renamed analyzer files;
- comment-only receiver implementations;
- CSS package and future-major producer drift;
- wrong existing persistence owners.

The existing 66-row component roster, route rows, current producer locks, and
persistence partition are hypotheses/censuses only until these mechanisms
fail closed.

## A4 bounded contract

A4 must preserve A3 and this fresh-Sol receipt immutable, and:

1. derive one closed relevant-file denominator with explicit binary/archive
   inclusion law;
2. parse Python relative/absolute/aliased imports and CSS imports;
3. delete the fabricated static-terminal node and all padding edges;
4. classify every file through real typed edges or one exact terminal
   disposition, without fixed graph counts;
5. derive routes, aliases, redirects, parameter shapes, client functions,
   URL helpers, and call sites from parsed live source with comments removed;
6. derive owner clauses from the canonical unit text and reject omission,
   duplication, wrong-existing owner, unauthorized MOVE, ID reuse, and
   tombstone drift;
7. pair lifetime acquisition with exact handle/callback/scope/cleanup/owner;
8. derive persistence, state/DI, worker/generated-client, receiver, package,
   producer, and full parent topology without seed carry;
9. move publication proof to a black-box isolated harness and prove repeated
   promotion, interruption, same-ID reuse, and rollback from observable
   bytes;
10. replace all synthetic mutants with real wrong-source or wrong-output
    controls;
11. retain all 396 frontend-design/in-app-Browser workflow cells as
    `RED-unwitnessed`, never as formation or execution credit;
12. emit a distinct immutable A4 candidate and receive a fresh Sol
    adjudication before any N or root-DAG integration.

## Root-DAG treatment

At the next batched reseal:

- `F.proto.inventory-a2-sol` is `rejected / SPLIT`;
- `F.proto.inventory-a3-luna` is satisfied candidate evidence only;
- `F.proto.inventory-a3-sol` is `rejected / SPLIT` and binds this receipt;
- no A2 or A3 Sol positive edge reaches `F.form.P3`;
- add `F.proto.inventory-a4-luna` and a later
  `F.proto.inventory-a4-sol`;
- only an accepted fresh-Sol successor may become the inventory input to
  `F.form.P3`.

Until then, Fourier P1/P2/P3, Clean A/B, and formation admission remain
non-satisfied.

