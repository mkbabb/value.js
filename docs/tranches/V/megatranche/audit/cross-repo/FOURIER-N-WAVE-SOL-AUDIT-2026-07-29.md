# Fourier N hostile Sol wave audit

**Date:** 2026-07-29  
**Mode:** tranche development only  
**Authority audited:**
`/Users/mkbabb/.codex/worktrees/d0be/fourier-analysis/docs/tranches/N`  
**Terminal:** **REJECT-AS-FORMED**

This does not choose or reject a product/storage architecture. It rejects the
claim that the 14 waves and 75 units are precepts-complete and execution-ready.

## 1. Generated inventory can certify false truth

The inventory's route, client-operation, lifetime and producer truth is
hand-maintained in `build-inventory.mjs`; it has no ownership/dangling
validation and writes outputs unconditionally.

Concrete wrong answers:

- it emits `/ → PaperView`, while the live router redirects `/` dynamically to
  the saved tab;
- all 66 workflows are filename-derived generic templates with no wave-owner
  field;
- it still emits `keyframes.js@6.0.0+stale-dist` after the blanket stale-dist
  claim was pruned.

The JSON's 538 nodes, 2,167 edges and zero dangling endpoints are internal
consistency only, not proof that curated dynamic edges are correct.

## 2. Component ownership is incomplete and overlapping

- W5.U2 owns C22–C26 while W5.U3 also owns C24.
- W5's hard gate says 12 workflows, the declared set has 11 unique components,
  and the compact spec says nine.
- W6 promises C31–C54 except C50 but omits shared Tooltip C34.
- W6.U1 and W6.U4 both own C54.
- W7.U1 claims C15 outside W7's bounds although W5 already owns it.
- W2's file table deletes `CanvasOverlayButton.vue` while its disjointness law
  assigns deletion only to W6.
- W5.U4 and W7.U5 reopen component-local styles/assets owned by earlier units.

## 3. Parent DAG omits required handoffs

The parent graph lacks W5→W6 and W5→W9 even though W5 owns the router first and
both later waves require its handoff. W12.U4 also depends on W6/W8 profiling,
but the parent DAG does not represent that late dependency.

## 4. Generated and terminal receivers drift

- `/v` lists W7+W11 but omits W6 loader ownership.
- `visualization_versions` lists W3 although W4 owns version
  membership/storage.
- D-18 routes the slug-wrapper migration to W2/W3 while its live import is the
  W7 session route.
- F-01, F-08 and X-05 omit W6 despite W6 owning loader/draft/entity repair.

## 5. Rejected F-L2 apparatus returns

The hostile ruling and PASS-3 prose prune the wrapper, regex analyzer and
standalone evidence registry. W0 nevertheless reintroduces a top-level
runner/aggregator, and W12 requires an aggregator plus “F-L2 analyzers.”
This is the process bureaucracy XR-15 rejected.

## What survives

KEEP W4's corrected F-L1 R2 binding. It correctly:

- treats R1 as falsified exploratory evidence;
- separates release and content identity;
- requires direct family mutants;
- binds overlap/partition sweeps and authorized read/revert p95;
- requires real physical counts and restore liveness;
- withholds selection until corrected evidence.

## Smallest reformation delta

1. Derive or validate route/client/lifetime ownership against live registries;
   fail nonzero on unresolved, dangling or unowned truth; add explicit workflow
   owners; remove stale Keyframes data; regenerate before certification.
2. Move shared C31–C34 semantics to W1.U4; assign C24 once; fold C54 layout
   into W6.U1; remove C15 from W7; keep W2 proof-only and W6 deletion-only;
   fold decoration into its component-owning unit.
3. Add W5→W6 and W5→W9; represent W12.U4's W6/W8 dependency; reconcile `/v`,
   version storage, D-18, F-01, F-08 and X-05 receivers.
4. Delete top-level aggregator/analyzer apparatus. Put stale/required-green
   mutants directly in real type, API, Playwright and deployed gates.
5. Do not preserve 75 units by inventing replacements. KISS folding may reduce
   the count.

