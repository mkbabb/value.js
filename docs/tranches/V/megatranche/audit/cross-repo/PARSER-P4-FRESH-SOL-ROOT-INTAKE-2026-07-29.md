# Parser P4 fresh-Sol root intake

Date: 2026-07-30  
Authority date: 2026-07-29  
Phase: tranche formation research only  
Terminal: **P4 REJECT/FOLD; exactly one P5-EUW fatal continuation**

## Root ruling

The P4 Sol–Luna–Sol loop did not produce a parser candidate.

- The Sol RSR, CISF, CTPT, and RSR+CTPT designs were not implemented by the
  Luna packet.
- Luna instead implemented F1/F2/F3. All 432 JSON mechanism rows are below
  `1x`; all 432 CSS rows execute the same handwritten recursive-descent body
  on control and candidate sides and receive zero mechanism, second-consumer,
  full-CSS, or performance credit.
- Exact equality remains RED at `93/186` complete-state rows and `126/186`
  value/result rows.
- P5-SRA and transaction-ownership leasing are terminal PRUNE. Leasing would
  change the incumbent locally atomic `then`/pair failure contract unless it
  introduced forbidden analysis, annotations, or dual executors.
- Exceptional-unwind mismatch is the sole distinct remaining research family.
  It has zero credit and begins with an ABI/equality/performance kill ladder.

No threshold, scannerless, ownership, equal-product, full-CSS, release, Value
rebind, ABI-freeze, or BBNF law changes.

## Corrected jury authority

The original jury root is preserved as immutable historical evidence:

```text
/Users/mkbabb/Documents/Codex/2026-07-29/parser-p4-fresh-sol-adjudication/outputs
checksums.sha256 = b3ca2da1096db1b04d26c6a03331f0f7996c3265bdfede24e6530d8a652f7dd7
```

It is not the binding jury coordinate because two metadata fields were wrong:

1. `FULL-CSS-GATE.json` cited a nonexistent Sol denominator digest;
2. `MECHANISM-GENEALOGY.json` expanded RSR under the wrong name.

The same fresh-Sol jury produced a correction-only successor:

```text
/Users/mkbabb/Documents/Codex/2026-07-29/parser-p4-fresh-sol-adjudication-a1/outputs
```

Root independently verified:

- exactly eight regular files and no subdirectories;
- the manifest twice: `7/7` green;
- no file write after the manifest refresh;
- exact Sol denominator bytes and manifest entry
  `0388a49c8ffe71045f30ab30c0d1e4a8fcdd332a93ac5e73bf0ff3ffd41e2815`;
- exact Sol name `Region-Slot Return ABI`;
- byte-identical adjudication, evidence replay, and P5 dispatch relative to the
  predecessor;
- one-line-only semantic diffs at
  `/authority/solDenominator/sha256` and `/p4Sol/0/expanded`.

| A1 file | SHA-256 |
|---|---|
| `P4-FRESH-SOL-ADJUDICATION.md` | `31bbec228877df7168fb918df439bb190de8149fd5bb468e4ba54ece7663717c` |
| `MECHANISM-GENEALOGY.json` | `0f62d70a8c1284eea9ef9eaae3a929a3687aba589900068f0588c905604de0ab` |
| `P4-EVIDENCE-REPLAY.json` | `3f3bc745c13b00c4a41836c0b0fb20d1c1101dd3bb8e72b3c771a7b7c1f26e46` |
| `P5-DISPATCH.md` | `d74e089877de4c037bd310bef976052958374f99176a8707cfa96e354a0ac263` |
| `FULL-CSS-GATE.json` | `6c0862cc2fb4673774397314a0ff10b8eb9ee052a25424e834f8b30da82eedcb` |
| `RECEIPT.md` | `c0e206473adccfaf1d6b7a0628a7ad32721507b86b9c77ab0f1a49cf00b28f9f` |
| `INTEGRITY.md` | `3df4f8ed108bada4e7110b97b3053a55dd26e1dd7acb78ac222c0d21e055bd86` |
| `checksums.sha256` | `3297c808d66ed3c528134a2567a4f85ba7602769270868abfd701fa80ce0b311` |

Only this A1 coordinate may satisfy the P4 fresh-Sol jury row.

## Full-CSS and consumer boundary

The binding denominator remains:

```text
raw scoped P/F/T       1,503
active scoped P/F/T    1,439
raw all Webref rows    1,717
active all categories  1,653
legacy aliases PRUNE      64
manual/prose rows         109
CSS Values 5 overlay       60 (non-additive)
```

CSS Syntax additionally binds all 25 token-equivalent decisions, ten entry
points, eleven tree algorithms, preprocessing, recovery, original UTF-16
spans, source preservation, and the pinned WPT manifest. A materialized token
plane is neither required nor allowed.

Standard rows include `ictcp()`, `jzazbz()`, and `jzczhz()`. Value extensions
are `hsv()` and `kelvin()` only. Keyframes remains exactly 53 references in 51
files: 49 direct imports in 47 files, two `import.meta.resolve()` literals,
and two bench HTML import-map keys.

Ownership is unchanged:

- parse-that owns generic runtime, state, spans, recovery, and result laws;
- Value owns the sole CSS grammar, typed values, opaque syntax, inverses, and
  UI;
- Keyframes consumes the Value surface and retains scheduling, composition,
  DOM resolution, timeline-to-progress, and curve-sampling policy;
- BBNF waits until released Value rebind and
  `V.L6.css-path-abi-freeze`.

## P5-EUW admission fence

One genuine Luna xhigh task was dispatched:

```text
task 019fb16a-09d7-7d80-ae5f-3d894e25855d
sole writer /Users/mkbabb/Documents/Codex/2026-07-29/parser-p5-euw-luna/outputs
```

The app-created 2026-07-30 locus is excluded and must remain empty.

The preflight is strictly ordered:

1. ABI/taxonomy census and exact composition proof;
2. complete M2 equality for live dispatch JSON and a real Value-owned generic
   combinator CSS vertical;
3. one scale-4 immutable-result timing cell, seven raw ratios all `>=10x`,
   then exact `7^7` bootstrap CI-low `>=10x`;
4. only if green, current Node/V8 allocation, reached-catch, optimization,
   deopt, GC, IC, and retained-heap assay;
5. only if green, the remaining 96 preflight configurations.

Current V8 supports structured exceptions, but its optimized handler lookup
can lazily deoptimize on a reached throw when a handler entry is
`kLazyDeopt`. Therefore exception lore is neither proof nor an automatic kill:
exact runtime pins and attributable reached-catch traces are mandatory.

Any ABI/equality mismatch, zero CSS mechanism counter, per-throw allocation,
candidate-only deopt/GC/IC/heap defect, raw ratio below `10x`, or bootstrap
CI-low below `10x` seals P5-EUW negative and stops the packet. No other family
may start inside it.

## Credit and ordering

This intake grants:

- P4 jury evidence identity;
- terminal negative genealogy;
- authority to run exactly one isolated P5-EUW fatal preflight.

It grants zero:

- parser formation admission;
- implementation or repository mutation;
- product, execution, package, release, or deployment credit;
- Value migration or rebind;
- Keyframes adoption;
- CSS/path ABI freeze;
- BBNF handoff.

The next admissible root event is the sealed P5 Luna packet followed by one
fresh Sol xhigh adjudication. No owner implementation may precede that jury.
