<!--
  PROVENANCE — M-21 C-11 (codex-provenance ledger row 28), 7e28 worktree census, ruled 2026-08-03
  original: /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/FORMATION-AUTHORITY-STATUS-R1-SUPERSEDED-2026-07-29.md
  original-mtime: 2026-07-29T21:25:35
  original-sha256: 6be15f28a66392a061711ea4563e20d2940f7470205a3a583e12f570dc4434f9
  original-bytes: 7766
  ruling: ADOPT-COPY — M-21 C-11 — 2026-08-03
  note: this comment block is prepended to otherwise byte-exact original content, so the
  copy's own sha256 differs; both digests are recorded in CENSUS.md. The original bytes
  are exactly the last original-bytes bytes of this file.
-->
# Value formation authority status

Date: 2026-07-29
Scope: formation authority only; no product-source execution

## Binding correction

Root's hostile DAG adjudication found that
`VALUE-FORMATION-PACKET-2026-07-29.md` §6 counted three specialized slices as
three successive full-subject passes. That does not satisfy
`MEGATRANCHE-DRIVE-SEED-2026-07-29.md` §§2 and 4.

The pre-absorption packet was preserved byte-for-byte through P1/P2/P3 as an
immutable historical **FORMATION CANDIDATE**:

```text
aa6840601388258246677c1c619deeadd763d5dbddb56f9feb92b2b803961840
```

Fresh Clean A later returned `AMEND` on CA-01. The current post-absorption
packet is:

```text
359262b6bc5ffd7285e0305499d31b4dfc44b592207b772823d932a4d3c6017b
```

Only V.L6's consumer-reference denominator changed: 53 references across 51
files = 49 direct imports in 47 files + two `import.meta.resolve` probes + two
bench HTML import-map keys. Prior pass evidence continues to bind the
historical `aa684060…` coordinate; it is not rewritten.

Its prior clean audits are retained as pre-reopen evidence, not current
admission:

```text
eea9ee13c9fdf79ed8b31be2e9c6850ba62e09a96cb64cc036975cbc0b90d5fd  FORMATION-CLEAN-AUDIT-1-2026-07-29.md
4638420279de8155df1757aeea3cc2b48836bf1795c9926da732246d8aac584e  FORMATION-CLEAN-AUDIT-2-2026-07-29.md
```

All substantive evidence, wave specifications, terminal dispositions,
prototypes, browser receipts, and cross-repository rulings in that coordinate
remain valid unless a later full-subject pass explicitly amends them.

## Superseding completion chain

Formation admission now requires this exact dependency order:

1. one Value full-family P1 over the canonical family denominator;
2. one independent full-family P2 bound to the exact P1 registry hash;
3. one fresh hostile full-family P3 bound to the exact P2 registry hash;
4. Clean A over the complete post-P3 corpus;
5. Clean B, started only after Clean A is immutable, that tests Clean-A
   absorption rather than inheriting its findings;
6. root verification and admission of the complete chain.

Each pass must contain research, synthesis, prototype/prototype-reuse,
critique, and agglomeration fields for every family. Each family receives one
terminal `KEEP`, `FOLD`, `MOVE`, `SPLIT`, or `PRUNE` disposition; the owning
wave action is recorded separately.

The prior GenericActionBar, CSS/path/transform, and API policy/membership
passes are folded into the appropriate full-family rows. They are never again
counted as P1, P2, or P3 by themselves.

## Current status

**FORMATION ADMITTED — ROOT DAG REBIND COMPLETE; ORDERED EXECUTION NOT
STARTED.**

The exact accepted full-subject chain is:

```text
b3929ebf6f0718c24d5c27cd9c2a14f41dd1067d0f25221250021fa9ca6c657b  P1
82a8b33ee2b57b354cec4ab3daf322f0061dcfee8141901db6a39ba0b6b163a1  P2
00fd0b7957cad35643addf31b8c990eb83515414841c6bc9f2452b16672216dc  P3
```

All three passes bind denominator
`1d6df52abdd89dcc2e56b0aaaca6cc7d4a516eb5e8493dd4547adeebd070dd85`
and exact successive predecessors. P3 owner intake opens fresh Clean A. It
does not itself satisfy Clean A, formation admission, or execution.

Root's independent recheck invalidated owner intake `7b5522ea…` as closing
evidence because it raced the P3 report/receipt seal. Its follow-up
`d342d0c7…` is likewise superseded. Both are preserved as race evidence, not
rewritten. P3 has now resealed its registry, report, and receipt together as
`00fd0b79…`, `ddf1af06…`, and `5a3245e9…`, replacing orphan validator
identities and removing the mutable formation-status reference. A new owner
re-intake binds all three final hashes, exact P2/denominator authority, race
invalidation `a6aecec3…`, and root graph `d0f9ad0d…` / `787b9669…` /
`b016b4ee…`. Root then rebound `V.form.P3` to registry `00fd0b79…` and
resealed the graph as `0603006b…` / `30d6da20…` / `b016b4ee…` with
validation receipt `7296a6e4…`. Fresh Clean A task `019fb037…` resumed from a
verified-empty output root with predecessor `00fd0b79…`.

The post-P3 root graph authority requires candidate and execution
`V.L1 -> V.L2 -> V.L5 -> V.L4`, requires `V.L2 -> V.L3`, permits only V.L3
and V.L4 to feed V.L6, and forbids a direct V.L5-to-V.L6 bypass. Current
read-only validation was 124 nodes, 171 edges, and M01–M21 rejected at the
last boundary. The graph bytes may continue to evolve as other repository
formation receipts land; each audit must bind its exact authorized tuple.
At that boundary, later Clean B and the final evidence rehash remained
mandatory.

Clean A remains pinned to its authorized post-P3 124-node/171-edge tuple even
though live root later advanced to 126/173 for Fourier A2 and Keyframes
formation. Root classified that as other-lane coordination. The audit must
nevertheless returned the Value-owned absorption mismatch between P3's correct
53-reference/51-file Keyframes census and the candidate/surface documents'
two-literal shorthand. Clean A sealed `AMEND` as report `c90203db…`,
validation `0e631267…`, and receipt `c858f954…`. Root accepted the verdict.
The exact V.L6/consumer-surface absorption is now present as packet
`359262b6…`, surface `f9a98d7d…`, and manifest `fa2c4959…`. P3 remains
unchanged. Root resealed the post-absorption authority as graph `a9080626…`,
Markdown `6045f2ed…`, validator `6f9358a4…`, and validation receipt
`23707e81…` with 129 nodes, 176 edges, and M01–M21 rejected. Replacement fresh
Sol Clean A task `019fb05d…` is active against exact dispatch `08858482…`.
It returned `CLEAN` as report `2a3d42f9…`, validation `14bb61c3…`, and
receipt `13c86d9d…`; owner intake accepts the exact triple. Root must now bind
that intake and reseal the graph before a genuinely later Clean B may open.
Root completed that rebind as graph `ba960351…`, Markdown `11637970…`,
validator `6f9358a4…`, and validation receipt `2e535dfa…` with 130 nodes,
177 edges, and M01–M21 rejected. The first Clean A is now an explicit
superseded/non-satisfying node. Root dispatched the sealed genuinely later
packet `ef4aeb27…` as fresh Sol task `019fb076…` with an isolated three-file
output root. Clean B returned `CLEAN` as report `3fceb394…`, validation
`23b41758…`, and receipt `bbb27015…`, with no findings and zero execution,
visual, product, release, or admission credit. The Value owner independently
revalidated and accepted that exact triple in owner intake `0c57bbb5…`.
Root's separate second critic returned `PASS` and independently rehashed the
triple and owner intake.

The Value owner then issued full-subject formation-admission receipt
`4aac4457…`. It rebinds the immutable post-CA01 packet `359262b6…` to the
genuine P1 → P2 → P3 → replacement Clean A → genuinely later Clean B chain.
It does not rewrite the packet, replay the superseded first Clean A, or revive
the pre-XR-23 specialized-pass admission. Root then bound `V.form.cleanB` only
from replacement Clean A and `V.form.admission` only from Clean B, with all
three stages bound to denominator `1d6df52a…`. Root receipt `e5ccabd0…`
records the post-admission quartet: graph `88578047…`, Markdown `69116c27…`,
validator `ba5789b3…`, and validation receipt `a60eabe5…`. Validation is green
at 130 nodes/177 edges/M01–M23; M22 rejects a wrong Clean-B predecessor and
M23 rejects admission coverage drift. No product implementation is
authorized.

No implementation wave is authorized. Parser P-B1 is acknowledgment and
formation-order evidence only. V.L1/V.L5 remain blocked on the released
parse-that successor and Value rebind. V.A2 remains blocked on the corrected
Fourier F-L1 R2 same-level rerun. V.L6 and V.G1 retain their Keyframes and
published Glass artifact gates.
