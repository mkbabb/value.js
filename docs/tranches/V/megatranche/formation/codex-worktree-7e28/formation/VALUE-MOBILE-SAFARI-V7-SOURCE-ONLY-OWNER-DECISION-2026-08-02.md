<!--
  PROVENANCE — M-21 C-11 (codex-provenance ledger row 28), 7e28 worktree census, ruled 2026-08-03
  original: /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/VALUE-MOBILE-SAFARI-V7-SOURCE-ONLY-OWNER-DECISION-2026-08-02.md
  original-mtime: 2026-08-02T20:32:50
  original-sha256: e1522725a855a1f413bea7a62e4bfc2c6897668f4b6f26d3a22d69c404325486
  original-bytes: 11474
  ruling: ADOPT-COPY — M-21 C-11 — 2026-08-03
  note: this comment block is prepended to otherwise byte-exact original content, so the
  copy's own sha256 differs; both digests are recorded in CENSUS.md. The original bytes
  are exactly the last original-bytes bytes of this file.
-->
# Value Mobile/Safari v7 source-only owner decision — 2026-08-02

## 0. Decision

**Option A:** `AUTHORIZE ONE FRESH NON-OVERLAPPING V7 SOURCE-ONLY COORDINATE`

**Precondition:** `ROOT-OWNED QUIESCENT GOVERNING CAPTURE REQUIRED BEFORE ANY V7 BYTE`

Mode is tranche development only. Authority is `NONE`. This decision grants no
product, parser, Browser, Safari, Simulator, API, Docker, package, source
execution, release, rebind, admission, owner-slot, or constellation credit.

This receipt is the only owner decision. It does not create a task, writer,
source file, directory, root capture, review, or execution. V6 remains frozen
and terminal. No coordinate after v7 is inferred or authorized.

## 1. Final pass-provenance boundary

This decision consumes the final authenticated pass-provenance boundary only
as historical formation registry and dependency chronology:

| Artifact | SHA-256 | Result |
|---|---|---|
| corrected 60-cell matrix | `7f69d258e242ee4976342dfb65b4e62c01dc2ae2764ee5111b45d28f5a2aef50` | historical registry only |
| hostile C | `d99e5f3f558d4eb621e871f1b1f383d75053823af5705b66e0e259e57c0d2648` | clean source review, zero authority |
| owner adjudication | `e7bded94f2912b5cd151a8bbf5cafd5763a5b0f3a3eec940fd7edd44d28bbac1` | historical formation only |
| reconciliation checksums | `de3dcb32c6db8661534eafb1691e73a1c09d6e9c30b7c58424f5e56bfbb04288` | 6/6 replayed from the live Value root |

The accepted historical denominator is 60 ordered phase cells: 30
`AUTHENTICATED_COMPLETE`, 25 `STALE_UNAUTHENTICATED`, and five `ABSENT`.
Value retains 15/15 authenticated historical formation cells, but its current
eligibility remains 0/15. Current-eligible subject passes remain 0/12 and all
five immutable cross-repository owner input slots remain null.

The first checksum replay attempted from the coordination directory and failed
because its rows are repository-root-relative. The read-only rerun from
`/Users/mkbabb/Programming/value.js` replayed all 6/6 entries. This was an
external invocation-path mistake, not an evidence defect.

## 2. Frozen v6 predecessor

The terminal v6 owner intake remains immutable at:

`/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/coordination/VALUE-MOBILE-SAFARI-V6-TERMINAL-OWNER-INTAKE-2026-08-02.md`

SHA-256:

`c6e8e458fbb81bf54201b5b527951b39284e4556867ece2b193a32b27768320f`

Its terminal is
`SOURCE_REVIEW_RED / ROOT_COORDINATE_DRIFT_DURING_AUTHORING / ZERO AUTHORITY`.
The frozen external packet contains exactly five regular `0644`/nlink-1 files,
605,719 bytes, with canonical census identity
`7cc98d71475a63b3224d36b91f2060ff9437f7b9a76fdbab67629b8716a5bf9f`.
Its checksum SHA is
`cfa6a6c2c18109e9331f185b692f78c2c40b16eb9bdb4b63bfd1987f2991c310`
and replays four payload rows.

V6's 306 members, 88 SFCs, 717 mount occurrences, 13 dynamic mounts, two
teleports, 15 routes, 11 panes, 33 page roles, 21 layers, two harnesses, 887
source subjects, 137 axes, and 243,038 static source-plan cells are archaeology
only. Controls are 0/44 executed, independent D2 is 0/887, and all 243,038
cells remain RED/unexecuted. Runtime multiplicity, equivalence, final
Kronecker, and final Apple denominators remain `OPEN`.

There is no v6 repin, repair, hostile continuation, source-ready inference, or
credit in this decision.

## 3. Exact authorized v7 coordinate

The only authorized successor root is:

`/Users/mkbabb/Documents/Codex/2026-08-02/value-mobile-safari-source-closure-v7-source/outputs/`

After the precondition in section 4 is satisfied, it may contain exactly two
regular mode-`0644`, nlink-1 files and no child directories, symlinks, or
special nodes:

1. `preflight-value-mobile-v7.mjs`
2. `SOURCE-READY.md`

The v7 source author may write only those two inert files in one bounded source
transaction. The author may not execute, import, syntax-check, parse, or run
the source; read product bytes outside the authenticated governing capture;
create a registry; or invoke Node, Browser, Safari, Simulator, API, Docker,
package, build, product, parser, network, release, or git mutation.

A first material source RED freezes the exact root. No same-root repair,
cleanup, repin, rewrite, review continuation, or reseal is authorized.

## 4. Quiescent governing-root capture precondition

No v7 directory or file may be created until the constellation root supplies a
separate immutable, root-owned capture and explicitly guarantees its governed
files will remain unchanged through the v7 authoring turn.

The capture must be authored outside the v7 root by a role distinct from the
v7 source author and must machine-bind:

1. a unique capture ID, nonce, capture time, owner identity, and source task;
2. the exact live Value root, HEAD, branch/detached state, scoped dirty-status
   bytes/SHA, and protected user-change declaration;
3. a codepoint-ordered closed membership of every governing root document and
   predecessor consumed by v7;
4. for every member: canonical path, kind, byte length, mode, nlink, SHA-256,
   and domain-separated member identity;
5. a length-framed, domain-separated capture root over the exact membership;
6. the v6 owner intake and all five v6 packet hashes/census identities;
7. the pass-provenance matrix, hostiles A/B/C, owner adjudication, law, and
   checksum identities;
8. every mobile/Safari plan, cross-repository Safari law, Value formation
   matrix/status, and source snapshot authority used by v7;
9. an explicit start-of-authoring root rehash and end-of-authoring root rehash;
10. a guarantee that any missing, extra, reordered, replaced, or changed
    governed member is terminal before a v7 readiness claim.

V7 must authenticate and consume the capture as immutable input. It may not
discover current pins from mutable roots, accept caller summaries, self-repin,
or treat coherent capture/source resealing as authority. If the capture is
absent or any governed byte changes, the v7 coordinate must remain absent or
freeze at its first RED.

## 5. Concrete production-control repair

V7 must replace the inherited disconnected control registries. The exact
control denominator remains 44 unless a separately authenticated owner law
changes it before authoring.

For each control, v7 source must declare and statically connect:

- one exact authenticated baseline artifact and production-consumed field;
- a concrete source-owned mutation function, exact target pointer/range,
  before value/bytes, after value/bytes, and non-noop proof;
- full before/after artifact identities and an authorized collateral mask;
- one real owning production predicate/function identity, module/source range,
  consumed fields, and unique expected code;
- an audit path that mutates after outer-pin setup, reauthenticates the mutant,
  and reruns the same case-blind production validator;
- an owner-only bypass that suppresses the exact owning function and changes
  only the expected owner finding;
- every other 43 production predicates recomputed and retaining their expected
  findings on the same mutant; and
- a control-of-control that changes the owning predicate implementation on the
  same mutant and proves the guard detects the causal change.

Metadata labels such as `auditOnlyDisabledLeafId`, mutation prose, fabricated
outcomes, generic codes, count-only registries, or a validator that never
consumes the mutated field are terminal RED. The machine denominator for
owner/nonowner retention is `44 x 43 = 1,892` ordered pairings, derived from
the exact control and predicate registries rather than asserted as a literal.

Two later independent static hostile reviews must separately reconstruct:

1. baseline/mutation/artifact/collateral/reauthentication exactness; and
2. predicate ownership, unique codes, owner bypass, all-nonowner retention,
   control-of-control, and production call reachability.

Both reviews must be CLEAN and owner-intaken before any distinct
materialization or execution release can be considered.

## 6. Complete mount and instance closure

V7 must rebuild the instance graph from the authenticated source capture. It
may preserve v6 observations only as born-RED comparisons.

The source must:

1. enumerate and exact-join all 115 declaration edges, 717 physical mount
   occurrences, 11 pane-dynamic edges, 13 dynamic `:is` sites, and two
   teleports without padding or count substitution;
2. resolve every static, pane, dynamic, package, built-in, and teleport target
   into an explicit terminal disposition;
3. carry route, branch, workflow, parent instance, exact edge ID, occurrence,
   path, line, canonical attributes, predicate identity, data-key policy,
   action semantics, and teleport target into each instance identity;
4. reject duplicate instance identities, unresolved targets, missing owners,
   cycles without an explicit cycle witness, and every silent `continue` or
   dropped edge;
5. derive the exact route-to-instance identity set and compare it to v6's
   inherited counts only after derivation;
6. include all 15 routes, 11 panes, 33 page roles, 21 layers, two harnesses,
   and every mounted/exported subject in one exact page/state/execution join;
7. keep layer state independent rather than borrowing workflow-owner state;
8. bind every dynamic choice and teleport destination to its exact predicate,
   target set, and data-dependent identity policy; and
9. keep 887 subjects, 1,272 historical route instances, 1,343 execution
   subjects, 2,686 blocks, and 243,038 cells as non-authoritative archaeology
   unless the complete derivation independently reproduces them.

No literal count check can replace set equality, exact membership, or
provenance closure. The final Kronecker and Apple denominators remain `OPEN`.

## 7. Design and execution boundary

V7 may carry the 887 D1 source drafts only as born-RED archaeology. It may not
manufacture independent D2. Exact D2 remains 0/887 until a separately
authorized, independently authored design-critique pass is sealed and
owner-intaken.

Every Apple, Browser, accessibility, motion-frame, performance, data, auth,
network, lifecycle, and interaction cell remains `UNEXECUTED_RED`. Real iOS
Simulator Mobile Safari and installed desktop Safari remain zero. No
emulation, diagnostic observation, or source plan banks execution credit.

## 8. Unchanged denominators and credit

| Boundary | Current value |
|---|---:|
| Value historical formation cells | 15/15 |
| Value current-eligible formation cells | 0/15 |
| Value non-parser specification | 11/11 |
| workflow specification / historical exact evidence | 88/88 / 72/88 |
| D/L/C specification / historical exact evidence | 264/264 / 218/264 |
| parser-law chronology | 14/34, paused |
| V.MSK1-V.MSK5 accepted source closure | 0/5 |
| v6 concrete controls | 0/44 |
| v6 independent D2 | 0/887 |
| cross-repository owner slots | 0/5 |
| current-eligible subject passes | 0/12 |
| real iOS Mobile Safari / desktop Safari | 0 / 0 |
| final Kronecker / Apple denominator | OPEN / OPEN |
| product/execution/release/rebind/admission/authority credit | 0 |

This decision changes no numerator, denominator, eligibility, slot, or credit.

## 9. One-way route and stop

The only route opened is:

`root quiescent capture -> fresh v7 inert source pair -> two independent static hostiles -> owner source adjudication`

The route stops there. No materialization, source execution, D2 authoring,
Apple execution, cross-repository slot, or successor follows automatically.
