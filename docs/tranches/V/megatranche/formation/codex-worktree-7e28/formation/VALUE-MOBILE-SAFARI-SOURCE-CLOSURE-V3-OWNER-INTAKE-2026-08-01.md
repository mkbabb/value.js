<!--
  PROVENANCE — M-21 C-11 (codex-provenance ledger row 28), 7e28 worktree census, ruled 2026-08-03
  original: /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/VALUE-MOBILE-SAFARI-SOURCE-CLOSURE-V3-OWNER-INTAKE-2026-08-01.md
  original-mtime: 2026-08-01T20:45:10
  original-sha256: 0a81da8ccbf6a36cbc91b3ebe174d3e8e92d8803e195fcc9527b63dba8ab4065
  original-bytes: 12782
  ruling: ADOPT-COPY — M-21 C-11 — 2026-08-03
  note: this comment block is prepended to otherwise byte-exact original content, so the
  copy's own sha256 differs; both digests are recorded in CENSUS.md. The original bytes
  are exactly the last original-bytes bytes of this file.
-->
# Value Mobile/Safari source-closure v3 owner intake — 2026-08-01

## 0. Terminal status

**Terminal:** `AMEND / FALSE-GREEN / SOURCE-ONLY RED`

This is the durable Value-owner intake for the frozen v3 source-closure root:

`docs/tranches/V/megatranche/formation/mobile-safari-source-closure-v3/`

The v3 generator and validator produced a mechanically self-consistent candidate,
but independent root review found a material PAGE -> STATE -> Kronecker coverage
failure. The first-RED law therefore froze the root before independent reviews,
adjudication, findings, verdict, node/tree manifests, or checksum seal. The
mechanical green files are retained as negative evidence and grant no authority.

There is no same-root correction, retry, cleanup, reseal, owner release, or v4
opening in this intake. V1 and v2 also remain frozen and unchanged.

## 1. Bound authority and frozen coordinates

### 1.1 Root durability coordinate used by v3

| Root authority | SHA-256 | Size / replay |
|---|---|---|
| constellation handoff | `a5f9843b3ebecc298987f492f92b7873a38be2337be9f9b63342755cf5547ad4` | 89,755 bytes |
| machine manifest | `a7f8d7a05f2d36b9a5070c7ad6f54745409734b9fc65d49fcbf03c6014d06202` | 72,098 bytes |
| checksum packet | `84f16051e671cdc8f448fe15da5cb3de9e0c6c060f452c1da706b73216fb6bba` | 6/6 replayed |
| pi handoff | `4775251edb3dd95023be32602482861b53972fbb0af9cc1610dd22394eefaffc` | 41,897 bytes |

The root guaranteed these four files would not change during the v3 turn.
They are chronology inputs only and grant no Value execution or product credit.

### 1.2 V3 snapshot identities

| Identity | Value |
|---|---|
| source snapshot root identity | `49d4692e83462ebe11bd1bbf96b1f27e54a97834dc22a115ef1820a2345a5f23` |
| live evidence root identity | `e639df849d10219d01b7486aac40e99536ba607ce847a989247e6e85eec02649` |
| authenticated entry | `demo/color-picker/index.html` |
| authenticated entry SHA-256 | `c8d0738e62fc0cb606aa474ea3304746526a45f0a123e54ecb850856fe90403b` |
| source snapshot | 310 files / 2,341,009 bytes |
| bounded local/live agreement | 306 identical; one known `scripts/dev/dev.sh` harness supersession; three isolated formation inputs; zero unresolved drift |

The independent root audit reported one before/after root-identity prefix,
`ead3aaf8...`. Because the full digest was not supplied, this intake does not
promote that prefix into an exact coordinate. Instead, the complete file census
below is bound by a reproducible canonical census SHA-256.

## 2. Exact frozen v3 census

The frozen root contains 22 regular files, two child directories, three
directories including the root, zero symlinks, and 19,848,452 regular-file
bytes. Every regular file is mode `0644` with nlink `1`. The child directories
are mode `0755`. The canonical codepoint-ordered census format is one
newline-terminated row per child:

`type<TAB>mode<TAB>nlink<TAB>size<TAB>sha256-or-dash<TAB>relative-path`

Its SHA-256 is:

`8ed4938331b1ee70ff938c8af710e8dbc879a84ccab0bebae88752340f04ac80`

| Path | SHA-256 |
|---|---|
| `COMPONENT-INVENTORY.json` | `9061128872ed32b6b03646d06592734b731b4f08860f2b3674cfef1fb06a9232` |
| `DESIGN-BRIEFS.json` | `8f555260b94508f0f42ac737cfc4416babf98365d760ebad28e6d2417c1790e7` |
| `EQUIVALENCE.json` | `3bde71acd37db6c145f3e52ec851caaad9d7ac260d6d7cc7cc8d30deb9d94ae3` |
| `GENERATION-VALIDATION.json` | `ab4d8938d600e5b1b850f8a6b4151cc1fb1f0adea1265a781a0be1f95c035d5b` |
| `KRONECKER-REGISTRY.json` | `ffd9b4a14e69be644826a8bd5bbe072f33a7ebbfd2363a170f49a54eef076e0d` |
| `MOUNT-GRAPH.json` | `4597ddcc4d9ca15040684197f7b9e389d2f4bf8cc6335f415436f439f346d0c0` |
| `PAGE-INVENTORY.json` | `d00edb047e1d38da64bd83e74ff62e3aa1ac7d3a6b3fff3e9021a4c3b9dfa3aa` |
| `README.md` | `a930ae57021c3da90e83258d70f45818e2a2be8dd9558b60736eee3009d8532d` |
| `SOURCE-SNAPSHOT.json` | `cf812c05539c6fd0b0975870fa24c173936401cad0dd7b6d1953ebc6c70618b4` |
| `STATE-REGISTRY.json` | `588bc82387d2ffa89fa9b3f3f821b864df271646383297740ae60a7f26f5472f` |
| `VALIDATION.json` | `e099ea40a71cb90f948da6de4407089011e9d837d875dc9df9e30613ea15de6e` |
| `raw/COMPONENT-PARSE-RAW.json` | `9dfaeb5b5551e5d558449105ed895a4b7adb55b82e774a63cb6c8309d2e6331a` |
| `raw/DESIGN-INPUTS-RAW.json` | `3ed23ab6962d7703fa33824b9046b313e05beb65dfdd96bfe8ba74f7ace795b4` |
| `raw/KRONECKER-FACTORS-RAW.json` | `8e9e1560fa498b6fcddacb5da772a34fd23c3b7767e1508709fb4e25560230f0` |
| `raw/MOUNT-PARSE-RAW.json` | `51fa8202710fbd8022a2948f62856e5f2e282cbb6f1ad0c3d0074a23f3e71a8f` |
| `raw/ROUTE-PARSE-RAW.json` | `09b13fd8d86d76817062c1cf8eb3e38386b761096198e2ba7aa64434cec437ae` |
| `raw/SOURCE-READ-RAW.json` | `0487b2e76054e12471bb641ac5379208ccea6552ec3b7a5945419b932e93548a` |
| `raw/STATE-ORIGINS-RAW.json` | `f669eef3e904d0d161c402f448ed2d0f2b0e93c63df3e901988a44dbd0bac39b` |
| `raw/VALIDATOR-RAW.json` | `e099ea40a71cb90f948da6de4407089011e9d837d875dc9df9e30613ea15de6e` |
| `tools/freeze-source-snapshot.mjs` | `f280cea40f604b73b0c07276bdf38c3df3b707104b6730f58e2810bced94da50` |
| `tools/generate-source-closure.mjs` | `a4067cdb4f50265e941c35d5abd7e2acd9f0a787d86a59396ee1b6aa5f76eedf` |
| `tools/validate-source-closure.mjs` | `811d96ee9a87a294795c9374942f2f30b28752b9a7b8bd6e5fe47a2da732f755` |

No review directory or review file landed. The Sol P1 reader was interrupted
after the first material RED; no Luna or later Sol adjudication ran.

## 3. Candidate census retained only as negative evidence

The frozen candidate mechanically enumerated:

- 14 named routes plus one wildcard;
- 69 page/layer subjects: 14 routes, one wildcard, 33 physical page-like
  surfaces, and 21 inline layers;
- 88 SFC workflows, with 86 mounted and two exported-unmounted harnesses;
- 115 declaration edges, 126 static mount edges, 11 dynamic mount edges, and
  1,272 route instances;
- 157 state records and 157 D1/D2 records;
- 1,295 execution subjects, 2,590 Kronecker blocks, and 2,590 singleton
  equivalence classes;
- preliminary iOS cells `1,609,605,648`, desktop cells `1,609,605,648`, and
  total cells `3,219,211,296`, all marked `UNEXECUTED_RED`.

These values describe the failed candidate. The block and cell denominators are
not exact and receive no closure credit.

## 4. First material falsifier

`PAGE-INVENTORY.json` names all 69 page/layer subjects and
`STATE-REGISTRY.json` contains records for all 69. However,
`KRONECKER-REGISTRY.json` constructs execution subjects only from:

- 1,272 mounted route instances;
- two harnesses; and
- 21 layers.

`ROUTE-01` through `ROUTE-14`, `ROUTE-WILDCARD`, and
`PAGE-LIKE-001` through `PAGE-LIKE-033` never enter the Kronecker execution
subjects. The layer blocks use their owner workflow's state rather than the
layer's own page/layer state. Thus all 69 page/layer state records are dead with
respect to the proposed execution denominator.

The validator checks internal block arithmetic but not page/state coverage.
Its green result is therefore a false-green and is superseded by this owner
intake. This is the terminal first RED.

## 5. Secondary falsifiers

### 5.1 Route-instance identity loss

Deleting only `instanceId` from each of the 1,272 route instances produces:

- 26 duplicate groups;
- 94 affected rows;
- 68 indistinguishable excess instances;
- a group-size histogram of 12 pairs and 14 five-row groups.

Each of the 14 named routes contains five indistinguishable
`ActionToolbar -> ActionButton` instances. The owning declaration edges
`MOUNT-079` through `MOUNT-083` carry distinct occurrence, source line, attrs,
copy, and action semantics, but route instances omit `viaEdgeId`, occurrence,
line, attrs, and a data-dependent identity policy. An arbitrary unique
`instanceId` cannot bind a Kronecker cell to the distinct control being audited.

### 5.2 Interaction, dynamic mounts, and harnesses

- Real `PaginationBar` Button and `@click` interactions are marked `N_A`.
- Thirteen dynamic components remain unresolved and undispositioned.
- The two exported-unmounted harnesses retain placeholder
  `variantsAndDefaults` contracts rather than exact props/events/slots,
  variants, defaults, and consumer assertions.

### 5.3 D1/D2 independence and specificity

- D1 and D2 are generated by the same program rather than independent
  authorial readings.
- All 157 D2 rows retain
  `DISJOINT_CRITIQUE_RULESET_D2_PENDING_INDEPENDENT_LUNA_CONFIRMATION`.
- All 157 D2 rows have the same `inputPaintChallenge` value.
- Removing injected subject IDs still leaves 35 semantic collisions.
- The validator's generic-copy check establishes the presence of `subjectId`,
  not independent subject-specific judgment.

### 5.4 Axes, snapshot, and seal

- O/I/X/N/T are recorded as counts rather than complete named coordinates.
- Singleton equivalence classes provide no reduction proof and do not repair
  missing page, state, or instance identities.
- Snapshot identity is not independently recomputed from the bound entry bytes.
- No node manifest, tree manifest, final checksum file, FINDINGS, or VERDICT
  exists.

## 6. Reconciliation with the terminal message

The Value terminal message correctly reported `SOURCE-ONLY RED`, 22 files,
three directories including root, 19,848,452 bytes, zero symlinks, the
route-instance duplicate census, generic D2 evidence, absent seal/reviews, and
zero credit. The independent root audit adds an earlier and broader primary
failure: the PAGE -> STATE -> Kronecker join is incomplete. That root finding
is the governing first RED; the route-instance and design findings remain
secondary corroboration.

Two failed read-only diagnostic invocations are preserved honestly:

1. A JavaScript wrapper parsed the shell expression
   `rel=${p#"$v3_root/"}` as a template expression and failed before invoking
   the shell with `SyntaxError: Missing } in template expression`. It had no
   shell exit code and changed no bytes. The escaped rerun exited 0.
2. A Node diagnostic guessed the wrong `DESIGN-BRIEFS.json` array key and
   exited 1 with `TypeError: drows is not iterable`. It changed no bytes. The
   schema-bound rerun using `briefs` exited 0.

Both are external inspection-script defects, not evidence that repairs or
supersedes the material source-closure falsifiers.

## 7. Credit vector and terminal dispositions

| Credit / gate | Value |
|---|---:|
| V.MSK1-V.MSK5 accepted source closure | 0/5 |
| real iOS Safari executions | 0 |
| installed desktop Safari executions | 0 |
| Browser/API/package/product executions | 0 |
| visual/product/release/authority credit | 0 |
| owner release | false |
| v4 authorized or opened | false |

Terminal dispositions:

- **KEEP** v3 bytes as negative formation evidence.
- **PRUNE** its mechanical green and preliminary denominator from positive
  authority.
- **FOLD** the root's PAGE -> STATE -> Kronecker falsifier and all secondary
  findings into a separately authorized successor specification.
- **PRUNE** any same-root repair, retry, cleanup, review continuation, or seal.
- **MOVE** all Apple, Browser, API, package, product, and release work to later
  independently authorized execution waves; none is opened here.

## 8. Smallest lawful fresh v4 successor

A fresh, non-overlapping v4 source-only root may be created only after a
separate root release. Its minimum born-RED requirements are:

1. recompute and authenticate the source snapshot from every bound entry byte,
   including `demo/color-picker/index.html`, before derivation;
2. join every route, wildcard, physical page-like surface, and inline layer to
   its own state record and its own Kronecker execution subject;
3. carry `viaEdgeId`, occurrence, source line, attrs, action/copy semantics,
   and an explicit data-dependent identity policy into every mounted instance;
4. parse and bind actual interactions rather than applying blanket state
   assignments or false `N_A` values;
5. resolve and disposition every dynamic component and portal;
6. replace harness placeholders with exact props/events/slots, variants,
   defaults, fixtures, and consumer assertions;
7. enumerate O/I/X/N/T as named coordinates and prove all applicability and
   equivalence reductions with own-reason hostiles;
8. produce subject-specific D1 briefs and a genuinely independent D2 receipt;
   generated IDs cannot serve as evidence of authorial independence;
9. validate 1:1 page/state/instance/block coverage, duplicate and ambiguity
   rejection, exact denominator arithmetic, and source/aggregate consistency;
10. preserve raw-first evidence and finish only after independent readings and
    adjudication with node manifest -> tree manifest -> checksum last, with no
    post-checksum writes.

Until that separate release exists, v3 remains the terminal frozen coordinate,
v4 does not exist, and every execution/product credit remains zero.
