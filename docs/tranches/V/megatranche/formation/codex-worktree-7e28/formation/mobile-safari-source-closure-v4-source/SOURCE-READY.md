<!--
  PROVENANCE — M-21 C-11 (codex-provenance ledger row 28), 7e28 worktree census, ruled 2026-08-03
  original: /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/mobile-safari-source-closure-v4-source/SOURCE-READY.md
  original-mtime: 2026-08-02T03:41:59
  original-sha256: 407450d70e9d7207ad3a369963bc246cd06d8127f9d52a4c8132b874a936ef99
  original-bytes: 9286
  ruling: ADOPT-COPY — M-21 C-11 — 2026-08-03
  note: this comment block is prepended to otherwise byte-exact original content, so the
  copy's own sha256 differs; both digests are recorded in CENSUS.md. The original bytes
  are exactly the last original-bytes bytes of this file.
-->
# Value Mobile/Safari source-closure v4 — source-only boundary

## Status

**`SOURCE_ARCHITECTURE_AUTHORED / STATIC_OWNER_REVIEW_REQUIRED / AUTHORITY NONE`**

This directory is the bounded v4 source-only successor authorized after the
frozen v3 `AMEND / FALSE-GREEN` intake. It contains exactly two inert source
documents and no child directory. The JavaScript source was not run, parsed by
Node, imported, checked, or used to generate any registry. No Browser, Safari,
iOS Simulator, API, Docker, package, product, network, release, or parser action
occurred.

The prospective source is not a materialization receipt and cannot admit
itself. A separate Value owner source review must authenticate these final
bytes and explicitly release a distinct materialization root before any
execution of the source. The first material source-review or later execution
RED freezes this root without repair or reseal.

## Frozen predecessor and authority pins

| Input | SHA-256 | Disposition |
|---|---|---|
| v3 owner intake | `0a81da8ccbf6a36cbc91b3ebe174d3e8e92d8803e195fcc9527b63dba8ab4065` | **KEEP** as terminal negative evidence |
| Mobile/Safari plan | `18f8ac83045496e6553d10d21e5cea26d2da43304952d4c67940aecb2e7a72ef` | **KEEP** as source law |
| cross-repo Safari law | `2ab310b45b9289b53f5e1993ef870a0ae80cdd12c2856628fa523df85bf3ae5e` | **KEEP** as source law |
| frozen resurrection handoff | `a5f9843b3ebecc298987f492f92b7873a38be2337be9f9b63342755cf5547ad4` | chronology only |
| v3 source snapshot | `cf812c05539c6fd0b0975870fa24c173936401cad0dd7b6d1953ebc6c70618b4` | **KEEP** as the single product-byte domain |
| source-root identity | `49d4692e83462ebe11bd1bbf96b1f27e54a97834dc22a115ef1820a2345a5f23` | exact 310 files / 2,341,009 bytes |
| authenticated entry | `demo/color-picker/index.html` / `c8d073bda8d6dee378c7f05e6b9365364ddf742563f0650225ad2aaaf2935a0d` | exact source member |

The worktree and live evidence tree both resolve product source to
`e01d0065fa6c7c80282280566af2b9a4add809bf`. The worktree is detached and the
live tree is separately dirty on `tranche-u`; the source architecture reads no
live product byte outside the already frozen snapshot. The v3 intake is bound
by file hash as negative evidence; its prose-level abbreviated entry digest is
not used to override the exact snapshot entry row.

## V3 falsifier folded into v4

V3 remains immutable and grants zero credit. Its source candidate contained 14
routes plus one wildcard, 33 page roles, 21 layers, 88 workflows, 1,272 mounted
instance seeds, two harnesses, 1,295 execution subjects, and 2,590 platform
blocks. The independent owner/root audit proved:

- all 69 page/layer state IDs were absent from Kronecker `sourceSubjectId`;
- 42 layer blocks borrowed state from only 18 workflow owners;
- removing arbitrary `instanceId` exposed 26 duplicate groups, 94 affected
  rows, and 68 indistinguishable excess rows;
- 13 dynamic `:is` sites were undispositioned;
- VC-036 and VC-049 harness contracts were placeholders;
- D1 and D2 were co-generated and generic;
- count-only axes, singleton equivalence, snapshot, and seal checks were
  insufficient.

The v4 source therefore derives execution identity from route, branch,
workflow, parent instance, exact edge, occurrence, source path and line,
canonical attributes, predicate-AST digest, data-key policy, action-semantic
IDs, and teleport target. It never accepts an arbitrary instance ordinal.

## Exact source contract

The inert production pipeline is fixed in this order:

1. `authenticateInputs`
2. `buildSourceSubjects`
3. `buildMountAndInstanceGraph`
4. `buildStateApplicability`
5. `bindIndependentDesignPasses`
6. `buildKroneckerPlan`
7. `validateProduction`

Only named read-only `node:fs` and `node:crypto` imports exist. The pipeline
accepts no caller summaries, callbacks, result booleans, control identifiers,
precomputed counts, or precomputed parser/design truth. The snapshot is read
once, canonically rehashed from all entry rows, frozen, and then shared by
reference. Product source is not reread.

The exact membership bounds are:

| Projection | Bound |
|---|---:|
| named routes + wildcard | 14 + 1 |
| physical workflows | 88 |
| page roles + layers | 33 + 21 |
| total semantic roles | 157 |
| visual barrel declarations | 25 |
| declaration/static/pane-dynamic edges | 115 / 126 / 11 |
| dynamic `:is` sites / teleports | 13 / 2 |
| historical instance-seed replay | 1,272, born-RED pending exact provenance replay |
| exact harnesses | VC-036 and VC-049 |
| independent design rows | D1 157 + D2 157 |
| named state axes | I37 + X31 + N24 + A23 + T22 = 137 |
| applicability decisions | 157 × 137 = 21,509 |

If—and only if—edge-bound replay independently retains 1,272 mounted seeds,
the derived execution-subject count is 1,272 + 2 + 69 = 1,343 and the two
platform classes yield 2,686 blocks. Neither value is accepted as a literal
input. The final Kronecker cell denominator remains `OPEN` until all 21,509
applicability decisions, D1/D2 receipts, fixtures, named platform coordinates,
and equivalence witnesses are complete.

## State and design boundary

Every one of the 157 semantic roles receives every named state coordinate.
Each future decision must be exactly one of:

- `REQUIRED_UNEXECUTED_RED`, with source evidence, fixture, reason, and its own
  control; or
- `N_A_SOURCE_IMPOSSIBLE`, with a closed impossibility reason, source evidence,
  and a false-N/A control.

The state vocabulary includes initial/inactive/idle/active, direct
interactions and editing/validation/submission/settling, auth/Admin, storage,
data, online/slow/offline/timeout/CORS/CSP, route and BFCache lifecycle,
rotation/safe-area/software keyboard, VoiceOver, Dynamic Type, RTL,
forced-colors equivalent, motion frames at 0/120/420, interruption, reversal,
reduced motion, long tasks, layout shift, memory, GPU, and background resume.

The `frontend-design` formation rubric is encoded as a hard D1/D2 contract.
D1 must name the user, job, protagonist, exact copy, hierarchy, measured-chroma
signature, typography, layout/container/responsive/safe-area behavior,
keyboard/focus/accessibility, one semantic motion owner, named frames and
performance budget, Golden Glass, Breath of Life, Movement of Momentum,
removable decoration, and terminal disposition. D2 must independently
challenge generic defaults, cards/dividers/containers, copy, protagonist,
paint-versus-interaction ownership, focus, VoiceOver, Dynamic Type, RTL,
reduced motion, performance, all three design doctrines, and removable
decoration. Every concept terminates in `KEEP|FOLD|MOVE|SPLIT|PRUNE` with an
exact receiver.

D1 and D2 deliberately remain capability-gated inputs. They require different
task IDs, immutable roots, authorial readings, and owner-pinned receipts. The
source does not manufacture “independence” by running two templates in one
program, and it cannot complete while those slots are pending.

## Hostile source-review registry

The source defines 36 exact own-leaf controls spanning:

- input pins and membership, snapshot identity;
- named route, wildcard, workflow, page, layer, and Admin coverage;
- static, dynamic, teleport, instance provenance, duplicate identity,
  harness, and barrel joins;
- state membership, false N/A, parsed interaction, and layer-state ownership;
- D1 specificity, D2 independence, and measurable performance;
- named rather than count-only axes, complete execution subjects, fake Safari,
  equivalence omission/erasure, and structural parity;
- control omission/duplication, checksum membership/order, post-write fence,
  zero-credit enforcement, and import allowlist.

Every control row binds one real production leaf, one concrete mutation, one
unique expected code, an owner-bypass result, all-nonowner retention, and a
control-of-control. Production is case-blind and does not receive the control
registry. The audit path can disable one real leaf only and is not called.

## Seal and credit boundary

This source-only root is intentionally unsealed as a materialized audit: there
is no generated registry, raw execution receipt, node manifest, tree manifest,
checksum packet, independent source-review result, owner acceptance, or Safari
evidence. A later materialization may seal only in raw receipt -> aggregates ->
node manifest -> tree manifest -> codepoint-ordered checksum-last order with no
post-checksum write.

Current counters are binding:

| Credit | Value |
|---|---:|
| owner source acceptance | 0 |
| V.MSK1-V.MSK5 materialized closure | 0/5 |
| real iOS Simulator Mobile Safari | 0 |
| installed desktop Safari | 0 |
| Browser / API / Docker / package / product execution | 0 |
| visual / release / authority / execution credit | 0 |
| owner release required | true |

Terminal dispositions: **KEEP** the authenticated snapshot and exact v3
negative evidence; **FOLD** the page/state/instance/design falsifiers into this
source architecture; **SPLIT** independent D1, D2, source review, materialized
registry, and later Apple execution authorities; **PRUNE** arbitrary IDs,
co-generated critiques, count-only axes, false N/A, fake Safari, caller truth,
same-root retry, and inferred credit; **MOVE** all materialization and execution
behind explicit owner releases.

