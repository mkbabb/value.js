<!--
  PROVENANCE — M-21 C-11 (codex-provenance ledger row 28), 7e28 worktree census, ruled 2026-08-03
  original: /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/mobile-safari-v7-control-map/RECEIPT.md
  original-mtime: 2026-08-02T20:54:42
  original-sha256: 3c00fa4dd2a4f60ab277a020a0b974fe2e6aa2455b78abe0242fb5521fba2223
  original-bytes: 3549
  ruling: ADOPT-COPY — M-21 C-11 — 2026-08-03
  note: this comment block is prepended to otherwise byte-exact original content, so the
  copy's own sha256 differs; both digests are recorded in CENSUS.md. The original bytes
  are exactly the last original-bytes bytes of this file.
-->
# Value Mobile/Safari v7 control-map owner HOLD — 2026-08-02

## Status

**Decision:** `TERMINAL HOLD / CONTROL_SEMANTIC_BIJECTION_ABSENT`

**Authority:** `NONE`

This receipt is additive tranche source law only. It creates no governing-root
capture and no v7 source coordinate. It runs no validator and grants no source,
product, parser, Browser, Safari, Simulator, API, Docker, package, release,
rebind, admission, owner-slot, or constellation credit.

## Bound law and frozen registries

The mapping binds the v7 owner decision
`e1522725a855a1f413bea7a62e4bfc2c6897668f4b6f26d3a22d69c404325486`,
the frozen v5 source/control tuple, the v5 hostile-order correction
`b9febba9ee253299b5917005b1f882974c9858007d1391015fad4576bb50c485`,
and the frozen v6 registry
`8b338c4e163714e68a484881d19838296893029386d6e88d63ee70a3e7028160`.

`CONTROL-MAP.json` enumerates every one of the 54 unique v5 controls exactly
once using only `CARRY|MERGED_INTO|SUPERSEDED|HISTORICAL_ONLY`. It also
enumerates all 44 v6 IDs exactly once in the target ledger and carries the
source leaf, family, mutation, expected code, target predicate, operation
class, artifact/field class, ambiguity candidates, and exactness disposition.

## First blocker

No total semantic bijection exists under the one-time source-disposition law.

Only 14/44 v6 semantic rows have a unique or exact merged v5
predecessor. 30/44 remain semantically unconsumed, and
39/54 v5 controls remain `HISTORICAL_ONLY` because assigning them
would require a split or an unrelated semantic substitution.

Concrete fanout conflicts include:

- v5 `MSK-C15` harness completeness versus v6 props, events, slots, defaults,
  and harness-promotion predicates;
- v5 `MSK-C21` D1 specificity versus six distinct v6 D1/copy/responsive/a11y/
  motion predicates;
- v5 `MSK-C24` named-axis membership versus v6 omission, duplicate, and
  dimension-drift predicates; and
- v5 `MSK-C13` instance provenance versus five distinct v6 source-evidence,
  ownership, predicate, and loop predicates.

V6 also contains predicates such as API ownership, mount-loop closure, pane
omission, responsive/accessibility/motion contracts, and report declaration
drift without one unique v5 semantic predecessor after every v5 row is consumed
once.

A capture cannot repair this by renaming, silently dropping rows, splitting one
v5 control across multiple v6 IDs, or assigning member-auth controls to
unrelated component/design predicates.

## Denominator effect

The desired v7 denominator remains 44. Its ordered nonowner-retention
denominator remains machine-derived as `44 * 43 = 1,892`. Because the semantic
mapping is not closed, admitted control rows and admitted nonowner retentions
remain zero.

The root-owned quiescence capture and v7 source root remain forbidden. A
separate owner law must either define a new exact 44-row semantic registry with
authenticated predecessor dispositions or retain all 54 v5 controls and amend
the v7 denominator. Neither action is authorized here.

## Unchanged convergence

| Boundary | Value |
|---|---:|
| Value historical formation cells | 15/15 |
| Value current eligibility | 0/15 |
| Value non-parser specification | 11/11 |
| parser-law chronology | 14/34, paused |
| V.MSK1-V.MSK5 accepted source closure | 0/5 |
| v6 controls executed | 0/44 |
| v6 independent D2 | 0/887 |
| cross-repository owner slots | 0/5 |
| current-eligible subject passes | 0/12 |
| final Kronecker / Apple denominator | OPEN / OPEN |
| product/execution/release/rebind/admission/authority credit | 0 |
