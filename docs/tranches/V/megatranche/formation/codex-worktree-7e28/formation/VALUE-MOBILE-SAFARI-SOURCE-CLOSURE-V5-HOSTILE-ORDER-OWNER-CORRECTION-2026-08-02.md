<!--
  PROVENANCE — M-21 C-11 (codex-provenance ledger row 28), 7e28 worktree census, ruled 2026-08-03
  original: /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/VALUE-MOBILE-SAFARI-SOURCE-CLOSURE-V5-HOSTILE-ORDER-OWNER-CORRECTION-2026-08-02.md
  original-mtime: 2026-08-02T14:33:53
  original-sha256: b9febba9ee253299b5917005b1f882974c9858007d1391015fad4576bb50c485
  original-bytes: 6522
  ruling: ADOPT-COPY — M-21 C-11 — 2026-08-03
  note: this comment block is prepended to otherwise byte-exact original content, so the
  copy's own sha256 differs; both digests are recorded in CENSUS.md. The original bytes
  are exactly the last original-bytes bytes of this file.
-->
# Value Mobile/Safari source-closure v5 hostile-order owner correction — 2026-08-02

## 0. Status

**Terminal:** `AMEND / SOURCE-ONLY RED`

**Governing first falsifier:** `DISCONNECTED_PRODUCTION_CONTROLS`

This additive owner correction changes only the ordering and authority of the
v5 source findings. It does not modify, repair, execute, review-continue,
clean, or reseal the frozen v5 root. It authorizes no successor coordinate and
grants no source, Browser, Safari, Simulator, API, Docker, package, product,
parser, release, rebind, admission, or constellation credit.

## 1. Bound predecessor and frozen v5 census

The governing v5 source ruling remains:

`docs/tranches/V/megatranche/formation/VALUE-MOBILE-SAFARI-SOURCE-CLOSURE-V4-OWNER-INTAKE-AND-V5-SOURCE-RULING-2026-08-02.md`

SHA-256:

`26e3bbc50a14f06a2b2cfcd1e692f99efce3489ddc3ff68914ce3f44ab2e9921`

Frozen source root:

`docs/tranches/V/megatranche/formation/mobile-safari-source-closure-v5-source/`

It contains exactly two regular files, no child directories, symlinks, or
special files, and 530,529 regular-file bytes. Both files are mode `0644` with
nlink `1`.

| File | Bytes | SHA-256 |
|---|---:|---|
| `preflight-value-mobile-v5.mjs` | 524,158 | `33bf743458517b53f469d35684c994e6d0a449b002a59c32a53929fdb53d8421` |
| `SOURCE-READY.md` | 6,371 | `c176fe28af0df1302b576b4c6b2c5d75db9e1bbdd3b05dfdcbb0d8f15677f89d` |

The SHA-256 of the codepoint-ordered, newline-terminated census rows
`basename<TAB>bytes<TAB>mode<TAB>nlink<TAB>sha256` is:

`3356b6b3f7682e9bb54d1f8e50240edf93209cd08c9d809875f71d6272a555cb`

These identities remain exact and unchanged.

## 2. Superseded finding order

The terminal v5 message named `MEMBER_IDENTITY_NOT_LENGTH_FRAMED` as the first
material defect. That ordering is superseded.

The pinned ruling at lines 130-149 requires an exact, byte-derived,
domain-separated member identity carrying path, kind, mode, nlink, encoding,
decoded byte length, and decoded-byte SHA-256. It does not require a particular
length-framing scheme. V5 lines 528-534 use a fixed versioned domain, a NUL
separator, and canonical JSON over raw-NUL-free structured fields. Length
framing may be a useful later hardening choice, but no separately authenticated
law made its absence a terminal v5 violation.

Disposition: **PRUNE** `MEMBER_IDENTITY_NOT_LENGTH_FRAMED` as the governing
first RED. **KEEP OPEN** length framing only as a non-authoritative future
hardening option.

## 3. Governing first material falsifier

`DISCONNECTED_PRODUCTION_CONTROLS` is the first incontestable source defect.

The pinned ruling lines 158-160 requires every control to disable or corrupt
one real production-consumed leaf and retain all nonowners. Count-only,
caller-supplied truth, and generic failure codes are explicitly insufficient.

The v5 source instead does the following:

1. Lines 340-417 declare 36 carried and 18 member controls as strings naming a
   leaf, mutation description, and expected code.
2. Lines 909-915 manufacture `PRODUCTION_LEAVES` from those declared strings
   and verify only registry length and uniqueness.
3. Lines 938-953 clone an arbitrary aggregate and attach
   `auditOnlyDisabledLeafId` and `auditOnlyMutation` metadata.
4. `validateProduction` never reads either audit-only field.
5. No control changes snapshot bytes, binary authority, route/mount/state
   input, predicate implementation, or another production-consumed value.
6. No mutated input is reauthenticated and no production pipeline is rerun.
7. No owning predicate is bypassed and no nonowner predicate is observed or
   retained.
8. Both registries declare `ownerBypassExpected` as
   `REJECT_SAME_EXPECTED_CODE`, contradicting the required owner-only causal
   bypass proof rather than implementing it.

The 54 rows therefore establish labels, not own-reason controls. Exact concrete
control closure is:

`0/54 = 0%`

This defect directly violates the pinned owner ruling and freezes v5 under the
first-RED law. `SOURCE-READY.md` remains part of the frozen negative-evidence
root but is superseded as a positive readiness claim.

## 4. Secondary denominator defects

The instance denominator remains conditional and open independently of the
control failure:

- line 795 silently `continue`s when an instance key was already seen;
- line 800 silently `continue`s when a static target is unresolved;
- the traversal at lines 788-804 follows only the static edge collection;
- the 11 pane-dynamic edges, 13 dynamic `:is` sites, and two teleports are not
  traversed into the instance identity set; and
- line 809 checks equality to the inherited literal `1,272` rather than
  deriving and exact-comparing a complete identity set across all
  `115 + 11 + 13 + 2` edge classes with missing, duplicate, unresolved, and
  silent-skip rejection.

Consequently, 1,272 route instances, 1,343 execution subjects, 2,686 platform
blocks, and the final Kronecker/Apple cell denominator remain conditional or
`OPEN`. None receives source-closure or execution credit.

## 5. Terminal dispositions

- **KEEP** the exact v5 bytes as negative source evidence.
- **FOLD** the disconnected-control proof and denominator defects into any
  later owner-designed successor law.
- **PRUNE** the v5 source-ready claim, all `54/54` control implications, and
  length framing as the governing first defect.
- **SPLIT** a future real mutation/reauthentication/rerun/bypass/nonowner
  control plane from registry labels.
- **MOVE** all source correction, reviews, materialization, Apple-platform
  execution, and product work behind new explicit owner rulings.

There is no same-root correction and no v6 inference or authorization in this
receipt.

## 6. Unchanged denominators and credit

| Boundary | Current value |
|---|---:|
| Value non-parser formation specification | 11/11 |
| workflow specification / historical exact evidence | 88/88 / 72/88 |
| D/L/C specification / historical exact evidence | 264/264 / 218/264 |
| Value parser-law chronology | 14/34, paused |
| V.MSK1-V.MSK5 accepted source closure | 0/5 |
| concrete v5 hostile controls | 0/54 |
| owner-accepted mobile source coordinates | 0 |
| real iOS Mobile Safari executions | 0 |
| installed desktop Safari executions | 0 |
| Browser/API/Docker/package/product executions | 0 |
| visual/product/release/rebind/admission/authority credit | 0 |

The cross-repository Value input slot remains null. Parser remains paused under
handoff `ced234406d3d9ad6bb13e4dce92452a596c9af55dd2091fd90a83f02502f20f7`.
