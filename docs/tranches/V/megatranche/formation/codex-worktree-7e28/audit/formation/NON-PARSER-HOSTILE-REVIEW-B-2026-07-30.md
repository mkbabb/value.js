<!--
  PROVENANCE — M-21 C-11 (codex-provenance ledger row 28), 7e28 worktree census, ruled 2026-08-03
  original: /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/audit/formation/NON-PARSER-HOSTILE-REVIEW-B-2026-07-30.md
  original-mtime: 2026-07-30T14:42:36
  original-sha256: 214f032198c8aef0a7d87792f80822ca847f1a9ff7aaf819202aac7854f0fdd6
  original-bytes: 4790
  ruling: ADOPT-COPY — M-21 C-11 — 2026-08-03
  note: this comment block is prepended to otherwise byte-exact original content, so the
  copy's own sha256 differs; both digests are recorded in CENSUS.md. The original bytes
  are exactly the last original-bytes bytes of this file.
-->
# Value non-parser hostile review B — false credit and forbidden-edge replay

Date: 2026-07-30
Verdict: CLEAN
Orthogonality: challenges evidence strength, authority, and graph polarity;
does not reuse review A's denominator/bijection test
Credit: formation-specification review only; zero execution/product credit

## Bound inputs

```text
a911162502a5a32cb512c00af266004e1d5d3f56ed2a963256b75caa3ee6c883  frontend specification
d51e0d3056ec4b3bdebc1bad2119d453de291189dadfce8e9dd2e2bcc5ae8eff  library/API/DAG specification
4792d8032463e82e6130ce9e9889268cf4759fa6689cadaaaaf1cdaad137c2ac  parser-law matrix before this closure
3f3f80c6099229c9502cec43f3a34e151d62ad279f4aa55205984844ac08377a  formation status before this closure
```

## Hostile questions and results

| Challenge | False-green attempted | Result |
|---|---|---|
| Browser coordinate | treat the first live `localhost:9000` observation as isolated evidence | rejected; only `127.0.0.1:41739` exact-archive diagnostic survives |
| Route sweep | convert 14/14 navigation into 88/88 component execution | rejected; component receipts remain 0 new and individually RED |
| H1/state | call a route clean because title/main/empty copy exists | rejected; zero H1 on all fourteen routes is a durable RED finding |
| Motion | call 0/120/420 observation a motion pass | rejected; it shows two simultaneous protagonists at 0/120 and no PRM/RTL/viewport coverage |
| Keyboard | infer keyboard completion from semantic buttons | rejected; harness Tab did not leave `body`; keyboard cells remain RED |
| Existing build | convert seven old entries building into eight target offerings executing | rejected; `/path` remains absent and 0/1 |
| API tests | convert 36/36 old tests into policy/membership closure | rejected; born-RED policy, membership, collision, numeric-route, and payloadHash cells remain unexecuted |
| API storage | use existing green tests or Fourier prototypes to select a complex backend | rejected; `LOCAL-SNAPSHOT` stays default and all complex candidates stay unselected |
| Payload identity | promote current `computeContentHash` or export `contentDigest` as final `payloadHash` | rejected; algorithm/source freeze and complete revision field set remain RED |
| Parser pause | mark `/css` or `/path` complete because their surface is specified | rejected; implementation/release/rebind stays blocked |
| Keyframes direction | create Value→Keyframes runtime dependency from the consumer contract | rejected; dependency polarity remains Keyframes→explicit Value capabilities |
| Glass candidate | treat development artifacts or build-watch freshness as final | rejected; published immutable Glass 8 complete artifact set still required |
| Package aliases | permit source/deep aliases to make the demo work | rejected; only generated literal public subpaths survive |
| Disposition | use `BUILD` as a terminal concept status | rejected; every concept ends in KEEP/FOLD/MOVE/SPLIT/PRUNE |
| Audit identity | relabel these self-reviews as V.Q1/V.Q2 or constellation Clean A/B | rejected; reviews are local hostile specification checks only |

## Credit vector replay

Every new artifact must carry:

```text
formationSpecification = true
productExecution       = false
apiExecutionCredit     = false
browserExecutionCredit = false
packageCredit          = false
consumerCredit         = false
storageSelectionCredit = false
parserCredit           = false
releaseCredit          = false
constellationClose     = false
```

Deleting any false field, toggling it true, or using a diagnostic receipt as
the sole source for an execution edge is a failure.

The parser-law matrix remains exactly 14/34. The non-parser closure is a
separate overlay and cannot manufacture P01–P09, C01–C08, or Q01–Q03.

## Forbidden-edge replay

The following edges remain invalid:

```text
/transform -> parse-that | /css | /color | /path
Value package -> Keyframes package
Keyframes eager root or named timing -> /css | /path | /transform | parse-that
Value demo/test -> raw Value source | raw parse-that | copied Glass CSS
UI role/state -> API authorization
slug | releaseHash | payloadHash -> global object membership
rejected/superseded prototype -> positive formation or execution edge
diagnostic Browser/API receipt -> product credit
```

All positive edges retain their named predecessor and immutable-artifact
requirements. No convenience alias, wrapper, compatibility entry, or dual
repository/parser path is allowed to repair a missing predecessor.

## Terminal review

No false-credit or graph-polarity survivor was found. The two formation
specifications are admissible inputs to the later local agglomeration while
all parser, Fourier, Keyframes, Glass, product, release, and final clean-audit
gates remain open.
