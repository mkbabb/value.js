claude-fable-5 (served model id — fresh Fable agglomerator, M-23)

# CENSUS — fourier-analysis formation agglomeration · 2026-08-03

**Role.** Fresh-Fable agglomeration (M-23) of the three Opus census lanes into the FORMATION
SUBSTRATE for the fourier-analysis sub-session. Lanes folded WHOLE, challenged per L-18 spirit;
every carried claim keeps its lane provenance (`[FE §n]` = lane-frontend, `[CRUD §n]` = lane-crud,
`[DOCS §n]` = lane-docs). Two contested claims were spot-verified read-only (§2); no product source
was mutated; the only write is this file.

---

## §0 — Commission (verbatim, `CONSTELLATION-COMMISSION-2026-08-03.md` §2 · lines 88–98)

> ### fourier-analysis
> - **Goal**: full frontend audit on the LATEST glass-ui (temp-link the newest dist); the
>   value.js↔fourier **CRUD API union** designed: palette CRUD + fourier-viz CRUD sharing a
>   provenance model with **git-like chains** — base version + derived variants, diff support with
>   trie-like compression, remixing, version history, user provenance. Visualization spec +
>   containerization examined for fidelity, design language, correctness, and deep performance.
> - **Horizon**: CRUD union prototype proves the provenance chain end-to-end (create → derive →
>   diff → remix → history walk) against both palette and fourier-viz objects; frontend audit
>   saturated.
> - **Admission**: fourier wave specs + the shared-provenance API contract document co-signed by the
>   value.js API row.

---

## §1 — Substrate + cross-lane reconciliation

| Fact | Lane agreement | Verdict |
|---|---|---|
| fourier branch `m/w1-bump-migration` | [FE caveat] = [DOCS §1] | AGREE |
| Working tree: **28 uncommitted paths** | [FE] `git status --porcelain \| wc -l` → 28 = [DOCS §1] 27 M + 1 ?? | AGREE (27 modified = package.json + lock + 24 `web/src` files + RUN-BOARD.md; the 1 untracked = value.js's O-14 letter) |
| fourier HEAD | [DOCS §1] `cd26c65` 2026-07-03 vs [CRUD header] "`14d83356`" | **CONTRADICTION → RESOLVED, see §2 C-1** |
| value.js substrate `tranche-u @ c654824e` | [CRUD header] = live git status | AGREE |
| Pins: value.js `^0.13.0`/inst 0.13.0 · glass-ui `^4.0.0`/inst 4.0.0 · keyframes `^4.3.0`/inst 4.3.0 | [FE §1 dep table] = [DOCS §9] (corroborated by `X/waves/W11.md:319`) | AGREE — producers at 4.0.0 / 7.0.0 / 6.0.0 respectively |
| value.js live import surface: **5 import statements / 4 files / 6 symbols, easing-only** | [FE §5 "5 sites"] = [DOCS §7c full paste] | AGREE (easings.ts ×2, ConvergencePlot.vue, useCurveTransition.ts, harmonics.ts; all bare-root specifiers that 4.0.0 no longer exports — latent, not live, while 0.13.0 remains installed) |
| WT bump diff = pure rename sweep, 24 src files / 46±46 lines | [FE §5 prior-art] consistent with [DOCS §1] 24-component list | AGREE |
| fourier `/api/visualizations` = 13 routes; whole-API as-built = 30 | [CRUD §4] 13 + [DOCS §5] O-14 counts repair (`as-built` 30 across all routers) | CONSISTENT |
| PLAW-BIND forbidden edge (direct parse-that→fourier) | [DOCS §7] 0 manifests / 0 source refs; 4 transitive copies, 3 versions | UNCONTESTED — the edge has never existed; keep the two-grep probe |
| glass-ui adoption depth: 95 named-import statements / 21 subpaths / 49 symbols / 0 reka-ui / 0 shadcn copies | [FE §3] sole source | UNCONTESTED (internal count drift flagged, §2 C-3/C-4) |

---

## §2 — Challenge marks (L-18): what I could falsify, and what survived

- **C-1 · lane-crud substrate hash WRONG — corrected.** Lane-crud's header pins "fourier-analysis @
  `14d83356`". Spot-verified: `git -C fourier-analysis rev-parse --verify 14d83356` → **ABSENT**;
  fourier HEAD is `cd26c65` (matches lane-docs' pasted probe), and `14d83356` is **value.js's**
  tranche-X-specified commit. Transcription error only — every other lane-crud row carries its own
  file:line and none depends on the hash. **Carry all lane-crud findings; substrate line reads
  `cd26c65`.**
- **C-2 · V-β and the fork-visibility divergence CONFIRMED by direct read.**
  `api/src/modules/palette/model.ts:85-86` ("_id is the content-hash"), `hash.ts:8-17` (canonical
  folds only `name`+`colors`, no slug scope) — V-β stands. `service/forks.ts:64` (`colors =
  source.colors` verbatim) and `:76` (`visibility: "public"` hard-coded) — the R-5 divergence rows
  stand as written.
- **C-3 · lane-frontend "51 of 66 SFCs + 4 TS modules" mis-partitioned.** Its own full import-line
  listing enumerates **49 SFCs + 3 TS modules + 1 CSS `@import`** with live glass-ui specifiers
  (52–53 files, vs the claimed 51; grep -rl 54 reconciles as +comment-only files). Off-by-small;
  changes no verdict. Use "≈51 files, 95 import statements" and re-measure at wave-open.
- **C-4 · lane-frontend metric-badge "7 imports / 6 files" is 7 files.** The seven listed sites are
  seven distinct files (EquationView, InfoCard, AnimationControls, EditorControlsDock,
  EquationPanel, GalleryAdminBanner, GalleryDraftsSection). Budget 7 files for the `./metric` cure.
- **C-5 · lane-frontend shadow aggregate "~1 990 LOC" is arithmetic drift.** Its own row sum is
  **2 079 LOC incl. `canvas-drawing/` (1 315 excluding)**. Use the summed figures.
- **C-6 · "fourier defect F-α" corroborated across lanes.** Lane-crud proves the version chain never
  deepens (sole writer `_write_root_version`, always `depth=0`); lane-docs independently shows
  fourier's J tranche ("visualization REMIX / CRUD + atom-diff provenance") is still OPEN, absorbed
  into a stalled M — i.e. the half-built chain is *known* unfinished work on their side, not an
  overlooked bug. Strengthens the finding and re-homes it as formation work, not blame.
- **C-7 · "latent, not live" cross-check holds.** Lane-frontend's value.js peer-floor rows and
  lane-docs' §9 consequence chain agree: the five `ERR_PACKAGE_PATH_NOT_EXPORTED` sites fire only
  once 4.0.0 installs — the bare-root breakage and the tri-package bump are the SAME event.
- Everything else folded below survived cross-examination; counts that should agree, agree (§1).

---

## §3 — The folded picture

### 3a · Frontend (lane-frontend, whole)

- **Shape**: Vue 3.5 SPA · Vite · Pinia · Tailwind v4 · 66 SFC + 65 TS ≈ 20.6k LOC; 8 routes, all
  lazy; contract-v2-clean resolution (no dist aliases) [FE §1]. Canvas2D throughout, **WebGL/WebGPU
  ABSENT**; three independent canvases (epicycle instrument reactive-redraw off a store rAF clock;
  ConvergencePlot with its own ungated rAF; FrequencyGraph watch-driven) + 12 SVG surfaces [FE §6].
- **Glass posture**: deepest, cleanest consumer in the constellation — 95 named-import statements /
  21 subpaths / 49 symbols; 0 direct reka-ui; 0 shadcn copies; 3 local `components/ui/` files are
  documented thin adapters, keep [FE §3]. Two upstream carries held locally: the `@utility
  cartoon-card` resurrection shim (25 sites) and the `--viz-amber` WCAG darken [FE §3, §8] — both
  glass-BH-inbox relays per standing law.
- **Shadows** [FE §4]: HARD — local `GlassTimeline.vue` (127, name-identical, producer subpath
  never imported) and `EasingPicker.vue`+`EasingCurvePreview.vue`+`lib/easings.ts` (the producer
  README's forbidden "fourth fork"; `./easing` ships at 7.0.0). CHARACTERFUL — `DarkModeToggle`
  (sun↔moon Fourier morph; keep, reconcile). SOFT — `BasisCanvas` + `canvas-drawing/` (1 311 LOC
  Canvas2D) vs glass-ui's GPU-backed `FourierField` sharing the `BasisComponent` type; not drop-in
  (interactive/multi-basis vs decorative) but duplicated coefficient substrate — highest-value,
  highest-risk convergence. CANDIDATES — PaperSearch family (832 LOC) vs `./search`+`useFuzzySearch`,
  carousel, NotationPills→`./chip`, InfoCard→`./card`, metric hosts; GalleryMarquee stays local
  (producer retired `./scrolling-text` at 5.0.0). Aggregate 2 079 LOC incl. canvas lib (§2 C-5).
- **The uplift break surface** [FE §5]: removed subpaths in live use (`metric-badge` ×7 files,
  `hover-card` ×2, `hover-popover` ×2), removed dock members (`DockIconButton` ×2,
  `DockDropdownTrigger` ×1), **`ToastVariant` definition-absent → hard typecheck break**
  (`useToast.ts:3,9`), `lucide-vue-next → @lucide/vue` ×35 sites, pencil-boil 0.4.1→^0.11.2.
  Prior-art rate: the 3.1→4.0 hop cost 46 lines; 4→7 is an order of magnitude above.
- **THE RESOLUTION DEADLOCK** [FE §5 🔴]: `keyframes@4.3.0` optional-deps `glass-ui ~4.0.0` (tilde
  hard-lock) while `glass-ui@7` peers `keyframes ^6.0.0` and `keyframes@6.0.0` deps
  `value.js 4.0.0` exactly ⇒ **glass 4→7 ∧ keyframes 4.3→6 ∧ value 0.13→4.0 is ONE atomic
  transaction; none lands alone.**
- **Hygiene banked / gaps**: off-screen rAF gating, `scheduler.yield()` INP floor, 18
  reduced-motion references — but the two rAF clocks themselves are **ungated under
  `prefers-reduced-motion: reduce`** (glass-ui 7 `DockBackgroundToggle` is the canonical seat)
  [FE §8]. **vitest ABSENT** — the only frontend gates are `vue-tsc` + 29 Playwright tests on a
  single chromium project [FE §0, §9]. Containerized multi-stage + hardened prod overlay; image
  build requires repo-root context (TeX compile coupling) [FE §7].

### 3b · The CRUD/provenance seam (lane-crud, whole)

- **Headline** [CRUD §0]: the atom-diff pattern was authored once (fourier, Python, shipping) and
  the value.js twin was **excised at T.W1 (TA-4)** — `lib/crud/atomdiff.ts` survives only as a
  compiled corpse in an untracked dist dir. The union's diff layer is one-sided. The canonical
  contract doc `J-diff-shape.md` (271 lines, repo-neutral on paper) lives only in fourier's tree.
- **Seam verdicts** [CRUD §2]: R-1 variants EXISTS both (remix requires a delta / fork is verbatim
  copy — confirmed §2 C-2); R-2 chains PARTIAL-fourier (F-α root-only) / EXISTS-value with V-α
  (revert writes no row, `versionCount` drifts unconditionally) + V-β (global content-hash `_id`
  collides across palettes — confirmed; fourier already solved this with compound
  `{viz_slug}:{set_hash}`); R-3 diff EXISTS-fourier / ABSENT-value (hashing half survives:
  `computeAtomHash`/`computeAtomSetHash`); **R-4 trie-like compression ABSENT BOTH SIDES** — pure
  greenfield, and both trees carry an explicit anti-tree KISS guardrail (`atomdiff.py:12-14`);
  R-5 remix EXISTS-fourier (per-atom overrides, tri-state palette_slug, no-op 422, idempotency,
  deliberately non-transactional) / PARTIAL-value (fork only, transactional, **born `public`** vs
  fourier's born `draft`); R-6 history PARTIAL-fourier (no revert, no by-hash get, unredacted
  breadcrumb F-γ leaking private ancestors) / EXISTS-value (revert + by-hash + the redacting
  `ProvenanceStep` union); R-7 user provenance EXISTS both (value ahead on hashed-token-at-rest;
  behind on nullable `userSlug` ⇒ silently unversioned edits, V-γ).
- **Additional fourier defect** F-β: PATCH `$set`s atom 5 without recomputing `set_hash` or writing
  a version — advertised hash goes stale; `palette_slug` can never be cleared to null [CRUD §2 R-2].
- **The one existing cross-repo link** [CRUD §5]: `Visualization.palette_slug` — soft, one-way,
  FK-unvalidated by design (F-02/inv-16); the reciprocal is ABSENT (zero fields/routes in value.js).
- **Friction vocabulary** [CRUD §6]: visibility enums differ (`draft|unlisted|public` vs
  `public|unlisted|private`), remix-vs-fork verb, cursor-vs-offset pagination, compound-vs-global
  version `_id`, snake/camel (the only permitted envelope difference per `J-diff-shape.md §4`).
- **Raw material** [CRUD §7]: fourier is reference for `AtomOp`/`diff_atoms`/set-hash/compound
  `_id`/two-walk provenance/immutable-ETag diff; value is reference for revert, by-hash get,
  redaction, transactions, branded types, hashed sessions.

### 3c · Docs / coordination / deploy state (lane-docs, whole)

- **fourier is FROZEN mid-wave**: HEAD `cd26c65` 2026-07-03; M.W1 `executed-partial` uncommitted;
  three tranches open at once (J · K-deploy · M) designed to close together at M's close
  [DOCS §0-2]. **M.W1b's gate (glass `^4.1.0`) is dead-by-supersession** (glass shipped 7.0.0) —
  M.W5–W9, then W10→W13, all blocked behind a cut that will never arrive in that form; unblocking
  is a re-grounding of the gate, not a bump [DOCS §2, §9].
- **No CLAUDE.md anywhere; no live INBOX ledger**; law = per-tranche `INVARIANTS.md` + a private
  precepts submodule pinned 2026-06-04 (invisible to CI by design) [DOCS §3]. inv-32
  (version-currency) is breached against every sibling; inv-33 ("all runtime colour derives from
  value.js") is the design-surface hook for W.L5.
- **The correspondence is one-way in fact and law**: four unanswered value→fourier letters (FN-1..7
  charter, the 2.0.0 note, O-14/facility-19 — the O-14 letter sits UNTRACKED in their tree — plus
  ADOPTION-ASKS rows pointed back at value.js: Ask 3, Ask 5, inv-22-color, cascade-vjs). Zero
  inbound rows from fourier in the V INBOX [DOCS §4-5, §8].
- **Routing/boundary law**: W.L5's value-side half ships in X-W9.f under D-15's grant; **the
  fourier-tree edits and π evidence are explicitly NOT X-W9 gates** — fourier's waves are authored
  in fourier's own sub-session (COMMISSION §2). G24 (analytic-arm drift, banked max|Δ| 0.192) and
  G33 (post-window `npm ls`) are the X-side gates carrying fourier evidence [DOCS §5, §8d].
- **Codex-era R3–R6 fourier coordinates are TERMINAL / SOURCE_RED, zero credit** — 11 files on the
  value side, none in fourier's tree; reopen requires a fresh explicit owner ruling; they do not
  block this formation [DOCS §4c].
- **Deploy**: two automated paths (CF Pages SPA gated on same-SHA green CI; self-hosted Docker API
  at the G.W7 hardening floor) — but the host is **44 commits behind with months of silent
  rollback**; the repair (M.W2–W4) is authored and unstarted [DOCS §6].
- **PLAW-BIND clean**: the forbidden direct parse-that→fourier edge has never existed; fourier's
  parsing today is a 117-line hand-rolled regex file (`web/src/lib/colors.ts`, no `oklch()` arm) —
  the exact deletion target of W.L5 item 2 and the I-9 re-trigger sentinel [DOCS §7].

---

## §4 — Candidate wave-shape sketch (names + one-line scope; NOT full specs)

1. **F.W0 · re-ground** — land-or-abandon the frozen 28-path M.W1a working tree; dispose of the
   J/K-deploy/M tri-tranche interlock and the dead M.W1b glass-4.1.0 gate; commit the untracked
   O-14 letter; establish the fourier-side mail ledger (E13 analogue) + a CLAUDE.md.
2. **F.W1 · the atomic tri-package uplift** — glass 4→7 ∧ keyframes 4.3→6 ∧ value 0.13→4.0 in one
   transaction, + `@lucide/vue` rename (35 sites) + pencil-boil ^0.11.2; cure the break surface
   (metric-badge ×7 files, hover-card/-popover ×4, dock members ×3, `ToastVariant`).
3. **F.W2 · value.js consumption to spec** — execute W.L5's fourier half under D-15's grant: 5 bare
   specifiers → `/easing`, delete the `colors.ts` hand-rolled arms (declared 3-line hex residual),
   discharge facility-19 I-1..I-10, answer FN-1..FN-7 + the 2.0.0 note.
4. **F.W3 · shadow retirement + glass suffusion** — GlassTimeline → `glass-ui/timeline`;
   EasingPicker/EasingCurvePreview → `glass-ui/easing` (no fourth fork); disposition the candidate
   shadows; relay the cartoon-card + `--viz-amber` carries to the glass BH inbox.
5. **F.W4 · frontend audit saturated** — per-component D/L/C audit on the uplifted tree against
   LATEST glass; the `BasisCanvas`↔`FourierField` convergence study; reduced-motion clock gating;
   dead-dep + vestigial-shadcn scrub; a unit-test floor (vitest) decision.
6. **F.W5 · the shared-provenance API contract** (ADMISSION KEYSTONE) — re-author `J-diff-shape` v2
   as the neutral, co-signed contract: chains, compound version `_id`, diff envelope, remix/fork
   verb + born-visibility reconciliation, redaction parity, trie-compression disposition, casing;
   co-signed by the value.js API row.
7. **F.W6 · provenance defect burn-down** — fourier F-α (deepen the chain), F-β (PATCH recompute +
   clearable palette_slug), F-γ (redact the breadcrumb); value-side V-α/V-β/V-γ routed to the
   value.js API row per the contract (value-tree edits are NOT fourier-wave gates).
8. **F.W7 · trie-like compression design** — the R-4 greenfield: owner ruling against the standing
   anti-tree KISS guardrail first, then a structural-sharing/delta design proven on both object
   kinds.
9. **F.W8 · CRUD union prototype end-to-end** — the horizon probe: create → derive → diff → remix →
   history walk against BOTH palette and fourier-viz objects, conformance fixtures both directions
   (FN-6 reader wired).
10. **F.W9 · deploy spine + evidence** — re-home M.W2–W4 (kill the 44-behind silent-rollback
    chronic), containerization fidelity audit, green inv-27 run ids, CWV/axe evidence.
11. **F.W10 · coordination discharge + close** — ADOPTION-ASKS reconciliation both directions,
    G24/G33 evidence relayed, terminal disposition of every deferred row (no re-booking), close.

---

## §5 — Top 10 defects/risks by severity, with provenance

1. **[P0] The tri-package resolution deadlock.** `keyframes@4.3.0` optional-deps `glass-ui ~4.0.0`;
   `glass-ui@7` peers `keyframes ^6` + `value.js ^4`; `keyframes@6` deps `value.js 4.0.0` exact —
   glass/keyframes/value must bump as ONE transaction, plus the lucide rename ×35 and pencil-boil.
   Nothing in the commission's frontend goal can start until it lands. [FE §5]
2. **[P0] The frozen mid-wave branch.** 28 uncommitted paths = the real, partially-executed M.W1a
   migration; HEAD one month stale; the 4→7 work cannot sequence atop an unlanded 3→4, and losing
   the tree loses M.W1a. Land-or-abandon is the formation's first act. [FE caveat · DOCS §0-1]
3. **[P0] The union's diff layer is one-sided.** value.js's `atomdiff.ts` + the `atomDiff` field
   were excised at T.W1/TA-4; only the hashing half survives. The co-signed contract cannot bind —
   `J-diff-shape.md §6`'s close-gate probes cannot even run on the value side — until the twin is
   restored. [CRUD §0, §2 R-3]
4. **[P1] Trie-like compression is greenfield AGAINST a documented guardrail.** R-4 has zero
   material on either side and both trees carry the explicit anti-tree KISS constraint
   (`atomdiff.py:12-14`); the commission requirement and the standing constraint collide — an
   owner ruling must precede design. [CRUD §2 R-4, §7]
5. **[P1] Chain-integrity defects on both sides.** fourier F-α (every version `depth=0`; the chain
   never deepens) + F-β (PATCH atom mutation leaves `set_hash` stale, `palette_slug` unclearable);
   value V-α (revert writes no row yet `$inc`s `versionCount` — permanent drift, no attribution) +
   V-β (global content-hash `_id` collides across palettes; confirmed §2 C-2) + V-γ (nullable
   `userSlug` ⇒ silently unversioned edits). The union inherits all five unless the contract
   settles them. [CRUD §2 R-2/R-7]
6. **[P1] Privacy divergence at the seam.** fourier's provenance breadcrumb emits slug/hash/author/
   date for private and draft ancestors (F-γ, no redaction) while value.js redacts to
   `{kind:"unavailable", ordinal}`; and the same verb's child is born `draft` on fourier vs
   hard-coded `public` on value (confirmed §2 C-2). Opposite defaults on privacy-adjacent surfaces
   are contract-blocking. [CRUD §2 R-5/R-6]
7. **[P1] The dead M.W1b gate holds fourier's entire design surface.** Glass `^4.1.0` never cut
   (producer at 7.0.0); M.W5–W9 → W10 → W11 → W13 all gated on it; nobody fourier-side has
   recorded the supersession. Unblocking is a re-grounding, not a bump. [DOCS §2, §9]
8. **[P2] The deploy chronic.** Host 44 commits behind `f2fe447`; every push builds, fails the
   health gate, silently rolls back; G/H/I/J never deployed. The repair (M.W2–W4) is authored,
   `planned`, unstarted. Any "prototype proves end-to-end" horizon claim is hollow if the API
   deploy path stays broken. [DOCS §6]
9. **[P2] The coordination dead-drop.** Four unanswered value→fourier letters (FN-1..7, 2.0.0
   note, O-14 untracked in their tree, ADOPTION-ASKS rows); zero inbound from fourier; no
   CLAUDE.md, no INBOX, precepts submodule 2 months stale and CI-invisible. The admission requires
   a CO-SIGNED contract — today there is no fourier-side surface that could receive, ledger, or
   sign it. [DOCS §0, §3-5, §8]
10. **[P2] Uplift lands with no unit-test net.** `ToastVariant` is a hard typecheck break, 11
    removed-subpath sites + 3 dock members must be cured — and vitest is ABSENT; the only gates
    are `vue-tsc` and 29 single-chromium Playwright tests. The break surface is an order of
    magnitude above the 46-line 3.1→4.0 prior-art. [FE §0, §5, §9]

---

## §6 — THE ADMISSION GAP

Admission = **fourier wave specs + the shared-provenance API contract document co-signed by the
value.js API row.** Between this census and that bar stand, in dependency order:

1. **No shared-provenance contract exists.** `J-diff-shape.md` is J-era, lives only in fourier's
   tree, predates the T.W1 excision, and covers only the diff envelope — not chains, trie
   compression, remix/fork unification, redaction, or born-visibility. It must be re-authored as
   v2 in a neutral home (FN-7's own unanswered ask) before anyone can sign it. [CRUD §0, §3 · DOCS §8b]
2. **Pre-contract design rulings owed**, each census-evidenced: trie-compression vs the anti-tree
   KISS guardrail (risk 4); remix-vs-fork verb + delta-required semantics; born-visibility default
   (draft vs public); visibility-enum mapping (`draft` vs `private`); version-`_id` scheme (adopt
   fourier's compound form); redaction parity (adopt value's `ProvenanceStep` union); pagination
   idiom. Without these the "contract" would codify contradictions. [CRUD §2, §6]
3. **value.js-side restoration prerequisite**: `lib/crud/atomdiff.ts` + `atomDiff` on
   `PaletteVersion` (excised TA-4) must be restored — or the contract explicitly re-scoped — since
   a co-signature over probes one side cannot run is void. This work belongs to the value.js API
   row, outside fourier's waves. [CRUD §0, §7]
4. **No fourier wave specs exist, and the ground isn't spec-able yet**: three interlocked open
   tranches (J/K-deploy/M) with a stale 14-wave board and a dead W1b gate must be dispositioned
   (absorb/supersede/close), and the 28-path frozen tree landed or abandoned, before fresh specs
   can pin a substrate. [DOCS §1-2 · FE caveat]
5. **The co-signing counterparty doesn't exist yet**: the "value.js API row" is a seat this
   formation must instantiate (and the boundary law respected — fourier-tree edits are authored in
   fourier's sub-session under COMMISSION §2; value-side halves ship via X-W9.f under D-15's
   grant; neither side's edits gate the other's waves). [DOCS §5, §8d]
6. **A fourier-side coordination surface must exist to receive and ledger the co-signed
   contract** — INBOX ledger + CLAUDE.md + the four unanswered letters answered — else the
   signature has nowhere durable to live. [DOCS §3-4]
7. **Codex R3–R6 residue formally not inherited**: an explicit formation-charter line that the
   TERMINAL/SOURCE_RED coordinates carry zero credit and reopen only by fresh owner ruling, so the
   wave specs are provably fresh-rooted. [DOCS §4c]

---

*Agglomeration only. Lanes read whole; two contested claims spot-verified read-only (fourier
`git log`/`rev-parse`; value.js `hash.ts`/`model.ts`/`forks.ts`). No file outside this one written.*

---

## ADDENDUM 2026-08-03 — Codex intake corpus folded (R3–R6); §6.7 corrected

claude-fable-5 (served model id — M-23 intake-adjudication master seat). Source:
`audit/codex-provenance/intakes/lane-fourier-r3-r6.md` (52 claims), folded per M-21 into
`audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md`. Tally: **34 adopted · 11 carried ·
1 refuted · 6 owner-gated**. The census body above is dated and unrewritten; this section amends it.

### THE §6.7 CORRECTION (explicit)

**§6 item 7's "Codex R3–R6 residue formally not inherited … carry zero credit" is SUPERSEDED by:**

> **Zero UNVERIFIED credit** — every Codex verdict enters the formation through per-claim
> adjudication (M-21); the adjudicated rows are in the master ledger
> (`audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md`).

"Zero credit" conflated *authority* with *measurement*. The authority is dead — the R-coordinates'
governance statuses are OWNER-GATED (OG-F1: re-root or freeze; lane evidence favors
freeze-with-adoption, and R6 itself declared `NO_SUCCESSOR`, making the ruling cheap). The
measurements are live, reproducible, and were re-reproduced against the live tree — they enter the
substrate by adjudication, row by row. §3c's "TERMINAL / SOURCE_RED, zero credit" line reads the
same way. The charter line the wave specs inherit: *no authority, no admission, no slot — and the
adjudicated structural census + model defects enter the substrate as measured fact.*

### Adopted facts that amend the census

1. **The substrate has not moved (R4-9, re-verified at fold).** fourier HEAD `cd26c653` / tree
   `9a66411d` today; the Codex worktree's `web/src` is byte-identical to live; the audited scope is
   the exact tree F.W0 opens on. Nothing in R3–R6 is stale-at-HEAD.
2. **The R4–R6 structural census enters the substrate** (thrice-stable across independent rounds,
   live-corroborated on all three checkable axes — 45 ops ✓ / 66 workflows ✓ / 9 routes ✓): 318
   repository members · 131 modules · 571 module edges · 512 physical callsites · 1,105 mounted
   subjects · 16 template loops (+16 native, R6) · 41 paths · 20 client functions · 2 clients
   without an operation · 33 visible-state rows · 900 explicit OPEN gaps · 36 client edges / 9 gap
   operations · 2 Teleports (`PaperSearchModal.vue:41`, `FullscreenViewer.vue:105` — fills a census
   gap) · 6 dynamic `:is` families · 35 Tooltip callsites over 9 consumers · mounted-instance
   denominator OPEN. R3's diverging products (545/519/1,376) are superseded by its own registry-join
   defects.
3. **§3a route-count CORRECTED: "8 routes, all lazy" is wrong.** Live `router/index.ts` carries
   **9 route records = 7 lazy component + 2 redirects, plus 1 alias** — the Codex figure, and its
   control model distinguishes all three kinds. Adopt the 9-record model (→ F.W4).
4. **§1 API-count row RECONCILED, both right:** 45 total decorators = **30 public-non-admin**
   (the census figure) **+ 13 admin + 1 app + 1 gallery**. Cite the triple 45/30/13. The
   **OpenAPI security defect is 0 of 45** — the admin arm is inside it (→ F.W5).
5. **Four method/model facts adopted** as formation constraints: (i) a registry audit that re-hashes
   bytes but never re-derives products proves the bytes unchanged, not the summary true — the
   F.W4/F.W5 evidence standard; (ii) identity ordering is `UTF8_BYTEWISE_CODEPOINT`, never
   `localeCompare`; (iii) **native-template-loop blindness** — loop evidence keyed to component
   callsites drops the entire `PaperSidebar` TOC subtree (3 nested `<li v-for>`, live 65/87/105);
   (iv) **operation↔client non-isolation** — an operation record embedding derived client
   back-references cannot attribute a defect to one side of the seam.
6. **Refuted: exactly one row** (R4-4, a filename transcription whose hash was correct). No
   substantive fourier claim died — the R-series is the Codex corpus's honest core.

### Carried rows (by owning wave)

| wave | rows |
|---|---|
| **F.W0** | R3-16/R4-11/R5-9/R6-10 (the 14-wave M board is the real disposition object — already §6 item 4; the 72/158/P29/slot superstructure does not transfer) · R4-12 (the `/tmp/fourier-r4-files.sha256` residue, 74,507 B, still on disk at fold — disposition, consume nothing) · R3-9 (record as unverifiable provenance assertion) · R5-3 (re-hash or drop) · plus: the still-registered Codex worktree `~/.codex/worktrees/d0be/fourier-analysis` should be dispositioned |
| **F.W3** | R3-7a — the `ui/tooltip` migration budget: 35 callsites / 9 consumers |
| **F.W4** | R3-10 (all 6 dynamic-`:is` sites budgeted) · R5-7 (count native element loops or inherit the blind spot) · X-2 (9-record route model) · X-9 (publish ONE member-scope law before any percentage) |
| **F.W5** | R3-7b (security descriptions 0/45 — contract input) · R3-7c (36/9 client-gap, triangulating census risk 9 + fourier's own M.W10 row) · R6-8 (operation identity independent of client identity; the join is a separate relation) · X-3 (the 45/30/13 triple) |
| **F.W6** | X-8 — F-α now corroborated three ways; strengthens §5 risk 5's routing, no new work |

### Wave-sketch adjustments forced

- **F.W0** gains the residue/worktree dispositions and the pin-hygiene rule (unverified Codex pins
  are re-hashed at use or dropped).
- **F.W4** gains three audit-model constraints (native loops, scope law, route model) — its
  per-component D/L/C audit inherits Codex's cured deriver model, not its broken R3 one.
- **F.W5** (ADMISSION KEYSTONE) gains two contract constraints (security-description coverage over
  the full 45; operation↔client separation) that the co-signed contract must satisfy or the
  F.W8 conformance fixtures will reproduce Codex's exact two-sided C31 failure.
- **§6 item 7 replaced** (this addendum's correction). **Two owner rulings join the admission
  gap's owed-rulings set:** OG-F1 (re-root vs freeze-with-adoption of the R-coordinates) and OG-F2
  (does the pre-write root-absence-receipt requirement bind successors, or was it Codex-era law?).
