SERVED MODEL: claude-opus-5[1m]

# CENSUS ADDENDUM — 2026-08-25 (fourier formation census)

⟨**DATED ADDENDUM-BESIDE (E-3).** The dated `CENSUS-2026-08-03.md` is **not rewritten** and is not
touched by this file: ⟨cmd⟩ `git diff --stat docs/tranches/V/megatranche/formation/fourier/CENSUS-2026-08-03.md`
→ **empty** at this seat, double-run. This file is the census-side surface the X·F sub-tranche's two
terminal waves were chartered to create: **CREATE BESIDE, co-owned** — `F-W10.md` §1a row 8 holds the
**CREATE** and owns the **§4.11 re-cut record**, the by-mechanism cut deviations, the **FR-CP-44**
metric-stack re-seat correction, the **FR-AFP-51** tense correction and the **§6.7 charter line in its
corrected wording**; `F-W9.md` §1a row 2 holds **MODIFY** and owns the **§4.10** deploy/coverage cells.
Filed under the owner's begin-word of 2026-09-17 (COHESION §0j) by **X.F.W10.a**, 2026-09-20.

**The registry is READ-ONLY and anti-rename holds by construction**: the re-cut below is a *record of
cuts already carried in two wave specs*, never an edit to any `registry/adjudicated/fr-*.md`, and no
banked id is re-booked, re-graded or renamed here. **The mechanism prose for every row lives in the two
specs' §2.2 SHARED SPLICE BYTES and is CITED, never copied** — a third rival copy of those bytes would
be exactly the unilateral divergence `G-F10-6 ≡ G-F9-21`'s Δ=∅ leg exists to catch.⟩

## Contents

1. **§4.10** — the deploy spine and the coverage void (X·F F.W9) — **RECEIVED BY SPLICE**, not authored here
2. **§4.11** — the F.W9/F.W10 re-cut record: 26 identities, one wave each
3. **§4.11a** — the by-mechanism cut deviations, and the one retired fabrication
4. **§4.11b** — the pending paired edits, re-disclosed and **not applied**
5. **§6.5-ERRATA** — FR-CP-44 (metric-stack re-seat) · FR-AFP-51 (tense)
6. **§6.7** — the charter line, in its corrected wording
7. **Scope** — what this addendum does not carry, named so nothing is silently dropped
8. **Receipts** — every figure above, read from the settled bytes and double-run

---

## §4.10 — the deploy spine and the coverage void (X·F F.W9)

⟨**RECEIVED BY SPLICE (E-F9-2), NEVER RE-AUTHORED.** These bytes were authored whole by **X.F.W9.c**
at `docs/tranches/X/execution/C/F-W9.md` §c.7, where that unit could lawfully write, and escalated as
**E-F9-2** because the CREATE of this file is F.W10's by both twins' own bytes. This seat performs the
receive and nothing else: it re-typed no word, re-graded no row and corrected nothing inside the region
— a drift here would be F.W10 authoring F.W9's carve, which §4a.1 forbids in the same breath that it
forbids the converse. The region is extracted by pattern, not by line address, and the de-quoting of the
record's blockquote marker is the ONLY transformation, proven reversible at the Receipts below.⟩

**§4.10 — THE DEPLOY SPINE AND THE COVERAGE VOID, EXECUTED (X·F F.W9, 2026-09-19).**
Authored by X.F.W9.c for `CENSUS-ADDENDUM-2026-08-25.md`; **addendum-BESIDE under E-3 — the dated
`CENSUS-2026-08-03.md` is not rewritten, and §6.7/§4.11 remain F.W10's.**

**(a) The deploy spine (census rows M.W2 · M.W3 · M.W4 ⊕ the containerization audit), re-homed
out of the dead M board and EXECUTED at fourier `1b4eb0b` · `e5c435d` · `b72170b` · `ceb7ee6` ·
`4f213e4` · `c5b7600` · `e111220`.** Readiness is separated from liveness; the API arm is
fail-closed on a same-SHA green CI run (inv-28 — the guard exercised against the live repo, green
SHA ADMITTED at run `26789704503`, `origin/master` REFUSED on its `failure` run `26913592291`);
the silent rollback is killed and the inv-31 observability floor stands. **The M-board rows are
disposed explicitly in `execution/C/F-W9.md` a.7 — no double-booking** — and
`docs/tranches/M/PROGRESS.md` stays **ASK-ONLY**, its update riding a relay. **Not executed and
named**: the host is behind and cannot be brought to HEAD from any seat of this wave — the shared
LE certificate expired 2026-08-26 (**E-F9a-1**, an OPERATOR ACT, constellation-wide); `docker
inspect` on the deployed stack is unreachable (**E-F9a-4**); the two nginx authorities are
**disposed, not collapsed** (**E-F9a-5**).

**(b) The coverage void (the P-12 packet the census gives to no wave), AUTHORED ONCE EACH AND
CITED MANY** — fourier `8fd35a9` · `019fb90` · `3913738` · `5256126` · `d3fab17` · `2b42f28` ·
`5b74d3f` · `4a5ffdb` · `3440510` · `160f7f3` · `214cb5e` · `ca64bef`. The unit FLOOR closed the
glob that left three `src/**` vitest files in no runner at all (**8 files / 57 tests → 13 / 84**,
confirmed in CI at run `35445782153`); the lint floor was made **able to fail**; the matrix gained
its coarse cell; five e2e/axe seats and a comparing visual checkpoint were authored (**12 → 18
specs · 0 → 8 `toHaveScreenshot` · ∅ → one snapshots directory**). **K11's *"quote 8, never 9"* is
a DATED figure superseded at these bytes** — the measurement is **18**, and G-12's published **8**
is left to its own wave to re-cut.

**(c) The evidence floor (census row M.W11), STOOD UP AND HONEST ABOUT WHAT IT FOUND** — fourier
`a6f50c8`. inv-27 now emits a citable run id bound to a covering SHA, and mints the record **only**
for a run whose every gating job concluded success — **measured live**: run `35445782153` concluded
`failure` and the evidence job **skipped**. **No covering green run id exists for any F-side green
at these bytes**, and the blocker is `web-build`'s `vue-tsc` error in `web/src/**`
(`ContourEditorCanvas.vue(42,9) TS6133`), which F.W9 does not own. A CWV baseline is published
**stated-invalid on its face** — LCP **5,972 / 5,964 ms** under 4× CPU + Slow-4G, of which the
eager entry chunk's transfer is **4,655 / 4,664 ms**, that chunk measuring **487,221 B / 87.9 %
long-float literals** because FR-AH-1's static `sun.json` import survives at
`DarkModeToggle.vue:89-90`. **The parsing substrate is CURED**: glass-ui 8.0.0's dist parses
(40 nodes / 1 valid `@source` / 0 garbage at-rules) where 4.0.0's did not.

**(d) Verbs.** **AUDITED YES · SPECIFIED YES · IMPLEMENTED YES (2026-09-19) · VERIFIED NO** —
the VERIFIED stamp is reserved to F.W10's close by `F-W9.md`'s own Close act. **Terminal
dispositions are F.W10's**, including FR-AUL-16's and FR-AH-45's homes, the eight checkpoint
items, and the eight paired obligations **P-1 · P-2 · P-4..P-9**, whose paired-edit authorization
is escalated as **E-F9-1**.

---

## §4.11 — the F.W9/F.W10 re-cut record: 26 identities, one wave each

**Why a re-cut existed at all.** Census §4.10/§4.11 make F.W9 the deploy-spine/evidence wave and
F.W10 the coordination/close wave — **two disjoint shapes** — while **54** occurrences of the joint
routing token `F.W9/W10` stood across **25** of the **66** adjudicated records, and the bare `F.W10`
routing stood at **0**. No adjudicated row was therefore *provably* homed at F.W10 by the digraph
alone, and neither twin could stamp until every joint routing was cut to exactly one wave. The
anatomy that keeps the cut from manufacturing rows out of boilerplate (RE-CUT-0's standing dissent)
— **quoted as `F-W10.md` §2.1's BANKED classification, not re-derived here, because this seat's own
shape probes do not reproduce its `14` and the honest reading is published at Receipts rather than
smoothed**: of the 54, **26** are routing identities — a defect row's own disposition, cut below —
**14** are the
routing-law header line listing every wave id (**zero rows minted**; re-worded as taxonomy, §4.11c),
**2** are same-identity restatements folding to their row, and **12** are verdict/adjudication prose,
which is **dated and immutable** and is annotated by addendum only.

**Seven records yield ZERO re-cut rows** — VisualizationView · CoefficientsPanel · FrequencyGraph ·
HarmonicLevelGrid · InfoCard · CanvasOverlayButton · fr-AdminUserList. CanvasOverlayButton and
InfoCard still enter the formation **by MECHANISM** (FR-COB-20 · F8-REACH-01/02), never by the
digraph, and F8-REACH-01/02 were ruled **DELETE, in one breath** at COHESION §0j.D (G-10) and are
executed at the tree.

**THE RECORD.** Identity · record-anchor as `F-W10.md` §2.2 spells it · the one wave. **The mechanism,
the locks and the preserved dissent for every row live in the two specs' §2.2 shared splice bytes and
are cited here, never copied.**

| # | record (as §2.2 spells it) | identity (kept for life) | cut to | status |
|---|---|---|---|---|
| 1 | `fr-AdminAuditLog:87` | **AA-44** | **F.W9** | booked |
| 2 | `fr-AdminFlaggedPanel:95 / :110` | **FR-AFP-42** (test seat) | **F.W9** | booked |
| 3 | `fr-AdminFlaggedPanel:95 / :110` | **FR-AFP-49** | **F.W9** | booked |
| 4 | `fr-App:91` | **C-13 (App)** | **F.W9** | booked |
| 5 | `fr-ContourPreview:71` | **38** | **F.W9** | booked |
| 6 | `fr-EquationModeToggle:41` | **FR-EMT-11** (= C-16) | **F.W9** | booked |
| 7 | `fr-EquationResult:41 / :72` | **FR-EQR-6** (axe leg) | **F.W9** | booked |
| 8 | `fr-EquationResult:41 / :72` | **FR-EQR-31** | **F.W9** | booked |
| 9 | `fr-EquationView:62 / :191` | **D·D-B2** (axe leg) | **F.W9** | booked |
| 10 | `fr-EquationView:62 / :191` | **C·D-28** | **F.W9** | booked |
| 11 | `fr-FunctionInput:80` | **C-13 = D-24** | **F.W9** | booked |
| 12 | `fr-GalleryAdminBanner:49` | **GAB-10** (coverage half) | **F.W9** | booked |
| 13 | `fr-GalleryCardModal:93` | **GCM-42** (= L-14) | **F.W9** | booked |
| 14 | `fr-GallerySearchBar:54` | **FR-GSB-17** | **F.W9** | booked |
| 15 | `fr-GalleryView:78` | **FR-GV-36** (= L-24) | **F.W9** | booked |
| 16 | `fr-ImageUpload:61` | **27** | **F.W9** | booked |
| 17 | `fr-PaperArticleWindow:58 / :90` | **PAW-12** (rider limb) | **F.W9** | booked · **deviates from canonical F.W3** (§4.11b) |
| 18 | `fr-PaperArticleWindow:58 / :90` | **PAW-34** (= C-i-5) | **F.W9** | booked |
| 19 | `fr-PaperArticleWindow:199` | **PAW-49** | **F.W0** | booked ⟨**re-cut by mechanism** — §4.11a⟩ |
| 20 | `fr-PaperArticleWindow:200` | **PAW-50** | **F.W4** | booked ⟨**re-cut by mechanism** — §4.11a⟩ |
| 21 | `fr-PaperSidebar:49` | **D-B2** (axe leg) | **F.W9** | booked |
| 22 | `fr-PaperSidebar:58` | **C-M4** | **F.W9** | booked |
| 23 | `fr-PaperSidebar:67` | **M7** | **F.W9** | booked (its tsconfig half **≡ MG-θ at F.W0** — one substrate edit, two records, **booked once**) |
| 24 | `fr-PaperView:120` | **D/i-1** | **F.W9** | booked |
| 25 | `fr-Tooltip:26` | **FR-TT-1** (promotion note) | **F.W4** | **CITE-THE-HOLDER — booked by NEITHER twin** ⟨§4.11a⟩ |
| 26 | `fr-UserSlugBar:53` | **FR-USB-16** (≡ r1-MISSED-5 ≡ r2-MISSED-1) | **F.W9** | booked |

**Tally, and it closes**: **F.W9 23** ⊕ **F.W0 1** (PAW-49) ⊕ **F.W4 1 booked** (PAW-50) ⊕ **F.W4 1
cited-not-booked** (FR-TT-1) = **26 identities, one wave each**. Every row's execution seat is
**F.W9-widened (or a minted F.W9a)** per the P-12 packet ruling except the three named at §4.11a;
F.W10's share of every F.W9-cut row is the **terminal-disposition record only**.

### §4.11a — the by-mechanism cut deviations, and the one retired fabrication

**TWO deviations from the banked digraph, and both are BY MECHANISM** — never by the digraph, never
by convenience, and each recorded here because a cut that departs from the census must say so where
the census can be read:

| id | banked digraph | cut to | the mechanism that moved it |
|---|---|---|---|
| **PAW-49** | `F.W3/W4` · `F.W9/W10` (canonical primary **F.W3**) | **F.W0** | the cure is a **repo-config line** — `noUncheckedIndexedAccess`, identical in kind to MG-θ/M7's tsconfig flags. Without it `resolveFigure`'s return asserts `width: number` while one whole arm yields `undefined`, and `vue-tsc` is green over a false type. **The one coverage-family row whose gate is FREE**: `npm run build` already runs `vue-tsc -b`, so no harness is minted. **FLAG-BEFORE-SPLIT** (§4a.5): the split is not type-safe until the flag lands or the return is honestly `number \| undefined` |
| **PAW-50** | `F.W9/W10` (canonical primary **F.W9**) | **F.W4** | **HARNESS-BEFORE-RIDER.** `web/` had no unit harness at all and `resolveFigure` was trapped in the SFC, so census **§4.5's DECISION** is its only true home; **F.W9 executes** and **F.W10 verifies**. It governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb and AA-44 — which is why §4a.5 makes it load-bearing for five other bookings |

**THE THIRD DEVIATION IS RETIRED — a fabrication, discharged rather than inherited.** Rounds 4–5 of
the spec's repair programme cut `fr-Tooltip:26`'s **FR-TT-1** to **F.W10** ⟨re-cut⟩ while
`conformance/CENSUS-CANONICAL.md` §2 homes it at **F.W4**. A booking at a wave the census does not
home is a **fabrication on R4-10's own terms**, and it was the last one standing in that programme.
Round 5 disclosed it **against its own interest and did not apply the fix**, because a unilateral
edit inside the shared splice bytes breaks `G-F9-21 ≡ G-F10-6`'s Δ=∅. **It was discharged at repair
round 6, 2026-08-29 (PASS-6 D-9), in BOTH twins in one pass**: the cut became **cite-the-holder
`F.W4`**, the identity stayed F.W4's single row, and **neither twin books it**. Nothing about the
finding, the fold or the close-gate design moved — only the cut.

**Verified at the bytes by this seat, 2026-09-20, double-run** — ⟨cmd⟩
`grep -E '^\| .?\`FR-TT-1\`? \|' CENSUS-CANONICAL.md` →
`` | `FR-TT-1` | `D-1` · `L-4` | `F.W4` · `F.W3` · `F.W9/W10` | **F.W4** <sub>legs: F.W3, F.W9/W10</sub> | ``,
and §2.2's cell in both specs now reads **F.W4** ⟨**CITED — the canonical's holder; NOT booked by
either twin**⟩. **So this addendum records TWO deviations and ONE discharged fabrication**, exactly
as `F-W10.md` §1a row 8 states it — and the naming legs for all **17** icon-only triggers were
already routed to F.W4 before the retirement, so no work moved with the cut.

### §4.11b — the pending paired edits: re-disclosed, and NOT applied

**PAIRED-EDIT LAW.** The §2.2 re-cut table, the by-mechanism entrant block and the F.W1
visual-regression checkpoint block are **SHARED SPLICE BYTES**: any change to them lands **in both
specs in ONE commit**, because a unilateral fix breaks the Δ=∅ leg of `G-F10-6 ≡ G-F9-21` — the very
gate this unit turns. **FIVE pairs stand disclosed in the spec's own register. This seat applies
NONE of them**, and could not lawfully do so in any case: `F-W10.md` and `F-W9.md` are **outside
this unit's writable set** and are IMMUTABLE at execution (E-3), with the spec's status `planned`.

| # | the pending pair | live state at these bytes (this seat, double-run) | holder |
|---|---|---|---|
| 1 | **AA-44 `9`→`8`** — the shared cell reads *"9 e2e never enter the audit tab"* while §4a.13's binding denominator reads *"AA-44: EIGHT specs, never nine"* | **OUTSTANDING and SYMMETRIC** — ⟨cmd⟩ `grep -c '9 e2e never enter the audit tab'` → **3** in `F-W10.md` and **3** in `F-W9.md`; the two shared regions still `diff` **empty**, so the asymmetry is a *content* error, not a splice drift | a paired edit; the spec calls it *"the one OUTSTANDING paired edit"* |
| 2 | **the 26-vs-17 F.W9 roster spread** ⟨*16 at E5-16*⟩ | **OUTSTANDING** — re-basing is assigned by **R4-10 to `F-W9`'s duty**, not to this end | F-W9 |
| 3 | **`PAW-49 → F.W0` and `PAW-12 → F.W9` vs canonical `F.W3` for both** | **OUTSTANDING** — ⟨cmd⟩ against `CENSUS-CANONICAL.md`: `PAW-49` → `**F.W3**`, `PAW-12` → `**F.W3**`. Both cuts operate on rows whose canonical primary is F.W3 | a paired edit |
| 4 | **`PAW-50 → F.W4` ⟨re-cut⟩ vs canonical `F.W9`** — **different in KIND** | **OUTSTANDING** — the canonical's **PRIMARY** is cut away from (`\| PAW-50 \| — \| F.W9/W10 \| **F.W9** \|`), where the other cuts move a **leg**; and §4a.5 makes PAW-50 load-bearing for five other bookings | a paired edit |
| 5 | **`FR-TT-1 → F.W10` vs canonical `F.W4`** | **DISCHARGED at the bytes** — repair round 6 (PASS-6 D-9) converted it to cite-the-holder in both twins in one pass; re-measured green above (§4.11a) | closed |

**Of the five, FOUR stand and ONE is discharged** — and the four are disclosed *against this wave's
interest*, since each is a live divergence between the re-cut this file records and the census of
record. An exclusion that names the ruling, the reason and the holder is a disposition; one that
names only the reason is a deferral wearing a verb. **The twin carries its own paired register at
the other end — `E-F9-1`, the eight paired obligations `P-1 · P-2 · P-4..P-9` — cited here, booked
here by nobody.**

### §4.11c — the routing-law taxonomy statement (zero rows minted)

The routing-law header line that enumerates every wave id is **taxonomy, not a routing**: it states
the vocabulary a record's disposition may use, and it mints **no row**. It is excluded **BY SHAPE** —
a taxonomy line names three or more wave tokens beside the disposition, in **any** of its spellings
(*"Routing law"*, *"Routes:"* **and** *"Dispositions:"*; excluding one of the three and counting the
other two as findings is a measured historical defect of this programme). The same holds for the
**same-identity restatements**, which fold to their row and are never counted twice, and for the
**verdict/adjudication prose**, which is **dated, immutable, and annotated by addendum only**.
**No row anywhere in §4.11 is minted from a taxonomy line, from a restatement or from verdict prose.**

---

## §6.5-ERRATA — two census corrections, landed beside rather than in

⟨Both are **NO-WAVE-OWNER census corrections** in the adjudicated registry — rows whose entire
content is *"the census says X and the tree says Y"*. They have exactly one lawful home: an addendum
beside the dated census. **Neither is a lane-doc rewrite**: `lane-frontend.md` and
`CENSUS-2026-08-03.md` are dated bytes and are not touched by this file.⟩

### FR-CP-44 — the metric-stack shadow candidacy is seated on the wrong hosts

**Banked, quoted from the frozen corpus** — ⟨cmd⟩ `sed -n '82p' registry/adjudicated/fr-CoefficientsPanel.md`:

> **FR-CP-44 = L/A-4 — INFO.** lane-frontend:443 seats the `./metric-stack → ./metric` shadow
> candidacy on the two 26/17-line hosts, neither of which renders a metric; the tree says it belongs
> on CoefficientsSpectrum.vue (whose row anatomy matches the MetricStack docblock almost line for
> line). Census correction; the re-seat is FR-CP-3's candidate cure, with the "by construction"
> overclaim already noted. **NO-WAVE-OWNER** (census correction) + F.W3 candidate.

**THE CORRECTION.** `lane-frontend.md:443`'s row reads
`` | `visualization/CoefficientsPanel.vue` / `EqCoefficientsPanel.vue` | 26 / 17 | `./metric-stack` (4.0.0) → `./metric` (7.0.0) | | ``
(⟨cmd⟩ `sed -n '443p' formation/fourier/lane-frontend.md`, this seat). **Neither host renders a
metric**, so the candidacy is seated where the shadow cannot be observed. **The census's shadow
candidacy for `./metric-stack → ./metric` is RE-SEATED onto `CoefficientsSpectrum.vue`** — the
component whose row anatomy matches the MetricStack docblock almost line for line — and the
**re-seat itself is F.W3's candidate act** (FR-CP-3's candidate cure), declared at `F-W10.md` §4b's
F.W3 row as *"FR-CP-44's re-seat candidate"*. **Two things are deliberately NOT done here**: the
lane doc is not rewritten (E-3), and the re-seat is **not booked** as an F.W10 act — this addendum
carries the *census correction*; the tree act belongs to the wave that holds the surface. **The
"by construction" overclaim in the banked candidacy stands noted, not laundered.**

### FR-AFP-51 — the ToastVariant break is UPLIFT-TARGET, not present-tense

**Banked, quoted from the frozen corpus** — ⟨cmd⟩ `sed -n '112p' registry/adjudicated/fr-AdminFlaggedPanel.md`:

> | FR-AFP-51 · L-27 | The census's ToastVariant break is an uplift-target break, not present-tense —
> at the pin the symbol resolves; tense clarification only | record — NO-WAVE-OWNER |

**THE CORRECTION, stated in the tense the census should have used.** The census reads
*"`ToastVariant` definition-absent → hard typecheck break (`useToast.ts:3,9`)"* in its uplift-break
surface bullet and *"`ToastVariant` **is** a hard typecheck break"* at its [P2] item 10. **The break
is a property of the UPLIFT TARGET, not of the tree the census measured**: at the pin the census
audited, the symbol **resolved** and nothing was broken. The correct reading is *"`ToastVariant`
**would become** definition-absent at the uplift target"* — an **UPLIFT-TARGET break**, one line of
the uplift budget, never a live defect. **Why the tense matters, in the census's own economy**: the
uplift budget is what the [P2] item prices, and a present-tense break inflates it with a defect that
does not exist at the measured pin. **The correction must reach whatever spec quotes the census break
table**, which is why it is filed here rather than left in a record.

**Read-only corroboration at the landed pin, this seat, 2026-09-20** (an observation, never a
re-derivation of the banked row): fourier `web/package.json` now pins `@mkbabb/glass-ui ^8.0.0` ·
`@mkbabb/keyframes.js ^6.0.0` · `@mkbabb/value.js ^4.0.0` — F.W1's atomic transaction — and
⟨cmd⟩ `grep -rn 'ToastVariant' web/src/` returns **one file, two hits, both PROSE in
`composables/useToast.ts` doc comments** (*"is definition-absent at the adopted pin: a toast
reports, so it …"* · *"The `ToastVariant` limb of the same row is F.W1's fold, cited and not …"*).
**The symbol is used nowhere as a type at these bytes**: the uplift landed and the limb was folded,
so the census's present-tense sentence is now stale **twice over** — wrong tense when written, and
describing a symbol the tree no longer uses. **This changes no banked grading and books nothing.**

### §6.5-ERRATA-S (dated 2026-09-22, `X.F.W10S.a`) — the two §2.5a census-input errata: `i-1 = L-INFO-2` · `PP-CENSUS`

⟨**Addendum-beside, E-3.** Landed by `X.F.W10S.a` under COHESION **§0aj** bullet 2 (F.W10 **CK-1**,
MEDIUM: *"The two census-input errata never landed at the surface §2.5a declares for them"*). The
bytes above this section — FR-CP-44, FR-AFP-51, and the Scope row's *"FLAGGED, NOT BOOKED"* — are
**not rewritten**. Like the two errata above, these are **NO-WAVE-OWNER census corrections**: they
land **beside** the dated census; `lane-frontend.md` and `CENSUS-2026-08-03.md` are **not touched**.
The terminal verbs stay where §4.2 booked them; this section carries only the **erratum text** that
§4.2's RESIDUAL named as owed.⟩

**The two rows, quoted VERBATIM from COHESION §4.2** — ⟨cmd⟩ `sed -n '524p;544p' docs/tranches/X/COHESION.md`
(row 65 at `:524`, row 76 at `:544`, this seat's settled bytes, 2026-09-22):

> | 65 | **`i-1 = L-INFO-2`** · fr-BasisSelector:92, INFO | *"(census correction UPHELD)… `lane-frontend.md:87`'s 'per-basis term sliders' is wrong. → NO-WAVE-OWNER (SS-3/SS-4 census input)."* | **CENSUS-INPUT** — a lane-doc correction; it lands in the CENSUS-ADDENDUM's errata beside FR-CP-44 / FR-AFP-51, **never as a lane-doc rewrite**. ⚠ **RESIDUAL, flagged not silent**: `X.F.W10.a` landed §1a #8's own enumeration and this row's erratum is **owed and not yet written** — its landing surface is outside this unit's writable set (see §4.2.F) |
>
> | 76 | **PP-CENSUS** (L-1, census-integrity half) · fr-PathPreview:31, MINOR | arrow-form strict: *"MINOR → NO-WAVE-OWNER (M-25 errata; SS-3/SS-4 must read the registry over `lane-frontend.md:183/:366/:369/:444/:565`, which describe a dead component as a live bespoke surface)."* | **CENSUS-INPUT** — the five lane-doc errata land in the CENSUS-ADDENDUM, **never as a lane-doc rewrite**. **The drain's SECOND self-falsifier**: a row whose content is *"SS-3/SS-4 must read the registry"* is exactly the row a drain reading a check file could not see. ⚠ Same **flagged residual** as row 65 |

#### `i-1 = L-INFO-2` — BasisSelector's sliders are GLOBAL contour parameters, not per-basis

**Banked, quoted from the frozen corpus** — ⟨cmd⟩ `sed -n '92p' registry/adjudicated/fr-BasisSelector.md`:

> - **i-1 = L-INFO-2 — INFO (census correction UPHELD).** The sliders are GLOBAL contour parameters (VV binds the same two refs to BasisSelector and ContourSettings — my read of :260/:265-267/:270); lane-frontend.md:87's "per-basis term sliders" is wrong. → **NO-WAVE-OWNER** (SS-3/SS-4 census input).

**THE CORRECTION.** `lane-frontend.md:87` reads
`` | `components/visualization/BasisSelector.vue` | 324 | Basis toggles + per-basis term sliders | ``
(⟨cmd⟩ `sed -n '87p' formation/fourier/lane-frontend.md`, this seat). **Read instead**: *"Basis
toggles + the GLOBAL contour-parameter sliders"* — the two slider refs are shared with
`ContourSettings` (the same refs bound at both hosts, per the banked read), so they are **not
per-basis**. SS-3/SS-4 read the registry over this lane row.

#### `PP-CENSUS` — `PathPreview.vue` is a dead component, not a live bespoke surface

**Banked, quoted from the frozen corpus** — ⟨cmd⟩ `sed -n '31p' registry/adjudicated/fr-PathPreview.md`:

> 2. **PP-CENSUS** (L-1, census-integrity half) — **MINOR → NO-WAVE-OWNER** (M-25 errata; SS-3/SS-4 must read the registry over `lane-frontend.md:183/:366/:369/:444/:565`, which describe a dead component as a live bespoke surface — the census audited the file, not the graph).

**THE CORRECTION — five lane-doc sites, each annotated here and none rewritten** (⟨cmd⟩ `sed -n
'<n>p' formation/fourier/lane-frontend.md`, this seat, for each `<n>`):

| site | the lane doc says | read instead |
|---|---|---|
| `lane-frontend.md:183` | `` `components/ui/PathPreview.vue` · 69 · Bespoke SVG path thumb (no glass-ui analogue) `` | a **dead** component (census-integrity half of L-1): the file exists, the render graph does not reach it |
| `lane-frontend.md:366` | *"`PathPreview.vue` is bespoke SVG"* among the 5 `src/components/ui` files | the file count stands; its standing as a live bespoke surface does not |
| `lane-frontend.md:369` | `PathPreview.vue` (69) under *"Bespoke with no glass-ui analogue"* | strike it from the live bespoke inventory; it is dead |
| `lane-frontend.md:444` | `` `components/ui/PathPreview.vue` · 69 · *(none)* · genuinely bespoke — no flag `` | "no flag" is wrong in kind: the flag is **dead code**, not a missing glass-ui analogue |
| `lane-frontend.md:565` | `ui/PathPreview.vue` in the SVG-surface enumeration | not a live SVG surface |

**Why the census missed it, in the banked words**: *"the census audited the file, not the graph."*
**This section books nothing**: the terminal verbs of both rows are §4.2's (rows 65 · 76), the
deletion or keeping of `PathPreview.vue` is not an act of this addendum, and no banked grading
moves. It discharges only the landing §4.2's RESIDUAL declared owed (F.W10 CK-1).

---

## §6.7 — the charter line, in its CORRECTED wording

**§6 item 7's *"Codex R3–R6 residue formally not inherited … carry zero credit"* is SUPERSEDED.
The charter line the wave specs inherit is, and is only, this:**

> **Zero UNVERIFIED credit — every Codex verdict enters the formation through per-claim adjudication (M-21)**; the adjudicated rows are in the master ledger (`audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md`).

⟨The charter line is written on **ONE line, unbroken**, deliberately: a gate that greps for it must
be able to find the whole sentence, and a wrapped quotation is a receipt that cannot be read by the
instrument that checks it.⟩

**The superseded wording is named, never re-used.** *"Carry zero credit"* conflated **authority**
with **measurement**. The authority is dead — the R-coordinates' governance statuses were
OWNER-GATED, and the owner has since ruled: **OG-F1 → FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE**
and **OG-F2 / OG-V2 → CODEX-ERA-SPECIFIC** (COHESION §0j.D, 2026-09-17), the latter stating in its
own bytes that *"the `FOURIER-R*` value-side files are records"* and that **"G-F10-8 closes on the
charter line"**. The measurements are live, reproducible, and were re-reproduced against the live
tree — they enter the substrate **by adjudication, row by row**, which is exactly what *zero
UNVERIFIED credit* means and what *zero credit* denied. The full charter sentence the census gives
the specs: *no authority, no admission, no slot — and the adjudicated structural census + model
defects enter the substrate as measured fact.*

**Provenance, so no seat re-litigates it**: the corrected wording is the dated
`CENSUS-2026-08-03.md` ADDENDUM's own §6.7 correction (M-23 intake-adjudication master seat;
34 adopted · 11 carried · 1 refuted · 6 owner-gated). **This addendum does not author it — it CARRIES
it onto the census-side surface the F specs were chartered to give it**, which is the whole of
`G-F10-8`'s charter limb. **Never *"carry zero credit"*; never a paraphrase.**

---

## Scope — what this addendum does NOT carry, named so nothing is silently dropped

| not carried here | why, and who holds it |
|---|---|
| Any edit to `CENSUS-2026-08-03.md`, `lane-frontend.md`, `lane-docs.md`, `lane-crud.md` | **E-3**: dated bytes are immutable and are annotated **beside**. `git diff --stat` on the dated census stays **empty** |
| Any edit to `registry/adjudicated/fr-*.md` | read-only + **anti-rename**. The re-cut is a table in two specs and this one record |
| Any edit to `F-W10.md` or `F-W9.md` | both are outside this unit's writable set and IMMUTABLE at execution; the five paired edits at §4.11b are **disclosed, not applied** |
| The census-input errata routed here by `F-W10.md` §2.5a's drain rows — **`i-1 = L-INFO-2`** (fr-BasisSelector:92) and **`PP-CENSUS`** (fr-PathPreview:31, the L-1 census-integrity half) | **FLAGGED, NOT BOOKED.** Both rows name this addendum as their landing surface *"beside FR-CP-44 · FR-AFP-51"*, but both are **§2.5a rows** — `X.F.W10.c`'s sections — while §1a row 8 enumerates this file's F.W10-owned content as the §4.11 re-cut record, the cut deviations, FR-CP-44, FR-AFP-51 and the §6.7 charter line. This seat writes exactly that enumeration and **does not reach into a sibling unit's sections**. **The obligation is live and is raised as a residual in `execution/C/F-W10.md` §X.F.W10.a**, so the close seat rules it rather than inheriting a silence |
| ↳ *dated line beside the row above, 2026-09-22, `X.F.W10S.a` (COHESION §0aj bullet 2; F.W10 CK-1)* | **LANDED 2026-09-22** at **§6.5-ERRATA-S**, beside FR-CP-44 / FR-AFP-51: both errata's text quoted VERBATIM from COHESION §4.2 rows 65 / 76, with the corrections to `lane-frontend.md:87` and `lane-frontend.md:183/:366/:369/:444/:565`. The row above stands as written (E-3) — it was true at its date |
| Terminal verbs for any NO-WAVE-OWNER row | `X.F.W10.c`'s drain into COHESION §4 — 89 rows / 89 ids / 89 terminal verbs. **This file books none of them** |
| The re-seat of the `./metric-stack → ./metric` shadow candidacy at the tree | **F.W3's candidate act** (§4b's F.W3 row). This addendum carries the census correction; the surface act is its holder's |

---

## Receipts — every figure above, read from the settled bytes and double-run

**RE-CUT-0's four corpus probes** — ⟨cmd⟩ run in `docs/tranches/V/megatranche/registry/adjudicated/`,
this seat, 2026-09-20, **each command run twice with identical output**:

| probe | run 1 | run 2 |
|---|---|---|
| `grep -o 'F\.W9/W10' fr-*.md \| wc -l` | **54** | **54** |
| `grep -l 'F\.W9/W10' fr-*.md \| wc -l` | **25** | **25** |
| `grep -oE '(→\|⇒) *\*{0,2}F\.W10' fr-*.md \| wc -l` (bare-F.W10 routing) | **0** | **0** |
| `grep -o 'F\.W10' fr-*.md \| wc -l` (**any** bare `F.W10` token, the stronger form) | **0** | **0** |
| `ls fr-*.md \| wc -l` | **66** | **66** |

**The two splice diffs (`G-F10-6 ≡ G-F9-21`'s Δ=∅ leg)** — ⟨cmd⟩ run in
`docs/tranches/X/fourier/waves/`, double-run, both **EMPTY** at exit 0 both runs:

- `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty** · **empty**
- `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty** · **empty**
- the spliced re-cut region spans ⟨cmd⟩ `... \| grep -cE '^\| fr-'` → **22** record-rows carrying **26** identities (four rows carry two identities each: `fr-AdminFlaggedPanel` · `fr-EquationResult` · `fr-EquationView` · `fr-PaperArticleWindow:58 / :90`), and §4.11's table above enumerates **26** id-rows: ⟨cmd⟩ `grep -cE '^\| [0-9]+ \| \`fr-' CENSUS-ADDENDUM-2026-08-25.md` → **26** · **26** ⟨**WRITE-THEN-MEASURE, and the first form was WRONG.** This receipt was first written as the unanchored `grep -cE '^\| [0-9]+ \| '`, which returns **31** at the settled bytes — the 26 record rows **plus §4.11b's five pending-pair rows**, which are numbered `1`–`5` in the same shape. The anchored form above is the honest instrument and reproduces **26** twice. The defect is published rather than silently repaired because a count taken over a file that is still being written is exactly the false-witness class this sub-tranche exists to abolish⟩, whose cuts tally ⟨cmd⟩ **F.W9 23** · **F.W0 1** · **F.W4 2** (one booked, one cite-the-holder) = **26**

**The §4.10 splice, proven reversible** (the de-quoting is the only transformation applied):
⟨cmd⟩ `awk '/^> \*\*§4\.10 — THE DEPLOY SPINE/,/^> is escalated as \*\*E-F9-1\*\*\./' execution/C/F-W9.md`
→ **44 lines**; de-quoted with `sed 's/^> //; s/^>$//'` → **44 lines**; and the round trip
⟨cmd⟩ `diff <(sed 's/^$/>/; s/^\([^>]\)/> \1/' plain) quoted` → **EMPTY**. The received region is
therefore byte-identical to `X.F.W9.c`'s authored carve modulo the record's blockquote marker.

**The canonical homes behind §4.11a/§4.11b** — ⟨cmd⟩ against
`docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md`, the census of record:

- `` | `PAW-49` | — | `F.W3/W4` · `F.W9/W10` | **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W9/W10</sub> | ``
- `` | `PAW-50` | — | `F.W9/W10` | **F.W9** <sub>legs: F.W9/W10</sub> | ``
- `` | `PAW-12` | `C-M-2` · `L-8` · `C-m-2` | `F.W3/W4` · `F.W9/W10` | **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W9/W10</sub> | ``
- `` | `FR-TT-1` | `D-1` · `L-4` | `F.W4` · `F.W3` · `F.W9/W10` | **F.W4** <sub>legs: F.W3, F.W9/W10</sub> | ``

**AA-44's outstanding pair, measured symmetrically** — ⟨cmd⟩
`grep -c '9 e2e never enter the audit tab'` → **3** in `F-W10.md` · **3** in `F-W9.md` (the shared
cell plus its two in-file restatements), against §4a.13's binding *"AA-44: EIGHT specs, never nine"*
— ⟨cmd⟩ `grep -o 'AA-44: EIGHT specs, never nine' F-W10.md` → one hit. **Symmetric in both twins, so
the Δ=∅ leg is unaffected and the correction stays a PAIRED edit.**

**THE ONE FIGURE THIS SEAT DOES NOT REPRODUCE, published against its own interest (INFO).**
`F-W10.md` §2.1's anatomy of the 54 is **26 identities ⊕ 14 routing-law boilerplate ⊕ 2
restatements ⊕ 12 verdict prose**. The 26 reproduce by enumeration (the table above). **The 14 does
not reproduce under this seat's own shape probes**: ⟨cmd⟩ in `registry/adjudicated/`, double-run —
`grep -h 'F\.W9/W10' fr-*.md \| wc -l` → **54** (one occurrence per line, so lines and occurrences
coincide); `grep -h 'F\.W9/W10' fr-*.md \| grep -cE '(F\.W[0-9]+[^0-9].*){3,}'` → **24**;
`grep -h 'F\.W9/W10' fr-*.md \| grep -cE 'Routing law\|Routes:\|Dispositions:'` → **8**. **Neither is
14**, and neither is the spec's predicate: §2.1's boilerplate class was hand-classified at authoring
as *the routing-law header line*, where the ≥3-wave-token shape also sweeps in verdict prose that
enumerates routes. **The anatomy is therefore carried as §2.1's BANKED classification and is not
re-derived here** — and the substance is untouched either way, because **what §4.11 books is the
26-identity cut, which is verified by enumeration and not by the residue's partition.** Filed INFO,
for the close seat and for whatever wave re-cuts the taxonomy detector.

**E-3 receipt** — ⟨cmd⟩ `git diff --stat docs/tranches/V/megatranche/formation/fourier/CENSUS-2026-08-03.md`
→ **empty**, double-run, and ⟨cmd⟩ `git status --porcelain docs/tranches/V/megatranche/` shows this
file as the **only** row from this seat.

**SELF-COUNT LAW — figures about THIS file, read from the settled bytes after the last write,
double-run, and stated with their self-inclusion disclosed.** ⟨cmd⟩ over
`CENSUS-ADDENDUM-2026-08-25.md` itself: the corrected **§6.7 charter line** matches as ONE whole
unbroken sentence — `grep -c 'Zero UNVERIFIED credit — every Codex verdict enters the formation
through per-claim adjudication (M-21)'` → **1** · **1**. The **superseded** wording of that charter
line survives **only inside quotation marks that name it superseded**: case-sensitive → **2** · **2**
hits, both in §6.7 — at *"is SUPERSEDED"* and at the closing *"never a paraphrase"* prohibition —
and the case-sensitive arm is the **operative** figure, unchanged by this receipt.

⟨**R3-3.8 BEFORE/AFTER PAIRING, DECLARED IN THREE STATES — this receipt twice counted a file its own
bytes were changing, caught itself both times, and publishes the sequence rather than the
post-image.** **(i)** Its first form published the case-insensitive arm as **4** and asserted *"this
receipt adds none of them"* — false the instant it was written, because that paragraph **quoted the
superseded wording twice while restating it**, so the true reading at those bytes was **6**.
**(ii)** The repair paragraph disclosed the 4→6 pairing — and, by quoting the phrase again to
explain it, kept the arm at **6**, a correction that preserved the defect it described. **(iii)**
This third form **names the wording without spelling it**, so the settled reading is **4** · **4**,
every one of the four inside §6.7's supersession bytes. **The case-sensitive arm read 2 at all three
states** and is published as operative for exactly that reason: *a receipt whose own bytes move its
figure is not a measurement, and the cure is an instrument the bytes cannot reach — not a better
sentence.* **The substance never moved**: the superseded wording is confined to supersession bytes
and stands nowhere as a live claim.⟩

**This file publishes NO count of its own length**, for the same reason: every attempt moved the
figure it printed (**374 → 387 → 397**, each stated inside the write that invalidated it). The
settled length is measured **after the last write, from outside**, and is published in this unit's
receipt at `docs/tranches/X/execution/C/F-W10.md` §X.F.W10.a — a file this seat is no longer editing
when the count is taken. **No count of this file is offered as any
gate's operand** — the gates' operands are the 66 adjudicated records and the two specs' spliced
regions, measured above.
