Served model id: `claude-fable-5` (fresh Fable agglomerator, M-23)

# keyframes.js — FORMATION CENSUS (agglomerated) — 2026-08-03

**Substrate of record:** `/Users/mkbabb/Programming/keyframes.js` at `origin/master` = `81a56990`
(the tree V actually executed in, via `keyframes-v-exec`). The local owner checkout sits at
`8281638c`, **41 commits behind, +1 local** — SACRED, never mutated (DOCS §0).
**Lanes consumed WHOLE:** `lane-library.md`, `lane-frontend.md`, `lane-docs.md` (all three
`claude-opus-5[1m]`). **Agglomerator probes** (read-only, marked AGG-P*): glass-ui declaration,
KfPillTabs survival, src/demo counts at `origin/master`.

**Commission section (verbatim, `CONSTELLATION-COMMISSION-2026-08-03.md` §2):**
> **Goal**: library + demo audited with the same burning lucidity (D/L/C per component, tri-fold
> jury); consumes the new value.js parser at its gate; full glass suffusion; colocation/
> modularization settled.
> **Horizon**: per-component saturation + adjudication convergence (two dry passes), demo visually
> audited Safari mobile+desktop.
> **Admission**: full keyframes wave specs with born-RED gates + the parser-consumption wave keyed
> to the parse-that lane's release condition.

---

## §0 — THE SUBSTRATE SCHISM (the agglomerator's headline; overrides lane headlines)

The three lanes did not census the same tree. LIB and FE censused the **stale owner checkout**
(`8281638c`, 5.3.5-era base + 1 local commit); DOCS proved `origin/master` is 41 commits ahead and
carries ALL of V's execution — the Value-4 transpose, the Glass-7 consume, the W5 module carves,
proof:structure R1–R6 (DOCS §0, §1.3). Consequences, each verified:

| # | Claim (lane) | At `origin/master` | Verdict |
|---|---|---|---|
| SCH-1 | FE F-1 **RED**: `@mkbabb/glass-ui` phantom — absent from package.json + lock | `package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` EXACT devDep (AGG-P1; DOCS §3.2; W2 `add20b7e`/`ebb08948` registry-only lock) | **FALSIFIED at HEAD.** True only of the stale checkout; the cure is DOCS §B-12 reconciliation, not a dependency wave |
| SCH-2 | FE S-1/F-2: KfPillTabs 217-line fork live | All 4 files present at HEAD incl. the shim and its test (AGG-P2) | **CONFIRMED at HEAD** — survives V; still the top glass-suffusion target |
| SCH-3 | LIB: 145 src `.ts` files, 22 636 LOC | **153** `.ts` at HEAD (AGG-P3); FOLD-FORWARD §C says "159 modules" per proof:structure (DOCS §1.3 — different counting basis, unreconciled) | LIB roster ~8 files stale; zone shapes broadly hold but W5 carved 6 modules |
| SCH-4 | LIB: 61 value.js `from`-lines (`/value` 15) | **62** (`/value` 16) (DOCS §0 probe) | LIB import census off-by-one at HEAD |
| SCH-5 | LIB Tier-A anchors A3/A5/A6 (`value-ast.ts:71`, `easing-registry.ts`) | Renamed by W5 carves: `compile/value/compile.ts:32`, `compile/easing/registry.ts`, `emit/backward/backward.ts`, `emit/easing-serialize.ts:71-73` (DOCS §6 drift table) | **4+ cited paths dead at HEAD**; the 13-site seam inventory is real but must be re-anchored |
| SCH-6 | FE roster: 58 `.vue`, 11 984 lines | **58** `.vue` at HEAD (AGG-P3) | Count holds; contents drifted (W2's 65-path demo slice landed post-substrate) — per-file claims need HEAD re-verification |
| SCH-7 | LIB L-1/L-2 depcruise defects | Unverified at HEAD; FOLD-FORWARD §C "0 depcruise violations" does NOT refute L-1 (silent coverage loss reds nothing) | **Re-verify at HEAD** before speccing |

**Formation law consequence:** no wave spec may cite a file:line until re-anchored at
`origin/master` (or the reconciled checkout). The lanes' *findings* survive; their *anchors* do not
all.

## §0.5 — The mail hazard (DOCS §0, unchanged severity)

Value packets **O-8** (parser totality, 2026-07-24) and **O-11** (library-band K1–K4, 2026-07-27)
were delivered as untracked files into the sacred stale checkout — absent from `origin/master` AND
from `keyframes-v-exec`. They are UNMARKED, UNANSWERED (keyframes silent 16 days), and were
**measured against the stale tree** (SCH-4/5). O-11 §A3 also under-enumerated: it missed keyframes'
3 `parseStylesheet` call sites (`compile/adapter.ts:222`, `scroll/grammar.ts:109`,
`validate.ts:182`) that LIB §4.1 independently catalogs (DOCS §6 row 17). An amendment-addendum,
delivered to an exec-visible path, is owed before any kf-side wave can consume these packets.

---

## §1 — Cross-lane reconciliation (counts that should agree)

| fact | LIB | FE | DOCS | verdict |
|---|---|---|---|---|
| package version / tag | 6.0.0 / v6.0.0 | — | 6.0.0 both trees; gitHead of the cut = `5a9183a7` (oldest of the 41 remote commits — ALL of V is unpublished) | AGREE; **no cut since 6.0.0, no Unreleased section** |
| value.js pin | `4.0.0` EXACT, 6/7 subpaths (`/quantize` never) | `4.0.0` (package.json:69) | `4.0.0` at HEAD (:70); pin doctrine bilaterally ratified | AGREE |
| glass-ui | (out of scope) | ABSENT from manifests, 7.0.0 installed | `7.0.0` EXACT devDep at HEAD | DOCS+AGG-P1 override FE (SCH-1) |
| demo scale | 184 `.ts`/`.vue` | 206 files, 58 `.vue`, 68 engine-consuming | — | AGREE on shared substrate; 58 holds at HEAD |
| tests | 131 files, 1 051 cases, **0 colocated** (100% external `test/` tree) | — | — | Unchallenged; two bench `*.measure.test.ts` picked up by NO vitest project (LIB §5) |
| runtime import cycles | **0** (17 type-only rings) | — | — | Unchallenged |
| kf V state | — | — | 5/13 waves CLOSED, W9 staged, 4 fold whole; successor = the shared vnext (= THIS mega-tranche) | Formation-defining: kf-side asks have **no owning wave** on their side today |

---

## §2 — Library fold (LIB, re-weighted by DOCS)

**Shape.** One top-level dir `src/animation/` (a zero-information level; worst stutter
`src/animation/engine/animation.ts`), 14 zones, `internal/` the clean universal sink (35 in, 0
out), 2 published entries only (`.` LIGHT static barrel / `./engine` HEAVY via `public.ts`), ES
only. The LIGHT/HEAVY boundary = exactly 2 dynamic imports; `load-engine.ts` is *the* value.js
firewall. `rolldownOptions.external` enumerates the 6 consumed value subpaths explicitly — **a 7th
consumed subpath requires editing that array or it silently bundles** (LIB §2).

**The parse seam (the parser-consumption wave's cut-point inventory; re-anchor per SCH-5):**
- **Tier A — 13 delegated grammar sites** (parseStylesheet ×3, parseCssValues, parseKeyframeSelector,
  parseTimingFunction ×2, parseCssScalar ×2, parseAnimationTimeline/Range, resolveKeyframes ×2) +
  9 AST-collector call sites (LIB §4.1).
- **Five distinct failure postures** across those sites: absorb-to-diagnostics (adapter), throw
  TypeError (value-ast, scroll `requireParsed`), swallow-to-`[]` (validate), throw structured
  `AnimationOptionError` (selector) (LIB §7.5). Strongest argument for a single kf-side `parse()`
  façade BEFORE consumption.
- **Tier B — CSSOM bridge** `ingest/cssom.ts` (466 L, god module, on the seam): the unescaped
  identifier→RegExp injection at `:214-216` (a CSS-escaped `@keyframes` name with regex
  metacharacters throws or mis-matches), plus the standing `:29-33` comment declaring value.js
  VJ-9 partial-input OPEN — a live self-declared handoff to the parser wave (LIB §4.2).
- **Tier C — 6 ad-hoc regex parses of CSS-ish text**, all deletion candidates, incl. a
  serialize→regex-reparse round trip inside `emit/view-transition.ts:146` (LIB §4.3).
- **Tier D — the serializer half**: `emit/css-text.ts` (in-degree 12, most-imported module) is a
  full AST→text writer living in kf; **DUAL-1** two live `reverseAnimationShorthand`s (kf's + the
  value.js one) both reachable from the emit tier — a value shorthand change moves ONE of them
  (LIB §7.4). The `serializeCssValue` fork at `emit/css-text.ts:41` (verified at HEAD) retires
  onto value's published export at the 4.1 bump, with known divergence `"a : b"`→`"a: b"`
  (DOCS §6 row 7).
- **Regression net**: 14-fixture round-trip corpus + manifest (byte/epsilon/text oracles) + 15
  parser-facing test modules; `roundTripScrollCSS` already codifies the replay-equality oracle the
  rest of the seam lacks (LIB §4.4/§4.7).
- **Demo blast radius**: 5 parse consumers incl. `useSquareTumble.ts:22 parseCssColor(css)` — the
  known **R1 shipping-crash surface** (LIB §4.6; megatranche R1).

**Flags.** 9 god modules (≥~437 L; 4 on or adjacent to the seam); 5 pure pass-through fragment
shims — the `presets/` trio is a documented split that never happened (4-hop re-export ladder,
hop 3 a no-op); 16 dir/name stutters + the `src/animation` single-child; 17 type-only cycle rings
(build-erased, real coupling); LEG-1 `legacyClauses()` the one genuine legacy path; LEG-3 stale
depcruise prose claiming the math leaves are "copies" when `:28` re-exports from
`@mkbabb/value.js/math` (verified at HEAD, DOCS §6 row 4); **L-1** 4 dead LIGHT-allowlist paths =
silent guard loss on the value.js firewall; **L-2** the promised no-cycle baseline file does not
exist (LIB §3.5, §7 — re-verify at HEAD per SCH-7).

---

## §3 — Frontend fold (FE, re-weighted)

**The clean half:** zero local shadcn `ui/` copies, zero direct reka-ui imports (reka reached only
through glass-ui), zero cva/clsx in demo source. Glass topology is right; suffusion is shallow —
**21 of 73 subpaths (29%)**, and the unreached 52 include exactly what the demo hand-rolls (FE §3).

**The shadow census (glass 7.0.0 primitives verified in the installed dist — no upgrade needed):**

| id | bespoke | lines | verdict |
|---|---|---|---|
| S-1 | `KfPillTabs` + composable | 217 | **REPLACE** — the blocking 4.0.1 ARIA rationale is fixed in 7.0.0 (`dist/tabs.js:232` conditional emission); survives at HEAD (AGG-P2). Residual: the `role=group` vs `role=tablist` *design* claim is not answered by the aria fix — carry into the spec |
| S-2 | type-only `/tabs` imports ×3 | — | the S-1 tell: data contract adopted, renderer rejected; prose disagrees with tree |
| S-3 | timeline cluster ×4 | 666 | **EVALUATE, own spec** — glass `/timeline` family (6 components + geometry) never imported; keyframe-editing semantics may exceed the primitive's contract |
| S-4 | `SequenceScrubber` | 162 | evaluate — `ScrubberTimeline`/`Slider` shape |
| S-5 | `AnimatedText` | 126 | evaluate — `/typewriter` unimported; owner requires per-char granularity, verify support first |
| S-6 | `App.skeleton` shimmer plate | 101 | partial — keep stage geometry, delegate the plate to `Skeleton` |
| S-7 | `CopyButton` | 113 | partial — no glass copy primitive; independent defect: runtime JS-string `@keyframes` injection (`:70,:83`) bypassing the cascade |
| S-8 | `TypingDots` | 125 | **KEEP** — exists to dogfood the engine (inv-ζ seam); replacement removes library coverage |

**Hygiene:** F-5 one DEAD back-compat shim + one incoherent shim (component from real path, type
through shim on adjacent lines) — direct `feedback_no_backwards_compat` violations; all 4 shim
files present at HEAD (AGG-P2). **98 unprefixed demo custom properties, zero `--kf-*`** — flat
global namespace shared with glass-ui tokens (FE §6.3). Soft coupling: `tab-idiom.css` /
`playback-idiom.css` style reka's generated DOM through glass-ui's render tree. PRM coverage
conscientious but mechanism-split (10 CSS blocks, 2 raw `matchMedia`, 1 `useMediaQuery`) with 2
delegation sites unverified (FE §6.5). State = hand-rolled stores + scene FSM, 71 composables, 63
colocated — the colocation idiom already mirrors value.js's; "colocation settled" is mostly a
ratification act, not a migration (FE §7).

**Structural cycle:** glass-ui peers on keyframes.js while kf's demo consumes glass-ui, held
together by the `@mkbabb/keyframes.js` self-alias dedupe (FE §8) — deliberate, but any parser wave
touching the entries must respect it.

---

## §4 — Coordination/release fold (DOCS)

- **kf V = CLOSED-BY-FOLD**: 5/13 waves closed, W9 staged on `v/w9-staging`, W7/W8/W11/W13 fold
  whole into the successor — which IS the shared vnext / this mega-tranche. FOLD-FORWARD §B (15
  rows) is the kf-side inheritance; §B-12 (owner-checkout reconciliation, one act, owner's hand) is
  the SCH-1..7 cure. OD-V3/OD-V5 remain unruled owner rows, never proxied (DOCS §1.3/§1.4).
- **The 4.1 cut is ONE coordination event** (CC-084: parser totality cure + SCI-1 + easing
  restoration, "no emergency 4.0.1 — ruled"): cut + kf repin + 8-curve visual delta (max|Δ| 0.192
  `ease-out-circ`) + `serializeCssValue` fork retirement + `sampleColorRamp` becoming real. The
  W.L6 `npm ls` gate's completion is **partly owned by a repo value.js cannot edit** (DOCS §3.3,
  §7.4).
- **K1–K4 verified at HEAD**: K1 easing reference instability (21/40 unstable, 9 collisions, the
  `.find()` reverse-map, load-bearing false docstring at `registry.ts:36`); K2 tautology spec +
  false "barrel" docstrings; K3 nine `noUnusedLocals` errors incl. the dead `/css` type import on
  the light/heavy boundary (`load-engine.ts:65`); K4 provenance gate (`deltaEOK` is NOT shipping;
  `sampleColorRamp` is) (DOCS §6 rows 3–6).
- **The `bezerPresets` fence is load-bearing at module evaluation**: a key removal is a kf boot
  `throw` at `registry.ts:43`; an addition silently widens kf's public registry (DOCS §3.3.3).
- **Converged rows needing one closing line**: D-GAP-6/`sampleBezier` conditional resolves to
  "not adopted" (4.1 declines it permanently); O-8's vehicle question was pre-empted by CC-084 —
  close honestly (DOCS §6 rows 1, 11).
- Deliberate non-findings: `CLAUDE.md` absence and `docs/precepts/` submodule are witnessed
  doctrine, not drift (DOCS §2, §7.5).

---

## (a) CANDIDATE WAVE-SHAPE SKETCH (names + one-line scope; NOT specs)

1. **KF.W0 · Substrate Settle** — owner-hand §B-12 reconciliation of the sacred checkout;
   re-anchor every lane finding at `origin/master`; re-run the drifted counts (SCH-3..7); the
   opening act keyframes' own W13 fold prescribes.
2. **KF.W1 · Mail Cure** — the O-8/O-11 amendment-addendum (HEAD anchors, the 3 missing
   `parseStylesheet` sites, the closed vehicle question, the D-GAP-6 resolution) delivered to an
   exec-visible path; row the mis-delivery in value's INBOX as a self-defect.
3. **KF.W2 · Parse Façade** — one kf-side `parse()` façade collapsing the 5 failure postures over
   the 13 Tier-A sites; delete the 6 Tier-C regexes incl. the cssom.ts identifier-injection; the
   pre-work that makes parser consumption one-touch.
4. **KF.W3 · Parser Consumption (GATED)** — keyed to the parse-that lane's release condition
   (PLAW-BIND → V.L1/V.L5 → packed Value release): repin 4.0.0→4.1.x, retire the
   `serializeCssValue` fork, unify DUAL-1, acknowledge the 8-curve delta; born-RED against the
   14-fixture round-trip corpus + the R1 demo crash surface.
5. **KF.W4 · K-Quartet Hygiene** — K1 stable easing identities, K2 tautology-spec delete +
   docstring cures, K3 `noUnusedLocals: true`, K4 provenance gate (G-L7a–d, all born-RED).
6. **KF.W5 · D/L/C Tri-Fold Library Audit** — per-component D/L/C over the 14 zones / 153+
   modules with the two-assume-WRONG-readers + adjudicator jury, iterated to dirigibility; kills
   the 17 type-rings, 9 god modules, presets shim ladder, name stutters, depcruise L-1/L-2.
7. **KF.W6 · Glass Suffusion (demo)** — S-1 replace + F-5 shim deletes + S-5/S-6/S-7 partials +
   the `--kf-*` token-namespace audit; suffusion raised choosily from 21/73.
8. **KF.W7 · Timeline Evaluate** — the 666-line timeline cluster + scrubber vs the glass
   `/timeline` family; evaluate-not-mechanical-swap, its own spec.
9. **KF.W8 · Structure & Colocation Settle** — the `src/animation` single-child flatten decision,
   test-tree isomorphism (adopt the two orphaned bench measure-tests), colocation idiom ratified.
10. **KF.W9 · Safari Visual Audit** — demo visually audited Safari mobile+desktop (the horizon's
    named condition); PRM mechanism unification.
11. **KF.W10 · Fold Discharge & Close** — FOLD-FORWARD §B's 15 rows terminal-dispositioned (W9
    staging landed-or-killed, OD-V3/OD-V5 ruled by the owner), coordination terminalized;
    re-booking forbidden.

## (b) TOP 10 DEFECTS/RISKS BY SEVERITY (with provenance)

1. **Substrate schism** — two of three lanes censused a tree 41 commits stale; 4+ anchors dead,
   counts off, one RED falsified; every spec must re-anchor at `origin/master` (DOCS §0; AGG §0).
2. **O-8/O-11 mis-delivery** — value's two live obligation packets untracked in the stale sacred
   checkout, invisible to any executor, unanswered 10–16 days, and measured against the stale tree
   (DOCS §0, §6 rows 1/15/17).
3. **R1 crash surface + parser totality** — live `parseCssColor(css)` in the kf demo
   (`useSquareTumble.ts:22`) atop value's 324-throw/one-root (`grammar.ts:181`) parser; the
   consumption wave's reason-for-being (LIB §4.6; DOCS §3.3; megatranche R1).
4. **Five failure postures on one seam** — the same value parse failure absorbed, thrown (×2
   shapes), or swallowed depending on site; a value diagnostic-shape change touches all five
   independently (LIB §7.5).
5. **CSSOM identifier→RegExp injection + VJ-9 OPEN** — unescaped CSSOM-supplied name interpolated
   into a RegExp at `cssom.ts:214-216`; the file's own comment declares the partial-input handoff
   unresolved (LIB §4.2).
6. **Serializer duals** — DUAL-1 two live `reverseAnimationShorthand`s + the diverged
   `serializeCssValue` fork (verified at HEAD): the round-trip's emit half moves out of lockstep
   with any value parser change until the 4.1 retirement (LIB §7.4; DOCS §6 row 7).
7. **K1 easing reference instability** — 21/40 unstable references, 9 collisions, a `.find()`
   reverse-map over them, under a load-bearing false docstring; corrupts serializer identity today
   (DOCS §6 row 3).
8. **8-curve shape change at the bump** — max|Δ| 0.192 on `ease-out-circ`, landing directly in
   kf's boot-evaluated registry, wholly unacknowledged by kf (DOCS §6 row 9; §3.3).
9. **Depcruise guard decay** — 4 dead LIGHT-allowlist paths = the value.js firewall's lint
   silently unguarded on four modules; plus the phantom no-cycle baseline (LIB §3.5; HEAD
   re-verification pending, SCH-7).
10. **KfPillTabs + shim hygiene** — a 217-line fork whose blocking rationale is 3 majors void,
    plus one dead and one incoherent back-compat shim, all surviving at HEAD (FE §5 S-1, §7.3;
    AGG-P2) — the sharpest glass-suffusion and no-legacy violations in the demo.

## (c) THE ADMISSION GAP

The commission admits keyframes on: **full wave specs with born-RED gates + the
parser-consumption wave keyed to the parse-that lane's release condition.** What stands between
this census and that bar:

1. **No wave specs exist.** This census is inventory + sketch; each of the ~11 sketched waves
   needs a full spec (scope, invariants, born-RED gate definition, verify protocol) per
   TRANCHE-AND-WAVE-SPEC.
2. **The substrate is not settled.** Until §B-12 reconciliation (owner's hand — value.js may NOT
   perform it) and a HEAD re-anchor pass, no spec can cite file:line honestly; SCH-3..7 quantify
   the drift.
3. **The parser-consumption key is not yet definable.** The parse-that lane is greenfield
   (OPEN_UNPROVEN hypothesis); its release condition — the routing law's packed-Value release —
   has no date or ratified gate text yet. KF.W3 can be specced only as *gate-keyed* (condition
   named, trigger external), never scheduled.
4. **D/L/C requires a component registry that does not exist.** LIB gives a module roster, FE a
   component roster; neither carries D/L/C scores. The registry (153+ src modules + 58 demo
   components), scoring rubric, and per-component tri-fold jury instantiation (two assume-WRONG
   readers + adjudicator, M-12 seats, two dry passes to convergence) must be authored.
5. **Write authority is unresolved.** keyframes.js is sibling-read-only under value.js law and
   the sacred-checkout rule; K1–K4 and every kf-side wave have no owning formation on the kf side
   today (their V folded into this vnext). The specs must name WHERE kf waves execute (the exec
   clone / a new kf tranche letter) and under whose hand.
6. **The mail cure precedes everything kf-side.** O-8/O-11 amendment-addendum (gap §0.5) must
   land exec-visible before any kf wave can consume its obligations; the D-GAP-6 and vehicle-
   question rows need their one-line honest closures.
7. **Glass-suffusion scope needs HEAD re-verification.** The shadow census (S-1..S-8) was measured
   pre-W2-slice; S-1 is confirmed surviving (AGG-P2) but per-file line counts and the S-3/S-4/S-5
   evaluate targets must be re-probed at HEAD before replacement specs are cut.
8. **Born-RED gate inputs need mapping to HEAD paths.** The 14-fixture corpus and 15 parser-facing
   test modules exist, but W5's carves moved consumers (SCH-5); each gate's RED input must name a
   HEAD-resolved path and a currently-failing assertion.

---

## ADDENDUM 2026-08-03 — Codex intake corpus folded (B10/B18/B19/B20/B21)

Served model id: `claude-fable-5` (M-23 intake-adjudication master seat). Source:
`audit/codex-provenance/intakes/lane-keyframes-b10-b21.md` (112 claims), folded per M-21 into
`audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md`. Tally: **85 adopted · 21 carried ·
5 refuted · 1 owner-gated**. The census body above is dated and unrewritten; this section amends it.

### What the corpus IS (scope law, adopted)

The B10–B21 lineage is a **reviewer-harness audit, not product coverage** (X-5: zero overlap with
this census's top-10 defects — no parser seam, no glass suffusion, no K1–K4, no serializer dual).
Its zero-authority self-declaration is accurate (X-6). It must never be cited as keyframes product
evidence, and its terminal-stop language is not load-bearing (X-7: "no successor follows" was
falsified by its own successors within hours, twice).

### Adopted facts that amend the census

1. **The schism has a fourth victim (X-1, verified live at fold).** The entire Codex "Keyframes v8"
   contract lineage is pinned to the sacred stale checkout `8281638c` — 41 commits behind
   `origin/master` `81a56990`. Every v8 denominator (414 contracts, 152 surfaces, 73,568 Safari
   cells, 184 demo members) is measured on the tree §0 already ruled non-authoritative. Extends
   SCH-1..7; the §B-12 reconciliation (KF.W0) must also disposition the v8 contract — re-anchor it
   or declare it stale-by-substrate, never citable for a live count.
2. **§1 "demo scale" row localized (X-2).** The 184 `.ts`/`.vue` figure (LIB = Codex, exact
   agreement) is the **stale-checkout working-tree** figure; tracked at `origin/master` = **185**.
   The agreement proves both censused the same stale worktree. KF.W0's re-count adopts 185-at-HEAD.
3. **58 `.vue` third-confirmed (X-3)** — Codex, census (AGG-P2/SCH-6), and live tree agree; the one
   Codex component count that survives the schism unchanged.
4. **Three CONFIRMED harness falsifiers become transferable gate-design law** (each verified to the
   line by the lane): B18-16 (a finalizer rewrote authenticated NODE/TREE bytes outside the
   authorized path — the gate failed for a reason its oracle never authorized), B18-20 (receipt
   validation checked envelope+count only, never per-receipt provenance — 5 MB of receipts never
   re-derived), B21-9..11 (a hand-listed 5-key envelope law vs the authenticated 8-key artifact —
   contract defect, not runtime accident). Two runtime falsifiers (Ruby 2.6 `filter_map`; the
   Base64/UTF-8 encoding-tag rejection) reproduced live, including at fold.
5. **Safari numerator corroborated, denominator rejected (B18-26).** Zero real-Safari execution
   agrees with the horizon's unmet condition; the 73,568-cell Kronecker denominator is a
   self-cross-product with no product consumer and is **rejected** — KF.W9 scopes its own list.
6. **The 357/414 seal is dead within Codex's own corpus (B18-23).** Superseded to
   `345 exact / 12 partial / 57 unresolved @ 8281638c`; the sole citable form, and never as HEAD
   coverage.

### Carried rows (by owning wave)

| wave | rows |
|---|---|
| **KF.W0** | X-1 (v8-contract re-anchor/stale-declare, joins §B-12) · B10-9, B19-10/11, B20-6, B21-7 (TCC re-reads, owed only if the v8 contract is ever cited) |
| **KF.W4** | B10-17/18/19/20/22/23 + B10-27 + B18-19 + B21-17 — fold into G-L7a–d as born-RED gate-authoring law: (a) one immutable map keyed by physical coordinate; (b) reauthenticate every payload node/checksum row at use; (c) read every declared predecessor authority, never allowlist; (d) derive envelope assertions from the pinned artifact; a gate may not re-derive its own oracle |
| **KF.W5** | X-4 (reconcile the 80/139/153/"159" counting bases — the SCH-3 drift class, now with a fourth basis) · B10-21 (control-coverage ratio as the shape of per-component coverage) |
| **KF.W9** | B18-26 (numerator inherited; denominator rejected; own scoped surface list) |
| **KF.W10** | B18-12/13/14 (three external coordinates no artifact reproduces — cannot close rows) · B18-23+B19-14+B20-14 (the 357/414 boundary block, one disposition) · B18-27 (slot 4-vs-5, settle or drop) |

### Wave-sketch adjustments forced

- **KF.W0** gains one work item: disposition the Codex v8 contract pin alongside §B-12 (adopted
  fact 1); adopt 185 as the HEAD demo count (fact 2).
- **KF.W4**'s G-L7a–d gate-authoring rules are extended with the corpus law (carried table above) —
  the only place Codex evidence changes how keyframes waves are *built* rather than what they audit.
- **KF.W5** adds the counting-basis reconciliation row; **KF.W9** adds the explicit
  denominator-rejection line; **KF.W10** adds the three terminal dispositions.
- **One owner ruling joins KF.W10's owner block (OG-KF1, from B21-18):** does the Codex B-lineage
  continue at all, and must any continuation re-anchor to `origin/master` `81a56990` before its
  denominators may be cited? Sits beside OD-V3/OD-V5; value.js cannot answer it.

Nothing else in the census body changes: the corpus touches no product finding, confirms no product
finding, and threatens no product finding — its contribution here is substrate localization plus
gate-authoring law, with everything else dying or gating exactly as the master ledger records.
