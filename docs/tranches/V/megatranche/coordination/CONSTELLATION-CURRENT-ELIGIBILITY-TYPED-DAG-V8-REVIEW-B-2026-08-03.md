# Current-eligibility typed DAG v8 — independent Review B

Date: 2026-08-03  
Verdict: **CLEAN / no material falsifier**  
Scope: governing topology, current facts, ordering, zero-authority law  
Mode: tranche development only  
Authority and credit: `0`

## Packet authentication

Review B independently reproduced the 9-file / 126,373-byte census, checksum
replay 8/8, checksum SHA
`b696d4f96103077838ec590b7f1b74d4dc28b3ef88459986e49a89595ef60e64`,
and corrected full packet identity
`540c17f0507fb804bb08e0bb4737978ea68624e21f3f5729a7579a393b637f70`.
All 16 external pins authenticated. Baseline validation returned no findings
and exit `0`. No file changed.

## Governing graph

The packet exact-binds 12 ordered nodes, 12 ordered positive edges, four
ordered forbidden patterns, and 12 required reachability pairs. The four
native owners alone have direct edges to `candidate:source`. The paused parser
receiver remains separate and has direct edges to `release:value` and
`close:constellation`; it has no direct edge to `candidate:source` or
`owner:fourier`. Fourier is reached from the parser only through
`release:value`. No self-loop, directed cycle, or downstream return exists.

Candidate order remains source -> seal -> Clean A -> owner intake -> genuinely
later Clean B -> close, with every stage absent.

## Current facts and evidence law

The exact Value facts are reports `88/88`, D/L/C `264/264`, owner controls
`0/44`, non-owner retentions `0/1,892`, package exports `7/8`, and admitted API
classes `0/9`. All Keyframes, Fourier, Glass, auxiliary, pass, root, and parser
facts exact-match their pins, including pass cells `0/60`, native inputs `0/4`,
parser receiver `0/1`, strict root `0/5`, and parser `14/34 PAUSED`.

The unsatisfied owner-input evidence law exact-binds 12 owner-receipt fields,
19 raw-process fields, five direct-call fields, production hostiles, owning
predicate suppression, every non-owner retention, and unknown/duplicate
suppression rejection. It records those obligations; it does not claim they
have run.

The 11-domain zero-authority vector is exact. Review B is CLEAN for source
semantics only.

