# Constellation frontend pre-execution source contract v5 — 2026-08-03

Status: `PRE_HOSTILE_SOURCE_CONTRACT / ZERO_AUTHORITY`

Mode: tranche development only

Execution and credit: 0

## What v5 changes

V5 is a fully materialized machine law. It does not patch, merge, retain, or
import any predecessor schema. V4 remains terminal RED at Markdown
`429dff41…`, JSON `24a7cf65…`, effective law `1e3af794…`, hostiles
`e67d97e3…` / `e567aa37…` / `b3503486…`, and adjudication `29f27401…`.

The machine file now carries every domain, record field list, cardinality,
foreign key, coverage bijection, process receipt, control law, and terminal
membership rule in its own bytes.

## Signed forward chronology

Every key record contains resolvable public-key bytes, role, validity interval,
lineage, and revocation entry. Root owner, coordinator, capture author, source
author, and both reviewers are distinct by key ID and public-key bytes. Every
signature has a literal artifact-specific domain and is checked at its issuance
time.

The chronology is strictly forward:

1. accepted common law;
2. packet-specific trust anchor;
3. root-signed capture ruling;
4. capture-author-signed raw capture;
5. separately authored and signed source bundle;
6. two independent signed source reviews;
7. root-signed source-bundle acceptance;
8. root-signed packet authorization;
9. later authorization checksum;
10. later coordinator acknowledgement;
11. independent dispatch-time absence proof under an exclusive parent lease;
12. later root dispatch;
13. atomic absent-root creation by the pinned construction controller;
14. packet copy, inner freeze, source validation and controls;
15. outer seal, then an outside-root signed owner stop and terminal tuple.

No artifact contains its own hash or a later hash. The absence auditor and
packet writer are distinct. The construction controller
must atomically create the exact absent writer root and write the construction
guard first; an earlier node or competing writer is fatal.

## Capture before interpretation

A separately pinned repository-seed denominator precedes the universe oracle.
The capture retains raw oracle and capture stdin/stdout/stderr, exact Git
HEAD/tree/object membership, raw porcelain, dirty tracked and untracked bytes,
peer receipts, and full embedded file bytes. Consumer, dependency, package, and
fixture traversal continues to a fixed point. Every seed is included or has an
independent exclusion witness. Blanket build/dist/generated/installed/package
or `node_modules` exclusions are forbidden.

## Preauthorized source bundle

The principal v4 omission is closed by a source bundle created after capture
but before packet authorization. Its exact artifact registry binds every
semantic declaration, schema, program, fixture, oracle, control plan, byte,
producer lineage, input, dependency, consumer, mode, nlink, hash, and root.

The source author differs from capture, both reviewers, and the packet writer.
Two reviewers use independent programs and no shared answer key. Only two clean
reviews permit the root owner to accept the exact source bundle. Packet
authorization then binds that immutable bundle. The later packet writer may
copy and seal; it may not generate a semantic declaration.

## Topology and complete subjects

The contract closes route, export, import, style, token, copy, asset, reverse
consumer, mount, dynamic, Teleport/portal, alias, API, storage, authorization,
fixture, and error/fallback graphs. Dynamic groups bind closed members and
predicates. Teleports bind source, target, open state, stacking, focus, scroll,
and cleanup. Every page, mounted instance, unmounted public harness, admin,
debug, error, and embedded visual runtime becomes one subject or explicit RED.
Page evidence never closes child subjects.

## Kronecker state and expectation closure

The twelve axes, eighteen state kinds, seven timepoints, fourteen expectation
kinds, and two future real-Safari platform classes are literal closed domains.
Applicability and state disposition are separate. Every required state case
joins a state vector and exact applicability tuple. For every state case,
all seven timepoints have exactly fourteen expectation dispositions.
Required expectations have typed values and independent oracles; authenticated
N/A and OPEN RED remain explicit.

Cell identity binds subject, all twelve axes, state disposition, state case,
and platform. Every required applicability tuple and compatible state kind has
one state case per platform; each state case already contains that platform, so
the cell join does not multiply it again. Each cell references exactly 98
expectation products. Source-stage execution remains
`NOT_EXECUTED`.

## Design and independent critique

Every subject has one D1 typed delta and one independent D2. D1 projections
exclude IDs and must be nontrivial. Equal D1 semantic projections require a
design-equivalence record. D2 must differ from D1, source construction, and
candidate generation by key, task, and root; it must carry subject-specific
field findings, evidence, and counterfactuals. Identical D2 projections across
non-equivalent subjects are fatal. A candidate generator cannot author its only
oracle.

## Concrete source controls

Controls start from independent exact inner clones. A pinned runner applies
concrete before/after bytes, records target ranges, checks masked non-target
equality, runs the same production source validator, embeds raw streams, binds
clone roots and residue, requires the exact derived owning leaf, retains every
non-owner finding, and rejects unknown/duplicate/reordered suppressions.
Distinct controls attack templated D1 and templated D2. Caller expected codes
and field-presence oracles cannot decide a verdict.

## Boundary

V5 requires three fresh independent hostile reviews and a separate owner
adjudication. Acceptance would establish only a common source-contract law.
Every repository still needs a separate trust/capture/source-bundle/packet
ruling. Native inputs remain `0/4`; parser receiver remains `0/1 PAUSED`;
strict root policy remains `0/5`. No product, Browser, Safari, API, Docker,
package, release, rebind, candidate, slot, or credit is authorized.
