# Fourier R6 terminal source RED — integration audit

**Date:** 2026-08-02

**Verdict:** `CLEAN_INTEGRATION / TERMINAL_RED_PRESERVED`

**Authority and credit:** `NONE / 0`

## Independent replay

The frozen R6 root replays as 484 regular files / 61 child directories /
7,846,757 file bytes, with zero links or special nodes. Files are mode `0644`,
nlink 1; directories are mode `0755`. Diagnostic identities are
`46493d36…` for the full tree and `957be47b…` for regular files. Terminal
receipt `09dfb634…` is the newest regular file.

Snapshot manifest and closure independently agree on identity `dad9f80f…`,
332 members, 386 nodes, and zero repository reads after closure. The native
loop results close all 11 rows, including C17 and 10 structural hostiles; the
baseline contains three authenticated PaperSidebar native-loop rows. This is
a real advance over R5 rather than a denominator relabel.

Exactly 31 plan/raw/receipt triplets exist. C01-C30 close. C31 preserves input
root identity before/after, but its normal run rejects for two changed leaves,
owner bypass retains the operation leaf, and the nonowner sweep is not
isolated. C32-C37 and all later seal/publication/hostile artifacts are absent.
No R6 command was rerun during integration.

The decision also requires an immediate-before-first-write absent-root proof.
No such receipt exists in the frozen coordinate, so historical construction
provenance remains unauthenticated. C31 is consequently the first persisted
executable control RED, not proof that every earlier admission precondition
closed.

## Integration result

R6 supersedes R5 only for persisted snapshot mechanism evidence,
native-template-loop closure, and controls C17-C30. Its missing pre-write proof
and C31 failure make it terminal, unsealed, unadmitted source archaeology. No
hostile, owner acceptance, pass, clean audit, slot, product action,
real-platform execution, release, or credit follows. Any successor requires a
fresh explicit non-overlapping owner ruling.
