# Constellation next source-wave ruling — 2026-08-02

Status: `AUTHORIZED_SOURCE_ONLY / EXECUTION_CLOSED / PARSER_PAUSED`  
Authority: none  
Product, package, Browser, Safari, Simulator, API, Docker, release, rebind, and constellation credit: 0

## 1. Purpose

The auxiliary successor wave has produced enough independent negative evidence
to move laterally. No RED packet is repaired in place. This ruling opens three
non-overlapping source-only coordinates so the remaining Value, Keyframes, and
Fourier audits can advance while the auxiliary hostile pair finishes and Glass
continues under its independent owner.

This is tranche development only. It does not authorize product imports,
application or package execution, builds, tests, Browser, real Safari, iOS
Simulator, APIs, Docker, dependency installation, repository mutation, git or
index mutation, candidate generation, release, or rebind. Parser and CSS work
remain paused under the separate parser handoff.

## 2. Immutable predecessor boundaries

### Value mobile/Safari v5

Preserve the exact two-file v5 root at
`/Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/mobile-safari-source-closure-v5-source`:

- `preflight-value-mobile-v5.mjs` SHA
  `33bf743458517b53f469d35684c994e6d0a449b002a59c32a53929fdb53d8421`;
- `SOURCE-READY.md` SHA
  `c176fe28af0df1302b576b4c6b2c5d75db9e1bbdd3b05dfdcbb0d8f15677f89d`;
- exact two regular files / 530,529 bytes / identity `3356b6b3…`.

V5 is terminal source RED. Its incontestable first defect is
`DISCONNECTED_PRODUCTION_CONTROLS`: 54 controls are metadata descriptions, no
production validator consumes their mutations, and concrete hostile closure is
0/54. Silent duplicate/unresolved continues and incomplete pane-dynamic,
`:is`, and teleport traversal also remain open. V5 grants no successor credit.

### Keyframes B10

An externally owned B10 coordinate already exists and must not be duplicated or
rewritten:

`/Users/mkbabb/Documents/Codex/2026-08-02/keyframes-v8-review-b10-source/outputs`

- `preflight-b10-source.mjs` SHA
  `f70f4fcf9966dcd06a2b69520eaad8c6c971eebdafe324d4f3691acd5e5c6458`,
  41,067 bytes;
- `SOURCE-READY.md` SHA
  `4452149310434fa60e7d7fee9b915dd553d860a205b3f81d68e7ba5494984931`,
  23,256 bytes.

It declares `SOURCE_READY_UNEXECUTED`, authority none, slot null, and owner
release required. This ruling authorizes a static hostile reading of those
source bytes only. It does not authorize importing or running the source,
Review B, candidate execution, or product work.

### Fourier mobile/Safari R2

Preserve the exact six-file R2 packet at
`/Users/mkbabb/Documents/Codex/2026-08-02/fourier-mobile-safari-instance-source-plan-r2/outputs`:

- exact six regular files / 87,487 bytes;
- checksums SHA
  `a780cb72a8ebc2bf2a3e79af5e98fe59d56e11b1f1a660cee1a8346962a7ea31`,
  replay 5/5;
- mode-token-644 identity
  `948bf43011c7d0e22eaa252c25d400cae36b84383e95fbe79731be535970d130`.

R2 is terminal source RED. Its controls delete or alter authenticated members
before the declared resolver leaf, include an inert synthetic ambiguity field,
accept a caller-supplied derive function, and execute 0/24 controls. Its
66-workflow, nine-Tooltip-edge, and 35-callsite observations remain
zero-credit archaeology.

## 3. Authorized source-only coordinates

The following parent roots were absent in a direct pre-ruling census. Each lane
has one sole writer and must fail closed if its root is unexpectedly present
before the writer begins.

| Lane | Sole writer root | Exact output contract |
|---|---|---|
| Value v6 source successor | `/Users/mkbabb/Documents/Codex/2026-08-02/value-mobile-safari-source-closure-v6/outputs/**` | `SOURCE-PINS-AND-CENSUS.json`, `INSTANCE-STATE-REGISTRY.json`, `AUDITOR.mjs`, `REPORT.md`, `checksums.sha256` |
| Keyframes B10 hostile A | `/Users/mkbabb/Documents/Codex/2026-08-02/keyframes-b10-static-hostile-a/outputs/**` | `REVIEW.md`, `FINDINGS.json`, `checksums.sha256` |
| Fourier R3 source successor | `/Users/mkbabb/Documents/Codex/2026-08-02/fourier-mobile-safari-instance-source-plan-r3/outputs/**` | `SOURCE-PINS-AND-CENSUS.json`, `MODULE-RESOLUTION.json`, `INSTANCE-STATE-REGISTRY.json`, `API-VISIBLE-STATE-REGISTRY.json`, `AUDITOR.mjs`, `REPORT.md`, `checksums.sha256` |

No author may write outside its exact root. All predecessor packets, product
repositories, worktrees, packages, caches, services, databases, browser state,
and git/index state remain read-only.

## 4. Value v6 contract

V6 must replace the disconnected v5 control architecture and close the exact
source-instance denominator before any Apple execution:

1. authenticate every raw source member by bytes and hash before parsing;
2. enumerate every route, wildcard, page, layer, pane, component instance,
   dynamic `:is` target, teleport, conditional mount, duplicate mount, Admin,
   debug, error, empty, loading, offline, permission, and unmounted harness;
3. carry stable repository-qualified instance IDs, source locations,
   occurrence, route and mount paths, predicate digests, canonical props,
   events, slots, defaults, copy/action semantics, container owner, and API or
   storage owner where applicable;
4. declare per-instance state/profile applicability with authenticated N/A
   witnesses; silence remains OPEN;
5. materialize a versioned, completely specified, independently reproducible
   identity and order law;
6. define concrete raw production-consumed mutants over the same validator,
   owning-leaf bypass, every nonowner retention, unknown/duplicate/erasure
   refusal, and raw mutation/result receipts;
7. author one subject-specific D1 brief per exact family or instance, keeping
   independent D2 at zero;
8. exact-compare one machine declaration to the report and stop at
   `SOURCE_READY_ZERO_AUTHORITY`.

The author may use static text, AST, hash, and census tooling over read-only
source. The author must not import or execute Value product modules or run the
auditor it writes.

## 5. Keyframes B10 hostile contract

The hostile reader must form an independent static hypothesis before reading
B10's success narrative and test at least:

1. exact two-file census, byte stability, declaration/prose agreement, import
   surface, and forbidden-action claims;
2. whether the same physical candidate files are captured once by physical
   coordinate or independently recaptured through descriptor aliases;
3. whether every sealed payload file is reauthenticated against the actual
   filesystem rather than trusting checksum/node rows;
4. raw JSON duplicate, membership, descriptor, type, case, width, authority,
   node/tree, git, and preservation leaves;
5. whether each concrete mutant reaches exactly one production-consumed owner,
   owner suppression closes it, and every nonowner preserves it without an
   expected-code oracle;
6. stale input pins, dead helpers, caller-derived summaries, coherent reseal,
   capture-time races, unbound fields, and false pass-by-gating paths.

It must return the first exact falsifier or `null`. It may not run B10, Review
B, the candidate validator, product code, or package code.

## 6. Fourier R3 contract

R3 must replace extension-matching and caller-derived controls with a complete
static module/instance/API-visible-state contract:

1. authenticate every causal Vue/TS/CSS/router/client/OpenAPI source member;
2. resolve extensionless imports, barrels, re-exports, aliases, dynamic
   imports, async components, template tags, teleports, and finite dynamic
   families from source;
3. emit one stable subject for every mounted instance, including duplicate
   Tooltip mounts, full route-to-instance path, mount predicate, props, events,
   slots, defaults, copy/action semantics, and container owner;
4. disposition all reachable and unmounted workflows, missing 404/chunk/error
   surfaces, nine client gaps, and three absent-or-prune API subjects without
   guessing;
5. bind all 45 mounted operations and OpenAPI auth/security/response classes to
   visible state owners or explicit RED gaps; OpenAPI security remains 0/45
   until source says otherwise;
6. specify exact per-subject applicability, performance thresholds, D1 owner,
   and later D2 receiver while keeping those denominators zero;
7. implement concrete source-consumed resolution/control mutations whose first
   leaf is derived by the same auditor, never by caller callbacks or inert
   metadata;
8. exact-compare one machine declaration to prose and stop at
   `SOURCE_READY_ZERO_AUTHORITY` with the corrected denominator still OPEN if
   any source family remains unresolved.

P29 remains 0/137 COMPLETE and Fourier runtime provenance remains unresolved;
neither blocks this source census and neither receives credit from it.

## 7. Stop and intake law

Every output is source evidence only. A packet with one material falsifier is
terminal at its coordinate and must not be repaired there. A source author does
not authorize its own hostile. A clean author return still requires two fresh
independent source hostiles and a repository-owner intake before it can populate
any cross-repository slot.

The five immutable cross-repository input slots remain null. Candidate
generation, rebase, seal, Clean A, owner intake, Clean B, global audits, real
Apple execution, product remedies, and release remain closed.
