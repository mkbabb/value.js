# Parser and CSS research pause handoff — 2026-08-02

Status: `PAUSED_RESEARCH / TERMINAL_SOURCE_RED / NO_ACTIVE_WRITER`
Mode: tranche development only
Authority: none
Parser, product, package, release, and rebind credit: 0

## 1. Owner ruling

Pause `parse-that` and Value CSS-parser development at the exact source boundary
below. This is a deliberate research stop, not a claim that the parser is
complete and not permission to discard its archaeology. Non-parser repository
audits continue independently.

Do not execute, import, materialize, repair, clean, reseal, release, or rebind
the parser from this packet. A later owner may revive the work only through the
resume protocol in section 8.

## 2. Standing ownership and routing law

- `parse-that` owns the generic scannerless runtime.
- Value owns the CSS grammar, CSS recovery, CSS consumers, and UI.
- Keyframes is a typed consumer, not a grammar owner.
- Fourier may receive parser effects only through an admitted, packed Value
  release.
- Direct `parse-that -> Fourier` credit is forbidden.
- Research evidence is not product authority. Source review is not parser
  execution. A replay is not release.

The only lawful receiver path remains:

```text
parser law or durable no-runtime ruling
  -> Value PLAW-BIND
  -> V.L1 / V.L5
  -> Value release and rebind
  -> packed Value
  -> Fourier F.W0 atomic tuple
  -> F.W1
```

## 3. Exact pause coordinates

### 3.1 Value parser-law receiver

| Artifact | Exact identity | Ruling |
|---|---|---|
| Value parser-law matrix | `/Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/VALUE-PARSER-LAW-CONVERGENCE-MATRIX-2026-07-30.md` | SHA-256 `244c448a90059002be1194e6a512191c96881a9413ceb021f3b8fe7b62b504a7`; `14/34 = 41.18%` formation chronology |
| Open parser-law cells | `P01-P09`, `C01-C08`, `Q01-Q03` | `20/34 = 58.82%` remain |
| Strict 3x family | five-cell law family | `0/5`; OPEN, paused |
| Strict 2x family | five-cell law family | `0/5`; OPEN, paused |
| Break-even family | five-cell law family | `0/5`; OPEN, paused |

Historical `10x`, P1-P6, and P6-A3 evidence remains immutable archaeology. It
does not satisfy the amended strict-3x, strict-2x, or break-even laws.

### 3.2 Current novelty-v12 construction source

| Fact | Exact value |
|---|---|
| Construction root | `/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-v12-construction` |
| Builder | `BUILD-V12.py` |
| Builder SHA-256 | `0732ebc27bc712d64d0b6ade30db13b9d8c7817524268af7d3d7474654b64ee8` |
| Builder identity | 243,827 bytes; mode 0644; nlink 1 |
| Preserved residue | `__pycache__/BUILD-V12.cpython-314.pyc` |
| Residue SHA-256 | `de1d62ff18da4851968136b1e3190c00f6c463b9b8a5e7b5d23103155cc00937` |
| Residue identity | 154,221 bytes; mode 0644; nlink 1; creator unknown |
| Intended target | `/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-and-experiment-v12` |
| Target at pause | **ABSENT** |

The residue is evidence. Do not remove, regenerate, normalize, or credit it.
The current builder is frozen at this pause boundary and must not be edited or
run in place.

The last zero-write owner dry audit reported these generated identities:

| Generated domain | Owner-reported SHA-256 / count |
|---|---|
| contract | `993a05831a4f91232f7a9fdeeb83c5e4bbe90b47a5dc56626c45de175b6733b4` |
| Markdown | `cfc305701b42059bea69d46c6c46b303093c705f5b9838f7d4df10f2f19d88af` |
| matrix | `f8cccb6d9953f64d1c1477e7740a4954afc334cc492a9ecdcf48ffa9c3058877` |
| predecessor registry | `cc8931121916611c22ec744a221a3512f8172707f3354e9e889bdc88cd61feb1` |
| generated source | `26757d48078ebe09093dbd2a0cb350373efaec6df21ed742edb1df46b2b2a209` |
| source size | 1,965,705 bytes |
| receipts / join edges / predicates | 14 / 399 / 167 |
| candidate / comparator / oracle calls | 522 / 160 / 137 |
| writes | 0 |

These are dry source-construction observations only. The absent target means no
generated packet, parser run, experiment, review, or acceptance exists.

### 3.3 Value CSS DREI boundary

Value CSS DREI-v11 is terminal `AMEND / RED`:

- 188 controls: 183 parsed and five raw;
- 376 alternates and 35,156 ordered non-owner pairs;
- parsed structural authenticity `183/183 = 100%`;
- production semantic authority `0/188`;
- control-of-control authority `0/188`;
- alternates `0/376`;
- non-owner retentions `0/35,156`;
- raw authority `0/5`;
- fresh clean reviews `0/2`.

The authoritative Value owner intake is:

`/Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/audit/cross-repo/VALUE-CSS-DREI-V11-TWO-REVIEW-OWNER-INTAKE-2026-08-02.md`

SHA-256:
`aa891714b3b6bb3386afda45201b203ac5aa1f2ef028466f303831197e992767`.

No CSS DREI-v12 coordinate is authorized by this handoff.

## 4. Novelty ruling

Broad novelty claims for staging, parser fusion, Wasm region memoization,
semantic actions, recovery, and inversion are closed as prior-art overlap. The
only retained hypothesis is narrower and remains `OPEN_UNPROVEN`:

> A finite, independently authenticated Value-CSS semantic and recovery algebra
> can compile to both source-direct JavaScript and zero-function-import Wasm,
> without a CST, while preserving exact equality of semantic value, byte
> complement, provenance, diagnostics, rollback, and malformed inverse.

This conjunction is a falsifiable research target, not a product promise or a
claim of novelty already won.

## 5. Terminal RED at the pause boundary

The latest builder incorporates useful repairs—exhaustive OR04/OR05 field
unions, exact run/control/fixture selectors, target versus non-target fixture
roles, target-only runtime suppressions, and explicit target-causality and
non-target-neutrality edges—but its predicate closure remains inconsistent.

The governing static falsifiers are:

1. OR05 declares 18 production predicate IDs while its required-value domain
   contains 20 formulas. The registry binds only the first 18.
2. The dropped formulas are caller/post-hoc rejection and close chronology;
   the last declared IDs are therefore misbound rather than merely absent.
3. The universal S01 runtime predicate requires a mismatch for every S01
   runtime leaf, while the non-target S01 law requires all sixteen products to
   be three-way byte-equal, with an empty finding set, exit 0, and null observed
   code. Both cannot be true for the same non-target row.
4. RR01 therefore cannot honestly aggregate role-qualified target-causality and
   non-target-neutrality results from the present predicate registry.

The earlier v12 second-pass reviews also remain terminal RED on OR04/OR05 union
closure, an undefined S01 run-kind, and detached target-fixture causality. The
current builder narrows those defects but does not supersede them with an
accepted packet.

## 6. What survives

**KEEP**

- the narrow dual-backend hypothesis;
- the independent semantic-product trust root;
- action-integrity versus coherently re-pinned semantic-mutant separation;
- exact no-CST product equality;
- raw-first observations and stage-specific unions;
- zero-function-import Wasm and closed memory/reification accounting;
- ABBA/economics/ablation separation;
- target-fixture causality and non-target baseline equality as distinct laws;
- Sol -> Luna -> fresh Sol -> owner and two later clean reviews.

**FOLD**

- v1-v11 terminal falsifiers into any future design;
- Value CSS DREI raw-authority and self-oracle failures;
- exact role-qualified target/non-target counts into RR01;
- complete predicate-ID/formula bijection and control-of-control coverage.

**PRUNE**

- broad novelty claims;
- a generic combinator-only search;
- caller expectations, self-authored answer keys, label-only controls, and
  post-hoc product sets;
- same-root repair, silent re-pin, timeout widening, or denominator rewrite;
- inferred product, parser, package, release, or rebind credit.

## 7. Pause law

While paused:

- do not run `BUILD-V12.py`;
- do not materialize the absent v12 target;
- do not import or execute generated parser/auditor source;
- do not mutate `parse-that`, Value parser/CSS product source, package state,
  tests, benchmarks, caches, or release coordinates;
- do not clean the preserved `__pycache__` residue;
- do not dispatch N4, parser-law P01, CSS DREI-v12, or a performance family;
- do not bind parser evidence into the non-parser cross-repository input slots.

Read-only hashing, census, archaeology, and handoff verification remain lawful.

## 8. Resume protocol

A later long-horizon parser session must:

1. authenticate this handoff, the current builder, the preserved residue, and
   target absence before any write;
2. receive an explicit owner ruling naming a fresh, non-overlapping writer
   root; never modify this construction root in place;
3. build an exact formula-to-predicate-ID bijection, rejecting missing, extra,
   duplicate, reordered, or misbound formulas;
4. split target S01 mismatch causality from non-target S01 neutrality into
   distinct production predicates, codes, receipts, and RR01 aggregates;
5. make every run kind, observation variant, fixture role, and expected outcome
   a closed authenticated union with no fallback;
6. prove every owner suppression, all non-owner retentions, invalid
   suppressions, and control-of-control from production-consumed raw state;
7. obtain three fresh independent static reviews over the frozen source design;
8. materialize only after a separate owner release, then run the prescribed
   Sol -> Luna -> fresh Sol -> owner sequence in isolated roots;
9. keep complete-product correctness ahead of timing and economics;
10. route any accepted result through Value's parser-law receiver before any
    consumer or Fourier claim.

## 9. Durable handoff to another harness

A future Codex or Claude Code instance must treat task IDs as chronology and
disk bytes as evidence. Recompute every hash and census before acting. Missing
bytes, a present v12 target, a changed builder, a changed residue, or a stale
owner receipt means STOP and request a new ruling.

The parser is paused because the current design is honestly RED, not because
the research is hopeless. The road forward is narrower, better specified, and
preserved.
