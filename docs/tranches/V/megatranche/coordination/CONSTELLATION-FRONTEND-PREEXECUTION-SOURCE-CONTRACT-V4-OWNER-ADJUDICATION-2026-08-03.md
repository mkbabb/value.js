# Constellation frontend pre-execution source contract v4 — owner adjudication

Date: 2026-08-03

Ruling: `V4_TERMINAL_SOURCE_RED / V5_SOURCE_CONTRACT_ONLY_AUTHORIZED`

Authority and credit: 0

## Ruling

V4 is immutable negative archaeology. Its exact Markdown, JSON, and effective
hash remain the authenticated coordinate. All three independent hostile reads
are accepted. No v4 repository packet, capture, key provisioning, execution,
slot admission, or credit is authorized.

The first complete defect domain is:

1. capture-author key IDs cannot resolve to trusted public-key bytes;
2. complete top-level replacement silently erases machine laws which prose and
   inert booleans claim to retain;
3. semantic declaration artifacts are not all owner-pinned before packet write;
4. leaf extraction, candidate lineage, timepoint products, state vectors,
   process receipts, terminal membership, universe seeds, and dispatch-time
   continuous absence are not denominator-closed.

## Bounded successor law

Exactly one v5 common source-contract construction is authorized. It must be a
fully materialized machine law, not a patch or inheritance claim. It must:

- define every schema, enum, field, cardinality, foreign key, and validation
  rule in its own bytes;
- provide resolvable role-distinct public keys, signed-artifact domains,
  validity/revocation checks, and a strictly forward chronology;
- insert a separately authored, independently reviewed, owner-pinned source
  bundle before packet authorization, with an exact artifact registry covering
  every semantic declaration/program byte;
- bind a separately pinned repository seed/search-root denominator and raw
  oracle execution receipt;
- exact-close subject/axis/state/timepoint/platform/expectation products;
- exact-close D1 deltas/equivalence and independent non-template D2 semantics;
- execute source controls on independent clones with concrete before/after
  bytes, masks, raw receipts, owning leaves, all non-owner retention, and
  refusal controls;
- define dispatch-time root absence, inner/outer/terminal membership domains,
  and a signed owner stop without self-reference.

V5 itself requires three fresh read-only hostiles and a separate owner
adjudication. No later packet, product execution, Browser/Safari/API/Docker,
package/release/rebind, candidate, slot, or credit is implied.
