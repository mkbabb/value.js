# value.js → glass-ui BK — IN-FLIGHT AUDIT SUPPLEMENT

> **SUPERSEDED 2026-07-29.** Glass's durable O19 receipt already says 31/31.
> The heading-reconciliation rider and pending post-fold wording below are
> closed historical transport notes. Current Value findings and the no-resend
> ruling live in `../CONVERGENCE-REAUDIT-2026-07-29.md`; the current peer
> boundary is `CONSTELLATION-BOUNDARY-2026-07-29.md`. Send no correction or
> repeat batch to Glass.

**Date:** 2026-07-29
**Posture:** non-interrupting, batched, zero new roster rows or gate seats
**Source:** independent value.js in-app Browser + source audit at value HEAD `d19da6d3` and glass
BK live tree
**Target:** active glass-ui BK execution after its graph-v3 fence

O-19 and its 31/31 receipt remain authoritative. This supplement does not reopen its rulings.

**Transmission:** sent once at 2026-07-29 11:03 EDT to active Codex task
`019fae33-533a-7d80-8907-00a33e555195`; transport accepted while the task remained active.
At 11:07 EDT that task acknowledged the exact queued scope and promised to fold it after graph-v3
with no new row/gate/source authority. Formal post-fold durable receipt remains pending. Do not
resend meanwhile.

## 1. Producer defects: no duplicate booking

The current Browser audit produced **no newly proven root-level glass defect**:

- value’s desktop dock collapses after five seconds because the consumer passes
  `collapse-delay=5000` and leaves the desktop dock collapsible;
- glass correctly makes the hidden full face `inert`;
- therefore the resting seal’s navigation discoverability is value-owned composition under the
  Breath-of-Life law, not a GlassDock defect;
- InstrumentChassis asks are dead with BK’s confirmed deletion; value will not clone its internals;
- Watercolor paint remains value-owned, while BK’s indicator slot/bounce row remains producer-owned;
- spectrum seam, roving dock keyboard, Slider a11y, Button attrs, Skeleton fill, and material rows
  retain their existing seats and must not be duplicated.

## 2. Two exact audit riders for existing BK seats

### BK-Dock existing keyboard/roving row

**Rider.** When the collapsed face is active, assert that every full-face descendant is inert and
unreachable in sequential focus; when focus enters the disclosure, expansion makes the full face
reachable before focus transfer. This is verification of existing behavior, not a new feature.

**Acceptance.** A keyboard-only test traverses collapsed → disclosure → expanded controls →
collapsed, with exactly one reachable face at every frame. No focus target may live outside the
visible aperture.

### BK-Watercolor indicator-slot row

**Rider.** Keep producer semantics paint-agnostic. The slot owns state, orientation, keyboard, and
the grab/settle spring; the consumer-supplied painted child owns no role or pointer handling.

**Acceptance.** Both orientations pass with a plain geometric child and a consumer watercolor
child; semantics and motion receipts are identical. No WatercolorDot/WatercolorSwatch symbol
returns to glass-ui.

## 3. Coordination law

- Fold these riders into existing rows after graph-v3; add zero roster rows and zero gate seats.
- Record one durable receipt after the current challenge/graph boundary. It may say the riders were
  already covered, but silence is not a receipt.
- Reconcile the existing O-19 receipt heading, which says `30/30`, with its terminal body, which
  says `31/31 (⊕ the §C row)`. This is documentation truth only, not a new product row.
- value.js continues tranche development and will consume the published Glass 8 contracts without
  local CSS copies, shims, aliases, or repins.
