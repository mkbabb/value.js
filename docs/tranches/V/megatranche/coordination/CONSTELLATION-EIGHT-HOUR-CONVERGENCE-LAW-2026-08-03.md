# Constellation eight-hour convergence law — 2026-08-03

Status: `ACTIVE_TIMEBOX / TRANCHE_DEVELOPMENT_ONLY`

Authority: none

Parser: `PAUSED_14_OF_34`

Product, Browser, Safari, API, Docker, package, release, rebind, candidate, and
constellation credit: `0`

## Outcome required by H+8

The eight-hour boundary closes with one checksum-resumable handoff that records
the strongest honestly converged source state for every repository in scope.
The boundary succeeds even when a lane remains RED, provided that its final
RED is exact, independently reproduced, non-overlapping, and routes one finite
remainder. It does not succeed by silently relabeling formation or source-plan
coverage as execution, platform, product, release, or slot admission.

The target sequence is:

1. accept one common pre-execution source law after three independent hostile
   readings, or freeze its final exact falsifier;
2. apply that law once to Value, Keyframes, Fourier, and the auxiliary/package
   family while consuming Glass only as a read-only external owner tuple;
3. admit only owner-accepted immutable native inputs; keep parser release a
   separate paused fifth receiver;
4. construct and hostile-audit one typed dependency DAG only after its inputs
   are immutable;
5. run Clean A, owner adjudication, and genuinely later Clean B only if all
   prerequisite source inputs and the DAG are accepted;
6. freeze checksums, convergence percentages, blockers, and restart commands
   before H+8.

## Lane budgets

| Window | Required work | Exit condition |
|---|---|---|
| H+0 to H+1 | Freeze and adjudicate the common V5 source law | Three exact hostile verdicts plus owner ruling |
| H+1 to H+4 | Parallel native and auxiliary source packets | One terminal tuple per lane; no unlimited repair chain |
| H+4 to H+6 | Owner-slot adjudication and typed DAG | Immutable accepted inputs only, or exact slot/DAG RED |
| H+6 to H+7 | Global Clean A, owner intake, later Clean B | Runs only if prerequisites are satisfied; otherwise explicit blocked gate |
| H+7 to H+8 | Checksums, matrix, handoff, cross-session receipts | Two stable snapshots, replay-green checksum packet, no fresh lineage |

## Finite repair law

- Every coordinate receives one authoring attempt and its required independent
  reviews.
- A mechanical failure may receive at most one same-law, fresh-root correction
  when the owner ruling explicitly authorizes it.
- A semantic falsifier terminates that coordinate. A successor is permitted
  only when the correction is bounded, non-overlapping, and still fits its
  lane budget.
- At H+7 no new author, reviewer, packet, successor, or execution coordinate is
  created. Remaining work is frozen and routed.
- No lane may hold the constellation open merely to improve a percentage.

## Admission law

The native owner denominator is exactly four: Value, Keyframes, Fourier, and
Glass. Parser release is a separate fifth close/release receiver and remains
paused. The strict root policy is therefore currently `0/4 + 0/1 = 0/5`.

An owner input is admissible only when all of the following are exact:

- authenticated repository-universe capture and immutable source bundle;
- complete membership, topology, dynamic-mount, alias, Teleport, reverse-
  consumer, state, applicability, D1, D2, and zero-credit declarations;
- production-consumed owner/nonowner/refusal controls with raw receipts;
- two independent clean source reviews and an owner acceptance;
- checksum, node/tree identity, chronology, and no post-seal writes.

Glass remains an independent, unblocked product lane. This coordinate may
consume its terminal owner tuple read-only but may not dispatch, pause, repair,
or claim Glass execution.

## Percentage law

Every reported percentage names its denominator and evidence class. At
minimum, formation/specification, authenticated source evidence, owner-control
closure, platform execution, product/release, native-input admission, and
strict-root convergence remain separate. `OPEN`, `PAUSED`, `RED`, and absent
denominators are never converted to zero-valued completion percentages.

## Hard stop

At H+8 the active work stops. The final handoff must name:

- exact accepted and rejected coordinates;
- current percentages by repository and subtopic;
- all slots and receiver states;
- the first remaining falsifier for every RED lane;
- the next authorized action, if any;
- proof that parser work stayed paused and product/platform execution stayed
  outside this tranche-development coordinate.

This law authorizes planning, source auditing, hostile review, owner
adjudication, checksumming, and handoff maintenance only. It grants no product,
execution, platform, package, release, rebind, or constellation authority.
