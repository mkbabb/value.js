# Frontend pre-execution source contract v1 — hostile B

Verdict: `AMEND / SOURCE_CONTRACT_RED`

Mode: independent read-only auxiliary/source-closure audit

Authority and credit: 0

## Reviewed coordinate

Hostile B independently read the same stable v1 identities:

- Markdown `edd6bf6e9be80e093e704697a6fb76256a95f17c93a4def84ea7b7d0db9b3ba1`;
- JSON `70bec13105b6042aa0d85e8e956e074bec6f02a142a154a0d3a5441f262bc6ce`.

## First material falsifier

`SOURCE_CLOSURE_ORACLE_ABSENT`.

The 17-member packet authenticates only an owner-selected metadata registry
plus owner-authored subjects, surface contracts, and topology. It contains no
full raw repository byte/tree snapshot, authenticated discovery
implementation/runtime, or independently expected membership root. Because
post-authentication filesystem rereads are forbidden, a writer can coherently
delete a CSS/token file or transitive consumer from every projection and
reseal successfully.

This reproduces the known auxiliary failure family: nine omitted styles in
the Atlas/playground/latex packet and 58 edges reaching 38 unpinned files in
the Pencil packet.

## Secondary defects

1. Applicability is unary and has neither a closed axis-member catalog nor
   cross-axis `when` predicates.
2. `semanticCoreSha256` is supplied rather than derived from a canonical D1
   projection, so trivial salt can disguise templated briefs.
3. reverse-import, mount, alias, and transitive-consumer closure are asserted,
   not independently derived.
4. source-stage controls are postponed, so the packet can be accepted without
   proving that its validator detects coherent deletion and reseal.
5. D1/D2 rows lack `authorLineageId` foreign keys.
6. UI and responsive expectations/oracles are absent as separate products.
7. source spans lack a byte-range, node-kind, and slice-digest schema.

## Smallest closure

V2 must add a content-addressed raw snapshot; independently pinned discovery
and expected-membership roots; a closed axis domain with joint tuple
predicates; canonical D1 semantic projection and subject delta; joined author
lineage; UI/UX/responsive expectations; and production-consumed source
controls for member/edge/consumer omission, alias split/merge, false `N/A`,
templated D1, reorder, replacement, and coherent reseal.

No product, Browser, Safari, API, Docker, package, parser, or release command
was run.
