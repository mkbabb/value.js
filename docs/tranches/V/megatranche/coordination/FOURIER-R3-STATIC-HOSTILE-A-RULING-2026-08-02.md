# Fourier R3 static hostile A ruling — 2026-08-02

Status: `AUTHORIZED_STATIC_SOURCE_HOSTILE_A / EXECUTION_CLOSED`  
Authority: none  
Product, API, Browser, Safari, Simulator, Docker, package, release, rebind,
P29, constellation-slot, and count credit: 0

## 1. Frozen input

This ruling consumes only the frozen source-author packet at:

`/Users/mkbabb/Documents/Codex/2026-08-02/fourier-mobile-safari-instance-source-plan-r3/outputs`

Independent read-only replay established:

- exactly seven regular files, mode `0644`, `nlink1`, zero child directories,
  symlinks, or special nodes;
- 6,563,070 total bytes;
- canonical `basename|bytes|0644|nlink|sha256\n` identity
  `0f9496d2d660a2b44dcb7b114e3fbfcc270800a2d3f0eb9052152010d0c2af33`;
- checksum file SHA
  `0d44e81f479af061596f7184c9a833e8ad24ff5d7b1349587ea4d612563c09df`,
  replay 6/6;
- `SOURCE-PINS-AND-CENSUS.json` SHA
  `e146c201eaae5aa990da063d4eeeff2df991a71c77f78d438a27fb389c92e7b0`;
- `MODULE-RESOLUTION.json` SHA
  `0ab049565ccd8809d90bd9da18d01f2fc4325c5aff9098a1cc5072720004a10d`;
- `INSTANCE-STATE-REGISTRY.json` SHA
  `5a8df9cf99f0028b42fa1a22c5ae0401f4992689673b5a2cabd477e845fbaa26`;
- `API-VISIBLE-STATE-REGISTRY.json` SHA
  `92e36a8d9e6b5e72d892856e11a692d724b4cc5281e56f1d493a44944e64e26a`;
- `AUDITOR.mjs` SHA
  `53bdfe1ae36525ced2e8ea45c5d1b1f1fd591967c0734cfb4c4197081910a9ba`;
- `REPORT.md` SHA
  `1ce82f3ae49f6a54d4ec58634ed1586f3c53118b5bda9e95dce03d18396836c3`.

The author verdict is
`SOURCE_READY_ZERO_AUTHORITY_DENOMINATOR_OPEN`. It is a claim for hostile
review, not an accepted result.

## 2. Fresh writer coordinate

The parent and output roots were absent in the immediately preceding direct
census. One independent static hostile author may write only:

`/Users/mkbabb/Documents/Codex/2026-08-02/fourier-r3-static-hostile-a/outputs/**`

The exact output contract is:

1. `REVIEW.md`;
2. `FINDINGS.json`;
3. `checksums.sha256`.

All must be regular mode-`0644`, `nlink1` files. No other node is authorized.
The frozen R3 packet, Fourier repository, every predecessor root, this
repository, peer repositories, caches, services, browser state, and git/index
state remain read-only.

## 3. Static hostile order

The hostile must form its hypothesis from the contract and raw machine files,
not from the author's success prose. It must test, in order:

1. exact packet census, byte stability, hashes, declaration/prose agreement,
   source-only chronology, and zero-credit law;
2. every predecessor, checkout, causal member, mode, link, and content pin,
   with authentication before parse or resolution;
3. whether the 189 source members are actually re-derived into the 545 module
   edges, 66 workflows, 519 template callsites, 1,376 subject records, and 45
   API operations, rather than merely authenticated beside caller-authored
   summaries;
4. extensionless imports, barrels, aliases, re-exports, dynamic imports, async
   components, template tags, six dynamic families, two Teleports, and every
   duplicate Tooltip occurrence;
5. full route-to-instance predicate, props, events, slots, defaults, copy,
   action, container, API, and storage joins, with silence and runtime
   multiplicity remaining OPEN;
6. exact disposition of the two unmounted workflows, missing 404/chunk/error
   surfaces, nine client gaps, three absent-or-prune API subjects, and
   OpenAPI security `0/45`;
7. whether the 24 controls mutate authenticated source or predicate inputs
   consumed by the same derivation path, or only authored summaries; owner
   suppression must remove only its internally derived owner, every nonowner
   suppression must retain it, and unknown, duplicate, erased, coherently
   resealed, or caller-expected controls must reject;
8. whether one independently reproducible identity/order law and one machine
   declaration are exact-compared without host-locale ordering, dead helpers,
   callbacks, caller summaries, or unbound fields.

The first material falsifier terminates this coordinate as `AMEND / SOURCE_RED`.
A null first falsifier is only `CLEAN_STATIC_A_ZERO_AUTHORITY`; it cannot admit
R3 and still requires a later, separately authorized hostile B and repository
owner intake.

## 4. Forbidden actions and stop

The hostile must not import, syntax-check, or execute `AUDITOR.mjs`; import or
execute product modules; run Node against the packet; start Fourier, API,
Docker, Browser, Safari, or Simulator; install packages; mutate any repository;
or generate a cross-repository candidate. Static text/JSON parsing, hashing,
filesystem census, and source comparison are the only permitted reads.

P29 remains `0/137 COMPLETE`, the Fourier input slot remains null, and all five
cross-repository slots remain null. Stop after the exact three-file hostile
packet and return it for root intake.
