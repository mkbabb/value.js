# TRUTH-TABLE — the constellation implementation truth (M-14 aggregated pass)

**Seat.** Fable IMPLEMENTATION-TRUTH adjudicator (M-14 aggregated pass). **Model observed:** `claude-fable-5`.
**Date** 2026-07-27 · **HEAD** `4f78e57b` (branch `tranche-u`) · master frozen at `6abef800` (2026-07-13); `git log --oneline master..tranche-u | wc -l` → **197** at this writing (the N–W seat measured 196; HEAD has advanced by the M-15/OM commits since — the drift is real and recorded, not an error in either document).

**Inputs, all read whole:** the twelve B:* seat reports under `excavation/extracts/` (value-exhortations · glass1-exhortations · glass2-exhortations · kf-fourier-exhortations · value-truth-A-M · value-truth-N-W · keyframes-truth · fourier-parsethat-truth · shadcn-library-census · design-canon-census · vnext-standing · glass-forward-compliance), plus `registry/DISEASE-REGISTRY.md` (33 DR rows + 22 FM mechanism families), `registry/ROOT-FINDINGS.md`, `STATE.md`, and the 14-file adjudicated corpus under `registry/adjudicated/`.

**Denominator honesty.** The per-tranche truth seats rowed **65 tranche units** with promise-vs-landed tables: value.js A..M (13) + N..W (10) · keyframes.js A..V (22) · fourier-analysis A..N (13) · parse-that A/B/Q/S/T/U/B′ (7). glass-ui's ~30 tranches and the slides corpus enter through the owner-voice excavations and the compliance ledger, **not** through per-tranche rows — the "~106 tranches" of the brief is covered at truth-table grain for 65 and at owner-voice grain for the rest. No row below pretends otherwise.

**Ruling discipline.** Where a row is already ruled by DISEASE-REGISTRY (DR-##), DEFECT-LEDGER, or an adjudicated apotheosis, it is **cited as corroboration, never re-argued**. Where this table *corrects* a ruled row, the correction is stated explicitly in §5. Every load-bearing seat claim used here was spot-verified this session (§4 — 17 checks, all held).

---

## §0 · The structural facts that bind every register

| # | Fact | Receipt |
|---|---|---|
| S-1 | **Neither U nor V ever merged; master is frozen at the T close (2026-07-13).** | `git log -1 master` → `6abef800 2026-07-13`; 197 commits ahead on `tranche-u` (verified this session) |
| S-2 | **Close tags stop at T.** | `git tag --list 'tranche-*'` → `tranche-r-close tranche-s-close tranche-t-close` — nothing for U or V (verified) |
| S-3 | **The e2e corpus is import-broken since V′ W43** while W43 certified "zero alias". | `grep -rln "demo/@" e2e/ | wc -l` → **7**; `ls demo/@` → No such file (verified) |
| S-4 | **The letter W is double-spent**: `docs/tranches/W/` holds the history-audit output while DISEASE-REGISTRY writes `W.W0..W.W8` for the next tranche. Owner naming ruling owed before wave authoring. | N–W seat §0 S-4; DISEASE-REGISTRY:197 |
| S-5 | **Close-record absence is the corpus norm, not the exception**: value 3/13 A..M unclosed (C, K, M) + O and V close-less in N..W; keyframes 6/22 (M,N,O,P,S,V); fourier 3/13 and they are the three most recent (J, K-deploy, M); parse-that's three shipped releases have no tranche folder at all. | per-seat censuses, §0 of each |
| S-6 | **Ratification has never implied execution anywhere in this corpus.** All 12 N/waves files carry RATIFIED stamps; 2 of 9 "never executed" waves shipped a tagged minor anyway; kf N was fully designed and never run; fourier K-deploy/M were authored and never begun; vnext is 0/193 by its own statement. | seat receipts below |

---

## §1 · THE HALF-BAKED REGISTER — landed-then-diverged, or partially executed

Every row: the tranche, the promise (quoted), and today's state. DR/MT cites = already-ruled corroboration.

### value.js

| # | Tranche · item | The promise, quoted | Today's state | Ruled? |
|---|---|---|---|---|
| HB-01 | S.W3→T→U→V′ · the boot/JS-eager budget | T ruled LCP≤2500/TBT≤300 a HARD gate *"no re-baseline, no preset-swap, no deferral"* | RE-BASELINED at S (280→346.4), ESCALATED through T's own unbounded escalate arm (LCP 5141/TBT 5988), escalated again at U, re-booked to W55 which never ran; `lighthouserc.json` read by nothing; numbers predate v4/glass-7 — **nobody has measured this product** | **DR-06** |
| HB-02 | T.W1 · whole-tree colocation | colocation across 3 trees, landed `f8e7eed`/`dfa46c4`/`77d21fc` | re-opened by measurement: POST-U-AUDIT A8 — *"242/248 demo TS/Vue files remain under `demo/@`… modularization is path movement, not a DAG"*; the `demo/@` tree then died at W43 but the DAG claim was never re-certified | N–W seat H-5 |
| HB-03 | V′ W43 · *"import-graph census shows zero alias"* | zero dangling references | the census excluded e2e/; **7 spec/fixture files import the deleted `demo/@` today** (verified); the re-gate was assigned to W55, unrun | N–W H-7; feeds **DR-09** |
| HB-04 | V′ W42/W45 · CI re-gate teeth | a one-time falsifier: a deliberately broken spec turns the job red | *"the exact demonstration W42 and W45 both waived"* — a gate whose teeth were never tested | **DR-09** (registry's own words) |
| HB-05 | U · terminality | 77/77 families DECIDED, machine-checked | V's own A1 ruling: *"U accepted `STILL-BOOKED`, `complete_with_misses`, `DECISION-PENDING-OWNER`… as terminal evidence"* — the gate validated vocabulary, not state | N–W H-11; FM-03 |
| HB-06 | V · 4.0.0 breaking discipline | S encoded the lesson: a **by-name MIGRATION table** (3.0.0 precedent) | 4.0.0's breaking list names classes only; `sampleColorRamp`/`sampleColorRampAt`/`mixColorsInto`/`toRgba8Into` vanish unnamed — `grep -rn` over src/+test/ → **0** (verified); only CHANGELOG:264,404 remember | **DR-21**; N–W H-9 |
| HB-07 | V · 4.0.0 as-published totality | *"failure-explicit"* (the package description) | `parseCssColor("oklch()")` throws a raw TypeError — 324 throws/1548 calls from one `!` (`src/css/grammar.ts` — `slash[0]!` verified in-tree this session); shipping to glass-ui 7.0.0 today | **DR-12 / MT-F024** |
| HB-08 | N.W7.B · tarball gate | `N.md:156` hard gate: *"tarball < 200kB unpacked"* | closed at 287 kB by rewriting the gate to ≤320 kB *in the same status line* — the moved-goalpost archetype | N–W H-1; FM-06 |
| HB-09 | S.W1 · 3.0.0 full-HDR claim | full HDR perceptual slate | shipped conversion pairs under a full-space claim; **caught by S's own independent gate (BLOCKED 9/11)**, cured at 3.1.0 with the correction in the CHANGELOG — the recovery shape of record | N–W H-3 |
| HB-10 | R→S→T→U · the glass 5.0.0 adopt | the joint cut at 5.0.0 | resolved only by the target moving (glass went 4.2.0→7.0.0; W44 adopted 7 whole); the deploy workflow still checks out `tranche/BG` (glass 5-era) while package.json pins ^7.0.0 | **DR-22** |
| HB-11 | A.W5 | style/token/state waves complete | *"complete in the working tree but committed nowhere"* — B.W0 had to ratify-and-commit a predecessor's work | A–M seat §1 |
| HB-12 | G3 · the 350-LoC src cap | `color/utils.ts` 1430→9 modules ≤350 | the wave itself regressed `dispatch.ts` to 391; remediated; re-breached at 372 by K.W0; **5 src files over 350 today** (stylesheet 899 · decompose 609 · path 564 · grammar 483 · anchors 377) | **DR-18** |
| HB-13 | I.W1 · status dual-write | drop *"scheduled at value.js-J"* | J never did it; landed **L.W3** two tranches late — a legacy field dual-written under a NO-legacy banner in the interim | A–M §9; FM-15 |
| HB-14 | F3 · the authorized keyframes.js write `470814e` | a cross-repo fix reaching the peer | *"LOCAL ONLY; user-discretionary push"* — carried unpushed through G and H; satisfied an invariant while never reaching the peer's remote | A–M §6 |
| HB-15 | H1 · withTransaction 16 sites | a standing invariant + reference doc | renegotiated to 14 at N.W3 (*"txns 18→14 justified-each"*) with no H-facing record | A–M §8 |
| HB-16 | J · the cohort CORE | `J/FINAL.md` declares *"EXECUTED + GREEN"* | **no J-attributed code commit exists** — created under `59aab42c` (K), FINAL committed under `b8afd1cf` (K, verified this session); lived ~4 weeks with no consumer; excised whole at T.W1 `a8ff7792` as *"write-only legacy"* | A–M §10; FM-20 |
| HB-17 | U-F54 · the real-GPU oracle | the headed-GPU slate flips | *"never ran in 7 rounds (SwiftShader)"* → converted into an owner-attested annex; six closes, four names, zero executions | **DR-07** |
| HB-18 | N · inv-N-7 phantom classes | every demo class resolves | closed at W5, **re-opened the same day**, routed to N.W14 — a wave that never ran | N–W seat #6 |
| HB-19 | B.W3 · the e2e abrogation thesis | 16 brittle specs → 3-spec smoke | reversed by accretion (D 3→21, E→36, today **71 specs/6 projects**) — and the accreted corpus gates nothing (no Playwright step in ci.yml) and is import-broken (S-3) | A–M §2; **DR-09** |
| HB-20 | M-14 clause 1 · glass-forward relay | *"ALL glass-ui-forward items delivered to that inbox and marked accordingly"* | **26 delivered · 11 UNRELAYED · 2 partial** — the debt concentrated in four adjudications (AdminUsersPanel, GenerateControls, GradientStopEditor, Markdown) all closed before O-16 went out; `inheritAttrs` = 0 across all six letters (re-verified this session) | glass-forward seat, whole |
| HB-21 | the parser proof gate · c14-css | mirror-primary architecture proven | composite **RED-of-distance**: equivalence GREEN (0 mirror defects) · coverage RED (0-of-52 TOTAL) · bench RED (sheet 0.0537); superseded upward by the TRIFOLD parser band (two total parsers, winner cand-O), whose bench **refutes** the standing "regex ~1.8× fastest" premise | STATE §parser; vnext O-1 |
| HB-22 | plugins/vite-defer-glass-fonts.ts | *"serve (dev): REPLACE the marker with the real `@import` so the dev server renders the true faces"* | the guard tests `demo/@/styles/style.css` — a path dead since `a61094e3` — so the transform never fires: **the dev server (and every Playwright/visual capture against it) has rendered in fallback typefaces since W43** | shadcn census C-01 (NEW there) |
| HB-23 | vnext · the formation seal | 2/2 CLEAN, six gpt-5.6-sol/ultra seats | the seal binds an epoch hashing 17 external files owned by three other programs (one in another repo); it died in two days and is **unrecoverable by construction**; 0/193 executed; the gate runner `.vnext/proof-runner.mjs` does not exist; the live R1 crash appears in no born-RED row | vnext seat D-1..D-4; FM-02 |

### the peer repos (constellation annex — value.js is the coupling's hub)

| # | Repo · item | The promise | Today | Cite |
|---|---|---|---|---|
| HB-24 | keyframes H · the false close | *"ALL ~97 proof:* gates GREEN… every request resolved"* | *"The certification is false of the product"* (I) — nine user-visible breakages; the archetype of the corpus's six-link overclaim chain (every close B→L found its predecessor's close false, through inv ε) | kf seat KF-α/KF-δ |
| HB-25 | keyframes T · the close of record | FINAL + tag | `T/FINAL.md:3` **still reads "DRAFT — Close NOT yet fired"** (re-verified this session) while `v5.2.0` was tagged 2026-07-09 — the close fired, the record didn't move | kf seat KF-γ |
| HB-26 | keyframes U · the dissolution | 227 proof keys→2, honest | it also silently voided T's four dock-crispness tripwires un-decided — caught only by V's DISPOSITIONS DISEASE audit | kf seat §20/§21 |
| HB-27 | fourier J.W2 · the 7 CORE endpoints | remix/publish/provenance/diff, 41 tests | **zero callers in api.ts** (0 hits, re-verified) and `gallery.ts:226` still PATCHes `{visibility:"public"}` — the named *"binary inv-15 breach"*, 53+ days on; W5 wiring booked to J.W5→M.W10, never run | fourier seat §10/§16-2 |
| HB-28 | fourier M.W1a · the keystone bump | all-packages-latest | executed 2026-06-17 and **never committed** — 27 files, 1204+/1261−, 40 days stale on `m/w1-bump-migration`; invisible to every ledger | fourier seat §12 |
| HB-29 | parse-that T/U/B | a formed successor tranche set | 0 waves run; **154 of 161 doc paths untracked** — one `git clean` ends the formation; the shipped releases (0.12.0/0.13.0/1.0.0) have no tranche folder at all | fourier-pt seat P-0/§15 |

---

## §2 · THE SILENT-DROP REGISTER

### §2.A — drops already rowed by the anti-rename ledger (corroborated, not re-argued)

The excavation independently re-derived every one of these; the registry's chain stands in each case. One line each:

DR-01 aurora derive (17 closes/13 names; landed N.W5, never tombstoned) · DR-02 blob extirpation (inverted to KEEP, no tombstone; "extirpat" = 0 in vnext) · DR-03/04/05 GAP-L5/GAP-L2/GAP-ARM (zero hits in all of V/reformation — the verify-at-adopt was never walked at W44, the exact event it waited for) · DR-06 the ~5s boot (HB-01) · DR-07 real-GPU oracle · DR-08 fourteen tranches with no visual-regression gate · DR-09 the CI cliff (185 Playwright tests, LHCI, boot-smoke — executed by no automation; also absorbs the bench/ deletion at `164343c1` and the D.W5 "CI runs all 3" drop) · DR-10 boot-smoke + the prod empty mount · DR-11 master red since 07-05, 11 skipped deploys · DR-12 R1 parse-honesty · DR-13 the 8 A-era glass asks (12 hand-written h-9 sites; `Ad-18` still cited in shipping source) · DR-14 siblingFsAllowTransient · DR-15 the precepts pin drift (now on entirely new SHAs) · DR-16 HG6 (stub re-verified this session: `T/audit/w8-certification/VERDICT-2026-07-12.md` — **"Status: AWAITING THE OWNER"**) · DR-17/18 the caps · DR-19 proof:* (deleted 06-02, rebuilt 4×, 81 sites in vnext) · DR-20 the PARK set (Color.try 11→12→3 with no threshold) · DR-21 SCI-1 (re-verified: 0 hits src/+test/) · DR-22 deploy checks out `tranche/BG` · DR-23 untracked authority · DR-24 dev.sh · DR-25 PRM-expand (the retirement gate FIRED at kf≥5.3 and nobody noticed) · DR-26 circularity restored at `test/v4-color-behavior.test.ts:66` · DR-27 the scene-hop frame numbers that never entered a ledger · DR-28 CH-8 UI half · DR-29 the owner-attest annex with no calendar slot · DR-30 prompt-recap · DR-31 the NCSU 301 inversion · DR-32 DesignSync · DR-33 the per-process idempotency LRU sold as durable.

### §2.B — NEW silent drops the registry missed (**the payload**)

| # | Dropped thing | Promised in | Evidence | Consequence / owed act |
|---|---|---|---|---|
| **ND-01** | **The library `Palette` domain — `colorScale` / `sampleToSVGPath`** | value C.W1 (2026-05-18); re-filed by fourier A §7, B §6, C δ, D §6.4, E ζ | `grep -rniE "colorscale|sampletosvgpath" src/` → **0** at 4.0.0 (re-verified) | **The constellation's longest drop: 14 months, five fourier tranches, four repos of ledger rows.** Not a DR row anywhere. Needs a terminal decision (build at the 4.1.0 cut, or RETIRE with a letter to fourier) — a fifteenth restatement is forbidden. |
| **ND-02** | **The correcting letter to fourier on `sampleColorRamp`** | value→fourier `N/VALUEJS-2.0.0-NOTE.md` §3 (2026-07-03): *"strike the M.W7 booking, `sampleColorRamp` already ships — a live import, no wait"* | the v4 cut `164343c1` (2026-07-17) retired the symbol; **no correction was ever sent**; fourier's M.W7 book is now unsatisfiable and fourier does not know | inv-32 book-rot running *backwards*, after inv-32 was authored to stop it. DR-21 books the restore; **the letter is a separate owed act** and must ride the same cut. |
| **ND-03** | **The Parse-Lab pane + gamut-truth indicator** | O.W7 (booked) → built + fused at R.W3/R.W4 (Q10 RATIFIED) | `grep -rl "ParseLab\|parse-lab\|gamut-truth" demo/` → 0; the *overlay* retirement was ruled (owner bracket B1, W40); **the Parse-Lab fuse into ColorInput was never ruled and is simply gone**; its gate `proof:parse-lab-mount` was authored for a wave that never ran and never re-homed | a built-then-deleted demo feature with a half-ruled tombstone; the un-ruled half needs one line in W.W0 (retire-with-rationale or re-book with a real owner) |
| **ND-04** | **The 14 GLASSUI-S-ASKS (L2..L16) as a per-item ledger** | S close: *"per-item live-verified table"*; T carried; U relayed some | V′ §D carries only 4 glass rows, none the L-series | distinct from DR-13's 8 A-era asks. Fold the survivors into the DR-13 W.W6 terminal-disposition act — same wave, second bundle, same rule (no producer-gated carry). |
| **ND-05** | **inv-N-10 — the abrogation sweep at every pin bump** | N §6, a standing structural gate | absent from the W44 glass-7 adoption gate list (CARRY-LEDGER §F enumeration) | the one standing sweep that would have caught dead-`variant` attrs and stale deploy pins at adoption time. Re-instate at the next pin bump or retire by name. |
| **ND-06** | **The U close ceremony itself** | U closed `complete_with_misses` 2026-07-13 | no `tranche-u-close` tag, unmerged (S-1/S-2, verified) | adjacent to DR-11 (which books the *merge*); the tag/ceremony row itself was never booked. One act inside W.W3 LANDING. |
| **ND-07** | **G.W5 — a wave that ran after the close, recorded nowhere** + the dead changesets apparatus | `f895048e` (2026-05-28, verified) | `grep -c "W5" G/FINAL.md` → **0** (verified); `.changeset/` = README+config only, zero changesets ever authored across v0.9.0→v4.0.0 | record-integrity: a whole wave exists only in git. Tombstone in W.W0; delete `.changeset/` or use it. |
| **ND-08** | **Un-SHA'd close ceremonies D–H** | each FINAL's close-honesty checklist asserts *"cites every commit"* | `D/FINAL.md:219` still reads `` `<this commit>` (to be filled…) `` and `Merge commit SHA: TBD` (re-verified); E/F/G/H carry `(this)`, `(next)`, `(merge)`, and five SHA cells holding the words `audit/docs/chore/merge` | five consecutive tranches never cited their own close commits. Historic; fix-forward rule: a FINAL with an unresolved SHA cell is not a close. |
| **ND-09** | **B's "value.js library-maintenance effort"** (11 recorded src/ gaps) | B.W3 `audit/B.W3-library-gap.md` | the phrase appears in `B/FINAL.md §3` and never again in C..M | the exact failure B5 was written to prevent: a named destination that never got a letter. The library band's sweep is its *de facto* discharge — say so in the band's record and close it. |
| **ND-10** | **`slugWords.ts` → shared `SLUG-WORDS.md` precepts data** | C U2 spec | `find . -name 'SLUG-WORDS*'` → empty; `api/src/modules/session/slugWords.ts` still opens `const ADJECTIVES = […]` | 14 months PENDING-NO-PULL. Decide once in W.W7 (the api wave): pull or retire. |
| **ND-11** | **The published consumer codemod `migrate-keyframes-js-lerp.mjs`** | E.W4; G gate #19 asserted it was *"in the npm tarball"* | deleted at `c4c58421` two weeks after the v0.8.0 breaking change it served | historic; the lesson (a breaking change's migration path is part of the break) feeds HB-06/DR-21's cut discipline. |
| **ND-12** | **The K.W0-ratified v1.0.0 cut at K.W6** | user verdict recorded: *"approved, cut at K.W6 close"* | K.W6 never ran; the tag was cut under another tranche's authority | a *user ratification* discharged by an unrun wave — the strongest instance of FM-19. Historic; the FM-19 ban ("no kill-date bound to a wave that can decline to close") is its discharge. |
| **ND-13** | **The 11 unrelayed glass-forward arms** (Button inheritAttrs+allowlist ×2 seats · ConfirmDialog preset · xs-height token · G-2 track-stops · G-3 ButtonProps · dead-`tag` census · dark-arm `--card` · rail SliderThumb slot ×2 rows · Skeleton `--muted` · +2 partial deliveries U-10/U-11) | each adjudication's own RELAY/GLASS-OWNED declaration, all ruled before O-16 (13:27) | term sweeps over all six sent letters → **0** for every identifying token (`inheritAttrs` re-verified 0/6 this session) | **M-14 clause 1 is NOT met.** Owed: ONE amendment letter to BJ (U-1..U-9 as new asks, U-10/U-11 as amendments to O-16 §C-2/§C-1), amendment-style like O-10a. No value.js gate goes RED when a relay does not leave — that is *why* it sat unnoticed. |
| **ND-14** | **The design canon's three named laws** | the owner: GOLDEN GLASS (invoked as law 07-27, defined nowhere in the constellation) · BREATH OF LIFE (one owner sentence 07-17, homeless in value.js, **0 occurrences in keyframes.js despite the 07-19 order naming it a co-target**) · MOVEMENT OF MOMENTUM (defined as "Liquid Weight" in glass-ui/DESIGN.md:113 — renamed, unregistered in CANON_HOMES, driver-only carve unruled) | design-canon seat §1 (0/4 coverage in every value.js design authority; the four-doc corpus is orphaned in both directions — 0 cross-citations in 12 cells) | Phase E cannot author the motion law until the momentum carve is an **owner mark**; GOLDEN GLASS must be *authored from lineage*, not cited. |
| **ND-15** | **CRUD-CONTRACT "ratified for both backends"** | fourier B (2026-05-27) | `find` in value.js → empty; fourier's copy exists; unilateral for 14 months | fold into the ND-01 decision: one letter settles both. |
| **ND-16** | **The last executable shadcn** — `cn()` + `clsx`/`tailwind-merge` devDeps + `components.json` pointing at three dead targets | the standing FULL-abrogation edict (owner, 07-27) | `cn(` consumers → **0**; `clsx`/`tailwind-merge` live at package.json:99/:108; `components.json` names `demo/@/components`, `tailwind.config.ts`, `@styles/style.scss` — all nonexistent (all re-verified) | the component half of the edict is DONE (0 shadcn SFCs); these three files are what makes "abrogated" literally true. Cheap; one commit in W.W8. |
| **ND-17** | **The owner's own excavation order** | 2026-06-09 (glass seat): *"I don't mean guess what the last 100+ message are — literally deploy an agent fleet to scan these"*; re-issued 06-25 and 07-13 | answered once, then let lapse until this swarm | M-14 **is** its discharge — record the lineage so the order is never re-dropped. |

**Peer-repo drops for the record (not value.js-owed, but value.js-adjacent):** fourier letter L minted and never created · fourier's e2e last proven green 2026-06-01 · `.cartoon-card` surviving two booked deletions (15 files) · the `visualization_versions` degenerate chain shipped-then-adjudged-phantom and still in the schema · kf's four dock-crispness tripwires de-tripwired (HB-26) · E's openapi-typescript codegen alive one tranche, whose orphaned lockfile entry broke the deploy chain from the grave.

---

## §3 · THE REJECTED REGISTER — explicit rejections that must never be re-proposed

Owner-ruled first (highest authority), then close-ruled with rationale. Re-proposing any of these without a new owner ruling is a defect.

| # | Rejected thing | The ruling, quoted | Date · where |
|---|---|---|---|
| RX-01 | **The proof:*/gate-farm idiom** | *"overfit junk … NEVER re-introduce; enforce invariants structurally"* — **read together with** the glass-side qualifier: *"Not blindly abrogated--if the use case is truly worthwhile, and deemed so after our twice critique, then it's kept"* | 2026-06-02 (owner) · 2026-07-16T21:09 (owner). The composite law: presumed contrivance; survives only an explicit adversarial critique. DR-19 books the owner ruling owed on vnext's 81 sites **before any adoption**. |
| RX-02 | **In-tranche parse-that / tape / BBNF work** | *"I decide it now. Parse-that work, like the tape, etc is to be for their own tranche set. We consume the repo outright as it's published now"* | 2026-07-18T04:22 (owner, E-46). Consuming published parse-that (c14-css does) is inside charter; editing/vendoring/forking it is not. |
| RX-03 | **Regex-based parsing** | *"All regex-based parsing should likely be entirely abrogated: for both value.js and keyframes.js"* | 2026-07-18T02:13 (owner, E-45). The parser band's bench (regex NOT fastest on accepted input; ~55–130× worse on the throw class under published+catch) removes the last performance excuse. No regex fast-path returns without the owner re-ruling. |
| RX-04 | **Codex/vnext as producer** | *"Codex has been totally abrogated in favor of Claude running sessions. All of that work is owned by you."* | 2026-07-27T22:34 (owner, M-15). vnext never resumes; it is absorbed item-by-item by ruling (the 28-row ledger in the vnext seat). |
| RX-05 | **R.W5 hero-lab** | KILLED by owner at ratification (Q1 FLIP); artifacts deleted `9ed9175`; the wave-letter gap kept deliberately as the record | R, 2026-07 — the model owner-kill. |
| RX-06 | **K-W3DIFF / a PaletteDiff render surface** | REFUTED-AS-CONTRIVANCE (*"no diff consumer exists; stored atomDiff write-only"*); alt-exit S.W5; physical routes excised T.W1 `a8ff7792` | the model close-rejection — carried to physical completion. Do not resurrect the atom-diff UI without a consumer-first case. |
| RX-07 | **The Normalized/Display type brand** (S.W2-3) | DECLINED-mechanical → **KILLED with rationale** at T.W1 (`T/FINAL.md:181`) — a permanent constraint | clean 2-hop terminal. |
| RX-08 | **keyframes-vue** | *"Overfit nonsense. Remove completely and revoke from NPM publishing."* — unpublished + deleted at kf R.W0 `23a68677` | 2026-06-24 (owner). Published once (0.1.0), retracted in 24h. |
| RX-09 | **kf Worker/OffscreenCanvas/Atomics engine path** · **the ScrollTimeline JS-sampler** · **Mod+K palette (FB-6)** | permanent KILLs, each re-affirmed at `V/DISPOSITIONS.md §B`; grep-verified empty by the kf seat | the peer corpus's cleanest kills; binding on any "engine worker" or command-palette proposal here. |
| RX-10 | **parse-that `SpanParser`** | *"measured ~10–14% SLOWER on V8/TS; the §7 jump-table hypothesis does not transfer from Rust"* — retired at 0.12.0 | measured-refusal precedent. |
| RX-11 | **The V′ armor corpus** (~70% of the palette spec) | RETIRED-AS-SPECULATIVE; *"re-formation is explicitly not a BANK"* | V′ B2. Any wave re-importing PROPORTION-AUDIT-era armor numbers re-imports a 3.4×-undercounted census (design-canon seat §4.3). |
| RX-12 | **WL's 7 declines** + **the approximated-discriminant analytic arm** (*"a labelled lie; max Δ 0.192"*) + **resolveCssColor** (DECLINED with a recorded re-trigger — owner-adjacent) | library-band apotheosis (RD-6 et al.) | the 4.1.0 cut ships what was RULED shipped; nothing declined returns without its named re-trigger firing. |
| RX-13 | **VJ-CSS2 `sibling-index()` new arm** (already parsed; pinned by tests) · **the Q L6 `color-contrast()` stub** (deleted at the grammar source) · **N.W9's v1.0.0 framing** (pruned as obsolete) · **`lerpArray` demote** (KEEP — premise refuted by the kf consume-edge) · **K-INV5's literal fix** (refuted by experiment; kernel landed differently) · **D's 12 LIB-RX perf claims** (KISS-gate) · **E's motion-canon Family B** (reviewed, not adopted) | per-close records, each with rationale preserved | the healthy denominator of refusals. |
| RX-14 | **"Batches of three"** and **"twice-challenged"** | superseded by the owner: *"Deploy a maximum of four workflows per batch"* (2026-07-24T20:51) and the thrice/tri-fold law (07-24 → 07-27 M-12) | citing the retired numbers after 07-24 is a stale-canon defect (glass2 C-3/C-6). |
| RX-15 | **Crons as the durability mechanism** | five CREATE→KILL oscillations, terminal state *"Disable your crons"* (07-20) + *"ensure our progress … is made durable against session walls"* (07-27, no cron named) | the invariant is **durability, not crons** (glass2 C-4); value.js's "NO CRONS by owner order" stands. |
| RX-16 | **Fleet-side re-derivations without model receipts** | the Fable mis-routing incident: pre-2026-07-18T03:06 "Fable" findings presumed incorrect until re-derived-and-unioned; *"a model declaration is not model execution"* | owner, E-17/P0.1. Receipts at every seat (L-11) is its standing discharge. |

---

## §4 · THE KEPT-PROMISES SAMPLE — the denominator

Honesty demands the base rate. The corpus is not a graveyard; the commit log, wherever spot-checked, is *accurate* — the lies live in the close documents, not in git.

- **75/75 cited commit SHAs resolve and match** across the three truth seats (value A–M 28/28 · N–W 25/25 · fourier 22/22), plus this seat's own 17-command re-verification (§below) — all held.
- **O**: seven published cuts in ~24h (0.13.1→1.0.2), the cleanest execution record in the value corpus (and the worst closure record — both true at once).
- **T.W0**: 11/11 PASS, the only zero-miss wave in N..W; **X1/X2** — a 3-tranche maintainer book killed by *re-classing it executable* (`bdfb4a5e`), the model chronic-cure.
- **S**: the 3.0.0 overclaim caught by S's own independent gate and cured at 3.1.0 with a by-name MIGRATION table — the recovery shape every later cut should have copied (HB-06 is its breach).
- **R**: the highest-fidelity close record in the corpus (per-wave gates, ι reflog sweep, 10 lessons); the R.W5 kill recorded with the letter-gap kept.
- **N.W5** `e32111c7`: the 9-tranche blob/aurora carry mechanically landed (its *tombstone* is what's missing — DR-01/02).
- **V′**: eight units closed with verified commits — W40..W45, W51, WL; **W44 adopted Glass 7.0.0 whole and cured the born-RED blank**; W45's 7 RED remediations; W51 byte-exact serializers.
- **U**: the 77-family machine-checked walk and U-F61 (4 single-sourced claims flagged, never laundered green) — method-level honesty even where terminality laundered (HB-05). **T-1..T-61**: 61/61 owner findings routed with cites — the best prompt-recap instrument in the corpus.
- **4.0.0 published**; CHANGELOG honest at class level; the packed-surface gate real.
- **keyframes**: I's correction wave (nine breakages behind actuating born-RED runtime gates); U's apparatus dissolution verified live today (2 proof keys, 0 flat scripts, the founding inv α surviving re-homed); R's atomic chronic re-point with line-cited co-edits; D-3's measured withhold; Q's DM-2 ninth-carry *genuine* browser-verified exit; K's P-TASTE (close on the owner's verdict — the only mechanism that ever beat "instruments green, owner says no").
- **fourier A..I**: closes on cited evidence with visibly improving discipline; H's four consecutive green runs; **G's false "CI green" corrected by H *appending* to G's own FINAL with run ids** — the right way to amend a close.
- **parse-that A**: three of four charter premises empirically falsified *mid-execution*, each recorded as a finding, and it still shipped three npm releases in 24h — the healthiest tranche in the peer corpus.
- **The healthy-rejection set** (§3 RX-05..RX-13): seven rejections carried to physical completion in value.js alone.
- **vnext's machinery was honest even where its result is void**: the fail-closed protocol genuinely bit (an oversize report and an invalid receipt both rejected); 878 KB of apparatus demoted with byte receipts; `c14-css` declares its own scope truthfully (`isolated-assay-only`, born-RED where unproven).
- **K.W1** (the re-grounding wave): went and read the peer code, found seven tranches of carried asks partly fiction — the corpus's single most valuable wave, and the method every carry-consuming formation must copy.

**Base rate, measured by the seats:** value N..W = 38 clean LANDED of 113 rows; the 22-tranche historical landing rate ROOT-FINDINGS uses is 38%. The kept promises are real and numerous — *and* the close vocabulary that reported them is the same vocabulary that laundered the drops. That is the whole finding.

---

## §5 · Spot-verification log (this session) + corrections to the standing record

**17 checks, all held** (commands + outputs in the session record): `9fce504a` exists and shipped N.W11.D while `N/FINAL.md:4` still claims the block was *"never executed"* (a documented falsehood in a FINAL, asserted here deliberately against N's own close record) · 7 e2e files import dead `demo/@` · the HG6 stub reads **"Status: AWAITING THE OWNER"** · `D/FINAL.md:219-221` placeholders live · `sampleColorRamp|mixColorsInto|toRgba8Into` = 0 in src/+test/ with CHANGELOG:264 remembering · master `6abef800` 2026-07-13, 197 ahead · tags stop at T · K and M have no FINAL.md · `J/FINAL.md` added by `b8afd1cf` (a K commit) · `f895048e` G.W5 exists, 0 hits in G/FINAL · `cn(` 0 consumers, clsx+tailwind-merge at package.json:99/:108 · `slash[0]!` at the grammar.ts crash site · `components.json` names three dead targets · `inheritAttrs` 0 across all six glass letters · fourier `gallery.ts:224-228` still PATCHes visibility with 0 CORE callers in api.ts · `colorScale|sampleToSVGPath` 0 in src/ · kf `T/FINAL.md:3` still DRAFT · vnext = 165 tracked files.

**Corrections this table makes to already-ruled rows** (per the mandate: never contradict silently):

1. **DEFECT-LEDGER :11131** claims `dominantColor` is consumed at three demo sites — those sites import `quantizePixels`/types; **none imports `dominantColor`**, and its only mention calls it *"the wrong tool"* (shadcn census L-03). The ledger row's cure survives; its consumer census does not.
2. **ROOT-FINDINGS RF-26** misdates the aurora origin as "Tranche D" — the chain starts at **A.W6 `065c6fe` (2026-05-19)**; DR-01(3) already books this correction; the A–M seat confirms it independently.
3. **`W/audit/history/V-vnext.md:35-38`** ("the entire 1.75 MB corpus is untracked… one `git clean` destroys it") is **stale since `5c13465d`** — 165 files tracked (re-verified). The parse-that T/U/B corpus, by contrast, is *still* in exactly that state (HB-29).
4. **DR-16's stub quote** ("empty — the owner's verdict lands here") differs byte-wise from the live file ("Status: AWAITING THE OWNER…"). Same substance; the verbatim line is recorded above so future greps hit.
5. **`megatranche/SCOPE.md §1` / STATE.md** pin HEAD `c654824e` — live HEAD is `4f78e57b`; the N–W seat's "196 commits" is now 197. Re-pin at the next STATE touch (the vnext `validate-current-dags` RED is this same class — a re-pin, not a re-measure).
6. **vnext's own `FORMATION-CLEAN-PASSES.json` ("clean", 2/2)** is overridden by its own validator (0/2 at the live epoch); the vnext seat's framing stands: retire the credit, keep the receipt.

---

## §6 · THE ADDENDA CONSEQUENCE — which extant wave specs carry half-baked or dropped premises

The registers above are only useful if they change what gets authored. Verdicts: **KEEP** (premise sound) · **RE-AUTHOR** (premise broken; content survives) · **PRUNE** (superseded; harvest only) · **BLOCKED-ON-OWNER**.

| Spec family | Verdict | The premise defect, named |
|---|---|---|
| **vnext's 193-wave registry** (waves/P-V·K-A·G-D·M-C) | **PRUNE** | superseded-by-megatranche (vnext seat's standing verdict): no runner exists for any of its 193 gate commands (FM-02 canonical), its seal is unrecoverable, it missed the live R1 crash, and it silently inverts two chronic mandates (aurora/blob KEEP without tombstones). Harvest the 15-row ABSORB set (seed bijection, handoff boundaries, api-contract, target-paths, STATE-ROUTING, KEYFRAMES-API, owner amendments, **c14-css** — the only implementation the D-23 ruled architecture has). Owner ruling owed on the 81 `proof:` sites before ANY tool adoption (RX-01/DR-19). |
| **V′ reformation tail W46–W56** (the bundled wave files) | **RE-AUTHOR-WITH-SUPERSESSION** | V-PRIME L1's own words make any fresh "BUILD W##" row for the 8 disease riders *"the forbidden re-booking"* — and the megatranche's W.W1..W.W5 rows are exactly that shape (N–W seat row 101). The cure is not to stop authoring; it is an **explicit owner supersession ruling** + per-rider tombstones citing the DR chains, so the new rows are successions of record, not re-bookings. Also: the tail ships as bundles against L3's standalone-wave law (relaxed without a ruling) — the re-authored waves must be standalone again. |
| **DISEASE-REGISTRY wave shapes W.W0..W.W8** | **KEEP as the spine, with three re-authored cells** | (a) **W.W2 PARSER**: DR-12's shape ("fix grammar.ts:181, enable no-non-null-assertion") predates the TRIFOLD parser band — the band's apotheosis (winner cand-O, 4 dissents awaiting the owner, OC-1 owning the bench bar) is now the governing input, and the standing "regex ~1.8× fastest" premise is refuted by three independent measurements. The 4.0.1/4.1.0 cut must also carry ND-02's fourier letter. (b) **W.W4 BOOT**: the 4919/5141 numbers predate v4, glass 7, and the alias death — the registry itself orders a re-measure; treat it as load-bearing. (c) **W.W0**: add ND-03/ND-07 tombstones and the ND-16 shadcn terminus to the existing docket; and the **letter-collision ruling (S-4)** must precede everything — W.W# rows cannot be minted for a letter whose directory is occupied by audit output. |
| **The adjudicated waves** — MT-APP-1 (+5 named siblings), MT-PICKER-1, V·MT-W-CSS1, V·L1..V·L4, the library-band 9-wave skeleton + the 4.1.0 cut | **KEEP** | post-excavation, evidence-grounded, already ordered (V·L1..L4 after MT-APP-1). Two riders: the 4.1.0 cut absorbs ND-01/ND-02/ND-15 (one dated cut + one letter discharges a 14-month cross-repo family); and every seat citing "batches of three"/"twice-challenge" is mechanically updated to the ≤4-workflow + thrice/tri-fold law (RX-14). |
| **Phase E — the design tri-fold** | **BLOCKED-ON-OWNER (two marks), then RE-AUTHOR demo/DESIGN.md** | ND-14: the momentum driver/observer carve is an escalated owner mark ("never a silent pick") and F034-b's curve depends on it; GOLDEN GLASS must be authored from the glass-ui lineage. `demo/DESIGN.md` is 12-of-13 anchors defective and legislates appearance without semantics (elevation/sub-pane = 0 in all four docs) — re-authoring is substantially a **FOLD**: the vnext `DESIGN-PROGRAM.md:170` motion table already contains the MT-F034 Transition row, cited by nothing. The design-canon seat's six born-RED gates are the wave's acceptance set. |
| **The glass relay amendment** (ND-13) | **AUTHOR NOW — a letter, not a wave** | one BJ amendment carrying U-1..U-9 + the U-10/U-11 corrections, O-10a-style. Until it leaves, M-14 clause 1 stays unmet and two convergently-derived producer asks (Button attr-closing) stay undelivered. |
| **V·π mini-tranche (Phase A/B, awaiting begin-word)** | **KEEP, with a collision flag** | its premise (parser-to-spec first) is *corroborated* by the parser band, but V·π and W.W2 now describe overlapping parser work under two live formations. The owner's begin-word must also rule the ordering/merge of the two, or the corpus mints its next FM-07 identifier collision. Its standing edicts E-1..E-5 remain binding. |
| **Any spec inheriting a carry-ledger without a grounding pass** | **RE-GROUND before consumption** | the K.W1 lesson (seven tranches of carried asks partly fiction) + fourier pattern 5 ("every one of these came from a probe against the live tree, never from reading a ledger") + the E-17 receipt rule. CARRY-LEDGER §B/§C/§D rows enter the new formation only through a probe, per row. |
| **Gate-adding waves generally** | constraint, not a verdict | E-28/E-30: the process apparatus is the owner's primary contrivance suspect; *"spend little time on contrived gates … the majority on direct code implementation and visual verification."* Every wave that adds a gate names the gate it retires, and W.W1's suite must actually RUN (it never has) with the falsifier demonstration W42/W45 waived twice. |

---

*End of TRUTH-TABLE.md. Registers: HB-01..HB-29 · §2.A 33 corroborated DR rows · ND-01..ND-17 (+6 peer-repo drops for the record) · RX-01..RX-16 · the kept-promises denominator · 17 spot-verifications · 6 corrections to the standing record · 9 addenda verdicts. Every claim carries a quote + file/date or a command receipt; nothing rests on memory recall.*
