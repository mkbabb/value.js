# Glass tranche deep batch: early

**Audit date:** 2026-07-29

**Physical directories:** `C`, `D`, `D-II`, `E`, `F`, `H`, `I`, `IOS27-MICRO`, `J`, `K`, `L`, `M`, `N`, `O`, `P`

**Evidence base:** directory-local plans, progress records, finals and audits; cited commits in `/Users/mkbabb/Programming/glass-ui`; current source at glass-ui `5841dd35` plus the named sibling worktrees. Paths beginning `docs/`, `src/`, `demo/`, or `tests/` are relative to glass-ui unless a sibling repository is named.

Disposition rates the promised mechanism against both its close proof and current code. `LANDED` means the mechanism or its direct successor is live; `HALF-LANDED` means only a material subset is live or execution remains open; `HISTORICAL-ONLY` means the mechanism landed and was later retired or replaced.

| Directory | Promised mechanism | Close claim | Implementation / commit evidence | Current-source evidence | Terminal disposition | Surviving carry / exact receiver |
|---|---|---|---|---|---|---|
| `C` | Operational-truth typography cutover: replace `font-mono-code` with `fira-code` and `text-2xs` with `text-micro`; do not add duplicate aliases (`docs/tranches/C/C.md`, W1.C). | Migration complete across 60 uses; old spellings absent (`docs/tranches/C/FINAL.md`). | `4f336752` performs the cutover; `2b31920c` records C close. | `fira-code` and `text-micro` remain in `demo/chassis/code/Code.vue`, `demo/stories/dock/overview.vue`, and `src/styles/theme/bridges.css`; fresh `rg` finds neither retired spelling in `src/` or `demo/`. | **LANDED** | **NONE.** C's candidate-debt census was input to D and is not an open C receiver (`docs/tranches/D/D.md`; `docs/tranches/D/FINAL.md`). |
| `D` | Enforce substrate-with-consumer: every export has a current consumer, a story, or a named clean deletion; converge on one dock package (`docs/tranches/D/D.md`). | Story consumers wired, stale surfaces removed, one dock retained, root/type/consumer gates passed (`docs/tranches/D/FINAL.md`). | D's story/delete slices are summarized in `D/FINAL.md`; `7289c0c7` supplies the final D-II redress, deletes the standalone rail and unused dock helpers, wires the touch gate, and carries tag `d-close`. | `src/components/dock/GlassDock.vue` is the dock authority; no standalone rail component package exists. `src/components/dock/composables/useDockTouchGate.ts` and `src/components/slider/Slider.vue` consume the touch-gate substrate. | **LANDED** | **NONE.** The publication cutover was received and closed by E (`docs/tranches/E/E.md`; `docs/tranches/E/FINAL.md`). |
| `D-II` | Cure the false D close by making the vertical rail `GlassDock variant="rail"`, deleting the duplicate rail package, and closing velocity, root-test, and type gates (`docs/tranches/D-II/D-II.md`). | Directory progress stopped at “pending commit/tag,” but the same change that added the records also landed the redress and `d-close` (`docs/tranches/D-II/PROGRESS.md`; commit `7289c0c7`). | `7289c0c7` adds the variant, removes the duplicate package and unused helpers, and closes the cited gates. | The exact discriminant is gone. `src/components/dock/styles/layer-group.css` records that `variant="rail"` was retired; `demo/stories/dock/rail.vue` and `demo/stories/manifest.ts` use `orientation="vertical"` on the single `GlassDock`. Commit `301bdaac` made that clean replacement. | **HISTORICAL-ONLY** | **NONE.** The one-dock result survives under `orientation="vertical"`; no open D-II work item is routed onward. |
| `E` | Replace the permissive publication surface with a curated root, explicit feature subpaths, a shared entry map, consumer migrations, and runtime/type proof (`docs/tranches/E/E.md`). | Root allowlist and subpath contract cut over; catch-all export removed; consumers and package proof passed (`docs/tranches/E/FINAL.md`). | `6ce14e5b` and `99e2998e` establish the substrate; `baf3cce5` closes E and carries `e-close`. | `package.json` currently has 72 explicit keys, no catch-all `./*`, and only the intentional asset pattern `./fonts/*`. `src/index.ts` still documents and implements a curated, heavy-peer-free root. | **LANDED** | **NONE.** F opened as a separate contract-hardening tranche, not unresolved E publication work. |
| `F` | Harden interaction, style, and rendering contracts; the load-bearing rendering cut keeps live Aurora contexts non-preserved, makes capture preservation explicit, keeps `renderAt` draw-only, and removes dead shader/runtime seams (`docs/tranches/F/F.md`). | Six implementation waves passed close proof; Aurora live/capture and shader contracts were recorded green (`docs/tranches/F/FINAL.md`; `docs/tranches/F/audit/W6-close-proof.md`). | `7dca4831`, `09280c0d`, `f1cd338d`, `5a8dd246`, `b523b20d`, and close fix `4abf39ac`. | `src/components/aurora/composables/runtime.ts` still resolves `preserveDrawingBuffer` by mode and exposes draw-only `renderAt`; `src/components/aurora/constants/shaders/aurora.frag.ts` and the uniform bridges still carry `uBrokenColor`. | **LANDED** | **NONE for this mechanism.** `F/FINAL.md` accepted five unrelated P3 residuals without naming a receiver; that routing omission remains a record defect, not an open Aurora contract item. |
| `H` | Make G's substrate honest: each G artefact must be wired to the stated bar or retired, and make post-close audit part of tranche closure (`docs/tranches/H/H.md`). | 77 G artefacts retired; post-close beta audit reported zero G-artefact library orphans (`docs/tranches/H/FINAL.md`; `docs/tranches/H/audit/W1-reconciliation-result.md`; `docs/tranches/H/audit/H-audit-β-substrate-without-consumer.md`). | `68e40977` lands the wire-or-retire cut; `cc57c91` in the precepts submodule lands the close-process rules. | Fresh current-source searches find none of the named retirees `SvgFilters`, `KeyboardShortcutsModal`, `TierBadge`, `LikeButton`, `useCollapse`, or `useMonacoTheme` in `src/` or `demo/`. | **LANDED** | **NONE.** H R-NEW-1, R-NEW-2, and R-NEW-3 were received by I.W4 `864e882a`, I.W1 `35773c4f`, and I.W5 `73c40fa4` respectively (`docs/tranches/I/FINAL.md`). |
| `I` | Bring the whole library to a stated steady state and dispose all 21 chronic-deferral rows, including alias/token retirement and a single dock keep-open authority (`docs/tranches/I/I.md`). | 21/21 chronic rows dispositioned; 44 retirements and the dock authority refactor landed (`docs/tranches/I/FINAL.md`; `docs/tranches/I/audit/I-audit-α-plan-vs-actual.md`). | `35773c4f` performs the main trim and retires `--accent-pink`; `987fc415` collapses dock keep-open authority; `950d1f4a` closes I. | The dock result survives through `src/components/dock/composables/dockContext.ts`, `useDockHold.ts`, and `src/components/slider/Slider.vue`. The literal steady-state claim does not: `--accent-pink` is live again in `src/styles/tokens/color-radius.css`, `light-dark.css`, `dark-arm.css`, and `src/styles/theme/bridges.css`. | **HALF-LANDED** | **NONE.** The token's later return is new source history, not a still-routed I row; I named no successor for it. |
| `IOS27-MICRO` | Converge the design-development loop, then execute FINAL W0-W7 in order (`docs/tranches/IOS27-MICRO/IOS27-MICRO.md`; `docs/tranches/IOS27-MICRO/FINAL/FINAL.md`). | Development closed by owner adjudication; W0 banked 6 PASS, 1 DEFER, 0 FAIL, while the joint execution cursor retained W1-W7 (`docs/tranches/IOS27-MICRO/FINAL/W0-PAINT-LEDGER.md`; `docs/tranches/BJ/EXECUTION-PROGRESS.md`, P-EX3). | `c107a9c4` and `e75b493f` close the development loop; `1d0c17c6` banks W0; `e374b3ad` later lands the W1 spring header and panel/orb-drop registers. | `src/styles/tokens/scheme-spring.css` contains the dock, panel, and orb-drop registers from W1. No source evidence closes W2-W7; the execution cursor still marks P-EX3 `QUEUED`. | **HALF-LANDED** | **`docs/tranches/BJ/EXECUTION-PROGRESS.md`, P-EX3.** The record queues W1-W7 and W0 row-7 re-drain in W7; current source reduces the unlanded set to W2-W7, but W1 still needs ledger reconciliation. |
| `J` | Extract a shared Configurator primitive and refit Aurora/metaballs consumers onto it as part of the tranche's gestalt repair (`docs/tranches/J/J.md`, W4). | Configurator, consumers, stories, and close gates landed; StoryChassis was explicitly declined for lack of substrate rather than silently deferred (`docs/tranches/J/FINAL.md`). | `499326a8` adds `Configurator`, its layers/rows/state helper, and the Aurora/metaballs consumers. | `src/components/configurator/` still contains the primitive family; `demo/examples/ConfiguratorExample.vue` consumes it; `tests/configurator-recursion.spec.ts` protects nested behavior. | **LANDED** | **NONE.** The StoryChassis idea was rejected by J's evidence guard and was not emitted as successor debt. |
| `K` | Promote the primary “audacious” action into `Button variant="primary-audacious"` plus `btn-audacious`, then migrate the dock consumers (`docs/tranches/K/K.md`, W6). | Variant, utility, and three consumers landed; the separate vueuse SCC trap remained open after additive Phase 1 (`docs/tranches/K/FINAL.md`). | `154d1d2e` lands the headline extraction; `a598b903` records the degraded Phase-1 publication result. | The exact action mechanism is retired. `src/components/dock/styles/controls/tab-button.css` states that the `btn-audacious` utility and auto-attachment are gone; fresh `rg` finds no live `primary-audacious` variant. | **HISTORICAL-ONLY** | **NONE.** K's non-headline SCC Phase 2 was received and closed by L.W1 in `d1de94b3` and verified by `fa6e6c7e` (`docs/tranches/L/FINAL.md`). |
| `L` | Close the vueuse SCC trap with a curated vueuse-free root and explicit heavy-peer subpaths (`docs/tranches/L/L.md`, W1). | Phase 2 removed heavy-peer-bearing root edges; root, type, package, and consumer proof closed (`docs/tranches/L/FINAL.md`). | `d1de94b3` lands the curated barrel and subpath split; `fa6e6c7e` records cross-repo SCC closure proof. | `src/index.ts` still states and implements a vueuse-free and keyframes-free curated root; its `@vueuse/core` occurrences are explanatory comments, while heavy-peer surfaces remain in explicit subpaths in `package.json`. | **LANDED** | **NONE.** M received the consumer standardization follow-up and closed it (`docs/tranches/M/FINAL.md`). |
| `M` | Standardize the constellation consumers on the v1.0 explicit-subpath publication surface (`docs/tranches/M/M.md`, W1). | Five consumers migrated across about 93 import sites; speedtest verified as no-op; per-consumer proofs recorded (`docs/tranches/M/FINAL.md`; `docs/tranches/M/audit/W1-Lane-*-proof.md`). | Glass orchestration commit `0e0a9a9f`; consumer commits keyframes `b788205`, fourier-analysis `301a95e`, words `0f16925`, and bbnf-buddy `e06d629`; the value.js SHA is omitted from `M/FINAL.md`. | Explicit subpath imports remain in `keyframes.js/demo/components/instrument/shell/EditorShell.vue`, `value.js/demo/color-picker/composables/boot/useAtmosphere.ts`, `fourier-analysis/web/src/App.vue`, `words/frontend/src/components/custom/wordlist/views/WordListView.vue`, and `bbnf-buddy/src/editor/components/dock/BottomDock.vue`. This spot-check proves the import shape, not present-day package compatibility. | **LANDED** | **NONE.** M's N-prefixed follow-ups were subsequently absorbed through N, O, and P; none remains an M-owned receiver (`docs/tranches/N/FINAL.md`; `docs/tranches/P/FINAL.md`). |
| `N` | Replace an over-pruning proposal with evidence-backed strategic wiring; the load-bearing wire connects `useTouchGate` to `Slider` (`docs/tranches/N/N.md`, W0). | Five strategic wires and follow-on density work landed; the prune-first audit error was recorded (`docs/tranches/N/FINAL.md`). | `b6c1eed1` lands the five-wire batch; `ffc02a9c` lands Configurator density and the dock-blur no-op ruling. | `src/components/slider/Slider.vue` imports and instantiates `useTouchGate`; the same file consumes optional dock context. `src/components/dock/composables/useDockTouchGate.ts` also retains the shared gate. | **LANDED** | **NONE.** N's O-prefixed rows were received by O and any remaining inheritance was dispositioned by P (`docs/tranches/O/FINAL.md`; `docs/tranches/P/FINAL.md`). |
| `O` | Replace dock string-key and dual-provider seams with typed dependency injection and paired strict/optional helpers; migrate all five consumers (`docs/tranches/O/O.md`, W2). | Typed context, helper pairs, five consumer migrations, and transitional-provider removal closed (`docs/tranches/O/FINAL.md`). | `ba546c7f` adds the typed context and dual-provide window; `7dce6452` migrates consumers and removes the transition. | `src/components/dock/composables/dockContext.ts` exports typed `provideDockContext`, strict `useDockContext`, and `useOptionalDockContext`; `GlassDock.vue`, `DockSeparator.vue`, `DockLayerGroup.vue`, and `Slider.vue` consume that authority. | **LANDED** | **NONE.** O's P rows and cross-repo rows are individually dispositioned in `docs/tranches/P/FINAL.md` §2. |
| `P` | Exit with zero P residuals while promoting `GlassScrubber`, the ProgressiveSidebar slotted chassis, `PaperBackdrop`, and later `MetricRow` (`docs/tranches/P/P.md`; `docs/tranches/P/FINAL.md`). | All 38 inherited items received terminal dispositions; W3/W5 promotions landed; close declared zero P residuals (`docs/tranches/P/FINAL.md`). | `df0e7e7e` lands the three W3 promotions; `f286ceaf` extends `MetricRow`; `9f774b48` records the 13-lane audit and P close at v1.8.4. | `src/components/paper-backdrop/` and `src/components/metric/MetricRow.vue` remain live with contract tests. The exact `GlassScrubber` variant is absent, and `tests/public-surface.spec.ts` records that the `ProgressiveSidebar` SFC is gone. | **HALF-LANDED** | **NONE.** P's archives are terminal dispositions, not successor work; `P/FINAL.md` declares zero P-owned residuals. |

## Counts

- `LANDED`: **10**
- `HALF-LANDED`: **3**
- `HISTORICAL-ONLY`: **2**
- `REJECTED`: **0**
- `DROPPED`: **0**
- Total physical directories: **15**

## Unresolved facts

- `D-II` has no directory-local `FINAL.md`; `PROGRESS.md` still says commit/tag pending even though `7289c0c7` landed both source and `d-close`.
- `F/FINAL.md` accepts five unrelated P3 residuals but names no exact receiver.
- `IOS27-MICRO` W1 source landed in `e374b3ad`, while BJ P-EX3 still queues W1-W7. No inspected record reconciles that cursor.
- `M/FINAL.md` says the value.js consumer commit landed but omits its SHA.
- M's sampled sibling sources retain explicit subpath imports, but this audit did not establish that every imported subpath is exported by current glass-ui 7.x or rerun all five consumer builds.
- Current-source checks used the glass-ui worktree at `5841dd35`; unrelated dirty BJ audit work was present and was not modified.
