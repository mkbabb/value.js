# SHADCN / LIBRARY CENSUS — excavation seat C (M-14)

**Seat.** `C:shadcn-library-census`, Opus banausic band, value.js mega-tranche PHASE X excavation.
**Model observed.** `claude-opus-5[1m]` (Opus 5, 1M context) — declared in the session system prompt:
*"You are powered by the model named Opus 5 (1M context). The exact model ID is claude-opus-5[1m]."*
`CLAUDE_MODEL` and `ANTHROPIC_MODEL` are both unset in env (`echo "CLAUDE_MODEL_ENV=${CLAUDE_MODEL:-unset} ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-unset}"` → `CLAUDE_MODEL_ENV=unset ANTHROPIC_MODEL=unset`), so the prompt declaration is the receipt.

**Subject.** `/Users/mkbabb/Programming/value.js` @ `5c13465d`, branch `tranche-u`, measured 2026-07-27 18:30–19:00 EDT. READ-ONLY: no file outside `docs/tranches/V/megatranche/excavation/` was written or modified.

**Standing edict under test.** FULL shadcn abrogation — components **and** style — toward glass-ui.
`docs/tranches/V/megatranche/SCOPE.md:146` verbatim: *"library into an apotheosis; **full shadcn abrogation** (components AND style); consolidation or"*.
Canon: `docs/tranches/V/ARCHITECTURE.md:39` verbatim: *"There is no `panes/` dumping ground, `demo/@`, TS/Vite project alias, `@src`, or one-line glass-ui forwarding directory."*

**Relation to the adjudicated corpus.** `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` (18,830 lines) already rules the `demo/ui/**` shim layer at **seven independent rows** (:3003, :4958, :5048, :5354, :5408, :5652, :8858) and the palette-browser seam at :5922 / :921 / :13378. **Those rows are cited, never re-argued.** Every row below is marked `RULED (cite)` or `NEW`. `NEW` means: `grep -c` over `DEFECT-LEDGER.md` + `ROOT-FINDINGS.md` returned **0** for the row's identifying token.

**Row count: 35** (§1 = 12, §2 = 11, §3 = 12).

---

## §1 — SHADCN ABROGATION STATUS

### 1.0 Where the tree actually lives

The shadcn tree is **not** at `demo/@/components/ui/` (project memory's location — stale). `demo/@` **does not exist**:

```
$ ls demo/@
ls: demo/@: No such file or directory
$ git log --oneline -15 -- demo/ui
f2c8f565 feat(v-w44)!: adopt @mkbabb/glass-ui 7.0.0 across the demo consumer surface
a61094e3 feat(v-w43b3)!: home the feature UI trees; demo/@ dies (D-c)
```

The tree is `demo/ui/` — 19 directories, and there is a second, unrelated `demo/shared/ui/` (2 app-owned SFCs).

### 1.1 File count — the component half is COMPLETE

```
$ find demo/ui -type f | wc -l          → 19
$ find demo/ui -type f -name "*.vue"    → 0
$ find demo/ui -type f -name "*.ts"     → 19
$ grep -c "^export" demo/ui/*/index.ts | awk -F: '{s+=$2} END {print s}'  → 20
```

**Not one shadcn-vue SFC survives.** All 19 files are `index.ts` re-export barrels; 20 export statements total (only `alert/` has two, plus a 9-line survival comment). This is the strongest single finding of the seat: the *component* half of the abrogation edict is **discharged**. What remains is a naming layer.

| # | Row | Status | Disposition |
|---|---|---|---|
| **A-01** | `demo/ui/**` = 19 dirs × 1 file, zero implementations, pure glass-ui forwarding | **RULED** — DEFECT-LEDGER :3003, :4958, :5048, :5354, :5408, :5652, :8858 all reach the same cure | **PRUNE (delete `demo/ui/` whole)** — carried, not re-argued |

### 1.2 Per-component usage census (repo-wide import census)

Method (exact, reproducible):

```
$ grep -rEn 'from "(\.\./)+ui/[a-z-]+"' demo --include='*.vue' --include='*.ts' | grep -v '^demo/ui/' | wc -l   → 90
$ grep -rEl 'from "(\.\./)+ui/[a-z-]+"' demo --include='*.vue' --include='*.ts' | grep -v '^demo/ui/' | wc -l   → 48
$ grep -rEn 'from "(\.\./)+ui/[a-z-]+"' src test e2e --include='*.vue' --include='*.ts' | wc -l                 → 0
```

Reconciles exactly with DEFECT-LEDGER :5415 (`90` statements / `48` files) and :5235 (`48`, correcting an earlier `35`).

| barrel | import statements | glass-ui target | forwards through |
|---|---:|---|---|
| `button` | **22** | `Button` | **root barrel** |
| `card` | 12 | `Card`+5 | root barrel |
| `badge` | 7 | `Badge`, `badgeVariants`, `BadgeVariants` | root barrel |
| `popover` | 7 | `Popover`+2 | root barrel |
| `select` | 6 | `Select`+7 | root barrel |
| `slider` | 5 | `Slider` | root barrel |
| `dropdown-menu` | 4 | `DropdownMenu`+13 | root barrel |
| `input` | 4 | `Input` | **`@mkbabb/glass-ui/forms`** (sole subpath reach) |
| `skeleton` | 4 | `Skeleton` | root barrel |
| `tooltip` | 4 | `Tooltip`+3 | root barrel |
| `dialog` | 3 | `Dialog`+7 | root barrel |
| `separator` | 3 | `Separator` | root barrel |
| `alert` | 2 | `Alert`+2, `AlertVariants` | root barrel |
| `avatar` | 2 | `Avatar`+2 | root barrel |
| `checkbox` | 2 | `Checkbox` | root barrel |
| `radio-group` | 2 | `RadioGroup`+1 | root barrel |
| `collapsible` | 1 | `Collapsible`+2 | root barrel |
| **`label`** | **0** | `Label` | root barrel |
| **`switch`** | **0** | `Switch` | root barrel |
| | **Σ 90** | | |

| # | Row | Evidence | Status | Disposition |
|---|---|---|---|---|
| **A-02** | `demo/ui/label/` — **zero consumers repo-wide**. `Label` appears nowhere in `demo/` outside its own barrel. | `grep -rEn 'from "(\.\./)+ui/label"' demo → 0 hits`; `grep -rn -E "\bLabel\b" demo` outside the barrel yields only `SelectLabel`/`DropdownMenuLabel`/`aria-label` | **NEW** (`grep -c "ui/label"` over ledger+root-findings → 0) | **PRUNE** — dead even under the "keep the barrels" counterfactual |
| **A-03** | `demo/ui/switch/` — **zero consumers repo-wide**. The only `Switch` tokens in `demo/` are the prose strings "Switch account" in 3 templates. | `grep -rEn 'from "(\.\./)+ui/switch"' demo → 0`; `demo/shell/dock/menus/MobileMenuDropdown.vue:56`, `ProfileSection.vue:81`, `palettes/browser/slug/PaletteSlugBar.vue:102` are the 3 prose hits | **NEW** | **PRUNE** |
| **A-05** | 18 of 19 barrels forward through the **root** `@mkbabb/glass-ui` barrel; only `input` names a subpath (`/forms`). | `demo/ui/input/index.ts:1` `export { Input } from "@mkbabb/glass-ui/forms";` vs `demo/ui/button/index.ts:1` `export { Button } from "@mkbabb/glass-ui";` | **RULED** — DEFECT-LEDGER :5354/:5361 measures the root barrel at 42 chunks / 167,733 B | **PRUNE with A-01** |

### 1.3 Styled-vs-stock

**Every one of the 19 is STOCK. Zero are STYLED.** Verified by a negative sweep: a loop over all 19 files counting lines that are neither a comment nor an `export … from "@mkbabb/glass-ui…"` prints **nothing** — no wrapper SFC, no `cva`, no default prop, no class merge (this negative also appears at DEFECT-LEDGER :5659, independently reached).

| # | Row | Evidence | Status | Disposition |
|---|---|---|---|---|
| **A-04** | **STOCK = 19/19; STYLED = 0/19.** The `alert` barrel's own 9-line header records the last styled one dying: *"This barrel previously held a local shadcn-vue re-implementation (Alert/AlertTitle/AlertDescription SFCs + a local `alertVariants` cva) that duplicated a primitive glass-ui ships… B.W2 … converted it to a re-export"* (`demo/ui/alert/index.ts:3-9`) | see above | **NEW** (the styled/stock split is not stated anywhere in the ledger) | **KEEP-as-finding** — the styling half of the edict is discharged *inside* `demo/ui/`; the residue is at call sites (A-06/A-07) and in `demo/shared/utils.ts` (A-08) |

### 1.4 The style residue that survived — where the abrogation is NOT complete

| # | Row | Evidence | Status | Disposition |
|---|---|---|---|---|
| **A-06** | **49 dead shadcn `variant=` attributes on `Button`.** glass-ui 7 `Button` speaks `emphasis` × `tone` × `size`; it has **no** `variant` prop, so the attribute leaks to the DOM as a literal. Measured by receiving tag: `Button 49` (DEAD), `WatercolorDot 8` (LIVE), `Badge 7` (LIVE), `SelectTrigger 1` (LIVE) = 65 shadcn-vocabulary attrs, 49 of them inert. Producer proof: `grep -oE "(emphasis\|tone\|variant\|size)\??:" node_modules/@mkbabb/glass-ui/dist/components/button/Button.vue.d.ts` → `emphasis: / emphasis?: / size: / size?: / tone: / tone?:` — **no `variant`**; the same probe on `badge/Badge.vue.d.ts` → `variant?: BadgeVariants['variant']` (so Badge's 7 are legitimate). Against this, the glass-7 vocabulary appears **5 times** total: `grep -rn -E '\bemphasis="\|\btone="' demo \| wc -l → 5`. | **RULED at the class level** — DEFECT-LEDGER :4940 / :5210 ("51 dead `variant` props fleet-wide"). **Extension:** my per-receiver decomposition (49 Button / 8 WatercolorDot / 7 Badge / 1 SelectTrigger) narrows the ledger's undifferentiated 51 and shows 16 of the flagged attrs are in fact valid producer props | **CONSOLIDATE-INTO-glass-7-vocabulary** (`variant`→`emphasis`×`tone` on Button only; leave Badge/SelectTrigger/WatercolorDot untouched) |
| **A-07** | **74 per-instance class overrides on producer primitives.** `grep -rn --include='*.vue' -oE '<(Button\|Badge\|Card\|CardHeader\|CardContent\|Input\|SelectTrigger\|SelectContent\|PopoverContent\|DialogContent\|Skeleton\|Separator\|Slider\|Tooltip[A-Za-z]*)[^>]*class="[^"]+"' demo \| wc -l` → **74**. Sample: `demo/workbenches/gradient/GradientPane.vue:20` `<Card tier="resting" class="pane-scroll-fade w-full overflow-y-auto overflow-x-hidden min-w-0 h-full"`; the identical 6-utility string recurs at `MixPane.vue:62` and `GeneratePane.vue:31`. | **NEW** as a measurement (edict-5 per-instance-override rows exist singly in the ledger; the fleet count does not) | **CONSOLIDATE-INTO-glass-ui** for the recurring pane-shell string (3+ identical instances = a producer `tier`/variant, not a call-site utility bag); **KEEP** genuinely local one-offs |
| **A-08** | **`cn()` is dead shadcn boilerplate with zero consumers, and it is the sole reason two devDependencies exist.** `demo/shared/utils.ts:1-6`:<br>`import { type ClassValue, clsx } from 'clsx'`<br>`import { twMerge } from 'tailwind-merge'`<br>`export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }`<br>Consumers: `grep -rn --include='*.vue' --include='*.ts' -E "[^a-zA-Z]cn\(" demo src test e2e` → **no output**. Dependency orphan: `grep -rn "clsx\|tailwind-merge" --include='*.ts' --include='*.vue' --include='*.js' --include='*.json' . --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=docs` → `package.json:99` + `package.json:108` + `package-lock.json` + **only** `demo/shared/utils.ts:1,2,5`. Forensic tell: lines 1-6 are single-quoted, semicolon-free, 2-space-indented — untouched shadcn-vue generator output sitting above a 4-space, double-quoted, semicolon-terminated house-authored `debounce`. | **NEW** (`grep -c "cn("`, `"clsx"`, `"tailwind-merge"` over ledger+root-findings → **0/0/0**) | **PRUNE** `cn()` + drop `clsx@^2.1.1` and `tailwind-merge@^3.6.0` from devDependencies. This is the last executable line of shadcn-vue in the repo. |
| **A-09** | **`components.json` — the shadcn-vue CLI config, still at the repo root, pointing at three dead targets.** Untouched since the repo was created: `git log --oneline -5 -- components.json` → a single commit, `35cd9d5d Initial move to new repo 🎉`. Its three path claims: `"components": "demo/@/components"` → `ls demo/@` = **No such file or directory**; `"config": "tailwind.config.ts"` → `ls tailwind.config.*` = **no matches found**; `"css": "@styles/style.scss"` → `find . -name "*.scss"` (excl. node_modules/.git) = **empty**, and the `@styles` alias was killed at W43·RF-15 (`vite.config.ts:68` verbatim: *"W43 (RF-15) killed the demo `@…` path aliases"*). Live hazard: `npx shadcn-vue add <x>` still resolves this file and would write SFCs into a nonexistent tree against a nonexistent Tailwind config. | **NEW** (`grep -c "components.json"` over ledger+root-findings → **0**) | **PRUNE** — deleting it is the formal act that makes "shadcn abrogated" true at the tooling layer, not just the source layer |
| **A-10** | **The shadcn-*named* design tokens are the PRODUCER's contract, not demo residue — do not mistake them for un-abrogated shadcn.** `--foreground` &c. are defined in `node_modules/@mkbabb/glass-ui/dist/styles/tokens/{light-dark,dark-arm,color-radius}.css` and `dist/styles/glass/ladder.css` (`grep -rlo -- "--foreground:" node_modules/@mkbabb/glass-ui/dist/`). The demo re-points exactly **three**: `demo/styles/foundation.css:251-252` (`--primary: var(--accent-view); --primary-foreground: var(--background);`) and `:735,:738` inside `@media (prefers-contrast: more)`. | **NEW** (negative finding, recorded to prevent a false positive being filed) | **KEEP-with-reason** — glass-ui inherited shadcn's token *naming*; renaming them is a producer concern (M-7: BJ is not ours), and the 3 demo re-points are deliberate accent plumbing |
| **A-11** | **The ESLint boundary rules that were supposed to police the demo import graph are inert.** `grep -n "demo/@" eslint.config.js` → `:235,:236,:237,:238` (file globs `demo/@/components/**`, `demo/@/lib/**`) and `:275,:276` (`demo/@/composables/**`) — every one of those trees was deleted at `a61094e3`. G-DEMO-1 and G-DEMO-3a are scoped **entirely** to `demo/@/composables/**` and therefore match zero files; G-DEMO-3b's surviving glob (`demo/color-picker/**`) bans the specifier group `@components/custom/palette-browser/**/*.vue`, and `grep -rn '@components' demo src --include='*.ts' --include='*.vue'` returns exactly **one prose comment** (`demo/palettes/browser/status/index.ts:5`) — the alias cannot occur. | **RULED** — DEFECT-LEDGER :921 and :13378 both reach this, with the same reproduction | **PRUNE the dead objects / RE-SCOPE the live intent to `demo/palettes/**`** — carried |
| **A-12** | **reka-ui — the radix successor — still has 4 direct type reaches from demo feature code, bypassing glass-ui.** `grep -rn 'from "reka-ui"' demo src` → `demo/workbenches/gradient/GradientVisualizer/GradientVisualizer.vue:28`, `demo/workbenches/mix/MixConfigBar.vue:15`, `demo/workbenches/generate/GenerateControls.vue:33`, `demo/scenes/atmosphere/AuroraPane.vue:25` — all four `import type { AcceptableValue } from "reka-ui"`. glass-ui declares reka-ui a peer and owns every Select; the demo naming reka-ui directly is the last structural trace of the pre-glass primitive layer. | **NEW** | **CONSOLIDATE-INTO-glass-ui** — glass-ui's `./select` should re-export the value type its own `Select` accepts; the demo should never name the headless library |

---

## §2 — CONTRIVANCE CANDIDATES (demo component tree)

Ruled rows are cited and closed. The census EXTENDS to areas the corpus has not reached.

| # | Row | Evidence | Status | Disposition |
|---|---|---|---|---|
| **C-01** | **`plugins/vite-defer-glass-fonts.ts` guards on a path that no longer exists — the glass-ui font corpus is never imported in DEV.** `plugins/vite-defer-glass-fonts.ts:72`: `if (!id.includes("demo/@/styles/style.css") \|\| !code.includes(MARKER)) { return null; }`. The marker now lives at `demo/styles/foundation.css:75` (`grep -rn "GLASS_FONTS_DEFERRED"` → exactly two hits: `demo/styles/foundation.css:75` and the plugin's own `MARKER` const at `:46`). No file in the repo has an id containing `demo/@/styles/style.css` (`find . -name "style.css"` excl. node_modules → **empty**; `demo/styles/` holds `animations/focus-ring/foundation/hljs/shell/utils.css`). The `transform` hook therefore **never fires**, and its dev branch — `const replacement = isBuild ? "" : '@import "@mkbabb/glass-ui/styles/fonts";'` — never runs. **Scope, measured honestly: DEV-ONLY.** `renderStart()` and `transformIndexHtml` are independent of `transform`, so the production/gh-pages path (emit `glass-fonts.css` + inject `<link media=print onload>`) is unaffected; in build the marker was going to be replaced by `""` anyway, and a CSS comment is equivalent. The break is that the dev server — *the surface the Playwright smoke suite runs against* — renders permanently in the metric-compatible `*-Fallback` faces. The file's own prose asserts the opposite: `demo/styles/foundation.css:69-71` verbatim *"the `deferGlassFonts` vite plugin … replaces the marker below with the real `@import` in DEV"*, and `plugins/vite-defer-glass-fonts.ts:33-36` verbatim *"serve (dev): REPLACE the marker with the real `@import` so the dev server renders the true faces exactly as before"*. | **NEW** (`grep -c "GLASS_FONTS_DEFERRED"` over ledger+root-findings → **0**; the one `vite-defer-glass-fonts` hit at DEFECT-LEDGER :2859 is an unrelated `ls plugins/` in a different row) | **PRUNE the stale guard** — repoint to `demo/styles/foundation.css`, or drop the id test entirely and gate on `code.includes(MARKER)` alone (which is already sufficient and cannot rot). Every visual capture taken against `npm run dev` since `a61094e3` was taken in fallback type. |
| **C-02** | **Eight one-file directories outside `demo/ui/`.** Enumerated by `for d in $(find demo -type d -not -path "*/public/*"); do …` (dirs with exactly 1 file and 0 subdirs): `demo/scenes/blob/` → `BlobPane.vue`; `demo/test/export/` → `byte-exact.test.ts`; `demo/platform/storage/` → `useSafeStorage.ts`; `demo/palettes/admin/` → `AdminPane.vue`; `demo/palettes/browser/dialog/composables/` → `useDialogBrowseActions.ts`; `demo/picker/controls/SpectrumCanvas/composables/` → `useSpectrumPlateStyle.ts`; `demo/workbenches/mix/composables/` → `useMixingState.ts`; `demo/workbenches/generate/composables/` → `useColorGeneration.ts`. Four of the eight are a `composables/` dir holding a single composable — the colocation idiom applied where there is nothing to co-locate. | **NEW** as an enumeration | **CONSOLIDATE** — collapse the four single-tenant `composables/` dirs into their parent (`useMixingState.ts` beside `MixPane.vue`, &c.). **KEEP** `demo/scenes/blob/`, `demo/palettes/admin/`, `demo/platform/storage/` — those are route/layer homes whose single occupancy is a fact about the product, not a decomposition tic |
| **C-03** | `demo/palettes/browser/index.ts` — 46-line self-declared "TOP-LEVEL SEAM" with **zero importers** | **RULED** — DEFECT-LEDGER :5922 (independently measured: *"has ZERO importers. All twelve real reaches go to sub-barrels"*) | **PRUNE** — carried |
| **C-04** | **`demo/palettes/browser/status/index.ts` is transitively dead, and its own header says so.** Its single "consumer" is `demo/palettes/browser/index.ts:46` — the barrel C-03 proves nobody imports. The real consumer bypasses it: `demo/palettes/browser/card/CurrentPaletteEditor.vue:193` `import ApiOfflineChip from "../status/ApiOfflineChip.vue";` (raw `.vue`, not the seam). The barrel's own comment concedes it: `demo/palettes/browser/status/index.ts:3-4` verbatim *"ApiOfflineChip's live consumer is CurrentPaletteEditor (internal, direct relative import)"*. So a barrel was authored, documented as bypassed, and wired only into a seam nobody enters. | **NEW extension** (C-03's ruled row names `slug/`, not `status/`) | **PRUNE** — delete with C-03; `CurrentPaletteEditor`'s direct import already works |
| **C-05** | **68 exported symbols in `demo/**/*.ts` have zero consumers anywhere in `demo/ + test/ + e2e/ + plugins/ + assets/ + vite.config.ts`.** Method: AST-ish regex over every `export (function\|const\|let\|class\|interface\|type\|enum) NAME` in the 162 demo `.ts` files, then a whole-word count of each name across the full consumer corpus excluding its own file. **12 are runtime values** (the strongest evidence — a dead type can still be structurally load-bearing, a dead const cannot): `createApiClient` + `API_CLIENT_KEY` (`demo/platform/transport/useApiClient.ts`), `composeTitle` (`demo/color-picker/router/useDocumentTitle.ts`), `smoothstep` + `VIVIDNESS_C_HI` + `VIVIDNESS_C_LO` (`demo/color-picker/composables/boot/atmosphere-calibration.ts`), `CSS_NATIVE_SPACES` (`demo/color-session/color-model.ts`), `isLoopbackHost` (`demo/platform/transport/availability.ts`), `channelMeta` (`demo/color-session/picker-color.ts`), `bezierLiteral` (`…/easing/easingCatalogue.ts`), `READOUT_CH` + `readoutCh` (`…/ColorComponentDisplay/readoutReservation.ts`), `SPECTRUM_LUMA_FLIP` (`demo/picker/controls/spectrumLuma.ts`), `ANON_SESSION_KEY` + `USER_TOKEN_KEY` (`demo/platform/auth/sessionToken.ts`). Each verified individually: `grep -rlnw --include='*.ts' --include='*.vue' --include='*.md' -- "$s" demo test e2e src assets` returns **only the defining file** for all twelve. | **NEW as a sweep.** Three of the twelve are already ruled for *other* defects and must not be double-booked: `CSS_NATIVE_SPACES` (DEFECT-LEDGER :11925, :13763 — ruled dead), `READOUT_CH` (:13730, :13786), `bezierLiteral` (:11152, :11154), `composeTitle` (:8791 — ruled for reading the query instead of the model, a different defect). The remaining **eight runtime rows are unruled.** | **PRUNE** the eight unruled runtime exports (or de-export where the symbol is used file-internally — `readoutCh`, `channelMeta`, `smoothstep`, `VIVIDNESS_C_*` are plausible file-locals wearing a public costume) |
| **C-06** | **The `*Deps` / `*Return` / `*Options` exported-interface idiom accounts for ~40 of C-05's 68.** Representative: `UseGradientInterpolationReturn`, `UseGradientCSSReturn`, `ImageSamplerDeps`, `LoupeCanvasDeps`, `InertiaGestureOptions`, `ImageQuantizeOptions`, `PaneRouterDeps`, `PaneActionRefs`, `UsePopupMutexOptions/Return`, `UseDockAdminModeOptions/Return`, `PalettePortsDeps` (+ `AdminPort`, `ColorTargetPort`, `LibraryPort`, `SessionPort`), `SwatchActionsDeps`, `DialogBrowseActionsDeps`, `MixingAnimationOptions`, `UseVersionHistory`, `UseAdminTags`, `UseTagEdit`, `UseAdminFlagged`, `UseAdminAudit`, `HydratedBootModel`, `BootSeedSource`, `UseViewAccentsOptions`, `OvertureBeats`. Every one is the parameter or return shape of a composable that exactly one call site invokes positionally. Exporting them buys nothing (TypeScript infers the return; the caller never names the Deps bag) and costs a public-API promise the tree does not keep. | **NEW** | **CONSOLIDATE** — drop `export` from the ~40 interface declarations in one mechanical pass; keep the interfaces (they document the shape at the definition site), retire only the promise |
| **C-07** | **`demo/shared/utils.ts` is a two-concept grab-bag sitting off the ratified `shared/` schema.** `ARCHITECTURE.md:31-33` enumerates `shared/` as exactly `ui/` ("only genuinely app-owned controls with 2+ consumers") and `content/` ("deliberately authored typed pedagogical snippets") — there is no `shared/utils.ts` slot, and there is no `shared/content/` directory in the tree at all (`find demo/shared -type f` → `ui/EmptyState.vue`, `ui/PaneHeader.vue`, `utils.ts`). The file holds a dead shadcn `cn` (A-08) plus the demo's one `debounce`, whose 7 importers all want only the latter (`grep -rn 'from "[^"]*shared/utils"' demo` → 7 files, **all** `import { debounce }`). | **NEW** for the schema-violation + grab-bag framing. The `debounce` symbol itself is ruled four ways (DEFECT-LEDGER :964 shared-instance collision, :1270/:1342 missing `.cancel()` on unmount, :8514 the false root-barrel rationale) — **not re-litigated** | **CONSOLIDATE** — after `cn` is pruned (A-08), the file is one function; home it where the ruled `debounce` rows land it. Also **BOOK** the missing `shared/content/` against the ruled `assets/docs/` row (DEFECT-LEDGER :6433) |
| **C-08** | Two test roots (`test/` + `demo/test/`), split not by subject: 10 of `test/`'s 21 files import `../demo/` | **RULED** — DEFECT-LEDGER :15070, :15077, :15082 | **CONSOLIDATE-INTO-`demo/test/`** — carried |
| **C-09** | `demo/shell/PaneSegmentedControl.vue` — a wrapper over `@mkbabb/glass-ui/tabs` `SegmentedTabs` whose only additions are a `v-if` guard, a number↔string adapter, and a `:deep(.segmented-tab)` media-query restyle of a producer internal | **RULED** — 12 hits in DEFECT-LEDGER | **CONSOLIDATE-INTO-glass-ui** — carried. Note for the record: the file's own comment claims the compact variant "compacts AT THE ROOT (never per-instance)" while the "root" it names is the demo wrapper, not the producer |
| **C-10** | **`demo/scenes/about/katex/index.ts` is NOT dead — recorded to prevent a false positive being filed.** A `demo`-scoped grep shows zero `Katex` importers, which reads as a dead 1-line barrel. It is not: the eleven consumers are markdown, outside every source tree. `grep -n 'import' assets/docs/*.md` → 11/11 identical `import { Katex } from "../../demo/scenes/about/katex";` (hex/hsl/hsv/hwb/kelvin/lab/lch/oklab/oklch/rgb/xyz `.md:2`). | **NEW** (negative). The *location* of `assets/docs/` is separately ruled at DEFECT-LEDGER :6433 | **KEEP-with-reason** — the barrel is the only stable specifier the `.md` files can name; it earns its existence |
| **C-11** | **`createApiClient` + `API_CLIENT_KEY` are dead today but RESERVED by a proposed cure — do not prune blind.** Both have zero consumers (C-05). But DEFECT-LEDGER :50 proposes exactly the wiring that revives them: *"Create demo/color-picker/main.ts holding createApp + `app.use(router)` + `app.provide(API_CLIENT_KEY, createApiClient())`…"*. Their deadness is a symptom of the missing `main.ts`, not of a redundant seam. | **NEW nuance** on a ruled cure | **KEEP-pending-C-11's-parent** — sequence after the `main.ts` row lands; prune only if that cure is retired |

---

## §3 — `src/` LIBRARY SURFACE

The library is 26 `.ts` files / 4,654 lines across `color/ css/ foundation/ transform/ subpaths/` + 4 roots. The published surface is exactly 7 subpaths (`./color ./value ./css ./easing ./math ./transform ./quantize`) enumerating **141 symbols** — 79 runtime, 62 type.

**Consumer universe measured:** `value.js/{demo,test,e2e}` + `keyframes.js/{src,demo}` + `glass-ui/src`. `fourier-analysis`, `sci-report`, and `parse-that` import **zero** value.js specifiers (`grep -rn '@mkbabb/value.js' <repo>/src <repo>/demo` → no output for all three).

Subpath uptake (`grep -rn --include='*.ts' --include='*.vue' -oE '@mkbabb/value\.js[^"'"'"' ]*'`):

| subpath | keyframes.js | glass-ui | value.js demo |
|---|---:|---:|---:|
| `/css` | 36 | 3 | live |
| `/math` | 35 (+3 in prose) | 0 | 0 |
| `/value` | 18 | 0 | 0 |
| `/easing` | 11 | 1 | live |
| `/color` | 8 | 6 | live |
| `/transform` | 2 | 0 | 0 |
| `/quantize` | **0** | **0** | 3 files |

| # | Row | Evidence | Status | Disposition |
|---|---|---|---|---|
| **L-01** | **18 published TYPE exports have zero consumers anywhere in the constellation** (not even in `test/`): `ChannelsBySpace`, `CollectedRule`, `CustomFunctionRule`, `DecomposedMatrix3D`, `EasingIssue`, `KeyframesBlock`, `Mat4`, `PathSample`, `PropertyRule`, `QuantizeIssue`, `RgbGamut`, `ScrollTimelineDescriptor`, `ScrollerKeyword`, `StyleRule`, `TimelineAxis`, `TimelineScopeValue`, `ViewInset`, `ViewTimelineDescriptor`. Zero runtime exports are in this state. | **NEW** | **KEEP-with-reason (most) / PRUNE (some).** Honest limit: a type can be structurally load-bearing without being *named* (`StyleRule` is a member of the consumed `Stylesheet`), so this is weaker evidence than a dead function. **PRUNE candidates where the parent is also unconsumed:** `Mat4`, `DecomposedMatrix3D`, `PathSample`, `QuantizeIssue`, `EasingIssue` — see L-02/L-08 |
| **L-02** | **The `decompose.ts` half of `/transform` — 609 lines, 6 runtime exports — has no consumer outside value.js's own test suite.** `decomposeMatrix2D` (test=6), `decomposeMatrix3D` (30), `recomposeMatrix2D` (3), `recomposeMatrix3D` (9), `slerp` (5), `interpolateDecomposed` (9) — **all** `demo/e2e=0, keyframes=0, glass-ui=0`. The subpath's *other* half is genuinely consumed: keyframes' 2 `/transform` imports are both `PathGeometry` (`keyframes.js/src/animation/svg/morph-svg.ts:45`, `morph-geometry.ts:18`), plus `getTotalLength` at `keyframes.js/src/animation/svg/draw-svg.ts`. `getPointAtLength` is test-only. So `src/transform/path.ts` (564 lines) earns the subpath; `src/transform/decompose.ts` (609 lines) is published, fully tested, and called by nobody. | **NEW** (the ledger's single `decomposeMatrix` hit at :50 is an unrelated cure) | **KEEP-with-reason (published-API stability) or PRUNE — needs an owner ruling.** State the choice honestly: this is a 609-line, 100%-tested, 0-consumer public API. Under M-10 ("no deferrals") it must terminate as *retire with rationale* or *keep as a declared standalone product surface* |
| **L-03** | **`dominantColor()` is a published API with zero call sites whose only mention in the constellation declares it wrong.** `grep -rlow "dominantColor" test demo e2e keyframes.js glass-ui` → `test/v4-quantize.test.ts`, `test/v4-c1.test.ts`, and `demo/workbenches/extract/composables/useExtractSession.ts` — and that third hit is a **comment**, `:10` verbatim: *"the library's `dominantColor()` re-quantizes and is the wrong tool"*. **Correction to the corpus:** DEFECT-LEDGER :11131 states *"src/subpaths/quantize.ts exports dominantColor/quantizePixels, consumed at demo/workbenches/extract/quantize-worker.ts:6-7, useExtractSession.ts:14, useImageQuantize.ts:9"* — those three sites import `quantizePixels`, `QuantizeOptions`, `QuantizedColor`. **None imports `dominantColor`.** | **NEW** | **PRUNE** — the sole informed consumer evaluated it and wrote down why it does not use it |
| **L-04** | **`parseCssValue` (singular) has zero consumers; `parseCssValues` (plural) has one.** `grep -rnw "parseCssValue" demo e2e keyframes.js/src glass-ui/src` → **no output** (test-only, 13 hits). `grep -rlnw "parseCssValues" …` → `keyframes.js/src/animation/compile/value-ast.ts`. Two published entry points one character apart, exported side by side at `src/css/index.ts` and `src/subpaths/css.ts`. | **NEW** (`grep -c 'parseCssValue\b'` over ledger+root-findings → **0**) | **CONSOLIDATE-INTO-`parseCssValues`** — a singular/plural pair distinguished only by arity is the classic near-name trap; if the singular is a genuine convenience, it belongs behind the plural, not beside it |
| **L-05** | **Two cubic-Bézier evaluators live in one library, mutually unaware.** Published: `src/foundation/math.ts:86` `deCasteljau` → `:99` `cubicBezier(t,x1,y1,x2,y2)` → `:105` `interpBezier` — a De Casteljau subdivision family. Private: `src/easing.ts:70` `bezierCoordinate(t,a,b)` (closed-form Bernstein) + `:75` `solveBezierX` (24-step bisection), used by `CubicBezier()` at `:116-127` and `easeInBounce` at `:91`. The `easing` module never imports `foundation/math` (`grep -rn 'from "\.' src` shows `src/easing.ts` importing only `./foundation/result`, twice). Neither `cubicBezier`, `deCasteljau`, nor `interpBezier` has a consumer outside `test/` (12/21/14 test hits, 0 elsewhere). | **NEW** for the duplication framing (the ledger's one `deCasteljau` hit is incidental) | **CONSOLIDATE-INTO-one-Bézier-core** — either `easing.ts` evaluates through `foundation/math`, or the published De Casteljau trio is retired as the redundant half. Two solvers means two numerical behaviours to keep in agreement, with no test asserting they agree |
| **L-06** | **`src/css/index.ts` and `src/subpaths/css.ts` are a stacked double barrel enumerating the identical 52 names by hand.** A set-diff of the two files' export lists: *in domain barrel but not published:* **none**; *published but not in domain barrel:* **none** — 52 = 52. `src/css/index.ts` has exactly **one** importer in the whole repo: `grep -rn 'css/index' src test` → `src/subpaths/css.ts:35` and `:56`. Sixty lines exist solely to be re-typed into fifty-six. | **NEW** | **CONSOLIDATE** — collapse to one barrel. Either `subpaths/css.ts` reaches `../css/{grammar,syntax,timeline,stylesheet,types}` directly (the domain barrel dies), or `subpaths/css.ts` becomes a single `export * from "../css/index"`. Two hand-maintained copies of one 52-name list is a drift generator with no reader |
| **L-07** | **`ColorFactory` is a dead re-export in the color domain barrel.** The same set-diff on the color pair shows one asymmetry: `src/color/index.ts` exports `ColorFactory`, `src/subpaths/color.ts` does not. `grep -rnw "ColorFactory" src test demo keyframes.js/src glass-ui/src` → **only** `src/color/model.ts:47` (declaration), `:101`, `:107` (internal use), and `src/color/index.ts:7` (the re-export). Nothing imports it from the barrel. | **NEW** | **PRUNE** the `src/color/index.ts:7` re-export line; the type stays where it is used, in `model.ts` |
| **L-08** | **`/quantize` is published to an empty room outside this repo.** Zero consumers in keyframes.js, glass-ui, fourier-analysis, sci-report, parse-that (`grep -rn "value.js/quantize" /Users/mkbabb/Programming/*/src /Users/mkbabb/Programming/*/demo` → only value.js's own 3 demo files). Its surface is `quantizePixels` (live, 3 demo sites) + `dominantColor` (L-03, dead) + 3 types of which `QuantizeIssue` is unconsumed (L-01). | **NEW** | **KEEP-with-reason** — `quantizePixels` is the demo Extract workbench's real engine (`demo/workbenches/extract/quantize-worker.ts:6`) and the subpath is what keeps the worker off the root barrel. Trim it to `quantizePixels` + `QuantizeOptions` + `QuantizedColor` |
| **L-09** | **`src/foundation/` is a misnomer: nothing in the library is founded on `math.ts`.** `src/foundation/math.ts` has exactly **one** importer across the whole of `src/`: `src/subpaths/math.ts`. (Contrast its sibling `src/foundation/result.ts`, 6 lines, with **10** internal importers — a real foundation.) The interpolation primitives `clamp/scale/lerp/lerpArray/logerp` are not used by `color/operations.ts` (which does the library's actual color interpolation) nor by `transform/`, which hand-rolls its own clamp at `src/transform/path.ts:523` (`Math.min(Math.max(length, 0), total)`). | **NEW** | **KEEP-with-reason, RENAME the home.** `/math` is the second-most-consumed subpath in the constellation (**35 keyframes.js imports**) — this is a first-class product surface, not dead weight. But it is a *leaf export*, not a foundation; filing it under `foundation/` misdescribes the graph, and `path.ts:523`'s inline clamp is a small real consolidation |
| **L-10** | **The "bare `@mkbabb/value.js` has no `.` export" hazard remains LATENT, not live — a tempting false positive killed by measurement.** A prefix-greedy grep over keyframes.js appears to show 15 bare-specifier reaches. Enumerated exactly (`grep -rn -oE '@mkbabb/value\.js[^"'"'"' ]*'`), all 15 are **prose**: 14 × `` `@mkbabb/value.js` `` and 1 × `` `@mkbabb/value.js`) `` inside doc comments. `grep -rn 'from "@mkbabb/value\.js"' keyframes.js/{src,demo} glass-ui/src` → **no output**. No consumer in the constellation writes the bare specifier. | **RULED** as a latent defect — DEFECT-LEDGER :5455, :8514, :13367, :15059, :18178 (:18178 already states *"NONE — latent, not live"*). This row **confirms** that verdict across the *sibling repos*, which the ruled rows scoped to `demo/` only | **KEEP-as-latent** — carried; the ruled cure (add a `.` export or delete the two prose claims) stands unchanged |
| **L-11** | `/math` — 35 keyframes.js imports; `/css` — 36 keyframes + 3 glass-ui; `/value` — 18 keyframes; `/color` — 8 + 6; `/easing` — 11 + 1. Five of seven subpaths carry real cross-repo load. | **NEW** (baseline measurement) | **KEEP-with-reason** — the published seam is doing its job; any consolidation must not disturb these five |
| **L-12** | `src/value.ts` — 36 lines: 4 types (`CssScalar/CssCall/CssList/CssValue`) + `isLayoutTrackingUnit` + a 40-entry `LAYOUT_UNITS` set. Thin enough to read as a candidate for absorption into `css/`. It is not: `/value` is keyframes.js's 3rd-heaviest subpath (18 imports) and `isLayoutTrackingUnit` has 3 real consumers (`keyframes.js/src/animation/waapi/eligibility.ts`, `resolve/browser.ts`, `compile/interp-slot.ts`). | **NEW** (negative) | **KEEP-with-reason** — a small module is not a contrived one when its consumers are external and load-bearing |

---

## Closing marks

1. **The component half of the shadcn edict is already discharged** — zero shadcn-vue SFCs remain (§1.1). What the corpus has been calling "shadcn abrogation" for seven ledger rows is a **naming layer**, not a component layer. The framing matters for the wave spec: the cure is a codemod over 90 import statements in 48 files with zero runtime delta, not a component migration.

2. **The last executable shadcn line in the repo is `cn()`** (A-08) — six lines in `demo/shared/utils.ts`, zero consumers, keeping two devDependencies alive. Deleting it plus `components.json` (A-09) is the act that makes the edict literally true.

3. **The highest-severity NEW finding is not a contrivance at all** — it is C-01, a stale path guard left behind by `demo/@`'s death that has been silently serving fallback typefaces to the dev server (and therefore to the Playwright smoke suite, and to every visual capture taken against `npm run dev`) since commit `a61094e3`.

4. **`src/` is materially healthier than `demo/`.** Zero published runtime exports are unreachable at the name level; the real rows are one 609-line 0-consumer half-subpath (L-02), one dead-by-its-own-consumer's-admission function (L-03), one duplicated Bézier core (L-05), and one hand-copied 52-name double barrel (L-06).

**Row count: 35** — §1 A-01…A-12 (12), §2 C-01…C-11 (11), §3 L-01…L-12 (12).
Of these: **24 NEW**, **8 RULED (cited, not re-litigated)**, **3 NEGATIVE findings recorded to prevent false positives** (A-10, C-10, L-10 — L-10 also counted as RULED).
