# DESIGN-CANON-BRIEF — the M-14 clause 5 standing + the Phase E tri-fold brief

**Seat.** Fable CONTRIVANCE+CANON adjudicator, M-14 final aggregated pass. **Model observed:** `claude-fable-5`.
**Date** 2026-07-27 · **HEAD** `4f78e57b` (registers OM-11..OM-13; `git diff --stat c654824e..HEAD -- demo src` → empty, so the design-canon census's tree measurements remain live at this HEAD).
**Charter.** SCOPE.md M-14.5: *"GOLDEN GLASS · BREATH OF LIFE · MOVEMENT OF MOMENTUM are the named canon concepts; the excavation finds their canonical definitions and audits adherence"* + M-14.4: all design work tri-fold — *"Fable + Opus author independent designs (frontend design always uses the frontend-design plugin), then a Fable seat agglomerates into a choosy apotheosis, scrupulously."*

**Primary input:** `extracts/design-canon-census.md` (79 rows, Opus seat), adopted after this seat's own probe battery — one census claim REFUTED and corrected below (§3), the load-bearing rest re-verified against the tree this session (§1.4, §2, §4). Corroborating: `EXHORTATION-CENSUS.md` theme 15, `TRUTH-TABLE.md` ND-14 + §6 Phase-E verdict, `glass-ui/docs/tranches/BJ/addenda/2026-07-24-refinement/{GESTALT,ARCHAEOLOGY}.md` as cited by the census.

---

## §1 · THE THREE TERMS — canonical standing

### 1.1 GOLDEN GLASS — **UNDEFINED ANYWHERE IN THE CONSTELLATION. The top finding stands.**

The owner typed the phrase exactly once — `2026-07-27T22:28:17.882Z`, three hours before this formation, invoked as established law (*"Think of our GOLDEN GLASS…"*) — and no definition exists in value.js, glass-ui, keyframes.js, or any sibling (census §1.2, occurrence matrix §1.1: value.js's own hits are all seat prompts/folds of that same message). What exists is a **lineage**, not a law:

- the glass-ui BC tier usage — *"the GOLDEN GLASS PILL — a translucent `--glass-bg-floating` fill"* (`tests-visual/search-custom.spec.ts:5`; BC wave docs ×3);
- four warm/restrained owner "gold" registers (*"somesort of golden factor of 2-3x"*, *"should we make AI a subtle, subtle, gold?"* — `ARCHAEOLOGY.md:275`);
- the negative constraints (*not grey, not metallic — "That disgusting metallic wash needs to be abrogated on every single page." — not "trite, shiny, and bright"*);
- and value.js's own `golden` hits, which are **proportion arithmetic** (`61.8033989%` — now published as an O-10a token ask), not material.

A prior glass-ui Fable arbiter independently reached the same verdict (*"authors the law from a lineage, not from nothing"* — `GESTALT.md:26`). **Standing: ABSENT — Phase E authors a candidate definition FROM LINEAGE (material: warm-cream translucent glass at the floating tier; proportion: the φ register; restraint: the four-gold negative space) and carries it to the owner as a MARK for ratification. The owner alone ratifies a term he coined; Phase E may draft, never enact.**

### 1.2 BREATH OF LIFE — **defined by ONE owner sentence; HOMELESS in value.js**

Canonical sentence, owner-typed 2026-07-17T04:09:20.806Z (glass-ui session `e79fce3f`):

> *"Our edict: we suffuse the breath of life into our components with affordance and facility to always display engagement; within our aristotelian proporition in the abstract."*

Compressed statement of law (glass-ui IOS27-MICRO): *"every component always displays engagement"*, operationalized by the six engagement scalars (`--flex-vel`, `--motion-weight`, `--engage-t`, `--overpull`, `--impulse`, `--scrub-t` — `CORPUS-STILLS-A.md:16`). The owner's 2026-07-19T22:33 order binds it into value.js AND keyframes.js demos explicitly (*"suffusing the BREATH OF LIFE from glass-ui … In both kyframes.js and value.js's demos"*). Coverage: **0/4 value.js design authorities**; the only value.js-side chapter is `vnext/DESIGN-PROGRAM.md:170` — cited by nothing; keyframes.js repo-wide = 0 occurrences, so the relay to the named co-target never landed (ND-14).
**Standing: DEFINED-BUT-HOMELESS — Phase E folds the sentence verbatim as law §1 of the re-authored DESIGN.md, with the engagement-ladder reading (rest→hover→press→engaged→modal), and relays the keyframes half to that repo's inbox.**

### 1.3 MOVEMENT OF MOMENTUM — **defined, RENAMED, unregistered, and carve-blocked**

The real written law lives in the producer under another name — `glass-ui/DESIGN.md:113`, re-verified verbatim this session:

> *"the _Liquid Weight is Universal_ law: every **driver** motion … carries weight, inertia, bounce, and squish; it anticipates, overshoots, follows through, and travels an arc … Motion that snaps tight with no give — instant, fade-only, no settle — is the anti-pattern, not a style."*

Three defects, all re-verified: (a) **the rename** — grepping the owner's phrase finds nothing; (b) **unregistered** — `grep -c "DESIGN.md" glass-ui/scripts/lib/canon-doc.mjs` → **0** (re-run this session) against 17 registered canon keys; (c) **the unruled carve** — `:115` scopes liquid weight to DRIVERS (*"Liquid weight is universal on DRIVERS, not on every pixel that moves"*, re-read this session in the `--motion-weight` paragraph) while the owner's own words say *"movement in generality"* (2026-06-23). The prior glass arbiter escalated exactly this: *"MOMENTUM's driver-scope carve → owner mark, never a silent pick"* (`GESTALT.md:76`). The carve decides whether the law binds observer-class motion — which is precisely MT-F034's sub-pane half.
**Standing: DEFINED-AS-LIQUID-WEIGHT — Phase E cites `glass-ui/DESIGN.md:113` as the producer law, states the rename explicitly so the owner's phrase is greppable, and raises the carve as an OWNER MARK before F034-b picks any curve.**

### 1.4 Adherence audit — re-verified numbers

| probe (this session) | result |
|---|---|
| `momentum` in the 4 value.js design docs | **0 / 0 / 0 / 0** |
| `elevation` in the 4 docs | **0 / 0 / 0 / 0** |
| `sub-pane` in the 4 docs | **0 / 0 / 0 / 0** |
| `transition` in the 4 docs | 11 / **0** / **0** / **0** |
| cross-citations among the 4 docs + megatranche corpus | 0 in all 12 cells (census §0, adopted) |

All three terms score 0/4 in every value.js design authority. The census headline holds whole.

---

## §2 · THE DESIGN-MD RE-AUTHORING VERDICT

**YES — re-author, then WIRE. The deeper defect is that no single document is the design authority.** The census's three proofs are adopted, each re-spot-checked:

1. **Orphaned both directions** — the two live authorities (`demo/DESIGN.md`, `VISUAL-CONSTITUTION.md`) never cite each other; the megatranche's adjudicated corpus cites neither (0 in 12 cells). A law nothing reads is folklore with a filename.
2. **Anchored to a deleted tree** — 12 of 13 staleness rows defective. Re-verified this session: `find demo -name style.css` → **0** (7 line-anchors dead); GooBlob refs → **0** (cited twice as a live consumer); duration rungs live = exactly **5 of 9 documented** (`fast` 32 · `normal` 21 · `instant` 3 · `slow` 2 · `panel` 2 = 60 reaches, matching the census's S-4/S-5 to the digit).
3. **The owner's three named laws have 0 coverage** (§1.4).

**And the sharpest census reading is adopted as the re-authoring's organizing principle: the doc legislates APPEARANCE WITHOUT SEMANTICS.** Three of ten marks (OM-1/2/7) are shadow marks reducing to one missing paragraph — *what a shadow claims*; three more (OM-3/4/9) are derivation marks reducing to one missing idea — *magnitudes derive from a scale, never from slack*. Appearance-level rules cannot stop semantic defects, which is why the marks recur.

### The specific gaps the owner's marks prove (OM-1..OM-13)

| Mark | MT row | The owner's words | The missing law |
|---|---|---|---|
| OM-1 | MT-F026 | *"mark these inappropriate shadows on the dock items"* | **ELEVATION SEMANTICS**: a shadow is a claim about height above the surface behind it; in-chrome vs free-floating registers. `elevation` = 0 in all four docs (re-verified) |
| OM-2 | MT-F027 | *"shadow edge artifacts--sharp corners, on some cards"* | **SHADOW GEOMETRY**: multi-layer casts share one outer silhouette; `DESIGN.md:182` canonises the zero-blur recipe with no geometry constraint |
| OM-3 | MT-F029 | *"the spacing here … is too much"* | **VERTICAL RHYTHM**: intervals derive from the type scale, not leftover flex slack |
| OM-4 | MT-F030 | *"easing config is awful, too rounded in some areas, not rounded enough in others"* | **RADIUS DERIVATION**: how a nested register derives from its parent; when circle may sit beside rounded-rect |
| OM-5 | MT-F031 | *"this nonsense needs to be removed"* | **PRODUCT-vs-DIAGNOSTIC SURFACES**: dev diagnostics never render as product chrome |
| OM-6 | MT-F032 | *"The blob current color is not nearly vibrant enough"* | **CHROMA FIDELITY**: a wide-gamut → sRGB-clamp budget for procedural surfaces |
| OM-7 | MT-F033 | *"these buttons have incorrect shadows and the like"* | **ONE LIGHT MODEL** per surface — inner shadow, outer highlight, cast elevation agree on one source |
| OM-8 | MT-F034 | *"transitions between panes, and sub-panes, need to be well-defined and ANIMATED, not just instantly transitioned"* | **TRANSITION MANDATE covering sub-panes** — `sub-pane` = 0 in all four docs (re-verified); see §4 for the measured split |
| OM-9/10 | MT-F035 | *"why does this not line up in height" / "But this does"* | **SHARED-ROW cross-pane track-start contract** — OM-10 proves it exists in code and is undocumented, the definition of folklore |
| OM-11/12 | MT-F036 | palette containers: shadow artifacts AND no hover state, *"this is to be fixed"* (`ROOT-FINDINGS.md:1532-1559`) | OM-2's geometry law extended (same mechanism, MT-F027 lineage) **plus an ENGAGEMENT floor** — a hoverable product surface with zero hover response violates BREATH OF LIFE's "always displays engagement"; the first mark that makes the §1.2 law load-bearing |
| OM-13 | MT-F037 | the easing code readout is not a proper glass rounded input (`ROOT-FINDINGS.md:1562-1581`) | **GLASS-FIRST for value-bearing readouts** — joins OM-4 in the easing cut; a readout is an input-class surface, not prose |

*(OM-11..13 postdate the census — registered at HEAD `4f78e57b`; this brief is their first design-canon intake, and Phase E's roster must carry all thirteen, not the census's ten.)*

---

## §3 · CORRECTION TO THE CENSUS (adopted with amendment — the refutation the mandate requires)

**The census's PRM-idiom baseline is wrong.** It claims (*§5.4 blocker 1, gate G-PRM-NO-PREFERENCE*) that only `.stagger-children` (`animations.css:43`) uses the mandated `@media (prefers-reduced-motion: no-preference)` idiom — "1 of 19." Measured this session: **6 blocks in 5 files** (`animations.css:43` · `SpectrumCanvas.vue:243` · `DockStatusLamp.vue:109` · `overture.css:117,:151` · `ApiOfflineChip.vue:81`), at least four wrapping their own `@keyframes` inside the idiom (verified by read: `lamp-dot-pulse`, `offline-dot-pulse`, `field-paint-in`, the overture pair). The gate's substance survives — the blunt global reduce-kill at `animations.css:184-192` still zeroes everything, and most of the 19 demo-owned keyframes sit outside `no-preference` blocks — but **Phase E mints the gate at the corrected baseline (6 conforming blocks / 5 files at HEAD `4f78e57b`)**, and the wave's PRM work is a *migration of the remainder plus retirement of the blunt kill*, not a greenfield. A gate born with a false baseline fails its own L-2 duty on day one.

All other census gate baselines re-verified and adopted unchanged: G-ONE-DESIGN-DOC RED (0/12 citations) · G-THREE-LAWS-STATED RED (0/3) · G-CANON-ANCHORS-LIVE RED (12/13) · G-SUBPANE-ANIMATED RED (2 of 3 — see §4) · G-ELEVATION-SEMANTIC RED (`elevation` 0/4; MT-F026's 2×2 measurement).

---

## §4 · THE MT-F034 SPLIT — the motion facts Phase E inherits (verified, not inferred)

- **F034-a (pane/route swaps): GREEN and better-specified than the mark implies.** `App.vue:87/:106/:132` pass `vj-enter`; `animations.css:228-278` carries dedicated pane geometry with the two-channel law. The wave's work is *documenting* it and extending to route-level scenes, not building it.
- **F034-b (sub-pane/segmented swaps): RED at 2 of 3 measured sites.** The OM-8 witness re-verified: `MixSourceSelector.vue` swaps its mode content through a bare `<template v-if="mode === 'colors'">` under a `SegmentedTabs` — a one-frame cut (the file's one `TransitionGroup` animates chips within a mode, never the swap). `AdminNamesPanel.vue`: `<Transition` count 0 (re-verified). `PaneSegmentedControl` → GREEN by delegation.
- **The right family already exists.** The three-family law re-read verbatim at `animations.css:57-80`: `vj-morph` — *"ONE surface, NEW content (in-place swaps)"* — with the standing gate *"a fourth name is a defect."* F034-b is an APPLICATION of `vj-morph`, not a new register; any Phase E design proposing a fourth family carries the burden of superseding a live in-code gate.
- **Transition census at HEAD:** 22 `<Transition` sites / 17 files, every named one keyed to the three families (re-verified; two sites bind `:name` dynamically to family names). `view-transition`/`startViewTransition` = 0 anywhere in the demo — a fact for the tri-fold to weigh, not a defect row.
- **Two inherited blockers, restated with §3's correction:** (1) the blunt PRM reduce-kill (with 6 conforming no-preference blocks already in-tree as the house pattern to extend); (2) the momentum driver/observer carve — an OWNER MARK (§1.3) that decides F034-b's curve class. **Phase E may author the sub-pane law's structure before the mark lands, but must ship the curve as a parameter awaiting the ruling — never a silent pick.**

---

## §5 · BRIEF FOR THE PHASE E DESIGN TRI-FOLDS (M-14 clause 4)

**Protocol (binding):** each design unit runs as a tri-fold — **one Fable author and one Opus author produce INDEPENDENT designs, both invoking the `frontend-design` plugin for any frontend-design authoring; neither sees the other's work nor a favored narrative; a Fable arbiter then agglomerates into a choosy apotheosis, scrupulously — with the arbiter obligated to attempt refutation of both drafts against the tree (L-14), not to average them.** Every seat returns a model receipt (L-11). Probe parsimony rides every seat (owner edict 2026-07-12). Concurrency ≤4 workflows (07-24 cap).

**Deliverable shape: ONE canonical `demo/DESIGN.md`, re-authored, satellites folded and deleted.** Section order (census §6, amended by this pass):

1. **The three laws first.** GOLDEN GLASS authored from lineage → owner mark for ratification (§1.1); BREATH OF LIFE folded verbatim + engagement ladder + the OM-11/12 hover-floor consequence (§2); MOVEMENT OF MOMENTUM cited to `glass-ui/DESIGN.md:113` with the rename stated and the carve escalated (§1.3).
2. **Material & elevation** — what a shadow claims; in-chrome vs free-floating; one light model. Closes OM-1/2/7 and the OM-11/12 geometry half.
3. **Proportion & derivation** — vertical rhythm from the type scale; radius derivation; the shared-row track-start contract. Closes OM-3/4/9/10. **Re-measure before folding any PROPORTION-AUDIT number** (the 3.4× undercount, census §4.3; CONTRIVANCE-REGISTER WA-15).
4. **Chroma fidelity** — the sRGB-clamp budget. Closes OM-6.
5. **Motion** — fold `vnext/DESIGN-PROGRAM.md:170`'s six-tier table wholesale (its Transition row 180–420 ms already IS MT-F034's mandate, authored nine days before OM-8 and cited by nothing — the census's one buried asset); add the sub-pane application of `vj-morph` (F034-b); restate PRM as no-preference-scoped **from the corrected 6-block baseline (§3)**; re-anchor every citation to sections, never to line numbers of deletable files.
6. **Product surfaces** — diagnostics never render as chrome (OM-5); value-bearing readouts are glass inputs (OM-13).
7. **Anti-patterns** — keep `DESIGN.md:377`'s roster, extend from OM-1..13.

**Wiring is half the wave** (the census's own verdict): the re-authored doc is cited by the megatranche registry and by demo code comments; `CANON_HOMES`-style registration is relayed to glass for their side (`glass-ui/DESIGN.md` unregistered — their repo, our letter); `VISUAL-CONSTITUTION.md` + `transition-inventory.md` + `frontend-design/color-picker.md` fold-or-delete with tombstones.

**Born-RED acceptance set** (census §6 gates, amended): G-ONE-DESIGN-DOC · G-THREE-LAWS-STATED · G-CANON-ANCHORS-LIVE · G-SUBPANE-ANIMATED · G-PRM-NO-PREFERENCE **at baseline 6/5-files** · G-ELEVATION-SEMANTIC — each falsifiable against today's tree, each RED today.

**Owner marks to raise BEFORE authoring closes (never silent picks):**
1. the MOMENTUM driver/observer carve — does liquid weight bind observer-class motion (sub-pane swaps, list reorders)? F034-b's curve depends on it;
2. GOLDEN GLASS ratification of the lineage-authored definition;
3. the OM-11/12 hover-state floor — confirm the BREATH-OF-LIFE reading (every hoverable product surface responds) as law, since it generalizes an ordered fix into a canon clause.

**π/DELTA obligations attach to every visual claim** (SCOPE §2.5): each OM-owning section names its pinned witness (`audit/visual/owner-marked/OM-*.png` already banked for all thirteen) and its before/after pair.

*Every claim above carries a quote + file/line, a command + output from this session, or a named seat cite whose load-bearing numbers were re-measured here. No memory recall is used as evidence anywhere in this document.*
