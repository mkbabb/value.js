# Glass tranche deep audit — late batch (AW–BK)

**Scope.** Fifteen physical directories, audited independently against their plan, progress, final/audit record, cited commits, and the 2026-07-29 `glass-ui` source tree. `LANDED` means the promised implementation and its cut reached a durable close; `HALF-LANDED` means material code landed but the promised close or full mechanism did not. Later retirement does not erase a historical landing. BK is reported only at its present execution boundary.

| dir | promised mechanism | close / current claim | commit evidence | current-source evidence | disposition | surviving carry → exact receiver |
|---|---|---|---|---|---|---|
| AW | Dock motion repair plus Aurora, Blob, and shared convergence primitives across W0–W33. | `glass-ui/docs/tranches/AW/AW.md` and `RECAP.md` stop after batch 1; there is no `PROGRESS.md` or `FINAL.md`. `AX/AX.md` records that AW shipped work without reaching its formal W33 close. | `eaba94fc` is the batch-1 integration of Aurora, Blob, glass atoms, W28, and W31. | The descendant families remain concrete at `src/components/{dock,aurora,blob}/`; this proves surviving code, not an AW close. | **HALF-LANDED** | **NONE current.** The interrupted close was received by `AX.W00` and the `AX.W01–W19` corrective band. |
| AX | Fail-closed live truth plus direct dock, constellation, glass, control, and demo repairs. | `AX/PROGRESS.md` records substantial W00–W63 delivery, but no AX `FINAL.md`; its close work was explicitly absorbed by later tranches rather than fired under AX. | Representative landed slices resolve: `8a996890` (dock W04), `45cfb799` (constellation W17), `a730782a` (slider W59). | The affected owners and focused tests still exist under `src/components/{dock,constellation,slider}/` and `tests/components/`; the old AX final/ledger proof keys are absent from current `package.json`. | **HALF-LANDED** | **NONE current.** `AY.W-COLOCATE`, `AY.W-LEG1`, `AZ.W-CARVE`, and `BA.W-HYGIENE` received the named close residue (`AX/PROGRESS.md`, successor-disposition rows). |
| AY | Corrective convergence with colocation, legacy removal, live-evidence accounting, and an honest 3.10.x cut. | `AY/FINAL.md` reports the corrective close; the stale 3.10.0 registry artifact was corrected by the true close at v3.10.1. | Tag `v3.10.1` resolves to `86591cea`, whose commit records the pre-close 3.10.0 artifact and the corrected release path. | AY’s `proof:live-verified-ledger` and `proof:ay-final` are no longer registered; current release truth is `typecheck` + build + tests + `verify:package` in `.github/workflows/release.yml`. That is later replacement, not evidence against the historical cut. | **LANDED** | **NONE current.** Four later evidence/feature reopenings were received by `AZ.W-GATES` and `AZ.W-PRUNE2`; AZ then cut v3.13.0. |
| AZ | Blob/motion/shell convergence, a reflection bar, gate hardening, and the 3.13.0 cut. | `AZ/FINAL.md` marks the implemented bands and close complete; v3.13.0 exists. | Tag `v3.13.0` resolves to `a63ee10e`; `83ea0ef9` closes the rail correction and `7a190984` closes motion W2. | The adaptive-backdrop mechanism remains live: `src/components/dock/GlassDock.vue` imports `src/composables/glass/useGlassBackdropLuminance.ts`; its typed CSS write is still consumed by dock material rules. | **LANDED** | **NONE current.** Post-cut debt was received by `BA.W-HYGIENE`, `BA.W-FADING-SCROLL`, `BA.W-DOCK-SECTIONS`, and `BA.W-FOURIER-STUDIO`. |
| BA | A whole-system material, dock, motion, demo, and component pass, guarded by an eight-surface gestalt close and a 4.0.0 cut. | `BA/FINAL.md` reports complete and v4.0.0 shipped. `BB/BB.md` §0 corrects the close record: the local-only close missed release/CI failures, the cardinal ledger parsed zero BA rows, and six post-merge fixes were needed. The code cut landed; the close oracle was weaker than claimed. | The implementation series is real (`d853bd0f`, `79863371`, `364e3d94`, `6cf5c318`, `36e7640c`); tag `v4.0.0` resolves to release-fix commit `855d2746`. | Material descendants remain in `src/styles/glass/{glass-capsule,surfaces,control-surfaces}.css`; the old `proof:ba-final`/`proof:ba-gestalt` entries are absent, while the release path now invokes ordinary builds, tests, package verification, and a separate pixel floor. | **LANDED** | **NONE current.** Close-integrity defects were received by `BB.W-CLOSE-BATTERY`, `BB.W-LEDGER-REPAIR`, `BB.W-GESTALT-GATE2`, and `BB.W-DISPOSITION-RESTAMP`. |
| BB | Make the close honest, add performance and consumer-adopt work, drain chronic debt, then run W-REFLECT3/W-CLOSE for 4.1.0. | `BB/PROGRESS.md` records many landed waves but no `FINAL.md`; the terminal reflection and cut never ran. `BC/PLAN.md` and `BC/WAVE-INDEX.md` classify the result as source-heavy, paint-unclosed, and requiring a new cut. | Landed examples include `90981d69` (integrity floor), `d02b153a` (lensing), `2928da41` (metal shimmer), and `b25d20fc` (ratchet drain). | The BB gate farm and close-battery keys were later removed; the current `scripts/release.sh` states its release operations directly. Several built component/material owners survive, but no current file can supply BB’s missing terminal verdict. | **HALF-LANDED** | **NONE current.** `BC.W-PM-BB`, `BC.W-GESTALT-FIRST`, `BC.W-FOLD-LEDGER`, `BC.W-VISUAL-RECONCILE`, and `BC.W-CUT` received the unfinished close. |
| BC | Rebuild from per-wave evidence: one paint-reading substrate, a machine fold ledger, source corrections, consumer work, and a full 4.1.0 cut. | `BC/EXECUTION-PROGRESS.md` reaches the cut; `BC/FINAL.md` records completion and v4.1.0 publication. | `02f5a1f8` authors the cut, the remediation chain culminates in tag `v4.1.0` at `9c0e06e2`, and `b6141eee` records the terminal viz evidence tranche. | `scripts/lib/paint-arm.mjs` still identifies `BC.W-PAINT-GATE` as its source and is imported by `scripts/reflect-capture-verify.mjs`; this is direct survival of the load-bearing measurement leaf. | **LANDED** | **NONE current.** The out-of-fence consumer tail was named to `BD.W-CROSSREPO-ADOPT-SWEEP` and `BD.W-SLIDES-REDEPLOY` (`BD/SEED.md`). |
| BD | First a post-BC discharge plan, then a greenfield warm-field, shared capsule, procedural, dock, motion, and demo build ending at 4.2.0. | `BD/IMPLEMENTATION-PROGRESS.md` says all ten phases shipped, with the final release checks and v4.2.0 complete. | The implementation spine runs `80c21e29` → `43b68c33` → `cf149cff` → `b30e7989` → `a5f184cd` → `b494e526`; tag `v4.2.0` is `91c6ca18`. | The shared capsule and paper-stage owners remain at `src/styles/glass/glass-capsule.css` and `src/components/paper-backdrop/PaperBackdrop.vue`; later tranches changed their consumers without erasing BD’s cut. | **LANDED** | **NONE current.** Later product corrections became BG’s named Field/Route, Glass, Dock, Motion, Demo, and Substrate families rather than an unowned BD tail. |
| BE | An iOS-27 dock hallmark: fission, contextual silhouettes, liquid material, Safari floor, bloom, jubilance, and de-shadcn cleanup. | `BE/SEED.md` calls itself planning-only, but eight BE implementation commits later landed a prototype/foundation/engine series and a “buildable close.” BF/BG audits then found several engines demo-private, unwired, or speculative; no BE `FINAL.md` exists. | `5e7145ee`, `0be4792a`, `5b7c4530`, `9b301f4a`, `ac09eb51`, and `9665d7b7` establish a real but incomplete implementation line. | Current source retains descendants such as `glass-capsule.css` and `useLiquidReveal.ts`, but definitions for `useLiquidMorph`, `useDockContextSilhouette`, `useBloomUp`, and `useCelebrationBurst` are absent. `BG/BE-BF-LEDGER.md` and `BG/audit/RESPEC-GESTALT/DEFERRAL-LEDGER.md` record their fold or deletion. | **HALF-LANDED** | **NONE current.** The still-valid work was routed by name to `BG.W-FLIP-ONE`, `BG.W-DOCK-FISSION-WIRE`, `BG.W-DEAD-CUT`, `BG.W-GLASS-CONSUMER-BAND`, and `BG.W-CUT`. |
| BF | Converge BE into one FLIP spine, wire or delete the dock engines, add Safari and evidence truth, and cut. | `BF/INDEX.md` is explicit: 31 fully developed wave specs, **no implementation**, execution awaiting greenlight. BG later dispositioned every BF row. | The only tranche commit is planning commit `9a765843`; there is no BF-attributed product implementation commit. | The planned spike definitions are absent from current `src/`; `BG/FOLD-LEDGER.json` maps BF work to BG owners or terminal retirement, including `BF.W-SPIKE-DELETE → BG.W-SPIKE-DELETE` and `BF.W-SILHOUETTE-REALIZE → BG.W-DOCK-CUT`. | **DROPPED** | **NONE current.** The plan was consumed by `BG.W-DEFERRED-LEDGER`, `BG.W-FLIP-ONE`, `BG.W-DOCK-FISSION-WIRE`, `BG.W-PAGE-COMPONENT-AUDIT`, and `BG.W-CUT`; speculative rows were retired in the same ledger. |
| BG | Collapse the BE/BF and post-4.2 defects into nine implementation families, require per-wave measurement and twice-challenge discipline, join BH’s restructure, and cut 5.0.0. | `BG/FINAL.md` is a development final, not an execution final. `BG/execution/EXECUTION-PROGRESS.md` records extensive DONE delivery but leaves `BG.W-CUT` at `PAINT-PENDING`; `BI/HANDOFF-PERFECTED-BI.md` therefore calls BG never cut-ready. A v5.0.0 tag later exists, but at a BI structural commit rather than a BG cursor close. | The execution line includes `89dc3dee` (route), `274a2a6e` (field), `4ef43856` + `5eb1933d` (composited gate), many subsequent family commits, and `c93a7f13` (cut machinery still marked paint-pending). Tag `v5.0.0` resolves later to `9a8761f0`. | `scripts/lib/paint-arm.mjs` retains BG’s dominant-hue extension over BC’s leaf; `src/components/PROCEDURAL-SUITE.md` and the current Aurora/Blob/Dock owners retain much of the product work. The cursor’s missing terminal state cannot be inferred from those files. | **HALF-LANDED** | **NONE current.** The unresolved close and evidence tail was absorbed by `BI.W-GESTALT-LEDGER-FILE`, `BI.W-PI-IN-CLOSE`, `BI.W-STAB-CLOSE`, and the BI cut line; v5.0.0 then tagged. |
| BH | Remove export indirection, carve structural owners, redistribute canon, consolidate gates, and make the joint 5.0.0 export break. | `BH/PLAN.md` and `README.md` still say awaiting execution, but the authoritative joint cursor in `BG/execution/EXECUTION-PROGRESS.md` marks the BH bands DONE. The stale header is a record defect; the restructure itself landed before v5.0.0. | Representative commits are `ca988a76` (alias codemod), `c98ac8c8` (fail-closed export regeneration), `a4247a01` (canon re-home), and `af384825` (WS12 close tail). | The mechanism survives in changed form: `scripts/regen-exports.mjs`, `scripts/lib/subpath-policy.mjs`, flat `src/components/*` semantic owners, and `scripts/verify-export-types.mjs`. Later BI work removed the remaining historical tiering. | **LANDED** | **NONE current.** The follow-on structure program was received by `BI.W-STRUCTURE-RESEQUENCE` and `BI.W-MS1-CENSUS-RECOMPUTE` through `BI.W-MS9-DIFFERENTIAL-CLOSE`. |
| BI | Union the post-BG repairs, BH structure work, component/material/motion redesign, consumer coordination, and a terminal 134-row P graph plus 20 Q rows, shipping 7.0.0. | `BI/HANDOFF-ACTIVE-EXECUTION.md` reports 61 P rows DONE, 2 achieved by abrogation, 22 other terminal rows, and **49 landed but nonterminal**; the Q rail also remained mixed. v7.0.0 shipped, but `BI/PLAN.md` is now superseded into BK and cannot claim full tranche terminality. | `490cc46e` lands the principal Glass 7 product/public-surface cut, `d17153ec` removes the old gate machinery, `c181f0a7` forms the Q rail, and tag `v7.0.0` resolves to `4ab12128`. | The current tree carries the cut’s families and public owners under `src/components/`, the motion domains under `src/composables/motion/`, and the strict package checker at `scripts/verify-export-types.mjs`. Current package version remains 7.0.0. | **HALF-LANDED** | **LIVE carry.** `BK/PORT.md` §1.4 routes nine Q rows to TR `#4/#9/#10/#11/#17/#22/#38/#39/#49/#50/#61/#65/#66/#68/#76`; §1.5 routes all 49 nonterminal P rows across TR `#8/#11/#18/#22/#26–#35/#38/#39/#43/#44/#47–#59/#64/#66/#68/#76/#79–#89`. |
| BJ | Post-7.0 grand-audit reformation: gate reduction, material and a11y repairs, story/performance work, four greenfields, consumer relays, and an 8.0.0 close. | Material code landed, but `BJ/EXECUTION-PROGRESS.md` remained acceptance-red in several bands and `BJ/addenda/2026-07-24-refinement/TERMINAL-ROSTER.md` states that its newly sealed 90-row roster had no source execution. `BJ/PLAN.md` and cursor are now provenance-only, superseded into BK. | Real partials include `35a30fbb` (a11y), `ddc20dc4` (type codemod), `22401a90` (radius gate), and the receipt boundary `abf46592`; later BJ commits are chiefly formulation and routing records. | Current `src/styles/theme/radius.css`, typography roles, and component/a11y tests contain landed portions. The current execution authority is not a BJ file: it is `BK/EXECUTION-PROGRESS.md` plus the TR pointers. | **HALF-LANDED** | **LIVE carry.** `BK/PORT.md` §2.1–§2.4 maps the nine BJ bands, greenfields, phase ledger, parked register, and returned work to TR `#2/#4/#8/#9/#10/#11/#17–#35/#38–#65/#66–#69/#71/#73/#74/#76–#89`. |
| BK | Execute the sealed 90-row terminal roster through Φ0–Φ7, beginning with source truth and a typed dependency/ownership graph, then cut and publish 8.0.0. | `BK/EXECUTION-PROGRESS.md` is current: row `#1 W-SURFACE-PIN` and `#75 W-STOP-HOOK` are `IN-FLIGHT`; #1 has a landed candidate plus cures, a failed round-3 boundary, and a bounded round-4 cure registered. #2 subtraction and the rest of Φ0 remain open. There is no `FINAL.md`, and unfinished work is not a terminal failure. | Current sequence: `858c1087`, `0557703f`, `950a703f`, `f3a83c9b` (Φ0 truth-detector candidate/cures); `9c43b5d7` and `ee5cbcfb` (typed graph v3); HEAD `5841dd35` records the round-3 boundary and round-4 cure. | `docs/tranches/BJ/audits/2026-07-28-library-dag/{build-import-dag-v3.mjs,IMPORT-DAG-V3.json,OWNER-MANIFEST.json}` and `tests/architecture/import-dag-v3.test.ts` are present. They are pre-source instruments; most product rows remain untouched by BK. | **ACTIVE** | **BK itself.** Current receivers are TR `#1` for the surface pin, `#2` for refract subtraction, `#4` for strays, `#5` for the prefix sweep, `#8` for package truth, and `#75` for the stop hook (`BK/EXECUTION-PROGRESS.md`). |

## Count and unresolved facts

- **15 directories:** 6 `LANDED`, 7 `HALF-LANDED`, 1 `DROPPED`, 1 `ACTIVE`; 0 `REJECTED`, 0 `HISTORICAL-ONLY`.
- The six `LANDED` directories are AY, AZ, BA, BC, BD, and BH. BA’s implementation/release landed despite the close-oracle defects BB later proved.
- BG has a real v5.0.0 tag, but its own execution cursor never records `BG.W-CUT` as DONE; the tag points at later BI commit `9a8761f0`. That provenance mismatch is why BG is `HALF-LANDED`, not `LANDED`.
- BE’s header says planning-only even though its implementation commits exist; BH’s header says awaiting execution even though the joint cursor and v5 history show delivery. Directory-header status is not reliable by itself.
- BK is open at Φ0. The last observed HEAD is `5841dd35`; no claim is made about Φ0 completion, the 90-row roster, or an 8.0.0 cut.
