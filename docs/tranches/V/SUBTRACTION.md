# Tranche V — The One Subtraction Front

W4 is the only repository-ceremony wave. Later waves may delete an obsolete implementation when its product replacement lands, but they do not mint another hygiene program.

## Delete in W4

- `scripts/gates/**` and every `proof:*` package script;
- declaration/comment/ledger oracles in `scripts/ci/**`, including expected-red accounting;
- the three live `test.fail()` tripwires and any test whose only assertion is that a future cure is missing;
- stale simulated/proxy benches, including the crashing `color-soa-fold` path and parse proxies;
- the superseded `docs/tranches/V/DESIGN-CAMPAIGN.md`, `HANDOFF-2026-07-13.md`, and entire `design/**` tree after W0 has mapped each still-substantive finding into the current decisions/ledgers; this deletes the dangling `design/portfolio.md` pointer together with every old charter, pseudo-gate, status file and executable instrument rather than preserving a second V process;
- ignored root PNGs, `.DS_Store`, uncited logs, untracked visual bulk, and obsolete generated evidence;
- merged/ancestor worktrees and branches under the documented safe-delete proof; the throwaway ahead branch is discarded, not harvested;
- zero-invoker `scripts/fonts/build-fraunces-tnum.py` and its old O-10c/numeric-readout comments; Fira Code owns numeric duty while the ordinary value-owned Fraunces identity asset is preserved for W31;

Keep standard `build`, `typecheck`, `lint`, unit, e2e, and pack commands. Retain at most the small dist-backed parser/interpolation/gamut microbench set that W2 proves current and useful; benches are developer instruments, never close authority.

## Delete when the replacement lands

| Item | Replacement / owner | Deletion wave |
|---|---|---|
| `plugins/vite-source-export.ts` plus exact-source golden | deliberately authored typed pedagogical snippets, typechecked against public `/color` | W6 |
| `plugins/vite-defer-glass-fonts.ts` | BI cacheable font assets + CSS-first loading | W17/W31 |
| root/public barrel parity machinery | sole capability entries + packed sibling cut | W7/W17 |
| dead forwarding barrels, bindings, local glass wrappers | owning topology/component replacement | W5–W17 |
| zero-consumer `.underline-tabs` override | surviving owner/Admin tabs consume P092 directly; no renamed override | W5/W6 |
| zero-consumer `panes/index.ts` barrel | direct feature leaves plus the one earned chassis entry; no aggregate alias | W5 |
| duplicated/global spectrum-range and pane-shell style ownership | one W21 axis/spectrum owner and W19 shell colocation; no second cascade owner | W6/W19/W21 |
| production `PointerDebugOverlay`, public `debug=1`, provider and Spectrum/Slider injected logging branches | browser devtools and ordinary focused interaction tests; no product-panel successor | W20 |
| runtime schema compatibility check | complete transformer/rehearsal/deploy cut | W11–W16/W33 |
| mocked flow status as CRUD proof | real Hono/replica-set API plus browser journeys | W16/W22–W24/W30 |
| `.claude/worktrees/glass-ui-pinned` install/path | post-BI evidence-versioned Glass semantic import/component migration plus the first clean live-sibling build/typecheck | W17 |
| keyframes `proof-css-parity.mjs` and `proof-scroll-roundtrip.mjs` command identities | **RETIRED, LANDED** at keyframes `70b32501`; W17 migrates/revalidates the surviving ordinary CSS/scroll tests against value 4 rather than deleting the files again | landed U; W17 validation |
| keyframes `proof-workaround-deletion.mjs` and its registry/prose oracle | **RETIRED, LANDED** at keyframes `70b32501`; no replacement proof script | landed U; W17 validation |
| keyframes `proof-diagnostics-channel.mjs` and `proof:diagnostics-channel` | **RETIRED, LANDED** at keyframes `70b32501`; structured diagnostics remain in `test/compile/diagnostics-channel.test.ts` | landed U; W17 validation |
| keyframes historical `docs/tranches/I/audit/investigate/probes/rc-parse-crash-seam.mjs` | ordinary parser-result tests on the live `/css` boundary; historical prose remains, executable dead-root reproducer does not | W17 |

## Preserve

- the user's dirty deletions of `CLAUDE.md` and `src/parsing/CLAUDE.md`;
- untracked T audit material currently present until W0 identifies cited vs uncited rows;
- any worktree/branch with unique user work that fails the safe-delete proof;
- `.claude/worktrees/glass-ui-pinned` through W4; classification is not authority to unpin before W17's post-BI evidence-versioned Glass migration;
- legitimate global CSS, data tables, and focused product tests merely because they are large.

Subtraction succeeds when the ordinary developer path is shorter and the product evidence is stronger—not when a replacement gate count reaches a chosen number.
