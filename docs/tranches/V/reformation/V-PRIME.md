# Tranche V — Re-formed Authority (V′) — DRAFT pending final adversary passes

**Status:** DRAFT · authored by the 2026-07-16 owner-ordered re-formation campaign
(`audit/REFORMATION-2026-07-16.md`, families RF-1…RF-29). Becomes the V authority when the
campaign's two fresh adversary passes and the owner brackets close. Until then `V.md` remains
the standing document; W41 (canon compression) executes the swap and the archival.

## 0. The verdict this re-formation answers

V hardened its specification against its own edge cases, not against any pressure this product
exerts. Of the 1.46MB living canon, ~30–35% specifies a proportionate product; the palette
stack is ~70% speculative armor; 9 of 10 sampled waves cannot be executed from their own file;
the convergence clock reached 2/2 twice and invalidated itself both times; 31 of 34 waves never
started while eight product chronics — two born in Tranche D — rode every close as decided-but-
never-run rows. Meanwhile the codebases are far healthier than the canon: src/ is near-idiomatic,
the demo component tree is already recursively colocated at component grain, api/ is near-
exemplar, and the honest api fix is ~15–20% of the surface the plan tore down. The product
vision (Optical Bench, the eight chosen mechanisms, the proportion register, the visual
constitution) is genuinely strong and survives intact.

**Therefore: V′ is an execution tranche.** The formation is over. No further formation passes,
no convergence clock, no new audit registries. Evidence is running waves with rendered product
witnesses, under the anti-partial machinery V already built (its strongest feature — carried
forward whole).

## 1. Standing laws (new or elevated; V.md §5 evidence law carries forward verbatim)

- **L1 — Execution gate (RF-26).** The eight disease riders (aurora-derive D-1, blob D-2,
  colocation CH-3, Q14-LCP CH-4, glass adoption CH-5, scene transitions CH-6, real-GPU CH-7,
  palette truth CH-8) may not be re-chartered, re-amended, or re-decided. Their next legitimate
  state is a RUNNING wave with product-green Browser evidence. A further "BUILD W##" row for
  any of them is the forbidden re-booking.
- **L2 — Single source of truth (RF-11).** Every immutable fact (producer hashes, byte counts,
  export counts) lives in exactly one document; every other site references it. The re-formed
  canon fixes the D27/D2 contradictions and the Keyframes byte-count drift at W40.
- **L3 — Standalone waves (RF-10).** Every wave file is executable from itself plus ≤2 named
  references. Historical prose lives in the archive, never inside a live wave.
- **L4 — Parsimony as product law (E3).** Fewer lines, fewer concepts; net-negative LoC
  wherever the product allows; effort goes to source and rendered verification. The one
  load-bearing gate (`verify-packed-surface.mjs`, RF-7) is protected; no new gates.
- **L5 — Sequencing law (RF-2, amended by the round-3 executability finding).** The dirty tree is
  committed in owned slices in W40's order; nothing is mass-staged; the four V-A107 tests are
  unstaged from the producer slice and land in the consumer slice; the demo consumer bulk commits
  as W40-S4, its OWN slice (V-A73 assigned the value-internal migration to atomic C1 — it lands
  late, not early), explicitly never counted as W44 evidence. Nothing is held across waves.
- **L6 — Design pathway (RF-22/E5).** All design routes through Fable + DesignSync. A value.js
  design-system project is created at W46-open; the visual constitution and proportion register
  are its source; components sync one at a time as frontend waves land them.
- **L7 — Coordination (E6/E7/E8/E13, RF-16).** Glass root-defects batch into
  `valuejs-inbox-*` letters — never interrupting the live glass agent; keyframes exchange is
  bilateral formation letters; atlas/SCI route via the P-tranche coordination dir. Relay-sent
  is never a terminal state (carried from D37). **The mail law (E13, owner-ruled 2026-07-17):**
  `coordination/INBOX.md` is the tranche mail ledger — every wave OPENS with a sweep of its four
  landing paths; new mail is rowed UNREAD before any other work; no wave CLOSES with UNREAD mail
  in its scope; relay-received counts only once rowed. INBOX.md is living canon (KEEP-class,
  outside W41's top-level size gate).
- **L8 — Pre-mount buildability (RF-28).** The pure pre-mount set — export byte-serializers,
  wire clients, the SeedToken parser, spectrum/console fixtures — may build and verify from a
  fresh clone BEFORE the routed mount lands, overriding V-A166's over-reach (whose styling ban
  binds only routed/gestalt work on the transient tree). W51's byte contract closes independently
  of W44; no pre-mount result may be counted as routed/gestalt evidence.

## 2. The re-formed wave set (W40-series; 34 → 17 + 1 lane; the frontend arc is 9 units)

**Step 0 of EVERY wave, before its first listed step (E13/L7):** the four-path mail sweep;
new mail is rowed in `coordination/INBOX.md` before any other work; no wave closes with
UNREAD mail in its scope. This line is the operational form of L7's mail law — wave files
do not restate it.

| Wave | Name | Absorbs | Gate summary (each wave file carries the full falsifiable set) |
|---|---|---|---|
| **W40** | Reconcile | W0(part), commit sequencing (UNOWNED→owned) | Dirty tree fully committed in owned slices per L5; **`picker-color.ts` + the 4 untracked v4 tests tracked in their owned slices (RF-29 B1/B4); aurora tests relocated into the runnable include (B7)**; RF-1 three feature-deletion decisions executed (bracket B1); canon truth fixes (D27/D2/byte-counts) landed; keyframes ff-only note sent. Gate: a fresh clone typechecks+tests green from committed state alone. Born-RED: it does not today. |
| **W41** | Canon compression | W4(doc half), RF-8/10/11/12 | Living canon ≤ ~150KB; V.md replaced by this authority; sediment archived under `docs/tranches/V/archive/`; AGENT-REGISTRY/PROGRESS/old handoffs marked historical; the W0–W33→W40-series mapping table is the single junction; clock deleted. Gate: a fresh reader executes any wave from its file + ≤2 refs (sampled ×3 by a non-author). |
| **W42** | Subtraction | W4(repo half), RF-4/5 | Staged deletion sweeps ratified + committed; 8 meta-tests (~842L) pruned; `oracle-slate-teeth.mjs` deleted; `boot-smoke`/`css-emission-probe` decided (D-bracket B4); the demo-typecheck CI re-gate wired (B3 implement); `.release-working/` removed+ignored; the 16 stale `wf_*` worktrees pruned (~11GB, RF-29 B5/B6); superseded design docs deleted (not modified); benches: the small proven-useful set question closed. Gate: `npm test` green with the pruned suite; no package.json script without a consumer; `git worktree list` = primary + intentional only. |
| **W43** | **Structure settlement** — THE disease-row wave (CH-3 dies here) | W5/W6 + RF-14/15/21/23 | src/: `v4/` dissolved to bare dirs, double-barrel collapsed, `stylesheet.ts`→`+timeline.ts` and `grammar.ts`→`+syntax.ts` splits, `foundation/` materialized, 8 empty dirs gone. demo/: `@` alias dies; 38 single-consumer files colocate; 23 true-shared land in color-session/palettes/platform/shell/shared; `panes/` 16→0; `style.css` 1043L→owners+foundation; `usePaletteManager` dissolves into narrow ports; ConsoleRail cross-feature import fixed. api/: `paginationQuery`→platform, session primitives behind service, `lru`→session, 5 prefix renames. **Down-metric (the chronic-killer): shared-bucket dirs 7→0 · panes 16→0 · v4-dir 1→0 · empty dirs 6 leaf→0 (parents fall with leaves) · redundant middle barrels →0 · css god modules 2→0.** Gate: typecheck+build+full suite+`verify-packed-surface` green; import-graph census shows zero alias/zero cross-feature-internal edges; public 7-entry surface byte-identical. |
| **W44** | Glass rail | W17a/W33a/W17b/W33(pkg) | Unchanged in substance — externally Glass-owned strict/native/Q003 close → fresh Glass7 publish → value consumer refresh: pinned Glass5 island replaced by registry triple, RF-13 five-site migration (`/controls`→successors, `/confirm-dialog`→`dialog`, `/toggle-chip`→`chip`/`toggle-group`), `writeClipboard`/`useClipboard` adoption (all sites, count-independent, direct — the adapter is DECLINED per the no-backwards-compat law), HeaderRibbon persistent-only rows, stale canon removed, lock regenerated, **routed mount witness**: nonempty `#app`, sole `<main>`, Lab space, numeric headline, sole Copy at the canonical URL. Born-RED: the app is blank today. CH-5 execution-gated here. |
| **W45** | Palette honest core | W1/W10–W16/W22–24(api half), RF-9/24 | The surgical set: 7 verified RED remediations (restore/versions/hash/revert retired into the right-sized release concept; indexed search, session-derived attribution, visibility-filtered provenance, real readiness, generated OpenAPI replacing the drifted hand table) + `/sessions/me` client recovery + idempotency where honest. The armor (Workspace/Policy/Principal-bitmask/CAS/RankEpoch/AEAD cursors/replay ring/mTLS edge/20 admin routes/encrypted legacy cut) RETIRED-AS-SPECULATIVE (D-bracket B2). CH-8 execution-gated here. Gate: api suite green; every mounted route in generated OpenAPI; the 7 RED reproductions turn green. |
| **W46** | Constitution & chrome law (F0) | W18 + the P051/P122 clauses lifted out of W21–W27/W29 | Sole owner of seat-law, proportion law, type/Card tuple; 18 composition pairs ratified once. Every later wave's P051/P122 obligation reduces to "consumes W46 fixture, local override count 0". CH-6 scene law seeded here. |
| **W47** | Shell & scene (F1) | W19 | Eleven routes incl. `/about`/`/easing` resolve direct; main/H1/active-subtree =1 per sampled frame; dock-expand scene rect 0px; `PaneSegmentedControl`/split-view 1→0; Dock pastel `Palettes`=1/non=0; V-A120 dock affordances retained-or-removed. CH-6 execution-gated. |
| **W48** | Picker instrument (F2) | W20 + W21 + W29's paint slice (one atomic close, one shared manifest) | Headline+editors+meters+two-named-axis spectrum+selector agree in one settled routed frame; contenteditable/dup-editor 0; pointer↔keyboard↔numeric↔AT parity; PR-01..03 anchors; Blob 0px chassis/seat/footprint vs a **captured `.26` before-frame reconstruction** (heals the RF-28 phantom gate). A137/A138 die here. Protected slices resume, never regress. |
| **W49** | Palette read (F3) | W22 minus export-state paragraph | Card count = entity count; exact quiet-opaque tuple; one native `aria-pressed` seat (pressed 0\|1, pressed-ID↔inspector-ID); protagonist/inspector proportions; storage-recovery preserves-before-reset — against W45's live api. |
| **W50** | Palette write (F4) | W23 minus export | Register→recover→rotate→lost-response-logout; draft→publish→unpublish→trash→restore with immutable public history, secret redaction, typed conflicts; no legacy route, no caller attribution. CH-8's UI half closes here. |
| **W51** | Palette export (F5 — pure, pre-mount-buildable under L8) | W23's five serializers | JSON/CSS/Tailwind/SVG/PNG byte-exact vs the `PALETTE-CONTRACT.md` export appendix (the W41 fold's verbatim guarantee); identical full-file hashes Chromium/Firefox/Safari; Prepare→Download reload identity. |
| **W52** | Admin suite (F6) | W24 | Five full-width routes; companion 50%→0; Names indicator ≤0.5px; half-open `[from,to)` audit windows; exact event-row cardinality — against W45 typed Problems. |
| **W53** | Workbenches (F7) | W25 + W26 + W27 (five features, disjoint commit slices) | Per feature: consumes W46 law (override 0) + W48 axis adoption + one action grammar + pointer↔keyboard parity + reduced-motion terminal. Carries the B1 ruling on the perceived-space stage (rebuild-on-v4 recommended). |
| **W54** | Atmosphere & GPU (F8) | W28 + W29's Blob studio | Boot: SeedToken negative set rejected; B0→app-ready atoms byte-identical; `groundRecordInject`/`__GROUND_*__` absent; `script-src 'self'`. Aurora: **D-1 aurora-derive execution-gated here** — derive-from-color RUNS or V′ does not close. Blob studio: D-2 gated; truthful lifecycle; arm-after-critical-paint 0px shift; WebGPU/WebGL2 parity. |
| **W55** | Adversarial close | W30/W31/W32 | a11y/mobile/zoom/RTL/AT operation truth · performance lab with the Q14-LCP budget (CH-4 gated: p75 LCP ≤2.5s on the named matrix — the ~5s boot dies or V′ does not close) · real-GPU gestalt (CH-7 gated: the oracle RUNS). Concise π/DELTA; reopen-not-accept. |
| **W56** | Release + canon | W33b | Ordered release of whatever V′ ships (4.x/5.0 per WL), downstream locks registry-only, final canon truth, deploy + live-wire verification (M3 headers), PROMPT-RECAP §7 (E1–E11) terminal. |
| **WL** | Library evolution (lane, parallel post-W43) | RF-17/27 + KF covenant | Per-row DECIDE ship-4.1/ship-5.0/decline-with-rationale: KF §B `parseTimingFunction` (already in `/css` — verify + answer), §C `unflattenObject`, §D diagnostics taxonomy, §E `CSSPropertyDescriptor` rename (breaking → 5.0-or-decline), §I D-GAP-1/5/6; SCI-1 `mixColorsInto` zero-alloc restore-or-bless (~3,243 marks/frame consumer), SCI-2 clamp-option vs consumer-clamp law, SCI-3 validated-literal `CubicBezier` vs bless-unwrap. Every verdict returns in the bilateral letters. |

DAG: W40 → W41/W42 → W43 → {W44 ∥ W45} → W46 → W47 → W48 → W49 → W50 ∥ W52 → W53/W54 → W55 → W56;
WL runs parallel from W43 and joins only at W56 (it gates no frontend wave);
plus WL → W56 (the version choice feeds the release). W51 (pure export, L8) may start any time
after W45. W44's glass half is externally gated and runs from now; W45/WL never wait on glass.
W48 follows W47 (declared dependency); W49 follows W48 (consumes its axis semantics).

## 3. Owner brackets (the only undecided rows; everything else is formed)

| # | Bracket | Recommendation (Fable) |
|---|---|---|
| B1 | RF-1 feature deletions: gamut-truth overlay · perceived-space plate · ink-walk | Rebuild the perceived-space plate on v4 at **W53** (it is the gradient workbench's honest instrument; old W27's spec depends on it; preservation tag `v-perceived-space-plate-ref-w40` holds the reference implementation — tag-only, no working-tree restore: the plate imports dead v4-cut APIs); terminally retire the gamut overlay and ink-walk (superseded by the veil semantics + the ink ladder); all three deletions land in W40-S4 atomic with their import-removals |
| B2 | RF-9/24 armor retirement (the ~70%) | RETIRE-AS-SPECULATIVE with the register preserved as archive; re-formation is explicitly not a BANK — a future multi-tenant product re-derives from scratch |
| B3 | RF-3 CI coverage: what re-gates (implementing waves NAMED) | Demo typecheck re-gates at **W42** (its Work item 9 wires ci.yml); api tests re-gate at **W45** (completion duty); the journey subset + the 11.7k-line e2e corpus pruning at **W55** (owns the .github edit). INCLUDES the RF-29 B9 sign-off: the 1607→345 unit-coverage drop is ratified as the v4-cut consequence, guarded by this re-gated set |
| B4 | Orphaned probes (`boot-smoke`, `css-emission-probe`) | Delete both; the W44 routed-mount witness and W50 journeys supersede them |
| B5 | U-era owner packet P1..P8 (still un-ruled, incl. T HG6) | Unchanged — remains the coupled owner event; V′ does not proxy it |

## 4. What this re-formation does NOT do

No source edits (campaign law). No reopening Value4/Keyframes6. No touching the live glass
agent's lane. No new proof frameworks, no BANK rows inside V′, no convergence clock, no
formation passes after the campaign's own two adversary checks. The proportion register,
visual constitution, optical bench compositions, EVIDENCE.md matrix, CONSUMER-CUT, and
ARCHITECTURE survive as the referenced canon of W43–W50.
