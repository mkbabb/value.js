# V′ CARRY-LEDGER — the complete fold-forward inventory (nothing lost: head, tail, or interval)

**Owner order (2026-07-17):** adopt Glass 7.0.0 → compaction → deep audit with the working
frontend → form the next tranche with ALL non-complete and deferred items folded forward.
This ledger is that fold's single source. Every row carries provenance (a D-ruling, a wave
spec, or an INBOX row). The next formation consumes this file WHOLE; a row may only leave it
by landing or by explicit owner retirement.

## §A — Executed and CLOSED (the head; context, not carry)

Formation CONVERGED (passes A∥B CLEAN; `FORMATION-CLOSED.md`) · W40 (D46/D47) · W41 (D49 +
sample gate) · W42 (D48/D56 CI) · W43 (D50–D53; CH-3 terminal; census 0) · W45 (D55; CH-8
api-half terminal) · WL (D54; 1 ship + 7 declines) · W51 GREEN-PURE (D57) · CI fully green
on `origin/tranche-u` · W44 adoption ordered by the owner (in flight at ledger authorship;
its close row appends below).

## §B — The unexecuted V′ wave tail (specs live at `reformation/waves/`; each file standalone)

| Wave | The whole of it | Riders folded in from elsewhere |
|---|---|---|
| **W46** (F0 constitution & chrome law) | Sole owner of seat-law/proportion-law/type-Card tuple; 18 composition pairs ratified once; CH-6 scene law SEEDED | L6: the Fable+DesignSync design-system project opens at W46-open |
| **W47** (F1 shell & scene) | Eleven routes direct; main/H1/active-subtree =1; dock-expand scene rect 0px; `PaneSegmentedControl` 1→0 (it lives ALIVE at `demo/shell/` for exactly this gate); CH-6 EXECUTED | **D53.iv:** the four pre-existing feature→shell/boot couplings (VIEW_MANAGER_KEY ×2 · `ViewId` type · `resolveCalibratedAtmosphere`) |
| **W48** (F2 picker instrument) | One atomic close; A137/A138 die; PR-01..03; pointer↔keyboard↔numeric↔AT parity; Blob 0px chassis vs the **`.26` before-frame reconstructed from tag `v-blob-b0-26-ref-w40`** (HeroBlob.vue:206 + seat.css:10 at the tag) | Protected slices resume-never-regress |
| **W49** (F3 palette read) | Card count = entity count; quiet-opaque tuple; native `aria-pressed` seat; storage-recovery — against W45's live api | |
| **W50** (F4 palette write) | Register→recover→rotate→lost-response-logout; draft→publish→unpublish→trash→restore; CH-8 UI half closes | **D55.iii:** the `__Host-value-session` cookie transition (spans api+client; may open an api slice) + non-UUIDv7 idempotency-key retirement. **D57:** the Prepare→Download seat (IndexedDB `ExportOperation`, FORMAT→FILENAME swap, fresh-activation download, 60s-`pagehide` lease) + RETIRE legacy `demo/palettes/export.ts`/`usePaletteExport.ts`, wiring panes to `export/serializers` |
| **W52** (F6 admin suite) | Five full-width routes; companion 50%→0; half-open audit windows; exact event-row cardinality — against W45 typed Problems | |
| **W53** (F7 workbenches) | Five features, disjoint slices; W46-law consumption (override 0); one action grammar each | **B1 arm-A:** rebuild the perceived-space plate ON v4 from tag `v-perceived-space-plate-ref-w40` (the tag holds PerceivedSpacePlate.vue + envelopePlatePaint.ts + usePerceivedRamp.ts; the old code imports dead v4-cut APIs — rebuild, not restore) |
| **W54** (F8 atmosphere & GPU) | Boot: SeedToken negative set; B0→app-ready atoms byte-identical; `script-src 'self'`. **D-1 aurora-derive RUNS or V′ does not close** (L1). D-2 blob truthful lifecycle; arm-after-critical-paint 0px; WebGPU/WebGL2 parity | |
| **W55** (adversarial close) | a11y/mobile/zoom/RTL/AT truth · **CH-4: p75 LCP ≤2.5s on the named matrix — the ~5s boot dies or V′ does not close** · **CH-7: the real-GPU oracle RUNS** · reopen-not-accept | **B3 tail:** the journey subset re-gate + the 11.7k-line e2e corpus pruning (owns the .github edit) |
| **W56** (release + canon) | Ordered release of whatever V′ ships; downstream locks registry-only (**I-8 posture:** active atlas already migrated — zero atlas work; sci-report crosses ATOMICALLY at its glass-7 consume, no early bump); final canon truth; deploy + M3 live-wire headers; PROMPT-RECAP §7 (E1–E11) terminal; **writes `reformation/TRANCHE-CLOSED.md`** | **D54/SCI-1 vehicle:** the 4.1.x `mixColorsInto`/`toRgba8Into` cut (additive; evidence tuple owed to atlas at the cut) feeds the version choice |

## §C — Rulings with live tails (beyond the wave rows above)

- **D49 residue:** `research/{STANDARDS,auth-cookie-order,proportion-register}.md` remain
  live-untracked (W41 deviation 4) — the next formation rules track-or-archive.
- **D53.vi:** the app-root rename (`demo/color-picker/` → `app/`) was ruled out-of-scope at
  W43 — next formation decides.
- **`scripts/dev/dev.sh`** — the LAST unowned dirty working-tree row (M, un-ruled since
  pre-V′). Owner rules commit-or-restore; no wave owns it.
- **B5** — the U-era owner packet P1..P8 (incl. T HG6): owner-held, never proxied (V-PRIME §3).
- **E13 standing law:** Step-0 four-path sweeps at every wave open (crons killed by owner
  order 2026-07-17 — INBOX §Durability; no cron returns unless ordered).

## §D — Standing external expectations (inbound we are owed; sweep for these)

- **keyframes: BOTH expectations DISCHARGED at W44 close (I-10, 2026-07-17)** — the
  consume-confirmation arrived (exact glass 7.0.0 on kf 6.0.0, value 4.0.0 deduped,
  wedge exited on their edge) and O-4's verdicts are all marked terminal (D-GAP-6
  DECLINE ACCEPTED; RF-18 enacted `add20b7e`). Live tail: if a future 4.1 ships
  `sampleBezier`, kf adopts — rides the W56/SCI-1 4.1.x vehicle row.
- **glass (BJ tranche):** the two §7 shipped-defect fixes — V-A95 aurora reverse-drag
  black-slab; Chip/Badge orphaned dist CSS (**our `/toggle-chip`→`<Chip>` site renders
  incomplete glass until it lands** — visual residual recorded at W44); plus the fuller
  handoff evidence the old acceptance contract named (Q003/native packet) if BJ produces it.
- **atlas/sci:** any reply to O-5 (SCI verdicts); their #227 atomic crossing happens on their
  side at their glass-7 consume.

## §E — The owner's post-compaction program (the order of record)

1. **Deep audit with the now-working frontend** (W44's routed mount green) — breadth-first
   against the LIVE product, informing…
2. **The next tranche formation**, which folds §B + §C + §D whole, plus everything the deep
   audit finds, plus W44's close row below. Formation discipline carries forward: born-RED
   gates, standalone wave files, the adversary clean-pass protocol, the E13 mail law, and
   the anti-partial machinery — per V-PRIME §1's standing laws.

## §F — W44 close row (appended at close; ruling D58)

**W44 CLOSED GREEN-WITH-RESIDUALS 2026-07-17 — Glass 7.0.0 ADOPTED WHOLE; THE BORN-RED
BLANK IS CURED.** Slices `f2c8f565` (deps+lock registry-only, island dead, five roster
sites + ~40 root-symbol drifts, clipboard adoption) · `ef57230b` (D48/D56 CI soft gates
flipped HARD) · `4c1e9270` (D51 doc-source retirement). Gates verbatim: lib/demo typecheck
0/0 · `npm test` 346/346 (aurora pair resolving via the real dep) · lint clean · build +
gh-pages exit 0 · packed surface 23/1/19/16/9/9/2·62 · `copyToClipboard` grep 0 ·
removed-subpath imports 0 · legacy-trio lock remnant 0 · routed witness green (one `<main>`,
Lab, numeric headline, one "Copy color", pageErrors []). Relay O-6 sent (M1 DarkModeToggle
passive gap · M2 useLayerTransition successor ask · M3 Chip §7 consumer evidence). CH-5
EXECUTION-TERMINAL.

**W44-born carries (fold with §B–§D):**

- **Deep-audit item — gh-pages prod-preview empty mount:** the PRODUCTION build previewed at
  a bare 127.0.0.1 origin mounts empty (boot-guard/ground-record quirk; dev witness green
  and canonical). First probe of the post-compaction deep audit.
- **`audit/rehearsal/` disposition:** pre-existing untracked scaffolding now carrying W44's
  HeaderRibbon manifest edit ON DISK, deliberately uncommitted (D58.iii). Next formation
  rules track-or-archive (joins the D49 research/ residue row, §C).
- **Glass §7 fixes expected inbound (§D already rows them):** at their BJ land, our
  EasingSpecimenStrip Chip site should self-heal (CSS-only) — verify visually; V-A95
  likewise. If M1/M2 draw replies (passive variant; public content-swap composable), fold
  at the receiving wave: M1→W47 (shell/chrome), M2→W47.
- **`ActionBarLayer` local transition shim:** honest but local; if glass ships a public
  successor, retire the shim at the consuming wave (W47).
