# V′ Frontend Arc — W46 · W47 · W48 (F0 · F1 · F2)

Standalone execution specs under `reformation/V-PRIME.md` (rows W46–W48, laws L1–L8). Each
is executable from itself plus its ≤2 named design-canon references (L3). Evidence is
Browser-first per `EVIDENCE.md §1–2`; no wave here creates a gate, harness or screenshot
taxonomy (L4). π/DELTA use the `EVIDENCE.md` matrix only.

---

# V.W46 — Constitution & Chrome Law (F0)

**Absorbs:** W18 + the P051/P122/type-tuple clauses lifted out of W21–W27/W29 (RF-28: those
shared laws were duplicated P051 ×14 / P122 ×15 across six waves and owned by none).
**Depends on:** W43 (physical structure settled), W44 (routed Glass7 mount — live register can
only be revalidated on a real mount), W45 (honest palette core, for the two retained-Card routes).
**Consumes (design canon, ≤2 per L3):** `VISUAL-CONSTITUTION.md`, `OPTICAL-BENCH-COMPOSITIONS.md`.
**Rider:** CH-6 scene law is **seeded** here (materials/regions/motion owners) and is
execution-gated at W47 — W46 does not itself run scene transitions.

## Current RED
- `ColorNutritionLabel.vue` (About body) still renders a static-definition **Alert**, an empty
  Tooltip/false hover trigger and seven repeated dividers (V-A139) — the born-RED for PR-14.
- Cards read flat cream/brown or muddy dark, vary chrome/selection treatment, and share padding
  with responsive workspace structure; empty companion Cards take half the stage; every Admin
  route still carries an unrelated `Palettes` companion (PR-04). Nested glass/casters repeat a
  boundary already carried by material+spacing (PR-05). Dock/tab/swatch glyphs conflate visible
  size with hit-target padding (PR-12). display/body/mono jurisdictions drift by pane.
- The shared chrome law lives nowhere: P051 (face/seat) and P122 (proportion/boundary/reserve)
  are each restated across the feature waves with no single owner (RF-28).

## Work (Browser-first per `EVIDENCE.md`; DesignSync opened at W46-open per L6)
1. **Open the value.js design-system project (L6).** Create it at W46-open; its source is the
   visual constitution + proportion register; components sync one at a time as later frontend
   waves land them. All design routes through Fable + DesignSync from here on.
2. Author the **eighteen** low-fi→real-render composition pairs (the eleven member routes plus
   Users, Names, Audit, Flagged, Tags, Account, storage-recovery). A family sheet counts only
   when each member has a separately named frame and separately measured protagonist/ratio/
   collapse/mobile-sequence/Card-decision/boundary-reserve/main-count.
3. Install the closed **type role matrix** (route H1→`text-display`+Fraunces; instrument
   identity→`--type-title`+Fraunces; palette identity→`--type-subheading`+Fraunces; section
   heading→`text-heading`+Plus Jakarta Sans; prose→`text-prose`; control/label→`text-small`
   non-bold; value/code→`text-mono-small`/`mono-caption`+Fira Code). P019's `1/√φ` Picker pair
   is the sole exception (its site is W48). About prose ≤66ch, only `Conversion paths` graph +
   fenced code exempt.
4. Implement the binding **PR-33/34/35 inset matrix** and one shared pastel-rainbow identity
   composition consumed by exactly two coordinates: Dock `Palettes` and Library-heading
   `Palettes` (its Dock/Library witnesses are W47/W49).
5. Delete About's Card/Alert/seven-dividers/empty-Tooltip; leave semantic conversion order
   readable without hover (PR-14/V-A139).
6. Transpose Picker's old `<Card>` to a landmark-neutral chassis with exact **0px** outer
   growth at each reference viewport and move the Blob footprint center one settled P122
   title-gap toward chassis center (`±0.5px`); hand the lock/paint to W48. W46 records
   baselines only — it does not claim final PR-01…03.

## Completion evidence
- **The W46 override-count-0 gate (the RF-28 heal, consumed by W49–W54).** W46 is the sole
  owner of (a) the P051 face/seat law, (b) the P122 proportion/boundary/reserve law, (c) the
  type matrix + retained-Card tuple `{size:"sm", material:"content", tier:"quiet",
  surface:"opaque", shadow:false, grain:false, specular:"off"}` with the invariant
  `C=--spacing(4)` five-rung ladder. Every later wave's P051/P122/Card obligation reduces to one
  falsifiable clause: **consumes the W46 fixture; local override/copy/definition count = 0.** A
  local pill/halo/pseudo-marker, a value-defined `--card-pad-*`, a value-local P122 interval
  override, or a duplicated divider reopens **this** fixture, not the feature wave.
- Eighteen pairs separately prove topology; no family substitute. Every P122 workbench passes
  boundaries `[]`/reserve `none`; the five Admin fields keep exactly one adjacent-row separator
  and no terminal rule; every other divider = 0. The eight workbenches pass golden
  `61.8033989/38.1966011` or preview `66.6666667/33.3333333`; Browse/Library pass selected
  protagonist `64…66.6666667` / inspector `33.3333333…36`.
- Browse/Library satisfy `Card count = rendered palette-entity count`; every other composition
  Card count = 0. Cartoon/grid/Card-selected variants = 0. Pastel identity consumers = exactly
  2 (Dock `Palettes` + Library `Palettes`), non-`Palettes` textual consumers = 0 per scheme.
- The closed type matrix resolves at 1440/390/320/actual-400%-zoom; About prose ≤66ch outside
  its two named wider species; Card/Alert/divider/empty-Tooltip counts in About all 0.
- Picker records transposition `0px/0px` and footprint displacement one title-gap toward center
  (`±0.5px`), plus baseline type/gap/`b₀` and the owner handoff to W48 — **no premature PR
  close**. CH-6 scene law is seeded (material/region/motion owners named) but not run here.

## Discharges
RF-22 (DesignSync pathway opened; register stays live through W47–W55), RF-28 chrome-law
single-ownership. PR-04/05/12/14/22/33/34/35. V-A139. Old-charter archaeology U-F10/F12/F17,
T-3/T-11/T-16/T-18/T-24/T-34/T-50/T-53/T-56.

---

# V.W47 — Shell & Scene (F1)

**Absorbs:** W19.
**Depends on:** W46 (constitution/materials/pastel identity/chrome law).
**Consumes (design canon, ≤2 per L3):** `OPTICAL-BENCH-COMPOSITIONS.md §2/§5`,
`VISUAL-CONSTITUTION.md §5.1`.
**Rider — CH-6 execution-gated here (verbatim per L1/RF-26):** scene transitions run RED→
product-green in this RUNNING wave. Per L1 the CH-6 rider may not be re-chartered, re-amended
or re-decided; its next legitimate state is this wave with product-green Browser evidence, and a
further "BUILD W##" scene-transition row is the forbidden re-booking.

## Current RED
- `demo/@/router/index.ts` is a stub router: line 16 declares the routes never drive
  `<router-view>`; line 19 `const Stub = { render: () => null }`; the array at lines 22–35
  registers **nine** member routes (`/`, `/palettes`, `/browse`, `/extract`, `/mix`,
  `/generate`, `/gradient`, `/atmosphere`, `/blob`) plus five `/admin/*` — **`/about` and
  `/easing` do not exist** (PR-15/register, PR-16). The catch-all at line 37 redirects unknown
  routes to `/`.
- The supplied Picker shell shows a dim home mark, a floating eye and an unlabeled vertical
  ellipsis with no purpose/state law (V-A120). Legacy view schema still pairs unrelated
  left/right panes and drives a global mobile `PaneSegmentedControl`. Direct hash transitions
  can leave a one-icon dock; scene mount is rAF-delayed/slab-crossfaded; title scales inside an
  unshrunk header; scenes can transiently expose competing mains/H1s/active AT subtrees.

## Work (Browser-first per `EVIDENCE.md`)
1. Install the **eleven** exact route records with a first-class Dock affordance each; place
   Easing adjacent to Gradient and About in the quiet trailing position; direct-load identity
   and title/H1/focus for `/about` and `/easing`.
2. Render Dock `Palettes` through W46's shared pastel composition (both schemes); every other
   Dock label neutral.
3. One stable shell `<main>`+H1; landmark-neutral route scenes; **one atomic ready-incoming
   handoff** that hides/inerts outgoing and activates incoming with no observable 0/2-subtree
   gap (CH-6). One crossfade owner; like-to-like continuity; reduced motion resolves to terminal
   geometry immediately.
4. Delete the global `PaneSegmentedControl`, its `:deep()` correction, companion labels and
   legacy split-view state; keep Dock route choice. Fixed reserved dock band; whole-header
   contraction at stuck.
5. Resolve V-A120 per site: keep exactly one route-identifying control; the `…` gains a named
   menu + explicit expanded state or is removed; the eye gains a durable named on/off state or
   is removed; labels perceptible in both schemes; no tooltip substitutes for purpose/state.

## Completion evidence
- Route/Dock inventory `9→11`; `/about` and `/easing` direct + Dock reachability `0→1`, no
  companion/query/redirect surrogate. Dock pastel `Palettes` count = 1, non-`Palettes` = 0 per
  scheme, computed composition equal to W46.
- First/middle/terminal/reversal frames each expose main/H1/active-route-subtree counts all = 1;
  no observer/microtask sees zero or two active subtrees (CH-6 gate). Dock-expand changes scene
  rect by **0px**; header stuck contracts title/padding/band; no blank/remount flash.
- Global `PaneSegmentedControl`/view-state/descendant-correction counts `1→0`; earned P092 tabs
  remain feature-owned. Narrow/zoom/software-keyboard modes use document scrolling with no
  nested trap. V-A120 home/eye/ellipsis retain-or-remove outcomes recorded with named
  menu/state attributes and zero duplicate action species.
- Consumes the W46 chrome fixture; shell-local P051/P122 override count = 0.

## Discharges
RF-28 (F1). RF-26 CH-6 execution gate. PR-15(register)/16/17/18. V-A120. Archaeology
T-1/T-14/T-31/T-36/T-37/T-42/T-48/T-52/T-57/T-58/T-61, U-F4/F7/F9/F11/F13/F76.

---

# V.W48 — Picker Instrument (F2)

**Absorbs:** W20 + W21 + W29's **Picker paint slice** — one atomic close, one shared manifest
(RF-28: Picker was 3 files for 1 atomic close). The Blob *studio*/workbench half of old W29
lives at W54, not here.
**Depends on:** W46 (chrome law), W47 (shell/scene, `/` route).
**Consumes (design canon, ≤2 per L3):** `VISUAL-CONSTITUTION.md §3.2`, `PROPORTION-AUDIT.md`
(PR-01…03/13/15-seed).
**Rider:** the RF-28 **phantom-gate HEAL** — see Completion evidence.

## Current RED
- `SpectrumCanvas.vue:8` sets `role="img"` on a pointer-only 2D saturation×lightness field with
  no named numeric S/V axes and no `role="slider"` keyboard path (V-A137).
- `ConsoleRail.vue:13` opens `role="tablist"` (aria-orientation vertical); its `role="tab"`
  buttons at lines 29–30 carry `aria-selected` but there is **no** `role="tabpanel"` /
  `aria-controls` association (V-A138) — role declarations without linkage.
- PR-01's label→headline void has two live causes: `seat.css:88` gives `.title-row` a
  Blob-derived minimum, and `readoutReservation.ts` (with `ColorComponentDisplay.vue:148–166`)
  always allocates Lab two lines and bottom-aligns a one-line value, so the unused legal-width
  line becomes blank air above it. Removing only Blob height cannot close PR-01. (Line anchors
  are today's — re-resolve post-W43/W44.)
- `ColorSpaceSelector.vue:200–209`/`ColorComponentDisplay.vue:136–140` skip a type rung
  (`display2→display4`) instead of one shared P019 `1/√φ` clamp (V-A140). Only the inert-Blob/
  sole-Copy leaf landed; `contenteditable` headline and duplicate editors remain.

## Work (Browser-first per `EVIDENCE.md`)
1. Make the contiguous Fira Code values a **read-only numeric headline**; delete the
   `contenteditable` path; consume one P019 paired clamp at exact `1/√φ` (Fraunces identity arm,
   Fira headline arm) — no local rung (V-A140).
2. Consume P122 `--instrument-title-gap` as `G`; close `I_after ≤ min(φG, I_before − G)` within
   `±0.5 CSS px` at P019 floor/fluid/ceiling and complete-route 1440/390/320/actual-400%-zoom.
   Delete the Blob-derived `.title-row` reservation (`seat.css:88`) and the two-line/bottom-
   align reservation (`readoutReservation.ts`); ordinary tuple = one line, true wrap grows below
   with P122 reserve `none`.
3. Build the shared **domain-neutral axis** composition over BI Slider; give SpectrumCanvas two
   named numeric axes (kills `role="img"`-only, V-A137) and ConsoleRail proper tab→tabpanel
   association (V-A138). W21 numeric fields are the sole editors, co-landing atomically with the
   headline `contenteditable` deletion — no intermediate dual path is mergeable.
4. Bind every bottom channel meter to the same selected-color state/formatted cell as the
   headline; retire static-range copy; keep the meter as `aria-live="off"` text; put channel
   name/value/unit in the slider `aria-valuetext`.
5. ColorSpaceSelector deletes `hide-indicator`, consumes the sole producer `SelectItem`
   indicator/gutter (no local pill/halo/pseudo/marker).
6. Lock W46's chassis/footprint; PROVE (not re-edit — `.325` is already committed in W40-S4)
   Picker's measured `b₀` central-alpha component growth from the
   captured baseline to `0.66 ±0.015` inside the locked footprint via the one effective
   `bodyRadius 0.26→0.325`; write the sole `audit/pi/w48/picker-painted-component/manifest.json`.

## Completion evidence
- **Phantom-gate HEAL (RF-28 / V-A167).** The before-packet is a **captured `.26`
  reconstruction**. The W40-pinned annotated tag `v-blob-b0-26-ref-w40` is the immutable CONFIG
  reference (`bodyRadius: 0.26` at `HeroBlob.vue:206` + the `seat.css:10` seat config) — it is
  NOT a runnable tree: its commit sits post-producer-cut/pre-consumer-migration and that demo
  cannot build. Reconstruct by applying the tag's config values onto the buildable post-W44 tree
  and rendering both engines × DPR 1/2. Record in the manifest the pipeline-independence argument:
  the Blob geometry path (bodyRadius → SDF → alpha) is color-API-independent by construction, so
  the v4 migration cannot confound the `.26`→`.325` growth comparison — state it, don't assume it.
  **Not** the nonexistent `pi/w17/p047-producer-baseline/manifest.json`. W48's after-rows pair coordinate-for-coordinate
  against that reconstruction; the current dirty tree may not be relabeled `b₀`.
- Headline + W21 numeric editors + four channel meters + two-named-axis Spectrum + selector
  **agree in one settled routed frame**. `contenteditable`/duplicate-editor count = 0. Pointer ↔
  keyboard/Home/End ↔ numeric ↔ AT reach identical color in LTR/RTL. Baseline
  `lab(92% 88.8 20 / 82.70%)` reads `92.0% | 88.8 | 20.0 | 82.70%`; moving `a→40.0` reads
  `92.0% | 40.0 | 20.0 | 82.70%` with `aria-valuetext="a axis 40.0"` agreeing; meter
  `aria-live="off"` count = 4, routine live events = 0.
- PR-01/02/03/13/15 anchors: exact line-box `G`; `I_after ≤ min(φG, I_before − G)` within
  `±0.5 CSS px` on every clamp+routed arm; blank line `1→0`; selector `hide-indicator`/local
  marker/pill/halo/pseudo = 0, producer marker = 1; specimen Copy `1→0`, action-region Copy = 1
  (total `2→1`). **A137 and A138 die here** (`role="img"`-only → two named axes;
  tablist gains tab→tabpanel linkage).
- **Blob 0px** chassis/seat/footprint vs the captured `.26` reconstruction; rendered central-
  alpha ratio `0.660192…0.660415`, centroid ≤1.342%, hull inset 12.0%, AA edge ≤1.885/≤0.948
  CSS px DPR1/2, WebGPU/WebGL2 alpha-area delta 0. The composited noninteractive Blob visibly
  occupies the headline→rail interval (screen-space bounds/occlusion recorded) or collapses to
  the named rhythm; buffer-only alpha never closes it (V-A120).
- Consumes the W46 chrome fixture; Picker-local P051/P122 override count = 0. Protected
  inert-Blob/sole-Copy slices resume and never regress.

## Discharges
RF-28 F2 merge (phantom-gate heal). PR-01/02/03/13/15(seed)/23. V-A137/A138/A140/A167/A120.
Archaeology T-2/T-4/T-5/T-7/T-16/T-17/T-28/T-33/T-34/T-35/T-40/T-44/T-50/T-51/T-59,
U-F9/F16/F25/F26/F27/F57.
