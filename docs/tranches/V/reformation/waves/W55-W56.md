# V′ Frontend Arc — W55 · W56 (Adversarial Close · Release + Canon)

Standalone execution specs under `reformation/V-PRIME.md` (rows W55–W56, laws L1–L8).
Executable from itself plus ≤2 named design-canon references (L3). Evidence is Browser-first per
`EVIDENCE.md §1–2`; a cited artifact absent from `git ls-files` is not evidence
(`EVIDENCE.md §6`). No new gate/framework/convergence-clock; the one load-bearing gate
`verify-packed-surface.mjs` (RF-7) is protected (L4).

---

# V.W55 — Adversarial Close

**Absorbs:** W30 (keyboard/mobile/zoom/RTL/AT truth) + W31 (CSS-first paint, fonts, chunks, Web
Vitals) + W32 (real-GPU gestalt + concise record). Fresh non-author readers; **fixes land in the
owning feature wave**, then restart the affected witness — reopen-not-accept.
**Depends on:** W46–W54 (every feature wave green).
**Consumes (design canon, ≤2 per L3):** `EVIDENCE.md`, `PROPORTION-AUDIT.md`.
**Riders — execution-gated here (verbatim per L1/RF-26):**
- **CH-4 gated: p75 LCP ≤2.5s on the named matrix — the ~5s boot dies or V′ does not close.**
- **CH-7 gated: the real-GPU oracle RUNS.**
Per L1 neither rider may be re-chartered/re-amended/re-decided; its next legitimate state is this
RUNNING wave with product-green Browser evidence, and a further "BUILD W##" row is the forbidden
re-booking.

## Current RED
- Card disclosure/reorder, swatch hover actions, eyedropper, gradient stops, spectrum and the
  former contenteditable headline lacked coherent keyboard semantics; overflow/100dvh and
  physical directions failed zoom/keyboard/RTL; several targets were 20–32px; palette cross-role
  failures had no final browser witness (W30 RED).
- Initial JS ≈342KiB gzip and mobile LCP ≈4.9–5.1s (Q14); renderer code statically reachable;
  inactive dialog/barrel code + broad side effects; embedded font CSS ≈100KiB gzip; proxy perf
  gates measured the wrong thing (W31 RED / RF-3 CI cliff).
- U's real-GPU annex never ran and relied on owner attestation; per-mechanism greens coexisted
  with muddy material, action sprawl and janky swaps (W32 RED).

## Work (Browser-first; headed/real-device only where inaccessible, `EVIDENCE.md §1/§5`)
1. **Operation truth (W30):** keyboard-only completes all palette/workbench/overlay/recovery
   journeys against W45; all eleven routes incl. `/about` and `/easing` reachable direct+Dock
   with distinct titles/H1s; every axis/reorder LTR/RTL; P092 only at owner/Admin-Names/Mix;
   exhaustive P051 face/seat/list migration; closed type matrix + About `66ch` at
   1440/390/320/actual-400%-zoom; re-prove Browse/Library Card tuple + native pressed-button
   seat; VoiceOver/Safari, NVDA/Chrome, touch-SR transcripts; forced colors + reduced motion.
2. **Performance lab (CH-4):** freeze `audit/pi/w55/interaction-manifest.json` (36 entries,
   M00–M17 × H-D/H-M) and the production-derived `activation-record.json` (every transitive
   asset in exactly one 18-bit activation family, no all-zero) **before** measurement; run the
   exact **1,440 loads** (18 coordinates × 2 hardware × (20 cold + 20 returning)) on H-D
   (MacBook Pro `Mac17,7`, 1440/120Hz) and H-M (physical Pixel 7, 390/60Hz); run the separate
   54-witness B00–B08 boot matrix; extract value-owned Fraunces to one hashed stylesheet; keep
   the waterfall free of Aurora/Blob/KaTeX/inactive-pane/dialog until causal action.
3. **Real-GPU gestalt (CH-7):** fresh non-author review on identified GPU Chrome/Safari; one
   canonical frame per the eighteen decided compositions + one witness per shared mechanism;
   freeze the immutable `{urlPath,byteLength,sha256}` SPA asset manifest; recreate the Lab/Blob
   crop and try to falsify every proportion/material/motion relation.

## Completion evidence
- Operation: route/Dock inventory = 11, `/about`/`/easing` reachability = 1 each; overflow = 0;
  main/H1/active-subtree = 1 at every frame; global-pane/contenteditable = 0; exact type-role
  equality; Card tuple/ladder/gutter + pressed-ID↔inspector-ID parity re-proven; four Lab meters
  read `92.0% | 88.8 | 20.0 | 82.70%` → after `a→40.0` `92.0% | 40.0 | 20.0 | 82.70%` with
  `aria-valuetext="a axis 40.0"`, meter `aria-live="off"` count 4, routine live events 0.
- **CH-4 terminal gate:** every cold/returning 20-run cell reports its own lab **p75 LCP ≤2.5s**,
  INP ≤200ms, CLS ≤0.1 with raw nodes/interactions/sources; no pooled route/device result; the
  ≈5s boot is dead or V′ does not close. All 54 B witnesses execute; each ready arm's B0/app-ready
  scheme/CSS/atoms match and are nonempty; B08 proves the terminal error alone. Inline-Fraunces
  and tnum-script dependencies `1→0`; each renderer request follows its recorded critical LCP
  candidate.
- **CH-7 gate:** the real-GPU oracle RUNS — the coverage map is orthogonal (no route×state×
  scheme×viewport Cartesian), every omitted combination points to its witness, and **two fresh
  gestalt passes enumerate zero new family or open visual gap**. Activation-record→SPA-manifest
  missing/extra/hash mismatch = 0. Concise π/DELTA; any product/asset fix reopens its owning wave
  and restarts the two-pass clock — reopen-not-accept.

## Discharges
RF-26 CH-4 + CH-7 execution gates; RF-3 CI re-gating — W55 OWNS the .github edit for bracket B3's
third re-gate (the journey subset enters ci.yml; the 11.7k-line e2e corpus prunes to those journeys),
exercised through the journey matrix; RF-7
protected. Archaeology U-F3/F16/F25–F27/F54/F55–F58/F64, Q14.

---

# V.W56 — Release + Canon

**Absorbs:** W33b (final canon/deploy/downstream-lock close). W33a's fresh Glass7 publication +
W44's routed-mount refresh are upstream and already landed; W56 does not republish immutable
Value4/Keyframes6.
**Depends on:** W44 (routed Glass7 consumer), W45 (honest palette core), W55 (frozen SPA asset
manifest + eighteen deterministic coordinates), WL (per-row library-evolution ship verdicts).
**Consumes (design canon, ≤2 per L3):** `EVIDENCE.md §6`, `OPTICAL-BENCH-COMPOSITIONS.md`.

## Current RED
- The living canon still carries sediment and the D27/D2/byte-count contradictions until the
  compression at W41; downstream demo/dev locks and the deploy live-wire proof are unrun; the
  2026-07-16 grand edicts E1–E11 have no terminal PROMPT-RECAP §7 rows with owners (RF-20); the
  library-evolution covenant rows (RF-17/27) have no shipped verdict feeding the version choice.

## Work (Browser-first; package/cross-engine automation only where inexpressible)
1. **Ordered release of whatever V′ ships (4.x / 5.0 per WL).** The version is decided by WL's
   per-row verdicts: any breaking library change (e.g. RF-17 §D's diagnostics-bearing parse-API
   reshape, if WL ships it — §E's rename already shipped inside immutable 4.0.0 per WL's census)
   forces 5.0-or-decline; otherwise ship 4.x. A fresh authorized checkout deletes prior
   output, runs the unconditional production build, packs **one** designated release-candidate
   tarball as the sole `npm publish <tgz>` input; `verify-packed-surface.mjs` and the seven-entry
   surface hold byte-identical (RF-7).
2. **Downstream locks are registry-only.** After publication, external consumers install registry
   artifacts and rerun their W44 smoke; no file link, force, fabricated/stale lock or hidden peer
   bridges the graph.
3. **Final canon truth (L2):** canon describes only the landed physical tree, seven entries,
   palette ontology, Optical Bench, CSS-first boot and the ordinary evidence path; the immutable
   facts live in one document and are referenced elsewhere; no hand-kept route table or `/docs`
   product route survives.
4. **Coordination outbound (RF-16, L7):** author the closing packets — glass
   `valuejs-inbox-YYYY-MM-DD-<topic>.md`, keyframes bilateral `KF-TO-VALUEJS-V`↔`VALUEJS-*`,
   atlas via the P-tranche coordination dir; relay-sent is never a terminal state.

## Completion evidence
- **PROMPT-RECAP §7 (E1–E11 terminal, RF-20 / E10) — physical home: `reformation/DISPOSITIONS.md §1`
  finalized in place** (the old PROMPT-RECAP.md is archived read-only at W41; DISPOSITIONS §1 is the
  seed and becomes the terminal recap):** every grand edict has one terminal row
  with an owner and no silent drop — **E1** colocation (demo+library, backend equivalent) →
  W43; **E2** structure idiom / no god modules → W43; **E3** KISS/parsimony net-negative LoC →
  L4 across the arc; **E4** gate/test pruning → W42; **E5** DesignSync design pathway → W46;
  **E6** glass root-defect batch/relay → this wave's outbound packets; **E7** bi-directional
  glass/sci/atlas comms → RF-16 packets; **E8** keyframes parallel-tranche coordination →
  bilateral letter; **E9** chronic→decided discipline → the RF-26 execution gates (D-1/D-2/CH-4/
  CH-6/CH-7/CH-8) all ran as product-green waves, none re-booked; **E10** prompt-recap totality →
  this section; **E11** live-Glass audit linkage → the W44 registry refresh (ephemeral archive
  never a committed lock). The over-claims RC-1→W10 and K-22→W16 are corrected.
- Before any release/deploy claim the served entry document + every transitive SPA asset
  reproduces W55's frozen `{urlPath,byteLength,sha256}` manifest and its canonical digest
  exactly; an added/absent/renamed/byte-different asset reopens W55, not a new golden here. From
  the live authority the in-app Browser opens a cold boot + each of the eighteen canonical
  coordinates at their W55 settlement tuples; static structure/state counts match exactly and
  dynamic frames satisfy the W55 numeric tolerances (P019 `1/√φ`, line-box/inset `±0.5 CSS px`,
  Blob `0.66 ±0.015`, Card/boundary/reserve counts).
- Live deploy confirms security headers, `st1_`/`__Host-value-session` cookie issue/replay/expiry
  and same-origin auth (**M3 headers**: HSTS/nosniff/CORP/referrer/CSP, `Cache-Control: no-store`);
  OpenAPI/client examples advertise `https://color.babb.dev/api` only. A genuine external deploy/
  webhook blocker leaves W56 **BLOCKED** with exact evidence — recording it is not completion.
- Candidate registry bytes equal `dist.integrity`; `exportsSha256`/declaration hashes obey the
  export-object byte law; two final canon audits are clean; every K/N/T/U/recent-prompt chronic,
  partial and banked route has terminal disposition. W56 alone records **SHIPPED**.

## Discharges
RF-20 PROMPT-RECAP §7 (E1–E11) totality; RF-16 coordination outbound; RF-17/27 library-evolution
ship verdicts feed the version choice; RF-7 protected. Archaeology U-F21/F28/F39/F61/F63/F66/F68/
F69/F71/F77 (U-F41 remains BANKED until NCSU VPN access).
