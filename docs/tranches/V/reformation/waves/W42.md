# V′.W42 — Subtraction (repo half)

**Absorbs:** old W4 (repo half — proof theater, benches, worktrees, residue) + RF-4/5.
**Depends on:** W40 (dirty tree committed before anything is pruned) — runs parallel to W41.
**Worktree/branch:** primary tree, current branch. This wave DELETES; it mints no new gate,
script, or framework (V-PRIME L4). Every deletion is a committed `git rm` (or `git worktree
remove`); the one load-bearing gate `scripts/ci/verify-packed-surface.mjs` is PROTECTED (RF-7) —
never in scope here.
**References (≤2):** `../../SUBTRACTION.md` (the one subtraction front) ·
`../../audit/REFORMATION-2026-07-16.md` (RF-4/5 + RF-29 B5/B6 evidence; the probes decision is
owner-bracket B4 — distinct namespace from RF-29's B-numbers).

## Current RED (verify each yourself)

1. **Proof-theater relocated into the unit suite (RF-4).** 8 meta tests (~842L) assert canon-sync /
   gitignore / shot-policy / apparatus-grep rather than product behavior. Verify:
   `wc -l` on the eight files (kill list below sums to 842).
2. **Dead + orphaned probes (RF-4, bracket B4).** `scripts/ci/oracle-slate-teeth.mjs` (187L) is
   fully dead; `scripts/ci/boot-smoke.mjs` (160L) + `scripts/ci/css-emission-probe.mjs` (171L) are
   CI-orphaned — bracket B4 rules DELETE both (W44 routed-mount witness + W50 journeys supersede them).
3. **`.release-working/` in lint scope (RF-29 B6).** Verify: `ls .release-working/ | head` (an
   operational clone — `.changeset`, `.env.example`, a duplicate `scripts/ci/oracle-slate-teeth.mjs`);
   un-ignored, uncommittable residue.
4. **16 stale `wf_*` worktrees, ~11GB (RF-29 B5).** Verify: `git worktree list | grep -c wf_` → 16
   (all under `.claude/worktrees/wf_*`).
5. **Orphan package.json scripts.** The `proof:*` scripts were `git rm`'d at W40-S1 but their
   `package.json` entries survive; boot-smoke/css-emission-probe/oracle scripts point at files this
   wave deletes. Verify: `grep -nE 'proof:|boot-smoke|css-emission|oracle' package.json`.
6. **W4 execution divergence (RF-5).** Superseded design docs were modified-not-deleted; ALL 11
   benches were deleted where `SUBTRACTION.md` said retain-the-proven-useful-set (bench/ is empty).

## Work

**1 — Prune the 8 meta tests (RF-4).** `git rm` each; total −842L:

| Path | Lines |
|---|--:|
| `test/oracle-feasibility-leg.test.ts` | 197 |
| `test/session-single-source.test.ts` | 133 |
| `test/frontend-headers.test.ts` | 103 |
| `test/demo-state-cluster.test.ts` | 96 |
| `test/dist/ground-single-source.test.ts` | 88 |
| `test/no-build-in-unit-suite.test.ts` | 84 |
| `test/dist/shot-policy.test.ts` | 71 |
| `test/dist/gitignore-auth.test.ts` | 70 |

**2 — Delete the dead + orphaned probes.** `git rm scripts/ci/oracle-slate-teeth.mjs` (187L, dead)
`scripts/ci/boot-smoke.mjs` (160L) `scripts/ci/css-emission-probe.mjs` (171L) — the latter two per
bracket B4. (css-emission-probe rode W40-S1 as a staged modification; it dies here — a superseded
modification, not a resurrection.)

**3 — Delete the superseded design docs (NOT modify — RF-5).** `git rm docs/tranches/V/DESIGN-CAMPAIGN.md`
and `git rm -r docs/tranches/V/design/` (the entire second-V process: `design/portfolio.md`
pointer + every old charter, pseudo-gate, status file and executable pass-4 instrument). These are
the "second V process" SUBTRACTION forbids; W41 already carried any still-substantive finding into
the KEEP canon. (Old handoffs are ARCHIVED at W41, not deleted here — the split is deliberate.)

**4 — Remove `.release-working/` and ignore it.** `rm -rf .release-working/`; add `.release-working/`
to `.gitignore`. It is an operational clone, never a tracked artifact.

**5 — Prune the 16 `wf_*` worktrees (RF-29 B5, safe-delete law).** For each path in
`git worktree list | grep wf_`: record merged-state / unique-commit / dirty-state / ownership
evidence (SUBTRACTION safe-delete proof), then `git worktree remove --force <path>`; finish with
`git worktree prune`. ~11GB reclaimed. Any `wf_*` carrying unique un-mergeable user work is
preserved and named, not force-removed.

**6 — Close the package.json script surface.** Delete every `scripts` entry whose target file no
longer exists (`proof:*`, `boot-smoke`, `css-emission-probe`, `oracle:*`, and — if step 8 removes
the runner — `bench`). No script may reference a deleted file; keep `build`, `typecheck`, `lint`,
`test`, pack, and the protected `verify:packed-surface`.

**7 — Ratify the staged deletion sweeps (RF-5).** The W40-committed producer deletions (proof gates,
old src/test trees) are the subtraction sweep — this wave ratifies them by leaving the tree with
one ordinary developer path; it does not re-delete.

**8 — Close the bench question (RF-5) — RULED at re-formation (Fable): full deletion RATIFIED.**
`bench/` is empty (all 11 deleted at W40-S1, diverging from SUBTRACTION's retain-the-proven-useful-
set). The deleted 11 were stale simulated/proxy benches; W55's performance lab owns real measurement
and creates its own instruments from its own spec if needed — never a resurrection. Remove the
`bench` script + any bench config; record the divergence-from-SUBTRACTION rationale in the
commit body. No orphan runner survives.

**9 — Implement bracket B3's first re-gate (RF-3):** wire the demo typecheck
(`npx vue-tsc -p tsconfig.demo.json --noEmit`) into the ci.yml producer job. The api-test re-gate is
W45's completion duty; the journey-subset re-gate is W55's. Gate: the CI run fails on a demo type
error introduced on a scratch branch (prove once, revert).

Commit the sweep as `chore(v-w42)!: prune proof-theater, orphaned probes, stale worktrees, release residue`
with a body naming every deleted path, the reclaimed worktree GB, and the bench decision.

## Completion evidence (falsifiable)

- `npm test` GREEN with the pruned suite; the 8 meta-test paths + `oracle-slate-teeth.mjs` +
  `boot-smoke.mjs` + `css-emission-probe.mjs` all return "No such file" (`ls` each).
- `git worktree list | grep -c wf_` = 0; `git worktree list` shows the primary + intentional only.
- `ls .release-working` → "No such file"; `grep -c '.release-working' .gitignore` ≥ 1.
- No package.json script without a consumer: for every `scripts` value, the referenced file exists
  (`grep -oE '(scripts|node)[^"&|]+\.(mjs|ts|sh)' package.json` → each path resolves).
- `git ls-files docs/tranches/V/DESIGN-CAMPAIGN.md docs/tranches/V/design/` returns nothing.
- `bench/` is either populated with the proven-useful subset + a working `npm run bench`, or the
  `bench` script is absent (whichever the commit body decided) — no half state.
- `verify-packed-surface.mjs` still present and invoked (RF-7 protection held).

Born-RED: 16 wf_* worktrees, `.release-working/`, all 8 meta tests + 3 probes present today.

## Discharges

RF-4 (proof-theater in the unit suite pruned; the two orphaned probes deleted per B4), RF-5 (W4
execution divergence closed — design docs deleted not modified, bench question settled),
RF-29 B5 (16 wf_* worktrees pruned) + B6 (`.release-working/` removed and ignored). Old ledger:
old W4's repo half; SUBTRACTION's "Delete in W4" list executed with the archive-not-destroy
carve-out handed to W41.
