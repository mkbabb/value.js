# W44 — Glass rail (value consumer refresh + routed mount; CH-5 dies here)

**Absorbs:** old W17a/W33a (glass, externally owned) + W17b/W33(pkg) (value side) + old W3's
glass ask/status reconciliation duty + registry
families RF-13 (consumer-surface drift), RF-26/CH-5 (glass-adoption disease rider), and the two
2026-07-16 GLASS-INBOUND marks (clipboard primitive; HeaderRibbon persistent-only). Their content
is inlined below — no third ref needed.
**Depends-on:** W43 (settled demo tree — the migration lands on colocated homes). EXTERNALLY gated
on the live glass agent's strict/native/Q003 close → fresh Glass 7 publish. W45/WL never wait on glass.
**Named refs (L3, ≤2):** `V-PRIME.md` (§2 W44 row, L1/L7) · `coordination/CONSTELLATION.md`
(inbox law + release order). The old handoff's §4 glass-boundary facts and §5 repair order are
INLINED below and in the acceptance contract; its archived copy
(`archive/HANDOFF-2026-07-16.md` post-W41) is background provenance only, never required reading.
**Worktree:** dedicated worktree; cross-repository lane — must NOT collide with the glass owner
(L7: glass root-defects batch into `valuejs-inbox-*` letters, never interrupt the live agent).

## Boundary — what this wave does and does NOT own

The **glass half is externally owned** (the live glass agent). This spec covers ONLY the value-side
W17b consumer refresh and the ACCEPTANCE of glass evidence. It does not respecify glass-internal work
(strict declarations, HeaderRibbon internals, Q003) beyond the acceptance contract below. Per L1,
CH-5 (glass adoption) is execution-gated here: RUNNING this refresh is its terminal state, not another
charter.

## Current RED (verifiable — born-RED: the app is blank today)

Fresh in-app Browser at the canonical URL
`http://127.0.0.1:9000/#/?space=lab&color=lab(92%25+88.8+20+/+82.7%25)` returns
`{"appChildren":0,"bodyText":0,"main":0,"overlay":0,"readyState":"complete"}` — a pre-mount ESM
failure (HANDOFF §5). `node_modules/@mkbabb/glass-ui` → a pinned Glass **5.0.0** island whose packed
chunks import removed Value-root APIs and nest Keyframes 5.2.0 / Value 3.1.0; that mixed graph
evaluates before `createApp().mount()`.

**RF-13 dead specifiers** (imports dead in packed Glass 7 — verified against the tree):

| Site | Line | Dead import | Successor (resolve symbol vs the immutable Glass 7 artifact) |
|---|---|---|---|
| `dock/menus/MobileMenuDropdown.vue` | 6 | `DarkModeToggle` from `glass-ui/controls` | `glass-ui/dark-mode-toggle` (Glass 7 adds it) |
| `dock/menus/ProfileSection.vue` | 8 | `DarkModeToggle` from `glass-ui/controls` | `glass-ui/dark-mode-toggle` |
| `panes/…/PalettesPane.vue`¹ | 133 | `ConfirmDialog` from `glass-ui/confirm-dialog` | `glass-ui/dialog` |
| `palette-browser/admin/AdminUsersPanel.vue` | 186 | `ConfirmDialog` from `glass-ui/confirm-dialog` | `glass-ui/dialog` |
| `gradient/…/EasingSpecimenStrip.vue` | 14 | `ToggleChip` from `glass-ui/toggle-chip` | `glass-ui/chip` (MIGRATION.md: toggle-chip → `<Chip>`) |

¹ table paths/lines are TODAY'S tree (pre-W43) except PalettesPane's, marked post-W43;
AdminUsersPanel and EasingSpecimenStrip also relocate in W43's colocation — the gate is
path-independent (repo-wide grep), so re-resolve every site against the settled tree, never
these line anchors. Provenance corrected per the
glass **install-truth packet** (glass-ui `b43b9f91`, 2026-07-17 — the Q060 delta table delivered
early; content inlined here, provenance-only, not a third L3 ref): the 6→7 bump removes ELEVEN
subpaths (`controls`, `icon-tooltip`, `icon-chip`, `color-swatch`, `focus-scope`, `motion-curves`,
`notification`, `spa-view`, + the metric triplet → `/metric`) and adds THREE (`dark-mode-toggle`,
`metric`, `styles/theme`); `confirm-dialog`/`toggle-chip` died earlier, at glass **5.0.0** (the
20-key drop its CHANGELOG under-reported — packet §2) — equally dead in packed Glass 7, which is
the surface this table migrates against. **Verified 2026-07-17: no demo import touches ANY other
removed subpath** (all 11 + the 5.0.0 survivors relayed wrongly as removed) — the five sites above
are the complete dead set. Packet §2 law adopted: the EXPORT-MAP DIFF, not CHANGELOG prose, is the
authoritative migration surface. The 40 root-entry imports still owe a symbol-level diff before
this refresh executes.

## Acceptance contract (glass evidence — independently verified before W17b runs)

Accept the glass owner's handoff ONLY when it delivers (HANDOFF §4 return contract): one fresh
absolute tarball path + byte count + SHA-256/SHA-512/npm-integrity/shasum; source commit/tree (or a
declared dirty synthetic-tree identity); package/export/typesVersions/declaration map (74 export /
68 typesVersions keys — the held 73/68 archive is NOT it; cross-check the install-truth packet's
Q060 delta: 82→74, 11 removed / 3 added incl. `styles/theme`); dependency/peer/optional map with Embla
ownership; strict declaration closure (`skipLibCheck:false` isolated consumer); build/test/demo-build/
package-verifier results; registry-free isolated import; and the native Q003/HeaderRibbon packet with
zero black slab, zero lost target, zero occlusion corruption on repeated forward+reverse native cycles.
Independently re-verify these facts (do not infer from prose). W33a owns publication; this wave does
not tag or publish glass.

## Work (value side, in HANDOFF §5 repair order — only after immutable Glass 7 exists)

1. **Replace the pinned Glass 5 island** with the registry triple: Glass 7 + Keyframes 6.0.0 +
   Value 4.0.0. Regenerate the demo consumer lock **registry-only** — no file/tarball/link/git edge,
   no symlink island, and **NEVER `--legacy-peer-deps`** (install-truth packet law: it masks
   ERESOLVE while installing a graph the library declares incompatible; the wedge closes only by
   Glass 7 existing — which is this wave's external gate, already required before step 1 runs).
   One physical deduped Value core; no Value 3 / Keyframes 5 remnant.
2. **RF-13 five-site migration** (table above): repoint each dead specifier to its successor,
   resolving the exact symbol against the packed Glass 7 surface. `/controls`→`dark-mode-toggle`,
   `/confirm-dialog`→`dialog`, `/toggle-chip`→`chip`/`toggle-group`.
3. **`writeClipboard` adoption (all current sites — 17 files at last census; the gate is
   count-independent, DIRECT — no adapter)** — GLASS-INBOUND clipboard mark.
   Glass 7 cut `copyToClipboard(text,{onCopyError}):Promise<boolean>`; the honest primitive is
   `writeClipboard(text):Promise<CopyResult>` where `CopyResult = {ok:true}|{ok:false;reason:"clipboard-api"|"no-api"}`.
   RULED (L4 + the standing no-backwards-compat law, overriding the inbound letter's convenience
   suggestion): migrate each site directly by call-site shape — ignore-failure sites
   destructure `{ok}`; report-failure sites branch on `r.ok`; confirmation/status-UI sites adopt
   `useClipboard({resetMs})` and delete their hand-rolled copy/timer machinery. The retired
   boolean+callback signature is NOT preserved locally; no preset adapter exists. (The letter's
   adapter sketch below is recorded as the DECLINED alternative —

   ```ts
   import { writeClipboard } from "@mkbabb/glass-ui";
   export const copyToClipboard = async (
       text: string,
       { onCopyError }: { onCopyError?: (reason: string) => void } = {},
   ): Promise<boolean> => {
       const r = await writeClipboard(text);
       if (!r.ok) onCopyError?.(r.reason);
       return r.ok;
   };
   ```

   — declined.) All current `copyToClipboard` sites
   (`App.vue`, `useColorPipeline.ts`, `usePaletteActions.ts`, gradient/mix/generate/color-picker/
   palette-browser components) migrate DIRECTLY by call-site shape per the ruling above; sites that
   show confirmation UI adopt `useClipboard({resetMs})` and drive off `status`, deleting any
   hand-rolled copy/timer machinery. No local util mediates.

3b. **Declare the dependency truth (RF-29 B2).** `@mkbabb/glass-ui` and `@mkbabb/keyframes.js`
   become DECLARED package.json dependencies at their immutable registry versions; the lock
   regenerates registry-only; `node_modules/@mkbabb/glass-ui` is a real installed package —
   `readlink node_modules/@mkbabb/glass-ui` = not-a-symlink (the 4.8GB pinned island is gone).
4. **HeaderRibbon persistent-only** — GLASS-INBOUND HeaderRibbon mark supersedes V-A92's opt-in-
   collapsible arm (VNode inference stays dead; persistent/`#items` stays). The value demo has ZERO
   HeaderRibbon component consumers (verified). The CANON rows the inbound letter enumerates
   (`ADDENDA.md` V-A92/V-A128, old `FINAL.md:40`, `PROMPT-RECAP.md:225`, `DECISIONS.md` D2/D41)
   are updated at **W40-S5** while those docs are still live — W41 archives most of them, and
   archived docs are read-only. W44 owns only the LIVE remainder: the rehearsal manifest
   `audit/rehearsal/w17/glass-7.0.0/manifest.json:130` `headerRibbonMode` expectation →
   persistent-only, plus accepting the persistent-only shape in the packed Glass 7 evidence.
5. **Remove stale demo canon** — the Value3/root/`/parsing`/`/units` producer references in active
   demo docs (HANDOFF §4.4). Run the full demo build incl. the separately stale lazy About closure.

## Completion evidence (falsifiable)

- **Routed mount witness** (V-PRIME, verbatim), reload the canonical URL in the in-app Browser and
  prove, in one settled frame: a **nonempty `#app` subtree**, exactly **one `<main>`**, selected
  **Lab space**, the **numeric headline**, and **ActionToolbar's sole Copy** (no duplicate Blob Copy).
  This is the reproduction of the born-RED blank observation turning green.
- The five RF-13 sites resolve against Glass 7 (typecheck + build green; no dead specifier remains).
- `writeClipboard`/`useClipboard` adopted at every site directly; repo-wide grep for
  `copyToClipboard` = 0 (count-independent; no glass import, no local adapter, no preserved signature).
- HeaderRibbon persistent-only rows updated at every enumerated canon/manifest site.
- The consumer lock is regenerated **registry-only** (no file/tarball trick; one deduped Value core);
  the pinned Glass 5 island is gone from `node_modules` topology.
- Independent verification of the accepted glass artifact recorded (hashes/declaration/export map);
  glass publication (W33a) is a separate, external transition — not claimed here.

## Discharges

RF-13 (consumer-surface drift, 5-site table) · RF-26/CH-5 (glass-adoption disease rider —
execution-terminal) · the two GLASS-INBOUND marks (clipboard primitive, HeaderRibbon persistent-only).
Backs L7 (glass coordination via inbox letters, never interrupting the live agent).
