# V′ Frontend Arc — W53 · W54 (F7 · F8)

Standalone execution specs under `reformation/V-PRIME.md` (rows W53–W54, laws L1–L8).
Executable from itself plus ≤2 named design-canon references (L3). Evidence is Browser-first per
`EVIDENCE.md §1–2`; real GPU is identified where translucency/Aurora/Blob/motion is claimed
(`EVIDENCE.md §5`). Both waves consume the **W46 override-count-0 gate**: P051/P122/Card
obligations = "consumes the W46 fixture, local override count = 0" (L4/RF-28).

---

# V.W53 — Workbenches (F7)

**Absorbs:** W25 (Generate) + W26 (Extract, Mix) + W27 (Gradient, Easing) — **five features,
disjoint commit slices**; a shared failure reopens W48 (axis) or W50 (palette semantics), never a
local variant.
**Depends on:** W46 (chrome law), W47 (`/generate` `/extract` `/mix` `/gradient` `/easing`
routes), W48 (the shared axis + P019/P051 laws), W50 (palette save/promotion semantics).
**Consumes (design canon, ≤2 per L3):** `VISUAL-CONSTITUTION.md §7`, `PROPORTION-AUDIT.md`
(PR-09).

## Bracket B1 — perceived-space stage (owner-gated; Fable recommends **rebuild-on-v4**)
The v4 API cut deleted the `/color` internals behind three dirty-tree features (RF-1). Two were
**RULED terminally retired at W40-S3 (bracket B1)** with their deletion rows landed in W40-S4's consumer
slice — the gamut-truth overlay (−983) and ink-walk/`normalizedColorNames` (−176), superseded by
the veil semantics + the ink ladder; W53 verifies their ABSENCE only (0 importers, 0 files). The
**perceived-space plate** (−1034) is the live bracket because the Gradient workbench's spec
depends on it. W53 specs **both arms**:
- **Arm A (recommended):** rebuild the perceived-space plate on the v4 API as the Gradient
  workbench's honest protagonist instrument; PR-09 ENLARGE closes against it.
- **Arm B:** terminally retire the plate; Gradient's protagonist falls back to the spectral
  meniscus preview alone and PR-09 closes against that.
The wave does not self-select; it lands whichever arm the owner ratifies at bracket B1.

**Path convention:** repo-root-relative; `index.html` = `demo/color-picker/index.html`;
`vite.config.ts` = the repo-root config.

## Current RED
- Generate: a flat strip plus rounded squares; Regenerate is a pill while Save/Copy are bare
  glyphs; three action registers compete; output is not a truthful draft (T-54/T-55).
- Extract: an underdeveloped flat control plate and pointer-first eyedropper.
- Mix: live 2–12-color and plural-palette capability hidden behind shadow palettes; animation
  obscures object relationships.
- Gradient: owner-declared unusable; controls/rail proportion weak; stop edits pointer-heavy.
- Easing: selector too small/broken relative to the keyframes curve instrument; perceived-space
  plate and its dependents are RED under the v4 cut (RF-1/B1).

## Work (Browser-first per `EVIDENCE.md`; per-feature slice)
1. **Generate** — P122 `golden` specimen/model/commit; narrow order specimen→model→commit;
   shared W48 count axis; truthful preset/harmony previews + seed provenance; one Dock action set
   (species `3→1`); stable geometry across regeneration.
2. **Extract** — image-dominant develop sequence; keyboard/numeric sampler x/y parity; Save
   preserves provenance; undeveloped result stays contextual.
3. **Mix** — ordered N-operand (2–12 colors / ≥2 unequal palettes) convergence trough; Colors/
   Palettes retained as the third earned P092 source-mode tab; each result color exposed as a
   named list/text item beside an inert face; one copy action labeled by object truth
   (`Copy color` / `Copy palette`) with a durable announced result.
4. **Gradient + Easing** — two independently direct-loadable P122 compositions (no nesting);
   spectral meniscus for Gradient only; each WatercolorDot stop is a face inside a separate named
   geometric seat owning selection/focus/drag; centered 19–22rem Easing curve; §5.2 signed
   stop/axis keys; land the bracket-B1 arm for the perceived-space stage.

## Completion evidence
- Per feature: **consumes the W46 law (override 0)** + **W48 axis adoption** + **one action
  grammar** + **pointer↔keyboard parity** + **reduced-motion terminal**. Every retained axis is
  a W48 consumer; only chromatic domains are spectral, time/easing neutral with the specimen
  moving through it.
- Generate golden three-region ratio + narrow order exact; action species `3→1`; regenerate
  output-only rect change. Extract stage shares + sampler parity + provenance. Mix proves
  2/3/12/unequal inputs, add/remove/reorder/leftover/provenance/no-filler; P092 four-edge
  `≤0.5px`, focus separate, override 0; result list-item count = output color count; copy
  name/result matches object kind.
- Gradient/Easing: independent route/chassis counts; PR-09 protagonist `19–22rem` curve /
  meniscus dominates supporting controls; every stop's geometric seat (not its face) owns
  selected/focused/grabbed state with **zero** face register; array order = visual order =
  serialization. The ratified B1 arm is live (plate rebuilt on v4, or retired with the meniscus
  fallback); gamut overlay + ink-walk absent. Under P051 no ring/shadow/pseudo crosses any
  organic face; feature-local P051/P122 override count = 0.

## Discharges
RF-28 (F7). RF-1 bracket B1 (perceived-space arm) execution + absence verification of the
retirements ruled at W40-S3 and landed at W40-S4 (rebuild source: tag
`v-perceived-space-plate-ref-w40`). PR-09.
Archaeology T-6/T-13/T-19/T-21/T-22/T-29/T-41/T-44/T-46/T-47/T-54/T-55, U-F8/F33.

---

# V.W54 — Atmosphere & GPU (F8)

**Absorbs:** W28 (chromatic boot + Aurora) + W29's **Blob studio** (the dedicated `/blob`
workbench; the Picker paint slice already closed at W48).
**Depends on:** W46 (chrome law), W47 (`/atmosphere` `/blob` routes), W48 (the frozen seed/atom
graph + `bodyRadius` calibration heal).
**Consumes (design canon, ≤2 per L3):** `VISUAL-CONSTITUTION.md §6/§6.2`, `EVIDENCE.md §5`.
**Riders — execution-gated here (verbatim per L1/RF-26):**
- **D-1 aurora-derive execution-gated here — derive-from-color RUNS or V′ does not close.**
- **D-2 blob-extirpation execution-gated here.** Per L1 neither rider may be re-chartered,
  re-amended or re-decided; the next legitimate state is this RUNNING wave with product-green
  Browser evidence, and a further "BUILD W##" row for either is the forbidden re-booking.

## Current RED
- `vite.config.ts:157` defines `function groundRecordInject(): Plugin` whose
  `transformIndexHtml` calls `injectGroundTokens(html)`, and line 170 registers it in
  `defaultPlugins`; `index.html:102–106` is its consumer — the `@property --saved-bg-n` block
  seeded with `__GROUND_LIGHT_0..3__` tokens. Pre-module paint therefore uses injected constants
  and a stop cache that cannot see URL precedence, while hydration separately parses URL/storage
  and derives the model — **two boot authorities** that can correct chroma after frame zero.
- Aurora runtime is statically reachable; atmosphere config is a generic selector/slider form;
  pointer response and zones are weak. Blob is a 31-slider settings dashboard; the dedicated
  workbench, lifecycle reduction and final product evidence are unbuilt (V-A167 heal already
  supplied the `.26` baseline at W48).

## Work (Browser-first per `EVIDENCE.md §5`, real GPU identified)
1. **Boot (§6.2):** replace the injected stop constants with the strict numeric `SeedToken` boot
   union; resolve/override the OS scheme first, apply the exact root `.dark` class +
   `colorScheme` synchronously, then validate/format tokens in the parser-free prepaint script
   and select URL→storage→resolved-default once. Delete `groundRecordInject` /
   `injectGroundTokens` (vite.config.ts:157/170), the `__GROUND_LIGHT_*__` injection
   (index.html:102–106), the stop cache and the initial `deriveAurora` twin. One same-origin
   external classic script in `<head>`, no inline code — `script-src 'self'`.
2. **Aurora (D-1):** derive the seven-atom color model from the frozen boot/computed atoms;
   project P043/P045/P054 lifecycle through P046's facade
   (`ground→eligible→queued-after-critical-paint→fetching→active|parked|unsupported|failed`);
   preview-dominant `66.6666667%` chassis; each atom/control maps to a perceptible effect;
   eight-zone `MAX_NUCLEI=8` fixture with WebGPU/WebGL2 parity.
3. **Blob studio (D-2):** the dedicated `/blob` canvas fills the exact `preview-dominant`
   66.6666667% stage (no Picker diameter reuse); compact essentials + scroll-confined advanced
   disclosure + reset/compare; every retained axis adopts W48 and visibly affects a named
   morphology variable; retire the disproportionate knobs; `/blob-config` runtime-free.

## Completion evidence
- **Boot:** the full negative `SeedToken` set is rejected (`var()`/`env()`/`currentColor`/system/
  relative/CSS-wide keywords/URL/calc/self-reference/malformed/out-of-range/noncanonical/corrupt-
  JSON/schema/color-only-legacy/mismatch/invalid-mode/invalid-default). Every ready arm's
  B0→app-ready scheme, selected CSS and seven-atom set are **byte-identical** and nonempty with
  no chroma/geometry correction; the invalid-default arm proves the separate terminal error with
  absent `--ground-seed`/atoms. `groundRecordInject`/`__GROUND_*__`/stop-cache/parallel TS
  derivation **absent**; the same-origin script passes strict `script-src 'self'`.
- **Aurora (D-1 gate):** derive-from-color RUNS — every select/axis visibly changes its named
  atom/effect on the live preview; Aurora enters after critical paint with **0px** rect/chroma
  discontinuity; lifecycle status truthful in UI + AT with retry; ambient motion stops within 5s
  or exposes a persistent announced still/pause. Failure/reduced-motion retains complete CSS
  truth. **D-1 does not close as a decision — it closes as running product-green Browser
  evidence.**
- **Blob (D-2 gate):** truthful `fetching/active/parked/unsupported/failed` lifecycle;
  **arm-after-critical-paint 0px shift**; the canvas fills the `preview-dominant` 66.6666667%
  stage across the viewport matrix; DPR edges crisp; WebGPU/WebGL2 parity (settled/fission seams
  observable, satellites detach as calm meatballs); PR-10 preview-first with acreage no longer
  exceeding the preview. Consumes the W46 fixture; atmosphere/Blob chrome override count = 0.

## Discharges
RF-28 (F8). RF-26 D-1 (aurora-derive) + D-2 (blob-extirpation) execution gates. PR-10. V-A167 (baseline
consumed from W48). Archaeology T-1/T-8/T-25/T-26/T-27/T-30/T-32/T-38/T-39/T-49/T-60,
U-F3/F5/F7/F11/F23.
