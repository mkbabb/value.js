# W43 — Structure settlement (THE disease-row wave; CH-3 dies here)

**Absorbs:** old W5/W6 + registry families RF-14 (src), RF-15 (demo), RF-21 (the
colocation/no-god-modules chronic), RF-23 (api).
**Depends-on:** W40 (the ENTIRE dirty tree committed in owned slices incl. the S4 consumer slice —
this wave opens on a fully committed tree, no held diffs collide with the restructure;
`picker-color.ts` tracked),
W41 (canon compression), W42 (subtraction). No frontend/palette wave depends on W43's
internals — only on the settled tree.
**Named refs (L3, ≤2):** `V-PRIME.md` (§1 laws L1–L8, §2 W43 row) · `ARCHITECTURE.md`
(§1 physical demo tree, §2 library DAG, §5 api structure). All file:line evidence below
is inlined; this file executes standalone.
**Worktree:** one dedicated worktree, single integration owner — this wave rewrites physical
topology across src/demo/api and must not interleave with another source lane. The public
7-entry export map is IMMUTABLE (package.json `exports`; `src/subpaths/*.ts` stay the surface).

**Path convention:** repo-root-relative throughout; api anchors abbreviate `api/src/modules/…`
(capsules) and `api/src/platform/…`; demo anchors abbreviate `demo/@/…`.

## Current RED (verifiable)

- **src:** `src/v4/` is a version-prefix dir (violates ARCHITECTURE §2 bare naming) holding
  the real code: `color.ts`+`color/`, `css.ts`+`css/`, `easing.ts`, `quantize.ts`, `value.ts`,
  `result.ts`. `src/subpaths/color.ts` re-exports from `../v4/color` which itself re-exports
  from `../v4/color/model` — a redundant double barrel. `src/v4/css/stylesheet.ts` = 940 L and
  `src/v4/css/grammar.ts` = 658 L are two god modules. `foundation/` is unmaterialized.
  `find src -type d -empty` = 6+ shells left by the cut (`src/parsing/{color,stylesheet,timeline}`,
  `src/quantize`, `src/units/color/{conversions,gamut}`; RF-14 counts 8 with their parents).
- **demo:** the `@` alias fronts generic buckets — `@/lib` (+`palette/`+`palette/api/`),
  `@/composables` (+`color/`+`palette/`+`auth/`), `@/utils`, `@/styles`. `@/components/custom/panes/`
  holds 16 files. `@/lib/gamut-ink.ts` has 0 importers (dead). `@/styles/style.css` = 1043 L.
  `ComponentSliders.vue` (ConsoleRail's host) imports the palette api barrel — a cross-feature edge.
- **api:** `paginationQuery` is defined in `palette/schema.ts:95` yet imported cross-capsule by
  `admin/routes/{colors,flagged,users}.ts`. `admin` reaches into session token internals directly:
  `admin/schema.ts:8`, `admin/service/users.ts:22`, `admin/service/impersonate.ts:27`
  (`hashSessionToken`, `SESSION_TTL_MS`, `USER_STATUSES`). 4+ redundant module-name prefixes
  (`admin/admin-auth.ts`, `admin/audit-log.ts`, `admin/repository/adminAudit.ts`,
  `session/resolve-session.ts`, `meta/route-table.ts`).

## Work

### (a) src — RF-14 (behind the immutable 7-entry surface)

1. **Dissolve `v4/` to bare dirs.** `v4/color.ts`→`color/index.ts` (the sibling barrel folds
   INTO the dir as its index — this IS the double-barrel collapse); `v4/color/{anchors,model,operations}.ts`→`color/`;
   `v4/css.ts`→`css/index.ts`; `v4/css/{grammar,named-colors,stylesheet,types}.ts`→`css/`;
   `v4/value.ts`→`value.ts`; `v4/easing.ts`→`easing.ts`; `v4/quantize.ts`→`quantize.ts`.
2. **Materialize `foundation/`** (DAG layer `foundation/result + math`): `v4/result.ts`→`foundation/result.ts`;
   `src/math.ts`→`foundation/math.ts`. Repoint internal `./result`/`../math` edges.
3. **Collapse redundant middle barrels →0.** Single-file features (`value`, `easing`, `quantize`,
   `foundation/result`, `foundation/math`) are their own leaf — no barrel. Multi-file `color/` and
   `css/` carry exactly one barrel (`index.ts`). `subpaths/*.ts` re-export DIRECTLY from the bare
   home (`../color`, `../css`, `../value`, `../easing`, `../quantize`, `../foundation/math`) with
   the export list BYTE-IDENTICAL to today.
4. **God-module seam splits (ARCHITECTURE §2 mandated).** `css/stylesheet.ts` (940) → keep
   `stylesheet.ts` + extract the animation/timeline collectors (`collectAnimationOptions`,
   `collectTimelineOptions`, `parseAnimationRange`, `parseAnimationTimeline`, `serializeTimelineOptions`)
   into `css/timeline.ts` (`+timeline`). `css/grammar.ts` (658) → keep `grammar.ts` + extract the
   value-syntax coercion (`coerceToSyntax` and its syntax helpers) into `css/syntax.ts` (`+syntax`).
   `css/index.ts` re-exports from the new files; the packed surface is unchanged.
5. **Remove the empty dirs** left by the cut — 6 leaf empties per `find src -type d -empty`
   (their parents empty out as the leaves go; enumerate at execution). Gate: `find src -type d -empty` = none.
6. **DIVERGE (anti-fragmentation):** do NOT execute the full ARCHITECTURE §2 DAG splits — no
   premature `value.ts`→model/operations split, no `color/space-schema`/`color/conversions`
   separation. `value.ts` and `color/model.ts` stay whole. Only the two genuine god modules split.

### (b) demo — RF-15 (under alias death)

1. **Kill the `@` alias** (tsconfig + vite): no `@/`, no `@src`, no one-line glass forwarding dir.
   All imports become relative to the physical home.
2. **Wave-open census** enumerates every `@/` file and classifies it by the promote-once rule
   (RF-15): **38 single-consumer files colocate** into their owning feature (recursive colocation,
   picker is the exemplar); **23 true-shared files** promote once into the ARCHITECTURE §1 homes —
   `color-session/` (~9), `palettes/` (~5), `platform/` (~5, generic auth/HTTP/storage only),
   `shell/` (~3), `shared/` (~1). Superficial similarity does not earn a shared home.
3. **`panes/` 16→0 — all 16 files accounted.** Ten feature panes DISTRIBUTE (Generate/Mix/
   Extract/Gradient → `workbenches/*`, Browse/Palettes → `palettes/`, Admin → `palettes/` admin,
   About → `scenes/about`, Aurora/Blob → `scenes/`); `ConfigSliderPane` PROMOTES to a scenes-shared
   control; `aurora-harmony-stops.ts` RELOCATES with its sole consumer AuroraPane into
   `scenes/atmosphere/`; `PaneSlot`/`PaneHeader` + `index.ts` retire (DELETE here);
   `PaneSegmentedControl` RELOCATES to `shell/` **unmodified and alive** — its deletion is
   W47's route-concern gate (1→0), so W43 must NOT delete it or that gate becomes unfailable.
   10 distribute + 1 promote + 2 relocate + 3 retire = 16.
4. **Delete `@/lib/gamut-ink.ts`** (0 importers, verified above) and, in the same commit, excise
   the stale `demo/DESIGN.md` gamut-ink/paint-consumer prose (≈line 338 — it documents this
   module and its already-deleted `gamutOverlayPaint`/`envelopePlatePaint` consumers; last PROSE
   referent — the remaining `style.css:287` comment dies in item 5's split).
5. **Split `@/styles/style.css` (1043 L)** → `styles/foundation.css` (reset/tokens/global type/
   material/focus/layout primitives only) + `styles/shell.css` + feature-owned stylesheets colocated
   with their component. Not a line-count trophy: legitimate global CSS stays in foundation.
6. **Dissolve `usePaletteManager`** (153 L god composable) into narrow, feature-owned ports
   (no-god-modules + ARCHITECTURE §1; rides the RF-9 right-sizing). Its 20+ consumers repoint to
   the specific port they use.
7. **Fix the ConsoleRail cross-feature edge:** `ComponentSliders.vue` must not import the palette
   api barrel; route the datum through the color-session domain or a prop.

### (c) api — RF-23 (near-exemplar; surgical only)

1. **Hoist `paginationQuery`** from `palette/schema.ts:95` to `platform/http/` (generic pagination
   with 6 cross-capsule consumers); repoint palette + admin routes.
2. **Session primitives behind the service:** admin stops importing `session/model` token internals
   directly; expose `hashSessionToken`/`SESSION_TTL_MS`/user-status via the session service interface
   (DI), so admin depends on the capsule boundary, not the model.
3. **`lru.ts` home — resolved by census (RF-23 flagged it for session colocation).** Enumeration
   shows 3 consumers spanning two capsules: `session/resolve-session.ts:25`, `http/idempotency.ts:50`,
   `http/rate-limit.ts:18`. All three import DOWN into `platform/` (legal per the import-direction
   law); there is no admin/runtime leak. LRU is therefore a genuine `platform/cache/` primitive and
   STAYS — the leak claim is retired by enumeration (record this in the close, à la RG-2).
4. **Prefix renames (E2 idiom, file-level only — route paths byte-identical):** exactly the
   verified basename-repeats-parent hits — `admin/admin-auth.ts`→`admin/auth.ts`,
   `admin/repository/adminAudit.ts`→`admin/repository/audit.ts`, `session/resolve-session.ts`→
   `session/resolve.ts`. EXEMPT: `meta/route-table.ts` (W45 deletes it — renaming it here would be
   churn against W45's OpenAPI replacement); the entity-echo repositories
   (`palette/repository/palette.ts` et al.) stay — repository-of-the-eponymous-entity is
   borderline-idiomatic, not disease (api audit's own low-priority call).

### (d) THE DOWN-METRIC table (the chronic-killer — measured before/after, recorded in the close)

| Metric | Before | After | Verification |
|---|---|---|---|
| shared-bucket dirs (generic `@/` buckets) | 7 | 0 | no generic `lib/`/`composables/`/`utils/` bucket survives; only named homes |
| `panes/` files | 16 | 0 | `find demo -type d -name panes` = ∅ |
| `v4/` dir | 1 | 0 | `test ! -d src/v4` |
| empty dirs (src, post-cut, leaf) | 6 | 0 | `find src -type d -empty` = ∅ (parents fall with leaves) |
| redundant middle barrels | ≥5 | 0 | one barrel per multi-file feature; single-file features have none |
| css god modules (`stylesheet.ts`, `grammar.ts`) | 2 | 0 | no split-target module exceeds the god threshold |

Recorded companions (not in the six rows, measured anyway): `style.css` 1043→foundation+owners;
`usePaletteManager` 153→narrow ports; `gamut-ink.ts` 1→0.

### (e) Execution-gate clause (L1)

This wave RUNNING is CH-3's (colocation/modularization) terminal state. Per L1 the disease rider
may NOT be re-chartered, re-amended, or re-decided; a further "BUILD W##" row for colocation is the
forbidden re-booking. The next legitimate state after this wave is product-green tree evidence, not
another plan.

## Completion evidence (falsifiable)

- `npm run typecheck` (lib + demo) green; `npm run build` (production lib) green;
  `npm test` full suite green; `npm run lint --max-warnings=0` green.
- `node scripts/ci/verify-packed-surface.mjs <packed.tgz>` green against `fixtures/public-types/value-v4.ts`
  — the 7-entry public surface is BYTE-IDENTICAL (runtime export counts `23/1/19/16/9/9/2`, strict
  type count `62` unchanged).
- Import-graph census: **zero `@/` alias edges** and **zero cross-feature-internal edges**
  (feature→feature imports only via `color-session`/`platform`/`shared`, never into another feature's
  descendants); api has zero route→repository edges and admin has zero direct session-model edges.
- The DOWN-METRIC table (d) is complete with before/after and each row's verification passing.
- api suite green; `find src -type d -empty` = ∅; `test ! -d src/v4`.

## Discharges

RF-14 (src structure) · RF-15 (demo colocation) · RF-21 (THE colocation/no-god-modules disease
row — CH-3 execution-terminal) · RF-23 (api structure). Backs L1 (execution gate) and L4 (parsimony:
net-negative concepts, one barrel per feature, no god modules).
