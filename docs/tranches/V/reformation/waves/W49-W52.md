# V′ Frontend Arc — W49 · W50 · W51 · W52 (F3 · F4 · F5 · F6)

Standalone execution specs under `reformation/V-PRIME.md` (rows W49–W52, laws L1–L8).
Executable from itself plus ≤2 named design-canon references (L3). Evidence is Browser-first per
`EVIDENCE.md §1–2` against W45's honest live API (no intercepted routes under test). Every wave
consumes the **W46 override-count-0 gate**: P051/P122/Card obligations = "consumes the W46
fixture, local override/copy/definition count = 0" — none re-decides the chrome law (L4/RF-28).

---

# V.W49 — Palette Read (F3)

**Absorbs:** W22 minus the export-state paragraph (that IndexedDB export-recovery clause rides
with W50/W51's export product).
**Depends on:** W45 (live palette API), W46 (Card tuple + pastel identity), W47 (shell), W48
(W21 hue/SV axes for color search).
**Consumes (design canon, ≤2 per L3):** `OPTICAL-BENCH-COMPOSITIONS.md §3/§4`,
`PROPORTION-AUDIT.md` (PR-22/26/27, PR-07 read-side).

## Current RED
- Empty `My Palettes` can consume half the scene; the same large watercolor trio repeats
  everywhere; shadow palettes impersonate content; retained Cards drift between size/material/
  chrome and encode selection themselves; card bodies combine click, drag, expansion, rename,
  menu, feedback and swatches; loading and empty are visually confounded (V-A141/A142). Color
  search uses a regex fallback rather than the bounded prefix contract (V-A158/RF-24). Proposals
  expose no caller-controlled attribution truth (V-A161/A163). Corrupt/schema-v1 storage can
  silently reset with no owned preservation/reset composition.

## Work (Browser-first per `EVIDENCE.md`)
1. Browse and Library use selected protagonist `64…66.6666667%` / inspector `33.3333333…36%`,
   100% when no selection; render only the `Palettes` substring of `My Palettes` through W46's
   shared pastel composition, `My` and all else neutral; palette identity `--type-subheading`
   + Fraunces.
2. Each palette = one bounded entity Card with the exact W46 tuple. The Card/article root is a
   **noninteractive container**: no activation/focus/selection state, palette-Card `[inert]`
   count `0`. Its one selection control is a native named
   `<button type="button" aria-pressed="false|true">` spanning specimen/identity; **no**
   `aria-selected`/`role="option"`/listbox/composite fiction. `aria-pressed="true"` iff the
   button's palette ID equals the selected inspector identity.
3. Retire cartoon/grid/Card-selection variants, root `role=article` click/press, inline
   expansion/rename/menu/transient feedback and hover-only card swatch actions (the write verbs
   move to W50's selected inspector); keep the compact swatch strip inert data (PR-07 read-side:
   palette hover paths retire into the selected inspector).
4. Distinguish Device Draft/Workspace/Published/Trash; quiet `Legacy system` attribution;
   content-hug empties with the static aria-hidden three-face `EmptyPaletteMark`; one
   search/filter family consuming W48's hue/SV axes; no filler.
5. Unsupported/corrupt storage replaces the Library body inside the sole main with
   diagnosis→preserve/export→separately-confirmed reset (W15 semantics), never an empty lie.

## Completion evidence
- `Card count = rendered palette-entity count`; every entity reports exact
  `sm/content/quiet/opaque/false/false/off`, invariant `C=--spacing(4)` five-rung ladder at
  1440/390/320/actual-400%-zoom, only the workspace gutter responding. Nested/field/lane/empty/
  inspector/cartoon/grid/selected-variant Cards = 0. Card/face selected delta = 0.
- One native named `type="button"` seat per entity; palette-seat `aria-selected`/option/listbox/
  composite/root-press counts = 0; pressed count `0|1`; pressed-ID ↔ selected-inspector-ID
  equality; no selection ⇒ every seat false + inspector absent. Pointer/Enter/Space/AT converge
  on the same pressed palette + inspector identity; an optional reorder handle is a separate
  named control.
- Library heading pastel substring count = 1, non-`Palettes` identity count = 0 per scheme, and
  its paired Dock witness (W47) agrees. Selected protagonist `64…66.6666667%`, complementary
  inspector `33.3333333…36%`, unselected 100%; secondary empty ≤15% or collapsed.
- A system row renders one quiet `Legacy system` label and no fabricated identity/edit
  affordance. Color search uses the bounded prefix contract (regex fallback absent); loading is
  request-backed; cursor failures typed; storage recovery preserves raw data **before** any
  confirmed reset with zero schema-v1 read. Consumes the W46 fixture; local chrome override
  count = 0 — against W45's live API.

## Discharges
RF-28 F3. PR-07(read)/22/26/27. V-A141/A142/A158/A161/A163. Archaeology
T-12/T-13/T-19/T-35/T-41/T-43/T-44/T-56.

---

# V.W50 — Palette Write (F4)

**Absorbs:** W23 minus the five export serializers (those are W51).
**Depends on:** W45 (lifecycle API), W46 (P092 owner-state tabs, chrome law), W47, W49 (entity
species + selected inspector).
**Consumes (design canon, ≤2 per L3):** `VISUAL-CONSTITUTION.md §5–5.2/§7`,
`OPTICAL-BENCH-COMPOSITIONS.md §4 Account`.
**Rider:** **CH-8's UI half closes here** (palette CRUD truth) — the running write journeys are
its product-green evidence; per L1 CH-8 may not be re-chartered or re-booked.

## Current RED
- CRUD/export feedback expires or reaches only console warnings; recovery/rotation has no
  complete product flow; history/revert can lie; reorder/expand is pointer-first; restore lacks
  inventory; vote/fork/report target identity is ambiguous; dialogs use retired wrappers.
  Session recovery, mandatory idempotency/header replay, active-public social access, strict
  Problem decoding and redacted fork/provenance semantics are unclosed (V-A145/A146/A156/A162/
  A164). No legacy route, caller attribution, raw lineage or malformed-error fallback may
  survive.

## Work (Browser-first per `EVIDENCE.md`)
1. Account = one modal side `Dialog` from the Dock: signed-out registration/recovery; active
   credential rotation/logout; secret-retention handoff replaces (not stacks) the action region;
   overlay only — no second main, no nested Card/Dialog.
2. One selected-object inspector/action set with persistent durable-outbox state (PR-06: one
   action/selection owner per semantic job; local pills/backgrounds disappear). Owner-state
   `SegmentedTabs` consume P092's single producer fill (four edges ≤0.5px, focus separate, no
   local fill).
3. Full lifecycle: create→edit→reorder→tag→rename→save→publish→history→restore-release→fork→
   vote→report→trash→restore, driving W45 resources without intercepted routes. Reorder uses
   §5.2's explicit grab/move/drop/cancel with announced ordinal, pointer=keyboard under LTR/RTL.
4. Durable persistent status for pending/failure/moderation/recovery (PR-08); recovery names
   preserve/reset consequences.

## Completion evidence
- Register→recover→rotate→**lost-response logout** proves: replacement cookie current + sibling
  401 immediately after rotation; secret non-persistence/re-entry/redaction; zero valid
  initiating session + one signed-out UI after logout replay settles. Recovery consumes the
  user's existing secret only (never generates/shows/revokes); public-handle regeneration
  absent; one UUIDv7 minted before send; idempotency replay distinguished from
  `idempotency_key_reused`/`public_handle_claimed`/proposed-secret-422/generic-401.
- draft→publish→unpublish→trash→restore stays one inventory class with honest substate and
  **immutable public history**: first→second keeps both visible; lineage unpublish/trash/owner-
  suspension hides all; one release withdrawal hides only that release. Fork asks the exact new
  slug before commit, reports `palette_slug_claimed` without phantom success, opens the returned
  revision-1 private Workspace, and exposes neither destination nor actor before Publish. Public
  provenance rechecks every ancestor; a hidden/purged hop renders only as an opaque unavailable
  ordinal.
- No legacy route, no caller attribution, typed conflicts throughout. Account overlay/main/Card
  counts, region order, opener focus return recorded. Consumes the W46 fixture; write-surface
  chrome override count = 0 — against W45's live API. **CH-8 UI half green.**

## Discharges
RF-28 (F4). RF-26 CH-8 (UI half — the rider's TERMINAL Browser death, L1). PR-06/08/25/28.
V-A145/A146/A156/A162/A164. Archaeology palette-CRUD tails, U-F56/F58.

---

# V.W51 — Palette Export (F5 — pure, pre-mount-buildable under L8)

**Absorbs:** W23's five serializers (RF-28: W23 was three products stapled; the export split is
pure).
**Depends on:** W45 (`ExportSnapshot`/`toRgba8`), W50 (the Prepare→Download seat integration).
**Buildable status:** **pre-mount-buildable under L8** — the serializers, their fixtures and the
byte-contract are pure functions over `ExportSnapshot` atoms; L8 unblocks them from the V-A166
over-reach, so W51 may build and prove its bytes before the routed mount lands. Only the seat
wiring waits on W50.
**Consumes (design canon, ≤2 per L3):** `EVIDENCE.md §4/§5`, `PROPORTION-AUDIT.md`.
(Byte authority: `PALETTE-CONTRACT.md`'s export appendix — the W41 fold carries the old
`PALETTE-EXPORT-CONTRACT.md` + `PALETTE-FACILITIES.md §6` content VERBATIM per the fold guarantee;
not design canon.)

## Current RED
- Export feedback expires; there is no byte-exact five-format serializer set over a durable
  immutable `ExportSnapshot`; PNG has no single owned DEFLATE/chunk/profile framing source; the
  Prepare→Download reload-identity contract is unproven across engines.

## Work (build pre-mount under L8; then Browser-first for the seat)
1. Build JSON/CSS/Tailwind/SVG/PNG serializers over the exact `ExportSnapshot` union + digest
   (the export appendix, ibid.): positional identifiers, one shared fixed-point OKLCH spelling,
   non-executable canonical-JSON Tailwind, font/network/script-free SVG rejecting `U+FFFE/U+FFFF`
   at the shared law, PNG constructing typed OKLCH atoms and consuming exactly one packed W8
   `toRgba8(...,{gamut:"clip"})` per atom with its own DEFLATE/chunk/profile framing (no table,
   copied coefficient, canvas or alternate helper).
2. The fixed-point export CSS spelling is a product-byte serializer over `ExportSnapshot` atoms
   only — it does not parse arbitrary CSS, convert a `Color`, or duplicate W9's `/css` contract.
3. Wire the Prepare→Download seat (over W50's durable record): **Prepare FORMAT** persists
   snapshot identity, swaps in place to **Download FILENAME** only after bytes/SHA/MIME/filename
   commit; Download is a fresh activation; reload consumes prepared bytes without reserialization.

## Completion evidence
- Every serializer matches the `PALETTE-CONTRACT.md` export appendix **byte-for-byte**; each format (not one
  representative) proves filename/MIME/full hash, exact parse/decode, `<>&"'`/comment/newline/
  Unicode/duplicate-name/slugless-draft vectors, `U+FFFE/U+FFFF` rejection and reload identity.
- Chromium/V8, Firefox/SpiderMonkey and Safari/WebKit record exact versions and **identical
  snapshots, prepared bytes and full-file hashes**. PNG additionally matches W8's decoded raster,
  all stored-block headers/BFINAL, Adler-32 and CRC against the sole `demo/palettes/export/png.ts`
  framing source; no second source digest exists.
- Prepare→reload→Download proves persisted prepared bytes, no reserialization, exact anchor
  properties and lease/revocation. If IndexedDB is unavailable before capture the seat enters
  W49's outer storage-recovery state and creates no ExportOperation (`storage_unavailable`
  absent). L8 buildability is falsifiable: the serializer suite is green from a fresh clone
  before any routed mount.

## Discharges
RF-28 F5 export split + L8 pre-mount set. Export byte-contract tails of old W23. Archaeology of
the five-format Prepare→Download residue.

---

# V.W52 — Admin Suite (F6)

**Absorbs:** W24.
**Depends on:** W45 (typed Problems + wire), W46 (P092 Names tab, chrome law), W47, W49.
**Consumes (design canon, ≤2 per L3):** `OPTICAL-BENCH-COMPOSITIONS.md §4`,
`PROPORTION-AUDIT.md` (PR-11).

## Current RED
- Five live admin routes use divergent table/filter/action patterns and each is forced beside an
  unrelated `Palettes` companion consuming half the desktop and creating a false mobile pane
  choice. Some retained facilities have no client consumer; actor/scope is obscured; destructive
  proposal/report/tag/account operations erase or partially mutate evidence; dangerous actions
  and mobile disclosure are inconsistent (PR-11).

## Work (Browser-first per `EVIDENCE.md`)
1. Give each of Users/Names/Audit/Flagged/Tags the **full main width**; remove the `Palettes`
   companion, its right label and its mobile pane toggle. One review-row/query grammar; quiet
   system attribution; direct scoped commands; no shared token/impersonation.
2. Admin Names' pending/approved state is an earned P092 `SegmentedTabs` consumer (producer
   fill/focus geometry, no local pill/descendant correction).
3. Audit's time control emits either no bounds or one complete **half-open `[from,to)`** window,
   explains the excluded upper bound, and never sends one-sided/equal/reversed/>366-day pairs;
   changing either bound restarts from page one (no cursor replay on changed query bytes).

## Completion evidence
- Companion/right-label/mobile-toggle area `50%→0` on all five routes. Names indicator four-edge
  error `≤0.5px`, one fill/focus, override count 0. Every retained admin target has a reachable
  exact-wire consumer; legacy batch/import/impersonation paths absent.
- Users renders the immutable creation reason + exactly one Principal/bootstrap attribution arm,
  preserved through revocation; its close confirmation names public/auth suppression, the exact
  30-day irreversible aftermath and the active→trashed Handle transition. Flagged renders the
  pinned `ReportTargetSnapshot` and stays reviewable after active-release change/purge; Tags
  never reuses a key.
- Audit Q-AUDIT emitted time-key count `0|2`; half-open boundary results and **exact event-row
  cardinality** hold; each row renders persisted action/subject with required correlation and
  omitted-or-present before/after digests — bootstrap shows one correlation with absent
  actor/command; a reviewed Report shows exactly one `report-review` row. Actor/scope/provenance
  visible; confirmation focus and durable result recorded — against W45's typed Problems.
  Consumes the W46 fixture; admin chrome override count = 0.

## Discharges
RF-28 (F6). PR-11. Admin facility disposal (color-name/tag/flag/user/batch/provenance).
Archaeology of the noun-only "admin" tail.
