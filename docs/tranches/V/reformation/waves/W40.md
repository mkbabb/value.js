# V′.W40 — Reconcile (commit the dirty tree in owned slices)

**Absorbs:** old W0 (part — the prompt/product-state atlas mapping duty) + the UNOWNED "commit
sequencing" work. **Depends on:** — (DAG root; W41/W42 depend on this).
**Worktree/branch:** primary tree, current branch `tranche-u` (HEAD `86a9a17`, where the dirty V
work lives). NEVER reset, NEVER mass-stage, NEVER switch branches — the dirty tree is user work
(campaign law). Commit the owned slices below in order; anything not named stays uncommitted.
**References (≤2):** `../V-PRIME.md` §1 (laws L2/L5) · `../../CONSUMER-CUT.md` §on-dirty-state
(the V-A107 producer/consumer boundary).

## Current RED (verify each yourself)

1. **Tree not git-reconstructible (RF-29 B1).** `demo/@/lib/picker-color.ts` is UNTRACKED yet
   imported by tracked demo files. Verify: `git status --porcelain demo/@/lib/picker-color.ts`
   → `??`; `git grep -l picker-color -- demo | wc -l` → 32. A fresh clone loses the file and the
   demo tree fails to resolve.
2. **4 untracked v4 consumer tests counted in the green 345 (RF-29 B4).** `test/gradient-v4-consume.test.ts`,
   `test/image-sampler-v4.test.ts`, `test/mix-v4.test.ts`, `test/picker-blob-config.test.ts` are
   `??`. Verify: `git status --porcelain test/*v4*.test.ts test/picker-blob-config.test.ts`.
3. **2 aurora tests silently dropped (RF-29 B7).** Relocated to `demo/test/glass/aurora-bracket.test.ts`
   and `demo/test/glass/aurora-motion.test.ts` (untracked), OUTSIDE the vitest `include`
   (`vitest.config.ts:23` = `["test/**/*.ts"]`). They never run. Verify: `npx vitest list 2>/dev/null | grep -c aurora` → 0.
4. **4 demo-consumer tests staged into the producer commit V-A107 forbids (RF-2).** `test/ink.test.ts`,
   `test/preview-chips.test.ts`, `test/value-domain-clamp.test.ts`, `test/view-accents.test.ts`
   are staged as modified. Verify: `git diff --cached --name-only | grep -E 'test/ink\.test\.ts|test/preview-chips\.test\.ts|test/value-domain-clamp\.test\.ts|test/view-accents\.test\.ts'` → exactly 4 rows.
5. **Canon contradictions (RF-11).** `DECISIONS.md` D27 still cites the retired `K_NEXT` rail
   (`grep -n K_NEXT DECISIONS.md`); D2 publishes in future tense against its own immutability
   header; byte counts from TWO distinct artifact families circulate untagged — the Keyframes
   archive lineage (184,710 / 184,708 / 184,684 held-defective · 184,254 strict-replacement
   pre-publish · **184,430 = the SHIPPED registry tarball, V-A117**) and the value.js
   bootstrap-transport lineage (36,447 / 38,126, V-A85/V-A81 — NOT Keyframes figures). No single
   source of truth per family.
6. **CLAUDE.md deletions unowned (RF-6).** root `CLAUDE.md` (D, unstaged) + `src/parsing/CLAUDE.md`
   (D, staged) + `src/units{,/color}/CLAUDE.md` (D, staged) are sanctioned user acts per
   `SUBTRACTION.md` "Preserve". **Owner-ruled 2026-07-17: TOTALITY — ALL EIGHT
   HEAD-tracked CLAUDE.md files die** (`git ls-tree -r HEAD --name-only | grep -c 'CLAUDE.md$'`
   = 8 — HEAD is the census surface; plain `ls-files` shows only 5 today because the three src
   deletions are already staged out of the index; not the 6 first enumerated — pass-7 census
   correction): the two modified survivors `src/subpaths/CLAUDE.md` (staged M →
   `git rm`, its modification never commits) and `demo/CLAUDE.md` (unstaged M → `git rm`,
   never commits), PLUS the two clean tracked files the census missed — `src/transform/CLAUDE.md`
   (60 L) and `api/CLAUDE.md` (244 L). **Zero CLAUDE.md files remain in the repo**
   (`git ls-files '*CLAUDE.md'` → empty is the gate). The still-substantive half of all eight
   is owed to canon (W41 fold).

Gate today: a fresh clone does NOT typecheck+test green from committed state alone — born-RED.

## Work (ordered slices — commit each only if it builds)

**S1 — v4 producer core.** Stage & commit the staged producer families: `src/v4/**` (13 new),
`src/subpaths/**` (color/css/value/easing/math/quantize + parsing.ts/units.ts deletions),
`src/parsing/**` + `src/units/**` + `src/quantize/**` deletions, `src/easing.ts`+`src/index.ts`
deletions, `src/math.ts`, `src/transform/**`, `fixtures/public-types/value-v4.ts` and
`scripts/ci/verify-packed-surface.mjs` (the RF-7 load-bearing gate — protect, never prune),
`scripts/gates/proof-*.mjs` + `scripts/ci/abrogation-sweep.mjs` deletions, empty `bench/*`
deletions, `tsconfig.lib.json`, `vite.library.ts`, `vite.config.ts` (lib arms), `package.json`,
`package-lock.json`, `CHANGELOG.md`, `README.md`, `docs/RELEASE.md`, and the sanctioned
`src/parsing/CLAUDE.md` + `src/units{,/color}/CLAUDE.md` + root `CLAUDE.md` deletions, plus the
owner-ruled (2026-07-17) deletions completing RF-6 totality: `src/subpaths/CLAUDE.md` (unstage
its modification, then `git rm` — the `src/subpaths/**` glob commits NO CLAUDE.md),
`src/transform/CLAUDE.md`, and `api/CLAUDE.md` (both clean-tracked; plain `git rm`). After S1,
the only CLAUDE.md left anywhere is `demo/CLAUDE.md`, which S4 deletes.
Commit: `feat(v4)!: value 4.0 producer surface + packed-surface gate; retire pre-v4 src trees`.
> Note: `scripts/ci/css-emission-probe.mjs` is staged-modified — carry it here honestly; W42
> deletes it per bracket B4. `.github/workflows/{ci,release}.yml` ride here as committed producer
> residue; the RF-3 "re-gate demo typecheck" decision is bracket B3 (implemented at W42;
> api→W45, journeys→W55), NOT W40. S5 additionally lands the HeaderRibbon persistent-only canon
> rows (V-A92/V-A128, old FINAL.md:40, PROMPT-RECAP.md:225, DECISIONS D2/D41) per the
> GLASS-INBOUND mark — these docs are still live here; W41 archives them read-only.

**S2 — producer tests.** Commit the 5 staged producer tests (`test/v4-c1`, `test/v4-color-behavior`,
`test/v4-css-emerging`, `test/v4-css-public`, `test/v4-quantize`), the pre-v4 test-tree deletions
(`test/units/**`, `test/parsing/**`, `test/quantize/**`, `test/__snapshots__`, old-location
`test/aurora-{bracket,motion}.test.ts`), and the `test/easing*.test.ts` / `test/math.test.ts`
modifications. **UNSTAGE the 4 V-A107 tests first** (`git restore --staged test/ink.test.ts
test/preview-chips.test.ts test/value-domain-clamp.test.ts test/view-accents.test.ts`) — they
commit in S4, the consumer slice (RF-2: never in the producer commit). Commit:
`test(v4): producer-surface behavior tests`.

**S3 — RF-1 rulings + preservation tags (bracket B1) — tag-only preservation, NO working-tree
restore.** At pre-S4 HEAD (S1/S2 touch nothing demo-side, so the plate, overlay, ink-walk and the
`.26` HeroBlob all remain tracked there) create TWO annotated tags on that one commit:
`v-perceived-space-plate-ref-w40` (the reference implementation of all three deleted features —
W53's Arm-A rebuild source) and `v-blob-b0-26-ref-w40` (the immutable `.26` baseline
`HeroBlob.vue:206` + `seat.css:10` — W48's before-frame reconstruction source). Record the two
terminal retirements — the SpectrumCanvas **gamut-truth overlay** family (~−983) and the
**ink-walk** family (~−176), superseded by veil semantics + the ink ladder — in the S5 truth-fix
DECISIONS delta. Do NOT `git restore` anything: the plate imports dead v4-cut APIs (`color2`,
`OKLChHueSweepBoundary`, `oklab2linearSrgbInto`, `cssToRawColor`) and restoring it would make this
wave's own typecheck gate unsatisfiable. Preservation is the tag; the deletions land in S4.

**S4 — THE CONSUMER SLICE (V-A73's C1 completion, landing late in its owned slice — RF-2/RF-29).**
One coherent commit of the entire demo consumer state: the ~69 modified `demo/@/**` consumer files
(the value-internal v4 migration V-A73 assigned to atomic C1) + the `demo/CLAUDE.md` **deletion**
(owner-ruled 2026-07-17: `git rm` — its unstaged modification is discarded, never committed) + ALL RF-1
deletion rows atomic with their import-removals (`SpectrumCanvas.vue` drops the overlay imports in
this same commit — HEAD imports them at lines 83–88, so deletions and consumer edits are
inseparable) + the perceived-space plate deletion (preserved by tag only) + `git add
demo/@/lib/picker-color.ts` (kills the B1 blocker) + the 4 untracked v4 consumer tests
(`test/{gradient-v4-consume,image-sampler-v4,mix-v4,picker-blob-config}.test.ts`) + the 4 V-A107
tests unstaged from S1 + the 2 relocated aurora tests (`demo/test/glass/aurora-{bracket,motion}.test.ts`)
with the `vitest.config.ts` `include` extension `"demo/test/**/*.ts"` (B7). This slice is
**explicitly NOT W44 evidence** — the routed app remains blank (pinned Glass5 island; W44's
born-RED) and no W44/W17b credit attaches. Commit:
`feat(demo)!: v4 consumer migration + ruled retirements (V-A73 C1 completion; not W44 evidence)`.

**S5 — canon truth-fix (RF-11 + the HeaderRibbon inbound mark, ONE commit).** Land the HeaderRibbon
persistent-only canon rows while these docs are still live (W41 archives them read-only):
`ADDENDA.md` V-A92 superseded-in-part + V-A128; old `FINAL.md:40`; `PROMPT-RECAP.md:225`;
`DECISIONS.md` D2/D41 — per `GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md`.
(These canon docs are UNTRACKED today — S5's commit ADDS them whole, truth-fix applied; that
is also what makes ADDENDA/FINAL/PROMPT-RECAP/DECISIONS tracked for W41's `git mv`.)
Then, in `DECISIONS.md`: rewrite D27 off the retired
`K_NEXT` rail onto the immutable Value 4.0→Keyframes 6.0.0 rail; rewrite D2's future-tense
publication to past/immutable per V-A126. In `DECISIONS.md`+`ADDENDA.md`: collapse the six
circulating byte counts to TWO single-source figures, one per artifact family: the Keyframes
authority is **184,430** (the SHIPPED immutable registry tarball, V-A117 — D2/D41's "184,254 sole
input" is relabeled pre-publish lineage, the exact drift the formation critique flagged); the
value bootstrap authority is its own recorded transport figure (V-A85/V-A81 lineage). Superseded
archive figures (184,710/708/684, 36,447) are referenced in the ADDENDA lineage only, never
restated as current (L2). Commit:
`docs(v-w40): canon truth-fix — D27 rail, D2 tense, per-family byte single-sources`.

**Per-slice buildability, read precisely:** between S1 and S4 the COMMITTED demo is transiently
broken (the migrated demo lives uncommitted in the worktree until S4) — the per-slice gate is
LIBRARY builds green per slice; the fresh-clone full-repo green gate is a post-S4 property. Do
not read "builds" as full-repo typecheck mid-sequence.

**Nothing is HELD after S4.** W44 owns only what does not exist in the tree today: the glass-island
replacement, the Glass-7 site migrations, the lock regeneration, and the routed-mount witness.
W43 opens on a fully committed tree — no held diffs to collide with its restructure.
**Path convention:** all paths repo-root-relative; `DECISIONS.md`/`ADDENDA.md` live under
`docs/tranches/V/`; git commands run from the repo root.

**Coordination (L7).** The RF-18 ff-only fact is ALREADY DELIVERED — O-2
(`../keyframes.js/docs/tranches/V/coordination/VALUEJS-INBOUND-2026-07-17-formation-exchange.md`,
sent 2026-07-17, INBOX O-2) carries it in §4.1. W40 authors NO letter. The one piece O-2 lacks —
the census-split recommendation (~185 dirty files byte-identical to immutable 6.0.0 `5a9183a7`;
resolution = ff-only advance + census split, not a cleanup) — rides the FIRST WL verdict letter
as one row (WL item 4). Relay-sent is never terminal (L7/D37).

## Completion evidence (falsifiable)

- `git status --porcelain demo/@/lib/picker-color.ts` returns nothing (tracked).
- `git stash -u && <fresh worktree from HEAD> && npm run typecheck && npm test` is GREEN from the
  committed state alone — no untracked file required to reconstruct or pass. (Born-RED: fails today.)
- `npx vitest list | grep -c aurora` ≥ 2 (the relocated suite runs).
- `git diff --cached --name-only` for the S1 producer commit contains ZERO of
  `test/ink\.test\.ts|test/preview-chips\.test\.ts|test/value-domain-clamp\.test\.ts|test/view-accents\.test\.ts`
  (anchored — V-A107 honored; the four land in S4).
- `git tag -l 'v-*-ref-w40' | wc -l` = 2 (both preservation tags exist, same commit);
  `git ls-files 'demo/@/components/custom/gradient/GradientVisualizer/PerceivedSpacePlate/*' | wc -l` = 0
  (deletion committed; reference lives at the tag).
- Post-S4: `git status --porcelain demo/ | grep -v '^??' | wc -l` = 0 (no modified demo rows remain).
- The five HeaderRibbon canon rows carry the persistent-only supersession (grep `persistent-only`
  in ADDENDA V-A92 section, DECISIONS D2/D41 = present).
- `grep -c K_NEXT DECISIONS.md` = 0; exactly ONE current-authority site per byte family —
  `184,430` (Keyframes registry truth) and the value bootstrap figure — with every other site
  referencing, not restating; `grep -rn '184,254' DECISIONS.md` shows only lineage-labeled rows
  (no "sole input"/current-authority claim survives).
- The keyframes ff-only letter file exists at its named path.

## Discharges

RF-1 (three feature-deletion rulings recorded + executed per B1), RF-2 (V-A107 unstage + consumer
bulk held), RF-6 (CLAUDE.md deletions committed, redistribute-half handed to W41), RF-11 (D27/D2/
byte-count single-source truth-fix), RF-18 (keyframes ff-only note sent), RF-29 B1/B4/B7. Old
ledger: W0's "map every K/N/T row to a W-owner" mapping duty completes in the W41 junction table;
the UNOWNED commit-sequencing row closes here.
