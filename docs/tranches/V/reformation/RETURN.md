# V′ — Campaign return contract (RETURN)

**Written at convergence, 2026-07-17** — the owner-ordered re-formation campaign
(2026-07-16, "V's been hardened and developed — but to what end. Ruthlessly critique it")
returns the re-formed tranche. Two independent parallel CLEAN adversary passes closed the
audit (see `FORMATION-CLOSED.md`). This document is the owner-facing contract: what was
delivered, what was ruled, what remains owner-held, and how execution opened.

## 1. What the campaign found and built

V had hardened its specification against its own edge cases, not product pressure (~70%
speculative armor in the palette stack; 9/10 sampled waves not executable from their own
file; the convergence clock self-invalidated twice; 31/34 waves never started). The re-formed
**V′ is an execution tranche**: authority `V-PRIME.md` (§0 verdict · §1 laws L1–L8 · §2 waves
W40–W56 + WL with the universal E13 Step-0 mail line · §3 owner brackets · §4 non-goals),
`DISPOSITIONS.md` (edicts E1–E13, RF→owner map, letters), 11 standalone wave files with
born-RED gates, `coordination/INBOX.md` (the E13 mail ledger), registry
`audit/REFORMATION-2026-07-16.md` (RF-1..RF-29 — every family disposed).

Campaign integrity: 12 adversary passes total; every finding since pass 5 was world-motion
(owner rulings, tree census, live inbound mail), not plan logic — answered structurally by
the E13 mail law, the freeze-frame law, and the parallel-pair convergence protocol.

## 2. Owner brackets B1–B5 — status under the 2026-07-17 totality order

The owner's execution order ("Begin and continue the current tranche… adhere exactly to the
plan… completed IN TOTALITY; do not relinquish control") is only satisfiable if the bracketed
rows proceed. Recorded reading: **B1–B4 execute per their recorded recommendations; a contrary
later owner ruling reopens the row.** B5 is untouched.

| # | Bracket | Ruling recorded | Reversibility |
|---|---|---|---|
| B1 | RF-1 feature deletions | Rebuild the perceived-space plate on v4 at **W53**; terminally retire gamut-truth overlay + ink-walk; deletions land W40-S4 atomic with import-removals | Tags `v-perceived-space-plate-ref-w40` + `v-blob-b0-26-ref-w40` hold the reference implementations — tag-only, reversible |
| B2 | RF-9/24 armor (~70%) | RETIRE-AS-SPECULATIVE; register preserved as archive; not a BANK | Archive preserved; re-derivable |
| B3 | RF-3 CI re-gates | demo typecheck @W42 · api tests @W45 · journeys + e2e-corpus pruning @W55; RF-29 B9 (1607→345) ratified as the v4-cut consequence guarded by this set | CI edits revertible |
| B4 | Orphaned probes | Delete `boot-smoke` + `css-emission-probe` @W42; W44 routed-mount witness + W50 journeys supersede | Recoverable from git history |
| B5 | U-era owner packet P1..P8 (incl. T HG6) | **NOT proxied** — remains the coupled owner event; V′ takes no position | — |

## 3. Execution opening

DAG: W40 → W41/W42 → W43 → {W44 ∥ W45} → W46 → W47 → W48 → W49 → W50 ∥ W52 → W53/W54 → W55 →
W56; WL parallel from W43, joins at W56; W51 any time post-W45 (L8). W44 externally gated on
the Glass 7.0.0 tag (W45/WL never wait on glass). Standing laws: V-PRIME §1 (L1–L8) + probe
parsimony + defect batches ≤3 into `valuejs-inbox-*` letters + Opus fanout / Fable-inline
depth + E13 Step-0 sweeps. First wave: **W40 Reconcile** (S1→S5, per-slice buildability =
LIBRARY green mid-sequence; fresh-clone full green is the post-S4 property).

## 4. Mail state at close

Inbound I-1..I-8 all READ/FOLDED/ACTED (zero UNREAD); outbound O-0..O-3 all SENT. Standing
expectations rowed: keyframes' Glass-7-tag consume confirmation; the WL verdict letters
(D-GAP-6 ship-or-decline + SCI-1/2/3 + KF §B–§E; the first one also carries the census-split
recommendation). The execution-era mail cron sweeps hourly; the durable truth is INBOX.md.
