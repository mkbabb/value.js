# V · PASS-2 AGGLOMERATION — the fold verdict + the pass-3 charters

**Pass 2 · AGGLOMERATE · 2026-07-13 · author: pass-2 agglomerator (design authority — owns the registry
verdicts).** Inputs folded: 3 charter lanes (A/B/C, all RAN), 3 fresh non-author charter critiques
(A 58% · B 45% · C 54%), 2 adversarial retro-critiques on the un-critiqued pass-1 families (F3 48→**40** ·
F4 44→**36**), the `charter-b-move-manifest.md` companion. **This pass discharges the pass-1 standing
obligation** ("pass 2 opens with an adversarial critic on F3 and F4 before their numbers are load-bearing"):
both retros RAN and both DOCKED. Every number below is a critic's earned number or an agglomerator
spot-check at HEAD (`d2db336`; the lanes RAN in isolated worktrees at `9423094`, nothing merged).

---

## §0 The fold in one breath

Pass 1 minted **the convergent skeleton** (F1-law ∘ F3-placement × F6-vehicle × banked-F5 battery + F4's
D3 gates × banked-F2 substrate × F4 diagnostics). Pass 2 **RAN that skeleton as three charters and earned
it a candidate identity**: the charters each scored ABOVE their constituent pass-1 families — a signal the
fusions are worth more than the parts, and the tell that **there is now ONE candidate thesis, not six
families.** The fold's two headline moves:

> **1. The retro pass converted the un-adversaried discount into measurement.** F3 (48%→**40%**) and F4
> (44%→**36%**) both DROPPED under a running adversary. F3's D1 premium ("the only family to RUN D1") did
> not survive: its one executed move was helper-colocation of a KERNEL every family produces, and its
> general colocation map was refuted by the consumer set. F4's DEFINING shape thesis (tree-mirrors-the-
> lattice) self-refuted to one carve F1 owns; its single most-confident claim (`#11` "0 external callers")
> was FALSE on disk (4 kf sites, a MAJOR). Both are re-scored, and their standalone family identity is
> **subsumed into the charters** — F3 into the WHAT-tree, F4's D3 dimension into the diagnostics layer.
>
> **2. The composed thesis is now THE candidate and earns its own registry row.** Charter A (F1∘F3, **58%**)
> dissolved the exact retro-f3 G4 blocker that capped ALL of pass-1's D1 claims — it ran the general
> colocation against a **genuinely GREEN demo** (typecheck 0→0, build 0, smoke 2/2), the goo-blob→blob
> U.W-ADOPT swap having cured the 12-error RED baseline the protos measured against. Charter B (F6+F2,
> **45%**) ran the per-file SCATTER (not the rename), the `units/index.ts` carve, and — the hardest built
> primitive of the pass — the **order-independent registry** that makes the color-SCC carve pass the
> `color2` suite (2 FAIL→0). Charter C (F4+F5, **54%**) split the diagnostics fork owner-law-clean,
> re-costed `#11` at its true MAJOR price, and authored the corrected **10→7** battery. **Composed thesis:
> 50%** — past the midpoint, THE candidate, a full pass-3 of named-but-un-closed primitives from convergence.

No family RETIRED to zero; the two poles that matter: **F4's lattice-mirror SHAPE thesis is dead** (retro-f4
G3 — self-refuted to F1's one carve, 0 executed by F4), surviving only as the owner-mandated D3 DIMENSION
inside Charter C; and **the pass-2 material advance is real, not a re-assert** — the RED-baseline debt that
made every pass-1 D1 "executed" claim a zero-delta-on-a-failing-typecheck is GONE, and the general
colocation now RUNS green with a runtime observation.

---

## §1 The pass-2 convergence spine (cross-lane FACTS — reported as facts, not verdicts)

The load-bearing agreements + the load-bearing DISAGREEMENT the pass-3 plan is built on. Each carries the
lane + critic that measured it.

1. **The RED-baseline blocker is DISSOLVED at HEAD — the pass's material advance.** The pass-1 "12
   `@mkbabb/glass-ui/goo-blob` module-resolution errors / unbuildable demo" baseline is FIXED (the
   U.W-ADOPT goo-blob→blob swap, commit `110b56f`; `grep glass-ui/goo-blob demo/` = 0). Charter A ran the
   general `composables/color/` colocation against this green baseline: **typecheck 0→0, build 0, smoke
   2/2 zero-console**. retro-f3 G4's "the D1 move is a 0-delta against a RED, non-building baseline" is
   **moot at HEAD** (confirmed independently by both fresh critics). This is why Charter A (58%) sits above
   F3-alone (40%): the debt that capped F3 is closed by a real green RUN.

2. **The owner's colocation edict yields PROMOTE, not feature-capsules — directionally robust across two
   independent instruments.** Run honestly on the worst-offender bucket (`composables/color/`, 18 files),
   CC-5 refutes the spec's "the other 15 → color-picker capsule." Both RAN lanes agree **promote-DOMINANT**;
   the feature-capsule thesis is refuted BY the manifest before a file moves. This DIRECTION survives every
   adversary. **CAVEAT (spine fact 6): the exact partition does not.**

3. **The order-independent registry is BUILT, RUN, and gated — the hardest primitive of the pass.** Charter
   B reproduced the color-SCC carve's runtime failure live on HEAD (`XYZ_FUNCTIONS` eager `const` table,
   `dispatch.ts`; carve compiles green but `color2 RGB→LAB/XYZ` **2 FAIL**, TDZ init-order), then made
   `XYZ_FUNCTIONS`/`XYZ_FROM_INTO` **lazy memos** → 2 FAIL→0, full suite 2326/2326, build 0. This is the
   cycle-severance primitive AGGLOMERATION-pass-1 §1 fact 3 said F3's color capsule was blocked on — no
   longer asserted, now the delivered severance primitive (critique-charter-b: "the hardest part, actually
   built" — strong credit).

4. **The F2 demo scanner is WIRED + VALIDATED by living its own false-positive.** Charter B built the
   deterministic scanner resolving `@`-alias + `.vue` script-block extraction + index/`.js`→`.ts`;
   reproduced the `ComponentSliders`-class false-positive first-hand (a `.vue` `<style>` comment-strip bug
   producing 3 NEW false-positives), then disarmed it (dead 10→7). The critique-f2 G4/G5 precondition ("no
   demo verdict actionable until the false-positive class is disarmed") is MET by reproduction, not
   assertion (critique-charter-b: "the strongest validation class").

5. **`#11` is a kf-coupled 2.0.0 MAJOR, not a freebie — re-cost VERIFIED exact on disk.** The single
   most-confident, unanimous pass-1 F4 claim ("`PropertyDescriptor` rename = 0 external callers, cheap,
   same-major") is FALSE: keyframes.js imports `type PropertyDescriptor` at **4 sites** in public
   `Map<string, PropertyDescriptor>` signatures; kf's own `KF-VALUEJS-2.0.0.md §KF-7` dispatches the rename
   on the value.js **2.0.0 grammar-rename cadence** with a downstream kf re-point + a standing BH/BI relay.
   Both fresh critics (retro-f4 G1, critique-charter-c) re-verified this to the site. The rename is still
   right (cures the real `PropertyDescriptor_2` API-Extractor mangle) — booked now at its TRUE price.

6. **THE LOAD-BEARING DISAGREEMENT: the two RAN lanes diverge on 5 of 18 destinations, two by reversal.**
   The convergence headline ("18/0, proven before a file moves"; "two independent instruments, same
   verdict, 1-file delta") is true ONLY at the binary PROMOTE/COLOCATE grain. At the DESTINATION grain — the
   grain the manifest exists to ratify and the codemod actually commits — charter-a (a **5-kernel** tree)
   and charter-b (an **8-kernel** tree) place **5 of 18 files differently** (critique-charter-b G1):
   `generate-color` (feature ↔ kernel, **REVERSAL**), `ink-walk` (kernel-follow ↔ app-root, **REVERSAL**),
   `useColorPipeline`/`useCustomColorNames`/`view-accent` (app-cluster ↔ kernel, different tier). And
   charter-a's flagship "17/18, 1 colocates" is itself flawed: the ONE colocation (`generate-color`)
   manufactures the exact cross-tree edge CC-5 exists to forbid (`useColorParsing:7` runtime-imports it →
   app-cluster → generate feature) — applied consistently CC-5 promotes it (18/18) (critique-charter-a G1).
   **The direction (spine fact 2) is settled; the exact table is predicate-sensitive and un-ratified.** This
   is the central pass-3 obligation.

7. **CC-5 — the payload the whole fold banks on to unblock F6 — is BUILT + RUN but STILL NOT
   ground-truth-validated.** `census.mjs` (153 lines, committed to the worktree — a real advance over F3's
   throwaway scripts) has **0** assert/validate/ComponentSliders hits (critique-charter-a G3); retro-f3 G3's
   tripartite closer (build ∧ validate ∧ run) is only 2/3 met, and spine fact 6's `generate-color`
   misplacement is that missing validation-leg materializing — a green-but-wrong placement the gates cannot
   catch.

8. **The src color MERGE was re-run by NO pass-2 lane.** Charter A verified the `proof:subpath-budget`
   floor (11/11) on the CURRENT un-merged tree and CITED F3's stale `6abef80` merged-tree result, then
   MIS-ATTRIBUTED the merge re-run to Charter B item 3 — which actually carves `units/index.ts` (a different
   move) (critique-charter-a G4, critique-charter-b G5). The `parsing/color → units/color` MERGE, the
   15-vs-11 edge-count instability, and merged-tree acyclicity-at-HEAD are all UN-re-run in pass-2. Only the
   severance PRIMITIVE the merge would consume (spine fact 3) is proven.

9. **The D3 diagnostics resolution is owner-law-clean AND partly re-derives an already-landed idiom.**
   Charter C split the fork correctly (Axis-A sink-vs-embed, memoize-decided; Axis-B warn-vs-fail-closed,
   decided per-site by CSS semantics — fail-closed where the grammar auto-recovers, embed-warn ONLY at the
   spec-mandated `@keyframes !important` site). BUT its `parseCSSValues` `.eof()` fail-closed **re-derives
   tranche-U U-F29** (`329932b`, confirmed on disk) — which landed `.eof()` full-input consumption + a typed
   `CSSParseError` on the sibling `parseCSSValue` — uncredited, and never reconciles with U-F29's explicit
   design that `parseCSSValues` (plural) is the deliberately PERMISSIVE full-list sibling
   (critique-charter-c G1). The resolution is landable (and consistent with in-tree precedent); its
   NOVELTY over-claims. The "permissive-string lossy class" it flags (`@@@`, `///`, `1px @ bad`) is asserted
   un-measured and contradicts the charter's own `kind:"unknown"` non-lossy logic (critique-charter-c G2) —
   one un-run round-trip decides whether that class exists at all.

10. **The corrected battery reproduces to the symbol.** `test:dist` = **10** gates at HEAD; Q13 floor = 5;
    the owner-aligned arithmetic is **10 → 7** (Q13-five + `barrel-pure` + `grammar-fuzz`), the META-gate
    DROPPED. `barrel-pure`'s RED set is a DERIVED runtime-purity property `{parsing, units, quantize}`
    (computed per run, `src/index.ts` PURE) — matching pass-1 §4 to the symbol. `grammar-fuzz` is a proven
    born-RED (#9/#11 flip exit 1→exit 0). `lib-correctness` honestly named as the 8-candidate keep, not
    smoothed. Verified by the fresh critic to the symbol (critique-charter-c). **This layer is the
    pass's most-converged component.**

---

## §2 The registry fold (per-family + per-charter + composed-thesis verdict)

**States**: ADVANCED (a live candidate) · BANKED (folds in as substrate/support, not a standalone shape) ·
MERGED (subsumed into a composition, loses standalone identity) · BLOCKED (a missing primitive as hard as
the original problem, hidden as complete — none fire) · RETIRED (dead, contributes nothing — the F4
shape-half alone, absorbed within its MERGE). **The retro F3/F4 numbers OVERRIDE pass-1's un-adversaried
scores.**

| Row | State | Conv% | One-line disposition |
|---|---|---|---|
| **F1 · CONSTELLATION-CONFORM** | **MERGED** → Charter A | 45 | the mandated (§0.2) LAW half; fused into the WHAT-tree; no new adversary this pass — 45 stands |
| **F2 · GRAPH-PROJECTION** | **BANKED** → Charter B | 40 | substrate now WIRED + VALIDATED (scanner + order-independent registry RUN); FAMILY thesis still self-refuted; 40 stands |
| **F3 · FEATURE-CAPSULE** | **MERGED** → Charter A | **40** | retro-f3 override (48→40): D1 premium did not survive; the src leak-kill + api ratify are real floor; placement half fused into the WHAT-tree |
| **F4 · TYPE-ONTOLOGY** | **MERGED** → Charter C | **36** | retro-f4 override (44→36): lattice-mirror SHAPE thesis RETIRED (self-refuted); the D3 diagnostics DIMENSION + corpus + `grammar-fuzz` survive as the layer |
| **F5 · STRANGLER-BY-GATE** | **BANKED** → Charter C | 42 | execution pole → F6; battery corrected+authored (10→7); no new adversary — 42 stands |
| **F6 · BIG-BANG CODEMOD** | **MERGED** → Charter B | 37 | the atomic vehicle, now proven on the per-file scatter + the src carve; fused into the execution half |
| **Charter A · the WHAT-tree (F1 ∘ F3)** | **ADVANCED — LEAD** | **58** | the pass's material advance: general colocation RAN green (retro-f3 G4 dissolved); placement DESIGN un-validated (G1/G2/G3/G5) |
| **Charter B · execution vehicle (F6 + F2 substrate)** | **ADVANCED** | **45** | best-RUN of the batch (5/5, no claim falsified on disk); convergence HEADLINE overstated in degree (5-file divergence, 18/0 a flat-predicate artifact) |
| **Charter C · D3 dimension + battery (F4 + F5)** | **ADVANCED** | **54** | closes retro-f4's two dominant drags (#11 re-cost, memoize over-scope); item-2 lossy leg soft (U-F29 uncredited, permissive-string un-measured) |
| **⇒ THE COMPOSED THESIS (A × B × C)** | **ADVANCED — THE CANDIDATE** | **50** | F1-law ∘ F3-placement × F6-vehicle+F2-substrate × F4-dimension+F5-battery; RUNS green on all three legs; capped by the un-ratified placement table + the un-re-run merge + the un-validated CC-5 |

### Why the composed thesis is THE candidate (and earns 50%)
Every charter earned MORE than its constituent pass-1 families (A 58 > F1 45 / F3 40; B 45 > F6 37 / F2 40;
C 54 > F4 36 / F5 42). The fusions closed the docks the parts could not. There is **no competing shape** —
F2/F5 are banked-supporting, not rivals; F4's shape-half is retired. The three legs each RUN green.
**The 50% is the mean of the three legs (52.3) minus a cross-lane convergence tax**: at the composition
level, two lanes running "the same CC-5 predicate" disagree on 28% of the bucket's destinations (spine fact
6) — a convergence gap NO single leg's score captures, because it is a property of the composition. Bounded
below by not sinking beneath the weakest leg (B 45) — the material advance (spine fact 1) is real and earns
the thesis its place above 45 — and above by not exceeding the strongest single leg (A 58). **50%: the
candidate, past the midpoint, a demonstrable pass-3 from convergence.** The protocol's 100% (zero
enumerated gaps + a fresh non-author full audit + two consecutive clean passes) is exactly pass-3's target.

### Per-family / per-charter rationale (the docks that set each number)

**F1 → MERGED (45).** The §0.2 mandated referent CANNOT die; it is the LAW half (500-raw-line ceiling,
pure-barrel + own-runtime-sibling rule, recursive colocation, the `proof:*` idiom, the measured api-vocab
ruling). Pass 2 fused it into Charter A (F1's law operated by F3's placement predicate). No independent
adversary re-scored F1 this pass; 45 stands. Its exposure to the glass-ui referent-instability (STRUCTURE-
SPEC is a 3-day-old round-6 artifact, not a fixed law) is now recovered + stated (charter-a §5) and
already mitigated (the barrel-pure gate is a runtime property, not a hardcoded set).

**F2 → BANKED (40).** The DEFINING thesis (computed placement, not human decree) remains self-refuted; the
FAMILY does not stand alone. What pass 2 ADVANCED is its INSTRUMENT: the scanner is now wired + validated
(spine fact 4) and the order-independent registry is BUILT + RUN (spine fact 3) — the two F2 preconditions
the pass-1 fold rode forward, discharged. Banked at 40; its substrate now de-risked inside Charter B's 45.

**F3 → MERGED (40, retro override).** retro-f3 docked the D1 premium: the one RAN move (`composables/
palette/`) validated helper-colocation of a cross-cutting KERNEL DI-hub (`PALETTE_MANAGER_KEY`, 21 inject
sites), not the feature-capsule thesis, and the general map was refuted by the consumer set. What survives
adversarial re-measurement: the api D2 ratify (0 route→repo) and the src color-merge coexistence proof
(`subpath-budget` 11/11, real esbuild trace) — real floor. F3 is the WHAT-tree PLACEMENT half, fused with
F1's law into Charter A; its standalone identity is subsumed. NOT BLOCKED — it discloses its own limits in
the proto (acyclic over-promise conceded, best-case labelled) and defers the missing primitives honestly.

**F4 → MERGED (36, retro override).** retro-f4 killed the SHAPE thesis: "tree mirrors the type lattice"
self-refuted to ONE carve (`units/index.ts`) that F1/F2 already reach and F4 did NOT execute — the unique
lattice-derived layout payload is ~0. The shape-half is **RETIRED within the merge**. What survives is the
owner-mandated D3 DIMENSION — the VJ corpus (2/11 open, 3-way verified, reproduces at HEAD), the
`proof:grammar-fuzz` born-RED, the `#11` rename (at its true MAJOR cost), the diagnostics decision — folded
into Charter C. Re-scoped from "family" to "a 3-item D3 punch list bolted onto the shape," exactly as
retro-f4 recommended.

**F5 → BANKED (42).** Both poles fold elsewhere: execution → F6 (the strangler's tolerated half-migrated
transient IS a dual path the owner's clean-break law forbids); enforcement → Charter C's battery (corrected
10→7, `barrel-pure` a derived property, META-gate dropped). A supporting LAYER distributed across the
charters it enforces, not a standalone family. 42 stands.

**F6 → MERGED (37).** The atomic vehicle, proven end-to-end in pass 1 (two-gate discipline: `audit
SAFE:true ∧ oracle delta==0`), now proven on the per-file SCATTER (not just the whole-dir rename) AND on a
src carve (Charter B, three/four green gates). Fused into the execution half; its standalone 37 records the
pass-1 rename-not-scatter cap that Charter B (45) closed.

**Charter A → ADVANCED — LEAD (58).** The pass's material advance and the pass-3 anchor: the general
colocation RAN green, dissolving retro-f3 G4. Capped below convergence by the placement-DESIGN half being
proven only to RESOLVE, never to be CORRECT — the flagship "17/1" manufactures a cross-tree edge (G1),
§1.3's "ZERO promoted → color-picker feature" is FALSE on disk (`ink`/`useContrastSafeColor` land in
color-picker feature components — G2), CC-5 is un-ground-truth-validated (G3), the color merge is un-re-run
+ mis-attributed (G4), and the app-cluster tier is an asserted, gate-blind placement (G5).

**Charter B → ADVANCED (45).** The best-RUN deliverable — 5/5 real executing instruments, NO load-bearing
claim falsified on disk (the failure mode that docked both retros). Held below 50 by degree-overstatement
in the composed thesis's convergence HEADLINE: "18/0, proven before a file moves" is a flat-predicate
artifact (COLOCATE fires only when ALL consumers sit in one feature — near-tautological for an intra-coupled
bucket; G2), and "two instruments, same verdict, 1-file delta" collapses at the destination grain (5-file
divergence; G1). Plus the disclosed build-not-smoke DI gate (G3), the un-re-run merge (G5), the 451/452
slip (G4).

**Charter C → ADVANCED (54).** Closes retro-f4's two dominant drags with independently-reproduced evidence:
`#11` re-cost EXACT on disk, memoize over-scope split cleanly into Axis-A/Axis-B. The battery correction is
the pass's most-converged component (spine fact 10). Capped by the item-2 lossy-success leg: the
`parseCSSValues` fail-closed re-derives U-F29 uncredited + unreconciled (G1), the permissive-string "lossy"
friction is asserted un-measured + self-contradicting (G2), the "single spec-mandated site" rests on the
proto's inherited enumeration never re-swept (G3).

---

## §3 The pass-3 charters (the minimum-third pass — what remains unproven)

Pass 3 runs the same convergent loop (RESEARCH → SYNTHESIZE → PROTOTYPE → CRITIQUE → AGGLOMERATE) against
these charters. The protocol's convergence bar (100% = zero enumerated gaps + a fresh non-author full
adversarial audit + two consecutive clean passes) is pass-3's explicit target. **Batches ≤3 concurrent;
prototypes RUN in isolated worktrees or marked SPEC-ONLY; docs-and-prototype-scoped only; the campaign never
edits execution-tranche surfaces.** Every pass-3 gap below carries the pass-2 critic finding that opened it.

### Charter α (pass-3) — HARDEN THE WHAT-TREE PLACEMENT to a ratifiable manifest — **the lead**
The composed thesis's dominant cap: the placement DESIGN is proven to RESOLVE, never to be CORRECT, and the
two lanes disagree on the destinations the manifest exists to ratify. Pass-3 must:
1. **Ground-truth-validate CC-5** (critique-charter-a G3, retro-f3 G3): add the hand-checked harness to
   `census.mjs` — the `ComponentSliders` live-control case + the `generate-color`/`useColorParsing`
   cross-tree pair — and re-run counting POST-MOVE destinations (not pre-move sibling relationships).
2. **Reconcile the 5-file cross-lane destination divergence into ONE ratified manifest** (critique-charter-b
   G1/G2): decide `generate-color` (feature vs kernel — its only external consumers are ONE feature; the
   COLOCATE is the stronger placement) and `ink-walk` (recursive-follow vs flat) ON the table; reconcile
   5-kernel vs 8-kernel; retire the flagship "17/1" for the honest count (18/0 or the reconciled table).
3. **Correct the written claims**: §1.3 scoped to the app-cluster subset (G2 — `ink`/`useContrastSafeColor`
   DO land in color-picker feature components, which is WHY they are kernel); `aurora-atoms` 3-not-2 (G6).
4. **Surface the app-cluster placement DESIGN fork** (critique-charter-a G5): "app-root `composables/color/`
   re-bucket vs further colocation into components" — a design choice the green gates cannot adjudicate.
5. **Run the 6-project smoke on the scattered tree** (critique-charter-b G3): the DI-order runtime
   confirmation the per-file scatter's `vite build` gate argued but did not measure.

### Charter β (pass-3) — RE-RUN THE SRC COLOR MERGE ON HEAD
The one load-bearing D3 result no pass-2 lane re-ran (spine fact 8). Pass-3 must:
1. **RUN `git mv parsing/color → units/color/parse` on HEAD** (critique-charter-a G4): re-measure
   `subpath-budget` + the esbuild `/color` trace; reconcile the 15-vs-11 cross-boundary edge-count
   instability (a ±27% swing across three runs — retro-f3 G3) before the coexistence proof is load-bearing.
2. **State merged-tree acyclicity-at-HEAD honestly** (critique-charter-b G5): the order-independent registry
   (Charter B, spine fact 3) is the delivered SEVERANCE primitive; the merge inherits the 17-file barrel SCC
   + gains `color→parsing`=5. Score the merge as "kills the cross-boundary color coupling, does NOT deliver
   an acyclic capsule standalone — acyclicity rides the registry," never as a raw leak-count win.

### Charter γ (pass-3) — CLOSE THE D3 LOSSY-SUCCESS ACCOUNTING
Charter C's soft leg (spine fact 9). Pass-3 must:
1. **Cite + reconcile U-F29** (`329932b`, critique-charter-c G1): re-scope the `parseCSSValues` item to
   "extend the ratified `.eof()`+`CSSParseError` idiom to the permissive plural entry," and argue why a
   full-list parser SHOULD reject an unconsumable tail while keeping its permissive wrap (U-F29's design at
   `parsing/index.ts:569-576`). Book the cost against U-F29, not the stale proto-era "OPEN."
2. **MEASURE the permissive-string round-trip** (critique-charter-c G2): run `parseCSSValues("1px @ bad")` /
   `"@@@"` / `"///"`; if verbatim, RETRACT friction #1 and align with §2's non-lossy-capture logic; if
   lossy, book it as a genuine open class and re-audit §2's `kind:"unknown"` verbatim=non-lossy claim for
   symmetry (say "captured STRUCTURALLY" — `body:null`, not "verbatim" — G6).
3. **Sweep for other silent-drop sites** (critique-charter-c G3): grep the declaration/rule/keyframe parsers
   for silent-skip idioms (`continue`, `.filter(Boolean)`, discarded-branch `.map`s) before asserting the
   spec-mandated-drop set is `{@keyframes !important}` alone.

### The pass-3 convergence gate (the protocol's minimum-third-pass requirement)
Beyond the three charters: (a) a **fresh non-author full adversarial audit** across the composed thesis (the
100%-convergence precondition — no author critiques their own lane); (b) the **minor-corrections cleanup**
(451-not-452 · barrel-cycle framed as unbuilt-banked-F2-not-incumbent · the no-masking headline caveated at
the one spec-forced site · 0-byte typecheck evidence captured with exit codes); (c) **two consecutive clean
passes** before convergence is declared. If pass-3 closes α/β/γ and the fresh audit enumerates zero
surviving gaps, the campaign converges to the tranche plan (wave sets with born-RED gates, π obligations, a
disposition per banked route).

---

## §4 The OWNER-FORK register (every presented-not-decided fork accumulated so far)

Enumerated in full, per the fences ("owner-reserved decisions stay owner-reserved"). Each lane held these
open and routed the choice OUT; NONE was pre-decided. The campaign authors the evidence; the owner rules.

| # | Fork | Pole A | Pole B | State | Evidence carried |
|---|---|---|---|---|---|
| **OF-1** | **The `@`-ban idiom** | eslint `no-restricted-imports` (incumbent, IDE-native, 0-FP) | glass-ui's mandated `proof:*` device-free grep ("never ESLint" §0.5.12) | **owner-reserved — BOTH CANNOT STAND** (eslint breaks F1's imported never-ESLint law) | eslint misses the 10 dynamic `import()` + 4/5 side-effect `@styles` sites; `proof:*` carries 15 prose-FP + no IDE. The atomic `git mv` cut is idiom-agnostic (needs neither) — charter-a §3, charter-b §6, charter-c §4.1 |
| **OF-2** | **The api vocabulary** | RATIFY `routes/service/repository` (measured-clean per-language variant) | CONFORM to `api/model/lib` (constellation uniformity) | **owner-reserved** | value.js api passes glass-ui's OWN `proof-backend-structure` gate GREEN (0 violations, 9/9 self-test); 5 vertical capsules, 0 real route→repo edges. Grammar satisfied; only segment VOCABULARY differs. F1 measured POLE B but does NOT ratify it — charter-a §4 |
| **OF-3** | **The `{value,diagnostics}` boundary vs additive soft-add** (NEW, retro-f4 G4) | full top-level `{value,diagnostics}` return (owner-aligned clean break; breaking: ~491 sites, a 2.0.0 major) | embed-warn at the one spec-forced site (churn-cheap) | **owner-reserved — NARROW residual** | Charter C LARGELY dissolves it: fail-closed (no masking) is the general answer; embed-warn survives ONLY at the single spec-mandated `@keyframes !important` drop where fail-closed is spec-INVALID. Presented at TRUE narrow scope (one site, not 491) — charter-c §4.3 |
| **OF-4** | **The app-cluster placement DESIGN** (NEW, critique-charter-a G5) | relocate the 12 app-cluster files to a new `demo/color-picker/composables/color/` sub-bucket | colocate them further into their consuming components | **surfaced — design fork the green gates cannot adjudicate** | typecheck/build/smoke prove RESOLUTION, are structurally blind to placement DESIGN; charter-a resolved this unilaterally by running one option. Not on the declared owner-reserved list — a pass-3/owner call to surface, not assert |
| **OF-5** | **The COUPLED OWNER EVENT** (from MEMORY) | — | — | **owner-reserved — the landing gate** | U-ratify + T-close as ONE (master-merge+tag · Q14 ack · T.W8 HG6 verdict). The campaign's pass-2/pass-3 execution FLOATS on this event; it authors, it does not land. Nothing in pass 2 merged |

**Booked, not forked** (a decided cost, recorded for the coupled event, NOT owner-reserved): `#11`
`PropertyDescriptor → CSSPropertyDescriptor` is a value.js **2.0.0 MAJOR** coupled to a named kf re-point
(`KF-VALUEJS-2.0.0.md §KF-7`, 4 kf sites) + a standing BH/BI relay obligation — a constellation adopt-event
rail, not a decision the owner must arbitrate (the rename is right; only its LANDING is gated on OF-5).

---

## §5 Agglomerator-verified facts + pass-2 convergence read

**Spot-checked at HEAD (`d2db336`), own authority:**
- **U-F29 is real and on disk** (`329932b`: "fix(parsing · U-F29): parseCSSValue loud-fails on trailing
  tokens; rename to parseCSSValues (LIB-G1)") — grounding critique-charter-c G1's "already-landed idiom"
  charge. The charter-c `.eof()` resolution IS a re-derivation; the pass-3 obligation to credit + reconcile
  it stands.
- **`composables/color/` = 18 `.ts` files** — matching every lane's substrate count.
- **The three charters + three critiques + two retros are all committed** (the pass-2 doc surface is
  complete); the V tree is otherwise clean.

**Pass-2 convergence read.** Six families entered pass 2 as four ADVANCED + two BANKED; pass 2 exits with
**one composed thesis (ADVANCED, THE candidate, 50%)** carried by three ADVANCED charters (A 58 · B 45 ·
C 54), four families MERGED into them (F1/F3/F6 + F4's D3 dimension), two BANKED as substrate/battery
(F2/F5), and the F4 lattice-mirror shape-half RETIRED within its merge. The material advance is real: the
RED-baseline debt that made every pass-1 D1 claim a zero-delta-on-a-failing-typecheck is GONE, and the
general colocation now RUNS green with a runtime observation. **The gating truth for pass 3**: the DIRECTION
is settled (promote-dominant, feature-capsule refuted, the vehicle proven, the registry built, the battery
corrected) but the exact PLACEMENT TABLE is un-ratified — two lanes running the same predicate disagree on
28% of destinations, CC-5 is un-ground-truth-validated, and the src color merge is un-re-run on HEAD. None
is a missing primitive as hard as the original problem; all three (α placement-manifest, β merge-re-run, γ
lossy-accounting) have named closers. Pass 3 is the convergence pass: close them, run the fresh full
adversary, and earn the two consecutive clean passes the protocol requires before the tranche plan.
