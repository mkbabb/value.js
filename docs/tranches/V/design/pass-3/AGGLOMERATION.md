# V · PASS-3 AGGLOMERATION — the fold verdict + the pass-4 gap-closers

**Pass 3 · AGGLOMERATE · 2026-07-13 · author: pass-3 agglomerator (design authority — owns the registry
verdicts).** Inputs folded: 3 charter lanes (α/β/γ, all RAN + committed), 1 fresh non-author adversarial
audit (the owner-verbatim / clean-break lens — CLEAN on clean-break, **62%** carriage, 4 clauses survey-only).
Every number below is a lane's measured number or an agglomerator spot-check at HEAD (`71c26bd`; the lanes
RAN in isolated worktrees fast-forwarded to `07bf61d`, nothing merged). **This pass discharges the pass-2
standing obligation** (close α/β/γ + run a fresh full adversary) and rules the pass against the protocol's
clean-pass bar.

---

## §0 The ruling up front — **pass-3 is NOT the clean pass (gaps survive)**

The protocol's clean bar (§0): *"100% = zero enumerated gaps + a fresh non-author adversarial audit + two
consecutive clean passes."* The three charters closed their **own** pass-2 caps with zero surviving in-charter
gaps — α ratified the placement to a unique 18/0 manifest, β re-ran the merge on HEAD, γ closed the lossy
accounting. **But the fresh audit enumerated a whole un-charted HALF of the owner's edict** — the four clauses
the convergent loop narrowed away from and never promoted to a charter:

| the surviving gap (owner clause) | on-disk fact (agglomerator-verified, this pass) | carried by any charter? |
|---|---|---|
| **clause 12 — library complexity REDUCTION** (the sharpest; the owner's own paragraph) | **14 src files > 500 LoC** un-carved: `parsing/color/color.ts` **754**, `scroll-timeline.ts` **658**, `parsing/index.ts` **644**, `stylesheet.ts` **643**, `easing.ts` **643**, `style-names.ts` **641**, `boundary.ts` 604, `decompose.ts` 603, `parsing/utils.ts` 603, `units/utils.ts` 601, `path.ts` 562, `dispatch.ts` 558, `parsing/math.ts` 536, `gamut.ts` 526 (`find src -name '*.ts' \| xargs wc -l \| awk '$1>500'`) | **NO** — every RAN library move is behavior-PRESERVING re-structure |
| **clause 4 — long-running dirs broken + encapsulated** | `panes/` 16 flat files / 2009 LoC · `style.css` 55 KB · the 14 god-modules. Only `units/index.ts` (**451**, below the 500 ceiling) was carved | **NO** |
| **clause 3 — "same for styles"** | `style.css` cohesion-carve + single-owner colocation | **NO** |
| **clause 11 — D4 hygiene minus the gate-cull** | **39** root PNGs · **14** worktrees · **11** benches · scripts-prune · stale docs · **2 live plugins** (`vite-defer-glass-fonts.ts` + `vite-source-export.ts`, wired in `vite.config.ts` — the survey HONESTLY CONTRADICTS the owner's "worthless → delete") | **NO** (only the gate-cull carried) |
| **clause 1 — demo-wide flatten / full @-abrogation** | method validated on **ONE** bucket (`composables/color/`, 18 files); the per-dir manifest across all 6 `demo/@/` sub-dirs + the ~321-site abrogation deferred to the landing wave; `@src`/`@assets` un-ruled | **PARTIAL** — method carried, full manifest deferred |

These are not refuted — they are **un-advanced**. The fresh auditor scored owner-verbatim carriage at **62%**
(strong on the six advanced clauses ~80–90%, dragged by these four at ~30–35%). **Gaps survive → per the
ruling protocol, the pass-4 charters close them (named in §3). The two-consecutive-clean-passes clock has NOT
started.** `cleanPass = false`.

> **What the pass DID earn** (and it is real): the composed thesis's three pass-2 caps — the un-ratified
> placement table, the un-re-run merge, the soft lossy leg — are CLOSED with first-hand HEAD measurement.
> The cross-lane 28%-destination-divergence TAX that pass-2 subtracted is DISCHARGED (α proved the ratified
> table UNIQUE by running counterfactual). The composed thesis rises **50% → 62%** — the audit's fresh
> carriage number, un-inflated, and now the ceiling the four un-charted clauses set.

---

## §1 The pass-3 convergence spine (cross-lane FACTS — reported as facts, not verdicts)

Each carries the lane that measured it and the agglomerator spot-check.

1. **The placement table is now RATIFIED, UNIQUE, and RUN green on the scattered tree (α).** The 5-of-18
   cross-lane destination divergence (pass-2 spine fact 6, the central pass-3 obligation) is reconciled into
   ONE 18-row table (`charter-alpha-manifest.md`): **6 KERNEL / 12 APP-CLUSTER / 0 FEATURE = 18/0 PROMOTE**.
   Proven the **unique** zero-manufactured-runtime-cross-tree-edge assignment by a running counterfactual
   (`_proto/counterfactual.mjs`: ratified **0** edges · charter-a **1** · charter-b **7**). The two reversals
   decided ON the table: `generate-color` feature→KERNEL (charter-a's "17/1" flagship manufactured the exact
   CC-5 edge it forbids → RETIRED for the honest **18/0**); `ink-walk` app-cluster→KERNEL (recursive follow
   keeps the private helper edge-free). CC-5 is ground-truth-validated: `census-postmove.mjs` harness **H1/H2/H3
   PASS (3/3, exit 0)** — retro-f3 G3's missing validation leg CLOSED (build ∧ validate ∧ run). The scatter of
   the ratified table RAN: 18 mv / 87 rewrites / SAFE:true, typecheck **DELTA 0**, `gh-pages` build **exit 0**,
   6-project smoke **182 passed · 1 failed · 2 skipped** (the 1 failure a glass-ui `dist/styles/index.css`
   ENOENT in a fixture — env, 0 references to `composables/color`, orthogonal to the scatter). Agglomerator
   spot-check: `composables/color/` = **18** `.ts` files ✓.

2. **The src color merge is re-run on HEAD — the one load-bearing D3 result no pass-2 lane re-ran (β).**
   `git mv parsing/color → units/color/parse` lands: typecheck 0 · `subpath-budget` **11/11** · `/color`
   esbuild trace **34 mod / 0 parsing / 0 parse-home** (grammar-free at BOTH homes, stronger than the C1 gate
   which the merge would move out from under). The **15-vs-11 edge-count instability is DISSOLVED** to a stated
   deterministic method: file-level = **13 @ HEAD** (the ±27% swing was a two-axis method×tree confound —
   file-level-at-`6abef80`=11 vs statement-level-at-HEAD=15, never the same measurement). The honest correction
   the re-run earned: the true blast radius is **4 renames + 6 src + 15 test files + the `canon-sync` doc gate**,
   NOT pass-2's "7 files / 3 consumers" (the pass-2 lanes never ran the full suite; `2326=2326` merged-tree,
   behavior-preserving).

3. **Acyclicity RIDES the registry — demonstrated on HEAD, not inferred (β).** The merge is **cycle-neutral**
   (the 14-file barrel SCC + the 25-file whole-src megacycle persist identically pre/post; the merge relocates
   nodes, the "cluster 4→3" shrink was a labelling artifact). The registry is the LOAD-BEARING severance edit,
   reproduced first-hand on the merged tree: eager+carve → **2 FAIL** (the exact `xyz2rgb` TDZ hazard) → lazy
   memo+carve → **0 FAIL** (2326 PASS), barrel SCC **14→5** (`index.ts` leaves the cycle). RESIDUAL (honest,
   out of β's remit): a 5-file leaf SCC (`dispatch ↔ conversions/{cylindrical,direct,kelvin,xyz-extended}`) =
   genuine conversion-leaf interdependence, so "acyclic capsule" is NOT globally delivered even post-carve —
   the merge+registry delivers the barrel-cycle severance the proto/retro named; leaf-decoupling is a separate
   (F2) primitive.

4. **The D3 lossy leg is closed — and one friction RETRACTED by measurement (γ).** All three critique-charter-c
   gaps closed: (G1) the `.eof()`+`CSSParseError` idiom is **twice-ratified in-tree** (U-F29 `329932b`
   `parsing/index.ts:518/528/542` + S.W1 F-9 `stylesheet.ts:633`) — charter-c's move re-scoped from "introduce"
   to "**extend** the ratified idiom," cost booked against U-F29; agglomerator spot-check: `329932b` IS an
   ancestor of HEAD ✓. (G2) **friction #1 RETRACTED** — the permissive-string class (`@@@`→`@@@`, `1px @ bad`
   verbatim, `///`→`/ / /` whitespace-only) is **NON-lossy** by measurement; the genuine lossy class is the
   **partial-recovery tail** (`)(`→`""`, `rgb(`→`rgb`) — which is exactly what item-1's `.eof()` extension
   closes. (G3) the "single spec-mandated site" is FALSIFIED — the keyframe parser silently drops at **2**
   sites (`stylesheet.ts:233` `!important` D1 + `:239-244` invalid `animation-composition` D2), same class, six
   grep false-positives cleared. **OF-3 widens from "1 site" to "1 class / 2 sites"; its CHARACTER is
   unchanged.**

5. **The fresh audit is CLEAN on clean-break — one disclosed, owner-routed masking residual.** The
   owner-verbatim / clean-break lens swept every spec for surviving alias/shim/dual-path/masking-fallback:
   the atomic `git mv` cut has **no** transitional-`@` window; the strangler dual-path pole was KILLED for
   being a dual path; the ONE residual is OF-3's embed-warn at the spec-forced `@keyframes` ignored-declaration
   class, which charter-γ ITSELF labels "a masking fallback by §0's own definition" — disclosed, scoped to 2
   sites, owner-routed. **No undisclosed violation in any spec.**

6. **THE LOAD-BEARING FINDING: the campaign advanced ~half the owner's surface and left the other half
   un-charted (audit §3).** The convergent loop narrowed onto `composables/color/` + the color-merge /
   diagnostics / battery and did NOT fan back out to the surfaces it deferred: **library complexity REDUCTION
   (clause 12), long-dir breaking (clause 4), styles (clause 3), the bulk of D4 hygiene (clause 11), the
   demo-wide manifest (clause 1).** These are the pass-4 charters. This is the gating truth for convergence.

---

## §2 The registry fold (per-charter + composed-thesis verdict)

**States**: ADVANCED (a live candidate) · BANKED (substrate/support) · MERGED (subsumed) · OPEN (enumerated,
un-charted — the new pass-4 surface) · RETIRED (dead). The composed-thesis % is the fresh audit's earned
carriage (62%), **never inflated** above it.

| Row | State | pass-2 → pass-3 | One-line disposition |
|---|---|---|---|
| **Charter A · the WHAT-tree (F1 ∘ F3)** | **ADVANCED — LEAD** | 58 → **68** | α closed the dominant cap: the placement is a RATIFIED, UNIQUE-by-counterfactual 18/0 manifest, CC-5 ground-truth-validated (3/3), RUN green on the scattered tree. Capped by SCOPE — one bucket, not the demo-wide manifest (→ pass-4 clause 1/ε) |
| **Charter B · execution vehicle (F6 + F2 substrate)** | **ADVANCED** | 45 → **62** | β re-ran the merge on HEAD, dissolved the 15-vs-11 instability to a deterministic **13**, demonstrated acyclicity-rides-registry (not inferred; 2 FAIL→0). Capped by the honest correction (true blast = 15 test + canon doc; 5-file leaf-SCC residual) |
| **Charter C · D3 dimension + battery (F4 + F5)** | **ADVANCED** | 54 → **68** | γ closed all three soft-leg gaps: U-F29 credited (idiom twice-ratified), friction #1 RETRACTED (permissive-string non-lossy), sweep found the 2nd spec-forced site. The D3 leg is now load-bearing |
| **⇒ THE COMPOSED THESIS (A × B × C)** | **ADVANCED — THE CANDIDATE** | 50 → **62** | three legs hardened + the cross-lane convergence TAX discharged (unique manifest); RE-CAPPED at the audit's fresh carriage (62%) by the four un-charted owner clauses (§3). The remaining 38% is now ENUMERATED with named closers |
| **⊕ clause 12 — library complexity REDUCTION** | **OPEN → pass-4 δ** | — → new | the sharpest un-charted clause: 14 god-modules > 500 LoC, un-carved; the owner's own paragraph has no charter |
| **⊕ clause 4/3/1 — long-dirs + styles + demo-wide manifest** | **OPEN → pass-4 ε** | — → new | `panes/` (2009 LoC), `style.css` (55 KB), the demo-wide per-dir flatten across all 6 `demo/@/` sub-dirs |
| **⊕ clause 11 — D4 hygiene (minus the gate-cull)** | **OPEN → pass-4 ζ** | — → new | 39 PNGs · 14 worktrees · 11 benches · scripts-prune · stale docs · the plugins disposition (OF-6, new) |

### Why the composed thesis is 62% (and why that is an ADVANCE, not a demotion)
Pass-2 scored it 50% = "mean of the three legs (52.3) minus the cross-lane convergence tax." Pass 3 (a)
**discharged that tax** — α's counterfactual proves the ratified table the unique zero-manufactured-edge
assignment, so two lanes no longer "disagree on 28% of destinations"; and (b) **hardened all three legs**
(A 58→68, B 45→62, C 54→68, leg-mean ~66). Left there, the composition would read ~66. **But the fresh
non-author auditor scored owner-verbatim carriage across ALL twelve clauses at 62%** — lower than the
leg-mean precisely because the three legs all sit inside the *advanced half* of the edict and cannot see the
four un-charted clauses. Per the campaign law ("the composed thesis's earned % from the audits, never
inflated"), **62% is the ceiling** — it is simultaneously a real advance over 50 (the tax is gone, the caps
closed) and the honest cap the un-charted half imposes. The remaining 38% is no longer diffuse: it is the
four enumerated clauses with the named δ/ε/ζ closers.

---

## §3 The surviving gaps → the pass-4 charters (named closers, per the ruling protocol)

Pass 4 runs the same convergent loop (RESEARCH → SYNTHESIZE → PROTOTYPE → CRITIQUE → AGGLOMERATE) against
these gap-closers, THEN the fresh full-thesis adversary. **Only a pass that runs with zero surviving gaps
starts the two-consecutive-clean-passes clock.** Batches ≤3 concurrent; prototypes RUN in isolated worktrees
or marked SPEC-ONLY; docs-and-prototype-scoped; the campaign never edits execution-tranche surfaces. Each
carries the audit finding that opened it.

### Charter δ (pass-4) — LIBRARY COMPLEXITY REDUCTION — **the lead** (closes audit gap 1, owner clause 12)
The sharpest un-carried clause. Every RAN pass-1/2/3 library move is behavior-PRESERVING re-structure
(`units/index.ts` 452→17 barrel+siblings preserves net LoC; the color merge is "cycle-neutral, relocates
nodes without rewiring edges", 2326=2326). Pass-4 must:
1. **Enumerate the god-module carve set** — the 14 src files > 500 LoC (`color.ts` 754, `scroll-timeline.ts`
   658, `parsing/index.ts` 644, `stylesheet.ts` 643, `easing.ts` 643, `style-names.ts` 641, …) — with a
   per-file **before/after LoC target** and the cohesion axis each splits along (parse-cluster subdirs like
   the landed `parsing/color|timeline|stylesheet` pattern; the `conversions/` leaf-split pattern).
2. **Name the complexity metric that goes DOWN** — not net LoC (a barrel split preserves it): max-file-LoC,
   the count of files > 500, the `dispatch.ts` fan-in hub (558 LoC, the color2 dispatch hub), or a cyclomatic
   measure. A carve that reduces the metric, distinguished from a relocation that does not.
3. **Reconcile with clause 4** — the god-module carve IS the D3 half of "long-running dirs broken"; the
   `barrel-pure` / 500-line-ceiling law (F1) is the enforcement, not the plan.

### Charter ε (pass-4) — LONG-DIRS + STYLES + DEMO-WIDE MANIFEST (closes audit gaps 2/3/5, clauses 4/3/1)
1. **`panes/`** (16 flat files, 2009 LoC) → a `panes/chassis/` carve + colocation (the D1 half of clause 4).
2. **`style.css`** (55 KB god-sheet) → cohesion-carve + single-owner colocation ("same for styles", clause 3
   — 0 charters to date; only specs f6/r1 + the portfolio mention it).
3. **The demo-wide flatten manifest** — extend α's VALIDATED one-bucket method (`census-postmove.mjs` + the
   3-tier predicate + the counterfactual) to a per-dir ratified table across all 6 `demo/@/` sub-dirs
   (`@components` 152 · `@lib` 85 · `@composables` 78 · `@utils` 6 alias sites) + the ~321-site @-abrogation,
   and **rule the `@src`/`@assets` scoping** (do they survive a literal "abrogation of @"? — the audit §2
   residual).

### Charter ζ (pass-4) — D4 HYGIENE DISPOSITION (closes audit gap 4, clause 11)
The six survey-only hygiene items, each to a booked disposition (delete / relocate / keep-with-reason):
39 root PNGs · 14 stale worktrees · 11 out-of-spec benches · stale docs · scripts-prune (dev + deploy.sh
survive on merit) · **the plugins fork** (the survey measured them LIVE + wired, contradicting the owner's
"worthless → delete entirely" — surfaced as **OF-6**, delete-after-relocate vs keep-wired).

### The pass-4 convergence gate
Beyond δ/ε/ζ: **the fresh non-author FULL-THESIS adversarial audit** (the "other" pass-3 audit the
owner-verbatim lens explicitly deferred — *"This is NOT the full-thesis convergence auditor (that is the
other pass-3 fresh audit)"*; it did not run this pass and is a pass-4 obligation) across the whole composed
thesis + the three new gap-closers. If pass-4 closes δ/ε/ζ and the fresh full-thesis audit enumerates zero
surviving gaps → pass-4 is the **first** clean pass; a pass-5 confirmation (pure fresh-audit-only) earns the
second consecutive clean pass, and the campaign converges to the tranche plan.

---

## §4 The OWNER-FORK register (every presented-not-decided fork, pass-3 fold)

Carried forward from pass-2 §4; this pass **widens OF-3, concretizes OF-4, and folds the NEW OF-6.**

| # | Fork | Pole A | Pole B | State | pass-3 movement |
|---|---|---|---|---|---|
| **OF-1** | The `@`-ban idiom | eslint `no-restricted-imports` (incumbent) | glass-ui's mandated `proof:*` grep ("never ESLint") | **owner-reserved** | untouched — the atomic `git mv` cut α RAN is idiom-agnostic (its own audit gate proves the cut, needs neither) |
| **OF-2** | The api vocabulary | RATIFY `routes/service/repository` (measured-clean) | CONFORM to `api/model/lib` | **owner-reserved** | untouched |
| **OF-3** | The `{value,diagnostics}` boundary vs embed-warn | full top-level `{value,diagnostics}` return (breaking: 491 sites, a 2.0.0 major) | embed-warn at the spec-forced `@keyframes` class | **owner-reserved — NARROW** | **WIDENED by γ: "1 site" → "1 class / 2 sites"** (D1 `!important` + D2 invalid `animation-composition`); CHARACTER unchanged (spec-forced drops where fail-closed is spec-invalid) |
| **OF-4** | The app-cluster placement DIRECTORY | re-bucket to `demo/color-picker/composables/color/` (RATIFIED default) | colocate into `custom/color-picker/composables/` | **surfaced — green gates cannot adjudicate** | **CONCRETIZED by α**: the 6-file pipeline→FEATURE runtime coupling evidence carried; the manifest ratifies the TIER, the owner rules the DIRECTORY |
| **OF-5** | The COUPLED OWNER EVENT | — | — | **owner-reserved — the landing gate** | untouched; the whole pass-3 execution FLOATS on it. Nothing merged. β books the merge's landing costs against it (test-repoint 15 · CLAUDE.md canon sweep · C1-regex widen) |
| **OF-6** | **The plugins disposition** (NEW, audit §5) | DELETE entirely (owner-verbatim: "worthless") | KEEP wired (the survey measured them LIVE: `vite-defer-glass-fonts.ts` + `vite-source-export.ts`, both in `vite.config.ts`) | **surfaced — owner-verbatim divergence** | the owner said "delete entirely"; the survey HONESTLY CONTRADICTS with measurement. Must become an explicit disposition (delete-after-relocate vs keep-wired) — routed to ζ, decided by the owner |

**Booked, not forked** (unchanged): `#11` `PropertyDescriptor → CSSPropertyDescriptor` is a value.js **2.0.0
MAJOR** coupled to a named kf re-point (`KF-VALUEJS-2.0.0.md §KF-7`, 4 kf sites) + a standing BH/BI relay —
a constellation adopt-event rail, gated on OF-5. β adds a second booked-not-forked landing cost: the merge's
`proof-subpath-budget.mjs` C1-regex widen (`/src/parsing/` → also `units/color/parse/`), else the gate
silently stops guarding the seam the merge creates.

---

## §5 Agglomerator-verified facts + the two-clean-pass clock

**Spot-checked at HEAD (`71c26bd`), own authority (every number with its command):**
- `composables/color/` = **18** `.ts` files ✓ (`ls demo/@/composables/color/*.ts | wc -l`).
- **14 src files > 500 LoC** ✓ (the clause-12 gap is REAL and un-carved: `color.ts` 754 is the repo's largest).
- `units/index.ts` = **451** ✓ (γ's "451-not-452" correction confirmed; charter-b §0/§3's "452" is the wrong
  number).
- U-F29 `329932b` **IS an ancestor** of HEAD ✓ (`git merge-base --is-ancestor` — γ's credit is grounded).
- Hygiene ✓: **39** root PNGs · **14** worktrees · **2** live plugins · **11** benches (the clause-11 gap is
  REAL; the plugins are wired, contradicting the owner — OF-6).
- α/β/γ + the audit are all committed (HEAD `71c26bd`); the V tree is otherwise clean.

**Credits (the campaign's epistemic discipline is genuine — it earns saying so):** γ RETRACTED its own
friction #1 (permissive-string is NON-lossy, measured verbatim) and the brief's "aurora-atoms 3-not-2" (α's
honest self-refutation — G6 counted a comment + a glass-ui import); β RETIRED "small blast" for the true
15-test + canon-doc radius; α RETIRED charter-a's flagship "17/1" for the honest 18/0; the "451-not-452"
correction is on disk. These retractions-earned-by-measurement are exactly the campaign law, honored.

**The two-clean-pass clock.** Pass 1 minted the skeleton; pass 2 ran it as three charters and earned the
composed candidate (50%); **pass 3 closed the three legs' caps + discharged the cross-lane tax (→62%) AND
surfaced, via the fresh audit, the un-charted half of the owner's edict.** Because gaps survive (the four
enumerated clauses), **pass 3 is NOT a clean pass** — the clock is at **0 of 2**. Pass 4 = the gap-closer
charters (δ complexity-reduction · ε long-dirs+styles+manifest · ζ hygiene) + the fresh full-thesis
adversary; if it enumerates zero surviving gaps it is the FIRST clean pass, and a pass-5 confirmation earns
the second. **The direction is settled and the whole remaining surface is now enumerated with named closers
— but the owner's most emphatic ask (clause 12, complexity reduction) and its longest sentence (clause 11,
hygiene) are un-charted, and convergence cannot be declared until a charter carries them.**
