# V · PASS-1 AGGLOMERATION — the fold verdict + the pass-2 charters

**Pass 1 · AGGLOMERATE · 2026-07-13 · author: pass-1 agglomerator (design authority — owns the registry
verdicts).** Inputs folded: 6 specs (f1–f6), 6 protos (f1/f3/f4/f5/f6 + proto-f2), 4 adversarial critiques
(f1 45% · f2 40% · f5 42% · f6 37%), 8 research artifacts. **Coverage gap noted + handled**: F3 and F4
carry specs + RAN protos + research but **no adversarial critique** (the broken fan-out over-selected F1;
the derivation trio F2/F3/F4 was the uncovered batch, and only F2 drew a critic). The agglomerator scores
F3/F4 directly from their RAN evidence, applying the critics' demonstrated stinginess + an explicit
un-adversaried discount, and flags both for a **pass-2 adversarial pass** as a standing obligation.

---

## §0 The fold in one breath

Pass 1 collapses **6 orthogonal families → 4 alive (ADVANCED) + 2 folded-in (BANKED)** — exactly the
composition shape the portfolio predicted ("ONE shape-thesis × ONE execution-thesis × a minimal gate set").
The convergent tranche skeleton the whole pass points at:

> **ONE WHAT-tree = F1 (the mandated glass-ui LAW half) ∘ F3 (the PLACEMENT + executed-move half)**,
> **executed by F6 (the atomic codemod vehicle)**, **held by a banked-F5 enforcement battery (corrected to
> the Q13 floor) + F4's two D3 gates**, **all numbers computed by the banked-F2 measurement substrate**,
> **with F4 adding the owner-mandated typed-diagnostics/validation DIMENSION on D3.**

No family RETIRED, none BLOCKED, none MERGED. The two dispositions that matter most:

- **F5's strangler-EXECUTION pole is dead** — reconciled to the owner's clean-break law it *becomes* F6
  (self-admitted, critique-f5 G3). Only its enforcement battery survives, as a supporting layer, not a family.
- **The F6 critic's BLOCK is LIFTED at the fold** — it recommended blocking F6 on "the target tree no family
  drew," but that was written in isolation *before* the fold saw that **F3 ran a real D1 colocation and
  supplies the per-file placement predicate (consumer-census)** the F1/F2/F5 critiques all named as CC-5's
  credible closer. F6's cargo now has a shape; the block does not fire (see §2·F6).

---

## §1 The convergence spine (cross-family FACTS — reported as facts, not verdicts)

Independent derivations, run by different mechanisms, land on the SAME nodes. These are the load-bearing
agreements the pass-2 plan is built on:

1. **The barrel-carve node.** `units/index.ts` (451 L, 3 own-runtime primitives `ValueUnit`/`FunctionValue`/
   `ValueArray` + 2 types, *and* it is the barrel) → carve into kind-named siblings behind a PURE re-export
   barrel, mirroring the already-landed `units/color/{base,spaces}` precedent. Reached by **F1** (§2.1
   own-runtime-sibling), **F2** (cycle spine), **F4** (type-lattice rationale), enforced by **F5**
   (`proof:barrel-pure`). **Agglomerator-verified (§4)**: the runtime RED set is `{parsing/index.ts (10
   own-runtime), units/index.ts (3), quantize/index.ts (2)}` — `src/index.ts` is runtime-PURE (0 runtime /
   23 type-only). The spec's hardcoded `{src/index.ts, parsing, units}` set is FALSE at HEAD; the gate MUST
   be a **runtime-purity property**, gate-derived per run (critique-f5 G6, critique-f2 confirm).

2. **api = verify-not-restructure — but it is an OWNER-FORK, not a settled ratify.** F1 (RAN glass-ui's own
   `proof-backend-structure.mjs` → GREEN, 0 violations), F2 (Tarjan: 0 non-trivial SCCs, 0 route→repository
   edges), F3 (`rg` → 0 real route→repo imports), F4 (ratify L-brands) all agree the boundary is clean.
   **The unresolved fork** (critique-f1 G9, critique-f2 G7): does the owner want the `routes/service/
   repository` vocab CONFORMED to glass-ui's `api/model/lib` for constellation uniformity, or the
   measured-clean L-boundary RATIFIED as a per-language variant? F1 measured the answer as "ratified
   variant" (§5.2 binds each language its own idiomatic segment names over a shared grammar) — **owner-reserved.**

3. **The color-system gravitational mass = one carve, two derivations, one unbuilt severance.** F2's
   color-SCC carve (redirect the 11 `from ".."` inside `conversions/*` → `from "../spaces"` + move `ch` →
   `channel.ts`) IS glass-ui §2.1; F3's color-capsule merge (`units/color` + `parsing/color`, RAN, kills
   11→0 cross-boundary edges, `proof:subpath-budget` GREEN 11/11 on the merged tree) is the same mass from
   the capsule angle. **BUT** neither lands acyclic: F2's carve **compiles green, FAILS at runtime**
   (`XYZ_FUNCTIONS` module-eval init-order breaks `color2` round-trip; needs an **order-independent
   registry** — a real, buildable refactor, unbuilt); F3's merge inherits the pre-existing 17-file barrel
   SCC + gains `color→parsing`=5. **Cycle severance is a genuine pass-2 primitive, owned by F3's color
   capsule, co-designed with the banked-F2 substrate.**

4. **CC-5 (the T3 "≥2 UNRELATED families" placement predicate) is the undefined hard part at the center of
   D1** — named as the sinking gap by critique-f1 (G2), critique-f2 (G2), critique-f5 (G7). **F3 supplies
   the closer**: two consumers are UNRELATED iff their nearest common feature-root is the demo root
   (consumer-set-intersection = F3's census, which agrees with F2's fan-in by independent method). This is
   the pass's sharpest cross-pollination: **F3's consumer-census IS the semantic overlay F1's law, F2's
   graph, and F5's globality gate all require.**

5. **D1 (the owner's PRIMARY surface) was EXECUTED by exactly ONE family — F3.** F1, F2, F6 all left the
   demo restructure un-run (critique-f1 G1, critique-f2 G1, critique-f6 F6-1 each score this as vacuous
   convergence against the owner's headline edict). F3's proto RAN the `composables/palette/` → capsule
   colocation with **0 typecheck regression, 2 external files touched**, AND the src color merge. This is
   the single most-valuable delivery against the owner's opening verbatim, and it is why F3 carries the
   pass's highest convergence.

6. **The D3 typed-diagnostics/validation DIMENSION is owner-mandated and F4 owns it.** The owner's §0
   explicitly cites "parsing validations" + "the recent keyframes.js tranches' `parseCSSValue` buggies and
   goblins." Measured 3 ways: **exactly 2 of 11 VJ bugs OPEN** — #9 (diagnostics unwired on the public
   entries) + #11 (`PropertyDescriptor` TS-lib collision). F4's proto RAN all 5 obligations AND **refuted
   its own spec** (the `onParseError` sink is a DEAD channel on `parseCSSValues` — 0/6 garbage inputs fire
   it, because `sepBy` auto-recovers; the memoize forcing-function proves an embedded-in-return diagnostic
   is strictly more correct than a sink for the memoized entries). #11 rename done (typecheck 0);
   `proof:grammar-fuzz` born-RED→GREEN. This dimension composes onto the shape; it is not a layout mechanism.

---

## §2 The registry fold (per-family verdict + rationale)

| Family | State | Conv% | One-line disposition |
|---|---|---|---|
| **1 · CONSTELLATION-CONFORM** | **ADVANCED** | 45 | the mandated WHAT-tree LAW half; fuses with F3 in pass 2 |
| **2 · GRAPH-PROJECTION** | **BANKED** | 40 | measurement substrate + gate-engine → folds into F3 (placement) + the F5 battery (runtime-edge cycle, union-entry dead-code) |
| **3 · FEATURE-CAPSULE** | **ADVANCED** | 48 | the WHAT-tree PLACEMENT/execution half; only family to RUN D1; supplies the CC-5 predicate |
| **4 · TYPE-ONTOLOGY** | **ADVANCED** | 44 | the owner-mandated D3 typed-diagnostics/validation DIMENSION; composes onto the shape |
| **5 · STRANGLER-BY-GATE** | **BANKED** | 42 | execution pole → F6; enforcement battery → the shape charter + F4's D3 gates (arithmetic corrected to Q13 floor) |
| **6 · BIG-BANG CODEMOD** | **ADVANCED** | 37 | the atomic execution VEHICLE; absorbs F5's execution pole; block LIFTED by F3's target-tree predicate |

### F1 · CONSTELLATION-CONFORM → **ADVANCED (45%)**
The charter-§0.2 mandated referent — it CANNOT die; it is the LAW half of the WHAT-tree (the 500-raw-line
ceiling, pure-barrel + own-runtime-sibling rule, §0.3 recursive colocation, the born-RED `proof:*` idiom,
the profile-agnostic §0–§6 that applies to a pure-TS library, and the measured api-vocab ruling). D3
(barrel-purity carve) is genuinely demonstrated; D2 is measured GREEN; D4 is a decidable census. It is
docked to 45% by the critic because its D1 payload is **un-prototyped** and rests on the undefined CC-5
overlay (now closed by F3), and its clean-break-vs-transitional-alias tension is unreconciled (now forced
ATOMIC by the F6 vehicle). **Not a standalone D1 answer — it is the spine, fused with F3.**

### F2 · GRAPH-PROJECTION → **BANKED (40%)**
The critic's own verdict: "the best substrate for a shape it cannot itself supply." Its DEFINING thesis —
"placement is a computed output, not a human decree" — is **self-refuted** (§7/§8 concede the promote/
globality decision needs a semantic overlay the DAG cannot compute). So the FAMILY, as a standalone shape,
does not survive. What DOES survive — and is mandatory — is its **instrument**: the ~90-line deterministic
runtime-vs-type-edge scanner, the union-entry `{product ∪ test ∪ e2e ∪ build}` dead-code method, the
runtime-edge `proof:barrel-cycle` (deflates madge's ~11.5× cycle overstate to the true 2 SCCs), and the
fan-in carve-priority ranking. **Disposition**: its placement mechanism folds into **F3** (F3 supplies the
semantic overlay F2 lacks; F2 supplies the numbers F3 asserts); its gate discipline folds into the **F5
battery**. Three F2 preconditions ride forward into F3's/F5's charters: (a) validate the `@`-alias demo
scanner before ANY demo delete — the census listed a **provably-live control dead** (`ComponentSliders`, a
rendered iOS-Safari pointer-capture-recovery subtree; critique-f2 G4); (b) co-design the order-independent
registry so the color-SCC carve passes the `color2` runtime suite (G3); (c) run F2's colocation engine on
demo end-to-end, not just fan-in counts (G1). Banked, not retired — the campaign stands on its numbers.

### F3 · FEATURE-CAPSULE → **ADVANCED (48% — the pass's highest, earned)**
The strongest-EXECUTED shape family and the only one to move the owner's PRIMARY surface. **Why 48% (above
F1's 45%, and stingy despite no critic)**: convergence measures how PROVEN the answer is, not how important
— and F3 alone RAN D1 (palette capsule, 0 regression) + the src color merge (subpath-budget GREEN) + it
delivers the CC-5 predicate three families are blocked on + it ratifies api by first-hand measurement.
**Why capped below 50%**: (i) the demo win is honestly best-case — `composables/palette/` was *already* a
sealed capsule (9 privates, 0 external consumers); the general colocation (`composables/color/`'s 15
single-consumers fanning into different features) is UN-RUN and will touch far more; (ii) F3 does NOT own
cycle severance — its "acyclic capsule" over-promises (proto measured the merge is seam-clean but inherits
the barrel SCC); (iii) "feature" is the wrong atom for src — the honest framing is DOMAIN capsules
(parse-vs-represent is a layer), which F3 concedes; (iv) **no adversary re-measured it** (the un-adversaried
discount). F3 is the WHAT-tree placement/execution half — fused with F1's law.

### F4 · TYPE-ONTOLOGY → **ADVANCED (44%)**
Not a shape thesis — an **owner-mandated D3 DIMENSION** (typed diagnostics + the parse-validation corpus)
the layout families are silent on, so it cannot bank into a shape family; it composes ONTO one. **Why 44%
(comparable to F1's 45%)**: it answers its narrow surface about as completely as F1 answers its broad
surface partially — all 5 §6 obligations RAN, and the proto did the adversary's own work (it REFUTED the
spec's "sink is sufficient" claim, the same self-refutation credit the F2 critic extended). Its layout half
(the `units/index.ts` carve) converges to the shared spine node (no independent credit). **Why capped**:
(i) explicitly single-surface (nothing native to D1, thin D4); (ii) the diagnostics fork has NO free option
(additive-sink insufficient for lossy-success; the clean `{ast,diagnostics}` return is the breaking
deferred hazard — 491 sites; grammar-fail-closed is a per-branch `.map()`-closure rewire) — a genuine
unresolved tension; (iii) the lossy-success closure is proven on ONE drop path (`@keyframes !important`);
the `kind:"unknown"` at-rule + `parseCSSValues` partial-recovery classes remain open; (iv) un-adversaried.
Its two D3 gates (`proof:grammar-fuzz` #9/#11, `proof:diagnostics-wired`) fold into the banked-F5 battery.

### F5 · STRANGLER-BY-GATE → **BANKED (42%)**
Two poles, both fold elsewhere. **Execution pole → F6**: reconciled to the owner's "NO dual paths, clean
breaks," the strangler's tolerated half-migrated transient IS a dual path; the only fix (complete-subtree
atomic waves) IS F6 at a smaller granularity (self-admitted, critique-f5 G3). **Enforcement pole → the
shape charter + F4**: the born-RED battery is real, verified-live at HEAD, consumer-backed
(`barrel-pure`/`barrel-cycle`-runtime-edge + the `grammar-fuzz` #9/#11 + `diagnostics-wired` targets all
still RED) — but it is a supporting LAYER distributed across the families it enforces, not a standalone
family. **Two corrections banked with it** (must survive into pass 2): (1) the REPLACE arithmetic is WRONG
as specified — the repo's OWN ratified Q13 floor for `test:dist` is **5**, tranche-u re-grew it to
**10** (agglomerator-verified §4); F5's "10 → 10 flat" *entrenches* the re-growth. The owner-aligned
arithmetic is **10 → 7** (the Q13 five + `barrel-pure` + `grammar-fuzz`), and the META-gate (a gate that
counts gates) is dropped as the exact config-coupled ceremony the owner's D4 attacks (G1/G8). (2) The
`@`-ban eslint-vs-`proof:*` idiom fork (G2) is **owner-reserved** — F5 settled it to eslint, which breaks
the mandated F1 "never ESLint" law; the campaign cannot ratify both. Route to the owner, do not pre-decide.

### F6 · BIG-BANG CODEMOD → **ADVANCED (37%)**
The campaign's best-proven mechanism — RAN end-to-end, discovered the load-bearing **two-gate discipline**
(codemod is correct iff `audit SAFE:true` ∧ `oracle vue-tsc delta==0`), 373 rewrites, `git log --follow`
survives, glass-ui's regex codemod transplants (the `ts-morph`-is-wrong-tool correction is verified: 67% of
the `@`-blast lives in `.vue` `<script setup>` that AST tools skip). It is the sole surviving execution
family (absorbs F5's execution pole). **The critic's BLOCK is LIFTED at the fold**: it recommended blocking
F6 on the absent recursive-colocation target tree, but the fold sees F3 ran one feature + supplies the
per-file placement predicate that computes the rest — the cargo now has a shape. **What keeps it at 37%**:
F6 demonstrated a RENAME (six whole-dir `git mv` + `@`-drop, preserving the exact global buckets the edict
exists to DISSOLVE), not the per-file colocation SCATTER the owner asked for (critique-f6 F6-1); it RAN 1
of 4 surfaces (D2/D3/D4 asserted); the reviewability-via-oracle claim holds only for a rename (the oracle
proves resolution, blind to placement DESIGN — F6-3). ADVANCED as the vehicle, gated on the F1∘F3 charter
delivering the per-file manifest.

---

## §3 The pass-2 charters

Pass 2 runs the same convergent loop (RESEARCH → SYNTHESIZE → PROTOTYPE → CRITIQUE → AGGLOMERATE) against
these charters. **Batches ≤3 concurrent; prototypes RUN in isolated worktrees or are marked SPEC-ONLY;
docs-and-prototype-scoped only; the campaign never edits U.W-CLOSE surfaces.**

### Charter A — the WHAT-tree (F1 ∘ F3, fused) — **the lead workstream**
The single shape thesis: glass-ui's LAW (F1) operated by F3's placement predicate. Pass-2 must RUN:
1. **The general demo colocation** — not the best-case sealed `palette` bucket, but `composables/color/`
   (the A1 "worst offender": 18 files, 15 single-consumers fanning into DIFFERENT features). Emit the
   per-file move-map via the consumer-census predicate (unrelated iff nearest common feature-root = demo
   root), RUN it in an isolated worktree, report typecheck exit + the touched-consumer count + whether
   provide/inject/setup-order survives (the demo analogue of the src runtime-init lesson — critique-f2 G1).
2. **The demo kernel, ratified** — the 5–6 composables that promote (`keys`/`useContrastSafeColor`/
   `usePaletteManager`/`ink`/`useViewManager`) into a tiny `composables/` kernel; everything else colocates.
3. **The clean-break decision, ASSERTED** — value.js's `@`-abrogation is ATOMIC (no transitional
   `@`-alias window; that is F6's `git mv` + rewrite in one commit/surface), reconciling glass-ui's
   transitional-alias mechanism against the owner's "no aliases, no dual paths" law (critique-f1 G3).
4. **The api owner-fork, surfaced NOT settled** — present the measured evidence (boundary GREEN under
   glass-ui's own gate) and hold `routes/service/repository` → `api/model/lib` as an explicit OWNER
   decision (uniformity vs clean-boundary-preservation), never a pre-decided ratify (critique-f1 G9 / f2 G7).
5. **The referent-stability audit** — recover the 2 undocumented glass-ui plan-vs-execution divergences
   (#4-5, READ-ONLY `../glass-ui/docs/tranches/BH/`) + state F1's exposure; flag that the STRUCTURE-SPEC
   itself moved at round-6 (not a fixed referent) (critique-f1 G4).
6. **The src DOMAIN-capsule set** — ratify F3's honest correction (domain not feature for src): `color`
   (merged), `parsing` (non-color grammar), `units`, `transform`, `quantize`, `math`/`easing` kernel,
   coexisting with the orthogonal `subpaths/` publish surface (directory ≠ export map).

### Charter B — the execution vehicle (F6) + the banked-F2 substrate
1. **RUN the per-file colocation SCATTER** (not a whole-dir rename) using Charter A's move-map — the actual
   owner's D1 edict; prove the two-gate discipline (`audit SAFE:true` ∧ `oracle delta==0`) survives a
   scattered per-file move (critique-f6 F6-1).
2. **A per-file destination MANIFEST the human ratifies BEFORE the codemod runs** — the reviewability story
   for placement DESIGN (which the oracle is blind to), replacing the 221-diff eyeball (critique-f6 F6-3).
3. **RUN one src god-module carve** through the codemod — the `units/index.ts` value-model carve behind the
   UNTOUCHED 8-key exports map — proving the vehicle on D3, where the atomic-vs-publish hazard actually
   lives; defer any export-map de-indirection exactly as glass-ui did (critique-f6 F6-4).
4. **Wire + VALIDATE the banked-F2 substrate**: the deterministic runtime-vs-type-edge scanner extended to
   resolve `@`-alias + `.vue`; NO demo dead-code verdict is actionable until the `ComponentSliders`-class
   false-positive is disarmed (critique-f2 G4/G5). Co-design the **order-independent registry** so the
   color-SCC carve passes the `color2` runtime suite (critique-f2 G3) — the cycle-severance primitive F3's
   color capsule needs.
5. **Re-derive against HEAD** (protos ran at `6abef80`; ~5% blast-drift) + fold a doc/prose alias sweep as
   the explicit third gate (critique-f6 F6-5/F6-6).

### Charter C — the D3 typed-diagnostics DIMENSION (F4) + the banked-F5 enforcement battery
1. **Resolve the diagnostics fork** (F4's no-free-option tension): the memoize forcing-function decides
   **embedded-in-return** for the memoized entries (`parseCSSValue`/`parseCSSStylesheet`), a threaded sink
   for the non-memoized `parseCSSValues`, and **grammar-fail-closed** where the grammar auto-recovers
   (aligns the owner's "no masking fallbacks"); land the #11 rename (done in proto, re-run on HEAD).
2. **Close the remaining lossy-success classes** — the `kind:"unknown"` at-rule capture + the
   `parseCSSValues` partial-recovery (the proto closed only `@keyframes !important`); the deep `.map()`-
   closure rewire.
3. **The enforcement battery, banked from F5, CORRECTED**: the REPLACE arithmetic is **10 → 7** (cull to
   the ratified Q13 floor of 5 behavioral gates + add `proof:barrel-pure` + `proof:grammar-fuzz`), NOT
   "flat at 10"; apply the overfit rubric to ALL of `dts-surface`/`lib-correctness`/`pack-manifest` against
   the Q13 precedent; **DROP the META-gate** (critique-f5 G1/G8). The `barrel-pure` gate is a **runtime-
   purity property** (gate-derived per run: `{parsing, units, quantize}` today), never a hardcoded
   membership set (critique-f5 G6, §4). `barrel-cycle` runs on runtime edges only.

### Owner-reserved forks (routed OUT of the campaign, NOT pre-decided)
- **The `@`-ban idiom**: eslint `no-restricted-imports` (cheap, IDE-integrated, the tree's incumbent) vs
  glass-ui's mandated `proof:*` "never ESLint" (critique-f5 G2). The campaign presents both empirically;
  the owner rules — because ratifying F5-eslint breaks F1-never-ESLint, and both cannot stand.
- **The api vocabulary**: conform to `api/model/lib` for constellation uniformity, or ratify
  `routes/service/repository` as a measured-clean per-language variant (§1 fact 2).
- **The COUPLED OWNER EVENT** (from MEMORY): U ratify + T close as ONE — this campaign's pass-2 execution
  floats on that event; it authors, it does not land.

---

## §4 Agglomerator-verified facts (own authority, spot-checked at HEAD)

The agglomerator re-ran the two most-disputed measurements rather than accept them on assertion:

- **`test:dist` chains exactly 10 gates** (`dts-surface · css-parity · round-trip-idempotent · perf-target
  · serialize-fidelity · subpath-budget · lib-correctness · barrel-parity · pack-manifest · close-ledger`;
  `size-graph` is the 11th `proof:*` script, CI-only, not in the chain). The repo's ratified Q13 floor is
  **5** (`css-parity`, `round-trip-idempotent`, `perf-target`, `serialize-fidelity`, `subpath-budget`).
  → F5's "10 → 10 flat" preserves a **5-gate re-growth above the owner-ratified floor**; the corrected
  arithmetic is **10 → 7** (critique-f5 G1 STANDS).
- **Barrel-purity is a runtime property, and the spec's RED set is stale.** Own-RUNTIME vs own-TYPE exports
  at HEAD: `src/index.ts` = **0 runtime / 23 type** (runtime-PURE), `parsing/index.ts` = **10 / 1**,
  `units/index.ts` = **3 / 2**, `quantize/index.ts` = **2 / 1**. → the real runtime RED set is
  `{parsing, units, quantize}`, NOT the spec's hardcoded `{src/index.ts, parsing, units}`. The gate must
  assert a runtime-purity INVARIANT, gate-derived per run (critique-f5 G6, critique-f2 confirm STAND).

---

## §5 Pass-1 convergence read

Six families entered; the pass reduced them to a single convergent skeleton (one shape × one execution ×
the enforcement battery × the substrate × the diagnostics dimension) with **zero families vapor, zero
blocked, zero retired** — the composition the portfolio forecast, now earned by RAN evidence. The gating
truth for pass 2: **the owner's PRIMARY surface (D1) has been executed exactly once (F3, best-case) and the
general colocation + the per-file scatter + the runtime-safe color carve are the three unbuilt primitives
the fold hands forward** — none as hard as the original problem, all with named closers. The un-adversaried
F3/F4 scores are the pass's one hygiene debt: **pass 2 opens with an adversarial critic on F3 and F4**
before their convergence numbers are trusted as load-bearing.
