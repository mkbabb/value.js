# V · pass-4 · Charter ζ — THE D4 HYGIENE KILL-LIST, RATIFIED

**Lane**: pass-4 charter ζ (the D4 hygiene disposition — closes **GA-4 / clause 11 + OF-6**). **Date**:
2026-07-13. **Model**: opus (declared). **Scope**: docs- and prototype-scoped; the census RAN in an isolated
worktree (`wf_cc1a5634-d01-3`, npm-ci'd, `file:../glass-ui` via the pinned-substrate symlink); **NO deletion
lands — the LANDING WAVE executes** this ratified manifest. Every claim carries the command that produced it;
every instrument is **committed durable** under `instruments/` (THE DURABLE-INSTRUMENT LAW), re-runnable from
the repo root against any tree. Pathspec `docs/tranches/V/**`; nothing merges (worktree = evidence only).

**What this closes** (pass-3 AGGLOMERATION §3): the six survey-only hygiene items the owner named ("base repo
is a mess … top-level screenshots · stale working trees · docs · benches out of date/spec · plugins worthless
→ delete entirely · scripts pointless bar dev + deploy.sh"), each moved from Pass-0 survey prose to a **booked
disposition** (SWEEP / KEEP-cited / RETIRE / CULL / owner-fork) with per-row proof. The gate-cull is
**cross-cited to charter C (10→7), not re-derived** (§7).

**Standing laws honoured**: PROBE-PARSIMONY (one purpose-built instrument per axis, summary lines not raw
dumps) · BORN-RED / NO-FALSE-GREEN HONESTY (the adopt-drift build-block is recorded as a block, never
papered with a fabricated number) · OBJECTIVE-HONESTY (NG-3: every KEEP/SWEEP line decided by a CHOSEN
objective STATES the rule and NAMES the alternative) · SWEEP-SAFETY (never touch master / `tranche-*` / a
live worktree / origin-tracked / uncommitted; when in doubt RECORD, not delete — U-F51/F52/F53 precedent) ·
OWNER-RESERVED (OF-6 presented with measured poles, never pre-decided).

**Durable instruments committed** (`docs/tranches/V/design/pass-4/instruments/`):
- `docs-linkgraph.mjs` — the stale-docs inbound-reference census (§5).
- `plugin-delta.mjs` — the OF-6 measured-cost instrument (§6).
- `hygiene-census.mjs` — the one-shot re-prober for §1–§4 (root-PNG grep · worktree 4-clause law · bench
  symbol-resolution · script consumer-cite), so every §1–§4 number re-runs from the repo root.

---

## §0 The ruling up front — the kill-list, one line per item

| # | D4 item | measured HEAD state | RATIFIED disposition |
|---|---|---|---|
| **1** | root PNG litter | **39** loose `*.png`, **20 MB**, ALL gitignored (`*.png`) + untracked (never committed) | **SWEEP all 39** — zero load-bearing citations (the 7 basename "hits" all resolve to a *different* path or describe the litter itself) |
| **2** | stale worktrees | **16** non-primary (drift from the doc's 14: +the live pass-4 campaign) — **0 pass the 4-clause law** | **PROTECT all 16 now; DEFER-with-proof** (U-F53 ruling at 16× scale). 4 are 0-unique → SWEEP-eligible once their agent dies + tree is confirmed/committed |
| **3** | out-of-spec benches | **11**; symbols cross-checked vs `src/` | **6 KEEP-current** (1 gate-wired HARD) · **4 RETIRE-stale** (3 self-contained ghosts + 1 benching an EXCISED API) · **1 REWRITE-candidate** |
| **4** | scripts prune | **19** files (11 gates + 8 non-gate) | **dev.sh + deploy.sh + deploy-hook.sh + 3 ci gates KEEP** (all live-consumer-cited) · **build-fraunces-tnum.py CULL** (0 invoker, proven) · **oracle-slate-teeth flagged overfit** · gates → §7 |
| **5** | stale docs | **975** tracked `docs/**/*.md`; linkgraph inbound census | **2 litter files SWEEP** (`.DS_Store`, `sweeps.log`) · **0 genuine orphans** (the 2 flagged are load-bearing standing docs) · **922 records KEEP-by-convention** |
| **6** | the plugins (OF-6) | **2** LIVE + wired plugins; owner said "worthless → delete entirely", both auditors CONTRADICT | **OWNER-FORK** — 3 poles per plugin with MEASURED cost (`deferGlassFonts` = +98.1 KB gz render-blocking / worse LCP; `sourceExportPlugin` = 10 reference pages + golden break). Presented, **not pre-decided** |
| **7** | the gate-cull | `test:dist` chains **10** gates | **CROSS-CITE charter C: 10→7 stands** (Q13-five retained + `barrel-pure` + `grammar-fuzz` added; EXCISE dts-surface/lib-correctness/pack-manifest/barrel-parity/close-ledger). **Not re-derived** |

---

## §1 The 39 root PNGs — SWEEP all 39 (zero load-bearing citations)

**Command**: `ls -la *.png` (main tree). **39 files · 20 MB · all gitignored** (`.gitignore:32` `*.png`,
`:33` `!demo/**/*.png` — so root PNGs are ignored, demo assets carved out) **and all untracked**
(`git ls-files '*.png'` lists only `demo/` + `docs/tranches/…` tracked images; none of the 39 root files).
A gitignored + never-committed file cannot be a load-bearing dependency of any committed doc.

**The reference sweep** (`hygiene-census.mjs --pngs`, grep each basename across the tracked repo): **7 of 39**
basenames appear in a doc — and **every one resolves to a DIFFERENT path, or describes the litter itself**:

| root PNG (basename) | the only "citation" | resolves to | verdict |
|---|---|---|---|
| `blob-initial.png` | `S/audit/lanes/blob-greenfield-tech.md:7` | **`.playwright-mcp/blob-initial.png`** (ephemeral MCP dir) | not the root file |
| `s15-picker-dpr1.png` | `S/audit/lanes/aliasing-dithering.md:6` | **`.playwright-mcp/s15-picker-dpr1.png`** | not the root file |
| `dock-expanded.png` | `B/audit/B.W4-visual-runtime.md:29` | **`B.W4-visual-runtime/1280x800-light-dock-expanded.png`** (subdir, diff name) | not the root file |
| `palettes-view.png` | `N/audit/lanes/C5.md:38` | **`shots/C5-01-palettes-view.png`** (diff path/name) | not the root file |
| `browse-working.png` | `N/audit/lanes/A6.md:159` | *"27 PNG screenshots occupy the repo root … visual clutter"* — **describes the litter** | not a dependency |
| `color-picker-initial.png` / `color-picker-fixed.png` | `K/…/DELTA.md:17` | *"two Apr-4 root captures … fold into the baseline … archive, do not delete"* — a **K.W6 note never executed** (K closed) | stale note, not a link |

**RATIFIED: SWEEP all 39** (`rm` the untracked root `*.png`). **Zero KEEP-cited.** The landing wave MAY
optionally repair the 2 already-dangling K-note doc links, but those links are broken TODAY (the files were
never tracked) — repair is cosmetic, not a blocker. Full 39-file list + sizes: re-run `hygiene-census.mjs
--pngs`. **OBJECTIVE stated (NG-3):** the SWEEP rule is *"a root PNG survives iff a committed doc has a
load-bearing dependency on THAT file at THAT path."* The alternative — *"survive if any doc mentions the
basename"* — is rejected: it would keep litter on the strength of already-broken links to differently-pathed
files. Under the load-bearing rule the answer is unambiguous: 39/39 SWEEP.

---

## §2 The 16 stale worktrees — the 4-clause law, per worktree, ALL PROTECTED (defer-with-proof)

**Command**: `git worktree list --porcelain` = **17 entries** (1 primary + **16 non-primary**). The count
drifted UP from the doc's 14 — the campaign measured 14 at `d2bf5e1`; the live pass-4 campaign added
`wf_cc1a5634-d01-{1,2,3}` and the `wf_d1113dbe-da7-*` lanes appeared since. **The number is a moving target;
the LAW is invariant** — I apply U's **4-clause worktree law** (U-F53 §2 precedent) per worktree.

**The law (CONJUNCTIVE — all 4 must pass to SWEEP; any fail → PROTECTED):**
1. **0-unique vs master** (`git cherry master <br> | grep -c '^+'` = 0 — fully merged, no unique work)
2. **clean tree** (`git -C <wt> status --porcelain` = empty — no uncommitted work)
3. **not-locked** (no `locked` line in porcelain — no active-agent lock)
4. **not-live** (no active agent process holding the worktree)

**Per-worktree evaluation** (`hygiene-census.mjs --worktrees`):

| worktree | C1 0-uniq (cherry) | C2 clean | C3 unlocked | C4 not-live | fails | verdict |
|---|---|---|---|---|---|---|
| `wf_cc1a5634-d01-3` (**THIS lane**) | ✓ 0 | ✗ (my uncommitted pass-4 dir — live) | ✗ **LOCKED** | ✗ **LIVE (me)** | C2,C3,C4 | PROTECTED |
| `wf_cc1a5634-d01-1` | ✗ 120 | ✗ (live-moving) | ✗ LOCKED | ✗ LIVE (δ/ε lane) | C1,C2,C3,C4 | PROTECTED |
| `wf_cc1a5634-d01-2` | ✗ 120 | ✗ (live-moving) | ✗ LOCKED | ✗ LIVE (δ/ε lane) | C1,C2,C3,C4 | PROTECTED |
| `wf_cb377abc-406-10` | ✓ **0** | ✗ 7 (`?? src/units/{value-unit,function-value,types,value-array}.ts` — a δ god-module carve) | ✓ | ? | **C2** | PROTECTED |
| `wf_cb377abc-406-11` | ✓ **0** | ✗ 3 | ✓ | ? | **C2** | PROTECTED |
| `wf_cb377abc-406-12` | ✓ **0** | ✗ 35 (`panes/` restructure — an ε carve) | ✓ | ? | **C2** | PROTECTED |
| `wf_cb377abc-406-14` | ✓ **0** | ✗ 2 | ✓ | ? | **C2** | PROTECTED |
| `wf_cb377abc-406-13` | ✗ 91 | ✗ 10 | ✓ | ? | C1,C2 | PROTECTED |
| `wf_cb377abc-406-15` | ✗ 1 | ✗ 1 | ✓ | ? | C1,C2 | PROTECTED |
| `wf_1112d3db-442-3` | ✗ 103 | ✗ 1 | ✓ | ? | C1,C2 | PROTECTED |
| `wf_1112d3db-442-4` | ✗ 103 | ✗ 2 | ✓ | ? | C1,C2 | PROTECTED |
| `wf_1112d3db-442-5` | ✗ 103 | ✗ 12 (`src/parsing/stylesheet/` carve) | ✓ | ? | C1,C2 | PROTECTED |
| `wf_c072f8ee-d1e-3` | ✗ 61 | ✓ | ✓ | **LIVE** (U.W-PERF-tail, per U-F53) | C1,C4 | PROTECTED |
| `wf_d1113dbe-da7-1` | ✗ 111 | ✗ 1 | ✓ | ? | C1,C2 | PROTECTED |
| `wf_d1113dbe-da7-2` | ✗ 111 | ✗ 37 (`src/` library edits) | ✓ | ? | C1,C2 | PROTECTED |
| `wf_d1113dbe-da7-3` | ✗ 111 | ✗ 1 | ✓ | ? | C1,C2 | PROTECTED |

*(The clean-state column is a live-moving value for the 3 active campaign lanes; `hygiene-census.mjs
--worktrees` re-runs it fresh. The VERDICT is invariant — every row fails ≥1 clause.)*

**RATIFIED: 0/16 pass the 4-clause law → PROTECT ALL, DEFER-with-proof** (exactly U-F53's ruling, now at 16×
scale). Every worktree fails ≥1 clause with a recorded proof:
- **3 are the LIVE pass-4 campaign** (`d01-1/2/3`, locked) — active agents, C3+C4 fail.
- **The dirty trees hold LOAD-BEARING uncommitted work** — sampled: `406-10` has untracked `value-unit.ts /
  function-value.ts / types.ts / value-array.ts` (a δ carve of `src/units/index.ts`); `406-12` a `panes/`
  restructure; `442-5` a `stylesheet/` carve. These are **sibling pass-4 prototype lanes**, not scratch —
  SWEEP-SAFETY protects them absolutely (C2 fail).
- **`git worktree prune --dry-run -v` → empty** (no stale admin entries even to GC).

**The landing-wave re-probe (named, not executed here):** at a quiet moment (all agents confirmed dead), the
4 **0-unique** trees (`406-10/11/12/14`) become the SWEEP frontier **iff** their carve-work is by-then
committed-or-abandoned (C2 clears) AND their agent is confirmed dead (C4 clears); the `442`/`da7` trees clear
only once their branches merge or are explicitly abandoned (C1). **No worktree is swept on this pass; the law
is applied and the deferral is proof-backed** (a proof-backed deferral is honest, not a drop).

---

## §3 The 11 benches — 6 KEEP-current · 4 RETIRE-stale · 1 REWRITE-candidate

**Command**: `ls bench/*.mjs` = **11**; each bench's imported symbols cross-checked against `src/`
(`hygiene-census.mjs --benches`). Two axes: **(a) imports resolve?** (does the symbol still exist) · **(b)
spec current?** (does it guard a LIVE surface of the current dist API, or re-measure a landed decision).

**OBJECTIVE stated (NG-3):** the KEEP/RETIRE line is decided by a CHOSEN objective —
> **RULE:** a bench KEEPs iff it guards a live perf-regression surface of the CURRENT dist API (change the
> real code → the bench output moves); it RETIREs iff it is **self-contained** (imports nothing from
> `dist`/`src`, so it CANNOT regress when the real code changes — it re-measures a landed before/after via two
> hand-written model classes) OR it benches an API that no longer exists.

The **ALTERNATIVE** (*"keep any bench that still runs green"*) is REJECTED by the owner's own words ("most of
our benches are out of date and spec"): all 11 run, but a self-contained model of a closed decision guards
nothing.

| bench | class | symbols vs src | spec | verdict |
|---|---|---|---|---|
| **css-parse-perf.mjs** | dist/value.js | `CSSValues·parseCSSStylesheet·gamutMap·DisplayP3Color` ✓ | **GATE-WIRED**: `scripts/gates/proof-perf-target.mjs:111` imports its `runBench` | **KEEP-current — HARD** (a Q13-retained gate depends on it; retiring it breaks `proof:perf-target`) |
| **color-alloc-hotpath.mjs** | dist/color.js (ns) | ✓ | live color-allocation hot path | **KEEP-current** |
| **color-interp.mjs** | dist/value.js | `normalizeValueUnits·prepareInterpVar·lerpColorValue·parseCSSColor` ✓ | live interpolation hot path | **KEEP-current** |
| **color2-direct-paths.mjs** | dist/value.js | `color2·{HSL,OKLAB,OKLCH,RGB,XYZ}Color·OKLAB_TO_LMS_MATRIX·LMS_TO_XYZ_MATRIX·transformMat3·gamutMap` ✓ | live `color2` DIRECT_PATHS dispatch | **KEEP-current** |
| **gamut-boundary.mjs** | dist/subpaths/color.js | `sampleGamutBoundary·sampleGamutBoundaryInto` ✓ | live gamut-boundary sampling (Q8/S.W1 raytrace) | **KEEP-current** |
| **numeric-soa.mjs** | dist/value.js | `lerpArray` ✓ | live numeric hot path | **KEEP-current** |
| **color-channel-access.mjs** | **self-contained** | none (models `PreL8` Map vs `PostL8` own-property inline) | re-measures the **landed** D.W1-L8 storage migration; `PreL8` is a hand-modeled ghost | **RETIRE-stale** |
| **computed-endpoint.mjs** | **self-contained** | none (models pre-C1 memo vs candidate inline) | re-measures the **landed** C1/C2/C4 memo optimization | **RETIRE-stale** |
| **parser-namelookup.mjs** | **self-contained** | none (mirrors a 155-name `COLOR_NAMES` list inline) | re-measures the **landed** E.W1 name-lookup strategy | **RETIRE-stale** |
| **color-soa-fold.mjs** | dist/value.js | `buildColorChannelPlan·packColorChannels·lerpColorChannels` **✗ MISS** | benches an **EXCISED** API (`59d1cf5` *"S.W1 W1-7 color-soa EXCISE: remove orphan public SoA API [BREAKING]"*) — no guard → `npm run bench` **CRASHES here** | **RETIRE-stale (broken)** |
| **color-dispatch.mjs** | `@mkbabb/parse-that` only | (parse-that combinators) ✓ | benches parse-that `any/dispatch/regex` as a **proxy** for `color2` dispatch — never touches value.js dist | **REWRITE-candidate** (re-anchor to the real `color2` dispatch, or RETIRE) |

**Tally: 6 KEEP-current · 4 RETIRE-stale · 1 REWRITE-candidate.**

**Two wirings the landing wave MUST update on retire:**
1. `package.json:80` `"bench"` chains 5 benches — **3 are RETIRE-stale** (`color-channel-access`,
   `parser-namelookup`, `computed-endpoint`) **and 1 is broken** (`color-soa-fold`). Retiring them requires
   rewriting the `bench` script to chain the KEEP-current set.
2. `scripts/gates/proof-perf-target.mjs:111` → `css-parse-perf.mjs` — the load-bearing dependency; **do not
   touch `css-parse-perf`.**

---

## §4 The scripts/ prune table — dev + deploy.sh survive on merit; every other script cited or culled

**Command**: `find scripts -type f` = **19** (11 `gates/` + 8 non-gate). Gates → **§7 (charter C's 10→7)**.
The 8 non-gate scripts, each **LIVE-CONSUMER-cited or CULL with the zero-consumer proof**
(`hygiene-census.mjs --scripts`):

| script | live consumer(s) | verdict |
|---|---|---|
| `scripts/dev/dev.sh` | `package.json:73` `"dev"` (owner-named survivor) | **KEEP** (owner) |
| `scripts/deploy/deploy.sh` | `.github/workflows/{ci.yml,deploy-pages.yml}` (2 refs) (owner-named survivor) | **KEEP** (owner) |
| `scripts/deploy/deploy-hook.sh` | the on-host half of `deploy.sh` (`deploy.sh:44/48/68` reference it; invoked ON the host via the webhook receiver) | **KEEP** (deploy.sh's inseparable pair) |
| `scripts/ci/boot-smoke.mjs` | `package.json:87` + `ci.yml` (inv-N-1 boot-truth) | **KEEP** (live gate) |
| `scripts/ci/abrogation-sweep.mjs` | `package.json:88` + `ci.yml` (glass-ui-pin drift guard) | **KEEP** (live gate) |
| `scripts/ci/css-emission-probe.mjs` | `package.json:89` + `ci.yml:632` (inv-N-7 BLOCKING) | **KEEP** (live gate) |
| `scripts/ci/oracle-slate-teeth.mjs` | `ci.yml:167` ONLY (not in package.json); a T-era G-ORACLE-1 CI-config assertion | **KEEP-wired — FLAG overfit** (has 1 live consumer so NOT zero-consumer-cull; but it is exactly the "CI-config-assertion" overfit class the owner's gate-cull attacks → route the overfit judgment to charter C / the owner) |
| `scripts/fonts/build-fraunces-tnum.py` | **ZERO invoker** — the 2 grep hits (`readoutReservation.ts:74`, `o10-type-locks.spec.ts:200`) are **provenance comments** naming the script, not invocations; no `.sh`/`.yml`/`.json` calls it | **CULL** (0-invoker, proven; a one-shot font generator whose output is already committed. Its provenance is captured in the 2 code comments — the landing wave MAY relocate those 2 lines' pointer, but the script itself is pointless per the owner's "bar dev + deploy.sh") |

**RATIFIED**: 7 KEEP (2 owner-named + deploy-hook pair + 3 live ci gates + oracle-slate-teeth-flagged) · **1
CULL** (`build-fraunces-tnum.py`, zero-invoker proven) · 11 gates → §7. **`scripts/.dev/logs/`** (portfolio
A4) is not tracked (`git ls-files scripts/.dev` = empty) — gitignore-swept already.

---

## §5 The stale-docs census — a linkgraph instrument (2 litter · 0 orphans · 922 records kept)

**Instrument**: `instruments/docs-linkgraph.mjs` (committed durable). It resolves every markdown link + bare
`docs/…`/relative `…​.md` path across the **1793 tracked text-file referrer corpus** and computes per-doc
inbound-reference counts (total + **from-LIVE-referrers-only**, where a LIVE referrer is any tracked file
OUTSIDE a closed-tranche record dir).

**OBJECTIVE stated (NG-3):**
> **RULE:** a docs/ file is a STALE CANDIDATE iff (inbound-from-LIVE == 0) AND it is NOT a tranche RECORD AND
> not the current campaign (V). Tranche records `docs/tranches/{A..U}/**` are **KEEP-BY-CONVENTION** regardless
> of inbound (portfolio A4: "A..U closed tranches are records, keep").

The **ALTERNATIVE** (*"stale = inbound-from-ANY == 0"*) is REJECTED: it would flag thousands of leaf audit
docs cross-linked only within their own closed tranche — contradicting the keep-records convention.

**Result** (`node instruments/docs-linkgraph.mjs`, root = main tree): **975 tracked `docs/**/*.md`.**

- **Non-md litter under docs/ → SWEEP (2):** `docs/.DS_Store` (untracked) · `docs/tranches/T/audit/w9-close/
  sweeps.log` (untracked). Both SWEEP-safe (untracked, non-record).
- **STALE CANDIDATES (non-record, 0-inbound-from-LIVE) → 2, both FALSE-positive on inspection:**
  - `docs/dev-deploy-standard.md` — `inboundTotal=8` (referenced by R/N/K records), 0 from live code. A
    **standing deploy-standard doc**, not orphan litter (CLAUDE.md's Structure section lists it). **KEEP**;
    a landing-wave MAY verify its currency vs `deploy.sh`, but it is load-bearing, not stale.
  - `docs/instructions/README.md` — a live pointer doc ("Read `docs/precepts/instructions/` first"). A README
    is an entry point, not a link target. **KEEP.**
- **922 closed-tranche RECORD docs → KEEP-by-convention.**

**Honest finding (right-sizing the owner's "docs are a mess"):** the docs footprint is overwhelmingly the
**intentional closed-tranche RECORD corpus** (922 of 975 = 95%), which is keep-by-convention — NOT orphan
litter. The genuinely-actionable docs hygiene on this axis is **2 untracked litter files**; there are **0
genuine orphan docs**. Whether the record footprint itself should be compacted (archived/tarred) is an
**owner-level question above ζ's scope** — surfaced, not taken.

---

## §6 OF-6 — THE PLUGINS CONTRADICTION, surfaced with measured poles (owner-decided)

**The contradiction**: the owner said *"our plugins are worthless and to be deleted entirely."* Both pass-3
auditors HONESTLY CONTRADICT: both plugins are **LIVE + wired** in `vite.config.ts` (`sourceExportPlugin()`
`:178`; `deferGlassFonts()` `:302/:314`). ζ does **not** decide this — it measures both poles.

**Instrument**: `instruments/plugin-delta.mjs` (committed durable).

### What each plugin DOES (read, not asserted)
- **`sourceExportPlugin`** (`plugins/vite-source-export.ts`, 178 LoC): the `?source` import query — at BUILD
  time it rewrites each exported TS declaration into a Prettier-formatted, highlight.js-highlighted HTML
  string, so the demo's color-space reference pages show **live, real source** with no runtime formatter
  shipped. A real `ts.createSourceFile` parse (replaced a brace-scan that truncated 15/18 destructured-param
  conversions — W4-4 P0-1). Has a test golden (`test/docs-source-snippets.test.ts`).
- **`deferGlassFonts`** (`plugins/vite-defer-glass-fonts.ts`, 127 LoC): the S.W3-9 CSS critical-path diet — it
  keeps the glass-ui base64-woff2 font corpus OFF the render-blocking `index.css` (emits it as a hashed
  asset + a non-blocking `<link media=print onload>`; FOUC-safe via the producer's metric-compat
  `*-Fallback` faces). Dev keeps it render-blocking (faithful). **Directly bears on the Q14/LCP escalation.**

### The MEASURED cost of DELETE-ENTIRELY (`plugin-delta.mjs`, current HEAD)
| plugin | measured delete-entirely cost |
|---|---|
| **`deferGlassFonts`** | `@mkbabb/glass-ui/styles/fonts.css` = **132,943 B raw / 100,417 B (98.1 KB) gzip** → re-inlined into the eager render-blocking `index.css`. **+98.1 KB gzip on the critical path** (corroborates the plugin's own S.W3 baseline: render-blocking 188 KB → 86 KB gz, delta ~102 KB). The Q14 escalation is ALREADY over budget (LCP 5141 vs ≤2500) → this **worsens the failing metric.** |
| **`sourceExportPlugin`** | **10** reference pages import `?source` (`assets/docs/{lab,hsl,rgb,lch,oklch,hsv,xyz,kelvin,oklab,hwb}.md`) + the test golden. Delete → the build **BREAKS at every `?source` import** (unresolved) unless all 10 consumers are also gutted → 10 reference pages lose their live source snippets + the golden breaks. |

**The empirical with/without `index.css` delta is ADOPT-GATED (recorded honestly, NOT faked):** `npx vite
build --mode gh-pages` in the worktree **fails before CSS emission** — `"./goo-blob" is not exported … from
@mkbabb/glass-ui` — the **glass-ui BH/BI adopt-drift** (the same "raw-HEAD boot stays substrate-blocked" door
named in U.W-CLOSE §3), orthogonal to the plugins. So the render-blocking `index.css` byte-diff is deferred to
the adopt-cut; the cost above stands on the **direct payload measurement** (deterministic: `deferGlassFonts`'s
entire job is to move exactly `fonts.css` out of `index.css`) + the plugin's **own committed S.W3 empirical
baseline**.

### The THREE POLES per plugin (presented, NOT pre-decided — owner rules OF-6)
| pole | `sourceExportPlugin` | `deferGlassFonts` |
|---|---|---|
| **A · KEEP wired** | cost: the 2-file `plugins/` dir the owner dislikes; benefit: 10 live reference pages | cost: same dir; benefit: −98.1 KB gz off the critical path (better LCP) |
| **B · DELETE-AND-INLINE** (kill the bare `plugins/` dir; move the logic into `vite.config.ts`, per the survey's own suggestion) | move ~178 LoC inline; **zero behavior change**; the `plugins/` dir vanishes | move ~127 LoC inline; **zero behavior change**; the dir vanishes |
| **C · DELETE-ENTIRELY (accept the regression)** | −178 LoC + the dir; **10 reference pages lose live snippets + golden breaks** | −127 LoC + the dir; **+98.1 KB gz render-blocking → worse LCP** (against a metric already failing Q14) |

**ζ's honest read (an input, not a ruling):** the owner's "worthless" is measurably false for both — but the
owner's *underlying* want (kill the bare `plugins/` dir) is fully served by **Pole B** at zero behavior cost.
**OF-6 is owner-reserved**; the three poles are laid out with numbers so the owner decides with the full cost
in view. Pole B is available for BOTH without regression; Pole C carries the two measured regressions above.

---

## §7 The gate-cull — cross-cited to charter C (10→7 stands, NOT re-derived)

Per the charter brief, ζ does **not** re-derive the gate-cull. **Charter C ruled it** (pass-2 `charter-c.md`
§0/§3.1, carried in pass-3 as load-bearing): `test:dist` chains **10** at HEAD (`package.json:86`:
dts-surface · css-parity · round-trip-idempotent · perf-target · serialize-fidelity · subpath-budget ·
lib-correctness · barrel-parity · pack-manifest · close-ledger) → **10 → 7** by applying the overfit rubric:

- **RETAIN (7)** = the **Q13-five behavioral** (`css-parity`, `round-trip-idempotent`, `perf-target`,
  `serialize-fidelity`, `subpath-budget`) **+ `barrel-pure`** (a DERIVED runtime-purity gate) **+
  `grammar-fuzz`** (the born-RED VJ-corpus gate).
- **EXCISE (5)** = `dts-surface` · `lib-correctness` (closest-to-keep) · `pack-manifest` · `barrel-parity` ·
  `close-ledger` — each config-/ceremony-/type-surface-coupled, carried by the type system + review.
- `size-graph` is **CI-only** (not in `test:dist`, not in the 7) — retained as the standing size budget
  (U-F64 B5: GREEN on the tree).

**ζ cross-cite only** — the ruling is charter C's; ζ records it so the D4 kill-list is complete. The
`scripts/gates/` file dispositions (11 files) FOLLOW charter C's cull (5 EXCISE = delete the 5 files; 2 new =
author `proof-barrel-pure.mjs` + `proof-grammar-fuzz.mjs`, currently worktree-only per charter-c:30).

---

## §8 What ζ closes, and the landing-wave handoff

**GA-4 / clause 11 is CHARTED** — every one of the six survey-only hygiene items is now a booked disposition
with per-row proof + a committed re-runnable instrument (no more "survey-only"):

| item | disposition | executes |
|---|---|---|
| 39 root PNGs | SWEEP all 39 (0 cited) | landing wave: `rm` untracked root `*.png` |
| 16 worktrees | PROTECT all (0/16 pass 4-clause) · defer-with-proof | landing wave: re-probe at quiet moment; sweep only those that then pass all 4 |
| 11 benches | 6 KEEP · 4 RETIRE · 1 REWRITE | landing wave: `rm` the 4; rewrite `package.json:80` bench script; re-anchor color-dispatch |
| scripts | 7 KEEP + 1 CULL (fraunces) · gates → §7 | landing wave: `rm build-fraunces-tnum.py`; excise 5 gates + author 2 (charter C) |
| stale docs | 2 litter SWEEP · 0 orphans · 922 records KEEP | landing wave: `rm` `.DS_Store` + `sweeps.log` |
| plugins (OF-6) | OWNER-FORK, 3 poles measured | **owner rules** → landing wave executes the chosen pole |

**Owner-reserved (ζ decides none):** **OF-6** (plugins delete-vs-inline-vs-keep — 3 poles with measured cost,
§6). The `oracle-slate-teeth` overfit judgment (§4) rides the charter-C gate-cull spirit → owner. The
record-corpus compaction question (§5) is above ζ's scope.

**Durable instruments (THE DURABLE-INSTRUMENT LAW discharged):** `docs-linkgraph.mjs` · `plugin-delta.mjs` ·
`hygiene-census.mjs` — all committed under `instruments/`, each re-runnable from the repo root against any
tree (`node docs/tranches/V/design/pass-4/instruments/<name>.mjs`). No claim in this charter rests on a
worktree-only `_proto/` that resets away.

**Nothing merged; nothing deleted.** This is the ratifiable manifest; the landing wave executes it.
