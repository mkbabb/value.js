# Tranche V — Decided Design Register

There are no owner-reserved choices in V. This register converts the old campaign forks, U close brackets, and the new route portfolio into terminal decisions.

**V-A168 current-stage rule:** Value4 and Keyframes6 are immutable and their producer order is discharged. The live release rail is W17a→W33a→W17b→W18–W32→W33b; Q003 must be product-green before W17a returns bytes. Older D37/D38 `G_NEXT`/producer-order wording is historical. No Value/Keyframes rebuild, corrupt-artifact decline, workaround or alias is authorized.

## 1. Route decisions

| Domain | Route | Decision | Rationale / re-trigger |
|---|---|---|---|
| Visual | **Optical Bench** | **BUILD W18–W29** | It corrects the observed proportional and hierarchy defects without making spatial interaction or palette identity a prerequisite for every tool. |
| Visual | Pigment Cabinet | **FOLD its persistent object/status language into W22–W23; RETIRE the global IA** | Palette identity should persist inside palette work, but forcing every single-color task through a catalogue makes the product palette-heavy. |
| Visual | Living Chromatic Field | **BANK** | Re-trigger only with a running picker+gradient+mix+save prototype that has keyboard/numeric parity, 390px parity, and no worse LCP/INP than the Optical Bench. |
| Palette | **Immutable releases + mutable workspaces** | **BUILD W11–W16, W22–W24** | Separates workbench truth from gallery truth; content hashes become safe immutable data while release identity stays palette-local. |
| Palette | Transactional mutable aggregate | **RETIRE** | It collapses editable workspace truth and immutable public-release truth into one mutable identity. The selected release/workspace ontology is load-bearing; a latency result may optimize its transaction, but cannot reopen this discarded model. |
| Palette | Event stream | **RETIRE** | Replay, projection recovery, lag, and schema evolution recreate the original problem without a demonstrated throughput need. |
| Library | **Disciplined final objects** | **BUILD W7–W9** | Removes subclass forest, SCCs, late registration, dynamic indexing, DOM coupling, and facade duplication while preserving object ergonomics for keyframes. |
| Library | Functional discriminated kernel | **RETIRE** | The selected final-object surface plus keyframes-owned `InterpSlot` closes the consumer contract without a parallel representation/mapping layer. Reopening a second value model would restore the concepts V is deleting; any genuinely new consumer requirement is a new ask, not this route. |
| Library | Injected algebra runtime | **RETIRE** | No second backend exists; TypeScript protocol plumbing adds rather than removes the hard part. |
| Performance | **Chromatic CSS-first progressive enhancement** | **BUILD W28, W29, W31** | The permanent CSS substrate and semantic shell are complete; renderer eligibility queues after critical paint and cannot create a later LCP. |
| Performance | Eager optimized GPU opening | **RETIRE** | The current graph is already effectively renderer-eager and remains about 5 seconds LCP. |
| Performance | Worker-owned scene | **BANK** | Re-trigger only if W31 meets lab LCP/INP yet either hardware class misses p95 frame interval ≤1.5× refresh or produces consecutive >50ms animation frames during the 20-cycle renderer journey. |

## 2. Product and architecture rulings

| ID | Ruling |
|---|---|
| D1 | value.js publishes **4.0.0** from this clean break. The stale `3.2.0 Unreleased` and old V `2.0.0` claims are retired. |
| D2 | Artifact order was Value 4.0.0 → Keyframes 6.0.0 → Glass 7.0.0 → external consumers; **Value 4.0.0 and Keyframes 6.0.0 shipped immutable and their producer order is discharged (V-A126)** — no future publish remains on that edge. The single current Keyframes registry byte authority (L2) is the **184,430**-byte SHIPPED tarball recorded once at ADDENDA V-A117; the 184,710-byte held archive and the 184,254-byte strict replacement are **pre-publish lineage only** (referenced, never restated as current — the earlier authoritative-input phrasing is retired drift). Glass's packed diff selected 7.0.0; V-A88/V-A90–V-A92/V-A96 require closed strict declarations and **persistent-only** HeaderRibbon native behavior (V-A92's opt-in-collapsible arm superseded by the 2026-07-16 glass mark; the persistent core intent — VNode inference dead, action-only composition persistent — stands), while V-A94/V-A95 require actual-aperture cue placement and post-drag layer integrity. V-A97 makes the publication graph asymmetric: Keyframes released from registry Value 4 without requiring unpublished Glass 7; Glass then releases from registry Value 4 + Keyframes 6; Keyframes's demo/dev Glass edge and mandatory lock refresh only afterward. These archives are not registry coordinates. W33 alone publishes the remaining fresh Glass candidate; immutable tags never move. |
| D3 | The broad `@mkbabb/value.js` root export is removed. Sole entries are `/color`, `/value`, `/css`, `/easing`, `/math`, `/transform`, `/quantize`. Old `/units` and `/parsing` names receive no alias; the consumer-less `/browser` proposal is retired, with its sole live computed-resolution mechanism transposed into Keyframes. |
| D4 | The demo tree is physical and ownership-first. A shared home is earned by at least two live consumers; edge count is a diagnostic, not the placement objective. |
| D5 | The API keeps `routes`, `service`, and `repository` vocabulary inside vertical domains. Its defect is composition/trust behavior, not nouns. |
| D6 | Parsing returns one discriminated `ParseResult<T>` with UTF-16 source spans. Throw/null/status+value/stringization/substitute-color paths die. |
| D7 | A closed immutable `Color<S>` with the exact 17-row `ARCHITECTURE.md §2` `SpaceId→ChannelsBySpace` schema, physical coordinates, `"none"` sentinel law, named factories and total static conversion anchors owns color identity. Exactly the thirteen CSS-native spaces carry canonical CSS spellings; `hsv`, `kelvin`, `ictcp` and `jzazbz` carry numeric library-display notation only, rejected by `/css` with no parser/serializer round trip. No space is left to an implementation registry or one-space prototype. Immutable CSS values are distinct from keyframes-owned mutable `InterpSlot`s. Free operations depend downward; the only live browser target/cache consumer owns that mechanism inside Keyframes and consumes value's pure layout-unit predicate. |
| D8 | Palette public handles are identifiers, never credentials. Recovery material is exact canonical `rs1_` + 32 CSPRNG bytes, shown once, with only the versioned secret-manager-keyed HMAC-SHA-256 verifier stored; rotation advances policy, invalidates old sessions and revokes the old verifier. Browser auth is one same-origin HttpOnly `__Host-value-session` carrying exact `st1_` + 32 CSPRNG bytes with a fixed 30-day non-sliding horizon; bearer headers/storage/CORS paths, public-handle regeneration and admin impersonation are retired. Named scoped principals replace the shared token. |
| D9 | Palette visibility is `private | public`; owner lifecycle is `active | trashed`; admin moderation is separately clocked `clear | withdrawn`. The unused `unlisted` state dies. Non-owner reads require active/public/moderation-clear and a visible immutable release. |
| D10 | Public palette content is an immutable release. Private edits live in an explicitly saved workspace. Conflicts offer load remote, duplicate local draft, or discard local; there is no automatic merge. |
| D11 | Palette clocks are handle generation, workspace revision and policy revision; an immutable release has a strong validator but no mutable clock. Publish supplies expected workspace and handle clocks and every mutable command CASes in its database write. |
| D12 | Device drafts and server workspaces are different named entities. Publishing is an explicit transition; remote palettes are never silently cloned into local identity. |
| D13 | Votes target the palette lineage; forks/reports target immutable releases; reports retain open/resolved/dismissed evidence and pin their target Release/ContentObject snapshot. Edges are authority. Public detail counts are exact current indexed joins; catalog card counts/order are explicitly labeled immutable RankEpoch values under one closed token/score/color-distance law. |
| D14 | W10 establishes target identity/platform code; W11–W14 transform the exact nine current collections under `PALETTE-DOMAIN.md §7` and `LEGACY-PALETTE-SOURCE-CONTRACT.md`; W16 rehearses twice; W33 write-freezes, builds an unreachable fresh candidate, verifies, bootstraps, switches one credential and only then removes the source from runtime reach. Only valid live public standard/featured palettes become read-only system lineages. Private/unlisted/archived/deleted content is encrypted offline with tombstone/retired-claim identity where claimable—never an inaccessible live row. Legacy handles are permanently reserved; no old row receives a Principal, credential, session, owner Workspace or invented attribution. Row-level source/target/disposition/provenance/reject material remains only in the restricted versioned object; the tracked public manifest is aggregate/redacted. |
| D15 | The dark material pole is restrained neutral Smoke, not seed-tinted brown. Chroma belongs to the ambient field, specimens, live accent, and the pastel Palettes mark. |
| D16 | Fraunces / Plus Jakarta Sans / Fira Code remain, with strict jurisdictions. The font corpus becomes ordinary cacheable producer assets; the custom deferral plugin dies. |
| D17 | BI component adoption is per-site and semantic: the two `ConfirmDialog` confirmations → P106 Dialog; `VersionHistoryDrawer` Sheet → P107 fixed-right Drawer; `ActionButton` and `PaletteSlugBar` HoverCards → P104 Tooltip (keyboard-reachable/described); `ColorInput` HoverCard → P103 Popover/disclosure preserving textbox focus and dismissal; `ToggleChip` → P091 Chip selection mode; old Dock sites → P034/P036/P040/P041. `InstrumentChassis` and `Slider` are consumed directly. The forwarding barrels and old names die; no blanket rename survives. |
| D18 | The two dirty `CLAUDE.md` deletions are preserved as user work. They are **BANKED** until independently attributed; a glass-ui-only Claude instruction is not authority to resolve them. |
| D19 | BI P047's current WebGPU-preferred/WebGL2-equivalent Blob contract stands. The historical single-WebGL2/drain-`metaball.wgsl` arm is **RETIRED**, not reopened in V. P043 owns shared procedural lifecycle, P045 capability/failure/engine identity, and P054 pause/resume/resource scheduling; P046/P047 expose only Aurora/Blob facade projections. W17 validates the producer measurement seam and freezes baseline `b0`; W29 alone changes Picker `geometry.bodyRadius` and owns the final painted-component ratio/centroid/hull result. |
| D20 | Font responsibility is split exactly without reviving declined process waves. Glass P019 owns the Plus Jakarta Sans/Fira Code product surface—faces/ranges/metrics/fallbacks and the family-neutral proportional pair—while P010 owns semantic projection and P127 owns hashed packed emission, URL resolution, dependency and release truth. Native font request/stability review belongs to P019's product boundary plus P127's packed-consumer smoke; declined P128's build-project generator and P061's scenario runner remain absent. Fraunces remains a value-owned identity face: W4 deletes the zero-invoker `scripts/fonts/build-fraunces-tnum.py` and old numeric-tnum comments, W20 moves all numeric duty to Fira, and W31 extracts Fraunces from inline HTML into one normal value stylesheet with hashed emitted WOFF2, metric-compatible fallback/preload/request/CLS evidence. The speedtest `?url` case remains an explicit Glass-face migration. |
| D21 | Frame zero uses one strict numeric `SeedToken` boot union, not injected stop constants or arbitrary CSS validation. A synchronous same-origin `prepaint-seed.js` first resolves and applies the persisted/OS light-dark scheme to root `.dark` plus `colorScheme`, then accepts only canonical bounded `v1:L:C:H`, selects URL token → versioned-storage token → resolved-scheme token, and records diagnostics without mutating corrupt input. Context-dependent/CSS-wide values and full-color/token mismatch can never seed prepaint. An invalid selected default is a terminal semantic boot error with no seed/atoms, not a fallback; otherwise the permanent CSS atom graph is the sole initial seed→ground derivation and hydration may retain a full input only when its `/color` projection matches the frozen token. |
| D22 | The 4.0 clean break includes every active first-party consumer, not only keyframes/glass. Atlas, bbnf-buddy, bbnf-lang, fourier-analysis/web, muster/frontend, sci-report, speedtest, slides and words/frontend are BUILD rows rehearsed with local tarballs in W17 and cut to registry artifacts in W33; glass Vite externalization and `profile-bundle.mjs`, Keyframes' execution-time AST module census plus two TS bench resolvers, two tracked HTML import maps and named script/config seams, Slides' dynamic keyframes load and Words' root/prebundle/retired-glass sites are explicit consumers. Keyframes commit `70b32501` is credited for already deleting four proof scripts; W17 migrates/revalidates surviving ordinary tests and retires the stale executable Tranche-I root-import probe. The detached bbnf fork, duplicate `slides-K` clone and non-package Hassio prototype are RETIRED as independent consumers. Execution entry re-censuses every live primary: current Atlas `SourceDataBrowser.vue`, bbnf-lang Vite/test-results, Fourier version files, Muster evidence/docs, sci-report untracked research, and Words primary/root-lock work are protected without misdescribing already-landed or now-clean files. |
| D23 | Initial admin authority is a one-time deployment-only transaction, not an HTTP/shared-token exception. Trusted workload identity authorizes it; an operator-generated, offline-kept recovery secret enters over stdin and only its verifier persists. The transaction creates a permanent bootstrap tombstone/PublicHandle claim, initial Principal/AuthFence, the exact eleven-scope Grant, exact PrincipalAuthority projection, AuthorityGuard `{epoch:1,eligibleCount:1}` and attributed audit event. Each grant/status command updates one bounded target projection and guard delta; full grant/Principal scans exist only in epoch-stable reconciliation. Concurrent self-removals yield one success/one `last_root_authority`; cross-target mutual removal yields one success and actor-fence failure. Re-bootstrap is impossible. |
| D24 | Color-name uniqueness belongs to a globally unique normalized-name slot, not an immutable proposal row. Exactly one pending/approved proposal occupies a slot; concurrent claims yield one success/one named 409. Reject/withdraw releases the slot for a new immutable attempt while every old proposal and decision remains. |
| D25 | Public handles and every historical palette slug share one permanent NamespaceClaim mechanism keyed by namespace plus normalized spelling. First acquisition inserts the never-deleted row; close/rename may only retire it, rename claims its destination before retiring its source, and purge retires any active claim before live-subject removal in the same transaction. Normalized tag keys are likewise permanent. One Principal may file one retained Report per immutable Release, so retry replays and any distinct duplicate returns only the caller's stable case instead of manufacturing or erasing pinned evidence. |
| D26 | Command receipts use server-derived namespaces, never a not-yet-created Principal ID: global `registration`, verified `recovery:<publicHandleClaimId>`, or authenticated `principal:<principalId>`, each paired with a canonical UUIDv7. Binding digests and durable browser shells omit all secrets/bearers; anonymous replay is authorized through the receipt-bound credential and issues a fresh valid session without repeating identity. Fork provenance is uniquely keyed by destination Handle, so one source may have many forks but one destination can have only one origin. |
| D27 | Keyframes U closed on the immutable Value 4.0→Keyframes 6.0.0 rail (the retired packed-diff placeholder label is now fixed to shipped Keyframes 6.0.0; L2/V-A126): `/css` timing `ParseResult` replaces and retires `getTimingFunction`; `/css` `parseKeyframeSelector` maps `from` to the frozen typed percent selector at `0` and `to` to the one at `1`, with canonical `0%`/`100%` serialization and typed reparse; immutable final values compile once into keyframes-owned `InterpSlot`s and retire `plain-vars`; `/value` exports one `isLayoutTrackingUnit` predicate so the WAAPI literal dies; live `@property` metadata is `CSSPropertyDescriptor` with no bare alias; diagnostics stay ordinary W9/W17 behavior. The selector correction adds no runtime or type export, and Keyframes retains no local parser or fallback. |
| D28 | D-GAP-1 **SHIPS** the exact six quart/quint catalog rows in 4.0. D-GAP-5 is **DECLINED AS SUPERSEDED**: no old flatten/leaf-predicate API grows, and both demo callers consume compiler/typed declaration output. D-GAP-6 is **DECLINED upstream**: the unused whole-element helper dies and one keyframes-local presentation sampler makes `flipY` explicit. No row is re-deadlined. |
| D29 | Cloudflare's HTTP proxy/Worker/WAF/Pages/VPC rail is **RETIRED** because it normalizes request paths before the application can enforce the no-alias namespace law and cannot emit the singular dynamic Problem at that boundary. Production is one dual-stack Layer-4 TCP load balancer with PROXY v2 feeding redundant private NGINX+njs/QuickJS TLS edges. One raw-buffer dispatcher serves the immutable SPA and relative `/api`, executes the exact projection-only path classifier, reconstructs/signs a closed header block, and reaches private TLS Hono only through mTLS plus MAC. It validates the exact response/cookie grammar and fully settles cookie-changing responses. No normalized, plaintext/public-origin or direct-CORS path survives. |
| D30 | Registration has only a 5/10m new-effect source budget. Recovery budgets failed proofs by source 10/15m and, after dummy/real verification, handle 5/15m. Exact completed registration replay and correct recovery proof/replay bypass hostile exhaustion; the registration-handle lockout counter is **RETIRED**. |
| D31 | Public optional authentication fails open to anonymous for absent/malformed/revoked/expired/stale session state; only a valid current session personalizes vote membership. `/sessions/me` remains strict and independent. |
| D32 | `PALETTE-WIRE-CONTRACT.md` owns one CSS `NamedColorInput`, exact Workspace colors/digest/base/tag projections, Audit subject/action enums, caller-safe `report_exists` reference, exact UUID/release/ETag grammar, title/field-issue registry, and cursor field/envelope/error placement. `PALETTE-CURSOR-CONTRACT.md` alone owns cursor query identity, scan coordinates, deterministic bytes and AES-GCM cryptography. No open DTO, second input spelling or second cursor codec survives. |
| D33 | JSON/CSS/Tailwind/SVG/PNG follow `PALETTE-EXPORT-CONTRACT.md` byte-for-byte. Positional tokens eliminate name collisions; Tailwind is data, SVG is font/network/script-free and PNG uses the authored deterministic profile/encoder. Library/canvas defaults are not alternate serializers. |
| D34 | Browser delivery order for concurrent same-name `Set-Cookie` responses is not controllable by the tab lease or edge deadline. The no-late-overwrite claim is **RETIRED**. The edge guarantees only complete bounded origin settlement; a stale token remains unauthorized, `/sessions/me` fails closed, and Account exposes explicit signed-out/recovery if a post-emission stale response wins. No bearer, alias cookie, dual auth path or masking retry is added. `research/auth-cookie-order.md` is the terminal ruling. |
| D35 | Glass BI's published `v6.0.0` is immutable at peeled commit `e5b3a2095b6c3e330b5d82ca3330f1eac4e3c895` (tag object `b262e2d30976d0330014f359803e0c2c65b9b691`, npm 6.0.0); the V delta selected 7.0.0 from the packed public diff, not from a predetermined epoch label. The active producer tree contains post-BI implementation and is preserved as moving executor state, not frozen formation authority: P019 type pair, P051 noninteractive face, P092 one-fill mechanism, P122 six-property source/tests plus universal-minimum deletion, P047 mass/origin seams and the profiler's original two-edge mechanism are credited; the profiler is **reopened** for V-A10's exact `/color`+`/css`+`/easing` set, while P019/P051/P092/P122 native Browser witnesses, P046 exact dual-engine/pointer evidence, P047's complete painted-component protocol and replacement of Search's planning-language statuses remain open. The exact direct handoffs were sent to active task `019f6610-6022-7df1-95b3-472b17a64656` on the same execution rail; W3 revalidates live HEAD/source/tests and W17 consumes only verified results, including the packed public-surface comparison against immutable 6.0.0 required by D2. This is a baseline correction, not a new meta-tranche, compatibility window, semver conclusion or claim that source-complete equals visually complete. |
| D36 | `ContentObject` itself is the sole per-digest reference-serialization authority. Exact nonnegative signed-int64 Workspace/Release/Report counters move atomically with every referent; the object exists iff their sum is positive and zero deletes synchronously. Workspace edits therefore cannot strand their prior digest, and acquire-versus-delete cannot commit snapshot write skew. The purge content phase, candidate queue, delayed scan, direct writer and reconciliation alternate are **RETIRED**; palette purge is `seal→releases→edges→complete`. |
| D37 | The Glass relay uses direct immutable handoff plus P127 package ownership because P004's registry, P061's scenario runner and P128's generated build graph are terminally retired. Each product wave owns ordinary tests and the native Browser review required by its actual public/product delta. W3 closes by deciding each ask and recording its present **LANDED**, **RED** or **DECLINED** evidence; it requires no future rehearsal or artifact coordinate. A coordination return may report **RE-DEADLINED**, but a material producer defect remains RED until W17 supplies the first clean staged rehearsal or W33 records **SHIPPED** final coordinates, unless **DECLINED** already names the architectural rationale and exact implemented replacement. Re-deadlining cannot close the producer/adoption/release row. The V-A5–V-A21/V-A65 owners remain on the evidence-derived Glass `G_NEXT`/W3→W17→W32/W33 or named value/product rail and cannot create a new tranche, gate farm, proof runner, consumer override or value-specific Glass branch. **V-A130 current-stage correction:** Value4 and Keyframes6 have since shipped; the live producer/adoption remainder is Glass-only strict/native/release closure, stale demo-canon cleanup and downstream refresh. |
| D38 | Glass `G_NEXT` consumes exactly three Value 4 capabilities: `/color` for failure-explicit final color objects/operations, `/css` for `parseCssColor(source:string): ParseResult<CssColor>` and `serializeCssColor(color:CssColor): Result<string,ColorIssue>`, and `/easing` for the exact failure-explicit numeric constructors plus named easing exports. `CssColor` is the closed thirteen-space CSS-native union; all seventeen spaces retain factory/conversion/object-oracle coverage, but the four library-only spaces must explicitly convert to `CssColor` and have no general library-string serializer. Fourier consumes the named exports and uses `easing(name)` for failure-explicit dynamic lookup; the `timingFunctions` aggregate and fallback die. Glass source imports, `vite.library.ts`, live `profile:bundle` accepted map and ordinary bite test agree on the literal three-entry set; `/css` is not rejected, and the root, prefix umbrella, old/private entries, local parser, fallback and alias are absent. **V-A130 current-stage correction:** Value4 and Keyframes6 are immutable and the registry-only graph is proven; W33 now packs/publishes only Glass7 from that graph, then downstream locks refresh. The earlier bootstrap/rehearsal publication procedure and stale Value3 demo canon are historical. |
| D39 | CSS color parse failure, unresolved cascade input, lazy-pending state and absent configuration remain distinct Result/domain-error states; none becomes empty string, inherited foreground, `var(--primary)`, a warm-card default or cached identity success. Glass's accent ink is certified against the actual typed band color with Value's Color-against-Color WCAG-ratio path. The old background-lightness proxy, `minContrast * 0.1` conversion and silent CSS fallback are retired. |
| D40 | Public `/color` hue operations, including `interpolateHue`, use the final schema's physical degree domain and return degrees; normalized-turn helpers remain GPU-local and do not define the public color contract. Glass Aurora owns warm/cool pole composition but consumes this degree-domain operation, with named wrap, no-overshoot, midpoint/zero-identity and maximum-throw vectors. |
| D41 | Ignored `dist` never satisfies prepare/pack. W7's bootstrap and V-A93's 184,254-byte Keyframes replacement are **pre-publish lineage evidence** superseded by the **184,430**-byte SHIPPED registry tarball (the single current byte authority at ADDENDA V-A117, L2); the 184,254-byte replacement removed the false public `implements` claim and passed strict external consumption before that publish. Glass now owns the fresh candidate closing V-A88/V-A90–V-A92 through **persistent-only** HeaderRibbon behavior (the opt-in-collapsible arm cut per the 2026-07-16 glass mark; persistent core intent preserved) and V-A94/V-A95 through honest Q003 cue/layer native behavior. W17 removes old pins, proves the routed mount after Glass7 and refreshes downstream consumers; it does not recreate Value rehearsal bytes. No alias, exposed internal loop members, shim, undeclared reach, hoisting, `skipLibCheck`, DOM/VNode inference, offset/overflow cover, engine mask, reset, timing cloak, consumer workaround, file link or dual path. W33 alone publishes the fresh Glass candidate; rehearsal hashes/HEAD are not release evidence. |

| D42 | The corrected bootstrap handoff is transport evidence only when its durable manifest exists on disk. The manifest records supplied path/bytes/SHA-256/SHA-512/integrity/source identities, seven no-root entries and ordinary verification counts; it deliberately omits guessed release/export/declaration hashes and never upgrades `BOOTSTRAP ONLY` to rehearsal or publication. |
| D43 | Catalog cursor immutability covers only rank/order coordinates. Catalog eligibility, access, current-policy and current-tag projections are rechecked at read and may skip an expired or changed epoch row; all other query routes remain live keyset views. The no-repeat/no-skip promise applies only to rows whose eligibility and complete sort coordinate remain unchanged. |
| D44 | The Value stylesheet parser owns comment elision and zero-argument timeline-function admission. A bootstrap that rejects leading/inter-rule comments or routes valid `view()` through a stricter generic value parser is held defective and cannot seed Keyframes rehearsal. The replacement is one canonical producer grammar with ordinary direct/round-trip/failure vectors; Keyframes adds no parser, normalizer, special route or fallback. |
| D45 | Animation shorthand expansion and cascade are producer semantics. `collectAnimationOptions(readonly Declaration[])` returns frozen `readonly CSSAnimationOptions[]`, structurally expanding the already-parsed shorthand and applying per-subproperty `!important`/source-order/reset laws plus CSS repeatable-list alignment. A singular consumer selects `.at(0)` explicitly; the producer never silently truncates valid comma lists. `collectTimelineOptions` consumes the same expansion for shorthand resets. Keyframes' temporary shorthand classifier is deleted before rehearsal; no text parser, regex, serialization/reparse, fallback or dual owner survives. |
| D46 | RF-1 feature-deletion rulings (bracket B1), executed W40-S4 atomic with their import-removals. (a) **Perceived-space plate** (`PerceivedSpacePlate.vue` + `envelopePlatePaint.ts`): rebuild-on-v4 at **W53** as the gradient workbench's honest instrument; the reference implementation is preserved tag-only at `v-perceived-space-plate-ref-w40` (no working-tree restore — the plate imports dead v4-cut APIs `color2`/`OKLChHueSweepBoundary`/`oklab2linearSrgbInto`/`cssToRawColor`). (b) **SpectrumCanvas gamut-truth overlay family** (~−983 LoC): **terminally retired**, superseded by veil semantics. (c) **Ink-walk family** (~−176 LoC): **terminally retired**, superseded by the ink ladder. The immutable `.26` HeroBlob baseline is preserved tag-only at `v-blob-b0-26-ref-w40` for W48's before-frame reconstruction. No feature is re-chartered; the tags are the sole preservation surface. |
| D47 | **W40 fresh-clone gate SCOPED (execution ruling, 2026-07-17).** The literal `npm run typecheck && npm test` all-green cannot precede W44 by the spec's own assignment: B7's vitest include extension makes the relocated aurora suite RUN, and it imports `@mkbabb/glass-ui/aurora` — resolvable only via the pinned Glass 5 island (a `node_modules` symlink absent from the committed lock), the exact island W44 replaces (W44's own born-RED). Execution evidence: library typecheck (`tsconfig.lib.json`) exit 0 from committed state alone; `npm ci` coherent (435 pkgs); `npm test` 345 passed, 30/32 files — the 2 failing files and ALL 144 demo-typecheck errors are 100% glass-island-attributable (124 `Cannot find module '@mkbabb/glass-ui…'` + cascades; ZERO value.js-attributable failures). RULING (execution lead, under the 2026-07-17 owner totality order): **W40 closes GREEN on its owned surface; the full fresh-clone green transfers to W44's completion evidence**, where the island replacement + registry-only lock regeneration already live. No formation reopen (V-PRIME §0/§4); no gate is weakened — it is re-homed to the wave that owns its blocker. |
| D48 | **B3's CI demo-typecheck step lands at W42 SOFT-ENFORCING (execution ruling, 2026-07-17).** The W42-verify pass proved the hazard: `@mkbabb/glass-ui`/`@mkbabb/keyframes.js` are undeclared (RF-29 B2, resolved only via the gitignored island symlink), so CI's `npm ci` cannot reconstruct the demo graph and `vue-tsc -p tsconfig.demo.json` fails on module resolution — an infra failure, not a type error. RULING (mirrors D47's re-homing logic): the `ci.yml` step lands at W42 as B3 orders, annotated `continue-on-error: true` — the signal stays visible in every producer run without turning CI red on a W44-owned blocker (a standing red would mask real regressions). **W44 flips the step to hard-enforcing** in the same slice that declares the dependencies and regenerates the lock (B2/3b). |
| D49 | **W41's byte tilde-target adjudicated (execution ruling, 2026-07-17).** The spec's "living canon ≤ ~150KB" conflicts with its own disposition table (KEEP docs = unchanged; the eight KEEP docs alone sum ~273K post-fold). The TABLE governs — no KEEP doc is compressed, then or later. RF-8's discharge is the qualitative one, achieved and measured: top-level 968K→**348K** (−64%), old `waves/` 408K→**0**, sediment relocated whole to `archive/` (1.1M, nothing deleted), live wave specs 140K untouched. The ~150KB figure is recorded as an authoring ESTIMATE made before the fold sums existed, not a hard cap; the gate is satisfied at 348K. Ancillary (same ruling): the five untracked KEEP-class top-level docs (EVIDENCE, OPTICAL-BENCH-COMPOSITIONS, PROPORTION-AUDIT, SUBTRACTION, VISUAL-CONSTITUTION) are TRACKED as-is — living canon must survive a fresh clone; zero content change. |
| D50 | **W43(a) src deviations accepted (execution rulings, 2026-07-17).** (i) Explicit `/index` specifiers on the four directory-barrel references: api-extractor's dts rollup stack-overflows on IMPLICIT directory-index resolution through subpath re-exports (empirically bisected) — substance unchanged, one barrel per multi-file feature, packed surface byte-identical. (ii) The css god-module row closes **2→1**: `grammar.ts` 658→483 fully de-god'd (`css/syntax.ts`); `stylesheet.ts` 940→899 — the residual is the CORE DECLARATION PARSER (the two collectors share seven helpers with what stays); a deeper split would manufacture the shared-helper module the wave's own DIVERGE clause forbids ("not a line-count trophy"). Also: two of the five named functions lived in grammar.ts, not stylesheet.ts — spec-premise drift, recorded. |
| D51 | **The gh-pages demo build is RED at the W43 base and CLOSES AT W44 (execution ruling, 2026-07-17).** Eight `?source`/`@src` source-embedding references in `assets/docs/*.md` point at the `src/units/color/*` tree retired at W40-S1 — proven pre-existing (reproduced at clean HEAD with all W43 work stashed; zero errors reference any W43-touched path). W44 item 5 owns "stale demo canon… producer references in active demo docs" and its evidence line is "run the full demo build" — the RED is W44's by the spec's own assignment (the D47 pattern). W43's completion evidence deliberately lists the production LIB build, not gh-pages. Every W43 lane verified no-new-errors against the frozen 8-error set. |
| D52 | **api residuals routed (execution ruling, 2026-07-17).** (i) `api/test/…config-truth.test.ts` asserts the presence/content of `api/CLAUDE.md` — a file the owner's totality ruling deleted at W40; the assertion is dead-by-owner-ruling and its removal/retarget rides **W45** (whose gate is the api suite full-green; 208/208 code tests pass today, 1 doc-assert suite fails). (ii) Two pre-existing `noUncheckedIndexedAccess` widenings on guaranteed `/:slug` route params (`crud.ts:100`, `publish.ts:42`; hono 4.12.30) narrowed with `!` — proven pre-existing at base via stash-repro; labeled commits; zero W43-caused tsc errors. |
| D53 | **W43 close census record (execution ruling, 2026-07-17).** (i) Panes disposition finalized 10 distribute + 1 promote + 4 relocate + 1 retire — PaneSlot (shell/) + PaneHeader (shared/ui/) relocated ALIVE: the spec's "retire" rested on stale census (9 live consumers; the router's transition mechanism); `panes/` dir = ∅ regardless. (ii) Item 7 was a NO-OP — ComponentSliders imports no palette barrel (stale RED attribution); the real edge (`ColorInput→proposeColorName`) died in the D-b api-layer split. (iii) The b1 "don't restructure components/" scoping under-cut the census gate; CURED by the B3 lane: D-a (color model → color-session), D-b (palettes/api split: transport→platform, auth→platform, color-naming→color-session), D-c (feature UI trees homed; `demo/@` DELETED; shadcn `ui/` moved byte-identical) — **final census: 0 illegal cross-feature edges**. (iv) FOUR pre-existing feature→shell/app-boot couplings enumerated and routed to **W47** (VIEW_MANAGER_KEY ×2, ViewId type, resolveCalibratedAtmosphere) — shell/app are not features; not census violations. (v) `lru.ts` leak claim RETIRED BY ENUMERATION (3 consumers, all importing down into platform). (vi) Picker feature home = `demo/picker/`; the app root stays `demo/color-picker/` (build-root rename out of wave scope). (vii) The unowned `tsconfig.demo.json` working-tree mod (v4 subpath-map comment update) was PRESERVED to the session scratchpad then superseded by the chain's committed alias-death rewrite of the same file — nothing lost, copy retrievable. |
| D54 | **WL lane DECIDED (2026-07-17) — all 8 covenant rows ruled, verified file:line against the settled tree; letters O-4 (keyframes) + O-5 (atlas) carry full rationale (L2: the letters are the rationale source; this row is the verdict record).** KF §B: decline-the-move — `parseTimingFunction` ALREADY ships on `/css` (grammar.ts:436→css/index.ts:42→subpaths/css.ts:53); `/easing` stays parser-free by architecture. §C `unflattenObject`: DECLINE — concept deleted, zero survivors, no consumer. §D diagnostics: confirm-shipped (`ParseResult` + closed 8-code `ParseIssue` on `/css`; 4.1 latent only if KF names a code). §E `CSSPropertyDescriptor`: confirm — the breaking rename rode 4.0.0; no 5.0 owed. §I D-GAP-6: **DECLINE** with the blessed pattern (`/math` `cubicBezier`+`cubicBezierToString` compose the sampler; SVG emit is consumer glue; re-opens as 4.1 `sampleBezier` only on a hard shared data-sampler need); D-GAP-1/5 ACKed-verified. SCI-1: **SHIP-4.1.x** — the sole ship (real measured consumer ~3,243 marks/frame; into-variants mirroring the blessed `lerpArray` out-buffer idiom; un-dated, execution-gated, evidence tuple owed at the cut — feeds W56's version choice). SCI-2: DECLINE — consumer-clamp blessed as law (`/math` `clamp`; reject-on-∉[0,1] is the honest raw contract). SCI-3: DECLINE — unwrap-at-init blessed; the fallible ctor is the single honest gate; branded twin = speculative armor. Outcome: 1 ship + 7 declines/confirms — parsimony-honest (L4). |
| D55 | **W45 CLOSED GREEN (2026-07-17) — the four executor interpretations RATIFIED.** (i) `versions`/`versions/:hash`/`revert` KEPT as the right-sized immutable-release surface — PALETTE-CONTRACT §1 (the governing honest target) and the wave's own falsifiable evidence outrank the Work-prose "retire" line; only the drifted `/restore` died (both docs agree; item 6's parity needed it). (ii) The generated OpenAPI is deliberately metadata-light (method+path from the mounted registry; security only where path-derivable) — encoding per-route auth would re-introduce the hand table the wave retires. (iii) The PALETTE-CONTRACT §2 cookie transition (`__Host-value-session` replacing `X-Session-Token`) is NON-SURGICAL, spans api+client, and is ROUTED to **W50** (the palette-write/auth journeys wave), which may open an api slice for it; the api-half core landed falsifiably (full-header idempotency replay, `/sessions/me` no-Set-Cookie-on-401). UUIDv7 keys proven positively; non-v7 keys not hard-rejected (7 green conformance tests + the live demo client depend on them — their retirement rides W50's client work). (iv) The B3 api CI job + D48 soft demo-typecheck gate get their live proof VIA THE BRANCH PUSH (owner push authorization), superseding the scratch-branch-red rehearsal the no-push law had deferred. |

## 3. Old V owner forks

| Fork | Decision |
|---|---|
| OF-1 alias-ban idiom | Delete aliases physically in W5/W7. No ESLint-vs-proof contest and no bespoke gate. |
| OF-2 API vocabulary | Keep routes/service/repository inside vertical modules; rebuild the process composition root in W10. |
| OF-3 diagnostics boundary | Full top-level discriminated `ParseResult`; no embedded warning exception. |
| OF-4 placement objective | Ownership-first literal colocation; promote only at two live consumers. |
| OF-5 coupled event | Ordered three-package cut in W17/W33. |
| OF-6 plugins | Source export becomes deliberately authored typed content in W6; fonts become a normal external hashed WOFF2 sheet in W17/W31; both plugins are then deleted. |
| OF-ε1 assets / `@src` | Typed docs/content registry under the demo; no alias and no deep relative imports from Markdown. |
| OF-ε2 panes | Route leaves live with their feature. Only shared pane chassis earns a directory. |
| NG-6 kernel→app type | Hoist the neutral return type to the kernel; no upward edge. |
| δ 500-line ceiling | Review signal only, never CI. Split by responsibility/branch complexity and require net deletion or a stated data/style exemption. |

## 4. Visual bracket decisions B-01…B-32

| ID | Decision | Owner |
|---|---|---|
| B-01 | Sticky header contracts padding, background band, and title by a real semantic token step; no compositor-only title scale. | W19 + BI P109 |
| B-02 | Scene swap has no empty frame, remount flash, or dock collapse; outgoing and incoming share one continuity owner. | W19 |
| B-03 | Sage may appear only as a perceptible ambient whisper, never as card chrome. | W18/W28 |
| B-04 | Fira Code remains the distinct numeric/alpha voice. | W18/W20 |
| B-05 | Picker Blob sits slightly center-ward: W18/W20 move and lock its footprint center from W0's chassis-local coordinate toward the chassis center by exactly one settled P122 title-gap (`±0.5px`) while preserving the header band and outer rect. W17 freezes measured baseline `b₀` while the original `geometry.bodyRadius=0.26` remains provenance; W29's sole effective paint delta sets `geometry.bodyRadius=0.325`. Under the unchanged seed/color/config/settled-frame/footprint protocol, the WebGPU/WebGL2 × DPR1/2 raw-buffer result is diameter ratio `0.66019…0.66042`, centroid error ≤`1.342%`, hull inset exactly `12.0%`, antialias edge ≤`1.885 CSS px` at DPR1 and ≤`0.948 CSS px` at DPR2, with identical alpha-area counts across engines and 0px chassis/seat/footprint movement. No `heroScale`, second radius, public introspection or alias exists; the Blob never reserves title height, grows the footprint/chassis or overlaps readout/rail. | W0/W17–W20/W29 + BI P047 |
| B-06 | First paint is immediately usable on the permanent CSS substrate; eligible renderers queue after critical paint and may not become a later LCP. | W28/W29/W31 |
| B-07 | Alpha readout appears only when the color space/value has meaningful non-unit alpha. | W20 |
| B-08 | W18 transposes the old Picker Card without outer inline/block growth at either reference viewport; W20 locks chassis/footprint/readout/rail and W29 changes only HeroBlob's effective `geometry.bodyRadius`, leaving seat/chassis/footprint CSS and morphology fixed. P122's boundary and block reserve are opt-in, so chassis adoption cannot hide bloat. The Blob workbench remains independently container-scaled. | W17–W20/W29 + BI P047/P122 |
| B-09 | Satellite fission uses a calm 0.16–0.32 progress window and reaches distinct meatball forms rather than spikes. | W29 + BI P047 |
| B-10 | The slider console is visibly a veil on real GPU and remains legible under reduced transparency. | W21 + BI P017/P063 |
| B-11 | Picker's Fraunces color-space label and Fira numeric headline consume one BI P019 paired clamp (`label/headline = 1/√φ` across floor/fluid/ceiling). Their line-box gap is P122 `--instrument-title-gap` `G`, not a Card-local/section token or renderer footprint, and rendered ink satisfies `I_after ≤ min(φG, I_before − G)` within `±0.5 CSS px` at all three arms and 1440/390. | W0/W17/W20/W32 + BI P019/P122 |
| B-12 | Cartoon caster is scheme-true and subordinate; dark mode is not a muddy brown plate fleet. | W18 |
| B-13 | Generate actions use one Dock control set, not a dropdown by default and not three registers. | W25 |
| B-14 | The shared pastel-rainbow `Palettes` identity has exactly two consumers in each scheme: the Dock label `Palettes` and only the `Palettes` substring of Library heading `My Palettes`. `My` stays neutral; every other non-`Palettes` text and every site outside those two consumers has identity-treatment count `0`. W18 owns the one composition, W19 its Dock witness and W22 its Library witness. | W18/W19/W22 |
| B-15 | One container-scaled vertical rhythm law serves desktop and mobile. | W18/W21/W30 |
| B-16 | Body section headings may remain 600; display titles and dropdown options are non-bold. | W18 |
| B-17 | Tracks carry live accent only when that axis encodes color; neutral quantities use neutral tracks. | W21 |
| B-18 | Gradient hatch/netting remains informative but subordinate (`AXIS_HEADROOM` near 1.0). | W27 |
| B-19 | Easing curve stage is centered and proportionate at roughly 19–22rem, container-clamped. | W27 |
| B-20 | Shimmer/skeleton appears only during a real pending operation; empty content never impersonates loading. | W22/W23 |
| B-21 | The global Dock `PaneSegmentedControl` and legacy left/right view-schema state are **RETIRED**; surviving P092 consumers are owner-state, Admin Names state and Mix Colors/Palettes source-mode tabs only. | W17/W19/W23/W24/W26/W30 |
| B-22 | Picker's large numeric headline is read-only output. W21's named numeric fields are the sole direct channel editors; `contenteditable` and duplicate edit paths die. | W20/W21/W30 |
| B-23 | Picker Blob is a noninteractive specimen, not a hidden Copy button. Copy appears exactly once as a named action in the action region. | W20/W29/W30 |
| B-24 | One stable shell `<main>` and H1 persist through route motion; route scenes are landmark-neutral, and one atomic ready-incoming commit simultaneously makes outgoing content inert/AT-hidden and incoming content the sole active subtree. No zero-active or dual-active interval exists. | W19/W30/W32 |
| B-25 | P051 WatercolorDot is a noninteractive face only; its button/tag branch dies across the exact live Dock seal/edit, ColorSpaceSelector, eyedropper, Spectrum, channel, palette, Generate, Mix and Gradient census. Operable sites receive an enclosing semantic seat; ornamental faces are aria-hidden; data-bearing static faces remain present as noninteractive named list/text content with zero activation, focus and drag semantics. This wording does not imply HTML `[inert]`; that attribute remains a separately measured modal/route mechanism. | W17/W19–W22/W25–W27/W30 |
| B-26 | P122 does not auto-divide by region presence. A boundary is explicit/opt-in and every retained divider has a grouping job; consumer CSS never hides one. | W17/W18–W29/W32 |
| B-27 | Color-space selection has one producer semantic checked marker/state visible in monochrome/forced colors and AT. Selected/unselected WatercolorDot face pixels/opacity are invariant; the face never carries a second selection signal. | W20/W30 |
| B-28 | Ordinary Picker values allocate one headline line. The old unconditional Lab two-line/bottom-aligned blank-above reserve dies; a genuinely wrapping tuple adds its second line below and consumes elastic space below the readout inside the fixed chassis. | W20/W21/W32 |
| B-29 | The always-mounted PointerDebugOverlay, public `debug=1` activation and injected Spectrum/Slider logging branches are **RETIRED**, not restyled or hidden behind another production flag. | W20 |
| B-30 | The Picker numeric headline is a domain readout, not a Card heading: `CardTitle`/orphan H3 and implicit live-status behavior die; one non-live labeled output text exposes the current tuple while axis controls announce edits. | W20/W21/W30 |
| B-31 | W0 records the Picker Blob footprint center in chassis-local coordinates. W18/W20 move that footprint toward the chassis center by exactly one settled P122 title-gap (`±0.5px`) and lock it at 1440/390; measuring only within the top-right footprint cannot pass. After W29's final paint change, the settled central-alpha centroid remains within 5% of the locked footprint center on each axis and its hull keeps ≥12% of the shorter footprint dimension from every edge. | W0/W18/W20/W29/W32 |
| B-32 | The member router is a closed eleven-destination inventory: `/`, `/palettes`, `/browse`, `/extract`, `/mix`, `/generate`, `/gradient`, `/easing`, `/atmosphere`, `/blob`, `/about`. About is a quiet trailing route rather than Picker's companion; Easing is a peer route adjacent to Gradient rather than a nested chassis. Each has one direct URL, Dock affordance, title/H1 and focus/scroll identity; no companion/query/redirect alias survives. | W6/W18/W19/W27/W30 |

## 5. U close packet rulings

- The U close is historical evidence, not terminal product truth.
- The owner-attest annex is re-opened as implementation in W18–W32; attestation alone closes nothing.
- Q14 is built through W28/W29/W31, not re-baselined and not carried as a gate.
- CI-teeth and the three armed `test.fail()` rows are retired in W4; ordinary product tests replace only the behavior that matters.
- The BI relay set is reconciled to current P waves in W3 and consumed in W17. “Relay sent” is not a terminal state.
- The 4.0 publish decision is made here and executed only in W33 after staged packed constellation evidence.
