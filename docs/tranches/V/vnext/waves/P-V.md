# P–V Wave Registry

## Shared formation and execution law

Formation and implementation are two distinct stages. Before implementation,
the proposed wave contract is challenged by two fresh hostile Sol formation
critics and a third fresh Sol formation adjudicator under
[`../FORMATION.md`](../FORMATION.md). That formation triad may amend the
contract, but it reviews no implementation state and cannot satisfy the later
implementation challenge.

After formation accepts the contract, every implementation wave follows this
order:

<!-- VNEXT-IMPLEMENTATION-ORDER {"schema":"vnext-implementation-execution-order/1","steps":["born_red","partition","implement","freeze","critic_a_and_critic_b","adjudicator","acceptance_gate_and_downstream_crater","acceptance_receipt","return"]} -->

1. Capture its born-RED witness and input digests.
2. Partition the affected DAG cluster at a size justified by its actual edges.
3. Implement only the formation-adjudicated surface.
4. Freeze the exact final implementation state and evidence offered for acceptance.
   That state remains immutable through challenge, gate, receipt, and return.
5. Run the one compact fresh `critic_a`/`critic_b` plus adjudicator triad
   required by
   [`../RETURN-CONTRACT.md`](../RETURN-CONTRACT.md). The critics independently
   challenge that same immutable final state; the adjudicator starts only after
   both complete.
6. After ratification, run the one canonical acceptance gate and downstream crater
   named by the dependency boundary against that unchanged state. Its receipt
   starts after adjudication and seals the exact implementation-challenge hash.
7. Seal the outer acceptance return without mutating the reviewed and gated state,
   then return the complete packet below.

The accepted state has exactly one final-state triad, not a second proof gate
or a replay of formation critique. A finding reopens its exact owner; repair
invalidates that reviewed state and requires one wholly fresh triad for the new
state, followed by the applicable acceptance gate/crater, before acceptance.

A defect reopens its owning wave. Closure waves audit; they never repair.

## Shared proof and return law

[`../RETURN-CONTRACT.md`](../RETURN-CONTRACT.md) is the only normative return
schema. A band may add fields only below its kebab-case named `annexes` map member and
may not redefine statuses or omit a universal field. P waves additionally
return typed `annexes.parser` with its exact scope class, the P00-validated
published 1.0.0 registry/no-link receipt projection, all four coordinate
domains, parser invocation and route counts, blocker dispositions, and the
byte-canonical external handoff digest. No candidate package branch exists.

Every row freezes `test/proof/<lowercase-wave-id>/manifest.json` before mutation
and exposes only `node .vnext/proof-runner.mjs
test/proof/<lowercase-wave-id>/run.mjs --manifest <path>`. The manifest binds the
born-RED, corpora/oracles, budgets, at-limit/`+1`, crater and expected proof
output. The run emits one strict JCS document plus LF; its generic gate receipt
and proof-output coordinates occur exactly once in the return's artifact union.
Missing, extra, mutable, skipped, linked, fallback or unhashed input/output is
RED. Audit waves precommit falsification; they never invent a synthetic failure.

Every `owner-precut` gate declares its removed surface and sorted governed
aliases; `deletion-judgment.mjs` adds the tombstone name and admits only that
exact scan vocabulary. Omitted aliases/tombstones and owner-added patterns are
hostile nonjoins.

## Parse substrate waves

| ID | Mission | Dependencies | Born-RED witness | Landing and deliverables | Falsifiable gates | Routing / π·DELTA | Exclusions / terminal disposition |
|---|---|---|---|---|---|---|---|
| P00 | Bootstrap the universal return proof, then freeze the exact packed published parse-that 1.0.0 and a fresh coordinated BBNF CSS module topology without waiting for upstream semantic closure. | None | BORN-RED — `tools/validate-return.mjs` is absent, so content-bound repository state, exact challenge-artifact union and gate-receipt/output joins are unproved; the package needs a fresh no-link receipt, the 15-module snapshot can drift, and upstream CSS semantics remain RED and outside Value authority. | First capture the immutable external born-RED/input bytes and pre-mutation manifest required by the universal execution law; then, before any repository mutation, implement and selftest one ≤15,000-byte `vnext-wave-return/3` validator providing the sole coordinate, recursive gitlink-aware repository digest, exact union including each actor's prompt/assignment-route/report, generic gate-output join and recursive dependencies. Only after that validator bootstrap capture the exact `@mkbabb/parse-that@1.0.0` registry receipt, historical BBNF-host capsule, current PT-E snapshot and upstream-authored runtime/non-runtime/edge topology receipt, explicitly structural only. | `proof:p00` first proves budget/selftest and rejects artifact substitution/omission/addition/reorder, same-path split identity, same-status/path dirty-byte mutation including nested gitlinks, copied state digest, challenge task/prompt/assignment-route/report nonjoin, gate-output mismatch, dependency/challenge/annex/π/DELTA union drift and self-hash/I/O failure; it then reproduces one non-link 1.0.0 install, hashes every public byte, runs the historical capsule, validates fresh zero-correction topology and refuses stale, moving, candidate or private-source input. | R0 · N/A(no-visual-claim) | Audit/coordination only after validator bootstrap; no upstream wait, CSS-support claim, BBNF/parse-that source read or mutation, T/U/bank/candidate use, range, republish, parser implementation or Value topology correction. |
| P01 | Build one adoption-free full-CSS prototype: an exact handwritten TypeScript combinator mirror of every runtime BBNF CSS module in P00. | P00, V01, V02 | BORN-ABSENT — the current Value parser is an ad-hoc regex/character implementation; no total parse-that mirror, typed CST/lowering boundary or measured replacement exists. | Exactly one `src/css/grammar/l4/<module>.ts` per P00 runtime BBNF module and an external isomorphic test owner; public parse-that 1.0.0 combinators over the original string; typed lossless CST with UTF-16 spans/recovery, separate DOM-congruent lowering, exact/edit/canonical serializers, full standards/Unicode/pathological/consumer corpus; compact W0 independent-authorship pilot only; historical `164343c1^` signatures/fixtures as an isolated behavior witness, never runtime. | `proof:p01` proves the BBNF→TS filename and AST-edge bijection with zero SCC/correction, public-package-only imports, full consumption, nonvacuous invocation, bounded termination, typed spans/diagnostics/serializers and differential rows; no generated runtime, second scanner/parser, encoded token buffer, historical import or production mutation exists. | R0 · N/A(no-visual-claim) | Prototype only; no production cut, legacy fallback, private/raw parse-that field, workspace link, BBNF semantic invention, stale fixed module count, Tape/SoA/Pratt/new-combinator assumption, formation-scale police, source-colocated test, alias or shim. |
| P02 | Adjudicate necessity/minimality for every prototype defect and route each without creating a Value-owned parse-that patch branch. | P01 | AUDIT — A generic defect report cannot prove unchanged published 1.0.0 is the cause, that a proposed upstream repair is minimal, or that active SK-V25 already owns it. | One P01 RED fixture per candidate; `not-reproduced`, `external-routed` or `blocks-adoption` disposition; minimal counterfactual patch sketch, exact affected paths/symbols and current coordination pin; append-only outbound digest to the active campaign and bank index under parse-that docs. | Every blocker reproduces from the P00 tarball and exact P01 route; no disposition claims local authority; duplicate/overlapping active work folds into its named upstream row; any unresolved blocker is explicitly adoption-blocking. | R0 · N/A(no-visual-claim) | Audit/design only; no parse-that source, package, lock, coordination-history edit, mutation intent, alias, shim or workaround. |
| P03 | Persist the P02-adjudicated research proposals in the uncommitted parse-that bank and prove zero executable ingress to Value. | P02 | BORN-RED — Research can be lost, silently converted into source, or copied from active T/U prototypes. | One proposal packet per routed blocker under `parse-that/docs/tranches/B/`, exact P01/P02 evidence hashes, alternatives and trigger; bank index plus before/after parse-that source/package tree hashes and Value ingress scan. | Every packet is documentation-only, uncommitted, content-addressed and linked to one P02 row; parse-that executable/package/lock bytes are unchanged; Value inputs contain no bank, T/U, Tape/SoA/Pratt or active-prototype path. | R0 · N/A(no-visual-claim) | No source fix, package build, candidate tarball, version bump, acknowledgement request, speculative optimization or claim that banking resolves the blocker. |
| P04 | Freeze the sole parse-that `no-rc` receipt. | P03 | AUDIT — A documentation-only branch can still mint or substitute a package while claiming no release. | Revalidate the exact P00 package receipt, registry integrity, archive projections, no-link install and zero parse-that source/package/lock delta; signed `unchanged-1.0.0-no-republish` receipt. | `validate-parse-that-package-receipt.mjs` passes on the identical P00 bytes; no new tarball, version, tag, source byte, workspace link or alternate digest exists; every P02 blocker is still explicitly routed or blocking. | R0 · N/A(no-visual-claim) | Receipt only; no publication, candidate, mutation, workaround or consumer adaptation. |
| P05 | Benchmark the exact C14/full-CSS harness, the retiring current parser and the historical witness under isolated processes. | P00, P01, P04 | BORN-ABSENT — no content-addressed three-subject record compares parse/lower/serialize behavior, scaling, termination, allocations or retained memory without memo/cache contamination. | Resurrect the method of `164343c1^:bench/css-parse-perf.mjs`; run current v4, historical witness and P01 separately; valid/malformed/Unicode sheets at 1/4/16/64/256 KiB; p50/p95 ns/op, MB/s, fitted scaling exponent, allocation bytes/op, post-GC retention, GC/RSS, output hash and exact Node/V8/OS/CPU/corpus identity; one retiring-regex baseline only. | `proof:p05` binds the unchanged P04 receipt, proves each process loads one parser, clears caches/memoization from throughput cells, requires output-hash correctness and bounded malformed termination, keeps the valid-input scaling exponent's 95% upper bound at most 1.10, and records rather than masks any adoption blocker. | R0 · N/A(no-visual-claim) | No benchmark-driven semantic waiver, dual parser process, cache-hit result mislabeled throughput, microbenchmark conformance claim, local parse-that fix, production adoption or repeated regex baseline. |
| P06 | Run packed direct-consumer, Value full-CSS and traced keyframes-transitive craters on the exact 1.0.0 receipt. | P00, P04, P05 | AUDIT — Consumer tests can hide lockfile drift, private imports, links, zero parser invocation or adaptations. | Exact P04 registry installs, lockfile/`npm ls`/integrity joins, invocation counts, route coverage, runtime/type/bundle results and casualty ledger for every P00 consumer. | All craters resolve the same P04 digest with no file/workspace link; every claimed scenario executes the parser; no adapter, private import, dual parser, version branch, alias, shim or workaround appears. | R0 · N/A(no-visual-claim) | No publication; keyframes is transitive, never an independent parse-that consumer. |
| P07 | Accept the exact unchanged 1.0.0 receipt or block Value adoption and hand that disposition to C10. | P04, P05, P06, V29, K09 | AUDIT — Final Value/keyframes semantics can expose a substrate blocker, let no-RC evade proof, or smuggle active novelty. | Exact accepted receipt/digest or `blocks-adoption`, suitability evidence, casualty/external-route ledgers, clean public-consumer traces, bundle/performance deltas and unconditional C10 no-republish instruction. | Acceptance requires every P02 row `not-reproduced` or `external-routed` with the current 1.0.0 corpus green; any surviving blocker prevents C10. V/K resolve the exact public digest with traced invocation and no global/run leakage. | R0 · N/A(no-visual-claim) | Audit-only; never publishes, repairs, substitutes a candidate, or converts an external blocker into a fallback. |

## Value, CSS, and color waves

| ID | Mission | Dependencies | Born-RED witness | Landing and deliverables | Falsifiable gates | Routing / π·DELTA | Exclusions / terminal disposition |
|---|---|---|---|---|---|---|---|
| V00A | Capture tri-tag, working, packed, API-by-name and consumer truth without choosing a target. | P00 | BORN-RED — v3.1.0, v4.0.0, dirty and packed surfaces disagree; the pre-4.0 capability loss has no total symbol ledger and qualitative DAG claims lack reproducible commands. | `v3.1.0`, `v4.0.0`, working and packed graphs; every historical symbol/family to one terminal restore/tombstone owner; `tagged-source.graph.json`, `working-source.graph.json`, `packed.exports.json`, `api-by-name.json`, `consumer-census.json` with commands/digests; one immutable current-inventory snapshot and capture/replay receipts for V29T. | `proof:v00a` regenerates every graph and the tri-tag symbol join byte-identically, rejects any unrowed historical loss or asserted edge/export without code/package evidence, and proves immutable replay after checkout drift. | R0 · N/A(no-visual-claim) | Read-only baseline; no moves, target counts, inferred worth, ambient replay reads or dirty tree presented as published truth. |
| V00B | Make the dependency strip the first mutation and prove the standing packed boundary. | V00A | BORN-RED — current manifests admit forbidden value→keyframes/Glass/self-dependency residue and the v4.0.0 changelog omits the known capability extinction. | Minimal manifest/import deletions; runtime-dependency allowlist `⊆ {@mkbabb/parse-that}` with exact `1.0.0`; explicit v1.1.0–v2.0.1 self-dependency prior-art refusal; packed graph, bundle crater, casualties, retro-tombstone backfill and exact modified-only deletion annex. | `proof:v00b` shows no runtime/peer/import edge to keyframes, Glass, demos, API or self, exactly one permitted published parse-that dependency when present, unchanged owned behavior, complete tombstones and no invented physical deletion. | R0 · N/A(no-visual-claim) | No topology transpose, forwarding module, adapter, alias, unrelated feature move, private parse-that source or unadjudicated removal. |
| V00C | Freeze ownership, naming, import and test-isomorphism constraints around the ruled seven-key surface. | V00A, V00B | RULING — D-1 terminally rejects the former six-key proposal: the root and seven keys survive, with only `./quantize` demoted. | `target-topology.json`, constraint schema and exact keys `./color`, `./css`, `./easing`, `./math`, `./path`, `./transform`, `./value`; unchanged root; sole `./quantize` tombstone with explicit specifier/symbol effects. | `proof:v00c` rejects SCCs, forbidden directions, repeated enclosing names, source-colocated tests and unsupported edges; proves all seven keys and root survive and only quantize is demoted. | R0 · N/A(no-visual-claim) | Constraint/ruling wave only; no `./value` or root change, cluster move, alias, shim or topology execution before V29T. |
| V01 | Lock the July 2026 standards universe and define the total operation-vector support algebra. | V00A | BORN-RED — Current claims cannot be reconstructed from exact standards artifacts and a scalar terminal state lies about mixed parse/type/refusal behavior. | `standards-lock.json` with CSSWG/Snapshot normative closure, at-risk allowlist, curated Webref and exact package integrities, WPT path/META projection, browser/OS/flags, Node/V8/CPU/OS and canonical hash; typed operation-vector schema. | `proof:v01` reconstructs the manifest without head reads and represents every fixture across parse/type/evaluate/resolve/adapt/exact/edit/canonical cells without ambiguity. | R0 · N/A(no-visual-claim) | Maturity and browser interop are independent axes; parse/preserve never implies type/evaluate; browser majority is not an oracle. |
| V02 | Generate immutable standards facts and total-join them with a separately reviewed support manifest. | V01 | BORN-RED — Hand registries omit types/functions/aliases/relations, collide base and extension rows, or embed adjudicated semantics in generated facts. | `registry-facts.generated.json`, `support-manifest.reviewed.json`, `feature-matrix.json`; composite key `(kind,name,for,source,base-or-extension)` and full at-rule/descriptor/property/selector/type/function/unit/alias/shorthand/WebIDL inventory. | `proof:v02` is reorder-stable and rejects missing, orphan, duplicate or ambiguous join rows; generated facts contain no handwritten evaluator or support verdict. | R0 · N/A(no-visual-claim) | Facts are not semantics. Normative `standards_compat_alias` rows are allowed; project migration aliases are not. |
| V03 | Transpose the adjudicated P01 execution-epoch mirror into the v4 public CSS parser with preprocessing, lossless CST/recovery and three serializers. | P06, V02, V00C | BORN-RED — Frozen v4 signatures/corpora, source maps, recovery and pathological nesting currently fail or disappear; exact mode can pass vacuously by returning root source; the P01 proposal is not yet production. | R-PARSER capability diff/tombstones; byte-identical complete `src/css/grammar/**` mirror and zero-correction DAG from P01; regenerate the parser leaves in `PARSER-CSS-COLOR.md` and the grammar source/test subvectors plus full grammar-snapshot projection in `VALUE-TARGET-PATHS.json` from P00's distinct execution manifest before source mutation; `SourceText{original,processed,boundaryMap}` with total left/right-biased UTF-16 boundaries; the pinned grammar's lexical module(s) are the only spanned lexical owners while every peer continues on the same parse-that string state; token/trivia/recovery interval partition; stable node IDs, conflict-refusing edits, restored corpus and co-scaling benchmark. | The same P01 execution manifest file/internal hash and BBNF commit bind target documents, source/test roots, `validate-css-module-isomorphism.mjs`, `validate-target-paths.mjs --css-manifest test/proof/p00/css-module-isomorphism.execution.json` and `proof:v03`; all prove exact topology and exercise every peer; CRLF/CR/FF/NUL/lone-surrogate/astral/escape/comment/bad string+URL/random edits pass; there is no root passthrough, token re-encoding, regex rescan, lost remainder, stack overflow or ambient DOM; at-limit passes and `+1` returns typed `resource_limit`. | R0 · N/A(no-visual-claim) | No stale formation-vector adoption, local edge correction, second scanner, `TokenStream`/`TokenCursor` layer, UTF-8 default, generated/dual parser, BBNF novelty, semantic coercion or DOM normalization. SVG path and value-definition languages remain distinct grammars. |
| V04 | Implement the complete pinned CSS value-definition language and bounded typed matcher/compiler. | V02, V03 | BORN-RED — Any multiplier/combinator/range/reference/type/function/alias may be handwritten, omitted, ambiguous or exponentially explosive. | Definition CST, matcher IR/compiler, diagnostics, full feature-matrix projection, nullable/permutation work counters and limits. | `proof:v04` compiles every expected definition deterministically, rejects nullable cycles and `+1` work, preserves spans, and fails on handwritten registry semantics. | R0 · N/A(no-visual-claim) | No property-specific generator evaluator, grammar-side hidden fallback or unbounded backtracking. |
| V05 | Implement typed dimensions, units and math with explicit context requirements; retire the static evaluator. | V04 | RULING — unit algebra, non-finites, signed zero and nested math drift, while a library-side evaluator would fabricate DOM/layout resolution that D-17 leaves to the platform. | Numeric AST, dimension algebra, canonicalization, context-requirement types for font/viewport/container/percentage basis, DOM-congruent unresolved values, refusal codes and differential corpus; no general evaluation API. | `proof:v05` proves parse/type/canonical behavior, poisoned-global independence and exact context-requirement propagation; no static evaluator/export/consumer path survives. | R0 · N/A(no-visual-claim) | No implicit or simulated DOM, viewport, font, layout, percentage resolution, ambient global, partial evaluator or hidden fallback. |
| V06 | Implement substitution over an explicitly already-cascaded token-value context. | V03, V04 | BORN-RED — Cycles, fallbacks, token boundaries, registered custom properties, `env()` and typed `attr()` can simulate cascade or recurse without bound. | Custom-property token provider, registrations/attribute/environment capabilities, graph/cycle diagnostics, bounded work and typed refusals. | `proof:v06` covers cycles/fallbacks and poisoned globals; results never depend on selector matching, sheet order, inheritance or ambient DOM; at-limit/`+1` are deterministic. | R0 · N/A(no-visual-claim) | No cascade, inheritance, computed-style lookup, layout, masking fallback or global environment. |
| V07 | Implement selectors, forgiving lists, nesting, namespaces, specificity and matching only through an explicit adapter. | V03, V04 | BORN-RED — Nested pseudos, web-compat selectors, invalid branches and specificity disagree or trigger unbounded backtracking. | Selector CST/types, specificity algebra, nesting model, adapter contract, normative `-webkit-` compatibility fact rows, serializers and work limits. | `proof:v07` passes pinned WPT projections, preserves rejected forgiving branches, enforces required quirks, and returns `resource_limit` at `+1`; poisoned globals stay untouched. | R0 · N/A(no-visual-claim) | No V06 edge, cascade/layout dependency, ambient live DOM, or invented selector substitution. |
| V08 | Implement query syntax and evaluate only pure leaves or caller-supplied capability results. | V05, V07 | BORN-RED — Media/container/supports/style/general-enclosed inputs are misclassified or authoritative booleans are fabricated without UA/layout/style context. | Typed query/condition models, `selector()` reuse, capability IDs, serializers, preserved general-enclosed nodes and refusal reasons. | `proof:v08` differentials parse truthfully; absent capability and poisoned globals yield preserve/delegate/refuse, never invented viewport/container/support results. | R0 · N/A(no-visual-claim) | No layout/style-query engine, live media-list imitation, implicit UA or DOM state. |
| V09 | Implement ordered lossless declarations, rules, stylesheets, layers and nesting with bounded recovery. | V03, V04, V07, V08 | BORN-RED — Duplicates, invalid rules, comments, layers, unknown at-rules and nested blocks reorder/disappear or overflow. | Ordered CST/model, edit operations, recovery nodes, source-order/layer records and nesting limits. | `proof:v09` exact/edit round-trips all pinned malformed sheets, preserves duplicates/trivia, terminates at limits and rejects `+1` with `resource_limit`. | R0 · N/A(no-visual-claim) | No cascade, selector matching, stylesheet loading, computed values or false live `CSSStyleSheet`. |
| V10I | Generate the exact pinned CSSOM WebIDL interface/member/exposure inventory. | V01, V02 | BORN-RED — Prose “WebIDL-shaped” claims omit members, mix dictionaries/interfaces and cannot prove exposure parity. | Hashed IDL facts, inheritance/member/exposure/exception matrix and feature-matrix links. | `proof:v10i` diffs every pinned Webref IDL row and rejects missing, extra, mis-inherited or unowned members. | R0 · N/A(no-visual-claim) | Facts only; no native branding, snapshots, browser objects or runtime implementation. |
| V10S | Implement immutable serializable CSSOM snapshots explicitly distinct from native live objects. | V09, V10I | BORN-RED — Current objects can imply owner nodes, list liveness, mutation, realm identity or native assignability. | Snapshot types, text adapters, ordered immutable models, refusal ledger and negative type/runtime probes. | `proof:v10s` proves no snapshot is branded/assignable as native, reads or mutates a document, claims liveness, or observes realm identity. | R0 · N/A(no-visual-claim) | No fake owner node, live rule list, cascade, layout, constructed-sheet adoption or cross-realm identity. |
| V10D | Implement only explicit live-DOM CSSOM adapters and pinned exception behavior. | V10I, V10S, V26, V27, V28 | BORN-RED — Browser adapters can hide document access, exception drift, unsupported members or snapshot/native conflation. | Capability-gated DOM adapters, realm/ownership requirements, exception mappings, oracle rows and typed refusals. | `proof:v10d` runs exact browser builds and poisoned/absent capabilities; unsupported is evidence, not skip-green; no snapshot becomes native. | R0 · N/A(no-visual-claim) | Adapter only; no polyfilled liveness, fake DOM, swallowed exception or ambient fallback. |
| V11 | Implement only inventoried Typed OM interfaces/members over owned domains, with opaque image snapshots and explicit DOMMatrix/SVG adapters. | V05, V10I, V10S, V13, V21, V22, V24C | BORN-RED — Repo-native values are currently liable to masquerade as native Typed OM/path/motion types or duplicate numeric/transform engines. | Exact wrapper/member matrix, style-map snapshots, opaque image values, DOMMatrix/SVG adapter capabilities, exception/refusal map and differentials. | `proof:v11` rejects invented native interfaces, document-dependent construction and duplicate domain math; native adapters route through V10D and unsupported rows refuse. | R0 · N/A(no-visual-claim) | No second numeric/image/transform/path engine, native branding, live style map, or Typed-OM conformance claim for unstandardized path/motion classes. |
| V12 | Restore the pure color numeric model, all parser-visible spaces, white points, adaptation, conversions and canonical numeric serialization. | V00C, V01 | BORN-RED — `display-p3-linear` and historical spaces/functions lack by-name parity; conversion routes, alpha and out-of-range policies can disagree. | Immutable model/space inventory, one conversion DAG, white-point/adaptation policy, reference vectors and capability diff. | `proof:v12` proves every parser-visible space has one model row, route independence and frozen tolerances; tagged/current/packed semantic losses have tombstones or restored vectors. | R0 · N/A(no-visual-claim) | No CSS grammar, gamut selection, profiles/DOM or blanket allocation claim. This pure domain is not parser-blocked. |
| V13 | Implement stable CSS Color 4 parse/type/lower/serialize over the sole CSS and numeric spines. | V03, V04, V12 | BORN-RED — CSS spellings for implemented spaces, missing components, percentages, angles and calculations fail or silently fall back. | Color 4 grammar adapter, typed AST, lowering, serializers and operation-vector evidence. | `proof:v13` covers the exact Color 4 inventory and author-source recovery; every numeric node uses V05 when evaluation is requested and unsupported context refuses. | R0 · N/A(no-visual-claim) | No parallel tokenizer/parser, relative-color syntax (V14), experimental row labelled stable or browser-majority oracle. |
| V14 | Own and route the exact CSS Color 5/6 syntax inventory with independent maturity and operation cells. | V05, V13 | BORN-RED — Relative colors, `light-dark()` and at-risk/placeholding drafts are rejected, conflated or advertised semantically without implementation. | Relative-color and color-form `light-dark()` types; routing of `color-mix`→V17, profiles/device-CMYK→V19, contrast→V20, image-form `light-dark`→V22; preserved/refused Color 6 placeholders. | `proof:v14` gives every pinned row a truthful vector; at-risk is not stable; placeholders never green type/evaluate; all routed rows have exactly one owner. | R0 · N/A(no-visual-claim) | No invented Color 6 semantics, profile/display context or preserved-syntax-as-typed claim. |
| V15 | Implement DeltaE OK/2000/ITP and the single CSS Color 4 production policy. | V12, V01 | BORN-RED — the current bisection is not the pinned §13 algorithm and a production Ray challenger would violate the one-policy law. | Exact ED pin; Local-MINDE production implementation; Ray Trace test-only exact-boundary oracle; EdgeSeeker `refused-at-evaluate` while its ED algorithm is incomplete; reference/metamorphic/JND evidence; static sole-production-call-path proof. | `proof:v15` passes difference/policy vectors and termination budgets, proves every CSS route uses Local-MINDE, proves Ray is reachable only from external tests, and never treats browser strings as a used-value oracle. | R0 · N/A(no-visual-claim) | No production challenger, implicit policy, universal-MINDE claim, browser-equality oracle, cusp/Halley CSS name, alias or fallback. |
| V15P | Restore and generalize the decided non-CSS Ottosson cusp/Halley kernel. | V12 | RULING — the pre-v4 sRGB kernel is a decided restore; the live tree lacks it and the prior KEEP/PRUNE arm illegally reopened worth. | Restore the historical sRGB kernel; price multi-gamut coefficient generalization as novelty; one explicitly non-CSS API; accuracy/continuity/performance evidence; reusable cusp contract consumed by V18H/V18V. | `proof:v15p` matches the historical and independent reference vectors, closes the frozen error/termination/allocation bounds, proves one kernel serves both OKH clients, and proves no `css-*` production path calls it. | R0 · N/A(no-visual-claim) | No prune arm, consumer-count verdict, CSS-conformance name, duplicated OKH solver, compatibility alias or oracle-only dead code. |
| V16A | Implement caller-owned-output foundational conversion, difference and selected-gamut kernels, inheriting SCI-1/D54. | V12, V15 | BORN-ABSENT — the decided Into family is absent; hot foundational loops allocate/copy or alias unsafely and synthetic benchmarks can be DCE/escape-analysis lies. | Blessed out-buffer shape; `convertColorInto`, difference and `mapColorToGamutInto` kernels; allocating façade separation; alias/overlap contract; named `safeAccentColor` hot-path kernel; exact Node/V8/CPU/OS protocol, observable checksum and optimization/deopt/allocation/GC/source evidence. | `proof:v16a` supports only “no observed V8 heap allocation at named warmed boundary”; allocating twins agree, aliases obey the frozen contract, and any source allocation/profile sample, DCE, deopt or practical regression fails. | R0 · N/A(no-visual-claim) | No parser/DOM/UI allocation claim, SoA, second semantic engine, private buffer ownership or release fork. |
| V16R | Ship the inherited SCI-1/D54/W56 additive value 4.1.x vehicle after its decided payload exists. | V00B, V15P, V16A, V18R | BORN-ABSENT — no 4.1.x vehicle, Into public family, retro-tombstone set or registry receipt exists, although SCI-1 already decided `mixColorsInto`/`toRgba8Into` and the union makes mix/ramp Into unconditional. | Additive `mixColorsInto`, `toRgba8Into`, `convertColorInto`, `mapColorToGamutInto` and `sampleColorRampInto` surface; safe-accent caller-output hot path; exact 4.1.x tarball/integrity, changelog and tri-tag tombstones; Atlas evidence tuple; registry-only downstream receipt; no `sampleBezier` revival. | `proof:v16r` proves every Into twin is output-identical, overlap-safe and has no observed warmed V8 heap allocation at the named boundary; the packed 4.1.x surface is additive over immutable 4.0.0, installs without links, preserves all seven public keys, carries every owed tombstone/evidence row and is the one W56 vehicle. | R0 · N/A(no-visual-claim) | No SoA, parser swap, major transpose, 4.0.0 mutation, candidate/RC, second release vehicle, workspace link, alias, shim or sampleBezier smuggling. |
| V16B | Keep SoA banked against the delivered V16R Into baseline and define its sole measured re-trigger. | V17, V18R, V16R | RULING — Into is already decided and ships in V16R; no named end-to-end workload currently proves that a new channel-fold primitive earns its complexity over those loops. | Immutable bank row; real demo/API batch corpus and Into baseline; re-trigger only when a named workload misses its accepted p95 latency or allocation budget by more than 10% in three independent runs; if triggered, SoA must improve that workload's p95 by at least 20% with a 95% confidence interval excluding 10%, preserve exact outputs, add no warmed allocation and regress no other pinned workload by more than 3%; otherwise it remains banked. | `proof:v16b` validates the V16R receipt, workloads, statistics and terminal bank/trigger record; absence of a trigger performs no prototype spend, and a fired trigger either clears every predeclared bound or retires the candidate completely. | R0 · N/A(no-visual-claim) | No Into re-adjudication, synthetic-only adoption, post-hoc threshold, universal zero-allocation claim, parallel engine, rebooking or revival of the 3.0.0 SoA surface. |
| V17 | Implement one current N-ary color interpolation and `color-mix()` engine. | V14, V16A | BORN-RED — Missing components, omitted/normalized percentages, hue arcs and premultiplication disagree; pairwise left-fold evidence cannot prove N-ary semantics. | Typed N-color expression, shared interpolation engine, canonical serializer and independent normative/reference corpus. | `proof:v17` covers N-color omission/normalization, alpha, hue methods, missing components, cross-space cases and list/work limits; no adapter owns separate math. | R0 · N/A(no-visual-claim) | No API persistence, gradient stop fix-up, unversioned recipe format or pairwise-only conformance claim. |
| V18R | Implement N-stop ramps over an independent scalar reference and the shared interpolation engine. | V17, V16A | BORN-RED — Current ramp paths disagree, allocate internally and leave zero/one/extreme-size behavior undefined. | Sampling contract, scalar oracle, optimized candidate, endpoints/continuity/memory invariants and bounded sizes. | `proof:v18r` compares independently structured implementations, covers zero/one/limit/`+1`, and rejects internal warmed allocations or endpoint drift. | R0 · N/A(no-visual-claim) | No coupled oracle, hidden extrapolation policy, or SoA adoption before V16B. |
| V18H | Restore OKHSL as an explicitly non-CSS client of the single cusp kernel. | V12, V16A, V15P | RULING — the decided OKHSL restore is absent and the former KEEP/PRUNE branch could strand or duplicate its required cusp math. | Historical recipe modernized to the v4 space table; V15P cusp-kernel reuse; independent reference/round-trip vectors; perceptual job, API, allocation and bundle evidence. | `proof:v18h` matches independent Ottosson vectors within the frozen tolerance, closes anchor/round-trip bounds and proves no second cusp/Halley implementation or CSS-conformance name. | R0 · N/A(no-visual-claim) | No prune arm, consumer-count verdict, duplicated solver, compatibility alias or report-only limbo. |
| V18V | Restore OKHSV as an explicitly non-CSS client of the single cusp kernel. | V12, V16A, V15P | RULING — the decided OKHSV restore is absent and the former KEEP/PRUNE branch could strand or duplicate its required cusp math. | Historical recipe modernized to the v4 space table; V15P cusp-kernel reuse; independent reference/round-trip vectors; perceptual job, API, allocation and bundle evidence. | `proof:v18v` matches independent Ottosson vectors within the frozen tolerance, closes anchor/round-trip bounds and proves no second cusp/Halley implementation or CSS-conformance name. | R0 · N/A(no-visual-claim) | No prune arm, consumer-count verdict, duplicated solver, compatibility alias or report-only limbo. |
| V19 | Restore the exact HDR/extended/profile inventory with validated capability delegation. | V13, V14, V16A | RULING — the 11-day HDR drop is unjust: factories survive while ICtCp/Jz/Rec.2100 CSS syntax and extended rows disappear or clamp. | Pinned HDR spaces/functions/limits, arbitrary-N profile descriptors, validated profile/resource/display capabilities, serializers and operation-vector refusals. | `proof:v19` preserves extended range, meets reference tolerances, poisons ambient display/fetch/DOM, and delegates/refuses validation/display absent capability. | R0 · N/A(no-visual-claim) | No hidden clamp, ambient headroom/network, unvalidated ICC acceptance or parse-equals-display claim. |
| V20 | Implement `contrast-color()` with stable WCAG 2.1 policy and experimental APCA. | V14, V16A | RULING — alpha/background assumptions drift and CSS's UA-defined choice is conflated with library metrics; D-18 orders WCAG21 republished and APCA experimental. | Separate CSS syntax/outcome model; named stable WCAG21 policy; APCA behind the shared experimental flag; compositing/candidate capabilities, serializers and typed refusals. | `proof:v20` passes independent metric references and explicit-context variations, proves APCA is unreachable without the flag, records browser disagreement and refuses absent background/candidate rules. | R0 · N/A(no-visual-claim) | No metric called universally mandated CSS, implicit canvas/background, stable APCA claim or universal accessibility threshold. |
| V21 | Implement gradients and stop fix-up with explicit geometry and color-management context. | V05, V13, V16A | BORN-RED — Repeating bounds, positions/hints and interpolation can resolve without line/reference-box/writing-mode/direction or duplicate color semantics. | Gradient types, pure normalized stop algorithm, geometry-context contract, shared interpolation, serializers, visual/numeric corpus and work bounds. | `proof:v21` passes normative fix-up vectors, at-limit/`+1`, poisoned globals and fixed raster/color-management visual differentials; missing geometry/profile context delegates/refuses. | R0 · N/A(no-visual-claim) | No canvas/browser pixels as sole oracle, gradient-local color engine or implicit layout. |
| V22 | Implement the complete pinned `<image>` union with standards aliases and explicit resource/environment capabilities. | V05, V13, V14, V21 | BORN-RED — `image(<color>)`, image-1D/`stripes()`, image-form `light-dark()` and alias/resource states are omitted or counted typed through generic preservation. | URL, `image()`, `image-set()`/`-webkit-image-set()`, cross-fade, element, paint, gradient, image-1D/stripes and light-dark nodes; exact spelling retention, canonical lowering, DPR/type/resource capabilities and limits. | `proof:v22` covers every inventory row; compatibility alias exact/canonical laws; resource-free determinism; poisoned fetch/DOM; typed delegate/refuse for resource/natural-size/layout cases and `+1` bounds. | R0 · N/A(no-visual-claim) | No network/worklet/element capture, second gradient parser, project alias or generic-preserve semantic green. |
| V23 | Implement geometry/basic-shape first, then filter/resource and shadow/color as named subdeliverables. | V05, V06, V13, V24P | BORN-RED — Shapes, lengths, substitution, colors and SVG/resource filters are conflated through image/effect ownership or ambient layout/document access. | Shared geometry/basic-shape types; filter/resource nodes; shadow/color nodes; serializers, composition rules, capability/refusal map and subreceipt hashes. | `proof:v23` proves ordered subdeliverables, shared numeric/color identity, zero image→transform edge, poisoned layout/SVG/DOM, and explicit delegation/refusal for resource/layout cases. | R0 · N/A(no-visual-claim) | No renderer, SVG loader, layout box, duplicate URL/color grammar or hidden shape→image dependency. |
| V24 | Prune `transform/decompose.ts` at the boundary with a complete tombstone. | V00C, V01 | RULING — the four-tree census proves vacuity and D-9 orders PRUNE; the former KEEP bar is superseded. | Exact intrinsic-job record, replacement inapplicability, casualties, governed aliases, public-symbol tombstone, truth receipts and repository-qualified source/type/export/docs effects in one owner-precut annex. | `proof:v24` validates the four-question judgment and the derived surface/aliases/tombstone scan vocabulary, rejects omission or owner-added patterns, and proves zero source/type/export/docs/consumer residue; V29T consumes the exact return. | R0 · N/A(no-visual-claim) | No KEEP arm, consumer-count re-litigation, CSS/Typed-OM/browser substitute, compatibility alias or path-list-only deletion. |
| V24C | Implement CSS transform parse/type/lower/serialize after the ruled V24 decomposition prune, over retained transform/math primitives only. | V03, V04, V05, V24 | RULING — CSS order, units and 2D/3D promotion remain required, while importing or recreating the pruned decomposition helper would smuggle D-9 back into the graph. | CSS transform AST, retained lowering/composition primitives, serializers, numeric/context capabilities, browser differentials and an explicit no-decomposition import contract. | `proof:v24c` first validates V24's PRUNE return, meets fixed matrix tolerances, refuses missing contextual units and proves no decomposition implementation, replacement façade or second matrix engine exists. | R0 · N/A(no-visual-claim) | No `transform/decompose` revival, DOMMatrix branding, live CSSOM, path grammar, motion layout, alias or hidden replacement. |
| V24P | Restore a bounded SVG path-syntax and geometry core with explicit partial errors. | V24 | BORN-RED — Current parser skips garbage, accepts invalid arity/arc flags, leaks NaN and permits explosive recursive subdivision. | `svg/path-syntax`, `geometry/curve`, `geometry/length`; `Result` plus partial error; `PathLimits{maxCodeUnits,maxCommands,maxNodes,maxSubdivisions,maxWork}`; iterative subdivision and independent invariants. | `proof:v24p` makes the four live repros RED→green, forbids NaN/stack overflow/garbage skip, passes limit/`+1` and SVG tolerance vectors, and records work deterministically. | R0 · N/A(no-visual-claim) | No CSS motion, layout/DOM measurement, image/effect import, native geometry branding or second matrix engine. |
| V24M | Implement CSS motion/path composition through an explicit `MotionGeometryContext` after the ruled decomposition prune. | V05, V23, V24, V24C, V24P | BORN-RED — offset paths, rays, reference boxes and orientation can resolve without geometry/layout or can duplicate transform/path primitives; the old plan also named the now-pruned V24 helper as reusable authority. | CSS motion types/serializers, reference-box/coordinate-box/ray/basic-shape/URL capability contract, retained V23 transform and V24P path composition, explicit refusal map and a no-decomposition edge proof. | `proof:v24m` validates V24's PRUNE return, passes pure vectors, poisons DOM/layout/fetch, and delegates/refuses every unresolved geometry/resource case; the static graph proves reuse of V23/V24C/V24P and zero `transform/decompose` residue. | R0 · N/A(no-visual-claim) | No decomposition revival, live measurement fallback, renderer, native geometry façade, alias or second path/matrix engine. |
| V25 | Implement stable Easing 1, experimental Easing 2 `linear()`, and Value-owned experimental `spring()` grammar. | V03, V04 | RULING — discontinuities, steps, extremes and serialization drift; D-16 assigns experimental spring syntax to Value while the solver remains Keyframes-owned. | Typed cubic/steps/linear values; experimental `spring()` syntax node and serializers behind the shared flag; versioned maturity; stable evaluators for standard easing only; sampling/inversion and work limits; typed delegation seam to K10. | `proof:v25` passes boundary-side vectors, invalid/extreme/limit/`+1` and round-trips; proves spring parses/serializes only under the flag, exposes no Value solver and delegates the exact typed parameters to K10. | R0 · N/A(no-visual-claim) | No Value spring solver, stable spring claim, blanket clamp, programmatic-K10 syntax leakage or historical parser fallback. |
| V26 | Implement CSS Animations 1 keyframe/animation syntax and types only. | V09, V25 | BORN-RED — CSS-wide keyframe names and `!important` currently survive; one bad selector can destroy valid siblings; list/order semantics drift. | Keyframe rule/types, shorthand/longhand coordination, ordered declaration linkage, sibling recovery, serializers and selector-count limits. | `proof:v26` turns named live repros green, passes pinned WPT projections, ignores/rejects `!important` per pin, preserves duplicate/order laws, and returns `resource_limit` at `+1`. | R0 · N/A(no-visual-claim) | No runtime timing, playback, DOM animation identity, cascade or second property parser. |
| V27 | Implement current scroll/view timeline, attachment-range and named-range syntax while tombstoning obsolete at-rules. | V05, V09, V26 | BORN-RED — `entry-crossing`/`exit-crossing` fail while obsolete `@scroll-timeline`/`@view-timeline` parse as current. | Current property/range AST, serializers, name-scope/geometry capability matrix, obsolete-syntax tombstones and differentials. | `proof:v27` makes crossing names green and obsolete at-rules typed-invalid; pure syntax round-trips; absent layout/scroll geometry delegates/refuses under poisoned globals. | R0 · N/A(no-visual-claim) | No scrolling geometry engine, stale at-rule compatibility path or live `AnimationTimeline`. |
| V28 | Implement the pinned Animation Triggers 1 syntax inventory with explicit capability delegation/refusal. | V09, V26, V27 | BORN-RED — Draft syntax/actions are stale or rejected wholesale and zero browser support can pass as all-skipped. | Exact 2026-05-21 draft pin, versioned timeline-trigger AST, current action union, event-placeholder preservation/refusal, serializer, browser capability matrix and refusal codes. | `proof:v28` round-trips typed rows, records unsupported browsers as evidence, rejects skip-green, and never infers runtime/event semantics from parse data. | R0 · N/A(no-visual-claim) | No polyfill-as-native, stale once/repeat/alternate/state union or frozen claim across draft revisions. |
| V29 | Materialize the total generated-facts/reviewed-support join and close every feature operation cell. | V02, V04, V05, V06, V07, V08, V09, V10I, V10S, V10D, V11, V12, V13, V14, V15, V15P, V16A, V16B, V17, V18R, V18H, V18V, V19, V20, V21, V22, V23, V24, V24C, V24P, V24M, V25, V26, V27, V28 | BORN-RED — Facts/support rows remain unmapped or documentation can drift from machine truth; generic preservation can masquerade as semantics. | Deterministic feature matrix, evidence links, generated docs, preserved-unknown CST nodes and operation-vector/capability-diff projections. | `proof:v29` rejects any missing/orphan/ambiguous cell, handwritten support in generated facts, manual-doc drift or preserve-only semantic green; all routed facilities have one owner. | R0 · N/A(no-visual-claim) | No omnibus semantics repair, fallback parser, closure-time threshold change or scalar terminal status. |
| V29T | Execute the final accepted module/export/test transpose after all terminal decisions and the 4.1.x vehicle. | V00C, V03, V04, V05, V06, V07, V08, V09, V10I, V10S, V10D, V11, V12, V13, V14, V15, V15P, V16A, V16R, V16B, V17, V18R, V18H, V18V, V19, V20, V21, V22, V23, V24, V24C, V24P, V24M, V25, V26, V27, V28, V29 | BORN-RED — early moves can preserve pruned modules, alter ruled exports or turn sand into unmeasured god modules; target-only lists can drop old paths or invent new ones. | Hashed final source/test/import/export graphs; direct seven-key entries; unchanged root; sole quantize tombstone; `value-public-surface.json` semantic/hash rows; exact topology regenerated from P00's CSS manifest and terminal resolutions; total V00A current→target ledger; moved isomorphic tests; exact owner-precut annex without semantic re-ownership. | `proof:v29t` proves zero conditional paths, exact grammar/test projection, total current coverage, cached owner returns, no invented target, zero runtime SCCs, all seven keys, unchanged root/`./value`, absent `./quantize`, no `internal`/`src/subpaths`/forwarders/aliases/source tests, and a fresh non-link packed probe over exact Git truth. | R0 · N/A(no-visual-claim) | Mechanical transpose only; no behavior repair, silent drop, ownerless target, duplicated judgment, synthetic deletion, key-surface change or compatibility path. |
| V30 | Exhaustively audit standards, operation vectors, semantic API continuity, packed exports and consumers. | V00A, V00B, V00C, V29, V29T | AUDIT — Export-name and sampled coverage can hide weakened semantics, missing rows, workspace imports and transitive casualties. | Total feature-matrix validation, `api-capability-diff.json`, `tombstones.json`, packed tarball/browser bundle, direct+peer+transitive consumer crater and independent adversarial sample. | `proof:v30` checks every row/cell and semantic vector from public packed surfaces; any skip, workspace link, same-name semantic loss or unowned casualty reopens its owner. | R0 · N/A(no-visual-claim) | Audit-only; no source/docs/registry repair, sampled-only close or export-name-only green. |
| V31 | Repeat every precommitted complexity, numeric, allocation, bundle and package bound without repair. | V30, V29T | AUDIT — Deep CSS/path inputs can stack-overflow; late audits can invent/weaken thresholds or microbenchmarks can hide allocation/deopt/DCE. | `limits.json` audit across parser/matcher/substitution/selectors/sheets/mix/gradient/image/path/easing/keyframes; deterministic counters, child-process time/memory and size-doubling slopes; pinned V8 allocation/deopt/GC and packed bundle/export reports. | `proof:v31` passes all at-limit and typed `+1` cases with no hang/stack overflow/OOM, reproduces named allocation claims and exact package boundaries; any defect reopens its owner and resets clean passes. | R0 · N/A(no-visual-claim) | Audit-only; no benchmark patch, threshold relaxation, export shim, hidden optimization or new bound. |
