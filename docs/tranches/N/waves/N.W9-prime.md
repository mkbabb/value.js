# N.W9′ — THE v1.0.0 CLOSE: the registry-pin discharge + the phantom-devDep excision + the π paired before/after + `FINAL.md` (the consummation wave)

- **Phase:** DEV (close) — spec **RATIFIED** (the WAVES-2 second block + the re-sequence
  N.W8/N.W9 → N.W8′/N.W9′ ratified 2026-06-15 — `EXECUTION-ORCHESTRATION.md §0`/`§5`,
  `WAVES-2.md §1`/`§2 DAG`). No longer PLANNED. · **Class:** CLOSE wave — the terminal wave of
  Tranche N; it ships **`v1.0.0`** (the value layer of the constellation reaches 1.0), discharges
  the registry pin W18 set up, drops the phantom keyframes devDep, runs the π paired before/after
  evidence lane across every page N.W10–N.W17 touched, re-confirms L's close on the post-N
  substrate, and authors `FINAL.md` with the authoritative counts.
- **Cut:** **`v1.0.0`** — the FIRST 1.x cut of `@mkbabb/value.js`. It follows `0.13.0`
  (N.W11 color-SOTA + N.W11.D `sampleColorRamp` + N.W11′ scroll grammar) and the BA-cut consume
  (N.W18). It is a SemVer-major (`0.12.0` → `1.0.0`), not a minor — the close declares the surface
  stable, not new (the surface widening was 0.13.0; v1.0.0 stabilizes it).
- **Round:** **R5 (close)** — the LAST round (`EXECUTION-ORCHESTRATION.md §2`). N.W8′ (hygiene +
  master-merge + wire-deploy + doc-truth) precedes it; N.W9′ runs after `{N.W8′, N.W18}` both green
  (`WAVES-2.md §2 DAG:281` — `W9′ ← {W8′, W18}`).
- **Locus:** **MANIFEST + DOCS only** — `package.json` (the glass-ui `file:` → registry pin,
  the phantom `@mkbabb/keyframes.js` devDep deletion, the transitive-peer declaration, the
  `version` bump to `1.0.0`); a `.changeset/*.md` major changeset; the π evidence lane
  (`docs/tranches/N/audit/impl/W9prime-DELTA.md` + per-page DELTAs, the `W6.A` structure
  re-run); `docs/tranches/N/FINAL.md` (NEW — the close record, the `FINAL.md` sibling of
  `docs/tranches/L/FINAL.md`). **NO `src/` edit, NO `test/` edit, NO CI edit** beyond the manifest
  pin (which is the close ceremony, not a source change).
- **DAG-deps:** depends on **N.W18** (the BA-cut consume sweep — it sets the pin TARGET and runs
  the inv-N-10 abrogation sweep against BA's retired set; W9′.A merely DISCHARGES the pin W18
  resolved) AND **N.W8′** (the master-merge + wire-deploy + doc-truth — the tree must be green on
  master and the production wire must serve HEAD-format before the close tag). It depends on
  **N.W10–N.W17** transitively (their π evidence is the input to W9′.B's DELTA lane). It is the
  TERMINAL consumer — no value.js wave consumes it; the constellation's keyframes-K consumes
  `v1.0.0` one tranche behind at K's own close.
- **MEASURE-FIRST:** the π paired before/after lane (W9′.B) IS the measure-first discipline at
  the close: every claim that a design wave fixed something is re-proven by a before/after pair
  (the `W6.A` per-page DELTA structure, `WebGL canvas rect + non-empty-pixel + temporal-motion`
  asserts — `WAVES-2.md §N.W9′ B`). The close does not assert "it works"; it shows the pair.

---

## §Thesis (one sentence)

> **N.W9′ closes Tranche N at `v1.0.0` by DISCHARGING the registry pin N.W18 resolved (glass-ui
> `file:../glass-ui` → the published BA cut, the phantom `@mkbabb/keyframes.js: file:../keyframes.js`
> devDep deleted, transitive peers declared — inv-N-6 / inv-30), re-proving every N.W10–N.W17
> design fix with a π paired before/after DELTA (the `W6.A` structure, WebGL-pixel + temporal
> asserts), re-confirming L's close survives the post-N substrate, and publishing `v1.0.0` +
> `FINAL.md` with the authoritative counts — the value layer of the constellation reaching 1.0, the
> pin a BOOK-with-trigger that holds the tag if the BA cut lags but never blocks the work.**

---

## §Provenance (the binding inputs + the file:line roots this wave discharges)

Each row cites its document and the load-bearing root. inv ε: every claim below is grounded in a
value.js `src`/manifest file:line, an audit-lane §section, or a command+output run TODAY against the
working tree (§State-verified).

- **`N.md §4` N.W9 (the charter wave) — the parent definition.** "**N.W9 — v1.0.0 close + π**
  *(re-sequenced AFTER WAVES-2 — `WAVES-2.md` N.W9′; the pin target moved 3.13.0 → the BA cut per
  `lanes2/X-GU.md §5.2`, discharged by N.W18)* … A: glass-ui `file:` → `^3.13.0` + transitive peers;
  drop the keyframes phantom devDep · B: the π paired before/after lane (DELTA.md per affected page;
  WebGL canvas rect + non-empty-pixel assertion — the precepts edict value.js authored) · C:
  re-confirm L's close on the post-N substrate · D: **v1.0.0** publish; `FINAL.md`" (`N.md:155`). The
  pin target was AMENDED 3.13.0 → the BA cut (`N.md:196-198`, `:253`).
- **`WAVES-2.md §N.W9′` — the re-sequenced close (the ratified second-block re-statement).**
  "**N.W9′ — v1.0.0 close + π (re-sequenced)** — A: the registry pin is **discharged by W18.A**
  (target = the BA cut; if the cut lags, v1.0.0 holds — the pin is the gate, not the work;
  BOOK-with-trigger unchanged in kind); drop the phantom `@mkbabb/keyframes.js` devDep; transitive
  peers · B: the π paired before/after lane (DELTA.md per affected page; WebGL canvas rect +
  non-empty-pixel + the temporal-motion assert) · C: re-confirm L's close on the post-N substrate;
  **v1.0.0** publish; `FINAL.md` (carries the authoritative counts)" (`WAVES-2.md:261-269`). The
  DAG: `W9′ ← {W8′, W18}` (`WAVES-2.md:281`).
- **`EXECUTION-ORCHESTRATION.md §2` R5 (the round-plan) — the ordering authority.** "**R5 (close)** |
  **N.W8′** (hygiene + master-merge + wire-deploy + doc-truth) → **N.W9′** (v1.0.0 + π + FINAL) | the
  wire-deploy ceremony + the v1.0.0 publish | pin discharged by W18" (`EXECUTION-ORCHESTRATION.md:76`).
  §4's cadence diagram places `v1.0.0 (N.W9′, pin = BA cut)` as the terminal value.js publish
  (`:135`), with kf-K consuming it at K's close (`:142-144`).
- **`cross-repo-dev-resolution.md` §2.1 / §4 (invariant 30, contract-v2) — the pin-shape law.**
  Every `@mkbabb/*` `exports["."]` declares the 3-key `{types, import, default}` shape (the
  `development` key STRUCK); a bare `@mkbabb/<pkg>` specifier resolves through the sibling's `exports`
  map to `dist/`. The §2.4 prohibitions forbid "**hoisting a transitive sibling into a grandparent's
  `devDependencies`** … phantom devDeps paper over a resolution failure and hide the real defect"
  (`:163-168`) — the EXACT anti-pattern the `@mkbabb/keyframes.js: file:../keyframes.js` devDep is
  (§State-verified), and the EXACT pattern inv-N-6 / W9′.A excises.
- **`N.md §6` inv-N-6 (the registry-consumption invariant) — the gate this wave verifies.**
  "**inv-N-6 — registry consumption at close.** glass-ui `^3.13.0` (no `file:`), keyframes phantom
  devDep dropped, transitive peers declared (DEC-2 / inv-30). **[AMENDED 2026-06-12: the pin target
  moved 3.13.0 → THE BA CUT (4.0.0) — WAVES-2 §4; discharged at N.W18.A.]**" (`N.md:195-198`).
- **`WAVES-2.md §4` inv-N-6 (the amendment) — the pin-target amendment.** "**inv-N-6** (registry
  consumption): pin target AMENDED 3.13.0 → **the BA cut** (X-GU §5.2); discharged at W18.A, gated
  only on the cut" (`WAVES-2.md:324-325`).
- **`docs/tranches/L/FINAL.md` — the `FINAL.md` shape this wave mirrors.** The §1 Authoritative
  counts table, §2 invariant-verdict table, §3 per-wave landings, the close-gate one-liner, the
  "CLOSED zero-deferral" disposition (`L/FINAL.md:16-129`). N.W9′'s `FINAL.md` is its sibling, scaled
  to N's 18-wave (two-block) shape.
- **The value.js ground-truth roots (the manifest this wave edits + the precedent it honors):**
  - `package.json` — `version: "0.12.0"` (§State-verified); `devDependencies["@mkbabb/glass-ui"] =
    "file:../glass-ui"` (the pin to discharge); `devDependencies["@mkbabb/keyframes.js"] =
    "file:../keyframes.js"` (the phantom to delete); `peerDependencies = {"prettier": "^3.0.0"}`
    (the place the transitive glass-ui peers join); `exports["."] = {types, import, default}` (already
    contract-v2 — `cross-repo-dev-resolution.md §2.1`, verified §State-verified).
  - `vite.config.ts:72-108` — the cross-repo resolution posture (NO `development` condition; the
    `server.fs.allow` widening is FONT-ASSET-only, a legitimate non-sibling-src purpose, NARROWED at
    E.W0 — `:86-107`). W9′ touches NONE of this; it is already contract-v2.
  - `docs/tranches/L/FINAL.md` — the close-record precedent (the most recent value.js tranche close).
  - `docs/tranches/{A..L}/FINAL.md` — the eleven prior closes (the `FINAL.md` is the standing close
    ceremony; N.W9′ writes the twelfth).

---

## §State-verified (the defect/absence proven TODAY by command + output)

Every claim re-verified against the working tree on **2026-06-15** on branch `tranche-f-handoff` at
version **`0.12.0`**. These are the live roots W9′ discharges — born-RED-witnessable TODAY.

**SV-1 — the version is `0.12.0`, NOT `1.0.0` (the close has not happened) — LIVE:**

```sh
$ node -e "console.log(require('./package.json').version)"
0.12.0
```

The package is at `0.12.0` (the N.W7 library-asks cut). `v1.0.0` does not exist. The close clause
(W9′.D) bumps `0.12.0` → `1.0.0` and publishes — born-RED on the version probe TODAY (the registry
carries no `1.0.0`).

**SV-2 — glass-ui is pinned via `file:`, NOT the registry (inv-N-6 VIOLATED at HEAD) — LIVE:**

```sh
$ node -e "const p=require('./package.json'); console.log('devDeps.glass-ui:', p.devDependencies?.['@mkbabb/glass-ui'])"
devDeps.glass-ui: file:../glass-ui
```

glass-ui resolves via a `file:../glass-ui` symlink — a workspace link to the SIBLING'S working tree,
NOT a published-registry semver range. This is precisely what inv-N-6 forbids at close ("glass-ui
`^3.13.0` (no `file:`)" — `N.md:195`, amended to the BA cut). The pin is the close-discharge
(W9′.A). Born-RED: the `file:` link IS the violation today.

**SV-3 — the phantom `@mkbabb/keyframes.js` devDep is PRESENT and UNUSED (the inv-30 §2.4
phantom-devDep anti-pattern, LIVE):**

```sh
$ node -e "const p=require('./package.json'); console.log('devDeps.keyframes:', p.devDependencies?.['@mkbabb/keyframes.js'])"
devDeps.keyframes: file:../keyframes.js
$ grep -rln "@mkbabb/keyframes" src/ demo/ test/
# → 0 (no file imports @mkbabb/keyframes anywhere)
```

`@mkbabb/keyframes.js: file:../keyframes.js` is declared in `devDependencies` but is imported by
NOTHING in `src/`, `demo/`, or `test/` (grep → 0). This is the EXACT
`cross-repo-dev-resolution.md §2.4` phantom-devDep anti-pattern — "**No hoisting a transitive sibling
into a grandparent's `devDependencies`** … phantom devDeps paper over a resolution failure and hide
the real defect" (`:163-168`). It is ALSO a `file:` link to keyframes' WIP tree (the back-edge the
acyclic spine forbids — value.js does NOT consume keyframes; keyframes consumes value.js). W9′.A
DELETES it. Born-RED: the unused `file:` devDep IS the defect today.

**SV-4 — value.js declares NO transitive glass-ui peers (inv-N-6's "transitive peers declared"
half, OPEN at HEAD) — LIVE:**

```sh
$ node -e "const p=require('./package.json'); console.log('peerDependencies:', JSON.stringify(p.peerDependencies))"
peerDependencies: {"prettier":"^3.0.0"}
```

`peerDependencies` carries only `prettier` (the N.W7 dist-eviction peer). inv-N-6 requires
"transitive peers declared (DEC-2 / inv-30)" at close (`N.md:195`). The glass-ui-side transitive
peers (the DEC-2 declaration the registry consumer needs) are NOT yet on the manifest. W9′.A adds
them when the registry pin lands. Born-RED: the peer block is incomplete today.

**SV-5 — the `exports` map is ALREADY contract-v2 (so the close pin needs NO exports change) — LIVE:**

```sh
$ node -e "const p=require('./package.json'); console.log(JSON.stringify(p.exports['.']))"
{"types":"./dist/index.d.ts","import":"./dist/value.js","default":"./dist/value.js"}
$ node -e "const p=require('./package.json'); console.log('build:watch:', p.scripts?.['build:watch'])"
build:watch: vite build --mode production --watch
```

`exports["."]` is the clean 3-key `{types, import, default}` shape — the `development` key absent,
`build:watch` present (`cross-repo-dev-resolution.md §2.1`/`§2.3`, verified). The contract-v2
migration landed at N.W1.C (`vite.config.ts:72-84`); W9′ inherits a clean exports surface and does
NOT re-litigate it. This is the SCOPE NARROWING for W9′.A: the pin is a `dependencies`/`peerDeps`
edit, not an `exports` rewrite.

**SV-6 — the BA cut (glass-ui 4.0.0) is NOT yet resolvable in the installed tree (the pin's
BOOK-with-trigger gate is LIVE) — LIVE:**

```sh
$ node -e "try{console.log(require('@mkbabb/glass-ui/package.json').version)}catch(e){console.log('probe:',e.message.split('\n')[0])}"
probe: Package subpath './package.json' is not defined by "exports" in …/glass-ui/package.json
```

The installed glass-ui does not expose `./package.json` (the exports surface is closed), and the BA
cut (4.0.0) is not yet published to the registry (`EXECUTION-ORCHESTRATION.md §0`: "glass-ui `3.13.0`
published, `tranche/BA` cutting 4.0.0 — LIVE"). The pin TARGET is the BA cut (`WAVES-2.md:324`); the
cut is in flight, not landed. This is exactly why W9′.A is **BOOK-with-trigger**: if the cut lags,
v1.0.0 HOLDS — the pin is the gate, not the work (`WAVES-2.md:263-264`). The close does NOT block on
the producer; it discharges the pin the moment the BA cut publishes (the consume edge, `N.W18.A`).

**SV-7 — `tranche-f-handoff` is 62 commits ahead of master (the W8′ master-merge precondition W9′
depends on transitively) — LIVE:**

```sh
$ git rev-list --count master..tranche-f-handoff
62
```

The close tree lives on `tranche-f-handoff`, 62 commits ahead of `master`. The master-merge is
**N.W8′'s** obligation (`WAVES-2.md §N.W8′`: "Master merge + CI green on master" `:243`), NOT W9′'s —
W9′ depends on W8′ having landed it (the DAG `W9′ ← {W8′, W18}`). W9′ tags `v1.0.0` on the MERGED,
green master. Recorded here so the implementer does not conflate the merge (W8′) with the tag (W9′).

**SV-8 — `FINAL.md` does not exist for N; the prior eleven closes do (the ceremony precedent) — LIVE:**

```sh
$ ls docs/tranches/*/FINAL.md | sed 's#docs/tranches/##;s#/FINAL.md##' | tr '\n' ' '
A B C D E F G H I J L
$ ls docs/tranches/N/FINAL.md 2>&1
ls: docs/tranches/N/FINAL.md: No such file or directory
```

Eleven tranches carry a `FINAL.md` (A–J, L; K is mid-tranche, M was the abandoned head moot at
`N.md:28`). N has NONE. W9′.D authors `docs/tranches/N/FINAL.md` — the twelfth close record, the
`L/FINAL.md` sibling. Born-RED: the file is absent today.

---

## §Goal

Close Tranche N at **`v1.0.0`** — the consummation of the two-block tranche (`N.md` title "the
consummation tranche … v1.0.0"). Concretely: (A) DISCHARGE the registry pin N.W18 resolved —
glass-ui `file:../glass-ui` → the published BA cut, the phantom `@mkbabb/keyframes.js` devDep
DELETED, transitive peers declared (inv-N-6 / inv-30 satisfied at close); (B) re-prove every
N.W10–N.W17 design fix with a π paired before/after DELTA (the `W6.A` per-page structure, the
WebGL-pixel + temporal asserts — the precepts edict value.js authored); (C) re-confirm L's
`api/src` close survives the post-N substrate (the 9 inv-L verdicts still hold); (D) bump
`0.12.0` → `1.0.0`, publish, and author `docs/tranches/N/FINAL.md` with the authoritative counts —
the value layer of the constellation reaching 1.0, the pin a BOOK-with-trigger that never blocks the
work. **The close is zero-deferral** (`N.md §9`: "N targets zero-deferral close; v1.0.0 is the
close") — every ledger item LANDS or is a named befitting-keep / BOOK-with-trigger.

---

## §Scope (the lanes, each at the gestalt seam)

MANIFEST + DOCS only. The lanes mirror `N.md §4` N.W9 A–D + `WAVES-2.md §N.W9′` A–C, scaled to the
two-block close.

| Lane | Work | Anchors (the root edited / the precedent mirrored) |
|---|---|---|
| **A — the registry-pin discharge (inv-N-6 / inv-30)** | (1) glass-ui `file:../glass-ui` → the PUBLISHED BA-cut semver range (the target N.W18.A set; `^4.0.0` or the exact published BA range — W18.A owns the precise number). (2) DELETE the phantom `@mkbabb/keyframes.js: file:../keyframes.js` devDep (unused, §SV-3; the inv-30 §2.4 phantom-devDep + back-edge). (3) DECLARE the transitive glass-ui peers (DEC-2; §SV-4 — the peer block currently `prettier`-only). (4) the `exports` map is UNCHANGED (already contract-v2, §SV-5 — NOT re-litigated). | `package.json` `devDependencies`/`peerDependencies`; `N.md:155,195`; `cross-repo-dev-resolution.md §2.1,§2.4`; the discharge of N.W18.A |
| **B — the π paired before/after lane** | The `W6.A` per-page DELTA structure RE-RUN at close: one `DELTA.md` per affected page (every page N.W10–N.W17 touched — picker, mix, gradient, extract, browse, docs, blob, aurora, dock, shell). Each DELTA carries a **before** (pre-N-block) and **after** (post-N-block) pair with the precepts-edict asserts: **WebGL canvas rect** (the canvas is laid out, non-zero rect), **non-empty-pixel** (the canvas draws — `readPixels` ≠ all-transparent), **temporal-motion** (the loop advances — two frames differ; the inv-N-12 perf-floor + the PRM gate hold). The lane is the measure-first close — it SHOWS each fix, it does not assert it. | `WAVES-2.md:266-267`; the `W6.A`/`N.W6.A` per-pane structure (`N.md:152` "the per-pane Fable design-audit wave"); `docs/tranches/N/audit/impl/W9prime-DELTA.md` (NEW) |
| **C — re-confirm L's close on the post-N substrate** | Re-run the 9 inv-L verdicts (`L/FINAL.md §2`) against the post-N tree: api `as any` = 0, `as unknown as` ≤ 1, no `sessionToken`/4-state `status`, no ad-hoc envelopes, no repo-leaks in routes, no raw `db` outside repositories, no `api/src` file > 350 LoC, no implicit `: any`, no drop-comments. N's `api/src` work (W3 CRUD) ran on top of L; the close confirms L's invariants SURVIVE. (Record any drift as a born-RED handoff — but the expectation is all-9-hold, `N.md §7` befitting-keep "All of L".) | `L/FINAL.md §2` (the 9 inv-L); `N.md:218` ("All of L … 161/161"); the post-N `api/src` tree |
| **D — the v1.0.0 publish + `FINAL.md`** | (1) bump `version` `0.12.0` → `1.0.0` (a SemVer MAJOR — the surface stabilizes, `N.md §1`). (2) the major `.changeset/*.md`. (3) publish to the registry; verify the published tarball resolves (`node -e 'import("@mkbabb/value.js")'`). (4) author `docs/tranches/N/FINAL.md` — the §1 authoritative-counts table, the §2 inv-N (1–12) verdict table, the §3 per-wave landings (W1–W18 + W8′/W9′), the close-gate one-liner, the zero-deferral disposition (the `L/FINAL.md` shape, scaled to N). | `WAVES-2.md:268`; `N.md:155`; `L/FINAL.md` (the shape); `package.json` `version`; `docs/tranches/N/FINAL.md` (NEW) |

**The pin-discharge target table (the exact manifest delta — for the implementer):**

```jsonc
// package.json — the W9′.A delta (the BA-cut range is N.W18.A's resolved number)
{
  "devDependencies": {
    // BEFORE: "@mkbabb/glass-ui": "file:../glass-ui",        ← the file: pin (SV-2)
    // BEFORE: "@mkbabb/keyframes.js": "file:../keyframes.js", ← the phantom (SV-3) — DELETE
  },
  "peerDependencies": {
    "prettier": "^3.0.0",
    // ADD: the DEC-2 transitive glass-ui peers (SV-4)
  },
  "version": "1.0.0"  // BEFORE: "0.12.0" (SV-1)
}
```

The glass-ui pin moves to a REGISTRY range (the BA cut, `^4.0.0` per N.W18.A's resolved number) —
NOT a `file:` link, NOT a vendored copy. value.js consumes the PUBLISHED glass-ui one tranche behind,
born-RED-gated (the acyclic spine, `cross-repo-dev-resolution.md`).

---

## §Hard gate (FALSIFIABLE · born-RED-witnessable on a named defect tree TODAY · P6 posture per clause)

The wave closes against the manifest probes + the π corpus + the inv-N/inv-L re-verification + the
publish. Every clause is born-RED today: the version is `0.12.0`, the `file:` pin is live, the
phantom devDep is present, `FINAL.md` is absent (§State-verified). Each clause greens only on the
close.

| # | Clause | Falsifiable test (the command) | Born-RED today | P6 posture |
|---|---|---|---|---|
| **1** | **No `file:` glass-ui pin (inv-N-6).** glass-ui resolves a published-registry semver range (the BA cut), not `file:../glass-ui`. | `node -e "const d=require('./package.json').devDependencies?.['@mkbabb/glass-ui']??require('./package.json').dependencies?.['@mkbabb/glass-ui']; process.exit(/^file:/.test(d)?1:0)"` → exit 0 | **RED** — `file:../glass-ui` today (SV-2) | a manifest probe, not a narrative claim |
| **2** | **No phantom keyframes devDep (inv-30 §2.4).** `@mkbabb/keyframes.js` appears in NO dependency block. | `node -e "const p=require('./package.json'); const all={...p.dependencies,...p.devDependencies,...p.peerDependencies}; process.exit(all['@mkbabb/keyframes.js']?1:0)"` → exit 0 | **RED** — `file:../keyframes.js` devDep today (SV-3) | a grep/probe artifact |
| **3** | **Transitive peers declared (inv-N-6 / DEC-2).** The glass-ui transitive peers are in `peerDependencies` (the block is no longer `prettier`-only). | `node -e "const p=require('./package.json'); process.exit(Object.keys(p.peerDependencies).length>1?0:1)"` → exit 0 | **RED** — `{prettier}` only today (SV-4) | a manifest probe |
| **4** | **`exports` stays contract-v2 (anti-regression).** The 3-key `{types,import,default}` shape holds; `development` ABSENT; `build:watch` PRESENT. | `node -e "const e=require('./package.json').exports['.']; process.exit((e.types&&e.import&&e.default&&!e.development)?0:1)"` → exit 0; the gate stays green | **GREEN today** (SV-5) — the regression clause: it must STAY green through the pin | the close must not regress the contract |
| **5** | **The version is `1.0.0` & published.** `package.json` `version` is `1.0.0`; the registry serves it; the published tarball imports. | `node -e "process.exit(require('./package.json').version==='1.0.0'?0:1)"` → exit 0; `npm view @mkbabb/value.js version` → `1.0.0`; `node -e 'import("@mkbabb/value.js").then(()=>process.exit(0))'` → 0 | **RED** — `0.12.0` today, no `1.0.0` on the registry (SV-1) | the published-consume artifact, not "API exists" |
| **6** | **The π corpus exists and asserts pixels + motion.** A `DELTA.md` per affected N.W10–W17 page; each carries the WebGL-rect + non-empty-pixel + temporal-motion assert pair (before/after). | `ls docs/tranches/N/audit/impl/*DELTA*.md` → nonempty; each DELTA cites a rect + readPixels-nonempty + two-frame-diff result | **RED** — no W9′ DELTA corpus today | π paired evidence (measure-first), not a textual claim |
| **7** | **L's 9 invariants re-confirmed on the post-N substrate.** The inv-L greps (`L/FINAL.md §2`) re-run clean against the post-N `api/src`; any drift is a named born-RED handoff. | `grep -rn 'as any' api/src \| wc -l` → 0 (inv-L-1); the 8 sibling greps per `L/FINAL.md §2` | the re-run is the close-audit; expectation: all-9-hold (RED only on drift) | independent re-verification, not trust |
| **8** | **`FINAL.md` exists with the authoritative counts.** `docs/tranches/N/FINAL.md` carries the §1 counts, the §2 inv-N (1–12) verdicts, the §3 per-wave landings, the close-gate line, the zero-deferral disposition. | `ls docs/tranches/N/FINAL.md` → present; it cites measured counts (tests, indexes, tarball size, the 12 inv-N verdicts) | **RED** — absent today (SV-8) | a doc artifact with measured numbers, not prose |
| **9** | **The BA-cut BOOK-with-trigger is honored.** IF the BA cut has not published, v1.0.0 HOLDS (the close does not ship a `file:` pin to dodge the wait); the pin lands on the cut, never blocks the work. | the cut is published (SV-6 probe → a 4.x version) before clause 1 greens; else the tag is BOOKED, not forced | **the gate, not the work** — `WAVES-2.md:263-264` | the BOOK-with-trigger discipline: the pin is conditional, the rest of the close is not |

**The headline gate (one line):** glass-ui pinned to the PUBLISHED BA cut (no `file:`) + the phantom
`@mkbabb/keyframes.js` devDep DELETED + transitive peers declared (inv-N-6 / inv-30) + the π
before/after corpus shows pixels-and-motion per affected page + L's 9 invariants re-confirmed +
**`v1.0.0` published & `FINAL.md` written** → **Tranche N is CLOSED, the value layer at 1.0** — the
pin a BOOK-with-trigger that holds the tag if the BA cut lags but never blocks the close work.

---

## §No-workaround (the named forbidden shortcuts for THIS wave)

- **NO shipping a `file:` pin to "close on time."** If the BA cut has not published, v1.0.0 HOLDS —
  the pin is BOOK-with-trigger (`WAVES-2.md:263-264`). Tagging `v1.0.0` with `@mkbabb/glass-ui:
  file:../glass-ui` still on the manifest would publish a non-reproducible package (the consumer
  cannot resolve a sibling's working tree) — the EXACT defect inv-N-6 closes. The close discharges
  the pin or BOOKS the tag; it never forces a `file:` tag.
- **NO vendored glass-ui copy, NO back-edge to keyframes.** value.js consumes the PUBLISHED glass-ui
  one tranche behind (the BA cut, registry semver), born-RED-gated. It NEVER vendors glass-ui's
  source, NEVER `file:`-links keyframes (the deleted phantom is the back-edge — value.js → kf is a
  CYCLE; the spine is glass-ui → value.js → keyframes, acyclic, each one tranche behind —
  `EXECUTION-ORCHESTRATION.md §0`/`§4`, `cross-repo-dev-resolution.md`).
- **NO re-litigating the `exports` map.** It is already contract-v2 (SV-5 — the `development` key
  struck at N.W1.C). W9′ touches `dependencies`/`peerDependencies`/`version` ONLY. Re-shaping the
  exports map at close would be churn on a clean surface (the no-contrivance edict, `N.md §10`).
- **NO π theatre — show the pixels and the motion, not a screenshot caption.** The DELTA lane MUST
  carry the falsifiable asserts (WebGL rect non-zero, `readPixels` non-empty, two-frame temporal
  diff — `WAVES-2.md:266-267`). A before/after image pair with no pixel/motion assertion is the
  "green source-shape gate that misses appearance/interaction/state" blind-spot the standing
  feedback names (`MEMORY.md` Gate blind-spots). The close re-PROVES; it does not re-caption.
- **NO trusting L's close — re-run the greps.** inv-L is re-CONFIRMED by independent re-grep against
  the post-N `api/src` (`L/FINAL.md §2`'s idiom: "verified by a 3-auditor independent close-audit …
  not a committed `proof:*` script"). N's W3 CRUD work touched `api/src`; the close must prove L's 9
  invariants SURVIVE, not assume it. Any drift is a born-RED handoff, not a silent carry.
- **NO `FINAL.md` without measured counts.** The close record carries the authoritative numbers
  (test count, index count, tarball unpackedSize, the 12 inv-N verdicts — each measured at close,
  per `L/FINAL.md §1`'s "measured at close" discipline). A `FINAL.md` of prose without the counts is
  the "derive, don't trust a frozen number" anti-pattern inverted (the counts MUST be re-measured at
  the close commit, not copied from a wave spec).
- **NO deferring the BA-blocked adopts past v1.0.0 as "post-N."** The BA-cut adopts (the `[data-*]`
  interims, the satellite shader, the watercolor ghost) DIE at N.W18, not W9′ — W9′ inherits a tree
  with zero surviving interims (`WAVES-2.md §N.W18` hard gate: "zero interim shims surviving"). If an
  interim survives into W9′, the close is RED — it is W18's defect surfacing, not a W9′ deferral.

---

## §Folds (the rows this wave discharges, each citing its lane + finding-id)

| Fold row | What it discharges | Lane / finding-id |
|---|---|---|
| **F1 — inv-N-6 SATISFIED (the registry pin at close)** | glass-ui `file:../glass-ui` → the published BA cut; no `file:` survives. The amended pin target (3.13.0 → the BA cut) is honored. | `N.md:195-198`; `WAVES-2.md:324-325`; lane A; SV-2 |
| **F2 — the phantom keyframes devDep EXCISED (inv-30 §2.4 + the back-edge)** | `@mkbabb/keyframes.js: file:../keyframes.js` (unused, §SV-3) DELETED — the phantom-devDep anti-pattern AND the value.js → kf back-edge both removed. | `cross-repo-dev-resolution.md:163-168`; `N.md:155,195`; lane A; SV-3 |
| **F3 — transitive peers declared (DEC-2)** | The glass-ui transitive peers join `peerDependencies` (the registry consumer's resolution graph completed). | `N.md:195` ("transitive peers declared (DEC-2 / inv-30)"); lane A; SV-4 |
| **F4 — the π paired before/after lane discharged (the precepts edict)** | Every N.W10–W17 page proven by a before/after DELTA with WebGL-pixel + temporal asserts — the measure-first close value.js authored the edict for. | `WAVES-2.md:266-267`; `N.md:155` (W9.B); the `W6.A` structure; lane B |
| **F5 — L's close re-confirmed on the post-N substrate** | The 9 inv-L verdicts (`L/FINAL.md §2`) re-run clean against the post-N `api/src` — L's legacy-excision survives N's CRUD work. | `N.md:155` (W9.C); `L/FINAL.md §2`; `N.md:218` befitting-keep; lane C |
| **F6 — `v1.0.0` published (the close)** | `0.12.0` → `1.0.0` SemVer major; the registry serves it; the tarball resolves — the value layer of the constellation at 1.0. | `N.md:155` (W9.D); `WAVES-2.md:268`; `EXECUTION-ORCHESTRATION.md:76,135`; lane D; SV-1 |
| **F7 — `docs/tranches/N/FINAL.md` authored (the twelfth close)** | The N close record with the authoritative counts + the 12 inv-N verdicts + the per-wave landings — the `L/FINAL.md` sibling. | `WAVES-2.md:268`; `L/FINAL.md` (the shape); lane D; SV-8 |
| **F8 — the BA-cut BOOK-with-trigger honored** | The pin is conditional on the BA cut publishing; v1.0.0 holds (never a `file:` tag) if the cut lags; the rest of the close is unblocked. | `WAVES-2.md:263-264,293-295`; `N.md:252-256`; SV-6 |

This wave does NOT discharge any U-row from the user audit (U1–U33) — those CLOSE across N.W10–N.W18
(`WAVES-2.md §5` coverage map). W9′ is the CLOSE wave; its only U-adjacent obligation is the π
re-proof (lane B) that every U-fix the design waves landed STILL holds at close, and that the W18
interim-deaths actually happened (a tree with zero surviving interims).

---

## §Hand-off (the BINDING cross-wave + cross-repo boundaries)

**Cross-wave (within value.js N):**

- **N.W18 (BA-cut consume) — the pin-SETTER; W9′ is the pin-DISCHARGER.** N.W18.A MOVES the pin
  target (3.13.0 → the BA cut), runs the FULL inv-N-10 abrogation sweep against BA's retired set, and
  KILLS every `[data-*]` interim (`WAVES-2.md §N.W18` hard gate: "zero interim shims surviving").
  W9′.A merely WRITES the resolved BA-cut semver onto the manifest — the discharge, not the
  resolution. **W9′ inherits from W18:** the exact pin number (the BA cut's published 4.x range), a
  tree with zero surviving interims, the abrogation sweep green. If W18 has not run, W9′ cannot
  discharge the pin (the target is unresolved) — the DAG edge `W9′ ← W18` is hard.
- **N.W8′ (hygiene + master-merge + wire-deploy + doc-truth) — the green-tree provider.** W8′ merges
  `tranche-f-handoff` → master (62 commits, SV-7), fires the wire-deploy ceremony (the production wire
  serves HEAD-format, inv-N-5 completes), and lands the doc-truth (RELEASE.md / CLAUDE.md / DESIGN.md
  per R4's exact lists). **W9′ tags `v1.0.0` on the MERGED, green, deployed master** — it does NOT
  re-do the merge or the deploy. The DAG edge `W9′ ← W8′` is hard: the close tag rides a green master,
  not the handoff branch.
- **N.W10–N.W17 (the design + library body) — the π input.** Every page these waves touched
  contributes a before/after DELTA to W9′.B's corpus. W9′ does NOT re-do the fixes; it re-PROVES them
  with the pixel + motion asserts. The interim-deaths (W18) and the fixes (W10–W17) are the SUBJECT
  of the π lane, not its work.
- **N.W11/N.W11′ (the 0.13.0 library cut) — the surface v1.0.0 stabilizes.** The color-SOTA + ramp +
  scroll-grammar surface shipped at 0.13.0 is part of what v1.0.0 declares stable. W9′ owes them
  nothing (they shipped at 0.13.0); v1.0.0 is the major that says the 0.13.0 surface is 1.0-stable.

**Cross-repo (the acyclic constellation spine — glass-ui one tranche ahead, keyframes one behind):**

- **glass-ui BA cut (4.0.0) — the UPSTREAM publish W9′'s pin consumes.** glass-ui ships the BA cut to
  the registry; value.js N.W18 consumes it (the abrogation sweep + adopt) and N.W9′ DISCHARGES the
  pin (`file:` → the published 4.x range). **The cadence (one beat behind):** glass-ui publishes BA
  → value.js N.W18 consumes it born-RED-gated → value.js N.W9′ tags v1.0.0 with the registry pin.
  **NEVER a `file:` link to glass-ui's WIP tree, NEVER a vendored copy** (`cross-repo-dev-resolution.md`).
  The BOOK-with-trigger: if BA lags, v1.0.0 holds (SV-6); the close does not block the producer, and
  the producer does not block the close's non-pin work.
- **keyframes.js K (the DOWNSTREAM consumer of v1.0.0) — the TERMINAL consume edge.** keyframes-K
  consumes value.js `v1.0.0` at K's OWN close (`EXECUTION-ORCHESTRATION.md §4`: "value.js ships v1.0.0
  (N close) → the constellation's value layer is 1.0; kf-K consumes it at K close"). The 0.13.0
  grammar cut (N.W11′ scroll + N.W11.D ramp) already lit kf-K.W9/K.W10 one beat behind; v1.0.0 is the
  STABILITY signal kf-K pins at its close. **value.js does NOT consume keyframes** (the deleted
  phantom devDep was the only back-edge, F2); the spine stays acyclic: glass-ui → value.js → kf, no
  cycle.
- **fourier-analysis — quiescent (CH-13 BOOK-with-trigger, fourier-owned).** fourier-web's
  `@mkbabb/value.js` bump is fourier-owned (`N.md §8`, `WAVES-2.md §3` BOOK-with-trigger CH-13); v1.0.0
  does not block on it and it does not block v1.0.0.

**The BOOK-with-trigger carries into v1.0.0 (named, zero-deferral):**

- **the BA-cut pin** (W18/W9′ — discharged on the cut publish; holds the tag if it lags, SV-6).
- **CH-10 keyframes precept-pin** (maintainer signal — `N.md §9`, `WAVES-2.md:293`).
- **CH-13 fourier quiescence** (fourier-owned — `WAVES-2.md:293`).
- **X14 SwiftShader harness residual** (renderer artifact; the playwright pin is the mitigation —
  `WAVES-2.md:294`).

Each is a TRIGGER, not a deferral — N closes zero-deferral (`N.md §9`); these re-check at their named
event, none blocks v1.0.0.

---

## §Design-decisions (the trade-offs RESOLVED)

- **D1 — SemVer MAJOR `0.12.0` → `1.0.0`, not a minor (resolved: MAJOR, the surface stabilizes).**
  The 0.x → 1.0 transition is the close declaring the public surface STABLE, not NEW. The new surface
  (color-SOTA + ramp + scroll grammar) shipped at the 0.13.0 MINOR (N.W11/W11′). v1.0.0 adds NO
  capability — it is the stability signal (the constellation's value layer reaching 1.0,
  `EXECUTION-ORCHESTRATION.md §4`). A minor `0.13.0` → `0.14.0` at the close would understate the
  milestone; a major correctly marks the 1.0 line. The bump is `0.12.0` → `1.0.0` (skipping the
  0.13.0 minor in the manifest's final state — 0.13.0 was the intermediate library cut, v1.0.0 is the
  close that supersedes it).
- **D2 — the pin is BOOK-with-trigger, NOT a hard blocker (resolved: hold the tag, never force a
  `file:` close).** The BA cut is in flight (SV-6); if it lags past the close-ready point, v1.0.0
  HOLDS — the pin is the gate, not the work (`WAVES-2.md:263-264`). The alternative — tagging v1.0.0
  with `file:../glass-ui` still on the manifest to "close on time" — publishes a non-reproducible
  package (inv-N-6's exact defect). **Resolution:** the close work (π, L re-confirm, FINAL.md) is
  unblocked and proceeds; the v1.0.0 TAG is conditional on the BA cut publishing. The pin lands on
  the cut, the tag follows. This is the BOOK-with-trigger discipline, not a deferral (N is
  zero-deferral; a trigger is not a defer).
- **D3 — the π lane RE-RUNS the W6.A structure, it does not invent a new one (resolved: reuse the
  standing facility).** N.W6.A authored the per-pane Fable design-audit wave as a "re-runnable
  standing facility" (`N.md:152`). W9′.B re-runs it at close for the before/after DELTAs — not a
  bespoke close-only harness. **Resolution:** the close π lane is the W6.A facility's close
  invocation; the asserts (WebGL rect + non-empty-pixel + temporal-motion) are the precepts edict
  value.js authored (`WAVES-2.md:266-267`), wired into the existing per-page DELTA structure. Reuse,
  not re-invent (KISS — `N.md §10`).
- **D4 — L's close is RE-CONFIRMED by re-grep, not trusted (resolved: independent re-verification).**
  N's W3 touched `api/src` (CRUD right-sizing); L's 9 invariants COULD have drifted. `L/FINAL.md §2`
  set the idiom: "verified by a 3-auditor independent close-audit (greps + suite re-runs … not a
  committed `proof:*` script)." **Resolution:** W9′.C re-runs the 9 inv-L greps against the post-N
  tree; the expectation is all-9-hold (`N.md §7` befitting-keep "All of L"), but the close PROVES it
  rather than assuming it. Any drift is a born-RED handoff into a successor, not a silent carry — the
  measure-first / trust-nothing discipline at the close boundary.
- **D5 — `FINAL.md` carries MEASURED counts at the close commit, not copied wave numbers (resolved:
  re-measure at close).** `L/FINAL.md §1` measured "at close" (161 tests, 26 indexes, 296-LoC largest
  file). The "derive, don't trust a frozen number" edict (`MEMORY.md`/`CLAUDE.md` test-count note)
  applies: the test count, index count, tarball unpackedSize, and the 12 inv-N verdicts are
  RE-MEASURED at the v1.0.0 commit, not copied from the wave specs. **Resolution:** `FINAL.md`'s §1
  counts table is populated by commands run against the close tree (`ls test/*.test.ts | wc -l`,
  `npm pack` unpackedSize, the inv-N greps), exactly as `L/FINAL.md` did — the close record is a
  measurement, not a narrative.
- **D6 — the `exports` map is left UNTOUCHED (resolved: no churn on a clean contract-v2 surface).**
  The exports map is already the 3-key contract-v2 shape (SV-5), migrated at N.W1.C. Re-shaping it at
  the close — even cosmetically — would be churn on a clean surface and risk regressing the
  `proof:resolution` contract (`cross-repo-dev-resolution.md §3`). **Resolution:** W9′ edits ONLY
  `dependencies`/`peerDependencies`/`version`; the exports map is verified-unchanged (gate clause 4,
  the anti-regression). The close stabilizes the surface; it does not re-author it.
