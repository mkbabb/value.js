# LANE R1 — the N execution audit

**Charge**: walk every N commit (`git log 5aa4a8a..HEAD`) against the charter §4 wave rows +
§6 invariants; record what landed / verified / remains (W6 full, W8, W9); spot-verify 6 commit
claims against tree truth; inventory the W6 charter items that DIED un-implemented; list the
glass-ui paired-authorship commits from this arm and their current dist state.

**Substrate at audit**: cwd `/Users/mkbabb/Programming/value.js`, branch `tranche-f-handoff`.
**HEAD is `b112e464`, NOT `199fd15`** — the brief's stated HEAD is one commit stale; the
2026-06-12 user-audit LEDGER commit (`b112e464`, +28 shots) landed on top of `199fd15`. The N
*code* range is `5aa4a8a..199fd15`; `b112e464` is docs-only (the second-fleet input canon). App
live at :9000, HTTP 200, **console-clean on boot** (0 errors/warns — inv-N-1 holds at runtime).

---

## 1 — The N commit walk (`5aa4a8a..HEAD`, newest→oldest)

16 commits. `5aa4a8a` (2026-06-11 18:16) is the ratification/dispatch-gate-CLOSED commit; the
15 above it are the wave execution + the trailing user-audit doc.

| # | SHA | When | Wave | What it claims |
|---|---|---|---|---|
| 1 | `b112e464` | 06-12 12:43 | — (docs) | The 2026-06-12 user audit: 28 shots + 33-finding LEDGER (U1–U33). Docs-only. |
| 2 | `199fd15f` | 06-12 04:14 | **W5 remediation + W1.D closure** (+ a W6.B sliver) | aurora `renderMode('auto')` falls to CSS on software GL (glass-ui `e59987ae` paired; hang was input-starvation); dock boots **expanded** on desktop (W6.B pulled forward; spring-settle premise REFUTED by probe); `force:true` extirpated from specs; **e2e 37/37 ×5 projects, 0 flaky** (inv-N-1 complete). |
| 3 | `3f4f0ed8` | 06-12 02:48 | **W7.C** | release value.js **0.12.0** — the N.W7 library wave; kf witness-flip notify recorded. |
| 4 | `cfb206c4` | 06-12 02:45 | **W2.C** | `parseCSSColor` root-typed at dispatch boundary (`ParsedColorUnit`); 11 demo + 2 lib casts deleted; ONE resolver `cssToRawColor` (inv-N-3); playwright SwiftShader harness; **e2e 2 app defects dispositioned to W5**. |
| 5 | `fc23c8ee` | 06-12 00:13 | **W2.B** | explicit `@source` directives; `css-emission-probe.mjs` blocking in CI (P9 killed for build output; unblocks W6's 14-route gate). |
| 6 | `44979ba2` | 06-11 23:58 | — (docs) | PROGRESS board → live truth. |
| 7 | `ed0dd00f` | 06-11 23:56 | **W7.B-perf** | `lerpArray` KEEP (kf consume-edge refutes demote); B3 endpoint-cache fixes (layout-epoch staleness + LRU bound); predispatch dedup; F7 precedence tested (1709 tests; tarball 287KB). |
| 8 | `b0cb8676` | 06-11 23:56 | **W4.E/F** | vhost → `api.color.babb.dev:8130` self-contained (DEC-9 honesty); `deploy-pages.yml` CI-gated CF-Pages. |
| 9 | `ee458e5e` | 06-11 23:56 | **W5.D** | dock adoptions: `useLayerTransition` fork (108 LoC) → glass-ui spring ODE; 13 raw separator divs → `DockSeparator`; "W5 verified green ×5 lanes with composited-pixel blob proof" (inv-N-4 closed). |
| 10 | `0deca844` | 06-11 23:05 | **W7.B-dist** | prettier evicted (rolldown external + optional peer; 586→283KB); parse-that `^0.9`; CI gate re-pointed at `npm pack` unpackedSize ≤320KB. |
| 11 | `e62567ab` | 06-11 23:05 | **W4.A/B/D** | compose single-node `rs0` (txn P0, docker mutation proof PASSED); `deploy-hook.sh`; `/health` `/docs` `/openapi.json` (inv-22-color; api 219/37). |
| 12 | `e32111c7` | 06-11 23:05 | **W5.A/B/C/E** | blob fork (1270 LoC) → glass-ui goo-blob with live-palette paletteStops; `deriveAurora` wired + AuroraPane rebuilt; watercolor fork + global `#watercolor-filter` extirpated; 5 phantom classes real (inv-N-4, inv-N-7). |
| 13 | `fe3c00c7` | 06-11 19:04 | **W3** | CRUD right-sizing: TOCTOU; txns 18→14; indexes 26→22 + sessions TTL; 30d session contract; fork-count self-heal; cursor-only pagination; PATCH reads 4→2; idempotency body-hash 409; `urn:contract:*` adopted (161→214 tests; inv-N-8). |
| 14 | `9cd815ed` | 06-11 18:58 | **W7.A** | keyframes next-slice: `linear()`/`steps()` parsers, `light-dark()`/`currentColor` sentinels, `toAnimationString` + output-space emit + P3 egress gamut, LRU memoize, path-geometry sampler, diagnostics sink, buffer-reuse unflatten, identity-aware arity pad (+95 tests → 1702; circular-init root-fix). |
| 15 | `d9c3b9f2` | 06-11 18:43 | **W1** | boot-truth: carousel→tablist gestalt + BouncyTabs→SegmentedTabs; mechanism-C remainder (self-alias→dist, `check-types.mjs` retired, reka `^2.9`); CI boot-smoke + abrogation sweep (inv-N-1, inv-N-10). |
| 16 | `e4b5f600` | 06-11 18:16 | **W2.A** | WithId read-side transposition; 26 escape casts → 0; aggregation boundaries typed (inv-N-2). |

**Wave→commit coverage**: W1 `d9c3b9f`+`199fd15`(W1.D closure) · W2.A `e4b5f60` · W2.B `fc23c8e`
· W2.C `cfb206c` · W3 `fe3c00c` · W4 `e62567a`+`b0cb867` (artifacts only — **wire deploy deferred
to W8**) · W5 `e32111c`+`ee458e5`+`199fd15`(remediation) · W7.A `9cd815e` · W7.B `0deca84`+`ed0dd00`
· W7.C `3f4f0ed`. **W6: NO code commit** (only a single W6.B sliver folded into `199fd15`).
**W8/W9: NO commit.**

---

## 2 — Spot-verification of 6 commit claims (tree truth)

All 6 verified TRUE against the tree. Method: `grep`/`ls` against the post-N working tree.

1. **W2.A — 26 WithId casts → 0** (`e4b5f60`): `grep -rE 'as \(?(Palette|ProposedName|Tag|AdminAuditEvent) &' api/src` → **0**. `WithId` flows: 47 uses; `forks.ts:31,168,192` return `WithId<Palette>`/`WithId<Palette>[]`; the parenthetical `forks.ts:206` cast is gone. **VERIFIED** (inv-N-2 holds).
2. **W5.A — blob fork deleted + consume** (`e32111c`): `demo/@/components/custom/goo-blob/`, `watercolor-dot/`, `lib/animation/webgl-utils.ts` all **absent**; `BlobPane.vue:12-13` + `HeroBlob.vue:28` import from `@mkbabb/glass-ui/goo-blob`. **VERIFIED** (inv-N-4 structurally; but see §5 — the *visible* result is refuted by U3/U33).
3. **W1 — boot-break imports** (`d9c3b9f`): no live `GlassCarousel`/`glass-carousel` import (only a comment at `ComponentSliders.vue:385`); no `BouncyTabs`; `SegmentedTabs` from `@mkbabb/glass-ui/tabs` at `MixSourceSelector.vue:4` + `PaneSegmentedControl.vue:18`; `ComponentSliders.vue:10` `role="tablist"`, `:22` `role="tab"`, roving-tabindex at `:202-215`. **VERIFIED**.
4. **W7.B/C — 0.12.0 + prettier eviction + parse-that** (`0deca84`/`3f4f0ed`): `package.json:3` `"version":"0.12.0"`; prettier is now an **optional peerDependency** (`:67-74`) + a devDep `^3.8.3` (the `/format` use); `parse-that` `^0.9.0` (`:65`); CI size gate re-pointed at `npm pack --dry-run` `unpackedSize ≤ 320KB` (`ci.yml:141-149`). **VERIFIED**.
5. **W4 — deploy artifacts** (`e62567a`/`b0cb867`): `compose.yaml:67` `command: ["mongod","--replSet","rs0","--bind_ip_all"]` + `:84` `rs.initiate({_id:"rs0",...})` + `:12` `replicaSet=rs0&directConnection=true`; `scripts/deploy-hook.sh` present (9722 B, +x); `routes/meta.ts:39` `/health`, `:63` `/docs`, `/openapi.json`; `.github/workflows/deploy-pages.yml` present. **VERIFIED** (artifacts; wire deploy is a W8 on-host item, honestly deferred).
6. **W3 — txns 18→14, indexes 26→22, urn:contract, 30d session** (`fe3c00c`): **exactly 14** `services.withTransaction(...)` call sites (cron 1, admin/palettes 1, admin/batch 2, admin/users 4, palette/crud 3, admin/tags 1, palette/versions 1, palette/forks 1 — excluding the type defn `inject-services.ts:68`, the internal `session.withTransaction` `:83`, and the factory `:107`); `createIndex` **= 22**; `urn:contract:*` adopted (`index.ts:103,113`); sessions TTL `expireAfterSeconds:0` (`db.ts:74`); session mint **= 30d** (`services/session/auth.ts:45` `SESSION_TTL_MS = 30*24*60*60*1000`, "the only place this constant lives" → the D2/K2/K3 7d-vs-30d conflict resolved to 30d). **VERIFIED**.

**Zero drift between commit claims and tree truth on the 6 sampled claims.** The claim-vs-truth
divergence is NOT in the code-vs-prose; it is in the **verified-DONE-vs-user-audit** divergence
(§5), which the LEDGER is the authority on.

---

## 3 — What landed / verified / remains (against §4 + §6)

### Landed + structurally verified (W1–W5, W7)
- **W1** DONE (`d9c3b9f`, closed by `199fd15`): typecheck-0-against-dts-complete-dist (precondition
  met at dispatch); boot-smoke; CI boot-smoke + abrogation sweep; **e2e 37/37 ×5, 0 flaky**.
  inv-N-1, inv-N-10 wired.
- **W2.A** DONE (`e4b5f60`): 26→0 (inv-N-2). **W2.B** DONE (`fc23c8e`): `@source` + css-emission-probe.
  **W2.C** DONE (`cfb206c`): one resolver `cssToRawColor` (inv-N-3), parse casts → 0.
- **W3** DONE (`fe3c00c`): all 9 sub-items (A–J) green per the commit; api 161→214 (inv-N-8).
- **W4** ARTIFACTS DONE (`e62567a`+`b0cb867`): rs0/hook/health-docs-openapi/vhost/CF-Pages. The
  **actual prod wire deploy is NOT done** — folded to W8 (the "on-host ceremony"). inv-N-5 is
  therefore **only half-true**: the *committed artifact* can run txns ✓, but the *production wire*
  still serves I-era code (the charter's own §1 deploy-truth P0) until W8 fires.
- **W5** DONE (`e32111c`+`ee458e5`+`199fd15` remediation): fork dirs deleted (inv-N-4), 5 phantom
  classes real (inv-N-7), dock adoptions, blob/aurora/watercolor consumed. **Caveat §5.**
- **W7** DONE (`9cd815e`+`0deca84`+`ed0dd00`+`3f4f0ed`): 11 kf items, perf-truth, prettier evicted,
  0.12.0 cut.

### Remains
- **W6 — fully DEAD** except one W6.B sliver. See §4.
- **W8** — **0 commits**. Branch is still `tranche-f-handoff` (NOT merged to master);
  `CHANGELOG.md`/`CONTRIBUTING.md`/`VENDOR-POLICY.md` deletions staged-but-uncommitted; `$OUT`
  still untracked at root; `docs/precepts` submodule still dirty (` m`); **plus new W6-attempt
  debris** left at root from the aborted baseline run: `.w6a-audit{,2,3,4,5}.mjs`,
  `mix-1440-snapshot.md`, `shot3-dataurl.txt`, `suffuse-dataurl.txt`,
  `docs/tranches/N/audit/impl/shots/w6a-shell-{console,probes}.json` — all must be swept by W8.B.
- **W9** — **0 commits**. glass-ui still `file:../glass-ui` (`package.json:79`); keyframes.js
  phantom devDep still present (`:80`). **NB: glass-ui 3.13.0 is now CUT** (§6) — the one
  cross-repo wait the charter named is *unblocked*; W9's pin is now actionable.

### Invariant ledger at HEAD
| inv | State | Evidence |
|---|---|---|
| inv-N-1 boot-truth | **HOLDS** | live boot console-clean; e2e 37/37; CI boot-smoke wired |
| inv-N-2 WithId | **HOLDS** | 0 casts; WithId flows repo→formatter |
| inv-N-3 one resolver | **HOLDS** | `cssToRawColor` sole path (W2.C) |
| inv-N-4 no demo design-system fork | **HOLDS (structural)** | goo-blob/watercolor/webgl-utils deleted, consumed from glass-ui |
| inv-N-5 deployed-truth | **HALF** | artifact can run txns ✓; **prod wire still I-era** (W8) |
| inv-N-6 registry consumption | **OPEN** | glass-ui `file:`; keyframes phantom devDep present (W9; now unblocked by 3.13.0) |
| inv-N-7 zero phantom classes | **HOLDS** | W5.E sweep green |
| inv-N-8 contract honesty | **HOLDS** | urn:contract adopted; 214 tests |
| **inv-N-9 PRM-complete** | **VIOLATED** | the mix-canvas RAF is STILL un-gated (`useMixingAnimation.ts:77,83,99,189,196` — no `matchMedia`/`prefers-reduced-motion` anywhere); the charter routed this fix to **W6.C**, which died → the one LIVE PRM hole ships in 0.12.0 |
| inv-N-10 abrogation-truth | **HOLDS** | abrogation-sweep + boot-smoke wired (W1) |

---

## 4 — The W6 items that DIED un-implemented (every one must re-fold)

**W6 was prepped, not run.** `docs/tranches/N/audit/impl/` has reports for W1–W5 + W7 but **no
`W6*.md`**. `shots/w6/` holds **61 `-before` baselines** (captured 06-12 04:27–04:35, *after*
`199fd15` at 04:14) and exactly **1 stray `-after`** (`mix-canvas-suffuse-after-reset.png`) — a
baseline run that was aborted. The ONLY W6 substance in the tree is the **W6.B dock-first-paint
sliver** pulled into `199fd15`: `Dock.vue:93` `:start-collapsed="false"` + `:always-expanded`.
Everything else below is DEAD and re-enters the fold (these map 1:1 onto the user LEDGER, which
makes them double-load-bearing):

**W6.A — the per-pane Fable design-audit wave** (the standing facility): the C9-structured
~10-Fable-agent + 1-synthesis re-runnable wave on all 14 routes. **Never run.** → re-fold; this
is the engine the LEDGER's whole-app findings (U1, U32) demand.

**W6.B — suffusion work-orders** (only the dock sliver landed; the rest DEAD):
- audacious-type ramp via one-file `PaneHeader.vue` promotion to display rungs (lifts all 14
  panes) — **DEAD** → maps to **U2/U31** (numbers must be hero-like/large/fluid).
- empty-state CTAs with colorful pops — **DEAD** → **U29** (clipped items need a hover state).
- celebratory motion: the zero-consumer Family-B tokens at save/copy/publish — **DEAD**.
- icon energy (semantic accent tints per view) — **DEAD**.
- glass-tier rebalance (picker = resting frost; the other 13 = flat wash) — **DEAD** → **U1, U32**
  (gray/dark wash; cards overly contrived).

**W6.C — per-pane corpus fixes** (verified STILL OPEN in tree):
- **mix-canvas RAF PRM gate** (`useMixingAnimation.ts:116,206`) — **DEAD; inv-N-9 violated in
  shipped 0.12.0** (the un-gated loop is live at `:77,83,99,189,196`).
- KaTeX `output:"html"` — **DEAD**: `Katex.vue:21` is still `output:"mathml"` (the matrix-mangle).
- docs measure ~68ch + φ ladder — **DEAD** → maps to **U4, U5** (docs spacing/padding/φ).
- easing-curve as gradient pane hero motif + tokenized stroke — **DEAD** → **U25, U27** (easing
  needs hierarchy + a first-class configurator ported from keyframes.js).
- mix: skip/PRM the 2.9s gate, un-withhold the result, preview-honesty (sRGB-vs-OKLab) — **DEAD**.
- gradient stop affordances; picker dual-hero + thumb live-color; 1440px mix clipping — **DEAD**.
- extract: thread `population`/dominance end-to-end + dominant-color hero swatch — **DEAD**.
- collapse `ExtractPane`↔`ImagePaletteExtractor` ~90% duplicate to one shell — **DEAD**.
- delete the mounted-but-`display:none` `EditDrawer` — **DEAD**: still imported/mounted
  (`ColorPicker.vue:33,72`).

**W6.D — modern-web** (verified STILL OPEN in tree):
- router 4→5 + typed routes + `VIEW_MAP` single-source — **DEAD**: `vue-router` still `^4.6.4`
  (`package.json:121`).
- `dispatch.ts` hue-cluster → `mix.ts` (real decomposition) — **DEAD**.
- demo `Palette` id-honesty — **DEAD**.
- delete the `VITE_API_URL` hack (typed degraded-backend) — **DEAD**: still live at
  `demo/@/lib/palette/api/client.ts:29` `import.meta.env.VITE_API_URL ?? DEFAULT_REMOTE_API_URL`.

> **Fold note**: every W6 row above is the *design* engine for the user LEDGER's design findings
> (U1/U2/U4/U5/U25/U27/U29/U31/U32 ride W6). W6's death is *the* gap between "all gates green" and
> the user's "overwhelmingly gray, fonts all wrong, contrived, not modern." W6 must be re-divined
> FIRST among the remaining waves, and the LEDGER findings folded INTO it.

---

## 5 — The load-bearing drift: verified-DONE vs the user audit (W5 visible-result refuted)

The 6 sampled claims have **zero code-vs-prose drift** — the implementations match their commit
messages. The real divergence is **structural-pass vs perceptual-truth**, and the LEDGER
(which OUTRANKS) is the authority:

- **W5.B aurora** claims "aurora visibly palette-derived ✓ (paired screenshots)" and `199fd15`
  added the software-GL CSS fallback. **Live truth (this machine, hardware GL "ANGLE Metal
  Renderer: Apple M5 Max")**: an aurora canvas IS present (1000×1612, `.app-layout` child), so the
  fallback does NOT engage — the *real GL* path runs. Yet **U33: "Background aurora completely
  broken: does not move, no shade variation."** body bg = `rgb(17,15,14)` near-black under a flat
  pink-gray wash (U1's "overwhelmingly GRAY AND DARK"). The W5.B π gate passed on its own
  composited-pixel proof; the user refutes the *result*. This is the single most important R1
  drift: **a wave can be "verified green ×5 lanes" and still ship the broken thing the user sees.**
- **W5.A blob** claims live-palette `paletteStops` coupling ✓. **Live truth**: two
  `goo-blob-canvas` carry `--blob-color: lab(92% 88.80 20 / 100%)` (coupling landed), but the
  rendered blob is white/pale — **U3: "colors FAR TOO WHITE; NO satellite blobs that orbit and
  meatball out."** The structural consume is real; the expressivity/derive is not what the user
  wants. (U3's `uSatColor[]` satellite-color is a glass-ui shader ask per charter §8 — confirmed
  infeasible without the shader change.)
- **U2/U31** (numbers columnar + must be hero-large-fluid), **U13** (component section lost its
  ellipse veil-card), **U14** (channel letters must center with sliders) — all ride the dead W6
  and are *visible right now* in the boot screenshot (`R1-boot-live.png`).

The W6 death is the proximate cause of the entire LEDGER §A–§H design corpus surviving into a
"green" tranche. Evidence: `docs/tranches/N/audit/lanes2/shots/R1-boot-live.png`.

---

## 6 — glass-ui paired-authorship commits from this arm + current dist state

Both `537c7f80` and `e59987ae` are real, on glass-ui **`master`**, and authored from the value.js
N arm:

1. **`537c7f80`** (06-11 18:42) — `fix(styles): segmented-tabs.css joins the styles/ surface`.
   The carve placed `segmented-tabs.css` co-located under `components/` where the dist build never
   ships it, leaving `styles/index.css:148 @import` dangling in every `./styles` consumer.
   **value.js N.W1 boot-smoke caught it** → paired fix from the value.js arm. This is the
   §1-charter "stale local dist CSS fatal" / the glass-ui-CSS-gap that `d9c3b9f`'s PROGRESS line
   names as "unblocked by the paired-authorship fix `glass-ui@537c7f80`."
2. **`e59987ae`** (06-12 03:49) — `fix(aurora): resolveRenderMode auto→css on a SOFTWARE WebGL
   renderer (SwiftShader/llvmpipe/MS-Basic)`. Paired with value.js `199fd15` (the W5 remediation:
   "the hang was input-starvation, not a crash"). **`e59987ae` IS glass-ui's current HEAD.**

**Current glass-ui dist state** (`../glass-ui/dist`, the `file:` symlink value.js consumes — note
`node_modules/@mkbabb/glass-ui → ../../../glass-ui`):
- **Rebuilt fresh 06-12 12:42** (after both paired commits).
- **`537c7f80` present**: `dist/styles/segmented-tabs.css` exists; `dist/styles/index.css:148`
  `@import "./segmented-tabs.css"` now resolves (the dangle is closed).
- **`e59987ae` present**: `dist/aurora.js` contains `resolveRenderMode`, `llvmpipe`, `software`
  (the software-GL fallback is compiled in).
- **glass-ui `package.json` version = `3.13.0`** — the clean-lineage cut the charter's inv-N-6/W9
  pin waits on. **The one cross-repo wait is now satisfied; W9's `file:`→`^3.13.0` pin is
  actionable** (it was uncut at N dispatch per PROGRESS:23/§9).
- **⚠ LIVE RISK — the dist ships ZERO `.d.ts`.** `find ../glass-ui/dist -name '*.d.ts'` → **0**
  (same through the value.js symlink). This is the EXACT dts-less condition that produced the
  original 91-error typecheck (74 TS7016) at N-open. The dist was rebuilt without dts emission, so
  a fresh `npm run typecheck` in value.js would go RED again unless glass-ui's watch re-emits the
  d.ts. This is precisely the charter §8 glass-ui **C-DTS ask** (the dts-emitting `build:watch`)
  and the inv-N-10 "dts-less `any`-typing silencer." The W1 typecheck-0 gate held only against a
  transiently-warm dts-complete dist; **the dist flap is live and re-fold-worthy** — the standing
  dts-emit ask (charter §8) must land in glass-ui's BA tranche before the value.js W9 registry pin,
  or the abrogation sweep (inv-N-10) re-fires on every glass-ui rebuild.

---

## 7 — R1 verdict

- **Code-vs-claim**: all 6 spot-checks TRUE; W1–W5 + W7 landed as the commits describe. No prose
  drift.
- **What remains**: **W6 in full** (only a one-line dock sliver landed — §4 enumerates every dead
  item, each re-folding and each backing a user-LEDGER finding); **W8** entirely (master merge,
  staged-deletion commit, `$OUT`/W6-debris sweep, submodule, doc-truth, *and the real prod wire
  deploy* — inv-N-5's missing half); **W9** entirely (glass-ui pin — now unblocked by 3.13.0 —
  keyframes phantom devDep drop, π, v1.0.0).
- **inv-N-9 is VIOLATED in shipped 0.12.0** (mix-canvas RAF un-gated — the fix died with W6.C).
- **inv-N-5 is HALF** (artifact ✓, prod wire still I-era).
- **The headline drift**: W5 (and the whole "all gates green" board) passed structurally while the
  user audit refutes the *visible* aurora (U33) and blob (U3) and the gray/contrived whole-app
  wash (U1/U32). W6's death is the proximate cause — its per-pane Fable wave was the design engine,
  and it never ran. **W6 must lead the re-divined remaining waves, with the LEDGER folded in.**
- **glass-ui dist**: both paired commits compiled in; 3.13.0 cut (W9 unblocked); **but the dist is
  dts-less right now** — a live, re-fold-worthy risk to the value.js typecheck gate.
