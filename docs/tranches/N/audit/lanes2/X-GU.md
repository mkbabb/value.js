# Lane X-GU — glass-ui BA overlap matrix (the cross-repo substrate for X-GU-ITEMS)

**Mandate.** Read glass-ui's just-authored tranche BA (`../glass-ui/docs/tranches/BA/`, AUTHORED 2026-06-12, pre-greenlight, NO impl) and map EVERY overlap with value.js's N-tranche needs as expressed by the user audit U1–U33 (`LEDGER.md`). Per overlap: **BA covers it** (cite the wave) / **partial** (the delta) / **absent** (a new glass-ui item we must author as an X-GU-ITEM ask). This is the overlap matrix; it does NOT implement, and (per the inv-16 fence) value.js does not write glass-ui — the absent rows become **asks** for the BA roster.

**Method.** Grounded against the 30 BA wave specs (read in full where overlapping) + `BA.md`/`EXECUTION-DAG.md`/`PROGRESS.md` + the glass-ui 3.13.0 `package.json` exports manifest. Live browser grounding of U33/U7/U8 was not possible — a sibling lane holds the exclusive chrome-devtools profile and the brief grants only an isolated context the running profile refuses; the documentary substrate (the wave specs) is the binding evidence for a tranche-development overlap map, and the live aurora/dropdown grounding is the blob/aurora/dropdown design lanes' province.

---

## §0 — The constellation state (the load-bearing facts)

- **glass-ui is at v3.13.0** (`../glass-ui/package.json`), the AZ close cut, published with provenance, `latest` on npm. **BA opens on 3.13.0 and cuts 4.0.0 at W-CLOSE** (hinge H4 DECIDED → 4.0.0: disco retirement + tabs/pager breaks + the A-list removal+re-add are an honest major). This is the version N.W9 must eventually pin (`inv-N-6`: `file:` → registry at the 3.13.0 cut). **The cut N waited on (3.13.0) is now shipped** — but BA introduces a 4.0.0 with breaking changes (disco, tabs, surface-axis, Dialog `variant`→`surface`). **N.W9's pin target must be reconsidered: 3.13.0 is the clean-lineage floor N chartered; BA's 4.0.0 is the surface where most U-findings actually land.** The acyclic spine holds (value.js is the pure sink; glass-ui peers value.js, never the reverse) so the version cross-wait is one-directional and gates only the final pin, not the work.
- **glass-ui 3.13.0 already exports** every primitive N.W5 consumed: `./goo-blob ./watercolor-dot ./aurora ./dock ./tabs ./carousel ./slider ./select ./configurator ./labeled-field ./toggle-chip ./motion`. N.W5 is DONE (`PROGRESS.md`): the fork is deleted, the demo consumes the published superset. **So most U-findings are not "does glass-ui have X" but "does BA's 4.0.0 fix X's quality, and does the demo re-consume after the BA cut."**
- **BA's whole thesis is the user's R8 audit** — the SAME user, the SAME hour-after-close re-open pattern, against the SAME glass system value.js consumes. The U1–U33 ledger and BA's R8/R9/R10 grounding are **two views of one defect surface**: U1 (gray/dark) ≡ BA's DARK-1/NO-GRAY; U6/U16 (dock morph/size) ≡ BA's dock band; U33 (aurora) ≡ BA's STAGE/DARK; U3 (blob) ≡ BA's GOO-REDRESS. This is the single most important finding of this lane: **BA is already authoring the producer-side fix for the bulk of the value.js user audit.** N's job at most U-rows collapses to (a) consume the BA 4.0.0 cut, (b) file the genuinely-absent items as asks.

---

## §1 — The overlap matrix (U-finding → BA disposition)

Legend: **COVERED** (a BA wave fixes the producer-side root) · **PARTIAL** (BA fixes part; a value.js delta or an explicit ask remains) · **ABSENT** (no BA wave; a net-new glass-ui ask — an X-GU-ITEM) · **DEMO** (value.js-side only; glass-ui has no part).

### A — Global / typographic / atmosphere

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U1** | app overwhelmingly GRAY AND DARK; fonts all wrong | **COVERED** (gray/dark) + **DEMO** (fonts) | `W-DARK-MATERIAL` (the dark elevation ladder + transmissive glass + chromatic `--primary`, the 4-L-point page↔card gap is the mechanical root) + `W-NO-GRAY` (the warm-48 ladder resolves achromatic C 0.002–0.016 < the 0.020 floor — the systemic "no gray" root) | **The gray IS a glass-ui token defect, not a demo regression.** BA roots it at the producer (`dark-arm.css:32/51`, `color-radius.css` neutral ladder). The N-side delta: the demo consumes the warm/luminous tokens for free at the 4.0.0 pin (token-first, zero per-site change). The FONT half (U1 "fonts all wrong") is demo-owned (value.js `demo/@/styles/typography.css`) — glass-ui has no part; N.W6 owns it. |
| **U2/U31** | color-space numbers columnar / should be HERO-like, fluid, audacious; card must not resize | **DEMO** | — | No glass-ui part — this is `ComponentSliders.vue` value layout + a `ch`-reservation/tabular-nums display ramp in value.js. N.W6 (suffusion) owns. The glass-ui-adjacent piece: the *audacious-type ramp* idiom BA suffuses in `W-SUFFUSE2` is a demo pattern, not a primitive. |
| **U33** | background aurora completely broken: does not move, no shade variation | **PARTIAL** | `W-STAGE` (the per-route aurora backdrop map + the contained-substrate dark recalibration `StoryHero.vue:88-94`) + `W-DARK-MATERIAL` (the aurora reads THROUGH the transmissive dark glass) | **STAGE fixes WHERE aurora paints and that it READS through dark glass — it does NOT diagnose "does not MOVE / no shade variation."** That symptom (a static CSS fallback painting because the software-GL probe `e59987ae` mis-fires on the real GPU, or `deriveAurora` deep-watch not re-deriving) is a value.js-DEMO consume defect (`AuroraPane.vue`, the `renderMode` consume) AND a possible glass-ui `<Aurora>` renderMode-detection bug. **ASK X-GU-A1**: glass-ui audit the `<Aurora>` software-GL renderMode probe — does it fall to the static CSS arm on a real GPU? If the probe is over-eager, the aurora never animates for EVERY glass-ui consumer (value.js + slides). This is a cohort-grade producer bug the value.js demo cannot fix at the consume edge. |
| **U32** | cards/layouts overly contrived; desktop = two cards side-by-side, clamped for pathological screens | **DEMO** | — | value.js grand-hierarchy/layout wave (N.W6). glass-ui provides the glass cards (W-NO-GRAY/W-SURFACE-AXIS warm them) but the side-by-side two-card desktop layout + the pathological-screen clamp is demo composition. |

### B — The blob (the hero)

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U3** | blob awful: hover broken/janky; colors FAR TOO WHITE (must derive from current color); NO satellites that orbit+metaball; satellites = shades of current color like deriveAurora | **COVERED** (hover + satellites) + **PARTIAL** (per-satellite color) | `W-GOO-REDRESS` — (1) satellite bridge holds across the WORST-CASE orbit envelope so a satellite never detaches as an unrelated disc (the `uSmoothK` band scaled by the `×1.2`/ecc-max excursion, `uploadBlobUniforms.ts:214`); (2) the pointer-wake seam (`watch(pointer.active) → renderer.wake()` in `GooBlob.vue`) so first-hover-after-park repaints same-frame, killing the "quick and jittery" lurch. The CSS chip-easing half of "jittery" is `W-GLASS-CAL` scope 7 (the toggle-chip flat-snap). | **The "colors FAR TOO WHITE / derive from current color / satellites = shades of the current color" is the `uSatColor[]` ask — and BA does NOT cover it.** The satellite metaballing is geometry-only uniforms; per-satellite stop color needs a shader change. N already chartered this as the **`uSatColor[]` glass-ui ask** (`N.md §8`, V4: "per-satellite blob color infeasible without shader change"). **ASK X-GU-B1** (the standing one, now sharpened): the `uSatColor[]` per-satellite shader extension — per-satellite stop-index + smin color-weight threading so satellites paint as derived shades of the live palette (the `deriveAurora`-like satellite tinting U3 demands). BA's W-GOO-REDRESS opens the named smin/orbit envelope seam (`uploadBlobUniforms.ts` + `useBlobSatellites.ts`) but explicitly fence-locks `metaball.frag` — `uSatColor[]` is the in-shader color uniform extension that fence-lock excludes. **This is the one blob ask BA leaves open; it must ride a BA wave (or a 4.x point release) before value.js can derive satellite colors.** The "colors too white" base case (not satellites) is the live-palette coupling N.W5 already landed (`deriveBlobPalette → paletteStops`), so the body derives; only the SATELLITES stay white until `uSatColor[]`. |
| **U30b** | blob bigger; positioned absolutely top-right of the picker card | **DEMO** | — | value.js `HeroBlob.vue` placement + size — a consume-side prop/layout decision. glass-ui `<GooBlob>` already takes geometry config; no producer change. N.W6/N.W5.A. |

### C — Dock + transitions

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U6** | dock animations/transitions take FAR too long to squish/morph; slow, laggy, jittery | **COVERED** | `W-GLASS-CAL` scope 3 (BA.W-GLASS-CAL.3 the **SPRING CLOCK** — the R10 [S1] architectural root: `springLinearStops` discards each spring's response so every CSS consumer rides a generic `--duration-*` clock; the per-spring `--spring-<name>-duration` vocabulary minted from the calibrated `(response, ζ)` table, ~20 sites swept off the generic clock) + `W-DOCK-MORPH-INSITU` (the morph on the one `--dock-morph-t` scalar, §7 perf-budget-decides) | **This is the single highest-leverage cross-repo find for U6.** The "too slow" is NOT a per-animation tweak — it is the `springLinearStops`-discards-response architectural defect that makes EVERY glass-ui spring CSS-consumer drag a dead sub-pixel tail. BA's spring-clock unit fixes it at the producer for all consumers. value.js gets the snappy dock morph for free at the 4.0.0 pin. **Delta**: this lands in Batch 4 (W-GLASS-CAL.3 FIRST within the wave, the §5a edge feeding W-TABS) — value.js cannot get U6's fix until the BA cut ships the re-timed springs. |
| **U16** | dock slow, NOT SIZED PROPERLY between transitions | **COVERED** | `W-DOCK-GEOMETRY` (the control-plate clip cluster: `--dock-control-safe-inset` decouples the plate diameter from the track cell — the plate==cell 0px-slack defect; the scroll-port cross-axis un-clip on fit-content shells) + `W-DOCK-SECTIONS` (the box-inviolate re-seat) | The "not sized properly between transitions" maps to BA's dock-clipping cluster (R8-6 round-button-cutoff, DC-1/DC-2): the control plate exactly fills its track cell with 0px slack, so the hover/active circle slices on the clip edge mid-transition. BA fixes it at the geometry root. **Delta**: value.js consumes the geometry tokens at the 4.0.0 pin; the dock-sizing fix is producer-side, zero demo edit. |
| **U12** | pane + card transitions not smooth; standardize pane/card nomenclature | **PARTIAL** | `W-GLASS-CAL.3` (the spring-clock re-time reaches card/surface lifts — "the card/surface lifts" is named in the ~20-site sweep) | The SPRING smoothness half is COVERED (same spring-clock root as U6). The **PANE transitions + the nomenclature standardization are DEMO** — value.js's `usePaneRouter` pane transitions + a value.js nomenclature doc. glass-ui has no "pane" concept; the card-transition spring is glass-ui's, the pane-transition orchestration is value.js's. N.W6 owns the nomenclature + pane-transition refinement on the re-timed glass-ui springs. |
| **U11** | desktop missing the second right-most pane | **DEMO** | — | value.js pane-router hydration residual (the W1D-closure). No glass-ui part. N.W6. |

### D — Dropdowns / selects (glass-ui core robustness)

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U7** | dropdown items must be the SAME scaled font-size as the select trigger | **COVERED** | `W-MENU-GLASS` (the shared `menuItemVariants` CVA — the canonical CVA composed by 13 menu/picker SFCs INCLUDING `select/SelectItem` — gains the `.glass-menu-row` register with the leading-glyph/label/trailing-glyph anatomy + 44px floor) | `SelectItem` rides the same `menuItemVariants` CVA W-MENU-GLASS re-cuts. **Delta**: the SPECIFIC ask "items = same scaled font-size as the trigger" is a typographic-parity assertion BA's MENU-GLASS does not explicitly name (it names the glass-quiet hover-lift plate + the anatomy, not the font-size-tracks-trigger contract). **ASK X-GU-D1** (sharpen MENU-GLASS or a 4.x rider): the menu-row/select-item font-size derives from / tracks the trigger's scaled font-size token, so the dropdown reads at the trigger's scale, not a hardcoded `text-sm`. Likely a one-token addition to the CVA; small. |
| **U8** | dropdowns must BOUND themselves on the page + scroll within — FIRST-CLASS in glass-ui; study the keyframes.js easing-picker dropdown mechanism | **ABSENT** | — | **No BA wave addresses dropdown collision-detection / page-bounding / scroll-within.** The grep across all 30 BA waves returns ZERO for collision/bound-on-page/scroll-within on the Select/dropdown surface. glass-ui's `Select`/`Popover`/`DropdownMenu` ride reka-ui's collision primitives but the user reports they overflow the viewport rather than bounding+scrolling. **ASK X-GU-D2** (net-new, HIGH priority — U8 is explicitly "FIRST-CLASS in glass-ui"): a glass-ui dropdown/select collision-and-scroll contract — the content plate bounds to the viewport (`avoidCollisions` + a `max-height: var(--reka-*-content-available-height)` + internal scroll, the standard reka-ui pattern the keyframes.js easing-picker reportedly implements correctly). The user names the keyframes.js easing-curve dropdown as the REFERENCE mechanism to port. This is a producer robustness gap BA never identified because the R8 audit did not exercise a viewport-overflowing dropdown. **It must be filed as a BA roster addition (or a 4.x point ask) — value.js cannot fix collision behaviour at the consume edge.** |
| **U23** | dropdown open animation jerks | **PARTIAL** | `W-GLASS-CAL.3` (the spring-clock re-time — "the dots/pager" and surface-lift consumers are swept; a dropdown open-transition riding a `--spring-*` easing on a generic `--duration-*` clock is exactly the jerk class) | The open-animation jerk is plausibly the SAME spring-clock-discards-response root (the transition runs the full generic duration past the spring's 90%-travel point → a dead tail that reads as a jerk at release). BA's spring-clock sweep likely reaches it IF the dropdown open uses a `--spring-*` easing. **Delta / ASK X-GU-D3**: confirm the Select/DropdownMenu open transition is in W-GLASS-CAL's ~20-site sweep census; if it rides a hardcoded duration (not a `--spring-*` easing), it is NOT swept and needs explicit inclusion. A small sharpen to W-GLASS-CAL's census or a 4.x rider. |
| **U30a** | the current color-space dropdown should be more audacious | **PARTIAL / DEMO** | `W-MENU-GLASS` (the glass-quiet register makes selects glassy by default) | The glassy-by-default register is COVERED. The "more AUDACIOUS" is a value.js consume-side variant choice — a demo decision to use a more expressive Select register (a larger/floating/section-tinted trigger). N.W6 owns the audacity; glass-ui supplies the glass register. No net-new primitive needed (the surface-axis + menu-glass register suffice). |

### E — Controls (sliders, pills, skeletons)

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U15** | sliders must be FIRST CLASS in glass-ui | **COVERED (already shipped) + PARTIAL (the "spectrum glass" register)** | glass-ui 3.13.0 already exports `./slider` (`src/components/ui/slider/Slider.vue`, the `.glass-slider` class — the slider IS a first-class glass primitive at HEAD). `W-DARK-MATERIAL` re-anchors the dark `--primary` the Slider range-fill reads (`Slider.vue:200`) off achromatic cream onto a real chroma. `W-GLASS-CAL` fences the slider thumb-halo `blur(2px)` OUT of the dial-back (it rides `--glass-level`). | **The slider primitive EXISTS and is first-class.** U15's "make it first class" is largely already satisfied — but the demo (N.W5) consumes it. **Delta for U20/U28** below: the *spectrum glass* slider (the color-component slider that shows the spectrum behind a glass track) is a value.js-specific composition, not a glass-ui primitive — glass-ui ships the glass track, value.js composes the spectrum behind it. **ASK X-GU-E1** (consider): a glass-ui Slider `surface`/`track-content` slot so the spectrum can ride behind the glass track idiomatically (W-SURFACE-AXIS's Skeleton-over-glass register is the precedent — a translucent glass plate over rich content). Currently low-priority; value.js can compose this demo-side. |
| **U28** | slider too thin | **PARTIAL / DEMO** | `W-GLASS-CAL` does NOT touch slider sizing (fences the thumb halo out) | Slider track thickness is a glass-ui Slider token (`--slider-track-size` or similar) OR a demo override. **ASK X-GU-E2** (small): a glass-ui Slider thickness token so the track reads substantial (the U28 "too thin" — and U20's "spectrum glass" wants a thicker track to show the spectrum). If the thickness is already token-driven, value.js sets it demo-side; if hardcoded, glass-ui must tokenize it. Verify at consume. |
| **U20** | skeletons too BLACK — should be glassy like glass-ui skeletons; slider = a glass slider but SPECTRUM glass | **COVERED (skeleton) + PARTIAL (spectrum slider)** | `W-SURFACE-AXIS` scope 6 — **the Skeleton over-glass register**: `<Skeleton surface="glass">` swaps the opaque `bg-muted` block to a translucent `color-mix(--muted N%, transparent)` via `--skeleton-glass-bg` so the frosted plate reads through; the shimmer composites over the translucent base. | **The "skeletons too black" is EXACTLY the W-SURFACE-AXIS Skeleton-over-glass register** (IG-C2 in BA's grounding — "an opaque `bg-muted` block punches a flat hole in the frosted plate"). BA fixes it at the producer. **Delta**: value.js's skeletons must consume `surface="glass"` at the 4.0.0 pin (the default stays opaque for back-compat — the glass register is opt-in). The "SPECTRUM glass slider" is the E1 ask above. |
| **U14** | channel letters must center EXACTLY with the sliders they affect | **DEMO** | — | value.js `ComponentSliders.vue` alignment. No glass-ui part (glass-ui supplies the slider; the letter-to-slider alignment is the demo's composition + the `W-CONFIG-CHASSIS` width contract precedent if value.js uses the configurator chassis). |
| **U13** | the color-space section used to sit in a hairline dock/veil ELLIPSE card — regression | **PARTIAL / DEMO** | `W-SURFACE-AXIS` (the `veil` register is now library-wide — `<Card surface="veil">`/`<GlassPanel surface="veil">` give the rimless legibility plate) | The VEIL encapsulation U13 wants ("hairline dock/veil ellipse card") is now a first-class glass-ui register (BA un-novelties `veil` from one Card site to the whole content/floating band). **Delta**: value.js re-wraps the color-space section in a `surface="veil"` card at the 4.0.0 pin — DEMO consume, the producer register exists post-BA. |
| **U21** | pills not centered | **DEMO** | — | value.js layout. No glass-ui part. |
| **U29** | items that clip need a hover state | **DEMO** | — | value.js. Adjacent to BA's `W-FADING-SCROLL` (the scroll-state-driven edge fade) if the clip is a scroll-overflow clip — but the hover-state-on-clip is demo composition. |

### F — Cards / shadows / rounding (the depth grammar)

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U17/U24/U26** | palettes + many cards not rounded; shadow FIGHTING; cards = glass cards WITH cartoon shadows; black hairline wrong (should be glassy); shadow too extreme; palette → FIRST-CLASS in-repo component with variants | **PARTIAL / DEMO** | `W-NO-GRAY` (the border family warm re-anchor — the "black hairline" reads warm-ink not concrete) + `W-SURFACE-AXIS` (the shared glass surface axis — cards route through the glass tier, the hairline rides `--glass-border-*`) | The HAIRLINE half ("black hairline wrong, should be glassy/warm") is COVERED by W-NO-GRAY's border re-anchor + the glass tokens. The **SHADOW-FIGHTING + rounding + the PaletteCard-as-a-first-class-component-with-variants are DEMO** — value.js owns the cartoon-shadow language (C9 verified intact: "excellent and one-language") and the PaletteCard componentization (a value.js in-repo component, `N.W6` design wave, the H.W3 god-module precedent). glass-ui supplies the glass card + warm border; value.js composes the cartoon shadow + radii roles + the PaletteCard variants. The shadow-fighting is a z-tier/shadow-layering bug in value.js's composition, not a glass-ui defect. |
| **U18** | the dashed outline = a dashed/GHOST variant of the watercolor dot — abstracted to glass-ui | **ABSENT** | — | **No BA wave addresses a watercolor-dot ghost/dashed variant.** glass-ui ships `./watercolor-dot` (`WatercolorDot.vue` — props: `color`, `animate`, `cycleDuration`, `range`, `seed`; NO `variant`/`ghost`/`dashed`/`outline` prop). The grep across all 30 BA waves returns watercolor ONLY in a passing `W-PROGRESS-GRADIENT` mention — never a ghost/dashed variant. **ASK X-GU-F1** (net-new): a `<WatercolorDot variant="ghost">` (or `outline`/`dashed`) register — a dashed/outlined rendering of the watercolor blob (the empty-slot/add-affordance state value.js's dashed outline represents). The user explicitly says "abstracted to glass-ui." This is a producer primitive extension BA never identified (the R8 audit did not surface the value.js palette dashed-slot). Must be a BA roster addition or a 4.x ask. |
| **U22** | "not a proper watercolor ghost"; UI needs refinement | **ABSENT** | — | Same surface as U18 — the watercolor ghost variant. BA absent. **ASK X-GU-F1** (folds with U18): the ghost variant must read as a PROPER watercolor ghost (the irregular-blob silhouette as an outline/low-alpha fill, not a CSS dashed rectangle). The `useWatercolorBlob` PRNG geometry exists; the ghost variant renders the same geometry as a stroke/low-alpha-fill instead of a solid fill. |

### G — Docs panes

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U4/U5** | no spacing between definition + about-the-color-space; padding inconsistent — GOLDEN-RATIO-backed sectional padding | **DEMO** | — | value.js docs styles + the φ ladder (`Markdown.vue`, docs measure ~68ch + φ ladder — `N.W6.C` already chartered). No glass-ui part. (BA's `W-SUFFUSE2` has a "canonical section rhythm" idiom but it is demo-side suffusion, not a portable primitive.) |

### H — Easing pane

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U25** | easing area needs work; more design hierarchy | **PARTIAL / DEMO** | `W-DEMO-AFFORDANCES` re-conceives the CURVE PICKER (glass chip rack, the contrast-color-immune plate-lift selected register) — but this is **glass-ui's OWN demo**, not a portable primitive | The curve-picker design hierarchy is improved in glass-ui's demo, but value.js cannot consume a "demo improvement." The DESIGN-HIERARCHY half is value.js-DEMO (N.W6). See U27 for the portable-primitive half. |
| **U27** | easing area → a FIRST-CLASS easing selector + configurator, ABSTRACTED FROM keyframes.js INTO glass-ui, supporting the panoply of easing fns, styled like keyframes.js | **ABSENT (cross-repo, two producers)** | — | **Neither BA nor keyframes K authors a portable easing-selector primitive in glass-ui.** BA's curve-picker work (`W-DEMO-AFFORDANCES`) is glass-ui's DEMO-LOCAL chip rack — explicitly fenced NOT to become a `SegmentedTabs` variant or a library component ("the recommended path is a DEMO-LOCAL chip rack"). keyframes K (`docs/tranches/K/`) is keyframes-internal product-truth work — the COLD-axis + TASTE-boundary correction — and consumes glass-ui PUBLISHED (`inv-16`), it does NOT push an easing-picker UP into glass-ui. **So U27 — an easing selector/configurator primitive IN glass-ui, supporting the full easing panoply, styled like keyframes.js — is owned by NO authored wave in either sibling.** **ASK X-GU-H1** (net-new, cross-repo, the biggest authoring gap): a glass-ui `<EasingPicker>`/`<EasingConfigurator>` primitive (or a keyframes.js → glass-ui contribution) — the named easing functions (the 30+ value.js `easing.ts` set + cubic-bezier/steps/linear()) as a selectable+configurable glass control, styled like keyframes.js's curve picker. value.js's `easing.ts` is the FUNCTION library (the math); the PICKER UI is the missing producer primitive. This needs explicit cross-repo authoring (a BA roster addition OR a keyframes contribution OR a new glass-ui wave) — it is the one U-finding that crosses BOTH siblings and is authored by NEITHER. The keyframes.js easing-curve picker (the U8-reference dropdown) is the styling reference; the abstraction target is glass-ui. |

### I — Functional defects (not design)

| U | finding | BA disposition | wave | delta / ask |
|---|---|---|---|---|
| **U9** | resetting the current color DOES NOT WORK | **DEMO** | — | value.js bug (`useColorModel`/the reset handler). No glass-ui part. N.W6. |
| **U10** | color conversion "quantization" awful: LAB → RGB pink "nothing close" | **LIBRARY (value.js itself)** | — | **This is a value.js SRC defect** (`src/units/color/`), not glass-ui and not demo — the highest-severity U-claim. No glass-ui overlap. N owns it in the library (color-space conversion correctness, SOTA refinement). Out of this lane's glass-ui scope but flagged: it is the one U-row that is value.js's OWN library, the pure sink — no producer can fix it for us. |

---

## §2 — The X-GU-ITEMS census (the asks the overlap surfaces)

The matrix yields **eight glass-ui asks** — items BA does NOT cover that value.js needs from the producer. These are the substrate for the X-GU-ITEMS lane. value.js writes NONE of them (`inv-16` foreign-repo fence); they are filed as BA roster additions / 4.x point asks via the cohort coordination channel.

| ask | U-source | what | priority | home |
|---|---|---|---|---|
| **X-GU-A1** | U33 | audit the `<Aurora>` software-GL renderMode probe — does it fall to the static CSS arm on a real GPU? (the "aurora does not move" producer root) | **HIGH** — a cohort-grade bug affecting every consumer; value.js cannot fix at the consume edge | a BA STAGE/aurora rider OR a 3.13.x/4.x point fix |
| **X-GU-B1** | U3 | the `uSatColor[]` per-satellite shader extension — per-satellite stop-index + smin color-weight so satellites paint as derived shades of the live palette | **HIGH** — already N-chartered (`§8`, V4); the ONE blob ask BA leaves open (fence-locks `metaball.frag`) | a BA wave that opens the metaball.frag color-uniform fence, OR a 4.x |
| **X-GU-D2** | U8 | a FIRST-CLASS dropdown/select collision-and-scroll contract — bound-to-viewport + scroll-within (the keyframes.js easing-picker mechanism) | **HIGH** — explicitly "first-class in glass-ui"; a producer robustness gap BA never identified | a BA roster addition (no current wave) |
| **X-GU-H1** | U27 | a glass-ui `<EasingPicker>`/configurator primitive abstracting the easing panoply, styled like keyframes.js | **HIGH** — crosses both siblings, authored by NEITHER | cross-repo authoring (BA addition OR keyframes→glass-ui contribution) |
| **X-GU-F1** | U18/U22 | a `<WatercolorDot variant="ghost">` (dashed/outline) register — the proper watercolor ghost the dashed-slot wants | **MEDIUM** — user says "abstracted to glass-ui"; BA absent | a BA watercolor rider OR a 4.x |
| **X-GU-D1** | U7 | the menu-row/select-item font-size tracks the trigger's scaled font-size token | **MEDIUM** — small CVA addition; W-MENU-GLASS adjacent | sharpen W-MENU-GLASS OR a 4.x rider |
| **X-GU-D3** | U23 | confirm the Select/DropdownMenu open transition is in W-GLASS-CAL's spring-clock sweep (else the jerk survives) | **LOW** — verify-at-consume; likely already swept | a W-GLASS-CAL census confirmation |
| **X-GU-E1/E2** | U15/U20/U28 | a Slider `track-content` slot (spectrum-behind-glass) + a track-thickness token (the "too thin" / "spectrum glass") | **LOW** — value.js can compose E1 demo-side; E2 needs verify | a Slider rider OR demo-side |

---

## §3 — The headline cross-repo findings (for the synthesis lane)

1. **BA is the producer-side fix for the bulk of the value.js user audit.** The SAME user drove BOTH apps; U1/U6/U16/U33/U3/U7/U20/U13 all root in glass-ui tokens/primitives BA is already re-authoring. **At ~16 of the 33 U-findings, N's job collapses to "consume the BA 4.0.0 cut" — not author anything.** The value.js demo-side waves (N.W6) shrink correspondingly.

2. **The spring-clock architectural root (`springLinearStops` discards response) is the single highest-leverage cross-repo find.** It is the producer root of U6 (dock slow), U12 (card transitions), U23 (dropdown jerk), and the whole "animations for springs suck/too slow" class. BA's `W-GLASS-CAL.3` fixes it for ALL consumers. value.js gets snappy springs everywhere for free at the pin.

3. **The version pin is the gate, and it MOVED.** N chartered the pin at glass-ui 3.13.0 (now cut). But BA cuts **4.0.0** with the breaking changes that carry most U-fixes (DARK-MATERIAL, NO-GRAY, SURFACE-AXIS, MENU-GLASS, GLASS-CAL springs, GOO-REDRESS, DOCK band). **N.W9's pin should target the BA 4.0.0 cut, not 3.13.0** — else value.js pins to a glass-ui that still has every U-defect BA fixes. This is a charter-update ask for N: the cross-repo wait is now "BA 4.0.0 cut," not "3.13.0 cut." The acyclic spine (value.js = pure sink) makes the wait one-directional and gate-only.

4. **Four genuinely-absent producer asks BA never identified** (X-GU-A1 aurora-probe, X-GU-D2 dropdown-collision, X-GU-F1 watercolor-ghost, X-GU-H1 easing-picker) plus the standing X-GU-B1 (`uSatColor[]`). These five are the net-new cross-repo authoring the value.js audit surfaces that the glass-ui R8 audit did NOT — because the value.js demo exercises surfaces (a viewport-overflowing dropdown, a dashed palette slot, a derived-satellite blob, an easing pane, a real-GPU aurora) the glass-ui storybook does not. **They must be filed onto the BA roster (or as 4.x point asks) before value.js can fix the corresponding U-findings at the consume edge.**

5. **U10 (LAB→RGB quantization) and the font/layout/pane/docs/PaletteCard rows are value.js's OWN** — no producer overlap. The pure sink's color-conversion correctness (U10) is the one U-finding no glass-ui cut can touch; it is value.js library work.

---

## §4 — Provenance + scope honesty

- Read in full: `BA.md`, `EXECUTION-DAG.md`, `PROGRESS.md`, and the wave specs `W-DOCK-SECTIONS`, `W-DOCK-MORPH-INSITU`, `W-DOCK-GEOMETRY`, `W-SURFACE-AXIS`, `W-CONFIG-CHASSIS`, `W-GLASS-CAL`, `W-GOO-REDRESS`, `W-DARK-MATERIAL` (head), `W-NO-GRAY`, `W-FADING-SCROLL` (head), `W-TABS` (head), `W-DEMO-AFFORDANCES` (head), `W-MENU-GLASS` (head), `W-STAGE` (grep-targeted on aurora). Grep-swept all 30 wave specs for watercolor / collision-scroll / slider / easing-picker. Verified the glass-ui 3.13.0 exports manifest + the `watercolor-dot`/`slider`/`select` source dirs. Cross-checked keyframes K (`K.md` + waves) for an easing-picker push into glass-ui — confirmed absent (K consumes glass-ui published, `inv-16`).
- NOT done: live browser grounding of U33/U7/U8 (a sibling lane holds the exclusive chrome-devtools profile; the running profile refuses a second/isolated instance). The overlap map is documentary — grounded against the binding wave specs, which is the correct evidence for a tranche-development overlap matrix. The live aurora/dropdown behaviour is the design/blob/aurora/dropdown lanes' province; this lane hands them the X-GU-A1/D2 asks to confirm.
- This lane implements nothing and writes no glass-ui file (`inv-16`). The asks are filed for the BA roster via the cohort channel.
