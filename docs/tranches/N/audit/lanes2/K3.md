# Lane K3 — FABLE cross-repo coherence critic (the authored sibling letters vs BA + K reality)

**Fleet**: second N-tranche deep-audit (lanes2). **Mode**: tranche development only — zero code
edits, zero doc edits outside this report. **Date**: 2026-06-12. **Substrate read in full**:
the user LEDGER (U1–U33), `WAVES-2.md`, `N.md`, `PROGRESS.md`, `X-GU.md`, `X-GU-ITEMS.md`,
`X-KF.md` (skim), `X-KF-ITEMS.md`, `L-PERF1/2/3.md` (targeted), `D5.md` (targeted);
glass-ui `BA.md` + `PROGRESS.md` + `coordination/{ATLAS-LETTER,VALUEJS-N2-ASKS}-2026-06-12.md`
+ targeted wave specs (W-GOO-REDRESS, W-DOCK-GEOMETRY, W-DOCK-MORPH-INSITU, W-MENU-GLASS,
W-TABS) + live source/dist greps; keyframes `K.md`, `L-SEED.md` (structure),
`waves/K.W1.md:548-558`, `waves/K.W4.md` (grep), `VALUEJS-N2-ASKS.md`, `package.json`,
the donor trio on disk, `docs/tranches/F/` (precedent check).

---

## §0 — Verdict in one paragraph

Both letters are **idiom-correct, non-duplicative, and anchored on verified mechanisms** —
the strongest cross-repo artifacts this constellation has produced (every spot-checked
file:line landed EXACT except one, §2-D4). The consume-sequencing (BA lands 4.0.0 → value.js
W18 consumes → W9′ pins) is coherent and one-directional. **The load-bearing defect is a
coverage hole, not a quality hole**: WAVES-2 anchors ~9 glass-ui-blocked rows (the L-PERF
producer cluster + the carried N.md §8 cohort rows + the D5-1 glyph companion) that appear in
NO filed ask and NO BA wave — the DAG's "asks already filed" is false for them, and they will
silently drop at the BA cut unless a letter addendum ships. Six defects follow, ranked.

---

## §1 — What the brief asked, answered

**Idiom-correct?** YES, both.
- The glass-ui letter sits at `coordination/VALUEJS-N2-ASKS-2026-06-12.md` beside the
  ATLAS-LETTER — verified the precedent: same title form ("BINDING cross-repo input"),
  same register structure (A mechanisms / B systemic / C capability / D confirmations /
  E the cut), same need-shaped-not-name-shaped discipline, same owner-folds posture
  (`?? ` untracked in both repos — uncommitted as briefed). The brief's hypothesized
  `waves/W-VALUEJS-N2-ASKS.md` placement would have been OFF-idiom (BA's `waves/` holds
  only lead-authored `BA.W-*` specs with File Bounds + Triumvirate dispatch — a consumer
  cannot legitimately author one); X-GU-ITEMS's location judgment (§1) is correct and
  BA has ALREADY acknowledged receipt (`BA/PROGRESS.md` Batch-0 event: "ALSO ARRIVED: the
  value.js N2 letter… the fold analysis rides the same workflow").
- The kf letter sits at K's tranche root beside `L-SEED.md`, matching the F-era inbound
  precedent `kf/docs/tranches/F/valuejs-sota-handoff-v2.md` (verified on disk). It adds no
  K wave, re-litigates nothing, and feeds exactly the artifact K.W1's re-pin-re-confirm
  clause awaits (`K.W1.md:550-556` verified VERBATIM: "HANDOFF, consume on the value.js
  publish (K.W1 owning, re-pin re-confirm)… the consume signal is `it.fails(` at
  `test/interpolate-anything.test.ts:256` flipping").

**Duplicating their existing waves?** NO — every "absent/net-new" claim re-verified:
- A-1: W-DOCK-GEOMETRY = plate-clip cluster; W-DOCK-MORPH-INSITU's `DockLayerGroup`
  mentions are shell in-situ layering (BA-DSM-4), NOT measure ordering. Genuinely unnamed.
- A-2/Register B: W-MENU-GLASS greps zero for collision/bound/available-height/font-rung;
  its scope is the `menuItemVariants` glass register. Genuinely net-new.
- A-3: the Slider appears in BA only as a consumer (X-GU §5.3 re-confirmed). The dead size
  axis verified live: `slider/index.ts:60-62` ships the CVA arbitrary-property utilities;
  zero backing rules in any `dist/styles/*.css` (`grep -c` = 0 across all 25 files).
- A-4: `atoms.ts:164-168` verified EXACT (`breathing: { nucleiDrift: 0, paletteDrift: 0,
  warpDrift: 0, breathDepth: 0.05 }`). W-STAGE/W-DARK-MATERIAL touch placement/legibility,
  never the motion table. Correctly framed outside the GL-shader fence.
- A-5: W-TABS has ZERO centering acceptance rows today — the acceptance-row addition is
  additive, not duplicative.
- C-1: `metaball-uniforms.glsl.ts:83-86` carries only `uSatPos/uSatRadius/uSatOpacity`;
  `grep uSatColor` = 0 repo-wide. The W-GOO-REDRESS fence verified (`BA.W-GOO-REDRESS.md:168-170`:
  editing `metaball.frag`/`sdf-body.glsl.ts` = "a fence breach (BA inv-9); STOP and
  triumvirate") — the letter's widen-the-seam-or-4.x framing is exactly right.
- C-2/C-3: watercolor-ghost and easing-configurator confirmed absent from all 29 waves
  (BA roster = 29, per BA.md/PROGRESS; X-GU.md's "30" is a trivial miscount, self-corrected
  in its own §5). The C-3 three-way boundary (math=value.js / time=kf / component=glass-ui)
  is consistently stated in BOTH letters and X-KF §1.2/L-SEED §7 — no drift between them.
- kf side: K.md defers the entire value.js half wholesale to L (L-SEED §7) — "K authors
  ZERO net-new VJ items" corroborated; K.W4's easing touches are ChromeDock/spring-scene
  only (`K.W4.md:343,590` verified; the trio untouched). The token-routed-restyle request
  is a legitimate non-collision caution, not a re-litigation.

**Do WAVES-2's glass-ui-blocked rows anchor on the BA items?** The NAMED-register rows, yes
— row-by-row: W13 blocked set = A-3 + A-2/WO-1/WO-3 ✓; W15's dock FLIP = A-1 ✓; W14.E = C-2 ✓;
W14.C = W-SURFACE-AXIS scope 6 (letter D-1, confirm-only — correct: BA.md verifies the
Skeleton over-glass register in W-SURFACE-AXIS) ✓; W18.A's headline adopt list maps onto
W-DARK-MATERIAL/W-NO-GRAY/W-GLASS-CAL.3/A-1/A-2/A-3/C-1/C-2/W-MENU-GLASS ✓; §5's coverage
parentheses cite the letter's register ids accurately ✓. **The UNNAMED rows do not anchor
on anything — that is defect D1/D2.**

**Consume-sequencing coherent with the 3.13.0 pin plan?** YES in substance: 3.13.0 shipped
(the trigger N chartered), BA opens ON 3.13.0 and cuts 4.0.0 (BA.md H4 **DECIDED → 4.0.0**,
verified), the U-fixes land in 4.0.0, so the pin amendment (inv-N-6 → "the BA cut") is the
only honest target — value.js pinning 3.13.0 would pin every U-defect BA fixes. The spine
stays acyclic (value.js = pure sink; the wait gates only the pin; W18 may lag without
blocking W8′; W9′ holds on the cut — all consistent across WAVES-2 §1/§2/§4, the letter's
Register E, and X-GU §5.2). Register E's by-name migration ask is grounded (verified:
`PaneSegmentedControl.vue:18` imports `@mkbabb/glass-ui/tabs` — the W-TABS `ui/Tabs`
retirement WILL hit value.js). Residual partial-application drift → D5 below.

---

## §2 — The defect ledger (ranked)

### D1 [P0 — coverage hole] The L-PERF producer asks were mandated, never filed

`L-PERF2.md:230` ("**Author a glass-ui (tranche BA) ask**" — LP2-2 GooBlob zombie second
canvas: one mount → two `<canvas>`, the 2nd 400×400/0×0 on a live GL context + RAF),
`L-PERF2.md:232` (LP2-4 — the `--dock-morph-t` 10-selector inheriting `calc()` cascade,
~13 fps morph), `L-PERF3.md:275` (LP3-1b — GooBlob visibility/`document.hidden`/PRM gate,
**P0**, grepped-zero in the dist), `L-PERF3.md:110,276` (LP3-3a — `card-header/title/desc-shrink`
keyframes animate padding/font-size/grid-rows, CLS 1.03, **P0**), `L-PERF1.md:171` (the aurora
DPR cap — "Owner: glass-ui (DPR cap in useAurora)"). **None of these appears in the letter**
(registers A–E carry zero of them) **and no BA wave owns them** (greps across all 29 specs:
zero hits for zombie/card-shrink/DPR/morph-t-narrowing; W-GOO-REDRESS's two scopes are
satellite-smin + pointer-wake only). Yet WAVES-2 anchors all five as "Blocked-on-BA (verify
at W18)" (W15) and lists three in W18.A's adopt sweep, and the §2 DAG states "asks already
filed." Consequences: at the BA cut these rows are undischargeable; `inv-N-12` ("no offscreen
live WebGL context") cannot fully close (the zombie canvas is producer-internal); the
inv-N-9 producer half (LP3-1b) dangles. This is precisely the failure X-GU-ITEMS §2 named
when it filed Register B for items that "would otherwise drop, violating the LEDGER's
zero-drop directive" — the same logic was not applied to the perf cluster. **Fix**: a
Register A′/B′ addendum to the letter (the perf cluster is mechanism-grade and live-traced —
it qualifies for Register A's bar) BEFORE BA's Batch-2/4 waves dispatch; or WAVES-2 re-labels
the five rows "UNFILED — letter addendum owed" so the drop is at least loud.

### D2 [P1 — coverage hole] The carried N.md §8 cohort rows likewise never crossed the boundary

WAVES-2 §5 maps "glass-ui cohort rows (uSatColor[], **AuroraConfig**, **C-DTS**, **devDep**,
**retired-classes manifest**, the cut) → W18.A". Only `uSatColor[]` (C-1) and the cut (E)
are in the letter. The `AuroraConfig` slider-section descriptor, C-DTS (the dts-emitting
`build:watch` — which W18.A's own "against a dts-complete dist" precondition leans on, the
R1 §6 dist-flap), the devDep bump (`@mkbabb/value.js ^0.10.0→^0.11.0`), and the
`.retired-classes.txt` currency ask exist in NO glass-ui artifact — not the letter, not any
BA wave (greps zero; W-CLOSE stages MIGRATION.md by-name tables, which MAY substitute for
the retired-classes manifest, but the substitution is recorded nowhere). These are N.md §8
standing asks that were only ever written in value.js's own tree. With BA being THE active
glass-ui tranche and the letter THE channel, they drop at the cut. **Fix**: fold into the
same letter addendum (the devDep + manifest rows are one-liners; AuroraConfig + C-DTS are
small Register-C/D items).

### D3 [P1 — staleness with a real deadline] "Pre-greenlight" is false; the fold has batch deadlines nobody stamped

BA is **GREENLIT 2026-06-12 and EXECUTING** — Batch 0 has two waves complete
(`BA/PROGRESS.md`: W-SHELL-HOLD `b2ad0ebe`, W-GESTALT-GATE `1c03a2d9`). X-GU.md's header
("pre-greenlight, NO impl") and the second-fleet brief's framing are stale. The letter
carries no fold-by urgency, but its asks have hard dispatch deadlines: C-1 before Batch 2
(W-GOO-REDRESS), A-1 before Batch 2/3 (W-DOCK-GEOMETRY / W-DOCK-MORPH-INSITU), A-2 + Register
B + A-5 before Batch 4 (W-MENU-GLASS / W-TABS / W-GLASS-CAL), A-4 before Batch 6 (W-STAGE).
Mitigated: BA's lead has the letter and routed it into the Batch-0 workflow — but the letter
itself should carry the deadline table (one row per register → owning batch), since BA
batches dispatch incrementally and a late fold forces 4.x point releases of items that
could have ridden their natural waves.

### D4 [P2 — precision, falsifiable-on-re-grep] "dist/components/ — a directory that does not exist" is false at HEAD

Verified live: `glass-ui/dist/components/` EXISTS, holding `custom/` + `ui/` trees of
**`.vue.d.ts` type declarations only** (e.g. `dist/components/ui/select/SelectContent.vue.d.ts`).
The letter's CONCLUSION fully stands — d.ts files carry no scannable class strings; the
compiled JS with the utilities is flat `dist/*.js` (`reka-popper-available-height` found only
in `dist/SelectScrollDownButton-CiDUxfEj.js`); zero backing rules in every dist stylesheet —
but the stated mechanism ("does not exist") is the one claim a BA folding lane re-grepping
anchors per BA inv-3 will FALSIFY, and a falsified premise invites discounting the whole
Register B diagnosis. (Likely cause: the donor lane probed the published tarball or a
pre-rebuild dist; W-CARVE2's Batch-0 rebuild regenerated the d.ts tree.) **Fix**: one-line
correction in the letter — "resolves to `dist/components/`, which holds only `.vue.d.ts`
declarations (no class strings); the compiled JS sits flat at `dist/*.js`, unscanned."
Same fix-shape, now un-falsifiable.

### D5 [P2 — partial amendment] inv-N-6's in-place text still says 3.13.0

The pin amendment is recorded in WAVES-2 §4 and annotated on N.md §4's W9 row — but N.md §6
(`inv-N-6 — registry consumption at close. glass-ui ^3.13.0`), §3 item 1, §9, and the live
task list (#35 "pin gated on glass-ui 3.13.0") are un-amended. X-GU-ITEMS §3 itself called
for "a one-line N charter amendment at the synthesis fold" — it was applied to the schedule
row, not the normative invariant table. A later lane reading §6 alone pins 3.13.0 and ships
every U-defect. **Fix**: the one-line §6 edit (+ §3/§9 echoes) at the synthesis fold.

### D6 [P3 — corpus incoherences, recorded for the fold]

1. **X-GU.md §5.1 vs the letter**: §5.1 declares D2+H1 "collapse to ONE publish… the single
   sharpest correction the second fleet adds"; the letter (correctly, on U-DROPDOWN's live
   root-cause) SPLITS them — U8's fix is glass-ui's own SelectContent bound + emission
   (A-2/B, P0, fixes the color-space dropdown immediately) while the easing-configurator
   (C-3) is a separate publish whose `EasingSelect` is merely the cap-idiom donor. WAVES-2
   follows the letter. X-GU.md §5.1 stands uncorrected — a synthesis reader could
   de-prioritize A-2 as "ships inside C-3." Needs a supersession stamp.
2. **Both letters cite the dead N.W6.C** as the value.js easing-consume site (glass-ui
   letter C-3 "the value.js gradient pane (N.W6.C, upgraded to consume)"; X-KF-ITEMS §3
   likewise). N.W6 is SUPERSEDED → WAVES-2 (consume = W18.B; interim = W16.B). Traceable
   via N.md's supersession pointer, but the letters predate WAVES-2's finalization and
   should be stamped at the owner-fold.
3. **The letter's provenance** says "glass-ui resolved at 3.13.0" while value.js
   `package.json:79` is `file:../glass-ui` (the local tree IS at version 3.13.0+Batch-0;
   loose, not wrong).
4. **W13's "per-density dock glyph (D5-1 companion)" blocked-on-BA row**: D5.md §261
   records "(+ glass-ui BA small ask)" — `--dock-icon-glyph` does NOT ride the density
   cascade (`offsets-sizing.css:276`) — another unfiled one-liner; fold into the D1 addendum.
5. **X-KF-ITEMS internal cite drift**: §1.4 says "K.W4.md:462-463,590", §5 says ":343,590";
   actual ChromeDock anchors sit at :343, :461-464, :590 — all real, the drift is cosmetic.
6. **X-GU.md method line** says "30 BA wave specs"; the roster is 29 (BA.md/PROGRESS;
   X-GU's own §5 says 29). Trivial.

---

## §3 — What is RIGHT and must not be re-litigated

- The need-shaped register idiom, the anti-duplication ledger (X-GU-ITEMS §4), and the
  fence-respecting framing of C-1 are exemplary — BA's lead can fold every named register
  without a single re-diagnosis (per BA inv-3, re-grep only).
- The kf letter's §5 verification log is the gold standard for cross-repo citation
  discipline: my independent spot-checks (kf `package.json:178-179`, `K.W1.md:550-556`,
  `EasingSelect.vue:29`, the donor trio + `easingGroups.ts`, `K.md:59`, the F-era precedent
  file) ALL landed exact. The four citation fixes it applied (§5) were real and correct.
- The pin-target move (3.13.0 → the BA cut) is the right call on the verified record
  (BA.md H4 DECIDED → 4.0.0; the U-fix mass lands there); the acyclic spine holds; the
  BOOK-with-trigger posture on W9′ is unchanged in kind.
- WAVES-2's named-register anchoring and its §5 zero-drop coverage map are sound for every
  row that HAS a filed producer commitment — the holes (D1/D2, ~9 rows) are the entirety
  of the gap.

## §4 — The one action this critique demands

A **letter addendum** (same file, new Register A′/B′ rows + four one-liners), authored
under the same sanctioned grant before BA Batch 2 dispatches: LP2-2 zombie canvas ·
LP3-1b visibility/PRM gate · LP3-3a card-shrink composited keyframes · LP2-4/5 morph-t
cascade narrowing · LP1 aurora DPR cap · D5-1 per-density glyph · AuroraConfig descriptor ·
C-DTS dts-emitting watch · devDep bump · retired-classes/MIGRATION substitution recorded —
plus the D4 one-line precision fix and the fold-by-batch deadline table. WAVES-2 then needs
only two line-edits (the DAG's "asks already filed" footnoted; inv-N-6's §6 echo) and
X-GU.md two supersession stamps (§5.1; "pre-greenlight").
