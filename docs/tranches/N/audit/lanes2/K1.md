# Lane K1 — FABLE critic: adversarial audit of the WAVES-2 revision

**Fleet**: second N-tranche deep-audit (lanes2). **Mode**: audit only — zero edits anywhere.
**Date**: 2026-06-12. **Target**: `docs/tranches/N/WAVES-2.md` + the `N.md`/`PROGRESS.md`
amendments. **Method**: every load-bearing number, file:line anchor, lane-report cite, and
coverage-map row in the revision was checked against (a) the working tree at HEAD
(`f3df3d8` = `199fd15` + 2 docs commits, package version 0.12.0), (b) the lanes2 corpus
(R1–R4 · D1–D8 · U-* · L-* · X-*), (c) the sibling repos' ask-letters, and (d) the user
LEDGER. ~70 anchors re-derived from primary sources; the verification log is §3.

---

## §0 — Verdict

**The revision is sound and exceptionally well-grounded — no fabrication found.** Of ~70
checked claims, ~63 verify exactly; the remainder are ±2-line drift or census imprecision.
The §5 coverage map is **complete**: all 34 LEDGER rows present, every R3-fold OPEN / BOOK /
VERIFY / ADVISORY row carries a wave, zero dropped rows found by independent walk of
`R3-fold-ledger-v2.md §5`. The pin-target amendment (3.13.0 → the BA cut) is exactly what
X-GU §5.2 establishes. Both sibling ask-letters exist, uncommitted, with registers precisely
as WAVES-2 cites them. Eight findings below, ranked; none blocks ratification — F1–F3 should
be folded as one-line corrections before or at dispatch.

---

## §1 — Findings, ranked

### F1 [MED] — "14 bespoke `*-enter-active` families" is off by two; the W17 gate grep will miscount

Tree truth: **12** demo-authored `*-enter-active` families (`edit-drawer`, `edit-overlay`,
`error-pop`, `eyedropper-fade`, `feedback-slide`, `pane-left`, `pane-right`, `pane-slide`,
`rename-slide`, `slug-bar-swap`, `swatch-item`, `toggle-icon`). The other two in U-DOCK §3's
census — **`fade-slide` and `pop` — are glass-ui-SHIPPED families**
(`../glass-ui/dist/styles/transitions.css:23,63`), consumed by 5 demo `<Transition name>`
sites (`MixPane.vue:107`, `MixResultDisplay.vue:35`, `ExtractPane.vue:43`,
`ImagePaletteExtractor.vue:12,59`) with zero demo CSS. The error originates in U-DOCK §3,
is carried by D5 §5b, and lands in W17.B verbatim. Two consequences:
1. The W17 hard gate "≤ 3 transition families (grep + typed prop)" must scope its grep to
   demo-authored CSS or it counts the producer's families and can never go green.
2. D5's 14→3 disposition maps several families ONTO `pop`/`fade` — names that **collide with
   glass-ui's shipped vocabulary**. The wave must decide consume-vs-author explicitly
   (consuming glass-ui's `pop`/`fade-slide` and authoring only `pane` is the cleaner shape,
   and changes the "3 semantic families" arithmetic).

### F2 [MED] — inv-N-7 is VIOLATED at HEAD and the §4 invariant-deltas omit it

`watercolor-swatch` (`MixSourceSelector.vue:148`) resolves to **no rule anywhere** — grep
over `demo/**` and the glass-ui dist/src = 0 definitions. It is a live phantom class of
exactly the P9 species inv-N-7 exists to kill. W14.E *knows* ("kill the phantom
`watercolor-swatch` class") but WAVES-2 §4 records only inv-N-9 as VIOLATED — inv-N-7's
live violation goes unrecorded, and `PROGRESS.md:19`'s "zero phantom classes ✓ (sweep
green, inv-N-7)" is **stale against the tree**. One-line fix: add inv-N-7 VIOLATED-at-HEAD
(1 class, closes at W14.E) to §4. Counter-note: U-cards §2's claim that `stagger-children`
is "still undefined" is itself WRONG — `animations.css:40` defines it (W5.E minted it);
the only live phantom is `watercolor-swatch`. WAVES-2 correctly does not carry the
stagger-children claim.

### F3 [MED] — U21 carries an unreconciled dual diagnosis (demo pb-2 vs glass-ui font-metrics)

W13.C adopts D7-9's measured verdict: the pill rides 4px high from `pb-2`
(`PaneSegmentedControl.vue:4` — verified in tree); "text within the pill is perfectly
centered, 3.5/3.5 — **glass-ui is innocent; no SegmentedTabs ask needed**" (D7 §4, live
measured). Yet W18.A/X-GU-ITEMS files **A-5 "SegmentedTabs pill centering" as a glass-ui
ask**, whose substance is D3 §6's contradictory diagnosis (font-swap metrics: "label sits
visibly off inside the white pill", indicator-anchored-to-trigger-box). Both diagnoses of
the same U21 cannot be binding; the revision files both without recording the conflict.
Risk: BA burns producer effort on an ask D7's measurement refutes — or the demo deletes
`pb-2` and U21 persists. Disposition needed: one sentence in W13.C ("A-5 is
verify-after-pb-2; withdraw if the deletion closes U21") or strike A-5 to confirm-only.

### F4 [LOW-MED] — PROGRESS.md now self-contradicts on the pin target / 3.13.0 state

The amended file carries three mutually inconsistent statements: the W9 wave-status row
says "pin gated on glass-ui 3.13.0 — **still uncut**" (PROGRESS:23) and the dispatch-split
repeats it (:55), while the new WAVES-2 board (:31) says the pin target **moved to the BA
cut**, and R1 §6 + X-GU §0 establish **3.13.0 IS cut** (glass-ui `package.json` = 3.13.0,
published, `latest`). Same class inside N.md: §3/§8/§9 still read "3.13.0" with only the
W9 table-cell annotated. The "history intact" posture explains N.md; it does not explain
the un-amended PROGRESS W9 row, which is a *status board* and now states a false present
tense ("still uncut"). Two-cell fix.

### F5 [LOW-MED] — The W10.B aurora temporal-gate parameters are S1-authored, not lane-measured

The gate "two samples 3 s apart, mean |Δ| ≥ 3/255 on ≥25% of a 16-pt grid; inverted under
PRM" appears **nowhere in the lanes2 corpus**. U-AURORA's measurement is the dead baseline
only (±1–2/255 over 2.5 s under `breathing`, `readPixels`); nothing was ever measured under
`drifting`. The parameters are plausible (drifting's 0.015/0.015/0.008 drift should clear
3/255 over 3 s) but the gate is **born uncalibrated** — it should carry a
"calibrate-at-implementation" note, and the inline placement after the U-AURORA cite reads
as if lane-derived. (The companion numbers ARE lane-grounded: `colorEnergy 0.55→0.7` +
`zones 4→5` = D5-8/D6 §5.1 — though W10.B cites neither lane for them.)

### F6 [LOW] — "U1–U33 + 8 directives": the LEDGER's standing-directives section has 6 bullets

`LEDGER.md:90-97` = six bullets. Eight is reachable only by splitting the compound bullets
(perf+design; glass-ui+keyframes items); as written the count is wrong. Cosmetic, but it is
the canon document's own census.

### F7 [LOW] — Coverage-map under-indexing on the D8-3 family

§5's net-new row "D8-3/4/5/6/7 a11y cluster → W13.D / W12.B / W16.A·E" omits the two
sub-rows the wave texts themselves route elsewhere: **D8-3b** (dock inert/visibility atomic
pairing) lands at **W17.E**, and **D8-3c** (DOM order = visual order) + **D8-8** (container
queries) land at **W12.C**. The "exactly once" attestation is scoped to LEDGER+R3 rows so
it technically holds; the net-new table is the imprecise one. One-cell fix.

### F8 [LOW] — Line-number drift class (±2, no load-bearing consequence; for the implementer)

- `Katex.vue:20` cited → `output:"mathml"` is at **:21** (R1 cites :21; D4's WO-2 anchor
  is the source of the :20).
- `panes/keys.ts:26-28` cited for the W10.B flip → `motion:"breathing"` at **:28** ✓ but
  `colorEnergy`/`zones` (also changed by the lane) sit at **:24-25**, outside the range.
- `SpectrumCanvas.vue:109` → the `getBoundingClientRect` read is at **:108**.
- `style.css:64-66` (font tokens) → **:63-65**.
- `viewSchema.ts:69-76` (D7 §2) vs `:68-76` (WAVES-2/D7 §7) — both defensible.
All symbols verified present; nothing dangles.

---

## §2 — What was hunted and NOT found

- **Fabricated anchors**: none. Every cited file exists; every cited symbol/line verified
  within ±2 lines (~70 checks, §3).
- **Fabricated lane cites**: none. Every "per lane X §Y" claim re-read at source — D6 §6's
  gate widths {490/616/695/704±4} verbatim; D7 §3.4's save-P0 code shape matches the tree
  (`await ensureUser()` unguarded before local `createPalette`, `usePaletteActions.ts:59-63`);
  D3 §4a's kC placebo matches (`useImageQuantize.ts:91-94` forwards `{k}` only); L-COLOR's
  0.2724→0.0464 / 83% / 11.5×JND / C-norm 0.545 / α=0.05 / `findCusp:141` all verified;
  D8-1's unlayered dist re-verified LIVE (`components.css`: 0 `@layer`,
  literal `.flex{display:flex}`); U-DOCK §1's 55→19px / ~455 ms dead-hold / 1-frame snap;
  U-DROPDOWN's 745px box; D2's 3061 ms mix baseline (timers 800/2300/2900 in
  `useMixingState.ts` confirmed in tree); R4's RM-1..9 + CM-1..7 lists carried exactly.
- **Double assignments in §5**: none — every split row parenthesizes its blocked half's
  landing site consistently with the wave texts (U3, U7, U8, U16, U18, U20, U22, U23, U27,
  U28, U32, U12's declared wave-pair).
- **Dropped R3 rows**: none — independent walk of R3 §5's roll-up (W8-bound ≈14, W9-bound,
  N2-design, cohort ≈18, BOOK 4, VERIFY 1, ADVISORY 1) found every row in WAVES-2 §5,
  including the W6-death debris sweep (all 8 debris files confirmed present in the tree)
  and the VJ-F1 VERIFY row correctly resolved to CLOSED (`src/transform/path.ts:478`
  `export class PathGeometry` ✓; kf letter §2 row 9 ✓).
- **Unmeasurable gates**: none. Each hard gate is mechanically checkable; the two
  judgment-shaped ones (W12 cartoon census, W14 cartoon budget) are operationalized by
  D6 §6's DOM-census definition. W17's "dock band ≈ 64 px spacious" anchors to D5
  WO-D5-1's "expanded box ≈ 320×64" ✓. W15's born-RED morph harness is honest by design.
- **Stale supersession claims**: none — R1 §1/§4 + the `199fd15` diff confirm W6 died with
  only the 2-line Dock.vue sliver; `impl/` has no W6 report; PROGRESS's W6 row matches.
- **Ask-letter drift**: none — `../glass-ui/docs/tranches/BA/coordination/VALUEJS-N2-ASKS-2026-06-12.md`
  (uncommitted `??`) carries registers A-1..A-5, B, C-1..C-3, D, E exactly as W18 lists;
  `../keyframes.js/docs/tranches/K/VALUEJS-N2-ASKS.md` (uncommitted `??`) carries the
  census correction, the **12-row** witness-flip slate, the does-NOT-flip ledger, the
  easing hand-off §4, and the VJ statuses exactly as W18.C summarizes.
- **inv-N-9 claim**: TRUE — `useMixingAnimation.ts` RAF at 77/83/99/189/196, zero
  `matchMedia`/PRM references; W16.C's `:77-206` anchor correctly supersedes R3's stale
  `:116,206`.

---

## §3 — Verification log (anchor → result)

Tree (all verified, ±2 unless noted): `useAppColorModel.ts:42-44,61-66` resetToDefaults +
debounced store-sync ✓ · `ColorInput.vue:247-250` focus-guarded repaint ✓ ·
`panes/keys.ts:22-29` aurora atoms (F8 range note) ✓ · glass-ui `atoms.ts:164-168`
MOTION_FIELDS, breathing zeroes drift at :166 ✓ EXACT · `usePaletteActions.ts:59-63` ✓
EXACT · `useImageQuantize.ts:91-94` ✓ · `client.ts:29` VITE_API_URL ✓ EXACT ·
`style.css:52-53` dual glass-ui imports ✓ EXACT · `App.vue:34-69` dual mounts ✓ ·
`BrowsePane.vue:135` ✓ EXACT · `gamut.ts:141/238/247/262-277` ✓ · `xyz-extended.ts:61-79`
xyz2rgb correctGamut; xyz2displayP3/rec2020 lack it ✓ · `dispatch.ts:301-306` ✓ EXACT ·
`constants.ts:72` oklch c max 0.5 ✓ EXACT · `--primary` not in demo style.css; D6 probe is
the ground ✓ · `style.css:132-133/146-159/189-209/215-225/222` layout + dark navy border ✓ ·
`index.html:11-18` Google-Fonts Fraunces/Fira ✓ EXACT · glass-ui exports `./styles/fonts` ✓ ·
`Markdown.vue:10` font-display + 1 scoped block ✓ · `ComponentSliders.vue` = **418 LoC** ✓
EXACT (R4-2) · `EasingSelector.vue:64` hsl(248,88%,71%) literal ✓ · `ConfigSliderPane.vue:137-139`
spectrum Slider ✓ EXACT · `ColorSpaceSelector.vue:16-37` sm:text-display + fira-code group ✓ ·
`DockViewSelect.vue:52-57/76-88` trigger, gray dot, pastel-rainbow row ✓ ·
`PaneSegmentedControl.vue:4` pb-2 ✓ EXACT · `ActionButton.vue:14` (per D8 re-verify) ✓ ·
`GradientStopEditor.vue:107-148` pointer-only (per D8) ✓ · `PaletteCard.vue:5-13` (per D3) ✓ ·
`GradientVisualizer.vue:122` shadow-card (per D3/D6) ✓ · `MixResultDisplay.vue:31` glass-floating
✓ EXACT · `GradientCodeEditor.vue:138` rounded-lg ✓ EXACT · `MixSourceSelector.vue:148-149`
watercolor-swatch phantom + dashed square ✓ · `CurrentPaletteEditor.vue:82-86` dashed circle ✓ ·
`utils.css:56` .dashed-well ✓ EXACT · 9 × `tier="wash"` ✓ EXACT · `Dock.vue:93/198/215-227` ✓ ·
`App.vue:247-252/269-306/296/315/331-368` ✓ (the `--spring-snappy`+`--duration-slow`
mis-pairing is verbatim in the tree) · `viewSchema.ts:32/68-76`, `usePaneRouter.ts:32` lucide ✓ ·
`SpectrumCanvas.vue:108` ✓ · `useSliderGradients.ts:26,45` ✓ · `useBrowsePalettes.ts:20` ✓ ·
`useMixingState.ts` timers 800/2900 in :64-104 ✓ · `useMixingAnimation.ts:77-196` RAF un-gated ✓ ·
`Katex.vue:21` mathml (F8) · `HeroBlob.vue:8` w-[7rem] ✓ EXACT · `ColorPicker.vue:2/4/22/25-30/32-33`
✓ · `path.ts:478` PathGeometry ✓ · glass-ui `Slider.vue:151-153` data-size ✓ ·
`package.json:79/80/121` ✓ EXACT · W6 debris ×8 files present ✓ · 0.12.0 published ✓.

Corpus: R1 §1/§3/§4/§5/§6 ✓ · X-GU §0/§1/§2/§5.1/§5.2 ✓ (BA cut = 4.0.0; re-pin decision
verbatim) · X-GU-ITEMS §1/§2/§3 ✓ · X-KF-ITEMS + the kf letter §1–§5 ✓ (12 rows counted) ·
R3 §1–§5 full walk ✓ · R4 §1–§3 ✓ · D1 §8 (D1-1/3/4/7) ✓ · D2 §1.3/1.4/2.2/2.3/3.1–3.5 +
D2-S2 ✓ (3061 ms measured) · D3 §0–§8 ✓ (D3-4..9 numbering exists) · D4 F2/F5/F6 + WO-1..7 +
the φ ladder values ✓ · D5 §5b/WO-D5-1/5/8/10 ✓ · D6 §0–§6 ✓ (gate widths verbatim; 10
self-clamps; dark ladder 6/12/16/20) · D7 §1–§7 ✓ (18 copy sites; D7-9 measured; Family-B
zero consumers; BA hinge H2 fence) · D8 P0–P2 + modern-web table ✓ (25/26; 12×24; 42ch;
unlayered dist) · U-AURORA §1–§5 ✓ (44 fps; refutation; the missing gate-spec = F5) ·
U-BLOB ✓ (seed L 0.91; chromaCeiling disproved) · U-DOCK §1/§3 ✓ (census = F1) ·
U-CONTROLS ✓ ("EXACTLY"; 189-LoC; 13 gap sites of 19 truncates) · U-DROPDOWN ✓ (745px) ·
U-FIXES §1 ✓ (chain NOT dead; the triad; random-seed-or-pulse) · U-cards §2/§3 ✓ (the
ellipse = deleted blob orbit; the stagger-children error noted at F2) · L-PERF1 ✓ (LCP
5012 ms; lucide boot-path) · L-PERF2 ✓ (~4-RAF floor; zombie 400×400; 35 rect reads;
10-selector cascade) · L-PERF3 ✓ (CLS 1.03; 103 ms computeScrollPosition; 33 conversions;
v-memo zero; ~170 KB; 2880×1800 ≈ 20 MB arithmetic ✓) · S1 ✓ (deliverables = the three
files, as found).

---

## §4 — Recommended pre-dispatch corrections (all one-liners)

1. W17.B + gate: "14 bespoke" → "12 bespoke (+2 glass-ui-shipped `pop`/`fade-slide` —
   consume, don't re-author)"; scope the gate grep to demo-authored CSS. (F1)
2. §4 invariant deltas: add "inv-N-7 — VIOLATED at HEAD (`watercolor-swatch`,
   `MixSourceSelector.vue:148`); closes at W14.E"; fix PROGRESS:19's stale ✓. (F2)
3. W13.C or W18.A: record the U21 conflict — A-5 becomes verify-after-pb-2-deletion. (F3)
4. PROGRESS:23/:55: "still uncut" → "3.13.0 cut; pin target = the BA cut (4.0.0)". (F4)
5. W10.B: mark the temporal-gate thresholds "calibrate at implementation"; cite D5-8 for
   colorEnergy/zones. (F5)
6. Header: "8 directives" → "6 directives". (F6)
7. §5 net-new row: add W17.E (D8-3b) + W12.C (D8-3c/D8-8). (F7)
