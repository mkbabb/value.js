# Lane S1 — THE RE-DIVINED WAVES (the keystone synthesis)

**Fleet**: second N-tranche deep-audit (lanes2). **Mode**: tranche development only — three
planning docs written, zero code edited. **Date**: 2026-06-12. **Substrate**: branch
`tranche-f-handoff`, HEAD `199fd15` + 0.12.0 + the user-audit commit.

## Deliverables (the only files written)

1. **`docs/tranches/N/WAVES-2.md`** (NEW) — the second wave block superseding the dead N.W6:
   **N.W10–N.W18** + W8′/W9′ re-sequenced after it. Every wave row carries lanes, file anchors,
   hard gate, and the unilateral-vs-glass-ui-blocked split. §5 is the zero-drop coverage map:
   all 34 LEDGER rows (U1–U33, U30 a/b), every OPEN R3 fold row, and every lanes2-born defect →
   exactly one primary wave.
2. **`docs/tranches/N/N.md`** — three minimal table-cell amendments: the §4 W6 row marked
   **SUPERSEDED → WAVES-2.md**; the W8/W9 rows note the re-sequence (+ the W9 pin-target move
   3.13.0 → the BA cut per X-GU §5.2). History intact.
3. **`docs/tranches/N/PROGRESS.md`** — the W6 row → **SUPERSEDED-BY-WAVES-2**; the WAVES-2
   board added (11 rows, all PLANNED, dispatch-gated on user ratification of this revision),
   with the honest W2.B note (emission correct; the live kill is the unlayered glass-ui dist
   cascade, owned by N.W10.D).

## The wave set in one breath

- **W10 Functional truth + substrate** (U9 / U33 / save-P0 / kC / **the D8-1 cascade kill =
  U11's root** / single-mount) — the gate-opener; everything desktop-visible hangs on it.
- **W11 Library color-SOTA** (U10: the gamut-map POLICY is the defect — cusp re-anchor + §13.2
  oracle + wide-gamut egress) — parallel, unilateral, clean π.
- **W12 GRAND HIERARCHY** (D6 keystone: the font root FIRST, the accent axis — `--primary` ≡
  `--foreground` today, the layout clamp + aspect law + container queries, the depth-grammar
  laws + the one `--card-edge` mint, the ramp + φ tokens).
- **W13 CONTROLS** / **W14 CARDS** — split demo-now vs glass-ui-blocked; every interim
  (consumer max-h cap, `[data-size]`, dashed harmonization) explicitly DIES at the W18 consume.
- **W15 PERF** (L-PERF1–3: the ~4-RAF idle floor is the ONE root of U6/U12/U23; the morph-truth
  harness ships born-RED until glass-ui A-1; prod Lighthouse is the missing artifact).
- **W16 PER-PANE** (D1–D4 + the dead-W6 corpus: picker hero numbers/blob, gradient curve-hero,
  mix un-withhold + the inv-N-9 PRM close, extract population hero + dup collapse, the dead
  markdown stylesheet, the modern-web carry incl. router-5).
- **W17 SHELL+MOTION+POPS** (dock spacious, 14→3 typed transition families, the view-select
  moment, the Family-B first consumers, the nomenclature standard).
- **W18 Cross-repo consume-at-pin** — gated on the BA cut; the ask-letters are ALREADY authored
  by X-GU-ITEMS/X-KF-ITEMS (uncommitted, owner-folds); U6/U16/U28/U8-first-class/U18-ghost/U3-satellites
  close HERE.
- **W8′** absorbs the wire-deploy ceremony (prod is still I-era) + R4's exact RELEASE.md/CLAUDE.md
  contradiction lists; **W9′** is close-only (pin discharged by W18).

## Load-bearing synthesis judgments (where lanes conflicted, the resolution)

- **U11 ownership**: D8-1's cascade forensics (unlayered `dist/styles/components.css` beats all
  layered utilities) outranks U-DOCK's "utility-generation hole" framing and the LEDGER's
  pane-router hypothesis — both observed the same death; D8 found the mechanism. W10.D carries
  the one-line `layer(glass-ui)` mitigation + the computed boot assert; the producer fix is the
  filed Register-B ask.
- **U21**: D7's measurement (demo `pb-2`; glass-ui innocent) outranks U-CONTROLS' SegmentedTabs
  hypothesis — demo-side delete, no glass-ui ask.
- **U13**: U-cards' git archaeology (the "ellipse veil" was the deleted blob orbit) — framed as a
  re-introduction (D1-5's veil capsule), not a restore.
- **inv-N-9** is VIOLATED in shipped 0.12.0 (R1/R4); the close rides W16.C (WO-M1's
  composable-boundary gate) — assigned once, not double-booked to the perf wave.
- **The pin moved**: inv-N-6 amended 3.13.0 → the BA cut (X-GU §5.2 — 3.13.0 still carries every
  U-defect BA fixes); the dts-less-dist flap (R1 §6) is a named precondition of the W18 sweep.
- **The dual-mount** folds into W10.D (same files as the cascade kill; L-PERF3's DAG demands it
  land BEFORE the desktop render becomes visible).
- **U10 framing**: L-COLOR proved the math CSS-Color-4-exact; the wave is a POLICY re-anchor
  (cusp/α), with the hue-hold kept mandatory (the browser's magenta rotation stays forbidden).

## Mandates honored

Zero drops (§5 attestation in WAVES-2.md); no-legacy (every interim names its death site);
gestalt (root fixes: cascade, font split-brain, gamut policy, FLIP measure — never symptom
patches); KISS (no new shared dirs; PaletteCard stays in-repo; glass-ui gets asks, not forks);
Fable mandated on every design implementation lane (W12–W14, W16, W17), orchestration on the
core model, opus/sonnet fan-out; chrome-devtools + frontend-design + modern-web bound into the
wave specs; π paired evidence at every design gate.
