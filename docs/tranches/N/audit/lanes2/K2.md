# LANE K2 — COVERAGE critic (Fable): WAVES-2.md vs the canon

**Lane**: K2, second N-fleet (lanes2), critic tier. **Date**: 2026-06-12. **Mode**: tranche
development only — zero edits anywhere; this report is the deliverable.
**Charge**: diff `WAVES-2.md`'s coverage (§1 wave texts + §5 map) against (1) the user LEDGER
(all 33 U-findings / 34 rows + the standing directives), (2) the dead-W6 charter items
(`N.md:150`), (3) R3's fold-ledger v2 (`lanes2/R3-fold-ledger-v2.md`, every OPEN/BOOK/VERIFY/
ADVISORY row), (4) the `N.md §8` cohort asks. Rule: every item appears **exactly once** or
carries an explicit recorded kill/defer rationale. List every drop and every dup.

**Verdict**: WAVES-2's coverage is near-complete and its §5 attestation is ~99% true.
**2 genuine drops** (both micro, both real), **1 duplicate claim** needing an ownership line,
**2 undeclared splits** (the parenthetical-split convention applied everywhere else but not
here), **4 cosmetic map↔text mismatches**. **Zero LEDGER rows dropped. Zero R3 rows dropped.
Zero §8 cohort asks dropped.** The drops are both dead-W6-charter/R3-internal residue, exactly
the class the zero-drop directive exists to catch.

---

## §1 — Set-by-set results

### 1a. The user LEDGER (34 rows + 6 directives) — COMPLETE, with 2 split-declaration defects

All 34 rows (U1–U33, U30 split a/b) appear in the §5 map (`WAVES-2.md:315-328`) exactly once.
Every blocked-half split is declared parenthetically for U1/U3/U6/U7/U8/U12/U16/U18/U20/U22/
U23/U27/U28/U32 — the convention works. Two rows violate it (§3 below: U26, U31), and one row
carries a duplicate CI-gate claim (§2: U11).

The standing directives: the LEDGER has **6** bullets (`LEDGER.md:90-97`); all 6 are
discharged:
1. zero-drop fold → §5 + this critique (the 2 drops in §2 are the gap);
2. perf + grand-design first-class waves → N.W15 + N.W12;
3. author tranche items in the glass-ui/keyframes repos → **verified on disk**:
   `../glass-ui/docs/tranches/BA/coordination/VALUEJS-N2-ASKS-2026-06-12.md` (23,716 B) +
   `../keyframes.js/docs/tranches/K/VALUEJS-N2-ASKS.md` (12,231 B), both recorded in the W18
   preamble (`WAVES-2.md:201-207`);
4. Fable design/synthesis + core orchestration → standing-mandates block (`WAVES-2.md:18-23`)
   + per-wave Fable annotations (W12/W13/W14/W16/W17);
5. chrome-devtools MCP + frontend-design + modern-web → same block;
6. tranche-development only → header (`WAVES-2.md:3-4`).

Count defect: `WAVES-2.md:7` cites "U1–U33 + **8** directives"; the LEDGER block has **6**
bullets. If S1 decomposed bullet 1 into three (ledger + dead-W6 + prior deferrals), that
decomposition is undeclared. Cosmetic, but a canon-citation must count its canon correctly
(§4-1).

### 1b. The dead-W6 charter (`N.md:150`) — 27 of 28 enumerable items homed; **1 DROP**

W6.A structure → W16 preamble (verbatim "the W6.A standing Fable per-pane structure governs").
W6.B (9 items): type-ramp/PaneHeader → W12.E; empty states + CTAs → W14.D; mix result →
W16.C WO-M4; docs h1 → W16.E WO-6; glass-tier rebalance → W12.D; celebratory Family-B
(save/copy/publish) → W17.D; icon energy → W17.D D7-1; dock first-paint → **landed in
execution** (the `199fd15` sliver) and explicitly recorded at W17.A ("first-paint expanded
posture KEPT — no re-litigation") — a model recorded-disposition.
W6.C (13 items): KaTeX html → W16.E WO-2; 68ch + φ → W16.E WO-7; easing hero motif +
tokenized stroke → W16.B; mix un-withhold/PRM → W16.C; preview honesty → W16.C WO-M2;
gradient-stop affordances → W16.B + W13.D; picker **dual-hero** → covered by decomposition
(D1 §1 resolves it "one voice, two channels"; W16.A implements D1-1 + D1-4); **thumb
live-color → DROPPED** (§2-1); 1440 mix clipping → W16.C; extract population/dominance →
W16.D; dup-shell collapse → W16.D; EditDrawer delete → W16.A.
W6.D (4 items): router-5/VIEW_MAP, dispatch→mix, Palette id-honesty → W16.F; VITE_API_URL →
W10.C. W6's gate ("per-pane DELTA evidence") → the W16 hard gate, verbatim.

### 1c. R3 fold-ledger v2 — COMPLETE on WAVES-2's side; **1 R3-internal carry dropped by both docs**

Every row of R3's §5 roll-up maps: the ~14 W8-bound rows (N-P0-2/3/5, CH-14a, X1/X2/X3,
X10, X13, kill-list, `$OUT`+staged, A5, the `.w6a-audit*.mjs` debris) → W8′ (`WAVES-2.md:
223-244` names the debris file-by-file); the 2 W9-bound rows → W18.A + W9′; the N2-design
bulk (T16/T19/T20/T21, X6/X8/X9/X12, the 5-item modern-web carry, all 34 U rows) → W10–W17
per the §5 R3 table (`WAVES-2.md:332-352`); the 4 BOOKs (CH-10, CH-13, X5, X14) → §3
carried verbatim; the 1 VERIFY (VJ-F1) → **resolved CLOSED with grounding**
(`src/transform/path.ts:478,512`); the 1 ADVISORY (X4) → W8′. X15 → REFUTED with recorded
rationale, and the refutation correctly does not drop U33's work (W10.B).

Two notes:
- **X4 disposition narrowed silently**: R3 §2 (X4 row) offers "a source-route enumeration
  test, OR accept the review-discipline risk + record"; W8′ carries only "the openapi
  table-vs-source **decision recorded**". The test option is still reachable inside "the
  decision," so this is a re-route, not a drop — but one word ("test-or-record") in W8′
  would keep the option visible at execution.
- **The drop**: R3 §1.5's glass-ui-AZ row (`R3-fold-ledger-v2.md:125`) closes with "CLOSED —
  notify glass-ui (§3)" and then R3's own §3 omits the notify; WAVES-2, X-GU.md, X-GU-ITEMS.md,
  and the authored glass-ui letter all omit it too (grep `motion-suite|easing stability` → 0
  hits outside R3). See §2-2.

### 1d. `N.md §8` cohort asks — COMPLETE (10/10)

glass-ui (7): the cut → W18.A with the **recorded inv-N-6 amendment** 3.13.0 → the BA cut
(`WAVES-2.md:211,301-302` — the amendment is explicit, exactly how a target-move must be
done); devDep bump, C-DTS, `uSatColor[]`, `AuroraConfig`, CH-4..8 (expanded, "(filed)"),
retired-classes manifest → all in W18.A + the §5 R3 table glass-ui row. fourier (2): matrix
corrections + web bump → W18.D. keyframes (1): 0.12.0 notify → W18.C "DISCHARGED". The W18
preamble's register list (A-1..A-5, B, C-1..C-3, D, E) matches X-GU-ITEMS.md §2 one-to-one;
the X-KF slate is grounded (12-row witness-flip incl. MCI-5 + the B2/VJ-3 consume edges —
`X-KF-ITEMS.md:33-46` — so W18.C's "MCI-5 **et al.**" resolves to real rows, plus the
does-NOT-flip ledger and the easing hand-off).

---

## §2 — DROPS (no WAVES-2 home, no recorded kill/defer)

**K2-D1 — W6.C "thumb live-color" (the slider thumb carries the live color).** A named
dead-W6 charter item (`N.md:150`, "picker dual-hero + **thumb live-color**"), declared DEAD
by R1 (`R1.md:142`), and explicitly carried by the design corpus — `D1.md:226`: "28px lg
spectrum tracks, **thumb carrying the live color (`thumb live-color`, the N.W6.C carry)**" —
inside the W13.A slider-consume work-order. But WAVES-2 never mentions a thumb anywhere:
W13.A (`WAVES-2.md:111`) specs the spectrum migration, the 28 px tracks, the rail, the
letters — no thumb-color clause; W16.A doesn't carry it; §5 has no row. It is not implied by
the consume either: U-CONTROLS (`U-CONTROLS.md:83-89`) establishes the glass-ui spectrum
thumb is a **size** mechanism (`--slider-thumb-size`, currently falling to the 1rem
fallback) — nothing in the primitive paints the live color into the thumb by construction.
**Fix**: one clause in W13.A ("thumb carries the live color — the W6.C carry, D1 §5") + a
W13 gate token. Severity: micro (one visual affordance), but it is precisely a "fold EVERY
un-fixed item" violation.

**K2-D2 — the glass-ui-AZ "notify" carry (easing-stability ⇐ proof:motion-suite).** R3
closes the AZ row with an explicit carry — "CLOSED — **notify glass-ui (§3)**"
(`R3-fold-ledger-v2.md:125`) — and the notify then appears in no §3 table, no WAVES-2 line,
and not in the authored BA letter. The carry originates as an R3-internal inconsistency, but
the zero-drop rule binds the fold, and WAVES-2 §5 claims "all R3 OPEN/BOOK/VERIFY/ADVISORY
rows" — this row's carry-half escaped. **Fix**: one recorded line, either way: (a) KILL —
glass-ui's grep-`proof:*` idiom is retired constellation-wide (the feedback record), the
notify is moot; or (b) fold a one-line "W7.A easing additions were additive" note into the
W18.A letter scope. Severity: trivial; the defect is the *unrecordedness*, not the work.

---

## §3 — DUPS / undeclared splits (double-homed without the declared-split convention)

**K2-X1 — U11's CI gate is claimed twice.** W10.D lands "extend boot-smoke with a COMPUTED
assert (`pane-wrapper` display ≠ none at 1440)" (`WAVES-2.md:53`) and its hard gate closes
U11 structurally (`:58`); W12.C then claims "**the U11 structural gate in CI**" (`:91`).
Two waves each appear to own the same CI artifact; §5 maps U11 → W10.D only. At execution
this either double-lands the assert or W12 assumes W10 didn't. **Fix**: one word in W12.C —
"re-point the W10.D boot-smoke assert under the new clamp/container-query layout" (verify,
not land).

**K2-X2 — U26 split undeclared.** §5 maps U26 → **W14.B** (`WAVES-2.md:319`), but U26's
root-token fix lands at **W12.B**: "de-navy `--border` … **U26's literal root**" (`:90`);
W14.B then does the application sweep ("U17/U19/U26", `:130`). Everywhere else a two-wave
row carries the parenthetical (cf. U20's "spectrum-slider half = W13.A"). **Fix**: §5 row →
"U26 | **W14.B** (border-token root = W12.B)".

**K2-X3 — U31 split undeclared.** §5 maps U31 → **W16.A** (`:325`), but the card-lock law —
U31's mechanism — is codified normative at **W12.E** ("the card-lock law (tabular-nums + ch
reservation, normative) into `DESIGN.md §Type`", `:93`); W16.A applies it ("U31's card-lock
by construction", `:170`). **Fix**: §5 row → "U31 | **W16.A** (card-lock law codified =
W12.E)".

---

## §4 — Cosmetic map↔text mismatches (coverage holds; the text should agree with itself)

1. **"+ 8 directives" miscount** (`WAVES-2.md:7`): the LEDGER standing-directives block has
   **6** bullets (`LEDGER.md:90-97`). If the count decomposes bullet 1 (ledger + dead-W6 +
   prior deferrals = 3), declare it; otherwise write 6.
2. **U20's W13.A half is unlabeled in the wave text**: §5 declares "spectrum-slider half =
   W13.A" (`:324`) and W14.C self-labels "U20a" (`:131`), but W13.A never cites U20 — the
   "U20b" token exists nowhere. One token in W13.A closes it.
3. **§5 net-new D8-3 cell under-lists its homes**: "D8-3/4/5/6/7 a11y cluster → W13.D /
   W12.B / W16.A·E" (`:359`) omits **D8-3b → W17.E** (the dock inert/visibility pairing
   assert, `:191`) and **D8-3c → W12.C** (DOM order = visual order, `:91`) — both homed in
   wave text but invisible from the map cell.
4. **The AZ-fleet adopt row is mapped but not enumerated at its home**: §5 sends "Button
   icon-sm / Select size / clampLabel / Tooltip mono" → W18.A (`:347`), but W18.A's adopt
   list (`:211`) names only the Select/slider items. The adopt sweep is checklist-driven at
   execution — enumerate all four in W18.A or the un-named two (Button icon-sm, Tooltip
   mono) risk a quiet skip at the pin.

---

## §5 — Affirmative attestation (what this critic verified WHOLE)

- **34/34 LEDGER rows** in §5 exactly once; 14 declared splits all internally consistent
  with their wave texts (U1, U3, U6, U7, U8, U12, U16, U18, U20, U22, U23, U27, U28, U32).
- **REFUTED set carries rationale and drops no work**: X15 (U33's work survives at W10.B),
  the U9 overstatement (the W10.A triad survives), U13-as-restore (re-introduction at
  W13.A), `chromaCeiling` (the seed fix survives at W16.A).
- **R3 roll-up fully mapped**: ~14 W8′ + 2 W9′ + ~40 N2 + ~18 cohort + 4 BOOK + 1 VERIFY
  (resolved) + 1 ADVISORY — with the two §2 caveats.
- **§8 cohort asks 10/10 homed**; the inv-N-6 pin-target amendment (3.13.0 → the BA cut) is
  explicitly recorded (`:301-302`), and both cross-repo ask letters exist on disk at the
  paths the W18 preamble cites.
- **No silent dup found beyond K2-X1** — `--card-edge` (mint W12.D → consume W14.B) and the
  φ ladder (mint W12.E → apply W16.E) are both *declared* producer/consumer splits, the
  correct idiom.

## §6 — The amendment slate (7 one-liners; all in WAVES-2.md)

1. W13.A: add "thumb carries the live color (`thumb live-color`, the W6.C carry — D1 §5)". [K2-D1]
2. §3 or W18.A: record the AZ easing-stability notify as KILL (proof idiom retired) or fold
   into the letter scope. [K2-D2]
3. W12.C: "the U11 structural gate" → "re-point the W10.D boot-smoke gate". [K2-X1]
4. §5: "U26 | W14.B (border-token root = W12.B)". [K2-X2]
5. §5: "U31 | W16.A (card-lock law codified = W12.E)". [K2-X3]
6. Line 7: "8 directives" → "6 directives" (or declare the decomposition). [§4-1]
7. W13.A "(U20b)" token; §5 D8-3 cell add "W17.E (3b) / W12.C (3c)"; W18.A enumerate the
   four AZ adopts. [§4-2/3/4]

With the slate applied, the §5 attestation ("zero silent drops") becomes literally true.
