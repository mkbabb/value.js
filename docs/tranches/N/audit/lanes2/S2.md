# Lane S2 — the critic fold (K1 + K2 + K3 → the planning docs)

**Fleet**: second N-tranche deep-audit (lanes2). **Mode**: tranche development only — zero code
edits; doc edits confined to the planning docs + the granted sibling ask letter. **Date**:
2026-06-12. **Inputs**: `K1.md` (adversarial anchors, 8 findings), `K2.md` (coverage, 7-item
slate), `K3.md` (cross-repo coherence, 6 defects). Every critic claim was **independently
re-verified against the tree/corpus before folding** (greps + seds logged below); two K1
sub-claims were overruled with evidence.

## Files edited (the change list)

1. **`docs/tranches/N/WAVES-2.md`** — 16 amendments + the new **§6 end-matter** (the critic-fold
   disposition ledger incl. overrules):
   - header `:7`: "8 directives" → **6** (LEDGER `:90-97` = 6 bullets) [K1-F6/K2 §4-1]
   - W10.B: temporal-gate thresholds marked **CALIBRATE-AT-IMPLEMENTATION** (S1-authored;
     U-AURORA measured only the `breathing` dead baseline ±1–2/255); D5-8/D6 §5.1 cited for
     `colorEnergy`/`zones`; anchor `keys.ts:26-28` → `:24-28` [K1-F5, F8]
   - W12.C: "the U11 structural gate in CI" → **re-point the W10.D boot-smoke gate**
     (verify, not re-land — kills the double-landing ambiguity) [K2-X1]
   - W13.A: **thumb live-color clause added** (the W6.C carry, D1 §5 — the K2 genuine drop;
     not implied by the consume: the primitive's thumb is a size mechanism only) + the
     **U20b token** + W13 gate token "the thumb paints the live color" [K2-D1, §4-2]
   - W13.C: the **U21 dual-diagnosis conflict recorded** — D7-9's live measurement governs
     (pill text centered 3.5/3.5; glass-ui innocent); X-GU **A-5 demoted to
     verify-after-`pb-2`-deletion** (W-TABS confirm-no-regress row only) [K1-F3]
   - W16.E: `Katex.vue:20` → `:21` [K1-F8]
   - W17.B + gate: "14 bespoke families" → **12 demo-authored** (+2 glass-ui-SHIPPED
     `pop`/`fade-slide`, `transitions.css:23,63`, consumed at 5 sites); the consume-vs-author
     decision made explicit (author ONLY `pane`; consume the producer's `pop`+`fade`); the
     gate grep **scoped to demo-authored CSS** (else it can never go green) [K1-F1]
   - W18 preamble + W18.A: the letter **A′/F addendum** cited; **BA GREENLIT-and-executing**
     stamped; the four AZ adopts **enumerated** (Button `icon-sm` · Select `size` ·
     `clampLabel` · Tooltip mono); A′-1..6/F register ids threaded [K2 §4-4, K3-D1/D2/D3]
   - W13/W15 blocked lists: every blocked row now cites a **named filed register**
     (D5-1 → A′-6; LP2-2 → A′-1; LP3-1b → A′-2; LP3-3a → A′-3; LP2-4/5 → A′-4; LP1 → A′-5)
   - §3: the R3 AZ-notify carry **KILLED with rationale** (the grep-`proof:*` idiom is retired
     constellation-wide; the substance — W7.A easing additions were additive — recorded) [K2-D2]
   - §4: **inv-N-7 VIOLATED-at-HEAD added** with the precise mechanism (the demo's bare
     `watercolor-swatch` at `MixSourceSelector.vue:148` matches no reachable rule — glass-ui's
     defs are SFC-scoped, dist-emitted as `watercolor-swatch[data-v-cb0117af]`); closes W14.E
     [K1-F2]
   - §5: U26 → "W14.B (border-token root = W12.B)"; U31 → "W16.A (card-lock law codified =
     W12.E)"; the D8-3 cell now names 3b → W17.E, 3c → W12.C [K2-X2/X3, K1-F7]
   - §5 attestation: the **re-verified-at-fold** paragraph appended (34/34 U-rows each exactly
     once — census re-run post-edit)
2. **`docs/tranches/N/N.md`** — the inv-N-6 pin-target amendment echoed at its three remaining
   in-place sites: §3 item 1, §6 (the normative invariant table — the K3-D5 "a §6-alone reader
   pins 3.13.0" hole), §9; plus the §8 "all glass-ui rows now FILED" record.
3. **`docs/tranches/N/PROGRESS.md`** — W9 row: "still uncut" → 3.13.0 IS cut, pin = the BA cut
   (4.0.0) [K1-F4]; the dispatch-split §: same; W5 row: "zero phantom classes ✓" →
   **inv-N-7 RE-OPENED 2026-06-12** (`watercolor-swatch`) [K1-F2].
4. **`../glass-ui/docs/tranches/BA/coordination/VALUEJS-N2-ASKS-2026-06-12.md`** (the granted
   sibling ask doc) — the K3-demanded addendum, closing the P0 coverage hole:
   - **Register A′** (the perf producer cluster, live-traced): A′-1 GooBlob zombie second
     canvas (LP2-2) · A′-2 visibility/PRM gate (LP3-1b, P0) · A′-3 `card-*-shrink` layout
     keyframes → CLS 1.03 (LP3-3a, P0) · A′-4 `--dock-morph-t` 10-selector calc-cascade
     (LP2-4/5) · A′-5 aurora DPR cap (LP1) · A′-6 per-density dock glyph (D5-1)
   - **Register F** (the standing N.md §8 carries): F-1 C-DTS dts-emitting watch ·
     F-2 the value.js ranges — devDep `^0.10.0`→`^0.12.0` AND **the peer range
     `^0.10.0 || ^0.11.0` does not admit the published 0.12.0** (`package.json:768/799` —
     an S2-fold sharpening of the stale §8 ask) · F-3 `AuroraConfig` descriptor ·
     F-4 retired-classes manifest OR the recorded MIGRATION.md substitution
   - the **fold-by-batch deadline table** (C-1/A′-1/A′-2 before Batch 2 · A-1/A′-4 before
     Batch 2/3 · A-2+B/A-5/A-3/A′-6 before Batch 4 · A-4/A′-5 before Batch 6 · the rest by
     the cut) + the BA GREENLIT context correction [K3-D3]
   - **precision fixes**: "`dist/components/` does not exist" → "`.vue.d.ts`-only tree; the
     compiled JS sits flat at `dist/*.js`, unscanned" ×2 (verified live — the falsifiable
     premise un-falsified) [K3-D4]; "30 BA waves" → 29 [K3-D6.6]; the dead "N.W6.C" consume
     cite → W16.B-interim / W18.B-consume [K3-D6.2]; Routing extended to the A′/F fold order.
5. **`../keyframes.js/docs/tranches/K/VALUEJS-N2-ASKS.md`** — **verified clean, NO edits**
   (K3 found only cosmetic cite drift in the lane report X-KF-ITEMS.md, not the letter; grep
   confirms the letter carries no dead-W6 cite).

## Overrules (critic wrong; tree evidence)

- **K1-F8/SpectrumCanvas**: K1 claimed the `getBoundingClientRect` read is at `:108`; tree
  (`sed -n '105,112p'`) puts the guard at `:108` and the rect read at **`:109`** — WAVES-2's
  original anchor stands (L-PERF2's LP2-1 row concurs).
- **K1-F8/style.css**: K1 claimed the font tokens sit at `:63-65`; tree: `@theme {` at `:63`,
  the three `--font-*` tokens at **`:64-66`** — WAVES-2's anchor stands.
- **K1-F2's literal mechanism** (substance UPHELD, precision corrected): "0 definitions" is
  imprecise — glass-ui defines `.watercolor-swatch` SFC-scoped (`WatercolorDot.vue:140-187`);
  the violation is unreachability from the demo's bare class, and the §4 entry says so.

All other critic claims verified TRUE before folding: the 12-family census (grep = 12 demo +
8 producer-shipped), the LEDGER 6-bullet count, R3 `:125`'s orphaned notify, D1 `:226`'s thumb
carry, D7 `:194-199`'s measured U21 verdict, BA's GREENLIT PROGRESS, `dist/components/`'s
`.vue.d.ts` reality, the letter's zero hits for zombie/DPR/card-shrink/morph-t/AuroraConfig/
dts/devDep/retired-classes, and the glass-ui peer-range gap.

## Recorded, no doc edit (lane reports are evidence, not plans)

X-GU.md's "pre-greenlight" header + §5.1's "collapse to ONE publish" judgment are
stale/superseded (recorded in WAVES-2 §6 — the letter's A-2/C-3 split governs; a synthesis
reader must not de-prioritize A-2). X-GU-ITEMS `:32/:46` + X-KF-ITEMS `:93/:126` carry the
same superseded cites. K3-D6.3 (provenance phrasing) and D6.5 (cite drift) — no action.

## Coverage re-verification (post-fix)

- §5 census re-run: **34 U-rows, each exactly once** (`grep -oE '\| U[0-9]+[ab]? \|' | uniq -c`
  → all 1); the U26/U31 splits now carry the declared parenthetical convention.
- K2's two genuine drops: **homed** (thumb live-color → W13.A + gate; AZ notify → KILLED §3).
- K3's ~10-row coverage hole: **closed** — every "Blocked-on-BA" row in W13/W14/W15/W18
  anchors on a named letter register (A-1..5, A′-1..6, B, C-1..3, D, E, F-1..4); the §2 DAG's
  "asks already filed" is literally true again.
- The §5 attestation is now literally true (the K2 §6 condition met).

## The final wave table (post-fold; unchanged in structure, corrected in content)

| Wave | One line | Blocked-on-BA? |
|---|---|---|
| N.W10 | Functional truth: U9 reset · U33 aurora (gate thresholds calibrate-at-impl) · save P0 · kC · the cascade kill (U11) + single-mount | no |
| N.W11 | Library color-SOTA (U10): gamut-policy re-anchor + §13.2 oracle + egress | no |
| N.W12 | GRAND HIERARCHY: fonts/accent FIRST · layout clamp + container queries (re-points the W10.D gate) · depth laws · ramp/φ | gray-token half at pin |
| N.W13 | CONTROLS: spectrum slider (+ **thumb live-color**, U20b) · dropdowns · U21 (`pb-2`; A-5 = verify-after) · a11y | size-axis, Select bound/rung (filed) |
| N.W14 | CARDS: PaletteCard first-class · depth sweep · glassy skeletons · CTAs · **inv-N-7 closes (E)** | watercolor-ghost C-2 |
| N.W15 | PERF: idle floor · reflows · harness born-RED | dock FLIP A-1 + **A′-1..5 (all filed)** |
| N.W16 | PER-PANE: picker hero · gradient+easing interim · mix (inv-N-9 closes) · extract · docs (`Katex.vue:21`) · modern-web | configurator → W18 |
| N.W17 | SHELL+MOTION+POPS: dock scale · **12→(1 authored + 2 consumed) families** · view-select · celebration · nomenclature | spring-clock at pin |
| N.W18 | Consume-at-pin: BA-cut re-pin + adopt sweep (**4 AZ adopts enumerated**) + A′/F verifies · configurator · kf/fourier | **the BA cut (4.0.0)** |
| N.W8′ | Hygiene + wire-deploy ceremony + doc-truth | no |
| N.W9′ | v1.0.0 + π + FINAL.md (pin discharged by W18; inv-N-6 amended at §3/§6/§9) | pin = the BA cut |
