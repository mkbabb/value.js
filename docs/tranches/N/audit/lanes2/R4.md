# Lane R4 — precepts + no-legacy sweep (second N-fleet)

**Scope**: re-affirm the standing mandates against the N-landed code (W0–W5+W7 landed; W6
DIED un-implemented; W8/W9 pending). Sweep the new/changed demo+src surfaces for compat shims,
dead branches, TODO/deferred markers, 400-LoC breaches, un-PRM'd loops (inv-N-9 census refresh).
Build the exact `docs/RELEASE.md` staleness contradiction list (the W8.D rewrite input) and the
`CLAUDE.md` drift inventory.

**Method**: live app probed on my own page (`localhost:9000`, console-clean, `<main aria-label="Color
tool panes">` mounts → inv-N-1 holds live). Code grounded at file:line against HEAD `b112e46`
(199fd15 + 0.12.0 + the user-audit doc commit). NO builds/installs/tests run.

**Verdict**: the standing mandates hold **broadly** on the N-landed surfaces — but TWO real
precept breaches survive, both traceable to **W6's death**, plus a stale dead-comment from the
N.W5 fork deletion. The doc-truth surface (`RELEASE.md`, `CLAUDE.md`) is **comprehensively
stale** and the W8.D/W8 inventory below is exact.

---

## 1 — REAL FINDINGS (precept breaches on N-landed code)

### R4-1 — inv-N-9 LIVE HOLE: the mix-canvas RAF loop is un-PRM-gated [P1]

`demo/@/components/custom/mix/composables/useMixingAnimation.ts` runs a continuous
`requestAnimationFrame` render loop (`render()` re-arms at lines **77, 83, 99, 189, 196**;
`running` toggled at `:195` start / `:200` stop) with **ZERO `prefers-reduced-motion` gating**.
This is **exactly** the one LIVE hole inv-N-9 (N.md §6 lines 200–203) names — and it was
chartered for **W6.C** (`gate the mix-canvas RAF (useMixingAnimation.ts:116,206)`), the wave that
**DIED un-implemented** (PROGRESS.md). So the invariant the charter claims as "verified census…
gated at W6.C" is **NOT satisfied at HEAD**.

- The global CSS guard `demo/@/styles/animations.css:60` (`@media (prefers-reduced-motion: reduce)`)
  neutralises CSS animations/transitions only — it **cannot** stop a JS RAF loop. The file's own
  comment (`animations.css:53–58`) confirms the architecture: WebGL/aurora RAFs are "handled
  separately"; it is **silent on the mix-canvas RAF** — confirming that loop is the uncovered one.
- Sweep result: `grep prefers-reduced-motion|usePreferredReducedMotion|reducedMotion` over
  `demo/@/` + `src/` (ex-`ui/`) returns **zero JS-level gating sites**. Every PRM gate in the demo
  is CSS-media-query-only (`animations.css` + the 4 `demo/hero-lab/*` heroes which DO gate their JS
  via `window.matchMedia`). The mix-canvas is the lone live, demo-shipped, JS-driven loop with no
  guard.
- RAF census (inv-N-9 refresh, ex-`ui/`): **8 demo RAF sites** across 4 files
  (`useMixingAnimation.ts` ×5, `useInertiaGesture.ts` ×2 — one-shot inertia, self-terminating;
  `ImagePaletteExtractor.vue:202` one-shot `await new Promise(raf)`; `SpectrumCanvas.vue:97`
  one-shot debounce; `ColorInput.vue:240` one-shot focus) + the `src/utils.ts:194` wrapper.
  Only `useMixingAnimation` is a *continuous* loop → the census matches the charter's "8 sites,
  one live hole" exactly, and the hole is open.

**Disposition**: re-fold into the re-divined W6 (or a standing-PRM micro-wave). The fix is a 1-line
early-out in `render()` on `matchMedia('(prefers-reduced-motion: reduce)').matches` (or render a
single settled frame, mirroring the goo-blob/aurora pattern the `animations.css` comment cites).

### R4-2 — 400-LoC demo cap BREACH: `ComponentSliders.vue` = 418 LoC [P2]

`demo/@/components/custom/color-picker/controls/ComponentSliders.vue` is **418 lines** — over the
hard demo cap (CLAUDE.md: "every `demo/` file (excluding shadcn-vue) stays ≤ 400 LoC"; the
"no `demo/` god module" precept, `feedback_no_god_modules.md`). It is the **only** demo file over
the cap (full sweep ran; next is `PaletteCard.vue` at 388).

**N-introduced**: at 0.11.0 (`e8cc1fb`) and 0.11.2 (`0cb5dd2`) the file was **333 LoC**; N.W1's
carousel→`role="tablist"` gestalt rewrite (`d9c3b9f`, the boot-break fix at `:117`) pushed it to
**418** — a **+85-line** breach the W1 wave introduced and no gate caught (there is no LoC gate in
CI; the cap stands "by review" per the precept, and review missed it).

**Disposition**: decompose per the precept — the obvious lift is the channel-letter rail + the
stagger-key machinery (`:158`) into a sub-component, which also serves U13 (restore the ellipse
veil card) and U14 (channel-letter centring) cleanly. Fold into the W6 ComponentSliders work-order.

### R4-3 — STALE DEAD-COMMENT: `animations.css` + `DESIGN.md` cite the deleted `useMetaballRenderer` [P3]

N.W5.A deleted the blob fork wholesale: `demo/@/components/custom/goo-blob/`,
`demo/@/components/custom/watercolor-dot/`, and `demo/@/lib/animation/webgl-utils.ts` are **gone**
(verified absent), the blob now consumes `@mkbabb/glass-ui/goo-blob`. But two demo docs still
name the dead module:

- `demo/@/styles/animations.css:56` — "The goo-blob WebGL RAF loop is handled separately in
  **useMetaballRenderer**" — that composable no longer exists in `demo/`; the goo-blob RAF is now
  glass-ui-owned. The comment is a dangling reference to deleted code (precept: no-legacy includes
  no legacy *comments* pointing at extirpated modules).
- `demo/DESIGN.md` also references `useMetaballRenderer` (grep hit).

**Disposition**: trivial comment fix — fold into the W5-remediation or W8 doc-truth pass. Low
severity but it is a no-legacy-comment violation and will mislead the next reader.

---

## 2 — RELEASE.md staleness inventory (the EXACT W8.D contradiction list)

`docs/RELEASE.md` is **comprehensively stale** — it documents a publish process whose mechanism,
gate matrix, and referenced files have all been superseded. Every contradiction below is verified
against HEAD. This is the exact list the W8.D rewrite needs.

| # | RELEASE.md claim (line) | HEAD truth | Severity |
|---|---|---|---|
| RM-1 | §Authority L3: "**There is no GitHub Actions release workflow** — publishing is … human-driven" | `.github/workflows/release.yml` **EXISTS** (tag-`v*.*.*`-triggered `npm publish` with provenance + changesets). The central premise of the whole doc is **false**. | **P0 — inverts the doc's thesis** |
| RM-2 | §1.3, §2, §3.6, §4 reference `node.js.yml` as "the CI matrix" / "the CI workflow" (L34, L42) | `.github/workflows/node.js.yml` **does not exist**; CI is `ci.yml` (unified K.W2 workflow) + `deploy-pages.yml` + `release.yml` | **P0** |
| RM-3 | §2 lists the gate matrix as **12 `proof:*` scripts** (`proof:dts-layout`, `:resolution`, `:no-deprecated`, `:no-ts-ignore`, `:as-any-budget`, `:as-unknown-as-budget`, `:no-deep`, `:no-bare-builtins`, `:codemod-publication`) at L47–59, +§3.6 L133 | The `proof:*` idiom was **RETIRED** (user-judged "overfit junk", `feedback-proof-idiom-retired.md`); **zero** `proof:*` scripts in `package.json`. The real CI ladder is lint → typecheck → build (+size budgets) → vitest → api test → bench → playwright → boot-smoke → css-emission-probe → abrogation-sweep. | **P0 — references a retired discipline** |
| RM-4 | §3.1, §3.6 (L79–84, L133), §4 (L178), §5 (L189) treat `CHANGELOG.md` as a live, edited artifact ("author the new section"; cites v0.6.0/0.7.0/0.8.0/0.9.0 templates) | `CHANGELOG.md` **deleted** (staged ` D` in git status; retired with the proof idiom per the feedback memory). The ceremony's "edit CHANGELOG" step references a deleted file; releases now use **changesets** (`.changeset/`) — RELEASE.md mentions changesets **zero** times. | **P0** |
| RM-5 | §Authority L3, §1.4 (L32), §3.6 (L131), §5 (L189) reference `CONTRIBUTING.md` as the authority for sibling-checkout + `--legacy-peer-deps` + codemod docs | `CONTRIBUTING.md` **deleted** (staged ` D`). Four cross-references dangle. | **P1** |
| RM-6 | §3.6 L136: "files: includes `scripts/migrate-*.mjs` — published codemods (e.g. `migrate-keyframes-js-lerp.mjs`)" | `package.json` `files` is **`["dist"]` only**; **no `scripts/migrate-*.mjs` exist** (glob: no matches). The published-shape description is wrong. | **P1** |
| RM-7 | §Cadence L5 + L84: "v0.6.0 closed D, v0.7.0 closed E, v0.8.0 closed F, v0.9.0 closed G" | The version↔tranche map is frozen at G. Current is **0.12.0**; F handed off at 0.11.0; v1.0.0 is the N close. The mapping table omits H,I,J,K,L,M,N and the 0.10→0.12 line entirely. | **P2 — incomplete, not wrong** |
| RM-8 | §2 L48: "`dist/value.js` size budget (≤ **145 KB raw**)" | `ci.yml` budget is **148480 bytes** (≤145KB ✓ consistent) BUT the doc omits the **whole-tarball gate** (npm-pack unpackedSize ≤ 320KB / 327680 bytes, N.W7.B) — the structural backstop that is now the load-bearing size gate. | **P2 — omission** |
| RM-9 | §2 L60: "`npm run bench` + bench-gate (L8 ≥ 5×, HSL→RGB ≥ 2×, nameParser ≥ 5×)" | Bench gates are accurate ✓ (ci.yml matches) — **this row is the one §2 line still true**. | OK |

**Net**: RM-1..RM-6 are P0/P1 falsehoods (the doc's authority premise, its CI filename, its entire
gate ladder, and three referenced files are all inverted/deleted). The W8.D "5 contradictions"
estimate **undercounts** — there are **6 P0/P1 contradictions + 2 P2 omissions**. The honest
rewrite must: (a) reverse the "no release workflow" thesis → document `release.yml` + changesets +
provenance; (b) repoint to `ci.yml`; (c) excise the entire `proof:*` ladder and CHANGELOG/
CONTRIBUTING/VENDOR-POLICY references; (d) correct the `files`/codemod shape; (e) extend the
version map through N/v1.0.0; (f) add the tarball gate.

---

## 3 — CLAUDE.md drift inventory (W8.D items, exact)

| # | CLAUDE.md line | Claim | HEAD truth |
|---|---|---|---|
| CM-1 | L86 | "9 collections / **27 indexes**" | **22** `createIndex` calls in `api/src/db.ts` (N.W3 dropped 26→22). 9 collections ✓. → "9 collections / **22 indexes**" |
| CM-2 | L11 | `npm run dev # dev server (**Vite default port**)` | `package.json`: `"dev": "vite **--port 9000**"`. Not the default port. |
| CM-3 | L20 | `npm run typecheck # **vue-tsc --noEmit (library + demo)**` | Actual: `vue-tsc -p tsconfig.lib.json --noEmit && vue-tsc -p tsconfig.demo.json --noEmit` (the K.W2 tsconfig split). The one-liner hides the split-config gate. |
| CM-4 | L113 | "Runtime: `@mkbabb/parse-that@**^0.7.0**`" | `package.json`: `^0.9.0` (N.W7 re-pin; F9 console-leak close). |
| CM-5 | L82, L86 | "`assets/docs/` — **10** color space reference pages (Vue + KaTeX)" | **11** files, and they are **`.md`** not `.vue`: hex, hsl, hsv, hwb, kelvin, lab, lch, oklab, oklch, rgb, xyz. |
| CM-6 | L7–11 (Build block) | Lists `build`, `build:watch`, `gh-pages`, `dev` only | Omits the **new N scripts**: `boot-smoke` (inv-N-1), `abrogation-sweep` (inv-N-10), `css-emission-probe` (inv-N-7/W2.B), plus `dev:hero-lab` / `build:hero-lab`. The verify block (L17–20) should name `npm run boot-smoke` + `npm run abrogation-sweep` as the boot-truth gates. |
| CM-7 | L62 (proof note) | The "grep-based `proof:*` invariant scripts … retired" note | ✓ accurate — but the test/spec counts: MEMORY.md says vitest 1709/36, playwright 42; the N.W7 commit says **1709 tests**, api **214** (N.W3, 161→214). CLAUDE.md defers counts to FINAL.md (correct policy) — no number to fix in CLAUDE.md itself, but the **per-tranche FINAL.md does not yet exist** (N un-closed). |

**Note (not a CLAUDE.md fix)**: the brief's "tests 1709/214" framing — vitest **1709**, api **214** —
matches the N.W7 (`ed0dd00`: "1709 tests") and N.W3 (`fe3c00c`: "161→214 tests") commits.
"indexes 22" matches my count. These are the authoritative N-landed counts for the FINAL.md.

---

## 4 — STANDING MANDATES re-affirmed (what HOLDS — no breach)

- **no-legacy / no-backwards-compat shims**: clean on N surfaces. `useLayerTransition` fork
  (108 LoC) is **deleted**, now consumed from `@mkbabb/glass-ui/dock` (`ActionBarLayer.vue:8`);
  the `subLayerProps` (`:67`) is the kept 7-LoC v-bind, NOT the retired `layerProps` shim
  (N.W5.D). check-types.mjs band-aid **retired** (absent). The blob/watercolor/webgl-utils forks
  **deleted**. `as any` in `src/` = **0** (CLAUDE.md budget holds); the lone demo `as any[]`
  (`color-utils.ts:40` Ctor reconstruction) is the documented clone-reinterpret irreducible.
- **inv-N-3 (one color-resolution path)**: HOLDS. `cssToRawColor`/`cssToRgb255`
  (`demo/@/lib/color-utils.ts:21,52`) is the **single** library-backed resolver
  (`parseCSSColor` → `normalizeColorUnit` → `color2`, zero DOM). The `getContext("2d")`/`fillStyle`
  sites in `useMixingAnimation.ts` are canvas **painting**, not color resolution — they call
  `cssToRgb255` for the resolve. No hidden `<div>`+`getComputedStyle` resolver survives.
- **inv-N-7 (zero phantom classes)**: HOLDS live. `glass-elevated` cleanly retired (0 demo uses);
  `glass-floating` resolves (live probe: box-shadow + `backdrop-filter:blur(16px) saturate(1.18)`,
  glass-ui-owned) used at 3 sites; `dashed-well` minted (`utils.css:56`, 2 uses); `pastel-rainbow-text`
  real (live RESOLVES); `stagger-children` defined (`animations.css:40`, inside the
  `prefers-reduced-motion:no-preference` block) + used (`ComponentSliders.vue:4`). All five former
  phantoms now resolve.
- **inv-N-1 (boot-truth)**: HOLDS live — console **clean** (zero error/warn), `<main aria-label="Color
  tool panes">` mounts (matches the boot-smoke `getByRole("main",{name:"Color tool panes"})` gate;
  my literal-`[role=main]` probe missed the implicit ARIA role — not a gate bug).
- **proof-idiom-retired**: HOLDS in `package.json` (0 `proof:*` scripts) and `ci.yml`. The ONLY
  live re-surfacing is `docs/RELEASE.md` (R4/RM-3 above) — a doc that predates the retirement.
- **TODO/deferred markers** on N surfaces: only **1** genuine deferral marker —
  `PaletteSlugBar.vue:16` (`<!-- TODO: collapse to <Button size='icon-sm'> when glass-ui ships the
  rung (Q.md ask #7) -->`), a cohort-ask placeholder, not a workaround. All other "legacy"/"temporary"
  hits are **domain vocabulary** (CSS legacy color syntax in `src/units/color/*`; `PaletteKind =
  "temporary"|"saved"|"remote"` in `lib/palette/utils.ts:18`) — not compat shims.
- **tranche-docs-lean**: N.md / PROGRESS.md are lean and structural; no proof-ceremony bloat.

---

## 5 — W8/W9 carry-items confirmed pending (correct — W8/W9 are PLANNED)

Not breaches (these waves haven't run) — confirmed-state for the W8 work-order:

- `$OUT` artifact (66 KB) still present at repo root → W8.B (delete + `.gitignore` the class).
- `CHANGELOG.md` / `CONTRIBUTING.md` / `VENDOR-POLICY.md` staged-for-deletion (` D`) but **not
  committed** → W8.B (commit the deletions). RELEASE.md still cross-references all three (R4/§2).
- `demo/CLAUDE.md` = **215 LoC** → W8.D (one-liner reduction). It is `M` in the snapshot but has no
  working-tree diff vs HEAD (committed); heed the L-close caveat (a prior inadvertent revert).
- `@mkbabb/keyframes.js` devDep (`package.json:80`) is a confirmed **phantom**: **zero** `import`
  of it in `src/` (all grep hits are prose/comments) → N.md §3.2 / N.W9.A drops it.
- `@mkbabb/glass-ui": "file:../glass-ui"` (`package.json:79`) → N.W9.A registry pin `^3.13.0`
  (gated on the uncut 3.13.0 — the one BOOK-with-trigger).
- `VITE_API_URL` hack still live (`demo/@/lib/palette/api/client.ts:29`) → was M.W6.D/N.W6.D
  "delete the VITE_API_URL hack (typed degraded-backend)"; **W6 died** → re-fold into the
  re-divined W6.

---

## 6 — Top items to fold into the re-divined waves (priority order)

1. **[P1] R4-1** — gate the mix-canvas RAF (`useMixingAnimation.ts`) on `prefers-reduced-motion`;
   inv-N-9 has a LIVE hole the dead W6.C was supposed to close. The brief's "inv-N-9 census
   refresh" → **the invariant is currently FALSE at HEAD.**
2. **[P2] R4-2** — decompose `ComponentSliders.vue` (418 → ≤400); N.W1 introduced an
   un-gated 400-LoC demo-cap breach (+85 lines).
3. **[P0-doc] RELEASE.md rewrite** — 6 P0/P1 contradictions (not 5): the "no release workflow"
   thesis is inverted, `node.js.yml` is gone, the entire `proof:*` ladder + CHANGELOG/CONTRIBUTING
   references are dead. The exact list is §2 above.
4. **[P1-doc] CLAUDE.md drift** — 6 concrete corrections (§3): indexes 27→22, port (9000),
   typecheck split-config, parse-that ^0.7→^0.9, 10→11 `.md` docs pages, + name the new
   boot-smoke/abrogation-sweep/css-emission-probe scripts.
5. **[P3] R4-3** — fix the `animations.css:56` + `DESIGN.md` dead-comment refs to the deleted
   `useMetaballRenderer` (no-legacy-comment).
