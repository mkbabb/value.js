# N.audit — Lane A1: Cumulative prompt ledger + mandate recap

**Scope**: every user mandate across the whole arc (A..M) with its disposition **at HEAD `0cb5dd2`
(branch `tranche-f-handoff`, 2026-06-11)**, tree-verified, plus the NEW 2026-06-11 mandate folded
as the latest entry. Read-only audit; tranche-development, not implementation.

**Sources read**: `docs/tranches/M/audit/prompt-coverage.md`, `docs/tranches/M/M.md §2`,
`docs/tranches/M/audit/fold-ledger.md`, `docs/tranches/L/FINAL.md`, `docs/tranches/K/audit/request-coverage.md`,
`docs/tranches/M/PROGRESS.md`. Every count below is re-verified against the live tree (the brief's
hard rule: do **not** trust documentation numbers).

---

## §0 — Headline verdict

The coverage docs (K request-coverage, M prompt-coverage) are **honest about history** — no mandate
was ever silently dropped, and the cross-walks A→J→K→L→M are accurate **as written at their authoring
dates**. BUT the M ledger is now **7 days stale**, and four load-bearing dispositions have shifted at
HEAD. The net:

- **2 of M's planned waves are now partly or wholly moot** (M.W1.A precept fossil; M.W7 dock-C1) —
  the work landed *outside* M's plan (release 0.11.1/0.11.2 + a later glass-ui cut), which **must be
  reconciled** into N rather than re-executed.
- **1 fresh P0 regression** the entire doc corpus is blind to: the demo typecheck is **RED with 3
  errors** from glass-ui API skew (`glass-carousel`→`carousel`, `BouncyTabs`→`SegmentedTabs`).
- **The two oldest mandates remain UNADDRESSED-IN-CODE** (aurora-derive m.13/D-open#6; blob-extirpation
  D-open#6) — exactly as M claimed — but their *blockers are now cleared* in glass-ui 3.10.1, so they
  are finally landable.
- The big architectural debt M scoped (WithId 25-cast class) is **fully intact and UNADDRESSED** —
  verified live at 25 casts / 0 `WithId` in `api/src`.

---

## §1 — The cumulative prompt ledger (every mandate, disposition at HEAD)

Disposition legend: **ADDRESSED** (landed + tree-verified) · **PARTIAL** (some lands, residual open) ·
**UNADDRESSED** (folded but no code yet) · **SUPERSEDED** (folded into a later tranche's plan) ·
**ONGOING** (standing invariant) · **REGRESSED** (was addressed, broke at HEAD).

### A-arc (turn-1 regression + the m.1–m.13 design mandates)

| # | Mandate | Source | Disposition @HEAD | Tree evidence |
|---|---|---|---|---|
| 1 | Un-break regression (`stops.length`, font 403, dropdowns/dock/animations) | A turn-1 | ADDRESSED | `A/FINAL.md §2`; cold-boot clean |
| 2 | Styling resilience (idiomatic Tailwind/glass-ui; no monolith/deprecated CSS) | A m.1 | ADDRESSED | A.W2 + D.W4 |
| 3 | Design audit (tokens, `@apply`, sizes/radii/shadows/hover/pop-ups) | A m.2 | ADDRESSED | A.W3 + D.W4 (`demo/DESIGN.md`) |
| 4 | Four-state buttons | A m.3 | ADDRESSED | A.W4 |
| 5 | Modals/dropdowns/pop-ups + hierarchy | A m.4 | ADDRESSED | A.W4/W5 |
| 6 | Duplicated components consistently styled | A m.5 | ADDRESSED | A.W3/W4 |
| 7 | Golden-ratio hierarchies; abrogate spreadsheet lists | A m.6 | ADDRESSED | A.W3 φ scale |
| 8 | Colocation + idiomatic `@apply` | A m.7 | ADDRESSED | A.W2 |
| 9 | Root-level component restyling | A m.8 | ADDRESSED (demo) / SUPERSEDED→glass-ui (variants) | demo done; glass-ui variants now in 3.10.1 src |
| 10 | **glass-ui for ALL styling/components** | A m.9 | PARTIAL | most components consume glass-ui; **demo still holds bespoke `goo-blob`/`watercolor-dot` dirs that now EXIST as glass-ui subpath exports** (see §2 row B) |
| 11 | Flatten complex components | A m.10 | ADDRESSED | Dock/App/PaletteDialog (A→H) |
| 12 | Gaps in value.js **AND** glass-ui | A m.12 | ADDRESSED (value.js) / SUPERSEDED (glass-ui AND) | library gaps closed B.W3 |
| **m.13** | Playwright user+admin flows; **blob+aurora idiomatic abstraction** | A m.13 | e2e ADDRESSED / **aurora+blob UNADDRESSED-IN-CODE** | see §2 rows A,B — the two oldest open mandates |

### B/D/E/F/G/H/I arc (layout, api refactor, fail-explicit, no-legacy, CRUD-conformance)

| # | Mandate | Source | Disposition @HEAD | Tree evidence |
|---|---|---|---|---|
| 14 | Dock sizing/layout contrived/overfit | B-4 | ADDRESSED | `--dock-pos` deleted |
| 15 | "Hung on e2e" | B-4 | ADDRESSED | 5-project smoke + CI |
| 16 | Simplify layout (preserve rendered styling) | B-4 | ADDRESSED | B.W1/W2 |
| 17 | Contract-v2 (drop `development`/`build:watch`; proof-resolution) | D-open#1 | **ADDRESSED @HEAD** (was the M.W1.A blocker) | `package.json` exports map has **NO `development` key** (lines 25–30: `types`/`import`/`default` only); cleaned in 0.11.1, published 0.11.2. **M.W1.A is moot.** |
| 18 | Surgical `api/` refactor; DI; no god >500 LoC | D-open#2 | ADDRESSED | D.W2; largest api file 296 LoC (`crud.ts`) |
| 19 | Fail-explicit; no silent fallbacks unless befitting; excise legacy | D-open/B-4 | ADDRESSED | L.W1 typed `ApiError`; inv-L-4 grep 0 |
| 20 | Frontend encapsulation/composables; split >500 LoC | D-open | ADDRESSED | D.W3 + H.W3 (≤400 demo) |
| 21 | Localized design-idiom catalog; no nested imports | D-open | ADDRESSED | D.W4 + D6 |
| 22 | Full Playwright every user+admin view (mock) | D-open#5 | ADDRESSED | 5 projects |
| **23/D#6** | **Aurora derived from a singular color (not cloud default)** | D-open#6 | **UNADDRESSED-IN-CODE** | producer `deriveAurora` SHIPPED in glass-ui (`src/composables/color/index.ts`); demo `AuroraPane.vue` consumer **unwired** — see §2 row A |
| **24/D#6** | **Extirpate blob facilities into glass-ui (remove bespoke)** | D-open#6 | **UNADDRESSED-IN-CODE** | demo still has `goo-blob/`, `watercolor-dot/`, `webgl-utils.ts`; glass-ui NOW exports `./goo-blob`,`./watercolor-dot` — see §2 row B |
| 25 | NO god modules/workarounds/fallbacks; DRY/KISS; lint+typecheck each interval | D-open + standing | ONGOING (1 REGRESSED probe) | dispatch.ts 349 (under cap); **but typecheck is RED at HEAD** — see §2 row C |
| 26 | Architectural transpositions (elegance/simplicity/performance) | E-open + standing | ONGOING | folded into N (WithId, color-resolver, dispatch extraction) |
| 27 | NO legacy code (retire `@deprecated`) | E/F + standing | ONGOING | upheld |
| 28 | DEEPLY audit with N agents; recap ALL prompts; planning-only at open | every open | ONGOING | **THIS audit is the latest discharge** (32 agents) |
| 29 | Analyze speedtest/glass-ui/fourier work | E-open | ADDRESSED | E/G/H peer audits |
| 30 | Relay carry-forward items for ratification | G-open | ONGOING | M's fold-ledger; N re-ratifies |
| 31 | "No deferrals" (the F thesis) | F-seed | ONGOING | F1; N folds chronic + fresh |
| 32 | Fix cross-repos (CRUD-CONTRACT v2 conformance) | I-open | ADDRESSED | I closed; api 161/161 |
| 33 | Constellation WAVE-D: REMIX/atom-diff; VAL re-gate | J-open | ADDRESSED (atom-diff/publish) / VAL UNADDRESSED | `api/src/lib/crud/atomdiff.ts`, routes `diff.ts`/`publish.ts` SHIP; J [P0] `visibility="public"` filter at `crud-list.ts:114`; **VAL-1 OKLab aurora-LUT still ship-or-KILL** (folded M.W5.B) |

### K/L/M-arc

| # | Mandate | Source | Disposition @HEAD | Tree evidence |
|---|---|---|---|---|
| K-1 | Cross-repo cohesion (glass-ui gaps, goo-blob→glass-ui, aurora-derive, OKLab dedup, acyclic deps, pane system, vue-router 5, modern-web, fold, recap, no-legacy, tranche-only) | 2026-06-02 | **SUPERSEDED→M** | M.md §0 supersede-K; most clauses still open (see M rows) |
| S1 | Execute → close tranche L (parallel agents, gate on green, no workarounds) | this-arc | ADDRESSED | L CLOSED `66dcd68`; api 161/161; inv-L-1..9 verified |
| S2 | Finish L close-audit; flag demo/CLAUDE.md revert | this-arc | ADDRESSED | committed; demo/CLAUDE.md recovered (but **dirty again at HEAD** — see §4) |
| S3 | 12-agent post-L deep audit; recap ALL; fold deferred; gestalt; planning-only | 2026-06-04 | ADDRESSED (= M.W0 charter) | M authored; **never ratified** (`M/PROGRESS.md`: "hard-blocked on explicit user ratification") |
| M.W1.A | Delete `development` export key | M | **ADDRESSED-by-release** (not by M) | exports map clean @HEAD — M.W1.A is moot |
| M.W1.B | Repoint demo self-alias src→dist; retire band-aids | M | UNADDRESSED | `vite.config.ts:37` self-alias still → `src/index.ts`; `dedupe`/`fs.allow`/`check-types.mjs` band-aids live |
| M.W1.C | reka `^2.0.0`→`^2.9` align | M | UNADDRESSED | demo still `reka-ui` behind glass-ui's reka |
| M.W2.A | Desktop pane-visibility P0 (`@source`) | M | UNADDRESSED | folded |
| M.W2.B | **WithId read-side transposition (25 casts → 0, 4 models)** | M | **UNADDRESSED — verified live** | `grep -E 'as [A-Z]\w+ & \{ _id' api/src` = **25** (Palette ×17, ProposedName ×5, Tag ×2, AdminAuditEvent ×1); `grep WithId api/src` = **0**. The escape class is fully intact. |
| M.W3 | 2 color-resolvers→1; parseCSSColor typed; PRM hole | M | UNADDRESSED | `useWatercolorBlob.ts:108,128` RAF; `parseCSSColor` memoize untyped (`src/parsing/color.ts:635`) |
| M.W4 | Publish value.js 0.11.0 | M | ADDRESSED+ (0.11.2 published) | exceeds the M.W4 target; spine-head done |
| M.W5 | Aurora consummation (C2 + VAL-1) | M | UNADDRESSED | producer shipped, consumer unwired |
| M.W6 | Modern-web + vue-router 4→5; VIEW_MAP; dispatch extraction; id-honesty; inv-K-5 | M | UNADDRESSED | `vue-router ^4.6.4` (package.json:110); dispatch.ts not extracted to mix.ts |
| M.W7 | glass-ui 3.3.0 cohort: blob C3 + dock C1 | M | **PARTIAL — anchors moved** | dock C1 **FIXED in glass-ui** (`GlassDock.vue:389/395` `:inert="!expanded"`/`"expanded"`); blob primitives **EXIST as glass-ui exports** (`./goo-blob`,`./watercolor-dot`); demo extirpation **NOT done** — see §2 row B |
| M.W8 | Infra: rsync→git-checkout deploy; CF-Pages; color vhost | M | UNADDRESSED | folded |
| M.W9 | v1.0.0 close + π + doc reconciliation | M | UNADDRESSED | v0.11.2 at HEAD; doc-drift live (§4) |

---

## §2 — The four shifted dispositions (the reconciliation the N plan MUST do)

### Row A — Aurora-derive (m.13 / D-open#6) — UNADDRESSED, now landable
- Producer `deriveAurora` **shipped** in glass-ui local checkout 3.10.1: `src/composables/color/index.ts`,
  `src/components/custom/aurora/composables/{color,atoms}.ts`.
- Demo consumer `demo/@/components/custom/panes/AuroraPane.vue` exists but is **not wired to derive**.
- M.W5 named the export correctly (`deriveAurora`, not the ghost `deriveAuroraPalette`). The blocker
  M cited ("producer unwired") is cleared on glass-ui's side — N can wire it.

### Row B — Blob-extirpation (D-open#6 / m.9) — UNADDRESSED, now landable
- Demo STILL holds the bespoke facilities: `demo/@/components/custom/goo-blob/`,
  `demo/@/components/custom/watercolor-dot/`, `demo/@/lib/animation/webgl-utils.ts` — all present.
- glass-ui 3.10.1 NOW exports `./goo-blob`, `./watercolor-dot`, `./aurora` (verified in its exports map).
  **M.W7's "re-anchor on glass-ui 3.3.0" is stale** — the primitives landed at a *later* cut (≥3.10.1).
- N's blob thrust (b) should: (1) extirpate the demo dirs onto the glass-ui exports, AND (2) the user's
  NEW ask — *compare demo-impl vs glass-ui-impl, divine increased expressivity + satellite metaballing*.

### Row C — typecheck RED (NEW regression, doc-corpus blind) — P0
- `npm run typecheck` is **RED with 3 errors** (verified live, full run):
  - `ComponentSliders.vue:117` — `Cannot find module '@mkbabb/glass-ui/glass-carousel'` (renamed `carousel`).
  - `MixSourceSelector.vue:4` — `'@mkbabb/glass-ui/tabs' has no exported member 'BouncyTabs'`.
  - `PaneSegmentedControl.vue:18` — same `BouncyTabs` (renamed `SegmentedTabs`; glass-ui
    `tabs/index.ts:1` AX.W53 unified the BouncyToggle/BouncyTabs/UnderlineTabs trio → `SegmentedTabs`).
- **This is a REGRESSION of mandate 25** (lint+typecheck must be green each interval). It arose from
  the demo consuming a glass-ui far newer than M anticipated (3.10.1 vs the planned 3.3.0). No tranche
  doc mentions it. **N must fold a glass-ui-API-realignment lane** (rename the 3 import sites) as a
  fast unilateral hardening item — a prerequisite for any green-gate.

### Row D — Dock C1 (M.W7 sub-item) — ADDRESSED-by-glass-ui
- M.W7 worried `GlassDock.vue:359 :inert still on raw expanded`. At glass-ui 3.10.1 the bug is
  **FIXED**: `GlassDock.vue:389 :inert="!expanded || undefined"`, `:395 :inert="expanded || undefined"`.
  N should *verify the dock e2e green* rather than re-author the fix (it's glass-ui's, already landed).

---

## §3 — The NEW 2026-06-11 mandate (latest ledger entry)

| # | Mandate (verbatim intent) | State | N disposition |
|---|---|---|---|
| **N-1** | 32-agent deep audit of plan/waves/all changes; devise path forward; **no quick solutions, no workarounds; idiomatic gestalt; architectural transpositions for elegance/simplicity/performance; NO legacy**; delineate chronically-deferred AND freshly-deferred and fold into new tranche; recap ALL prompts ever + ensure addressed; **planning only** | **THIS DELIVERABLE** | the 32-lane audit → tranche N (planning-only at open). This A1 ledger discharges the "recap ALL prompts" clause. |
| **N-1a** | **Frontend-design audit of ALL UI panes** — design hierarchy, visual incongruences, suffuse glass/grid/math design language, large audacious typography, colorful audacious icon-pops (increase within proportion), animation targets | NEW / UNADDRESSED | N frontend-design wave (other lanes capture per-pane); supersedes A m.2/m.6 at a higher altitude |
| **N-1b** | **Blob** — compare demo-impl vs glass-ui-impl; divine increased expressivity + satellite metaballing + its place in the pane; plan a **standing per-pane frontend-design audit wave** on the Fable model using latest glass-ui | NEW / UNADDRESSED | folds the chronic blob-extirpation (D-open#6) AND adds expressivity; see §2 row B. The standing-audit-wave is a process artifact N must define. |
| **N-1c** | Audit value.js AND the fourier-analysis value API, deployment CI, CRUD systems for **scale + simplicity without contrivance, KISS** | NEW / UNADDRESSED | **NOTE for N**: the fourier "value API" is a **Python/FastAPI** service (`fourier-analysis/api/main.py`, `routers/`, `services/`, `pyproject.toml`) — NOT a TS value.js consumer; cross-repo READ-only. value.js's own api is the Hono/Mongo one (80 `.ts`, 30 `withTransaction` sites). |

---

## §4 — Mandates claimed addressed that REGRESSED or drifted at HEAD

1. **REGRESSED — typecheck green (mandate 25)**: 3 demo errors live (§2 row C). The L/M close gates
   all asserted `vue-tsc 0`; that is **no longer true** at HEAD due to glass-ui API skew.
2. **DOC-DRIFT — M.W1.A still planned to "delete the `development` key"** but the key is already gone
   (exports clean since 0.11.1). M's headline thesis (§1 of M.md: "the precept fossil must die before
   v1.0.0") is **already discharged**; N must strike this from the plan, not execute it.
3. **DOC-DRIFT — M.W7 anchored on glass-ui "3.3.0"** which never shipped as specced; the dock-fix +
   blob-export landed at a later cut (≥3.10.1). N must re-anchor on the live glass-ui (3.10.1
   `tranche/AY`; registry 3.12.0; judged stale-lineage, next cut 3.13.0 per brief).
4. **DOC-DRIFT — `demo/CLAUDE.md` dirty again** (gitStatus `M demo/CLAUDE.md`); `docs/precepts`
   submodule dirty (`LESSONS-LEARNED.md`, `tranche/SPEC.md`). M.W9.C scoped a doc-reconciliation
   sweep; it is unstarted and the dirt persists.
5. **NOT-A-REGRESSION but worth flagging**: M was **never ratified** (`M/PROGRESS.md` status board:
   all M.W1–W9 "PLANNED", "hard-blocked on explicit user ratification"). So nothing M planned is owed
   as "broken" — but everything M folded is still owed, now re-homed into N with corrected premises.

---

## §5 — What N inherits (the consolidated open-mandate set)

**Chronically deferred (3+ tranches, still open):**
- Aurora-derive-from-color (D-open#6, ~9 tranches) — **landable now** (producer shipped). [N: aurora wave]
- Blob-extirpation + expressivity (D-open#6, ~9 tranches) — **landable now** (glass-ui exports). [N-1b]
- WithId read-side type-escape class (L under-measured; 25 casts / 0 WithId verified live). [N: type wave]
- VAL-1 OKLab aurora-LUT ship-or-KILL (J kill-date). [N: aurora wave, ship-or-kill]
- 2 color-resolvers→1; parseCSSColor typed; PRM watercolor hole. [N: elegance wave]
- vue-router 4→5; VIEW_MAP single-source; dispatch.ts→mix.ts extraction. [N: modern-web wave]
- Infra: rsync→git-checkout deploy, CF-Pages, color vhost. [N: infra wave]
- demo self-alias repoint + band-aid retirement (M.W1.B/C). [N: resolution wave]

**Freshly deferred / NEW at this HEAD:**
- **typecheck RED — glass-ui API skew (P0, doc-blind)**: rename `glass-carousel`→`carousel`,
  `BouncyTabs`→`SegmentedTabs` at the 3 demo sites. [N: prerequisite hardening]
- glass-ui re-anchor 3.3.0→3.10.1/3.13.0 (stale anchor). [N: cohort wave]
- demo/CLAUDE.md + docs/precepts submodule doc-reconciliation. [N: close]
- The three NEW thrusts (N-1a frontend-design, N-1b blob+standing-audit-wave, N-1c value+fourier-api
  scale/KISS). [N: dedicated waves]

**Already-discharged-by-release (strike from any new plan):**
- `development` export key deletion (M.W1.A) — exports clean @HEAD.
- value.js 0.11.0 publish (M.W4) — 0.11.2 published.
- dock C1 :inert fix (M.W7 sub) — fixed in glass-ui 3.10.1.

---

## §6 — Tree-verification appendix (every count re-checked, no doc-trust)

| Claim (doc) | Live result | Cmd |
|---|---|---|
| `development` key at `package.json:27` | **ABSENT** (exports = types/import/default) | `grep -n development package.json` → none |
| value.js version | **0.11.2** | `package.json:3` |
| branch / HEAD / ahead-of-master | `tranche-f-handoff` / `0cb5dd2` / **39** | `git rev-list --count master..HEAD` |
| WithId cast class in api/src | **25** | `grep -rEn 'as [A-Z]\w+ & \{ _id' api/src` |
| `WithId` usages in api/src | **0** | `grep -rn WithId api/src` |
| src `as any` / `as unknown as` | **0 / 2** | `grep -rn` (matches CLAUDE.md) |
| demo blob dirs | goo-blob ✓, watercolor-dot ✓, webgl-utils.ts ✓ | `ls` |
| glass-ui exports goo-blob/watercolor-dot/aurora | **yes** (3.10.1) | `node -e exports` |
| glass-ui `deriveAurora` | **present** | `grep -rln deriveAurora glass-ui/src` |
| GlassDock `:inert` | **FIXED** (`!expanded`/`expanded`, L389/395) | `grep inert GlassDock.vue` |
| typecheck | **RED — 3 errors** (glass-carousel, BouncyTabs ×2) | `npm run typecheck` |
| dispatch.ts / mix.ts LoC | 349 / 81 | `wc -l` |
| vue-router | `^4.6.4` (router 5 not done) | `package.json:110` |
| api largest file | `crud.ts` 296 | `wc -l` |
| api withTransaction sites | 30 | `grep -rn withTransaction api/src` |
| J atom-diff/publish | shipped (`atomdiff.ts`, `diff.ts`, `publish.ts`) | `ls`/`grep` |
| J [P0] visibility filter | `crud-list.ts:114 f.visibility="public"` | `grep` |
| glass-ui local | 3.10.1 / `tranche/AY` | `package.json` + git |
| keyframes local | 4.1.0 | `package.json` |
| docs/precepts submodule | dirty (LESSONS-LEARNED.md, tranche/SPEC.md) | `git status` |
| demo/CLAUDE.md | dirty (`M`) | `git status` |
| M ratification | NEVER (all waves PLANNED, blocked on ratification) | `M/PROGRESS.md` |
| fourier "value API" | **Python/FastAPI** (`main.py`, not TS) | `ls fourier-analysis/api` |

**Net**: zero un-recapped prompts; the coverage docs are historically honest. But the M plan is 7 days
stale — strike 3 already-discharged items, fold 1 fresh P0 (typecheck RED), re-anchor the cohort on
glass-ui 3.10.1+, and carry the rest into N with corrected premises. The two oldest mandates
(aurora-derive, blob-extirpation) remain UNADDRESSED-IN-CODE but are finally *unblocked* on glass-ui's side.
