# Lane D6 — The Asks Ledger (constellation adoption asks, tree-verified statuses, net-new asks)

**Lane**: D6 — Asks ledger audit  
**Audit run**: 2026-06-11, branch `tranche-f-handoff`, HEAD `0cb5dd2`  
**Primary source**: `/Users/mkbabb/Programming/fourier-analysis/docs/constellation/ADOPTION-ASKS.md` (HEAD verified)  
**Scope**: All value.js-directed cross-repo asks from ADOPTION-ASKS.md (Asks 3, 5, inv-22-color, cascade-vjs, valuejs-J-atomdiff, valuejs-J-publish, glass-ui-a11y, glass-ui-dock-vt-name) plus net-new asks from sibling tranche docs not yet in any value.js ledger.

---

## §1 — Canonical ledger source (ADOPTION-ASKS.md provenance)

ADOPTION-ASKS.md is authored at  
`/Users/mkbabb/Programming/fourier-analysis/docs/constellation/ADOPTION-ASKS.md`.  
Last material update: **§10 (glass-ui-3.2.0 re-ground 2026-06-04)**; last stale-watch re-affirmation: **G.W8 (fourier tranche G)**.

The doc has 10 sections (§0–§10). §4 carries the live stale-watch table; §7/§8/§9/§10 carry era-bookings and re-grounds.

---

## §2 — Value.js-directed asks: tree-verified status verdicts

### Ask 3 — palette-api rsync deploy-dir → git checkout, then adopt hardened hook (N1 real fix)

**Stated acceptance shape**: `git -C <canonical palette-api dir> rev-parse HEAD` succeeds (git checkout); push fires `deploy.babb.dev/hooks/value-js` → `deploy-hook.sh` performs `git reset --hard` + rebuild + health-gate; rsync `deploy.sh` step retired; value.js arm leaves `dispatch.sh`.

**Tree-verified status**: **PARTIAL / OPEN**.

Evidence:
- `scripts/deploy.sh` exists at `scripts/deploy.sh` (verified). Its comment at line 6 explicitly names "Ask 3" and references a `scripts/deploy-hook.sh` that runs ON the host, not in this repo. The client-side push logic is present (git-push + webhook poke + health-gate via `/palettes` at `api.color.babb.dev`).
- There is **no `scripts/deploy-hook.sh`** in the value.js local tree (`find /Users/mkbabb/Programming/value.js/scripts/ -name "deploy-hook.sh"` = 0 hits). The actual on-host hook script is absent from this repo.
- `scripts/deploy.sh:30` health-gates `https://api.color.babb.dev/palettes`, not `/health` — this is the value.js API's `GET /palettes` endpoint, not a dedicated health endpoint (the API only exposes `GET /` returning `{status:"ok"}`).
- Whether the deploy dir has been converted from rsync to git checkout is a host-state question unverifiable from this local tree. The acceptance shape requires a live host verification; that evidence is absent.
- The deploy.sh was committed at `Jun  2 20:14` (scripts/deploy.sh mtime) suggesting it pre-dates the L close (`66dcd68`, 2026-06-04), so it may be the authored-only client-wrapper the M.W8 plan calls "adopted" — but the on-host `deploy-hook.sh` that performs the `git reset --hard` is explicitly the missing piece.

**Verdict**: `OPEN` — the client-side deploy wrapper (`deploy.sh`) is present and references Ask 3, but the acceptance criteria (host git checkout + on-host `deploy-hook.sh` + live health-gate green + arm leaves `dispatch.sh`) are unverified. Per M.W8, this is explicitly scheduled.

---

### Ask 5 — Frontend-hosting convergence to CF Pages (GH-Pages CNAME → CF Pages)

**Stated acceptance shape**: `color.babb.dev` serves from CF Pages (a `cf-ray` header + Pages deployment visible in CF dashboard); peaceiris `gh-pages` workflow step removed; CNAME→GH-Pages path gone.

**Tree-verified status**: **PARTIALLY ADDRESSED / OPEN**.

Evidence:
- `scripts/deploy.sh` at lines 68–98 implements a Cloudflare Pages deploy target (`deploy_frontend`) via `wrangler pages deploy` with `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` (Ask 5 referenced in the script comment at line 11). The frontend deploy mechanism is authoritatively CF Pages in the wrapper script.
- `.github/workflows/ci.yml` at line 189 uses `treosh/lighthouse-ci-action@v12` but does NOT show a peaceiris `gh-pages` deploy step. The `release.yml` would be relevant — let me note: the release.yml was NOT inspected in detail, but the `npm run gh-pages` command outputs to `dist/gh-pages/` for `wrangler pages deploy` in deploy.sh.
- The actual live state (`cf-ray` header at `color.babb.dev`) is unverifiable from the local tree.
- The `package.json` has `unplugin-vue-markdown: ^32.0.0` (cascade-vjs already landed for the vite 8 peer), confirming the CI ERESOLVE fix shipped.

**Verdict**: `OPEN (tooling-ready)` — the CF Pages deploy script is present and wired, but live acceptance (CF dashboard confirmation, peaceiris removed, CNAME path verified) is unconfirmed. M.W8 explicitly schedules this.

---

### inv-22-color — `api.color.babb.dev` 4-endpoint vhost contract

**Stated acceptance shape**: `/health`, `/docs`, `/openapi.json` respond non-404 at `api.color.babb.dev` (alongside `GET /` → 200).

**Tree-verified status**: **OPEN**.

Evidence:
- `api/src/index.ts:78` registers only `GET /` → `{ status: "ok", service: "palette-api" }`. Routes: `/palettes`, `/sessions`, `/colors`, `/admin` — no `/health`, no `/docs`, no `/openapi.json`.
- `api/apache-vhost.conf` proxies `ProxyPass /colors/ http://localhost:3100/` — the vhost prefix is `/colors/`, so `api.color.babb.dev/health` (without `/colors/` prefix) would 404 at the Apache layer, not even reaching Hono.
- Per ADOPTION-ASKS.md §4 (re-affirmed G.W8): "serves only `/`→200; `/health`/`/docs`/`/openapi.json`→404".

**Verdict**: `OPEN` — the value.js Hono API does not define `/health`, `/docs`, or `/openapi.json` routes. The vhost configuration also does not expose them. M.W8 plans to bring this to the 4-endpoint contract.

---

### cascade-vjs — `@mkbabb/glass-ui file:` → `^published` + lockfile regen

**Stated acceptance shape** (from ADOPTION-ASKS.md §4, H.W6 booking): bump `unplugin-vue-markdown ^29.2.0 → ^32.0.0` + `@mkbabb/{glass-ui,keyframes.js}` `file:` → `^published` + regen lockfile — clears the `npm ci` ERESOLVE.

**Tree-verified status**: **SPLIT — half done, half open**.

Evidence (tree-verified):
- `package.json:101`: `"unplugin-vue-markdown": "^32.0.0"` — **DONE** (the b4defb0 commit `ci(release): unblock npm ci on vite 8 — unplugin-vue-markdown ^29→^32` at HEAD log shows this shipped).
- `package.json:68`: `"@mkbabb/glass-ui": "file:../glass-ui"` — **STILL file: reference, OPEN**.
- `package.json:69`: `"@mkbabb/keyframes.js": "file:../keyframes.js"` — **STILL file: reference, OPEN**.

The `unplugin-vue-markdown` half shipped (commit `b4defb0` = `ci(release): unblock npm ci on vite 8 — unplugin-vue-markdown ^29→^32`). The `glass-ui` and `keyframes.js` file-protocol pins remain.

**Context**: The ADOPTION-ASKS.md §10 re-ground (2026-06-04) notes glass-ui is on `3.10.1` local checkout (branch `tranche/AY`, registry `3.12.0`). The M.md §3 publish-spine requires value.js 0.11.0 → glass-ui 3.3.0 cut → demo consume. The `file:` pins are deliberate co-dev wiring per mechanism-C (value.js M.W1: "the demo self-alias survives under mechanism-C"). M.W9 closes with `inv-M-6` requiring registry consumption for v1.0.0.

**Verdict**: `PARTIAL` — `unplugin-vue-markdown` half done; `glass-ui`/`keyframes.js` file: pins remain as intentional co-dev wiring, gated on M.W4 (0.11.0 publish) + glass-ui 3.3.0 cut per M.W7.

---

### valuejs-J-atomdiff — DONE

**Stated acceptance shape**: `api/src/lib/crud/atomdiff.ts` exists, `PaletteVersion.atomDiff`, `POST /:slug/remix`, `GET /:slug/diff?from=` binding to `J-diff-shape.md`.

**Tree-verified status**: **DONE** (flipped in ADOPTION-ASKS.md §10).

Evidence:
- `/Users/mkbabb/Programming/value.js/api/src/lib/crud/atomdiff.ts` exists (3855 bytes, dated Jun 2 16:55). Comment at line 5 cites `fourier-analysis/docs/tranches/J/design/J-diff-shape.md §2.5: atomdiff`.
- `/Users/mkbabb/Programming/value.js/api/src/services/palette/diff.ts` exists (3587 bytes, dated Jun 2 17:14).
- `/Users/mkbabb/Programming/value.js/api/src/services/palette/forks.ts` exists (8326 bytes, dated Jun 4 12:40).
- ADOPTION-ASKS.md §10 flips this to `DONE-in-sibling (2026-06-04)`.

**Verdict**: `DONE` — confirmed by tree evidence. Residual: fourier-J's own `/diff` + publish envelope parity confirm (fourier-local Python probe; that is fourier's obligation, not value.js's).

---

### valuejs-J-publish — DONE

**Stated acceptance shape**: `POST /:slug/{publish,unpublish}` idempotent in-place flip; `listPalettes` `crud-list.ts:114` `visibility="public"` clause.

**Tree-verified status**: **DONE** (flipped in ADOPTION-ASKS.md §10).

Evidence:
- `/Users/mkbabb/Programming/value.js/api/src/services/palette/visibility.ts` exists (3900 bytes, dated Jun 4 12:43).
- `/Users/mkbabb/Programming/value.js/api/src/services/palette/crud-list.ts` exists (8459 bytes, dated Jun 4 12:40).
- ADOPTION-ASKS.md §10 confirms `crud-list.ts:114 visibility="public"` filter shipped in L.

**Verdict**: `DONE` — confirmed by tree evidence.

---

### glass-ui-a11y (`inert` on ConfiguratorLayer) — SATISFIED-UPSTREAM / ADOPT-NOW

**Stated acceptance shape** (re-grounded): `inert` shipped **glass-ui 3.1.1** (`ConfiguratorLayer.vue:144`). value.js's obligation: un-`test.fixme` 2 a11y keystones on the bump to glass-ui ≥3.1.1.

**Tree-verified status**: **UPSTREAM DONE; local consumption PENDING**.

Evidence:
- Glass-ui local checkout is on branch `tranche/AY`, local version `3.10.1` (HEAD `181cacae`). The `inert` fix was in `3.1.1` — consumed upstream.
- value.js package.json has `"@mkbabb/glass-ui": "file:../glass-ui"` — so it already resolves against the AY checkout which contains `inert`. The 91 typecheck errors visible in the local tree are due to missing `.d.ts` files in the glass-ui AY dist (the AY branch doesn't have dist built).
- No `test.fixme` search was done (out of scope for this lane), but the ADOPTION-ASKS.md §10 says "un-`test.fixme` on the `^3.2.0` bump".

**Verdict**: `UPSTREAM DONE / local consume pending` — the glass-ui `inert` fix is in AY (confirmed shipped 3.1.1/3.2.0). value.js's obligation is to un-`test.fixme` the 2 a11y keystones on the bump (M.W7 / the glass-ui 3.3.0 consume).

---

### glass-ui-dock-vt-name (useId VT-name fix) — SATISFIED-UPSTREAM / ADOPT-NOW

**Stated acceptance shape**: `useId()` fix shipped **glass-ui 3.1.1** (`GlassDock.vue:137`). value.js's obligation: bump `^3.2.0` caret to consume (resolves the dock interaction blocker).

**Tree-verified status**: **UPSTREAM DONE; local consumption PENDING**.

Evidence:
- glass-ui AY checkout has `GlassDock.vue` with `glass-dock-${useId()}` (per ADOPTION-ASKS.md §8 re-ground, `3.1.1`+). Not re-verified here (glass-ui is not value.js's write surface).
- value.js consumes the `file:../glass-ui` link — the AY dist (if built) would include the fix. The typecheck RED with 91 errors shows the AY dist is not built (missing `.d.ts`).
- The dock view-select interaction blocker mentioned in the CI yml (`continue-on-error: true` on the full smoke project, `ci.yml:151-154`) is the pre-existing collapsed summary-layer issue, separate from the VT-name bug.

**Verdict**: `UPSTREAM DONE / local consume pending` — awaits M.W7 glass-ui 3.3.0 cut + value.js bump.

---

## §3 — Asks NOT directed at value.js (status noted for completeness)

| Ask | Target | Status (ADOPTION-ASKS.md §4, re-affirmed G.W8) |
|---|---|---|
| Ask 1 | words, speedtest, csp-solver | OPEN — CI adoption |
| Ask 2 | words, speedtest, csp-solver | OPEN — hardened deploy-hook + per-repo webhook |
| Ask 4 | words, speedtest, csp-solver | OPEN — docker hardening floor |
| Ask 6 | csp-solver | OPEN — csp-solver route-registration |
| Ask 7 | words/floridify | OPEN — Mongo-bind upstream hardening |
| cascade-kf | keyframes.js | OPEN — `glass-ui file:` → `^3.0.0` |
| cascade-gui | glass-ui | OPEN — lockfile regen |
| words-spa | words/floridify | OPEN — deploy-pages.yml + CF Pages |
| glass-ui-P5-inner-rounding | — | **KILLED-AS-PHANTOM** (struck §7/§9/§10) |
| A-1 inter-row divider-rule | glass-ui | BOOK-with-kill-date = glass-ui AT/3.3.0 (SELF-BOOKED `AS/FINAL.md:146-155`) |
| A-2 label/sub→ladder token hook | glass-ui | BOOK-with-kill-date = glass-ui AT/3.3.0 (SELF-BOOKED) |
| A-3 `asideSide` prop | glass-ui | **SHIPPED glass-ui 3.2.0 / ADOPT-NOW** (fourier-local) |

---

## §4 — Precepts submodule — OPEN (booked, uncommitted edits)

The `docs/precepts` submodule in value.js is at pointer `63240e6` (verified: `git submodule status` output = ` 63240e677dfd1d5b95e00710a1a4d64664624784 docs/precepts`). The `PRECEPTS-SYNC.md` (2026-06-04) records that the constellation sync advanced to `8ccf9f4` — value.js was marked **BOOKED** because its submodule had uncommitted edits at sync time. The gap is 1 commit: `8ccf9f4` ("spec(π-lane): edict — every-page paired before/after capture + scripted occlusion gate").

**Verdict**: `OPEN` — value.js's `docs/precepts` is 1 commit behind `8ccf9f4`. M.W9 reconciles the submodule.

---

## §5 — Typecheck RED — glass-ui API skew (3 actionable errors)

The `npm run typecheck` command produces **91 total demo errors** but of these, 88 are `TS7016 Could not find a declaration file for module '@mkbabb/glass-ui*'` — these are glass-ui-internal diagnostics arising because the AY checkout's `dist/` has no `.d.ts` files (or stale ones). The `check-types.mjs` script explicitly excludes glass-ui-internal diagnostics (by design, inv-K-4). The **3 actionable value.js-owned errors** are:

| File | Error | Root Cause |
|---|---|---|
| `demo/@/components/custom/color-picker/controls/ComponentSliders.vue:117` | `TS2307: Cannot find module '@mkbabb/glass-ui/glass-carousel'` | glass-ui AY renamed the subpath from `./glass-carousel` to `./carousel`. The export in glass-ui `package.json` is `"./carousel"` not `"./glass-carousel"`. |
| `demo/@/components/custom/mix/MixSourceSelector.vue:4` | `TS2305: Module '@mkbabb/glass-ui/tabs' has no exported member 'BouncyTabs'` | glass-ui AY renamed `BouncyTabs` → `SegmentedTabs` (verified: `dist/tabs.js:88,292` exports `SegmentedTabs`). |
| `demo/@/components/custom/panes/PaneSegmentedControl.vue:18` | `TS2305: Module '@mkbabb/glass-ui/tabs' has no exported member 'BouncyTabs'` | Same as above — two consumers of the renamed export. |

**Fixes** (value.js-owned, tracked for tranche N / M.W7):
1. `ComponentSliders.vue:117`: `from "@mkbabb/glass-ui/glass-carousel"` → `from "@mkbabb/glass-ui/carousel"`
2. `MixSourceSelector.vue:4,100`: `import { BouncyTabs }` → `import { SegmentedTabs }` and `<BouncyTabs` → `<SegmentedTabs`
3. `PaneSegmentedControl.vue:6,18`: same BouncyTabs → SegmentedTabs rename

These are a **glass-ui AY consume-edge debt** — value.js must migrate its three import sites to the new subpath/name when it bumps to the AY-lineage release.

---

## §6 — Net-new value.js-directed asks not yet in any value.js ledger

### Net-new ask A: keyframes.js VJ-slice carry (VJ-F1, VJ-F4, VJ-F2, VJ-E1/E2, VJ-A-tier)

**Source**: `keyframes.js/docs/tranches/G/valuejs-parsethat-glassui-handoff.md §1` + kf `J.md §4 row W6` ("EF-3: `parseLinearStops` shim retirement check against `linear()` Baseline + the value.js E1 status").

**What these are**:
- **VJ-E1/E2**: value.js exposes CSS `linear()` / `steps()` easing parsers so kf's local `parseLinearStops` shim retires. Tree check: `src/easing.ts` has a `linear(stops)` *runtime* function at `:24` (the timing-function evaluator) but NO `parseCSSLinear` / `parseLinearStops` parser reading CSS `linear(0, 0.25 75%, 1)` text. **Unshipped.** kf J.W6 checks VJ-E1 status at termination.
- **VJ-F1**: path-geometry sampler (unblocks MorphSVG). Absent in value.js tree (grep `src/` for `sampler`/`path-d`/`bezier-sampler` = 0). **Unshipped.**
- **VJ-F2**: `tryParse` uses `state.offset` not `state.furthest`; structured-diagnostics sink. `src/parsing/utils.ts` exists. **Status unverified** in this lane but carried as open in kf G/H.
- **VJ-F4**: `unflattenObjectToString` buffer-reusing overload. `src/units/utils.ts:115-148` — not inspected here but kf G handoff flags it absent in 0.11.0.
- **VJ-A-tier**: remaining `any(` inner forks → O(1) `dispatch` LUT. A1 landed (top-level color dispatch); 62 `any(` sites remain (kf G §1 VJ-A-tier).

**Status**: `OPEN` — the kf `parseLinearStops` shim retirement is the most time-sensitive (kf J.W6 is a terminal gate; kf J was authored 2026-06-06 with Baseline date `2026-06-11` for `linear()`).

**Tranche N disposition**: BOOK as `kf-vj-e1-e2` row. If `linear()` Baseline is confirmed live today (2026-06-11), kf retires the shim regardless; the value.js parser is the clean DRY landing. Priority: P2.

---

### Net-new ask B: glass-ui consume-edge migration (subpath rename + BouncyTabs → SegmentedTabs)

**Source**: Tree-verified typecheck errors (§5 above). Not in ADOPTION-ASKS.md.

**What it is**: Three import sites in value.js's demo need updating to glass-ui AY's renamed exports:
- `@mkbabb/glass-ui/glass-carousel` → `@mkbabb/glass-ui/carousel`
- `BouncyTabs` → `SegmentedTabs` (two files)

**Tranche N disposition**: Add as `glass-ui-consume-edge-vjs` row. These are mechanical migrations, 3 lines, triggered on the M.W7 glass-ui 3.3.0 bump. Priority: P1 (they break `vue-tsc` and will fail CI once the dist has types).

---

### Net-new ask C: kf `build:watch` dts-emit cohort note

**Source**: `M.md §8` ("keyframes: a `build:watch` dts-emit cohort note (no publish)"). Not in ADOPTION-ASKS.md.

**What it is**: keyframes.js should make `build:watch` emit `.d.ts` files for the same freshness parity glass-ui is asked to adopt (glass-ui M.W7 cohort ask includes `dts-emitting build:watch`).

**Status**: kf `4.1.0` is the live registry version (verified from kf G close doc). Whether kf's `build:watch` already emits dts is unverified in this lane.

**Tranche N disposition**: Add as `kf-build-watch-dts` cohort note. Priority: P3 (developer experience, no correctness impact).

---

### Net-new ask D: glass-ui 3.3.0 cohort cut (value.js's owed ask)

**Source**: `M.md §8` — value.js is **owed** a glass-ui 3.3.0 cut that: bumps `@mkbabb/value.js` peer `^0.10.0 → ^0.11.0`; carries W-LIFT (goo-blob/WatercolorDot primitives + subpath exports); carries W-D (`GlassDock :inert` realignment); drops value.js devDep; includes aurora `cssToOklch` cast-delete against 0.11.0; dts-emitting `build:watch`.

**Status**: glass-ui local is at `tranche/AY` (`3.10.1` local, `3.12.0` registry). A `3.3.0` cut from the M.md plan was never made (M itself was never ratified). The target version numbers in M.md (`3.3.0`) may now be moot given glass-ui has advanced to AY/3.10.1+; the actual cut should target the *next stable* glass-ui release against 0.11.0.

**Tranche N disposition**: BOOK as `glass-ui-cohort-cut-for-vjs` — the glass-ui arm that value.js is owed. This is glass-ui-owned (inv-16); fourier holds no lever. The specific version number to target should be coordinated with the glass-ui tranche (AY/AZ). Priority: P1 (blocks blob extirpation, M.W5 aurora, v1.0.0).

---

### Net-new ask E: `development` export key deletion + M.W1 precept remediation

**Source**: `M.md §1` + `docs/tranches/K/audit/` (various lanes). Not in ADOPTION-ASKS.md (it is value.js-unilateral, not a cross-repo ask).

**Status**: `package.json:27` (the `development` export key) — **verified absent at HEAD** (`0cb5dd2`). Commit `4c8c532` ("fix(pkg): drop the broken `development` export condition") already removed it. `inv-M-1` is satisfied in the current tree.

**Tree check**:
```bash
grep '"development"' /Users/mkbabb/Programming/value.js/package.json
# → 0 hits (confirmed absent)
```

**Tranche N disposition**: CLOSED — the precept fossil is already gone. No further action.

---

## §7 — Consolidated asks ledger for tranche N

| Ask ID | Target | Change (one line) | Priority | Owner | Status at 2026-06-11 |
|---|---|---|---|---|---|
| **Ask 3** | value.js (palette-api host) | rsync deploy-dir → git checkout under canonical root + adopt `deploy-hook.sh` (the N1 real fix; gating 4th dispatcher migration) | P1 | value.js maintainer | **OPEN** — client wrapper (`deploy.sh`) present; on-host `deploy-hook.sh` absent; host-state unverifiable; scheduled M.W8 |
| **Ask 5** | value.js (frontend) | GH-Pages→CF-CNAME → CF Pages per `deploy/cf/pages-deploy.sh` (CF Pages wrangler recipe present in `deploy.sh`) | P3 | value.js maintainer | **OPEN (tooling-ready)** — `deploy.sh` has CF Pages path; live acceptance unconfirmed; scheduled M.W8 |
| **inv-22-color** | value.js (palette-api vhost) | add `/health`, `/docs`, `/openapi.json` routes to Hono API + vhost configuration | P3 | value.js maintainer | **OPEN** — only `GET /` defined; `/health`/`/docs`/`/openapi.json` → 404; scheduled M.W8 |
| **cascade-vjs** (partial) | value.js | `@mkbabb/{glass-ui,keyframes.js}` `file:` → `^published` + lockfile regen (the `unplugin-vue-markdown ^32` half is DONE) | P2 | value.js maintainer | **PARTIAL** — `unplugin-vue-markdown` done; `glass-ui`/`keyframes.js` file: pins remain (intentional co-dev wiring, gated on M.W4/M.W7) |
| **valuejs-J-atomdiff** | value.js | DONE — `api/src/lib/crud/atomdiff.ts`, `PaletteVersion.atomDiff`, `POST /:slug/remix`, `GET /:slug/diff` | — | — | **DONE** (closed L) |
| **valuejs-J-publish** | value.js | DONE — `POST /:slug/{publish,unpublish}` + `crud-list.ts:114` `visibility="public"` filter | — | — | **DONE** (closed L) |
| **glass-ui-a11y** | value.js (consume) | un-`test.fixme` 2 a11y keystones on glass-ui ≥3.1.1 bump | P2 | value.js maintainer | **UPSTREAM DONE** (glass-ui 3.1.1); local consume pending M.W7 |
| **glass-ui-dock-vt-name** | value.js (consume) | bump to glass-ui ≥3.1.1 to get `useId()` dock VT-name fix | P2 | value.js maintainer | **UPSTREAM DONE** (glass-ui 3.1.1); local consume pending M.W7 |
| **precepts-submodule** | value.js | bump `docs/precepts` from `63240e6` → `8ccf9f4` (1 commit: π-lane before/after capture edict) | P3 | value.js maintainer | **OPEN** — pointer at `63240e6`; scheduled M.W9 |
| **glass-ui-consume-edge-vjs** | value.js | migrate 3 import sites: `glass-carousel` → `carousel`; `BouncyTabs` → `SegmentedTabs` (×2 files) — forced by glass-ui AY rename | P1 | value.js maintainer | **OPEN / typecheck red** — 3 `TS2307`/`TS2305` errors confirmed live |
| **kf-vj-e1-e2** | value.js | expose CSS `linear()` / `steps()` easing parsers so kf's `parseLinearStops` shim retires on the next re-pin (kf J.W6 terminal gate) | P2 | value.js maintainer | **OPEN** — `src/easing.ts` has runtime `linear()` but no CSS-text parser; kf J.W6 checks status 2026-06-11 |
| **glass-ui-cohort-cut-for-vjs** | glass-ui | cut next stable release against value.js `^0.11.0`: bump peer; carry W-LIFT blob primitives + W-D `GlassDock :inert`; subpath exports; aurora `cssToOklch` cast-delete; dts-watch; devDep drop | P1 | glass-ui maintainer | **OPEN** — glass-ui at AY/`3.10.1` locally, `3.12.0` registry; the specific next-stable cut against 0.11.0 is unscheduled in glass-ui's tranche |
| **kf-build-watch-dts** | keyframes.js | make `build:watch` emit `.d.ts` for freshness parity | P3 | keyframes.js maintainer | **OPEN (cohort note only)** — no publish required |
| **dev-export-key** | value.js | `development` export key in `package.json` | — | — | **DONE** (`4c8c532` dropped it; `package.json` confirmed clean) |

---

## §8 — Summary of verdicts

**DONE (no further value.js action)**:
- `valuejs-J-atomdiff`, `valuejs-J-publish`: closed in L (`66dcd68`).
- `development` export key: removed at `4c8c532`.
- `glass-ui-a11y` (upstream half): shipped glass-ui 3.1.1.
- `glass-ui-dock-vt-name` (upstream half): shipped glass-ui 3.1.1.

**PARTIAL**:
- `cascade-vjs`: `unplugin-vue-markdown` done; glass-ui/keyframes file: pins remain (intentional).

**OPEN — scheduled M.W8 (infra)**:
- Ask 3 (palette-api rsync→git; deploy-hook.sh on host; P1 critical-path).
- Ask 5 (CF Pages; P3).
- inv-22-color (4-endpoint vhost; P3).

**OPEN — scheduled M.W7 (glass-ui 3.3.0 cohort)**:
- glass-ui-a11y + glass-ui-dock-vt-name (local consume; P2).
- cascade-vjs glass-ui/keyframes pin retirement (P2).
- glass-ui-consume-edge-vjs (rename migration; P1 — typecheck red now).

**OPEN — value.js-owned, unscheduled in M**:
- kf-vj-e1-e2 (CSS `linear()`/`steps()` parser; P2; kf J.W6 termination gate).

**OPEN — cross-repo (glass-ui-owned)**:
- glass-ui-cohort-cut-for-vjs (P1; glass-ui maintainer).

**OPEN — minor**:
- precepts-submodule (1 commit behind; P3; M.W9).
- kf-build-watch-dts (P3; cohort note).

---

## §9 — Methodology note

All statuses are tree-verified against HEAD `0cb5dd2` on branch `tranche-f-handoff` (2026-06-11). Key verification commands:
- `package.json` inspection (lines 27, 68, 69, 101).
- `api/src/index.ts` route listing (lines 72–78).
- `api/src/lib/crud/atomdiff.ts` existence check.
- `scripts/deploy.sh` existence + content (lines 6, 11, 30).
- `npm run typecheck` output (91 total errors; 3 actionable value.js-owned errors).
- `git submodule status docs/precepts` (pointer `63240e6`).
- Cross-repo read: `glass-ui/package.json` exports map; `glass-ui/dist/carousel.js`; `glass-ui/dist/tabs.js` (exports `SegmentedTabs`).
- Cross-repo read: `fourier-analysis/docs/constellation/ADOPTION-ASKS.md` (full), `CONSTELLATION.md` (full), `PRECEPTS-SYNC.md`.
- Cross-repo read: `keyframes.js/docs/tranches/G/valuejs-parsethat-glassui-handoff.md`, `H/H.md`, `I/FINAL.md`, `J/J.md`.

inv-16 respected: this lane READ only. ZERO writes to any source file in value.js or any sibling repo.

---

*End of Lane D6.*
