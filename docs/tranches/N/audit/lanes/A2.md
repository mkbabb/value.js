# Lane A2 — Precepts and federation delta

**Audit date:** 2026-06-11  
**Branch:** tranche-f-handoff, HEAD 0cb5dd2  
**Scope:** docs/precepts submodule state; canonical constellation federation docs;
which invariants and decisions bind value.js today; what changed since 2026-06-04;
every new obligation or ask aimed at value.js; the precept-sync state.

---

## 1. docs/precepts submodule state

### 1.1 Submodule pointer vs canonical

```
git ls-tree HEAD docs/precepts
→  160000 commit 63240e677dfd1d5b95e00710a1a4d64664624784  docs/precepts
```

The parent repo's committed gitlink is **63240e6**.  
The canonical origin/main of the precepts repo is **8ccf9f4** (as confirmed by
`git -C docs/precepts log --oneline origin/main`).

**value.js is pinned ONE commit behind canonical.** The missing commit is:

```
8ccf9f4  spec(π-lane): edict — every-page paired before/after capture +
         scripted occlusion gate
```

This commit (2026-06-04) added to `instructions/tranche/SPEC.md` the
"Before/after capture — every page, paired, scripted" block — the canonical
edict for paired π captures across the constellation.

### 1.2 Dirty working tree

`git -C docs/precepts status` shows two modified files — **not staged, not committed**:

| File | Nature of change |
|---|---|
| `instructions/LESSONS-LEARNED.md` | Added a new entry at the end: "2026-06-02 — Single-Snapshot π Misses Disappearing Elements (value.js Blob Regression)" — a lessons-learned entry sourced from value.js K.W4 analysis |
| `instructions/tranche/SPEC.md` | Added a detailed `#### Before/after + compare-at-close` subsection under the π lane section — a more expanded version of the same π paired-capture discipline |

**Diagnosis:** These dirty edits are the **authoring site** of the π-lane edict that was
subsequently canonicalized in `8ccf9f4`. The dirty tree is value.js's own draft that
preceded or accompanied the canonical commit; it contains ADDITIONAL content not present
in `8ccf9f4` (specifically: the `LESSONS-LEARNED.md` blob-regression entry, and
SPEC.md's `#### Before/after + compare-at-close` section is MORE DETAILED than what
`8ccf9f4` added — the canonical commit's SPEC.md block uses different wording).

**The dirty SPEC.md does NOT contain the `8ccf9f4` paragraph verbatim** (`grep -c
"Before/after capture — every page" instructions/tranche/SPEC.md` = 0). The two
versions are parallel elaborations of the same discipline, not one superseding the other.

### 1.3 PRECEPTS-SYNC.md verdict

The hub's `PRECEPTS-SYNC.md` (last updated 2026-06-04) records:

> **value.js: BOOKED (inv-16′)** — its `docs/precepts` submodule has uncommitted edits
> (2 files; likely the authoring site of this very edict). A checkout would CLOBBER
> that work. value.js's own session commits/reconciles its precept edits, then bumps.
> fourier holds no lever.

This exactly describes the current state. The BOOKED status is still open — no subsequent
bump commit has landed in value.js (`git log HEAD -- docs/precepts` shows the last
pointer bump at commit `73f421e`: "sync submodule to canonical 63240e6", which was
value.js's last pointer-advance).

### 1.4 Required action (value.js-owned, not a fourier lever)

The value.js maintainer must:
1. Decide whether to integrate or discard the local dirty edits in `docs/precepts/`
2. Commit the reconciled precept files into the submodule (a commit to the `mkbabb/precepts` repo from within `docs/precepts/`)
3. Bump the parent repo's gitlink pointer to the new commit (or to `8ccf9f4` if the local changes are dropped)

Until this happens: the dirty precepts tree remains an uncommitted draft, value.js stays
one commit behind canonical on the π-lane edict, and the PRECEPTS-SYNC BOOKED entry stays
open.

---

## 2. Constellation invariants binding value.js today

From `fourier-analysis/docs/tranches/INVARIANTS.md` and the CONSTELLATION.md/ADOPTION-ASKS
review, the following invariants are explicitly cited as binding value.js:

### 2.1 Core cross-repo invariants (from INVARIANTS.md / CONSTELLATION.md)

| Invariant | Source | What it requires of value.js |
|---|---|---|
| **inv-16** (B-Inv 16) | `B.md §2`; CONSTELLATION §2 | Write only `value.js/**`; all cross-repo contracts are READ. No silent edits to fourier, glass-ui, etc. |
| **inv-16′** (H-Inv 16′) | `H.md §2`; CONSTELLATION §2 | Cross-repo writes only under an explicit, user-authorized, NAMED, ledgered inv-16′ sweep; each its own commit, booked to an `ADOPTION-ASKS` entry, gated on the target's own green CI |
| **inv-26** (G-Inv 26) | `G.md §2` | Single-contract-source: the api↔web type boundary has one canonical source — enforced in value.js's `api/src/` (the L invariant: typed `ApiError`, typed session tokens, no `as any`) |
| **inv-27** (H-Inv 27) | `H.md §2` | green-means-green: a "CI green" close-claim cites a green CI run id covering every job. Currently NOT MET for value.js on this branch (no cited green run id; typecheck RED with 3 demo errors) |
| **inv-28** (H-Inv 28) | `H.md §2` | verified-deploy-of-record: the automated deploy path ships only green-CI SHAs. The palette-api deploy arm is LATENT-BROKEN (palette-api deploy dir is an rsync target, not a git checkout — Ask 3 in ADOPTION-ASKS.md; predates this branch but still open) |

### 2.2 Precept-namespace invariants (constellation-wide, from precepts/LESSONS-LEARNED + SPEC)

The precepts namespace carries invariants 28–33 (glass-ui / constellation-wide), which are
NAMESPACE-PARTITIONED from fourier's tranche-local set (see INVARIANTS.md §3). The ones
most relevant to value.js as a consumer:

| Precept invariant | Meaning | value.js state |
|---|---|---|
| **invariant 28** (zero-deferral at close) | No open item left without an explicit owner + date | Tranche M authored 2026-06-04, never ratified — the biggest deferral on the board |
| **invariant 29** (one-release-tag-attribution) | Each released version has exactly one attributed tag | Honored: value.js publishes under clean npm version tags |
| **invariant 30** (cross-repo dev-resolution-contract; `precepts/cross-repo-dev-resolution.md`) | The `development` exports-map condition is ABROGATED — no `file:` resolution in production builds | **Violated**: `package.json` uses `file:../glass-ui` and `file:../keyframes.js` (cascade-vjs, open ADOPTION-ASKS row) |

The precepts' `invariant 30` (cross-repo dev-resolution-contract) is the **direct precept
backing** for the ADOPTION-ASKS row `cascade-vjs`. This violation is confirmed at
`/Users/mkbabb/Programming/value.js/package.json`:

```json
"@mkbabb/glass-ui": "file:../glass-ui",
"@mkbabb/keyframes.js": "file:../keyframes.js"
```

Both use `file:` resolution, not published caret pins. This is live in package.json AND in
package-lock.json (modified per git status).

---

## 3. What changed since 2026-06-04

### 3.1 Constellation heads moved

Per CONSTELLATION.md §9 (2026-06-04 re-ground):

| Member | 2026-06-04 state | 2026-06-11 observed state |
|---|---|---|
| **glass-ui** | 3.2.0 PUBLISHED (npm), local at 3.2.0 | npm registry: **3.12.0**; local checkout: **3.10.1** (branch tranche/AY) — 10 registry releases past the June-04 snapshot |
| **keyframes.js** | 3.0.0 PUBLISHED | npm registry: **4.1.0**; local: **4.1.0** — one major version ahead |
| **value.js** | J→K→L CLOSED (`66dcd68`); branch `docs/constellation-grand-audit-2026-06-03` | Branch `tranche-f-handoff`, HEAD `0cb5dd2` (39 commits ahead of master); published `0.11.2` |

### 3.2 New galaxy: tri-tranche run (fourier hub, authored 2026-06-04/05)

A new coordination layer was authored in `fourier-analysis/docs/constellation/tri-tranche-run/`:

- **COORDINATION.md** — three-session run (S1 keyframes-D, S2 glass-ui/slides, S3 feedback-coder)
  with dependency DAG, publish ledger, and autonomy boundaries
- **RUN-BOARD.md** — live blackboard updated as of 2026-06-05:
  - S1 (keyframes-D): **E IMPL+CLOSED** at `fe9b120`; D.W5+close BLOCKED on **E1** (glass-ui 3.3.0)
  - S2 (glass-ui/slides): AU.W0–W6 DONE; slides F.W0–W4 DONE; AU.W7+ in progress; 3.3.0 NOT YET PUBLISHED
  - S3 (feedback-coder): LIVE ML/NLP tranche, gate-free

The **E1 root hinge** (glass-ui 3.3.0) is still **BLOCKED** — glass-ui is at 3.12.0 on npm
but the tri-tranche-run's AU (tranche AY locally, 3.10.1) is still working toward 3.3.0 in
the context of this particular doc's sequencing. This appears to be a naming discontinuity:
the tri-tranche-run was authored around glass-ui's then-upcoming 3.3.0 release, but the
registry has since moved to 3.12.0 through a separate publish chain. The RUN-BOARD still
shows E1=BLOCKED, which is stale relative to the current registry (3.12.0 >> 3.3.0).

**value.js role in the tri-tranche run**: BOOKED (dirty tree). The RUN-BOARD §7 "Cross-links"
states: "S2 → value.js inbox on E1 PUBLISHED: glass-ui 3.3.0 unblocks value.js K.W3 (the
`/goo-blob` + `/watercolor-dot` consumption — value.js's arm, name-forward)."

### 3.3 New constellation doc: DOCK-ANIMATION-CONVERGENCE.md (2026-06-04)

This spec drove keyframes-C and glass-ui AT dock slices in a single campaign. value.js is
explicitly BOOKED in this doc:

> **value.js** (dirty tree): the dock-consumer alignment (adopt the `<Role>Dock` convention
> once glass-ui ships the docs note) + any deck/spring dogfood + the **cascade-vjs**
> caret/lock fix (`ADOPTION-ASKS.md` row). BOOK — value.js reconciles its tree first,
> then executes on a clean checkout.

### 3.4 New keyframes hand-off: valuejs-sota-handoff-v2.md (2026-06-06)

`keyframes.js/docs/tranches/F/valuejs-sota-handoff-v2.md` is a SOTA augmentation
charter proposing 6+ waves of value.js improvements (parser architecture, interpolation
hot path, color alloc, spec/correctness gaps, etc.). It is a PROPOSAL, not a directive —
value.js's owner sequences, scopes, accepts, or defers each item. It supersedes the E v1
(`docs/tranches/E/valuejs-sota-handoff.md`).

---

## 4. Open obligations and asks binding value.js

### 4.1 HARD obligations (constellation-binding, not optional)

| ID | Obligation | Source | Priority | Status |
|---|---|---|---|---|
| **cascade-vjs** | Bump `@mkbabb/glass-ui file:` → `^published` + `@mkbabb/keyframes.js file:` → `^published` + regen lockfile — clears `npm ci` ERESOLVE (cascade HEAD); also fixes precept invariant 30 violation | ADOPTION-ASKS.md; DOCK-ANIMATION-CONVERGENCE §7 | P2 | **OPEN** — confirmed violated: `file:../glass-ui` and `file:../keyframes.js` in package.json |
| **precepts bump** | Reconcile dirty docs/precepts edits → commit to mkbabb/precepts → bump parent gitlink to canonical (at minimum to 8ccf9f4, preferably with local edits merged upstream) | PRECEPTS-SYNC.md (BOOKED 2026-06-04) | P2 | **OPEN** — pinned at 63240e6, behind 8ccf9f4 by 1 commit, 2 uncommitted local edits |
| **inv-27 typecheck** | vue-tsc --noEmit must be green (0 errors) for any close claim to be honest | H-Inv 27; CLAUDE.md typecheck section | P0 | **RED** — 3 errors from glass-ui API skew (BouncyTabs→SegmentedTabs; `glass-carousel`→`carousel` subpath; GlassCarousel/GlassCarouselItem rename) |
| **Ask 3 (ADOPTION-ASKS)** | Convert palette-api rsync deploy-dir to a git checkout under canonical root, then adopt hardened `deploy-hook.sh` (the N1 real fix; the latent-broken deploy arm) | ADOPTION-ASKS.md §3 | P1 | **OPEN** — re-affirmed G.W8 2026-06-01; no change observed |
| **inv-22-color** | Bring `api.color.babb.dev` to the 4-endpoint vhost contract (`/health`/`/docs`/`/openapi.json` currently 404) | ADOPTION-ASKS.md §4; INVARIANTS.md §2.7 | P3 | **OPEN** — booked G.W8 |

### 4.2 Glass-ui API skew (typecheck RED — P0)

The brief states: "npm run typecheck is RED with 3 demo errors from glass-ui API skew
(a glass-carousel subpath import that should be carousel with renamed exports, and
BouncyTabs renamed SegmentedTabs)."

Verified live from the source tree:

| Error location | Old API | New glass-ui API | File |
|---|---|---|---|
| `ComponentSliders.vue:117` | `@mkbabb/glass-ui/glass-carousel` | `@mkbabb/glass-ui/carousel` | `demo/@/components/custom/color-picker/controls/ComponentSliders.vue` |
| `ComponentSliders.vue:117` | `GlassCarousel, GlassCarouselItem` | `Carousel, CarouselItem` (per `src/carousel.ts` exports) | same |
| `MixSourceSelector.vue:4` + `PaneSegmentedControl.vue:18` | `BouncyTabs` from `glass-ui/tabs` | `SegmentedTabs` (confirmed: `glass-ui/src/components/custom/tabs/index.ts` comment: "The former BouncyToggle/BouncyTabs/UnderlineTabs trio + the standalone") | two files |

These errors flow from `file:../glass-ui` resolving to the local checkout at 3.10.1,
which has these API renames relative to the version value.js's demo code was written
against. Fixing `cascade-vjs` alone (bumping to `^3.12.0` from registry) would CHANGE
which glass-ui version is in scope — the registry at 3.12.0 may have these same renames,
or the renames may be the demo code's responsibility. Either way, **inv-27 is violated**:
no green typecheck run id can be cited until these 3 errors are resolved.

### 4.3 Soft / named-forward obligations

| ID | Obligation | Source | Priority | Status |
|---|---|---|---|---|
| **Ask 5** | Converge GH-Pages→CF-CNAME to CF Pages per `deploy/cf/pages-deploy.sh` | ADOPTION-ASKS.md §5 | P3 | OPEN — re-affirmed G.W8 |
| **dock-consumer alignment** | Adopt the `<Role>Dock` vocabulary once glass-ui ships the docs note (name-forward, gated on glass-ui dock base landing) | DOCK-ANIMATION-CONVERGENCE §7 | P3 | BOOKED — glass-ui dock slices landed in glass-ui AT at-dock-convergence |
| **valuejs-sota-handoff-v2** | 6+ waves of parser/interpolation/color improvements (proposals from keyframes F audit) | keyframes.js `docs/tranches/F/valuejs-sota-handoff-v2.md` | P2 | BOOKED — value.js owner scopes/sequences |
| **Tranche M ratification** | value.js tranche M is authored (planning-only 2026-06-04) but never ratified | MEMORY.md; M.md + PROGRESS.md | P1 | UNRATIFIED — 39 commits on branch ahead of master, tranche M planning-only |
| **fourier K.W6 parity confirm** | fourier's own `/diff` + publish envelope parity probe (fourier-local Python probe) — the residual from value.js-J/K/L being DONE | CONSTELLATION.md §9; ADOPTION-ASKS.md §10 | P2 | fourier-owned, not value.js |

---

## 5. Precept-sync state summary

| Repo | Pinned at | Canonical (origin/main) | Gap | Local dirty edits | Action |
|---|---|---|---|---|---|
| **value.js** | `63240e6` | `8ccf9f4` | 1 commit behind | YES — 2 files (LESSONS-LEARNED.md blob entry; SPEC.md expanded π section) | Reconcile local edits → commit to submodule → bump parent pointer. BOOKED per PRECEPTS-SYNC.md |
| fourier-analysis | `8ccf9f4` | `8ccf9f4` | in sync | No | Done |
| keyframes.js | `8ccf9f4` | `8ccf9f4` | in sync | No | Done |
| speedtest | `8ccf9f4` | `8ccf9f4` | in sync | No | Done |
| words | `8ccf9f4` | `8ccf9f4` | in sync | No | Done |
| muster | `8ccf9f4` | `8ccf9f4` | in sync | No | Done |
| glass-ui | `63240e6` | `8ccf9f4` | 1 commit behind | YES (3 files per PRECEPTS-SYNC.md) | BOOKED |

Both value.js and glass-ui remain BOOKED since the 2026-06-04 sync sweep.

---

## 6. Key finding: the dirty precepts edits are a superset of canonical

The local dirty edits in value.js's `docs/precepts/` working tree are NOT
INCOMPATIBLE with 8ccf9f4 — they are an EXPANDED, richer version of the same
edict. Specifically:

- `8ccf9f4` added 18 lines to SPEC.md with a "Before/after capture — every page, paired,
  scripted" block phrased as a standalone paragraph.
- The dirty SPEC.md has a formal `#### Before/after + compare-at-close` subsection with
  bullet-point detail (Affected page / BEFORE / AFTER / COMPARE-AT-CLOSE / WebGL/canvas
  / Archive) that is more actionable and precise.
- The dirty LESSONS-LEARNED.md has a concrete value.js case study (the HeroBlob
  regression) that is not in 8ccf9f4 at all.

The reconciliation path is: merge `8ccf9f4` into the local submodule checkout, manually
integrate value.js's additions (they don't conflict), commit to mkbabb/precepts, then bump
the parent gitlink. Alternatively: push value.js's version as a new commit to origin/main
(since the edits are richer than 8ccf9f4), making them canonical for the whole constellation.

---

## 7. Key finding: CONSTELLATION.md §1 roster is stale

The CONSTELLATION.md §1 roster was re-grounded at 2026-06-04 (§9), but the actual
constellation state has diverged further since then. The value.js row reads:

> "J EXECUTED+GREEN → K CLOSED → L CLOSED (`66dcd68`, 2026-06-04) | branch
> `docs/constellation-grand-audit-2026-06-03`, dirty (precepts)"

The actual current state: branch `tranche-f-handoff`, HEAD `0cb5dd2`, published
`0.11.2`, tranche M authored (planning-only, 2026-06-04). The roster is stale but
CONSTELLATION.md's §9 commentary accurately described the pattern: "reconcile against
reality whenever a member's tranche head moves." No update to the manifest was made
for the M tranche opening.

---

## 8. CANONICAL-ORDERING.md coverage

The CANONICAL-ORDERING.md ends at §20 (ordering ν′, fourier-K-deploy, 2026-06-04). It
covers through fourier tranche K-deploy. No §21 or later section exists. The current
value.js tranche M is NOT reflected in CANONICAL-ORDERING.md — it is a value.js-local
development, and CANONICAL-ORDERING.md only adds value.js entries when there's a
cross-repo cohort relationship (like the J cohort). value.js-M has no cohort edge to
fourier, so its absence from CANONICAL-ORDERING is correct.

---

## 9. Conclusion

**Precept state:** value.js is one canonical commit (8ccf9f4, the π-lane paired-capture
edict) behind, with 2 locally-authored precept edits uncommitted in the working tree. The
dirty edits are RICHER than the canonical version and constitute the original authorship site.
Reconciliation requires committing the submodule edits and bumping the parent pointer.

**Federation obligations:** Four named obligations bind value.js as P0/P1/P2 today:
1. (P0) Fix 3 typecheck errors from glass-ui API skew before any close can cite inv-27
2. (P1) Deploy-arm repair: rsync→git checkout (Ask 3, latent-broken, ADOPTION-ASKS.md)
3. (P2) cascade-vjs: `file:` → `^published` pins for glass-ui and keyframes.js (precept
   invariant 30 violation; ADOPTION-ASKS row; DOCK-ANIMATION-CONVERGENCE §7 booking)
4. (P2) Precepts bump: reconcile dirty local edits → commit → bump gitlink

**What changed since 2026-06-04:** glass-ui moved from 3.2.0 to 3.12.0 on npm (10
releases); keyframes.js moved from 3.0.0 to 4.1.0; a tri-tranche-run coordination layer
was authored; DOCK-ANIMATION-CONVERGENCE.md was authored; valuejs-sota-handoff-v2.md
was authored at keyframes.js F; value.js published 0.11.0 → 0.11.1 → 0.11.2.

**The cohort debt is discharged.** CONSTELLATION.md §9 confirms: value.js's J/K/L
obligation is DONE-in-sibling. The only fourier-side residual is fourier's own parity
probe (fourier-local; K.W6). value.js owes the cohort nothing further on that front.
