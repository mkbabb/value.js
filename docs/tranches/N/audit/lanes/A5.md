# A5 — Chronic & deferred census re-verification

**Lane**: A5 · **Auditor**: deep-audit wave (2026-06-11) · **Branch**: tranche-f-handoff · **HEAD**: 0cb5dd2

Source ledger: `docs/tranches/M/audit/fold-ledger.md` (authored 2026-06-04, planning-only — M never ratified).
All verdicts are re-verified live against the tree at HEAD plus sibling repos:
glass-ui local 3.10.1 (`tranche/AY`), keyframes.js 4.1.0 (`tranche-g-impl`), fourier-analysis tranche K.

---

## §1 — P0 census re-verdict (11 distinct P0s)

### A1/C-LIVE/D-1/E1/F-INV4 — `development` export precept-violation

**DISCHARGED** — by value.js 0.11.1 (`b4defb0`). The `development` key is absent from
`package.json` exports at HEAD (`package.json:24-29`): only `types`, `import`, `default`.
keyframes.js G FINAL.md confirms the upstream unblock was consumed at kf re-pin.

### A2 — L closed against invalidated precondition

**STILL-OPEN** — `docs/tranches/L/FINAL.md` §3 (L.W0) confirms L dispatched on the K.W2
head, NOT the K.W2.5-corrected substrate. K.W2.5 was specced 2026-06-03 and never executed
(plan-only in `docs/tranches/K/PROGRESS.md:12`). The precondition invalidation is unfixed.

### A3/D-3/F-W26 — Desktop pane-visibility P0 (Tailwind v4 `@source` gap)

**STILL-OPEN** — `demo/@/styles/style.css` imports `@mkbabb/glass-ui/styles`, which
provides `@source "../components"` at `glass-ui/src/styles/index.css:152` — scanning
glass-ui's own component tree only. value.js/demo has **zero** `@source` directives for
its own `demo/@/components/` tree. `grep -rn "@source" demo/` hits only a comment at
`style.css:10`. The Tailwind v4 class-scan gap for demo-local utilities is live.

### C-REKA — reka-ui version skew

**STILL-OPEN** — `package.json:95` declares `reka-ui: "^2.0.0"`. glass-ui's devDep at
`glass-ui/package.json:803` is `^2.9.7`; its peer is `^2.0`. The ledger asked for a bump
to `^2.9` in value.js's own declaration to narrow the resolution window; not done.

### C-DTS/E4 — glass-ui `build:watch` JS-only (dts freshness keystone unmet)

**STILL-OPEN** — confirmed: `glass-ui/package.json` scripts show `build:watch = "vite build --watch"` with no `emit-types` call. `emit-types = "vue-tsc --project tsconfig.build.json && node scripts/flatten-subpath-types.mjs"` is only wired into the full `build` script. During watch, `dist/*.d.ts` are stale or absent. `glass-ui/vite.config.ts:15-17` explicitly documents that declarations are out-of-band from vite.

### D-2/E2/F-CH1-8 — glass-ui 3.2.0 "spent anchor"; blob/dock-fix

**CHANGED-SHAPE** — glass-ui shipped 3.3.0 (AV) with dock-rebuild + aurora OETF fix,
3.9.0 (AX), and 3.10.1 (AY close). The original spend-anchor (3.2.0 without blob/dock) is
superseded. However: (a) blob-lift (`./goo-blob`, `./watercolor-dot`) shipped in glass-ui
3.3.0, but value.js demo STILL uses bespoke copies — all 13 import sites use
`@components/custom/goo-blob` / `@components/custom/watercolor-dot`, NOT the glass-ui
subpaths; (b) glass-ui AY W-BLOB3 **deliberately stripped** the `ColorResolver` DI seam
and formally recorded the value.js blob repatriation as BOOKED demo-only (see
`glass-ui/docs/tranches/AY/audit/PRUNE-LEDGER.md:186`). So the shape changed: glass-ui
provides the components, but the lift is now an explicit value.js-N ask, not a
glass-ui-3.3.0 anchor.

### E3 — Publish-spine broke (glass-ui 3.2.0 cut against 0.10.0, aurora cast survived)

**DISCHARGED (spine unblocked)** — value.js 0.11.0 and 0.11.2 are published. glass-ui
peer range is `"^0.10.0 || ^0.11.0"` (`glass-ui/package.json:766`), which accepts 0.11.x.
keyframes.js 4.1.0 pins `"@mkbabb/value.js": "^0.11.2"` (registry, not file:). The
"aurora cast" concern: no surviving cast using value.js types was found in
`glass-ui/src/components/custom/aurora/` at HEAD. The aurora now externalizes value.js as
a peer (CHANGELOG 3.3.0).

*Residual*: glass-ui devDep still `"@mkbabb/value.js": "^0.10.0"` (its own internal dev
pin is stale but inconsequential for consumers; the peer range is correct). Value.js
devDeps use `file:../glass-ui` and `file:../keyframes.js` (demo-dev resolution pattern,
not a publish-spine defect).

### F-AURORA / CH-2 — aurora-derive-from-color

**STILL-OPEN** — `deriveAurora` is defined in glass-ui at
`glass-ui/src/components/custom/aurora/composables/color.ts` and exported via
`glass-ui/src/components/custom/aurora/index.ts`. Value.js demo (`demo/color-picker/App.vue:212`)
initializes `const auroraConfig = reactive<AuroraConfig>(structuredClone(DEFAULT_AURORA_CONFIG))`
and never calls `deriveAurora`. `grep -rn "deriveAurora" demo/` → 0 hits. The wire
(aurora palette derives from active color) is unimplemented.

### F-BLOB / CH-1 / CH-3 — blob-extirpation

**CHANGED-SHAPE (producer shipped; consumer still bespoke)** — glass-ui exports
`./goo-blob` and `./watercolor-dot` subpaths at 3.10.1 (`glass-ui/package.json:415,
479`). Value.js demo still has:
- `demo/@/components/custom/goo-blob/` (GooBlob.vue, types.ts, composables/, shaders/)
- `demo/@/components/custom/watercolor-dot/` (WatercolorDot.vue, composables/)
- All 13 consumer import sites use `@components/custom/goo-blob` / `@components/custom/watercolor-dot`

glass-ui AY W-BLOB3 stripped the ColorResolver DI seam and formally booked value.js
repatriation as a named future trigger (`PRUNE-LEDGER.md:186`: "BIND a real consumer #2
(value.js repatriation — the DI seam was built for it and it never arrived) OR formally
book demo-only + STRIP the speculative ColorResolver DI ceremony" — **STRIP chosen**).
The lift is now a value.js-N obligation.

### L-PC-1 — WithId read-side transposition (25 casts / 13 files)

**STILL-OPEN** — live count at HEAD: `grep -rn "as.*& {.*_id" api/src/` returns **26
instances** across 13 files (25 singular + 1 array form at
`services/palette/forks.ts:206`). Ledger said 25; actual is 26 (the array-cast was
missed). All four model types confirmed:
- Palette: `crud.ts` (5), `crud-list.ts` (3), `forks.ts:routes` (3), `forks.ts:services` (2), `versions.ts:routes` (1), `visibility.ts` (2), `users.ts:admin` (1) = 17
- ProposedName: `queries.ts` (3), `proposals.ts` (1), `colors.ts:admin` (1) = 5
- Tag: `tags.ts:admin` (1), `queries.ts` (1) = 2
- AdminAuditEvent: `audit.ts` (1) = 1
- Array (Palette): `forks.ts:services:206` = 1 (unlisted in the ledger's "25")

### F-PUBLISH-SPINE — publish spine un-started

**PARTIALLY DISCHARGED** — value.js 0.11.2 is on npm (published). keyframes.js 4.1.0
consumes `^0.11.2` from registry. glass-ui peer range covers `^0.11.0`. Value.js v1.0.0
(the full-spine milestone) is not yet published. Value.js devDeps still declare
`file:../glass-ui` and `file:../keyframes.js` — appropriate for dev/demo resolution but
the M.W9 v1.0.0 close event (registry consumption) is not done.

---

## §2 — Chronic spine re-verdict

### CH-1 — Metaballs/GooBlob API → glass-ui lift

**STILL-OPEN (changed shape)** — glass-ui `./goo-blob` subpath exists and exports
`GooBlob` at `glass-ui/src/components/custom/goo-blob/index.ts`. But value.js demo
`GooBlob` imports from `@components/custom/goo-blob` (own copy). The lift is booked
value.js-side per AY W-BLOB3 decision.

### CH-2 — Aurora derive helper (`deriveAurora`)

**STILL-OPEN** — producer shipped (glass-ui 3.3.0+). Consumer wiring in value.js demo is
absent (App.vue uses `DEFAULT_AURORA_CONFIG`, never `deriveAurora`). AY W-AUR2 confirmed
the `deriveAurora` composable is shipped and stabilized; the `<Aurora derive-color>` prop
was RETIRED as substrate-without-consumer. Consumer wiring is value.js-N work.

### CH-3 — `BlobDot`/WatercolorDot lift

**STILL-OPEN (changed shape)** — same as CH-1/F-BLOB. glass-ui ships `./watercolor-dot`
(WatercolorDot.vue + useWatercolorBlob.ts). Value.js demo still uses its own copy. AY
PRUNE-LEDGER KEEP-EVIDENCES watercolor-dot at glass-ui (1 internal consumer, the value.js
repatriation as the named ≥2-consumer trigger).

### CH-4..CH-8 — glass-ui primitive asks (Select size, clampLabel, Tooltip mono, Button icon-sm, Tabs underline)

**PARTIALLY DISCHARGED**:
- **Button `icon-sm`**: DISCHARGED — `glass-ui/src/components/ui/button/index.ts:94` has `'icon-sm': 'h-(--control-h-xs) w-(--control-h-xs) p-0'`.
- **Select `size`**: DISCHARGED — `glass-ui/src/components/ui/select/SelectTrigger.vue:13` has `size?: 'sm' | 'default'`.
- **Tabs underline**: DISCHARGED — `SegmentedTabs` variant `"underline"` shipped (AV) at `glass-ui/src/components/custom/tabs/SegmentedTabs.vue:52`.
- **clampLabel**: NOT FOUND — no `clampLabel` prop or utility in glass-ui src at HEAD.
- **Tooltip mono**: NOT FOUND — no Tooltip mono-font variant in glass-ui at HEAD; `IconTooltip.vue:26` uses `font-display`, not mono.

CH-6/CH-8 ship-or-KILL note: BouncyTabs was deleted (unified into SegmentedTabs at AV/3.3.0). CH-8 is DISCHARGED by the unification.

### CH-9 — `dist/glass-ui.css` font-inlining residual

**STILL-OPEN** — no evidence of font-inlining resolution in glass-ui. The `@font-face` declarations remain in `src/styles/typography.css` (Tailwind-source path), not inlined into `dist/glass-ui.css`. The `fs.allow` concern is unreduced.

### CH-10 — keyframes precept-pin convergence

**CHANGED-SHAPE** — keyframes.js 4.1.0 now pins `value.js ^0.11.2` and `parse-that ^0.9.0` from registry (no file: links). glass-ui devDep pins `@mkbabb/keyframes.js: "^4.1.0"` (registry). The convergence is partially done. Residual: value.js's own devDep for keyframes is still `file:../keyframes.js` (demo-dev resolution). This is a dev-only issue, not a publish-spine defect.

### CH-11 — aurora+blob precept-§10 wire-before-retire

**STILL-OPEN** — depends on CH-2 (aurora wire) and CH-3 (blob lift), both still open.

### CH-13 — fourier Phase-0 quiescence

**BOOK** — fourier-owned; unchanged. Fourier K tranche (2026-06-04) is IMPL with a deploy-spine focus; no value.js lever.

### CH-14a — gh-pages OIDC-auth half → CF-Pages

**DISCHARGED** — `scripts/deploy.sh` (deploy_frontend section) implements the CF-Pages deploy via `wrangler pages deploy`. The `demo/color-picker/public/CNAME` is still present for GH-Pages domain, but the deploy path is CF-Pages. Residual M.W8.B item (formal CF-Pages CNAME retirement) is minor.

### CH-12 — speedtest CW Phase-2 value.js-participation

**KILLED — permanent** (confirmed unchanged).

---

## §3 — K-carry re-verdict

| ID | Live state | Verdict |
|---|---|---|
| K-INV4 | `development` key absent from exports | **DISCHARGED** — 0.11.1 |
| K-W26 | no @source for demo components | **STILL-OPEN** (= A3) |
| K-DOCK | BouncyTabs stale import (2 sites); glass-ui SegmentedTabs shipped | **STILL-OPEN** — value.js migration needed |
| K-PARSE | 11 `as ValueUnit<Color<…>>` casts post `parseCSSColor` in demo | **STILL-OPEN** |
| K-VAL1 | no `springLinearStops` / linear() parser in value.js easing | **STILL-OPEN** |
| K-W3DIFF | no `PaletteDiff.vue` found in demo | **STILL-OPEN** (may not have been created) |
| K-PALID | `Palette.id: string` (required, not optional) — API doesn't guarantee `id` for Palette (uses slug as PK) | **STILL-OPEN** |
| K-INV5 | `VITE_API_URL` still used at `demo/@/lib/palette/api/client.ts:29` | **STILL-OPEN** |
| K-DISP | `dispatch.ts` = 349 LoC (within cap); no hue-cluster extraction to `mix.ts` | **STILL-OPEN** |
| K-W5RT | `vue-router: "^4.6.4"` in package.json; no upgrade to v5 | **STILL-OPEN** |

**K-DOCK detail**: value.js demo has 2 `BouncyTabs` imports (`demo/@/components/custom/mix/MixSourceSelector.vue:4`, `demo/@/components/custom/panes/PaneSegmentedControl.vue:18`) that import from `@mkbabb/glass-ui/tabs` — a deleted export (AV/3.3.0). These cause typecheck failures (2 of the 3 reported errors from the brief). The glass-ui AY W-CONSUMER ledger covers fourier-analysis and words/frontend but **does NOT include value.js** (`W-CONSUMER-ledger.md` enumerated 5 stale imports across 2 siblings — value.js is absent). This is a net-new finding.

**K-PARSE detail**: `grep -rn "parseCSSColor.*as\|as ValueUnit<Color" demo/` → 11 casts across 8 files, exceeding the ledger estimate of 9-11. The `parseCSSColor` function IS typed (`(input: string): ValueUnit` in the memoized wrapper), but the return `ValueUnit` is generic and callers narrow it with explicit `as ValueUnit<Color<ValueUnit<number>>, "color">` casts. K-PARSE's fix is annotating the memoize return to flow through, deleting all 11 casts.

---

## §4 — Architectural transpositions re-verdict

| ID | Live state | Verdict |
|---|---|---|
| L-PC-1 | 26 `as <Model> & {_id: unknown}` casts / 13 files (see §1) | **STILL-OPEN** |
| L-PC-2 | `as Record<string, unknown>` in aggregation (audit.ts:64/73, users.ts:49) | **STILL-OPEN** |
| E1 | 3+ divergent CSS-color→RGB resolvers: `cssToRgb` (useMixingAnimation.ts:28), `cssColorToRgb` (useMetaballRenderer.ts:58), `resolveColor` (two gradient composables) | **STILL-OPEN** |
| E2/T1 | `useWatercolorBlob` has no PRM gate — `requestAnimationFrame` runs unconditionally when `animate=true`; no `matchMedia("(prefers-reduced-motion)")` in either the demo or glass-ui copy | **STILL-OPEN** |
| E4/T3 | `useLayerTransition` at `demo/@/components/custom/dock/composables/useLayerTransition.ts:1` is explicitly labelled "Local fork"; glass-ui exports its own at `glass-ui/src/components/custom/dock/composables/index.ts:4` | **STILL-OPEN** |
| K-PARSE | (see §3) | **STILL-OPEN** |
| K-DISP | (see §3) | **STILL-OPEN** |

**E1 detail**: Found `cssToRgb` at `demo/@/components/custom/mix/composables/useMixingAnimation.ts:28`, `cssColorToRgb` at `demo/@/components/custom/goo-blob/composables/useMetaballRenderer.ts:58`, and two `resolveColor` at `demo/@/components/custom/gradient/composables/useGradientInterpolation.ts:37` and `useGradientCSS.ts:25`. glass-ui provides `useResolveTokenColor` at `glass-ui/src/composables/dom/useResolveTokenColor.ts` as the canonical single un-wrap path. Value.js demo does not import it (0 hits).

---

## §5 — Infra asks re-verdict

| Ask | Live state | Verdict |
|---|---|---|
| Ask 3 — rsync→git | `scripts/deploy.sh` `deploy_api()` uses `git push` + webhook + health-gate; no rsync | **DISCHARGED** |
| Ask 5 — CF-Pages CNAME | `scripts/deploy.sh` `deploy_frontend()` uses `wrangler pages deploy`; CF-Pages project | **DISCHARGED** |
| inv-22-color | `api/src/index.ts` has no `/health`, `/docs`, `/openapi.json` routes | **STILL-OPEN** |

**Note**: `scripts/deploy-hook.sh` was referenced in `deploy.sh` comments but does NOT exist in the tree (`ls scripts/` → check-types.mjs, deploy.sh, dev.sh, generate-favicon.mjs). The API deploy relies on an on-host script invoked by the adnanh/webhook receiver — its absence from the repo is not necessarily a defect (it may live on-host), but it is worth noting.

---

## §6 — Doc-drift re-verdict

| Item | Live state | Verdict |
|---|---|---|
| VAL-9 re-bookings not struck from K.md §7 | `docs/tranches/K/K.md:169` shows VAL-9 **KILLED** with explanation; earlier ledger framing struck | **DISCHARGED** |
| dirty `docs/precepts` submodule | `git submodule status docs/precepts` → `m docs/precepts` (modified) | **STILL-OPEN** |
| K PROGRESS.md K.W2 inv-K-4 framing | `PROGRESS.md:11` still describes inv-K-4 mechanism-A as "CLOSED" alongside K.W2.5 corrective | **STILL-OPEN** (confusing framing persists) |
| `request-coverage.md` over-claims | `docs/tranches/K/audit/request-coverage.md:5` has ADDRESSED claim for contract-v2 TS-half | **STILL-OPEN** |
| `demo/CLAUDE.md` inadvertent revert | Memory says "RESOLVED (recovered from dangling blob this session)" at L-close | **DISCHARGED** |

---

## §7 — L-SEED re-verdict

| Monitor | Live state | Verdict |
|---|---|---|
| cron-txn | `api/src/cron.ts:50-51`: `listAllSlugs()` called outside transaction, then `deleteOrphaned(paletteSlugs)` — the slug-list read and vote-delete are non-atomic; a palette could be deleted between the two calls leaving orphaned votes | **STILL-OPEN** — live TOCTOU |
| bench-script | No `bench/` directory with palette API benches; only `bench/color-*.mjs` files — the phantom monitor | **DROPPED** per ledger |
| I-tail conformance | Landed at K.W2 api-lane (161/161 api tests) | **KEEP (closed)** |

---

## §8 — NET-NEW deferrals to value.js (since 2026-06-04)

Items booked TO value.js in sibling-repo tranche docs created after the M audit:

### N-NEW-1 — BouncyTabs stale imports NOT in glass-ui W-CONSUMER ledger

**Source**: `glass-ui/docs/tranches/AY/audit/W-CONSUMER-ledger.md` (2026-06-09). The
ledger covers 5 stale imports across fourier-analysis and words/frontend. value.js has 2
`BouncyTabs` stale imports (`demo/@/components/custom/mix/MixSourceSelector.vue:4`,
`demo/@/components/custom/panes/PaneSegmentedControl.vue:18`) that are NOT in the ledger.
These import a deleted symbol from `@mkbabb/glass-ui/tabs` (AV/3.3.0 clean break). They
cause 2 of the 3 typecheck errors reported in the brief. The carrier-wave needed: value.js
N.W-TABS-MIGRATE. The migration is the drop-in `<SegmentedTabs>` swap as documented in
the W-CONSUMER ledger rationale.

### N-NEW-2 — glass-carousel subpath + export rename

**Source**: `demo/@/components/custom/color-picker/controls/ComponentSliders.vue:117`
imports `{ GlassCarousel, GlassCarouselItem }` from `@mkbabb/glass-ui/glass-carousel`.
Two bugs: (a) the subpath is `./carousel` not `./glass-carousel`
(`glass-ui/package.json:415`); (b) exports are `Carousel`, `CarouselContent`, etc. — NOT
`GlassCarousel` or `GlassCarouselItem` (`glass-ui/src/carousel.ts` exports
`GlassCarouselPager` but not `GlassCarousel`). This is the third typecheck error. Carrier
wave: N.W-CAROUSEL-FIX.

### N-NEW-3 — glass-ui AY W-BLOB3 formally books value.js blob repatriation

**Source**: `glass-ui/docs/tranches/AY/FINAL.md` W-BLOB3 wave disposition (2026-06-10).
glass-ui deliberately stripped the `ColorResolver` DI seam (`proof:no-value-default`
retired; `PRUNE-LEDGER.md:186` records `goo-blob` as KEEP with `"value.js repatriation"`
as the named ≥2-consumer trigger). The value.js-side obligation: demo must migrate its
bespoke `goo-blob` + `watercolor-dot` to the glass-ui subpaths OR maintain them as
demo-only with a formal `consumer-evidence` doc. This is the CH-1/CH-3 obligation now
formally ledgered from the glass-ui side.

### N-NEW-4 — keyframes.js G handoff: value.js-HANDOFF residuals still open

**Source**: `keyframes.js/docs/tranches/G/valuejs-parsethat-glassui-handoff.md` and
`keyframes.js/docs/tranches/G/FINAL.md:186-197`. The following value.js-owned items
were booked from keyframes.js G (none yet landed in value.js):
- **VJ-F1**: path-geometry sampler (SVG point/tangent at fraction) — unblocks kf MorphSVG
- **VJ-E1/E2**: `linear()` / `steps()` CSS easing string parsers — would retire kf's `parseLinearStops` shim; `parseCSSLinear` does not exist in `src/parsing/`
- **MCI-5**: identity-aware fn-arity pad (flip kf's `it.fails` `proof:fn-arity-pad` witness)
- **VJ-F2**: structured-diagnostics sink + `tryParse` `furthest` swap
- **VJ-F6**: LRU bound on `getComputedValue.cache`

Of these, VJ-E1/E2 and VJ-F6 are the most concrete near-term asks. `src/utils.ts` memoize has `maxCacheSize` already but `getComputedValue` cache is unbounded. The `linear()` parser at `src/easing.ts` handles execution (cssLinear) but not CSS string parsing.

---

## §9 — Summary verdict table

| Ledger row | Verdict |
|---|---|
| A1/C-LIVE/D-1/E1/F-INV4 — development export | **DISCHARGED** — 0.11.1 |
| A2 — L closed on invalid precondition | **STILL-OPEN** |
| A3/D-3/F-W26 — @source gap | **STILL-OPEN** |
| C-REKA — reka bump | **STILL-OPEN** |
| C-DTS/E4 — build:watch JS-only | **STILL-OPEN** |
| D-2/E2/F-CH1-8 — blob/dock anchor | **CHANGED-SHAPE** — anchor superseded; blob now N-obligation |
| E3 — publish-spine | **DISCHARGED** (partial spine; v1.0.0 still pending) |
| F-AURORA/CH-2 — aurora-derive | **STILL-OPEN** |
| F-BLOB/CH-1/CH-3 — blob-extirpation | **CHANGED-SHAPE** — glass-ui ships; demo still bespoke; N-obligation |
| L-PC-1 — WithId 25→26 casts | **STILL-OPEN** (26 live) |
| F-PUBLISH-SPINE — v1.0.0 | **PARTIALLY DISCHARGED** |
| CH-1 — Metaballs lift | **STILL-OPEN** (changed shape) |
| CH-2 — deriveAurora | **STILL-OPEN** |
| CH-3 — WatercolorDot lift | **STILL-OPEN** (changed shape) |
| CH-4..CH-8 — glass-ui primitives | **PARTIALLY** — icon-sm+select-size+underline DISCHARGED; clampLabel/Tooltip-mono NOT FOUND |
| CH-9 — font-inlining | **STILL-OPEN** |
| CH-10 — keyframes precept-pin | **CHANGED-SHAPE** — kf registry, value.js devDep still file: |
| CH-11 — aurora+blob §10 wire | **STILL-OPEN** |
| CH-12 — speedtest CW Phase-2 | **KILLED** |
| CH-13 — fourier Phase-0 | **BOOK** |
| CH-14a — gh-pages OIDC | **DISCHARGED** |
| K-INV4 | **DISCHARGED** |
| K-W26 | **STILL-OPEN** |
| K-DOCK | **STILL-OPEN** (BouncyTabs stale, not ledgered in glass-ui) |
| K-PARSE | **STILL-OPEN** (11 casts) |
| K-VAL1 | **STILL-OPEN** |
| K-W3DIFF | **STILL-OPEN** |
| K-PALID | **STILL-OPEN** |
| K-INV5 | **STILL-OPEN** |
| K-DISP | **STILL-OPEN** |
| K-W5RT | **STILL-OPEN** |
| L-PC-1 | **STILL-OPEN** |
| L-PC-2 | **STILL-OPEN** |
| E1 — CSS-color resolvers | **STILL-OPEN** |
| E2/T1 — watercolor-dot PRM | **STILL-OPEN** |
| E4/T3 — useLayerTransition fork | **STILL-OPEN** |
| Ask 3 — rsync→git | **DISCHARGED** |
| Ask 5 — CF-Pages | **DISCHARGED** |
| inv-22-color | **STILL-OPEN** |
| doc-drift: VAL-9 struck | **DISCHARGED** |
| doc-drift: precepts dirty | **STILL-OPEN** |
| doc-drift: K PROGRESS.md framing | **STILL-OPEN** |
| doc-drift: request-coverage.md | **STILL-OPEN** |
| doc-drift: demo/CLAUDE.md | **DISCHARGED** |
| L-SEED cron-txn | **STILL-OPEN** |
| L-SEED bench | **DROPPED** |
| L-SEED I-tail | **KEEP (closed)** |

**Net**: 8 DISCHARGED · 4 CHANGED-SHAPE · 28 STILL-OPEN · 2 BOOK/KILL · 4 NET-NEW
