# Lane E6 — Cohort & publish-spine: live cross-repo state (evidence-grounded)

**Date:** 2026-06-11 · **Repo:** value.js @ `tranche-f-handoff` HEAD `0cb5dd2` · value.js published **0.11.2**
**Scope:** Establish the live cross-repo constellation state (glass-ui, keyframes.js, fourier-analysis)
with registry + on-disk evidence; determine what remains on the spine to value.js **v1.0.0**; which
formerly cohort-gated M waves are now unilateral; the exact version-pin moves for tranche N.

**Verdict:** The spine is **walked**. value.js is published (0.11.2), every producer the chronic
mandates waited on is published (glass-ui ships blob/aurora/dock/watercolor on the registry;
keyframes 4.1.0 already deps `^0.11.2`). **No producer blocks value.js v1.0.0.** N.md §3 already
captures this correctly (it even cites this lane at line 129). My lane confirms and grounds the claims
with registry evidence and flags three precise residuals.

---

## 1. value.js — the pure sink (own state)

| Field | Value | Evidence |
|---|---|---|
| name / version | `@mkbabb/value.js` **0.11.2** | `package.json:2,19`; `npm view value.js version` → `0.11.2` |
| runtime deps | **only** `@mkbabb/parse-that@^0.8.2` | `package.json` deps; no cohort peer |
| peerDeps | **{} (none)** | `package.json` |
| exports map | single `"."` → `./dist/value.js` (precept-clean; `development` key gone since 0.11.1 `4c8c532`) | `package.json:28-29` |
| devDeps (cohort) | `@mkbabb/glass-ui: file:../glass-ui` (L68), **`@mkbabb/keyframes.js: file:../keyframes.js` (L69)** | `package.json:68-69` |

**P1 — phantom keyframes devDep.** `package.json:69` declares `@mkbabb/keyframes.js: file:../keyframes.js`
as a devDep, but **value.js imports keyframes.js nowhere**. Verified: `grep -rE "from ['\"]@mkbabb/keyframes"
src/ demo/` returns **zero** import sites. (The 10 grep hits for the bare word "keyframes" in
`src/easing.ts`, `src/parsing/*.ts`, `demo/@/styles/animations.css` etc. are the CSS `@keyframes`
concept, not the package import.) This is a dead dependency edge — drop it. (N.md §3 step 2 line 129
already books this as "Drop the phantom `@mkbabb/keyframes.js` devDep … (E6).")

**Topology fact (verified, not from prose):** value.js depends on **no cohort peer** at the library
level. Its only `value.js → glass-ui` edge is `value.js(demo) → @mkbabb/glass-ui` (82 consume sites,
below), which is **demo-only and unpublished**, so the graph stays **acyclic**. value.js is the **pure
sink** — publish it first. This is already true: 0.11.2 is live.

**value.js demo glass-ui consumption (82 sites):** bare `@mkbabb/glass-ui` 38×, `/dock` 15×, `/search`
5×, `/dom` 3×, `/confirm-dialog` 3×, `/tabs` 2×, `/forms`/`/dark`/`/controls`/`/configurator`/`/aurora`
1× each, **and `/glass-carousel` 1×** — a subpath **no glass-ui version ever exported** (registry 3.12.0
ships `./carousel`, not `./glass-carousel`). This is one of the three typecheck-RED demo errors
(cross-lane: C1/N.W1.A). The `/aurora` import (1×) is the only blob/aurora producer-subpath the demo
consumes today — `deriveAurora` is consumed **0×** (N.md table line 94).

---

## 2. glass-ui — the consumer that already shipped the producers

### 2.1 Local checkout vs registry — STALENESS CONFIRMED

| | Version | Branch / HEAD |
|---|---|---|
| **Local checkout** `/Users/mkbabb/Programming/glass-ui` | **3.10.1** | `tranche/AY`, HEAD `7a19098` (2026-06-11 15:58 "AZ.W-MOTION2 COMPLETE") |
| **Registry `latest`** | **3.12.0** | published 2026-06-11 11:48 |

**P1 — local glass-ui checkout is stale by 2 minor lines** (3.10.1 on disk vs 3.11.0/3.11.1/3.11.2/3.12.0
on the registry). The `file:../glass-ui` symlink in value.js's devDeps therefore resolves a **3.10.1
tree** (its `package.json` version is `3.10.1`). The branch HEAD has advanced into AZ-era motion work
(`tranche/AY` is the branch name but commits read "AZ.W-MOTION2"), but **the `package.json` version
field was not bumped past 3.10.1 on this branch** — so a `file:` install gets pre-3.11 content. N.W1.C /
N.W5 plan to rebuild a clean local dist before consuming; N.W9 pins the registry `^3.13.0`.

### 2.2 Published version timeline (registry, `npm view @mkbabb/glass-ui time`)

```
3.1.1  2026-06-02   3.5.1  2026-06-07   3.9.0  2026-06-09
3.2.0  2026-06-04   3.6.0  2026-06-07   3.10.0 2026-06-10  ← deprecated
3.3.0  2026-06-06   3.7.0  2026-06-08   3.10.1 2026-06-10  ← true AY close (pin)
3.4.0  2026-06-07   3.8.0  2026-06-08   3.11.0 2026-06-10  ← stale-lineage (self-declared)
3.5.0  2026-06-07                       3.11.1 2026-06-10  3.11.2 2026-06-10  3.12.0 2026-06-11
```

### 2.3 What shipped 3.3.0 → 3.12.0 (from local CHANGELOG.md + registry exports)

- **3.3.0 (AV, 2026-06-06):** dock rebuilt from first principles (single `SpringProgress` width-morph
  driver, the dual-driver freeze killed); **aurora OETF/gamma fix** (`linearToSrgb` before write,
  shared `procedural-color.glsl.ts` chunk); two-slider unification; iOS-26 Liquid Glass token
  evolution; offscreen-pause perf; god-module decomposition (no `src/` > 500 LoC); **the blob trio +
  `/color` leaf carried** (`/goo-blob` + `/watercolor-dot` subpaths, the OKLCh shader proven
  bit-identical to value.js's CPU port — `proof:blob-color-equivalence`). **Registry confirms 3.3.0
  already exports `./aurora`, `./goo-blob`, `./watercolor-dot`** — the blob/aurora producers shipped at
  3.3.0, contradicting M.md's premise that they were *future* work gated on a 3.3.0 cut.
- **3.9.0 (AX, GOLDEN convergence):** `--glass-level` single typed inheriting `@property` scalar; dock
  perfected on one `<GlassDock>` root; every page a glass container over a rich background; the
  live-verified-ledger forcing function; **`BouncyTabs`/`SegmentedTabs` reconciliation** (one of the
  three demo typecheck-RED errors — `BouncyTabs` renamed `SegmentedTabs`, value.js demo
  `PaneSegmentedControl.vue:18` still imports the old name; cross-lane C1).
- **3.10.0 → 3.10.1 (AY, the corrective close):** substrate rebuilds (blob mount fix, van-Gogh aurora
  analytic crescent-comma dabs, configurators on the library `Configurator`); dock finished (perfect
  circle collapsed pill, layout-isolated morph); ruthless prune of 4 zero-consumer subpaths; **NEW
  `/underline`** (`<GlassUnderline>`). 3.10.0 is **deprecated on npm** ("Stale pre-close artifact …
  pin 3.10.1"); 3.10.1 is the true close cut.
- **3.11.0 → 3.12.0:** the local CHANGELOG.md (frozen at 3.10.1) does **not** document these. They are
  the AZ-era continuation (motion gallery, dock RAIL3 redress per the branch HEAD commit message).
  glass-ui's own MEMORY/judgment marks 3.11/3.12 **stale-lineage** (next clean cut **3.13.0**). Note:
  3.11.0/3.11.1/3.11.2/3.12.0 are **NOT formally `npm deprecate`'d** (only 3.10.0 carries a deprecation
  string) — the staleness is a project-internal judgment, not a registry flag.

### 2.4 glass-ui's value.js peer/dev range (THE spine constraint on glass-ui's side)

| Source | value.js range | Note |
|---|---|---|
| **Registry 3.12.0** peerDeps | `^0.10.0 \|\| ^0.11.0` | satisfies value.js 0.11.2 ✓ — but never tightened to drop 0.10 |
| Registry 3.12.0 keyframes peer | `^2.2.0 \|\| ^3.0.0 \|\| ^4.0.0` | satisfies keyframes 4.1.0 ✓ |
| **Local 3.10.1** peerDeps | `^0.10.0 \|\| ^0.11.0` | same |
| **Local 3.10.1 devDeps** | **`^0.10.0`** | **STALE** — excludes 0.11.x; glass-ui builds/tests against an old value.js |

**P2 — glass-ui's value.js *devDep* is stale (`^0.10.0`).** Its *peer* range admits 0.11.x (so consumer
installs resolve value.js 0.11.2 fine), but its *devDep* is `^0.10.0`, meaning glass-ui's own
build/test pulls a 0.10 value.js. This is **glass-ui-owned**, not a value.js v1.0.0 blocker, but it
means glass-ui has never actually CI-verified against 0.11.x. N.md §3 line 120 books this as "its
*devDep* `^0.10.0` is stale — glass-ui-owned ask." The clean fix rides glass-ui's 3.13.0 cut.

---

## 3. keyframes.js — consumes value.js, NOT published

| Field | Value | Evidence |
|---|---|---|
| version / branch | **4.1.0**, branch `tranche-j-dev`, HEAD `f5df9f8` (2026-06-11 "tranche-J WZ close") | `git`, `package.json` |
| **value.js dep** | **`@mkbabb/value.js: ^0.11.2`** (runtime dep) | `package.json` deps |
| import sites | **24** across `src/animation/*` (utils, waapi, group, format, constants, engine, adapter, index, animations, frame-compiler) | `grep -rE "@mkbabb/value" src/` |
| symbols imported | `COMPUTED_UNITS`, `unflattenObjectToString`, `ValueUnit`, `lerp`, `CSSCubicBezier`, `steppedEase`, `Stylesheet` (type), … | `grep` |
| **published?** | **NO** — `npm view @mkbabb/keyframes` → **E404 Not Found** | registry |

**Facts for the brief:** keyframes' value.js dep range is **`^0.11.2`** (already pinned to the current
value.js — this is the tightest pin in the constellation and it *forced* the 0.11.2 publish during the
keyframes-F hand-off, per N.md line 28). keyframes is **deep into value.js** (24 import sites,
core symbols). It is **not published to npm** (E404). value.js consumes keyframes **nowhere** (the
phantom devDep, §1) — the edge is strictly one-directional: keyframes → value.js. The spine direction
is correct and acyclic.

---

## 4. fourier-analysis — consumes value.js (web subpackage)

| Field | Value | Evidence |
|---|---|---|
| repo root | `private` Playwright-only shell (no name/deps) | `package.json` |
| **web subpackage** | `fourier-analysis-web`, branch `master`, HEAD `0167268` | `web/package.json` |
| value.js dep | **`@mkbabb/value.js: ^0.10.0`** | `web/package.json` |
| glass-ui dep | `@mkbabb/glass-ui: ^3.1.0` | same |
| keyframes dep | `@mkbabb/keyframes.js: ^2.2.0` | same |
| import sites | **5** `from "@mkbabb/value.js"` in `web/src` | `grep` |

**P2 — fourier-web pins `@mkbabb/value.js@^0.10.0`, which EXCLUDES 0.11.x.** `^0.10.0` resolves to
`>=0.10.0 <0.11.0` (caret pre-1.0 semver pins the minor), so fourier-web cannot install value.js
0.11.2. This is **fourier-owned** (not a value.js blocker) but it's a real consumer-staleness edge: if
the constellation wants fourier on the current value.js, fourier-web must bump `^0.10.0 → ^0.11.0`
(also glass-ui `^3.1.0 → ^3.13.0`, keyframes `^2.2.0 → ^4.x` to match the cohort). N.md §3 line 121
books this as "fourier-web pins `^0.10.0` (excludes 0.11.x — fourier-owned bump ask)." **Yes, fourier
consumes value.js** (the brief's question) — 5 sites in `web/src`.

---

## 5. What remains on the spine to value.js v1.0.0

The acyclic spine `value.js(lib) → glass-ui(lib) → keyframes(lib) → value.js(lib)` (sink-first) is
already **walked**:

1. **value.js published — DONE** at 0.11.2 (exports precept-clean since 0.11.1; `development` key gone).
2. **glass-ui producers published — DONE.** Registry 3.12.0 (and even 3.3.0) ship `./goo-blob`,
   `./aurora`, `./watercolor-dot`, `./dock`. glass-ui's peer admits value.js 0.11.x.
3. **keyframes consumes the current value.js — DONE.** 4.1.0 deps `^0.11.2`.

**Therefore NO producer blocks value.js v1.0.0.** The remaining work is value.js-internal +
consumer-wiring, all **unilateral** except the single 3.13.0 pin wait:

- **Unilateral now:** drop the phantom keyframes devDep; the v1.0.0 version cut; tighten own types;
  consume the already-published glass-ui blob/aurora/watercolor subpaths (the wiring, not a cohort wait).
- **The ONE cross-repo wait:** glass-ui's **3.13.0** clean cut (since glass-ui self-declares 3.11/3.12
  stale-lineage). This gates **only the final registry pin** (`file:../glass-ui → ^3.13.0`), not any
  feature work — N.W5 consumes against a clean *local* dist; N.W9 swaps the pin to `^3.13.0`.

---

## 6. Formerly cohort-gated M waves now UNILATERAL (producer already shipped)

M.md (2026-06-04, never ratified) gated frontend waves on a **future** glass-ui 3.3.0 cut. That cut
**already happened** (2026-06-06) and shipped the producers. So:

| M wave | M's premise (cohort-gated) | Live reality | Status now |
|---|---|---|---|
| **M.W5** aurora re-anchor | "wire `deriveAurora` after glass-ui 3.3.0 ships it" | glass-ui ships `./aurora` since 3.3.0 (3.12.0 live); `deriveAurora` consumable | **UNILATERAL** — consumer wiring (→ N.W5.B) |
| **M.W7** blob extirpation | "lift bespoke blobs → glass-ui 3.3.0" | glass-ui ships `./goo-blob` (superset) since 3.3.0 | **UNILATERAL** — delete the 1270-LoC fork, consume (→ N.W5.A) |
| **M.W7-D** watercolor-dot | cohort | glass-ui ships `./watercolor-dot` since 3.3.0 | **UNILATERAL** — consume (→ N.W5.C) |
| **M.W4** value.js 0.11.0 publish + spine head | "publish 0.11.0 first" | published — overshot to **0.11.2** during keyframes-F hand-off | **MOOT/DONE** — M's lead lane + spine head spent |
| **M.W1** strike `development` key | v1.0.0 gate blocker | deleted at **0.11.1** (`4c8c532`) | **DONE** |

**Conclusion:** every M cohort gate has **dissolved into unilateral consumer wiring** (N.md §1 line 29-33
states this; this lane grounds it with registry export evidence). N correctly re-frames the blob/aurora
mandates as N.W5 wiring waves, not cohort waits.

---

## 7. Exact version-pin moves for tranche N

1. **value.js `package.json:69` — DELETE** `"@mkbabb/keyframes.js": "file:../keyframes.js"` (phantom
   devDep; zero imports). [P1, unilateral, N.W9.A / N.W8]
2. **value.js demo glass-ui pin** — hold `@mkbabb/glass-ui: file:../glass-ui` through N.W5 (against a
   **clean rebuilt local dist**, since the on-disk 3.10.1 tree is pre-3.11 content), then migrate to
   **`^3.13.0`** when glass-ui cuts it (3.11/3.12 self-declared stale; do NOT pin 3.12.0). Add the
   3.12.0+ transitive peers required (N.md cites `pencil-boil`/`pastel-rainbow`-class additions). [P1,
   N.W9.A — the one cross-repo-gated move]
3. **value.js → 0.12.0** after N.W7 (library-asks wave); **v1.0.0** at N.W9 close. [unilateral]
4. **(glass-ui-owned, track as cohort ask):** glass-ui devDep `@mkbabb/value.js: ^0.10.0 → ^0.11.0`
   (and peer can drop `^0.10.0`); rides glass-ui's 3.13.0 cut. [P2, not a value.js blocker]
5. **(fourier-owned, track as cohort ask):** fourier-web `@mkbabb/value.js: ^0.10.0 → ^0.11.0`,
   `@mkbabb/glass-ui: ^3.1.0 → ^3.13.0`, `@mkbabb/keyframes.js: ^2.2.0 → ^4.x`. [P2, not a value.js
   blocker]

---

## 8. Cross-checks against N.md (consistency)

N.md §3 (lines 116-130) and the chronic table (lines 94-95) already encode this lane's findings
**correctly** — including the explicit `(E6)` citation at line 129 for the phantom devDep, the
glass-ui-devDep-stale and fourier-pin asks at lines 120-121, and the 3.13.0-gated final pin at lines
125-127/153. **No contradiction found.** This lane upgrades those prose claims to registry-grounded
evidence and adds the precise import-site / export-subpath counts. The one nuance worth surfacing for
ratification: glass-ui 3.11/3.12 staleness is a **project-internal judgment**, not an `npm deprecate`
flag (only 3.10.0 is formally deprecated) — so a naive `^3.x` resolve WOULD pull 3.12.0; the 3.13.0
pin discipline must be enforced by the explicit version range, not by relying on npm to hide stale
cuts.

---

## Findings summary (tags)

- **P1** value.js `package.json:69` phantom `@mkbabb/keyframes.js` devDep — value.js imports keyframes 0×; drop it.
- **P1** Local glass-ui checkout stale at 3.10.1 (registry 3.12.0); `file:../glass-ui` resolves pre-3.11 content — rebuild clean dist before N.W5 consume; pin `^3.13.0` (not 3.12.0) at N.W9.
- **P1** Spine is walked: value.js 0.11.2 published, all glass-ui producers (blob/aurora/watercolor/dock) on registry, keyframes 4.1.0 deps `^0.11.2` — **no producer blocks v1.0.0.**
- **P2** glass-ui registry 3.12.0 peer value.js `^0.10.0 || ^0.11.0` (admits 0.11.2 ✓) but devDep `^0.10.0` is stale — glass-ui never CI-tested against 0.11.x (glass-ui-owned).
- **P2** fourier-web pins `@mkbabb/value.js@^0.10.0` (excludes 0.11.x) — fourier consumes value.js (5 sites) but can't install current; fourier-owned bump ask.
- **P2** glass-ui 3.11/3.12 staleness is project-internal judgment, NOT `npm deprecate` (only 3.10.0 is flagged) — enforce the 3.13.0 pin by explicit range, not npm hiding.
- **Info** Formerly cohort-gated M waves (W5 aurora / W7 blob+watercolor) are now UNILATERAL consumer wiring — glass-ui shipped the producers at 3.3.0 (2026-06-06).
- **Info** keyframes.js NOT published (E404); 24 value.js import sites; the `^0.11.2` pin forced the 0.11.2 cut.
