# N charter critique — LENS 3 (Mandate Fidelity + Gestalt)

Critic: adversarial K3. Repo: value.js @ `tranche-f-handoff` HEAD `0cb5dd2`, 2026-06-11.
Method: every finding carries a primary-evidence correction (tree grep / file:line / build artifact / live curl re-read of the lane evidence). READ-ONLY; wrote only this file.

**Verdict: sound-with-fixes.** The charter's seven load-bearing theses (boot-break, deploy-stale, WithId-26, prettier-in-dist, PRM mix-canvas hole, perf-orphan, satellite-ask-honesty) are all primary-evidence-grounded and survived direct attack. The defects are real but bounded: one mischaracterized gate-precondition that makes a W1 hard-gate un-meetable as written, one genuinely-unfolded P1 contract divergence, one evidence-provenance collapse, and a cluster of count imprecisions. None of these unsound the supersede-M decision or the wave DAG; they are pre-dispatch fixes.

---

## P1 — material defects

### P1-A. The W1 "typecheck 0" hard-gate is un-meetable as written; the charter mischaracterizes the failure as "RED ×3" when it is RED ×91, and the dominant 90 are a glass-ui-dts cohort cost the charter books as a *non-blocking* ask.

- **Doc location:** N.md:50 ("`npm run typecheck` is RED"), N.md:58 ("blocking typecheck"), N.md:140 (W1 gate "typecheck 0"), PROGRESS.md:13/:47 ("typecheck ✗ (3)" / "the brief's 3 demo errors"), inv-N-1 (N.md:176).
- **Primary evidence:** `npm run typecheck` at HEAD = **91 errors**, not 3. Breakdown: `74× TS7016` ("Could not find a declaration file for module '@mkbabb/glass-ui...'" — glass-ui dist currently ships **zero** `.d.ts` files: `ls /Users/mkbabb/Programming/glass-ui/dist/*.d.ts` → "no matches"), `16× TS7006` (implicit-any params in `HeroControls.vue`/`App.vue:214` — unrelated to the boot-break), and exactly **`1× TS2307`** (the `glass-carousel` module — the *only* one of the charter's "3" that is a TS error). The `BouncyTabs` imports resolve at the module level and produce NO typecheck error (V1 correctly classed them as runtime-undefined latents), so even the "3" is really "1 TS error + 2 latent runtime breaks."
- **Why it matters (mandate: no-workaround, KISS, gestalt):** the synthesis itself records the cause — glass-ui's live `build:watch` flaps its dist and "broke an audit fleet" (synthesis.md:38; C-DTS in N.md:215). The 74 TS7016 errors are exactly that flap. The W1 gate "typecheck 0" therefore **cannot hold while glass-ui ships zero dts via the `file:` symlink** — and the dts-emitting `build:watch` (C-DTS) is booked in §8 as a *glass-ui-owned cohort ask*, not a W1 prerequisite. So inv-N-1's "typecheck is a blocking gate" structurally depends on a cross-repo ask the charter does not gate W1 on. Either (i) C-DTS must move from a §8 ask to a W1.A hard prerequisite, or (ii) the gate must be scoped to "value.js-authored errors = 0" (the `check-types.mjs` value.js-scoped idea the charter is *retiring* in W1.C). The charter retires the only mechanism that made the gate meetable, then asserts the un-scoped gate.
- **Fix:** restate the boot-break as "1 TS2307 (glass-carousel) + 2 runtime-latent (BouncyTabs)"; promote C-DTS to a W1 precondition OR scope inv-N-1's typecheck gate to value.js-authored diagnostics and say so.

### P1-B. A P1 contract divergence — session TTL 7d vs the one-contract-source spine's 30d, plus dead 30d-cron code — is verified live but folded NOWHERE. The mandate is "fold ALL chronically-deferred + deferred items" and thrust (c) is the value.js+fourier CRUD-convergence audit.

- **Doc location:** absent. `grep -in "session.ttl|7d|7-day|30d|SESSION_TTL"` across N.md + fold-ledger.md + prompt-coverage.md + PROGRESS.md → **zero hits**. N.W3.H covers only URN convergence + conformance gaps; the fold-ledger §2/§6 has no row for it.
- **Primary evidence:** `api/src/services/session/auth.ts:36` `const SESSION_TTL_MS = 7 * 24 * 60 * 60 * 1000;` ("7 days — the only place this constant lives"), minted at both register (`:95`) and login (`:147`). The cron reaper targets `lastSeenAt < now − 30d` (`cron.ts:20` `THIRTY_DAYS_MS`). D2 §3 (lanes/D2.md:59-61) flags this as a **P1 one-contract-source breach** (contract §6 binds 30d) AND an internal dead-code inconsistency: anything past 7d is already reaped by the `expiresAt` index, so the 30d cron stale-session arm is dead code. This is precisely a "no-contrivance / KISS / fold-all-deferrals" item under thrust (c) — and it is one line to fix plus the cron reconcile.
- **Why it matters:** the charter claims "zero un-recapped prompts" (N.md:111) and "Zero items leave N un-dispositioned" (fold-ledger.md:98), but a lane the charter cites for thrust (c) (D2) surfaced a P1 the fold-ledger silently drops. This directly contradicts the fold-all mandate.
- **Fix:** add a fold-ledger row (session TTL 7→30 + cron reconcile) → N.W3; or, if value.js intends to keep 7d, record it as a sanctioned divergence with the contract decision in N.W3.H (which is where the URN decision already lives).

### P1-C. The C8 blob-design lane was OVERWRITTEN by its V4 re-verification; there is no distinct "C8 design" artifact, yet the charter cites "C8/C8v2 design complete" as if two pieces of work exist.

- **Doc location:** N.md:105 ("C8/C8v2 design complete"), prompt-coverage.md:25 ("C8/C8v2 design complete"), synthesis.md:16 ("redeployed C8").
- **Primary evidence:** `lanes/C8.md` and `lanes/n-audit-C8v2.md` are **byte-identical** (both open `# LANE V4 — C8 blob redeploy re-verification`; diff = empty). So the "32 lane files + ... + C8v2" inventory double-counts one artifact. The original C8 *design* prose (the naive design the V4 lane spends its body *correcting* — "THREE C8 sub-claims need correction") **does not exist on disk**; V4 §0 states the prior C8 file "does not exist on this filesystem." The charter's design corpus for the headline blob mandate is therefore a single verification lane, not "design + re-verification."
- **Why it matters:** not a soundness break (V4 is excellent primary evidence and the blob disposition is correct), but it is a tranche-doc-honesty defect — the evidence inventory ("37 distinct lanes, 32 lane files + 4 verification files + C8v2", synthesis.md:25-26) counts the same file twice and implies a design lane that the tree shows was lost in the /tmp wipe.
- **Fix:** either restore/author the distinct C8 design content, or state plainly that C8's design was subsumed into V4 (one artifact) and correct the inventory count.

---

## P2 — count/scoping imprecisions (do not unsound the plan)

### P2-A. The "1270-LoC blob fork" and PaneHeader claims verify; the phantom-class census under-counts.
- `pastel-rainbow-text` is used at **3** sites (`PalettesPane.vue:4`, `PaletteDialogHeader.vue:37`, `DockViewSelect.vue:88`) and **IS defined** scoped at `PaletteDialogHeader.vue:94` — so it is a real rule inside one component and a phantom in the other two. The charter (V5, PROGRESS.md:44, fold-ledger §5) says "pastel-rainbow-text (2 sites)" and frames it as wholly phantom. Undercount + mis-frame; the disposition (promote to a shared utility — N.W5.E) is still correct. Doc: PROGRESS.md:44, fold-ledger.md:84.

### P2-B. The D4 lane the charter cites for thrust (c) under-counts withTransaction sites (16) vs the charter's 18; the charter sided with V2 (correct) but the cited lane disagrees with the charter on its own headline number.
- Primary evidence: `grep -rn "services\.withTransaction(" api/src` = 18 caller sites (V2's table is exact: cron 1 + crud 4 + admin/users 4 + admin/tags 1 + admin/palettes 1 + admin/batch 2 + versions 1 + forks 1 + auth 2 + votes 1 = 18). D4.md:8 says "16 distinct call sites" and proposes "16→~12"; N.W3.B says "18→~13". The charter resolved to 18 correctly, but N.W3.B's "~13" target inherits D4's arithmetic (D4 lists ~4 droppable: register/login/vote + one more) off a 16 base — re-derive the target off 18 (likely ~14, which is what N.W3's own gate says: "txn sites ≤14"). Minor internal inconsistency between N.md:142 ("18→~13") and the same row's gate ("≤14"). Doc: N.md:142.

### P2-C. The prettier-in-dist claim is SOUND (survives) — recording it so it is not re-litigated.
- The charter's E3/T6/N.W7.B claim (prettier = 54% of the 560.7KB tarball, CI gate blind to it) is correct. Primary evidence: `src/parsing/serialize.ts:135-137` dynamic-imports `prettier` + `prettier/plugins/postcss`; rolldown code-splits these into `dist/postcss-CWmVrHvs.js` (196KB) + `dist/standalone-DlyrSOeX.js` (112KB) = 308KB shipped; `dist/value.js` (128KB) is prettier-free; the CI gate (`.github/workflows/ci.yml:85-92`) asserts only `stat dist/value.js ≤ 148480`, structurally blind to the 308KB. No fix needed — this is a strong finding.

### P2-D. Satellite-metaballing expressivity is HONESTLY reduced to a cohort ask (not quietly dropped) — recording so it is not mistaken for a gap.
- The mandate's headline ("satellite metaballing") does NOT land as code in N. But the charter says so plainly: N.md:105 ("`uSatColor[]` = glass-ui shader ask"), §8 (the glass-ui-owned ask, spec named), §9 ("lands when glass-ui ships it — a consume-only delta here"). Primary evidence backs the infeasibility: `grep -rn uSatColor` across glass-ui/src + demo → **zero** (satellites are geometry-only uniforms `uSatPos/uSatRadius/uSatOpacity`; both forks smin-merge into one SDF coloured once — V4/C8v2 §(c)). What N DOES land (spatial `paletteStops` live-palette coupling) is the supported lever. This is mandate-honest, KISS-respecting deferral with a named trigger — not a band-aid. No fix; flagging that the expressivity *headline* is a §8 ask, so ratification should not expect a satellite-colour demo in N.W5.

### P2-E. N.W6.A (the per-pane Fable wave) is specced as a facility, NOT pre-dispatched as a code wave — concrete enough to dispatch the *sub-fleet*, but ratifiers should know it spawns its own audit before any work-order.
- Primary evidence: there is **no `docs/tranches/N/audit/per-pane/` dir**; the per-pane corpus is the C1-C7/C10 audit lanes + C9 §5 (which defines the 14-agent rubric, gates, output home, cadence). C9 §5.3 explicitly defers the shared rubric to "authored W1 of the wave." So N.W6.A dispatches a sub-fleet (gate A = boot-green on 14 routes, then ~10-14 Fable per-pane agents + 1 synthesis), and only THEN promotes work-orders (N.W6.B). The structure is concrete and re-runnable (this is the standing-facility mandate, satisfied), but it is a meta-wave: it is not "edit these files." The suffusion *work-orders* in N.W6.B (PaneHeader display-rung ramp, glass-tier rebalance, celebratory motion) ARE concrete and dispatchable independently. Verified: `PaneHeader.vue:3` is `text-heading` (one file lifts all 11 PaneHeader consumers — the "1 of 14 panes audacious type" + "one-file promotion" claims hold). Doc: N.md:145, C9.md:199-249. No fix required; note for ratifier expectation.

---

## What I attacked and could NOT break (claims that survive)

- **Boot-break (V1):** `ComponentSliders.vue:117` imports `{GlassCarousel, GlassCarouselItem}` from `@mkbabb/glass-ui/glass-carousel` — verified verbatim; no glass-ui version exports that subpath or those symbols (`./carousel` → `Carousel`/`CarouselItem`). `BouncyTabs` at `MixSourceSelector.vue:4` + `PaneSegmentedControl.vue:18` — verified; glass-ui `/tabs` exports only `SegmentedTabs`. SOUND.
- **Deploy-stale (V3):** the charter's "prod serves I-era code" is grounded by V3's live curl of `/palettes?limit=1` (wire carries `id`+`status`, lacks `published`/`atomSetHash`; `/diff` `/remix` `/publish` → 404; NCSU alias byte-identical, same rate-limit bucket). D5's shallower "200 with modern envelope" read does NOT contradict it (D5 never inspected fields). The charter correctly sided with V3. SOUND.
- **WithId 26 (V2):** byte-exact 25 strict casts + `forks.ts:206` parenthetical = 26. SOUND.
- **PRM mix-canvas hole (E1):** `useMixingAnimation.ts` has 4 `requestAnimationFrame` sites and **no** `matchMedia`/`prefers-reduced-motion` gate. SOUND (inv-N-9, N.W6.C).
- **Perf-orphan (E1):** `lerpArray` has zero non-test/non-bench callers (`src/math.ts:48` def + `src/index.ts:175` re-export only) while the demo runs `mixColors` (5 consumers). SOUND (T5, N.W7.B).
- **Desktop-P0 @source (C9):** `style.css` has only 2 comment mentions of `@source`, no real directive. SOUND (N.W2.B).
- **compose.yaml no-rs0 (V2):** confirmed no `--replSet`. SOUND (N.W4.A).
- **e2e 37 / write-only indexes / 4-read PATCH / 26 createIndex (V2/D5):** all verify. SOUND.

## Gestalt verdict
The plan is overwhelmingly a true gestalt-restoration tranche, not a band-aid pile: it deletes (1270-LoC blob fork, 308KB prettier, 26 casts, 2 resolvers, dual pagination, dead cron/indexes), consumes-not-forks (glass-ui blob/aurora/watercolor/dock), and installs the missing structural gate (boot-smoke) so the white-screen class is un-shippable. The KISS/no-contrivance mandate is honored in N.W3 (right-size txns, TTL index deletes cron code, kill dead storage). The only true mandate UNDER-service is fold-completeness (P1-B: the session-TTL P1 is dropped). The only true gestalt-risk is P1-A: the W1 typecheck gate is asserted against a number the charter mis-states by 30× and made un-meetable by retiring its own scoping mechanism while booking the dts fix as a non-gating cohort ask. Fix those two and N is sound to dispatch.
