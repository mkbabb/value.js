# C.W2 — palette-api alignment to ratified contract + `api/src/crud/` utility module landing

**Opens after**: C.W1 close (library `Palette` published with `@latest` tag); U1's revision of invariant 16 admits per-language utility modules ≤500 LOC per side.
**Agents**: 3 parallel (A `formatPalette` retire + migrator **+ utility-helper consumption**; B `cron.ts:24` invert **via `pinnedCron.cronPrune`** + indexes; C contract-conformance route alignment + e2e **+ `api/src/crud/` landing per U4**).
**Hard gate**: `npx tsx api/src/migrate-palette-schema.ts` exits 0 with backfill counts + spot-check; `formatPalette` `??` fallback fully retired (`git grep "\?\?" api/src/routes/palettes.ts` zero on the seven fields); `cron.ts:24` no longer uses `$nin` over unbounded set (delegates to `pinnedCron.cronPrune`); named e2e specs green against the new schema; **`api/src/crud/` module landed per `coordination/CRUD-LIB-TS.md` (U4 spec); `palettes.ts` USES the named helpers (`slugWithRetry`, `cursors.paginate`, `errors.problem`, `etag.requireIfMatch`, `idempotency.idempotent`, `softdelete.*`, `pinnedCron.cronPrune`); per-language LOC ≤ 500; slug-words sourced from `docs/precepts/data/slug-words.json` per U2**.
**Status**: provisional (hardened from C.md §3 row W2; H5 §4 supplies migrator shape; H5 §5.6 makes the `cron.ts` invert load-bearing; U1 admits the utility module landing alongside the entity alignment).

## Scope (inherits value.js-A invariants 1–5, value.js-B invariants B1–B5, cohort invariants 14–17)

1. Author `api/src/migrate-palette-schema.ts` per H5 §4.2 shape: idempotent per-document `$set` of `DEFAULTS = { tags: [], versionCount: 1, forkCount: 0, forkOf: null, forkOfHash: null, oklabColors: [] }`; backfill `currentHash` via `computeContentHash` from `api/src/hash.ts:13` (H5 §5.5 — `currentHash` is the only non-static default and the contract refuses null on a real palette); post-condition `countDocuments({ $or: [{tags: {$exists: false}}, …] })` must be zero or the script throws. Mirrors `api/src/migrate-slugs.ts:25-67` and `migrate-oklab.ts:1-85` idiom (H5 §4.1).
2. **Retire `formatPalette` `??` defaulting** at `api/src/routes/palettes.ts:11-27` (the seven fields H5 §2 verified — `tags`, `versionCount`, `forkCount`, `forkOf`, `forkOfHash`, `currentHash`, `oklabColors`). The function reduces to H5 §4.4 shape:

   ```ts
   function formatPalette(doc: any, votedSlugs?: Set<string>): any {
       const { _id, sessionToken, ...rest } = doc;
       return { id: _id.toString(), ...rest, isLocal: false, voted: votedSlugs ? votedSlugs.has(doc.slug) : undefined };
   }
   ```

   The retirement is the cohort-invariant-17 deletion proof and the brittleness-window closing move (cohort `C.md §8`).
3. **`cron.ts:24` `$nin` invert** (H5 §5.6, cohort invariant 16 / cohort cron policy in `CRUD-CONTRACT.md §8`): today `paletteSlug: { $nin: paletteSlugs }` where `paletteSlugs = await db.collection("palettes").distinct("slug")` is the unbounded-set anti-pattern. Replace with the per-doc walk: iterate `votes`, look up each palette by slug, delete the vote if the palette is missing (or convert to `pinned: bool` flag pattern per `CRUD-CONTRACT.md §8`). Mirrors fourier-A.W4 janitor invert. The cron walk is bounded by the votes count (which is intrinsically bounded by user activity), not by the universe of palette slugs.
4. Align `api/src/routes/palettes.ts` to `CRUD-CONTRACT.md` per-section conformance: §1 identity (no `currentHash` in user-facing URLs); §2 slug shape (`^[a-z]+(-[a-z]+){3}$`); §3 ownership (anonymous publish → 401; `userSlug` required non-null on `POST /palettes`); §4 visibility (3-state `draft|unlisted|public` — today the palette doc carries `status: "published" | "featured"` per H5 §2.1, which is **moderation tier**, not visibility; introduce a separate `visibility` field per the contract); §5 soft-delete (`deletedAt` field; cron grace window; `POST /palettes/:slug/restore`); §6 sessions (unchanged — already conforms); §7 admin moderation (idempotent flag/dismiss; `palette_audit` retention).
5. Align `api/src/routes/{sessions,admin}.ts` to the contract §6 and §7 must-have fields (verify shapes match the ratified contract; mostly conformance check — value.js's existing patterns largely match).
6. Update `api/src/types.ts` (currently 5 lines — only `AppEnv` per H5 §5.8) with a `PaletteDoc` shape matching the library `Palette` content + the contract DTO fields (`slug`, `userSlug`, `visibility`, `deletedAt`, `content: PaletteJSON`, moderation fields).
7. Update `api/src/db.ts:21-79` collection indexes: add `deletedAt` index; add `visibility` index; if `pinned: bool` retrofit chosen (per cohort `CRUD-CONTRACT.md §8`), add `pinned` index.
8. Update Playwright specs **by name** (H5 §5.9): `e2e/palette-features.spec.ts`, `e2e/palette-slug-management.spec.ts`, `e2e/propose-name.spec.ts` exercised against the migrated schema; new spec `e2e/palette-visibility-soft-delete.spec.ts` covering create-draft → publish → unlisted → delete → restore → grace-window-hard-delete.
9. Update CHANGELOG.md with the breaking-or-additive disposition (the brittleness window closes; the schema migration is the breaking change confined to one wave per cohort `C.md §8`).
10. **Land `api/src/crud/` utility module per `coordination/CRUD-LIB-TS.md` (U4 spec)** alongside the palette-api alignment (per U1's revised invariant 16). The module is bounded — ≤500 LOC total across `index.ts`, `slugs.ts` (`slugWithRetry`), `cursors.ts` (`paginate`), `errors.ts` (`problem` per RFC 9457), `etag.ts` (`requireIfMatch`), `idempotency.ts` (`idempotent` middleware), `softdelete.ts` (`softDelete`, `restore`, `hardDeletePastGrace`), `pinnedCron.ts` (`cronPrune` — the bounded-query helper that `cron.ts:24` delegates to). `routes/palettes.ts` USES these helpers; `cron.ts` delegates to `pinnedCron.cronPrune`; `migrate-palette-schema.ts` consumes `softdelete` / `cursors` helpers. Utility's own vitest specs at `test/crud/*.test.ts` close on helper contracts independently of entity behaviour. Slug-words data sourced from `docs/precepts/data/slug-words.json` per `coordination/SLUG-WORDS.md` (U2 spec) — `slugs.ts` imports the JSON via the precepts location; replaces `api/src/slugWords.ts` as the slug-words source.
11. Author vitest specs for the utility itself: `test/crud/slugs.test.ts` (DuplicateKeyError retry, word-list membership), `test/crud/cursors.test.ts` (base64url roundtrip, stable ordering), `test/crud/errors.test.ts` (RFC 9457 problem+json shape), `test/crud/etag.test.ts` (`If-Match` 428/412 semantics), `test/crud/idempotency.test.ts` (replay returns cached response), `test/crud/softdelete.test.ts` (state machine), `test/crud/pinnedCron.test.ts` (bounded query — no `$nin` over unbounded set).

## File Bounds

| File | Access |
|---|---|
| `api/src/migrate-palette-schema.ts` | create |
| `api/src/routes/palettes.ts` | modify (retire `formatPalette` `??` defaulting; add visibility/soft-delete endpoints) |
| `api/src/routes/sessions.ts` | modify (conformance check; minor) |
| `api/src/routes/admin.ts` | modify (conformance check; idempotency assertions) |
| `api/src/cron.ts` | modify (line 24 `$nin` invert) |
| `api/src/types.ts` | modify (add `PaletteDoc` type) |
| `api/src/db.ts` | modify (add `deletedAt`, `visibility`, `pinned` indexes) |
| `e2e/palette-features.spec.ts` | modify (verify against new schema) |
| `e2e/palette-slug-management.spec.ts` | modify |
| `e2e/propose-name.spec.ts` | modify |
| `e2e/palette-visibility-soft-delete.spec.ts` | create |
| `api/src/crud/index.ts` | create (per U4 spec) |
| `api/src/crud/slugs.ts` | create (per U4 spec) |
| `api/src/crud/cursors.ts` | create (per U4 spec) |
| `api/src/crud/errors.ts` | create (per U4 spec) |
| `api/src/crud/etag.ts` | create (per U4 spec) |
| `api/src/crud/idempotency.ts` | create (per U4 spec) |
| `api/src/crud/softdelete.ts` | create (per U4 spec) |
| `api/src/crud/pinnedCron.ts` | create (per U4 spec) |
| `test/crud/slugs.test.ts` | create |
| `test/crud/cursors.test.ts` | create |
| `test/crud/errors.test.ts` | create |
| `test/crud/etag.test.ts` | create |
| `test/crud/idempotency.test.ts` | create |
| `test/crud/softdelete.test.ts` | create |
| `test/crud/pinnedCron.test.ts` | create |
| `api/src/slugWords.ts` | modify-carve (delegate to `crud/slugs.ts` which consumes the precepts data per U2) |
| `CHANGELOG.md` | modify |

Do NOT touch: `src/**` (C.W1 owns library), `demo/**` (C.W3 owns demo wiring), `coordination/CRUD-CONTRACT.md` (fourier-B owns). The utility module is **bounded** at ≤500 LOC total (`wc -l api/src/crud/*.ts`); exceeding the bound trips U1's "framework-in-disguise" guard and the wave fails its close.

## Agent Units

### C.W2.a Migrator + `formatPalette` retire (consumes utility helpers)

- Mechanism: author `api/src/migrate-palette-schema.ts` per H5 §4.2 (idempotent iteration, per-field `$set`, `currentHash` backfill via `computeContentHash`, post-condition straggler check that throws on incomplete); the migrator consumes `softdelete` + `cursors` helpers from the U4 utility (landed by Agent C in this wave); run dry-run against a snapshot; capture report; commit the script; **then** edit `api/src/routes/palettes.ts:11-27` to the H5 §4.4 bare shape (the seven `??` clauses deleted), additionally refactoring the route handlers to consume `slugWithRetry`, `cursors.paginate`, `errors.problem`, `etag.requireIfMatch`, `idempotency.idempotent`, `softdelete.*` from `api/src/crud/`. The deletion is the gate.
- Files: `api/src/migrate-palette-schema.ts` (create), `api/src/routes/palettes.ts` (modify).
- Sub-gate: `npx tsx api/src/migrate-palette-schema.ts` exits 0 with `total > 0`, `updated >= 0`, `spotCheck` ≥ 1 row; `git grep "\?\?" api/src/routes/palettes.ts` returns zero matches against the seven retired fields (`tags|versionCount|forkCount|forkOf|forkOfHash|currentHash|oklabColors`); `grep -E "from .*api/src/crud" api/src/routes/palettes.ts` returns ≥ 6 imports (the named utility helpers are consumed, not just available).

### C.W2.b `cron.ts:24` `$nin` invert via `pinnedCron.cronPrune` + indexes

- Mechanism: rewrite `api/src/cron.ts:18-25` (the orphaned-votes cleanup): replace `await db.collection("palettes").distinct("slug")` + `{ paletteSlug: { $nin: paletteSlugs } }` with a delegation to `pinnedCron.cronPrune` from `api/src/crud/` (landed by Agent C). The helper encapsulates the bounded-query pattern; `cron.ts` becomes a thin caller. Bounded by votes count, which is intrinsically bounded per cohort `CRUD-CONTRACT.md §8`. Add `deletedAt`, `visibility`, `pinned` indexes to `db.ts:21-79`.
- Files: `api/src/cron.ts` (modify), `api/src/db.ts` (modify).
- Sub-gate: `git grep "\$nin" api/src/cron.ts` returns zero matches against unbounded-set sources; `grep -E "cronPrune|from .*api/src/crud" api/src/cron.ts` returns ≥ 1 (delegation in place); `grep -E "deletedAt|visibility" api/src/db.ts` shows index creation for both; `npx tsx api/src/cron.ts` invoked manually completes with bounded `[cron]` log line.

### C.W2.c Contract-conformance route alignment + e2e + `api/src/crud/` landing

- Mechanism: extend `api/src/routes/palettes.ts` with visibility + soft-delete + restore endpoints per `CRUD-CONTRACT.md §4`/§5; update `api/src/types.ts` with the `PaletteDoc` shape; verify `sessions.ts` and `admin.ts` shapes match the contract; update three named e2e specs + author the new `palette-visibility-soft-delete.spec.ts`. **Land `api/src/crud/` utility module per U4**: 8 module files + 7 named vitest specs; slug-words JSON imported from `docs/precepts/data/slug-words.json` per U2; `api/src/slugWords.ts` reduces to a thin delegate. Utility module is bounded at ≤500 LOC total.
- Files: `api/src/routes/{palettes,sessions,admin}.ts` (modify), `api/src/types.ts` (modify), `api/src/slugWords.ts` (modify-carve), `e2e/palette-features.spec.ts` (modify), `e2e/palette-slug-management.spec.ts` (modify), `e2e/propose-name.spec.ts` (modify), `e2e/palette-visibility-soft-delete.spec.ts` (create), `api/src/crud/**` (create — 8 files per U4), `test/crud/**` (create — 7 vitest specs).
- Sub-gate: `npx playwright test e2e/palette-features.spec.ts e2e/palette-slug-management.spec.ts e2e/propose-name.spec.ts e2e/palette-visibility-soft-delete.spec.ts --project=chromium` green; conformance: `curl -s -o /dev/null -w "%{http_code}" -X POST localhost:5173/api/palettes` without session returns `401` (ownership conformance per `CRUD-CONTRACT.md §3`); `npx vitest run test/crud/` exits 0 (7 utility specs green); `wc -l api/src/crud/*.ts | tail -1` reports ≤ 500 lines total (U1 size bound).

## Hard Gate

1. `npx tsx api/src/migrate-palette-schema.ts` exits 0; output captures `total`, `updated`, `hashesComputed`, `perField` map, `spotCheck` (≥ 1 row) per H5 §4.2 `MigrationReport` shape.
2. **`formatPalette` retired**: `git grep "tags ?? \[\]\|versionCount ?? 1\|forkCount ?? 0\|forkOf ?? null\|forkOfHash ?? null\|currentHash ?? null\|oklabColors ?? \[\]" api/src/routes/palettes.ts` returns zero.
3. **`cron.ts:24` `$nin` retired**: `git grep "\\\$nin.*distinct\|\\\$nin.*paletteSlugs" api/src/cron.ts` returns zero.
4. `npx playwright test e2e/palette-features.spec.ts e2e/palette-slug-management.spec.ts e2e/propose-name.spec.ts e2e/palette-visibility-soft-delete.spec.ts --project=chromium` all green; report shows 0 console errors, 0 non-2xx assets per value.js-A invariant 3 / B invariant B4.
5. **Ownership conformance** (`CRUD-CONTRACT.md §3`): `curl -X POST -H "Content-Type: application/json" -d '{"name":"x"}' localhost:5173/api/palettes` (no session token) returns `401`.
6. **Soft-delete conformance** (`CRUD-CONTRACT.md §5`): create → delete → restore round-trip against a real palette resolves; deleted entity not in `GET /api/palettes`; restored entity returns.
7. **Index conformance**: `mongosh --eval "db.palettes.getIndexes()"` shows `deletedAt`, `visibility` indexes (and `pinned` if retrofit chosen).
8. **Library-no-persistence regression** (cohort invariant 15 — sanity sweep): `grep -rE "mongodb|express|hono|fetch\(" src/` returns zero (W1's gate still holds; W2 does not regress it).
9. `CHANGELOG.md` records the schema migration entry; `docs/tranches/C/PROGRESS.md` row W2 cites the migration commit + run log.
10. **Brittleness window closed** (cohort `C.md §8`): the formatPalette deletion proof + the migration completion is the close move; no `??` defaulting survives the wave.
11. **Utility module landed (per U1 invariant 16 / U4 spec)**: `ls api/src/crud/` lists all 8 files (`index.ts`, `slugs.ts`, `cursors.ts`, `errors.ts`, `etag.ts`, `idempotency.ts`, `softdelete.ts`, `pinnedCron.ts`); `npx vitest run test/crud/` reports all 7 specs green; `wc -l api/src/crud/*.ts | tail -1` total ≤ 500 (U1 size bound — framework-in-disguise guard).
12. **Helper consumption proof** (substrate-with-consumer per cohort invariant 4): `grep -E "from .*api/src/crud" api/src/routes/palettes.ts` returns ≥ 6 imports covering the named helpers; `grep -E "cronPrune" api/src/cron.ts` returns ≥ 1 (cron delegates to the utility); `grep -E "softdelete|cursors" api/src/migrate-palette-schema.ts` returns ≥ 1 (migrator consumes utility helpers).
13. **Slug-words data consumption** (per U2): `grep -E "slug-words.json" api/src/crud/slugs.ts` returns ≥ 1; the precepts JSON resolves at import time; `api/src/slugWords.ts` reduces to a thin delegate (`wc -l api/src/slugWords.ts` ≤ 20).

## Verification Artefacts

- `api/src/migrate-palette-schema.ts` (created).
- Migration run log captured at `docs/tranches/C/audit/W2-migrate-palette-schema.log` (total + per-field + spotCheck).
- `git diff --stat W2-start..W2-close -- api/src/routes/palettes.ts` showing the `formatPalette` LOC drop.
- `git diff W2-start..W2-close -- api/src/cron.ts` showing the `$nin` retirement.
- Playwright report at `e2e/playwright-report/` (4 specs green).
- `mongosh` index listing captured pre-migration + post-migration.

## Dependencies

- **Depends on**: C.W1 close (library `Palette` published — `PaletteDoc` type composes over it).
- **Blocks**: C.W3 (demo native consumption depends on the aligned api shape); C.W4 close.

## Archaeology

The `formatPalette` `??` defaulting at `api/src/routes/palettes.ts:11-27` carries the comment "Ensure new fields always have defaults for pre-migration documents" — the exact phrasing cohort invariant 17 forbids ("migration is verified, not hoped"). It is the cohort-symmetric finding to fourier's `user_slug: None` orphan path (H4 §2.R2 names them as parallel findings). H5 §5.6 elevated the `cron.ts:24` invert from coordination-doc mention to a load-bearing W2 close-on — the invert mirrors fourier-A.W4's janitor invert, which is the precedent that retires the unbounded `$nin` pattern in both repos. Cohort `CRUD-CONTRACT.md §8` is the binding spec.
