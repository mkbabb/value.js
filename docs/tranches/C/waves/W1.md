# C.W1 — Library `Palette` + `colorScale` + `sampleToSVGPath`

**Opens after**: C.W0 close (value.js-B closed; fourier-B.W1 ratified `CRUD-CONTRACT.md`; sign-off committed per `C.md:7` corrected wording per H5 §1.4).
**Agents**: 3 parallel (A library type + vitest; B demo `Palette → PersistedPalette` rename + first consumer swap; C `sampleToSVGPath` at `src/math.ts` + version publish).
**Hard gate**: `npm view @mkbabb/value.js@<new-version> version` returns the bumped version; `@latest` tag updated; vitest specs named in §scope green; demo `Palette` interface fully renamed (`git grep "interface Palette\b\|: Palette\b" demo/`); one demo consumer swapped to library type proves the substrate.
**Status**: provisional (hardened from C.md §3 row W1; H5 §3 supplies the concrete TypeScript signature; H5 §5.1/§5.2/§5.3 supply the consumer-churn budget).

## Scope (inherits value.js-A invariants 1–5, value.js-B invariants B1–B5, cohort invariants 14–17)

1. Author `src/palette/` directory with `index.ts`, `types.ts`, `sample.ts`, `serde.ts` per H5 §3.2: the `Palette` class with `stops`, `ramps`, `fromCSS`, `sample(t, hueMethod?)`, `resample(n)`, `toSpace(space)`, `toCSS(outputSpace?)`, `toSRGBSafe()`, `toJSON()`, `fromJSON()`, `equals()`. **Working space is OKLCh** (H5 §3.1 justification: library mix default is OKLab at `src/units/color/mix.ts:31`; OKLCh adds hue axis for `HueInterpolationMethod` ramps; `gamutMapOKLab` already ships at `src/index.ts:206`; LCh would re-introduce CIE-1976 non-uniformity).
2. Author `colorScale(stops: ReadonlyArray<string>, t: number, opts?)` at `src/palette/index.ts` — the free-function form of `Palette.sample` for call sites already holding CSS strings (H5 §3.2 signature).
3. Author `sampleToSVGPath(fn, n?, opts?)` at **`src/math.ts`** (H5 §3.3 — H5 §5.7 corrects C.md §5's pointer at `src/easing.ts`; the existing `cubicBezierToSVG` lives at `src/math.ts:68-83` and `sampleToSVGPath` generalises it). Existing `cubicBezierToSVG` re-expressed as `sampleToSVGPath((t) => cubicBezier(t, x1, y1, x2, y2)[1], 1000)` to prove the generalisation (regression coverage).
4. Re-export `Palette`, `colorScale`, `sampleToSVGPath`, `PaletteStop`, `PaletteRamp`, `PaletteJSON` from `src/index.ts` barrel (currently 355 lines per H5 §2).
5. Author named vitest specs at `test/palette/{construct,sample,resample,serde,gamut}.test.ts` and `test/math/sampleToSVGPath.test.ts`: construction normalisation (sort-on-construct, OKLCh conversion), sample at endpoints + midpoints (compare to `mixColors` direct call), `resample` preserves `position` linearity, `toJSON`/`fromJSON` roundtrip lossless for both CSS and raw-OKLCh stops, `toSRGBSafe` clamps every out-of-gamut stop (regression against current `gamutMapOKLab`), `sampleToSVGPath` produces byte-identical output to `cubicBezierToSVG` when invoked with the same parameters at n=1000.
6. **Demo `Palette → PersistedPalette` rename** (H5 §5.1, the largest consumer-churn item C.md omits): `demo/@/lib/palette/types.ts:7-28` `Palette` interface renamed to `PersistedPalette`; the 14+ consumers identified by H5 §2.1 (sample: `demo/@/composables/palette/{usePaletteStore,useBrowsePalettes,usePaletteActions,useSlugMigration,usePaletteExport,usePaletteManager}.ts`, `demo/@/composables/auth/useAdminUsers.ts`, `demo/@/components/custom/mix/MixSourceSelector.vue`) updated to import `PersistedPalette`. `mixPalettes` at `demo/@/lib/palette/mix.ts:70-102` (H5 §5.3) keeps its signature but operates on `PersistedPalette[]`; positioned as a `PersistedPalette` operation layered over library `Palette` per H5 §3.4.
7. Swap **one demo site** (`demo/@/components/custom/mix/MixSourceSelector.vue` — the smallest consumer) to import the library `Palette` as a first-class type, proving substrate-with-consumer (cohort invariant 4 / value.js-A invariant 4). The remaining demo sites land in C.W3.
8. Bump version (semver minor per H5 §4 — additive library surface; no breaking changes); run `npm publish` against the public registry; verify `npm dist-tag ls @mkbabb/value.js` shows `latest → <new-version>` (H5 §6.4 R-T-2 explicit `@latest` tag pin).
9. Update CHANGELOG.md with the additive surface entry citing the C.W1 close commit.

## File Bounds

| File | Access |
|---|---|
| `src/palette/index.ts` | create |
| `src/palette/types.ts` | create |
| `src/palette/sample.ts` | create |
| `src/palette/serde.ts` | create |
| `src/math.ts` | modify (add `sampleToSVGPath`; re-express `cubicBezierToSVG` over it) |
| `src/index.ts` | modify (barrel re-exports) |
| `test/palette/construct.test.ts` | create |
| `test/palette/sample.test.ts` | create |
| `test/palette/resample.test.ts` | create |
| `test/palette/serde.test.ts` | create |
| `test/palette/gamut.test.ts` | create |
| `test/math/sampleToSVGPath.test.ts` | create |
| `demo/@/lib/palette/types.ts` | modify (rename interface) |
| `demo/@/lib/palette/{mix,export,api,utils}.ts` | modify (`Palette` → `PersistedPalette` in signatures) |
| `demo/@/composables/palette/*.ts` | modify (rename consumers — H5 §2.1 enumerates) |
| `demo/@/composables/auth/useAdminUsers.ts` | modify |
| `demo/@/components/custom/mix/MixSourceSelector.vue` | modify (first library-`Palette` consumer) |
| `package.json` | modify (version bump) |
| `CHANGELOG.md` | modify |

Do NOT touch: `api/src/**` (C.W2 owns), `demo/color-picker/**` + `demo/hero-lab/**` (C.W3 owns the broader demo wiring), `docs/tranches/B/**` (predecessor closed).

## Agent Units

### C.W1.a Library `Palette` + `colorScale` + vitest

- Mechanism: implement `src/palette/{index,types,sample,serde}.ts` per H5 §3.2 signature; eager OKLCh conversion in constructor (uses existing `oklab2oklch` at `src/units/color/utils.ts:142-145` and `mixColors` / `interpolateHue` for sampling); author the 5 `test/palette/*.test.ts` files; barrel re-export from `src/index.ts`.
- Files: `src/palette/**` (create), `src/index.ts` (modify), `test/palette/**` (create).
- Sub-gate: `npx vitest run test/palette/` exits 0; `npx vitest run test/palette/sample.test.ts -t "sample at t=0.5"` returns the expected midpoint OKLCh stop within 1e-6 tolerance; `grep -rE "mongodb|express|hono|fetch\(" src/palette/` returns zero (invariant 15 enforced).

### C.W1.b Demo `Palette → PersistedPalette` rename + first library consumer

- Mechanism: rename `demo/@/lib/palette/types.ts:7-28` `Palette` → `PersistedPalette`; update the 14+ consumers (H5 §2.1) with sed-driven mechanical rename + manual review; swap `demo/@/components/custom/mix/MixSourceSelector.vue` to import the library `Palette` (the proof-of-consumer site).
- Files: `demo/@/lib/palette/{types,mix,export,api,utils}.ts` (modify), `demo/@/composables/palette/*.ts` (modify, ≥6 files per H5 §2.1), `demo/@/composables/auth/useAdminUsers.ts` (modify), `demo/@/components/custom/mix/MixSourceSelector.vue` (modify).
- Sub-gate: `git grep -wE "interface Palette\b|: Palette\b" demo/ -- ':!**/*.md'` returns zero (rename complete; library import unambiguous); `npx vue-tsc -b --force` green from `demo/`.

### C.W1.c `sampleToSVGPath` + version publish + CHANGELOG

- Mechanism: add `sampleToSVGPath(fn, n?, opts?)` to `src/math.ts` per H5 §3.3; re-express the existing `cubicBezierToSVG` (`src/math.ts:68-83`) over it; author `test/math/sampleToSVGPath.test.ts` with the byte-identical regression; bump `package.json` (semver minor); `npm publish`; `npm dist-tag add @mkbabb/value.js@<new-version> latest` to pin `@latest`; CHANGELOG entry.
- Files: `src/math.ts` (modify), `test/math/sampleToSVGPath.test.ts` (create), `package.json` (modify), `CHANGELOG.md` (modify).
- Sub-gate: `npx vitest run test/math/sampleToSVGPath.test.ts` green; `npm view @mkbabb/value.js@<new-version> version` returns the new version; `npm dist-tag ls @mkbabb/value.js` shows `latest: <new-version>`.

## Hard Gate

1. `npx vitest run test/palette/ test/math/sampleToSVGPath.test.ts` green; all six new test files present (run command: `npx vitest run test/palette/construct.test.ts test/palette/sample.test.ts test/palette/resample.test.ts test/palette/serde.test.ts test/palette/gamut.test.ts test/math/sampleToSVGPath.test.ts`).
2. `npx vue-tsc -b --force` green from `demo/`.
3. `npm run build` from `src/` produces a clean `dist/` with `Palette`, `colorScale`, `sampleToSVGPath` in the `.d.ts` surface (`grep -E "export (class Palette|function colorScale|function sampleToSVGPath)" dist/index.d.ts` returns ≥ 3).
4. **Library-no-persistence gate** (value.js-A invariant + cohort invariant 15): `grep -rE "mongodb|express|hono|fetch\(" src/palette/ src/math.ts` returns zero; `grep -rE "import .* from .*api/" src/` returns zero.
5. **Demo rename complete**: `git grep -wE "interface Palette\b" demo/@/lib/palette/` returns zero (matched only the `PersistedPalette` rename); `git grep -wE "PersistedPalette" demo/` returns ≥ 14 files.
6. **First consumer swap**: `git grep "from \"@mkbabb/value\.js\"" demo/@/components/custom/mix/MixSourceSelector.vue` references the library `Palette` symbol.
7. **Version published with `@latest` tag**: `npm view @mkbabb/value.js@<new-version> version` returns the new version; `npm dist-tag ls @mkbabb/value.js | grep "^latest:"` resolves to the new version.
8. **Playwright probe** (value.js-A invariant 3 / B invariant B4, wave-qualified — library + demo touch warrants full probe): `npx playwright test e2e/smoke/ --project=chromium` green at 3 viewports × light+dark × 0 console errors × 0 non-2xx assets.
9. `CHANGELOG.md` records the additive surface entry with the close-commit hash; `docs/tranches/C/PROGRESS.md` row W1 cites the publish hash + the `@latest` tag confirmation.

## Verification Artefacts

- `test/palette/{construct,sample,resample,serde,gamut}.test.ts` (created, green).
- `test/math/sampleToSVGPath.test.ts` (created, green).
- `npm publish` output captured to `docs/tranches/C/audit/W1-publish.log`.
- `npm dist-tag ls @mkbabb/value.js` output captured (the `@latest` confirmation).
- Playwright report at `e2e/playwright-report/` (3 viewports light+dark green).
- Demo rename diff stat captured (`git diff --stat <W1-start>..<W1-close> demo/`).

## Dependencies

- **Depends on**: C.W0 close (`CRUD-CONTRACT.md` ratified at fourier-B.W1; value.js-B closed); H5 §3 design substrate.
- **Blocks**: C.W2 (api alignment consumes the library `Palette` shape in `PaletteDoc`); C.W3 (demo native consumption builds on the rename + first-consumer swap); **fourier-B.W4** (the cross-repo consumer — the `colors.ts` gut requires the published npm version per `~/Programming/fourier-analysis/docs/tranches/B/B.md:97`).

## Archaeology

H5 §2.1/§2.2/§2.3 are the critical inventory corrections C.md §5 missed: the demo *already has* a `Palette` interface consumed by ≥14 files, a `mixPalettes` palette-mixing primitive at `demo/@/lib/palette/mix.ts:70`, and a `cssToRawColor`/`rawColorToCSS` shim at `demo/@/lib/color-utils.ts:21` — none named in the original C.md. The rename to `PersistedPalette` (H5 §5.1) is the honest substrate-with-consumer move. H5 §3.1 grounds the OKLCh choice in five extant-code citations; LCh is rejected. H5 §5.7's file-pointer correction (`sampleToSVGPath` at `src/math.ts`, not `src/easing.ts`) is honoured in scope §3.
