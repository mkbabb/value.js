# C.W3 — Demo native `Palette` consumption

**Opens after**: C.W1 close (library type published). May run concurrently with C.W2 per `C.md:74` provided W2 has progressed past the `PaletteDoc` shape (C.W3 consumes both library `Palette` and `PersistedPalette` DTOs).
**Agents**: 2 parallel (A `color-picker` + palette-browser; B `hero-lab`).
**Hard gate**: `demo/color-picker` and `demo/hero-lab` consume the library `Palette` as a first-class type; `PersistedPalette` (the C.W1 demo rename) layers over it explicitly with documented composition; Playwright re-probe 3 viewports × light+dark green; the W1 first-consumer swap (`MixSourceSelector.vue`) is no longer the only library-`Palette` site.
**Status**: provisional (hardened from C.md §3 row W3; H5 §5.2 supplies the `HeroPalettePreset` position requirement; H5 §2.1/§2.2 supply the consumer surface).

## Scope (inherits value.js-A invariants 1–5, value.js-B invariants B1–B5, cohort invariants 14–17)

1. Swap `demo/color-picker/App.vue` (298 lines per H5 §2 row 8) to consume the library `Palette` as the working in-memory representation; `PersistedPalette` (C.W1 rename) is the storage/wire shape that wraps `content: PaletteJSON` over the library type. The composition pattern: `PersistedPalette { slug, userSlug, voteCount, ..., content: PaletteJSON } → Palette.fromJSON(persisted.content)` on load; `Palette.toJSON()` on save.
2. Swap `demo/@/components/custom/palette-browser/` consumers (38 entries / 43 files per H5 §2 row 7) — the full set, not just `MixSourceSelector.vue`. The browser-tab consumers (`PaletteBrowseTab.vue`, `PaletteCard.vue`, `PaletteCardGrid.vue`, `PaletteCardMenu.vue`, `CurrentPaletteEditor.vue`, `MiniColorPicker.vue`, `PaletteColorStrip.vue`, `MigratePalettesDialog.vue`, etc.) operate on library `Palette` for in-memory rendering; `PersistedPalette` only at the api boundary (load + save).
3. Swap `demo/@/composables/palette/*.ts` (the 6+ composables H5 §2.1 enumerated — `usePaletteStore`, `useBrowsePalettes`, `usePaletteActions`, `useSlugMigration`, `usePaletteExport`, `usePaletteManager`, `usePaletteManagerWiring`) to consume library `Palette` for operations (`sample`, `resample`, `toSRGBSafe`, `toCSS`) and `PersistedPalette` only for identity/persistence.
4. **`hero-lab` position** (H5 §5.2, the C.md omission): `demo/hero-lab/lib/types.ts:3-12` `HeroPalettePreset { id, label, description, surface, surfaceAlt, shadow, tileStops, atmosphereStops }` is **not** a `Palette` in the cohort sense — it carries demo-specific UI fields (`surface`, `surfaceAlt`, `shadow`). C.W3 chooses composition: `HeroPalettePreset` keeps its UI fields but adds a `palette: Palette` field; the `tileStops`/`atmosphereStops` derive from `palette.resample(n)` calls rather than carrying parallel stop arrays. `demo/hero-lab/lib/palettes.ts` `HERO_PALETTES` array updated; `demo/hero-lab/App.vue` + `demo/hero-lab/components/**` consumers updated.
5. Retire `mixPalettes` from demo to library if the C.W1/W2 disposition admitted it; otherwise document its position as a `PersistedPalette`-level operation that consumes library `Palette` internally (H5 §2.2 disposition). Default position per H5 §3.4: `mixPalettes` stays demo-side as a `PersistedPalette` operation, internally calls `colorScale` + library composition.
6. Retire `demo/@/lib/color-utils.ts:21,51` `cssToRawColor`/`rawColorToCSS` shim if the library now exposes a `colorFromCSS`/`colorToCSS` pair (H5 §5.4 — explicitly deferred from C.W1 per H5 §7 secondary; the position here is documentation only if not lifted, named removal if lifted).
7. Validate the palette save → reload → load → fork → vote → delete → restore flow renders identically to the value.js-B close baseline (no visual regression). Capture before/after screenshots if any pixel-level drift surfaces.
8. Update `docs/tranches/C/PROGRESS.md` row W3 with the close commit + Playwright report path + the per-consumer migration commit hashes.
9. **No utility-module consumption from the demo** (per U1's bounded invariant 16): demo flows continue to consume the palette-api via its HTTP boundary (`fetch`/`useBrowsePalettes`/`usePaletteStore` etc.). The `api/src/crud/` utility landed in C.W2 is **server-side only**; the demo never imports from `api/src/crud/`. This boundary is enforced by `git grep -E "from .*api/src/crud" demo/` returning zero at W3 close — the utility module is bounded to the api repo surface and does not leak into client code.

## File Bounds

| File | Access |
|---|---|
| `demo/color-picker/App.vue` | modify |
| `demo/@/components/custom/palette-browser/*.vue` | modify (≥ 20 SFCs — the full library-`Palette` swap; not exhaustively enumerated here but covered by the file-bounds glob) |
| `demo/@/components/custom/palette-browser/composables/*.ts` | modify |
| `demo/@/composables/palette/*.ts` | modify (6+ composables) |
| `demo/hero-lab/App.vue` | modify |
| `demo/hero-lab/lib/types.ts` | modify (`HeroPalettePreset` composes `Palette`) |
| `demo/hero-lab/lib/palettes.ts` | modify (`HERO_PALETTES` array) |
| `demo/hero-lab/components/*.vue` | modify |
| `demo/@/lib/palette/mix.ts` | modify (per disposition — internal `colorScale` consumption) |
| `e2e/palette-features.spec.ts` | re-run (no edit; W2 may have updated) |

Do NOT touch: `src/**` (C.W1 owns), `api/src/**` (C.W2 owns), `demo/@/lib/palette/types.ts` (rename done in C.W1), `demo/@/lib/color-utils.ts` (per H5 §5.4 only if lift decided in C.W1 — default: no touch).

## Agent Units

### C.W3.a `color-picker` + palette-browser

- Mechanism: rewrite `demo/color-picker/App.vue` to use library `Palette` as working state; convert each `palette-browser/` SFC to consume `Palette` for in-memory ops (`.sample`, `.toCSS`, `.toSRGBSafe`) and `PersistedPalette` only at api boundary; rewrite the 6+ composables to consume library `Palette` for operations.
- Files: `demo/color-picker/App.vue` (modify), `demo/@/components/custom/palette-browser/**` (modify, 38+ entries), `demo/@/composables/palette/*.ts` (modify, 6+).
- Sub-gate: `npx vue-tsc -b --force` green from `demo/`; `git grep "Palette\.fromJSON\|Palette\.fromCSS" demo/@/components/custom/palette-browser/ demo/@/composables/palette/` returns ≥ 5 sites (the library type is actually consumed, not just imported).

### C.W3.b `hero-lab` composition + `HeroPalettePreset`

- Mechanism: rewrite `demo/hero-lab/lib/types.ts:3-12` `HeroPalettePreset` to add `palette: Palette` field; rewrite `lib/palettes.ts` `HERO_PALETTES` to construct each preset's `palette` via `Palette.fromCSS(...)`; update `App.vue` + the 6 `components/*.vue` SFCs to derive `tileStops`/`atmosphereStops` from `palette.resample(n).toCSS()`.
- Files: `demo/hero-lab/lib/types.ts` (modify), `demo/hero-lab/lib/palettes.ts` (modify), `demo/hero-lab/App.vue` (modify), `demo/hero-lab/components/*.vue` (modify, 6 files).
- Sub-gate: `npx vue-tsc -b --force` green from `demo/`; `git grep "Palette\." demo/hero-lab/` returns ≥ 3 sites; visual smoke pass — hero-lab atmosphere + tile renders identically pre/post on `1280×800` light viewport.

## Hard Gate

1. `npx vue-tsc -b --force` green from `demo/` (run command: `cd demo && npx vue-tsc -b --force`).
2. **Native consumption proof**: `git grep -E "Palette\.fromJSON|Palette\.fromCSS|new Palette\(|\.sample\(|\.resample\(|\.toCSS\(|\.toSRGBSafe\(" demo/` returns ≥ 10 sites across `color-picker`, `palette-browser`, `hero-lab`, and composables (the library type is consumed, not merely imported).
3. **`PersistedPalette` boundary clear**: `git grep "PersistedPalette" demo/` shows occurrences confined to api-boundary files (`api.ts`, `usePaletteStore.ts`, `useBrowsePalettes.ts`); library-`Palette` is used for in-memory operations.
4. **`HeroPalettePreset` composes `Palette`**: `grep "palette: Palette" demo/hero-lab/lib/types.ts` matches; `grep "Palette\.fromCSS" demo/hero-lab/lib/palettes.ts` matches once per `HERO_PALETTES` entry.
5. **Playwright probe** (value.js-A invariant 3 / B invariant B4, wave-qualified — demo-touching wave warrants full probe): `npx playwright test e2e/palette-features.spec.ts e2e/palette-slug-management.spec.ts e2e/propose-name.spec.ts e2e/palette-visibility-soft-delete.spec.ts --project=chromium` green at 3 viewports (375×667, 1280×800, 1440×900) × light+dark × 0 console errors × 0 non-2xx assets.
6. **Visual regression smoke** (qualitative — no pixel-diff CI): a maintainer-eye sweep of `color-picker` palette save/load/fork and `hero-lab` preset switching shows no rendered drift from value.js-B close baseline; deltas (if any) recorded in `docs/tranches/C/audit/W3-visual-diff.md` with disposition.
7. **Library-no-persistence regression** (cohort invariant 15 — sanity sweep): `grep -rE "mongodb|express|hono|fetch\(" src/` still returns zero (W1's gate still holds; W3 does not regress it).
8. `docs/tranches/C/PROGRESS.md` row W3 cites the close commit + the Playwright report path; `coordination/CRUD-CONSTELLATION.md` row "Demo — palette-browser / color-picker / hero-lab" marked as discharged for W3.
9. **Utility-module boundary preserved** (per U1 invariant 16 — server-side only): `git grep -E "from .*api/src/crud" demo/` returns zero. The demo continues to consume the palette-api via its HTTP boundary; the `api/src/crud/` helpers landed in C.W2 do not enter client code.

## Verification Artefacts

- `git diff --stat <W3-start>..<W3-close> -- demo/` showing the consumer-migration LOC churn.
- Playwright report at `e2e/playwright-report/` (4 specs × 3 viewports × light+dark green).
- `docs/tranches/C/audit/W3-visual-diff.md` (created with disposition; empty body permitted if no drift).
- `npx vue-tsc -b --force` output (clean).
- Per-consumer-file rewrite commit list captured in `docs/tranches/C/PROGRESS.md`.

## Dependencies

- **Depends on**: C.W1 close (library `Palette` published + demo rename complete + first consumer site swap done); C.W2 progress past `PaletteDoc` type (the api boundary shape the demo persists to/from). Per `C.md:74` "W2 + W3 are independent at file bounds and may run concurrently" — file bounds confirm disjointness (W2 = `api/src/` + `e2e/` spec edits; W3 = `demo/` excluding `demo/@/lib/palette/types.ts`).
- **Blocks**: C.W4 (close ceremony cannot run while consumer migration is incomplete).

## Archaeology

H5 §2.1/§2.2/§2.3 are the inventory corrections C.md §5 missed: the demo already had `Palette` (renamed in C.W1 → `PersistedPalette`), `mixPalettes` (`demo/@/lib/palette/mix.ts:70-102`), and a `cssToRawColor`/`rawColorToCSS` shim (`demo/@/lib/color-utils.ts:21,51`) — the C.W3 scope honestly enumerates the consumer-churn budget rather than treating the surface as greenfield. H5 §5.2 specifically names `HeroPalettePreset` (`demo/hero-lab/lib/types.ts:3-12`) as a parallel "palette" type C.md treated as a single-row consumer; the resolution here is **composition** (the preset adds a `palette: Palette` field; `tileStops`/`atmosphereStops` derive from `palette.resample(n).toCSS()`), not replacement, because the preset legitimately carries UI-specific fields (`surface`, `surfaceAlt`, `shadow`) that are not palette stops. H5 §6.4 R-T-3 confirmed W2+W3 concurrency is safe as long as W1 has published the library `Palette` and the demo rename to `PersistedPalette` is complete; this spec honours that ordering by file-bounds disjointness (W2 owns `api/src/` + `e2e/`; W3 owns `demo/` excluding `demo/@/lib/palette/types.ts` which is C.W1's rename target). H5 §5.4's deferral of `colorFromCSS`/`colorToCSS` from C.W1 (the shim lift) is the explicit "deferred to W3 only if W1 lifted it" branch in scope §6 — default is no touch, preserving zero-deferral honesty per value.js-B invariant B5.
