# OWNER RULING — aurora presence + entrance + interactability (2026-07-05, pre-round-3)

## §0 Verbatim owner text (wins over any encoding below)

> There seems to be a noticable darkening/lightening effect when loading the page, which is
> fine, but not necessarily what we want to be so explicit. We want the aurora to be strong in
> effect, and have a greater variance in the derived C and H values (subtle, though, but with
> more noticeable). And with better background interatcability via the mouse and so forth.

## §1 Encoding — three rulings, all landing in S.W6 (round 3, not yet dispatched)

1. **The entrance (load darkening/lightening too explicit)** → rider on **W6-1**: the
   "boot → first frame is one material" law EXTENDS to the entrance choreography — the
   backdrop must not arrive as an explicit dark→light (or light→dark) snap. Note the W3-2
   interplay: HeroBlob/aurora now defer behind idle-callback (`ffafc8c`), which makes the
   arrival MORE visible — the deferral mechanics stay (the perf gate holds); the ARRIVAL gets
   designed (fade/derive-in on the house motion grammar, PRM-honest). A designed entrance, not
   a reverted deferral.
2. **Aurora stronger + greater derived C/H variance ("subtle, though, but with more
   noticeable")** → amplifies **W6-3** (field-derivation richness): the ruling raises the
   design bar — the field must read as STRONG presence with visibly greater chroma + hue
   variance off the pick, while staying subtle in register (variance ≠ noise; the §Triumvirate
   derive-tuning halt still binds). The seed-side levers land demo-side; the producer-side
   variance atoms (`hueSpread`/chroma-variance) were ALREADY letter L2 items — this ruling is
   their amplification-by-owner and rides the addendum dispatch (§2).
3. **Background interactability via the mouse** → NEW item **W6-7** (demo consume half):
   pointer-reactive atmosphere — the mouse influences the field (parallax/attraction/local
   bloom; design call in-wave, PRM-honest, and it must NOT fight the blob's own pointer
   choreography — one pointer grammar across the backdrop + hero). Producer half: if the
   aurora engine exposes no pointer door, that is a NEW letter ask (L19) with the standing
   hedge (in-window miss → producer-gap row, re-verify at W8); NO demo fork of the engine.

## §2 Dispatch note

The L2-amplification + the L19 pointer-door ask ride ONE addendum letter into the producer
inbox, batched with whatever producer-owned findings the card-lighting forensics lane
(`card-lighting-forensics-2026-07-05.md`, in flight) returns — one dispatch, not three.

## §3 Gate effect

W6 §Hard gate row 3 (field richness) now carries the ruling's bar (strong presence + greater
C/H variance, subtle in register); a new row covers W6-7 (pointer-reactivity landed or the
L19 gap-row recorded); the W6-1 row gains the entrance clause (no explicit load snap).
