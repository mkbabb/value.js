# S · PASS1-VERDICT — critic fleet scores + mustFix backlog (2026-07-04)

**Mean 86 / Min 72** (gate = the R-campaign discipline: loop until 100/100 on the MINIMUM).

## crit-completeness — 95

**mustFix:**
- §3.2 W0-3 / §9.2 traceability-prompts row 3: the NCSU-alias retirement (owner ratification 2.2 'no ncsu alias'; traceability §9 row 3) is mapped to W0-1/W0-3, but neither item's text nor anywhere else in the spec mentions the alias or the mbabb.fi.ncsu.edu domain (grep 'ncsu|alias' over SYNTHESIS.md = zero hits). CURE: add one explicit line to W0-3 verifying/retiring the ncsu alias against the now-canonical color.babb.dev, OR book it in §7.3 as a maintainer-on-host op with a named trigger, so the claimed home actually discharges the ratified directive instead of pointing at api-lineage items that don't cover it.

**dissents:**
- §9.2 dropdown-select-consistency row maps the S-5 '@mbabb→@MBABB' P1 to 'W5-4 ×4', but the S-5 casing cure actually lives at W7-6 (the S-5 anchor per §1/§9.1) and at design-docs-about P1-5→W7-6. The finding IS covered at W7-6; the dropdown row's citation just points at the wrong home (W5-4 owns only the ProfileSection button-migration aspect). Cosmetic ledger inconsistency, not a drop — fix the citation to read W5-4(buttons)/W7-6(casing).
- §9.2 systematically excludes P2s from several lanes (design-picker P2-1/P2-2, design-dock-shell P2-10..14, design-gradient P2-13..21) while opportunistically folding others' P2s (state-color P2-1/2, api-crud P2 F-2/7/8/9). Within the table's stated 'P0/P1' scope this is compliant, and I verified design-picker's two P2s are explicit non-defects ('sound instrument work; two nits' / 'other lanes own these'), so no genuine defect is dropped — flagged only so the successor knows the P2 tier is handled per-lane-judgment, not uniformly.
- The motion lane cites 'perf-transitions P1-7' which does not exist in perf-transitions.md (that lane numbers P1-1..P1-4). This is a lane-internal mis-citation, not a SYNTHESIS drop; the finding it names (0.55s dock hover-morph) is correctly captured at W3-5 and letter L13. No spec action required, but the successor should not chase a phantom P1-7.

## crit-root-routing — 91

**mustFix:**
- §7.1 L1 (and §0.7 line 46): the letter attributes all 3 WebKit GLSL defects to a single fixed commit `5cf8e8f0`, but two of them are live at glass-ui HEAD `a633784f` and appear introduced/widened by the LATER `BG.W-AUR-METAL-FINISH` work — `structureTensorField` was widened vec3→vec4 (the .w metal-gradient lane), so flow.glsl.ts:20 still forward-declares `vec3` against a `vec4` definition (mediums.glsl.ts:43) and brush.glsl.ts:330 assigns the vec4 into `vec3 tf`. Pinning the ask to a stale commit contradicts the spec's own §10.3 'every live probe cites the glass-ui HEAD.' CURE: cite the verified HEAD (`a633784f`) in L1, or state 'defect set verified at HEAD; provenance spans 5cf8e8f0..BG.W-AUR-METAL-FINISH' so the producer agent inspects the current tree (with the vec4-widening context) rather than a stale state where L1b/L1c may not yet exist. The file:line anchors themselves are all exact and correct.

**dissents:**
- W6-2's completion criterion (dark-field L band ≤0.42) HARD-depends on producer letter L2 (a P1) landing, with 'interim: no demo shim' explicitly forbidding a fallback. The spec is meticulous that W8 is off-critical-path ('books never gates') but does NOT symmetrically flag that design-wave completions (W6-2, and W7-3 on L4) are effectively gated on producer letter items — L2 is a P1 competing with 4+ P0 letter asks (L1/L3/L4/L5). Betting W6/W7 close on same-day producer P1 cures (§7.1 header) is optimistic; recommend §3.8/§7.1 name which letter items hard-gate a consuming wave's completion and provide an explicit fallback book (e.g. defer the dark-band to the W8 adopt) if the producer doesn't land in-window. This is a real structural tension but defensible given the ~10-min BG commit cadence and D8-1 same-day-cure precedent — not blocking.
- Minor: CLAUDE.md still records parse-that at `^0.13.0` while package.json (and the spec, correctly) reflects the live `^1.0.0` re-pin (W0-4 DISCHARGED). Not a spec defect — the spec is right and the stale doc line is out of S's routing scope — noting only that a W0-5-adjacent doc-truth pass could sweep it.

## crit-design-register — 87

**mustFix:**
- §3.9 W7-1 vs W7-4 — REGISTER CONTRADICTION on the dock's chromatic source. W7-1 makes the collapsed 'wax seal' a WatercolorDot in the LIVE picked color (icon inked in foreground/ink); W7-4 makes the expanded trigger speak `--accent-view` (a per-view HUE ROTATION of the live color). For every non-picker view these two sources differ by the view's hue-shift, so the collapse/expand `vj-morph` slides hue with no specified transition — an accidental colour-jump on a flagship moment. CURE: add one clause to W7-1 (or a §2.3 note) declaring how the seal's live-colour dot reconciles with the trigger's `--accent-view` ring across the morph (e.g. the seal ring adopts `--accent-view` while only the dot-fill stays live; or the morph is a designed cross-fade), and require the light/dark × collapsed/expanded π archive to confirm it reads as intentional, not jarring.
- §3.6 W4-1 — the title-as-component rest/hover affordance is under-specced for a flagship (S-1) change. W4-1 gives only 'caret-only affordance' and drops the treatment the picker lane bound (design-picker P1-2: 'The caret is the only rest-state affordance; hover may underline/ink-shift — the editorial link grammar — never re-grow a surface'). As written the new typographic-title selector ships with an implementer-invented hover state, which risks re-growing exactly the veil-surface the item deletes. CURE: fold the picker-lane hover grammar into W4-1 as bindable prose — hover is an ink-shift/underline (the editorial link register), never a surface re-grow — so both hosts get the same title-hover treatment.

**dissents:**
- §8 Q4 vs §3.6 W4-7 touch the SAME element ('My Palettes', rendered through `PaneHeader.vue:3` per the browse lane) with two registers: W4-7/Q5 unifies all 9 pane titles to ink Fraunces display voice, while Q4's recommended default RE-DERIVES 'My Palettes' as a 3-stop accent ramp — keeping ONE pane title a chromatic exception among 8 now-uniform ink titles. The browse lane's option (a) 'excise — the heading joins every other pane title (ink, display voice)' is the more register-coherent default given W4-7 lands in the same tranche. I'd flip Q4's recommended default to EXCISE (or at minimum cross-reference W4-7 so the ramp is understood as a display-voice fill on an already-ink title, not a competing register). Note this is surfaced correctly via the Q-table, so the mechanism is sound — only the recommended default fights the unification.
- §3.6 W4-5 asserts the channel rail re-homes onto an existing `<SegmentedTabs orientation="vertical">` with a WatercolorDot active ring, but a segmented control's native active-indicator is a sliding filled pill — a DIFFERENT visual register from the 'slim inscribed glass column + WatercolorDot ring' the picker lane (P1-6) designed as a bespoke Tabs rail variant. Whether SegmentedTabs can carry the dot-ring indicator without producing two competing active indicators is asserted, not grounded against the live component. The booked producer 'letter-rail' fallback de-risks this, but the compatibility should be verified in-wave before the bespoke-variant book is treated as merely contingent.
- §3.6 W4-8 forbids per-level hue spin on doc headings ('one-accent L/C ladder, no per-level hue spin', retiring the +40°/+20° carousel) while §8 Q4 re-introduces a 3-stop ANALOGOUS (mild hue-spin) ramp on the pane-title heading. Two heading surfaces, two opposite principles about hue-variation in headings. Analogous stops are defensibly 'one voice', so this is not a hard contradiction — but the spec should state the boundary (why L/C-only for markdown h2/h3 yet analogous-hue for the pane title) so a later pass doesn't 'fix' one to match the other.

## crit-architecture — 77

**mustFix:**
- §3.3 W1-8 (god-module round, src): the item text adopts only lib-color-audit's ~200-LoC `color/serialize.ts` lift for `src/units/color/index.ts` (968 LoC) and drops god-module-dry-census's complementary split (lift the 15 space subclasses, ~400 LoC, to `spaces.ts`). Doing the serialize.ts lift alone leaves the file at roughly 750-770 physical LoC — still far over the hard 500-LoC cap both lanes invoke — so the item as scoped does not actually clear the cap it exists to clear, and §3.3's own Completion criteria list no god-module-cap check at all to catch this. Cure: either fold both lifts (serialize.ts AND spaces.ts) into W1-8's text, or explicitly state why the serializer-only lift is judged sufficient (e.g. a code-only-LoC cap reading), and add a cap-check to W1's gate riders.
- §3.3 W1-8: god-module-dry-census also names `src/units/utils.ts` (722 LoC — flagged SPLIT-WORTHY for genuinely mixed concerns: DOM container-query/font-metric code should move to `dom-metrics.ts`) and `src/units/normalize.ts` (550 LoC — layout-cache extraction) as real, non-data-shaped splits. Neither appears in W1-8's enumerated item text, yet §9.2's ledger row ('god-module-dry-census | ... src table | ... src splits → W1-8') asserts full absorption under a §9 'Zero drops' banner. Cure: name both extractions in W1-8 (or explicitly defer them with a stated reason, matching the treatment already given to `stylesheet.ts`/`color.ts` as 'sequenced LAST') so the zero-drop claim is true by construction, not by a vague bucket citation.
- §3.8 S.W6 Completion + item W6-2/W6-3: the criterion 'dark-field L band ≤0.42' (and the Goal's 'the field varies H AND C legibly') is stated as an unconditional wave-close bar, but W6-2's own text says the dark L band only 'becomes reachable' once glass-ui letter L2 (the AuroraAtoms `scheme`/`hueSpread` door) lands — an unscheduled, trigger-gated cross-repo ask, not S's own work. Every other producer-dependent bar in this spec is explicitly hedged (W6-4's satellite-bead count reads 'post-producer-cut, else the demo-geometry half's own gate'; W8 is entirely off the critical path per 'books never gates'). This one criterion carries no such hedge, so S.W6 could structurally fail to close on its own stated bar through no fault of S's work, in violation of the books-never-gates discipline this same doc states as binding. Cure: add the same fallback pattern to the L-band/H-C-variation criterion (e.g. 'or record the L2 miss and re-verify at the 5.0.0 adopt' as W6-4 does), so W6's completion is never silently gated on an unfired external book.
- §3.11 S.W9 (CLOSE): unlike every other wave (§3.2-§3.10), W9 has no paired 'Goal:'/'Completion:' sentence — it is a flat bullet list of deliverables. The binding-disciplines line in §0 states 'paired goal/completion at tranche + wave + item level' as a hard requirement; the close wave is the one place it's dropped. Cure: prepend a one-sentence Goal (e.g. 'the tranche's own gates are proven to hold against the live tree, not just against their own green run') ahead of the existing bullet list, matching the W0-W8 structure.

**dissents:**
- Minor, not scored as a mustFix: S.W7's stated Completion criterion (§3.9) enumerates numeric/behavioral checks that map cleanly to W7-1/W7-2/W7-4/W7-5, but W7-3 (luma truth), W7-6 (dock furniture), and W7-7 (shell dedup) have no explicit check in that Completion sentence — they rely solely on the π-archive review as the catch-all. Given §6.1 explicitly designs the π matrix as the correct-sized instrument for exactly this class of finding (and explicitly rejects a broader pixel-diff gate), this is plausibly intentional rather than an oversight, but a reader could mistake it for an item silently exempted from any close-time verification.
- W1-7 bundles a fairly wide grab-bag of unrelated small fixes (Q3/Q2 policy decisions, a null-narrow, an O(N^2) flatten note, docstring truth, `unpackMatrixValues` 2D-branch bug) into one item cell. This is consistent with the spec's own stated LEAN-authoring discipline and each sub-fix is independently well-grounded in lib-core-value-audit/legacy-sweep-src, so I did not mark it as a defect, but a future implementer should treat it as N small PRs, not one.
- The synthesis's cross-lane reconciliation is otherwise excellent and explicitly exercised (§10 lesson 1 names four cases it resolved against the live tree) — the two color/index.ts split recommendations reconciled incompletely (mustFix #1/#2 above) stand out as the one place that discipline visibly slipped, against an otherwise strong track record in this same document.

## crit-feasibility — 72

**mustFix:**
- §5 W2-2 / §3.3 W1-6 — intra-round hard dependency breaks the stated parallel-round claim. §3.1's round table schedules W1 and S.W2 in the SAME round ("1: W1 ∥ W2" — parallel dispatch), but W2-2 ("src-side accent stringification") explicitly requires W1-6's new `safeAccentCssString` export before it can land. If W1 and W2 truly run in parallel, W2-2's agent either blocks (defeating the parallelism this round is supposed to buy) or hand-duplicates the helper it's about to consume, creating rework/merge friction. Cure: promote the tiny `safeAccentCssString` helper to S.W0 as a one-function library patch (it doesn't need the rest of W1 to land first), OR add an explicit intra-round ordering note to §3.1 (mirroring W1-8's own self-declared 'sequenced LAST' discipline), OR move W2-2 into S.W4 which already runs strictly after both W1 and W2 close.
- §6.2 perf-budget row 'Eager cold-load ≤420KB total, JS ≤280KB' has no credible mechanism behind its CSS half. Measured baseline is 358KB JS + 184KB CSS gzip = 543KB. The only committed action (§3.5 W3-2: defer HeroBlob/aurora-canvas/PointerDebugOverlay behind idle-callback) is JS-only, and its own source lane (perf-general.md §5, 'P2… not asserting a specific bug… worth a coverage pass') explicitly declines to commit a CSS-reduction mechanism, leaving the 184KB CSS share as an unowned rider split ambiguously between demo-build and glass-ui-producer. Worse, `PointerDebugOverlay` is dev-only tooling with 'zero end-user exposure' per motion-animation-inventory.md:181, so deferring it saves ~0 production bytes; the real lever (HeroBlob+aurora canvas) is a small fraction of the 200.4KB eager index chunk. Reaching ≤280KB JS (a ~78KB cut) and ≤420KB total (CSS effectively needing to drop to ~140KB) from W3-2 alone is not credible arithmetic — this budget risks being silently missed and only discovered at wave-close re-measurement. Cure: name a concrete, owned CSS-reduction item (e.g. a Tailwind coverage/purge pass with its own stated target and owner) as its own W3 sub-item, or split the single aggregate gate into a JS-only near-term number (achievable via W3-2) plus an explicitly booked/deferred CSS number, rather than presenting one composite figure no listed item can reach.
- §3.7 S.W5 ('Suffusion II') is oversized relative to its own 'Max 6 agents/wave' cap and lacks the lane split the tranche's own precedent (R.W4 Lane A/B/C) established for comparable scope. It bundles 13 items (W5-1..W5-13) across 7 distinct feature surfaces — Browse, the Palettes dialog, CurrentPaletteEditor, Extract, Mix, Generate, Gradient (correctness + visual), and Admin — with no internal lane structure. W5-6 alone ('Extract + Mix + Generate recomposition') folds three independent feature-page redesigns (extract's hover/stat/eyedropper rework, mix's empty-state + producer-verify + composable relocation, generate's hero-plate + regenerate action) into one bullet; W5-11 ('Gradient correctness P0s') is itself a 4-defect rewrite (atomic applyCSS, code-editor truce, stop-editor rework, radial silent-drop fix). R.W4 needed 3 explicit lanes for a narrower surface (cards / shell+motion / view-select) at the same wave granularity. Cure: split S.W5 into explicit lanes (e.g. Lane A: browse/palettes/admin grammar unification; Lane B: extract/mix/generate; Lane C: gradient correctness + aliasing) with independent completion gates, mirroring the R.W4 precedent — or promote W5-6 and W5-11 to their own wave-level items with named agent allocation.
- §3.1 round 2 (W3 ∥ W4) schedules two parallel waves that both edit `ColorPicker.vue` with no coordination note: W3-4 ('Pane-swap payload') anchors `ColorPicker.vue:314-318` and W4-2 ('Header re-composition') anchors `ColorPicker.vue:29`. Different line ranges reduce but don't eliminate the risk — both items restructure the same file's template/composition in the same parallel round, which is exactly the single-writer-discipline hazard this angle is meant to catch (structural edits shift line numbers and can turn a clean hunk-level split into a real merge conflict). Cure: add an explicit file-ownership note to §3.1's round-2 description (e.g. 'W4-2 lands first in round 2; W3-4 rebases' or vice versa) so the two fleets don't produce simultaneous conflicting diffs to the same file.

**dissents:**
- vue-router 4→5 migration (W2-7) is folded into the already-dense W2 architectural-transposition wave (9 items: color-pipeline rewrite, DI seam, 30-fn api service-layer unification, browse-actions dedup, composable hygiene, plus the router migration) even though the router migration's own source lane (deferred-books-census.md §2) explicitly states 'a real migration-scope estimate…was NOT done in this pass and should be the S wave-item's first task, not assumed trivial,' and vue-router 5 carries known real breaking-change surface (typed routes, data loaders, memory-history changes). Q11's escape hatch ('de-scope to a book ONLY if the probe finds a disproportionate surface') has no objective threshold for 'disproportionate,' so the decision lands as discretion exercised right as W2 is starting its own heavy lift. Recommend either an objective bound on Q11, or explicitly sequencing W2-7 as W2's LAST sub-item (mirroring W1-8's self-declared 'sequenced LAST, do not split under time pressure' discipline) so a scope blow-up doesn't stall the rest of W2's closure. Flagged as a dissent rather than mustFix because the spec does provide a (soft) escape valve.
- W7-5's '≤100ms view-switch first post-click frame' budget (§6.2, from 254.7ms baseline) is achieved by the design lane's own prescribed mechanism — sequence the cheap settle/morph beat into frame 1 and defer the expensive pane mount one frame behind — which is sound, but this is a metric redefinition (what counts as 'first frame') rather than a reduction in total swap cost: perf-transitions.md's P1-1 measured the actual mount itself costing a 183ms long task, and that cost doesn't disappear, it just moves to frame 2+ where the stated gate no longer measures it. Not necessarily wrong engineering, but the wave-close verification for W7-5/W3-4 should assert against the FULL view-switch completion (not just frame 1) so the fix isn't validated by a gate that only watches the part that got cheap.
- The 'Idle picker frame p50 ≤13ms' gate (§6.2) is achievable via W3-3's demo-side idle-gate (pausing the blob's RAF after N ms of inactivity) rather than needing the glass-ui producer's per-frame-CPU profile (letter L5) to land first — this is good (the gate is reachable by S's own work), but the spec never states the idle-gate's inactivity threshold N relative to whatever measurement window the future frame-budget spec (§6.1 item 5) will sample over. If the test window is shorter than N, the gate would fail even though the intended 'true idle' behavior is correct. Worth a one-line coordination note tying W3-3's threshold to the eventual spec's sampling window, not a structural objection.

## crit-evidence — 94

**mustFix:**
- §3.3 W1-7 bullet: reword the clause "`unpackMatrixValues` 2D branch returns 0 for out-of-plane rotations" — as written it reads as a factual description of CURRENT behavior. Verified against src/units/utils.ts:294-307 and lib-core-value-audit.md P2-5: the live code actually computes nonsense atan2-derived values (`rotateY = atan2(-c, a)`, `rotateX = atan2(b, d)`) for the 2D matrix() branch, NOT 0. "Return 0" is the lane's *candidate fix*, not the current state. Reword to something like: "`unpackMatrixValues` 2D branch emits nonsense atan2-derived rotateX/rotateY for a planar transform (utils.ts:304-306) — fix: return 0 for the two out-of-plane rotations" so an implementer can't read the bullet as already-satisfied.

**dissents:**
- Minor/cosmetic: W1-2 cites the two-branch idiom precedent as `index.ts:188-196`, but that range is the explanatory COMMENT describing the idiom; the lane report (lib-parsing-audit.md:331) itself cites the actual branch implementation at `index.ts:197-205`. Both point to the same mechanism in the same function so this is not a factual error, just an imprecise line-range choice worth tightening before the spec becomes the verbatim corpus.
- The glassui-consume-map lane records its audit-time repo state as value.js `c5aa091`, one commit behind the synthesis's stated substrate `b5f94bc` (confirmed ancestor via git merge-base; the intervening commit only touched Dock.vue's dead onTransitionEnd consume, unrelated to any claim I checked). No material drift found in this instance, but per the synthesis's own §10 lesson 3 ("the producer moves daily — treat drift as a designed-for condition"), lane-level 'verified at HEAD' claims should be spot-re-verified against the synthesis's own pinned substrate commit at wave-open, not just trusted at their original audit-time stamp.
