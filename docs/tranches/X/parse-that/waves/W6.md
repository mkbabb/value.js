# X.P.W6 — the CSS grammar comes home to value.js, in BBNF on parse-that; the custom algebra retires

## State
**Opens after**: X.P.W5 (STOPPED by the owner's ruling 2026-09-23; its `.a`–`.c` bytes stand as evidence). **Minted by**: COHESION §0by. **Model**: Opus 5.5 every seat. **Record**: `docs/tranches/X/execution/D/X-P-W6.md`.

## Authority — the owner's ruling, verbatim (2026-09-23)
Asked whether the parser is parse-that-idiomatic and why it lives in parse-that, after the orchestrator recommended *"Put the CSS grammar in value.js, written directly in parse-that's combinators, as D-23 said. Keep parse-that a general parsing library, with only fixes to parse-that itself going there."*, the owner ruled: **"What you recommend. No custom grammar, unless it's BBNF."** (D-23, 2026-07-20: *"BBNF modules mirrored 1:1 into parse-that TypeScript combinators … parse-that consumed as exact published … parse-that-internal work stays PT-E"*.)

## The law this wave enforces
- The CSS grammar is **value.js's**, lives in value.js, and is authored as **BBNF** (the grammar is the source of truth), turned into **parse-that parsers** by the published BBNF toolchain (`@mkbabb/bbnf-lang` or parse-that's own BBNF loader — measure which is the published, idiomatic path and name it), or — only where BBNF cannot express a production — written directly in parse-that's combinator API. **No intermediate operation algebra, no bespoke DSL, no hand-rolled scanner.** The 22-op AC-1 algebra, its reifier and its Wasm emitter retire.
- parse-that stays a general parsing library: only parse-that-internal fixes land there (e.g. the packrat latch `f5169b1`).
- The frozen `/css` public surface does not move (D-23: `parseStylesheet` / `collect*` / the `ParseIssue` union / keyframes' consume sites).

## Units (serial; the ADJACENT-LINE RULE binds)
- **`.r` (Opus) — parse-that returns to a library.** On parse-that master, one ordinary commit (never a force-push, never a history rewrite) removes the CSS surface: `typescript/src/css/**`, the `./css` export, the `ac1.wasm` build, the css-specific tests/harness (moved as evidence into value.js `docs/tranches/X/parse-that/evidence/W6/retired-seam/` first, so nothing is lost), and restores `proof:no-css-surface` with a dated note citing this ruling. Library fixes stay. The unpublished 2.0.0 release commit is superseded: parse-that's next release carries only library changes (versioned by its own semver, published when the owner's npm login exists; the tag waits). Gate: parse-that `npm test` + `proof:all` GREEN; `git grep` finds no CSS grammar in parse-that.
- **`.b` (Opus, effort high) — the BBNF grammar in value.js.** Author `src/css/grammar/*.bbnf` (restore and extend the BBNF grammars value.js carried until `36f918d2`, and consult parse-that's and bbnf-lang's `grammar/css`), wired through the BBNF toolchain into parse-that parsers behind the frozen `/css` surface. Full spec coverage in the BBNF: CSS Color 4 incl. legacy comma forms with `none` refused (KFA-14; this discharges X-W12 `.l`), `calc()` in channels, `display-p3-linear`, relative colour with its context contract; CSS Color 5 `color-mix()` and `light-dark()` (through the existing context path, a colour-scheme field — no new export); `var()` in the `animation` shorthand (css-variables-1 §3). Port X.P.W5 `.c`'s WPT-derived cases (`css-color5.test.ts`, the legacy-form pins) into value.js tests. Gate: those cases GREEN; `vue-tsc`/`tsc` 0; the grammar files are the single source (no second grammar in TS).
- **`.h` (Opus) — equivalence against the shipping parser.** Port the differential harness and corpus into value.js tests; run the BBNF parser against the current `src/css/grammar.ts` over the full corpus (value's tests, the demo CSS, keyframes' CSS, the assay corpus); every divergence classified by the DIVERGENCE-LEDGER rulings (the per-cell and F-w4f-1 class rulings carry) or rowed new; MIRROR-DEFECTS 0 ×2.
- **`.x` (Opus) — the swap.** value.js parses CSS only through the BBNF grammar; `src/css/grammar.ts`'s hand-rolled parser is deleted (no shim); `npm test` GREEN; the bench of record (`bench/`) read BBNF vs the retired hand parser at a quiesced load and recorded (the D-23 perf bar); keyframes.js's CSS behaviour unchanged against value.js HEAD (its test suite linked against the local build).
- **RC-P** is re-read by a dated addendum to RELEASE-CONDITION: `ADMITTED` (Wasm) retires with the algebra; the seam is value.js's own BBNF grammar; KF.W3 stays gate-keyed on the re-read predicate at X-W11's coordinate.
