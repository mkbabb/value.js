`claude-opus-5[1m]`

# X.P.W3 - Totality and Recovery to Spec

**Sub-tranche**: X·P (`docs/tranches/X/parse-that/waves/`), authority `docs/tranches/X/COHESION.md`
§0/§1 SS-5, ratified under SCOPE.md M-25 ¶1. **Alias law (R-B idiom)**: the sibling spelling
`P.W3` denotes `X.P.W3`. It is a search alias, never a rename — `X.P.W<N>.<x>` is the
`WAVE_SPEC.md` §5 form and is authoritative. No sibling file is re-glyphed.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** X·P inherits the parser pause
(`PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` §7) and tranche X's execution gate: nothing in this
wave opens, writes, builds, benches, or releases until the **owner's begin-word**. Specifying is
not doing. This file is authored read-only against frozen evidence; every baseline below was
measured read-only on 2026-08-03 and is pasted with its command.

## 2. State

**Name**: W3 - Totality and Recovery to Spec
**Opens after**: X.P.W0 (pause authentication per handoff §9 + the fresh writer root opened at its
FIRST WRITE), X.P.W1 (the semantic-and-recovery algebra: formula↔predicate-ID bijection, closed
authenticated unions, no-CST commitment), X.P.W2 (dual-target realization: source-direct JS +
zero-function-import Wasm). **The dependency is on the conditions, not the labels** — §2b
enumerates each as a checked open-precondition, so a sibling re-numbering cannot silently open
this wave early.
**Agents**: 5 — phase 1: 3 parallel Opus 5 seats (`.a`, `.b`, `.c`, disjoint paths) · phase 2: 1
serial Opus 5 seat (`.d`, the differential floor, needs `.a`–`.c` landed) · phase 3: 1 **fresh
Fable adjudicator** (`.e`, M-23 §1 — closure/bijection proof and the divergence apotheosis are
adjudication, not mechanism)
**Hard gate**: ten conditions in §6 — the 52-export universe TOTAL · the R1 throw class dead ·
typed recovery for every malformed input over a closed union with no fallback · dual-target
recovery identity · the equivalence floor held at zero mirror-defects · labelled diagnostics
without arming · latch/depth/boundary cures · a bench table whose BAR is owner-gated. **Nine are
born-RED with the failing probe measured and pasted; one is MEASURE-AT-OPEN with its exact
command.**
**Status**: planned

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | `GATE-VERDICT.md` (composite RED-of-distance) · `registry/adjudicated/parser-band.md` · `PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` · INBOX O-15 |
| SPECIFIED | **YES — by this file, 2026-08-03** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md`; X·P's own L-20 conformance loop closes the sub-tranche stamp |
| IMPLEMENTED | **NO** | — gates green + bytes landed in the fresh root stamps this (R-A) |
| VERIFIED | **NO** | — stamped **only** at X.P.W4's sub-tranche release close (R-A); no wave stamps VERIFIED at its own close |

### 2a. Goal criterion

This wave succeeds if, when work ends, **there is no CSS string — well-formed, malformed, hostile,
or not a string at all — that makes the candidate parser do anything other than return a typed
result**, and if the 52-export contract value.js already ships is covered by name and shape rather
than approximated. Totality is not a quality attribute here; it is the frozen type's own promise.
`ParseResult<T>` is `{ok:true, value, diagnostics:[]} | {ok:false, diagnostics:[ParseIssue,
...ParseIssue[]]}` (`src/css/types.ts:25-27`) — a function that throws has not failed to be fast,
it has failed to have the type it declares.

### 2b. Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring | check at open |
|---|---|---|---|
| OP-1 | **Owner begin-word for X·P.** The pause (§7) forbids execution, materialization, mutation, and dispatch. | STANDING PAUSE. `PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md:186-197`. | The owner's dated word, cited by commit body. No inference from this file. |
| OP-2 | **X.P.W0 authentication PASSED.** Handoff §9: recompute every hash and census before acting; a present v12 target, a changed builder, a changed residue, or a stale receipt means **STOP**. | UNRUN. Pinned identities: builder SHA-256 `0732ebc2…4ee8` (243,827 B), residue `de1d62ff…0937` (154,221 B), matrix `244c448a…04a7`, DREI intake `aa891714…2767`, target `/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-and-experiment-v12` **ABSENT**. | W0's pasted hash table, all rows matching. Any mismatch halts X·P, not just this wave. |
| OP-3 | **The fresh writer root exists and is the only writer.** `/Users/mkbabb/Programming/parse-that-css-totality-p2`, cloned from packet commit `f5757082`, opened at X.P.W0's FIRST WRITE (M-22 ¶3). | **ABSENT by design** — verified `ls -d /Users/mkbabb/Programming/parse-that-css-totality-p2` → *No such file or directory*, 2026-08-03. `f5757082` resolves in `/Users/mkbabb/Programming/parse-that` (`f575708 docs(parser-audit): bank N2 v10 …`). | Root present; `/Users/mkbabb/Programming/parse-that` and every frozen root byte-unchanged (`git -C … status --porcelain` empty). |
| OP-4 | **X.P.W1's bijection is closed.** Handoff §8.3: an exact formula→predicate-ID bijection rejecting missing, extra, duplicate, reordered, or misbound formulas. | UNRUN. The named live defect: **OR05 declares 18 production predicate IDs while its required-value domain contains 20 formulas**; the registry binds the first 18, so the last declared IDs are *misbound*, not absent (§5.1–5.2). | W1's bijection report, both set-differences ∅. This wave's recovery union inherits the same closure discipline; opening without it re-imports the defect. |
| OP-5 | **The bench BAR.** The `≥10x` floor is **RETIRED AS LAW**; the replacement strict-3x / strict-2x / break-even families are **UNRATIFIED** (`0/5` each). OC-1 (bench-bar recalibration) is open from the 07-20 gate and unresolved. | UNRULED. | No ruling is required to open — G-10 **reports** ratios and marks the BAR `OWNER-GATED-PENDING-RATIFICATION`. **Inventing a bar is a defect**, and a wave that passes or fails on an unratified bar is void. |

OP-1 and OP-2 are hard: a wave that opens without them repeats the exact failure the pause exists
to prevent — treating research evidence as product authority.

### 2c. Corpus transposition (M-25 ¶2 — what this wave folds, and as what)

| source row | verb | lands as |
|---|---|---|
| `GATE-VERDICT.md` P-2 coverage RED (0 TOTAL / 3 PARTIAL / 49 ABSENT over 52) | BUILD | the wave's universe; units `.a`/`.b`; gates **G-1**, **G-2** |
| `GATE-VERDICT.md` F-2 / R1 — `parseCssColor("oklch()")` shipping crash | BUILD | the **named enemy**; unit `.c`; gates **G-3**, **G-4**, **G-5** |
| `GATE-VERDICT.md` P-1 equivalence GREEN (0 mirror-defects, 403 strings) | **FLOOR — inherited, never regressed** | unit `.d`; gate **G-7**. A GREEN arm is not a finished arm: it is graduated to the full surface and re-proven, and a regression is a wave-blocker |
| `GATE-VERDICT.md` OC-1 (bench bar) / OC-2 (staged reading) | CARRY-UNRULED | OP-5; gate **G-10** reports, never adjudicates |
| `parser-band.md` VERDICT — cand-O ruled winner (4-file `ast`/`spec`/`grammar`/adapter base) | ADOPT | the seeded architecture X.P.W1/W2 realize; this wave conforms it |
| `parser-band.md` "what cand-O owes cand-F", debts 1–5 | BUILD (binding) | 1 → **G-8** · 2 → **G-10** reject leg · 3 → **G-9** · 4 → `.c` (exactness + JS-boundary guard) · 5 → `.a` corpus union |
| `parser-band.md` born-RED G1–G8 | FOLD | subsumed: G1→**G-2**, G2–G5→**G-6** conformance rows, G6→**G-7**, G7→**G-10**, G8→X.P.W1's idiom gate (architecture, not totality) |
| `parser-band.md` DISSENTS (token juxtaposition · non-finite · try/catch posture · bench epistemics) | **DECLARED-DIVERGENCE ROWS, not silent picks** | `.d`'s ledger, asserted in both directions; **G-7** fails if any divergence is unrowed |
| Pause handoff §6 KEEP (no-CST product equality · zero-function-import Wasm · malformed inverse · raw-first observations) | BUILD | `.b`, `.c`; gates **G-5**, **G-6** |
| Pause handoff §6 PRUNE (same-root repair · silent re-pin · timeout widening · denominator rewrite) | PROHIBITION | §3 closing prohibitions; **G-10**'s denominator clause |
| Pause handoff §8.5 (closed authenticated union, no fallback) | BUILD | unit `.b`; gate **G-4** |
| INBOX **O-15 / PT-01** — `label` no-op unless armed; arming couples an unconditional `console.error` | BUILD | **G-8** (labels available UNARMED; zero `console.*` on the parse path) |
| INBOX **O-15 / PT-03** — `PACKRAT_ARMED` one-way latch; 93.9 → 138.2 ns/parse (1.47×); `resetPackrat()` → 139.3 | BUILD | **G-9** latch leg + **G-10**'s mandatory arm-state declaration |
| INBOX **O-15 / PT-04** — `Parser.lazy` depth 7,761 OK, thrown `RangeError` at 7,762 | BUILD | **G-9** depth leg (cand-O debt 3: bounded by construction, not by catch) |
| INBOX **O-15 / PT-07** — 5/5 non-string inputs throw raw `TypeError`; `.parse()` returns `undefined` on failure | BUILD | **G-3** boundary leg (our cure sits **above** parse-that; it is not an ask to them) |
| SCOPE.md **M-22 ¶4** — all 3x/2x budgets restate against **1,636,680 µs** | BINDING | **G-10** |

## 3. Scope

1. Enumerate the universe mechanically: the 52 frozen `/css` exports (19 runtime + 33 types) read
   from `src/css/index.ts` at a pinned value.js commit, emitted as a machine-readable conformance
   manifest in the fresh root — never hand-transcribed.
2. Drive every runtime export to **TOTAL**: covered by name and by shape, with a runnable
   assertion per row over a corpus that includes the export's own accept set and reject set.
3. Drive every type export to **TOTAL** by bidirectional assignability under `tsc --noEmit`, so a
   widened or narrowed candidate type fails the compile rather than passing a name check.
4. Author the recovery algebra as a **closed authenticated union**: every failure carries a code
   drawn from the frozen 8-code `ParseIssue` union, a byte range, a non-empty `expected` list, and
   the observed `actual` — with **no fallback arm** anywhere in the lowering.
5. Kill the R1 throw class by construction: no `throw` reachable from any public entry, no
   `undefined` return on failure, and a JS-boundary invariant that converts non-string arguments
   into `ok:false` rather than a raw `TypeError`.
6. Make depth exhaustion an ordinary `ok:false`: the one lazy back-edge carries an explicit depth
   bound, so the stack ceiling becomes a diagnostic instead of a `RangeError` (cand-O debt 3).
7. Prove recovery **identity across both targets**: source-direct JS and zero-function-import Wasm
   produce byte-identical `{ok, code, start, end, expected, actual}` for every corpus input.
8. Graduate the P-1 differential harness from the 403-string W0 subset to the full 52-export
   surface against the **sha-pinned published 4.0.0 tarball** — never the working tree, whose
   `dist/` is known to drift from what npm ships.
9. Row every intended difference from the incumbent as a **declared divergence** with its
   adjudication, asserted in both directions; an undeclared difference is a mirror-defect and
   reddens the floor.
10. Publish the three-leg bench table (accepted / reject / R1-class) as measured ratios against the
    conservative reconstruction, with the arm-state of the packrat latch printed on every row and
    the pass/fail BAR marked `OWNER-GATED-PENDING-RATIFICATION`.

**Prohibitions, from the pause handoff §6 PRUNE and §7.** No same-root repair — every byte is
written in the fresh root, never in `/Users/mkbabb/Programming/parse-that` or any frozen root. No
silent re-pin of a corpus, fixture, or expectation to make a gate pass. No timeout widening. No
denominator rewrite: a bench claim restated against anything but 1,636,680 µs is void. No
self-authored answer keys — the oracle is the published tarball and the CSS specification text, in
that order, and where they disagree the disagreement is a rowed divergence, not a preference. No
CST. No product, parser, package, release, or rebind credit is inferred from anything this wave
measures; adoption is X.P.W4's seam and X·V's bytes.

### 3a. Triumvirate Dispatch

Mandatory (research + plan augment + redress), not optional; the orchestrator may not redispatch
the failing unit alone.

- **File-bound expansion.** Any write proposed outside §4's table — in particular any byte under
  `/Users/mkbabb/Programming/value.js/src/**`, `/Users/mkbabb/Programming/parse-that/**`,
  `~/.codex/**`, `~/Documents/Codex/**`, or any frozen root. A totality wave that edits the
  incumbent to make its own differential pass has inverted the experiment.
- **Hard-gate failure that is not local-edit-recoverable.** G-5 (dual-target recovery identity)
  failing in a direction that is not a lowering bug — a divergence rooted in the Wasm numeric or
  memory model is an architecture question for X.P.W1/W2, not a patch here. G-7 going RED with a
  *newly discovered* incumbent defect (a sixth R-class) — that is a finding routed to X·V and the
  fleet, not a mirror repair.
- **Third diagnostic iteration.** Any diagnose→edit→re-measure loop reaching iteration three on
  the same gate halts. G-1's 52-row matrix in particular: three passes without a monotone increase
  in the TOTAL count means the universe is mis-specified, not under-implemented.
- **Any proposal to widen the union.** Adding a ninth `ParseIssue` code is a contract change to a
  frozen surface. It halts the wave and goes to X·V + the owner; it is never a local decision.

## 4. File Bounds

Two roots. The **fresh writer root** carries the mechanism; **this repo's docs tree** carries the
evidence. No third root is writable, in any unit, for any reason.

| File | Access |
|---|---|
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/src/css/**` | create/modify (the candidate grammar, adapter, and recovery lowering) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/test/css-totality/**` | create (the 52-row conformance suite) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/test/css-recovery/**` | create (the malformed-inverse suite) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/test/css-equivalence/**` | create (the differential harness + vendored sha-pinned tarball fixture) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/scripts/css-universe.mjs` | create (emits the 52-row manifest; G-1's generator) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/scripts/css-recovery-closure.mjs` | create (G-4's union-closure + no-fallback checker) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/scripts/css-dual-target-identity.mjs` | create (G-5) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/scripts/css-bench-three-leg.mjs` | create (G-10) |
| `docs/tranches/X/parse-that/evidence/W3/**` | create (pasted outputs, manifests, JSON results, sha256 sidecars) |
| `docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md` | create (`.d`'s rowed divergences; `.e` adjudicates) |
| `docs/tranches/X/parse-that/waves/W3-CLOSE.md` | create (ten gates, RED-before / GREEN-after, pasted) |
| `docs/tranches/V/megatranche/registry/harvest/x-p-w3.json` | create (L-13 harvest for this wave's workflow; `.e` at close) |
| `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` | modify-append (by `harvest-journals.mjs`, once, unit `.e`; never rewritten by hand) |
| `docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs` | **execute, no write** (G-2's anchor probe, re-run unmodified — bounded because the gate invokes it; R-E) |
| `docs/tranches/V/megatranche/workflows/harvest-journals.mjs` | **execute, no write to itself** — `.e`'s sub-gate invokes it verbatim: `node docs/tranches/V/megatranche/workflows/harvest-journals.mjs`; it writes only the two registry rows named above (R-E) |
| `docs/tranches/V/megatranche/prototypes/css-parser/cand-o/**` | **read, no write** (the adopted architecture; seed source for the fresh root) |
| `docs/tranches/V/megatranche/prototypes/css-parser/cand-f/**` | **read, no write** (the folded suite and the five debts) |
| `docs/tranches/X/COHESION.md` | modify-carve (§5 status board line only, at close) |

**Do NOT touch**: `/Users/mkbabb/Programming/value.js/src/**` · `demo/**` · `api/**` · `test/**` ·
`e2e/**` · `scripts/**` (incl. `scripts/dev/dev.sh`, unowned, NEVER) · `package.json` ·
`/Users/mkbabb/Programming/parse-that/**` (read-only evidence; the fresh-root law) ·
`/Users/mkbabb/Documents/Codex/**` and `~/.codex/**` (preserved evidence, read/hash only, C-02) ·
`/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-v12-construction/**` (frozen builder +
the preserved `__pycache__` residue — **do not clean, regenerate, normalize, or credit**) ·
`/Users/mkbabb/Programming/keyframes.js/**` · `/Users/mkbabb/Programming/fourier-analysis/**` ·
`docs/tranches/V/apotheosis/**` and `docs/tranches/V/megatranche/coordination/**` (dated evidence,
append-never-rewrite).

### 4a. Disjointness

No two units share a `create`/`modify` path. `.a` owns `scripts/css-universe.mjs` and
`test/css-totality/**`; `.b` owns the recovery lowering under `src/css/**` plus
`scripts/css-recovery-closure.mjs`; `.c` owns the boundary/depth/latch cures, which live in
`src/css/entry.ts` and `src/css/bounds.ts` — **two files `.b` does not open** (the split is stated
here, at authoring, because `src/css/**` is otherwise a shared glob and a shared glob is the
hidden conflict the X·V W5-D1 defect taught). `.d` owns `test/css-equivalence/**` and
`DIVERGENCE-LEDGER.md`; `.e` writes `W3-CLOSE.md` and the ledger's adjudication section, appended
after `.d` commits, and holds the **sole** `modify-carve` on `COHESION.md` (§5 status-board line
only) and the **sole** append to `DEFECT-LEDGER.md` — both single-owner acts at close.
`harvest-journals.mjs`, `registry/harvest/`, and `DEFECT-LEDGER.md` are shared append-only
infrastructure across every lane (L-13); the append is a script's act, never a hand's, which is why
the row is `modify-append` and the harvester itself is execute-only.

**Cross-wave.** X.P.W2 holds `src/css/**` in the fresh root and closes before this wave opens
(§2 Opens-after); the inheritance is sequential, never concurrent. No X·V wave writes in the fresh
root, and this wave writes nothing under `/Users/mkbabb/Programming/value.js/src/**` — the two
trees are disjoint by construction, which is what makes X·V's byte-ownership (X.P.W4 §Seam)
true rather than promised.

### 4b. Worktree Plan

Phase 1's three seats run in git worktrees of the **fresh root**, not of value.js, and — per the
ONE-root law canonical X.P.W2 §4b states and this wave adopts unchanged — **inside** that root,
never beside it: *"worktrees **inside** the fresh root, never beside it (ONE named root, no siblings
ever created)"*. `<p2>` below is `/Users/mkbabb/Programming/parse-that-css-totality-p2` — §4's fresh
writer root, and the sole writer.

| Agent unit | Worktree | Build dirs |
|---|---|---|
| X.P.W3.a | `<p2>/.worktrees/w3a` | own `typescript/node_modules`; `CARGO_TARGET_DIR=<p2>/target/w3a` (the Wasm half is Rust-backed; a shared target dir races) |
| X.P.W3.b | `<p2>/.worktrees/w3b` | as above, `CARGO_TARGET_DIR=<p2>/target/w3b` |
| X.P.W3.c | `<p2>/.worktrees/w3c` | as above, `CARGO_TARGET_DIR=<p2>/target/w3c` |

**No `parse-that-*` directory is created beside the frozen roots by this wave, ever.** A sibling of
the form `<p2>-w3a` would be indistinguishable by name from an unlawful root in X.P.W0's census grep
of `~/Programming`, which is precisely why the law reads *inside*.

Phase 2 (`.d`) and phase 3 (`.e`) are serial on clean main after phase 1 integrates. The
orchestrator runs `git worktree list` and `git worktree add` in the fresh root before dispatch, and
records both that **no worktree of the read-only `parse-that` root exists** and that the fresh root
has no sibling.

## 5. Agent Units

**Model seats (M-23; an undeclared seat is a defect).** `.a`–`.d` are **Opus 5 implementation
seats** (M-23 §2) — this wave authors conformance mechanism, not design; the architecture was
adjudicated in `parser-band.md` and realized at X.P.W1/W2. `.e` is a **fresh Fable adjudicator**
(M-23 §1): closure proofs and divergence adjudication are the "complexity and adjudication" class
the ruling reserves for Fable, and the seat must be fresh — an author cannot adjudicate his own
union.

### X.P.W3.a The 52-row conformance universe

- **Goal**: a machine-generated matrix in which every one of the 52 frozen exports is a row with a
  runnable assertion, so "coverage" is a count nobody can round up.
- **Mechanism**: `scripts/css-universe.mjs` reads the pinned value.js commit's `src/css/index.ts`
  and emits `{runtime: string[19], types: string[33]}` plus, per row, `{name, kind, arity,
  accept[], reject[], verdict}`. Verdicts are `TOTAL | PARTIAL | ABSENT` on the `coverage.md`
  legend (TOTAL = covered by name **and** shape). The accept/reject corpora fold, as a union: the
  GROUND-A empty-arg cross-product (**21 heads × 10 fillings**), cand-F's 4,000-case mutation
  fuzz, cand-O's replayable mulberry32 30,000-input corpus, the 148 named colours, the 403-string
  P-1 corpus, and the 172-input R1 corpus. Conflicting expectations between the two folded suites
  resolve to `parser-band.md`'s adjudications (hue wrap → unwrapped; juxtaposition → accept;
  `1e400` → `color_non_finite`), each carried as a `.d` divergence row rather than a silent pick.
- **Files**: `scripts/css-universe.mjs`, `test/css-totality/**`.
- **Sub-gate**: G-1, G-6.

### X.P.W3.b The recovery algebra — a closed union with no fallback

- **Goal**: every rejection is a *description* of the rejection, drawn from a union that is closed
  by construction rather than by review.
- **Mechanism**: the lowering emits `ParseIssue` values only from the frozen 8-code union
  (`css_syntax`, `trailing_input`, `keyframe_selector_invalid`, `color_context_required`,
  `syntax_descriptor_invalid`, `syntax_mismatch`, `animation_option_invalid`,
  `timeline_option_invalid`; `src/css/types.ts:11-19`). `scripts/css-recovery-closure.mjs`
  asserts, over the built graph and the emitted corpus results: the emitted code set ⊆ the frozen
  set; **every** frozen code is emitted by at least one corpus input (both set-differences ∅, the
  X·V 117-row idiom); no `default:` / `else` arm exists in the code-selection switch; every
  `ok:false` carries a non-empty `diagnostics` tuple (the frozen type requires
  `readonly [ParseIssue, ...ParseIssue[]]`, so an empty array is a *type* violation, not a style
  one); and every issue's `[start,end)` indexes real input bytes with `expected.length ≥ 1`.
  Handoff §8.5's rule, applied to the parser: a closed authenticated union with no fallback.
- **Files**: fresh root `src/css/{lower,diagnostics,codes}.ts`, `scripts/css-recovery-closure.mjs`,
  `test/css-recovery/**`.
- **Sub-gate**: G-4, and G-8's label leg.

### X.P.W3.c The R1 execution — boundary, depth, and latch

- **Goal**: the named enemy dies by construction, in all three of the shapes that actually reach a
  shipping caller.
- **Mechanism**: three cures, one unit because they share the entry module and splitting them
  would put two writers on one file.
  1. **The throw class.** No `throw` is reachable from any public entry; the guard cand-O keeps is
     retained only as a proven non-load-bearing shield, and its non-load-bearing-ness is itself
     asserted by a raw-`parseState`-over-corpus test (cand-O's instrument, which the band requires
     to survive). The dissent stands recorded: cand-F holds a shield converts an impossible bug
     into a silent `ok:false` and ships without one; if `.c` lands the depth bound below, that
     position becomes tenable and removal is a live option for X.P.W4, not a decision here.
  2. **The JS boundary (PT-07).** Non-string arguments — `null`, `undefined`, numbers, objects,
     symbols — return `ok:false` with `css_syntax` and `actual: null`, never a raw `TypeError`.
     This is our invariant **above** parse-that, explicitly not an ask to them (O-15 says so in
     the letter's own "what is NOT in this letter").
  3. **Depth (PT-04) and the latch (PT-03).** The one lazy back-edge carries an explicit depth
     bound so exhaustion is an ordinary `ok:false` (cand-O debt 3). The packrat arm-state is
     readable and **resettable** — the measured one-way latch (`:678` false, `:722` true, read at
     `:682`/`:714`, no assignment back to false anywhere in the bundle) must not exist in the
     candidate, and `resetPackrat()` must actually disarm rather than only clearing the memo store.
  Also folded here, both candidates having converged on them independently: the
  `(p * 255) / 100` exactness discipline (cand-F's comment — "2.55 is inexact in binary; 100% must
  be exactly 255") and the non-string guard's placement above the grammar.
- **Files**: fresh root `src/css/entry.ts`, `src/css/bounds.ts`, `test/css-recovery/boundary/**`.
- **Sub-gate**: G-3, G-9.

### X.P.W3.d The differential floor, graduated

- **Goal**: the equivalence arm that was GREEN at pilot scale is still GREEN at full scale, and
  every place the candidate deliberately differs from the incumbent is written down before anyone
  can be surprised by it.
- **Mechanism**: graduate the P-1 harness from `{oklch(), cubic-bezier(), qualified stylesheets}`
  to all 52 exports. The oracle is the **vendored, sha-pinned published 4.0.0 tarball** — never
  `dist/` in the working tree (cand-O's dist-drift finding; the repo's own
  `dist/subpaths/css.js` differs from what 4.0.0 ships, which is why a source-resolved import
  cannot witness the defect). A **MIRROR-DEFECT** keeps its P-1 definition: (A) DIVERGENT_VALUE,
  (B) MIS_ACCEPT, (C) FALSE_REJECT_IN_SHAPE. Every remaining difference is a row in
  `DIVERGENCE-LEDGER.md` carrying: input, incumbent result, candidate result, spec citation,
  adjudication, and the **direction of behaviour change for a consumer**. The four preserved
  dissents seed the ledger (token juxtaposition — widens acceptance, owner may overrule toward
  cand-F's stricter line; non-finite numerals — three-way split, GROUND-C ruling owed; try/catch
  posture; bench epistemics). R1–R5 from `GATE-VERDICT.md` F-2 are held as **spec-correct
  regression fixtures** — the mirror preserves spec-correctness, never bug-compatibility.
- **Files**: `test/css-equivalence/**`, `docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md`.
- **Sub-gate**: G-7.

### X.P.W3.e Closure adjudication and the divergence apotheosis

- **Goal**: one signed reading of whether the union is genuinely closed and whether each declared
  divergence is a defensible reading of the specification rather than a convenient one.
- **Mechanism**: a **fresh** Fable adjudicator, obligated under L-14 to attempt refutation — of
  the closure proof (find an input reaching no declared code), of at least one divergence row's
  spec reading, and of the claim that the shield is non-load-bearing. Not an averaging pass. It
  records a named refutation attempt per claim, with its outcome, and writes the ten gates'
  RED-before / GREEN-after outputs into `W3-CLOSE.md`. At close it also runs the lane's evidence
  chain: `node docs/tranches/V/megatranche/workflows/harvest-journals.mjs`, files `x-p-w3.json`, and
  carves the `COHESION.md` §5 status-board line.
- **Files**: `docs/tranches/X/parse-that/waves/W3-CLOSE.md`, `DIVERGENCE-LEDGER.md` §Adjudication,
  `docs/tranches/V/megatranche/registry/harvest/x-p-w3.json` (create),
  `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` (modify-append),
  `docs/tranches/X/COHESION.md` (modify-carve).
- **Sub-gate**: the close report exists with all ten gates' pasted outputs; at least one
  load-bearing claim carries a recorded refutation attempt; **no gate is reported green on a
  narrative**; and `node docs/tranches/V/megatranche/workflows/harvest-journals.mjs` exits 0 with
  `docs/tranches/V/megatranche/registry/harvest/x-p-w3.json` present, its seat list non-empty and
  its **count equal to the units dispatched** (five: `.a`–`.e`) — a harvest reporting fewer seats
  than were dispatched, or any seat with zero journal rows, is RED, not a passing summary (L-13).

## 6. Hard Gate

Ten conditions. **Nine are born-RED with the failing probe measured read-only on 2026-08-03**
against value.js HEAD `c654824e`+working-tree; one is MEASURE-AT-OPEN with its exact command,
because its subject does not exist until X.P.W2 lands. Each states how it can fail for its
intended reason (L-19).

**G-1 — UNIVERSE-52-TOTAL.** All 52 frozen `/css` exports verdict **TOTAL** in the generated
matrix: 19 runtime by name **and** shape, 33 types by bidirectional assignability.
*Command*: `node scripts/css-universe.mjs --check --pinned-value-commit <sha>` (emits the matrix
and exits non-zero while any row is not TOTAL) followed by `npx tsc --noEmit -p
test/css-totality/tsconfig.assignability.json` (the 33 type rows; bidirectional assignment in one
compile).
*RED baseline*, measured today:

```
$ node -e "const m=await import('./dist/subpaths/css.js'); console.log(Object.keys(m).length)"
19
$ (export type { … } block in src/css/index.ts)
33
   → universe = 52
```

Assay coverage at the 07-20 census (`coverage.md` Surface 1): runtime **0 TOTAL / 3 PARTIAL / 16
ABSENT**; types **0 TOTAL / 0 PARTIAL / 33 ABSENT**. **0 of 52 TOTAL.**
*Falsifier*: a row marked TOTAL whose assertion does not execute, or whose accept/reject corpus is
empty, fails. Counting a PARTIAL as TOTAL is the specific dishonesty this gate exists to prevent —
the legend is asserted mechanically, not applied by judgement. Deleting any export from the
manifest without a corresponding frozen-surface change also fails (the manifest is generated from
`index.ts`, so it cannot drift silently).

**G-2 — R1 ANCHOR PROBE, ZERO THROWS.** `docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs`,
**unmodified**, run against the packed candidate: exit 0.
*RED baseline*, re-run by this seat 2026-08-03 (the probe packs the repo and installs the tarball
into a temp dir — it witnesses shipped bytes, not source resolution):

```
$ node docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs
RED  parseCssColor             102/172 throw
RED  parseCssScalar            102/172 throw
RED  parseCssValue              60/172 throw
RED  parseCssValues             60/172 throw
ok   parseKeyframeSelector       0/172 throw
ok   parseStylesheet             0/172 throw
ok   parseTimingFunction         0/172 throw
ok   parseAnimationTimeline      0/172 throw
ok   parseAnimationRange         0/172 throw

TOTAL 324 throws / 1548 calls
DISTINCT FAILURE MODES: 1
  324x  TypeError: Cannot read properties of undefined (reading 'replace')
```

**4 of 9 entry points RED; 324/1548; one failure mode.** *Falsifier*: the probe targets all nine
public parsers, so a candidate that cures `parseCssColor` alone stays red — which is the point;
this gate cannot be discharged by a slice. Modifying the probe voids the gate.

**G-3 — TYPED RECOVERY AT THE BOUNDARY.** Every degenerate function head and every non-string
argument returns `ok:false` with a frozen code; zero throws; zero `undefined` returns.
*RED baseline*, measured today against the working-tree dist:

```
$ node --input-type=module -e "const m=await import('./dist/subpaths/css.js');
  for (const h of ['rgb','rgba','hsl','hsla','lab','lch','oklab','oklch','hwb','color'])
    try { m.parseCssColor(h+'()') } catch(e){ console.log('THREW', h+'()', e.constructor.name) }"
THREW rgb()   TypeError      THREW rgba()  TypeError      THREW hsl()   TypeError
THREW hsla()  TypeError      THREW lab()   TypeError      THREW lch()   TypeError
THREW oklab() TypeError      THREW oklch() TypeError      THREW hwb()   TypeError
THREW color() TypeError
empty-body throws: 10/10
```

Boundary leg, inherited from O-15 **PT-07**: 5/5 non-string inputs throw a raw `TypeError` in
parse-that, and `.parse()` returns `undefined` on failure — indistinguishable from a successful
`undefined`. *Falsifier*: returning `ok:false` with an **empty** diagnostics array passes a naive
"didn't throw" check and fails this gate, because the frozen type demands a non-empty tuple.
Catching the throw at the call site and re-shaping it is explicitly not a cure — a masking
fallback fails the gate by inspection of the entry module.

**G-4 — UNION CLOSED, NO FALLBACK.** Emitted code set ⊆ frozen 8-code set (⊆ direction), **and**
every frozen code is emitted by at least one corpus input (⊇ direction), **and** zero `default:`
arms in code selection.
*Command*: `node scripts/css-recovery-closure.mjs --corpus test/css-totality/corpus.json
--frozen-union <pinned-value-commit>:src/css/types.ts` — prints both set-differences and the
fallback-arm inspection; exits non-zero on any non-empty difference or any fallback arm.
*RED baseline*: no candidate exists in the fresh root; the incumbent's own closure is unproven and
its nearest relative in the corpus is the live OR05 defect — **18 declared predicate IDs against a
20-formula domain, the registry binding only the first 18, so the last declared IDs are misbound**
(handoff §5.1–5.2). That is the exact disease this gate immunizes against, transposed from
predicates to diagnostic codes. *Falsifier*: an input reaching a fallback arm; a frozen code no
corpus input can produce (dead code in the contract is as much a defect as an undeclared one); a
ninth code appearing anywhere. This is **not a grep gate** — the ⊇ direction requires executing the
corpus and collecting emitted codes.

**G-5 — DUAL-TARGET RECOVERY IDENTITY. — MEASURE-AT-OPEN.**
*Command*: `node scripts/css-dual-target-identity.mjs --js dist/css.js --wasm dist/css.wasm
--corpus test/css-totality/corpus.json` — for every corpus input, the JS target's and the Wasm
target's `{ok, code, start, end, expected, actual}` must be **byte-identical** after JSON
canonicalization.
*Why MEASURE-AT-OPEN and not inferred*: **no Wasm artifact exists**. Verified 2026-08-03:
`find /Users/mkbabb/Programming/parse-that /Users/mkbabb/Programming/value.js -name '*.wasm'
-not -path '*/node_modules/*'` returns only two unrelated Chrome-profile TTS binaries inside a
`.claude/worktrees` capture directory — **zero project artifacts**. The subject lands at X.P.W2;
an honest baseline is impossible before then and inventing one would be the defect this law names.
*Falsifier*: a single differing byte fails, and a difference in `expected` ordering counts —
canonicalization sorts nothing, because a diagnostic's expectation order is part of its meaning.
This gate is the executable half of the retained hypothesis (handoff §4): "exact equality of
semantic value, byte complement, provenance, diagnostics, rollback, and malformed inverse" across
both targets.

**G-6 — SPEC CONFORMANCE ROWS (the parser-band born-REDs, folded).** All of: `rgba(1, 2, 3, 0.5)`
→ `{rgb,[1,2,3],0.5}` and `hsla(120, 50%, 50%, 0.5)` → `{hsl,[120,0.5,0.5],0.5}`;
`parse("hsl(120 50 50)") ≡ parse("hsl(120 50% 50%)")` bit-for-bit; `rgb(300 -20 3)` → `[255,0,3]`;
`rgb(1 2 3 / 1.5)` → alpha `1`; and `rgb(1,2,3,)`, `rgb(1 2 3 / )`, `rgb(1, 2 3)`,
`hsl(120%, 50%, 50%)`, `lch(50% 50% 50%)`, `hwb(120, 30%, 40%)`, `rgb(1. 2 3)` all reject.
*Command*: `npx vitest run test/css-totality/spec-conformance.test.ts` — one named assertion per
row, so a failure prints which row.
*RED baseline*, each confirmed by direct probe against published 4.0.0 in `parser-band.md`: the
two legacy 4-arg forms **REJECT** (the most-deployed colour syntax on the web fails); the two
spelling forms disagree **100×** (`[120,50,50]` vs `[120,0.5,0.5]`); `rgb(300 -20 3)` returns
`[300,−20,3]` unclamped; `rgb(1 2 3 / 1.5)` rejects where §4.2 says clamp; all seven unsound
inputs **ACCEPT**. *Falsifier*: each row is an independent assertion — a candidate that fixes the
clamps and regresses the legacy forms fails, and the gate names which row. `rgb(1, 2 3)` is the
row found by the *bench* honesty gate, not by the suite: the comma→space rewrite makes separators
invisible, so a candidate that reproduces the rewrite passes every value check and still fails
here.

**G-7 — EQUIVALENCE FLOOR HELD AT ZERO, FULL SURFACE.** Zero MIRROR-DEFECTs (DIVERGENT_VALUE /
MIS_ACCEPT / FALSE_REJECT_IN_SHAPE) across all 52 exports against the vendored sha-pinned 4.0.0
tarball, **and** every non-defect difference present as a `DIVERGENCE-LEDGER.md` row asserted in
both directions.
*Command*: `npx vitest run test/css-equivalence/` (the graduated harness; the vendored tarball's
sha256 is asserted in-test before any comparison runs, so an unpinned oracle fails first) and
`node scripts/css-universe.mjs --cross-check-ledger docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md`.
*RED baseline*: GREEN at pilot scale only — 0 mirror-defects over the **403-string** P-1 corpus,
whose comparability boundary is the intersection of the frozen surface with C14's declared W0
shape, i.e. `{oklch() colours, cubic-bezier() easings, qualified stylesheets}`. Against the full
52-export surface the harness has **never been run**: 49 of 52 exports had no assay peer at the
census. The floor is therefore inherited as a floor, not as a pass. Supporting evidence the
graduation must not lose: the cand-F/cand-O differential (86 structured inputs → 79 agree / 7
disagree, all seven adjudicated; **30,000 seeded fuzz inputs → 0 acceptance disagreements**).
*Falsifier*: one mirror-defect reddens it, and so does an **unrowed** intentional difference —
the gate treats "we meant to do that" without a ledger row as identical to a defect. A row whose
"direction of behaviour change for a consumer" field is empty fails; that field is what the KF and
glass packets quote.

**G-8 — LABELLED DIAGNOSTICS, UNARMED, SILENT.** Every rejection carries a human-readable
`expected` label (cand-F's idiom: `"<named-color>"`, `"<hex-color> (3, 4, 6 or 8 digits)"`), the
labels are available with diagnostics **UNARMED**, and the parse path emits **zero** `console.*`
calls.
*Command*: `npx vitest run test/css-recovery/labels.test.ts` (every corpus rejection's
`expected[0]` is a named production, asserted with the diagnostics module never armed) and
`node scripts/css-recovery-closure.mjs --assert-no-console` (walks the built graph's reachable
module set from the public entries; a `console.*` call site on that set fails).
*RED baseline*, O-15 **PT-01**, measured against the published parse-that dist: `label` is a
**no-op unless diagnostics are armed**, and arming couples an unconditional `console.error`
(`dist/diagnostics-DDazRHgl.js:14` + `dist/packrat-entry-*.js:881`) — **the two are reachable only
together**. The incumbent value.js side is worse in kind: cand-O's `never` arm yields an opaque
`(?!)` expectation where cand-F yields a name (`parser-band.md`, debt 1 — "the single clearest
thing cand-F does better"). *Falsifier*: a candidate that ships labels by arming diagnostics fails
on the `console.*` leg; a candidate that silences the logger by patching parse-that's dist fails
the fresh-root/read-only bounds. The cure is a label surface that never depended on arming.

**G-9 — BOUNDED BY CONSTRUCTION: DEPTH AND LATCH.** Depth exhaustion returns `ok:false` with a
frozen code (never `RangeError`), and the packrat arm-state is symmetric — `resetPackrat()`
disarms.
*Command*: `npx vitest run test/css-recovery/boundary/depth.test.ts test/css-recovery/boundary/latch.test.ts`
— the depth suite generates an input at `bound` and at `bound + 1` and asserts `ok:false` for both
with no thrown error; the latch suite arms, measures, resets, and asserts the arm-state readback
returns to false within one process.
*RED baseline*, O-15 **PT-03** / **PT-04**, measured: `Parser.lazy` arity 1, deepest OK **7,761**,
**thrown `RangeError` at 7,762**; `PACKRAT_ARMED` is a **one-way latch** (`:678` false, `:722`
true, read at `:682`/`:714`, **no assignment back to false anywhere in the bundle**) — UNARMED
**93.9 ns/parse** → ARMED **138.2** = **1.47×**, and `resetPackrat()` leaves it at **139.3**, i.e.
it clears the memo store and does not disarm. *Falsifier*: generate an input one level past the
declared bound; a `RangeError` fails, and so does a silent truncation that returns `ok:true` on a
partially consumed input (that is a MIS_ACCEPT and reddens G-7 as well). The latch leg fails if
the arm-state cannot be observed and restored within one process.

**G-10 — BENCH TABLE, THREE LEGS, BAR OWNER-GATED.** A published table with legs
*shared-accepted* / *reject* / *R1-class*, interleaved cells, median-of-rounds, printed sink,
published 4.0.0 as baseline, and **the packrat arm-state printed on every row**. Every 3×/2×
budget restates against the conservative reconstruction **1,636,680 µs** (M-22 ¶4).
*Command*: `node scripts/css-bench-three-leg.mjs --baseline-tarball <sha-pinned 4.0.0>
--rounds 40 --discard 10 --denominator 1636680` — prints the three legs, the arm-state per row,
the restated budgets, and the literal string `BAR: OWNER-GATED-PENDING-RATIFICATION`.
*RED baseline*: no candidate table exists. The measured inputs this gate inherits, and must not
contradict silently — `parser-band.md` cross-bench, two runs: shared-accepted published
**1500 / 1553 ns**, cand-F **1261 / 1289** (×1.19–1.20), cand-O drop-in **1466 / 1541** (×1.01–1.02),
cand-O node **1247 / 1290**; reject published **716 / 693**, cand-F **801 / 810** (×0.86–0.89),
cand-O **1121 / 1088** (×0.64); R1 class published+catch **82,559 / 78,034 ns** vs cand-F
**632 / 630**, cand-O **937 / 923** (~100× slower even when the caller catches, and callers that
don't catch crash).
**THE BAR IS OWNER-GATED-PENDING-RATIFICATION.** The `≥10x` floor is **RETIRED AS LAW**: against
the citable denominator the 10× budget is **163,668 µs** while the fixed native floor is
**311,883 µs** — the floor exceeds the budget by **1.906×**, so 10× is arithmetically impossible
and the conclusion *strengthens* under the conservative restatement. Strict-3× = **545,560 µs**
(headroom **233,677 µs**); strict-2× = **818,340 µs** (headroom **506,457 µs**); both families are
**UNRATIFIED (0/5, 0/5)**, as is break-even (0/5). **UNCITABLE**: the `1,870,633 µs` denominator,
its derived 10× budget `187,063 µs`, and the `311,661` / `623,434` headrooms — all frozen until
`P4-EVIDENCE-REPLAY.json` is readable under a TCC grant.
*Falsifier*: this gate **reports and cannot fail on a bar** — it fails if a ratio is published
outside the three legs, if any budget cites the uncitable denominator, if a row omits its
arm-state, or if the table asserts a pass/fail verdict the owner has not ratified. It also fails
if it silently reconciles with the 07-20 gate's "LIVE regex measured FASTEST ~1.8×": that reading
is now contradicted by three independent measurements (two candidates and an arbiter, three
methods), and the contradiction is a row in the table, not an erasure. **Inventing a bar is a
defect.**

## 7. Format And Lint Cadence

In the fresh root: `npx tsc --noEmit`, `npx eslint .`, and `npx vitest run` after each phase-1
integration batch and before close; `cargo fmt --check` + `cargo clippy -- -D warnings` for the
Wasm half at the same cadence; `git diff --check` on every commit. In this repo the wave is
docs-only — Prettier over the touched `.md`, plus `git diff --check`, plus G-1's manifest
resolution as the replacement evidence for the absent document linter. **No proof-farm script is
authored for any gate** (L-19): G-1/G-4/G-5/G-10 are executable programs whose output is the
evidence, and G-2 re-runs an existing probe unmodified. The retired `proof:*` grep idiom is not
resurrected.

## 8. Verification Artefacts

- `docs/tranches/X/parse-that/evidence/W3/universe-52.json` — the generated matrix, every row with
  its verdict and its assertion path; sha256 sidecar.
- `docs/tranches/X/parse-that/evidence/W3/r1-anchor-{before,after}.txt` — the unmodified anchor
  probe's output at open and at close.
- `docs/tranches/X/parse-that/evidence/W3/recovery-closure.json` — emitted-vs-frozen code sets,
  both set-differences, and the no-fallback inspection result.
- `docs/tranches/X/parse-that/evidence/W3/dual-target-identity.json` — per-input JS/Wasm
  comparison, with the count of differing bytes (target: 0).
- `docs/tranches/X/parse-that/evidence/W3/equivalence-full-surface.json` — 52-export differential
  results against the vendored tarball, with the tarball's sha256 recorded inline.
- `docs/tranches/X/parse-that/evidence/W3/bench-three-leg.md` — the table, arm-state per row,
  budgets restated against 1,636,680 µs, BAR marked `OWNER-GATED-PENDING-RATIFICATION`.
- `docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md` — every declared divergence with its
  adjudication and consumer-direction field.
- `docs/tranches/X/parse-that/waves/W3-CLOSE.md` — the ten gates, RED-before / GREEN-after, pasted.
- `docs/tranches/V/megatranche/registry/harvest/x-p-w3.json` — the L-13 harvest, seat count equal to
  the units dispatched, with the `DEFECT-LEDGER.md` append it produced and the carved `COHESION.md`
  §5 status-board line cited beside it.
- Commit hashes for each phase, in both roots, with the fresh root's hashes recorded here (this
  repo cannot contain the fresh root's history).

## 9. Commit Plan

Fresh root (`parse-that-css-totality-p2`):

- `feat(x-p-w3/universe): generate the 52-export conformance manifest and its corpora` (`.a`; body
  pastes the born-RED 0-of-52 tally and the folded corpus provenance).
- `feat(x-p-w3/recovery): closed 8-code union, no fallback arm` (`.b`; body pastes both
  set-differences).
- `fix(x-p-w3/boundary): kill the R1 throw class — non-string guard, depth bound, symmetric latch`
  (`.c`; body pastes the before/after of the 10/10 empty-body class and the 7,761/7,762 depth
  probe).
- `test(x-p-w3/equivalence): graduate the differential harness to the full 52-export surface`
  (`.d`; body records the vendored tarball sha256 and the divergence row count).

This repo (docs-only, pathspec commits):

- `docs(x-p-w3): evidence — universe, closure, identity, equivalence, bench` (orchestrator, after
  each phase integrates).
- `docs(x-p-w3): divergence ledger + closure adjudication` (`.e`; body carries the model receipt
  and at least one recorded refutation attempt).
- `docs(x-p-w3/chain): harvest x-p-w3.json + DEFECT-LEDGER append + COHESION §5 carve` (`.e`, at
  close; body pastes the harvester's exit code and the harvested seat count against the five units
  dispatched).
- `docs(x-p-w3): close report` (orchestrator; this close stamps **IMPLEMENTED** — **VERIFIED is
  X.P.W4's sub-tranche release close to stamp, never this one** (R-A)).

`scripts/dev/dev.sh` is unowned and never touched. No commit in this wave may contain a path under
`/Users/mkbabb/Programming/value.js/src/**`.

## 10. Dependencies

- **Depends on**: X.P.W0 (pause authentication + fresh root, §2b OP-2/OP-3), X.P.W1 (the algebra
  and its bijection, OP-4), X.P.W2 (both targets built — G-5 has no subject without the Wasm one);
  the owner's begin-word (OP-1).
- **Blocks**: **X.P.W4** — the seam packet quotes this wave's universe matrix, divergence ledger,
  and bench table verbatim, and the release condition RC-P names G-1/G-2/G-7/G-10 as three of its
  six conjuncts. X.P.W4 cannot be authored around a missing W3.
- **Cross-repo edges (declared from this end; each is reciprocated in the named file — the X·V
  W4-D2 lesson, now law)**:
  - → **X·V**: this wave writes **zero** bytes under value.js `src/**`. It consumes `src/css/index.ts`
    and `src/css/types.ts` as a **frozen contract read at a pinned commit**, and the published
    4.0.0 tarball as the differential oracle. X·V's `X-W9` §5.g already records the reciprocal
    ("This wave does not adopt a parser; it records why value.js ships none of parse-that today"),
    and its G31 falsifier — *"adopt a parser inside this wave"* — is the other end of this edge.
  - → **X·KF**: nothing here. KF.W3's gate keys on X.P.W4's release condition, never on this
    wave's greens (COHESION.md §2: gate-keyed, never scheduled).
  - → **X·F**: nothing here, and nothing ever directly. Direct `parse-that → fourier` is
    **FORBIDDEN** (standing routing law; handoff §2).
- **Not opened here**: OC-1's bar (OP-5), the GROUND-C `±Infinity` contract ruling, the
  `color-mix()` both-sides gap (verdict "neither", GROUND-A P-028), and the relative-colour arm,
  which stays **recognised-not-validated** until a css-color-5 wave.

## 11. Archaeology

Four prior attempts on this exact ground, each with the guardrail it earned:

1. **The 07-20 proof gate** read flatly and **deadlocked**: totality could only be cured by
   building the mirror out, but the gate preceded the building. *Guardrail*: this wave IS the
   building, and it inherits the equivalence arm as a **floor** rather than re-proving a settled
   claim (§2c). Its own totality gate is a close condition, not a precondition to execution —
   OC-2's staged reading, applied.
2. **The C14 assay's honest self-limit.** It declared itself a W0-only prototype ("there is
   deliberately no barrel"), and the census correctly read 0-of-52. The failure was never the
   assay's; it was reading a pilot as a surface. *Guardrail*: G-1's manifest is **generated from
   `index.ts`**, so the universe cannot be quietly narrowed to what the candidate happens to cover.
3. **The v12 predicate closure — OR05's 18 IDs against 20 formulas.** The registry bound the first
   18 and the last declared IDs were *misbound rather than absent*, which is worse: the packet
   looked closed. *Guardrail*: G-4 asserts **both** set-differences and forbids fallback arms;
   OP-4 refuses to open until W1's bijection is proven by the same discipline.
4. **The retired `proof:*` grep idiom** (owner judgement, 2026-06-02: "overfit junk"). *Guardrail*:
   §7 names it and refuses it; every gate here executes a corpus or compiles a program, and G-4
   explicitly is **not** a grep gate.

## 12. L-18 rider — acceptance is not close

Landing the ten gates green makes this wave **IMPLEMENTED**, not ACCEPTED. Per **L-18**, no
implemented wave is ACCEPTED until it has survived **two** challenging gestalt passes, each a
**quartet of Opus 5 skeptics** working the three altitudes (total-tranche: was this wave optimal in
the greater plan, was the spec adhered to, what friction arose · wave · feature), each assuming the
implementation is wrong on some reasonable basis. The bases that apply directly here: a corpus that
proves totality against inputs the candidate's own author chose; a divergence ledger that
rationalizes rather than adjudicates; a "closed" union closed only by the corpus that happens to
exist; a dual-target identity proven on a corpus too small to reach the numeric edges. Superlatives
are reported alongside defects, each with provenance and proof. Both quartets are then adjudicated
and agglomerated by a **fresh Fable instance**; any incongruity surfaced is fully addressed,
ratified, and dispatched — never parked.

**Four-verb status**: this file makes X.P.W3 **SPECIFIED**. It is not AUDITED beyond the baselines
pasted in §6, not IMPLEMENTED, and not VERIFIED. Gates-green at this wave's own close stamps
**IMPLEMENTED**; **VERIFIED is stamped only at X.P.W4's sub-tranche release close** (R-A).

---

## ADDENDUM 2026-09-18 (dated, beside — E-3; COHESION §0p) — two units added to §5 after the NOT-CONFORMANT close

**Why.** `.e`'s L-14 pass measured the retained shield LOAD-BEARING on the Wasm lowering (ESC-e1) and two spec-conformance MIS_ACCEPTs (F-e1/F-e2). §3a's route for a Wasm-memory-model divergence (X.P.W1/W2) is closed; COHESION §0p is the triumvirate's redress. §4 is unchanged — `typescript/src/css/**` is already create/modify for this wave; `test/css-recovery/boundary/**` is `.c`'s named test home and is shared serially.

### X.P.W3.f The capacity bounds — every fixed region of the Wasm memory model declared, carried by both lowerings, rejected as an ordinary `ok:false`

- **Goal**: no valid input can reach a `throw` in either lowering, and the two targets answer identically at every declared bound; the shield measures non-load-bearing on the capacity witnesses (`SHIELD.caught` 0).
- **Mechanism** (COHESION §0p, the depth bound's idiom in `bounds.mjs`): (1) `THETA` gains one declared capacity per fixed region — input window, mark journal, value stack, arena, the C/D/P journals guarded by `W_OVF` — each VALUE read from the build's layout constants, never hand-typed, never re-sized here; (2) both lowerings reject at the bound on the parse path with `css_syntax` and a label naming it (`marks <= 32768`), the JS lowering measuring the same quantity against the same value; (3) `witnessAtCapacity(region)` generators and `assertCapacityBound` read-backs off both lowerings and the label surface; (4) growable regions and a ninth code are REFUSED; (5) the witnesses join G-5's corpus as a boundary band; G-9 gains a capacity leg.
- **Files**: `typescript/src/css/bounds.mjs`, `entry.mjs`, `lowering-js/index.mjs`, `lowering-wasm/index.mjs` (+ the layout-constants module both import), `test/css-recovery/boundary/**`.
- **Sub-gate**: G-3 (shield non-load-bearing on the witnesses), G-5 (re-run, identity on the boundary band), G-9 (capacity leg). **Seat: Fable** (M-12 — the Wasm memory model is architecture).

### X.P.W3.g The dimension-token boundary — css-syntax-3 §4.3.3 at juxtaposition

- **Goal**: `hsl(120deg50%50%)`, `rgb(255none none)` and every input whose juxtaposed number+ident run tokenizes as a single invalid `<dimension-token>` are REJECTED by both lowerings with `css_syntax`; legacy `hsl(120, 50, 50)` stays rejected per `<legacy-hsl-syntax>`, recorded as a divergence row against the incumbent (which mis-accepts it).
- **Mechanism**: the juxtaposition width decision (ALGEBRA.md DM-2's row) is realised at the token boundary in `algebra/**` — a numeric token followed without whitespace by an ident-start is one dimension token, not two values; the fixture pair is spec-cited (css-syntax-3 §4.3.3 quoted at the fixture) and generated, not pinned.
- **Files**: `typescript/src/css/algebra/**`, `test/css-recovery/**` (its own fixture file). Runs AFTER `.f` (both re-run G-5).
- **Sub-gate**: G-5 (re-run), G-7 (the new divergence row counted, the mirror-defect total not increased by a candidate MIS_ACCEPT). **Seat: Opus.**

**§Sequencing addendum**: `.e` → `.f` → `.g` → close (VERIFY-ONLY) → check. **§Commit Plan addendum**: one commit per unit, pathspec, `feat(x-p-w3/.f): …` / `fix(x-p-w3/.g): …`, receipts in `execution/D/X-P-W3.md`.

**ADDENDUM 2026-09-18 (second, beside — E-3; COHESION §0q).** `.f`'s **Files** line is widened by grant: `typescript/src/css/algebra/tables.mjs` (capacity labels appended after `"<string>"`, no index moves) · `typescript/src/css/diagnostics.mjs` (promoted rows) · `typescript/src/css/lowering-js/runtime.mjs` + `lowering-js/js-alg.mjs` (a high-water counter on the C/P journals the JS lowering already keeps). `.f`'s **Mechanism** gains: class 2 = peak-with-grant; class 3 (value stack · arena · expsnap) = unreachable-by-construction — Θ declares each from `layout.mjs`, a per-unit ceiling `K` derived from the emitter's node table, `cap₃ ≥ K × bound₁` asserted at load (class-1 bounds lowered in Θ, both lowerings, where the built regions require it); G-5 fold = a capacity-band identity leg in `boundary/capacity.test.ts` beside the unchanged 79,674/0. `.g` unchanged, still after `.f`.

---

## ADDENDUM 2026-09-19 (third, beside — E-3; COHESION §0r) — the unauthored surface: four implementation units

**Why.** `universe-52.json` at the third close: runtime TOTAL 0 · PARTIAL 3 · ABSENT 16; types TOTAL 5 · ABSENT 28. §3 items 2–3 and §2a demand every export TOTAL; §5 assigned no unit to author the six unrealized public entries and their families. §4 unchanged (`typescript/src/css/**` and `typescript/test/**` are this wave's). **Common law for `.h`–`.j`**: author in the 22-op algebra through `buildGrammar(A)` (one source, both lowerings); dispatch rows in `algebra/tables.mjs`; labels appended after `"<string>"` (K-10); promoted rows in `diagnostics.mjs`; the export on `harness-adapter.mjs` and in `build/ac1.d.ts`; corpus band = the universe row's own `accept`/`reject`; frozen codes only; `node build.mjs` reproducible (K-9); receipt = G-1 rows for the unit's exports · G-2 22/22 · G-3 `SHIELD.caught` 0 · G-5 identity · G-9 capacity leg with Θ re-derived and REPORTED per entry. **Serial: `.h` → `.i` → `.j` → `.k`** (shared modify paths).

### X.P.W3.h The value grammar — `parseCssScalar` · `parseCssValue` (syntax vocabulary; `syntax_mismatch`, `syntax_descriptor_invalid`) · `parseCssValues` · `coerceToSyntax` · `serializeCssColor`; `parseCssColor` → TOTAL; types `CssColorSpace` · `CssLinearStop` · `ParseIssue` · `ParseResult`
- **Files**: `typescript/src/css/algebra/**` (a `grammar/value.mjs` module composed in `grammar.mjs` is the preferred shape), `diagnostics.mjs`, `harness-adapter.mjs`, `build.mjs`/`build/**`, `test/css-totality/**` (the unit's own fixture files), `test/css-recovery/**` (own files). **Seat: Fable.**
- **Sub-gate**: G-1 (its rows TOTAL), G-2, G-3, G-4 (its codes emitted), G-5, G-9.

### X.P.W3.i The animation family — `parseAnimationRange` · `parseAnimationTimeline` · `parseKeyframeSelector` (`keyframe_selector_invalid`) · `serializeTimelineOptions` · `collectAnimationOptions` (`animation_option_invalid`) · `collectTimelineOptions`; the fifteen animation/timeline/range/trigger/view types
- **Files**: as `.h` (`grammar/animation.mjs`). **Seat: Opus.** **Sub-gate**: G-1 · G-2 · G-3 · G-4 · G-5 · G-9.

### X.P.W3.j The stylesheet collectors — `collectDeclarations` · `collectStyleRules` · `collectKeyframes` · `collectPropertyDescriptors` · `collectCustomFunctions`; `parseStylesheet` → TOTAL; `CollectedRule` · `KeyframeRule` · `KeyframesBlock` · `PropertyRule` · `StylesheetItem` · `CSSPropertyDescriptor` · `CustomFunctionDescriptor` · `CustomFunctionParameter` · `CustomFunctionRule`
- **Files**: as `.h` (`grammar/stylesheet.mjs`). **Seat: Opus.** **Sub-gate**: G-1 · G-2 · G-3 · G-4 · G-5 · G-9.

### X.P.W3.k Universe closure and the ledger — G-1 52/52 (or the honest remainder by id) · G-4 C-3 8/8 · ESC-g1's sixth emitter family (§6 INCUMBENT-DEFECT; count-driven headings, prose and `equivalence.test.ts` assertions in one commit; the legacy-hsl row counted, census 30) · F-z2 (the mirror-defect denominator's heads read from the ORACLE's pinned surface, never the candidate's `R_disp`) · G-7 re-run
- **Files**: `typescript/scripts/**`, `test/css-totality/**`, `test/css-equivalence/**` (emitter, `lib/**`, `equivalence.test.ts`, `shape.mjs`), the divergence ledger document, `docs/tranches/X/parse-that/evidence/W3/**`. **Seat: Opus.** **Sub-gate**: G-1 · G-4 · G-7.

**§Sequencing addendum**: `.g` → `.h` → `.i` → `.j` → `.k` → close (VERIFY-ONLY) → check. **R-f1 / CAP-1** (Θ.input 65,458) is X.P.W4's, shape ruled at COHESION §0r; W3 closes with the derived Θ rowed.

**ADDENDUM 2026-09-19 (fourth, beside — E-3; COHESION §0s).** `.h`, `.i` and `.j` each gain, **for CTOR rows only**: `typescript/src/css/lowering-js/js-alg.mjs` (`CTORS`), `typescript/src/css/lowering-wasm/wasm-alg.mjs` (`emitCtors`, the colour-space list included) and `typescript/src/css/bounds.mjs` (`CTOR_ALLOC` / `CTOR_SCRATCH_CELLS`); the four name-sets stay equal and asserted at load; `layout.mjs` and every CAP untouched. `.h` additionally gains `typescript/src/css/entry.mjs` for the surface compositions of `coerceToSyntax(source, syntax)` and `parseCssValue`'s syntax arm (`syntax_descriptor_invalid` spanning the source; `syntax_mismatch` with the descriptor's own alternatives as `expected`, through `selectCode`). `.k`'s Mechanism gains: `test/css-totality/lib/adjudications.mjs` from literal overrides to **class predicates** (one per ruling id, each with a printed census asserted `≤` the ruling's measured population), and `scripts/css-recovery-closure.mjs` exercising the two-argument entry so C-3 counts `syntax_*`. Until `.k` lands, `.h`–`.j` report raw misses AND misses-in-adjudicated-classes per row, never TOTAL-by-assertion. Order unchanged: `.h` → `.i` → `.j` → `.k`.

---

## ADDENDUM 2026-09-19 (fifth, beside — E-3; COHESION §0v) — the at-rule family, the seals, IMPLEMENTED

### X.P.W3.l The at-rule and nesting families (E-j1) · `splitSelectors` (E-j2) · F-k2 / F-k3 — Fable seat
- **Goal**: `parseStylesheet` TOTAL over `@keyframes` · `@property` · `@function` · `@scope` · `@starting-style` · `@scroll-timeline` · `@view-timeline` · unknown at-rules (± body) · nested style bodies; `collectKeyframes` · `collectPropertyDescriptors` · `collectCustomFunctions` no longer dead; the stylesheet corpus band witnesses all ten shapes (generated from the ORACLE's accept set).
- **Mechanism**: the grammar answers the raw item tree (`sheetOver`); `entry.mjs` completes and validates (E-h3 surface compositions: `isSupportedSyntaxDescriptor`/`coerceToSyntax`, `parseTimingFunction(serializeCssValue(…))`, `parseDeclarations`' checks); new CTOR rows under the §0s quartet; `splitSelectors` drops empty parts in both lowerings (`lowering-js/values.mjs`, `lowering-wasm/runtime.mjs` GRANTED); the forbidden legacy comma forms (css-color-4 §8.1) rejected; a comment inside a declaration block is never a name. GROUND-C per COHESION §0v: overflow is not a syntax error; range per production.
- **Files**: `typescript/src/css/**` (all four CTOR realizations; `entry.mjs`; `lowering-js/values.mjs`; `lowering-wasm/runtime.mjs`), `test/css-totality/**` (own fixture/band files), `test/css-recovery/**` (own files). **Sub-gate**: G-1 (parseStylesheet + the five colour/value rows re-read), G-2, G-3, G-5, G-9 (Θ re-derived and reported).

### X.P.W3.m The seals and the stamp — Opus seat, after `.l`
- **Acts**: re-emit `evidence/W3/universe-52.json` and `evidence/W3/equivalence-full-surface.json` at the final HEAD by their own generators (double-run, digests recorded); bank `evidence/W3/r1-anchor-after.txt` over the CANDIDATE's nine entries (`--at typescript/src/css`; the value.js-root form measures the incumbent and is not this bank); re-run G-1 · G-4 · G-7 · G-9; stamp W3 **IMPLEMENTED-with-carried-REDs**, every carried cell attributed to a ruling id (GROUND-C · ID-1/ID-2 · R-f1 · E-k2), in `W3.md`'s four-verb table by dated addendum and in the LEDGER row.
- **Files**: `docs/tranches/X/parse-that/evidence/W3/**`, `typescript/scripts/**` (the `--at` anchor form only), `execution/D/X-P-W3.md`, `execution/LEDGER.md`, this file (the stamp addendum). **Sub-gate**: OP-2's three artefacts at their final values.

**§Sequencing addendum**: `.k` → `.l` → `.m` → close (VERIFY-ONLY) → check.
