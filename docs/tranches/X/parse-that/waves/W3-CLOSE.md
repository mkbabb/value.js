SERVED MODEL: claude-fable-5-1

# X.P.W3 — CLOSE REPORT (the `.e` adjudication seat, 2026-09-18; the orchestrator stamps at close)

`W3.md` §4: _"`docs/tranches/X/parse-that/waves/W3-CLOSE.md` — create (ten gates, RED-before /
GREEN-after, pasted)"_; §5 `.e`: _"writes the ten gates' RED-before / GREEN-after outputs into
`W3-CLOSE.md`"_; §8: _"the ten gates, RED-before / GREEN-after, pasted"_. Written by the **fresh Fable
adjudicator** `.e` (M-23 §1) at its own clock, **2026-09-18 14:29–14:40 EDT**, against the settled
bytes of `<p2>` = `/Users/mkbabb/Programming/parse-that-css-totality-p2` at **`dc52ed5`** (`.d`'s last
commit; `git status --porcelain` → `?? .worktrees/` alone, before and after every run here) and
value.js pin **`6aca8602`** (`.a`'s, adopted by `.d`, adopted here unchanged). Every reading below was
**run twice** by this seat; where a run is timing-free the two outputs were byte-identical, and where
a program writes a JSON artefact the regenerated artefact was compared by sha256 against the
committed one. **No gate is reported green on a narrative.** The sitting's date of record stays
2026-09-17 (the begin-word's); the wall clock is the receipt.

**One sentence of verdict.** All ten gates re-read at this clock: **GREEN** G-6 · G-8 · G-5 (on its
command) · G-10 (well-formed, no verdict — by law); **GREEN on their commands but with a load-bearing
claim REFUTED under L-14** G-3 (the shield) and G-9's depth leg beside its RED latch leg; **RED, each
with a named owner** G-1 (5 of 52) · G-2 (324/1,548, structural) · G-4 (C-3 · C-4) · G-7 (5,890) ·
G-9 latch (ESC-c1). The **L-13 harvest sub-gate reads 5 of 6 — RED under its letter** (§5). This seat
stamps **no verb**: gates-green is the condition for IMPLEMENTED and the gates are not green;
VERIFIED is X.P.W4's alone (R-A).

---

## 0. Crash-recovery — the inherited DRAFT, read whole, and what was kept

The host restart of 2026-09-18 killed a first `.e` seat mid-flight (`X-P-W3.md` §R2.2, **F-r2.1**).
It left four uncommitted artefacts, every one inside this unit's writable set, and this seat read
each **whole** before writing a byte: `COHESION.md` (+36, the §5 line), `DIVERGENCE-LEDGER.md` (+37,
§6.1), `DEFECT-LEDGER.md` (+405/−1) and the untracked `registry/harvest/x-p-w3.json` (123,158 B). This
file, which both drafts cited by path, **did not exist** (⟨cmd⟩ `find …/value.js …/parse-that-css-totality-p2
-name '*W3-CLOSE*'` → nothing). The draft was treated as a **draft, not a result**: every number in it
was re-measured at this seat's clock and the draft's text was **rewritten** where the measurement
disagreed. The disagreements, so the correction is visible rather than silent:

| draft claim | measured at this seat | disposition |
|---|---|---|
| the Wasm overflow is _"the fixed `C` journal (`C_CAP` 65,536)"_ at _"8,192 rules"_ | the overflowing region is the **mark journal** (`MARK_CAP` **32,768**): 8,190 rules → 32,766 marks (`C` 24,570); **8,191 rules → 32,770 marks**, the first throw. Bisected at the RAW path, last OK **8,190** (98,280 B), first THROW **8,191** (98,292 B); the draft's byte count was right and its region and rule count were not | **rewritten** (§3 R-iii) |
| the JS lowering _"dies of V8 heap exhaustion … on an 11 MB and a 13 MB stylesheet and takes 2,749 MB for a 5 MB one"_ | **not reproduced**: 5 MB → `ok:true`, max RSS **1,259 MB**; 13 MB → `ok:true`, max RSS **2,963 MB** (heap limit 4,192 MB, node v26.0.0; `/usr/bin/time -l`); **24 MB → `FATAL ERROR: … JavaScript heap out of memory`, exit 134** | **rewritten** with this seat's own sizes (F-e6) |
| _"84/84 non-string cells"_ · _"28 shield-fired cells, all Wasm"_ · _"18,982-source band"_ | this seat's own bands: **72** non-string cells (12 kinds × 3 entries × 2 lowerings), **6** shield-fired cells in the refutation script (all Wasm), closure band **2 × 20,000** sources | replaced by this seat's figures |
| the harvester writes _"141 files (90 NEW + 2 CHANGED)"_; X.P.W3 _"ran as TWO workflow runs"_ | **143** files per run, **94 NEW + 2 CHANGED** against the tracked set; the wave has run as **THREE** workflow runs (the second resume that dispatched this seat is `wf_a9980aef-425`) | **rewritten**; the fold is schema `fold/2`, regenerated by program (§5) |
| `DEFECT-LEDGER.md` bytes (sha256 `8092610b…`) | the unmodified harvester, run by this seat, wrote **the same bytes** — and in run B wrote them **itself** onto the repo path | **kept** — they are the script's, not the draft's (§5) |
| the spec sentences quoted for PB-03 · S-1/ADJ-2 · ADJ-3/S-2, and the measured inputs | every quoted sentence found **verbatim** in the drafts fetched at this clock; every measured input **reproduces** on the oracle and both lowerings (§3 R-ii) | **kept in substance, re-worded at the bytes**; the _"§10.9 citation does not exist"_ line is corrected to what is true: §10.9 exists and is _Type Checking_; the sentence the row attributes to it appears nowhere in the draft |

Inherited paths, named as the law requires: `docs/tranches/X/COHESION.md` ·
`docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md` · `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md`
· `docs/tranches/V/megatranche/registry/harvest/x-p-w3.json`. Not inherited and never touched:
`docs/tranches/V/coordination/INBOX.md`, `docs/tranches/V/reformation/CARRY-LEDGER.md`,
`docs/tranches/X/execution/{A,C}/*.md`, the `demo/**` / `e2e/**` rows (sibling tracks), and
`scripts/dev/dev.sh` (unowned, never staged).

---

## 1. The ten gates — RED-before (the open's §Baseline, read-only, 2026-09-18 00:1x) → this seat's reading

The BEFORE column is the wave record's `§Baseline` (`X-P-W3.md` L97–170), taken read-only at the open
before any cure and **not re-taken** by the two resumes (R.3 / R2.3: re-baselining a cured tree would
measure the cures, not the disease). The AFTER column is **this seat's own run**, at `<p2>` `dc52ed5`.

| gate | BEFORE (open, read-only) | AFTER (this seat, 2026-09-18 14:29–14:33 EDT, ×2) | verdict |
|---|---|---|---|
| **G-1** UNIVERSE-52-TOTAL | generator ABSENT; census **0 of 52 TOTAL** | `--check` exit **1**: **5 of 52 TOTAL** (runtime 0/3/16 · types 5/0/28); `tsc` exit **2**, **56** diagnostics (54 TS2694 + 2 TS2724), all in the generated assignability program, 0 unattributed; `universe-52.json` regenerated **sha256-equal** to the committed artefact | **RED** — F-a.6 (no implementing seat for 44 absent peers + 3 partial rows) |
| **G-2** R1 ANCHOR, ZERO THROWS | **324 / 1548 · 1 mode · 4 of 9 RED** | probe **unmodified** (sha256 `77678a57…`, last commit `c0078d96`; `git status` on its dir empty before and after): **324 throws / 1548 calls · 1 mode · 4 of 9 RED**, exit 1, **twice, identical** | **RED, structural** — ESC-d2 (the probe packs the incumbent; this wave writes 0 bytes there) |
| **G-3** TYPED RECOVERY AT THE BOUNDARY | **10/10** empty-body heads throw `TypeError`; PT-07 5/5 raw `TypeError` | **0/10** heads throw, both lowerings, all `ok:false css_syntax`; **72** non-string cells (12 kinds × 3 entries × 2 lowerings) → 0 throws · 0 `undefined` · **one** shape `css_syntax [0,0) ["<string source>"] actual:null`; R1 `parseCssColor("oklch()")` → typed rejection at `[6,7)`, identical in both; suites `boundary.test.ts` 77 ✓ · `no-throw.test.ts` 19 ✓; `SHIELD.caught` **0** after all of it | **GREEN on every leg the gate's command runs — but the proof leg _"the shield is non-load-bearing"_ is REFUTED at §3 R-iii on a VALID input beyond the corpus, and G-3's own falsifier (_"a masking fallback fails the gate by inspection of the entry module"_) then fires on the Wasm lowering. NOT reported green.** F-e5 / ESC-e1 |
| **G-4** UNION CLOSED, NO FALLBACK | checker ABSENT | C-1 ⊆ built graph GREEN (288 sites, 0 outside) · C-2 ⊆ executed GREEN (0 of 3,744 issues outside) · **C-3 ⊇ RED (5 of 8 unemitted)** · **C-4 RED (2 inherited dead arms)** · C-5 tuple · C-6 span · C-7 · C-8 · C-9 · C-10 GREEN; six negative controls fire; exit 1; `recovery-closure.json` regenerated **sha256-equal** (`25fa6a4b…`) | **RED** — E-1 · E-2 (and F-e10: the shield is a **third** fallback arm C-4's inspection does not count) |
| **G-5** DUAL-TARGET IDENTITY | comparator ABSENT (the subject exists) | **79,674 cells · six-tuple differing 0 (0 bytes) · full-diagnostics 0 · value 0 · threw 0**; 21/21 boundary cells identical; exit 0; `dual-target-identity.json` regenerated **sha256-equal** (`b68bab94…`) | **GREEN on its command.** Beyond the corpus the two targets **part ways on a VALID input** (§3 R-iii: js `ok:true`, wasm `ok:false`) — the §3a _"direction rooted in the Wasm … memory model"_ trigger, **routed, not patched** (ESC-e1) |
| **G-6** SPEC CONFORMANCE ROWS | suite ABSENT; incumbent readings stand | `spec-conformance.test.ts` **13 ✓** (the twelve named rows a…l + the guard) · `universe.test.ts` 59 ✓ → **72 / 72**, twice | **GREEN** |
| **G-7** EQUIVALENCE FLOOR, FULL SURFACE | harness + ledger ABSENT | oracle sha256 `7f80658c…` + registry sha512 asserted in-test; taxonomy UNMOVED; **26,551 distinct × 3 entries × 2 lowerings**; **MIRROR-DEFECTS 5,890** (spec-undecided 3,982): `parseCssColor` 4,027 (A 1566 / B 1200 / C 1261) · `parseTimingFunction` 170 · `parseStylesheet` 1,693; rows 52 · COMPARED 8 · NO-PEER 44; ledger 29 rows, 0 empty directions; suite **1 failed / 23 passed** (the born-RED gate assertion); `--cross-check-ledger` **GREEN, 16 / 0 not carried**; `equivalence-full-surface.json` regenerated **sha256-equal** (`d03b458c…`) | **RED at 5,890** — ESC-d1 |
| **G-8** LABELS, UNARMED, SILENT | suite ABSENT; PT-01 stands | C-7 **0 of 3,744** unnamed first expectations · C-8 `isDiagnosticsEnabled()` false before and after · C-9 **0 executed writes**, 0 authored call sites, 1 inherited (guarded); `--assert-no-console` **GREEN, exit 0**; `labels.test.ts` 11 ✓ | **GREEN** |
| **G-9** BOUNDED BY CONSTRUCTION | PT-04 cliff 7,761/7,762; PT-03 one-way latch | depth **64 / 65 / 7,761 / 7,762** → `ok:false` in **both** lowerings (64 = the context guard's code, 65+ = `css_syntax "<nesting-depth> (at most 64 levels)"`, `value` absent), 0 thrown; `depth.test.ts` 15 ✓. Latch `{readable:true, before:false, armed:true, afterReset:true, symmetric:false}`; `latch.test.ts` **5 ✓ / 2 ✗** | **depth GREEN · latch RED** — ESC-c1 |
| **G-10** BENCH TABLE, BAR OWNER-GATED | table ABSENT | a **third run** beside `.d`'s two (§2.10): three legs · 9 rows · arm-state on every row · `sink 580440` · budgets restated against **1,636,680 µs** only · `--denominator 1870633` **REFUSED, exit 2** · `BAR: OWNER-GATED-PENDING-RATIFICATION` · `VERDICT: none` · well-formedness OK, exit 0 | **WELL-FORMED, NO VERDICT** (§0j.E OC-1) |

**Tally at this clock: 4 GREEN as their commands read them (G-5 · G-6 · G-8 · G-10-well-formed) ·
1 GREEN-on-command-with-its-proof-leg-REFUTED (G-3) · 1 split (G-9) · 4 RED (G-1 · G-2 · G-4 · G-7).**
The five REDs are the five carried into this seat's dispatch by name (`X-P-W3.md` R2.1); none moved,
none was moved. Two new readings are this seat's own: G-3's proof leg and G-5's intent, both at §3.

### 1.1 — G-1, pasted

```
⟨cmd⟩ node typescript/scripts/css-universe.mjs --check --pinned-value-commit 6aca86020b6b2605e7d0f04fccb6601746e387f7
assignability  one compile, 33 types × 2 directions
  tsc exit     2 · diagnostics attributed 56 · unattributed 0
1   parseCssColor        runtime  parser  6075  20529  26604  PARTIAL  FALSE_REJECT_IN_SHAPE ×1345 · DIVERGENT_VALUE ×2054 · MIS_ACCEPT ×1200
6   parseTimingFunction  runtime  parser   503  26101  26604  PARTIAL  FALSE_REJECT_IN_SHAPE ×147 · MIS_ACCEPT ×23
19  parseStylesheet      runtime  parser  2256  24348  26604  PARTIAL  FALSE_REJECT_IN_SHAPE ×1287 · DIVERGENT_VALUE ×442 · MIS_ACCEPT ×236
tally  runtime 0 TOTAL / 3 PARTIAL / 16 ABSENT · types 5 / 0 / 28 · ALL 5 of 52 TOTAL
  16 adjudications · 22 witnessed inputs · 19 diverge from the incumbent · 0 NOT honoured by the candidate
RED — 47 of 52 rows are not TOTAL (3 PARTIAL, 44 ABSENT).                                   EXIT=1  (×2 identical)
⟨cmd⟩ cd typescript && npx tsc --noEmit -p test/css-totality/tsconfig.assignability.json
  56 × error TS (54 TS2694 · 2 TS2724), every one in test/css-totality/generated/assignability.generated.ts   EXIT=2  (×2)
⟨cmd⟩ … --emit <scratch>/repro-universe-52.json → 155452 B, sha256 0005f26b10c0…  == evidence/W3/universe-52.json
```

### 1.2 — G-2, pasted (the probe unmodified; run 1 and run 2 differ only in a PID)

```
⟨cmd⟩ shasum -a 256 docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs
77678a574d7c6b11448b19b6ab8fc0686dddf405a01cd7ea16757bc0837ad4ec   (last commit c0078d96 2026-07-27)
⟨cmd⟩ node docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs           14:32:16 · 14:32:28 EDT
RED  parseCssColor             102/172 throw
RED  parseCssScalar            102/172 throw
RED  parseCssValue              60/172 throw
RED  parseCssValues             60/172 throw
ok   parseKeyframeSelector       0/172 throw
ok   parseStylesheet             0/172 throw
ok   parseTimingFunction         0/172 throw
ok   parseAnimationTimeline      0/172 throw
ok   parseAnimationRange         0/172 throw

TOTAL 324 throws / 1548 calls
DISTINCT FAILURE MODES: 1
  324x  TypeError: Cannot read properties of undefined (reading 'replace')

RED — 324 totality violations. A ParseResult-returning parser must not throw.          EXIT=1  (×2)
⟨cmd⟩ git status --porcelain -- docs/tranches/V/megatranche/audit/probes/   → (empty), before and after
```

Identical to the 2026-08-03 baseline, the open's B.1 and `.d`'s d.5, to the call. The probe's
subject is the incumbent (`REPO = ../../../../../..` → value.js, `npm pack`ed and imported), which
this wave writes zero bytes of by law; the candidate realizes 3 of its 9 entries (F-c6). ESC-d2 stands.

### 1.3 — G-3 and G-9, pasted (this seat's own probe over both public surfaces)

```
⟨cmd⟩ node <scratch>/g3g9-probe.mjs      (×2, byte-identical)
G-3 heads   js   empty-body throws: 0/10 · undefined 0 · ok:false-with-empty-tuple-or-not-false 0 · codes [css_syntax]
G-3 heads   wasm empty-body throws: 0/10 · undefined 0 · ok:false-with-empty-tuple-or-not-false 0 · codes [css_syntax]
G-3 boundary js   36 cells (12 non-string kinds × 3 entries) · throws 0 · undefined 0 · distinct shapes 1:
     {"ok":false,"diagnostics":[{"code":"css_syntax","start":0,"end":0,"expected":["<string source>"],"actual":null}]}
G-3 boundary wasm 36 cells · throws 0 · undefined 0 · distinct shapes 1 (the same shape)
G-3 R1 js   parseCssColor("oklch()") → {"ok":false,"diagnostics":[{"code":"css_syntax","start":6,"end":7,
                                          "expected":["<number>","<none-keyword> ('none')"],"actual":")"}]}
G-3 R1 wasm (identical)
SHIELD.caught after G-3 legs: 0
DEPTH_BOUND=64 DEPTH_CODE=css_syntax DEPTH_PRODUCTION="<nesting-depth> (at most 64 levels)"
G-9 depth js      64  ok=false color_context_required "<context-free-color> …"          value-present=false
G-9 depth js      65  ok=false css_syntax "<nesting-depth> (at most 64 levels)"          value-present=false
G-9 depth js    7761  ok=false css_syntax "<nesting-depth> (at most 64 levels)"          value-present=false
G-9 depth js    7762  ok=false css_syntax "<nesting-depth> (at most 64 levels)"          value-present=false
G-9 depth wasm  64 / 65 / 7761 / 7762 — the same four readings
G-9 LATCH {"readable":true,"before":false,"armed":true,"afterReset":true,"symmetric":false, sites: packrat.ts:158/224/273/297}
⟨cmd⟩ npx vitest run --config typescript/test/css-recovery/boundary/vitest.config.ts
 Test Files  1 failed | 3 passed (4)      Tests  2 failed | 116 passed (118)      (×2)
   boundary.test.ts 77 ✓ · no-throw.test.ts 19 ✓ · depth.test.ts 15 ✓ · latch.test.ts 5 ✓ / 2 ✗ (L-3 RESETTABLE; the published record)
```

The twelve non-string kinds are `.c`'s ten plus a boxed `String` and `0`; none reaches the grammar.

### 1.4 — G-4 and G-8, pasted

```
⟨cmd⟩ cd typescript && node scripts/css-recovery-closure.mjs --corpus test/css-recovery/corpus.json \
        --frozen-union c654824e:src/css/types.ts --out <scratch>/recovery-closure.json                 (×2)
corpus          685 inputs · frozen union c654824e:src/css/types.ts · reachable set 30 modules from 2 public entries
executed        4158 calls · 3710 rejections · 3744 issues, over 2 lowerings × 3 entries
CTRL  ×6        every negative control fires
C-1   ⊆ built graph        GREEN  288 code sites · outside frozen 0
C-2   ⊆ executed corpus    GREEN  emitted [color_context_required css_syntax trailing_input] · outside frozen 0
C-3   ⊇ executed corpus    RED    frozen \ emitted = 5 [animation_option_invalid keyframe_selector_invalid
                                  syntax_descriptor_invalid syntax_mismatch timeline_option_invalid]
C-4   no fallback arm      RED    authored 0 · inherited 2 · measured DEAD: far.code === null on 0 of 3710 rejections
C-5 tuple · C-6 span · C-7 expected[0] named (0 of 3744 unnamed) · C-8 unarmed (false/false) · C-9 silence (0 writes) · C-10 22-operator  GREEN
union authentication  8 codes, both differences ∅ · branch census default: 3 (authored 0) · else 28 (authored 0)
RED — 8 of 10 legs green; negative controls all fire.                                                       EXIT=1
      evidence sha256 25fa6a4b08653334ed506a0b38aa690c96fa6e92d500f08473056bd5ebfc9240  == evidence/W3/recovery-closure.json (×2)
⟨cmd⟩ node scripts/css-recovery-closure.mjs --assert-no-console
GREEN — 2 of 2 legs green (--assert-no-console: C-8 · C-9); negative controls all fire.                    EXIT=0  (×2)
⟨cmd⟩ npx vitest run --config typescript/test/css-recovery/vitest.config.ts
 Test Files  2 failed | 4 passed (6)      Tests  3 failed | 139 passed (142)      (×2)
   labels.test.ts 11 ✓ · closure.test.ts 12 ✓ / 1 ✗ (the ⊇ row, born-RED) · + .c's boundary project above
```

### 1.5 — G-5, pasted

```
⟨cmd⟩ cd typescript && node scripts/css-dual-target-identity.mjs --js src/css/build/ac1.js \
        --wasm src/css/build/ac1.wasm --corpus test/css-totality/corpus.json                   (×2, byte-identical)
js artifact    src/css/build/ac1.js   — sha256 5f300b7ea43e1d38   ·   wasm artifact src/css/build/ac1.wasm — sha256 7ce0382b22716585
canonical      keys [ok, code, start, end, expected, actual] in FIXED order · NO array sorted · NO number rounded
  parseCssColor         26551 cells · six-tuple differ 0 · full-diagnostics differ 0 · value differ 0 · threw 0
  parseTimingFunction   26551 cells · six-tuple differ 0 · full-diagnostics differ 0 · value differ 0 · threw 0
  parseStylesheet       26551 cells · six-tuple differ 0 · full-diagnostics differ 0 · value differ 0 · threw 0
  boundary (7 declared non-string values × 3 entries = 21 cells) — identical across both targets: ALL
cells 79674 · six-tuple differing cells 0 (differing bytes 0) · full-diagnostics differing 0 · value differing 0 · threw 0
GREEN — the two targets produce byte-identical {ok, code, start, end, expected, actual} for every corpus input.   EXIT=0
      --out → sha256 b68bab94c01cd2eae24a3b74b0b8096a1bbd30b12d8d75e04233b26dd5686216  == evidence/W3/dual-target-identity.json
```

### 1.6 — G-6, pasted

```
⟨cmd⟩ npx vitest run --config typescript/test/css-totality/vitest.config.ts       14:29:49 · 14:29:51 EDT
 ✓ test/css-totality/spec-conformance.test.ts (13 tests)
 ✓ test/css-totality/universe.test.ts (59 tests)      G-1 reading: runtime 0/3/16 · types 5/0/28 · 5 of 52 TOTAL (REPORTED, never asserted)
 Test Files  2 passed (2)      Tests  72 passed (72)                                                        EXIT=0  (×2)
```

### 1.7 — G-7, pasted

```
⟨cmd⟩ cd typescript && node test/css-equivalence/run-full-surface.mjs --pinned-value-commit 6aca8602…     (×2, identical modulo PID)
oracle     value.js-4.0.0.tgz — 37290 B · sha256 7f80658ca4e16e99ccbb41ad6c9d8c08b2e5f86a7c951d2a833c97f89fb303ae
           cross-check vs the unpacked cand-o vendor tree: AGREE (css.js + css.d.ts byte-identical)
taxonomy   UNMOVED · sha256 554c2993cebd3ed3 · classes A DIVERGENT_VALUE / B MIS_ACCEPT / C FALSE_REJECT_IN_SHAPE
corpus     26604 rows → 26551 distinct · run 26551 · F-c3 unwrapped 172 · replay pin AGREES
  parseCssColor          COMPARED  26551 cells × 2 lowerings · mirror-defects 4027 (spec-undecided 2461) · lowerings AGREE
                           DIVERGENT_VALUE 1566 · MIS_ACCEPT 1200 · FALSE_REJECT_IN_SHAPE 1261 · COVERAGE_NARROWING 84 · FIXTURE_R1 211
  parseTimingFunction    COMPARED  26551 cells × 2 lowerings · mirror-defects  170 (spec-undecided  170) · MIS_ACCEPT 23 · FALSE_REJECT_IN_SHAPE 147
  parseStylesheet        COMPARED  26551 cells × 2 lowerings · mirror-defects 1693 (spec-undecided 1351) · A 342 · B 236 · C 1115
  + 5 type rows COMPARED · 44 rows NO-PEER (CN-1 ×6 · CN-2 ×10 · CN-3 ×28)
rows 52 · COMPARED 8 · NO-PEER 44 · ledger 29 rows (ADJUDICATED 16 · DISSENT 4 · FIXTURE 5 · LABEL 1 · NARROWING 3) · empty consumer-direction fields 0
MIRROR-DEFECTS  5890   (of which spec-undecided 3982)
RED — the equivalence floor is NOT held at full surface. 5890 mirror-defects over 8 compared rows.          EXIT=1
      --out → sha256 d03b458c0f095c7b201c17ded478fcfcfdf7c53b54f48668450b6c4166e41595  == evidence/W3/equivalence-full-surface.json
⟨cmd⟩ npx vitest run --config typescript/test/css-equivalence/vitest.config.ts
 Test Files  1 failed (1)      Tests  1 failed | 23 passed (24)     × G-7 — ZERO mirror-defects across all 52 exports   (×2)
⟨cmd⟩ node typescript/scripts/css-universe.mjs --cross-check-ledger docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md
rows     16 adjudicated conflicts, 0 not carried      GREEN — every adjudicated conflict is rowed.               EXIT=0  (×2)
```

**F-e8, read off this paste.** `.d`'s receipt (d.3) pastes `parseCssColor`'s non-defect tallies as
_"COVERAGE_NARROWING 2320 · FIXTURE_R1 4585 · DECLARED_DIVERGENCE 15"_. The settled program prints
**84 · 211**, and the committed `equivalence-full-surface.json` (`d03b458c…`, which `.d` itself
banked) carries `COVERAGE_NARROWING 84 · DECLARED_DIVERGENCE 0 · FIXTURE_R1 211` (and `AGREE 22229`,
which with the three defect classes sums to exactly 26,551). The three receipt figures are produced by
nothing in the tree; the mirror-defect counts, which are the gate, are unaffected. Recorded as a
transcription defect in the record, curable only by `.d`'s author as a dated addendum-beside.

### 1.8 — G-10, pasted (a third run, beside `.d`'s two in `evidence/W3/bench-three-leg.md`)

```
⟨cmd⟩ cd typescript && node scripts/css-bench-three-leg.mjs --baseline-tarball test/css-equivalence/vendor/value.js-4.0.0.tgz \
        --rounds 40 --discard 10 --denominator 1636680                                          14:32:15 EDT, run ALONE
method   40 rounds, first 10 discarded, MEDIAN of the kept 30 · interleaved, arm order rotates by round · cell 192 inputs · node v26.0.0 · darwin/arm64
  leg              arm                    ns/op  ratio (published/arm)  arm-state
  shared-accepted  published 4.0.0       1178.6                      1  N/A — published bundle carries no packrat [a]
  shared-accepted  candidate js          2161.9                  0.545  UNARMED — before & after [b]
  shared-accepted  candidate wasm        1671.1                  0.705  UNARMED — before & after [b]
  reject           published 4.0.0        544.2                      1  N/A [a]
  reject           candidate js          1762.7                  0.309  UNARMED [b]
  reject           candidate wasm        1177.7                  0.462  UNARMED [b]
  R1-class         published 4.0.0      41284.3                      1  N/A [a]
  R1-class         candidate js          1552.5                 26.592  UNARMED [b]
  R1-class         candidate wasm        1048.8                 39.362  UNARMED [b]
sink 580440 · latch readable true · before false · armed true · afterReset true · symmetric false
budgets against 1636680 µs: ≥10× 163668 (RETIRED AS LAW; native floor 311883 exceeds it 1.906×) · strict-3× 545560 (headroom 233677, UNRATIFIED 0/5)
                            · strict-2× 818340 (headroom 506457, UNRATIFIED 0/5) · break-even 1636680 (headroom 1324797, UNRATIFIED 0/5)
contradiction C-1 (07-20 "LIVE regex FASTEST ~1.8×" vs parser-band vs this table) — NOT RECONCILED AND NOT ERASED
BAR: OWNER-GATED-PENDING-RATIFICATION
VERDICT: none. The bench table is RECORDED-NOT-GATING (COHESION §0j.E OC-1). Inventing a bar is a defect.
well-formedness  OK — the four G-10 falsifier conditions all hold                                            EXIT=0
⟨cmd⟩ … --denominator 1870633   → REFUSED … VOID                                                             EXIT=2
```

Absolute ns/op are roughly half of `.d`'s two runs (2,274.8 / 2,264.6 published on the accept leg
against 1,178.6 here) — a different load on a box four tracks share; the **ratios** land within the
same band (0.459 / 0.461 there, 0.545 here; R1-class 26.9 / 24.8 there, 26.6 here). **Nothing is
concluded from that here**, no bar is set, inferred or reconciled, and the 07-20 reading stays a row.

---

## 2. The close sub-gate (`W3.md` §5 `.e`), conjunct by conjunct

| conjunct | reading |
|---|---|
| the close report exists with all ten gates' pasted outputs | this file, §1.1–§1.8 — every gate's own command, its exit code and its output, pasted from this seat's run files |
| at least one load-bearing claim carries a recorded refutation attempt | **three**, §3: closure (NOT refuted on its ⊆ half; its no-fallback conjunct REFUTED through the shield), a divergence row's spec reading (S-1/ADJ-2 **REFUTED**, ADJ-3/S-2 **half-REFUTED**, PB-03 **UPHELD**), the shield (**REFUTED**) |
| no gate is reported green on a narrative | every GREEN in §1 is a program's exit code and output at this clock; G-3's and G-5's beyond-corpus readings are reported **against** the candidate rather than averaged away |
| `harvest-journals.mjs` exits 0 · `x-p-w3.json` present · seat list non-empty · count = units dispatched | exit **0** (×3) · present (129,579 B) · 5 seats, none with zero rows · **5 of 6 — RED under the letter** (§5) |

---

## 3. L-14 — three named refutation attempts, each with its outcome

Method, stated before the outcomes. **Refute, never average.** Each attempt was designed to find the
input the claim's own evidence could not have seen: the closure proof was measured on `.b`'s 685-row
and `.a`'s 26,604-row corpora, so the attempt runs a **fresh band this seat generated from its own
seed**; the divergence rows cite specification sentences, so the attempt **fetches the drafts at this
clock** and looks for inputs on which the cited sentence and the candidate part ways; the shield was
proved dead over the corpus, so the attempt looks **beyond the corpus** at the one thing a corpus of
short strings never exercises — size. Every script ran from the scratchpad against the settled
`<p2>` bytes, twice, and wrote nothing but stdout.

### R-i — "the recovery union is closed" (`.b`, G-4)

**Attempt.** Two fresh bands, seeds `0x9e3779b1` and `0x00c0ffee`, **20,000 distinct sources each**
(mulberry32; 80 % mutations of 41 seed strings with 90 hostile splices — NUL, C1 bytes, lone
surrogates, `1e400`, unbalanced brackets to depth 200, `var(` × 70, comment openers, `@`-preludes;
12 % hostile concatenations; 8 % random code points, 35 % of them above U+007F), every one under the
Wasm input window so that R-iii's finding is not double-counted. The frozen eight were read from
**value.js `6aca8602:src/css/types.ts` by `git show`**, not from the candidate's own table. Each
source × 3 entries × 2 lowerings through the **public** surface, plus the RAW path for the
`far.code === null` fallback probe.

```
⟨cmd⟩ node <scratch>/refute-closure.mjs 0x9e3779b1 20000        (×2, byte-identical)
seed 0x9e3779b1 · band 20000 distinct sources · max length 905 · non-ASCII-bearing 7047
public calls 120000 · accepted 454 · rejected 119546 · issues 142508
emitted codes: css_syntax 140516 · trailing_input 1388 · color_context_required 604
  threw 0 · undef 0 · outsideFrozen 0 · emptyTuple 0 · okWithDiagnostics 0 · spanBreach 0 · actualMismatch 0
  emptyExpected 0 · unnamedExpected 0 · nullFarCode 0 · rawThrew 0 · targetsDiffer 0 · SHIELD.caught 0
⟨cmd⟩ node <scratch>/refute-closure.mjs 0x00c0ffee 20000
seed 0xc0ffee · band 20000 · public calls 120000 · accepted 476 · rejected 119524 · issues 141968
emitted codes: css_syntax 139838 · trailing_input 1534 · color_context_required 596 · every counter above 0 · SHIELD.caught 0
```

**Outcome — NOT REFUTED on the half `.b` proved, REFUTED on the half `.b` could not see.** Over
240,000 public calls no input reached a code outside the eight, an empty tuple, a breached span, an
unnamed expectation, a raw throw, an `undefined`, a fallback-arm hit or a cross-target difference; the
⊆ direction and the tuple/span laws held on a band their author never chose. The **⊇ direction stays
RED by `.b`'s own measurement** (C-3, five codes unemitted) and needs no refuting. But G-4's falsifier
names _"an input reaching a fallback arm"_, and R-iii below finds one: on the Wasm lowering a valid
8,191-rule stylesheet is answered `css_syntax` by `entry.mjs`'s `catch` — a code **no failure site in
the algebra raised**, selected by an arm that answers "nothing raised" with a code of its own. That is
the exact shape §11 item 3 names, and C-4's inspection does not count it because its `ABSENCE` regex
looks for `far.code === null`, not for `catch` (**F-e10**). The union is closed in the ⊆ sense and
**not** closed in the no-fallback sense the gate requires. The two bands' acceptance rate (0.4 %) is
disclosed: they exercise the rejection algebra hard and the acceptance side lightly.

### R-ii — the divergence rows' spec readings (`.d`, `DIVERGENCE-LEDGER.md` §6's three named rows)

Drafts fetched at this clock (⟨cmd⟩ `curl -sSL https://drafts.csswg.org/{css-color-4,css-syntax-3,css-values-4}/`,
2026-09-18 14:34 EDT; 1,254,931 / 620,126 / 1,018,170 B), tags stripped, every quotation below found
**verbatim** by regex over the text. Every input run through the sha-pinned oracle (`7f80658c…`,
`@mkbabb/value.js@4.0.0`) and **both** lowerings (⟨cmd⟩ `node <scratch>/refute-divergence.mjs`, ×2
identical; js ≡ wasm on every row).

| row | attempt | measured | outcome |
|---|---|---|---|
| **PB-03** — the two `hsl` spellings (_"the incumbent's `[120,50,50]` is the defect"_) | read css-color-4 §7.1 for the number/percentage equivalence; then look for the input the generalized reading mis-handles: the **legacy comma form** with bare numbers | §7.1: `<modern-hsl-syntax> = hsl( [<hue> \| none] [<percentage> \| <number> \| none] [<percentage> \| <number> \| none] [ / [<alpha-value> \| none] ]? )` and _"For saturation, 100% or 100 is a fully-saturated, bright color … For lightness, 50% or 50 represents the "normal" color"_ — a bare `50` **is** `50%`. `hsl(120 50 50)` → incumbent `[120,50,50]`, candidate `[120,0.5,0.5]`. `<legacy-hsl-syntax> = hsl( <hue>, <percentage>, <percentage>, <alpha-value>? )` admits **no** `<number>`: `hsl(120, 50, 50)` → incumbent **`ok [120,50,50]`** (MIS-ACCEPT), candidate `REJECT css_syntax [11,16) ["<percent-sign>"]`; `hsl(120, 50%, 50)` → incumbent `ok [120,0.5,50]` (a percent and a bare number in one colour), candidate `REJECT [16,17)` | **UPHELD, verbatim.** Not refuted — and a **NEW, UNROWED divergence** surfaced beside it (the legacy-comma bare-number form; two witnesses): exactly the "unrowed intentional difference" G-7 counts as a defect. Returned as **F-e2** for a `.d`-emitted row at X.P.W4, never hand-added to a generated file |
| **S-1 / ADJ-2** — token juxtaposition (_"ACCEPT — the css-syntax token-stream reading browsers implement"_) | find an input on which the cited token-stream reading and the candidate's grammar-level whitespace-optional juxtaposition part ways: a number immediately followed by an **ident-start** | css-syntax-3 §4.3.3 _consume a numeric token_: _"If the next 3 input code points would start an ident sequence, then: Create a <dimension-token> with the same value, type flag, and sign character as number, and a unit set initially to the empty string. Consume an ident sequence. Set the <dimension-token>'s unit to the returned value."_ So `120deg50` is **one** dimension-token (unit `deg50`) and `255none` **one** (unit `none`); neither is a `<hue>` or a channel. Measured: `hsl(120deg50%50%)` → candidate **`ok [120,0.5,0.5]`**, incumbent REJECT; `rgb(255none none)` → candidate **`ok [255,"none","none"]`**, incumbent REJECT. The three rowed inputs (`rgb(50%20%30%)` · `rgb(1.5.5 3)` · `hsl(120 50%50%)`) stay correctly accepted (`%` cannot start an ident sequence; `.5` starts a number); so do `rgb(255-0 153 / 0.5)` (`-0` is a number-token, not an ident-start) and `hsl(120 50%none)` (percentage-token then ident-token). `rgb(1e 2 3)` and `rgb(none255 0)` are rejected by both | **REFUTED as stated.** The candidate implements a strict **superset** of the tokenizer's maximal munch: on the dimension-merge class it accepts what §4.3.3 makes a single invalid token. The row's *direction* ("WIDENS acceptance") stands but is understated — in this class the widening exceeds css-syntax and is a candidate **MIS_ACCEPT against the spec**, the strongest evidence yet for the owner's reserved overrule toward cand-F's stricter line. Returned as **F-e1** (a grammar act, X.P.W4) |
| **ADJ-3 / S-2** — non-finite numerals (_"clamp where a clamp exists, reject the unclamped non-finite channel"_; citation _"css-values-4 §10.9 — a numeric token outside the implementation range is not a <number>"_) | test the REJECT half against the spec's own words for `<hue>`, and check the citation | css-color-4 §4.3: _"This number is normalized to the range [0,360). … In hsl(calc(-infinity) 0 0) or hsl(calc(infinity) 0 0), the <hue> component is again normalized to 0 degrees."_ css-values-4 §5: _"When a value cannot be explicitly supported due to range/precision limitations, it must be converted to the closest value supported by the implementation … If an <angle> must be converted due to exceeding the implementation-defined range of supported values, it must be clamped to the nearest supported multiple of 360deg."_ Measured: `hsl(1e400 0% 50%)` and `oklch(0.5 0.1 1e400)` → candidate `REJECT css_syntax [17,17) ["<finite-number>"]`, both lowerings. The clamp half: `rgb(1e400 0 0)` → `[255,0,0]`, `rgb(-1e400 0 0)` → `[0,0,0]`, `rgb(0 0 0 / 1e400)` → alpha 1. The citation: css-values-4 **§10.9 exists and is _Type Checking_** (math functions); the sentence the row attributes to it (_"outside the implementation range is not a <number>"_) occurs **nowhere** in the draft (0 regex hits), and §10.12 _Range Checking_ says the opposite of invalidation | **REFUTED on the REJECT half for `<hue>`; UPHELD on the clamp half; the citation is wrong.** Neither sentence makes an over-range hue invalid; both give it a **finite** value (0deg). The GROUND-C `±Infinity` contract question stays **OWNER-OWED** (`W3.md` §10) and is not ruled here; but the ledger may no longer describe the REJECT half as a *spec reading* — it is cand-O's preference. Returned as **F-e3**. Beside it, **F-e9**: the row's `lab(50 1e400 0)` witness is honoured **vacuously** — the candidate rejects `lab(50 10 10)` with the identical `[0,13) ["<color>"]` because `lab` is a declared coverage narrowing, so that witness tests nothing about non-finite handling |
| **ADJ-1** — hue not wrapped at parse time (an observation, not a divergence) | read §4.3 against both engines | `hsl(480 50% 50%)` → **both** `hue 480`; `hsl(-120 …)` → both `−120`; `hsl(360 …)` → both `360`, where §4.3 says the number _"is normalized to the range [0,360)"_ and `hsl(360 0 0)` _"is normalized to 0 degrees"_ | a **shared** non-normalization, so not a candidate-vs-incumbent divergence and it reddens nothing here; recorded for X.P.W4's adoption packet as a value-level question (**F-e4**) |

**The signed reading.** Of the three rows §6 offered: **one upheld (PB-03), one refuted as stated
(S-1/ADJ-2), one half-refuted with a wrong citation (ADJ-3/S-2).** Two of the three declared
divergences are therefore not yet *defensible readings of the specification* in the form the ledger
carries them. None of this moves G-7's 5,890 by a cell; it moves the *character* of part of that
number — `.d`'s unadjudicated `MIS_ACCEPT` samples split in both directions (`rgb(255-0 153 / 0.5)`
is spec-VALID under §4.3.3; `hsl(120deg50%50%)`-class inputs are spec-INVALID), which is why `.d`'s
"counted AGAINST the candidate" convention was the honest one and why a spec-executable oracle is the
instrument this wave lacks. The consumer-direction fields of S-1/ADJ-2 and ADJ-3/S-2 are amended
**beside, in the ledger's §6.1**, never in the generated rows above it.

### R-iii — "the retained shield is non-load-bearing" (`.c`, G-3's proof leg)

**Attempt.** `.c` proved the shield dead over 160,710 raw calls on inputs whose longest is under a
kilobyte. The Wasm lowering runs in **fixed regions** (`lowering-wasm/layout.mjs`: `INPUT_CAP`
1,048,576 · `C_CAP` 65,536 · `P_CAP` 65,536 · `MARK_CAP` 32,768 · `D_CAP` 4,096 · `VSTACK_CAP` 65,536 ·
`ARENA_CAP` 7,208,960) and its `parse()` **throws** at `lowering-wasm/index.mjs:266` (input over the
window) and `:274` (`W_OVF` set: _"a journal, the value stack or the arena overflowed its fixed
region"_) — two `throw` statements on the public parse path that `.c`'s T-1 (which counts `throw`
tokens in the six shipped closures) cannot see. So the attempt is: **grow a VALID stylesheet** until
a region overflows, and cross the input window by one code unit, on both lowerings, public and RAW.

```
⟨cmd⟩ node <scratch>/refute-shield.mjs        (×2, byte-identical; SHIELD.caught 0 at start)
A rules=8192  parseStylesheet  len=98304
    js   public→ ok:true · shield fired 0 · raw returned
    wasm public→ ok:false css_syntax [0,98304) exp=["<stylesheet>"] · shield fired 1 · RAW THREW Error: HALT: a journal, the value stack or the arena overflowed its fixed region
A' wasm RAW threshold: last OK rules=8190 (98280 B) · first THROW rules=8191 (98292 B)
B cap+1  parseCssColor        len=1048577  js ok:true · wasm ok:false css_syntax [0,1048577) exp=["<color>"] · shield fired 1 · RAW THREW … exceeds the module's 1048576-byte window
B cap+1  parseTimingFunction  len=1048577  js ok:true · wasm ok:false … ["<timing-function>"] · shield fired 1
B cap+1  parseStylesheet      len=1048577  js ok:true · wasm ok:false … ["<stylesheet>"] · shield fired 1
B at-cap (1048576) — all three entries ok:true on BOTH lowerings, shield 0
C decls=20000  parseStylesheet  len=200003  js ok:true · wasm ok:false css_syntax [0,200003) · shield fired 1 · RAW THREW … overflowed its fixed region
SHIELD.caught at end: 6   (all six faults: kind=wasm)
⟨cmd⟩ (the region) js sigma rules=8190 { V 8190, C 24570, P 24570, D 0, marks 32766 } · rules=8191 { …, marks 32770 }   ← MARK_CAP 32768
```

**Outcome — REFUTED.** On the Wasm lowering, `parseStylesheet` over **8,191 rules of `a{color:red}`
(98,292 B — a valid stylesheet)** overflows the **mark journal** (`MARK_CAP` 32,768: 32,766 marks at
8,190 rules, 32,770 at 8,191); the RAW path **throws**, and the public entry answers `ok:false
css_syntax [0,98292) ["<stylesheet>"]` **only because the shield fired** (`SHIELD.caught` +1). The
same on every input over 1,048,576 code units, for all three entries, and on a single rule carrying
20,000 declarations. The JS lowering answers **`ok:true`** on every one of these. Three consequences,
each measured rather than inferred: (1) the shield is **load-bearing** on the Wasm target — without it
G-3's _"zero throws"_ is false for valid inputs of ordinary size; (2) the answer it gives is a
**FALSE_REJECT of a valid input** with a code no grammar site raised (R-i's fallback arm); (3) **G-5's
intent falls beyond the corpus** — the two targets produce different `{ok,…}` for the same valid
input, in the direction §3a names as _"rooted in the Wasm … memory model"_ and routes to X.P.W1/W2 as
an architecture question. The dissent `.c` recorded (cand-F: a shield converts an impossible bug into
a silent `ok:false`) is therefore not only tenable but **exemplified**: the bug is not impossible, and
the rejection it produces is silent to a consumer.

Beside it, the JS lowering's own ceiling, measured because a refutation that stops at the Wasm side
would be half a reading (**F-e6**, INFO): `a{color:red}` × N through `parseStylesheet` — **1 MB →
`ok:true`, RSS 358 MB · 5 MB → `ok:true`, RSS 1,259 MB · 13 MB → `ok:true`, RSS 2,963 MB · 24 MB →
`FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of
memory`, exit 134** (heap limit 4,192 MB, node v26.0.0). About 205 B of heap per input byte; the
abort is a process death no `try` can catch and no `ParseResult` describes. It is a resource limit
every AST-building parser has somewhere, recorded so the adoption packet knows where this one's is.

**What this seat did NOT do about it.** It did not edit `entry.mjs`, `lowering-wasm/**`, or any
byte of `<p2>` — the cure is a lowering act (a declared capacity bound answered by a typed rejection
the way the depth bound is, or regions that grow) under `src/css/**`, which `W3.md` §4a gives to `.b`
and `.c` for *their* named cures and to nobody for this; it did not add the failing inputs to any
corpus (`.a`'s create row); it did not narrow G-3 or G-5 to "the corpus" by re-wording. **ESC-e1**
is returned with the measurement.

---

## 4. Escalations returned by this seat (§3a — none is this seat's to rule)

- **ESC-e1 — the shield is load-bearing on the Wasm lowering, and the two targets diverge on valid
  input beyond the corpus.** Measured at §3 R-iii. Two §3a triggers fire at once: G-3's falsifier by
  inspection of the entry module (a catch that *makes* the surface total for a class of inputs), and
  G-5 _"failing in a direction rooted in the Wasm numeric or memory model"_ — _"an architecture
  question for X.P.W1/W2, not a patch here."_ Routed to the orchestrator and, through it, to X.P.W4's
  adoption seam (which must not adopt a target whose totality rests on a catch) and to the contract's
  owner. The cure's shape is stated, not taken: either the fixed regions become a **declared bound
  with a typed rejection** (the depth bound's own idiom — `bounds.mjs` makes `Θ.depthBound` a
  declared value, an assertion, and a witness generator) so that both lowerings answer the same
  `ok:false` with the same code at the same size, or the regions grow with the input. Either is a
  write under `<p2>/typescript/src/css/**` and a G-5 re-run.
- **ESC-e2 — L-13's letter cannot be met by the harvesting seat (5 of 6).** §5. The same structural
  shape as X.P.W0 R-3 and X.P.W1 R-3; only a post-return re-harvest by the orchestrator, with the
  same unmodified command, can read six.
- **Carried, not re-opened, and not curable here**: `.a` F-a.6 (G-1, no implementing seat) · `.b`
  E-1 / E-2 / E-3 · `.c` ESC-c1 (the latch, `src/parse/**` is unadmitted) · `.d` ESC-d1 (G-7) /
  ESC-d2 (G-2). Each keeps the owner its author named.

---

## 5. The evidence chain — `harvest-journals.mjs` verbatim, `x-p-w3.json`, the ledger append, the §5 carve

`W3.md` §5 `.e`: _"`node docs/tranches/V/megatranche/workflows/harvest-journals.mjs` exits 0 with
`…/registry/harvest/x-p-w3.json` present, its seat list non-empty and its count equal to the units
dispatched"_; §4: the harvester is _"execute, no write to itself — … it writes only the two registry
rows named above (R-E)"_.

**K.7(i), third firing, measured first.** The script is unmodified — sha256
`77a6e04c20674f1575891ac9f3daa8421491fc2ec98bd784dc61b4563c978a28`, last commit `c0078d96`
(2026-07-27), `git status --porcelain -- docs/tranches/V/megatranche/workflows/` → 0 lines. Read at its
bytes: it writes `registry/harvest/<NAMES[runId] ?? runId>.json` for **every** workflow journal in the
session directory (144 dirs at this clock) and rewrites `registry/DEFECT-LEDGER.md` **wholesale**; it
cannot emit a file named `x-p-w3.json` (no `NAMES` row maps to it — `x-p-w0.json` and `x-p-w1.json`
were filed the same way, by their `.d`/`.e` seats). Run in the shared tree it would write files in
**no** row of this wave's §4. So it was run with the **verbatim command, from a scratch mirror whose
`workflows/harvest-journals.mjs` is a symlink to the repo's own file** (the executed bytes are the
repo's), the two output paths resolving relative to `cwd`:

```
⟨cmd⟩ cd <mirror-A> && node docs/tranches/V/megatranche/workflows/harvest-journals.mjs         14:36:43 EDT
harvested 2966 agent results · 7585 defects · {"BLOCKER":914,"MAJOR":3245,"MINOR":2196,"INFO":718,"MEDIUM":192,"HIGH":111,…}
EXIT=0
⟨cmd⟩ (shasum over registry/harvest before/after)  written 143 files · NEW 94 · CHANGED 2 (wf_247fe7fa-e67.json, wf_6f552e12-062.json) · UNCHANGED 47
⟨cmd⟩ cd <mirror-B, registry/DEFECT-LEDGER.md → SYMLINK to the repo's file> && node docs/…/harvest-journals.mjs     14:36:55 EDT
harvested 2966 agent results · 7585 defects · …                                                  EXIT=0
⟨cmd⟩ third run (mirror-A again, 14:38)                                                           EXIT=0
⟨cmd⟩ shasum -a 256 over all three runs' 143 harvest files → IDENTICAL, run for run
⟨cmd⟩ shasum -a 256 registry/DEFECT-LEDGER.md   (mirror-A · the repo's file after run B)
8092610ba3117de87ff03f50bbc502b584ab111e602d9a8a8df4adfedff84356   ×2   (118,756 L · 10,551,839 B)
```

**The `DEFECT-LEDGER.md` append is the script's act, literally**: in run B the harvester's own
`writeFileSync` landed on the repo path through the symlink, and the bytes it wrote equal run A's to
the hash. No hand touched the file. What the append is: **7,520 → 7,585** (+65), and **every one of
the 65 is an empty stub** (`### \`\` ·  ·` / `**Defect.** `) — X.P.W0 **R-5** reproduced a third
time (the harvester's row template drops the conformance schema): stubs **1,487 → 1,552**, so the
headline reads **7,585 = 6,033 with content + 1,552 stubs**. `git diff --check` flags **130**
trailing-whitespace lines in the append, all the script's (X.P.W1 R-5's shape); hand-editing generated
bytes would be the masking fix the standing law forbids, so they land as written and are recorded.
Read at the diff: **six hunks, all six the script's**, and no sibling seat's line among them (F-r.2
verified before staging: the working-tree diff of this file is wholly this run's output).

**`x-p-w3.json`** (129,579 B, sha256 `f657d114…`, schema `x-p-w3.e.harvest-fold/2`, regenerated twice
by the same program with identical bytes): the harvester's own run files for the wave's **three**
runs embedded **verbatim** (`wf_25c53370-7ee` 81,320 B `ebe56ca9…` · `wf_3e7ab295-b6d` 33,020 B
`4f9fd230…` · `wf_a9980aef-425` 4,191 B `149ad0cc…`, each byte-identical across the two runs), with
the envelope computed from them by program: `dispatched` 6 · `planned` (which run's seat-0 plan named
each unit) · `harvested` 5 — `.0` DONE 8 commits · `.a` PARTIAL 6 · `.b` PARTIAL 6 · `.c` PARTIAL 4
· `.d` PARTIAL 9, each with exactly one result row · `absent` `["X.P.W3.e"]` · `zeroRowSeats` `[]`.

**Seat count: 5 of 6 — RED under L-13's letter.** `.e` is structurally absent because a harvesting
unit cannot appear in its own harvest; its result row lands in `wf_a9980aef-425`'s journal only after
this seat returns. The dead predecessor `.e` seat left **no** result row anywhere (a killed seat
writes none) and is not counted. No returned seat has zero rows. This is X.P.W0 R-3 and X.P.W1 R-3
reproduced exactly, reported RED rather than as a passing summary.

**The COHESION §5 status-board line** is carved (one bullet, dated 2026-09-18, under the X·P W2 line;
the draft's bullet is rewritten to this file's readings); nothing else in `COHESION.md` is touched.

**A note the ruling deserves.** COHESION §0k.1 (HG-7) says the X-track run records are _"harvested
once, at X-W11's close"_ for the completeness validator's corpus. This wave's §5 `.e` sub-gate and
the seat-0 brief both order the harvest here; the two are compatible (this fold is the X·P lane's L-13
evidence, and X-W11's is the validator's corpus), and the orchestrator is the one to say so if it is
not.

---

## 6. §8 Verification artefacts, present at the bytes

| artefact | state at this seat |
|---|---|
| `evidence/W3/universe-52.json` (+ sidecar) | present, `0005f26b…` — regenerated by this seat, **sha256-equal** |
| `evidence/W3/r1-anchor-{before,after}.txt` | `after` present (`.d`); `before` is the open's B.1 paste in `X-P-W3.md` and `W3.md` §6 — **no separate `before` file exists** (recorded; the record's paste is the artefact) |
| `evidence/W3/recovery-closure.json` | present, `25fa6a4b…` — regenerated, **sha256-equal** |
| `evidence/W3/dual-target-identity.json` | present, `b68bab94…` — regenerated, **sha256-equal** |
| `evidence/W3/equivalence-full-surface.json` | present, `d03b458c…` — regenerated, **sha256-equal** |
| `evidence/W3/bench-three-leg.md` | present (`.d`'s two runs); this seat's third run pasted at §1.8, **not** written into the artefact (`.d`'s create row; E-3) |
| `DIVERGENCE-LEDGER.md` | 29 generated rows (`.d`) + **§6.1 Adjudication** (this seat, the one hand-written block) |
| `W3-CLOSE.md` | this file |
| `registry/harvest/x-p-w3.json` | present, fold/2, 5 of 6 seats (§5) |
| `<p2>` hashes | `bd10e5c` (.a) · `39503f8` `1cfa29b` `f666b6f` (.b) · `f14f59f` (.c) · `d8d7169` `2e1b006` `dde249d` `dc52ed5` (.d); `.0`'s six precede them; **this seat wrote nothing in `<p2>`** |

---

## 7. Findings raised by this seat (measured, not cured here; each with an owner)

| id | severity | finding | owner |
|---|---|---|---|
| **F-e1** | MAJOR | S-1/ADJ-2's widening exceeds css-syntax on the dimension-merge class (`hsl(120deg50%50%)`, `rgb(255none none)` accepted; §4.3.3 makes each a single invalid `<dimension-token>`) — a candidate MIS_ACCEPT against the spec, evidence for the owner's reserved overrule toward cand-F's stricter line | grammar act, X.P.W4 / owner |
| **F-e2** | MAJOR | an **unrowed** divergence: legacy-comma `hsl()` with bare numbers (`hsl(120, 50, 50)`, `hsl(120, 50%, 50)`) — incumbent MIS-ACCEPTS, candidate rejects per `<legacy-hsl-syntax>`; G-7 treats an unrowed intentional difference as a defect | `.d`'s emitter, X.P.W4 |
| **F-e3** | MAJOR | ADJ-3/S-2's REJECT half is not spec-backed for `<hue>` (css-color-4 §4.3 normalizes infinity to 0deg; css-values-4 §5 clamps to a multiple of 360deg); the row's "css-values-4 §10.9" citation names _Type Checking_ and the attributed sentence does not exist. GROUND-C stays owner-owed | `.d` / owner (GROUND-C) |
| **F-e4** | MINOR | ADJ-1: neither engine normalizes hue into [0,360) at parse time (`hsl(480 …)` → 480 in both); shared, so not a divergence | X.P.W4 adoption packet |
| **F-e5** | **HIGH** | **the shield is load-bearing on the Wasm lowering** (§3 R-iii): `MARK_CAP` overflow at 8,191 valid rules, `INPUT_CAP` at 1,048,577 code units, a 20,000-declaration rule — RAW throws, public answers `ok:false css_syntax` through the catch, JS answers `ok:true`. G-3's proof leg and G-5's intent both fall beyond the corpus | **ESC-e1** — orchestrator → X.P.W1/W2 (architecture) / X.P.W4 (adoption) |
| **F-e6** | INFO | the JS lowering's memory ceiling: ~205 B of heap per input byte; 13 MB parses at 2.96 GB RSS; 24 MB aborts the process (exit 134) under node's 4,192 MB heap limit | X.P.W4 adoption packet |
| **F-e7** | MINOR | `DIVERGENCE-LEDGER.md` is generated; re-running `emit-divergence-ledger.mjs` will **drop §6.1**. Any regeneration must re-append it (or the emitter must learn a preserved tail) | `.d`'s emitter, X.P.W4 |
| **F-e8** | MINOR | `.d`'s receipt d.3 pastes three non-defect tallies (`COVERAGE_NARROWING 2320 · FIXTURE_R1 4585 · DECLARED_DIVERGENCE 15`) that neither the settled program (84 · 211 · —) nor `.d`'s own committed evidence (`84 · 211 · 0`) produces; the gate's mirror-defect counts are unaffected | `.d`'s author — dated addendum-beside |
| **F-e9** | MINOR | ADJ-3's `lab(50 1e400 0)` witness is honoured vacuously: `lab(` is a declared coverage narrowing and every `lab(…)` is rejected with the identical `[0,n) ["<color>"]` | `.a`'s adjudication table / `.d`'s emitter |
| **F-e10** | MAJOR | C-4's fallback-arm inspection (`ABSENCE` regex on `far.code === null`) does not see the **third** arm: `entry.mjs`'s `catch → shieldIssue → selectCode("css_syntax")`, reachable by F-e5's inputs. The gate's "no fallback arm" leg is therefore RED on three arms, not two, and the third is **live**, not dead | rides `.b` E-2 |
| **F-e11** | MINOR | K.7(i) fired a third time: the harvester writes 143 files per run (94 NEW + 2 CHANGED against the tracked set) and rewrites the ledger wholesale; §4's _"writes only the two registry rows"_ mis-describes it. The 65-row append is 65 empty stubs (W0 R-5; stubs 1,487 → 1,552) with 130 whitespace flags | orchestrator — a dated addendum-beside to `W3.md` §4, never a spec patch |
| **F-e12** | MINOR | L-13 reads 5 of 6 (ESC-e2); W0 R-3 / W1 R-3 reproduced | orchestrator (post-return re-harvest) |
| **F-e13** | INFO | the inherited draft's corrections, itemized at §0 — the region, the rule count, the JS memory figures, the cell counts, the file counts, the run count | recorded here; the draft is superseded |
| **F-e14** | INFO | §8 names `r1-anchor-before.txt`; no such file exists — the BEFORE reading lives in the record's B.1 paste and `W3.md` §6 (both dated, both immutable). Not authored here: `evidence/W3/**` is `.a`/`.d`'s create rows | orchestrator |

---

## 8. Verbs, and what this seat does not stamp (R-A · `W3.md` §12)

| verb | value | by whom |
|---|---|---|
| AUDITED | YES | unchanged |
| SPECIFIED | YES | unchanged |
| IMPLEMENTED | **NOT stamped by this seat.** `W3.md` §12: _"Gates-green at this wave's own close stamps IMPLEMENTED"_; the gates are not green (§1). The orchestrator's close commit (§9 item 4) is the stamping act; X.P.W2's precedent (`complete_with_misses`, IMPLEMENTED-with-carried-REDs under §0n.2) is the orchestrator's to apply or refuse | orchestrator |
| VERIFIED | NO — **X.P.W4's alone** | R-A |

**L-18 rider.** Two challenging quartets and a fresh-Fable agglomeration stand between IMPLEMENTED and
ACCEPTED; nothing here is ACCEPTED. The bases §12 names apply with force after this close: _"a
'closed' union closed only by the corpus that happens to exist"_ is exactly what R-i/R-iii found on the
no-fallback conjunct, and _"a dual-target identity proven on a corpus too small to reach the numeric
edges"_ is what R-iii found one region past the corpus's largest input.

**Bound discipline, as a measurement.** This seat's whole write set: this file · `DIVERGENCE-LEDGER.md`
§6.1 · `registry/harvest/x-p-w3.json` · `registry/DEFECT-LEDGER.md` (by the script) · `COHESION.md`
§5 (one bullet) · `execution/D/X-P-W3.md` (its receipt) · `execution/LEDGER.md` (its row cells and a
timeline line). ⟨cmd⟩ `git -C <p2> status --porcelain` → `?? .worktrees/` before and after every run;
**zero bytes** in `<p2>`, in value.js `src/**`, in any producer tree, in the frozen roots.
`scripts/dev/dev.sh` was never opened.
