SERVED MODEL: claude-opus-5[1m]

# W1 — DATED ADDENDA, 2026-09-18 — §Bounds, widened by ONE row (E-6)

**An addendum BESIDE `W1.md`, never a patch of it (E-3).** `W1.md` was authored 2026-08-03 and its
wave CLOSED; this file carries exactly one grant, ruled at `COHESION.md` **§0n.4** on 2026-09-17 and
landed here by **X.P.W3.0**, the wave's first act. It is authored by a W3 seat because W1 is closed
and a closed wave cannot amend itself; it is dated and beside, because `W1.md`, `W1-CLOSE.md` and
`evidence/W1/**` are immutable (E-3).

**The immutability, measured at this seat, before and after this file was written:**

```
⟨cmd⟩ shasum -a 256 docs/tranches/X/parse-that/waves/W1.md
519df03ff21b48f3b2c4f352d6a4d8ae98c86d3dde117ba786b6c924204c6d09   (718 L · 55,891 B)
```

`<p2>` = `/Users/mkbabb/Programming/parse-that-css-totality-p2`.

---

## B-1 · §4 File Bounds — `harness/bench/lib/engines.mjs` gains ONE registration path

`COHESION.md` **§0n.4**, verbatim and whole:

> **§0n.4 · E-6 — W1's bench bounds, widened by dated addendum (an owner act, taken under
> delegation).** `harness/bench/lib/engines.mjs` may gain an `--engine=<adapter>` registration path
> (a closed chain becomes a chain plus one declared external cell); landed by **X.P.W3** under a
> dated E-3 addendum to W1's §Bounds, so G-7 has a subject. K-5 stays recorded-not-gating (OC-1); the
> table is printed for both lowerings of the graduated seed, two runs pasted. Not `.i`'s.

| Path                                 | Access under `W1.md` §4 (L142)                                                                           | Access under this addendum                                                  | Owner        |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------ |
| `<p2>/harness/bench/lib/engines.mjs` | `create (unit .d — the bench harness)`, W1 having closed, this reads execute + read for every later wave | **modify**, for the `--engine=<adapter>` registration path and nothing else | **X.P.W3.0** |

**The grant is one arm, and it is bounded by its own shape.** No other byte of
`<p2>/harness/bench/**` is admitted — `bench.ts` in particular stays **execute, no write** (it is
the gate-invoked entry `W1.md` §4 L143 and `W2.md` §4 pin by name, `npx tsx harness/bench/bench.ts`,
and its argv contract is a cross-file coordinate, not a local convenience). `aggregate.mjs`,
`finalize.mjs`, `cell.mjs`, `census.mjs`, `diagnostics-suite.mjs`, `lib/{boundary,corpora,latch,stats}.mjs`,
`METHOD.md`, `package.json` and the three banked result files are untouched.

### B-1.1 — what was landed, measured at the bytes

The four named engines are **unchanged**, `loadEngine` still **throws** on an id it cannot resolve
(`unknown engine <name>`), and the caller declares the id — nothing discovers a candidate from the
filesystem, so `W2.md` §3 item 7's _"a per-candidate harness is a defect"_ is not touched: the arm
resolves against the **same adapter contract every W2 probe already reaches a candidate through**
(`harness/w2/README.md` §2), and nothing in the file knows the name of any candidate.

The id is `adapter:<path-to-harness-adapter.mjs>#<js|wasm>`. The lowering fragment is **required** —
§0n.4 asks for the table _"printed for both lowerings of the graduated seed"_, and defaulting the
fragment would let a row print a lowering nobody declared. The three doors are the bench's own
(`color` · `easing` · `sheet`), mapped onto the adapter's `entry(prod)` boundary — BND-1, `ALGEBRA.md`
§5.8 — so `classify()` reads an adapter cell exactly as it reads the incumbent, with throws counted
and never swallowed into "reject".

```
⟨cmd⟩ node -e "… e.adapterEngineIds(['--rounds=40','--engine=typescript/src/css/harness-adapter.mjs#js', …])"
["adapter:typescript/src/css/harness-adapter.mjs#js","adapter:typescript/src/css/harness-adapter.mjs#wasm"]

adapter:…#js    -> color,easing,sheet | lowerings.js.entry("P:color")   | pins: adapter,jsEntry,dts,wasm
   classify oklch(0.7 0.1 30): accept | oklch(): reject | non-string: reject
   easing cubic-bezier(0,0,1,1): accept | sheet a{color:red}: accept
adapter:…#wasm  -> color,easing,sheet | lowerings.wasm.entry("P:color") | pins: adapter,jsEntry,dts,wasm
   classify oklch(0.7 0.1 30): accept | oklch(): reject | non-string: reject
   easing cubic-bezier(0,0,1,1): accept | sheet a{color:red}: accept

closed-chain: unknown engine nope
no-lowering:  HALT: engine id 'adapter:…/harness-adapter.mjs' names no lowering…
absent:       HALT: adapter ABSENT at …
published still loads: function
```

**W1's own G-5 leg re-read after the change, because a widened bound that breaks the gate it was
widened for is worse than no widening:**

```
⟨cmd⟩ npx tsx harness/bench/bench.ts --rounds=3 --warmup=1 --no-finalize --out=<scratch>/bench-smoke.json
EXIT=0 · stderr bytes: 0
GREEN — 8 timing cells + 1 census cell, each in its own process, each proving PACKRAT_ARMED === false
        at entry and at exit, no PID repeated; three legs published separately …
⟨cmd⟩ git -C <p2> status --porcelain -- harness/   →   M harness/bench/lib/engines.mjs   (one line, this grant's own)
```

The smoke run wrote its raw rows to a scratch path via the entry's own `--out` flag, so the three
banked result files under `harness/bench/` are byte-unchanged; `--rounds=3` is a smoke, **not** a
bench reading, and **no number from it is published as a ratio anywhere**.

### B-1.2 — what this grant does NOT do

It sets **no bar** and prints **no verdict on a speed reading**: `COHESION.md` §0j.E OC-1 rules the
bench table **RECORDED-NOT-GATING**, K-5 stays recorded-not-gating, and _"inventing a bar is a
defect"_. It publishes no ratio, restates no budget, and touches no denominator — every ratio row
that ever cites this arm still restates against **1,636,680 µs** (SCOPE.md M-22 ¶4), and the
`1,870,633 µs` denominator and its derivatives stay **UNCITABLE**. It stamps no verb on W1, whose
four-verb line is closed and unmoved. The table itself is **`.d`'s** to print, for both lowerings,
two runs pasted (§0n.4).
