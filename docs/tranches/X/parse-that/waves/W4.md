`claude-opus-5[1m]`

# X.P.W4 - The PLAW-BIND Integration and the Release Condition

**Sub-tranche**: X·P (`docs/tranches/X/parse-that/waves/`), authority `docs/tranches/X/COHESION.md`
§0/§1 SS-5 and **§2** (the cross-sub-tranche dependency graph), ratified under SCOPE.md M-25 ¶1.
**Alias law (R-B idiom)**: the sibling spelling `P.W4` denotes `X.P.W4` — a search alias, never a
rename; `X.P.W<N>.<x>` is the `WAVE_SPEC.md` §5 form and is authoritative.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** X·P inherits the parser pause
(`PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` §7 — *"do not … release, or rebind the parser from this
packet"*) and tranche X's execution gate. Nothing here opens, packs, publishes, or rebinds until
the **owner's begin-word**. Specifying a release protocol is not performing one.

**This wave writes no value.js source bytes, in any unit, under any reading.** It specifies the
**seam**; X·V owns its bytes (COHESION.md §2 routing law). A seam spec that lands as an edit has
destroyed the property that makes it a seam.

## 2. State

**Name**: W4 - The PLAW-BIND Integration and the Release Condition
**Opens after**: X.P.W3 IMPLEMENTED (the 52-export universe TOTAL, the R1 throw class dead, the
equivalence floor held, the divergence ledger written, the three-leg table published). The
dependency is on those artefacts, not on the label — §2b checks each.
**Agents**: 4 — phase 1: 2 parallel Opus 5 seats (`.a` seam contract, `.b` packed-release and Wasm
admission) · phase 2: 1 serial Opus 5 seat (`.c` the release-condition predicate and its
reciprocity rows) · phase 3: 1 **fresh Fable adjudicator** (`.d`, M-23 §1 — the sub-tranche
release close is adjudication, and it is the single site that stamps VERIFIED)
**Hard gate**: ten conditions in §6 — a 52-row seam contract · zero value.js source bytes · the
packed candidate proven from the tarball · the Wasm zero-function-import admission · an executable
RC-P evaluator that is honestly FALSE today · both-ends reciprocity for KF.W3 and F.W0 · the
forbidden edge held at zero · the packet rowed with a lawful delivery path and mail closed · the
adoption-gap given a terminal disposition · the R-A stamp. **Nine born-RED with the failing probe
measured and pasted; one is an INHERITED-GREEN FLOOR with its baseline measured.**
**Status**: planned

### Four-verb status (separate fields)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | pause handoff §2 (routing law) / §8 (resume protocol) · `GATE-VERDICT.md` · `parser-band.md` · COHESION.md §2 · `X/waves/W9.md` §5.g + G31 · `X/waves/W11.md` (the release idiom this wave mirrors) |
| SPECIFIED | **YES — by this file, 2026-08-03** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md`; X·P's L-20 loop closes the sub-tranche stamp |
| IMPLEMENTED | **NO** | — |
| VERIFIED | **NO** | — **stamped here, and only here** (R-A) |

**R-A — the stamping site, ratified for X·P.** Gates green + bytes landed stamps a wave
**IMPLEMENTED**; that is all any sibling X·P wave's own close may claim. **VERIFIED is stamped
only at this wave's release close**, against G-3 (packed-candidate proof), G-4 (Wasm admission)
and G-5 (the RC-P evaluator). X.P.W4 advances its own four-verb table row and every sibling X·P
wave's from IMPLEMENTED to VERIFIED **in one act at close** — a row edit in each file's four-verb
table (§4's carve; OP-6 checks the table form at open); no X·P wave stamps VERIFIED at its own
close. ACCEPTED is later still — post-quartet, per the L-18 rider in §12. This mirrors X·V's
X-W11 exactly, at the sub-tranche altitude, so the two stamping disciplines cannot drift.

### 2a. Goal criterion

This wave succeeds if a reader who has never met this program can answer three questions from one
document each: **what exactly does value.js receive** (a seam contract, 52 rows, signatures and
divergences), **how does it arrive** (a packed release, proven from the tarball rather than the
source tree, with the Wasm target's zero-import property proven by an inspection command), and
**when may the consumer act** (a predicate, RC-P, that a script evaluates and a person cannot
argue with). The wave fails if any of the three is answerable only by reading a status word.

### 2b. Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring | check at open |
|---|---|---|---|
| OP-1 | **Owner begin-word for X·P**, and specifically for the release limb — handoff §8.8 requires "a separate owner release" beyond the resume ruling. | STANDING PAUSE. Two distinct owner acts are required; the begin-word for X·P is not itself a release authorization. | Two dated owner words, cited by commit body. |
| OP-2 | **X.P.W3 artefacts exist**: `universe-52.json` all TOTAL, `r1-anchor-after.txt` exit 0, `equivalence-full-surface.json` 0 mirror-defects, `DIVERGENCE-LEDGER.md` non-empty with every row's consumer-direction field filled, `bench-three-leg.md` present. | UNRUN. | File presence + the four values re-read, not re-narrated. |
| OP-3 | **OC-1 discharged in either direction** (see G-5 conjunct 5). Either the owner ratifies a bench bar in writing, or he rules that admission is decided on correctness with the bench table recorded-not-gating. | **UNRULED** since 2026-07-20. The `≥10x` floor is retired as law; strict-3x / strict-2x / break-even are **UNRATIFIED (0/5 each)**. | A dated ruling of either shape. **The wave may open unruled** — RC-P simply evaluates FALSE, honestly, and says which conjunct failed. |
| OP-4 | **The X·V adoption surface.** Some X·V (or successor) wave must be authorized to consume the seam. | **DOES NOT EXIST — this is the wave's principal finding.** `X/waves/W9.md` §5.g states "This wave does not adopt a parser; it records why value.js ships none of parse-that today", and **G31's falsifier is literally "adopt a parser inside this wave"**. None of X-W0..X-W11 adopts one. | G-9's terminal disposition. This is a **coordination mark carried to the owner**, never a unilateral wave invention: X·P does not get to author an X·V wave. |
| OP-5 | **The release vehicle.** value.js `4.0.0` is immutable; `package.json#version` is set by **X-W9's dated cut** and released by **X-W11**. | Measured 2026-08-03: `package.json#version` = **4.0.0**; `npm view @mkbabb/value.js version` → **4.0.0**. | Whether the adoption rides tranche X's cut or a later one is an **owner scheduling ruling** (G-9 §disposition B). RC-P is written so it is true of *whichever* version carries it — it names a variable `V`, never a literal. |
| OP-6 | **The four-verb status table exists in all five X·P wave files.** The R-A stamp act is a *row* edit under §4's carve, so every sibling must carry §2's four-verb table — `AUDITED` / `SPECIFIED` / `IMPLEMENTED` / `VERIFIED` as four separate rows — never a one-line verb string. | Measured on the authoring bytes: W2/W3/W4 carried the table; **W0 §2 and W1 §2 carried the one-line form** (`IMPLEMENTED ✗ · VERIFIED ✗`) — per-file `VERIFIED` counts `0,0,1,1,1`, **sum 3**. That content-preserving conversion was **performed at the X·P round-3 L-20 repair, 2026-08-04**, by W0's and W1's own files: per-file counts re-measured `1,1,1,1,1`, **sum 5**. This wave books the dependency as discharged and still never performs a conversion itself. | `grep -c '^### Four-verb status' docs/tranches/X/parse-that/waves/W[0-4].md` → `1` for each of the five files, and G-10's open-state count sums to **5**. A sibling still in one-line form has **no carve target**: `.d` halts and routes the normalization back to the L-20 loop. This wave never reflows a sibling's prose — §4's carve is one row and nothing more. |

### 2c. Corpus transposition (M-25 ¶2)

| source row | verb | lands as |
|---|---|---|
| Pause handoff §2 — the lawful receiver path (`parser law → Value PLAW-BIND → V.L1/V.L5 → release+rebind → packed Value → Fourier F.W0 → F.W1`) | **BUILD — this wave is the middle three links** | §3.1–3.5; units `.a`, `.b`, `.c` |
| Pause handoff §2 — "Direct `parse-that → Fourier` credit is forbidden"; "research evidence is not product authority; a replay is not release" | PROHIBITION | **G-7**, and §3's closing prohibitions |
| Pause handoff §8.10 — "route any accepted result through Value's parser-law receiver before any consumer or Fourier claim" | BUILD | **RC-P** conjunct 6 (ROUTED) |
| Pause handoff §6 KEEP — "zero-function-import Wasm and closed memory/reification accounting" | BUILD | **G-4**; unit `.b` |
| Pause handoff §8.8 — "materialize only after a separate owner release" | BINDING | OP-1 |
| `GATE-VERDICT.md` OC-1 (bench bar) | CARRY-UNRULED, **with an exit** | OP-3; **RC-P conjunct 5 is a disjunction**, so an unruled bar cannot silently become a permanent veto *or* an invented standard |
| `GATE-VERDICT.md` OC-2 staged reading — totality becomes the parser band's **close gate**, and "NO consumer band rides the mirror until G-1/G-2/G-3" | **ADOPT — it is RC-P's ancestor** | RC-P conjuncts 2, 3, 5 are OC-2's G-1/G-2/G-3, restated as commands against a registry coordinate rather than against a working tree |
| `parser-band.md` §Scope note — relative colour **recognised-not-validated**; `color-mix()` a both-sides gap | DECLARED | the seam contract's §Non-goals; a consumer that assumes either is mis-informed by omission |
| COHESION.md §2 — "X·P release condition → KF.W3 (parser consumption): gate-keyed, never scheduled" | **BUILD — the load-bearing edge** | §3.4; **G-6** reciprocity |
| COHESION.md §2 — "Cross-repo edges are declared FROM BOTH ENDS in the spec files (the X·V W4-D2 lesson, now law)" | BINDING | **G-6**, both directions asserted by grep over two files |
| `X/waves/W9.md` §5.g + G31 | **CONFLICT — surfaced, not resolved** | OP-4; **G-9** |
| `X/waves/W11.md` §Scope 3 + `scripts/ci/verify-packed-surface.mjs` | ADOPT (idiom) | **G-3** — prove the **packed bytes**, never the source tree |
| INBOX **O-15** (PT-01/03/04/07) | FOLD | the seam contract's "what the consumer inherits from parse-that" section; the four are consumer-visible properties, not internal trivia |
| SCOPE.md **M-22 ¶3** (fresh root) / **¶4** (1,636,680 µs) | BINDING | §4 bounds; **G-5** conjunct 5's citation discipline |

## 3. Scope

1. Author `SEAM-CONTRACT.md`: one row per frozen `/css` export (52), each carrying name,
   signature, candidate provider, divergence disposition (`identical` / `declared-divergence <id>`
   / `not-provided`), and the consumer-visible direction of any behaviour change. Plus a
   Non-goals section naming relative colour (recognised-not-validated) and `color-mix()` (a
   both-sides gap) explicitly, so absence is declared rather than discovered.
2. Name the **lowering seam in both vocabularies**: the parser-law matrix coordinates **V.L1** and
   **V.L5** (Codex-authored, audit-subject per M-21 — cited as the receiver's historical name),
   and the live X·V coordinates that actually hold the bytes — `src/css/**`, held by **X-W9.a**,
   with `src/css/index.ts` / `src/css/types.ts` as the frozen contract surface. A seam named in
   only one vocabulary is unresolvable by half the constellation.
3. Specify the packed-release protocol: pack the candidate, install the tarball into a clean
   consumer, resolve the 52 symbols **from the installed bytes**, and refuse forbidden deep
   specifiers — the X-W11 idiom, applied one repo upstream.
4. Specify the Wasm target's admission test: zero function-kind imports, proven by an inspection
   command that requires no toolchain beyond node, plus an empty-import-object instantiation and a
   printed full import list (the "closed memory/reification accounting" half).
5. State the **RELEASE CONDITION `RC-P`** as a six-conjunct predicate with one command per
   conjunct, and ship the evaluator that computes it.
6. Write the reciprocity rows: the exact sentence **KF.W3** must carry, the exact sentence
   **F.W0** must carry, and the exact sentence an X·V adoption wave must carry — each quoting
   RC-P by name.
7. Give the adoption-wave gap (OP-4) a terminal disposition and carry it to the owner as a mark.
8. Author the **release packet in this repo** — `docs/tranches/X/parse-that/RELEASE-PACKET.md`,
   quantified over the version variable `V` exactly as RC-P is, never a literal — row it in
   `docs/tranches/V/coordination/INBOX.md` per E13 with **that in-repo path as the delivery
   point**, and hand its cross-repo placement to the root session's batched consumer communique:
   **COHESION.md §1 SS-6** (*"ONE batched BJ letter at the next boundary"*, root-authored, E13).
   The wave hands the letter to the courier; it never enters the neighbour's house, and it never
   opens an ad-hoc channel of its own. Then close X·P's mail to zero UNREAD.
9. Close the sub-tranche: advance X.P.W0..X.P.W4 from IMPLEMENTED to VERIFIED in one act (R-A),
   as a row edit in each file's four-verb table, with the four verbs kept in separate fields.
10. Run the **L-13 harvest** at close: file `docs/tranches/V/megatranche/registry/harvest/x-p-w4.json`
    with a seat count equal to the units dispatched, and let `harvest-journals.mjs` make the single
    `DEFECT-LEDGER.md` append. This is lane law in canonical W0/W1/W2 — its omission is a recorded
    kill (W2 §1), not a housekeeping lapse.

**Prohibitions.** No byte under `/Users/mkbabb/Programming/value.js/src/**`, `demo/**`, `api/**` —
the seam is a contract and a harness, never a patch. No edit to any sibling repo
(`keyframes.js`, `fourier-analysis`, `glass-ui`): packets are letters, and a letter is not a
commit in someone else's tree — the packet lands in **this** repo and rides the one lawful
courier, COHESION.md §1 SS-6's batched communique. No direct `parse-that → fourier` binding, in a
manifest, an import, a document, or a claim. No invented bench bar. No release credit inferred
from a replay. No
version literal in RC-P — the predicate quantifies over `V`, because a predicate that names
`4.1.0` is a schedule wearing a gate's clothes.

### 3a. Triumvirate Dispatch

Mandatory (research + plan augment + redress); the orchestrator may not redispatch the failing
unit alone.

- **File-bound expansion.** Any proposal to write under value.js `src/**`, `demo/**`, `api/**`, or
  under any sibling repo, from inside this wave. This is the wave's defining constraint; its
  violation is not a scope creep but a category error, and the correct response is a scope reveal
  routed to X·V and the owner.
- **Hard-gate failure that is not local-edit-recoverable.** G-4 failing because the toolchain
  emits function imports the architecture assumed away — that is an X.P.W2 architecture question,
  not a link-flag tweak. G-9 with the owner silent on OP-4 — the disposition is his to make; the
  wave records `BLOCKED-ON <exact condition>` and closes, it does not invent an X·V wave.
- **Third diagnostic iteration.** Any gate re-run three times without a monotone change. The
  packed-install loop specifically (G-3): a third failed install attempt is an environment or
  packaging fault to be diagnosed, never retried a fourth time.
- **Any pressure to relax RC-P.** A proposal to drop a conjunct, weaken a command to a document
  read, or add an "or the owner says so" escape hatch halts the wave. RC-P's value is exactly its
  unarguability; an escape hatch converts a gate into a preference.

## 4. File Bounds

| File | Access |
|---|---|
| `docs/tranches/X/parse-that/SEAM-CONTRACT.md` | create (the 52-row contract + non-goals + inherited-from-parse-that section) |
| `docs/tranches/X/parse-that/scripts/seam-contract-check.mjs` | create (G-1's cross-check; owned by `.a`, lives beside the contract — R-E) |
| `docs/tranches/X/parse-that/RELEASE-CONDITION.md` | create (RC-P: the predicate, the six commands, the reciprocity sentences) |
| `docs/tranches/X/parse-that/evidence/W4/**` | create (pasted outputs, packed-surface JSON, wasm import listings, RC-P evaluator output, sha256 sidecars) |
| `docs/tranches/X/parse-that/waves/W4-CLOSE.md` | create (ten gates RED-before / GREEN-after; the X·P terminal state table; the R-A stamp act) |
| `docs/tranches/X/parse-that/RELEASE-PACKET.md` | create (**the packet itself**, by `.d`: the seam summary, every `declared-divergence` row's consumer-direction field, and `RC-P(V)`'s six rows quantified over `V` — never a version literal. This in-repo path **is** the delivery point the INBOX row names; SS-6 carries it outward) |
| `docs/tranches/X/parse-that/waves/W0.md`, `W1.md`, `W2.md`, `W3.md` | modify-carve — **the four-verb status table's `VERIFIED` row only**, at close, by `.d` alone (R-A). All four carry that table (W0/W1 normalized at the X·P round-3 L-20 repair, 2026-08-04; OP-6 re-checks the form at open); a sibling whose §2 is still a one-line verb string has **no carve target**, and `.d` halts rather than reflowing prose |
| `docs/tranches/V/coordination/INBOX.md` | modify — mail rows only (E13); the dated sent-row names `docs/tranches/X/parse-that/RELEASE-PACKET.md` as the delivery point **and** the SS-6 batch that carries it cross-repo |
| `docs/tranches/X/COHESION.md` | modify-carve — **§1 SS-5 status cell and §5 status board line only**; §2's edge text and §1's SS-6 row are authored by the root session and are quoted here, never rewritten |
| `docs/tranches/V/megatranche/registry/harvest/x-p-w4.json` | create (L-13 harvest for this wave's workflow, by `.d` at close) |
| `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` | modify-append (by `harvest-journals.mjs`, once, unit `.d`; never rewritten by hand) |
| `docs/tranches/V/megatranche/workflows/harvest-journals.mjs` | **execute, no write** (`.d`'s close sub-gate — the harvest-seat-count condition — invokes it verbatim: `node docs/tranches/V/megatranche/workflows/harvest-journals.mjs`; it writes only the two registry rows named above — R-E) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/scripts/rc-p-evaluate.mjs` | create (the RC-P evaluator; G-5) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/scripts/wasm-admission.mjs` | create (G-4's inspection command) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/typescript/scripts/packed-candidate-surface.mjs` | create (G-3) |
| `scripts/ci/verify-packed-surface.mjs` | **execute, no write** (G-3's idiom reference and its value-side leg; X-W9 owns the bytes — R-E) |
| `docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs` | **execute, no write** (RC-P conjunct 2's command, re-run unmodified — R-E) |
| `docs/tranches/X/waves/W9.md`, `W11.md` | **read, no write** (X·V's bytes; the conflict at G-9 is reported, never edited from here) |

**Do NOT touch**: `/Users/mkbabb/Programming/value.js/src/**` · `demo/**` · `api/**` · `test/**` ·
`e2e/**` · `package.json` · `.github/workflows/**` · `scripts/dev/dev.sh` (unowned, NEVER) ·
`docs/tranches/X/waves/**` (X·V's twelve specs — read-only from X·P) ·
`docs/tranches/X/keyframes/**` and `docs/tranches/X/fourier/**` (X·KF and X·F author their own
reciprocity sentences; G-6 **verifies** them, it does not write them) ·
`/Users/mkbabb/Programming/parse-that/**` · `/Users/mkbabb/Programming/keyframes.js/**` ·
`/Users/mkbabb/Programming/fourier-analysis/**` · `~/.codex/**` · `~/Documents/Codex/**` and every
frozen root · `docs/tranches/V/apotheosis/**` and `docs/tranches/V/megatranche/coordination/**`
(dated evidence, append-never-rewrite).

### 4a. Disjointness

No two units share a `create` / `modify` / `modify-carve` path. `.a` owns `SEAM-CONTRACT.md` and
`scripts/seam-contract-check.mjs`;
`.b` owns `wasm-admission.mjs`, `packed-candidate-surface.mjs`, and `evidence/W4/packed*` +
`evidence/W4/wasm*`; `.c` owns `RELEASE-CONDITION.md`, `rc-p-evaluate.mjs`, and
`evidence/W4/rc-p*`; `.d` owns `W4-CLOSE.md`, `RELEASE-PACKET.md`, the four sibling waves' VERIFIED
table rows, `INBOX.md`, the two COHESION cells, and the close-time L-13 harvest
(`registry/harvest/x-p-w4.json` create; the single `DEFECT-LEDGER.md` append, made by the script).
`.a` and `.b` are the only parallel pair and they share no path.

**Cross-wave.** `INBOX.md` is also written by X·V's **X-W11.e** and by X-W9's packet unit; those
are different waves in a different sub-tranche with a different open window, and E13's ledger is
append-only by row — but the orchestrator must not run this wave's `.d` concurrently with an open
X·V mail unit. Declared here because a shared append-only file is exactly where a "no conflict"
assumption goes wrong. `docs/tranches/X/COHESION.md` is root-session-owned; `.d` writes two cells
under an explicit carve and quotes §2 (and §1's SS-6 row) verbatim rather than restating it.
`DEFECT-LEDGER.md` and `registry/harvest/` are shared append-only infrastructure across every lane
(L-13); the harvest is idempotent and append-only by construction, which is why an execute-only
bounds row on the harvester plus the two named output rows is the whole of this wave's claim on it.

### 4b. Worktree Plan

Phase 1 is one docs-only seat and **one** build seat; that build seat runs in a git worktree of the
**fresh root**, not of value.js, and — per the ONE-root law canonical X.P.W2 §4b states and this
wave adopts unchanged — **inside** that root, never beside it: *"worktrees **inside** the fresh root,
never beside it (ONE named root, no siblings ever created)"*. `<p2>` below is
`/Users/mkbabb/Programming/parse-that-css-totality-p2` — §4's fresh writer root, and the sole
writer. The docs writes land in this repo on clean main between phases.

| Agent unit | Worktree | Build dirs |
|---|---|---|
| X.P.W4.a | (docs-only; no worktree — writes in `value.js` on clean main) | n/a |
| X.P.W4.b | `<p2>/.worktrees/w4b` | own `typescript/node_modules`; `CARGO_TARGET_DIR=<p2>/target/w4b` (the Wasm admission half is Rust-backed; a shared target dir races, and a bench that reads the cache is not a bench) |

**No `parse-that-*` directory is created beside the frozen roots by this wave, ever.** A sibling of
the form `<p2>-w4b` would be indistinguishable by name from an unlawful root in X.P.W0's census grep
of `~/Programming`, which is precisely why the law reads *inside*.

Phases 2 and 3 are serial on clean main. The orchestrator runs `git worktree list` and
`git worktree add` in the fresh root before phase-1 dispatch, records both that **no worktree of the
read-only `parse-that` root exists** and that the fresh root has no sibling, and commits between
phases.

## 5. Agent Units

**Model seats (M-23).** `.a`–`.c` are **Opus 5 seats** (M-23 §2): a seam contract, a packaging
protocol, and a predicate evaluator are mechanism. `.d` is a **fresh Fable adjudicator**
(M-23 §1): the sub-tranche release close is an adjudication act, it is the only site that stamps
VERIFIED, and the adjudicator must be fresh — a seat that authored a gate cannot certify it.

### X.P.W4.a The seam contract

- **Goal**: a document from which X·V could implement the adoption without asking X·P a single
  question, and from which a consumer could predict every behaviour change before it happens.
- **Mechanism**: one row per frozen `/css` export, generated from X.P.W3's `universe-52.json` and
  hand-annotated only in the disposition column. The seam is named in both vocabularies (§3.2):
  **V.L1 / V.L5** (the parser-law matrix's receiver coordinates — cited as historical naming,
  M-21 audit-subject, never as standing authority) and **`src/css/**` held by X-W9.a** (the live
  X·V bytes). Three further sections: **Non-goals** (relative colour recognised-not-validated;
  `color-mix()` a both-sides gap, GROUND-A P-028); **What the consumer inherits from parse-that**
  (O-15's PT-01 label/diagnostics coupling, PT-03 latch, PT-04 depth, PT-07 boundary — each with
  its X.P.W3 cure state); and **Open contract questions** (the GROUND-C `±Infinity` ruling and the
  token-juxtaposition dissent, both owner-rulable without disturbing the rest).
- **Files**: `docs/tranches/X/parse-that/SEAM-CONTRACT.md`,
  `docs/tranches/X/parse-that/scripts/seam-contract-check.mjs`.
- **Sub-gate**: G-1, G-2.

### X.P.W4.b The packed-release protocol and the Wasm admission

- **Goal**: every claim about the candidate is a claim about bytes that were packed, installed,
  and imported — not about a source tree that happens to be on disk.
- **Mechanism**: `npm pack --ignore-scripts` in the fresh root; install the tarball into a clean
  temp consumer; resolve every seam-contract symbol **from the installed path**; assert forbidden
  deep specifiers refuse. The rule and its reason come from the value side and transfer exactly:
  the repo's own `dist/` is known to drift from what the registry ships (cand-O's dist-drift
  finding), so a source-resolved import cannot witness what a consumer receives (L-12). Then the
  Wasm admission (G-4), whose inspection command needs no toolchain beyond node and is therefore
  runnable by anyone auditing the claim later.
- **Files**: `scripts/packed-candidate-surface.mjs`, `scripts/wasm-admission.mjs`,
  `docs/tranches/X/parse-that/evidence/W4/{packed-surface.json,wasm-imports.json}`.
- **Sub-gate**: G-3, G-4.

### X.P.W4.c The release condition and its reciprocity

- **Goal**: one predicate, six commands, no adjectives — and both far ends of the graph carrying
  the same sentence.
- **Mechanism**: author `RELEASE-CONDITION.md` (§6a below is its normative core, reproduced there
  verbatim) and `rc-p-evaluate.mjs`, which takes `--version <V>` and prints a six-row table plus
  an overall verdict, exiting non-zero while any conjunct is false. Then write the three
  reciprocity sentences (§6 G-6) and deliver them: to X·KF's authoring seat for **KF.W3**, to
  X·F's for **F.W0**, and to X·V + the owner for the adoption wave. **Delivering a sentence is
  not writing it into someone else's file** — X·KF and X·F own their bytes exactly as X·V owns
  value.js's.
- **Files**: `docs/tranches/X/parse-that/RELEASE-CONDITION.md`, `scripts/rc-p-evaluate.mjs`,
  `evidence/W4/rc-p-evaluation.json`.
- **Sub-gate**: G-5, G-6, G-7.

### X.P.W4.d The sub-tranche release close

- **Goal**: X·P ends with one table a reader can hold, and exactly one act that moves five verb
  lines.
- **Mechanism**: a **fresh** Fable adjudicator, obligated under L-14 to attempt refutation of the
  wave's three central claims before certifying them — that the seam is complete, that RC-P is
  unarguable, and that no value.js byte moved. It writes `W4-CLOSE.md` with the ten gates'
  RED-before / GREEN-after outputs pasted; gives the adoption gap (OP-4) its terminal disposition
  (G-9); authors `RELEASE-PACKET.md` **in this repo**, rows it in `INBOX.md` with that in-repo path
  as the delivery point, and hands cross-repo placement to the SS-6 batched communique (G-8) —
  never a write into `keyframes.js/**`, never a fresh channel; runs the L-13 harvest and files
  `x-p-w4.json` (the `DEFECT-LEDGER.md` append is the harvester's, not the seat's); and performs the
  **single R-A stamp act** advancing X.P.W0..X.P.W4 from IMPLEMENTED to VERIFIED as a row edit in
  each file's four-verb table. It then updates COHESION.md §1 SS-5 and §5 under the §4 carve.
- **Files**: `waves/W4-CLOSE.md`, `docs/tranches/X/parse-that/RELEASE-PACKET.md`, the four sibling
  waves' four-verb-table VERIFIED rows, `INBOX.md`, `docs/tranches/X/COHESION.md` (two cells),
  `docs/tranches/V/megatranche/registry/harvest/x-p-w4.json` (create),
  `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` (modify-append, by script).
- **Sub-gate**: G-8, G-9, G-10 green; and the harvest JSON's seat count equals the units dispatched
  (four), per L-13's corollary that a seat harvested with zero journal rows is RED, not green.

## 6. Hard Gate

Ten conditions. **Nine born-RED with the failing probe measured read-only on 2026-08-03** against
value.js HEAD `c654824e`+working-tree; **one (G-7) is an INHERITED-GREEN FLOOR** with its baseline
measured and its regression falsifier named. Each states how it can fail for its intended reason
(L-19).

### 6a. RC-P — THE RELEASE CONDITION (normative)

> **`RC-P(V)` is TRUE if and only if all six conjuncts below evaluate TRUE for the same version
> `V` of `@mkbabb/value.js`. KF.W3 opens if and only if `RC-P(V)` is TRUE, evaluated by running
> the six commands against the registry coordinate `V` — never by reading a status word in any
> document, including this one.**

That sentence is load-bearing for the whole constellation graph: it is the sole edge from X·P into
the consumer half, and **COHESION.md §2** states it from the other end — *"X·P release condition →
KF.W3 (parser consumption): gate-keyed, never scheduled; routing is PLAW-BIND — parser → value
(X·V L1/L5 surfaces) → packed release → consumers. Direct parse-that→fourier is FORBIDDEN."*

| # | conjunct | command | TRUE when |
|---|---|---|---|
| 1 | **PUBLISHED(V)** | `npm view @mkbabb/value.js@<V> dist.shasum` then `npm pack @mkbabb/value.js@<V>` then `node scripts/ci/verify-packed-surface.mjs <tarball>` | the coordinate resolves at the registry **and** the packed `/css` subpath resolves all 52 frozen symbols from the installed bytes; exit 0 |
| 2 | **TOTALITY(V)** | `node docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs` (unmodified) against `V`'s registry tarball | exit 0 — **0 throws / 1548 calls** across all nine public parsers |
| 3 | **EQUIVALENCE(V)** | X.P.W3's full-surface differential harness, `V` vs the sha-pinned 4.0.0 tarball | **0 MIRROR-DEFECTs**, and every difference has a `DIVERGENCE-LEDGER.md` row with a non-empty consumer-direction field |
| 4 | **ADMITTED(V)** | `node scripts/wasm-admission.mjs <artifact>` | **0 function-kind imports**, empty-import-object instantiation succeeds, full import list printed and accounted |
| 5 | **BAR-DISCHARGED** | read the dated OC-1 ruling | the owner has ruled **either** a ratified bench bar (and `V`'s three-leg table meets it) **or** that admission is decided on correctness with the bench table recorded-not-gating. A **disjunction by design**: an unruled bar must not become a permanent veto, and it must never become an invented standard |
| 6 | **ROUTED(V)** | `grep -n 'RC-P' docs/tranches/V/coordination/INBOX.md` for the dated sent-row; `ls docs/tranches/X/parse-that/RELEASE-PACKET.md`; plus the two-grep forbidden-edge probe `grep -c 'parse-that' /Users/mkbabb/Programming/fourier-analysis/package.json` and the same over that repo's `web/package.json` | the packet naming `V` exists at its in-repo path, its dated INBOX row names **that** path as the delivery point and the **SS-6** batch carrying it cross-repo (E13), **and** the direct `parse-that → fourier` edge is absent from fourier's manifests and source |

**RC-P is FALSE today, and every conjunct is FALSE, measured 2026-08-03**: (1) the only published
version is **4.0.0** (`npm view @mkbabb/value.js version` → `4.0.0`; `package.json#version` →
`4.0.0`); (2) the anchor probe reports **324 throws / 1548 calls**, 4 of 9 entry points RED, one
failure mode; (3) the full-surface harness has never been run — the 07-20 GREEN covers a
403-string W0 subset, 49 of 52 exports having had no assay peer; (4) **zero project `.wasm`
artifacts exist** on either root; (5) OC-1 has been unruled since 2026-07-20; (6) `grep -c
'X·P\|X.P.W\|RC-P' docs/tranches/V/coordination/INBOX.md` → **0**, and `ls
docs/tranches/X/parse-that/RELEASE-PACKET.md` → *No such file or directory* — the packet does not
exist and has no row, in either direction.

**G-1 — SEAM CONTRACT COMPLETE, 52/52.** Every frozen `/css` export has a row with name,
signature, provider, disposition, and consumer-direction; both set-differences against
`universe-52.json` are ∅; the Non-goals section names relative colour and `color-mix()` explicitly.
*Command*: `node docs/tranches/X/parse-that/scripts/seam-contract-check.mjs
docs/tranches/X/parse-that/SEAM-CONTRACT.md
docs/tranches/X/parse-that/evidence/W3/universe-52.json
docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md` — prints both set-differences and every row
whose disposition contradicts the ledger; exits non-zero on any. The checker is owned by `.a`
and lives beside the contract it checks, so `.a` needs neither the fresh root nor `.c`'s
evaluator to close its own gate.
*RED baseline*: `SEAM-CONTRACT.md` does not exist — **0 of 52 rows**. The universe is measured:
`Object.keys(await import('./dist/subpaths/css.js')).length` → **19** runtime;
`src/css/index.ts`'s `export type { … }` block → **33** types; 19 + 33 = **52**.
*Falsifier*: a row whose disposition is `identical` while `DIVERGENCE-LEDGER.md` holds a row for
the same export fails (the two documents are cross-checked mechanically, not by reading). A 53rd
row without an `index.ts` origin fails. An empty consumer-direction field on a
`declared-divergence` row fails — that field is precisely what the KF and glass packets quote, and
a blank there is how a behaviour change reaches a consumer as a surprise.

**G-2 — ZERO VALUE.JS SOURCE BYTES.** Across the whole wave,
`git -C /Users/mkbabb/Programming/value.js status --porcelain -- src api demo test e2e` is empty at
every commit.
*RED baseline*, in the sense this gate means: the property is **unasserted** — no wave in X·P has
ever run the check, and the invariant that makes "seam, not edit" true is currently a sentence in
a document rather than an executed assertion. Measured today for the record: the paths are clean
(`git status --porcelain -- src` → 0 lines, 2026-08-03), so the gate starts from a true state and
its job is to keep it true through nine commits.
*Falsifier*: one byte under those paths at any commit fails the wave, not the unit — and the
correct response is a revert plus a scope reveal to X·V, never a rationalization that "it was only
a type". This gate is the executable form of §2's "X·V owns its bytes".

**G-3 — PACKED CANDIDATE, PROVEN FROM THE TARBALL.** `npm pack --ignore-scripts` in the fresh
root; install into a clean temp consumer; every seam-contract symbol resolves **from the installed
path**; forbidden deep specifiers refuse.
*Command*: `node scripts/packed-candidate-surface.mjs --seam
docs/tranches/X/parse-that/SEAM-CONTRACT.md` (packs, installs into a temp consumer, resolves every
row from the installed path, asserts the deep-specifier refusals, emits `packed-surface.json`),
cross-checked against the value-side idiom by `node scripts/ci/verify-packed-surface.mjs <tarball>`.
*RED baseline*: no candidate package exists — the fresh root itself is **ABSENT by design**
(`ls -d /Users/mkbabb/Programming/parse-that-css-totality-p2` → *No such file or directory*,
2026-08-03; it opens at X.P.W0's FIRST WRITE, and specifying is not doing). The idiom is measured
and live on the value side: `scripts/ci/verify-packed-surface.mjs` already asserts the `/css`
subpath's 19 runtime exports against a tarball, and this gate mirrors it one repo upstream.
*Falsifier*: a symbol that resolves in the source tree and not in the tarball fails — which is the
entire reason the gate installs rather than imports. A `files` field that ships more than it
declares also fails; the consumer's `node_modules` is the denominator, not the repo.

**G-4 — WASM ZERO-FUNCTION-IMPORT ADMISSION.** The Wasm artifact declares **zero function-kind
imports**, instantiates against an **empty import object**, and its complete import list is
printed and accounted (the "closed memory/reification accounting" clause).
*Exact inspection command* (no toolchain beyond node; verified executable by this seat against a
minimal module on 2026-08-03, which returned `imports total 0 function-kind 0 []`):

```
$ node --input-type=module -e "
  import {readFileSync} from 'node:fs';
  const bytes = readFileSync(process.argv[1]);
  const mod  = new WebAssembly.Module(bytes);
  const imps = WebAssembly.Module.imports(mod);
  const fns  = imps.filter(i => i.kind === 'function');
  console.log(JSON.stringify({total: imps.length, functionKind: fns.length, imports: imps}, null, 1));
  new WebAssembly.Instance(mod, {});           // must not throw
  process.exit(fns.length === 0 ? 0 : 1);
" -- <artifact>.wasm
```

*RED baseline*: **no project Wasm artifact exists.** Measured today —
`find /Users/mkbabb/Programming/parse-that /Users/mkbabb/Programming/value.js -name '*.wasm'
-not -path '*/node_modules/*'` returns exactly two files, both unrelated Chrome-profile TTS
binaries inside a `.claude/worktrees/**/.chrome-profile-*/WasmTtsEngine/` capture directory —
**zero project artifacts**. The subject is built at X.P.W2.
*Falsifier*: a single function-kind import fails, **and so does a passing static count paired with
an instantiation that needs a non-empty import object** — the two legs exist because static
inspection alone can be satisfied by a module whose glue smuggles the dependency in at
instantiation. Memory, table, or global imports do not fail the gate but **must appear in the
printed list**; an unaccounted import of any kind fails, because the KEEP clause is closed
accounting, not merely zero functions.

**G-5 — RC-P EVALUATOR EXISTS AND REPORTS HONESTLY.** `node scripts/rc-p-evaluate.mjs --version
<V>` prints the six-row table of §6a and exits non-zero while any conjunct is false, naming which.
*RED baseline*: the evaluator does not exist, and **all six conjuncts are FALSE** with the
measurements pasted under §6a above.
*Falsifier*: an evaluator that returns TRUE with any conjunct unmeasured fails; so does one that
reads a document's status word for any conjunct other than 5 (BAR-DISCHARGED, which is a ruling
and can only be read) and 6's mail leg. A conjunct implemented as "a previous run said so" fails —
each command runs against the registry coordinate at evaluation time, because a release condition
that trusts a cached verdict is a schedule.

**G-6 — RECIPROCITY, BOTH ENDS.** Three sentences exist in three files owned by three different
seats, each naming `RC-P`:
- **KF.W3** (X·KF): *"This wave opens if and only if `RC-P(V)` evaluates TRUE per
  `docs/tranches/X/parse-that/RELEASE-CONDITION.md` §6a, run against the registry coordinate `V`;
  the repin 4.0.0→`V`, the `serializeCssValue` fork retirement, and the DUAL-1 unification are all
  downstream of that evaluation."*
- **F.W0** (X·F): *"fourier receives parser effects only through an admitted, packed value.js
  release satisfying `RC-P`; the direct `parse-that → fourier` edge is forbidden and is asserted
  absent by the two-grep probe."*
- **The X·V adoption wave** (owner-gated, OP-4/G-9): *"This wave consumes
  `docs/tranches/X/parse-that/SEAM-CONTRACT.md` at `src/css/**`; X·P writes no bytes here."*
*RED baseline*: **neither sub-tranche's wave directory exists.** Measured today —
`ls -d docs/tranches/X/keyframes/waves docs/tranches/X/fourier/waves` → *No such file or
directory* for both; COHESION.md §0 lists X·KF and X·F as **FORMING** (specs await the intake
CARRY table). And the third sentence has no file to live in at all — that is G-9.
*Falsifier*: a `grep -rn 'RC-P' docs/tranches/X/{keyframes,fourier}/waves/` returning fewer than
two hits fails. A sentence that cites this wave by *number* instead of by predicate name fails —
wave numbers renumber, predicates do not. **X·P may not satisfy this gate by writing in those
directories**; the §4 bounds forbid it, and a reciprocity a seat authored on both ends is not a
reciprocity.

**G-7 — FORBIDDEN EDGE HELD AT ZERO. — INHERITED-GREEN FLOOR.** Direct `parse-that → fourier` is
absent from fourier's manifests and source.
*GREEN baseline, measured today*: `grep -c 'parse-that' /Users/mkbabb/Programming/fourier-analysis/package.json`
→ **0**; `web/package.json` → **0 hits**. The fourier census concurs and adds the nuance that
matters: the edge *has never existed*, and parse-that reaches that tree only **transitively** (4
copies on disk, 3 versions, via bbnf-lang / keyframes.js / latex-paper / value.js).
*Falsifier*: a direct dependency entry, a source import, or a document claiming fourier consumes
parse-that reddens it. The transitive copies are **not** a violation and must not be "cured" — a
seat that deletes a transitive copy to make a grep prettier has broken a working tree to satisfy a
gate. This gate is a floor: it can only be lost, never won, and losing it voids the wave.

**G-8 — MAIL CLOSED (E13), AND THE PACKET HAS A LAWFUL DELIVERY PATH.** The release packet naming
`V` **exists in this repo** at `docs/tranches/X/parse-that/RELEASE-PACKET.md` (created by `.d`, §4);
it is rowed in `docs/tranches/V/coordination/INBOX.md` with a dated sent-row naming **that in-repo
path** as the delivery point; its cross-repo placement rides the root session's batched consumer
communique — **COHESION.md §1 SS-6**, *"Glass communique assembly … ONE batched BJ letter at the
next boundary … root-authored (Fable); E13"*, quoted, never rewritten (§4 carve) — and the row
names the batch. X·P closes with **zero UNREAD**.
*Command*: `ls docs/tranches/X/parse-that/RELEASE-PACKET.md` (exists) then `grep -n
'RELEASE-PACKET\|RC-P\|SS-6' docs/tranches/V/coordination/INBOX.md` (the dated sent-row, its
delivery point, and its batch) then `grep -c 'UNREAD' docs/tranches/V/coordination/INBOX.md`
restricted to X·P rows — zero at close.
**This gate's cure arm stays inside this wave's bounds**: `keyframes.js/**` is Do-NOT-touch (§4),
so "send" here means *author in-repo + row + hand to the courier*. SS-6 is the only lawful
cross-repo write path in tranche X, and it is root-authored; a wave that opens its own channel has
substituted an ad-hoc letter for the batched-root law.
*RED baseline*: `grep -c 'X·P\|X.P.W\|RC-P' docs/tranches/V/coordination/INBOX.md` → **0** — X·P
has no mail row of any kind, in either direction — and `ls
docs/tranches/X/parse-that/RELEASE-PACKET.md` → *No such file or directory*, 2026-08-03. The
standing obligations the packet must discharge are on the ledger: **O-15** (the parse-that evidence
packet, SENT 2026-07-27, explicitly *"no ask, no bug claim, no dependency on a reply"* — the packet
must not retroactively convert it into an ask), and keyframes.js's exact pin, measured today at
**`"@mkbabb/value.js": "4.0.0"`** in `keyframes.js@6.0.0`.
*Falsifier*: a packet sent without a ledger row, or a row without a delivery path, fails. **A packet
written directly into a sibling repo's tree fails even if its content is perfect** — those paths
are §4's Do-NOT-touch list — and so does a letter dispatched outside SS-6's batch. A row whose
delivery point is a path only the sender can see fails. A wave that closes with an UNREAD row
fails. Placing the packet
where the recipient does not look is the recorded failure mode — O-15 had to be placed at
parse-that's `docs/` root because that repo has no `coordination/` directory anywhere — which is
exactly why the row must name **both** the in-repo path and the courier, not just a file.

**G-9 — THE ADOPTION GAP HAS A TERMINAL DISPOSITION.** OP-4's finding ends this wave in exactly
one of three recorded states, never in silence.
*RED baseline, and it is a genuine structural finding, not a formality*: **no X·V wave adopts a
parser.** `X/waves/W9.md` §5.g reads *"This wave does not adopt a parser; it records why value.js
ships none of parse-that today"*, and G31's falsifier is literally *"Record a dependency state the
manifest does not show, **or adopt a parser inside this wave**"*. X-W9's measured dependency set is
exactly `{"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}` — `@mkbabb/parse-that`
absent. None of X-W0..X-W11 holds the adoption. The three admissible dispositions:
**(A)** the owner authorizes a successor X·V wave (a thirteenth X wave, or a post-X tranche wave)
that consumes `SEAM-CONTRACT.md` at `src/css/**`;
**(B)** the owner amends X-W9's G31 to admit adoption inside tranche X, which also decides whether
the adoption rides X-W9's dated cut and X-W11's release (OP-5);
**(C)** the row closes `BLOCKED-ON <exact condition> + <re-trigger command>` (the X-W11.a idiom,
L-4) and X·P ends with RC-P permanently FALSE until that condition fires.
*Falsifier*: closing with the gap unstated, or with X·P having authored an X·V wave to fill it,
fails. **A sub-tranche does not get to write another sub-tranche's waves** — that is the whole
content of "spec the seam, not the edit", applied to the spec layer itself.

**G-10 — THE R-A STAMP ACT.** At close, X.P.W0..X.P.W4 each read IMPLEMENTED→**VERIFIED** in one
act, performed by `.d` alone; the four verbs remain in **separate rows of each file's four-verb
status table**; and no X·P wave stamped VERIFIED at its own close.
*Command*: `cat docs/tranches/X/parse-that/waves/W[0-4].md | grep -c 'VERIFIED | \*\*NO\*\*'` —
**5** at open, **0** at close. The `cat` is load-bearing: a five-file `grep -c` prints five
`path:count` lines and never a total, so the count must be a **sum** over the concatenation, and
the sum is the assertion. Its exactness rests on the table form OP-6 checks — five files × one
`VERIFIED` row each. And `grep -rn 'VERIFIED' docs/tranches/X/parse-that/waves/W[0-3]-CLOSE.md`
must return no line in which a sibling close report claims the stamp for itself.
*RED baseline*: all five X·P waves read `Status: planned`, `IMPLEMENTED: NO`, `VERIFIED: NO` —
0 of 5 stamped, which is correct and is what this gate must find at open. Measured against the
pre-normalization bytes the per-file counts were `0,0,1,1,1` — **sum 3**, because W0 §2 and W1 §2
then carried the one-line verb form. That is precisely why OP-6 exists: with the four-verb table
landed in all five files the sum reads **5**, and this gate does not open against a sum of 3. The
normalization landed at the X·P round-3 L-20 repair and the sum is **5 as of 2026-08-04**,
re-measured with the command above; the open-state check is still run at open, not inherited.
*Falsifier*: a sibling wave whose close report stamps VERIFIED fails the gate and voids that
wave's close; so does a stamp act that conflates ACCEPTED with VERIFIED (ACCEPTED is post-quartet,
§12). A stamp performed while any of G-1..G-9 is red fails — the act is the *consequence* of the
gates, never a substitute for them. A sum other than **5** at open fails and halts the act: either
a sibling's §2 is not in table form (OP-6, no carve target) or a wave has self-stamped. Reading a
per-file `grep -c` line as if it were the total is the misreading this command is written to
prevent.

## 7. Format And Lint Cadence

This wave is docs-plus-scripts. In this repo: Prettier over the touched `.md`, `git diff --check`
on every commit, and G-1's mechanical cross-check between `SEAM-CONTRACT.md` and
`universe-52.json` as the replacement evidence for the absent document linter. In the fresh root:
`npx tsc --noEmit` and `npx eslint .` over the three new scripts after each phase and before
close; `git diff --check`. **No proof-farm script is authored for any gate** (L-19) — G-3, G-4 and
G-5 are ordinary programs whose printed output is the evidence, G-2 and G-6 are one-line checks
recorded in the close report, and G-7 re-runs an existing two-grep probe. The retired `proof:*`
grep idiom is not resurrected.

## 8. Verification Artefacts

- `docs/tranches/X/parse-that/SEAM-CONTRACT.md` — 52 rows, non-goals, inherited-from-parse-that,
  open contract questions.
- `docs/tranches/X/parse-that/RELEASE-CONDITION.md` — RC-P verbatim, the six commands, the three
  reciprocity sentences, the COHESION.md §2 citation.
- `evidence/W4/packed-surface.json` — the tarball's sha256 + per-symbol resolution from the
  installed path + the forbidden-specifier refusals.
- `evidence/W4/wasm-imports.json` — the full import list, the function-kind count, and the
  empty-import instantiation result.
- `evidence/W4/rc-p-evaluation.json` — the six-row table at open (all FALSE) and at close, with
  the evaluated version `V` recorded.
- `evidence/W4/value-source-untouched.txt` — the `git status --porcelain` output at every commit
  (G-2's chain of custody).
- `evidence/W4/reciprocity-grep.txt` — the two-file grep proving both far ends carry `RC-P`.
- `docs/tranches/X/parse-that/waves/W4-CLOSE.md` — ten gates RED-before / GREEN-after; the X·P
  terminal state table; the G-9 disposition; the R-A stamp act.
- `docs/tranches/X/parse-that/RELEASE-PACKET.md` — the packet: the seam summary, the
  consumer-direction field of every declared divergence, and `RC-P(V)`'s six rows over `V`.
- The INBOX sent-row for that packet, naming its in-repo path as the delivery point and the SS-6
  batch that carries it cross-repo (COHESION.md §1).
- `docs/tranches/V/megatranche/registry/harvest/x-p-w4.json` — the L-13 harvest, seat count equal
  to the units dispatched; the `DEFECT-LEDGER.md` append is the harvester's.
- Commit hashes for each phase in both roots.

## 9. Commit Plan

- `docs(x-p-w4/seam): the 52-row seam contract — V.L1/V.L5 and src/css/** named in both
  vocabularies` (`.a`; body pastes the 19+33=52 measurement and the non-goals).
- `feat(x-p-w4/packed): pack-install-resolve the candidate; prove the Wasm zero-function-import
  admission` (`.b`, fresh root; body pastes the import listing and the instantiation result).
- `docs(x-p-w4/release-condition): RC-P — six conjuncts, six commands, three reciprocity
  sentences` (`.c`; body pastes the all-FALSE evaluation and quotes COHESION.md §2).
- `docs(x-p-w4/close): X·P sub-tranche release close — R-A stamp, adoption-gap disposition, packet
  + mail + L-13 harvest` (`.d`; pathspec covers `waves/W4-CLOSE.md`, `RELEASE-PACKET.md`, the four
  sibling waves' VERIFIED rows, `INBOX.md`, the two COHESION cells, and
  `registry/harvest/x-p-w4.json` + the `DEFECT-LEDGER.md` append; body carries the model receipt,
  the recorded refutation attempts, the G-9 letter, and the SS-6 batch the packet was handed to).

Pathspec commits only. `scripts/dev/dev.sh` is unowned and never touched. **No commit in this wave
may contain a path under `/Users/mkbabb/Programming/value.js/src/**`, `demo/**`, `api/**`, or any
sibling repo** — G-2 asserts it, and the commit plan states it so a reviewer can check the claim
without running anything.

## 10. Dependencies

- **Depends on**: X.P.W3 (all five artefacts, OP-2) · X.P.W2 (the Wasm artifact — G-4 has no
  subject without it) · X.P.W0 (the fresh root, and the pause authentication that makes any of
  this lawful) · the owner's begin-word **and** the separate release word (OP-1, handoff §8.8).
- **Blocks**: **KF.W3** (parser consumption) — gate-keyed on RC-P, never scheduled. **F.W0's
  atomic tuple** — fourier receives parser effects only through an admitted, packed value.js
  release, and never directly. The **X·V adoption wave**, once it exists (G-9).
- **Cross-repo edges (declared from this end; each reciprocated in the named file — the X·V W4-D2
  lesson, now law)**:
  - → **X·V**: this wave writes zero value.js source bytes (G-2). It hands `SEAM-CONTRACT.md` to
    whichever wave the owner authorizes at G-9, and it **reads** `X/waves/W9.md` §5.g and G31 to
    state the conflict. X·V's reciprocal sentence is owed and named in G-6.
  - → **X·KF**: RC-P is the gate on KF.W3. Reciprocal sentence owed, verbatim in G-6. The release
    packet is authored **in this repo** (`docs/tranches/X/parse-that/RELEASE-PACKET.md`) and
    reaches keyframes.js on the standing batched communique (COHESION.md §1 **SS-6**,
    root-authored); X·P writes no byte under `keyframes.js/**` and opens no channel of its own.
  - → **X·F**: RC-P is the gate on F.W0's tuple; the direct edge is **forbidden** and asserted
    absent by G-7. Reciprocal sentence owed, verbatim in G-6.
  - → **glass-ui**: coordinate-only. glass 7.0.0 imports `parseCssColor` and feeds it
    user-supplied strings, so it is on the owed R1 relay; the release packet joins the standing
    batched BJ communique (COHESION.md §1 SS-6) rather than opening a new channel.
- **Not opened here**: the OC-1 ruling itself (OP-3 — the wave reports, the owner rules); the
  GROUND-C `±Infinity` contract question; the token-juxtaposition dissent, which the owner may
  overrule toward cand-F's stricter line without disturbing the rest of the verdict; the
  css-color-5 wave that would promote relative colour from recognised to validated.

## 11. Archaeology

Four prior attempts at binding a parser to its consumers, each with the guardrail it earned:

1. **The V.L1/V.L5 receiver rows** — booked BLOCKED on `PLAW-BIND` since 2026-07-30, with "no
   source work starts" as their entire content. The blockage was real; what was missing was a
   *checkable* unblocking condition, so the rows could only ever be unblocked by assertion.
   *Guardrail*: RC-P is six commands. A row that unblocks on a document's status word is exactly
   the failure mode §6a's load-bearing sentence forbids.
2. **The 07-20 gate's deadlock.** Totality could only be cured by building, but the gate preceded
   the building; OC-2's staged reading was the release valve and it was never ruled. *Guardrail*:
   OC-2's G-1/G-2/G-3 survive here as RC-P conjuncts 2, 3 and 5 — restated as commands against a
   **registry coordinate** rather than a working tree, so the staging is mechanical rather than
   editorial.
3. **X-W4-D2, the one-directional cross-repo edge** — an edge declared at one end only, which
   passed two conformance passes before a third caught it. It is now tranche law that edges are
   declared from both ends. *Guardrail*: G-6 asserts both far ends by grep over files this wave is
   **forbidden to write**, which is what makes the assertion mean something.
4. **The Codex parser program's own release posture** — replays credited as releases, prose
   credited as product, and a v12 target that was ABSENT while its packet read complete.
   *Guardrail*: G-3 proves the packed bytes and not the tree; G-2 keeps the seam a seam; and the
   handoff's own words are the standing rule this wave is built on — *"research evidence is not
   product authority. Source review is not parser execution. A replay is not release."*

## 12. L-18 rider — acceptance is not close

Landing the ten gates green makes this wave **IMPLEMENTED**, and — uniquely in X·P, on the G-3,
G-4 and G-5 receipts — lets this close stamp **VERIFIED** for the sub-tranche (R-A). It never
makes it **ACCEPTED**. Per **L-18**, no implemented wave is ACCEPTED until it has survived **two**
challenging gestalt passes, each a **quartet of Opus 5 skeptics** working the three altitudes
(total-tranche · wave · feature), each assuming the implementation is wrong on some reasonable
basis. The bases that apply directly here: a seam contract complete on its own terms but silent on
something a consumer needs; an RC-P conjunct that looks mechanical and is in fact a document read;
a Wasm admission satisfied statically while the glue smuggles a dependency in at instantiation; a
reciprocity sentence that cites a wave number instead of a predicate; and an adoption-gap
disposition that reads as a decision while being a deferral. Superlatives are reported alongside
defects, each with provenance and proof. Both quartets are then adjudicated and agglomerated by a
**fresh Fable instance**; any incongruity surfaced is fully addressed, ratified, and dispatched —
never parked.

**Four-verb status**: this file makes X.P.W4 **SPECIFIED**. It is not AUDITED beyond the baselines
pasted in §6, not IMPLEMENTED, and not VERIFIED. VERIFIED is stamped **at this wave's own release
close and nowhere else in X·P** (R-A) — which is why §6 G-10 requires every sibling X·P wave's
close report to stamp IMPLEMENTED and no more.

---

**ADDENDUM 2026-09-19 (dated, beside — E-3; COHESION §0v).** §2 `Opens after` / OP-2 read as follows: X.P.W3 **IMPLEMENTED-with-carried-REDs-by-id** is the accepted form, iff every non-TOTAL cell of the re-emitted `universe-52.json`, every residual mirror-defect of the re-emitted `equivalence-full-surface.json`, and every non-zero reading of the candidate-side `r1-anchor-after.txt` is attributed to a ruling id from {GROUND-C · ID-1/ID-2 · R-f1 · E-k2} and none is unauthored surface. The seam contract (`.a`/`.b`) publishes those cells as **PENDING-ADJUDICATION** dispositions; `.d`'s fresh adjudicator rules them (ID-1/ID-2 and the GROUND-C cells first); RC-P evaluates the real state. The opening seat's first act is ESC-e2's harvest of W3 by the §0p scratch-mirror procedure.

**ADDENDUM 2026-09-19 (second, beside — E-3; COHESION §0w).** The OP-2 id-set is `{GROUND-C · ID-1/ID-1b · ID-2 · ID-3 · ID-4 · ID-5 · PB-11 · R-f1 · E-k2}` — each a ruled class with a predicate and a printed census. The biconditional is checked mechanically: every non-TOTAL cell of `universe-52.json` carries one of those ids; every miss entry of `equivalence-full-surface.json` carries a `rulingId` from the set. A tag that is not a ruled class is not a member.

**ADDENDUM 2026-09-19 (third, beside — E-3; COHESION §0y). The supplement wave `X.P.W4S` — the X·P adjudicative close.** Two serial units. **`X.P.W4.e` (Opus) — packaging (F-w4b-3, cure (a)).** Writable, beside §4: `<p2>/typescript/package.json` (`files` ships the css runtime + the Wasm artefact; `exports["./css"]` with `import` + `types`) · `<p2>/typescript/src/css/build.mjs` and `src/css/build/**` (the build emits the 33 frozen types as in-package bytes, byte-copied from the sha-pinned vendored 4.0.0 declaration with the sha in the header; `ac1.d.ts` re-exports from that file) · `src/css/lowering-js/js-alg.mjs` and `src/css/bounds.mjs` (the `tsx/esm/api` runtime import dies — the library is imported through the package's own built entry; `tsx` as a runtime dependency is refused) · the one artefact-loader line of `src/css/lowering-wasm/index.mjs` iff G-3's resolve leg names it · `evidence/W4/**` as NEW dated files beside `.b`'s. Gate: G-3 `resolved 52 of 52`, `G3 GREEN`, double-run, positive control in a staged tree outside both repos. **`X.P.W4.f` (a FRESH Fable adjudicator, M-23 §1) — the adjudicative half of `.d` (E-r2-1).** Writable: `SEAM-CONTRACT.md` (disposition + consumer-direction cells of the 45 `PENDING-ADJUDICATION` rows only) · `docs/tranches/X/parse-that/ADJUDICATION-W4.md` (create; every cell's ruling by §0w id and predicate — GROUND-C per §0v) · `DIVERGENCE-LEDGER.md` (modify-append, `rulingId` fields) · `waves/W4-CLOSE.md` (append a dated §12) · `RELEASE-PACKET.md` (dated addendum section) · `INBOX.md` (mail rows) · the five four-verb `VERIFIED` rows (R-A, one act, iff G-1..G-9 GREEN at this seat's own re-run) · `COHESION.md` §1 SS-5 cell + §5 board line (iff the stamp is performed) · `<p2>/typescript/scripts/rc-p-evaluate.mjs` + `RELEASE-CONDITION.md` §2.3 dated addendum (the V-tarball arm of conjunct 3, Q-RC-1) · `registry/harvest/x-p-w3.json` and `x-p-w4s.json` (create, §0p scratch-mirror). **§6 G-10, shape (a) (F-w4c-1):** *Command*: `for f in docs/tranches/X/parse-that/waves/W[0-4].md; do awk '/^### Four-verb status/{t=1;next} /^#/{t=0} t' "$f"; done | grep -c 'VERIFIED | \*\*NO\*\*'` — **5** at open, **0** after the stamp. Dated addenda quoting a table are outside the sum by construction. **Q-RC-2**: ADMITTED is not vacuous; a Wasm-free `V` reads FALSE. Record `execution/D/X-P-W4S.md`; X.P.W4's CLOSED row is untouched.

**ADDENDUM 2026-09-19 (fourth, beside — E-3; COHESION §0aa). Unit `X.P.W4.e2` (Opus), serial between `.e` and `.f`.** Writable: `<p2>/typescript/scripts/packed-candidate-surface.mjs` (the check-line emitter only — read each frozen name's type-parameter arity from the installed declaration and instantiate with `unknown` per parameter; the negative control must still fire) · `<p2>/typescript/src/parse/packrat-entry.ts` (export `packratEnter` / `packratExit` — export lines only) · the `dist/` it regenerates · `<p2>/typescript/src/css/bounds.mjs` (import the latch reader from `@mkbabb/parse-that/packrat`; the `tsx/esm/api` require dies) · `evidence/W4/**` as NEW dated files. Gate: G-3 `resolved 52 of 52`, `G3 GREEN`, double-run, positive control; `grep -rn 'tsx' src/css/**` over shipped bytes → 0. **`.f` opens iff `.e2` reads `G3 GREEN`** (the serial lock), then exactly as the third addendum states.

**ADDENDUM 2026-09-19 (fifth, beside — E-3; COHESION §0ab). Units `.g` ∥ `.h` → `.f2`.** **`X.P.W4.g` (Opus)** — writable: `docs/tranches/X/parse-that/scripts/seam-contract-check.mjs` (checks E and G learn the post-adjudication vocabulary; negative controls fire) · `<p2>/typescript/test/css-totality/run-full-surface.mjs` (the `:60` literal dies — the re-export set is measured from `entry.mjs`; the emitter's carry gains `§10`) · `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` (modify-append, by the harvester through the §0p run-B symlink only) · `evidence/W4/**` NEW dated files. Gate: G-1 `VERDICT: GREEN`, double-run, three negative controls printed RED. **`X.P.W4.h` (Opus)** — writable: `<p2>/typescript/src/css/**` for the declaration production only (`!important` tail per css-syntax-3 §5.4.7; the NAME is one ident-token per §5.4.4; §0s's quartet law if a ctor row changes) · `<p2>/typescript/test/css-totality/**` re-emission outputs as NEW dated files · `docs/tranches/X/parse-that/ADJUDICATION-W4.md` (dated addendum) · `SEAM-CONTRACT.md` (cells of the rows F-w4f-2 / F-w4f-1 touch only) · `DIVERGENCE-LEDGER.md` (modify-append: the F-w4f-1 row) · `evidence/W4/**` NEW dated files. Gate: cells `#40`/`#41` `identical`; the full differential re-run reads 0 mirror-defects outside ruled ids; the W3 totality gate still TOTAL. **`X.P.W4.f2` (fresh Fable)** — the third addendum's `.f` stamp set (five `VERIFIED` rows · two COHESION cells · `W4-CLOSE.md` dated §13 · `RELEASE-PACKET.md` dated addendum · `x-p-w4s.json` fold/2), iff G-1..G-9 GREEN at its own re-run.
