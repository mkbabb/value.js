SERVED MODEL: claude-opus-5[1m]

# W2 — DATED ADDENDA, 2026-09-17

**An addendum BESIDE `W2.md`, never a patch of it (E-3).** `W2.md` was agglomerated 2026-08-04 and
is immutable; this file carries the corrections and grants that arrived after it was sealed. Authored
by unit **X.P.W2.0** (COHESION `§0l`'s opening unit), under the grants `§0l` names by id.

**The immutability, measured at this seat, before and after this file was written:**

```
⟨cmd⟩ shasum -a 256 docs/tranches/X/parse-that/waves/W2.md
248eb0889f7ef101591435bd6bd2b6b5efc7e571cf9955a6360338507b29a4cd   (999 L · 82,540 B)
```

That digest is the one `execution/D/X-P-W2.md` §Open banks as the spec of record. No byte of `W2.md`
is touched by this addendum, this unit, or anything downstream of it; the reader holds the two files
together, `W2.md` first and this one beside.

---

## A-1 · §4 File Bounds — FOUR paths admitted (grant: COHESION `§0l` E-1, R-7; X-P-W1.md D-4)

`W2.md` §4 (L485–549) was written on 2026-08-04, when `<p2>` was **ABSENT** (§4's own Surface-B
preamble says so verbatim) and before this sitting's execution-record convention existed. Four paths
this wave must write appear in **no** row of that table. Each is admitted here, by the ruling that
grants it, with the reason it could not have been a row on 2026-08-04.

`<p2>` = `/Users/mkbabb/Programming/parse-that-css-totality-p2`.

### A-1.1 — Surface B additions (the fresh root owns its toolchain)

| Path                     | Access                     | Grant                                                                                                                                                                                                                                                                                                                                                          | Why it is not in §4                                                                                            |
| ------------------------ | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `<p2>/package.json`      | **create** (X.P.W2.0 only) | COHESION `§0l` **E-1**, verbatim: _"the cure is the idiomatic one: **X.P.W2's first commit lands `<p2>/package.json`** (`"private": true`; exact-pinned devDependencies `tsx` · `typescript` · `vitest`) **and its lockfile**, under a dated E-3 addendum to `parse-that/waves/W2.md` §4 admitting those two paths (Q-1's named §3a expansion, granted here)"_ | The obstruction it cures was **discovered at X.P.W1's execution** (D-F5, R-b1), five weeks after §4 was sealed |
| `<p2>/package-lock.json` | **create** (X.P.W2.0 only) | the same clause (_"and its lockfile"_)                                                                                                                                                                                                                                                                                                                         | as above                                                                                                       |

**The constraint on the manifest's contents is part of the grant, not a seat's taste.** `"private":
true`; `tsx`, `typescript` and `vitest` pinned **exactly** (no `^`, no `~`, no range); and **no
`type` field** — `<p2>/harness/bench/package.json` already declares `"type": "module"` for the
directory whose stderr G-5 reads byte-empty, and that file is W1's, in this wave's
**execute-no-write** surface (§4's R-E row), so the root manifest must not reach over it. A root
`type` would also re-type every `.ts`/`.js` file under `<p2>` whose nearest manifest is the root —
a resolution change this wave has no licence to make.

**What this grant is NOT.** It does not admit `<p2>/typescript/package.json` (untouched, W1's Q-1
still binds), any `npm ci` or `npm install` **inside** `<p2>/typescript/`, any global install, any
edit of `/Users/mkbabb/package.json` (the owner's file, outside every bound — **NEVER touched**), or
any write under `<p2>/harness/**`. `<p2>/node_modules/` is created by `npm install` and is
**gitignored at `<p2>/.gitignore:2`** — it is never a tracked path and never a commit member.

### A-1.2 — Surface A additions

| Path                                                        | Access                                | Grant                                                                                                                                                                                                                                                                      | Why it is not in §4                                                                                                                                     |
| ----------------------------------------------------------- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/tranches/X/parse-that/evidence/W0/roots-census-v2.sh` | **create** (X.P.W2.0 only)            | COHESION `§0l` **R-7**, verbatim: _"(roots-census.sh's missing EPERM disposition) → X.P.W2's open, under the same dated §4 addendum (one path: `docs/tranches/X/parse-that/evidence/W0/roots-census-v2.sh`, a successor beside the sealed original, never an edit of it)"_ | X.P.W0 CHECK 1's **D-3** was raised 2026-09-17; X.P.W1 correctly **RETURNED, NOT SCHEDULED** because `evidence/W0/**` is in no row of `W1.md` §4 either |
| `docs/tranches/X/parse-that/waves/W2-ADDENDA-2026-09-17.md` | **create** (this file, X.P.W2.0 only) | `§0l` E-1's _"under a dated E-3 addendum to `parse-that/waves/W2.md` §4"_ — the addendum is the instrument the ruling names, so it admits itself                                                                                                                           | a file the ruling calls into existence cannot be a row of the table it amends                                                                           |

**`roots-census.sh` is SEALED.** The successor is written **beside** it. The original's bytes, its two
captures (`census-before.txt` · `census-after.txt`, `diff` empty and re-proved a third and a fourth
time at X.P.W0's close and CHECK 1) and its digest `7f0c5b13…19d7` are **immutable evidence**: G-3's
whole proof is that the before/after pair diffs to empty, and an edited instrument voids it
retroactively. The successor is therefore a **new instrument with a new name**, and it is **not**
substituted into G-3's captured pair.

### A-1.3 — the three standing orchestration paths (X-P-W1.md **D-4**, INFO)

| Path                                    | Access                                                                                    | Owner                                                       |
| --------------------------------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| `docs/tranches/X/execution/D/X-P-W2.md` | **modify-append** (this lane's execution record; each unit appends its own receipt block) | every X.P.W2 seat, own block only                           |
| `docs/tranches/X/execution/LEDGER.md`   | **modify-append / minimal in-place replacement of this wave's own row cells**             | seat 0 and the close seat; four tracks edit it concurrently |
| `docs/tranches/V/coordination/INBOX.md` | **modify-append** (dated sweep lines and `I-n` rows)                                      | the **E13** mail law's seat-0/close acts                    |

D-4's own finding stands unchanged and is restated, not re-argued: _"Not a bounds violation.
`W1.md` was authored 2026-08-03, before this sitting's execution-record convention existed; all three
are standing orchestration infrastructure every track of this sitting writes, the third mandated by
the **E13** mail law."_ The same is true of `W2.md`, authored 2026-08-04. They are rowed here so the
next close does not re-discover them as findings for the fourth time.

### A-1.4 — §4a Disjointness is UNDISTURBED

`W2.md` §4a (L550–561) requires that no two units share a `modify` or `modify-carve` path. All four
A-1.1/A-1.2 paths are **create**, and all four are owned by **X.P.W2.0 alone** — no other unit of this
wave writes any of them. The three A-1.3 paths are append-only orchestration surfaces whose
concurrency discipline (own block; own row cells; re-read immediately before editing) is the
standing law of this sitting, not a new one. **Cross-wave**: none of the seven is a Surface-B path of
any concurrently-open wave, and `roots-census-v2.sh` is a new name, so `evidence/W0/**`'s sealed rows
are untouched.

---

## A-2 · D-3 — THE UNIFORM POSTURE OF G-2, G-4 AND G-5, IN ONE SENTENCE

X-P-W1.md **D-3** (MINOR, _"a presentation asymmetry, disclosed elsewhere in the same document"_)
records that W1's close table marks G-4 and G-5 plain GREEN while G-2 alone carries the literal-form
caveat, though `§C.6`'s E-1 names all three. The cure the finding asks for is one sentence, and this
is it:

> **At X.P.W1's close, G-2, G-4 and G-5 stood in one and the same posture — product-GREEN, literal-
> form-BLOCKED: each gate's product was measured through the gate's own relative entry with cwd
> `<p2>` and the exact binary `npx tsx` resolves, and the only thing that did not run from `<p2>` was
> npm's `npx` launcher, which did not run for any of the three.**

The gate table read alone under-claims G-2 and does not over-claim G-4/G-5, so W1's close errs
conservative; nothing was hidden, and no verb moves here. Under `§0l` E-1 this unit re-runs **all
three literal forms** once the toolchain lands, and pastes the readings **beside** W1's in
`execution/D/X-P-W2.md` §Unit receipts — beside, because W1's record is dated evidence (E-3) and is
not rewritten by a later measurement.

---

## A-3 · R-13 — `<p2>`'s BASE IS STATED, AND BECOMES THE REF-OF-RECORD

COHESION `§0l` **R-13**, verbatim: _"`<p2>` is pinned by convention only; X.P.W2's opening commit
states its base (`f5757082`) in its body, which is the ref-of-record from then on."_

The base is **`f5757082ca160dd5f25fcf437e692c9df8f7e78d`** — X.P.W0's ruled clone point, an ancestor
of `<p2>`'s HEAD (`git merge-base --is-ancestor` → true at X.P.W2's open, with W1's four commits
sitting on it). This unit's `<p2>` commit body states it. From that commit forward the base is cited
by that 40-hex, not by convention.

---

## A-4 · WHAT THIS ADDENDUM DOES NOT DO

It stamps **no verb**, moves **no gate**, adds **no unit**, widens **no gate's GREEN condition**, and
sets **no bench bar** (`§3a`'s bar trigger stays armed; COHESION `§0j.E` OC-1 holds the table
RECORDED-NOT-GATING). It admits exactly seven paths and states two sentences of record. `W2.md` §3,
§3a–§3e, §5, §6, §7 and §9 are read **exactly as their author wrote them**; `W2.md` §4's own
**Do-NOT-touch** list is untouched and binds in full — in particular
`/Users/mkbabb/Programming/parse-that/**` (including its uncommitted working tree, K-10), `~/.codex/**`,
`~/Documents/Codex/**`, every frozen root, value.js `src/**` · `api/**` · `test/**` · `e2e/**` ·
`demo/**` · `package.json` · `package-lock.json`, `scripts/dev/dev.sh`, the two sealed W2 author arms,
and every `dist/**` or publish surface anywhere.
