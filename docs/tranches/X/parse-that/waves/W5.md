# X.P.W5 — the CSS seam lands: parse-that's CSS surface merged, equivalent, published; value.js on it; CSS Color 5 coverage

## State
**Opens after**: X.P.W4S CLOSED. **Minted by**: COHESION §0bl (2026-09-23), on the owner's question *"what's the status on our css parser implementation with parse-that--are we fully spec compliant and parse-that idiomatic, too?"* and order *"Keep task on the other processes, too, like value.js, value.js, and parse-that."* **Model**: Opus 5.5 every seat. **Record**: `docs/tranches/X/execution/D/X.P.W5.md`.
**Trees**: parse-that `/Users/mkbabb/Programming/parse-that` (master) and the CSS work on `/Users/mkbabb/Programming/parse-that-css-totality-p2` branch `w2/harness` (no remote, NOT merged); value.js `tranche-u`.

## Measured starting point (2026-09-23, the orchestrator seat)
- value.js parses CSS with its own hand-written grammar (`src/css/grammar.ts`, `syntax.ts`); no parse-that dependency. keyframes.js parses through value.js 4.0.0.
- parse-that master ships NO CSS surface (`proof:no-css-surface`); the CSS parser lives only on the unmerged p2 branch.
- `RC-P(4.0.0)` = FALSE, 4 of 6 conjuncts (PUBLISHED · EQUIVALENCE with 44 mirror-defects · ADMITTED, no `.wasm` · ROUTED); TOTALITY and BAR TRUE.
- value.js 4.1.0 `parseCssColor`: `color-mix()` and `light-dark()` rejected (unsupported); relative color needs a context by design; `oklch()` returns a clean diagnostic (the old crash is gone).

## Goal criterion
parse-that's CSS surface is on parse-that master, idiomatic to parse-that's own combinator idiom (its CLAUDE.md + README), published; the 44 equivalence mismatches are each cured or rowed as an intended divergence in `DIVERGENCE-LEDGER.md`; value.js parses CSS through the seam (one parser, the hand grammar retired, no dual path); CSS Color 4/5 value syntax is covered including `color-mix()` and `light-dark()`; `RC-P` re-evaluated TRUE at the value.js coordinate X-W11 cuts.

## Units (serial)
- **`.a` (Opus, effort high) — merge.** Bring `w2/harness` onto parse-that master (merge or rebase with history kept; never force-push), retire `proof:no-css-surface`'s premise in the same commit with a dated note, parse-that's own test suite + proofs GREEN; push parse-that (owner-authorized).
- **`.b` (Opus, effort high) — equivalence.** The 44 mirror-defects from the RC-P harness: each cured at the parser (either side) or rowed as an intended divergence with its spec citation; harness exit 0.
- **`.c` (Opus) — Color 5.** `color-mix()` (all interpolation spaces + hue methods) and `light-dark()` parse and evaluate per CSS Color 5; relative color keeps its context contract; WPT-derived cases added as tests. Lands in the seam grammar, not in value.js's hand grammar.
- **`.d` (Opus) — publish + admit.** Version and publish parse-that (npm, owner-authorized); decide the `.wasm` question from the seam contract (ship it, or amend ADMITTED with a dated ruling request to the orchestrator if the TS build is the contract); `ROUTED` confirmed.
- **`.e` (Opus, effort high) — value.js on the seam.** value.js depends on the published parse-that; `src/css/*` parses through it; the hand grammar is deleted (no compatibility shim); value.js vitest + the BBNF equivalence tests GREEN; keyframes.js unaffected until it bumps (KF.W3 stays gate-keyed on RC-P). This unit touches value.js `src/**` — it must not collide with Track A: run it only when Track A's current wave is not writing `src/css/**` (check `git log` of the last hour), else ESCALATE for sequencing.
Close: RC-P re-evaluated (all six conjuncts, double-run); every unit's gates.

**ADDENDUM 2026-09-23 (COHESION §0bx) — the resume.** Landed: `.a` (parse-that `4eac70c1`, the CSS surface merged, pushed) · `.b` (`902172d`: MIRROR-DEFECTS 152 → 0 — every cell had already been ruled; the harness honoured only 16 of the rulings) · `.c` (`ec18f4b`: `color-mix()` resolves, WPT computed 887/887, JS ≡ Wasm over 1,776 WPT inputs; legacy comma forms pinned; `light-dark()` → `color_context_required`) · `.d` release commit `488523c` (2.0.0, `ac1.wasm` shipped) pushed but NOT published (npm token E401 — an OWNER ACT). Rulings:
- **F-W5c-1 — RE-RULED to the spec**: CSS Color 4 legacy `rgb()/rgba()/hsl()/hsla()` do not admit `none` (the WPT invalid files refuse it). PB-01/02 is superseded for the LEGACY arms only; the cure (one alpha edit per legacy arm) and a dated DIVERGENCE-LEDGER row land together, the 17 corpus cells re-read.
- **F-W5d-1 — (b)**: RC-P conjunct 4 (ADMITTED) reads V's installed DEPENDENCY CLOSURE (value.js must not vendor a dependency's `.wasm`); a dated addendum to RELEASE-CONDITION §2.4 states it, the evaluator reads `node_modules/@mkbabb/parse-that/**/*.wasm` reached from V's install.
- **ESC-c1 — GRANTED to `.f`**: `resetPackrat()` disarms `PACKRAT_ARMED` (`typescript/src/parse/packrat.ts:273/297`); `boundary/latch.test.ts` ×2 GREEN; css-recovery 475/475.
- **Coverage to spec (the owner asked "fully spec compliant") — `.g`**: SC-1 `calc()` in colour channels (css-color-4 admits it; resolved at parse where operands are absolute, else a context verdict); SC-2 `color(display-p3-linear …)` (widen `CssColorSpace` — 2.0.0 is a major); R-c-2 `light-dark()` resolves through the SAME context path value.js already uses for relative colour (add a colour-scheme field to that context type; no 53rd export). R-b-2 (`var()` in the `animation` shorthand, css-variables-1 §3) lands here with its divergence row. The corpus-count tests (R-b-1) re-pinned to the measured corpus with a dated note. Gates: the WPT-derived cases for each, JS ≡ Wasm, MIRROR-DEFECTS 0 ×2.
- **Perf (`proof:perf`) — honest-RED by instrument `PT-PERF-LOAD`** until a quiesced read (load < 4) is taken; the orchestrator takes it in a quiet window. Not a gate on publish.
- Order: [`.f`] → [`.g`] → [`.d`] (re-cut the unpublished 2.0.0 at the final commit; publish iff `npm whoami` succeeds, else ESCALATE the owner act again) → [`.e`] (only if 2.0.0 is on the registry).
