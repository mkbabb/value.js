SERVED MODEL: claude-opus-5[1m]

# W3 — DATED ADDENDA, 2026-09-18

**An addendum BESIDE `W3.md`, never a patch of it (E-3).** `W3.md` was authored 2026-08-03 against
frozen evidence and is immutable; this file carries the grants and corrections that arrived after it
was sealed — every one of them ruled at `COHESION.md` §0n (2026-09-17), five weeks later. Authored by
unit **X.P.W3.0**, the wave's first act (`§0n.3`), under the grants named below by id.

**The immutability, measured at this seat, before and after this file was written:**

```
⟨cmd⟩ shasum -a 256 docs/tranches/X/parse-that/waves/W3.md
53ebaf426aa8bce07c1cf9db8d4c74ecfea6319810031bb10394194a0fbc93d0   (673 L · 51,921 B)
```

No byte of `W3.md` is touched by this addendum, this unit, or anything downstream of it. The reader
holds the two files together, `W3.md` first and this one beside.

`<p2>` = `/Users/mkbabb/Programming/parse-that-css-totality-p2`.

---

## A-1 · §4 File Bounds — FOUR paths admitted

`W3.md` §4 (L148–183) is a two-root table: the fresh writer root carries the mechanism, this repo's
docs tree carries the evidence. Four paths this wave must write appear in **no** row of it. Each is
admitted here by the ruling that grants it, with the reason it could not have been a row on
2026-08-03.

### A-1.1 — the fresh root (mechanism)

| Path                                     | Access                     | Grant                                                                                                                                                                                                                                                                              | Why it is not in §4                                                                                                                                                                                                                              |
| ---------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `<p2>/harness/bench/lib/engines.mjs`     | **modify** (X.P.W3.0 only) | COHESION `§0n.4` **E-6**, verbatim: _"`harness/bench/lib/engines.mjs` may gain an `--engine=<adapter>` registration path (a closed chain becomes a chain plus one declared external cell); landed by **X.P.W3** under a dated E-3 addendum to W1's §Bounds, so G-7 has a subject"_ | `harness/**` did not exist on 2026-08-03 — X.P.W1 authored it five weeks later. The reciprocal W1-side grant is `W1-ADDENDA-2026-09-18.md` §B-1, which is the file `§0n.4` names; this row is its W3-side twin so a reader of either lands right |
| `<p2>/harness/w2/coverage-52-report.mjs` | **modify** (X.P.W3.0 only) | X.P.W2 close **K.8 / K.6 D-i2** (MAJOR), owner column _"`.g` / W3's open seat"_: the reporter throws whenever a candidate is actually present, so G-6's survivor-verb half is unrunnable                                                                                           | same — `harness/w2/**` is X.P.W2's, authored 2026-09-17. The defect was raised at X.P.W2's close and routed here by name                                                                                                                         |

**What these two grants are NOT.** They do not admit any other byte of `<p2>/harness/**`. In
particular `harness/bench/bench.ts`, `harness/bench/*.mjs`, `harness/bench/lib/*` other than
`engines.mjs`, `harness/totality/**` and every other `harness/w2/*.mjs` stay **execute + read, no
write** exactly as `W2.md` §4's R-E rows and `W1.md` §4 leave them — an edit there remains a §3a
halt. They do not admit `<p2>/typescript/src/parse/**` (see §A-3), `<p2>/experiments/w2/**` other
than the contract addendum below, or any write outside `<p2>`.

### A-1.2 — this repo (evidence and contract)

| Path                                                               | Access                                | Grant                                                                                                                                                                                       |
| ------------------------------------------------------------------ | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/tranches/X/parse-that/waves/W3-ADDENDA-2026-09-18.md`        | **create** (this file, X.P.W3.0 only) | the instrument the rulings name; an addendum a ruling calls into existence cannot be a row of the table it amends (the `W2-ADDENDA-2026-09-17.md` §A-1.2 precedent, verbatim reasoning)     |
| `docs/tranches/X/parse-that/waves/W1-ADDENDA-2026-09-18.md`        | **create** (X.P.W3.0 only)            | `§0n.4` — _"under a dated E-3 addendum to W1's §Bounds"_. It is authored **here**, by W3's first act, because W1 is closed; it is a dated addendum beside `W1.md`, never an edit of it      |
| `docs/tranches/X/parse-that/algebra/ALGEBRA-ADDENDA-2026-09-18.md` | **create** (X.P.W3.0 only)            | `§0n.3` — _"landed by **X.P.W3's first act** as ONE dated addendum-beside to `ALGEBRA.md` (both homes, sha256-equal), never by `.i`"_. This is the value.js home; the `<p2>` home is §A-1.3 |

### A-1.3 — the `<p2>` home of the same contract addendum, and the standing orchestration paths

| Path                                                         | Access                                                    | Grant                                                                                                                                                                                                                        |
| ------------------------------------------------------------ | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `<p2>/experiments/w2/contract/ALGEBRA-ADDENDA-2026-09-18.md` | **create** (X.P.W3.0 only), **sha256-equal to A-1.2's**   | `§0n.3`'s _"both homes, sha256-equal"_. `ALGEBRA.md` itself is `67c8253abaecb29a0b16a862bf63ad25ff1140d91f06248c843b9c253fc537de` in both homes at this seat's clock, and the addendum's own pair must be equal the same way |
| `docs/tranches/X/execution/D/X-P-W3.md`                      | **modify-append** (own receipt block)                     | X-P-W1.md **D-4**, rowed at `W2-ADDENDA-2026-09-17.md` §A-1.3 for the same reason and restated, not re-argued: standing orchestration infrastructure every track of this sitting writes                                      |
| `docs/tranches/X/execution/LEDGER.md`                        | **modify-append / minimal in-place row-cell replacement** | as above                                                                                                                                                                                                                     |
| `docs/tranches/V/coordination/INBOX.md`                      | **modify-append** (dated sweep lines and `I-n` rows)      | the **E13** mail law                                                                                                                                                                                                         |

### A-1.4 — §4a Disjointness is UNDISTURBED

`W3.md` §4a (L184–197) requires that no two units share a `create`/`modify` path. All six A-1.1/A-1.2/A-1.3
mechanism-and-contract paths are owned by **X.P.W3.0 alone**; no other unit of this wave writes any of
them, and none is a `create` row of `.a`, `.b`, `.c`, `.d` or `.e`. §4a's own split of
`<p2>/typescript/src/css/**` between `.b` (`lower`/`diagnostics`/`codes`) and `.c` (`entry`/`bounds`)
is untouched: `.0`'s writes under that glob are `algebra/grammar`, `lowering-js/{js-alg,index}`,
`harness-adapter` and the generated `build/**` — **none of the five names §4a assigns**, so the
"shared glob" hazard §4a names (the X·V W5-D1 defect) does not arise. The three A-1.3 paths are
append-only orchestration surfaces whose concurrency discipline (own block; own row cells; re-read
immediately before editing) is the standing law of this sitting.

---

## A-2 · §5 NAMES `.ts`, THE SURVIVOR IS `.mjs` — a transcription reconciliation, recorded once

`W3.md` §5 names `src/css/{lower,diagnostics,codes}.ts`, `entry.ts`, `bounds.ts` — **`.ts`**. The
graduated survivor is **`.mjs` throughout**:

```
⟨cmd⟩ find <p2>/typescript/src/css -type f | wc -l          → 17
⟨cmd⟩ find <p2>/typescript/src/css -name '*.ts' | wc -l     →  1   (build/ac1.d.ts, a generated declaration)
```

§4's first row admits the whole glob `<p2>/typescript/src/css/**` create/modify, so **no bounds
question arises**. Each unit keeps the spec's _names_ and adopts the tree's _extension_, and states
the choice in its receipt. `W3.md` was authored 2026-08-04, before AC-1 was selected (`§0n.1`). This
is a transcription reconciliation, not a scope change, and it is written down here so no unit
improvises it.

---

## A-3 · WHAT THIS ADDENDUM DOES NOT DO

It stamps **no verb**, moves **no gate**, adds **no unit**, widens **no gate's GREEN condition**, and
sets **no bench bar** (`W3.md` §3a's bar trigger stays armed; COHESION `§0j.E` OC-1 holds the table
RECORDED-NOT-GATING and _"inventing a bar is a defect"_). It admits exactly the paths tabled above.

`W3.md` §3, §3a, §5, §6, §7, §9 and §12 are read **exactly as their author wrote them**, and §4's own
**Do-NOT-touch** list binds in full and unmodified: `/Users/mkbabb/Programming/value.js/src/**` ·
`demo/**` · `api/**` · `test/**` · `e2e/**` · `scripts/**` (incl. `scripts/dev/dev.sh`, unowned,
NEVER) · `package.json` · `/Users/mkbabb/Programming/parse-that/**` (read-only evidence; the
fresh-root law) · `/Users/mkbabb/Documents/Codex/**` and `~/.codex/**` · the frozen v12 builder and
its preserved `__pycache__` residue · `/Users/mkbabb/Programming/keyframes.js/**` ·
`/Users/mkbabb/Programming/fourier-analysis/**` · `docs/tranches/V/apotheosis/**` and
`docs/tranches/V/megatranche/coordination/**`.

**`<p2>/typescript/src/parse/**`IS NOT ADMITTED BY THIS ADDENDUM.**`§0n.5`**ADOPTS** the clone
point's three`typescript/src/parse/**` commits **as the base** — adoption of an ancestor is a
statement about where the numbers were taken, not a licence to write new bytes there. The library is
what this wave builds **on\*\* (OP-7), and a wave that moves its own substrate in the same sitting it
re-points onto it has measured two things at once. F-h8's `w2/ac3-scan-union` disposition is
therefore returned with its measurement rather than taken here — see `execution/D/X-P-W3.md`,
X.P.W3.0's receipt, act 7.
