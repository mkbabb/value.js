claude-opus-5[1m]

# X.P.W0 - Pause-State Verification and the Fresh Root

**Preamble.** This file **SPECIFIES**; it does not execute. Every act below — the hash
authentication, the frozen-roots census, and above all the opening of
`/Users/mkbabb/Programming/parse-that-css-totality-p2` — **awaits the owner's begin-word**. At
authoring time the fresh root **does not exist and must not be created**; G-4's RED half is the
proof that it is absent today, and creating it is this wave's act, not this file's. Specifying is
not doing.

## 2. State

**Name**: W0 - Pause-State Verification and the Fresh Root
**Opens after**: tranche X opens (X ratified, SCOPE M-22 ¶1) AND the owner's begin-word for the
X·P lane. No predecessor wave exists — this is the lane's first file and the successor to the
`PAUSED_RESEARCH / TERMINAL_SOURCE_RED / NO_ACTIVE_WRITER` handoff of 2026-08-02.
**Agents**: 4 — phase 1: 2 parallel (Opus 5 authentication seat ∥ Opus 5 census seat, M-23 §2) ·
phase 2: 1 serial (Opus 5, the fresh-root opening — the lane's FIRST WRITE) · phase 3: 1 serial
(Opus 5, evidence-chain wiring)
**Hard gate**: the pause handoff's every named identity re-hashed with a three-state disposition
(MATCH / MISMATCH / EPERM, never conflated), eighteen preserved roots enumerated and byte-unchanged
across the wave, `parse-that-css-totality-p2` cloned from `f5757082ca160dd5f25fcf437e692c9df8f7e78d`
with the shared parse-that repository provably untouched, and the lane's journal→harvest chain
live — eight gates, all eight RED at HEAD `2636c238`, measured 2026-08-03 and pasted below
**Status**: planned

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | the pause handoff (`PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md`) + the megatranche provenance audit |
| SPECIFIED | **YES — by this file, 2026-08-03** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20, §12); X·P's L-20 conformance loop closes the sub-tranche stamp |
| IMPLEMENTED | **NO** | — landing the eight gates green + the close report stamps this, at this wave's own close (R-A, §9/§12) |
| VERIFIED | **YES — 2026-09-20, stamped by `X.P.W4.s` at the X·P sub-tranche release close (R-A), one act over W0..W4** | G-1..G-9 GREEN at the stamping seat's own clock, double-run (`execution/D/X-P-W4S.md` § X.P.W4.s · `waves/W4-CLOSE.md` §14); **VERIFIED, not ACCEPTED** (W4.md §12 / L-18 — the two quartets are still owed) |

**COMPLETABLE (C-06)**: **YES, once open.** The wave writes exactly two byte-classes: a new
directory tree at `/Users/mkbabb/Programming/parse-that-css-totality-p2` (a fresh clone, touching no
existing root) and documents under `docs/tranches/X/parse-that/`. It imports no generated parser
source, runs no builder, materializes no v12 target, publishes no package surface, and asks nothing
of any producer. Every gate is answerable from local bytes plus `git`, `shasum`, and one existing
in-repo script. Nothing here waits on a sibling wave, on `~/Documents` access, or on any external
repository — the three TCC-walled rows return **EPERM**, and EPERM is a complete result under G-2,
not a deferral.

### 2a. Goal criterion

This wave succeeds if, when work ends, **a writer can begin parser work on a root that is provably
fresh, from a commit that is provably the ruled packet commit, with the pause handoff's every claim
re-derived from disk rather than quoted — and with every preserved root demonstrably as untouched
after the wave as before it.** The pause was declared because the design was honestly RED; resuming
it honestly means the resumption's own first act leaves an audit trail no later reader has to take
on trust.

### 2b. Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring |
|---|---|---|
| OP-1 | **Owner begin-word for the X·P lane.** Handoff §7 forbids dispatching parser-law P01, CSS DREI-v12, or a performance family while paused; §8 step 2 requires "an explicit owner ruling naming a fresh, non-overlapping writer root" | Root **named** under delegated judgment (M-22 ¶3: `parse-that-css-totality-p2`, cloned from `f5757082`). The **begin-word itself is OPEN**. A wave that opens without it re-runs the pause it is supposed to lift. |
| OP-2 | **`~/Documents` TCC grant** — would let the builder SHA, the residue SHA, and the v12 target's absence be verified rather than recorded as unreachable | **DENIED at authoring**, measured: `ls /Users/mkbabb/Documents/Codex/2026-08-02/` → `Operation not permitted`. The wave opens **either way**; G-2 exists precisely so the denial is a recorded state and not a silent pass. |
| OP-3 | **Disk headroom for the clone** | `df -h /Users/mkbabb` → **165Gi avail** of 1.8Ti (91% used), 2026-08-03. `parse-that/.git` = **28M**; a `--no-hardlinks` clone plus a `f5757082` checkout is comfortably inside it. Re-measure at open; a clone that fails midway leaves a partial root, which G-4 must read as RED, not as opened. |
| OP-4 | **Handoff §9 STOP conditions unfired.** "Missing bytes, a present v12 target, a changed builder, a changed residue, or a stale owner receipt means STOP and request a new ruling" | The two in-reach identities **verify today** (§6 G-1). The three out-of-reach ones are EPERM. **A MISMATCH on any in-reach row halts the wave and returns to the owner** — it is not a local repair. |

### 2c. Corpus transposition (M-25 ¶2 — what this wave folds)

| hitherto source | fold |
|---|---|
| `PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` §3.1/§3.2/§3.3 (the exact pause coordinates), §7 (pause law), §8 (resume protocol), §9 (durable handoff) | the authority this wave discharges end-to-end; units .a and .c, gates G-1/G-2/G-7 |
| SCOPE **M-22 ¶3** (fresh writer root named; frozen roots untouched forever) | unit .c; gates G-3, G-4, G-5 |
| SCOPE **M-22 ¶5** / F-7 (dated `.sha256` packets are append-never-rewrite; **git object IDs are the living receipts**) | unit .a's output format — a dated append-only ledger file, never an edit of an existing `.sha256` |
| SCOPE **M-24 ¶4** (evidence law: `~/.codex/**`, `~/Documents/Codex/**`, the frozen roots, the 90 in-repo 7e28 copies are PRESERVED EVIDENCE — read/hash only, provenance never renamed) | unit .b; gate G-3 |
| **CC-087** (the parse-that decree vs the dependency tree disagree — BUILD → X-W9, under the pause handoff's ground truth) | this wave supplies X-W9's ground-truth input; it does not itself reconcile the decree |
| **L-13** (harvest before re-deploy) + `registry/harvest/parser-band.json` (the lane already has a harvest artifact) | unit .d; gate G-6 |
| **PLAW-BIND** routing law (handoff §2): parser → Value → packed release → consumers; direct parse-that→Fourier FORBIDDEN | gate G-8, declared from both ends per the X·V W4-D2 lesson |
| `GATE-VERDICT.md` (composite RED-of-distance), `registry/adjudicated/parser-band.md` (cand-O ruled winner) | **NOT consumed here** — they are X.P.W1's corpus. W0 establishes the ground; W1 ports the instruments. |

## 3. Scope

1. Re-derive every identity the pause handoff names, from disk, with the command and its literal
   output pasted — and record each as **MATCH**, **MISMATCH**, or **EPERM**, never collapsing the
   third into the first two.
2. Enumerate every preserved root in the parser lane's blast radius — the eight `parse-that`
   worktrees, the three `.codex` worktrees, the two value.js sibling worktrees, the three TCC-walled
   Codex evidence paths, the `.p-totality` archive, and the ephemeral parser-proof job tree
   (8+3+2+3+1+1 = **18**, the count G-3's census table holds) — each with its git identity or its
   unreachability, and bind them under one never-touch law.
3. Record that the seven frozen `parse-that` roots are **worktrees of one shared repository**, and
   derive the consequence: the fresh root is opened by **clone**, never by `git worktree add`,
   because a worktree add mutates the shared repository that six frozen roots depend on.
4. Open `/Users/mkbabb/Programming/parse-that-css-totality-p2` at the lane's FIRST WRITE, by
   `git clone --no-hardlinks` from `/Users/mkbabb/Programming/parse-that`, checked out detached at
   `f5757082ca160dd5f25fcf437e692c9df8f7e78d`, and prove afterward that `parse-that`'s worktree
   registry, HEAD, branch set, and working tree are bit-for-bit what they were before.
5. Prove object-store isolation: the fresh root's `.git` shares **zero inodes** with any frozen
   root's, so a future `gc`, `prune`, or `repack` in either cannot reach the other.
6. Write the never-touch law as an executable re-census, not as prose: one command that prints the
   eighteen roots' identities, run before the wave's first write and again at close, with the two
   outputs diffed to empty.
7. Wire the lane's evidence chain: name the workflow id, run the harvest after every seat
   completion or kill, and file the lane's harvest JSON alongside the existing
   `registry/harvest/parser-band.json`.
8. Restate PLAW-BIND from this end and record the reciprocal edge that `COHESION.md §2` already
   declares from the other, so the routing law is checkable from either side.

No parser source is read for meaning, no grammar is written, no benchmark is run, no builder is
executed, no v12 target is materialized, and no `__pycache__` residue is cleaned. Handoff §7 binds
this wave verbatim.

### 3a. Triumvirate Dispatch

Mandatory, not optional, on any of:

- **A §9 STOP condition firing.** A MISMATCH on the parser-law matrix, the DREI intake, or (under a
  later TCC grant) the builder or residue; or a **present** v12 target. The handoff's own words —
  "means STOP and request a new ruling" — make this an owner return, not an orchestrator retry. The
  triumvirate researches which byte moved and when, and drafts the ruling ask; it does **not**
  re-hash and hope.
- **File-bound expansion.** Any write outside §4's table — in particular any write inside
  `/Users/mkbabb/Programming/parse-that`, any of the six frozen sibling worktrees, `~/.codex/**`, or
  `~/Documents/**` — invalidates the wave outright. A clone that needs to touch the source
  repository to succeed is a scope reveal about the clone mechanism, not an edit to authorize.
- **The clone failing twice.** `git clone` failing for a reason that is not disk (auth, corruption,
  a locked index in the shared repo) halts on the second attempt. A third attempt against a shared
  object store is exactly how a frozen root acquires a `.git/gc.log`.
- **Third diagnostic iteration.** Any diagnose→act→re-measure loop reaching iteration three on the
  same gate halts; the orchestrator may not redispatch the failing unit alone.

## 4. File Bounds

| File | Access |
|---|---|
| `docs/tranches/X/parse-that/evidence/W0/PAUSE-AUTHENTICATION-2026-XX-XX.md` | create (dated; append-never-rewrite per M-22 ¶5 / F-7) |
| `docs/tranches/X/parse-that/evidence/W0/FROZEN-ROOTS-CENSUS-2026-XX-XX.md` | create (dated; the eighteen roots + the never-touch law) |
| `docs/tranches/X/parse-that/evidence/W0/roots-census.sh` | create (the executable re-census of §3.6) |
| `docs/tranches/X/parse-that/evidence/W0/census-before.txt` | create (roots-census output, pre-first-write) |
| `docs/tranches/X/parse-that/evidence/W0/census-after.txt` | create (roots-census output, at close) |
| `docs/tranches/X/parse-that/EVIDENCE-CHAIN.md` | create (workflow id, journal path, harvest cadence, seat receipts) |
| `docs/tranches/X/parse-that/waves/W0-CLOSE.md` | create (the eight gates, RED-before / GREEN-after pasted) |
| `/Users/mkbabb/Programming/parse-that-css-totality-p2/**` | **create — the wave's act.** Absent at authoring (G-4's RED half); opened once, by clone, at the FIRST WRITE |
| `docs/tranches/V/megatranche/workflows/harvest-journals.mjs` | execute, no write to itself (G-6 invokes it; it writes `registry/harvest/*.json` + `registry/DEFECT-LEDGER.md` — both named below) |
| `docs/tranches/V/megatranche/registry/harvest/x-p-w0.json` | create (harvest output for this lane's workflow) |
| `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` | modify-append (harvest appends; never rewritten by hand) |
| `docs/tranches/X/COHESION.md` | modify-carve (§0 X·P status row + §5 status board line only) |

Do NOT touch, under any circumstance, at any point in this wave:

- `/Users/mkbabb/Programming/parse-that` — the read-only evidence root, **and the shared repository
  of six frozen worktrees**. It carries uncommitted modifications at census (`.cargo/config.toml`,
  `README.md`, `rust/Cargo.lock`, `rust/parse_that/src/lib.rs`,
  `rust/parse_that/src/parsers/scan/decode.rs`, +). Those are evidence. Do not stash, restore,
  commit, prune, gc, or `git worktree prune` — the last would delete the `prunable` registry entry
  for `/private/tmp/parse-that-m2-baseline-20260729`, which is itself a record.
- `/Users/mkbabb/Programming/parse-that-css-totality`, `…-p1-e`, `…-p1-r`, `…-p1-vk`,
  `/Users/mkbabb/Programming/parse-that-runtime-probes`, `/Users/mkbabb/Programming/parse-that-skv26`
  — the six frozen sibling worktrees, including
  `parse-that-css-totality`'s two untracked files (`data`, `docs/tranches/B/PARSER-RESURRECTION-HANDOFF-2026-07-31.md`,
  the latter being provenance finding F-6's uncommitted handoff).
- `~/.codex/**` (all three worktrees: `7e28`, `9167`, `d0be`) — read/hash only, C-02 discipline.
- `~/Documents/Codex/**` — TCC-walled and preserved either way; the builder is "frozen at this
  pause boundary and must not be edited or run in place", and the `__pycache__` residue must not be
  removed, regenerated, normalized, or credited.
- `/Users/mkbabb/Programming/value-css-totality-audit`, `/Users/mkbabb/Programming/value-xw1-demo-boot`
  — value.js sibling worktrees off this lane's path.
- `/Users/mkbabb/Programming/.p-totality/**` — evidence archives, including the 323,894,591-byte
  `evidence-archive-2026-08-03.tar.zst`.
- `src/**`, `demo/**`, `api/**`, `test/**`, `e2e/**` in this repository, `scripts/dev/dev.sh`
  (unowned, never touched), and every `docs/tranches/X/waves/W*.md` (X·V's own files).

### 4a. Disjointness

No two units share a `modify` or `modify-carve` path. `.a` creates `PAUSE-AUTHENTICATION`; `.b`
creates `FROZEN-ROOTS-CENSUS`, `roots-census.sh`, and `census-before.txt`; `.c` creates the fresh
root and nothing else; `.d` creates `EVIDENCE-CHAIN.md`, `x-p-w0.json`, `census-after.txt` and holds
the sole `modify-carve` on `COHESION.md`. `DEFECT-LEDGER.md` is appended by a script, by one unit
(`.d`), once.

**Cross-wave.** `docs/tranches/X/parse-that/**` is a virgin subtree — no X·V, X·KF, or X·F wave
names any path under it (`docs/tranches/X/waves/W0..W11.md` bound `docs/**` only through their own
enumerated tables; `COHESION.md:18` reserves this directory to X·P by name). `COHESION.md` is
touched by the cohesion spine's owner (the session root) and by this wave's `.d` — the carve is two
lines and is taken at close, after the root exists, so no concurrent writer can be mid-edit.
`harvest-journals.mjs` and `DEFECT-LEDGER.md` are shared infrastructure across every lane; the
harvest is idempotent and append-only by construction (L-13), which is why the row is
`modify-append` and not `modify`.

### 4b. Worktree Plan

Phase 1's two units (`.a`, `.b`) write disjoint new files under `docs/tranches/X/parse-that/` and
commit before phase 2 dispatches; phases 2 and 3 are single-unit and serial. **No sibling git
worktree is created for this wave** — and the reason is load-bearing rather than incidental: this
wave's subject matter *is* worktree hygiene, and adding a value.js worktree while asserting that no
repository was mutated invites exactly the confusion G-3 exists to foreclose. All four units run in
the primary checkout at `/Users/mkbabb/Programming/value.js`. No Rust target dir applies
(`CARGO_TARGET_DIR` N/A); the fresh root's own toolchain is X.P.W1's concern.

## 5. Agent Units

**Model seats (M-23; an undeclared seat is a defect).** Every unit in this wave is mechanical —
hashing, enumeration, a clone, and a script invocation. There is no design work and no adjudication.
Therefore **all four units are Opus 5 implementation seats (M-23 §2)**. If a §9 STOP fires, the
triumvirate that §3a mandates is the first Fable seat this lane spends.

### X.P.W0.a Pause authentication ledger

- **Goal**: every identity the pause handoff asserts is re-derived from disk today, so no later
  reader has to trust a quoted digest.
- **Mechanism**: Opus 5 seat (M-23 §2). For each row of §6 G-1's table run the literal command and
  paste its literal output. Record one of three dispositions per row — **MATCH** (digest equals the
  handoff's), **MISMATCH** (digest differs; halt per §3a), **EPERM** (the path is unreachable; the
  digest is *unknown*, and the row says so). Additionally re-derive the two in-repo `ADOPT-COPY`
  digests and explain their divergence structurally rather than waving at it: the in-repo copy of
  the parser-law matrix hashes `0002ed93…` against the original's `244c448a…` because a 685-byte
  provenance comment is prepended, and the original bytes are exactly the **last 30,242 bytes** of
  the copy — a claim the unit verifies with `tail -c 30242 … | shasum -a 256`, not with prose.
  Output is a **dated, append-only** file (M-22 ¶5); no existing `.sha256` packet is edited.
- **Files**: `docs/tranches/X/parse-that/evidence/W0/PAUSE-AUTHENTICATION-2026-XX-XX.md` (create).
- **Sub-gate**: G-1 and G-2 green. Every handoff-named identity appears exactly once with exactly
  one disposition; the count of rows disposed equals the count of rows named; zero rows carry a
  digest the unit did not itself compute this session.

### X.P.W0.b Frozen-roots census and the never-touch law

- **Goal**: an enumeration of every root the parser lane could damage, and a command that proves it
  did not.
- **Mechanism**: Opus 5 seat (M-23 §2). Enumerate the eighteen roots of §6 G-3 with, for each: path,
  git identity (`rev-parse --short HEAD` + branch, or "not a repository", or "EPERM"), file count
  excluding `.git` and `node_modules`, and porcelain status. Record the **shared-repository finding**
  explicitly: `git -C /Users/mkbabb/Programming/parse-that worktree list` returns eight entries, seven
  of them the frozen roots and one `prunable`; therefore the frozen roots have no independent object
  stores, and any write to a branch ref, any `gc`, and any `worktree prune` is a write to all seven.
  Author `roots-census.sh` as the executable form of the law — it prints the enumeration and nothing
  else, writes nowhere, and is invoked twice (§3.6). Capture `census-before.txt` **before** unit .c
  dispatches.
- **Files**: `docs/tranches/X/parse-that/evidence/W0/FROZEN-ROOTS-CENSUS-2026-XX-XX.md` (create),
  `docs/tranches/X/parse-that/evidence/W0/roots-census.sh` (create),
  `docs/tranches/X/parse-that/evidence/W0/census-before.txt` (create).
- **Sub-gate**: G-3's before-half green. `roots-census.sh` exits 0, writes no byte outside its own
  stdout, and its output names every path in §4's do-not-touch list with no omissions and no
  additions.

### X.P.W0.c The fresh root, opened

- **Goal**: `parse-that-css-totality-p2` exists, at the ruled commit, with every other root
  provably unchanged.
- **Mechanism**: Opus 5 seat (M-23 §2). This unit performs the lane's **FIRST WRITE**. Exactly:

  ```
  git clone --no-hardlinks --no-checkout \
      /Users/mkbabb/Programming/parse-that \
      /Users/mkbabb/Programming/parse-that-css-totality-p2
  git -C /Users/mkbabb/Programming/parse-that-css-totality-p2 \
      checkout --detach f5757082ca160dd5f25fcf437e692c9df8f7e78d
  ```

  `--no-hardlinks` is not a stylistic flag. A default local clone hardlinks `.git/objects` into the
  source repository; a later `gc`/`repack`/`prune` in *either* root then operates on shared inodes,
  which is a write to six frozen worktrees by a path nobody audits. `--no-checkout` + explicit
  detached checkout is chosen over `--branch` because the ruling names a **commit**
  (`f5757082`, M-22 ¶3), and `codex/css-totality-combinators-20260729` is a live branch name in the
  shared repo whose tip could move; the detached HEAD pins the ruled bytes and creates no branch ref
  in the clone that invites a push. **No remote is added, no push is ever run, no branch is created,
  and nothing in the fresh root is built, installed, or executed in this wave** — the toolchain is
  X.P.W1's.
- **Files**: `/Users/mkbabb/Programming/parse-that-css-totality-p2/**` (create, once).
- **Sub-gate**: G-4 and G-5 green. HEAD resolves to the full 40-hex
  `f5757082ca160dd5f25fcf437e692c9df8f7e78d`; the clone's `git status --porcelain` is empty; the
  source repository's `worktree list`, `rev-parse HEAD`, `branch -a`, and `status --porcelain` are
  byte-identical before and after.

### X.P.W0.d Evidence chain and routing declaration

- **Goal**: no seat's output can be lost to a session wall, and PLAW-BIND is checkable from this end.
- **Mechanism**: Opus 5 seat (M-23 §2). Author `EVIDENCE-CHAIN.md`: the lane's workflow id, the
  literal journal path (`<session>/subagents/workflows/<runId>/journal.jsonl`), the L-13 rule that
  the harvest runs **after every workflow completion or kill and before any re-deploy**, the
  re-deploy rule (`resumeFromRunId`, never a fresh invocation), and the L-13 corollary that a
  summary is not a result — `componentsRun: N` with every row `NO_JURY` is a failed run, and under
  the owner's ≤4-concurrent-workflow cap this lane never fans out wide enough to manufacture that
  false negative. Run the harvest; file `x-p-w0.json`. Restate PLAW-BIND verbatim from the handoff
  §2 chain and cite `COHESION.md` **§2's X·P release-condition bullet by section anchor plus the
  quoted sentence — never by line number** (G-8: COHESION is a live document; the two literal
  fragment greps are the locator), so the edge is written from both ends. Capture `census-after.txt` and diff it against `census-before.txt`. Carve
  `COHESION.md`'s X·P status row from FORMING to the wave's landed state and add one status-board
  line.
- **Files**: `docs/tranches/X/parse-that/EVIDENCE-CHAIN.md` (create),
  `docs/tranches/V/megatranche/registry/harvest/x-p-w0.json` (create),
  `docs/tranches/V/megatranche/registry/DEFECT-LEDGER.md` (modify-append, by script),
  `docs/tranches/X/parse-that/evidence/W0/census-after.txt` (create),
  `docs/tranches/X/COHESION.md` (modify-carve).
- **Sub-gate**: G-3's after-half, G-6, and G-8 green.

## 6. Hard Gate

Eight conditions, **all eight RED at authoring** — with G-2's RED being of a distinct kind: not a
failed assertion but a *recorded denial*, which is precisely the state that gate exists to make
un-loseable. Every baseline below was measured read-only on **2026-08-03** against value.js HEAD
`2636c238`, node v26.0.0, darwin arm64; commands and outputs are pasted, none inferred. Each gate
states how it can fail for its intended reason (**L-19**).

**G-1 — HANDOFF IDENTITIES RE-DERIVED.** Every identity the pause handoff names carries a
disposition this session computed.

*RED baseline*, `shasum -a 256 <path>`, 2026-08-03:

```
ced234406d3d9ad6bb13e4dce92452a596c9af55dd2091fd90a83f02502f20f7  docs/tranches/V/megatranche/coordination/PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md
244c448a90059002be1194e6a512191c96881a9413ceb021f3b8fe7b62b504a7  /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/formation/VALUE-PARSER-LAW-CONVERGENCE-MATRIX-2026-07-30.md
aa891714b3b6bb3386afda45201b203ac5aa1f2ef028466f303831197e992767  /Users/mkbabb/.codex/worktrees/7e28/value.js/docs/tranches/V/megatranche/audit/cross-repo/VALUE-CSS-DREI-V11-TWO-REVIEW-OWNER-INTAKE-2026-08-02.md
0002ed933f7628797792c28258c37b150b2eb10a767eb5113db20225757d0f50  docs/tranches/V/megatranche/formation/codex-worktree-7e28/formation/VALUE-PARSER-LAW-CONVERGENCE-MATRIX-2026-07-30.md
```

| handoff row | expected identity | measured 2026-08-03 | disposition |
|---|---|---|---|
| the handoff itself (lane authority) | — (self) | `ced23440…f20f7` | **MATCH** — this is the byte-identity every downstream citation pins |
| parser-law matrix, §3.1 | `244c448a…504a7`, 30,242 B | `244c448a…504a7` | **MATCH** ✓ |
| Value CSS DREI-v11 owner intake, §3.3 | `aa891714…92767` | `aa891714…92767` | **MATCH** ✓ |
| in-repo `ADOPT-COPY` of the matrix | — | `0002ed93…7d0f50`, 30,927 B | **EXPLAINED** — 685-byte provenance header prepended; the original bytes are the last 30,242 (`tail -c 30242 … \| shasum -a 256` must return `244c448a…`) |
| `BUILD-V12.py`, §3.2 | `0732ebc2…64ee8`, 243,827 B | **unreadable** | **EPERM** |
| `BUILD-V12.cpython-314.pyc` residue, §3.2 | `de1d62ff…c00937`, 154,221 B | **unreadable** | **EPERM** |
| v12 target `…/parser-novelty-and-experiment-v12`, §3.2 | **ABSENT** | **unreadable** | **EPERM — not ABSENT** (see G-2) |

**Why this is RED today**: four of the seven rows have no disposition recorded anywhere in this
repository's tracked tree, and the four MATCH/EXPLAINED rows above were computed by the *authoring*
session, not by the wave. The gate closes when the wave's own dated ledger carries all seven with
its own outputs pasted. *Falsifier*: change one byte of the parser-law matrix in the 7e28 worktree
and the gate must go red and halt to the owner — never repair. A row disposed by quoting the
handoff rather than by running `shasum` fails; the ledger records the command per row for exactly
this reason. *Consumer named (L-19)*: the next parser writer, and the handoff §9 instruction to
"treat task IDs as chronology and disk bytes as evidence."

**G-2 — EPERM IS NOT ABSENCE.** The three `~/Documents/Codex` rows are reported as **unreachable**,
and no gate, report, or close document in this lane asserts the v12 target is absent on the strength
of a failed `ls`.
*RED baseline*, 2026-08-03: `ls /Users/mkbabb/Documents/Codex/` → `Operation not permitted`;
`ls /Users/mkbabb/Documents/Codex/2026-08-02/` → `Operation not permitted`. The pause handoff §3.2
records the target as **ABSENT** as of 2026-08-02 by an author who could read the directory; this
session cannot re-derive that, and the difference matters because handoff §9 makes "a present v12
target" a STOP condition. Today, **this lane cannot detect that STOP condition at all** — which is
itself the finding.
*Falsifier*: any sentence in the wave's artifacts of the form "the v12 target is absent" that is not
immediately qualified by the measurement date and the reader's access state fails the gate. Under a
later TCC grant the row converts to MATCH or fires §3a's STOP; the grant does not retroactively make
today's EPERM a verification. *Not a grep gate*: this is a filesystem-permission assertion whose
whole content is the distinction between two failure modes.

**G-3 — EIGHTEEN ROOTS, BYTE-UNCHANGED.** Every preserved root is enumerated with its identity, and
`diff census-before.txt census-after.txt` is **empty**.

*RED baseline*, measured 2026-08-03 (`git -C <root> rev-parse --short HEAD`, `find <root> -type f
-not -path '*/.git/*' -not -path '*node_modules*' | wc -l`):

```
parse-that                          ef10d5b  [master]                                  20261 files   DIRTY (5+ rust paths)
parse-that-css-totality             f575708  [codex/css-totality-combinators-20260729]   757 files   2 untracked
parse-that-css-totality-p1-e        35fd252  [codex/css-totality-p1-e]                   277 files
parse-that-css-totality-p1-r        4175325  [codex/css-totality-p1-r]                   277 files
parse-that-css-totality-p1-vk       a0f122f  [codex/css-totality-p1-vk]                  277 files
parse-that-runtime-probes           99e9862  [codex/runtime-kernel-probes-20260729]      337 files
parse-that-skv26                    e31fbfe  [codex/sk-v26-parse-that]                 51162 files
/private/tmp/parse-that-m2-baseline-20260729   de36d57 (detached)                    PRUNABLE — path gone, registry entry is a record
.codex/worktrees/7e28/value.js      e01d0065 (detached)                                3967 files
.codex/worktrees/9167/                                                                 (enumerate at open)
.codex/worktrees/d0be/                                                                 (enumerate at open)
value-css-totality-audit            dea7a93c [codex/css-totality-prototype-20260729]
value-xw1-demo-boot                 d19da6d3 [codex/xw1-demo-boot-20260729]
~/Documents/Codex/2026-08-02/parser-novelty-v12-construction                            EPERM
~/Documents/Codex/2026-08-02/parser-novelty-and-experiment-v12                          EPERM
~/Documents/Codex/2026-07-29/parser-p4-fresh-sol-adjudication/outputs                   EPERM
/Users/mkbabb/Programming/.p-totality/                          6 entries + evidence-archive-2026-08-03.tar.zst (323,894,591 B)
/Users/mkbabb/.claude/jobs/9e7dadd0/tmp/parser-proof/           EPHEMERAL job tree — the parser-proof harnesses (X.P.W1's port source)
```

**0 of 18 are enumerated in any tracked file today**; `roots-census.sh` does not exist. The
load-bearing structural fact the census must state: `git -C /Users/mkbabb/Programming/parse-that
worktree list` returns **eight** entries, so seven of these roots share **one** object store — they
are not seven independent copies, and a single `gc` reaches all of them.
*Falsifier*: run `git -C /Users/mkbabb/Programming/parse-that gc` (or `worktree prune`, or any
branch write) between the two censuses and the diff must be non-empty. A census that lists roots
without their identities cannot detect that, and fails by construction. `.p-totality` and the job
tree are included **because** they are the two roots most likely to be treated as scratch.

**G-4 — P2: ABSENT TODAY, THEN OPENED AT THE RULED COMMIT.**
*RED baseline*, 2026-08-03:

```
$ test -e /Users/mkbabb/Programming/parse-that-css-totality-p2 && echo PRESENT || echo ABSENT
ABSENT
```

**The RED is the point**: at authoring the root must not exist, and this line is that proof. GREEN
condition, after the wave's act: `git -C /Users/mkbabb/Programming/parse-that-css-totality-p2
rev-parse HEAD` prints exactly `f5757082ca160dd5f25fcf437e692c9df8f7e78d`;
`git … status --porcelain` is empty; `git … rev-parse --abbrev-ref HEAD` prints `HEAD` (detached);
`git … remote -v` prints nothing.
Provenance of the commit, verified 2026-08-03 in the source repository: `f5757082ca160dd5f25fcf437e692c9df8f7e78d`,
dated `2026-08-02 06:49:24 -0400`, subject *"docs(parser-audit): bank N2 v10 two-review terminal
intake and novelty boundary"*, reachable from `codex/css-totality-combinators-20260729` and
**not an ancestor of `master`** (`git merge-base --is-ancestor f5757082 HEAD` → non-zero) — i.e. the
packet commit lives on the frozen branch, exactly as the ruling implies, and cannot be reached by
cloning the default branch.
*Falsifier*: a clone left at `codex/css-totality-combinators-20260729`'s *tip* instead of the pinned
commit passes a naive "the root exists" check and fails this gate, because the tip is a moving ref
in a shared repository. A partial clone (interrupted, per OP-3) leaves a directory that exists and
has no valid HEAD — also red.

**G-5 — OBJECT-STORE ISOLATION.** The fresh root's `.git/objects` shares **zero inodes** with
`/Users/mkbabb/Programming/parse-that/.git/objects`, and `parse-that`'s own state is bit-identical
across the wave.
*RED baseline*: vacuously red — the fresh root does not exist, so the assertion has no subject.
GREEN condition, three checks, all after the clone: (i) the pre/post capture of
`git -C /Users/mkbabb/Programming/parse-that worktree list && git … rev-parse HEAD && git … branch -a
&& git … status --porcelain` diffs to empty; (ii) an inode intersection over
`find <each>/.git/objects -type f -exec stat -f '%i' {} +` is empty; (iii) `parse-that/.git`'s size
is unchanged (**28M** at census — a clone that wrote into the source shows here).
*Falsifier*: perform the opening with `git worktree add` instead of `git clone` and (i) fails
immediately — the worktree registry gains an entry and a branch ref is created in the shared
repository. Perform it with a default `git clone` (no `--no-hardlinks`) and (ii) fails — the object
files are the same inodes. This gate exists because both wrong mechanisms produce a root that looks
correct.

**G-6 — EVIDENCE CHAIN LIVE.** The lane's harvest runs and files its JSON.
*RED baseline*, 2026-08-03: `ls docs/tranches/V/megatranche/registry/harvest/` returns ten entries
including `parser-band.json` — the parser band's *prior* harvest — and **no `x-p-*` file exists**;
`docs/tranches/X/parse-that/EVIDENCE-CHAIN.md` does not exist. The harvester itself is present and
executable (`docs/tranches/V/megatranche/workflows/harvest-journals.mjs`, 5,445 B) and
`registry/DEFECT-LEDGER.md` is 10,494,611 B at census.
GREEN condition: `node docs/tranches/V/megatranche/workflows/harvest-journals.mjs` exits 0 and
`registry/harvest/x-p-w0.json` exists with a non-empty seat list whose count equals the units
dispatched.
*Falsifier*: a workflow summary reporting seats with **zero** journal rows harvested is RED, not
green — L-13's corollary, and the exact shape of the false negative that a rate wall produces. A
run whose harvest JSON has fewer seats than were dispatched fails; the count is the assertion.

**G-7 — PAUSE LAW UNBROKEN.** Each of handoff §7's seven prohibitions has a check that would catch
its violation, and all seven pass.
*RED baseline*: no such check exists in any tracked file. The seven, with their checks: no
`BUILD-V12.py` run (shell history + no new bytes under the construction root — **EPERM-bounded, and
the wave says so**); no v12 target materialized (same bound); no generated parser/auditor source
imported or executed (the fresh root is cloned but **not** installed or run in this wave — G-4's
`status --porcelain` empty is the evidence); no mutation of `parse-that`, Value parser/CSS product
source, package state, tests, benchmarks, caches, or release coordinates (G-3 + G-5 + this repo's
`git status --porcelain -- src test e2e api package.json package-lock.json` empty); the residue not
cleaned (EPERM-bounded); no N4 / parser-law P01 / CSS DREI-v12 / performance-family dispatch (the
wave's unit list is four units, all named above, none of them a family); no parser evidence bound
into non-parser cross-repository input slots (G-8).
*Falsifier*: three of the seven are only checkable under a TCC grant, and the gate **must say so
per-row** rather than reporting seven greens. A blanket "pause law observed" claim fails.

**G-8 — PLAW-BIND DECLARED FROM BOTH ENDS.** The routing law appears in this lane's tree with its
chain verbatim, and the reciprocal declaration is cited by file and **section anchor** — never by
line number: `COHESION.md` is a declared live document (its own header: "live document … kept
current at every boundary"), so a line coordinate rots by design and a gate discharged against a
rotted coordinate is discharged against the wrong bytes. The identity is the quoted sentence,
located by `grep`.
*RED baseline*, 2026-08-03: `docs/tranches/X/parse-that/` contains only `waves/`; the law is
declared from the **other** end only — `docs/tranches/X/COHESION.md` **§2 (the cross-sub-tranche
dependency graph), the X·P release-condition bullet**: ("X·P release condition → KF.W3 … routing is
PLAW-BIND — parser → value (X·V L1/L5 surfaces) → packed release → consumers. **Direct
parse-that→fourier is FORBIDDEN**"). **1 of 2 ends declared.** The bullet wraps across lines, so it
is located by two literal fragment greps rather than by a coordinate:
`grep -n 'X·P release condition → KF.W3' docs/tranches/X/COHESION.md` and
`grep -n 'parse-that→fourier is FORBIDDEN' docs/tranches/X/COHESION.md` — each must return at least
one row, and the returned rows are pasted in the close report in place of a line pin.
GREEN condition: `EVIDENCE-CHAIN.md` carries the handoff §2 chain verbatim
(`parser law or durable no-runtime ruling → Value PLAW-BIND → V.L1 / V.L5 → Value release and rebind
→ packed Value → Fourier F.W0 atomic tuple → F.W1`), names the forbidden edge, and cites
`docs/tranches/X/COHESION.md` **§2, the X·P release-condition bullet**, quoting the sentence rather
than pinning a line. Note for the wave, not a defect of it: `V·L5` is CC-011/C-13's **RETIRE** row —
the label is used in five documents and defined in none, and X-W0.i rewrites the routing law against
`V·L1..V·L4` plus an explicitly defined new cut. This lane therefore states the chain **and** flags
that its own downstream node is mid-rename; it does not silently adopt either spelling.
*Falsifier*: delete the `COHESION.md` citation and the edge is one-directional — the exact X·V W4-D2
defect that made both-ends declaration law. Second falsifier, the one a line pin could not express:
either fragment grep returning **zero** rows means the reciprocal end was rewritten away, and the
gate is RED — whereas a citation of the form `COHESION.md:45` goes on resolving to whatever line 45
now holds (today the SS-13 row) and would report green over the wrong bytes.

## 7. Format And Lint Cadence

Documents have no repository-wide linter. `git diff --check` on every commit; Prettier over the
touched `.md` files at each commit. `roots-census.sh` is checked with `sh -n` (syntax) and run twice
(§3.6) — its second run *is* its test. No `npm run lint` / `typecheck` / `test` runs in this wave:
it touches zero TypeScript and zero product source, and inventing a green test run over untouched
code would be exactly the vacuous evidence **L-19** forbids. The replacement evidence is G-3's
before/after diff and G-5's three isolation checks. **No proof-farm script is authored for any
gate**: seven of the eight gates are `shasum`/`git`/`test` one-liners recorded in the close report,
and the eighth (G-6) invokes an existing harvester.

## 8. Verification Artefacts

- `docs/tranches/X/parse-that/evidence/W0/PAUSE-AUTHENTICATION-2026-XX-XX.md` — the seven-row
  identity ledger with per-row command and literal output, dated, append-only.
- `docs/tranches/X/parse-that/evidence/W0/FROZEN-ROOTS-CENSUS-2026-XX-XX.md` — eighteen roots, the
  shared-repository finding, and the never-touch law.
- `docs/tranches/X/parse-that/evidence/W0/roots-census.sh` + `census-before.txt` + `census-after.txt`,
  with the `diff` output (expected: empty) pasted in the close report.
- The clone receipts: `rev-parse HEAD`, `status --porcelain`, `remote -v` from the fresh root, and
  the pre/post quadruple from `/Users/mkbabb/Programming/parse-that`, plus the inode-intersection
  result and `du -sh parse-that/.git` before and after.
- `docs/tranches/X/parse-that/EVIDENCE-CHAIN.md` and
  `docs/tranches/V/megatranche/registry/harvest/x-p-w0.json`.
- `docs/tranches/X/parse-that/waves/W0-CLOSE.md` — the eight gates with RED-before / GREEN-after
  outputs pasted, and the per-row EPERM disclosures stated as such.
- Commit hashes for each phase; model receipts for all four seats.

## 9. Commit Plan

- `docs(x-p-w0/auth): re-derive the pause handoff's identities — seven rows, three dispositions`
  (unit .a; body carries the four computed digests and names the three EPERM rows).
- `docs(x-p-w0/census): the eighteen preserved roots, the shared-repository finding, and the
  never-touch law` (unit .b; body states that seven frozen roots share one object store).
- `chore(x-p-w0/root): open parse-that-css-totality-p2 at f5757082 by no-hardlink clone`
  (unit .c; body carries the pre/post source-repository quadruple and the inode-intersection result.
  **This commit's diff in *this* repository is empty** — the act is outside the repo; the commit
  exists to timestamp the receipts, which is why the body is mandatory).
- `docs(x-p-w0/chain): evidence chain, harvest, and PLAW-BIND declared from this end`
  (unit .d; body cites `COHESION.md` **§2's X·P release-condition bullet** by anchor + quoted
  sentence, never by line, and flags the V·L5 rename).
- `docs(x-p-w0): close report + evidence` (orchestrator, at close; stamps **IMPLEMENTED** —
  **VERIFIED is the X·P release close's to stamp, never this one** (R-A)).

Pathspec commits only. `scripts/dev/dev.sh` is unowned and never touched.

## 10. Dependencies

- **Depends on**: the owner's begin-word (OP-1); tranche X's ratification (M-22 ¶1); the pause
  handoff `ced23440…` as the lane's authority; `harvest-journals.mjs` (present, 5,445 B).
- **Blocks**: **X.P.W1** (harness, corpus and the honest bench) — W1's every port lands *in* the
  fresh root and cannot begin before it exists; and every later X·P wave transitively.
- **Blocks (cross-repo, gate-keyed not scheduled)**: **KF.W3** keys on this lane's release
  condition, reached only through PLAW-BIND — parser → Value (X·V L1/L5 surfaces) → packed release →
  consumers. Nothing in this wave advances that condition; W0 opens the ground it will be earned on.
- **Not opened here** — handoff §8 steps 3–8, each given an **explicit disposition**, because a
  promise of "later waves" that no wave owns is a dangling forward reference (the X·V repin-census
  orphan's shape). The lane as specified is W0→W1→W2→W3→W4 and ends at W4; the mapping is therefore
  total:

  | handoff §8 step | disposition |
  |---|---|
  | 3 — formula↔predicate-ID bijection (no missing/extra/duplicate/reordered/misbound formula) | **OWNED: X.P.W2 G-2** (OP-BIJECTION, registries not counts) |
  | 5 — every run kind / observation variant / fixture role / expected outcome a closed authenticated union, no fallback | **OWNED: X.P.W3 G-4** (UNION CLOSED, NO FALLBACK) |
  | 8, release limb — "materialize only after a separate owner release" | **OWNER-GATED: X.P.W4 OP-1** — a second dated owner word, distinct from the X·P begin-word; no wave seat may grant it |
  | 4 — split target-S01 mismatch causality from non-target S01 neutrality into distinct predicates, codes, receipts, RR01 aggregates | **DORMANT-UNLESS-NC-0-REVIVED** |
  | 6 — prove owner suppressions, non-owner retentions, invalid suppressions, and control-of-control from production-consumed raw state | **DORMANT-UNLESS-NC-0-REVIVED** |
  | 7 — three fresh independent static reviews over the frozen source design | **DORMANT-UNLESS-NC-0-REVIVED** |
  | 8, sequence limb — the Sol→Luna→fresh-Sol→owner sequence in isolated roots | **DORMANT-UNLESS-NC-0-REVIVED** |

  **DORMANT-UNLESS-NC-0-REVIVED** is a terminal disposition, not a schedule: those four are steps of
  the v12 *generator* program, which `X.P.W2` pre-kills as **NC-0** by citation, and reviving it
  lawfully "requires resume-protocol steps 3–7 plus a separate owner release that no W2 seat may
  grant itself." Absent that release they are owned by **no X·P wave**, and this record — not a
  forward pointer — is their home. They are named here only so no reader mistakes W0's completion for
  resumption of the v12 program. **This wave does not revive `BUILD-V12.py`'s design; it establishes
  the ground on which a successor design may be built.**
- **Not opened here**: `~/Documents` TCC (OP-2) — an owner/system action, not a wave act.

## 11. Archaeology

Three prior attempts at parser resumption, each with its guardrail:

1. **The v1–v11 novelty program**, terminal RED across eleven revisions, with the current builder
   still carrying an inconsistent predicate closure (OR05 declares 18 production predicate IDs over
   a 20-formula required-value domain; the two dropped formulas are caller/post-hoc rejection and
   close chronology, so the last declared IDs are **misbound rather than absent**; the universal S01
   runtime predicate and the non-target S01 law cannot both hold for the same row). *Guardrail*:
   this wave repairs **nothing** in that design and imports none of its source. It opens a root. The
   bijection and the S01 split are handoff §8 steps 3–4, and §10's disposition table is where each
   one's owner is written down — step 3 at `X.P.W2` G-2, step 4 DORMANT-UNLESS-NC-0-REVIVED — rather
   than left to "a wave that has a design to apply them to."
2. **Same-root repair**, explicitly PRUNED by the handoff §6 ("same-root repair, silent re-pin,
   timeout widening, or denominator rewrite"). The construction root was edited in place across
   revisions until its own reviews could not be reconciled. *Guardrail*: M-22 ¶3's fresh-root law,
   made mechanical by G-5 — not "we intend not to touch the old root" but an inode intersection and
   a byte-identical pre/post quadruple.
3. **The receipt that changed.** Provenance finding F-7: dated `.sha256` packets were **rewritten**,
   producing two broken seals (the 07-31 resurrection at 5/6 OK, the eight-hour at 20/24 OK) and the
   ruling that a receipt which changes is not a receipt. *Guardrail*: M-22 ¶5 — this wave's ledger
   is a **new dated file**, append-never-rewrite, and git object IDs are the living receipts. No
   existing `.sha256` is touched, including the ones that are known-broken and marked
   SUPERSEDED-BY-GIT rather than repaired.

## 12. L-18 rider — acceptance is not close

Landing the eight gates green makes this wave **IMPLEMENTED**, not ACCEPTED. Per **L-18**, no
implemented wave is ACCEPTED until it has survived **two** challenging gestalt passes, each a
**quartet of Opus 5 skeptics** working the three altitudes (total-tranche: was opening the fresh
root the optimal first act in the greater plan · wave · feature), each assuming the implementation
is wrong on some reasonable basis. Bases with obvious purchase here: a "verification" that quotes
the handoff's digests instead of computing them; an EPERM silently rendered as ABSENT; a clone that
mutated the shared repository in a way the before/after quadruple was too coarse to see; a census
that enumerates roots without identities and therefore cannot detect a change; a harvest whose JSON
has fewer seats than were dispatched. Superlatives are reported alongside defects, each with
provenance and proof. Both quartets are then adjudicated and agglomerated by a **fresh Fable
instance** into an apotheosis; any incongruity surfaced is fully addressed, ratified, and dispatched
— never parked.

**L-20** binds this file: it is re-validated against
`docs/precepts/instructions/tranche/WAVE_SPEC.md` at authoring, at every amendment, and at each
L-18 pass.

Reference, not ceremony: this section changes who signs the wave off, not what §6 measures.
