SERVED MODEL: claude-opus-5[1m]

# NEGATIVE-CONTROL FIXTURE CONTRACT, CLEAN — X.P.W4.g (NOT the seam contract)

The POSITIVE control for the three negative ones: a synthetic `SEAM-CONTRACT.md` in the real one's
shape over the fixture universe's three names, carrying no defect. It must read GREEN. Each of the
three negative-control contracts beside it is this file with exactly ONE cell changed, so a RED
verdict names the change and nothing else.

<!-- SEAM-ROWS:BEGIN -->

| #   | export    | kind    | signature               | candidate provider | disposition                                        | consumer direction |
|-----|-----------|---------|-------------------------|--------------------|----------------------------------------------------|--------------------|
| 1   | `alpha`   | runtime | `function(s: string)`   | fixture            | PENDING-ADJUDICATION [`GROUND-C`]                   | WIDENS (fixture)   |
| 2   | `beta`    | runtime | `function(s: string)`   | fixture            | identical (`NC-DEAD` RETIRED — fixture ledger §10)   | NO CHANGE          |
| 3   | `gamma`   | runtime | `function(s: string)`   | fixture            | declared-divergence [`NC-LIVE`]                      | NARROWS (fixture)  |

<!-- SEAM-ROWS:END -->

<!-- SURFACE-WIDE:BEGIN -->

No fixture ledger row is surface-wide; both name an export in a subject field.

<!-- SURFACE-WIDE:END -->

## Non-goals

Relative colour is recognised-not-validated, and `color-mix()` is a both-sides gap. Named here
because check K reads this section and not the file at large.
