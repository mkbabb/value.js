SERVED MODEL: claude-opus-5[1m]

X.P.W4.e2 — E-w4e-2 CURE (a): the latch reader rides the package's own `./packrat` subpath, and the
`tsx/esm/api` runtime import DIES from the shipped bytes. COHESION §0aa · W4.md FOURTH dated addendum.
NEW dated file, beside `.e`'s `c2-built-entry-surface-2026-09-19-w4e.txt` — nothing overwritten (E-3).

THE CURE, in three lines of consequence:
  src/parse/packrat-entry.ts   + export { packratEnter, packratExit } from "./packrat.js";
  src/css/bounds.mjs           loadPackratInstrument() now awaits import("@mkbabb/parse-that/packrat")
                               and import("@mkbabb/parse-that"); the createRequire/tsImport pair is gone.
  dist/**                      regenerated by `npm run build` (gitignored; it reaches the gate only
                               through `npm pack`'s `files` list, which is why G-3 was re-run AFTER).
A module-local "have we armed" flag was REFUSED, as §0aa and `.e` both state: it would ANSWER the
question latch.test.ts L-3 exists to MEASURE.

════ 1 · THE SHIPPED BYTES ARE tsx-FREE ══════════════════════════════════════════════════════════
⟨cmd⟩ grep -rn "tsx" src/css/                    (in <p2>/typescript)
  src/css/bounds.mjs:823: * `require_("tsx/esm/api")` and `tsImport("../parse/packrat.ts", …)`: the library's TypeScript
  src/css/bounds.mjs:824: * SOURCES, loaded through a loader. `src/css/**` is shipped and `src/parse/**` is not, and `tsx` is
  src/css/lowering-js/js-alg.mjs:38: * `import { tsImport } from "tsx/esm/api"` and this line read
  src/css/lowering-js/js-alg.mjs:42: * works. Inside a consumer it cannot: `src/parse/**` is not shipped and `tsx` is not a dependency of
  src/css/lowering-js/js-alg.mjs:45: * seam symbol could resolve — G-3 read `resolved 0 of 52` against exactly this line. Declaring `tsx`

⟨cmd⟩ grep -rn "tsx" src/css/ | grep -vE ":[0-9]+: *(\*|//|/\*)"     (CODE hits only)
  -> 0 code hits. All five remaining hits are doc-comment prose recording the removal
     (js-alg.mjs:38,42,45 are .e's; bounds.mjs:823,824 are this unit's).

⟨cmd⟩ tar -xzf <the packed tarball> && grep -rn "tsx" package/src/css/ | grep -v comment-prefixed
  (no output) -> 0 code hits over the SHIPPED src/css bytes

⟨cmd⟩ grep -rn "require_\|tsImport" package/src/css/          (every hit, so none is hidden)
  unpack/package/src/css/bounds.mjs:823: * `require_("tsx/esm/api")` and `tsImport("../parse/packrat.ts", …)`: the library's TypeScript
  unpack/package/src/css/bounds.mjs:833: * it was. `tsImport` did not dedupe: each call yielded its own module namespace with its own
  unpack/package/src/css/lowering-js/js-alg.mjs:38: * `import { tsImport } from "tsx/esm/api"` and this line read
  unpack/package/src/css/lowering-js/js-alg.mjs:39: * `await tsImport("../../parse/index.ts", import.meta.url)` — the library's TYPESCRIPT SOURCES,
  unpack/package/src/css/lowering-js/index.mjs:16:// `tsImport` of the same library would be a second `Parser` class, and `instanceof` across the two
  -> every one is comment prose; no executable occurrence survives in the tarball.

⟨cmd⟩ the installed manifest's runtime dependencies
  dependencies = {}
  -> R-3 (the close's residual: a shipped lazy require of a TypeScript loader against deps {})
     is DISCHARGED at the bytes, not disclosed.

════ 2 · THE SUBPATH CARRIES THE READER, FROM A CONSUMER'S node_modules ══════════════════════════
  @mkbabb/parse-that/packrat exports : memoize mergeMemos packratEnter packratExit resetPackrat
  packratEnter / packratExit present : true
  ONE shared chunk (memoize identity) : true
  Parser on the root entry            : true
  packratEnter() while unarmed        : null

════ 3 · THE INSTRUMENT'S READING IS PRESERVED WHOLE — BEFORE vs AFTER ═══════════════════════════
Both readings taken by THIS seat, with the same probe, in the order latch.test.ts reads it:
parse through `src/css/entry.mjs` first, then L-1 read, L-2 arm, L-3 reset-and-read-back, then the
published record. BEFORE was measured against HEAD 93bcb83's bounds.mjs (tsImport), AFTER against
the settled bytes of this unit.

  reading                                    BEFORE (tsImport)   AFTER (./packrat subpath)
  ----------------------------------------   -----------------   -------------------------
  parsing armed the latch?                    false               false
  L-1 readable, and reads false                false               false
  L-2 arms                                     true                true
  L-2 read-back                                true                true
  L-3 after resetPackrat()   [BORN-RED]        true                true
  record.readable                              true                true
  record.armed                                 true                true
  record.symmetric           [BORN-RED]        false               false
  record.sites                                 PACKRAT_LATCH_SITES, unchanged

Field for field identical. ESC-c1's born-RED (`resetPackrat()` clears the tables and leaves
PACKRAT_ARMED true — the one-way latch) is still MEASURED red, not asserted away: that is the whole
reason cure (b) was refused.

WHAT THE CURE MADE STRONGER, stated rather than smoothed: `tsImport` did not dedupe, so the
instrument read ITS OWN library instance and could not claim to read the one the lowerings hold.
`dist/parse.js` and `dist/packrat.js` share one chunk, and `js-alg.mjs` addresses the library through
the same export map — so the instrument now reads THE LATCH THE JS LOWERING ITSELF HOLDS, and the
"parsing does not arm the latch" leg became a live assertion about the candidate's own instance.
It still reads false.

AFTER, verbatim:
  {
   "sites": {
    "module": "typescript/src/parse/packrat.ts",
    "declaration": 158,
    "reader": 224,
    "reset": 273,
    "arm": 297,
    "publishedDistCoordinates": {
     "false": 678,
     "true": 722,
     "readAt": [
      682,
      714
     ]
    }
   },
   "afterParsingArmed": false,
   "L1_readableFalse": false,
   "L2_arms": true,
   "L2_readBack": true,
   "L3_afterReset": true,
   "record": {
    "sites": {
     "module": "typescript/src/parse/packrat.ts",
     "declaration": 158,
     "reader": 224,
     "reset": 273,
     "arm": 297,
     "publishedDistCoordinates": {
      "false": 678,
      "true": 722,
      "readAt": [
       682,
       714
      ]
     }
    },
    "readable": true,
    "before": true,
    "armed": true,
    "afterReset": true,
    "symmetric": false
   }
  }
