SERVED MODEL: claude-fable-5-1

# X.P.W4.f2 — the ten gates at a FRESH adjudicator's own clock (2026-09-19; UTC 2026-09-20 03:39–03:41)

Every reading double-run; both writing scripts given `--out` into the session scratchpad; the raw
outputs banked beside this file as NEW dated files (E-3 — nothing under `evidence/W4/**` from an
earlier seat was touched). value.js HEAD `8afe7145`; `<p2>` HEAD `fede7d3`. Raw JSON/txt files carry
no header line because they are the commands' verbatim output; this file is the receipt over them.

| gate | ⟨cmd⟩ | reading (run 1 ≡ run 2 unless stated) | verdict |
|---|---|---|---|
| **G-1** | `node docs/tranches/X/parse-that/scripts/seam-contract-check.mjs SEAM-CONTRACT.md evidence/W3/universe-52.json DIVERGENCE-LEDGER.md` | rows `contract 52 · universe 52 · ledger rows 48`; set-differences ∅/∅; census `identical 45 · declared-divergence 7`; §10 retirements read (`CN-2, CN-3, R4`, 37 bindings released); ADJUDICATION §2 read (6 rows terminal); **`VERDICT: RED — 2 check(s) failed`**: **[F]** `parseStylesheet: unknown id F-w4f-1` · **[J]** `§11.4 F-w4f-2` (a ledger row naming no export, not declared surface-wide); **EXIT=1**, `diff -q run1 run2` silent | **RED** — `g1-w4f2-2026-09-19.txt` |
| **G-2** | `git status --porcelain -- src api demo test e2e` · `git show --name-only --format=` over every X.P.W4S commit | 13 dirty rows, all `demo/**` + `e2e/**` — a Track-A seat's (X·P wrote none); **0** paths under `src/demo/api/test/e2e` in any X.P.W4S commit (`93bcb83` … `d357aad9`) | **GREEN for X·P** |
| **G-3** | `node scripts/packed-candidate-surface.mjs --seam …/SEAM-CONTRACT.md --out <scratch>` (in `<p2>/typescript`) | `tarballSha256 004bbcce02e08b89…` (moved from `.e2`'s `f8aede11…` because `fede7d3` changed the packed grammar bytes) · `entryCount 91` · **`resolved 52 of 52`** · `refusals 5` · `G3 GREEN` · **EXIT=0** ×2 | **GREEN** — `packed-surface-2026-09-19-w4f2-run{1,2}.json` |
| **G-4** | `node scripts/wasm-admission.mjs src/css/build/ac1.wasm` | `functionKindImportsTotal 0 · unaccountedImportsTotal 0 · "verdict": "GREEN"`, **EXIT=0** ×2 (runs differ only in `generatedAt`); `shasum -a 256 ac1.wasm` → `f0d063d6241812de…`, 662339 B, last commit `93bcb83` — the artefact `.h` did not rebuild (the module is assembled at runtime from the class tables) | **GREEN** — `wasm-imports-2026-09-19-w4f2.txt` |
| **G-5** | `node scripts/rc-p-evaluate.mjs --version 4.0.0 --out <scratch>` | `RC-P(4.0.0) = FALSE — 3 of 6: 1 PUBLISHED(V) · 3 EQUIVALENCE(V) (arm V read 20962 mirror-defects over V's installed /css, full corpus, no limit) · 4 ADMITTED(V)` (2 · 5 · 6 TRUE); **EXIT=1** ×2, tables identical line for line. Negative control `--version 0.0.0-does-not-exist` → conjuncts 1–4 `MEASURED: NO`, FALSE, no conjunct TRUE unmeasured, EXIT=1 | **GREEN — honestly FALSE** — `rc-p-evaluation-2026-09-19-w4f2.{json,txt}`, `rc-p-negative-control-2026-09-19-w4f2.txt` |
| **G-6** | `grep -rn 'RC-P' docs/tranches/X/keyframes/waves/ docs/tranches/X/fourier/waves/ \| wc -l` | **25** hits over **5** files | **GREEN** |
| **G-7** | `grep -c 'parse-that' ../fourier-analysis/package.json`; same over `web/package.json` | **0** · **0** | **GREEN — floor held** |
| **G-8** | `ls -l RELEASE-PACKET.md`; `grep -c 'RELEASE-PACKET\|RC-P\|SS-6' INBOX.md`; the positional Status-cell awk (column 6) | packet **26645 B**; `RELEASE-PACKET` 2 · `RC-P` 2 · `SS-6` 28; **`ROWS=77 UNREAD=0`** ×2 | **GREEN** |
| **G-9** | `W4-CLOSE.md` §4 | disposition **(C)**, quoted by id from COHESION §0i.1; **0** paths under `docs/tranches/X/waves/` in any X.P.W4S commit | **GREEN** |
| **G-10** shape (a) | `for f in W[0-4].md; do awk '/^### Four-verb status/{t=1;next} /^#/{t=0} t' "$f"; done \| grep -c 'VERIFIED \| \*\*NO\*\*'` | **5** ×2 (the correct open-state); `grep -c '^### Four-verb status' W[0-4].md` → `1 1 1 1 1` (OP-6); `grep -rn VERIFIED W[0-3]-CLOSE.md` → 10 lines, every one disclaiming the stamp | **RED (withheld)** — the act is not performed while G-1 is RED |

**GREEN: G-2 · G-3 · G-4 · G-5 · G-6 · G-7 · G-8 · G-9. RED: G-1 · G-10.** The stamp is WITHHELD by
gate id **G-1** (§6 G-10's falsifier: *"a stamp performed while any of G-1..G-9 is red fails"*).

## Why G-1 is RED — measured at the ledger's bytes, not the checker's

The checker's ledger row reader is `line.match(/^### (.+?) — /)` (`seam-contract-check.mjs:112`),
the grammar every §6 row and `.f`'s §10 (`#### §10.x`, deliberately one level down) conform to.
`.h`'s `DIVERGENCE-LEDGER.md` §11 (`e31b8450`) does not:

1. **[F]** — the `F-w4f-1` row lives in a `| rulingId | subject | … |` table under `### §11.1 The row`
   (no ` — `), so the reader mints **no ledger row with id `F-w4f-1`**; `SEAM-CONTRACT.md` row 19
   (`parseStylesheet`) cites `` `F-w4f-1` `` in its disposition, and check F reads an id that is
   neither a ledger row nor a §0w ruled class.
2. **[J]** — `### §11.4 F-w4f-2 — DISCHARGED, so it opens no row here` DOES match the reader, so a
   phantom row `§11.4 F-w4f-2` is minted; it names no export and is not in the contract's
   `SURFACE-WIDE:BEGIN/END` block. This is the exact trap `.f`'s receipt recorded avoiding
   (*"a first draft with `###` made check J fire on four phantom rows"*).

Both are the ledger's form against the checker's standing grammar — not the checker's vocabulary
(`.g`'s cure and its three negative controls stand). `DIVERGENCE-LEDGER.md` is in no row of this
unit's writable set; the cure is returned by id (E-w4f2-1) to the triumvirate.
