SERVED MODEL ID (fold author, line-1 declaration): claude-fable-5

# X·P REFINEMENT FOLD — 2026-08-06 · the one dated addendum

## 1. Header — provenance, posture, the reading rule

**Provenance.** The fifteen parse-that module apotheoses — `pt-ansi · pt-core · pt-debug ·
pt-diagnostics · pt-index · pt-lazy · pt-leaf · pt-packrat · pt-packrat-entry · pt-parser ·
pt-parsers · pt-split · pt-state · pt-utils · pt-utils-entry` — are adjudicated and banked at
`docs/tranches/V/megatranche/registry/adjudicated/pt-*.md`, the set completed by commit
**`1a3e9af1`** ("PARSE-THAT ADJUDICATION COMPLETE 15/15 MODULES — the X·P refinement-fold input
set is whole"). This addendum is the M-23/M-27 ¶2 fold of that whole input set onto the X·P wave
corpus.

**E-3 posture (addenda, never patch).** The dated wave specs
`docs/tranches/X/parse-that/waves/W0.md..W4.md` and `waves/CONFORMANCE-2026-08-03.md` are **NEVER
edited**. This file is the fold's **sole write**. Where this addendum strikes or corrects a phrase
in a dated spec, the strike is recorded here and the spec's bytes stand untouched — the dated
record is immutable; the addendum is the later adjudicated word.

**The reading rule.** At execution, each wave reads **its dated spec PLUS this addendum**, in that
order, and where the two disagree the addendum's clause governs. Every W1 measurement seat
additionally reads §7 (MEASURE-AT-OPEN) as the single authoritative wave-open measurement list.
Nothing here re-stages, re-weights, or re-composites the specs' gates (OC-2/CC-096 staging stands);
this addendum sharpens predicates, adds corpus rows and riders, and records exclusions.

**Fold classes** (the collator's, kept): **cl.1** = spec sharpening / law / correction · **cl.2**
= cure-lock / binding contract clause · **cl.3** = MEASURE-AT-OPEN row (§7) · **cl.4** = EXCLUDED,
does not fold (§8).

**Arithmetic (post-L-20 pass-3 repair, 2026-08-07) — THE OUT SIDE, AND ONLY WHAT THIS DOCUMENT
ITSELF ENUMERATES.** Every figure here is countable from this file's own bytes: **144 FOLDED** —
**129 per-wave sharpenings**, contiguously numbered with exactly one provenance line each
(W0 ×2 · W1 ×23 · W2 ×77 · W3 ×12 · W4 ×15), plus the **15** consolidated MEASURE-AT-OPEN module
rows of §7 — **+ 16 EXCLUDED** (§8's numbered blocs) = **160 BOOKINGS OUT**. The **six** L-20
splits and recoveries that carry the per-wave total from the first pass's **123** to **129** are
each a SPLIT of a multi-facet or multi-limb row, or a recovered drop — never a new identity:
W4.12's **M4** facet →
W2.76 (§1a n.6); W1.16's **LZ-19** limb → W2.77 (§1a n.8); pt-utils-entry **PT-UE-9**'s **W0** limb
→ W0.2 (§1a n.10); pt-lazy **LZ-17**'s **W4** release-condition limb → W4.15 (§1a n.13); and the
two `REFINEMENT FOLD`-routed drops recovered at W1.23 and W2.75 (§1a n.7). Two rows that once
carried a second, unstated booking of another row's identity now carry a CROSS-REFERENCE instead
and are counted ONCE (W2.38 → W2.65, §1a n.9; W2.28 → W2.34, §1a n.12): the count does not move,
because no row was ever added for them.

**The IN side is a RECEIPT, NOT DOCUMENT-INTERNAL ARITHMETIC.** The collator's intake figures —
**155 collated rows**, **+2** recovered by the L-20 fold check = **157 IN**, less the one
row-collapsing duplicate merge (§1a n.2) — are **workflow-journal receipts** (journal
`wf_14d5549b-eda`); the collated 155-row set is **enumerated NOWHERE in this file**. This addendum
therefore asserts **no in-equals-out identity**: the OUT side above is falsifiable against these
bytes, the IN side only against the journal, and it is carried here as provenance, never as proof.

### 1a. Collator challenged — corrections, never silent

*Entries **1-4** are the collator corrections recorded at authoring; entries **5-7** are the L-20
fold check's **first-pass** repairs, entries **8-10** its **second-pass** repairs, and entries
**11-16** its **third-pass** repairs — all landed in place 2026-08-07 (dated specs still
byte-untouched — E-3 holds). Each pass swept an axis the prior pass had not: pass 1 the DROP axis
(rows with no home), pass 2 the ROUTING axis (rows with the wrong home) and the merge register,
pass 3 the LIMB and POINTER axes (limbs of two-wave rows, internal cross-references, and the
epistemic status of every "measured" token).*

1. **pt-index C-i1 re-routed W4 → W1.** The collator targeted the O-15 cite correction at W4; but
   the rule is "correct the letter's cite ONLY where downstream texts quote it," and the live
   downstream quotes of the `:881` cite in the tracked X·P wave corpus are **THREE**:
   **W1.md:476** (G-5's RED-baseline paragraph), **W3.md:497** (G-8's RED baseline), and
   **W2-opus-author.md:432** (the EQ-4 "reachable only together" argument, quoting
   `dist/packrat-entry-*.js:881`). **NO W4 TEXT QUOTES THE CITE** — which is the whole warrant for
   the re-route, and the third site does not touch it. Folded once at W1 (row W1.22) with the
   W3.md:497 and W2-opus-author.md:432 anchors listed. (**L-20 repair**: the earlier "the only two"
   was an overbroad enumeration; the conclusion it carried is unchanged.)
2. **pt-ansi row 11 merged into pt-core row 3.** One identity collated twice. **WARRANT CORRECTED
   (L-20 repair)**: the cross-book note lives in **pt-core row 3**, NOT in the ansi record —
   pt-core.md row 3 carries "**Identity note**: pt-ansi row 11 recorded this same fact from the
   neighbor module at MAJOR — ONE defect, cross-booked, **primary here**", while pt-ansi row 11
   (C-D-6) carries a bare `**W4** — RC-P chunk-topology unit` disposition with no identity note at
   all. The merge stands on that note (one identity, primary pt-core) and the fold-count reduction
   with it; only the attribution of the warrant was wrong. Folded once at W4 (row W4.3), dual
   provenance carried. Fold count reduced by one accordingly.
3. **pt-lazy A.5 reversal — routing annotated, not moved.** The collator routed the reversal to
   W2; the apotheosis's own closing verdict names "exactly the W3 totality frame, not a
   lazy-specific indictment." Folded at W2 (it binds the Stage-3/5 comparison discipline of the
   candidate field) with an explicit W3 cross-fold clause (row W2.35); recorded here so the
   routing tension is visible, not silent.
4. **Near-duplicate facets kept separate, cross-referenced** (per "merge duplicates once"):
   PT-PARS-8 appears as provenance inside the merged JS-boundary fold (W1.1) *and* as its own
   gate-checkable-law row (W1.4) — distinct content (the RC-1 receipts + the test-fork finding),
   kept, cross-referenced. Likewise pt-core rows 1/4 (one cure, two facets: W2.2/W2.3), and the
   diagnostic-conservation law's two rosters (PT-PARSER-12 at W2.44 ↔ PT-LEAF-3 at W2.39 — one
   law, two owners: merge-never-destroys-D vs terminal totality at EOF).
   **DUAL RESIDENCE — DECLARED, NEVER INFERRED** (row 13's declaration added by the L-20 pass-3
   repair, n.11). Exactly TWO source rows live BOTH inside a folded row and inside an §8 excluded
   bloc, and both do so for the same stated reason — the LAW folds, the IN-LIBRARY CURE does not:
   **pt-core row 11** (PRIMARY provenance of W3.9 · pt-core EXCLUDED bloc 5) and **pt-ansi row 13**
   (co-identity of W2.8 · pt-ansi EXCLUDED bloc 4). No other row has two residences.
   **THE COMPLETE MERGE REGISTER** (rewritten by the L-20 pass-2 repair; the first pass's "the ONE
   pair that is MERGED, not kept separate" was **FALSE against this document's own labels** — at
   least four other rows carry a MERGED label over a named pair. **Completed again by the pass-3
   repair, which found the register itself short one row — W2.8, n.11**). Exhaustively, every row
   here that collapses TWO OR MORE NAMED SOURCE ROWS — or two bookings of ONE source row — into ONE
   booking:
   - **W1.12** ⟨pt-packrat C-7 · pt-packrat-entry M-7 / E-B1 · **pt-lazy LZ-21**⟩ — one identity,
     restated per-format. LZ-21 is a source-stated **ride** ("Identity: rides pt-packrat C-7's
     REFINEMENT FOLD … never re-booked here"), carried in provenance, never a second booking.
   - **W2.8** ⟨pt-debug B-1 (= pt-ansi row 13 / M-1)⟩ — ONE caret-coordinate defect read from two
     modules, collapsed under an explicit "same identity" label; the two apotheoses disagree on
     OWNER and the disagreement is resolved, not silenced, at n.11. Added by the pass-3 repair.
   - **W2.32** ⟨pt-lazy LZ-9 · pt-packrat M3⟩ — one identity by LZ-9's own disposition; booked at
     W2, cross-referenced from W4.12 (n.5).
   - **W2.34** ⟨pt-lazy LZ-5 / A.6⟩ — the SOLE booking of LZ-5 / A.6; W2.28, which formerly also
     carried it as a titled subject, now cross-references it and books it no more (n.12).
   - **W2.44** ⟨pt-parser PT-PARSER-12 · pt-leaf C-miss-3⟩ — ONE LAW, two rosters (with W2.39).
   - **W2.65** ⟨pt-state PT-STATE-7⟩ — the SOLE booking of PT-STATE-7; W2.38, which formerly also
     carried it, now cross-references it and cites it no more (n.9).
   - **W3.3** ⟨pt-diagnostics roster rows 8 and 9⟩ — 9 folds via 8.
   - **W4.3** ⟨pt-core row 3 · pt-ansi row 11⟩ — one identity cross-booked, primary pt-core; the
     merge n.2 records as reducing the collated fold count by one.
   - **W4.7** ⟨pt-ansi rows 4+5 · pt-core row 47 · pt-diagnostics row 17⟩ — one acceptance list.
   Distinct in kind from all nine are the five **MERGED CROSS FOLD** rows (**W1.1 · W1.2 · W1.3 ·
   W4.1 · W4.2**), which fold MANY witnesses of ONE invariant drawn from many modules and name every
   witness in provenance: cross-folds by construction, not duplicate-collapses.
5. **IDENTITY MERGE (L-20 repair): pt-lazy LZ-9 ≡ pt-packrat M3 — ONE ROW, kept at W2.** LZ-9's own
   disposition declares `identity: pt-packrat M3, one row`; pt-packrat M3 is filed
   `MINOR → REFINEMENT FOLD: W2's enumeration discipline — dead flag machinery is the
   counter-example class`. The addendum had booked the holding TWICE — at **W2.32** (from the lazy
   side) and again inside **W4.12**'s M3 facet (from the packrat side) — with no stated merge. The
   enumeration discipline is **W2's** (the closed-enumeration class, W2.md:139-142, folded at
   W2.46), so the merged row stays at **W2.32**, carrying the packrat-side consequence (`.trim()`
   mints two ids and burns one against the id ceiling) and dual provenance; **W4.12 keeps a
   CROSS-REFERENCE, not a booking.**
6. **MIS-HOMED WAVE CORRECTED (L-20 repair): pt-packrat M4 re-homed W4 → W2 (row W2.76).** M4's
   holding — **G-10's `memoize = 0` sweep must bind the ROOT BARREL, not only `./packrat`** — is a
   sharpening of **W2's** G-10 (`W2.md:808-812`, the source-direct-idiom structural sweep). It sat
   under §6's W4 heading, where a W4 seat reading "G-10" resolves it to W4's own
   **G-10 — THE R-A STAMP ACT** (`W4.md:521`), an entirely different gate; and `grep -n memoize
   waves/W4.md` returns no sweep row. Re-homed to W2 as its own numbered row at the tail of §4;
   W4.12 cross-references it. The §4 heading count moves with it.
7. **TWO DROPPED ROWS RECOVERED (L-20 repair): pt-lazy LZ-13 → W1.23 and LZ-18 → W2.75.** Both are
   `REFINEMENT FOLD`-routed at source (pt-lazy.md:45 and :50) and appeared NOWHERE in the addendum
   — not folded in §2-§6, not carried in §7, not named in §8's pt-lazy bloc. **LZ-13** (the
   instrument law: the invocation-count red test + the depth-class test are W1 harness obligations)
   folds at **W1.23**. **LZ-18**'s two W2-RETIRE-discharged halves stay absorbed by the retire
   verdict, as its disposition directs; its **surviving half** — the variance/typing posture binding
   W2's candidate typing — folds at **W2.75**. No whole ROW was missing **ON THE DROP AXIS** —
   and that is all this entry certifies (**narrowed by the L-20 pass-2 repair, and narrowed AGAIN
   by the pass-3 repair**): it swept for whole rows with NO home, never for rows with the WRONG
   home, and never for a single LIMB of a two-wave row. The **routing** axis is swept at **n.8**,
   which found one mis-home and two narrowed limbs; the **limb** axis is swept at **n.13**, which
   recovered pt-lazy LZ-17's dropped W4 rider and banked two further limb-level gaps. The
   unqualified sentence this entry once carried — "nothing else was missing" — did not survive
   either sweep and is struck.
8. **WAVE MIS-HOMING CORRECTED, AND THE ROUTING SWEEP RUN (L-20 pass-2 repair): pt-lazy LZ-19
   re-routed out of the W1.16 bundle → W2 (row W2.77).** LZ-19's disposition reads
   `**REFINEMENT FOLD → W2** registry discipline: one tag per producer (or typed `args[0]`) for any
   registry-comparing gate` (`pt-lazy.md:51`), and the registry-comparing gates are **W2's G-10 and
   G-11** (`W2.md:808-838` — the structural graph-walk and the scan-capability census). It was
   folded at **W1.16**, where a W1 seat resolves "G-10" to W1's own **G-10 — THE FIVE DEBTS ARE
   MEASURED, NOT ASSUMED** (`W1.md:564-580`), an entirely different gate: the same defect class as
   n.6, the same harm. **W1.16 now carries only its genuine W1 limbs** — LZ-20 (source-routed
   `REFINEMENT FOLD → W1 §3.9`) and LZ-22 (source-routed `REFINEMENT FOLD → W1` evidence law) — and
   the two rows cross-reference each other in both directions.
   **THE SWEEP ITSELF — the axis n.7 never covered.** Every folded row's section was compared
   against its source row's stated wave, across all fifteen apotheoses. Result: **one mis-home**
   (LZ-19, above) and **two silent limb-narrowings**, both curable in place without a new row and
   both cured:
   - **pt-lazy M11** routes "strike 'stable property' from **W1/W2** prose"; W1.3 struck only
     `W1.md:553-554`, while the identical clause ships at `W2.md:832` (G-11's pasted born-RED
     baseline). W1.3's strike now names **both** sites.
   - **pt-leaf PT-LEAF-24** routes a W1 alloc-count oracle **AND** a W2 law ("the deliverable is
     allocated after the first commit point"); W1.17 carried only the W1 half. The W2 law now
     stands beside it, as PT-LEAF-25's two limbs in the same row already did.
   Everything else verified CORRECTLY HOMED **except the two rows the pass-3 repair names — W1.9
   (n.16, an unstated re-home whose body nonetheless resolves the gate correctly) and pt-lazy LZ-17
   (n.13, a dropped W4 limb)**; that exception struck, the two-wave routings below each already
   named their other limb: PT-LEAF-14 (W0/W2 → W0.1, with its W2.16 pointer), pt-packrat C-7
   (W1/W4 → W1.12, with its W4.4/W4.5 pointer), PT-LEAF-5 · PT-UTILS-4 · PT-PARSER-2 (W1 corpus +
   W2 boundary clause → W1.2, both limbs in the body), PT-LEAF-25 (W1/W2 → W1.17), pt-lazy LZ-8
   (W2 O-8/K-6 + a ratified W1 rider → W2.31), pt-ansi row 12 (W1 G-5 + W2 R-LAW-3 → W1.7),
   pt-diagnostics row 19 (W3.b + W1 rider → W3.5), pt-parser PT-PARSER-16 (W2 §3b + W3's
   typed-recovery unit → W3.8), and PT-UE-1 (W2 RETIRE + `REFINEMENT FOLD → W1` → W1.1/W1.19). The
   first two-wave routing found with a **dropped** limb was PT-UE-9 (n.10); the pass-3 limb sweep
   found a **second**, pt-lazy LZ-17 (n.13) — so "the ONE" is struck, and the certification this
   entry now carries is bounded by n.13's enumeration.
9. **DOUBLE BOOKING CLOSED (L-20 pass-2 repair): pt-state PT-STATE-7 is booked ONCE, at W2.65.**
   The row stood as the sole subject of **W2.65** *and* inside **W2.38**'s merged provenance, both
   asserting **TAG-DISPATCH BANNED**, with no merge and no cross-reference — the identical pattern
   n.5 cures for LZ-9 ≡ M3. (**CORRECTED by the L-20 pass-3 repair**: this entry also called it
   "the last uncrossed duplicate in the provenance set", which was **FALSE** — pt-lazy LZ-5 / A.6
   stood double-booked at W2.28 and W2.34 and is cured at **n.12**. The claim of lastness is
   struck; what this entry certifies is this one cure.) The booking
   stays at **W2.65**, where PT-STATE-7 is the sole subject and where its upgraded content lives
   (C-8's falsifier KILLED; the acceptance divergence
   `string("abc").trim(regex(/\s*/)).parse("abc")` — **DERIVED ANALYTICALLY, NEVER EXECUTED**;
   pt-state's own proof cell closes "Derived analytically this session" and ruling K7 reads
   "Re-derived **analytically**", n.15). **W2.38 keeps a CROSS-REFERENCE, not a
   booking**: its trivia-class fold stands on PT-LEAF-7 (+ PT-UTILS-2, PT-UE-22) and names the
   pt-state-side statement of the same ban at W2.65 — the W2.32 idiom exactly. Entered in n.4's
   register.
10. **DROPPED LIMB RECOVERED (L-20 pass-2 repair): pt-utils-entry PT-UE-9's W0 limb → W0.2.**
    PT-UE-9 is routed `**REFINEMENT FOLD → W0/W4**: tier promises asserted over **import graphs**,
    never file existence (identity: pt-leaf PT-LEAF-17's W4 rider)` (`pt-utils-entry.md:51`), but
    only the **W4** limb was folded (inside W4.2's merged provenance) and §2 carried exactly one
    row. The **W0** limb now folds at **W0.2**, cross-referenced both ways with W4.2 — the same
    two-wave handling W0.1 already gives PT-LEAF-14 and W1.12 gives pt-packrat C-7.
11. **UNREGISTERED MERGE REGISTERED, DUAL RESIDENCE DECLARED, OWNERSHIP TENSION RESOLVED (L-20
    pass-3 repair): W2.8 = pt-debug B-1 ≡ pt-ansi row 13 / M-1.** W2.8 collapses TWO named source
    rows into ONE booking under an explicit identity label — its provenance reads
    ⟨pt-debug.md B-1 (= pt-ansi.md row 13 / M-1, same identity)⟩ — and it appeared in NEITHER n.4's
    merge register NOR its near-duplicate list. It is now **entered in the register** (n.4).
    **DUAL RESIDENCE, DECLARED**: pt-ansi row 13 is simultaneously named in **§8 bloc 4**'s
    NO-WAVE-OWNER enumeration ("rows 3, 6-9, **13**, 15, 16-cure, 17-20"). That is intended and is
    now stated where pt-core row 11's identical arrangement is stated — the second and last of the
    document's two dual residences.
    **THE OWNERSHIP TENSION, STATED AND RESOLVED — never silently.** The two apotheoses route the
    SAME defect to DIFFERENT owners: pt-debug B-1 files it **`X.P.W2`** ("the renderer WRAP's
    `{row, col}`-relative caret contract kills this class by construction"), while pt-ansi row 13
    files it **`NO-WAVE-OWNER` — render tier** ("any relocation wave cures it by shifting the caret
    by `start`"). **RESOLUTION: the tension is over the OBJECT, not the wave, and both dispositions
    are honoured whole.** The CANDIDATE-side law — the successor's caret is specified in RENDERED
    coordinates, killing the class by construction — is a W2 wrap-contract clause and folds at
    **W2.8**, pt-debug's own routing. The INCUMBENT-side repair — shifting parse-that's live caret
    by `start` and marking the ellipsis offset — is render-tier work no W0..W4 unit owns (EQ-4
    quarantines rendering outside the algebra) and stays **banked, unfolded, in §8 bloc 4**,
    pt-ansi's own routing. Neither disposition is overridden; the two name different acts on
    different trees, and the ansi record's sharper SCOPE (the class bites only when the line
    exceeds 74 chars AND the failure column > 37 — `start === 0` aligns correctly) is carried into
    W2.8's body, which is why the merge is a merge and not a duplicate.
12. **DOUBLE BOOKING CLOSED, per the W2.65 idiom (L-20 pass-3 repair): pt-lazy LZ-5 / A.6 is
    booked ONCE, at W2.34.** LZ-5 / A.6 stood as the TITLED SUBJECT and a provenance entry of BOTH
    **W2.28** ("pt-index m-12 (+ pt-lazy LZ-5/A.6)", ⟨pt-index.md m-12 · pt-lazy.md LZ-5 / A.6⟩)
    and **W2.34** ("pt-lazy LZ-5 / A.6", ⟨pt-lazy.md · LZ-5 + superlative 6⟩), both restating the
    A.6 holding — the public seam suffices to build the bounded back-edge ABOVE the library, no
    1.1.0 ask required — with no merge, W2.28 carrying a ONE-WAY "Cross-ref W2.34" and W2.34
    carrying no back-pointer and no demotion. **THE APTER BOOKING IS W2.34**, by the same test n.9
    applied to PT-STATE-7: it is the row where LZ-5 is the SOLE subject and where the whole source
    disposition lives (G-11 owns the class — `ok:false` from a constructed bound, never RangeError,
    W2.md:824-838 — with the W3 rider at W3.11, and A.6's adjudication with the library-side arity-2
    named as the better cure). **W2.28 keeps a CROSS-REFERENCE, NOT A BOOKING**: its subject is
    pt-index **m-12** (DEBT-3 owed one layer below where the band assigned it), it quotes the A.6
    adjudication as the reason the debt is dischargeable above the library, and it cites LZ-5 no
    more in provenance. **Reciprocal pointers both ways** — W2.28 → W2.34 and W2.34 → W2.28 — as
    n.5 and n.9 require. Entered in n.4's register. No row lost, no renumbering.
13. **DROPPED LIMB RECOVERED (L-20 pass-3 repair): pt-lazy LZ-17's W4 release-condition rider →
    W4.15 — and the LIMB sweep that found it.** LZ-17's disposition is TWO-WAVE:
    `**NO-WAVE-OWNER** — sequencing law for the RETIRE (explicit init FIRST, then delete); **W4**
    release-condition rider (artifact truth)` (`pt-lazy.md:49`). Only the NWO/sequencing half was
    carried (§8 bloc 9); the **W4 rider was booked nowhere** — the addendum's two mentions of LZ-17
    both sat inside that one excluded bloc. The rider folds at **W4.15**, with a reciprocal note in
    §8 bloc 9: LZ-17 now has a DECLARED two-limb residence, exactly as PT-UE-9 does (n.10).
    **THE LIMB SWEEP** — every two-wave source disposition read for a limb the addendum never
    booked, the axis neither n.7 (whole rows) nor n.8 (wrong wave) covered. Two further limb-level
    gaps were found and **banked in §8 rather than folded**, each for a stated reason:
    - **pt-core row 13**'s `**W4 note**` ("the stranded union member is public typed surface and
      must be excised or made constructible at the next semver cut") — the row itself is already
      banked in §8 bloc 5; the W4 note is an instruction to a SEMVER CUT, priced by W4.14 and
      carried by W4.7's declaration-surface acceptance list, so bloc 5 now names it rather than
      leaving it implied.
    - **pt-state PT-STATE-6**'s `NO-WAVE-OWNER` limb — its **W1 rider** already rides §7 pt-state
      row 4 (the 8-own-key assertion at open), but the row was absent from §8 bloc 14's
      enumeration; it is added there.
14. **BROKEN INTERNAL CROSS-REFERENCE CORRECTED (L-20 pass-3 repair): W1.13's rider pointer
    W2.24 → W2.45 — and the POINTER sweep.** W1.13's honest bound closed "a consumer driving a
    memoized parser via raw `.parser()` opens NO epoch and the hazard revives (cross-ref W2.24
    rider)". **W2.24 is pt-index M-5 — the newline index built once per parse** — and carries no
    rider about raw entry or epochs at all; the intended target is **W2.45** ("pt-packrat C-1 —
    memoization as a parse parameter (BLOCKER) + the W1 raw-entry rider"), whose body carries
    "**W1 RIDER**: a stray raw entry in a harness ACCUMULATES ROOT MEMO". A W1 seat following the
    old pointer landed on an unrelated row — the exact mis-cite class n.1/n.6/n.8 exist to police.
    **THE SWEEP**: every `W⟨d⟩.⟨d⟩` reference in every body, provenance line, and §1a entry was
    extracted and resolved against the row it names — **190 references, all resolving to a defined
    row, and each read against that row's title/subject**. This was the only pointer landing on the
    wrong subject; no other stale W-number survives.
15. **EPISTEMIC INFLATION CORRECTED (L-20 pass-3 repair): an ANALYTICALLY DERIVED divergence is no
    longer reported as MEASURED.** Two pointer sites — **n.9** and **W2.38** — wrote "the
    **measured** acceptance divergence `string("abc").trim(regex(/\s*/)).parse("abc")`". The source
    says the opposite in terms: pt-state PT-STATE-7's proof cell closes "**Derived analytically this
    session**" and ruling K7 reads "**Re-derived analytically**". The divergence was NEVER EXECUTED.
    Both sites now read **DERIVED ANALYTICALLY, NEVER EXECUTED**, and the booking row **W2.65**
    (whose body already said only "a real ACCEPTANCE DIVERGENCE") now states the derivation status
    at the booking itself. This matters because §9 promises "no baseline is re-measured here" — a
    document that stamps nothing may not launder a derivation into a measurement.
    **THE SWEEP**: every "measure"/"measured"/"witness"/"witnessed" token in the file was read
    against its receipt. All others resolve to executed or source-certain receipts, or already
    declare their status (W2.26's "CONFIRMED by static derivation"; §7's "static derivation stands
    — UNRUN-BY-LAW"; §7's "source-certain by Rust spec; the executable witness needs ONE cargo
    test"); §7's whole function is to name what has NOT been measured. These two were the only
    inflations.
16. **UNSTATED RE-HOME STATED (L-20 pass-3 repair): W1.9 folds pt-ansi row 2's W1 LIMB, and the
    row's stated primary is W2.** pt-ansi row 2's disposition reads `**W2** — dissolves under row
    1's injected policy. Coverage of the coloured arm becomes possible and mandatory
    post-injection: a W1-harness witness rider (MEASURE-AT-OPEN row 3)` (`pt-ansi.md:24`). W1.9
    books it under §3 (W1) with no re-route note — the same move n.6 and n.8 each gave a numbered
    entry. **Mitigating, and the reason this is a register defect and not a routing harm**: W1.9's
    body already names **W2.1** explicitly, so no gate name resolves to the wrong wave. **STATED**:
    the row is TWO-WAVE like the nine at n.8. Its **W2 limb — the dissolution** — is not a separate
    booking because it is not a separate holding: row 1's injected render policy IS the dissolution,
    and it folds at **W2.1**. What folds at **W1.9** is the source's own second clause, the
    **W1-harness witness rider**, which the same sentence makes "possible and mandatory
    post-injection" and which §7 pt-ansi row 3 measures at open. W1.9's provenance now says so.

---

## 2. W0 — Pause-State Verification and the Fresh Root (2 sharpenings)

**W0.1 · pt-leaf PT-LEAF-14 — src-consumption load-order proof obligation** (cl.1)
`export let whitespace` (non-optional, never initialized in-module) + cross-module top-level
`_initWhitespace()` under `sideEffects:false` — correctness hangs on **barrel statement order**,
and an ESM entry evaluating `leaf.js` first assigns a still-TDZ `let` and throws at src level
(bundle-scope safe: single chunk). **CURE-LOCK on the fresh root**: any consumer of the fresh
root's **src** carries a **LOAD-ORDER PROOF OBLIGATION**; otherwise it consumes **dist only,
STATED**. The 3-symbol cure is killed: `parser.ts:6` imports FIVE and the two omitted
(`whitespace`, `_initWhitespace`) ARE the deferral. (The in-cycle-mutable-exports ban binds the
candidate at W2 — see W2.16.)
*Sharpens*: W0.c (W0.md:241-268) — the unit rightly builds/installs/executes **nothing**
("the toolchain is X.P.W1's", W0.md:262-264); this row makes the consumption posture an explicit
obligation on the **first consuming wave** (W1), not an accident of W0's abstinence.
⟨pt-leaf.md · PT-LEAF-14⟩

**W0.2 · pt-utils-entry PT-UE-9 (W0 limb) — a TIER PROMISE IS AN IMPORT-GRAPH CLAIM, and the fresh
root inherits a FALSE one** (cl.1 — **W0 limb recovered by the L-20 fold check, §1a n.10**)
The clone opens at a commit whose own tier promise is already false: `core.ts:4-5` denies loading
the diagnostics and packrat tiers, and **BOTH of its first two clauses are false for `/core`
itself** — the packrat chunk imports the diagnostics chunk, so **every subpath inherits both**, and
the defect is FAMILY-LEVEL (causes: `vite.config.ts`'s five entries with no `manualChunks`, plus
`src/parse/utils.ts` fusing the diagnostics accumulator at `:6-148` with the byte scanners at
`:150-186`). **FOLD ON W0**: nothing in the fresh root's census or routing declaration may restate
an in-tree tier promise AS FACT — a promise about what a tier does not load is an **IMPORT-GRAPH
CLAIM**, discharged only by walking the graph, **never by file existence + `typeof`** (the W4.1
vacuity class). W0 opens the root and asserts nothing about it; this row makes that abstinence
**stated posture rather than accident**, and hands the assertion instrument to the wave that first
consumes the tier. (The W4 limb — the release gate loading the shipped entry in a bare process and
asserting the LOADED-MODULE SET under both conditions, with the byte closures — is owned by
**W4.2**; identity: pt-leaf **PT-LEAF-17**'s W4 rider, per this row's own disposition.)
*Sharpens*: W0.c (W0.md:241-268, "nothing in the fresh root is built, installed, or executed in
this wave", W0.md:262-264) and W0.d's routing declaration (W0.md:270-291) — a declaration that
repeated `core.ts:4-5` would carry a falsehood forward under W0's own signature.
⟨pt-utils-entry.md · PT-UE-9 (W0 limb; the W4 limb at W4.2)⟩

---

## 3. W1 — Harness, Corpus, and the Honest Bench (23 sharpenings)

**W1.1 · MERGED CROSS FOLD — the JS-boundary entry invariant** (cl.1)
ONE fold row, many witnesses: entry is via `parseState` + `isError` ONLY; `.parse()` truthiness
**BANNED**; the invariant asserts on `state.isError`, NEVER on the return (an undefined-check
passes every witness); and **NEVER READ `.value` UNDER `isError`** (`.value` on error is garbage
by construction, not merely undefined). Sharpeners: `many(1)`-FAIL and `many(0)`-SUCCESS both
yield truthy `[]`; a failed `then` returns the first sub-parser's residue; `minus()` leaves the
EXCLUDED parser's value; `quotedString()` returns an array from a declared `Parser<string>`;
`jsonParser` returns a truthy wrongly-typed partial on 6/6 rejected inputs; the published d.ts
`parse(val: string): T` is unsound under strictNullChecks. RIDER: adding `offset === src.length`
is necessary AND **MEASURED INSUFFICIENT** (csvParser corrupts with `isError=false` and
`offset===len` — see W1.19). Typed home already frozen: value.js `src/foundation/result.ts` +
ParseResult's non-empty failure tuple (`types.ts:25-27`).
*Sharpens*: W1 scope 9 (W1.md:106-107) and G-10's GUARD rows (W1.md:576-577) — every harness leg,
oracle, and corpus assertion in the lane is bound to this entry discipline.
⟨pt-core rows 2/19 · pt-leaf PT-LEAF-16 · pt-parser PT-PARSER-1 · pt-parsers PT-PARS-8/-11/-17/MISS-1 · pt-state PT-STATE-1 · pt-utils-entry PT-UE-1/-3 · pt-index m-11⟩

**W1.2 · MERGED CROSS FOLD — non-string boundary is positive typeof admission, never
throw-inference** (cl.1)
O-15 PT-07's "5/5 non-string inputs throw" is **REFUTED**; the boundary is three-to-four-valued:
throw / silent-reject / **SILENT-ACCEPT-COERCED** (`eof().parseState([])` → ok; `regex(/a*/)`
`test()`-coerces a number to an empty-match success; `whitespace.parseState(42)` → `isError:false`
via `0 >= undefined` → ToString; `skipWhitespace({src:42})` → silent no-op empty document; boxed
String silently works). W2's boundary clause is a **POSITIVE `typeof x === "string"` admission
test at entry**, never a catch and never throw-inference; W1's boundary corpus gains the 8-probe
row and the 7-input × 5-callable matrix (message strings recorded, never asserted verbatim —
engine text). **Corrects W1.md:582's and W2.md:161-162's PT-07 readings** — the G-10 baseline
paste (W1.md:576, "5/5 throw raw TypeError") stands as a dated record; the *reading* of it is
amended here.
⟨pt-index B-3 · pt-leaf PT-LEAF-5 · pt-parser PT-PARSER-2 · pt-utils PT-UTILS-4 · pt-utils-entry PT-UE-25 · pt-packrat-entry L§9 PT-07⟩

**W1.3 · MERGED CROSS FOLD — the G-9 depth law** (cl.1)
G-9 asserts a **TYPED BOUND EXISTS**, never a constant. The normative sentence must bound declared
depth below the **COLD COMPOSED ceiling of the lane's deepest composed grammar** with stated
margin — never the bare-lazy 7,761 figure (a corpus declared at 5,000 satisfies G-9 literally and
crashes every JSON-shaped composed grammar). Depth baselines carry (shape, stack-size, JIT state,
prior-overflow) qualifiers. **STRUCK from the record this addendum governs: "the ceiling is a
stable property" — AT BOTH SITES THAT SHIP IT, `W1.md:553-554` AND `W2.md:832`** (G-11's pasted
born-RED baseline carries the identical clause; M11's disposition strikes it from **W1/W2** prose,
and the first fold pass named only the W1 site — **corrected by the L-20 pass-2 routing sweep,
§1a n.8**) — the corpus itself carries 7,787 (default) beside 7,761
(`--stack-size=984`), and a seat measured 1,580 COLD → 2,305 WARM in ONE process. RIDER (pt-debug
MISS-9): for any print path W2 keeps, **declare-your-WORK, not only declare-your-depth** —
`.then`/`.or` spines are 2^depth calls at frame depth = depth, so the observable failure at depth
≈30 is an unbounded HANG, never a RangeError.
*Sharpens*: G-9 whole (W1.md:544-562); cross-binds W2 G-11 (W2.md:824).
⟨pt-core row 18 · pt-leaf L-m12 · pt-parsers PT-PARS-12 (RC-3/RC-4 — strengthens the SAME row, one identity) · pt-lazy M11 · pt-debug MISS-9⟩

**W1.4 · pt-parsers PT-PARS-8 — the band's :108 idiom elevated to GATE-CHECKABLE LAW** (cl.1)
Entry via `parseState` + `isError` ONLY — any harness leg reading `.parse()` truthiness
**MEASURES NOTHING**. Proof (RC-1, seat-measured): all 7 `invalid.jsonl` vectors REJECT under
`parseState().isError` but return TRUTHY under `.parse()`, so `expect(result).not.toBeUndefined()`
— the suite's ONLY idiom — passes on EVERY malformed vector. The tests certify a **FORK**:
`csv.test.ts` re-declares the grammar byte-for-byte and never imports `csvParser`; `json.test.ts`
re-declares a DIFFERENT grammar; `invalid.jsonl` ships unread. (Facet of W1.1; receipts here.)
⟨pt-parsers.md · PT-PARS-8 + RC-1⟩

**W1.5 · pt-parsers PT-PARS-2 — the W1 REJECTION-LEG LAW** (cl.1)
EVERY grammar in the lane's corpus carries a **REJECTION LEG**. An acceptance-only totality
harness scores a TOTAL-ACCEPT PARSER AT 100% — and this module is the existence proof: `csvParser`
has no rejection power structurally (`many(min=0)`'s only failure branch is unreachable; seat
census 0/500 + 0/8 curated), no record separator (README's own `a,b,c\n1,2,3` → ONE row), and
`",,,"` succeeds consuming ZERO of 3 bytes with an empty value and no error channel.
*Sharpens* (**anchor corrected, L-20 repair**): the accept/reject taxonomy is unit **.b**'s — the
equivalence oracle at W1.md:233-256, whose imported MIRROR-DEFECT classes **B** `MIS_ACCEPT` and
**C** `FALSE_REJECT_IN_SHAPE` (W1.md:241-242) are the only rejection-bearing definitions in the
wave — and the reject leg is unit **.d**'s (W1.md:289-292, "three legs, kept separate:
shared-accepted · reject-non-throwing · R1-throw-class"). Unit **.a** (W1.md:208-232) is the
52-export TOTAL/PARTIAL/ABSENT coverage manifest and defines NO accept/reject corpus — which is
exactly this row's warrant, not a counter-example to it: an acceptance-only totality harness scores
a total-accept parser at 100%.
⟨pt-parsers.md · PT-PARS-2 + MISS-4⟩

**W1.6 · pt-parsers PT-PARS-15/-16/-19 — corpus riders** (cl.2)
(15) The lane's corpus carries **TRAILING-GARBAGE rows per grammar**, asserted via CONSUMED
OFFSET, never value shape — no `.eof()` anywhere in the module; every export is a prefix parser
presented as a document parser. (16) **CONTROL-CHAR rows**: raw U+0001–U+001F accepted inside JSON
strings, and `json.ts:23`'s `indexOf("\\")` fast path routes control-bearing backslash-free
strings AROUND the JSON.parse oracle BY CONSTRUCTION — a fast path must be probed **on the class
it skips the oracle for**. (19) Rejection-shape assertions in the lane bind `furthest`, NEVER
`expected` (positionally accurate, textually silent under the shipping default).
⟨pt-parsers.md · PT-PARS-15, -16, -19⟩

**W1.7 · pt-ansi row 12 / C-B1 (re-attributed) — the both-descriptor gate** (cl.2)
CURE-LOCK on W1 G-5 + W2 R-LAW-3: G-5 today redirects/asserts **STDERR ONLY** (W1.md:487-494) and
R-LAW-3's probe patches only `console.error`/`warn` (W2.md:281-283, :721-723); a lowering printing
via `Parser#debug()`'s **`console.log` default** passes both while printing on every corpus input.
**G-5 must capture and assert BOTH descriptors byte-empty; R-LAW-3's probe must also patch
`console.log`.** The hole survives ansi's deletion (K4). W2.md:724-725's own words rule the class:
a probe that cannot fail for its intended reason is itself a defect.
⟨pt-ansi.md · roster row 12⟩

**W1.8 · pt-ansi row 16 / M-4 — the debugDepth interlock** (cl.2)
`debugDepth` is never restored on throw (no try/finally around `debug.ts:358-379`). CURE-LOCK:
curing W1.7 (patching `console.log`) **ARMS this leak** — the two rows interlock. W1 probe-suite
rider: the purity probe's own throw must not corrupt later renders (run under a throwing logger in
a disposable process — §7 pt-ansi row 5). The one-line try/finally in the incumbent is
NO-WAVE-OWNER (§8).
⟨pt-ansi.md · roster row 16 + MEASURE-AT-OPEN 5⟩

**W1.9 · pt-ansi row 2 / L-D2 (W1 witness-rider limb) — coloured-arm coverage rider** (cl.2)
`enabled` is a zero-way latch (import-time const, unexported, no setter, no FORCE_COLOR); the
coloured arm is uncoverable and the suite hand-rolls an SGR-only strip. **Post-injection (W2.1),
coverage of the coloured arm becomes POSSIBLE AND MANDATORY** — a W1-harness witness rider.
**ROUTING STATED (L-20 pass-3 repair, §1a n.16)**: the source row's stated primary is **W2** —
"dissolves under row 1's injected policy" — and that dissolution is not a second holding but
**W2.1** itself (row 1's injected render policy). What folds HERE is the disposition's own second
clause, the **W1-harness witness rider**, measured at open by §7 pt-ansi row 3.
⟨pt-ansi.md · roster row 2 (W1 witness-rider limb; the W2 dissolution is W2.1's — §1a n.16)⟩

**W1.10 · pt-core row 36 / C-18 — RED-7 instrument repair BEFORE any figure is banked** (cl.1)
`parsethat-surface-gaps.mjs` — the probe W1's G-4 baseline runs (W1.md:436) and G-10 adopts as the
lane's standing input (W1.md:564-580, the "RED — 7 gap(s)" print) — is itself defective: the
probe row at `:48` can never go RED (`!ARM ? false : false`); the header claims
workspace-runnability the layout does not give; and the probe **embeds a live hrtime bench at
:41-43 that runs on EVERY invocation and arms the latch under `--arm`**. Repair rows 47/48 (with
row 46) and re-home or delete the embedded bench BEFORE any RED-7 figure is banked; run the probe
only in a **disposable process** (it constructs memoized parsers and can arm diagnostics — §7
pt-ansi row 4).
⟨pt-core.md · roster row 36 + MEASURE-AT-OPEN 5⟩

**W1.11 · pt-diagnostics row 32 / R2-M4 — G-5 upgraded from stderr proxy to positive predicate**
(cl.1)
**BANK AT OPEN**: the P1 predicate — fresh state + `mergeErrorState(s, "probe")`;
`s.expected !== undefined` — is a pure, side-effect-free flag reader through PUBLISHED exports,
upgrading G-5 from a byte-empty-stderr PROXY to a **positive entry/exit assertion** (the G-4
shape), closing the transitive-dependency-arms hole C·D-11 correctly named. Asserted `=== false`
at bench-cell **ENTRY AND EXIT**. Until banked, G-5's stderr proxy stands as specified
(W1.md:465-498).
⟨pt-diagnostics.md · roster row 32 + MEASURE-AT-OPEN 5 + S11⟩

**W1.12 · pt-packrat C-7 + pt-packrat-entry M-7/E-B1 — one process AND one module format per
cell** (cl.1)
W1's CELL LAW (W1.md:94-96, :279-285) **GAINS "one module format per cell"**: the ESM and CJS
chunks each carry their own module globals — restated per-format, the duplication is **SIX**
bindings, not four (`MEMO`, `HEADS`, `LR_STACK`, `PACKRAT_ARMED`, `CURRENT_SRC`, `GROWING`;
`LR_STACK` and `CURRENT_SRC` omitted from the challenge table). Memo keys carry no copy
discriminator; all four proof gates load `dist/parse.js` only, so no gate can fail for it. (The
W4 side — "no dual-instance state in the shipped artifact" — is owned by W4.4/W4.5.) **Source-stated
ride carried here (L-20 repair, §1a n.4):** pt-lazy **LZ-21** — the dual-package fork of
`LAZY_PARSER_CACHE` and `Parser` identity across the declared `import`/`require` conditions — is
the same holding at a third door; its own disposition reads "Identity: **rides pt-packrat C-7's
REFINEMENT FOLD** (one-format-per-cell; no dual-instance state in the shipped artifact) — never
re-booked here", so it rides this row's fold and is named, not re-booked.
⟨pt-packrat.md C-7 · pt-packrat-entry.md M-7 / E-B1 · pt-lazy.md LZ-21 (MERGED — one identity, restated per-format)⟩

**W1.13 · pt-parser PT-PARSER-7 / SEAT-K1 — G-4's predicate is CONSTRUCTED, not called** (cl.1)
G-4's predicate becomes **"no memoizer was CONSTRUCTED"** (W1.md:434-459; the §3a note at
W1.md:124-127 already says the grammar may reach `makeMemoized()` transitively):
`PACKRAT_ARMED = true` is `makeMemoized`'s FIRST STATEMENT, one-way, process-wide, cross-module,
and `parseState` calls `packratEnter` unconditionally. **SEAT-K1** (killed against both challenges
AND both readers): the memo-key-perturbation consequence is **MEMO-INERT** — `packratEnter`
installs fresh empty tables per top-level parse, so construction-order id drift changes the key
VALUE, never its uniqueness. Survivors: the ≈2.1M id budget, the O-8 posture, dual-package twin
counters, printer identity. HONEST BOUND → W2's entry-discipline clause: a consumer driving a
memoized parser via raw `.parser()` opens NO epoch and the hazard revives (cross-ref **W2.45**'s
W1 raw-entry rider — pointer corrected from W2.24 by the L-20 pass-3 repair, §1a n.14).
⟨pt-parser.md · PT-PARSER-7 + seat ruling 1 / K18 + MEASURE 8⟩

**W1.14 · pt-packrat-entry E-M4 / K7 — G-4's reading STANDS AS WRITTEN** (cl.1)
E-M4's "G-4 is unsatisfiable until this file gains a fourth export" is **KILLED**: W1.md:434-459
satisfies G-4 TODAY via static dist-read of the latch plus per-cell latch print from the installed
dist — two routes, no fourth export. What survives is a MINOR API-observability gap, and any
accessor must land in `packrat.ts` first (the entry can only re-export it).
⟨pt-packrat-entry.md · E-M4 + K7⟩

**W1.15 · pt-packrat-entry E-m4 — chunk-vs-source provenance on every cite** (cl.1)
Rollup named the shared core chunk after a 5-line module, so the corpus now cites `parser.ts` code
as "packrat-entry". **RE-ISSUE PT-01's cites as `chunk:882 (source: parser.ts:68)`**. Companion
coordinate law (pt-leaf/pt-parser): any re-run **RE-DERIVES the content-addressed chunk filename
from `dist/core.js:1`'s import specifier AT THE MOMENT OF USE**; hardcoded names void silently on
the next build. Applies to W1's own baseline cites (`packrat-entry chunk :678,:722`, W1.md:439,
:575).
⟨pt-packrat-entry.md E-m4 · pt-leaf.md provenance-drift INFO / MEASURE #11 · pt-parser.md INFO-3⟩

**W1.16 · pt-lazy LZ-20 / LZ-22 — the boundary-enumeration and evidence laws** (cl.1 —
**LZ-19 SPLIT OUT to W2.77 by the L-20 fold check, §1a n.8**)
(LZ-20) The boundary invariant (W1 scope 9, W1.md:106-107) must **ENUMERATE EVERY VALUE-RECEIVING
PUBLIC EXPORT** — asserting it only over `parse`/`parseState` leaves a /core-wide hole
(`getLazyParser(non-function)`); source-routed `REFINEMENT FOLD → W1 §3.9`. (LZ-22) **EVIDENCE
LAW**: sha-pin any gate-read dist before any `dist:line` cite — already discharged for the
published tarball by `package-receipt.json` (tarball sha256 + installed-tree sha256 + full export
list); source-routed `REFINEMENT FOLD → W1` evidence law. **The third limb this row once carried —
LZ-19's registry discipline — is source-routed `REFINEMENT FOLD → W2` and binds W2's
registry-comparing gates G-10/G-11 (`W2.md:808-838`), NOT W1's own G-10 (`W1.md:564-580`); it is
booked at W2.77 and nowhere here.**
⟨pt-lazy.md · LZ-20, LZ-22 (LZ-19 re-routed to W2.77, §1a n.8)⟩

**W1.17 · pt-leaf PT-LEAF-24 / -25 / L-M12′ — bench-leg and construction-shape folds** (cl.1)
(24) `fuseAll` allocates the result array BEFORE the first child runs — every FAILED `all()`
allocates garbage on exactly the reject-path leg the band makes a binding bench debt; **W1's
reject-path leg gains an ALLOC-COUNT ORACLE** (the leg exists: W1.md:289-292); the cure is a pure
hoist (`leaf.ts:191→193, :222→224, :257→260`, all three arms). **W2 limb, source-routed and
restored by the L-20 pass-2 routing sweep (§1a n.8): the algebra's law is THE DELIVERABLE IS
ALLOCATED AFTER THE FIRST COMMIT POINT** — the W1 oracle measures the breach, the W2 law forbids
it, and the first fold pass carried only the former. (25) One API, two element kinds: arity 2-3 PACKED literals, arity ≥4 permanently HOLEY
`new Array(n)`; the W2 law = ONE construction shape (`[]` contiguous preferred, NO engine
assumption); **W1 pins the elements-kind predicate on the lane's node**. (L-M12′) The arity-2/3
unroll vs the general arm is BENCHED NOWHERE and pinned by no test; the one checkable number in
the unroll comments — "59 all() sites" — is measurably FALSE (61) → W1 equivalence oracle.
⟨pt-leaf.md · PT-LEAF-24, -25, L-M12′ (ruling 1)⟩

**W1.18 · pt-state PT-STATE-3 — the /core exclusion assertion, BORN-RED** (cl.2)
RE-RULED ATTRIBUTION: the /core retention edges are `core.ts:7` (Parser) + `parser.ts:3`
(parserDebug/parserPrint at :695/:699) + `parser.ts:7` (`packratEnter` unconditional at :43);
`state.ts:2` is a FOURTH edge, load-bearing only for bundling consumers importing ParserState
alone. **NO state.ts-confined cure exists** — L-M1's cure is a provable no-op. Honest cures: amend
the prose, or add the exclusion assertion **BORN-RED** and split Parser's debug/packrat edges
behind a seam. **W1's harness subpath reading gains the missing exclusion assertion, born-RED
until one of the two lands.** (Import-graph mechanics: W4.2.)
⟨pt-state.md · PT-STATE-3 + rulings 4 / K3 / K4⟩

**W1.19 · pt-utils-entry PT-UE-1 — the CSV fixture signature is the BORN-RED witness** (cl.2)
`csvParser` silently corrupts real CSV and reports UNQUALIFIED SUCCESS — no return-path signal
exists (value well-formed, `isError=false`, `offset===len`) — and the corruption is a
**RE-SEGMENTATION**, not a truncation: the repo's own `data/csv/data.csv` (5 lines × 10 columns)
→ 4 rows of `[10, 2, 24, 27]` fields. A cure is proven by 5 rows × 10 fields with the `""` cells
decoded, NEVER by `length > 0`. C-4's "no concept of a row" half is KILLED (fully-quoted input
row-splits correctly), so the defect is one character class + the inexpressible RFC 4180 `""` +
`sepBy`'s silent break on the empty field. (This is W1.1's insufficiency witness.)
⟨pt-utils-entry.md · PT-UE-1 + MEASURE-AT-OPEN 7⟩

**W1.20 · pt-utils-entry ruling 5 / K13 — the RECEIPT LAW** (cl.1)
Consumer-root HEAD receipts are **RE-RESOLVED AT USE and recorded PER-RECORD**; no seat "corrects"
one absolute number into another and calls it repaired (three receipts in this corpus's chain —
`95cfbb15` → `3ea0144c` → `3ecf0267` — were each TRUE BRIEFLY). Paired win: 8/8 dist artifacts
measured sha256-IDENTICAL between `typescript/dist` and the vendored npm 1.0.0 tarball, so the
OP-7 "the dist may not be master" provenance hedge is VOID and every `[dist]` cite sits on the
exact bytes consumers receive — **an identity that VOIDS ON THE NEXT `vite build`**.
⟨pt-utils-entry.md · seat ruling 5 / K13 + MEASURE-AT-OPEN 5 and 8⟩

**W1.21 · pt-utils-entry ruling 1 / K3 — the resolution-base law** (cl.1)
FLEET LESSON, ruled at the resolver against a reader confirmation AND against pt-index's two r2
readers: "@mkbabb/parse-that is not installed / the RED-7 probe cannot run" is **FALSE** — the
package IS installed at `prototypes/css-parser/node_modules`, and `import.meta.resolve` FROM THE
PROBE SCRIPT'S OWN DIRECTORY resolves all three specifiers (Node's ESM resolver walks up from the
SCRIPT's URL, not the invoker's cwd; the probe's own header says so). **Any "not installed here"
claim MUST NAME ITS RESOLUTION BASE.** Re-running a challenge's command at the challenge's chosen
root is AGREEMENT, NOT VERIFICATION.
⟨pt-utils-entry.md ruling 1 / K3 · pt-index.md killed-claims 1-2 + DISSENT 1 · pt-leaf.md C-i2⟩

**W1.22 · pt-index C-i1 (+ PT-UE-27/K4, PT-LEAF-23) — the O-15 cite corrected where quoted**
(cl.1 — **re-routed from W4, §1a n.1**)
O-15's `:881` cite is the **GUARD line** (`if (isDiagnosticsEnabled()) {`); **`:882` is the
`console.error` call**. Downstream texts quoting it: **W1.md:476** ("`packrat-entry-*.js:881`
fires an unconditional `console.error`") and **W3.md:497** (same pairing) — at execution both read
as `:881 (guard) / :882 (call)`. The companion **`:14` cite IS exact** and correctly paired with
the label-no-op half — a correction that rewrites `:14` would DELETE A CORRECT CITE
(pt-utils-entry ruling 2). Second record: the opt-in `console.error` at `:262` is `parserDebug`'s
default logger — which DIFFERS from `Parser.debug`'s `console.log` default (feeds W1.7).
⟨pt-index.md C-i1 · pt-utils-entry.md PT-UE-27 + K4 · pt-leaf.md PT-LEAF-23⟩

**W1.23 · pt-lazy LZ-13 — the INSTRUMENT LAW: the invocation-count red test and the depth-class
test are W1 HARNESS OBLIGATIONS** (cl.1 — **recovered by the L-20 fold check, §1a n.7**)
There are ZERO direct tests for the module, and the deeper fact is instrumental, not coverage-shaped:
**the suite owns NO INVOCATION-COUNT MECHANISM AT ALL** (`toHaveBeenCalled` / `callCount` → 0 hits,
both readers agreeing) and **no depth test anywhere**. That is why LZ-1 (the dual cache), LZ-3 (a
fresh cache per decorator call) and LZ-14 (guards that retry a malformed body forever) are
UNOBSERVABLE to it, and it is the reason the 7,761 depth pin survived four months unchallenged.
**FOLD: W1's harness carries BOTH instruments as obligations — (i) the invocation-count red test
(P3-6's three lines: a counting thunk, one parse, an exact call-count assertion) and (ii) the
depth-class test** — independently of whatever the package's own suite ever grows; package-side
tests stay owner-held, on the same O-15 letter channel §8 bloc 9 names for this module. The two are the instruments the rest of this
addendum silently assumes: W1.3's depth law and §7's whole depth-ceiling family are unfalsifiable
without a depth-class assertion, and the ONE-CACHE spec (W2.30/W2.33 — "a harness cell must NEVER
`toString()` a grammar it also identity-measures") cannot be witnessed at all without a call count,
because running a thunk twice is invisible in every value the parse returns.
*Sharpens*: unit .d (W1.md:274-305) and G-9 (W1.md:544-562) — both specify WHAT to measure; this
row names the two instruments that must EXIST for either to be able to fail.
⟨pt-lazy.md · LZ-13 (N-9/P3-6)⟩

---

## 4. W2 — The Dual-Target Algebra (77 sharpenings)

### 4a. Algebra state, laws, and dispatch (pt-core + pt-ansi)

**W2.1 · pt-ansi row 1 / L-D1·C-D-4 — render policy INJECTED; the G-6 String(state) rider** (cl.1)
The render path takes `{color, sink}` as **INJECTED policy**; the ambient stderr oracle is retired
(all four §3c candidates pre-declare the kill — W2.md:304-408). The UNDECLARED tension folds as a
**NAMED RIDER on the equivalence scope**: is `String(state)` inside the compared drop-in surface?
`ParserState#toString()` is ANSI-decorated, so `String(state)` and everything over it is
TTY-dependent. *Sharpens*: EQ-4's rendering-non-normative note (W2.md:266) — the rider must be
answered where the compared surface is declared (W1.md:243-248's vendored-tarball rule).
⟨pt-ansi.md · adjudicated roster row 1⟩

**W2.2 · pt-core row 1 / B-1·C-2 — the AC-3 declared-divergence lock (nullable regex at EOF)**
(cl.2)
`regex()` short-circuits at EOF before the pattern is consulted, so every nullable pattern fails
at end of input — incl. the /core-exported `whitespace`. **AC-3's scan-union owns the `leaf.ts`
repair as a DECLARED DIVERGENCE ROW** for `.h` (repairing it changes `whitespace` for every
consumer; W2.md:444-447 Stage 3 + W2.md:524's scan-union seat). **W1 RIDER**: the equivalence
oracle's corpus gains inputs whose last token is a nullable regex — P-1's GREEN could not have
contained one.
⟨pt-core.md · roster row 1⟩

**W2.3 · pt-core row 4 / D-04·C-3 — the same AC-3 repair surface (no merge at EOF)** (cl.2)
The EOF arm of `regex()` never calls `mergeErrorState`, so `furthest` never advances and the
rendered error sits at offset 0. **Two /core leaves, two incompatible failure protocols. ONE cure,
ONE declared-divergence row shared with W2.2.** Commonest authoring error in the target domain
(truncated `oklch(0.5 0.1`).
⟨pt-core.md · roster row 4⟩

**W2.4 · pt-core row 6 / M2 — trim name-sniff, the EQ-1 divergence source** (cl.2)
`.trim(p)` SILENTLY DISCARDS `p` whenever `p.context?.name === "whitespace"`; the substituted
charCode loop is ASCII-only while `whitespace` = `/\s*/` includes NBSP/U+2000-200A. Keyed on a
mutable public string that is a sanctioned union member. **BINDING: the algebra wrap must not key
behavior on a mutable name; whitespace policy becomes an explicit operator parameter.**
*Sharpens*: the capability families (W2.md:246-251) and the closed enumeration (W2.md:139-142).
⟨pt-core.md · roster row 6⟩

**W2.5 · pt-core rows 8+9 / C-5 + D-11 — the R-LAW-3 discharge pair** (cl.2)
On /core, `expected` is ALWAYS undefined — `utils.ts:33/:38` gate the SEEDING itself — and /core
exports no enable/disable. The only cure (arming from /diagnostics) buys `parser.ts:67-69`'s
unconditional `console.error` on every failed parse; the gate lives inside `mergeErrorState`, not
at the render site. **The two rows DISCHARGE TOGETHER** (R-LAW-3, W2.md:281-283). An unwrapped
Parser cannot pass R-LAW-3's own console-patching probe.
⟨pt-core.md · roster rows 8 and 9⟩

**W2.6 · pt-core row 10 / D-12 — the O-8 anti-latch, sharpened at construction** (cl.2)
The packrat latch arms at wrapper **CONSTRUCTION** (`packrat.ts:290`), so it is process-global and
grammar-agnostic — one import-time `memoize()` anywhere taxes every unrelated parse forever;
`resetPackrat` never disarms (early-returns on the latch it never clears). **Arming becomes a
parse parameter** (O-8, W2.md:253-257). The 1.47× is O-15 N=1, never re-derived (§7).
⟨pt-core.md · roster row 10⟩

**W2.7 · pt-core rows 17 + 41/42 (SF-1) — dispatch fail-loud guard: one cure, three silent-drop
modes** (cl.2)
KEEP + WIDEN, **fail-loud at construction BEFORE any CSS head-table lands**: (a) non-ASCII table
keys silently dropped past 127 while the label still advertises them; (b) the 3-char range
heuristic fires on ANY `x-y` key, inverted ranges map nothing; (c) SF-1 (K17 REVERSED, live
witness): `internParser` mints one index per distinct parser ENTRY, so a 257-entry table
**SILENTLY MIS-DISPATCHES** (`isError` false, value from the wrong parser) and a 129-entry table
self-rejects. One guard discharges all three: `internParser` throws RangeError past 127 — the
`getCijKey` posture. *Sharpens*: the channel-table-dispatch family (W2.md:248).
⟨pt-core.md · roster rows 17, 41, 42 + Seat correction 1⟩

### 4b. pt-debug — the print/diagnostic tier at W2

**W2.8 · pt-debug B-1 = C-5 (MERGED with pt-ansi row 13 / M-1, §1a n.4/n.11) — the caret
coordinate contract** (cl.2, BLOCKER)
The caret pad is computed in ORIGINAL-source columns while `summarizeLine` re-origins the line;
displacement = `columnNum − start − 3`, unbounded; bites whenever the line exceeds 74 chars AND
the failure column > 37 (`start===0` aligns correctly — pt-ansi row 13's scope correction).
**The renderer WRAP's coordinate contract must gate the caret in RENDERED coordinates, killing the
class by construction.** Minified CSS is the library's stated domain and the X·P corpus shape; no
test pins a caret position. (Architecture/cure split with W3: see W2.17's C-11 clause.)
**MERGE + DUAL RESIDENCE, DECLARED (L-20 pass-3 repair)**: one defect read from two modules —
pt-debug B-1 routes it **`X.P.W2`** (the wrap contract kills the class by construction), pt-ansi
row 13 routes it **`NO-WAVE-OWNER` — render tier** (any relocation wave cures the incumbent by
shifting the caret by `start`). Both stand: **the candidate-side LAW folds HERE**, and **the
incumbent-side render-tier repair stays banked in §8 bloc 4**, where pt-ansi row 13 also lives —
declared dual residence, the same law-folds/cure-does-not split pt-core row 11 carries (§1a n.4).
⟨pt-debug.md B-1 (= pt-ansi.md row 13 / M-1, same identity — MERGED, §1a n.11; row 13's
render-tier cure banked at §8 bloc 4)⟩

**W2.9 · pt-debug M-10 — printer totality over the declared union** (cl.1)
The printer is information-free on 11 of the 29 declared context names (chain, dispatch, eof,
lookAhead, mapState, memoize, mergeMemo, minus, peek, recover, whitespace), each erasing its whole
subtree; all 11 are public surface; dispatch's args ARE the parsers, directly consumable by the
existing any/or arm. **FOLD CLAUSE: totality over the context-name union — a printer arm per
declared name, or a closed IR that cannot omit one.**
⟨pt-debug.md · M-10⟩

**W2.10 · pt-debug MISS-7 + M-3 — printer escaping and exclusion rendering** (cl.1)
The grammar printer emits its own metacharacters unescaped (`string('"')` prints `"""`; terminals
containing `| [ ] { } ! ? << >>` render as printer syntax) — output is ambiguous and not
round-trippable; CSS delimiter parsers are routine in the band's grammars. **Successor printer
spec: escaping or a quoted-terminal IR.** Companion: `a.not(b)` prints `!"a"` — the excluded
parser is dropped and the rendering asserts the negation of what the parser accepts; `a.not()`
prints identically. **The successor must render exclusion explicitly.**
⟨pt-debug.md · MISS-7 and M-3⟩

**W2.11 · pt-debug MISS-4 + MISS-5 — CRLF / trailing-whitespace fixture classes** (cl.1)
`summarizeLine`'s `trimEnd()` desynchronizes the caret on SHORT lines too — the lines both
challenges certify as correct: rendered content is trimmed, the pad is not, so every CRLF source
mis-points by ≥1 at end-of-line, overshoot unbounded. And the secondary block emits `lines[i]`
RAW, so on CRLF a bare `\r` returns the terminal cursor to column 0 and the marker line
OVERWRITES the source line. **The WRAP's coordinate contract must be GATED ON NAMED FIXTURE
CLASSES: CRLF + trailing-whitespace.**
⟨pt-debug.md · MISS-4 and MISS-5⟩

**W2.12 · pt-debug MISS-3 + i-3 — the Diagnostic value spec: source identity and field
decisions** (cl.1)
The `(Diagnostic, src)` API cannot enforce that `src` is the string the offsets came from — and
the package's own README worked example gets it wrong (collects from `cssWithErrors`, renders
against `css`); Diagnostic carries no source identity though `collectDiagnostic` had `state.src`
in hand, and the clamped renderer degrades to a confidently-wrong caret, never a throw. **The
Diagnostic-as-value spec CARRIES ITS SOURCE IDENTITY.** Same breath: `Diagnostic.offset` is
carried and never rendered — render it or drop the field (write-only; a wave mapping offset→start
would invent semantics). *Sharpens*: the D limb (W2.md:239); cross-ref W3.6 (nothing maps FROM
Diagnostic).
⟨pt-debug.md · MISS-3 and i-3 · pt-diagnostics.md row 26⟩

**W2.13 · pt-debug MISS-10 — a memoized printer carries a context fingerprint** (cl.1)
`PARSER_STRINGS` caches on `parser.id` while `Parser.context` is public-mutable and the library
itself rewrites it post-construction (`eof()` reassigns `p.context`; `whitespace.context.name`
set post-init) — a cached rendering can never be invalidated. **Any memoized successor printer
carries a CONTEXT FINGERPRINT, not just a cycle guard.**
⟨pt-debug.md · MISS-10⟩

**W2.14 · pt-debug i-1 — observers go through the invocation path** (cl.1)
`parserDebug` bypasses `call()` — attaching `.debug()` could change what its subject accepts;
saved today only by a second dead mechanism (the flag machinery: sole `.flags=` write onto a
constructed-then-discarded Parser; `FLAG_EOF` has a declaration and a read and NO write site).
Whoever revives flags inherits the Heisenbug. **SPEC CLAUSE: observers go through the SAME
invocation path as execution.**
⟨pt-debug.md · i-1⟩

**W2.15 · pt-debug i-2 — falsy-id hygiene** (cl.1)
The printer's only cycle terminators boolean-read a numeric id (`if (!id)` / `if (id)`) against a
counter that starts at 0; latent only because `_initWhitespace()` at `parser.ts:711` permanently
owns id 0 — an accident of module-eval order a bundler may drop. **Cure: `id != null` /
`id === undefined` sentinel, never truthiness.** The same id-as-flag conflation is B-3's live
sibling.
⟨pt-debug.md i-2 · pt-lazy.md M5 · pt-state.md PT-STATE-28 · pt-index.md AD-10⟩

**W2.16 · pt-debug C-10 — recursion renders a rule name; no vacuous gate** (cl.1)
Every recursion point renders as the bare token `lazy`, and the one recursive-fixture test asserts
only `toBeTruthy()` — satisfied by the string `"lazy"` alone. **Successor spec: recursion renders
a rule name or back-reference.** Rides the L-19 no-vacuous-gate law (W4.1). Companion candidate
law from W0.1's family: **no in-cycle mutable exports; no back-edges through the barrel** (see
W2.30).
⟨pt-debug.md · C-10⟩

**W2.17 · pt-debug B-3 / M-2 / M-4 / M-5 / C-7 / C-11 — the RETIRE bloc with the welds** (cl.2)
**RETIRE `parserPrint` / `parserDebug` / the two toString welds.** Cure-locks carried:
(a) `parserPrint` is NOT a function of its argument — printing an enclosing grammar permanently
caches `"lazy"` under a NESTED lazy's own id; the trigger is NESTING, not recursion; the successor
is a **FOLD OVER THE IR**, not a closure walk. (b) M-2's exponential is in `.then`/`.or` spine
depth (see W1.3's declare-your-WORK rider). (c) M-5: printing a lazy parser INVOKES the user's
thunk and mints a second, retained grammar. (d) C-7: cutting the welds (`state.ts:2` ⇄
`debug.ts:1`, `parser.ts:3` ⇄ `debug.ts:3`) un-pins 7.6-8.4 kB from four of five subpaths and is
what makes /core's docstring true (cross-ref W4.3). (e) C-11/PT-01: **R-LAW-3 owns the
architecture, W3 owns the labelled-diagnostics-without-arming condition** (W3.md:487-503 G-8) —
the two waves SPLIT architecture/cure.
⟨pt-debug.md · B-3, M-2, M-4, M-5, C-7, C-11 + Closing verdict⟩

### 4c. pt-diagnostics at W2

**W2.18 · pt-diagnostics rows 2/4/5/6 — the retirement anchors** (cl.2, BLOCKERs)
**RETIRE-IN-WHOLE ADJUDICATED AND HELD.** (2) The journal is a **VALUE of the parse** (`D` in
(V,C,P,D) — W2.md:239), never a side channel — `collectDiagnostic` alone among its siblings
carries no `diagnosticsEnabled` guard, no reset on any parse boundary, no src handle; live witness
P5: two unarmed recovered parses → journal 2. (4) Labels and an unconditional `console.error` are
reachable ONLY TOGETHER; `enableDiagnostics()` is arity-0 with no scope and no logger while
`parser.ts:693` and `debug.ts:355` both take injectable loggers — the seam was understood and not
applied; W3.b's `--assert-no-console` walk is the named discharge (W3.md:493-494). (5) The band's
#1 binding debt (labelled zero-width failure with named expectations, W2.md:155-156) is
**UN-DISCHARGEABLE through this module as shipped** — discharged in the wave's own value-level D
journal, NEVER via `mergeErrorState` labels. (6) The O-8 violation is TOTAL: 6 of 6 value exports
read or write process-global mutable state; **no wrapper can launder any**.
⟨pt-diagnostics.md · roster rows 2, 4, 5, 6⟩

### 4d. pt-index at W2

**W2.19 · pt-index C-B2 (+M-9) — O-8 forbids the construction BY NAME** (cl.2)
Nine of 34 exports are process-global latches published across TWO independent module graphs (ESM
+ CJS chunks each carry their own `PACKRAT_ARMED`, `diagnosticsEnabled`, `collectedDiagnostics`,
`PARSER_ID`); the latch has exactly one write and it is `true`, AT CONSTRUCTION; `resetPackrat`
does not disarm; NO PUBLISHED READER (M-9). **O-8 (W2.md:253-257) forbids the construction BY
NAME; the WRAP-9 ruling (diagnostics + packrat as parse parameters) owns the cure**; M-9's harness
consequence lives at W1 G-4 (W1.md:434).
⟨pt-index.md · C-B2 / M-9⟩

**W2.20 · pt-index B-4 + seat riders — whitespace RETIRE reason-weight (the trim escape-hatch
defeated)** (cl.2)
CURE-LOCK: **any successor whitespace terminal carries a BORN-RED EOF-acceptance gate.** Riders
defeat the corpus's own escape-hatch claim: (i) `.trim(…, false)` returns
`all(whitespace, this, whitespace)` at `parser.ts:484-485` BEFORE the name guard — a `.trim()`
call that IS broken — and that branch is cast `as unknown as Parser<T[]>`, a second tuple lie;
(ii) the flag path NEVER invokes the caller's parser, so `.trim(customParser)` with the only legal
whitespace-shaped name in the closed 29-union silently swaps in the ASCII scanner (charCodes
9-13/32 — not even `/\s*/`'s Unicode class).
⟨pt-index.md · B-4 + seat riders⟩

**W2.21 · pt-index AD-1 + AD-4 + m-3 — KEEP-riders that must be banned or guarded in the
candidate** (cl.2)
(AD-1) `regex(r, matchFunction)` can advance the offset **NEGATIVELY** — a failed sticky exec
resets `lastIndex` to 0 and a matchFunction returning non-empty on null (its DECLARED contract)
yields `ok(match, 0 − savedOffset)`, rewinding to 0; `many`/`sepBy`'s `===` zero-advance guards
are BLIND to it → unbounded loop from a published leaf; S-2 rescoped. Rider: `sticky.lastIndex`
is read AFTER the user callout returns — a re-entrant matchFunction silently corrupts the advance.
(AD-4) `mapState` hands user code a PROTOTYPE-CHAINED LIVE ALIAS of the NEW state as `oldState`,
typed `ParserState<T>`, minting a second hidden class; S-4 scoped to the default path. (m-3)
dispatch's key DSL: unbounded intern, idx 128-255 wraps negative (silent rejection), idx ≥256
wraps POSITIVE (routes to the WRONG parser), non-ASCII silently discarded, a literal `-` in a
3-char key inexpressible — **fail-loud guard posture, the `getCijKey` standard one file over**
(with W2.7).
⟨pt-index.md · AD-1, AD-4, m-3 (+ AD-1's r1·NEW-H rider)⟩

**W2.22 · pt-index B-2 — the G8 widening (four undefined producers)** (cl.1)
`all()`'s tuple type lies — arity-1 unwrap plus drop-undefined at six sites make runtime arity
data-dependent. G8's "no opt under all" ban MISSES THREE MORE undefined producers: `eof()`
(`leaf.ts:14`), empty-match regex (`:342/:355`), and matchFunction-`""`. **WIDEN the band's G8 and
the W2 idiom gates (G-10, W2.md:808) to ALL FOUR producer arms; tuple soundness by construction in
the candidate algebra.** `any()`'s arity-1 arm is type-correct by contrast — the exact contrast
isolating this as a type LIE, not a design necessity.
⟨pt-index.md · B-2⟩

**W2.23 · pt-index M-3 + C-M4 — DEBT-1 riders (total toString; open labelling)** (cl.1)
(a) `Parser.prototype.toString()` THROWS for any default-context parser — and the band's own
cand-F `reject()` idiom builds exactly such parsers. **DEBT-1 RIDER: a default-context parser must
stringify TOTALLY in the candidate.** (b) `createParserContext` is published against a CLOSED
29-literal union no downstream grammar can extend — a consumer cannot legally name a custom
production (the cand-F idiom is a compile error). **DEBT-1: labelling must be OPEN and
first-class** (W2.md:155-156). (Consequence-2 killed: the union IS reconstructible via
`NonNullable<ParserContext["name"]>`.)
⟨pt-index.md · M-3 and C-M4⟩

**W2.24 · pt-index M-5 — the newline index is built ONCE PER PARSE** (cl.1)
Recovery is O(n) per collected diagnostic (slice + lastIndexOf + line-array split) → O(n·k) in the
feature built to collect many. **D-limb spec: the newline index is built ONCE PER PARSE on the
threaded state.** The "2k splits" rate is killed — the second split is conditional; k splits are
unconditional, and a ceiling was presented as a rate.
⟨pt-index.md · M-5⟩

**W2.25 · pt-index M-10 (+ pt-parsers MISS-2) — O-8 sharpener: instance-level retained parse
state** (cl.1)
`Parser.state` retains the parsed source FOREVER (public field, writes `:66/:71`, read `:68`, zero
clears) — including the module-singleton `jsonParser`/`csvParser`. **The anti-latch law must ALSO
FORBID INSTANCE-LEVEL RETAINED PARSE STATE, not only module globals** (O-8, W2.md:253-257).
Measured: after one big parse, `jsonParser.state.src.length` = 40,001 and `state.value` = the live
20,000-element array, both reachable from the module const, last-writer-wins.
⟨pt-index.md M-10 · pt-parsers.md MISS-2 · pt-parser.md PT-PARSER-6/-20⟩

**W2.26 · pt-index M-13 — the fresh-root module-graph law** (cl.1)
`index.ts:14` creates a real import cycle (three `from "../index.js"` back-edges, two dead);
safety rests on UNPINNED STATEMENT ORDER and the TDZ crash is CONFIRMED by static derivation.
**Fresh-root law: NO BACK-EDGES THROUGH THE BARREL; order-independence GATED.** (The
`export let` + post-evaluation `_initWhitespace()` cycle break is the RIGHT shape for a genuine
ESM cycle — the defect is the cycle, not its cure; both challenge files read a correct solution as
an impurity.)
⟨pt-index.md · M-13 + superlative⟩

**W2.27 · pt-index AD-6 — the D-limb reference implementation already ships downstream** (cl.1)
EVIDENCE ROW: value.js /css already returns diagnostics as VALUES — `ParseResult<T>`'s failure arm
is `{ok:false; diagnostics: readonly [ParseIssue, ...]}` with NO VALUE KEY (`src/css/types.ts:25-27`;
W2.md:292-295 holds it load-bearing). Consequence: challenge-C's "no usage evidence left to appeal
to" method note is WRONG — a live reference implementation of the W2 D-limb sits in the same
repository — and C-B1 reframes from unexplained severance to a **CONTRACT MIGRATION whose paper
trail is the type**, not the changelog.
⟨pt-index.md · AD-6 + killed-claims 6⟩

**W2.28 · pt-index m-12 — DEBT-3 re-homed one layer below** (cl.1)
PT-04's depth ceiling ships unbounded, unparameterised, and THROWN; the DEBT-3 debt is owed ONE
LAYER BELOW where the band assigned it. Both recursion primitives are arity 1, so a bounded
back-edge has nowhere to go; the seam (`Parser` + `createParserContext` + `createLazyCached`, all
public) nonetheless suffices to build the bounded back-edge ABOVE the library — no 1.1.0 ask
required. *Sharpens*: the bounded-back-edge family "depth bound an algebra parameter"
(W2.md:249-250) and G-11 (W2.md:824).
**CROSS-REFERENCE, NOT A BOOKING (L-20 pass-3 repair, §1a n.12)**: the seam-suffices holding this
row leans on is **pt-lazy LZ-5 / A.6**, which stood titled here AND at **W2.34** with no merge.
It is booked ONCE, at **W2.34** — the row where it is the sole subject and where its whole
disposition lives (G-11 owns the class; the W3 rider; the library-side arity-2 as the better cure).
This row QUOTES that adjudication as the reason the DEBT-3 debt is dischargeable above the library
and cites LZ-5 no more — the W2.65 idiom exactly.
⟨pt-index.md m-12 (pt-lazy LZ-5 / A.6 → booked at W2.34, §1a n.12)⟩

**W2.29 · pt-index AD-15 — seams before tiers** (cl.1)
`class Parser` spans 685 of `parser.ts`'s 711 lines, 25 methods, NO internal seam — the
source-level reason the /core tier promise cannot be kept (its import set rides with it into every
entry). **CANDIDATE DESIGN LAW: seams before tiers.** The constellation's no-god-modules precept,
unenforced upstream.
⟨pt-index.md · AD-15⟩

### 4e. pt-lazy at W2

**W2.30 · pt-lazy LZ-2 / M1 / M2 — the G-10/G-11 lazy-edge walk rule + the A.7 cure trap** (cl.1)
OPERATIONAL RULE RATIFIED: the harness descends a lazy edge via `args[0]` EXACTLY ONCE, caches at
the node, and NEVER via `getLazyParser` (which returns the phantom shadow graph); **ACROSS A LAZY
EDGE ASSERT PRESENCE, NEVER IDENTITY, ALLOCATION, OR TOTAL COUNT.** CURE TRAP (M1, found
independently by both readers, re-derived by the seat): challenge-C A.7's priced cure — populate
`context.parser` on a lazy node — **CRASHES `parserPrint` on every recursive grammar**
(`parserString` is resolved from `context.parser` BEFORE the switch; memo writes happen on the way
UP; the `case "lazy"` id-latch is the only cycle terminator and is reachable only BECAUSE
`context.parser` is undefined). The sound cure is a NEW accessor slot with the memo write moved
ABOVE the recursion; A.7's semver row re-prices from minor to REGRESSION. M2:
`expect(byName.get("lazy")).toBe(1)` is a LOWER BOUND — the census must pair graph-walk with
call-site grep and print both. *Sharpens*: G-10/G-11 (W2.md:808-838).
⟨pt-lazy.md · LZ-2, M1, M2 + seat ruling 4⟩

**W2.31 · pt-lazy LZ-8 / M3 — O-8/K-6 scope ALL user-callback seams; K-6 gains a steady-state
clause** (cl.1)
(a) The anti-latch construction rule must scope ALL user-callback seams, not lazy alone — `chain`
(`parser.ts:137`) invokes the continuation DURING the parse and may construct a `memoize()`;
`map`/`mapState` callbacks run inside the same `packratEnter` bracket. Exclusivity clauses in
L-M3 and N-1 are STRUCK. **W1 RIDER RATIFIED: the EXIT-side `PACKRAT_ARMED` assertion is
load-bearing — a cell can arm ITSELF mid-parse** (W1.md:456-463 already asserts entry AND exit;
this is its warrant). (b) M3: **K-6 AS WRITTEN (W2.md:473-474) KILLS EVERY CANDIDATE WITH A LAZY
BACK-EDGE INCLUDING THE RULED WINNER** — parse #1 pays construction by design; the saving
steady-state gloss lives only inside AC-3(d)'s predicted-failure list. **K-6 gains "in steady
state (parse #N vs parse #2)"** — one clause, or `createLazyCached` is out of the algebra §6
keeps.
⟨pt-lazy.md · LZ-8 and M3⟩

**W2.32 · pt-lazy LZ-9 / M4 (MERGED with pt-packrat M3, §1a n.5) — the flag bypass is a DESIGNED
cross-lowering invariant; dead flag machinery is the enumeration counter-example** (cl.1)
The `.parser()`-not-`.call()` bypass at `lazy.ts:22` is NOT an accident: the Rust lowering
DOCUMENTS it as a designed invariant (`lazy.rs:58-60` — "the cached inner parser never has flags
set (flags live on the outer wrapper)") that the TS side states NOWHERE. ADJUDICATED: **KEEP the
bypass, FIX `trim()`, WRITE THE INVARIANT DOWN IN TS** — routing lazy through `.call()` would
violate the cross-lowering contract, not repair a latency. **Enumeration discipline — ONE ROW, the
declared identity honoured (§1a n.5): dead flag machinery is the COUNTER-EXAMPLE CLASS** for W2's
closed-enumeration discipline (W2.md:139-142; with W2.46), booked HERE and nowhere else. LZ-9's own
disposition declares `identity: pt-packrat M3, one row`, and pt-packrat M3 is the same holding read
from the packrat side — carried whole: **`.trim()` mints two ids and burns one against the id
ceiling**, the sharpest live consequence of keeping machinery nothing writes to (its riders cash
into pt-packrat C-1 and C-8, folded at W2.45 and W3.11). W4.12 carries a CROSS-REFERENCE, not a
second booking.
⟨pt-lazy.md · LZ-9 and M4 · pt-packrat.md M3 (MERGED — one identity, §1a n.5)⟩

**W2.33 · pt-lazy LZ-1 (+K-14) — ONE cache, folded via an accessor (M1-constrained)** (cl.2)
Two unreconciled caches over one thunk: printing a lazy parser re-runs the body, mints a shadow
subtree with fresh ids memoised into `PARSER_STRINGS` under ids that can NEVER appear in packrat's
`(id,offset)` MEMO, retained for process life (the WeakMap's weakness defeated by `context.args`
holding `fn`); no `clearLazyCache()` while both siblings ship resets. **Successor spec: ONE cache,
printer folded onto the EXECUTION cache via an ACCESSOR — never `context.parser`, memo-write above
the recursion (W2.30's constraint).** Trigger bound: explicit stringification only
(`${p}` / `String(p)` / `.toString()`) — NOT `console.log`, NOT default `.debug()`. **W1 RIDER: a
harness cell must NEVER `toString()` a grammar it also identity-measures.**
⟨pt-lazy.md · LZ-1 + K-14⟩

**W2.34 · pt-lazy LZ-5 / A.6 — depth as an algebra parameter, with the consumer-act finding —
THE SOLE BOOKING OF LZ-5 / A.6, §1a n.12** (cl.2)
**ONE BOOKING (L-20 pass-3 repair)**: LZ-5 / A.6 formerly stood as a titled subject here AND in
**W2.28**, both restating the seam-suffices holding, with no merge and only a one-way pointer. It
is booked HERE AND NOWHERE ELSE — here because this is the row where it is the sole subject and
where the whole source disposition lives; **W2.28 keeps a CROSS-REFERENCE**, and its own subject,
pt-index m-12's one-layer-below DEBT-3 re-homing, stands there undisturbed.
No depth parameter, no ceiling, and NO `catch` anywhere on the parse path (exactly two
try/finally, zero catch) — the failure mode is an uncaught host RangeError escaping `parse()`,
unreachable by `recover()`'s documented promise. **G-11 owns the class (`ok:false` from a
constructed bound, never RangeError; W2.md:824-838) with the W3 rider (the breach becomes a
ParseIssue value — K-8/R-LAW-3 class; see W3.11).** A.6 ADJUDICATED: the public seam SUFFICES to
build the bounded back-edge ABOVE the library, gate-indistinguishable — no 1.1.0 ask required; a
library-side arity-2 remains the better cure and is the shape `surface-gaps.mjs:37` reds on.
The consumer-side consequence — the DEBT-3 debt owed one layer below where the band assigned it —
is pt-index m-12's, at **W2.28**, which quotes this adjudication and books it no more.
⟨pt-lazy.md · LZ-5 + superlative 6 (SOLE booking; W2.28 cross-references, §1a n.12)⟩

**W2.35 · pt-lazy A.5 REVERSAL — the incumbent is the SAME depth class (frame of record)** (cl.2;
routing annotated, §1a n.3)
RULED, reversing a reader confirmation: challenge-C A.5's "the incumbent handles every nesting
axis iteratively, no ceiling at all, none can throw a RangeError" is **FALSE** — the four entry
points it names are precisely the recursive ones (`parseValueInternal` self-recurses at
`grammar.ts:344/:354/:364/:383`; `parseStyleBody` ↔ `parseItems` mutually recurse; `collect`'s
`visit` recurses). The six `let depth` counters are the TOKENIZER'S SPLITTERS, not the tree
builder. **The honest comparison is Result-typed malformed-input signalling vs thrown ceilings IN
BOTH — the W3 totality frame, not a lazy-specific indictment.** Binds W2 Stage-3/5 comparison
discipline (no candidate argument may cite a "ceiling-free incumbent"); cross-folds to W3's G-9
frame (W3.md:504).
⟨pt-lazy.md · seat ruling 1 / K-1⟩

### 4f. pt-leaf at W2

**W2.36 · pt-leaf PT-LEAF-2 (+ -21, -22) — the sequence/undefined decision and its missing
premise** (cl.2)
THE decision the algebra turns on: **ONE sequence operator, absence EXPLICIT,
`.opt()`-under-sequence legal again, G8 retired.** Missing premise (-22): `Parser.then` KEEPS
undefined (unconditional 2-tuple) while `all()` DROPS it — two sequencing operators, two value
algebras, and leaf's fusion docblock advertises the then→all migration WITHOUT stating it is not
shape-preserving. (-21): `wrap(start,end,false)` and `.trim(p,false)` are built on `all()` and
inherit compaction whole — both first-party compositions are acceptance witnesses. Live in the
library's own GREEN suite ("e…undefined" masked by parseFloat). *Sharpens*: the sequence family
(W2.md:247).
⟨pt-leaf.md · PT-LEAF-2, -21, -22⟩

**W2.37 · pt-leaf PT-LEAF-28 — the positional law (G8 over-approximates)** (cl.2)
The drop-undefined defect is POSITIONALLY CONDITIONAL and the consumer's history proves it: a
TRAILING dropped element is accidentally safe for positional destructuring; a LEADING or MIDDLE
drop silently binds the wrong value. BOTH recorded production crashes are LEADING-position
(`index.ts:205-216` "stops is not iterable"; `math.ts:144-150` "args is not iterable" — the second
uncited by either challenge), and the surviving compensations are tail-detection at exactly the
non-trailing sites. Consequence: the blanket ban is a DEFENSIBLE BUT UNSTATED APPROXIMATION (a
structural graph-walk cannot see position) — **G8 gains that annotation and RETIRES WITH
PT-LEAF-2's cure, NOT BEFORE. W1 corpus rows for leading/middle/trailing optionals.**
⟨pt-leaf.md · PT-LEAF-28 (β-miss 2)⟩

**W2.38 · pt-leaf PT-LEAF-7 (+ PT-UTILS-2, PT-UE-22) — the trivia class fixed BY
CITATION** (cl.2)
THREE mutually inconsistent whitespace alphabets ship in one package, NONE of them CSS's
({09-0D,20} / JS `\s` incl. NBSP-BOM-LS / `<=32` incl. NUL+C0 — all three on the root barrel), and
`.trim()` silently substitutes the charCode scanner for the parser the caller passed, DISPATCHING
ON A MUTABLE PUBLIC STRING TAG. No mechanism has a complement channel: **COMP-1 UNSATISFIABLE BY
CONSTRUCTION** (W2.md:241-244). CURE-LOCK: a span-emitting `trivia` operator whose class is fixed
by a **css-syntax-3 §4.2 CITATION — NEVER HARVESTED FROM ANY IN-TREE CLASS**; all three mechanisms
retire together; **TAG-DISPATCH BANNED** (identity, not string, selects behavior). W1 rider:
non-ASCII sweep with ESCAPES, never raw glyphs. **CROSS-REFERENCE, NOT A BOOKING (L-20 pass-2
repair, §1a n.9)**: the pt-state-side statement of this same ban — **pt-state PT-STATE-7**, whose
own disposition files it as "tag-dispatch banned: identity, not a mutable string, selects behavior
(matches pt-leaf … precedent PT-LEAF-7)" — is booked ONCE at **W2.65**, which carries the killed
falsifier and the measured acceptance divergence; this row cites it no more, exactly as W4.12 cites
W2.32.
⟨pt-leaf.md PT-LEAF-7 · pt-utils.md PT-UTILS-2 · pt-utils-entry.md PT-UE-22 (pt-state PT-STATE-7 → booked at W2.65, §1a n.9)⟩

**W2.39 · pt-leaf PT-LEAF-3 (+ PT-PARS-22, α-M-4) — terminal totality + diagnostic conservation
at EOF** (cl.2)
`regex()` rejects unconditionally at EOF WITHOUT MERGING, so every nullable pattern (incl. the
exported `whitespace`, 89 consumer refs) fails at EOF with `furthest=−1` and the rendered error
points at the BACKTRACKED offset — truncated input, the commonest authoring error, displays
offset 0. The `test()` path already handles EOF correctly, so the guard is a semantics-changing
fast exit. Rides the COMP-1 terminal-totality law; the furthest-merge ASYMMETRY becomes the
**DIAGNOSTIC-CONSERVATION LAW (merge may never destroy D — one law with W2.44)**. Public-export
reachability witness: `numberParser().parseState("")`; armed-mode sharpening: `any(regex,regex)`
at EOF yields `expected: []`. **W1 rider: EOF-inclusive corpus row.** Incumbent deletion is a
2.0.0 event (W4.14).
⟨pt-leaf.md PT-LEAF-3 · pt-parsers.md PT-PARS-22 · pt-utils.md α-M-4 rider⟩

**W2.40 · pt-leaf PT-LEAF-11 — two NAMED choice families** (cl.2)
Three spellings of "try this alternative", three post-failure offset disciplines: `any()` arity ≥2
restores, `any()` arity 1 LEAKS, `dispatch()`'s hit path NEVER CAPTURES `savedOffset` at all — a
committed choice sold by its own JSDoc as the drop-in for backtracking `any()`, with no label on
selected-arm failure. Both consumer tables survived only inside re-restoring wrappers (luck, not
contract). **CURE-LOCK: backtracking choice and committed choice become TWO NAMED OPERATOR
FAMILIES, each with a stateable EQ-5** (W2.md:247, :267); the loop-not-table correction stands —
NO TABLE-SIDE ADAPTER CAN FIX A LEAK IN THE LOOP.
⟨pt-leaf.md · PT-LEAF-11⟩

**W2.41 · pt-leaf PT-LEAF-6/-8/-10/-12/-13 — the five dispatch WRAPPER REQUIREMENTS** (cl.2)
The LUT is **RE-AUTHORED, never imported**. (i) EXPLICIT RANGE CONSTRUCTOR + throw on
reversed/ambiguous keys — any 3-char key with middle `-` is silently a range: `"+-."` (the CSS
number-start set VERBATIM) admits `,` as a value head, and the in-tree bench itself writes a range
key beside a bare `-`. (ii) Tables are ordered `Array<[key,parser]>` or Map, NEVER a plain object
— `Object.entries` integer-key hoisting makes the JSDoc's own "0-9" unoverridable by a digit key
IN EITHER INSERTION ORDER while letters obey last-wins. (iii) Non-ASCII keys REJECTED LOUDLY at
construction (out-of-bounds TypedArray writes are silent no-ops while the label still advertises
them). (iv) BOUND the intern index or widen (Int16Array, 256 B once) — 128-255 fail closed, ≥256
fail OPEN; index 255 stores as −1, byte-identical to the `fill(-1)` sentinel. (v) Heterogeneous
inference so V stays assignable under G-10 (design-forward only — both consumer receipts KILLED at
the archive bytes). (-13 re-graded MAJOR→MINOR by ruling 2.)
⟨pt-leaf.md · PT-LEAF-6, -8, -10, -12, -13⟩

**W2.42 · pt-leaf PT-LEAF-17 / -23 — the EQ-4 delivery side** (cl.2)
(17) /core ships leaf's three label CONSTRUCTIONS but withholds
`enableDiagnostics`/`mergeErrorState`, so labels are unobservable on the tier — while shipped
`dist/core.js` already pulls the packrat AND diagnostics chunks: the consumer pays for the
capability it is denied. **EQ-4 owns the class: LABELS ARE FIRST-CLASS VALUES, NEVER MODE-GATED**
(W2.md:266) — leaf's PRODUCTION already complies (construction-time-constant labels, zero
error-path allocation), so the whole problem is DELIVERY-side. (23) `mergeErrorState`'s two array
assignments sit OUTSIDE the diagnostics guard — two fresh arrays per furthest-advance on the
shipping default; the armed tier's `console.error` is uninjectable. **R-LAW-3's delivery side:
diagnostics are values, allocation-free when disarmed, printed by no one.**
⟨pt-leaf.md · PT-LEAF-17 and -23⟩

**W2.43 · pt-leaf C-§7-regex-overstated — state the retirement as the DUAL-TARGET BET** (cl.1)
C-§7's RETIRE-regex verdict claims "four independent reasons, each sufficient"; THREE of the four
are ~5-line repairs (the EOF-guard deletion; the delta hoist+gate; the entry-boundary typeof
guard, which is not in leaf at all). The disposition rests on the FOURTH ALONE — the AC-1/AC-2
host-object bet (no zero-import Wasm lowering; W2.md:313-345), an ARCHITECTURAL COMMITMENT, not a
defect. **W2 states the retirement as the dual-target bet, with the note that ABSENT AC-3, regex
is repairable in ~5 lines. The wave must not read four defects where there is one bet.**
⟨pt-leaf.md · C-§7-regex-overstated (β-miss 7, INFO)⟩

### 4g. The conservation law + pt-packrat (+entry) at W2

**W2.44 · pt-parser PT-PARSER-12 (+ pt-leaf C-miss-3) — the DIAGNOSTIC-CONSERVATION LAW** (cl.2)
ONE LAW, two rosters (with W2.39): **MERGE MAY NEVER DESTROY D.** `not()`'s negate arm and
`minus()` fail with NO merge and NO label — not curable from inside (the failure cause is a
sub-parser SUCCEEDING, so nothing merged anywhere) — while the disciplined siblings all merge at
8 sites. Sharpening: `or()` has no exhausted-path merge while `any()` does, and `any()`'s merge is
UNLABELLED and fires AFTER the offset restore, so when `furthest < savedOffset` (reachable via
merge-free arms incl. regex-at-EOF) it takes the `utils.ts:29` branch and **CLEARS the accumulated
expected-set** — the divergence can DESTROY diagnostics, not merely omit them. Discharges the
band's labelled-zero-width-failure debt together (W2.md:155-156).
⟨pt-parser.md PT-PARSER-12 · pt-leaf.md C-miss-3 (MERGED — one law)⟩

**W2.45 · pt-packrat C-1 — memoization as a parse parameter (BLOCKER) + the W1 raw-entry rider**
(cl.2)
Epoch soundness is bound to the `parseState` boundary while `p.parser` / `p.call` are PUBLIC and
the suite itself drives its soundness assertions through the raw entry (3 sites). **O-8/K-6 own
the class: memoization becomes a PARSE PARAMETER, so there is no boundary to bypass.** W1 RIDER: a
stray raw entry in a harness ACCUMULATES ROOT MEMO — the one-process law's teeth (W1.md:94-96).
Corrected statement of record: the suite's `resetPackrat()` calls include NON-VACUOUS clears of
raw-entry residue; none is load-bearing; the crash arm has ZERO coverage anywhere.
⟨pt-packrat.md · C-1 + §CORRECTIONS 1⟩

**W2.46 · pt-packrat C-14 — an operator set cannot carry two names for one operation** (cl.2)
`mergeMemos` ≡ `memoize` (one-line calls into `makeMemoized` differing only in a string whose only
branching reader repo-wide is a `"whitespace"` compare); served PLURAL vs recorded SINGULAR
`"mergeMemo"`; the sole use asserts nothing distinguishing. **The closed-enumeration discipline
(W2.md:139-142 class) forbids it.**
⟨pt-packrat.md · C-14⟩

**W2.47 · pt-packrat C-15 — O-8's phrasing sharpened** (cl.1)
O-8's phrasing sharpens to **"NO GLOBAL WHOSE VALUE IS A FUNCTION OF CONSTRUCTION HISTORY"** — the
sharpest witness is the mid-parse-construction window: a lazy-armed first parse permanently
retains its memo cells and whole source string in the module root (the epoch snapshots it by
reference forever after). Retention, never a wrong answer; zero consumers.
⟨pt-packrat.md · C-15⟩

**W2.48 · pt-packrat M2 — the candidate API publishes an epoched POSITIONED entry or rules the
use case out IN WRITING** (cl.1)
`parseState(val)` / `parse(val)` both construct a FRESH state at offset 0; no positioned overload
exists; `packratEnter` is exported from NEITHER entry — a positioned parse HAS NO EPOCHED PATH,
and the library's own >1MB soundness test needs `st.offset = span` by hand. Honesty rider:
`p.parse(src.slice(k))` re-keys every cell to offset 0 — it cannot express the
distinct-offsets-one-keyspace property and forfeits span fidelity. **RE-PRICES C-1's cure from a
doc line to a SURFACE.**
⟨pt-packrat.md · M2 (missed, admitted at MAJOR)⟩

**W2.49 · pt-packrat L-B1 + L-M2 — the cure class for the sibling L-axis blockers** (cl.2)
L-only rows retain their challenge-file standing and sharpen the W2/W3 spec the same way C-1/C-8
do: L-B1 (the ε-substitution admits strings OUTSIDE the grammar — the positional-only predicate
cannot see the requesting rule) and L-M2 (NaN/negative offsets bypass the key guard). **THE CURE
CLASS IS NAMED: a candidate with a STRUCTURAL INVOLVED-SET GUARD and a `Number.isSafeInteger`
BOUNDARY.**
⟨pt-packrat.md · sibling L-axis identity map⟩

**W2.50 · pt-packrat-entry M-2 — the K-8 census gains the untyped RangeError row** (cl.1)
Non-export ≠ non-reachability: `getCijKey` is called inside `recall` (`:306`) and `memoizeFn`
(`:437`) — both inside the parser `memoize()` returns — so its RangeError escapes the public
`.parse()` UNTYPED, and `packrat-entry.d.ts` declares no error channel. C-§2's "Not a K-8 row"
exclusion is KILLED; **the K-8 census (W2.md:477-478) gains the row.**
⟨pt-packrat-entry.md · M-2 + K6⟩

### 4h. pt-parser at W2

**W2.51 · pt-parser PT-PARSER-14 — EQ-4: rendering is non-normative AND total-or-absent** (cl.2)
NEW: `Parser.prototype.toString()` THROWS a raw Error for ANY Parser built through the public
constructor (context defaults to `{}`; `parserPrint` destructures it, falls to
`default: undefined`, and throws) — `String(p)`, a template literal, or `debug(recursivePrint=true)`
on any hand-built leaf throws, EXACTLY the construction posture the band credits cand-F with. **A
printer that can throw on a public construction is neither non-normative nor total** (EQ-4,
W2.md:266). W1 rider: a printer-throw sweep belongs in the boundary corpus beside the non-string
probes (§7 pt-parser row 9).
⟨pt-parser.md · PT-PARSER-14 (α-miss 2) + MEASURE 9⟩

**W2.52 · pt-parser PT-PARSER-15 — the state-copy law: total or forbidden** (cl.2)
`ParserState.clone()` drops the entire diagnostic substate while PRESERVING `furthest` — a clone
inherits a furthest offset with NO LABELS, and `mergeErrorState(clone, label)` below that furthest
is a SILENT NO-OP. ON A LIVE PATH: packrat's eval-set re-entry (`scratch = live.clone()`) discards
every diagnostic the re-evaluation accumulates. Also INVALIDATES L-m6's proposed cure (clone is
not a faithful copy). **Law: a (V,C,P,D) copy is TOTAL OR FORBIDDEN.**
⟨pt-parser.md PT-PARSER-15 (α-miss 4) · pt-core.md row 14 · pt-diagnostics.md row 21⟩

**W2.53 · pt-parser PT-PARSER-11 / -13 / -34 — operators enter the algebra SPECIFIED, never
inherited** (cl.2)
~12 of 38 published members are dead (six Parser methods with zero call sites, six ParserState
members, plus a convention split: `getLineNumber` 0-based vs `getLineAndColumn` 1-based, one
class, undocumented); the dead-surface gate is a hardcoded two-name ban list with a vacuity guard
for its own grep and NONE for its own coverage. **CURE-LOCK RIDER (-34): the ONLY producer of
`"trailing-content"` — one of the two frozen Suggestion kinds — sits inside the UNREACHABLE
`FLAG_EOF` branch, and `trailing_input` is one of value.js's eight frozen ParseIssue codes; W2
must RE-FOUND trailing-content in the algebra so the deletion does not ORPHAN THE CAPABILITY**
(cross-ref W3.b's code union, W3.md:257-260, and W2.57).
⟨pt-parser.md · PT-PARSER-11, -13, -34 (+ pt-utils.md PT-UTILS-18: the union is 1-member at runtime)⟩

### 4i. pt-parsers at W2

**W2.54 · pt-parsers PT-PARS-5 — `Parser<any>` is structurally unsatisfiable under G-10** (cl.2)
**G-10 RIDER: `any` is structurally unsatisfiable against "exactly the frozen /css value types,
zero extra properties" (W2.md:236) — a candidate surface shipping `Parser<any>` FAILS ADMISSION.**
`jsonParser` ships `Parser<any>` into `dist/parsers/json.d.ts`; strict tsc exits 0 on four wrong
bindings; `JsonValue` is exported twice and types nothing.
⟨pt-parsers.md · PT-PARS-5⟩

**W2.55 · pt-parsers PT-PARS-9 — numberParser is a DECLARED-DIVERGENCE row** (cl.2)
The incumbent utility ships the class the band REJECTED (the adjudicated non-finite debit):
`+1`/`.5` reject, `01` → 0 at 1/2, `1.` → 1 at 1/2, `0x10` → 0 at 1/4, `1e400` → Infinity. **ANY
CANDIDATE NUMERAL PRODUCTION IS MEASURED AGAINST value.js's `numberToken` SEMANTICS
(`grammar.ts:129-132`, anchored + finite-guarded), NEVER AGAINST THIS EXPORT.** (Non-finite
posture stays a declared mark, W2.md:297-302.)
⟨pt-parsers.md · PT-PARS-9 + MISS-8⟩

**W2.56 · pt-parsers PT-PARS-13 / -14 / MISS-6 — the lifecycle law and the kill-by-number
dead-surface ledger** (cl.2)
(13) **LIFECYCLE LAW: parsers are module-scope values with STABLE IDENTITY** — dispatch interns BY
REFERENCE, so factory idioms DOUBLE table slots; measured mint-deltas 15 / 8 / 2 Parser instances
per call. (14) The successor algebra carries NO flags channel and no dead choke point: `call()`
(`parser.ts:437-478`) + `unsafeCall` + the discarded `flaggedParser` enumerated as
PROVABLE-BY-GREP dead surface. **RC-2 RULING carried: the EOF cure is `Parser.eof()`, NEVER FLAG
revival — a wave taking the L-disposition literally would RESURRECT A DEAD ALGEBRA.** (MISS-6)
S-4's mechanism must read "arming requires a call site this module lacks", NEVER "the code is in a
different chunk" (the ./utils chunk CONTAINS the packrat implementation).
⟨pt-parsers.md · PT-PARS-13, -14, MISS-6 + RC-2⟩

### 4j. pt-split at W2

**W2.57 · pt-split PT-SPLIT-1 — the delimiter domain is ONE NON-SURROGATE SCALAR VALUE** (cl.1)
ADJUDICATED CURE SPEC. Guard (indexOf, multi-char-aware) and consumer (`text[i] === delim`, one
UTF-16 unit) answer OPPOSITELY for every `delim.length !== 1`. SURROGATE RIDER (re-measured):
`splitBalanced('a\u{1F600}b','\uD83D')` → `["a","\uDE00b"]` — a matching LONE SURROGATE splits
INSIDE the astral pair and emits ill-formed UTF-16, which **HOLES BOTH CHALLENGES' PROPOSED CURE**
("one code point" admits U+D83D; "one character" is ambiguous). The Rust twin types `delim: u8` —
unrepresentable there. **The EQ-1 specimen `("a::b","::")` rides the W2 .g seat** (W2.md:617-631).
⟨pt-split.md · PT-SPLIT-1⟩

**W2.58 · pt-split PT-SPLIT-2 + S-SUCCESSOR — the WRAP TARGET IS A COMMITTED ARTIFACT** (cl.1)
CURE-LOCK: W2 §3b wrap-obligation 2 is CSS Syntax **§4.3.7 escape state**, and THE WRAP TARGET IS
`rust/parse_that/src/parsers/scan/balanced.rs` — **112 COMMITTED lines neither challenge read**,
carrying separate open/close, a quotes list, `escape: u8` with a memchr2 skip loop, and
terminators: FOUR of this roster's cures already written, in-tree. **The .g seat WRAPS it; it does
not design it.** (AC-3, W2.md:346-359.)
⟨pt-split.md · PT-SPLIT-2 + superlative S-SUCCESSOR + Closing verdict⟩

**W2.59 · pt-split PT-SPLIT-8 / -11 / -12 — enumerate, never fold; write the boundary down**
(cl.1)
(8) **ENUMERATE BRACKET FAMILIES, NEVER FOLD THEM** — one depth counter shares `(` and `[`, so
`'f(a],b)'` splits inside the call; both lowerings identical (a SPECIFICATION defect the mirror
faithfully reproduced). (11) The wrap spec WRITES DOWN the arm-order boundary: a delimiter drawn
from `"'()[]` can never split while the guard affirms it; the domain narrowing does NOT cure this
(`(` is a legal scalar). (12) Token classes ({}/comments) are W2 §3b's enumeration business;
`balanced.rs` already carries quotes/terminators fields.
⟨pt-split.md · PT-SPLIT-8, -11, -12⟩

**W2.60 · pt-split PT-SPLIT-6 — the cheapest live AC-1 specimen (RED by construction)** (cl.2)
The AC-1 signature leak already REALIZED in the tree's only dual-target pair: string/UTF-16 units
vs u8/bytes; the TS side answers inputs the Rust side cannot pose, and the Rust side PANICS BY
LANGUAGE SPECIFICATION on a continuation-byte delimiter (`split.rs:60-61` slices off a char
boundary) where TS returns the input. W2 §3c predicted exactly this as AC-1 failure (a)/K-2. **A
dual-target equality probe FAILS EQ-1 on `("a::b","::")` BEFORE ANY CANDIDATE WRITES A LINE — a
gate that can fail for its intended reason (L-19), RED by construction until W2.57's narrowing
lands.**
⟨pt-split.md · PT-SPLIT-6 + ruling 5 + MEASURE R5/R6⟩

### 4k. pt-state at W2

**W2.61 · pt-state PT-STATE-1 — the W2 failure-signal law** (cl.1)
W2's failure-signal law NAMES B-1 EXPLICITLY: **value meaningful ⟺ `!isError`**; the
non-empty-tuple failure arm is the shape that makes the alternative UNREPRESENTABLE
(W2.md:292-295). `err()`, the one paired transition, has ZERO CALLERS; the live protocol is
uncoupled raw writes. The corpus's own recorded row (W1.md:577 "returns undefined") UNDER-STATES
the tree — the residue is GRAMMAR-DEPENDENT. W1 keeps the RED GUARD row as a live probe (K1: it
RUNS).
⟨pt-state.md · PT-STATE-1⟩

**W2.62 · pt-state PT-STATE-9 — the tier CO-PUBLISHES the diagnostic writer with the extension
point** (cl.1)
/core publishes the sanctioned extension point (`ParserFunction`) but NO LABEL WRITER:
`mergeErrorState` — the sole entry to the expected-set — is on `.` and /diagnostics but NOT on
core.ts (verified: ∉ the 18 keys). A /core-only consumer authoring a leaf can set
`isError = true` AND NOTHING ELSE — every failure they can express is opaque, precisely
parser-band debt #1 (W2.md:155-156), and the workaround RE-CROSSES the boundary `core.ts:3-5`
sells.
⟨pt-state.md · PT-STATE-9 (β-miss 1)⟩

**W2.63 · pt-state PT-STATE-23 — O-8 gains "no public toggle without a public query"** (cl.1)
`resetErrorState`, `isDiagnosticsEnabled` and `popLastDiagnostic` are exported from `utils.ts` but
reachable from NO published subpath — a consumer can ARM diagnostics but can neither QUERY the
flag nor reset the error quartet as a unit, so the cure both challenges prescribe for `clone()` is
UNREACHABLE. **A one-way public setter with no public getter is O-8's anti-pattern at the API
layer; O-8's wording (W2.md:253-257) gains the clause.**
⟨pt-state.md · PT-STATE-23 (β-miss 3)⟩

**W2.64 · pt-state PT-STATE-27 (+ -29) — W2 PRICES THE CODE-ASSIGNMENT LAYER** (cl.1)
Field-level gaps neither challenge measured against the 52-export /css contract: ParseIssue
requires a `code` from a FROZEN 8-MEMBER UNION and ParserState has NO code field anywhere;
`Suggestion.kind`'s 2 members are a **DISJOINT VOCABULARY, NOT A SUBSET** (corrects C-14's
framing); and `ParseIssue.expected` is REQUIRED while `state.expected` is undefined by default on
the shipping path — the default path produces an UNREPRESENTABLE ParseIssue, EQ-4's exact
disqualifier (W2.md:266). **W2 prices the code-assignment layer EXPLICITLY; it is where EQ-4 is
won or lost for any lowering hosted on this state.** (Cross-ref W3.6/W3.8.)
⟨pt-state.md · PT-STATE-27 (β-miss 4) and PT-STATE-29⟩

**W2.65 · pt-state PT-STATE-7 (+ ruling K7) — closed operator union, no fallback kind;
TAG-DISPATCH BANNED (upgraded via a killed falsifier) — THE SOLE BOOKING OF PT-STATE-7,
§1a n.9** (cl.2)
**ONE BOOKING (L-20 pass-2 repair)**: PT-STATE-7 formerly sat here AND in W2.38's provenance, both
asserting the same ban with no merge and no cross-reference. It is booked HERE AND NOWHERE ELSE —
here because this is the row where it is the sole subject and where the upgrade lives; **W2.38
keeps a CROSS-REFERENCE**, and the trivia-class holding it shares with PT-LEAF-7 (+ PT-UTILS-2,
PT-UE-22) stands there undisturbed. UPGRADE: C-8's falsifier ("`regex(/\s*/)` always succeeds zero-width … no acceptance difference")
is FALSE — `regex()` fails at EOF BEFORE the pattern runs, so `.trim(regex(/\s*/))` takes the wrap
path whose trailing end-call runs at post-inner offset and FAILS THE WHOLE WRAP when the inner
consumed to EOF: `string("abc").trim(regex(/\s*/)).parse("abc")` REJECTS where
`string("abc").trim().parse("abc")` ACCEPTS. A real ACCEPTANCE DIVERGENCE keyed on a MUTABLE
STRING TAG the library itself rewrites post-hoc — and per W2.61 the rejection is not even visible
in `.parse()`'s return. **The divergence row joins W1's acceptance corpus.**
⟨pt-state.md · PT-STATE-7 + ruling K7⟩

**W2.66 · pt-state PT-STATE-2 / -14 (+ -26) — the state-identity law and first-class
mark/rollback** (cl.2)
`ok`/`err`/`from` MUTATE the receiver and return it RE-TAGGED (`return this as unknown as
ParserState<S>` ×3, the file's only 3 casts) while the emitted .d.ts is indistinguishable from
PURE CONSTRUCTORS, and the three genuinely dangerous methods are the ONLY UNDOCUMENTED ones;
`ParserFunction` is published from `.` and /core so consumers MUST return through them. **Law:
result constructors are NON-ALIASING, or the in-place protocol is documented AND mark/rollback is
a separate first-class op (EQ-5/R-LAW-1; W2.md:250, :267, :275-278).** Companions: `save()`
captures 2 of 8 fields and `restore()` FORCES `isError = false`; the `offset` parameter is a DELTA
while the field is absolute, undocumented (`state.ok(v, newOffset)` silently DOUBLES the offset).
⟨pt-state.md · PT-STATE-2 and PT-STATE-14 (+ PT-STATE-26)⟩

### 4l. pt-utils (+entry) at W2

**W2.67 · pt-utils PT-UTILS-1 — scan primitives as combinator-surface citizens (BLOCKER)** (cl.2)
The two byte-scanners — AC-3's NAMED SUBSTRATE — cannot be delivered without instantiating BOTH
module globals from ANY door: they share a source module with `diagnosticsEnabled` and
`collectedDiagnostics`, so Rollup welds them into the 0-import diagnostics chunk, and the /utils
door prices at 46,131 B (also pulling the packrat chunk carrying `PACKRAT_ARMED`), CJS arm
unshakeable. **CURE-LOCK: THE SPLIT INTO `scan.ts` + diagnostics IS AC-3'S STATED PRECONDITION** —
a conforming consumer has no lawful door, the K-6/O-8 kill class. Honest bound preserved: globals
inert at import; structural availability, not measured cost. *Sharpens*: scope 12 (W2.md:188-191)
+ AC-3 (W2.md:346-359).
⟨pt-utils.md · PT-UTILS-1⟩

**W2.68 · pt-utils PT-UTILS-3 / -5 — the span-emitting scanner carries a LABELLED
unterminated-comment failure** (cl.2)
(3) An unterminated `/*` is OBSERVATIONALLY IDENTICAL to a legitimate division `/` (both leave
offset at the slash — measured identical:true) while css-syntax-3 §4.3.2 calls it a parse error
consuming to EOF, and the module's own `reportUnclosedDelimiter` sits 90 lines above unused; the
named downstream emits EXACTLY the diagnostic this destroys. (5) The scanners are
**COMP-1-HOSTILE BY CONSTRUCTION**: both end `state.offset = i;`, return void, write NO complement
channel — they destroy exactly the bytes `C` is defined to hold (W2.md:237, :241-244), with no
hook. **AC-3 authors the complement-emitting scanner ON THE KEPT TECHNIQUE; the raw exports
RETIRE.**
⟨pt-utils.md · PT-UTILS-3 and -5⟩

**W2.69 · pt-utils PT-UTILS-8 (+ ruling 1) — arming as a parse parameter dissolves the label-cost
class** (cl.2)
Callee-side arming forces call-site cost: `mergeErrorState` takes a STRING, not a thunk, so
consumer label expressions evaluate DISARMED, and `isDiagnosticsEnabled` is unreachable from every
subpath so consumers cannot branch. **LABEL COST LAW: construction-time labels (already the leaf
idiom) or thunks.** CORRECTION binding on any wave consuming pt-diagnostics row 14: the ONE live
internal instance is `parser.ts:419`'s eager slice on every failed `wrap()` close — the
`:469-472` instance both readers AND the pt-diagnostics record cite is DEAD CODE (`FLAG_EOF` has
zero assignments; `.eof()` routes through `skip(eof())`).
⟨pt-utils.md · PT-UTILS-8 + ruling 1 (corrects pt-diagnostics.md row 14's cell)⟩

**W2.70 · pt-utils PT-UTILS-18 — "trailing-content" has NO REACHABLE WRITER** (cl.2)
NEW (seat-found): `addSuggestion`'s sole EXTRA-MODULE call site is the dead `FLAG_EOF` branch, so
its only runtime-reachable caller is intra-module — therefore `Suggestion.kind`'s
`"trailing-content"` member has NO REACHABLE WRITER and the closed 2-member union is **1-MEMBER AT
RUNTIME**. Sharpens the 8-code taxonomy need: ParseIssue is D's element type, and a union that is
effectively "unclosed-delimiter" cannot serve it. (With W2.53's re-founding obligation.)
⟨pt-utils.md · PT-UTILS-18⟩

**W2.71 · pt-utils PT-UTILS-11 identity ruling — MINOR at family precedent; the cure design is
W2's** (cl.2)
The `:34-35` disarmed-path array allocations are MINOR at identity (pt-diagnostics row 22's K4
already re-rated exactly this row; both readers' MAJOR corrected). CURE RULING CARRIED: **the
LENGTH-GUARD, not the gated hoist** — the hoist strands stale non-empty
suggestions/secondarySpans across an arm→disarm transition mid-state-lifetime, changing observable
behavior; and the naive gate cure is additionally unsound (mis-attribution; a frozen sentinel
throws under armed `addSuggestion`). **CURE DESIGN IS W2'S, NOT A PATCH.**
⟨pt-utils.md PT-UTILS-11 · pt-diagnostics.md row 22 + Seat ruling 4 · pt-parser.md PT-PARSER-18⟩

**W2.72 · pt-utils-entry PT-UE-6 / PT-UE-3 — the EOF cure has an ORDERING LOCK** (cl.2)
CURE-LOCK (ordering rider, source-verified): the live EOF cure is `Parser.eof()` =
`this.skip(eof())` — the `FLAG_EOF` machinery both challenges credit is UNREACHABLE DEAD CODE —
but `.eof()` DOES NOT CURE the residue defect, because skip's error arm KEEPS `state.value`, so
`.parse()` still returns residue. **THE `.parse()`/residue repair MUST LAND FIRST OR WITH IT, or
the EOF fix is INVISIBLE AT THE PUBLIC SURFACE.** EOF is a total-consumption judgment in the
algebra; incumbent `.eof()` is priced 2.0.0 (acceptance narrowing; W4.14).
⟨pt-utils-entry.md · PT-UE-6 + PT-UE-3 (α-miss ordering rider)⟩

**W2.73 · pt-utils-entry PT-UE-2 — a wrong-V injector is invisible to K-1 and lethal to the
wave** (cl.2)
EQ-1 RIDER (K-1, W2.md:465-466): `escapedString`/`quotedString` do NOT unescape — `.skip()` keeps
the LEFT value so the `\uXXXX` payload is ANNIHILATED (`"\\u0041"` → `"u"`; `'"a\\u0041b"'` →
`"aub"`); only escapes whose LETTER EQUALS THEIR VALUE survive. The correct decoder ships 30 LINES
AWAY on the same subpath (`json.ts:22-24` → `"aAb"`). Two escape semantics, one 7-export surface,
both public at 1.0.0. **A wrong-`V` injector is INVISIBLE TO K-1.**
⟨pt-utils-entry.md · PT-UE-2⟩

**W2.74 · pt-utils-entry PT-UE-14 — the error CLASS FLIPS at the cold boundary** (cl.2)
The recursion ceiling on the shipped jsonParser is a JIT-state-dependent RANGE far below 7,761,
the throw escapes `.parse()` uncaught, AND AT THE COLD BOUNDARY THE CLASS ITSELF FLIPS:
`SyntaxError: Invalid regular expression: … Stack overflow` (V8's lazy regex compile failing under
exhausted stack, naming the library's own numeral pattern as invalid) at boundary depths,
RangeError above — a consumer's `instanceof RangeError` guard is UNSOUND and the error text points
at a grammar bug that does not exist. FOUR mutually inconsistent bad-input postures on one
surface. **W1's oracle asserts the CLASS SET and the escape path, never a depth constant** (§7
pt-utils-entry row 1; with W1.3).
⟨pt-utils-entry.md · PT-UE-14 + MEASURE-AT-OPEN 1⟩

### 4m. L-20 repair rows (folded 2026-08-07)

*Numbered at the tail so the sealed W2.1-W2.74 sequence — and every cross-reference into it —
stands unmoved; each row names the module bloc it belongs to. W2.75-W2.76 land from the fold
check's first pass, W2.77 from its second (the routing sweep, §1a n.8).*

**W2.75 · pt-lazy LZ-18 (surviving half) — the candidate's TYPING POSTURE: no phantom parameter,
erasure STATED at the seam** (cl.1 — **recovered by the L-20 fold check, §1a n.7**; belongs to §4e's
pt-lazy bloc)
Two of LZ-18's three halves are **ABSORBED, not re-booked**: the phantom `T` published at
`dist/lazy.d.ts:9` and the file's only un-annotated `any` leave the building with the **W2 RETIRE**
verdict already carried (LZ-3/LZ-4's capability ruling; the excision itself banked at §8 bloc 9) —
that is what LZ-18's disposition directs. **The surviving half is the algebra-wide one, and it binds
W2's CANDIDATE TYPING**: `createLazyCached`'s precise-looking `ParserState<T>` signature is **ERASED
ON CONTACT WITH `ParserFunction<any>`** — a signature that READS as parametric and constrains
nothing, the type-level twin of the admission bar W2.54 sets against `Parser<any>`. **FOLD: the
candidate algebra publishes NO PHANTOM TYPE PARAMETER — every declared parameter is inhabited by a
value the signature actually constrains, and any erasure at a seam (a thunk, a `ParserFunction`, a
cache slot) is DECLARED AT THAT SEAM, never left to be discovered in the emitted `.d.ts`.** With
W2.54 (`any` is structurally unsatisfiable under G-10, W2.md:236) this is the typing posture every
operator the algebra §6 keeps is measured against.
⟨pt-lazy.md · LZ-18 (D-C12/P3-1/P3-2) — the two W2-RETIRE-discharged halves stay absorbed⟩

**W2.76 · pt-packrat M4 — G-10's `memoize = 0` sweep BINDS THE ROOT BARREL, not only `./packrat`**
(cl.1 — **re-homed from W4.12 by the L-20 fold check, §1a n.6**; belongs to §4g's pt-packrat bloc)
G-10's structural sweep — "no `.opt()` child of any `all()`; `lazy ≤ 1` with an explicit depth
bound; **`memoize = 0`**" (W2.md:808-812) — is a **W2** gate, and its `memoize = 0` clause must bind
**THE ROOT BARREL**, not only the `./packrat` subpath: **the default surface arms the latch**, so a
candidate importing `memoize` from `.` passes a `./packrat`-scoped sweep while arming
`PACKRAT_ARMED` process-wide and one-way (W2.19's construction-forbidden-BY-NAME ruling; W1.13's
CONSTRUCTED-not-called predicate). The sweep's SCOPE is what this row sharpens, not its predicate.
**Filing note (L-20 repair)**: this row stood under §6's W4 heading, where "G-10" resolves to W4's
own **G-10 — THE R-A STAMP ACT** (W4.md:521) — an entirely different gate, and W4.md carries no
`memoize` sweep at all — so a W4 seat reading it there would have bound the wrong gate.
⟨pt-packrat.md · M4⟩

**W2.77 · pt-lazy LZ-19 — REGISTRY DISCIPLINE: one tag per producer for any registry-comparing
gate** (cl.1 — **split out of the W1.16 bundle by the L-20 pass-2 fold check, §1a n.8**; belongs to
§4e's pt-lazy bloc)
Four names for one concept, and **one `"lazy"` tag (`state.ts:149`) cannot distinguish the three
producers** — a thunk from a bound-method `args[0]` (proof cells `state.ts:141-152`;
`debug.ts:316-318`). The
alphabetical-first clause is KILLED (K-16); what survives is that the tag is the very thing making
the structural walk possible (S-5), which is why its granularity is a **gate** question and not a
naming one. **FOLD: any gate that COMPARES A REGISTRY carries one tag per producer — or a typed
`args[0]` — so that two structurally distinct producers can never collapse into one registry cell.**
The registry-comparing gates are **W2's**: **G-10**'s structural graph-walk (`W2.md:808-812` — "no
`.opt()` child of any `all()`; `lazy ≤ 1` with an explicit depth bound; `memoize = 0`", which walks
the BUILT graph precisely because a grep cannot) and **G-11**'s printed scan-capability census
(`W2.md:824-838`), both of which read producers off the registry a candidate publishes. With W2.30
(the harness descends a lazy edge via `args[0]` exactly once) this is the same instrument seen from
the tag side. **Filing note (L-20 repair)**: this limb was folded inside **W1.16**, where "G-10"
resolves to W1's own **G-10 — THE FIVE DEBTS ARE MEASURED, NOT ASSUMED** (`W1.md:564-580`, the
RED-7 published-surface probe) — a gate with no registry in it at all; W1.16 now carries only its
two genuine W1 limbs and points here.
⟨pt-lazy.md · LZ-19 (D-C13, trimmed; K-16/S-5) — source-routed `REFINEMENT FOLD → W2`⟩

---

## 5. W3 — Totality and Recovery to Spec (12 sharpenings)

**W3.1 · pt-diagnostics row 18 / R2-M1 — recovered-success representation RULED BEFORE W3.b
OPENS** (cl.1)
BINDING, and it must be **RULED BEFORE THE WAVE OPENS**, not improvised mid-wave: value.js's
frozen `ParseResult<T>` forbids diagnostics on SUCCESS — the success arm is
`{ok:true; value; diagnostics: readonly []}`, an EMPTY TUPLE — so `recover()`'s entire product
(partial success carrying N diagnostics, which GROUND-B asserts GREEN) is **UNREPRESENTABLE in the
/css contract**. Both challenges analyze only the failure arm. At adoption the lowering must
discard-on-success, flip `ok:false` on a success, or reopen the frozen contract; **the choice is
recorded in the wave spec** — under E-3, that means: recorded in this addendum's execution ledger
or a dated owner ruling BEFORE W3.b dispatches. *Sharpens*: unit .b (W3.md:253-270), which
currently specifies only the failure arm's closed union.
⟨pt-diagnostics.md · roster row 18⟩

**W3.2 · pt-diagnostics row 7 / L·B-3+C·D-18 — mark/rewind per R-LAW-1 (BLOCKER, kill REVERSED)**
(cl.2)
`recover()`'s rollback is an unqualified LIFO `pop()`, not a mark/rewind: under the canonical
two-level composition (outer recover whose sync itself recovers — EXACTLY a resilient CSS
stylesheet's block/declaration shape) a failed outer sync DESTROYS the inner's legitimate record
and RETAINS the outer record it promised to remove, silently, length restored. LIVE WITNESS P3
(neither reader ran it): journal = 1 where `parser.ts:673` promises 0, survivor = D_outer.
**W3.b implements mark/rewind per R-LAW-1 (record journal length before sync, truncate on
failure), per W2.md:275-278.** (W3.b's current text, W3.md:253-270, carries no mark/rewind clause
— this row adds it.)
⟨pt-diagnostics.md · roster row 7 + Seat ruling 1⟩

**W3.3 · pt-diagnostics rows 8+9 — the EQ-5 rollback-exactness rider** (cl.1)
`collectDiagnostic` ends in `resetErrorState` on the LIVE parse state (`furthest = −1`) and
NEITHER recover exit path restores it; the failed-sync path — whose whole purpose is to let the
error propagate untouched — propagates a state whose furthest/expected/suggestions/secondarySpans
were already destroyed (`popLastDiagnostic` restores the BUFFER, nothing restores the STATE; the
`parser.ts:673` comment is true of the journal and false of the state). Live witness P4: furthest
2 → 0, unarmed. **BINDING on W3.b's mark/rewind: snapshot {furthest, expected, suggestions,
secondarySpans} around `sync`, not just journal length.**
⟨pt-diagnostics.md · roster rows 8 and 9 (MERGED — 9 folds via 8)⟩

**W3.4 · pt-parser PT-PARSER-3 — recovery as an append-only per-parse VALUE (BLOCKER, LIVE)**
(cl.2)
SEVERITY RAISED because THE COMBINATOR IS LIVE: the band's only real parse-that consumer wires
`.recover()` and asserts `getCollectedDiagnostics()` UNARMED, with two PASSING vitest assertions —
L-M1's "write-only" premise is DEAD and L-B3's own "unreachable in practice" falsifier is refuted.
`recover()` pushes into a module-global `Diagnostic[]` with NO IDENTITY on the record and pops
POSITIONALLY on sync failure; the buffer is OUTSIDE the packrat epoch (PackratEpoch has five
members, not six); the pop path leaves the outer parse's furthest DESTROYED. **W3.b's mark/rewind
snapshots the FOUR state fields, not just journal length** (one discipline with W3.2/W3.3).
⟨pt-parser.md · PT-PARSER-3 + seat ruling 2 / SEAT-K2⟩

**W3.5 · pt-diagnostics row 19 / R2-M2 — R-LAW-5 declared + the non-final-arm corpus row** (cl.2)
R-LAW-5 — the ONE recovery law that names `recover` (W2.md:287-290) — appears ZERO times in either
challenge doc, and live parse-that violates its declaration requirement: a recovered branch
returns `state.ok(sentinel)` and `any()` returns on the first non-error arm, so a recovering
NON-FINAL alternative silently STARVES every later arm. Nothing in README declares it; zero tests
pin it. Lands on the band: both candidates are any/dispatch-shaped. **W3.b declares the
alt/recover rule (forbid non-final-arm recovery or declare the alternative); W1 RIDER: the
malformed corpus gains a non-final-arm recovery construction so the G-4 probe can bite** (§7
pt-diagnostics row 4).
⟨pt-diagnostics.md · roster row 19 + MEASURE-AT-OPEN 4⟩

**W3.6 · pt-diagnostics row 25 (+26) / C·D-12 — Diagnostic ↛ ParseIssue: NO total map** (cl.1)
Diagnostic is structurally non-conformant with frozen ParseIssue on all five EQ-4 fields: no
`code`, two positions with no invariant, no `end` (unrecoverable — `found` clamps at 20),
armed-only `expected`, `found` where `actual: string|null` is wanted. **NO total
information-preserving map exists. W3.b's codes/diagnostics unit maps furthestOffset → start and
derives `end` from its OWN span discipline; NOTHING maps from Diagnostic.** Rider:
`Diagnostic.offset` is write-only — record it as un-interpreted, never map it (with W2.12).
*Sharpens*: unit .b's `{lower,diagnostics,codes}.ts` (W3.md:268).
⟨pt-diagnostics.md · roster rows 25 and 26⟩

**W3.7 · pt-diagnostics row 27 / C·D-15 — recovery-skipped bytes enter C as spans** (cl.1)
`found` is lossy four ways on one line (fixed UTF-16 cut with a lone-surrogate hazard,
single-character escape list letting `\r`/`\t`/`\x1b` into ANSI context, a fresh RegExp per
record, and NO extent field anywhere) — the shape's only byte-level record cannot carry
COMP-1/R-LAW-2 (W2.md:241-244, :279-280); the complement cannot be reconstructed. **W3.b:
recovery-SKIPPED BYTES enter `C` as `(offset, length, kind)` spans; `found` is not a carrier and
nothing ports it.**
⟨pt-diagnostics.md · roster row 27⟩

**W3.8 · pt-parser PT-PARSER-16 — the ParseIssue adoption arithmetic, PRICED BEFORE THE WAVE**
(cl.1)
BINDING as EXPLICIT PRICED OBLIGATIONS, not discoveries-at-open: parse-that's default-path failure
state can produce AT MOST ONE of value.js's five frozen ParseIssue fields. `start` derivable
(`parser.ts:60`); **`end` has NO PRODUCER ANYWHERE** (ParserState carries offsets only;
SecondarySpan is a point; the 15 `*Span` builders were excised at 1.0.0 — a KNOWN delegation per
CHANGELOG r6 #8, but NO corpus row priced the frozen field); `expected` is armed-only;
`actual`/`found` exists only via `collectDiagnostic` — an O-8 breach to obtain. **W2 §3b and W3's
typed-recovery unit each carry the three missing producers.**
⟨pt-parser.md · PT-PARSER-16 (β-miss 2)⟩

**W3.9 · pt-debug B-2 + MISS-1 + i-4 — one line/column law, one implementation (BLOCKER)** (cl.2)
`state.ts:128`'s `lastIndexOf("\n", offset−1)` clamps at offset 0 per ECMA-262, so a leading
newline is FOUND: `"\nabc"` → `{line:2, column:−1}` where truth is `{1,0}`. MISS-1 is the decisive
fact: `formatDiagnostic`'s HEADER prints the CORRECT `1:0` from `collectDiagnostic`'s slice-first
math while the caret block underneath recomputes through the broken helper — **one rendered
diagnostic contradicts itself**. **CURE DIRECTION IS RULED: install the slice-first algorithm INTO
`state.ts`, never the reverse** (the reverse cure injects a negative column into every offset-0
Diagnostic). Three implementations, two dead, disagreeing. *Sharpens*: W3.b + G-3/G-8
(W3.md:398, :487).
⟨pt-debug.md B-2 / MISS-1 / i-4 · pt-diagnostics.md rows 20/30 + K1 · pt-core.md row 11 (PRIMARY; its in-library cure stays EXCLUDED, §8)⟩

**W3.10 · pt-packrat C-3 — disarm-and-refuse-mid-grow** (cl.2)
Public `resetPackrat()` can CRASH a live grow (three non-null asserts on a clearable Map with user
code on the stack via `.map`/`.chain`/`.mapState`/`recover` under `evalParser`) and is
non-load-bearing everywhere in-tree, while the module header still INSTRUCTS the call. **W3's
already-written ruling applies (W3.md:288-292): the disarm must ACTUALLY DISARM rather than only
clearing.**
⟨pt-packrat.md · C-3⟩

**W3.11 · pt-packrat C-8 + S-7 — the budget breach becomes a ParseIssue VALUE** (cl.2)
In-parse RangeError off an unresettable, unobservable process-global counter (K-8 class,
W2.md:477-478), burn rate DOUBLED by `.trim()`'s discarded parser. **W3 totality unit: the breach
becomes a ParseIssue VALUE per K-8 and R-LAW-3 — and the LANDING TYPE ALREADY SHIPS FROZEN on
value.js's 52-export /css surface, so the conversion is RE-PLUMBING, NOT INVENTION.** *Sharpens*:
G-9 (W3.md:504) and unit .c (W3.md:288-292).
⟨pt-packrat.md · C-8 + superlative S-7⟩

**W3.12 · pt-split PT-SPLIT-3 / -9 — typed boundary and typed recovery, the receiving type
already frozen** (cl.2)
(3) The laundering is LOOP-WIDE, not fast-path-only: every loop operation is Array-polymorphic, so
`splitBalanced(["x",",","y"],",")` → `[["x"],["y"]]` from the SLOW path, typed `string[]`. A cure
at the `return [text]` line alone LEAVES THIS OPEN; **the cure is a named boundary invariant at
entry** per W2 §3.3 and parser-band debt 4. (9) An unterminated quote swallows the remainder and
`string[]` makes failure unrepresentable — while ParseResult's `ok:false` arm with its NON-EMPTY
tuple is ALREADY EXPORTED on the frozen /css surface: **the cure is not a design question, the
type is waiting.** *Sharpens*: G-3 (W3.md:398).
⟨pt-split.md · PT-SPLIT-3 and -9⟩

---

## 6. W4 — The PLAW-BIND Integration and the Release Condition (14 sharpenings)

**W4.1 · MERGED CROSS FOLD — the gate-authoring / vacuity law** (cl.1)
**No wave gate may be satisfiable by its failure mode (L-19).** Gates assert
REACHABILITY/EXCLUSION over real module graphs, never a name list, never `existsSync`+`typeof`,
never a braces-only regex that cannot see `export *`. Exhibits: pt-debug m-11 (the two functions
carrying the BLOCKERs are the two with vacuous assertions); pt-state PT-STATE-8
(`proof-no-span-surface` enumerates 15 excised builder names so it is GREEN over the residue by
construction); pt-split PT-SPLIT-7 (NO `proof:*` script runs in CI at all — `npm test` is bare
vitest; the cross-tree sweep opens `if (!existsSync(dir)) return acc` and its vacuity guard only
checks that dispatch is defined, never a nonzero visited-file count); `dist-surface.test.ts`'s
regex leaves 5 of 34 exports in NEITHER compared set. **Release-condition predicate: any census
gate a release cites must be CI-wired AND assert a nonzero visited-file count.** *Sharpens*: RC-P
and every W4 gate (W4.md:318-319's L-19 sentence made a checkable predicate); restates
W2.md:724-725 and W1.md:349-353 at release altitude.
⟨pt-debug.md m-11 · pt-state.md PT-STATE-8 · pt-split.md PT-SPLIT-7 · pt-leaf.md PT-LEAF-18/-20 riders · pt-index.md C-M1 · pt-parsers.md PT-PARS-6/MISS-9 · pt-utils-entry.md PT-UE-12⟩

**W4.2 · MERGED CROSS FOLD — subpath/tier isolation asserted over IMPORT GRAPHS** (cl.1)
**The release gate imports the shipped entry in a BARE process and asserts the LOADED-MODULE SET
(and, under both `import` and `require` conditions), never file existence + typeof.** Evidence:
`dist/core.js:1` → packrat chunk → diagnostics chunk in two hops; `dist/core.cjs:3` is an EAGER
non-elidable require (tree-shaking has no purchase on a require graph, so "a bundler may drop it"
covers only one of the two shipped conditions); /utils = 46,131 B ESM / 47,293 B CJS, marginal
cost over /core only 703 B. **W1 rider (pt-state): the harness's subpath reading gains the missing
exclusion assertion BORN-RED** (= W1.18) until Parser's debug/packrat edges split or the prose
amends. *Sharpens*: G-3's packed-surface resolution (W4.md:383) — resolution is not isolation.
(**PT-UE-9 is routed `W0/W4` at source; this row is its W4 limb — the W0 limb, that a tier promise
carried out of the fresh root is an import-graph claim and never a file-existence one, is owned by
**W0.2**, L-20 pass-2 repair §1a n.10.)
⟨pt-leaf.md PT-LEAF-17 rider · pt-parsers.md PT-PARS-7 · pt-packrat.md C-16 · pt-lazy.md LZ-11 · pt-utils.md PT-UTILS-1 rider · pt-utils-entry.md PT-UE-9 (W4 limb; W0 limb at W0.2) · pt-state.md PT-STATE-3 · pt-core.md row 20⟩

**W4.3 · pt-core row 3 / D-03·C-1 (MERGED with pt-ansi row 11 / C-D-6, §1a n.2) — the core.ts
docblock cure-lock + RC-P chunk topology (BLOCKER)** (cl.2)
`core.ts:3-5`'s three denials are false on two of three clauses in the shipped artifact —
the core chunk carries the diagnostics accumulator (5 symbols, chunk line 1) AND the packrat tier
(`makeMemoized` :721, `memoize` :844, `PACKRAT_ARMED` :678/:682/:714/:722); only json/csv
survives — and it is unshakeable by construction (`parseState` calls `packratEnter`/`Exit` every
parse; `mergeErrorState` reached from 15+7 sites; debug/print are class methods). Byte closures:
/core = 45,428 B = 93.52% of the root barrel; delta 3,148 B = 6.48%. **BINDING: any wave touching
`core.ts` corrects the comment to shipped truth or makes it true.** + W1/D-17 gate note: a
module-graph assertion would have caught it the day `core.ts` was written (= W4.2's mechanism).
⟨pt-core.md · roster row 3 · pt-ansi.md row 11 (cross-book, merged)⟩

**W4.4 · pt-core row 5 / M1 — the dual-package hazard is LIVE** (cl.2)
The exports map declares `import` AND `require` for all five subpaths; the build ships two
complete module instances (packrat chunk 40,576 ESM / 41,041 CJS; diagnostics 3,516/3,787). Three
process-global latches exist TWICE. Measured 4/4 identity probes FALSE. The subpath-gate loads
only the import targets. **RC-P: single-format ship, or a cross-format identity/latch gate**
(+ row 44's identity-gate rider). (W1 cell-law half: W1.12.)
⟨pt-core.md · roster row 5⟩

**W4.5 · pt-utils-entry PT-UE-21 — dual-package forks the DIAGNOSTICS globals too** (cl.2)
The dual-package split forks the diagnostics module globals as well (`utils.js` imports
`diagnostics-DDazRHgl.js`; `utils.cjs` requires `diagnostics-DpUY87_d.cjs` — two instances of
`diagnosticsEnabled`/`collectedDiagnostics`), so `enableDiagnostics()` from one format SILENTLY
NO-OPS for the other format's parsers — and /diagnostics is a published subpath whose ENTIRE
CONTRACT is those globals. **W4 rider: a dual-package RESOLUTION MATRIX** (with W4.4).
⟨pt-utils-entry.md · PT-UE-21 (β-miss)⟩

**W4.6 · pt-core row 20 (+48) / D-17·C-12 — the RC-P gate surface** (cl.2)
The only gate naming /core tests RESOLUTION, not isolation: three typeof checks over four
subpaths; dist-surface derives its expectation FROM the source barrel; no golden export list
anywhere — deleting 16 of 18 runtime exports leaves every gate green. **RC-P: module-graph
assertion + golden export list + declaration-surface check** (with W4.2/W4.7). Row 48
(Suggestion/SecondarySpan completeness) rides this work.
⟨pt-core.md · roster rows 20 and 48⟩

**W4.7 · pt-ansi rows 4+5 + pt-core row 47 (SF-2) + pt-diagnostics row 17 — the
declaration-surface acceptance list (MERGED)** (cl.2)
W4.b/RC-P: dts-include narrowed or `@internal`-tagged and the subpath gate extended to the
**DECLARATION surface**. Acceptance list = the seven top-level orphans
{ansi, debug, lazy, leaf, parser, split, state} (**`index.d.ts` is the routed types target, NOT an
orphan — corrects L-D4**) PLUS `dist/parsers/{csv,json,index,utils}.d.ts` (declaration-only ghosts
of the json/csv tier /core denies) PLUS `dist/debug.d.ts`'s formatter declarations with no runtime
in any bundle. Ten ansi symbols carry no `@internal`, no deprecation, zero changelog rows.
*Sharpens*: G-3 (W4.md:383) — the packed gate resolves runtime symbols only.
⟨pt-ansi.md rows 4+5 · pt-core.md row 47 (SF-2) · pt-diagnostics.md row 17⟩

**W4.8 · pt-ansi row 10 / C-B2 — the prototype-weld cure-lock** (cl.2)
The TTY payload rides 4 of 5 entries into browser-only consume paths, PINNED BY CLASS PROTOTYPES
(`state.ts:136-138`, `parser.ts:690-700`) which `sideEffects:false` cannot reach — no bundler cure
exists. **W4.a seam + W4.b/RC-P: welds → free functions, renderer re-homed behind a SIXTH
`./debug` entry** (2.0.0 precedent = the 1.0.0 `*Span` excision). Unminified region 28-285 =
8,517/40,576 = 21.0%; minified figures NOT re-derived (§7 pt-ansi row 1).
⟨pt-ansi.md · roster row 10⟩

**W4.9 · pt-ansi row 14 / M-2 — the acceptance-probe spelling** (cl.1)
C-B2's prescribed acceptance probe is case-blind: source spells `\x1b`, Rollup ships `\x1B`
(measured: `grep -c 'x1b'` → 0/exit 1 vs `grep -oi` → 18 on the chunk carrying all nine arrows).
**The acceptance spec is sharpened to `grep -ci 'x1b\['` or a raw U+001B byte match.**
challenge-C:105 and challenge-L:179 disagree; only one probe can fail. **BINDS the value.js
adoption gate before it first runs** — W4.a/.b author the spec the X·V adoption wave executes
(W4.md:246-276); the probe itself has NEVER RUN (§7 pt-ansi row 2).
⟨pt-ansi.md · roster row 14 + Closing verdict⟩

**W4.10 · pt-debug M-1 = C-1+C-2 (+ MISS-8, SUP-6) — published-surface parity (exports-map ↔
.d.ts ↔ docs)** (cl.2)
Four functions mandated for `./diagnostics` by parse-that's own ratified A.W3, documented in
README, importable from NO published entry; the gate never looks; `dist/debug.d.ts` still declares
all 8. **The W4 parity gate is authored FROM `dist-surface.test.ts`'s own header (SUP-6): it
states its own provenance (the 0.8.2 incident) and thereby makes its own blind spot diagnosable —
source-barrel→dist cured, published-.d.ts→exports-map OPEN.** RIDER (MISS-8): the parity gate
should ALSO diff docs-documented names against resolvable names — `api.md` documents four
unreachable names and a `ParserState.addCursor` METHOD that has never existed.
⟨pt-debug.md · M-1 + MISS-8 + SUP-6⟩

**W4.11 · pt-index AD-5 + C-m5 + C-m4 + C-M11 — release conditions** (cl.2)
(AD-5) Shipped BASENAME COLLISIONS: `dist/utils.js` (the /utils entry bundle, 7 names) vs
`dist/utils.d.ts` (the diagnostics/scanner module, intersection 2 names); same structure for
`packrat.js`/`.d.ts`. Inside one tarball `./utils.js` resolves to two different modules; only the
explicit `types` keys save modern resolvers; **NO GATE compares a subpath's types against its
import.** (C-m5) The genuine orphans are `ansi.d.ts` + `debug.d.ts` only — the other internal
.d.ts are LOAD-BEARING via `dist/index.d.ts`'s relative re-exports. (C-m4) `./package.json` absent
from the exports map. (C-M11) **Manifest claims must be checked against ARTIFACT BYTES** — the
gate mandating `sideEffects:false` only stats paths, so it cannot fail for its intended reason
(W4.1's class).
⟨pt-index.md · AD-5, C-m5, C-m4, C-M11⟩

**W4.12 · pt-packrat M1 — the artifact-parity clause (the `.d.ts` / `.js` export-count split)**
(cl.1)
(M1) `dist/packrat.d.ts` declares SIX exports from `packrat.ts` while `dist/packrat.js` exports
THREE from `packrat-entry.ts`; both ship under `files:["./dist"]`; a node10 deep import typechecks
against 6 names and throws at runtime against 3 → **W4 artifact-parity clause** (with W4.7/W4.10).
**CROSS-REFERENCES, NOT BOOKINGS (L-20 repair, §1a n.5/n.6)** — this row's two other pt-packrat
facets sharpen **W2** gates and are folded under the W2 heading, because a W4 seat reading "G-10"
in §6 resolves it to W4's own **G-10 — THE R-A STAMP ACT** (W4.md:521), not W2's `memoize = 0`
sweep (W2.md:808-812): **M3** (dead flag machinery = the counter-example class for W2's enumeration
discipline; `.trim()`'s two-ids-one-burned consequence) is ONE IDENTITY with pt-lazy LZ-9 and is
booked once at **W2.32**; **M4** (the `memoize = 0` sweep binds the ROOT BARREL, not only
`./packrat`) is booked at **W2.76**.
⟨pt-packrat.md · M1 (M3 → W2.32 · M4 → W2.76)⟩

**W4.13 · pt-packrat-entry C-10 / M-8 / M-9 / M-10 — harvest-framing folds** (cl.1)
(C-10) Corrected chronology carried into the harvest: the 1.0.0 freeze (2026-07-03) PRECEDES the
dependency removal (07-17) and the band ruling (07-27) — "froze in defiance of a known ruling"
must not propagate. (M-8) CHANGELOG:100-102 ratifies the zone map and FORBIDS splitting
`parser.ts` IN THE SAME SENTENCE — the framing is "the ruling ratified its own falsification", not
"the build falsified a ruling". (M-9) The two challenges grade the identical non-publication of
the epoch primitives a SUPERLATIVE and a BLOCKER against the same commit with identical
measurements and neither names the conflict — **adjudicated for the superlative; the harvest
cannot bank both**. (M-10) PROVENANCE HYGIENE: OP-7 receipts must record TREE STATE.
⟨pt-packrat-entry.md · C-10, M-8, M-9, M-10⟩

**W4.14 · pt-leaf PT-LEAF-27 — THE SEMVER PRICING LAW (changes how every other row leaves the
building)** (cl.1)
**BINDING ON W4's RELEASE CONDITION AND THE O-15 CHANNEL: every proposed cure carries a SEMVER
CLASS.** parse-that is published 1.0.0 and the standing ask is a MINOR (O-15: evidence packet
attached to the 1.1.0 ask, owner-held, NO code change requested) — yet the challenges' top-ranked
cures are BREAKING: the rank-1 deletion of `leaf.ts:327-330` is a semantic WIDENING (inputs that
reject today would accept); every `all()`/`any()`/dispatch semantics change and all five §7
RETIREs are 2.0.0 events. 1.1.0-ELIGIBLE: PT-LEAF-4 (delta gate), -10 (Int16Array), -19 (doc
move), -20 (dead internal machinery), -23 (guard), -24 (hoist), -25 (fill/contiguous), L-m1,
L-m9, L-m13. 2.0.0 EVENTS: PT-LEAF-1/-2/-3/-6/-7/-8/-11/-18 and every export retirement. **NONE of
the 2.0.0 rows may ride the standing minor.** (Applied across §8's excluded blocs at
pt-parser INFO-5, pt-utils, pt-utils-entry.)
⟨pt-leaf.md · PT-LEAF-27 (β-miss 1) + Closing verdict⟩

---

## 7. MEASURE-AT-OPEN — the consolidated wave-open measurement list (cl.3; W1's rig owns it)

**Hoisted law, stated once** (each module row below inherits it whole): **one fresh process per
cell · one module format per cell (W1.12) · nothing armed in any timing cell · every
arming/probing PID is a POISON CELL — disposable, never reused in a timing cell · NO PROBE NUMERAL
IS PORTABLE (the pt-leaf K18 law) · no number below is a bar (the bar is OWNER-GATED,
W1.md:515-527) · absolute figures RETIRED by the apotheoses STAY RETIRED here — ratios between
interleaved cells only (W1.md:441-448) · non-ASCII rows use ESCAPES, never raw glyphs · every
census states its counting unit beside its pattern · chunk names re-derived from the dist entry's
import specifier at the moment of use (W1.15) · consumer-root HEAD receipts re-resolved at use
(W1.20).**

- **pt-ansi (rows 1-6)** — (1) C-B2's MINIFIED figures (3,509 B ansi+debug region / 13,883 total /
  25.3%) are an esbuild re-bundle, not statically derivable; re-run before any byte figure is
  banked forward. (2) The adoption acceptance probe (build value.js ./css against a parse-that
  substrate; probe with W4.9's corrected spelling → expect 0) has NEVER RUN. (3) The
  `enabled === true` arm has NEVER been observed in any session (every seat's stderr was piped) —
  one cell under a real PTY witnesses both redirection directions (`> file` polluted; `2> file`
  de-coloured). (4) `parsethat-surface-gaps.mjs` RED-7 figure — the prober ARMS diagnostics and
  constructs memoized parsers; **disposable process only**. (5) The W1.8 leak witness —
  two-emission probe under a throwing logger, disposable process only (module-global
  `debugDepth`). (6) K8's V8 const-folding question — measure only if the factory refactor is
  re-proposed.
- **pt-core (rows 1-6)** — (1) PT-03 armed-tax **1.47×** — O-15 N=1, one machine, NEVER re-derived
  by any core seat; re-measure BEFORE the figure is banked into any gate or cure justification.
  (2) PT-04 depth ceiling — a stack-budget property, not a library constant: 7,761 (O-15) and
  ≥9,000 (L) are machine-facts of DIFFERENT CONSTRUCTIONS; G-9 asserts a typed bound exists, NEVER
  a number (W1.3). (3) V8 shape/allocation costs (mapState's Object.create views,
  per-construction/per-advance arrays, HOLEY arms) — all unmeasured; harness rows only if a cure
  is justified on SPEED rather than hygiene. (4) Minified/gzip byte figures — `minify:false`
  ships, so 93.52%/6.48% and the 73%-maps ratio are GRAPH FACTS while the absolutes move; the byte
  closures in W4.3 are the unminified baseline. (5) RED-7 probe — **repair first** (never-RED row,
  never-GREEN row, embedded bench, wrong workspace; W1.10), then disposable process only. (6)
  Dual-format latch-split witness — one disposable process at W4 open ONLY IF the dual-format ship
  survives RC-P.
- **pt-debug (7 rows)** — (M-2) exponential curve — depth 12→18: 1.21→31.70 ms (N=1), prior pass
  depth 22 → 10.7 s; assert the CLASS (≈2× per +1), never constants. (m-9) print ceiling —
  non-monotonic 5000 OK / 6500 RangeError / 7000 OK (inherited, never re-run); record as an
  INTERVAL with the V8-frame-size invalidation. (PT-04) parse ceiling — lazy deepest OK 7,761 /
  RangeError 7,762; W1's corpus depth bound restated against the re-measured ceiling. (M-6/MISS-2)
  allocation — ~200 kB/step multi-line; ~20 MB per 100 diagnostics on 100 kB (derivational);
  heap-delta per call IF any of this code survives the W2 WRAP. (i-2/C-15) first fresh id — 38 via
  root barrel with ±1 probe-order drift; ENTRY-SPECIFIC, measure per entry. (C-13) escape
  leakage — 20 ESC sequences into a plain-array logger under a pty; cell-dependent, re-measure in
  the cell owning G-5. (unarmed baseline) 60.9 ns/parse median from `parsethat-surface-gaps.mjs` —
  the runner is RESCUED ONTO TRACKED DISK by W1's own gates (G-3).
- **pt-diagnostics (rows 1-5)** — (1) C·B-4's 160.8 MiB / 800,004-object retention —
  challenger-measured, N=1, `--expose-gc`, never re-run; re-measure before banking; the STRUCTURAL
  mechanism is settled without it. (2) L·M-1's magnitude — INFO-A's refutation is METHOD-UNSOUND
  (a heapUsed delta measures retention, not young-gen churn); if any cure is justified on speed,
  the instrument is an INTERLEAVED-CELL THROUGHPUT BENCH on a progressing grammar (the DEBT-2
  reject leg), NOT a heapUsed delta. (3) C·D-6's measured ESM/CJS non-identity — one
  disposable-process witness at W4 open ONLY IF dual-format survives RC-P (shared with pt-core
  row 6). (4) **W1 CORPUS ROWS**: a non-final-arm recovery construction (so the R-LAW-5 starvation
  probe can bite; W3.5) and a ZERO-WIDTH `sync` inside `many().recover()` (truncates silently at
  the first recovery while the diagnostic stays in the buffer — an R-LAW-4 under-count). (5) The
  G-5 amendment — bank the P1 predicate at bench-cell ENTRY AND EXIT (W1.11); until then G-5's
  stderr proxy stands as specified.
- **pt-index (rows 1-7)** — (1) M-1's import-time construction count (37 vs 1
  dispatch/Int8Array) — runtime import probe, fresh process. (2) C-M10's tree-shake byte cells
  (36,359 / 37,142 / 32,782 / 118 / 901 / 304) carried by both readers as receipts and NEVER
  re-measured in r2, plus r1·NEW-C's /utils cells; re-pin against the wave's dist. (3) The
  rollup-retention probe for L·M-2/C-M11 (r1 reports a four-cell run at rollup 4.59.0); re-pin at
  wave open. (4) C:S-6's fold-to-zero: `grep -c PACKRAT_ARMED` → 0 on a memoize-free /core
  bundle. (5) O-15 PT-03's 1.47× and PT-04's 7,761-frame ceiling — cited receipts, NEVER
  re-measured by any seat in this corpus; W1 G-9 re-establishes them. (6) C-M5's RED-7 executed
  FROM THE css-parser WORKSPACE (proven resolvable, W1.21) — turn both readers' derived tables
  into ONE run receipt; unarmed run only. (7) AD-1's negative-advance loop — statically certain;
  an optional two-line bounded-iteration witness makes it exhibit-grade. r1·NEW-F's
  holey-vs-packed magnitude is THE CORPUS'S ONLY TRUE BENCH ROW.
- **pt-lazy (rows 1-6)** — (1) DEPTH-CEILING FAMILY — reconcile 2,833 (cold, shape A) / 6,203
  (cold, shape B) / 10,336×3 (cold, wrapper-nest) / 7,761-7,762 (`--stack-size=984` per harvest;
  ALSO W1's pasted default run — the M11 contradiction) / 7,787 (default, harvest) / 8,277
  (default, challenge-C, same instrument) under W1 cell law: ONE SHAPE PER CELL, stack-size
  declared, cold/warm separated, prior-overflow state pinned. Gate predicates stay CLASS-ONLY
  (RangeError thrown → must become `ok:false`; arity). (2) LZ-8 falsifier:
  `Parser.lazy(() => memoize(inner))`, parse once, assert the next `packratEnter` snapshot's
  `memo.size > 0` — fresh disposable process; if 0, LZ-8 is WITHDRAWN. (3) LZ-1 quantification:
  id-burn doubling on `toString()` (receipt 6+6) + retained-heap witness, under a sha-pinned dist.
  (4) Hysteresis + JIT spread: intra-process 2.89× and the post-overflow collapse (3,000 fails
  where 1,000 passes) — decides whether W1's bench cells must be ONE PARSE PER PROCESS near depth
  work. (5) M10: mapState foreign-shape magnitude (`--trace-maps` class). (6) S-4's IC probe
  (`--trace-ic` over ≥16 lazy nodes) — INFO-grade, only if the candidate multiplies back-edges.
- **pt-leaf (rows 1-11)** — (1) Elements-kind predicate — re-run the five-predicate
  `--allow-natives-syntax` probe on the lane's PINNED node; W2 pins ONE construction shape against
  the MEASURED predicate, not folklore. (2) Offset-monotonicity oracle — asserts
  `result.offset ∈ [entry, src.length]` over EVERY operator incl. hostile matchFunction pairs;
  carries K18's law: the invariant is `offset := lastIndex(last run) − savedOffset`, NO PROBE
  NUMERAL IS PORTABLE. (3) Diagnostic realloc counts on the reject-path leg. (4) Reject-path alloc
  hoist — before/after alloc-count under `--expose-gc` via the existing proof:perf mechanism. (5)
  Fused-vs-chained depth ceiling against PT-04's baseline. (6) Arity-equivalence property suite —
  arity 2/3 unrolls vs the general arm, then-vs-sequence shape equivalence, and W2.37's POSITIONAL
  rows (leading/middle/trailing optionals) — the missing `leaf.test.ts`'s successor. (7) Non-ASCII
  whitespace sweep — ASCII corpora CANNOT separate the three classes; NUL/C0/NBSP/BOM/U+2028/VT
  rows WITH ESCAPES. (8) Module-edge assertion — the release gate imports shipped ./core in a bare
  process and asserts the LOADED-MODULE SET (W4.2). (9) Dispatch probe-design law — arms must be
  TOTAL on any first character (`regex(/./).map(tag)`) so a mis-dispatch is observable; both
  challenge seats hit the masking trap independently. (10) Census instrument law — function
  exports counted as BOUNDED CALL SITES `(^|[^.[:alnum:]_])sym\(`, value exports as bounded
  references; §10's prose is STRUCK, the numbers stand. (11) Chunk-name re-derivation rule
  (hoisted; W1.15).
- **pt-packrat (rows 1-11)** — G-4's never-armed clause re-specified as the RETAINED-HEAP form (no
  arm-state accessor exists on any published surface, so "read from the dist" can only be the
  proof-packrat-armed Map-count mechanism). (1) PT-B1 revival — prediction `b.value === "hello"`;
  `"world"` demotes C-1 to a doc-gap MINOR. (2) Mid-grow `resetPackrat()` crash — a `.map` calling
  `resetPackrat` inside an LR grow; no throw refutes the crash arm. (3) The latch ratio — UNARMED
  vs ARMED vs after-reset; the RATIO and one-wayness are the claim of record (O-15's
  93.9/138.2/139.3 vs W1's 55.6 unarmed; absolute ns non-portable). (4) Dual-copy witness — import
  `parse.js` + require `parse.cjs` in one process; overlapping `Parser().id` ranges confirm the
  collision precondition, then the split-latch arm in a second disposable process. (5) Lazy-armed
  retention heap snapshot. (6) L-M3 seed identity. (7) L-M4 nested head (`HEADS.has(pos)` throw
  over the full suite). (8) L-M7 counter-probe — prediction: the "cache hit" is a COLD RE-PARSE
  and the named gate is vacuous. (9) L-i1 memoized depth ceiling vs 7,761 — prediction materially
  lower. (10) C-14 identity rider — `memoize(p)` vs `mergeMemos(p)` byte-identical. (11) L-B1 ε
  counterexample — `H = H R | 'x'; R = H 'z'` over `"xz"`; prediction: spurious accept consuming
  the whole input.
- **pt-packrat-entry (rows 1-6)** — one deliberately-arming process per cell, one module format
  per cell (the M-7 rule), against the dist as built at wave-open, anchors re-resolved first. (1)
  E-B1 end-to-end — the mixed-format falsifier (ESM memoize node inside a CJS-driven graph);
  prediction `second === "hello"`; `"world"` refutes mechanism 1 and drops the package-scope row
  to the id-collision arm alone. (2) M-3 magnitude — allocation delta of the two throwaway
  MemoCell literals over a grow-heavy armed corpus. (3) **ARM-TAX RE-BASELINE — ALL ABSOLUTE ns
  RETIRED** (93.9 is already contradicted by W1's own 55.6); report RATIOS BETWEEN INTERLEAVED
  CELLS ONLY per W1.md:441-448's standing law; the 1.47× and 139.3-after-reset claims re-derive
  same-run. (4) C-3/E-M1 percentages (91.1%, 44,250/48,576) re-derive against the wave-open dist.
  (5) Import-time Parser census — reconcile 37 (this corpus) vs 36 (index/M-1) ONCE, against ONE
  build. (6) PT-04 memoized frame-cost — binary-search in an arming cell; prediction: strictly
  below 7,761.
- **pt-parser (rows 1-9)** — (1) Latch blast radius — construct `memoize(p)` in one module, NEVER
  CALL IT, measure an unrelated grammar's parse in the same process vs a fresh one; poison cell by
  definition. (2) Diagnostic realloc counts on the reject leg, DISARMED. (3) `wrap()` slice
  magnitude — genuinely-unclosed-input leg ONLY (the reject-alternative leg PROVABLY does not
  slice). (4) Elements-kind predicate — shared cell with pt-leaf row 1. (5) Recursion ceiling —
  G-9 owns the row; 7,761/7,762 cited as a CLASS, never a constant; fused-vs-chained rides the
  same rig. (6) The ARMED expected-set probes (C-M7's falsifier and the destructive `any()`-merge
  witness, W2.44) — **UNRUN-BY-LAW**; static derivation stands; disposable cells. (7) Retention
  magnitude — root-parser count × retained-src size; one reference per root is the static truth.
  (8) **RAW-`.parser()` EPOCH BYPASS** — one cell demonstrating memo persistence when a memoized
  parser is driven without `parseState`; DECIDES WHETHER W2's ENTRY-DISCIPLINE CLAUSE IS A LAW OR
  A FOOTNOTE (W1.13). (9) Printer-throw sweep — `String(p)` over every public construction form,
  in the boundary corpus beside the eight non-string probes (W2.51).
- **pt-parsers (rows 1-5)** — NO NUMBER HERE IS CITABLE AS A CONSTANT. (1) Composed depth ceilings
  carried as INTERVALS with JIT STATE DISCLOSED: array [1,580 cold – 3,275 warm-best] · object
  [1,213 – 1,631] · bare-lazy 7,761 (O-15, immutable) · JSON.parse ≥ 40,000. W1's cell measures
  COLD AND WARMED in separate labeled runs; any G-9 declared depth clears the COLD FLOOR with
  margin. Seat protocol: fresh process + immediate bisect = cold; 5,000 tier-up parses then
  re-bisect = warm. (2) Reject-path allocation — the 2 dead arrays + second ParserState per failed
  parse; joins pt-leaf row 3 on the same rig leg. (3) Mint-count baselines — `quotedString()` 15 ·
  `escapedString()` 8 · `numberParser()` 2 · `.trim()` 2-minus-1-discarded. (4) CSV rejection
  census — 0/500 seed-pinned (LCG 12345 over a 12-char CSV alphabet); a re-runnable falsifier
  against any repaired grammar. (5) Singleton retention footprint — heap delta of
  `state.src` + `state.value` pinned by the module consts after a large parse (40,001 chars +
  20,000 elements live post-return).
- **pt-split (rows R1-R6)** — (R1) L-D6's `text[i]` vs charCodeAt ratios (1.41×–1.47× two-byte;
  ~1.04× Latin-1) — N=1 unisolated box, and THE CURE IS COUPLED TO W2.57's DOMAIN NARROWING:
  measure only against the narrowed signature (the scan rewrite CANNOT LAND FIRST). (R2) L-D11's
  fast-path tax/benefit (±4–8%, sign flips) — recorded variance; re-run only under W1's
  interleaved three-leg cell if at all. (R3) C-8's guard-double-scan (1.298× / 0.841×) weighted by
  the in-tree 95:5 absent:present ratio — expected wash; meaningful only if the guard export
  survives, which it should not. (R4) The absent-delimiter fast path (131×–142× / ~12×) — **THE
  ONE NUMBER A SUCCESSOR MUST NOT REGRESS**; becomes a W1 bench leg. (R5)
  `split_balanced("é,a", 0xA9)` panic witness — source-certain by Rust spec; the executable
  witness needs ONE cargo test IN THE LAWFULLY-OPENED W0 ROOT, never in the read-only main
  checkout. (R6) The dual-target EQ-1 probe on `("a::b","::")` — runs the day the W2 .g harness
  exists; RED BY CONSTRUCTION until W2.57's narrowing lands.
- **pt-state (rows 1-6)** — no figure asserted; arming is out of the seat's bounds. (1)
  REJECT-PATH ALLOCATION LEG — arrays allocated per failed parse (default vs armed), incl. the
  per-furthest-advance UNGUARDED pair (`utils.ts:34-35`). (2) DEAD ALIAS OVERWRITE — cost of
  `parser.ts:61-64`'s construct-then-alias on the failure path. (3) ARMED RENDER COST — per-failed-
  parse cost of the unconditional `console.error(toString())`: TWO full-source materializations
  (`debug.ts:58` + `state.ts:131`), armed leg only. (4) **SHAPE ASSERTION AT OPEN** — assert
  `Object.keys(new ParserState("x"))` equals the 8-key ORDER at EVERY harness open (a cheap
  runtime assert, NOT a bench) — turns the useDefineForClassFields hazard from invisible to GATED.
  (5) Context retention footprint — heap held by contexts per 1k-parser grammar, only if a
  candidate hosts on parse-that. (6) `PARSER_STRINGS` growth — interned bytes after any harness
  leg that PRINTS a parser.
- **pt-utils (rows 1-7)** — (1) Disarmed-path allocation magnitude — 2 arrays/furthest-advance +
  the 4-array failed-parse floor; **HEAPUSED DELTAS INADMISSIBLE** (they see retention, not
  young-gen churn). (2) `collectDiagnostic` O(n·k) line/col cost — n-chars × k-recovered-failures
  on the recovery leg, UNARMED (the path is ungated); cure candidates measured in the K1-RULED
  DIRECTION ONLY (slice-first arithmetic into `state.ts`, one prefix copy dropped; W3.9). (3)
  Scanner class sweep — the 28-code-point over-consumption row set (NUL/C0/ESC) + the U+000B trim
  delta, ESCAPES ONLY; asserts whichever trivia class W2 cites, against ALL THREE shipping
  classes. (4) /utils door price re-derivation with chunk names derived from `dist/core.js:1` at
  that moment. (5) /utils boundary probe table — the 8-row non-string posture table incl.
  boxed-String and the numeric SILENT NO-OP, run against the published package. (6)
  Zero-width-sync orphan witness — the `many():538` trace as an executable corpus row: parse
  succeeds, buffer length must be 0; RED TODAY BY CONSTRUCTION. (7) Subpath closure + declaration
  parity gates — import shipped /core and /utils in bare processes; assert the loaded-module set,
  assert each subpath's `types` stem resolves declarations of the module its `import` stem was
  built from, and assert the RECOVER LIFECYCLE (write/read/clear) is CLOSED on any subpath
  shipping any of it.
- **pt-utils-entry (rows 1-8)** — (1) Recursion ceiling + FAILURE CLASS — cold/warm depth bisect
  on the pinned node, RECORDING THE ERROR CLASS AT EACH BOUNDARY DEPTH (SyntaxError band vs
  RangeError band), through the shipped jsonParser, per fresh process; the oracle asserts the
  CLASS SET and the escape path, never a depth constant; 7,761 is quoted as a SAMPLE (W2.74). (2)
  `mergeErrorState` allocation counts — mechanism source-certain; magnitudes (~87-128 arrays/KB)
  re-measured with counting accessors on the ACCEPT-path leg. (3) Latch cost — O-15's 93.9→138.2
  ns (1.47×) stays N=1 until W1 arms the latch IN A DISPOSABLE CELL ONLY; measure the /utils path
  with and without a FOREIGN `memoize()` in-process. (4) Instrument repair rows — the RED-7 probe
  gains a real latch predicate (replacing the constant-false `:48` row) and, if the corpus wants
  this subpath measured, a /utils import; until then "never measured" stands as a row-level fact
  (W1.10). (5) Chunk-name re-derivation from `dist/utils.js:1-2` at the moment of use; the 8/8
  sha identity VOIDS on the next vite build (W1.20). (6) Boundary probe matrix — 7 inputs × 5
  callables plus `new String` rows, expected postures per cell (throw-message strings recorded but
  NOT asserted verbatim — engine text; W1.2). (7) CSV fixture signature — `data/csv/data.csv` →
  fieldCounts `[10, 2, 24, 27]` is the BORN-RED witness for any repair (W1.19). (8) Receipt law —
  consumer-root HEADs recorded per-record at time of use (hoisted; W1.20).

---

## 8. EXCLUDED register (cl.4) — collated, deliberately NOT folded

Each row is banked, not lost: the registers below name where the debt lives and why no X·P wave
owns it. The O-15 channel is **owner-held**; parse-that's tree, manifest, gates, and docs are
**outside every X·P wave scope** (W2.md:193-197; W4.md:132-140); the W4.14 semver law prices
every ride.

1. **pt-packrat-entry K5 — RETIRE-AS-REDUNDANT, never RETIRE-AS-CURE**: an OWNER DECISION on the
   O-15 channel, priced at one major by C-3's own precedent, retirement condition MEASURED-MET;
   every cited clause binds the CANDIDATE, not a third party's export map. Not folded into any
   wave.
2. **pt-packrat-entry C-1≡E-M1, E-M2, E-M3, C-6≡E-M5, M-1, M-5 + the MINOR/INFO tail**: all four
   surviving MAJORs are parse-that-side and NO-WAVE-OWNER (nominal tier boundary at 91.1% of the
   library by evaluated bytes; 2-of-3 names one function; a reset that neither resets nor disarms;
   the self-contradicting existsSync-only gate that CONCEALED M-1; the untypable `require`
   condition). No X·P wave owns parse-that's manifest, gates, or docs.
3. **pt-split PT-SPLIT-14 / PT-SPLIT-15 — X·V RELAY (loud flag), NOT X·P**: the ONLY TWO
   LIVE-CONSUMER DEFECTS IN THIS CORPUS, and NO X·P GATE WILL EVER WITNESS THEM —
   value.js `splitTopLevel`'s backslash-parity-blind escape check (`grammar.ts:71`) swallows
   declaration bodies (measured: legal `content: "a\\"; color: red` → ONE part where the retired
   `splitBalanced` returns two; replicated at FIVE sites beneath shipped `parseStylesheet`), and
   the four/five scanners track NO `[]` at all (`'[a,b], .c'` splits inside the attribute
   selector). Both X·V-OWNED (W4.md:132: this lane writes no value.js source bytes); **FLAGGED FOR
   RELAY to the X·V formation ledger** — the flag is this register's loudest row.
4. **pt-ansi NO-WAVE-OWNER bloc (rows 3, 6-9, 13, 15, 16-cure, 17-20)**: EQ-4 quarantines
   rendering OUTSIDE the algebra, so no W0..W4 unit owns render-tier safety; recorded
   sharpest-first (row 13's pre-truncation caret; row 3's missing OSC/CSI-total sanitizer binding
   any ./debug relocation; the SGR-only reference strip) so the render-tier/2.0.0 relocation
   INHERITS rather than rediscovers.
5. **pt-core NO-WAVE-OWNER bloc (rows 11-14, 24-30, 34, 35, 46; row 43 calibration)**: rows banked
   for any FUTURE parse-that source wave (three line/column conventions with a clamp converting a
   detectable negative into a plausible wrong answer; the stranded `"trailing-content"` Suggestion
   variant; sticky-rewrite `^`-anchor silencing; state aliasing; splitBalanced escapes), framed by
   row 43: /core has ZERO CONSUMERS — every consequence is prospective. **Row 35 added by the L-20
   repair** (L:D-29, MINOR, `NO-WAVE-OWNER`): `Parser.eof()` allocates 2 Parsers + 2 contexts and
   DISCARDS the context `skip()` just built (`parser.ts:638-642` — `this.skip(eof())` then a
   `p.context =` overwrite; the challenge's "three allocations" count corrected at source). It is
   an allocation row on the very method W2.72 makes the live EOF cure, so the register must bank it
   rather than lose it — but no X·P wave owns an in-library `eof()` repair.
6. **pt-debug NO-WAVE-OWNER bloc (C-4 doc limb, C-14/C-16 docs limb, MISS-8, the A.W3
   spec-regression record)**: X·P waves do NOT own parse-that main's docs and the pause forbids
   touching them; banked for the M-27 parity program; MISS-8 nonetheless strengthens W4.10's gate
   shape.
7. **pt-diagnostics NO-WAVE-OWNER bloc (rows 8-11, 20-23, 30-31 + the in-library cures for rows 2
   and 7)**: the state-destroying reset and the LIFO/rebind journal pair (rows 10+11 are ONE
   IDENTITY, one one-token cure — the live-binding getter vs the rebinding clear) recorded
   sharpest-first for any future parse-that source wave; the LAW halves folded at W2.18/W3.2.
8. **pt-index NO-WAVE-OWNER bloc (M-6, M-12, m-2, m-5, m-6, m-9, C-m3, AD-10, AD-11, i-1, C-i3,
   r1·NEW-I, r1·NEW-M)**: routed to parse-that's own hygiene communique (E-3 addenda posture) —
   dead flag machinery, the ungated-but-recover-read arrays (cure `length = 0`, gating would be
   UNSOUND), the phantom eslint-disable directives, the unbounded printer recursion.
9. **pt-lazy NO-WAVE-OWNER bloc (LZ-3/4 excision, LZ-6, LZ-7, LZ-10, LZ-12, LZ-14, LZ-16, LZ-17
   sequencing, M5)**: ten filed rows collapse to ONE disposition — DELETE the 14-line dead
   decorator — with a SEQUENCING LAW (LZ-17 first: make `_initWhitespace` explicit, because
   `whitespace` is id 0, which is ALSO what keeps the id-0 printer latch unreachable), on the
   owner-held O-15 letter channel.
10. **pt-leaf NO-WAVE-OWNER bloc (PT-LEAF-18/-19/-20/-26 note, L-m1/m4/m6/m13, C-m6, L-miss-M5,
    C-miss-4/5)**: incumbent repairs SPLIT BY SEMVER CLASS per W4.14 — 1.1.0-eligible dead-code
    and doc rows (incl. the vacuous FLAG_EOF test that must be deleted WITH the machinery) vs the
    2.0.0 `trimStateWhitespace` retirement; the dead-export gate is hardcoded to two past
    deletions and structurally cannot catch the next instance (law half folded at W4.1).
11. **pt-packrat NO-WAVE-OWNER bloc (C-4, C-5, C-11, C-18, M5; C-13/C-17 records)**: consumer docs
    document methods the class has never had; the one api.md semantics sentence is false three
    ways; the header ships guidance C-3 makes hazardous; C-17's `row(!ARM ? false : false, …)`
    instrument defect is booked to megatranche instrument hygiene (the file otherwise frozen
    evidence; its X·P consequence folded at W1.10).
12. **pt-parser NO-WAVE-OWNER bloc (rows 1, 6, 9, 11-15, 18-19, 29, 35-36, semver-split per
    INFO-5)**: 1.1.0-eligible guards/hoists/doc notes (row 18 W2-DESIGNED per W2.71) vs 2.0.0
    d.ts-truth-telling, PARSER_ID/this.state retirement, and every entry-semantics change — NONE
    may ride the standing minor; frame of record: value.js consumes ZERO parse-that.
13. **pt-parsers NO-WAVE-OWNER bloc (PT-PARS-1, -2 grammar, -3, -4, -10, -15, -16, -20, -21, -23,
    MISS-3/-7/-8/-9/-10)**: every in-place repair is outside all X·P wave scopes (adding
    parse-that to package.json is forbidden by law, keeping the consume edge at zero); all three
    BLOCKERs are in the GRAMMAR, not the packaging; the zero-consumer measurement prices the
    `*Span`-excision precedent at COST ZERO; NO WAVE MAY CITE the five dead runtime exports as
    inheritable surface.
14. **pt-state NO-WAVE-OWNER bloc (rows 11-13, 15-19, 21-22, 24-26, 28)**: rides the standing
    S.W1 1.1.0+ ask-letter channel or waits for a 2.0.0; sharpest: PT-STATE-24 — `PARSER_STRINGS`,
    the family's ONLY unbounded, never-cleared process-global growth, entered accidentally via
    `Parser.toString()`, which both challenges missed while auditing the other globals.
15. **pt-utils NO-WAVE-OWNER bloc (PT-UTILS-2 doc, -6, -12, -14, -15, -16, -19, semver-priced)**:
    1.1.0 = the source split (AC-3's stated precondition — the fold half lives at W2.67), doc
    repairs, dead fluent returns, the twice-implemented `<=32` predicate; 2.0.0 = any predicate
    change, the unterminated-comment posture change, every export retirement.
16. **pt-utils-entry NO-WAVE-OWNER bloc (PT-UE-4/-5/-7/-8/-10/-11/-12/-15/-16/-17/-18/-19/-20/
    -23/-24/-26..-31)**: 2.0.0 events that must not ride the standing 1.1.0 ask (CSV grammar,
    escape decoding, `.eof()` narrowing, export/subpath retirements) vs 1.1.0-eligible widening/
    rewires/gate repairs; NOTE PT-UE-15: the honest manifest is UNSHIPPABLE WITHOUT EDITING THE
    GATE (manifest-gate hard-fails on any `sideEffects` value other than `false`, and
    proof:manifest runs FIRST in proof:all) — gate and manifest must be repaired TOGETHER.

---

## 9. Closing — status verbs

This addendum is **ADJUDICATED INPUT** to the X·P conformance surface: the fifteen apotheoses'
wave-bound holdings, folded once, dated, E-3-conformant (zero edits to any dated spec; this file
is the sole write). It **STAMPS NOTHING** — no wave's four-verb table moves, no gate goes GREEN or
RED by this document, no bar is set, no baseline is re-measured here (every number above is a
quotation of an adjudicated record, carried with its retirement status). The CONFORMANCE ledger's
verdicts stand as issued; at execution each wave reads its dated spec PLUS this addendum (§1's
reading rule), and W1's rig owns §7 at open. The four collator corrections and the six L-20
repair entries (§1a n.1-n.10) are recorded, not silent.

**L-20 repair pass, 2026-08-07 — landed in place, dated specs still byte-untouched.** The
adversarial fold check returned DEFECTIVE-BUT-SOUND-IN-SUBSTANCE; every defect it named is repaired
here and nowhere else: two ∅-FORWARD drops recovered (pt-lazy LZ-13 → W1.23, LZ-18's surviving half
→ W2.75); one double-booked identity merged to one row (LZ-9 ≡ pt-packrat M3, kept at W2.32,
cross-referenced from W4.12); one mis-homed W2 gate re-homed (pt-packrat M4 → W2.76); and five
cite/warrant corrections (§1a n.2's warrant re-attributed to pt-core row 3; §1a n.1's enumeration
widened to the three quoting sites with its conclusion intact; pt-lazy LZ-21's source-stated ride
named in W1.12's provenance; pt-core row 35 banked into §8 bloc 5; W1.5's anchor moved to units .b
and .d).

**L-20 repair pass 2, 2026-08-07 — the routing axis, and the honest register.** The recheck
returned DEFECTIVE-BUT-SUBSTANTIALLY-REPAIRED with five surviving defects; all five are repaired
here and nowhere else. (1) **The mis-homing that pass 1 never swept for**: pt-lazy **LZ-19**,
source-routed `REFINEMENT FOLD → W2`, sat inside the W1.16 bundle — split out to **W2.77**, W1.16
left carrying only LZ-20 and LZ-22, both rows cross-referenced (§1a n.8). (2) **The routing sweep
itself ran** — every folded row's section compared against its source row's stated wave across all
fifteen apotheoses — finding, beyond LZ-19, two narrowed limbs, both cured in place: M11's strike of
"the ceiling is a stable property" now names `W2.md:832` beside `W1.md:553-554` (W1.3), and
PT-LEAF-24's W2 allocation law now stands beside its W1 oracle (W1.17). (3) **pt-state PT-STATE-7**
is booked ONCE, at **W2.65**; W2.38 keeps a cross-reference (§1a n.9). (4) **pt-utils-entry
PT-UE-9**'s dropped **W0** limb folds at **W0.2**, cross-referenced with W4.2 (§1a n.10). (5) §1a
n.4's "the one pair that is MERGED" — false against this file's own labels — is replaced by a
**complete merge register** of all seven collapsing rows plus the five MERGED CROSS FOLD rows they
must not be confused with; and §1's arithmetic is rewritten to claim **only the OUT side, which
this document enumerates**, with the collator's IN-side intake figures demoted to what they are:
workflow-journal receipts (`wf_14d5549b-eda`), never document-internal arithmetic. Post-repair
truth: **143 FOLDED · 16 EXCLUDED** (128 per-wave + 15 MEASURE + 16 excluded = 159 bookings).

**EXECUTION AWAITS THE OWNER'S BEGIN-WORD** — every fold above is a booking, not an act.
