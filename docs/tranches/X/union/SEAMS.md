# X-WHOLE UNION PASS — THE SEAM REGISTER

**Authority**: `docs/tranches/X/COHESION.md` §2 (the cross-sub-tranche dependency graph) + §3.4
(*"Cross-repo edges reciprocal (both files); zero cycles"*) + §0f (all three loops closed; the
union pass is the sole remaining artifact). Authored by the seam-adjudicator seat, 2026-08-30.

**Posture — read before anything else.** **Status: `planned` EVERYWHERE.** This pass is
**planning**. **Nothing here opens product source.** **Zero VERIFIED stamps are issued in current
voice** — this file advances no wave's four-verb row in any sub-tranche. **E-3 governs**: every
conviction below is an **addendum-beside** naming its cure at the owning file; **no sibling spec,
no COHESION section, no conformance artifact and no mail ledger was edited by this seat**. The
**owner's begin-word is the only execution key**, and no cure below may be executed before it.

**Operand set (the four develop-complete spec layers, read read-only at this seat)**

| sub-tranche | spec home | close state consumed here |
|---|---|---|
| X·V | `waves/W0..W11.md` + `CONFORMANCE-2026-08-03.md` + `REFINEMENT-FOLD-2026-08-28.md` + `refinement/` | CONFORMANT pass 4 (§0c) |
| X·KF | `keyframes/waves/KF-W0..KF-W10.md` | CONFORMANT pass 8 (§0e; `keyframes/conformance/VERDICT.md`) |
| X·F | `fourier/waves/F-W0..F-W10.md` + `fourier/conformance/CENSUS-CANONICAL.md` | CONFORMANT pass 15 (§0f; canonical FROZEN `f44362757458`) |
| X·P | `parse-that/waves/W0..W4.md` + `parse-that/REFINEMENT-FOLD-2026-08-06.md` | CONFORMANT pass 10 |
| mail | `docs/tranches/V/coordination/INBOX.md` (E13) + the glass BK inbox | O-20 SENT · I-28/I-29 ROWED · O-21 owed |

**Method.** A seam is adjudicated **CONSISTENT** only when the same fact is stated at **both** ends
in the ends' own bytes, quoted by a runnable command. A seam stated at one end only, or stated
two ways, is **CONVICTED** with a named cure. Commands below were run at this seat, read-only,
2026-08-30, from `docs/tranches/X/` unless a path says otherwise. Where a live sibling's bytes
are quoted, the quote carries its command; **no line anchor into a live sibling is minted by this
file** (the class X·F E-11 and X·KF D3-3/D3-4 convict on sight — see S-19, which convicts one).

---

## §1 The register

**24 seams · 15 CONSISTENT · 9 CONVICTED.**

| id | seam | ends | verdict |
|---|---|---|---|
| S-1 | RC-P → KF.W3, gate-keyed never scheduled | X.P.W4 §6a ↔ COHESION §2 | **CONSISTENT** |
| S-2 | RC-P reciprocity — the keyframes sentence | X.P.W4 G-6(1) ↔ KF-W3 head-matter | **CONSISTENT** |
| S-3 | RC-P reciprocity — the fourier sentence | X.P.W4 G-6(2) ↔ F-W0 | **CONVICTED** |
| S-4 | The X·V adoption surface (the chain's middle link) | X.P.W4 OP-4/G-9 ↔ X-W9 §5.g/G31 ↔ COHESION §2 | **CONVICTED** |
| S-5 | The repin coordinate's spelling | KF-W2 D-8 ruling ↔ KF-W5 ×2 / KF-W10 ×1 ↔ X-W9 | **CONVICTED** |
| S-6 | F.W2's packet-P-7 consumption reciprocal | F-W2 §Cross-edges ↔ X-W9 + X-W9-FOLD | **CONVICTED** |
| S-7 | I-28 → the six glass-8.0.0 external rows | INBOX I-28 ↔ X·V §EXTERNAL X-EXT-1..6 | **CONSISTENT** |
| S-8 | F.W1's re-pin is 8.0.0-aware | F-W1 §1/G1/ESC-1 ↔ I-21/I-28 facts | **CONSISTENT** |
| S-9 | "sub-tranches never consume glass 8 independently" | COHESION §2 ↔ X·KF ∪ X·F (silence) | **CONVICTED** |
| S-10 | keyframes' glass 7.0.0 EXACT devDep | KF-W0 §B-12/C-1 ↔ O-20 measurement-pin table | **CONSISTENT** |
| S-11 | The 2026-08-29 O-20 ACK and the 9.0.0 cut | glass BK outbound ↔ INBOX (absent) | **CONVICTED** |
| S-12 | The timeline families — one producer surface, no contradictory asks | KF-W7 C-15/§Verdict ↔ F-W3 SR-1/g3 | **CONSISTENT** |
| S-13 | O-21 minted exactly once | KF-W1 §3/§4/C-10/G-KF1-7 ↔ all citing siblings | **CONSISTENT** |
| S-14 | No O-number collisions | X·V · X·KF · X·F · X·P outbound-id census | **CONSISTENT** |
| S-15 | F.W3's g20 batched letter vs the dispatched O-20 | F-W3 g20/S-5 ↔ O-20 packet ↔ COHESION §1 SS-6 ↔ X.P.W4 G-8 | **CONVICTED** |
| S-16 | The O-20 entry denominator | F-W10 §4b ↔ COHESION §4a ↔ the packet's own close | **CONVICTED** |
| S-17 | BH-relay obligations (producer rows never become frontend hacks) | all four sub-tranches | **CONSISTENT** |
| S-18 | The minted-unauthored successors, substance | KF-W10 §6.D ↔ COHESION §0d | **CONSISTENT** |
| S-19 | §0d's anchor into KF-W4 | COHESION §0d ↔ KF-W4 §Sequencing | **CONVICTED** |
| S-20 | Sibling citation of KF.W11/12/13 | X·V · X·F · X·P (measured ∅) ↔ X·KF R-15 | **CONSISTENT** |
| S-21 | The repair-arms-a-defect coupled events | COHESION §0a ↔ the eleven F-W* specs | **CONSISTENT** |
| S-22 | F.W1 land-or-lose, one spelling | F-W1 §4 step 4 ↔ ten citing specs | **CONSISTENT** |
| S-23 | The FR-NP-32 substrate pre-gate, cite-both-never-substitute | F-W0 ↔ ten siblings ↔ O-20 A-1 ↔ COHESION §0a | **CONSISTENT** |
| S-24 | The one atomic trigger-gated cut, X·V-internal | X-W0.j ↔ X-W4.g ↔ §EXTERNAL | **CONSISTENT** |

---

## §2 THE PLAW-BIND CHAIN (S-1 … S-6)

The chain as the spine states it: **X·P release condition → X·V L1/L5 (parser adoption) → the
packed release → KF.W3's repin and F.W2's consumption grants.** Walked at the bytes of all four
spec sets, it holds at three of five joints and is one-ended at two.

### S-1 · The release condition and its terminus — **CONSISTENT**

⟨cmd⟩ `grep -n 'X·P release condition → KF.W3' COHESION.md`
> `66:- **X·P release condition → KF.W3** (parser consumption): gate-keyed, never scheduled; routing is`
> `67:  PLAW-BIND — parser → value (X·V L1/L5 surfaces) → packed release → consumers. **Direct`
> `68:  parse-that→fourier is FORBIDDEN** (standing routing law).`

⟨cmd⟩ `sed -n '/^### 6a\./,/^That sentence is load-bearing/p' parse-that/waves/W4.md`
> *"**`RC-P(V)` is TRUE if and only if all six conjuncts below evaluate TRUE for the same version
> `V` of `@mkbabb/value.js`. KF.W3 opens if and only if `RC-P(V)` is TRUE, evaluated by running
> the six commands against the registry coordinate `V` — never by reading a status word in any
> document, including this one.**"* — and, three lines on, X·P quotes COHESION §2 **from the other
> end** verbatim.

Six conjuncts, six commands (PUBLISHED · TOTALITY · EQUIVALENCE · ADMITTED · BAR-DISCHARGED ·
ROUTED), all FALSE at the dated measurement. Both ends say gate-keyed, never scheduled; both
forbid the direct parse-that→fourier edge. **Squares.**

### S-2 · The keyframes half of the G-6 reciprocity — **CONSISTENT**

X·P commissions the sentence and forbids itself from writing it:

⟨cmd⟩ `sed -n '/^\*\*G-6 — RECIPROCITY/,/reciprocity a seat authored on both ends/p' parse-that/waves/W4.md`
> *"**KF.W3** (X·KF): *"This wave opens if and only if `RC-P(V)` evaluates TRUE per
> `docs/tranches/X/parse-that/RELEASE-CONDITION.md` §6a, run against the registry coordinate `V`;
> the repin 4.0.0→`V`, the `serializeCssValue` fork retirement, and the DUAL-1 unification are all
> downstream of that evaluation."*"* … *"**X·P may not satisfy this gate by writing in those
> directories**; the §4 bounds forbid it, and a reciprocity a seat authored on both ends is not a
> reciprocity."*

X·KF carries it, verbatim, at its only lawful site:

⟨cmd⟩ `sed -n '9p' keyframes/waves/KF-W3.md`
> *"> **This wave opens if and only if `RC-P(V)` evaluates TRUE per
> `docs/tranches/X/parse-that/RELEASE-CONDITION.md` §6a, run against the registry coordinate `V`;
> the repin 4.0.0→`V`, the `serializeCssValue` fork retirement, and the DUAL-1 unification are all
> downstream of that evaluation.**"*

⟨cmd⟩ `grep -c 'RC-P' keyframes/waves/KF-W3.md` → **15**. Named as a **predicate**, never a wave
number (`KF-W3.md:11`). **Squares — byte-for-byte.**

### S-3 · The fourier half of the G-6 reciprocity — **CONVICTED**

G-6's second commissioned sentence: *"**F.W0** (X·F): *"fourier receives parser effects only
through an admitted, packed value.js release satisfying `RC-P`; the direct `parse-that → fourier`
edge is forbidden and is asserted absent by the two-grep probe."*"* Its falsifier is explicit:
*"a `grep -rn 'RC-P' docs/tranches/X/{keyframes,fourier}/waves/` returning fewer than two hits
fails."*

⟨cmd⟩ `grep -rl 'RC-P' fourier/waves/` → **no output** (zero of eleven F-W* specs).

What F.W0 carries instead is the **negative half only**:

⟨cmd⟩ `grep -n 'X·P (parse-that)' fourier/waves/F-W0.md`
> *"| **X·P (parse-that)** | **A declared NON-EDGE. Direct parse-that→fourier is FORBIDDEN**
> (standing routing law). Routing is PLAW-BIND: parser → value (X·V L1/L5 surfaces) → packed
> release → consumers. Declared here so no F.W0 act — mail, pin, or manifest — opens one. |"*

The forbidden edge is declared; the **consumption predicate is not**. `RC-P` is nowhere named in
X·F, so the fourier end has no predicate to evaluate and X.P.W4's two-hit falsifier stays RED from
that side. X·KF already convicts this from *its* end and says so in its own voice — ⟨cmd⟩
`grep -n 'the fourier half is still absent' keyframes/waves/KF-W3.md` → *"This file discharges the
**keyframes half** of the reciprocity; the fourier half is still absent, so X.P.W4's own two-hit
falsifier remains RED from the other end"* — so the gap is **declared at two of three ends and
unanswered at the third**.

**CURE.** X·F takes a dated **addendum-beside** at `F-W0.md`'s X·P cross-edge row adopting G-6's
second sentence **verbatim and by predicate name** (`RC-P`, never `X.P.W4`), leaving the
NON-EDGE declaration untouched beside it; X·F's next conformance round records the adoption and
re-runs `grep -rn 'RC-P' keyframes/waves/ fourier/waves/` as the two-ended receipt. **X·P may not
write it** (its §4 bounds, and G-6's own "authored on both ends is not a reciprocity").

### S-4 · The chain's middle link — the X·V adoption surface — **CONVICTED (at the spine)**

X·P names the gap as its principal finding and refuses to fill it unilaterally:

⟨cmd⟩ `grep -n 'OP-4' parse-that/waves/W4.md | head -1`
> *"| OP-4 | **The X·V adoption surface.** Some X·V (or successor) wave must be authorized to
> consume the seam. | **DOES NOT EXIST — this is the wave's principal finding.** `X/waves/W9.md`
> §5.g states "This wave does not adopt a parser; it records why value.js ships none of parse-that
> today", and **G31's falsifier is literally "adopt a parser inside this wave"**. None of
> X-W0..X-W11 adopts one. | G-9's terminal disposition. This is a **coordination mark carried to
> the owner**, never a unilateral wave invention: X·P does not get to author an X·V wave. |"*

X·V's end says the same thing, in its own bytes and from the other direction:

⟨cmd⟩ `sed -n '296p;373p' waves/W9.md`
> `:296` *"…adopt a parser; it records why value.js ships none of parse-that today."*
> `:373` *"| G31 | parse-that position recorded against ground truth | … | **RED [measured]** —
> dependencies are exactly `{"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}`;
> `@mkbabb/parse-that` absent while the readopt decree stands | Record a dependency state the
> manifest does not show, **or adopt a parser inside this wave** |"*

So **X·P and X·V agree**: no wave holds the adoption. The odd end is **the spine**, which states
the chain as if the middle link existed — *"routing is PLAW-BIND — parser → value (**X·V L1/L5
surfaces**) → packed release → consumers"* — while X·V's own carry ruling folds exactly those
surfaces into the wave that declines the adoption: ⟨cmd⟩ `grep -n 'CC-091' waves/W9.md`
> *"| CC-091 | library-band RD-1..RD-5, RD-7..RD-13 | FOLD | **W.L1→X-W9.a** · W.L2→X-W9.b ·
> W.L3→X-W9.c · W.L4→X-W9.d · W.L6→X-W9.f · W.L7→X-W9.i. **W.L5's value-side half** ships in
> X-W9.f …"*

And the X·V refinement fold — the layer §0c says *"now carries every NWO row at a home"* — never
touches it: ⟨cmd⟩ `grep -rl 'RC-P' waves/ refinement/` → **no output**; ⟨cmd⟩
`grep -c 'SEAM-CONTRACT\|OP-4' REFINEMENT-FOLD-2026-08-28.md` → **0**. The fold *sharpens* G31
(⟨cmd⟩ `grep -n 'G31' refinement/X-W9-FOLD.md` → *"| **G31** | Gains the **reclassification** leg
(NG-9); today it reads the same map and asserts only the parse-that absence | W9.16 |"*) —
strengthening the **absence**, never homing the adoption.

This is not a defect in X·P or X·V; both stated it honestly. It is a **spine defect**: §2 asserts
a chain whose middle link both named ends disclaim, and G-9's three admissible dispositions
(**A** a successor X·V wave · **B** amend G31 to admit adoption inside tranche X, deciding whether
it rides X-W9's cut and X-W11's release · **C** close `BLOCKED-ON <condition> + <re-trigger>`) are
**owner-owed and unrecorded at the spine**.

**CURE.** COHESION §2's PLAW-BIND bullet takes a dated addendum (a new §0g letter — §0a..§0f are
OCCUPIED) stating: the L1/L5 leg is **UNHOMED**, its three dispositions are G-9's A/B/C, and it is
**owner-owed**; the X-whole G-F register carries G-6's third sentence (*"This wave consumes
`docs/tranches/X/parse-that/SEAM-CONTRACT.md` at `src/css/**`; X·P writes no bytes here"*) as
**UNHOMED-PENDING-OWNER**, not as an existing edge. No wave is authored by this cure; the
disposition is the owner's at the X-W0 sitting.

### S-5 · The repin coordinate — predicate vs literal — **CONVICTED**

X·KF ruled the spelling at repair round 3 and struck the literal at three of its own sites:

⟨cmd⟩ `grep -n 'D-8 cure, repair round 3' keyframes/waves/KF-W2.md | head -1`
> *"**D-8 cure, repair round 3**: the round-1/round-2 literal `4.1.x` is **struck at all three
> sites** (**OP-5 · §Cross-edges · KF.W3 · §Excluded**) … **`RC-P` is a predicate by W3's ratified
> law, never a number this spec may forecast** … A version literal drifts exactly as a line number
> does, and this spec's own doctrine — *"a phantom coordinate convicts"* — reaches it."*

The literal survives at three sibling sites the ruling never swept:

⟨cmd⟩ `grep -rn '4\.0\.0→4\.1\.x' keyframes/waves/`
> `keyframes/waves/KF-W5.md:353` · `keyframes/waves/KF-W5.md:445` · `keyframes/waves/KF-W10.md:569`

KF-W5:445 — *"KF.W3 is gated on the parse-that release condition (PLAW-BIND → V.L1/V.L5 → packed
Value release). **This wave must NOT pre-empt the 4.0.0→4.1.x repin.**"* KF-W10:569 — *"parse-that
release condition → value (X·V L1/L5) → packed release → **KF.W3** repin 4.0.0→4.1.x."* Both are
faithful to the routing and unfaithful to the **coordinate law** their own sub-tranche ratified.
X·V's *"the 4.1 cut"* (`W9.md:1`, ×15) is **not** convicted — X·V owns its own version and names
its own cut; the defect is the **consumer** forecasting a producer's number the producer has not
published (`package.json#version` measures `4.0.0`).

**CURE.** KF-W5 (×2) and KF-W10 (×1) take an addendum-beside converting `4.0.0→4.1.x` to
`4.0.0→`V`, the registry coordinate `RC-P(V)` resolves to`, exactly as KF-W2's D-8 cure spells it;
the class-sweep is stated across the **file boundary** this time (the LAW F(2) failure KF-W3:311
already minutes about a different class). The routing prose is untouched.

### S-6 · F.W2's consumption grant and its owed X·V reciprocal — **CONVICTED**

X·F declares a value-side reciprocal by name, under the spine's own both-ends law:

⟨cmd⟩ `sed -n '303p' fourier/waves/F-W2.md`
> *"| **X·V / V·π** parser-seam program | **F.W2 DECLARES; value.js OWES the reciprocal** | Packet
> P-7's reciprocal, per COHESION §2's both-ends law: **(a)** `color2` rejects its own sibling
> parser's output … **(d)** the G18 reciprocal … `parseStylesheet` is **total on the string arm**
> … **but throws on Buffer input** (`e.startsWith is not a function`), **the surviving totality
> nit for X-W9**; **(e) direct parse-that→fourier is FORBIDDEN** … |"*

The named receiver books nothing: ⟨cmd⟩ `grep -rc 'P-7' waves/ REFINEMENT-FOLD-2026-08-28.md` →
**0** at every X·V file; ⟨cmd⟩ `grep -rln 'startsWith is not a function' waves/ refinement/` →
**no output**. X-W9's totality battery is the string arm only (G2's 56-vector empty-body class),
and the W9 fold's NG-2 **widens the string arm** (`<any-ident>()`, `calc()`, `foo()`) without ever
reaching the Buffer input. So a declared cross-repo edge has a declaring end and no receiving end
— precisely what COHESION §2's closing bullet forbids (*"Cross-repo edges are declared FROM BOTH
ENDS in the spec files (the X·V W4-D2 lesson, now law)"*).

**CURE.** X·V takes an addendum-beside at `refinement/X-W9-FOLD.md` (never at `W9.md`, which is
UNTOUCHABLE under the fold's own E-3 posture) booking F.W2's P-7 clauses (a)–(d) as a **received**
reciprocal: clause (d)'s Buffer-input throw joins the R1/totality family as a named row with its
own born-RED witness, and clauses (a)–(c) are dispositioned ACCEPT / DECLINE-with-reason. If the
row is declined, the decline is written **at the X·V end** so F.W2's cell has an answer to cite.

---

## §3 THE GLASS CHAIN (S-7 … S-11, S-24)

### S-7 · I-28 → §EXTERNAL X-EXT-1..6 — **CONSISTENT**

⟨cmd⟩ `grep -n 'The ids are' REFINEMENT-FOLD-2026-08-28.md`
> *"Source = `docs/tranches/V/coordination/INBOX.md` row **I-28** (glass **8.0.0 PUBLISHED**,
> `v8.0.0` @ `17a11bc5`; letter of 2026-08-09 + §5 addendum 08-25; ROWED 2026-08-28 by the SS-6
> sweep). The ids are **X-EXT-1..6**, cited ⟨INBOX I-28 · X-EXT-n⟩ — never as registry ids."*

Six rows, six owners, all six break classes named in the letter and none invented: X-EXT-1
`SearchBar` DELETE → X-W7 (four measured `demo/palettes/` edges) · X-EXT-2 `./forms`→`./input` →
X-W8 · X-EXT-3 `grain` removed → X-W6 · X-EXT-4 `--slider-track-bg` no-alias → X-W4.g (mail says
×4, the tree measures 5 — **both figures stated, neither silently elected**) · X-EXT-5 o7 re-pin →
X-W1 · X-EXT-6 peer `vue-component-type-helpers ^3.0.3` → X-W0.j. The letter's own not-restated
block and the zero-break classes are carried as **named absences**. The fold's own EXTERNAL
receipt was corrected from 9 bookings to **8** with the two pointer-bookings named (W9's X-EXT-6,
W11's X-EXT-5). **Squares against I-28 clause for clause.**

### S-8 · Does F.W1's re-pin read 8.0.0-aware? — **CONSISTENT (yes, explicitly)**

⟨cmd⟩ `sed -n '5p' fourier/waves/F-W1.md`
> *"**Name**: F.W1 — glass-ui **4→7(→8, G1-gated)** ∧ keyframes 4.3→6 ∧ value.js 0.13→4.0, ONE
> land-or-lose transaction, + the `lucide-vue-next`→`@lucide/vue` rename + the pencil-boil floor
> correction"*

⟨cmd⟩ `grep -n '| \*\*G1\*\* |' fourier/waves/F-W1.md`
> *"**RE-PIN (OWNER-GATED, ESC-1).** This spec's target-TAG + ADOPTED-COMMIT-HASH cell is filled;
> every producer `file:line` in all 66 records re-resolves at that hash. … Cell EMPTY at
> authoring"*

And the 8.0.0 branch is carried **per row**, not as a mood: K-8/SR-1 (*"`SliderVariant` … `"scrubber"|"spectrum"` only at 8.0.0 … **re-book iff G1 adopts 8**"*), M-2 (*"the v7 floor is `^0.9.2` … `^0.11.2` is the 8.0.0 fact"*), GCM-22 (*"**Flips on G1: superseded if target = 8.0.0**"*), FR-FG-20 (*"if G1 adopts 8, the `text-admin-label` break RETURNS"*), `fr-ConvergencePlot SC-3` (*"**8.0.0-ONLY, and the gating is the row's substance**"*), plus K-1's version-lying window (*"producer WT self-reported 7.0.0 for 5 days while carrying 8.0.0 deletions"*). **The re-pin act reads 8.0.0-aware at row granularity. Consistent with §EXTERNAL, which also refuses to pre-elect the tag.**

### S-9 · "sub-tranches never consume glass 8 independently" — **CONVICTED (one-ended)**

The spine states an atomic law binding all sub-tranches:

⟨cmd⟩ `sed -n '72,74p' COHESION.md`
> *"- **Glass-8 census (X-W0.j) → X.W4.g**: the one atomic trigger-gated cut; **sub-tranches never
> consume glass 8 independently.**"*

No sibling reciprocates it, anywhere:

⟨cmd⟩ `grep -rl 'X-W0\.j' keyframes/waves/ fourier/waves/` → **no output**
⟨cmd⟩ `grep -rl 'W4\.g' keyframes/waves/ fourier/waves/` → **no output**
⟨cmd⟩ `grep -rn 'glass 8 independently\|Glass-8 census' keyframes/waves/ fourier/waves/ waves/` → **no output**

Meanwhile both sibling sub-tranches contemplate a producer major **under their own local
escalations**: X·F's G1/ESC-1 (S-8 above, a fourier-owned owner gate), and X·KF —

⟨cmd⟩ `grep -n 'KC-5' keyframes/waves/KF-W6.md`
> *"**VERSION-BUMP NOTE (the only row arguing a producer major): glass-ui 8.0.0's outline-painted
> focus cures it; must be weighed against KF.W0's 7.0.0 pin before the wave commits to either
> arm.**"*

Neither declares whether the atomicity law **binds** it (it plausibly does not — each repo pins
its own producer dependency, and the X-W0.j census is a *value.js* repin act) or is
**declared-inapplicable**. Under §2's own closing bullet an edge stated at one end is not an edge,
and this one is stated at the spine alone while two sub-tranches route around it.

**CURE.** Two moves, neither of which edits a sibling's ruling. (1) COHESION §2's Glass-8 bullet is
**re-scoped by addendum** to name the repo it governs — *"the one atomic trigger-gated cut **in
value.js**; no X·V wave consumes glass 8 outside X-W0.j → X-W4.g"* — so the universal quantifier
stops reaching trees it does not own. (2) X·F's `F-W1.md` §Cross-edges and X·KF's `KF-W0.md`
§Sequencing each take a one-line addendum-beside: *"the COHESION §2 Glass-8 atomic cut is a value.js
act; this repo's producer pin is decided at G1/ESC-1 (resp. §B-12's 7.0.0 pin) and creates no edge
into X-W0.j"* — the declaration that makes the non-edge measurable rather than merely absent.

### S-10 · The measurement-pin table — **CONSISTENT**

⟨cmd⟩ `sed -n '7p' ../../../../glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md`
> *"**Measurement-pin honesty (read first)**: your 08-09 letters say 8.0.0 shipped. Items below
> state the pin they were measured at (**fourier installs 4.0.0; keyframes installs 7.0.0 exact;
> value installs 7.0.0**). Your K-1-window caution is ours too…"*

Each end measures to that table. X·KF: ⟨cmd⟩ `grep -n 'AGG-P1' keyframes/waves/KF-W0.md | head -1`
→ *"`origin/master` package.json:77 carries `@mkbabb/glass-ui: 7.0.0` as an **EXACT devDep**
(census SCH-1, AGG-P1), so the phantom-dependency headline is FALSIFIED at HEAD"* — and
§B-12 is the owner-hand reconciliation of the stale checkout that made it look otherwise. X·F
installs 4.0.0 (F-W0's corrupt-dist pre-gate is measured **at 4.0.0**). X·V's own dependency map
measures `^7.0.0` (`W9.md:373`). **Every per-repo pin in the letter matches the spec that owns it.**

### S-11 · The 2026-08-29 O-20 ACK and the 9.0.0 cut — **CONVICTED (unrowed inbound; the glass chain's pin question re-opens)**

A glass reply to O-20 has been sitting in the BK inbox since 2026-08-29 and has **no ledger row**:

⟨cmd⟩ `ls -la /Users/mkbabb/Programming/glass-ui/docs/tranches/BK/coordination/`
> `glass-outbound-2026-08-29-valuejs-o20-ack.md` (3,438 B, 2026-08-29 16:41)

⟨cmd⟩ `grep -c 'o20-ack\|9\.0\.0' docs/tranches/V/coordination/INBOX.md` (from the repo root) → **0**.
The ledger's last rows are O-20 · I-28 · I-29; **no I-30.**

Its contents move the glass chain at every end. ⟨cmd⟩ `sed -n '1,5p;30,45p' <the ack>`
> *"# O-20 ACK — the A-1 answer, the batch receive, the 9.0.0 pin"*
> *"**Your ask — a producer gate that parses its own published `./styles`, not a version bump — is
> ACCEPTED.** The close ran it by hand; the **permanent arm lands in the O-20 disposition wave**"*
> *"A-2..A-14, §B-1..§B-7, and §C-1 are **received whole** at row 76."*
> *"**9.0.0 is CUT**: tag `v9.0.0` at `d4f7b24f`, pushed. `MIGRATION.md` §9.0.0 is the re-read datum
> — **`./search` removed whole** …, **`./canvas` gone**, the 8.1.0 dark-sync fold riding the same
> section."*
> *"**Publish-state honesty, in your own coin**: the npm registry PUT is currently walled … **every
> content claim in this letter is tag-pinned, not registry-pinned**."*

Three consequences the union pass may not leave unstated. (i) **E13**: *"a wave may not CLOSE with
UNREAD mail"* and the union boundary is a boundary — this letter is not merely unread, it is
**unrowed**, the identical mechanism I-28's row minutes as having cost 19 days. (ii) **Every glass
end is now pinned behind the producer**: X·V at 8.0.0 (§EXTERNAL), X·KF at 7.0.0 (§B-12), X·F at
4.0.0 with a 4→7(→8) branch — and the producer is at **9.0.0**, with `./search` removed whole,
which is the *successor fact* to X-EXT-1's `SearchBar` deletion and touches the same four
`demo/palettes/` edges. (iii) **F.W1's PIN-LAW is unsatisfiable against 9.0.0 as written** —
*"EVERY §7 uplift row pins to the PUBLISHED artifact before the transaction executes"* — because
9.0.0 is **tag-pinned and registry-absent** by the producer's own disclosure. This is not a
contradiction between two X specs; it is a fact none of them has yet been told.

**CURE.** (1) Row it **I-30** at this boundary (E13, the SS-6 sweep's own idiom), with the A-1
ACCEPTED disposition and the 9.0.0 tag/registry split stated in the row — a ledger act, not a wave
act. (2) X-W0.j's four-condition Glass-8 verdict is **widened by addendum** to enumerate **8.0.0
AND 9.0.0** as candidate targets, so the census returns a dated verdict over the real option set;
§EXTERNAL's six rows keep their ids and gain a `9.0.0 successor fact` column where the letter
supplies one (`./search` whole-removal beside X-EXT-1). (3) F.W1's PIN-LAW takes a one-clause
addendum distinguishing **tag-pinned** from **registry-pinned**, so an owner electing 9.0.0 at
ESC-1 is electing a tag and knows it. (4) F.W0's FR-NP-32 pre-gate keeps its BLOCKER (it is
measured at the **adopted 4.0.0** and remains true there) and gains the producer-side terminus the
ACK supplies: the permanent parse-gate lands in glass's O-20 disposition wave. **No spec's verdict
is reversed by this cure; four of them are told a fact.**

### S-24 · The atomic cut, X·V-internal — **CONSISTENT**

X-EXT-6 → **X-W0.j** (*"the repin act itself"*), X-EXT-4 → **X-W4.g** (*"the receiving surface, per
W0.4's cure shape — the census's PASS branch emits the receiving-bounds delta"*), and every X-EXT
row is declared to *"ride the glass-adopt family (X-W0.j census → X-W4.g trigger)"*. The trigger
and its receiver are named identically at the fold, at `ROUND-1-ASSIGNMENTS.md` §C, and at the
per-wave folds' EXTERNAL carriages (W0·W1·W2·W4·W6·W7·W8·W9·W11, receipt corrected to **8**
bookings + 2 pointers). **One trigger, one receiver, one spelling.**

---

## §4 THE TIMELINE FAMILIES (S-12)

### S-12 · KF.W7 (timeline evaluate) ↔ F.W3 (GlassTimeline) — **CONSISTENT: no contradictory asks**

Two sub-tranches audit the **same producer surface** (`@mkbabb/glass-ui` `/timeline`) in the same
boundary. Their verdicts must not pull the producer two ways. They do not.

X·KF rules the shadow **partial and bounded**, on the importable surface only:

⟨cmd⟩ `sed -n '92p;120p' keyframes/waves/KF-W7.md`
> `:92` *"One disposition table, per surface, each ruled **ADOPT · PARTIAL · KEEP-BESPOKE** with
> named reason, against the **importable** surface only"*
> `:120` *"**C-15** … the glass `/timeline` shadow is real for the 0..1 playhead ONLY:
> `dist/components/timeline/index.d.ts` …"* — with the rail's counterpart measured **absent**
> (`geometry.d.ts` has no percent↔position map, no zoom/pan).

X·F rules the prescribed adoption **rejected**, and its gate asserts a re-derivation, not an import:

⟨cmd⟩ `grep -n 'ADOPTION-ROUTE LOCK (SR-1)' fourier/waves/F-W3.md`
> *"**ADOPTION-ROUTE LOCK (SR-1)**: the prescribed 4.0.0 `ScrubberTimeline` adoption is REJECTED —
> stale twice over (K-14 7.0.0 progressbar ruling; 8.0.0 `SliderVariant` → `"scrubber" |
> "spectrum"`), and it regresses the accessible name … K-16: the prescribed import is unexported at
> both layers"*

⟨cmd⟩ `grep -n '| \*\*g3\*\* |' fourier/waves/F-W3.md`
> *"| **g3** | **TIMELINE FORK — one composition** | … `grep glass-ui/timeline web/src` → 0. | Both
> forks resolved into ONE unconditional-session composition … **SR-1 REJECTS the prescribed
> `ScrubberTimeline` adoption — the gate asserts the re-derivation, not an import.** |"*

**The two verdicts are the same verdict at two altitudes**: the importable `/timeline` surface is
too thin to adopt except for the 0..1 playhead, so keyframes rules per-surface against the
*importable* set and fourier refuses the import outright. Neither asks the producer for something
the other forbids. Both route the producer half to the **same** register — X·KF ⟨cmd⟩
`grep -n 'glass-ui BH relay (SS-6)' keyframes/waves/KF-W7.md` → *"(feeds, NEVER patches locally …):
C-15's `/timeline` export gaps; TooltipContent's non-consumption of
`--reka-tooltip-content-available-height` …"*; X·F's S-5 relay roster for GlassTimeline's AX-1 /
CU-1 / PD-1 / LF-1. And **O-20 carries both, once each**: A-8 (*"`/timeline` export gaps … the
evaluation input that split the keyframes timeline verdict"*, sourced `X/keyframes/waves/KF-W7.md`),
A-9 (TooltipContent), A-10 (`fr-GlassTimeline PD-1`, sourced `X/fourier/waves/F-W3.md`), §B's
GlassTimeline a11y producer share. **Same producer surface, two consumers, one letter, zero
contradictory asks.** (The *vehicle* by which F.W3 believes its half travels is defective — that is
S-15, a mail seam, not a timeline seam.)

---

## §5 MAIL (S-13 … S-17)

### S-13 · O-21 minted exactly once — **CONSISTENT**

⟨cmd⟩ `grep -rl 'O-21' keyframes/waves/ fourier/waves/ waves/ parse-that/waves/`
> `keyframes/waves/KF-W1.md` · `KF-W2.md` · `KF-W4.md` · `KF-W9.md` · `KF-W10.md` — **X·KF only.**

Exactly one file **mints**; four **cite by id**. The mint sites are all inside KF.W1 and all
governed by one law: ⟨cmd⟩ `grep -n 'MINT LAW' keyframes/waves/KF-W1.md | head -1`
> *"**MINT LAW** …: *the commissioned id is **O-21** (= the measured ledger maximum **O-20** + 1,
> measured 2026-08-28 at `INBOX.md:95` / commit `48003630`); the executing seat **re-runs the
> max-grep at open** and, if the ledger has moved again, mints **max+1** and records the delta as a
> dated addendum before executing — **a spec-frozen outbound literal is never trusted over the
> measured ledger.***"*

— minted after round 1 commissioned **O-20**, *"an id spent nine minutes before that write"*. The
citers name it as a **vehicle**, never as their own act (KF-W2 D-13: *"`O-21` — KF.W1's outbound
row"*; KF-W10 §6.C-B/§6.D). **One mint, four citations, one law, and the law survives the ledger
moving.** Note for the executing seat: under S-11's cure the ledger gains **I-30** (an *inbound*
id), which does not move the outbound maximum — but the MINT LAW's re-measure at open is exactly
what makes that safe without editing KF-W1.

### S-14 · No O-number collisions — **CONSISTENT**

⟨cmd⟩ `grep -rEo 'O-1[0-9]|O-2[0-9]' fourier/waves/ waves/ | cut -d: -f1,3 | sort -u`
> X·F cites O-10 · O-14 · O-15 · O-17 · O-20 (all **spent** ids, cited as history) · X·V cites
> O-18 · O-19. **No X·V, X·F or X·P file mints an outbound id.** X·P's own outbound act is a
> **packet authored in-repo and handed to SS-6's courier**, never a fresh channel (W4 G-8).

### S-15 · F.W3's g20 letter vs the dispatched O-20 — **CONVICTED**

⟨cmd⟩ `grep -c 'O-20' fourier/waves/F-W3.md` → **0.**

⟨cmd⟩ `grep -n '| \*\*g20\*\* |' fourier/waves/F-W3.md`
> *"| **g20** | **THE REGISTER AND THE LETTER exist** | `ls …F-W3-DO-NOT-EXECUTE.md` → No such
> file (verified 2026-08-28); `grep -c "F.W3" docs/tranches/V/coordination/INBOX.md` → 0 — **no
> SS-6 batched letter for this wave's producer rows exists.** | `.f` has written the
> DO-NOT-EXECUTE register … **AND filed ONE batched SS-6 letter in INBOX carrying every
> §C.E/GLASS-RELAY item** — `--viz-amber` the only cartoon/amber ask; DOCK-ACTIVE not re-sent. |"*

⟨cmd⟩ `grep -n 'S-5 · SS-6 producer relay' fourier/waves/F-W3.md`
> *"ONE batched BH letter (g20) carries: the GlassTimeline consolidated items (AX-1's four-item
> same-edit law · CU-1's cursor deletion · **PD-1's docblock-vs-render contradiction** · `LF-1` …)
> · `--viz-amber` (the ONLY cartoon/amber item; cartoon-card ask RETIRED) · **the `--motion-accent`
> re-scope of the `--viz-easing` ask** · `./confirm-dialog`'s retirement note · … · **`./clipboard`**
> … **DOCK-ACTIVE is closed upstream — do NOT re-send (FR-COB-11).**"*

Every emphasized item **has already been dispatched**, on 2026-08-28, in the letter whose sha is
`f6e04c86ddb8`: A-10 is PD-1 with source cell `X/fourier/waves/F-W3.md`; §B carries the
`--viz-easing`→`--motion-accent` re-scope and `./clipboard`; §D-1 is DOCK-ACTIVE-do-not-resend;
§D-2 is the cartoon-card retirement with `--viz-amber` surviving, again sourced
`X/fourier/waves/F-W3.md`. And the batch is **not a wave's to author** at either far end:

⟨cmd⟩ `grep -n 'SS-6 | \*\*Glass communique assembly\*\*' COHESION.md`
> *"| SS-6 | **Glass communique assembly** | … | ONE batched BJ letter at the next boundary |
> **root-authored (Fable); E13** | ACCRETING |"*

⟨cmd⟩ `grep -n 'SS-6 is the only lawful cross-repo write path' parse-that/waves/W4.md`
> *"**SS-6 is the only lawful cross-repo write path in tranche X, and it is root-authored; a wave
> that opens its own channel has substituted an ad-hoc letter for the batched-root law.**"*

X·F's own sibling gets this right and says so in its bytes — ⟨cmd⟩ `grep -n 'does not re-send it'
fourier/waves/F-W10.md` → *"**The boundary letter already dispatched on 2026-08-28 is O-20** …
and it carries **FR-NP-32 ≡ fr-PaperSidebar M1** at row A-1 — the F-side boundary **reconciles
AGAINST that packet, it does not re-send it**."* **F.W3 is the one end that never learned the
letter left.** Its RED baseline is a true reading of a stale question (`grep -c "F.W3" INBOX.md`
→ 0 is true, because the INBOX row cites the *packet*, not the wave), and its GREEN would execute
a re-send of five dispatched items into a letter O-20's own close certifies as *"zero re-sends"*.

**CURE.** `F-W3.md` takes a dated addendum-beside re-baselining g20 against the packet, in F.W10's
own form: (i) the RED baseline's probe becomes sha-pinned to the dispatched letter
(`shasum -a 256 <O-20 path> | cut -c1-12` → `f6e04c86ddb8`) plus the roster diff, not
`grep -c "F.W3" INBOX.md`; (ii) the GREEN's cargo becomes the **O-20 residual** — every §C.E /
GLASS-RELAY item **minus** those O-20 carries (PD-1 · `--viz-easing`→`--motion-accent` ·
`./clipboard` · `--viz-amber`/cartoon-card · DOCK-ACTIVE), the dispatched five riding as
**status-notes** exactly as F.W10 rides FR-NP-32; (iii) the verb changes from *"filed ONE batched
SS-6 letter"* to *"accreted to the SS-6 register for the root-authored boundary letter"*, which is
what COHESION §1 and X.P.W4 G-8 both say the lawful act is. **No ask is withdrawn and no packet is
re-sent** — the two failure modes the register exists to prevent.

### S-16 · The O-20 entry denominator — **CONVICTED**

X·F books a correction **against the spine**:

⟨cmd⟩ `grep -n "COHESION §4a's \"26 entries\" for O-20" fourier/waves/F-W10.md`
> *"| ⟨ROUND 4⟩ COHESION §4a's "26 entries" for O-20 | the true figure (**22** id-rows, sha
> `f6e04c86ddb8`) is corrected **in this file** at §4b, where the borrowed count was quoted;
> COHESION itself is a §1a #3 **MODIFY-carve** whose writes wait on the owner's begin-word … **The
> count is corrected where it was consumed; the source is flagged, never silently edited** |"*

— and at §4b: *"COHESION §4a's **26** is named superseded here and is corrected at its own site
under the §1a #3 carve, never quoted onward."*

The packet's own close disagrees:

⟨cmd⟩ `sed -n '57p' <the O-20 packet>`
> *"**Close**: consolidated per the four-workflow-era batch law — **one letter, twenty-six
> entries**, zero re-sends."*

⟨cmd⟩ `grep -cE '^\| A-[0-9]+ \|' → 14` · `^\| B-[0-9]+ \|` → **7** · `^\| C-[0-9]+ \|` → **1** ·
`grep -cE '^[0-9]+\. '` over §D → **4**. **14 + 7 + 1 = 22 id-rows; + 4 §D negative-space entries =
26 entries.** F-W10's arithmetic is right and its **framing is wrong**: 22 and 26 are two
denominators of one packet (id-rows vs entries), not a figure and its error. The §D items are
entries by the letter's own structure — DOCK-ACTIVE's do-not-resend, the cartoon-card retirement,
the ariaLabel already-ruled note and the owed I-21a ratification notice are **cargo**, each with a
consumer obligation, and X·F's own S-15 roster cites two of them. The conviction is not the number:
it is that **an X·F spec instructs the spine to correct a figure that is true**, which — executed —
would put a false correction into the constellation's only shared register.

**CURE.** F-W10's cell takes an addendum-beside restating the reconciliation instead of the
correction: *"**22 id-rows** (`A-1..A-14` · `B-1..B-7` · `C-1`, sha `f6e04c86ddb8`) **⊕ 4 §D
entries = the packet's own 26** (its close line, quoted) — two denominators of one letter; COHESION
§4a's figure is the packet's and stands."* The *"corrected at its own site"* instruction is
**withdrawn**; COHESION §4a is edited by nobody. The hash pin and the row-by-row enumeration —
X·F's genuinely good work here — are kept whole.

### S-17 · The BH-relay obligation — **CONSISTENT**

One rule, four ends, one spelling: *"producer rows NEVER become frontend hacks"* (F-W3 S-5;
F-W9 §14 *"Glass-producer rows ride SS-6, **never** become frontend hacks"*), *"feeds, NEVER
patches locally — owner edict: every producer-level change relays to the active BH inbox"*
(KF-W7 §Sequencing), the §4a accretion register on the X·V side, and X·P's G-8 (*"'send' here
means author in-repo + row + hand to the courier"*). The live inbox path is stated identically
where it appears (`glass-ui/docs/tranches/BK/coordination/`, BJ retained as history). **The single
breach is S-15's verb — the obligation itself is uniform.**

---

## §6 SUCCESSORS (S-18 … S-20)

### S-18 · The minted-unauthored successors — **CONSISTENT (sentence for sentence)**

⟨cmd⟩ `grep -n 'MINTED (KF-W4 R-15 homing' keyframes/waves/KF-W10.md`
> *"> **KF.W11 · KF.W12 · KF.W13 — MINTED (KF-W4 R-15 homing; KF-W1 edge 9, flag DISCHARGED),
> UNAUTHORED; cargo = the 17-packet partition 9+6+2 with the six travelling locks; authoring seat =
> the SS-1/SS-2 authoring block; X·KF W10's close stamps the AUTHORED ELEVEN only.**"*

⟨cmd⟩ `sed -n '188,193p' COHESION.md`
> *"**KF.W11 · KF.W12 · KF.W13 — MINTED (KF-W4:249's R-15 homing block; listed at KF-W1 cross-edge
> 9), UNAUTHORED; cargo = the 17-packet partition 9+6+2 with the six travelling locks; authoring
> seat = the SS-1/SS-2 authoring block; X·KF W10's close stamps the AUTHORED ELEVEN only.**"*

Partition, locks, seat, stamp-scope: identical. The cargo checks out at the third end too —
⟨cmd⟩ `sed -n '256,258p' keyframes/waves/KF-W4.md` → *"KF.W11 · Demo Scene Repair (**9**)"* /
*"KF.W12 · Authoring-Surface Repair (**6**)"* / *"KF.W13 · Chrome, Dock & Transport Repair (**2**)
— **MINTED here, declared**"*, and KF-W1's cross-edge 9 (⟨cmd⟩ `sed -n '376p' keyframes/waves/KF-W1.md`)
homes **zero** of the 17 and enumerates the canonical roster. Mechanism F (*"a mint with neither an
authored spec nor a register row is a hard escape on sight"*) is declared program-wide at KF-W10
and adopted at §0d. **Squares.**

### S-19 · §0d's line anchor into a live sibling — **CONVICTED (MINOR, and it is already dead)**

§0d cites the mint as *"**KF-W4:249**'s R-15 homing block"*.

⟨cmd⟩ `sed -n '249p' keyframes/waves/KF-W4.md | wc -c` → **1** (the line is **blank**).
⟨cmd⟩ `grep -n 'R-15' keyframes/waves/KF-W4.md` → the homing block is at **`:254`**, its three
wave bullets at `:256`–`:258`.

The anchor is dead by five lines, and the **form** is the defect independent of the drift: X·F
convicts this exact class at its own E-11 (*"the same repair round amended F-W0 and shifted its
gates uniformly +12 … `:199` addressed a blank line"*) and X·KF at D3-3/D3-4 (*"both STRUCK as
dead … `:54` is **blank**"*), and both minted the standing cure — **§-heading + row id, never a
line, into a file that is still being written**. The spine holds itself to a lower bar than the
specs it governs.

**CURE.** A dated §0d addendum re-anchors to **`KF-W4.md` §Sequencing, the R-15 homing block**
(§-heading + rule id, no numeral), matching KF-W10's own spelling of the same citation (*"KF-W4
R-15 homing"*). **§0d is not rewritten** — dated sections are never rewritten; the addendum states
the strike and the replacement anchor, per E-3. Recommended as a one-line rider on the same §0g
letter S-4's cure opens, so the spine takes one dated addendum at this boundary rather than three.

### S-20 · Sibling citation of the successors — **CONSISTENT**

⟨cmd⟩ `grep -rl 'KF\.W11\|KF\.W12\|KF\.W13' waves/ fourier/waves/ parse-that/waves/` → **no
output.** Eleven X·KF files cite them; **no** X·V, X·F or X·P spec does. Correct by construction —
the successors carry kf-internal NO-WAVE-OWNER cargo and open no cross-repo edge, so silence at
the other three sub-tranches is the right shape, not a gap.

---

## §7 TRIGGER-GATED CUTS AND COUPLED EVENTS (S-21 … S-23)

### S-21 · The repair-arms-a-defect rows — **CONSISTENT**

COHESION §0a names four coupled events the authoring block *"MUST carry"*: *"the repair-arms-a-defect
rows (fr-PaperSearchModal same-commit riders, PAW-44/LAW-3, MPC-31 one-cut law, FR-MSP-6
two-channel lock)"*. Each is present under **one** spelling across the sub-tranche that owns it:

⟨cmd⟩ `for t in PAW-44 LAW-3 MPC-31 FR-MSP-6 PaperSearchModal; do grep -rl "$t" fourier/waves/; done`
> PAW-44 · LAW-3 · MPC-31 · FR-MSP-6 → **7 specs each**; PaperSearchModal → **8 specs**.

And the lock text travels verbatim rather than paraphrased — ⟨cmd⟩ `grep -n 'PAW-44 restoration
lands' fourier/waves/F-W1.md` → *"**LAW-3**: PAW-44 restoration lands **WITH-or-AFTER** PAW-1 +
PAW-30, never before."* (carried verbatim at repair round 2, the round-1 paraphrase struck).
FR-MSP-6's *"two-channel lock"* and MPC-31's *"one-cut law"* are named by those exact phrases at
every citing site. **The spine's list and the specs' bytes agree id for id.**

### S-22 · F.W1 land-or-lose — **CONSISTENT (one spelling, ten citers)**

⟨cmd⟩ `grep -rho 'land-or-lose transaction — .\{0,70\}' fourier/waves/ | sort -u`
> *"land-or-lose transaction — the **TWELVE-limb roster chartered at F-W1 §4 step 4**, cited whole
> and never restated…"* — the same clause, with the same anchor form (§ + step, no line), in every
> one of the ten files that cite it; ⟨cmd⟩ `grep -rl 'land-or-lose' fourier/waves/` → **10 of 11**.

The cardinality (**TWELVE limbs**) and the charter site are identical everywhere, and the specs
that cite it also carry the failure branch (*"a partial landing is the land-or-lose failure the
L-18 base forbids"*). **A coupled event named identically at every citing spec — the criterion,
met.**

### S-23 · The FR-NP-32 substrate pre-gate — **CONSISTENT (with S-11's new fact riding)**

⟨cmd⟩ `grep -rl 'FR-NP-32' fourier/waves/` → **11 of 11.** The citation law is stated the same way
wherever it appears: *"**FR-NP-32 (≡ fr-PaperSidebar M1)** … **two banked witnesses of ONE fact;
the citation form is cite-both, never substitute** (banked ids are original for life)."* COHESION
§0a names it identically (*"the F.W0 substrate pre-gates (corrupt glass-ui 4.0.0
dist/styles/index.css; dirty-worktree settle)"*), and O-20 carries it at **A-1** as the batch's
head row with the ask stated verbatim (*"the emitter fix AND a producer gate that parses its own
published `exports["./styles"]` — NOT a version bump"*). **Four ends, one id-pair, one ask.** The
producer has now **ACCEPTED** it (S-11) — a fact that discharges the *ask*, not the *pre-gate*: the
adopted pin is 4.0.0 and the corruption is still in those bytes, so F.W0's BLOCKER stands and gains
a terminus. That rider rides S-11's cure and changes no verdict here.

---

## §8 CONVICTIONS — THE CURE LIST

Nine, each an **addendum-beside** at the file that owns the end, none executable before the
owner's begin-word.

| # | seam | owning end | cure |
|---|---|---|---|
| 1 | S-3 | `fourier/waves/F-W0.md` | Adopt X.P.W4 G-6's fourier sentence verbatim **by predicate name** (`RC-P`) beside the existing NON-EDGE row; X·F's next round re-runs the two-ended grep. X·P may not write it. |
| 2 | S-4 | `COHESION.md` (new §0g) | Declare the L1/L5 adoption leg **UNHOMED / owner-owed**, name G-9's dispositions A/B/C, and carry G-6's third sentence in the X-whole G-F register as UNHOMED-PENDING-OWNER — not as an existing edge. |
| 3 | S-5 | `keyframes/waves/KF-W5.md` ×2 · `KF-W10.md` ×1 | Convert the literal `4.0.0→4.1.x` to `4.0.0→`V` at `RC-P(V)`` per KF-W2's D-8 ruling, swept **across the file boundary** this time. |
| 4 | S-6 | `refinement/X-W9-FOLD.md` | Book F.W2's packet-P-7 reciprocal as **received**: clause (d)'s `parseStylesheet` Buffer-input throw as a named row with a born-RED witness; (a)–(c) ACCEPT or DECLINE-with-reason, written at the X·V end so F.W2 has an answer to cite. |
| 5 | S-9 | `COHESION.md` §2 + `F-W1.md` + `KF-W0.md` | Re-scope the Glass-8 atomic-cut law to **value.js**; each sibling declares the non-edge in one line, so the absence is measurable rather than merely absent. |
| 6 | S-11 | `docs/tranches/V/coordination/INBOX.md` (+ X-W0.j · §EXTERNAL · F-W1 PIN-LAW · F-W0) | Row the 2026-08-29 ACK as **I-30**; widen X-W0.j's verdict to 8.0.0 **and** 9.0.0; give §EXTERNAL a 9.0.0-successor-fact column; give F.W1's PIN-LAW the tag-vs-registry clause; give FR-NP-32 its producer-side terminus. |
| 7 | S-15 | `fourier/waves/F-W3.md` | Re-baseline g20 against the dispatched O-20 by sha; cargo becomes the **residual**, the five dispatched items ride as status-notes; the verb becomes *accretes to SS-6*, not *files*. |
| 8 | S-16 | `fourier/waves/F-W10.md` | Restate 22 id-rows **⊕** 4 §D entries **= the packet's own 26**; withdraw the *"correct COHESION at its own site"* instruction; keep the hash pin and the enumeration. |
| 9 | S-19 | `COHESION.md` §0d (rider on §0g) | Re-anchor the mint citation to **KF-W4 §Sequencing, the R-15 homing block** — §-heading + rule id, no line number, matching KF-W10's own spelling. |

**Shape of the nine.** Six are **one-ended edges** (S-3 · S-4 · S-6 · S-9, and S-15/S-16 in the mail
layer) — the single failure mode COHESION §2's closing bullet was written to prevent, recurring at
the union altitude precisely because each sub-tranche closed its own loop against its own bytes.
Two are **coordinate-form** defects (S-5 predicate-vs-literal, S-19 line-anchor-into-a-live-sibling),
both of them classes the sub-tranches already convict internally and neither of which the spine
holds itself to. One (S-11) is not a contradiction between specs at all but an **unrowed fact** —
the producer moved to 9.0.0 and told us, and no end has been told. **None of the nine reverses a
sub-tranche verdict; all nine are additive.**

---

## §9 CLOSING POSTURE

The X-whole seam surface is **24 seams, 15 stated consistently at both ends, 9 convicted with cures
named**. The three CONFORMANT loops did their work: every seam that lives **inside** one
sub-tranche's bytes holds, and every conviction above lives in the space **between** two ends —
which is what the union pass exists to read and what no per-sub-tranche loop could have caught.

**Status is `planned` for every wave, every gate, every cut, in all four sub-tranches, unchanged by
this file.** No four-verb row moved. **No VERIFIED stamp is issued here in any voice.** No product
source was opened, no sibling spec edited, no COHESION section written, no mail row added — the
nine cures above are **stated, not executed**, per E-3, and each names the file that owns its end
so the owner can direct them one at a time or as a batch. **The owner's begin-word remains the only
execution key.**
