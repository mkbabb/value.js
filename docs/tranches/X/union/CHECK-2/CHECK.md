SERVED MODEL ID (fresh hostile union-check seat, line-1 declaration): claude-opus-5[1m]

# X-WHOLE UNION CHECK 2 — THE CLOSING CHECK

**Verdict: UNION-CLEAN.**

**Operands.** `union/SEAMS.md` (incl. §ERRATA-1) · `union/G-F-ADJUDICATION.md` ·
`EXECUTION-RUNBOOK.md` · the repair-round-1 cures at `COHESION.md` §0g/§2/§0d ·
`docs/tranches/V/coordination/INBOX.md` I-30 · `waves/W0.md` §ADDENDUM ·
`REFINEMENT-FOLD-2026-08-28.md` §EXTERNAL·9 · `refinement/X-W9-FOLD.md` §20 · and the seven
addenda-carrying sub-tranche specs (F-W0 · F-W1 · F-W3 · F-W10 · KF-W0 · KF-W5 · KF-W10).
Register of the prior check: `union/CHECK-1/CHECK-RETURN.json` (2 MAJOR + 4 MINOR).

**Method.** Read-only, this seat, 2026-08-30, at the working tree above HEAD `e4ba3a37`. Every
figure below is the return of a command run here. No file outside `union/CHECK-2/` was written.

---

## §0 THE BAR, AND WHERE THIS LANDS

**UNION-CLEAN = zero defects above MINOR-with-mitigation.** Eight defects survive verification;
**all eight are MINOR-with-mitigation or INFO**; **zero MAJOR**; **zero MEDIUM**. Both CHECK-1
MAJORs and all four CHECK-1 MINORs are individually **CLOSED at the bytes** (§4). The bar is met
and the verdict is stated plainly: **UNION-CLEAN**.

Weighed in both directions. Nothing here was inflated: the two heaviest findings (a stale
corpus-wide grep in the runbook; one unlanded limb of a five-site cure) each leave every edge,
gate, order, arithmetic figure and verdict in the four artifacts **unchanged**, and each carries a
one-command cure. Nothing was softened either: the runbook's false universal *is* named as a
false universal, the unlanded limb *is* named as unlanded, and the register's own recurring
receipt-fidelity class is charged a third and fourth time in §3.

---

## §1 AXIS 1 — THE NINE CONVICTIONS: EVERY CURE LANDED, BOTH ENDS WALKED

Nine convictions, nine cures, walked at the owning end **and** the far end. All nine are dated
`2026-08-30`, all nine are **addenda-beside** (the original line's bytes are preserved verbatim
and the addendum is appended inside `⟨…⟩` — `git diff` shows one deletion and one insertion per
site because the append shares the line, not because a byte was rewritten).

| # | seam | prescribed site | landed | verified at |
|---|---|---|---|---|
| 1 | S-3 | `F-W0.md` X·P cross-edge row | ✅ | G-6's second sentence adopted **verbatim** and **by predicate name**; the NON-EDGE declaration stands untouched beside it |
| 2 | S-4 | `COHESION.md` new §0g | ✅ | §0g.1 declares the L1/L5 leg **UNHOMED / owner-owed**, quotes G-9's A/B/C unreworded, carries G-6's third sentence as **UNHOMED-PENDING-OWNER** |
| 3 | S-5 | `KF-W5.md` ×2 · `KF-W10.md` ×1 | ✅ | all three sites; roster enumerated **across the file boundary** at each |
| 4 | S-6 | `refinement/X-W9-FOLD.md` (never `W9.md`) | ✅ | §20 books P-7 (a)–(d) RECEIVED; `git status --porcelain waves/W9.md` → **empty** |
| 5 | S-9 | `COHESION.md` §2 + `F-W1.md` + `KF-W0.md` | ✅ | §0g.3 re-scopes to value.js; F-W1 at **§Cross-edges**, KF-W0 at **§Sequencing** (heading `:679`) — both the prescribed sections |
| 6 | S-11 | `INBOX.md` (+ X-W0.j · §EXTERNAL · F-W1 PIN-LAW · F-W0) | ⚠️ **4 of 5** | I-30 ✅ · W0.j widened ✅ · §EXTERNAL·9 ✅ · F-W1 PIN-LAW ✅ · **F-W0 FR-NP-32 terminus ✗** (defect D-2) |
| 7 | S-15 | `F-W3.md` | ✅ | g20 re-baselined sha-pinned; roster **partitioned 11 residual ⊕ 5 dispatched = 16**; verb → *accretes* |
| 8 | S-16 | `F-W10.md` | ✅ | both sites (§4b cell + §Excluded row); reconciliation replaces the correction; instruction **withdrawn** |
| 9 | S-19 | `COHESION.md` §0d rider on §0g | ✅ | §0g.2 strikes `KF-W4:249`, re-anchors to §-heading + rule id, **no numeral re-issued** |

**Both-ends receipts, re-run here.**

- **S-3 far end now closes the two-hit falsifier.** `grep -rl 'RC-P' fourier/waves/ keyframes/waves/`
  → `F-W0.md` · `KF-W2.md` · `KF-W3.md` · `KF-W5.md` · `KF-W10.md` — **hits in both directories**,
  exactly what the addendum promised. Whitespace-normalised comparison of the adopted sentence
  against `parse-that/waves/W4.md` G-6: **character-identical** through the terminal period.
- **S-6's born-RED witness independently reproduced** (not inherited). At `dist/subpaths/css.js`,
  `package.json#version` → `4.0.0`:
  `parseStylesheet(Buffer.from("a{color:red}"))` → **`TypeError: e.startsWith is not a function`** ·
  `parseCssColor("light-dark()")` → **`TypeError: Cannot read properties of undefined (reading 'replace')`** ·
  `parseCssColor("light-dark(red, blue)")` → **`{"ok":false,"diagnostics":[{"code":"css_syntax",…}]}`** ·
  `parseStylesheet("a{color:oklch()}")` → the string-arm throw. **All four exact.**
  `grep -rn "colorUnit2\|color2" src/` → ∅ and `dist/subpaths/*.js` → ∅, so X-P7-a's
  decline-with-reason rests on a measured absence.
- **S-16's arithmetic reproduced against the sha-pinned packet.** `A-*` **14** · `B-*` **7** ·
  `C-*` **1** = **22** id-rows; `^\|` total **28**; `wc -l` **59**; §D `^[0-9]+\. ` → **4**;
  `shasum -a 256 | cut -c1-12` → **`f6e04c86ddb8`**; close line reads *"one letter, twenty-six
  entries, zero re-sends."* **22 ⊕ 4 = 26** — the reconciliation is right and the withdrawn
  instruction was the defect.
- **S-11's ledger act at true bytes.** ACK `wc -c` → **3293**, `shasum` → **`caed90705234`**,
  mtime **Aug 29 16:41**. Every one of I-30's four quoted blocks (A-1 ACCEPTED · the receive at
  row 76 · the 9.0.0 cut at tag `v9.0.0` @ `d4f7b24f` · tag-pinned-not-registry-pinned) matches
  the ACK **verbatim**. `grep -c '^| I-30 |'` → **1**, at the ledger tail after I-29.
- **S-11's §EXTERNAL·9 measurements reproduced.** `grep -rn "glass-ui/search"` → **exactly 4**, at
  the four coordinates named (`BrowsePane.vue:195` · `PalettesPane.vue:149` · `AdminPane.vue:87` ·
  `PaletteSlugBar.vue:132`); `glass-ui/canvas` → **0**;
  `git -C ../glass-ui show v9.0.0:package.json` → version **9.0.0**, `./input` PRESENT ·
  `./styles` PRESENT · `./forms`/`./search`/`./canvas` ABSENT ·
  `peerDependencies["vue-component-type-helpers"]` → **`^3.0.3`**. The note's own honesty rider
  (the tag's `MIGRATION.md` prose says `8.0.0` while the manifest at that tag says `9.0.0`) is a
  real and correctly-stated divergence.
- **S-19's replacement anchor resolves uniquely.** `sed -n '249p' KF-W4.md | wc -c` → **1**
  (blank, the strike earned); `grep -c 'NO-WAVE-OWNER packets, HOMED from this end' KF-W4.md` → **1**;
  `grep -c '^## §Sequencing' KF-W4.md` → **1**; R-15 block at `:254`, bullets `:256`–`:258`,
  heading `:244`.

---

## §2 AXIS 2 — §ERRATA-1 AT THE TRUE BYTES

**E1-1 (S-10, MAJOR) — the re-derivation STANDS, three-ended.**
`grep -n 'AGG-P1' keyframes/waves/KF-W0.md | head -1` returns the **D-4 repair-directive row**,
not the quoted sentence — the conviction is correct. The sentence's real home is the megatranche
record: `sed -n '6p' registry/adjudicated/kf-AmigaScene.md` returns it with **no bold, nested
quotes intact, terminal period present** — so E1-1's charge of emphasis-minting and voice-borrowing
is true. The replacement basis holds at three independent ends: producer letter `:7`
(*"keyframes installs 7.0.0 exact"*) · KF-W0's own `:514` RED baseline and `:522` GREEN target ·
and the manifest itself, `git -C ../keyframes.js show origin/master:package.json` → `:77`
`"@mkbabb/glass-ui": "7.0.0"` inside `devDependencies`. **S-10 CONSISTENT on a basis it did not
previously have; the §1 count 15/9 unchanged.** Two receipt-fidelity nits ride it (D-5, D-6).

**E1-2 (S-11, MINOR) — TRUE.** `ls -la` of the BK dir returns exactly the four rows quoted; the
ACK is **3293 B** and **3438 B** belongs to `ATLAS-TO-GLASS-2026-08-03-AUDIT-BATCH.md`, the row
immediately above, dated Aug 3. Verdict CONVICTED and its four-part cure untouched — correct.

**E1-3 (S-14, MINOR) — TRUE, with one stale line (D-4).** The published command's mis-cut is real
(`grep -o` under `-r` emits two colon-fields; `-f1,3` prints paths). The re-cut's two regex
changes are load-bearing exactly as claimed. Limb (c) is right to refuse the challenge: O-15 is
cited in `fourier/waves/F-W10.md` and in `parse-that/`, so *"cited as history by both X·F and
X·P"* stands and **no erratum was owed there** — the errata correctly declined to "repair" a true
row. **S-14 CONSISTENT.**

---

## §3 AXES 3–5 — RUNBOOK REALITY · POSTURE · MAIL

**Runbook reality.**

- **Track C carries F.W9's full predecessor chain.** `F-W9.md:15` declares
  *"Opens after: F.W0 (substrate pre-gates, hard) → F.W1 (atomic) → F.W3/W4 (the cures this wave
  gates)"* on the §State **Four-verb** line — and the runbook now draws all four in-edges **in the
  diagram** and carries the dedicated edge row at `:256`. The CHECK-1 MAJOR is closed twice over.
- **The heads claim matches the settled edges.** §1.0's root-set re-derivation is computed, not
  asserted: Track A `{X-W0}` · Track B `{KF.W0, KF.W1}` (KF-W1 `:35` — *"**Not** after X.KF.W0"*) ·
  Track C `{F.W0}` **only after the repair** — the file says so in its own voice · Track D
  `{X.P.W0}`. *"Four track heads, five root waves"* is stated and reconciled against the
  four-workflow cap. Honest.
- **The F.W1 charter is quoted at its true bytes with the `/9` extension outside the quotation.**
  `sed -n '5p' fourier/waves/F-W1.md` → *"glass-ui 4→7(→8, G1-gated) ∧ keyframes 4.3→6 ∧ value.js
  0.13→4.0, ONE land-or-lose transaction, + the `lucide-vue-next`→`@lucide/vue` rename + the
  pencil-boil floor correction"* — **character-identical** to the runbook's quotation, with the
  9.0.0 branch moved into a `⊘` note beside it. CHECK-1 MINOR-1 closed. (Its supporting grep has
  since gone false — D-1.)
- **§1 provenance is truthful, 8 of 11.** Per-file measurement: `Opens after` present at
  `F-W0:53` · `F-W1:7` · `F-W2:48` · `F-W3:100` · `F-W5:35` · `F-W7:83` · `F-W8:63` · `F-W9:15`
  = **8**; absent at `F-W4` (its only hit `:33` cites F-W1's line, as the runbook says) ·
  `F-W6` · `F-W10` = **3**. Exactly the split the repaired table publishes.
- **Ordering edges cite real declarations — 22 anchors spot-walked, 21 resolve.**
  `F-W0:53` · `F-W1:7` · `F-W2:48` · `F-W3:100` · `F-W4:33` · `F-W5:35` · `F-W5:176` ·
  `F-W6:542` · `F-W7:83` · `F-W8:63` · `F-W9:15` · `F-W9:218` · `F-W10:7` · `F-W10:107` ·
  `COHESION:74` · `KF-W1:35` · `KF-W4:207` · `KF-W4:270` · `KF-W6:121` · `KF-W6:568` ·
  `KF-W8:38` · `KF-W9:20` · `KF-W10:22` — **all resolve to the quoted content**. The single
  failure is `F-W6:537` (D-3). Track D's five §State edges all resolve at `parse-that/waves/`.
- **Every wave exists in a spec.** 12 + 11 + 11 + 5 = **39** authored waves, files present;
  KF.W11/12/13 correctly held outside the order as MINTED-UNAUTHORED. **27 gates** = 10 + 5 + 7 + 5.
- **Zero execution verbs in current voice.** The whole file read. §3.1/§3.3's imperatives are
  checkpoint instructions predicated on the begin-word; §1.4's *"X.P.W4 is the single site … that
  stamps VERIFIED"* describes a spec's declaration, it issues nothing.

**Posture.** Status `planned` is declared in every artifact's own voice and no four-verb row moves
anywhere. `grep 'VERIFIED **YES'`-class scan over all three union artifacts → **∅**; G-F §6.1
states *"No VERIFIED stamp, in any voice"* and §0 grades every disposition ADJUDICATED. No product
source was opened for edit anywhere in the round (X-W9-FOLD §20's runs are read-only executions of
the published `dist/`, correctly labelled). **E-3 held**: every cure is an addendum-beside, every
original line's bytes stand, `waves/W9.md` is untouched, and the two CONFORMANT verdicts are
undisturbed — `git diff HEAD -- keyframes/conformance fourier/conformance` prints **nothing** and
`git status --porcelain` over both dirs is **empty**, so neither conformance layer moved by a byte.
`scripts/dev/dev.sh` untouched by the round.

**G-F re-derived independently.** Corpus `ls *.md | grep -vE '^(kf|fr|pt)-' | wc -l` → **92** ·
NWO occurrences **604** · records **73** · P1 bold-leading rows **442** — all four reproduce
cell-for-cell. Six sampled ⟨record·id⟩ carriages re-resolved at namespace-correct fold
coordinates: ⟨PaginationBar · C-5⟩ `X-W4-FOLD:117` · ⟨BrowsePane · M6⟩ `X-W3-FOLD:880` ·
⟨BrowsePane · M13⟩ `X-W7-FOLD:2097` · ⟨AboutPane · AB-33⟩ `X-W0-FOLD:1448` ·
⟨wb-mix-configbar · MC-23⟩ `X-W6-FOLD:1189` · ⟨PaletteCard · R-4⟩ `X-W7-FOLD:2279`. R-1's
MAJOR-bearing absence claim holds under an independent namespace scan: every `AP-*` hit in the
layer resolves to `AuroraPane`/`PaletteCardMenu`, and `initApiEnvironment` → **0** and `baseUrl`
→ **0** layer-wide.

**Mail law.** I-30 rowed once, at the tail, with true ACK bytes (§1). E13's four-path sweep is
carried at runbook §5.3 and names the ACK as the second letter the stale-pin habit cost.

---

## §4 THE CHECK-1 REGISTER — EACH DEFECT, INDIVIDUALLY CLOSED

| CHECK-1 | severity | status | closing receipt |
|---|---|---|---|
| Track C drops F.W9's predecessor chain | MAJOR | **CLOSED** | `F-W9.md:15` chain drawn in the §1.3 diagram **and** carried as an edge row `:256`; §1.0 recomputes Track C's root set to `{F.W0}` and names the prior error |
| S-10 rests on a synthesized quotation | MAJOR | **CLOSED** | §ERRATA-1 E1-1 re-issues the stamp on producer letter `:7` + KF-W0 `:514`/`:522` + `origin/master:package.json:77`; all three re-run true here |
| Runbook misquotes the F.W1 charter with `/9` | MINOR | **CLOSED** | quotation now character-identical to `F-W1.md:5`; the 9.0.0 branch sits in a `⊘` note outside it |
| S-11's ACK byte size is the adjacent row's | MINOR | **CLOSED** | E1-2 corrects 3,438 → **3293 B**; re-measured here |
| S-14's receipt cannot produce its output | MINOR | **CLOSED** | E1-3 publishes a runnable re-cut over all four layers with an anchored pattern; limb (c) correctly refuses a false "repair" |
| §1 overstates X·F provenance | MINOR | **CLOSED** | the 8/3 split is published and reproduces exactly per-file |

**6 of 6 closed. Zero carried forward.**

---

## §5 DEFECTS — 8, ranked; none above MINOR-with-mitigation

**D-1 · MINOR (mitigated) — the runbook publishes a corpus-wide absence receipt its own round
falsified, including in the file the sentence names.** §1.3's F.W1-atomic row, curing CHECK-1's
MINOR-1, adds: `grep -c '9\.0\.0' fourier/waves/F-W1.md` → **0**, and
`grep -rl '9\.0\.0' waves/ keyframes/waves/ fourier/waves/ parse-that/waves/ COHESION.md` →
*"no output — no X spec anywhere contains the string."* Re-run at the settled bytes: the first
returns **2**; the second returns **`waves/W0.md` · `fourier/waves/F-W1.md` · `COHESION.md`**.
The same row also states the S-11 PIN-LAW clause is *"owed at F-W1's PIN-LAW … not as a fact
already in the spec"* — it **is** in the spec. Cause is write ordering inside one round
(EXECUTION-RUNBOOK mtime `04:34`, F-W1 `04:37`, COHESION `04:39`), not fabrication. A false
universal is the X·KF loop's own named ban, so it is charged.
**Mitigation**: the cure it supports is independently true — `sed -n '5p' F-W1.md` still reads
`4→7(→8, G1-gated)` with no `9.0.0`, so the quotation is correct and the `⊘` instruction stands.
No edge, gate, order, arithmetic figure or verdict moves. **Cure: re-run the two greps and restate
the receipt (and the tense) in a dated line.**

**D-2 · MINOR (mitigated) — S-11's cure landed at 4 of its 5 named sites; `F-W0`'s FR-NP-32
producer-side terminus is unlanded.** SEAMS §8 row 6 names five acts; the fifth — *"give FR-NP-32
its producer-side terminus"* (the permanent parse-gate lands in glass's O-20 disposition wave, A-1
ACCEPTED) — is absent: `grep -c 'DATED ADDENDUM-BESIDE' fourier/waves/F-W0.md` → **1**, and that
one is the S-3 cure; `grep -rl '9\.0\.0' fourier/waves/` returns **F-W1 only**. `REFINEMENT-FOLD`
§EXTERNAL·9 itself books it as *"owed at those files"*, alongside F-W1's clause — which did land,
so the fold's own sentence is half-stale.
**Mitigation**: S-11 and S-23 both state that this limb *"discharges the ask, not the pre-gate"* —
F.W0's BLOCKER is measured at the adopted **4.0.0** and stands either way, so **no verdict, gate or
order changes**. The fact is not lost to the program: runbook §2.3 gate 18 carries it (*"The
producer ACCEPTED the ask (the 08-29 ACK, §3.2); the pre-gate stands at the adopted 4.0.0 bytes
regardless"*), as do INBOX I-30 and COHESION §0g's rider. What is missing is the note **at F.W0's
own end**. **Cure: one dated line beside the FR-NP-32 pre-gate row.**

**D-3 · MINOR (mitigated) — the runbook's repaired provenance table anchors F-W6 to a blank line.**
§1's derivation table cites *"its own §4 **In-wave order** `:537`"*. Measured:
`sed -n '537p' fourier/waves/F-W6.md` → **blank**; the quoted text
(*"`a` preconditions (FW6-G19 receipt + F.W5 clause set + FW6-G18 rulings)"*) is at **`:538`**.
`F-W6.md` is unmodified this round, so this is a wrong anchor as authored, not drift — and it lands
on a blank line, the precise instance-shape X·F's **E-11** and SEAMS **S-19** both convict by name.
CHECK-1's own receipt said `:536`; the repair wrote `:537`; the truth is `:538`.
**Mitigation**: the companion anchor in the same cell (`:542`, *"F.W0 substrate pre-gates FIRST"*)
resolves, the quoted string is verbatim and `grep`-unique, and the F.W5 and F.W0 edges it derives
both hold. **Cure: replace the numeral with the §-heading + the quoted fragment, per §0g.2's own
new standing rule.**

**D-4 · MINOR (mitigated) — §ERRATA-1 E1-3(b)'s re-cut no longer reproduces.** The published loop
prints `waves → O-18a O-19`; re-run here it prints **`waves → O-18a O-19 O-20`**, because the same
round's `waves/W0.md` §ADDENDUM cites the O-20 ACK. SEAMS mtime `04:35` post-dates W0.md's `04:34`,
so the receipt was already stale when banked.
**Mitigation**: the added token is a **citation of a spent id**, not a mint — exactly the case
E1-3(d) legislates (*"Ids appearing under two layers are two citations of one ledger row, never two
mints"*) — so **S-14 CONSISTENT is untouched** and no collision is created. **Cure: re-run and
restate the one row.**

**D-5 · MINOR (mitigated) — E1-1's replacement receipt shows 2 of the 4 hits its command returns,
in a fenced block presented as the command's output.** `grep -n 'glass-ui": "7.0.0"'
keyframes/waves/KF-W0.md` returns **`:437` · `:453` · `:514` · `:522`**; the errata prints only
`:514`/`:522` and describes them as *"the two lines"*.
**Mitigation**: both printed lines are exact at their bytes, and the two omitted hits **also**
state the 7.0.0 exact devDep — so the replacement basis is stronger than shown, not weaker, and
the three-ended verdict is unaffected. **Cure: add `| sed -n '3,4p'` to the published command, or
print all four.**

**D-6 · MINOR (mitigated) — E1-1's producer-end quote mints emphasis absent from the packet's
bytes.** The errata prints *"(fourier installs 4.0.0; **keyframes installs 7.0.0 exact**; value
installs 7.0.0)"*; `sed -n '7p'` of the O-20 packet carries **no bold** on that clause. This is
E1-1's own defect #3 committed inside E1-1's own cure — the third and fourth instance of the class
the file already minutes as recurring.
**Mitigation**: every figure inside the quotation is exact; the packet declares no
byte-exactness rule of its own (unlike the KF-W0 passage E1-1 rightly charges); no verdict moves.
**Cure: drop the added asterisks, or mark them "emphasis added".**

**D-7 · INFO — S-11's quotation of the ACK's receive line adds a section mark.** SEAMS §3 prints
*"A-2..A-14, §B-1..§B-7, and §C-1"*; the ACK reads *"A-2..A-14, §B-1..B-7, and §C-1"*. I-30 rows
it correctly. Cosmetic; no figure or id changes.

**D-8 · INFO — G-F §5 R-1's parenthetical of AuroraPane-resolving ids is incomplete.** It lists
`(AP-17, AP-24, AP-29, AP-30, AP-33)`; `AP-12` and `AP-31` also resolve to `AuroraPane` in the
layer. The load-bearing claim — *no* hit in the `ApiOfflineChip` namespace, `initApiEnvironment`
→ 0 and `baseUrl` → 0 layer-wide — **reproduces exactly**, so R-1's MAJOR-bearing finding stands
whole.

---

## §6 CLOSE

**UNION-CLEAN.** Twenty-four seams, nine cures, one runbook, one G-F adjudication, one ledger row,
walked hostilely at the bytes. Both CHECK-1 MAJORs are closed and stay closed under re-derivation;
all four CHECK-1 MINORs are closed individually; the G-F arithmetic reproduces cell-for-cell from
an independent corpus walk; the born-RED witness the S-6 cure books was reproduced here from the
published `dist/` rather than inherited; the two CONFORMANT conformance layers are byte-untouched
by `git`; and status is `planned` with zero VERIFIED stamps in any voice across all four artifacts.

The eight surviving defects share one shape and it is worth the next seat's attention: **six of the
eight are receipts that went stale or pointed one line, one row, or one hit next door.** Not one is
a wrong conclusion. The register convicted this class at S-19, convicted itself for it at E1-1, and
predicted its recurrence — *"word-output is not stale-proof; it is merely stale-slower."* The round
that cured nine convictions in twelve minutes proved the prediction, in the ordering of its own
writes. **Every cure is one command, and none of them is a blocker.**

**Execution gate unchanged: nothing in any sub-tranche opens product source until the owner's
begin-word.**
