SERVED MODEL ID (runbook author B, line-1 declaration): claude-opus-5[1m]

# X-WHOLE EXECUTION RUNBOOK — DRAFT B

**Authority**: `docs/tranches/X/COHESION.md` §0f (all three loops closed; the union pass is the
sole remaining artifact) + §2 (the cross-sub-tranche dependency graph). Operands: the four
develop-complete spec layers, `union/SEAMS.md`, `union/G-F-ADJUDICATION.md`, and
`docs/tranches/V/coordination/INBOX.md`. Authored 2026-08-30 by runbook author B, independently.

**POSTURE — read before anything else.**

- **Status is `planned` for every wave, every gate, every cut, in all four sub-tranches.** This
  file moves no four-verb row and issues **zero VERIFIED stamps in current voice**.
- **This pass is planning. Nothing here opens product source.** Every command below is a
  *read-only measurement over shipped bytes* (L-19) — none writes, none is a proof script.
- **E-3 governs**: this is an **addendum-beside**. No sibling spec, no COHESION section, no
  conformance artifact, no fold file and no mail ledger was edited to produce it.
- **The owner's begin-word is the only execution key.** Nothing below may be executed before it,
  and the begin-word is the owner's alone to speak — it is never inferred from a status word in
  any document, including this one.
- Sibling trees (`../keyframes.js`, `../fourier-analysis`, `../parse-that`, `../glass-ui`) are
  **READ-ONLY** until their own wave opens. See §5.

---

## §1 THE ORDER

**39 authored waves** across four sub-tranches — X·V 12 (`W0..W11`) · X·KF 11 (`KF.W0..KF.W10`) ·
X·F 11 (`F.W0..F.W10`) · X·P 5 (`W0..W4`) — plus **3 minted-unauthored** (KF.W11 · KF.W12 ·
KF.W13, COHESION §0d), which take no verb from any close and enter no order until the SS-1/SS-2
authoring block authors them.

### §1.0 The four tracks are genuinely parallel

Each sub-tranche edits a **different repository**. The dependency graph between them is thin and
is enumerated at §1.5. Under the four-workflow cap (§5) the executor runs **at most four
sub-tranche heads concurrently**, and in practice the four heads are:

| track | head | repo | gated on |
|---|---|---|---|
| A · X·V | `X-W0` | `value.js` | begin-word only |
| B · X·KF | `KF.W0` ∥ `KF.W1` | `keyframes.js` (+ `keyframes-v-exec`) | begin-word; KF.W0's OP-1 is the **owner's hand** |
| C · X·F | `F.W0` | `fourier-analysis` | begin-word only |
| D · X·P | `X.P.W0` | `parse-that` + the fresh root | begin-word only |

**The four heads may open together.** Nothing in track A gates track B, C or D at the head.

### §1.1 Track A — X·V (`waves/W0..W11.md`, as amended by `REFINEMENT-FOLD-2026-08-28.md`
§READINESS R.1)

```
X-W0  (sitting · registers · census · the §3.e escalation slate)   ── FIRST; gates everything
  ├─ X-W1  (harness)                    ← W0's registers size the oracle slate
  ├─ X-W2  (boot cost) ──blocks──► X-W5 (shell composition)
  ├─ X-W3  (api)
  ├─ X-W9  (library)                    ← the epoch rule; the widened R1 battery inside commit 1
  ├─ X-W4  (dock/shell actions)         ← .g cells HELD by W0.j's 0/4 trigger
  ├─ X-W6  (scenes/workbenches)   ⟂ W7 on four shared paths
  ├─ X-W7  (palettes/admin)       ← N-1 mount substrate = W1's plugin-vue install
  ├─ X-W10 (canon)
  ├─ X-W8  (subtraction)                ── LAST of the demo waves
  └─ X-W11 (release + verified close)   ── LAST; consumes everything
```

**Dependency reason, edge by edge:**

| edge | reason (source) |
|---|---|
| → X-W0, first | The G-F register, the census and the §3.e escalation slate are minted here; W4.g's rows *"MUST have the G-F register to land in — which is why W0 runs first"* (R.3). |
| X-W0 → X-W1 | The oracle slate (o11/o14/o20/…) is handed from W0's registers; the R2 dead-locator census and R3 oracle-dialect pass run **before** the HARD flip (R.3: *"X-W1 HARD flip: held by R2+R3"*). |
| X-W2 → X-W5 | Boot cost is measured before shell composition rewrites it; and **W2's `AFTER.json` is non-terminal until W5's A1/A2 land** — a two-way seam, so W2 does not stamp its artifact before W5. |
| X-W1 → X-W7 | W7's N-1 gate specifies mounted assertions on an absence: `@vue/test-utils` is installed and unused and `vitest.config.ts` registers no `plugin-vue`. **The mount substrate is W1's install.** Six of W7's twenty gates are unrunnable before it. |
| X-W0.j → X-W4.g | The **one atomic trigger-gated cut**. W0.j's four-condition Glass-8 census is the trigger; W4.g is the receiving surface. FAIL branch (today) → the rows join MX-CLUSTER / the extract cluster. See §3.2. |
| X-W6 ⟂ X-W7 | Four shared paths; **W7's CrossEdges state the write order.** Genuinely concurrent otherwise. |
| X-W8 last of demo | *"it deletes what W4/W5/W6/W7 rewire"* (R.1). Plus one back-gate: **W5's C3 may not close while Dock G-L (an X-W8 probe) is RED** — the single place a subtraction wave back-gates a composition wave. |
| X-W11 last | Release + verified close; consumes everything, incl. 51 OUT-OF-WAVE census rows handed to owning folds. |

**Parallel inside track A**: W1 · W2 · W3 · W9 · W4 · W6 · W10 are independent of one another
once W0 closes, subject only to the four-workflow cap and the W6⟂W7 write-order.

### §1.2 Track B — X·KF (`keyframes/waves/KF-W0..KF-W10.md`)

```
KF.W0  Substrate Settle    ∥    KF.W1  Mail Cure         ── both open at the begin-word
   │                                 │
   │  (OP-1 = the OWNER'S HAND:      │  (deliberately completable WITHOUT §B-12)
   │   git fetch && git reset --hard │
   │   origin/master — value.js      │
   │   must NOT perform it)          │
   ▼                                 │
KF.W4  K-Quartet + Gate Chassis  ◄───┘   ── THE DECLARED SEQUENCING HEAD
   │      (G-KFW4-1 must land before ANY repair packet or UNIT opens)
   ├─ KF.W2  Parse Façade          ── publishes both registries before any cure commit
   ├─ KF.W5  Library Tri-Fold      ── S-0: G-XSS lands as commit 1, before any other byte
   ├─ KF.W6  Glass Suffusion       ── ∥-atomic with KF.W4 on scripts/observe/demo/usability.mjs
   ├─ KF.W7  Timeline Evaluate     ── G14 resolves against KF.W2's posture registry
   ├─ KF.W8  Structure & Colocation── after W0 · W4 · W5, and after W6's R4-1 atomic commit
   └─ KF.W9  Safari Visual Audit   ── after KF.W0 §B-12 ONLY (KF.W4 explicitly NOT a precondition)
                     │
                     ▼
              KF.W10  Fold Discharge & Close   ── LAST; hard-blocks on W0 · W1 · W9/SS-13

KF.W3  Parser Consumption — GATE-KEYED, NEVER SCHEDULED (see §1.5)
KF.W11 · KF.W12 · KF.W13 — MINTED, UNAUTHORED; sequence after KF.W0 + the KF.W4 vue-tsc gate
```

**Dependency reason, edge by edge:**

| edge | reason (source) |
|---|---|
| KF.W0 ∥ KF.W1 | KF-W1 §State: *"**Not** after X.KF.W0 … this wave is deliberately **completable without §B-12**, because the exec-clone delivery path exists precisely so the mail cure never queues behind the owner's hand."* KF-W0's spec claims the reverse edge; **the conflict is RECORDED, not silently resolved** (KF-W1 §7 cross-edge 2). At execution the operator reads the conflict as a *permission* (W1 may start early), never as a licence to skip W0's re-anchor for W1's own anchors. |
| KF.W0 → KF.W4 | KF-W0 §Sequencing, the KF.W4 cross-edge: **"may not open before this re-anchor"** — G-0.7's roster is KF.W4's precondition; the C-12 demo-typechecking arm folds to the single banked no-SFC-typecheck identity. |
| KF.W4 = head | KF-W4 §Sequencing: *"This wave is the declared sequencing head of X·KF. No repair packet and no UNIT may open before **G-KFW4-1** lands"* — stated independently by **eight** banked records. *"Nothing in the template layer is verifiable until a `.vue` file can fail a build"* (KF-CE-16). |
| KF.W0 → KF.W2 | KF-W2 §Sequencing hard order 1: the wave's opening commit **states its ref** (`origin/master 81a56990` or later); no anchor is trusted before it. |
| KF.W2 → KF.W7 | KF-W2 hard order 2: *"KF.W7 may not decide a failure posture before KF.W2 publishes the registry"* — **declared at both ends**; KF-W7's G14 resolves against that registry. |
| KF.W4 → KF.W6 | KF-W6 §Sequencing → KF.W4: *"the vue-tsc gate precedes verification of every typed cure here."* Plus **ATOMIC BUNDLE**: `scripts/observe/demo/usability.mjs` is *"written ONLY inside the KF.W4∥KF.W6 atomic bundle (one commit, both seats)"*. |
| KF.W6 → KF.W8 | KF-W8 §Sequencing R-1: W6's **R4-1 atomic commit lands first; W8's G7 measures after**; the MOVE-vs-RESHELL guard — *"`CopyButton.vue`'s MOVE is KF.W8's; W6's reshell MODIFIES IN PLACE"*; the two acts **never share a commit**. |
| KF.W0 → KF.W9 | KF-W9 S-1 (BLOCKING): KF.W0 §B-12 names the execution substrate; OP-3 (safaridriver reachable) + OP-5 (fresh build at the pinned ref) + OP-4 (per-cell capability record). **OP-6 declares KF.W4 not a precondition — this wave measures, it does not type-cure.** |
| KF.W9 → KF.W10 | KF-W9 S-10 (*PRECEDES; W10 DEPENDS*) at six enumerated anchors in KF-W10; KF-W10 §State opens after **KF.W0 · KF.W1 · KF.W9/SS-13**. |
| KF.W0 → KF.W10 | KF-W0's C-17 **MINT PRECEDES** KF-W10's CARRY-FORWARD RECORD block; `CARRY-C-3`/`CARRY-C-5` consume the ids C-17 assigns. **W0 names; W10 rules. W10 consumes; it never mints.** |
| KF.W1 → KF.W10 | KF.W1's outbound row **O-21** is the vehicle KF-W10 names by id (§END KF-W10 D-8). See §3.4. |

**Parallel inside track B**: after KF.W4's G-KFW4-1 lands, W2 · W5 · W6 · W7 are concurrent
subject to the W2→W7-posture edge and the W4∥W6 atomic bundle; **KF.W9 is concurrent with the
whole W4 fan-out**, since it depends on KF.W0 alone.

### §1.3 Track C — X·F (`fourier/waves/F-W0..F-W10.md`)

```
F.W0  Re-ground the substrate  ── FIRST, unconditionally; the pre-gate wave
   │   internal order: G-4 → G-5 → the quarantine
   │                   G-1 → G-11 → G-10's lift
   │                   G-3 → the SENDs
   │
   ├────────────────────────────► F.W5  Shared-provenance contract (ADMISSION KEYSTONE)
   │                                 │    (NOT gated on F.W1)
   │                                 ├─ F.W6  (after FW6-G19 receipt + F.W5 clause set + G18)
   │                                 ├─ F.W7  (⊙ OWNER-GATED on G-F7-1; unit b cannot open
   │                                 │         until the owner speaks)
   │                                 └─ F.W8  (also after F.W1.close and F.W2)
   ▼
F.W1  ONE ATOMIC LAND-OR-LOSE TRANSACTION (TWELVE limbs)   ⊙ ESC-1/G1 OWNER-GATED
   │   opens after F.W0 closes (G2 · G3 · MISS-LC2 · F8-REACH-01+02) AND ESC-1 ruled
   ├─ F.W2  (specifier arm only; the COLOURS arm is NOT so gated — fr-App C-2)
   ├─ F.W3  Shadow retirement + glass suffusion
   ├─ F.W4  Frontend audit
   └─ F.W9 ──► F.W10   ── RE-CUT-0 (G-F9-21 ≡ G-F10-6) precedes both stamps; PAIRED EDIT
```

**Dependency reason, edge by edge:**

| edge | reason (source) |
|---|---|
| F.W0 first, unconditionally | F-W0 §6a lock 1 (COHESION §0a verbatim): *"F.W0 substrate pre-gates FIRST. **G-4 · G-1 · G-13 · G-12 precede F.W1 SIZING.***"* The bank's *"F.W1 runs a build anyway"* clause is **STRUCK**: *"F.W1 would open on a red build, and no uplift delta computed against a sheet that does not parse is trustworthy."* Restated identically at F-W2 6a, F-W4 §5.1.1, F-W6 §4.1 (*"F.W0 failing to re-ground HALTS this wave"*), F-W9 §4a.2, F-W10 §4a.2. |
| F.W0 internal: G-4 → G-5 | *"No build can run while the stylesheet does not parse; `web/dist` is not touched until G-4 and G-5 are green, because delete-before-rebuild destroys the only emission evidence the corpus has."* |
| F.W0 internal: G-1 → G-11 → G-10 | *"Anchors cannot be re-resolved against a tree no commit holds; the 8-site lift is applied at re-resolved anchors, never at the record's raw line numbers."* |
| F.W0 internal: G-3 → the SENDs | The P-1/P-6 packets name *"whoever holds the mail seat"*; **the seat is created here**, so no letter leaves before G-3. |
| **FR-NP-32 before every dist-dependent witness** | `grep -rl 'FR-NP-32' fourier/waves/` → **11 of 11**. The corrupt glass-ui 4.0.0 `dist/styles/index.css` reddens every emitted-CSS readback; G-5's GREEN is *"build exits 0 **and** the post-build readbacks fire"*. Citation form is **cite-both, never substitute**: `FR-NP-32 (≡ fr-PaperSidebar M1)` (SEAMS S-23, CONSISTENT at four ends). |
| F.W0 → F.W1 | F-W1 §State: opens after F.W0 closes AND begin-word AND **ESC-1 ruled (G1)**. F-W1 §4 step 2: *"F.W0 CLOSES before F.W1 opens — this step is F.W1 reading F.W0's published results, never F.W1 containing F.W0's acts."* |
| F.W1 atomic | F-W1 §4 step 4: **ONE commit, TWELVE limbs.** *"A partial landing is the land-or-lose failure the L-18 base forbids"*; splitting it turns four zero-console-error e2e gates RED (FR-EQR-3). The extent is **cited whole, never restated** — ten of eleven F-W* specs cite it in one spelling (SEAMS S-22). |
| F.W1 → F.W3 / F.W4 | COHESION §2: *"F.W1 tri-package uplift precedes F.W3/F.W4 — no fourier frontend spec may assume the uplifted tree before W1 lands it"*; restated as *"§2 law"* at F-W4 §5.1.2, F-W5 §4, F-W9 §4a.3, F-W10 §4a.3. |
| F.W1 → F.W2, **partially** | F-W2 §State: *"F.W1 landed **for the specifier arm only**; the colours arm is *not* so gated (fr-App C-2)"* — the `colors.ts` cure is landable at 0.13.0 today, alone. **This is a genuine partial-parallel edge and the runbook honours it.** |
| F.W0 → F.W5 (not F.W1) | F-W5 §State: F.W0 (HARD, §4 X-1) · the intake adjudication · the registry whole. **F.W5 is the ADMISSION KEYSTONE and is not downstream of the uplift** — it can run beside F.W1. |
| F.W5 → F.W6 / F.W7 / F.W8 | F-W6 in-wave order `a` consumes *"the F.W5 clause set"*; F-W7 §State names F.W5 §2 clauses E16 · E1 · E3⊕G4 · E10 as **hard predecessors**; F-W8 §State names F.W5's *"three artifacts"* as inputs. |
| F.W5 G11 → F.W1 sizing | fr-AdminFlaggedPanel ruling 5: *"the F.W5-W8 producer-or-retire decision must precede F.W1's sizing where grades are load-bearing."* **A back-edge into the atomic wave — the operator schedules the G11 ruling before F.W1 sizes.** |
| F.W9 ↔ F.W10 | **RE-CUT-0 precedes every other row**: neither stamps before G-F9-21 ≡ G-F10-6 closes; **any change to the shared §2.2 bytes is a paired edit in ONE commit**; *"F.W10 does not author F.W9."* |

### §1.4 Track D — X·P (`parse-that/waves/W0..W4.md`) — strictly serial

```
X.P.W0 ─► X.P.W1 ─► X.P.W2 ─► X.P.W3 ─► X.P.W4
```

| edge | reason (source, `**Opens after**` line at each §State) |
|---|---|
| → W0 | *"tranche X opens (X ratified, SCOPE M-22 ¶1) AND the owner's begin-word for the X·P lane. No predecessor wave exists — this is the lane's first file."* |
| W0 → W1 | *"X.P.W0 **closed** (the fresh root exists at the ruled commit and the frozen roots are proven byte-unchanged)."* |
| W1 → W2 | *"X.P.W0 closed GREEN … **AND X.P.W1 IMPLEMENTED** (the three instruments landed in the fresh root: `harness/totality/**` · `harness/equivalence/**` · `harness/bench/**`, plus the bar ledger)."* |
| W2 → W3 | W3 names W0 + W1 + W2 and adds: *"**The dependency is on the conditions, not the labels** — §2b enumerates each as a checked open-precondition, so a sibling re-numbering cannot silently open this wave early."* |
| W3 → W4 | *"X.P.W3 IMPLEMENTED (the 52-export universe TOTAL, the R1 throw class dead, the equivalence floor held, the divergence ledger written, the three-leg table published)."* |

**X.P.W4 is the single site in the whole X·P lane that stamps VERIFIED** (its `.d` fresh-Fable
adjudicator seat). Nothing downstream of X·P is scheduled by that stamp — see §1.5.

### §1.5 The cross-sub-tranche edges — the only four

**E-1 · PLAW-BIND: X.P.W4's `RC-P(V)` → KF.W3. GATE-KEYED, NEVER SCHEDULED.**
`RC-P(V)` is TRUE iff all six conjuncts (PUBLISHED · TOTALITY · EQUIVALENCE · ADMITTED ·
BAR-DISCHARGED · ROUTED) evaluate TRUE **for the same version `V` of `@mkbabb/value.js`**,
*"evaluated by running the six commands against the registry coordinate `V` — never by reading a
status word in any document, including this one."* All six are FALSE at the dated measurement.
**Direct `parse-that → fourier` is FORBIDDEN** (standing routing law, declared at both COHESION §2
and F-W0's X·P cross-edge row as a NON-EDGE). KF.W3 carries the opening sentence verbatim at its
only lawful site (SEAMS S-1/S-2, CONSISTENT). **The operator never schedules KF.W3; it opens or it
does not.**

**E-2 · The chain's middle link is UNHOMED — an owner decision, not a wave.**
The spine states `parser → value (X·V L1/L5 surfaces) → packed release → consumers`, but **no X·V
wave adopts a parser**: `X/waves/W9.md` §5.g records why value.js ships none of parse-that today
and G31's own falsifier is literally *"adopt a parser inside this wave."* X·P names the gap as its
principal finding (OP-4) and refuses to fill it unilaterally. **SEAMS S-4 convicts the spine and
routes G-9's three dispositions — A (a successor X·V wave) · B (amend G31 to admit adoption inside
tranche X) · C (close `BLOCKED-ON <condition> + <re-trigger>`) — to the owner.**
▶ **RUNBOOK ACTION: this is an agenda item at the X-W0 opening sitting. It is not a wave, it is not
scheduled, and it is not the executor's to elect.** Related: KF.W3 declares two publication asks
against `V` (`serializeCssValue` unexported from the packed `./css` surface; `reverseAnimationShorthand`
absent at every measured coordinate) — both belong on the same dated cut as X-W9's CC-084.

**E-3 · SS-4 contract co-signature ↔ F.W5 ∥ the X·V API row.**
COHESION §2: *"neither side's edits gate the other's waves; the value-side atomdiff restoration
(TA-4) is a named prerequisite or the contract is re-scoped explicitly."* **F.W5 and X-W3/X-W9 run
concurrently.** F.W5's owner rulings (E16 trie-vs-KISS, D3 moderation producer-or-retire, OG-F1,
OG-F2) are **FLAGGED INLINE, never presumed** — they join the X-W0 sitting agenda alongside E-2.

**E-4 · F.W2's packet P-7 declares a value-side reciprocal that X·V has not booked.**
SEAMS S-6, CONVICTED: `grep -rc 'P-7' waves/ REFINEMENT-FOLD-2026-08-28.md` → **0** at every X·V
file. Clause (d) — `parseStylesheet` **total on the string arm but throws on Buffer input**
(`e.startsWith is not a function`) — is *"the surviving totality nit for X-W9"* and has no
receiving row. ▶ **RUNBOOK ACTION**: X-W9 opens against `refinement/X-W9-FOLD.md` and books
(a)–(d) as **received** — accept or decline-with-reason, written at the X·V end so F.W2's cell has
an answer to cite. Clause (d)'s Buffer throw joins the R1/totality family with its own born-RED
witness, **inside X-W9's commit 1** where the widened R1 battery already lives.

**Non-edge, declared so it is measurable**: the COHESION §2 Glass-8 atomic-cut law
(`X-W0.j → X-W4.g`) is stated at the spine alone; `grep -rl 'X-W0\.j' keyframes/waves/
fourier/waves/` → no output. SEAMS S-9 convicts the universal quantifier and re-scopes the law to
**value.js**. Each sibling pins its own producer dependency at its own gate (X·F at G1/ESC-1;
X·KF at KF.W0 §B-12's 7.0.0 exact devDep). **No sibling wave takes an edge into X-W0.j.**

---

## §2 THE FIRST GATES — born-RED, to run at the begin-word

**23 gates.** Every command below is quoted from its spec at the cited coordinate and was
confirmed present there by this seat, read-only, 2026-08-30. Every one is a `git`/`grep`/`node -p`
one-liner or a named live witness (L-19) — **no proof script is authored for any gate**.

**Standing law on running them (X·V §READINESS R.2, program-wide):** *"Running them at wave-open
re-dates the baselines; **a PASS on any of them before its cure lands is itself a finding and goes
to the sitting.**"* Two X·V gates are already in that state (gate 2's leg-4 flip; gate 10's
green-at-pin) — both routed to the sitting via §EXTERNAL.

### §2.1 X·V — ten gates ⟨`REFINEMENT-FOLD-2026-08-28.md` §READINESS R.2, round-1 corrected forms⟩

| # | gate | command (literal) | expected |
|---|---|---|---|
| 1 | **X-W0 G-F**, the register | `ls docs/tranches/V/megatranche/registry/adjudicated/*.md \| grep -vE '/(kf\|fr\|pt)-' \| xargs grep -c 'NO-WAVE-OWNER' \| grep -v ':0' \| wc -l` | **73** records / **604** occurrences; `grep -c NO-WAVE-OWNER docs/tranches/X/waves/*.md` → 0 in all twelve. **RED until the set-difference is ∅ both ways.** |
| 2 | **X-W0.j** Glass-8 repin census (the X-W4.g trigger) | `node -p "JSON.parse(require('fs').readFileSync('node_modules/@mkbabb/glass-ui/package.json','utf8')).version"` | **7.0.0**. Census **1/4** — legs 1–3 unchanged, leg 4 flipped by I-28. **FAIL branch is the live branch**; the .g cells stay held. |
| 3 | **X-W9 G2/NG-2**, the R1 throw class | `node -e 'import("./dist/subpaths/css.js").then(m=>m.parseCssColor("oklch()"))'` | **TypeError** (unhandled rejection; likewise `calc()`, `foo()` — the class is `<any-ident>()`). RED. |
| 4 | **W-HYGIENE H-c** falsifier | `node -e 'const fs=require("fs");const t=JSON.parse(fs.readFileSync("tsconfig.demo.json","utf8").replace(/^\s*\/\/.*$/gm,""));const p=require("./package.json").exports;console.log(Object.keys(t.compilerOptions.paths).length, Object.keys(p).length)'` | **`10 7`** — the 10/7-key drift. RED; **cure = DELETE the inert block, never repair.** |
| 5 | **X-W4 A4** witness | `sed -n '81,87p' demo/shell/dock/layers/SlugEditLayer.vue` | the placeholder-only accessible name. RED at both matrices. |
| 6 | **X-W5 D4** witness | `grep -c '<Transition ' demo/workbenches/mix/MixSourceSelector.vue` | **0**, against the bare `<template v-if="mode === 'colors'">` mode swap at `:114`. RED. |
| 7 | **X-W7 N-1**, the mount substrate | `grep -rn 'mount(' test/ demo/ --include='*.test.ts' \| wc -l` ; `grep -c 'plugin-vue' vitest.config.ts` | **0** and **0**. Six of W7's twenty gates specify mounted assertions on this absence. RED. |
| 8 | **X-W1 R2**, the dead-locator census | `grep -rn 'plate-caption' e2e/ demo/` | a live un-skipped spec asserts a class deleted at `a68ecdc1`. RED; **`test.skip()` is deferral under a new name and does not discharge it.** |
| 9 | **X-W8 G-9** null-DELTA witness | `sed -n '8p;22p' demo/palettes/browser/admin/PaginationBar.vue` | the null-DELTA pair (both `variant="outline"`); collides with X-W7's quiet-migration reading. RED. |
| 10 | The glass exports-map parity watch | `node -p "const e=JSON.parse(require('fs').readFileSync('node_modules/@mkbabb/glass-ui/package.json','utf8')).exports;['./blob','./chip','./forms'].map(k=>k+':'+(k in e)).join(' ')"` | **`./blob:true ./chip:true ./forms:true`** — **NOT RED, GREEN-at-pin.** Stays on the slate because **it flips RED at the 8.0.0 repin** (`./forms` dies — X-EXT-2), which is exactly what it must catch. |

### §2.2 X·KF — five gates

| # | gate | coordinate | command (literal) | expected |
|---|---|---|---|---|
| 11 | **G-0.1 SUBSTRATE SETTLED** | `KF-W0.md` §Gates | `git rev-list --count HEAD..origin/master` · `git status --short \| wc -l` · `git diff --name-only origin/master \| wc -l` | **41** · **252** · **325**; HEAD `8281638c`, `origin/master` `81a56990`, neither an ancestor. RED. |
| 11b | — its falsifier (**the two surfaces do not nest**) | same | `comm -13 <(git status --short \| sed 's/^...//' \| sort -u) <(git diff --name-only origin/master \| sort -u) \| wc -l` | **225** frontier-diff files NOT in the status set (and `comm -23` → **152**, `comm -12` → **100**). **GREEN requires BOTH enumerations, never one plus a difference.** |
| 12 | **G-0.2 MANIFEST SINGLE-STATE** | `KF-W0.md` §Gates | `git show origin/master:package.json` · `grep -c glass-ui package.json` · `node -p "require('./node_modules/@mkbabb/glass-ui/package.json').version"` | `:77 "@mkbabb/glass-ui": "7.0.0"` **exact devDep** · **0** (the worktree DELETES the row) · **7.0.0** — four disagreeing states. RED. |
| 13 | **G-KF1-1 DELIVERY** | `KF-W1.md` §Gates | `ls $PROG/keyframes-v-exec/docs/tranches/V/coordination/` | **9 entries + `vnext/`** — *"No addendum. No O-8. No O-11."* RED. ⟨`$PROG` is declared once, at that gate: the directory holding the sibling clones.⟩ |
| 14 | **G-KF1-2 ANCHOR LIVENESS** | `KF-W1.md` §Gates | `git cat-file -e origin/master:src/animation/compile/value-ast.ts` (run **in `keyframes-v-exec`**, against `origin/master`, never bare `HEAD`) | **ABSENT** — O-11 §A3's primary authored-input anchor. RED. Per-anchor, not blanket. |
| 15 | **G-KFW4-1 the vue-tsc gate** (THE SEQUENCING HEAD) | `KF-W4.md` §Gates row 1 | `ls node_modules/.bin/vue-tsc` · `npx tsc --noEmit --listFiles` | **ABSENT at both coordinates** · **126 demo files, 0 `.vue`**. RED. The redefinition is `"check": "vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && npm run proof:structure"` — **the `&& npm run proof:structure` tail SURVIVES verbatim** (NO-SILENT-DELETION falsifier, scope = all five opened scripts). Fails equally if `demo/env.d.ts:3-7`'s `DefineComponent<{},{},any>` shim survives. |

### §2.3 X·F — five gates ⟨all `F-W0.md` §4⟩

| # | gate | command (literal) | expected |
|---|---|---|---|
| 16 | **G-1** — the 28-path M.W1a tree is SETTLED, per path, minuted | `git rev-parse --abbrev-ref HEAD` · `git rev-parse --short HEAD` · `git status --porcelain \| wc -l` | `m/w1-bump-migration` · `cd26c65` · **28** (27 ` M` + 1 `??`). RED. **A wholesale reset is a gate FAILURE, not a gate pass** — rows 24 (FR-EMT-25) and 25 (C:S-2) are NAMED EXCEPTIONS that must LAND. |
| 17 | **G-2** — the O-14 letter is COMMITTED | `git ls-files docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md` | returns the path (GREEN). RED today: it is the sole `??` row. |
| 18 | **G-4** — the adopted producer stylesheet PARSES | `grep -o '/\*' web/node_modules/@mkbabb/glass-ui/dist/styles/index.css \| wc -l` · `grep -o '\*/' … \| wc -l` | **17** vs **8** (against **15 / 6** in `src/styles/index.css`); `postcss([@tailwindcss/postcss])` → `CssSyntaxError: Unterminated string: 's own'`. RED. **A consumer-side patch is a GATE FAILURE.** ⟨**FR-NP-32 (≡ fr-PaperSidebar M1)** — cite both, never substitute.⟩ |
| 19 | **G-5** — `npm ci && npm run build` runs to completion in `web/` | `git ls-files web/dist` · `ls -ld web/dist` | **0** · present, dated `Jun 12 18:13`. RED (two live breaks: G-4 and MISS-A7). GREEN = build exits 0 **and** the post-build readbacks fire. **`web/dist` is not touched until G-4 and G-5 are green.** |
| 20 | **G-13** — the producer pin is re-derived at the ADOPTED commit | producer `package.json` read + installed-consumer pin read | producer reads **8.0.0**; installed consumer pin **4.0.0**. RED. **The pin cell is the COMMIT HASH, not the version label** (P-4). Its 2026-08-28 addendum is *"a dated reading, never a live fact — re-measure at wave open."* |

### §2.4 X·P — three gates ⟨all `parse-that/waves/W0.md` §6⟩

| # | gate | command (literal) | expected |
|---|---|---|---|
| 21 | **G-1 HANDOFF IDENTITIES RE-DERIVED** | `shasum -a 256 <path>` over the four named identities | `ced23440…f20f7` (the handoff itself) · `244c448a…504a7` (parser-law matrix, 30,242 B) · `aa891714…92767` · `0002ed93…d0f50`. Three-state disposition **MATCH / MISMATCH / EPERM, never conflated.** |
| 22 | **G-3 EIGHTEEN ROOTS, BYTE-UNCHANGED** | `git -C <root> rev-parse --short HEAD` · `find <root> -type f -not -path '*/.git/*' -not -path '*node_modules*' \| wc -l`, then `diff census-before.txt census-after.txt` | the eighteen-root table (`parse-that ef10d5b` 20261 files DIRTY · `parse-that-css-totality f575708` 757 files · …). GREEN = **the diff is empty.** |
| 23 | **G-8 PLAW-BIND DECLARED FROM BOTH ENDS** | `grep -n 'X·P release condition → KF.W3' docs/tranches/X/COHESION.md` · `grep -n 'parse-that→fourier is FORBIDDEN' docs/tranches/X/COHESION.md` | each returns ≥1. **1 of 2 ends declared** at the dated baseline. **Located by literal fragment grep, never by line coordinate** — COHESION is a declared live document and a coordinate into it rots by design. |

---

## §3 THE SEAM PROTOCOL — the seams as execution-time checkpoints

`union/SEAMS.md` adjudicates **24 seams · 15 CONSISTENT · 9 CONVICTED**, every conviction an
**addendum-beside at the file that owns the end**, none executable before the begin-word. The
runbook's job is to place each one on the wave calendar.

### §3.1 The nine convictions, as checkpoints

| # | seam | checkpoint — when the operator touches it | owning end |
|---|---|---|---|
| 1 | **S-3** fourier half of the G-6 reciprocity absent (`grep -rl 'RC-P' fourier/waves/` → ∅) | **At F.W0's opening**, before G-3's SENDs. Adopt G-6's second sentence verbatim **by predicate name** (`RC-P`), beside the existing NON-EDGE row. **X·P may not write it.** | `fourier/waves/F-W0.md` |
| 2 | **S-4** the L1/L5 adoption leg UNHOMED | **X-W0 opening sitting**, agenda item 1 (= §1.5 E-2). Owner elects G-9 A / B / C. | `COHESION.md` new §0g |
| 3 | **S-5** repin coordinate spelled as a literal | **Before KF.W5 and KF.W10 open.** Convert `4.0.0→4.1.x` to `4.0.0→`V` at `RC-P(V)`` per KF-W2's D-8 ruling — **`RC-P` is a predicate, never a number this spec may forecast.** | `KF-W5.md` ×2 · `KF-W10.md` ×1 |
| 4 | **S-6** F.W2's packet P-7 has no receiving end | **At X-W9's opening** (= §1.5 E-4). Book (a)–(d) as received; the Buffer-input throw gets its own born-RED witness. | `refinement/X-W9-FOLD.md` (never `W9.md` — UNTOUCHABLE under the fold's E-3 posture) |
| 5 | **S-9** Glass-8 atomicity stated at one end only | **At X-W0, F.W1 and KF.W0 openings.** Re-scope the law to value.js; each sibling declares the non-edge in one line so the absence is **measurable rather than merely absent**. | `COHESION.md` §2 + `F-W1.md` + `KF-W0.md` |
| 6 | **S-11** the 2026-08-29 O-20 ACK is unrowed | **AT THE BEGIN-WORD, before any wave opens** — see §3.3. | `INBOX.md` (+ X-W0.j · §EXTERNAL · F-W1 PIN-LAW · F-W0) |
| 7 | **S-15** F.W3's g20 believes the letter has not left | **Before F.W3 opens.** Re-baseline g20 against the dispatched O-20 by sha (`f6e04c86ddb8`); cargo becomes the **residual**; the verb becomes *accretes to SS-6*, not *files*. **No ask withdrawn, no packet re-sent.** | `fourier/waves/F-W3.md` |
| 8 | **S-16** the O-20 denominator | **Before F.W10 closes.** Restate **22 id-rows ⊕ 4 §D entries = the packet's own 26**; withdraw the *"correct COHESION at its own site"* instruction — **the figure it would correct is true.** | `fourier/waves/F-W10.md` |
| 9 | **S-19** §0d's line anchor into a live sibling (`KF-W4:249` is blank; the block is at `:254`) | **Rider on the same §0g letter as S-4** — one dated addendum at the spine, not three. Re-anchor to **§-heading + rule id, no numeral**. | `COHESION.md` §0d |

### §3.2 The glass-8.0.0 repin decision point

**The trigger and its receiver are X·V-internal and consistently spelled** (SEAMS S-24): X-EXT-6 →
**X-W0.j** (the repin act itself); X-EXT-4 → **X-W4.g** (the receiving surface). Eight EXTERNAL
bookings + two pointers across the per-wave folds.

**The decision point sits at the X-W0 sitting, and it has grown a third option.**

- **Measured today**: X·V pin **7.0.0**; the 8.0.0 receipt EXISTS (INBOX **I-28**, `v8.0.0` @
  `17a11bc5`); the X-W0.j census reads **1/4** and the **FAIL branch is live** — the .g cells stay
  held.
- **§EXTERNAL** carries the six minted rows: **X-EXT-1** `SearchBar` DELETE → X-W7 (four measured
  `demo/palettes/` edges; the `.input-bar` recipe survives on `./styles`) · **X-EXT-2**
  `./forms`→`./input` → X-W8 · **X-EXT-3** `grain` removed → X-W6 · **X-EXT-4**
  `--slider-track-bg` no-alias → X-W4.g (**mail says ×4, the tree measures 5 — both figures
  stated, neither silently elected**) · **X-EXT-5** o7 re-pin → X-W1 · **X-EXT-6** peer
  `vue-component-type-helpers ^3.0.3` → X-W0.j.
- **The third option (S-11)**: a glass reply has sat in the BK inbox since **2026-08-29**
  (`glass-outbound-2026-08-29-valuejs-o20-ack.md`) announcing **9.0.0 CUT** — tag `v9.0.0` at
  `d4f7b24f`, `./search` **removed whole**, `./canvas` gone, the 8.1.0 dark-sync fold riding the
  same section — and stating in the producer's own coin that **every content claim is tag-pinned,
  not registry-pinned** (the npm PUT is walled).

▶ **RUNBOOK ACTION at the sitting**: widen X-W0.j's four-condition verdict to enumerate **8.0.0
AND 9.0.0** as candidate targets, so the census returns a dated verdict over the *real* option set;
give §EXTERNAL's six rows a `9.0.0 successor fact` column where the letter supplies one
(`./search` whole-removal beside X-EXT-1). **The sibling pins are not moved by this**: X·KF stays
at its KF.W0 §B-12 7.0.0 exact devDep; X·F elects its own target at **ESC-1/G1**, whose PIN-LAW
(*"EVERY §7 uplift row pins to the PUBLISHED artifact before the transaction executes"*) needs the
**tag-pinned-vs-registry-pinned** clause before an owner can elect 9.0.0 knowingly. **F.W0's
FR-NP-32 BLOCKER stands regardless** — it is measured at the *adopted* 4.0.0 and the corruption is
still in those bytes; what the ACK supplies is the producer-side terminus (A-1 **ACCEPTED**; the
permanent parse-gate lands in glass's O-20 disposition wave).

### §3.3 The mail obligations — O-21, and the I-30 that comes first

**Before any wave opens** (E13: *a wave may not CLOSE with UNREAD mail; the union boundary is a
boundary*):

1. **Row the 2026-08-29 O-20 ACK as I-30** — with the A-1 ACCEPTED disposition and the 9.0.0
   tag/registry split stated in the row. **A ledger act, not a wave act.** It is not merely
   unread, it is **unrowed** — the identical mechanism I-28's own row minutes as having cost 19
   days. `grep -c 'o20-ack\|9\.0\.0' docs/tranches/V/coordination/INBOX.md` → **0**; the ledger's
   last rows are O-20 · I-28 · I-29.
2. **Run the four-path sweep** (§5.3) at the same sitting.

**O-21 — minted exactly once, by KF.W1** (SEAMS S-13, CONSISTENT). `grep -rl 'O-21'` across all
four wave sets → **X·KF only**: `KF-W1.md` mints; `KF-W2` · `KF-W4` · `KF-W9` · `KF-W10` cite by
id as a **vehicle**, never as their own act. The **MINT LAW** governs and survives the ledger
moving:

> *the commissioned id is **O-21** (= the measured ledger maximum **O-20** + 1, measured
> 2026-08-28 at `INBOX.md:95` / commit `48003630`); the executing seat **re-runs the max-grep at
> open** and, if the ledger has moved again, mints **max+1** and records the delta as a dated
> addendum before executing — a spec-frozen outbound literal is never trusted over the measured
> ledger.*

▶ **Note for the KF.W1 seat**: I-30 is an **inbound** id and does not move the outbound maximum —
but the MINT LAW's re-measure at open is exactly what makes that safe **without editing KF-W1**.

**No X·V, X·F or X·P wave mints an outbound id** (SEAMS S-14). X·F cites O-10/O-14/O-15/O-17/O-20
as spent history; X·V cites O-18/O-19; **X·P's outbound act is a packet authored in-repo and handed
to SS-6's courier**, never a fresh channel (W4 G-8). **SS-6 is the only lawful cross-repo write
path in tranche X, and it is root-authored** — a wave that opens its own channel has substituted
an ad-hoc letter for the batched-root law. This is exactly what S-15 convicts in F.W3's g20.

### §3.4 The coupled events, restated for the executor

| event | the lock, in one spelling at every citing site |
|---|---|
| **F.W1 land-or-lose** | *"the TWELVE-limb roster chartered at F-W1 §4 step 4, cited whole and never restated"* — 10 of 11 F-W* specs, same clause, same anchor form (§ + step, no line). **A partial landing is the land-or-lose failure the L-18 base forbids.** |
| **FR-NP-32 pre-gate** | 11 of 11. *"**FR-NP-32 (≡ fr-PaperSidebar M1)** … two banked witnesses of ONE fact; the citation form is **cite-both, never substitute**."* Carried at O-20 row **A-1**. |
| **repair-arms-a-defect riders** | `fr-PaperSearchModal` same-commit riders · **PAW-44/LAW-3** (*"PAW-44 restoration lands **WITH-or-AFTER** PAW-1 + PAW-30, never before"*, carried verbatim) · **MPC-31 one-cut law** · **FR-MSP-6 two-channel lock** — each present in 7 fourier specs under one spelling; PaperSearchModal in 8. |
| **KF-AV-28 standing supersession rider** | KF.W7's evaluate verdict may supersede any NO-WAVE-OWNER behavioral cure. **Both limbs travel**: the supersession sentence *and* the §7 caution + NON-bespoke clause — a packet carrying only the first can still be cured by a primitive swap the caution forbids. |
| **X-W4.g / X-W0.j** | one trigger, one receiver, one spelling — see §3.2. |
| **RE-CUT-0** | `G-F9-21 ≡ G-F10-6`; neither F.W9 nor F.W10 stamps before it closes; any change to the shared §2.2 bytes is a **paired edit in one commit**. |
| **KF.W4 ∥ KF.W6** | `scripts/observe/demo/usability.mjs` — *"read-only for W6 alone; written ONLY inside the atomic bundle (one commit, both seats); never unilaterally, never concurrently."* |

### §3.5 The fifteen CONSISTENT seams — verify-at-open, do not re-litigate

S-1 · S-2 · S-7 · S-8 · S-10 · S-12 · S-13 · S-14 · S-17 · S-18 · S-20 · S-21 · S-22 · S-23 ·
S-24. Each is stated at both ends in the ends' own bytes. **The executor re-runs the quoted
command at wave-open to re-date the reading; a divergence is a finding for the sitting, never a
licence to re-adjudicate the seam.** Note especially **S-12**: X·KF (KF.W7 C-15, per-surface
ADOPT/PARTIAL/KEEP-BESPOKE against the *importable* surface) and X·F (F.W3 SR-1, the prescribed
`ScrubberTimeline` adoption REJECTED, the gate asserts a re-derivation not an import) are **the
same verdict at two altitudes**, and O-20 carries both once each (A-8, A-10). **Two consumers, one
producer surface, zero contradictory asks** — do not let an executing seat manufacture one.

---

## §4 THE RESIDUE — what has a home but no wave

### §4.1 SS-8 — the UNPROVEN-NEEDS-LIVE visual-audit slot (value.js)

**~751 residue items** (739 counted from §UNPROVEN sections across the corpus, **corrected +12**
for wb-gradient-visualizer, whose section lists only R-13/R-14/R-15 because r1's R-1..R-12 ride
one prose sentence — true count 15). **These are LIVE-WITNESS obligations. They route to SS-8, the
visual-audit sub-session, never to X waves; a wave gate that claims one discharges nothing.**

Heaviest feeds: picker-spectrumcanvas 17 · PreviewStrip 16 · shell-dock-profilesection 16 ·
wb-gradient-visualizer 15 · BrowsePane 14 · wb-gradient-codeeditor 14 ·
wb-gradient-easingspecimenstrip 14 · wb-mix-animationcanvas 14 · wb-gradient-easingauthoringstage
13 · wb-mix-resultdisplay 13.

**THE ZERO-RESIDUE GAP — blocks SS-8 completeness.** Fourteen records carry **no §UNPROVEN section
at all**: App · AdminUsersPanel · ColorInput · ColorPicker · ColorSpaceSelector · ConfigSliderPane ·
Dock · GenerateControls · GradientStopEditor · Markdown · chassis-fitness · layout-gestalt ·
library-band · parser-band — the pre-X cohort. **SS-8 may not be called complete until this cohort
is swept for live-witness obligations.** Epistemic statuses travel verbatim: UNPROVEN stays
UNPROVEN, adopted stays adopted, hypothesis stays hypothesis — **never upgraded by transcription.**

SS-8 is task #4, OPEN, real-Safari automation unblocked since 2026-07-27. It is **bounded and
parsimonious** (§5.2) and it is **not a wave** — it runs beside the tracks, not inside them.

### §4.2 SS-13 — the per-repo visual audits

- **X·KF**: KF.W9 owns **the kf surface ONLY** — *"fourier/value.js residues never fold into its
  denominator."* Backlog **≈590 probes** = **565 enumerated + 25 prose-carried**, over 57 of the
  58 records, **0 executed today** — the single largest concrete backlog in the registry. The
  reproducing command is the amended `##`-or-`###` form; the unamended `##`-only form returns 563
  and would manufacture a 2-count divergence against S-13's triumvirate trigger. KF.W9 also
  produces the **OD-V3 capture packet** (both transport homes × four duplicating scenes × 390 AND
  1280) and the **OD-V5 390 at-rest observation**, recorded either way — **and rules nothing**:
  OD-V3/OD-V5 are OWNER-GATED at KF.W10's owner block, *"never proxied."*
- **X·F**: the SS-13 residues named in F-W1-CARRY cross-edge 9 are carried **as a named class, not
  absorbed** — each stays with the record that names it and rides that record's own wave (F.W3/W4
  cures). F.W9's claim over them is **the instrument only**: a checkpoint that would catch their
  visual regression. **No id re-booked, none renamed.**
- **X·P**: n/a — no demo.
- **Shared-apparatus rule**: KF.W9 and **X-W11 G8** use shared apparatus at **SEPARATE cells at
  distinct coordinates**; neither consumes the other's captures (*"all five corroborate G8's
  premise; NONE clears it"*).

### §4.3 The FORMATION-BOUNDARY packets — 4 terminal, riders booked id-for-id

From `REFINEMENT-FOLD-2026-08-28.md` §1 (**15 HOMED · 4 FORMATION-BOUNDARY**; W-HYGIENE counted
HOMED with one limb at the boundary). **Every FORMATION-BOUNDARY booking names the G-F register as
its landing and the sitting as its elector.**

| packet | what carries forward |
|---|---|
| **PRE-X MT-REGISTER** (the 2026-07-27 cohort) | the per-record MT-*→X mapping; the same cohort as §4.1's zero-residue gap |
| **BOUNDARY-SCOPE** (AboutPane AB-4/AB-5) | X-W6's fold books them; disposition is the boundary's |
| **AUTH-SESSION SUBSTRATE** | X-W3-FOLD F-4 banks AF-50 NO-WAVE-OWNER |
| **MIGRATE-DIALOG IDENTITY-FLOW** | behind the **OWNER-DECISION gate** |
| **W-HYGIENE**, one limb | the 8 records that route it in; *silence is not adoption* |

### §4.4 The G-F residuals — 3, booked not waived ⟨`union/G-F-ADJUDICATION.md` §5⟩

**G-F is MET WITH RESIDUALS**; X-whole may stamp SPECIFIED against §3.3. The three are
**pre-execution duties on the X-W0 sitting**, not blockers:

1. **R-1 · the ApiOfflineChip transport cluster — MAJOR-bearing.** Seven rows (AP-12 · AP-17 ·
   AP-24 · AP-29 · AP-30 · AP-31 · AP-33) absent from the entire X·V fold layer, triple-tested
   (`initApiEnvironment` → **0 layer-wide**; `baseUrl` → **0 layer-wide**). **AP-17** is a
   CONFIRMED MAJOR — the cooldown gate admits an **unbounded burst** against docs promising *"ONE
   probe"* twice; **AP-12** is MAJOR (third parallel ownership of *"the backend is not
   answering"*). The surface is *known unclaimed and said so in writing* — `demo/platform/transport/**`
   has no X-wave claimant. **Sub-finding: COHESION §4's `×5` does not match the 7 rows measured.**
   ▶ Cure: book as **G-F slate entry 15** at `X-W0-FOLD.md`'s §8 idiom, carrying all seven
   ⟨record · id⟩ id-for-id, and correct `×5`→`×7` **in the addendum, never in §4's bytes**.
2. **R-2 · two missing `≡`-pointers** — ⟨AdminFlaggedPanel · AF-12⟩ (= the AdminListItem D-5
   identity, which *is* carried) and ⟨ColorNutritionLabel · R6⟩. **No identity is orphaned.** Cost:
   two lines.
3. **R-3 · the register's file home is unelected** — CLERICAL. §1 rules it moot: **the fold and
   canonical layers ARE the register of record** (identity-grain, not pointer-grain). ▶ The sitting
   either elects candidate A or ratifies §1; **the adjudicator recommends the latter.**

---

## §5 THE SEAT LAW

### §5.1 Model tiering — M-12 TRI-FOLD (current law; supersedes M-11 Opus-5-for-all)

- **Fable orchestrates.** The session root drives the sub-sessions; it does not do the toughest
  work itself.
- **Toughest work = Fable-worker + Opus-worker → fresh-Fable arbiter apotheosis.** Twice-authored,
  then agglomerated by a *fresh* seat (M-23 §1). This is the idiom every design wave and every
  adjudication in tranche X was authored under, and it is the idiom execution inherits.
- **Opus solo for mechanical/challenge work.** Censuses, greps, gate runs, single-file cures.
- **Receipts every seat.** Line-1 served-model declaration; ⟨…⟩ provenance pointers; dated
  corrections **appended, never rewritten**.
- **Tier is declared per spawn.** `CLAUDE_CODE_SUBAGENT_MODEL` was REMOVED 2026-07-19 after
  silently forcing all-Fable; mid-session settings edits do not reach a running session — **probe
  the served tier**. Aliases, never pinned ids.
- **MAX 4 CONCURRENT WORKFLOWS** (owner cap 2026-07-24). This **overrides** ultracode/commissioned
  scale. Rationale, and it is a correctness argument: **a killed jury seat reads as a clean
  component (NO_JURY), so wide fan-out under a rate wall manufactures false negatives.**

Per-wave seat counts are declared in each spec's §State `**Agents**` line and are binding
(e.g. X.P.W0 = 4 across 3 phases; X.P.W2 = 9 across 6; F.W9 = 3 **serial, never parallel** because
units 2 and 3 both write `web/package.json`).

### §5.2 Probe parsimony (owner edict 2026-07-12)

**Parsimonious Playwright / DevTools-MCP; fastidious design and code analysis.** Every wave's LAW
block carries this. Concretely for execution:

- Shared-capture families are declared so **no probe is double-spent** — KF.W9 S-8 enumerates six
  families (the three-rect probe ONE mount; the composited cartoon plate ONCE; one 390×844 +
  375×667 mobile pass; one WHC pass; ONE iOS device session; ONE AT pass).
- **Discriminators and falsifiers are stated per packet**: a capture of one transport home, or of
  either home at one viewport only, **does not satisfy** the OD-V3 packet — *the ask is a
  comparison.*
- **webkit-engine and safari-app are separate evidence cells; never infer one from the other**
  (glass's law, adopted verbatim 2026-07-27).
- Live probes are **bounded**; SS-8/SS-13 evidence packets are **keyed to apotheosis row ids**, not
  free-form screenshots.

### §5.3 The mail law (E13)

**Every wave OPENS with the four-path sweep.** New mail gets a row in
`docs/tranches/V/coordination/INBOX.md` (status UNREAD) **BEFORE any other work**. **No wave closes
with UNREAD mail addressed to its scope.** Mail is MARKED, never merely remembered — relay-received
is never terminal (D37): a letter counts only once rowed.

The four paths, verbatim:

1. `docs/tranches/V/` + `docs/tranches/V/coordination/`
2. `../glass-ui/docs/tranches/BK/coordination/` — **the ACTIVE tranche's dir, not a pinned letter.**
   *The stale BI pin cost 19 days on the 08-09 letters → I-28/I-29.* It has now cost a second
   letter: the 2026-08-29 O-20 ACK, unrowed at this boundary (§3.3).
3. `../keyframes.js/docs/tranches/V/coordination/` — their `<SENDER>-INBOUND-*` grammar; **the
   tranche root is NOT a mail path.**
4. `../sci-report/atlas/docs/tranches/P/coordination/`

**NO CRONS** (owner order 2026-07-17). Vigilance rests entirely on the wave Step-0 sweeps plus the
on-disk ledger plus user memory. **And on the delivery-path lesson (I-26): a letter that lands
untracked in a frozen checkout is NOT delivered — the path, not the content, was the defect.** That
is precisely what KF.W1 exists to cure, and why no kf-side wave may consume O-8/O-11 obligations
until it lands (COHESION §2).

### §5.4 Commits

- **Pathspec commits only.** `scripts/dev/dev.sh` (value.js) is **never staged** — unowned, dirty
  by standing arrangement, NEVER touch (restated at F-W0 §6a lock 9).
- **Commit families that must not split** are declared per wave and are binding: KF.W2's façade +
  its first Tier-A repoint; each Tier-C deletion **with** its replacement call; F.W1's single
  atomic `feat(fourier)!:` commit; KF.W8's `git mv` + four import repoints + the docblock, one
  commit; the RE-CUT-0 paired edit.
- **One commit per meaning** (KF.W3 L-7): *"a retirement folded into a refactor hides a behaviour
  change."*
- **E-3 everywhere**: censuses and lane corrections are **addenda, never patches**; the dated specs
  and the adjudicated registry are immutable beside the specs that cite them.

### §5.5 Read-only laws for sibling trees

| tree | law until its wave opens |
|---|---|
| `../keyframes.js` (the sacred checkout) | **READ-ONLY.** The `git fetch && git reset --hard origin/master` is **the OWNER'S HAND** — `lane-docs.md:380`: *"Blocked from our side by lane law and by their sacred-checkout rule — value.js must NOT perform it."* **A wave that performs the reset itself, or opens without the begin-word, re-runs the schism it exists to close.** All KF.W0-era probes are read-only `git` commands. |
| `../fourier-analysis` | **READ-ONLY** throughout the X·F conformance loop and until F.W0 opens. **Any producer-tree WRITE is a halt condition** — producer defects leave via NWO-1, never as frontend hacks. **No `git stash` anywhere in F.W0** — a stash would erase rows 24/25, the tree's only copies of two correct repairs. |
| `../parse-that` | **READ-ONLY, permanently for this program.** X·P writes only in the **fresh root** (`parse-that-css-totality-p2`, cloned from `f5757082…`), with the shared repository **provably untouched** (G-3/G-5: `.git/objects` shares **zero inodes**). |
| `../glass-ui` | **READ-ONLY, always.** Producer rows ride **SS-6** and **NEVER become frontend hacks** — one rule, four ends, one spelling (SEAMS S-17, CONSISTENT). *"Do not patch locally"* is the producer's own explicit instruction. |
| value.js `src/`, `demo/`, `api/` | **READ-ONLY until the begin-word.** Nothing in any sub-tranche opens product source before it. |

---

## §6 WHAT CONFORMANT MEANT — one paragraph per sub-tranche

**X·V** ⟨`REFINEMENT-FOLD-2026-08-28.md` §ROUND-4, verdict 2026-08-28, banked `c88e1fed`⟩ — The
fresh hostile pass-4 check (`refinement/PASS-4-CHECK-RETURN.json`) returned **CONFORMANT with zero
defects above MINOR-with-mitigation**, all three residuals in the declared one-round-lag class. The
fold layer carries **1,890 rows** across the twelve per-wave fold files (W0 37 · W1 56 · W2 22 ·
W3 27 · W4 202 · W5 88 · W6 530 · W7 638 · W8 108 · W9 46 · W10 110 · W11 26 — **row bands verified
gapless by full enumeration**), 19 packets terminal (15 HOMED + 4 FORMATION-BOUNDARY), the six I-28
glass-8.0.0 external rows minted as X-EXT-1..6, 22 bounds convictions answered + 44 delegated, and
§READINESS stated planning-only. *"The census loop that opened at 262 escapes (pass 2) closed by
monotone strata: 84 → 123/124 → residue ∅. No defect names a missing row, a false booking, a broken
lock, or an E-3 breach."* **E-3 was falsifiable and held**: `git diff --stat HEAD --
docs/tranches/X/waves/` prints nothing; the twelve dated specs are byte-identical.

**X·KF** ⟨`keyframes/conformance/VERDICT.md`, 2026-08-29, banked `3c4807d0`⟩ — Pass 8 returned
**CONFORMANT**: hashes clean against `PASS-7/SEAL.md`, **census escapes ZERO** (held from pass 7),
every pass-7 register item closed **at the bytes**, all residuals MINOR-with-mitigation (headed by
the RULINGS-6 spec-side reachability gap, cured from the certificate layer by CLOSE-CERT-6 §10's
instrument enumeration; each carries its named one-command cure). Eight passes: authoring →
122-defect pass 1 → seven repair rounds under an accreted law set (REGISTRY-FIRST · quote-by-command
· LAW A import-graph · LAW B closure-claim ban · stable anchors · staged-repair barriers · **LAW E
hash-proven close** · LAW F shadows · the false-universal ban · the surgical work-order idiom).
Convergence: carriage 91 → 97.2 → 97.8 → 98.2% → zero-escape; **double-homings 6 → 0**; the
machinery layer died in the lean rounds 6–7.

**X·F** ⟨`fourier/conformance/VERDICT.md`, 2026-08-30⟩ — Pass 15 returned **CONFORMANT**: hashes
clean against `PASS-14/SEAL.md` with the canonical **FROZEN at `f44362757458`, seventh consecutive
round**; **roster escapes ZERO / fabrication ZERO, eighth consecutive pass on both**; a three-item
tail entirely below the bar with stated mitigations (P15-1 MINOR, P15-2 LOW, P15-3 INFO). Fifteen
passes over eleven specs and a 66-record corpus — the longest of the three loops and the one that
minted the most law, all of it now binding on every successor round anywhere in X: **the census
freeze** (`CENSUS-CANONICAL.md` the sole operand; a gate citing any other is defective on sight) ·
**the star-topology pin certificate** (eleven mutually-pinning files have no fixed point; one
certificate pinning eleven does) · **strictly-sequential convergence rounds** · **spelling-agnostic
numeric sweeps** · **the self-count law** · **authority-list-first numeric reconciliation** ·
**write-then-measure** · **measure-don't-inherit closure** (*"the certificate was righter than the
orchestrator's briefing, and the pass-14 check said so on the record"*).

**X·P** ⟨`parse-that/REFINEMENT-FOLD-2026-08-06.md`, CONFORMANT pass 10, banked `08232cd3`⟩ — The
fifteen module apotheoses (`pt-ansi … pt-utils-entry`, set completed at `1a3e9af1`) are folded onto
the five wave specs as **one dated addendum**. Post-repair truth at pass 10 is **UNCHANGED from
pass 8**: **148 FOLDED · 16 EXCLUDED** (133 per-wave + 15 MEASURE + 16 excluded = **164
bookings**), the partition `42 · 16 · 72 · 3 = 133`, 133 contiguous row headers (W0 1-2 · W1 1-23 ·
W2 1-80 · W3 1-12 · W4 1-16, no duplicates), **ZERO unresolved pointers**. **This pass minted no
row, removed none, split none, merged none, re-homed none and trimmed none** — its two repairs were
claims the file made *about itself*: a count word beside its own enumeration, and a figure that
went stale the instant a pass minted the row superseding it. Its lesson is now program law: **a
pass that moves the arithmetic must re-grep its own arithmetic tokens before it banks.**

---

## §7 CLOSING POSTURE

**Thirty-nine authored waves, four tracks, twenty-three born-RED first gates, twenty-four
adjudicated seams, three named residue programs — all `planned`.** No four-verb row moves in this
file. **No VERIFIED stamp is issued here in any voice.** No product source was opened, no sibling
spec edited, no COHESION section written, no mail row added. The nine seam cures and the three G-F
residuals are **stated, not executed**, per E-3, each naming the file that owns its end so the
owner can direct them one at a time or as a batch.

**Two acts are owed before the first wave opens, and neither is a wave**: (i) **row the 2026-08-29
O-20 ACK as I-30** (§3.3) — a ledger act; (ii) **the X-W0 opening sitting** elects the L1/L5
disposition (§1.5 E-2), the Glass-8/9 census target (§3.2), the G-F register home (§4.4 R-3), and
receives F.W5's flagged owner rulings (§1.5 E-3).

**The owner's begin-word remains the only execution key.**
