SERVED MODEL ID (runbook author A, line-1 declaration): claude-fable-5[1m]

# X-WHOLE EXECUTION RUNBOOK — DRAFT A (independent)

**Authority**: `COHESION.md` §0f (all three loops closed; the union pass — seams · G-F · this
runbook — is the sole remaining artifact) + §2 (the cross-sub-tranche dependency graph) +
`union/SEAMS.md` (24 seams, 15 CONSISTENT / 9 CONVICTED) + `union/G-F-ADJUDICATION.md`
(G-F MET WITH RESIDUALS). Authored by runbook seat A, 2026-08-30, read-only over the four
develop-complete spec layers.

**Posture — read first.** **Status: `planned` EVERYWHERE.** This is a planning artifact. It
schedules nothing; it states the order the specs themselves declare so that the owner's begin-word
lands on a stated picture instead of a re-derivation. **Nothing here opens product source. Zero
VERIFIED stamps are issued in any voice. E-3 governs: this file is an addendum beside the specs;
no sibling spec, no COHESION section, no conformance artifact, no mail ledger is edited by it.
The owner's begin-word is the only execution key.**

**The operand set** (close states per `COHESION.md` §0c/§0e/§0f/§0a):

| sub-tranche | waves | close state |
|---|---|---|
| X·V (value.js) | `waves/W0..W11.md` (12) + `REFINEMENT-FOLD-2026-08-28.md` + `refinement/` | fold CONFORMANT pass 4 (`c88e1fed`) |
| X·KF (keyframes.js) | `keyframes/waves/KF-W0..KF-W10.md` (11; KF.W3 GATED) | CONFORMANT pass 8 (`3c4807d0`) |
| X·F (fourier-analysis) | `fourier/waves/F-W0..F-W10.md` (11) | CONFORMANT pass 15; canonical FROZEN `f44362757458` |
| X·P (parse-that) | `parse-that/waves/W0..W4.md` (5) | fold CONFORMANT pass 10 (`08232cd3`) |

**39 authored waves** sequence below; **KF.W11 · KF.W12 · KF.W13 are MINTED-UNAUTHORED**
(register of record `keyframes/waves/KF-W10.md` §6.D; authoring seat = the SS-1/SS-2 block;
mechanism F: a mint with neither an authored spec nor a register row is a hard escape on sight).

---

## §1 THE ORDER

Derived from the specs' own sequencing declarations: X·V `REFINEMENT-FOLD-2026-08-28.md`
§READINESS R.1 (the fold-amended order); X·KF `KF-W4.md` §Sequencing (the declared sequencing
head) + each wave's `Opens after`; X·F `F-W0.md` §6a locks + `F-W1.md` §4; X·P `W4.md` §10.
Every edge carries its dependency reason. The four sub-tranches live in four repos: **the four
tracks are genuinely parallel** except where the PLAW-BIND chain, the SS-6 mail channel, and the
owner's four-workflow cap serialize them (§5).

### 1.1 Stage A — the four openings (parallel across repos; each first in its own track)

| open | why first | owner items due AT this opening |
|---|---|---|
| **X-W0** (Formation: sitting · registers · census) | R.1: *"first, gates everything"*. The sitting receives the §3.e escalation slate, §EXTERNAL X-EXT-1..6, the glass-8 census X-W0.j, and the G-F R-1 booking (§4.4 below) | G-9 disposition (the X·P adoption surface, SEAMS S-4 — A / B / C); the SEAMS §8 cure batch if the owner directs it; the S-11 glass-9.0.0 widening |
| **F.W0** (Re-ground the substrate) | F-W0 §6a lock 1, verbatim: *"F.W0 substrate pre-gates FIRST … G-4 · G-1 · G-13 · G-12 precede F.W1 SIZING"* — *"F.W1 would open on a red build"* | OG-F1 (freeze-with-adoption vs re-root); G-15's four rulings; G-10's KEEP/ISOLATE/DELETE breath |
| **KF.W0** (Substrate Settle §B-12) → **KF.W1** (Mail Cure) | KF-W4 §0 OP-2: *"Hard precondition — this wave cannot open before it"* (every anchor re-resolves at `origin/master 81a56990`; local `8281638c` DISQUALIFIED). Then KF.W1: *"the mail cure precedes everything kf-side"* — no kf wave consumes O-8/O-11 obligations before O-21 lands (COHESION §2; I-26) | **OP-1 — kf write authority (owner-held; nothing kf-side dispatches until ruled)**; OP-6 (`smooth-step-3`) before KF.W4 unit `.e`'s G-14 arm |
| **X.P.W0** (Pause verify + fresh root) | X.P.W0 §6: the fresh root *"opens at X.P.W0's FIRST WRITE"*; G-1/G-2/G-3 authenticate the pause state before any parser work is lawful | OC-1 (bench bar) is RC-P conjunct 5's subject — may be ruled any time before X.P.W4 evaluates; the separate **release word** (X.P.W4 OP-1, handoff §8.8) |

### 1.2 The X·V track (order per §READINESS R.1, as amended by the fold)

```
X-W0 ── first, gates everything
  ├─ X-W1  harness (R2 dead-locator census + R3 oracle dialect BEFORE the HARD flip)
  ├─ X-W2  boot cost ──blocks── X-W5  shell composition (bindPane escalation resolved
  │                                    AT THE SITTING before open; PSC deletion +
  │                                    C1 successor same-wave)
  ├─ X-W3  api (route-spec-first law a.4; AdminGate seam contested with W7)
  ├─ X-W9  library (epoch rule; widened R1 battery inside commit 1)
  ├─ X-W4  dock/shell actions (A-1-before-A-3; .g cells held by the 0/4 trigger;
  │        MP-3 same-change guard)
  ├─ X-W6  scenes/workbenches (opens against its fold; write-order with W7 on
  │        4 shared paths — W7's CrossEdges state the order)
  ├─ X-W7  palettes/admin (N-1 mount substrate = W1's plugin-vue install, so W1 first)
  ├─ X-W10 canon (riders cite-don't-fork)
  ├─ X-W8  subtraction — LAST of the demo waves ("it deletes what W4/W5/W6/W7 rewire")
  └─ X-W11 release + verified close — consumes everything
```

Surviving cross-wave edges (R.1, verbatim reasons): **W2 → W5** (blocks; W2's AFTER.json
non-terminal until W5's A1/A2 land) · **W6 ⟂ W7** on four shared paths · **W5's C3 may not close
while Dock G-L (an X-W8 probe) is RED** — the one back-gate from the subtraction wave · **X-W8
last among demo waves** · **X-W11 last**.

### 1.3 The X·KF track

```
KF.W0  substrate settle (§B-12)          ── hard precondition of everything kf-side
KF.W1  mail cure (mints O-21)            ── "precedes everything kf-side"; anchors citable after
KF.W4  K-quartet + gate chassis          ── THE SEQUENCING HEAD: "No repair packet and no UNIT
                                            may open before G-KFW4-1" (KF-W4 §Sequencing,
                                            eight banked records state it independently)
  ├─ KF.W2  parse façade   ∥  KF.W5  D/L/C library audit+repair   (library half;
  │         KAD-1 front-loaded at W5 — "the fix blocks nothing … land it first")
  ├─ KF.W6  glass suffusion (demo) — W4's two atomic bundles (KF-AT-4, KF-AT-10)
  │         "land as the first commits of KF.W6"
  ├─ KF.W7  timeline evaluate — "the KF.W4 vue-tsc gate precedes verification of
  │         every typed cure here" (W4 §Cross-edges); W4's manifest edit precedes W7's
  └─ KF.W8  structure/colocation — Opens after KF.W0 · KF.W4 · KF.W5, + KF.W6 SCOPED
            for G7's measurement only (KF-W8 §State); three W8 gates wait on W4's commit 2
KF.W9  Safari visual audit — Opens after KF.W0 ONLY ("W4 is not a precondition — this wave
       measures, it does not type-cure"); PRECEDES KF.W10 (six anchors, A1..A6)
KF.W10 fold discharge + close — Opens after W0 · W1 · W9/SS-13 · and W2·W4·W5·W6·W7·W8·W9
       IMPLEMENTED; stamps the AUTHORED ELEVEN only
KF.W3  parser consumption — GATE-KEYED, NEVER SCHEDULED: "opens if and only if RC-P(V)
       evaluates TRUE per RELEASE-CONDITION.md §6a, run against the registry coordinate V"
KF.W11/12/13 — MINTED-UNAUTHORED successors (cargo = the 17-packet partition 9+6+2 with the
       six travelling locks); AUTHORED by the SS-1/SS-2 block before any packet executes;
       all 17 packets sequence after G-KFW4-1; the drag-seam and transport/ribbon packets
       sequence AFTER KF.W7's per-surface SWAP verdict (the KF-AV-28 standing rider)
```

### 1.4 The X·F track

```
F.W0  re-ground the substrate — pre-gates G-4 · G-1 · G-13 · G-12 precede F.W1 SIZING;
      G-4 gates G-5 gates the web/dist quarantine; G-1 gates G-11 gates G-10's lift;
      G-3 gates the P-1/P-6 SENDs; G-6 is authoring+measurement ONLY (the declare+lock
      landing is F.W1's); G-10 lift-then-delete ONE commit; no git stash anywhere
F.W1  the atomic tri-package uplift — ONE land-or-lose transaction, "the TWELVE-limb
      roster chartered at F-W1 §4 Sequencing, intra-wave step 4, cited whole and never
      restated" (glass 4→7(→8, G1/ESC-1 owner-gated) ∧ keyframes 4.3→6 ∧ value 0.13→4.0
      + Button rewrite + lucide rename + FR-EQC-7 vaul-vue gate + …); WU-A RE-PIN is the
      first obligation — "nothing below sizes before it"; a partial landing is the
      land-or-lose failure the L-18 base forbids
F.W2  value.js consumption to spec ── after W1 (inherits F.W0's lattice; C·D-11's backward
      pin makes F.W2 un-landable alone)         ∥ parallel with:
F.W3 / F.W4  shadow retirement + suffusion / saturated frontend audit — "F.W1 precedes
      F.W3/F.W4 (COHESION §2 law) — no fourier frontend spec may assume the uplifted tree
      before W1 lands it"; FR-COB-17's 8-site aria-pressed lift homes at W3/W4 but is
      SEQUENCED WITH F.W0's git rm — same commit
F.W5  shared-provenance contract (ADMISSION KEYSTONE; SS-4 twice-authored, owner rulings
      FLAGGED INLINE — trie-vs-KISS et al.; TA-4 value-side atomdiff restoration is a named
      prerequisite or the contract re-scopes explicitly; neither side's edits gate the
      other's waves)
F.W6  provenance defect burn-down (library half)
F.W7  trie greenfield — OWNER-GATED ⊙; depends HARD on F.W0 (quotes G-11's anchor table +
      G-12's denominator table; "If F.W0 fails, the wave HALTS")
F.W8  CRUD union prototype (the horizon probe)
F.W9  deploy spine + evidence floor — G-F9-1 owns the unit-test FLOOR; F.W0's G-9 owned
      only the SEAT ("the seat is a precondition of the floor, never a down payment on it")
F.W10 coordination discharge + close — reconciles the boundary AGAINST the dispatched O-20
      packet ("it does not re-send it"); terminal disposition of every deferred row
```

### 1.5 The X·P track and the PLAW-BIND chain

```
X.P.W0  pause verify + fresh root  →  X.P.W1  harness/corpus/honest bench
  →  X.P.W2  dual-target algebra (twice-authored)  →  X.P.W3  totality + recovery to spec
  →  X.P.W4  PLAW-BIND integration + RC-P (depends on W3 all five artefacts · W2's Wasm
             artifact · W0's fresh root — X.P.W4 §10)
```

**The chain, exactly as jointly stated** (COHESION §2 + X.P.W4 §6a + SEAMS §2): X·P release
condition **RC-P(V)** → X·V L1/L5 adoption surface → packed value.js release → consumers.
**Direct parse-that→fourier is FORBIDDEN** (standing routing law; asserted absent by X.P.W4 G-7's
two-grep probe). Three execution facts ride this chain:

1. **KF.W3 opens iff `RC-P(V)` is TRUE**, evaluated by running the six conjunct commands
   (PUBLISHED · TOTALITY · EQUIVALENCE · ADMITTED · BAR-DISCHARGED · ROUTED) against the registry
   coordinate `V` — *"never by reading a status word in any document, including this one"*
   (X.P.W4 §6a). All six were FALSE at 2026-08-03 measurement.
2. **The chain's middle link is UNHOMED** (SEAMS S-4, CONVICTED at the spine): no X·V wave holds
   the parser adoption — X-W9 §5.g declines it and G31's falsifier forbids it in-wave. The
   disposition is **owner-owed at the X-W0 sitting**: G-9's A (successor X·V wave) / B (amend G31)
   / C (BLOCKED-ON + re-trigger). **No executor schedules KF.W3 or the F.W2 parser grants until
   the owner disposes G-9 and RC-P evaluates TRUE.**
3. **F.W2's packet-P-7 reciprocal is owed at the X·V end** (SEAMS S-6 cure: booked at
   `refinement/X-W9-FOLD.md` as received, before X-W9's battery closes).

### 1.6 The coupled events and trigger-gated cuts (binding at execution, COHESION §0a + R.3)

- **X-W0.j → X-W4.g** — the ONE atomic trigger-gated cut, **value.js-internal** (S-9 cure
  re-scopes the spine's universal): census PASS → X-W4.g discharges its 62 dispositions;
  FAIL (today: pin 7.0.0, census 1/4) → the rows join MX-CLUSTER and MUST have the G-F register
  to land in — *"which is why W0 runs first."*
- **X-W1 HARD flip** held by R2 + R3 (dead-locator census + oracle dialect first).
- **W6's 47 GV routes** — trigger converted: *"W6 opens against its fold."*
- **X·KF tree: TRACK-or-defer while the successor authoring runs; never ARCHIVE.**
- **The repair-arms-a-defect rows** (one spelling, verified S-21): fr-PaperSearchModal
  same-commit riders · **PAW-44/LAW-3** (*"restoration lands WITH-or-AFTER PAW-1 + PAW-30, never
  before"*) · **MPC-31 one-cut law** · **FR-MSP-6 two-channel lock**.
- Same-commit/one-act locks carried in-spec: F.W0 G-10 lift+rm one commit · KF K2
  delete-and-correct one commit · KC-34 before the KC-8/KC-9 keep-mounted repair ·
  G-KFW9-9's two-deletion pair is ONE act · X·V PSC deletion + C1 successor same-wave ·
  A-1-before-A-3 · MP-3 same-change guard.

---

## §2 THE FIRST GATES — born-RED, to run at begin-word, literal commands

All read-only. *"Running them at wave-open re-dates the baselines; a PASS on any of them before
its cure lands is itself a finding and goes to the sitting"* (§READINESS R.2). Each command below
was verified present at its cited spec coordinate by this seat, 2026-08-30.

### 2.1 X·V — the R.2 slate (REFINEMENT-FOLD-2026-08-28.md §READINESS R.2, r1-corrected forms)

1. **X-W0 G-F (the register)**:
   `ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | xargs grep -c 'NO-WAVE-OWNER' | grep -v ':0' | wc -l` → **73**; RED until the set-difference is ∅ both ways.
2. **X-W0.j Glass-8 repin census (the X-W4.g trigger)**:
   `node -p "JSON.parse(require('fs').readFileSync('node_modules/@mkbabb/glass-ui/package.json','utf8')).version"` → **7.0.0**; census **1/4**, FAIL branch live. *Execution note: run under S-11's widened option set — 8.0.0 AND 9.0.0 (9.0.0 tag-pinned, registry-absent per the unrowed ACK).*
3. **X-W9 G2/NG-2 (the R1 throw class)**:
   `node -e 'import("./dist/subpaths/css.js").then(m=>m.parseCssColor("oklch()"))'` → **TypeError**; the battery must widen past the nine colour functions (`<any-ident>()`).
4. **W-HYGIENE H-c falsifier**: the JSONC-stripped 10/7-key drift probe (R.2 item 4) → **`10 7`**; cure = DELETE the inert block, never repair.
5. **X-W4 A4 witness**: `sed -n '81,87p' demo/shell/dock/layers/SlugEditLayer.vue` → placeholder-only accessible name. RED.
6. **X-W5 D4 witness**: `grep -c '<Transition ' demo/workbenches/mix/MixSourceSelector.vue` → **0** against the bare mode swap at `:114`. RED.
7. **X-W7 N-1 (mount substrate absent)**: `grep -rn 'mount(' test/ demo/ --include='*.test.ts' | wc -l` → 0; `grep -c 'plugin-vue' vitest.config.ts` → 0. RED.
8. **X-W1 R2 headline member**: `grep -rn 'plate-caption' e2e/ demo/` → live un-skipped spec asserting a class deleted at `a68ecdc1`. RED; `test.skip()` does not discharge it.
9. **X-W8 G-9 null-DELTA witness**: `sed -n '8p;22p' demo/palettes/browser/admin/PaginationBar.vue` — both lines `variant="outline"`. RED; cure collides with X-W7's quiet-migration reading (sitting item).
10. **The glass exports-map parity watch**: the `./blob ./chip ./forms` keyset probe (R.2 item 10) → GREEN-at-pin; **flips RED at the 8.0.0 repin (`./forms` dies — X-EXT-2)**, which is what it must catch.

### 2.2 X·KF (KF-W0 §B-12 · KF-W1 C-10/G-KF1-7 · KF-W4 §Gates)

1. **Substrate disqualification probe** (KF.W0 §B-12 / the round-5 receipt): `git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`; local `git status --porcelain | wc -l` → **252** at the stale worktree — every anchor re-resolves at `origin/master 81a56990` or the wave does not open.
2. **The O-21 MINT LAW re-measure** (KF-W1, quoted at SEAMS S-13): *"the executing seat re-runs the max-grep at open and, if the ledger has moved again, mints max+1 and records the delta as a dated addendum — a spec-frozen outbound literal is never trusted over the measured ledger."* First act of KF.W1.
3. **G-KFW4-1 (the sequencing head)** — born-RED witnesses (KF-W4 §Gates): `ls node_modules/.bin/vue-tsc` → **ABSENT**; `npx tsc --noEmit --listFiles` → **126 demo files, 0 `.vue`**. GREEN = `npm run check` redefined to `vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && npm run proof:structure` (the `proof:structure` tail SURVIVES verbatim) **and** the `demo/env.d.ts:3-7` `DefineComponent<{},{},any>` shim retired — *"a green run with the shim standing is the no-op this gate exists to prevent."*
4. **KF.W3's gate is a predicate, not a schedule**: evaluate `RC-P(V)` by its six commands only (§1.5); the repin literal is `4.0.0→V` per KF-W2's D-8 ruling (S-5 cure sweeps the three surviving `4.1.x` literals first).

### 2.3 X·F (F-W0 §4; the pre-gates that precede F.W1 sizing)

1. **G-4 — the corrupt dist (FR-NP-32 ≡ fr-PaperSidebar M1; cite both, never substitute)**:
   `grep -o '/\*' web/node_modules/@mkbabb/glass-ui/dist/styles/index.css | wc -l` → **17** vs `grep -o '\*/' …` → **8** (src is 15/6); repro with the app's toolchain: `postcss([@tailwindcss/postcss])` over `@import "@mkbabb/glass-ui/styles"` → `CssSyntaxError: Unterminated string: 's own'`. **GREEN only upstream — "A consumer-side patch is a GATE FAILURE."** The producer ACCEPTED the ask (the unrowed 08-29 ACK); the pre-gate stands at the adopted 4.0.0 bytes regardless.
2. **G-1 — the 28-path tree settles**: `git status --porcelain | wc -l` → **28**; every path ruled LAND / ABANDON / LAND-WITH-CORRECTION in `SUBSTRATE-LEDGER.md`; rows 24/25 MUST LAND; *"a wholesale reset is a gate FAILURE."*
3. **G-13 — the producer pin re-derived at the ADOPTED commit** (re-measure at wave open; the `207bf174` reading is dated): the pin cell is the COMMIT HASH, never the version string.
4. **G-12 — ONE corrected-denominator table published**; superseded figures FORBIDDEN downstream (e.g. the 8-spec e2e count; any 7-spec figure is dead).
5. **G-3 — the mail seat**: `ls …/fourier-analysis/docs/tranches/F/coordination/INBOX.md` → *No such file*; GREEN = ledger + `CLAUDE.md` exist, the three extant 2026-05-29 letters TRIAGED AT CREATION, P-1/P-6 logged SENT. **G-3 gates the SENDs.**
6. **FR-NP-32 before dist-dependent witnesses**: SS-13 residues *"BLOCKED, not merely unrun … gated on G-4/G-5 and must not be queued as independent visual work"* (F-W0 §6b).

### 2.4 X·P (X.P.W0 §6 — eight conditions, all RED at authoring)

1. **G-1 — handoff identities re-derived**: `shasum -a 256 docs/tranches/V/megatranche/coordination/PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` → `ced23440…f20f7`, plus the six sibling rows, each with the wave's OWN pasted output — *"a row disposed by quoting the handoff rather than by running shasum fails."*
2. **G-2 — EPERM is not absence**: the `~/Documents/Codex` rows report **unreachable**; any "the v12 target is absent" sentence unqualified by date + access state fails the gate.
3. **G-3 — eighteen roots, byte-unchanged**: `diff census-before.txt census-after.txt` empty over the enumerated preserved roots.
4. **W4 G-2 — zero value.js source bytes**: `git -C /Users/mkbabb/Programming/value.js status --porcelain -- src api demo test e2e` empty at every commit — *"the executable form of 'X·V owns its bytes'."*

---

## §3 THE SEAM PROTOCOL — the adjudicated seams as execution-time checkpoints

The register is `union/SEAMS.md` §1: **24 seams · 15 CONSISTENT · 9 CONVICTED**, every conviction
an addendum-beside with a named owning end (SEAMS §8), none executable before the begin-word.
Execution discipline, in order:

1. **Before ANY wave opens**: the nine cures of SEAMS §8 are dispositioned at the X-W0 sitting
   (or by owner batch-direction) — they are cheap, all additive, and six of them are one-ended
   edges that would otherwise be re-litigated mid-wave. Highest priority: **S-11** (row the
   2026-08-29 glass O-20 ACK as **I-30**; the 9.0.0 cut is tag-pinned, registry-absent) and
   **S-4** (the UNHOMED adoption leg → owner).
2. **The glass-8.0.0 repin decision point** — one decision, three ends, never merged:
   - **X·V**: X-W0.j census (R.2 gate 2) is the only lawful trigger; PASS emits X-W4.g's
     receiving-bounds delta. Under S-11's cure the census enumerates **8.0.0 AND 9.0.0** as
     candidate targets; §EXTERNAL X-EXT-1..6 keep their ids (X-EXT-1's successor fact:
     `./search` removed whole at 9.0.0).
   - **X·F**: the repin is **F.W1 G1/ESC-1, owner-gated** — the target-TAG + ADOPTED-COMMIT-HASH
     cell is EMPTY at authoring and every producer `file:line` in all 66 records must re-resolve
     at the adopted hash; F.W1's PIN-LAW gains the tag-vs-registry clause (S-11 cure) so an owner
     electing 9.0.0 knows it elects a tag.
   - **X·KF**: §B-12's 7.0.0 EXACT devDep stands; KC-5 is *"the only row arguing a producer
     major"* and is weighed against the pin before KF.W6 commits to either arm.
   - The spine's atomic-cut law is **re-scoped to value.js** (S-9 cure); each sibling declares
     the non-edge in one line so the absence is measurable.
3. **The O-21 mail obligations**: KF.W1 mints **exactly one** outbound id under the MINT LAW
   (max-grep at open; I-30 is inbound and does not move the outbound maximum — S-13's note).
   O-21 carries the O-8/O-11 amendment-addendum to an exec-visible keyframes path (HEAD anchors,
   the 3 missing `parseStylesheet` sites, the closed delivery-vehicle question, the D-GAP-6 line
   — I-26's cure). Four siblings cite the id as a vehicle; none re-mints.
4. **SS-6 is the only lawful cross-repo write path** (root-authored; X.P.W4 G-8: *"a wave that
   opens its own channel has substituted an ad-hoc letter for the batched-root law"*). F.W3's g20
   re-baselines against the dispatched O-20 by sha `f6e04c86ddb8` — cargo is the **residual**,
   the five dispatched items ride as status-notes (S-15 cure); F.W10 reconciles against the
   packet, *"it does not re-send it"*. The O-20 denominator stands at the packet's own **26**
   (22 id-rows ⊕ 4 §D entries — S-16 cure withdraws the false correction).
5. **BH-relay law, uniform at all four ends** (S-17): *"producer rows NEVER become frontend
   hacks"* — every producer-level finding accretes to the SS-6 register for the next boundary
   letter; the live inbox is `glass-ui/docs/tranches/BK/coordination/` (sweep the LIVE tranche
   dir, never a pinned letter — the stale-BI pin cost 19 days).
6. **Coordinate discipline**: stable anchors only into live siblings (§-heading + row/gate id;
   no line numbers — the class S-19/E-11/D3-3 convict); predicates, not version literals
   (`RC-P(V)`, never `4.1.x` — S-5); *"cite both, never substitute"* for FR-NP-32's canonical
   pair (S-23).

---

## §4 THE RESIDUE — what execution must carry that no wave gate discharges

1. **SS-8 (value.js visual audit; the UNPROVEN-NEEDS-LIVE slot)** — fold §4: **~751** residue
   items route to SS-8 (the real-Safari matrix task), *"never to X waves; a wave gate that claims
   one discharges nothing."* Heaviest feeds: picker-spectrumcanvas 17 · PreviewStrip 16 ·
   shell-dock-profilesection 16 · wb-gradient-visualizer 15. **THE ZERO-RESIDUE GAP**: fourteen
   records carry no §UNPROVEN section — *"SS-8 may not be called complete until this cohort is
   swept."* Epistemic statuses travel verbatim: UNPROVEN stays UNPROVEN, never upgraded by
   transcription. Probe parsimony governs; webkit-engine and safari-app are separate evidence
   cells.
2. **SS-13 (kf demo + fourier frontend visual audits)** — the kf surface is **KF.W9's own wave**
   (opens after KF.W0 only; every live/rendered witness is W9's; *"this wave may not claim a live
   receipt it did not run"*); the fourier surface is **gated on F.W0 G-4/G-5** — blocked residues
   are never queued as independent visual work (§2.3.6). SS-13 residues fold into neither the
   other's denominator (KF-W9 S-10).
3. **The FORMATION-BOUNDARY packets** — fold §1's count: **19 packets terminal = 15 HOMED + 4
   FORMATION-BOUNDARY** (W-HYGIENE split: counted HOMED, one limb at the G-F register). The
   four: **PRE-X MT-REGISTER** (the 2026-07-27 cohort) · **BOUNDARY-SCOPE** (AboutPane AB-4/AB-5)
   · **AUTH-SESSION SUBSTRATE** (AF-50) · **MIGRATE-DIALOG IDENTITY-FLOW** (behind the
   OWNER-DECISION gate). Every FORMATION-BOUNDARY booking names the G-F register as its landing
   and **the X-W0 sitting as its elector** — they are sitting agenda, not wave cargo.
4. **The G-F pre-execution duty (R-1)** — `union/G-F-ADJUDICATION.md` §5: the **ApiOfflineChip
   transport cluster (7 rows incl. AP-17 CONFIRMED MAJOR — the unbounded cooldown burst)** is
   booked as **G-F slate entry 15** at the X-W0 sitting, id-for-id, with the
   `demo/platform/transport/**` claimant question beside W0.22's `ErrorBoundary.vue` contention;
   §4's `×5` corrected to `×7` in the addendum, never in §4's bytes. R-2 (two `≡`-pointers) and
   R-3 (register file home) ride the same sitting. **Booked, not waived; not a blocker on the
   SPECIFIED stamp.**
5. **The minted-unauthored successors** (KF.W11/12/13) — their authoring is SS-1/SS-2-block work
   that must complete before their 17 packets execute; X·KF W10's close never stamps them
   (§0d/S-18). Silence at X·V/X·F/X·P is correct by construction (S-20).

---

## §5 THE SEAT LAW — who executes, and under what standing orders

1. **Model tiering (M-12 TRI-FOLD, current law)**: the session root (Fable) orchestrates;
   toughest work = Fable-worker ∥ Opus-worker → fresh-Fable arbiter apotheosis; Opus solo for
   mechanical/challenge seats; receipts every seat; served-model line-1 declarations on authored
   artifacts. Tier aliases, never pinned IDs.
2. **The four-workflow cap (owner, 2026-07-24)**: max 4 concurrent workflows, overriding any
   scale instinct — *"a killed jury seat reads as a clean component (NO_JURY), so wide fan-out
   under a rate wall manufactures false negatives."* The four parallel tracks of §1.1 fit the cap
   at one workflow each; intra-wave concurrency yields first (e.g. KF-W4's declared peak
   concurrency 2).
3. **The mail law (E13)**: every wave OPENS with the four-path sweep (`docs/tranches/V/` +
   `V/coordination/` · `../glass-ui/docs/tranches/BK/coordination/` (the LIVE tranche) ·
   `../keyframes.js/docs/tranches/V/coordination/` · `../sci-report/atlas/docs/tranches/P/
   coordination/`); new mail is ROWED before any other work; **no wave closes with UNREAD mail in
   scope**; NO CRONS (owner order). First sweep at begin-word rows I-30 (§3.1).
4. **Probe parsimony (owner edict 2026-07-12)**: parsimonious playwright/DevTools; fastidious
   design+code analysis; bounded live probes only where a rendered frame is the evidence
   (SS-8/SS-13/KF.W9); a nightly run colour is residue, never a gate input.
5. **Pathspec commits only**: `scripts/dev/dev.sh` is unowned and never staged (F-W0 §6a lock 9
   binds it cross-repo); each repair/wave banks its own pathspec commit.
6. **Read-only sibling laws**: every sibling tree is read-only until its own wave opens under its
   own authority — **kf write authority is OP-1, owner-held, unresolved**; the X·F tree was
   read-only through fifteen passes and stays so until F.W0 opens; X·P writes **zero** value.js
   source bytes ever (W4 G-2); the sacred-checkout rule and the ruling-1 substrate law
   (measure at `origin/master`, never a stale worktree) bind every seat. Pinned authorities are
   IMMUTABLE (epoch rule); frozen operands (`CENSUS-CANONICAL.md` @ `f44362757458`) are cited,
   never re-derived.
7. **The X·F program-laws bind every successor round anywhere in X** (§0f): census freeze ·
   star-topology pin certificate · strictly-sequential convergence · spelling-agnostic numeric
   sweeps · the self-count law · authority-list-first numeric reconciliation · write-then-measure
   · measure-don't-inherit closure.
8. **Escalation**: triumvirate triggers as each spec declares them (any write outside §Bounds ·
   any third diagnose→edit→re-measure iteration on one gate · any pressure to allowlist — never
   an implementer's decision); owner rulings are FLAGGED INLINE and never presumed (F.W5/SS-4;
   OP-6; OG-F1; ESC-1; G-9; OC-1).

---

## §6 WHAT CONFORMANT MEANT — one paragraph per sub-tranche

**X·V** (`REFINEMENT-FOLD-2026-08-28.md` §ROUND-4, `c88e1fed`; criteria at §READINESS R.4): the
fold layer reached CONFORMANT at pass 4 when the twelve dated wave specs remained byte-identical
(E-3 proven by `git diff --stat` empty), §0's 1,890-row arithmetic reproduced from the folds' own
blocks, all 19 NO-WAVE-OWNER packets were terminal (15 HOMED id-for-id + 4 FORMATION-BOUNDARY),
all 22 bounds convictions carried conviction+cure+owner, the X·P idiom held throughout, and the
posture stayed `planned` with ADJUDICATED never upgraded to VERIFIED. It is a statement about the
**spec layer's** completeness and honesty — not about the tree, which no gate has yet measured
at execution.

**X·KF** (`keyframes/conformance/VERDICT.md`, pass 8, `3c4807d0`): CONFORMANT = hashes clean
against `PASS-7/SEAL.md`, **census escapes ZERO held two passes running**, every pass-7 register
item closed at the bytes, all residuals MINOR-with-mitigation, each carrying its named
one-command cure. Eight passes: authoring → a 122-defect pass 1 → seven rulings/repair rounds
under the accreted law set (REGISTRY-FIRST · quote-by-command · the import-graph and
closure-claim-ban laws · stable anchors · hash-proven close · the false-universal ban), carriage
converging 91% → zero-escape. Status of every wave stayed `planned` throughout.

**X·F** (`fourier/conformance/VERDICT.md`, pass 15): CONFORMANT = hashes clean against
`PASS-14/SEAL.md` with the canonical FROZEN at `f44362757458` seven consecutive rounds, **roster
escapes ZERO / fabrication ZERO for eight consecutive passes**, every pass-14 register item
closed at the bytes, and a three-item tail (1 MINOR · 1 LOW · 1 INFO) entirely below the bar with
stated mitigations. The longest loop of the three and the one that minted the most law (§5.7);
the fourier tree was READ-ONLY throughout and every repair round banked its own pathspec commit.

**X·P** (`parse-that/REFINEMENT-FOLD-2026-08-06.md`, CONFORMANT pass 10, `08232cd3`): the
refinement fold folded the 15-module adjudication corpus into the five wave specs with per-row
limb treatment (the DECLARED TWO-LIMB idiom), closing its loop at pass 10 with the wave layer's
own laws intact — the fresh root uncreated by design, the Plane B bench bar OWNER-GATED (OC-1),
and the release condition RC-P left as a predicate that only registry-coordinate measurement can
flip. Its idiom (line-1 served-model declaration · ⟨cmd⟩ provenance · dated corrections appended
never rewritten) became the template the other three layers were held to.

---

**Close.** 39 authored waves, four parallel tracks, one serialized chain (PLAW-BIND), one mail
channel (SS-6), one trigger-gated cut (X-W0.j → X-W4.g), one gate-keyed wave (KF.W3), three
minted-unauthored successors, nine seam cures, four boundary packets, and one pre-execution duty
(G-F R-1) — all of it **planned**, none of it moving until the owner says begin.
