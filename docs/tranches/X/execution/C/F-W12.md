SERVED MODEL: claude-opus-5-5[1m]

# X·F F.W12 — execution record (Track C)

Spec: `docs/tranches/X/fourier/waves/F-W12.md` (48 lines, read whole). Authority: COHESION §0as (minted 2026-09-22), §0ao (owner fourier docket), under the 2026-09-17 begin-word. Tree: `/Users/mkbabb/Programming/fourier-analysis` `m/w1-bump-migration`, baseline `41e49af`.

## Open

Date: 2026-09-22 (execution opened under the 2026-09-17 begin-word). Seat 0, `claude-opus-5-5[1m]`.

**Crash-recovery.** `git -C fourier-analysis status --porcelain` → 22 rows: 21 × `docs/tranches/J/audit/screenshots/before/*.png` + `?? .worktrees/` — none inside F.W12 §1 Bounds; no inherited partial work. value.js dirty rows are sibling-owned (Track A/B e2e + evidence, `dev.sh`) — none in this wave's set.

**Preconditions ("Opens after: F.W11 CLOSED (CONFORMANT, fourier `41e49af`)").**
- Ledger: ⟨`grep -n "^| F.W11 " LEDGER.md | cut -c1-80`⟩ → `| F.W11 | F.W10S CLOSED … | **CLOSED 2026-09-17** ⟵ **CHECK 2 (RESUME 2, L-20 pass 2) CONFORMANT` — MET.
- Bytes: ⟨`git -C fourier-analysis log --oneline -1`⟩ → `41e49af fix(web): X.F.W11 R-close-1 — Edit contour rides GlassDock's persistent-end slot …` — HEAD = the spec's baseline — MET.
- Producer: ⟨`grep '"version"' web/node_modules/@mkbabb/glass-ui/package.json`⟩ → `"version": "8.0.0"`; exports map carries `"./number-field"`; `glass-ui/src/components/number-field/` = NumberField.vue · NumberFieldInput.vue · NumberFieldStep.vue · context.ts · index.ts · styles.css — the cure's producer surface SHIPS (§0as) — MET.

**E13 Step-0 mail sweep (four paths, read-only).** BK is still the newest glass tranche dir (⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → BK · BJ · BI). ⟨`find <path> -maxdepth 1 -type f -newermt "2026-09-22 00:00"`⟩ → value.js `V/`: EVIDENCE.md · VISUAL-CONSTITUTION.md · OPTICAL-BENCH-COMPOSITIONS.md (Track A working docs, not mail) · `V/coordination`: INBOX.md · BK/coordination: `glass-outbound-2026-09-22-consumers-10.0.0.md` (12 INBOX hits) · `fourier-to-glass-2026-09-17-nwo1-bh-relay.md` (9) · `value-to-glassui-2026-09-DD-fw4-relay.md` (10) — all already rowed · kf `V/coordination`: empty · atlas `P/coordination`: empty. **0 unrowed · 0 UNREAD in scope** (INBOX tail I-39). Sweep line appended to INBOX.md.

## Baseline

All commands read-only in `fourier-analysis/web` at `41e49af`, double-run (runs 1 and 2 identical).

| gate | command | BEFORE (×2) | verdict |
|---|---|---|---|
| G-F12-1 (imports) | `grep -c NumberField src/components/morph/HarmonicLevelGrid.vue src/components/morph/MorphPhaseConfig.vue src/components/visualization/BasisSelector.vue` | 0 · 0 · 0 → **0/5 sites** | RED (born-RED holds) |
| G-F12-1 (ArrowUp probe) | Playwright probe — not runnable before the cure (no NumberField to step) | — | RED by construction |
| G-F12-2a | `grep -c 'level-input' e2e/contrast-pairs.ts` | **5** (`:88` comment · `:109`/`:110` HLG-37 · `:146`/`:147` FMD-19) | RED |
| G-F12-2b | `grep -c 'AppHeader.vue' src/style.css` | **2** (`:307`, `:323`) | RED |
| G-F12-3 (grep) | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | **6** (`:43` comment · `:57` `:202` `:238` `:249` `:261`) · **1** (`:102`) | RED |
| G-F12-3 (two full 9-worker runs, `:290` executed) | not re-run at open — cite F.W11 Check 2 (RESUME 2) MINOR: `networkidle` wait skips `:290` under full-run load | banked | RED (cited) |
| G-F12-4 | `npm run build` | exit 0, `✓ built in 8.31s` (21.3 s wall) | build limb green; relay-row limb RED (no R-e-3 BK row) → compound RED |

Pasted:
```
⟨grep -c NumberField …⟩ → BasisSelector.vue:0 · HarmonicLevelGrid.vue:0 · MorphPhaseConfig.vue:0
⟨grep -n 'type="number"\|level-input\|num-input' …⟩ → BasisSelector.vue:164 type="number" · :190 type="number" · MorphPhaseConfig.vue:47 class="num-input …" · HarmonicLevelGrid.vue:26 / :47 class="level-input …"
⟨grep -n level-input e2e/contrast-pairs.ts⟩ → :88 // *"`.grid-cell` border … `.level-input` border 1.361 L … · :109 id: "HLG-37[level-input-border]" · :110 what: ".level-input 1.5px boundary …" · :146 id: "FMD-19[level-input-focus-border]" · :147 what: ".level-input:focus boundary …"
⟨grep -n AppHeader src/style.css⟩ → :307 Mirrors the canonical pattern at AppHeader.vue:174-177 · :323 … cured here and at `AppHeader.vue` /
⟨grep -n networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts⟩ → :43 (comment) · :57 · :202 · :238 · :249 · :261 · resolve-stack.ts:102 — all waitForLoadState("networkidle", { timeout: 60_000 })
⟨grep -rn 'type="search"' src/components⟩ → gallery/GallerySearchBar.vue:84 · gallery/AdminUserList.vue:405
```

**Open-seat findings for `.a` (bounds-reading, not re-rulings).** (i) G-F12-2a counts 5, not 2: the `:88` citation comment also names `.level-input`; the gate's literal `0` binds the whole file, so the `:88` comment is retired/re-worded with the two rows (same file, in §1 Bounds). The dated retirement line must not itself spell `level-input`. (ii) G-F12-3's literal `0` binds the whole of `visualization-ux.spec.ts` — six sites, not only `:33`'s `openWorkspace`; the file is in §1 Bounds. (iii) R-e-3 has two consumer sites (`GallerySearchBar` · `AdminUserList`); both are read-only here — the cure is ONE BK relay row, no consumer edit. **greenBeforeCure: none** (G-F12-4's build limb is a floor, the compound gate is RED).

## Unit plan

Serial, one concurrent (spec §State "Agents: 2 units — [`.a`] → [`.b`]"); both Opus (no Fable/design-author seat named). Push `m/w1-bump-migration` at every unit close (orchestrator note). glass-ui READ-ONLY; `package.json` untouchable (§1).

| unit | model | sections | writable | gates | locks |
|---|---|---|---|---|---|
| `F.W12.a` | opus | §2 `### F.W12.a` (:20-35) · §1 Bounds (:12-17) · §3 (:40-45) | fourier `web/src/components/morph/HarmonicLevelGrid.vue` · `web/src/components/morph/MorphPhaseConfig.vue` · `web/src/components/visualization/BasisSelector.vue` · `web/e2e/contrast-pairs.ts` · `web/src/style.css` (prose `:307` `:323` only) · `web/e2e/visualization-ux.spec.ts` · `web/e2e/resolve-stack.ts` · `web/e2e/screenshots/f-w12/**` · value.js record · `INBOX.md` (R-e-3 = O-51) | G-F12-1 · G-F12-2 · G-F12-3 · G-F12-4 | never a local number input styled as the producer's; never a longer timeout/retry loop; E-3 dated line beside for the retired rows; no consumer hack for the search glyph |
| `F.W12.b` | opus (verify-only) | §2 `### F.W12.b` (:36-37) · §3 all (:39-45) · §4 Close act (:47-48) | value.js record · `LEDGER.md` · `INBOX.md` | G-F12-1..4 ×2 (re-read) | honest-RED only with a relay id; verify-only (no code bytes) |

**Briefs.**
- `.a`: (1) R-e-1 — read `glass-ui/src/components/number-field/*` whole; move HLG `.level-input` ×2, MPC `.num-input`, BasisSelector `:164`/`:190` onto `NumberField` from `@mkbabb/glass-ui` (8.0.0 exports `./number-field`), preserving min/max/step/ids/labels/v-model; adjacent Slider stays; retire the now-dead local field CSS. (2) R-e-2 — retire HLG-37/FMD-19 rows + the `:88` comment's `.level-input` clause with a dated line beside (not spelling `level-input`); re-point `style.css:307/:323` to `AppDock.vue`. (3) R-e-3 — INBOX O-51 BK relay row + record letter section; no consumer edit. (4) replace every `networkidle` in the two e2e files with the app's readiness signal (mounted landmark / awaited compute response). Probe ArrowUp 5/5 ×2, screenshots to `e2e/screenshots/f-w12/`; two full 9-worker runs with `:290` executed. Pathspec commits; push.
- `.b`: verify-only close — re-run G-F12-1..4 ×2 at the settled bytes, bounds diff over `41e49af..HEAD`, E13 sweep, LEDGER row → CLOSED (or honest-RED with relay id), push.

## Unit receipts

### F.W12.a

Seat: `claude-opus-5-5[1m]` (served), 2026-09-22. Spec read whole (48 lines); record read header → Unit plan; COHESION §0as read (the §0as ruling is the one this unit consumes).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → the 21 `docs/tranches/J/audit/screenshots/before/*.png` rows + `?? .worktrees/` — none inside this unit's writable set; ⟨`git -C value.js status --porcelain | grep -E 'F-W12|INBOX'`⟩ → empty. No inherited partial work.

#### Letter — FW12-BK-1 (R-e-3), the relay O-51 carries

**To**: glass-ui (the producer; `glass-ui/docs/tranches/BK/coordination/`, the newest glass tranche dir at this clock). **From**: X·F Track C, F.W12 `.a`. **Kind**: producer observation, one row; no consumer byte answers it.

- **Observation.** glass-ui's `Input` (`@mkbabb/glass-ui/input`, 8.0.0) passes `type="search"` through to the native `<input>`, and its `field-control` recipe does not address the WebKit/Blink `::-webkit-search-cancel-button` pseudo-element. Chromium and Safari therefore paint their own clear glyph (a UA-styled ✕, off the producer's icon set, size rung and focus grammar) at the inline end of every search-typed `Input` once it holds text. Consumer sites: fourier `web/src/components/visualization/gallery/GallerySearchBar.vue:84` and `…/gallery/AdminUserList.vue:405`.
- **Ask.** Decide the glyph at the producer: either suppress the UA cancel button in the `field-control` recipe for `type="search"` and offer a producer clear affordance (the `./search` surface is the natural home), or document that the UA glyph is intended. Either answer is the producer's; fourier authors no `::-webkit-search-cancel-button` rule (spec §2 R-e-3: "no consumer hack").
- **Waits on it**: nothing in F.W12. glass-ui is READ-ONLY; no producer byte was written. Carriage rides the standing SS-6 boundary batch (the O-47/O-48/O-50 precedent).

#### Acts (in order)

1. **Producer read (R-e-1).** `glass-ui/src/components/number-field/*` read whole (index · context · NumberField · NumberFieldInput · NumberFieldStep · styles.css; 253 lines) plus `demo/stories/forms/number-field.vue`. `NumberField` wraps Reka `NumberFieldRoot` (min/max/step/formatOptions/`update:modelValue: [number]`); `NumberFieldInput` is the `role="spinbutton"` input on `field-control`, mono tabular centred; steppers are optional. Installed pin: `web/node_modules/@mkbabb/glass-ui` exports `./number-field`.
2. **Pre-move attributes at true bytes.** ⟨`git show 2435f78 -- …HarmonicLevelGrid.vue …MorphPhaseConfig.vue`⟩ → the native fields before F.W11 `.e`: Low `min=1 :max=highLevel-1 step=1`, High `:min=lowLevel+1 :max=maxLevel step=1`, Duration `min=50 max=800 step=10`; BasisSelector `:164`/`:190` still native `type="number"` (Harmonics 1/500/1 · Sample Points 128/4096/128, `aria-label`). All preserved.
3. **R-e-1 landed** — fourier **`a9fa5fb`**. Five `<NumberField>` mounts (HLG ×2 · MPC ×1 · BasisSelector ×2) with `<NumberFieldInput>` carrying the existing `:id` (HLG/MPC, so `<label for>` still names it) or `aria-label` (BasisSelector). Handlers take the committed number; the clamps stay the range authority (`NaN` from a cleared field folds to the floor as `Number("")`/`parseInt("")` did). `formatOptions { maximumFractionDigits: 0, useGrouping: false }` so `4096` reads and round-trips as `4096` (the persistence spec asserts `toHaveValue("2048")`). The adjacent Sliders are untouched. Dead local chrome retired: HLG `.level-input` rule → `.level-field { width; flex-shrink }`; MPC `.num-input` (width + right-align) → `.num-field { width }`; BasisSelector `.inline-number` (transparent fill, underline border + hover/focus paint, `outline:none`, zero padding, spin-button suppression, 3 rules) → `.inline-number { width: 4.5rem }`. No stepper buttons (the Slider is the pointer affordance); no local number input anywhere. ⟨`npx eslint` the three files⟩ → exit 0.
4. **R-e-2 landed** — fourier **`22ae0c1`**. `contrast-pairs.ts`: `HLG-37[level-input-border]` and `FMD-19[level-input-focus-border]` rows removed, each replaced by a dated `X.F.W12 .a (2026-09-22, COHESION §0as · R-e-2)` comment beside saying why (no spelling of the class); the `:88` audit quote elided to its grid-cell clause with a dated line naming the retired clause. `HLG-37[grid-cell-border]`, `HLG-37[grid-cell-fill]` and `FMD-18` unchanged. `style.css` prose only: the two citations re-pointed to `AppDock.vue`, with the retirement dated (X.F.W11 `.c`).
5. **Readiness (check-2 MINOR) landed** — fourier **`5b75cf0`**. `openWorkspace`: arms `waitForResponse` for BOTH `POST …/compute/epicycles` and `…/compute/bases` before the upload (what `ContourSettings.runCompute` awaits and renders from), asserts each 200, then the existing `.play-btn--mini` landmark. `/equation`: arms `POST /api/equations/compute` before `goto` (the `visual-checkpoint` precedent, `fd9d1f2`), asserts 200. `/paper` `/morph` `/demo/shape-extractor`: the wait is dropped; their mounted-landmark `expect` is the readiness (`/morph` also waits on the `Low` spinbutton, the control mass its comment names). `resolve-stack.ts` `openArm`: `expect(getByRole("main")).toBeVisible()` — `<main>` mounts after `main.ts` has imported every stylesheet. No timeout raised, no retry. Consequential edit in the same file: the save-contour guard's control snapshot selector `input[type="number"], input[type="range"]` → `input[type="number"], input[role="spinbutton"], input[type="range"]`, so the two BasisSelector readouts (no longer `type="number"`) stay pinned instead of silently dropping out.
6. **G-F12-1 probe + evidence** — fourier **`c5836a7`**: `web/e2e/screenshots/f-w12/arrowup-probe.mjs` + 10 PNGs (`run1-*`, `run2-*`; force-added, `web/.gitignore:1` ignores `*.png` — the `f-w11-b` precedent). The probe presses ArrowDown then ArrowUp on each spinbutton (High boots AT `maxLevel`, so ArrowUp alone cannot move it — first read `50 → 50` was that, not a defect) and requires both deltas = the field's step.
7. **R-e-3 relay** — value.js **`d40bf9d1`**: INBOX row **O-51** (glass-ui BK, SENT, SS-6 carriage) pointing at the letter above. No consumer byte on `GallerySearchBar`/`AdminUserList`.
8. **Push.** ⟨`git push origin m/w1-bump-migration`; `git rev-parse --short HEAD origin/m/w1-bump-migration`⟩ → `c5836a7` · `c5836a7`.

#### Gates (BEFORE → AFTER, each read twice at the settled bytes, fourier `c5836a7`)

| gate | command | BEFORE | AFTER (×2) | verdict |
|---|---|---|---|---|
| G-F12-1 imports | `grep -l 'from "@mkbabb/glass-ui/number-field"'` over the 3 files · `grep -hc '<NumberField$'` summed | 0/5 | 3 files · **5/5 mounts** · 5/5 | GREEN |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs 1` / `… 2` (dev stack :3100) | RED by construction | run 1 **5/5** · run 2 **5/5** (Low 5→4→5 · High 50→49→50 · Duration 150→140→150 · Harmonics 200→199→200 · Sample Points 1024→896→1024; `role=spinbutton` on all five) | GREEN |
| G-F12-2 | `grep -c 'level-input' e2e/contrast-pairs.ts` · `grep -c 'AppHeader.vue' src/style.css` | 5 · 2 | **0 · 0** · 0 · 0 | GREEN |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | 6 · 1 | **0 · 0** · 0 · 0 | GREEN |
| G-F12-3 full runs | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` | `:290` skipped in both Check-2 runs | run 1: **92p · 12f · 3s**, save-contour keystone (`:290` at 41e49af, now `:322`) **✓ executed**; run 2: **80p · 16f · 3s · 8 did not run**, keystone **not run** | **RED** (see below) |
| G-F12-4 | `npm run build` · E13 sweep · `grep -c '^\| O-51 ' INBOX.md` | build 0; relay absent | build exit 0 (`✓ built in 10.69s`, then `10.36s` at the settled bytes) · 0 UNREAD · **1** | GREEN |

Targeted, before the full runs: ⟨`npx playwright test e2e/visualization-ux.spec.ts e2e/settings-persistence.spec.ts --project=chromium`⟩ → **11 passed** (every keystone, the save-contour keystone, and both persistence round-trips through the new spinbuttons). `contrast-floor` + `slider-scrub-contrast` → the three `contrast-floor` rows fail exactly as in the `735319e` stable set (36 pairs now, was 38; the failing pairs are all banked `reproduced`/`DRIFT` rows, none from this unit); `slider-scrub-contrast` ×2 pass through the new `openArm`.

**Failure sets against the `735319e` stable 14** (F-W11 record §Baseline). Run 1: 12 = the stable 14 minus `visual-baseline equation @ 1280x800` / `@ 375x667` (passed this run; not claimed cured). Run 2: 16 = the stable 14 + `visual-baseline equation @ 1440x900` + `visualization-ux :109` (the old `:95`) — both in the banked run-1-only load-flaky six. **No failure absent at 41e49af** in either run.

#### ESCALATION — ESC-F12a-1 (G-F12-3 full-run leg): the spec's premise is half-right, measured

The spec (§2 item 4) names the cure: replace the `networkidle` wait with the app's readiness signal. That cure is landed and green on its own terms (grep 0 · 0; the wait now resolves on the compute pair, not the network). But the keystone still fails to execute in one of the two full runs, and the reason is **not the wait**:

- Run 2's `:109` (keystone 1, first in `test.describe.serial`) failed `Test timeout of 30000ms exceeded`; its failure screenshot shows the workspace fully rendered (epicycles drawn, dock mounted). The serial describe then skipped the other 8 tests, the save-contour keystone among them.
- ⟨diagnostic full run 3, `--workers=9 --trace=on`, NOT counted as a gate run⟩ → `:109` passed at 33.4 s; its trace, steps over 0.5 s: `goto /visualize` **7.7 s** · `setInputFiles` (upload) **7.6 s** · the compute-pair wait **19.9 s** from upload · axe **0.5 s**. In isolation (⟨`… visualization-ux.spec.ts:109 --trace=on`⟩ → 8.3 s) the same compute wait is **3.5 s**. In run 1 `:109` took **29.1 s**, on the 30 s edge.
- So under 9-worker load the dev server and the compute backend alone spend ~30 s of the test's default budget before the readiness wait can resolve. The old `networkidle` timeout was this cost surfacing at the wait; now it surfaces at the test budget.

The cures available are outside this unit's grant or its locks, so the seat substitutes none: (a) `test.slow()`/`setTimeout` on the workspace keystones = "a longer timeout" (lock); (b) dropping `.serial` so one slow keystone no longer skips the independent `:290` (the file's tests each open their own workspace; `ca58321` gives no reason for serial) = a harness change the spec does not name; (c) accepting `:109` as the banked load-flake with `:290` executed in the targeted and 2-of-3 full runs = a gate reading, not a byte. **Asks the orchestrator to rule** (a) / (b) / (c) or another route. The bytes landed here stand on their own either way: they remove the network dependence and keep every wait deterministic.

#### Residuals

- `docs/tranches/J/audit/screenshots/before/*.png` (21, dirty at open) are rewritten by every full run's `visual-baseline` capture; not in this set, not staged.
- `test-results/` from the diagnostic run (gitignored) left in place.

#### Commits

fourier `m/w1-bump-migration` (pushed, `c5836a7`): `a9fa5fb` R-e-1 · `22ae0c1` R-e-2 · `5b75cf0` readiness · `c5836a7` G-F12-1 evidence. value.js: `d40bf9d1` INBOX O-51 · this record (next commit).

## Close

Seat `F.W12.b` (verify-only), `claude-opus-5-5[1m]`, 2026-09-22. Spec read whole (48 lines); record read header → `## Unit plan` + `.a`'s receipt. Crash-recovery: fourier dirty = the 21 J-audit PNGs + `?? .worktrees/` (as at open; none in the writable set); value.js dirty rows are sibling-owned + `dev.sh` — none in this seat's set; no inherited work. Bytes: fourier HEAD = `c5836a7` = `origin/m/w1-bump-migration`.

### Gates (BEFORE at `41e49af` → AFTER at `c5836a7`, re-read ×2 by this seat)

| gate | command | BEFORE | AFTER run 1 · run 2 | verdict |
|---|---|---|---|---|
| G-F12-1 imports | `grep -l 'from "@mkbabb/glass-ui/number-field"'` ×3 files · `grep -c '<NumberField\b'` over the concatenation | 0 · 0 | **3 · 5** · **3 · 5** (HLG `:19` `:43` · MPC `:40` · BasisSelector `:182` `:210`; the one surviving `type="number"` string is BasisSelector `:240`, a comment) | GREEN |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs close1` / `close2` (dev stack :3100 → 200); output PNGs moved to the scratchpad, not committed (`.a`'s committed run1/run2 PNGs untouched) | RED by construction | **5/5 · 5/5** — Low 5→4→5 · High 50→49→50 · Duration 150→140→150 (step 10) · Harmonics 200→199→200 · Sample Points 1024→896→1024 (step 128); `role=spinbutton` ×5 | GREEN |
| G-F12-2 | `grep -c 'level-input' e2e/contrast-pairs.ts` · `grep -c 'AppHeader.vue' src/style.css` | 5 · 2 | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | 6 · 1 | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 full runs | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` | `:290` skipped (F.W11 Check 2) | run 1: **84p · 13f · 3s · 7 did not run** — `:109` ✓, `:122` ✘ `Test timeout of 30000ms exceeded` → serial skip, **`:322` (the old `:290`) NOT run**; run 2: **80p · 16f · 3s · 8 did not run** — `:109` ✘ `page.waitForResponse: Test timeout of 30000ms exceeded` → **`:322` NOT run** | **RED** (0 of 2 at this seat; ESC-F12a-1) |
| G-F12-4 | `npm run build` ×2 · E13 sweep · `grep -c '^\| O-51 ' INBOX.md` | build 0 · relay absent | exit 0 (`✓ built in 9.23s`) · exit 0 (`11.33s`) · 0 UNREAD · **1** | GREEN |

**Failure sets.** Run 1's 13 = `.a`'s run-1 twelve (contrast-floor ×3 · equation-interaction `:77` · fullscreen `:40` · gallery-admin-a11y ×4 · visual-checkpoint ×3 — a subset of the `735319e` stable 14) + `visualization-ux :122` (keystone 2 of the serial block, 30.5 s). Run 2's 16 = `.a`'s run-2 sixteen exactly (the 12 + visual-baseline `:57` ×3 + `visualization-ux :109`). Both visualization-ux failures are the same mechanism `.a` measured: the serial block's workspace keystones spend the 30 s default budget under 9-worker load before the compute-pair readiness wait resolves (run 2's error is `page.waitForResponse: Test timeout of 30000ms exceeded`), and `test.describe.serial` then skips the save-contour keystone. The readiness cure is landed; the keystone-executes leg is not reached.

### Bounds (landed-wrong check)

⟨`git -C fourier-analysis diff --stat 41e49af..c5836a7`⟩ → 18 files, all inside §1 Bounds: the 3 components · `e2e/contrast-pairs.ts` · `e2e/visualization-ux.spec.ts` · `e2e/resolve-stack.ts` · `src/style.css` (diff = comment lines `:307`/`:323` only — prose) · `e2e/screenshots/f-w12/**` (probe + 10 PNGs). No `glass-ui/**`, no `package.json`, no other spec. value.js: `d40bf9d1` = `INBOX.md` only · `7efcd0bd` = this record only. **Landed-wrong: 0.** Commit families unsplit (R-e-1 · R-e-2 · readiness · evidence, one meaning each).

### Commit roster

fourier `m/w1-bump-migration` (= origin): `a9fa5fb` R-e-1 NumberField ×5 · `22ae0c1` R-e-2 HLG-37/FMD-19 retired + style.css prose re-pointed · `5b75cf0` e2e readiness (networkidle → compute pair / landmarks) · `c5836a7` G-F12-1 probe + 10 PNGs. value.js: `d40bf9d1` INBOX O-51 (FW12-BK-1) · `7efcd0bd` `.a` receipts · this Close + the LEDGER row (next commit).

### E13

Four paths swept read-only (`find -maxdepth 1 -type f -newermt "2026-09-22 00:00"`): value.js `V/` → EVIDENCE.md · VISUAL-CONSTITUTION.md · OPTICAL-BENCH-COMPOSITIONS.md (Track A working docs, not mail) · `V/coordination` → INBOX.md · glass-ui BK (still newest: `ls -dt` → BK · BJ · BI) → `glass-outbound-2026-09-22-consumers-10.0.0.md` · `fourier-to-glass-2026-09-17-nwo1-bh-relay.md` · `value-to-glassui-2026-09-DD-fw4-relay.md` — the same three as at open, all rowed · keyframes.js `V/coordination` → empty · atlas → no coordination dir (as every prior sweep). **0 UNREAD.** O-51 row present (1).

### Escalations

- **ESC-F12a-1 — OPEN, unruled** (no ruling found: ⟨`grep -rln ESC-F12a-1 docs/tranches/X`⟩ → this record only). This seat re-measured it: 0 of 2 full runs executed the save-contour keystone (`.a` measured 1 of 2). The failing leg is not producer-owned, so §4's "CLOSED honest-RED with its relay id" does not apply. The routes stay as `.a` named them: (a) `test.slow()`/timeout = forbidden by the "never a longer timeout" lock; (b) drop `test.describe.serial` so one slow keystone cannot skip the independent `:322` = a harness change the spec does not name; (c) a gate re-reading. **Owner of the ruling: the orchestrator / COHESION (Track C).** This verify-only seat cures nothing.

### Residuals

- **G-F12-3 full-run leg** → owner: the ESC-F12a-1 ruling, then a repair unit in `web/e2e/visualization-ux.spec.ts` (Track C, fourier).
- **21 J-audit PNGs** (`docs/tranches/J/audit/screenshots/before/*`) rewritten by every full run's `visual-baseline`; dirty since before open, outside this wave's set, left unstaged → owner: Track C's J-audit evidence home (not F.W12).
- **FW12-BK-1** (UA search-cancel glyph) → owner: glass-ui BK via O-51; no consumer byte.
- The banked failing set (contrast-floor ×3, admin axe ×4, visual-checkpoint ×3, equation-interaction, fullscreen, visual-baseline load-flakes) → owners as banked at F.W11 / `735319e`; none new here.

### State

G-F12-1 · G-F12-2 · G-F12-4 GREEN ×2; G-F12-3 grep leg GREEN ×2, full-run leg **RED** (0/2). §4 closes only on G-F12-1..4 GREEN, or an honest-RED that is producer-owned. Neither holds, so F.W12 is **PARTIAL**, not CLOSED. Four-verb line: R-e-1 · R-e-2 · R-e-3 · readiness **IMPLEMENTED**. The keystone-executes criterion is **not met**, pending ESC-F12a-1. This seat stamps nothing VERIFIED: the spec designates no VERIFIED stamp for it.

## Check 1

Fresh adversarial L-20 pass 1, VERIFY-ONLY — `claude-opus-5-5[1m]`, 2026-09-22 (sitting of record 2026-09-17, the owner's begin-word). Authored no byte of the wave's cures, of `.a`'s receipt or of `## Close`; wrote **0** fourier, **0** glass-ui, **0** product bytes. Spec read whole (48 lines); record read header → `## Unit plan` + `## Close`; the four fourier commits read by `git show --stat` and the readiness diff (`5b75cf0`) whole.

**Crash-recovery.** fourier porcelain = the 21 J-audit PNGs + `?? .worktrees/` (as at open/close; none in this seat's set). value.js porcelain = sibling-owned e2e/evidence rows + `CARRY-LEDGER.md` + `dev.sh` + six `?? .r1-*.mjs` — none in this seat's set (record + LEDGER). No inherited work. Bytes: ⟨`git rev-parse HEAD origin/m/w1-bump-migration`⟩ → `c5836a7…` ×2 (pushed).

**Verdict: NOT-CONFORMANT — 0 BLOCKER · 0 CRITICAL · 1 HIGH · 0 MINOR · 3 INFO.** Every GREEN the close claims reproduces; the one RED the close left is NOT relieved by the spec's own §4, so the row stays **PARTIAL**.

### Gates reproduced (this seat's own commands, ×2, fourier `web/` at `c5836a7`)

| gate | command | run 1 · run 2 | verdict |
|---|---|---|---|
| G-F12-1 imports | `grep -l 'from "@mkbabb/glass-ui/number-field"'` over HLG · MPC · BasisSelector \| `wc -l`; `cat` the three \| `grep -c '<NumberField\b'` | **3 · 5** · **3 · 5** | GREEN (reproduces) |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs chk1` / `chk2` (dev stack :3100 → 200); output PNGs moved to the scratchpad, committed run1/run2 PNGs untouched (porcelain clean after) | **5/5 · 5/5** — Low 5→4→5 · High 50→49→50 · Duration 150→140→150 · Harmonics 200→199→200 · Sample Points 1024→896→1024; `role=spinbutton` ×5 | GREEN (reproduces) |
| G-F12-2 | `grep -c 'level-input' e2e/contrast-pairs.ts` · `grep -c 'AppHeader.vue' src/style.css` | **0 · 0** · **0 · 0** | GREEN (reproduces) |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | **0 · 0** · **0 · 0** | GREEN (reproduces) |
| G-F12-3 full runs | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` | run 1: **82p · 14f · 3s · 8 did not run**, EXIT 1 — `:109` ✘ `Test timeout of 30000ms exceeded` (at `openWorkspace` `:46`/`:47`, the compute-pair wait) → serial skip `:122`…**`:322` NOT run**; run 2: **76p · 17f · 3s · 11 did not run**, EXIT 1 — `:109` ✘ same → **`:322` NOT run** | **RED (reproduces; 0/2 at this seat, 0/4 across close + check)** |
| G-F12-4 | `npm run build` ×2 · E13 sweep · `grep -c '^\| O-51 ' INBOX.md` | exit 0 (`✓ built in 40.13s`, load avg ~97) · exit 0 (`12.06s`) · 0 UNREAD in F.W12 scope · **1** | GREEN (reproduces) |

**gatesReproduced = 5 of the close's 5 GREEN limbs** (G-F12-1 imports · G-F12-1 probe · G-F12-2 · G-F12-3 grep · G-F12-4); the RED limb reproduces as RED. Failure sets: run 1 = the close's banked set (contrast-floor ×3 · equation-interaction `:77` · fullscreen `:40` · gallery-admin-a11y ×4 · visual-checkpoint ×3 · visual-baseline `:57` ×1) + `visualization-ux :109`; run 2 = the same + visual-baseline `:57` ×3 + `paper-performance :158` (a load flake at load avg ~97; file untouched by this wave — INFO I-2).

### Axes

1. **GREENs reproduce** — all five (table above).
2. **Bounds** — ⟨`git show --stat` `a9fa5fb` · `22ae0c1` · `5b75cf0` · `c5836a7`⟩ → 3 components · `contrast-pairs.ts` + `style.css` (prose only) · `resolve-stack.ts` + `visualization-ux.spec.ts` · `e2e/screenshots/f-w12/**` (probe + 10 PNGs) — all inside §1. value.js: `19b8de91` · `d40bf9d1` · `7efcd0bd` · `e1a5ad30` = record · INBOX · LEDGER only. ⟨`git log --format=%h 19b8de91~1..HEAD -- scripts/dev/dev.sh | wc -l`⟩ → **0**. No `glass-ui/**`, no `package.json`.
3. **No masking** — `5b75cf0` read whole: no `test.skip`/`fixme`/`test.slow`, no retry loop, no lengthened timeout (the network-quiet waits carried `60_000`; the replacements carry the same `60_000`); the `:322` control snapshot WIDENS (adds `input[role="spinbutton"]` so the two BasisSelector fields stay pinned) — not a narrowing. R-e-1 rides the producer's `NumberField` (no local number input). R-e-3 is mail only.
4. **Commit families** — four fourier commits, one meaning each (R-e-1 · R-e-2 · readiness · evidence); unsplit.
5. **E-3** — ⟨`git diff --stat 19b8de91~1..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/ docs/tranches/X/conformance`⟩ → **empty**.
6. **Mail** — O-51 present (1); INBOX carries no UNREAD row in F.W12 scope. One NEW inbound file since the close's sweep (INFO I-1).
7. **Four-verb** — R-e-1 · R-e-2 · R-e-3 · readiness at **IMPLEMENTED**; nothing stamped VERIFIED; lawful.
8. **Goal criterion at the bytes** — limb 1 (every numeric field steps by keyboard on `NumberField`) **MET** (probe 5/5 ×2); limb 3 (stale rows retired) **MET**; limb 2 (*"the keystone suite exercises `:290` in a full run"*) **NOT MET** — 0/2 full runs here executed `:322` (the old `:290`).
9. **Published figures** — the close's gate figures reproduce; its full-run tallies (84p/13f · 80p/16f) are load-dependent and differ from this seat's (82p/14f · 76p/17f) in count but not in the verdict or mechanism.
10. **Honest-RED adjudication** — see H-1. **Successors**: no authored wave declares *"Opens after: F.W12"* (⟨`grep -rln 'F\.W12' docs/tranches/X/fourier docs/tranches/X/*.md`⟩ → the spec + COHESION/RUNBOOK KF.W12 homonyms only); nothing is blocked by this row beyond Track C's own tail.

### Register

- **H-1 · HIGH · G-F12-3 full-run leg RED and UNRELIEVED.** Receipt: two full 9-worker runs above, `:109` ✘ `Test timeout of 30000ms exceeded` inside `openWorkspace`'s compute-pair wait (`visualization-ux.spec.ts:46-47`), `test.describe.serial` (`:98`) then skips `:122`…`:322`; 0/2 here, 0/2 at the close. Relief test at the spec bytes: §4 admits only *"a producer-owned RED → CLOSED honest-RED with its relay id"* — this leg is consumer-harness (fourier e2e), not producer-owned; the spec routes it to no successor wave and names no honest-RED id; ESC-F12a-1 is unruled (⟨`grep -rln ESC-F12a-1 docs/tranches/X`⟩ → this record + LEDGER only). It is one of the goal criterion's three limbs, so HIGH. Mechanism note: the readiness waits carry `timeout: 60_000` inside the default 30 s test budget — the test budget, not the wait, is what expires under load; the cure lands the right signal but cannot finish inside the budget the serial block allows. Cure: the ESC-F12a-1 ruling (orchestrator / COHESION, Track C) — e.g. a ruled budget for the serial block's workspace keystones or un-serialising the independent `:322` — then a repair unit in `web/e2e/visualization-ux.spec.ts`, re-run ×2 with `:322` executed and green in both. The close named this owner correctly; it is not honest-RED.
- **I-1 · INFO · new inbound mail after the close's sweep.** `glass-ui/docs/tranches/BK/coordination/glass-outbound-2026-09-22-fourier-o23-o32-reply.md` (mtime 18:25, uncommitted in glass-ui) is addressed to Track C, replies to O-23/O-32 (F.W1/F.W4), and is not yet rowed in INBOX (⟨`grep -c fourier-o23-o32-reply INBOX.md`⟩ → 0). Not F.W12 scope; its §2.3 (`NumberField` paints the perimeter rung) is consistent with R-e-2's retirement. Cure: the next Track C seat's E13 sweep rows it.
- **I-2 · INFO · `paper-performance.spec.ts:158` failed in run 2 only** (load avg ~97); file untouched by the wave. No action.
- **I-3 · INFO · the full runs rewrite the 21 J-audit PNGs** (dirty before open; unstaged by this seat), as the close's residuals record.

**Honest-RED set: EMPTY** (H-1 is not relieved). **LEDGER**: the row STAYS `PARTIAL`; an event line is appended.

## Repair 1

Seat: `claude-opus-5-5[1m]`, 2026-09-22. Register: `## Check 1` (1 HIGH · 3 INFO). Crash-recovery: ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit/screenshots`⟩ → `?? .worktrees/` only; the record and LEDGER are clean in value.js. No inherited partial work. Fourier HEAD is still `c5836a7`.

| defect | cure | commit | gate re-reading |
|---|---|---|---|
| **H-1 · HIGH** G-F12-3 full-run leg RED, no relief | **ESCALATED, not cured.** The cure needs the ESC-F12a-1 ruling first. ⟨`grep -rln ESC-F12a-1 docs/`⟩ → `LEDGER.md` · `C/F-W12.md` only. ⟨`grep -n F.W12 COHESION.md`⟩ → §0as only, and there is no later ruling. Both candidate byte-cures sit outside this seat's grant. (a) `test.slow()` or `setTimeout` is a longer timeout, which the spec §2.4 lock forbids. (b) Un-serialising `test.describe.serial` at `visualization-ux.spec.ts:98` is a harness change. §1 Bounds grants only `:33` (the readiness signal), and both `.a` and Check 1 route this change to a ruling. Even with (b) in place, `:322` still opens its own workspace inside the same 30 s budget: 9-worker load spent ~30 s at `:109` (see `.a`'s trace). So (b) alone does not prove that `:322` is green ×2. The root cause is the budget under load, and a ruling has to decide it. | none | unchanged: no bytes moved, so no gate could move. The banked Check 1 reading stands (0/2 runs executed `:322`). |
| INFO · BK `fourier-o23-o32-reply` has no INBOX row | not in F.W12's scope (O-23/O-32). The next Track C E13 sweep owns it. ⟨`grep -c fourier-o23-o32-reply INBOX.md`⟩ → 0 | none | n/a |
| INFO · `paper-performance.spec.ts:158` run-2-only failure | no action (untouched file, load ~97) | none | n/a |
| INFO · 21 J-audit PNGs rewritten by full runs | outside bounds. The owner is the Track C J-audit evidence home. | none | n/a |

**Escalation (to the orchestrator / COHESION, Track C): ESC-F12a-1, re-raised.** Rule one of these: (a) a budget for the serial block's workspace keystones; (b) un-serialise `:98` and grant `visualization-ux.spec.ts` beyond `:33`; (c) a gate re-reading. Then mint a repair unit in `web/e2e/visualization-ux.spec.ts` that re-runs the full suite ×2 with `:322` executed and green in both. Until that ruling lands, the row stays **PARTIAL**.

Self-count: 4 defects registered · 0 cured · 1 escalated · 3 INFO recorded with no action.

## Check 2

Fresh adversarial L-20 pass 2, VERIFY-ONLY — `claude-opus-5-5[1m]`, 2026-09-22 (sitting of record 2026-09-17). Authored no cure byte; wrote **0** fourier, **0** glass-ui, **0** product bytes. Spec read whole (48 lines); record read header → `## Unit plan` + `## Close` + `## Check 1` + `## Repair 1`; the four fourier commits by `git diff --stat 41e49af..HEAD` + an added-line masking scan; the probe script read whole.

**Crash-recovery.** fourier porcelain (minus the 21 J-audit PNGs) → `?? .worktrees/` only; value.js porcelain = sibling-owned rows + `dev.sh` + `?? .r1-*.mjs` — none in this seat's set (record + LEDGER). No inherited work. ⟨`git rev-parse HEAD origin/m/w1-bump-migration`⟩ → `c5836a7…` ×2: **no fourier byte has moved since Check 1 / Repair 1.** ESC-F12a-1: ⟨`grep -rln ESC-F12a-1 docs/`⟩ → `LEDGER.md` · `C/F-W12.md` only; ⟨`grep -n 'F\.W12\|ESC-F12' COHESION.md`⟩ → §0as only (plus KF.W12 homonyms) — **still unruled.**

**Verdict: NOT-CONFORMANT — 0 BLOCKER · 0 CRITICAL · 1 HIGH · 0 MINOR · 3 INFO.** All five GREEN limbs reproduce; the one RED limb reproduces RED and is not relieved by §4.

### Gates reproduced (fourier `web/` at `c5836a7`)

| gate | command | run 1 · run 2 | verdict |
|---|---|---|---|
| G-F12-1 imports | `grep -l 'from "@mkbabb/glass-ui/number-field"'` ×3 files \| `wc -l`; `cat` the three \| `grep -c '<NumberField\b'` | **3 · 5** · **3 · 5** | GREEN |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs chk2a` / `chk2b` (:3100 → 200); output PNGs moved to the scratchpad; porcelain clean after | **5/5 · 5/5** (Low 5→4→5 · High 50→49→50 · Duration 150→140→150 · Harmonics 200→199→200 · Sample Points 1024→896→1024; `role=spinbutton` ×5) | GREEN |
| G-F12-2 | `grep -c 'level-input' e2e/contrast-pairs.ts` · `grep -c 'AppHeader.vue' src/style.css` | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 full run | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` | **80p · 16f · 3s · 8 did not run**, EXIT 1 — `:109` ✘ `page.waitForResponse: Test timeout of 30000ms exceeded` at `openWorkspace` `:46`/`:47` → serial skip `:122` … **`:322` NOT run** | **RED** (0/1 here; 0/5 cumulative across close ×2 · Check 1 ×2 · Check 2 ×1). A second run was not spent: the gate needs `:322` green in BOTH, so one RED run fixes the verdict. |
| G-F12-4 | `npm run build` ×2 · E13 · `grep -c '^\| O-51 ' INBOX.md` | exit 0 (`✓ built in 5.96s`) · exit 0 (`6.75s`) · 0 UNREAD in F.W12 scope · **1** | GREEN |

**gatesReproduced = 5** (the close's five GREEN limbs); **gatesFailed = G-F12-3 full-run leg**.

### Axes

1. **GREENs reproduce** — 5/5 (table).
2. **Bounds** — ⟨`git -C fourier-analysis diff --stat 41e49af..HEAD`⟩ → 18 files: the 3 components · `contrast-pairs.ts` · `resolve-stack.ts` · `visualization-ux.spec.ts` · `style.css` (diff = the two comment blocks only) · `e2e/screenshots/f-w12/**` (probe + 10 PNGs) — all inside §1. value.js ⟨`git show --stat`⟩ `19b8de91` · `d40bf9d1` · `7efcd0bd` · `e1a5ad30` · `b6e676eb` · `c5614cd2` → INBOX · record · LEDGER only. ⟨`git log --format=%h 19b8de91~1..HEAD -- scripts/dev/dev.sh | wc -l`⟩ → **0**.
3. **No masking** — added-line scan ⟨`git diff 41e49af..HEAD -- web/e2e web/src | grep -E '^\+.*(skip|fixme|slow\(|setTimeout|timeout|retr|try \{|catch)'`⟩ → only the carried `60_000` waits (same value the removed network-quiet waits held) and the probe's `.catch(() => {})` on its settle-wait (INFO I-4: the delta assertion after it is strict, so a swallowed wait yields FAIL, never a false PASS). No `test.skip`/`slow`, no retry, no local number input, no glass-ui/node_modules byte.
4. **Commit families** — four fourier commits, one meaning each; unsplit.
5. **E-3** — ⟨`git diff --stat 19b8de91~1..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/ docs/tranches/X/conformance`⟩ → **empty**.
6. **Mail** — O-51 present (1); no UNREAD row in F.W12 scope. BK `glass-outbound-2026-09-22-fourier-o23-o32-reply.md` still unrowed (⟨`grep -c fourier-o23-o32-reply INBOX.md`⟩ → 0) — O-23/O-32 scope, not F.W12 (INFO I-1, carried).
7. **Four-verb** — R-e-1 · R-e-2 · R-e-3 · readiness at IMPLEMENTED; nothing stamped VERIFIED; lawful.
8. **Goal criterion** — limbs 1 and 3 MET at the bytes; limb 2 (*"the keystone suite exercises `:290` in a full run"*) **NOT MET** (`:322`, the old `:290`, not run).
9. **Published figures** — gate figures reproduce; full-run tallies are load-dependent (80p/16f here vs 84p/13f · 80p/16f · 82p/14f · 76p/17f earlier) with the same verdict and mechanism.
10. **Honest-RED adjudication** — G-F12-3 full-run leg: §4 relieves only *"a producer-owned RED → CLOSED honest-RED with its relay id"*. This leg is fourier's own e2e harness (the serial block's 30 s budget under 9-worker load), not producer-owned; the spec routes it to no successor and names no honest-RED id; ESC-F12a-1 is unruled. **Not relieved.** **Successors**: ⟨`grep -rn 'Opens after.*F\.W12' docs/tranches/X/`⟩ → no F-wave hit (KF homonyms only) — no successor conjunct names F.W12, so nothing is blocked beyond Track C's own tail.

### Register

- **H-1 · HIGH (carried from Check 1, unchanged) · G-F12-3 full-run leg RED, unrelieved.** Receipt: the full run above: `:109` ✘ `page.waitForResponse: Test timeout of 30000ms exceeded` (`visualization-ux.spec.ts:47`); `test.describe.serial` skips `:122`…`:322`. Cure: the ESC-F12a-1 ruling (orchestrator / COHESION, Track C). The options are (a) a ruled budget for the serial block's workspace keystones, (b) un-serialise `:98` and grant `visualization-ux.spec.ts` beyond `:33`, or (c) a gate re-reading. Then a repair unit re-runs ×2 with `:322` executed and green in both.
- **I-1 · INFO** — the BK o23-o32 reply is still unrowed; the next Track C E13 sweep rows it.
- **I-3 · INFO** — full runs rewrite the 21 J-audit PNGs (dirty before open; left unstaged).
- **I-4 · INFO** — `arrowup-probe.mjs` swallows its settle-wait and exits 0 on a FAIL row. This is not masking, because the verdict is printed per row and the tally is strict. A future probe could set `process.exitCode` on a FAIL.

**Honest-RED set: EMPTY.** **LEDGER**: the row STAYS `PARTIAL`; an event line is appended.

## Repair 2

Seat: `claude-opus-5-5[1m]`, 2026-09-22. Register: `## Check 2` (1 HIGH · 3 INFO). Crash-recovery: ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only; value.js porcelain → `CARRY-LEDGER.md` (sibling) + `dev.sh` (never touched) — nothing in this seat's set. No inherited partial work. ⟨`git rev-parse --short HEAD refs/remotes/origin/m/w1-bump-migration`⟩ → `c5836a7` ×2: no fourier byte has moved since Check 1.

| defect | cure | commit | gate re-reading |
|---|---|---|---|
| **H-1 · HIGH** G-F12-3 full-run leg RED, unrelieved (`:322` not run, 0/5) | **ESCALATED, not cured.** ESC-F12a-1 is still unruled: ⟨`grep -rln ESC-F12a-1 docs/`⟩ → `LEDGER.md` · `C/F-W12.md` only; ⟨`grep -n 'ESC-F12\|F\.W12' COHESION.md \| grep -v KF`⟩ → `:2743` · `:2752` (§0as, the minting) only. Every byte-cure lies outside this seat's grant: (a) `test.slow()`/`setTimeout` = a longer timeout, forbidden by §2.4's lock; (b) un-serialising `visualization-ux.spec.ts:98` = a harness change beyond §1's `:33` grant; (c) a gate re-reading is the orchestrator's, not a repair seat's. | none | not re-run: no cure byte landed, so no gate can move. Check 2's reading stands (80p · 16f · 3s · 8 did not run, `:322` not run). |
| I-1 · INFO BK `fourier-o23-o32-reply` unrowed | out of F.W12 scope (O-23/O-32); the next Track C E13 sweep rows it. ⟨`grep -c fourier-o23-o32-reply INBOX.md`⟩ → 0 | none | n/a |
| I-3 · INFO 21 J-audit PNGs rewritten by full runs | outside bounds; left unstaged (Track C J-audit evidence home) | none | n/a |
| I-4 · INFO `arrowup-probe.mjs` exits 0 on a FAIL row | INFO, optional; the tally is strict and G-F12-1 is GREEN ×2, so no byte is spent (the probe is committed evidence) | none | n/a |

**Escalation (orchestrator / COHESION, Track C): ESC-F12a-1, third raise.** Two repair rounds cannot move this row. The next act must be a ruling on (a) / (b) / (c), followed by a repair unit in `web/e2e/visualization-ux.spec.ts` that re-runs the full suite ×2 with `:322` executed and green in both. Another repair round on the same register spends a seat with nothing to change. The row stays **PARTIAL**.

Self-count: 4 defects registered · 0 cured · 1 escalated · 3 INFO recorded, no action.

## Check 3

Fresh adversarial L-20 pass 3, VERIFY-ONLY. Seat `claude-opus-5-5[1m]`, 2026-09-22 (sitting of record 2026-09-17). This seat wrote **0** fourier, **0** glass-ui and **0** product bytes. The spec was read whole (48 lines). From the record it read the header through `## Unit plan`, then `## Check 2` and `## Repair 2`.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only. The value.js porcelain holds `CARRY-LEDGER.md` (a sibling's) and `dev.sh` (never touched); nothing is in this seat's set. **No fourier byte has moved:** ⟨`git branch -vv`⟩ → HEAD `c5836a7`, and ⟨`git ls-remote origin m/w1-bump-migration`⟩ → `c5836a70…`. **ESC-F12a-1 is still unruled:** ⟨`grep -rln ESC-F12a-1 docs/`⟩ → `LEDGER.md` · `C/F-W12.md` only, and ⟨`grep -n 'ESC-F12\|F\.W12' COHESION.md | grep -v KF`⟩ → `:2743` · `:2752` (the §0as minting) only.

**Verdict: NOT-CONFORMANT — 0 BLOCKER · 0 CRITICAL · 1 HIGH · 0 MINOR · 2 INFO.** All five GREEN limbs reproduce ×2. The full-run leg reproduces RED a sixth time, and the spec's §4 does not relieve it.

### Gates reproduced (fourier `web/` at `c5836a7`)

| gate | command | run 1 · run 2 | verdict |
|---|---|---|---|
| G-F12-1 imports | `grep -l 'from "@mkbabb/glass-ui/number-field"'` over the three files \| `wc -l`, then `cat` the three \| `grep -c '<NumberField\b'` | **3 · 5** · **3 · 5** | GREEN |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs chk3a` / `chk3b` against :3100 (200). The output PNGs were moved to the scratchpad and the porcelain was clean afterwards. | **5/5 · 5/5**: Low 5→4→5 · High 50→49→50 · Duration 150→140→150 · Harmonics 200→199→200 · Sample Points 1024→896→1024, each `role=spinbutton` | GREEN |
| G-F12-2 | `grep -c 'level-input' e2e/contrast-pairs.ts` · `grep -c 'AppHeader.vue' src/style.css` | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 full run | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` | **80 passed · 16 failed · 3 skipped · 8 did not run**, EXIT 1. `:109` ✘ at 30.3 s (`visualization-ux.spec.ts:112`), so the serial block skipped `:122` … and **`:322` shows `-` (NOT run)**. | **RED**: 0/1 here and 0/6 cumulative. No second run was spent, because the gate needs `:322` green in BOTH runs and one RED run already settles the verdict. |
| G-F12-4 | `npm run build >/dev/null 2>&1; echo $?` ×2 · E13 · `grep -c '^\| O-51 ' INBOX.md` | exit **0** · **0** (`✓ built in 6.48s` / `7.39s`) · 0 UNREAD in F.W12 scope · **1** | GREEN |

**gatesReproduced = 5; gatesFailed = G-F12-3 full-run leg.**

### Axes

1. **GREENs reproduce.** 5/5 ×2 (table above).
2. **Bounds.** The fourier diff `41e49af..HEAD` has not changed since Check 2: four commits (`a9fa5fb` · `22ae0c1` · `5b75cf0` · `c5836a7`), all inside §1. The value.js commits made since Check 2 are ⟨`git show --stat 8f62216a 94301dc4`⟩ → record + LEDGER only. ⟨`git log --format=%h 19b8de91~1..HEAD -- scripts/dev/dev.sh | wc -l`⟩ → **0**.
3. **No masking.** No code byte has moved since Check 2's added-line scan (only the carried `60_000` values; no skip, slow or retry, and no glass-ui or node_modules byte).
4. **Commit families.** Unsplit, one meaning each (unchanged).
5. **E-3.** ⟨`git diff --stat 19b8de91~1..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/ docs/tranches/X/conformance`⟩ → **empty**.
6. **Mail.** O-51 is present (1). The INBOX tail status cells I-37..O-52 have no UNREAD in F.W12 scope. BK `glass-outbound-2026-09-22-fourier-o23-o32-reply.md` is still unrowed (⟨`grep -c`⟩ → 0), but its header is addressed *"To … F.W1 unit `b` … (O-23), and F.W4 unit `.z` … (O-32)"*, so it is out of F.W12 scope (INFO I-1, carried).
7. **Four-verb.** Everything is at IMPLEMENTED and nothing is stamped VERIFIED, which is lawful.
8. **Goal criterion.** Limbs 1 and 3 are MET. Limb 2 (*"the keystone suite exercises `:290` in a full run"*) is **NOT MET**: `:322`, the old `:290`, did not run.
9. **Published figures.** The gate figures reproduce. The full-run tallies depend on load but keep the same verdict and mechanism (80p/16f, identical to Check 2).
10. **Honest-RED adjudication.** §4 relieves only *"a producer-owned RED → CLOSED honest-RED with its relay id"*. The G-F12-3 full-run leg is RED in fourier's own e2e harness: `:109`'s 30 s budget under 9-worker load, then serial skip. It is not producer-owned. The spec routes it to no successor and names no honest-RED id, and ESC-F12a-1 is unruled. **It is not relieved.** **Successors:** ⟨`grep -rn 'Opens after.*F\.W12' docs/tranches/X/`⟩ → no F-wave hit, so no successor conjunct names F.W12 and nothing else is blocked.

### Register

- **H-1 · HIGH (carried from Check 1/2, unchanged).** G-F12-3's full-run leg is RED and unrelieved. Receipt: the run above, where `:109` ✘ at 30.3 s and `:322` did not run. Cure: the ESC-F12a-1 ruling (orchestrator / COHESION, Track C) on one of three options: (a) a ruled budget for the serial block, (b) un-serialise `:98` with a grant beyond `:33`, or (c) a gate re-reading. After the ruling, a repair unit re-runs the suite ×2 with `:322` executed and green in both runs.
- **I-1 · INFO.** The BK o23-o32 reply is unrowed (out of scope); the next Track C E13 sweep rows it.
- **I-3 · INFO.** The full run rewrote the 21 J-audit PNGs, which were already dirty before this seat and were left unstaged.

**Honest-RED set: EMPTY.** **LEDGER:** the row STAYS `PARTIAL` and an event line is appended. A fourth check or repair on this register cannot move it; the next act is the ruling.

# RESUME — SECOND SITTING (2026-09-22, on COHESION §0au; sitting of record 2026-09-17)

SEAT 0 (RESUME OPEN), `claude-opus-5-5[1m]`. E-3: no byte above this heading is rewritten. Spec read whole (53 lines, incl. `## ADDENDUM 2026-09-22` `:51-53`). Ruling of record: **COHESION §0au** (`9e3e4d8b`) — ESC-F12a-1 ruled: (a) REFUSED (no `test.slow()`, no timeout raise) · (b) GRANTED with a proof obligation · (c) applies to `:109` ALONE. From this record the seat read the header through `## Unit plan` and `## Check 3` only.

## Open (resume)

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only — nothing inside `.a2`'s set (`web/e2e/visualization-ux.spec.ts` · `web/e2e/screenshots/f-w12/**`); no inherited partial work. value.js porcelain: `CARRY-LEDGER.md` · `X-W6.md` · a W6 evidence PNG (Track A, sibling-owned) · `dev.sh` (never touched) — none in this wave's set.

**alreadyDone (commits exist).** `F.W12.a` — fourier `a9fa5fb` · `22ae0c1` · `5b75cf0` · `c5836a7` (⟨`git log --oneline -5`⟩, HEAD `c5836a7`; ⟨`git ls-remote origin m/w1-bump-migration`⟩ → `c5836a70…` = pushed). Never re-dispatched. **Owed:** `F.W12.a2` → `F.W12.b2`.

**Preconditions.** F.W11 CLOSED (ledger row unchanged, met at the first open). The ruling the resume needs exists at bytes: ⟨`grep -n '^## §0au' COHESION.md`⟩ → `:2774` (read to the file end `:2794`). LEDGER row `F.W12` = `PARTIAL — G-F12-3 full-run leg RED … ESC-F12a-1 awaits ruling` → now ruled. **MET.**

**E13 Step-0.** Four paths swept read-only; BK still the newest glass dir (BK · BJ · BI). One unrowed letter found: BK `glass-outbound-2026-09-22-fourier-o23-o32-reply.md` (469 lines; header *"To: value.js tranche X, Track C … F.W1 unit `b` … (O-23), and F.W4 unit `.z` … (O-32)"*) → **rowed I-40 UNREAD** (Track C, outside F.W12 scope — F.W12's only mail is O-51); sweep line appended to INBOX. kf / atlas coordination: empty. **0 UNREAD in F.W12 scope.**

## Unit plan (resume)

Serial, one concurrent (addendum `:51-53`: [`.a2`] → [`.b2`]); both Opus (no Fable seat named). Push `m/w1-bump-migration` at every unit close. glass-ui READ-ONLY; `package.json` untouchable (§1).

| unit | model | sections | writable | gates | locks |
|---|---|---|---|---|---|
| `F.W12.a2` | opus | ADDENDUM 2026-09-22 `:51-53` · §2 `### F.W12.a` item 4 (`:32-35`) · §3 G-F12-3 (`:42-43`) · COHESION §0au (b)+(c) | fourier `web/e2e/visualization-ux.spec.ts` (the `.serial` block `:98-…` and any per-test bootstrap it needs) · `web/e2e/screenshots/f-w12/**` · value.js this record | G-F12-3 full-run leg (`:322` EXECUTED + green in two full 9-worker runs; no failure absent at `41e49af`; `:109` under (c) only if it passes alone ×2) · G-F12-3 grep leg stays 0·0 | §0au (a) REFUSED: no `test.slow()`, no timeout raise, no retry; no assertion/wait/timeout change; independence PROVEN before `.serial` is dropped; never `test.skip`/fixme |
| `F.W12.b2` | opus (verify-only) | ADDENDUM `:53` · §3 all (`:39-45`) · §4 (`:47-48`) | value.js this record · `LEDGER.md` · `INBOX.md` | G-F12-1..4 ×2 (re-read) | verify-only (no code bytes); honest-RED only for a producer-owned RED with its relay id |

**Open-seat reading for `.a2` (not a re-ruling).** ⟨`grep -n 'describe\|test(' e2e/visualization-ux.spec.ts`⟩ → the block opens `:98` (`test.describe.serial`) and holds nine tests: `:109` `:122` `:139` `:171` `:208` `:262` `:274` `:290` `:322`. Each body opens with its own navigation (`openWorkspace(page)` at `:109/:122/:139/:171/:322`; `page.goto` at `:262/:274/:290`), so independence is plausible at the bytes but must be PROVEN per §0au (each alone from a fresh page ×2, receipts) before the flag goes.

## Baseline (resume)

RESUME-mode rule: only the gate the owed unit turns is re-run; the rest cite Check 3's banked reading at the same bytes (fourier `c5836a7`, unmoved).

| gate | command | BEFORE | verdict |
|---|---|---|---|
| G-F12-3 full-run leg (the one `.a2` turns) | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` (web `:3100` 200 · api `/api/health` `{"status":"ok"}`) | **80 passed · 16 failed · 3 skipped · 8 did not run**, EXIT 1 (1.4m). `:109` ✘ **30.3 s**; `:122 :139 :171 :208 :262 :274 :290` and **`:322` all `-` (NOT run)** | **RED** (born-RED holds; 0/7 cumulative) — one RED run settles it, second run not spent |
| G-F12-3 grep leg | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | **0 · 0** | GREEN (landed at `.a` `5b75cf0`; not a before-cure green — `.a2` does not turn it) |
| G-F12-1 · G-F12-2 · G-F12-4 | cited: Check 3 `## Check 3` gate table | GREEN ×2 at `c5836a7` | GREEN (`.a`'s, banked) |

Failing files in the baseline run (⟨`grep ✘ … \| sort -u`⟩): contrast-floor `:82` `:128` · equation-interaction `:77` · fullscreen `:40` · gallery-admin-a11y `:91` `:103` `:114` `:125` · visual-baseline `:57` · visual-checkpoint `:80` `:101` `:122` · visualization-ux `:109` — the banked load-flaky family; the `.a2` gate compares its runs' failures against `41e49af`. The run rewrote the J-audit PNGs again (already dirty, outside every set, left unstaged — §0au routes them to the owner close report).

**greenBeforeCure: none.**

## Unit receipts (resume)

### F.W12.a2

Seat `claude-opus-5-5[1m]`, 2026-09-22, on COHESION §0au (b)+(c). The spec was read whole (53 lines, incl. ADDENDUM `:51-53`). From this record the seat read the header through `## Unit plan (resume)` and `## Baseline (resume)`, plus `## Check 3`. Writes: fourier `web/e2e/visualization-ux.spec.ts` + this record only. Nothing was written to `e2e/screenshots/f-w12/**` because no evidence file was owed there.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only. Nothing was dirty in `.a2`'s set, so no inherited partial work. Every value.js dirty row belongs to a sibling or is `dev.sh`, and none is in this set.

**Act 1: independence proven (§0au (b), proof obligation).** Each of the nine tests in the block was run ALONE from a fresh page, twice:
⟨`for r in 1 2; do for L in 109 122 139 171 208 262 274 290 322; do BASE_URL=http://localhost:3100 npx playwright test e2e/visualization-ux.spec.ts:$L --workers=1 --reporter=list; done; done`⟩ (web `:3100` 200 · api `/api/health` `{"status":"ok"}`) → **18/18 exit 0, each `1 passed`**:

| line | r1 | r2 |
|---|---|---|
| `:109` workspace default | 1 passed (7.0s) | 1 passed (7.6s) |
| `:122` ContourSettings | 1 passed (7.2s) | 1 passed (6.8s) |
| `:139` ExportModal | 1 passed (6.7s) | 1 passed (8.2s) |
| `:171` dropdown | 1 passed (7.0s) | 1 passed (7.3s) |
| `:208` /equation | 1 passed (2.0s) | 1 passed (2.2s) |
| `:262` /paper | 1 passed (2.3s) | 1 passed (3.3s) |
| `:274` /morph | 1 passed (2.0s) | 1 passed (1.9s) |
| `:290` /shape-extractor | 1 passed (1.9s) | 1 passed (1.9s) |
| `:322` save_contour_then_recompute | 1 passed (10.7s) | 1 passed (7.5s) |

`:109` passed alone ×2, so §0au (c) (B-4) is available to it.

**Act 2: bootstrap.** No test needed one. Reading the bytes: every body opens with its own navigation (`openWorkspace(page)` at `:109` `:122` `:139` `:171` `:322`; `page.goto` / `waitForResponse`+`goto` at `:208` `:262` `:274` `:290`). No module-level state is shared, since the file-scope bindings are `TEST_IMAGE` and two helper functions. Act 1 confirms this. **0 bootstraps added.**

**Act 3: `.serial` retired.** `test.describe.serial(` → `test.describe(` at `:98`, with a dated 8-line comment above it citing §0au (b) and these receipts. This shifts every later test by +8 lines: `:109`→`:117` · `:122`→`:130` · `:139`→`:147` · `:171`→`:179` · `:208`→`:216` · `:262`→`:270` · `:274`→`:282` · `:290`→`:298` · **`:322`→`:330`**. The added-line scan is ⟨`git diff c5836a7..HEAD | grep -E '^[+-]' | grep -E 'timeout|expect|wait|slow|skip|retr'`⟩ → 1 hit, the comment's word "skipped". **No assertion, wait or timeout changed; no `test.slow`, no retry, no skip or fixme.** Commit fourier **`3e709e4`** (pathspec `web/e2e/visualization-ux.spec.ts`), pushed: ⟨`git ls-remote origin m/w1-bump-migration`⟩ → `3e709e42…`.

**Act 4: the two full nine-worker runs.** ⟨`BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list`⟩ ×2 at the landed bytes. Pair 1 used the pre-commit working tree, whose bytes differ from `3e709e4` by one comment word only. Every failure set was compared against the `41e49af` reference: the `735319e` stable 14 plus the run-1-only load-flaky six (F-W11 record §Baseline `:55-80`), with `visualization-ux :95` now at `:117`.

| run | tallies | `:330` (the spec's `:290`) | failures absent at `41e49af` | host load (`uptime`) |
|---|---|---|---|---|
| **pair 1 · r1** | 86p · 18f · 3s (1.7m), EXIT 1 | **✘ `Test timeout of 30000ms exceeded` (30.1 s) — EXECUTED, RED** | **3**: `visualization-ux :330` · `visualization-ux :130` (ContourSettings, ✘ 30.2 s) · `visual-checkpoint :163` (item 3, `waitForResponse` 30 s test timeout) | load avg ~44 (15-min), with a sibling seat's headless chromium live |
| **pair 1 · r2** | 91p · 13f · 3s (1.0m), EXIT 1 | **✓ 23.4 s** | **0** | — |
| pair 2 · r3 | 89p · 15f · 3s (1.1m), EXIT 1 | **✓ 26.8 s** | **0** | 17.2 / 29.6 / 44.4 |
| pair 2 · r4 | 89p · 15f · 3s (1.1m), EXIT 1 | **✓ 25.6 s** | **0** | 21.5 / 28.3 / 42.8 |

`:117` (the old `:109`) ✘ at 30.5 s in r1 is in the reference set and falls under §0au (c) because it passed alone ×2 (Act 1). In r2–r4 it passed.

**Pair 2 was run to characterise the pair-1 RED, not to replace it.** It is recorded here beside pair 1, and pair 1's reading stands as the pair of record.

**Gates, BEFORE → AFTER.**
- **G-F12-3 grep leg:** ⟨`grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts`⟩ ×2 → **0 · 0** → **0 · 0**. GREEN, and it stays GREEN.
- **G-F12-3 full-run leg:** BEFORE, `:322` NOT RUN (0/7 cumulative; the serial skip). AFTER, `:330` **EXECUTED in 4/4 full runs**, the serial skip mechanism is gone, and it was green in 3/4.
  - **Pair of record (r1·r2): RED.** In r1, `:330` hit the 30 s test budget, and three failures were absent at `41e49af`.
  - **Pair 2 (r3·r4): GREEN.** `:330` ✓ ×2 and 0 failures were absent.

**Residual (the reason this reads PARTIAL).** §0au (b) is discharged: independence is proven and `.serial` is retired. What remains is what the flag used to hide. Under nine workers against one dev server plus one compute backend, a test that runs the full `openWorkspace` upload→extract→compute bootstrap takes ~20–30 s. The default 30 s test budget is therefore load-marginal, and each test now fails on its own instead of hiding its successors. At host load ~44, `:330`, `:130` and `visual-checkpoint :163` crossed the budget. At load ~17–21 nothing did. §0au (a) refuses every budget lever, and none of the wait, timeout, `test.slow` or retry levers lies within this seat's grant. The next act is a ruling on this reading, not a seat act: either (c) extends to the load-timeout class, or the full-run leg is measured on a quiesced host. **ESC-F12a2-1.**

**Residuals, INFO.** The four full runs rewrote the 21 J-audit PNGs again. They were already dirty, lie outside every set and were left unstaged (§0au: owner close report). The playwright `test-results/` dir is git-ignored.

**Commits.** fourier `3e709e4` (pushed) · value.js this record.

### F.W12.b2

Seat `claude-opus-5-5[1m]`, 2026-09-22, VERIFY-ONLY close on COHESION §0au (sitting of record 2026-09-17). This seat wrote **0** fourier, **0** glass-ui and **0** product bytes. The spec was read whole (53 lines, incl. ADDENDUM `:51-53`). From this record the seat read the header through `## Unit plan`, `## Check 3`, and the resume block through `### F.W12.a2`. COHESION was read from §0as to the file end (`:2794`, §0au is the last addendum). **ESC-F12a2-1 is unruled:** ⟨`grep -n 'ESC-F12a2' COHESION.md`⟩ → 0 hits.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only. value.js porcelain holds `CARRY-LEDGER.md`, the J-audit PNGs and `dev.sh`, none of them in this seat's set. No inherited partial work.

**Act 1: gates ×2 at fourier `3e709e4` (= origin, ⟨`git rev-list --left-right --count HEAD...origin/m/w1-bump-migration`⟩ → `0 0`).**

| gate | command | run 1 · run 2 | verdict |
|---|---|---|---|
| G-F12-1 imports | `grep -l 'from "@mkbabb/glass-ui/number-field"' <3 files> \| wc -l` · `cat <3 files> \| grep -c '<NumberField\b'` | **3 · 5** · **3 · 5** | GREEN |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs b2a` / `b2b` (:3100 200; output PNGs moved to the scratchpad, porcelain clean after) | **5/5 · 5/5**: Low 5→4→5 · High 50→49→50 · Duration 150→140→150 · Harmonics 200→199→200 · Sample Points 1024→896→1024, each `role=spinbutton` | GREEN |
| G-F12-2 | `grep -c 'level-input' e2e/contrast-pairs.ts` · `grep -c 'AppHeader.vue' src/style.css` | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | **0 · 0** · **0 · 0** | GREEN |
| G-F12-3 full run | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` ×2 (web 200 · api `{"status":"ok"}`) | r1: **83p · 21f · 3s** (1.8m) EXIT 1, `:330` **✘ 30.3 s** (EXECUTED) · r2: **87p · 17f · 3s** (1.8m) EXIT 1, `:330` **✘ 30.3 s** (EXECUTED) | **RED** |
| G-F12-4 | `npm run build >log 2>&1; echo $?` ×2 · E13 (Act 3) · `grep -c '^\| O-51 ' INBOX.md` | exit **0 · 0** (`built in 4.73s` / `3.28s`) · 0 UNREAD in F.W12 scope · **1** | GREEN |

**Full-run detail.** Failures absent at `41e49af` were computed as ⟨`grep ✘ rN.log | grep -o 'e2e/[a-z0-9-]*\.spec\.ts:[0-9]*' | sort -u | comm -23 - ref.txt`⟩. `ref.txt` holds the 14 files:lines from the F-W11 record §Baseline `:55-80` (stable 14 plus load-flaky 6), with `visualization-ux :95` mapped to `:117`, the same reference `.a2` used.
- r1 new: `visualization-ux :130` · `:147` · `:179` · `:216` · `:330` (5).
- r2 new: `visualization-ux :130` · `:147` · `:179` · `:330` (4).

Every new failure is `Test timeout of 30000ms exceeded` at 30.2–30.4 s. That is the load-timeout class `.a2` named in ESC-F12a2-1, and none is an assertion failure. `:117` also timed out in both runs; it is in the reference set, and §0au (c) covers it because it passed alone ×2.

Host load ⟨`uptime`⟩: r1 14.4 → 41.6 (1-min), r2 29.2 → 54.5. A sibling seat's value.js `playwright test` (pid 4555) was live during r2 (⟨`ps aux | grep playwright`⟩).

**Act 2: bounds.** ⟨`git diff --name-only 41e49af..HEAD`⟩ → 18 paths. They are `contrast-pairs.ts`, `resolve-stack.ts`, `visualization-ux.spec.ts`, 11 × `e2e/screenshots/f-w12/**` (the probe script plus 10 PNGs), the three `.vue` sites and `style.css`. Every path is inside §1 or the addendum's `.a2` grant; no `glass-ui/**` path and no `package.json`. The 5 commits are `a9fa5fb` · `22ae0c1` · `5b75cf0` · `c5836a7` · `3e709e4`. ⟨`git log --format=%h 19b8de91~1..HEAD -- scripts/dev/dev.sh | wc -l`⟩ → **0**.

**Act 3: E13.** BK is still the newest glass dir (⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → BK · BJ · BI). The 2026-09-22 files in the four paths (⟨`find <p> -maxdepth 1 -type f -newermt "2026-09-22 00:00"`⟩):
- BK: `…fourier-o23-o32-reply` (INBOX hits 2), `…consumers-10.0.0` (14), `…nwo1-bh-relay` (11) and `…fw4-relay` (12). All are rowed.
- value.js `V/`: Track A working docs and INBOX.
- kf `V/coordination` and atlas `P/coordination`: empty.

**0 unrowed.** O-51 is `SENT`. I-40 is outside F.W12 scope (Track C F.W1/F.W4). **0 UNREAD in F.W12 scope.**

**Act 4: push.** ⟨`git push origin m/w1-bump-migration`⟩ → `Everything up-to-date`; ⟨`git ls-remote`⟩ → `3e709e42…`.

## Close (resume)

**Verdict: `PARTIAL`. The row does not move to CLOSED.** Five of six gate limbs are GREEN ×2: G-F12-1 imports and probe, G-F12-2, the G-F12-3 grep leg, and G-F12-4. The sixth, **the G-F12-3 full-run leg, is RED ×2**.

§0au (b) did its part. `:330` (the spec's `:290`) was EXECUTED in both runs, where the baseline showed 0/7 because of the serial skip. It was not green in either run: `Test timeout of 30000ms exceeded` at 30.3 s in both. Each run also had 4–5 keystone failures absent at `41e49af`, all from the same 30 s test-budget class.

- **§4 relief does not apply.** The RED is not producer-owned: it sits in fourier's own e2e harness, a 30 s default test budget under nine workers plus host load of 29–54. So there is no relay id, and no honest-RED is lawful.
- **§0au (a) forbids the budget levers.** No `test.slow`, no timeout raise, no retry. This seat is verify-only in any case.
- **Next act: a ruling on ESC-F12a2-1 (`.a2`'s escalation, carried unchanged).** One of two:
  - (i) extend §0au (c) to the load-timeout class, meaning each such keystone passes alone ×2 (proven at `.a2` Act 1, 18/18);
  - (ii) take the full-run leg's reading on a quiesced host (no sibling Playwright run live, load below ~20; `.a2` pair 2 at load 17–21 was GREEN ×2 with 0 new failures).
- **Cumulative `:330` full-run record since `3e709e4`:** 6 executions, 3 green (`.a2` r2 · r3 · r4) and 3 timed out (`.a2` r1 · b2 r1 · b2 r2). Every timeout came at host load ≥ ~40.

**Residuals (INFO).** The two full runs rewrote the 21 J-audit PNGs again. They were already dirty, lie outside every set, were left unstaged, and are routed to the owner close report (§0au).

**LEDGER.** Row `F.W12` → `PARTIAL`, plus one event line.

## Close (resume, close seat)

Verify-only close seat, `claude-opus-5-5[1m]`, 2026-09-22. This seat wrote 0 fourier, 0 glass-ui and 0 product bytes. Crash-recovery: ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only, so no inherited partial work. No COHESION ruling after §0au: ⟨`grep -c F12a2 COHESION.md`⟩ → `0`, so ESC-F12a2-1 is still unruled. The trees are unmoved: fourier HEAD = origin = `3e709e42`.

**Verdict: `PARTIAL`. The row stays `PARTIAL`.** Five limbs reproduce GREEN ×2. The G-F12-3 full-run leg is RED ×2 for the third close in a row, with the same load-timeout class.

### Gates (BEFORE at `41e49af` → AFTER at fourier `3e709e4`, this seat's own commands ×2)

| gate | command | BEFORE | AFTER r1 · r2 | verdict |
|---|---|---|---|---|
| G-F12-1 imports | `grep -h 'import.*NumberField'` over the 3 files · `grep -cE '<NumberField(\s\|$\|>)'` | 0/5 sites | 3 files import `@mkbabb/glass-ui/number-field`; tags HLG 2 · MPC 1 · Basis 2 = **5** · same | GREEN ×2 |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs close{1,2}` (PNGs moved to the scratchpad `fw12close/`, not committed) | RED | **5/5** (hlg-low 4→5 · hlg-high 49→50 · mpc-duration 140→150 · basis-harmonics 199→200 · basis-points 896→1024) · **5/5** | GREEN ×2 |
| G-F12-2 | `grep -c level-input e2e/contrast-pairs.ts` · `grep -c AppHeader.vue src/style.css` | 5 · 2 | **0 · 0** · **0 · 0** | GREEN ×2 |
| G-F12-3 grep leg | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | 6 · 1 | **0 · 0** · **0 · 0** | GREEN ×2 |
| G-F12-3 full-run leg | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` | `:290` not run | r1: 84 passed · **20 failed** · 3 skipped (load 20→35); r2: 83 passed · **21 failed** · 3 skipped (load 24→45; sibling value.js + csc411 Playwright live, 7 headless procs). `:330` EXECUTED both runs, **✘ 30.2 s timeout both runs** | **RED ×2** |
| G-F12-4 | `npm run build` · E13 · `grep -c O-51 INBOX.md` | build exit 0; relay row absent (compound RED) | exit **0** · exit **0**; 0 UNREAD in scope; O-51 row present | GREEN ×2 |

Full-run failures in visualization-ux: r1 = `:117` `:130` `:147` `:179` `:330` (all 30.2 s); r2 = `:117` `:130` `:147` `:179` `:216` `:330` (30.2-30.4 s). `:117` is the old `:109`, which also failed at baseline. `:130` `:147` `:179` `:216` `:330` are absent at `41e49af`, where they were skipped by `.serial`. Every one of them is the 30 s default test-budget class. The other failures (contrast-floor, equation-interaction :77, fullscreen :40, gallery-admin-a11y ×4, visual-baseline :57, visual-checkpoint ×3) are the banked load-flaky family seen at baseline. Logs: scratchpad `fw12close/full-r1.log`, `full-r2.log`.

Cumulative `:330` full-run record since `3e709e4`: 8 executions, 3 green and 5 timed out. Every timeout came at host load ≥ ~33 with sibling runs live.

### Bounds (landed-wrong check)

⟨`git -C fourier-analysis diff --stat 41e49af..HEAD`⟩ → 18 files, all inside §1 or the addendum's `.a2` grant: the 3 components, contrast-pairs.ts, resolve-stack.ts, visualization-ux.spec.ts, style.css, and `e2e/screenshots/f-w12/**` (probe + 10 PNGs). There is no glass-ui path and no package.json. ⟨`git show --stat 3e709e4`⟩ → `web/e2e/visualization-ux.spec.ts` only, which is `.a2`'s set. The value.js commits `492bc3c0` (the record) and `be39dcbe` (the record + LEDGER) are inside the `.a2`/`.b2` sets. **Landed-wrong: none.**

### Commit roster

fourier `a9fa5fb` · `22ae0c1` · `5b75cf0` · `c5836a7` (`.a`) · `3e709e4` (`.a2`), all pushed (origin = `3e709e42`). value.js `d40bf9d1` (O-51) · `7efcd0bd` · `ffafdd4b` · `492bc3c0` · `be39dcbe` · this close commit.

### E13

BK is still the newest glass dir (`ls -dt` → BK · BJ). ⟨`find -maxdepth 1 -type f -newermt "2026-09-22 00:00"`⟩ found 4 BK letters, all rowed (INBOX hit counts 2 · 14 · 11 · 12; the o23-o32 reply = I-40, Track C F.W1/F.W4, outside F.W12 scope). value.js `V/coordination` → INBOX.md only. The kf `V/coordination` and atlas `P/coordination` paths are empty. O-51 is SENT. **0 UNREAD in F.W12 scope.**

### Escalations

**ESC-F12a2-1 is carried unchanged and is still unruled.** The one remaining RED is fourier's own 30 s default test budget under nine workers plus host load. It is not producer-owned, so §4's honest-RED does not apply, and §0au (a) refuses the budget levers. It needs a ruling on one of two options: (i) extend §0au (c) to the load-timeout class (each keystone passed alone ×2 at `.a2`, 18/18); or (ii) measure the full-run leg on a quiesced host. This seat could not quiesce the host: sibling Playwright runs (value.js smoke, csc411) were live during both runs.

### Residuals

- The full-run leg is RED. The owner is COHESION, via the ruling on ESC-F12a2-1. Re-dispatch the close after that ruling.
- INFO: the two full runs rewrote the 21 J-audit PNGs again. They were already dirty, lie outside every set, and were left unstaged. Owner: the owner close report (§0au).

### State

The four-verb line does not move. `F.W12` stays **PARTIAL**; it is not IMPLEMENTED, because G-F12-3's full-run leg is RED.

**LEDGER.** The row cell was refreshed to cite this close, and one event line was appended.

## Check 1 (resume — fresh adversarial L-20 pass 1 of the close at fourier `3e709e4`)

Seat `claude-opus-5-5[1m]`, 2026-09-22, VERIFY-ONLY, independent (authored none of the wave's bytes). Spec read whole (53 lines incl. ADDENDUM); record read at header→`## Unit plan`, `## Unit plan (resume)`/`## Baseline (resume)`, the `.a2` receipt, and the last two `## Close` sections; COHESION §0au read whole (`:2774-2794`). Crash-recovery: ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only. Trees: fourier HEAD = origin = `3e709e42`. ⟨`grep -c F12a2 COHESION.md`⟩ → `0`: ESC-F12a2-1 still unruled.

**Verdict: `NOT-CONFORMANT`.** Every GREEN the close claims reproduces. The close honestly reports PARTIAL. But the one RED gate, the G-F12-3 full-run leg, has no relief at the spec bytes, so the wave cannot close.

### Gates reproduced (this seat's own commands, fourier `web/` at `3e709e4`)

| gate | command | r1 · r2 | verdict |
|---|---|---|---|
| G-F12-1 imports | `grep -h 'import.*NumberField'` ×3 files · `grep -cE '<NumberField(\s\|$\|>)'` | 3 × `@mkbabb/glass-ui/number-field`; HLG 2 · MPC 1 · Basis 2 = **5** · same | GREEN ×2 |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs chk1a` / `chk1b` (PNGs moved to scratchpad `fw12chk1/`, not committed) | **5/5** (4→5 · 49→50 · 140→150 · 199→200 · 896→1024) · **5/5** | GREEN ×2 |
| G-F12-2 | `grep -c level-input e2e/contrast-pairs.ts` · `grep -c AppHeader.vue src/style.css` | 0 · 0 · 0 · 0 | GREEN ×2 |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | 0 · 0 · 0 · 0 | GREEN ×2 |
| G-F12-3 full-run | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` (web 200 · api `{"status":"ok"}`; load 37→32, 17 sibling headless procs live) | **84 passed · 20 failed · 3 skipped** (1.3m), EXIT 1. `:330` EXECUTED, **✘ `Test timeout of 30000ms exceeded` (30.1 s)**; also ✘ `:117` `:130` `:147` `:179` (30.2-30.3 s) | **RED** (one RED run settles it; second not spent) |
| G-F12-4 | `npm run build` ×2 · `grep -c O-51 INBOX.md` · E13 | exit 0 · exit 0 · 2 hits · 0 UNREAD in scope | GREEN ×2 |

**gatesReproduced: 5 of 5 claimed-GREEN limbs.** The full-run log is in the scratchpad at `fw12chk1/full-r1.log`. Its failures outside visualization-ux are the banked load-flaky family (contrast-floor `:82` `:128` · fullscreen `:40` · visual-checkpoint `:80` `:101` `:122` · equation-interaction `:77` · visual-baseline `:57`).

### Axes

1. **GREENs reproduce.** Yes: 5/5 limbs ×2 (table above).
2. **Bounds.** ⟨`git -C fourier-analysis diff --stat 41e49af..HEAD`⟩ → 18 files, all inside §1 or the `.a2` grant. ⟨`git show --stat`⟩ per commit: `a9fa5fb` = the 3 components · `22ae0c1` = contrast-pairs.ts + style.css · `5b75cf0` = resolve-stack.ts + visualization-ux.spec.ts · `c5836a7` = `e2e/screenshots/f-w12/**` (probe + 10 PNGs) · `3e709e4` = visualization-ux.spec.ts. There is no glass-ui path and no package.json. value.js `d40bf9d1` INBOX · `7efcd0bd` record · `ffafdd4b` INBOX + record + LEDGER · `492bc3c0` record · `be39dcbe`/`8e67dd26` record + LEDGER, all in-set. `dev.sh` is not in any commit. **Clean.**
3. **Masking.** ⟨`git diff 41e49af..HEAD -- web/e2e web/src | grep -iE 'timeout|skip|fixme|retry|slow|catch|allow'`⟩ → no `test.skip`/`fixme`/`slow`/retry, and no timeout raise on any test. The `{ timeout: 60_000 }` values move *with* the replaced `networkidle` waits: same budget, a new signal. The one `.catch(() => {})` is inside the evidence probe's value-change wait, and the probe then compares values, so a swallowed wait still prints FAIL. That is INFO, not masking. **Clean.**
4. **Commit families.** One commit per meaning: R-e-1, R-e-2, readiness, evidence, and de-serial each have their own commit. **Clean.**
5. **E-3.** The spec's history is ⟨`git log -- fourier/waves/F-W12.md`⟩ → `9f0614e8` (§0as mint) and `9e3e4d8b` (§0au dated ADDENDUM beside), both COHESION seats, neither this wave's. ⟨`git diff --stat 7efcd0bd~1..HEAD -- …/registry/adjudicated/ scripts/dev/dev.sh`⟩ → empty. **Held.**
6. **Mail.** O-51 is SENT and rowed. I-40 is UNREAD but belongs to Track C F.W1/F.W4 (O-23/O-32), outside F.W12 scope, and is rowed. **0 UNREAD in scope.**
7. **Four-verb line.** The close kept `PARTIAL` and did not claim CLOSED. **Lawful.**
8. **Goal criterion at the bytes.** Two of three limbs are MET: every numeric field steps by keyboard on the producer's `NumberField`, and the stale rows are retired. "The keystone suite exercises `:290` in a full run" is MET literally, because `:330` EXECUTED in every run since `3e709e4`. §3 G-F12-3 and the §0au reading, however, require it green ×2, and that is not met.
9. **Figures.** Published tallies reproduce in kind: close r1 84p·20f·3s; this seat 84p·20f·3s. The visualization-ux ✘ set is `:117 :130 :147 :179 :330` in this seat's run, identical to close r1. **Reproduces.**
10. **Honest-RED adjudication, G-F12-3 full-run leg.**
    - **Producer-owned?** No. It is fourier's own harness, the default 30 s test budget under 9 workers plus host load, so no relay id exists.
    - **Later-wave routing?** None. No spec or LEDGER row declares "Opens after F.W12" and none takes this cure.
    - **Named honest-RED?** None. §4 relieves only a producer-owned RED. §0au (c) relieves `:109`/`:117` alone and explicitly "does not relieve `:322`".
    - **Result: UNRELIEVED.** The record's residual register does name an owner, COHESION via ESC-F12a2-1, and that owner is right. But an escalation that awaits a ruling is not relief.

**Successors.** None declared: ⟨`grep -rln 'after.*F\.W12' fourier/ execution/ EXECUTION-RUNBOOK.md`⟩ → only LEDGER.md (F.W12's own row). Nothing is blocked by this wave.

### Register

| severity | claim | receipt | cure |
|---|---|---|---|
| HIGH | G-F12-3 full-run leg RED with no relief at the spec bytes: `:330` (the spec's `:290`) ✘ 30 s test timeout, plus `:130 :147 :179` new relative to `41e49af` (skipped by `.serial` there). Not producer-owned, not routed to a successor, not a named honest-RED; §0au (c) excludes `:322`. | this seat's full run: 84p·20f·3s, `:330` ✘ 30.1 s at load 32-37; close seat r1/r2 identical class; cumulative `:330` since `3e709e4` = 9 executions, 3 green | COHESION rules ESC-F12a2-1: either (i) extend §0au (c) to the load-timeout class (each passed alone ×2, 18/18 at `.a2`), or (ii) take the reading on a quiesced host. Then re-dispatch the close. No consumer budget lever (§0au (a)). |
| INFO | The evidence probe has a `.catch(() => {})` on its wait. | `arrowup-probe.mjs` value-change wait | None needed. The value comparison still fails loudly. |
| INFO | Full runs rewrite the 21 J-audit PNGs, which sit outside every set. | `git status` (pre-dirty, left unstaged) | Owner close report (§0au). |

**LEDGER.** The row is not moved (NOT-CONFORMANT); one event line was appended.

## Repair 1 (resume — round 1 against Check 1 (resume))

Seat `claude-opus-5-5[1m]`, 2026-09-22. This seat wrote **0** fourier, **0** glass-ui and **0** product bytes. Spec read whole (53 lines incl. ADDENDUM `:51-53`); record read at `## Open (resume)`→`## Unit plan (resume)` and `## Check 1 (resume)` only. Crash-recovery: ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only — nothing inside `.a2`'s set; no inherited partial work.

| sev | defect | cure | commit | gate re-reading |
|---|---|---|---|---|
| HIGH | G-F12-3 full-run leg RED (`:330` + `:130 :147 :179` 30 s timeouts), unrelieved | **ESCALATED — no in-bounds cure exists.** Both cures the check names lie outside this seat: (i) extending §0au (c) is a COHESION ruling; (ii) a quiesced-host reading is not in the seat's control — ⟨`uptime`⟩ → load averages `23.77 30.52 34.86` on ⟨`sysctl -n hw.ncpu`⟩ `18` cores (four tracks live). Every consumer lever is refused by §0au (a) (no `test.slow`, timeout raise or retry); `.serial` is already retired under §0au (b). ⟨`grep -rn 'ESC-F12a2-1' docs/tranches/X/COHESION.md`⟩ → 0 lines: still unruled. | none | not re-run — no cure landed, so no gate could move; banked reading stands (`:330` 9 executions since `3e709e4`, 3 green). |
| INFO | `arrowup-probe.mjs` `.catch(() => {})` on its wait | none needed (the value comparison fails loudly) | none | — |
| INFO | full runs rewrite 21 J-audit PNGs (outside every set) | owner close report (§0au) | none | — |

**Self-count.** 3 defects read · 0 cured · 1 escalated (HIGH, ESC-F12a2-1 carried) · 2 INFO needing no cure. **Row stays `PARTIAL`.** Next act unchanged: a COHESION ruling on ESC-F12a2-1, then re-dispatch the close.

## Check 2 (resume — fresh adversarial L-20 pass 2 of the close at fourier `3e709e4`)

Seat `claude-opus-5-5[1m]`, 2026-09-22, VERIFY-ONLY, independent (authored none of the wave's bytes). Spec read whole (53 lines incl. ADDENDUM `:51-53`); record read at header→`## Unit plan`, `## Open (resume)`→`## Baseline (resume)`, and the last `## Check 1 (resume)` + `## Repair 1 (resume)`; COHESION §0au read whole (`:2774-2794`). Crash-recovery: ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only; value.js dirty rows (`CARRY-LEDGER.md` · `A/X-W6.md` · `dev.sh`) are sibling-owned, none in this set. Trees: ⟨`git log --oneline -1` · `git ls-remote origin m/w1-bump-migration`⟩ → fourier HEAD = origin = `3e709e42`. ⟨`grep -n 'F12a2' COHESION.md`⟩ → 0 lines: **ESC-F12a2-1 still unruled**. Host: ⟨`uptime`⟩ → load `22.71 29.74 34.45` (open) · `24.28 28.82 33.16` (after the runs).

**Verdict: `NOT-CONFORMANT`.** Every claimed GREEN reproduces ×2. The one RED gate, the G-F12-3 full-run leg, reproduces RED in both of this seat's runs, and the spec gives it no relief.

### Gates reproduced (this seat's own commands, fourier `web/` at `3e709e4`)

| gate | command | r1 · r2 | verdict |
|---|---|---|---|
| G-F12-1 imports | `grep -h 'import.*NumberField'` over the 3 files · `grep -cE '<NumberField(\s\|$\|>)'` per file | 3 × `@mkbabb/glass-ui/number-field`; HLG 2 · MPC 1 · Basis 2 = **5** · same | GREEN ×2 |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs chk2a` / `chk2b` (PNGs moved to scratchpad `fw12chk2/`, not committed) | **5/5** (5→4→5 · 50→49→50 · 150→140→150 · 200→199→200 · 1024→896→1024) · **5/5** | GREEN ×2 |
| G-F12-2 | `grep -c level-input e2e/contrast-pairs.ts` · `grep -c AppHeader.vue src/style.css` | 0 · 0 · 0 · 0 | GREEN ×2 |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | 0 · 0 · 0 · 0 | GREEN ×2 |
| G-F12-3 full-run | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` (web `:3100` 200 · api `{"status":"ok"}`) | r1 **85p · 19f · 3s** (1.2m) EXIT 1: `:330` EXECUTED **✓**; ✘ `:117 :130 :147 :179` (30.1-30.2 s, all at `openWorkspace`'s compute `waitForResponse` `:47`). r2 **84p · 20f · 3s** (1.2m) EXIT 1: `:330` EXECUTED **✘ 30.1 s**; ✘ `:117 :130 :147 :179` | **RED** ×2 (`:330` green 1 of 2; `:130 :147 :179` fail in both, and they did not fail at `41e49af`) |
| G-F12-4 | `npm run build` ×2 · `grep -n O-51 INBOX.md` · E13 | exit 0 · exit 0 · O-51 row `:143` present · 0 UNREAD in scope | GREEN ×2 |

**gatesReproduced: 5 of 5 claimed-GREEN limbs.** Logs are in the scratchpad at `fw12chk2/full-r1.log` and `full-r2.log`. The failures outside visualization-ux are the banked load-flaky family: contrast-floor `:82`×2 `:128` · equation-interaction `:77` · fullscreen `:40` · visual-baseline `:57`×3 · visual-checkpoint `:80 :101 :122` in both runs, and gallery-admin-a11y `:91 :103 :114 :125` in r1 only. The runs rewrote the 21 J-audit PNGs again. They were already dirty, sit outside every set, and were left unstaged.

### Axes

1. **GREENs reproduce.** Yes, all 5 limbs ×2 (table above).
2. **Bounds.** ⟨`git -C fourier-analysis diff --stat 41e49af..HEAD`⟩ → 18 files, 270+/152−. ⟨`git show --stat`⟩ per commit:
   - `a9fa5fb`: the 3 components
   - `22ae0c1`: contrast-pairs.ts + style.css
   - `5b75cf0`: resolve-stack.ts + visualization-ux.spec.ts
   - `c5836a7`: `e2e/screenshots/f-w12/**` (probe + 10 PNGs)
   - `3e709e4`: visualization-ux.spec.ts (the `.a2` grant)

   Every path is inside §1 or the ADDENDUM grant. No glass-ui path and no package.json. value.js wave commits `19b8de91`…`11c77bd5` touch only the record, `LEDGER.md` and `INBOX.md`. `dev.sh` is in no commit. **Clean.**
3. **Masking.** ⟨`git diff 41e49af..HEAD -- web/e2e web/src | grep -E '^[+-]' | grep -iE 'timeout|skip|fixme|retry|slow|catch|allow|serial'`⟩ → no `test.skip`/`fixme`/`slow`/retry. The `60_000` values are the budgets the retired `networkidle` waits carried, moved onto the new signal. `test.describe.serial` → `test.describe` is the §0au (b) grant, and its independence proof is receipted at `.a2`. The probe's `.catch(() => {})` is INFO, because the probe's value comparison fails loudly. **Clean.**
4. **Commit families.** One commit per meaning: R-e-1, R-e-2, readiness, evidence and de-serial each have their own commit. **Clean.**
5. **E-3.** ⟨`git diff --stat 19b8de91~1..HEAD -- …/registry/adjudicated/ scripts/dev/dev.sh docs/tranches/X/fourier/waves/`⟩ → only `F-W12.md` +5. That change is `9e3e4d8b`, the COHESION §0au dated ADDENDUM beside the spec, a COHESION seat's act, not this wave's. **Held.**
6. **Mail.** O-51 (the R-e-3 relay) is rowed at INBOX `:143`. I-40 is UNREAD but it is Track C F.W1/F.W4 (O-23/O-32), outside F.W12 scope. **0 UNREAD in scope.**
7. **Four-verb line.** The row has stayed `PARTIAL` through the close and Check 1/Repair 1, and no seat has claimed CLOSED. **Lawful.**
8. **Goal criterion at the bytes.**
   - Numeric fields step by keyboard on the producer's `NumberField`: **MET** (5/5 ×2).
   - Stale rows retired: **MET**.
   - "The keystone suite exercises `:290` in a full run": literally, `:330` EXECUTED in both runs. §3 G-F12-3 and §0au, however, require it green in both with no failure absent at `41e49af`. That is **NOT MET**.
9. **Figures.** This seat's runs (85p·19f·3s, 84p·20f·3s) are the same class as Check 1 (resume) (84p·20f·3s) and the close runs. The ✘ set `:117 :130 :147 :179`, plus `:330` in one run, is identical. **Reproduces.**
10. **Honest-RED adjudication, G-F12-3 full-run leg.**
    - **Producer-owned?** No. The timeouts sit inside fourier's own `openWorkspace` compute wait (`visualization-ux.spec.ts:47`), under the 30 s default test budget with nine workers and host load 24-34. There is no relay id.
    - **Later-wave routing?** None. ⟨`grep -rln 'after.*F\.W12'`⟩ → only F.W12's own LEDGER row, so no successor takes the cure.
    - **Named honest-RED?** None. §4 relieves only a producer-owned RED. §0au (c) relieves `:109` (now `:117`) alone and states it "does not relieve `:322`" (now `:330`). `:130 :147 :179` are named by no relief at all.
    - **Result: UNRELIEVED.** The residual register names COHESION as the owner through ESC-F12a2-1, and that owner is correct. But an unruled escalation is not relief.

**Successors.** None declared. No spec or runbook row reads "Opens after F.W12", so no successor is blocked by this wave.

### Register

| severity | claim | receipt | cure |
|---|---|---|---|
| HIGH | The G-F12-3 full-run leg is RED and the spec bytes give it no relief. `:330` (the spec's `:290`) is green in only 1 of 2 full nine-worker runs (✘ 30.1 s test timeout in r2). `:130 :147 :179` fail in both runs; at `41e49af` they were skipped by `.serial`, not failing, so they are failures absent at `41e49af`. None of this is producer-owned or routed to a successor, and it is not a named honest-RED: §0au (c) covers `:109`/`:117` only and excludes `:322`. | This seat's r1 85p·19f·3s (`:330` ✓) and r2 84p·20f·3s (`:330` ✘ 30.1 s). All four `:117–:179` failures time out at `openWorkspace` `waitForResponse` (`:47`). Host load 22-34 on 18 cores. Cumulative `:330` since `3e709e4`: 11 executions, 4 green. | COHESION rules ESC-F12a2-1: either (i) extend §0au (c) to the load-timeout class (the tests passed alone ×2, 18/18 at `.a2`), or (ii) take the reading on a quiesced host. Then re-dispatch the close. No consumer budget lever: §0au (a) refuses it. |
| INFO | `arrowup-probe.mjs` has a `.catch(() => {})` on its wait. | The probe's value-change wait. | None needed. The value comparison still fails loudly. |
| INFO | Full runs rewrite the 21 J-audit PNGs, which sit outside every set. | `git status` (already dirty, left unstaged). | Owner close report (§0au). |

**Honest-RED set:** none. The only RED gate is unrelieved.

**Self-count.** 3 defects: 1 HIGH · 0 MINOR · 2 INFO. 5 GREEN limbs reproduced ×2. 1 gate failed (G-F12-3 full-run leg).

**LEDGER.** The row is not moved (NOT-CONFORMANT) and stays `PARTIAL`. One event line is appended.

## Repair 2 (resume — round 2 against Check 2 (resume))

Seat `claude-opus-5-5[1m]`, 2026-09-22. This seat wrote **0** fourier, **0** glass-ui and **0** product bytes. Spec read whole (53 lines incl. ADDENDUM `:51-53`); record read at header→`## Unit plan`, `## Repair 1 (resume)` and `## Check 2 (resume)` only. Crash-recovery: ⟨`git -C fourier-analysis status --porcelain`⟩ → 21 × J-audit PNGs + `?? .worktrees/` — nothing inside `.a2`'s set; no inherited partial work. Trees: ⟨`git -C fourier-analysis log --oneline -1`⟩ → `3e709e4` (unmoved since Check 2).

| sev | defect | cure | commit | gate re-reading |
|---|---|---|---|---|
| HIGH | G-F12-3 full-run leg RED (`:330` green 1 of 2; `:130 :147 :179` fail ×2, absent at `41e49af`), unrelieved | **ESCALATED — still no in-bounds cure.** (i) Extending §0au (c) to the load-timeout class is a COHESION ruling. ⟨`grep -c F12a2 docs/tranches/X/COHESION.md`⟩ → `0`, and ⟨`grep -n '^## §0a[u-z]' COHESION.md`⟩ → `:2774` §0au only (no later ruling). (ii) A quiesced-host reading is not in this seat's control: ⟨`uptime`⟩ → load `20.02 26.57 32.01` on ⟨`sysctl -n hw.ncpu`⟩ `18` cores. §0au (a) refuses every consumer lever (no timeout raise, `test.slow` or retry). | none | not re-run: no cure landed, so no gate could move. Check 2's r1/r2 (85p·19f·3s / 84p·20f·3s) stand |
| INFO | `arrowup-probe.mjs` `.catch(() => {})` on its wait | none needed (the value comparison fails loudly) | none | — |
| INFO | full runs rewrite 21 J-audit PNGs (outside every set) | owner close report (§0au) | none | — |

**Self-count.** 3 defects read · 0 cured · 1 escalated (HIGH, ESC-F12a2-1 carried) · 2 INFO needing no cure. **Row stays `PARTIAL`.** Next act unchanged: a COHESION ruling on ESC-F12a2-1 (or a quiesced-host reading), then re-dispatch the close `.b2`.

## Check 3 (resume — fresh adversarial L-20 pass 3 of the close at fourier `3e709e4`)

Seat `claude-opus-5-5[1m]`, 2026-09-22, VERIFY-ONLY, independent (authored none of the wave's bytes; wrote 0 fourier, 0 glass-ui, 0 product bytes). Spec read whole (53 lines incl. ADDENDUM `:51-53`); record read at header→`## Unit plan`, `## Open (resume)`→`## Baseline (resume)`, `## Close (resume)` + `## Close (resume, close seat)`, and the last `## Check 2 (resume)` + `## Repair 2 (resume)`. Crash-recovery: ⟨`git -C fourier-analysis status --porcelain | grep -v J/audit`⟩ → `?? .worktrees/` only; value.js dirty rows (`CARRY-LEDGER.md` · `dev.sh`) are sibling-owned / never-touch, none in this set. Trees: ⟨`git log --oneline -1` · `git ls-remote origin m/w1-bump-migration`⟩ → fourier HEAD = origin = `3e709e42` (unmoved). Ruling: ⟨`grep -n 'F12a2' COHESION.md`⟩ → 0 lines; ⟨`git log --oneline -1 -- COHESION.md`⟩ → `9e3e4d8b` (§0au) — **ESC-F12a2-1 still unruled**. Host: ⟨`uptime`⟩ → `16.39 25.16 31.32` at open · `27.59` before r2 · `40.76` after r2 on 18 cores; sibling csc411 Playwright (webkit + chromium headless) live throughout — the host was not quiesced.

**Verdict: `NOT-CONFORMANT`.** Every claimed GREEN reproduces ×2; the G-F12-3 full-run leg reproduces RED ×2 and the spec bytes give it no relief.

### Gates reproduced (this seat's own commands, fourier `web/` at `3e709e4`)

| gate | command | r1 · r2 | verdict |
|---|---|---|---|
| G-F12-1 imports | `grep -h 'import.*NumberField'` over the 3 files · `grep -cE '<NumberField(\s\|$\|>)'` per file | 3 × `{ NumberField, NumberFieldInput } from "@mkbabb/glass-ui/number-field"`; HLG 2 · MPC 1 · Basis 2 = **5** (re-read identical) | GREEN ×2 |
| G-F12-1 probe | `node e2e/screenshots/f-w12/arrowup-probe.mjs chk3a` / `chk3b` (PNGs moved to scratchpad `fw12chk3/`, not committed; porcelain clean after) | **5/5** (5→4→5 · 50→49→50 · 150→140→150 · 200→199→200 · 1024→896→1024) · **5/5** | GREEN ×2 |
| G-F12-2 | `grep -c level-input e2e/contrast-pairs.ts` · `grep -c AppHeader.vue src/style.css` | 0 · 0 (re-read identical) | GREEN ×2 |
| G-F12-3 grep | `grep -c networkidle e2e/visualization-ux.spec.ts e2e/resolve-stack.ts` | 0 · 0 (re-read identical) | GREEN ×2 |
| G-F12-3 full-run | `BASE_URL=http://localhost:3100 npx playwright test --workers=9 --reporter=list` (web `:3100` 200 · api `{"status":"ok"}`) | r1 **85p · 19f · 3s** (1.3m) EXIT 1: `:330` EXECUTED **✘ 30.1 s**; ✘ `:117 :147 :179`. r2 **83p · 21f · 3s** (1.3m) EXIT 1: `:330` EXECUTED **✘ 30.1 s**; ✘ `:117 :130 :147 :179 :216` | **RED ×2** (`:330` green 0 of 2; `:147 :179` fail both, absent at `41e49af`) |
| G-F12-4 | `npm run build` ×2 · `grep -n O-51 INBOX.md` · E13 | exit 0 · exit 0 · O-51 row `:143` · 0 UNREAD in scope | GREEN ×2 |

**gatesReproduced: 5 of 5 claimed-GREEN limbs.** Logs: scratchpad `fw12chk3/full-r1.log`, `full-r2.log`. Outside visualization-ux the ✘ set is the banked load-flaky family (contrast-floor `:82`×2 `:128` · equation-interaction `:77` · fullscreen `:40` · gallery-admin-a11y `:91 :103 :114 :125` · visual-baseline `:57`×3 · visual-checkpoint `:80 :101 :122`), identical in both runs. The runs rewrote the 21 J-audit PNGs again (already dirty, outside every set, left unstaged).

### Axes

1. **GREENs reproduce.** Yes — all 5 limbs ×2 (table above).
2. **Bounds.** ⟨`git -C fourier-analysis diff --stat 41e49af..HEAD`⟩ → 18 files, 270+/152−; non-evidence paths = the 3 components · `contrast-pairs.ts` · `resolve-stack.ts` · `visualization-ux.spec.ts` · `style.css`; the rest `e2e/screenshots/f-w12/**`. All inside §1 or the ADDENDUM `.a2` grant; no glass-ui path, no `package.json`. `dev.sh` in no wave commit. **Clean.**
3. **Masking.** ⟨`git diff 41e49af..HEAD -- web/e2e web/src | grep '^+' | grep -iE 'test\.skip|fixme|slow\(|retries|allow'`⟩ → empty. **Clean** (the probe's `.catch(() => {})` stays INFO: its value comparison fails loudly).
4. **Commit families.** One commit per meaning (R-e-1 `a9fa5fb` · R-e-2 `22ae0c1` · readiness `5b75cf0` · evidence `c5836a7` · de-serial `3e709e4`). **Clean.**
5. **E-3.** ⟨`git diff --stat 19b8de91~1..HEAD -- …/registry/adjudicated/ scripts/dev/dev.sh docs/tranches/X/fourier/waves/`⟩ → only `F-W12.md` +5 = the COHESION §0au dated ADDENDUM (`9e3e4d8b`, a COHESION act beside the spec). **Held.**
6. **Mail.** BK still newest (`ls -dt` → BK · BJ); the 4 BK letters dated 2026-09-22 are rowed; O-51 SENT at `:143`; I-40 UNREAD is Track C F.W1/F.W4, outside F.W12 scope. **0 UNREAD in scope.**
7. **Four-verb line.** Row stays `PARTIAL`; no seat claimed CLOSED. **Lawful.**
8. **Goal criterion at the bytes.** NumberField keyboard stepping **MET** (5/5 ×2); stale rows retired **MET**; "`:290` in a full run" — EXECUTED ×2 but the §3/§0au bar (green in both, no failure absent at `41e49af`) **NOT MET**.
9. **Figures.** This seat's runs (85p·19f·3s · 83p·21f·3s) sit in the class the close and Checks 1-2 published (84-85p · 19-21f · 3s); the ✘ set recurs (`:117 :147 :179 :330` both runs; `:130 :216` in r2). **Reproduces.**
10. **Honest-RED adjudication — G-F12-3 full-run leg.** Producer-owned? **No** — the timeouts are fourier's own 30 s default test budget at `openWorkspace`'s compute wait under nine workers + host load; no relay id. Later-wave routing? **None** (no spec reads "Opens after F.W12"). Named honest-RED? **None** — §4 relieves only a producer-owned RED; §0au (c) relieves `:109`/`:117` alone and says it "does not relieve `:322`" (`:330`); `:130 :147 :179 :216` are named by no relief. **UNRELIEVED.** The residual register names COHESION (via ESC-F12a2-1) as owner — correct owner, but an unruled escalation is not relief.

**Successors.** None declared (no spec/runbook row reads "Opens after: F.W12"); no successor is blocked by this wave.

### Register

| severity | claim | receipt | cure |
|---|---|---|---|
| HIGH | G-F12-3 full-run leg RED, no relief at the spec bytes: `:330` (spec `:290`) ✘ in both full nine-worker runs; `:147 :179` fail in both (skipped, not failing, at `41e49af` → failures absent at `41e49af`); not producer-owned, not successor-routed, not a named honest-RED (§0au (c) excludes `:322`) | r1 85p·19f·3s, r2 83p·21f·3s, `:330` ✘ 30.1 s both; load 16→41 on 18 cores, sibling csc411 Playwright live; cumulative `:330` since `3e709e4`: 13 executions, 4 green | COHESION rules ESC-F12a2-1 — (i) extend §0au (c) to the load-timeout class (each passed alone ×2, 18/18 at `.a2`) or (ii) read the leg on a quiesced host — then re-dispatch `.b2`. §0au (a) refuses every consumer lever |
| INFO | `arrowup-probe.mjs` `.catch(() => {})` on its wait | the probe's value-change wait | none (the comparison fails loudly) |
| INFO | full runs rewrite the 21 J-audit PNGs (outside every set) | `git status` (already dirty, unstaged) | owner close report (§0au) |

**Honest-RED set:** none — the only RED gate is unrelieved.

**Self-count.** 3 defects: 1 HIGH · 0 MINOR · 2 INFO. 5 GREEN limbs reproduced ×2. 1 gate failed (G-F12-3 full-run leg).

**LEDGER.** Row not moved (NOT-CONFORMANT); stays `PARTIAL`. One event line appended.
