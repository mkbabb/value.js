SERVED MODEL: claude-opus-5-5

# X.F.W13 — execution record (Track C · X·F)

Spec: `docs/tranches/X/fourier/waves/F-W13.md` (48 lines, read whole). Minted by COHESION §0bc (2026-09-23).
Authority: the owner's 2026-09-23 fourier + dock docket (verbatim in spec §Authority; rows OA-11..OA-17);
frames `docs/tranches/X/fourier/evidence/W13/owner-2026-09-23-{1..5}.png` (all five read by this seat).
Sitting of record: 2026-09-17 (the owner's begin-word, COHESION §0j); calendar 2026-09-23.
Model law: Opus 5.5 for every seat (owner 2026-09-23, COHESION §0bc; Fable not used).

## Open

SEAT 0, `claude-opus-5-5`, VERIFY-AND-BANK — zero fourier bytes, zero glass-ui bytes.

### Crash-recovery sweep
- ⟨`git -C ../fourier-analysis status --porcelain`⟩ → 22 rows: 21 ` M docs/tranches/J/audit/screenshots/**` (the
  `visual-baseline` rewrite residue, COHESION §0aw operator item — NOT in this wave's writable set, not touched)
  + `?? .worktrees/`. **Zero dirty paths under `web/src/**` or `web/e2e/**`** → no inherited partial work.
- value.js porcelain: ` M docs/tranches/V/reformation/CARRY-LEDGER.md` (sibling seat's) · ` M scripts/dev/dev.sh`
  (standing, never touched). Neither in this seat's set.

### Preconditions ("Opens after: F.W12 CLOSED")
- LEDGER row F.W12: `**CLOSED 2026-09-17 (honest-RED: G-F12-3-LOAD)** ⟵ CHECK 1 (RESUME 2, L-20 pass 1)
  CONFORMANT-HONEST-RED` — MET (COHESION §0aw).
- Tree: ⟨`git -C ../fourier-analysis branch --show-current; git log --oneline -1`⟩ → `m/w1-bump-migration` ·
  `3e709e4` = spec baseline; `origin/m/w1-bump-migration` → `3e709e42` (in sync). MET.
- LEDGER row F.W13 read `planned` before this seat (no prior record; not RESUME).
- Owner-gated items: none open. §0bc rules OA-11/OA-12 a PRODUCER row (relay **O-55**, SENT, INBOX row O-55);
  `.d` is producer-gated by the spec's own text — adopt if installable, else `DOCK-SCROLL-MORPH` honest-RED.

### E13 Step-0 mail sweep (four paths)
- Reference clock: the X-W7 CLOSE sweep (06:32:48). ⟨`find <path> -maxdepth 1 -type f -newermt 2026-09-23T06:32:48`⟩ →
  `V/coordination/INBOX.md` (itself) · glass `BK/coordination/valuejs-outbound-2026-09-23-dock-scroll-morph-relay.md`
  (OUR O-55 mirror, already rowed) · glass `BL/CHARTER.md` + `BL/FORMATION-PROGRESS.md` · empty on `V/`,
  keyframes `V/coordination`, atlas `P/coordination`.
- Glass newest tranche dir: **BL** now exists (charter 09:38 today) but has **no `coordination/`**; BL/*.md are
  glass's own formation docs (⟨`grep -ni "value.js|valuejs|scroll|O-55" BL/*.md`⟩ → one line, the owner brief
  "look over … the megatranche in value.js" — not mail addressed to value.js). **BK remains the live inbox**; O-55
  is filed there "fold into BL". Atlas newest dir `U` (no `coordination/`).
- **0 new I-n · 0 UNREAD in F.W13 scope.** Sweep line appended to INBOX.md.
- UNREAD rows scope-checked (O-20 · I-30 · I-31 · I-32 · I-35 · O-39 · I-40). **I-40 is SCOPE-ADJACENT**: the glass
  O-23/O-32 reply (`BK/coordination/glass-outbound-2026-09-22-fourier-o23-o32-reply.md`) at `:262-273` rules
  `useSidebarFollow` exempts by `data-sidebar-follow-exempt` (10.0.0), not the `.sidebar-top-btn` class, and
  `useScrollTracker` reads `rootId` — the paper sidebar's follow/state seam that frame 1's CONTENTS toggle sits on.
  Assigned to **`.a`**: read `:255-280` while naming the toggle's cause, and mark I-40's status cell (READ, with the
  disposition) in the same sitting. The other six rows are outside F.W13's scope (X·V / X·KF / cross-program).

- COHESION read to the file end: §0bd (2026-09-23, same day) — O-55 was ALSO sent live to glass session `glass-ui-9d`
  (msg `5be9806b`) beside O-56 (value.js docket; its G-1 "dock morph" is the same producer half). Producer halves carry
  honest-RED ids `DOCK-SCROLL-MORPH` / `DOCK-MORPH-ROOT` until glass ships. `.d` re-reads BK/BL for a reply at its sitting.

- §0be (landed during this sitting, `f2798787`): glass BL (session `glass-ui-9d`) ACKNOWLEDGED O-53..O-56, banked at glass
  `a97a9ddd`, rowed in glass `BL/audit/INBOUND.md`; landing version fixed when BL's PLAN forms (likely a major above
  10.0.1) — our honest-RED ids stand until then (the ack is §0be-rowed, not a new I-n). §0be also rules the
  **served-page instrument rule** for KF.W13U (owner observation reproduced on the served page; harness-only GREEN
  does not count). F.W13's owner frames are served-page observations too: INFO to every unit — reproduce each frame on
  the served fourier dev page before curing, and pair each e2e gate with the before/after served-page crop.

## Baseline (BEFORE — read-only, fourier `3e709e4`, 2026-09-23)

| gate | command | BEFORE | reading |
|---|---|---|---|
| G-a | `ls web/e2e/f-w13-radius.spec.ts` | **RED** | spec absent (born-RED) |
| G-b | `ls web/e2e/f-w13-image-controls.spec.ts` | **RED** | spec absent (born-RED) |
| G-c | `ls web/e2e/f-w13-image-empty.spec.ts` | **RED** | spec absent (born-RED) |
| G-d / `DOCK-SCROLL-MORPH` | producer probe (below) | **RED** | no scroll-compact / dock progress seat at installed 8.0.0 nor at glass src 10.0.1 |
| screenshots | `ls web/e2e/screenshots/f-w13` | **RED** | dir absent |
| typecheck | `npm run typecheck` | **SPEC-LITERAL DEFECT** | `npm error Missing script: "typecheck"` — web/package.json has no such script; the equivalent is the build's first leg `npx vue-tsc -b` (below). Units read typecheck as `npx vue-tsc -b` (no script is added: `package.json` is outside §1 bounds except as `.d` states). |
| vue-tsc | `npx vue-tsc -b` ×2 | GREEN | run1 exit=0 errors=0 · run2 exit=0 errors=0 |
| unit | `npx vitest run` ×2 | GREEN | 13 files / 84 tests passed ×2 |
| existing e2e | `npx playwright test` | CITED, not re-run | banked at F.W12 close (COHESION §0aw): every product gate GREEN ×2; full-run leg honest-RED by instrument `G-F12-3-LOAD` (host load). "Unchanged-GREEN" is read per unit against the specs it touches + the F.W12 figure; a full run needs a quiesced host (§0av bar). |

Receipts (quote-by-command):
- ⟨`for f in f-w13-radius f-w13-image-controls f-w13-image-empty; do ls e2e/$f.spec.ts; done`⟩ → `No such file or directory` ×3; ⟨`ls e2e/screenshots/f-w13`⟩ → `No such file or directory`.
- ⟨`grep '"version"' web/node_modules/@mkbabb/glass-ui/package.json`⟩ → `8.0.0`; web pin `"@mkbabb/glass-ui": "^8.0.0"`; ⟨`npm view @mkbabb/glass-ui versions`⟩ → tail `9.0.0, 10.0.0, 10.0.1`.
- ⟨`grep -rlE 'compactOnScroll|scrollCompact|progressRim' node_modules/@mkbabb/glass-ui/dist | wc -l`⟩ → `0`; same over `../glass-ui/src/components/dock` (10.0.1) → `0`; `ScrollProgressRim` ships standalone (`dist/scroll-progress-rim.js`). `useDockState` = `collapsed | hover | pinned` (COHESION §0bc). O-55 SENT; glass BL formation opened today, no reply.
- Reference named by the spec (`words frontend/src/components/custom/search/composables/useSearchBarScroll.ts`): ⟨`find ~/Programming -maxdepth 6 -name useSearchBarScroll.ts`⟩ → nothing locally (no floridify checkout under `~/Programming`). INFO for `.d`: the reference rides O-55; this wave writes no consumer copy of it regardless.
- **greenBeforeCure: none** (every wave gate born-RED; vue-tsc/vitest GREEN are regression floors, not cures).

## Unit plan

4 units, **strictly serial** (spec §State Agents line; COHESION §0bc): `[.a] → [.b] → [.c] → [.d]`, one at a time
(`.b` and `.c` both modify `ImageUpload.vue` and its siblings; `.a` and `.b` both may touch `style.css`). Every seat
Opus 5.5. Tree `../fourier-analysis` `m/w1-bump-migration`; glass-ui READ-ONLY (producer asks ride O-55 / BK mail
from the value.js relay seat, never frontend hacks). ESCALATED units do not halt the wave. Common: pathspec commits,
one commit per meaning; `web/package.json` untouched except by `.d` (a glass bump, one commit, only if the ask is
installable); typecheck = `npx vue-tsc -b` (spec-literal defect above); the existing e2e specs a unit touches re-run
GREEN ×2; before/after screenshots at the owner's crops under `web/e2e/screenshots/f-w13/`.

| unit | model | sections | writable | gates | locks |
|---|---|---|---|---|---|
| F.W13.a | opus | §1 (l.18-21) · §2 F.W13.a (l.24-32) · §3 (l.47-48) | `web/src/**` · `web/e2e/f-w13-radius.spec.ts` · `web/e2e/screenshots/f-w13/**` · value.js `C/F-W13.md` · `LEDGER.md` · `INBOX.md` (I-40 cell only) | G-a ×2 · vue-tsc 0 · vitest 84/84 · touched e2e GREEN ×2 | no per-instance override where a root/primitive owns it; I-40 marked |
| F.W13.b | opus | §2 F.W13.b (l.34-36) · §3 | `web/src/**` · `web/e2e/f-w13-image-controls.spec.ts` · `web/e2e/screenshots/f-w13/**` · `C/F-W13.md` · `LEDGER.md` | G-b ×2 · census re-run 0 hand-rolled · vue-tsc 0 · vitest | serial after `.a` |
| F.W13.c | opus | §2 F.W13.c (l.38-41) · §3 | `web/src/**` · `web/e2e/f-w13-image-empty.spec.ts` · `web/e2e/screenshots/f-w13/**` · `C/F-W13.md` · `LEDGER.md` | G-c ×2 (incl. PRM context) · vue-tsc 0 · vitest | serial after `.b` (same files) |
| F.W13.d | opus | §2 F.W13.d (l.43-45) · §3 | `web/package.json` + lockfile (bump only if installable) · `web/src/**` · `web/e2e/f-w13-dock*.spec.ts` · `web/e2e/screenshots/f-w13/**` · `C/F-W13.md` · `LEDGER.md` · `INBOX.md` | G-d ×2 or `DOCK-SCROLL-MORPH` honest-RED (owner glass, O-55) | NO consumer copy of the behaviour (SS-6); bump one commit |

Briefs (as dispatched):
- **.a** — Paste computed `border-radius` per surface: paper sidebar CONTENTS header + collapse button (frame 1), the back/undo control (frame 2), visualizer panels Function/Controls/Coefficients/equation card/plot card (frame 5). Re-point each to glass radius tokens / surface primitives (read Card/Surface/Button README+types whole); icon-only controls = producer icon-button, true circle. Reproduce frame 1 toggle + frame 2 control failure, name cause at bytes (read I-40 reply :255-280 on useSidebarFollow/rootId; mark I-40), cure at owner. Write f-w13-radius.spec.ts (radius on scale, circles, toggle collapses, back navigates) ×2.
- **.b** — Walk every control in image mode sidebar+main (ImageUpload.vue and siblings): act+observe, primitive? Paste table. Cure each non-working control at its owner; move hand-rolled fields/buttons/sections onto glass primitives. Write f-w13-image-controls.spec.ts: each control acts ×2; census re-run 0 hand-rolled.
- **.c** — Retire the sidebar "Drop or click to upload — PNG/JPG/SVG ≤ 10 MB" card; main-area drop target is the ONE affordance (click-to-browse + format/size line, producer type). No image → main full width, NO sidebar; dragenter signals; drop/pick → sidebar enters with producer motion tokens (width/translate, PRM-honoured); clear → leaves. Write f-w13-image-empty.spec.ts per spec G-c ×2 incl. PRM.
- **.d** — Re-probe installed and latest glass (npm view + dist grep + BK/BL reply to O-55) for dock scroll-compact + progress seat. If installable within the app's major policy: bump (one commit), wire page scroll to rim, gate G-d (400px scroll → compact; hover/focus → expanded; rim = fraction ±2%; clipped to radius) ×2. Else NO consumer copy: record probe, G-d honest-RED `DOCK-SCROLL-MORPH` (owner glass, O-55), carried to X-W8 + kf adoption row.

## Unit receipts

### F.W13.a

SEAT `.a`, `claude-opus-5-5` (served model; Opus 5.5 per §0bc). Spec read whole (48 lines); record read header → `## Unit plan`;
COHESION §0bc · §0bd · §0be read; I-40 reply `:255-280` read. Tree fourier `m/w1-bump-migration` @ `3e709e4`.

**Crash-recovery.** ⟨`git -C ../fourier-analysis status --porcelain`⟩ → 21 ` M docs/tranches/J/audit/screenshots/**` + `?? .worktrees/`
(not in this set, untouched); **0 dirty paths under `web/src/**` / `web/e2e/**`** → no inherited partial work. value.js: the two standing rows only.

**1. Served-page reproduction (BEFORE, `http://localhost:3100`, vite of this tree; 1440×900 dark; probe = playwright `getComputedStyle`).**

| frame | surface | computed BEFORE | owner of the shape | cause at the bytes |
|---|---|---|---|---|
| 1 | CONTENTS header `.sidebar-header` | `0px` (196×40, a text row, no plate) | — (the plate is `.sidebar-nav`, `12px` = `--radius-panel`) | — |
| 1 | the control beside CONTENTS `.sidebar-top-btn` | **`4px`** on 40×40 | glass `Button icon-only` (`border-radius: calc(var(--button-size)/2)`, size = `--button-size` square) | `PaperSidebar.vue` `.sidebar-top-btn{border-radius:var(--radius-sm); border:1px solid…; background:none}` — unlayered per-instance override of the producer's geometry |
| 1 | section disclosures `.sidebar-disclosure` | **`8px`** on 40×40 | same | `.sidebar-disclosure{border-radius:calc(var(--radius) - 2px); background:none; border:none}` |
| 2 | history control `.overlay-back` | `9999px` on **32×40** — a stadium | glass `Button icon-only` | `PaperView.vue` `.overlay-btn{width:2rem;height:2rem}` UNLAYERED vs the producer's layered `min-block-size:var(--button-size)` (40px) → only the inline axis shrank; plus restated border/fill/blur/shadow/hover-lift/press and a re-added focus outline |
| 5 | Function panel · Controls panel (`FunctionInput.vue` `.cartoon-card`) | **`0px`** · **`0px`** | glass `--radius-card` (Card / `.glass-card`) | `style.css` `@utility cartoon-card{@apply cartoon-surface; border-color; background}` — the shim never carried a radius |
| 5 | equation card `.eq-card` · plot card | **`0px`** · **`0px`** | same | same shim |
| 5 | Coefficients `ConfiguratorLayer` | `16px` | glass ConfiguratorLayer | already on scale (the reference) |

Resolved tokens on the page: `--radius-card` = `var(--radius-2xl)` = 16px; `--radius-panel` = 12px (glass 8.0.0 `dist/styles`).
Producer reads (whole): glass README (198 l.), `button/Button.vue.d.ts` + `button/styles.css`, `card/Card.vue.d.ts` + `card/styles.css`,
`surface/Surface.vue.d.ts`, `collapsible/*.d.ts`.

**2. The two controls — reproduced, cause named.**
- Frame 1 ("it doesn't even work"). ⟨probe: paper `scrollTop` 6000 → click `.sidebar-top-btn` → 0⟩: the control DID scroll the paper — but
  it is `@click="scrollToTop"` (`useScrollNavigation.ts:490`), a scroll-to-top of the PAPER wearing a `ChevronUp` in the CONTENTS header's
  disclosure seat. At the paper's top (where the ToC is read, owner frame 1: the search pill above it) it is a no-op, and it never acts on the
  list it sits on. Cause = the control's meaning, not a dead handler. The spec's own words name it the CONTENTS toggle ("toggle collapses the
  contents"); cured at the owner (`PaperSidebar.vue`): header + list wrapped in the glass `Collapsible` (`v-model:open`, default open),
  the control is a `CollapsibleTrigger as-child` → glass `Button emphasis=quiet size=md icon-only`, `aria-label="Contents"`, `aria-expanded`
  from the producer; the chevron turns on `[aria-expanded="false"]` (NOT `data-state` — measured: the Tooltip trigger stamps its own
  `data-state="closed"` on the same node and wins it). The per-instance `.sidebar-top-btn` block and the `.sidebar-disclosure`
  radius/border/fill/hover overrides retire.
- I-40 (`:255-280`) consumed: the follow in use is latex-paper's `useSidebarFollow` (`dist/vue.js:602` exempts `[data-toc-id], .sidebar-top-btn`);
  glass 10.0.0 moves its twin to `data-sidebar-follow-exempt`. The renamed toggle (`.sidebar-contents-toggle`) needs no exemption: its
  pointerdown sets `manualOverride`, which the next paper scroll clears (`scheduleFromScroll`, `vue.js:583-586`) — read at the bytes, not
  assumed. `rootId`/`parentId` (§3.4): nothing in this unit reads `parentId`. I-40 status cell → READ (value.js `16a93175`).
- Frame 2. ⟨probe: click ToC row 5 → `scrollTop` 86488, `.overlay-back` present → click → 215, control gone⟩: the back control WORKS; its
  defect is the owner's own word for frame 2 — "not a circle" (32×40, cause above). Cured at the owner (`PaperView.vue`): `.overlay-btn`
  keeps layout only (`pointer-events:auto; position:relative` for the badge); geometry, ink, shadow, hover, press and focus ring return to the Button.
- Frame 5: `cartoon-card` takes `border-radius: var(--radius-card)` at its ONE root (`style.css`) — no site restates it.

**3. Commits (fourier, pathspec, one per meaning; pushed `origin/m/w1-bump-migration` → `7d55e75`).**
`83a723d` PaperSidebar (OA-13) · `1a4631a` PaperView (OA-14) · `3c4f720` style.css (OA-17) · `7d55e75` G-a spec + 6 crops
(`web/e2e/screenshots/f-w13/frame{1-contents,2-back,5-equation}-{before,after}.png`; `-f` past `web/.gitignore:1 *.png`, as F.W11 did).
Before crops taken from a detached scratch worktree of `3e709e4` served on :3177 (removed after); after crops from :3100.
value.js: `16a93175` (INBOX I-40 cell).

**4. AFTER (served page).** toggle 40×40 `9999px` (the glass `disclosure-trigger` `--radius-control`) · section disclosures 40×40 `9999px` ·
back 40×40 `20px` · Function / Controls / equation card / plot card `16px` · Coefficients `16px` · toggle: `aria-expanded` true → click →
false, `[data-toc-id]` rows 0, nav 792 → 134 px → click → true, rows back.

**5. Gates (BEFORE → AFTER).**

| gate | command | BEFORE | AFTER |
|---|---|---|---|
| G-a | `BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-radius.spec.ts --project=chromium` ×2 | absent (born-RED); the same spec against the `3e709e4` tree on :3177 → **0/3** (frame 1: no "Contents" button; frame 2: `w−h` 7.94 > 0.5; frame 5: Function radius 0 ≠ 16) | **3/3 ×2** (3.7 s · 3.8 s) |
| vue-tsc | `npx vue-tsc -b` ×2 | 0 errors | **exit 0, 0 errors ×2** |
| unit | `npx vitest run` ×2 | 13 files / 84 | **13 files / 84 passed ×2** |
| touched e2e | `paper-performance` + `paper-search` (chromium, 4 workers) ×2 | — | **8/8 ×2** |
| touched e2e | `equation-interaction` ×2 | — | **1/1 ×2** |
| touched e2e | `visual-checkpoint.spec.ts` ×2 | at `3e709e4` (:3177): items 1·6·7, 2, 5 **✘** (pre-existing, gallery snapshots — not this unit's surfaces); item 3 ✓ | items 1·6·7, 2, 5 ✘ (unchanged, pre-existing) · **item 3 `checkpoint-disclosure-body.png` ✘ ×2 — 440 px (ratio 0.01), the diff is ONLY the four rounded corners of the Function and Controls panels**, i.e. the owner-ordered cure itself |
| crops | `ls web/e2e/screenshots/f-w13` | absent | 6 files (frames 1, 2, 5 before/after) |

**Escalation ESC-F13a-1 (bounds).** `visual-checkpoint.spec.ts:163` item 3 checkpoints `.eq-panel-left` — the two panels OA-17 orders
rounded — against a snapshot that encodes their square corners. Its re-baseline (`web/e2e/visual-checkpoint.spec.ts-snapshots/
checkpoint-disclosure-body-chromium-darwin.png`, `--update-snapshots` on that one test) is outside this unit's writable set
(`web/e2e/f-w13-radius.spec.ts` + `screenshots/f-w13/**`), so it is NOT written here. Ask: grant that one path (the diff is the cure, 440 px
at the corners, banked read of `…-diff.png`) or rule it to `.b`/close. Items 1·6·7, 2, 5 are RED at the baseline tree too — outside F.W13.

**Residuals.** (r1) The desktop ToC no longer carries a scroll-to-top (the control is the disclosure the spec names); the mobile ToC keeps
its own (`MobileFloatingToc.vue:73`) — INFO for the owner report. (r2) `.sidebar-link` rows keep their list-row `calc(var(--radius) - 2px)`
(text rows, not icon-only; not in frames 1/2/5) — census INFO. (r3) Two other `cartoon-card` sites carry their own radius utility:
`BatchActionBar.vue:82` `rounded-card` (now redundant with the root) and `AdminUserList.vue:590` `rounded-lg` — outside frames 1/2/5, left
for `.b`'s census. (r4) `data-sidebar-follow-exempt` + `rootId` adoption ride the glass 10.x bump (I-40), not the 8.0.0 pin.

### F.W13.b

SEAT `.b`, `claude-opus-5-5` (served model; Opus 5.5 per §0bc). Spec read whole (48 lines); record read header → `## Unit plan` + the
`.a` receipt (method: served :3100, crops, ESC-F13a-1); COHESION §0bc · §0bd · §0be read to the file end. Tree fourier `m/w1-bump-migration` @ `7d55e75`.

**Crash-recovery.** ⟨`git -C ../fourier-analysis status --porcelain`⟩ → the 21 ` M docs/tranches/J/audit/screenshots/**` + `?? .worktrees/`
(not in this set, untouched); **0 dirty paths under `web/src/**` / `web/e2e/**`** → no inherited partial work.

**1. Served-page census (BEFORE, `http://localhost:3100`, vite of this tree at `7d55e75`; 1440×900 dark; probe = playwright, every
`button, input, select, [role=button|slider|combobox|spinbutton]` under `.viz-panel-left-wrap` + `.canvas-stage`, acted and read back).**
Owner frame 3 reproduced at rest (Image layer + the upload strip + the canvas "Drag & drop an image here" target); the loaded state
(golden-retriever upload → `/w/…`) carries the rest of the image mode's controls.

| # | control (owner file) | acted → observed (BEFORE) | primitive? (BEFORE) | cure (AFTER) |
|---|---|---|---|---|
| 1 | Image layer disclosure (`ImageUpload.vue`) | click → `aria-expanded` true→false→true ✓ | glass `ConfiguratorLayer` ✓ | — |
| 2 | upload strip "Drop or click to upload…" (`ImageUpload.vue`) | click → filechooser ✓ | **hand-rolled `<button class="source-strip">`**, own fill/border/hover/focus, **6px** corner on 341×70 | glass `Button emphasis=secondary` (20px = its capsule on 40 h) · `7d375e9` |
| 3 | canvas drop target (`VisualizationView.vue` `.canvas-clickable`) | click → filechooser ✓ | pointer-only `<div @click>` | **spec-assigned to `.c`** (§2 F.W13.c.1: "the main area's drop target is the ONE affordance … the producer's type") — not moved here |
| 4 | replace image (preview overlay, `ImageUpload.vue`) | mouse click → filechooser ✓; **keyboard: unreachable** (no role, no name, no tab stop) | **hand-rolled `<div @click>`** over the preview | overlay → drag-over signal only (`pointer-events:none`); **glass `Button` "Replace image"** · `7d375e9` |
| 5 | Decomposition reset (`BasisSelector.vue`) | **BROKEN**: fresh workspace N=200 with reset LIT; click → N=**50** (a private `DEFAULTS` copy drifted from `lib/defaults.ts` `n_harmonics: 200`) | glass Button restyled: **4px** corner on 40×40, `pointer-events:none` fake-disabled (still focusable, Enter-live) | reads `CONTOUR_DEFAULTS` / `ANIMATION_DEFAULTS`; `.reset-icon-btn` block retires → 40×40 **20px circle**, `:disabled` · `a43190e` |
| 6 | basis toggles ℱ/Tₙ/Pₙ | click → `aria-pressed` flips ✓ | glass Button ✓ (18px on 36) | — |
| 7 | Harmonics / Sample Points NumberField + Slider | fill 40 → slider 40 ✓; ArrowRight → 41 ✓ | glass NumberField + Slider ✓ | — |
| 8 | Contour reset (`ContourSettings.vue`) | click → defaults ✓ | glass Button restyled: **4px** corner, fake-disabled | same retirement → circle, `:disabled` · `98fc769` |
| 9 | Strategy select | open → 6 options → Otsu ✓ | glass Select, **per-instance `border-2 … rounded-lg h-10`** → **10px** corner | overrides dropped → producer capsule · `98fc769` |
| 10 | ML Threshold / Blur Sigma / Min Area / Max Contours / Smoothing numeric fields (`ui/SliderControl.vue`) | fill 0.3 → slider 0.3 ✓ | **hand-rolled `<input class="inline-number">`**: 44×21, **0px**, no accessible name, underline restyle | glass `NumberField`+`NumberFieldInput` (`aria-label`, precision from `step`, Max Contours' "All" = placeholder) · `6e908fe` |
| 11 | the five Sliders | keyboard → value ✓ | glass Slider ✓ | — |
| 12 | Advanced disclosure | click → expanded ✓ | glass Collapsible ✓ | — |
| 13 | retry banner + Retry (on `store.error`) | not reachable on a healthy backend (read at the bytes) | **hand-rolled banner** (own border/tint/radius) + Button with `.retry-btn` full restyle | glass `Alert tone=destructive announce=polite` + `AlertDescription` + plain Button (`:loading`) · `98fc769` |
| 14 | Coefficients disclosure | click → toggles ✓ | glass ConfiguratorLayer ✓ | — |
| 15 | play / pause (`AnimationControls.vue`, dock `#persistent`) | Enter/Space toggle ✓; **mouse at rest: hover expands the dock and the control runs 477→52 px** (see residual r1) | **hand-rolled `<button class="play-btn">`**: 40×32 collapsed / 48×40 expanded stadium, literal-rgba glass recipe | glass **`DockControl`** (40×40 `9999px` circle; `active` → `aria-pressed`+`data-active`); rainbow kept on the producer knob `--dock-control-active-bg` + PRM arm · `95653ee` |
| 16 | canvas dock (Edit contour · View options · Equation · Fullscreen · Publish) + animation dock (speed Select · More options · timeline) | — | glass `DockControl` / `Select` / `DockTrigger` / `Slider` ✓ | — (the dock is `.d`'s surface) |

Hand-rolled BEFORE = **7** (rows 2 · 4 · 5 · 8 · 10 · 13 · 15; row 3 is `.c`'s). Two controls did not work: row 5 (reset left the defaults)
and row 4 (keyboard-unreachable). Producer reads (types, whole): `button/Button.vue.d.ts`, `number-field/*.d.ts` + `number-field-*.js`
(placeholder rides `$attrs`), `dock/DockControl.vue.d.ts` + `dock/styles/controls/icon-button.css` (`[data-active]` reads
`--dock-control-active-bg`), `alert/*.d.ts` + the root-bundle `Alert` (no `./alert` subpath at 8.0.0 → imported from the root barrel,
as `Checkbox` already is).

**2. Commits (fourier, pathspec, one per meaning; NOT pushed — see ESC-F13b-1).** `6e908fe` SliderControl → NumberField · `a43190e`
BasisSelector reset (app defaults + circle) · `98fc769` ContourSettings (reset circle, Select trigger, Alert) · `7d375e9` ImageUpload
(strip + Replace image → Button) · `95653ee` AnimationControls (play → DockControl) · `faad043` G-b spec + 4 crops
(`web/e2e/screenshots/f-w13/frame3-image-{rest,controls}-{before,after}.png`, `-f` past `web/.gitignore:1 *.png`; before taken on :3100 at
`7d55e75` before the first edit, after on :3100 at the cured tree; `deviceScaleFactor 2`, `.viz-configurator` crop = the owner's frame).

**3. AFTER census (served page).** ⟨probe re-run⟩ → upload `data-slot=button` 341×40 `20px` · Replace image `button` 341×36 `18px` ·
both resets 40×40 **`20px`** (circle) · strategy `select-trigger` `9999px` · every numeric field `role=spinbutton` `data-slot=input`
`9999px` with its label as name · play `dock-icon-button` 40×40 `9999px` at both poles. **Hand-rolled 7 → 0**; the one pointer-only
target left is row 3, `.c`'s by spec text.

**4. Gates (BEFORE → AFTER).**

| gate | command | BEFORE | AFTER |
|---|---|---|---|
| G-b | `BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-image-controls.spec.ts --project=chromium` ×2 | absent (born-RED); first draft vs the cured tree caught the reset-to-50 defect (row 5) | **3/3 ×2** (8.0 s · 21.8 s) and at committed `faad043` **3/3 ×2** (5.2 s · 4.6 s) |
| census | the spec's `handRolled()` (every control in `.viz-panel-left-wrap` + `.canvas-stage` outside a producer `data-slot` / dock control), in all 3 tests | 7 (probe) | **`[]`** ×2 |
| vue-tsc | `npx vue-tsc -b` ×2 | 0 | **exit 0 ×2** |
| unit | `npx vitest run` ×2 | 13 / 84 | **13 files / 84 passed ×2** |
| crops | `ls web/e2e/screenshots/f-w13/frame3-*` | absent | **4 files** |
| touched e2e | 12 specs (coarse-pointer · equation-interaction · contour-extraction · fullscreen · settings-persistence · timeline-scrub · workspace-flow · visualization-ux · visual-checkpoint · f-w13-radius · slider-scrub-contrast · contrast-floor), `--workers=4`, then the load-sensitive four at `--workers=2` | baseline tree `7d55e75` (scratch worktree on :3178, removed after): contrast-floor ×3 + fullscreen ✘ (pre-existing), visual-checkpoint 1·6·7 / 2 / 5 ✘ (pre-existing), item 3 ✘ (ESC-F13a-1) | **RED by this unit's cure in two specs outside the writable set** (ESC-F13b-1): `visualization-ux` ×5 keystones (helper `:72` waits on `.animation-dock .play-btn--mini`) · `contour-extraction:43` (`:66-76` reaches the blur field as `input[type="number"]`, falls back to `input[type="range"]`). Pre-existing unchanged: contrast-floor ×3 · fullscreen · visual-checkpoint 1·6·7 / 2 / 5. visual-checkpoint item 3 stays ✘ (ESC-F13a-1; its `.eq-panel-left` now also carries the NumberField fields). All others ✓ (34 passed in run 1 incl. equation-interaction, settings-persistence, timeline-scrub, slider-scrub-contrast, f-w13-radius, coarse-pointer). |

**Escalation ESC-F13b-1 (bounds).** Two existing specs locate retired hand-rolled markup; their one-line re-points are outside this unit's
set (`web/e2e/f-w13-image-controls.spec.ts` + `screenshots/f-w13/**`), so they are NOT written here and the commits are held unpushed:
(a) `web/e2e/visualization-ux.spec.ts:72` `page.locator(".animation-dock .play-btn--mini")` → `page.locator(".animation-dock").getByRole("button", { name: /(Play|Pause) animation/ })`
(the settle landmark keeps its meaning: the collapsed dock's persistent play control, now the DockControl); (b) `web/e2e/contour-extraction.spec.ts:66-76`
`blurSection.locator('input[type="number"]')` / the `input[type="range"]` fallback → `page.getByRole("spinbutton", { name: "Blur Sigma" })`
(fill + Enter, as the spec already does). Ask: grant those two paths (or rule them to `.c`/close) and re-run both ×2; (c) the ESC-F13a-1
re-baseline of `visual-checkpoint` item 3 now covers this unit's fields too.

**Residuals.** (r1) **PLAY-RUNAWAY** — the persistent play control sits at the dock's inline start; hovering the collapsed dock expands it
from its centre and the control moves 477 → 52 px under the pointer (measured ×2, BEFORE and AFTER the cure), so a first mouse click at
rest lands on the empty dock. Keyboard and a second click act. The geometry is the producer dock's hover morph (`#persistent` seat in a
centre-anchored expand) — routed to `.d` / O-55 (`DOCK-SCROLL-MORPH` family); no consumer re-anchoring written. (r2) Row 3 (canvas drop
target) is `.c`'s. (r3) The retry `Alert` is verified at the bytes and by vue-tsc only — `store.error` is unreachable on a healthy backend.
(r4) Paused, the play control is the plain glass circle; the rainbow paints while playing (it was always-on under the old `::before`).
(r5) `SliderControl` also serves the equation panels (FunctionInput, EquationPanel): their numeric fields are the glass NumberField now too.

### F.W13.c

SEAT `.c`, `claude-opus-5-5` (served model; Opus 5.5 per §0bc). Spec read whole (48 lines); record read header → `## Unit plan` + the `.b`
receipt; COHESION §0bc · §0bd · §0be · §0bf read (ESC-F13b-1 found unruled). Tree fourier `m/w1-bump-migration` @ `faad043`.

**Crash-recovery.** ⟨`git -C ../fourier-analysis status --porcelain`⟩ → the 21 ` M docs/tranches/J/audit/screenshots/**` + `?? .worktrees/`
(not in this set, untouched); **0 dirty paths under `web/src/**` / `web/e2e/**`** → no inherited partial work.

**1. Measured at the bytes, BEFORE (served `http://localhost:3100`, 1440×900 dark, playwright probe).**
- ⟨probe `/visualize` at rest⟩ → `aside 400 px display:flex · stage 1022 px · .viz-panel-left-wrap present · upload buttons ["Drop or click to
  upload — PNG/JPG/SVG ≤ 10 MB"]` — owner frame 4 reproduced; the canvas ALSO paints a "Drag & drop an image here" dashed box (`lib/canvas-drawing/placeholder.ts:59`) under a pointer-only `.canvas-clickable` `<div @click>` (`.b`'s row 3, spec-assigned here) → **three** upload surfaces for one act.
- Producer: glass `Configurator` renders `<aside class="configurator-aside">` **unconditionally** — ⟨compiled `dist/configurator-BoeScnDL.js`⟩ `u("aside", …)` with no slot guard at the installed 8.0.0; ⟨`grep -n aside glass-ui/src/components/configurator/Configurator.vue`⟩ same at src 10.0.1 (`a97a9ddd`). No glass drop-zone primitive (package exports listed: none). Motion tokens: `--spring-panel` / `--spring-panel-duration` / `--spring-panel-exit-duration` / `--enter-overlay-slide` (`styles/tokens/*`); the durations are **not** PRM-zeroed at the token (only `--motion-weight`), so the consumer transition carries its own PRM arm, as glass `transitions.css` does.
- **Defect found on the first AFTER probe**: the pick swapped the whole workspace for the "Loading workspace…" plate (probe saw `PLATE`, no enter transition — the sidebar remounted, never arrived). Cause at the bytes: `stores/workspace.ts` `uploadImage` raised the shared `loading` flag that `VisualizationView.vue:204` reads as `store.loading && !store.imageSlug` = "a workspace is loading".

**2. Cures (fourier, pathspec, one per meaning; NOT pushed — held with `.b`'s under ESC-F13b-1 / ESC-F13c-1).**
- `34059d6` `stores/workspace.ts` — an upload is not a workspace load: `uploadImage` raises its own `uploading` flag (reset clears it); `loading` keeps its one meaning. The plate no longer tears the workspace down mid-upload.
- `c82c674` `VisualizationView.vue` · `ImageUpload.vue` · `lib/canvas-drawing/placeholder.ts` — (a) the sidebar strip retires; the canvas's painted box paints only while computing; the `.canvas-clickable` div click retires. (b) The main area's `.drop-target` is the ONE affordance: glass `Button emphasis=secondary size=lg` "Drop or click to upload" (`:loading="store.uploading"`) + the format line `PNG/JPG/SVG ≤ 10 MB` in the producer's caption type (`text-caption text-muted-foreground`), carrying the page's one `data-testid="image-file-input"` (ImageUpload's replace input drops the testid → exactly one at any time). dragenter → `data-dragging` → the glass `--focus-ring-color` ring on the target; the full-screen "Drop image anywhere" overlay now shows only when an image exists (a drop there replaces). (c) No image → `v-if` removes the sidebar content, `data-sidebar="none"` collapses the aside band to `minmax(0,0)` with the aside `display:none`, the stage fills the frame; the mobile Controls/Canvas tabs render only with a sidebar. Image lands → the band opens (`transition: grid-template-columns var(--spring-panel-duration) var(--spring-panel)`) while the content enters (`viz-sidebar`: opacity + `translateX(var(--enter-overlay-slide))` on the same pair); leave on `--spring-panel-exit-duration`, the band held open until `@after-leave`. `@media (prefers-reduced-motion: reduce)` → `transition: none` on all three.
- `4922b0b` `web/e2e/f-w13-image-empty.spec.ts` + crops `web/e2e/screenshots/f-w13/frame4-image-empty-{before,after}.png` · `frame4-image-loaded-after.png` (`-f` past `web/.gitignore`; before = :3100 at `faad043` pre-edit, after = :3100 at the cured tree; `deviceScaleFactor 2`, full viewport — the owner's crop is the sidebar card, which after the cure does not exist, so the whole frame is the honest crop).

**3. AFTER probe (served page).** ⟨probe rest⟩ → `aside 0 display:none · stage 1422 · wrap 0 · upload buttons 1 · plate false`; ⟨after pick⟩ →
`aside 400 flex · stage 1022 · wrap 1 · plate false`; transitions seen: `grid-template-columns@configurator`, `opacity@viz-panel-left-wrap`, `transform@viz-panel-left-wrap`; ⟨same, `reducedMotion: reduce`⟩ → none of the three (only pre-existing producer scroll-fade / preview `scale-in`).

**Instrument note.** `test.use({ reducedMotion: "reduce" })` at describe level did not reach the page under this config (measured in-test: `matchMedia("(prefers-reduced-motion: reduce)").matches === false`, the transitions ran). The PRM test emulates on the page (`page.emulateMedia`, the idiom `visual-checkpoint.spec.ts:65` uses) and ASSERTS the media query before relying on it. The falsifier is live: with PRM not in force the same assertion read `["configurator:grid-template-columns","sidebar:opacity","sidebar:transform"]`.

**4. Gates (BEFORE → AFTER).**

| gate | command | BEFORE | AFTER |
|---|---|---|---|
| G-c | `BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-image-empty.spec.ts --project=chromium` ×2 | absent (born-RED) | **3/3 ×2** (1.8 s · 2.3 s) and at committed `4922b0b` **3/3 ×2** (1.8 s · 3.7 s) — rest: 0 sidebar, stage ≈ frame, 1 upload button, picker opens, dragenter signals; pick: sidebar present + band ≥ 300 px + enter ran; PRM: `[]` |
| vue-tsc | `npx vue-tsc -b` ×2 | 0 | **exit 0 ×2** (at `4922b0b`) |
| unit | `npx vitest run` ×2 | 13 / 84 | **13 files / 84 passed ×2** |
| crops | `ls web/e2e/screenshots/f-w13/frame4-*` | absent | **3 files** |
| touched e2e | `--workers=4`: f-w13-image-controls · visualization-crud · workspace-flow · fullscreen · settings-persistence · gallery · f-w13-radius → 24 passed / 4 failed / 3 skipped; `--workers=4`: visualization-ux · contour-extraction · timeline-scrub · equation-interaction · coarse-pointer → 23 / 7; re-run visualization-ux + equation-interaction `--workers=2` → 5 / 5 | — | GREEN: visualization-crud (its "Drop or click to upload" wait reads the new Button), workspace-flow, settings-persistence, gallery, f-w13-radius, timeline-scrub, coarse-pointer, equation-interaction (its `--workers=4` miss = load, passed at 2). **Pre-existing RED, not this unit's**: fullscreen:40 (✘ at `.b`'s baseline too) · visualization-ux ×5 (`.play-btn--mini`, ESC-F13b-1 a) · contour-extraction:43 (`input[type=range]`, ESC-F13b-1 b). **RED BY THIS UNIT'S SUPERSESSION**: f-w13-image-controls ×3 (ESC-F13c-1). |

**Escalation ESC-F13c-1 (bounds).** G-b's `web/e2e/f-w13-image-controls.spec.ts` (`.b`'s, outside this set) pins the state `.c` is ordered to
retire: `openImageMode` (`:60-64`) waits for the sidebar's Image layer AT REST, and the rest test (`:76-93`) clicks the retired strip
(`/Drop or click to upload/` inside `.viz-panel-left-wrap`) and `.canvas-container.canvas-clickable`. ⟨run ×2⟩ → **3 failed**, all at
`locator('.viz-panel-left-wrap').getByRole('button', { name: /^Image/ })` not visible. Re-point asked (one sitting with ESC-F13b-1):
`openImageMode` waits for `.drop-target` and returns the side locator unasserted; the rest test's Image-disclosure + strip + canvas-click
limbs retire (G-c now owns the rest state: one affordance, picker, dragenter) and its `handRolled()` census moves after `uploadImage`;
the two image tests are otherwise unchanged. Ask: grant that path to `.c`/close or rule it to `.d`.

**Residuals.** (r1) The sidebar enters when the image LANDS (the upload response sets `imageMeta`), not at the drop instant; locally the gap is
the upload round-trip, during which the drop target's glass Button shows `:loading`. (r2) "Leaves if the image is cleared": wired (`v-if` +
leave transition + band closes on `@after-leave`), but the only clear path in the tree (`store.reset()`) lives on the error card outside the
Configurator, so no gate observes the leave. (r3) The producer's `<aside>` is always in the DOM (8.0.0 and src 10.0.1); "no sidebar" is read as
no sidebar content (`.viz-panel-left-wrap` count 0) + the aside taking no box (`display:none`, band 0). A slot-guarded aside would be a
producer ask — not filed (the consumer layout is the band owner). (r4) The PRM instrument note above.

### F.W13.d

SERVED MODEL: claude-opus-5-5 · 2026-09-23 · probe-only seat (no consumer byte written)

**Crash-recovery.** ⟨`git -C ../fourier-analysis status --porcelain`⟩ → only `docs/tranches/J/audit/screenshots/before/*.png` (M ×21) + `?? .worktrees/` — none inside this unit's writable set (`web/package.json`, lockfile, `web/src/**`, `web/e2e/f-w13-dock.spec.ts`, `web/e2e/screenshots/f-w13/**`); nothing inherited. value.js dirty rows (`CARRY-LEDGER.md`, `scripts/dev/dev.sh`, kf `animation-audit/`) are outside the set — untouched.

**Acts (the §2 F.W13.d.2 probe, re-run at this sitting).**
1. ⟨`grep '"version"' web/node_modules/@mkbabb/glass-ui/package.json`⟩ → `8.0.0`; ⟨`grep glass-ui web/package.json`⟩ → `"^8.0.0"`.
2. ⟨`npm view @mkbabb/glass-ui versions --json | tail -5`⟩ → `8.0.0, 9.0.0, 10.0.0, 10.0.1`; ⟨`npm view @mkbabb/glass-ui dist-tags`⟩ → `{ latest: '10.0.1' }`. No version above the baseline figure.
3. Installed dist: ⟨`grep -rlE 'compactOnScroll|scrollCompact|progressRim|scroll-compact|scrollProgress' node_modules/@mkbabb/glass-ui/dist | wc -l`⟩ → `0`. `DockProps` (`dist/components/dock/composables/useDockShellProps.d.ts:10`) = `fitContent · position · backdropMode · alwaysExpanded · shape` — no scroll input, no compact state, no progress/rim seat. `GlassDock.vue.d.ts` slots = `persistent · default · collapsed · search`; exposes `expanded · isPinned · expand · collapse · keepOpen · release` — no scroll/progress surface.
4. glass src (HEAD `a97a9ddd`, 10.0.1): `useDockShellProps.ts` props = `fitContent · backdropMode · shape · orientation · collapse (false|"closed"|"open") · backgroundCanvas`; ⟨`grep -rniE 'progress|rim|compact' src/components/dock/GlassDock.vue src/components/dock/composables/*.ts`⟩ → no dock compact state / rim seat.
5. Near-misses examined and REJECTED as not the ask: `useScrollChrome({ collapseOnScroll })` (`src/composables/motion/scroll/useScrollChrome.ts:51`, also in 8.0.0 dist) and `useDockSearch`'s `collapseOnScroll` (`src/components/dock/composables/useDockSearch.ts:90`, tokens `--dock-search-shrink-start/end` in `styles/density.css:86-98`) drive the **dock-search** field's shrink and a generic chrome `collapseT` ramp — `GlassDock` itself has no compact geometry to receive it, and `ScrollProgressRim` has no dock seat. Composing either onto the fourier dock would be the consumer copy of the morph + rim that SS-6 forbids.
6. Mail: ⟨`find glass-ui/docs/tranches -newer BK/coordination/valuejs-outbound-2026-09-23-dock-scroll-morph-relay.md -type f`⟩ → only our own O-56 mirror + BL audit files. `BL/audit/INBOUND.md:10` rows O-55 (R-1 compact-on-scroll, R-2 rim in dock edge); `BL/audit/round-1/L11.md:78` → `O-55 … none yet | OPEN`; `L06.md:330` → `FOLD O-55`; `L05b.md:20` HIGH (scroll-minimize dropped, BUILD). No reply, no landing version. 0 UNREAD in scope.

**Decision.** The ask is NOT installable (not in 8.0.0, not published in 10.0.1, not in glass HEAD src). Per §2 F.W13.d.2 and SS-6: **no bump, no `web/src` edit, no `f-w13-dock.spec.ts`, no consumer copy.**

**Gate.** G-d: BEFORE RED (probe) → AFTER **RED — honest-RED by id `DOCK-SCROLL-MORPH`** (owner: glass, relay O-55; BL formation folding, landing version unfixed, expected a major above 10.0.1).

**Carries.** X-W8 row already present (`waves/W8.md:532`, third addendum). X-W12 (`waves/W12.md`) and the keyframes adoption row carry NO `DOCK-SCROLL-MORPH` text at the bytes (⟨`grep -ln DOCK-SCROLL-MORPH docs/tranches/X/waves/*.md docs/tranches/X/keyframes/waves/*.md`⟩ → `W8.md` only); both files lie outside this unit's writable set → carried by LEDGER line here; the addendum-beside in `W12.md` + the kf wave spec is owed by the orchestration seat (§0bc names the kf row).

**Commits.** fourier: none. value.js: this receipt + one LEDGER line.

## Close

CLOSE SEAT, `claude-opus-5-5`, VERIFY-ONLY: this seat wrote no fourier bytes, no glass-ui bytes and no product bytes. Spec read whole (48 lines).
From the record it read the header through `## Unit plan` and the `.c` and `.d` receipts; COHESION was grepped through §0bk and no ruling on
ESC-F13a-1, ESC-F13b-1 or ESC-F13c-1 exists (⟨`grep -n ESC-F13 COHESION.md`⟩ → empty). Tree: fourier `m/w1-bump-migration` @ `4922b0b`,
served at `http://localhost:3100` by `vite web --port 3100` with cwd = the fourier tree.

**Crash-recovery.** ⟨`git -C ../fourier-analysis status --porcelain`⟩ → 21 ` M docs/tranches/J/audit/screenshots/**` + `?? .worktrees/`. None of
these is in this seat's set, and none was touched. value.js: ` M CARRY-LEDGER.md` (a sibling's) and ` M scripts/dev/dev.sh` (standing). Nothing inherited.

### 1. Commit audit (`git show --stat`, every sha)
| unit | fourier shas | paths | in bounds |
|---|---|---|---|
| .a | `83a723d` `1a4631a` `3c4f720` `7d55e75` | PaperSidebar.vue · PaperView.vue · style.css · f-w13-radius.spec.ts + 6 crops | yes |
| .b | `6e908fe` `a43190e` `98fc769` `7d375e9` `95653ee` `faad043` | SliderControl · BasisSelector · ContourSettings · ImageUpload · AnimationControls · f-w13-image-controls.spec.ts + 4 crops | yes |
| .c | `34059d6` `c82c674` `4922b0b` | stores/workspace.ts · ImageUpload · VisualizationView · lib/canvas-drawing/placeholder.ts · f-w13-image-empty.spec.ts + 3 crops | yes |
| .d | none (probe-only) | — | yes |
| value.js | `16a93175` (INBOX, I-40 cell) · `3d77eeff` `fc8b9bc2` `d9f5caec` `dd9a7165` (this record) · `687548d8` `989b4648` `f0d20411` `c348ba76` (LEDGER) | one path each | yes |

**Landed-wrong: 0.** Every path sits under `web/src/**`, the unit's own `web/e2e/f-w13-*` spec, `web/e2e/screenshots/f-w13/**`, or its value.js cell.
`web/package.json` is untouched, and no glass-ui byte was written.

### 2. Gates re-run at this seat (fourier `4922b0b`), BEFORE → AFTER
| gate | command | BEFORE (`3e709e4`) | AFTER (this seat, ×2) |
|---|---|---|---|
| G-a | `BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-{radius,image-controls,image-empty}.spec.ts --project=chromium` | absent (born-RED) | **GREEN 3/3 ×2**: frame 1 disclosure circle + collapse · frame 2 circle + back · frame 5 card radius |
| G-b | same run | absent (born-RED) | **RED 0/3 ×2**: `.b` measured it 3/3 ×2 at `faad043`. At HEAD all three fail at `expect(locator).toBeVisible()` on `.viz-panel-left-wrap … Image`, because `.c`'s ordered cure retired the resting sidebar that G-b pins. This is ESC-F13c-1, still unruled |
| G-c | same run | absent (born-RED) | **GREEN 3/3 ×2**: rest (no sidebar, one affordance) · pick (sidebar enters) · PRM (no motion) |
| G-d | re-probe: ⟨`grep version node_modules/@mkbabb/glass-ui/package.json`⟩ `8.0.0` · ⟨`npm view … dist-tags`⟩ `latest 10.0.1` · dist grep `compactOnScroll\|scrollCompact\|progressRim` → `0` · glass src `components/dock` → `0` (glass HEAD `d29be90e`) | RED | **honest-RED `DOCK-SCROLL-MORPH`**: owner glass, relay O-55 (OPEN in BL `INBOUND.md:10`) |
| typecheck | `npx vue-tsc -b` (spec-literal `npm run typecheck` is absent, a Baseline defect) | 0 | **exit 0, 0 errors ×2** |
| unit | `npx vitest run` | 13 / 84 | **13 files / 84 passed ×2** |
| screenshots | `git ls-files web/e2e/screenshots/f-w13` | absent | **13 files**: frame 1, 2 and 5 before/after · frame 3 controls + rest before/after · frame 4 empty before/after + loaded-after. All five frames covered |

**Existing e2e.** ⟨9 touched specs: visualization-ux · contour-extraction · visual-checkpoint · equation-interaction · workspace-flow ·
settings-persistence · timeline-scrub · paper-performance · paper-search, `--workers=4`⟩ → run 1 **40 passed / 11 failed** · run 2 **41 passed / 10 failed**:
- visualization-ux ×5 keystones and contour-extraction:43 fail in both runs. They pin retired hand-rolled markup (ESC-F13b-1, unruled).
- visual-checkpoint item 3 fails in both runs. The OA-17 corners need a re-baseline outside the bounds (ESC-F13a-1, unruled).
- visual-checkpoint 1·6·7, 2 and 5 fail in both runs. They were already RED at `3e709e4`, before this wave.
- equation-interaction:77 failed in run 1 only, with "notation pill flipped but series did not change" under 4 workers. Run solo
  ⟨`--workers=1`⟩ it measured **1/1 ×2** (2.2 s · 2.1 s), so it reads as host-load flake, not a regression.
**Unchanged-GREEN is not met**: 6 existing tests are RED because of this wave's cures, and every one is outside the bounds.

### 3. E13 close sweep
Clock: the `.d` receipt (`dd9a7165`, 2026-09-23T11:00:08). ⟨`find <path> -maxdepth 2 -newermt …`⟩ finds:
- `V/coordination/INBOX.md` (itself).
- Glass BK: only our own `valuejs-outbound-*` (O-58 and the X-W7 fallthrough relay); no `glass-outbound`.
- BL `audit/*`: glass-internal, and O-55 still OPEN.
- `X/coordination`, keyframes `V/coordination` and fourier `F/coordination/INBOX.md` (mtime Sep 17): empty.

**0 new I-n · 0 UNREAD in F.W13 scope.** The UNREAD rows are O-20 · I-30 · I-31 · I-32 · I-35 · O-39, all scoped out at the open. I-40 is marked READ (`16a93175`).

### 4. Escalations (all unruled; returned to COHESION)
- **ESC-F13a-1**: re-baseline `web/e2e/visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-darwin.png`. The snapshot pins the
  square panels the owner ordered rounded; the diff is at the corners only.
- **ESC-F13b-1**: re-point two lines in two specs:
  - `visualization-ux.spec.ts:72` → `.animation-dock` getByRole button `/(Play|Pause) animation/`.
  - `contour-extraction.spec.ts:66-76` → the NumberField spinbutton.
- **ESC-F13c-1**: re-point `f-w13-image-controls.spec.ts`:
  - `openImageMode` (`:60-64`) waits on `.drop-target`.
  - The rest test's Image-disclosure, strip and canvas-click limbs retire (G-c owns rest).
  - The `handRolled()` census moves after upload.
  This one ruling returns G-b to GREEN.
All three are one class: a test pins surfaces an ordered cure retired, and the cure lies outside the unit's writable set. The §0bk
pattern (grant supplement units) fits them. One supplement unit granted those four test paths would close the wave.

### 5. Residuals (named owners)
- `DOCK-SCROLL-MORPH` (OA-11/12): owner **glass** (O-55, BL). It is carried at X-W8 `waves/W8.md:532`. The X-W12 and keyframes carry rows are
  absent at the bytes (`.d`'s finding) and are owed by the **COHESION seat**.
- `.a`: the desktop ToC lost its scroll-to-top, and `.sidebar-link` rows keep their list-row radius. Owner: **X·F next wave**.
  `data-sidebar-follow-exempt` and `rootId` wait on the glass 10.x bump (owner: **X-W8 / F bump wave**).
- `.c` r1–r4:
  - The sidebar enters on landing, not on drop.
  - The leave path has no gate.
  - The glass `<aside>` is always in the DOM. A slot-guarded aside would be a **glass** ask, not filed.
  - PRM is emulated through `page.emulateMedia`.
- visual-checkpoint 1·6·7, 2 and 5: RED before this wave. Owner: **X·F gallery snapshots** (not F.W13).
- equation-interaction:77: flakes under 4 workers. Owner: **X·F instrument** (the `G-F12-3-LOAD` class).

### 6. State
Track the owner's goal criterion (spec §Goal):
- Frames 1, 2 and 5 are CURED.
- Frame 3 is CURED at the product. Its gate is RED under ESC-F13c-1.
- Frame 4 is CURED.
- The dock is honest-RED (producer).
F.W13 therefore moves **OPEN → PARTIAL 2026-09-17**. It is not IMPLEMENTED: G-b is RED at HEAD, and the existing-e2e "unchanged-GREEN" leg is
RED on three unruled bounds escalations. The spec's §State names no seat to stamp VERIFIED, so this close does not stamp it.
Push: fourier `origin/m/w1-bump-migration` `7d55e75..4922b0b` (9 shas: `.b` + `.c`, held until now); value.js `tranche-u`.

## Check 1

FRESH ADVERSARIAL CHECK (L-20 pass 1), `claude-opus-5-5`, VERIFY-ONLY: this seat wrote 0 fourier bytes, 0 glass-ui bytes and 0 product bytes.
It read the spec whole (48 lines), and from the record the header through `## Unit plan` plus `## Close`. It ran every gate command itself at fourier `4922b0b` (= origin),
served at `http://localhost:3100`. Crash-recovery: fourier porcelain = 21 ` M docs/tranches/J/audit/screenshots/**` (not in any F.W13 set, untouched);
value.js = ` M CARRY-LEDGER.md` (a sibling's) · ` M scripts/dev/dev.sh` (standing). Nothing inherited.

**Verdict: NOT-CONFORMANT**: 2 HIGH · 1 MINOR · 2 INFO. The close's own `PARTIAL` reading is confirmed, and the LEDGER status is left as `PARTIAL`.

### Axes
1. **The claimed GREENs reproduce (6 of 6).** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-{radius,image-controls,image-empty}.spec.ts --project=chromium` ×2⟩ →
   `6 passed / 3 failed` in both runs. G-a is 3/3 ×2 and G-c is 3/3 ×2. The 3 failures are all `f-w13-image-controls.spec.ts` (`:76` · `:96` · `:194`), each at `:63`
   `expect(side.getByRole("button",{name:/^Image/})).toBeVisible()`, so G-b is **0/3 ×2**, as the close reports. ⟨`npx vue-tsc -b` ×2⟩ → exit 0 ×2.
   ⟨`npx vitest run` ×2⟩ → 13 files / 84 passed ×2. The existing-e2e RED also reproduces: ⟨`npx playwright test e2e/visualization-ux.spec.ts e2e/contour-extraction.spec.ts --workers=4`⟩ → `6 failed / 17 passed`.
2. **Bounds.** ⟨`git show --stat` on all 13 fourier shas⟩ → every path is under `web/src/**` or `web/e2e/f-w13-*` + `web/e2e/screenshots/f-w13/**`.
   `web/package.json` and the lockfile are untouched. ⟨`git log 3d465e70^..HEAD -- scripts/dev/dev.sh`⟩ → empty. Landed-wrong: 0.
3. **Masking.** ⟨`git diff 3e709e4..4922b0b -- web | grep '^+' | grep -E 'skip|fixme|try {|catch|allowlist|!important|node_modules'`⟩ → 0 hits. The `data-slot` reads are in
   the tests only, where they assert the producer's primitive and do not restyle it. None of the RED specs was edited or narrowed.
4. **Commit families.** One meaning per commit. The `.a`/`.b`/`.c` product commits are separate from their gate commits, and `.c`'s root defect (`34059d6`) is separate from its cure.
5. **E-3.** ⟨`git diff --stat 3d465e70^..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/`⟩ → empty. The spec's last commit is `2d950825` (the mint).
6. **Mail.** The INBOX UNREAD rows are O-20 · I-30 · I-31 · I-32 · I-35 · O-39, all outside F.W13's scope. I-40 is READ. Since the `.d` clock the glass BK/BL has held only our own outbound files plus BL-internal
   `audit/*`, with no glass-outbound to value.js. Clean.
7. **Four-verb line.** OPEN → PARTIAL is lawful, and the close declined to stamp IMPLEMENTED/VERIFIED. This check keeps it there.
8. **Goal criterion.** Frames 1, 2, 4 and 5 are met at the bytes. Frame 3 is cured at the product (census 7 → 0, `.b`), but its gate is RED. The dock limb is producer-gated by the spec's own text.
9. **Published figures.** 13 shas · 13 crops (⟨`git ls-files web/e2e/screenshots/f-w13 | wc -l`⟩ in the close) · G-a 3/3 · G-c 3/3 · G-b 0/3 · 84/84 · 6 existing RED all reproduce.
10. **Honest-RED adjudication, against the spec bytes:**
    - **G-d → `DOCK-SCROLL-MORPH`: RELIEVED.** Spec §2 `.d` item 2 names the id and its owner (glass, O-55), and §3 reads "G-d or `DOCK-SCROLL-MORPH`". It is carried at X-W8 `waves/W8.md:532`.
    - **G-b: NOT RELIEVED.** §3 requires "G-a · G-b · G-c ×2 each" and names no relief for G-b. It is not producer-owned, and no later wave is routed its cure.
      Nor is its cure outside the wave's bounds. §1 grants `web/e2e/**` "(new `f-w13-*` specs + screenshots)", and `f-w13-image-controls.spec.ts` is such a spec.
      The narrowing to one spec per unit came from the record's own Unit plan, not from the spec. So ESC-F13c-1 escalated a cure the wave was lawfully able to land.
    - **Existing e2e "unchanged-GREEN": NOT RELIEVED.** This covers 6 tests: visualization-ux ×5 and contour-extraction:43 (ESC-F13b-1), plus visual-checkpoint item 3 (ESC-F13a-1).
      Their cures (`visualization-ux.spec.ts`, `contour-extraction.spec.ts`, the checkpoint snapshot) do sit outside §1, so they need a COHESION grant. They are still a §3 gate RED with no spec relief.
    - The pre-existing visual-checkpoint 1·6·7, 2 and 5 were RED at `3e709e4`. They are not this wave's regression (INFO).
    - **Honest-RED set: {`DOCK-SCROLL-MORPH`}.**

**Successors.** No authored spec declares `Opens after: F.W13` (⟨`grep -rn "Opens after" docs/tranches/X | grep -i "F.W13"`⟩ → only F-W13.md's own line).
F.W14 is named at COHESION `:3027` but not yet authored. X-W8's row `:532` gates on glass shipping O-55, not on this wave's close. So no successor is blocked by this verdict.

### Register
| # | severity | claim | receipt | cure |
|---|---|---|---|---|
| 1 | **HIGH** | G-b is RED 0/3 ×2 at HEAD, with no relief in the spec | the ×2 run above: `:63` toBeVisible fails on the retired resting sidebar | Re-point `f-w13-image-controls.spec.ts` per the ESC-F13c-1 text (`openImageMode` waits on `.drop-target`, the rest limbs retire to G-c, the census moves after upload), then run G-b ×2. This is within §1 (`web/e2e/**` new `f-w13-*` specs), so a Track C repair seat can land it with no ruling |
| 2 | **HIGH** | §3 "existing e2e suite unchanged-GREEN" is RED: 6 tests fail because of this wave's ordered cures | ⟨visualization-ux + contour-extraction⟩ → 6 failed / 17 passed; visual-checkpoint item 3 per the close | COHESION grants a supplement unit the ESC-F13a-1/b-1 paths (the two spec lines + the checkpoint re-baseline, as in the §0bk pattern), then the touched specs run GREEN ×2 |
| 3 | MINOR | ESC-F13c-1 was misclassified as a bounds escalation. The spec's §1 covers the path, and only the record's Unit plan narrowed it | spec l.19 `web/e2e/**` (new `f-w13-*` specs + screenshots) | Record the correction in the repair; no COHESION ruling is needed for defect 1 |
| 4 | INFO | Spec-literal `npm run typecheck` does not exist (`Missing script`). `npx vue-tsc -b` is the equivalent and reads GREEN ×2 | Baseline row | Correct it in a dated addendum-beside when the spec is next touched |
| 5 | INFO | `.c` r1–r4 (the sidebar enters on landing, not on drop · the leave path has no gate · the aside is always in the DOM · PRM is emulated) and `.a`'s ToC scroll-to-top loss are owner-named residuals | Close §5 | X·F next wave / glass ask |

LEDGER: the status stays **PARTIAL 2026-09-17**, with an event line appended. No CLOSED stamp: two HIGH are unrelieved.

## Repair 1

REPAIR SEAT (round 1), `claude-opus-5-5`, 2026-09-23. It read the spec whole (48 lines), and from the record the header through `## Unit plan` plus the ESC and `## Check 1` sections.
Crash-recovery: fourier porcelain = 21 ` M docs/tranches/J/audit/screenshots/**` + `?? .worktrees/` (not in any F.W13 set, untouched). value.js = ` M CARRY-LEDGER.md` (a sibling's) · ` M scripts/dev/dev.sh` (standing). Nothing inherited under `web/src/**` or `web/e2e/**`.

| # | defect | cure | commit | gate re-reading |
|---|---|---|---|---|
| 1 | HIGH: G-b RED 0/3 ×2 | `f-w13-image-controls.spec.ts` re-pointed per ESC-F13c-1. `openImageMode` waits on `.drop-target` and returns the sidebar locator unasserted. The rest test's Image-disclosure, strip and canvas-click limbs are gone at rest (G-c owns the rest shape). The rest limb now drives the drop target's glass Button (`data-slot=button`) to the picker by click and by Enter. After `uploadImage`, the Image layer's disclosure toggles both ways and the `handRolled()` census runs. The two image tests are unchanged. | fourier `f3af627` (pushed `4922b0b..f3af627`) | ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-{radius,image-controls,image-empty}.spec.ts --project=chromium` ×2⟩ → `9 passed` ×2. **G-a 3/3 ×2 · G-b 3/3 ×2 · G-c 3/3 ×2** |
| 2 | HIGH: existing e2e not unchanged-GREEN | **not cured: ESCALATED.** The cure paths (`visualization-ux.spec.ts:72`, `contour-extraction.spec.ts:66-76`, `visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-darwin.png`) are existing specs and a snapshot. Spec §1 grants only *new* `f-w13-*` specs and screenshots. ⟨`grep -n ESC-F13 COHESION.md`⟩ → empty, so no grant exists. | none | ⟨`npx playwright test e2e/visualization-ux.spec.ts e2e/contour-extraction.spec.ts --project=chromium --workers=4` ×2⟩ → `6 failed / 17 passed` ×2, unchanged from Check 1 |
| 3 | MINOR: ESC-F13c-1 misclassified | Corrected here. ESC-F13c-1 is **WITHDRAWN**. Its path is inside spec §1 l.19 (`web/e2e/**`, new `f-w13-*` specs). The narrowing came from this record's Unit plan, not from the spec. Cured by row 1, and no COHESION ruling is needed. | (this record) | — |
| 4 | INFO: `npm run typecheck` missing | Recorded, no cure. The equivalent is ⟨`npx vue-tsc -b` ×2⟩ → exit 0 ×2. A dated addendum beside the spec is left to the next seat that touches the spec (E-3). | — | vue-tsc exit 0 ×2 |
| 5 | INFO: residuals | These keep their owners from Close §5. | — | — |

**Escalation (returned to COHESION): ESC-F13a-1 + ESC-F13b-1.** These are unchanged in substance. One supplement unit, following the §0bk pattern and granted those three paths, closes §3's "existing e2e unchanged-GREEN" leg.
**Status after repair.** G-a, G-b and G-c are GREEN ×2. G-d is honest-RED `DOCK-SCROLL-MORPH` (relieved). The existing-e2e leg is RED 6 (+ checkpoint item 3) pending that grant. The LEDGER row stays **PARTIAL**.

## Check 2

FRESH ADVERSARIAL CHECK (L-20 pass 2), `claude-opus-5-5`, VERIFY-ONLY, 2026-09-23: this seat wrote 0 fourier bytes, 0 glass-ui bytes and 0 product bytes.
It read the spec whole (48 lines), and from the record the header through `## Unit plan`, `## Close`, `## Check 1` and `## Repair 1`. Every gate it cites it ran itself
at fourier `f3af627` (= `origin/m/w1-bump-migration`), served at `http://localhost:3100`. Crash-recovery: fourier porcelain = 21 ` M docs/tranches/J/audit/screenshots/**`
(outside every F.W13 set, untouched). value.js porcelain = sibling rows (`demo/**`, `CARRY-LEDGER.md`, `B/KF-W13U.md`) plus ` M scripts/dev/dev.sh` (standing). None of them is in this seat's set, so nothing was inherited.

**Verdict: NOT-CONFORMANT**: 1 HIGH · 1 MINOR · 2 INFO. Repair 1 cured Check 1's HIGH #1 (G-b), and that cure reproduces. HIGH #2 (§3 "existing e2e suite unchanged-GREEN") stands unrelieved, so the LEDGER status stays `PARTIAL 2026-09-17`.

### Axes
1. **The claimed GREENs reproduce (6 of 6).**
   - ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-radius.spec.ts e2e/f-w13-image-controls.spec.ts e2e/f-w13-image-empty.spec.ts --project=chromium` ×2⟩ → `9 passed` exit 0, then `9 passed` exit 0. So G-a 3/3, G-b 3/3 and G-c 3/3, ×2.
   - ⟨`npx vue-tsc -b` ×2⟩ → exit 0 ×2.
   - ⟨`npx vitest run` ×2⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)` ×2.
   - ⟨`git ls-files web/e2e/screenshots/f-w13 | wc -l`⟩ → `13`.
   - The existing-e2e RED also reproduces. ⟨`npx playwright test e2e/visualization-ux.spec.ts e2e/contour-extraction.spec.ts --project=chromium --workers=4` ×2⟩ → `6 failed / 17 passed` ×2. The failures are `contour-extraction.spec.ts:43` (at `:76` `blurSlider.fill` timeout) and `visualization-ux.spec.ts` :117 · :130 · :147 · :179 · :330. The five visualization-ux failures share one line, `:72` `.animation-dock .play-btn--mini` toBeVisible → element(s) not found, which is the retired hand-rolled play control (ESC-F13b-1). This seat did not re-run visual-checkpoint item 3 (ESC-F13a-1); the close's reading is cited.
2. **Bounds.**
   - ⟨`git show --stat f3af627`⟩ → `web/e2e/f-w13-image-controls.spec.ts` only (21+/14−). That path is inside §1 l.19.
   - The value.js `944d6a85` touches `C/F-W13.md` and `LEDGER.md` only.
   - ⟨`git diff 3e709e4..f3af627 --stat -- web/package.json web/package-lock.json`⟩ → empty.
   - ⟨`git log 2d950825..HEAD -- scripts/dev/dev.sh`⟩ → empty.
   - ⟨`git log 3e709e4..HEAD -- web/e2e/visualization-ux.spec.ts web/e2e/contour-extraction.spec.ts web/e2e/visual-checkpoint.spec.ts-snapshots`⟩ → empty, so the RED specs were neither edited nor narrowed.
   - Landed-wrong: 0.
3. **Masking.**
   - ⟨`git diff 3e709e4..f3af627 -- web | grep '^+' | grep -E 'test\.skip|fixme|try \{|catch|allowlist|!important|node_modules'`⟩ → 0 hits.
   - Repair 1 retires the rest-state strip and canvas-click limbs. Both surfaces were retired by `.c`'s ordered cure, and the rest limb now drives the one surviving affordance, by pointer and by key.
   - The disclosure limb changed from literal `true/false` to a round trip relative to the post-upload state (`not.toHaveAttribute(before)` then `toHaveAttribute(before)`). It still proves both toggles, so it is not a narrowing of what the spec asks ("each control acts"). INFO, recorded below.
4. **Commit families.** `f3af627` has one meaning (the G-b re-point), and `944d6a85` is the record and ledger of that repair. No family is split.
5. **E-3.** ⟨`git diff --stat 2d950825..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/ scripts/dev/dev.sh`⟩ → empty.
6. **Mail.**
   - ⟨`find glass BK/coordination glass BL fourier F/coordination -newermt 2026-09-23T11:00`⟩ → only our own `valuejs-outbound-*` (O-58 and the X-W7 relay) plus glass-internal BL `audit/*`. There is no glass-outbound to value.js.
   - O-55 is OPEN at BL `INBOUND.md:10`.
   - The INBOX UNREAD rows (O-20 · I-30 · I-31 · I-32 · I-35 · O-39) are all outside F.W13's scope. Clean.
7. **Four-verb line.** OPEN → PARTIAL is lawful and unmoved. This check does not advance it.
8. **Goal criterion.** Frames 1, 2, 3, 4 and 5 are met at the bytes, and each has a gate GREEN ×2. The dock limb is producer-gated by the spec's own text (§Goal: "on the producer's dock, never a consumer re-implementation").
9. **Published figures.** Repair 1's "G-a/G-b/G-c 9/9 ×2", "6 failed / 17 passed ×2" and "vue-tsc exit 0 ×2" all reproduce exactly.
10. **Honest-RED adjudication, against the spec bytes:**
    - **G-d → `DOCK-SCROLL-MORPH`: RELIEVED.** §2 `.d` item 2 names the id and its owner (glass, O-55), and §3 reads "G-d or `DOCK-SCROLL-MORPH`". The residual register (Close §5) names glass/O-55 and the X-W8 carry, and O-55 is OPEN at BL.
    - **Existing e2e "unchanged-GREEN": NOT RELIEVED.** It covers 6 reproduced tests (ESC-F13b-1) plus visual-checkpoint item 3 (ESC-F13a-1, cited).
      - §3 lists the leg as a gate with no relief.
      - It is not producer-owned, because the RED specs pin this wave's own retired consumer markup.
      - No later wave is routed its cure.
      - The spec names no honest-RED id for it.
      - Its cure paths (existing specs and a snapshot) sit outside §1, which grants only *new* `f-w13-*` specs. So the record escalated them correctly. ⟨`grep -n ESC-F13 docs/tranches/X/COHESION.md`⟩ → still empty: no grant, no ruling.
      - Escalating is the lawful act, but an escalation is not relief. The leg stays a real §3 RED.
    - The pre-existing visual-checkpoint 1·6·7, 2 and 5 were RED at `3e709e4`. They are not this wave's regression (INFO, owner X·F gallery snapshots).
    - **Honest-RED set: {`DOCK-SCROLL-MORPH`}.**

**Successors.** ⟨`grep -rn "Opens after" docs/tranches/X | grep -i "F.W13\|F-W13"`⟩ → only F-W13.md's own line (`:4`, "F.W12 CLOSED", which is GREEN). No authored wave declares an `Opens after` conjunct on F.W13, so no successor is blocked by this verdict. X-W8's `DOCK-SCROLL-MORPH` carry gates on glass shipping O-55, not on this close.

### Register
| # | severity | claim | receipt | cure |
|---|---|---|---|---|
| 1 | **HIGH** | §3 "the existing e2e suite unchanged-GREEN" is RED. 6 tests (+ checkpoint item 3) fail on the surfaces this wave's ordered cures retired, and the spec gives no relief | ⟨visualization-ux + contour-extraction, `--workers=4` ×2⟩ → `6 failed / 17 passed` ×2; 5× `:72 .play-btn--mini` not found; `contour-extraction:76` fill timeout | COHESION rules ESC-F13a-1 + ESC-F13b-1. One supplement unit, following the §0bk pattern, is granted `web/e2e/visualization-ux.spec.ts:72` → the DockControl `/(Play\|Pause) animation/`, `web/e2e/contour-extraction.spec.ts:66-76` → the NumberField spinbutton, and the `checkpoint-disclosure-body` re-baseline. Then the touched specs run GREEN ×2 |
| 2 | MINOR (mitigated) | Repair 1's disclosure limb asserts a round trip relative to the post-upload state, not literal `true`→`false` | `f3af627` diff: `not.toHaveAttribute("aria-expanded", before)` then `toHaveAttribute(…, before)` | None needed. Both directions are still proven, and a literal pin would couple to the post-upload default |
| 3 | INFO | Spec-literal `npm run typecheck` does not exist. `npx vue-tsc -b` is the equivalent (exit 0 ×2) | Baseline row | A dated addendum-beside when the spec is next touched (E-3) |
| 4 | INFO | `.c` r1–r4 and `.a`'s ToC scroll-to-top loss are owner-named residuals | Close §5 | X·F next wave / glass ask |

LEDGER: the status stays **PARTIAL 2026-09-17**, with an event line appended. No CLOSED stamp, because HIGH #1 is unrelieved.

## Repair 2

REPAIR SEAT (round 2), `claude-opus-5-5`, 2026-09-23. It read the spec whole (48 lines), and from the record the header through `## Unit plan` plus `## Repair 1` and `## Check 2`. It wrote 0 fourier bytes and 0 glass-ui bytes.
Crash-recovery: fourier porcelain = 21 ` M docs/tranches/J/audit/screenshots/**` + `?? .worktrees/` (outside every F.W13 set, untouched). Nothing was inherited under `web/src/**` or `web/e2e/**`. Fourier HEAD = `f3af627`.

| # | defect | cure | commit | gate re-reading |
|---|---|---|---|---|
| 1 | HIGH: §3 existing e2e not unchanged-GREEN | **not cured: ESCALATED again.** The cure paths are `web/e2e/visualization-ux.spec.ts:72`, `web/e2e/contour-extraction.spec.ts:66-76` and the `checkpoint-disclosure-body` snapshot. They are existing specs and a snapshot. Spec §1 grants only *new* `f-w13-*` specs and screenshots, and says "Do NOT touch … every other spec". ⟨`grep -n ESC-F13 docs/tranches/X/COHESION.md`⟩ → empty. The last addendum is §0bl, and it holds no F.W13 grant. With no grant, editing these paths would be an out-of-bounds write. | none | ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/visualization-ux.spec.ts e2e/contour-extraction.spec.ts --project=chromium --workers=4` ×2⟩ → `6 failed / 17 passed` (1.3m) then `6 failed / 17 passed` (1.2m). The failures are unchanged: `contour-extraction:43`, and `visualization-ux` :117 · :130 · :147 · :179 · :330 |
| 2 | MINOR (mitigated): disclosure round-trip limb | None needed. Check 2 itself rules "None needed", and there is no one-command cure to apply. | — | G-b is unchanged at `f3af627` (Check 2: 3/3 ×2) |
| 3 | INFO: `npm run typecheck` | Recorded. The addendum-beside belongs to the seat that next touches the spec, and the spec dir is outside this unit's writable set. | — | — |
| 4 | INFO: residuals | These keep their owners from Close §5. | — | — |

**Escalation (returned to COHESION): ESC-F13a-1 + ESC-F13b-1.** These are unchanged. The cure is the one Check 2 wrote: one supplement unit on the §0bk pattern, granted the three paths above. It re-points `:72` to the DockControl `/(Play|Pause) animation/`, re-points `:66-76` to the NumberField spinbutton, and re-baselines `checkpoint-disclosure-body`. The touched specs then run GREEN ×2.
**Status.** G-a, G-b and G-c are GREEN ×2 (Check 2). G-d is honest-RED `DOCK-SCROLL-MORPH` (relieved). The existing-e2e leg is RED 6 (+ checkpoint item 3), pending the grant. LEDGER stays **PARTIAL**.

## Check 3

FRESH ADVERSARIAL CHECK (L-20 pass 3), `claude-opus-5-5`, VERIFY-ONLY, 2026-09-23: this seat wrote 0 fourier bytes, 0 glass-ui bytes and 0 product bytes.
It read the spec whole (48 lines), and from the record the header through `## Unit plan`, `## Close`, `## Repair 1`, `## Check 2` and `## Repair 2`. Every gate below it ran itself
at fourier `f3af627` (= `origin/m/w1-bump-migration`, unmoved since Check 2), served at `http://localhost:3100` (HTTP 200).
Crash-recovery: fourier porcelain = 21 ` M docs/tranches/J/audit/screenshots/**` + `?? .worktrees/` (outside every F.W13 set). value.js porcelain = sibling rows (`demo/**`, `e2e/**`, `CARRY-LEDGER.md`, `waves/W7-*`, evidence dirs) + ` M scripts/dev/dev.sh` (standing). Neither this record nor LEDGER.md was dirty, so nothing was inherited.

**Verdict: NOT-CONFORMANT**: 1 HIGH · 1 MINOR · 2 INFO. The state is byte-identical to Check 2's: Repair 2 cured nothing and re-escalated, and COHESION still holds no grant.
⟨`grep -n 'ESC-F13' docs/tranches/X/COHESION.md`⟩ → empty; the last addendum is §0bl (`:3022`), which holds no F.W13 grant. The LEDGER status stays `PARTIAL 2026-09-17`.

### Axes
1. **The claimed GREENs reproduce (6 of 6).**
   - ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-radius.spec.ts e2e/f-w13-image-controls.spec.ts e2e/f-w13-image-empty.spec.ts --project=chromium` ×2⟩ → `9 passed (4.9s)` exit 0, then `9 passed (5.9s)` exit 0. So G-a, G-b and G-c are each 3/3 ×2.
   - ⟨`npx vue-tsc -b` ×2⟩ → exit 0 ×2.
   - ⟨`npx vitest run` ×2⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)` ×2.
   - ⟨`git ls-files web/e2e/screenshots/f-w13 | wc -l`⟩ → `13`.
   - The existing-e2e RED also reproduces. ⟨`npx playwright test e2e/visualization-ux.spec.ts e2e/contour-extraction.spec.ts --project=chromium --workers=4` ×2⟩ → `6 failed / 17 passed` exit 1 ×2. The failures are `contour-extraction.spec.ts:43` (at `:76` `blurSlider.fill`) and `visualization-ux.spec.ts` :117 · :130 · :147 · :179 · :330 (at `:72` `.animation-dock .play-btn--mini` not found). This seat did not re-run visual-checkpoint item 3 (ESC-F13a-1); the close's reading is cited.
2. **Bounds.**
   - ⟨`git diff --name-only 3e709e4..f3af627 | grep -vE '^web/src/|^web/e2e/f-w13-|^web/e2e/screenshots/f-w13/'`⟩ → empty.
   - ⟨`git diff --stat 3e709e4..f3af627 -- web/package.json web/package-lock.json`⟩ → empty.
   - ⟨`git log 2d950825..HEAD -- scripts/dev/dev.sh`⟩ → empty. Landed-wrong: 0.
3. **Masking.** ⟨`git diff 3e709e4..f3af627 -- web | grep '^+' | grep -cE 'test\.skip|fixme|try \{|catch *\(|allowlist|!important|node_modules'`⟩ → `0`. The existing RED specs are unedited, so nothing was narrowed.
4. **Commit families.** No new commit since Check 2. The families audited at Close §1 and Check 2 stand unsplit.
5. **E-3.** ⟨`git log 2d950825..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/`⟩ → empty. `F-W13.md`'s last commit is its minting, `2d950825`.
6. **Mail.**
   - ⟨`find glass BK/coordination BL fourier F/coordination -newermt 2026-09-23T11:00`⟩ → our own `valuejs-outbound-*` (O-58 and the X-W7 relay) plus glass-internal BL `audit/*`. There is no glass-outbound to value.js.
   - O-55 is still OPEN at BL `INBOUND.md:10`.
   - The UNREAD rows (O-20 · I-30 · I-31 · I-32 · I-35 · O-39) are all outside F.W13's scope. Clean.
7. **Four-verb line.** OPEN → PARTIAL is lawful and unmoved, and this check does not advance it.
8. **Goal criterion.** Frames 1–5 are met at the bytes, and each is gated GREEN ×2. The dock limb is producer-gated by the spec's own text ("on the producer's dock, never a consumer re-implementation").
9. **Published figures.** Repair 2's `6 failed / 17 passed ×2`, and Check 2's `9 passed ×2`, `13/84 ×2`, `vue-tsc 0 ×2` and `13` crops, all reproduce exactly.
10. **Honest-RED adjudication, against the spec bytes:**
    - **G-d → `DOCK-SCROLL-MORPH`: RELIEVED.** §2 `.d` item 2 names the id and its owner (glass, O-55), and §3 reads "G-d or `DOCK-SCROLL-MORPH`". Close §5 names glass/O-55 and the X-W8 carry.
    - **Existing e2e "unchanged-GREEN": NOT RELIEVED.** It covers 6 reproduced tests (ESC-F13b-1) plus visual-checkpoint item 3 (ESC-F13a-1).
      - §3 lists the leg with no relief.
      - It is not producer-owned: the RED specs pin this wave's own retired consumer markup.
      - No later wave is routed its cure.
      - The spec names no honest-RED id for it.
      - Its cure paths sit outside §1 ("Do NOT touch … every other spec"), and COHESION holds no grant.
      - Escalating is the lawful act, and Repair 2 correctly declined an out-of-bounds write. But an escalation is not relief.
    - The pre-existing visual-checkpoint 1·6·7, 2 and 5 are INFO: they were RED at `3e709e4`.
    - **Honest-RED set: {`DOCK-SCROLL-MORPH`}.**

**Successors.** ⟨`grep -rn "Opens after" docs/tranches/X | grep -iE "F\.W13|F-W13|F\.W14"`⟩ → only F-W13.md's own `:4` ("F.W12 CLOSED", GREEN). §0bl names a future F.W14 for the UIA-F rows, but it is unauthored and declares no conjunct. **No successor is blocked** by this verdict.
**Loop note.** Three passes have found the same HIGH, and no bounded seat can cure it. The next act is COHESION's: rule ESC-F13a-1 + ESC-F13b-1, as the §0bk supplement-unit grant pattern did for X-W7. Another repair or check round without that ruling cannot change the verdict.

### Register
| # | severity | claim | receipt | cure |
|---|---|---|---|---|
| 1 | **HIGH** | §3 "the existing e2e suite unchanged-GREEN" is RED. 6 tests (+ checkpoint item 3) fail on the surfaces this wave's ordered cures retired, and the spec gives no relief | ⟨visualization-ux + contour-extraction, `--workers=4` ×2⟩ → `6 failed / 17 passed` exit 1 ×2; `:72 .play-btn--mini` not found; `contour-extraction:76` fill | COHESION grants ESC-F13a-1 + ESC-F13b-1 through one supplement unit on the §0bk pattern. The unit re-points `visualization-ux.spec.ts:72` → the DockControl `/(Play\|Pause) animation/`, re-points `contour-extraction.spec.ts:66-76` → the NumberField spinbutton, and re-baselines `checkpoint-disclosure-body`. Then the specs run GREEN ×2 |
| 2 | MINOR (mitigated) | The G-b disclosure limb asserts a round trip relative to the post-upload state | `f3af627` diff | None needed (Check 2) |
| 3 | INFO | Spec-literal `npm run typecheck` does not exist. `npx vue-tsc -b` is the equivalent (exit 0 ×2) | Baseline row | A dated addendum-beside when the spec is next touched (E-3) |
| 4 | INFO | `.c` r1–r4 and `.a`'s ToC scroll-to-top loss are owner-named residuals | Close §5 | X·F next wave / glass ask |

LEDGER: the status stays **PARTIAL 2026-09-17**, with an event line appended. No CLOSED stamp, because HIGH #1 is unrelieved.
