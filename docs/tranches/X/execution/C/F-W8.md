SERVED MODEL: claude-opus-5[1m]

# X.F.W8 — execution record (Track C · X·F · the CRUD union prototype, end to end)

Spec: `docs/tranches/X/fourier/waves/F-W8.md` (374 lines, read WHOLE at this seat).
Authority: COHESION `§0i` · `§0j` (the begin-word) · `§0j.D` (X·F's owner docket) · `§0k`–`§0v`
(read to file end; only `§0j.D` and `§0k.1`/`§0k.2` bear on this wave).
Runbook: `§1.3` (Track C order) · `§3.4` (locks) · `§5` (seat law).

---

## Open

**Date of record**: 2026-09-17 (the owner's begin-word, COHESION §0j). Wall clock of this sitting:
2026-09-19. Seat: **SEAT 0 (OPEN)**, `claude-opus-5[1m]`, VERIFY-AND-PLAN only — this seat authors
**no** wave artefact, turns **no** gate, and writes **no** byte of the nine `§2a` paths.

### Crash-recovery (standing law, host restart 2026-09-18)

⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js status --porcelain` → **11 rows**:

```
 M demo/color-picker/App.vue            ⟵ Track A (X·V), not mine
 M demo/color-session/keys.ts           ⟵ Track A
 M demo/picker/ColorPicker.vue          ⟵ Track A
 M demo/shell/dock/ActionToolbar.vue    ⟵ Track A
 M demo/shell/dock/Dock.vue             ⟵ Track A
 M demo/shell/dock/layers/ActionBarLayer.vue    ⟵ Track A
 M demo/shell/dock/layers/GenericActionBar.vue  ⟵ Track A
 M demo/shell/usePaneRouter.ts          ⟵ Track A
 M docs/tranches/V/reformation/CARRY-LEDGER.md  ⟵ not in F.W8's writable set
 M eslint.config.js                     ⟵ not in F.W8's writable set
 M scripts/dev/dev.sh                   ⟵ UNOWNED, never staged, never touched
```

**Not one of the eleven is inside F.W8's `§2a` writable set.** No inherited work on this unit
exists; nothing was stashed, restored or touched. ⟨cmd⟩
`git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain | wc -l` → **0** (and the
fourier tree is READ-ONLY, always — this wave writes zero fourier bytes in any verb).

### Preconditions — `Opens after: F.W0 · F.W1.close · F.W2 · F.W5` (§1 State `:63`)

Verified at the LEDGER **and** at the bytes:

| precondition | ledger (`docs/tranches/X/execution/LEDGER.md`) | bytes |
|---|---|---|
| **F.W0** (substrate pre-gates — HARD) | `:62` **CLOSED 2026-09-17** (honest-RED G-4 · G-5 · G-8 · G-15(d)) | `fourier-analysis/docs/tranches/F/SUBSTRATE-LEDGER.md` — **201,985 B**, present; it carries §2.1 (G-11 ANCHOR table) and §2.2 (G-12 DENOMINATOR table), the two tables every F.W8 anchor is re-resolved against (D-19). `fourier-analysis/docs/tranches/F/coordination/INBOX.md` present (23,278 B) — the mail-ledger surface COHESION §0k.1 declares LIVE |
| **F.W1.close** | `:64` **CLOSED 2026-09-18** (honest-RED G9 · G20(b)); CHECK 1 CONFORMANT-HONEST-RED | — |
| **F.W2** (ordering) | `:65` **CLOSED 2026-09-17** (honest-RED G15); IMPLEMENTED 2026-09-17 | — |
| **F.W5** (the ADMISSION KEYSTONE — *artifacts, not prose*) | `:63` **CLOSED 2026-09-17**; CHECK 2 CONFORMANT, 22/22 gates GREEN at an independent clock | all three artifacts present at `docs/tranches/X/fourier/contract/`: `J-diff-shape-v2.md` (244,274 B) · `operation-register.md` (38,255 B) · `OWNER-RULINGS-F.W5.md` (18,702 B) |

**All four preconditions MET.** The wave opens.

### Owner-gated items — RULED, cited by id, never presumed (§5.7; runbook `OG-F1` row)

The spec names **4** owner-gated conditions (§1 `:65`: G1, G7, G12, and G3's denominator freeze).
Each is ruled at **COHESION §0j.D** and is consumed, never re-opened:

| spec item | ruling id (COHESION §0j.D) | the word, as ruled |
|---|---|---|
| **G1** ⊙ — TA-4 disposition | **F-SS4REST R1** | *"**RE-SCOPE value.js out of the diff clause** (a one-sided §6 verdict, stated explicitly) — `atomdiff.ts` is wholly excised from value.js and restoring it is value-side authoring that would couple value.js's release train to the fourier contract (the §0i.1 logic)."* ⟹ the spec's §1b branch **(b)** is the ruled branch; the honest default becomes the ruled default. Branch (a) (restoration as PREREQUISITE) is **dead**. |
| **G7** ⊙ — off-state / cardinality admission (M-9) | **F-SS4REST R5** | *"**STOP MINTING** the off-state `[]`; the contract does not admit it (no silent rewrite)."* ⟹ the §R R3 fixture proves **that** branch and no other; F.W8 may not author on a guessed branch and no longer needs to. |
| **G12** ⊙ — codegen vs hand-typed twins (FR-AFP-71) | **F-SS4REST R7** | *"**CODEGEN** — twins derived from one source (inv-16/inv-26 restated; structural invariants over prose)."* ⟹ the register **IS** the generated shape, or each departure is documented with its reason. |
| **G3** ⊙ — the C31 denominator freeze | **OG-F1** | *"**FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE** (the lane's own evidence; measurements live-reproduced; R6 `NO_SUCCESSOR`)."* ⟹ the R-coordinates stay **frozen** and the C31 measurement enters **by adoption**; terminal RED **30/37** is the denominator of record, and **32/38 only if lawfully replaced** by C31A/C31B. No re-cut denominator may be published on this seat's or any unit's own authority. |

**One further §0j.D row lands ON this wave and is carried into the unit plan** — **F-PRODRET**
(R3 ≡ D3 ≡ G11, the admission gate): *"**PRODUCER**, as a **port** of value.js's shipped verb
(`POST /:slug/flag`) under the SS-4 contract, **homed at F.W8** (the CRUD union prototype) with F.W5
writing the clause; FR-AFP grades re-derive at the populated surface."* F.W8 owns **no product
source** (§1c, §2b), so the home is discharged **in the register**: the port is *specified* with its
operation model and its authority class, and the bytes are the fourier API row's act. Two further
§0j.D rows are consumed as context and book nothing here: **R4** (REMOVE the affordance), **R8**
(REMIX + BORN-PRIVATE), **R9** (DELETE the dead session subsystem — the §M M2 latent-bootstrap row's
ruled disposition), **OG-F2** (CODEX-ERA-SPECIFIC).

**No later addendum (§0k–§0v) re-opens, moves or adds an F.W8 ruling.** ⟨cmd⟩
`grep -n "F\.W8" docs/tranches/X/COHESION.md` → **one substantive line, `:987`** — the F-PRODRET
home quoted above; every other hit is `KF.W8`, Track B's wave.

### E13 Step-0 — the four-path mail sweep (§5.3)

Swept read-only at this seat's own clock, compared against **every row** of
`docs/tranches/V/coordination/INBOX.md`; classification taken from each row's **Status cell by
position**, never from a bare `grep -i unread`; `INBOX.md` **self-excluded** (SELF-COUNT law).

```
⟨cmd⟩ sed 's/\\|/@PIPE@/g' INBOX.md \
      | awk -F'|' '/^\| [IO]-[0-9]+[a-z]? \|/ {s=$6; gsub(/^ +| +$/,"",s);
                   if (s ~ /^\*\*?UNREAD/) c++} END {print c+0}'
  → 0        (double-run: 0 ≡ 0)
⟨cmd⟩ grep -cE '^\| [IO]-[0-9]+[a-z]? \|' INBOX.md          → 78 rows
```

1. **`docs/tranches/V/` + `V/coordination/`** — ⟨cmd⟩
   `find docs/tranches/V -maxdepth 2 -type f -name '*.md' -newermt "2026-09-19 02:30"` → **1
   member, `INBOX.md` (self)**: nothing has landed since the KF.W11-open sweep at the file's tail.
   10 depth-1 `.md` ⊕ 24 coordination entries; the five 2026-09-18 22:0x letters are **ours**
   (O-34..O-38).
2. **`../glass-ui/docs/tranches/BK/coordination/`** — **BK re-confirmed the newest tranche dir**:
   ⟨cmd⟩ `/bin/ls -dt ../glass-ui/docs/tranches/*/ | head -3` → `BK/` (Sep 18 17:53) · `BJ/` ·
   `BI/`. **9** entries; newest `glass-outbound-2026-09-18-valuejs-o26-reply.md` = **I-35, rowed**
   (`INBOX.md:115`).
3. **`../keyframes.js/docs/tranches/V/coordination/`** — **13** entries; newest inbound-grammar
   letter `VALUEJS-INBOUND-2026-09-17-o8-o11-amendment-addendum.md` = **O-21, ours, delivered**.
   `INBOUND-LEDGER.md` is keyframes' own ledger, not a letter to us.
4. **`../sci-report/atlas/docs/tranches/P/coordination/`** — **28** entries; newest
   `valuejs-inbound-2026-07-27-library-band-export-delta.md` = **O-12, ours**; path UNMOVED.

**Result: ZERO unrowed letters addressed to value.js · ZERO new `I-n` minted (register tail stays
I-35 / O-38) · ZERO UNREAD in F.W8's scope.** `INBOX.md` is therefore **not touched by this seat**
(the wave's one append-only row is unit `e`'s, at close — §2a).

---

## Baseline — the §4 born-RED gate set, measured BEFORE any cure

**Reading discipline (§4, binding).** **(1) MEASURE-AT-OPEN**: every fourier figure and anchor is
re-confirmed against F.W0's published §2.1/§2.2 tables before citation (D-19); this record asserts
none as fact. **(2) Split verdict**: a gate closes *for F.W8* when the fixture/assertion is
**authored with its disposition**; it goes GREEN only when the named owner lands the change.

### B.1 — The F.W8 side: all nine `§2a` artefacts ABSENT (double-run)

```
⟨cmd⟩ for p in <the six creatable paths>; do [ -e "$p" ] && echo PRESENT || echo ABSENT; done
  ABSENT  docs/tranches/X/fourier/conformance/union-prototype-walk.md
  ABSENT  docs/tranches/X/fourier/conformance/fixture-register.md
  ABSENT  docs/tranches/X/fourier/conformance/C31-two-sided-reproduction.md
  ABSENT  docs/tranches/X/fourier/conformance/OWNER-RULINGS-F.W8.md
  ABSENT  docs/tranches/X/fourier/conformance/walk/union-walk.mjs      (dir `walk/` absent too)
  ABSENT  docs/tranches/X/fourier/waves/F-W8/carry-closure.md          (dir `F-W8/` absent too)
run 2 — identical, six for six.
⟨cmd⟩ ls docs/tranches/X/coordination/ | grep -i 'fn6\|fixture-register'  → no output
      (the relay letter does not exist)
⟨cmd⟩ grep -rl "F8-C31A" docs/tranches/X --include="*.md"
  → docs/tranches/X/fourier/waves/F-W8.md   (the spec alone; 0 of 2 controls exist)
```

**Every one of the 16 gates is RED on the F.W8 side at open**, by absence of the artefact that would
carry its disposition. Nothing is inherited, nothing is half-written.

### B.2 — Gate table, BEFORE

| # | gate | BEFORE verdict | measured at this seat |
|---|---|---|---|
| **G1** ⊙ | TA-4 disposition declared | **RED** (ruled, undeclared) | value-side, re-run below (B.3). The ruling exists (**F-SS4REST R1**); **no artefact of this wave declares it** |
| **G2** | five-verb walk has a client edge on both sides | **RED** | `union-prototype-walk.md` ABSENT ⇒ **0 of 10 cells** carry a disposition + authority class |
| **G3** ⊙ | two-sided C31 reproduced, then isolated | **RED** | `C31-two-sided-reproduction.md` ABSENT; **0 of 2 controls (F8-C31A/B) exist**. ▲ and see **F-1** below — the client leaf's own bytes have moved |
| **G4** | every fixture generated FROM the operation model | **RED** | `fixture-register.md` ABSENT ⇒ no row names a source operation model |
| **G5** | model output byte-matches the wire | **RED** | register ABSENT; no bypass is named |
| **G6** | round-trip replay reproduces its own frame | **RED** | no round-trip assertion exists |
| **G7** ⊙ | off-state / cardinality admission ruled | **RED** (ruled, unproven) | ruling **F-SS4REST R5** = STOP MINTING; **no fixture proves the ruled branch** |
| **G8** | cache identity ⊇ consumed request fields | **RED** | no derive-leg fixture asserts `contour_hash` instability |
| **G9** | the probe does not perturb what it measures | **RED** | no walk record ⇒ no declared-and-subtracted perturbation |
| **G10** | create leg idempotent-or-declared, both sides | **RED** | register ABSENT ⇒ neither half recorded |
| **G11** | validate-at-boundary exists somewhere | **RED** | witness re-measured below (**F-3**); no fixture asserts error-not-`NaN` |
| **G12** ⊙ | one generated/checked contract shape | **RED** (ruled, unbuilt) | ruling **F-SS4REST R7** = CODEGEN; the register does not exist to be it |
| **G13** | liveness — producer AND consumer per field | **RED** | the walk does not exist ⇒ the unconsumed set is unmeasured |
| **G14** | fixtures BOTH directions; FN-6 relayed | **RED** | register ABSENT · letter ABSENT · no INBOX row |
| **G15** | citation closure, ∅ in BOTH directions | **RED** | `F-W8/carry-closure.md` ABSENT; the difference is **unrun** |
| **G16** | the admission horizon's only UI entry loads the saved entity | **RED** | no assertion exists; witness re-measured below (**F-2**) |

### B.3 — G1's value-side witness, re-run at this seat (A-1 / A-3, §1b)

```
⟨cmd⟩ grep -rn "atomdiff\|atomDiff\|AtomDiff" $V/api/src $V/src
  api/src/modules/palette/__tests__/palettes-forks.test.ts:9:
     * atom-diff were excised at T.W1 — TA-4 — so the remix/atomDiff wire cases are
  → 1 hit, double-run 1 ≡ 1
⟨cmd⟩ ls $V/api/src/lib            → No such file or directory
⟨cmd⟩ ls $V/api/dist/lib/crud/     → atomdiff.d.ts · atomdiff.d.ts.map · atomdiff.js · atomdiff.js.map
                                     (4 untracked build artifacts — the corpse, unchanged)
⟨cmd⟩ git log --oneline --all -- api/test/conformance/diff.test.ts
  a8ff7792 refactor(T.W1 · api · TA-4): excise the write-only atom-diff apparatus (the /remix+/diff fold)
  7351297f test(R · W6): the 5 wire-envelope shape-fixture rows in diff.test.ts
  …
⟨cmd⟩ git show --name-status --oneline a8ff7792 | grep 'conformance\|atomdiff'
  D  api/src/lib/crud/atomdiff.ts
  D  api/test/conformance/diff.test.ts
```

**A-1 reproduces exactly**: 0 extant · 1 landed-then-excised · 0 reciprocated.

**A-3 has drifted again — recorded as a re-resolve obligation, not adopted** (D-19, the value-side
half the F.W0 lock does not reach): the spec's corrected anchor reads `model.ts:84`; ⟨cmd⟩
`grep -n "interface PaletteVersion" api/src/modules/palette/model.ts` → **`:125`**. The *file* and
*interface* are right; the **line** has moved. Every unit re-resolves before citing.

### B.4 — Three findings of this seat's own (R.2: a GREEN before its cure is a finding)

The fourier tree has moved since the spec was sealed (2026-08-30). ⟨cmd⟩
`git -C $F log --oneline -1` → **`21e11b0` fix(fourier-web): X.F.W3.c — the search family leaves the
box it could not escape, and is scored on purpose**, worktree **0 dirty**. Three born-RED witnesses
were spot-measured read-only against it (probe parsimony, §5.2 — four bounded reads, no live probe):

- **F-1 · G3's client leaf reads CURED at HEAD — the gate is still RED, the *witness* is not.**
  The spec's C31 reproduction rests on *"C31's client-side edit at `web/src/lib/api.ts:420`
  (PATCH→PUT) breaks the client↔operation match"*. ⟨cmd⟩
  `sed -n "$(grep -n 'export async function updateVisualization' web/src/lib/api.ts | cut -d: -f1),+14p"`
  → the function now stands at **`:497`** and issues **`{ method: "PATCH", … }`** against
  `@router.patch("/{slug}")` (`api/routers/visualizations.py:350`). **The two-sided REJECT may not
  reproduce at this HEAD.** This is unit `c`'s subject, measured at unit `c`'s own clock: the
  reproduction is the DIAGNOSTIC, and a diagnostic that no longer fires is a finding to be stated
  with its receipt — **never** a gate quietly declared GREEN, and **never** credited to F.W8
  (FR-GIG-5 mirror: no wave is credited with another's cure; the cure, if it is one, belongs to
  whichever wave landed it, and unit `c` names that wave from the git record).
- **F-2 · G16's witness HOLDS, its anchors do not.** `loadVisualization` is specified at
  `workspace.ts:197-232`; ⟨cmd⟩ `grep -rn "loadVisualization" web/src` → **4 hits, all inside
  `stores/workspace.ts`** (`:211` the declaration · `:250` the internal `loadSnapshot` alias ·
  `:462`/`:463` the export + its comment). **Zero call sites outside the store** — the data-dead
  reading stands; the line anchors have moved by ~14.
- **F-3 · G11's witness HOLDS, its anchor does not.** The single unchecked cast specified at
  `api.ts:192` now stands at ⟨cmd⟩ `grep -n "as T" web/src/lib/api.ts` → **`:270`
  `return { data: (await res.json()) as T, etag, response: res };`** (with `:267`'s
  `undefined as T` beside it).

▲ **A fourth reading, banked as SCOPED and explicitly NOT a verdict.** The spec's G2/P5 witness is
*"seven of thirteen `/api/visualizations` operations … have **zero client function**"*. A crude
decl-scan over `web/src` ⟨cmd⟩
`grep -rnE "(function|const) [A-Za-z]*<verb>[A-Za-z]*[ (=]" "$F/web/src" --include="*.ts" --include="*.vue"`
returns `remix 0 · publish 6 · unpublish 1 · forks 0 · provenance 0 · diff 3 · versions 0`, whose
`publish` hits are store-level wrappers (`stores/gallery.ts:307,:325`) — exactly the shape the
witness already describes as *"PATCHes `{visibility}` behind an extra GET"*. **A name is not an
edge**: the per-verb disposition is unit `a`'s measurement, taken against **F.W5's
`operation-register.md`** (consumed, never re-derived — §6 and X-9 bind), and no coverage percentage
is published before X-9's member-scope law.

**No gate measured GREEN before its cure.** The three findings above are witness-level and are
carried into the unit briefs so that no unit inherits a stale anchor as fact.

---

## Unit plan

**5 serial** (§1 `Agents`; §2c Disjointness: units **b**, **c**, **d** share the fixture-register
identity space `F8-CLIENT-*` / `F8-C31*`, so they are **sequenced, not parallelised**:
`a → b → c → d → e`, **each committing before the next opens**; a dirty tree at handoff halts the
wave). Concurrency **1**. Model: **opus** for all five — §Half declares the *library half — Opus
author seat*, and the spec names no Fable, fresh-Fable, adjudicator or design-author seat (the
tri-fold's Fable arbiter belongs to the authoring layer, which closed at PASS-15).

**Binding on every unit**: docs-only wave — **no product source byte moves in either repo**, and
`npm run typecheck`/`test`/`lint` are *not* this wave's gates but its **escape tripwire**, run once
at close (§5d). Zero fourier bytes, in any verb, ever (§5c). Pathspec commits only, one per unit,
`--no-verify`, `scripts/dev/dev.sh` never staged. E-3: every correction is a dated addendum-beside;
F.W5's artifacts, the 66 adjudicated records, `CENSUS-CANONICAL.md` and `COHESION.md` are
**read-only operands**. Every short id is **record-qualified** (R-5). Anchors are re-resolved against
F.W0 §2.1/§2.2 before citation (D-19) — F-1/F-2/F-3 above prove the obligation is live.

| unit | model | spec sections | writable | gates | locks / families |
|---|---|---|---|---|---|
| **F.W8.a** | opus | §1a `:99-101` · §1b `:103-118` · §3 §P `:167-178` · §4 G1/G2/G16 `:232,:233,:247` · §5a `:253-255` · §5c F.W0/F.W1/F.W2/F.W5 rows `:265-268` | `docs/tranches/X/fourier/conformance/union-prototype-walk.md` (create) | **G1 · G2 · G16** (+ the M3 admin-leg and M5 denominator cells that ride G2) | FR-GIG-5 mirror · K-1 (13/44, never zero) · X-9 (member-scope law before any percentage) · D9 vocabulary · X-4 CHANGED–CONTESTED · D-19 · probe parsimony |
| **F.W8.b** | opus | §3 §J `:180-190` · §4 G4/G5/G11/G12 `:235,:236,:242,:243` · §2a `:128-144` · §5b `:257-259` | `…/conformance/fixture-register.md` (create) | **G4 · G5 · G11 · G12** | fixture-direction law (generated FROM the operation model) · K-3 · K9 · MF-9 resolve-or-strike (F-W5 §3 G22) · `UTF8_BYTEWISE_CODEPOINT` · R-5 · `canonical_digest` SKIPPED-sanctioned · F-PRODRET port row |
| **F.W8.c** | opus | §3 J1 `:184` · §4 G3 `:234` · §5b · §5c F.W9/W10 `:271` | `…/conformance/C31-two-sided-reproduction.md` (create) · `…/conformance/fixture-register.md` (append `F8-C31*` rows only) | **G3** ⊙ | OG-F1 denominator freeze (30/37 terminal; 32/38 only if lawfully replaced) · the verbatim C31A/C31B constraint set · zero fourier bytes · **shares the register with b/d — serial lock** |
| **F.W8.d** | opus | §3 §R `:192-201` · §M `:203-211` · §D D2 `:218` · §4 G6–G13 `:237-244` · §5c F.W6/F.W7 `:269-270` | `…/conformance/walk/union-walk.mjs` (create; `mkdir -p walk/`) · `…/conformance/fixture-register.md` (append §R/§M/§D rows only) | **G6 · G7 · G8 · G9 · G10 · G13** | AUTHORED, **NOT RUN** (F.W9/W10 runs it) · DO-NOT-REGENERATE tripwire · FR-GV-24 · K12 · M-CK abort-key · BC-20 not re-booked · F-SS4REST R5 (G7's ruled branch) · **serial lock with b/c** |
| **F.W8.e** | opus | §3 D1 `:217` · §4 G14/G15 `:245-246` · §6 `:286-318` · §6a `:320-374` · §5d `:280-282` · §2a INBOX row | `…/conformance/OWNER-RULINGS-F.W8.md` (create) · `…/waves/F-W8/carry-closure.md` (create) · `docs/tranches/X/coordination/value-to-fourier-2026-09-DD-fn6-reader-and-fixture-register.md` (create) · `docs/tranches/V/coordination/INBOX.md` (**append-only**, one row) | **G14 · G15** | E13 append-only (no existing row rewritten) · G15 operand = frozen `CENSUS-CANONICAL.md` §2 only · record-qualified pairs · **∅ in BOTH directions or the wave HALTS** · the letter is the only vehicle; the FN-6 reader stays fourier-owned |

### Briefs

**F.W8.a — the walk map.** Create `union-prototype-walk.md`: the five verbs (create · derive · diff ·
remix · history) × **two object kinds** = **10 cells**, each carrying a bound client method + named
consumer state **or** `SERVER_ONLY`, **plus an authority class** (M4) consumed from F.W5's
`operation-register.md` — never re-derived (the 45 = 30+13+1+1 triple and the 0/45 figure are cited,
not recomputed). Report **per verb which side cannot answer**, and state the two divergences
(born-`draft` vs born-`public`; fourier's deliberate non-transaction) **without harmonising them**.
Declare **G1's ruled branch out loud** (§0j.D F-SS4REST R1): the diff leg is **fourier-only**, the
union's diff verdict one-sided, in the record and in every cell that touches it. Carry the admin leg
(M3 batch-write state preservation) and G16's assertion (saved entity + slug/ETag, no cross-owner
draft row). Re-resolve every anchor via F.W0 §2.1/§2.2 first; no percentage before X-9.

**F.W8.b — the fixture register.** Create `fixture-register.md`: one row per fixture, **both
directions**, each naming the **operation model it is generated FROM** — a row sourced from a client
type is a gate failure by construction (G4). Every structurally-untyped operation is **typed at F.W5
or NAMED AND STRUCK with its reason** (J3). Name each `json.dumps` bypass whose model-generated
fixture cannot byte-match the wire (G5, `tier`/`likes`), resolving **or striking MF-9** before
quoting GCM-10's cure. Carry the boundary-validation row (G11, epicycle payload: error, not `NaN`)
and the one-generated-shape row (G12, ruled **CODEGEN**, F-SS4REST R7). Add the **F-PRODRET** port
row — value.js's `POST /:slug/flag` ported under the SS-4 contract, specified with its model and
authority class; bytes are the fourier API row's act. Bounds generated, never invented (J4); K-3
bars the 422-straddle. Record-qualify every id.

**F.W8.c — C31: reproduce, then split.** Measure the seam **at your own clock** first: the spec's
witness (`api.ts:420` PATCH→PUT) reads **CURED at HEAD** — `updateVisualization` is at `:497` and
issues PATCH against `@router.patch("/{slug}")` (see Baseline F-1). Name the wave that landed it
from the git record and claim **no credit** (FR-GIG-5). Then write
`C31-two-sided-reproduction.md`: the reproduction attempt with its **raw decoded receipt** (or the
honest non-reproduction with its receipt), followed by the **F8-C31A / F8-C31B** split — sole mutable
target per control, owner-only bypass, all-non-owner retention, same production validator, **no
caller-supplied expected code**. Denominator: **30/37 terminal, OWNER-FROZEN under OG-F1**; 32/38
only if lawfully replaced. Reproduction is the DIAGNOSTIC; isolation is the ACCEPTANCE. Zero fourier
bytes: no output, no scratch file, no run that writes.

**F.W8.d — the walk script + the round-trip, measurement and liveness assertions.** Create
`conformance/walk/union-walk.mjs` — **authored here, EXECUTED at F.W9/W10; no run under this wave**
(a run is F.W9's deploy spine, and it mutates fourier *data*, never bytes). Author the **§M
non-perturbation declaration** (G9): the walk's reads are provably non-mutating **or** the
perturbation is declared and subtracted, and auth mutations pass `retryOn429: false` or surface the
wait. Author the **§R** assertions: every atom of the version-identity set survives create → update
→ reload **with server-side units** (G6, incl. R5's last-visible-is-persisted); the derive-leg
`contour_hash` **instability** assertion (G8); create-leg idempotent-or-declared on both sides
(G10); and **G7's ruled branch — STOP MINTING `[]`** (F-SS4REST R5): the fixture proves a minted
off-state is **refused with a diagnostic, never laundered**. Author the **§D liveness** measurement
(G13): the traversal touches every envelope field and reports the unconsumed set, each member
carrying drop-or-consume. The walk must **never regenerate canonical geometry** (D2 tripwire).

**F.W8.e — rulings, relay, closure.** Create `OWNER-RULINGS-F.W8.md` carrying **only** what F.W5's
block does not own — (i) the fixture-direction law's exception set (FR-IC-6) and (ii) the TA-4
re-scope branch, now **RULED** at §0j.D F-SS4REST R1 and therefore *recorded as consumed*, never
re-ruled. Author the relay letter
`docs/tranches/X/coordination/value-to-fourier-2026-09-DD-fn6-reader-and-fixture-register.md` — the
**only** vehicle by which fourier receives the register; the FN-6 reader strategy stays
**fourier-owned**; a fixture re-opening `canonical_digest` **fails G14**. Append **one** E13 row to
`V/coordination/INBOX.md` (append-only; rewrite nothing). Run **G15's closure**: set-difference in
**BOTH directions** between the canonical rows this spec names and the §3 + §6 + §6a citation rows,
over **record-qualified `(record, id)` pairs**, against the frozen `CENSUS-CANONICAL.md` §2 rosters
only — never a check file, never a re-cut from the corpus. Commit the output to
`waves/F-W8/carry-closure.md`. **F.W8 books no canonical row: a booked row is a fabrication.** A
non-empty difference after two closure passes **HALTS the wave**. Close with the §5d tripwire run.

---

## Unit receipts

*(empty at open — each unit appends its own receipt below, line 1 `SERVED MODEL: <id>`, with its
commit hash, the gates it turned with their split verdicts, and the paths it wrote)*
