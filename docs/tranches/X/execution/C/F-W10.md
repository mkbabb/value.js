SERVED MODEL: claude-opus-5[1m]

# X.F.W10 — Coordination Discharge and the X·F Close (frontend half) — EXECUTION RECORD

⟨Track C · X·F (fourier-analysis) · the **TERMINAL** F wave · opened on the owner's begin-word of
2026-09-17 (COHESION §0j). Spec of record: `docs/tranches/X/fourier/waves/F-W10.md` (515 lines,
`planned`, twice-authored 2026-08-28, eleven repair rounds). **This record is the seat's own
surface; the spec is IMMUTABLE (E-3) and no byte of it is edited by this wave** — the spec's §1a
row 1 `CREATE` was discharged at authoring, and every correction this execution needs is a dated
addendum BESIDE.⟩

## Open

**Date**: the seat's own clock **rolled 2026-09-19 → 2026-09-20 during this open** and both readings
are published rather than one smoothed: every probe below was run on 09-19, the ledger row and this
record land on 09-20, and the **sitting of record stays 2026-09-17**, the owner's begin-word
(COHESION §0j), which is what the ledger status cell carries.
**Mode**: fresh OPEN — the ledger row read `planned` and no `C/F-W10.md` existed at this seat's
first act.

### Crash-recovery sweep (standing law) — no inherited work in this wave's writable set

⟨cmd⟩ `git status --porcelain` (value.js, this seat, at open) → 18 rows. Intersection with F.W10's
§1a writable set (the five CREATE paths · `COHESION.md` · `INBOX.md` · this record) = **∅**. The
dirty `docs/tranches/X/execution/{A/X-W5.md, A/X-W9.md, B/KF-W13.md, D/X-P-W4S.md}` and
`docs/tranches/V/reformation/CARRY-LEDGER.md` are **sibling seats' paths on other tracks** and were
not read for write, not staged and not touched; `scripts/dev/dev.sh` is the standing unowned dirty
row (NEVER touched); the `demo/**` + `e2e/**` rows belong to Track A. `LEDGER.md` reads dirty by a
sibling seat's bytes and is therefore re-read immediately before this seat's minimal in-place edit.

⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` → **1** row, `?? .worktrees/` (the X·F wave
worktrees). No partial predecessor work exists in any F.W10 path in either tree.

### Preconditions — verified at the bytes AND in the ledger

**F-W10 declares NO `Opens after` line** (runbook §1.3, the `F.W9 ↔ F.W10` edge row): its position
is derived — F-W9 `:15`'s chain places F.W9 above it, RE-CUT-0 is stated at both ends, and F-W10
`:7` §Scope fixes it as *"The F-side **terminal** wave"*. Since F.W10 sits below F.W9, **F.W9's
four restored predecessors are transitively F.W10's**.

| precondition | ledger | at the bytes |
|---|---|---|
| **F.W9** (direct) | `CLOSED 2026-09-17 (honest-RED: 11 gates)` ⟵ CHECK 2 CONFORMANT-HONEST-RED | `C/F-W9.md` exists (2199+ lines); its `c.7` carries the §4.10 census carve **authored whole, awaiting F.W10's CREATE** (E-F9-2) |
| **F.W0** (transitive, substrate) | `CLOSED 2026-09-17` | F8-REACH-01/02 DELETE executed: ⟨cmd⟩ `find web/src -name 'InfoCard.vue' -o -name 'CanvasOverlayButton.vue' \| wc -l` → **0** |
| **F.W1** (transitive, atomic) | `CLOSED 2026-09-18` | the tri-package uplift landed: `web/package.json` → `@mkbabb/glass-ui ^8.0.0` · `@mkbabb/keyframes.js ^6.0.0` · `@mkbabb/value.js ^4.0.0` (was `^0.13.0`) |
| **F.W3** (transitive) | `CLOSED 2026-09-19` | — |
| **F.W4** (transitive) | `CLOSED 2026-09-17` | the `/paper` producer relay left as **O-33** |
| **RE-CUT-0** (§4a.1: precedes every other row) | — | both shared-splice receipts run at this seat return **empty** (see Baseline G-F10-6) |

**Owner-gated items — RULED; this seat cites and re-opens nothing.** `OG-F1` →
**FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE** (COHESION §0j.D; GAB-13 discharges to a
disclosure line). `OG-F2 / OG-V2` → **CODEX-ERA-SPECIFIC** (§0j.D; the finding stays adopted, the
`FOURIER-R*` files are records, *"G-F10-8 closes on the charter line"*). `F8-REACH-01/02` → **G-10:
DELETE, in one breath** (§0j.D). `ESC-1 / G1` → glass **8.0.0**, registry-pinned `v8.0.0` @
`17a11bc5` (§0i.3). **L-4's ToC-ownership escalation** → **ESC-2, ONE owner** — a single
`useSidebarFollow` owned by the paper view through a typed `InjectionKey`; ruled at F.W1, F.W4
executes (§0o) — so §2.3's *"F.W10 CANNOT STAMP TERMINAL DISPOSITION WHILE THE ESCALATION IS
UNANSWERED"* is **satisfied by ruling, not by presumption**. **SS-4's seven rulings** →
`F-SS4REST` (§0j.D): R1 (TA-4) **RE-SCOPE value.js out of the diff clause**, R4 REMOVE, R5 STOP
MINTING, R6 KEEP, R7 CODEGEN, R8 REMIX + BORN-PRIVATE, R9 DELETE; `F-TRIE` → **NO TRIE**;
`F-PRODRET` → **PRODUCER**, homed at F.W8.

### E13 Step-0 — the four-path mail sweep (runbook §5.3), run READ-ONLY at open

| path | reading (this seat, 2026-09-19) |
|---|---|
| 1. `docs/tranches/V/` + `docs/tranches/V/coordination/` | newest mail is the **2026-09-18** 4.1.0 quartet (parse-that addendum-2 · fourier facility19 delta · glassui r1 relay · atlas export-delta refresh) ⊕ the `valuejs-outbound-2026-09-18-kfw7-bh-relay*` pair; **every one already rowed** |
| 2. `../glass-ui/docs/tranches/BK/coordination/` | ⟨cmd⟩ `ls -td ../glass-ui/docs/tranches/*/ \| head -3` → `BK/` · `BJ/` · `BI/` — **BK is still the newest tranche dir**. Its nine files' newest are `glass-outbound-2026-09-18-valuejs-o26-reply.md` · `glass-outbound-2026-09-17-{valuejs-o20-disposition,constellation-o20-relay,bbnf-lang-9.0.0-addendum}.md`; all rowed |
| 3. `../keyframes.js/docs/tranches/V/coordination/` | newest is `VALUEJS-INBOUND-2026-09-17-o8-o11-amendment-addendum.md` (our own outbound, rowed) |
| 4. `../sci-report/atlas/docs/tranches/P/coordination/` | newest is `valuejs-inbound-2026-07-27-library-band-export-delta.md`; rowed |

**Nothing dated 2026-09-19 or 2026-09-20 exists on any of the four paths** — ⟨cmd⟩ `find <the four
paths> -name '*2026-09-19*' -o -name '*2026-09-20*'` → **no output**.

**Unrowed mail addressed to value.js: ZERO.** No `I-n` is minted by this open, and `INBOX.md` is
therefore **not touched by this seat** (the open commit's pathspec carries only the record and the
ledger).

**Classification read BY POSITION, never by a bare `grep -c UNREAD`** (the standing correction):
⟨cmd⟩ `awk -F'|' '/^\| *[IO]-[0-9]+ *\|/ { if ($6 ~ /UNREAD/) print $2 }' INBOX.md` → **four rows**
— `O-20` · `I-31` · `I-32` · `O-39` — and **every one of the four describes a prior or already-
answered state** in its own Status cell (`O-20` **SENT** 2026-08-28 with a sweep note *about* two
unread rows at the far end; `I-31` **FOLDED 2026-09-17**; `I-32` **READ IN FULL + ROUTED —
TERMINAL AS MAIL 2026-09-19**; `O-39` **SENT** 2026-09-19). The bare `grep -c UNREAD` answers
**82**, which is this file's own header vocabulary and prose, not a row census; the positional row
census is ⟨cmd⟩ `awk -F'|' '/^\| *[IO]-[0-9]+ *\|/{n++} END{print n}'` → **78**.

**E13 verdict at open: 0 unrowed · 0 rows carrying a live UNREAD status in F.W10's scope.**

## Baseline — the spec's §3 gates, run READ-ONLY at open (the BEFORE state)

Twelve gates, every one declared born-RED at 2026-08-28 (G-F10-12 born **SPLIT**). This seat re-ran
each witness at its own clock on 2026-09-19, **double-running every corpus probe**, and publishes
the reading whether it flatters the spec or not. **R.2 applies: a GREEN before its cure is a
finding, not a credit** — six legs have gone green since the spec was written, every one of them by
a *predecessor wave's* landed work, and each is named below so the close cannot bank a
predecessor's cure as F.W10's.

| gate | born state (spec, 2026-08-28) | THIS SEAT, 2026-09-19 | verdict at open |
|---|---|---|---|
| **G-F10-1** asks reconciled both ledgers | RED — 4 value-targeted asks OPEN; INBOX rows 0 | `ADOPTION-ASKS.md` live table: **Ask 3** (P1, `rsync→git checkout`) · **Ask 5** (P3, CF Pages) · **inv-22-color** (P3) · **cascade-vjs** (P2) all still read **OPEN — re-affirmed G.W8**. Value INBOX: ⟨cmd⟩ `grep -ci` → `Ask 3` **0** · `Ask 5` **0** · `inv-22` **0** · `cascade-vjs` **0** · `ADOPTION-ASKS` **0** | **RED — reproduces exactly** (E-8's UNROWED, not unread) |
| **G-F10-2** inbound-mail parity | RED — no fourier CLAUDE.md, `find docs -name INBOX.md` → 0, facility-19 untracked | fourier `CLAUDE.md` **EXISTS**; ⟨cmd⟩ `find docs -name INBOX.md` → **1** (`docs/tranches/F/coordination/INBOX.md`, **242 lines**, live — COHESION §0k); facility-19 **TRACKED** (`git ls-files` → `docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md`). FN legs: ⟨cmd⟩ `grep -c 'FN-'` in their INBOX → **0**, while our side already sent `docs/tranches/X/coordination/value-to-fourier-2026-09-18-fn-answers.md` | **SPLIT — three legs GREEN BEFORE CURE** (F.W0's act, COHESION §0k); **RED** on the FN-1..FN-7 fourier-side verbs |
| **G-F10-3** G24 drift RELAYED | RED, MEASURE-AT-OPEN — banked 8/22 curves, max\|Δ\| 0.192 | ⟨cmd⟩ `node docs/tranches/V/megatranche/audit/probes/fourier-value-import-drift.mjs` → `ERR_MODULE_NOT_FOUND … fourier-analysis/web/node_modules/@mkbabb/value.js/dist/value.js`. The package **is installed** (4.0.0) and its `dist/` ships `anchors-*.js` · `operations-*.js` · `result-*.js` · `subpaths/` — **no `dist/value.js`**: the probe's import path is the 0.13-era layout, stale against the 4.x export map | **RED — UNRUNNABLE AS WRITTEN**, cause named. The probe file is **outside §1a** — F.W10 relays the measured state, it does not repair X-W9's instrument |
| **G-F10-4** G33 packets + post-window pin truth | RED — `web/package.json:18` `^0.13.0`; `npm ls` ELSPROBLEMS ×6 | fourier `web/package.json` → `@mkbabb/value.js ^4.0.0` · `@mkbabb/glass-ui ^8.0.0` · `@mkbabb/keyframes.js ^6.0.0`. ⟨cmd⟩ `npm ls @mkbabb/value.js` → **exit 0** in fourier (`4.0.0`, keyframes 6.0.0 dedupes onto it) **and exit 0** in keyframes.js | **SPLIT — the pin leg is GREEN BEFORE CURE** (F.W1's atomic transaction); the **five packets + five INBOX rows** leg is untested and stands |
| **G-F10-5** latex carry has a receiving surface | RED — register absent; `grep -c 'latex-paper' INBOX.md` → 0/1 | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` **ABSENT**. But ⟨cmd⟩ `grep -c 'latex-paper' INBOX.md` → **4**, and two are real relay rows: **O-24** (`X/coordination/fourier-to-latexpaper-2026-09-17-nwo5-latex-relay.md`, F.W1 unit `b`) and **O-33** (`value-to-latexpaper-…-fw4-relay.md`, F.W4) | **RED on the register**; the baseline **moved again** (E-14's own lesson) — two dispatched letters exist and the register must RECONCILE against them, never re-send |
| **G-F10-6** ≡ **G-F9-21** re-cut complete, Δ=∅ | RED on the corpus leg | double-run, `registry/adjudicated/`: `F\.W9/W10` → **54** · records → **25** · bare `F.W10` → **0** · `ls fr-*.md` → **66** (identical both runs). Splice receipts, this seat: checkpoint-block `diff` → **empty**; re-cut-table `diff` → **empty** | **RED on the corpus leg, GREEN on BOTH splice legs before cure** — and the spec's own warning binds: *"a later seat must not read the green leg as the gate"* |
| **G-F10-7** NWO drain, F side | RED — COHESION §4 every row an X·V slug, ZERO fourier rows | §4 now carries **one** fourier block: **F.W3's S-8 EMISSION 2026-09-19** (seven census-methodology rows it emits and does not own). The **F.W10 drain block — 89 rows / 89 ids / 89 terminal verbs — is ABSENT.** Corpus detector, double-run: `NO-WAVE-OWNER` occurrences **167** · lines **165** · records **56** of **66** · en-dash arm **0** | **RED** — the F.W3 emission is a *different* block and is cited, never counted |
| **G-F10-8** Codex residue not inherited | RED — charter line in no F spec; OG-F1/F2 unanswered | **OG-F1 and OG-F2/OG-V2 are RULED** (COHESION §0j.D). ⟨cmd⟩ `grep -rl 'Zero UNVERIFIED credit' fourier/waves/*.md` → **F-W10.md only** (the spec quoting its own obligation); the census addendum that must carry it does not exist. `/tmp/fourier-r4-files.sha256` **GONE** (R4-12 moot, re-confirmed). Codex worktree `~/.codex/worktrees/d0be/fourier-analysis` **still registered** at `cd26c65`. `FOURIER-R*` → **9** our side | **RED** on the charter line + the six terminal verbs; the two owner legs are **discharged by ruling** |
| **G-F10-9** NEXT-COMMUNIQUE ledger truth | RED — intake `:221` declares the channel EMPTY | `INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` **ABSENT**; the dated intake's `### NEXT-COMMUNIQUE` heading stands untouched at its own bytes | **RED** |
| **G-F10-10** close-gate a11y honesty (FR-TT-1 lock) | RED — `grep -rln "admin" web/e2e/` → 0 files; no axe on `/paper`; `/equation` one screenshot slug across 8 specs | ⟨cmd⟩ in fourier: `grep -rln "admin" web/e2e/` → **5 files**; `grep -rl AxeBuilder web/e2e/` → **4**; `/paper` named in **5** specs; `ls web/e2e/*.spec.ts` → **18** (was 8) | **SPLIT — the three born-RED witnesses are GREEN BEFORE CURE** (F.W9's seats). The gate itself is unrun: the close must run the axe close-gate over `/paper` + `/equation` + the admin tab, or record the NAMED, DATED WAIVER |
| **G-F10-11** F.W0 substrate terminal | RED — glass 4.0.0 dist: 35 top-level nodes, ZERO valid `@source`; GAB-13 28 dirty rows; F8-REACH un-dispositioned | at the **8.0.0** pin (§0i.3): ⟨cmd⟩ `postcss.parse(dist/styles/index.css)` → **OK, 40 top-level nodes**; `grep -c '@source'` → **1** valid, `grep -c 'source\`'` → **0**. GAB-13: `git status --porcelain \| wc -l` → **1** (`?? .worktrees/`), the WT diff LANDED. F8-REACH: both files **GONE** (`find` → 0), the §0j.D `DELETE` verb executed — **no HOLD survives** | **GREEN BEFORE CURE on all three limbs** — every one of them a *predecessor's* act (F.W1's pin · F.W0's land + delete); F.W10's claim is the VERIFICATION, which is exactly what §2.3 says it is |
| **G-F10-12** every relay arm has a grep-able receipt | SPLIT — GREEN by receipt on U-1..U-11 and the `@source`/FR-NP-32 arm; RED on `MetricPill` · `IntersectionObserver` · `latex-paper` | packets re-swept: ⟨cmd⟩ `find ../glass-ui/docs -name 'valuejs-outbound-*.md' \| wc -l` → **12** (eleven ⊕ the 2026-09-18 KF.W6 BH relay). Term sweep across all twelve: `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0** | **SPLIT, unchanged in shape**; the denominator moved 11 → **12** and is published, not inherited |

**Structural note carried into the plan (not a cure).** Six legs read GREEN before any F.W10 byte
exists — G-F10-2's three parity legs, G-F10-4's pin leg, G-F10-6's two splice legs, G-F10-10's
three coverage witnesses, G-F10-11's three substrate limbs, and (by receipt) G-F10-12's two U-arms.
**Not one of them is F.W10's work**: they are F.W0's, F.W1's, F.W4's and F.W9's landings, which is
precisely the shape §2.3 gives this wave — *F.W0 RULES; F.W10 VERIFIES, never re-rules.* The close
seat must therefore state each as VERIFIED-GREEN-BY-PREDECESSOR with the predecessor named, never
as a gate this wave turned.

## Unit plan

**The spec declares NO `Agents` line** — ⟨cmd⟩ `grep -c 'Agents' waves/F-W10.md` → **0**, unlike its
twin (`F-W9.md:15` = *"3 serial … never parallel"*). Runbook §5.1 makes a declared count binding;
where none is declared, the plan is derived from **§1a's eight owned rows**, **§3's twelve gates**
and **§4a's thirteen ordering locks**, and it inherits the twin's posture: **one writer at a time.**

**Concurrency = 1 throughout** (well inside the four-workflow cap, and inside §0ae/§0af/§0ag's
durability posture). The reason is structural, not cautious: **every unit's writable set contains
this record**, so any two concurrent units would share a modify path — which the seat law forbids —
and `COHESION.md` and `INBOX.md` are additionally shared with the three sibling tracks. Serial
execution is what buys the disjointness the spec's §1a table assumes.

**Model**: `a`–`g` are **opus** — M-12 TRI-FOLD's *"Opus solo for mechanical/challenge seats
(censuses, greps, gate runs, single-file cures)"*, which is what a ledger act is. `h` is **fable**,
because the spec's own **Close act** names it: *"runs the §3.5 L-20 conformance loop with a
**declared-Fable final pass**"* (§4b's X-whole row calls that loop *"the sub-tranche's own close
act"*). No other Fable, adjudicator or design-author seat appears anywhere in F-W10.md.

| unit | model | spec sections executed (heading · lines) | writable set (§1a) | gates | locks / same-commit families |
|---|---|---|---|---|---|
| **X.F.W10.a** — RE-CUT-0 and the census addendum | **opus** | §2.1 RE-CUT-0 `:51–64` · §2.2 the re-cut table + P-12 locks `:66–122` · §1a row 8 `:33` · §3 **G-F10-6** `:441` + **G-F10-8**'s charter limb `:443` · §4a.1 `:455` · §4b's **F.W9** row `:473` · §5 rows `:494`/`:497` | `docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` (**CREATE BESIDE**) · this record | **G-F10-6** · **G-F10-8** (charter limb) | **RE-CUT-0 PRECEDES EVERY OTHER ROW** (§4a.1) — nothing after `a` may stamp until its Δ=∅ receipts are banked · **PAIRED-EDIT LAW**: the shared §2.2 bytes may change only in BOTH specs in ONE commit — so the five disclosed pending pairs (**AA-44 `9`→`8`** + §5's four canonical divergences) are **re-disclosed and NOT applied** · **E-3**: `CENSUS-2026-08-03.md` is never rewritten · **E-F9-2**: F.W9's §4.10 carve is RECEIVED by splice from `C/F-W9.md` `c.7`, never re-authored |
| **X.F.W10.b** — the substrate verification, the Codex residue and the intake addendum | **opus** | §2.3 whole `:124–133` · §2.4's **NEXT-COMMUNIQUE** row `:148` · §2.5's head row `:161` and **FR-COB-20** `:183` · §3 **G-F10-8** `:443` · **G-F10-9** `:444` · **G-F10-11** `:446` · §4a.2 `:456` · §4b's **F.W0** row `:475` · §5 `:495`/`:507` | `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` (**CREATE BESIDE**) · this record | **G-F10-8** · **G-F10-9** · **G-F10-11** | **F.W0 SUBSTRATE PRE-GATES FIRST** (§4a.2) — verify, never re-rule · **E-3 addendum-beside**: `git diff --stat` on `INTAKE-ADJUDICATION-2026-08-03.md` must stay **empty** · **OG-F1 / OG-F2 / G-10 are RULED** (COHESION §0j.D) and are cited by id · **HOLD-is-RED**: F8-REACH carries `DELETE`, already executed — verify, book nothing |
| **X.F.W10.c** — the NO-WAVE-OWNER drain into COHESION §4 and the SS-6 accretion at §4a | **opus** | §2.5 `:153–184` · §2.5a `:185–319` · §2.5b `:320–339` · §R10-LAW `:340–366` · §2.7 `:389–398` · §3 **G-F10-7** `:442` · **G-F10-9**'s register limb `:444` · §4a.12 `:466` · §4b's **SS-6** row `:479` · §5's `:498`/`:499`/`:500` exclusions | `docs/tranches/X/COHESION.md` — **MODIFY-CARVE ONLY**: §4 (add the fourier NWO block) · §4a (accrete the fourier-registry SS-6 glass rows) · this record | **G-F10-7** · **G-F10-9** (one-register limb) | **§2/§3 of COHESION are root-authored — quoted, NEVER rewritten** (§1a row 3) · **S-16 IS ALREADY DISCHARGED IN THE SPEC** (§4b + §5 dated addenda, 2026-08-30): **22 id-rows ⊕ 4 §D entries = the packet's own 26**, and the *"correct COHESION §4a"* instruction is **WITHDRAWN** — §4a's 26 **STANDS** and is edited by nobody · **reconcile AGAINST the dispatched O-20, never re-send** (§4b) · **PRODUCER ROWS NEVER BECOME FRONTEND HACKS** · glass-ui is READ-ONLY (§1c) — new arms accrete to §4a, they do not travel |
| **X.F.W10.d** — the LATEX-PAPER-RELAY register | **opus** | §2.6 whole `:367–388` · §1a row 2 `:27` · §3 **G-F10-5** `:440` + **G-F10-12**'s latex arm `:447` · §4a.6 `:460` · §4a.10 `:464` · §4b's **LATEX-PAPER-RELAY** row `:480` · §5 `:508`/`:509` | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` (**CREATE**) · this record | **G-F10-5** · **G-F10-12** (latex arm) | **SC-3 ANCHOR LAW**: every anchor from `dist/vue.js` or the record — **never producer-src** · the **K-9-killed `:root` alias snippet may never appear as a prescribed repair** · **LAW-3** (PAW-44 restores WITH-or-AFTER PAW-1 + PAW-30) · **LAW-4** one clearance authority · **L-11 + M2 HARD LOCK** (no `.sidebar-top-btn` rename before the relay lands) · **PAW-39: quote 31, never 35** · reconcile against the **already-dispatched O-24 and O-33**; no third channel, no re-send |
| **X.F.W10.e** — the ADOPTION-ASKS reconciliation letter | **opus** | §2.4's **ASK-3** `:139` · **ASK-5** `:140` · **INV-22-COLOR** `:141` · **CASCADE-VJS** `:142` · **FN-1..FN-7** `:143` · **VALUEJS-2.0.0-NOTE** `:144` · **O-14 + INBOUND-PARITY** `:145` · §1a rows 4–5 `:29–30` · §3 **G-F10-1** `:436` · **G-F10-2** `:437` · §4a.13 `:467` · §4b's **fourier sub-session** row `:482` · §5 `:503`/`:504`/`:505`/`:509` | `docs/tranches/V/megatranche/coordination/FOURIER-ADOPTION-ASKS-RECONCILIATION-2026-08-25.md` (**CREATE**) · `docs/tranches/V/coordination/INBOX.md` (**mail rows only, append; existing rows never rewritten**) · this record | **G-F10-1** · **G-F10-2** | **the PATH is fixed so no seat invents a third channel** (§1a row 5) · **COMMISSION §2**: zero fourier-tree writes — ask, then verify receipt · **cascade-vjs is answered as the SPLIT using the EXISTING `docs/tranches/R/letters/CASCADE-VJS-RESPONSE.md`** — never a fresh letter · **no verb may name a peer actor's action as an acceptance condition** (G-F10-1's own falsifier) · the 2.0.0 note is answered **AT 4.0.0** · FN-5's *"BEFORE or WITH fourier M.W10"* rider is answered in that order or explicitly re-scoped |
| **X.F.W10.f** — the G24/G33 evidence relay | **opus** | §2.4's **G24** `:146` · **G33** `:147` · **RELAY-RECEIPT-LAW** `:149` · **MG-ι** `:150` · **GAB-29** `:151` · §1a row 6 `:31` · §3 **G-F10-3** `:438` · **G-F10-4** `:439` · **G-F10-12** `:447` · §4a.12 `:466` · §4b's **X·V** row `:481` · §5 `:506`/`:508` | `docs/tranches/V/megatranche/coordination/FOURIER-EVIDENCE-RELAY-G24-G33-2026-08-25.md` (**CREATE**) · `docs/tranches/V/coordination/INBOX.md` (**mail rows only, append**) · this record | **G-F10-3** · **G-F10-4** · **G-F10-12** | **RELAY-ONLY — the gates are X-W9's and are never claimed**; no fourier-tree edit is credited as an X-W9 gate · **MEASURE-AT-OPEN** on every relayed number; the banked 0.192 may NOT be quoted as current · **G33 identity discipline ×6**: `fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8`, ONE row, never re-booked per component · **RELAY-RECEIPT-LAW binds this wave to itself** (G-F10-12): a receipt is READ, never asserted · **MG-ι** relays value-side with the OWNER-MAIL FLAG; the parse-that→fourier edge is never minted |
| **X.F.W10.g** — the close: the F-side stamp of COHESION §3.1–§3.6 | **opus** | §Close act `:515` · §3 all twelve `:430–448` · §4b's **X-whole** row `:484` · §4a whole `:453–467` · §2.8 errata `:399–429` · §5 whole `:488–511` | `docs/tranches/X/COHESION.md` (§5 boundary row · the §0 X·F status cell — **carve only**) · `docs/tranches/X/execution/LEDGER.md` (F.W10's row + event line) · `docs/tranches/V/coordination/INBOX.md` (the E13 close-sweep line only) · this record | **all twelve, re-run at its own clock, double-run** | **no gate is argued green**: honest-RED with its failing case named is the lawful reading (the spec's own §3 posture) · every GREEN-BEFORE-CURE leg is stamped **VERIFIED-GREEN-BY-PREDECESSOR with the predecessor named** · **S-16 restated before close** · **E13**: no wave closes with UNREAD mail · **VERIFIED stays NO** — §1's four-verb line reserves that stamp; the close moves IMPLEMENTED only as far as the bytes permit |
| **X.F.W10.h** — the §3.5 L-20 conformance final pass | **fable** | §Close act `:515`'s conformance loop · §4b's **X-whole** row `:484` (§3.5) · L-18's two quartet gestalt passes | this record (its verdict section) · `docs/tranches/X/execution/LEDGER.md` (the row's verdict cell only) | — (VERIFY-ONLY: it re-runs, it authors no cure) | **VERIFY-ONLY**: authors no byte of any unit's artefact · **L-18**: no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance · the eight named L-18 bases are the checklist, headed by *"a drain complete-looking because it counted only what it chose to look at"* |

**Order**: strictly serial — `a` → `b` → `c` → `d` → `e` → `f` → `g` → `h`. `a` is first by §4a.1
(RE-CUT-0 precedes every other row) and `b` second by §4a.2 (F.W0 substrate pre-gates first);
`c` needs `a`'s re-cut receipts and `b`'s substrate verdict before it can claim a terminal verb for
any drain row; `d`, `e` and `f` are the three discharge acts and touch disjoint CREATE paths but
share `INBOX.md` and this record; `g` closes and `h` adjudicates. **No two units ever hold a write
lock on the same path.**

### Briefs

- **X.F.W10.a** — Re-run RE-CUT-0's four corpus probes and the two splice `diff`s, double-run, and
  bank them. Then CREATE `CENSUS-ADDENDUM-2026-08-25.md` (addendum-BESIDE; the dated
  `CENSUS-2026-08-03.md` is never touched) carrying: the **§4.11 re-cut record** (26 identities,
  one wave each); the **by-mechanism cut deviations** — `PAW-49 → F.W0` and `PAW-50 → F.W4`, with
  the third, `FR-TT-1 → F.W10`, recorded as **RETIRED at repair round 6** (cite-the-holder F.W4);
  the **FR-CP-44 metric-stack re-seat** correction; the **FR-AFP-51 tense** correction
  (UPLIFT-TARGET, not present-tense); and the **§6.7 charter line in its CORRECTED wording** —
  *"Zero UNVERIFIED credit — every Codex verdict enters the formation through per-claim
  adjudication (M-21)"*, never the superseded *"carry zero credit"*. **Receive F.W9's §4.10 carve
  by splice from `C/F-W9.md` `c.7`** (E-F9-2) — it is authored whole there and is not re-authored.
  Re-disclose the five pending paired edits; apply none.

- **X.F.W10.b** — VERIFY the F.W0 substrate terminal and CREATE the intake addendum. Verify at the
  bytes: FR-NP-32's dist **parses at the 8.0.0 pin**; GAB-13's worktree **landed** (porcelain 1);
  both F8-REACH files **gone**, `DELETE` executed, **no HOLD surviving**. Then CREATE
  `INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` beside the dated intake: amend the
  **NEXT-COMMUNIQUE** contradiction so every surviving ask sits in **exactly one register**
  (glass → SS-6 · latex → the §2.6 register), and give each Codex-residue row a terminal verb —
  **R3-9** record-only, the **R3-16 + R4-11 + R5-9 + R6-10** board row, **R4-12 MOOT** (the sha256
  file is gone; nothing consumed), **R5-3**, and the still-registered `~/.codex/worktrees/d0be`
  worktree. `git diff --stat` on the dated intake must stay **empty**.

- **X.F.W10.c** — Drain the F-side NO-WAVE-OWNER register into **COHESION §4** as one fourier
  block: **89 rows / 89 distinct ids / 89 terminal verbs** — §2.5's 23 ⊕ §2.5a's 36 ⊕ §2.5a-ii's
  16 ⊕ §2.5a-iii's 14 — every verb one of NAMED WAVE · NEXT-BOUNDARY LEDGER (BH-RELAY or
  CENSUS-INPUT) · KILL-WITH-RATIONALE · CITE-CONSUMING-WAVE · FOLD-WITH-TARGET, plus the ONE class
  verb over the weak class **enumerated whole** (32 lines / 19 records). **FR-NP-32 heads the
  table; `fr-App C-12` lands as the NEGATIVE record verbatim.** Re-run §2.5a-iii's five partition
  probes against the 66 first — `31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33 = 165` — and publish the transcript; a
  roster asserted rather than derived is the exact failure three repair rounds convicted. Then
  accrete the fourier SS-6 glass rows at **§4a**, reconciling against O-20, re-sending nothing.
  Cite F.W3's S-8 emission block; never count it in.

- **X.F.W10.d** — CREATE `LATEX-PAPER-RELAY.md`: row every §2.6 carry with its banked ids kept and
  none re-booked — **PAW-1** (42 sites; the `:root` alias limb KILLED) · **PAW-2** (+ its coupling
  law) · **PAW-11** · **PAW-28 ⊕ PAW-45 ⊕ PAW-46** (one letter, three ids) · **PAW-32** ·
  **PAW-38** · **PAW-39** · the **K-11 flattener rider** · **C-06** · **L/D5 + L/D30** · **L/D8** ·
  **L/D29** · **C-19 ⊕ PAW-33** · **C-20** · **L-11 + M2** — then **answer PAW-1's F.W1
  fourth-package question in the ledger** by reading what F.W1 actually ruled (added to the
  transaction, or explicitly seated), and **name the delivery point** (the producer has no inbox
  path — E-7). Reconcile against the two dispatched letters **O-24** and **O-33**; send nothing new.

- **X.F.W10.e** — CREATE the four-ask reply letter at its fixed path and row each ask in `INBOX.md`
  with a **disposition verb**: **Ask 3** (the P1 critical path) ADOPT / DECLINE-with-rationale /
  DONE — the verb only, never infrastructure execution; **Ask 5** against its verbatim machine
  checkable criterion (`cf-ray` from CF Pages, peaceiris removed) — the KEYFRAMES half is declared
  ∦, never absorbed; **inv-22-color** verb + relay, repair is maintainer-owned; **cascade-vjs** as
  the **SPLIT** — the `unplugin-vue-markdown` bump ADOPTABLE, the `file:`→published limb **DECLINED
  against R's Q4 ratification** — landed by relaying the EXISTING `R/letters/CASCADE-VJS-RESPONSE.md`
  into their ledger. Then **FN-1..FN-7** (reconciling against our already-sent `value-to-fourier-
  2026-09-18-fn-answers.md`), the **2.0.0 note answered AT 4.0.0**, and the inbound-parity legs:
  their INBOX and CLAUDE.md **exist**, facility-19 is **tracked** — verify, credit F.W0, claim
  nothing.

- **X.F.W10.f** — CREATE the evidence relay letter. **G24**: re-measure at open — the probe is
  UNRUNNABLE as written (its `dist/value.js` import predates the 4.x export map); relay the
  measured state and its cause, quote the banked 0.192 as **banked**, never as current, and repair
  no instrument outside §1a. **G33**: relay the packet + INBOX legs and the post-window `npm ls`
  reading (exit **0** in both trees today — F.W1's landing, credited to F.W1), holding the ONE
  identity `C-1 ≡ C-8` and carrying the uncommitted-bump dissent. Row both in `INBOX.md`. Then
  **G-F10-12's own law applied to this wave**: sweep all twelve packets for every declared arm's
  identifying term and publish what is missing (`MetricPill` · `IntersectionObserver` ·
  `latex-paper` = **0**) — a receipt READ, never asserted. Relay **MG-ι**'s value-side limb with
  the OWNER-MAIL FLAG.

- **X.F.W10.g** — The close. Re-run all twelve gates at this seat's own clock, **double-run**, and
  read each honestly: GREEN · honest-RED with its failing case named · SPLIT. Stamp the **F-side of
  COHESION §3.1–§3.4** (§3.1 measured against `CENSUS-CANONICAL.md`, the frozen census of record,
  never a carry, never a check file), record the **§5 boundary row**, move the **§0 X·F status
  cell** off `FORMING`, and audit the wave's commit roster for bounds. Re-state **S-16** (22 ⊕ 4 =
  26; the *"correct COHESION"* instruction stays withdrawn). Re-run the E13 sweep; close with zero
  UNREAD in scope. **VERIFIED stays NO.**

- **X.F.W10.h** — The declared-Fable **L-20 conformance final pass** over the whole X·F sub-tranche
  close (§3.5), VERIFY-ONLY: re-run the units' claimed greens at its own clock, test the roster by
  **re-running §2.5a-iii's five probes against the 66** rather than by reading any sentence, check
  every terminal verb for the *"decision that is really a deferral"* shape, and adjudicate against
  L-18's eight named bases. It authors no cure and no artefact byte; its output is a verdict and,
  if it finds one, a defect register.

## Unit receipts

⟨Each unit appends its own `### X.F.W10.<id>` subsection here, at its own clock, with its commits
named by sha and its gates read from the settled bytes. Nothing is written here in advance.⟩

### X.F.W10.a

**RE-CUT-0 and the census addendum** · opus (`claude-opus-5[1m]`) · 2026-09-20 · **status DONE**,
with `G-F10-6` read **honest-SPLIT** and its corpus leg published unchanged rather than argued away.

**Crash-recovery sweep (standing law), run BEFORE any other act.** ⟨cmd⟩ `git status --porcelain`
(value.js) → 18 rows at this seat's open; intersection with **this unit's** writable set
(`formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` · `execution/C/F-W10.md`) = **∅** — the addendum
did not exist and the record was clean. ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` →
**1** row, `?? .worktrees/`. **No predecessor's partial work exists in either of this unit's paths**,
so nothing is inherited, and the sibling dirty rows (`demo/**`, `e2e/**`, `A/X-W5.md`,
`CARRY-LEDGER.md`, the staged `D demo/shell/PaneSegmentedControl.vue`, `scripts/dev/dev.sh`) were
neither read for write, staged, nor touched.

#### a.1 RE-CUT-0's four corpus probes — DOUBLE-RUN AND BANKED (§4a.1's precondition)

⟨cmd⟩ in `docs/tranches/V/megatranche/registry/adjudicated/`, each command run twice, **identical
output both runs**:

| probe | run 1 | run 2 |
|---|---|---|
| `grep -o 'F\.W9/W10' fr-*.md \| wc -l` | **54** | **54** |
| `grep -l 'F\.W9/W10' fr-*.md \| wc -l` | **25** | **25** |
| `grep -oE '(→\|⇒) *\*{0,2}F\.W10' fr-*.md \| wc -l` | **0** | **0** |
| `grep -o 'F\.W10' fr-*.md \| wc -l` (**any** bare token — the stronger form, run because the weaker one can only under-count) | **0** | **0** |
| `ls fr-*.md \| wc -l` | **66** | **66** |

**All four reproduce the spec's banked figures exactly** — 54 across 25 of 66, bare `F.W10` **0** —
and the stronger bare-token form returns 0 as well, so no routing to F.W10 exists anywhere in the
corpus under either detector.

#### a.2 Both splice diffs — DOUBLE-RUN, both EMPTY (the Δ=∅ leg of `G-F10-6 ≡ G-F9-21`)

⟨cmd⟩ in `docs/tranches/X/fourier/waves/`, read-only, twice each, **exit 0 with zero output both
runs**:

- checkpoint block (F.W9 authors · F.W10 receives, R2-6a): `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty · empty**
- re-cut table + entrant block (F.W10 authors · F.W9 receives): `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty · empty**
- extent of the shared table: ⟨cmd⟩ `… \| grep -cE '^\| fr-'` → **22** record-rows carrying **26**
  identities (four rows carry two ids each: `fr-AdminFlaggedPanel` · `fr-EquationResult` ·
  `fr-EquationView` · `fr-PaperArticleWindow:58 / :90`)

**Both directions of the splice are symmetric at these bytes, so §4a.1's precondition is BANKED and
the units after `a` may stamp.** The spec's own warning is honoured verbatim: the green splice leg
is **not** read as the gate.

#### a.3 The §4.10 carve — RECEIVED BY SPLICE, never re-authored (E-F9-2)

⟨cmd⟩ `awk '/^> \*\*§4\.10 — THE DEPLOY SPINE/,/^> is escalated as \*\*E-F9-1\*\*\./' execution/C/F-W9.md`
→ **44 lines** (extracted by PATTERN, not by line address, so a later edit to the twin's record
cannot silently re-point this receipt). De-quoted with `sed 's/^> //; s/^>$//'` → **44 lines**, and
the round trip ⟨cmd⟩ `diff <(sed 's/^$/>/; s/^\([^>]\)/> \1/' plain) quoted` → **EMPTY**. The
de-quoting of `X.F.W9.c`'s blockquote marker is therefore **the only transformation applied**, and
it is reversible. **No word of F.W9's carve was re-typed, re-graded or corrected** — including its
`K11 "quote 8, never 9" is superseded at 18` finding and its `VERIFIED NO` verb, both of which this
seat carried untouched although they bear on F.W10's own close.

#### a.4 The CREATE — `CENSUS-ADDENDUM-2026-08-25.md`, §1a row 8's enumeration and nothing else

Commit **`cd4df502`** · **1 file changed, 405 insertions**, pathspec on the commit itself. Line 1 is
`SERVED MODEL: claude-opus-5[1m]`. Settled size, measured **from outside, after the last write**:
⟨cmd⟩ `wc -l` → **405** · **405**, `wc -c` → **33881** · **33881**.

| section | what landed | owner |
|---|---|---|
| **§4.10** | F.W9's deploy-spine / coverage-void carve, **spliced** (a.3) | X.F.W9.c |
| **§4.11** | the re-cut record: **26 identities, one wave each**, in a 26-row table, each row carrying the record anchor as §2.2 spells it, the identity kept for life, the one wave, and its booked / cite-the-holder status | this unit |
| **§4.11a** | the **two** by-mechanism deviations with the mechanism that moved each, and the **third RETIRED** | this unit |
| **§4.11b** | the **five** pending paired edits, re-disclosed with their live state and holder; **none applied** | this unit |
| **§4.11c** | the routing-law taxonomy statement — boilerplate excluded **by shape, in all three spellings**; zero rows minted | this unit |
| **§6.5-ERRATA** | **FR-CP-44** metric-stack re-seat · **FR-AFP-51** tense | this unit |
| **§6.7** | the charter line, **corrected wording, one unbroken line** | this unit |
| **Scope** + **Receipts** | what is not carried and why; every figure double-run | this unit |

**The re-cut tally closes by enumeration, not by assertion**: ⟨cmd⟩ over the settled addendum,
double-run — `grep -cE '^\| [0-9]+ \| \`fr-'` → **26** · **26**; cut column → **F.W9 23** ·
**F.W0 1** (PAW-49) · **F.W4 2** (PAW-50 booked ⊕ FR-TT-1 cite-the-holder) = **26**.

**The mechanism prose was CITED, never copied.** A third carrier of the shared §2.2 bytes would be a
rival copy that no `diff` gates — precisely the divergence Δ=∅ exists to catch — so §4.11's table
carries identity · anchor · wave · status and points at the specs for mechanism, locks and dissent.

#### a.5 The two by-mechanism deviations, and the retired third — measured against the canonical

⟨cmd⟩ against `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` (the census of record; a
denominator cites the canonical, a quotation cites the corpus — §1b's distinction, kept):

| id | canonical row (quoted) | §2.2's cut | reading |
|---|---|---|---|
| `PAW-49` | `` \| `PAW-49` \| — \| `F.W3/W4` · `F.W9/W10` \| **F.W3** … `` | **F.W0** ⟨re-cut⟩ | **deviation, by mechanism** — a repo-config flag (`noUncheckedIndexedAccess`), the one coverage-family row whose gate is FREE |
| `PAW-50` | `` \| `PAW-50` \| — \| `F.W9/W10` \| **F.W9** … `` | **F.W4** ⟨re-cut⟩ | **deviation, by mechanism** — HARNESS-BEFORE-RIDER; census §4.5's DECISION is its only true home |
| `FR-TT-1` | `` \| `FR-TT-1` \| `D-1` · `L-4` \| `F.W4` · `F.W3` · `F.W9/W10` \| **F.W4** … `` | **F.W4**, ⟨CITED — the canonical's holder; NOT booked by either twin⟩ | **RETIRED at repair round 6** (PASS-6 D-9): the round-4/5 `→ F.W10` fabrication was converted to cite-the-holder in both twins in one pass. **Re-verified GREEN at the bytes this seat measured** |

**So the addendum records TWO deviations and ONE discharged fabrication** — exactly the shape §1a
row 8 names, and the count is derived from the canonical rather than inherited from the sentence.

#### a.6 The five pending paired edits — RE-DISCLOSED, and NONE APPLIED

| # | pair | live state (this seat, double-run) | applied? |
|---|---|---|---|
| 1 | **AA-44 `9`→`8`** | ⟨cmd⟩ `grep -c '9 e2e never enter the audit tab'` → **3** in `F-W10.md` · **3** in `F-W9.md`; §4a.13's binding rule reads *"AA-44: EIGHT specs, never nine"* — the error is **symmetric**, so Δ=∅ is unaffected | **NO** |
| 2 | the **26-vs-17 F.W9 roster spread** ⟨16 at E5-16⟩ | OUTSTANDING; **R4-10 assigns the re-basing to `F-W9`'s duty**, not this end | **NO** |
| 3 | `PAW-49 → F.W0` · `PAW-12 → F.W9` vs canonical **F.W3** for both | OUTSTANDING, canonical rows quoted at a.5 | **NO** |
| 4 | `PAW-50 → F.W4` vs canonical **F.W9** — **different in KIND** (the canonical **PRIMARY** is cut away from; §4a.5 makes it load-bearing for five other bookings) | OUTSTANDING | **NO** |
| 5 | `FR-TT-1 → F.W10` vs canonical **F.W4** | **DISCHARGED** at repair round 6, re-verified at the bytes | n/a |

**Why none was applied, stated at the bytes and not as caution**: the five sit inside the **SHARED
§2.2 SPLICE BYTES**, where a unilateral landing breaks `G-F10-6 ≡ G-F9-21`'s Δ=∅ — the gate this
unit turns — **and** both specs are **outside this unit's writable set** and IMMUTABLE at execution
(E-3) with status `planned`. ⟨cmd⟩ `git diff --stat docs/tranches/X/fourier/` → **0 lines**, twice.
The twin's own paired register (`E-F9-1`, obligations `P-1 · P-2 · P-4..P-9`) is **cited, booked by
nobody**.

#### a.7 The two census errata, and the charter line

**FR-CP-44 (metric-stack re-seat).** Quoted from the frozen corpus — ⟨cmd⟩
`sed -n '82p' registry/adjudicated/fr-CoefficientsPanel.md` → *"FR-CP-44 = L/A-4 — INFO.
lane-frontend:443 seats the `./metric-stack → ./metric` shadow candidacy on the two 26/17-line
hosts, neither of which renders a metric; the tree says it belongs on CoefficientsSpectrum.vue …
NO-WAVE-OWNER (census correction) + F.W3 candidate."* The lane row itself — ⟨cmd⟩
`sed -n '443p' formation/fourier/lane-frontend.md` → `` | `visualization/CoefficientsPanel.vue` /
`EqCoefficientsPanel.vue` | 26 / 17 | `./metric-stack` (4.0.0) → `./metric` (7.0.0) | | ``. The
**census correction lands in the addendum**; the **tree re-seat is left to F.W3** (§4b's F.W3 row,
*"FR-CP-44's re-seat candidate"*); the lane doc is **not rewritten** and the *"by construction"*
overclaim stands noted, not laundered.

**FR-AFP-51 (tense).** ⟨cmd⟩ `sed -n '112p' registry/adjudicated/fr-AdminFlaggedPanel.md` →
*"| FR-AFP-51 · L-27 | The census's ToastVariant break is an uplift-target break, not present-tense
— at the pin the symbol resolves; tense clarification only | record — NO-WAVE-OWNER |"*. The census
carries the present tense twice (its uplift-break-surface bullet and its **[P2] item 10**, *"is a
hard typecheck break"*); the addendum states the **UPLIFT-TARGET** reading and names why the tense
matters — **a present-tense break inflates the uplift budget with a defect that did not exist at the
measured pin**. Read-only corroboration at the **landed** pin, published as an observation and not
as a re-derivation: fourier `web/package.json` now pins `@mkbabb/glass-ui ^8.0.0` ·
`@mkbabb/keyframes.js ^6.0.0` · `@mkbabb/value.js ^4.0.0`, and ⟨cmd⟩ `grep -rn 'ToastVariant'
web/src/` → **1 file, 2 hits, both PROSE** in `composables/useToast.ts` doc comments. **The symbol
is used nowhere as a type**, so the census sentence is stale twice over. **Nothing re-graded.**

**§6.7, the charter limb of `G-F10-8`.** The line lands in its **CORRECTED** wording, written on ONE
unbroken line so the instrument that checks it can read the whole sentence — ⟨cmd⟩ over the settled
addendum, double-run: `grep -c 'Zero UNVERIFIED credit — every Codex verdict enters the formation
through per-claim adjudication (M-21)'` → **1** · **1**. The **superseded** wording survives only
inside supersession bytes: case-sensitive **2** · **2**, case-insensitive **4** · **4**, all four in
§6.7's supersession paragraph. The owner ruling this limb consumes is cited, never re-opened:
**OG-F2 / OG-V2 → CODEX-ERA-SPECIFIC** (COHESION §0j.D), whose own bytes read *"the `FOURIER-R*`
value-side files are records"* and **"G-F10-8 closes on the charter line."**

#### a.8 Gate readings — BEFORE → AFTER

| gate | BEFORE (this record's baseline, 2026-09-19) | AFTER (this seat, 2026-09-20, double-run) |
|---|---|---|
| **G-F10-6** ≡ **G-F9-21** | **RED on the corpus leg**; GREEN on both splice legs *before cure* | **SPLIT, and read honestly.** GREEN: both splice `diff`s **empty** twice (a.2); the **26** identities enumerated with **one wave each** and the tally closing 23/1/2 (a.4); `git diff --stat` on `registry/` → **0** and on `fourier/` → **0**, so the gate's *"falsified by ANY edit to `fr-*.md`"* clause is satisfied by measurement. RED-unchanged: the corpus figures **54 / 25-of-66 / bare `F.W10` 0** reproduce exactly. **The spec binds this seat twice over — *"the gate STAYS RED on its corpus leg"* and *"a later seat must not read the green leg as the gate"* — so this unit does NOT stamp it green; the terminal reading is `X.F.W10.g`'s at the close, where the acceptance clause (26 cut · same table both specs · Δ=∅ both directions) can be weighed against a corpus leg the same gate's falsifier FORBIDS curing** |
| **G-F10-8** (charter limb only) | **RED** — ⟨cmd⟩ `grep -rl 'Zero UNVERIFIED credit' fourier/waves/*.md` → **F-W10.md only** (the spec quoting its own obligation); *"the census addendum that must carry it does not exist"* | **GREEN on the charter limb** — the addendum exists at `cd4df502` and carries the line in its corrected wording, whole and unbroken, verified twice. **The limb and nothing more**: G-F10-8's six terminal verbs (R3-9 · the R3-16+R4-11+R5-9+R6-10 board row · R4-12 · R5-3 · the registered `~/.codex/worktrees/d0be` worktree) are **`X.F.W10.b`'s**, and this unit books none of them |

#### a.9 Residuals, disclosures and escalations

1. **RESIDUAL — two census-input errata are routed to this file by rows this unit does not own.**
   `F-W10.md` §2.5a's `i-1 = L-INFO-2` (fr-BasisSelector:92) and `PP-CENSUS` (fr-PathPreview:31,
   the L-1 census-integrity half) each name the CENSUS-ADDENDUM as their landing surface *"beside
   FR-CP-44 · FR-AFP-51"* — but both are **§2.5a rows, i.e. `X.F.W10.c`'s sections**, while §1a row
   8 enumerates this file's F.W10-owned content as the §4.11 re-cut record, the deviations,
   FR-CP-44, FR-AFP-51 and §6.7. **This seat wrote exactly that enumeration and did not reach into a
   sibling unit's sections.** The obligation is therefore **live and flagged, not discharged**, and
   it carries a **bounds problem for the close to rule**: `X.F.W10.c`'s writable set is
   `COHESION.md` + this record — **it cannot write the addendum**. Either `c` is granted the
   addendum path for those two rows, or the close seat lands them, or they are explicitly re-homed.
   Named here so no reader mistakes the silence for an oversight.
2. **INFO — one banked figure does not reproduce under this seat's own probes, published against
   interest.** §2.1's anatomy of the 54 is `26 ⊕ 14 boilerplate ⊕ 2 restatements ⊕ 12 verdict
   prose`. The **26 reproduce by enumeration**. The **14 does not**: ⟨cmd⟩ in
   `registry/adjudicated/`, double-run — `grep -h 'F\.W9/W10' fr-*.md \| wc -l` → **54** (one
   occurrence per line, so lines and occurrences coincide); `… \| grep -cE '(F\.W[0-9]+[^0-9].*){3,}'`
   → **24**; `… \| grep -cE 'Routing law\|Routes:\|Dispositions:'` → **8**. **Neither is 14, and
   neither is the spec's predicate** — §2.1's boilerplate class was hand-classified at authoring as
   *the routing-law header line*, where the ≥3-token shape also sweeps in verdict prose that
   enumerates routes. The anatomy is therefore **carried as §2.1's BANKED classification and is not
   re-derived**, and **what §4.11 books is the 26-identity cut, verified by enumeration** — the
   residue's partition is not its operand. Flagged for `X.F.W10.g` and for whatever wave re-cuts the
   taxonomy detector.
3. **DISCLOSURE — three self-counting receipts caught themselves mid-write.** A count of the file
   being written is a measurement the write invalidates. Each is published as a BEFORE/AFTER pairing
   inside the addendum rather than as a post-image: (i) the re-cut row probe first published
   `grep -cE '^\| [0-9]+ \| '` → 26, which returns **31** at the settled bytes (the 26 record rows
   **plus §4.11b's five numbered pending-pair rows**) — cured by anchoring the probe to `` `fr- ``,
   which returns **26** twice; (ii) a supersession-wording receipt asserted *"this receipt adds none
   of them"* while quoting the phrase twice, and (iii) its own first correction preserved the defect
   by quoting it again to explain it — cured by an instrument the bytes cannot reach: the wording is
   **named without being spelled**, and the **file publishes no count of its own length** (the three
   attempts read 374 → 387 → 397, each stated inside the write that invalidated it; the settled
   **405** is measured from outside, in this record).
4. **NO ESCALATION.** Every act landed inside the writable set; the specified cure was possible at
   the bytes and was performed as specified.

#### a.10 Bounds audit for this unit

⟨cmd⟩ `git show --stat cd4df502` → **1 file changed, 405 insertions(+)**, the file being
`docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` — **inside the
writable set**, and no sibling path rode the commit although the shared index carried a staged
deletion (`D demo/shell/PaneSegmentedControl.vue`) and 17 other dirty rows at commit time: the
pathspec on the commit itself is what kept them out. E-3 verified at the bytes, double-run:
`git diff --stat` → **0 lines** on the dated `CENSUS-2026-08-03.md`, **0** on
`docs/tranches/V/megatranche/registry/`, **0** on `docs/tranches/X/fourier/`.
`scripts/dev/dev.sh` untouched. **No fourier-tree byte written** (that tree was read only).

### X.F.W10.b

**The substrate verification, the Codex residue and the intake addendum** · opus (`claude-opus-5[1m]`) ·
2026-09-20 · **status DONE**, with **G-F10-8 GREEN** · **G-F10-11 GREEN (verified-green-by-predecessor on
every limb)** · and **G-F10-9 read honest-SPLIT** — its three legs this unit owns are GREEN at the bytes
and its fourth, the latex register's EXISTENCE, is **G-F10-5's and unit `d`'s**, named rather than stamped.

**Crash-recovery sweep (standing law), run BEFORE any other act.** ⟨cmd⟩ `git status --porcelain`
(value.js) → **25** rows at this seat's open ⟨*CORRECTED BEFORE THIS UNIT CLOSED, and the correction is
published rather than the figure silently fixed: this cell first read* ~~24~~ *, which did not reproduce
against the seat's own enumeration of the 25 printed rows. The reading is also **time-stamped by
construction** — four tracks share this index, and the same probe returns **17** at this unit's close
(twice), siblings having landed in between; a shared-index row count is a clock reading, never a
constant*⟩; intersection with **this unit's** writable set
(`audit/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` · `execution/C/F-W10.md`) = **∅** —
the addendum did not exist and the record carried only `a`'s settled receipts. ⟨cmd⟩
`git -C ../fourier-analysis status --porcelain` → **1** row, `?? .worktrees/`. **No predecessor's partial
work exists in either of this unit's paths**; nothing is inherited. The sibling dirty rows (`demo/**`,
`e2e/**`, `src/css/**`, `A/X-W5.md`, `CARRY-LEDGER.md`, the staged `D demo/shell/PaneSegmentedControl.vue`,
`package.json`/`package-lock.json`, `scripts/dev/dev.sh`) were neither read for write, staged, nor touched.

#### b.1 §2.3 · the F.W0 substrate, VERIFIED at the bytes (G-F10-11) — F.W0 rules, F.W10 verifies

All probes read-only in `/Users/mkbabb/Programming/fourier-analysis` at HEAD **`cef242d`**, **double-run,
identical both runs**. **R.2 binds and is honoured: a GREEN before its cure is a finding, not a credit** —
each limb below names the predecessor whose landing turned it.

| limb | born state (spec, 2026-08-28) | THIS SEAT, 2026-09-20 | verdict |
|---|---|---|---|
| **FR-NP-32 (≡ fr-PaperSidebar M1)** — cite-both, never substitute | glass-ui **4.0.0** dist: `postcss.parse` → **35** nodes, **ZERO** valid `@source`, garbage `` source` `` at `:203–222`; `vite dev`/`vite build` RED at `^4.0.0` | at the **8.0.0** pin: ⟨cmd⟩ `postcss.parse(web/node_modules/@mkbabb/glass-ui/dist/styles/index.css)` → **PARSE OK · 40 top-level nodes** (twice) · valid `@source` at-rules → **1** (`params "../*.js"`) · `grep -c '@source'` → **1** · `grep -c 'source\`'` → **0**; installed **8.0.0** against `web/package.json` `"^8.0.0"` | **GREEN — VERIFIED-GREEN-BY-PREDECESSOR: F.W1's atomic tri-package transaction** under §0i.3's `v8.0.0` @ `17a11bc5` election. The relay leg was already discharged and is **READ**: O-20 row **A-1**, `grep -c 'FR-NP-32'` → **1** · `grep -c '@source'` → **1** |
| **GAB-13** — the audited artifact was the DIRTY WORKTREE | `git status --porcelain \| wc -l` → **exactly 28** (incl. `web/package.json`) | ⟨cmd⟩ same probe → **1** (twice); the one row is `?? .worktrees/`, this sub-tranche's own wave worktrees, **not a source edit** | **GREEN — VERIFIED-GREEN-BY-PREDECESSOR: F.W0's LAND.** Under OG-F1 (**WORKTREE-AS-BASELINE**, §0j.D) GAB-13 owes only a **disclosure line**, and it is written into the addendum at §D row 2 — *the audit measured a dirty worktree and said so; the diff has since landed; HEAD `cef242d` holds the audited bytes.* DU's preserved INFO dissent is recorded as having been right, never re-graded |
| **F8-REACH-01 / -02** — **HOLD IS RED by the source's own law** | both un-dispositioned | ⟨cmd⟩ `find web/src -name 'InfoCard.vue' -o -name 'CanvasOverlayButton.vue' \| wc -l` → **0** (twice). ⟨cmd⟩ `git show --name-status 5842377` → `D …/equation/InfoCard.vue` · `D …/visualization/CanvasOverlayButton.vue` **in ONE commit** with the `aria-pressed` lift (`M CanvasControlsDock.vue` · `M EditorControlsDock.vue` · `M ConvergenceTimeline.vue` · `M docs/tranches/F/SUBSTRATE-LEDGER.md`) | **GREEN — the §0j.D `G-10` verb `DELETE` EXECUTED "in one breath"; NO HOLD SURVIVES.** ⟨cmd⟩ `grep -rn "from .*CanvasOverlayButton\|from .*InfoCard" web/src` → **0**. One residual token disclosed, not swept: `web/src/style.css:349` names `fr-CanvasOverlayButton` in a comment authored by **X.F.W3 `.e`** — a record-id citation, not an import or mount |
| **FR-COB-20** (§2.5's row, this unit's) | HOLD un-dispositioned | its entire claim is the verification above | **VERIFIED — no HOLD survives to close.** Nothing re-booked, re-graded or renamed; no work manufactured |

#### b.2 §2.3's Codex-R3–R6 row · the five terminal verbs (G-F10-8)

Landed as `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md`
§C, each verb carrying a **falsifier** so it is a disposition and not a deferral, and **none booking work**:
**R3-9** RECORD-ONLY, never an operand · the **R3-16 + R4-11 + R5-9 + R6-10** board row **SPLIT BY LIMB**
(the 14-wave board TRUE → **CITE-THE-HOLDER**, fourier's `docs/tranches/M/M.md`, COMMISSION §2; the
72/158/P29/slot superstructure **EXCLUDED-WITH-REASON**, non-transferable) · **R4-12 MOOT —
KILL-WITH-RATIONALE** · **R5-3 DROP** (the lane's second disjunct) · the worktree
`~/.codex/worktrees/d0be/fourier-analysis` **FROZEN-AND-RETAINED AS A RECORD**, out of bounds for every F
wave (§1c), the prune an **owner act** that blocks nothing.

**The measurements behind them, double-run at this seat:**

| probe | reading (both runs) | what it settles |
|---|---|---|
| `ls -la /tmp/fourier-r4-files.sha256` | *"No such file or directory"* | **R4-12 MOOT**; the banked *"74,507 B still on disk"* is named **stale**, not quietly dropped |
| `grep -rl 'e70543907d' docs/tranches/X/ \| wc -l` | **0** | **NONE of R4-12's rows consumed** — the gate's own fourth leg, measured, not asserted |
| `grep -rl 'fourier-r4-files' docs/tranches/X/ \| wc -l` | **13** | thirteen X-side files **name** the residue to disposition it; naming ≠ consuming, and the sharper probe above is what would catch a consumption |
| `grep -rl '7c1b1d59' docs/tranches/X/ \| wc -l` | **0** | **the R5-3 DROP costs the formation nothing** — no X-tranche byte rests on the unverified pin |
| `git -C ../fourier-analysis worktree list` | `/Users/mkbabb/.codex/worktrees/d0be/fourier-analysis  cd26c65 (detached HEAD)` | the worktree is **still registered**; the intake's 2026-08-03 observation reproduces, and takes its verb |
| `grep -cE '^\| *\*{0,2}M\.W[0-9]+' ../fourier-analysis/docs/tranches/M/M.md` · `grep -oE 'M\.W[0-9]+' … \| sort -u \| wc -l` | **14** · **14** | the lane's X-7 row (*"'14 waves' is TRUE and maps to a real board"*) **reproduces at the live tree** |
| `ls docs/tranches/V/megatranche/coordination/FOURIER-R* \| wc -l` · `find ../fourier-analysis/docs -name 'FOURIER-R*' \| wc -l` | **9** · **0** | the born-RED witness reproduces exactly — and is **discharged by ruling**, not by moving files: §0j.D makes the value-side `FOURIER-R*` files **records** |

**The two owner legs are DISCHARGED BY RULING and are cited, never re-ruled**: **OG-F1** →
FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE · **OG-F2 / OG-V2** → CODEX-ERA-SPECIFIC, both COHESION
**§0j.D**. OG-V2's dissent is preserved, not resolved away.

**The charter limb, verified from outside (it is unit `a`'s landing, not this unit's).** ⟨cmd⟩ in
`formation/fourier/`, double-run: `grep -c 'Zero UNVERIFIED credit' CENSUS-ADDENDUM-2026-08-25.md` → **2**
(the charter line quoted whole at `:280`, and its one-line grep-able cross-check at `:379`);
`grep -c 'carry zero credit'` → **2**, and **both are confined to supersession bytes** — `:277`
*"§6 item 7's … is SUPERSEDED"* and `:301` *"Never 'carry zero credit'; never a paraphrase"*. The
superseded wording therefore appears **nowhere as the operative statement**, which is the spec's own idiom
at §2.3. **G-F10-8's charter limb is GREEN and is credited to unit `a`.**

#### b.3 §2.4's NEXT-COMMUNIQUE row · the addendum-beside (G-F10-9)

**Created**: `INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md`, commit **`077d3e5f`** — **1 file changed, 226
insertions(+)**, nothing else swept although the shared index carried a staged deletion
(`D demo/shell/PaneSegmentedControl.vue`) and every other dirty row of a **four-track shared index**
(**25** at this unit's open, **17** at its close — the same correction as the sweep cell above): the
**pathspec on the commit itself** kept them out. The filename's `2026-08-25` is the spec's **fixed path** (§1a row 7, so no seat invents a
second channel); the acts are **2026-09-20** and the file says so in its own head rather than smoothing
the two dates into one.

**The amendment**: the intake's `:221` **EMPTY** stands **TRUE of the 364-claim Codex corpus it
adjudicated** and **FALSE of the 66-record registry authored after it** — both dated, neither withdrawn,
which is precisely the spec's *"Dissent: none — both statements stand dated"*.

**Exactly ONE register per surviving ask, and the legs are measured:**

| leg | receipt (this seat, double-run) | state |
|---|---|---|
| the dated bytes are not rewritten (**E-3**) | ⟨cmd⟩ `git diff --stat -- …/INTAKE-ADJUDICATION-2026-08-03.md \| wc -l` → **0**, twice; the file is untouched in working tree and index alike | **GREEN** |
| glass → **SS-6**, one row each | **seventeen** arms enumerated in the addendum's §B.2, transcribed from §2.4's own bytes; measured **from outside the write** at the settled file: ⟨cmd⟩ `grep -c '\| \*\*SS-6\*\* \|'` → **17**, twice (self-count law: the file publishes the enumeration, this record publishes the count) | **GREEN** |
| latex → the **§2.6** register, cited by holder | the §2.6 region's anchors re-resolved at true bytes first (`### §2.6` at **:367**, `### §2.7` at **:389**) and the roster **not re-enumerated** — a second enumeration is the rival operand R2-6b/R4-3 convict | **GREEN as an assignment** |
| the two registers are **disjoint** | all seventeen arms probed against the extracted §2.6 region → **0 total hits**, twice. **One homonym disclosed against the receipt's own interest**: bare `L-10` hits **1** — *"PaperSidebar L-10 ≡ ★MF-10"* inside §2.6's C-20 cell, a different id sharing a short spelling (R-5) — while the disambiguated `L-10/D-M4` returns **0** | **GREEN** |
| **no glass row cured consumer-side** (the gate's explicit falsifier) | the addendum prescribes **no** consumer-side repair for any of the seventeen; each routes, and `glass-ui/**` was read-only throughout — **no producer tree, no fourier tree, no registry record was written by this unit** | **GREEN** |
| the latex register **exists** | ⟨cmd⟩ `ls docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` → **absent** at this clock | **RED — and it is NOT this unit's leg**: it is **G-F10-5**, unit `d`'s CREATE. Named, not stamped |

#### b.4 Gate readings, BEFORE → AFTER

| gate | BEFORE (this record's Baseline, 2026-09-19) | AFTER (this unit, 2026-09-20) |
|---|---|---|
| **G-F10-8** | **RED** — charter line in no F spec; the six terminal verbs absent; `/tmp` residue gone; worktree still registered; `FOURIER-R*` 9 / 0 | **GREEN.** Charter line landed (unit `a`, verified from outside); **five terminal verbs landed with falsifiers**; **NONE of R4-12's rows consumed** (0 under `docs/tranches/X/`, twice); OG-F1 + OG-F2/OG-V2 **discharged by ruling** at §0j.D and cited by id, re-ruled by nobody |
| **G-F10-9** | **RED** — the addendum absent; `:221` declares the channel EMPTY | **honest-SPLIT.** Dated bytes untouched (E-3, empty ×2) · seventeen glass arms in ONE register · latex assigned to §2.6's register · disjointness measured ×2 · no glass row cured consumer-side — **all GREEN**; the **register-existence** leg is G-F10-5's and stands **RED until unit `d` lands**. Read SPLIT rather than stamped, per this wave's own posture |
| **G-F10-11** | **GREEN BEFORE CURE on all three limbs** (Baseline's reading) | **GREEN — and stamped as VERIFIED-GREEN-BY-PREDECESSOR with the predecessor named on every limb**: the dist parses at the 8.0.0 pin (**F.W1**), the WT diff landed (**F.W0**) with GAB-13's disclosure line now written, both F8-REACH files deleted in one commit `5842377` (**F.W0**, §0j.D `G-10`) with **no HOLD surviving** and no import left. **F.W10's claim is the VERIFICATION, exactly as §2.3 defines it — not the cure** |

#### b.5 Residuals, disclosed rather than silent

1. **G-F10-9's register-existence leg stands RED** and is **unit `d`'s** (G-F10-5). This unit settled the
   *assignment*; calling the gate whole would be the deferral-wearing-a-verb the wave exists to abolish.
2. **The SS-6 accretion of the seventeen arms into `COHESION.md` §4a is unit `c`'s act** (G-F10-9's
   register limb). `COHESION.md` is **outside this unit's writable set** and was not opened for write.
   **O-20 already carries the head arm and is never re-sent** — the F-side boundary reconciles against
   that dispatched packet.
3. **The Codex worktree stays registered.** Its verb is terminal (**RETAIN-AS-RECORD**) with a falsifier,
   and the prune is an **owner act** available at any moment, blocking nothing — `~/.codex/**` is §1c
   out-of-bounds for every F wave, so a wave verb that moved it would itself be the out-of-bounds write.
4. **Two self-counting figures caught themselves mid-write.** Both `grep -rl … docs/` readings for the
   R4-12 sha and the R5-3 pin moved by **exactly one** while the addendum was being written, because the
   addendum quotes both tokens. They are published **settled** (**4** and **6**) with the writing file
   named inside each, never in the flattering pre-write form (**3** and **5**); the two load-bearing legs
   (`docs/tranches/X/` → **0** and **0**) are untouched by the write, the file not living under `X/`.
5. **One homonym disclosed by the disjointness probe** (bare `L-10` ≠ `L-10/D-M4`) — found by the
   instrument, published rather than smoothed away.
6. **The intake's superseded charter wording is spelled twice in unit `a`'s census addendum**, both times
   inside supersession bytes. Verified and recorded here because the gate's falsifier is about the
   *operative* statement, and the operative statement is the corrected one.
7. **E13**: this unit minted no mail and did not open `INBOX.md` (outside its writable set); the open's
   sweep — 0 unrowed, 0 live-UNREAD rows in F.W10's scope — stands unchanged at this unit's clock.
8. **NO ESCALATION.** Every act landed inside the writable set; the specified cure was possible at the
   bytes and was performed as specified; nothing was improvised, masked or worked around.

#### b.6 Bounds audit for this unit

⟨cmd⟩ `git show --stat 077d3e5f` → **1 file changed, 226 insertions(+)**, the file being
`docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` — **inside
the writable set**; no sibling path rode it although the shared index held a staged deletion and 23 other
dirty rows at commit time. This record lands by its own pathspec in a **separate commit, one meaning
each**. E-3 verified at the bytes, double-run: `git diff --stat` → **0** on
`INTAKE-ADJUDICATION-2026-08-03.md`, **0** on `docs/tranches/V/megatranche/registry/`, **0** on
`docs/tranches/X/fourier/`. `scripts/dev/dev.sh` untouched. **No fourier-tree byte and no glass-ui byte
written** — both trees were read only. **`LEDGER.md` not touched by this unit** (the wave's row is `g`'s).

### X.F.W10.c

**The NO-WAVE-OWNER drain into COHESION §4 and the SS-6 accretion at §4a** · opus (`claude-opus-5[1m]`) ·
acts run **2026-09-20**, sitting of record **2026-09-17** (the owner's begin-word, COHESION §0j).
Writable set as dispatched: `docs/tranches/X/COHESION.md` (**MODIFY-CARVE ONLY** — §4 block · §4a
accretion) ⊕ this record. **Nothing else was opened for write.**

#### c.0 Crash-recovery sweep — no inherited work in this unit's writable set

⟨cmd⟩ `git status --porcelain` (value.js, this seat, at open) → **17** rows; intersection with this
unit's writable set (`COHESION.md` · this record) = **∅**. ⟨cmd⟩ `git -C ../fourier-analysis status
--porcelain` → **1** row, `?? .worktrees/` — the same pre-existing X·F worktree row every seat of this
wave has read. The staged deletion `D demo/shell/PaneSegmentedControl.vue` and the sixteen other dirty
rows belong to sibling seats on Tracks A and D and were **not read for write, not staged and not
touched**; `scripts/dev/dev.sh` is the standing unowned dirty row (NEVER touched). **No predecessor seat
left partial work on this unit.** Re-read at close: porcelain still carries `D demo/shell/…` **staged**,
byte-for-byte as at open — a pathspec commit moved nobody else's index entry.

#### c.1 THE DERIVATION FIRST — the five partition probes re-run fresh from the 66, double-run

The brief's order is the point: *a roster asserted rather than derived is what three repair rounds
convicted*, so nothing was written until the partition closed at this seat's own clock. ⟨cmd⟩ one base
(`docs/tranches/V/megatranche/registry/adjudicated`), `bash` + pinned BSD `/usr/bin/grep`, **no `-P`, no
`\K`, no lookaround, no bounded wildcard**; the three probe variables declared once, every row re-using
them; **every figure run twice, identical both runs**:

| class | lines | records | run 1 | run 2 |
|---|---|---|---|---|
| routing-law / taxonomy boilerplate (`$BOIL`, by SHAPE) | **31** | — | 31 | 31 |
| row-level **STRICT** | **68** | **35** | 68 / 35 | 68 / 35 |
| row-level **WEAK** | **32** | **19** | 32 / 19 | 32 / 19 |
| **prose STRICT** (walked to its row id) | **1** | 1 | 1 | 1 |
| prose WEAK (dated verdict bytes) | **33** | — | 33 | 33 |
| **TOTAL** `grep -h 'NO-WAVE-OWNER' fr-*.md \| wc -l` | **165** | **56** of **66** | 165 / 56 / 66 | 165 / 56 / 66 |

**`31 + 68 + 32 + 1 + 33 = 165` — the partition exhausts its operand**, reproducing the spec's transcript
to the line. Corroborating readings, same clock, double-run: occurrences **167** · records carrying any
token **56** · `ls fr-*.md` **66** · **the en-dash arm `grep -o 'NO–WAVE' fr-*.md | wc -l` → 0** (∅ today;
it stays in the probe so it can never silently stop being ∅). **Strict roster = 69 dispositions across 36
records** (68 row-level ⊕ the prose-strict `L-19+C-N1`).

**The two per-class record counts were taken by TWO INDEPENDENT INSTRUMENTS and made to agree.** The
spec's own `cut -d: -f1` pipeline was revoked once already at R4-4.4 for reading a line number as a
record, so this seat did not re-run the dead form: (i) a filename-retaining `grep -H` pipeline with the
row/strict predicates applied to the rebuilt line, and (ii) a **per-file loop** that never splits on a
colon at all. Both return **35 / 19**, twice. *Two instruments agreeing is the receipt; one instrument
run twice is only a stable reading.*

#### c.2 The §4 landing — one fourier block, 89 rows, and the pointer row that finds it

**Committed `269852ab`** — ⟨cmd⟩ `git show --stat 269852ab` → **1 file changed, 287 insertions(+)**, the
file being `docs/tranches/X/COHESION.md`. **No sibling path rode the commit** although the shared
four-track index carried a staged deletion and sixteen other dirty rows at commit time: the **pathspec on
the commit itself** is what kept them out (⟨cmd⟩ `git show --name-only 269852ab` → exactly one path).

**Shape of the landing.** One pointer row appended to §4's own table (after `fr-ContourSettings i-1`, the
idiom §4.1's `DRAINED 2026-09-18` row established) ⊕ the block itself as **`### §4.2`**, seated between
§4.1's last byte and `## §4a`. **Purely additive, measured before the copy and again after**: ⟨cmd⟩
`diff <live> <spliced> | grep -cE '^<'` → **0** removed/changed original lines · `grep -cE '^>'` → **277**
added. The alphabet disclosure at c.4 added ten more, again with **0** removed.

**The block's own sections**: **§4.2.A** the drain proper, **23** rows (F-W10 §2.5) · **§4.2.B** the strict
remainder, **36** (§2.5a) · **§4.2.C** the sixteen corpus-derived escapes, **16** (§2.5a-ii) · **§4.2.D**
the fresh derivation's own landings, **14** (§2.5a-iii(c)) · **§4.2.E** the weak class **enumerated whole**
with its ONE class verb · **§4.2.F** the escape test and the gate reading.

**SELF-COUNT LAW — the count is measured from OUTSIDE the file that carries the enumeration, at the
settled bytes, twice.** ⟨cmd⟩ this record's seat, over `COHESION.md` after the copy landed:
`awk '/^### §4.2 —/,/^## §4a /' … | grep -cE '^\| [0-9]+ \| '` → **89**, and
`… | grep -oE '^\| [0-9]+ \|' | sort -u | wc -l` → **89**, with min/max **1 / 89** — so the roster is 89
rows at 89 *distinct* numbers with no gap and no repeat. **23 + 36 + 16 + 14 = 89** re-derives true.
*The block publishes the enumeration; this record publishes the count.*

**Every row bears a TERMINAL verb — measured, not asserted.** ⟨cmd⟩ over the settled §4.2 region,
double-run: rows matching the verb alternation → **89**; rows matching none → **0** (the complement was
printed, and it is empty). **FR-NP-32 heads the table** at §4.2.A row 1 with its disposition verbatim and
its O-20 `A-1` receipt READ; **`fr-App C-12` lands as the NEGATIVE record verbatim** at row 9 —
*"R6-8 and R3-7c have NO leaf in this component — a per-component sweep must not manufacture one."*

**The two neighbouring fourier populations are CITED AND NEVER COUNTED IN**, in the block's own words:
§4's **F.W3 S-8 EMISSION 2026-09-19** (seven census-methodology rows F.W3 emits and does not own) and
**§4.1**'s fourteen (the X·KF/X·V set-difference). The drain's operand is the **66 `fr-*.md`** and nothing
else; neither block enters this roster and this roster enters neither.

#### c.3 The escape test against the CENSUS OF RECORD — 65 booked or cited, 0 escaped

**The operand is named once and pinned.** ⟨cmd⟩ `shasum -a 256 docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md | cut -c1-12`
→ **`f44362757458`**, twice — the digest F-W10 §2.5b carries, unmoved, so a later rewrite of the census is
detectable rather than deceptive. ⟨cmd⟩ `grep -c '^### F\.W10' "$C"` → **0**, twice: **F.W10's record-side
roster is ZERO by the canonical's own enumeration**, not by this block's assertion.

**The four NWO rosters were read out of §2 at those pinned bytes** and tallied by enumeration, not by
quoting a headline: `NWO (packet)` **32** · `NWO→SS-3` **27** · `NWO→SS-13` **3** · `NWO→SS-5` **3** =
**65**. Each `(record, id)` pair was resolved **record-qualified** against §4.2.A–E — an id alone is not
an identity, which is precisely how `R2-missed-1` and `R1-MISSED-5` escaped four earlier passes under bare
roster numbers. **Result: 63 resolve to a per-id row; 2 resolve through §4.2.E's NAMED six** —
`fr-CanvasOverlayButton FR-COB-14` (`NWO→SS-3`) and `fr-GalleryInfiniteGrid MISS-5` (`NWO→SS-5`, carried
as `FR-GIG-20`'s restatement, one home). **65 booked or cited · 0 escaped**, and the two that land through
the class are **named rather than left inside a headline** — which is the entire reason §4.2.E enumerates
the weak class instead of counting it.

**The fabrication test, the other direction**, is recorded and **not re-performed as this unit's act**:
F.W10's canonical roster being empty, every id-bearing cell of §2.2's re-cut table must be a citation, and
the two that were not (`FR-TT-1` → canonical F.W4, discharged to cite-the-holder at repair round 6;
`PAW-50` → canonical F.W9) are the spec's **PENDING PAIRED EDITS inside the SHARED §2.2 SPLICE BYTES**,
re-disclosed and **NOT applied** by `X.F.W10.a`. **None is a row of this block.**

#### c.4 The alphabet, measured — and widened honestly where the corpus's own wording is wider

The block declares a five-family closed alphabet. Measured over the settled bytes, **seven of the 89 rows
carry the corpus's OWN banked wording instead of a five-family spelling**, and rather than launder them
into the alphabet the block **names them and publishes the mapping**: `BOOKED HERE` (rows 5 · 14) ·
`ADOPTED AS LAW` (row 15) · `NAMED FOLD` (row 64) · `KILL-AS-DESTINATION` (row 22) · `SPLIT EXACTLY AS
BANKED` (rows 67 · 86, **two banked halves, two verbs, never one averaged verb**). ⟨cmd⟩ per-wording
counts over the settled region, double-run: **2 · 1 · 1 · 1 · 2**. *A row's banked words are kept; what is
measured is that every row is terminal.* **This paragraph exists because the first draft of the block
claimed a closed alphabet the bytes did not carry** — caught by the seat's own probe before the commit,
and cured by disclosure rather than by re-wording seven banked cells.

#### c.5 The §4a accretion — fourteen of seventeen reconciled and cited, three accreted, none re-sent

**Committed `5f557674`** — ⟨cmd⟩ `git show --stat 5f557674` → **1 file changed, 4 insertions(+)**, the
file being `COHESION.md`; ⟨cmd⟩ `git show --name-only` → exactly one path. Purely additive again
(`grep -cE '^<'` → **0**).

**The sweep, arm by arm, against every outbound glass packet — a receipt READ, never asserted.** ⟨cmd⟩
`ls ../glass-ui/docs/tranches/B*/coordination/valuejs-outbound-*.md docs/tranches/X/coordination/*glass*.md`
→ **15 packets** (the twelve `valuejs-outbound-*` under BJ/BK ⊕ NWO-1 ⊕ the token-color seat letter O-30 ⊕
O-32). Each of `X.F.W10.b`'s seventeen §B.2 arms was probed across all fifteen, double-run:

| disposition at the bytes | arms | where |
|---|---|---|
| **DISPATCHED / DISPOSED — cited, never re-sent** | **14** | `FR-NP-32` **O-20 A-1** ⊕ NWO-1 §1 · `FR-COB-7` **S-1** · `FR-COB-8` **S-2** · `FR-COB-23` **S-4** · `FR-COB-26` **S-5** · `FR-MSP-12` **S-6** · `FR-EQR` light `--success` **S-11** ⊕ **O-32 §2.1** · `HLG-32` and `FR-NP-11` fold onto `FR-COB-8`/`FR-COB-12` (**O-20 A-3**) · `L-10/D-M4` **O-20 B-7** ⊕ **O-32 §2.4** · `GAB-2` **NWO-1 L-4** · `GAB-9`'s 3.26:1 rung **rides L-4** · `AA-3` **WITHDRAWN with cause, O-32 §1.2** · `fr-AppHeader` cm-serif limb **O-32 §4.1** · `FR-GIG-5` **NWO-1 L-1** |
| **NEVER LEFT under its own identifying term → ACCRETES** | **3** | **F10-1** `fr-VisualizationView D-26` · **F10-2** `fr-PaperView D/M-7` · **F10-3** `fr-AppHeader`'s light `--tier-featured` rung |

⟨cmd⟩ `grep -l` per term across all fifteen packets, double-run: `D-26` → **0** · `D/M-7` → **0** ·
`tier-featured` → **0**; and the two mechanism phrases `underline indicator` / `anchor-positioning` → **0**
as well, so the zero is not an artefact of one spelling. **F10-3 is disjoint from its two nearest
neighbours and the disjointness is stated rather than assumed**: NWO-1 **L-4** asks the tier-**tint home**,
§4a **F3-7** adds the tier-**chip-plate** token, and neither names the light `--tier-featured` **contrast
rung**.

**The register limb's four legs, each measured twice**: (i) **exactly one register per surviving ask** —
all seventeen sit in **SS-6**, fourteen by prior dispatch and three by this accretion; (ii)
**disjointness** — the three accreted arms probed against F-W10 §2.6's extracted region (`:367–388`,
anchors re-resolved at true bytes first) → **0 hits**; (iii) **no glass row cured consumer-side** — none of
the three carries a consumer prescription, and ⟨cmd⟩ `git -C ../glass-ui status --porcelain | wc -l` →
**0** at open and at close, glass-ui **READ-ONLY** throughout; (iv) **nothing re-sent** — the accretion is
a *register* act, it mints no letter and no INBOX row, and the three arms ride the **next** SS-6 boundary
letter by the register's own standing rule.

**S-16 RESTATED AT THE REGISTER IT CONCERNS, and the instruction left WITHDRAWN.** The
`DISPATCHED 2026-08-28` row's **26** is **O-20's own count, quoted from the packet's own close line** —
**22 id-rows ⊕ 4 §D entries = 26**, two denominators of one letter, both true. ⟨cmd⟩
`grep -c '26 entries' COHESION.md` → **1**, twice, **unchanged from open**: §4a's figure **STANDS and was
edited by nobody**, no §1a #3 carve was spent on it, and this unit executed the *withdrawal* rather than
the superseded *"correct COHESION §4a"* instruction. **Executed as originally written, that instruction
would have put a false correction into the constellation's only shared register.**

#### c.6 Gate readings, BEFORE → AFTER

| gate | BEFORE (this record's Baseline, 2026-09-19) | AFTER (this unit, 2026-09-20, double-run) |
|---|---|---|
| **G-F10-7** NWO drain, F side | **RED** — the spec's born state reads *"COHESION §4 is live-seeded from SS-7 batch 1, every row an X·V slug, **ZERO fourier rows**"*; re-read at the baseline's clock §4 carried **one** fourier block, **F.W3's S-8 EMISSION**, *"a different block, cited never counted in"*, and the **F.W10 drain block — 89 rows / 89 ids / 89 terminal verbs — was ABSENT** | **GREEN.** §4 carries the fourier block at **§4.2** with a pointer row in §4's own table. **89 rows · 89 distinct numbers (1–89, no gap, no repeat) · 89 terminal verbs · 0 rows without one** — measured from outside the file, twice. **FR-NP-32 heads the table**; **`fr-App C-12` lands as the NEGATIVE record verbatim**; the weak class carries **ONE class verb** over a roster **enumerated whole** (32 lines / 19 records, the six class-verbed rows NAMED). Against the gate's own falsifiers: roster **derived from the 66**, never a check file · detector stated **inline**, both dash spellings, all three row shapes, taxonomy excluded **by SHAPE** · partition **closes** (`31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33 = 165`) · **no class verb stands where the detector grades STRICT** · and **no subset is called zero-left-open**: the escape test runs against the **pinned canonical** (`f44362757458`), record-qualified, and returns **65 booked-or-cited · 0 escaped** |
| **G-F10-9** (register limb only) | **honest-SPLIT** at `X.F.W10.b`: dated bytes untouched · seventeen glass arms in ONE register · latex assigned to §2.6's register · disjointness measured · no glass row cured consumer-side — **all GREEN**; the **register-existence** leg **RED until unit `d` lands**. The **SS-6 accretion into `COHESION.md` §4a was explicitly named as this unit's act** (`b.5` residual 2) | **GREEN on the register limb.** The seventeen arms sit in **exactly one** register: **14 cited to their dispatched packets and re-sent nowhere**, **3 accreted at §4a** as `F10-1` · `F10-2` · `F10-3`. Disjointness against §2.6 → **0**, twice · glass-ui porcelain **0** at open and close · **no glass row cured consumer-side** · **no letter minted, no INBOX row minted** — an accretion is a register act. **The gate as a whole STAYS SPLIT and this unit does not stamp it**: its **latex register-existence leg is G-F10-5's, unit `d`'s CREATE**, and ⟨cmd⟩ `ls docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` → **still absent** at this clock. *Calling the gate whole here would be the deferral-wearing-a-verb this wave exists to abolish* |

#### c.7 Residuals, disclosed rather than silent

1. **⚠ THE ONE LIVE RESIDUAL — a bounds fact, not a missing disposition, and it is `X.F.W10.a`'s flagged
   item reaching this unit.** Drain rows **65** (`i-1 = L-INFO-2`, fr-BasisSelector:92) and **76**
   (`PP-CENSUS`, fr-PathPreview:31) name `formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` as the landing
   surface for their lane-doc errata. **Both rows carry their terminal verb here** — which is what §3.3's
   deadline requires — but the **erratum text** is owed in a file **outside this unit's writable set**
   (`COHESION.md` + this record). `a.9` residual 1 predicted exactly this and asked the close to rule it.
   **Either `X.F.W10.g` lands the two errata, or they are explicitly re-homed.** Flagged in the block
   itself at §4.2.F so no reader of COHESION mistakes the silence for an oversight.
2. **The alphabet was wider than the block's first draft claimed** (c.4) — found by this seat's own probe,
   published as a mapping, and **not cured by re-wording seven banked cells**, which would have been a
   rename in a register whose first law is that banked ids and banked words are kept.
3. **One instrument of the spec's was NOT re-run, on purpose.** §2.5a-iii's record-count pipeline was
   revoked at R4-4.4 for reading a line number as a record; this seat re-cut it two ways instead and made
   the two agree (c.1). A revoked receipt is not re-run to make a number appear; it is replaced.
4. **`C3`'s standing caveat is carried, not discharged**: the canonical's arrow-clause class was found in a
   five-record spot audit of sixty-six and **is not proven confined to five** — §0.4's own *"WHAT THIS
   ROUND DID NOT CURE"* item 1 owes a corpus-wide re-derivation, **and that is a canonical act, never a
   wave's**. This block therefore cites the canonical and re-cuts no census detector.
5. **E13**: this unit minted no mail, opened no `INBOX.md` (outside its writable set) and sent nothing. The
   open's sweep — **0 unrowed · 0 rows carrying a live UNREAD status in F.W10's scope** — stands unchanged
   at this unit's clock; the three accreted arms are **register rows awaiting the next boundary letter**,
   not mail.
6. **NO ESCALATION.** Every act landed inside the writable set; the specified cure was possible at the
   bytes and was performed as specified; nothing was improvised, masked or worked around.

#### c.8 Bounds audit for this unit

**Two commits, one meaning each** — `269852ab` (the §4 drain block, G-F10-7) and `5f557674` (the §4a
accretion, G-F10-9's register limb) — **each carrying `docs/tranches/X/COHESION.md` and nothing else**
(⟨cmd⟩ `git show --name-only` on both → one path apiece). **The carve held and is measured, not asserted**:
⟨cmd⟩ `git diff 3616a2d8 HEAD -- COHESION.md | grep -E '^@@'` → **three hunks**, at §4's table, at §4.2's
insertion point and at §4a's tail; ⟨cmd⟩ `… | grep -cE '^-[^-]'` → **0 removed or changed original lines**.
**§2 and §3 are root-authored and are byte-identical to their pre-unit state** — ⟨cmd⟩ `shasum -a 256` over
each extracted region, before and after: §2 `150fba0ae29e` → `150fba0ae29e`, §3 `bd96d63d3f04` →
`bd96d63d3f04`. **`COHESION.md` was re-read immediately before each edit** (three sibling tracks share it)
and both splices were verified additive against the **live** file rather than against a stale copy.

⟨cmd⟩ `git diff --stat -- docs/tranches/V/megatranche/registry/` → **0** · `-- docs/tranches/X/fourier/` →
**0** — the **registry is READ-ONLY** and the **spec is IMMUTABLE (E-3)**; not one byte of either moved.
`git -C ../glass-ui status --porcelain` → **0** (glass-ui READ-ONLY always) · `git -C ../fourier-analysis
status --porcelain` → **1**, the pre-existing `?? .worktrees/`. **No fourier-tree byte, no glass-ui byte,
no product source, no `LEDGER.md` byte** (the wave's row is `g`'s) **and no `INBOX.md` byte** were written.
`scripts/dev/dev.sh` untouched. The sibling seats' seventeen dirty rows — including the staged
`D demo/shell/PaneSegmentedControl.vue` — are byte-identical at close to their state at open.

⌧ **ERRATUM to c.8, 2026-09-20, same sitting — published rather than silently fixed (E-3 shape).** The
closing sentence reads *"the sibling seats' **seventeen** dirty rows … are byte-identical at close to their
state at open."* **The identity claim is sound; the COUNT is not, and it could not have been.** ⟨cmd⟩
`git status --porcelain | wc -l` → **17** at this unit's open, **16** at its close — **a four-track shared
index is a clock, not a constant**: `docs/tranches/X/execution/A/X-W9.md` and `docs/tranches/X/execution/LEDGER.md`
each appeared and then left the list while this unit ran, committed by Track A and by the chassis seat
(`03f76440` · `dfa0573c`, neither this seat's). **What is actually measured, and what the audit needs, is
the invariant**: the staged `D demo/shell/PaneSegmentedControl.vue` is **still staged** at close, byte-for-byte
as at open; **every path this seat wrote is one of the two in its writable set**; and **each of the three
commits carries exactly one path** (⟨cmd⟩ `git show --name-only --format='' <sha>` → `COHESION.md` ·
`COHESION.md` · `execution/C/F-W10.md`). *A count of a shared index is a reading with a shelf life and must
be stamped as one — the same discipline this wave applies to self-counts, applied to the index.*

### X.F.W10.d

⟨the LATEX-PAPER-RELAY register · **opus** (`claude-opus-5[1m]`) · 2026-09-20 · spec sections
executed: **§2.6** `:367–388` whole · §1a row 2 `:27` · §3 **G-F10-5** `:440` + **G-F10-12**'s
latex arm `:447` · §4a.6 `:460` · §4a.10 `:464` · §4b's **LATEX-PAPER-RELAY** row `:480` · §5
`:508`/`:509`. Writable set: `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` (**CREATE**) ⊕ this
record. **One CREATE, one commit; a second commit for this receipt.**⟩

#### d.0 Crash-recovery sweep — no inherited work in this unit's writable set

⟨cmd⟩ `git status --porcelain` (value.js, at this unit's open) → 16 rows; intersection with this
unit's writable set = **∅** (`LATEX-PAPER-RELAY.md` did not exist; `C/F-W10.md` was clean at
`2b9ab798`, unit `c`'s own last commit). ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` →
**1** row, `?? .worktrees/` — unchanged since open. The dirty `demo/**`, `e2e/**`,
`execution/A/X-W5.md`, `CARRY-LEDGER.md` and `scripts/dev/dev.sh` rows are **sibling seats' and the
standing unowned row**: not read for write, not staged, not touched.

#### d.1 The measurement pass, run BEFORE a byte was written (SC-3 verified, not assumed)

The spec's anchor law is a *falsifier* on this unit's own gate, so every anchor §2.6 states was
re-resolved at the installed bytes first. Installed pin: ⟨cmd⟩
`node -e "…require('…/@mkbabb/latex-paper/package.json')"` → **0.2.1**,
`files = ["dist","grammar","src/vue/theme.css"]`, exports `. ./transform ./vite ./vue ./theme`;
⟨cmd⟩ `ls …/src/vue` → **`theme.css`** alone — **the §2.6 head's "no composable src" witness
reproduces exactly**, which is what forces every virtualiser anchor to `dist/`.

| anchor (bank) | ⟨cmd⟩ at this seat | reading |
|---|---|---|
| PAW-2 / K-19 `dist :869` | `awk 'NR==869' dist/vue.js` | `if (measuredHeights.get(id) === normalized) return;` — **HOLDS exactly** |
| PAW-2 cache sites | `grep -n 'SESSION_HEIGHT_CACHE\|syncMeasuredHeight' dist/vue.js` | declared `:790` · written `:871` · read `:822`/`:941` · fn `:867` · call sites `:885`/`:892` |
| C-06 `d.ts:19` | `awk 'NR>=19 && NR<=20' dist/vue/composables/useVirtualSectionWindow.d.ts` | `activeId:` / `activeRootId: import("vue").ComputedRef<any>;` — **HOLDS exactly**, `grep -c` → **2** |
| PAW-1's 42 · C-20's 6 | `grep -c 'hsl(var(' src/vue/theme.css` ×2 · `grep -c 'hsl(var(--section-heading'` | **42 · 42** and **6**; `wc -c` → **15764** |
| PAW-11's 5 emitters | `grep -c '<a class="paper-ref' dist/chunk-A7GY23HR.js` ×2 | **5 · 5**, against `<a href=` → **2** — the href-less fact holds |
| PAW-38 slug | `grep -n 'slugify' dist/chunk-A7GY23HR.js` | `:92` `function slugify(text)` · `:773` `const id = slugify(range.title);` — **no uniquifier at either end** |
| PAW-39 `sourceLevel` | `grep -n 'sourceLevel' dist/vue.js` | **one hit, `:1050`** — the field IS emitted; the outline drops it |
| PAW-32 slot types | `grep -rl '__VLS_Slots' dist` | **2** component d.ts (`PaperSection`, `Theorem`); the rest carry **0** `slots` |
| PAW-45/46 memo | `grep -n 'new Map()' dist/vue.js` · `grep -n 'function useKatex'` | `var cache = new Map();` **:387**, immediately above `useKatex(macros)` **:388** — module-scoped, keyed on tex while macros are a parameter |
| K-11 flattener | `grep -n 'flattenPaperSections' dist/vue.js` | `:987` banner · `:1040` fn · `:1851` export; its walk at `:1042/:1052` passes `parentId` **correctly** |
| **L/D29 `:27/:32`** | `grep -n 'parentId\|useTreeIndex' dist/vue.js` | ⚠ **DRIFT**: the banked pair addresses `src/vue/tracking/useTreeIndex.ts`, **a path this package does not ship**. **INTENT at the true bytes: `:85` and `:90`**, inside `useTreeIndex` `:73–119`, masked by `isDescendant` `:103/:108`. Recorded at the register's §5; **the bank is not edited** (E-3) |
| M2's exemption | `grep -n 'sidebar-top-btn' dist/vue.js` | `:602` `if (target?.closest("[data-toc-id], .sidebar-top-btn")) return;` — the hard-coded literal the lock is about |

**Two figures were deliberately NOT re-derived** and are labelled as citations in the register:
**PAW-39's census (quote 31, never 35 — SC-2)** and **PAW-45/46's `PaperView :38` call site** (a
*consumer* coordinate). Re-deriving either would mint a rival number against a seat-corrected
census — the exact defect shape this sub-tranche has convicted three times.

#### d.2 The delivery point — E-7 re-measured at the producer's own tree

⟨cmd⟩ `find /Users/mkbabb/Programming/latex-paper/docs -type d -name coordination | wc -l` → **0** ·
⟨cmd⟩ `find /Users/mkbabb/Programming/latex-paper -iname 'INBOX.md' -not -path '*/node_modules/*' |
wc -l` → **0** · ⟨cmd⟩ `ls …/latex-paper/docs/tranches/` → **`VIRT`**. **E-7 holds at the bytes**:
alone among the constellation's producers (glass `BK/coordination/` · keyframes `V/coordination/` ·
atlas `P/coordination/` · fourier `F/coordination/INBOX.md`), **latex-paper has no mail surface at
all** — which is precisely why the register lives in our tree and why naming a delivery point is
part of the gate rather than a courtesy.

**Named, in one line**: `latex-paper/docs/tranches/VIRT/coordination/valuejs-inbound-<date>-<slug>.md`
— the producer's own tranche dir plus the `coordination/` leaf every sibling producer already
carries, with the filename idiom **measured** from the two that use it. **The hop is the X
formation mail seat's act** (sibling trees are READ-ONLY to every wave seat), which is the same
routing both INBOX rows already state in their own words.

#### d.3 Reconciliation against O-24 and O-33 — cited, and NOTHING re-sent

The baseline's own lesson (E-14, *"the baseline moved"*) applies a third time: ⟨cmd⟩
`grep -c 'latex-paper' docs/tranches/V/coordination/INBOX.md` → **4** (the spec's cell says 0-then-1).
Two are real relay rows, and their contents were enumerated from the letters' **own bytes**, never
from their INBOX prose: ⟨cmd⟩ `grep -oE '(PAW|WU|C)-[A-Za-z0-9]+' … | sort | uniq -c` →
**O-24**: `6 C-06` · `2 WU-S` · `1 PAW-1`; **O-33**: `6 PAW-38` · `1` each of
`PAW-54 PAW-46 PAW-45 PAW-39 PAW-32 PAW-30 PAW-28 PAW-1 K-9`.

**Three dispatch verbs, and the middle one is the honest finding of this unit**: O-24 §4 `:131-138`
carries four rows **by WU-S row label** — *"slot-type · slug-uniqueness · sourceLevel · ref"* —
which are **PAW-32 · PAW-38 · PAW-39 · PAW-11** with their ids unspelled. Recorded as
**DISPATCHED (by row label)** rather than silently promoted to "id spelled" or demoted to
"missing": a later seat grepping the letter for `PAW-11` finds nothing and would otherwise conclude
the ask never left. Likewise **L/D29** is **DISPATCHED (by mechanism)** — O-33 §3 `:52-59` carries
the `parentId` defect verbatim in both producers' shapes, id unspelled.

⊘ **The one consequential RED inside a dispatched row**: **C-19 ⊕ PAW-33 is SPLIT.** ⟨cmd⟩
`grep -c vite` → **0** in O-24; O-33's single `vite` hit is a command path
(`grep -rn "theme.css" src/ vite.config.ts`), not the ask. **The katex half left; the vite half
never did** — while F.W1's residual 6 re-states *both* ranges to NWO-5 **in our record**. The
consumer-side `^8.0.16 → ^7.3.6` conforming masks it today and is reversible the day the producer
widens; it is **not** a producer fix and is not recorded as one.

#### d.4 PAW-1's F.W1 fourth-package question — ANSWERED, from F.W1's own gate

The brief says *answer it by reading what F.W1 actually ruled*, so the answer was taken from
`C/F-W1.md`'s **G16** rows and not from this wave's expectation:

- **arm one died by measurement**: the 42 are **published producer bytes**
  (`files[2]` = `src/vue/theme.css`; `grep -rn "hsl(var(" web/src | wc -l` → **0**), so an in-place
  rewrite is a `node_modules` patch — **a HIGH defect**, not a judgement call.
- **the ruling**: *"G16 therefore resolves to arm two, and the named owner is the latex-paper
  producer"* — stamped **GREEN on the second arm, owner named**, seated by **NWO-5 = O-24**, whose
  header reads `**To**: the **latex-paper** producer`.
- **the transaction stayed THREE packages** — glass `^8.0.0` · keyframes `^6.0.0` · value `^4.0.0`
  at `fourier/web/package.json`. **latex-paper is not in it** and stays **0.2.1**, the latest
  published version, *"so no producer fix exists to adopt"*.

**⇒ The answer in the ledger: SEATED EXPLICITLY (arm 2) — NOT added to the transaction.** The
register states it with the seat's four coordinates: gate **G16 arm 2** · instrument **O-24
(NWO-5)** · owner **the latex-paper producer** · receiving surface **this register** · delivery
point **d.2's path**. **PAW-1 is discharged as a question and stays open as an ask** — the 42 sites
are still 42 — which is what *seated, not cured* means, and nothing here credits F.W1 with a cure
it did not make.

#### d.5 The CREATE, and its self-count read from the settled bytes

`docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` — **295 lines · 36,954 B** at close. Sections: §0
what it is/is not ⊕ the SC-3 and K-9 statements · §1 the delivery point · §2 the O-24/O-33
reconciliation · §3 the fourth-package answer · §4 the roster ⊕ §4a the self-count · §5 anchor
conformance and the one drift · §6 the ten locks · §7 gate readings · §8 residuals.

**SELF-COUNT, double-run after the bytes settled** (never composed and then asserted):

| figure | ⟨cmd⟩ | reading |
|---|---|---|
| roster rows | `grep -cE '^\| \*\*[0-9]+\*\* \|' LATEX-PAPER-RELAY.md` | **15 · 15**, numbered `1..15`, no gap, no duplicate |
| parity with the spec | `awk 'NR>=372 && NR<=388' waves/F-W10.md \| grep -c '^\| \*\*'` | **15 · 15** — **one row per §2.6 carry, one-to-one** |
| banked ids | raw token sweep of the carry column, `sort -u \| wc -l` | **22**, partitioned **in the register** to **20 roster ids** ⊕ `L/D21` (cited inside row 4 as PAW-46's twin — counting it again would double-book the fold) ⊕ `C-2` (**not an id**: the probe's own false positive over row 7's `SC-2`). **20 ≥ 18**, the spec's *"≥18 named carries"* |
| status tally | `grep -oE '\| \*\*(UN)?DISPATCHED\|\| \*\*SPLIT' \| sort \| uniq -c` | **9 DISPATCHED · 1 SPLIT · 5 UNDISPATCHED**, identical both runs; **9+1+5 = 15** closes against the row count. The probe matches only a *cell-opening* verb, so row 13's mid-cell "vite half UNDISPATCHED" is correctly excluded |
| SC-3 falsifier | anchor column → `grep -c 'Programming/latex-paper'` | **0** — not one of the fifteen anchors reaches the producer's working tree |
| K-9 falsifier | `grep -n ':root'` | **4** hits, **every one a statement of the KILL** (§0 · row 1 · §6 lock 1 · §7's falsifier row); **the snippet is prescribed nowhere** |
| C-20 double-count | `grep -n -o '48'` | **4** hits, all lawful: the spec line cite `:480` (`:8`), PAW-2's banked anchor `:48` (`:153`), the sentence *"publishing 42 + 6 as 48 would be the double-count"* (`:165`) and lock 9's *"Never 48."* (`:226`) — **no 48 is published as a figure**. ⊙ a first, windowed form of this probe (`grep -o '.\{22\}48.\{18\}'`) returned **3**: it silently dropped the line-end hit at `:226`. **The probe was replaced rather than its answer kept** — a context window is a filter, and a count taken through one is a count of what the filter admitted |
| PAW-39 census law | `grep -n '\b35\b'` | **2** hits, both the law's own wording *"quote 31, never 35"*; **35 is published as a figure nowhere** |

⊙ **Two self-corrections, applied before the commit rather than disclosed after it** — both caught
by running the probe against the draft instead of trusting the sentence: (1) a first draft claimed
*"fourteen dispatched arms"*, which no probe returns — replaced by the measured **9 / 1 / 5**;
(2) a first draft claimed *"no path under `…/latex-paper/src/` appears anywhere in this file"*,
which **its own sentence falsified** (`grep -c` → 1). The claim was re-scoped to the roster's
**anchor column**, where the probe reads **0**, and the residual prose mentions are named. *A
universal measured over one section is the exact shape §2.8 E-4 convicts.*

#### d.6 Gate readings, BEFORE → AFTER

| gate | BEFORE (wave baseline, 2026-09-19) | AFTER (this unit, 2026-09-20) |
|---|---|---|
| **G-F10-5** the LATEX-PAPER carry has a receiving surface | **RED** — the register **ABSENT**; the INBOX reading had moved 0 → 1 → **4** | **GREEN**, on all of the gate's own acceptance conjuncts, each measured: the file **exists** · **every §2.6 carry rowed** (15 ↔ 15, 20 ids, none re-booked) · **PAW-1's F.W1 question answered in the ledger** (SEATED at G16 arm 2) · **with a named delivery point** (d.2) · falsifier *producer-src anchor* → **0** · falsifier *K-9 snippet prescribed* → **absent** |
| **G-F10-12** latex arm | **RED** — `latex-paper` → 0 across the sent packets | **RED, unchanged and not argued away.** ⟨cmd⟩ `find glass-ui/docs -name 'valuejs-outbound-*.md' \| wc -l` → **12**, of which `grep -l 'latex-paper'` → **0** (these are the *glass* packets — counting them as this arm's denominator is E-2's corrected instrument error); **the arm's true denominator** ⟨cmd⟩ `find latex-paper/docs -type f -name '*valuejs*' \| wc -l` → **0**, *because the path does not exist*. A receipt is READ, never asserted, and **there is nothing to read** — so this unit **does not stamp G-F10-12** |

**The RED belongs to the hop.** State recorded exactly: **AUTHORED + ROWED (×2) · UNDELIVERED (×2)
· five roster rows and one half-row not yet authored in any letter.** What this register cures is
the half it can: the arms are now rowed where a gate can find them, and the delivery point has an
address. What it does not cure, it does not claim.

#### d.7 Residuals and the L-11 + M2 HARD LOCK, verified live

1. **Five UNDISPATCHED rows ⊕ one half** — PAW-2 · the K-11 flattener rider · L/D5 + L/D30 · L/D8 ·
   L-11 + M2 · C-19's vite limb. Their cure is **the NEXT letter the mail seat sends**. **This unit
   minted no letter and no INBOX row** — the register is ASK-ONLY and a third channel is what
   F-W10 §5 `:509` forbids.
2. **§4a.10's HARD LOCK is LIVE — and it has been HONOURED.** ⟨cmd⟩
   `grep -rn 'sidebar-top-btn' fourier-analysis/web/src` → **4** hits, the class **unchanged** at
   `PaperSidebar.vue:121/:277/:309`, with the lock written into the file's own comment at `:110`
   (*"⊘ `PS-M2` RELAY-BEFORE-RENAME: the CLASS NAME is unchanged"*) against the producer's
   exemption at `dist/vue.js:602`. **F.W4 closed without renaming it**, which is the lock's
   acceptance condition and is banked here rather than assumed.
3. **L/D29's anchor drift** is recorded at the register's §5 and **the bank is not edited** (E-3).
4. **`PAW-54` rides O-33 but is not a §2.6 carry** — cited in the register's §8 and **NOT rowed**;
   rowing it would book an id this wave's roster does not carry.
5. **The one-register invariant holds**: latex rows here, glass rows at COHESION §4a (unit `c`).
   **No escalation.** No spec cure was impossible at the bytes; nothing was substituted.

#### d.8 Bounds audit for this unit

**Writable set honoured, hard bound**: `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` (CREATE) ⊕
`docs/tranches/X/execution/C/F-W10.md` (this receipt). **No other path written** — not
`COHESION.md`, not `INBOX.md`, not `LEDGER.md` (this unit turns no ledger cell; the close seat
owns the row). `/Users/mkbabb/Programming/latex-paper/**`, `fourier-analysis/**` (including
`web/node_modules/**`) and `glass-ui/**` were **READ-ONLY throughout** — `ls` · `find` · `grep` ·
`awk` · `sed` · `wc` · `node -e` as a *reader* only; **zero** bytes written in any sibling tree,
**no `node_modules` patch**, no pin moved. `scripts/dev/dev.sh` untouched and unstaged; the
sibling-seat dirty rows left exactly as found. **Pathspec commits only** — two commits, one path
each. No `git stash`, no `reset`, no force-push. **Commits**: `4278b798` (the CREATE) ⊕ this
receipt's own.

---

### X.F.W10.e

⟨**SERVED MODEL**: `claude-opus-5[1m]` · the ADOPTION-ASKS reconciliation letter and the four-ask
INBOX rows · spec sections executed: §2.4 **ASK-3** · **ASK-5** · **INV-22-COLOR** · **CASCADE-VJS** ·
**FN-1..FN-7** · **VALUEJS-2.0.0-NOTE** · **O-14 + INBOUND-PARITY** · §1a rows 4–5 · §3 **G-F10-1** ·
**G-F10-2** · §4a.13 · §4b's fourier-sub-session row · §5's four exclusions. **Two artefact commits
(the second a correction), one receipt commit.**⟩

#### e.0 — Crash-recovery sweep and bounds, before any write

⟨cmd⟩ `git status --porcelain` (value.js, at this seat's open) → 18 rows. **Intersection with this
unit's writable set** — the letter path, `V/coordination/INBOX.md`, this record — was **∅** at open
(the letter did not exist; `INBOX.md` and this record both read clean at HEAD). **No predecessor seat's
partial work exists on this unit**, so nothing is inherited, finished or rewritten. The dirty
`demo/**`, `e2e/**`, `docs/tranches/X/execution/{A,D}/…` and `CARRY-LEDGER.md` rows belong to sibling
seats on other tracks and were **not read for write, not staged, not touched**; `scripts/dev/dev.sh`
is the standing unowned dirty row and was never opened.

⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` → **`?? .worktrees/`**, one row, **at open and
again at close, byte-identical**. **ZERO fourier-tree writes** (COMMISSION §2 — asked, never made),
which is this unit's hardest lock and is verified by the same command at both ends rather than asserted.

#### e.1 — MEASURE BEFORE EDIT: every ask re-measured at the live bytes, double-run

**The baseline this record banked at open is reproduced, and then it moves.** Each ask below was
re-measured at this seat's own clock, **2026-09-20**, every reading run twice.

**ASK-3 (P1).** ⟨cmd⟩ `git log --follow --oneline -- scripts/deploy/deploy-hook.sh | tail -1` →
**`e62567ab feat(deploy): N.W4.A/B/D — … deploy-hook.sh authored (Ask 3) …`** — the hook exists
(⟨cmd⟩ `wc -l` → **201**), was authored **for this ask by name**, declares the canonical deploy dir a
git checkout at `:46`, and **refuses to run** against a non-checkout at `:144`. The residual is the
**host-side directory conversion**, which no commit in any tree can perform.

**ASK-5 (P3).** ⟨cmd⟩ `curl -sS -o /dev/null -D - https://color.babb.dev/` → **`HTTP/2 200`** ·
**`server: cloudflare`** · **`cf-ray: a3de3eeabb32e61d-IAD`** (run 1) / **`a3de427a79498214-IAD`**
(run 2) — **published as a per-request clock reading, never as a constant**, which is the c.8 lesson of
this wave applied to a header. ⟨cmd⟩ `grep -rc 'peaceiris' .github/workflows/*.yml` → `ci.yml:`**0** ·
`deploy-pages.yml:`**0** · `release.yml:`**0**, both runs. **The ask's verbatim machine-checkable
criterion is MET.**

**INV-22-COLOR (P3) — the reading MOVED, in both directions at once.** ⟨cmd⟩
`curl -sSk -o /dev/null -w '%{http_code}'` over `/` `/health` `/docs` `/openapi.json` →
**200 · 200 · 200 · 200**, both runs: the 404s the ask records are **gone** (cured in the same N.W4
commit `e62567ab`). But ⟨cmd⟩ the same probe **without** `-k` → **`000`**, `curl: (60) SSL certificate
problem: certificate has expired`; and ⟨cmd⟩
`openssl s_client -connect api.color.babb.dev:443 -servername api.color.babb.dev </dev/null | openssl x509 -noout -dates -subject`
→ `notBefore=May 28 00:12:08 2026 GMT` · **`notAfter=Aug 26 00:12:07 2026 GMT`** ·
**`subject=CN=sudoku.babb.dev`**. **Expired AND wrong-name.** The two facts are booked as **two
things** — the ask DONE, a new live defect filed beside it under an owner-mail flag — because folding a
new transport failure into an old ask's verb leaves the ask open forever against something it never named.

**CASCADE-VJS (P2).** ⟨cmd⟩ `grep -n 'unplugin-vue-markdown' package.json` → `:114 "^32.0.0"`;
⟨cmd⟩ `grep -c '"file:' package.json package-lock.json` → **0** · **0**. ⟨cmd⟩
`git log -S '"file:../glass-ui"' --oneline -- package.json | head -1` → **`164343c1 feat(v4)!: value 4.0
producer surface …`**, whose diff **removes** both `file:` lines. The R.W7 decline's own **named exit**
was taken, on value.js's motion — disclosed in the letter rather than left to rot.

**FN-1..FN-7 + the 2.0.0 note.** ⟨cmd⟩ `grep -n '| \*\*M.W10\*\*' ../fourier-analysis/docs/tranches/M/PROGRESS.md`
→ `:25 … | ` **`planned`** — **M.W10 has NOT closed**, so FN-5's rider (*"BEFORE or WITH fourier
M.W10"*) is **still satisfiable**, which is a measurement and not a hope; the spec's *"one non-answer
with a dated consequence"* has not yet had its consequence. ⟨cmd⟩ `grep -n '@mkbabb/' ../fourier-analysis/web/package.json`
→ `:23 "@mkbabb/value.js": "^4.0.0"`; installed **4.0.0**; ⟨cmd⟩ `npm view @mkbabb/value.js version` →
**4.0.0** — pin, install and registry agree, so the note's peer-floor ask is **DISCHARGED at 4.0.0**
and the reply states 4.0.0, **never the stale 2.0.0**.

**⚠ ANCHOR DRIFT, re-resolved rather than quoted (the METHOD's first clause).** §2.4's
VALUEJS-2.0.0-NOTE row carries *"§3's M.W7 `sampleColorRamp` booking still not struck in
`M/PROGRESS.md:22`"*. **At the true bytes that address does not hold**: ⟨cmd⟩
`grep -c 'sampleColorRamp' ../fourier-analysis/docs/tranches/M/PROGRESS.md` → **1**, at **`:44`**, and
`:22` (the M.W7 row) contains **no** such token. **INTENT GIVEN AT THE TRUE BYTES**: `:44` reads
*"value.js 0.13.0 [`sampleColorRamp` shipped]"* — the row is no longer a 0.13.0 *booking*, it is a
*shipped* claim, and the claim is what needs correcting. ⟨cmd⟩ `grep -rln 'sampleColorRamp' src/ dist/`
→ **4 files** with working `"version": "4.1.0"`, **while the registry serves 4.0.0** — i.e. the SCI-1
restoration is **built and UNPUBLISHED**, so the honest wording relayed is *RETIRED-BY-DELETION AT
4.0.0, RESTORED IN 4.1 UNDER SCI-1, UNPUBLISHED AS OF 2026-09-20*, **with no cut date promised**.

**INBOUND PARITY.** ⟨cmd⟩ `find docs -name INBOX.md` in fourier → **1**
(`docs/tranches/F/coordination/INBOX.md`, ⟨cmd⟩ `wc -l` → **242**); ⟨cmd⟩ `ls CLAUDE.md` → present;
⟨cmd⟩ `git ls-files | grep facility19` → tracked, rowed at their **M-4**. **All three legs GREEN
BEFORE CURE and credited to X.F.W0 unit `b` (COHESION §0k.1) — this unit claims none of them.**

#### e.2 — The letter, CREATED at its spec-fixed path

`docs/tranches/V/megatranche/coordination/FOURIER-ADOPTION-ASKS-RECONCILIATION-2026-08-25.md`,
**411 lines**, line 1 `SERVED MODEL: claude-opus-5[1m]`. **The `2026-08-25` in the path is the SPEC
ROW's authoring date and the letter says so in its own header** — §1a row 5 fixes the path *"so no
seat invents a third channel"*, so the date was **not** re-stamped into a second filename and nothing
is back-dated. Sections: §0 the verb register · §1 Ask 3 **ADOPT** · §2 Ask 5 **DONE** (keyframes half
**∦, declared and not absorbed**) · §3 inv-22-color **DONE** ⊕ the TLS defect filed beside it ·
§4 cascade-vjs **SPLIT, relayed from the EXISTING `R/letters/CASCADE-VJS-RESPONSE.md`** (31 lines,
R.W7 2026-07-04) with the §4.2 correcting minute · §5 FN reconciled against **O-29**, not re-answered ·
§6 the note answered **AT 4.0.0** · §7 parity verified and credited to F.W0 · §8 three declinable asks
back · §9 the ledger coordinates.

**The G-F10-1 falsifier was run against the letter's own text before it shipped**: *"a verb naming a
peer actor's action as an acceptance condition."* Every one of the four verbs is discharged by bytes or
measurements at **our** end, and the letter states it in terms: *"If you row none of the three asks in
§8, not one verb above changes."* The one leg that is a fourier act — logging the reply — is **asked**
in §8 and **measured** in §7, never made a condition of a disposition.

#### e.3 — The four asks ROWED, and an id race disclosed rather than smoothed

**I-36 (Ask 3 · ADOPT) · I-37 (Ask 5 · DONE) · I-38 (inv-22-color · DONE ⊕ owner-mail flag) ·
I-39 (cascade-vjs · SPLIT) · O-44 (the letter · SENT)** appended to `V/coordination/INBOX.md`.
**Append-only, proven rather than promised**: ⟨cmd⟩ `diff <(grep -v my-five-rows INBOX.md) INBOX.before`
→ **identical** — *no existing row was rewritten*. Row census, double-run: **82 → 88**
(⟨cmd⟩ `grep -cE '^\| [IO]-[0-9]+[a-z]? \|'`), my five ⊕ a sibling's O-43 and its sweep line.
Cell shapes match each table (**6 cells** for `I-`, **5** for `O-`), so the positional Status reads
later sweeps use keep working; **no status cell of mine contains the bare word UNREAD**, which would
have manufactured a false positive in every future sweep.

**⚑ THE RACE, RECORDED.** This letter was first rowed **O-43**. A sibling seat on Track D
(**X.P.W4.s**) wrote **its own O-43** plus a sweep line into this shared file in the seconds between
this seat's write (`cp`, verified 87 rows) and its `git add` — so commit **`a8fa79d2`** carried a
sibling's uncommitted bytes and the file briefly held **two rows under one id**. **Cure, at this
seat's own row only**: the EARLIER row keeps the id; this seat renumbered **its own** row to **O-44**
and re-pointed its four `I-` rows and the letter's §9 (commit **`66e35333`**, ⟨cmd⟩ `git diff --stat`
→ **5 insertions / 5 deletions**, and ⟨cmd⟩ a grep for changes to the sibling's `O-43` row → **0**).
**Nothing was reverted, unstaged or reset** — the sibling's bytes stay committed and are disclosed
here, in the letter, and in the row itself, because the alternative was to touch a path that belongs
to another seat.

#### e.4 — Gate readings, BEFORE → AFTER (double-run; nothing argued green)

| gate | BEFORE (this record's banked open) | AFTER (this seat, 2026-09-20, double-run) | verdict |
|---|---|---|---|
| **G-F10-1** asks reconciled in BOTH ledgers | **RED** — four asks OPEN; value INBOX `grep -ci` → `Ask 3` **0** · `Ask 5` **0** · `inv-22` **0** · `cascade-vjs` **0** · `ADOPTION-ASKS` **0** (E-8: UNROWED) | value side: the same greps → **2 · 2 · 2 · 2 · 5**; ⟨cmd⟩ `grep -cE '^\| I-3[6-9] \|'` → **4**, each with a disposition verb; cascade-vjs answered **as the SPLIT from the existing R.W7 letter** (no new letter exists — §5's exclusion honoured); ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` → **`?? .worktrees/`**, unchanged at open and close. Far end: their ledger `grep -ci` → `ADOPTION-ASKS` **0** · `cascade-vjs` **0** · `Ask 3` **0** | **SPLIT — the value-side leg is GREEN by act; the "reply landed in fourier's ledger" leg is RED and is NOT this seat's to turn** (COMMISSION §2 / §5: *"Every fourier-tree write … their sub-session; F.W10 asks and verifies receipt"*). **Asked in §8, measured in §7. Not argued green** |
| **G-F10-2** inbound-mail parity | **SPLIT** — three legs GREEN before cure (their INBOX · CLAUDE.md · facility-19 tracked); **RED** on the FN-1..FN-7 fourier-side verbs | the three parity legs re-verified **GREEN** and **credited to X.F.W0 `b`** (COHESION §0k.1), claimed by nothing here; FN legs: ⟨cmd⟩ `grep -ci 'FN-'` in their ledger → **0 · 0**, while our answers went out at **O-29** (2026-09-18) and are reconciled at §5; **FN-5's rider is still satisfiable — `M.W10` measured `planned`** | **SPLIT, unchanged in shape and honest about it**: the far-end verb leg stays **RED**; the rider leg is **answerable in the order it asks for**, which is more than the spec's born-RED reading had |

**Not one GREEN leg above is claimed as this wave's work.** The three parity legs are F.W0's; the
Ask-3 hook and the inv-22 route table are **N.W4's** (`e62567ab`); the CF-Pages spine is N.W4.F's;
the cascade-vjs decline is **R.W7's letter**, relayed and not re-authored. This unit's own act is the
correspondence: one letter, five rows, two gates read honestly.

#### e.5 — Residuals, and what is deliberately NOT done here

1. **⚑ OWNER-MAIL FLAG — `api.color.babb.dev` TLS.** Expired 2026-08-26 and issued for
   `CN=sudoku.babb.dev`; every verifying client gets `000`. A **live production reachability failure**
   of the api vhost, strictly more severe than the P3 it stands behind. **Maintainer-owned
   infrastructure, outside every X·F bound** (§5) — **recorded, not repaired, not scheduled.** Rowed
   at **I-38**, flagged in the letter's §3.2.
2. **Ask 3's host-side conversion** — the rsync dir → git checkout on the deploy host. No commit can
   make it; the hook self-verifies it at `:144`. Booked, unscheduled, maintainer-owned.
3. **The far-end ledger legs of G-F10-1 / G-F10-2** — fourier logging O-29 and this letter, and
   rowing the FN verbs. **Asked in §8; receipt verified absent in §7.** Turning them here would
   require a fourier-tree write, which COMMISSION §2 forbids and §5 excludes by name.
4. **The anchor drift at `M/PROGRESS.md:22`** — re-resolved to `:44` with INTENT given at the true
   bytes (e.1). Recorded, per E-3, **beside** — no dated spec byte was edited by this unit.
5. **The unpublished 4.1.0 `sampleColorRamp` restoration** — disclosed to the counterparty *before*
   it can mislead, with **no cut date promised** (the O-8/CC-084 lesson).
6. **ESCALATIONS: none.** Every act the unit was given was possible at the bytes as specified.

#### e.6 — Self-count and bounds

**SELF-COUNT LAW.** The figures this section publishes were read from the **settled** files, from
outside them: the letter is **411** lines (⟨cmd⟩ `wc -l`, after the last append) with line 1
`SERVED MODEL: claude-opus-5[1m]`; `INBOX.md` carries **88** id-rows, of which **five are this
unit's** (`I-36` · `I-37` · `I-38` · `I-39` · `O-44`); **four** value-side ask greps moved `0 → 2`
and `ADOPTION-ASKS` `0 → 5`; **zero** far-end greps moved.

**BOUNDS.** Writes: **exactly three paths**, all inside this unit's writable set — the letter
(CREATE), `V/coordination/INBOX.md` (append + this seat's own five rows corrected), and this record.
**`fourier-analysis/**` was READ-ONLY throughout** (`git`, `grep`, `find`, `sed -n`, `wc`, `node -e`
as a reader only) and its porcelain is byte-identical at open and close; **glass-ui untouched**;
`scripts/dev/dev.sh` never opened; sibling-seat dirty rows left exactly as found, and the one sibling
row that entered a commit by race is **disclosed, not reverted**. **Pathspec commits only** — three
commits: **`a8fa79d2`** (the letter ⊕ the five rows, one meaning), **`66e35333`** (the id-collision
correction at this seat's own row), and this receipt's own. No `git stash`, no `reset`, no
force-push, no cron.

**One draft claim corrected by its own probe, published rather than quietly fixed.** This receipt was
drafted saying the letter is **404** lines — the reading taken *before* §9's id-race disclosure was
appended. Re-measured at the settled bytes, double-run: ⟨cmd⟩ `wc -l` → **411 · 411**, and both sites
(e.2 and e.6) now carry **411**. **A figure read from a draft is a figure read from a file that no
longer exists**; the count follows the settled bytes, which is the whole of WRITE-THEN-MEASURE.

### X.F.W10.f

⟨The G24/G33 evidence relay · `claude-opus-5[1m]` · 2026-09-20, this seat's own clock · sitting of
record **2026-09-17**, the owner's begin-word (COHESION §0j). **RELAY-ONLY: G24 and G33 are X-W9's
gates and are never claimed here**; no fourier-tree edit is credited as an X-W9 gate; the banked
`0.192` is never quoted as current.⟩

#### f.0 — Crash-recovery sweep, mail, and bounds before any write

⟨cmd⟩ `git status --porcelain` (value.js) → **24 rows** ⟨*a draft figure corrected by its own probe,
published rather than quietly fixed — this line first read **23**, a recount of the open transcript
returns **24**, and at this unit's close the same command returns **21** with a CHANGED composition
(two sibling execution records committed away, a new Track A `??` spec appeared). A porcelain count
is a reading of a live tree at one instant, and it is published as one*⟩; intersection with this unit's writable set
(the letter · `V/coordination/INBOX.md` · this record) = **`INBOX.md` only**, dirty by the **units
`.a`–`.e` commits of this same wave** and by sibling-track rows, **not** by a killed predecessor of
this unit: ⟨cmd⟩ `git log --oneline -1 -- docs/tranches/V/coordination/INBOX.md` → `66e35333`
(unit `.e`'s id-collision cure), and the letter path did not exist at this seat's first act
(⟨cmd⟩ `ls …/FOURIER-EVIDENCE-RELAY-G24-G33-2026-08-25.md` → **No such file**). **No inherited
partial work on this unit.** ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` → **1** row
(`?? .worktrees/`) · ⟨cmd⟩ `git -C ../glass-ui status --porcelain` → **0** — identical at open and
at close, both trees READ-ONLY throughout.

**E13 (unit-level)**: ⟨cmd⟩ `find` over the four landing paths for `*2026-09-19*` / `*2026-09-20*`
→ the single hit is `V/megatranche/audit/probes/src-surface-totality.2026-09-19.mjs`, **a probe, not
mail**. **Zero unrowed mail at this seat**; the two rows this unit mints are outbound.

#### f.1 — G24 re-measured at open: the instrument is UNRUNNABLE TO COMPLETION, and the cause is named

⟨cmd⟩ `node docs/tranches/V/megatranche/audit/probes/fourier-value-import-drift.mjs` (×2, identical)
→ legs 1 and 2 print **RED**, then **leg 3 — the drift leg — throws**:
`ERR_MODULE_NOT_FOUND … fourier-analysis/web/node_modules/@mkbabb/value.js/dist/value.js`; exit
**1**, by the uncaught throw and **not** by the probe's own `process.exit(red ? 1 : 0)` at `:74`.

**Cause, read at the bytes rather than guessed**: leg 3's *old* operand (`:54`) is the **0.13.0-era
layout** inside fourier's install. The package **is** installed — ⟨cmd⟩ `node -e` on its
`package.json` → `4.0.0`, `has "." key: false`, `main`/`module` **undefined**, export map
`./color ./value ./css ./easing ./math ./transform ./quantize` — and ⟨cmd⟩ `ls …/dist/` returns
`anchors-C_wdoOYd.js` · `operations-CB_1wGy4.js` · `result-CZJK1CwL.js` · `subpaths`, **no
`dist/value.js`**. **F.W1's uplift removed the instrument's reference build from the machine.**

**Repaired by nobody at this seat.** The probe is **outside §1a**, and its cure is already homed:
**`ESC-W9-G24-SUBSTRATE`** (COHESION §0ac) — *"X-W9.f measures the eight analytic arms against the
registry `0.13.0` tarball … `../fourier-analysis` stays untouched."* **Cited, not duplicated. No
`try/catch` around the throw, no import rewritten, no fallback.**

**The banked figure is labelled BANKED in both places it appears** (letter §1.2 and the O-45 row),
and X-W9's own reproduction is **READ**: `evidence/W9/bench-table-4.1.md` §3 (tarball
`mkbabb-value.js-0.13.0.tgz`, 90,832 bytes, sha256 `b943f722a968…`) reproduces *"8 of 22 … worst
1.923e-1 (ease-out-circ) RED"*; §4 reads the **4.1.0 worktree** cut at *"0 of 22 … worst 4.563e-6
GREEN"*. **Not banked as this wave's green**: ⟨cmd⟩ `npm view @mkbabb/value.js version` → **4.0.0**
against ⟨cmd⟩ `node -p "require('./package.json').version"` → **4.1.0** — **the cure is unpublished,
and what fourier resolves is the build in which the eight arms drift.**

**⚠ THE TWO EIGHTS, kept apart by measurement rather than by care.** G24's drifting eight
(`ease-out-circ` · `ease-in-expo` · `ease-in-circ` · `ease-in-quad` · `ease-in-cubic` ·
`ease-out-sine` · `ease-in-sine` · `ease-out-quad`) and ESC-4's imported eight (`linear` ·
`easeInOutQuad` · `easeOutCubic` · `easeInOutCubic` · `easeInOutSine` · `easeOutExpo` ·
`easeInOutExpo` · `easeInOutCirc`) are **different sets in different spellings**: ⟨cmd⟩ `node -e`
kebab-mapping the second → `drift n=8 · imported n=8 · **intersection: 0**`. Fourier imports exactly
the names that did **not** drift and owns the fourteen that did — **credited to F.W1 / ESC-4
(COHESION §0o), and explicitly NOT credited as an X-W9 gate.**

**Two instrument-scope drifts filed beside the probe, repaired by nobody**: (1) leg 1's five-site
list is a **hard-coded literal** (`:26-32`) reporting consumer breakages that no longer exist —
⟨cmd⟩ `grep -rn 'from "@mkbabb/value.js"' web/src/` → **0** against `…/value.js/'` → **7** subpath
sites, and `timingFunctions` → **2 hits, both prose comments**, zero call sites; (2) leg 1 reads
**this worktree's** `package.json` (`4.1.0`) for a question about what fourier **resolves** (`4.0.0`).

#### f.2 — G33 relayed: three legs read, the fourth declared PRE-WINDOW, the identity held to ONE row

⟨cmd⟩ ×2, identical both runs: `ls docs/tranches/V/coordination/*-inbox-2026-09-18-*.md | wc -l` →
**5** (atlas · fourier · glassui · keyframes · parse-that) · `grep -cE '^\| O-3[4-8] \|'` → **5** ·
`../keyframes.js/package.json:71` → `"@mkbabb/value.js": "4.0.0"` exact. **All three credited to
`X-W9.i`**, whose own record reads *"G33 GREEN on its three readable legs"*.

⟨cmd⟩ `npm ls @mkbabb/value.js`, exit captured ×2 per tree → **fourier `web` exit 0 · 0**
(`glass-ui@8.0.0 → deduped` · `keyframes.js@6.0.0 → deduped` · direct `4.0.0`) and **keyframes.js
exit 0 · 0**. ⊘ **PUBLISHED AS A PRE-WINDOW READING, NOT AS THE POST-WINDOW LEG** — G-F10-4's own
falsifier names *"reading the post-window leg before the window"*, and the window is shut: the
registry serves **4.0.0** while this worktree reads **4.1.0**, untagged. The move off `^0.13.0` is
**F.W1's** (⟨cmd⟩ `grep -n '@mkbabb' web/package.json` → `glass-ui ^8.0.0` · `keyframes.js ^6.0.0` ·
`latex-paper ^0.2.1` · `pencil-boil ^0.11.2` · `value.js ^4.0.0`) — **a fourier-tree edit, credited
to F.W1 and not to any X-W9 gate.**

**IDENTITY DISCIPLINE ×6 — ONE row, and it is one paragraph of the letter (§2.3).**
`fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8`, component-altitude INFO, quoted from the frozen
corpus: `fr-AnimationControls.md:36` *"ONE identity, component-altitude INFO, never re-booked per
component"* · `fr-BasisCanvas.md:59` *"one identity, → F.W1, not a second booking"*. The six
reproductions are **named so no seventh row is minted** — `C:B-1` · `C-2` · `C-4` · `C-27` · `F-1` ·
`PaperView C-03` (whose only increment is the live-today framing). Its banked condition (glass 4.0.0
peering `^0.10.0 || ^0.11.0`, `ELSPROBLEMS exit 1`) is **discharged at the bytes by F.W1**, claimed
by nothing here.

**The uncommitted-bump dissent TRAVELS, with its predicate re-measured**: ⟨cmd⟩
`git -C ../fourier-analysis status --porcelain` → **1** row, `?? .worktrees/` (no tracked
modification) and ⟨cmd⟩ `git log --oneline -1 -- web/package.json` → **`8fd35a9`** — **the manifest
is committed**, so *"red today is true of the working tree ONLY"* no longer holds in either
direction and the exit-0 is a committed-tree reading. **Not withdrawn — recorded with the
measurement that retires its condition** (GAB-13's own standard). ⚠ Adjacent disclosure: ⟨cmd⟩
`git -C ../keyframes.js status --porcelain` → **2** rows, both `??` letters under
`docs/tranches/V/coordination/`; **neither is a manifest**, so the pin and exit-0 readings stand.

#### f.3 — G-F10-12's own law applied to this wave: TWELVE packets, three zeros, THREE DIFFERENT CAUSES

⟨cmd⟩ `find ../glass-ui/docs -name 'valuejs-outbound-*.md' | wc -l` → **12** (×2). **The denominator
moved 11 → 12 and is published, not inherited** — the twelfth is
`BK/coordination/valuejs-outbound-2026-09-18-kfw6-bh-relay.md`.

⟨cmd⟩ `grep -rl <term> …/B*/coordination/valuejs-outbound-*.md | wc -l`, **triple-run**, all twelve:
`MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0**.

| arm | cause of the zero |
|---|---|
| `MetricPill` | **CORRECT AND MUST STAY 0** — GAB-29 is a **cure-falsifier, not a destination**; a packet naming it would be the adoption-off-the-name it exists to prevent. **No relay debt booked** |
| `IntersectionObserver` (FR-GIG-5) | **RED — AUTHORED, NOT LANDED** (below) |
| `latex-paper` | **RED, but not against this path** — a different producer, with **no inbox path at all** (E-7, re-measured by unit `.d`); a glass packet cannot discharge it |

**The cross-check that sharpens the RED, published against this sub-tranche's own interest.** Over
the wider 15 (the twelve ⊕ the three glass-addressed letters in **our** tree): `IntersectionObserver`
→ **1** and `FR-GIG-5` → **1**, both in `docs/tranches/X/coordination/fourier-to-glass-2026-09-17-nwo1-bh-relay.md`
(**NWO-1**, rowed **O-23**); `latex-paper` → **1**, in
`docs/tranches/X/coordination/value-to-glassui-2026-09-DD-fw4-relay.md` (rowed **O-32**). **The
letters exist; neither sits under the producer's inbox path** — G-F10-12's own falsifier, *"a relay
counted green because a letter was authored but not landed under the producer's path."* `X.F.W10.c`
reasonably counted `FR-GIG-5` as dispatched at NWO-1 **L-1**; **both readings are true of different
questions** (disposed in our register · receipt absent at the recipient's path), and the carriage hop
is the **X formation mail seat's** act, exactly as `O-43`'s own status cell states. ⚠ Disclosed
because it is visible: two of those filenames carry a **literal `2026-09-DD` date token** — further
evidence of authored-not-landed; **outside this unit's writable set, nothing renamed.**

**The two GREEN-BY-RECEIPT arms, READ at the bytes**: `o17-full-debt-discharge.md` (76 lines) —
⟨cmd⟩ `grep -oE 'B-[0-9]+' | sort -u` → **B-1 … B-10**, ⟨cmd⟩ `grep -oE 'U-[0-9]+' | sort -u` →
**U-1 … U-11**, `:20` reading *"B-1 (U-1 + U-5 — CONVERGENT…)"* — ten rows carrying eleven arms; and
⟨cmd⟩ `grep -rl '@source'` / `grep -rl 'FR-NP-32'` over the twelve → **exactly one file each, the
same file**, `o20-authoring-block-batch.md` (row A-1). **Receipts READ, never asserted; nothing
re-sent.**

#### f.4 — MG-ι ⚑ OWNER-MAIL FLAG, and GAB-29's terminal record

**MG-ι's value-side limb** is relayed at the letter's **§4** and rowed at **O-46** with the flag,
into the destination COHESION **§4.2 row 10** already assigns (`X.F.W10.c`'s landing — **cited, not
re-booked**). **No fourier wave adopts it**; the fourier-side remedy constraint (`colorUnit2`, never
`color2`) stays **F.W1's**. **ROUTE LAW honoured — no `parse-that → fourier` edge is minted.**

⚠ **The MEASURE-AT-OPEN finding that makes the routing more than a formality**: the limb is stated
against the **0.13.0** surface and both of its symbols have moved at 4.x. ⟨cmd⟩ `Object.keys()` over
each published subpath → **`color2` absent from all seven** (`./color` 28 · `./css` 20 · `./value` 1
· `./easing` 17 · `./math` 9 · `./transform` 3 · `./quantize` 2), **`colorUnit2` absent likewise**,
and the parser ships as **`parseCssColor`** on `./css` (⟨cmd⟩ `grep -rn 'export function
parseCssColor' src/` → `src/css/grammar.ts:290`), not `parseCSSColor`. **A seat grepping 4.x for
`color2` finds nothing and would wrongly read the defect as cured.** Re-stating the limb against the
4.x seam is a **V·π question and an owner call — NOT re-adjudicated here**, and the row is
deliberately not pre-answered (§5: *"owner acts: FLAGGED INLINE, never presumed"*).

**GAB-29 — terminal, and still a falsifier.** Re-measured at the build fourier resolves: ⟨cmd⟩
glass-ui installed → **8.0.0**; `grep -rl 'MetricPill' …/dist` → **0 files**; `grep -rl 'MetricBadge'`
→ **0 files**; `ls …/dist | grep -i metric` → `metric.d.ts` · `metric.js`, the former reading
`export * from "./components/metric/index.js";` and the export map giving `"./metric"`. **The banked
record holds**: both names are dead, the live primitive is `./metric` (K-4's `<Metric
posture="cell">`), and the row **keeps the wave off two dead primitives** rather than naming a relay
destination.

#### f.5 — The CREATE and the two rows

**Commit `d2319db1`** — ⟨cmd⟩ `git show --stat` → **2 files changed, 362 insertions(+), 0
deletions**: the letter (**360** lines, CREATE) ⊕ `INBOX.md` (**+2**). ⟨cmd⟩
`git show HEAD -- …/INBOX.md | grep -E '^[+-]\| '` → **`+| O-45 |` and `+| O-46 |` and nothing
else** — **append-only, proven**: no existing row was rewritten and **no sibling seat's bytes entered
the commit** (the unit `.e` race, guarded against rather than repeated: the id was re-read
immediately before the write, the rows were inserted after the last `^| O-44 |` line by one awk
pass, the row census verified **88 → 90** with `0` deletions, and the commit followed within the same
act).

**One row per meaning, and the reason it is two rows and not three** — O-45 rows the **letter**, its
Letter cell carrying the **G24 leg** and the **G33 leg** under those literal labels so a later
positional sweep greps either; O-46 rows **MG-ι's owner-mail flag**, which has a *different
recipient* (the owner ⊕ the V·π/parser-seam ledger) and a *different disposition* from the relay, and
which §1a row 4 enumerates as its own mail row. **Cell shapes match the Outbound table (5 cells)**
and **no status cell of this unit contains the bare word UNREAD**, which would manufacture a false
positive in every future positional sweep.

#### f.6 — Gate readings, BEFORE → AFTER (double-run; nothing argued green)

| gate | BEFORE (this record's banked open, 2026-09-19) | AFTER (this seat, 2026-09-20, double-run) | verdict |
|---|---|---|---|
| **G-F10-3** G24 drift RELAYED, never inherited, never owned | **RED — UNRUNNABLE AS WRITTEN**, cause named; *"the probe file is outside §1a — F.W10 relays the measured state, it does not repair X-W9's instrument"* | the probe reproduces its failure exactly (exit **1** ×2, leg 3 `ERR_MODULE_NOT_FOUND`); the relay obligation is **DISCHARGED** — letter §1 + O-45, with the cause, the `ESC-W9-G24-SUBSTRATE` routing, the consumer-side state and the two instrument drifts | **RED, honestly** — the gate's GREEN condition is a **drift measurement at F.W10's open**, and no instrument at this seat can take it. **X-W9.f's substitute measurement is NOT banked as this wave's green** (that would be *"claiming the gate"*, the first falsifier), and its operand is the **unpublished 4.1.0**. **None of the three falsifiers tripped**: `0.192` labelled BANKED twice · gate not claimed · every fourier-tree edit credited to F.W1/ESC-4 |
| **G-F10-4** G33 packets sent + post-window pin truth | **SPLIT** — the pin leg GREEN before cure (F.W1's atomic transaction); *"the five packets + five INBOX rows leg is untested and stands"* | the untested leg is now **measured and GREEN — and credited to `X-W9.i`**: packets **5** ×2 · rows **O-34..O-38** = **5** ×2 · keyframes' exact pin `"4.0.0"` at `:71`. `npm ls` **exit 0 in BOTH** trees, ×2 each — published **as PRE-WINDOW** (registry `latest` **4.0.0** vs worktree **4.1.0**, untagged) | **SPLIT, and it stays split.** Three legs GREEN-BY-PREDECESSOR (X-W9.i / F.W1); the **post-window leg is UNREADABLE until the 4.1.0 window opens**, and calling today's exit-0 the post-window reading is the gate's own named falsifier. Identity held to **ONE** row; the dissent **travels** |
| **G-F10-12** every declared relay arm has a grep-able receipt | **SPLIT, unchanged in shape**; denominator moved 11 → **12**; `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0** | the sweep re-run **triple** at the moved denominator reproduces **0 · 0 · 0**; the two GREEN arms **READ** (o17's `B-1..B-10` over `U-1..U-11`; o20's single-file `@source`/`FR-NP-32`); and the three zeros are published **with three different causes**, the RED sharpened to **AUTHORED-NOT-LANDED** by the wider-15 cross-check | **SPLIT — not averaged, not argued green.** This unit's act is the **law applied to this wave**: a receipt READ, never asserted; the denominator published, not inherited; and a finding filed against our own register's reading rather than smoothed |

#### f.7 — Residuals, disclosed rather than silent

1. **The G24 instrument stays broken, and this unit did not touch it.**
   `probes/fourier-value-import-drift.mjs` is outside §1a; its leg-3 oracle died with F.W1's uplift
   and its leg-1 site list is a stale literal. **Routed, not repaired** — `ESC-W9-G24-SUBSTRATE`
   (COHESION §0ac) already owns the cure at `X-W9.f`, and a fallback, skip or rewritten import here
   would have been the masking-fallback defect the standing law names.
2. **⚑ OWNER-MAIL — MG-ι's value-side limb cannot be actioned as written** (f.4): `color2` and
   `colorUnit2` are absent from every 4.x published subpath and the parser ships as `parseCssColor`.
   **Flagged at O-46, re-adjudicated by nobody here.**
3. **G-F10-12's F-side arms stay RED**, and the RED is now *sharper* than the spec's born state:
   `IntersectionObserver`/`FR-GIG-5` and `latex-paper` are **authored in our tree and not landed
   under the producer's path** (O-23 · O-32, two filenames still carrying `2026-09-DD`). **The
   carriage hop is the X formation mail seat's act** — sibling trees are READ-ONLY to every wave
   seat — so this unit **rows the finding and moves no byte** toward it.
4. **A reading filed against our own register.** `X.F.W10.c` counted `FR-GIG-5` as *dispatched at
   NWO-1 L-1*; under G-F10-12's law the arm has **no receipt at the recipient's path**. Both are
   true of different questions and **both are published**; nothing of `.c`'s is edited, reverted or
   re-counted.
5. **The 4.1.0 cure is unpublished.** X-W9.f's zero-drift table is measured against the worktree
   build; fourier resolves **4.0.0**, where the eight arms drift. Recorded so no seat reads *"G24 is
   fixed"* off an evidence file whose operand the registry does not serve.
6. **ESCALATIONS: none.** Every act this unit was given was possible at the bytes as specified: the
   relay is a relay, the unrunnable instrument was **relayed as unrunnable** rather than substituted,
   and no cure was improvised in place of the specified one.

#### f.8 — Self-count and bounds

**SELF-COUNT LAW.** Read from the **settled** files, from outside them, double-run: the letter is
⟨cmd⟩ `wc -l` → **360 · 360** lines with line 1 `SERVED MODEL: claude-opus-5[1m]`; `INBOX.md` moved
⟨cmd⟩ `grep -cE '^\| [IO]-[0-9]+[a-z]? \|'` → **88 → 90**, **two rows this unit's** (`O-45` · `O-46`);
the packet denominator is **12**, the term sweep **0 · 0 · 0**, the G33 packet/row legs **5 · 5**, and
every `npm ls` exit **0**, each read twice.

**BOUNDS.** Writes: **exactly three paths**, all inside this unit's writable set — the letter
(CREATE), `docs/tranches/V/coordination/INBOX.md` (append-only, +2), and this record.
**`fourier-analysis/**` READ-ONLY throughout** (`git`, `grep`, `ls`, `sed`, `node -e` as readers
only), porcelain **1 · 1** at open and close; **glass-ui READ-ONLY always**, porcelain **0 · 0**;
**keyframes.js READ-ONLY**; `COHESION.md` **not opened for write** (its §4.2 row 10 is cited, not
edited); `LEDGER.md` not touched by this unit; `scripts/dev/dev.sh` never opened; the X-W9 probe
**not repaired, not patched, not wrapped**. **Pathspec commits only** — `d2319db1` (the letter ⊕ its
two rows, one meaning) and this receipt's own. No `git add -A`, no `-u`, no `commit -a`, no stash, no
reset, no force-push, no cron.

### X.F.W10.g

⟨**THE CLOSE.** Track C · X·F · the LAST F wave's close seat · `claude-opus-5[1m]` · 2026-09-20.
**This seat cured nothing, authored no artefact byte of `a`–`f`, minted no letter and moved no gate
verdict it did not measure at its own clock.** Its acts are four: re-run all twelve gates
double-run and read each honestly; stamp the F-side of COHESION §3.1–§3.4 and record the §5
boundary row; audit the wave's whole commit roster for bounds; set the ledger row and its event
line. **VERIFIED stays NO** — §3.5's declared-Fable pass (`X.F.W10.h`) is owed.⟩

#### g.0 — Crash-recovery, mail and bounds, before any write

⟨cmd⟩ `git status --porcelain` (value.js, this seat, at open) → **23** rows. **Intersection with
this unit's writable set — `COHESION.md` · `LEDGER.md` · `INBOX.md` · this record — is ∅**, so no
killed predecessor's partial work exists on any path this seat may write. The dirty
`docs/tranches/X/execution/A/X-W5.md` and `docs/tranches/V/reformation/CARRY-LEDGER.md` are **Track
A's seats' paths**, read for nothing and never staged; the `demo/**` + `e2e/**` rows and the two
untracked `W6-evidence/**` trees are Track A's; `scripts/dev/dev.sh` is the standing unowned dirty
row and was **never opened**. ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` → **1** row,
`?? .worktrees/`; ⟨cmd⟩ `git -C ../glass-ui status --porcelain` → **0**. Both sibling trees stayed
READ-ONLY for the whole seat (`git`, `grep`, `find`, `ls`, `sed`, `node -e` as readers only).

**`LEDGER.md` was re-read immediately before its edit** (⟨cmd⟩ `sed -n '72p'`), because three other
tracks edit that file concurrently; the edit is a **minimal in-place replacement of F.W10's own row
cells** plus an **appended** event line — the file is never rewritten.

#### g.1 — The twelve gates, re-run at this seat's own clock, every reading double-run

**5 GREEN · 5 SPLIT · 2 honest-RED · 0 argued green.** The spec's §3 posture binds every cell
below: *no gate is a proof-farm script*, a split is published as a split rather than averaged into
a false colour, and **every green-before-cure leg is stamped VERIFIED-GREEN-BY-PREDECESSOR with the
predecessor named** — never banked as this wave's.

| gate | THIS SEAT, 2026-09-20 (double-run) | reading |
|---|---|---|
| **G-F10-1** asks reconciled in BOTH ledgers | fourier's live table: ⟨cmd⟩ `sed -n '109,125p' docs/constellation/ADOPTION-ASKS.md \| grep -c 'OPEN'` → **12** ×2 against ⟨cmd⟩ `… \| grep -c .` → **17** rows — E-1's corrected denominator reproduces. Value side: ⟨cmd⟩ `grep -ci` on `INBOX.md` → `Ask 3` **2** · `Ask 5` **2** · `inv-22` **2** · `cascade-vjs` **2** · `ADOPTION-ASKS` **5**, and ⟨cmd⟩ `grep -cE '^\| I-3[6-9] \|'` → **4** rows with disposition verbs. Far end: ⟨cmd⟩ `grep -ci` on `docs/tranches/F/coordination/INBOX.md` → `ADOPTION-ASKS` **0** · `cascade-vjs` **0** · `Ask 3` **0** | **SPLIT.** The value-side leg is GREEN **by `X.F.W10.e`'s act**; the *"reply landed in fourier's ledger"* leg is **RED and is not this side's to turn** — §5 excludes *"Every fourier-tree write … their sub-session; F.W10 asks and verifies receipt"*, and **a verb naming a peer actor's action as an acceptance condition is this gate's own first falsifier**. Not argued green |
| **G-F10-2** inbound-mail parity | fourier `CLAUDE.md` **EXISTS**; ⟨cmd⟩ `find docs -name INBOX.md \| wc -l` → **1**; ⟨cmd⟩ `git ls-files docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md \| wc -l` → **1** (TRACKED). FN legs: ⟨cmd⟩ `grep -c 'FN-' docs/tranches/F/coordination/INBOX.md` → **0** | **SPLIT.** The three parity legs are **VERIFIED-GREEN-BY-PREDECESSOR — `X.F.W0` `b`** (COHESION §0k.1), claimed by nothing here. The **FN-1..FN-7 fourier-side verb leg stays RED**; our answers went out at O-29 and the rider is answerable in the order it asks for |
| **G-F10-3** G24 drift RELAYED, never inherited, never owned | ⟨cmd⟩ `node docs/tranches/V/megatranche/audit/probes/fourier-value-import-drift.mjs` → **EXIT 1, twice**, `ERR_MODULE_NOT_FOUND` resolving `…/@mkbabb/value.js/dist/value.js` — the 0.13-era layout, stale against the 4.x export map | **honest-RED, with the failing case named.** The gate's GREEN condition is a **drift measurement at F.W10's open** and no instrument at this seat can take it. **Relayed, never repaired, never wrapped, never skipped** — the cure is `ESC-W9-G24-SUBSTRATE` (COHESION §0ac) at `X-W9.f`. None of the three falsifiers tripped: `0.192` is never quoted as current, the gate is X-W9's and is not claimed, and no fourier-tree edit is credited as an X-W9 gate |
| **G-F10-4** G33 packets + post-window pin truth | fourier `web/package.json` → `@mkbabb/value.js ^4.0.0` · `@mkbabb/glass-ui ^8.0.0` · `@mkbabb/keyframes.js ^6.0.0`; keyframes pins ⟨cmd⟩ `grep -n '"@mkbabb/value.js"' ../keyframes.js/package.json` → `:71` **`"4.0.0"`** exactly. ⟨cmd⟩ `npm ls @mkbabb/value.js` → **exit 0** in fourier (`4.0.0`, keyframes 6.0.0 deduped) **and exit 0** in keyframes.js. Packets/rows: ⟨cmd⟩ `grep -cE '^\| O-3[4-8] \|' INBOX.md` → **5** | **SPLIT, and it stays split.** The pin leg is **VERIFIED-GREEN-BY-PREDECESSOR — `F.W1`'s atomic transaction**; the five-packet/five-row leg is **VERIFIED-GREEN-BY-PREDECESSOR — `X-W9.i`**. The **post-window leg is UNREADABLE** until the 4.1.0 window opens, and **calling today's exit-0 the post-window reading is the gate's own named falsifier** — published **PRE-WINDOW**, as `f` published it |
| **G-F10-5** the LATEX-PAPER carry has a receiving surface | ⟨cmd⟩ `ls -la docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` → **EXISTS**, 36,954 bytes; `grep -c 'latex-paper' INBOX.md` → **5** | **GREEN**, on the gate's own acceptance conjuncts as `X.F.W10.d` measured them: the register exists · every §2.6 carry rowed (15 ↔ 15, 20 ids, none re-booked) · PAW-1's F.W1 question answered in the ledger · a named delivery point · zero producer-src anchors · the K-9 `:root` snippet absent |
| **G-F10-6** ≡ **G-F9-21** the re-cut is complete, Δ=∅ both directions | Splice, ×2: ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(… F-W10.md)` → **0 bytes**, twice; ⟨cmd⟩ `diff <(awk '/^### §2\.2/,/^### §2\.3/' F-W9.md) <(… F-W10.md)` → **exactly 3 hunks**, twice (E-10's scope holds). Corpus, ×2 in `registry/adjudicated/`: ⟨cmd⟩ `grep -o 'F\.W9/W10' fr-*.md \| wc -l` → **54** · `grep -l … \| wc -l` → **25** · ⟨cmd⟩ `grep -o '\bF\.W10\b' fr-*.md \| wc -l` → **0** · `ls fr-*.md \| wc -l` → **66** | **SPLIT.** GREEN on **both** splice legs; **RED, unchanged, on the corpus leg** — and the gate's own falsifier (*"ANY edit to `fr-*.md`"*) **FORBIDS curing it**, so the RED is structural, not neglect. The spec binds this seat twice over — *"the gate STAYS RED on its corpus leg"* and *"a later seat must not read the green leg as the gate"* — and **this close does not stamp it green.** The five pending paired edits (AA-44 `9`→`8` ⊕ §5's four canonical divergences) remain **re-disclosed and NOT applied**: a unilateral landing inside the shared §2.2 splice bytes is exactly what breaks Δ=∅ |
| **G-F10-7** the NO-WAVE-OWNER register drained on the F side | Measured **from outside the file**, twice: ⟨cmd⟩ `awk '/^### §4\.2/,/^## §4a/' COHESION.md \| grep -cE '^\| *[0-9]+ *\|'` → **89 · 89**; distinct ids ⟨cmd⟩ `… \| grep -oE '^\| *[0-9]+ *\|' \| tr -dc '0-9\n' \| sort -n \| uniq \| wc -l` → **89**, min **1** max **89** (**no gap, no repeat**); rows carrying no terminal verb → **0 · 0**. **FR-NP-32 heads the table** at row 1; `fr-App C-12` present. Escape test against the **frozen census of record** — ⟨cmd⟩ `shasum -a 256 fourier/conformance/CENSUS-CANONICAL.md \| cut -c1-12` → **`f44362757458`**, the digest §2.5b pins, **unmoved** — its §3 TOTALS give `NWO (packet)` **32** · `NWO→SS-3` **27** · `NWO→SS-13` **3** · `NWO→SS-5` **3** = **65**, and ⟨cmd⟩ `grep -c '^### F\.W10' CENSUS-CANONICAL.md` → **0**: F.W10's canonical roster is **ZERO by the canonical's own enumeration** | **GREEN.** `X.F.W10.c`'s escape test returns **65 booked-or-cited · 0 escaped**, record-qualified, and this seat re-derived its **operand** rather than its headline — the four roster figures come from the canonical's **own §3 TOTALS**, not from a self-count of any spec. Against the gate's falsifiers: roster **derived from the 66**, never a check file · no subset called zero-left-open · no class verb standing where the detector grades STRICT · the partition closes (`31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33 = 165`) |
| **G-F10-8** the Codex residue is not inherited; the owner rulings answered | ⟨cmd⟩ `grep -rl 'Zero UNVERIFIED credit' docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` → the addendum (landed `cd4df502`), carrying the **corrected** wording. ⟨cmd⟩ `ls /tmp/fourier-r4-files.sha256` → **No such file or directory** (R4-12 moot). The `~/.codex/worktrees/d0be/fourier-analysis` worktree → **still REGISTERED**, its verb terminal (**RETAIN-AS-RECORD**). ⟨cmd⟩ `ls …/coordination/FOURIER-R*.md \| wc -l` → **9** | **GREEN.** Charter line landed in its corrected wording; five terminal verbs landed at `X.F.W10.b` with falsifiers; **none of R4-12's rows consumed**. `OG-F1` (**FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE**) and `OG-F2 / OG-V2` (**CODEX-ERA-SPECIFIC**) are **discharged BY RULING** at COHESION §0j.D and cited by id — **re-ruled by nobody here**, which is what §2.3's *"F.W0 rules; F.W10 verifies"* requires |
| **G-F10-9** NEXT-COMMUNIQUE ledger truth | ⟨cmd⟩ `ls …/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` → **EXISTS** (`077d3e5f`); ⟨cmd⟩ `git diff --stat -- …/INTAKE-ADJUDICATION-2026-08-03.md` → **empty** — the dated bytes are untouched, which is E-3's whole shape. Register disjointness: glass arms → SS-6 (fourteen cited to their dispatched packets, three accreted at COHESION §4a as `F10-1`/`F10-2`/`F10-3` by `X.F.W10.c`); latex arms → `LATEX-PAPER-RELAY.md` (`X.F.W10.d`), which now **exists** | **GREEN.** At `b` this gate was honest-SPLIT because its **register-existence** leg was unit `d`'s and unlanded; `d` landed it, so the last open conjunct closes here. Its falsifier — *"any glass row cured consumer-side"* — is **not tripped**: nothing producer-owned was cured in a consumer tree by any unit of this wave |
| **G-F10-10** close-gate a11y honesty — the FR-TT-1 promotion lock | **The legs FIRST, because the gate forbids reading the run before them.** ⟨cmd⟩ `grep -c 'aria-label'` at this clock, twice: `EditorControlsDock.vue` **13** · `CanvasControlsDock.vue` **8** · `FunctionInput.vue` **3** — L-4's citable table is *Editor ×10 · Canvas ×6 · Wand2 ×1 = 17*, and every `<Tooltip>`-wrapped icon trigger carries an explicit **NAME**, not the shim's only-while-open `aria-describedby`. Coverage witnesses, all moved off the born-RED state: ⟨cmd⟩ `grep -rln "admin" web/e2e/ \| wc -l` → **5** · `grep -rl AxeBuilder web/e2e/ \| wc -l` → **4** · `/paper` named in **8** specs · `/equation` in **8** · `ls web/e2e/*.spec.ts \| wc -l` → **18** (was 8) · `grep -c 'checkA11y'` → `visualization-ux.spec.ts` **12**, `gallery-admin-a11y.spec.ts` **5** | **honest-RED, with the failing case named — and NO WAIVER, because none is lawful.** The gate offers two roads: the close axe run over `/paper` **+** `/equation` **+** the admin tab at zero serious/critical, **OR** the 17 naming legs recorded as a NAMED, DATED WAIVER. **The legs are MEASURED CURED** (F.W4's landing, re-read above), so the waiver road is **closed** — a waiver of a cured leg would be a record of something that is not true — and **the run is the whole question**. The run of record is **F.W9 `b.9`**'s, bounded per §5.2 and cited rather than re-spent: `/equation` **GREEN** · `/paper` **GREEN** · `/morph` **GREEN** (beside) · **the admin tab RED, 4 of 4: `[serious] aria-hidden-focus` ×2, targets `span[aria-hidden="true"]:nth-child(1)` and `:nth-child(3)`**. **The RED is a PRODUCER row** — the two nodes sit inside **glass-ui 8.0.0's `Metric` tile** (`posture="cell"`, the ruled GAB-2/K-4 seat), banked at F.W9 as `E-F9b-2` → **SS-6 relay**. It **rides the letter and never becomes a frontend hack to turn a gate green** (§5: *"producer rows NEVER become frontend hacks"*). **F.W9 EXECUTES the coverage; F.W10 owns the terminal record** (§4b's F.W9 row), and this is that record: **RED, not waived, not argued, not re-spent** |
| **G-F10-11** F.W0 substrate is terminal before the close | At the **8.0.0** pin (§0i.3): ⟨cmd⟩ `postcss.parse(dist/styles/index.css)` → **OK, 40 top-level nodes**; ⟨cmd⟩ `grep -c '@source'` → **1**. GAB-13: ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain \| wc -l` → **1** (`?? .worktrees/`), the WT diff LANDED. F8-REACH: ⟨cmd⟩ `find web/src -name 'InfoCard.vue' -o -name 'CanvasOverlayButton.vue' \| wc -l` → **0**, both files GONE, **no HOLD surviving** | **GREEN — and stamped VERIFIED-GREEN-BY-PREDECESSOR on every limb, with the predecessor named.** The parsing dist is **F.W1**'s tri-package uplift; the landed worktree diff is **F.W0**'s under §0j.D's `OG-F1` FREEZE-WITH-ADOPTION; both deletions are **F.W0**'s `5842377` under §0j.D's `G-10` DELETE-in-one-breath. **F.W10's claim is the VERIFICATION and nothing more**, which is exactly what §2.3 defines it as |
| **G-F10-12** every declared relay arm has a grep-able receipt | ⟨cmd⟩ `find ../glass-ui/docs -name 'valuejs-outbound-*.md' \| wc -l` → **12** (the denominator moved 11 → 12 and is **published, not inherited**). Term sweep across all twelve, twice: `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0**. The two GREEN arms **READ**, not asserted: `o17`'s `B-1..B-10` over `U-1..U-11`, and `o20`'s ⟨cmd⟩ `grep -c '@source'` → **1** / `grep -c 'FR-NP-32'` → **1** | **SPLIT, born honestly and closing honestly.** Two arms GREEN-BY-VERIFIED-RECEIPT; **three arms RED**, and `X.F.W10.f` sharpened the RED rather than smoothing it: the `IntersectionObserver`/`FR-GIG-5` and `latex-paper` arms are **AUTHORED IN OUR TREE AND NOT LANDED UNDER THE PRODUCER'S PATH** (`O-23` · `O-32`, filenames still carrying `2026-09-DD`). **The carriage hop is the X formation mail seat's act** — sibling trees are READ-ONLY to every wave seat — so this close **rows the finding and moves no byte toward it** |

**A spelling-agnostic note, published because the single-spelling probe would have manufactured a
false RED.** §3.4's reciprocity check over the five `REQUIRED` far ends read **`F.W9` 86 · `F.W4`
0 · `F.W0` 5 · `F.W1` 7 · `F.W5` 0** under ⟨cmd⟩ `grep -o 'F\.W10'` — two zeros. Re-run
spelling-agnostically per §0f's program law, twice: ⟨cmd⟩ `grep -oE 'F[.-]W10' <spec> \| wc -l` →
**131 · 2 · 14 · 17 · 18** — **every far end names this wave.** The dot-only probe missed the
`F-W10` spelling the specs use for file-path citations. **The figure that would have flattered a
finding is the one this seat re-derived**, and both readings are given.

#### g.2 — The F-side stamp of COHESION §3.1–§3.4

**§2 and §3 of COHESION are root-authored and are QUOTED here, never rewritten** (§1a row 3). This
seat's carve is exactly two cells: the **§0 X·F status cell** and a **§5 boundary row**. The stamp
below is the *reading* those two cells record.

| criterion (quoted) | F-side measurement | verdict |
|---|---|---|
| **§3.1** *"Every intake-adjudication CARRY row lands in exactly one sub-tranche wave (set-difference ∅ both directions)"* | Measured **against `CENSUS-CANONICAL.md`, the stamped census of record** — never a carry, never a check file (R4-3 · R4-10 · R2-6b). ⟨cmd⟩ `shasum -a 256 … \| cut -c1-12` → **`f44362757458`** ×2, **FROZEN and unmoved** seven rounds running (§0f). Its §2 homes every id at exactly one wave (`F.W0` 55 · `F.W1` 330 · `F.W2` 23 · `F.W3` 919 · `F.W4` 1007 · `F.W5` 27 · `F.W5-W8` 89 · `F.W9` 16 · `GLASS-RELAY` 22 · the four NWO buckets 65), and ⟨cmd⟩ `grep -c '^### F\.W10'` → **0**: **F.W10 homes no row of its own**, so its §2.2 re-cut table is citation, never a rival home | **STAMPED.** The two id-bearing cells that were not citations — `FR-TT-1` (canonical **F.W4**) and `PAW-50` (canonical **F.W9**) — are the spec's **PENDING PAIRED EDITS inside the SHARED §2.2 SPLICE BYTES**, re-disclosed by `a` and **deliberately NOT applied**; §5 names them, their ruling and their holder, which is a disposition rather than a deferral |
| **§3.2** *"Every census wave-sketch id (KF.W0–W10, F.W0–W10) maps to a full spec or a terminal kill with rationale"* | ⟨cmd⟩ `ls docs/tranches/X/fourier/waves/F-W*.md \| wc -l` → **11** — `F-W0` · `F-W1` · `F-W2` · `F-W3` · `F-W4` · `F-W5` · `F-W6` · `F-W7` · `F-W8` · `F-W9` · `F-W10`, a full spec for every sketched id, **zero kills needed and zero silent drops**. Every one of the eleven reads CLOSED in this ledger's Track C table | **STAMPED** |
| **§3.3** *"Every NO-WAVE-OWNER row reaches a terminal home … before X-whole stamps SPECIFIED"* — **the deadline clause, and F.W10 is the LAST F wave** | Discharged **by `X.F.W10.c`'s PUBLISHED DERIVATION over the 66**, not by this row's assertion: the partition of all **165** NWO lines closes (`31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33`), the strict roster is **69 dispositions / 36 records**, and the landed block carries **89 rows · 89 distinct ids 1–89 · 89 terminal verbs · 0 without one**, re-measured from outside the file **twice** at this seat. The escape test against the pinned canonical returns **65 booked-or-cited · 0 escaped**, record-qualified | **STAMPED.** `fr-PathPreview`'s three escapes — the records that proved a deadline clause at the last wave has no successor to catch what it misses — are homed with per-id verbs |
| **§3.4** *"Cross-repo edges reciprocal (both files); zero cycles"* | The five `REQUIRED` far ends each name this wave, measured **spelling-agnostically** and twice: ⟨cmd⟩ `grep -oE 'F[.-]W10' <spec> \| wc -l` → `F-W9` **131** · `F-W4` **2** · `F-W0` **14** · `F-W1` **17** · `F-W5` **18**. The F.W5 reciprocal is verified at the *clause*, not the count — §4b quotes F-W5 §2 clause **A1** (*"BOOKS NOTHING — cites only: `AA-45` → F-W10 §2.5"*) and its §7 edge row. **ROUTE LAW holds**: no `parse-that → fourier` edge is minted anywhere in this wave, so no cycle is introduced | **STAMPED**, with the dot-only probe's two false zeros published beside the true reading |
| **§3.5** *"Each sub-tranche closes its own L-20 conformance loop … with a declared-Fable final pass"* | **NOT STAMPED — OWED.** The spec's own Close act names it, the unit plan seats it as `X.F.W10.h` (**fable**, VERIFY-ONLY), and **this seat is not it** | **OWED.** This is why `VERIFIED` stays **NO** and the wave is **CLOSED, not ACCEPTED** |

**The stamp is bounded by its own honesty.** §3.1–§3.4 are stamped over a wave whose gate reading
is **5 GREEN · 5 SPLIT · 2 honest-RED**, and the two REDs are named in the same §5 row that carries
the stamp. A stamp that hid them would be the *"terminal disposition stamped while an owner ruling
is outstanding"* base L-18 lists — so the boundary row states the stamp **and** the residue in one
breath, and **L-18's two quartet gestalt passes remain owed after `h`.**

#### g.3 — S-16, restated before close

**`22 ⊕ 4 = 26`, and the instruction stays withdrawn.** The O-20 packet's `A-1..A-14` ⊕ `B-1..B-7`
⊕ `C-1` are **22 id-rows**; its `## §D Negative space` carries **4** numbered entries; the packet's
own close line reads *"one letter, twenty-six entries, zero re-sends."* **They are two denominators
of ONE letter, both true — not a figure and its error.** Accordingly:

1. **COHESION §4a's `26` is the packet's own count and it STANDS.** The spec's round-4 instruction
   *"COHESION §4a's 26 is named superseded here and is corrected at its own site"* was **STRUCK as
   an instruction** by the 2026-08-30 S-16 addenda at F-W10 §4b and §5, and it **stays withdrawn**.
   Executed as written it would have put a **false correction into the constellation's only shared
   register**.
2. **COHESION §4a is edited by nobody on this account.** ⟨cmd⟩ `git diff --stat` over this seat's
   commits touches §0 and §5 of `COHESION.md` and **nothing in §4 or §4a** — the §1a #3 MODIFY-carve
   for that figure is **unspent**, and no begin-word is consumed by it.
3. **What is kept whole** is the good work beside the withdrawn verb: the `f6e04c86ddb8` hash pin,
   the row-by-row enumeration and the `^\|`-total cross-check (**28** = 22 rows + 3 headers + 3
   separators). *The defect was never the arithmetic; it was the verb.*

#### g.4 — The commit-roster bounds audit — 18 commits, and `dev.sh` in zero of them

The wave's whole roster, read by ⟨cmd⟩ `git show --name-only --format= <sha>` per commit:

| unit | commits | paths touched |
|---|---|---|
| open | `2ace946e` | this record · `LEDGER.md` |
| `a` | `cd4df502` · `cee59a4f` | `formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` · this record |
| `b` | `077d3e5f` · `13bfec7c` · `3616a2d8` | `codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` · this record |
| `c` | `269852ab` · `5f557674` · `4880dc04` · `2b9ab798` | `COHESION.md` (§4 · §4a) · this record |
| `d` | `4278b798` · `f294c12c` | `X/fourier/LATEX-PAPER-RELAY.md` · this record |
| `e` | `a8fa79d2` · `66e35333` · `80924add` | `coordination/FOURIER-ADOPTION-ASKS-RECONCILIATION-2026-08-25.md` · `INBOX.md` · this record |
| `f` | `d2319db1` · `806e0196` · `e00e3629` | `coordination/FOURIER-EVIDENCE-RELAY-G24-G33-2026-08-25.md` · `INBOX.md` · this record |
| `g` | `ed5b8def` · `8990c8bd` · `5d8e67e8` (+ this receipt) | `COHESION.md` (§0 · §5) · `LEDGER.md` · `INBOX.md` · this record |

**Findings: none.** ⟨cmd⟩ per commit, `git show --name-only --format= <sha> \| grep -c 'dev\.sh'` →
**0 for all 18** (`sort \| uniq -c` → `18 0`) ⟨**and the denominator is CORRECTED to a pairing at
g.8**: 18 is the roster *before* this seat's own writes; over the complete committed roster the
same probe returns `21 0`. Both are published, the invariant is the claim⟩. **Every path lies inside the wave's §1a writable
set**; no commit reaches `demo/**`, `e2e/**`, `src/**`, a sibling tree, or another track's
execution record. **Every commit carries its own pathspec on the `commit` itself** — the guard the
X-W0 contamination booked — and **no `git add -A`, no `-u`, no `commit -a`, no stash, no reset, no
force-push, no cron** appears anywhere in the wave. **Zero writes landed in `fourier-analysis`,
`glass-ui`, `keyframes.js` or `latex-paper` by any unit of this wave**, which is the same fact
G-F10-12's three REDs report from the other side: the arms are authored here and **must be carried**
by the formation mail seat, because a wave seat may not write them into the producer's tree.

#### g.5 — The four cells this seat wrote

1. **COHESION §0, the X·F status cell — moved off `FORMING`** (`ed5b8def`). It now reads
   **SPECIFIED** 2026-08-30 (the fifteen-pass L-20 loop, §0f; canonical FROZEN `f44362757458`) ·
   **EXECUTED 2026-09-20** with all eleven waves CLOSED and the F-side of §3.1–§3.4 stamped, and it
   states in the same cell that **§3.5's declared-Fable pass is OWED** and **VERIFIED stays NO**.
   *A status cell that said only the first half would be the deferral-wearing-a-verb this wave
   exists to abolish.*
2. **COHESION §5, the 2026-09-20 boundary row** (same commit) — the twelve gate readings with every
   green-before-cure leg credited to its predecessor by name, the two honest-REDs with their failing
   case, the S-16 restatement, the bounds figure and the `VERIFIED NO`.
3. **`LEDGER.md`, F.W10's row** (`8990c8bd`) — **minimal in-place replacement of this wave's own
   row cells**, the file re-read immediately before the edit because three other tracks hold it
   concurrently; the status cell names the two honest-REDs and the five SPLITs **in the cell
   itself**, so no reader can take `CLOSED` for `ACCEPTED`.
4. **`LEDGER.md`'s event line** (same commit) — **appended**, never inserted.
5. **`INBOX.md`, the E13 close-sweep line** (`5d8e67e8`) — **one paragraph, +2 lines**; no row
   rewritten, no row re-classified, no `I-n` minted.

#### g.6 — E13 at close

⟨cmd⟩ `find <the four paths> -name '*2026-09-19*' -o -name '*2026-09-20*'` → **no output**: nothing
has arrived on any path since this wave's open sweep. Newest per path, re-read at this clock: (1)
the **2026-09-18** 4.1.0 quartet ⊕ the `kfw7-bh-relay` pair; (2) `glass-outbound-2026-09-18-valuejs-o26-reply.md`,
**`BK/` still the newest glass tranche dir**; (3) `VALUEJS-INBOUND-2026-09-17-o8-o11-amendment-addendum.md`
(our own outbound); (4) unmoved at 2026-07-27. **0 unrowed.**

**Classification read BY POSITION, never by a bare `grep -c UNREAD`.** ⟨cmd⟩
`awk -F'|' '/^\| *[IO]-[0-9]+[a-z]? *\|/ { if ($6 ~ /UNREAD/) print $2 }' INBOX.md` → the **same
four** rows as at this wave's open, twice: `O-20` · `I-31` · `I-32` · `O-39` — each describing a
**prior or already-answered** state in its own Status cell. Row total ⟨cmd⟩ `awk … END{print n}` →
**90** (it was 88 at `f`'s open; `f` added `O-45`/`O-46`, `e` added five, and this seat added
**none**). The bare `grep -c 'UNREAD'` answers **89**, which is this file's header vocabulary and
prose — **not a row census**, and published beside the positional reading so the two can never be
quoted as one another.

**E13 verdict at close: 0 unrowed · 0 rows carrying a live UNREAD status in F.W10's scope.**
**Outbound obligation NEGATIVE** — this seat sent no letter, wrote no producer byte, and opened no
sibling tree for write.

#### g.7 — Residuals, disclosed rather than silent

1. **`X.F.W10.h` IS OWED, AND IT IS THE WAVE'S LARGEST RESIDUAL.** §3.5 and the spec's Close act
   both name a **declared-Fable** final pass, and the unit plan seats it. It did not run at this
   sitting. **The consequence is stated in every cell this seat wrote**: the sub-tranche is
   **CLOSED, not ACCEPTED**; **VERIFIED stays NO**; and L-18's **two quartet gestalt passes**
   remain owed after `h`. *Nothing here stamps §3.5, and nothing here reads `h`'s absence as
   relief.*
2. **The G24 instrument stays broken and this seat did not touch it.**
   `probes/fourier-value-import-drift.mjs` is outside every F.W10 unit's writable set; its leg-3
   oracle died with F.W1's uplift. **Relayed as unrunnable, never substituted** — a fallback, a
   `try/catch`, a skip or a rewritten import here would each have been the masking-fallback defect
   the standing law names. Owner: `ESC-W9-G24-SUBSTRATE` at `X-W9.f` (COHESION §0ac).
3. **The admin-axe producer arm is AUTHORED-NOT-LANDED — and that is a live finding, not a
   closed one.** `[serious] aria-hidden-focus` ×2 on glass-ui 8.0.0's `Metric` tile holds
   `G-F10-10` honest-RED. It is rowed twice in `INBOX.md` and authored in
   `docs/tranches/X/coordination/value-to-glassui-2026-09-DD-fw4-relay.md` — **whose filename still
   carries `2026-09-DD`** — and ⟨cmd⟩ `grep -c 'aria-hidden-focus'` over `COHESION.md` → **0**: the
   arm is **not among `X.F.W10.c`'s three §4a accretions** (`F10-1` · `F10-2` · `F10-3`). **This
   seat's carve does not reach §4a**, so it **files the finding rather than curing it out of
   bounds.** Owner: the X formation mail seat (carriage) ⊕ SS-6 (the register row).
4. **`G-F10-12`'s three arms stay RED**, sharper than born: `MetricPill` **0**,
   `IntersectionObserver` **0**, `latex-paper` **0** across denominator **12**, with `O-23`/`O-32`
   authored here and not landed under the producer's path. Same carriage owner as (3).
5. **`G-F10-6`'s corpus leg is RED BY CONSTRUCTION and must stay so.** The gate's own falsifier
   forbids any edit to `fr-*.md`, so **54 / 25-of-66 / bare `F.W10` 0** cannot be cured by any seat
   without falsifying the gate. The **five pending paired edits** (AA-44 `9`→`8` ⊕ §5's four
   canonical divergences) are re-disclosed and **NOT applied**; R4-10 assigns the F.W9 roster
   re-basing to **F-W9's** duty.
6. **The `~/.codex/worktrees/d0be/fourier-analysis` worktree stays REGISTERED**, with the terminal
   verb **RETAIN-AS-RECORD** booked at `X.F.W10.b` and its falsifier. Not deleted, not consumed.
7. **`G-F10-1`'s and `G-F10-2`'s far-end legs are not this side's to turn**, and no verb anywhere
   in this wave names a peer actor's action as an acceptance condition — the first falsifier of
   `G-F10-1`, honoured rather than quoted.
8. **`G-F10-4`'s post-window leg is UNREADABLE today.** The registry serves **4.0.0**; the cure
   lives in the unpublished **4.1.0**. Today's `npm ls` exit-0 is published **PRE-WINDOW** so no
   later seat reads it as the post-window measurement.
9. **ESCALATIONS: none.** Every act this unit was given was possible at the bytes as specified. The
   two honest-REDs are readings, not impossibilities: `G-F10-3`'s cure is owned elsewhere by
   ruling, and `G-F10-10`'s cure is a producer row that rides SS-6. **Neither was substituted,
   waived, argued green, or masked.**

#### g.8 — Self-count and bounds

**SELF-COUNT LAW — and two draft figures corrected by their own probe, published as corrections
rather than replaced.** Read from the **settled** bytes, from **outside** the files, double-run and
then re-derived against git so the before-image is measured and not remembered: ⟨cmd⟩
`git show <sha>~1:<path> \| wc -l` / `git show <sha>:<path> \| wc -l` →

| file | before | after | Δ |
|---|---|---|---|
| `docs/tranches/X/COHESION.md` | **2272** | **2310** | **+38** (one replaced status cell ⊕ one §5 boundary row) |
| `docs/tranches/X/execution/LEDGER.md` | **450** | **451** | **+1** (the appended event line; the F.W10 row is a *replacement*, not an addition) |
| `docs/tranches/V/coordination/INBOX.md` | **361** | **363** | **+2** (the sweep paragraph) |

**This block's draft said `2272 → 2312` and `449 → 450`; both are struck by the measurement above
and neither is smoothed away.** The COHESION figure was a *prediction* (a stat line's "40 changed"
read as 40 inserted, where the true shape is 39 inserted ⊕ 1 deleted); the LEDGER figure was a
**clock reading with a shelf life** — `449` was true when this seat opened and **450** when it
committed, because **a sibling track appended to the shared ledger in between**. *The second is the
more useful error: a count of a live sibling is a measurement with a shelf life even when it is
honest, which is exactly why E-19 forbids banking one as an operand.*

Row census, unchanged by this seat and read at both ends: ⟨cmd⟩
`awk -F'|' '/^\| *[IO]-[0-9]+[a-z]? *\|/{n++} END{print n}'` → **90 → 90** — **zero rows minted
here**.

**THE ROSTER DENOMINATOR, RE-STATED AS A PAIRING — the third figure this seat corrected against
itself, and the one worth the most.** g.4's audit and the first cut of the §5 boundary row and the
ledger row all published *"`dev.sh` in **0 of 18** commits"*. **18 was the roster BEFORE this
seat's own writes**, and by the time the sentence was committed the seat had added three more —
**an auditor counting a roster it is still adding to.** Re-measured over the complete committed
roster at this clock: ⟨cmd⟩ `for h in <21 shas>; do git show --name-only --format= "$h" | grep -c
'dev\.sh'; done | sort | uniq -c` → **`21 0`** (twenty-one commits, every one scoring zero), and
⟨cmd⟩ `… | sort -u | grep -cv '^docs/tranches/'` → **0** unique paths outside `docs/tranches/`.
**Both figures are now published together** — **0 of 18** at the audit's clock, **0 of 21**
including this seat's — in g.4, in COHESION §5 and in the ledger row and event line (`273830b7`,
a minimal in-place correction of **this seat's own cells**, no dated byte rewritten). **The
invariant is what travels, not the integer**: `dev.sh` in zero, no path outside bounds, every
commit pathspec'd on the `commit` itself. *A receipt is not the number it prints — it is the
command that prints it, and a count of a live roster is a measurement with a shelf life even when
the seat taking it is honest.*

**BOUNDS.** Writes: **exactly four paths**, every one inside this unit's declared writable set —
`docs/tranches/X/COHESION.md` (**carve only**: the §0 X·F status cell · the §5 boundary row; §2,
§3, §4 and §4a **not opened**), `docs/tranches/X/execution/LEDGER.md` (this wave's own row cells,
in place, plus an appended event line), `docs/tranches/V/coordination/INBOX.md` (the close-sweep
line only), and this record. **`fourier-analysis/**` READ-ONLY throughout** (porcelain **1 · 1** at
open and close, `?? .worktrees/` unchanged); **`glass-ui` READ-ONLY always** (porcelain **0 · 0**);
**`keyframes.js` READ-ONLY**; the registry `fr-*.md` **never opened for write**; `CENSUS-CANONICAL.md`
read and hashed, never edited; the dated `INTAKE-ADJUDICATION-2026-08-03.md` and
`CENSUS-2026-08-03.md` untouched (E-3); the spec `F-W10.md` **IMMUTABLE and unedited**;
`scripts/dev/dev.sh` **never opened**. **Pathspec commits only**, four of them (`ed5b8def` ·
`8990c8bd` · `5d8e67e8` · this receipt's own), each one meaning. No `git add -A`, no `-u`, no
`commit -a`, no stash, no reset, no force-push, no cron, no push.

### X.F.W10.h

SERVED MODEL: claude-fable-5-1

⟨**THE §3.5 DECLARED-FABLE FINAL PASS** · Track C · X·F · the LAST F wave · VERIFY-ONLY · acts run
2026-09-20 (01:0x–01:5x EDT), sitting of record 2026-09-17 (the owner's begin-word, COHESION §0j).
**This seat authored no cure and no byte of any unit's artefact**; it re-ran every claimed reading at
its own clock, re-derived the roster from the 66 records rather than from any sentence, read every
terminal verb for the deferral shape, and adjudicated the whole against L-18's named bases. Its
output is the verdict at h.6 and the defect register at h.5. Writes: this section and the ledger
row's verdict cell — nothing else.⟩

#### h.0 — Crash-recovery, bounds and mail, before any write

⟨cmd⟩ `git status --porcelain -- docs/tranches/X/execution/C/F-W10.md docs/tranches/X/execution/LEDGER.md`
→ **empty**: no killed predecessor left partial work on either path this seat may write. The wider
porcelain carries Track A's `demo/**` · `e2e/**` · `A/X-W5.md` · `A/X-W6.md` · `CARRY-LEDGER.md` rows,
the staged `D demo/shell/PaneSegmentedControl.vue`, and `scripts/dev/dev.sh` — none read for write,
none staged, none touched. ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain` → **1** (`?? .worktrees/`);
⟨cmd⟩ `git -C ../glass-ui status --porcelain` → **0**. Both sibling trees READ-ONLY throughout (`grep`,
`sed`, `find`, `ls`, `git show`, `node -e` as readers; `npm ls` and the G24 probe run read-only).
The spec is unmoved: ⟨cmd⟩ `git log --since=2026-08-31 -- fourier/waves/F-W10.md fourier/waves/F-W9.md`
→ **empty** · ⟨cmd⟩ `git status --porcelain -- docs/tranches/X/fourier/waves/` → **0**. E13: the
close's g.6 sweep stands at this clock; no file dated 2026-09-20 has appeared on any of the four
paths since it ran, and this seat minted no mail.

#### h.1 — The partition, RE-RUN from the 66, double-run — the roster tested by derivation, not by reading

Base `docs/tranches/V/megatranche/registry/adjudicated/`, pinned BSD `/usr/bin/grep`, the three
probe variables declared exactly as F-W10 §2.5a-iii(a) declares them (`$BOIL` · `$ROW` · `$STRICT`),
**no `-P`, no `\K`, no lookaround**. ⟨cmd⟩ `git status --porcelain -- registry/adjudicated` → **0**
(the corpus is the committed corpus).

| class | run 1 | run 2 |
|---|---|---|
| `ls fr-*.md \| wc -l` | **66** | 66 |
| `grep -h 'NO-WAVE-OWNER' fr-*.md \| wc -l` (lines) | **165** | 165 |
| `grep -o … \| wc -l` (occurrences) · `grep -l … \| wc -l` (records) · `grep -o 'NO–WAVE' … \| wc -l` (en-dash arm) | **167 · 56 · 0** | 167 · 56 · 0 |
| taxonomy boilerplate (`grep -cE "$BOIL"`) | **31** | 31 |
| row-level STRICT (`grep -vE "$BOIL" \| grep -E "$ROW" \| grep -cE "$STRICT"`) | **68** | 68 |
| row-level WEAK (`… \| grep -cvE "$STRICT"`) | **32** | 32 |
| prose STRICT (`grep -vE "$ROW" \| grep -cE "$STRICT"`) | **1** | 1 |
| prose WEAK | **33** | 33 |

**`31 + 68 + 32 + 1 + 33 = 165` — the partition exhausts its operand at this seat's clock**, reproducing
the spec's transcript and `c.1`'s to the line. Per-class record counts by a per-file loop (never
`cut -d: -f1`, the form R4-4.4 revoked): row-STRICT **35** records · row-WEAK **19** · the one
prose-strict line is `fr-PaperSearch.md`'s (`L-19+C-N1`). Strict roster = **69 dispositions / 36
records**, as claimed. **The claim that a later seat can re-run the derivation in five commands is
TRUE** — this seat did, and nothing in it was read from a sentence.

#### h.2 — The landed roster and the escape test, measured from OUTSIDE the file and against the pinned census

**The block.** ⟨cmd⟩ `awk '/^### §4\.2/,/^## §4a/' COHESION.md | grep -cE '^\| *[0-9]+ *\|'` → **89 · 89**;
distinct ids ⟨cmd⟩ `… | grep -oE '^\| *[0-9]+ *\|' | tr -dc '0-9\n' | sort -n | uniq | wc -l` → **89**,
min **1** · max **89** (no gap, no repeat); the block's six sub-sections §4.2.A–F read **23 · 36 · 16 · 14**
rows ⊕ the weak class ⊕ the escape test, and `23 + 36 + 16 + 14 = 89`. `FR-NP-32` heads row 1;
`fr-App C-12` is row 9, the NEGATIVE record verbatim. **Operand hygiene**: ⟨cmd⟩ `grep -c 'CHECK.md\|CARRY.md'`
over the §4.2 region → **0**; ⟨cmd⟩ `grep -c 'F-W10-CHECK' execution/C/F-W10.md` → **0** — no roster in
this wave was measured against a check file or a per-wave carry (R2-6b), and the only census operand
named anywhere is the canonical.

**The census of record is unmoved.** ⟨cmd⟩ `shasum -a 256 fourier/conformance/CENSUS-CANONICAL.md | cut -c1-12`
→ **`f44362757458`**; ⟨cmd⟩ `grep -c '^### F\.W10' CENSUS-CANONICAL.md` → **0**.

**The escape test, RE-RUN by this seat's own instrument** (a bash script, kept in the scratchpad, not
the record — the receipt is the command shape): the four NWO rosters were cut out of the canonical's §2
by their own headings — `NWO (packet)` **21 lines / 32 ids** · `NWO→SS-13` **3 / 3** · `NWO→SS-3`
**22 / 27** · `NWO→SS-5` **2 / 3** = **65 `(record, id)` pairs, 65 distinct** — and each pair was
tested against the 89 numbered rows with the id **boundary-anchored** (`(^|[^A-Za-z0-9])ID([^A-Za-z0-9]|$)`)
and the record name required to co-occur: **41** resolve on the same row line; **23** resolve by the
block's own table idiom (a record named once and its following rows anchored `· :NN` — resolved with
the record name within the four preceding rows: `AA-46`/`AA-47` under `AA-45`'s `fr-AdminAuditLog:88`,
the five `PAW-*` under `PAW-32`'s `fr-PaperArticleWindow:88`, and so on); **1** — `fr-CanvasOverlayButton
FR-COB-14` — resolves on no numbered row and **through §4.2.E's NAMED six**, exactly as `c.3` states.
**65 booked or cited · 0 escaped**, run twice, identical. `c.3`'s split reads *"63 per-id · 2 via the
class"*; this seat's reads **64 · 1**, because `fr-GalleryInfiniteGrid MISS-5` co-occurs on row 30's
line (`FR-GIG-20`'s *"Its `MISS-5` restatement is the same identity, one home"*) and the probe credits
it there — **the same 65, the same zero, one pair counted at a different lawful site.** Published as a
difference of instrument, not a correction.

**The prior populations are cited and not counted**: the block is the third fourier population in §4
(after §4.1's fourteen and F.W3's seven-row S-8 emission) and its 89 include neither.

#### h.3 — Every claimed reading, re-run at this seat's own clock

Every figure below is this seat's own probe on 2026-09-20; each corpus/file count was run twice.
"= g" means the close's reading reproduces exactly.

| gate | this seat | agrees with the close? |
|---|---|---|
| **G-F10-1** | fourier `ADOPTION-ASKS.md:109–125` → **17** rows / **12** `OPEN`; value `INBOX.md`: `Ask 3` 2 · `Ask 5` 2 · `inv-22` 2 · `cascade-vjs` 2 · `ADOPTION-ASKS` 5; `I-36..I-39` **4** rows, each `READ + ACKED 2026-09-20` with a disposition verb (DONE · DONE · DONE · RELAY of the existing R letter) and **no verb naming a peer's act as an acceptance condition**; far end `docs/tranches/F/coordination/INBOX.md`: `ADOPTION-ASKS` **0** · `cascade-vjs` **0** | **= g · SPLIT stands** |
| **G-F10-2** | fourier `CLAUDE.md` EXISTS · `find docs -name INBOX.md` → **1** · facility-19 `git ls-files` → **1** · `FN-` in their INBOX → **0** | **= g · SPLIT stands**; the three parity legs are F.W0's (§0k.1), credited there |
| **G-F10-3** | ⟨cmd⟩ `node …/fourier-value-import-drift.mjs` (cwd `fourier-analysis/web`) → **exit 1**, `ERR_MODULE_NOT_FOUND` on `…/@mkbabb/value.js/dist/value.js`, twice | **= g · honest-RED**, failing case named; relay letter quotes `0.192` **labelled BANKED** at its `:68`/`:129`, never as current |
| **G-F10-4** | fourier `web/package.json` → `glass-ui ^8.0.0` · `keyframes.js ^6.0.0` · `value.js ^4.0.0`; keyframes `package.json:71` → `"4.0.0"`; `npm ls @mkbabb/value.js` → **exit 0** in fourier **and** in keyframes.js; `O-34..O-38` → **5** rows | **= g · SPLIT stands** — the exit-0 pair is PRE-WINDOW (4.0.0 served; the cure is 4.1.0) and is not read as the post-window leg |
| **G-F10-5** | `LATEX-PAPER-RELAY.md` EXISTS, **36,954** bytes; `latex-paper` in INBOX → **5**; **26** bold-headed carry rows; `dist/vue.js` anchors **13**; the one `src/…:NN` coordinate in the file is **L/D29's banked pair, quoted as DRIFT and re-seated at `dist/vue.js:85/:90`** (SC-3 honoured — the producer-src address is named as the defect, not used as an anchor); `:root` appears **4×, every one the K-9 KILL restated**, never a prescribed repair; the delivery point named (**6** mentions) | **= g · GREEN** |
| **G-F10-6 ≡ G-F9-21** | checkpoint-block `diff` → **0 bytes** · re-cut-table `diff` → **0 bytes** · whole-§2.2 `diff` → **3 hunks** (E-10's seat-local head sentences); corpus: `F\.W9/W10` **54** · **25** records · bare `F.W10` **0** · **66** files; AA-44's *"9 e2e"* cell present in **both** twins (`grep -c` → 1 · 1) | **= g · SPLIT stands**; the five pending paired edits are **NOT applied** — the twins are byte-unmoved since 2026-08-30 (`git log` empty, porcelain 0) |
| **G-F10-7** | h.1 ⊕ h.2 above — partition **165**, roster **89/89/89**, escape **65/0**, operand the canonical at `f44362757458` | **= g · GREEN, with the residual h.5 D-1 names** |
| **G-F10-8** | `CENSUS-ADDENDUM-2026-08-25.md`: `Zero UNVERIFIED credit` **2** · `carry zero credit` **2**, both occurrences naming the superseded wording as superseded (`:277` *"is SUPERSEDED"*, `:301` *"Never …"*); `/tmp/fourier-r4-files.sha256` → **No such file**; `FOURIER-R*.md` → **9**; `~/.codex/worktrees/d0be/fourier-analysis` → **present** (RETAIN-AS-RECORD); OG-F1 / OG-F2 / G-10 read RULED at COHESION §0j.D, ESC-2 at §0o | **= g · GREEN** |
| **G-F10-9** | `INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` EXISTS; ⟨cmd⟩ `git diff --stat -- …/INTAKE-ADJUDICATION-2026-08-03.md` → **empty** | **= g · GREEN**; see h.5 I-2 for the dated file's 09-17 history |
| **G-F10-10** | `aria-label` counts `EditorControlsDock` **13** · `CanvasControlsDock` **8** · `FunctionInput` **3** (the 17 legs cured, F.W4's); `grep -rln admin web/e2e/` → **5** · `AxeBuilder` → **4** · `*.spec.ts` → **18**; `/paper` and `/equation` each in **8** files under `web/e2e/` (**6** counting `*.spec.ts` only — the close's 8 is the directory-wide probe; both published); the run of record is F.W9 `b.9` (`C/F-W9.md:923`): admin **RED 4-of-4**, `[serious] aria-hidden-focus` ×2 on the `Metric` tile → `E-F9b-2` → SS-6 | **= g · honest-RED**, no waiver in play, none lawful |
| **G-F10-11** | `postcss.parse(glass-ui 8.0.0 dist/styles/index.css)` → **OK, 40 nodes** · `@source` **1** · `` source` `` **0**; fourier porcelain **1** (`?? .worktrees/`); `InfoCard.vue`/`CanvasOverlayButton.vue` under `web/src` → **0** | **= g · GREEN, VERIFIED-GREEN-BY-PREDECESSOR ×3** (F.W1 · F.W0 · F.W0), each credited by name in g.1 — the credit is correct |
| **G-F10-12** | `valuejs-outbound-*.md` under `glass-ui/docs` → **12**; term sweep `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0**; `o20`: `@source` **1** · `FR-NP-32` **1** · sha `f6e04c86ddb8` unmoved; `X/coordination/*2026-09-DD*` → **3** authored-not-landed letters | **= g · SPLIT stands** |

**Every reading the close published reproduces at this seat: 5 GREEN · 5 SPLIT · 2 honest-RED · 0
argued green.** The GREEN-BEFORE-CURE legs are credited to F.W0 (parity ×3 · porcelain · the two
deletions), F.W1 (the pin · the parsing dist), F.W4 (the 17 naming legs), F.W9 (the coverage witnesses)
and X-W9.i (the five packets) — each by name, none banked as F.W10's.

#### h.4 — The 89 terminal verbs read one by one for the "decision that is really a deferral" shape

The verb column of every numbered row was extracted from the settled §4.2 bytes (`awk -F'|'`, last
non-empty cell) and read whole. The alphabet actually carried: NAMED WAVE · NEXT-BOUNDARY LEDGER
(BH-RELAY / CENSUS-INPUT) · KILL-WITH-RATIONALE · CITE-CONSUMING-WAVE · FOLD-WITH-TARGET ·
EXCLUDED-WITH-REASON, plus the seven banked wordings `c.4` discloses (`BOOKED HERE` ×2 · `ADOPTED AS LAW`
· `NAMED FOLD` · `KILL-AS-DESTINATION` · `SPLIT EXACTLY AS BANKED` ×2 — the last two carrying two verbs
for two banked halves, never one averaged verb). Shapes that were tested and PASS: the two
*conditional-cleanup* kills (rows 71 · 82) schedule no wave and let a change ride free if another cure
opens the file — the record is the disposition, nothing is deferred to it; row 7's SS-4 rider records
how a decline would be written, not a condition on a peer; every BH-RELAY row names a READ receipt
(O-20 rows, NWO-1 S-n, I-32 A-n) or, for `D-26` (row 58), states *never sent* and accretes at §4a
`F10-1` rather than claiming a send. **One shape FAILS the test at the close, not at the row** — rows
**65** (`i-1 = L-INFO-2`) and **76** (`PP-CENSUS`): their verb is CENSUS-INPUT with the landing surface
named as `CENSUS-ADDENDUM-2026-08-25.md`, and both rows carry a ⚠ flag that the erratum text is owed
there. That is a disclosed deferral with a named decision-owner — lawful **only while the owner rules
it** — and h.5 D-1 records what happened to it.

**Pending paired edits — disclosed and NOT applied, verified at the bytes.** The five (AA-44 `9`→`8`
⊕ §5's four canonical divergences) live inside the shared §2.2 splice; both twins are byte-unmoved
since the 2026-08-30 S-16 addenda (h.3, G-F10-6), `a.6` re-discloses all five, and the close applied
none. **No roster measured against a check file or a carry** (h.2). **Every GREEN-BEFORE-CURE leg
credited to its predecessor** (h.3). **No owner-gated item re-ruled**: OG-F1 · OG-F2/OG-V2 · G-10 ·
ESC-2 · F-SS4REST are cited by id to §0j.D / §0o by `b`, `g` and this seat alike.

#### h.5 — The defect register, and L-18's two gestalt passes over the named bases

**PASS A — the measurement pass** (h.1–h.3): every published figure re-derived by this seat's own
command; every one reproduces. **PASS B — the reading pass** (h.4 ⊕ the close's own residual, bounds
and stamp sections read against the unit receipts that fed them). Pass B is where the finding lives.

| # | grade | finding | where the base bites |
|---|---|---|---|
| **D-1** | **HIGH** | **The close DROPPED a decision two units handed it.** `a.9` residual 1 (*"a bounds problem for the close to rule … Either `c` is granted the addendum path for those two rows, or the close seat lands them, or they are explicitly re-homed"*) and `c.7` residual 1 / §4.2.F (*"Either the close seat lands the two errata, or they are explicitly re-homed"*) both route the **owed erratum text** for `i-1 = L-INFO-2` (fr-BasisSelector:92) and `PP-CENSUS` (fr-PathPreview:31 — the five `lane-frontend.md:183/:366/:369/:444/:565` errata F-W10 §2.5a-iii(c) says *"land in the CENSUS-ADDENDUM (§1a #8)"*) to the close for a ruling. ⟨cmd⟩ `grep -n 'L-INFO-2\|PP-CENSUS' CENSUS-ADDENDUM-2026-08-25.md` → one row, `:312`, **"FLAGGED, NOT BOOKED"** — the text never landed; ⟨cmd⟩ `grep -c 'L-INFO-2\|PP-CENSUS\|erratum'` over **g.7** → **0**, over the **§5 boundary row** (COHESION `:124–`) → **0**, over the **ledger row + event line** → **0**. The close neither landed them, nor re-homed them, nor carried them as a residual: **the item vanished at the seat that was asked to decide it.** G-F10-7 stays GREEN on its own terms (both rows carry a terminal verb, and CENSUS-INPUT's next-boundary ledger row exists at §4.2 itself), but the wave's spec-declared content for §1a #8 is short two errata and the close's residual register is incomplete on exactly the row its own stamp depends on | **the head base — *"a drain complete-looking because it counted only what it chose to look at"* — in its fifth form: the residual register counted what the close seat chose to look at.** Also *"a disposition verb that reads as a decision while being a deferral"*: rows 65/76's deferral was lawful only while its named decision-owner ruled it, and it did not |
| **I-1** | INFO | `g.1`'s G-F10-10 cell reads *"`/paper` named in **8** specs"*; the `*.spec.ts` count is **6**, the directory-wide `web/e2e/` count **8** (helpers/fixtures included). The gate's witness is the axe run, not this count; published so neither figure is quoted for the other | denominator hygiene (PASS-1 D3's family), no verdict moved |
| **I-2** | INFO | The dated `INTAKE-ADJUDICATION-2026-08-03.md` carries a 2026-09-17 commit **`fc92ed52`** (KF.W0 G-0.8: **+57 / −0**, an in-file append, no byte rewritten) — outside F.W10, before its open. G-F10-9's literal witness (`git diff --stat` empty) holds and `b`'s addendum-beside touched nothing; recorded so the E-3 register knows the file's history is not *"untouched since 08-03"* | E-3 provenance; no verdict moved |
| **I-3** | INFO | `c.3` and this seat split the same 65/0 as **63 + 2** vs **64 + 1** (`MISS-5` credited on row 30's line by this seat's boundary probe). Same set, same zero; a difference of instrument, published | none |

**The eight bases, adjudicated** (the spec's Close-act list, headed as the brief heads it): (1) *a re-cut
complete-looking because it counted boilerplate* — **CLEAR**: the 31 taxonomy lines are excluded by
`$BOIL`'s shape and added back into 165; (2) *a drain complete-looking because it counted only what it
chose to look at* — **CLEAR at the drain** (h.1/h.2 re-derive it from the 66) and **BITTEN at the close's
residual register** (D-1); (3) *its second form, a check-file operand* — **CLEAR**: `CHECK.md`/`CARRY.md`
appear on no closure LHS; (4) *its third form, an operand corrected with the derivation skipped* —
**CLEAR**: the derivation was RUN by `c` and RE-RUN here, `fr-PathPreview`'s three rows are 76–78;
(5) *a class verb where the detector grades STRICT / a predicate that does not reproduce* — **CLEAR**:
the ten strict rows carry per-id verbs, the weak six are named, the predicate reproduces both ways;
(6) *a band disclosure wrong twice* — **CLEAR**: no band, a partition that closes; (7) *a spec that must
count itself* — **CLEAR**: every roster figure in this record is measured from outside the file it
counts; (8) *a verb that is a deferral* — **BITTEN once** (D-1's rows 65/76 at the close). The further
named bases — relay receipts asserted (**CLEAR**, o17/o20 read, sha pinned), the inbox sweep at the
wrong tranche (**CLEAR**, BK is newest, denominator 12), the wrong denominator (**CLEAR**, 17/12
reproduces), the twelve-limb transaction (**CLEAR**, the pins read whole), coordinate cites into live
siblings (**CLEAR**, gate ids only), the inverted splice (**CLEAR**, 0 bytes both directions), the
producer's own id absent from the close-gate (**CLEAR**, FR-NP-32 heads row 1 with A-1 read), an axe
gate argued green over open legs (**CLEAR**, legs cured, gate honest-RED), a terminal disposition over
an outstanding owner ruling (**CLEAR**, every one ruled at §0j.D/§0o) — **CLEAR**.

#### h.6 — VERDICT

**§3.5's declared-Fable final pass over the X·F close returns: NOT-CONFORMANT-AT-THE-CLOSE — 1 HIGH ·
0 MINOR · 3 INFO — on a wave whose every measured claim REPRODUCES.** Stated in the four-verb's own
terms so no reader inflates or deflates it:

- **The measurements are true.** The partition (165), the roster (89/89/89), the escape test (65/0
  against the frozen canonical), all twelve gate readings and every predecessor credit reproduce at an
  independent Fable seat by re-derivation, not by reading. Nothing was argued green; the two honest-REDs
  name their failing case; the five SPLITs are read as splits.
- **The close is short one decision.** D-1 is not a measurement error and not a re-ruling — it is a
  ruling the wave's own units asked the close to make, which the close did not make and did not carry.
  Its cure is one of the two acts `a.9`/`c.7` already named: **land the two errata in
  `CENSUS-ADDENDUM-2026-08-25.md`** (the `lane-frontend.md:183/:366/:369/:444/:565` five for `PP-CENSUS`
  and the `i-1 = L-INFO-2` lane-doc correction, beside FR-CP-44 / FR-AFP-51 as F-W10 §2.5a-iii(c)
  prescribes) by a seat granted that path, **or explicitly re-home them** with the rationale written at
  §4.2 rows 65/76 and in the close's residual register. **This seat, VERIFY-ONLY, does neither** — the
  addendum is outside its writable set and a re-homing is a ruling. It files the defect and returns it.
- **The F-side stamps of §3.1–§3.4 STAND** as the close stamped them: §3.3's deadline is discharged
  because rows 65/76 are terminally homed at a next-boundary ledger row (§4.2) — the residual is the
  erratum *text* in a sibling artefact, not a missing home. **§3.5 is now PERFORMED and RETURNS a
  defect**; it does not stamp CONFORMANT until D-1 is cured or re-homed by a dated addendum beside.
- **ACCEPTED is NOT stamped and VERIFIED stays NO.** L-18's *two quartet gestalt passes* were run here
  as two passes of ONE Fable instance (PASS A measurement · PASS B reading) — this seat is the
  adjudicator the law names, not the two Opus-5 skeptic quartets the law also names (COHESION §5's
  X·P reading of L-18: *"two Opus-5 skeptic quartets + a fresh Fable adjudication"*). The X·F release
  close reserves VERIFIED (F-W10 §1's four-verb line; LEDGER `:11`), and nothing here pre-empts it.

**Ledger effect**: the row's `CLOSED 2026-09-20 (honest-RED …)` status stays as the close wrote it —
this pass moves no gate verdict — and gains this pass's verdict clause; **`h` is no longer OWED; a
repair of D-1 is.**

#### h.7 — Self-count and bounds

**SELF-COUNT LAW**: this section's own extent is read from the settled bytes by a later seat, never
banked here (E-19). **BOUNDS**: writes on **exactly two paths** — this record (this section, appended)
and `LEDGER.md` (F.W10's own row cells, minimal in-place, re-read immediately before the edit; one
appended event line). No artefact of `a`–`g` opened for write; no spec byte; no COHESION byte; no
INBOX byte; no sibling tree (fourier porcelain **1 · 1**, glass-ui **0 · 0** at open and close);
`scripts/dev/dev.sh` never opened. The escape-test script lives in the session scratchpad and is not
a deliverable — its command shape is the receipt (h.2). Pathspec commits only, two: this receipt, and
the ledger cell. No `git add -A`, no `-u`, no `commit -a`, no stash, no reset, no force-push, no cron,
no push.

## Close

SERVED MODEL: claude-opus-5[1m]

⟨**THE VERIFY-ONLY CLOSE SEAT** · Track C · X·F · acts run 2026-09-20 at this seat's own clock ·
sitting of record **2026-09-17** (the owner's begin-word, COHESION §0j). **This seat cured nothing,
authored no artefact byte of `a`–`h`, moved no gate verdict it did not measure, and applied none of
the five pending paired edits.** Its acts are the five the close protocol names: audit every unit's
commits for existence and bounds; **re-run all twelve §3 gates itself**; run the spec's verification
conjuncts as written; re-sweep E13's four paths; and write this section with the gate table
BEFORE→AFTER, the commit roster, the residuals with named owners, the escalations and the four-verb
line moved only as far as the spec permits. **Writes: this section and `LEDGER.md`'s F.W10 row
cells + one appended event line.** Nothing else — not `COHESION.md`, not `INBOX.md`, not any
artefact of `a`–`h`, not one byte of the spec (E-3).⟩

### C.0 — Crash-recovery and bounds, before any write

⟨cmd⟩ `git status --porcelain -- docs/tranches/X/execution/C/F-W10.md docs/tranches/X/execution/LEDGER.md`
→ **empty**: no killed predecessor left partial work on either path this seat may write. The wider
porcelain carries Track A's `demo/**` · `e2e/**` · `A/X-W5.md` · `A/X-W6.md` · `CARRY-LEDGER.md`
rows, the staged `D demo/shell/PaneSegmentedControl.vue` and the untracked
`docs/tranches/X/waves/W6-evidence/gates/` tree — **every one a sibling seat's path on another
track**, none read for write, none staged, none touched. `scripts/dev/dev.sh` is the standing
unowned dirty row and was **never opened**. ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain`
→ **1** (`?? .worktrees/`) · ⟨cmd⟩ `git -C ../glass-ui status --porcelain` → **0**. Both sibling
trees, the frozen corpus and both wave specs stayed READ-ONLY: ⟨cmd⟩
`git status --porcelain -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/`
→ **0**, so **E-3 holds at the bytes across this seat as it did across `a`–`h`**.

### C.1 — The commit roster: every unit's commits EXIST, and every path lies inside that unit's writable set

Enumerated by ⟨cmd⟩ `git log --format='%h %s' --since=2026-09-18 | grep -E '^[0-9a-f]+ docs\(x-f-w10'`
→ **24** shas, ⊕ the OPEN commit `2ace946e` ⊕ `h`'s ledger commit `460ae889` = **26**. Each one's
paths read by ⟨cmd⟩ `git show --name-only --format='' <sha>`:

| unit | commits | paths touched | §1a row | bounds |
|---|---|---|---|---|
| open | `2ace946e` | this record · `LEDGER.md` | — · runbook | **IN** |
| `a` | `cd4df502` · `cee59a4f` | `formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` · this record | **#8** | **IN** |
| `b` | `077d3e5f` · `13bfec7c` · `3616a2d8` | `codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` · this record | **#7** | **IN** |
| `c` | `269852ab` · `5f557674` · `4880dc04` · `2b9ab798` | `X/COHESION.md` (§4 · §4a) · this record | **#3** | **IN** |
| `d` | `4278b798` · `f294c12c` | `X/fourier/LATEX-PAPER-RELAY.md` · this record | **#2** | **IN** |
| `e` | `a8fa79d2` · `66e35333` · `80924add` | `coordination/FOURIER-ADOPTION-ASKS-RECONCILIATION-2026-08-25.md` · `V/coordination/INBOX.md` · this record | **#5 · #4** | **IN** |
| `f` | `d2319db1` · `806e0196` · `e00e3629` | `coordination/FOURIER-EVIDENCE-RELAY-G24-G33-2026-08-25.md` · `V/coordination/INBOX.md` · this record | **#6 · #4** | **IN** |
| `g` | `ed5b8def` · `8990c8bd` · `5d8e67e8` · `273830b7` · `63cc592b` · `6063e843` | `X/COHESION.md` (§0 · §5) · `LEDGER.md` · `V/coordination/INBOX.md` · this record | **#3 · #4** · runbook | **IN** |
| `h` | `8a1c6e9f` · `460ae889` | this record · `LEDGER.md` | — · runbook | **IN** |

**Findings: ZERO out-of-bounds writes across all 26.** The union of paths touched is exactly
**eight files**: the five §1a CREATE targets (#2 · #5 · #6 · #7 · #8), the §1a #3 MODIFY-carve
(`COHESION.md`), the §1a #4 mail surface (`INBOX.md`), plus this record and `LEDGER.md`. **No
commit reaches `demo/**`, `e2e/**`, `src/**`, `api/**`, `package.json`, a sibling tree, another
track's execution record, or `~/.codex/**`** — ⟨cmd⟩ per sha, `git show --name-only --format= <sha> |
grep -cE '^(demo|e2e|src|api|test)/|scripts/dev/dev\.sh|^package\.json'` → **0 for all 26**
(`sort | uniq -c` → `26 0`). **`scripts/dev/dev.sh` appears in zero of the twenty-six.**

**Two honest notes on the denominator, neither a finding.** (i) `LEDGER.md` is **not** a row of
F-W10 §1a; its edit right comes from the execution runbook's close protocol, and every seat that
touched it (`open`, `g`, `h`, this one) did so by **minimal in-place replacement of F.W10's own row
cells plus appended event lines** — ⟨cmd⟩ `git show --stat` on `6063e843` · `8990c8bd` · `460ae889`
shows no rewrite of the file. (ii) `g.4` published the roster at **18** and corrected itself to
**21** at `g.8`; the complete committed roster at this seat's clock is **26**, because `g`'s own
last two commits and `h`'s two landed after those readings. **All three figures are true readings
of a growing roster at three instants**, and the invariant — *zero out of bounds, `dev.sh` in zero*
— is what reproduces at every one of them.

### C.2 — The twelve §3 gates, BEFORE → AFTER, re-run by THIS seat, every reading double-run

**BEFORE** = the spec's born state (2026-08-28) as §3 declares it. **AFTER** = this seat's own
probe on 2026-09-20, run twice, published whether it flatters the wave or not. **R.2 binds: a GREEN
before its cure is a finding, not a credit** — every green-before-cure leg is stamped
**VERIFIED-GREEN-BY-PREDECESSOR** with the predecessor named.

| gate | BEFORE (spec, born) | AFTER — this seat, 2026-09-20 ×2 | verdict |
|---|---|---|---|
| **G-F10-1** asks reconciled in BOTH ledgers | RED — 4 value-targeted asks OPEN, INBOX rows **0** | fourier `ADOPTION-ASKS.md:109–125` → **12 OPEN of 17** rows (×2, E-1's corrected denominator); value `INBOX.md` ⟨cmd⟩ `grep -cE '^\| *I-3[6-9] '` → **4** rows, each a disposition verb; far end ⟨cmd⟩ `grep -ci 'ADOPTION-ASKS\|cascade-vjs'` on `fourier docs/tranches/F/coordination/INBOX.md` → **0** | **SPLIT** — value leg GREEN by `e`; the fourier-ledger leg RED and **not this side's to turn** (§5 · COMMISSION §2). Not argued green |
| **G-F10-2** inbound-mail parity | RED — no fourier `CLAUDE.md`, `find docs -name INBOX.md` → 0, facility-19 `??` | fourier `CLAUDE.md` **EXISTS** · ⟨cmd⟩ `find docs -name INBOX.md \| wc -l` → **1** · ⟨cmd⟩ `git ls-files …facility19…` → **1** · ⟨cmd⟩ `grep -c 'FN-'` in their INBOX → **0** | **SPLIT** — three parity legs **VERIFIED-GREEN-BY-PREDECESSOR (`X.F.W0`, COHESION §0k.1)**; FN-1..FN-7 fourier-side verbs stay RED |
| **G-F10-3** G24 drift RELAYED | RED, MEASURE-AT-OPEN — banked 8/22 curves, max\|Δ\| 0.192 | ⟨cmd⟩ `node …/fourier-value-import-drift.mjs` (cwd `fourier-analysis/web`) → **EXIT 1**, `ERR_MODULE_NOT_FOUND` on `…/@mkbabb/value.js/dist/value.js` (stdout **0 bytes**), reproduced | **honest-RED, failing case named.** The 0.13-era import path is stale against the 4.x export map. Relayed, never repaired, never wrapped, never skipped; cure = `ESC-W9-G24-SUBSTRATE` at X-W9.f. No falsifier tripped |
| **G-F10-4** G33 packets + post-window pin truth | RED — `web/package.json` `^0.13.0`; `npm ls` ELSPROBLEMS ×6 | fourier `web/package.json` → `glass-ui ^8.0.0` · `keyframes.js ^6.0.0` · `value.js ^4.0.0`; ⟨cmd⟩ `npm ls @mkbabb/value.js` → **exit 0** (4.0.0, keyframes 6.0.0 deduped); ⟨cmd⟩ `grep -cE '^\| *O-3[4-8] '` → **5** rows | **SPLIT** — pin leg **VERIFIED-GREEN-BY-PREDECESSOR (`F.W1`)**, packets/rows leg **by-predecessor (`X-W9.i`)**; the **post-window** leg is UNREADABLE until the 4.1.0 window, and reading today's exit-0 as post-window is the gate's own falsifier. Published **PRE-WINDOW** |
| **G-F10-5** the latex carry has a receiving surface | RED — register absent; `grep -c 'latex-paper' INBOX.md` → 0/1 | `LATEX-PAPER-RELAY.md` **EXISTS**, ⟨cmd⟩ `wc -c` → **36954** ×2; ⟨cmd⟩ `grep -c 'latex-paper' INBOX.md` → **5** | **GREEN** — landed by `d` at `4278b798`; every §2.6 carry rowed, PAW-1's F.W1 question answered, delivery point named, SC-3 honoured |
| **G-F10-6 ≡ G-F9-21** re-cut complete, Δ=∅ | RED on the corpus leg — 54 / 25-of-66 / bare `F.W10` 0 | corpus ×2 in `registry/adjudicated/`: `F\.W9/W10` → **54 · 54** · records → **25 · 25** · bare `F.W10` → **0 · 0** · `ls fr-*.md` → **66 · 66**. Splice ×2: checkpoint-block `diff` → **0 bytes**, re-cut-table `diff` → **0 bytes** | **SPLIT — and the corpus RED is STRUCTURAL, not neglect**: the gate's own falsifier forbids ANY edit to `fr-*.md`. The spec binds twice (*"the gate STAYS RED on its corpus leg"* · *"a later seat must not read the green leg as the gate"*) and **this seat does not stamp it green** |
| **G-F10-7** the NWO register drained on the F side | RED — COHESION §4 every row an X·V slug, ZERO fourier rows | partition re-derived from the 66 with `$BOIL`/`$ROW`/`$STRICT` exactly as §2.5a-iii(a) declares them, ×2: **31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33 = 165 · 165**. The landed block measured **from outside the file** ×2: **89** numbered rows · **89** distinct ids · min **1** max **89** (no gap, no repeat). Census of record ⟨cmd⟩ `shasum -a 256 CENSUS-CANONICAL.md \| cut -c1-12` → **`f44362757458`** ×2, unmoved | **GREEN**, with residual **R-1** (below) named against it |
| **G-F10-8** the Codex residue is not inherited | RED — charter line in no F spec; OG-F1/F2 unanswered | ⟨cmd⟩ `grep -c 'Zero UNVERIFIED credit' CENSUS-ADDENDUM-2026-08-25.md` → **2** (corrected wording; the superseded wording confined to supersession bytes); `/tmp/fourier-r4-files.sha256` → **No such file**; `FOURIER-R*.md` → **9 · 9**; `~/.codex/worktrees/d0be/fourier-analysis` → **still registered** (verb: RETAIN-AS-RECORD) | **GREEN** — OG-F1 / OG-F2/OG-V2 **discharged BY RULING** at COHESION §0j.D and cited by id; re-ruled by nobody |
| **G-F10-9** NEXT-COMMUNIQUE ledger truth | RED — the dated intake `:221` declares the channel EMPTY | `INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` **EXISTS** (`077d3e5f`); ⟨cmd⟩ `git diff --stat -- …/INTAKE-ADJUDICATION-2026-08-03.md` → **empty** ×2 | **GREEN** — E-3's shape held at the bytes; glass arms → SS-6, latex arms → the §2.6 register, each in exactly one register; the *"glass row cured consumer-side"* falsifier untripped |
| **G-F10-10** close-gate a11y honesty (FR-TT-1 lock) | RED — `grep -rln admin web/e2e/` → 0 files; no axe on `/paper`; `/equation` one screenshot slug | legs first: ⟨cmd⟩ `grep -c 'aria-label'` → `EditorControlsDock.vue` **13** · `CanvasControlsDock.vue` **8** · `FunctionInput.vue` **3** — the 17 naming legs **measured CURED** (F.W4's). Witnesses: `grep -rln admin web/e2e/` → **5** · `AxeBuilder` → **4** · `ls web/e2e/*.spec.ts` → **18** | **honest-RED, failing case named, NO WAIVER — because none is lawful.** The legs being cured **closes** the waiver road, so the run is the whole question; the run of record (F.W9 `b.9`) is admin **RED 4-of-4**, `[serious] aria-hidden-focus` ×2 inside glass-ui 8.0.0's `Metric` tile — **a PRODUCER row riding SS-6 as `E-F9b-2`, never a frontend hack** |
| **G-F10-11** F.W0 substrate terminal | RED — glass 4.0.0 dist: 35 nodes, ZERO valid `@source`; 28 dirty rows; F8-REACH un-dispositioned | at the **8.0.0 pin as fourier installs it** (⟨cmd⟩ `node -e` on `web/node_modules/@mkbabb/glass-ui/package.json` → **8.0.0**): `postcss.parse(dist/styles/index.css)` → **OK, 40 top-level nodes** · `grep -c '@source'` → **1** · ``grep -c 'source`'`` → **0**. GAB-13: fourier porcelain → **1** (`?? .worktrees/`). F8-REACH: ⟨cmd⟩ `find web/src -name 'InfoCard.vue' -o -name 'CanvasOverlayButton.vue'` → **0** | **GREEN — VERIFIED-GREEN-BY-PREDECESSOR ×3**: the parsing dist is **F.W1**'s uplift, the landed worktree is **F.W0**'s under §0j.D `OG-F1`, both deletions are **F.W0**'s under §0j.D `G-10`. F.W10's claim is the verification, exactly as §2.3 defines it |
| **G-F10-12** every declared relay arm has a grep-able receipt | SPLIT — GREEN on U-1..U-11 + the `@source`/FR-NP-32 arm; RED on three | ⟨cmd⟩ `find ../glass-ui/docs -name 'valuejs-outbound-*.md' \| wc -l` → **12 · 12** (denominator moved 11 → 12, published not inherited). Term sweep across all twelve ×2: `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0** | **SPLIT, unchanged in shape and sharpened rather than smoothed**: two arms GREEN-BY-VERIFIED-RECEIPT; three RED, two of them **authored in our tree and not landed under the producer's path** (`O-23` · `O-32`). Sibling trees are READ-ONLY to every wave seat, so the finding is rowed and no byte moves toward it |

**AFTER, totalled: 5 GREEN · 5 SPLIT · 2 honest-RED · 0 argued green** — **identical to `g`'s
close reading and to `h`'s independent re-derivation.** Every figure above was measured by this
seat's own command and every one reproduces double-run.

### C.3 — The verification conjuncts, run as written

**F-W10.md declares no `§Verification Artefacts` section** — ⟨cmd⟩ `grep -cE '^#+ .*Verification'
waves/F-W10.md` → **0**. Its verification surface is the **Close act** (`:515`) plus each gate's own
falsifier, and those are what this seat ran, conjunct by conjunct.

| conjunct (quoted from the Close act / §4b's X-whole row) | this seat's reading | verdict |
|---|---|---|
| *"when G-F10-1..12 are GREEN"* | **NOT satisfied, and the spec's own §3 posture is what governs instead**: two gates are honest-RED with their failing case named and neither is curable by this wave (G-F10-3's instrument is X-W9's; G-F10-10's RED is a producer row riding SS-6) | **HONEST-RED CLOSE** — the lawful reading, not a green one |
| *"F.W10 stamps the F-side of COHESION §3.1–§3.6"* | §3.1 STAMPED against the frozen canonical (`f44362757458`, ⟨cmd⟩ `grep -c '^### F\.W10' CENSUS-CANONICAL.md` → **0**: F.W10 homes no row of its own) · §3.2 STAMPED (⟨cmd⟩ `ls fourier/waves/F-W*.md \| wc -l` → **11**, a spec for every sketched id, zero kills) · §3.3 STAMPED by `c`'s published derivation, re-derived here (165 / 89 / 65-0) · §3.4 STAMPED (five REQUIRED far ends, read spelling-agnostically) | **§3.1–§3.4 STAMPED** |
| *"runs the §3.5 L-20 conformance loop with a declared-Fable final pass"* | **PERFORMED** by `X.F.W10.h` (`claude-fable-5-1`, VERIFY-ONLY, `8a1c6e9f`) and it **RETURNS A DEFECT**: NOT-CONFORMANT-AT-THE-CLOSE, 1 HIGH · 0 MINOR · 3 INFO, on a wave whose every measured claim reproduces | **§3.5 PERFORMED, NOT CONFORMANT** |
| *"records the boundary row in COHESION §5"* | ⟨cmd⟩ `awk '/^## §5/,0' COHESION.md \| grep -n 'F\.W10'` → the **2026-09-20 X·F BOUNDARY** row — cited **prefix-free by its own dated bytes** per R2-2, never by a coordinate into a live sibling — carrying the stamp **and** the residue in one breath | **LANDED** |
| *"L-18 still applies — no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance"* | `h` ran **two passes of ONE Fable instance** (PASS A measurement · PASS B reading) and said so against its own interest; the law's two Opus-5 skeptic quartets did not sit | **ACCEPTED WITHHELD** |
| the eight named L-18 bases | `h` adjudicated each: **seven CLEAR**, one **BITTEN** — *"a drain complete-looking because it counted only what it chose to look at"* in its fifth form, at the close's **residual register** rather than at the drain | **one base bitten — residual R-1** |

**The §3.5 defect re-verified at the bytes by this seat, not taken on `h`'s word.** ⟨cmd⟩
`grep -n 'L-INFO-2\|PP-CENSUS' CENSUS-ADDENDUM-2026-08-25.md` → **one row, `:312`, reading
"FLAGGED, NOT BOOKED"**; ⟨cmd⟩ `grep -c 'lane-frontend.md:183' CENSUS-ADDENDUM-2026-08-25.md` →
**0** — the erratum text §2.5a-iii(c) routes to §1a #8 **never landed**. The two rows' terminal
verbs **do** stand at COHESION §4.2 rows **65** (`i-1 = L-INFO-2`) and **76** (`PP-CENSUS`), both
**CENSUS-INPUT**, so §3.3's deadline is genuinely discharged and **G-F10-7 stays GREEN on its own
terms** — what is short is the spec-declared *content* of §1a #8, and the close's residual register.
**`h`'s HIGH reproduces at this seat, independently.**

### C.4 — E13: the four paths swept again at the close, READ-ONLY

| path | reading (this seat, 2026-09-20) |
|---|---|
| 1. `docs/tranches/V/` + `docs/tranches/V/coordination/` | newest is still the **2026-09-18** 4.1.0 quartet (parse-that addendum-2 · fourier facility19 delta · glassui r1 relay · atlas export-delta refresh); all rowed |
| 2. `../glass-ui/docs/tranches/BK/coordination/` | ⟨cmd⟩ `ls -td ../glass-ui/docs/tranches/*/ \| head -2` → `BK/` · `BJ/` — **BK still the newest tranche dir**; newest files `glass-outbound-2026-09-18-valuejs-o26-reply.md` · `valuejs-outbound-2026-09-18-kfw6-bh-relay.md`; all rowed |
| 3. `../keyframes.js/docs/tranches/V/coordination/` | newest value-addressed item unmoved since 2026-09-17; rowed |
| 4. `../sci-report/atlas/docs/tranches/P/coordination/` | newest `valuejs-inbound-2026-07-27-library-band-export-delta.md`; unmoved, rowed |

⟨cmd⟩ `find <the four paths> -name '*2026-09-20*' -o -name '*2026-09-21*'` → **0** files. **Nothing
new has landed on any path since `g`'s close sweep and `h`'s re-check, and this seat minted no mail.**

**Classification read BY POSITION, never by a bare `grep -c UNREAD`** (the standing correction),
double-run: ⟨cmd⟩ `awk -F'|' '/^\| *[IO]-[0-9]+ *\|/ { if ($6 ~ /UNREAD/) print $2 }'` → **four
rows — `O-20` · `I-31` · `I-32` · `O-39`** — the same four as at open, **every one describing a
prior or already-answered state** in its own Status cell (`O-20` SENT · `I-31` FOLDED · `I-32` READ
IN FULL + ROUTED · `O-39` SENT). The bare ⟨cmd⟩ `grep -c 'UNREAD'` answers **91**, which is this
file's own header vocabulary and prose rather than a row census; the positional row census is
⟨cmd⟩ `awk -F'|' '/^\| *[IO]-[0-9]+ *\|/{n++} END{print n}'` → **86 · 86**, tail `O-44` · `O-45` ·
`O-46`.

**A difference of INSTRUMENT, published rather than smoothed.** Sibling closes on this shared file
have published **78** (this wave's own open), **88** (KF.W13's `d` sweep) and **86** (here). The
three are not a drifting fact: they are three row-shape predicates over a file four tracks append
to concurrently, read at three instants. **This seat publishes its own predicate and its own
reading and corrects nobody's** — what matters for E13, and what reproduces under every one of
them, is **zero unrowed mail addressed to value.js and zero rows carrying a live UNREAD status in
F.W10's scope.**

**E13 verdict at the close: 0 unrowed · 0 live-UNREAD in scope. The wave does not close with
unread mail.**

### C.5 — Residuals, every one with a NAMED owner

| # | residual | grade | owner |
|---|---|---|---|
| **R-1** | **The two census-input errata never landed.** `a.9` residual 1 and `c.7` residual 1 both handed the close a ruling — *"either `c` is granted the addendum path for those two rows, or the close seat lands them, or they are explicitly re-homed"* — and **the close made no ruling and carried no residual**. At the bytes: `CENSUS-ADDENDUM-2026-08-25.md` carries one row reading **"FLAGGED, NOT BOOKED"** and ⟨cmd⟩ `grep -c 'lane-frontend.md:183'` → **0**. The wave's spec-declared §1a #8 content is short the `i-1 = L-INFO-2` lane-doc correction and the five `lane-frontend.md:183/:366/:369/:444/:565` errata for `PP-CENSUS` | **HIGH** (`h` D-1, re-verified here) | **a REPAIR seat of X.F.W10 granted the §1a #8 path** — the cure is one of the two acts already named: land the errata beside FR-CP-44 / FR-AFP-51 as §2.5a-iii(c) prescribes, **or** explicitly re-home them with the rationale written at COHESION §4.2 rows 65/76 and in this register. **This seat is VERIFY-ONLY and does neither** |
| **R-2** | **G-F10-3 honest-RED — the G24 instrument is unrunnable**, `ERR_MODULE_NOT_FOUND` on the 0.13-era `dist/value.js` path against the 4.x export map. The probe file is outside every F.W10 writable row; **not repaired, not patched, not wrapped, not skipped** | RED-by-input-state | **`X-W9.f` under `ESC-W9-G24-SUBSTRATE`** (COHESION §0ac) |
| **R-3** | **G-F10-10 honest-RED — the admin axe run of record is RED 4-of-4**, `[serious] aria-hidden-focus` ×2 on `span[aria-hidden="true"]` nodes **inside glass-ui 8.0.0's `Metric` tile**. The 17 FR-TT-1 naming legs measure **CURED**, which closes the waiver road rather than opening it | RED, producer-owned | **glass-ui, via the SS-6 relay** — banked at F.W9 as `E-F9b-2`. **Never a frontend hack to turn a gate green** |
| **R-4** | **G-F10-1's fourier-ledger leg and G-F10-2's FN-1..FN-7 verbs** stay RED. Our side's acts landed (the reconciliation letter, `I-36..I-39`, `O-29`'s FN answers); the far end has not rowed them | SPLIT | **the fourier sub-session** (COMMISSION §2) — asked, never written by us; **a verb naming a peer's act as an acceptance condition is this gate's own first falsifier** |
| **R-5** | **G-F10-4's post-window leg is UNREADABLE**, not green: today's `npm ls` exit-0 pair is the **4.0.0** serve, and the cure is 4.1.0. Reading it as the post-window figure is the gate's own named falsifier | SPLIT, deliberate | **the 4.1.0 release window** (X·V) — re-read there, never here |
| **R-6** | **G-F10-6's corpus leg is STRUCTURALLY RED**: 54 routings across 25 of 66 records reproduce, and the gate's own falsifier forbids **ANY** edit to `fr-*.md`. The green splice legs must not be read as the gate | RED-by-construction | **nobody — by design.** The record is the disposition |
| **R-7** | **G-F10-12's three RED arms** — `MetricPill` · `IntersectionObserver` · `latex-paper` = **0** across all twelve packets. Two of the three are **authored in our tree and not landed under the producer's path** (`O-23` · `O-32`, filenames still carrying `2026-09-DD`) | SPLIT | **the X formation mail seat** — the carriage hop is its act; sibling trees are READ-ONLY to every wave seat |
| **R-8** | **The five pending paired edits stand, and none was applied**: `AA-44` `9`→`8` ⊕ §5's four canonical divergences (`FR-TT-1` · the 26-vs-17 F.W9 roster spread · `PAW-49`/`PAW-12` · `PAW-50`). They sit inside the **shared §2.2 splice bytes**, where a unilateral landing breaks Δ=∅ | disclosed, not deferred | **R4-10 assigns the F.W9 roster re-basing to `F-W9`'s duty**; the rest need one paired seat writing **both twins in one commit**. Both twins are byte-unmoved (⟨cmd⟩ `git status --porcelain -- fourier/waves/` → **0**) |
| **R-9** | **L-18's two quartet gestalt passes are not satisfied.** `h` ran two passes of ONE Fable instance and said so against its own interest; the law also names two Opus-5 skeptic quartets, which did not sit | ACCEPTED withheld | **the X·F release close / orchestrator** |

### C.6 — Escalations

**NONE from this seat.** No write was attempted or made outside the close protocol's two paths;
no workaround, masking fallback, `test.skip`, allowlist, copied producer selector or `node_modules`
patch appears anywhere in this wave's twenty-six commits — ⟨cmd⟩ per sha over the union of paths,
**zero product-source files touched**. The two honest-REDs are **relieved by named owners** (R-2 ·
R-3) rather than escalated fresh, because each already carries a standing escalation id
(`ESC-W9-G24-SUBSTRATE` · `E-F9b-2` → SS-6).

### C.7 — Landed-wrong

**NONE.** Every one of the twenty-six commits touches only paths inside its unit's §1a writable set
(C.1), every commit carried its own pathspec on the `commit` itself, and the shared four-track index
swept nothing in: the union of touched paths is **eight files** and **zero** of them belong to
another track's execution record, another repo, or the product source.

### C.8 — The four-verb line, moved only as far as the spec permits

F-W10 §1 reads **AUDITED YES · SPECIFIED YES · IMPLEMENTED NO · VERIFIED NO**, with *"EXECUTION IS
NOT AUTHORIZED BY THIS FILE — nothing opens product source until the owner's begin-word."* The
begin-word came **2026-09-17** (COHESION §0j) and no product source was opened by any seat of this
wave.

| verb | before | after | authority |
|---|---|---|---|
| **AUDITED** | YES | **YES** — unmoved | registry-adjudicated, re-measured at authoring |
| **SPECIFIED** | YES | **YES** — unmoved | by the spec itself, eleven repair rounds |
| **IMPLEMENTED** | NO | **YES, WITH ONE OWED REPAIR** — seven of the eight §1a rows' declared content landed and is verified at the bytes here; **§1a #8's content is short the two errata of R-1** | this seat, over the twenty-six-commit roster |
| **VERIFIED** | NO | **NO — and it stays NO** | **not this wave's stamp to make.** `LEDGER.md`'s own standing line reads *"No row here stamps VERIFIED — only the spec's own designated seat does"*; for X·F that seat is the X-whole close, not F.W10. §3.5's declared-Fable pass **ran and returned a defect**, so nothing here even argues for it |

**ACCEPTED is NOT stamped** (L-18: the two quartet gestalt passes are not satisfied — R-9).

### C.9 — Bounds and self-count

**SELF-COUNT LAW**: this section's own extent is read from the settled bytes by a later seat and is
**not banked here** (E-19) — every roster figure above is measured from **outside** the file it
counts. **BOUNDS**: this seat wrote **exactly two paths** — this record (this `## Close` section,
appended) and `LEDGER.md` (F.W10's own row cells by minimal in-place replacement, re-read
immediately before the edit because four tracks share the file, plus one appended event line). No
artefact of `a`–`h` was opened for write; no spec byte; no `COHESION.md` byte; no `INBOX.md` byte;
no sibling tree (fourier porcelain **1 · 1**, glass-ui **0 · 0** at open and close);
`scripts/dev/dev.sh` never opened. **Pathspec commits only**, each pathspec repeated on the
`commit` itself. No `git add -A`, no `-u`, no `commit -a`, no stash, no reset, no force-push, no
cron.
