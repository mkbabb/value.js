SERVED MODEL: claude-opus-5[1m]

# F.W2 — execution record (Track C · X·F · fourier-analysis)

**Wave**: `docs/tranches/X/fourier/waves/F-W2.md` — *value.js consumption to spec: root retirement, colour routing, facility 19*
**Seat**: SEAT 0 (OPEN), `claude-opus-5[1m]`. **Sitting date 2026-09-17** (the begin-word's sitting, the convention this ledger's Track-C rows carry); **wall clock at this seat 2026-09-18** — disclosed rather than elided, per E-3.
**Authority**: the owner's begin-word 2026-09-17 (COHESION §0j), which opened execution and authorized publish/push/pull/deploy.

---

## Open

### Preconditions — verified at the bytes AND in the ledger

§1 *Opens after*: **F.W0 closed** (substrate pre-gates — hard, §6) · **F.W1 landed** for the specifier arm only (the colours arm is not so gated, fr-App C-2) · **the adjudicated registry whole**.

| conjunct | ledger reading | byte receipt | verdict |
|---|---|---|---|
| **F.W0 closed** | `LEDGER.md` row `F.W0` → **CLOSED 2026-09-17 (honest-RED: G-4 · G-5 · G-8 · G-15(d))** | ⟨cmd⟩ `ls -la /Users/mkbabb/Programming/fourier-analysis/docs/tranches/F/SUBSTRATE-LEDGER.md` → **201,985 B, 2026-09-17 15:03** — F.W0's G-11 anchor table / G-12 denominator table / G-13 pin table, the single owned authority §2b and §6b quote. F.W0's commits present in fourier: `3079a92` · `1193003` · `c429d7b` · `cddd1fa` · `8bc7736` · `9930e80` · `b3b736c` · `edee6bf` · `87ecc85` · `5842377` | **MET** |
| **F.W1 landed (specifier arm)** | `LEDGER.md` row `F.W1` → **CLOSED 2026-09-18 (honest-RED: G9 · G20(b))** | ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis log --oneline -3` → `538db90 feat(fourier)!: the atomic tri-package uplift` · `3bac3d5 fix(fourier/viz): the --viz-* palette cure at the current pin`. Installed pins re-measured: ⟨cmd⟩ `node -p "JSON.stringify(require('…/web/package.json').dependencies)"` → `@mkbabb/glass-ui ^8.0.0` · `@mkbabb/keyframes.js ^6.0.0` · `@mkbabb/value.js ^4.0.0`; installed value.js **4.0.0**, exports `./color ./value ./css ./easing ./math ./transform ./quantize`. F-W1's own close record states it: *"the F.W1 conjunct is GREEN — the specifier arm IS the atomic transaction and it landed at `538db90`"* (`execution/C/F-W1.md`, **Successor waves** table, `F.W2` row) | **MET** |
| **adjudicated registry whole** | — | ⟨cmd⟩ `ls docs/tranches/V/megatranche/registry/adjudicated/ \| wc -l` → **232** entries (66 `fr-*.md` + `library-band.md` + the band's siblings), immutable under E-1/E-3 | **MET** |

**G10's sequencing condition is therefore satisfied**: the value.js-direct specifier arm is no longer un-landable — the pin moved inside F.W1's one atomic transaction, exactly as §6a and the runbook §1.3 edge *"F.W1 → F.W2, partially"* require.

**Worktree substrate.** fourier `538db90` on branch `m/w1-bump-migration`, **0 dirty entries** (⟨cmd⟩ `git status --porcelain \| wc -l` → `0`, double-run) — the spec's authoring-time *"28 dirty worktree entries"* is SUPERSEDED by F.W0's G-1 LAND (`1193003`) and is retained here only as the history of a superseded reading. value.js `tranche-u`; the dirty paths in the value.js tree (`demo/**` ×10 · `docs/tranches/V/reformation/CARRY-LEDGER.md` · `scripts/dev/dev.sh` · the untracked X evidence dirs and two e2e specs) are **outside this wave's writable set** and belong to sibling seats — untouched, never staged, and `scripts/dev/dev.sh` never touched at all.

### CRASH-RECOVERY sweep (standing law)

⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain` → **∅**. ⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js status --porcelain` → 12 modified + 4 untracked, **none inside F.W2's §2a writable set**: no `docs/tranches/X/fourier/RULINGS-F.W2.md`, no `facility-19.md`, no `docs/tranches/X/coordination/value-to-*`, no `app-wave/fourier-viz-*.png`, and `docs/tranches/V/coordination/INBOX.md` is clean. **No killed predecessor seat's partial work exists on this unit.** Nothing inherited; nothing stashed; nothing restored.

### E13 Step-0 — the four-path mail sweep

Swept read-only at this seat's own clock, compared against **every row** of `docs/tranches/V/coordination/INBOX.md`, classification taken from each row's own **Status cell** and never from the filename.

| # | path | entries | newest | rowed? |
|---|---|---|---|---|
| 1 | `docs/tranches/V/` + `docs/tranches/V/coordination/` | 19 + 19 (`INBOX.md` self-excluded, SELF-COUNT law) | `valuejs-outbound-2026-09-18-kfw7-bh-relay.md` (09-18 01:41) | **YES — O-28** (ours, outbound) |
| 2 | `../glass-ui/docs/tranches/BK/coordination/` | 9 | `glass-outbound-2026-09-18-valuejs-o26-reply.md` (09-18 14:41) | **YES — I-35**, rowed 2026-09-18 at X-W1 RESUME; its Routing cell reads *"X·KF (Track B), NOT X-W1"* |
| 3 | `../keyframes.js/docs/tranches/V/coordination/` (the SACRED checkout — read-only) | 12 | `VALUEJS-INBOUND-2026-09-17-o8-o11-amendment-addendum.md` | **YES — our own O-21** |
| 4 | `../sci-report/atlas/docs/tranches/P/coordination/` | 28 | `valuejs-inbound-2026-07-27-library-band-export-delta.md` | **YES** (ours, outbound) |
| +C | the Track-C fourier mail-ledger surface (COHESION §0k.1) `../fourier-analysis/docs/tranches/F/coordination/` | 4 (`INBOX.md` + 3 letters) | `INBOX.md` 2026-09-17 13:13; no letter newer than 2026-05-29 | **YES — `M-1`…`M-4` all rowed**; M-1 → F.α, M-2 → F.W3, M-3 ANSWERED-DISCHARGED, M-4 LOGGED at `cddd1fa`. None is F.W2's |

**BK re-confirmed the newest glass-ui tranche dir** — ⟨cmd⟩ `ls -dt ../glass-ui/docs/tranches/*/ \| head -3` → `BK/` · `BJ/` · `BI/`.

**Result: 0 unrowed · 0 new `I-n` minted · I-35 remains the inbound tail · 0 UNREAD addressed to F.W2's scope.** The five rows whose status cells literally read `UNREAD` are **I-31/I-32/I-34 → X-W0 (Track A)**, **I-33 → the X formation mail seat** (whose Routing cell has that seat relay each sibling's section onward, *"fourier → X·F's mail-ledger surface"* — an obligation on the formation seat, not on this wave's units), and **I-35 → X·KF (Track B)**. `INBOX.md` is **not** edited by this seat; the wave's own E13 rows (the FN-1..FN-7 reply and the facility-19 relay) are unit `.d`'s append-only acts, per §2a.

---

## Baseline — the §5 gates, read-only, at wave open

All 20 gates are declared **born RED** by §5. Every figure below was measured at this seat's clock and **double-run**; the two runs agree byte-for-byte. Commands are literal and absolutely pathed; the fourier tree was read only (grep · `ls` · `sed` · node module read) — **no witness wrote the fourier tree.**

### R.2 — the born-RED declaration versus the measured tree

**F.W1's landing moved five of this wave's gates before F.W2 opened.** That is a finding, minuted here rather than absorbed: the spec's born-RED table was authored 2026-08-28 against the pre-uplift tree, and ESC-4 (COHESION §0o, 2026-09-18 — *"DRIFT REFUSED — no acceptance, silent or explicit"*) released to F.W1 *"the six bare-root import lines"* and *"limb 1's lockstep half"*. F.W2 inherits the landing; it does not claim the GREEN (FR-GIG-5, and §1b's own bar).

| gate | §5 born-RED | measured now | reading |
|---|---|---|---|
| **G1** every specifier resolves at the target | RED — 5 of 5 sites `ERR_PACKAGE_PATH_NOT_EXPORTED` | ⟨cmd⟩ `/usr/bin/grep -rn 'from "@mkbabb/value\.js"' /Users/mkbabb/Programming/fourier-analysis/web/src/ \| /usr/bin/wc -l` → **0**. Every value.js import in the tree is subpath-keyed: `ConvergencePlot.vue:5` · `useCurveTransition.ts:8` · `harmonics.ts:5` → `@mkbabb/value.js/easing`; `easings.ts:35` → `@mkbabb/value.js/easing`; `colors.ts:20` → `@mkbabb/value.js/color`; `colors.ts:21` → `@mkbabb/value.js/css` | **GREEN BEFORE ITS CURE** — landed at `538db90`/`3bac3d5` under ESC-4 |
| **G2** the `timingFunctions` mapping is re-derived, not transcribed | RED — leg 2 RED; both symbols 0 hits | value.js side unchanged: ⟨cmd⟩ `/usr/bin/grep -rn 'easingNames\|timingFunctions' src/ dist/ \| /usr/bin/wc -l` → **0** (the RED *input* still holds — there is nothing to transcribe). fourier side: `web/src/lib/easings.ts` now carries the re-derivation in its own docblock — *"The eight keys the producer still publishes analytically are imported from `@mkbabb/value.js/easing`; the other fourteen are DEFINED HERE, each reproducing the pre-bump 0.13.0 function EXACTLY"* — with a `cssCubicBezier` solver and eight named imports (`linear, easeInOutQuad, easeOutCubic, easeInOutCubic, easeInOutSine, easeOutExpo, easeInOutExpo, easeInOutCirc`) | **CURE LANDED, RECORD OWED** — the mapping exists; §2a still owes its recording in `RULINGS-F.W2.md` (unit `.c`) |
| **G3** the oklch/light-dark tokens resolve | RED — 5/6 → `#888888` | ⟨cmd⟩ `node docs/tranches/V/megatranche/audit/probes/fourier-vizcolor-oklch.mjs` → **5/6 RED**, but the probe names `glass-ui@4.0.0 :263/:264/:265/:245/:145` while fourier now installs **8.0.0**, and it simulates the `cssVarToHex` regex path that **no longer exists** in `colors.ts`. **The reading is STALE, not a measurement** | **UNMEASURED — a D-19 finding** (see below). Green owner is F.W1's; F.W2 owns the deletion of the arms that caused it |
| **G4** the SNAPSHOT is fixed, not the parser swapped | RED — `color: VIZ_COLORS.fourier` copied into a plain object literal at module evaluation | ⟨cmd⟩ `/usr/bin/sed -n '1,40p' …/visualization/lib/basis-display.ts` → `export const basisDisplay: Record<string, BasisDisplay> = reactive({ fourier: { …, color: computed(() => VIZ_COLORS.fourier) }, … })`, with a docblock stating the cure in its own words (*"The colour is DERIVED from `VIZ_COLORS`, never copied out of it"*). No value copy survives | **GREEN BEFORE ITS CURE** — F.W1's banked D-2/BC-5/C-3 fix; F.W2 keeps only the LOCK and the green-criterion |
| **G5** the `hexToRgba` deletion lands with its join | RED — **5 call sites / 3 files** | ⟨cmd⟩ `/usr/bin/grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ \| /usr/bin/grep -v 'export function'` → **5 lines**, the same five coordinates: `BasisCanvas.vue:261` · `epicycles.ts:258` · `epicycles.ts:283` · `golden-shimmer.ts:55` · `golden-shimmer.ts:58` | **RED — reproduces exactly.** The enumeration IS the figure |
| **G6** the declared hex residual has a validation arm and a deletion date | RED — zero validation exists; `colors.ts` is exactly 117 lines | ⟨cmd⟩ `/usr/bin/wc -l < …/web/src/lib/colors.ts` → **182** (the 117 is superseded by `3bac3d5`). A validation arm now EXISTS: `hexChannels()` opens `if (!HEX_COLOR.test(hex)) { throw new TypeError(…) }`, and both survivors route through it. **The deletion date is still unwritten** | **HALF-GREEN BEFORE ITS CURE** — validation landed, the RD-6/RD-7 deletion date (fourier's 4.1 adoption) is still owed |
| **G7** the charter names every hand-rolled colour authority | RED — six authorities against a charter that names one | ⟨cmd⟩ `ls docs/tranches/X/fourier/RULINGS-F.W2.md` → *No such file or directory* | **RED** — ruling gate, unit `.c` |
| **G8** the cure-seat contest is ruled before ACT(2) is authored | RED — `./dom` one of glass-ui's **80** published subpaths | ⟨cmd⟩ `node -p "…glass-ui/package.json).exports…"` → **8.0.0 · 70 keys · `./dom` present: true** (the 80 was a 4.0.0 figure; re-measured at the ADOPTED pin per D-19/G-13). ⟨cmd⟩ `ls …/glass-ui/dist/composables/dom/useTokenColor.d.ts` → present. **And the seat is already ADOPTED in the tree**: `colors.ts:20` → `import { createTokenColorCache } from "@mkbabb/glass-ui/dom"`. ⟨cmd⟩ `ls docs/tranches/X/fourier/RULINGS-F.W2.md` → absent | **RED — ruling gate**, and a finding: R-ii's *presumptive* seat shipped in the tree before F.W2 ruled it. The ruling is still owed and is not retro-fitted to the landing |
| **G9** the post-uplift parser constraint is measured, not transcribed | RED — 0 hits, both symbols deleted | ⟨cmd⟩ `/usr/bin/grep -rn 'colorUnit2\|color2' src/ dist/ \| /usr/bin/wc -l` → **0**, reproduces. The 4.0.0 `oklch()`/`light-dark()` probe of `parseCssColor` is still unrun | **RED** — the measurement is owed (unit `.d`) |
| **G10** the value.js-direct specifier arm does not attempt to land alone | RED — the pin is held from BELOW | Pins now `glass-ui ^8.0.0` · `keyframes.js ^6.0.0` · `value.js ^4.0.0`; installed value.js **4.0.0** | **DISCHARGED BY F.W1** — the atomic transaction moved the pin; the arm did not land alone |
| **G11** the denominator is colour-value-keyed, not import-keyed | RED — no such census exists | No colour-VALUE census artefact in tree. The three blindness instances re-measured live: ImageUpload's 6 `STATIC.rainbow` hexes · ContourEditorCanvas's **8** inline amber attributes · CanvasControlsDock's two `--viz-amber` reads with zero imports | **RED — reproduces** |
| **G12** the π probe pair exists | RED — no such files exist | ⟨cmd⟩ `ls docs/tranches/V/megatranche/audit/probes/app-wave/` → `BORN-RED-2026-07-27.json` · `app-shell-truth-probe.mjs` — **the four PNGs are absent** | **RED — reproduces** |
| **G13** facility 19 exists under RD-8's one-way law | RED — `facility-19.md` absent; the correspondence grep returns **1 line in 1 file** | ⟨cmd⟩ `ls docs/tranches/X/fourier/facility-19.md` → *No such file or directory*. ⟨cmd⟩ `/usr/bin/grep -rniE 'megatranche\|facility-19\|library-band' /Users/mkbabb/Programming/fourier-analysis/docs/ \| /usr/bin/wc -l` → **7** (was 1; the F.W0-era mail-ledger surface and SUBSTRATE-LEDGER added six). The figure moved; **the RED state did not** | **RED — the artefact is absent.** The 1→7 drift is minuted, not averaged |
| **G14** the substrate compiles and the anchors are re-ground | RED — 28 dirty entries; `git cat-file -t 14d83356` → not a valid object | fourier worktree **0 dirty**; F.W0 closed its own gates and published SUBSTRATE-LEDGER.md | **F.W0's gate; F.W2 states it and never closes it.** Its RED input is discharged at F.W0's end |
| **G15** the easing migration declares its curve drift in the BANKED figures | RED — `14 of 22 drift analytic→CubicBezier, of which 8/22 are MATERIAL (max Δ 0.192)` | **The banked figure is SUPERSEDED BY OWNER RULING.** ESC-4 (COHESION §0o): *"DRIFT REFUSED — no acceptance, silent or explicit … The gate is MPC-5's sampler re-run: every one of the 22 samples EQUAL (Δ = 0 at every sample point) … No CubicBezier approximation is admitted for any key."* `easings.ts`'s docblock records the execution of that ruling | **RED — RE-SHAPED BY RULING.** G15 can no longer close on a declared 14/22 divergence; it closes on the Δ = 0 re-run, recorded as a dated E-3 addendum-beside (unit `.a`). **Cite ESC-4; never re-open it** |
| **G16** `easing()` is called once, never on the render path | RED — `false`; fresh closure per call | ⟨cmd⟩ `node -e "import('./dist/subpaths/easing.js').then(m=>{…})"` → `identical: false`. Neutralized by shape: `easings.ts` builds its catalogue once at module scope | **RED as measured; neutralized by shape** — the RED input (per-invocation `easing(name)`) must not be re-armed |
| **G17** the deletion count is honest | RED — `hexToRgb` has ZERO call sites outside its own declaration | ⟨cmd⟩ `/usr/bin/grep -rn 'hexToRgb(' …/web/src/ \| /usr/bin/grep -v hexToRgba` → **1 line**, `colors.ts:180`, its own declaration. A dead export | **RED — reproduces** |
| **G18** the value.js-side reciprocal is filed, not folded into fourier's ledger | RED — the filing does not exist | No filing in tree | **RED — reproduces** |
| **G19** census closure against `CENSUS-CANONICAL.md` | RED — the difference was non-empty | ⟨cmd⟩ `/usr/bin/grep -n '^### F\.W2' docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` → `### F.W2 — **23 rows**` *(errata round 5 → read 31; errata round 6 E6-3 → read 27; 4 killed-claims rows → TERMINAL)*. The set-difference against §8a's 23 bookings is **unrun at this open** | **RED** — the closure is owed (unit `.d`); the operand is the canonical and **nothing else** (R4-10) |
| **G20** the banked easing cluster is CITED, never re-minted; its two cure-shape locks dispositioned | RED — `RULINGS-F.W2.md` absent | ⟨cmd⟩ `ls docs/tranches/X/fourier/RULINGS-F.W2.md` → absent | **RED** — ruling gate, unit `.c`. **And R-iii is now partly overtaken**: ESC-4 ruled the easing cure-shape at F.W1's end, exactly as R-iii routed it. `.c` records the routing AND the ruling that answered it |

### Findings banked at open (not gates)

**F-1 · BOTH SPINE PROBES ARE STALE AT THE UPLIFTED TREE (D-19 / MEASURE-AT-OPEN).** §5's Denominator note rests the wave's two "live executable probe" witnesses on `fourier-value-import-drift.mjs` and `fourier-vizcolor-oklch.mjs`. Measured this seat:

- ⟨cmd⟩ `node docs/tranches/V/megatranche/audit/probes/fourier-value-import-drift.mjs` — **leg 1 prints five bare-root site coordinates that no longer exist in the tree** (`easings.ts:9`, `:16`, `ConvergencePlot.vue:5`, `useCurveTransition.ts:8`, `harmonics.ts:5` — every one now subpath-keyed), i.e. a frozen claim, not a reading; **leg 3 CRASHES** — `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js/dist/value.js'`, because the installed pin is now 4.0.0 and 4.0.0 publishes no `dist/value.js`. **The gate's drift leg cannot execute at all.**
- ⟨cmd⟩ `node docs/tranches/V/megatranche/audit/probes/fourier-vizcolor-oklch.mjs` → `5/6 tokens collapse to the #888888 fallback`, attributed to `glass-ui@4.0.0` line anchors and to the `cssVarToHex` regex path **that `3bac3d5` deleted**.

**Consequence, stated and not worked around**: G1/G2/G15's and G3's named witnesses are **evidence of the pre-uplift tree**. The cure is a root-cause one — the probes are re-grounded at the adopted pin as part of the unit that owns the gate (`.a` for the drift probe, `.b` for the vizcolor probe), each re-grounding landing as a **dated E-3 addendum-beside**, never a patch of the dated evidence. **No gate may be read GREEN off a stale probe**, and no probe may be silenced, skipped or allowlisted to make one read GREEN.

**F-2 · G8's PRESUMPTIVE SEAT SHIPPED BEFORE THE RULING.** `createTokenColorCache` from `@mkbabb/glass-ui/dom` is live at `colors.ts:20` under `538db90`/`3bac3d5`, which is R-ii's disposition executed. The ruling record is nevertheless owed in full and is **not** to be written as a ratification of a landing: R-ii's two escalated conditions (SS-6's sanctioned-seat confirmation; SS-13's `getComputedStyle` oklch-vs-rgb serialization) stand exactly as written, and `.d`'s SS-6 relay still carries the ask.

**F-3 · G15's BANKED FIGURE IS SUPERSEDED BY ESC-4, AN OWNER-DELEGATED RULING.** The spec's G15 closes on *"a probe-pinned declared divergence row in the banked figures"* (14/22 · 8/22 material · max Δ 0.192). COHESION §0o's ESC-4 **refuses the drift** and sets the gate at Δ = 0 on all 22 keys. F.W2 **cites ESC-4 by id and never re-opens it**; the amendment lands as a dated addendum-beside to `F-W2.md` §5, per E-3 (addenda, never patches).

**F-4 · TWO §5 FIGURES MOVED WITHOUT CHANGING A GATE'S STATE** — `colors.ts` 117 → **182** lines; the G13 correspondence grep 1 → **7** lines; glass-ui's exports keyset 80 (@4.0.0) → **70** (@8.0.0). Each is re-stated at the measured value in the table above; **no superseded figure is carried forward as current** (G-12).

---

## Unit plan

**§1 Units: 4** — `.c` rulings (serial, first) → `.a` specifier+easing ∥ `.b` colours (parallel; no shared modify path) → `.d` facility 19 + the letter. **Peak concurrency 2**, inside the owner's four-workflow cap. The spec names **no** Fable, fresh-Fable, adjudicator or design-author seat → all four seats are **Opus** (M-12 TRI-FOLD: Opus solo for mechanical/challenge seats; runbook §5.1).

**Ordering law (§6a Author-before-choose)**: *"unit `.c` (R-i/R-ii/**R-iii** + the G2/G15 mapping) **before** `.b` authors ACT(2), and **R-iii before `.a` authors ACT(1)**"*. `.c` is therefore strictly first and alone. `.d` is last (its ledger and its letter consume what `.a`/`.b` land, and its G19 closure reads the settled bookings).

**Disjointness of the parallel pair** — `.a` modifies `easings.ts` · `ConvergencePlot.vue` · `useCurveTransition.ts` · `harmonics.ts`; `.b` modifies `colors.ts` · `golden-shimmer.ts` · `epicycles.ts` · `BasisCanvas.vue` · `ContourPreview.vue` · `ContourEditorCanvas.vue` + the four PNGs. **Intersection ∅.** `harmonics.ts`'s `spectrumColor` fork (`:81`) is **locked, never edited here** — its collapse is F.W3 `.d`'s and its execution half F.W4's (§X.1-v4 · `CENSUS-CANONICAL.md`).

| group | units |
|---|---|
| **1** | `.c` |
| **2** | `.a` ∥ `.b` |
| **3** | `.d` |

### Unit `.c` — the rulings record

- **Model**: opus
- **Sections**: §4 *Rulings — the three contests this wave owns* (`:229-242`) · §2a's `RULINGS-F.W2.md` create row (`:79`) · §5 gates **G7** (`:258`), **G8** (`:259`), **G20** (`:271`), **G2** (`:253`), **G15** (`:266`) · §6a *Author-before-choose* (`:292`) · §3 A2/A6 (`:134`, `:142-154`), C1 (`:192`), C2 (`:194`), B9 (`:176`)
- **Writable**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/RULINGS-F.W2.md`
- **Gates**: G7 · G8 · G20 (+ records the G2/G15 mapping re-derivation)
- **Locks**: strictly first, alone; R-iii must exist before `.a` authors ACT(1) and the whole record before `.b` authors ACT(2). One commit.

### Unit `.a` — specifier + easing

- **Model**: opus
- **Sections**: §3 Arm A A1–A7 (`:130-156`) · §2a fourier-edit rows 1–4 (`:94-97`) · §5 **G1** · **G2** · **G10** · **G15** · **G16** · §6a *Same-commit riders* (`:287`) · §2b items 3 · 7 (`:115`, `:119`) · §6b's F.W1 and X-W9 rows
- **Writable** (fourier, D-15 direct-edit grant): `web/src/lib/easings.ts` · `web/src/components/equation/ConvergencePlot.vue` · `web/src/components/equation/composables/useCurveTransition.ts` · `web/src/components/equation/lib/harmonics.ts`; plus a dated addendum-beside under `docs/tranches/X/fourier/` for the G15 re-shape and the drift-probe re-grounding
- **Gates**: G1 · G2 · G15 · G16 (G10 discharged by F.W1, re-stated not re-closed)
- **Locks**: A2's re-derived mapping + A3's `lerp` re-point travel in ONE commit with the specifier edit; anti-rename (`L/B-1 + C/B-1` is F.W1's identity, cited never re-minted); ESC-4 is RULED — cite, never re-open; `harmonics.ts:81`'s `spectrumColor` is locked, not edited

### Unit `.b` — colours

- **Model**: opus
- **Sections**: §3 Arm B B1–B13 (`:158-188`) · §2a fourier-edit rows 5–10 (`:98-103`) + the four-PNG create row (`:83`) · §5 **G3** · **G4** · **G5** · **G6** · **G11** · **G12** · **G17** · §6a *Same-commit riders* · *One cut, B1⊕B2⊕B3* · *NEVER-clause B11/K-8* · *Anti-rename law, B5* (`:287-291`) · §6b's F.W3/W4 and SS-13 rows
- **Writable** (fourier): `web/src/lib/colors.ts` · `web/src/lib/golden-shimmer.ts` · `web/src/components/visualization/lib/canvas-drawing/epicycles.ts` · `web/src/components/visualization/BasisCanvas.vue` · `web/src/components/visualization/ContourPreview.vue` · `web/src/components/visualization/ContourEditorCanvas.vue`; (value.js): `docs/tranches/V/megatranche/audit/probes/app-wave/fourier-viz-{light,dark}-{before,after}.png` (force-added past `.gitignore:34 *.png`) + a dated addendum-beside for the vizcolor-probe re-grounding
- **Gates**: G5 · G6 · G11 · G12 · G17 (G3/G4 are F.W1's GREEN — `.b` owns only the lock and the green-criterion, and claims neither)
- **Locks**: the `hexToRgba` deletion lands in ONE commit with the B7 join across **five call sites in three files** and the validated residual; the amber cut is ONE cut — **nine** sites / two files with the `--contour-stroke` mint; B2's literal is cured **by name**, never swept as collateral; CSS-expressible colour never routes through the JS resolver being deleted; ONE ramp identity `M-β1`, execution F.W4's

### Unit `.d` — facility 19 + the letter + closure

- **Model**: opus
- **Sections**: §3 Arm F F1 (`:225`) · Arm E E2 (`:221`) · §2a rows 2 · 4 · 5 · 6 (`:78`, `:80-82`) · §5 **G9** · **G13** · **G18** · **G19** · §6b's `X·V / V·π`, `GLASS-RELAY (SS-6)` and `SS-13` rows (`:303`, `:305-306`) · §7 *Excluded* (`:310-351`) · §8a–§8d (`:352-465`)
- **Writable**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/facility-19.md` · `/Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/value-to-fourier-2026-09-18-fn-answers.md` · `/Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/value-to-glassui-2026-09-18-token-color-seat.md` · `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` (**append-only**, no rewrite of any existing row)
- **Gates**: G9 · G13 · G18 · G19
- **Locks**: RD-8's **one-way** law, never the bidirectional schema of `API-FACILITY-ISOMORPHISM.json`; `counts` annotated as-specified **with** an as-built census and its command; FN-5 names SS-4's owner-gated G4 and **pre-empts no ruling**; FN-7 is F.W5's both-ends; the `DD` in the two filenames resolves to the **execution date, never back-dated** (the F.W6 O-27 precedent); G19's operand is `CENSUS-CANONICAL.md` §2's F.W2 roster **verbatim and nothing else** — no detector, no re-cut, no check file; a non-empty difference after two closure passes is a triumvirate halt, never a redispatch

---

## Unit receipts

*(empty at open — each unit appends its own receipt here)*

### `.c` — the rulings record

**Seat**: unit `.c`, `claude-opus-5[1m]`, sitting 2026-09-17 (wall clock 2026-09-18, 15:0x–15:5x EDT). **Writable set (hard bound)**: `docs/tranches/X/fourier/RULINGS-F.W2.md` — one path, one commit. **Strictly first and alone** (§6a *Author-before-choose*): nothing of `.a` or `.b` is authored, and no fourier byte was written — every fourier-side witness below is a read (`grep` · `sed` · `ls` · node module read).

**CRASH-RECOVERY sweep (standing law).** ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain` → **∅**. ⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js status --porcelain` → 12 modified ⊕ 4 untracked, **none inside this unit's writable set**: `RULINGS-F.W2.md` was **ABSENT** (⟨cmd⟩ `/bin/ls /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/` → `carry` · `conformance` · `contract` · `evidence` · `waves`, no `RULINGS-F.W2.md`). **No killed predecessor seat's partial work exists on this unit; nothing inherited, nothing stashed, nothing restored.** `scripts/dev/dev.sh` untouched.

**E13 (unit-level).** Swept at this seat's clock, delta against the X-W1-resume sweep (14:2x) — ⟨cmd⟩ `/usr/bin/find <each of the four paths ⊕ `../fourier-analysis/docs/tranches/F/coordination`> -maxdepth 1 -type f -name '*.md' -newermt "2026-09-18 14:30"` → `docs/tranches/V/coordination/INBOX.md` (**self**, SELF-COUNT law) and `../glass-ui/docs/tranches/BK/coordination/glass-outbound-2026-09-18-valuejs-o26-reply.md` (**already rowed I-35**; Routing cell *"X·KF (Track B), NOT X-W1"*). **0 unrowed · 0 new `I-n` · 0 UNREAD in `.c`'s scope.** `INBOX.md` not written by this seat.

#### Acts, in order

**ACT 1 — measure before writing (D-19 / MEASURE-AT-OPEN).** Every predicate the three rulings rest on re-measured at today's bytes, each double-run:

- corpus quotations reproduced at the frozen bank — ⟨cmd⟩ (base `…/registry/adjudicated/`) `/usr/bin/sed -n '57p' fr-AdminAuditLog.md` (AA-20) · `'46p' fr-MorphPhaseConfig.md` (MPC-2) · `'58p' fr-EasingCurvePreview.md` (CENSUS-MC) · `'72p' fr-EquationView.md` (M-RTC) · `'52p' fr-EasingPicker.md` (L/B-1 + C/B-1) · `'23p'`+`'50p'` `fr-EquationPanel.md` · `'41p' fr-InfoCard.md` · `'47p' fr-ImageUpload.md` · `'55p' fr-ContourPreview.md` — **all reproduce byte-exactly**.
- the six G7 authorities enumerated live in the fourier tree at `538db90`: `actionTone` `AdminAuditLog.vue:67`/`:135` · `energyColor` `notation.ts:44` with consumers `EquationPanel.vue:6`/`:38` ⊕ `EquationView.vue:7`/`:70` · `spectrumColor` **4 declarations** (`transforms.ts:3` · `harmonics.ts:81` · `FrequencyGraph.vue:42` · `CoefficientsSpectrum.vue:55`) · the NotationPills triad `notation.ts:15-17` → `NotationPills.vue:24` → `:43-45` · ImageUpload's six hexes over seven coordinates `:150-156` ≡ `colors.ts:28-29` · the amber **1 + 8 = 9** at `ContourPreview.vue:45` ⊕ `ContourEditorCanvas.vue:261,:307,:308,:315,:319,:328,:329,:333`.
- the G8 facility at the **ADOPTED** pin: glass-ui **8.0.0**, `./dom` present, `resolveTokenColor`/`createTokenColorCache`/`useTokenColor` in `dist/dom.js`, both `composables/dom/*.d.ts` present; **the seat already ships at `colors.ts:20`** and the three regex arms are gone from the file.
- the G2/G15 mapping at both landed ends: `easing(name): Result<…>` `src/easing.ts:166`, DIRECT-then-PRESETS order, `DIRECT_EASINGS` `:94-114` **19 entries**, `./easing` **16 exports**, `bezierPresets` **30 keys**; the 22 catalogue keys partitioned **by identity** → **8 DIRECT / 14 fall-through**, enumerated; fourier's landed `easings.ts` imports exactly those **8** and defines the **14** (6 bézier-authored ⊕ 8 analytic), `EASING_FNS` exhaustive, the `as EasingFn` cast gone.

**ACT 2 — author `RULINGS-F.W2.md`.** §0 substrate · **§1 R-i** (WIDEN, the complete six-authority roster, each **widened WITH its reason**, zero exclusions, cures homed, zero minted; §1.3 answers the seventh-authority question at the bank — `TIER_INFO` folds into **M-N1**, not a new row; §1.4 records two banked-figure divergences; §1.5 records two drifted anchors) · **§2 R-ii** (producer facility = presumptive seat; ACT(2)'s `parseCssColor` route narrowed to **non-token STRINGS**; **SS-6 and SS-13 both escalated and INTACT**, measured unanswered; **§2.3 handles finding F-2** — a ruling on its own reasons, never a ratification of the landing, with G8's RED input measured *not* to have occurred) · **§3 R-iii** (MPC-2 quoted at `fr-MorphPhaseConfig.md:46` and CENSUS-MC at `fr-EasingCurvePreview.md:58`, each with **F.W1** named as home; the **§2a-rows-1–4 collision** stated; **G1's 5-of-5 `ERR_PACKAGE_PATH_NOT_EXPORTED`** on the record; **no pre-emption**; **§3.3 minutes that COHESION §0o ESC-4 has since answered it at F.W1's end**, quoted with two disclosed cuts, cited by id and never re-opened; §3.4 minutes one emphasis divergence in the dated spec as a note **beside**, never a patch) · **§4** the `easing()` mapping **re-derived at the landed bytes** · §5 gate readings · §6 residuals.

**ACT 3 — lawfulness pass over the settled bytes, double-run.** ⟨cmd⟩ `/usr/bin/grep -oE '⟨cmd⟩ `[^`]*\$[^`]*`' RULINGS-F.W2.md` → **no output** (no command consumes a shell variable) · ⟨cmd⟩ `/usr/bin/grep -o '…/[a-zA-Z@]' RULINGS-F.W2.md` → **no output** (no elided command path; two were found on the first pass, at §4.3's `sed` and §5's G7 cell, and **completed to literal absolute paths** before commit — the R3-9.1 unrunnable-machinery class, caught by the file's own law) · ⟨cmd⟩ `/usr/bin/grep -n 'grep -[a-z]*P' RULINGS-F.W2.md` → **no output** (BSD-portable; no PCRE, no lookaround) · ⟨cmd⟩ `/usr/bin/grep -noE 'F-W[0-9]+\.md:[0-9]|COHESION\.md:[0-9]' RULINGS-F.W2.md` → **no output** (no line coordinate into a live sibling; siblings cited by §-heading, gate id or row label). The §4.2 partition command was re-run **from a neutral cwd** to prove it carries no implicit base → `DIRECT 8 …` · `FALLTHROUGH 14 …`, identical both runs.

**ACT 4 — commit (one commit, pathspec on the commit itself).** `b469b989` — *docs(x-f-w2/.c): RULINGS-F.W2 — R-i WIDEN (six-authority G7 roster) · R-ii M-RTC presumptive seat · R-iii routed, ESC-4 minuted · the G2/G15 easing() mapping re-derived at the landed bytes*. ⟨cmd⟩ `git show --stat --oneline b469b989` → **1 file changed, 239 insertions(+)**, the file being `docs/tranches/X/fourier/RULINGS-F.W2.md` and nothing else — **no sibling seat's staged path swept in**.

#### Gate readings — BEFORE → AFTER

| gate | BEFORE (wave open) | AFTER (this unit) | witness |
|---|---|---|---|
| **G7** | **RED** — `/bin/ls …/RULINGS-F.W2.md` → *No such file or directory*; six authorities against a charter naming one | **GREEN** | the record's **§1**: R-i recorded with the complete six-authority roster, each **widened with its reason**, zero exclusions, cures homed at F.W4 / F.W3 / F.W3 `.d`+F.W4 / `.b`, **zero minted** |
| **G8** | **RED** — ruling absent; facility verified live (and already shipping at `colors.ts:20`, finding F-2) | **GREEN** | the record's **§2**: R-ii recorded; ACT(2) narrowed to non-token strings; **SS-6 · SS-13 intact**; the landing ruled on, not ratified; the gate's RED input (*re-implements a producer surface*) measured **false** at the landed bytes |
| **G20** | **RED** — `/bin/ls …/RULINGS-F.W2.md` → absent; the locks had no record | **GREEN** | the record's **§3**: both locks quoted at their coordinates with **F.W1** named, the §2a-rows-1–4 collision stated, **G1's 5/5** on the record, no pre-emption, ESC-4 minuted as the answer |
| **G2** | **RED** — both prescribed symbols 0 hits; the mapping had no record | **record leg GREEN**; gate closure stays `.a`'s | the record's **§4**: the mapping re-derived at both ends (8/14 by identity, resolution order, landed catalogue) |
| **G15** | **RED** — and RE-SHAPED: the banked `14/22 · 8/22 · max Δ 0.192` superseded by **ESC-4** (Δ = 0, all 22) | **record leg GREEN**; the Δ = 0 sampler re-run stays `.a`'s | **§3.3** cites ESC-4 by id and never re-opens it; **§4.2** enumerates the partition |

**No other gate is touched, and no GREEN of F.W0/F.W1/F.W3/F.W4/W.L6 is claimed** (FR-GIG-5).

#### Findings this unit adds to the record

- **F-5 · `energyColor`'s banked blast radius is 3 consumers / 2 routes; the tree has 2 / 2.** `InfoCard.vue` was DELETED under **COHESION §0j.D's `G-10`** at fourier `5842377`, so `InfoCard.vue:5/:14` is no longer part of F.W4's regression surface. **Grade, identity and cure home untouched** — a divergence recorded, never a re-grade or a re-book.
- **F-6 · `fr-ContourPreview` row 22 disagrees with itself** — its ADJUDICATED cell says *cure ten sites*, its description cell says *9 sites*, and the tree says **nine** (1 + 8). `.b` cures nine and mints no eleventh interpretation.
- **F-7 · two roster anchors drifted under `3bac3d5`** — `CoefficientsSpectrum.vue:47 → :55`, `STATIC.rainbow colors.ts:13-16 → :27-30` (hexes `:28-29`). INTENT resolves at the true bytes; **no dated spec is patched** (E-3).
- **F-8 · `TIER_INFO` is not a seventh colour authority** — the bank folds its three literals into **M-N1** with `energyColor`'s three (`fr-InfoCard.md:41`'s `:26/:32/:38 ≡ :45/:46/:47`, reproducing byte-for-byte at HEAD). The widened charter reaches them through roster member 2; **no row minted**.
- **F-9 · the dated spec's §4 R-iii renders MPC-2's parenthetical bold-and-italic where the bank sets no emphasis** — the R4-7.1 class the spec polices elsewhere. Minuted at the record's §3.4, **beside**, never propagated and never patched.

#### Residuals and escalations

**Escalations: none.** Every act the unit's sections prescribe was performed as specified, at the bytes.

Residuals carried to their owners: **SS-6** and **SS-13** remain OPEN (unit `.d`'s relay; glass-ui READ-ONLY always) · **G15's Δ = 0 sampler re-run** and **G2's cure-side reading** are `.a`'s · the **dated addenda-beside** the measurements imply (G15's ESC-4 re-shape; glass-ui 80 → 70 keys; `colors.ts` 117 → 182 lines; the two drifted anchors; `energyColor` 3 → 2 consumers) land as **addenda, never patches** · `fr-ContourPreview`'s banked *ten* stands as a banked divergence against the tree's nine.

**Ordering law discharged**: R-iii exists before `.a` authors ACT(1), and the whole record exists before `.b` authors ACT(2) — both by this commit.

---

### `.a` — specifier + easing

**Seat**: unit `.a`, `claude-opus-5[1m]`, sitting 2026-09-17 (wall clock 2026-09-18, 15:5x–16:1x EDT). **Writable set (hard bound)**: fourier `web/src/lib/easings.ts` · `web/src/components/equation/ConvergencePlot.vue` · `web/src/components/equation/composables/useCurveTransition.ts` · `web/src/components/equation/lib/harmonics.ts`; value.js `docs/tranches/X/fourier/`. **Gates owned**: G1 · G2 · G15 · G16 (G10 discharged by F.W1, re-stated not re-closed). **Ordering law satisfied**: `.c`'s R-iii exists (`b469b989`) and was read FIRST, before any byte of ACT(1) was authored.

**CRASH-RECOVERY sweep (standing law).** ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain` → **∅**, HEAD `538db90` on `m/w1-bump-migration`. ⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js status --porcelain` → 12 modified ⊕ 4 untracked, **none inside this unit's writable set** — `docs/tranches/X/fourier/evidence/w2/` did not exist (⟨cmd⟩ `/bin/ls docs/tranches/X/fourier/evidence/` → `w1` only). **No killed predecessor seat's partial work exists on this unit; nothing inherited, nothing stashed, nothing restored, no path outside the writable set touched.** `scripts/dev/dev.sh` never touched.

**E13 (unit-level).** Six paths swept read-only at this seat's clock (the four ⊕ the atlas P-lane ⊕ the Track-C fourier mail-ledger surface, COHESION §0k.1) — ⟨cmd⟩ `/usr/bin/find <each> -maxdepth 1 -type f -name '*.md' -newermt "2026-09-18 15:00"` → **no output on any of the six**. The five rows whose Status cell reads `UNREAD` (I-31 · I-32 · I-33 · I-34 · I-35) route to **X-W0 (Track A)**, the **X formation mail seat** and **X·KF (Track B)** — **0 UNREAD in `.a`'s scope**, exactly as the wave-open sweep found. `INBOX.md` **not written** by this seat (it is `.d`'s append-only act).

#### Acts, in order

**ACT 1 — read `.c`'s R-iii first, then measure before editing (D-19 / MEASURE-AT-OPEN).** R-iii (`RULINGS-F.W2.md` §3) routes MPC-2 and CENSUS-MC to **F.W1**, pre-empts nothing, and its §3.3 minutes that **COHESION §0o's `ESC-4`** answered both at F.W1's end. **ESC-4 is RULED: cited by id throughout this unit, never re-opened.** The anti-rename lock holds — `L/B-1 + C/B-1` is **F.W1's** identity (F-W1 §2 WU-K's row), **cited, never re-minted, no GREEN claimed** (FR-GIG-5).

**ACT 2 — G1 measured at the bytes: GREEN BEFORE ITS CURE, and no credit taken.** ⟨cmd⟩ `/usr/bin/grep -rn 'from "@mkbabb/value\.js"' /Users/mkbabb/Programming/fourier-analysis/web/src/ | /usr/bin/wc -l` → **0**. The re-grounded probe's leg 1 walks **129** source files and enumerates every value.js import as subpath-keyed: `ConvergencePlot.vue:5` `/easing` · `useCurveTransition.ts:8` `/easing` · `harmonics.ts:5` `/easing` · `colors.ts:20` `/color` · `colors.ts:21` `/css` · `easings.ts:35` `/easing`. **The five bare-root statements retired inside F.W1's one atomic transaction (`538db90`)**, released to it by ESC-4's *"the six bare-root import lines"*. **F.W2 claims none of that GREEN.**

**ACT 3 — F-1's ROOT CURE: the drift probe re-grounded at the adopted pin, as a dated E-3 addendum-beside.** The dated probe is **not patched, not silenced, not skipped, not allowlisted** — it keeps its bytes as a true reading of the pre-uplift tree. Measured stale-and-unrunnable at the uplifted tree: ⟨cmd⟩ `node docs/tranches/V/megatranche/audit/probes/fourier-value-import-drift.mjs` → leg 1 prints five coordinates the tree no longer holds; **leg 3 raises `ERR_MODULE_NOT_FOUND …/@mkbabb/value.js/dist/value.js`** at the 4.0.0 pin. The re-grounded probe is published **beside** it at `docs/tranches/X/fourier/evidence/w2/fourier-value-import-drift-2026-09-18.mjs`: leg 1 **walks** the census live, leg 2 reads both ends, leg 3 is **ESC-4's Δ = 0 sampler**. It **obtains its own baseline** (the published `@mkbabb/value.js@0.13.0`, version-pinned, into `os.tmpdir()`) because 4.0.0 is what fourier installs, and it reads the consumer catalogue through **fourier's own esbuild**, never re-typed. **Neither tree is written** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain | /usr/bin/wc -l` → **0** after three runs. Reading, double-run: exit **1**, `diff` of the two captures **EMPTY** — **leg 1 ok · leg 2 ok · leg 3 RED (4/22)**.

**ACT 4 — G15: ESC-4's Δ = 0 sampler run over ALL 22 KEYS, never the struck 14/22.** Landed `EASING_PRESETS[k].fn` vs published 0.13.0 `timingFunctions[k]`, **22 keys × 1001 samples**, re-run at **100001** samples with the same four maxima and no fifth. **18 of 22 are EXACTLY 0**; **all 14 corpus-in-file orphans are BIT-IDENTICAL**; **4 producer-DIRECT keys carry a residual** — `ease-in-out-expo` **4.4409e-16** · `ease-in-out-quad` · `ease-out-cubic` · `ease-in-out-cubic` **1.1102e-16** each. **Cause, measured at both dists**: value.js **re-expressed the same analytic closed forms** between 0.13.0 (Penner `(t /= .5)`) and 4.0.0 (easings.net) — algebraically equal, 1–2 ULP apart. **ZERO CubicBezier approximation anywhere**, so ESC-4's express prohibition is met at all 22 — **but its literal gate ("every one of the 22 samples EQUAL") is not.** See the escalation below; the full measurement, both readings and the four function bodies are at the addendum's §B.

**ACT 5 — A3 / SC-4 / C-19: the lerp cure, both halves in ONE commit (§6a same-commit riders).** `useCurveTransition.ts:85`'s `export function lerp(a, b, t) { return a + (b - a) * t; }` **deleted**; `ConvergencePlot.vue:11`'s named list loses `lerp` and `@mkbabb/value.js/math` supplies it at the new `:6`. **§2b item 7 confirmed at the bytes** — one declaration, one importer, ten call sites — *"the re-point is mandatory or ConvergencePlot's import breaks"*. Numeric consequence measured, not glossed: the producer's `(1-t)*a + t*b` vs the deleted `a+(b-a)*t` → **20864 of 81081 samples differ, max |Δ| = 2.274e-13** at magnitude 1e3 (~1 ULP), producer form **endpoint-exact**. No easing key routes through this `lerp`, so ESC-4's law is untouched by it.

**ACT 6 — G2 and G16 closed on the landed bytes; nothing re-armed.** **G2**: both prescribed ACT(1) symbols absent at the target (⟨cmd⟩ (cwd `/Users/mkbabb/Programming/value.js`) `/usr/bin/grep -rn 'easingNames\|timingFunctions' src/ dist/` → no output), so nothing could be transcribed; the evidence that the consumer's mapping is **re-derived** is ACT 4's measurement — **all 14 in-file keys reproduce their 0.13.0 function bit-identically**, which a transcription of a non-existent `easingNames()` could not do. Both RED inputs measured **absent**: no `easing()` call, therefore no `.value` unwrap and no issue arm to swallow; `getEasingFn`'s `??` is a fallback over a **total** map. **G16**: the producer instability reproduces (⟨cmd⟩ → `easing('ease-in-out').value === easing('ease-in-out').value` → **false**, matching X-W9 `G23`) and **cannot reach the render path** — ⟨cmd⟩ `/usr/bin/grep -rnoE '[^a-zA-Z]easing\(' /Users/mkbabb/Programming/fourier-analysis/web/src/ | /usr/bin/wc -l` → **0**; `EASING_PRESETS` is built **once** at module scope (`easings.ts:186`). **This unit wrote no `easing(name)` call and left the module-scope catalogue UN-REARMED.** `harmonics.ts:81`'s `spectrumColor` is **LOCKED and was not edited** — ⟨cmd⟩ `/usr/bin/grep -n 'export function spectrumColor' …/lib/harmonics.ts` → `:81`, the anchor re-resolving exactly.

**ACT 7 — gate witnesses at the landing, double-run.** ⟨cmd⟩ (cwd `/Users/mkbabb/Programming/fourier-analysis/web`) `npx vue-tsc -b --force` → exit **1**, **18 diagnostics** = **17 `TS6133` ⊕ 1 `TS6196`**, ⟨cmd⟩ `… | /usr/bin/grep -c 'error TS2307'` → **0**, `diff run1 run2` → **EMPTY**. **That is byte-for-byte F.W1's banked close reading** (its §Close `G11` row): **zero diagnostics added, zero removed**, and **none of the 18 names a file this unit touched**. ⟨cmd⟩ `npx vite build` → exit **0**, `✓ built in 6.27s` — the new `/math` specifier resolves through vue-tsc **and** vite.

**ACT 8 — commits (one commit per meaning; pathspec ON the commit itself).**

| commit | repo | meaning | ⟨cmd⟩ `git show --stat --oneline` |
|---|---|---|---|
| **`0cc9b00`** | fourier | *refactor(F.W2/.a): A3/SC-4 — the sole hand-rolled lerp retires to `@mkbabb/value.js/math`* — **the family that must not split**: deletion ⊕ re-point | **2 files changed, 2 insertions(+), 6 deletions(-)** — `ConvergencePlot.vue` · `useCurveTransition.ts` and nothing else |
| **`af369978`** | value.js | *docs(x-f-w2/.a): dated addendum-beside — the drift probe re-grounded at the adopted pin; ESC-4's Δ = 0 sampler run* | **2 files changed, 430 insertions(+)** — the addendum ⊕ the re-grounded probe, under `docs/tranches/X/fourier/evidence/w2/` and nothing else |

**No sibling seat's staged path was swept into either commit**; neither `git add -A`/`-u` nor `git commit -a` was used; nothing was reset, unstaged or stashed. **The §6a rider family is intact within each repo.** The cross-repo split is structural — one commit cannot span two repositories — and is disclosed here rather than elided: the fourier commit body names the rider law it discharges, and the addendum records A2's cure-side mapping reading that travels with it.

#### Gate readings — BEFORE → AFTER

| gate | BEFORE (wave open) | AFTER (this unit) | witness |
|---|---|---|---|
| **G1** every specifier resolves at the target | **RED** born; **GREEN-before-cure** at open | **GREEN** | ⟨cmd⟩ `grep -rn 'from "@mkbabb/value\.js"' …/web/src/ \| wc -l` → **0**; probe leg 1 enumerates 6 subpath-keyed imports over 129 files. **Landed by F.W1 at `538db90` under ESC-4 — F.W2 claims no credit** (FR-GIG-5) |
| **G2** the `timingFunctions` mapping is re-derived, not transcribed | **RED** — both prescribed symbols 0 hits; cure-side unread | **GREEN** | probe leg 2: producer publishes neither symbol; consumer carries **0 CODE references**. The re-derivation is proven by ACT 4's **14/14 bit-identical** in-file arm, not by reading a docblock. Both RED inputs measured absent. Record leg was `.c`'s §4 |
| **G15** the easing migration's curve drift, **at ESC-4's Δ = 0** | **RED** — and RE-SHAPED: banked 14/22 · 8/22 · max Δ 0.192 **superseded by ESC-4** | **RED — HONEST RED, 4 of 22** | 18/22 **exactly 0**; 14/14 corpus-in-file **bit-identical**; 4 producer-DIRECT keys ≤ **4.4409e-16**. ESC-4's *no-CubicBezier* prohibition **met at all 22**; its literal *EQUAL* **not met at 4**. **Escalated, not laundered** — addendum §B.4 |
| **G16** `easing()` is called once, never on the render path | **RED** as measured; neutralized by shape | **GREEN — and un-rearmed** | producer identity **false** (X-W9 `G23`'s twin); consumer `easing(` call sites **0**; catalogue built once at module scope. **No per-invocation `easing(name)` written by this unit** |
| **G10** the value.js-direct arm does not land alone | **DISCHARGED BY F.W1** | **re-stated, NOT re-closed** | the pin moved inside the atomic transaction; the arm never landed alone |

**No other gate is touched, and no GREEN of F.W0/F.W1/F.W3/F.W4/W.L6 is claimed** (FR-GIG-5).

#### Findings this unit adds to the record

- **F-10 · `F-W2.md` §2b item 7's "one hand-rolled `lerp`" is TWO at HEAD.** `useCurveTransition.ts:85` (A3's target, **cured**) and **`easings.ts:49`** — the de Casteljau step inside the `cssCubicBezier` solver **F.W1 landed under ESC-4**. **The second is NOT cured and NOT touched**: A3's carry, §2a row 3 and §2b item 7 all name the first and nothing else, and §2a row 1 gives `easings.ts` the specifier retirement and the mapping re-derivation, no `lerp` cure. It is **formula-identical** to `/math`'s `lerp`, and `deCasteljau` is itself a published `./math` export — so the collapse is available and is **F.W3/F.W4/W.L6's to price**. Improvising it inside ESC-4's zero-drift solver is the act this unit refuses.
- **F-11 · the 4-key producer residual is a value.js finding with a consumer-visible measurement.** It belongs on the **X·V / V·π** and **X-W9 / W.L6** edges, and since §6b routes **X-W9 §Hard Gate's `G24`** as *"the drift gate … G15 is its consumer-side twin"*, **G24's producer-side owner should read the addendum's §B before closing it.** Relay vehicle is `.d`'s letter ⊕ its append-only INBOX row.
- **F-12 · `easingNames` in the consumer is a NAME COINCIDENCE, not ACT(1)'s symbol** — `MorphPhaseConfig.vue:41`/`:104`, where `const easingNames = EASING_PRESET_NAMES` is a local template binding over the corpus's own catalogue. Recorded so no successor's grep reads it as a transcription of the prescription.
- **F-13 · A1's specifier anchors re-resolve, and MISSED-F's costing correction is discharged at the bytes** — `easings.ts`'s two statements at `:9` and `:10-16` are now **ONE import block at `:26-35`**; the duplicated specifier is gone. **F.W1 owns that; F.W2 claims none of it.**

#### Residuals and escalations

**ESCALATION — G15's four-key ULP residual, raised and NOT resolved.** ESC-4's gate is literal (*"every one of the 22 samples EQUAL (Δ = 0 at every sample point)"*) and four keys are not equal. **A ruling this unit must cite and never re-open is not a ruling this unit may re-interpret to its own threshold.** The competing reading is on the record and is **not adopted here**: `F-W2.md` §3's **A1** already banks this same residual as *"numerically identical to 0.13.0 (max|Δ| ≤ 4.44e-16 over 1001 samples) — the direct-symbol arm is a drift-free rename"*. **No cure inside this unit's bounds closes it**: ESC-4 *prescribes the mechanism* (*"the 8 survivors re-point to `@mkbabb/value.js/easing`"*), so defining those four in-file would make the sampler read zero **by contradicting the ruling that set the sampler** — the masking class, refused; and the root cause is `value.js src/easing.ts`, which §1b puts out of this wave's bounds. **Two dispositions, both above this seat, neither taken**: **(i)** read ESC-4's *EQUAL* at double precision and adopt A1's banked identity — G15 turns GREEN with no byte moving; **(ii)** treat the re-expression as a producer defect and route the analytic-form restoration to **X-W9 / W.L6** — G15 stays RED until it lands.

Residuals carried to their owners: **F-10**'s second `lerp` → F.W3/F.W4/W.L6 · **F-11**'s producer finding → the `.d` relay and X-W9 `G24` · the **18 `TS6133`/`TS6196`** diagnostics remain **F.W0's honest-RED substrate** (zero added, zero removed by this unit; several in files outside every F.W2 writable set, and curing them is not a limb) · `INBOX.md` untouched, `.d`'s to append.

### `.b` — colours

**Seat**: unit `.b`, `claude-opus-5[1m]`, sitting 2026-09-17 (wall clock 2026-09-18, 15:5x–17:2x EDT). **Writable set (hard bound)**: six fourier files (`colors.ts` · `golden-shimmer.ts` · `epicycles.ts` · `BasisCanvas.vue` · `ContourPreview.vue` · `ContourEditorCanvas.vue`) ⊕ `docs/tranches/V/megatranche/audit/probes/app-wave/` ⊕ `docs/tranches/X/fourier/`. **Nothing outside it was written**, at either tree. `scripts/dev/dev.sh` untouched. `.c`'s R-ii was read first and in full (§6a *Author-before-choose*), and `.a`'s landed receipt was read before this unit's first byte.

**CRASH-RECOVERY sweep (standing law).** ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain` → **∅**. ⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js status --porcelain` → 14 modified ⊕ 5 untracked, **none inside this unit's writable set**: no `app-wave/fourier-viz-*.png`, nothing under `docs/tranches/X/fourier/evidence/w2/` but `.a`'s own two files (a sibling unit's, untouched), and none of the six fourier paths. **No killed predecessor seat's partial work exists on this unit; nothing inherited, nothing stashed, nothing restored.**

**E13 (unit-level).** Delta sweep at this seat's clock against the `.c` sweep (15:0x) — ⟨cmd⟩ `/usr/bin/find <the four paths ⊕ ../fourier-analysis/docs/tranches/F/coordination> -maxdepth 1 -type f -name '*.md' -newermt "2026-09-18 15:00"` → **no output**. **0 unrowed · 0 new `I-n` · 0 UNREAD in `.b`'s scope** (the five literal-UNREAD rows route to Track A, X·KF and the X formation mail seat, per the wave record's own Step-0). `INBOX.md` not written by this seat — it is `.d`'s append-only act.

#### Acts, in order

**ACT 1 — measure before editing (D-19 / MEASURE-AT-OPEN).** Every anchor this unit's sections name, re-resolved at fourier `538db90`+`0cc9b00` before a byte moved, each double-run:

- **G5's five coordinates reproduce exactly** — ⟨cmd⟩ `/usr/bin/grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ | /usr/bin/grep -v 'export function'` → **5 lines**: `BasisCanvas.vue:261` · `epicycles.ts:258` · `epicycles.ts:283` · `golden-shimmer.ts:55` · `golden-shimmer.ts:58`. **The enumeration IS the figure**; the rider law is sized at FIVE and was never sized at four here.
- **G17 reproduces** — ⟨cmd⟩ `/usr/bin/grep -rn 'hexToRgb(' …/web/src/ | /usr/bin/grep -v hexToRgba` → **1 line**, `colors.ts:180`, its own declaration. A dead export.
- **The amber cut's nine coordinates reproduce exactly** — ⟨cmd⟩ `/usr/bin/grep -n 'hsl(40' …/ContourPreview.vue …/ContourEditorCanvas.vue` → `ContourPreview.vue:45` ⊕ `ContourEditorCanvas.vue:261,:307,:308,:315,:319,:328,:329,:333` (**1 + 8 = 9**), five alphas `{0.85, 0.6, 0.5, 0.3, none}`.
- **G6's half-landing confirmed**: `hexChannels()` opens `if (!HEX_COLOR.test(hex)) { throw new TypeError(…) }` (landed at `3bac3d5`); **no deletion date anywhere in the file** — ⟨cmd⟩ `/usr/bin/grep -c 'DELETION DATE' …/colors.ts` → `0`.
- **G11/G12 born RED as declared**: no colour-value census artefact in tree; ⟨cmd⟩ `/bin/ls docs/tranches/V/megatranche/audit/probes/app-wave/` → `BORN-RED-2026-07-27.json` · `app-shell-truth-probe.mjs`, the four PNGs **absent**.

**ACT 2 — the G12 "before" frames, captured BEFORE the first edit.** A before/after pair whose "before" is taken after the cut is not a pair. One headed evaluate, both schemes, `/equation` at fourier's own dev server; the R-5 cascade readback taken in the same evaluate. Landed at `app-wave/fourier-viz-{light,dark}-before.png`.

**ACT 3 — ONE CUT (B1⊕B2⊕B3), fourier `0106a85`.** `--contour-stroke` minted, **nine** sites cured across **two** files in ONE commit, five alphas preserved as `stroke-opacity` / `fill-opacity` / `color-mix()` stops:

| site | before | after |
|---|---|---|
| `ContourPreview.vue:45` | `stroke="hsl(40 90% 55% / 0.85)"` | `stroke="var(--contour-stroke)" stroke-opacity="0.85"` |
| `ContourEditorCanvas.vue:261` | `stroke="hsl(40 90% 55% / 0.85)"` | `stroke="var(--contour-stroke)" stroke-opacity="0.85"` |
| `:307` `.control-point` fill | `hsl(40 90% 55% / 0.6)` | `fill: var(--contour-stroke); fill-opacity: 0.6` |
| `:308` `.control-point` stroke | `hsl(40 90% 55%)` | `stroke: var(--contour-stroke)` |
| `:315` `:hover` fill | `hsl(40 90% 55% / 0.5)` | `fill-opacity: 0.5` |
| `:319` `.spline-path` shadow | `drop-shadow(… hsl(40 90% 55% / 0.3))` | `drop-shadow(… color-mix(in srgb, var(--contour-stroke) 30%, transparent))` |
| `:328` · `:329` keyframes | `0.3` · `0.5` literal | `color-mix()` at **30%** · **50%** |
| `:333` `.selected` fill | `hsl(40 90% 55%)` | `fill: var(--contour-stroke); fill-opacity: 1` |

**B2's unreachable slash-alpha literal is cured BY NAME** — it is the `/ 0.85` form at `ContourPreview.vue:45` and its on-stage twin at `ContourEditorCanvas.vue:261`, named here and cured as the first two rows of the cut, never swept as collateral by the `colors.ts` deletion (which, as B2 predicted, reaches neither: the two commits touch disjoint files). **`hsl(40` is now ∅ tree-wide** — ⟨cmd⟩ `/usr/bin/grep -rn 'hsl(40' /Users/mkbabb/Programming/fourier-analysis/web/src/` → *(no output, exit 1)*; the two explanatory comments name the retired literal as *"the retired inline amber (hue 40, 90%, 55%)"* precisely so that the gate's own count command is not answered by prose.

**ACT 4 — ONE COMMIT: the ACT(2) deletion with its join, fourier `f7fa1e3`.** The deletion list and its five live consumers land together — the wave's hardest sequencing lock:

- `colors.ts`: **`hexToRgba` and `hexToRgb` deleted**; the **declared hex residual** (`hexChannels` + one exported arm) keeps its validation throw and **gains its deletion date** — *"DELETION DATE = fourier's 4.1 adoption of value.js"* (RD-6/RD-7), stated at the residual and repeated at the arm that consumes it.
- **the B7 join**: the golden halo is named in **one** place, `golden-shimmer.ts`'s `goldenShimmerShadow(alpha)`, and the three canvases that painted it stop re-deriving it — `golden-shimmer.ts:55`/`:58` and `BasisCanvas.vue:261` all call it. When F.W4 cures the swatch side (`fr-ConvergenceLegend` C-2's consumer-side render fix, **not claimed here**), it is one line, not a hunt through three canvases.
- `epicycles.ts:258`/`:283` take the palette's own `fourier` entry through the same residual (`withAlpha`), preserving `0.25` and the pulsing `glowAlpha` exactly.
- **Honest accounting (G17)**: `hexToRgb` had **zero** call sites and is deleted **for free**; the residual assumes **`hexToRgba`'s one arm only**, which is the gate's own words — *"the 3-line residual (G6) must not be sized as if it replaced both."*
- **The NEVER-clause (B11/K-8) holds in both commits**: the CSS-expressible colour (the nine contour sites) goes to `var(--contour-stroke)` and never through the JS resolver; the JS-only colour (Canvas2D, which has no cascade to resolve `var()` against) keeps the declared residual. **Nothing re-implements a producer surface** — the token resolution is still glass-ui `/dom`'s `createTokenColorCache`, per `.c`'s R-ii.

**Verification at each landing, double-run**: ⟨cmd⟩ `npx vue-tsc -b --force` → **18 diagnostics before and after**, all `TS6133`/`TS6196` unused-symbol, the only one in a file this unit touched being the pre-existing `ContourEditorCanvas.vue(42,9) 'dragging'` — **zero added, zero removed** (F.W0's honest-RED substrate, G-15(a) LAND). ⟨cmd⟩ `npx vite build` → `✓ built in 2.79s`, the production bundle green with the `color-mix()` stops and the re-pointed modules.

**ACT 5 — the G12 "after" frames + the pair's receipt, value.js `e32a5613`.** Same evaluate, same viewport, after both fourier commits. Four PNGs force-added past `.gitignore:34` (⟨cmd⟩ `/usr/bin/sed -n '34p' .gitignore` → `*.png`). **R-5 cascade lock met on every readback**: `--viz-fourier` declares `light-dark(oklch(0.579 0.201 30.4),oklch(0.693 0.151 28.1))` in LIGHT and resolves to `oklch(0.693 0.151 28.1)` in DARK; **the chrome is correctly coloured in the same frame** (`rgb(157, 101, 21)` light · `rgb(232, 185, 109)` dark = `--viz-amber` at each pin). The pairs are **byte-identical per scheme** — ⟨cmd⟩ `shasum -a 256 …/fourier-viz-*.png` → `29a469008cf5…` for both light frames, `baea05f51e1e…` for both dark — which is the wave's goal criterion measured rather than asserted: **the ACT(2) migration changes no pixel of the surfaces it touches.**

**ACT 6 — G11's colour-VALUE census, value.js `12613490`.** `docs/tranches/X/fourier/evidence/w2/F-W2-b-colour-value-census-2026-09-18.md` with its instrument beside it (`colour-value-census-2026-09-18.mjs`), so every figure is one command away and none is typed from memory. **130 files scanned · 16 author brand colour · 65 brand values · 7 files (27 values) invisible to an import-keyed inventory**; comments blanked before counting (9 excluded), neutrals and pure compositions classified out, and an interpolation with any authored channel counted — which is how the four `spectrumColor` ramps enter a census that two of their four un-imported files would hide from. **The sixteen zero-cells are re-measured on both axes and subtracted from the CONSUMPTION denominator with their ids**: all sixteen consume **0** value.js/keyframes, and **two of them author 15 brand values** (`fr-AnimationControls` C-18 → 10 · `fr-PaperSearch` → 5). **Neither is re-booked as a defect and neither is counted in the denominator** — G11's RED input forbids both. Before/after of this unit's own cut, **measured, not derived by subtraction**: the same instrument over the wave-open bytes reads **18 files · 74 values** (invisible **9 · 36**), the settled tree **16 · 65** (invisible **7 · 27**).

**ACT 7 — F-1's colour half: the `--viz-*` probe RE-GROUNDED, value.js `8e6dbe61`.** A dated **addendum-beside** (`F-W2-b-addendum-2026-09-18.md` + `fourier-vizcolor-oklch-2026-09-18.mjs`); **the dated probe keeps its bytes and is neither patched, silenced, skipped nor allowlisted.** It is stale two ways — it transcribes glass-ui **4.0.0** token text into its own source, and it simulates `cssVarToHex`, which `3bac3d5` **deleted** — so its `5/6` is neither GREEN nor RED but a reading of a function that no longer exists. The re-grounded probe asks the same question in three legs: the producer's **shipped** declarations parsed by value.js's own `parseStylesheet`; **what value.js resolves without a cascade** (**5 context-free · 4 requiring the engine**, each with its typed diagnostic); and the **live used values** at both schemes. ⟨cmd⟩ `node docs/tranches/X/fourier/evidence/w2/fourier-vizcolor-oklch-2026-09-18.mjs` → **exit 0**, double-run byte-identical, **0/10 tokens collapse to `#888888`**, hues distinct **5/5** per scheme. **Leg 2 is the measurement that retires the string-matching resolver by construction**: `light-dark(…)` returns `css_syntax`/`["CSS color"]` and `var(--section-color-5)` returns **`color_context_required`** — which arm is correct is a fact about the document, not about the string, and that is `.c`'s R-ii partition arrived at by measurement. **The live leg is REQUIRED**: unreachable exits **2 INCONCLUSIVE**, never 0.

**ACT 8 — commits (five, one meaning each, pathspec on the commit itself; no family split).**

| commit | tree | meaning | ⟨cmd⟩ `show --stat` |
|---|---|---|---|
| `0106a85` | fourier | ONE CUT — the mint ⊕ nine amber sites | **2 files**, +38 −10 |
| `f7fa1e3` | fourier | ONE COMMIT — the deletion ⊕ the five-site join ⊕ the dated residual | **4 files**, +46 −18 |
| `e32a5613` | value.js | G12's four PNGs | **4 files** |
| `12613490` | value.js | G11's census ⊕ its instrument | **2 files**, +369 |
| `8e6dbe61` | value.js | F-1's re-grounded probe ⊕ the addendum | **2 files**, +395 |

**No sibling seat's staged path was swept into any of the five** — each `--stat` lists exactly the paths its pathspec names, and the value.js index carried four other seats' dirty paths throughout.

#### Gate readings — BEFORE → AFTER

| gate | BEFORE (wave open, re-measured at this seat) | AFTER | witness |
|---|---|---|---|
| **G5** the `hexToRgba` deletion lands with its join | **RED** — **5 call sites / 3 files**, enumerated | **GREEN** | ⟨cmd⟩ `/usr/bin/grep -rn 'hexToRgba(' …/web/src/ \| /usr/bin/grep -v 'export function'` → **∅** (exit 1), double-run. The five consumers land in the SAME commit (`f7fa1e3`) as the deletion, three of them reunited on `goldenShimmerShadow` as ONE source |
| **G6** the residual has a validation arm and a deletion date | **HALF-GREEN** — validation landed at F.W1's `3bac3d5`; **deletion date unwritten** (`grep -c 'DELETION DATE'` → `0`) | **GREEN** | the throw survives untouched (⟨cmd⟩ `grep -c 'throw new TypeError' …/colors.ts` → **1**) and the date is written (⟨cmd⟩ `grep -c "DELETION DATE = fourier's 4.1 adoption" …/colors.ts` → **1**), with `withAlpha`'s docblock repeating it at the arm that consumes it |
| **G11** the denominator is colour-value-keyed, not import-keyed | **RED** — no such census exists | **GREEN** | `F-W2-b-colour-value-census-2026-09-18.md` ⊕ its instrument; **65 brand values / 16 files**, **27 of them invisible to any import-keyed instrument**, the sixteen zero-cells subtracted and cited by id, zero rows booked and zero ids minted |
| **G12** the π probe pair exists | **RED** — the four PNGs absent | **GREEN, with a declared SS-13 residual** | four PNGs at §2a's own create paths, force-added past `.gitignore:34`; R-5 readback met in both schemes; chrome correct in the same frame. **What the frames do not carry is stated, not implied** (below) |
| **G17** the deletion count is honest | **RED** — `hexToRgb` a dead export at `colors.ts:180` | **GREEN** | ⟨cmd⟩ `/usr/bin/grep -rn 'hexToRgb(' …/web/src/ \| /usr/bin/grep -v hexToRgba` → **∅** (exit 1). Deleted for free; the residual is sized at **one** arm, `hexToRgba`'s, exactly as the gate's RED input requires |
| **G3** the oklch/light-dark tokens resolve | **UNMEASURED at open — the named probe was stale** (F-1) | **F.W1's GREEN, re-grounded and reproduced — NOT CLAIMED** | the re-grounded probe's leg 3: 0/10 greyed, 5/5 distinct per scheme. Green owner is F.W1 (`fr-BasisCanvas` D-1/BC-1/C-1, landed `3bac3d5`); `.b` owns the deletion of the arms that caused the RED and claims none of that GREEN (FR-GIG-5) |
| **G4** the SNAPSHOT is fixed, not the parser swapped | **GREEN BEFORE ITS CURE** (F.W1's `basisDisplay` derivation) | **unchanged — NOT CLAIMED** | `.b` holds only the lock and the green-criterion: no value copy of `VIZ_COLORS` was introduced by either commit, and the two commits touch no snapshot site |

**No other gate is touched, and no GREEN of F.W0/F.W1/F.W3/F.W4/W.L6 is claimed** (FR-GIG-5).

#### Findings this unit adds to the record

- **F-12 · value.js 4.0.0's `parseStylesheet` rejects the legacy comma form `rgba(r,g,b,a)`.** Minimal repro at fourier's own installed copy — ⟨cmd⟩ `node -e "…parseStylesheet('.dark{--x: rgba(0,0,0,0.3);}')"` → `ok:false`, `css_syntax`, `expected ["scalar"]`, `actual "rgba(0,0,0,0.3)"`; `rgb(0 0 0 / 0.3)` parses clean, and `.dark{color: rgba(0,0,0,0.3);}` fails identically, so it is not a custom-property quirk. Live consequence: glass-ui 8.0.0's `dist/styles/tokens/dark-arm.css` fails on its **first** declaration. **A value.js-side reciprocal for the X·V / V·π seam (§6b), routed to `.d`** (G9/G18 are its gates); `.b` claims no GREEN from it and the re-grounded probe reports it rather than swallowing it.
- **F-13 · the 4.0.0 corrupt-stylesheet tail is GONE at the adopted pin.** ⟨cmd⟩ `parseStylesheet` on glass-ui **8.0.0**'s `dist/styles/index.css` → **ok: true**, 1,514 chars (an `@import` manifest, not the 13,833-char sheet whose tail was prose). Context for F.W0/F.W1's FR-NP-32 record; **claimed by nobody here**.
- **F-14 · "mint `--contour-stroke` once" landed as ONE token declared at TWO roots, and the reason is measured.** §2a's bounds name the two SFCs and no stylesheet, and the two components have **no shared ancestor**: `ContourEditorCanvas` mounts under `VisualizationView.vue:204` **and** standalone under `FullscreenViewer.vue:115`, `ContourPreview` under `VisualizationView.vue:257` only. A single `:root` mint inside either SFC would make the other's colour depend on an unrelated module being loaded — the invisible coupling this wave exists to kill. **ONE token, ONE definition (`var(--viz-amber)`), NINE cured sites, TWO declaration sites**, each at the root of an independently mountable surface. Recorded as a divergence from the spec's word *once*, with its reason, rather than quietly satisfied.
- **F-15 · B1's contrast cure, measured at the live cascade** (over `--card`, WCAG 2): light **1.627:1 → 3.469:1** at the shipped `0.85` and **4.517:1** at full alpha; dark **5.724 → 6.013 / 7.699**. **SC 1.4.11's 3:1 non-text bar is cleared in both schemes** where the literal failed light by a factor of two. Two banked figures re-measured at the **8.0.0** pin as divergences, neither averaged: the bank's **1.660:1** reads **1.627:1** here and its **4.625:1** reads **4.517:1** — same direction, same magnitude, same ruling; the third digit moved because the pin did.
- **F-16 · `colors.ts` is 192 lines** (⟨cmd⟩ `/usr/bin/wc -l < …/colors.ts`), the spec's **117** having become **182** at `3bac3d5` and **192** here. **The file grew while its hand-rolled colour surface shrank** — two exported arms deleted, one residual declared, and the rest is the docblock that names the deletion date. No superseded figure is carried forward as current (G-12).
- **F-17 · `fr-ContourPreview` row 22's banked *ten* against the tree's nine** stands as the banked divergence it is (R-i §1.4.2 · F-6). This unit cured **nine** and minted no eleventh reading.

#### Residuals and escalations

**Escalations: none.** Every act this unit's sections prescribe was performed as specified, at the bytes; no specified cure was found impossible, and nothing was substituted.

Residuals carried to their owners:

- **SS-13 (UNPROVEN-NEEDS-LIVE), declared and not worked around.** G12's frames carry the app shell, the cascade and the chrome; they do **not** carry the convergence curves or the nine cured contour sites, because both want backend-extracted data — `/equation` computes through FastAPI over MongoDB and the docker daemon is down at this host (⟨cmd⟩ `docker info` → *"Cannot connect to the Docker daemon"*), and booting a database is not a parsimonious probe. **The gate's DELTA cell — *"four grey curves → four distinct hues"* — is F.W1's delta, discharged at `3bac3d5` before F.W2 opened**, and is not re-capturable as a "before" without reverting a landed commit. The re-grounded probe's leg 3 is the honest witness of that delta and it is green. The live-frame confirmation of the amber cut travels to **SS-13 item 1** as this unit's residual.
- **F-12's `parseStylesheet` reciprocal** → unit `.d` (the X·V / V·π filing) and X-W9.
- **The cures this unit does not own, cited and never double-cured**: `fr-ConvergenceLegend` C-2's consumer-side render fix and `fr-ConvergencePlot` D-1's palette item → **F.W4** · the four `spectrumColor` declarations (`M-β1`, ONE identity, original for life) → **F.W4** books execution, **F.W3 `.d`** the fork collapse · `fr-NotationPills` FR-NP-9's `color-mix` cure → **F.W3** under K-8 · `fr-ImageUpload` row 13's eight hexes → dies with roster 5's cure, F.W2 books only the census obligation.
- **The 18 `TS6133`/`TS6196` diagnostics** remain F.W0's honest-RED substrate — **zero added, zero removed** by this unit.
- `INBOX.md` untouched; the wave's E13 rows are `.d`'s append-only act.

### `.d` — facility 19 + the letter + closure

**Seat**: unit `.d`, `claude-opus-5[1m]`, sitting 2026-09-17 (wall clock **2026-09-18, 16:2x–16:5x EDT** — disclosed, per E-3). **Writable set (hard bound), four paths**: `docs/tranches/X/fourier/facility-19.md` · `docs/tranches/X/coordination/value-to-fourier-2026-09-18-fn-answers.md` · `docs/tranches/X/coordination/value-to-glassui-2026-09-18-token-color-seat.md` · `docs/tranches/V/coordination/INBOX.md` (**append-only**). **Gates owned**: G9 · G13 · G18 · G19. **Last unit**: `.a`'s and `.b`'s landed receipts were read in full before the first byte, and `.c`'s **R-ii** was read whole for the SS-6 relay's ask 1.

**CRASH-RECOVERY sweep (standing law).** ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain` → **∅**, HEAD `f7fa1e3`. ⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js status --porcelain` → 12 modified ⊕ 4 untracked, **none inside this unit's writable set**: no `facility-19.md` (⟨cmd⟩ `/bin/ls docs/tranches/X/fourier/facility-19.md` → *No such file or directory*), neither `value-to-*` letter (⟨cmd⟩ `/bin/ls docs/tranches/X/coordination/` → five letters, no `value-to-fourier-2026-09-18-*`, no `value-to-glassui-*`), and `INBOX.md` **clean** (⟨cmd⟩ `git status --porcelain docs/tranches/V/coordination/INBOX.md` → no output). **No killed predecessor seat's partial work exists on this unit; nothing inherited, nothing stashed, nothing restored, no path outside the writable set touched.** `scripts/dev/dev.sh` never touched — it remains ` M`, unstaged, as the standing arrangement requires.

**E13 (unit-level).** Six paths swept read-only at this seat's clock (the four landing paths ⊕ the atlas P-lane ⊕ the Track-C fourier mail-ledger surface, COHESION §0k.1), as a delta against `.b`'s sweep — ⟨cmd⟩ `/usr/bin/find <each of the six> -maxdepth 1 -type f -name '*.md' -newermt "2026-09-18 17:20"` → **no output on any of the six**; ⟨cmd⟩ `/bin/ls -dt ../glass-ui/docs/tranches/*/ | head -3` → `BK/` · `BJ/` · `BI/`. **0 unrowed · 0 new `I-n` · I-35 remains the inbound tail · 0 UNREAD in F.W2's scope** (the five literal-`UNREAD` rows route to Track A, the X formation mail seat and Track B, classification taken from each row's own Status cell). **This unit's own two rows are appended below, which is the wave's E13 act.**

#### Acts, in order

**ACT 1 — measure before writing (D-19 / MEASURE-AT-OPEN), every figure double-run.** Nothing below was inherited from A1, from O-14 or from a sibling's transcript.

- **The import set re-measured AT HEAD, not inherited.** §3 **F1** states *"the six-symbol import set the ledger must enumerate is measured in A1 and is complete at HEAD"*; **at these bytes it is ELEVEN, and the six is a true reading of the pre-uplift tree that is false now.** The instrument (published whole in the ledger, so it can be falsified) walks every `.ts`/`.vue`/`.js` under `web/src` and matches import blocks **whole**, because a line-oriented grep truncates the multi-line block at `easings.ts:26-35` → `distinct (symbol,subpath) pairs: 11 | import statements: 7 | total bindings: 14`. **8 × `./easing`** (`easeInOutSine` · `linear` · `easeInOutQuad` · `easeOutCubic` · `easeInOutCubic` · `easeOutExpo` · `easeInOutExpo` · `easeInOutCirc`) · **1 × `./math`** (`lerp`) · **1 × `./color`** (`toRgba8`) · **1 × `./css`** (`parseCssColor`). **All eleven measured `EXISTS` at shipped 4.0.0.** The superseded six is named as superseded, never averaged.
- **Completeness closed both ways, each direction with its falsifier stated.** The census's denominator is checked rather than trusted: ⟨cmd⟩ `/usr/bin/grep -rn '@mkbabb/value\.js' /Users/mkbabb/Programming/fourier-analysis/web/src/` → **8 lines**, of which `lib/easings.ts:11` is **prose in a docblock**; `8 − 1 = 7` = the instrument's statement count. **No `import * as`, no default, no dynamic `import()` form exists to be missed.** The instrument's `(bare root)` arm is live and returns **∅** — G1 re-witnessed from the ledger's end, credit claimed by nobody (that retirement is F.W1's at `538db90`).
- **`counts`, as-specified with an as-built sibling census.** as-specified ⟨cmd⟩ `node -p "JSON.stringify(require('…/API-FACILITY-ISOMORPHISM.json').counts)"` (literal path in the ledger) → `counts.fourier.http` **41**; as-built ⟨cmd⟩ `cd /Users/mkbabb/Programming/fourier-analysis && /usr/bin/grep -rnE '@router\.(get|post|patch|put|delete)' api/routers/*.py | /usr/bin/wc -l` → **30**, enumerated per router (`contours 4 · equations 2 · images 7 · sessions 4 · visualizations 13`; `4+2+7+4+13 = 30`, **the enumeration is the figure**). **Record-only; not a finding against fourier's API, and no byte of the JSON is written.**
- **O-14's ten I-rows re-measured at 4.0.0 rather than transcribed.** **No I-row's STATUS moved** — `easingNames` · `toHex` · `resolveCssColor` **ABSENT**; `easing` · `parseCssColor` · `convertColor` · `toRgba8` **EXIST** with the same contracts; `timingFunctions` **DELETED**; I-4's five-prototype-key throw and I-8's options-bag requirement both reproduce; I-7 returns the letter's own channels byte-for-byte. **What moved is the consumer, not the library.**

**ACT 2 — `facility-19.md` authored under RD-8's ONE-WAY law** (`docs/tranches/X/fourier/facility-19.md`, 226 L / 31,798 B). §1 the law quoted at the bank's own bytes with the **receipt range `:124,128` and the SPAN `:124-127` stated separately**, the bank's single italic span reproduced and **no word bolded that the bank does not bold**; §2 the eleven-symbol ledger, each symbol **exactly once**, with subpath · status · consumers · obligation; §3 completeness both ways with the instrument published in full; §4 `counts` as-specified ⊕ as-built ⊕ command; §5 the I-1..I-10 payload re-measured; §6 the G9 measurement; §7 the six reciprocals and the *what this does NOT do* table; §8 sha256 pins at quote time. **`API-FACILITY-ISOMORPHISM.json`'s bidirectional schema is READ as precedent and NOT extended** — ⟨cmd⟩ `git show --name-only --format= HEAD | /usr/bin/grep -c 'API-FACILITY-ISOMORPHISM'` → **0**.

**ACT 3 — the FN-1..FN-7 reply letter, the ONLY vehicle** (`value-to-fourier-2026-09-18-fn-answers.md`, 143 L / 20,294 B). **All seven answered**: FN-1/2/3/4/6 **ROUTED to F.W5/F.W6** with the reason on the record (*F.W2 owns the ledger and the answer, never the substance*); **FN-5 names SS-4's OWNER-GATED `G4` and pre-empts no ruling**, with the minute — the `.c`/ESC-4 precedent applied — that **COHESION §0j.D `F-SS4REST R1 (TA-4)` has since answered it** (*"RE-SCOPE value.js out of the diff clause"*, ⟨cmd⟩ `/usr/bin/grep -n 'RE-SCOPE value.js out of the diff clause' docs/tranches/X/COHESION.md` → the `§0j.D` `F-SS4REST` paragraph), **cited by id, never re-opened, and read as one-sided by its own text — it rules the value.js half and not fourier's**; the **M.W10 rider travels unsoftened**; **FN-7 both-ends at F.W5**. §3.1 measures the 2.0.0 note's peer-floor ask **DISCHARGED** (`web/package.json:19` `^4.0.0` · installed 4.0.0 · ⟨cmd⟩ `npm view @mkbabb/value.js version` → 4.0.0). **§3.2 re-authors the §3 strike to the B6 words: *RETIRED-BY-DELETION AT 4.0.0, SCHEDULED FOR 4.1 UNDER SCI-1*** — both halves load-bearing, because *retired by deletion* alone states a falsehood the moment W.L6 lands (⟨cmd⟩ `grep -rn 'sampleColorRamp\|mixColorsN' src/ dist/ | wc -l` → **0**; the re-ship is `X/waves/W9.md` §Scope item 8 · `G21`). §5 files the six reciprocals; §6 names what the letter deliberately does not do; §7 asks three things back.

**ACT 4 — the SS-6 glass relay, three asks** (`value-to-glassui-2026-09-18-token-color-seat.md`, 124 L). **(1)** the sanctioned-seat question for `@mkbabb/glass-ui/dom`, re-measured at the **ADOPTED 8.0.0** (`./dom` present; all three symbols in `dist/dom.js`; `useTokenColor.d.ts` present; **the spec's 80-key figure named superseded — the 8.0.0 keyset is 70**), carrying R-ii's *"a shipped consumer is evidence the seat is usable, never evidence it is sanctioned"* ⊕ the **SS-13 serialization question with our own measurement attached**: `parseCssColor` accepts `oklch`/`rgb`/`color()`/`lab`/`lch`/`oklab`/`hsl`/`rgb(… / α)` and **rejects the legacy comma `rgba(r, g, b, a)` and `color-mix()`**, so the seam's one failure mode is conditional on the serialization and is **measured not reachable for fourier's four opaque tokens** — asked as a question, not filed as an incident. **(2)** the `--viz-amber` carry stays **ONE** carry, gaining the banked `fr-ConvergenceLegend` **D-L11 + C-4** second denominator quoted at its bytes (4.56 / 3.17 / 2.53) ⊕ `.b`'s live re-measure, with the **stale D.W4.d comment** disclosed as a **fourier-side** fact (glass 8.0.0 ships `--viz-amber: var(--section-color-5)`, an alias, not the asserted `hsl(35 70% 42%)`). **(3)** `text-admin-label` measured **0 hits four ways** — producer dist, producer source, fourier `web/src`, value.js `demo`+`src` — so **MG-β's collision premise is GONE at the adopted pin** and the ask is a confirmation, **explicitly not a request to re-mint a retired utility**. **GLASS-UI READ-ONLY HONOURED ABSOLUTELY: zero glass bytes written.**

**ACT 5 — the two E13 rows, appended** (`O-29` fourier · `O-30` glass-ui), after `I-35`, in the 5-cell outbound shape. **The append-only lock is MEASURED, not asserted** — ⟨cmd⟩ `git diff --numstat -- docs/tranches/V/coordination/INBOX.md` at the row landing → `2 0` (and `4 0` with this seat's sweep line), **zero deletions, no existing row edited, re-keyed or re-worded**. Cell arity verified against the header (`| # | Date | To | Letter | Status |`) with escaped pipes discounted, since a `\|` inside a ⟨cmd⟩ does not split a Markdown cell: both new rows render **5 cells**, matching `O-28` and `I-35`.

**ACT 6 — lawfulness pass over the SETTLED bytes, double-run.** Three defects were caught by the files' own laws and **cured before commit, not declared away**: **(i)** facility 19's per-router receipt used a `for f in …; do … "$f" …; done` loop — a ⟨cmd⟩ consuming a shell variable — **replaced by `grep -c` over a glob**, which answers the same question with no variable at all; **(ii)** one `…/easing.js` elided path completed to a literal absolute path (the R3-9.1 unrunnable-machinery class); **(iii)** the glass relay's `awk '{print $1}'` carried a `$` between backticks, **replaced by `cut -c1-2`**. Final readings, both runs, all three files: ⟨cmd⟩ `/usr/bin/grep -oE '⟨cmd⟩ \`[^\`]*\$[^\`]*\`' <file> | wc -l` → **0** · `/usr/bin/grep -o '…/[a-zA-Z@]' <file> | wc -l` → **0** · `/usr/bin/grep -c 'grep -[a-z]*P' <file>` → **0** (BSD-portable; no PCRE, no lookaround) · `/usr/bin/grep -oE 'F-W[0-9]+\.md:[0-9]|COHESION\.md:[0-9]' <file> | wc -l` → **0** (no line coordinate into a live sibling spec; siblings cited by §-heading, gate id or row label — the frozen corpus and product source are the two admissible classes and are marked as such). ⟨cmd⟩ `git diff --check` → clean. **Line 1 of each created file is its `SERVED MODEL:` line.**

**ACT 7 — commit (ONE commit; the spec's own commit plan declares this family and it is not split).** §Format/lint cadence reads *"spec · rulings · **facility-19 + INBOX row + letters** · close"* — the ledger, its two letters and their E13 rows are one meaning and land together, so no row can ever cite a letter that is not yet committed. **`c096c8ab`** — *docs(x-f-w2/.d): facility 19 under RD-8's one-way law + the FN-1..FN-7 reply + the SS-6 glass relay + two E13 rows*. ⟨cmd⟩ `git show --stat --oneline c096c8ab` → **4 files changed, 497 insertions(+)**, and the four are exactly this unit's four writable paths. **No sibling seat's staged path was swept in** (the value.js index carried four other seats' dirty paths throughout); pathspec was given **on the commit itself**; no `git add -A`/`-u`, no `commit -a`, no reset, no unstage, no stash.

#### Gate readings — BEFORE → AFTER

| gate | BEFORE (wave open, re-measured at this seat) | AFTER | witness |
|---|---|---|---|
| **G9** the post-uplift parser constraint is **measured**, not transcribed | **RED** — both symbols deleted (0 hits); *"the 4.0.0 `oklch()`/`light-dark()` probe of `parseCssColor` is still unrun"* | **GREEN** | leg 1 ⟨cmd⟩ `/usr/bin/grep -rn 'colorUnit2\|color2' src/ dist/ \| /usr/bin/wc -l` → **0**, so the banked rule names two symbols that do not exist at the target — **a 0.13.0-window rule, not transferred**. Leg 2 probed at 4.0.0 **on BOTH copies** (fourier's installed published tarball and value.js's own `dist/`), **agreeing row for row, double-run**: `oklch(…)` **ok:true** · `light-dark(…)` **ok:false `css_syntax`** · `var(--…)` **ok:false `color_context_required`** · hex/`rgb()` controls ok · legacy `rgba(,,,)` **ok:false**. **The `light-dark()` probe-element rule is the one constraint that transfers whole**, and fourier's landed path honours it (used-value read → parse). Recorded at `facility-19.md` §6 |
| **G13** facility 19 exists under RD-8's **one-way** law | **RED** — ⟨cmd⟩ `ls docs/tranches/X/fourier/facility-19.md` → *No such file or directory* | **GREEN** | the file exists (226 L). **Each of the gate's three RED inputs measured FALSE at the settled bytes**: the bidirectional schema is **not** extended (JSON untouched — 0 in the commit's file list); `counts` carries its **as-built census AND its command** (as-specified 41 · as-built 30, enumerated per router); and **no symbol is listed without a consumer, no import is unlisted** — the set-difference is ∅ both ways, with the instrument published so it can be falsified. *(The gate's second, fourier-side leg — the correspondence grep — reads **7** unchanged, as a value.js-tree create cannot move it; the **1 → 7** drift was minuted at wave open and is not re-litigated here.)* |
| **G18** the value.js-side reciprocal is **FILED**, not folded into fourier's ledger | **RED** — the filing does not exist | **GREEN** | the **Buffer-input totality nit** re-measured at the adopted pin and filed to **X-W9**: ⟨cmd⟩ `parseStylesheet` on glass-ui **8.0.0**'s `dist/styles/index.css` → **STRING `ok:true`** (1,514 B) · **BUFFER `THROW TypeError: e.startsWith is not a function`** · `null`/`undefined` **THROW** on `.length` · `42` and `{}` **`ok:true`** (a non-string silently accepted) — the surface **widened by measurement**, double-run. Filed at `facility-19.md` §7 **R-d** and the letter §5 **R-d**, both routed to **X-W9**, and rowed at **O-29**. **Both RED inputs avoided**: arm B's unreproduced `reading 'slice'` throw is **not** filed as fact (it reproduces with neither input), and the corrupt-tail diagnostic is **not** routed as a value.js defect — it is glass evidence, and at 8.0.0 it is **gone** (`ok: true`), context for F.W0/F.W1's FR-NP-32, claimed by nobody. **F.W2 claims no GREEN from any of it** |
| **G19** census closure against `CENSUS-CANONICAL.md` | **RED** — the set-difference was **unrun** at this open | **GREEN — ∅ BOTH WAYS** | **LHS = the canonical §2 `F.W2` roster, read whole and VERBATIM, and nothing else** (no detector, no re-cut, no check file, no §-arithmetic): ⟨cmd⟩ `/usr/bin/grep -n '^### F\.W2' docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` → `### F.W2 — **23 rows**`; operand sha256 ⟨cmd⟩ `shasum -a 256 … \| cut -c1-12` → **`f44362757458`**, **unmoved from the spec's pin**. **RHS = §8a's 23 bookings** (its 8 `CITED, NOT BOOKED` rows excluded, by a whole read of the table). **LHS 23 (distinct 23) · RHS 23 (distinct 23) · `LHS \ RHS` (escaped) = ∅ · `RHS \ LHS` (fabricated) = ∅**, double-run, id-for-id |

**No other gate is touched, and no GREEN of F.W0/F.W1/F.W3/F.W4/W.L6 is claimed** (FR-GIG-5).

#### Findings this unit adds to the record

- **F-18 · THE R1 CLASS REPRODUCES AT 4.0.0 — `parseCssColor` IS NOT TOTAL, and `fr-App` K-11's discharge does not transfer.** ⟨cmd⟩ at both 4.0.0 copies, double-run: `parseCssColor("oklch()")` and `("light-dark()")` **THROW** `TypeError: Cannot read properties of undefined (reading 'replace')`; `("__proto__")` and `("constructor")` **THROW** `TypeError: e.trim is not a function`; `"oklch("` · `"oklch(0.5)"` · `""` · `"toString"` · `"valueOf"` · `"hasOwnProperty"` all return the total `ok:false css_syntax` arm. **K-11's discharge was measured at 0.13.0 ONLY and G9's cell says so; the 4.0.0 target is now probed and the answer is that the crash survives.** **Consumer reachability measured, not assumed**: fourier's sole call site is `cssColorToHex(css)` (`lib/colors.ts:90`), whose argument is a `getComputedStyle` **used value** from the producer's token cache, with an unset property guarded one line earlier — **so the defect is value.js's with no live consumer path.** Filed as `R-a`/`R-b` → **X·V / V·π** ⊕ **X-W9**. **Not wrapped in a `try`, not worked around, not cured in the consumer.**
- **F-19 · `easing(name)` throws on five prototype keys at 4.0.0** — `__proto__` · `constructor` · `toString` · `valueOf` · `hasOwnProperty` each raise `TypeError: … is not iterable`, while `easing('nope')` correctly returns `{ok:false, error.code: easing_name_unknown}`. **O-14's I-4 row banked this as *"throws on 5 prototype keys"* and it reproduces exactly.** Filed as `R-c` → **X-W9 / W.L6**, beside the `easingNames()` ship.
- **F-20 · G18's nit is WIDER than "Buffer".** Beyond the banked Buffer throw, `null` and `undefined` throw on `.length`, and — the sharper half — **`42` and `{}` return `ok:true`**: `parseStylesheet` *silently accepts a non-string* rather than rejecting it. A totality filing sized at "Buffer" alone would under-state the surface; it is filed at what was measured.
- **F-21 · §3 F1's "six-symbol import set" is ELEVEN at HEAD.** A true reading of the pre-uplift tree, false at the settled bytes, moved by F.W1's transaction and by `.a`/`.b`'s own landings. **The ledger enumerates the tree, which is the only thing RD-8's law admits**; the six is recorded as superseded at both its statement sites (the ledger §2 and the letter §4) and **is not averaged**. Dated spec unpatched (E-3).
- **F-22 · `text-admin-label` is measured absent FOUR ways at the adopted pin**, so **MG-β's AA-15 collision premise is gone**: 0 in glass-ui 8.0.0's `dist/` (including the `class-names` chunk, so the regex-allowlist residue went with it), 0 in glass-ui's source, 0 in fourier's `web/src`, 0 in value.js's `demo`+`src`. **Recorded as a measurement, not a re-grade**: the banked row's disposition stays with its owner, and the SS-6 ask is a confirmation rather than a rescue.
- **F-23 · the SS-13 serialization question has a measured half.** value.js 4.0.0's `parseCssColor` accepts every modern used-value serialization (`oklch` · `rgb` space-form · `color()` · `lab` · `lch` · `oklab` · `hsl` · `rgb(… / α)`) and rejects exactly two relevant forms — **the legacy comma `rgba(r, g, b, a)`** (`.b`'s F-12, now with its consumer-seam consequence stated) and **`color-mix()`**. **So "drop-in vs format adapter" turns on what the cascade serializes for an alpha-carrying token**, and the failure mode is a silent `ok:false` (the consumer keeps its authored fallback), not a throw. **Not reachable for fourier's four opaque tokens today** — asked of the producer as a question, and filed value-side as `R-e`.
- **F-24 · a sibling moved mid-write, and the receipt was re-shaped rather than re-numbered.** The glass tree's dirty set read `11 M · 2 ??` when the relay's opening paragraph was drafted and `11 M · 3 ??` on the settle re-run minutes later (the new entry glass's own `src/components/_shared/overlay/shortcuts.ts`). **The claim was re-based from a cardinality onto a membership** — the negative grep for any path this letter could have produced, **0** on both runs — which is the §8d lesson applied at authoring time rather than discovered at a later pass.

#### Residuals and escalations

**Escalations: none.** Every act this unit's sections prescribe was performed as specified, at the bytes; no specified cure was found impossible, and nothing was substituted, masked or skipped.

Residuals carried to their owners:

- **The six filed reciprocals are OPEN by construction, and that is their correct state**: `R-a`/`R-b`/`R-e` → **X·V / V·π** ⊕ **X-W9** · `R-c`/`R-d` → **X-W9** · `R-f` (SCI-1) → **X-W9 / W.L6**. **F.W2 files; it does not cure, and it claims no GREEN for any of them.**
- **SS-6 and SS-13 remain OPEN and are now ASKED** — the relay exists and is rowed (`O-30`); R-ii's two conditions are discharged by glass-ui's answers, never by this seat. **SS-13's live-frame half stays `.b`'s declared residual** (UNPROVEN-NEEDS-LIVE, backend-gated, under binding probe parsimony).
- **Delivery of both letters into the sibling trees is the X formation mail seat's act** (the O-27 precedent): this seat's bound is four value.js paths, and **zero fourier bytes and zero glass bytes were written** (fourier `git status --porcelain` → **∅**, HEAD `f7fa1e3`, before and after).
- **Filename divergence, disclosed rather than elided**: §2a's create rows spell the two letters `value-to-fourier-2026-08-DD-fn-answers.md` and `value-to-glassui-2026-08-DD-token-color-seat.md`. The `DD` resolves to the **execution date, never back-dated** (the F.W6 O-27 precedent, and this wave's own §Unit-plan lock), and **the month moved with it** — the files land at the `2026-09-18` spellings the writable set binds. The dated spec is **not patched** (E-3).
- **`.a`'s G15 ULP escalation and `.b`'s SS-13 frame residual are untouched by this unit** — neither is within `.d`'s gates, and neither is resolved by anything above.
- **`INBOX.md`'s wave rows are now landed**; the wave does not close with UNREAD mail in its scope.
