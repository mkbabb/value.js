SERVED MODEL: claude-opus-5-5

# F.W14 — execution record (Track C, X·F)

Spec: `docs/tranches/X/fourier/waves/F-W14.md` (26 lines, read whole, incl. the §0bv / §0bw / §0ca addenda). Authority: COHESION §0bu (OA-42) · §0bv (OA-43) · §0bw (OA-44/45) · §0ca (audit fold, UIA-F-51 relay-only) · §0cb R-5 (10.0.1 repin cures no docket row by credit; `^8` moves explicitly) · §0bs (repin pattern) · §0bt (ADJACENT-LINE RULE). Tree: `/Users/mkbabb/Programming/fourier-analysis`, branch `m/w1-bump-migration`, HEAD `216ffbd` (= origin). Frames: `docs/tranches/X/fourier/evidence/W14/owner-2026-09-23-{malformed,grayed,blurry,hierarchy}.png` (malformed read at open).

## Open

SEAT 0, `claude-opus-5-5`, 2026-09-23 15:14 EDT. Fresh open (no prior record; LEDGER F.W14 row = `planned`).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → 21 ` M docs/tranches/J/audit/screenshots/before/*.png` + `?? .worktrees/` (the banked F.W13 state; the PNGs are the `.r` J-audit rewrite residual — owned by `.r`, left untouched here). value.js: this record absent, `LEDGER.md` clean. Nothing inherited.

**Preconditions ("Opens after": F.W13 CLOSED and the UI-AUDIT fourier register landed).**
- ⟨`grep -n "^| F.W13 " LEDGER.md`⟩ → `**CLOSED 2026-09-17 (honest-RED: DOCK-SCROLL-MORPH)**` (RESUME 1 Check 1, CONFORMANT-HONEST-RED). GREEN.
- ⟨`ls -la docs/tranches/X/audit/UI-AUDIT-fourier.md`⟩ → 192338 bytes; ⟨`grep -oE "UIA-F-[0-9]+" … | sort -u | wc -l`⟩ → **256** rows (register Counts: BROKEN 51 · HIGH 70 · MEDIUM 93 · LOW 42; 74 with a GLASS part, Appendix B). GREEN.
- Owner-gated items: all RULED — OA-42 §0bu, OA-43 §0bv, OA-44/45 §0bw, the audit fold §0ca, the repin §0bs/§0cb R-5; model law = Opus 5.5 every seat (§0bc, spec §State). Nothing re-opened.

**E13 Step-0 (four paths, read-only, 15:14 EDT).** ⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → `BL/ BK/ BJ/` (BL newest; BK coordination still swept). ⟨`find <V, V/coordination, BK/coordination, BL, keyframes V/coordination, atlas P/coordination, fourier docs/tranches> -newermt '2026-09-23 14:50'`⟩ → value.js `INBOX.md` (self); BK `valuejs-outbound-2026-09-23-ui-audit-relay.md` (ours, O-59); BL `FORMATION-PROGRESS.md`, `audit/{INBOUND,REGISTRY}.md`, `audit/round-2/R2-05.md`, `design/structure/pass-1/D1-*.md` (glass-internal; `7362b3bf` = O-59 banked = I-44, rowed READ; `9831f0e4` = I-43, FOLDED §0cb); keyframes V · atlas P · fourier: none. No glass-outbound letter to value.js/fourier. **0 unrowed · 0 new UNREAD in F.W14 scope.** I-44 (UIA-F-51 recorded blocking at BL) is the glass half `.u` routes by id.

## Baseline

BEFORE, fourier `216ffbd`, served `http://localhost:3100` (⟨`curl -w %{http_code}`⟩ → `200`), host ⟨`uptime`⟩ → load 53.9 / 54.7 / 38.3. Probe = one bounded headless script (removed after; fourier porcelain unchanged), 1440×900, light, DPR 1 and 2; logs scratchpad `fw14-probe{1,2}.json`, `fw14-tsc.log`, `fw14-vitest.log`.

| gate | unit | BEFORE | reading |
|---|---|---|---|
| G-m pin | `.m` | ⟨`grep '@mkbabb/glass-ui' web/package.json`⟩ → `"^8.0.0"`; ⟨installed `node_modules/@mkbabb/glass-ui/package.json` version⟩ → `8.0.0`; ⟨`npm view @mkbabb/glass-ui version`⟩ → `10.0.1` | **RED** |
| typecheck | Close | ⟨`npx vue-tsc -b`⟩ → exit 0 | GREEN (hold) |
| unit tests | Close | ⟨`npx vitest run`⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)` | GREEN (hold) |
| e2e full suite | Close | banked at this same HEAD (F.W13 RESUME 1 Close ×2 + Check 1): `18 failed · 3 skipped · 95 passed`; baseline-RED set = vc `:80 :101 :122` · fullscreen `:40` · contrast-floor `:82`×2 `:128` · gallery-admin-a11y `:91 :103 :114 :125` + the `G-F12-3-LOAD` timeout family. Not re-run (RESUME-economy: HEAD unmoved since the banked double-run) | baseline-RED named |
| G-t (OA-42) | `.t` | bytes: `AdminAuditLog.vue:298-314` — each row a `cartoon-card` (border + offset stamp = the doubled rule) on `grid-cols-[auto_auto_1fr_auto]`; the action a glass `Badge size="sm"` (8.0.0) holding `JANITOR:HARD_DELETE_VISUAL`; the timestamp `toLocaleString` in an `auto` column that wraps (frame). Siblings with the same list shape: `AdminFlaggedPanel.vue`, `AdminUserList.vue`. Served probe owed at `.t`'s own BEFORE (admin session required) | **RED** (frame + bytes) |
| G-g (OA-43) | `.g` | `/w` at rest (no image): the only sidebar node `configurator-aside` → 0 ancestors with `opacity≠1`/`filter≠none`/`inert`, bg transparent. The image-loaded state (the frame's state) is NOT measured here — `.g` owns that BEFORE | rest limb GREEN (vacuous: sidebar absent at rest); loaded state RED by frame |
| G-p (OA-44) | `.p` | backing store vs CSS×DPR: `/equation` 988×572 → `988×572`@1 · `1976×1144`@2 ✓; `/w` 1422×810 → `1422×810`@1 · `2844×1620`@2 ✓; `/`, `/morph`, `/gallery`: 0 visible canvases at rest. DPR sources: `useCanvasSetup.ts:21-31`, `ConvergencePlot.vue:203`, `FrequencyGraph.vue:75`; `BasisCanvas.vue:530` unmeasured (image mode). No `devicePixelContentBoxSize`, no DPR-change listener anywhere (⟨`grep -rn devicePixelContentBoxSize web/src`⟩ → 0) | width/height limb **GREEN-BEFORE-CURE at rest** (finding); DPR-change/zoom limb + stroke-edge crop + loaded-state canvases RED |
| G-h (OA-45) | `.h` | census (bytes): slider sites — FunctionInput 2 · HarmonicLevelGrid 2 · MorphPhaseConfig 1 · BasisSelector 3 · ContourSettings 5 · EditorControlsDock 1 · EquationPanel 1 · GlassTimeline 2 (+ `ui/SliderControl.vue` the local wrapper); served `role=slider`: `/equation` 3 · `/morph` 5 · `/w` rest 0. The idiom is not one shape (frame: label / value line / thumbless bar = 3 rows) | **RED** |
| G-u | `.u` | 256 UIA-F rows open (register), 74 with a glass part; UIA-F-51 GLASS (relay-only via O-59, I-44 blocking at BL) | **RED** |
| G-r | `.r` | ⟨`grep -rniE 'scroll.?to.?top' web/src`⟩ → only `MobileFloatingToc.vue` + `PaperView.vue` (desktop ToC has none); `.sidebar-link` radius named at `style.css:346`; `e2e/visual-baseline.spec.ts:34` + `visual-checkpoint.spec.ts:16` write into `docs/tranches/J/audit/screenshots/` (21 PNGs dirty now); `.c` r1–r4 open (F.W13 Close §5) | **RED** |

**GREEN-BEFORE-CURE (R.2 findings):** (1) G-p's backing-store limb at rest on `/equation` and `/w` at DPR 1 and 2 — the owner's blur is therefore not an unscaled backing store in these states; `.p` must find the cause in the loaded/animated states (image mode, epicycle draw, thumbnails, any CSS transform/scale of the canvas, a DPR change after mount) and gate the stroke-edge crop. (2) G-g's opacity/filter/inert limb at `/w` rest — vacuous (no sidebar at rest); the frame's state is image-loaded.

## Unit plan

Seven units, all Opus 5.5 (spec §State; no Fable/design-author seat named — `.h` is an Opus design seat at effort high). Strictly serial per the spec's final Order line (§0bw): **[`.m`] → [`.t`] → [`.g`] → [`.p`] → [`.h`] → [`.u`] → [`.r`]**; concurrency 1 (every unit may touch `web/src/**`). Writable paths are fourier-relative unless marked `vjs:`; every unit also writes `vjs:docs/tranches/X/execution/C/F-W14.md` (its receipt) and minimal cells/lines of `vjs:…/execution/LEDGER.md`. The spec carries no §File Bounds table; the sets below derive from each unit's text plus the ADJACENT-LINE RULE (§0bt). Never: glass-ui, sibling repos, `scripts/dev/dev.sh`, test/assertion deletion. Glass rows ride O-59/BL by id (never a local override). Locks: `.m`'s pin + lockfile + migration = ONE commit family (`feat(web)!:` repin), not split; `.t`/`.g`/`.p`/`.h` each: cure commit(s) + its gate spec + frames; `.r`'s re-point of the J-audit writers and the restore of the 21 tracked PNGs to HEAD ride ONE commit.

| id | spec section | writable | gates |
|---|---|---|---|
| F.W14.m | Units `.m` (F-W14.md:11) + §0bs + §0cb R-5 | `web/package.json`, `web/package-lock.json`, `web/src/**`, `web/e2e/**` (only goldens/selectors the migration moves, diff read first) | G-m (installed 10.0.1 exact); vue-tsc 0; vitest 84/84; e2e `--workers=1` REDs ⊆ baseline set, load recorded; DOCK-SCROLL-MORPH re-read |
| F.W14.t | Units `.t` (:12) | `web/src/components/visualization/gallery/**`, `web/e2e/f-w14-admin-table.spec.ts`, `web/e2e/screenshots/f-w14/**` | G-t ×2 headed, 1440+390, both themes: badge scrollWidth ≤ clientWidth; timestamp one line at 1440; one computed rule between rows |
| F.W14.g | ADDENDUM §0bv (:17) | `web/src/**`, `web/e2e/f-w14-veil.spec.ts`, `web/e2e/screenshots/f-w14/**` | G-g ×2 headed, light+dark, 1440+390: opacity 1 / no filter / no inert on every ancestor; surface ΔE_OK ≤ 0.02 to glass light token + equal to other panes; ink AA |
| F.W14.p | ADDENDUM §0bw `.p` (:20) | `web/src/**` (canvas owners + `composables/useCanvasSetup.ts`), `web/e2e/f-w14-dpr.spec.ts`, `web/e2e/screenshots/f-w14/**` | G-p ×2 headed DPR 1+2: every canvas width/height = round(client×dpr) incl. after resize/DPR change; stroke edge ≤ 2 device px; before/after crops |
| F.W14.h | ADDENDUM §0bw `.h` (:21) | `web/src/**`, `web/e2e/f-w14-control-row.spec.ts`, `web/e2e/screenshots/f-w14/**`, design notes in the record | G-h: census re-run all slider rows on the idiom; row height = idiom ±4 px; before/after frames per page 1440+390 |
| F.W14.u | Units `.u` (:13) + ADDENDUM §0ca (:24-26) | `web/src/**`, `web/e2e/**` (additive), `web/e2e/screenshots/f-w14/**`, `vjs:docs/tranches/X/relay/` (dated addendum-beside only), `vjs:docs/tranches/V/coordination/INBOX.md` (rows/lines) | every UIA-F row: CURED at cause with its falsifier ×2, or ROUTED-GLASS by id (O-59); frameless rows framed on the served page BEFORE cure; UIA-F-51 relay-only |
| F.W14.r | Units `.r` (:14) | `web/src/components/paper/**`, `web/src/style.css`, `web/e2e/visual-baseline.spec.ts`, `web/e2e/visual-checkpoint.spec.ts`, `web/e2e/f-w14-residuals.spec.ts`, `docs/tranches/J/audit/screenshots/before/*.png` (restore to HEAD only) | desktop ToC scroll-to-top present + acts; `.sidebar-link` radius on the canon; `.c` r1–r4 each cured or ruled; full e2e leaves `git status` of `docs/tranches/J/` clean ×2 |

Close (after `.r`): vue-tsc 0 · vitest · e2e `--workers=1` (REDs = the named baseline set) · served-page gates re-read by the check seat.

## Unit receipts

### F.W14.m

SERVED MODEL: claude-opus-5-5 · 2026-09-23 · spec F-W14.md `.m` (l.11) + COHESION §0bs (repin pattern) + §0cb R-5 (the `^8` moves explicitly; the repin credits no docket row) + §0bt (ADJACENT-LINE RULE).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only the 21 ` M docs/tranches/J/audit/screenshots/before/*.png` + `?? .worktrees/` (`.r`'s residual, outside this set). Nothing inherited.

**Read (read-only, whole).** glass-ui `CHANGELOG.md` §10.0.1 (l.3-26), §10.0.0 (l.27-206), §9.0.0 (l.207-309); `MIGRATION.md` §10.0.0 (l.8-375), §9.0.0 (l.376-579), §8.1.0 scanned for breaks (additive; shipped inside 9.0.0). Break list vs the fourier tree (⟨`grep -rnE "glass-ui/(canvas|search)|colorResolver|ColorResolver|defaultBlobColorResolver|ringsAt|renderAt|startCollapsed|start-collapsed|alwaysExpanded|always-expanded|collapseDelay|collapse-delay|dock-max-inline-size|cm-serif|sidebar-top-btn|useSidebarFollow|parentId|InstanceType<typeof SegmentedTabs|text-caption|darkModeSyncScript|aria-modal|<Metric|handmark" web/src web/index.html web/e2e`⟩):
- `./canvas`, `./search`, `colorResolver`/`ColorResolver`/`defaultBlobColorResolver`, `ringsAt`, `renderAt`, handmark types, `--dock-max-inline-size`, `AuroraAtoms.light`: 0 consumer sites.
- `GlassDock` 14→6 props (9.0.0): 4 sites. `cm-serif` → `font-serif-math` (10.0.0): 25 sites / 16 files. `SegmentedTabs` generic (10.0.0): 3 `$event as` casts. `darkModeSyncScript()` re-hash (10.0.0): the inlined copy in `web/index.html`. Layering into `@layer components` (10.0.0) + the new `.configurator-shell`: found at the served page (below).
- No action (recorded): `useSidebarFollow` here is latex-paper's, not glass's (`PaperView.vue:8`); no `parentId` reader; `text-caption` at `VisualizationView.vue:272` takes the producer's upright paint (no `italic` restored); `<Metric>` sites pass no `delta`; `aria-modal` only in comments.

**Acts, in order.**
1. ⟨`npm install --save-exact @mkbabb/glass-ui@10.0.1`⟩ → `changed 1 package`; `web/package.json:19` `"^8.0.0"` → `"10.0.1"`; lockfile: version/resolved/integrity 10.0.1 and the optional `@mkbabb/pencil-boil` peer gone (9.0.0). ⟨`node -p "require('./node_modules/@mkbabb/glass-ui/package.json').version"`⟩ → `10.0.1`; ⟨`npm ls @mkbabb/glass-ui`⟩ → `@mkbabb/glass-ui@10.0.1`.
2. ⟨`npx vue-tsc -b --force`⟩ → exit 0 on the bare bump. The type-level breaks do not reach this tree; the prop removals fall through as attributes and do not fail to compile, so they were migrated from the break list.
3. GlassDock (9.0.0 fold onto `collapse`): `CanvasControlsDock.vue:41` drops `:start-collapsed="true"`; `EditorControlsDock.vue:63` drops `:collapse-delay="2000" :start-collapsed="true"`; `AnimationControls.vue:118-119` drops both (default `collapse="closed"` = the old collapsed mount; `collapseDelay` has no successor, so every dock idles 3600 ms); `AppDock.vue:71` `always-expanded` → `:collapse="false"`.
4. `cm-serif` → `font-serif-math`, 25 sites / 16 files (perl, word-bounded; ⟨`grep -rn cm-serif web/src`⟩ → only the historical prose in `style.css:54-69`). `style.css:92` comment re-stated: the app's `@theme --font-serif-math` replaces the producer's new `serif` default. ⟨served `curl /src/style.css | grep -o '.font-serif-math…'`⟩ → `.font-serif-math { font-family: var(--font-serif-math) }` emitted; `.cm-serif` no longer emitted.
5. SegmentedTabs generic: `GalleryView.vue` (`GalleryTab` alias; `tabOptions` typed `SegmentedTabOption<GalleryTab>[]`), `VisualizationView.vue:230`, `EquationView.vue:348`: the `:model-value` + `$event as …` pair becomes `v-model`. vue-tsc 0.
6. `style.css` G-F9-5 block (`@layer glass-overrides { :where(.glass-capsule-track) { --muted-foreground: var(--on-glass-muted-strong) } }` + its comment) deleted: ⟨`grep -rhoE ':where\([^{]*glass-capsule-track…' dist`⟩ → `:where(.feedback-tone,.glass-capsule,.glass-capsule-track){--muted-foreground: var(--on-glass-muted-strong);…}` in `styles/glass/ladder.css`. The producer now binds this itself (10.0.0 "three label sites"), so the consumer copy is retired, not shimmed.
7. **Adjacent edit (§0bt)** `web/index.html:88-90,102`: the inlined dark-mode sync script re-derived ⟨`node -e 'import {darkModeSyncScript} from "@mkbabb/glass-ui/dark"; console.log(darkModeSyncScript())'`⟩ → 313 B (10.0.0: an unreadable store reads as absent), with its provenance line re-dated. Reason: the file's own contract says it holds the generator's output at the adopted pin, and the pin moved. Same repo, same concern.
8. Dev server: the long-lived `vite web --port 3100 --strictPort` (pid 14707, started 09-22) was still serving the 8.0.0 prebundle. I restarted it with the same command, and its log reads `Re-optimizing dependencies because lockfile has changed`.
9. **e2e run 1** (a scouting run on the served 10.0.1 before step 10; load ⟨`uptime`⟩ 25.13 → 45.19): `19 failed · 3 skipped · 94 passed`. Six REDs outside the baseline: `f-w13-image-controls :80 :104 :202` and `f-w13-image-empty :64 :97 :117`, all failing on `.drop-target` "Received: hidden". Three more were golden moves: `visual-checkpoint :144 :163 :198`.
   - **Root cause, measured** with a headless probe of `/visualize` walking up the ancestor chain: `.drop-target` h=0 ← `.configurator-stage` h=0 ← `section.configurator` h=0, gtc `1038px 400px` ← `div.configurator-shell` h=**2** ← `div.contents`. At 10.0.0 the Configurator wraps its grid in an outer `.configurator-shell`, and its declared `class` prop lands on that shell. The host's scoped `.viz-configurator[data-v]` rules therefore matched nothing: the shell is not the component's scoped root, which is the producer's `display: contents` expand host (and that is where `data-sidebar` falls through). The flex envelope was lost, the stage went to 0 px, and the rest-state band closure stopped working, leaving the aside at the producer default of 400 px.
10. **Configurator migration** (`VisualizationView.vue` scoped style). The shell rule (flex:1, min-height:0, margins) now uses `:deep(.viz-configurator)`. The aside band moves off the host's `grid-template-columns` and onto the producer's `--configurator-aside-{min,max}` pair (320/360 at ≥1024, 360/400 at ≥1280, 400/440 at ≥1536), which the grid's two-column `@container (inline-size >= 64rem)` rule reads. At rest the band closes through `[data-sidebar="none"] > :deep(.viz-configurator)` { both tokens 0px }, and the aside through `… > [data-slot="configurator"] > .configurator-aside { display:none }`. The band transition and its PRM zeroing are now on `:deep(.viz-configurator > [data-slot="configurator"])`, as is the mobile flex column; the stage rule becomes `:deep(.viz-configurator .configurator-stage)`.
    - Probe after the change: at 1440, shell 1424×812, grid 1422×810, gtc `1422px 0px`, aside `none`, stage and drop 1422×810 (the same stage box the Baseline measured at `/w`). At 390, shell 382×828, grid 380×826, stage 380×826.
    - **e2e selector the migration moves** (diff read): `f-w13-image-empty.spec.ts:37` now reads the transition target as `.viz-configurator > [data-slot="configurator"]` (the grid, where the band transition now runs) instead of `.viz-configurator` (the shell). The assertion `configurator:grid-template-columns` is unchanged.
    - ⟨`playwright test f-w13-image-empty f-w13-image-controls --workers=1`⟩ → `6 passed`.
    - Residual (r1): the two-column switch is now the producer's container query (shell inline-size ≥ 64rem), not the viewport's 1024 px. For viewports of about 1024-1040 px the grid stays single-column while `isDesktop` is already true.
11. **Golden moves, each diff read before re-baselining.**
    - `checkpoint-disclosure-body` (2141 px): the diff sits only on the Presets chip rows and the Notation chips. They shifted horizontally from 10.0.1's Button stadium pad (xs/sm 8 → 10/14 px) and 10.0.0's upright `text-caption` chip rung.
    - `checkpoint-admin-banner` (195 px): the diff is only the Logout button, from the same stadium pad.
    - `checkpoint-tooltip-trigger-mobile` (86 px, 60×61 both): a sub-pixel shift of the icon inside the unchanged box.
    - All three are producer paint, not consumer regressions. Re-baselined with ⟨`playwright test visual-checkpoint.spec.ts -g "item 8 —|item 3 —|@coarse item 4" --update-snapshots`⟩ → `3 passed`. vc `:80 :101 :122` (baseline-RED) were NOT re-baselined.
12. Commit (ONE family: pin, lockfile, root migration, the adjacent `index.html` edit, the e2e selector and the 3 goldens; 29 paths): fourier **`3c688d1`** `feat(web)!: X.F.W14.m — repin @mkbabb/glass-ui ^8.0.0 -> 10.0.1 exact, migrated at the root`. Pushed: ⟨`git rev-parse HEAD origin/m/w1-bump-migration`⟩ → both `3c688d16`.

**Gates, BEFORE → AFTER (settled bytes, double-run).**
| gate | BEFORE | AFTER |
|---|---|---|
| G-m pin | `"^8.0.0"`, installed `8.0.0` | ⟨`grep glass-ui web/package.json`⟩ → `"10.0.1"`; installed ⟨`node -p …version`⟩ → `10.0.1` — **GREEN** |
| vue-tsc | exit 0 | ⟨`npx vue-tsc -b --force`⟩ ×2 → exit 0 · exit 0 — **GREEN** |
| vitest | 13/84 | ⟨`npx vitest run`⟩ ×2 → `13 passed (13)` · `84 passed (84)`, both runs — **GREEN** |
| e2e `--workers=1` | 18 failed · 3 skipped · 95 passed (banked) | run 2: `10 failed · 3 skipped · 103 passed (4.4m)`, load 34.30 → 54.18; run 3: `10 failed · 3 skipped · 103 passed (3.4m)`, load 54.18 → 16.28. REDs both runs = vc `:80 :101 :122` · contrast-floor `:82`×2 `:128` · gallery-admin-a11y `:91 :103 :114 :125` — **⊆ baseline, GREEN**. fullscreen `:40` and the G-F12-3-LOAD timeouts did not fire in either run. contrast-floor datum: light `18 of 36 pairs`, dark `10 of 36 pairs` (no banked count to compare) |
| DOCK-SCROLL-MORPH | honest-RED (O-55) | re-read at the installed 10.0.1 dist: ⟨`grep -rlE 'compactOnScroll|scrollCompact|progressRim|scroll-compact|scrollProgress' dist | wc -l`⟩ → `0`; `DockProps` = `fitContent · backdropMode · shape · orientation · collapse · backgroundCanvas`; the exposed handle is `expanded · isPinned · isHeld · graspHeld · isTransitioning · expand · collapse · keepOpen · release`; ⟨`grep -rniE 'rim|progress|compact' dist/components/dock/*.d.ts …`⟩ → 0 dock hits. **Still honest-RED, not cured by the repin** (owner: glass, O-55) |

**§0cb R-5.** No docket row is credited to this repin. The glass rows are re-read by their own owners: `.u` through O-59/BL, and the landing repin once BL names a version.

**Residuals.**
- (r1) The container-vs-viewport two-column seam at about 1024-1040 px (step 10).
- (r2) The `collapseDelay` 2000 → producer 3600 ms idle on the three canvas docks, which has no knob at 10.0.
- (r3) `visualization-crud.spec.ts:664` stays `fixme`: it cites producer SegmentedTabs underline ink 4.33:1, which 10.0.0 lists as 5.22/5.02. I did not un-fixme it here (R-5: no credit); `.u` re-reads it.
- (r4) Historical prose that still names `:start-collapsed` (`CanvasControlsDock.vue:127`) and `cm-serif` (`style.css:54-69`) is left as record.
- (r5) The e2e runs rewrote `docs/tranches/J/audit/screenshots/before/*.png` again. This is `.r`'s residual; those files were not staged.

**Adjacent edits (§0bt).** `web/index.html:88-90,102` (step 7).

**Escalations.** None.

### F.W14.t

SERVED MODEL: claude-opus-5-5 · 2026-09-23 · spec F-W14.md `.t` (l.12) + COHESION §0bu (OA-42) + §0bt (ADJACENT-LINE RULE) · frame `fourier/evidence/W14/owner-2026-09-23-malformed.png` (read).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only the 21 ` M docs/tranches/J/audit/screenshots/before/*.png` (these are `.r`'s) + `?? .worktrees/`. Nothing in this unit's writable set was dirty, so nothing was inherited.

**Anchors at the true bytes (fourier `3c688d1`).** `AdminAuditLog.vue:298-314` holds each row as a `cartoon-card grid grid-cols-[auto_auto_1fr_auto]`, with the timestamp as a `span.tabular-nums` and the action as a glass `Badge size="sm"`. The anchor held, with no drift.
- The glass Badge at 10.0.1 is already content-sized: `badge-atom inline-flex … whitespace-nowrap` plus `:where(.badge-atom){flex:none}` (dist `mark.css`). The overflow is therefore caused by the consumer's per-row grid, not by the Badge.
- Siblings: `AdminUserList.vue:590` (`cartoon-card` rows) and `AdminFlaggedPanel.vue:364` (a glass `Card` per entry).

**Acts, in order.**
1. **Gate first.** I wrote `web/e2e/f-w14-admin-table.spec.ts`, 12 cases: the three panels × 1440/390 × light/dark, with a colour scheme per describe block.
   - Fixtures: the shared `stubAdminApi` plus two routes of its own, registered later so they answer first. The audit route serves the frame's five long actions (`janitor:hard_delete_visualizations`, `set_tier:featured`, `suspend_user`, `batch_users:delete`, `janitor:prune_audit`). The flagged route serves two entries, so there is an adjacent pair to measure.
   - The panel is the labelled collection itself (`[aria-label=…]`).
   - Assertions, per-item and soft:
     - every `.badge-atom` has `scrollWidth ≤ clientWidth`;
     - for each adjacent row pair, the painted rules are counted: the upper row's border-bottom, plus the lower row's border-top, plus any outer shadow on the upper row with a positive y offset. The count must be exactly 1;
     - Audit Log at 1440: each `time[datetime]` makes one line box (distinct `Range.getClientRects()` tops) and has `fontVariantNumeric ⊇ tabular-nums`.
   - Frames go to `e2e/screenshots/f-w14/{before|after}-<panel>-<w>-<scheme>.png`.
2. **BEFORE, measured on the served page** (`http://localhost:3100`, dev server on `3c688d1`, headed, `--workers=1`). ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-admin-table.spec.ts --project=chromium --workers=1 --headed`⟩ → `12 failed`.
   - **Audit Log**, all four cells: every adjacent pair reads **3 rules** (`upper border-bottom 2px + lower border-top 2px + upper offset shadow oklab(… / 0.32) -3px 3px 0px 0px, …`), which is the frame's doubled rule.
   - **Audit Log at 390**, both themes: **4 of 5 badges overflow** (`janitor:hard_delete_visualizations`, `set_tier:featured`, `batch_users:delete`, `janitor:prune_audit`). The target column collapses to 0 px, so `getByText('spiral-lattice-04')` reads `hidden` (the same as UIA-F-42), and the timestamp wraps over two lines. That is the owner's frame, reproduced. The frames are `before-audit-log-390-{light,dark}.png`.
   - **Audit Log at 1440**: the badges fit and the timestamp is one line. Those two limbs were already GREEN at 1440; only the rule limb was RED.
   - **Users**, all four cells: 3 rules per pair (the same `cartoon-card` stamp).
   - **Flagged**, all four cells: `[role="listitem"]` → `element(s) not found`. The glass Card drops the consumer's `role` (UIA-F-43), so the list had no listitems. A bounded headless probe (deleted afterwards) read the two `.flagged-card`s as `border-top 1px` / `border-bottom 1px` with an 8 px gap between them, at both 1440 and 390, so each adjacent pair reads **2 rules** and the role is `null`. No before-frame was written for Flagged, because the locator failed before the screenshot step. The probe numbers above are that cell's BEFORE.
3. **Cure: Audit Log** (`AdminAuditLog.vue`). The glass `DataTable` (`@mkbabb/glass-ui/data-table`, 10.0.1) sits on one glass `Card size="sm"` plate.
   - Columns (`auditColumns`), each a small render component that receives the producer's `value`/`row` props:
     - Time: a `<time datetime>` with `font-mono tabular-nums whitespace-nowrap`;
     - Action: the glass `Badge` (variant, tone and size unchanged), which the table cell sizes to its content;
     - Target: `block truncate`, with the full value kept in `title` (AA-40). The cell is `w-full max-w-0`, so it is the only column that yields;
     - IP hash: `text-mono-micro`, with the sr-only full hash (AA-15/AA-40).
   - Rows are keyed `${page}-${i}` through `_id`. `status` is loading/ready, and `filtered` is `hasFilters`, so the `empty` and `filtered-empty` slots carry AA-33's two headlines.
   - The producer's cell-padding tokens are set to the log's density: `[--table-cell-px:--spacing(3)] [--table-cell-py:--spacing(2)]`. `text-xs` was not added, because that utility is dead under glass ≥8 (UIA-F-57).
   - The old `Loading audit entries…` block is retired: the producer's own skeleton renders for loading with no rows.
   - Each body row is the producer's `TableRow`: one `border-b`, and `[&_tr:last-child]:border-0`.
   - At 390 the table scrolls inside the producer's `table-container` (`relative w-full overflow-auto`) rather than squeezing a cell. The error `role=alert` block, the AA-12 live region and the pagination `nav` are unchanged.
4. **Sweep: Users** (`AdminUserList.vue`). The rows now sit on one glass `Card size="sm"` plate (`v-show="userRows.length"`). `role="list"` is on an inner `div`, because the Card drops `role`.
   - Each row loses `cartoon-card rounded-lg` and takes the producer's table-row rule, `border-b border-border … last:border-b-0`, coloured with the `--border` token. The Badge is unchanged.
5. **Sweep: Flagged** (`AdminFlaggedPanel.vue`). The per-entry `<Card role="listitem">` becomes a `div role="listitem"` row (`flagged-row border-b border-border p-3 last:border-b-0`) on ONE `Card` plate (`flagged-queue`, `v-else` + `v-show`).
   - The F.W3 accent is kept and split: `--glass-accent: var(--destructive)` goes on the plate, and the 6 % destructive wash goes on each row.
   - The list now holds real listitems, which cures UIA-F-43's consumer half on this surface. The glass half, where Card drops `role`, stays with O-59.
6. ⟨`npx vue-tsc -b`⟩ → exit 0. ⟨`npx vitest run`⟩ → `13 passed (13)` · `84 passed (84)`.
7. **Regression read.** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/gallery-admin-a11y.spec.ts --workers=1`⟩ → `4 failed`: `:91 :103 :114 :125`, the named baseline-RED set.
   - Every one of them is the same single violation, `aria-hidden-focus` on `div[role="region"] > span[aria-hidden]` (2 nodes). That is the banner's region, not these panels.
   - No new rule fired: there is no table, list or listitem violation.
8. **Commit** (one family: the cure in 3 files, the gate spec and 20 frames; 24 paths): fourier **`b5a650f`** `fix(web/admin): X.F.W14.t — OA-42: …`. Pushed: ⟨`git rev-parse --short HEAD`; `… origin/m/w1-bump-migration`⟩ → `b5a650f` · `b5a650f`.
   - The frames are force-added under `web/.gitignore:1 *.png`, as F.W13's were: 12 `after-*` plus 8 `before-*` (audit and users).

**Gates, BEFORE → AFTER** (settled bytes `b5a650f`, served `:3100`, headed, `--workers=1`, double-run). ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-admin-table.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `12 passed (50.3s)` · run 2 `12 passed (46.9s)` (load ⟨`uptime`⟩ 94.86 after run 2).

| gate | BEFORE | AFTER |
|---|---|---|
| G-t badges (`scrollWidth ≤ clientWidth`), 1440+390 × light+dark | audit 390: 4/5 overflow (both themes); 1440: fit | every badge fits in all 12 cells, ×2 — **GREEN** |
| timestamp one line + tabular at 1440 | GREEN at 1440 (wrapped at 390, the frame) | 5/5 `<time>` one line box, `tabular-nums`, ×2 — **GREEN** |
| exactly one computed rule between rows | audit 3 · users 3 · flagged 2 (+ 0 listitems) | 1 per pair in every panel/width/theme, ×2 — **GREEN** |
| sweep over Flagged + Users | RED (above) | GREEN (the same 3 checks, same runs) |

**Residuals.**
- **(R-t-1, GLASS, relay only: never a local override.)** The glass `TableRow` (`border-b`) and `TableHeader` (`[&_tr]:border-b`) bind no border colour. Neither glass nor this app ships a base `border-color`, so the DataTable's row rules paint in `currentColor`.
  - Evidence: a bounded headless probe (deleted afterwards) at 1440 read each `tr` `borderBottomColor` as equal to its own `color`. Light: `rgb(28, 25, 23)` (header row `rgb(112, 89, 66)`). Dark: `rgb(233, 230, 226)`. The `--border` token is `light-dark(hsl(32 26% 70%), hsl(30 16% 34%))`.
  - Effect: the audit table's single rule is ink-coloured. It is still exactly one rule, so G-t is GREEN, but it is heavier than the swept Users and Flagged rows, which bind `border-border`.
  - I did not override the producer here, following the lock. It is owed to glass BL by id. `.u` holds the O-59 relay's writable set, so it should add this row there. Its source is glass `src/components/table/TableRow.vue:14` and `TableHeader.vue:11`.
- (R-t-2) The Flagged cell has no before-frame. The spec's locator failed before its screenshot step, because the Card dropped `role`. The BEFORE for that cell is the probe reading in act 2.
- (R-t-3) The UIA-F rows this cure touches are **not self-credited**. `.u` credits each one with its own falsifier:
  - UIA-F-42 (audit at 390: 0 px target, badge over timestamp);
  - UIA-F-108 (ledgers → DataTable): the audit ledger is now DataTable. The users ledger is on the one-rule row idiom inside a Card, not DataTable, because it has a selection checkbox, two-line identity and per-row actions;
  - UIA-F-43 consumer half (the flagged list now holds real listitems);
  - UIA-F-252 (in part: loading is now the producer skeleton).
- (R-t-4) The DataTable has `responsive` left false, so at 390 the audit table scrolls horizontally inside the producer's container. The card projection was not used because it draws a bordered card per row with a gap between them, which is two rules per pair.

**Adjacent edits (§0bt).** None.

**Escalations.** None.

### F.W14.g

SERVED MODEL: claude-opus-5-5 · 2026-09-23 · spec F-W14.md ADDENDUM §0bv (l.16-17) + COHESION §0bv (OA-43) + §0bt (ADJACENT-LINE RULE) · frame `fourier/evidence/W14/owner-2026-09-23-grayed.png` (read: light theme, image mode, the right sidebar under a grey veil beside the cream stage).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only `.r`'s 21 ` M docs/tranches/J/audit/screenshots/before/*.png` + `?? .worktrees/`. Nothing in this unit's writable set was dirty, so nothing was inherited.

**Cause at the bytes (fourier `b5a650f`, served `:3100`, image loaded on `/w/…`, headed).**
- ⟨ancestor walk from the first sidebar control to `html`⟩ → every ancestor reads `opacity 1`, `filter none` and `inert false`. The only backdrop filter is the producer's aside, `blur(20px) saturate(1.5)`, and `saturate` above 1 does not desaturate. So there is no opacity, filter or inert veil.
- The one painted surface under the controls is the producer shell `div.configurator-shell.glass-floating` (glass 10.0.1 `Configurator`). Its background computes to `color(srgb 0.204 0.148 0.083 / 0.18)`.
  - That value is glass's light veil: `--glass-veil-ink: oklch(0.28 0.035 70)` at the floating rung `0.14 + 0.04 = 0.18`, with `--glass-level: 1`. It is smoke laid over the paper `rgb(251 250 248)`, and the DOM composite reads `rgb(214.7 211.9 207.2)`.
  - Every other box between the shell and the controls is transparent: `.configurator-aside`, the scroll-port, `.viz-panel-left-wrap`, `.viz-panel-left`, and `.configurator-layer` (its `--configurator-section-tint` is `transparent` in the light arm).
- The stage is not veiled, because the consumer's `.canvas-container.cartoon-card` paints `--card` = `rgb(253 245 236)` over the same shell. So the aside was the only pane whose controls sat directly on the smoke. The frame's grey sidebar beside a cream stage is exactly this.
- Inks on the smoke: every `--muted-foreground` subtitle and value read **4.44:1**. The pressed `Epicycles` label (bare `--viz-fourier` on its 12 % tint) read **2.74:1**. The contour `Advanced` trigger, inked at `color-mix(--foreground 40 %, transparent)`, read **2.35:1**, which is a disabled look on a live control.

**Acts, in order.**
1. **Gate first.** I wrote `web/e2e/f-w14-veil.spec.ts` with four cases: 1440 and 390, each in light and dark (`colorScheme` per describe block). Each case loads the image through the page's own file input (`image-file-input`, `assets/animals/golden-retriever.webp`) and waits for `/w/…`, then for `Replace image`, and for every time-based animation in the sidebar to finish. It writes the frame `e2e/screenshots/f-w14/{before|after}-veil-<w>-<scheme>.png`, then opens every `ConfiguratorLayer` so that every control the sidebar holds is live. It reads three limbs in the page:
   - **Veil limb.** For every visible control (`button`, `input`, and `[role=slider|spinbutton|button|radio|switch]`), walk its ancestors and flag any `opacity≠1`, any `filter≠none`, any `grayscale()` or `saturate(<1)` in `backdrop-filter`, and any `inert`.
   - **Surface limb.** Composite every background from the root down to `.viz-panel-left`. Colours are resolved by a 1×1 canvas, so the value is the one that paints. Compare the result with `var(--card)`, resolved in the active theme, and with the stage pane's painted surface. Both must be within ΔE_OK ≤ 0.02, and the surface must be opaque.
   - **Ink limb.** For every visible text node in the sidebar, composite its `color` over its composited background and compute the WCAG ratio. The floor is 4.5, or 3 for large text.
   - **Instrument notes.** The first draft counted a closed layer's `configurator-layer-region[inert]`. Closed-disclosure inert is correct semantics, not a veil, so the gate now opens every layer and measures every control live. Scroll-timeline animations (the fading scroll-port's feather) never finish, so the settle wait ignores them.
2. **BEFORE, served, headed.** ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-veil.spec.ts --project=chromium --workers=1 --headed`⟩ → `4 failed`.
   - **Light, 1440 and 390:** `veils=0`. The surface reads `rgba(214.741, 211.937, 207.231, 1)` against `--card` = the stage = `rgb(253 245 236)`, so ΔE_OK(surface, card) = ΔE_OK(surface, stage) = **0.1036**. 51 inks, of which **38** are below AA (the diff's `+ 40` lines are `Array [` + 38 entries + `]`), with a minimum of 2.35: the `--muted-foreground` set at 4.44, `Epicycles` at 2.74, `Advanced` at 2.35, and `Fourier spectrum` at 4.02.
   - **Dark, 1440 and 390:** `veils=0`. The surface reads `rgba(13.635, 10.878, 7.024, 1)` against `--card` = the stage = `rgb(53 42 34)`, so ΔE_OK = **0.1445**. The ink minimum is 5.47.
   - Frames: `before-veil-{1440,390}-{light,dark}.png`. The light 1440 frame reproduces the owner's frame: a grey aside beside the cream stage.
3. **Cure 1, the surface (`VisualizationView.vue:253`).** The Configurator takes `class="viz-configurator glass-opaque"`.
   - `.glass-opaque { --glass-level: 0 }` is glass's named OPAQUE escape (glass `docs/canon/glass-system.md` §`--glass-level`: *"`level=0` is the OPAQUE escape (solid `--card` + `blur(0)`) through the SAME machinery"*; dist `styles/glass/ladder-undershadow.css`). The shell's own `--glass-veil` recipe, `color-mix(… ink/rung … calc(level·100%), var(--card))`, then resolves to `--card`.
   - The producer's tone is not overridden. The consumer selects the glass rung for an opaque instrument on paper, and that rung is the surface the stage card and every other fourier pane already paint.
   - A template comment records the measured cause.
   - Because the level inherits, I read the stage docks (play dock, canvas-controls dock) on the after-frames. Both still render as before.
4. **Cure 2, the pressed basis label (`BasisSelector.vue`, `.basis-toggle[aria-pressed=true]`).** The label ink becomes `--pill-ink: color-mix(in oklab, var(--pill-color) 75%, var(--foreground))`. Before, it was the bare glass `--viz-*` stroke hue.
   - Offline derivation (scratchpad `ink.py`, both arms, all three bases, on their 12 % tint over `--card`). The bare hue gives 3.70 / 5.17 / 4.51 in light and 3.98 / 4.51 / 4.62 in dark. At 75 % it gives 5.32 / 6.81 / 6.17 in light and 5.02 / 5.46 / 5.54 in dark.
   - The model's 3.70 matches the page's reading of 3.69 for the Epicycles label on `--card`.
   - The hue, the tint and the border are unchanged.
5. **Cure 3, the Advanced trigger (`ContourSettings.vue`, `.advanced-trigger`).** The ink changes from `color-mix(--foreground 40%, transparent)` to `var(--muted-foreground)`, and the hover changes from 60 % to `var(--foreground)`. The old ink was the literal greyed-out look on a live disclosure.
6. **Checks.** ⟨`npx vue-tsc -b`⟩ → exit 0. ⟨`npx vitest run`⟩ → `13 passed (13)` · `84 passed (84)`.
7. **Regression read over the image mode.** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-image-controls.spec.ts e2e/f-w13-image-empty.spec.ts e2e/contrast-floor.spec.ts e2e/visualization-ux.spec.ts e2e/slider-scrub-contrast.spec.ts --workers=1`⟩ → `3 failed · 17 passed (43.8s)`.
   - The 3 failures are contrast-floor `:82` light and dark plus `:128`, which is exactly the named baseline-RED set.
   - Their pair lists contain no basis, pill or Advanced pair: ⟨`grep -ciE 'basis|advanced|pill' <log>`⟩ → `0`.
   - The visual-checkpoint goldens hold no `/w` state (⟨`ls e2e/visual-checkpoint.spec.ts-snapshots/`⟩ → gallery / admin / equation only), so no golden moves.
8. **Commit (one family: 3 cure files + the gate spec + 8 frames = 12 paths).** fourier **`001bdcf`** `fix(web/visualize): X.F.W14.g — OA-43: …`. The frames were force-added under `web/.gitignore:1 *.png`, following `.t`'s precedent.
   - Pushed: ⟨`git fetch; git rev-parse --short HEAD origin/m/w1-bump-migration`⟩ → `001bdcf` · `001bdcf`.

**Gates, BEFORE → AFTER.** Settled bytes `001bdcf`, served `:3100`, headed, `--workers=1`, double-run. ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-veil.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `4 passed (11.1s)` · run 2 `4 passed (9.8s)`. Load ⟨`uptime`⟩ after run 2: 40.09 / 48.36 / 58.17.

| gate | BEFORE | AFTER (×2) |
|---|---|---|
| G-g veil: every ancestor of every live sidebar control has `opacity 1`, no filter, no grayscale or saturate < 1, no inert (1440+390 × light+dark) | `veils=0` in all 4 cells (GREEN-BEFORE-CURE: the veil was never an opacity, filter or inert) | `veils=0`, 4/4 — **GREEN** |
| sidebar surface = glass `--card` (ΔE_OK ≤ 0.02) = the other pane's surface | light 0.1036 · dark 0.1445 vs both | light `rgb(253 245 236)` = card = stage, ΔE 0.0000; dark `rgb(53 42 34)` = card = stage, ΔE 0.0000 — **GREEN** |
| controls' ink AA against it | light: 38 of 51 below AA, min 2.35; dark: min 5.47 | 51 inks each; min **5.26** light · **4.66** dark; 0 below floor — **GREEN** |

**Residuals.**
- **(R-g-1, GLASS, relay only, for `.u` to row into O-59; the relay's writable set is `.u`'s.)** The glass `Configurator` in the light arm lays its controls aside straight on the `glass-floating` veil. That veil is `oklch(0.28 0.035 70 / 0.18)` smoke, measured `rgb(215 212 207)` over the paper, and `--configurator-section-tint` is `transparent` in the light arm. As a result, glass's default inspector renders every secondary ink (`--muted-foreground`) at 4.44:1 against it, below AA.
  - fourier now selects the opaque rung, which is a consumer choice made on the canon's escape. The producer default still fails AA for any consumer on the floating tier over paper.
  - Source: glass `src/components/configurator/` (shell class `glass-floating`) and `styles/glass/veil.css` (light `--glass-veil-ink`/`--glass-veil-base`).
- (R-g-2) The owner frame's `Replace image` button, the NumberField plates and the slider tracks now sit on `--card`. Their own fills are glass's, and they read as before relative to the new surface (after-frames). The control-row idiom itself (label / value / bar on three rows) is `.h`'s OA-45 and is not touched here.
- (R-g-3) The UIA-F rows this cure touches are not self-credited. `.u` credits each one with its own falsifier.

**Adjacent edits (§0bt).** None. All three cure files are under `web/src/**`.

**Escalations.** None.

### F.W14.p

SEAT `.p`, `claude-opus-5-5`, 2026-09-23 16:05–16:30 EDT. Spec: F-W14.md ADDENDUM §0bw `.p` (line 20), OA-44, frame `owner-2026-09-23-blurry.png` (read: a thin, low-alpha epicycle chain on the grid). Consumed: COHESION §0bw, §0ca, §0cb R-5, §0bt. Tree `m/w1-bump-migration`, HEAD at open `001bdcf`.

**Acts, in order.**
1. **Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → the 21 `docs/tranches/J/audit/screenshots/before/*.png` (`.r`'s, untouched) + `?? .worktrees/`; nothing under `web/src/**` or `web/e2e/f-w14-dpr*`. Nothing inherited.
2. **Census (bytes).** ⟨`grep -rln "<canvas\|getContext\|OffscreenCanvas\|createImageBitmap\|devicePixelRatio" web/src`⟩ → three canvases, no WebGL (⟨`grep -rn webgl`⟩ → 0): `BasisCanvas.vue:530` (epicycles, image/contour views, via `useCanvasSetup`), `ConvergencePlot.vue` (`/equation`), `FrequencyGraph.vue` (the image mode's Coefficients layer). Three separate DPR setups: `useCanvasSetup.ts:21-31` (a container ResizeObserver; no DPR-change path), `ConvergencePlot.vue:203` (it reallocated the bitmap in `draw()`, every animation frame), `FrequencyGraph.vue:75` (sized only on data change: no resize or DPR path at all). ⟨`grep -rn devicePixelContentBoxSize`⟩ → 0. The thumbnails are `<img>`, not canvases. A headless DPR 2 probe of `/gallery` read `naturalWidth` 1024×577 against a 221×165 CSS `object-fit: cover` box, which needs 442×330 device px. The thumbnails are not upscaled. The image overlay is the contour's own source image, drawn at alpha 0.28. It is the photo, not a viz stroke, so it is left as it is.
3. **The epicycle strokes.** `epicycles.ts:152-155` draws the chain under `ctx.scale(fit.fitScale)`, so `lineWidths {circle 4, arm 3.5}` were multiplied by the fit. That made them sub-pixel, alpha-0.5 strokes when the fit shrinks the figure, and it grew them on hover. This is the owner's frame: `before-dpr-epicycles-2x-figure.png` reproduces it.
4. **Gate spec written first**: `web/e2e/f-w14-dpr.spec.ts`. It has 4 tests: {`/equation`, image mode on `/w`} × DPR {1, 2}, with limbs rest → viewport resize 1180×820 → DPR swap (CDP `deviceScaleFactor`) → zoom 125% (CSS viewport ÷1.25 and DPR ×1.25). At rest it takes a stroke-edge crop from the stage's left field at mid-height, with an evidence crop of the chain.
5. **Instrument findings (measured, recorded in the spec).**
   - (a) Under DPR emulation, Chromium's `devicePixelContentBoxSize` reports the physical screen, not the emulated DPR. ⟨probe `.dpcb-probe.mjs`, 300.5-px div⟩ → headed DSF 1: `{"dpr":1,"dev":601}`, headed DSF 2: `{"dpr":2,"dev":601}`, headless: `301` at both.
   - (b) A CDP DPR change signals the page nothing. ⟨probe⟩ → `[2,false,[["ro",1,601]]]`: `devicePixelRatio` is 2, `(resolution:1dppx)` no longer matches, and there is no `change` event, no `resize` event and no new observer entry. Media queries are re-evaluated only on the next media-value change: ⟨probe + `emulateMedia`⟩ → `[["mq",2]]`. The instrument therefore follows each DPR swap with an `emulateMedia({reducedMotion})` round-trip, which changes no feature the app reads. No app code is touched and no event is synthesised.
   - (c) After a DPR swap, neither capture path renders at the live DPR: Playwright's capture keeps the context's DPR and CDP's headed capture takes the physical 2×. The stroke-edge crop is therefore read at the launch DPR only, and the crop's own scale is asserted (`toBeCloseTo(dpr)`).
   - (d) **Anchor drift, read at INTENT.** `clientWidth` is the CSS box rounded to an integer. The image-mode stage is fractional (802.5 / 798.4375 px), and there the literal `round(clientWidth×dpr)` names a bitmap one device px wider than the box, which would force a resample. The gate reads the same law at layout precision: per axis, `width === round(rect.width×dpr)` or the device-snapped box `round((l+w)·dpr) − round(l·dpr)`. On an integer box this equals `round(clientWidth×dpr)`.
6. **BEFORE, served** (`:3100` = vite on the working tree, HEAD bytes swapped in for the run and restored byte-exact from scratchpad after), headed, `--workers=1`, double-run. ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-dpr.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `2 failed · 2 passed (16.7s)` · run 2 `2 failed · 2 passed (15.3s)`, identical reds:
   - DPR 1: `DPR 1→2: overflow-x-auto > canvas.block 691×120 ≠ (691×2)×(120×2)` and `zoom 125% (dpr 1.25): … 691×120 ≠ (691×1.25)×(120×1.25)`.
   - DPR 2: `DPR 2→1: … 1382×240 ≠ (691×1)×(120×1)` and `zoom 125% (dpr 2.5): … 1382×240 ≠ (691×2.5)×(120×2.5)`.
   - `/equation` passed only because ConvergencePlot reallocated every frame. BasisCanvas passed its swaps only because the stage's border snaps per DPR and so moves its content box.
   - **The stroke-edge limb at rest was GREEN-BEFORE-CURE** on the final instrument. At HEAD the grid is crisp at rest at DPR 1 and 2, so the blur the owner saw is not a stale bitmap at rest. It is (i) the fit-scaled sub-pixel chain strokes (act 3; compare the figure crops) and (ii) the missing DPR path (the reds above).
   - Earlier drafts of the instrument read `4`–`11` transitions at rest. Those readings were the 1.8-px axis counted as a 1-px stroke, plus captures resampled after a DPR swap. Both instrument errors are fixed in 5(c) and in the ink filter (runs above 2× the crop's median ink are excluded; resampling conserves ink, so a smeared grid line still counts). The early readings are not evidence.
7. **Cure** (`1278284`, one commit: the cure, its gate spec and the crops).
   - `useCanvasSetup.ts` is rewritten as **the one DPR-aware idiom**:
     - It takes `(canvasRef, onResize)` and observes **the canvas itself**, with `box: "device-pixel-content-box"` where `ResizeObserverEntry.prototype` has it.
     - The device box is used only when it lies within 1 px of `css×dpr` on each axis (snapping). Otherwise the store is `round(css×dpr)`, so the 5(a) emulation gap cannot size a bitmap that the page then resamples.
     - A `(resolution: <dpr>dppx)` media query is re-armed at each change.
     - It never writes `style.width/height`, so the CSS box belongs to layout and nothing can stretch a smaller bitmap.
     - It reallocates only a changed bitmap.
     - `ctx.setTransform(deviceW/css, 0, 0, deviceH/css, 0, 0)`, so `lineWidth` is CSS px.
     - `surface.dpr` = the applied scale.
   - `BasisCanvas.vue`:
     - The call site is updated.
     - `exportFrame` copies `s.ctx.getTransform()` instead of re-deriving the DPR.
     - The canvas's CSS box is now its stylesheet's `inset:0; 100%`, the stage's padding box. Before, an inline style gave it the container's border-box width.
   - `ConvergencePlot.vue`:
     - `draw()` paints on the surface, which ends the per-frame `canvas.width =`.
     - Its own ResizeObserver is deleted.
     - The canvas becomes `absolute inset-0 size-full` inside the already-`relative` container, so the bitmap cannot feed back into the flex height.
   - `FrequencyGraph.vue`:
     - The content-sized CSS box moves to the template's `:style` (`canvasWidth` × `HEIGHT`).
     - `draw()` paints on the surface.
     - The label ink reads `getComputedStyle(ctx.canvas)`.
     - The `onMounted` draw is gone (the composable draws on mount).
   - `epicycles.ts`: the circle and arm `lineWidth` are divided by `fit.fitScale`, so strokes stay CSS px under the fit transform. The dot radii are geometry and are left alone.
8. **Unit checks.** ⟨`npx vue-tsc -b`⟩ → exit 0. ⟨`npx vitest run`⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)`.
9. **Regression read (canvas-touching specs).** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/{contrast-floor,f-w13-image-controls,f-w13-radius,fullscreen,timeline-scrub,f-w13-image-empty,f-w14-veil,visualization-ux,visual-checkpoint,equation-interaction,workspace-flow,settings-persistence}.spec.ts --project=chromium --workers=1`⟩ → `6 failed · 40 passed (1.3m)`.
   - The 6 are exactly the named baseline-RED ids: contrast-floor `:82`×2 `:128` and visual-checkpoint `:80 :101 :122`. Nothing new is red.
   - The run rewrote `.g`'s four tracked `after-veil-*.png`. They were restored to HEAD by exact path (they are `.g`'s evidence, not this unit's).

**Gates, BEFORE → AFTER.** Settled bytes `1278284`, served `:3100`, headed, `--workers=1`, double-run. ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-dpr.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `4 passed (12.7s)` · run 2 `4 passed (11.7s)`. Load ⟨`uptime`⟩ after run 2: 44.47 / 46.92 / 49.40.

| gate | BEFORE (HEAD `001bdcf`) | AFTER (`1278284`) |
|---|---|---|
| G-p backing store, every canvas (BasisCanvas · ConvergencePlot · FrequencyGraph), at rest / resize / DPR swap / zoom 125 %, DPR 1 and 2 | **RED**: FrequencyGraph at DPR swap + zoom, at both DPRs (4 misfits ×2 runs) | **GREEN** ×2 |
| 1-px stroke edge ≤ 2 device px of transition (grid crop at rest, crop scale asserted = DPR) | GREEN-BEFORE-CURE (the grid was crisp at rest; blur = the chain strokes + the DPR path) | **GREEN** ×2 |
| before/after crops committed | — | **GREEN**: 8 PNGs `web/e2e/screenshots/f-w14/{before,after}-dpr-epicycles-{1x,2x}-{rest,figure}.png` (force-added past `web/.gitignore` `*.png`, as `.g` did) |

**Inherited paths.** None.

**Adjacent edits (§0bt).** None. Every edited path is under `web/src/**`, plus the unit's own spec and crops.

**Residuals.**
- **p1 (instrument, recorded).** The spec's literal `round(clientWidth×dpr)` cannot hold on a fractional CSS box. The gate reads it at layout precision (5(d)). A check seat should read the gate with that formula, not the integer one.
- **p2 (instrument, recorded).** Headed Chromium cannot render a crop at a changed DPR, and it cannot fire the DPR media event from CDP without a media re-evaluation (5(b), 5(c)). The DPR-swap limb is measured on the backing store, and the stroke-edge limb at the launch DPR.
- **p3 (visual, for `.h`'s eye, not a defect).** With strokes in CSS px, the fitted chain now draws at its authored 4 / 3.5 px at every fit (`after-dpr-epicycles-2x-figure.png`), where before it drew at about the fit × those widths. If the design wants a lighter chain, the change goes in `lineWidths` (`BasisCanvas.vue:176,360`), not in the fit.

**Escalations.** None.

### F.W14.h

SEAT `.h`, `claude-opus-5-5`, 2026-09-23. Spec: F-W14.md ADDENDUM §0bw `.h` (line 21), OA-45. Frame `owner-2026-09-23-hierarchy.png` (read: "Harmonics N" alone on a line; the `200` field alone on a right-aligned line; a thick bar with no thumb). Consumed: COHESION §0bw, §0cb R-5, §0cc, §0bt. Tree `m/w1-bump-migration`, HEAD at open `1278284`.

**Scope note.** The computed task scoped this seat to §0bw `.h` (line 21) and its three gates. The §0cc addendum (OA-50, the admin panels) also names `.h`. It was **not executed in this seat** and remains owed (see Residual h4).

**Design notes (the idiom).**
- **One control row.** `ui/SliderControl.vue` (`[data-control-row]`) is now the only labelled slider in the app. It is two lines and never three:
  - Line 1 is the glass `Label` (`--type-small`, weight 500, `--foreground`, bound to the field by `for`/`id`). It may carry a mono `token` (`N`, `--type-micro`) or a muted `subtitle` (`--type-caption`, ellipsised, never wrapping). The glass `NumberField` (`sm`) sits at the line end, with an optional `unit` (`ms`).
  - Line 2 is the glass `Slider` directly beneath, with a **visible thumb** and a **track fill**.
  - The rhythm is `--space-atom` inside the row and `--space-body` between rows.
- **A missing primitive, composed meanwhile.** Glass 10.0.1 ships no slider with both a thumb and a fill:
  - The `scrubber` thumb is `width:0; opacity:0` by contract. That is the frame's bar.
  - The `spectrum` thumb is visible, but its `.slider-range` paints nothing.
  - `LabeledSlider` carries no inline value.
  - The row therefore composes through the producer's public surface: `variant="spectrum" size="md"`, with the fill painted on its documented `--glass-slider-track-background` token. The gradient stop is at the thumb centre under reka's `contain` alignment, computed from glass's own `--slider-thumb-size`.
  - Routed to glass BL by id: **BL-FW14H-1** (a slider with a visible thumb and a fill, at a slimmer rung than `md`'s 24 px track) and **BL-FW14H-2** (a labelled slider with an inline value field). These ride the O-59 relay line.
- **One card hierarchy.** Measured, the producer's own section header (glass `ConfiguratorLayer`) is `--type-heading` (25.888 px) in serif at weight 600, with the sub as a `text-micro` mono muted token. Every fourier card title now takes that rung: `CollapsibleSection`, `EquationPanel`, `MorphPhaseConfig`, `HarmonicLevelGrid`. The subtitles and descriptions take `--type-micro` or `--type-caption`, and card insets `--space-family`. Page titles are on the scale too:
  - morph `--type-display-1`;
  - paper `text-display-2` (fluid; the sm/md breakpoint ladder retires);
  - shape-extractor `--type-heading` / `--type-subheading`.
- **Finding (measured).** Under glass's theme bridge (`theme/bridges.css`: `--text-sm: initial; --text-xs: initial`), `text-sm` and `text-xs` size nothing. ⟨probe on the served `/equation`⟩ → `text-sm` 18.608 px = `text-xs` 18.608 px = body. So every `text-sm` label and card title in the app was inheriting the body size, which flattens the hierarchy. The rows this unit touched now name glass rungs (`text-small`, `text-caption`, `text-micro`, `text-heading`). **29 files** use `text-sm` (⟨`grep -rlw text-sm web/src | wc -l`⟩ → 29). Those outside this unit's rows are Residual h3.

**Acts, in order.**
1. **Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → the 21 `docs/tranches/J/audit/screenshots/before/*.png` (`.r`'s, untouched) + `?? .worktrees/`. Nothing was under `web/src/**` or `web/e2e/f-w14-control-row*`, so nothing was inherited.
2. **Census (bytes, pasted).** ⟨`grep -rnE "<(Slider|SliderControl|LabeledSlider)([ >]|$)" web/src`⟩ → 18 hits:
   - `SliderControl` ×8: ContourSettings 5 · FunctionInput 2 · EquationPanel 1.
   - Raw glass `Slider` ×7: BasisSelector 2 · HarmonicLevelGrid 2 · MorphPhaseConfig 1 · EditorControlsDock 1 · GlassTimeline 1.
   - The remaining 3 hits are docblock mentions. The baseline's "BasisSelector 3 / GlassTimeline 2" counted those.
   - Pages and modes that mount a slider: `/equation`, `/morph`, and `/w` image mode (basis + contour + Advanced), `/w` contour edit (magnet popover), and the `/w` Equation overlay. `/`, `/paper`, `/gallery` and `/demo/shape-extractor` mount none; they are framed and read for type only.
   - **GlassTimeline's playhead is outside the idiom**, named in the gate: it is a transport scrubber whose caret carries the value, not a labelled parameter row.
3. **Gate written first:** `web/e2e/f-w14-control-row.spec.ts`. It walks every page and mode above at 1440 and 390 and reads every `role="slider"`:
   - on `[data-control-row]`;
   - label and value field on one line (vertical overlap);
   - track beneath that line;
   - thumb ≥ 4 px wide with opacity > 0;
   - fill (a painted range or a gradient track);
   - row height = the median ± 4 px.

   It also reads h1–h3, card titles and subtitles, and row labels against glass's `--type-*` rungs, and the rows' gaps against `--space-*`. Both scales are resolved in the page from probe elements, not copied. Frames are `e2e/screenshots/f-w14/{before,after}-page-<page>-<w>.png`.
4. **BEFORE, served** (`:3100`, headed, `--workers=1`, HEAD `1278284`). ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-control-row.spec.ts --project=chromium --workers=1 --headed`⟩ → **2 failed**, `rows off the idiom`:

   | vw | rows | on idiom | visible thumb | label+value on one line with the track beneath | heights (px) |
   |---|---|---|---|---|---|
   | 1440 | 15 | 0 | 0 | 7 (equation 2 · contour 5) | 36 (morph ×5) · 56 · 91.9 (magnet) · 104.4 (basis ×2 = the frame) |
   | 390 | 14 | 0 | 0 | 7 | 36 · 56 · 101.4 |

   - The Morph card at the floor read `fill=0`, which is MPC-8's empty capsule.
   - Type BEFORE, from a probe on the same bytes before those files were edited:
     - morph `.demo-title` 32 px (**off** the scale);
     - paper h1 52 px (**off**);
     - morph card titles `text-lg` 18 px (between rungs);
     - shape-extractor h1 = h2 = 18.6 px (flat);
     - every `text-sm` title = body.
   - Instrument corrections made before the recorded run are disclosed:
     - Waiting on a scrubber thumb fails by contract, so the gate waits on the slider root.
     - The canvas docks rest collapsed and are expanded first.
     - The `/w` Equation overlay did not open under the dock toggle in this instrument, so its `Terms` row (the same `SliderControl`) is read by construction.
     - The type reading was widened to h1–h3 after the BEFORE run. Its BEFORE figures are the probe readings above.
5. **Cure** (fourier `ef9dfc6`, pushed; one commit holding the cure, its gate spec and 30 frames).
   - `ui/SliderControl.vue` is rewritten as the idiom (design notes above). `token` and `unit` props were added. The unused default slot and the stale `--slider-range-bg` retint block retired.
   - `BasisSelector.vue`, `HarmonicLevelGrid.vue`, `MorphPhaseConfig.vue` (duration + `unit="ms"` + marks) and `EditorControlsDock.vue` (magnet; the read-only `Metric` retires, and the popover is 12rem on the spacing scale) moved onto `SliderControl`. With them went their local `NumberField`+`Slider` compositions, array-model adapters, dead retint blocks and `useId` label pairs; the idiom owns `for`/`id`.
   - Card and page type:
     - `CollapsibleSection.vue` title → `text-heading`, and its sub → `text-micro font-mono` (no dash);
     - `EquationPanel.vue` title → `text-heading`, rhythm `gap-body`;
     - `FunctionInput.vue` field labels → `text-small` on `--foreground`, rhythm `space-y-body` / `space-y-family`;
     - `FourierMorphDemo.vue` title → `--type-display-1` and lede → `--type-body`;
     - `FourierShapeExtractor.vue` h1/h2 → heading/subheading;
     - `PaperView.vue` h1 → `text-display-2 leading-display`.
6. **Unit checks.** ⟨`npx vue-tsc -b`⟩ → exit 0, no output. ⟨`npx vitest run`⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)`.
7. **Regression read.** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/{slider-scrub-contrast,equation-interaction,visualization-ux,settings-persistence,workspace-flow,coarse-pointer,contrast-floor,f-w13-image-controls,f-w13-radius,visual-checkpoint,f-w14-veil,f-w14-dpr,timeline-scrub}.spec.ts --project=chromium --workers=1`⟩ → `10 failed · 38 passed (2.8m)`. That is 6 baseline-RED (contrast-floor `:82`×2 `:128`, visual-checkpoint `:80 :101 :122`) and 4 others:
   - equation-interaction `:77` and visualization-ux `:219` waited on `.slider-subtitle`, the class this unit renamed. They were repointed to `[data-row-sub]` as **adjacent edits**. The re-run of both passed; visual-checkpoint `:163`'s wait site was repointed the same way.
   - f-w14-dpr `:206` was red headless only. Its gate is headed. ⟨`npx playwright test e2e/f-w14-dpr.spec.ts --headed --workers=1`⟩ → `4 passed`.
   - **visual-checkpoint `:163` `checkpoint-disclosure-body.png`** is RED at `26497 px (0.11)`. The diff was read: it is confined to the `/equation` left panel's two cards, meaning the titles on the heading rung, the labels on `text-small`, and the two slider rows on the idiom. This is the owner-ordered render, not a regression. Re-baselining a golden is outside this unit's writable set, so it is **ESC-F14h-1**.
   - The run rewrote the tracked `.p`/`.g` PNGs (`after-dpr-*`, `after-veil-*`). They were restored to HEAD by exact path.

**Gates, BEFORE → AFTER.** Settled bytes `ef9dfc6`, served `:3100`, headed, `--workers=1`, double-run. ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-control-row.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `2 passed (17.1s)` · run 2 `2 passed (17.7s)`. ⟨`python3 -c …test-results/f-w14-control-row-after-{1440,390}.json`⟩ → `1440 15 heights [68] allIdiom True` · `390 14 heights [64] allIdiom True`.

| gate | BEFORE (`1278284`) | AFTER (`ef9dfc6`) |
|---|---|---|
| G-h census: every slider row on the one idiom (label + value on one line, slider beneath, visible thumb + fill) | **RED**: 0/15 on the idiom @1440 and 0/14 @390; 0 visible thumbs | **GREEN** ×2: 15/15 and 14/14 on every limb |
| no control row taller than the idiom's measured height ± 4 px | **RED**: 36 · 56 · 91.9 · 104.4 px (four shapes) | **GREEN** ×2: every row 68 px @1440 and 64 px @390 (spread 0) |
| per page before/after frames at 1440 and 390; card title/subtitle on the type scale; rhythm on the spacing scale | **RED**: morph title 32 px and paper h1 52 px off the scale; card titles 18 px between rungs; `text-sm` sizes nothing | **GREEN** ×2: `offType`/`offSpace` empty on all 7 pages and modes (h1–h3, card titles and subs, row labels and subs; row gaps). Frames: 15 before + 15 after committed (`{before,after}-page-{home,paper,gallery,shape-extractor,equation,morph,image}-{1440,390}` + `image-edit-magnet-1440`) |

**Inherited paths.** None.

**Adjacent edits (§0bt).** Each is the selector of a test oracle whose hook this unit renamed (`.slider-subtitle` → `[data-row-sub]`). No assertion was removed.
- `web/e2e/equation-interaction.spec.ts:68,213`: the mount-condition waits.
- `web/e2e/visualization-ux.spec.ts:203` (comment) and `:240` (wait).
- `web/e2e/visual-checkpoint.spec.ts:189,211`: the disclosure-body locator and wait.

**Residuals.**
- **h1 (glass, routed).** BL-FW14H-1: a slider with thumb and fill (the composition uses `spectrum md`, whose 24 px track is heavier than a slim rung would be). BL-FW14H-2: a labelled slider with an inline value. Both ride O-59 by id.
- **h2 (instrument).** `slider-scrub-contrast.spec.ts`'s docblock still describes the scrubber liquid-fill recipe (88 % tint). The painted fill is now the tint at 100 % on `--muted-medium`, so its asserted floors are conservative and still pass. Its prose is stale for `.u`/`.r` to re-word, outside this unit's set.
- **h3 (type, sweep).** `text-sm`/`text-xs` size nothing under glass's bridge in every file that uses them (29 files by grep). This unit moved the rows and cards it owns. The remaining sites (admin, gallery, paper body, ContourSettings' Select items) belong to `.u`'s UIA-F sweep and to OA-50.
- **h4 (owed).** OA-50 (§0cc, the admin panels' one card/row idiom, which also routes to `.h`) was not in this seat's computed scope and is not executed. It needs its own `.h` sitting (census of every admin page, headed 1440/390 in both themes).

**Escalations.**
- **ESC-F14h-1**: the golden `web/e2e/visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-*.png` needs re-baselining to the OA-45 render. The diff was read: 26 497 px (0.11), confined to the `/equation` panel's card titles, labels and two slider rows. That path is outside `.h`'s writable set. This is the same class as F.W13's ESC-F13a-1 (the §0bm grant). Until it is granted, `:163` joins the named RED set.

### F.W14.u

SEAT `claude-opus-5-5`, 2026-09-23. Spec: `F-W14.md` Units `.u` (:13) + ADDENDUM §0ca (:24-26), read whole; COHESION §0ca–§0cf read. Register: `audit/UI-AUDIT-fourier.md` (256 rows; Appendix B = 74 rows with a glass part).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only the 21 `docs/tranches/J/audit/screenshots/before/*.png` (`.r`'s) + `?? .worktrees/`; nothing under `web/src` or `web/e2e`. value.js: `LEDGER.md` dirty (a sibling seat's; not touched). **Nothing inherited.**

**Instrument.** Served `http://localhost:3100` (Vite dev of this tree, HMR; API live, read-only GETs + route stubs). One falsifier file `web/e2e/f-w14-uia.spec.ts`, one test per cured row, named by id. Every cure is proven by RED-before (the cure's own diff reverted, the test run, the diff re-applied) then GREEN ×2. Rows whose register entry carries a frame are framed; frameless rows are framed at `web/e2e/screenshots/f-w14/uia/` before their cure.

**Acts (in order).**
1. **UIA-F-1** — `GalleryFeaturedCarousel.vue`: header row (with `CarouselPager`) moved inside `<Carousel>`. ⟨falsifier with a featured fixture, cure reverted⟩ → `1 failed` (strip absent) → ⟨cure applied ×2⟩ → `1 passed` ×2. fourier **`a72dd1e`**.
2. **UIA-F-2 + UIA-F-3** — `useWorkspaceLoader.ts`: `loadFromRoute` dispatches on `route.name` (`visualization` → `loadVisualization`, `workspace` → `loadWorkspace`), watcher keyed on name+both slugs, acts only on its own two routes. ⟨reverted⟩ → F-2 `waitForResponse` timeout (GET never sent) · F-3 URL `…/w/cosmic-edging-nutmeg-horse` (bounced) → ⟨applied ×2⟩ → `2 passed` ×2, 0 pageerrors (the orphaned View Transition was the bounce's; no `.catch` shim added). fourier **`8e17213`**.
3. **UIA-F-50 + UIA-F-4 + UIA-F-49** — new `components/shared/NotFoundCard.vue` (glass Card family + Button, `h1`); router last route `/:pathMatch(.*)*` → it, `meta.noindex` → `<meta name=robots content=noindex>` (removed on leave); `VisualizationView` error branch → the same card, route-aware copy naming the slug, Tooltip dropped, action labelled "Upload a new image". ⟨reverted⟩ → `3 failed` → ⟨applied ×2⟩ → `3 passed` ×2. vue-tsc exit 0. fourier **`1a632a2`**.
4. **UIA-F-7** — `AnimationControls.vue`: the collapsed readout is glass `Progress` (`size="sm"`, `modelValue=anim.t`, `max=1`, aria-label) inside the same render boundary; the scoped `.mini-fill` rule deleted. Served probe after: `role=progressbar`, `aria-valuenow 0.33`, indicator 48×8 px painted `rgb(28,25,23)`. ⟨reverted⟩ → `1 failed` (no progressbar) → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`3098e6d`**.
5. **UIA-F-17** — `BasisCanvas.vue`: `FrameLayers {epicycles, trail}` threaded into `drawEpicycleFrame`/`drawMultiBasesFrame` (default all-on for the live draw); `exportFrame` reads `withEpicycles`/`withTrail`. Falsifier exports paused frames through the real dialog and counts painted pixels. ⟨reverted⟩ → `{all 41125, noChain 41122, noTrace 41125, again 41125}` → removal 3 px vs floor 411 → `1 failed` → ⟨applied⟩ → `{all 41189, noChain 33834, noTrace 38341, again 41183}` → `1 passed` ×2. fourier **`1623166`**.
6. **UIA-F-16** — `ContourEditorCanvas.vue`: `onPointerUp` wraps the raw `usePointDrag` handler and calls `emitState()`. ⟨reverted⟩ → Undo `disabled` after a drag (`Expected false · Received true`) → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`7dd8a10`**.
7. **UIA-F-15** — `ContourEditorCanvas.vue`: shortcuts on `@keydown` of the focusable `.editor-shell` (window listener + `onMounted/onUnmounted` deleted; a picked point focuses the shell, since the drag's `preventDefault` suppresses the focus move); `VisualizationView.vue`: the opacity-hidden editor wrapper is `inert` outside edit mode. ⟨reverted⟩ → after leaving edit mode and pressing Backspace in a sidebar spinbutton, point count `Expected 1023 · Received 1024` (the hidden editor's state moved under a sidebar keystroke) → ⟨applied ×2⟩ → `1 passed` ×2 (Delete still deletes a picked point in edit mode; the field keeps its Backspace; wrapper `inert`). fourier **`7958cfa`**.
8. **UIA-F-18** — `VisualizationView.vue` `handlePublish`: `if (publishing.value …) return`; a null save toasts only a real `store.error`; `workspace.ts` `saveVisualization` clears `error` at its start (so an abort is not a failure). ⟨reverted⟩ → dblclick sends `2` POSTs (`Expected 1 · Received 2`) → ⟨applied ×2⟩ → `1 passed` ×2 (one POST, "Published!", 0 "Could not save"). fourier **`34d8297`**.
9. **UIA-F-19** — the overlay's redundant `@drop` removed (`VisualizationView.vue`); `useImageUpload.handleDrop` leaves an already-handled drop (`defaultPrevented`) to its inner owner; `workspace.uploadImage` re-entrancy guard. ⟨reverted⟩ → synthetic drop over a loaded image → `2` POST `/api/images` → ⟨applied ×2⟩ → `1` ×2. fourier **`f514500`**.
10. **UIA-F-20** — `PaperView.vue`: the saved section id is read synchronously in setup (`restoreSectionId`) before the `immediate` persisting watcher removes the key; `onMounted` restores from it. ⟨reverted⟩ → after reload `.paper-scroll` scrollTop `0` → ⟨applied ×2⟩ → `> 1000`, saved section in view, `1 passed` ×2. fourier **`d35ab92`**.
11. **UIA-F-24** — `MobileFloatingToc.vue`: `.floating-toc-row` = navigate button (`selectSection`) + a separate icon Button (`aria-label "Subsections of …"`, `aria-expanded`) that toggles. ⟨reverted⟩ at 390/touch → tapping the last chapter left the dropdown open (`Expected 0 · Received 13` rows) → ⟨applied ×2⟩ → dropdown closes, scroller advances > 2000 px, disclosure flips `aria-expanded`; `1 passed` ×2. fourier **`f2b99a8`**.
12. **UIA-F-31** — `equation/lib/harmonics.ts` gains `revealWeight(j, cursor, blend, n)` (1 everywhere once `cursor ≥ n−1`); `ConvergencePlot.vue` uses it. Falsifier = vitest `src/components/equation/lib/harmonics.test.ts`. ⟨`node -e` the old expression at j=c=N−1, BLEND 10⟩ → `0.0909` (RED) → ⟨`npx vitest run …harmonics.test.ts` ×2⟩ → `Tests 2 passed (2)` ×2. fourier **`427d69e`**.
13. **UIA-F-36** — `AdminUserList.vue`: `BatchActionBar` mounted after the list `Card`. ⟨reverted⟩ → ticking a row moved it `68` px → ⟨applied ×2⟩ → 0 px, bar in viewport below the rows; `1 passed` ×2. fourier **`3da9ce0`**.
14. **UIA-F-37** — `AdminUserList.vue`: toolbar `flex-wrap`, search `basis-full sm:basis-0 sm:flex-1`, SelectTrigger literals dropped. ⟨reverted⟩ at 390 → search width `42` px → ⟨applied ×2⟩ → > 300 px, no control past the viewport, no literal classes; `1 passed` ×2. fourier **`4655d6e`**.
15. **O-63 relay** — `docs/tranches/X/relay/X-ALL-BK-UI-AUDIT-ADDENDUM-2026-09-23-FW14U.md` (dated addendum beside O-59, E-3) + INBOX row `O-63`: 48 pure-GLASS rows ROUTED under O-59; 26 split rows' glass halves under O-59; **UIA-F-51 relay-only, blocking (I-44), 0 local override** (⟨`grep -rn "Toaster\|toast-viewport\|VIEWPORT" web/src --include=*.css`⟩ at the unit's end, below); plus `.t`'s R-t-1 and `.h`'s BL-FW14H-1/2 (not in O-59 before). Not mirrored into glass-ui (READ-ONLY to this seat): owed to the next BK mirror act. value.js **`a64e7316`**.
16. **UIA-F-42** — already cured at cause by `.t` (`b5a650f`, DataTable); falsifier added: at 390 every audit cell width > 0, one badge per row, 0 badges overflowing. ⟨×2⟩ → `1 passed` ×2. fourier **`96b66ad`** (test only).
17. **UIA-F-48** (consumer half) — `GalleryDraftsSection.vue`: `px-4` wrapper replaces `mx-4` on the `Collapsible`. ⟨reverted⟩ → card right `1296` > column−15 `1265` → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`191c86d`**.
18. **UIA-F-41** (consumer half) — `GalleryView.vue`: `SegmentedTabs` inside its own `min-w-0 overflow-x-auto` track. (First attempt `overflow-x-clip` on the column was measured and rejected: with `overflow-y:auto` the clip computes to `hidden`, which focus still scrolls.) ⟨reverted⟩ at 390 → column `scrollLeft > 0` after focusing the last tab → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`b803a32`**.
19. **UIA-F-45** — `GalleryCardModal.vue`: `@open-auto-focus="focusTitle"` (prevent + focus the `data-initial-focus` title), replacing the bare `.prevent`. ⟨reverted⟩ → focus not inside the dialog (`Expected true · Received false`) → ⟨applied ×2⟩ → focus on the title, 6 Tabs stay in the dialog; `1 passed` ×2. fourier **`73d823b`**.
20. **UIA-F-40** — `GalleryView.vue` `onMounted`: `Promise.all([resetAndFetch, refreshDrafts, activateAdmin→replace])`, grid first. ⟨reverted, stats delayed 3 s⟩ → grid card not visible within 2.5 s → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`5a92498`**.
21. **UIA-F-47** — `gallery.publishDraft` writes the created slug into the draft's `savedSnapshots` (`saveDraft`); `GalleryView` lists only drafts with no snapshot and refreshes after publish (the dead `publishedHashes` removed); `workspace._saveDraftNow` carries the loaded draft's snapshots (`draftSnapshots`, reset on a new upload). ⟨reverted⟩ → the published row stayed (`Expected 0 · Received 1`) → ⟨applied ×2⟩ → the row is gone, and still gone after reload; `1 passed` ×2. fourier **`26c216a`**.
22. **UIA-F-33 + UIA-F-34 + UIA-F-32** — `EquationResult.vue`: a header row (`#leading` slot · `#actions` slot + Copy, `gap 0.5rem`, `margin-inline-start:auto`); `.copy-pos` absolute layer and the `2rem` dodge padding deleted. `EquationView.vue`: the Σ|a+b toggle → `#leading`, the Info Popover → `#actions`; `.eq-mode-anchor`/`.info-anchor` (the `right:3.25rem` literal) deleted; `.eq-card` `height:10rem; overflow:hidden` → `min-height:10rem`. Served probe after: 1440 Info x1334–1374 · Copy x1382–1422 (8 px apart), equation top 129 > controls bottom 127; 390 Info 288–328 · Copy 336–376, equation top 170 = controls bottom. ⟨reverted⟩ → F-33/34 `control 0 over the equation` at 1440 and 390; F-32 popover clipped (visible fraction < 0.99) → ⟨applied ×2⟩ → `3 passed` ×2. fourier **`1c7a9c4`**.
23. **UIA-F-12** — `CanvasControlsDock.vue:70` and `EditorControlsDock.vue:160, :191`: `Popover trigger="hover"` → `trigger="click"` (View options; the editor's magnet slider and view toggles). ⟨reverted⟩ → three Tabs after Enter reach `["Edit contour","Pause animation","Timeline"]` → ⟨applied ×2⟩ → Tab reaches Contour trace + Image overlay, and Enter toggles `aria-pressed`; `1 passed` ×2. fourier **`57d880d`**.
24. **UIA-F-5** — `VisualizationView.vue`: the dock anchor is right-anchored at every width; the `dock-centered` re-seat (and its transition) is deleted. ⟨reverted⟩ at 390 → `Equation is hittable: false` → ⟨applied ×2⟩ → every expanded-dock control's centre hits itself, in view; `1 passed` ×2. fourier **`f6894c3`**.
25. **UIA-F-28 + UIA-F-29 + UIA-F-30** — `UserSlugBar.vue` template rewritten onto the dock's transient families: logged out → `Popover` + `DockTrigger for="popover"` ("Log in") holding a `<form>` (label, Input, the error `<p role=alert>` on its own wrapping line, Generate + Submit); logged in → `DropdownMenu` + `DockTrigger for="dropdown"` (`Account: <slug>`; abbrev `sm:` only, since the full slug is in the menu label, reachable by touch) holding the slug, "Copy your slug" and "Log out". Every accessible name the earlier rows fixed is kept; `e2e/gallery.spec.ts` login oracle ×2 → `1 passed` ×2. ⟨reverted⟩ at 390 → the slug field was not hittable (F-28); the dark-mode toggle was off the plate (F-30, `{inView,hit}` ≠ `{true,true}`) → ⟨applied ×2⟩ → `2 passed` ×2 (the error `scrollWidth ≤ clientWidth`, right edge ≤ 390, dock width unchanged by the open form). fourier **`fb8b5df`**.
26. **UIA-F-22** — `PaperView.vue`: `isDesktop = useMediaQuery("(min-width: 1024px)")`; `PaperSidebar v-if="isDesktop"`, `MobileFloatingToc v-if="!isDesktop && …"`. ⟨reverted⟩ at 390/touch → `31` duplicate `paper-search*` ids → ⟨applied ×2⟩ → 0 duplicates, one listbox, a tapped result scrolls; `1 passed` ×2; `paper-search.spec.ts` `3 passed`. fourier **`9a7cf7d`**.
27. **UIA-F-23** — `PaperSearch.vue` gains a `boundary` prop (defaults to its root); `MobileFloatingToc.vue` passes its search bar. BEFORE (measured at the F-22 tree, pre-cure) → `Close search` still present after one tap → ⟨applied ×2⟩ → closed and stays closed; `paper-search.spec.ts` `3 passed`. fourier **`e87d203`**.

**Disposition table (all 256 UIA-F rows).** The counts were computed by script from the register's owner column, Appendix B and this receipt's commits (⟨`python3` over `uia-own.txt` · `glass-only.txt` · `glass-split.txt`⟩ → total `256`). Every class is listed by id.

| class | n | rows · evidence |
|---|---|---|
| **CURED** at cause, falsifier RED-before → GREEN ×2 | **31** | F-1 `a72dd1e` · F-2, F-3 `8e17213` · F-4, F-49, F-50 `1a632a2` · F-5 `f6894c3` · F-7 `3098e6d` → re-cured `ec3fb0b` (act 28) · F-12 `57d880d` · F-15 `7958cfa` · F-16 `7dd8a10` · F-17 `1623166` · F-18 `34d8297` · F-19 `f514500` · F-20 `d35ab92` · F-22 `9a7cf7d` · F-23 `e87d203` · F-24 `f2b99a8` · F-25 `ef3c124` · F-28, F-29, F-30 `fb8b5df` · F-31 `427d69e` · F-32, F-33, F-34 `1c7a9c4` · F-36 `3da9ce0` · F-37 `4655d6e` · F-40 `5a92498` · F-45 `73d823b` · F-47 `26c216a` |
| **SPLIT**: consumer half CURED, glass half ROUTED (O-59) | **2** | F-41 `b803a32` · F-48 `191c86d` |
| **CURED-BY-.t** (cited, not re-cured) | **2** | F-42 `b5a650f` + this unit's falsifier `96b66ad` (GREEN ×2) · F-108 `b5a650f` (the audit ledger is DataTable; falsifier = `.t`'s G-t spec) |
| **CURED-BY-.h** (cited, not re-cured) | **2** | F-75 (one slider-row idiom), F-116 (/morph scrubbers get a thumb): `ef9dfc6`, falsifier = `.h`'s G-h census spec |
| **ROUTED-GLASS** (pure GLASS, O-59; now also O-63) | **48** | F-6 8 10 11 13 27 38 43 44 **51** 56 58 72 78 84 90 91 100 122 123 125 126 127 129 131 134 135 138 139 140 141 142 143 145 215 216 217 218 220 221 222 223 224 225 226 227 228 230 |
| **SPLIT**: glass half ROUTED (O-59), consumer half **OPEN** | **24** | F-57 60 77 80 85 88 94 105 106 107 124 128 130 132 133 136 137 144 146 147 148 149 219 229 |
| **ROUTED-SERVER** (the cause is outside `web/src`, this unit's writable set) | **3** | F-35 (`src/fourier_analysis/symbolic/simplification.py`) · F-83 (`latex_rendering.py`) · F-112 (SERVER) |
| **OPEN**, server-dependent | **1** | F-39: the q/tier/basis filters need `api/routers/visualizations.py`, which is outside `web/src`. The register's alternative, deleting the controls, is a product decision, not a cure. |
| **OPEN**, consumer, not reached this sitting | **143** | BROKEN F-9 14 21 26 46, and HIGH, MEDIUM and LOW by id in the block below |

Check: 31+2+2+2+48+24+3+1+143 = **256** ✓.

**The 143 OPEN consumer rows, by severity.** (The split rows' consumer halves and F-39 are listed in the table above.)
- BROKEN (5): F-9 F-14 F-21 F-26 F-46
- HIGH (47): F-52 F-53 F-54 F-55 F-59 F-61 F-62 F-63 F-64 F-65 F-66 F-67 F-68 F-69 F-70 F-71 F-73 F-74 F-76 F-79 F-81 F-82 F-86 F-87 F-89 F-92 F-93 F-95 F-96 F-97 F-98 F-99 F-101 F-102 F-103 F-104 F-109 F-110 F-111 F-113 F-114 F-115 F-117 F-118 F-119 F-120 F-121
- MEDIUM (65): F-150 F-151 F-152 F-153 F-154 F-155 F-156 F-157 F-158 F-159 F-160 F-161 F-162 F-163 F-164 F-165 F-166 F-167 F-168 F-169 F-170 F-171 F-172 F-173 F-174 F-175 F-176 F-177 F-178 F-179 F-180 F-181 F-182 F-183 F-184 F-185 F-186 F-187 F-188 F-189 F-190 F-191 F-192 F-193 F-194 F-195 F-196 F-197 F-198 F-199 F-200 F-201 F-202 F-203 F-204 F-205 F-206 F-207 F-208 F-209 F-210 F-211 F-212 F-213 F-214
- LOW (26): F-231 F-232 F-233 F-234 F-235 F-236 F-237 F-238 F-239 F-240 F-241 F-242 F-243 F-244 F-245 F-246 F-247 F-248 F-249 F-250 F-251 F-252 F-253 F-254 F-255 F-256

**Gate readings (BEFORE → AFTER).**

| gate | BEFORE | AFTER |
|---|---|---|
| G-u: every UIA-F row CURED with a falsifier GREEN ×2, or ROUTED-GLASS by id | **RED**: 256 open | **RED (PARTIAL)**: 88 of 256 dispositioned (37 consumer rows cured: 31 here, 2 split consumer halves here, 2 by `.t`, 2 by `.h`; 48 ROUTED-GLASS; 3 ROUTED-SERVER). 168 are still owed: 143 OPEN, 24 split consumer halves, F-39. |
| every frameless row framed on the served page BEFORE its cure | n/a | **GREEN (vacuous for the rows cured)**: every row cured here carries a register frame in its Frame column (checked row by row against `UI-AUDIT-fourier.md` before each cure), so no new BEFORE frame was owed. The served-page BEFORE reading of each cure is its RED-before falsifier run, recorded in its act. |
| UIA-F-51 relay-only, 0 local override | relay O-59 · I-44 | **GREEN**: ⟨`grep -rn "Toaster\|toast-viewport\|data-slot=\"toast" web/src`⟩ → only `App.vue:5` (import) and `App.vue:146` `<Toaster />` (no prop, class or style). The row is carried in O-63 as blocking. |
| falsifier file `web/e2e/f-w14-uia.spec.ts` | none | ⟨`npx playwright test e2e/f-w14-uia.spec.ts --project=chromium --workers=1` ×2⟩ → `32 passed (1.1m)` · `32 passed (1.2m)` |
| typecheck | exit 0 | ⟨`npx vue-tsc -b`⟩ → exit 0 |
| unit tests | 84/84 (13 files) | ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` (+ `harmonics.test.ts` 2) |

A measurement note: an earlier ×2 run at `--workers=2` read `31/32` and then `29/32`. The two extra reds were `firstSavedViz` → `res.ok() false`, the API's read rate limit (`ratelimit-limit: 180`, `remaining 27` read by `curl -D -`). The third was F-17's pointer pause, discarded mid-morph (UIA-F-8, a glass row). The fix was in the instrument, not the product: the lookup is cached per worker and F-17 pauses by keyboard. That is `c815101`. No assertion was weakened.

**Inherited paths.** None.

**Adjacent edits (§0bt).** `web/e2e/f-w14-control-row.spec.ts:245`: `.h`'s G-h opened the editor's magnet popover with `.hover()`. UIA-F-12 moved that popover to `trigger="click"`, so the oracle's interaction follows it (`.click()`). No assertion is changed. fourier `0f94a2f`. The one new unit test, `web/src/components/equation/lib/harmonics.test.ts`, is inside `web/src/**`.

**Residuals.**
- **u1 (G-u, the bulk).** 168 rows are still owed: 143 OPEN consumer rows (listed above), 24 split consumer halves and F-39. This seat cured 33 rows at cause. Each has a falsifier proven RED against its own reverted diff and GREEN ×2. The owed rows need further `.u` sittings, working the register BROKEN (F-9 14 21 26 46) → HIGH → MEDIUM → LOW.
  - **F-9** (the 390 More-options menu) is to be resolved by F-81, which moves speed and easing to the animation pane. That is a HIGH design row.
  - **F-14** (fullscreen as a second, deaf editor) needs the single live stage moved into the takeover.
  - **F-21** (raw LaTeX in search labels) needs KaTeX-rendered labels whose fuzzy highlight marks only text segments.
  - **F-26** (theorem result → wrong chapter): the register pins no root cause. Tracing `thm.label` → `labelMap.sectionId` is the next probe.
  - **F-46** (Like is not a toggle): there is no like endpoint in `api/routers`, so the server half routes with it.
- **u2 (server rows).** F-35, F-83 and F-112, plus F-39's API half, need `src/fourier_analysis/**` or `api/**` writes. Those are outside `.u`'s writable set (`web/src/**`). They are routed to the next X·F wave's server unit, not escalated as a bound breach.
- **u3 (instrument).** The live API's read rate limit (180 per window) is a shared instrument constraint for any served-page spec that reads real rows. Specs that read real rows should cache per worker, or run at `--workers=1`.
- **u4 (h2, handed to .u by .h).** `slider-scrub-contrast.spec.ts`'s stale docblock was not re-worded here. It is prose-only and is left for `.r`.
- **u5 (relay mirror).** O-63 is written value.js-side only. glass-ui is read-only to this seat, so the BK/coordination mirror is owed to the next mirror act.

**Escalations.** None. No specified cure was impossible at the bytes. The unfinished rows are a PARTIAL for scope, not a blocked cure.

**Late acts.**
28. **UIA-F-7 re-cure (self-caught regression).** A non-regression run of the sibling gates, ⟨`npx playwright test e2e/f-w14-admin-table.spec.ts e2e/f-w14-control-row.spec.ts e2e/gallery.spec.ts e2e/equation-interaction.spec.ts --workers=1`⟩, read `1 failed · 21 passed (5.9m)`. The failure was `.h`'s G-h @1440 `settle()`, which timed out at 300 s on the image page. Bounded probe ⟨`document.getAnimations()` running, finite, on `/v/<slug>`⟩ → `["CSSTransition:transform@progress-value-fill"] 1`. Glass `Progress`'s fill carries a `transform` transition, and the 60 fps clock restarts it every frame. **Cure:** the readout keeps its own div fill, styled by `.mini-progress :deep(.mini-fill)` from the scoped root (the root carries the scope; the render-function child does not), with `role=progressbar` (0..1, `aria-valuetext` %). No producer selector is touched. ⟨probe again⟩ → `[] 0`. ⟨the `:deep` rule removed⟩ → F-7 `painted Expected > 0 · Received 0` (RED) → ⟨restored ×2⟩ → F-7 + F-17 `2 passed` ×2. fourier **`ec3fb0b`**.
29. **Non-regression, after `ec3fb0b` and `0f94a2f`.**
    - The `.h` G-h spec was re-run twice: ⟨`npx playwright test e2e/f-w14-control-row.spec.ts --workers=1` ×2⟩ → `2 passed (16.4s)` · `2 passed (17.2s)`.
    - The other three sibling specs were run once, in the act-28 sitting. That run read 21 passed. The `admin-table` (`.t`), `gallery` and `equation-interaction` specs are all among the 21.
    - The UIA falsifier file was re-run twice: ⟨`e2e/f-w14-uia.spec.ts --workers=1` ×2⟩ → `32 passed (1.1m)` · `32 passed (1.1m)`.
    - Prior evidence was restored (E-3). Running the sibling specs rewrote 27 of `.t`'s and `.h`'s committed `web/e2e/screenshots/f-w14/after-*.png` frames. Each rewritten frame was restored to HEAD with ⟨`git checkout --` on the exact 27 paths⟩. No frame of theirs is changed.
    - Result: ⟨`git status --porcelain web`⟩ → clean.
    - The 21 `docs/tranches/J/…/before/*.png` files were dirty before this unit started. They belong to `.r` and were not touched.

**E13.** This is a Step-0 read at close, over the same four paths as the Open. It found one new glass-side file, `BK/coordination/chicago-inbound-2026-09-23-dock-live-veil-and-hover-lens.md`. That letter is from another consumer to glass, so it is not in F.W14 scope. It found no glass outbound letter to value.js or fourier. The result is **0 UNREAD in scope**. O-63 is rowed.
