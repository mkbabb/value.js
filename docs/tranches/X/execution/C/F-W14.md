SERVED MODEL: claude-opus-5-5

# F.W14 — execution record (Track C, X·F)

Spec: `docs/tranches/X/fourier/waves/F-W14.md` (26 lines, read whole, incl. the §0bv / §0bw / §0ca addenda). Authority: COHESION §0bu (OA-42) · §0bv (OA-43) · §0bw (OA-44/45) · §0ca (audit fold, UIA-F-51 relay-only) · §0cb R-5 (10.0.1 repin cures no docket row by credit; `^8` moves explicitly) · §0bs (repin pattern) · §0bt (ADJACENT-LINE RULE). Tree: `/Users/mkbabb/Programming/fourier-analysis`, branch `m/w1-bump-migration`, HEAD `216ffbd` (= origin). Frames: `docs/tranches/X/fourier/evidence/W14/owner-2026-09-23-{malformed,grayed,blurry,hierarchy}.png` (malformed read at open).

## Open

SEAT 0, `claude-opus-5-5`, 2026-09-23 15:14 EDT. Fresh open (no prior record; LEDGER F.W14 row = `planned`).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → 21 ` M docs/tranches/J/audit/screenshots/before/*.png` + `?? .worktrees/` (the banked F.W13 state; the PNGs are the `.r` J-audit rewrite residual — owned by `.r`, left untouched here). value.js: this record absent, `LEDGER.md` clean. Nothing inherited.

**Preconditions ("Opens after": F.W13 CLOSED and the UI-AUDIT fourier register landed).**
- ⟨`grep -n "^| F.W13 " LEDGER.md`⟩ → `**CLOSED 2026-09-17 (honest-RED: DOCK-SCROLL-MORPH)**` (RESUME 1 Check 1, CONFORMANT-HONEST-RED). GREEN.
- ⟨`ls -la docs/tranches/X/audit/UI-AUDIT-fourier.md`⟩ → 192338 bytes; ⟨`grep -oE "UIA-F-[0-9]+" … | sort -u | wc -l`⟩ → **256** rows (register Counts: BROKEN 51 · HIGH 70 · MEDIUM 93 · LOW 42; 74 with a GLASS part, Appendix B). GREEN.
- Owner-gated items: all RULED — OA-42 §0bu, OA-43 §0bv, OA-44/45 §0bw, the audit fold §0ca, the repin §0bs/§0cb R-5; model law = Opus 5.5 every seat (§0bc, spec §State). Nothing re-opened.

**E13 Step-0 (four paths, read-only, 15:14 EDT).** ⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → `BL/ BK/ BJ/` (BL newest; BK coordination still swept). ⟨`find <V, V/coordination, BK/coordination, BL, keyframes V/coordination, atlas P/coordination, fourier docs/tranches> -newermt '2026-09-23 14:50'`⟩ → value.js `INBOX.md` (self); BK `valuejs-outbound-2026-09-23-ui-audit-relay.md` (ours, O-59); BL `FORMATION-PROGRESS.md`, `audit/{INBOUND,REGISTRY}.md`, `audit/round-2/R2-05.md`, `design/structure/pass-1/D1-*.md` (glass-internal; `7362b3bf` = O-59 banked = I-44, rowed READ; `9831f0e4` = I-43, FOLDED §0cb); keyframes V · atlas P · fourier: none. No glass-outbound letter to value.js/fourier. **0 unrowed · 0 new UNREAD in F.W14 scope.** I-44 (UIA-F-51 recorded blocking at BL) is the glass half `.u` routes by id.

## Baseline

BEFORE, fourier `216ffbd`, served `http://localhost:3100` (⟨`curl -w %{http_code}`⟩ → `200`), host ⟨`uptime`⟩ → load 53.9 / 54.7 / 38.3. Probe = one bounded headless script (removed after; fourier porcelain unchanged), 1440×900, light, DPR 1 and 2; logs scratchpad `fw14-probe{1,2}.json`, `fw14-tsc.log`, `fw14-vitest.log`.

| gate | unit | BEFORE | reading |
|---|---|---|---|
| G-m pin | `.m` | ⟨`grep '@mkbabb/glass-ui' web/package.json`⟩ → `"^8.0.0"`; ⟨installed `node_modules/@mkbabb/glass-ui/package.json` version⟩ → `8.0.0`; ⟨`npm view @mkbabb/glass-ui version`⟩ → `10.0.1` | **RED** |
| typecheck | Close | ⟨`npx vue-tsc -b`⟩ → exit 0 | GREEN (hold) |
| unit tests | Close | ⟨`npx vitest run`⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)` | GREEN (hold) |
| e2e full suite | Close | banked at this same HEAD (F.W13 RESUME 1 Close ×2 + Check 1): `18 failed · 3 skipped · 95 passed`; baseline-RED set = vc `:80 :101 :122` · fullscreen `:40` · contrast-floor `:82`×2 `:128` · gallery-admin-a11y `:91 :103 :114 :125` + the `G-F12-3-LOAD` timeout family. Not re-run (RESUME-economy: HEAD unmoved since the banked double-run) | baseline-RED named |
| G-t (OA-42) | `.t` | bytes: `AdminAuditLog.vue:298-314` — each row a `cartoon-card` (border + offset stamp = the doubled rule) on `grid-cols-[auto_auto_1fr_auto]`; the action a glass `Badge size="sm"` (8.0.0) holding `JANITOR:HARD_DELETE_VISUAL`; the timestamp `toLocaleString` in an `auto` column that wraps (frame). Siblings with the same list shape: `AdminFlaggedPanel.vue`, `AdminUserList.vue`. Served probe owed at `.t`'s own BEFORE (admin session required) | **RED** (frame + bytes) |
| G-g (OA-43) | `.g` | `/w` at rest (no image): the only sidebar node `configurator-aside` → 0 ancestors with `opacity≠1`/`filter≠none`/`inert`, bg transparent. The image-loaded state (the frame's state) is NOT measured here — `.g` owns that BEFORE | rest limb GREEN (vacuous: sidebar absent at rest); loaded state RED by frame |
| G-p (OA-44) | `.p` | backing store vs CSS×DPR: `/equation` 988×572 → `988×572`@1 · `1976×1144`@2 ✓; `/w` 1422×810 → `1422×810`@1 · `2844×1620`@2 ✓; `/`, `/morph`, `/gallery`: 0 visible canvases at rest. DPR sources: `useCanvasSetup.ts:21-31`, `ConvergencePlot.vue:203`, `FrequencyGraph.vue:75`; `BasisCanvas.vue:530` unmeasured (image mode). No `devicePixelContentBoxSize`, no DPR-change listener anywhere (⟨`grep -rn devicePixelContentBoxSize web/src`⟩ → 0) | width/height limb **GREEN-BEFORE-CURE at rest** (finding); DPR-change/zoom limb + stroke-edge crop + loaded-state canvases RED |
| G-h (OA-45) | `.h` | census (bytes): slider sites — FunctionInput 2 · HarmonicLevelGrid 2 · MorphPhaseConfig 1 · BasisSelector 3 · ContourSettings 5 · EditorControlsDock 1 · EquationPanel 1 · GlassTimeline 2 (+ `ui/SliderControl.vue` the local wrapper); served `role=slider`: `/equation` 3 · `/morph` 5 · `/w` rest 0. The idiom is not one shape (frame: label / value line / thumbless bar = 3 rows) | **RED** |
| G-u | `.u` | 256 UIA-F rows open (register), 74 with a glass part; UIA-F-51 GLASS (relay-only via O-59, I-44 blocking at BL) | **RED** |
| G-r | `.r` | ⟨`grep -rniE 'scroll.?to.?top' web/src`⟩ → only `MobileFloatingToc.vue` + `PaperView.vue` (desktop ToC has none); `.sidebar-link` radius named at `style.css:346`; `e2e/visual-baseline.spec.ts:34` + `visual-checkpoint.spec.ts:16` write into `docs/tranches/J/audit/screenshots/` (21 PNGs dirty now); `.c` r1–r4 open (F.W13 Close §5) | **RED** |

**GREEN-BEFORE-CURE (R.2 findings):** (1) G-p's backing-store limb at rest on `/equation` and `/w` at DPR 1 and 2 — the owner's blur is therefore not an unscaled backing store in these states; `.p` must find the cause in the loaded/animated states (image mode, epicycle draw, thumbnails, any CSS transform/scale of the canvas, a DPR change after mount) and gate the stroke-edge crop. (2) G-g's opacity/filter/inert limb at `/w` rest — vacuous (no sidebar at rest); the frame's state is image-loaded.

## Unit plan

Seven units, all Opus 5.5 (spec §State; no Fable/design-author seat named — `.h` is an Opus design seat at effort high). Strictly serial per the spec's final Order line (§0bw): **[`.m`] → [`.t`] → [`.g`] → [`.p`] → [`.h`] → [`.u`] → [`.r`]**; concurrency 1 (every unit may touch `web/src/**`). Writable paths are fourier-relative unless marked `vjs:`; every unit also writes `vjs:docs/tranches/X/execution/C/F-W14.md` (its receipt) and minimal cells/lines of `vjs:…/execution/LEDGER.md`. The spec carries no §File Bounds table; the sets below derive from each unit's text plus the ADJACENT-LINE RULE (§0bt). Never: glass-ui, sibling repos, `scripts/dev/dev.sh`, test/assertion deletion. Glass rows ride O-59/BL by id (never a local override). Locks: `.m`'s pin + lockfile + migration = ONE commit family (`feat(web)!:` repin), not split; `.t`/`.g`/`.p`/`.h` each: cure commit(s) + its gate spec + frames; `.r`'s re-point of the J-audit writers and the restore of the 21 tracked PNGs to HEAD ride ONE commit.

| id | spec section | writable | gates |
|---|---|---|---|
| F.W14.m | Units `.m` (F-W14.md:11) + §0bs + §0cb R-5 | `web/package.json`, `web/package-lock.json`, `web/src/**`, `web/e2e/**` (only goldens/selectors the migration moves, diff read first) | G-m (installed 10.0.1 exact); vue-tsc 0; vitest 84/84; e2e `--workers=1` REDs ⊆ baseline set, load recorded; DOCK-SCROLL-MORPH re-read |
| F.W14.t | Units `.t` (:12) | `web/src/components/visualization/gallery/**`, `web/e2e/f-w14-admin-table.spec.ts`, `web/e2e/screenshots/f-w14/**` | G-t ×2 headed, 1440+390, both themes: badge scrollWidth ≤ clientWidth; timestamp one line at 1440; one computed rule between rows |
| F.W14.g | ADDENDUM §0bv (:17) | `web/src/**`, `web/e2e/f-w14-veil.spec.ts`, `web/e2e/screenshots/f-w14/**` | G-g ×2 headed, light+dark, 1440+390: opacity 1 / no filter / no inert on every ancestor; surface ΔE_OK ≤ 0.02 to glass light token + equal to other panes; ink AA |
| F.W14.p | ADDENDUM §0bw `.p` (:20) | `web/src/**` (canvas owners + `composables/useCanvasSetup.ts`), `web/e2e/f-w14-dpr.spec.ts`, `web/e2e/screenshots/f-w14/**` | G-p ×2 headed DPR 1+2: every canvas width/height = round(client×dpr) incl. after resize/DPR change; stroke edge ≤ 2 device px; before/after crops |
| F.W14.h | ADDENDUM §0bw `.h` (:21) | `web/src/**`, `web/e2e/f-w14-control-row.spec.ts`, `web/e2e/screenshots/f-w14/**`, design notes in the record | G-h: census re-run all slider rows on the idiom; row height = idiom ±4 px; before/after frames per page 1440+390 |
| F.W14.u | Units `.u` (:13) + ADDENDUM §0ca (:24-26) | `web/src/**`, `web/e2e/**` (additive), `web/e2e/screenshots/f-w14/**`, `vjs:docs/tranches/X/relay/` (dated addendum-beside only), `vjs:docs/tranches/V/coordination/INBOX.md` (rows/lines) | every UIA-F row: CURED at cause with its falsifier ×2, or ROUTED-GLASS by id (O-59); frameless rows framed on the served page BEFORE cure; UIA-F-51 relay-only |
| F.W14.r | Units `.r` (:14) | `web/src/components/paper/**`, `web/src/style.css`, `web/e2e/visual-baseline.spec.ts`, `web/e2e/visual-checkpoint.spec.ts`, `web/e2e/f-w14-residuals.spec.ts`, `docs/tranches/J/audit/screenshots/before/*.png` (restore to HEAD only) | desktop ToC scroll-to-top present + acts; `.sidebar-link` radius on the canon; `.c` r1–r4 each cured or ruled; full e2e leaves `git status` of `docs/tranches/J/` clean ×2 |

Close (after `.r`): vue-tsc 0 · vitest · e2e `--workers=1` (REDs = the named baseline set) · served-page gates re-read by the check seat.

## Unit receipts

### F.W14.m

SERVED MODEL: claude-opus-5-5 · 2026-09-23 · spec F-W14.md `.m` (l.11) + COHESION §0bs (repin pattern) + §0cb R-5 (the `^8` moves explicitly; the repin credits no docket row) + §0bt (ADJACENT-LINE RULE).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only the 21 ` M docs/tranches/J/audit/screenshots/before/*.png` + `?? .worktrees/` (`.r`'s residual, outside this set). Nothing inherited.

**Read (read-only, whole).** glass-ui `CHANGELOG.md` §10.0.1 (l.3-26), §10.0.0 (l.27-206), §9.0.0 (l.207-309); `MIGRATION.md` §10.0.0 (l.8-375), §9.0.0 (l.376-579), §8.1.0 scanned for breaks (additive; shipped inside 9.0.0). Break list vs the fourier tree (⟨`grep -rnE "glass-ui/(canvas|search)|colorResolver|ColorResolver|defaultBlobColorResolver|ringsAt|renderAt|startCollapsed|start-collapsed|alwaysExpanded|always-expanded|collapseDelay|collapse-delay|dock-max-inline-size|cm-serif|sidebar-top-btn|useSidebarFollow|parentId|InstanceType<typeof SegmentedTabs|text-caption|darkModeSyncScript|aria-modal|<Metric|handmark" web/src web/index.html web/e2e`⟩):
- `./canvas`, `./search`, `colorResolver`/`ColorResolver`/`defaultBlobColorResolver`, `ringsAt`, `renderAt`, handmark types, `--dock-max-inline-size`, `AuroraAtoms.light`: 0 consumer sites.
- `GlassDock` 14→6 props (9.0.0): 4 sites. `cm-serif` → `font-serif-math` (10.0.0): 25 sites / 16 files. `SegmentedTabs` generic (10.0.0): 3 `$event as` casts. `darkModeSyncScript()` re-hash (10.0.0): the inlined copy in `web/index.html`. Layering into `@layer components` (10.0.0) + the new `.configurator-shell`: found at the served page (below).
- No action (recorded): `useSidebarFollow` here is latex-paper's, not glass's (`PaperView.vue:8`); no `parentId` reader; `text-caption` at `VisualizationView.vue:272` takes the producer's upright paint (no `italic` restored); `<Metric>` sites pass no `delta`; `aria-modal` only in comments.

**Acts, in order.**
1. ⟨`npm install --save-exact @mkbabb/glass-ui@10.0.1`⟩ → `changed 1 package`; `web/package.json:19` `"^8.0.0"` → `"10.0.1"`; lockfile: version/resolved/integrity 10.0.1 and the optional `@mkbabb/pencil-boil` peer gone (9.0.0). ⟨`node -p "require('./node_modules/@mkbabb/glass-ui/package.json').version"`⟩ → `10.0.1`; ⟨`npm ls @mkbabb/glass-ui`⟩ → `@mkbabb/glass-ui@10.0.1`.
2. ⟨`npx vue-tsc -b --force`⟩ → exit 0 on the bare bump. The type-level breaks do not reach this tree; the prop removals fall through as attributes and do not fail to compile, so they were migrated from the break list.
3. GlassDock (9.0.0 fold onto `collapse`): `CanvasControlsDock.vue:41` drops `:start-collapsed="true"`; `EditorControlsDock.vue:63` drops `:collapse-delay="2000" :start-collapsed="true"`; `AnimationControls.vue:118-119` drops both (default `collapse="closed"` = the old collapsed mount; `collapseDelay` has no successor, so every dock idles 3600 ms); `AppDock.vue:71` `always-expanded` → `:collapse="false"`.
4. `cm-serif` → `font-serif-math`, 25 sites / 16 files (perl, word-bounded; ⟨`grep -rn cm-serif web/src`⟩ → only the historical prose in `style.css:54-69`). `style.css:92` comment re-stated: the app's `@theme --font-serif-math` replaces the producer's new `serif` default. ⟨served `curl /src/style.css | grep -o '.font-serif-math…'`⟩ → `.font-serif-math { font-family: var(--font-serif-math) }` emitted; `.cm-serif` no longer emitted.
5. SegmentedTabs generic: `GalleryView.vue` (`GalleryTab` alias; `tabOptions` typed `SegmentedTabOption<GalleryTab>[]`), `VisualizationView.vue:230`, `EquationView.vue:348`: the `:model-value` + `$event as …` pair becomes `v-model`. vue-tsc 0.
6. `style.css` G-F9-5 block (`@layer glass-overrides { :where(.glass-capsule-track) { --muted-foreground: var(--on-glass-muted-strong) } }` + its comment) deleted: ⟨`grep -rhoE ':where\([^{]*glass-capsule-track…' dist`⟩ → `:where(.feedback-tone,.glass-capsule,.glass-capsule-track){--muted-foreground: var(--on-glass-muted-strong);…}` in `styles/glass/ladder.css`. The producer now binds this itself (10.0.0 "three label sites"), so the consumer copy is retired, not shimmed.
7. **Adjacent edit (§0bt)** `web/index.html:88-90,102`: the inlined dark-mode sync script re-derived ⟨`node -e 'import {darkModeSyncScript} from "@mkbabb/glass-ui/dark"; console.log(darkModeSyncScript())'`⟩ → 313 B (10.0.0: an unreadable store reads as absent), with its provenance line re-dated. Reason: the file's own contract says it holds the generator's output at the adopted pin, and the pin moved. Same repo, same concern.
8. Dev server: the long-lived `vite web --port 3100 --strictPort` (pid 14707, started 09-22) was still serving the 8.0.0 prebundle. I restarted it with the same command, and its log reads `Re-optimizing dependencies because lockfile has changed`.
9. **e2e run 1** (a scouting run on the served 10.0.1 before step 10; load ⟨`uptime`⟩ 25.13 → 45.19): `19 failed · 3 skipped · 94 passed`. Six REDs outside the baseline: `f-w13-image-controls :80 :104 :202` and `f-w13-image-empty :64 :97 :117`, all failing on `.drop-target` "Received: hidden". Three more were golden moves: `visual-checkpoint :144 :163 :198`.
   - **Root cause, measured** with a headless probe of `/visualize` walking up the ancestor chain: `.drop-target` h=0 ← `.configurator-stage` h=0 ← `section.configurator` h=0, gtc `1038px 400px` ← `div.configurator-shell` h=**2** ← `div.contents`. At 10.0.0 the Configurator wraps its grid in an outer `.configurator-shell`, and its declared `class` prop lands on that shell. The host's scoped `.viz-configurator[data-v]` rules therefore matched nothing: the shell is not the component's scoped root, which is the producer's `display: contents` expand host (and that is where `data-sidebar` falls through). The flex envelope was lost, the stage went to 0 px, and the rest-state band closure stopped working, leaving the aside at the producer default of 400 px.
10. **Configurator migration** (`VisualizationView.vue` scoped style). The shell rule (flex:1, min-height:0, margins) now uses `:deep(.viz-configurator)`. The aside band moves off the host's `grid-template-columns` and onto the producer's `--configurator-aside-{min,max}` pair (320/360 at ≥1024, 360/400 at ≥1280, 400/440 at ≥1536), which the grid's two-column `@container (inline-size >= 64rem)` rule reads. At rest the band closes through `[data-sidebar="none"] > :deep(.viz-configurator)` { both tokens 0px }, and the aside through `… > [data-slot="configurator"] > .configurator-aside { display:none }`. The band transition and its PRM zeroing are now on `:deep(.viz-configurator > [data-slot="configurator"])`, as is the mobile flex column; the stage rule becomes `:deep(.viz-configurator .configurator-stage)`.
    - Probe after the change: at 1440, shell 1424×812, grid 1422×810, gtc `1422px 0px`, aside `none`, stage and drop 1422×810 (the same stage box the Baseline measured at `/w`). At 390, shell 382×828, grid 380×826, stage 380×826.
    - **e2e selector the migration moves** (diff read): `f-w13-image-empty.spec.ts:37` now reads the transition target as `.viz-configurator > [data-slot="configurator"]` (the grid, where the band transition now runs) instead of `.viz-configurator` (the shell). The assertion `configurator:grid-template-columns` is unchanged.
    - ⟨`playwright test f-w13-image-empty f-w13-image-controls --workers=1`⟩ → `6 passed`.
    - Residual (r1): the two-column switch is now the producer's container query (shell inline-size ≥ 64rem), not the viewport's 1024 px. For viewports of about 1024-1040 px the grid stays single-column while `isDesktop` is already true.
11. **Golden moves, each diff read before re-baselining.**
    - `checkpoint-disclosure-body` (2141 px): the diff sits only on the Presets chip rows and the Notation chips. They shifted horizontally from 10.0.1's Button stadium pad (xs/sm 8 → 10/14 px) and 10.0.0's upright `text-caption` chip rung.
    - `checkpoint-admin-banner` (195 px): the diff is only the Logout button, from the same stadium pad.
    - `checkpoint-tooltip-trigger-mobile` (86 px, 60×61 both): a sub-pixel shift of the icon inside the unchanged box.
    - All three are producer paint, not consumer regressions. Re-baselined with ⟨`playwright test visual-checkpoint.spec.ts -g "item 8 —|item 3 —|@coarse item 4" --update-snapshots`⟩ → `3 passed`. vc `:80 :101 :122` (baseline-RED) were NOT re-baselined.
12. Commit (ONE family: pin, lockfile, root migration, the adjacent `index.html` edit, the e2e selector and the 3 goldens; 29 paths): fourier **`3c688d1`** `feat(web)!: X.F.W14.m — repin @mkbabb/glass-ui ^8.0.0 -> 10.0.1 exact, migrated at the root`. Pushed: ⟨`git rev-parse HEAD origin/m/w1-bump-migration`⟩ → both `3c688d16`.

**Gates, BEFORE → AFTER (settled bytes, double-run).**
| gate | BEFORE | AFTER |
|---|---|---|
| G-m pin | `"^8.0.0"`, installed `8.0.0` | ⟨`grep glass-ui web/package.json`⟩ → `"10.0.1"`; installed ⟨`node -p …version`⟩ → `10.0.1` — **GREEN** |
| vue-tsc | exit 0 | ⟨`npx vue-tsc -b --force`⟩ ×2 → exit 0 · exit 0 — **GREEN** |
| vitest | 13/84 | ⟨`npx vitest run`⟩ ×2 → `13 passed (13)` · `84 passed (84)`, both runs — **GREEN** |
| e2e `--workers=1` | 18 failed · 3 skipped · 95 passed (banked) | run 2: `10 failed · 3 skipped · 103 passed (4.4m)`, load 34.30 → 54.18; run 3: `10 failed · 3 skipped · 103 passed (3.4m)`, load 54.18 → 16.28. REDs both runs = vc `:80 :101 :122` · contrast-floor `:82`×2 `:128` · gallery-admin-a11y `:91 :103 :114 :125` — **⊆ baseline, GREEN**. fullscreen `:40` and the G-F12-3-LOAD timeouts did not fire in either run. contrast-floor datum: light `18 of 36 pairs`, dark `10 of 36 pairs` (no banked count to compare) |
| DOCK-SCROLL-MORPH | honest-RED (O-55) | re-read at the installed 10.0.1 dist: ⟨`grep -rlE 'compactOnScroll|scrollCompact|progressRim|scroll-compact|scrollProgress' dist | wc -l`⟩ → `0`; `DockProps` = `fitContent · backdropMode · shape · orientation · collapse · backgroundCanvas`; the exposed handle is `expanded · isPinned · isHeld · graspHeld · isTransitioning · expand · collapse · keepOpen · release`; ⟨`grep -rniE 'rim|progress|compact' dist/components/dock/*.d.ts …`⟩ → 0 dock hits. **Still honest-RED, not cured by the repin** (owner: glass, O-55) |

**§0cb R-5.** No docket row is credited to this repin. The glass rows are re-read by their own owners: `.u` through O-59/BL, and the landing repin once BL names a version.

**Residuals.**
- (r1) The container-vs-viewport two-column seam at about 1024-1040 px (step 10).
- (r2) The `collapseDelay` 2000 → producer 3600 ms idle on the three canvas docks, which has no knob at 10.0.
- (r3) `visualization-crud.spec.ts:664` stays `fixme`: it cites producer SegmentedTabs underline ink 4.33:1, which 10.0.0 lists as 5.22/5.02. I did not un-fixme it here (R-5: no credit); `.u` re-reads it.
- (r4) Historical prose that still names `:start-collapsed` (`CanvasControlsDock.vue:127`) and `cm-serif` (`style.css:54-69`) is left as record.
- (r5) The e2e runs rewrote `docs/tranches/J/audit/screenshots/before/*.png` again. This is `.r`'s residual; those files were not staged.

**Adjacent edits (§0bt).** `web/index.html:88-90,102` (step 7).

**Escalations.** None.

### F.W14.t

SERVED MODEL: claude-opus-5-5 · 2026-09-23 · spec F-W14.md `.t` (l.12) + COHESION §0bu (OA-42) + §0bt (ADJACENT-LINE RULE) · frame `fourier/evidence/W14/owner-2026-09-23-malformed.png` (read).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only the 21 ` M docs/tranches/J/audit/screenshots/before/*.png` (these are `.r`'s) + `?? .worktrees/`. Nothing in this unit's writable set was dirty, so nothing was inherited.

**Anchors at the true bytes (fourier `3c688d1`).** `AdminAuditLog.vue:298-314` holds each row as a `cartoon-card grid grid-cols-[auto_auto_1fr_auto]`, with the timestamp as a `span.tabular-nums` and the action as a glass `Badge size="sm"`. The anchor held, with no drift.
- The glass Badge at 10.0.1 is already content-sized: `badge-atom inline-flex … whitespace-nowrap` plus `:where(.badge-atom){flex:none}` (dist `mark.css`). The overflow is therefore caused by the consumer's per-row grid, not by the Badge.
- Siblings: `AdminUserList.vue:590` (`cartoon-card` rows) and `AdminFlaggedPanel.vue:364` (a glass `Card` per entry).

**Acts, in order.**
1. **Gate first.** I wrote `web/e2e/f-w14-admin-table.spec.ts`, 12 cases: the three panels × 1440/390 × light/dark, with a colour scheme per describe block.
   - Fixtures: the shared `stubAdminApi` plus two routes of its own, registered later so they answer first. The audit route serves the frame's five long actions (`janitor:hard_delete_visualizations`, `set_tier:featured`, `suspend_user`, `batch_users:delete`, `janitor:prune_audit`). The flagged route serves two entries, so there is an adjacent pair to measure.
   - The panel is the labelled collection itself (`[aria-label=…]`).
   - Assertions, per-item and soft:
     - every `.badge-atom` has `scrollWidth ≤ clientWidth`;
     - for each adjacent row pair, the painted rules are counted: the upper row's border-bottom, plus the lower row's border-top, plus any outer shadow on the upper row with a positive y offset. The count must be exactly 1;
     - Audit Log at 1440: each `time[datetime]` makes one line box (distinct `Range.getClientRects()` tops) and has `fontVariantNumeric ⊇ tabular-nums`.
   - Frames go to `e2e/screenshots/f-w14/{before|after}-<panel>-<w>-<scheme>.png`.
2. **BEFORE, measured on the served page** (`http://localhost:3100`, dev server on `3c688d1`, headed, `--workers=1`). ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-admin-table.spec.ts --project=chromium --workers=1 --headed`⟩ → `12 failed`.
   - **Audit Log**, all four cells: every adjacent pair reads **3 rules** (`upper border-bottom 2px + lower border-top 2px + upper offset shadow oklab(… / 0.32) -3px 3px 0px 0px, …`), which is the frame's doubled rule.
   - **Audit Log at 390**, both themes: **4 of 5 badges overflow** (`janitor:hard_delete_visualizations`, `set_tier:featured`, `batch_users:delete`, `janitor:prune_audit`). The target column collapses to 0 px, so `getByText('spiral-lattice-04')` reads `hidden` (the same as UIA-F-42), and the timestamp wraps over two lines. That is the owner's frame, reproduced. The frames are `before-audit-log-390-{light,dark}.png`.
   - **Audit Log at 1440**: the badges fit and the timestamp is one line. Those two limbs were already GREEN at 1440; only the rule limb was RED.
   - **Users**, all four cells: 3 rules per pair (the same `cartoon-card` stamp).
   - **Flagged**, all four cells: `[role="listitem"]` → `element(s) not found`. The glass Card drops the consumer's `role` (UIA-F-43), so the list had no listitems. A bounded headless probe (deleted afterwards) read the two `.flagged-card`s as `border-top 1px` / `border-bottom 1px` with an 8 px gap between them, at both 1440 and 390, so each adjacent pair reads **2 rules** and the role is `null`. No before-frame was written for Flagged, because the locator failed before the screenshot step. The probe numbers above are that cell's BEFORE.
3. **Cure: Audit Log** (`AdminAuditLog.vue`). The glass `DataTable` (`@mkbabb/glass-ui/data-table`, 10.0.1) sits on one glass `Card size="sm"` plate.
   - Columns (`auditColumns`), each a small render component that receives the producer's `value`/`row` props:
     - Time: a `<time datetime>` with `font-mono tabular-nums whitespace-nowrap`;
     - Action: the glass `Badge` (variant, tone and size unchanged), which the table cell sizes to its content;
     - Target: `block truncate`, with the full value kept in `title` (AA-40). The cell is `w-full max-w-0`, so it is the only column that yields;
     - IP hash: `text-mono-micro`, with the sr-only full hash (AA-15/AA-40).
   - Rows are keyed `${page}-${i}` through `_id`. `status` is loading/ready, and `filtered` is `hasFilters`, so the `empty` and `filtered-empty` slots carry AA-33's two headlines.
   - The producer's cell-padding tokens are set to the log's density: `[--table-cell-px:--spacing(3)] [--table-cell-py:--spacing(2)]`. `text-xs` was not added, because that utility is dead under glass ≥8 (UIA-F-57).
   - The old `Loading audit entries…` block is retired: the producer's own skeleton renders for loading with no rows.
   - Each body row is the producer's `TableRow`: one `border-b`, and `[&_tr:last-child]:border-0`.
   - At 390 the table scrolls inside the producer's `table-container` (`relative w-full overflow-auto`) rather than squeezing a cell. The error `role=alert` block, the AA-12 live region and the pagination `nav` are unchanged.
4. **Sweep: Users** (`AdminUserList.vue`). The rows now sit on one glass `Card size="sm"` plate (`v-show="userRows.length"`). `role="list"` is on an inner `div`, because the Card drops `role`.
   - Each row loses `cartoon-card rounded-lg` and takes the producer's table-row rule, `border-b border-border … last:border-b-0`, coloured with the `--border` token. The Badge is unchanged.
5. **Sweep: Flagged** (`AdminFlaggedPanel.vue`). The per-entry `<Card role="listitem">` becomes a `div role="listitem"` row (`flagged-row border-b border-border p-3 last:border-b-0`) on ONE `Card` plate (`flagged-queue`, `v-else` + `v-show`).
   - The F.W3 accent is kept and split: `--glass-accent: var(--destructive)` goes on the plate, and the 6 % destructive wash goes on each row.
   - The list now holds real listitems, which cures UIA-F-43's consumer half on this surface. The glass half, where Card drops `role`, stays with O-59.
6. ⟨`npx vue-tsc -b`⟩ → exit 0. ⟨`npx vitest run`⟩ → `13 passed (13)` · `84 passed (84)`.
7. **Regression read.** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/gallery-admin-a11y.spec.ts --workers=1`⟩ → `4 failed`: `:91 :103 :114 :125`, the named baseline-RED set.
   - Every one of them is the same single violation, `aria-hidden-focus` on `div[role="region"] > span[aria-hidden]` (2 nodes). That is the banner's region, not these panels.
   - No new rule fired: there is no table, list or listitem violation.
8. **Commit** (one family: the cure in 3 files, the gate spec and 20 frames; 24 paths): fourier **`b5a650f`** `fix(web/admin): X.F.W14.t — OA-42: …`. Pushed: ⟨`git rev-parse --short HEAD`; `… origin/m/w1-bump-migration`⟩ → `b5a650f` · `b5a650f`.
   - The frames are force-added under `web/.gitignore:1 *.png`, as F.W13's were: 12 `after-*` plus 8 `before-*` (audit and users).

**Gates, BEFORE → AFTER** (settled bytes `b5a650f`, served `:3100`, headed, `--workers=1`, double-run). ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-admin-table.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `12 passed (50.3s)` · run 2 `12 passed (46.9s)` (load ⟨`uptime`⟩ 94.86 after run 2).

| gate | BEFORE | AFTER |
|---|---|---|
| G-t badges (`scrollWidth ≤ clientWidth`), 1440+390 × light+dark | audit 390: 4/5 overflow (both themes); 1440: fit | every badge fits in all 12 cells, ×2 — **GREEN** |
| timestamp one line + tabular at 1440 | GREEN at 1440 (wrapped at 390, the frame) | 5/5 `<time>` one line box, `tabular-nums`, ×2 — **GREEN** |
| exactly one computed rule between rows | audit 3 · users 3 · flagged 2 (+ 0 listitems) | 1 per pair in every panel/width/theme, ×2 — **GREEN** |
| sweep over Flagged + Users | RED (above) | GREEN (the same 3 checks, same runs) |

**Residuals.**
- **(R-t-1, GLASS, relay only: never a local override.)** The glass `TableRow` (`border-b`) and `TableHeader` (`[&_tr]:border-b`) bind no border colour. Neither glass nor this app ships a base `border-color`, so the DataTable's row rules paint in `currentColor`.
  - Evidence: a bounded headless probe (deleted afterwards) at 1440 read each `tr` `borderBottomColor` as equal to its own `color`. Light: `rgb(28, 25, 23)` (header row `rgb(112, 89, 66)`). Dark: `rgb(233, 230, 226)`. The `--border` token is `light-dark(hsl(32 26% 70%), hsl(30 16% 34%))`.
  - Effect: the audit table's single rule is ink-coloured. It is still exactly one rule, so G-t is GREEN, but it is heavier than the swept Users and Flagged rows, which bind `border-border`.
  - I did not override the producer here, following the lock. It is owed to glass BL by id. `.u` holds the O-59 relay's writable set, so it should add this row there. Its source is glass `src/components/table/TableRow.vue:14` and `TableHeader.vue:11`.
- (R-t-2) The Flagged cell has no before-frame. The spec's locator failed before its screenshot step, because the Card dropped `role`. The BEFORE for that cell is the probe reading in act 2.
- (R-t-3) The UIA-F rows this cure touches are **not self-credited**. `.u` credits each one with its own falsifier:
  - UIA-F-42 (audit at 390: 0 px target, badge over timestamp);
  - UIA-F-108 (ledgers → DataTable): the audit ledger is now DataTable. The users ledger is on the one-rule row idiom inside a Card, not DataTable, because it has a selection checkbox, two-line identity and per-row actions;
  - UIA-F-43 consumer half (the flagged list now holds real listitems);
  - UIA-F-252 (in part: loading is now the producer skeleton).
- (R-t-4) The DataTable has `responsive` left false, so at 390 the audit table scrolls horizontally inside the producer's container. The card projection was not used because it draws a bordered card per row with a gap between them, which is two rules per pair.

**Adjacent edits (§0bt).** None.

**Escalations.** None.

### F.W14.g

SERVED MODEL: claude-opus-5-5 · 2026-09-23 · spec F-W14.md ADDENDUM §0bv (l.16-17) + COHESION §0bv (OA-43) + §0bt (ADJACENT-LINE RULE) · frame `fourier/evidence/W14/owner-2026-09-23-grayed.png` (read: light theme, image mode, the right sidebar under a grey veil beside the cream stage).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only `.r`'s 21 ` M docs/tranches/J/audit/screenshots/before/*.png` + `?? .worktrees/`. Nothing in this unit's writable set was dirty, so nothing was inherited.

**Cause at the bytes (fourier `b5a650f`, served `:3100`, image loaded on `/w/…`, headed).**
- ⟨ancestor walk from the first sidebar control to `html`⟩ → every ancestor reads `opacity 1`, `filter none` and `inert false`. The only backdrop filter is the producer's aside, `blur(20px) saturate(1.5)`, and `saturate` above 1 does not desaturate. So there is no opacity, filter or inert veil.
- The one painted surface under the controls is the producer shell `div.configurator-shell.glass-floating` (glass 10.0.1 `Configurator`). Its background computes to `color(srgb 0.204 0.148 0.083 / 0.18)`.
  - That value is glass's light veil: `--glass-veil-ink: oklch(0.28 0.035 70)` at the floating rung `0.14 + 0.04 = 0.18`, with `--glass-level: 1`. It is smoke laid over the paper `rgb(251 250 248)`, and the DOM composite reads `rgb(214.7 211.9 207.2)`.
  - Every other box between the shell and the controls is transparent: `.configurator-aside`, the scroll-port, `.viz-panel-left-wrap`, `.viz-panel-left`, and `.configurator-layer` (its `--configurator-section-tint` is `transparent` in the light arm).
- The stage is not veiled, because the consumer's `.canvas-container.cartoon-card` paints `--card` = `rgb(253 245 236)` over the same shell. So the aside was the only pane whose controls sat directly on the smoke. The frame's grey sidebar beside a cream stage is exactly this.
- Inks on the smoke: every `--muted-foreground` subtitle and value read **4.44:1**. The pressed `Epicycles` label (bare `--viz-fourier` on its 12 % tint) read **2.74:1**. The contour `Advanced` trigger, inked at `color-mix(--foreground 40 %, transparent)`, read **2.35:1**, which is a disabled look on a live control.

**Acts, in order.**
1. **Gate first.** I wrote `web/e2e/f-w14-veil.spec.ts` with four cases: 1440 and 390, each in light and dark (`colorScheme` per describe block). Each case loads the image through the page's own file input (`image-file-input`, `assets/animals/golden-retriever.webp`) and waits for `/w/…`, then for `Replace image`, and for every time-based animation in the sidebar to finish. It writes the frame `e2e/screenshots/f-w14/{before|after}-veil-<w>-<scheme>.png`, then opens every `ConfiguratorLayer` so that every control the sidebar holds is live. It reads three limbs in the page:
   - **Veil limb.** For every visible control (`button`, `input`, and `[role=slider|spinbutton|button|radio|switch]`), walk its ancestors and flag any `opacity≠1`, any `filter≠none`, any `grayscale()` or `saturate(<1)` in `backdrop-filter`, and any `inert`.
   - **Surface limb.** Composite every background from the root down to `.viz-panel-left`. Colours are resolved by a 1×1 canvas, so the value is the one that paints. Compare the result with `var(--card)`, resolved in the active theme, and with the stage pane's painted surface. Both must be within ΔE_OK ≤ 0.02, and the surface must be opaque.
   - **Ink limb.** For every visible text node in the sidebar, composite its `color` over its composited background and compute the WCAG ratio. The floor is 4.5, or 3 for large text.
   - **Instrument notes.** The first draft counted a closed layer's `configurator-layer-region[inert]`. Closed-disclosure inert is correct semantics, not a veil, so the gate now opens every layer and measures every control live. Scroll-timeline animations (the fading scroll-port's feather) never finish, so the settle wait ignores them.
2. **BEFORE, served, headed.** ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-veil.spec.ts --project=chromium --workers=1 --headed`⟩ → `4 failed`.
   - **Light, 1440 and 390:** `veils=0`. The surface reads `rgba(214.741, 211.937, 207.231, 1)` against `--card` = the stage = `rgb(253 245 236)`, so ΔE_OK(surface, card) = ΔE_OK(surface, stage) = **0.1036**. 51 inks, of which **38** are below AA (the diff's `+ 40` lines are `Array [` + 38 entries + `]`), with a minimum of 2.35: the `--muted-foreground` set at 4.44, `Epicycles` at 2.74, `Advanced` at 2.35, and `Fourier spectrum` at 4.02.
   - **Dark, 1440 and 390:** `veils=0`. The surface reads `rgba(13.635, 10.878, 7.024, 1)` against `--card` = the stage = `rgb(53 42 34)`, so ΔE_OK = **0.1445**. The ink minimum is 5.47.
   - Frames: `before-veil-{1440,390}-{light,dark}.png`. The light 1440 frame reproduces the owner's frame: a grey aside beside the cream stage.
3. **Cure 1, the surface (`VisualizationView.vue:253`).** The Configurator takes `class="viz-configurator glass-opaque"`.
   - `.glass-opaque { --glass-level: 0 }` is glass's named OPAQUE escape (glass `docs/canon/glass-system.md` §`--glass-level`: *"`level=0` is the OPAQUE escape (solid `--card` + `blur(0)`) through the SAME machinery"*; dist `styles/glass/ladder-undershadow.css`). The shell's own `--glass-veil` recipe, `color-mix(… ink/rung … calc(level·100%), var(--card))`, then resolves to `--card`.
   - The producer's tone is not overridden. The consumer selects the glass rung for an opaque instrument on paper, and that rung is the surface the stage card and every other fourier pane already paint.
   - A template comment records the measured cause.
   - Because the level inherits, I read the stage docks (play dock, canvas-controls dock) on the after-frames. Both still render as before.
4. **Cure 2, the pressed basis label (`BasisSelector.vue`, `.basis-toggle[aria-pressed=true]`).** The label ink becomes `--pill-ink: color-mix(in oklab, var(--pill-color) 75%, var(--foreground))`. Before, it was the bare glass `--viz-*` stroke hue.
   - Offline derivation (scratchpad `ink.py`, both arms, all three bases, on their 12 % tint over `--card`). The bare hue gives 3.70 / 5.17 / 4.51 in light and 3.98 / 4.51 / 4.62 in dark. At 75 % it gives 5.32 / 6.81 / 6.17 in light and 5.02 / 5.46 / 5.54 in dark.
   - The model's 3.70 matches the page's reading of 3.69 for the Epicycles label on `--card`.
   - The hue, the tint and the border are unchanged.
5. **Cure 3, the Advanced trigger (`ContourSettings.vue`, `.advanced-trigger`).** The ink changes from `color-mix(--foreground 40%, transparent)` to `var(--muted-foreground)`, and the hover changes from 60 % to `var(--foreground)`. The old ink was the literal greyed-out look on a live disclosure.
6. **Checks.** ⟨`npx vue-tsc -b`⟩ → exit 0. ⟨`npx vitest run`⟩ → `13 passed (13)` · `84 passed (84)`.
7. **Regression read over the image mode.** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/f-w13-image-controls.spec.ts e2e/f-w13-image-empty.spec.ts e2e/contrast-floor.spec.ts e2e/visualization-ux.spec.ts e2e/slider-scrub-contrast.spec.ts --workers=1`⟩ → `3 failed · 17 passed (43.8s)`.
   - The 3 failures are contrast-floor `:82` light and dark plus `:128`, which is exactly the named baseline-RED set.
   - Their pair lists contain no basis, pill or Advanced pair: ⟨`grep -ciE 'basis|advanced|pill' <log>`⟩ → `0`.
   - The visual-checkpoint goldens hold no `/w` state (⟨`ls e2e/visual-checkpoint.spec.ts-snapshots/`⟩ → gallery / admin / equation only), so no golden moves.
8. **Commit (one family: 3 cure files + the gate spec + 8 frames = 12 paths).** fourier **`001bdcf`** `fix(web/visualize): X.F.W14.g — OA-43: …`. The frames were force-added under `web/.gitignore:1 *.png`, following `.t`'s precedent.
   - Pushed: ⟨`git fetch; git rev-parse --short HEAD origin/m/w1-bump-migration`⟩ → `001bdcf` · `001bdcf`.

**Gates, BEFORE → AFTER.** Settled bytes `001bdcf`, served `:3100`, headed, `--workers=1`, double-run. ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-veil.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `4 passed (11.1s)` · run 2 `4 passed (9.8s)`. Load ⟨`uptime`⟩ after run 2: 40.09 / 48.36 / 58.17.

| gate | BEFORE | AFTER (×2) |
|---|---|---|
| G-g veil: every ancestor of every live sidebar control has `opacity 1`, no filter, no grayscale or saturate < 1, no inert (1440+390 × light+dark) | `veils=0` in all 4 cells (GREEN-BEFORE-CURE: the veil was never an opacity, filter or inert) | `veils=0`, 4/4 — **GREEN** |
| sidebar surface = glass `--card` (ΔE_OK ≤ 0.02) = the other pane's surface | light 0.1036 · dark 0.1445 vs both | light `rgb(253 245 236)` = card = stage, ΔE 0.0000; dark `rgb(53 42 34)` = card = stage, ΔE 0.0000 — **GREEN** |
| controls' ink AA against it | light: 38 of 51 below AA, min 2.35; dark: min 5.47 | 51 inks each; min **5.26** light · **4.66** dark; 0 below floor — **GREEN** |

**Residuals.**
- **(R-g-1, GLASS, relay only, for `.u` to row into O-59; the relay's writable set is `.u`'s.)** The glass `Configurator` in the light arm lays its controls aside straight on the `glass-floating` veil. That veil is `oklch(0.28 0.035 70 / 0.18)` smoke, measured `rgb(215 212 207)` over the paper, and `--configurator-section-tint` is `transparent` in the light arm. As a result, glass's default inspector renders every secondary ink (`--muted-foreground`) at 4.44:1 against it, below AA.
  - fourier now selects the opaque rung, which is a consumer choice made on the canon's escape. The producer default still fails AA for any consumer on the floating tier over paper.
  - Source: glass `src/components/configurator/` (shell class `glass-floating`) and `styles/glass/veil.css` (light `--glass-veil-ink`/`--glass-veil-base`).
- (R-g-2) The owner frame's `Replace image` button, the NumberField plates and the slider tracks now sit on `--card`. Their own fills are glass's, and they read as before relative to the new surface (after-frames). The control-row idiom itself (label / value / bar on three rows) is `.h`'s OA-45 and is not touched here.
- (R-g-3) The UIA-F rows this cure touches are not self-credited. `.u` credits each one with its own falsifier.

**Adjacent edits (§0bt).** None. All three cure files are under `web/src/**`.

**Escalations.** None.

### F.W14.p

SEAT `.p`, `claude-opus-5-5`, 2026-09-23 16:05–16:30 EDT. Spec: F-W14.md ADDENDUM §0bw `.p` (line 20), OA-44, frame `owner-2026-09-23-blurry.png` (read: a thin, low-alpha epicycle chain on the grid). Consumed: COHESION §0bw, §0ca, §0cb R-5, §0bt. Tree `m/w1-bump-migration`, HEAD at open `001bdcf`.

**Acts, in order.**
1. **Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → the 21 `docs/tranches/J/audit/screenshots/before/*.png` (`.r`'s, untouched) + `?? .worktrees/`; nothing under `web/src/**` or `web/e2e/f-w14-dpr*`. Nothing inherited.
2. **Census (bytes).** ⟨`grep -rln "<canvas\|getContext\|OffscreenCanvas\|createImageBitmap\|devicePixelRatio" web/src`⟩ → three canvases, no WebGL (⟨`grep -rn webgl`⟩ → 0): `BasisCanvas.vue:530` (epicycles, image/contour views, via `useCanvasSetup`), `ConvergencePlot.vue` (`/equation`), `FrequencyGraph.vue` (the image mode's Coefficients layer). Three separate DPR setups: `useCanvasSetup.ts:21-31` (a container ResizeObserver; no DPR-change path), `ConvergencePlot.vue:203` (it reallocated the bitmap in `draw()`, every animation frame), `FrequencyGraph.vue:75` (sized only on data change: no resize or DPR path at all). ⟨`grep -rn devicePixelContentBoxSize`⟩ → 0. The thumbnails are `<img>`, not canvases. A headless DPR 2 probe of `/gallery` read `naturalWidth` 1024×577 against a 221×165 CSS `object-fit: cover` box, which needs 442×330 device px. The thumbnails are not upscaled. The image overlay is the contour's own source image, drawn at alpha 0.28. It is the photo, not a viz stroke, so it is left as it is.
3. **The epicycle strokes.** `epicycles.ts:152-155` draws the chain under `ctx.scale(fit.fitScale)`, so `lineWidths {circle 4, arm 3.5}` were multiplied by the fit. That made them sub-pixel, alpha-0.5 strokes when the fit shrinks the figure, and it grew them on hover. This is the owner's frame: `before-dpr-epicycles-2x-figure.png` reproduces it.
4. **Gate spec written first**: `web/e2e/f-w14-dpr.spec.ts`. It has 4 tests: {`/equation`, image mode on `/w`} × DPR {1, 2}, with limbs rest → viewport resize 1180×820 → DPR swap (CDP `deviceScaleFactor`) → zoom 125% (CSS viewport ÷1.25 and DPR ×1.25). At rest it takes a stroke-edge crop from the stage's left field at mid-height, with an evidence crop of the chain.
5. **Instrument findings (measured, recorded in the spec).**
   - (a) Under DPR emulation, Chromium's `devicePixelContentBoxSize` reports the physical screen, not the emulated DPR. ⟨probe `.dpcb-probe.mjs`, 300.5-px div⟩ → headed DSF 1: `{"dpr":1,"dev":601}`, headed DSF 2: `{"dpr":2,"dev":601}`, headless: `301` at both.
   - (b) A CDP DPR change signals the page nothing. ⟨probe⟩ → `[2,false,[["ro",1,601]]]`: `devicePixelRatio` is 2, `(resolution:1dppx)` no longer matches, and there is no `change` event, no `resize` event and no new observer entry. Media queries are re-evaluated only on the next media-value change: ⟨probe + `emulateMedia`⟩ → `[["mq",2]]`. The instrument therefore follows each DPR swap with an `emulateMedia({reducedMotion})` round-trip, which changes no feature the app reads. No app code is touched and no event is synthesised.
   - (c) After a DPR swap, neither capture path renders at the live DPR: Playwright's capture keeps the context's DPR and CDP's headed capture takes the physical 2×. The stroke-edge crop is therefore read at the launch DPR only, and the crop's own scale is asserted (`toBeCloseTo(dpr)`).
   - (d) **Anchor drift, read at INTENT.** `clientWidth` is the CSS box rounded to an integer. The image-mode stage is fractional (802.5 / 798.4375 px), and there the literal `round(clientWidth×dpr)` names a bitmap one device px wider than the box, which would force a resample. The gate reads the same law at layout precision: per axis, `width === round(rect.width×dpr)` or the device-snapped box `round((l+w)·dpr) − round(l·dpr)`. On an integer box this equals `round(clientWidth×dpr)`.
6. **BEFORE, served** (`:3100` = vite on the working tree, HEAD bytes swapped in for the run and restored byte-exact from scratchpad after), headed, `--workers=1`, double-run. ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-dpr.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `2 failed · 2 passed (16.7s)` · run 2 `2 failed · 2 passed (15.3s)`, identical reds:
   - DPR 1: `DPR 1→2: overflow-x-auto > canvas.block 691×120 ≠ (691×2)×(120×2)` and `zoom 125% (dpr 1.25): … 691×120 ≠ (691×1.25)×(120×1.25)`.
   - DPR 2: `DPR 2→1: … 1382×240 ≠ (691×1)×(120×1)` and `zoom 125% (dpr 2.5): … 1382×240 ≠ (691×2.5)×(120×2.5)`.
   - `/equation` passed only because ConvergencePlot reallocated every frame. BasisCanvas passed its swaps only because the stage's border snaps per DPR and so moves its content box.
   - **The stroke-edge limb at rest was GREEN-BEFORE-CURE** on the final instrument. At HEAD the grid is crisp at rest at DPR 1 and 2, so the blur the owner saw is not a stale bitmap at rest. It is (i) the fit-scaled sub-pixel chain strokes (act 3; compare the figure crops) and (ii) the missing DPR path (the reds above).
   - Earlier drafts of the instrument read `4`–`11` transitions at rest. Those readings were the 1.8-px axis counted as a 1-px stroke, plus captures resampled after a DPR swap. Both instrument errors are fixed in 5(c) and in the ink filter (runs above 2× the crop's median ink are excluded; resampling conserves ink, so a smeared grid line still counts). The early readings are not evidence.
7. **Cure** (`1278284`, one commit: the cure, its gate spec and the crops).
   - `useCanvasSetup.ts` is rewritten as **the one DPR-aware idiom**:
     - It takes `(canvasRef, onResize)` and observes **the canvas itself**, with `box: "device-pixel-content-box"` where `ResizeObserverEntry.prototype` has it.
     - The device box is used only when it lies within 1 px of `css×dpr` on each axis (snapping). Otherwise the store is `round(css×dpr)`, so the 5(a) emulation gap cannot size a bitmap that the page then resamples.
     - A `(resolution: <dpr>dppx)` media query is re-armed at each change.
     - It never writes `style.width/height`, so the CSS box belongs to layout and nothing can stretch a smaller bitmap.
     - It reallocates only a changed bitmap.
     - `ctx.setTransform(deviceW/css, 0, 0, deviceH/css, 0, 0)`, so `lineWidth` is CSS px.
     - `surface.dpr` = the applied scale.
   - `BasisCanvas.vue`:
     - The call site is updated.
     - `exportFrame` copies `s.ctx.getTransform()` instead of re-deriving the DPR.
     - The canvas's CSS box is now its stylesheet's `inset:0; 100%`, the stage's padding box. Before, an inline style gave it the container's border-box width.
   - `ConvergencePlot.vue`:
     - `draw()` paints on the surface, which ends the per-frame `canvas.width =`.
     - Its own ResizeObserver is deleted.
     - The canvas becomes `absolute inset-0 size-full` inside the already-`relative` container, so the bitmap cannot feed back into the flex height.
   - `FrequencyGraph.vue`:
     - The content-sized CSS box moves to the template's `:style` (`canvasWidth` × `HEIGHT`).
     - `draw()` paints on the surface.
     - The label ink reads `getComputedStyle(ctx.canvas)`.
     - The `onMounted` draw is gone (the composable draws on mount).
   - `epicycles.ts`: the circle and arm `lineWidth` are divided by `fit.fitScale`, so strokes stay CSS px under the fit transform. The dot radii are geometry and are left alone.
8. **Unit checks.** ⟨`npx vue-tsc -b`⟩ → exit 0. ⟨`npx vitest run`⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)`.
9. **Regression read (canvas-touching specs).** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/{contrast-floor,f-w13-image-controls,f-w13-radius,fullscreen,timeline-scrub,f-w13-image-empty,f-w14-veil,visualization-ux,visual-checkpoint,equation-interaction,workspace-flow,settings-persistence}.spec.ts --project=chromium --workers=1`⟩ → `6 failed · 40 passed (1.3m)`.
   - The 6 are exactly the named baseline-RED ids: contrast-floor `:82`×2 `:128` and visual-checkpoint `:80 :101 :122`. Nothing new is red.
   - The run rewrote `.g`'s four tracked `after-veil-*.png`. They were restored to HEAD by exact path (they are `.g`'s evidence, not this unit's).

**Gates, BEFORE → AFTER.** Settled bytes `1278284`, served `:3100`, headed, `--workers=1`, double-run. ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-dpr.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `4 passed (12.7s)` · run 2 `4 passed (11.7s)`. Load ⟨`uptime`⟩ after run 2: 44.47 / 46.92 / 49.40.

| gate | BEFORE (HEAD `001bdcf`) | AFTER (`1278284`) |
|---|---|---|
| G-p backing store, every canvas (BasisCanvas · ConvergencePlot · FrequencyGraph), at rest / resize / DPR swap / zoom 125 %, DPR 1 and 2 | **RED**: FrequencyGraph at DPR swap + zoom, at both DPRs (4 misfits ×2 runs) | **GREEN** ×2 |
| 1-px stroke edge ≤ 2 device px of transition (grid crop at rest, crop scale asserted = DPR) | GREEN-BEFORE-CURE (the grid was crisp at rest; blur = the chain strokes + the DPR path) | **GREEN** ×2 |
| before/after crops committed | — | **GREEN**: 8 PNGs `web/e2e/screenshots/f-w14/{before,after}-dpr-epicycles-{1x,2x}-{rest,figure}.png` (force-added past `web/.gitignore` `*.png`, as `.g` did) |

**Inherited paths.** None.

**Adjacent edits (§0bt).** None. Every edited path is under `web/src/**`, plus the unit's own spec and crops.

**Residuals.**
- **p1 (instrument, recorded).** The spec's literal `round(clientWidth×dpr)` cannot hold on a fractional CSS box. The gate reads it at layout precision (5(d)). A check seat should read the gate with that formula, not the integer one.
- **p2 (instrument, recorded).** Headed Chromium cannot render a crop at a changed DPR, and it cannot fire the DPR media event from CDP without a media re-evaluation (5(b), 5(c)). The DPR-swap limb is measured on the backing store, and the stroke-edge limb at the launch DPR.
- **p3 (visual, for `.h`'s eye, not a defect).** With strokes in CSS px, the fitted chain now draws at its authored 4 / 3.5 px at every fit (`after-dpr-epicycles-2x-figure.png`), where before it drew at about the fit × those widths. If the design wants a lighter chain, the change goes in `lineWidths` (`BasisCanvas.vue:176,360`), not in the fit.

**Escalations.** None.

### F.W14.h

SEAT `.h`, `claude-opus-5-5`, 2026-09-23. Spec: F-W14.md ADDENDUM §0bw `.h` (line 21), OA-45. Frame `owner-2026-09-23-hierarchy.png` (read: "Harmonics N" alone on a line; the `200` field alone on a right-aligned line; a thick bar with no thumb). Consumed: COHESION §0bw, §0cb R-5, §0cc, §0bt. Tree `m/w1-bump-migration`, HEAD at open `1278284`.

**Scope note.** The computed task scoped this seat to §0bw `.h` (line 21) and its three gates. The §0cc addendum (OA-50, the admin panels) also names `.h`. It was **not executed in this seat** and remains owed (see Residual h4).

**Design notes (the idiom).**
- **One control row.** `ui/SliderControl.vue` (`[data-control-row]`) is now the only labelled slider in the app. It is two lines and never three:
  - Line 1 is the glass `Label` (`--type-small`, weight 500, `--foreground`, bound to the field by `for`/`id`). It may carry a mono `token` (`N`, `--type-micro`) or a muted `subtitle` (`--type-caption`, ellipsised, never wrapping). The glass `NumberField` (`sm`) sits at the line end, with an optional `unit` (`ms`).
  - Line 2 is the glass `Slider` directly beneath, with a **visible thumb** and a **track fill**.
  - The rhythm is `--space-atom` inside the row and `--space-body` between rows.
- **A missing primitive, composed meanwhile.** Glass 10.0.1 ships no slider with both a thumb and a fill:
  - The `scrubber` thumb is `width:0; opacity:0` by contract. That is the frame's bar.
  - The `spectrum` thumb is visible, but its `.slider-range` paints nothing.
  - `LabeledSlider` carries no inline value.
  - The row therefore composes through the producer's public surface: `variant="spectrum" size="md"`, with the fill painted on its documented `--glass-slider-track-background` token. The gradient stop is at the thumb centre under reka's `contain` alignment, computed from glass's own `--slider-thumb-size`.
  - Routed to glass BL by id: **BL-FW14H-1** (a slider with a visible thumb and a fill, at a slimmer rung than `md`'s 24 px track) and **BL-FW14H-2** (a labelled slider with an inline value field). These ride the O-59 relay line.
- **One card hierarchy.** Measured, the producer's own section header (glass `ConfiguratorLayer`) is `--type-heading` (25.888 px) in serif at weight 600, with the sub as a `text-micro` mono muted token. Every fourier card title now takes that rung: `CollapsibleSection`, `EquationPanel`, `MorphPhaseConfig`, `HarmonicLevelGrid`. The subtitles and descriptions take `--type-micro` or `--type-caption`, and card insets `--space-family`. Page titles are on the scale too:
  - morph `--type-display-1`;
  - paper `text-display-2` (fluid; the sm/md breakpoint ladder retires);
  - shape-extractor `--type-heading` / `--type-subheading`.
- **Finding (measured).** Under glass's theme bridge (`theme/bridges.css`: `--text-sm: initial; --text-xs: initial`), `text-sm` and `text-xs` size nothing. ⟨probe on the served `/equation`⟩ → `text-sm` 18.608 px = `text-xs` 18.608 px = body. So every `text-sm` label and card title in the app was inheriting the body size, which flattens the hierarchy. The rows this unit touched now name glass rungs (`text-small`, `text-caption`, `text-micro`, `text-heading`). **29 files** use `text-sm` (⟨`grep -rlw text-sm web/src | wc -l`⟩ → 29). Those outside this unit's rows are Residual h3.

**Acts, in order.**
1. **Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → the 21 `docs/tranches/J/audit/screenshots/before/*.png` (`.r`'s, untouched) + `?? .worktrees/`. Nothing was under `web/src/**` or `web/e2e/f-w14-control-row*`, so nothing was inherited.
2. **Census (bytes, pasted).** ⟨`grep -rnE "<(Slider|SliderControl|LabeledSlider)([ >]|$)" web/src`⟩ → 18 hits:
   - `SliderControl` ×8: ContourSettings 5 · FunctionInput 2 · EquationPanel 1.
   - Raw glass `Slider` ×7: BasisSelector 2 · HarmonicLevelGrid 2 · MorphPhaseConfig 1 · EditorControlsDock 1 · GlassTimeline 1.
   - The remaining 3 hits are docblock mentions. The baseline's "BasisSelector 3 / GlassTimeline 2" counted those.
   - Pages and modes that mount a slider: `/equation`, `/morph`, and `/w` image mode (basis + contour + Advanced), `/w` contour edit (magnet popover), and the `/w` Equation overlay. `/`, `/paper`, `/gallery` and `/demo/shape-extractor` mount none; they are framed and read for type only.
   - **GlassTimeline's playhead is outside the idiom**, named in the gate: it is a transport scrubber whose caret carries the value, not a labelled parameter row.
3. **Gate written first:** `web/e2e/f-w14-control-row.spec.ts`. It walks every page and mode above at 1440 and 390 and reads every `role="slider"`:
   - on `[data-control-row]`;
   - label and value field on one line (vertical overlap);
   - track beneath that line;
   - thumb ≥ 4 px wide with opacity > 0;
   - fill (a painted range or a gradient track);
   - row height = the median ± 4 px.

   It also reads h1–h3, card titles and subtitles, and row labels against glass's `--type-*` rungs, and the rows' gaps against `--space-*`. Both scales are resolved in the page from probe elements, not copied. Frames are `e2e/screenshots/f-w14/{before,after}-page-<page>-<w>.png`.
4. **BEFORE, served** (`:3100`, headed, `--workers=1`, HEAD `1278284`). ⟨`FW14_PHASE=before BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-control-row.spec.ts --project=chromium --workers=1 --headed`⟩ → **2 failed**, `rows off the idiom`:

   | vw | rows | on idiom | visible thumb | label+value on one line with the track beneath | heights (px) |
   |---|---|---|---|---|---|
   | 1440 | 15 | 0 | 0 | 7 (equation 2 · contour 5) | 36 (morph ×5) · 56 · 91.9 (magnet) · 104.4 (basis ×2 = the frame) |
   | 390 | 14 | 0 | 0 | 7 | 36 · 56 · 101.4 |

   - The Morph card at the floor read `fill=0`, which is MPC-8's empty capsule.
   - Type BEFORE, from a probe on the same bytes before those files were edited:
     - morph `.demo-title` 32 px (**off** the scale);
     - paper h1 52 px (**off**);
     - morph card titles `text-lg` 18 px (between rungs);
     - shape-extractor h1 = h2 = 18.6 px (flat);
     - every `text-sm` title = body.
   - Instrument corrections made before the recorded run are disclosed:
     - Waiting on a scrubber thumb fails by contract, so the gate waits on the slider root.
     - The canvas docks rest collapsed and are expanded first.
     - The `/w` Equation overlay did not open under the dock toggle in this instrument, so its `Terms` row (the same `SliderControl`) is read by construction.
     - The type reading was widened to h1–h3 after the BEFORE run. Its BEFORE figures are the probe readings above.
5. **Cure** (fourier `ef9dfc6`, pushed; one commit holding the cure, its gate spec and 30 frames).
   - `ui/SliderControl.vue` is rewritten as the idiom (design notes above). `token` and `unit` props were added. The unused default slot and the stale `--slider-range-bg` retint block retired.
   - `BasisSelector.vue`, `HarmonicLevelGrid.vue`, `MorphPhaseConfig.vue` (duration + `unit="ms"` + marks) and `EditorControlsDock.vue` (magnet; the read-only `Metric` retires, and the popover is 12rem on the spacing scale) moved onto `SliderControl`. With them went their local `NumberField`+`Slider` compositions, array-model adapters, dead retint blocks and `useId` label pairs; the idiom owns `for`/`id`.
   - Card and page type:
     - `CollapsibleSection.vue` title → `text-heading`, and its sub → `text-micro font-mono` (no dash);
     - `EquationPanel.vue` title → `text-heading`, rhythm `gap-body`;
     - `FunctionInput.vue` field labels → `text-small` on `--foreground`, rhythm `space-y-body` / `space-y-family`;
     - `FourierMorphDemo.vue` title → `--type-display-1` and lede → `--type-body`;
     - `FourierShapeExtractor.vue` h1/h2 → heading/subheading;
     - `PaperView.vue` h1 → `text-display-2 leading-display`.
6. **Unit checks.** ⟨`npx vue-tsc -b`⟩ → exit 0, no output. ⟨`npx vitest run`⟩ → `Test Files 13 passed (13)` · `Tests 84 passed (84)`.
7. **Regression read.** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/{slider-scrub-contrast,equation-interaction,visualization-ux,settings-persistence,workspace-flow,coarse-pointer,contrast-floor,f-w13-image-controls,f-w13-radius,visual-checkpoint,f-w14-veil,f-w14-dpr,timeline-scrub}.spec.ts --project=chromium --workers=1`⟩ → `10 failed · 38 passed (2.8m)`. That is 6 baseline-RED (contrast-floor `:82`×2 `:128`, visual-checkpoint `:80 :101 :122`) and 4 others:
   - equation-interaction `:77` and visualization-ux `:219` waited on `.slider-subtitle`, the class this unit renamed. They were repointed to `[data-row-sub]` as **adjacent edits**. The re-run of both passed; visual-checkpoint `:163`'s wait site was repointed the same way.
   - f-w14-dpr `:206` was red headless only. Its gate is headed. ⟨`npx playwright test e2e/f-w14-dpr.spec.ts --headed --workers=1`⟩ → `4 passed`.
   - **visual-checkpoint `:163` `checkpoint-disclosure-body.png`** is RED at `26497 px (0.11)`. The diff was read: it is confined to the `/equation` left panel's two cards, meaning the titles on the heading rung, the labels on `text-small`, and the two slider rows on the idiom. This is the owner-ordered render, not a regression. Re-baselining a golden is outside this unit's writable set, so it is **ESC-F14h-1**.
   - The run rewrote the tracked `.p`/`.g` PNGs (`after-dpr-*`, `after-veil-*`). They were restored to HEAD by exact path.

**Gates, BEFORE → AFTER.** Settled bytes `ef9dfc6`, served `:3100`, headed, `--workers=1`, double-run. ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-control-row.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `2 passed (17.1s)` · run 2 `2 passed (17.7s)`. ⟨`python3 -c …test-results/f-w14-control-row-after-{1440,390}.json`⟩ → `1440 15 heights [68] allIdiom True` · `390 14 heights [64] allIdiom True`.

| gate | BEFORE (`1278284`) | AFTER (`ef9dfc6`) |
|---|---|---|
| G-h census: every slider row on the one idiom (label + value on one line, slider beneath, visible thumb + fill) | **RED**: 0/15 on the idiom @1440 and 0/14 @390; 0 visible thumbs | **GREEN** ×2: 15/15 and 14/14 on every limb |
| no control row taller than the idiom's measured height ± 4 px | **RED**: 36 · 56 · 91.9 · 104.4 px (four shapes) | **GREEN** ×2: every row 68 px @1440 and 64 px @390 (spread 0) |
| per page before/after frames at 1440 and 390; card title/subtitle on the type scale; rhythm on the spacing scale | **RED**: morph title 32 px and paper h1 52 px off the scale; card titles 18 px between rungs; `text-sm` sizes nothing | **GREEN** ×2: `offType`/`offSpace` empty on all 7 pages and modes (h1–h3, card titles and subs, row labels and subs; row gaps). Frames: 15 before + 15 after committed (`{before,after}-page-{home,paper,gallery,shape-extractor,equation,morph,image}-{1440,390}` + `image-edit-magnet-1440`) |

**Inherited paths.** None.

**Adjacent edits (§0bt).** Each is the selector of a test oracle whose hook this unit renamed (`.slider-subtitle` → `[data-row-sub]`). No assertion was removed.
- `web/e2e/equation-interaction.spec.ts:68,213`: the mount-condition waits.
- `web/e2e/visualization-ux.spec.ts:203` (comment) and `:240` (wait).
- `web/e2e/visual-checkpoint.spec.ts:189,211`: the disclosure-body locator and wait.

**Residuals.**
- **h1 (glass, routed).** BL-FW14H-1: a slider with thumb and fill (the composition uses `spectrum md`, whose 24 px track is heavier than a slim rung would be). BL-FW14H-2: a labelled slider with an inline value. Both ride O-59 by id.
- **h2 (instrument).** `slider-scrub-contrast.spec.ts`'s docblock still describes the scrubber liquid-fill recipe (88 % tint). The painted fill is now the tint at 100 % on `--muted-medium`, so its asserted floors are conservative and still pass. Its prose is stale for `.u`/`.r` to re-word, outside this unit's set.
- **h3 (type, sweep).** `text-sm`/`text-xs` size nothing under glass's bridge in every file that uses them (29 files by grep). This unit moved the rows and cards it owns. The remaining sites (admin, gallery, paper body, ContourSettings' Select items) belong to `.u`'s UIA-F sweep and to OA-50.
- **h4 (owed).** OA-50 (§0cc, the admin panels' one card/row idiom, which also routes to `.h`) was not in this seat's computed scope and is not executed. It needs its own `.h` sitting (census of every admin page, headed 1440/390 in both themes).

**Escalations.**
- **ESC-F14h-1**: the golden `web/e2e/visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-*.png` needs re-baselining to the OA-45 render. The diff was read: 26 497 px (0.11), confined to the `/equation` panel's card titles, labels and two slider rows. That path is outside `.h`'s writable set. This is the same class as F.W13's ESC-F13a-1 (the §0bm grant). Until it is granted, `:163` joins the named RED set.

### F.W14.u

SEAT `claude-opus-5-5`, 2026-09-23. Spec: `F-W14.md` Units `.u` (:13) + ADDENDUM §0ca (:24-26), read whole; COHESION §0ca–§0cf read. Register: `audit/UI-AUDIT-fourier.md` (256 rows; Appendix B = 74 rows with a glass part).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → only the 21 `docs/tranches/J/audit/screenshots/before/*.png` (`.r`'s) + `?? .worktrees/`; nothing under `web/src` or `web/e2e`. value.js: `LEDGER.md` dirty (a sibling seat's; not touched). **Nothing inherited.**

**Instrument.** Served `http://localhost:3100` (Vite dev of this tree, HMR; API live, read-only GETs + route stubs). One falsifier file `web/e2e/f-w14-uia.spec.ts`, one test per cured row, named by id. Every cure is proven by RED-before (the cure's own diff reverted, the test run, the diff re-applied) then GREEN ×2. Rows whose register entry carries a frame are framed; frameless rows are framed at `web/e2e/screenshots/f-w14/uia/` before their cure.

**Acts (in order).**
1. **UIA-F-1** — `GalleryFeaturedCarousel.vue`: header row (with `CarouselPager`) moved inside `<Carousel>`. ⟨falsifier with a featured fixture, cure reverted⟩ → `1 failed` (strip absent) → ⟨cure applied ×2⟩ → `1 passed` ×2. fourier **`a72dd1e`**.
2. **UIA-F-2 + UIA-F-3** — `useWorkspaceLoader.ts`: `loadFromRoute` dispatches on `route.name` (`visualization` → `loadVisualization`, `workspace` → `loadWorkspace`), watcher keyed on name+both slugs, acts only on its own two routes. ⟨reverted⟩ → F-2 `waitForResponse` timeout (GET never sent) · F-3 URL `…/w/cosmic-edging-nutmeg-horse` (bounced) → ⟨applied ×2⟩ → `2 passed` ×2, 0 pageerrors (the orphaned View Transition was the bounce's; no `.catch` shim added). fourier **`8e17213`**.
3. **UIA-F-50 + UIA-F-4 + UIA-F-49** — new `components/shared/NotFoundCard.vue` (glass Card family + Button, `h1`); router last route `/:pathMatch(.*)*` → it, `meta.noindex` → `<meta name=robots content=noindex>` (removed on leave); `VisualizationView` error branch → the same card, route-aware copy naming the slug, Tooltip dropped, action labelled "Upload a new image". ⟨reverted⟩ → `3 failed` → ⟨applied ×2⟩ → `3 passed` ×2. vue-tsc exit 0. fourier **`1a632a2`**.
4. **UIA-F-7** — `AnimationControls.vue`: the collapsed readout is glass `Progress` (`size="sm"`, `modelValue=anim.t`, `max=1`, aria-label) inside the same render boundary; the scoped `.mini-fill` rule deleted. Served probe after: `role=progressbar`, `aria-valuenow 0.33`, indicator 48×8 px painted `rgb(28,25,23)`. ⟨reverted⟩ → `1 failed` (no progressbar) → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`3098e6d`**.
5. **UIA-F-17** — `BasisCanvas.vue`: `FrameLayers {epicycles, trail}` threaded into `drawEpicycleFrame`/`drawMultiBasesFrame` (default all-on for the live draw); `exportFrame` reads `withEpicycles`/`withTrail`. Falsifier exports paused frames through the real dialog and counts painted pixels. ⟨reverted⟩ → `{all 41125, noChain 41122, noTrace 41125, again 41125}` → removal 3 px vs floor 411 → `1 failed` → ⟨applied⟩ → `{all 41189, noChain 33834, noTrace 38341, again 41183}` → `1 passed` ×2. fourier **`1623166`**.
6. **UIA-F-16** — `ContourEditorCanvas.vue`: `onPointerUp` wraps the raw `usePointDrag` handler and calls `emitState()`. ⟨reverted⟩ → Undo `disabled` after a drag (`Expected false · Received true`) → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`7dd8a10`**.
7. **UIA-F-15** — `ContourEditorCanvas.vue`: shortcuts on `@keydown` of the focusable `.editor-shell` (window listener + `onMounted/onUnmounted` deleted; a picked point focuses the shell, since the drag's `preventDefault` suppresses the focus move); `VisualizationView.vue`: the opacity-hidden editor wrapper is `inert` outside edit mode. ⟨reverted⟩ → after leaving edit mode and pressing Backspace in a sidebar spinbutton, point count `Expected 1023 · Received 1024` (the hidden editor's state moved under a sidebar keystroke) → ⟨applied ×2⟩ → `1 passed` ×2 (Delete still deletes a picked point in edit mode; the field keeps its Backspace; wrapper `inert`). fourier **`7958cfa`**.
8. **UIA-F-18** — `VisualizationView.vue` `handlePublish`: `if (publishing.value …) return`; a null save toasts only a real `store.error`; `workspace.ts` `saveVisualization` clears `error` at its start (so an abort is not a failure). ⟨reverted⟩ → dblclick sends `2` POSTs (`Expected 1 · Received 2`) → ⟨applied ×2⟩ → `1 passed` ×2 (one POST, "Published!", 0 "Could not save"). fourier **`34d8297`**.
9. **UIA-F-19** — the overlay's redundant `@drop` removed (`VisualizationView.vue`); `useImageUpload.handleDrop` leaves an already-handled drop (`defaultPrevented`) to its inner owner; `workspace.uploadImage` re-entrancy guard. ⟨reverted⟩ → synthetic drop over a loaded image → `2` POST `/api/images` → ⟨applied ×2⟩ → `1` ×2. fourier **`f514500`**.
10. **UIA-F-20** — `PaperView.vue`: the saved section id is read synchronously in setup (`restoreSectionId`) before the `immediate` persisting watcher removes the key; `onMounted` restores from it. ⟨reverted⟩ → after reload `.paper-scroll` scrollTop `0` → ⟨applied ×2⟩ → `> 1000`, saved section in view, `1 passed` ×2. fourier **`d35ab92`**.
11. **UIA-F-24** — `MobileFloatingToc.vue`: `.floating-toc-row` = navigate button (`selectSection`) + a separate icon Button (`aria-label "Subsections of …"`, `aria-expanded`) that toggles. ⟨reverted⟩ at 390/touch → tapping the last chapter left the dropdown open (`Expected 0 · Received 13` rows) → ⟨applied ×2⟩ → dropdown closes, scroller advances > 2000 px, disclosure flips `aria-expanded`; `1 passed` ×2. fourier **`f2b99a8`**.
12. **UIA-F-31** — `equation/lib/harmonics.ts` gains `revealWeight(j, cursor, blend, n)` (1 everywhere once `cursor ≥ n−1`); `ConvergencePlot.vue` uses it. Falsifier = vitest `src/components/equation/lib/harmonics.test.ts`. ⟨`node -e` the old expression at j=c=N−1, BLEND 10⟩ → `0.0909` (RED) → ⟨`npx vitest run …harmonics.test.ts` ×2⟩ → `Tests 2 passed (2)` ×2. fourier **`427d69e`**.
13. **UIA-F-36** — `AdminUserList.vue`: `BatchActionBar` mounted after the list `Card`. ⟨reverted⟩ → ticking a row moved it `68` px → ⟨applied ×2⟩ → 0 px, bar in viewport below the rows; `1 passed` ×2. fourier **`3da9ce0`**.
14. **UIA-F-37** — `AdminUserList.vue`: toolbar `flex-wrap`, search `basis-full sm:basis-0 sm:flex-1`, SelectTrigger literals dropped. ⟨reverted⟩ at 390 → search width `42` px → ⟨applied ×2⟩ → > 300 px, no control past the viewport, no literal classes; `1 passed` ×2. fourier **`4655d6e`**.
15. **O-63 relay** — `docs/tranches/X/relay/X-ALL-BK-UI-AUDIT-ADDENDUM-2026-09-23-FW14U.md` (dated addendum beside O-59, E-3) + INBOX row `O-63`: 48 pure-GLASS rows ROUTED under O-59; 26 split rows' glass halves under O-59; **UIA-F-51 relay-only, blocking (I-44), 0 local override** (⟨`grep -rn "Toaster\|toast-viewport\|VIEWPORT" web/src --include=*.css`⟩ at the unit's end, below); plus `.t`'s R-t-1 and `.h`'s BL-FW14H-1/2 (not in O-59 before). Not mirrored into glass-ui (READ-ONLY to this seat): owed to the next BK mirror act. value.js **`a64e7316`**.
16. **UIA-F-42** — already cured at cause by `.t` (`b5a650f`, DataTable); falsifier added: at 390 every audit cell width > 0, one badge per row, 0 badges overflowing. ⟨×2⟩ → `1 passed` ×2. fourier **`96b66ad`** (test only).
17. **UIA-F-48** (consumer half) — `GalleryDraftsSection.vue`: `px-4` wrapper replaces `mx-4` on the `Collapsible`. ⟨reverted⟩ → card right `1296` > column−15 `1265` → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`191c86d`**.
18. **UIA-F-41** (consumer half) — `GalleryView.vue`: `SegmentedTabs` inside its own `min-w-0 overflow-x-auto` track. (First attempt `overflow-x-clip` on the column was measured and rejected: with `overflow-y:auto` the clip computes to `hidden`, which focus still scrolls.) ⟨reverted⟩ at 390 → column `scrollLeft > 0` after focusing the last tab → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`b803a32`**.
19. **UIA-F-45** — `GalleryCardModal.vue`: `@open-auto-focus="focusTitle"` (prevent + focus the `data-initial-focus` title), replacing the bare `.prevent`. ⟨reverted⟩ → focus not inside the dialog (`Expected true · Received false`) → ⟨applied ×2⟩ → focus on the title, 6 Tabs stay in the dialog; `1 passed` ×2. fourier **`73d823b`**.
20. **UIA-F-40** — `GalleryView.vue` `onMounted`: `Promise.all([resetAndFetch, refreshDrafts, activateAdmin→replace])`, grid first. ⟨reverted, stats delayed 3 s⟩ → grid card not visible within 2.5 s → ⟨applied ×2⟩ → `1 passed` ×2. fourier **`5a92498`**.
21. **UIA-F-47** — `gallery.publishDraft` writes the created slug into the draft's `savedSnapshots` (`saveDraft`); `GalleryView` lists only drafts with no snapshot and refreshes after publish (the dead `publishedHashes` removed); `workspace._saveDraftNow` carries the loaded draft's snapshots (`draftSnapshots`, reset on a new upload). ⟨reverted⟩ → the published row stayed (`Expected 0 · Received 1`) → ⟨applied ×2⟩ → the row is gone, and still gone after reload; `1 passed` ×2. fourier **`26c216a`**.
22. **UIA-F-33 + UIA-F-34 + UIA-F-32** — `EquationResult.vue`: a header row (`#leading` slot · `#actions` slot + Copy, `gap 0.5rem`, `margin-inline-start:auto`); `.copy-pos` absolute layer and the `2rem` dodge padding deleted. `EquationView.vue`: the Σ|a+b toggle → `#leading`, the Info Popover → `#actions`; `.eq-mode-anchor`/`.info-anchor` (the `right:3.25rem` literal) deleted; `.eq-card` `height:10rem; overflow:hidden` → `min-height:10rem`. Served probe after: 1440 Info x1334–1374 · Copy x1382–1422 (8 px apart), equation top 129 > controls bottom 127; 390 Info 288–328 · Copy 336–376, equation top 170 = controls bottom. ⟨reverted⟩ → F-33/34 `control 0 over the equation` at 1440 and 390; F-32 popover clipped (visible fraction < 0.99) → ⟨applied ×2⟩ → `3 passed` ×2. fourier **`1c7a9c4`**.
23. **UIA-F-12** — `CanvasControlsDock.vue:70` and `EditorControlsDock.vue:160, :191`: `Popover trigger="hover"` → `trigger="click"` (View options; the editor's magnet slider and view toggles). ⟨reverted⟩ → three Tabs after Enter reach `["Edit contour","Pause animation","Timeline"]` → ⟨applied ×2⟩ → Tab reaches Contour trace + Image overlay, and Enter toggles `aria-pressed`; `1 passed` ×2. fourier **`57d880d`**.
24. **UIA-F-5** — `VisualizationView.vue`: the dock anchor is right-anchored at every width; the `dock-centered` re-seat (and its transition) is deleted. ⟨reverted⟩ at 390 → `Equation is hittable: false` → ⟨applied ×2⟩ → every expanded-dock control's centre hits itself, in view; `1 passed` ×2. fourier **`f6894c3`**.
25. **UIA-F-28 + UIA-F-29 + UIA-F-30** — `UserSlugBar.vue` template rewritten onto the dock's transient families: logged out → `Popover` + `DockTrigger for="popover"` ("Log in") holding a `<form>` (label, Input, the error `<p role=alert>` on its own wrapping line, Generate + Submit); logged in → `DropdownMenu` + `DockTrigger for="dropdown"` (`Account: <slug>`; abbrev `sm:` only, since the full slug is in the menu label, reachable by touch) holding the slug, "Copy your slug" and "Log out". Every accessible name the earlier rows fixed is kept; `e2e/gallery.spec.ts` login oracle ×2 → `1 passed` ×2. ⟨reverted⟩ at 390 → the slug field was not hittable (F-28); the dark-mode toggle was off the plate (F-30, `{inView,hit}` ≠ `{true,true}`) → ⟨applied ×2⟩ → `2 passed` ×2 (the error `scrollWidth ≤ clientWidth`, right edge ≤ 390, dock width unchanged by the open form). fourier **`fb8b5df`**.
26. **UIA-F-22** — `PaperView.vue`: `isDesktop = useMediaQuery("(min-width: 1024px)")`; `PaperSidebar v-if="isDesktop"`, `MobileFloatingToc v-if="!isDesktop && …"`. ⟨reverted⟩ at 390/touch → `31` duplicate `paper-search*` ids → ⟨applied ×2⟩ → 0 duplicates, one listbox, a tapped result scrolls; `1 passed` ×2; `paper-search.spec.ts` `3 passed`. fourier **`9a7cf7d`**.
27. **UIA-F-23** — `PaperSearch.vue` gains a `boundary` prop (defaults to its root); `MobileFloatingToc.vue` passes its search bar. BEFORE (measured at the F-22 tree, pre-cure) → `Close search` still present after one tap → ⟨applied ×2⟩ → closed and stays closed; `paper-search.spec.ts` `3 passed`. fourier **`e87d203`**.

**Disposition table (all 256 UIA-F rows).** The counts were computed by script from the register's owner column, Appendix B and this receipt's commits (⟨`python3` over `uia-own.txt` · `glass-only.txt` · `glass-split.txt`⟩ → total `256`). Every class is listed by id.

| class | n | rows · evidence |
|---|---|---|
| **CURED** at cause, falsifier RED-before → GREEN ×2 | **31** | F-1 `a72dd1e` · F-2, F-3 `8e17213` · F-4, F-49, F-50 `1a632a2` · F-5 `f6894c3` · F-7 `3098e6d` → re-cured `ec3fb0b` (act 28) · F-12 `57d880d` · F-15 `7958cfa` · F-16 `7dd8a10` · F-17 `1623166` · F-18 `34d8297` · F-19 `f514500` · F-20 `d35ab92` · F-22 `9a7cf7d` · F-23 `e87d203` · F-24 `f2b99a8` · F-25 `ef3c124` · F-28, F-29, F-30 `fb8b5df` · F-31 `427d69e` · F-32, F-33, F-34 `1c7a9c4` · F-36 `3da9ce0` · F-37 `4655d6e` · F-40 `5a92498` · F-45 `73d823b` · F-47 `26c216a` |
| **SPLIT**: consumer half CURED, glass half ROUTED (O-59) | **2** | F-41 `b803a32` · F-48 `191c86d` |
| **CURED-BY-.t** (cited, not re-cured) | **2** | F-42 `b5a650f` + this unit's falsifier `96b66ad` (GREEN ×2) · F-108 `b5a650f` (the audit ledger is DataTable; falsifier = `.t`'s G-t spec) |
| **CURED-BY-.h** (cited, not re-cured) | **2** | F-75 (one slider-row idiom), F-116 (/morph scrubbers get a thumb): `ef9dfc6`, falsifier = `.h`'s G-h census spec |
| **ROUTED-GLASS** (pure GLASS, O-59; now also O-63) | **48** | F-6 8 10 11 13 27 38 43 44 **51** 56 58 72 78 84 90 91 100 122 123 125 126 127 129 131 134 135 138 139 140 141 142 143 145 215 216 217 218 220 221 222 223 224 225 226 227 228 230 |
| **SPLIT**: glass half ROUTED (O-59), consumer half **OPEN** | **24** | F-57 60 77 80 85 88 94 105 106 107 124 128 130 132 133 136 137 144 146 147 148 149 219 229 |
| **ROUTED-SERVER** (the cause is outside `web/src`, this unit's writable set) | **3** | F-35 (`src/fourier_analysis/symbolic/simplification.py`) · F-83 (`latex_rendering.py`) · F-112 (SERVER) |
| **OPEN**, server-dependent | **1** | F-39: the q/tier/basis filters need `api/routers/visualizations.py`, which is outside `web/src`. The register's alternative, deleting the controls, is a product decision, not a cure. |
| **OPEN**, consumer, not reached this sitting | **143** | BROKEN F-9 14 21 26 46, and HIGH, MEDIUM and LOW by id in the block below |

Check: 31+2+2+2+48+24+3+1+143 = **256** ✓.

**The 143 OPEN consumer rows, by severity.** (The split rows' consumer halves and F-39 are listed in the table above.)
- BROKEN (5): F-9 F-14 F-21 F-26 F-46
- HIGH (47): F-52 F-53 F-54 F-55 F-59 F-61 F-62 F-63 F-64 F-65 F-66 F-67 F-68 F-69 F-70 F-71 F-73 F-74 F-76 F-79 F-81 F-82 F-86 F-87 F-89 F-92 F-93 F-95 F-96 F-97 F-98 F-99 F-101 F-102 F-103 F-104 F-109 F-110 F-111 F-113 F-114 F-115 F-117 F-118 F-119 F-120 F-121
- MEDIUM (65): F-150 F-151 F-152 F-153 F-154 F-155 F-156 F-157 F-158 F-159 F-160 F-161 F-162 F-163 F-164 F-165 F-166 F-167 F-168 F-169 F-170 F-171 F-172 F-173 F-174 F-175 F-176 F-177 F-178 F-179 F-180 F-181 F-182 F-183 F-184 F-185 F-186 F-187 F-188 F-189 F-190 F-191 F-192 F-193 F-194 F-195 F-196 F-197 F-198 F-199 F-200 F-201 F-202 F-203 F-204 F-205 F-206 F-207 F-208 F-209 F-210 F-211 F-212 F-213 F-214
- LOW (26): F-231 F-232 F-233 F-234 F-235 F-236 F-237 F-238 F-239 F-240 F-241 F-242 F-243 F-244 F-245 F-246 F-247 F-248 F-249 F-250 F-251 F-252 F-253 F-254 F-255 F-256

**Gate readings (BEFORE → AFTER).**

| gate | BEFORE | AFTER |
|---|---|---|
| G-u: every UIA-F row CURED with a falsifier GREEN ×2, or ROUTED-GLASS by id | **RED**: 256 open | **RED (PARTIAL)**: 88 of 256 dispositioned (37 consumer rows cured: 31 here, 2 split consumer halves here, 2 by `.t`, 2 by `.h`; 48 ROUTED-GLASS; 3 ROUTED-SERVER). 168 are still owed: 143 OPEN, 24 split consumer halves, F-39. |
| every frameless row framed on the served page BEFORE its cure | n/a | **GREEN (vacuous for the rows cured)**: every row cured here carries a register frame in its Frame column (checked row by row against `UI-AUDIT-fourier.md` before each cure), so no new BEFORE frame was owed. The served-page BEFORE reading of each cure is its RED-before falsifier run, recorded in its act. |
| UIA-F-51 relay-only, 0 local override | relay O-59 · I-44 | **GREEN**: ⟨`grep -rn "Toaster\|toast-viewport\|data-slot=\"toast" web/src`⟩ → only `App.vue:5` (import) and `App.vue:146` `<Toaster />` (no prop, class or style). The row is carried in O-63 as blocking. |
| falsifier file `web/e2e/f-w14-uia.spec.ts` | none | ⟨`npx playwright test e2e/f-w14-uia.spec.ts --project=chromium --workers=1` ×2⟩ → `32 passed (1.1m)` · `32 passed (1.2m)` |
| typecheck | exit 0 | ⟨`npx vue-tsc -b`⟩ → exit 0 |
| unit tests | 84/84 (13 files) | ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` (+ `harmonics.test.ts` 2) |

A measurement note: an earlier ×2 run at `--workers=2` read `31/32` and then `29/32`. The two extra reds were `firstSavedViz` → `res.ok() false`, the API's read rate limit (`ratelimit-limit: 180`, `remaining 27` read by `curl -D -`). The third was F-17's pointer pause, discarded mid-morph (UIA-F-8, a glass row). The fix was in the instrument, not the product: the lookup is cached per worker and F-17 pauses by keyboard. That is `c815101`. No assertion was weakened.

**Inherited paths.** None.

**Adjacent edits (§0bt).** `web/e2e/f-w14-control-row.spec.ts:245`: `.h`'s G-h opened the editor's magnet popover with `.hover()`. UIA-F-12 moved that popover to `trigger="click"`, so the oracle's interaction follows it (`.click()`). No assertion is changed. fourier `0f94a2f`. The one new unit test, `web/src/components/equation/lib/harmonics.test.ts`, is inside `web/src/**`.

**Residuals.**
- **u1 (G-u, the bulk).** 168 rows are still owed: 143 OPEN consumer rows (listed above), 24 split consumer halves and F-39. This seat cured 33 rows at cause. Each has a falsifier proven RED against its own reverted diff and GREEN ×2. The owed rows need further `.u` sittings, working the register BROKEN (F-9 14 21 26 46) → HIGH → MEDIUM → LOW.
  - **F-9** (the 390 More-options menu) is to be resolved by F-81, which moves speed and easing to the animation pane. That is a HIGH design row.
  - **F-14** (fullscreen as a second, deaf editor) needs the single live stage moved into the takeover.
  - **F-21** (raw LaTeX in search labels) needs KaTeX-rendered labels whose fuzzy highlight marks only text segments.
  - **F-26** (theorem result → wrong chapter): the register pins no root cause. Tracing `thm.label` → `labelMap.sectionId` is the next probe.
  - **F-46** (Like is not a toggle): there is no like endpoint in `api/routers`, so the server half routes with it.
- **u2 (server rows).** F-35, F-83 and F-112, plus F-39's API half, need `src/fourier_analysis/**` or `api/**` writes. Those are outside `.u`'s writable set (`web/src/**`). They are routed to the next X·F wave's server unit, not escalated as a bound breach.
- **u3 (instrument).** The live API's read rate limit (180 per window) is a shared instrument constraint for any served-page spec that reads real rows. Specs that read real rows should cache per worker, or run at `--workers=1`.
- **u4 (h2, handed to .u by .h).** `slider-scrub-contrast.spec.ts`'s stale docblock was not re-worded here. It is prose-only and is left for `.r`.
- **u5 (relay mirror).** O-63 is written value.js-side only. glass-ui is read-only to this seat, so the BK/coordination mirror is owed to the next mirror act.

**Escalations.** None. No specified cure was impossible at the bytes. The unfinished rows are a PARTIAL for scope, not a blocked cure.

**Late acts.**
28. **UIA-F-7 re-cure (self-caught regression).** A non-regression run of the sibling gates, ⟨`npx playwright test e2e/f-w14-admin-table.spec.ts e2e/f-w14-control-row.spec.ts e2e/gallery.spec.ts e2e/equation-interaction.spec.ts --workers=1`⟩, read `1 failed · 21 passed (5.9m)`. The failure was `.h`'s G-h @1440 `settle()`, which timed out at 300 s on the image page. Bounded probe ⟨`document.getAnimations()` running, finite, on `/v/<slug>`⟩ → `["CSSTransition:transform@progress-value-fill"] 1`. Glass `Progress`'s fill carries a `transform` transition, and the 60 fps clock restarts it every frame. **Cure:** the readout keeps its own div fill, styled by `.mini-progress :deep(.mini-fill)` from the scoped root (the root carries the scope; the render-function child does not), with `role=progressbar` (0..1, `aria-valuetext` %). No producer selector is touched. ⟨probe again⟩ → `[] 0`. ⟨the `:deep` rule removed⟩ → F-7 `painted Expected > 0 · Received 0` (RED) → ⟨restored ×2⟩ → F-7 + F-17 `2 passed` ×2. fourier **`ec3fb0b`**.
29. **Non-regression, after `ec3fb0b` and `0f94a2f`.**
    - The `.h` G-h spec was re-run twice: ⟨`npx playwright test e2e/f-w14-control-row.spec.ts --workers=1` ×2⟩ → `2 passed (16.4s)` · `2 passed (17.2s)`.
    - The other three sibling specs were run once, in the act-28 sitting. That run read 21 passed. The `admin-table` (`.t`), `gallery` and `equation-interaction` specs are all among the 21.
    - The UIA falsifier file was re-run twice: ⟨`e2e/f-w14-uia.spec.ts --workers=1` ×2⟩ → `32 passed (1.1m)` · `32 passed (1.1m)`.
    - Prior evidence was restored (E-3). Running the sibling specs rewrote 27 of `.t`'s and `.h`'s committed `web/e2e/screenshots/f-w14/after-*.png` frames. Each rewritten frame was restored to HEAD with ⟨`git checkout --` on the exact 27 paths⟩. No frame of theirs is changed.
    - Result: ⟨`git status --porcelain web`⟩ → clean.
    - The 21 `docs/tranches/J/…/before/*.png` files were dirty before this unit started. They belong to `.r` and were not touched.

**E13.** This is a Step-0 read at close, over the same four paths as the Open. It found one new glass-side file, `BK/coordination/chicago-inbound-2026-09-23-dock-live-veil-and-hover-lens.md`. That letter is from another consumer to glass, so it is not in F.W14 scope. It found no glass outbound letter to value.js or fourier. The result is **0 UNREAD in scope**. O-63 is rowed.

### F.W14.r

SEAT `.r`, `claude-opus-5-5`, 2026-09-23. Spec: `F-W14.md` Units `.r` (:14) + ADDENDUM (c) §0cf (:49) + ADDENDUM (d) §0cj (:52); F.W13 Close §5 residuals (`execution/C/F-W13.md:395-405, 797`); `.u` hand-off u4 (this record `:500`). Fourier start HEAD `0f94a2f`.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → the 21 ` M docs/tranches/J/audit/screenshots/before/*.png` (this unit's own residual, run-dirtied by prior e2e runs, not a predecessor seat's edit) + `?? .worktrees/`. Nothing under `web/src/**` or `web/e2e/**` dirty. Nothing inherited.

**Anchors at the true bytes.**
- `PaperView.vue:331` (brief) drifted → the ToC model's `scrollToTop: () => scrollToTop()` is at `PaperView.vue:345`; `MobileFloatingToc.vue:22` injects it and `:73-76` calls it. INTENT kept: the desktop control calls the same model member.
- `style.css:346` (baseline) drifted → the `.sidebar-link` corner note is `style.css:322-326`; the corner itself lives at `PaperSidebar.vue:358` (`calc(var(--radius) - 2px)` = 8 px).
- The canon, read at the installed glass 10.0.1: ⟨`grep -o 'interactive-item[^"]*' dist/rowClass-*.js`⟩ → `interactive-item glass-menu-row …`; ⟨`dist/styles/utilities/base.css`⟩ → `.interactive-item{…border-radius: var(--radius-lg)…}`; `radius.css` → `--radius-lg: var(--radius)` = 10 px. Glass's row idiom is `--radius-lg`.
- `visual-baseline.spec.ts:30-35` wrote to `../../docs/tranches/J/audit/screenshots/{before,after}` by default; `visual-checkpoint.spec.ts:16` is prose only (it names that sink; it writes nothing there). ⟨`grep -rln 'tranches/J' web/e2e`⟩ → those two files only.
- `.c` r3 at 10.0.1: ⟨python read of `dist/configurator-DiwVN573.js`⟩ → the render is `d("aside", H, [...])` with no `v-if` guard; the aside is always in the DOM at the pin too.
- `.c` r4 at Playwright 1.61.0: ⟨`grep -n reducedMotion node_modules/playwright/types/test.d.ts`⟩ → it appears only in the `contextOptions` docblock (`:7538`); `PlaywrightTestOptions` (`:7064`) has no `reducedMotion` key. A bare `test.use({ reducedMotion })` is dropped, which is the measured cause of F.W13's "did not reach the page".

**Acts, in order.**
1. **`.a` r1 — the desktop ToC's scroll-to-top** (`PaperSidebar.vue`). The CONTENTS header now holds a `.sidebar-header-actions` group: a glass `Button emphasis=quiet size=md icon-only` (`aria-label="Scroll to top"`, `ArrowUpToLine`, Tooltip "Scroll to top") that calls the injected model's `scrollToTop()`, then the untouched CONTENTS disclosure. It sits beside the disclosure, not in its seat. Follow: its pointerdown sets latex-paper's `manualOverride`, and the paper scroll it causes clears it (F.W13 `.a` read, `vue.js:583-586`). No exemption is needed.
2. **`.a` r2 — `.sidebar-link` radius on the canon** (`PaperSidebar.vue:358`): `calc(var(--radius) - 2px)` → `var(--radius-lg)`, 8 px → 10 px. The `style.css:322-326` note now names the token. `.floating-toc-item` (`--radius-md`) is not named by the residual and was left alone.
3. **`.c` r1 — the sidebar enters on the pick** (`VisualizationView.vue`). `hasSidebar = hasImage || store.uploading`, so the sidebar enters when the upload starts, which is the pick or drop itself. The Image layer's `Progress` (`ImageUpload.vue`) is now also shown while `store.uploading`, named "Uploading the image", so the layer is not empty in flight.
4. **`.c` r2 — the leave path, now reachable and gated.** A failed upload ends the flight with no image, so `hasSidebar` goes false. That failure also swaps the workspace for the not-found card, which unmounts the Configurator under the leave, so `@after-leave` never fires and `sidebarPresent` stayed `true`. Measured falsifier: with the hook removed, "Upload a new image" re-mounted a **400 px empty aside column**. The cure is `@vue:unmounted="onConfiguratorUnmounted"` on the Configurator, which re-reads the band from `hasSidebar`. Band after = `0`.
5. **`.c` r3 — RULED, a glass ask by id: `CONFIGURATOR-ASIDE-UNGUARDED`.** The producer renders `<aside class="configurator-aside">` unconditionally at 10.0.1 (anchor above). The consumer keeps its lawful handling: no sidebar content, and `data-sidebar="none"` collapses the band to 0 (G-c2 reads `0`). A slot-guarded aside is a producer change. No local override was made. The relay letter is outside this unit's writable set, so the id goes to the relay/COHESION seat for the next O-59 addendum (residual R-r-1).
6. **`.c` r4 — CURED at the instrument.** PRM is now spelled `test.use({ contextOptions: { reducedMotion: "reduce" } })` in `f-w14-residuals.spec.ts` G-c4 and, as an adjacent edit, in the F.W13 PRM describe. Its `page.emulateMedia` workaround and false comment were retired, and its `matchMedia` assertion was kept.
7. **J-audit writers re-pointed and the PNGs restored, in one act.** `visual-baseline.spec.ts` now writes by default to `web/test-results/visual-baseline/{before,after}`, which is git-ignored at `.gitignore:84`. `VISUAL_OUT` still re-points it. The `visual-checkpoint.spec.ts:16` prose was updated. ⟨`git checkout HEAD -- <the 21 paths from git status --porcelain docs/tranches/J/>`⟩ → `21` paths, and then ⟨`git status --porcelain docs/tranches/J/ | wc -l`⟩ → `0`. Restoring to HEAD leaves no byte diff, so commit `a54f71d` carries the re-point and the restore is the same act.
8. **§0cf — dock-plate composite re-measured** (served `/paper`, 1440, headless probe `scratchpad/fw14r/dockprobe.mjs`). Light: `.dock-plate` bg `color(srgb 0.204 0.148 0.083 / 0.1)` with backdrop `blur(16px) saturate(1.2)` over body `rgb(251,250,248)`, which composites to ≈ `rgb(231,229,225)`, a warm grey. Dark: `/ 0.14` ink over `rgb(11,10,9)`. **GLASS-VEIL-GREY stands honest-RED** (O-62). The pin is unchanged at 10.0.1, and there is no local override.
9. **§0cj — DOCK-TRIGGER-CLIP read** (same probe, light and dark). `.dock-layer--full` has `overflow: auto hidden` and `padding-block: 4px 4px`. Row 48 px, trigger 40 px, insets 4/4, 4 triggers. The trigger focus ring is `solid 2px`, offset `2px`, so it extends exactly 4 px and fits flush. The hover capsule on the brand trigger is uncut in `dock-hover-light.png`. Clipping ancestors (⟨`dockchain.mjs`⟩): the glass row, and the app shell `div.h-dvh` (`hidden/hidden`), which lies well outside the dock and cuts nothing. There is no fourier consumer clip to cure. **DOCK-TRIGGER-CLIP recorded honest-RED** per §0cj: the producer structure that cuts any capsule wider than 4 px is present at the pin, with no local override.

**Commits** (fourier `m/w1-bump-migration`, pushed; ⟨`git log --oneline -1 origin/m/w1-bump-migration`⟩ → `a54f71d`): `4a87666` (`.a` r1+r2) · `7808c65` (`.c` r1+r2) · `0966631` (gates + `.c` r4) · `a54f71d` (the J-audit re-point together with the 21-PNG restore, which is one commit per the lock).

**Falsifiers.** ⟨the four cured sources swapped to `0f94a2f` bytes from HEAD, then `npx playwright test e2e/f-w14-residuals.spec.ts --workers=1`, then restored byte-exact from scratchpad⟩ → `4 failed`: G-r1 `Scroll to top` count `0`; G-r2 `8px` vs canon `10px`; G-c1 `.viz-panel-left-wrap` not found in flight; G-c2 not found in flight. G-c4 is an instrument read and is green by construction. Removing only `@vue:unmounted` makes G-c2 fail with band `400` (expected `0`).

**Gates, BEFORE → AFTER** (served `:3100` = vite on the working tree, `--workers=1`).

| gate | BEFORE | AFTER |
|---|---|---|
| G-r1 desktop ToC scroll-to-top, glass Button beside the disclosure, acts | absent (count 0) | ⟨`f-w14-residuals.spec.ts`⟩ **5/5 ×2** (3.6 s · 2.9 s): `data-slot=button`, same parent as the `Contents` trigger, paper `scrollTop` >1000 → <2, disclosure stays `aria-expanded=true` |
| G-r2 `.sidebar-link` radius on the canon (computed) | `8px` | **`10px` = computed `var(--radius-lg)`** ×2 |
| `.c` r1 enters on the pick | sidebar absent in flight | **present in flight**, "Uploading the image" progressbar, band ≥ 300 px before `/w/<slug>` ×2 |
| `.c` r2 leave path gated | ungated | **gated** ×2: a failed upload leaves `.viz-panel-left-wrap` count 0, and after "Upload a new image" the aside band is `0` |
| `.c` r3 | open | **RULED**: glass ask `CONFIGURATOR-ASIDE-UNGUARDED` (R-r-1) |
| `.c` r4 PRM | emulated per page | **CURED**: `contextOptions.reducedMotion` in force (G-c4 ×2); F.W13 PRM describe `3/3 ×2` |
| J clean after full e2e ×2 | 21 ` M` | ⟨`git status --porcelain docs/tranches/J/ \| wc -l`⟩ after run 1 → **0**, after run 2 → **0**; ⟨`ls web/test-results/visual-baseline/before \| wc -l`⟩ → **21** (the captures land in the sink) |
| vue-tsc | 0 | ⟨`npx vue-tsc -b`⟩ **exit 0 ×2** |
| vitest | 84/84 (13 files) | ⟨`npx vitest run`⟩ **14 files / 86 passed ×2** (the 14th file came with `.u`) |
| full e2e `--workers=1` ×2 | named baseline set | run 1 `13 failed · 3 skipped · 159 passed (6.3m)`, load 31.05 → 29.97 · run 2 `14 failed · 3 skipped · 158 passed (7.2m)`, load 29.97 → 50.25 |

**The e2e REDs, read by id.** Both runs are all named or banked, with nothing new from this unit.
- contrast-floor `:82`×2 and `:128`, gallery-admin-a11y `:91 :103 :114 :125`, and fullscreen `:40` (run 2): the baseline set.
- visual-checkpoint `:81 :102 :123`: the baseline `:80 :101 :122`. They moved by +1 line from this unit's prose edit at `:16-17`.
- visual-checkpoint `:164`: `.h`'s ESC-F14h-1 golden (`/equation`, 0.11).
- f-w14-dpr `:206` DPR 1 image (run 1): `.h`'s banked headless-only red. ⟨`--headed` ×2⟩ → `4 passed` ×2.
- f-w14-uia `:166` UIA-F-17 (run 2): a `locator.hover` 30 s timeout at load 50, the `G-F12-3-LOAD` class. ⟨isolated ×2⟩ → `1 passed` ×2.

**Adjacent edits (§0bt).**
- `web/e2e/f-w13-image-empty.spec.ts:114-122`: `.c` r4's own site. The PRM describe now uses `contextOptions`, and its `page.emulateMedia` workaround and false comment are gone. The `matchMedia` assertion is kept.
- `web/src/components/visualization/ImageUpload.vue:141-150`: the r1 cure's in-flight busy state on the sidebar's Image layer. It is the same concern as the sidebar enter, and the `Progress` condition and name widened.

**Evidence restored (E-3).** The two full runs rewrote 35 committed `web/e2e/screenshots/f-w14/after-*.png` frames (`.t`/`.g`/`.p`/`.h`), all clean at this unit's open. ⟨`git checkout HEAD -- <the 35 exact paths>`⟩ → fourier porcelain = `?? .worktrees/` only. This is the same act `.u` recorded.

**Residuals.**
- **R-r-1 — `CONFIGURATOR-ASIDE-UNGUARDED`** (glass ask, owner: the relay/COHESION seat). This unit may not write `relay/`. The id rides the next O-59 addendum: glass `Configurator` renders its `<aside>` unconditionally at 10.0.1, and the ask is a slot-guarded aside, or one that takes no box when `#controls` is empty.
- **R-r-2 — GLASS-VEIL-GREY honest-RED** (O-62), re-measured as act 8.
- **R-r-3 — DOCK-TRIGGER-CLIP honest-RED** (O-63), read as act 9. No consumer clip exists to cure.
- **R-r-4 — `.u` hand-off u4** (the `slider-scrub-contrast.spec.ts` stale docblock, prose only). That path is outside this unit's writable set and was not written. Owner: Close / the next X·F wave.
- **R-r-5** — the `VISUAL_OUT`-less J harness now writes into `test-results/`, which Playwright clears at every run start. Re-minting the J trees needs `VISUAL_OUT=<J dir>`, which is stated at `visual-baseline.spec.ts:28-31`.

**Escalations.** None.

## Close

SEAT Close, `claude-opus-5-5`, 2026-09-23 18:30 EDT. Verify-only; nothing cured here. Spec read whole (52 lines incl. addenda (a)-(d)).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; value.js: only the standing `.github/workflows/ci.yml`, `CARRY-LEDGER.md`, `scripts/dev/dev.sh` and two untracked sibling evidence dirs (none in this seat's set). Nothing inherited.

**E13 (four paths, 18:3x EDT).** ⟨`find <glass BL, BK/coordination, value V/coordination, keyframes V/coordination, atlas P/coordination, fourier docs/tranches> -newermt '2026-09-23 15:14'`⟩ → BK `valuejs-outbound-…-glass-veil-grey.md` + `…-dock-trigger-clip.md` (ours, O-62/O-63; I-46 banks O-63); BK `chicago-inbound-2026-09-23-dock-live-veil-and-hover-lens.md` (chicago→glass, not addressed to value.js/fourier); BL `FORMATION-PROGRESS.md`, `audit/*`, `design/structure/*` (glass-internal). INBOX tail = I-46 READ, last sweep X-W12 18:0x "0 UNREAD". **0 unrowed · 0 UNREAD in F.W14 scope.**

**Act 1 — commit roster, read by `git log` + `git show --stat`.** fourier `m/w1-bump-migration`, ⟨`git log --oneline 216ffbd..HEAD | wc -l`⟩ → **39**, HEAD `a54f71d` = origin.
- `.m` `3c688d1` (one `feat(web)!:` family: `package.json`, lockfile, `web/src/**`, 3 goldens, `f-w13-image-empty.spec.ts`, adjacent `index.html`). In set.
- `.t` `b5a650f` (`gallery/AdminAuditLog|AdminFlaggedPanel|AdminUserList.vue`, `f-w14-admin-table.spec.ts`, `screenshots/f-w14/*`). In set.
- `.g` `001bdcf`, `.p` `1278284`: `web/src/**` + own spec + frames. In set.
- `.h` `ef9dfc6`: `web/src/**`, own spec, frames, + declared adjacent `equation-interaction.spec.ts`, `visualization-ux.spec.ts`, `visual-checkpoint.spec.ts` (selector repoints). In set.
- `.u` 30 commits `a72dd1e`…`0f94a2f`: `web/src/**` + `f-w14-uia.spec.ts` (+ adjacent `f-w14-control-row.spec.ts:245`). In set.
- `.r` `4a87666` · `7808c65` · `0966631` · `a54f71d`. **Landed-wrong (LW-1, writable-set drift):** `7808c65` writes `web/src/components/visualization/VisualizationView.vue`, which is outside `.r`'s planned set (`paper/**`, `style.css`, the J writers, its spec) and is not declared as an adjacent edit (only `ImageUpload.vue` is). The spec's `.r` text owns `.c` r1–r4, whose cure lives in that file, so the content is on-concern; the fault is the unit plan's set plus the undeclared path. Recorded, not repaired here.
- value.js receipts/ledger: `cce4628c` (open) · `0d3da45f` `6576047f` (.m) · `6c1e8d91` `000f6221` (.t) · `233aa37f` (.g) · `6de0952c` `51a96fe2` (.p) · `5d9942bf` (.h) · `c6d6cbc7` `b2dbb28c` (.u) · `f1a7d30b` `42027297` (.r). Each touches only the record / LEDGER (the open also rowed INBOX).

**Close resumed (SEAT Close-2, `claude-opus-5-5`, 2026-09-23 19:01 EDT).** The seat above was killed mid-work. Its uncommitted `## Close` partial (crash-recovery, E13, Act 1) is inherited: I read it whole, and it conforms. Act 1 was re-verified: ⟨`git log --oneline 216ffbd..HEAD | wc -l`⟩ → `39`. ⟨`git log --name-only 216ffbd..HEAD | sort -u`⟩ shows no path outside `web/{src,e2e}/**` except `web/package.json`, `web/package-lock.json` and `web/index.html`, all three `.m`'s. ⟨`git show --stat 7808c65`⟩ → `ImageUpload.vue`, `VisualizationView.vue`, which confirms LW-1. Its full-e2e double-run is also inherited: the scratchpad `close-e2e{1,2}.log` at `a54f71d`. Run 1 finished with `13 failed · 3 skipped · 159 passed`. Run 2 was killed at 161/175. Both runs are cited, not credited, and the double-run was re-run here. Crash-recovery at this seat: ⟨`git -C fourier-analysis status --porcelain`⟩ → `35` ` M web/e2e/screenshots/f-w14/*.png` (mtimes 18:40-18:43, rewritten by the predecessor's e2e run) + `?? .worktrees/`. These are the r5 class of run-dirt, and they are restored to HEAD by exact path at the end of this seat.

**Instrument note.** The first attempt at this seat ran `npx playwright test` with no `BASE_URL`. The config default is `http://localhost:3000`, and ⟨page snapshot⟩ → `{"status":"ok","service":"palette-api"}`, which is a different service. That run was killed at 14/175 while the load stood at 322-368, and it is **discarded, not cited**. Every reading below uses `BASE_URL=http://localhost:3100`, the served fourier vite, at ⟨`curl -w %{http_code}`⟩ → `200`. The installed glass reads ⟨`node -p require(...glass-ui/package.json).version`⟩ → `10.0.1`.

**E13 (four paths, 19:0x EDT).** ⟨`find <glass BL, BK/coordination, value V/coordination, keyframes V/coordination, atlas P/coordination, fourier docs/tranches> -newermt '2026-09-23 18:25' | grep -v /captures/`⟩ → `glass-ui/docs/tranches/BL/audit/round-4/R4-02.md` only. That file is a glass-internal audit round, not addressed to value.js or fourier (⟨`grep -niE 'fourier|value.?js'`⟩ → only a route-census line). The INBOX tail is I-46 READ. **0 unrowed · 0 UNREAD in F.W14 scope.**

### Act 2 — gates, BEFORE → AFTER (fourier `a54f71d` = origin, all readings ×2)

| gate | BEFORE (Baseline, `216ffbd`) | AFTER (this seat) | verdict |
|---|---|---|---|
| G-m pin | `^8.0.0`, installed 8.0.0 | ⟨`grep glass-ui web/package.json`⟩ → `"10.0.1"`; installed `10.0.1` | GREEN |
| vue-tsc | exit 0 | ⟨`npx vue-tsc -b --force`⟩ → **exit 0 ×2** | GREEN |
| vitest | 13 files / 84 | ⟨`npx vitest run`⟩ → **14 files / 86 passed ×2** | GREEN |
| full e2e `--workers=1` | 18 failed · 3 skipped · 95 passed (banked) | ⟨`BASE_URL=…:3100 npx playwright test --workers=1`⟩ → run 1 **13 failed · 3 skipped · 159 passed (7.9m)**, load 46.6→64.1 · run 2 **13 failed · 3 skipped · 159 passed (7.0m)**, load 64.1→74.0. The same 13 in both runs | **RED: 3 outside the named baseline set** |
| unit gates, headed (G-t · G-g · G-p · G-h · G-r) | RED (Baseline table) | ⟨`FW14_PHASE=after … f-w14-{admin-table,veil,dpr,control-row,residuals}.spec.ts --project=chromium --headed --workers=1`⟩ → run 1 **27 passed (1.5m)** · run 2 **1 failed · 26 passed (1.5m)** | G-t · G-g · G-h · G-r GREEN ×2; **G-p RED (1 of 2 headed)** |
| G-u register (`f-w14-uia.spec.ts`, inside full e2e) | 256 open | 32/32 passed in both full runs. The register itself is `.u` PARTIAL: 168 rows owed | falsifiers GREEN; register **PARTIAL** |

**The 13 full-e2e REDs, read by id (identical in both runs).**
- **Named baseline set (10).** contrast-floor `:82`×2 and `:128`; gallery-admin-a11y `:91 :103 :114 :125`; visual-checkpoint `:81 :102 :123`, which are the baseline `:80 :101 :122` moved +1 line by `.r`'s prose edit. fullscreen `:40` and the LOAD family did not fire.
- **visual-checkpoint `:164` `checkpoint-disclosure-body.png`**: ⟨diff⟩ → `26812 pixels (ratio 0.11)` on `.eq-panel-left`. This is `.h`'s **ESC-F14h-1**. The golden needs re-baselining to the OA-45 render, a writable-set grant nobody has given. Still owed.
- **f-w14-dpr `:206` DPR 1 image (headless)**: ⟨⟩ → `DPR 1→2: canvas-container > canvas.canvas-el 1599×1452 ≠ (798.59375×2)×(726×2) [client 799×726]`. The unit calls it headless-only, but the **headed** gate also went RED on run 2: ⟨⟩ → `G-p at DPR 2 … DPR 2→1: … 799×726 ≠ (798.34375×1)×(726×1) [client 798×726]`. On the fractional image stage the backing store is 1 device px off both the rect-rounded anchor and the device-snapped anchor. **G-p is not GREEN ×2.**
- **coarse-pointer `:105` (mobile-chromium)**: ⟨⟩ → `shell-header controls below the 44px coarse touch floor … • Log in → 40.0px`. This is **not** in the named baseline set. It was absent from `.m`'s double-run (10 failed, list at `.m`'s receipt), and none of `.t`, `.g`, `.p`, `.h`, `.u` or `.r` names it. The logged-out "Log in" became a glass `DockTrigger for="popover"` at `.u`'s `fb8b5df` (UIA-F-28/29/30, `UserSlugBar.vue:193`). The consumer passes no size, and the trigger renders at 40 px under the coarse pointer. `.r` counted 13 REDs in its run 1 but did not name this one. **A new RED that no receipt records** (LW-2).

### Act 3 — §Verification Artefacts
The spec has no separate §Verification Artefacts section. Its Close line is "vue-tsc 0, vitest, e2e at `--workers=1` (REDs = the named baseline set), served-page gates re-read by the check seat", and it was run as written above. After the run, the served-gate PNG dirt was cleared: ⟨`git status --porcelain`⟩ → 33 ` M web/e2e/screenshots/f-w14/*.png`, restored to HEAD by exact path from the list `fw14-rundirt.txt`. That leaves ⟨`git status --porcelain`⟩ → `?? .worktrees/` only. ⟨`git status --porcelain docs/tranches/J/`⟩ → empty after both full runs, so `.r`'s sink holds.

### Landed-wrong (recorded, not repaired)
- **LW-1** (inherited from the predecessor, re-verified): `.r`'s `7808c65` writes `VisualizationView.vue`, which is outside `.r`'s planned set. It is on-concern, but it is not declared as an adjacent edit.
- **LW-2**: `.u`'s `fb8b5df` leaves the shell's "Log in" at 40 px under the coarse pointer. That breaks coarse-pointer `:105`, which was GREEN at `.m`, and no receipt records it.
- **LW-3**: `.p`/`.h` carry f-w14-dpr `:206` as "headless-only red", but the headed gate went RED 1 of 2 at this seat (DPR 2→1, image stage).

### Residuals (named owners)
- **R-C-1** ESC-F14h-1: the `checkpoint-disclosure-body` golden needs re-baselining to the OA-45 render. Owner: COHESION/owner grant, then the next F seat.
- **R-C-2** LW-2, the coarse floor on the "Log in" DockTrigger. Owner: the next F wave, a consumer size on the trigger. If the DockTrigger coarse floor is glass-owned, it goes to BL by relay.
- **R-C-3** G-p on the fractional image stage, where the backing store is off by 1 device px after a DPR swap. Owner: the `.p` successor (next F wave).
- **R-C-4** `.u` register: 168 UIA-F rows owed, and 24 SPLIT consumer halves are open. Owner: the next F wave (per `.u`'s receipt).
- **R-C-5** honest-RED **GLASS-VEIL-GREY** (O-62) and **DOCK-TRIGGER-CLIP** (O-63, I-46 banked at glass `a2eb1c06`), plus **DOCK-SCROLL-MORPH** (O-55). Owner: glass BK/BL.
- **R-C-6** the carried `.m` r1/r2, `.t` R-t-1 (the TableRow bare border, glass), `.h` BL-FW14H-1/2, and `.r` R-r-5. Owners as named in those receipts.

### Escalations
None new beyond ESC-F14h-1 (open). No write outside this seat's set. The run-dirt restore is to HEAD by exact path.

### Commit roster
As listed under Act 1 above: 39 fourier commits `216ffbd..a54f71d` (= origin), plus the value.js receipts and ledger. This seat adds only the record and ledger commit.

**Four-verb line.** F.W14: **PARTIAL**. The seven units have landed, but IMPLEMENTED is withheld. The Close gate "e2e REDs = the named baseline set" reads RED ×2: vc `:164` (ESC-F14h-1), f-w14-dpr `:206` and coarse-pointer `:105`. G-p is RED on 1 of 2 headed runs, and `.u` is PARTIAL (168 rows). The spec designates no VERIFIED stamp for this seat.

## Check 1

SEAT Check 1 (L-20 pass 1, fresh adversarial), `claude-opus-5-5`, 2026-09-23 19:5x EDT. Spec read whole (52 lines, addenda (a)-(d)). Record read: header through `## Unit plan`, `## Close`, and grep-located receipt lines. fourier `a54f71d` = origin (⟨`git rev-parse HEAD origin/m/w1-bump-migration`⟩ → both `a54f71d4…`); ⟨`git log --oneline 216ffbd..HEAD | wc -l`⟩ → `39`. Served `:3100` → `200`. Load at open ⟨`uptime`⟩ → 90.95 / 66.62 / 89.83.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; value.js: only the standing sibling/unowned dirt (none in this seat's set). Nothing inherited.

### Axes

1. **Claimed GREENs reproduce.** ⟨`npx vue-tsc -b --force`⟩ → `tsc=0`. ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)`. ⟨`grep glass-ui web/package.json`; installed version⟩ → `"10.0.1"` · `10.0.1`. GREEN ×1 here + the Close's ×2 (the Close's double-run is banked; these three are reproduced).
2. **Writes outside §File Bounds.** ⟨`git log --name-only --format= 216ffbd..HEAD | sort -u | grep -vE '^web/(src|e2e)/'`⟩ → `web/index.html`, `web/package-lock.json`, `web/package.json` (all `.m`'s, declared). value.js wave commits (13, `cce4628c`…`42027297`) touch only the record, LEDGER and (open) INBOX. ⟨`git log cce4628c^..HEAD -- scripts/dev/dev.sh`⟩ → empty. LW-1 confirmed (⟨`git show --stat 7808c65`⟩ → `ImageUpload.vue`, `VisualizationView.vue`), on-concern, undeclared: MINOR.
3. **Masking fallbacks.** ⟨`git diff 216ffbd..HEAD -- web/e2e web/src | grep '^+' | grep -E 'test\.skip|\.fixme|skip\(|allowlist|catch'`⟩ → one hit: `f-w14-uia.spec.ts` helper `settle()` = `page.waitForLoadState("networkidle").catch(() => undefined)` (a settle wait, not a defect mask; INFO). No deleted test/expect: the only removed `expect` lines are `.slider-subtitle` → `[data-row-sub]` selector repoints (`.h`'s declared adjacent edits, same assertion). Goldens moved: 3 (`.m`, declared). The `.c` r4 `emulateMedia` → `contextOptions.reducedMotion` edit strengthens, not narrows. No HIGH.
4. **Commit families.** `.m` one `feat(web)!:` family; units one-meaning commits; not split. GREEN.
5. **E-3.** ⟨`git log cce4628c^..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/`⟩ → only `ddfdb9dc` `ff9160bd` `166ac76a` — the COHESION §0cc/§0cf/§0cj addenda-beside (not wave commits). No wave write. GREEN.
6. **Mail.** ⟨`grep -n '| UNREAD' INBOX.md`⟩ → no UNREAD status row; the Close's two sweeps (18:3x, 19:0x) read 0 UNREAD in scope. GREEN.
7. **Four-verb line.** The Close wrote PARTIAL and withheld IMPLEMENTED. That is lawful and honest. It is not a close.
8. **Goal at the bytes.** The spec's goals are not all met. `.u` covers "every `UIA-F-n` row": ⟨record `:478`/`:492`⟩ → 88 of 256 are dispositioned and 168 are owed. Addendum (b), OA-50, gives `.h` one admin idiom across every admin page. ⟨record `:319`, `:410` h4⟩ → it was "not executed in this seat" and is "owed". **The Close's residual register (R-C-1..6) does not name OA-50/h4.**
9. **Figures reproduce.** The Close named 3 REDs outside the baseline. Here, each was re-run ×2 with `BASE_URL=http://localhost:3100 --workers=1`:
   - ⟨`visual-checkpoint.spec.ts:164`⟩ → `26812 pixels (ratio 0.11 …) are different` ×2.
   - ⟨`coarse-pointer.spec.ts:105` (mobile-chromium)⟩ → `• Log in → 40.0px` ×2.
   - ⟨`f-w14-dpr.spec.ts:206 --project=chromium` (headless)⟩ → run 1 `1 failed · 3 passed`, `DPR 1→2: … 1599×1452 ≠ (798.5×2)×(726×2) [client 799×726]`; run 2 `4 passed`. That is flaky RED, as the Close reported. The run rewrote 4 `after-dpr-*.png`, which were restored to HEAD by exact path. ⟨`git status --porcelain`⟩ → `?? .worktrees/`.
   All three figures reproduce.
10. **Honest-RED adjudication, one gate at a time at the spec bytes.**
   - **GLASS-VEIL-GREY** is RELIEVED. Addendum (c) names it by id and says "F.W14 closes with GLASS-VEIL-GREY honest-RED unless the pinned glass cures it". Owner: glass via O-62. `.r` §8 re-measured the plate.
   - **DOCK-TRIGGER-CLIP** is RELIEVED. Addendum (d) says "Record honest-RED DOCK-TRIGGER-CLIP". Owner: glass via O-63 (banked as I-46). `.r` §9 read it.
   - **DOCK-SCROLL-MORPH** is RELIEVED. It is the F.W13 carried honest-RED, and `.m` was told to re-read it. Owner: glass via O-55.
   - **Named baseline set** is RELIEVED by the spec Close line "REDs = the named baseline set": contrast-floor `:82`×2 and `:128`, gallery-admin-a11y `:91 :103 :114 :125`, and vc `:81 :102 :123`, which are the baseline lines shifted by +1.
   - **vc `:164` (ESC-F14h-1)** is NOT RELIEVED. It is outside the named set, and the spec routes it to no successor. The golden changed because of `.h`'s own OA-45 render. Under §0bt, re-baselining "the string an oracle asserts for copy you changed" is an adjacent edit in the same concern, so no grant was needed. Owner named (R-C-1).
   - **f-w14-dpr `:206` / G-p** is NOT RELIEVED. `.p`'s own spec gate is "for every canvas, `width === round(clientWidth × dpr)` … headed ×2". It reads RED 1 of 2 headed at Close and 1 of 2 headless here. It is not producer-owned, no successor wave exists, and the spec names no honest-RED id for it. Owner named (R-C-3, "next F wave", unauthored).
   - **coarse-pointer `:105`** is NOT RELIEVED. `.u`'s `fb8b5df` regressed it: the gate was GREEN at `.m`, and the Log in trigger is now 40 px under the coarse pointer. It is consumer-owned, since the consumer passes no size. Owner named (R-C-2).
   - **G-u register (168 rows)** is NOT RELIEVED. The spec says every row is "cured at its cause, or routed to glass by id". Owner named (R-C-4).
   - **OA-50 (`.h` admin idiom)** is NOT RELIEVED, and no owner is named in the Close register.
11. **Successors.** ⟨`grep -rn 'Opens after.*F\.W14' docs/tranches/X`⟩ → no match. No authored wave opens after F.W14, so no successor is blocked. Nothing in this wave can be marked GREEN for a successor.

### Register (severity · claim · receipt · cure)
- **HIGH**. `.u` did not meet its goal: 168 of 256 UIA-F rows are neither cured nor routed. Receipt: record `:478`/`:492`, Close R-C-4. Cure: a `.u` continuation sitting that disposes of every remaining row, each with a falsifier ×2, or routes it through O-59 by id.
- **HIGH**. OA-50, addendum (b), the one admin card/row idiom across every admin page, was not executed. The Close residual register does not name it. Receipt: record `:319`, `:410` (h4). Cure: a `.h` sitting on OA-50. It takes a census of every admin page, then gates headed at 1440 and 390 in both themes with before/after frames, and re-reads `.t` against the idiom.
- **HIGH**. G-p (`.p`'s own gate) is not GREEN ×2. On the fractional image stage the backing store sits 1 device px off `round(client×dpr)` after a DPR swap. Receipt: `f-w14-dpr.spec.ts:206`, RED 1 of 2 here and 1 of 2 headed at Close. Cure: size the backing store from `devicePixelContentBoxSize`, or one rounding rule shared with the gate's anchor, at `BasisCanvas`/`useCanvasSetup`. Then G-p must read GREEN ×2 headed.
- **HIGH**. A regression: coarse-pointer `:105` was GREEN at `.m` and is RED after `.u` `fb8b5df`. The Log in DockTrigger renders at 40 px against the 44 px floor. Receipt: re-run ×2 → `Log in → 40.0px`. Cure: the consumer passes the coarse-floor size on `UserSlugBar.vue`'s trigger. If the floor is glass-owned, it goes to BL by id, and until then the gate stays RED.
- **MEDIUM**. The vc `:164` golden is stale against `.h`'s own render. The escalation (ESC-F14h-1) was unnecessary under §0bt. Receipt: re-run ×2 → `26812 px (0.11)`. Cure: re-baseline `checkpoint-disclosure-body-chromium-darwin.png` in `.h`'s concern as an adjacent edit, after reading the diff.
- **MINOR**. LW-1: `7808c65` writes `VisualizationView.vue` outside `.r`'s planned set, and it is not declared as an adjacent edit. The change is on-concern (`.c` r1/r2). Mitigation: it is recorded at Close.
- **INFO**. `f-w14-uia.spec.ts` `settle()` has `networkidle` with `.catch(() => undefined)`. It is a harness settle wait, not a defect mask.

### Verdict
**NOT-CONFORMANT.** Four HIGH remain, and three gates are RED with no relief from the spec's own text. The honest-RED set that is lawfully relieved is GLASS-VEIL-GREY · DOCK-TRIGGER-CLIP · DOCK-SCROLL-MORPH, plus the named baseline set. The LEDGER status stays **PARTIAL** and is not moved to CLOSED; one event line is appended. Gates reproduced here: vue-tsc, vitest, G-m pin, e2e vc`:164`, coarse`:105`, dpr`:206` (6). The last three reproduce the Close's RED readings.

## Repair 1

SEAT Repair 1, `claude-opus-5-5`, 2026-09-23 ~20:00–21:xx EDT. Register: `## Check 1` (4 HIGH · 1 MEDIUM · 1 MINOR · 1 INFO). Spec read whole (52 lines, addenda (a)–(d)). Record read: header through `## Unit plan`, `## Close`, `## Check 1`, and grep-located lines of the `.h`/`.u` receipts.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; fourier HEAD `a54f71d` = origin. Nothing inherited in any writable set. Served `:3100` → `200`; load at open ⟨`uptime`⟩ → 69.0 / 72.6 / 87.7.

### Defect → cure → commit → gate re-reading

1. **HIGH — coarse-pointer `:105` regression (LW-2).** Cause, measured at the bytes: `AppDock.vue` gives its own trigger faces the dock control cell twice — the block floor through the producer hook `--dock-trigger-min-height` (set on `.app-dock`) and the inline floor through `min-inline-size: var(--dock-control-size)` on `.logo-trigger, .nav-trigger`. `UserSlugBar.vue`'s two `DockTrigger`s (made DockTriggers at `.u` `fb8b5df`) got only the first; the icon-only "Log in" at 390 is 40 px wide. Glass's own coarse floor on `.dock-trigger` is a `::after` hit pseudo (`controls/touch-floor.css`), not a box floor, so the box is consumer-owned: no BL relay. **Cure**: both account triggers carry `class="account-trigger"`, scoped `min-inline-size: var(--dock-control-size)` — the same measure AppDock's triggers take. fourier **`dd15d42`**. ⟨`BASE_URL=…:3100 npx playwright test e2e/coarse-pointer.spec.ts --workers=1` ×2⟩ → `4 passed (2.3s)` · `4 passed (2.8s)` (`:105` was RED ×2 at Check 1: `Log in → 40.0px`).
2. **HIGH — G-p not GREEN ×2 (R-C-3 / LW-3).** Cause, measured: `useCanvasSetup.ts` observed the canvas with `{ box: "device-pixel-content-box" }` only. A ResizeObserver reports a change only when the observed box changes, and under DPR emulation that box is the physical screen's (the file's own docblock measures it). So when the fractional image stage settled by a sub-pixel after the DPR swap (798.5 → 798 CSS px), the physical device box did not move, no entry fired, and the bitmap stayed at the stale box. The three failing readings are that one mechanism: `1597×… ≠ (798×2)`, `1598 ≠ (798.515625×2)`, `1599 ≠ (798.5×2)`. **Cure** (the Check's own idiom, one rounding rule): the observer watches the CSS content box, so any layout change of the box re-sizes it. Where the engine supports it, a second observer watches the device-pixel box, so a snap that moves no CSS size still re-sizes. Both feed the one entry law (`devicePixelContentBoxSize` when it describes the live DPR, else `round(css × dpr)`). fourier **`dc733e2`**. Falsifier: the pre-cure file restored in the working tree only (the committed cure copied aside and put back by `cp`; no stash) → ⟨`f-w14-dpr.spec.ts --project=chromium` ×5 headless⟩ → **3 of 5 RED** (the three readings above). Cure → ⟨×5 headless⟩ → `4 passed` ×5; ⟨`FW14_PHASE=after … --headed` ×2⟩ → `4 passed (12.9s)` · `4 passed (12.5s)`. **G-p GREEN ×2 headed at DPR 1 and 2** (both DPRs are described in the one file). The run-rewritten `after-dpr-*.png` (4) were restored to HEAD by exact path. The committed crops remain `.p`'s evidence (E-3).
3. **HIGH — OA-50 (addendum (b)), unexecuted `.h` h4.** Executed here as the `.h` OA-50 sitting.
   - **Census** (`GalleryView.vue` admin tabs, bytes): Users `AdminUserList.vue`, Flagged `AdminFlaggedPanel.vue`, Audit Log `AdminAuditLog.vue` (`.t`'s glass DataTable). The chrome over them is `GalleryAdminBanner.vue` and `BatchActionBar.vue`. No other admin surface exists (⟨`grep -rl 'adminMode\|isAdmin' web/src`⟩ → the gallery files + stores + `api.ts`).
   - **BEFORE, served** (⟨`FW14_PHASE=before … f-w14-admin-idiom.spec.ts`⟩, frames `before-oa50-{users,flagged,audit-log}-{1440,390}-{light,dark}.png`). Flagged read against the owner frame: Keep/Dismiss/Delete stacked in a column; "Normal" bare; the reporter line in `text-xs` with the mono family at 18.6 px, larger than the 10 px byline (`.h`'s finding: `text-sm`/`text-xs` size nothing under glass's bridge); "3 flags" over one listed flag; at 390 the title truncated to `spiral-…` beside the stacked column. Users at 390: the uppercase `ENTRIES/JOINED/SEEN` run wrapped mid-pair. Gate: RED on every Users and Flagged case (0 rows on an idiom; the pre-cure files carry no `data-admin-*` hook, so all 8 fail. The printed reading of the first two was the stack of failures above: no action group, no labelled tier, and `"3 flags" over 0 listed`). The Audit Log's 4 cases are GREEN-before (`.t`'s DataTable cells are already ≤ `--type-small`).
   - **The idiom** (`web/src/components/visualization/gallery/admin-row.css`, the one source both lists import): one grid per row, `lead · body · actions`. The media lead is inset by the row's `--space-body` padding. The title is one `--type-small` mono line, ellipsised. Meta is a labelled `dl` (`dt`/`dd` pairs that never break mid-pair) on `--type-micro` mono muted: Owner · Posted · Tier on Flagged, Entries · Joined · Seen on Users. A flag's reason and detail are `--type-caption`, and its reporter line is on the meta rung. Every flag is listed, and when the record carries fewer flags than its count the list says `Showing k of N flags`. The action group is ONE horizontal row: beside the body on a wide list, and beneath it (start-aligned) when the list itself is narrower than 36 rem, decided by a container query rather than the viewport. The rhythm is `--space-body` and `--space-atom`. The five admin files' remaining `text-sm`/`text-xs` utilities on text were moved to glass rungs (`text-small`/`text-caption`), and the five inert copies on controls (Button/Input) were removed; they sized nothing.
   - **Glass halves, relayed by id, no local override**: BL-FW14OA50-1 (Button/Badge stadium radius vs the `--radius-field` canon) and BL-FW14OA50-2 (no row/action-group primitive). Filed as **O-65** beside O-59/O-63, value.js `184ccad0`.
   - **Gate** `web/e2e/f-w14-admin-idiom.spec.ts` (G-a). Per panel × 1440/390 × light/dark, it checks: every row on `[data-admin-row]`; media inset ≥ 8 px; actions share one top and do not overrun the row; the title on `--type-small`; every meta on `--type-micro`; detail on `--type-caption`, with the rungs resolved in-page; every field has a `dt`; the flag count matches the listed flags or the note. It also re-reads `.t`'s Audit Log cells at ≤ `--type-small`. fourier **`c47dc5b`** (cure + gate + 24 before/after frames, one family). ⟨`FW14_PHASE=after … f-w14-admin-idiom.spec.ts f-w14-admin-table.spec.ts --project=chromium --headed --workers=1` ×2⟩ → `24 passed (1.6m)` · `24 passed (1.6m)`: **G-a 12/12 and the `.t` re-read G-t 12/12, ×2 headed**. The after-frames were read (flagged 1440 light, 390 dark; users 390 light): the actions sit in one row, the fields are labelled, the rungs are consistent, and the note is present. G-t's run-rewritten `after-{users,flagged,audit-log}-*.png` (12) were restored to HEAD (`.t`'s evidence, E-3). Neighbours: ⟨`gallery.spec.ts gallery-admin-a11y.spec.ts f-w14-admin-table.spec.ts --workers=1`⟩ → `4 failed · 19 passed`. The 4 are the named-baseline `gallery-admin-a11y :91 :103 :114 :125`, and `:103`/`:114`'s only violation id is `aria-hidden-focus` (⟨grep `"id"`⟩). No `dl`/`list` rule fires.
4. **HIGH — `.u` register: 168 UIA-F rows owed (R-C-4). NOT cured in this seat. Escalated.** Measured scale: `.u`'s own sitting dispositioned 88 of 256 rows (33 cured at cause, each with a falsifier RED against its reverted diff and GREEN ×2). The remainder is 143 OPEN consumer rows, 24 split consumer halves and F-39. Four of those rows (F-35, F-83, F-112, and F-39's API half) need `api/**` / `src/fourier_analysis/**`, which is outside every F.W14 writable set (`.u` u2). The Check's own cure names "a `.u` continuation sitting". The remaining rows are inside bounds, but each needs a served frame, a cure and a falsifier ×2, which is several `.u` sittings of work, not a repair act. No row was touched here, so none is claimed.
5. **MEDIUM — vc `:164` stale golden (ESC-F14h-1).** The diff was read before re-baselining (expected vs actual PNG): the whole 26 812 px (0.11) difference is `.h`'s OA-45 render on the `/equation` left panel. The card titles moved onto the heading rung with mono subtitles, the slider thumbs became visible, the value field went inline, and the Display-terms subtitle is ellipsised. There is nothing outside `.h`'s concern. Under §0bt this is an adjacent edit of the oracle for copy the unit changed. ⟨`… visual-checkpoint.spec.ts:164 --project=chromium --update-snapshots`⟩ re-baselined `checkpoint-disclosure-body-chromium-darwin.png` only (⟨`git status --porcelain`⟩ → that one path). fourier **`a8aa0a1`**. ⟨`:164` ×2⟩ → `1 passed (1.7s)` · `1 passed (1.8s)`. **ESC-F14h-1 is closed.**
6. **MINOR — LW-1.** Declared here as an **adjacent edit (§0bt)**: `.r` `7808c65` `web/src/components/visualization/VisualizationView.vue`. Reason: `.c` r1/r2's cure (the image sidebar entering on the pick, and the aside band re-read when the Configurator unmounts) lives in that file's band logic. It is the same concern as `.r`'s `.c` residuals, and `.r` did not declare it. No bytes change.
7. **INFO — `settle()` `.catch`.** No action (the Check rules it a harness settle idiom, not a mask).

**Adjacent edits (this seat, §0bt).**
- `web/e2e/visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-darwin.png` (item 5): the oracle for `.h`'s own render.
- `vjs:docs/tranches/X/relay/X-ALL-BK-UI-AUDIT-ADDENDUM-2026-09-23-FW14H-OA50.md` + one `INBOX.md` O-65 row (item 3): addendum (b) orders OA-50's glass half "by relay only".

**Inherited paths.** None.

### Gate re-reading (fourier `c47dc5b`, write-then-measure, ×2)

| gate | Check 1 | Repair 1 |
|---|---|---|
| vue-tsc | exit 0 | ⟨`npx vue-tsc -b --force` ×2⟩ → `tsc=0` · `tsc=0` |
| vitest | 14 / 86 | ⟨`npx vitest run` ×2 (after `c47dc5b`)⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2 |
| coarse-pointer `:105` | RED ×2 (`Log in → 40.0px`) | whole file `4 passed` ×2 — **GREEN** |
| G-p `f-w14-dpr.spec.ts` | RED 1/2 headed, 1/2 headless | headed `4 passed` ×2, headless `4 passed` ×5 (pre-cure 3/5 RED) — **GREEN** |
| vc `:164` | RED ×2 (26 812 px) | `1 passed` ×2 — **GREEN** |
| G-a OA-50 (new) + G-t re-read | — | headed `24 passed (1.6m)` ×2 — **GREEN** |
| G-u register | PARTIAL, 168 owed | unchanged, **escalated** (item 4) |
| full e2e `--workers=1` | 13 failed ×2 (3 outside the baseline) | **NOT READ — instrument outage.** Run 1 was started at 20:11 (load 42.5). By test 14/187 it had 13 REDs, all `contour-extraction.spec.ts` (`:16`×11, `:43`, `:86`) with `page.waitForURL` timeouts, and the page read `Internal server error`. The instrument was probed directly: ⟨`curl -F file=@<fresh random PNG> :8000/api/images`⟩ → `500 {"detail":"Internal server error"}`; ⟨`curl :8000/api/gallery/cursor?limit=1`⟩ → `500`; ⟨`curl :8000/api/health`⟩ → `{"status":"ok"}` (it does not touch the DB); ⟨`nc -z localhost 27017`⟩ → `1` (refused), and no `mongod` process is running. The fourier API's local MongoDB (`api/config.py:9` `mongodb://localhost:27017/fourier`) went down during this seat. The uploads in `f-w14-dpr.spec.ts` passed at ~20:00, so the outage came after that. The run was killed and is **discarded, not cited**. Repairing the database is outside every writable set, and the one recent brew mongod log (09-22) reports WiredTiger metadata corruption, so it is not a restart this seat may make. Every gate a Repair-1 cure can move was read ×2 above. The API-stubbed gates (G-a, G-t, coarse) and the DPR/vc gates all ran before the outage or do not use the DB. |

**Run-dirt.** Every gate-rewritten tracked PNG (`after-dpr-*` ×4, `after-{users,flagged,audit-log}-*` ×12) was restored to HEAD by exact path. ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/`.

### Commits
- fourier (`m/w1-bump-migration`): `dd15d42` (coarse floor) · `dc733e2` (G-p) · `a8aa0a1` (vc `:164` golden, adjacent) · `c47dc5b` (OA-50 idiom + G-a + frames).
- value.js: `184ccad0` (O-65 relay + INBOX row) · this record's Repair 1 commit.

### Escalations
- **ESC-R1-1 (`.u` bulk, 168 rows).** This needs dedicated `.u` continuation sittings, as the Check's cure names. It is not a repair act (item 4). Four of the rows need `api/**`, which is outside F.W14's bounds (`.u` u2).
- **ESC-R1-2 (instrument).** fourier's local MongoDB on `:27017` is down: uploads and gallery reads return 500. The Close gate's full e2e double-run cannot be read until the owner or instrument restores it. The next Check seat should re-read full e2e on a restored DB.

### Verdict
Cured: 3 HIGH (coarse `:105`, G-p, OA-50), 1 MEDIUM (vc `:164`) and 1 MINOR (LW-1, declared), so **5 cured**. Escalated: the `.u` bulk (HIGH) and the DB instrument outage. The LEDGER row stays **PARTIAL**, and one event line is appended.

## Check 2

SEAT Check 2 (L-20 pass 2, fresh adversarial), `claude-opus-5-5`, 2026-09-23 ~20:2x EDT. Spec read whole (52 lines, addenda (a)–(d)). Record read: header through `## Unit plan`, `## Close`, `## Check 1`, `## Repair 1`, plus the `.u` G-u lines by grep (`:476-494`). fourier `m/w1-bump-migration` HEAD `c47dc5b` = origin (⟨`git ls-remote origin m/w1-bump-migration`⟩ → `c47dc5bd…`); ⟨`git log --oneline a54f71d..HEAD`⟩ → 4 (Repair 1's `dd15d42 dc733e2 a8aa0a1 c47dc5b`). Load at open ⟨`uptime`⟩ → 22.5 / 37.1 / 54.6.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; value.js: only standing sibling/unowned dirt (none in this seat's set). Nothing inherited.

**Instrument.** Served `:3100` → `200`. fourier's MongoDB is still down: ⟨`nc -z localhost 27017`⟩ → `1`; ⟨`curl :8000/api/gallery/cursor?limit=1`⟩ → `500`. ESC-R1-2 stands, so every DB-backed gate (full e2e, G-p image-stage limb) is unreadable at this seat.

### Axes
1. **Claimed GREENs reproduce.** ⟨`npx vue-tsc -b --force`⟩ → `tsc=0`. ⟨`npx vitest run` ×2⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2. ⟨`BASE_URL=…:3100 npx playwright test e2e/coarse-pointer.spec.ts e2e/visual-checkpoint.spec.ts:164 e2e/f-w14-dpr.spec.ts --workers=1` ×2⟩ → `2 failed · 7 passed` ×2. The 7 passes are coarse `:105` file 4/4, vc `:164` 1/1, and the dpr rest limbs. The 2 fails are both G-p image-stage tests (DPR 1, DPR 2), each `TimeoutError: page.waitForURL: Timeout 20000ms exceeded`: the image upload hits the 500 DB. That is the instrument, not a reading of the cure. ⟨`FW14_PHASE=after … f-w14-admin-idiom.spec.ts f-w14-admin-table.spec.ts --project=chromium --headed --workers=1` ×2⟩ → `24 passed (1.6m)` ×2 (API-stubbed). Reproduced: vue-tsc · vitest · coarse `:105` · vc `:164` · G-a · G-t. **Not reproducible here:** G-p image-stage limb (Repair 1 read it headed 4/4 ×2 before the outage; cited, not credited) and the Close gate's full e2e `--workers=1` (never read after Repair 1).
2. **Writes outside the unit sets.** ⟨`git show --stat`⟩ of the four Repair commits: `UserSlugBar.vue` · `useCanvasSetup.ts` · the one vc golden (declared adjacent) · `gallery/{AdminAuditLog,AdminFlaggedPanel,AdminUserList,BatchActionBar,GalleryAdminBanner}.vue` + new `gallery/admin-row.css` + `e2e/f-w14-admin-idiom.spec.ts` + 24 `screenshots/f-w14/*oa50*` frames. All are inside `web/{src,e2e}/**`. value.js `184ccad0` (relay addendum + INBOX row, declared) and `f9ae7100` (record + LEDGER). ⟨`git log cce4628c^..HEAD -- scripts/dev/dev.sh`⟩ → empty. GREEN.
3. **Masking.** ⟨`git show dd15d42 dc733e2`⟩: a scoped `min-inline-size` class and a second ResizeObserver, both root cures. There is no try/catch, skip or allowlist. ⟨`git show c47dc5b -- web/e2e | grep -E 'skip|fixme|catch|allow|route\('`⟩ → only `page.route("**/api/admin/flagged**")`, the gate's API fixture (the same harness as `.t`'s G-t), not a mask. No assertion was deleted. GREEN.
4. **Commit families.** One commit per meaning: `c47dc5b` is the OA-50 cure, its gate and its frames as one family. GREEN.
5. **E-3.** ⟨`git log cce4628c^..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/`⟩ → `166ac76a` `ff9160bd` `ddfdb9dc`, which are only the COHESION §0cj/§0cf/§0cc addenda-beside. No wave commit wrote there. GREEN.
6. **Mail.** ⟨`grep -n UNREAD INBOX.md`⟩ → only sweep lines saying "0 new UNREAD"; there is no UNREAD status row. O-65 is rowed (`184ccad0`). GREEN.
7. **Four-verb line.** The row reads PARTIAL. That is lawful, and it is not a close.
8. **Goal at the bytes.** Four of Check 1's HIGH are now met at the bytes: coarse `:105`, G-p (its cure is read, though the image limb is unreadable here), OA-50 and the vc golden. `.u`'s goal is still not met: "every `UIA-F-n` row … cured at its cause, or routed to glass by id" stands at 88 of 256 (record `:478`). The 168 owed rows include 5 BROKEN rows (F-9, F-14, F-21, F-26, F-46; record `:492`), and Repair 1 touched none of them (Repair 1 item 4).
9. **Figures reproduce.** Repair 1's published readings for vue-tsc, vitest, coarse `4 passed` ×2, vc `:164` `1 passed` ×2, and G-a + G-t `24 passed (1.6m)` ×2 all reproduce exactly. G-p image `4 passed` ×2 does not reproduce here because of the instrument outage.
10. **Honest-RED adjudication, at the spec bytes.**
   - **GLASS-VEIL-GREY** is RELIEVED by addendum (c): "F.W14 closes with GLASS-VEIL-GREY honest-RED unless the pinned glass cures it". Owner: glass via O-62.
   - **DOCK-TRIGGER-CLIP** is RELIEVED by addendum (d): "Record honest-RED DOCK-TRIGGER-CLIP". Owner: glass via O-63 / I-46.
   - **DOCK-SCROLL-MORPH** is RELIEVED. It is the F.W13 honest-RED that `.m` was ordered to re-read. Owner: glass via O-55.
   - **The named baseline e2e set** (contrast-floor `:82`×2 `:128`, gallery-admin-a11y `:91 :103 :114 :125`, vc `:81 :102 :123`) is RELIEVED by the Close line "REDs = the named baseline set".
   - **G-u, 168 UIA-F rows** is NOT RELIEVED. The spec routes no part of `.u` to a successor and names no honest-RED id for it. Only the glass parts have relief ("or routed to glass by id"), and those 48 are already routed. The owner is named: Repair 1 ESC-R1-1, "`.u` continuation sittings". But no authored wave or COHESION ruling carries it: ⟨`grep -n 'ESC-R1-1\|F\.W14' COHESION.md`⟩ → no ruling after §0cj on the `.u` bulk. An escalation is not relief.
   - **The Close gate, full e2e `--workers=1`** is UNREAD, not RED. The instrument is the fourier MongoDB outage (ESC-R1-2, owner/instrument). No credit is given, and the gate is not measured since Repair 1's four commits.
11. **Successors.** ⟨`grep -rln 'Opens after.*F\.W14' docs/tranches/X`⟩ → only this record and `C/F-W13.md` (prose), with no wave spec. No successor declares F.W14 as a conjunct, so none is blocked.

### Register (severity · claim · receipt · cure)
- **HIGH**. `.u` is unmet: 168 of 256 UIA-F rows are neither cured nor routed. They include 5 BROKEN rows: F-9, F-14, F-21, F-26 and F-46. Receipt: record `:478`/`:492`; Repair 1 item 4 (not touched). Cure: `.u` continuation sittings, BROKEN first, each row cured at cause with a falsifier ×2 or routed to O-59 by id. The 4 `api/**` rows need an owner/COHESION grant widening F.W14's bounds, or a ruling that routes them to a named wave. Otherwise, an owner ruling that re-homes the bulk to an authored successor wave would convert this row to a relieved residual.
- **HIGH**. The spec Close gate (full e2e `--workers=1`, REDs = the named baseline set) is unread after Repair 1's four commits. This check also cannot re-read the G-p image-stage limb. Receipt: ⟨`nc -z localhost 27017`⟩ → 1; ⟨`/api/gallery/cursor`⟩ → 500; dpr image tests `waitForURL` timeout ×2. Cure: restore fourier's local MongoDB (owner/instrument act; brew log reports WiredTiger metadata corruption), then run full e2e ×2 and G-p headed ×2.
- **INFO**. The G-a and G-t run-dirt (12 `screenshots/f-w14/*.png`) was restored to HEAD by exact path; ⟨`git status --porcelain`⟩ → `?? .worktrees/`.

### Verdict
**NOT-CONFORMANT.** Two HIGH remain. First, the `.u` register is unrelieved. Second, the Close gate cannot be measured. Repair 1's five cures reproduce: coarse, vc `:164`, G-a, G-t, and vue-tsc/vitest. The relieved honest-RED set is GLASS-VEIL-GREY · DOCK-TRIGGER-CLIP · DOCK-SCROLL-MORPH, plus the named baseline set. The LEDGER row stays **PARTIAL**, and one event line is appended. Gates reproduced: 6 (vue-tsc, vitest, coarse `:105`, vc `:164`, G-a, G-t). Unread: G-p image limb and full e2e.

## Repair 2

SEAT Repair 2, `claude-opus-5-5`, 2026-09-23 ~20:20–20:50 EDT. Register: `## Check 2` (2 HIGH · 1 INFO). Spec read whole (52 lines, addenda (a)–(d)). Record read: header through `## Unit plan`, the `.u` G-u/residual lines by grep (`:470-515`), `## Repair 1`, `## Check 2`.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; fourier HEAD `c47dc5b` = origin. value.js: only standing sibling/unowned dirt, none in this seat's set. Nothing inherited.

**Instrument at open.** ⟨`nc -z localhost 27017`⟩ → `1`; ⟨`curl :8000/api/gallery/cursor?limit=1`⟩ → `500`; ⟨`curl :3100`⟩ → `200`; ⟨`pgrep -fl mongod`⟩ → empty; load ⟨`uptime`⟩ → 45.0 / 41.7 / 49.7. The MongoDB outage (ESC-R1-2) stands.

### Defect → cure → commit → gate re-reading

1. **HIGH — `.u` register, 168 rows owed.** Worked as a `.u` continuation sitting, BROKEN first, each row cured at its cause with a falsifier in the new additive file `web/e2e/f-w14-uia-r2.spec.ts`, proven RED against the pre-cure bytes and then GREEN ×2. Every row cured here carries a register frame, so no new BEFORE frame was owed (the same reading `.u` made). **7 rows dispositioned this seat:**
   - **UIA-F-26 (BROKEN).** Cause, measured at the bytes: `paperSearchIndex.ts` gave a labelled theorem/figure entry `id: thm.label` (`thm:residue`). The renderer mints the DOM id `thm-residue` (published in `labelMap` as `anchorId`). `useScrollNavigation.performScroll` resolved the owner section through `ownerSectionByTargetId` (which knows the key spelling) and then landed with `getElementById("thm:residue")`, which never exists, so the reader stayed wherever the virtual window settled. Cure: `buildSearchIndex(sections, labels)` resolves a label to its anchor (`anchorFor`), falling to the owning section, never to the raw key; `usePaperSearch` takes `labelMap`, passed by `PaperView`. fourier **`23352db`**. Falsifier: Enter on Thm 4.2.1 (Residue) and Thm 2.10.2 (Parseval) must put `#thm-residue` / `#thm-parseval` in the scroller viewport → pre-cure **2/2 RED**, cure `2 passed` ×2.
   - **UIA-F-21 (BROKEN).** Cause: `PaperSearchResultRow` printed `highlightFuzzy(resultLabel(r))`, the raw label TeX, with marks computed over the source. Cure: `searchHelpers.highlightLabel` splits a label into text and `$…$` math. The math is typeset by the paper's own KaTeX (`PAPER_CONTEXT.renderInline`, the paper's macros). The fuzzy match runs over the text segments only, so no `<mark>` lands inside math. An unlabelled equation is its TeX, typeset whole. The first run of the cure caught a second cause: figure and code caption labels were cut at 200 code units, which could split a `$…$` span (`… annulus in the $w`). `clipLabel` now ends a label before an unclosed span. fourier **`b7297ac`**. Falsifier (queries `convergence`, `pipeline`, `Parseval`, `energy`): no `$` or `\cmd` outside `.katex`, KaTeX present, 0 marks inside `.katex` → pre-cure **4/4 RED**, cure `4 passed` ×2.
   - **UIA-F-46 (BROKEN), consumer half.** Cause: `stores/gallery.ts` `like()` hard-coded `liked = true` and `+1`. Cure: `like(slug, liked)` moves the count one step in the direction of the requested state (floor 0), and `GalleryView.handleLike` asks for the inverse of `likedHashes`. fourier **`b4a4311`**. Falsifier (stubbed gallery): press, press, then the modal and the card read `aria-pressed=false` at the original count → pre-cure **RED** (`aria-pressed` stayed `true`), cure `1 passed` ×2. **Server half** (persisting the like): ⟨`grep -rn like api/routers`⟩ shows no like route under the converged CRUD shape (`gallery.py:3` records the carve-out). It is ROUTED-SERVER with F-35/F-83/F-112/F-39 (`.u` u2): `api/**` lies outside every F.W14 writable set.
   - **UIA-F-63 + UIA-F-65 (HIGH).** Causes: the palette's Clear called `search.close()`, which dismisses as well as clears; its empty branch printed "No results" for an empty query. Cure: `usePaperSearch.clear()` owns the clear (query, debounced query, selection, cache, all in one tick) and `close()` = dismiss + `clear()`; the palette's Clear calls `clear()` and refocuses its field; the empty branch prompts when the query is empty, and "No results" answers only a query that matched nothing. fourier **`1c2f8fd`**. Falsifiers: F-63 pre-cure **RED** ("No results" ×1 at empty) · F-65 pre-cure **RED** (dialog gone after Clear); cure `2 passed` ×2.
   - **UIA-F-59 (HIGH), consumer half.** Cause: `PaperSearchDropdown.measure()` bound `width: ${r.width}px`, the 196 px rail field; the plate was a hand 1.5 px border at `calc(var(--radius) - 2px)`. Cure: width = max(field, min(24 rem, room to the viewport margin)); the panel carries the producer's menu plate (`glass-overlay-plate[data-reveal=menu]`: `--radius-card`, `--overlay-pad`), and the hand border, corner, shadow and padding retire. The plate's warm veil tone is glass's (O-62, GLASS-VEIL-GREY, relieved). fourier **`9619ef3`**. Falsifier (1440): panel ≥ 22 rem, inside the viewport, radius = resolved `--radius-card`, on the menu plate → pre-cure **RED** (`width 196 · radius 8px · plate false`), cure `1 passed` ×2. Frame read (scratchpad `f59.png`, 1440 light, "convergence"): full titles, `L²` typeset, marks on text only.
   - **UIA-F-62 (HIGH), consumer.** Cause: the in-field ✕ labelled "Clear search" called `close()`; the floating bar also offered Expand. Cure: `PaperSearchInput.clearQuery()` → `search.clear()` + focus; `PaperSearch` offers Expand on the sidebar variant only. fourier **`3c66fb9`**. Falsifier (390, touch): no visible Expand; after Clear search, Close search is still visible, the field is empty and focused → pre-cure **RED** (Expand present), cure `1 passed` ×2.
   - **Not cured, measured reason — the other two BROKEN rows.** **UIA-F-9** (390 More-options menu) and **UIA-F-14** (fullscreen as a deaf second editor) live only on a loaded visualization stage (`AnimationControls` / `FullscreenViewer` mount under `VisualizationView` with a workspace). Every path to that state reads or writes the fourier MongoDB (`firstSavedViz` → `GET /api/visualizations` → `500`; upload → `POST /api/images` → `500`). The served BEFORE frame, the falsifier's RED and its GREEN ×2 cannot be read. A cure landed without them would be unmeasured, so none was made.
   - **G-u after this seat:** ⟨register lists, `.u` `:470-515` minus this seat's 7⟩ → **95 of 256 dispositioned; 161 owed** (136 OPEN: BROKEN F-9 and F-14, DB-blocked · 43 HIGH · 65 MEDIUM · 26 LOW; plus 24 split consumer halves and F-39. Recounted by ⟨grep -oE "F-[0-9]+" over the `:466-469` lists minus this seat's 7 | wc -l⟩ → 2 · 43 · 65 · 26). The cure rate this seat is 7 rows at root with falsifiers in ~30 min on the paper and gallery surfaces that need no DB. The visualize-stage rows (the largest block) need the DB. **Escalated** (ESC-R2-1 below): the bulk is not a repair act, and no spec text or COHESION ruling relieves it.
2. **HIGH — Close gate full e2e and G-p image limb unread (DB down).** Not curable inside any writable set. The repair is the instrument's: restore fourier's MongoDB (the 09-22 brew log reports WiredTiger metadata corruption; a `--repair` or a restore can lose data, so it is an owner act). Re-probed at close: ⟨`nc -z localhost 27017`⟩ → `1`; cursor → `500`. **Escalated** (ESC-R2-2). The gallery neighbours this seat's F-46 cure could move were read: ⟨`f-w14-uia.spec.ts -g "F-45|F-47|F-40|F-48|F-41"` ×2⟩ → F-40/F-41/F-45 green; F-47 and F-48 red ×2 at `firstSavedViz` → `res.ok() false` (the DB, `:45`), which is not a reading of the cure.
3. **INFO — run-dirt.** No gate this seat rewrites a tracked PNG; ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` at every commit.

**Adjacent edits (§0bt).** None. Every path is inside `.u`'s writable set (`web/src/**`, `web/e2e/**` additive).

**Inherited paths.** None.

### Gate re-reading (fourier `3c66fb9` = origin, write-then-measure, ×2)

| gate | Check 2 | Repair 2 |
|---|---|---|
| vue-tsc | `tsc=0` | ⟨`npx vue-tsc -b` after each cure; `-b --force` at the end⟩ → `tsc=0` every time |
| vitest | 14 / 86 ×2 | ⟨`npx vitest run` ×2 at `3c66fb9`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2 |
| falsifiers `f-w14-uia-r2.spec.ts` (new) | — | ⟨`BASE_URL=…:3100 npx playwright test e2e/f-w14-uia-r2.spec.ts e2e/paper-search.spec.ts --project=chromium --workers=1` ×2⟩ → `14 passed (18.3s)` · `14 passed (19.1s)` (11 falsifiers + `paper-search.spec` 3) |
| paper-search neighbours (F-22/23/25) | green | ⟨`f-w14-uia.spec.ts -g "F-22\|F-23\|F-25"`⟩ → `3 passed` after each paper cure |
| gallery neighbours | — | F-40/F-41/F-45 green ×2; F-47/F-48 red ×2 at the DB (`firstSavedViz` `:45`), instrument |
| G-u register | 88 / 256, 168 owed | **95 / 256, 161 owed**: RED (PARTIAL), escalated (ESC-R2-1) |
| full e2e `--workers=1` | unread (DB) | **unread**: DB still down at close (ESC-R2-2) |
| G-p image-stage limb | unread (DB) | **unread**: same instrument |

### Commits
- fourier (`m/w1-bump-migration`, pushed; ⟨`git ls-remote origin m/w1-bump-migration`⟩ → `3c66fb911b0a`): `23352db` (F-26) · `b7297ac` (F-21) · `b4a4311` (F-46 consumer) · `1c2f8fd` (F-63 + F-65) · `9619ef3` (F-59 consumer) · `3c66fb9` (F-62).
- value.js: this record's Repair 2 commit and one LEDGER event line.

### Escalations
- **ESC-R2-1 (`.u` bulk, 161 rows).** It needs owner/COHESION action of one of two kinds. (a) Author `.u` continuation sittings as a named successor wave (or a COHESION ruling that re-homes the bulk). The measured rate is ~7 rows per repair seat, and the 161 rows include the whole visualize-stage block. (b) Widen F.W14's bounds to `api/**` for the server rows: F-35, F-83, F-112, F-39's API half, and now F-46's like endpoint. A repair seat cannot lawfully convert an unrelieved register of this size, and each row still needs its frame, cure and falsifier ×2.
- **ESC-R2-2 (instrument, carries ESC-R1-2).** fourier's local MongoDB on `:27017` is down (WiredTiger metadata corruption in the 09-22 brew log). It blocks the Close gate's full e2e ×2, the G-p image-stage limb, every `/v/` and `/w/` served falsifier, and the served frames that UIA-F-9 and UIA-F-14 need. It is an owner act: a repair or restore may lose data.

### Verdict
**Cured: 7 register rows.** BROKEN F-21, F-26 and the F-46 consumer half; HIGH F-59 (consumer), F-62, F-63 and F-65. Each was cured at its cause, with a falsifier RED before the cure and GREEN ×2 after. **Escalated: 2** (ESC-R2-1, the `.u` bulk; ESC-R2-2, the DB instrument). The INFO needs no action. The LEDGER row stays **PARTIAL**, and one event line is appended.

## Check 3

SEAT Check 3 (L-20 pass 3, fresh adversarial), `claude-opus-5-5`, 2026-09-23 ~20:4x EDT. Spec read whole (52 lines, addenda (a)–(d)). Record read: header through `## Unit plan`, `## Close`, `## Check 1`, `## Repair 1`, `## Check 2`, `## Repair 2`. fourier `m/w1-bump-migration` HEAD `3c66fb9` = origin (⟨`git ls-remote origin m/w1-bump-migration`⟩ → `3c66fb911b0a…`). Load ⟨`uptime`⟩ → 41.75 / 34.21 / 39.39.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; value.js: only standing sibling/unowned dirt (none in this seat's set). Nothing inherited.

**Instrument.** ⟨`nc -z localhost 27017`⟩ → `1`; ⟨`curl :8000/api/gallery/cursor?limit=1`⟩ → `500`; ⟨`curl :3100`⟩ → `200`. ESC-R2-2 stands.

### Axes
1. **Claimed GREENs reproduce.** ⟨`npx vue-tsc -b --force`⟩ → `tsc=0`. ⟨`npx vitest run` ×2⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2. ⟨`BASE_URL=…:3100 npx playwright test e2e/f-w14-uia-r2.spec.ts e2e/paper-search.spec.ts --project=chromium --workers=1` ×2⟩ → `14 passed (19.7s)` · `14 passed (21.2s)` (Repair 2's 11 falsifiers + paper-search 3). ⟨`coarse-pointer.spec.ts --workers=1` ×2⟩ → `4 passed` ×2. ⟨`git status --porcelain`⟩ after → `?? .worktrees/`. Reproduced: vue-tsc · vitest · Repair 2 falsifiers · coarse `:105` (4). G-a/G-t/vc `:164` banked GREEN ×2 at Check 2 on bytes Repair 2 did not touch (paper/gallery-store paths only).
2. **Writes outside bounds.** ⟨`git show --stat`⟩ of `23352db b7297ac b4a4311 1c2f8fd 9619ef3 3c66fb9` → only `web/src/components/paper/**`, `web/src/stores/gallery.ts`, `GalleryView.vue`, and the new additive `web/e2e/f-w14-uia-r2.spec.ts` — inside `.u`'s set. value.js `ad9e5649` → record + LEDGER only. ⟨`git log cce4628c^..HEAD -- scripts/dev/dev.sh`⟩ → empty. GREEN.
3. **Masking.** ⟨`git diff c47dc5b..3c66fb9 -- web | grep '^[-+]' | grep -E 'skip|fixme|catch|allow|route\('`⟩ → no hit on a removed line and no skip/catch/allowlist added; no `expect` removed. GREEN.
4. **Commit families.** One commit per row (F-63+F-65 share one cause, one commit). GREEN.
5. **E-3.** ⟨`git log cce4628c^..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/`⟩ → only `166ac76a ff9160bd ddfdb9dc` (COHESION addenda-beside). GREEN.
6. **Mail.** ⟨`find <glass BK/coordination, value V/coordination, keyframes V/coordination, fourier docs/tranches> -newermt '2026-09-23 20:15'`⟩ → `INBOX.md` only (sibling sweep lines); ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`. GREEN.
7. **Four-verb line.** Row reads PARTIAL. Lawful; not a close.
8. **Goal at the bytes.** `.u` "every `UIA-F-n` row … cured at its cause, or routed to glass by id" stands at 95 of 256 (Repair 2), 161 owed incl. BROKEN F-9 and F-14. NOT MET.
9. **Figures reproduce.** Repair 2's `14 passed` ×2, `tsc=0`, `14/86` ×2 reproduce. 256 − 95 = 161 matches.
10. **Honest-RED adjudication.**
   - GLASS-VEIL-GREY — RELIEVED, addendum (c); owner glass (O-62).
   - DOCK-TRIGGER-CLIP — RELIEVED, addendum (d); owner glass (O-63 / I-46).
   - DOCK-SCROLL-MORPH — RELIEVED, F.W13 carried; owner glass (O-55).
   - Named baseline e2e set — RELIEVED by the spec Close line.
   - **G-u, 161 rows** — NOT RELIEVED. ⟨`grep -n 'ESC-R1-1\|ESC-R2-1\|F\.W15' COHESION.md`⟩ → no F.W14 ruling (the one ESC-R1-1 hit, `:2855`, is another wave's); the last addendum §0ck is X.P.W7. An escalation names an owner but is not the spec's relief.
   - **Close gate full e2e `--workers=1`** — UNREAD (MongoDB down, ESC-R2-2, owner act). Not credited; not relieved by spec text.
   - **G-p image-stage limb** — UNREAD, same instrument.
11. **Successors.** ⟨`grep -rln 'Opens after.*F\.W14' docs/tranches/X`⟩ → no wave spec; no successor blocked.

### Register (severity · claim · receipt · cure)
- **HIGH** · `.u` unmet: 161/256 UIA-F rows neither cured nor routed (incl. BROKEN F-9, F-14) · Repair 2 G-u line; no COHESION ruling · owner/COHESION: re-home the bulk to an authored successor wave (converts to a relieved residual) or author `.u` continuation sittings; widen bounds to `api/**` for F-35/F-83/F-112/F-39/F-46-server.
- **HIGH** · Close gate full e2e ×2 and G-p image limb unread since Repair 1 · ⟨`nc -z :27017`⟩ → 1, cursor → 500 · owner restores fourier MongoDB; then full e2e ×2 + G-p headed ×2.

### Verdict
**NOT-CONFORMANT.** Two HIGH unchanged from Check 2 (neither is curable by a repair seat; both await owner acts ESC-R2-1 / ESC-R2-2). Relieved honest-RED set: GLASS-VEIL-GREY · DOCK-TRIGGER-CLIP · DOCK-SCROLL-MORPH + the named baseline set. LEDGER stays **PARTIAL**; one event line appended. Gates reproduced: 4.

## RESUME 1 — Open (COHESION §0cl + F-W14.md addendum (e))

SEAT 0 (RESUME OPEN), `claude-opus-5-5`, 2026-09-23 ~20:45–21:05 EDT. Spec read whole (70 lines, addenda (a)–(e)). Record read: header through `## Unit plan` + `## Check 3` (the last section). COHESION §0cl read whole. fourier `m/w1-bump-migration` HEAD `3c66fb9` = origin (⟨`git ls-remote origin m/w1-bump-migration`⟩ → `3c66fb911b0a`). Load ⟨`uptime`⟩ → 38.82 / 39.06 / 40.32.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. ⟨`git status --porcelain docs/tranches/X/execution/ docs/tranches/V/coordination`⟩ → empty. Nothing inherited.

**alreadyDone (addendum (e), §0cl; commits verified in fourier `git log`):** `.m` `3c688d1` · `.t` `b5a650f` · `.g` · `.p` · `.h` (+ Repair 1 `c47dc5b`, `dc733e2`, `a8aa0a1`) · `.r` `4a87666`/`7808c65` · `.u` PARTIAL (88/256 dispositioned; Repair 2 `23352db`…`3c66fb9`) — RE-HOMED WHOLE to F.W14U (`fourier/waves/F-W14U.md`, LEDGER row QUEUED). None re-dispatched.

**Preconditions.** Owner-gated items RULED: `.h2` grant + `.p2` + G-u re-home + MongoDB restore = §0cl; honest-REDs GLASS-VEIL-GREY §0cf, DOCK-TRIGGER-CLIP §0cj, DOCK-SCROLL-MORPH (F.W13). Model law: Opus 5.5 every seat. Instrument: ⟨`nc -z localhost 27017`⟩ → `0` (fresh mongod up) · ⟨`curl :8000/api/gallery/cursor?limit=1`⟩ → `200` · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200`.

**E13 Step-0 (21:00 EDT).** ⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → `BL/ BK/ BJ/` (BL has no coordination/; BK stays the mail path). ⟨`find <path> -maxdepth 1 -type f -newer INBOX.md | wc -l`⟩ → value V/ `0` · V/coordination `0` · BK/coordination `0` · BL root `1` (`FORMATION-PROGRESS.md`, glass-internal cursor `f5682c03`, not addressed to value.js) · keyframes V/coordination `0` · atlas P/coordination `0` · fourier docs/tranches `0`. INBOX I-rows UNREAD → `0` (the one `| UNREAD` grep hit, `:406`, is a sweep line). Nothing to row; INBOX untouched.

## RESUME 1 — Baseline (BEFORE, fourier `3c66fb9`, read-only, ×2)

Served `http://localhost:3100`, `--workers=1`, chromium; logs scratchpad `fw14o-{vc,dpr}-{1,2}.log`. G-p run with `FW14_PHASE=zz-open-probe` (no committed crop overwritten; ⟨`ls e2e/screenshots/f-w14 | grep -c zz-`⟩ → `0`; fourier porcelain after → `?? .worktrees/`).

| gate | unit | BEFORE ×2 | reading |
|---|---|---|---|
| vc `:164` item 3 checkpoint-disclosure-body | `.h2` | ⟨`playwright test e2e/visual-checkpoint.spec.ts`⟩ → `:164` passed ×2 (the golden carried by Repair 1 `a8aa0a1`, 70370 → 68056 bytes) | **GREEN-BEFORE-CURE** (finding: the re-baseline already landed as a §0bt adjacent edit; §0cl grants it retroactively as `.h2`) |
| vc `:81 :102 :123` | Close | failed ×2 | baseline-RED (named set, formerly `:80 :101 :122`, +1 line) |
| vc `:145` item 8 admin banner | Close | failed ×2: `593 pixels (ratio 0.01) are different`, snapshot `checkpoint-admin-banner.png` | **RED outside the named set** — new since the fresh DB (unreadable Check 1–3); NOT in `.h2`'s grant |
| G-p `/equation` DPR 1 + 2 (`f-w14-dpr.spec.ts:206`, headed) | `.p2` | passed ×2 each | GREEN (hold) |
| G-p image stage DPR 1 + 2 (headed) | `.p2` | failed ×2 each: `page.waitForURL: Timeout 20000ms` at `openImage` `:194` | **RED by instrument**: ⟨`curl -F file=@assets/animals/golden-retriever.webp;type=image/webp :8000/api/images`⟩ → `500`; API log → `OSError: [Errno 30] Read-only file system: '/data'` (`image_storage.py:50` `_blob_dir`, `settings.blob_dir` default `/data/blobs`). The §0cl API restart ran without `BLOB_DIR`; fourier's own harness sets it (`scripts/e2e.sh:37` `export BLOB_DIR="${BLOB_DIR:-/tmp/fourier-e2e-blobs}"`). The fractional-stage 1-px miss itself is therefore not yet reproduced at this HEAD |
| typecheck · vitest | Close | banked Check 3 at this HEAD: `tsc=0` · `14/86` ×2 | GREEN (hold, not re-run: HEAD unmoved) |
| full e2e `--workers=1` | Close | unread since Repair 1 (MongoDB); first read at the close | owed |

**Instrument prescription (binding on `.p2` and the close):** the fourier API must serve with `BLOB_DIR` set (restart it with `BLOB_DIR=/tmp/fourier-e2e-blobs` beside the existing `MONGO_URI`, or run e2e through `scripts/e2e.sh`), then `POST /api/images` → 200 is read before any image-stage gate. A process restart is not a repo write.

## RESUME 1 — Unit plan

Two units, Opus 5.5 each (§0cl; no Fable/design seat named). Strictly serial per addendum (e): **[`F.W14.h2`] → [`F.W14.p2`]**, then the close. Concurrency 1. fourier-relative paths unless `vjs:`; each unit also writes its receipt in `vjs:docs/tranches/X/execution/C/F-W14.md` and minimal LEDGER cells/lines. Never: glass-ui, sibling repos, `scripts/dev/dev.sh`, test/assertion deletion, `api/**` (granted to F.W14U only).

| id | spec section | writable | gates |
|---|---|---|---|
| F.W14.h2 | addendum (e) `[F.W14.h2]` (F-W14.md:58-60) + §0cl | `web/e2e/visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-*.png` only | diff read + confined to `/equation` left-panel titles/labels/two slider rows (`.h` OA-45 render); vc `:163`/`:164` GREEN ×2 |
| F.W14.p2 | addendum (e) `[F.W14.p2]` (F-W14.md:61-64) + §0bw `.p` | `web/src/components/visualization/composables/useCanvasSetup.ts`, its consumers (`BasisCanvas.vue`, `equation/ConvergencePlot.vue`, `equation/FrequencyGraph.vue`) only where the one rule moves them, `web/e2e/f-w14-dpr.spec.ts`, `web/e2e/screenshots/f-w14/**` | G-p headed ×2 at DPR 1 and 2, `/equation` + image stage, rest/resize/DPR change/zoom, backing size = the same one rounding rule; stroke-edge crop ≤ 2 device px; falsifier (the 798.59-px stage 1-px miss) RED pre-cure |

Close (after `.p2`): G-u counts the 88 dispositioned and cites F.W14U for the 168; vue-tsc 0 · vitest ×2; full e2e `--workers=1` ×2 with load (API with `BLOB_DIR`), REDs ⊆ named baseline set (vc `:81 :102 :123` · fullscreen `:40` · contrast-floor · gallery-admin-a11y · `G-F12-3-LOAD`) + GLASS-VEIL-GREY · DOCK-TRIGGER-CLIP · DOCK-SCROLL-MORPH; vc `:145` (admin banner, 593 px) must be read and adjudicated there (data fixture seeded by the spec's own setup, never by hand; not a granted golden).

## RESUME 1 — Unit receipts

### F.W14.h2

SEAT `.h2`, SERVED MODEL: `claude-opus-5-5`, 2026-09-23 ~20:50 EDT. Spec read whole (70 lines). Record read: header through `## RESUME 1 — Unit plan`; `a8aa0a1` located by grep (`:709-735`). COHESION §0cl read whole (`:3253-3270`, the file end). fourier `m/w1-bump-migration` HEAD `3c66fb9`. Load ⟨`uptime`⟩ → `30.26 36.59 39.17`.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. Nothing in the writable set was dirty; nothing inherited.

**Act 1: the `a8aa0a1` golden diff read.** The grant's glob has one file, `checkpoint-disclosure-body-chromium-darwin.png`. ⟨`git log --oneline -- <png>`⟩ → `a8aa0a1` (Repair 1, the §0bt adjacent edit), then `216ffbd` (F.W13.e, §0bm). ⟨`git show a8aa0a1^:<png>` / `a8aa0a1:<png>`⟩ gives old and new, each 400×632, 70370 → 68056 bytes. ⟨`cmp new.png <HEAD png>`⟩ → identical: HEAD carries `a8aa0a1`'s bytes. Both PNGs and ⟨`magick compare -metric AE -fuzz 10% old new diff.png`⟩ → `49632 (0.196)` were read as images:
- **Changed content:**
  - "Function — f(x)" became the heading-rung title `Function` with a mono `f(x)` subtitle.
  - "Controls — harmonics & display" became `Controls` with a mono `harmonics & display` subtitle.
  - The field labels (Expression, Domain, Presets, Harmonics, Display terms) moved onto `text-small`. The Harmonics and Display-terms subtitles became inline, and the latter is ellipsised.
  - The two slider rows now show a visible thumb with the value field inline on the label line.
- **Everything else is vertical reflow, measured by shifted-crop difference (PIL, threshold 25):**
  - Function-card body, old rows 55–350: the residual is lowest at dy = +8, at 3299 px. What remains sits only on the label text rows (bands 50–70, 140–150, 240–250) and on the Presets chips.
  - Presets chips, old rows 265–350: dy = +10 leaves `8` px, so they are a pure translation.
  - The Controls card below moves down with the taller title, and the Notation row is pushed below the 632-px clip.
- **Result: confined.** No pixel changes outside `/equation`'s left-panel Function and Controls cards. The change is exactly `.h`'s OA-45 render: card titles and subtitles on the type scale, labels, and two slider rows, plus the reflow they cause. There is no colour, radius or chip-styling change.
- The two counts differ because they use different metrics. Playwright's comparator counted `26812 px (0.11)` in Repair 1; ImageMagick AE at fuzz 10% counts `49632`. Both describe the same region.

**Act 2: the render at HEAD against the committed golden, served ×2.** The test sits at `:164`. ⟨`playwright test e2e/visual-checkpoint.spec.ts --list`⟩ → `:164:5 item 3`. `:163` is the line above it, and ⟨`playwright test …:163`⟩ → `No tests found`, so `:163`/`:164` name one test. I ran ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/visual-checkpoint.spec.ts:164 --project=chromium --workers=1`⟩ twice:
- run 1 → `1 passed (2.8s)`
- run 2 → `1 passed (1.7s)`

The render has not diverged since `a8aa0a1`, so the spec's conditional re-baseline (`--update-snapshots`) was **not** run. After both runs, ⟨`git status --porcelain`⟩ → `?? .worktrees/` only.

**Act 3: the §0cl grant recorded.** The `.h2` grant, which is the same class as §0bm ESC-F13a-1, **ratifies `a8aa0a1`**, the Repair 1 adjacent-edit re-baseline, as `.h2`'s landing. No new commit touches the PNG. No other golden moved. vc `:145` (admin banner, 593 px RED) and `:81 :102 :123` were not granted and were not touched.

| gate | BEFORE (seat-0 baseline, `3c66fb9`) | AFTER (this seat, `3c66fb9`) |
|---|---|---|
| diff read, confined to `/equation` left-panel titles, labels and two slider rows (`.h` OA-45) | read in Repair 1, not ratified | **GREEN**: read again from the bytes; changes limited to the two cards plus pure reflow (chips dy +10 → 8 px residual) |
| vc `:163`/`:164` checkpoint-disclosure-body | passed ×2 | **GREEN ×2**: `1 passed` ×2, served `:3100`, `--workers=1` |

**Commits.** fourier: none, because the golden landed as `a8aa0a1` and this seat ratifies it. value.js: this record, one commit, plus a LEDGER event line.
**Adjacent edits.** None. **Residuals.** None owned by `.h2`. vc `:145` is adjudicated at the close, per the plan. **Escalations.** None.

### F.W14.p2

SEAT `.p2`, SERVED MODEL: `claude-opus-5-5`, 2026-09-23 ~20:40–21:00 EDT. Spec read whole (70 lines, addendum (e) `[F.W14.p2]` + §0bw `.p`). Record read: `## RESUME 1` open/baseline/plan + `.h2`'s receipt; `.p`/Close/Repair 1 G-p lines located by grep (`:258-296`, `:614-629`). COHESION §0cl read whole (`:3253-3270`, the file end). fourier `m/w1-bump-migration` HEAD `3c66fb9` at open. Load ⟨`uptime`⟩ → `31.95 36.57 38.91`.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; nothing inherited in the writable set.

**Act 1 — the instrument (seat-0 prescription).** The API (pids 47981/47991, cwd fourier, no `MONGO_URI`/`BLOB_DIR` in env ⟨`ps eww`⟩; `api/config.py:9,17` defaults `mongodb://localhost:27017/fourier`, `/data/blobs`) was stopped and restarted as ⟨`MONGO_URI=mongodb://localhost:27017/fourier BLOB_DIR=/tmp/fourier-e2e-blobs COMPUTE_RATE_LIMIT=1000 WRITE_RATE_LIMIT=1000 uv run uvicorn api.main:app --host 0.0.0.0 --port 8000`⟩ — the values `scripts/e2e.sh:27-37` sets (the two rate limits ride along for the close's full e2e). ⟨`curl -F file=@assets/animals/golden-retriever.webp … :8000/api/images`⟩ → `200` · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200`. Process restart, no repo write.

**Act 2 — the named miss, sought on the old gate.** ⟨`FW14_PHASE=zz-p2-pre BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-dpr.spec.ts --project=chromium --workers=1 --headed --repeat-each=3`⟩ → `12 passed (34.6s)`: the intermittent 1-px miss (Close: `1599×1452 ≠ (798.59375×2)`, `799 ≠ 798.34375×1`) did not recur in 3 headed repeats (at this HEAD the image stage rests at 1018×806 CSS, not 798.59). The old `fits()` accepted two products (`round(extent×dpr)` or the edge-snapped span); the composable had three sizing paths (`setupCanvas` = `round(rect×dpr)`; entries = `round(css×dpr)`, or the device box only when within 1 px of it). Scratch crops removed (⟨`ls … | grep -c ^zz-p2-pre`⟩ → `4`, deleted).

**Act 3 — the gate onto the rule, then the falsifier.** `web/e2e/f-w14-dpr.spec.ts`: `readCanvases` now reads each visible canvas's CSS content box and `devicePixelContentBoxSize` off one fresh `ResizeObserver` entry (the entry shape the composable sizes from); `fits()` calls `backingSize` **imported from `useCanvasSetup`** (the rule itself, not a restatement); `misfits` prints expected vs css/device/client. Falsifier bytes: HEAD's composable with only `backingSize`/`BoxSize` exported beside it, unused by its logic (so the gate compiles against pre-cure behaviour). ⟨`FW14_PHASE=zz-p2-fals … f-w14-dpr.spec.ts --headed --workers=1`⟩ → **`4 failed`** (all four: DPR 1/2 × `/equation`/image). Samples: `rest: canvas-container > canvas.canvas-el 1018×806 ≠ 2036×1612 [css 1018×806 @ dpr 1; device 2036×1612]` · `DPR 2→1: … 798×726 ≠ 1596×1452` · `rest: convergence-container > canvas.absolute 988×535 ≠ 1976×1069 [css 988×534.640625]` · `zoom 125% (dpr 2.5): overflow-x-auto > canvas.block 1728×300 ≠ 1382×240`. Scratch crops deleted.

**Act 4 — the cure (`useCanvasSetup.ts`).** One exported rule, `backingSize(css, device, dpr)`: the device-pixel box when present, else `Math.round(cssBox × dpr)` per axis. `apply()` takes the CSS box + the device box and sizes only through it. Entries (content-box observer + device-pixel-box observer) pass `entry.devicePixelContentBoxSize[0]` where supported. `setupCanvas()` re-observes the canvas where device boxes exist (the device box is only ever read off an entry, delivered before the next paint) and falls back to the rect through the same rule elsewhere; `onMounted` arms the observers (and calls `setupCanvas` only on the fallback). Retired: the rect product, the entry product, and the within-1-px "snapped" guard. Consumers: ⟨`grep -rnE 'devicePixelRatio|\.dpr\b|Math\.round\([^)]*dpr' web/src | grep -v useCanvasSetup.ts`⟩ → empty — no consumer carries its own rounding, so `BasisCanvas.vue`, `ConvergencePlot.vue`, `FrequencyGraph.vue` are unmoved. ⟨`npx vue-tsc --noEmit`⟩ → exit 0 · ⟨`npx vitest run`⟩ → `86 passed (86)`.

**Consequence recorded (the ruled rule under DPR emulation).** `.p` (`:258`) measured that headed Chromium reports the **physical** 2× screen's device box under an emulated DPR; `.p` therefore guarded the device box off when it disagreed with `css×dpr`. §0cl's rule takes the device box whenever present, so at an emulated DPR 1 (or 1.25) on this 2× display every canvas now holds the physical-resolution bitmap (e.g. 2036×1612 for a 1018×806 box) with the transform scaled to match — supersampled, never upscaled. On a real display the device box equals the page's DPR. The stroke-edge limb (≤ 2 device px, read at the launch DPR) stays GREEN, and the committed 1× crop reads a crisp 1-px grid.

**Act 5 — G-p on the settled bytes, headed ×2.** ⟨`FW14_PHASE=after BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-dpr.spec.ts --project=chromium --workers=1 --headed`⟩ → run 1 `4 passed (14.0s)` · run 2 `4 passed (13.3s)`; load after run 2 ⟨`uptime`⟩ → `40.52 35.93 38.15`. Crops rewritten under `after-`: `after-dpr-epicycles-1x-{rest,figure}.png` and `…-2x-figure.png` changed bytes; `…-2x-rest.png` byte-identical (unchanged in porcelain).

| gate | BEFORE | AFTER (fourier `2b86203`) |
|---|---|---|
| G-p `f-w14-dpr.spec.ts:206` headed, DPR 1 + 2, `/equation` + image: rest, resize, DPR change, zoom | image RED by instrument (seat 0, `POST /api/images` 500); old gate `12 passed` ×3-repeat after Act 1 (miss not recurring) | **GREEN ×2** (`4 passed` ×2) |
| backing = the one rule, gate asserts that same rule | three sizing paths; gate = two raw products | **GREEN**: one `backingSize`, imported by the gate |
| stroke-edge crop ≤ 2 device px | GREEN (`.p`) | **GREEN ×2** (asserted in the image limb) |
| falsifier RED pre-cure | — | **RED 4/4** on pre-cure logic under the rule gate (Act 3); the literal 798.59-px/1599 instance not reproduced in 3 repeats at this HEAD (stage rests at 1018 CSS) |

**Commits.** fourier `2b86203` (useCanvasSetup.ts + f-w14-dpr.spec.ts + 3 crops; one meaning), pushed: ⟨`git ls-remote origin m/w1-bump-migration`⟩ → `2b8620322398`. value.js: this receipt + LEDGER event line.
**Inherited paths.** None. **Adjacent edits.** None. **Residuals.** The literal fractional-stage instance is covered by the rule (one path, no product), not re-produced verbatim; the close re-reads G-p. API now serves with `BLOB_DIR=/tmp/fourier-e2e-blobs` for the close. **Escalations.** None.

## RESUME 1 — Close

SEAT CLOSE (verify-only), `claude-opus-5-5`, 2026-09-23 ~20:55–21:20 EDT. Spec read whole (70 lines, addenda (a)–(e)). Record read: header through `## RESUME 1 — Unit plan`, the `.h2`/`.p2` receipts, and the prior `## Close` (by sed range). fourier `m/w1-bump-migration` HEAD `2b86203` = origin (⟨`git ls-remote origin m/w1-bump-migration`⟩ → `2b8620322398`).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. The record and LEDGER were clean. Nothing inherited.

**Instrument.** ⟨`nc -z localhost 27017`⟩ → `0` · ⟨`curl :8000/api/gallery/cursor?limit=1`⟩ → `200` · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl -F file=@…golden-retriever.webp :8000/api/images`⟩ → `200` (the API is serving with `BLOB_DIR`, as `.p2` restarted it) · ⟨`curl :3100`⟩ → `200`.

### Act 1 — commits against their writable sets
- `.h2`: fourier none new. `a8aa0a1` ⟨`git show --stat`⟩ → only `web/e2e/visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-darwin.png` (70370 → 68056), which is the granted glob. value.js `c732a320` → this record and `LEDGER.md` only. In bounds.
- `.p2`: fourier `2b86203` ⟨`git show --stat`⟩ → `useCanvasSetup.ts`, `web/e2e/f-w14-dpr.spec.ts`, and 3 PNGs under `web/e2e/screenshots/f-w14/`. value.js `5f3ee569` → this record and `LEDGER.md` only. In bounds.
- Landed-wrong: none.

### Act 2 — gates BEFORE → AFTER (fourier `2b86203`, write-then-measure, ×2)

| gate | BEFORE (RESUME 1 baseline, `3c66fb9`) | AFTER (this seat) | verdict |
|---|---|---|---|
| vue-tsc | `tsc=0` (banked) | ⟨`npx vue-tsc -b --force`⟩ → `tsc=0` ×2 | GREEN |
| vitest | 14/86 (banked) | ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2 | GREEN |
| `.h2` vc `:164` checkpoint-disclosure-body | passed ×2 | ⟨`playwright test e2e/visual-checkpoint.spec.ts:164 --workers=1`⟩ → `1 passed (1.4s)` · `1 passed (1.2s)` | GREEN |
| `.p2` G-p `f-w14-dpr.spec.ts`, **headed**, DPR 1 + 2, `/equation` + image | image limb RED by instrument | ⟨`FW14_PHASE=zz-close … --headed --workers=1`⟩ → `4 passed (16.9s)` · `4 passed (10.5s)` | GREEN (the spec's instrument) |
| full e2e `--workers=1` (headless, served `:3100`) | unread since Repair 1 | ⟨`BASE_URL=http://localhost:3100 npx playwright test --workers=1`⟩ → run 1 **25 failed · 3 skipped · 170 passed (7.3m)**, load 39.13 → 16.58 · run 2 **24 failed · 3 skipped · 171 passed (6.9m)**, load 15.87 → 24.92. Run 2's set is run 1's minus fullscreen `:40` | **RED: 14 outside the named set, ×2** |

**The full-e2e REDs by id (⟨`diff` of the two sorted failure lists⟩ → only `fullscreen.spec.ts:40:5` differs).**
- **Named baseline set (10 ×2, plus fullscreen `:40` in run 1).** contrast-floor `:82`×2 and `:128` · gallery-admin-a11y `:91 :103 :114 :125` · vc `:81 :102 :123`. Relieved.
- **f-w14-uia ×12, ×2** (`:65 :76 :125 :166 :236 :250 :277 :304 :457 :540 :634 :661` = UIA-F-2/3/7/17/16/15/18/19/48/47/12/5). The same error each time: ⟨⟩ → `the served API holds a saved visualization — Expected: > 0, Received: 0` (`f-w14-uia.spec.ts:48`, `firstSavedViz` reads `GET /api/visualizations?limit=1`). These falsifiers depend on data that lived in the old Homebrew database. Addendum (e) orders such fixtures "seeded by the spec's own setup, never by hand". No unit seeded them, and no `.h2`/`.p2` bound covers it. **RED, outside the named set.** The file belongs to `.u`, which is re-homed whole to F.W14U. F.W14U's bounds include `web/e2e/**`, but it opens only after F.W14 CLOSED.
- **f-w14-dpr `:228` (G-p at DPR 2, image), headless, ×2**: ⟨⟩ → `rest: stroke edge spans 4 device px of transition (crop 2×, 30 crossings)`. The spec's G-p instrument is headed, and headed reads GREEN ×2 above. In the headless full suite, however, the stroke-edge limb exceeds its ≤ 2 device px bound at DPR 2. `.p2`'s receipt carries only the headed reading. **RED, outside the named set.** This carries forward from the prior Close's LW-3, now in the stroke limb rather than the size limb.
- **vc `:145` item 8 admin banner, ×2**: ⟨⟩ → `593 pixels (ratio 0.01) are different`, `checkpoint-admin-banner.png`. The seat-0 baseline read it first, on the fresh DB. It is not in the `.h2` grant. **RED, outside the named set.** The plan left it for this close to adjudicate. Verify-only, I find no relief in the spec text: neither a granted golden nor a spec-setup seed exists.
- The honest-RED ids GLASS-VEIL-GREY, DOCK-TRIGGER-CLIP and DOCK-SCROLL-MORPH did not fire as e2e failures. They stand relieved per addenda (c) and (d) and F.W13.
- G-u: `.u` dispositioned 88 of 256 rows. The 168 owed are cited to F.W14U (`fourier/waves/F-W14U.md`), per the close relief.

### Act 3 — §Verification Artefacts
The spec has no separate section. Its Close line ("vue-tsc 0, vitest, e2e at `--workers=1` (REDs = the named baseline set), served-page gates re-read by the check seat") was run as written above. Run dirt: ⟨`git status --porcelain`⟩ after the runs → 47 ` M web/e2e/screenshots/f-w14/*.png` (the full suite's default `after-` phase). I restored these to HEAD by exact path from scratchpad `fw14c-rundirt.txt` and deleted the 4 `zz-close-*` scratch crops. ⟨`git status --porcelain`⟩ → `?? .worktrees/` only.

### Act 4 — E13
⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → `BL/ BK/ BJ/`. ⟨`find <path> -maxdepth 1 -type f -newermt '2026-09-23 20:40'`⟩ → value V/ none · V/coordination none · glass BK/coordination none · BL `FORMATION-PROGRESS.md` only (glass-internal cursor `8a965766`, not addressed to value.js or fourier) · keyframes V/coordination none · fourier docs/tranches none. atlas has no `docs/tranches` path. ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`. No UNREAD mail in scope.

### Residuals (named owners)
- **R-C2-1** f-w14-uia ×12 need a saved-visualization fixture seeded by the spec's own setup (addendum (e)). Owner: COHESION/owner. One option is a ruling that homes the seed into F.W14U (whose bounds carry `web/e2e/**` and `api/**`) and relieves F.W14's close of these 12. The other is a bounded `.s` unit in F.W14.
- **R-C2-2** f-w14-dpr `:228` headless stroke edge, 4 device px at DPR 2 ×2 (headed GREEN ×2). Owner: a `.p` successor, or a COHESION ruling on whether the gate's headless run is in scope.
- **R-C2-3** vc `:145` admin-banner golden, 593 px ×2 on the fresh DB. Owner: COHESION/owner, which can grant a re-baseline (as §0cl did for `.h2`) or order a seeded fixture.
- **R-C2-4** G-u, 168 UIA-F rows. Owner: F.W14U (authored).
- **R-C2-5** honest-RED GLASS-VEIL-GREY (O-62), DOCK-TRIGGER-CLIP (O-63), DOCK-SCROLL-MORPH (O-55). Owner: glass BK/BL.
- Carried: prior Close R-C-6 (`.m` r1/r2, `.t` R-t-1, `.h` BL-FW14H-1/2, `.r` R-r-5). Owners as named there.

### Escalations
- **ESC-C2-1**: three REDs outside the named baseline set ×2 (R-C2-1/2/3), none within this close's verify-only remit or the `.h2`/`.p2` bounds. F.W14U's "Opens after: F.W14 CLOSED" is therefore blocked until COHESION rules relief or mints a cure unit.

### Commit roster (RESUME 1)
fourier: `a8aa0a1` (ratified as `.h2`) · `2b86203` (`.p2`). value.js: `c732a320` (`.h2` receipt) · `5f3ee569` (`.p2` receipt) · this close (record and LEDGER).

**Four-verb line.** F.W14: **PARTIAL**. `.h2` and `.p2` landed, and their gates are GREEN ×2 on the spec's instruments. vue-tsc 0 and vitest 86/86 hold ×2. The Close gate "e2e REDs = the named baseline set" reads RED ×2 with 14 failures outside the set, from 3 causes (R-C2-1/2/3). IMPLEMENTED is withheld. The spec designates no VERIFIED stamp for this seat.

## RESUME 1 — Check 1 (L-20 pass 1, fresh adversarial)

SEAT CHECK, SERVED MODEL: `claude-opus-5-5`, 2026-09-23 ~21:10–21:40 EDT, verify-only. Spec read whole (70 lines, addenda (a)–(e)). Record read: `## RESUME 1` Open → Close (`:865-1020`). fourier `m/w1-bump-migration` HEAD `2b86203`; value.js close commit `734a42df`. Crash-recovery: ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. Instrument: ⟨`nc -z localhost 27017`⟩ → succeeded · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200` · ⟨`curl -F file=@…golden-retriever.webp :8000/api/images`⟩ → `200` · ⟨`curl ':8000/api/visualizations?limit=1'`⟩ → `{"items": [], …}` (no saved visualization in the fresh DB). Load at open ⟨`uptime`⟩ → `20.46 22.95 27.47`.

### Axes (1)–(9)
- **(1) Claimed GREENs reproduce (fourier `2b86203`, served `:3100`, `--workers=1`).**
  - vue-tsc: ⟨`npx vue-tsc -b --force`⟩ → exit `0` ×2.
  - vitest: ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2.
  - `.h2` vc `:164`: ⟨`playwright test e2e/visual-checkpoint.spec.ts:164 :145 --project=chromium`⟩ → `:164` `1 passed` ×2.
  - `.p2` G-p headed: ⟨`FW14_PHASE=zz-ck1 playwright test e2e/f-w14-dpr.spec.ts --headed`⟩ → `4 passed (11.0s)` · `4 passed (10.1s)`.
  - **5 of 5 claimed GREENs reproduce.**
- **(2) Bounds.** ⟨`git show --stat 2b86203`⟩ → `useCanvasSetup.ts`, `f-w14-dpr.spec.ts` and 3 `after-dpr-*` PNGs, all inside `.p2`'s set. ⟨`git show --stat a8aa0a1`⟩ → the one granted golden. value.js `c732a320` / `5f3ee569` / `734a42df` → only the record and `LEDGER.md`. `scripts/dev/dev.sh` is untouched by every commit. **Clean.**
- **(3) Masking.** I read the `2b86203` diff: no try/catch, skip, allowlist or node_modules patch.
  - The `fits()` gate now imports `backingSize` from the composable. It no longer checks the §0bw literal `width === round(clientWidth × dpr)`.
  - That is the change addendum (e) orders ("the gate compares the backing size against the same rule, not a raw product"). It is lawful, not a narrowed assertion. It is recorded as INFO-1 below.
- **(4) Commit families.** `.p2` is one commit with one meaning. `.h2` has no new fourier commit, because `a8aa0a1` is ratified. **Clean.**
- **(5) E-3.** ⟨`git diff --stat 734a42df~3..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/ scripts/dev/dev.sh`⟩ → empty. **Held.**
- **(6) Mail.** ⟨`grep UNREAD INBOX.md`⟩ gives 4 I-row hits: I-30, I-31, I-32, I-35. Each is prose mentioning a prior or quoted status, not an UNREAD status cell.
  - ⟨`find … -newermt '2026-09-23 21:05'`⟩ → only glass `BL/FORMATION-PROGRESS.md`, which is glass-internal. **Clean.**
- **(7) Four-verb.** The close wrote PARTIAL and withheld IMPLEMENTED. That is lawful given the REDs below.
- **(8) Goal at the bytes.** `.h2`/`.p2` hold. OA-44's crispness at DPR 2 is NOT met in the headless suite: the stroke edge spans 4 device px. See D-2.
- **(9) Figures reproduce.** ⟨`FW14_PHASE=zz-ck1 BASE_URL=… npx playwright test --workers=1`⟩ → `24 failed · 3 skipped · 171 passed (6.9m)`, load 20.5 → 15.8. That is the close's run 2 exactly: the same 23 ids, with contrast-floor `:82` counted twice.
  - Run dirt: 47 `zz-ck1-*` crops, which git ignores. I deleted them by name. ⟨`git status --porcelain`⟩ → `?? .worktrees/`.

### (10) Honest-RED adjudication (at the spec bytes, addendum (e) "Close relief")
The Close gate: full e2e `--workers=1` REDs must fall within the named baseline set plus GLASS-VEIL-GREY, DOCK-TRIGGER-CLIP and DOCK-SCROLL-MORPH.

**Relieved:**
- contrast-floor `:82 :128`
- gallery-admin-a11y `:91 :103 :114 :125`
- vc `:81 :102 :123`
- fullscreen `:40` (run 1 only)
- GLASS-VEIL-GREY (O-62, §0cf)
- DOCK-TRIGGER-CLIP (O-63, §0cj)
- DOCK-SCROLL-MORPH (O-55, F.W13)
- G-u, the 168 rows re-homed to F.W14U. This is the spec's own relief, and the close counts 88 and cites F.W14U.

**Unrelieved:**
- **f-w14-uia ×12**: ⟨`playwright test e2e/f-w14-uia.spec.ts`⟩ → `12 failed · 20 passed`, each at `:48` "the served API holds a saved visualization — Received: 0". The addendum *orders* these fixtures "seeded by the spec's own setup, never by hand". That is a duty, not a relief, and no unit discharged it.
- **f-w14-dpr `:228`, headless**: ×2 → `1 failed · 3 passed`, with "rest: stroke edge spans 4 device px of transition (crop 2×, 30 crossings)". Neither the named set nor any id covers it.
- **vc `:145`, admin banner**: ×2 → `593 pixels (ratio 0.01)`. It is not in `.h2`'s grant, and no seed exists.

The close's residual register names owners only as "COHESION/owner" (R-C2-1/2/3, ESC-C2-1). No ruling exists: ⟨`grep -n 'ESC-C2-1\|§0cm' COHESION.md`⟩ → empty.

### Register
| id | severity | claim | receipt | cure |
|---|---|---|---|---|
| D-1 | HIGH | Close gate RED ×2: f-w14-uia ×12 fail on an unseeded saved-visualization fixture. Addendum (e) orders seeding by the spec's own setup, and it is undone | ⟨`curl ':8000/api/visualizations?limit=1'`⟩ → `{"items": []}`; f-w14-uia `12 failed` at `:48` | Either a bounded F.W14 unit seeds the fixture in the spec's own setup (a `beforeAll` POST through the API or a playwright globalSetup), or a COHESION ruling re-homes the seed plus these 12 into F.W14U and relieves F.W14's Close |
| D-2 | HIGH | Close gate RED ×2: f-w14-dpr `:228` (DPR 2, image) stroke edge spans 4 device px headless, over the ≤ 2 bound. OA-44's crispness is unmet in the suite the Close gate runs | headless `1 failed` ×2 · headed `4 passed` ×2 | A `.p` successor finds why the headless 2× stroke blurs (bitmap vs device box under headless emulation), or COHESION rules the headless run out of G-p's scope |
| D-3 | HIGH | Close gate RED ×2: vc `:145` admin-banner golden, 593 px, on the fresh DB. It is outside every grant | `:145` failed ×2, `593 pixels (ratio 0.01)` | Read the diff first. If it is data-driven, seed the banner's fixture in the spec's setup; if it is render-driven, an owner-granted re-baseline like §0cl's `.h2` |
| INFO-1 | INFO | G-p's size limb now imports the implementation's `backingSize`. Under DPR emulation, an emulated DPR 1 holds the physical 2× bitmap (supersampled, not upscaled). The literal 798.59-px miss was not re-produced | `2b86203` diff; `.p2` receipt Act 3 (falsifier RED 4/4) | None. Addendum (e) prescribes the same-rule gate |

**Successor conjuncts.** F.W14U "Opens after: F.W14 CLOSED" is **RED**. F.W14U is lawfully blocked until D-1..D-3 are cured or ruled. No other wave names F.W14 CLOSED: ⟨`grep -rl 'F.W14 CLOSED' fourier/waves`⟩ → `F-W14U.md` only.

**Verdict: NOT-CONFORMANT.** 3 HIGH, all Close-gate REDs outside the named set with no spec relief, plus 1 INFO. 5/5 claimed GREENs reproduce. The LEDGER status cell stays PARTIAL, and an event line is appended. Escalation carried: ESC-C2-1, which needs a COHESION ruling or a cure unit for D-1..D-3.

## RESUME 1 — Repair 1

SEAT REPAIR (round 1), SERVED MODEL: `claude-opus-5-5`, 2026-09-23 ~21:20–21:55 EDT. Spec read whole (70 lines, addenda (a)–(e)). Record read: header → `## Unit plan`, `## RESUME 1` Open → Unit plan, `## RESUME 1 — Close`, `## RESUME 1 — Check 1` (by sed range). Crash-recovery: ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; nothing inherited. Writable set: the RESUME 1 plan's rows plus `.u`'s `web/e2e/**` (additive) for the addendum (e) seeding duty; never `api/**`, glass-ui, goldens outside a grant.

### D-1 (HIGH) — f-w14-uia ×12 on the unseeded saved-visualization fixture → CURED
- **Cause at the bytes.** `f-w14-uia.spec.ts:43-51` (`firstSavedViz`) only READ `GET /api/visualizations?limit=1`; nothing in the spec made the row exist. On the fresh DB (§0cl) ⟨`curl ':8000/api/visualizations?limit=1'`⟩ → `{"items": [], …}` (re-read at this seat's open) → 12 tests fail at `:48`.
- **Cure (fourier `d3b2652`, `web/e2e/f-w14-uia.spec.ts` only).** A file-level `test.beforeAll` (the spec's own setup, addendum (e)) on a `playwright.request` context at the project's `baseURL`: it reuses the first public visualization if one exists; otherwise it seeds one the way the app does — `POST /api/sessions` → `POST /api/images` (multipart, `assets/animals/golden-retriever.webp`) → `POST /api/images/<slug>/extract-contour` (the frontend-default settings `visualization-crud.spec.ts` mirrors) → `POST /api/visualizations` `{visibility: "public", …}` → `201`. `firstSavedViz()` now returns the seeded row (the `:48`-message assertion kept, as `not.toBeNull`). No DB write by hand, no `api/**` edit, no skip.
- **A race the seeded run exposed, cured in the same file.** Run 2 read `31 passed · 1 failed`: UIA-F-47 `:627` — the page toast `Error Draft has no contour`. `stores/workspace.ts` saves the draft's contour through a 1000 ms debounce (`scheduleDraftSave`) after extraction; the test's fixed `waitForTimeout(1500)` raced it. The wait now polls the stored draft itself (`indexedDB` `fourier-drafts`/`drafts`, key = the image slug) until it carries a contour (30 s bound). The assertions after it are unchanged.
- **Gate re-reading (served `:3100`, `--workers=1`, chromium).** ⟨`playwright test e2e/f-w14-uia.spec.ts`⟩ — run 1 on the EMPTY DB (the seed path; after it ⟨`curl …?limit=5`⟩ → one public row `crisp-falling-garnet-condor`, image `young-running-sapphire-lark`) → `32 passed (1.2m)`; run 2 (reuse, pre-race-cure) → `31 passed · 1 failed` (F-47, above); after the race cure → ⟨`-g UIA-F-47`⟩ `1 passed (4.4s)`, then the whole file `32 passed (1.2m)` · `32 passed (1.1m)`. BEFORE (Check 1, banked ×2): `12 failed · 20 passed`.

### D-2 (HIGH) — f-w14-dpr `:228` headless, stroke edge 4 device px at DPR 2 → ESCALATED (cause measured; the only app-side cure is the guard §0cl retired)
- **Reproduced.** ⟨`FW14_PHASE=zz-rp1 playwright test e2e/f-w14-dpr.spec.ts` (headless)⟩ → `1 failed · 3 passed (11.6s)`, `rest: stroke edge spans 4 device px of transition (crop 2×, 30 crossings)`.
- **The crop's bytes.** ⟨PIL over `zz-rp1-dpr-epicycles-2x-rest.png` (320×240)⟩ → `2x2 blocks identical 18080 of 19200` (94 %); a scan row reads `(32, 4.0), (33, 4.0), (34, 5.0), (35, 5.0)` — every device pixel doubled. The committed headed crop `after-dpr-epicycles-2x-rest.png` reads `(33, 7.2), (34, 8.2)` — no doubling. So headless DPR 2 paints a 1× bitmap stretched 2×.
- **The cause (bounded probe, headless Chromium, `/equation`, `deviceScaleFactor` 1 then 2).** ⟨fresh RO entry on `.convergence-container canvas`⟩ → DPR 1: `{"dpr":1,"w":988,"css":988,"dev":988}` · DPR 2: `{"dpr":2,"w":988,"cw":988,"css":988,"dev":988,"mm":true}`. Under headless emulation `devicePixelRatio` = 2 and `(resolution: 2dppx)` matches, but `devicePixelContentBoxSize` reports the 1× physical box. §0cl's rule (`backingSize`: the device box whenever the engine reports one) therefore allocates 988 at DPR 2, and G-p's size limb, which imports that same rule, passes by construction. Headed on this retina host the device box is the physical 2×, so headed reads GREEN — the same engine disagreement, the other way (INFO-1).
- **Why no cure lands here.** The one app-side cure is to prefer `round(css × dpr)` when the device box and DPR disagree. §0cl and `.p2`'s code comment (`useCanvasSetup.ts`) name that exact guard as retired ("no guard that trades the device box for a product when the two disagree"). Pinning the file to `headless: false` would only pick the instrument where the defect cannot be seen. Real displays report a device box that agrees with DPR, so this is an emulation artefact that the ruled rule cannot tell apart from a real display. Which fix is right is COHESION's decision: (a) rule G-p's headless run out of scope, since the spec's G-p instrument is "headed ×2", or (b) amend §0cl's rule so the device box is trusted only when it agrees with `round(css × dpr)` to within 1 px.
- Scratch crops `zz-rp1-*` deleted by name (git-ignored).

### D-3 (HIGH) — vc `:145` admin-banner golden, 593 px → ESCALATED (the diff is from a render change, and re-baselining needs the owner's grant)
- **Diff read first, as the cure orders.** ⟨`playwright test e2e/visual-checkpoint.spec.ts:145`⟩ → `1 failed`. Expected, actual and diff images were viewed. The banner data is **stubbed** (`stubAdminApi`): ENTRIES 128 · FEATURED 7 · SAVED 41 · VIEWS 9.3K · LIKES 452 · STORAGE 70.0 MB, the same in both images. The diff paints red on the "Admin Mode" title **only**, which is now a smaller rung.
- **Cause.** ⟨`git log --oneline 3c688d1..HEAD -- web/src/components/visualization/gallery/GalleryAdminBanner.vue`⟩ → `c47dc5b` (`.h` OA-50, Repair 1): `font-serif-math text-sm` → `text-small` on the title (the one-type-scale move). The golden's last re-baseline was `3c688d1` (`.m`), before that. The change comes from the render. It is not data-driven, and no seed could cure it.
- **Why no cure lands here.** Check 1's cure for a render change is "the owner grants a re-baseline, as §0cl did for `.h2`". §0cl's grant covers `checkpoint-disclosure-body-chromium-*.png` only. ⟨`grep -n 'admin-banner' COHESION.md`⟩ → `:2702` only, which is §0aq's grant to F.W11, now spent. Re-baselining without a grant would be the ungranted golden move the E-3 law forbids.

### INFO-1 — no action (the cure is "none needed"). D-2 above measures the same engine disagreement from the other side.

### Close gate re-read after the cure (fourier `d3b2652` = origin, served `:3100`, `--workers=1`, headless, ×2)
⟨`FW14_PHASE=zz-rp1 BASE_URL=http://localhost:3100 npx playwright test --workers=1 --reporter=line`⟩:
- run 1 → **`13 failed · 3 skipped · 182 passed (7.1m)`**, load `10.70` → `20.40`
- run 2 → **`13 failed · 3 skipped · 182 passed (7.1m)`**, load `20.40` → `14.05`

The failure lists are identical across the two runs:
- **The named baseline set (11 failures).** contrast-floor `:82`×2 and `:128` · gallery-admin-a11y `:91 :103 :114 :125` · vc `:81 :102 :123` · fullscreen `:40`, which the close's run 1 also read. These are relieved.
- f-w14-uia: **0** failures, down from 12. D-1 is cured.
- f-w14-dpr `:228` ×1 is ESCALATED as D-2. vc `:145` ×1 is ESCALATED as D-3.

BEFORE (Close ×2 and Check 1): `24/25 failed · 171/170 passed`. The honest-RED ids GLASS-VEIL-GREY, DOCK-TRIGGER-CLIP and DOCK-SCROLL-MORPH did not fire as e2e failures. The spec files were the only thing the cure moved: vue-tsc and vitest cover `web/src`, which is unchanged, so they hold as banked (Check 1: `tsc=0` ×2, `14/86` ×2).

Run dirt: 47 `zz-rp1-*` crops, git-ignored, deleted by name. ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/`. E13: the Check 1 sweep was at 21:05 and this seat wrote no mail. ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`.

### Repair 1 tally
- **Defects cured: 1 (D-1).**
- **Escalated: 2**, each with its cause measured:
  - **D-2 → ESC-R1-1.** Headless Chromium reports a 1× `devicePixelContentBoxSize` at emulated DPR 2. §0cl's rule then allocates a 1× bitmap. The only app-side cure is the disagreement guard that §0cl retired.
  - **D-3 → ESC-R1-2.** The admin-banner diff comes from a render change (`c47dc5b` `.h` OA-50 title rung) and needs an owner re-baseline grant.
- **INFO: 1**, with no action.
- **Commits.** fourier `d3b2652`, pushed. value.js: this section plus one LEDGER event line.

**Four-verb line.** F.W14 stays **PARTIAL**. The Close gate's REDs outside the named set fall from 14 to 2 ×2, and both of those wait on the COHESION/owner rulings above.

## RESUME 1 — Check 2 (L-20 pass 2, fresh adversarial)

SEAT CHECK, SERVED MODEL: `claude-opus-5-5`, 2026-09-24 ~00:30–00:55 EDT, verify-only. I read the spec whole (70 lines, addenda (a)–(e)). From the record I read the `## RESUME 1` Open → Unit plan (`:865-903`), the `## RESUME 1 — Close`, `— Check 1` and `— Repair 1` sections (by sed range). fourier `m/w1-bump-migration` HEAD `d3b2652` = origin (⟨`git ls-remote`⟩ → `d3b2652354ef`). Crash-recovery: ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only, and ⟨`git status --porcelain docs/tranches/X/execution/`⟩ → empty. Instrument: ⟨`nc -z localhost 27017`⟩ → succeeded · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200` · ⟨`curl ':8000/api/visualizations?limit=1'`⟩ → one public row `crisp-falling-garnet-condor` (Repair 1's seed, reused). Load at open ⟨`uptime`⟩ → `4.00 3.85 3.63`.

### Axes (1)–(9)
- **(1) Claimed GREENs reproduce (fourier `d3b2652`, served `:3100`, `--workers=1`, chromium).**
  - vue-tsc: ⟨`npx vue-tsc -b --force`⟩ → exit `0` ×2.
  - vitest: ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2.
  - `.h2` vc `:164`: it passed in both full runs below. Neither failure list contains it.
  - `.p2` G-p headed: ⟨`FW14_PHASE=zz-ck2 playwright test e2e/f-w14-dpr.spec.ts --headed --workers=1`⟩ → `4 passed (15.4s)` · `4 passed (10.8s)`.
  - Repair 1's D-1 cure: f-w14-uia had **0** failures in both full runs, down from 12. The seed-reuse path is the one exercised.
  - Repair 1's full-e2e figure: ⟨`FW14_PHASE=zz-ck2 BASE_URL=http://localhost:3100 npx playwright test --workers=1 --reporter=line`⟩:
    - run 1 → **`12 failed · 3 skipped · 183 passed (7.2m)`**, load `5.43` → `22.93`
    - run 2 → **`12 failed · 3 skipped · 183 passed (7.5m)`**, load `22.93` → `20.80`
  - ⟨`sort | uniq -c` of the `N) [chromium] ›` ids⟩ gives the identical list for both runs: contrast-floor `:82`×2 and `:128` · gallery-admin-a11y `:91 :103 :114 :125` · vc `:81 :102 :123` · f-w14-dpr `:228` · vc `:145`.
  - Repair 1 read 13 failures. The difference is fullscreen `:40`, a named-set flake that passed in both of my runs.
  - **6 of 6 claimed GREENs reproduce.**
- **(2) Bounds.** ⟨`git show --stat d3b2652`⟩ → `web/e2e/f-w14-uia.spec.ts` only (107+/23−). That is inside the Repair 1 writable set: `.u`'s `web/e2e/**` for addendum (e)'s seeding duty. It touches no `api/**` and no golden. value.js `3fcfe248` / `8de569c9` → only this record and `LEDGER.md`. `scripts/dev/dev.sh` is untouched: ⟨`git log f6467f5b~1..HEAD -- scripts/dev/dev.sh`⟩ → empty. **Clean.**
- **(3) Masking.** I read the `d3b2652` diff whole.
  - The `beforeAll` seeds through the app's own API chain. Its `try/finally` only disposes the request context, and every step keeps an `expect(…ok())`.
  - The `:48` message assertion stays, as `not.toBeNull`.
  - UIA-F-47's fixed `waitForTimeout(1500)` became an `expect.poll` on the stored IndexedDB draft's `contour`, bounded at 30 s. That strengthens the wait; the assertions after it are unchanged.
  - The diff has no skip, no allowlist, no node_modules patch and no catch-around-defect. **Clean.**
- **(4) Commit families.** `d3b2652` is one commit with one meaning (D-1 plus the race that the seeded run exposed in the same file). **Clean.**
- **(5) E-3.** ⟨`git diff --stat f6467f5b~1..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/ scripts/dev/dev.sh`⟩ → empty. **Held.**
- **(6) Mail.** ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`. ⟨`find <path> -maxdepth 1 -type f -newermt '2026-09-23 21:55'`⟩ covered value V/, V/coordination, glass BK/coordination, glass BL, keyframes V/coordination and fourier docs/tranches. The only hit was `INBOX.md` itself, carrying sibling seats' O-66 and sweep lines, none addressed to fourier. **Clean.**
- **(7) Four-verb.** Repair 1 kept PARTIAL and withheld IMPLEMENTED, which is lawful.
- **(8) Goal at the bytes.**
  - `.h2`, `.p2` and D-1 hold.
  - OA-44 is still unmet at DPR 2 in the headless suite that the Close gate runs. ⟨run 1 log `:338`⟩ → `rest: stroke edge spans 4 device px of transition (crop 2×, 30 crossings)`.
  - vc `:145` still differs. ⟨log `:1089`⟩ → `593 pixels (ratio 0.01)`, `checkpoint-admin-banner.png`.
- **(9) Figures.** Repair 1's `13 failed` reproduces as `12`, the same set minus the named-set flake fullscreen `:40`. The run dirt was 47 `zz-ck2-*` crops, which git ignores. I deleted them by name. ⟨`git status --porcelain`⟩ → `?? .worktrees/`.

### (10) Honest-RED adjudication (at the spec bytes, addendum (e) "Close relief")
**Relieved:**
- contrast-floor `:82 :128`
- gallery-admin-a11y `:91 :103 :114 :125`
- vc `:81 :102 :123`
- fullscreen `:40`, the named set
- GLASS-VEIL-GREY (O-62, §0cf)
- DOCK-TRIGGER-CLIP (O-63, §0cj)
- DOCK-SCROLL-MORPH (O-55, F.W13)
- G-u, whose 168 rows are re-homed to F.W14U (`F-W14U.md`, the spec's own relief)

The three honest-RED ids did not fire as e2e failures.

**Unrelieved.** Neither is in the named set, is an honest-RED id, or has a later-wave route in the spec:
- **f-w14-dpr `:228`** (headless, DPR 2, image stroke edge): 4 device px ×2. Repair 1 measured the cause as headless Chromium reporting a 1× `devicePixelContentBoxSize` at emulated DPR 2, and escalated it as ESC-R1-1.
- **vc `:145`** (admin-banner golden): 593 px ×2. The change comes from a render change (`c47dc5b`, the `.h` OA-50 title rung), and no grant covers it. Escalated as ESC-R1-2.

No ruling has landed on either. ⟨`grep -n 'ESC-R1-1\|ESC-R1-2\|admin-banner' COHESION.md`⟩ shows only older, unrelated rows (`:2637-2646`, `:2855` are other waves' ESC-R1 ids; `:2702` is §0aq's spent F.W11 grant). The last addendum is §0cl (`:3253`). The residual register names only "COHESION/owner" as owner, with no minted unit and no ruling.

### Register
| id | severity | claim | receipt | cure |
|---|---|---|---|---|
| D-2 | HIGH | Close gate RED ×2: f-w14-dpr `:228` (DPR 2, image) stroke edge 4 device px headless, over the ≤ 2 bound. Unrelieved; ESC-R1-1 is unruled | full e2e ×2 `:228` failed; log `:338` "spans 4 device px"; headed `4 passed` ×2 | A COHESION ruling on ESC-R1-1: either (a) G-p's headless run is out of scope (the spec's instrument is "headed ×2"), homed as a headed-only project, or (b) §0cl's rule is amended to trust the device box only when it agrees with `round(css × dpr)` within 1 px |
| D-3 | HIGH | Close gate RED ×2: vc `:145` admin-banner golden, 593 px. The diff comes from a render change and no grant covers it. Unrelieved; ESC-R1-2 is unruled | full e2e ×2 `:145` failed; log `:1089` "593 pixels (ratio 0.01)" | An owner/COHESION re-baseline grant for `checkpoint-admin-banner*.png` (as §0cl did for `.h2`), after the check seat confirms the diff is confined to the `c47dc5b` title rung |
| INFO-1 | INFO | fullscreen `:40` (named set) passed in both runs here but failed once in Repair 1. It is a flake inside the relieved set | both run lists | None |

**Successor conjuncts.** F.W14U "Opens after: F.W14 CLOSED" is **RED**. ⟨`grep -rl 'F.W14 CLOSED' fourier/waves`⟩ → `F-W14U.md` only. F.W14U is lawfully blocked until ESC-R1-1 and ESC-R1-2 are ruled and the resulting cure or grant lands.

**Verdict: NOT-CONFORMANT.**
- 2 HIGH (D-2, D-3), both Close-gate REDs outside the named set, with no spec relief and no ruling.
- 1 INFO.
- 6 of 6 claimed GREENs reproduce.
- The LEDGER status cell stays PARTIAL, and an event line is appended.
- Carried: ESC-R1-1 and ESC-R1-2 need a COHESION ruling. No repair seat can cure either within the current bounds and rulings.

## RESUME 1 — Repair 2

SEAT REPAIR (round 2), SERVED MODEL: `claude-opus-5-5`, 2026-09-24. I read the spec whole (70 lines, addenda (a)–(e)) and the register of `## RESUME 1 — Check 2` (`:1126`). Crash-recovery: ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. No inherited edits. fourier HEAD is `d3b2652`.

Ruling check: ⟨`grep -n 'ESC-R1-1\|ESC-R1-2\|admin-banner' COHESION.md`⟩ finds only `:2855`, another wave's ESC-R1 grant. The last addendum is still §0cl (`:3253`), and its last commit is `03563e5f`. No ruling covers F.W14's ESC-R1-1 or ESC-R1-2.

### D-2 (HIGH): f-w14-dpr `:228` headless, 4 device px → ESCALATED (not cured)
- **Re-read.** ⟨`FW14_PHASE=zz-rp2 BASE_URL=http://localhost:3100 npx playwright test e2e/f-w14-dpr.spec.ts --workers=1` (headless)⟩ → `1 failed · 3 passed (9.7s)` and `1 failed · 3 passed (9.5s)`. Both runs report `rest: stroke edge spans 4 device px of transition (crop 2×, 30 crossings)`. I deleted the crops by name.
- **Why this seat cannot cure it.** §0cl dictates the backing-store rule: `devicePixelContentBoxSize` when present, else `Math.round(cssBox × dpr)`. Headless Chromium reports a 1× device box at emulated DPR 2, as Repair 1 measured. Two app-side cures exist, and neither is lawful here:
  - Distrusting the device box brings back the within-1px guard that §0cl retired. That is option (b), and it amends a ruling.
  - Scoping G-p to headed runs changes the gate's instrument. That is option (a), and it changes the gate's scope.
- Either option is a COHESION ruling. A repair seat would also be masking the defect if it chose one.
- The headed gate stays GREEN (Check 2: `4 passed` ×2).

### D-3 (HIGH): vc `:145` admin-banner golden, 593 px → ESCALATED (not cured)
- **Re-read.** ⟨`BASE_URL=http://localhost:3100 npx playwright test e2e/visual-checkpoint.spec.ts:145 --workers=1`⟩ → `593 pixels (ratio 0.01 of all image pixels) are different` ×2.
- **Why this seat cannot cure it.** Re-baselining `checkpoint-admin-banner*.png` needs a grant, as §0bm and §0cl gave for other goldens. The only grant in force is §0cl's `checkpoint-disclosure-body`. Rewriting this golden without one would mask the defect.

### INFO-1: fullscreen `:40` flake inside the named set → no action.

### Result
- **Cured:** 0.
- **Escalated:** 2. ESC-R1-1 (D-2) and ESC-R1-2 (D-3) both need a COHESION or owner ruling.
- **Commits:** fourier none. value.js: this record only.
- **Status:** the LEDGER cell stays PARTIAL.

## RESUME 1 — Check 3 (L-20 pass 3, fresh adversarial)

SEAT CHECK, SERVED MODEL: `claude-opus-5-5`, 2026-09-24 ~00:48–01:10 EDT, verify-only. I read the spec whole (70 lines, addenda (a)–(e)). From the record I read the RESUME 1 Open → Unit plan (`:865-903`), the `## RESUME 1 — Close`, `— Check 2` and `— Repair 2` sections, by sed range. fourier `m/w1-bump-migration` HEAD is `d3b2652` = origin (⟨`git ls-remote`⟩ → `d3b2652354ef`), unmoved since Check 2, and Repair 2 landed no fourier commit. Crash-recovery: ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. Record and LEDGER were clean, so nothing was inherited. Instrument: ⟨`nc -z localhost 27017`⟩ → succeeded · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200`.

**Ruling check first.** ⟨`grep -n 'ESC-R1-1\|ESC-R1-2\|admin-banner' COHESION.md`⟩ → only `:2855` (another wave's `.c2` grant). The last addendum is still §0cl (`:3253`, commit `03563e5f`). ⟨`git log -3 -- F-W14.md`⟩ shows the spec unamended. **No ruling relieves ESC-R1-1 or ESC-R1-2.**

### Axes (1)–(9)
- **(1) Claimed GREENs reproduce** (fourier `d3b2652`, served `:3100`, `--workers=1`, chromium):
  - vue-tsc: ⟨`npx vue-tsc -b --force`⟩ → exit `0` ×2.
  - vitest: ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2.
  - `.h2` vc `:164`: passed in both full runs below.
  - `.p2` G-p, headed: ⟨`FW14_PHASE=zz-ck3 playwright test e2e/f-w14-dpr.spec.ts --headed --workers=1`⟩ → `4 passed (14.7s)` · `4 passed (11.5s)`.
  - Repair 1's D-1: f-w14-uia had 0 failures in both full runs.
  - Full e2e: ⟨`FW14_PHASE=zz-ck3 BASE_URL=http://localhost:3100 npx playwright test --workers=1 --reporter=line`⟩
    - run 1 → **`12 failed · 3 skipped · 183 passed (7.0m)`**, load `17.76` → `17.52`
    - run 2 → **`12 failed · 3 skipped · 183 passed (7.7m)`**, load `17.52` → `32.81`
  - ⟨`sort | uniq -c` of the failure ids⟩ gives the same list for both runs, and it matches Check 2's: contrast-floor `:82`×2 and `:128` · gallery-admin-a11y `:91 :103 :114 :125` · vc `:81 :102 :123` · **f-w14-dpr `:228`** (log `:337` `rest: stroke edge spans 4 device px of transition (crop 2×, 30 crossings)`) · **vc `:145`** (log `:1088` `593 pixels (ratio 0.01)`, `checkpoint-admin-banner.png`).
  - **6 of 6 claimed GREENs reproduce.**
- **(2) Bounds.** Since Check 2 the only commits are value.js `1ec352ee` and `27726b7a`. ⟨`git show --stat`⟩ → this record and `LEDGER.md` only. There is no new fourier commit. ⟨`git log f6467f5b~1..HEAD -- scripts/dev/dev.sh`⟩ → empty. **Clean.**
- **(3) Masking.** The diff is unchanged since Check 2, which read `d3b2652` whole and found it clean. Repair 2 declined both masking routes: a golden rewrite without a grant, and an unruled scope change to G-p. **Clean.**
- **(4) Commit families.** Nothing new beyond record commits, one per meaning. **Clean.**
- **(5) E-3.** ⟨`git diff --stat f6467f5b~1..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/ scripts/dev/dev.sh`⟩ → empty. **Held.**
- **(6) Mail.** ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`. ⟨`find <path> -maxdepth 1 -type f -newermt '2026-09-24 00:00'`⟩ covered value V/, V/coordination, glass BK/coordination, glass BL, keyframes V/coordination and fourier docs/tranches. It found only glass `BL/FORMATION-PROGRESS.md`, a glass-internal design-loop cursor that is not a letter to fourier or value.js. **Clean.**
- **(7) Four-verb.** PARTIAL is held and IMPLEMENTED withheld, which is lawful.
- **(8) Goal at the bytes.** `.h2`, `.p2` and D-1 hold. In the headless suite that the Close gate runs, OA-44's stroke-edge bound still fails at DPR 2 (`:228`), and the admin-banner golden still differs (`:145`).
- **(9) Figures.** Check 2's `12 failed · 3 skipped · 183 passed` ×2 reproduces exactly, with the same set. Run dirt was 47 `zz-ck3-*` crops (git-ignored), which I deleted by name. ⟨`git status --porcelain`⟩ → `?? .worktrees/`.

### (10) Honest-RED adjudication (at the spec bytes, addendum (e) "Close relief")
**Relieved:**
- contrast-floor `:82 :128`
- gallery-admin-a11y `:91 :103 :114 :125`
- vc `:81 :102 :123`
- fullscreen `:40`, the named baseline set
- GLASS-VEIL-GREY (O-62, §0cf)
- DOCK-TRIGGER-CLIP (O-63, §0cj)
- DOCK-SCROLL-MORPH (O-55, F.W13)
- G-u, whose 168 rows are re-homed to F.W14U (`F-W14U.md`)

The three honest-RED ids did not fire as e2e failures. Their owner is glass BK/BL.

**Unrelieved, ×2.** Neither is in the named set, is an honest-RED id, or has a later-wave route in the spec text:
- **f-w14-dpr `:228`**, headless, DPR 2: the stroke edge spans 4 device px against a bound of ≤ 2. It is escalated as ESC-R1-1, and no ruling covers it.
- **vc `:145`**, the admin-banner golden: 593 px differ. It is escalated as ESC-R1-2, and no grant covers it.

The residual register names only "COHESION/owner", with no minted unit.

### Register
| id | severity | claim | receipt | cure |
|---|---|---|---|---|
| D-2 | HIGH | Close gate "e2e REDs = the named baseline set" RED ×2: f-w14-dpr `:228` (headless DPR 2, image) stroke edge 4 device px > 2. Unrelieved (ESC-R1-1 unruled after two repair rounds) | full e2e ×2 → `:228` failed, log `:337`; headed `4 passed` ×2 | COHESION ruling on ESC-R1-1: (a) scope G-p to its spec instrument ("headed ×2") as a headed-only project, or (b) amend §0cl's rule to trust `devicePixelContentBoxSize` only when it agrees with `round(css × dpr)`; then a cure seat |
| D-3 | HIGH | Close gate RED ×2: vc `:145` `checkpoint-admin-banner.png` 593 px, from a render change with no golden grant (ESC-R1-2 unruled) | full e2e ×2 → `:145` failed, log `:1088` | An owner/COHESION re-baseline grant (as §0cl gave `.h2`), after a seat confirms the diff is confined to `c47dc5b`'s OA-50 title rung |
| INFO-1 | INFO | fullscreen `:40` (named set) passed in all 4 runs of Check 2 and Check 3 | both failure lists | None |

**Successor conjuncts.** F.W14U's "Opens after: F.W14 CLOSED" is **RED**. F.W14U is lawfully blocked until ESC-R1-1 and ESC-R1-2 are ruled and the resulting cure or grant lands.

**Verdict: NOT-CONFORMANT.**
- 2 HIGH (D-2, D-3), both unrelieved Close-gate REDs.
- 6 of 6 claimed GREENs reproduce.
- The LEDGER status stays PARTIAL, and an event line is appended.
- The L-20 loop has now run 3 checks and 2 repairs. The two remaining REDs are owner/COHESION acts (ESC-R1-1, ESC-R1-2), not repair-seat work.

## RESUME 2 — Open (COHESION §0cm + F-W14.md addendum (f))

SEAT 0 (RESUME 2 OPEN), SERVED MODEL: `claude-opus-5-5`, 2026-09-24 ~01:05 EDT. Spec read (header, units, addenda (e)+(f) whole). Record read: RESUME 1 Open → Unit plan (`:865-903`) + the last section `## RESUME 1 — Check 3` (`:1220-1279`). COHESION §0cm read whole to the file end (`:3272-3281`, the last addendum). fourier `m/w1-bump-migration` HEAD `d3b2652` (⟨`git log --oneline -1`⟩). Load ⟨`uptime`⟩ → 45.44 / 38.58 / 29.84.

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. Nothing inherited in `.s`'s bounds (`web/e2e/**`, `web/playwright.config.*`).

**alreadyDone (addendum (f), §0cm; commits in fourier `git log`):** `.m` `3c688d1` · `.t` `b5a650f` · `.g` · `.p` · `.h` · `.r` `4a87666` · `.h2` (golden `a8aa0a1`, ratified §0cm) · `.p2` `2b86203`. `.u` re-homed whole to F.W14U (§0cl). ⟨`git -C fourier-analysis log --oneline | grep -c 'W14.s'`⟩ → `0`: `.s` is owed.

**Preconditions.** ESC-C2-1 (= Check 3's D-2 ESC-R1-1 + D-3 ESC-R1-2, and D-1's seed) RULED at §0cm: `.s` seeds through the public API, makes seeded goldens deterministic, and gives `f-w14-dpr.spec.ts` a headed chromium project (§0be). Model law: Opus 5.5. Instrument (§0cm's lawful e2e instrument): ⟨`nc -z localhost 27017`⟩ → succeeded · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200` · ⟨`ps eww <uvicorn pid>`⟩ → `BLOB_DIR=/tmp/fourier-e2e-blobs COMPUTE_RATE_LIMIT=1000 WRITE_RATE_LIMIT=1000 MONGO_URI=mongodb://localhost:27017/fourier`.

**E13 Step-0 (01:08 EDT).** ⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → `BL/ BK/ BJ/` (BK stays the mail path). ⟨`find <path> -maxdepth 1 -type f -newer INBOX.md`⟩ → value V/ · V/coordination · glass BK/coordination · glass BL · keyframes V/coordination · atlas P/coordination · fourier docs/tranches: all empty. ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`. Nothing to row; INBOX untouched.

## RESUME 2 — Baseline (BEFORE, fourier `d3b2652`, read-only, ×2)

⟨`FW14_PHASE=zz-open2 BASE_URL=http://localhost:3100 npx playwright test e2e/visual-checkpoint.spec.ts e2e/f-w14-uia.spec.ts e2e/f-w14-dpr.spec.ts --project=chromium --workers=1 --reporter=line`⟩ (headless, as the full suite runs today) → run 1 **`5 failed · 36 passed (1.6m)`**, load after `57.10`; run 2 **`5 failed · 36 passed (1.8m)`**, load after `53.76`. The same failure set both runs. Probe crops `zz-open2-*` ×4 deleted by name; porcelain after → `?? .worktrees/`.

| gate | BEFORE ×2 | reading |
|---|---|---|
| `f-w14-uia` 12 cases read the seeded record (`firstSavedViz` `:110`) | 0 failed ×2 (the spec's own `beforeAll` `:66` reuses the first public viz or mints one) | **GREEN-BEFORE-CURE** (finding: Repair 1 `d3b2652`'s per-spec seed already turns the reading; §0cm moves the seed to a suite-level global setup / worker fixture that is namespaced and torn down, which the per-spec seed is not, since it reuses whatever `items[0]` is and never tears down) |
| vc `:145` item 8 admin banner (`checkpoint-admin-banner.png`) | failed ×2: `593 pixels (ratio 0.01) are different` | RED — the test uses `stubAdminApi`; `.s` must read the diff, and find what in the banner is DB-derived and unstubbed |
| `f-w14-dpr.spec.ts:228` G-p DPR 2 image, headless chromium | failed ×2: `rest: stroke edge spans 4 device px of transition (crop 2×, 30 crossings)` | RED (the headless instrument; headed banked GREEN 4/4 ×2 at Check 3) |
| vc `:81 :102 :123` | failed ×2 (54 px · 43 px · 448×636 vs 448×622) | baseline-RED (the named set, unchanged) |
| vue-tsc · vitest | banked at Check 3 at this HEAD: exit 0 · 86/86 ×2 | GREEN (hold; HEAD unmoved, not re-run) |
| full e2e `--workers=1` | banked at Check 3: `12 failed · 3 skipped · 183 passed` ×2 = named set + `:228` + `:145` | owed at the close |

## RESUME 2 — Unit plan

One unit, Opus 5.5 (§0cm; no Fable or design seat named). Concurrency 1. Then the close. Paths are fourier-relative unless marked `vjs:`. The unit also writes its receipt under `## RESUME 2 — Unit receipts` here and minimal LEDGER cells/lines. Never: glass-ui, sibling repos, `scripts/dev/dev.sh`, `api/**`, deleting tests or assertions, `test.skip`/`fixme`, and no DB writes except through `/api`.

| id | spec section | writable | gates |
|---|---|---|---|
| F.W14.s | addendum (f) `[F.W14.s]` (F-W14.md:72-80) + §0cm + §0be | `web/e2e/**` (incl. a global setup/teardown or worker fixture, `f-w14-uia.spec.ts`, `visual-checkpoint.spec.ts` and its `checkpoint-admin-banner-chromium-*.png` only if the diff is confined to seeded content) · `web/playwright.config.*` | (1) seed via public `/api` with a session, idempotent + namespaced + torn down; `f-w14-uia` 12 cases read the seeded record, GREEN ×2; (2) vc `:145` re-read against the seeded state: GREEN, or a diff confined to seed-controlled content re-baselined with frozen dates/names, anything else cured; (3) a headed chromium project owns `f-w14-dpr.spec.ts`, headless projects exclude it, never skipped; the full suite reads `:228` headed GREEN ×2 |

Close (after `.s`): vue-tsc 0 · vitest ×2; full e2e `--workers=1` ×2 with load (API on `scripts/e2e.sh`'s env, `BLOB_DIR` etc., §0cm). REDs must be within the named set (vc `:81 :102 :123` · fullscreen `:40` · contrast-floor · gallery-admin-a11y · `G-F12-3-LOAD`) + GLASS-VEIL-GREY · DOCK-TRIGGER-CLIP · DOCK-SCROLL-MORPH. G-u stays re-homed to F.W14U (§0cl).

## RESUME 2 — Unit receipts

### F.W14.s

SEAT `.s`, SERVED MODEL: `claude-opus-5-5`, 2026-09-24 ~01:10 EDT. Spec read whole (`F-W14.md`, 80 lines, addendum (f) `:72-80`); COHESION §0cm (`:3272-3281`, the file end) and §0be; record read: header, RESUME 2 Open → Unit plan (`:1281-1316`), Repair 1 D-1..D-3 (`:1082-1100`).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; value.js record + LEDGER clean. Nothing inherited.

**Instrument.** ⟨`uptime`⟩ → load `110.65`; ⟨`nc -z localhost 27017`⟩ → succeeded; ⟨`curl -X POST :8000/api/sessions`⟩ → `200`; ⟨`curl :3100`⟩ → `200`; ⟨`ps eww <uvicorn :8000>`⟩ → `BLOB_DIR=/tmp/fourier-e2e-blobs COMPUTE_RATE_LIMIT=1000 WRITE_RATE_LIMIT=1000 MONGO_URI=mongodb://localhost:27017/fourier` (§0cm's lawful instrument).

#### Act 1 — the suite seeds through the public `/api` (G-s1) — fourier `251dbad` (pushed)
- `web/e2e/global-seed.ts` (new): Playwright `globalSetup`. It runs `POST /api/sessions` → `POST /api/images` (`assets/animals/golden-retriever.webp`) → `POST /api/images/<slug>/extract-contour` → `POST /api/visualizations` `{visibility:"public", title:"e2e-seed:f-w14", tags:["e2e-seed:f-w14"]}` → `201`. The token, slug and image slug are recorded in `web/e2e/.seed/<host>.json`, a self-ignoring directory (setup writes `.gitignore` = `*`; ⟨`git status --porcelain`⟩ never lists it). The handle is exported as env `FOURIER_E2E_SEED`. **Idempotent:** a recorded row that is still live and carries the namespace is reused. **Teardown** (the function `globalSetup` returns): `GET` for the ETag → `DELETE /api/visualizations/<slug>` with `If-Match` + the session → `DELETE /api/sessions` → the record is removed. The image stays: it is content-addressed, and the API has no image delete. ⟨seeded run⟩ image `young-running-sapphire-lark` = the same asset Repair 1's upload made.
- `web/e2e/fixtures/seed.ts` (new): `SEED_NAMESPACE`, `SEED_ENV`, `seededViz()`. The null-guard assertion moved here as `expect(raw, …).toBeTruthy()`. No assertion was dropped.
- `web/e2e/f-w14-uia.spec.ts`: the per-spec `beforeAll` (`:66-108`, which reused `items[0]` or minted one, with no namespace and no teardown) is retired. `firstSavedViz()` (`:110` → `:46`) returns `seededViz()`. The `fs`/`path` imports go with it.
- **Admin-banner data:** none to seed. Every figure on the banner comes from `stubAdminApi`. Read on the expected and actual frames: ENTRIES 128 · FEATURED 7 · SAVED 41 · VIEWS 9.3K · LIKES 452 · STORAGE 70.0 MB, identical in both.
- **Idempotency falsifier.** ⟨`npx tsx <probe: globalSeed(cfg) twice with no teardown between, then the teardown>`⟩ → `run1 {"slug":"serene-pressing-vanilla-wren",…} run2 {"slug":"serene-pressing-vanilla-wren",…} same true` · `after teardown GET 404`. The probe file was deleted by name.
- **Teardown falsifier.** After each gate run: ⟨`curl :8000/api/visualizations/dusky-cutting-khaki-leopard`⟩ → `404`; ⟨`ls -a e2e/.seed`⟩ → `.gitignore` only; ⟨`curl ':8000/api/visualizations?limit=5'`⟩ → `[('crisp-falling-garnet-condor', None)]`, Repair 1's un-namespaced row, whose token nobody holds (residual R-1).

#### Act 2 — vc `:145` admin banner re-read against the seeded state (G-s2) → ESCALATED (ESC-S-1)
- ⟨`BASE_URL=:3100 playwright test e2e/visual-checkpoint.spec.ts:145 --project=chromium`⟩ → `1 failed`, `593 pixels (ratio 0.01 of all image pixels) are different`. I viewed the expected, actual and diff PNGs whole.
- **What the diff covers:** the red marks sit on the "Admin Mode" title glyphs **only**. The title renders at a smaller rung in the actual frame. The stat tiles are unmarked: the numbers, labels, colours, grid and Log out are the same in both frames.
- **The banner has no DB-derived content.** Every figure comes from `stubAdminApi` (`fixtures/gallery.ts` `ADMIN_STATS`), and the title is a literal string. Seeding the database changes no pixel in the banner. It re-read `593 px` in the seeded runs ×2 (below), the same figure as BEFORE.
- **Cause at the bytes:** ⟨`git log --oneline 3c688d1..HEAD -- web/src/components/visualization/gallery/GalleryAdminBanner.vue`⟩ → `c47dc5b` (`.h` OA-50 Repair 1). ⟨`git show c47dc5b -- …GalleryAdminBanner.vue`⟩ → `font-serif-math text-sm font-semibold` → `font-serif-math text-small font-semibold` on the `Admin Mode` span. The golden's last re-baseline is `3c688d1` (`.m`), which predates that change.
- **Why no cure lands:** addendum (f) grants a re-baseline only for "a difference … confined to seeded content, such as names, counts or dates". This diff is a type-scale render change from `.h`'s ratified one-admin-idiom, so the grant does not cover it. Under the addendum, "Anything else is a real regression and gets cured", and the only cure available is to revert the title rung in `web/src/components/visualization/gallery/GalleryAdminBanner.vue`. That file is outside `.s`'s bounds (`web/e2e/**` + `web/playwright.config.*`). The ADJACENT-LINE RULE does not reach it either: the cure would be in a different concern, and it would undo `.h`'s owned design. So no golden move and no src edit. **ESC-S-1 → COHESION:** either (a) grant a re-baseline of `checkpoint-admin-banner-chromium-*.png` as `.h`'s OA-50 render, as §0cl did for `.h2`'s disclosure body, or (b) rule the `text-small` title a regression and assign its cure to a unit with `web/src/**` bounds.

#### Act 3 — the GPU instrument runs headed (G-s3) — fourier `251dbad` then `b45a111` (pushed)
- `251dbad` declared the `chromium-headed` project (`devices["Desktop Chrome"]`, `headless: false`) with a project `testMatch: /f-w14-dpr\.spec\.ts/`, and `testIgnore` on `chromium` + `mobile-chromium`. **Falsified by the close's vitest:** ⟨`npx vitest run`⟩ ×2 → `1 failed | 85 passed (86)`, `G-F9-1 … Playwright's default testMatch is still in force` (`e2e/unit/unit-floor-population.vitest.ts:155`, `expect(PW_CONFIG).not.toMatch(/^\s*testMatch:/m)`). The assertion stays: it is the unit floor's population oracle.
- **INTENT at the true bytes** (the brief's "testMatch … testIgnore" form cannot hold under G-F9-1). `b45a111` scopes the instrument by tag, the house idiom of the `@coarse` cell. `f-w14-dpr.spec.ts:224`'s G-p describes carry `{ tag: "@gpu" }`, `chromium-headed` uses `grep: /@gpu/`, and `chromium` / `mobile-chromium` use `grepInvert` for it (composed through `excluded()`). No test is skipped or marked `fixme`, and no assertion changed. ⟨`npx playwright test --list | … | uniq -c`⟩ → `4 [chromium-headed]` (f-w14-dpr `:228` ×4) · `189 [chromium]` · `5 [mobile-chromium]` = 198, the Check 3 total (12 + 3 + 183). After: ⟨`npx vitest run`⟩ ×2 → `86 passed (86)` ×2; ⟨`npx tsc --noEmit -p tsconfig.json`⟩ (e2e included) → exit 0.

#### Gate readings (served `:3100`, API on §0cm's instrument, `BASE_URL=http://localhost:3100`)
- **Targeted, after `251dbad`, ×2.** ⟨`FW14_PHASE=zz-s{1,2} playwright test e2e/f-w14-uia.spec.ts e2e/f-w14-dpr.spec.ts e2e/visual-checkpoint.spec.ts:145 --workers=1`⟩. Run 1 → `1 failed · 36 passed (1.7m)`, load after `124.73`. Run 2 → `1 failed · 36 passed (2.5m)`, load `106.62 → 202.26`. The failure both runs is vc `:145` at `593 px`. f-w14-uia: 32/32 both runs, of which F-2..F-47 read the seeded row. f-w14-dpr: `[chromium-headed]` 4/4 both runs.
- **Close: vue-tsc and vitest.** ⟨`npx vue-tsc -b --noEmit`⟩ ×2 → exit `0` · `0`. ⟨`npx vitest run`⟩ ×2 at `b45a111` → `86 passed (86)` ×2 (at `251dbad`: 85/86 ×2, see Act 3).
- **Close: full e2e at `b45a111`,** ⟨`FW14_PHASE=zz-full{1,2} playwright test --workers=1 --reporter=line`⟩:
  - run 1: load `68.65 → 34.62`, **`12 failed · 3 skipped · 183 passed (8.2m)`**;
  - run 2: load `34.62 → 45.63`, **`11 failed · 3 skipped · 184 passed (8.1m)`**;
  - both runs: `[chromium-headed]` f-w14-dpr `:228` ×4 GREEN.

| gate | BEFORE ×2 (d3b2652) | AFTER ×2 (b45a111) | verdict |
|---|---|---|---|
| G-s1 seed via `/api`, idempotent, namespaced, torn down; f-w14-uia 12 cases read it | 0 failed; per-spec seed, un-namespaced, no teardown | 32/32 targeted ×2; idempotency probe `same true`; teardown `404` ×3; full ×2: f-w14-uia 0 failed in run 2, 1 load flake in run 1 (R-2) | **GREEN** |
| G-s2 vc `:145` against the seeded state | 593 px ×2 | 593 px ×4 (targeted ×2 + full ×2), confined to the `Admin Mode` title rung (`c47dc5b`), not seed content | **RED → ESC-S-1** |
| G-s3 headed project owns f-w14-dpr, headless projects exclude it, `:228` headed GREEN ×2 | `:228` 4 device px headless ×2 | `[chromium-headed]` 4/4 ×4 (targeted ×2 + full ×2); `--list` headless projects hold 0 f-w14-dpr tests | **GREEN** |
| Close: REDs within the named set + honest-REDs | full 12 failed ×2 (named + `:228` + `:145`) | run 2: 11 = named 10 (contrast-floor ×3 · gallery-admin-a11y ×4 · vc `:81 :102 :123`) + `:145`; run 1: the same 11 + f-w14-uia `:162` UIA-F-17 | **RED** (`:145` = ESC-S-1; `:162` = R-2, once) |

#### Residuals
- **R-1.** `crisp-falling-garnet-condor` is Repair 1's seed row. It is public, has no namespace, and its session token was never recorded, so no teardown can delete it through `/api`. It stays in the dev DB, and the retired per-spec seed can no longer mint new rows. Removing it needs its owner session or an admin act, which this seat's bounds do not allow (no DB writes by hand).
- **R-2.** f-w14-uia `:162` UIA-F-17 timed out once, in full run 1 at load ~69–109: `Test timeout of 30000ms exceeded`, `locator.hover` waiting on `Play animation` in the second of four exports (`:195`). Run 2 passed it; alone it passed in `15.4s`. That is a structural budget: 4 export round-trips inside the 30 s default under load. The first export passed, so the page loaded the seeded row. It is not seed-caused. It is left unedited because it is `.u`'s falsifier (UIA rows are re-homed to F.W14U).

#### Escalations
- **ESC-S-1 (G-s2).** vc `:145` is a render-change diff and not seed-confined; addendum (f) grants no re-baseline for it, and its only cure sits in `web/src/**`, outside `.s`'s bounds. COHESION must rule (a) grant the `checkpoint-admin-banner-chromium-*.png` re-baseline as `.h`'s OA-50 render, or (b) assign the `text-small` title revert to a `web/src` unit.

#### Commits (fourier `m/w1-bump-migration`, pushed to origin)
- `251dbad`: seed globalSetup + teardown, `fixtures/seed.ts`, f-w14-uia rewired, headed project. One meaning.
- `b45a111`: the headed project is tag-scoped (`@gpu`) and not a `testMatch`, which G-F9-1 forbids.
- No golden moved.

## RESUME 2 — Close

SEAT CLOSE (verify-only), SERVED MODEL: `claude-opus-5-5`, 2026-09-24 ~01:38–02:05 EDT. Spec read whole (`F-W14.md`, 80 lines, addenda (a)–(f)). Record read: RESUME 2 Open → Unit plan (`:1281-1316`), the `.s` receipt (`:1318`–end), and the prior `## RESUME 1 — Close` (`:969-1020`, sed range). fourier `m/w1-bump-migration` HEAD `b45a111` = origin (⟨`git rev-parse --short origin/m/w1-bump-migration`⟩ → `b45a111`).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only; value.js record + LEDGER clean. Nothing inherited.

**Instrument.** ⟨`uptime`⟩ → load `55.07`; ⟨`nc -z localhost 27017`⟩ → succeeded; ⟨`curl -X POST :8000/api/sessions`⟩ → `200`; ⟨`curl :3100`⟩ → `200`.

### Act 1 — commits against their writable sets
- fourier `251dbad` ⟨`git show --stat`⟩ → `web/e2e/f-w14-uia.spec.ts` · `web/e2e/fixtures/seed.ts` · `web/e2e/global-seed.ts` · `web/playwright.config.ts` (4 files, +222 −73). In bounds (`web/e2e/**` + `web/playwright.config.*`).
- fourier `b45a111` ⟨`git show --stat`⟩ → `web/e2e/f-w14-dpr.spec.ts` (+1 −1, the `@gpu` tag) · `web/playwright.config.ts`. In bounds.
- value.js `535981f9` → this record only; `5603b398` → `LEDGER.md` only (+1 line). In bounds.
- No golden moved (⟨`git log --oneline -1 -- …checkpoint-admin-banner-chromium-darwin.png`⟩ → `3c688d1`). **Landed-wrong: none.**

### Act 2 — gates BEFORE → AFTER (fourier `b45a111`, served `:3100`, write-then-measure, ×2)
| gate | BEFORE (RESUME 2 baseline, `d3b2652`) | AFTER (this seat) | verdict |
|---|---|---|---|
| vue-tsc | exit 0 (banked) | ⟨`npx vue-tsc -b --noEmit`⟩ → exit `0` ×2 | GREEN |
| vitest (incl. G-F9-1 unit floor) | 86/86 (banked) | ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2 | GREEN |
| G-s3 project scoping | f-w14-dpr in headless projects | ⟨`npx playwright test --list \| … \| uniq -c`⟩ → `4 [chromium-headed]` · `189 [chromium]` · `5 [mobile-chromium]` (= 198); headless projects hold 0 f-w14-dpr tests, none skipped | GREEN |
| G-s3 `:228` headed in the full suite | `:228` headless RED 4 device px ×2 | `[chromium-headed] f-w14-dpr.spec.ts:228:13` ×4 (DPR 1/2 × equation/image) in both full runs, absent from both failure lists | GREEN ×2 |
| G-s1 seed via `/api`, namespaced, torn down | per-spec seed, un-namespaced, no teardown | mid-run 1 ⟨`cat e2e/.seed/localhost_3100.json`⟩ → slug `blazing-dashing-quartz-elk`; ⟨`curl :8000/api/visualizations/<slug>`⟩ → `title e2e-seed:f-w14`, `tags ['e2e-seed:f-w14']`, public, live; after each run ⟨`ls -a e2e/.seed`⟩ → `.gitignore` only and after run 2 ⟨`curl …/blazing-dashing-quartz-elk`⟩ → `404`; ⟨`curl ':8000/api/visualizations?limit=20'`⟩ → `1 [('crisp-falling-garnet-condor', None)]` (R-1 only, no seed row left) | GREEN (idempotency: `.s`'s probe `same true`, not re-run; the record-reuse path read in `global-seed.ts`) |
| G-s1 f-w14-uia 12 cases read the seeded row | 0 failed (per-spec seed) | f-w14-uia 0 failed in full runs 1 and 2 (UIA-F-17 `:162` passed both; `.s`'s R-2 did not recur) | GREEN ×2 |
| G-s2 vc `:145` admin banner | 593 px ×2 | `593 pixels (ratio 0.01) are different` ×2; golden last moved `3c688d1`; ⟨`git log 3c688d1..HEAD -- …GalleryAdminBanner.vue`⟩ → `c47dc5b`; ⟨`git show c47dc5b`⟩ → `Admin Mode` span `text-sm` → `text-small`. Not seed content. | **RED → ESC-S-1** |
| Close: full e2e `--workers=1`, REDs ⊆ named set + honest-REDs | 12 failed ×2 (named + `:228` + `:145`) | run 1: load `57.41 → 32.24`, **`12 failed · 3 skipped · 183 passed (7.5m)`**; run 2: load `30.27 → 20.30`, **`11 failed · 3 skipped · 184 passed (7.3m)`**. ⟨`diff` of the sorted failure lists⟩ → only `fullscreen.spec.ts:40` (run 1). | **RED ×2 — exactly one test outside the set: vc `:145`** |

**REDs by id.** Named set, both runs: contrast-floor `:82` ×2 · `:128` · gallery-admin-a11y `:91 :103 :114 :125` · vc `:81` (54 px) · `:102` (43 px) · `:123` (448×636 vs 448×622), plus fullscreen `:40` in run 1 (named). Outside: vc `:145` ×2. The honest-RED ids GLASS-VEIL-GREY, DOCK-TRIGGER-CLIP and DOCK-SCROLL-MORPH fire no e2e failure and stay relieved (addenda (c)/(d)). G-u: 88 of 256 dispositioned; 168 cited to F.W14U (§0cl).

### Act 3 — §Verification Artefacts
The spec has no separate section; its Close line and addendum (f)'s Close bullet were run as written above. Run dirt: ⟨`git -C fourier-analysis status --porcelain`⟩ after both runs → `?? .worktrees/` only (`FW14_PHASE=zz-close{1,2}` wrote no tracked crop).

### Act 4 — E13
⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → `BL/ BK/ BJ/`. ⟨`find <path> -maxdepth 1 -type f -newer INBOX.md`⟩ → value V/ · V/coordination · glass BK/coordination · glass BL · glass P/coordination · keyframes V/coordination · fourier docs/tranches: all empty; atlas has no `docs/tranches`. ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`. No UNREAD mail in scope.

### Residuals (named owners)
- **R-C3-1** vc `:145` admin-banner golden, 593 px ×2: the `Admin Mode` title rung `.h` changed in `c47dc5b` (OA-50). Owner: COHESION, via ESC-S-1.
- **R-C3-2** (`.s` R-1) `crisp-falling-garnet-condor` is Repair 1's un-namespaced seed row, and no one holds its token. Owner: an owner/admin act, or F.W14U (its bounds carry `api/**`).
- **R-C3-3** (`.s` R-2) UIA-F-17 `:162` runs four exports inside the 30 s default. It did not recur ×2 this close. Owner: F.W14U (`.u`'s falsifier).
- **R-C3-4** G-u, 168 UIA-F rows. Owner: F.W14U (authored).
- **R-C3-5** honest-RED GLASS-VEIL-GREY (O-62), DOCK-TRIGGER-CLIP (O-63), DOCK-SCROLL-MORPH (O-55). Owner: glass BK/BL.
- Carried: RESUME 1 Close R-C2-5, and the prior Close R-C-6 (`.m` r1/r2, `.t` R-t-1, `.h` BL-FW14H-1/2, `.r` R-r-5). Owners as named there. R-C2-1 (seed) and R-C2-2 (`:228` headless) are **discharged** by `.s`.

### Escalations
- **ESC-S-1** (carried from `.s`, confirmed at the bytes): vc `:145` is the only e2e RED outside the named set, ×2. Its diff is `.h`'s type-rung render, not seeded content, so addendum (f) grants no re-baseline, and the cure is outside `.s`'s bounds. COHESION rules one of two: (a) grant a `checkpoint-admin-banner-chromium-*.png` re-baseline as `.h`'s OA-50 render, as §0cl did for `.h2`; or (b) assign the title revert to a `web/src` unit. F.W14U, which opens after F.W14 CLOSED, stays blocked on this one ruling.

### Commit roster (RESUME 2)
fourier: `251dbad` · `b45a111` (`.s`, pushed). value.js: `535981f9` (`.s` receipt) · `5603b398` (`.s` LEDGER) · this close (record and LEDGER).

**Four-verb line.** F.W14: **PARTIAL**. `.s` landed in bounds, and G-s1 and G-s3 are GREEN ×2 on the spec's instruments. vue-tsc 0 and vitest 86/86 hold ×2. The Close gate "REDs within the named set" reads RED ×2 on exactly one test, vc `:145` (ESC-S-1). IMPLEMENTED is withheld. The spec designates no VERIFIED stamp for this seat.

## RESUME 2 — Check 1 (L-20 pass 1, fresh adversarial)

SEAT CHECK 1, SERVED MODEL: `claude-opus-5-5`, 2026-09-24 ~01:55 EDT. Spec read whole (`F-W14.md`, 80 lines, addenda (a)–(f)). Record read: RESUME 2 Open → Close (`:1281-1420`). fourier `m/w1-bump-migration` HEAD `b45a111`. ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. Instrument: ⟨`nc -z localhost 27017`⟩ → succeeded · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200`.

### Axes
- **(2) bounds.** ⟨`git show --stat 251dbad b45a111`⟩ → `web/e2e/f-w14-uia.spec.ts` · `web/e2e/fixtures/seed.ts` · `web/e2e/global-seed.ts` · `web/playwright.config.ts` | `web/e2e/f-w14-dpr.spec.ts` · `web/playwright.config.ts`: all in `web/e2e/**` + `web/playwright.config.*`. value.js `535981f9` · `5603b398` · `4094cab4` → record + LEDGER only. ⟨`git log 535981f9~1..HEAD -- scripts/dev/dev.sh`⟩ → empty. The golden did not move: ⟨`git log -1 -- …checkpoint-admin-banner-chromium-darwin.png`⟩ → `3c688d1`.
- **(3) masking.** Nothing found. `global-seed.ts` has one `try`, a `try/finally` that disposes the teardown request context. It is not a catch around a defect: every non-2xx response throws (`ok()` → `throw new Error`). There is no `skip` or `fixme`. The five removed `expect`s in `f-w14-uia.spec.ts` belong to the retired per-spec seed, and the same checks now live in `global-seed.ts`, where a failure throws. The `seededViz()` null guard moved to `fixtures/seed.ts` as `toBeTruthy()`. `f-w14-dpr` gained a tag (`{ tag: "@gpu" }`) and lost no assertion. The G-F9-1 oracle is kept and honoured: `b45a111` scopes the tests by tag, not by `testMatch`.
- **(4) families.** `251dbad` is the seed plus the headed project. `b45a111` is the tag-scoping fix that G-F9-1 forced. These are two meanings with no family split. The receipt, LEDGER and close are each one commit.
- **(5) E-3.** ⟨`git diff --stat 535981f9~1..HEAD -- docs/tranches/X/fourier/waves/ docs/tranches/V/megatranche/registry/adjudicated/`⟩ → empty.
- **(6) mail.** ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`.
- **(7) four-verb line.** The LEDGER row moved `OPEN … RESUME 2` → `PARTIAL — vc :145 … ESC-S-1` (`4094cab4`), a minimal cell replacement plus one event line. That is lawful, and PARTIAL is the honest verb.

## RESUME 2 — Close 2 (post-reboot re-run, COHESION §0cn)

SEAT CLOSE (verify-only), SERVED MODEL: `claude-opus-5-5`, 2026-09-24 ~09:47 EDT. The host reboot (§0cn) killed the Check 1 seat above mid-write; this seat re-runs the close on the restored instrument. Spec read whole (`F-W14.md`, 80 lines, addenda (a)–(f)). Record read: RESUME 2 Open → Unit plan (`:1281-1315`), the last `## RESUME 2 — Close` (`:1372-1420`) and the inherited Check 1 fragment (`:1422-1432`). COHESION §0cn read whole. fourier `m/w1-bump-migration` HEAD `b45a111` = origin (⟨`git rev-parse --short origin/m/w1-bump-migration`⟩ → `b45a111`).

**Crash-recovery.** ⟨`git -C fourier-analysis status --porcelain`⟩ → `?? .worktrees/` only. value.js: this record carried +12 uncommitted lines, the killed Check 1 seat's partial `## RESUME 2 — Check 1` (axes 2–7, no verdict). It is a sibling seat's work, not this unit's: kept byte-for-byte, not completed, committed with this close so the tree is clean, and flagged here as **incomplete (no verdict)**. LEDGER clean.

**Instrument (§0cn).** ⟨`uptime`⟩ → `9.68 87.35 95.40` · ⟨`nc -z localhost 27018`⟩ → succeeded · ⟨`curl -X POST :8000/api/sessions`⟩ → `200` · ⟨`curl :3100`⟩ → `200` · ⟨`ps eww <:8000 pid>`⟩ → `MONGO_URI=mongodb://localhost:27018/fourier BLOB_DIR=/tmp/fourier-e2e-blobs COMPUTE_RATE_LIMIT=1000 WRITE_RATE_LIMIT=1000`.

### Act 1 — commits against their writable sets
No commit landed in F.W14's bounds since the RESUME 2 Close (fourier HEAD `b45a111` unmoved). Re-read: ⟨`git show --stat 251dbad`⟩ → `web/e2e/f-w14-uia.spec.ts` · `web/e2e/fixtures/seed.ts` · `web/e2e/global-seed.ts` · `web/playwright.config.ts` (+222 −73); ⟨`git show --stat b45a111`⟩ → `web/e2e/f-w14-dpr.spec.ts` · `web/playwright.config.ts` (+11 −9). value.js `535981f9` (record) · `5603b398` (LEDGER) · `4094cab4` (record + LEDGER, +52 −1). Admin-banner golden ⟨`git log -1 -- …checkpoint-admin-banner-chromium-darwin.png`⟩ → `3c688d1` (unmoved). **Landed-wrong: none.**

### Act 2 — gates BEFORE → AFTER (fourier `b45a111`, served `:3100`, API on mongo :27018, ×2)
| gate | BEFORE (RESUME 2 Close, pre-reboot) | AFTER (this seat, post-reboot) | verdict |
|---|---|---|---|
| vue-tsc | exit 0 ×2 | ⟨`npx vue-tsc -b --noEmit`⟩ → exit `0` ×2 | GREEN |
| vitest (incl. G-F9-1) | 86/86 ×2 | ⟨`npx vitest run`⟩ → `Test Files 14 passed (14)` · `Tests 86 passed (86)` ×2 | GREEN |
| G-s3 `:228` headed in the full suite | `[chromium-headed]` ×4, absent from failures ×2 | `[190..193/198] [chromium-headed] f-w14-dpr.spec.ts:228:13` ×4 in both runs, absent from both failure lists | GREEN ×2 |
| G-s1 f-w14-uia reads the seeded row | 0 failed ×2 | f-w14-uia 0 failed in both runs | GREEN ×2 |
| G-s2 vc `:145` admin banner | RED ×2 (593 px, ESC-S-1) | RED ×2 (in both failure lists) | **RED → ESC-S-1 (unruled)** |
| G-s1 seed torn down | `.seed` = `.gitignore` only; seeded slug 404 | mid-run ⟨`curl ':8000/api/visualizations?limit=20'`⟩ → seed `vast-drawing-maple-falcon` titled `e2e-seed:f-w14`; after run 2 ⟨`curl …/vast-drawing-maple-falcon`⟩ → `404`, list → `1 [('crisp-falling-garnet-condor', None)]`, ⟨`ls -a e2e/.seed`⟩ → `.gitignore` only | GREEN |
| Close: full e2e `--workers=1`, REDs ⊆ named set + honest-REDs | 12 / 11 failed (named + `:145`) | ⟨`FW14_PHASE=zz-close3{1,2} BASE_URL=http://localhost:3100 npx playwright test --workers=1 --reporter=line`⟩ → run 1: load `13.83 → 25.05`, **`16 failed · 3 skipped · 4 did not run · 175 passed (6.7m)`**; run 2: load `25.05 → 22.58`, **`16 failed · 3 skipped · 4 did not run · 175 passed (6.8m)`**; ⟨`diff` of the sorted failure lists⟩ → identical | **RED ×2 — six tests outside the set** |

**REDs by id (both runs, identical).** Named set: contrast-floor `:82` ×2 · `:128` · gallery-admin-a11y `:91 :103 :114 :125` · vc `:81 :102 :123` (10). Outside: vc `:145` (ESC-S-1, unchanged) and five **new since the reboot**: contour-extraction `:133` (console `500` ×3) · visualization-crud `:481` @mobile/@laptop/@desktop (console `500` ×7; its serial tail = the 4 did-not-run) · workspace-flow `:176` (`expect(blobResponse.status).toBe(200)` → `500`).

**Cause of the five, measured at the bytes (instrument, not F.W14 code).** ⟨`grep '" 500' ~/.dev-logs/fourier-api.log`⟩ → `GET /api/images/young-running-sapphire-lark/{overlay,thumbnail}` 500; traceback `FileNotFoundError: '/private/tmp/fourier-e2e-blobs/young-running-sapphire-lark.thumb'`. ⟨`ls /private/tmp/fourier-e2e-blobs | wc -l`⟩ → `0` before and after both runs. The reboot (§0cn) wiped `/private/tmp`, so `scripts/e2e.sh`'s `BLOB_DIR=/tmp/fourier-e2e-blobs` is empty, while mongo on :27018 (`~/.mongo-dev`) kept the image rows. Upload is content-addressed (`api/routers/images.py:110-113`, `sha256` → `store_image_asset`), so every fixture upload resolves to the existing row `young-running-sapphire-lark` and never rewrites its blob; both live viz rows (the seed and `crisp-falling-garnet-condor`) point at it. A missing blob surfaces as `500`, not `404` or a re-store. The fix sits outside F.W14 (`api/**`, the dev-instrument layout); this verify-only seat cures nothing.

### Act 3 — §Verification Artefacts
The spec has no separate section; its Close line and addendum (f)'s Close bullet were run as written above. Run dirt: ⟨`git -C fourier-analysis status --porcelain`⟩ after both runs → `?? .worktrees/` only.

### Act 4 — E13
⟨`ls -dt glass-ui/docs/tranches/*/ | head -3`⟩ → `BL/ BK/ BJ/`. ⟨`find <path> -maxdepth 1 -type f -newer INBOX.md`⟩ → value V/ · V/coordination · glass BK/coordination · keyframes V/coordination · fourier docs/tranches: `0`; glass BL: `1` = `FORMATION-PROGRESS.md` (BL's own resume cursor, `f538a252`, not mail to value.js). ⟨`grep -c '^| I-.*| UNREAD' INBOX.md`⟩ → `0`. No UNREAD mail in scope.

### Residuals (named owners)
- **R-C4-1** vc `:145` admin-banner golden, RED ×2 (`.h`'s `c47dc5b` title rung). Owner: COHESION, via ESC-S-1.
- **R-C4-2** the post-reboot blob loss: 5 e2e REDs (contour-extraction `:133`, visualization-crud `:481` ×3, workspace-flow `:176`) from `BLOB_DIR` on the wiped `/private/tmp` beside a persistent mongo. Owner: COHESION via ESC-C4-1 (instrument), and F.W14U (`api/**`) for the product half, a content-addressed upload that never re-stores a missing blob and a missing blob answered `500`.
- **R-C4-3** Check 1 (RESUME 2) is incomplete: the killed seat wrote axes 2–7 and no verdict. Owner: the next Check seat, which re-runs it whole.
- Carried unchanged from the RESUME 2 Close: R-C3-2 (`crisp-falling-garnet-condor`, un-namespaced, no token holder), R-C3-3 (UIA-F-17 `:162`, 0 failures ×2 here), R-C3-4 (G-u, 168 rows → F.W14U), R-C3-5 (GLASS-VEIL-GREY O-62 · DOCK-TRIGGER-CLIP O-63 · DOCK-SCROLL-MORPH O-55, no e2e failure). Owners as named there.

### Escalations
- **ESC-S-1** (carried, still unruled): as ruled-for in the RESUME 2 Close: (a) grant the `checkpoint-admin-banner-chromium-*.png` re-baseline as `.h`'s OA-50 render, or (b) assign the title revert to a `web/src` unit.
- **ESC-C4-1** (new): the §0cn instrument is not the §0cl one. `scripts/e2e.sh` puts `BLOB_DIR` on `/tmp`, which the reboot wiped, while fourier's mongo (:27018, `~/.mongo-dev/fourier`) kept the image rows. COHESION rules the restore: either reset the fourier dev DB to fresh (as §0cl did), or move `BLOB_DIR` to a persistent path beside the DB, which touches `scripts/e2e.sh`, outside F.W14's bounds. Then the close re-reads full e2e ×2. No F.W14 unit can cure this in bounds, and no DB writes are lawful except through `/api`.

### Commit roster (RESUME 2, Close 2)
fourier: `251dbad` · `b45a111` (`.s`, pushed; unchanged). value.js: `535981f9` · `5603b398` · `4094cab4` · this Close 2 (record, including the inherited Check 1 fragment, and LEDGER).

**Four-verb line.** F.W14: **PARTIAL** (unmoved). vue-tsc 0, vitest 86/86, G-s1 and G-s3 are GREEN ×2. The Close gate "REDs within the named set" reads RED ×2 on six tests: vc `:145` (ESC-S-1) and five blob-`500` tests (ESC-C4-1, instrument). IMPLEMENTED is withheld. The spec designates no VERIFIED stamp for this seat.
