SERVED MODEL: claude-opus-5-5[1m]

# X.F.W11 — the owner audit docket, F-side (execution record)

Spec: `docs/tranches/X/fourier/waves/F-W11.md` (46 lines, read whole) — minted by COHESION **§0ao**
(`:2531-2565`, the owner's live audit docket OA-1..OA-6, verbatim there); F-side rows OA-1 · OA-2 ·
OA-3. §0ao.1 (OA-7..OA-10) routes to `KF.W13T.e`, not here. Sitting of record 2026-09-17 (the
owner's begin-word, COHESION §0j); this seat's clock 2026-09-22. Seat 0 = OPEN, VERIFY-AND-BANK:
zero fourier-analysis bytes, zero glass-ui bytes, zero product bytes written.

## Open

- **Date**: 2026-09-22 (sitting 2026-09-17, §0j). Track C · X·F. Seat 0 `claude-opus-5-5[1m]`.
- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` only (no
  inherited partial work inside `web/src/**` or `web/e2e/**`); value.js writable set
  (`execution/C/F-W11.md` · `LEDGER.md` · `INBOX.md`) clean — the record did not exist.
- **Precondition 1 — F.W10S CLOSED** (spec §State "Opens after"): LEDGER row `F.W10S` reads
  `CLOSED 2026-09-17 (honest-RED: G-F9-11 · G-F9-8 crud:664 · G-F9-5 contrast-floor leg)` ⟵ CHECK 2
  CONFORMANT-HONEST-RED; COHESION §0al (`:2453`) "every X·F wave is CLOSED; Track C is COMPLETE";
  the §0al act (`:664` note carries E-F10S-b2/A-3) landed at fourier `735319e`. **MET.**
- **Precondition 2 — baseline tree**: ⟨cmd⟩ `git -C fourier-analysis log --oneline -1` →
  `735319e docs(web/e2e): crud :664 keystone note carries its escalation id (E-F10S-b2 / A-3) — COHESION §0al`;
  branch `m/w1-bump-migration`; ⟨cmd⟩ `git rev-parse --short origin/m/w1-bump-migration` → `735319e`
  (pushed, equal to the spec's named baseline). **MET.**
- **Owner-gated items**: none open. The wave is minted by owner ruling (§0ao, verbatim quote there);
  glass-ui READ-ONLY and gaps-by-mail per §0j; producer is glass-ui **8.0.0** in
  `web/node_modules/@mkbabb/glass-ui/package.json` (the §0i ratified pin).
- **E13 Step-0 mail sweep** (four paths, read-only; BK confirmed newest: ⟨cmd⟩ `ls glass-ui/docs/tranches | tail -4`
  → `… BI BJ BK C D …`: BK is the last of the B-series, no `BL`). ⟨cmd⟩ `find <path> -maxdepth 1 -type f -newermt "2026-09-21 00:00"` →
  value.js `V/`: empty · `V/coordination`: `INBOX.md` only · BK/coordination: `glass-outbound-2026-09-22-consumers-10.0.0.md`
  (INBOX mentions ×5), `fourier-to-glass-2026-09-17-nwo1-bh-relay.md` (×2), `value-to-glassui-2026-09-DD-fw4-relay.md` (×3)
  — all three already rowed/noted · keyframes.js `V/coordination`: empty · atlas `P/coordination`: empty. Highest row
  `I-39`; **0 new rows, 0 UNREAD in scope**; a dated sweep line appended at the INBOX tail.

## Baseline

All commands run READ-ONLY at fourier `735319e` (worktree clean), cwd `fourier-analysis/`, 2026-09-22.

| gate | command (spec §3) | BEFORE (run 1 · run 2) | verdict |
|---|---|---|---|
| G-F11-1 | `grep -rEn 'border-radius:\s*0[^.]\|rounded-none' web/src \| wc -l` | **6 · 6** | RED (spec baseline 6 — reproduces) |
| G-F11-2 (a) | `grep -rEn 'border-radius:\s*[0-9]' web/src \| wc -l` | **43 · 43** | RED |
| G-F11-2 (b) | `grep -rEn 'border-radius' web/src \| grep -vc 'var(--radius'` | **47 · 47** | RED |
| G-F11-3 (bytes) | `grep -n 'paper-grid' web/src/components/paper/PaperView.vue` | `:455` `<div class="paper-grid">` · `:691` · `:699` | RED |
| G-F11-3 (browser) | one bounded probe, 1440×900, routes `/` `/paper` `/visualize` (node + `@playwright/test` chromium, computed `background-image` on every element wider than 300px) | grid layer = `div.paper-grid` **976×131574** on `/` and `/paper`; **absent** on `/visualize` → 0 of 3 cells at this viewport = viewport | RED |
| G-F11-4 (bytes) | `test -f web/src/components/layout/AppDock.vue` · `grep -rln 'glass-ui/dock' web/src` | **absent** · 3 files (`EditorControlsDock` · `CanvasControlsDock` · `AnimationControls`), none a shell dock | RED |
| G-F11-4 (e2e legs) | `shell-header.spec.ts` [chromium] · `coarse-pointer.spec.ts` [mobile-chromium] (inside the full run) | 4/4 passed · 4/4 passed (both runs) | legs GREEN at the OLD markup — they are the regression floor, not the gate |
| G-F11-5 (bytes) | per file `grep -c '@mkbabb/glass-ui'` over the eight chrome files | CoefficientsPanel 1 · ContourSettings 4 · EquationPanel 2 · AnimationControls 3 · CanvasControlsDock 2 · EditorControlsDock 4 · GlassTimeline 1 · ExportModal 3 → **8/8 ≥ 1** | **GREEN-BEFORE-CURE** (finding R.2-1) |
| G-F11-5 (e2e leg) | `visualization-ux.spec.ts` [chromium] keystones | run 1: `:95` timed out at `waitForLoadState("networkidle")` under 9-worker load; run 2: all passed | GREEN at run 2 (load-flaky at run 1) |
| G-F11-6 (build) | `cd web && npm run build` (`vue-tsc -b && vite build`) | **exit 0** (`✓ built in 5.23s`) | GREEN at baseline (the gate is "stays green") |
| G-F11-6 (e2e) | `cd web && BASE_URL=http://localhost:3100 npx playwright test --reporter=list` (107 tests, 2 projects, 9 workers) | run 1: **70 passed · 20 failed · 3 skipped · 14 did not run** · run 2: **90 passed · 14 failed · 3 skipped** | baseline failure set banked below |
| G-F11-7 | E13 sweep (Open) | 0 UNREAD in scope | GREEN at open (re-measured at `.d`) |

**The stable baseline failure set** (⟨cmd⟩ `comm -12 fail1 fail2 | wc -l` → **14**; every one in both runs):

```
[chromium] › e2e/contrast-floor.spec.ts:128:5 › X·F F.W4 — contrast floor (G-F4-CONTRAST-FLOOR) › every named pair has a re-derivable expression
[chromium] › e2e/contrast-floor.spec.ts:82:9 › X·F F.W4 — contrast floor (G-F4-CONTRAST-FLOOR) › every named pair meets its floor — dark arm
[chromium] › e2e/contrast-floor.spec.ts:82:9 › X·F F.W4 — contrast floor (G-F4-CONTRAST-FLOOR) › every named pair meets its floor — light arm
[chromium] › e2e/equation-interaction.spec.ts:77:5 › S2 — /equation: compute → notation → budget → reload (G-F9-6) › the four-step interaction drives 
[chromium] › e2e/fullscreen.spec.ts:40:5 › Fullscreen viewer (G-F9-11) › enters fullscreen and asserts against the teleported root @mutating
[chromium] › e2e/gallery-admin-a11y.spec.ts:103:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin users panel
[chromium] › e2e/gallery-admin-a11y.spec.ts:114:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin flagged panel
[chromium] › e2e/gallery-admin-a11y.spec.ts:125:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin audit log panel
[chromium] › e2e/gallery-admin-a11y.spec.ts:91:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin banner: the stat cells are graded for the first
[chromium] › e2e/visual-baseline.spec.ts:57:9 › π capture [before] equation @ 1280x800
[chromium] › e2e/visual-baseline.spec.ts:57:9 › π capture [before] equation @ 375x667
[chromium] › e2e/visual-checkpoint.spec.ts:101:5 › G-F9-23 — the F.W1 checkpoint set, instrumented › item 2 — `cartoon-card` hover, the state no spec 
[chromium] › e2e/visual-checkpoint.spec.ts:122:5 › G-F9-23 — the F.W1 checkpoint set, instrumented › item 5 — GCM-22's `p-0` insets, on a modal no spe
[chromium] › e2e/visual-checkpoint.spec.ts:80:5 › G-F9-23 — the F.W1 checkpoint set, instrumented › items 1 · 6 · 7 — the card's shadow, its badge rim
```

**Run-1-only (load-flaky; passed at run 2)**, 6 — ⟨cmd⟩ `comm -23 fail1 fail2`:

```
[chromium] › e2e/visual-baseline.spec.ts:57:9 › π capture [before] equation @ 1440x900
[chromium] › e2e/visualization-crud.spec.ts:481:9 › visualization CRUD lifecycle @ desktop (1440×900) @mutating › full lifecycle: upload → draft → pub
[chromium] › e2e/visualization-crud.spec.ts:481:9 › visualization CRUD lifecycle @ laptop (1280×800) @mutating › full lifecycle: upload → draft → publ
[chromium] › e2e/visualization-crud.spec.ts:481:9 › visualization CRUD lifecycle @ mobile (375×667) @mutating › full lifecycle: upload → draft → publi
[chromium] › e2e/visualization-ux.spec.ts:95:5 › B.W2 — visualization UX coherence (a11y keystones) › keystone: workspace default has no serious/criti
[chromium] › e2e/workspace-flow.spec.ts:83:5 › Asset-based workspace flow › extract-contour returns contour asset
```

**Baseline findings (for the unit seats; none re-opens a ruling):**

- **B-1 (instrument, VITIATED run discarded).** A first full-suite attempt ran against the playwright
  default `BASE_URL` `http://localhost:3000` — measured to be value.js's `palette-api`
  (⟨cmd⟩ `curl -s localhost:3000/` → `{"status":"ok","service":"palette-api"}`; pid cwd `value.js/api`),
  not fourier: 82 failed · 1 passed · 21 did not run. It is NOT a baseline. The fourier dev stack is
  `http://localhost:3100` (`<title>Fourier Analysis`, pid cwd `fourier-analysis`). **Every F.W11 e2e
  command must set `BASE_URL=http://localhost:3100`.**
- **B-2 (R.2 GREEN-BEFORE-CURE, G-F11-5).** The import-count leg is already 8/8 at `735319e`: every
  chrome file imports *something* from `@mkbabb/glass-ui`. The count cannot witness `.c`'s act, so
  `.c`'s receipt must carry the substantive witness per file: which producer surface primitive
  (panel/card/configurator/dialog) now renders the file's outer chrome, cited by its import line
  and its root element, plus the committed screenshot.
- **B-3 (the grid's producer seam, G-F11-3).** The grid paint is the PRODUCER's: glass-ui 8.0.0
  `dist/styles/paper.css` ships `@layer components { .paper-grid { background-image: var(--paper-grid-texture); … } }`
  (+ `.dark` blend and a `prefers-reduced-transparency` arm). Locally `PaperView.vue:691-704` reuses
  the SAME class name for the sidebar/content column layout (`display:grid`; 220px + 48rem at ≥1024px).
  The class does two jobs; `.b` separates them at the root (the layout gets its own local name; the
  producer's class, or its `--paper-grid-texture` token, paints a fixed full-viewport layer at the app
  root under `App.vue`'s existing `paper-texture` shell `:124`) — never a copied producer rule.
- **B-4 (e2e floor).** The 14 stable failures are pre-existing (F.W4/F.W9/F.W10S honest-REDs and
  producer relays A-2/A-3 among them). G-F11-6 at `.d` compares its failure set to THIS stable set;
  a run-1-only row reappearing is re-run once in isolation before it counts.

## Unit plan

Order (spec §State **Agents** line, :4, and §0ao): **[`.a` ∥ `.b`] → [`.c`] → [`.d`]**. The
orchestrator's 1-concurrent cap turns the first group into two serial groups; `.a` and `.b` share
one modify path (`web/src/components/paper/PaperView.vue`: `.a` owns `:623`'s radius, `.b` owns
`:455`/`:691-699`'s grid), so they may not run concurrently in any case. Dispatched groups:
`[F.W11.a] → [F.W11.b] → [F.W11.c] → [F.W11.d]`. Every unit pushes `m/w1-bump-migration` at its close.

| unit | model | spec sections | writable (spec §1 Bounds) | gates it turns | locks |
|---|---|---|---|---|---|
| `F.W11.a` Radii and surfaces | opus | §2 `### F.W11.a` (:26-27) · §3 G-F11-1/-2 (:36-37) | `web/src/**` (root cures) | G-F11-1 · G-F11-2 | one commit per component family; tokens read from producer `dist/**/*.css`, never invented |
| `F.W11.b` The grid is the app's | opus | §2 `### F.W11.b` (:28-29) · §3 G-F11-3 (:38) | `web/src/**` · `web/e2e/screenshots/**` | G-F11-3 | paper keeps its own surface; probe ≤3 viewports × 3 routes (§0ao law, §5.2) |
| `F.W11.c` Dock + visualizer glass idiom | **fable** (design-author) | §2 `### F.W11.c` (:30-31) · §3 G-F11-4/-5 (:39-40) | `web/src/**` · create `web/src/components/layout/AppDock.vue` · `web/e2e/shell-header.spec.ts` · `web/e2e/visualization-ux.spec.ts` (same-commit-as-markup only) · `web/e2e/screenshots/**` | G-F11-4 · G-F11-5 | spec-edit ONLY in the commit of the markup it measures, names unchanged, no skip/fixme/loosening; one home per control; producer gaps = BK relay rows, never a local dock |
| `F.W11.d` Close | opus (verify-only) | §2 `### F.W11.d` (:32-33) · §3 all (:35-42) · §4 Close act (:44-46) | value.js record · `LEDGER.md` · `INBOX.md` | G-F11-6 · G-F11-7 (+ re-run 1..5 double) | honest-RED only with a relay id |

Common bounds: **Do NOT touch** `glass-ui/**` · `web/package.json` deps · `fr-*.md` · `scripts/dev/dev.sh`
· every other `web/e2e/*.spec.ts`. All units may append their receipt to this record.
Every e2e command in this wave runs with `BASE_URL=http://localhost:3100` (Baseline B-1). `.c` answers
B-2 with a per-file substantive witness; `.b` answers B-3 by separating the layout class from the producer paint.

## Unit receipts

### F.W11.a — Radii and surfaces (`claude-opus-5-5[1m]`, 2026-09-22)

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` + the J-audit `docs/**/*.png`
  rows (outside `web/src/**`, not this unit's, untouched). Nothing inherited in `web/src/**`; record clean.
- **Rulings consumed**: COHESION §0ao (OA-1, owner verbatim); §0j (glass-ui READ-ONLY); §0ap tail — nothing for F.W11.a.
- **Anchors at true bytes** (fourier `735319e`): all six G-F11-1 sites and all 43 G-F11-2(a) sites matched the spec's
  cited lines (no drift). The `rounded-none` = `GalleryDraftsSection.vue:98`. G-F11-2(b)'s 47 = the 43 + `style.css:337`
  (`inherit`) + three prose comments (`PaperArticleWindow.vue:194` · `FourierMorphDemo.vue:308` · `HarmonicLevelGrid.vue:310`).
- **Tokens read, never invented** — ⟨cmd⟩ `tr ';{}' '\n\n\n' < web/node_modules/@mkbabb/glass-ui/dist/styles/theme/radius.css | grep -- --radius`
  → `--radius 0.625rem · -xs 4px · -sm 4px · -md 6px · -lg var(--radius) · -xl 12px · -2xl 1rem · -3xl 1.5rem · -pill 9999px ·
  -card · -panel (=xl) · -dialog · -media · -button · -field (=2xl) · -strip · -control · -badge · -dock · -tab · -floor (=xs) · -ctx · -inset`.
  Each also verified in the built bundle: ⟨cmd⟩ `cat web/dist/assets/*.css | grep -c -- "--radius-$t:"` → 1 for each of
  sm xs md lg panel pill badge control field floor.
- **Producer surfaces read (the "yield" rule)**, each ⟨cmd⟩ a grep over `glass-ui/dist`: `[data-slot="dialog-content"]{border-radius: var(--radius-3xl)}` ·
  `.popover-content{border-radius: var(--radius-panel)}` · `.interactive-item{…border-radius: var(--radius-lg)}` (menu rows,
  `menu__item interactive-item glass-menu-row`) · `.glass-resting{…border-radius: var(--radius-ctx)}` · `.glass-wash{…var(--radius-ctx)}` ·
  `button{border-radius:var(--radius-button)}` · SelectTrigger chunk `rounded-pill`. `.scroll-progress` ships NO radius (token applied locally).
- **Acts, one commit per component family** (fourier `m/w1-bump-migration`; each site guarded by an exact-bytes check before edit):
  1. `e844536` **paper** — `PaperView:623` progress bar `0 1px 1px 0` → `--radius-pill` (the squared left edge abrogated) ·
     `:660` `.paper-article` + `PaperSidebar:249` + `PaperArticleWindow:285` `0.75rem` → `--radius-panel` · `:728` 2rem
     `.overlay-btn` `50%` → `--radius-pill` · `:776` 16px badge `8px` → `--radius-badge` · `:797` + `MobileFloatingToc:370`
     `0.375rem` → `--radius-md` · `MobileFloatingToc:266` + `PaperSidebar:285` `0.25rem` → `--radius-sm`.
  2. `058ec3f` **paper search** — `PaperSearchDropdown` `.paper-search-results--floating` (radius 0 + `border-left/right: none`,
     which served only the squared seam) DELETED with its now-dead `:class` binding (⟨cmd⟩ `grep -rn 'paper-search-results--' web/src web/e2e`
     → the binding only); the floating panel reads its base token corner · `PaperSearchInput:219` radius 0 deleted from the
     floating arm (its `:focus-within` outline now follows the base token) · `:200` + `ResultRow:81` `3px` → `--radius-xs` ·
     `ResultRow:130` `<mark>` `1px` → `--radius-floor`.
  3. `a4abe7c` **visualization** — YIELD: `FullscreenViewer:179` `.fs-dialog` radius 0 (→ the producer dialog's `--radius-3xl`)
     and `:196` children radius 0 (→ their own token corners), comments annotated · `GalleryDraftsSection:98` `rounded-none` removed
     (→ glass Button `--radius-button`) · `SpeedSelect:103` (→ SelectTrigger `rounded-pill`) · `AnimationControls:312` `.menu-item`
     (→ `.interactive-item` `--radius-lg`) · `GallerySearchBar:254` `.filter-panel.glass-resting` (→ `--radius-ctx`). TOKENS:
     play-btn + `::before`/`::after` `9999px` → `--radius-control` · `.mini-progress`/`.mini-fill` `2px` on a 4px bar → `--radius-pill` ·
     `CanvasControlsDock:176` 6px dot → `--radius-pill` · `GallerySearchBar:210` `.search-pill` field `0.5rem` → `--radius-field` ·
     `ContourSettings:441` banner `0.5rem` → `--radius-lg` · `:461` + `ExportModal:106` `0.375rem` → `--radius-md` · `ContourSettings:422`
     + `BasisSelector:308` `0.25rem` → `--radius-sm`.
  4. `16a6ea4` **equation** — YIELD: `EquationView:693` `.info-hovercard` (glass `PopoverContent` → `.popover-content` `--radius-panel`) ·
     `ConvergenceLegend:62` `.glass-wash` plate (→ `--radius-ctx`). TOKENS: `EquationView:642` `3px` → `--radius-xs` · `FrequencyGraph:258`
     scrollbar thumb `2px` on 4px → `--radius-pill` · `FunctionInput:291` `.preset-pill` on a glass Button `9999px` → `--radius-control`.
  5. `9e4d523` **morph** — `HarmonicLevelGrid:240` · `MorphPhaseConfig:262` · `FourierShapeExtractor:292` `0.375rem` → `--radius-md`;
     prose at `FourierMorphDemo:308` / `HarmonicLevelGrid:310` reworded to "corner radius" (G-F11-2(b) counts declarations; no rule changed).
  6. `f3d1bf9` **layout** — `DarkModeToggle:209` square `50%` → `--radius-pill` · `AppHeader:192` `0.4375rem` → `--radius-md` ·
     `:292` 1.75rem badge `9999px` → `--radius-badge` (AppHeader's fate is `.c`'s; its corners are tokens meanwhile).
  7. `0824371` **focus ring** — `style.css:337` `border-radius: inherit` DELETED = banked `GCM-23` TAKEN: it re-shaped a focused
     element to its PARENT's corner; all four ringed classes own their corner (`.sidebar-link` PaperSidebar `calc(var(--radius) - 2px)` ·
     `.floating-toc-item` `--radius-md` · `.callout-btn` glass Button `--radius-button` · `.gallery-card` `rounded-xl`) and an outline
     follows its own element. `PaperArticleWindow`'s `PAW-22` note records the taking.
- **Push**: ⟨cmd⟩ `git push origin m/w1-bump-migration` then `git fetch; git rev-parse --short HEAD origin/m/w1-bump-migration` → `0824371` · `0824371`.
- **Gates, BEFORE → AFTER** (cwd `fourier-analysis`, HEAD `0824371`, double-run on the settled bytes):
  | gate | BEFORE (Baseline) | AFTER run 1 · run 2 |
  |---|---|---|
  | G-F11-1 `grep -rEn 'border-radius:\s*0[^.]\|rounded-none' web/src \| wc -l` | 6 | **0 · 0** GREEN |
  | G-F11-2(a) `grep -rEn 'border-radius:\s*[0-9]' web/src \| wc -l` | 43 | **0 · 0** GREEN |
  | G-F11-2(b) `grep -rEn 'border-radius' web/src \| grep -vc 'var(--radius'` | 47 | **0 · 0** GREEN |
  | G-F11-6 build leg `cd web && npm run build` | exit 0 | **exit 0** (`✓ built in 11.16s`) |
  Wider squared-edge sweep: ⟨cmd⟩ `grep -rEn 'radius:\s*0\|rounded-[a-z]*-?none\|rounded-\[0' web/src` → 0 lines.
  Regression floor (not this unit's gate): ⟨cmd⟩ `BASE_URL=http://localhost:3100 npx playwright test e2e/shell-header.spec.ts
  e2e/visualization-ux.spec.ts --project=chromium` → `13 passed (23.8s)`.
- **Residuals (named, not closed here)**: (R-a-1) five `calc(var(--radius) - Npx)` sites (`PaperSidebar:355/:389` · `PaperSearchInput:135` ·
  `PaperSearchDropdown:211` · `PaperSearchResultRow:65` · `PaperSearchModal:259`) are token-derived and pass both gates; untouched.
  (R-a-2) native `<input>`s at `HarmonicLevelGrid .level-input` · `MorphPhaseConfig .num-input` and the `PaperSearchInput` /
  `GallerySearchBar` field wraps now read tokens but are not glass-ui `Input`s — a component swap is `.c`'s visualizer-idiom
  scope (search/morph are outside it), flagged for `.c`/`.d` to adjudicate, no act here. (R-a-3) the fullscreen dialog now shows
  its producer `--radius-3xl` corners at the viewport edge (the yield the spec prescribes); `fullscreen.spec.ts:40` is a banked
  baseline RED (G-F9-11), not re-measured by this unit.
- **Escalations**: none. **Writes outside the set**: none (fourier `web/src/**` + this record only).

### F.W11.b — The grid is the app's (`claude-opus-5-5[1m]`, 2026-09-22)

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` + the J-tranche
  `docs/.../before/*.png` rows only — nothing inside `web/src/**` or `web/e2e/screenshots/**`; nothing inherited.
- **Anchors at true bytes** (spec `:455/:691`, B-3 `:691-699`): ⟨cmd⟩ `grep -n paper-grid web/src/components/paper/PaperView.vue`
  → `:455` `<div class="paper-grid">` · `:691` `.paper-grid {` · `:699` `.paper-grid {` (≥1024px arm) — held, no drift. A 4th
  mention, prose only: `useScrollNavigation.ts:400` (comment naming the layout element). Producer seam re-read:
  `node_modules/@mkbabb/glass-ui/dist/styles/paper.css` → `@layer components{.paper-grid{background-image: var(--paper-grid-texture);…}
  .dark .paper-grid{background-blend-mode: screen;} @media (prefers-reduced-transparency: reduce){…none}}`, reached via
  `style.css:3 @import "@mkbabb/glass-ui/styles"`. `/visualize` = the `alias` of `/w/:imageSlug?` (router `:75`); `/` redirects to the saved tab.
- **Act 1 — separate the two jobs at the root** (fourier `576e40a`, 3 files, +13/−5):
  (i) layout: `PaperView.vue:455/:691/:699` `.paper-grid` → `.paper-columns` (declarations byte-identical: `display:grid`,
  `1fr`, and `220px minmax(0, 48rem)` at ≥1024px); the `useScrollNavigation.ts:400` comment follows the rename.
  (ii) paint: `App.vue` shell gains `isolate` and, as its first child, `<div class="paper-grid fixed inset-0 -z-10
  pointer-events-none" aria-hidden="true" />` — the PRODUCER's class, no rule copied; `isolate` makes the shell the
  stacking context so `-z-10` sits above the shell's `bg-background paper-texture` ground and under every route's content.
  Theme-awareness is the producer's `.dark` arm. The paper keeps its own surface (`.paper-article`: `background: var(--card)`,
  untouched).
- **Act 2 — bounded probe** (§5.2; 3 viewports × 3 routes, node + playwright chromium, `BASE_URL=http://localhost:3100`,
  per cell: count of `.paper-grid`, its `getBoundingClientRect()` vs `innerWidth×innerHeight`, computed `background-image ≠ none`):
  ⟨cmd⟩ `node gridprobe.mjs` run 1 → every cell `n=1 box={x:0,y:0,w:vw,h:vh} pos:fixed z:-10 bg:linear-gradient(…)` →
  **`PASS 9/9`**; run 2 → **`PASS 9/9`**. Screenshots (run 1) read by eye at `/paper` 1440×900 and `/visualize` 1280×800:
  grid under header, sidebar, card and canvas margins, full viewport; the paper card sits on it.
- **Act 3 — evidence** (fourier `78ab4f9`): 9 PNGs `web/e2e/screenshots/f-w11-b/{root,paper,visualize}-{1440x900,1280x800,375x667}.png`,
  force-added — ⟨cmd⟩ `git check-ignore -v …` → `web/.gitignore:1:*.png` (precedent: 6 tracked PNGs under `web/`); `.gitignore` not touched.
- **Build**: ⟨cmd⟩ `cd web && npm run build` → **exit 0** (`✓ built in 3.25s`).
- **Push**: ⟨cmd⟩ `git push origin m/w1-bump-migration` → ⟨cmd⟩ `git rev-parse --short origin/m/w1-bump-migration` → `78ab4f9`.
- **G-F11-3 BEFORE → AFTER**: bytes ⟨cmd⟩ `grep -c paper-grid web/src/components/paper/PaperView.vue` → 3 → **0 · 0**;
  browser 0/3 (1440 only; `div.paper-grid` 976×131574, absent on `/visualize`) → **9/9 · 9/9**; screenshots committed. **GREEN.**
- **Residuals**: (R-b-1) the layer honours the producer's `prefers-reduced-transparency` arm (grid off) — by design, not probed.
  (R-b-2) the `isolate` shell makes teleported overlays (body-level) paint above all in-shell chrome regardless of in-shell
  z-index; no e2e was run by this unit — `.d`'s G-F11-6 full run is the witness.
- **Escalations**: none. **Writes outside the set**: none (fourier `web/src/**` · `web/e2e/screenshots/**` · this record).

### F.W11.c — The dock and the visualizer's glass idiom (`claude-fable-5-1`, design-author, 2026-09-22)

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` + modified `docs/tranches/J/audit/screenshots/before/*.png`
  (outside every F.W11 row, untouched); value.js `execution/C/F-W11.md` · `INBOX.md` clean. Nothing inherited in `web/**`.
- **Open state**: fourier HEAD `78ab4f9` = `origin/m/w1-bump-migration` (`.b`'s close); `.a`/`.b` receipts read; COHESION §0j core + §0j.D +
  §0ao read whole, §0k–§0an grepped for `AppHeader|AppDock|GlassDock|glass-ui/dock|DockLayer|DockTrigger` → 0 rows (no ruling beyond §0ao
  consumes here). Dev stack ⟨cmd⟩ `curl -s localhost:3100/ | grep -o '<title>[^<]*'` → `<title>Fourier Analysis` (B-1 honoured on every e2e command).
- **Anchors measured before edit**:
  - Spec §2 `.c` names the header's controls "navigation, search, `About Fourier analysis`, `Dark mode`". At the true bytes `AppHeader.vue`
    (413 lines, read whole) carried NO search control: ⟨cmd⟩ `grep -rln search web/src/components/layout web/src/App.vue` → 0. The search fields
    in the tree are route-scoped (`PaperSearchInput` on `/paper`, `GallerySearchBar` on `/gallery`), not shell controls, and lifting one into
    the dock would give a control two homes. **INTENT taken at the true bytes**: the dock carries the header's ACTUAL controls — the About
    trigger, the nav trigger, the account group (`UserSlugBar`: `Log in` / `Copy your slug` / `Log out` / slug field, + the admin badge, a status
    mark), and `Dark mode`. No search control was invented; the producer's `search` slot stays unused. (D-19-class drift, recorded.)
  - `coarse-pointer.spec.ts:115` (READ-ONLY, not this unit's) keys on `header.app-header` and measures every control's `boundingBox()`
    (`minAxis`, :38-41) — so the dock must live inside that landmark and each control's OWN box must reach 44px under coarse emulation.
  - Producer read (`web/node_modules/@mkbabb/glass-ui` 8.0.0): `DockTrigger for="popover"` renders reka's bare `PopoverTrigger`
    (`src/components/dock/DockTrigger.vue`), which needs the click root — glass `Popover trigger="hover"` seats a `HoverCardRoot` on a fine
    pointer (`popover/Popover.vue:85`). The 44px floor for trigger faces is a `::after` hit pseudo (`dock/styles/controls/touch-floor.css`),
    while the face's own box is `min-height: var(--dock-trigger-min-height, auto)` (`controls/triggers.css`) — the producer publishes the
    hook; `.dock-control` is sized by `--dock-control-size` (≥44px coarse via `--dock-control-floor`, `density.css`).
  - The eight chrome files' outer chrome at `78ab4f9`: `CoefficientsPanel:14` + `ContourSettings:225` `ConfiguratorLayer` · `AnimationControls:118`
    + `CanvasControlsDock:41` + `EditorControlsDock:63` `GlassDock` · `ExportModal:46` `Dialog` — six of eight ALREADY on producer surfaces
    (the spec's premise drifted); `EquationPanel:59` `div.eq-panel.glass-wash` (a hand-copied producer recipe class + a local `box-shadow` +
    `rounded-xl`) and `GlassTimeline:207` `div.timeline-row` (a control row that paints no plate; its surface is the enclosing dock; the producer's
    `timeline` family is a segment list, not a scrubber — `dist/components/timeline/types.d.ts`) were the two to judge.
- **Acts (fourier `m/w1-bump-migration`, pathspec commits, one per meaning)**:
  1. `53aaa6f` **the dock** — `web/src/components/layout/AppDock.vue` CREATED (line 1 `SERVED MODEL: claude-fable-5-1`; 297 lines):
     `<header class="app-header">` (the landmark the coarse spec keys on; a plain centered flex row — no plate, no sticky, no backdrop filter)
     wrapping `<GlassDock orientation="horizontal" shape="pill" always-expanded>` from `@mkbabb/glass-ui/dock` with, in order: `Popover` (click
     root) + `DockTrigger for="popover" aria-label="About Fourier analysis"` (the ℱ wordmark face) + the attribution `PopoverContent` ·
     `DockSeparator` · `DropdownMenu` + `DockTrigger for="dropdown" :aria-label="Navigate — current section ${label}"` + the five `DropdownMenuItem`
     rows with `aria-current` · `DockSeparator` · `<div class="contents" role="group" aria-label="Account">` (admin badge + `UserSlugBar`) ·
     `DockSeparator` · `DarkModeToggle` (`aria-label="Dark mode"`, `aria-pressed`). Floors: `.app-dock{--dock-trigger-min-height: var(--dock-control-size)}`
     + `.logo-trigger,.nav-trigger{min-inline-size: var(--dock-control-size)}` + `.dark-mode-toggle{--toggle-size: var(--dock-control-size)}` —
     one cell, one token. Portaled-surface divergence kept inside `@layer glass-overrides` as before. `AppHeader.vue` DELETED (`git rm`;
     every control has one home). `App.vue:7/:151` import + mount → `AppDock`; the `MG-epsilon` comment rewritten as the decided choice (the
     inert `sticky`/`backdrop-blur` treatment retired with the header). `useFourierMorph.ts:28` mount-chain comment → `App → AppDock (né AppHeader)`.
     Stale `AppHeader.vue:174-177` citations in `style.css:307/:323` are D.W4 historical records — left (E-3).
  2. `6d1e53d` **the equation card** — `EquationPanel.vue`: `div.eq-panel.glass-wash` → `<Card tier="floating" size="sm" class="eq-panel">` with
     `CardHeader` (title · energy `Metric` · close) and `CardContent` (notation pills · terms slider · KaTeX), import `{ Card, CardHeader, CardContent }
     from "@mkbabb/glass-ui/card"` (:12); `.eq-panel` keeps only position/z/offset/max-width — `p-2.5`, `rounded-xl` and the local `box-shadow`
     retire to the producer's `--card-pad` rhythm, `--radius-card` corner and floating cast.
  3. `ce176e6` **evidence** — `web/e2e/screenshots/f-w11-c/{dock-1440x900-header,dock-375x667-coarse-header,equation-card-1440x900}.png`
     (`git add -f`, the `web/.gitignore:1 *.png` rule, as `.b` did for `f-w11-b/`).
- **Push**: ⟨cmd⟩ `git push origin m/w1-bump-migration; git fetch; git rev-parse --short HEAD origin/m/w1-bump-migration` → `ce176e6` · `ce176e6`.
  ⟨cmd⟩ `git status --porcelain web` → empty.

- **Gates, BEFORE → AFTER** (cwd `fourier-analysis`, HEAD `ce176e6`, settled bytes, double-run):
  | gate | BEFORE (Baseline) | AFTER run 1 · run 2 |
  |---|---|---|
  | G-F11-4 (bytes) `test -f web/src/components/layout/AppDock.vue` · `grep -n GlassDock AppDock.vue \| grep glass-ui/dock` | absent · 3 dock files, none a shell dock | **present** · `:35 import { GlassDock, DockTrigger, DockSeparator } from "@mkbabb/glass-ui/dock"` — both runs; `grep -rln glass-ui/dock web/src \| wc -l` → **4 · 4** |
  | G-F11-4 (e2e) `BASE_URL=http://localhost:3100 npx playwright test e2e/shell-header.spec.ts e2e/coarse-pointer.spec.ts --reporter=list` | 4/4 · 4/4 at the OLD markup | **8 passed (2.8s)** — `shell-header` [chromium] 4/4 (About card opens by click · nav trigger named `Navigate — current section Gallery` + 5 menuitems · navigates and re-names to `… Equation` · `Dark mode` `aria-pressed` flips, name stable) · `coarse-pointer` [mobile-chromium] 4/4 incl. `:105` every shell-header control ≥ 44px. **Neither spec edited** (0 bytes; the LOCK's same-commit clause never engaged). |
  | G-F11-4 (browser floors, coarse 375×667, `isMobile`+`hasTouch`) | — | dock box 300×66; `About Fourier analysis` **46.8×46.8** · `Navigate — current section Paper` **62.7×46.8** · `Log in` **44×44** · `Dark mode` **46.8×46.8**; fine 1440×900: dock 503×56, controls 40px (the comfortable rung). 0 console errors both. |
  | G-F11-5 (bytes) per-file `grep -c '@mkbabb/glass-ui'` | 1·4·2·3·2·4·1·3 (8/8, GREEN-BEFORE-CURE) | CoefficientsPanel **1** · ContourSettings **4** · EquationPanel **3** · AnimationControls **3** · CanvasControlsDock **2** · EditorControlsDock **4** · GlassTimeline **1** · ExportModal **3** → **8/8 · 8/8** |
  | G-F11-5 (e2e) `BASE_URL=http://localhost:3100 npx playwright test e2e/visualization-ux.spec.ts --project=chromium --reporter=list` | run 1 `:95` load-flaky, run 2 all passed | **9 passed (19.5s)** — keystones 1–8 + `save_contour_then_recompute`; `:95` passed at run 1 (1 worker) |
  | G-F11-6 (kept, not this unit's) `npx vue-tsc -b` · `npm run build` | exit 0 | **exit 0 · exit 0** (vue-tsc twice; build once, `✓ built`) |
- **B-2 answered — the substantive witness per chrome file** (surface primitive · its import line · its root element · evidence):
  | file | producer surface at the root | import | root | evidence |
  |---|---|---|---|---|
  | `CoefficientsPanel.vue` | `ConfiguratorLayer` (configurator) — unchanged | `:4` | `:14` | `equation-card-1440x900.png` (right column, "Coefficients") |
  | `ContourSettings.vue` | `ConfiguratorLayer` + `ConfiguratorRow` — unchanged | `:19` | `:225` | same (right column, "Contour") |
  | `EquationPanel.vue` | **`Card` (`Surface`, `tier="floating"`) + `CardHeader` + `CardContent`** — CURED HERE | `:12` | `:64` | same (top-left card); probe: `data-slot="card"`, classes `glass-floating`, `border-radius 16px` (= `--radius-card`), producer cast, header+content slots present |
  | `AnimationControls.vue` | `GlassDock` + `DockTrigger for="dropdown"` — unchanged | `:8` | `:118` | same (bottom pill) |
  | `CanvasControlsDock.vue` | `GlassDock` + `DockControl` + `DockSeparator` — unchanged | `:7` | `:41` | same (top-right pill) |
  | `EditorControlsDock.vue` | `GlassDock` + `DockControl` + `DockSeparator` — unchanged | `:6` | `:63` | (edit mode; not opened — keystone 1 covers the mount) |
  | `GlassTimeline.vue` | the producer `Slider` in a control row whose plate IS the enclosing `GlassDock` (AnimationControls `:105`, ConvergenceTimeline `:91`); no plate of its own to re-express | `:70` | `:207` | same (inside the bottom pill) |
  | `ExportModal.vue` | `Dialog` + `DialogContent` (dialog) — unchanged | `:11` | `:46` | keystone 3 (Dialog-open a11y-clean) |
- **Design notes (why each surface is what it is)**:
  - *A dock, not a bar*: the owner's OA-3 asks for "a dock" — a pill (`shape="pill"`, the producer's default) centered in the landmark, always
    expanded (a shell nav never auto-collapses), the glass plate being the only surface; the landmark row keeps no plate, so the app's grid
    (`.b`) shows through around it. `position` stays `inline` because the landmark is a flex sibling of the app's sole scroller — `fixed`/`sticky`
    would re-create the inert treatment `MG-epsilon` flagged.
  - *Faces*: transient surfaces ride `DockTrigger` (the producer's one trigger face) so hover plate, press spring, specular gleam and the coarse
    hit pseudo are the producer's; `DockControl` is not used because no shell control is a plain button (the dark-mode control is its own
    morphing component with the same `aria-pressed` contract), and `DockLayer` is not used because the shell owns no selectable panes.
  - *The About card is a click disclosure on both pointers* (was hover on fine): the dock's popover face binds reka's `PopoverTrigger`, which
    needs the click root; one activation everywhere is the contract FR-AH-13 wanted for touch.
  - *Groups*: ordinary DOM with a name (`role="group" aria-label="Account"`) and a `DockSeparator` between groups, as the dock README prescribes;
    the admin badge is a status mark inside the account group, not a control.
  - *EquationPanel → `Card tier="floating"`*: it floats over the canvas at `--z-controls`, so the floating tier's own cast/blur/corner replace a
    hand-copied `.glass-wash` + a local shadow; `CardHeader`/`CardContent` give the producer's pad rhythm (`size="sm"` = the atom rung).
- **Residuals / observations (named, not closed here)**:
  - (R-c-1) The producer floors a `DockTrigger` face by a `::after` hit pseudo, not its own box; a consumer that omits the published
    `--dock-trigger-min-height` hook would measure < 44px by `boundingBox()` under coarse emulation while the tap target is in fact 44px.
    Not a gap (the hook exists and is used here); an instrument/README nuance for BK's attention in the next mail round, not a relay row.
  - (R-c-2) `.a`'s R-a-2 (`GallerySearchBar` field wraps are not glass `Input`s) is gallery chrome, outside the eight visualizer files — untouched.
  - (R-c-3) The "search" control the spec names does not exist in the shell (anchor drift above); the dock's `search` slot is unused by design.
- **E13 (Step-0, four paths, read-only)**: BK still the newest glass dir (⟨cmd⟩ `ls glass-ui/docs/tranches | grep '^B' | tail -1` → `BK`);
  files newer than 2026-09-22 00:00 — value.js `V/`: `EVIDENCE.md` · `VISUAL-CONSTITUTION.md` · `OPTICAL-BENCH-COMPOSITIONS.md` (Track A tranche
  documents, not letters) · `V/coordination`: `INBOX.md` · BK/coordination: the same three letters already rowed at Open · keyframes `V/coordination`
  + atlas `P/coordination`: empty → **0 unrowed, 0 UNREAD in scope**; INBOX not written.
- **Bounds**: writes = `web/src/components/layout/{AppDock.vue (new), AppHeader.vue (deleted)}` · `web/src/App.vue` · `web/src/composables/useFourierMorph.ts`
  · `web/src/components/visualization/EquationPanel.vue` · `web/e2e/screenshots/f-w11-c/*.png` · this record. Specs untouched; glass-ui untouched;
  `scripts/dev/dev.sh` untouched; no LEDGER edit (the row is `.d`'s).
- **Verdict**: G-F11-4 **GREEN** · G-F11-5 **GREEN** (bytes 8/8 with the per-file witness above; keystones 9/9). **DONE.**

## RESUME 1 (2026-09-22, seat 0 re-open, `claude-opus-5-5[1m]`, VERIFY-ONLY)

- **Units already done**: none. ⟨cmd⟩ `git -C fourier-analysis log --oneline --all | grep -i W11` → no `X.F.W11` commit
  (fourier HEAD still `735319e`); value.js carries only the OPEN (`584a899a`) and the chassis note (`3c388fb3`: the first
  `.a` seat refused on the harness's latest-message framing, no bytes written). All four units owed.
- **Crash-recovery**: fourier-analysis porcelain → `?? .worktrees/` plus modified `docs/tranches/J/audit/screenshots/before/*.png`
  — outside every F.W11 writable row (`web/src/**` · `web/e2e/**`), not this wave's, untouched. Nothing inherited in `web/**`.
- **E13 re-sweep**: four paths, BK still newest; the only files newer than 2026-09-22 00:00 are the three BK letters already
  noted at Open → 0 unrowed, 0 UNREAD in scope; INBOX not touched.
- **Baseline**: cited from `## Baseline` (banked this same day at `735319e`, tree unmoved) — no re-run owed.
- **Dispatch unchanged**: `[F.W11.a] → [F.W11.b] → [F.W11.c] (fable) → [F.W11.d]`.
