SERVED MODEL: claude-opus-5-5[1m]

# X.F.W11 — the owner audit docket, F-side (execution record)

Spec: `docs/tranches/X/fourier/waves/F-W11.md` (46 lines, read whole) — minted by COHESION **§0ao**
(`:2531-2565`, the owner's live audit docket OA-1..OA-6, verbatim there); F-side rows OA-1 · OA-2 ·
OA-3. §0ao.1 (OA-7..OA-10) routes to `KF.W13T.e`, not here. Sitting of record 2026-09-17 (the
owner's begin-word, COHESION §0j); this seat's clock 2026-09-22. Seat 0 = OPEN, VERIFY-AND-BANK:
zero fourier-analysis bytes, zero glass-ui bytes, zero product bytes written.

## Open

- **Date**: 2026-09-22 (sitting 2026-09-17, §0j). Track C · X·F. Seat 0 `claude-opus-5-5[1m]`.
- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` only (no
  inherited partial work inside `web/src/**` or `web/e2e/**`); value.js writable set
  (`execution/C/F-W11.md` · `LEDGER.md` · `INBOX.md`) clean — the record did not exist.
- **Precondition 1 — F.W10S CLOSED** (spec §State "Opens after"): LEDGER row `F.W10S` reads
  `CLOSED 2026-09-17 (honest-RED: G-F9-11 · G-F9-8 crud:664 · G-F9-5 contrast-floor leg)` ⟵ CHECK 2
  CONFORMANT-HONEST-RED; COHESION §0al (`:2453`) "every X·F wave is CLOSED; Track C is COMPLETE";
  the §0al act (`:664` note carries E-F10S-b2/A-3) landed at fourier `735319e`. **MET.**
- **Precondition 2 — baseline tree**: ⟨cmd⟩ `git -C fourier-analysis log --oneline -1` →
  `735319e docs(web/e2e): crud :664 keystone note carries its escalation id (E-F10S-b2 / A-3) — COHESION §0al`;
  branch `m/w1-bump-migration`; ⟨cmd⟩ `git rev-parse --short origin/m/w1-bump-migration` → `735319e`
  (pushed, equal to the spec's named baseline). **MET.**
- **Owner-gated items**: none open. The wave is minted by owner ruling (§0ao, verbatim quote there);
  glass-ui READ-ONLY and gaps-by-mail per §0j; producer is glass-ui **8.0.0** in
  `web/node_modules/@mkbabb/glass-ui/package.json` (the §0i ratified pin).
- **E13 Step-0 mail sweep** (four paths, read-only; BK confirmed newest: ⟨cmd⟩ `ls glass-ui/docs/tranches | tail -4`
  → `… BI BJ BK C D …`: BK is the last of the B-series, no `BL`). ⟨cmd⟩ `find <path> -maxdepth 1 -type f -newermt "2026-09-21 00:00"` →
  value.js `V/`: empty · `V/coordination`: `INBOX.md` only · BK/coordination: `glass-outbound-2026-09-22-consumers-10.0.0.md`
  (INBOX mentions ×5), `fourier-to-glass-2026-09-17-nwo1-bh-relay.md` (×2), `value-to-glassui-2026-09-DD-fw4-relay.md` (×3)
  — all three already rowed/noted · keyframes.js `V/coordination`: empty · atlas `P/coordination`: empty. Highest row
  `I-39`; **0 new rows, 0 UNREAD in scope**; a dated sweep line appended at the INBOX tail.

## Baseline

All commands run READ-ONLY at fourier `735319e` (worktree clean), cwd `fourier-analysis/`, 2026-09-22.

| gate | command (spec §3) | BEFORE (run 1 · run 2) | verdict |
|---|---|---|---|
| G-F11-1 | `grep -rEn 'border-radius:\s*0[^.]\|rounded-none' web/src \| wc -l` | **6 · 6** | RED (spec baseline 6 — reproduces) |
| G-F11-2 (a) | `grep -rEn 'border-radius:\s*[0-9]' web/src \| wc -l` | **43 · 43** | RED |
| G-F11-2 (b) | `grep -rEn 'border-radius' web/src \| grep -vc 'var(--radius'` | **47 · 47** | RED |
| G-F11-3 (bytes) | `grep -n 'paper-grid' web/src/components/paper/PaperView.vue` | `:455` `<div class="paper-grid">` · `:691` · `:699` | RED |
| G-F11-3 (browser) | one bounded probe, 1440×900, routes `/` `/paper` `/visualize` (node + `@playwright/test` chromium, computed `background-image` on every element wider than 300px) | grid layer = `div.paper-grid` **976×131574** on `/` and `/paper`; **absent** on `/visualize` → 0 of 3 cells at this viewport = viewport | RED |
| G-F11-4 (bytes) | `test -f web/src/components/layout/AppDock.vue` · `grep -rln 'glass-ui/dock' web/src` | **absent** · 3 files (`EditorControlsDock` · `CanvasControlsDock` · `AnimationControls`), none a shell dock | RED |
| G-F11-4 (e2e legs) | `shell-header.spec.ts` [chromium] · `coarse-pointer.spec.ts` [mobile-chromium] (inside the full run) | 4/4 passed · 4/4 passed (both runs) | legs GREEN at the OLD markup — they are the regression floor, not the gate |
| G-F11-5 (bytes) | per file `grep -c '@mkbabb/glass-ui'` over the eight chrome files | CoefficientsPanel 1 · ContourSettings 4 · EquationPanel 2 · AnimationControls 3 · CanvasControlsDock 2 · EditorControlsDock 4 · GlassTimeline 1 · ExportModal 3 → **8/8 ≥ 1** | **GREEN-BEFORE-CURE** (finding R.2-1) |
| G-F11-5 (e2e leg) | `visualization-ux.spec.ts` [chromium] keystones | run 1: `:95` timed out at `waitForLoadState("networkidle")` under 9-worker load; run 2: all passed | GREEN at run 2 (load-flaky at run 1) |
| G-F11-6 (build) | `cd web && npm run build` (`vue-tsc -b && vite build`) | **exit 0** (`✓ built in 5.23s`) | GREEN at baseline (the gate is "stays green") |
| G-F11-6 (e2e) | `cd web && BASE_URL=http://localhost:3100 npx playwright test --reporter=list` (107 tests, 2 projects, 9 workers) | run 1: **70 passed · 20 failed · 3 skipped · 14 did not run** · run 2: **90 passed · 14 failed · 3 skipped** | baseline failure set banked below |
| G-F11-7 | E13 sweep (Open) | 0 UNREAD in scope | GREEN at open (re-measured at `.d`) |

**The stable baseline failure set** (⟨cmd⟩ `comm -12 fail1 fail2 | wc -l` → **14**; every one in both runs):

```
[chromium] › e2e/contrast-floor.spec.ts:128:5 › X·F F.W4 — contrast floor (G-F4-CONTRAST-FLOOR) › every named pair has a re-derivable expression
[chromium] › e2e/contrast-floor.spec.ts:82:9 › X·F F.W4 — contrast floor (G-F4-CONTRAST-FLOOR) › every named pair meets its floor — dark arm
[chromium] › e2e/contrast-floor.spec.ts:82:9 › X·F F.W4 — contrast floor (G-F4-CONTRAST-FLOOR) › every named pair meets its floor — light arm
[chromium] › e2e/equation-interaction.spec.ts:77:5 › S2 — /equation: compute → notation → budget → reload (G-F9-6) › the four-step interaction drives 
[chromium] › e2e/fullscreen.spec.ts:40:5 › Fullscreen viewer (G-F9-11) › enters fullscreen and asserts against the teleported root @mutating
[chromium] › e2e/gallery-admin-a11y.spec.ts:103:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin users panel
[chromium] › e2e/gallery-admin-a11y.spec.ts:114:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin flagged panel
[chromium] › e2e/gallery-admin-a11y.spec.ts:125:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin audit log panel
[chromium] › e2e/gallery-admin-a11y.spec.ts:91:5 › X·F F.W4 — admin-mode a11y (G-F4-ADMIN-AXE) › admin banner: the stat cells are graded for the first
[chromium] › e2e/visual-baseline.spec.ts:57:9 › π capture [before] equation @ 1280x800
[chromium] › e2e/visual-baseline.spec.ts:57:9 › π capture [before] equation @ 375x667
[chromium] › e2e/visual-checkpoint.spec.ts:101:5 › G-F9-23 — the F.W1 checkpoint set, instrumented › item 2 — `cartoon-card` hover, the state no spec 
[chromium] › e2e/visual-checkpoint.spec.ts:122:5 › G-F9-23 — the F.W1 checkpoint set, instrumented › item 5 — GCM-22's `p-0` insets, on a modal no spe
[chromium] › e2e/visual-checkpoint.spec.ts:80:5 › G-F9-23 — the F.W1 checkpoint set, instrumented › items 1 · 6 · 7 — the card's shadow, its badge rim
```

**Run-1-only (load-flaky; passed at run 2)**, 6 — ⟨cmd⟩ `comm -23 fail1 fail2`:

```
[chromium] › e2e/visual-baseline.spec.ts:57:9 › π capture [before] equation @ 1440x900
[chromium] › e2e/visualization-crud.spec.ts:481:9 › visualization CRUD lifecycle @ desktop (1440×900) @mutating › full lifecycle: upload → draft → pub
[chromium] › e2e/visualization-crud.spec.ts:481:9 › visualization CRUD lifecycle @ laptop (1280×800) @mutating › full lifecycle: upload → draft → publ
[chromium] › e2e/visualization-crud.spec.ts:481:9 › visualization CRUD lifecycle @ mobile (375×667) @mutating › full lifecycle: upload → draft → publi
[chromium] › e2e/visualization-ux.spec.ts:95:5 › B.W2 — visualization UX coherence (a11y keystones) › keystone: workspace default has no serious/criti
[chromium] › e2e/workspace-flow.spec.ts:83:5 › Asset-based workspace flow › extract-contour returns contour asset
```

**Baseline findings (for the unit seats; none re-opens a ruling):**

- **B-1 (instrument, VITIATED run discarded).** A first full-suite attempt ran against the playwright
  default `BASE_URL` `http://localhost:3000` — measured to be value.js's `palette-api`
  (⟨cmd⟩ `curl -s localhost:3000/` → `{"status":"ok","service":"palette-api"}`; pid cwd `value.js/api`),
  not fourier: 82 failed · 1 passed · 21 did not run. It is NOT a baseline. The fourier dev stack is
  `http://localhost:3100` (`<title>Fourier Analysis`, pid cwd `fourier-analysis`). **Every F.W11 e2e
  command must set `BASE_URL=http://localhost:3100`.**
- **B-2 (R.2 GREEN-BEFORE-CURE, G-F11-5).** The import-count leg is already 8/8 at `735319e`: every
  chrome file imports *something* from `@mkbabb/glass-ui`. The count cannot witness `.c`'s act, so
  `.c`'s receipt must carry the substantive witness per file: which producer surface primitive
  (panel/card/configurator/dialog) now renders the file's outer chrome, cited by its import line
  and its root element, plus the committed screenshot.
- **B-3 (the grid's producer seam, G-F11-3).** The grid paint is the PRODUCER's: glass-ui 8.0.0
  `dist/styles/paper.css` ships `@layer components { .paper-grid { background-image: var(--paper-grid-texture); … } }`
  (+ `.dark` blend and a `prefers-reduced-transparency` arm). Locally `PaperView.vue:691-704` reuses
  the SAME class name for the sidebar/content column layout (`display:grid`; 220px + 48rem at ≥1024px).
  The class does two jobs; `.b` separates them at the root (the layout gets its own local name; the
  producer's class, or its `--paper-grid-texture` token, paints a fixed full-viewport layer at the app
  root under `App.vue`'s existing `paper-texture` shell `:124`) — never a copied producer rule.
- **B-4 (e2e floor).** The 14 stable failures are pre-existing (F.W4/F.W9/F.W10S honest-REDs and
  producer relays A-2/A-3 among them). G-F11-6 at `.d` compares its failure set to THIS stable set;
  a run-1-only row reappearing is re-run once in isolation before it counts.

## Unit plan

Order (spec §State **Agents** line, :4, and §0ao): **[`.a` ∥ `.b`] → [`.c`] → [`.d`]**. The
orchestrator's 1-concurrent cap turns the first group into two serial groups; `.a` and `.b` share
one modify path (`web/src/components/paper/PaperView.vue`: `.a` owns `:623`'s radius, `.b` owns
`:455`/`:691-699`'s grid), so they may not run concurrently in any case. Dispatched groups:
`[F.W11.a] → [F.W11.b] → [F.W11.c] → [F.W11.d]`. Every unit pushes `m/w1-bump-migration` at its close.

| unit | model | spec sections | writable (spec §1 Bounds) | gates it turns | locks |
|---|---|---|---|---|---|
| `F.W11.a` Radii and surfaces | opus | §2 `### F.W11.a` (:26-27) · §3 G-F11-1/-2 (:36-37) | `web/src/**` (root cures) | G-F11-1 · G-F11-2 | one commit per component family; tokens read from producer `dist/**/*.css`, never invented |
| `F.W11.b` The grid is the app's | opus | §2 `### F.W11.b` (:28-29) · §3 G-F11-3 (:38) | `web/src/**` · `web/e2e/screenshots/**` | G-F11-3 | paper keeps its own surface; probe ≤3 viewports × 3 routes (§0ao law, §5.2) |
| `F.W11.c` Dock + visualizer glass idiom | **fable** (design-author) | §2 `### F.W11.c` (:30-31) · §3 G-F11-4/-5 (:39-40) | `web/src/**` · create `web/src/components/layout/AppDock.vue` · `web/e2e/shell-header.spec.ts` · `web/e2e/visualization-ux.spec.ts` (same-commit-as-markup only) · `web/e2e/screenshots/**` | G-F11-4 · G-F11-5 | spec-edit ONLY in the commit of the markup it measures, names unchanged, no skip/fixme/loosening; one home per control; producer gaps = BK relay rows, never a local dock |
| `F.W11.d` Close | opus (verify-only) | §2 `### F.W11.d` (:32-33) · §3 all (:35-42) · §4 Close act (:44-46) | value.js record · `LEDGER.md` · `INBOX.md` | G-F11-6 · G-F11-7 (+ re-run 1..5 double) | honest-RED only with a relay id |

Common bounds: **Do NOT touch** `glass-ui/**` · `web/package.json` deps · `fr-*.md` · `scripts/dev/dev.sh`
· every other `web/e2e/*.spec.ts`. All units may append their receipt to this record.
Every e2e command in this wave runs with `BASE_URL=http://localhost:3100` (Baseline B-1). `.c` answers
B-2 with a per-file substantive witness; `.b` answers B-3 by separating the layout class from the producer paint.

## Unit receipts

### F.W11.a — Radii and surfaces (`claude-opus-5-5[1m]`, 2026-09-22)

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` + the J-audit `docs/**/*.png`
  rows (outside `web/src/**`, not this unit's, untouched). Nothing inherited in `web/src/**`; record clean.
- **Rulings consumed**: COHESION §0ao (OA-1, owner verbatim); §0j (glass-ui READ-ONLY); §0ap tail — nothing for F.W11.a.
- **Anchors at true bytes** (fourier `735319e`): all six G-F11-1 sites and all 43 G-F11-2(a) sites matched the spec's
  cited lines (no drift). The `rounded-none` = `GalleryDraftsSection.vue:98`. G-F11-2(b)'s 47 = the 43 + `style.css:337`
  (`inherit`) + three prose comments (`PaperArticleWindow.vue:194` · `FourierMorphDemo.vue:308` · `HarmonicLevelGrid.vue:310`).
- **Tokens read, never invented** — ⟨cmd⟩ `tr ';{}' '\n\n\n' < web/node_modules/@mkbabb/glass-ui/dist/styles/theme/radius.css | grep -- --radius`
  → `--radius 0.625rem · -xs 4px · -sm 4px · -md 6px · -lg var(--radius) · -xl 12px · -2xl 1rem · -3xl 1.5rem · -pill 9999px ·
  -card · -panel (=xl) · -dialog · -media · -button · -field (=2xl) · -strip · -control · -badge · -dock · -tab · -floor (=xs) · -ctx · -inset`.
  Each also verified in the built bundle: ⟨cmd⟩ `cat web/dist/assets/*.css | grep -c -- "--radius-$t:"` → 1 for each of
  sm xs md lg panel pill badge control field floor.
- **Producer surfaces read (the "yield" rule)**, each ⟨cmd⟩ a grep over `glass-ui/dist`: `[data-slot="dialog-content"]{border-radius: var(--radius-3xl)}` ·
  `.popover-content{border-radius: var(--radius-panel)}` · `.interactive-item{…border-radius: var(--radius-lg)}` (menu rows,
  `menu__item interactive-item glass-menu-row`) · `.glass-resting{…border-radius: var(--radius-ctx)}` · `.glass-wash{…var(--radius-ctx)}` ·
  `button{border-radius:var(--radius-button)}` · SelectTrigger chunk `rounded-pill`. `.scroll-progress` ships NO radius (token applied locally).
- **Acts, one commit per component family** (fourier `m/w1-bump-migration`; each site guarded by an exact-bytes check before edit):
  1. `e844536` **paper** — `PaperView:623` progress bar `0 1px 1px 0` → `--radius-pill` (the squared left edge abrogated) ·
     `:660` `.paper-article` + `PaperSidebar:249` + `PaperArticleWindow:285` `0.75rem` → `--radius-panel` · `:728` 2rem
     `.overlay-btn` `50%` → `--radius-pill` · `:776` 16px badge `8px` → `--radius-badge` · `:797` + `MobileFloatingToc:370`
     `0.375rem` → `--radius-md` · `MobileFloatingToc:266` + `PaperSidebar:285` `0.25rem` → `--radius-sm`.
  2. `058ec3f` **paper search** — `PaperSearchDropdown` `.paper-search-results--floating` (radius 0 + `border-left/right: none`,
     which served only the squared seam) DELETED with its now-dead `:class` binding (⟨cmd⟩ `grep -rn 'paper-search-results--' web/src web/e2e`
     → the binding only); the floating panel reads its base token corner · `PaperSearchInput:219` radius 0 deleted from the
     floating arm (its `:focus-within` outline now follows the base token) · `:200` + `ResultRow:81` `3px` → `--radius-xs` ·
     `ResultRow:130` `<mark>` `1px` → `--radius-floor`.
  3. `a4abe7c` **visualization** — YIELD: `FullscreenViewer:179` `.fs-dialog` radius 0 (→ the producer dialog's `--radius-3xl`)
     and `:196` children radius 0 (→ their own token corners), comments annotated · `GalleryDraftsSection:98` `rounded-none` removed
     (→ glass Button `--radius-button`) · `SpeedSelect:103` (→ SelectTrigger `rounded-pill`) · `AnimationControls:312` `.menu-item`
     (→ `.interactive-item` `--radius-lg`) · `GallerySearchBar:254` `.filter-panel.glass-resting` (→ `--radius-ctx`). TOKENS:
     play-btn + `::before`/`::after` `9999px` → `--radius-control` · `.mini-progress`/`.mini-fill` `2px` on a 4px bar → `--radius-pill` ·
     `CanvasControlsDock:176` 6px dot → `--radius-pill` · `GallerySearchBar:210` `.search-pill` field `0.5rem` → `--radius-field` ·
     `ContourSettings:441` banner `0.5rem` → `--radius-lg` · `:461` + `ExportModal:106` `0.375rem` → `--radius-md` · `ContourSettings:422`
     + `BasisSelector:308` `0.25rem` → `--radius-sm`.
  4. `16a6ea4` **equation** — YIELD: `EquationView:693` `.info-hovercard` (glass `PopoverContent` → `.popover-content` `--radius-panel`) ·
     `ConvergenceLegend:62` `.glass-wash` plate (→ `--radius-ctx`). TOKENS: `EquationView:642` `3px` → `--radius-xs` · `FrequencyGraph:258`
     scrollbar thumb `2px` on 4px → `--radius-pill` · `FunctionInput:291` `.preset-pill` on a glass Button `9999px` → `--radius-control`.
  5. `9e4d523` **morph** — `HarmonicLevelGrid:240` · `MorphPhaseConfig:262` · `FourierShapeExtractor:292` `0.375rem` → `--radius-md`;
     prose at `FourierMorphDemo:308` / `HarmonicLevelGrid:310` reworded to "corner radius" (G-F11-2(b) counts declarations; no rule changed).
  6. `f3d1bf9` **layout** — `DarkModeToggle:209` square `50%` → `--radius-pill` · `AppHeader:192` `0.4375rem` → `--radius-md` ·
     `:292` 1.75rem badge `9999px` → `--radius-badge` (AppHeader's fate is `.c`'s; its corners are tokens meanwhile).
  7. `0824371` **focus ring** — `style.css:337` `border-radius: inherit` DELETED = banked `GCM-23` TAKEN: it re-shaped a focused
     element to its PARENT's corner; all four ringed classes own their corner (`.sidebar-link` PaperSidebar `calc(var(--radius) - 2px)` ·
     `.floating-toc-item` `--radius-md` · `.callout-btn` glass Button `--radius-button` · `.gallery-card` `rounded-xl`) and an outline
     follows its own element. `PaperArticleWindow`'s `PAW-22` note records the taking.
- **Push**: ⟨cmd⟩ `git push origin m/w1-bump-migration` then `git fetch; git rev-parse --short HEAD origin/m/w1-bump-migration` → `0824371` · `0824371`.
- **Gates, BEFORE → AFTER** (cwd `fourier-analysis`, HEAD `0824371`, double-run on the settled bytes):
  | gate | BEFORE (Baseline) | AFTER run 1 · run 2 |
  |---|---|---|
  | G-F11-1 `grep -rEn 'border-radius:\s*0[^.]\|rounded-none' web/src \| wc -l` | 6 | **0 · 0** GREEN |
  | G-F11-2(a) `grep -rEn 'border-radius:\s*[0-9]' web/src \| wc -l` | 43 | **0 · 0** GREEN |
  | G-F11-2(b) `grep -rEn 'border-radius' web/src \| grep -vc 'var(--radius'` | 47 | **0 · 0** GREEN |
  | G-F11-6 build leg `cd web && npm run build` | exit 0 | **exit 0** (`✓ built in 11.16s`) |
  Wider squared-edge sweep: ⟨cmd⟩ `grep -rEn 'radius:\s*0\|rounded-[a-z]*-?none\|rounded-\[0' web/src` → 0 lines.
  Regression floor (not this unit's gate): ⟨cmd⟩ `BASE_URL=http://localhost:3100 npx playwright test e2e/shell-header.spec.ts
  e2e/visualization-ux.spec.ts --project=chromium` → `13 passed (23.8s)`.
- **Residuals (named, not closed here)**: (R-a-1) five `calc(var(--radius) - Npx)` sites (`PaperSidebar:355/:389` · `PaperSearchInput:135` ·
  `PaperSearchDropdown:211` · `PaperSearchResultRow:65` · `PaperSearchModal:259`) are token-derived and pass both gates; untouched.
  (R-a-2) native `<input>`s at `HarmonicLevelGrid .level-input` · `MorphPhaseConfig .num-input` and the `PaperSearchInput` /
  `GallerySearchBar` field wraps now read tokens but are not glass-ui `Input`s — a component swap is `.c`'s visualizer-idiom
  scope (search/morph are outside it), flagged for `.c`/`.d` to adjudicate, no act here. (R-a-3) the fullscreen dialog now shows
  its producer `--radius-3xl` corners at the viewport edge (the yield the spec prescribes); `fullscreen.spec.ts:40` is a banked
  baseline RED (G-F9-11), not re-measured by this unit.
- **Escalations**: none. **Writes outside the set**: none (fourier `web/src/**` + this record only).

### F.W11.b — The grid is the app's (`claude-opus-5-5[1m]`, 2026-09-22)

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` + the J-tranche
  `docs/.../before/*.png` rows only — nothing inside `web/src/**` or `web/e2e/screenshots/**`; nothing inherited.
- **Anchors at true bytes** (spec `:455/:691`, B-3 `:691-699`): ⟨cmd⟩ `grep -n paper-grid web/src/components/paper/PaperView.vue`
  → `:455` `<div class="paper-grid">` · `:691` `.paper-grid {` · `:699` `.paper-grid {` (≥1024px arm) — held, no drift. A 4th
  mention, prose only: `useScrollNavigation.ts:400` (comment naming the layout element). Producer seam re-read:
  `node_modules/@mkbabb/glass-ui/dist/styles/paper.css` → `@layer components{.paper-grid{background-image: var(--paper-grid-texture);…}
  .dark .paper-grid{background-blend-mode: screen;} @media (prefers-reduced-transparency: reduce){…none}}`, reached via
  `style.css:3 @import "@mkbabb/glass-ui/styles"`. `/visualize` = the `alias` of `/w/:imageSlug?` (router `:75`); `/` redirects to the saved tab.
- **Act 1 — separate the two jobs at the root** (fourier `576e40a`, 3 files, +13/−5):
  (i) layout: `PaperView.vue:455/:691/:699` `.paper-grid` → `.paper-columns` (declarations byte-identical: `display:grid`,
  `1fr`, and `220px minmax(0, 48rem)` at ≥1024px); the `useScrollNavigation.ts:400` comment follows the rename.
  (ii) paint: `App.vue` shell gains `isolate` and, as its first child, `<div class="paper-grid fixed inset-0 -z-10
  pointer-events-none" aria-hidden="true" />` — the PRODUCER's class, no rule copied; `isolate` makes the shell the
  stacking context so `-z-10` sits above the shell's `bg-background paper-texture` ground and under every route's content.
  Theme-awareness is the producer's `.dark` arm. The paper keeps its own surface (`.paper-article`: `background: var(--card)`,
  untouched).
- **Act 2 — bounded probe** (§5.2; 3 viewports × 3 routes, node + playwright chromium, `BASE_URL=http://localhost:3100`,
  per cell: count of `.paper-grid`, its `getBoundingClientRect()` vs `innerWidth×innerHeight`, computed `background-image ≠ none`):
  ⟨cmd⟩ `node gridprobe.mjs` run 1 → every cell `n=1 box={x:0,y:0,w:vw,h:vh} pos:fixed z:-10 bg:linear-gradient(…)` →
  **`PASS 9/9`**; run 2 → **`PASS 9/9`**. Screenshots (run 1) read by eye at `/paper` 1440×900 and `/visualize` 1280×800:
  grid under header, sidebar, card and canvas margins, full viewport; the paper card sits on it.
- **Act 3 — evidence** (fourier `78ab4f9`): 9 PNGs `web/e2e/screenshots/f-w11-b/{root,paper,visualize}-{1440x900,1280x800,375x667}.png`,
  force-added — ⟨cmd⟩ `git check-ignore -v …` → `web/.gitignore:1:*.png` (precedent: 6 tracked PNGs under `web/`); `.gitignore` not touched.
- **Build**: ⟨cmd⟩ `cd web && npm run build` → **exit 0** (`✓ built in 3.25s`).
- **Push**: ⟨cmd⟩ `git push origin m/w1-bump-migration` → ⟨cmd⟩ `git rev-parse --short origin/m/w1-bump-migration` → `78ab4f9`.
- **G-F11-3 BEFORE → AFTER**: bytes ⟨cmd⟩ `grep -c paper-grid web/src/components/paper/PaperView.vue` → 3 → **0 · 0**;
  browser 0/3 (1440 only; `div.paper-grid` 976×131574, absent on `/visualize`) → **9/9 · 9/9**; screenshots committed. **GREEN.**
- **Residuals**: (R-b-1) the layer honours the producer's `prefers-reduced-transparency` arm (grid off) — by design, not probed.
  (R-b-2) the `isolate` shell makes teleported overlays (body-level) paint above all in-shell chrome regardless of in-shell
  z-index; no e2e was run by this unit — `.d`'s G-F11-6 full run is the witness.
- **Escalations**: none. **Writes outside the set**: none (fourier `web/src/**` · `web/e2e/screenshots/**` · this record).

### F.W11.c — The dock and the visualizer's glass idiom (`claude-fable-5-1`, design-author, 2026-09-22)

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` + modified `docs/tranches/J/audit/screenshots/before/*.png`
  (outside every F.W11 row, untouched); value.js `execution/C/F-W11.md` · `INBOX.md` clean. Nothing inherited in `web/**`.
- **Open state**: fourier HEAD `78ab4f9` = `origin/m/w1-bump-migration` (`.b`'s close); `.a`/`.b` receipts read; COHESION §0j core + §0j.D +
  §0ao read whole, §0k–§0an grepped for `AppHeader|AppDock|GlassDock|glass-ui/dock|DockLayer|DockTrigger` → 0 rows (no ruling beyond §0ao
  consumes here). Dev stack ⟨cmd⟩ `curl -s localhost:3100/ | grep -o '<title>[^<]*'` → `<title>Fourier Analysis` (B-1 honoured on every e2e command).
- **Anchors measured before edit**:
  - Spec §2 `.c` names the header's controls "navigation, search, `About Fourier analysis`, `Dark mode`". At the true bytes `AppHeader.vue`
    (413 lines, read whole) carried NO search control: ⟨cmd⟩ `grep -rln search web/src/components/layout web/src/App.vue` → 0. The search fields
    in the tree are route-scoped (`PaperSearchInput` on `/paper`, `GallerySearchBar` on `/gallery`), not shell controls, and lifting one into
    the dock would give a control two homes. **INTENT taken at the true bytes**: the dock carries the header's ACTUAL controls — the About
    trigger, the nav trigger, the account group (`UserSlugBar`: `Log in` / `Copy your slug` / `Log out` / slug field, + the admin badge, a status
    mark), and `Dark mode`. No search control was invented; the producer's `search` slot stays unused. (D-19-class drift, recorded.)
  - `coarse-pointer.spec.ts:115` (READ-ONLY, not this unit's) keys on `header.app-header` and measures every control's `boundingBox()`
    (`minAxis`, :38-41) — so the dock must live inside that landmark and each control's OWN box must reach 44px under coarse emulation.
  - Producer read (`web/node_modules/@mkbabb/glass-ui` 8.0.0): `DockTrigger for="popover"` renders reka's bare `PopoverTrigger`
    (`src/components/dock/DockTrigger.vue`), which needs the click root — glass `Popover trigger="hover"` seats a `HoverCardRoot` on a fine
    pointer (`popover/Popover.vue:85`). The 44px floor for trigger faces is a `::after` hit pseudo (`dock/styles/controls/touch-floor.css`),
    while the face's own box is `min-height: var(--dock-trigger-min-height, auto)` (`controls/triggers.css`) — the producer publishes the
    hook; `.dock-control` is sized by `--dock-control-size` (≥44px coarse via `--dock-control-floor`, `density.css`).
  - The eight chrome files' outer chrome at `78ab4f9`: `CoefficientsPanel:14` + `ContourSettings:225` `ConfiguratorLayer` · `AnimationControls:118`
    + `CanvasControlsDock:41` + `EditorControlsDock:63` `GlassDock` · `ExportModal:46` `Dialog` — six of eight ALREADY on producer surfaces
    (the spec's premise drifted); `EquationPanel:59` `div.eq-panel.glass-wash` (a hand-copied producer recipe class + a local `box-shadow` +
    `rounded-xl`) and `GlassTimeline:207` `div.timeline-row` (a control row that paints no plate; its surface is the enclosing dock; the producer's
    `timeline` family is a segment list, not a scrubber — `dist/components/timeline/types.d.ts`) were the two to judge.
- **Acts (fourier `m/w1-bump-migration`, pathspec commits, one per meaning)**:
  1. `53aaa6f` **the dock** — `web/src/components/layout/AppDock.vue` CREATED (line 1 `SERVED MODEL: claude-fable-5-1`; 297 lines):
     `<header class="app-header">` (the landmark the coarse spec keys on; a plain centered flex row — no plate, no sticky, no backdrop filter)
     wrapping `<GlassDock orientation="horizontal" shape="pill" always-expanded>` from `@mkbabb/glass-ui/dock` with, in order: `Popover` (click
     root) + `DockTrigger for="popover" aria-label="About Fourier analysis"` (the ℱ wordmark face) + the attribution `PopoverContent` ·
     `DockSeparator` · `DropdownMenu` + `DockTrigger for="dropdown" :aria-label="Navigate — current section ${label}"` + the five `DropdownMenuItem`
     rows with `aria-current` · `DockSeparator` · `<div class="contents" role="group" aria-label="Account">` (admin badge + `UserSlugBar`) ·
     `DockSeparator` · `DarkModeToggle` (`aria-label="Dark mode"`, `aria-pressed`). Floors: `.app-dock{--dock-trigger-min-height: var(--dock-control-size)}`
     + `.logo-trigger,.nav-trigger{min-inline-size: var(--dock-control-size)}` + `.dark-mode-toggle{--toggle-size: var(--dock-control-size)}` —
     one cell, one token. Portaled-surface divergence kept inside `@layer glass-overrides` as before. `AppHeader.vue` DELETED (`git rm`;
     every control has one home). `App.vue:7/:151` import + mount → `AppDock`; the `MG-epsilon` comment rewritten as the decided choice (the
     inert `sticky`/`backdrop-blur` treatment retired with the header). `useFourierMorph.ts:28` mount-chain comment → `App → AppDock (né AppHeader)`.
     Stale `AppHeader.vue:174-177` citations in `style.css:307/:323` are D.W4 historical records — left (E-3).
  2. `6d1e53d` **the equation card** — `EquationPanel.vue`: `div.eq-panel.glass-wash` → `<Card tier="floating" size="sm" class="eq-panel">` with
     `CardHeader` (title · energy `Metric` · close) and `CardContent` (notation pills · terms slider · KaTeX), import `{ Card, CardHeader, CardContent }
     from "@mkbabb/glass-ui/card"` (:12); `.eq-panel` keeps only position/z/offset/max-width — `p-2.5`, `rounded-xl` and the local `box-shadow`
     retire to the producer's `--card-pad` rhythm, `--radius-card` corner and floating cast.
  3. `ce176e6` **evidence** — `web/e2e/screenshots/f-w11-c/{dock-1440x900-header,dock-375x667-coarse-header,equation-card-1440x900}.png`
     (`git add -f`, the `web/.gitignore:1 *.png` rule, as `.b` did for `f-w11-b/`).
- **Push**: ⟨cmd⟩ `git push origin m/w1-bump-migration; git fetch; git rev-parse --short HEAD origin/m/w1-bump-migration` → `ce176e6` · `ce176e6`.
  ⟨cmd⟩ `git status --porcelain web` → empty.

- **Gates, BEFORE → AFTER** (cwd `fourier-analysis`, HEAD `ce176e6`, settled bytes, double-run):
  | gate | BEFORE (Baseline) | AFTER run 1 · run 2 |
  |---|---|---|
  | G-F11-4 (bytes) `test -f web/src/components/layout/AppDock.vue` · `grep -n GlassDock AppDock.vue \| grep glass-ui/dock` | absent · 3 dock files, none a shell dock | **present** · `:35 import { GlassDock, DockTrigger, DockSeparator } from "@mkbabb/glass-ui/dock"` — both runs; `grep -rln glass-ui/dock web/src \| wc -l` → **4 · 4** |
  | G-F11-4 (e2e) `BASE_URL=http://localhost:3100 npx playwright test e2e/shell-header.spec.ts e2e/coarse-pointer.spec.ts --reporter=list` | 4/4 · 4/4 at the OLD markup | **8 passed (2.8s)** — `shell-header` [chromium] 4/4 (About card opens by click · nav trigger named `Navigate — current section Gallery` + 5 menuitems · navigates and re-names to `… Equation` · `Dark mode` `aria-pressed` flips, name stable) · `coarse-pointer` [mobile-chromium] 4/4 incl. `:105` every shell-header control ≥ 44px. **Neither spec edited** (0 bytes; the LOCK's same-commit clause never engaged). |
  | G-F11-4 (browser floors, coarse 375×667, `isMobile`+`hasTouch`) | — | dock box 300×66; `About Fourier analysis` **46.8×46.8** · `Navigate — current section Paper` **62.7×46.8** · `Log in` **44×44** · `Dark mode` **46.8×46.8**; fine 1440×900: dock 503×56, controls 40px (the comfortable rung). 0 console errors both. |
  | G-F11-5 (bytes) per-file `grep -c '@mkbabb/glass-ui'` | 1·4·2·3·2·4·1·3 (8/8, GREEN-BEFORE-CURE) | CoefficientsPanel **1** · ContourSettings **4** · EquationPanel **3** · AnimationControls **3** · CanvasControlsDock **2** · EditorControlsDock **4** · GlassTimeline **1** · ExportModal **3** → **8/8 · 8/8** |
  | G-F11-5 (e2e) `BASE_URL=http://localhost:3100 npx playwright test e2e/visualization-ux.spec.ts --project=chromium --reporter=list` | run 1 `:95` load-flaky, run 2 all passed | **9 passed (19.5s)** — keystones 1–8 + `save_contour_then_recompute`; `:95` passed at run 1 (1 worker) |
  | G-F11-6 (kept, not this unit's) `npx vue-tsc -b` · `npm run build` | exit 0 | **exit 0 · exit 0** (vue-tsc twice; build once, `✓ built`) |
- **B-2 answered — the substantive witness per chrome file** (surface primitive · its import line · its root element · evidence):
  | file | producer surface at the root | import | root | evidence |
  |---|---|---|---|---|
  | `CoefficientsPanel.vue` | `ConfiguratorLayer` (configurator) — unchanged | `:4` | `:14` | `equation-card-1440x900.png` (right column, "Coefficients") |
  | `ContourSettings.vue` | `ConfiguratorLayer` + `ConfiguratorRow` — unchanged | `:19` | `:225` | same (right column, "Contour") |
  | `EquationPanel.vue` | **`Card` (`Surface`, `tier="floating"`) + `CardHeader` + `CardContent`** — CURED HERE | `:12` | `:64` | same (top-left card); probe: `data-slot="card"`, classes `glass-floating`, `border-radius 16px` (= `--radius-card`), producer cast, header+content slots present |
  | `AnimationControls.vue` | `GlassDock` + `DockTrigger for="dropdown"` — unchanged | `:8` | `:118` | same (bottom pill) |
  | `CanvasControlsDock.vue` | `GlassDock` + `DockControl` + `DockSeparator` — unchanged | `:7` | `:41` | same (top-right pill) |
  | `EditorControlsDock.vue` | `GlassDock` + `DockControl` + `DockSeparator` — unchanged | `:6` | `:63` | (edit mode; not opened — keystone 1 covers the mount) |
  | `GlassTimeline.vue` | the producer `Slider` in a control row whose plate IS the enclosing `GlassDock` (AnimationControls `:105`, ConvergenceTimeline `:91`); no plate of its own to re-express | `:70` | `:207` | same (inside the bottom pill) |
  | `ExportModal.vue` | `Dialog` + `DialogContent` (dialog) — unchanged | `:11` | `:46` | keystone 3 (Dialog-open a11y-clean) |
- **Design notes (why each surface is what it is)**:
  - *A dock, not a bar*: the owner's OA-3 asks for "a dock" — a pill (`shape="pill"`, the producer's default) centered in the landmark, always
    expanded (a shell nav never auto-collapses), the glass plate being the only surface; the landmark row keeps no plate, so the app's grid
    (`.b`) shows through around it. `position` stays `inline` because the landmark is a flex sibling of the app's sole scroller — `fixed`/`sticky`
    would re-create the inert treatment `MG-epsilon` flagged.
  - *Faces*: transient surfaces ride `DockTrigger` (the producer's one trigger face) so hover plate, press spring, specular gleam and the coarse
    hit pseudo are the producer's; `DockControl` is not used because no shell control is a plain button (the dark-mode control is its own
    morphing component with the same `aria-pressed` contract), and `DockLayer` is not used because the shell owns no selectable panes.
  - *The About card is a click disclosure on both pointers* (was hover on fine): the dock's popover face binds reka's `PopoverTrigger`, which
    needs the click root; one activation everywhere is the contract FR-AH-13 wanted for touch.
  - *Groups*: ordinary DOM with a name (`role="group" aria-label="Account"`) and a `DockSeparator` between groups, as the dock README prescribes;
    the admin badge is a status mark inside the account group, not a control.
  - *EquationPanel → `Card tier="floating"`*: it floats over the canvas at `--z-controls`, so the floating tier's own cast/blur/corner replace a
    hand-copied `.glass-wash` + a local shadow; `CardHeader`/`CardContent` give the producer's pad rhythm (`size="sm"` = the atom rung).
- **Residuals / observations (named, not closed here)**:
  - (R-c-1) The producer floors a `DockTrigger` face by a `::after` hit pseudo, not its own box; a consumer that omits the published
    `--dock-trigger-min-height` hook would measure < 44px by `boundingBox()` under coarse emulation while the tap target is in fact 44px.
    Not a gap (the hook exists and is used here); an instrument/README nuance for BK's attention in the next mail round, not a relay row.
  - (R-c-2) `.a`'s R-a-2 (`GallerySearchBar` field wraps are not glass `Input`s) is gallery chrome, outside the eight visualizer files — untouched.
  - (R-c-3) The "search" control the spec names does not exist in the shell (anchor drift above); the dock's `search` slot is unused by design.
- **E13 (Step-0, four paths, read-only)**: BK still the newest glass dir (⟨cmd⟩ `ls glass-ui/docs/tranches | grep '^B' | tail -1` → `BK`);
  files newer than 2026-09-22 00:00 — value.js `V/`: `EVIDENCE.md` · `VISUAL-CONSTITUTION.md` · `OPTICAL-BENCH-COMPOSITIONS.md` (Track A tranche
  documents, not letters) · `V/coordination`: `INBOX.md` · BK/coordination: the same three letters already rowed at Open · keyframes `V/coordination`
  + atlas `P/coordination`: empty → **0 unrowed, 0 UNREAD in scope**; INBOX not written.
- **Bounds**: writes = `web/src/components/layout/{AppDock.vue (new), AppHeader.vue (deleted)}` · `web/src/App.vue` · `web/src/composables/useFourierMorph.ts`
  · `web/src/components/visualization/EquationPanel.vue` · `web/e2e/screenshots/f-w11-c/*.png` · this record. Specs untouched; glass-ui untouched;
  `scripts/dev/dev.sh` untouched; no LEDGER edit (the row is `.d`'s).
- **Verdict**: G-F11-4 **GREEN** · G-F11-5 **GREEN** (bytes 8/8 with the per-file witness above; keystones 9/9). **DONE.**

### F.W11.d — Close (`claude-opus-5-5[1m]`, VERIFY-ONLY, 2026-09-22)

- **Crash-recovery**: ⟨cmd⟩ `git -C value.js status --porcelain` → `M CARRY-LEDGER.md` (a sibling's) · `M scripts/dev/dev.sh` (never touched)
  — the writable set (`execution/C/F-W11.md` · `LEDGER.md` · `INBOX.md`) clean; ⟨cmd⟩ `git -C fourier-analysis status --porcelain` →
  `?? .worktrees/` + the J-tranche `docs/tranches/J/audit/screenshots/before/*.png` (not this wave's; untouched). Nothing inherited.
- **Tree**: ⟨cmd⟩ `git -C fourier-analysis rev-parse --short HEAD origin/m/w1-bump-migration` (after `git fetch`) → `ce176e6` · `ce176e6`
  — **`m/w1-bump-migration` pushed, 0 ahead**. F.W11 commits ⟨cmd⟩ `git log --oneline 735319e..HEAD | wc -l` → 12
  (`.a` e844536 · 058ec3f · a4abe7c · 16a6ea4 · 9e4d523 · f3d1bf9 · 0824371; `.b` 576e40a · 78ab4f9; `.c` 53aaa6f · 6d1e53d · ce176e6).
- **Bounds, path-by-path** over `735319e..ce176e6`: ⟨cmd⟩ `git diff --name-only 735319e ce176e6 | wc -l` → **44** = `web/src/**` **32**
  (30 M · 1 A `components/layout/AppDock.vue` · 1 D `components/layout/AppHeader.vue`; ⟨cmd⟩ `… -- web/src | grep -vE '\.(vue|ts|css)$'` → 0)
  + `web/e2e/screenshots/f-w11-b/*.png` **9** + `web/e2e/screenshots/f-w11-c/*.png` **3**; ⟨cmd⟩ `… | grep -E 'glass-ui|dev.sh|package.json|fr-' | wc -l` → **0**;
  no `web/e2e/*.spec.ts` touched (neither named spec was edited — lawful: edits were permitted, not owed). **IN BOUNDS.**
- **Gates, re-run double at this seat's clock** (cwd `fourier-analysis/`):

| gate | ⟨cmd⟩ | run 1 · run 2 | verdict |
|---|---|---|---|
| G-F11-1 | `grep -rEn 'border-radius:\s*0[^.]\|rounded-none' web/src \| wc -l` | **0 · 0** (baseline 6) | GREEN |
| G-F11-2 | `grep -rEn 'border-radius:\s*[0-9]' web/src \| wc -l` · `grep -rEn 'border-radius' web/src \| grep -vc 'var(--radius'` | **0/0 · 0/0** (baseline 43/47) | GREEN |
| G-F11-3 | `grep -c paper-grid web/src/components/paper/PaperView.vue` + own probe `node gridprobe.mjs` (scratchpad; 3 viewports × `/` `/paper` `/visualize`, `BASE_URL` `:3100`, one `.paper-grid`, box `{0,0,vw,vh}`, `position:fixed`, `background-image≠none`) | bytes **0 · 0**; probe **PASS 9/9 · PASS 9/9** (post-`.c`, the dock landed above it); 9 screenshots tracked at `78ab4f9` | GREEN |
| G-F11-4 | `grep -n glass-ui/dock web/src/components/layout/AppDock.vue` + e2e legs | `:35 import { GlassDock, DockTrigger, DockSeparator } from "@mkbabb/glass-ui/dock"` ×2; `shell-header` 4/4 · 4/4 · `coarse-pointer` 4/4 · 4/4 (full runs) + isolated 17/17 (viz-ux 9 + shell 4 + coarse 4) | GREEN |
| G-F11-5 | per file `grep -c '@mkbabb/glass-ui'` (8 files) + `visualization-ux.spec.ts` | 1·4·3·3·2·4·1·3 → **8/8 ≥1** ×2 (B-2 per-file witness at `.c`); keystones run 1: `:95` timed out (the baseline's own run-1-only load row; 8 serial siblings did not run) · run 2: **9/9**; ⟨cmd⟩ `npx playwright test e2e/visualization-ux.spec.ts:95 --project=chromium` isolated → 1 passed | GREEN |
| G-F11-6 (build) | `cd web && npm run build` | **exit 0 · exit 0** (`✓ built in 4.04s` · `3.92s`) | GREEN |
| G-F11-6 (e2e) | `BASE_URL=http://localhost:3100 npx playwright test --reporter=list` (run 2 `--reporter=list,json`) | run 1: **77 passed · 19 failed · 3 skipped · 8 did not run** · run 2: **86 passed · 18 failed · 3 skipped** | **RED** (below) |
| G-F11-7 | E13 four-path sweep | 0 new letters since Open · 0 UNREAD · 0 producer gaps owed a row | GREEN |

- **G-F11-6 failure-set comparison** (⟨cmd⟩ `comm -12 f1 f2 | wc -l` → **18** stable; `comm -23 f1 f2` → 1 run-1-only; `comm -13` → 0):
  - **14 of 18 = the banked stable baseline set, row for row** (contrast-floor ×3 · equation-interaction `:77` · fullscreen `:40` ·
    gallery-admin-a11y ×4 · visual-baseline equation `1280x800`/`375x667` · visual-checkpoint `:80`/`:101`/`:122`) — cited, not re-counted (B-4).
  - **visual-baseline equation `@ 1440x900`** — failed both full runs here (the baseline had it run-1-only). Same failure as its two baseline
    siblings (⟨cmd⟩ error → `Test timeout of 30000ms exceeded … page.waitForTimeout` after the spec's own `networkidle` goto); isolated
    ⟨cmd⟩ `npx playwright test e2e/visual-baseline.spec.ts --project=chromium -g "equation @ 1440x900"` → **1 passed**. Counted as the
    baseline's load-flaky row, not new.
  - **visualization-ux `:95`** — run-1-only (as at baseline); isolated → **1 passed**. Not new.
  - **THREE NEW DETERMINISTIC REDs, absent at `735319e`** (both full runs + two isolated re-runs ⟨cmd⟩ `npx playwright test e2e/visual-checkpoint.spec.ts:144 …:163 …:182` → **3 failed · 3 failed**):
    1. `[chromium] visual-checkpoint.spec.ts:144` item 8 — `checkpoint-admin-banner.png`: 39 px (0.01) differ; the `-diff.png` shows a
       regular dot lattice across the translucent banner = the app-wide grid (`.b` `576e40a`, OA-2) now painting beneath it.
    2. `[chromium] visual-checkpoint.spec.ts:163` item 3 — `checkpoint-disclosure-body.png`: expected 400×644, received 400×632; the
       expected/actual pair also differ in captured STATE (Harmonics **20**/Display **10** in the snapshot vs **8**/**8** now, the
       Parseval auto-select pressed) — i.e. the capture moment of an async auto-select changed, not only chrome. Attribution to a single
       F.W11 commit NOT measured (verify-only seat; no bisection tree stood up).
    3. `[mobile-chromium] visual-checkpoint.spec.ts:182` @coarse item 4 — `checkpoint-tooltip-trigger.png`: 86 px (0.03) differ on the
       Parseval wand trigger.
  - These are **consumer-side** REDs: the committed G-F9-23 baselines (`web/e2e/visual-checkpoint.spec.ts-snapshots/`, tracked at
    `ca64bef`) predate the owner-ruled restyle. **No producer owns them, so no BK relay id can carry them, and spec §4's honest-RED
    clause (producer-owned only) does not apply.** The cure (an eye-reviewed re-baseline of those three PNGs, and for item 3 a
    determination whether the 20/10 → 8/8 capture drift is a behaviour change or the checkpoint's own race) writes
    `web/e2e/visual-checkpoint.spec.ts-snapshots/**` — outside §1 Bounds (not a granted row; `visual-checkpoint.spec.ts` is an
    "every other `web/e2e/*.spec.ts`" do-not-touch) and outside this seat's writable set. Not taken. **ESCALATION ESC-F11d-1.**
- **E13 (four paths, read-only)**: ⟨cmd⟩ `ls glass-ui/docs/tranches | grep '^B' | tail -1` → `BK` (no `BL`); ⟨cmd⟩ `find <path> -maxdepth 1
  -type f -newermt "2026-09-22 00:00"` → value.js `V/`: `EVIDENCE.md` · `VISUAL-CONSTITUTION.md` · `OPTICAL-BENCH-COMPOSITIONS.md` (Track A
  documents, not letters) · `V/coordination`: `INBOX.md` · BK/coordination: the three letters rowed at Open (⟨cmd⟩ `grep -c consumers-10.0.0
  INBOX.md` → 7) · keyframes `V/coordination`: empty · atlas: ⟨cmd⟩ `find atlas -maxdepth 4 -name coordination` → none (the INBOX's
  `atlas/docs/tranches/P/coordination/` path does not exist in the checkout — recorded; 0 letters). **0 unrowed, 0 UNREAD in scope.**
  Producer gaps: `.a`/`.b`/`.c` name none (`.c` R-c-1 explicitly "not a relay row"; the dock/Card/Dialog capabilities all shipped) → **0 owed
  BK rows**. A dated sweep line appended at the INBOX tail.
- **Residual adjudication (R-a-2, handed to `.c`/`.d`)**: native `<input>`s (`HarmonicLevelGrid .level-input` · `MorphPhaseConfig .num-input`)
  and the `PaperSearchInput`/`GallerySearchBar` wraps read tokens (G-F11-1/-2 GREEN) but are not glass `Input`s. Spec `.a` asks that where
  glass-ui ships the component "the local styling yields to the producer's surface"; no gate measures it. A verify-only seat cannot act; carried
  as **R-d-1** for the L-20 check / owner report beside ESC-F11d-1, not closed here.
- **Commits (this seat)**: value.js record + LEDGER + INBOX only (hash in the LEDGER event line). Fourier: 0 bytes; glass-ui: 0; dev.sh: 0.
- **Verdict**: G-F11-1 · -2 · -3 · -4 · -5 · -7 **GREEN ×2**; **G-F11-6 RED** (3 new consumer-side snapshot rows, ESC-F11d-1). §4's CLOSED /
  CLOSED-honest-RED conditions are NOT met → **the wave is not closed; LEDGER row → `PARTIAL 2026-09-17 (G-F11-6 RED · ESC-F11d-1)`**. `## Close`
  below records the same.

## RESUME 1 (2026-09-22, seat 0 re-open, `claude-opus-5-5[1m]`, VERIFY-ONLY)

- **Units already done**: none. ⟨cmd⟩ `git -C fourier-analysis log --oneline --all | grep -i W11` → no `X.F.W11` commit
  (fourier HEAD still `735319e`); value.js carries only the OPEN (`584a899a`) and the chassis note (`3c388fb3`: the first
  `.a` seat refused on the harness's latest-message framing, no bytes written). All four units owed.
- **Crash-recovery**: fourier-analysis porcelain → `?? .worktrees/` plus modified `docs/tranches/J/audit/screenshots/before/*.png`
  — outside every F.W11 writable row (`web/src/**` · `web/e2e/**`), not this wave's, untouched. Nothing inherited in `web/**`.
- **E13 re-sweep**: four paths, BK still newest; the only files newer than 2026-09-22 00:00 are the three BK letters already
  noted at Open → 0 unrowed, 0 UNREAD in scope; INBOX not touched.
- **Baseline**: cited from `## Baseline` (banked this same day at `735319e`, tree unmoved) — no re-run owed.
- **Dispatch unchanged**: `[F.W11.a] → [F.W11.b] → [F.W11.c] (fable) → [F.W11.d]`.

## Close (2026-09-22, `.d`, `claude-opus-5-5[1m]`, VERIFY-ONLY)

- **Status**: **PARTIAL** — not CLOSED, not CLOSED honest-RED. fourier `ce176e6` = `origin/m/w1-bump-migration` (pushed).
- **GREEN ×2**: G-F11-1 (0) · G-F11-2 (0/0) · G-F11-3 (bytes 0; probe 9/9 ×2) · G-F11-4 (GlassDock import; shell 4/4, coarse 4/4) ·
  G-F11-5 (8/8; keystones 9/9) · G-F11-7 (0 UNREAD; 0 gaps owed). G-F11-6 build leg exit 0 ×2.
- **RED**: G-F11-6 e2e leg — 3 failures absent at `735319e`, deterministic (visual-checkpoint `:144` admin banner · `:163` disclosure body ·
  `[mobile]` `:182` tooltip trigger); consumer-side stale G-F9-23 snapshot baselines vs the owner-ruled restyle (+ an item-3 state drift
  20/10 → 8/8 not yet attributed). No producer relay id exists for them, so §4's honest-RED clause cannot be used.
- **ESCALATION ESC-F11d-1** — ask: a §3a grant (or a ruled unit) of `fourier-analysis/web/e2e/visual-checkpoint.spec.ts-snapshots/`
  `{checkpoint-admin-banner,checkpoint-disclosure-body,checkpoint-tooltip-trigger}*.png` to a seat that (i) attributes item 3's
  harmonic-count drift (behaviour change vs the checkpoint's own capture race) and cures the root if it is behaviour, then (ii) re-baselines the
  three PNGs with an eye-review receipt; then a close seat re-runs G-F11-6. **Carried**: R-d-1 (R-a-2 native inputs not glass `Input`s).

## Close — RE-VERIFY (2026-09-22, `.d` re-dispatch, `claude-opus-5-5[1m]`, VERIFY-ONLY)

The prior `## Close` above is immutable (E-3); this is a dated re-verification beside it. Cured nothing.

- **Crash-recovery**: value.js writable set (`execution/C/F-W11.md` · `LEDGER.md` · `INBOX.md`) clean; fourier porcelain = the
  J-tranche `docs/tranches/J/audit/screenshots/before/*.png` only (not this wave's; untouched). Nothing inherited.
- **Tree unmoved**: ⟨cmd⟩ `git -C fourier-analysis rev-parse --short HEAD origin/m/w1-bump-migration` (after fetch) → `ce176e6` · `ce176e6`;
  ⟨cmd⟩ `git log --oneline ce176e6..HEAD | wc -l` → 0. ESC-F11d-1 has NO grant: ⟨cmd⟩ `grep -rln ESC-F11d-1 docs/tranches/X` → LEDGER + this record only (no COHESION ruling).
- **Commits/bounds**: all 12 fourier commits exist; ⟨cmd⟩ per commit `git show --name-only` filtered `grep -vE '^web/src/|^web/e2e/screenshots/f-w11-'` → 0 for all 12;
  ⟨cmd⟩ `git diff --name-only 735319e ce176e6 | grep -E 'glass-ui|dev.sh|package.json|fr-|\.spec\.ts' | wc -l` → 0. **No landed-wrong.**

| gate | BEFORE (`735319e`) | AFTER (run 1 · run 2 / reading) | verdict |
|---|---|---|---|
| G-F11-1 | 6 | 0 · 0 | GREEN |
| G-F11-2 (a/b) | 43 / 47 | 0/0 · 0/0 | GREEN |
| G-F11-3 bytes | 3 | 0 · 0 (probe 9/9 ×2 banked at prior Close, tree unmoved) | GREEN |
| G-F11-4 | AppDock absent | `from "@mkbabb/glass-ui/dock"` ×1 · ×1; shell-header + coarse-pointer absent from the full-run failure list | GREEN |
| G-F11-5 | 8/8 (GREEN-before-cure, B-2) | 1·4·3·3·2·4·1·3 = 8/8; visualization-ux absent from the failure list | GREEN |
| G-F11-6 build | exit 0 | exit 0 (`✓ built in 9.26s`) | GREEN |
| G-F11-6 e2e | 14 stable failures | full run: **89 passed · 15 failed · 3 skipped** = 12 of the banked 14 (visual-baseline equation ×2 passed this run) + **the same 3 new** (`visual-checkpoint :144` · `:163` · `[mobile] :182`); isolated re-run of those 3 → **3 failed** | **RED** |
| G-F11-7 | 0 UNREAD | four-path sweep: BK newest; files newer than 2026-09-22 00:00 = the same set the prior Close recorded (3 Track-A V/ docs · INBOX · 3 rowed BK letters); keyframes empty; atlas path absent → 0 UNREAD, 0 gaps owed | GREEN |

- **Verdict**: unchanged — **PARTIAL** (G-F11-6 RED, consumer-side stale G-F9-23 snapshots, ESC-F11d-1 still open, no grant). Four-verb line
  not moved (spec §4: CLOSED only on 1..7 GREEN or producer-owned honest-RED). **Residuals**: ESC-F11d-1 (owner/orchestrator grant of
  `web/e2e/visual-checkpoint.spec.ts-snapshots/` three PNGs + item-3 20/10→8/8 attribution) · R-d-1 (native inputs not glass `Input`s; L-20/owner).

## Check 1 (2026-09-22, fresh adversarial L-20 pass 1, `claude-opus-5-5[1m]`, VERIFY-ONLY)

Independent seat; authored no byte of the cures, receipts or either Close. Wrote 0 fourier / 0 glass-ui bytes.

- **Crash-recovery**: value.js writable set clean; fourier porcelain = J-audit `docs/**/before/*.png` only (not this wave's; untouched).
- **Axis 1 (GREENs reproduce)** at fourier `ce176e6` = `origin/m/w1-bump-migration`, ×2: G-F11-1 → 0·0 · G-F11-2 → 0/0·0/0 ·
  G-F11-3 bytes `grep -c paper-grid PaperView.vue` → 0·0 (probe 9/9 cited from `.b`/Close, tree unmoved) · G-F11-4 `AppDock.vue:35`
  `import { GlassDock, DockTrigger, DockSeparator } from "@mkbabb/glass-ui/dock"` · G-F11-5 per-file counts 1·4·3·3·2·4·1·3 = 8/8 ·
  G-F11-6 build ⟨cmd⟩ `npm run build` → exit 0 (`✓ built in 20.37s`). ⟨cmd⟩ `BASE_URL=http://localhost:3100 npx playwright test
  shell-header coarse-pointer visualization-ux visual-checkpoint` → **17 passed · 6 failed**: shell-header, coarse-pointer (44px floor),
  visualization-ux keystones all pass; failures = the 3 banked visual-checkpoint rows (`:80` `:101` `:122`) **+ the same 3 new**
  (`:144` · `:163` · `[mobile] :182`). The Close's RED reproduces exactly. G-F11-7: no `| UNREAD` table row in INBOX; 0 producer gaps named.
- **Axis 2 (bounds)**: 12 fourier commits `735319e..ce176e6`; every path `web/src/**` or `web/e2e/screenshots/f-w11-*` (filter → 0 lines);
  no spec, package.json, glass-ui or dev.sh path. dev.sh untouched by this wave.
- **Axis 3 (masking)**: ⟨cmd⟩ `git diff 735319e ce176e6 -- web/src | grep '^+' | grep -E 'try \{|catch|\.skip|fixme|!important|eslint-disable|@ts-ignore|as any'` → 0.
- **Axis 4**: `.a` one commit per family (6 + focus ring), `.b` cure+evidence, `.c` dock / Card / evidence — one per meaning, no split family.
- **Axis 5 (E-3)**: spec last touched `ba1b03d5` (the §0ao mint); registry unchanged; prior `## Close` left intact beside its RE-VERIFY.
- **Axis 7**: LEDGER row reads `PARTIAL 2026-09-17 (G-F11-6 RED · ESC-F11d-1)` — lawful (§4 not met); unchanged by this check.
- **Axis 10 (honest-RED adjudication)**: G-F11-6 e2e leg is RED by 3 failures absent at `735319e`. Spec §4 relieves ONLY a producer-owned
  RED "with the relay id"; these are consumer-side stale G-F9-23 snapshot PNGs whose drift this wave's own restyle caused. No relay id, no
  successor routing, no named honest-RED → **UNRELIEVED**. The snapshot directory is outside §1 Bounds, so the cure needs the grant
  ESC-F11d-1 asks for (owner/orchestrator) — the escalation is correctly raised, but it does not relieve the gate.
- **Successors**: no wave declares "Opens after F.W11" (grep over `docs/tranches/X` → only KF.W11 hits); nothing lawfully blocked.

| severity | claim | receipt | cure |
|---|---|---|---|
| HIGH | G-F11-6 e2e RED, unrelieved by §4 | 3 new `visual-checkpoint` failures reproduce (`:144` `:163` `[mobile] :182`) | grant ESC-F11d-1: attribute item-3 drift, re-baseline 3 PNGs w/ eye review, re-run G-F11-6 |
| MINOR | spec `.a` "input … local styling yields to the producer's surface" unmet (R-d-1): native inputs + search wraps are not glass `Input`s | Close RE-VERIFY residual R-d-1; no gate measures it | fold into the ESC-F11d-1 repair unit or owner-rule it out of F.W11 |
| INFO | Close's figures reproduce; bounds, masking, E-3, mail clean | above | — |

**Verdict: NOT-CONFORMANT** (one unrelieved HIGH). LEDGER status NOT moved to CLOSED.

## Repair 1 (2026-09-22, repair seat round 1, `claude-opus-5-5[1m]`)

Register: `## Check 1` above (1 HIGH · 1 MINOR · 1 INFO). Wrote 0 fourier / 0 glass-ui bytes; dev.sh untouched.

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → `?? .worktrees/` + the J-audit
  `docs/tranches/J/audit/screenshots/before/*.png` (outside every F.W11 row; not this wave's; untouched). value.js writable set clean
  bar sibling-owned `CARRY-LEDGER.md` / `dev.sh` / the KF probe (not touched). Nothing inherited in `web/**`.
- **Tree**: ⟨cmd⟩ `git rev-parse --short HEAD` / `origin/m/w1-bump-migration` (after fetch) → `ce176e6` · `ce176e6`.
- **Grant check**: ⟨cmd⟩ `grep -rln ESC-F11d-1 docs/tranches/X` → `LEDGER.md` + this record only. No COHESION ruling, so no §3a grant exists.

### HIGH — G-F11-6 e2e leg RED (visual-checkpoint `:144` · `:163` · `[mobile] :182`)

- **Reproduced**: ⟨cmd⟩ `BASE_URL=http://localhost:3100 npx playwright test e2e/visual-checkpoint.spec.ts:144 …:163 …:182` → **3 failed**
  (`:144` 39 px / 0.01 · `:163` expected 400×644, received 400×632 · `[mobile] :182`). Item 3 isolated ×2 → **failed ×2**, 3679 px (0.02) both runs.
- **Item-3 attribution (the part the register asked for)**: the harmonic-count drift is **the checkpoint's own capture race, not a behaviour
  change**. Measured at the same bytes (`ce176e6`), three captures: the first run's actual shows **Harmonics 20 / Display 10** (the baseline's
  state); the two isolated re-runs show **8 / 8** (⟨cmd⟩ `md5 -q` of both actuals → identical `15ad2bbc…`). The test waits only for
  `.slider-subtitle` to be visible (`visual-checkpoint.spec.ts:172-174`), not for the server's `effectiveN` to settle; `EquationView.vue:57`
  seeds `autoHarmonics = ref(true)` and `FunctionInput.vue:224` shows `vizHarmonics` once compute returns (8 for `x(π−x)`), else the
  seeded 20. The F.W11 equation diff (⟨cmd⟩ `git diff 735319e ce176e6 -- web/src/components/equation web/src/composables`) is five radius
  lines and one comment line. No behaviour byte moved, so there is no `web/src` root to cure. The **deterministic** part of item 3 is the
  **height 644 → 632** plus the app grid showing in the gutter. Items 8 and 4 are deterministic too: the grid dots under the translucent
  banner (`576e40a`) and the trigger re-render. All three are render changes from the owner-ruled restyle (§0ao).
- **Cure**: none is lawful from this seat. The cure has two parts. (i) Re-baseline three PNGs under
  `web/e2e/visual-checkpoint.spec.ts-snapshots/`. (ii) Close the item-3 race so that re-baseline is stable: make the checkpoint wait on
  the compute settle. That writes `web/e2e/visual-checkpoint.spec.ts`. Both paths fall outside spec §1 Bounds: the snapshot directory has
  no row, and the spec file is an "every other `web/e2e/*.spec.ts`" do-not-touch. No `web/src` edit can turn the gate green, and forcing
  one would be a masking cure. **ESCALATED — ESC-F11d-1 stands**, now narrowed. What the grant must cover:
  `visual-checkpoint.spec.ts` (item 3's wait only, so it settles on the Parseval compute; no loosened threshold) and the three
  snapshot PNGs, re-baselined with an eye-review receipt.

### MINOR — R-d-1 (native inputs and search wraps are not glass `Input`s)

- **Cure**: not taken. There is no one-command cure. Re-seating `HarmonicLevelGrid .level-input`, `MorphPhaseConfig .num-input` and the
  `PaperSearchInput` / `GallerySearchBar` wraps on the producer `Input` is component work that no gate measures. It also re-renders
  surfaces that snapshot-bearing specs capture, which would widen the very RED ESC-F11d-1 carries. Check 1's own cure routes it
  to "the ESC-F11d-1 repair unit, or the owner rules it out of F.W11". **ESCALATED with ESC-F11d-1** for that ruling.

### INFO — no action.

### Gate re-reading (no fourier byte moved, so no gate could move; the RED legs were re-measured)

| gate | reading (run 1 · run 2) | verdict |
|---|---|---|
| G-F11-1 | 0 · 0 | GREEN |
| G-F11-2 (b) `grep -rEn border-radius web/src \| grep -vc 'var(--radius'` | 0 · 0 | GREEN |
| G-F11-3..5 · -7, G-F11-6 build | cited from Check 1 at `ce176e6`, tree unmoved | GREEN |
| G-F11-6 e2e | the 3 new rows → 3 failed; item 3 isolated → failed · failed | **RED (ESC-F11d-1)** |

- **Commits (this seat)**: value.js record + LEDGER only (see the LEDGER event line). Cured 0; escalated 2 (HIGH → ESC-F11d-1 narrowed ·
  MINOR R-d-1 → owner ruling).
- **Verdict**: **PARTIAL stands** (G-F11-6 RED · ESC-F11d-1). The LEDGER row was not moved.

## Check 2 (2026-09-22, fresh adversarial L-20 pass 2, `claude-opus-5-5[1m]`, VERIFY-ONLY)

Independent seat; authored no byte of the cures, receipts, Closes, Check 1 or Repair 1. Wrote 0 fourier / 0 glass-ui bytes; dev.sh untouched.

- **Crash-recovery**: value.js writable set (`execution/C/F-W11.md` · `LEDGER.md` · `INBOX.md`) clean; fourier porcelain = the J-audit
  `docs/tranches/J/audit/screenshots/before/*.png` only (not this wave's; untouched). Nothing inherited.
- **Tree**: ⟨cmd⟩ `git rev-parse --short HEAD` · `origin/m/w1-bump-migration` (after fetch) → `ce176e6` · `ce176e6`; 12 commits `735319e..HEAD`.
- **Grant check**: ⟨cmd⟩ `grep -rln ESC-F11d-1 docs/tranches/X` → `LEDGER.md` + this record only; COHESION's last addendum is §0ap (KF.W13S),
  none rules ESC-F11d-1 or R-d-1. The Repair 1 escalation is still ungranted.
- **Axis 1 (GREENs reproduce, ×2)**: G-F11-1 → 0·0 · G-F11-2 → 0/0 · 0/0 · G-F11-3 bytes `grep -c paper-grid PaperView.vue` → 0·0 (probe 9/9
  cited from `.b`, tree unmoved) · G-F11-4 `AppDock.vue:35` `import { GlassDock, DockTrigger, DockSeparator } from "@mkbabb/glass-ui/dock"` ·
  G-F11-5 per file 1·4·3·3·2·4·1·3 = 8/8 · G-F11-6 build ⟨cmd⟩ `npm run build` → exit 0 (`✓ built in 7.78s`) · G-F11-7 ⟨cmd⟩ awk over the INBOX
  status column for `UNREAD` → 0 rows; BK is still the newest glass dir (no new letter since the `.d` sweep).
  ⟨cmd⟩ `BASE_URL=http://localhost:3100 npx playwright test e2e/shell-header e2e/coarse-pointer e2e/visualization-ux e2e/visual-checkpoint` →
  **17 passed · 6 failed**. shell-header, coarse-pointer (44px) and visualization-ux keystones all pass. The failures are the 3 banked rows
  (`:80` `:101` `:122`) **plus the same 3 new** (`:144` · `:163` · `[mobile] :182`). Isolated re-run of the 3 → **3 failed**
  (`:163` 400×644 expected vs 400×632 received). **G-F11-6 e2e leg RED reproduces.**
- **Axis 2 (bounds)**: ⟨cmd⟩ `git diff --name-only 735319e HEAD | grep -vE '^web/src/|^web/e2e/screenshots/f-w11-' | wc -l` → 0; no spec,
  `package.json`, glass-ui or `dev.sh` path (dev.sh last touched `85cfea2c`, tranche T).
- **Axis 3 (masking)**: ⟨cmd⟩ added lines under `web/src` matching `try {|catch|.skip|fixme|!important|eslint-disable|@ts-ignore|as any` → 0.
- **Axis 4**: commit families unchanged since Check 1 (one per meaning, no split).
- **Axis 5 (E-3)**: spec last touched `ba1b03d5` (the §0ao mint); ⟨cmd⟩ `git log --since=2026-09-21 -- …/registry/adjudicated/` → 0; prior sections intact.
- **Axis 6**: 0 UNREAD in scope. **Axis 7**: LEDGER row `PARTIAL 2026-09-17 (G-F11-6 RED · ESC-F11d-1)` — lawful, NOT moved by this check.
- **Axis 8 (goal criterion)**: every clause is met at the bytes (no squared edge, token radii, glass surfaces, app grid, GlassDock with names intact),
  except that the `.a` clause "input … local styling yields to the producer's surface" is still open (R-d-1).
- **Axis 9**: the published figures of Close, RE-VERIFY, Check 1 and Repair 1 reproduce (0 · 0/0 · 8/8 · 17p/6f · 3 failed · 400×644 vs 400×632).
- **Axis 10 (honest-RED)**: G-F11-6 e2e is RED because of 3 failures that were not present at `735319e`. Spec §4 relieves only a
  **producer-owned** RED "with the relay id". These failures are consumer-side G-F9-23 snapshot PNGs, and this wave's own restyle made them drift.
  No relay id exists, no successor wave is routed to them, and no honest-RED names them. **UNRELIEVED.** The cure (Repair 1's narrowed
  ESC-F11d-1: the item-3 wait in `visual-checkpoint.spec.ts` and 3 re-baselined PNGs) lies outside §1 Bounds and needs the owner/orchestrator grant.
- **Successors**: no wave declares "Opens after F.W11" (only KF.W11 hits), so nothing is lawfully blocked by this wave.

| severity | claim | receipt | cure |
|---|---|---|---|
| HIGH | G-F11-6 e2e leg RED, unrelieved by §4 (consumer-side, no relay id) | `visual-checkpoint :144 · :163 · [mobile] :182` fail ×2 at `ce176e6`; ESC-F11d-1 ungranted | grant ESC-F11d-1 (the spec item-3 settle-wait + 3 PNG re-baseline with eye review); then a close seat re-runs G-F11-6 |
| MINOR | R-d-1: native inputs and search wraps are not glass `Input`s (spec `.a` "yields to the producer's surface") | Repair 1 §MINOR; no gate measures it | owner ruling with ESC-F11d-1 |
| INFO | 17 GREEN readings reproduce; bounds, masking, E-3 and mail are clean | above | — |

**Verdict: NOT-CONFORMANT** (one unrelieved HIGH). Gates reproduced GREEN: G-F11-1 · -2 · -3 · -4 · -5 · -6 build · -7. Failed: G-F11-6 e2e.
The LEDGER status is NOT moved.

## Repair 2 (2026-09-22, repair seat round 2, `claude-opus-5-5[1m]`)

Register: `## Check 2` above (1 HIGH · 1 MINOR · 1 INFO). Wrote 0 fourier / 0 glass-ui bytes; dev.sh untouched.

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain web/ | wc -l` → 0 (nothing inherited in `web/**`); the J-audit
  `docs/tranches/J/audit/screenshots/before/*.png` + `.worktrees/` are outside every F.W11 row, untouched. value.js writable set clean.
- **Tree**: ⟨cmd⟩ `git rev-parse --short HEAD` · `origin/m/w1-bump-migration` → `ce176e6` · `ce176e6` (unmoved since Check 2).
- **Grant check**: ⟨cmd⟩ `grep -rln 'ESC-F11d-1\|R-d-1' docs/tranches/X` → `LEDGER.md` + this record only; COHESION's last addendum is still
  §0ap (`:2587`, KF.W13S). **No grant exists.**

### HIGH — G-F11-6 e2e RED (visual-checkpoint `:144` · `:163` · `[mobile] :182`) → ESCALATED (ESC-F11d-1 stands)

- **Re-measured**: ⟨cmd⟩ `BASE_URL=http://localhost:3100 npx playwright test e2e/visual-checkpoint.spec.ts:144 …:163 …:182` → **3 failed**
  (`:163` "Expected an image 400px by 644px, received 400px by 632px. 3679 pixels (ratio 0.02)").
- **Why no in-bounds cure**: both halves of the cure write paths spec §1 forbids or does not grant. `web/e2e/visual-checkpoint.spec.ts` is an
  "every other `web/e2e/*.spec.ts`" do-not-touch, and `web/e2e/visual-checkpoint.spec.ts-snapshots/*.png` has no row (`web/e2e/screenshots/**`
  is evidence-create only). The only `web/src` edit that could turn the rows green would revert the owner-ruled §0ao restyle (644→632 height,
  app grid in the gutter). That would be a masking cure, so it is not taken. The item-3 harmonic drift is the spec's own wait race
  (Repair 1 attribution), not a `web/src` behaviour.
- **Grant required (unchanged from Repair 1)**: the item-3 settle-wait on the compute in `visual-checkpoint.spec.ts` (no loosened threshold) +
  re-baseline of the 3 snapshot PNGs with an eye-review receipt; then a close seat re-runs G-F11-6.

### MINOR — R-d-1 → ESCALATED (with ESC-F11d-1)

No one-command cure exists: re-seating `HarmonicLevelGrid .level-input`, `MorphPhaseConfig .num-input` and the `PaperSearchInput` /
`GallerySearchBar` wraps on the producer `Input` is component work. It would also re-render snapshot-bearing surfaces and widen the
ESC-F11d-1 RED. The owner rules it, or it is folded into the ESC-F11d-1 unit.

### INFO — no action.

### Gate re-reading (no fourier byte moved)

| gate | reading (run 1 · run 2) | verdict |
|---|---|---|
| G-F11-1 | 0 · 0 | GREEN |
| G-F11-2 (b) | 0 · 0 | GREEN |
| G-F11-3..5 · -7 · -6 build | cited from Check 2 at `ce176e6`, tree unmoved | GREEN |
| G-F11-6 e2e | the 3 new rows → 3 failed (this seat) · 3 failed (Check 2) | **RED (ESC-F11d-1)** |

- **Verdict**: cured 0 · escalated 2. **PARTIAL stands**; the LEDGER row is not moved.

## Check 3 (2026-09-22, fresh adversarial L-20 pass 3, `claude-opus-5-5[1m]`, VERIFY-ONLY)

This seat is independent: it wrote no byte of the cures, receipts, Closes, Checks 1-2 or Repairs 1-2. It wrote 0 fourier bytes and 0 glass-ui bytes, and left dev.sh untouched.

- **Crash-recovery**: the value.js writable set is clean. Fourier porcelain shows only the J-audit `docs/tranches/J/audit/screenshots/before/*.png`, which lies outside every F.W11 row and was not touched. Nothing was inherited.
- **Tree**: ⟨cmd⟩ `git rev-parse --short HEAD origin/m/w1-bump-migration` → `ce176e6` · `ce176e6`. The tree has not moved since Check 2.
- **Grant check**: ⟨cmd⟩ `grep -rln 'ESC-F11d-1\|R-d-1' docs/tranches/X` → `LEDGER.md` and this record only. The last COHESION addendum is still §0ap (`:2587`, KF.W13S). **No grant has been issued.**
- **Axis 1 (GREENs, ×2)**: G-F11-1 → 0·0. G-F11-2 (a/b) → 0/0 · 0/0. G-F11-3: `grep -c paper-grid PaperView.vue` → 0·0; the 9/9 probe is cited from `.b` because the tree is unmoved. G-F11-4: `AppDock.vue:35` imports `GlassDock` from `@mkbabb/glass-ui/dock`. G-F11-5 per file → 1·4·3·3·2·4·1·3 = 8/8. G-F11-6 build: ⟨cmd⟩ `npm run build` → exit 0 (`✓ built in 7.79s`). G-F11-7: an awk over the INBOX status cells finds 0 UNREAD, and BK is still the newest glass dir with no BK letter newer than the `.d` sweep.
  ⟨cmd⟩ `BASE_URL=http://localhost:3100 npx playwright test e2e/shell-header e2e/coarse-pointer e2e/visualization-ux e2e/visual-checkpoint` → **17 passed · 6 failed**. shell-header, coarse-pointer (44px) and the visualization-ux keystones all pass. The 6 failures are the 3 banked rows (`:80` `:101` `:122`) plus the 3 new ones (`:144` · `:163` · `[mobile] :182`). An isolated re-run of the 3 new rows → **3 failed**, with `:163` reporting "Expected an image 400px by 644px, received 400px by 632px". **The G-F11-6 e2e RED reproduces for the third time.**
- **Axis 2 (bounds)**: ⟨cmd⟩ `git diff --name-only 735319e HEAD | grep -vE '^web/src/|^web/e2e/screenshots/f-w11-' | wc -l` → 0. `web/package.json` and `web/e2e/*.spec.ts` show no diff. dev.sh was untouched.
- **Axis 3 (masking)**: ⟨cmd⟩ lines added under `web/src` that match `try {|catch|.skip|fixme|!important|eslint-disable|@ts-ignore|as any` → 0.
- **Axis 4**: the commit families are unchanged since Check 1. **Axis 5 (E-3)**: the spec was last touched at `ba1b03d5` (the mint), and the registry/adjudicated dir has 0 commits since 2026-09-21. **Axis 6**: 0 UNREAD in scope.
- **Axis 7**: the LEDGER row still reads `PARTIAL 2026-09-17 (G-F11-6 RED · ESC-F11d-1)`. That status is lawful, and this check does NOT move it.
- **Axis 8**: the goal criterion is met at the bytes. The one open item is the `.a` "yields to the producer's surface" clause for native inputs (R-d-1), which is MINOR and has no gate.
- **Axis 9**: every published figure reproduces: 0 · 0/0 · 8/8 · 17p/6f · 3 failed · 400×644 vs 400×632.
- **Axis 10 (honest-RED)**: G-F11-6 e2e has 3 failures that did not exist at the `735319e` baseline. Spec §4 relieves only a producer-owned RED that carries a relay id. These rows are consumer-side G-F9-23 snapshots that drifted because of this wave's own §0ao restyle. There is no relay id, no successor routing and no named honest-RED, so the RED is **UNRELIEVED**. The cure writes `visual-checkpoint.spec.ts` and its `-snapshots/*.png`, both outside §1 Bounds, so it needs the ESC-F11d-1 grant.
- **Successors**: no wave declares "Opens after F.W11" (a grep finds only KF.W11/KF.W9 hits), so no successor is blocked.

| severity | claim | receipt | cure |
|---|---|---|---|
| HIGH | The G-F11-6 e2e leg is RED and §4 does not relieve it (the failures are consumer-side and have no relay id) | `visual-checkpoint :144 · :163 · [mobile] :182` fail ×2 at `ce176e6`; ESC-F11d-1 is still ungranted after Repair 2 | The owner/orchestrator grants ESC-F11d-1: the item-3 settle-wait plus a re-baseline of the 3 PNGs with an eye-review receipt. A close seat then re-runs G-F11-6 |
| MINOR | R-d-1: the native inputs and search wraps are not glass `Input`s | Repair 2 §MINOR; no gate measures it | The owner rules it with ESC-F11d-1 |
| INFO | 17 GREEN readings reproduce; bounds, masking, E-3 and mail are clean | above | — |

**Verdict: NOT-CONFORMANT** (one unrelieved HIGH). Gates reproduced GREEN: G-F11-1 · -2 · -3 · -4 · -5 · -6 build · -7. Failed: G-F11-6 e2e. The LEDGER status is NOT moved.

## RESUME 2 (2026-09-22, seat 0 re-open on COHESION §0aq, `claude-opus-5-5[1m]`, VERIFY-AND-BANK)

Sitting of record 2026-09-17 (§0j). This seat wrote 0 fourier-analysis bytes, 0 glass-ui bytes and 0 product bytes, and left dev.sh untouched.
Authority: COHESION **§0aq** `### F.W11` (ESC-F11d-1 **GRANTED to `.e`**, in order: R-d-1 Input move → item-3 settle-wait → three PNGs re-baselined with an eye-review receipt; then `.d2` re-runs G-F11-6) and the spec's **ADDENDUM 2026-09-22** (`F-W11.md:50-51`, unit `.e` + gate G-F11-8 + close `.d2`).

### Open

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain | grep -v 'docs/tranches/J/audit/screenshots/before'` → `?? .worktrees/` only. The J-audit `before/*.png` rows lie outside every F.W11 row and were not touched. `web/src/**` and `web/e2e/**` are clean, so nothing was inherited. The value.js writable set is clean (porcelain: `CARRY-LEDGER.md` belongs to a sibling; `dev.sh` is untouched by standing law).
- **Already done (commits exist, never re-dispatched)**: `.a` (`e844536` … `0824371`) · `.b` (`576e40a` · `78ab4f9`) · `.c` (`53aaa6f` · `6d1e53d` · `ce176e6`) · `.d` (close record §Close, VERIFY-ONLY). ⟨cmd⟩ `git log --oneline --all --grep 'F.W11.e'` → empty, so **`.e` is owed** and **`.d2` is owed**.
- **Tree**: ⟨cmd⟩ `git rev-parse --short HEAD origin/m/w1-bump-migration` → `ce176e6` · `ce176e6`, on branch `m/w1-bump-migration`. The tree is unmoved since Check 3.
- **Precondition (the grant)**: §0aq `### F.W11` names ESC-F11d-1 as GRANTED to `.e`, and the spec addendum at `:50-51` names the writable ⊕. **MET.** Producer: glass-ui **8.0.0** in `web/node_modules` exports `./input` (⟨cmd⟩ `node -e …exports` → `[ './input' ]`), which `FunctionInput.vue`, `UserSlugBar.vue`, `AdminUserList.vue` and `AdminAuditLog.vue` already consume.
- **E13 Step-0 mail sweep** (four paths, read-only; BK is still the newest glass B-series dir: ⟨cmd⟩ `ls glass-ui/docs/tranches | grep '^B' | tail -3` → `BI BJ BK`). ⟨cmd⟩ `find <path> -maxdepth 1 -type f -newermt "2026-09-22 00:00"` → value.js `V/`: `EVIDENCE.md` · `VISUAL-CONSTITUTION.md` · `OPTICAL-BENCH-COMPOSITIONS.md` (Track A documents, not letters) · `V/coordination`: `INBOX.md` · BK/coordination: `glass-outbound-2026-09-22-consumers-10.0.0.md`, `fourier-to-glass-2026-09-17-nwo1-bh-relay.md`, `value-to-glassui-2026-09-DD-fw4-relay.md` (all three were already rowed or noted at earlier sweeps) · keyframes.js `V/coordination`: empty · atlas `P/coordination`: empty. The highest row is `I-39`. ⟨cmd⟩ `grep '^| I-' INBOX.md | grep -c '| UNREAD'` → **0**. **0 new rows, 0 UNREAD in scope.** A dated sweep line was appended at the INBOX tail.

### Baseline (RESUME 2 — only the gates the owed units turn; G-F11-1..5 and 7 cite Check 3, tree unmoved at `ce176e6`)

| gate | command | reading ×2 | state |
|---|---|---|---|
| **G-F11-8** (born-RED, addendum) | per file in {`morph/HarmonicLevelGrid.vue` · `morph/MorphPhaseConfig.vue` · `paper/search/PaperSearchInput.vue` · `visualization/gallery/GallerySearchBar.vue`}: `grep -cE "\bInput\b.*from ['\"]@mkbabb/glass-ui/input['\"]"` | **0/4 · 0/4** | RED, as born |
| — native `<input>` census on the same four | `grep -c '<input'` per file | 2 · 1 · 2 · 1 (6 elements) | the sites `.e` moves |
| **G-F11-6** (e2e leg, the rows `.e` turns) | `cd web && BASE_URL=http://localhost:3100 npx playwright test e2e/visual-checkpoint --reporter=list` (dev stack up: `curl localhost:3100/` → 200) | run 1: **6 failed** · run 2: **6 failed** | RED: the 3 new rows (`:144` · `:163` · `[mobile] :182`) plus the 3 banked-at-`735319e` rows (`:80` · `:101` · `:122`) |
| G-F11-6 (build) | cited from Check 3 (`npm run build` exit 0 at `ce176e6`) | — | GREEN, carried |
| G-F11-1..5 · 7 | cited from Check 3 (the tree is unmoved) | — | GREEN, carried |

- The run-1 and run-2 failure sets are identical: `:80` `:101` `:122` `:144` `:163` (chromium) and `:182` (mobile-chromium). All six fail with `expect(locator).toHaveScreenshot(expected) failed`.
- **Scope note for `.e`**: §0aq grants the re-baseline of **three** PNGs only: `checkpoint-admin-banner` (`:144`), `checkpoint-disclosure-body` (`:163`) and `checkpoint-tooltip-trigger-mobile` (`:182`). The `:80`/`:101`/`:122` rows (`card-resting`, `card-hover`, `card-modal`) were already failing at `735319e`. They are in the banked baseline failure set, and `.e` must NOT re-baseline them.
- **Green-before-cure**: none. Both born-RED gates (G-F11-8 and the G-F11-6 e2e leg) read RED.

### Unit plan (RESUME 2)

Order (the spec addendum `:51` and §0aq): **[`.e`] → [`.d2`]**, serial, with one unit in flight at a time. `alreadyDone`: `F.W11.a` · `F.W11.b` · `F.W11.c` · `F.W11.d`.

| unit | model | spec sections | writable | gates | locks |
|---|---|---|---|---|---|
| `F.W11.e` Input move → settle-wait → re-baseline | opus | ADDENDUM 2026-09-22 (`:50-51`) · §2 `.a` "yields to the producer's surface" (`:27`) · §0aq `### F.W11` | fourier `web/src/components/**` (the four sites) · `web/e2e/visual-checkpoint.spec.ts` (the item-3 settle-wait only) · `web/e2e/visual-checkpoint.spec.ts-snapshots/` (the 3 granted PNGs, force-added) · value.js record + `LEDGER.md` + `INBOX.md` | G-F11-8 (4/4 ×2) · leaves the `:144`/`:163`/`[mobile] :182` rows green ×2 · G-F11-1/-2 stay 0 · build exit 0 | order is binding (Input move, THEN settle-wait, THEN re-baseline) · no threshold/`maxDiffPixels` change, no skip/fixme · the `:80`/`:101`/`:122` PNGs are NOT touched · accessible names and 44px floors are preserved · glass-ui is READ-ONLY and any gap is a BK relay row · one commit per meaning · push `m/w1-bump-migration` at close |
| `F.W11.d2` Close | opus (VERIFY-ONLY) | §2 `.d` (`:32-33`) · §3 G-F11-1..8 · §4 (`:44-46`) · ADDENDUM | value.js record · `LEDGER.md` · `INBOX.md` | G-F11-1..8 double · the full G-F11-6 e2e run compared to the `735319e` stable set | honest-RED only with a relay id · E13 · bounds path-by-path · push |

## Unit receipts (RESUME 2)

### F.W11.e — Input move → settle-wait → re-baseline (`claude-opus-5-5[1m]`, 2026-09-22)

Authority: spec ADDENDUM 2026-09-22 (`F-W11.md:50-51`) · §2 `.a` "the local styling yields to the producer's surface" (`:27`) · COHESION §0aq `### F.W11` (ESC-F11d-1 GRANTED to `.e`). Fourier opened at `ce176e6`; `BASE_URL=http://localhost:3100` (⟨cmd⟩ `curl -s -o /dev/null -w '%{http_code}' localhost:3100/` → `200`). 0 glass-ui bytes written; dev.sh untouched.

- **Crash-recovery**: ⟨cmd⟩ `git -C fourier-analysis status --porcelain` → only the J-audit `docs/tranches/J/audit/screenshots/before/*.png` rows and `?? .worktrees/`, all outside this unit's set. `web/src/**` and `web/e2e/**` were clean, so nothing was inherited. The value.js writable set was clean too.
- **Anchors at true bytes**: all four sites matched the grant. `HarmonicLevelGrid.vue:18/:39` had two `<input type="number" class="level-input">`. `MorphPhaseConfig.vue:40` had `<input type="number" class="num-input">`. `PaperSearchInput.vue:59` had a bare `<input>` inside a `<label class="paper-search-input-wrap">` that painted the field chrome. `GallerySearchBar.vue:84` had a bare `<input type="search">` inside `.search-pill`, which also painted the field chrome. The idiom is `FunctionInput.vue:4` (`import { Input } from "@mkbabb/glass-ui/input"`, with `:model-value` plus a native `@change`/`@input` on `$event.target`).
- **Producer read (read-only)**: `dist/components/input/types.d.ts` shows `Input` is a single `<input>` root with `inheritAttrs: false`, the attrs forwarded, `size` of `sm|md|lg`, and a `type` union that is text-shaped only. `number` is fenced out ("`number` is Reka's NumberField"). The CSS is `.field-control[data-kind=input]{block-size:var(--field-control-height);border-radius:var(--radius-pill)}`, with a `:focus-visible` outline and `::placeholder` set to `--muted-foreground`.

**Act 1: R-d-1, the Input move** (fourier **`2435f78`**, 4 files, +194/−203)
- `HarmonicLevelGrid` ×2 and `MorphPhaseConfig` ×1 now use `<Input type="text" inputmode="numeric" size="sm" :model-value=… @change=…>`. The ids (`useId`), the `for` labels, the classes and the `emitLow`/`emitHigh`/`emitDuration` handlers are unchanged. The clamps in those handlers were already the range authority, so the `min`/`max`/`step` attributes, which are inert on `type="text"`, retire.
  - The local field chrome retires in both files: boundary, radius, fill, `outline:none`, transition, spin-button suppression, the FMD-19 `--viz-legendre` focus paint and the MPC-22 per-card focus tint. Only the layout remains: the width, the `text-align` and the no-shrink. A dated comment at each rule says so.
- `PaperSearchInput`: the `<input>` becomes `<Input>`. The `<label>` wrap stays (MISS-DU4 hit area) but keeps only layout: `position:relative`, flex, `cursor:text`. The glyph is positioned inside the field. The two `xs` actions sit in a positioned `span.paper-search-actions`, and the field's inline padding reserves `2 × var(--control-h-xs)` for them.
  - MISS-DU1 now holds structurally: the field's height is the producer's `md` rung, and the actions are positioned, so they cannot grow it.
  - The ref is now the component instance, so `focus()` reaches `$el`, which is the `<input>` root.
  - The floating variant's `border:none; background:transparent; padding:0` retires because there is no wrap chrome left to strip.
- `GallerySearchBar`: `.search-pill` loses its fill, border and `--radius-field` and becomes a positioning context. The Search glyph and `span.search-actions` (clear · Separator · filter toggle) are seated inside the field. The FR-GSB-12 50% placeholder dilution yields to the producer's `::placeholder`.
- **Design finding, cured in the same commit before landing**: the first probe showed both search glyphs painted UNDER the field. The producer's field is its own stacking context (glass backdrop), so a positioned glyph earlier in tree order is covered by it. The glyph now follows the `<Input>` in tree order (commented at both sites). No z-index literal was added.
- Probe (bounded, 1440×900, one script run twice then deleted): `.level-input` n=2, `data-slot=input`, box 56×36, label `Low`; typing 3 then Enter → value `3`. `.num-input` has `data-slot=input`, box 56×36, label `Duration`. The gallery `searchbox "Search gallery by slug"` has `data-slot=input`, 512×40, and `Clear search` is visible after typing. The paper `combobox "Search the paper"` has `data-slot=input`, 196×40 before a keystroke and 196×40 after, with the wrap also 196×40.
  - Screenshots were viewed. The level and duration fields are glass pills. The gallery and paper fields are glass pills with the glyph at the start and the actions at the end.

**Act 2: the item-3 settle-wait** (fourier **`fd9d1f2`**, `web/e2e/visual-checkpoint.spec.ts` +16, the item-3 body only)
- Before `page.goto("/equation")`, the test now registers `page.waitForResponse(POST /api/equations/compute$)` with a 60 s timeout, then asserts that response is `200` before `freeze`.
- Cause, measured at the bytes: `EquationView.vue:48` seeds `effectiveN` to 20 and `:325` fires the first compute on mount when the fresh context has no cache. The response sets `effectiveN`, which gives 8/8 for `x(π−x)`.
- No threshold, no `maxDiffPixels`, no skip.
- Measured before the re-baseline, at `2435f78` plus the wait, running `:163` three times: each run failed identically against the OLD PNG (`Expected 400×644, received 400×632. 3679 pixels (0.02)`). ⟨cmd⟩ `md5 -q …/checkpoint-disclosure-body-actual.png` → `15ad2bbc1543ec3cbcc51af0d2fecd94`, which is the same hash Repair 1 banked for the settled 8/8 capture. The race is closed: the capture is deterministic.

**Act 3: the re-baseline of the three granted PNGs, force-added** (fourier **`01279eb`**)
- The old PNGs were saved to the scratchpad first. ⟨cmd⟩ `npx playwright test e2e/visual-checkpoint.spec.ts:144 …:163 …:198 --update-snapshots` → `3 passed`. The `[mobile] :182` test sits at `:198` after Act 2's +16, which is the INTENT at the true bytes.
- ⟨cmd⟩ `md5 -q` over all six PNGs, before → after:
  - admin-banner: `4323cc97…` → `b73775f3…`
  - disclosure-body: `ae948899…` → `15ad2bbc…`
  - tooltip-trigger-mobile: `7e12031a…` → `972f5aaf…`
  - card-hover `75c45aa8…`, card-modal `10cb83db…` and card-resting `0188d693…` are **unchanged**. The banked `:80`/`:101`/`:122` PNGs were not touched.
- **Eye-review receipt**: each old and new PNG was viewed side by side, and the pixels were diffed with PIL.
  - **admin-banner** (1248×161 both; every pixel moves a little): the app's graph-paper grid (`.b`, `576e40a`) now shows through the translucent banner and its six stat cells. The fill shifts by one level, from (240,233,223) to (241,234,224). The amber 1px admin border (157,101,21) is present in both at x=0 and y=1. The text, values (128 · 7 · 41 · 9.3K · 452 · 70.0 MB), layout and `Log out` are identical. This is a render change from the §0ao restyle, not a behaviour change.
  - **disclosure-body** (400×644 → 400×632): Harmonics reads 20 → **8** and Display terms 10 → **8**, which is the settled compute (the old baseline had captured the pre-response seed). The panel is 12 px shorter. The grid shows in the gutter below the Controls card. The Function card (expression, Domain `0 to π`, Compute, presets with `x(π−x)` active) is identical, and so are the Notation toggles. This is the settled state plus a render change, not a behaviour change.
  - **tooltip-trigger-mobile** (60×61 both; 2256 px differ by any channel): the Wand2 trigger's disc, rim, glyph and size are visually identical at 3× zoom. The differing pixels are the antialiased corners outside the circle, where the grid now lies behind the rim. This is a render change only.

**Gates, BEFORE → AFTER** (fourier HEAD `01279eb` = `origin/m/w1-bump-migration`, pushed)

| gate | command | before | after (×2) |
|---|---|---|---|
| **G-F11-8** | per file `grep -cE "\bInput\b.*from ['\"]@mkbabb/glass-ui/input['\"]"` (the four sites) | 0/4 · 0/4 | **1 1 1 1 = 4/4 · 4/4** |
| **G-F11-6 e2e** (`.e`'s rows) | `BASE_URL=http://localhost:3100 npx playwright test e2e/visual-checkpoint --reporter=line` | 6 failed ×2 | run 1: **3 passed · 3 failed** · run 2: **3 passed · 3 failed**. `:144` · `:163` · `[mobile] :198` (formerly `:182`) are GREEN both runs; the 3 failures are exactly the banked `:80` · `:101` · `:122` (the `735319e` set) |
| G-F11-1 | `grep -rEn 'border-radius:\s*0[^.]\|rounded-none' web/src \| wc -l` | 0 | **0 · 0** |
| G-F11-2 | `grep -rEn 'border-radius:\s*[0-9]' web/src \| wc -l` ; `grep -rEn 'border-radius' web/src \| grep -vc 'var(--radius'` | 0 / 0 | **0 / 0 · 0 / 0** |
| G-F11-6 build | `npm run build` (`vue-tsc -b && vite build`) | exit 0 (Check 3) | **exit 0** after Act 1 (`✓ built in 5.67s`) · **exit 0** at HEAD |
| G-F11-4 + G-F11-5 | `npx playwright test e2e/shell-header e2e/coarse-pointer e2e/visualization-ux` | 17 passed (Check 3) | read 1 (the same set plus `gallery.spec` · `paper-search` · `slider-scrub-contrast`, at HEAD): 28 passed, 1 failed, the `visualization-ux :290` `save_contour_then_recompute`. The failing check was `[aria-label="Edit contour"]` hidden under parallel load, which is dock code `.e` never touched. Isolated ×3 → **1 passed ×3**. Read 2 → **17 passed · 0 failed** |

- Site-adjacent specs also ran before the commit: `gallery.spec` (the `searchbox` name and the drawer toggle), `paper-search` ×3 (the `combobox` name, listbox, navigation) and `slider-scrub-contrast` all passed. `gallery-admin-a11y` ×4 failed on `aria-hidden-focus` from a reka focus-guard `<span>`. Those are in the banked baseline set (record `:62-65`), are unrelated to these sites, and are cited, not re-counted.
- **Bounds**: ⟨cmd⟩ `git diff --name-only ce176e6 HEAD` → the 4 `.vue` files, `visual-checkpoint.spec.ts` and the 3 granted PNGs, which is 8 paths, all inside the writable set. The transient probe `web/.probe-e.mjs` was never staged and was deleted.
- **Commits** (fourier, pathspec, one per meaning, in the binding order): `2435f78` (Input move) → `fd9d1f2` (settle-wait) → `01279eb` (re-baseline). ⟨cmd⟩ `git push origin m/w1-bump-migration` → pushed; `git rev-parse --short HEAD origin/m/w1-bump-migration` → `01279eb` · `01279eb`.

**Residuals (for `.d2`; none gate-bearing)**
- **R-e-1**: Arrow-key stepping on the three numeric fields retires with `type="number"`, because `Input`'s type fence excludes `number`. Each field sits beside a producer `Slider` bound to the same value, so keyboard stepping stays on the slider. The producer's number surface is `./number-field` (Reka NumberField); the grant named `Input`, so the adoption was not substituted.
- **R-e-2**: The `e2e/contrast-pairs.ts` rows `HLG-37[level-input-border]` and `FMD-19[level-input-focus-border]` still describe the retired local `.level-input` boundary and focus paint. They are token stacks, not element reads, so they measure unchanged. That file is outside `.e`'s bounds, and retiring the rows belongs to a later contrast unit.
- **R-e-3**: `GallerySearchBar`'s `type="search"` still paints the UA's `::-webkit-search-cancel-button` beside the consumer's `Clear search` Button. This predates `.e`: the old `<input type="search">` had the same UA glyph, and ⟨cmd⟩ `grep -rho 'search-cancel-button' node_modules/@mkbabb/glass-ui/dist src` → 0. Offered to `.d2` as a candidate BK relay observation, not claimed as a gap here.
- **Escalations**: none. **E13**: this unit made no sweep (RESUME 2's Step-0 sweep stands). No mail was written and no INBOX row added.
