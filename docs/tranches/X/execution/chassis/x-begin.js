export const meta = {
  name: 'x-begin',
  description: 'Tranche X begin-word pre-acts (runbook §3.3 + sitting agenda): mail round ∥ 27-gate baseline (2 seats) ∥ sitting dossier — all read-only except the mail-round addenda',
  phases: [
    { title: 'Pre-acts', detail: 'Opus ×4 in parallel: mail round · gates 1-10+23-27 · gates 11-22 · sitting dossier', model: 'opus' },
  ],
}

const ROOT = '/Users/mkbabb/Programming/value.js'
const X = `${ROOT}/docs/tranches/X`
const EXEC = `${X}/execution`
const RB = `${X}/EXECUTION-RUNBOOK.md`
const INBOX = `${ROOT}/docs/tranches/V/coordination/INBOX.md`
const TRAILER = 'Claude-Session: https://claude.ai/code/session_01QkbQV4VgkoQgSoUj2oKZim'
const LAW = `STANDING LAW: pathspec commits only (git add <paths> && git commit --no-verify --quiet -m "<msg>" -m "${TRAILER}"); NEVER stage or touch ${ROOT}/scripts/dev/dev.sh; E-3 — never rewrite dated evidence, corrections are dated addenda; portable BSD commands; WRITE-THEN-MEASURE (every published count read from settled bytes, double-run); SELF-COUNT LAW (self-excluding probes); quote-by-command with ⟨cmd⟩ receipts; sibling trees ../keyframes.js ../fourier-analysis ../parse-that ../glass-ui are READ-ONLY (no writes, no stash, no reset, no npm install/ci/build); no product source (src/ demo/ api/ test/ e2e/) is opened for edit by this seat. Line 1 of any file you create: 'SERVED MODEL: <your model id>'. Return ONLY the JSON schema asked.`

phase('Pre-acts')

const MAILP = `You are the MAIL-ROUND SEAT at tranche X's begin-word (2026-09-17). Authority: ${RB} §3.3 + §5.3, COHESION §0i.4 (${X}/COHESION.md). ${LAW}
ACTS, in order:
(1) FOUR-PATH SWEEP: list newest-first (ls -lt) ${ROOT}/docs/tranches/V/ + ${ROOT}/docs/tranches/V/coordination/ · ${ROOT}/../glass-ui/docs/tranches/BK/coordination/ (also confirm BK is still the newest tranche dir: ls -d ${ROOT}/../glass-ui/docs/tranches/*/ | tail -3) · ${ROOT}/../keyframes.js/docs/tranches/V/coordination/ · ${ROOT}/../sci-report/atlas/docs/tranches/P/coordination/ (+ its MAIL-REGISTER.md tail). Compare against every row of ${INBOX} (I-1..I-30, O-0..O-20; the ledger tail is I-30, the 2026-08-29 glass ACK). Any letter addressed to value.js and not rowed → append a row UNREAD (id = next I-n, date, from, path, ≤2-line summary) BEFORE anything else. Then append at the END of ${INBOX} a dated line: '**Sweep 2026-09-17 (begin-word, runbook §3.3)**: four paths swept; newest per path = <file@mtime ×4>; <N> unrowed letters rowed (or: nothing unrowed — I-30 remains the tail).' — one line, no table edit.
(2) THE UNION CHECK-2 TAIL: read ${X}/union/CHECK-2/CHECK-RETURN.json (6 MINOR-mitigated + 2 INFO). Cure each as a DATED ADDENDUM-BESIDE at the owning file, in that file's own addendum idiom (⟨…2026-09-17…⟩ or a dated line — read how the file already does it): (a) ${RB}:250 — re-run both greps at settled bytes and restate the receipt+tense in a dated sub-line inside the same table row (zero-line-delta: do not add lines that displace sibling coordinates); (b) ${X}/fourier/waves/F-W0.md — one dated line beside the FR-NP-32 pre-gate row (G-4) recording the producer-side terminus (A-1 ACCEPTED at I-30; the BLOCKER stands at the adopted 4.0.0 bytes); (c) ${RB} §1 provenance table — F-W6 anchor ':537' → §-heading + quoted fragment (COHESION §0g.2 rule); (d) ${X}/union/SEAMS.md §ERRATA-1 E1-3(b) — re-run and restate the waves row (O-18a O-19 O-20) as a dated sub-line; (e) SEAMS §ERRATA-1 E1-1 — publish all four hits (:437 :453 :514 :522) as a dated sub-line; (f) SEAMS §ERRATA-1 E1-1 — mark the minted bold 'emphasis added' in a dated sub-line; (g) INFO: SEAMS §3 S-11 section-mark — one dated sub-line noting the ACK's byte form '§B-1..B-7'; (h) INFO: ${X}/union/G-F-ADJUDICATION.md §5 R-1 — dated sub-line adding AP-12 and AP-31 to the AuroraPane-resolving parenthetical. For every figure you publish: measure AFTER all edits land, then re-run once more (double-run) and confirm reproduction.
(3) COMMIT (pathspec = exactly the files you touched): 'docs(X·exec): begin-word mail round — four-path sweep (<summary>); union check-2 tail cured as dated addenda (6 MINOR + 2 INFO)'.
(4) Append to ${EXEC}/LEDGER.md's P-1 row nothing — instead append ONE line to its '## Event log' section: '- 2026-09-17 — P-1 mail round: <newMail> rowed, 8 tail items cured, commit <hash>.' and commit that with pathspec ${EXEC}/LEDGER.md: 'docs(X·exec): ledger — P-1 banked'.
Return ONLY: {newMail: number, cured: number, commit: string, ledgerCommit: string, notes: string[] (≤5)}.`
const MAILO = { label: 'mail-round', phase: 'Pre-acts', model: 'opus', schema: { type: 'object', required: ['newMail', 'cured', 'commit'], properties: { newMail: { type: 'number' }, cured: { type: 'number' }, commit: { type: 'string' }, ledgerCommit: { type: 'string' }, notes: { type: 'array' } } } }

const gatesP = (part, which, extra) => `You are a GATES-BASELINE SEAT at tranche X's begin-word (2026-09-17). Authority: ${RB} §2 (the 27 born-RED first gates) and its standing law R.2: 'Running them at wave-open re-dates the baselines; a PASS on any of them before its cure lands is itself a finding and goes to the sitting.' ${LAW}
YOU OWN GATES ${which}. For EACH gate: run the LITERAL command quoted in §2 at the repo it names (value.js = ${ROOT}; keyframes = ${ROOT}/../keyframes.js — the kf gates address origin/master, never bare HEAD, exactly as the runbook spells them; keyframes-v-exec = ${ROOT}/../keyframes-v-exec; fourier = ${ROOT}/../fourier-analysis (its web/ subtree); parse-that = ${ROOT}/../parse-that; glass = ${ROOT}/../glass-ui). READ-ONLY, absolutely: no npm ci, no build, no fetch that writes, no reset, no stash — where a gate's GREEN condition is an act (G-5's build, G-12's table, G-3's ledger, G-11) bank only its read-only receipts and mark the act-half UNRUN. ${extra}
Record per gate: #, name, cwd, the command as run, the full output (if >40 lines: first 30 + '… (N lines)'), the runbook's expected reading, and VERDICT ∈ {RED-AS-EXPECTED, DIVERGENT (state exactly how the reading differs from the expected), GREEN-BEFORE-CURE (an R.2 sitting finding), UNRUNNABLE (why)}. Write ${EXEC}/gates/BASELINE-2026-09-17-${part}.md (mkdir -p) with a summary table first, then the per-gate receipts. Commit pathspec that file: 'docs(X·exec): gates baseline ${part} — <n> RED-AS-EXPECTED / <n> DIVERGENT / <n> GREEN-BEFORE-CURE / <n> UNRUNNABLE'.
Return ONLY: {gates: [{n: number, verdict: string, note: string}], commit: string}.`
const gatesO = (part) => ({ label: `gates-${part}`, phase: 'Pre-acts', model: 'opus', schema: { type: 'object', required: ['gates', 'commit'], properties: { gates: { type: 'array', items: { type: 'object', required: ['n', 'verdict'], properties: { n: { type: 'number' }, verdict: { type: 'string' }, note: { type: 'string' } } } }, commit: { type: 'string' } } } })

const G1P = gatesP('value-parse', '1-10 (X·V, §2.1) and 23-27 (X·P, §2.4)', `Gate 3 needs ${ROOT}/dist/subpaths/css.js — if dist is absent, do NOT build; mark UNRUNNABLE-NO-DIST and also run the same probe against the packed registry tarball only if node_modules/@mkbabb/value.js exists (it may not). Gate 23: run shasum on the named handoff and paste the sibling rows the wave names (${X}/parse-that/waves/W0.md §6 G-1). Gate 24: the ~/Documents/Codex probes — report the access state (EPERM/absent/present) with date, three-state. Gate 25: run the eighteen-root census per W0.md §6 G-3 (rev-parse + file count per root) — this is the 'before' census; save it as ${EXEC}/gates/census-before-2026-09-17.txt. Gate 27: the value.js porcelain over src api demo test e2e.`)
const G2P = gatesP('kf-fourier', '11-15 (X·KF, §2.2) and 16-22 (X·F, §2.3)', `Gate 11's falsifier uses process substitution — run under bash -c. Gate 16 (G-1): the fourier tree is expected at branch m/w1-bump-migration, cd26c65, 28 porcelain rows — also paste the 28 rows. Gate 18 (G-4): the comment-token counts over ${ROOT}/../fourier-analysis/web/node_modules/@mkbabb/glass-ui/dist/styles/index.css (if node_modules is absent, say so — UNRUNNABLE-NO-INSTALL; do not install). Gate 20 (G-13): producer package.json at ${ROOT}/../glass-ui + installed consumer pin from web/package.json + web/node_modules if present; the cell is the commit hash — record git -C ../glass-ui rev-parse v8.0.0 (expect 17a11bc5) and v9.0.0. Gate 22 (G-3): ls the F coordination path.`)

const DOSSP = `You are the SITTING-DOSSIER SEAT at tranche X's begin-word (2026-09-17). The orchestrator RULES; you GATHER — you rule nothing. ${LAW}
Enumerate EVERY owner-gated decision the four root waves and their immediate successors need before or at opening, from the spec bytes, and write ${EXEC}/SITTING-DOSSIER-2026-09-17.md. Sources (read the cited sections whole): ${X}/waves/W0.md (§Agent Units X-W0.g the owner sitting packet — the seven owner rows CC-014/DR-29 incl. CC-019/020/021/023; the §3.e escalation slate; X-W0.j); ${RB} §4.3 (four FORMATION-BOUNDARY packets, sitting-elected) + §4.4 (G-F R-1/R-2/R-3 bookings); ${X}/keyframes/waves/KF-W0.md (§State, §Scope, §Gates, §Agent Units, §Sequencing — OP-1 write authority, §B-12 the reset: quote the exact commands the spec prescribes and forbids, and the 'owner's hand' clauses verbatim) and KF-W1/KF-W2/KF-W9/KF-W10 wherever OP-2..OP-7 and OD-V3/OD-V5 are DEFINED (grep -n 'OP-[0-9]' and 'OD-V' and read the defining passage); ${X}/fourier/waves/F-W0.md §5 'Owner rulings — FLAGGED INLINE' (OG-F1, G-15's four contradictions, G-10's F8-REACH-01/02 breath, G-F7-1, ESC-1) + ${X}/fourier/waves/F-W5.md (E16 trie-vs-KISS · D3/G11 producer-or-retire · OG-F2) + F-W7 (G-F7-1 definition); ${X}/parse-that/waves/W0.md + W4.md (OC-1 bench bar; OP-1 the separate release word; OP-4/OP-5/OP-6 as defined there). Already RULED — cite, do not re-open: COHESION §0i.1 (G-9 = C), §0i.2 (X-W0.j target 8.0.0 @ 17a11bc5), §0i.3 (ESC-1/G1 = 8.0.0 @ 17a11bc5).
FOR EACH ITEM a fixed block: **id** · **owning spec:line** · **the question in the spec's words** (quote ≤3 lines, ⟨cmd⟩ sed receipt) · **options exactly as the spec enumerates them** · **the spec's own default/recommendation, if any (quote)** · **downstream cost of each option** (which waves/gates move) · **what is needed to rule it** (a measurement you can take now read-only — take it and paste it).
PLUS one extra item BRANCH-TOPOLOGY: value.js HEAD is branch tranche-u; origin/master carries 3 commits tranche-u lacks (44ddaff7, e2652f1c, 7334c793 — 2026-07-16 'feat(package-v4): cut the exact-seven immutable capability surface' + two ci(release)); merge-base 86a9a178. Measure read-only: git diff --stat HEAD origin/master grouped by top-level dir; name precisely what master has that tranche-u lacks and vice-versa at the release/CI layer (.github/workflows/release.yml, scripts/ci/*.mjs, package.json scripts) and at test/; read ${X}/waves/W9.md and W11.md §State/§Scope/§Commit Plan to say which wave's acts touch release/CI and therefore owns the reconciliation; enumerate options (merge origin/master into tranche-u before X-W9 · cherry-pick the three · declare tranche-u the tree of record and re-derive the CI layer in X-W11 · retarget: rebase tranche-u onto master) with costs and the risk each carries for the 4.0.0 immutable publish.
Commit pathspec the dossier: 'docs(X·exec): sitting dossier — <n> owner-gated items gathered + BRANCH-TOPOLOGY'.
Return ONLY: {items: [{id, spec, question, options: string[], specDefault, measured}], commit} (≤28 items; keep each field ≤300 chars).`
const DOSSO = { label: 'sitting-dossier', phase: 'Pre-acts', model: 'opus', schema: { type: 'object', required: ['items', 'commit'], properties: { items: { type: 'array', items: { type: 'object', required: ['id', 'spec', 'question'], properties: { id: { type: 'string' }, spec: { type: 'string' }, question: { type: 'string' }, options: { type: 'array' }, specDefault: { type: 'string' }, measured: { type: 'string' } } } }, commit: { type: 'string' } } } }

const seats = [
  ['mail', () => agent(MAILP, MAILO)],
  ['gates-value-parse', () => agent(G1P, gatesO('value-parse'))],
  ['gates-kf-fourier', () => agent(G2P, gatesO('kf-fourier'))],
  ['dossier', () => agent(DOSSP, DOSSO)],
]
let results = await parallel(seats.map(([, f]) => f))
// retry any dead seat once
for (let i = 0; i < seats.length; i++) {
  if (!results[i]) { log(`${seats[i][0]} died — one retry`); results[i] = await seats[i][1]() }
}
const [mail, g1, g2, doss] = results
log(`mail: ${mail ? `${mail.newMail} new, ${mail.cured} cured, ${mail.commit}` : 'DEAD'}`)
log(`gates value/parse: ${g1 ? g1.gates.map(g => `${g.n}:${g.verdict}`).join(' ') : 'DEAD'}`)
log(`gates kf/fourier: ${g2 ? g2.gates.map(g => `${g.n}:${g.verdict}`).join(' ') : 'DEAD'}`)
log(`dossier: ${doss ? `${doss.items.length} items, ${doss.commit}` : 'DEAD'}`)
return { mail, gatesValueParse: g1, gatesKfFourier: g2, dossier: doss }
