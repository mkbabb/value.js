SERVED MODEL: claude-opus-5[1m]

# KF.W6 — Glass Suffusion (demo) · EXECUTION RECORD

Track B (X·KF) · tranche X · opened on the owner's begin-word (2026-09-17, COHESION §0j).
Spec of record: `docs/tranches/X/keyframes/waves/KF-W6.md` (661 L, read WHOLE at this seat).
Row bank: `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (201 rows / 54 records).
Substrate: `/Users/mkbabb/Programming/keyframes.js`, branch `master` = `origin/master` = **`ae83da07`**.

---

## Open

**Date**: 2026-09-17 (seat clock 23:58 EDT → 2026-09-18 00:0x EDT).

### Preconditions, verified at the bytes AND in the ledger

| condition | source | measurement at this seat | verdict |
|---|---|---|---|
| **KF.W4 CLOSED** (the declared sequencing head) | ledger row `KF.W6 \| KF.W4` | ⟨`awk -F'\|' '/^\| KF\.W4/' LEDGER.md`⟩ → **CLOSED 2026-09-17 (honest-RED: G-KFW4-1 · G-KFW4-3 · G-KFW4-4 · G-KFW4-5 · G-KFW4-7)**, CHECK 2 CONFORMANT-HONEST-RED | **MET** |
| **G-KFW4-1 landed** — "no repair packet and no UNIT may open before G-KFW4-1 lands" (runbook §1.2) | keyframes.js `package.json` | ⟨`node -e "…package.json.scripts"`⟩ → `check = vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && npm run proof:structure` — **the vue-tsc gate is live and blocking** | **MET** |
| **KF.W0 §B-12 substrate settle** | COHESION §0j.C | ⟨`git -C ../keyframes.js rev-parse --abbrev-ref HEAD; rev-parse --short=8 HEAD; … origin/master`⟩ → `master` · `ae83da07` · `ae83da07` (**equal**); `git status --porcelain` → **2 untracked only** (the O-8/O-11 mail survivors), 0 tracked modifications | **MET** |
| **The KF.W4∥KF.W6 atomic-bundle half exists and applies** | orchestrator note · runbook §3.4 | ⟨`git -C ../keyframes.js apply --check …/execution/B/KF-W4-usability-bundle.patch`⟩ → **CLEAN** (exit 0); `git diff --stat -- scripts/observe/demo/usability.mjs` → **empty** (KF.W4 committed no byte of it, as its half declares); file is **410 L** | **MET** |
| **The bundle's stated dependency landed** | patch header "DEPENDENCY, stated" | ⟨`ls scripts/gates/register-census.mjs`⟩ → present; ⟨`grep -c wordWrapperSelector demo/styles/font-roles.json`⟩ → **1** | **MET** |
| **glass-ui pin** (KC-5's version-bump note; §0g.3 re-scope) | keyframes.js `package.json:78` | `"@mkbabb/glass-ui": "7.0.0"` EXACT; installed dist reports `version 7.0.0`. **§0g.3 rules the glass-8 atomic cut a VALUE.JS act that binds no sibling manifest; X·KF's producer pin is decided at KF-W0 §B-12's 7.0.0 pin.** KC-5's 8.0.0 arm is therefore **NOT available to this wave** — the 7.0.0 pin holds | **MET (ruled)** |
| **§Bounds files present** | §Bounds table | 25 named paths probed, **0 MISSING** (line counts in §Baseline · Drift) | **MET** |

**Preconditions NOT met, and their spec-declared fallbacks (not blockers — the spec pre-decides each):**

- **KF.W7 is `OPEN 2026-09-17` (opened by a sibling seat while this baseline was being banked; its row re-read at the ledger's bytes immediately before this edit — 6 units planned, all 15 of its gates RED, `execution/B/KF-W7.md`). No per-surface SWAP verdict has landed at this clock.** Therefore (a) no per-surface SWAP verdict exists, so every **KF-AV-28 GOVERNED SURFACE** row (`AnimationVisualizer.vue` KF-AV-11 · `PlaybackRibbon.vue` the `--color-progress` half · `SequenceScrubber.vue` KF-SCR-2 **and** `D·D-7` · `useRafLoop` KF-AV-27) is spent **CONDITIONALLY** — the seat STATES the verdict state before spending and accepts the throwaway explicitly (§Carry standing rider). (b) **N-2 is unruled**, so `KeyframeTimeline D-9`'s MAJOR is **not spent on an inert control** (§Sequencing 5). (c) **KAD-6 STANDS ALONE HERE** (W7's R-7 twin fold undeclined by absence). **Each governed-row unit (`.f` `.g` `.h` `.i` `.j`) RE-READS the KF.W7 ledger row and `execution/B/KF-W7.md` at its own open and STATES which verdicts, if any, have since landed BEFORE spending a governed cure** — the discharge is stated, never inferred (KF-W7 G15's discipline).
- **P-7 (the glass-ui 6.0.0 probe)** is unresolved at open. §Sequencing 5 makes it a **hard precondition of the W6-K cure set only** — it resolves inside unit `.j`, before that unit's first cure commit.
- **KF.W5's KF-AT-8/-9/-23** — KF.W5 is **CLOSED 2026-09-17 (honest-RED: G-FROMSTRING · G-ID · G-TAX)**. The seat re-measures those three at `.i`'s open; absent them, **S-5's answer is the written template-only ruling** (§Sequencing 5's own fallback), never a `splitText` adoption.

### E13 Step-0 — the four-path mail sweep

Swept read-only at this seat's own clock (**2026-09-17 23:58 EDT**), compared against **every row** of `docs/tranches/V/coordination/INBOX.md`, classification taken from each row's **status cell**, never from a bare `grep -i unread` (X.P.W0 CHECK 1 **D-1**); `INBOX.md` self-excluded (SELF-COUNT law).

⟨cmd⟩ `/usr/bin/find <each path> -maxdepth 1 -type f -name '*.md' -newermt "2026-09-17 22:00"` →
only `docs/tranches/V/coordination/INBOX.md`@23:36 (**self**, excluded). Delta against the 23:33 X.P.W2-relaunch sweep = **∅**.

1. `docs/tranches/V/` (11 `.md`) + `V/coordination/` (18 entries) — the unrowed root files are **authority documents, not mail**; the four unrowed `value-inbox-2026-07-20-*.md` are **OUR OWN OUTBOUND** (held for the owner's bridge), and `CONSTELLATION.md` is a register, not a letter.
2. `../glass-ui/docs/tranches/BK/coordination/` — **BK re-confirmed the newest tranche dir** ⟨`ls -ltd ../glass-ui/docs/tranches/*/ | head -5`⟩ → `BK/`@2026-09-17 20:15 > `BJ/`@08-03 > `BI/`@07-28. 7 files: the three 2026-09-17 letters rowed **I-32 · I-33 · I-34** (routing cells: the X formation mail seat / X-W0.j / X-EXT-1..6 — **none routes to KF.W6**), the 08-29 ACK rowed **I-30**, `valuejs-outbound-2026-08-28-…` = **O-20** (ours), the two `ATLAS-*` files atlas→glass.
3. `../keyframes.js/docs/tranches/V/coordination/` — 12 entries + `vnext/`; `VALUEJS-INBOUND-*` are **our own outbound** (the I-26 delivery cure; its two survivors are the tree's only untracked rows), `ATLAS-/GLASS-/SPEEDTEST-INBOUND-*` are keyframes-addressed.
4. `../sci-report/atlas/docs/tranches/P/coordination/` — 28 files, all 2026-07-15…08-03; the value.js-addressed pair is rowed, the remainder are atlas's own P-lane records.

**Result: 0 unrowed · 0 new `I-n` minted here · 0 UNREAD addressed to KF.W6's scope.**
KF.W6's own mail obligation is **OUTBOUND** and is unit `.b`'s act: the **ONE** BH communiqué at the named path of record (`docs/tranches/X/keyframes/relay/KF-W6-BH-COMMUNIQUE.md`), mirrored to the active glass tranche's inbox (`BK`) per the owner edict 2026-07-12 and rowed here as a new `O-n`. Nothing is owed at open.

---

## Baseline

**Read-only, double-run, at `origin/master ae83da07`.** The spec's anchors were taken at `81a56990`; **D-19 requires re-verification at wave-open**, and this section is that re-verification. Status at authoring was **ALL FIFTEEN RED**; status measured here is **ALL FIFTEEN RED**. **greenBeforeCure = ∅** (R.2: no gate is green before its cure).

| gate | measured at `ae83da07` | verdict |
|---|---|---|
| **G-W6-1** phantom-token extinction | `--dock-margin:` definitions → **0**; `var(--dock-margin)` → **10** (9 live `layout.css:80,92,117,135,161,166,204` + `TransportDock.vue:389,395`; 1 prose `TransportDock.vue:379`). `--shadow-glass` → **1 consumer** (`App.skeleton.vue:64`) / **0 defs**. `--dock-label-padding-inline` → **1 consumer** (`ChromeDock.vue:383`) / **0 defs** | **RED** (reproduces) |
| **G-W6-2** KfPillTabs extinction + preservation | `git grep -l KfPillTabs -- demo/ test/` → **11 files**. `App.vue:176` → `provide(TABS_EXTERNALLY_MANAGED_KEY, true);` unconditional. Installed glass 7.0.0 `./tabs` → `{"types":"./dist/tabs.d.ts","import":"./dist/tabs.js"}`, and `dist/tabs.js`'s export list is **`export { W as SegmentedTabs }`** — exactly one symbol; `useTabRovingFocus-Dh4yBGxq.js` is a **private hashed chunk**; root-barrel `grep -c useTabRovingFocus dist/index.js` → **0**. **W6-AUTH-1 re-verified at the frontier, third independent reading.** `KfPillTabs.test.ts` → **7 `it(` cases**, import at `:28` via the `@components` alias root | **RED** |
| **G-W6-3** shim + dead-barrel deletion | Line counts: `composables/useKfPillTabs.ts` **4** · `transport/index.ts` **14** · `instrument/index.ts` **27** · `useRafLoop.ts` **19** · `composables/useAnimationGroupPlayback.ts` **1**. Import graph: **A-1** SET = {`test/demo/state/no-shadow-playback-authority.test.ts:21`} ⟨exact specifier `"../../../demo/components/instrument/transport/composables/useAnimationGroupPlayback"`⟩ · **A-2** SET = {`ChannelControls.vue:230`, `import type { KfPillTabOption }`} · **A-5** SET = {`instrument/index.ts:24` `export * from "./transport"`} · **A-6** SET = **∅** · **A-7** SET = {`AnimationControlsGroup/useAnimationProgress.ts:5`, `AnimationVisualizer.vue:51` (alias root)} · **A-8** SET = {`ChannelControls.vue:242`, used `:402`} | **RED** (all nine censuses reproduce) |
| **G-W6-4** phantom utilities | `btn-interactive` → **8 sites / 6 files**, enumerated: `RibbonBar.vue:135` (`RIBBON_BUTTON_CLASS`) · `PlaybackRibbon.vue:55` · `CubeScene.vue:188` · `CubeScene.vue:193` · **`SequenceTarget.vue:31`** · `SpringPhysicsFacet.vue:74` · `SpringPhysicsFacet.vue:105` · `SpringScene.vue:168`. Rules: **0** in demo, **0** in the glass dist css. `.tap-floor` → **2 hits / 1 file** (`design-idioms.css:81` its own comment + `:82` its selector) → **0 adopters** | **RED** |
| **G-W6-5** same-name shadow census | `--rainbow-*` → 7 demo defs `design-idioms.css:15-21` (hsl) beside the glass producer's own (`dist/glass-ui.css`); `--amiga-red: var(--rainbow-red)` `:24`; `.text-gold` class `design-idioms.css:88` + 4 consumer sites; `--scale-hover: 1.08` `design-idioms.css:41` with its own *"mirrors glass-ui's"* comment `:40` | **RED** |
| **G-W6-6** focus contract | `focus-ring` → **11 sites / 7 files**; `field-control` → **0 / 0** (declared NEGATIVE witness); `aria-selected` → **5 sites / 2 files** — `KfPillTabs.vue:24` the only live binding, the other four `useTabStripScroll.ts:25,48,50,53` comments/selector strings, **both files inside the S-1 delete family** | **RED** |
| **G-W6-7** target size | `touch-hit-area` → **0 / 0** (declared NEGATIVE witness — KF-SST-30's producer utility cannot expand a target); `tap-floor` → **2 / 1**, zero adopters | **RED** |
| **G-W6-8** typography-cure collision guard | `max-h-24` → **exactly one site repo-wide**, `TimelineHoverPreview.vue:20`, on the same element as the swap target: `class="font-mono text-admin-label text-muted-foreground max-h-24 overflow-y-auto w-full" data-register="code"`. `tracking-normal` → **0**. `normal-case` → **5 sites** (`MbabbMenu.vue:5,:64` · `SharePopover.vue:19` · `ChannelOptions.vue:254,:265`) — **every shipped `normal-case` is unpaired** | **RED** |
| **G-W6-9** glass-first suffusion ledger | **ZERO MOUNTS, six** — `Textarea` **0** · `NumberField` **0** · glass `Progress` **0** · glass `Skeleton` **0** · `Surface`/`PaperBackdrop` **0** · `./canvas` (`useCanvas2D`/`resolveCanvasColor`) **0**. **CONSUMED, three** — `FadingScroll` **2 live mounts on both axes** (`EasingTarget.vue:42` `axis="x"`, `:76` `axis="y"`, import `@mkbabb/glass-ui/fading-scroll` at `:137`) + 1 PROSE site (`ChannelControls.vue:51`, CC-N-2) · `Card` **24 mounts** · `ToggleGroup` **mounted** (`EasingTarget.vue:50`, `ToggleGroupItem` `:56`, import `@mkbabb/glass-ui/toggle-group` `:139`). **No S-7/S-9/S-10 discharge list is written; no decline is written; `SPF-10` undecided** | **RED** |
| **G-W6-10** prose-with-code truth | `SpringSidebar` → **12 comment sites / 8 files** (`KeyframesEditor.vue` 2 · `KfPillTabs.vue` 1 · `SpringHeatmap.vue` 1 · `SpringPhysicsFacet.vue` 1 · `SpringScene.vue` 2 · `StartingStyleTarget.vue` 3 · `useSpringHotPath.ts` 1 · `useSpringLinearStops.ts` 1) and `git ls-tree -r --name-only HEAD \| grep -c SpringSidebar` → **0** — every one names a file that does not exist. `ONE WRITER` (article-less, the PASS-6 D-P6-5 spelling) → **5 sites / 3 files** (`useSceneMachineRouterBinding.ts:10,:86` · `ChannelControls.vue:343` · `useSelectedControlSurface.ts:42,:88`). `contractAnim` → **4 hits, all comments** | **RED** |
| **G-W6-11** canvas/theme colour discipline | `resolveCanvasColor` → **0 / 0** · `onFlipSettled` → **0 / 0** (both declared NEGATIVE — the producer surface ships and is unconsumed) · `fillStyle` → **7 sites / 3 files** | **RED** |
| **G-W6-12** aurora presence + config coherence | `initStrategy` → **1 site**, `HeroAurora.vue:27` `:runtime-options="{ initStrategy: 'eager' }"`. `light: true` → **1 site**, `HeroAurora.vue:75` `interactivity: { light: true }`. `.aurora-root` → **0**. `--aurora-opacity-ceiling` → **2 hits, BOTH comments** naming a test file (see Drift) | **RED** |
| **G-W6-13** phantom-token audit completion | distinct `--custom-property` names referenced across `demo/**/*.css` + `demo/**/*.vue` → **124**; of those, `--kf-*` prefixed → **0**. Every demo custom property is unprefixed and **undispositioned**; no disposition table exists | **RED** |
| **G-W6-14** producer relay dispatch | ⟨`ls docs/tranches/X/keyframes/`⟩ → `W0 · artefacts · carry · conformance · evidence · registries · waves` — **no `relay/` directory**; `docs/tranches/X/keyframes/relay/KF-W6-BH-COMMUNIQUE.md` **does not exist**. No communiqué at the glass BK inbox either | **RED** |
| **G-W6-15** the EditorHeader atomic delete | `git grep -n EditorHeader -- demo/ test/ src/ scripts/` → **2 hits**: `shell/index.ts:2` (the ONE live importer, the barrel re-export) + `layout.css:15` (prose/token context). `<EditorHeader` → **0 mounts**. `header-items-max-w` → **3 hits**: `EditorHeader.vue:92` (sole consumer), `:90` (its comment), `layout.css:15` (the declaration that BECOMES the orphan). Barrel = **4 exports** {EditorShell, EditorHeader, EditorStartScreen, SharePopover} **over an 8-SFC directory** | **RED** |

### Drift against the spec's `81a56990` anchors (D-19 — recorded, not adjudicated)

The frontier moved from `81a56990` to `ae83da07` (KF.W0 §B-12 settle + the W1/W2/W4/W5/W9 landings). **No gate's verdict moved and no census SET changed.** Coordinate drift, measured per file:

- **ZERO drift on the load-bearing EH family** (the RULINGS-5 R5-1 restoration): `EditorShell.vue:20` `<SharePopover />` · `:36` `class="aspect-square w-8 scale-on-hover"` (the shortcuts `Button`'s defeated axis) · `:44` `<DarkModeToggle` · **`:45` `title="Toggle dark mode"` (EH-4)** · **`:46` `class="aspect-square w-8 scale-on-hover"` (EH-5)** · `:47` close. `MbabbMenu.vue:20` title (EH-4's second spend site) · `:21` `class="aspect-square w-5"` (recorded beside, never a second EH-5 spend). `EditorHeader.vue:25`/`:26` the FORK's copy (moot-on-delete). **Every anchor re-resolves exactly.**
- `ChannelControls.vue` **456 → 460 L**; `useTabStripScroll` import `:242` UNCHANGED, its call `:398 → :402`; `ONE WRITER` `:339 → :343`; `:230` `KfPillTabOption` UNCHANGED; `:51` FadingScroll prose UNCHANGED.
- `EditorShell.vue` **259 → 264 L** (anchors unmoved, above).
- `animationDescriptions.ts` **129 → 114 L** — KF-ET-35's `:108`/`:116`/`:128-129` coordinates **MUST be re-resolved by unit `.k`** before the carve is spent.
- `ChromeDock.vue` `--dock-label-padding-inline` **`:381` → `:383`**.
- `SpringScene.vue` `btn-interactive` **`:167` → `:168`**.
- `HeroAurora.vue` `light: true` **`:70` → `:75`**; `initStrategy` **`:28` → `:27`**.
- `TimelineHoverPreview.vue:20` now carries an added `data-register="code"` attribute; the `max-h-24` class string and G-W6-8's witness are intact.
- `font-roles.json` now carries `wordWrapperSelector` (KF.W4 `.d`) — **73 L**.
- `test/demo/instrument/KfPillTabs.test.ts` = **201 L**, 7 cases (the spec gave no count).
- **NEW CONSTRAINT, absent from the spec because it postdates `81a56990`**: `test/demo/instrument/aurora-opacity-ceiling.test.ts` **exists** and, per `HeroAurora.vue:17`/`:36`'s own comments, *"asserts this literal"* — the two `--aurora-opacity-ceiling` hits in `demo/` are those comments. **W6-K's presence cure (KF-HA-2) therefore REDs a live test unless the test moves with it, in the same commit.** Booked as unit `.j`'s lock; it is not a gate this wave minted and no `proof:*` mark is involved.
- `G-W6-2`'s 11-file `KfPillTabs` set is unchanged in COUNT; its membership now includes `test/demo/instrument/kf-toolbar-keyboard.test.ts` and `transport-play-actuation.test.ts`. **Unit `.c` re-enumerates the set before the delete** and states any member the S-1 family does not reach.

---

## Unit plan

**Twelve units, eleven ordered groups, peak concurrency 2** (owner cap 4 workflows; runbook §5.1 — *"intra-wave concurrency yields first"*). The spec declares **no** parallel structure: §Sequencing is nine hard orderings plus twelve commit families, and the §Bounds file graph is dense enough that only one pair (instrument-side ∥ scene-side) is genuinely path-disjoint. That pair is group 6; everything else is serial by measurement, not by caution.

**Model tiering (M-12 TRI-FOLD, runbook §5.1)**: Opus solo for the mechanical/census/delete seats; Fable for the four seats whose product is a *decision register* the spec asks to be **written** — the BH communiqué and the phase-1 rulings (`.b`), the per-property disposition table (`.e`), the one-register-per-role pass (`.h`), the evaluate-not-swap ledger with its discharge lists (`.i`) — and a fresh Fable for the closing measurement seat (`.l`).

**Binding on every unit**: line 1 of any created file = `SERVED MODEL: <model id>`; WRITE-THEN-MEASURE with every published count double-run; quote-by-command; pathspec commits ON THE COMMIT ITSELF; E-3 (dated addenda beside, never patches — the 14 E-3 addenda `(i)`…`(xiv)` this spec owes at the bank are **flagged upward, never written by an executing seat**); no `proof:*` mark is minted (house law, owner 2026-06-02); `scripts/dev/dev.sh` NEVER touched; glass-ui source READ-ONLY always.

### Group 1 — `.a` (alone; THE FIRST COMMIT)

- **id** `KF.W6.a` · **model** opus
- **sections** §Bounds `:119-121` (the `usability.mjs` ONE POSTURE, DH-4) · §Sequencing → KF.W4 cross-edge item (6) `:568` · runbook §3.4 row `KF.W4 ∥ KF.W6` · §Bounds census A-11(4) `:156`
- **writable** `keyframes.js: scripts/observe/demo/usability.mjs` · `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** — (the bundle lock; it turns no `G-W6-n`)
- **locks** THE KF.W4∥KF.W6 ATOMIC BUNDLE — *"written ONLY inside the atomic bundle (one commit, both seats); never unilaterally, never concurrently"*. **This is the wave's FIRST commit and the ONLY commit any unit of this wave may make to that path.**

### Group 2 — `.b` (alone; hard blocker on every interim mitigation)

- **id** `KF.W6.b` · **model** fable
- **sections** §Gates `G-W6-14` `:546` · §Sequencing hard orders 2 · 3 · 5 · 7 · 8 `:551-559` · §Sequencing → glass-ui BH relay `:575` · §Carry W6-AUTH-1 `:235` · W6-D `.tap-floor` `:301`
- **writable** `value.js: docs/tranches/X/keyframes/relay/KF-W6-BH-COMMUNIQUE.md` (new) · `glass-ui: docs/tranches/BK/coordination/valuejs-outbound-2026-09-18-kfw6-bh-relay.md` (**MAIL DELIVERY ONLY** — the mirror the owner edict 2026-07-12 requires; not one byte of producer source) · `value.js: docs/tranches/V/coordination/INBOX.md` (the new `O-n` row + a dated line) · `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** G-W6-14
- **locks** §Sequencing 7 — **the relay is dispatched BEFORE any interim demo-side mitigation of a producer seam is committed**, so every later unit's interim is recorded as interim; the communiqué is **ONE** letter at the named path (L-19's admitted witness form) with the cure-shape constraints intact (**LIFO/FIFO asymmetry · getter-not-selector · `default: undefined` · `touch-hit-area`'s one-utility-two-behaviours**); §Sequencing 8's one-decision registers are ruled here, not re-opened downstream.

### Group 3 — `.c` (alone)

- **id** `KF.W6.c` · **model** opus
- **sections** §Carry W6-A `:231-261` · W6-B `:262-269` · W6-C's ChannelControls/KfPillTabs limbs `:270-291` · §Bounds censuses A-1…A-10 `:126-145` · §Sequencing hard order 4 `:553` + the S-1 and shim commit families `:562`
- **writable** (all `keyframes.js`) `demo/components/instrument/transport/KfPillTabs.vue` (DEL) · `.../transport/KfPillTabs/useKfPillTabs.ts` (DEL) · `.../transport/composables/useKfPillTabs.ts` (DEL) · `.../transport/composables/useAnimationGroupPlayback.ts` (DEL) · `.../transport/composables/useRafLoop.ts` · `.../transport/composables/useScrollFade.ts` · `.../transport/index.ts` (DEL) · `demo/components/instrument/index.ts` (DEL) · `.../transport/channel-controls/composables/useTabStripScroll.ts` (DEL) · `.../transport/channel-controls/ChannelControls.vue` · `.../transport/controls-pane/ControlsPaneWrapper.vue` + `.css` · `.../transport/AnimationControlsGroup.vue` · `test/demo/state/no-shadow-playback-authority.test.ts` · `test/demo/instrument/KfPillTabs.test.ts` · `demo/app/App.vue` · `demo/app/dock/ChromeDock.vue` · `demo/components/instrument/shell/EditorShell.vue` · `demo/components/instrument/keyframes/KeyframesStringControls.vue` · `demo/scenes/cube/CubeScene.vue` · `demo/styles/tab-idiom.css` · `demo/styles/design-idioms.css` · `demo/styles/font-roles.json` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** G-W6-2 · G-W6-3
- **locks** **S-1 DELETE FAMILY, ONE COMMIT** (W6-A whole + `font-roles.json`'s `filing-tab*` empty-set selectors + `tab-idiom.css`'s 48-line corpse + **`design-idioms.css:7`'s `@import "./tab-idiom.css";` removal** + the `extraTabs` 4-ancestor `#tabs-trigger` chain END-TO-END) · **`useAnimationGroupPlayback` shim delete + `no-shadow-playback-authority.test.ts:21` repoint ONE ATOMIC COMMIT (R3-1.2)** · `useRafLoop`'s inline arm carries **BOTH** A-7 consumers in its own commit · K2/KF-CE-41 delete+comment ONE commit · **LOCK (b): the T.G9 warm re-homes onto ChromeDock's control `<Select>` BEFORE the branch dies** · **KPT-SUP-4: the 7-case gate MIGRATES, never deletes** · `:230` repoints at `../KfPillTabs/useKfPillTabs`, **NOT** the `.vue` · `tab-idiom.css`'s whole-file delete stays **unavailable until C-14 is decided** (its terminal block `:75-79` is C-14's subject) — strip the corpse, keep the file.

### Group 4 — `.d` (alone)

- **id** `KF.W6.d` · **model** opus
- **sections** §Carry W6-L `:459-485` · §Bounds census A-11 `:146-163` · §Gates `G-W6-15` `:545` · §Sequencing commit families `:562` + the → KF.W8 cross-edge `:572`
- **writable** (all `keyframes.js`) `demo/components/instrument/shell/EditorHeader.vue` (**DELETE**) · `demo/components/instrument/shell/index.ts` · `demo/styles/layout.css` · `demo/components/instrument/shell/EditorShell.vue` · `demo/app/dock/ChromeDock.vue` · `demo/app/dock/MbabbMenu.vue` · `demo/app/App.vue` · `demo/components/instrument/transport/TransportDock.vue` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** G-W6-15 (+ G-W6-1's dock arm)
- **locks** **R4-1 ONE ATOMIC COMMIT, three limbs**: delete `EditorHeader.vue` **+** strike barrel line `shell/index.ts:2` **+** strike `layout.css:15`'s `--header-items-max-w`; the barrel edit is lawful **here and only here** (R4-5(c) — KF.W6 is its ONE write owner; KF.W8 `G7` MEASURES and performs no barrel edit) · **EH-4 / EH-5 / EH-8 are LIVE rows at their OWN anchors (LAW F(3))** — never disposed by co-id, host record or fold carrier · `C-22`'s `D-27` and `RR-2 M7` **stay NO-WAVE-OWNER and no green here discharges them** · the **MbabbMenu motion** family (MM-4 + MM-15 + MM-30 + MM-12 + MM-8/-9/-29/-31) with the MUST-CARRY rider travelling wherever ChromeDock M-4 lands · `CopyButton.vue` **does not move** (R-13: the MOVE is KF.W8's).

### Group 5 — `.e` (alone)

- **id** `KF.W6.e` · **model** fable
- **sections** §Carry W6-D `:292-320` · W6-C's KF-AX-11/-12/-31 z-contract row `:290` · §Gates `G-W6-1` `:531` · `G-W6-4` `:534` · `G-W6-5` `:535` · `G-W6-13` `:543`
- **writable** (all `keyframes.js`) `demo/styles/layout.css` · `demo/styles/design-idioms.css` · `demo/styles/style.css` · `demo/styles/playback-idiom.css` · `demo/app/App.skeleton.vue` · `demo/components/instrument/transport/TransportDock.vue` · `.../transport/controls-pane/RibbonBar.vue` · `.../transport/controls-pane/ControlsPaneWrapper.vue` + `.css` · `.../transport/components/DemoGlobalChrome.vue` · `demo/components/playback/PlaybackRibbon.vue` · `demo/components/CopyButton.vue` · `demo/components/instrument/timeline/components/TimelineHoverPreview.vue` · `demo/components/instrument/keyframes/KeyframesEditor.vue` · `.../keyframes/CSSCodeEditor.vue` · `demo/components/instrument/shell/KeyboardShortcutsModal.vue` · `demo/scenes/cube/CubeScene.vue` · `demo/scenes/cube/CubeAxisLines.vue` · `demo/scenes/sequence/SequenceTarget.vue` (**line `:31` ALONE**) · `demo/scenes/sequence/SequencePlayhead.vue` · `demo/scenes/spring/SpringPhysicsFacet.vue` · `demo/scenes/spring/SpringScene.vue` · `demo/scenes/square/SquareScene.vue` + `.css` · `demo/DESIGN.md` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** G-W6-1 · G-W6-4 · G-W6-5 · G-W6-13
- **locks** **MISSED-H FOLDS FIRST** — the demo deliberately shadows glass's namespace (`design-idioms.css:9-11` states the policy); **the product is a per-property DISPOSITION TABLE (RETAINED-BY-POLICY / PREFIXED / RETIRED / NO-SURFACE), never a blanket rename** · **KF-APP-6 = define-it-or-delete-the-chain; a per-site fallback IS the masking the row convicts** (identity-guard: cite the bank, do not re-derive — fourth refiling) · `--shadow-glass-*` bridge spelling does not emit; any interim reads `--glass-shadow-quiet`/`-resting` · the **8-site `.btn-interactive` sweep is WHOLE** — all eight sites are in §Bounds and *a green claimed over 7 of 8 is the defect the bound exists to catch* · `SequenceTarget.vue` is granted for **`:31` ALONE** · the z-contract recut (KF-AX-11 + -12 + -31) is **ONE motion**; `scripts/lib/demo-driver.mjs:69`'s mirror is **DECLARED, never cured** (out of §Bounds) · **SPF-32: NO RENAME SPEND** · the audit **includes script-side constants** (KF-CB-27).

### Group 6 — `.f` ∥ `.g` (the wave's one genuinely path-disjoint pair)

- **id** `KF.W6.f` · **model** opus · **sections** §Carry W6-F `:329-341` · W6-E `:321-328` · W6-H's instrument rows `:369-395` · §Gates `G-W6-6` `:536` · `G-W6-7` `:537`
  **writable** (all `keyframes.js`) `demo/components/instrument/keyframes/KeyframeCard.vue` · `.../keyframes/components/KeyframeCardList.vue` · `.../keyframes/KeyframesEditor.vue` · `.../keyframes/CSSCodeEditor.vue` · `.../keyframes/components/KeyframesAddDialog.vue` · `demo/components/instrument/timeline/KeyframeTimeline.vue` · `.../timeline/TimelineCaret.vue` · `.../timeline/components/TimelineTrack.vue` · `.../timeline/components/TimelineHoverPreview.vue` · `.../timeline/CSSPasteDialog.vue` · `demo/components/instrument/shell/SharePopover.vue` · `demo/components/CopyButton.vue` · `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` · `demo/styles/design-idioms.css` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
  **locks** **KF-KE-30's `.focus-ring` collision RESOLVES BEFORE G-W6-6 ASSERTS** (heads family F) · **K-9 INVERSION: `.focus-ring` lands FIRST and discharges D·M-5 immediately — it never waits on the NumberField evaluation; the primitive swap is second** · `aria-selected` is **NOT** a permitted vehicle · `field-control` is never erased (root-styling law) · **KF-SST-30: `touch-hit-area` cannot expand a target** — it annotates KF-CO-24 and CubeScene MISS-3 rather than rewriting them (E-3) · no *"44px house practice"* cite exists · D-7's SS-13 row is the **single** contrast-measurement site (TimelineTrack D-5 rides it) · KF-SKEL-22: every figure RE-DERIVED at use.
- **id** `KF.W6.g` · **model** opus · **sections** §Carry W6-H's scene rows `:369-395` · §Gates `G-W6-11` `:541`
  **writable** (all `keyframes.js`, `demo/scenes/**` ONLY) `demo/scenes/amiga/AmigaScene.vue` + `utils.ts` · `demo/scenes/spring/SpringHeatmap.vue` · `.../spring/SpringPhysicsFacet.vue` · `.../spring/SpringScene.vue` · `.../spring/SpringTrace.vue` · `.../spring/StartingStyleTarget.vue` · `demo/scenes/cube/CubeScene.vue` · `.../cube/CubeTarget.vue` + `CubeTarget.css` · `.../cube/CubeAxisLines.vue` · `.../cube/matrix-editor/MatrixEditor.vue` · `demo/scenes/sequence/SequenceScrubber.vue` · `.../sequence/SequenceScene.vue` · `.../sequence/SequenceTarget.css` · `.../sequence/SequencePlayhead.vue` · `demo/scenes/easing/EasingTarget.vue` + `.css` · `.../easing/EasingSidebar.vue` · `.../easing/EasingScene.vue` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
  **locks** **ONE canvas-colour-discipline decision** (SpringHeatmap D-M2 + D-M3 + C-M-1 + AmigaScene's re-bake, one commit family) · **ESCALATION ARMED: D-M3 → BLOCKER iff the SS-13 probe lands branch (a)** · `CubeTarget #8`/`#9` are decided in the **SAME motion as `#58`**; their rendered witnesses are KF.W9's and **neither end closes either id alone** · bodies resolve **at the bank by id, never re-derived**; an unquotable r1 body is **flagged upward, never silently re-derived and never dropped** · **KF-AV-28 GUARD on `SequenceScrubber.vue`** — state the (absent) SWAP verdict before spending `D·D-7` · the AmigaScene packet is filed as **design defects, not WCAG 1.4.11**.
  **Disjointness receipt**: `.f` writes only `demo/components/**` + `demo/styles/design-idioms.css`; `.g` writes only `demo/scenes/**`. **Zero shared paths.**

### Group 7 — `.h` (alone)

- **id** `KF.W6.h` · **model** fable · **sections** §Carry W6-G `:342-368` · §Gates `G-W6-8` `:538`
- **writable** (all `keyframes.js`) `demo/components/instrument/timeline/KeyframeTimeline.vue` · `.../timeline/TimelineCaret.vue` · `.../timeline/components/TimelineTrack.vue` · `.../timeline/components/TimelineHoverPreview.vue` · `.../timeline/CSSPasteDialog.vue` · `demo/components/instrument/shell/SharePopover.vue` · `.../shell/AnimatedText.vue` · `.../shell/EditorStartScreen.vue` · `.../shell/KeyboardShortcutsModal.vue` · `demo/app/dock/MbabbMenu.vue` · `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` · `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` · `.../keyframes/KeyframesEditor.vue` · `demo/scenes/sequence/SequenceScrubber.vue` · `demo/scenes/square/SquareInstrument.vue` · `demo/scenes/spring/SpringHeatmap.vue` · `demo/styles/style.css` · `demo/styles/font-roles.json` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** G-W6-8
- **locks** **MISSED-3, THE CURE-COLLISION LAW (binds this wave AND KF.W7)**: a `text-mono-small` swap **resizes or drops `max-h-24` IN THE SAME COMMIT**; **`normal-case` ALWAYS pairs `tracking-normal` (MM-29)** at all five shipped sites; **no wave ships the regression** · **ONE register decision per role**, decided once and never twice (KF-SS-9) · **KF-AT-22's cure-discrimination rider, verbatim: `:root`, NEVER `@theme`** · SP-4 + SP-7 are **ONE edit** · KF-EST-14 is a SEQUENCING CONSTRAINT — any sheet consolidation re-keys the override to `.hero-deck` in the same motion · **KF.W7 rules N-2's wire-or-delete BEFORE D-9's MAJOR is spent** (unruled at open ⇒ not spent) · KF-AV-28 guard on `SequenceScrubber.vue` (KF-SCR-2).

### Group 8 — `.i` (alone)

- **id** `KF.W6.i` · **model** fable · **sections** §Carry W6-I `:396-424` · §Gates `G-W6-9` `:539` · §Sequencing hard order 8 `:559`
- **writable** (all `keyframes.js`) `demo/components/instrument/shell/AnimatedText.vue` · `.../shell/EditorStartScreen.vue` · `.../shell/KeyboardShortcutsModal.vue` · `.../shell/SharePopover.vue` · `demo/app/App.skeleton.vue` · `demo/app/dock/MbabbMenu.vue` · `demo/components/CopyButton.vue` · `demo/components/instrument/timeline/CSSPasteDialog.vue` · `.../timeline/components/TimelineTrack.vue` · `.../timeline/TimelineCaret.vue` · `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` · `.../keyframes/KeyframesEditor.vue` · `.../keyframes/KeyframeCard.vue` · `.../keyframes/CSSCodeEditor.vue` · `demo/components/instrument/transport/channel-controls/LayerConfigPanel.vue` · `demo/scenes/spring/SpringPhysicsFacet.vue` · `.../spring/StartingStyleTarget.vue` · `demo/scenes/easing/EasingTarget.vue` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** G-W6-9
- **locks** **FAMILY LAW, verbatim: kf-PlaybackRibbon consumes the real `Slider` and carries BLOCKERs anyway — "swap onto the primitive" is NEVER SUFFICIENT as a cure. EVALUATE-NOT-SWAP is the default verdict shape** · **S-5's FIVE-CONSTRAINT SET is MUST-CARRY** and KF-AT-8 is its NAMED BLOCKER (KF.W5's library cure PRECEDES adoption; absent it the answer is the **written template-only ruling**) · **S-6 CURE INVERSION**: delegate to the demo's own **stage-card register**, never the bare glass `Skeleton`, and **never cure radius against glass's detuned shipped values** (KF-SKEL-4) · **S-7's S-2 constraint**: a real `<button>` stays in the DOM · **DISCHARGE DISCIPLINE (gate condition): the seat STATES which rows the S-7/S-9/S-10 swaps discharge BEFORE spending any cure** · **ONE FadingScroll decision for the wave, authored ON the shipped `EasingTarget.vue:42`/`:76` two-axis precedent** — authoring it greenfield is the defect the struck quantifier would have caused · `SPF-10` and TimelineTrack `m-17` enter the ledger as **named VARIANT-level members**, decided or declined in writing; a demo-side patch of the producer's merger is an SS-6 defect · **KF-ET-10's cure lands producer-side or as the ToggleGroup reshell — a scene-side repair order is UNEXECUTABLE** · KAD-6 **STANDS ALONE HERE** (W7's twin fold absent).

### Group 9 — `.j` (alone)

- **id** `KF.W6.j` · **model** opus · **sections** §Carry W6-J `:425-439` · W6-K `:440-458` · §Gates `G-W6-12` `:542` · §Sequencing hard order 5 `:555` + the → KF.W9 cross-edge `:573`
- **writable** (all `keyframes.js`) `demo/components/instrument/shell/HeroAurora.vue` · `.../shell/TypingDots.vue` · `.../shell/SharePopover.vue` · `demo/app/App.vue` · `demo/app/App.skeleton.vue` · `demo/components/CopyButton.vue` · `demo/components/instrument/keyframes/KeyframesEditor.vue` · `.../keyframes/components/KeyframesAddDialog.vue` · `demo/components/instrument/timeline/KeyframeTimeline.vue` · `demo/scenes/cube/CubeAxisLines.vue` · `demo/scenes/cube/CubeTarget.css` · `demo/scenes/spring/SpringHeatmap.vue` · `demo/scenes/square/SquareScene.css` · `test/demo/instrument/aurora-opacity-ceiling.test.ts` (**only if and when the presence cure moves the literal it asserts — in the SAME commit**) — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** G-W6-12
- **locks** **P-7's 6.0.0 probe RESOLVES BEFORE THIS CURE SET EXECUTES** (one install; note the legacy-peer-deps constraint) · **ONE aurora config motion** (KF-HA-1/-2/-3/-8/-10/-13/-15/-16/-17/-20 + KF-APP-15/-16/-48 + the prose limb) · **KAD-11 is a PAIR** with the `KeyframesEditor.vue:254-258` twin, and it carries **`G-KFW9-6`'s unification constraint VERBATIM**: *"no unification lands that snaps a progress animation to a rest state its own banked defect makes idle-indistinguishable; group arm via `g.respectReducedMotion`, standalone arms via options bags; the KAD-11 pair lock holds"* — **this wave holds the TOKENIZATION half; neither end may record `G-KFW9-6` or `KAD-11` as closed on its own** · **KF-HA-10 is an OWNER RULING QUESTION, not a fix order** — split row with KF.W9's frame-cost measurement; a ruling to KEEP is spent only with W9's figure in hand and the seat states which it had · **KF-APP-48 ARITHMETIC LOCK: EXACTLY 7 of 12 keys change** — the "clobbers 12" claim is killed three times, do not re-derive · **EH-9 ≡ SP-10**: the bare deletion ships a snapping `hover:opacity-50` — the recede-affordance books WITH it, ONE SharePopover motion batch · **KF-TD-6 may NEVER DELETE THE CSS FLOOR** · **FRONTIER LOCK (this record's Drift §): `test/demo/instrument/aurora-opacity-ceiling.test.ts` asserts the ceiling literal — a presence cure lands WITH its test in the same commit, never by skipping or loosening the test.**

### Group 10 — `.k` (alone)

- **id** `KF.W6.k` · **model** opus · **sections** §Carry W6-M `:486-501` · W6-N `:502-520`
- **writable** (all `keyframes.js`) `demo/app/App.skeleton.vue` · `demo/components/instrument/shell/EditorShell.vue` · `.../shell/EditorStartScreen.vue` · `.../shell/KeyboardShortcutsModal.vue` · `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` · `.../keyframes/components/KeyframeCardList.vue` · `.../keyframes/KeyframeCard.vue` · `.../keyframes/CSSCodeEditor.vue` · `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` · `.../transport/AnimationControlsGroup.vue` · `.../transport/controls-pane/ControlsPaneWrapper.vue` + `.css` · `demo/components/instrument/timeline/KeyframeTimeline.vue` · `demo/scenes/spring/SpringScene.vue` · `.../spring/SpringTrace.vue` · `.../spring/StartingStyleTarget.vue` · `demo/scenes/easing/EasingTarget.vue` + `.css` · `.../easing/EasingScene.vue` · `demo/scenes/sequence/SequenceScene.vue` · `demo/utils/reference-data/animationDescriptions.ts` · `demo/styles/layout.css` · `demo/styles/font-roles.json` — plus `value.js: docs/tranches/X/execution/B/KF-W6.md`
- **gates** — (feeds `G-W6-9`'s KSM composition and `G-W6-10`'s figure; turns neither alone)
- **locks** **KF-SKEL-1: a PERSISTENT announcer at/above `.scene-host` — putting text in a live region born inside the suspension is INSUFFICIENT** (in-tree idiom `CopyButton.vue:15`) · **KF-SS-31's `SpringSidebar` = 12 sites / 8 files re-pin and `G-W6-10`'s witness are ONE FIGURE (PROPAGATION LOCK)** — a correction of either updates both in the same motion; **any of the 8 files outside §Bounds is DECLARED, never silently cured** · **KF-SKEL-9 is decided AT THE EMITTER** (`AnimationControlsGroup` / `ControlsPaneWrapper`), and **the cure may not be authored in a way that resurrects C·D-4's premise** · **KAD-17 WHOLE, and its BLOCKER arm is DEAD AND STAYS DEAD** (value.js's tokenizer `/\s/` matches U+00A0 — never re-book the mangled-parse claim) · **KF-SS-4 FOLDS ≡ KF-ES-8 as an EXTENSION, never a re-booking**; KF-ET-33 / KF-ES-36 likewise · **KF-SS-38 is POST-KILL RESIDUE — the sweep must not resurrect the killed claim** · **KF-ET-35's `:108`/`:116`/`:128-129` coordinates are RE-RESOLVED first** (the file drifted 129 → 114 L) · **the ten W5 re-homes land as TEN IDS, never as an ownership rule.**

### Group 11 — `.l` (alone; the closing measurement seat)

- **id** `KF.W6.l` · **model** fable (fresh — authors none of the cure bytes it measures)
- **sections** §Carry W6-C `:270-291` (the limbs no earlier unit's file-touch carried) · §Gates `G-W6-10` `:540` and the whole fifteen-row table `:527-546` · §Sequencing `:547-579`
- **writable** (`keyframes.js`) `demo/components/instrument/transport/channel-controls/ChannelControls.vue` · `.../channel-controls/ChannelOptions.vue` · `demo/components/instrument/shell/EditorStartScreen.vue` · `demo/scenes/spring/SpringTrace.vue` · `.../spring/SpringPhysicsFacet.vue` · `demo/scenes/cube/CubeScene.vue` · `demo/styles/style.css` · `demo/styles/layout.css` · `demo/styles/tab-idiom.css` · `demo/DESIGN.md`; (`value.js`) `docs/tranches/X/execution/B/KF-W6.md` · `docs/tranches/X/execution/LEDGER.md` · `docs/tranches/V/coordination/INBOX.md` (the close sweep line)
- **gates** G-W6-10 (+ the wave-wide double-run measurement of all fifteen)
- **locks** **KF-CE-41: the comment is part of the defect surface — the cure commit carries the comment** (K2: delete + correction = ONE commit) · **R-6: limbs travel BY MANIFEST, id-for-id, never by class-claim — this wave claims no limb by taxonomy alone** · **the phantom-AUTHORITY `proof:*` limb is EXCLUDED → KF.W4's G-KFW4-8, and NO NEW `proof:*` MARK IS MINTED by this wave** (house law, owner 2026-06-02) · **LIMB-LABEL AUTHORITY (R2-14): the manifest is the ROUTING authority, the BANK is the LABEL authority; where they disagree the routing stands and the label is corrected, never the reverse** · `G-W6-10`'s bound: *a born-RED gate whose column carries a figure the document has already convicted is RED for the wrong reason* — every witness cell is re-measured at final bytes · honest-RED is **named and owner-routed**, never conceded and never masked; the close states which gates are GREEN, which honest-RED, and the owner-gated questions still open (`.tap-floor`'s ruling if undecided, KF-HA-1/-2/-10's owner wordings, C-14, the KF-AV-28 governed rows).

---

## Unit receipts

*(appended by each unit at its close — one block per unit, `SERVED MODEL` line, acts in order, commands quoted with their outputs, gates measured double-run)*
