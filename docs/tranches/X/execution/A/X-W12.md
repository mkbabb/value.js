SERVED MODEL: claude-opus-5-5

# X-W12 — execution record (Track A, X·V) — the owner's value.js dockets (OA-4..6 · OA-18..25 · OA-54 · OA-55 · UIA-V)

Spec: `docs/tranches/X/waves/W12.md` (75 lines, read whole) with its addenda §0bn (unit `.l`) · §0bs (opens after X-W7R) · §0by (`.l` RETIRED) · §0ca (UI-audit fold) · §0cb (G-2/G-3/G-4 consumer halves) · §0ci (OA-54 unit `.t`; R-5 "X-W12 proceeds on 7.0.0") · §0cj (OA-55 DOCK-TRIGGER-CLIP honest-RED).
Authority: owner dockets 2026-09-22 + 2026-09-23 verbatim in W12.md §Authority; frames `waves/owner-2026-09-23/frame-{1..4}.png`; `audit/owner-2026-09-23-colorspace-dropdown.png`; `audit/owner-2026-09-23-dock-trigger-clip.png`. Minted COHESION §0bd. Every seat Opus 5.5 (owner 2026-09-23; §0bc).

## Open

- **Date**: 2026-09-23 (execution under the owner's 2026-09-17 begin-word). Seat 0 (OPEN), `claude-opus-5-5`. HEAD at open `5f9dcfe0` (branch `tranche-u`).
- **Mode**: FRESH open — LEDGER row X-W12 read `planned`, no prior record, no prior unit commits.
- **Crash-recovery**: `git status --porcelain` → 5 modified + 2 untracked paths, none inside X-W12's §1 bounds (`.github/workflows/ci.yml` = X.P.W6R stopped seat's inherited path §0ch; `CARRY-LEDGER.md`, `B/KF-W13R.md`, `C/F-W14.md`, kf/parse-that evidence dirs = sibling tracks; `scripts/dev/dev.sh` = unowned, never touched). Nothing inherited.

### Preconditions ("Opens after", verified at the bytes AND the ledger)

| precondition | source | ledger | bytes | verdict |
|---|---|---|---|---|
| X-W7 CLOSED | W12.md:4 | `LEDGER.md:35` → **CLOSED 2026-09-17 (honest-RED: G3, G11, G14-grep, CI-npm-test C-5/NG-6)**, promoted at CHECK 1 RESUME round 3 (`LEDGER.md:668`) | record `execution/A/X-W7.md` present | MET |
| X-W7R CLOSED (§0bs addendum) | W12.md:47 | `LEDGER.md:36` → **CLOSED 2026-09-17 (honest-RED: B1-pin-10.0.1, B2-G14-grep, B5-smoke@10.0.1, GLASS-VEIL-GREY O-62)**; CHECK 1 `LEDGER.md:691` "X-W12 not blocked" | record `execution/A/X-W7R.md` present | MET |
| glass pin = 7.0.0 (R-5 hold) | COHESION §0ci R-5 "X-W12 proceeds on 7.0.0" | — | ⟨cmd⟩ `grep -n '"@mkbabb/glass-ui"' package.json` → `88: "@mkbabb/glass-ui": "^7.0.0",` | MET (all glass-owned rows read on 7.0.0; the §0bs "read on 10.0.1" clause is superseded by §0ci R-5 → X-W7L) |
| `.l` retired | COHESION §0by; W12.md:49 | — | `src/css/grammar.ts` deleted by X.P.W6 `.x` (§0cg) | `.l` NOT planned |
| owner frames present | W12.md:7 | — | ⟨cmd⟩ `ls waves/owner-2026-09-23/` → frame-1..4.png; `ls audit/` → both owner PNGs + `UI-AUDIT-value.md` (6148 lines) | MET |

### E13 Step-0 mail sweep (2026-09-23 ~18:0x EDT)

Four paths + glass's newest dir. ⟨cmd⟩ `ls -t ../glass-ui/docs/tranches/ | head -1` → `BL` (BK/coordination remains the outbound mail path; BL has no coordination/). ⟨cmd⟩ `find <path> -maxdepth 2 -type f -newer INBOX.md` per path:
- `docs/tranches/V` + `V/coordination` → 0
- `../glass-ui/docs/tranches/BK/coordination` → 0
- `../glass-ui/docs/tranches/BL` → 1: `FORMATION-PROGRESS.md` (glass-internal cursor, commit `519b0f27` "D1 pass 1 closed … pass 2 running"; not addressed to value.js → no row)
- `../keyframes.js/docs/tranches/V/coordination` → 0
- `../sci-report/atlas/docs/tranches/P/coordination` → 0

INBOX tail = I-46 (READ). **0 unrowed · 0 UNREAD in scope.** Sweep line appended to INBOX.md. INFO: `INBOX.md` carries two `| O-63 |` rows (FW14U addendum and DOCK-TRIGGER-CLIP) — an id collision recorded, not repaired here (E-3; the rows are immutable).

## Baseline

Read-only; dev server `npx vite --port 9001 --strictPort` (killed after); headless Chromium = SwiftShader (not the §0ax D1 real-GPU cell). Raw outputs in the seat scratchpad; the salient lines are pasted.

| gate (unit) | command | BEFORE | reading |
|---|---|---|---|
| close: vue-tsc demo 0 | `npx vue-tsc -p tsconfig.demo.json --noEmit` ×2 | EXIT 0 ×2 (5.58 s · 5.53 s) | GREEN (guard) |
| `.b` guard: X-W5 cold-nav witness (`f55e59b7`) + `.b`/`.c` guard W6 g2 | `VJS_E2E_PORT=9931 npx playwright test e2e/smoke/oracles/cold-nav-scene-enter.spec.ts e2e/smoke/views/companion-pane-track-start.spec.ts --project=smoke` ×1 | `3 passed (1.1m)`, EXIT 0 | GREEN (guards — must stay GREEN) |
| `.d` g1 card-rhythm probe | `node docs/tranches/V/megatranche/audit/probes/x-w6/gate-card-rhythm.mjs` ×2 | `GATE g1 — RED: 2 interval(s) fail.` ×2, EXIT 1 ×2 | **RED** (born-RED, as expected) |
| `.c` About = Picker height, no page overflow | bounded probe (1440×900 dark) ×2 | `.about-card` height **7458.8 px**; `documentElement.scrollHeight` **7585** vs `innerHeight` 900 ×2 | **RED** (page overflows ~8.4×; OA-5/OA-21 reproduced) |
| `.c` `w12-about-clip.spec.ts` | `ls e2e/smoke/w12-*` | no matches | RED by absence (unit authors it) |
| `.e` `w12-dock.spec.ts` | same | no matches | RED by absence |
| `.e` 0 local dock keyframes | `grep -rn "@keyframes" demo/shell/dock` | **5**: `ActionButton.vue:128 action-pulse` · `:133 action-spin` · `ColorInput.vue:317 input-mode-flash` · `:370 crown-appear` · `DockStatusLamp.vue:110 lamp-dot-pulse` | **RED** (note: spec text said `components/custom/dock`; the live dir is `demo/shell/dock`) |
| G-2 dead Button `variant=` | multiline awk over `<Button …>` tags in `demo/**/*.vue` | **13 in 7 files**: AdminNamesPanel 3 · AdminFlaggedPanel 3 · AdminUsersPanel 2 · AdminTagsPanel 2 · UserSortMenu 1 · AdminAuditPanel 1 · PalettesPane 1 | **RED** (matches §0cb exactly) |
| G-3 wall-clock park | `grep -rn "BLOB_IDLE_MS\|SLEEPY_POSE_MS\|idleTimer\|poseTimer" demo` | **18** hits, all in `demo/picker/visual/HeroBlob.vue` | **RED** |
| `.a` ground never black | bounded probe: 4×4 px screenshot at (2,440) after 4.6 s idle ×2 | rgba **39,0,38,255** ×2 (deep maroon, not black) | **GREEN-before-cure at 1 SwiftShader sample** → finding (see below) |
| `.b` census >1 owner/property | bounded probe: `document.getAnimations()` at t≈600 ms ×2 | 23 animations · 17 elements · **0** multi-owner ×2 | **GREEN-before-cure at 1 instantaneous sample** → finding |
| `.a` drag p95 ≤ 16.7 ms (real GPU) · `.a` black-ground script · `.b` CLS · `.d` blob frame-diff · `.t` text trigger · coverage-gap captures | — | instrument not yet authored (each is the unit's own deliverable) | RED by absence |

**R.2 findings (GREEN before cure).** (1) `.a` black ground: one idle sample on SwiftShader reads maroon `39,0,38` — consistent with glass's G-4 "maroon, never black" at 7.0.0. The owner's black is therefore conditional (tab-hide/show, resize, drag, long pages, retina). **UIA-V-1** (BROKEN) names the likely mechanism: the atmosphere canvas is sized to the document (1440×7558 at `/` dark; 390×8687) and exceeds the WebGPU 8192 texture limit, so the field dies — which ties OA-18 to OA-21's unclipped About pane. `.a` must reproduce before curing. (2) `.b` census: one instantaneous sample shows no double owner. The spec's double-animation lives across the enter window and pane switches, so a single sample cannot falsify it; `.b`'s instrument must sample continuously through boot and three pane switches.

## Unit plan

**Order (strictly serial, 1 concurrent):** [`.a`] → [`.b`] → [`.c`] → [`.t`] → [`.d`] → [`.e`] → [`.u1`] → [`.u2`] → [`.u3`]. `.l` RETIRED (§0by). `.t` (§0ci, no declared slot) sits before `.d` because both write the Picker card's space-name trigger: `.d` designs the hierarchy on the already-text trigger. The UIA-V register (§0ca; 676 rows) is homed by page: each named unit takes its own pages' rows; `.u1..u3` take the rest. §0bl allows "X-W12 (or a supplement after it)": `.u3` banks a row-by-row disposition and routes any residue to a named supplement — never a silent drop. ESCALATED units do not halt the wave.

**Wave writable set (W12.md §1):** `demo/**` · `e2e/smoke/**` (new `w12-*` specs/oracles) · `docs/tranches/X/waves/W12-evidence/**` · this record · `LEDGER.md` · `docs/tranches/V/coordination/INBOX.md` (mail rows). `src/**` ONLY for `.a` if its trace names a library hot path (one commit, `npm test` GREEN). **Never**: `glass-ui/**`, `package.json`/lock pins (R-5 hold on 7.0.0), `scripts/dev/dev.sh`.

**Locks binding every unit:** glass READ-ONLY; producer halves ride O-55/O-56/O-59/O-62/O-63 by relay, never a consumer copy (SS-6/S-17); no masking (no try/catch around a defect, test.skip, allowlist); X-W5 cold-nav witness (`f55e59b7`) and W6 g2 stay GREEN; custom animations are moved/tokenized, never deleted (owner feedback); one commit per meaning; ADJACENT-LINE RULE (§0bt).

| unit | model | spec | writable | gates | honest-RED ids available |
|---|---|---|---|---|---|
| `.a` ground + drag | opus | W12.md:20-23 §2 `.a`; :59-61 G-4; UIA-V app-ground-atmosphere rows | `demo/**` (atmosphere/App/shell.css), `e2e/smoke/w12-ground*.spec.ts`, `w12-drag*`, `W12-evidence/a/**`; `src/**` iff traced | 0 black samples over the repro script ×2; drag p95 ≤ 16.7 ms headed real GPU ×2 (SwiftShader banked beside) | O-56 G-4 follow-up (producer cause) |
| `.b` motion | opus (effort high) | W12.md:25-28 | `demo/**`, `e2e/smoke/w12-motion*`, `W12-evidence/b/**` | census 0 multi-owner ×2; boot CLS ≤ 0.02; cold-nav + g2 GREEN ×2; WebMs (boot + 3 pane switches) | — |
| `.c` About clip | opus | W12.md:30-31; UIA-V about-pane rows | `demo/scenes/about/**`, shell row contract, `e2e/smoke/w12-about-clip.spec.ts`, `W12-evidence/c/**` | heights ±1 px at 1440×900 / 1280×800 / 2846×1650-eq ×2; radius-clipped screenshot; no page overflow | — |
| `.t` text trigger | opus | W12.md:63-70 (§0ci OA-54); UIA-V color-space-select rows | `demo/**` (picker trigger + census siblings), `e2e/smoke/w12-text-trigger*`, `W12-evidence/t/**` | headed Chromium 1440 & 390 × both themes: no bg / no radius plate; focus ring; pointer + keyboard open/select; before/after | GLASS-VEIL-GREY (O-62); text-trigger emphasis gap → relay + id |
| `.d` Picker hierarchy + blob | opus (effort high, design-author) | W12.md:33-36; :60 G-3; UIA-V home-picker + blob-view rows | `demo/picker/**`, `e2e/smoke/w12-picker*`, `W12-evidence/d/**` | g1 GREEN (BEFORE RED ×2); blob frame-diff > 0 idle ×2 unless PRM; owner-frame re-crops | G3-PARK-CLOCK (if 7.0.0 has no settled seam); O-56 G-3 (matte) |
| `.e` dock | opus (effort high) | W12.md:38-39; :72-75 (§0cj); UIA-V dock-* rows (7 pages) | `demo/shell/dock/**`, `e2e/smoke/w12-dock.spec.ts`, `W12-evidence/e/**` | pill bg = dock control token & radius ≥ h/2 ×2; 0 local dock layer keyframes (BEFORE 5); WebM per layer switch; ancestor-chain clip check | DOCK-MORPH-ROOT (O-55/O-56); DOCK-TRIGGER-CLIP (O-63) |
| `.u1` gaps + G-2 + palettes/browse | opus | W12.md:50-56 (§0ca); :59 G-2 | `demo/palettes/**`, other `demo/**` Button sites, `e2e/smoke/w12-uia*`, `W12-evidence/u/**` | coverage-gap captures banked; 4 UNRESOLVED → verdicts; G-2 0 dead `variant=` + removed-prop sweep; each row sha or id | O-59 for glass rows |
| `.u2` workbenches | opus | §0ca; UIA-V extract/mix/generate/gradient/gradient-easing/atmosphere-view rows | `demo/workbenches/**`, `demo/scenes/**` (not about), `W12-evidence/u/**` | each row sha / O-59 / honest-RED | O-59 |
| `.u3` admin + residue | opus | §0ca; UIA-V admin-*, not-found, pane-plates, pointer-debug-overlay rows + every unhomed row | `demo/**`, `W12-evidence/u/**` | full UIA-V disposition table (676 rows, self-counted); residue routed to a named supplement | O-59 |

### Briefs (as dispatched)

- **`.a`** — Author `w12-ground` repro (cold load, idle 30 s, tab hide/show, resize, drag; dev + gh-pages build; pixel reads of the ground outside panes). Test UIA-V-1 first: canvas sized to document (App.vue:17 `absolute inset-0` in growing `.app-layout`, shell.css:78-86) > 8192 WebGPU limit → dead field. Cure at the root (viewport-fixed field), bisect the rest (seed, mount/unmount, context loss). Name each aurora artefact on frames 1-4 with a capture. Drag: 2 s trace on surface + each slider → long tasks, per-input work; cure coalescing/fan-out. Producer cause → O-56 G-4 follow-up.
- **`.b`** — Continuous `getAnimations()` + transition-class census through boot and 3 pane switches: per element owner/count/duration; table. One owner per element per property; boot overture sequenced on glass motion tokens; cure jitter causes (layout shift, transform+top/left, re-mount mid-enter). Keep cold-nav witness + g2 GREEN ×2; CLS ≤ 0.02; WebMs committed.
- **`.c`** — About pane outer box = Picker pane height via the shared row contract (g2's track); body scrolls inside on glass's scroll primitive, radius clips the scroller; no page overflow (BEFORE: about 7458.8 px, doc 7585 at 1440×900). Author `w12-about-clip.spec.ts` (3 viewports, ±1 px, clipped screenshot) ×2.
- **`.t`** — OA-54: census every large display-type dropdown trigger; make each a text trigger (display label + chevron, no plate/fill/stadium; ink hover; visible focus ring) using glass's own quiet/text emphasis at 7.0.0; if none, relay + honest-RED id, no local copy. Surviving large holders take `--radius-field`. Gate: headed 1440 & 390, both themes, pointer + keyboard; before/after frames. Grey = GLASS-VEIL-GREY (O-62), never overridden.
- **`.d`** — Design pass on frame 4: title = card's display step, numerals take the space with tighter padding, no dead band; g1 GREEN (BEFORE RED ×2: 61.22 px residue, 55.41 px inflation). Blob: frame-diff 5 s idle + hover; G-3: retire the wall-clock park (`BLOB_IDLE_MS`/`SLEEPY_POSE_MS`/`idleTimer`/`poseTimer`, 18 hits in HeroBlob.vue) onto glass's settled seam if 7.0.0 ships one, else honest-RED G3-PARK-CLOCK + O-row, no local engine. Design notes + re-crops.
- **`.e`** — Login + `@user` pills take plate colour/radius from glass's dock control (no local bg, no square corners); layer transitions on `DockLayer`/`DockCrossfade`, no local layer keyframes (BEFORE 5 `@keyframes` in `demo/shell/dock`; move/tokenize element micro-animations, never delete). `w12-dock.spec.ts` ×2; WebM per layer switch. Missing iOS-27 morph → DOCK-MORPH-ROOT (O-55/O-56). DOCK-TRIGGER-CLIP (O-63) honest-RED with hover/selected/focus frames; check the consumer ancestor chain for a local clip and cure only that.
- **`.u1`** — Capture the §0ca critic gaps on the served page (Browse delete-confirm dialog; dock-color-input at 390 + dark; admin-flagged populated; palettes-view second leg; every frameless UIA-V row); resolve the 4 UNRESOLVED rows (UI-AUDIT-value.md:4243 :4871 :5775 :5986) to verdicts. G-2: move the 13 `variant=` bindings to `emphasis`/`tone` at the root, then sweep other removed glass props. Cure palettes/browse/tag/version/flag/migrate rows; glass rows → O-59 cite.
- **`.u2`** — UIA-V rows for extract, mix, generate, gradient, gradient-easing-authoring, atmosphere-view: cure CONSUMER rows at the root, cite O-59 for GLASS rows, split rows get both. One commit per meaning; each row → sha or id.
- **`.u3`** — admin-*, not-found, pane-plates, pointer-debug-overlay rows + every row no earlier unit homed; then the full self-counted UIA-V disposition table (676 rows: sha / O-59 / honest-RED / routed-to-supplement), zero silent drops.

## Unit receipts


### X.W12.a

**Seat**: `claude-opus-5-5`, 2026-09-23, RESUME of a killed predecessor seat. **Status: PARTIAL** — the ground gate GREEN ×2 on every cell; the drag gate RED (the named residual below).

**Crash-recovery.** ⟨cmd⟩ `git status --porcelain` → inside the writable set: `demo/color-session/ColorSpaceSelector.vue` (modified) and `e2e/smoke/w12-drag.spec.ts` (untracked); ⟨cmd⟩ `git log` → predecessor commit `9711abdb` (UIA-V-1 cure + `e2e/smoke/w12-ground.spec.ts`). Both inherited paths were read whole and judged against the spec: the ColorSpaceSelector hunk gates the catalog specimens on the menu being open (an OA-19 per-input-recompute cure, conforms) and the drag spec is the §2 `.a`.3 instrument (conforms). Both were measured before commit (below). Other dirty paths (`.github/workflows/ci.yml`, `CARRY-LEDGER.md`, `B/KF-W13R.md`, `C/F-W14.md`, `scripts/dev/dev.sh`, the kf/parse-that evidence dirs) are sibling-owned and were not touched.

**E13.** ⟨cmd⟩ `find <V, V/coordination, glass BK/coordination, glass BL, keyframes V/coordination, atlas P/coordination> -maxdepth 1 -type f -newer INBOX.md` → 0 files on every path. 0 UNREAD in scope.

**Environment note.** ⟨cmd⟩ `uptime` at the first gate run → load averages `367.06 278.84 192.35` (18 cores; sibling seats' browsers). The first three ground runs timed out in `page.goto`/`mouse.move` under that load (no sample was read black); every figure below comes from runs after the load fell (`66.77` → `28.90`). The drag frame times are read on a shared, loaded host; the bisect below uses the same host for every variant, so the comparisons stand.

#### Acts, in order

1. **The ground: UIA-V-1 first (predecessor `9711abdb`, verified here).** The canvas was `absolute inset-0` inside the growing `.app-layout`. Cure: `fixed inset-x-0 top-0 h-lvh`, so the field is viewport-sized. **BEFORE, measured now** at the pre-cure class (App.vue reverted to `absolute inset-0 w-full h-full` for one run, then restored with `git checkout HEAD -- demo/color-picker/App.vue` → `RESTORED`). ⟨cmd⟩ `W12_REAL_GPU=1 npx playwright test e2e/smoke/w12-ground.spec.ts --headed -g "(#/)"` →
   - `backing=2160x11368 box=1440x7579`
   - `maxBacking=11368 gpuErrors=2055`
   - the console carries `[aurora] init failed: Error: [object GPUValidationError]`
   - `Expected: <= 8192 Received: 11368`, 1 failed
   - The four ground pixels **stay identical before and after the drag** (`80,21,48 | 80,23,49 | 116,20,23 | 116,21,24`): the field is frozen.

   This BEFORE run already carried acts 2 and 3, and it read `black=0`. With the field failing, those two acts keep the page on the palette ground instead of a black canvas. The owner's frames were taken without them, and they read pure `0,0,0`: ⟨cmd⟩ PIL `getpixel` → frame-1 (40,800) (2800,300) (1400,20) (40,1600) = `(0,0,0)` ×4, frame-2 (10,700) = `(0,0,0)`, frame-4 (10,300) (10,20) = `(0,0,0)`. That is neither glass's maroon nor the body ground. It is a dead field presenting black.
2. **UIA-V-57 / UIA-V-56 → `bbebe3f8`.** `demo/styles/foundation.css` body ground gets `background-attachment: fixed`.
   - Before, the unattached root background was positioned against the document. On a long page the 135° ramp stretched over thousands of px, giving the flat single-stop slab of UIA-V-56 wherever the field was absent. Past the layout it restarted at its darkest stop, giving the scroll-end plum seam of UIA-V-57.
   - The no-field terminal (W2-3) is now the whole palette in every viewport.
   - UIA-V-56's fix shape ("bind the CSS gradient on failure") is not taken. The standing W2-3 contract says that on a failed field the persisted gradient ground IS the honest terminal, and with this act that ground is a full palette ramp, not a slab.
3. **UIA-V-191 → `028c2462`.** `useAtmosphere.ts` `auroraArrived` now also requires `aurora.rendererStatus.value.phase !== "error"`.
   - `isArmed` alone reads "armed once". It stayed true through a per-frame validation storm, so the canvas arrived at opacity 1 painting nothing.
   - This uses the producer's shipped status seam (glass 7.0.0 `useAurora.d.ts:45`), with no local engine.
4. **The w12-ground reproduction script made honest → `bd85df95`.** Every page reported `visible` under Playwright, so the tab-hide leg never hid the page. Measured `hide(visible)/show` in headless, in headed, and with the headed window minimised (`Browser.setWindowBounds minimized`), then dropped.
   - The leg now freezes the page on the CDP web-lifecycle door (`Page.setWebLifecycleState frozen→active`).
   - It also drives the page's own `visibilitychange` door, and records `hide(frozen+visibilitychange)`.
   - Residual, instrument-side: an OS-level hide cannot be read under Playwright, so the owner's literal tab-away is exercised through the page's own doors, not the embedder.
5. **Aurora artefacts on owner frames 1-4, named with captures → `aa899c63`** (`docs/tranches/X/waves/W12-evidence/a/`, force-added because `*.png` is git-ignored):
   - **A1 DEAD-FIELD BLACK** (`A1-f1-left-gutter-black-0-0-0.png`): the whole ground outside the plates reads `0,0,0` on frames 1, 2 and 4. Cause: the canvas arrived while the oversized swapchain failed (UIA-V-1 + UIA-V-191). Cured by `9711abdb` + `028c2462`.
   - **A2 DOCK PLATE ON A DEAD FIELD** (`A2-f1-dock-plate-on-dead-field.png`): the dock's maroon plate floats on black at the top of frame 1. Its maroon tint is the only colour left from the derive. Same cause as A1.
   - **A3 FROZEN FIELD** (no still frame can show it; it is read instead): in the BEFORE run the ground pixels stay unchanged across the drag, while AFTER they track the colour (`drag` step `36,17,33 …` vs the idle `49,0,38 …`, SwiftShader; real GPU `52,22,39 …` after the scroll). Same cause as A1.
   - **A4 PLATE/SHADOW STEP ON BLACK** (`A3-f1-right-gutter-black-plate-shadow-step.png`, `A4-f2-plate-corner-step-on-black.png`): the About plate's top-right corner and its shadow register one step apart, which is legible only against a black ground. This is a plate geometry artefact, not the aurora. It is handed to `.c` (About clip) and `.b` (plate-land motion).
   - **A5 CARD EDGE ON BLACK** (`A5-f4-picker-card-edge-on-black.png`, frame 4): the same A1 ground beside the Picker card.
   - Frame 3 (the Login pill) carries no aurora artefact. Its plate reads `74,67,61`, a dock concern owned by `.e`.
   - **AFTER, real GPU**: `after-realgpu-nav-mix-to-root.png` shows the live field behind both panes after `/mix → /`.
   - No banding, flicker or jump artefact was captured on the frames. The frames are stills, and the live runs below read no black sample and no validation error.
6. **Drag: per-input recompute → `d86ce9b3`** (the inherited hunk). The producer Select keeps its items rendered while closed, so the catalog's 18 specimens (one is the kelvin inverse, a 39,001-step search in `src/`'s `et`) re-ran on every drag frame in each host. They are now computed only while the menu is open.
   - Measured: headed real GPU, `w12-drag.spec.ts`, with the hunk removed (patch saved, `git checkout HEAD --`, reapplied → `REAPPLIED`). BEFORE, top self time includes `$e@anchors-DtONOvhx.js:534` = 50/166/124/139/262 ms and `et@anchors…:542` = 26/46 ms per target (the kelvin pair). AFTER, both are absent from every target's top six.
   - The library's brute-force kelvin search is no longer on the drag path, so `src/**` stays untouched: the trace names no library hot path once the consumer's per-frame call is gone.
   - Frame p95 BEFORE → AFTER, real GPU (surface · L · a · b · alpha):

     | reading | surface | L | a | b | alpha |
     |---|---|---|---|---|---|
     | BEFORE | 39.3 | 40.9 | 42.8 | 42.6 | 21.6 |
     | AFTER run 1 | 42.4 | 42.4 | 61.2 | 50.8 | 21.3 |
     | AFTER run 2 | 40.6 | 31.5 | 40.2 | 42.2 | 20.4 |

   - So the cure removes JS work but does not move p95. The frame is bound elsewhere (act 7).
7. **Drag bisect: the remaining frame cost is a whole-document style recalc** (read-only probes on a dev server, headed real GPU, CDP `Tracing` over a 2 s surface drag; every variant monkey-patches the page, and no product bytes changed).
   - `a@@mkbabb_glass-ui_dock.js:628` (283-509 ms self) is glass's dock-overflow `measure()`. Its `MutationObserver` (subtree + characterData) sees the dock ColorInput's `innerText` write on every colour change (⟨probe⟩ 25× `childList span.color-input` per 2 s drag). It schedules a rAF that reads `scrollWidth`, so it is the first forced layout of each frame and is billed the frame's pending style work.
   - Per-root-property write counts per drag frame: `--saved-bg-0..3`, `--saved-bg`, `--accent-live`, `--accent-view`, `--palettes-ramp-0..2`, `--palettes-ramp-title-0..2` (each 25-28 per 28 updates).
   - Bisect (`styleRecalc ms` per 2 s · `elementCount` p50 · frame p95):

     | variant | styleRecalc ms | elementCount p50 | frame p95 |
     |---|---|---|---|
     | base | 1103 / 1125 | 563 | 31.9 / 30.7 |
     | no `--palettes-ramp*` | 1129 | 563 | 31.7 |
     | no `--accent*` | 1167 | 563 | 40.9 |
     | no `--saved-bg*` | 740 | 116 | 30.6 |
     | no root writes at all | 534 | 113 | 21.2 |
     | **`:root{transition:none}` only** | **715 / 765** | **116** | 22.4 / 32.0 |

   - **Named cause:** the W2-2 material clock (`foundation.css` `:root { transition: --saved-bg-0..3 200ms }`) keeps a CSS transition running on the ROOT element for the whole drag. Each write restarts it, and a running transition on the root restyles the whole document every animation frame (563 elements, about 7 ms per frame). Inheritance is not the driver: with the stops re-registered `inherits: false` and the ground moved to `html`, the reading stayed at 563 elements / 1146-1171 ms. That attempt was reverted, unlanded, and its patch is banked in the seat scratchpad.
   - The cure is to host the 200 ms breath on a leaf that has no descendants, not on `:root`, keeping the oracles' `documentElement` reads of `--saved-bg`. It touches the W2-2/F-12 ground contract and four cold-load oracles' premises, so it was not improvised here. It is recorded below as residual **GROUND-CLOCK-ON-ROOT**. Even the no-transition variant reads p95 22.4/32.0, above 16.7, so the gate needs this cure and the remaining per-frame style/JS work too.
8. **Producer residue → INBOX addendum-beside O-56 G-4** (appended to `docs/tranches/V/coordination/INBOX.md`, no new letter). It carries UIA-V-55 and UIA-V-192:
   - glass's substrate does not clamp its backing store to `maxTextureDimension2D`
   - it retries init every frame after a size failure, and never re-arms on a later in-limit size
   - `isArmed` stays true through the storm

   Relay only; there is no consumer copy. The dock-overflow `measure()` forced layout (act 7) is producer code, but it is billed work that the root transition creates, not work of its own. It is not relayed until GROUND-CLOCK-ON-ROOT is cured and a re-read still names it.

#### Gates — BEFORE → AFTER (each read from the run logs in the seat scratchpad; ×2 = two separate runs)

| gate | BEFORE | AFTER run 1 | AFTER run 2 | verdict |
|---|---|---|---|---|
| **G1** 0 black-ground samples over the reproduction script (cold+1 s/+3 s · nav · idle 6/12/20/32 s · hide/show · resize ×2 · drag · scroll; 4 px probes × 11-12 steps), dev · SwiftShader | — (UIA-V-1 does not fire on the `css` substrate: `backing=300x150`) | `samples=44 black=0` + `samples=48 black=0`, `gpuErrors=0`, `2 passed` | same, `2 passed` | **GREEN ×2** |
| G1, gh-pages build (`vite build --mode gh-pages`, rebuilt after every demo edit; served by `serve-built.mjs` :9942) · SwiftShader | — | `black=0` 44 + 48, `2 passed` | `black=0` 44 + 48, `2 passed` | **GREEN ×2** |
| G1, headed real GPU (`W12_REAL_GPU=1 --headed`, ANGLE Metal Apple M5 Max), dev | `maxBacking=11368 gpuErrors=2055`, 1 failed (pixels frozen; owner frames `0,0,0`) | `black=0 maxBacking=2160 gpuErrors=0` ×2 routes, `2 passed` | same, `2 passed` | **GREEN ×2** |
| **G2** drag p95 ≤ 16.7 ms, headed real GPU (§0ax D1) | surface 39.3 · L 40.9 · a 42.8 · b 42.6 · α 21.6 | 42.4 · 42.4 · 61.2 · 50.8 · 21.3 | 40.6 · 31.5 · 40.2 · 42.2 · 20.4 | **RED ×2** (GROUND-CLOCK-ON-ROOT) |
| G2 SwiftShader, banked beside (dev + gh-pages) | surface `moves=3 frames=2 p95=1631.9` | dev: surface `frames=4`, `collector dead` | dev `frames=2`; gh-pages `frames=7`/`6` | banked. Software raster starves input (2-13 moves per 2 s), and the spec's `frames > 10` collector guard fails on every soft run. No figure is claimed. |
| **G3** each aurora artefact on frames 1-4 named with a capture | — | A1-A5 + AFTER frame, `aa899c63` | — | **GREEN** |
| **G4** X-W5 cold-nav witness + W6 g2 | `3 passed` (open baseline) | `3 passed (1.3m)` | `3 passed (1.1m)` | **GREEN ×2** |
| vue-tsc demo (close guard) | EXIT 0 | no diagnostics | no diagnostics | GREEN ×2 |

#### UIA-V app-ground-atmosphere rows (17 rows, self-counted: ⟨cmd⟩ `grep -c "^#### UIA-V-[0-9]* · [A-Z]* · app-ground-atmosphere" UI-AUDIT-value.md` → 17)

**Cured here:**

| row | disposition |
|---|---|
| UIA-V-1 | CURED `9711abdb` (verified: BEFORE `maxBacking=11368 gpuErrors=2055` → AFTER `2160`, `0`) |
| UIA-V-57 | CURED `bbebe3f8` |
| UIA-V-56 | CURED at its symptom by `bbebe3f8` + `028c2462` (the terminal is the full palette ramp, never a slab; the W2-3 contract kept) |
| UIA-V-191 | CURED `028c2462` |

**Routed to glass (relay only):**

| row | disposition |
|---|---|
| UIA-V-55 | GLASS → the O-56 G-4 addendum (INBOX) |
| UIA-V-192 | GLASS+CONSUMER. The consumer half is gone with UIA-V-1: no in-app surface reaches the limit now. The glass half → the O-56 G-4 addendum. |

**Open, owed:**

| row | disposition |
|---|---|
| UIA-V-186 | OPEN (GLASS+CONSUMER, dark band L 0.18-0.42). The gate's black threshold passes (dark ground reads `49,0,38`/`36,17,33` SwiftShader, `52-129` real GPU), but the owner's "black" wording over a near-black dark band is a design ruling on X-W6 i2's shipped band, not something this seat can change unilaterally. Owed: a ruling, or `.d`'s design pass. |
| UIA-V-188 | OPEN. Remounting on glass `<Aurora>` is a structural migration. Sizing is now cured at the consumer and the fallback and status are handled (acts 2-3). Owed: `.u2` (atmosphere) or a supplement. |

**Handed to other units (their concern, same wave):**

| row | disposition |
|---|---|
| UIA-V-58 · 187 · 461 · 462 · 463 | → `.b` (motion: plate-land/vj-enter owners, clocks, blob re-emerge, NaN durations) |
| UIA-V-59 · 190 | → `.e`/`.u3` (dock admin-mode latch; dark accents) |
| UIA-V-189 | → `.u3` (ErrorBoundary plate/focus) |
| UIA-V-464 | → `.u1` (UNRESOLVED-class row) |

**Count:** 4 cured + 2 routed + 2 open + 9 handed = **17**.

#### Commits (this unit, pathspec each)
`9711abdb` (predecessor, UIA-V-1 + w12-ground) · `bbebe3f8` (UIA-V-57/56) · `028c2462` (UIA-V-191) · `bd85df95` (w12-ground hide leg) · `d86ce9b3` (OA-19 specimens) · `d2598360` (w12-drag) · `aa899c63` (captures) · the record/INBOX commit that carries this receipt.

**Adjacent edits:** none. **src/\*\*:** untouched (no library hot path after `d86ce9b3`). **package.json pins:** untouched.

#### Residuals / escalation
- **GROUND-CLOCK-ON-ROOT (drag gate RED ×2).** The 200 ms `--saved-bg-*` transition lives on `:root`. During a drag it runs every frame and restyles the whole document (563 elements, ~7 ms/frame; the bisect is in act 7).
  - The cure is to host the breath on a descendant-free layer while the oracles keep reading `--saved-bg` on `documentElement`. It changes the W2-2/F-12 ground contract, so it needs a unit that owns that contract (`.b` owns "one owner per property / glass motion tokens", or a supplement).
  - The no-transition upper bound still reads p95 22.4/32.0 on this loaded host, so the gate also owes the remaining per-frame style/JS: the dock text write → glass dock-overflow forced layout, and the root `--accent*`/`--palettes-ramp*` writes on a view that does not show palettes.
- The tab-hide leg is exercised through the page's own doors. Playwright never reports `hidden` (instrument limit).
- The SwiftShader drag reading is starved by software raster; its `collector dead` guard fails on every soft run (banked, not claimed).
- **Atmosphere oracles re-read (the ground contract this unit touched).** ⟨cmd⟩ `playwright test atmosphere-cold-load.spec.ts oracles/o28-atmosphere-coldload.spec.ts oracles/o2-real-hydration-coldload.spec.ts --project=smoke` → `4 passed`, 1 failed: o28 "the first painted atmosphere is not the seeded one" ×3 seeds.
  - **Not introduced by this unit.** The same run with `foundation.css`, `useAtmosphere.ts`, `App.vue` and `ColorSpaceSelector.vue` set to the X-W12 open bytes (`git show 1a35dac0:<path>`, then restored with `git checkout HEAD --`, status clean) → the identical 3 errors, 1 failed.
  - o28 reads the `--saved-bg-*` inline-style timeline against `first-paint`. No act here writes or reorders it. This is a standing RED outside `.a`'s writable concern; it is left to the wave close's smoke re-read.
- **Note (INBOX landing):** the O-56 G-4 addendum (act 8) was committed inside a sibling seat's pathspec commit `8b419438` (the whole-file INBOX staging carried it), so `38c78427` holds the record only. The addendum is at `INBOX.md:490` at HEAD.

### X.W12.b

**Seat**: `claude-opus-5-5`, 2026-09-23, FRESH (no prior `.b` receipt). **Status: DONE** — all four gates GREEN ×2.

**Crash-recovery.** ⟨cmd⟩ `git status --porcelain | grep -E '^.. (demo/|e2e/smoke/w12-motion|W12-evidence/b/|X-W12.md|LEDGER.md)'` → empty. Nothing inherited.

**Spec/rulings read.** W12.md whole (75 lines); record header → Unit plan + `.a` receipt tail; COHESION §0bd, §0ca–§0ck (no `.b`-specific ruling; §0ci R-5 = glass 7.0.0 held). `.a`'s residual GROUND-CLOCK-ON-ROOT names `.b` as a possible owner; it is **not** taken here (see Residuals).

**Environment.** Headless Chromium, SwiftShader, dev server, 1440×900 dark. ⟨cmd⟩ `uptime` → load 27–40 on 18 cores during every run (sibling seats). Frames came as slowly as 1–2 s apart at times (probe below).

#### Acts, in order
1. **The instrument (measure before editing).** I authored `e2e/smoke/w12-motion.spec.ts`. An init script runs from the first script on the page. It samples `document.getAnimations()` every frame and also listens for `transitionrun`/`animationstart` (capture). For each animation it records element, owner (`transition(<prop>)` / `@keyframes <name>` / `waapi <id>`), properties (transform ≡ translate/rotate/scale), delay, duration and document-timeline start. It also runs a removal MutationObserver for frame-exact cuts and a `layout-shift` observer that records source rects. The windows are boot (`/#/` until still for 1 s), then three router switches: picker→palettes (inspector swap), palettes→gradient (both regions) and gradient→picker (both regions, back direction). **Gate:** 0 elements with more than one owner on one property while their held intervals overlap; boot CLS ≤ 0.02. **Reported:** stacked travel (a child and an ancestor moving at once, in ms), transform beside layout properties, cuts and re-mounts, re-runs.
2. **BEFORE ×2.** ⟨cmd⟩ `VJS_E2E_PORT=994{1,2} W12_CENSUS_OUT=… W12_WEBM_DIR=… npx playwright test e2e/smoke/w12-motion.spec.ts --project=smoke` → run 1: `rows 174 · elements 50 · boot CLS 0.0220 · switch CLS {picker→palettes 0.0219, palettes→gradient 0.0220, gradient→picker 0.0184}`, `multi-owner 0`, `ancestor-stacked transform 10`; run 2: `rows 216 · boot CLS 0.0220`, `multi-owner 0`, `ancestor-stacked 11`. The strict gate read **0 ×2 before any cure**, so the open's instantaneous reading holds across the whole window. What the owner calls "double-animated" is a different set of defects, named below. Committed `185a394d` (instrument + `W12-evidence/b/before/` tables ×2, WebM, 1 fps sheet).
3. **The defects the census and WebM named (BEFORE):**
   - **D1 · a region entered twice.** A swap to an unresolved lazy pane slid its plate phase in, slid it out (out-in), then slid the pane in. At boot, the inspector mounted as a plate and its About pane flew in on the pane-SWAP slide (translateX 110% + 2°) 7 s after the stage plate landed. It never landed beside the stage plate (sheet frames r2c1–r2c7).
   - **D2 · the boot landing had two declared owners.** The scoped `.pane-shell { transition: transform … }` (0,2,0) outranked `.overture-appear-active` (0,1,0), so the left plate landed at `0+350` (smooth, no stagger delay) instead of the overture's `40+440` snappy (census row `div.pane-shell transition(transform) 0+350`).
   - **D3 · stacked travel inside the landing.** `stagger-child-in` (+220 ms) ran on the rail letters and channel strips while the shell was still moving (10–11 stacked rows).
   - **D4 · a KeepAlive return re-ran the boot breath.** On gradient→picker, `plate-land`, `field-paint-in`, `stagger-child-in` ×8 and `blob-emerge` all restarted beneath the shell's `vj-enter` travel. The browser restarts CSS animations on a re-inserted element.
   - **D5 · layout shift during enter.** `.pane-main` centred the row vertically. When the About pane mounted, the row grew 685→788 px and the unchanged Picker card jumped 47 px (source rects `div.pane-container 199,159 1042×685→199,112 1042×788`). Each switch shifted the kept card the same way.
   - **D6 · a header re-flow on return.** The picker header's scroll-condense observer read the off-canvas travel as "scrolled past". It condensed and then re-expanded (padding transitions; CLS 0.0150 + 0.0033 on gradient→picker).
   - **D7 · a timer-ended swap.** The region rules declared opacity + transform, but opacity is pinned at 1, so only one `transitionend` ever arrives. Vue ended every swap on its fallback timer, which counts from the class flip, and the leaving About card was cut 40–95 ms short (the census cut line).
4. **Cures, each at its cause (one commit per meaning):**
   - **D1 → `e1aaee3e`** (`demo/shell/PaneSlot.vue`, `usePaneRouter.ts`). Each lazy pane gets one memoized chunk load, shared by the plate phase's async wrapper and a new `preloadPane()`. A swap to an unresolved pane holds for at most `PANE_LOAD_DELAY_MS` (200 ms) while the chunk loads. It then commits straight to the resolved pane: one leave, one enter. A slower chunk commits the plate phase exactly as X.W5.d4 (`f55e59b7`) keyed it, so `(pane, resolved)` and out-in are untouched. The `<Transition>` mounts with the first RESOLVED pane, so a lazy region's first arrival rides the overture appear grammar. Until then the plate phase renders bare, and it leaves with no travel.
   - **D2 + D3 + D4 → `1a696ca6`** (`overture.css`, `ColorPicker.vue`, `SpectrumCanvas.vue`, `ComponentSliders.vue`, `ConsoleRail.vue`). This is one meaning: the opening breath runs as one sequenced entrance, on one clock, once.
     - `--overture-plate-land` now equals glass `--spring-snappy-duration`; the local `440ms` copy is retired.
     - The appear landing is a keyframe (`overture-plate-settle`, `backwards`) on `overture-appear-active`. It is no longer a from→to transition, which Vue can only start two frames after insertion. The landing therefore starts on the same frame as the beats inside it, and the scoped `.pane-shell` transition can neither outrank it nor be triggered by it. That rule stays because O-16 R11 asserts it; a comment records why it is now inert at boot.
     - `--overture-stagger-base` = `--overture-left-delay` + `--overture-plate-land`, so the channel cascade starts as the plate settles. `plateOpening` now drops when the cascade's own animations finish, not on the 850 ms timer.
     - `plate-land`, `field-paint-in` and `blob-emerge` are scoped to `.pane-shell[data-arrival="first"]`. `data-arrival` flips to `return` in `onDeactivated`. A return re-measures through HeroBlob's existing `onActivated` → `resume()`, the path PRM already takes.
     - The channel cascade's `stagger-children` class is live only for fresh row sets: first mount and every space re-key. Deactivation retires it while the rows are detached.
     - No animation was deleted. Every beat still runs on the first arrival and on a space change.
   - **D5 → `7465354d`** (`demo/styles/shell.css`). `.pane-main` now uses `justify-content: flex-start`, which anchors the row under the route title. No oracle asserts vertical centring (⟨cmd⟩ `grep -rln 'pane-main\|centred' e2e/smoke` → only `t31-dock-band.spec.ts`, which asserts the dock pin and a gap ≥ 7 px; it passed, act 5).
   - **D6 → `c486e882`** (`demo/picker/composables/useHeaderCondense.ts`). The condense verdict is skipped while the sentinel's pane carries a travel class (`vj-enter-enter-active`, `vj-enter-leave-active`, `overture-appear-active`). Travel is not scroll.
   - **D7 → `059bccea`, REVERTED by `3aac361b`.** I made the region swap rules transform-only so that Vue would await the one real `transitionend`. The adjacent-oracle run (act 5) showed that O-16 R3 asserts the leave's opacity leg. The census also did not support the change: AFTER run 2 on those bytes still read one cut. ⟨probe⟩ `probe-leave.mjs` (event timeline on the leaving panes, seat scratchpad) → `leave-from 941 → leave-to 2028`, removal `2241`, **no transition frame rendered**. The cuts are the loaded host's frame starvation: 1.1–2.0 s between Vue's two `nextFrame`s. So the file was restored to its pre-`059bccea` bytes, and the census now reports cuts without asserting them (`708182db`). No oracle was touched.
5. **Adjacent-oracle read.** ⟨cmd⟩ `playwright test oracles/o16-computed-cascade oracles/o4-order-invariance oracles/t31-dock-band dual-pane-1440 --project=smoke` → `13 passed, 1 failed` (O-16 R3 `opacity duration: Expected "0.2s", Received undefined`). That failure is D7's; it was reverted, and O-16 is re-run on the final bytes (Gates).

#### Census table (final bytes, run 1 — per element: owner, count, delay+duration; pasted from `W12-evidence/b/after/census-final-run1.md`, the enter/boot rows; spectrum-dot idle rows collapsed)

| window | element | owner | props | runs | delay+duration ms |
|---|---|---|---|---|---|
| boot | `html.dark` | transition(--saved-bg-1) | --saved-bg-1 | 1 | 0+200 |
| boot | `html.dark` | transition(--saved-bg-0) | --saved-bg-0 | 1 | 0+200 |
| boot | `html.dark` | transition(--saved-bg-3) | --saved-bg-3 | 1 | 0+200 |
| boot | `html.dark` | transition(--saved-bg-2) | --saved-bg-2 | 1 | 0+200 |
| boot | `html.dark` | transition(--saved-bg) | --saved-bg | 1 | 0+200 |
| boot | `div.pane-shell.flex.flex-col` | @keyframes overture-plate-settle | transform | 1 | 40+440 |
| gradient→picker | `div.pane-shell.flex.flex-col` | transition(transform) | transform | 1 | 0+440 |
| boot | `div.glass-resting.card.rounded-card` | @keyframes plate-land | box-shadow | 1 | 40+440 |
| boot | `div.spectrum-picker.flex.w-full` | @keyframes field-paint-in-ad23e00d | opacity,background-size | 1 | 180+420 |
| boot | `button.channel-rail-item.font-display.text-subheading` | @keyframes stagger-child-in | opacity,transform | 1 | 480+300 |
| boot | `button.channel-rail-item.font-display.text-subheading` | transition(color) | color | 1 | 0+300 |
| boot | `button.channel-rail-item.font-display.text-subheading` | @keyframes stagger-child-in | opacity,transform | 1 | 520+300 |
| boot | `button.channel-rail-item.font-display.text-subheading` | transition(color) | color | 1 | 0+300 |
| boot | `button.channel-rail-item.font-display.text-subheading` | @keyframes stagger-child-in | opacity,transform | 1 | 560+300 |
| boot | `button.channel-rail-item.font-display.text-subheading` | transition(color) | color | 1 | 0+300 |
| boot | `button.channel-rail-item.font-display.text-subheading` | @keyframes stagger-child-in | opacity,transform | 1 | 600+300 |
| boot | `button.channel-rail-item.font-display.text-subheading` | transition(color) | color | 1 | 0+300 |
| boot | `div.channel-strip.min-w-0.flex` | @keyframes stagger-child-in | opacity,transform | 1 | 480+300 |
| boot | `div.channel-strip.min-w-0.flex` | @keyframes stagger-child-in | opacity,transform | 1 | 520+300 |
| boot | `div.channel-strip.min-w-0.flex` | @keyframes stagger-child-in | opacity,transform | 1 | 560+300 |
| boot | `div.channel-strip.min-w-0.flex` | @keyframes stagger-child-in | opacity,transform | 1 | 600+300 |
| boot | `span[watercolor-swatch].w-7.h-7.shrink-0` | transition(border-bottom-left-radius) | border-bottom-left-radius,border-bottom-left-radius | 1 | 0+600 |
| boot | `span[watercolor-swatch].w-7.h-7.shrink-0` | transition(border-bottom-right-radius) | border-bottom-right-radius,border-bottom-right-radius | 1 | 0+600 |
| boot | `span[watercolor-swatch].w-7.h-7.shrink-0` | transition(border-top-left-radius) | border-top-left-radius,border-top-left-radius | 1 | 0+600 |
| boot | `span[watercolor-swatch].w-7.h-7.shrink-0` | transition(border-top-right-radius) | border-top-right-radius,border-top-right-radius | 1 | 0+600 |
| boot | `span[watercolor-swatch].dock-seal-wax.watercolor-swatch` | transition(border-bottom-left-radius) | border-bottom-left-radius,border-bottom-left-radius | 1 | 0+600 |
| boot | `span[watercolor-swatch].dock-seal-wax.watercolor-swatch` | transition(border-bottom-right-radius) | border-bottom-right-radius,border-bottom-right-radius | 1 | 0+600 |
| boot | `span[watercolor-swatch].dock-seal-wax.watercolor-swatch` | transition(border-top-left-radius) | border-top-left-radius,border-top-left-radius | 1 | 0+600 |
| boot | `span[watercolor-swatch].dock-seal-wax.watercolor-swatch` | transition(border-top-right-radius) | border-top-right-radius,border-top-right-radius | 1 | 0+600 |
| boot | `span.slider-track` | transition(background-color) | background-color,background-color | 1 | 0+200 |
| boot | `span.slider-track` | transition(background-color) | background-color,background-color | 1 | 0+200 |
| boot | `div.glass-resting.card.rounded-card` | @keyframes overture-plate-settle | transform | 1 | 120+440 |
| gradient→picker | `div.glass-resting.card.rounded-card` | transition(transform) | transform | 1 | 0+440 |
| boot | `nav.dock-band.overture-dock-veiled` | @keyframes overture-plate-land | opacity,transform | 1 | 0+440 |
| boot | `canvas[atmosphere-canvas].atmosphere-canvas.fixed.inset-x-0` | transition(opacity) | opacity | 1 | 0+900 |
| boot | `div.pointer-events-none.hero-blob-anchor` | @keyframes blob-emerge | transform | 1 | 0+500 |
| picker→palettes | `svg.lucide.lucide-palette-icon.lucide-palette` | transition(stroke) | stroke | 1 | 0+200 |
| picker→palettes | `button.dock-trigger.dock-select-trigger.view-select-trigger` | transition(--accent-view) | --accent-view | 1 | 0+550 |
| palettes→gradient | `button.dock-trigger.dock-select-trigger.view-select-trigger` | transition(--accent-view) | --accent-view | 1 | 0+550 |
| gradient→picker | `button.dock-trigger.dock-select-trigger.view-select-trigger` | transition(--accent-view) | --accent-view | 1 | 0+550 |
| picker→palettes | `div` | @keyframes vj-settle | transform | 1 | 0+440 |
| palettes→gradient | `div` | @keyframes vj-settle | transform | 1 | 0+440 |
| gradient→picker | `div` | @keyframes vj-settle | transform | 1 | 0+440 |
| picker→palettes | `svg.lucide.lucide-house-icon.lucide-house` | transition(opacity) | opacity | 1 | 0+200 |
| picker→palettes | `svg.lucide.lucide-house-icon.lucide-house` | transition(transform) | transform | 1 | 0+200 |
| picker→palettes | `svg.lucide.lucide-palette-icon.lucide-palette` | transition(opacity) | opacity | 1 | 0+200 |
| picker→palettes | `svg.lucide.lucide-palette-icon.lucide-palette` | transition(transform) | transform | 1 | 0+440 |
| palettes→gradient | `svg.lucide.lucide-palette-icon.lucide-palette` | transition(opacity) | opacity | 1 | 0+200 |
| palettes→gradient | `svg.lucide.lucide-palette-icon.lucide-palette` | transition(transform) | transform | 1 | 0+200 |
| picker→palettes | `div.glass-resting.card.rounded-card` | transition(transform) | transform | 1 | 0+440 |
| gradient→picker | `div.glass-resting.card.rounded-card` | transition(transform) | transform | 1 | 0+200 |
| picker→palettes | `canvas[goo-blob-canvas].goo-blob-canvas` | @keyframes substrate-reveal-bloom | opacity,filter | 1 | 0+1100 |
| gradient→picker | `canvas[goo-blob-canvas].goo-blob-canvas` | @keyframes substrate-reveal-bloom | opacity,filter | 1 | 0+1100 |
| palettes→gradient | `svg.lucide.lucide-rainbow-icon.lucide-rainbow` | transition(opacity) | opacity | 1 | 0+200 |
| palettes→gradient | `svg.lucide.lucide-rainbow-icon.lucide-rainbow` | transition(transform) | transform | 1 | 0+440 |
| gradient→picker | `svg.lucide.lucide-rainbow-icon.lucide-rainbow` | transition(opacity) | opacity | 1 | 0+200 |
| gradient→picker | `svg.lucide.lucide-rainbow-icon.lucide-rainbow` | transition(transform) | transform | 1 | 0+200 |
| palettes→gradient | `div.relative.w-full.mx-auto` | transition(transform) | transform | 1 | 0+440 |
| gradient→picker | `div.relative.w-full.mx-auto` | transition(transform) | transform | 1 | 0+200 |
| gradient→picker | `svg.lucide.lucide-house-icon.lucide-house` | transition(opacity) | opacity | 1 | 0+200 |
| gradient→picker | `svg.lucide.lucide-house-icon.lucide-house` | transition(transform) | transform | 1 | 0+440 |
| boot | `span[watercolor-swatch].spectrum-dot.absolute.-translate-x-1/2` | transition(transform) | transform | … | 0+200 |

Every row reads `runs 1`: each owner runs once per window. On a KeepAlive return (gradient→picker) the only picker rows are the shell's `vj-enter` `transition(transform) 0+440` and the About card's. Before, that window carried `plate-land`, `field-paint-in`, 8× `stagger-child-in` and `blob-emerge` (`before/census-run1.md`). ⟨cmd⟩ `grep -c 'gradient→picker.*\(plate-land\|stagger-child-in\|field-paint\|blob-emerge\)' after/census-final-run{1,2}.md` → `0` / `0`.

#### Gates — BEFORE → AFTER (final bytes = HEAD after `3aac361b`; logs in the seat scratchpad; ×2 = two separate runs)

| gate | command | BEFORE | AFTER | verdict |
|---|---|---|---|---|
| continuous census: 0 elements with >1 owner per property | `npx playwright test e2e/smoke/w12-motion.spec.ts --project=smoke` ×2 | `multi-owner 0` ×2 (strict gate green before; the owner's defects D1–D6 were found by the census's other lines) | `multi-owner 0` ×2 (`1 passed (58.6s)` · `1 passed (53.9s)`) | **GREEN ×2** |
| CLS during boot ≤ 0.02 | same spec | `boot CLS 0.0220` ×2 | `boot CLS 0.0000` ×2; switch CLS ≤ `0.0001` (before ≈ `0.02` per switch) | **GREEN ×2** (RED→GREEN) |
| X-W5 cold-nav witness + W6 g2 | `npx playwright test e2e/smoke/oracles/cold-nav-scene-enter.spec.ts e2e/smoke/views/companion-pane-track-start.spec.ts e2e/smoke/oracles/o16-computed-cascade.spec.ts --project=smoke` ×2 | GREEN at open (`3 passed`) | `5 passed (48.8s)` · `5 passed (56.6s)` (O-16 rides along) | **GREEN ×2** |
| before/after WebM of boot + 3 pane switches | `W12_WEBM_DIR=…` on the census run | — | `W12-evidence/b/before/boot-and-3-switches.webm` (`185a394d`) · `after/boot-and-3-switches.webm` (`708182db`), each with a 1 fps contact sheet | **GREEN** (committed) |
| guard: vue-tsc demo 0 | `npx vue-tsc -p tsconfig.demo.json --noEmit` ×2 | EXIT 0 | EXIT 0 ×2 | GREEN |
| guard: eslint (touched files) | `npx eslint <8 touched files>` | — | no output | GREEN |
| stacked travel (reported) | census `ancestor-stacked` | 10 / 11 (cascade, blob and dots inside the landing and return) | 7 / 9, and **0 consumer enter rows**: all remaining rows are the glass `WatercolorDot` `animate` idle transform (the dot's own `transition(transform) 0+200`, placed by `left`/`top`) and the dock icon inside `vj-settle` (the dock is `.e`'s) | reported |
| cut / re-mount (reported) | census `re-mount mid-enter` | 2 / 1 | 1 / 0 | reported (host starvation, D7 probe) |

The AFTER WebM was recorded on the `708182db` bytes, which carried `059bccea`'s transform-only leave. That rule changed no visible motion, because the opacity leg it removed never ran. The final-byte census tables are `after/census-final-run{1,2}.md` (`da91c04c`).

#### Commits (this unit, pathspec each)
`185a394d` (instrument + BEFORE) · `e1aaee3e` (D1 one enter per region) · `1a696ca6` (D2–D4 opening breath: one clock, once) · `059bccea` (D7, reverted) · `7465354d` (D5 band anchor) · `c486e882` (D6 condense) · `708182db` (census timeline + AFTER evidence) · `3aac361b` (restores `059bccea`) · `da91c04c` (final-byte census ×2) · plus the record commit carrying this receipt.

**Adjacent edits:** none. **Locks:** glass untouched (read only); `f55e59b7`'s `(pane, resolved)` keying is intact (witness GREEN ×2); no animation deleted, since every beat still runs on the first arrival or a space change; no oracle or assertion edited; `scripts/dev/dev.sh` untouched.

#### Residuals / escalation
- **Design change to note for `.c`/`.d`:** the scene row is now top-anchored under the route title (D5). A short scene sits about 47 px higher than it did when centred. `.c` (About = Picker height) no longer affects vertical position.
- **Boot sequencing across regions** holds only to each region's chunk arrival. The stage lands at `--overture-left-delay` and the inspector at `--overture-right-delay` after its own chunk resolves. A cold chunk slower than 200 ms shows its plate bare and then lands. The dock's arrival is gated by the producer mount-morph (`useDockArrival`, `.e`/O-55), and on this loaded host it landed 2.7–5 s after the stage plate.
- **Idle motion inside the plate** (glass `WatercolorDot animate`) is producer-owned; there is no consumer act.
- **GROUND-CLOCK-ON-ROOT** (`.a`'s residual) is not taken. It is a drag-frame cost on the root `--saved-bg-*` clock, not a per-element double owner (the census shows the five `--saved-bg*` transitions once each on `html`). It stays with `.a`'s named owner or a supplement.
- **Cut lines under starvation:** Vue's `<Transition>` fallback timer can end a swap before its first frame renders when frames come 1–2 s apart. This is recorded; there is no consumer cure.

### X.W12.c

**Seat**: `claude-opus-5-5`, 2026-09-23, FRESH (no prior `.c` receipt). **Status: DONE** — the four named gates GREEN ×2; one stricter reading of this unit's own instrument (page overflow at 1280×800) is RED on the Picker's height and handed to `.d` by id.

**Crash-recovery.** ⟨cmd⟩ `git status --porcelain` → nothing inside `.c`'s writable set (`CARRY-LEDGER.md`, `B/KF-W13V.md`, kf evidence dirs = sibling tracks; `scripts/dev/dev.sh` untouched). Nothing inherited.

**Spec/rulings read.** W12.md whole (75 lines); record header → Unit plan + `.b` receipt; COHESION §0bd, §0ca–§0cl by grep (no `.c`-specific ruling). Consumed: §0ax ESC-W5d2-2 (P-5a `align-items: start` REFUSED: equal-height pairing is kept, so the cure must not un-stretch the row); §0ay ESC-W5t-2 (a comment beside a pane's root breaks `Transition mode="out-in"` in dev; it bit here, act 5); `.b`'s D5 (row top-anchored).

**Anchors at true bytes.** The spec names "the glass scroll primitive": at the installed glass 7.0.0 it is `FadingScroll` (`@mkbabb/glass-ui/fading-scroll`, root `.fading-scroll.fading-scroll--y` = the scroll port, `overflow-y: auto`, edge feathers on a `scroll(self block)` timeline). "The shared row contract (g2's track)" = `.pane-container`'s grid in `demo/styles/shell.css` (g2 asserts both panes start on one track). The About pane's root was `Card.about-card.pane-scroll-fade … overflow-y-auto h-full` — a scroller whose `h-full` resolved against an unbounded wrapper, so it never scrolled (UIA-V-64's diagnosis, confirmed).

#### Acts, in order
1. **Instrument first.** Authored `e2e/smoke/w12-about-clip.spec.ts`. At 1440×900, 1280×800 and 1423×825 @ DPR 2 (the owner's 2846×1650 frame), dark, it reads the settled Picker pane box and About pane box (each region's first child), the document scroll height, and the About card's scroller/overflow/radius. Test A asserts About == Picker ±1 px, a glass `.fading-scroll--y` inside the card that really overflows and scrolls while `window.scrollY` stays 0, the card `overflow: hidden|clip` with radius > 0, and writes the card screenshot (scrolled mid-body) to `W12_ABOUT_OUT`. Test B asserts `documentElement.scrollHeight ≤ innerHeight + 1`.
2. **BEFORE** (HEAD `a18cb1ca` bytes, dev server, SwiftShader) ⟨cmd⟩ `VJS_E2E_PORT=9951 npx playwright test e2e/smoke/w12-about-clip.spec.ts --project=smoke` → `3 failed`:
   - 1440×900: About **7458.84** vs Picker **684.77**; doc **7585** vs 900; scroller `null`; card `overflow hidden auto`, radius 16
   - 1280×800: About **7567.98** vs Picker **682.98**; doc **7694** vs 800
   - 2846×1650-eq: About **7605.00** vs Picker **684.77**; doc **7731** vs 825
   Banked `W12-evidence/c/before/readings.md` (`bcee2e08`). The owner's frame 2 is the BEFORE frame.
3. **The cure → `38b35385`** (`demo/styles/shell.css`, `demo/scenes/about/AboutPane.vue`), one meaning: the companion follows the row.
   - **The row contract (shell.css).** `.pane-container` becomes the named query container `pane-row` (inline axis only; its width is `width:100%` plus the owner-ruled clamp, never its content). A pane that declares `.pane-row-follow` on its root gives its region wrapper `contain: size` while the grid holds two tracks: `@container pane-row (inline-size >= calc(2 * 25rem + clamp(0.5rem, 1.25vw, 1.618rem)))`, which is exactly `auto-fit`'s two-track condition. The wrapper then contributes nothing to the row's height, so the Picker's content sets the row. `align-self: stretch` (default; P-5a's pairing kept) hands the wrapper that height; a stretched grid item is definite, so the card's `h-full` resolves. In ONE column the query does not match and About grows the document as before (X.W5.c's one scrolling column; mobile walk asserts it). A size query cannot read custom properties, so `--pane-min`/`--pane-gap` are written out, and the comment names the coupling. The rule sits on the WRAPPER because `animations.css` puts `contain: layout paint` on the travelling pane during a swap, which would replace a `contain` on the card.
   - **The scroller (AboutPane.vue).** The card is `flex flex-col overflow-hidden h-full` (its radius clips); the body sits in glass `<FadingScroll axis="y" :fade-start="false">` carrying `pane-scroll-fade flex-1 min-h-0` (the `--pane-scroll` timeline PaneHeader reads moves with the scroll to the port). The start feather is off because the sticky PaneHeader's veil is the port's start edge, and a mask there would fade the title. No consumer copy of any glass rule.
4. **Adjacent guards on `38b35385` ×2** (⟨cmd⟩ w12-about-clip + g2 + cold-nav + O-16 + O-21 + mobile walk + page-load-mobile, `--project=smoke --project=smoke-mobile`) → `4 failed / 10 passed` and `3 failed / 11 passed`. **g2** "the region never settled … renders no pane yet" on `/#/mix` ×2, and **mobile walk** "About region hidden" on the return to Home ×2. Both are REAL regressions of this cure.
5. **Root found and cured → `4b7639c0`.** Probe (seat scratchpad `probe-leave.mjs`, 1440 dark): after `/` → `/#/mix` the inspector region stayed EMPTY for 6 s+, then for `/` and `/#/palettes` too — the slot never mounted another pane after About left. Bisected by writing one file back to the baseline at a time: ColorNutritionLabel at baseline → still EMPTY; `contain` rule disabled by style tag → still EMPTY; FadingScroll animation disabled → still EMPTY; FadingScroll swapped for a plain div → still EMPTY; **AboutPane at baseline → `MIX PRESENT`**. The cause was the doc comment I had placed BESIDE the `<Card>` root. In dev that makes a root fragment, and the out-in continuation is lost: §0ay ESC-W5t-2's class, measured again. The comment moved inside the root → `MIX PRESENT`. The same commit makes `readSettled` wait for `.about-card` itself; run 1 had read the region's loading plate at a cold start (scroller `null`, radius 0).
6. **UIA-V about-pane rows cured in this unit's set:**
   - **UIA-V-4 → `a89fa035`** (`ColorNutritionLabel.vue`). Each conversion chip sat in its own `TooltipProvider/Tooltip` whose content was an empty `contents` box, so hovering produced a 1×1 px `role=tooltip` with no text. The chip also wore `cursor-pointer` with no action. The empty tooltip layer (and its import) and the pointer cursor are removed; the hover highlight, the chip's one real behaviour, stays (O-18 `graph-node` still asserts it).
   - **UIA-V-206 → `b1bc7b52`** (same file). The hover held the hovered path's node NAMES and every chip lit any node in that list. It now holds the hovered chip's index, so only that chip lights up.
   - **UIA-V-64 → `38b35385` + `4b7639c0`** (the acts above).

#### Gates — BEFORE → AFTER (final bytes `4b7639c0`; ×2 = two separate runs on a playwright-managed dev server `:9953`; SwiftShader, dark)

⟨cmd⟩ `VJS_E2E_PORT=9953 npx playwright test e2e/smoke/w12-about-clip.spec.ts e2e/smoke/views/companion-pane-track-start.spec.ts e2e/smoke/oracles/cold-nav-scene-enter.spec.ts e2e/smoke/oracles/o16-computed-cascade.spec.ts e2e/smoke/oracles/o21-space-catalog-truth.spec.ts e2e/smoke/mobile/walk.spec.ts e2e/smoke/mobile/page-load-mobile.spec.ts --project=smoke --project=smoke-mobile` ×2 → `1 failed · 13 passed (2.6m)` ×2. The one failure is the 1280×800 overflow row below.

| gate | BEFORE | AFTER run 1 | AFTER run 2 | verdict |
|---|---|---|---|---|
| About outer height == Picker pane height ±1 px, 1440×900 | 7458.84 vs 684.77 | 684.77 vs 684.77 | 684.77 vs 684.77 | **GREEN ×2** |
| same, 1280×800 | 7567.98 vs 682.98 | 682.98 vs 682.98 | 682.98 vs 682.98 | **GREEN ×2** |
| same, 2846×1650-eq (1423×825 @2) | 7605.00 vs 684.77 | 684.77 vs 684.77 | 684.77 vs 684.77 | **GREEN ×2** |
| scroller = glass `FadingScroll` (y) inside the card, clipped by the radius (clipped screenshot) | scroller `null`; card itself `overflow hidden auto`, never scrolled | `.fading-scroll--y` clientHeight 683/681/683 vs scrollHeight 7604/7566/7603; card `overflow hidden hidden`, radius 16 px; scrolled mid-body with `window.scrollY` 0 | same | **GREEN ×2**; frames `W12-evidence/c/after/about-clip-{1440x900,1280x800,2846x1650-eq}.png` (rounded corners clip the scrolled body) |
| no page overflow, 1440×900 (the gate's named reading) | scrollHeight **7585** vs 900 | **900** vs 900 | 900 vs 900 | **GREEN ×2** |
| no page overflow, 2846×1650-eq | 7731 vs 825 | 825 vs 825 | 825 vs 825 | **GREEN ×2** |
| no page overflow, 1280×800 (the instrument's own stricter reading) | 7694 vs 800 | **809** vs 800 | 809 vs 800 | **RED ×2 — not the About pane's.** The Picker pane alone is 682.98 px, and the shell chrome (dock band, gaps, padding) is 126 px: 682.98 + 126 = 809. The About pane now equals the Picker exactly, so it adds 0 px. The cause is the Picker card's dead band (UIA-V-61, ~61 px between title and numerals), which is `.d`'s work (design pass: "no empty interval"). This test is `.d`'s born-RED reading at 800 px. |
| W6 g2 (Picker↔About + Mix control) | GREEN at open | `passed` | `passed` | **GREEN ×2** (RED ×2 on `38b35385`, cured `4b7639c0`, act 5) |
| guard: X-W5 cold-nav witness (`f55e59b7`) + O-16 | GREEN | passed | passed | GREEN ×2 |
| guard: O-21 space-catalog truth (reads `.about-card`) | — | passed | passed | GREEN ×2 |
| guard: mobile walk + page-load-mobile (one column: About present, grows the document) | — | passed | passed | GREEN ×2 (RED ×2 on `38b35385`, cured `4b7639c0`) |
| guard: vue-tsc demo | EXIT 0 | EXIT 0 | EXIT 0 | GREEN ×2 |
| guard: eslint (touched files) | — | clean | — | GREEN |
| O-18 "graph nodes" (touched `ColorNutritionLabel.vue`) | — | hover fill/ink leg passes; the test fails later on **component-name** ink 5.41 (light) / 4.36 (dark) < 5.75 | — | **PRE-EXISTING RED, not this unit's.** The same two numbers were read with `AboutPane.vue` written back to `a18cb1ca` (⟨cmd⟩ `-g "graph nodes"` → `Received: 5.41`, `4.36`, `2 failed`; the file was restored at once). The component-name ink is the guard-produced `componentInk`, which `.c` did not touch; UIA-V-204 names the same ink. It is routed with V-204 → `.u3`. |

A note on the first attempt: a manually started vite on `:9951` without the e2e env tripped page-load-mobile's zero-console-errors check (the MISCONFIGURED API banner) and a cold-nav plate reading. That server was stopped and both runs were re-taken on playwright's own server; those are the numbers above.

#### UIA-V about-pane rows — disposition (self-counted: ⟨cmd⟩ `grep -c '^#### UIA-V-[0-9]* · [A-Z]* · about-pane' docs/tranches/X/audit/UI-AUDIT-value.md` → **21**; table rows = 21)

| row | sev | disposition | where |
|---|---|---|---|
| UIA-V-4 | BROKEN | **CURED `a89fa035`** | empty tooltip layer and the action-less pointer cursor removed |
| UIA-V-62 | HIGH | routed → `.t` (color-space-select rows) | `demo/color-session/ColorSpaceSelector.vue` + `format-color`, outside `.c`'s set |
| UIA-V-63 | HIGH | routed → `.u3` | KaTeX display blocks are compiled markdown HTML (`Markdown.vue:301-306`). A glass `FadingScroll` cannot wrap generated nodes without a render-level change to the markdown pipeline, and copying glass's `.fading-scroll--x` classes onto them would be a consumer copy (LOCK). The clip is now visible inside the bounded card (frame `W12-evidence/c/after/page-1440x900.png`, right edge of the `a*` formula) |
| UIA-V-64 | HIGH | **CURED `38b35385` + `4b7639c0`** | the page is the viewport (900/900 at 1440×900); the Picker stays in view |
| UIA-V-65 | HIGH | routed → `.u3` | app-wide body face loading (`foundation.css:64-81` + the vite font plugin): a different concern from the About clip |
| UIA-V-202 | MEDIUM | O-59 (adopt via repin) | glass Alert is `rounded-lg` at 7.0.0 and `rounded-card` at HEAD; R-5 holds the pin at 7.0.0 |
| UIA-V-203 | MEDIUM | routed → `.u3` | heading scale: `Markdown.vue:157-182` raw `text-4xl…` literals plus `AboutPane` `text-title` h2 — one type-scale pass with V-474/V-477 |
| UIA-V-204 | MEDIUM | routed → `.u3` | dark accent in `useMarkdownColors.ts` + `componentInk`. The guard-produced ink is asserted by O-18 (R33/NG-2), so it needs its own contrast pass |
| UIA-V-205 | MEDIUM | routed → `.u3` | content: `assets/docs/oklch.md` vs `space-catalog.ts`, outside `.c`'s set |
| UIA-V-206 | MEDIUM | **CURED `b1bc7b52`** | hover lights only the hovered path |
| UIA-V-207 | MEDIUM | routed → `.u3` (design ruling) | the About title's selector is the O-21 oracle's `aboutTrigger`; removing it is a design decision, not a clip cure |
| UIA-V-208 | MEDIUM | O-59 (glass SelectContent collision padding) | GLASS |
| UIA-V-209 | MEDIUM | O-59 (cured at HEAD; adopt via repin) | GLASS |
| UIA-V-210 | MEDIUM | routed → `.t` | `format-color` (formatSpecimen), with V-62 |
| UIA-V-472 | LOW | O-59 + `.t` | GLASS SelectTrigger focus-return; the trigger is `.t`'s |
| UIA-V-473 | LOW | routed → `.u3` | the nutrition label's key/value grids onto glass primitives: a component rebuild |
| UIA-V-474 | LOW | routed → `.u3` | with V-203/V-477 (heading scale + outline) |
| UIA-V-475 | LOW | routed → `.e` | dock collapse on select-open, cross-seat |
| UIA-V-476 | LOW | O-59 + `.t` | GLASS SelectContent scroll-into-view |
| UIA-V-477 | LOW | routed → `.u3` | outline: needs a heading-level offset in the markdown renderer, since the guide's own h2s sit under the section headings |
| UIA-V-478 | LOW | routed → `.e` | dock Login/@user chips (`.e`'s pills) |

Totals: **3 CURED** (V-4, V-64, V-206) · **5 O-59** (V-202, V-208, V-209; the glass halves of V-472, V-476, whose consumer halves go to `.t`) · **13 routed** by id (`.t` 2: V-62, V-210 · `.e` 2: V-475, V-478 · `.u3` 9: V-63, V-65, V-203, V-204, V-205, V-207, V-473, V-474, V-477); 3 + 3 pure-O-59 + 2 split + 13 = 21, and `.u3` banks the final disposition (Unit plan).

#### Commits (this unit, pathspec each)
`bcee2e08` (instrument + BEFORE) · `38b35385` (row contract + glass scroller; UIA-V-64) · `a89fa035` (UIA-V-4) · `b1bc7b52` (UIA-V-206) · `4b7639c0` (root comment inside the root: the out-in regression cured; the instrument waits for the pane) · `a14d7186` (AFTER evidence ×2 + frames) · plus the record commit carrying this receipt.

**Adjacent edits:** none. **Locks:** glass untouched; `FadingScroll` is used as shipped, with no consumer copy of its rules. No masking: no try/catch, no skip, no allowlist, and no oracle or assertion edited. The row contract keeps P-5a's stretch pairing. The preserve-animations edict holds: no animation was touched. `scripts/dev/dev.sh` untouched. The temporary baseline worktree (seat scratchpad) was created and removed; the temporary single-file write-backs used for bisecting were restored with `git checkout HEAD -- <my file>` (`git status` clean for `demo/scenes` after each).

#### Residuals / hand-offs
- **`.d` (born-RED for it):** `w12-about-clip` "no page overflow" at 1280×800 reads 809/800 because of the Picker's own height (UIA-V-61's dead band). When `.d` lands, this test turns GREEN or reads the new number; it is not an About defect.
- **The row contract's coupling:** `shell.css`'s `@container pane-row` threshold writes `--pane-min` (25rem) and `--pane-gap` (`clamp(0.5rem, 1.25vw, 1.618rem)`) out as literals, because a size query cannot read custom properties. A change to either token must change that line too; the comment beside it says so.
- **Header veil over scrolled text:** the About pane now scrolls for the first time, so its sticky `PaneHeader` veil sits over moving content, as it already does on the Palettes, Mix and Gradient panes. The title stays legible over the blurred text (frames), and the veil is the shared `PaneHeader` design (`demo/shared/ui/`, outside `.c`'s set). No action is taken; it is noted for `.u3`'s pane-plates pass.
- **UIA-V rows:** 3 cured, 5 O-59, 13 routed by id (table above). `.u3` owns the final register.
- **Mail:** no new mail was read or sent by this unit (no glass row was minted; V-202/208/209/472/476 cite the existing O-59).

### X.W12.t

SERVED MODEL: claude-opus-5-5 (closing seat; the predecessor seat, same model, landed the code commits and was killed before its receipt).

**Crash-recovery.** ⟨cmd⟩ `git status --porcelain` → inside `.t`'s writable set only `?? docs/tranches/X/waves/W12-evidence/t/` (inherited: 16 before frames + `before-readings.json` + 2 list-row frames + 32 after frames, 3.4 MB). Judged against the spec (§0ci "before and after frames"): conforming, committed as-is. Inherited commits on `.t` (read whole by `git show`): `67bc5144` · `d40b574c` · `b98faac1` · `83d1c61f` · `f4ba97e9`. No uncommitted `demo/**` or spec edits were found.

**Census (OA-54 "every sibling found in a census").** ⟨cmd⟩ `grep -rln -e SelectTrigger -e DropdownMenuTrigger demo` → 11 files. The display-type triggers are those carrying `font-display`: only `demo/color-session/ColorSpaceSelector.vue`, mounted in two hosts (the Picker card title, and the About heading sentence as `.space-trigger--inline`). The rest are control-sized: `size="sm"` in GradientVisualizer ×3, MixConfigBar ×3 and GenerateControls ×2; `h-9 text-caption` in AuroraPane ×4; plus the dock, profile, card-menu and sort-menu triggers. None is a large display holder, so no surviving large holder needed the `--radius-field` re-corner. **Census = 1 component × 2 hosts.**

**Acts (in order).**
1. `67bc5144` — the trigger rides glass 7.0.0's own `SelectTrigger variant="ghost"` (its quiet/text register; already bound before this unit, and its paint is transparent, borderless and backdrop-free). The unit made two changes. The caret now draws at full ink (`--select-chevron-opacity: 1`), so hover and press affordance comes from ink and the chevron (UIA-V-479). The focus ring keeps glass's `--focus-ring-shadow` token but moves its corner from `--radius-md` to `--radius-field` (UIA-V-480; DESIGN.md:385-391, O-58). The same commit authors the gate `e2e/smoke/w12-text-trigger.spec.ts`.
2. `d40b574c` — UIA-V-5: the colour-space list is capped at reka's available width on a phone.
3. `b98faac1` — UIA-V-66 / -212: the list rows ride glass's row padding and its accent selection dot.
4. `83d1c61f` — UIA-V-211: out-of-gamut specimen captions are marked at rest.
5. `f4ba97e9` — INBOX **O-66 TEXT-TRIGGER-ROOT** filed beside O-59, covering two things. (a) Glass ≥8.0.0 removed the ghost arm with no alias, so the text register must exist at the root before the X-W7L repin; otherwise the title regresses to the owner's grey capsule (UIA-V-214). (b) At 7.0.0 the ghost arm composes `shadow-none` over `.focus-ring:focus-visible`, and value.js re-applies glass's own token until the root ships. No local trigger copy.
6. `d230f3c9` (this seat) — evidence: `W12-evidence/t/{before,after}/`.

**Gates (BEFORE → AFTER).** BEFORE is read from `W12-evidence/t/before/before-readings.json`, 4 cells. AFTER is ⟨cmd⟩ `VJS_E2E_PORT=9937 npx playwright test e2e/smoke/w12-text-trigger.spec.ts --project=smoke --headed`, run twice (8 tests = 2 viewports × 2 themes × 2 hosts).
| gate | BEFORE | AFTER |
|---|---|---|
| headed 1440 & 390, both themes: no background and no radius plate | bg `rgba(0, 0, 0, 0)`, bgImage none, border 0, box-shadow all transparent at rest and on hover, but the corner computed `9999px` (a latent stadium) | `8 passed (1.8m)` EXIT 0 · `8 passed (1.5m)` EXIT 0: paint-free at rest, on hover and while open |
| focus-visible paints a ring | a ring was painted (`--focus-ring-shadow`) on `6px` = `--radius-md` | a ring is painted, and its corner equals `--radius-field` (asserted < 9999) — GREEN ×2 |
| menu opens and selects by pointer and keyboard | — | Enter/ArrowDown/Enter and click/click-option: GREEN ×2 |
| before/after frames | 16 frames (`before/`) | 32 frames (`after/`, picker + about) — GREEN |
| guard: `npx vue-tsc -p tsconfig.demo.json --noEmit` | EXIT 0 (baseline) | EXIT 0 |

**Residuals / honest-RED.** **TEXT-TRIGGER-ROOT (O-66)**: the text register exists at 7.0.0, but it vanishes at the ≥8.0.0 repin, and the ghost focus-ring defect is producer-side. Both ride the relay. **GLASS-VEIL-GREY (O-62)**: the grey the owner saw is glass's veil wherever a glass plate remains. It was recorded here and not overridden. The trigger itself paints no plate at 7.0.0. **Adjacent edits:** none. **Escalations:** none.

### X.W12.d

SERVED MODEL: claude-opus-5-5 (effort high, design-author seat).

**Crash-recovery.** ⟨cmd⟩ `git status --porcelain` filtered to `.d`'s writable set → only ` M docs/tranches/V/reformation/CARRY-LEDGER.md` (outside the set; a sibling's). Nothing inherited. **E13 sweep** (six paths, `-newer INBOX.md`) → 0 everywhere; line appended with O-68 (`f02ae015`).

**Anchors verified at the bytes.** The dead band is `ColorComponentDisplay.vue` `.readout { min-height: calc(var(--readout-lines) * 1.12em); align-content: flex-end }` fed by `readoutReservation.ts` `READOUT_LINE_CAPACITY_CH = 11.7`. At that capacity lab (16.15ch), oklab (12.15ch) and oklch (12.85ch) derive 2 lines. The rung was `min(--type-display-4, max(11.65cqi, 2.618rem))` = **54.65px at 1440**, above the title's `--type-display-2` = **53.28px**, so the hierarchy was inverted. G-3: ⟨cmd⟩ `grep -rn "BLOB_IDLE_MS\|SLEEPY_POSE_MS\|idleTimer\|poseTimer" demo | wc -l` → **18**, all in `demo/picker/visual/HeroBlob.vue`. The glass 7.0.0 seam: ⟨cmd⟩ `grep -n settled node_modules/@mkbabb/glass-ui/dist/components/blob/Blob.vue.d.ts` → `63: settled: Readonly<Ref<boolean>>`. `useMetaballRenderer.d.ts:61-72` documents it as the demand gate's own quiescence predicate, and says a consumer parks its own idle logic only while `settled` is true. `blob.js:1399-1441` shows the demand gate: `shouldContinue` parks at `mood.isSettled() && pointer.isAtRest() && satellites.isQuiescent()` and arms a `setTimeout` wake for the next satellite event or auto-mood retarget. **7.0.0 ships the seam, so G3-PARK-CLOCK is not needed.**

**Design notes (frame 4, OA-24).** The owner asked three things. Should the dropdown title be larger? Should the numerals be larger with less padding? Why is there so much blank space? The readings, then the pass:
- *The blank space* was a reserved second line that paints nothing. At every one-line lab value the reservation was 61.22px of air (g1 RESIDUE), and the readout box was inflated by 55.41px. T.W4-2 anchored the reservation below the numbers, T.W6.5-P anchored it above, and the owner marked it both times. Moving it again cannot cure it. The only cure is to reserve nothing that does not ink.
- *The title is the card's display step.* It stays at `--type-display-2` (53.28px at 1440, 32.93px at 390), which is now the largest type in the card at every viewport. Before this pass the numerals, at display-4, were larger than the title. So the fix does not enlarge the title; it makes the numerals subordinate to it.
- *The numerals take the space.* The rung is now derived from **THE MEASURE**, the catalog's widest shown worst-case tuple (lab `100.0, -125.0, -125.0` = 16.15ch = 10.82em in the tnum cell). It is `font-size: min(var(--type-display-1), 100cqi / var(--readout-measure))`, so the widest space fills the header's line: 448px of 469px at 1440 (measured `sumEm` 10.70 × 41.89px). It is capped one ladder step below the title. Every shown space is therefore **one line by construction**. The lock is the painted line, and the card rect still never moves on a value change (card 609.27px at 1440 for both the reference and the worst-case value; 527.09px at 390).
- *Tighter padding.* The header went from ≈227px to **151.03px** at 1440 (from the UIA-V-61 reading to the measured value). The title row, the 7.17px `--picker-header-rhythm` gap and the numerals now stack with no band. The condensed-state readout re-key in `header.css` is retired, because the readout is already the compact one-line register at rest.
- **g1's largest-interval finding is DISCHARGED on the scale.** The largest interval is now `12px — figure → div.glass-quiet.card → ok · 2×--phi-0`, where it was `61.22px RESIDUE`.

**Acts (in order).**
1. `82ba99e1` — the hierarchy. `readoutReservation.ts` now derives `READOUT_MEASURE_CH` (the maximum `packWidth` over the catalog's shown channels, which is lab's 16.15) and `READOUT_MEASURE_EM` (× the 0.67em tnum cell), and sets the line capacity equal to the measure. In `ColorComponentDisplay.vue`, `.readout` changes to `font-size: min(var(--type-display-1), calc(100cqi / var(--readout-measure)))`. `--readout-measure` is bound, and the now-constant `--readout-fit` binding is dropped (`readoutFit` itself stays; `readoutLineCount` still calls it). `header.css` loses the condensed `.readout` re-key and its PRM transition line.
2. `dda1f85c` — G-3. In `HeroBlob.vue`, `BLOB_IDLE_MS`, `SLEEPY_POSE_MS`, `idleTimer`, `poseTimer`, `noteBlobActivity`, the `blobPaused` ref and `:paused` binding, `onScopeDispose` and the `immediate` watch are all retired. `onActivated` keeps only the re-seed, because the fresh `heroStops` reference is itself the producer's wake. `onEmergeEnd` loses its park guard. ⟨cmd⟩ `grep -rn "BLOB_IDLE_MS\|SLEEPY_POSE_MS\|idleTimer\|poseTimer" demo | wc -l` → **18 → 0**. No local engine was added.
3. `ed494f3c` — `e2e/smoke/w12-picker-blob.spec.ts` (new) has three legs: idle, hover and PRM. Each leg takes the frame diff of `.hero-blob-anchor` over an opaque test-only plate, captured after the engine's first WebGL draw. WebGL draw counts are logged beside it. PRM uses the house `page.emulateMedia` idiom (o9/o11); a describe-level `test.use({reducedMotion})` was measured *not* to emulate here (`matchMedia` → false).
4. `f02ae015` — INBOX: an E13 line and **O-68 BLOB-WAKE-SEAM**, filed beside O-56 G-3.
5. `e76087fb` + `4563cd5c` — the evidence in `W12-evidence/d/`. `before/` holds owner frame 4 and the UI-audit card frames (1440 and 390, dark and light), which were the served page before this pass. `after/` holds the card at 1440@2x and 390@3x, in dark and light, for the reference value `lab(38% 32 24)` and the worst case `lab(100% -125 -125)`, plus a frame-4 re-crop per viewport and theme. `readings/` holds the g1 and blob gate outputs and `after-card-readings.json`. The PNGs are force-added past `.gitignore:34 *.png` (4.2 MB) because the gate requires them committed.

**Adjacent edits (§0bt ADJACENT-LINE RULE)**, all in `82ba99e1` and all the same concern (the readout lock):
- `e2e/smoke/oracles/readout-seam.spec.ts` legs (i) and (ii), lines ~86-146. They asserted `lab keeps its honest 2-line lock` (`toBe(2)`), air-above ≥ reserved delta, and that the worst-case tuple inks 2 lines. That is exactly the dead band the owner and g1 condemn. They are re-stated to the measure law with the same measures and EPS: lock 1, dead band below ≤ EPS, air above ≤ EPS (strengthened from ≥ delta), and the worst case inks its one locked line. A dated ADDENDUM is in the header doc. No assertion was deleted, and leg (iii) is untouched.
- `test/readout-reservation.test.ts:28-30`. The mirror constant `CAPACITY_CH = 11.7` becomes `16.15`, the catalog measure. It mirrors the module constant this unit changed.

**Gates (BEFORE → AFTER).**
| gate | command | BEFORE | AFTER |
|---|---|---|---|
| g1 card rhythm (1440, `lab(38% 32 24)`) | `node docs/tranches/V/megatranche/audit/probes/x-w6/gate-card-rhythm.mjs` against dev :9001; AFTER ×2 on a freshly restarted server (STALE-SERVER LAW) | `GATE g1 — RED: 2 interval(s) fail.` ×2 (run 1 EXIT 1; run 2 read by its tail; matches the banked baseline ×2): `61.22px h3.card-title.readout content-top → span.readout-cell top RESIDUE` and `55.41px … INFLATED`; LARGEST `61.22px` | **`GATE g1 — GREEN`** ×2, EXIT 0 ×2; LARGEST `12px — figure → div.glass-quiet.card → ok · 2×--phi-0`; readout box 46.91px, lock 1 |
| blob frame-diff > 0 at 5 s idle | `VJS_E2E_PORT=9001 npx playwright test e2e/smoke/w12-picker-blob.spec.ts --project=smoke --workers=1` ×2 | idle **0.000/255 · 0 draws** (final instrument, run 2; run 1's idle leg did not boot, `toBeAttached` failed, the dist-churn flake below); earlier plated instrument run: 0.000/255 | **9.414/255 · 45 draws** · **9.518/255 · 46 draws**, `3 passed` ×2 |
| blob frame-diff > 0 on hover | same | 0.000/255 · 0 draws ×2 (final instrument) | **7.810/255 · 52 draws** · **5.449/255 · 31 draws** ×2 |
| "unless PRM" (the producer holds one frame) | same, PRM leg | 0.000/255 · 0 draws ×2 | 0.000/255 · 0 draws ×2 (unchanged, GREEN) |
| owner-frame re-crops + design notes committed | — | none | `e76087fb` + `4563cd5c` (17 PNGs + readings); design notes above |
| guard: `npx vue-tsc -p tsconfig.demo.json --noEmit` | ×1 after each landing | EXIT 0 | EXIT 0 (after `82ba99e1`; after `dda1f85c`, once the sibling-churned `dist/` had rebuilt its `.d.ts`) |
| guard: readout oracles | `npx playwright test e2e/smoke/oracles/readout-seam.spec.ts e2e/smoke/oracles/o10-type-locks.spec.ts --project=smoke` | — | `8 passed`, `1 failed`: readout-seam 3/3, O-10b line locks at 1440 and 390 GREEN (every space rendered ≤ lock, min-height ≡ lock × lh, no overflow), O-10c GREEN. The 1 failure is `o10-type-locks.spec.ts:145` (390, About), which times out on `getByRole('navigation').getByRole('button', {name:'About'})`: the 390 dock rendered its slug-entry layer, not the nav. That is outside the readout and outside this unit's diff; recorded, not cured |
| guard: `npx vitest run test/readout-reservation.test.ts` | — | 14/14 | 14/14 |
| after-card readings (double-run) | capture script ×2 | header ≈227px at 1440 (UIA-V-61); the numerals were larger than the title | header **151.03px** at 1440 and 119.09px at 390; title 53.28 / 32.93px vs numerals 41.89 / 25.89px; card rect identical for the reference and worst-case values (609.27 / 527.09px); lock 1 in all 8 cells ×2 |

**UIA-V rows (home-picker + blob-view, 37 rows = 20 + 17, self-counted; ⟨cmd⟩ `grep -n '^#### UIA-V-[0-9]* · [A-Z]* · \(home-picker\|blob-view\)' audit/UI-AUDIT-value.md`).**
- **Cured here:**
  - UIA-V-61, the 61px dead band and the orphaned `20.0` at 390: `82ba99e1`. One line at 1440 and at 390 by construction.
  - UIA-V-48, the consumer half: `dda1f85c`. The hero is no longer dead after 5.3 s.
  - UIA-V-199, the "costs header space" consumer half: `82ba99e1`. The header is 227 → 151px.
- **Relayed:**
  - The glass half of UIA-V-48 (config edits wake nothing) → O-68 BLOB-WAKE-SEAM.
  - The matte/orb look (UIA-V-199, glass half) → O-56 G-3, unchanged.
- **Out of this unit's bounds.** These go to `.u3`'s residue per the Unit plan ("every unhomed row"), because `.d`'s writable set is `demo/picker/**` + `demo/styles/**` and the blob pane lives outside it:
  - home-picker: UIA-V-2 (the space-select popover, `.t`'s surface)
  - home-picker: UIA-V-3 (the header condense scroll root), -60 (the ConsoleRail stadium), -193, -194, -195, -196, -197, -198 (O-62), -200, -201 (`.e`), -465, -466, -467, -468, -469 and -470 (About pane, `.c`'s surface), -471
  - blob-view: UIA-V-47, -160, -161, -162, -163, -164, -396..-402, -619..-621
- **Not re-dispositioned row by row here.** `.u3` owns the full self-counted table.

**Residuals.**
- **SEAT-390-TUPLE-REACH.** At 390 with the worst-case lab tuple, the bead's armed fission tail can momentarily cross the last figure. Frame: `after/390-dark-card-worst.png`. The old 2-line lock's reserved line had been standing in as the bead's clearance. The body clears the line. The tail is transient, and it comes from `HERO_FISSION_AMP` plus `orbitRadius 0.4` in the consumer's hero register. This is the ornament-footprint question UIA-V-199 routes to the owner. It is recorded and not cured, to keep the dead band from coming back.
- **O-68 BLOB-WAKE-SEAM** (producer). A save drip on a quiescent engine renders at the next scheduled wake.
- **Environment.** A sibling track's `src/css/bbnf` edits churned `dist/` through the `build:watch` watchers. The app was unbootable for about 5 minutes (00:53-00:57), and "Failed to fetch dynamically imported module HeroBlob.vue" appeared on a second dev server. Every reading above was taken after BOOT-OK. `demo/styles/foundation.css` HMR fired repeatedly throughout from a sibling seat; this unit never touched it.

**ESCALATION ESC-W12d-1: the W3-3 idle-park guards contradict this spec's gate.** W12.md's gate ("blob frame-diff > 0 at idle") plus G-3 retire the wall-clock park. Four standing specs assert that park. All are outside `.d`'s writable set, and their contract is larger than an adjacent line:
- `e2e/smoke/webgl-blob-idle.spec.ts:53` "0 un-gated idle rAF". AFTER: `blob drew 32 frames over 2500ms` → RED by design.
- `e2e/smoke/mobile/blob-presence-mobile.spec.ts:56`, the idle-parked leg.
- `e2e/smoke/perf/idle-frame-budget.spec.ts:48`.
- `e2e/smoke/oracles/o12-blob-seat.spec.ts:134` "parked bead answers a hover". It was already RED at BEFORE (0.01/255) and reads 0.87/255 AFTER; the decorative Blob has no pointer host at 7.0.0.
- The mirror in `e2e/smoke/fixtures/blob-timing.ts` (`BLOB_IDLE_MS`/`SLEEPY_POSE_MS` "=== HeroBlob.vue") is now dead.

They need a ruling. Either they are restated on the `settled` seam (the loop draws only while the engine is unsettled) with an idle budget re-derived for a live, fission-armed hero, or `HERO_FISSION_AMP` is tuned so the engine settles between beats. `.d` did not edit them. It did not flip or delete an assertion either.

**Verdict: DONE.** All three unit gates are GREEN ×2: g1, the blob idle and hover frame diff (with PRM holding), and the re-crops plus notes committed. G3-PARK-CLOCK was not needed, because 7.0.0 ships the `settled` seam and the park now rides it. g1's largest-interval finding is discharged on the scale. Commits: `82ba99e1` · `dda1f85c` · `ed494f3c` · `f02ae015` · `e76087fb` · `4563cd5c`, plus this receipt. Escalation **ESC-W12d-1** (the W3-3 idle-park guards) is for the wave's close to rule.

### X.W12.e

**Seat:** `claude-opus-5-5`, Track A, 2026-09-24, RESUME of a killed seat (host reboot, COHESION §0cn). Spec: W12.md:38-39 (§X.W12.e, OA-6/OA-22); :72-75 ADDENDUM (d) OA-55 DOCK-TRIGGER-CLIP (§0cj); the Unit plan row `.e`; UIA-V dock-* rows. Glass is READ-ONLY on pin ^7.0.0 (§0ci R-5).

**Crash-recovery.** ⟨cmd⟩ `git status --porcelain -- <writable set>` → `?? e2e/smoke/w12-dock.spec.ts` (350 lines) and, ignored, `W12-evidence/e/before/dock-trigger-{rest,hover,selected,focus-visible}.png`. I read the spec whole and judged it against W12.md:38-39 and :72-75. Its three legs conform: (1) pill plate and radius, (2) layer switches on DockCrossfade with a keyframe census and WebMs, (3) the trigger-clip frames and the ancestor-chain census. I finished it rather than rewriting it. The changes:
- The hover reading re-aims for up to 6 s until the plate settles. Before this, @mbabb read `oklab(0 0 0 / 0)` while holding `:hover` in 2 of 3 runs, because the row settles under the pointer.
- The census counts consumer keyframes only on the layer machinery (face, crossfade, layer host, dock body). An element's own feedback inside a face, such as ColorInput's mode flash on propose, is reported as `element:`.
- `TOP_FACE` excludes the nested sub-layer crossfade.
- There is a new `action-sub-layer` switch.
- The mobile-edit leg is rebuilt on a seeded local palette. The predecessor's add-slot path cannot work at 7.0.0, because the slot is a dead `SPAN` (O-15 BLOCKER).
- Line 1 is now `SERVED MODEL`.

I regenerated the before PNGs from the BEFORE run and committed them. Inherited paths: `e2e/smoke/w12-dock.spec.ts` and `docs/tranches/X/waves/W12-evidence/e/before/*.png`.

**BEFORE (measured before any edit).** ⟨cmd⟩ `VJS_E2E_PORT=9941 … npx playwright test e2e/smoke/w12-dock.spec.ts --project=smoke` → `2 failed · 3 passed (4.1m)`:
- (1) pills RED: `Login rest plate — Expected "rgba(0, 0, 0, 0)" Received "oklab(0.414855 0.00942762 0.0161359 / 0.6304)"`. Banked in `readings/before-pills.json`. Both pills rest and hover on the generic Button's filled plate, `oklab(… / 0.6304)`, where the dock control is transparent at rest and hovers on `color(srgb 0.2074 0.165013 0.1326 / 0.72)`. The squared inner plate shows in `before/dock-trigger-hover.png`.
- (2) tools-action-bar and login-slug-edit GREEN. mobile-edit RED: `waiting for locator('[aria-label^="Add current color "]')`, a dead instrument path.
- (3) GREEN: focus-visible true, 0 clipping ancestors in all 4 states.

⟨cmd⟩ `grep -rn "@keyframes" demo/shell/dock | wc -l` → **5**. The sub-layer swap in `layers/ActionBarLayer.vue:82-128` was a local `useLayerTransition` timer copy of the retired producer composable, 260 ms `setTimeout` (UIA-V-500).

**Acts (in order).**
1. **`038c795c` — the pills ride glass's dock controls (OA-22).** In `demo/shell/dock/menus/ProfileSection.vue`:
   - Login is now `<DockControl shape="tab">`.
   - The Profile and @mbabb menu triggers are now `<DockTrigger for="dropdown">`. It is glass's own `DropdownMenuTrigger`, with `.dock-trigger.dock-dropdown-trigger`.
   - The plate is now glass's: `background: transparent` at rest, `--dock-control-hover-bg` / `-press-bg` / `-active-bg`, and `border-radius: var(--dock-control-radius, var(--radius-pill))`, from `glass-ui/dist/components/dock/styles/controls/{tab-button,triggers}.css`.
   - The generic Button, with its filled squared plate and dead `borderColor`, is gone. The certified live-colour ink stays, on the `color` channel only.
2. **`13b81c30` — 0 local dock keyframes.** `action-pulse` and `action-spin` (ActionButton), `input-mode-flash` and `crown-appear` (ColorInput), and `lamp-dot-pulse` (DockStatusLamp) moved verbatim into `demo/styles/animations.css`, next to `vj-settle`. None was deleted, and the no-preference gate on the lamp's use stays in the SFC.
   - Finding: `crown-appear` was bound from an inline `style=` (`ColorInput.vue:38`). Vue's scoped compiler renames a scoped `@keyframes` with a hash and rewrites only the scoped block, so the crown's entrance could never match. It matches now.
3. **`04b1e822` — the Tools bar's actions ↔ input sub-layer rides glass's `DockCrossfade`.** In `layers/ActionBarLayer.vue` the swap is now `<DockCrossfade :active="activeSubLayer" reserve="inline">` over `<DockLayer id="actions">` and `<DockLayer id="input">`. The producer owns the opacity overlap on the one dock spring, the inert and `aria-hidden` on the inactive face, and the focus transfer. The local shim is retired (UIA-V-500 and UIA-V-244; SS-6/S-17).
4. **`bfc2e937` — the spec and evidence.** It commits `w12-dock.spec.ts` (finished as above) with before/after trigger frames (`git add -f`), 3 layer-switch WebMs, the DOCK-MORPH-ROOT probe `W12-evidence/e/morph-width-probe.mjs` and its ×2 JSONs.
5. **`26228e62` — the banked readings.** `W12-evidence/e/readings/{before,after-run1,after-run2}-{pills,trigger-clip}.json`.

**Adjacent edits (COHESION §0bt).**
- `demo/styles/animations.css`, the new block before the PRM guard, in `13b81c30`. It is the only home for the moved keyframes: the gate needs 0 `@keyframes` in `demo/shell/dock`, and the owner's law forbids deleting them. Global keyframes are centralized there (precedent: `vj-settle`).
- `e2e/smoke/flows/color-propose.spec.ts:77,101,107,111`, in `04b1e822`. The sub-layer oracle read the retired shim's `> .dock-layer` children; it now reads glass's `> .dock-face`. The same three-part law holds (one active face, the other `inert`), and no assertion is removed.

**Gates, BEFORE → AFTER (AFTER ×2, settled bytes at `26228e62`).** The instrument for both AFTER runs: ⟨cmd⟩ `VJS_E2E_PORT=9941 VJS_E2E_PERF_PORT=9942 W12E_OUT=… W12E_WEBM_DIR=…/e/after/webm W12E_FRAMES_DIR=…/e/after npx playwright test e2e/smoke/w12-dock.spec.ts e2e/smoke/flows/color-propose.spec.ts --project=smoke`. It read `2 failed · 7 passed (1.8m)` in run 1 and `2 failed · 7 passed (1.5m)` in run 2. The same two legs failed both times, and both are named below.

| gate | BEFORE | AFTER run 1 | AFTER run 2 | verdict |
|---|---|---|---|---|
| (1) Login + @mbabb: rest plate = the glass dock trigger's rest; hover = `--dock-control-hover-bg`; every radius ≥ h/2 | RED: rest `oklab(…/0.6304)` vs `rgba(0, 0, 0, 0)` | Login: rest `rgba(0, 0, 0, 0)`, hover `color(srgb 0.2074 0.165013 0.1326 / 0.72)` = token, h 38, r 9999. @mbabb: same, h 30.97 | identical | **GREEN ×2** |
| 0 local `@keyframes` in `demo/shell/dock`, moved not deleted | 5 | ⟨cmd⟩ `grep -rn "@keyframes" demo/shell/dock \| wc -l` → **0**; `grep -c "@keyframes" demo/styles/animations.css` → 11 (6 + 5) | 0 | **GREEN** |
| (2) layer switch rides DockCrossfade, with 0 consumer keyframes on the layer machinery | tools GREEN · login GREEN · no sub-layer leg | action-sub-layer · tools-action-bar · login-slug-edit: all GREEN, `consumer: []`, faces 6 | identical | **GREEN ×2 (3 of 4 switches)** |
| WebM per layer switch, committed | none | `after/webm/{action-sub-layer,tools-action-bar,login-slug-edit}.webm` | — | **3 of 4.** mobile-edit is honest-RED **MOBILE-EDIT-ENTRY** (below) |
| (3) DOCK-TRIGGER-CLIP frames (rest, hover, selected, focus-visible) + the ancestor-chain census | 0 clipping ancestors in 4 states; focus-visible true | 0 in 4 states (0 consumer, 0 producer); focus-visible true | identical | consumer half **GREEN ×2**. Honest-RED **DOCK-TRIGGER-CLIP** (O-63) is carried, see below |
| DOCK-MORPH-ROOT (O-55/O-56; O-64) | — | ⟨cmd⟩ `node W12-evidence/e/morph-width-probe.mjs http://localhost:9000 after/morph-width-run1.json` → tools `[479.5, 302]` · back `[302, 479.5]` · login `[479.5, 315]`: 2 distinct widths over 27-40 rAF frames, `morphingFrames 0` | identical (`run2.json`) | honest-RED **DOCK-MORPH-ROOT** ×2 |
| X-W5 cold-nav witness + W6 g2 | GREEN (Baseline) | ⟨cmd⟩ `npx playwright test e2e/smoke/oracles/cold-nav-scene-enter.spec.ts e2e/smoke/views/companion-pane-track-start.spec.ts --project=smoke` → `3 passed (56.8s)` | `3 passed (52.5s)` | **GREEN ×2** |
| close guard: vue-tsc demo | EXIT 0 | ⟨cmd⟩ `npx vue-tsc -p tsconfig.demo.json --noEmit` → EXIT 0 | EXIT 0 | **GREEN ×2** |
| `color-propose.spec.ts` (adjacent) | — | :34 and :89 (the sub-layer law, on `.dock-face`) pass; :128 fails `the propose submission never reached the network` | identical | pre-existing born-RED. It is the identical failure recorded at `X-W4.md:1485`, which the file's own header names as UIA-V-12's defect. Not this unit's |

**Honest-REDs and residuals.**
- **DOCK-MORPH-ROOT** (O-55/O-56; the producer row is O-64, registered at glass `5804d8cc`, I-47). The installed glass 7.0.0 swaps dock layers as an opacity crossfade with a measure-once peak reserve; its own doc says "NOT a per-swap FLIP". The dock's inline size therefore snaps in one frame, and `data-morphing` is never set on a layer switch (×2 above). An iOS-27 extent morph is producer work. No consumer copy was made.
- **DOCK-TRIGGER-CLIP** (O-63). I checked the consumer ancestor chain at 1440×900, dark, DPR 2, in all 4 states, ×2, and walked every ancestor below `<body>` for overflow, clip-path or paint containment. It holds **no consumer clip**, so there is nothing for value.js to cure.
  - At 7.0.0 the chain also holds **no producer clip** at rest, hover, selected or focus-visible. value.js composes `DockLayerGroup` → `DockCrossfade`, and the O-63 mechanism (`.dock-layer--full` `overflow: auto hidden`) is not in its chain.
  - I ran a one-off light-theme probe of the hover chain at the same size. It also read 0 clipping ancestors, and its frame is not committed.
  - The owner's frame is therefore not reproduced at these states. The one producer clip seen in glass's CSS is the morph-window `clip-path: inset(0 calc((1 - var(--dock-morph-t)) * 100%) 0 0)` on `.glass-dock[data-morphing] .dock-crossfade > .dock-face.is-active > .dock-face-content` (`dist/components/dock/styles/crossfade.css`). It applies only while the dock is morphing.
  - The id stays honest-RED per §0cj, to be re-read at X-W7L. No local padding or overflow override was added.
- **MOBILE-EDIT-ENTRY** (new id; the palettes surface is `.u1`'s). The dock's `mobile-edit` layer (`Dock.vue:168`, active when `!isDesktop && editTarget`) has no served entry at 390×844 on glass 7.0.0:
  - The current palette's add-slot is a dead `SPAN` (O-15 BLOCKER; `WatercolorDot` has `inheritAttrs:false`).
  - A seeded local palette's swatch popover paints its "Edit color" button off-screen. A bounded probe read `boundingBox` `{x:-189, y:879.48, w:28, h:28}`. The leg now fails in 5 s on `toBeInViewport` with the id in its message. It no longer times out after 180 s.
  - The popover placement is in `demo/palettes/**`, outside this unit's bounds and not an adjacent line. It is routed to `.u1` (palettes rows) and `.u3` residue.
- **Vitest note (not this unit's).** ⟨cmd⟩ `npx vitest run test/dock-status-lamp.test.ts test/status-lamp.test.ts demo/test/color-session/format-color.test.ts` → `1 failed | 86 passed`. The failure is `format-color.test.ts:180` "A1 · color-session/ColorSpaceSelector.vue reads through format-color". This unit's diff touches no `color-session` file. Both dock-lamp suites pass.

**UIA-V dock-* rows.** ⟨cmd⟩ `grep -c '^#### UIA-V-[0-9]* · [A-Z]* · dock' audit/UI-AUDIT-value.md` → **143** rows. They span **8** pages, not the plan's "7": dock-main 20 · dock-view-select 19 · dock-action-bar-color 19 · dock-color-input 21 · dock-slug-edit-layer 19 · dock-mobile-menu 15 · dock-profile-menu 17 · dock-mbabb-menu 13.
- **Cured here (7):**
  - UIA-V-69, -90, -94, -95, -228 (the pills), by `038c795c`.
  - UIA-V-500 (the sub-layer `setTimeout` crossfade), by `04b1e822`.
  - UIA-V-244, cured in part: the local crossfade shim is retired (`04b1e822`), and the scoped keyframes leave the SFC (`13b81c30`). Its "one-off vocabulary" half, joining the vj-morph/vj-celebrate families, is routed with UIA-V-501.
- **Not cured here:** UIA-V-504 (the off-stage MAIN layer hit-testable). Glass 7.0.0's `DockLayer` already renders `inert` on an inactive face (`dist/dock.js` `inert: !i.value || void 0`), so the row needs re-reading → `.u3`.
- **Producer (by relay):** UIA-V-67 and -68 (collapse/expand morph) → DOCK-MORPH-ROOT / O-55 / O-64. UIA-V-272 (glass HEAD vs 7.0.0) → X-W7L.
- **Not re-dispositioned row by row here.** Following `.d`'s precedent, the remaining 133 go to `.u3`, which owns the full self-counted table. Examples: UIA-V-501, the seat pulse is not on the spring vocabulary (moved, not re-tokenized); the slug-layer and identity behaviour rows -14..-21 and -83..-93; the 390 overflow rows -8, -9, -10, -17, -87 and -251.

**Mail (E13).** ⟨cmd⟩ `find <6 paths> -maxdepth 1 -type f -newer INBOX.md` → 0 everywhere except glass `BL/FORMATION-PROGRESS.md`. That file is glass's internal cursor (`f538a252`, redeploy), is not addressed to value.js and gets no row. **0 unrowed · 0 UNREAD in scope.** No new O-row is needed: DOCK-MORPH-ROOT (O-64) and DOCK-TRIGGER-CLIP (O-63) are already relayed, and this unit changed nothing at glass level.

**Verdict: PARTIAL.** Every cure the spec names landed and measures GREEN ×2: the pills on glass's dock control, 0 local dock keyframes (moved, none deleted), and the layer transitions, including the Tools sub-layer, on DockCrossfade. The X-W5 and g2 guards and vue-tsc are GREEN ×2. DOCK-MORPH-ROOT and DOCK-TRIGGER-CLIP are recorded honest-RED as the spec directs, with 0 consumer clips found. The one unmet gate is the fourth WebM (mobile-edit): its layer has no served entry, MOBILE-EDIT-ENTRY, whose cause lies in `demo/palettes/**`. Commits: `038c795c` · `13b81c30` · `04b1e822` · `bfc2e937` · `26228e62`, plus this receipt.
