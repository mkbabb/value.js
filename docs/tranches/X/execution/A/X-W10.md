SERVED MODEL: claude-opus-5[1m]

# X-W10 — Canonical Design and Motion · SEAT 0 (OPEN attempt) — **BLOCKED**

Track A (X·V). Spec: `docs/tranches/X/waves/W10.md` (read whole, 455 lines). Runbook: `../../EXECUTION-RUNBOOK.md`
§1.1 · §3.4 · §5. Rulings: `../../COHESION.md` §0i · §0j · §0k · §0k(F) · §0l · §0m · §0n · §0o (read to file end).

**Verdict of this seat: the wave does NOT open today.** The `Opens after` condition at the spec's own
§2 bytes is measured FALSE — five of its five named predecessors are `planned` at this clock. Nothing
was written into any §4 File-Bounds path; no gate baseline was banked; the unit plan below is authored
and banked so the real open resumes from bytes, not from memory.

## Open

- **Date**: 2026-09-18 (sitting date of record 2026-09-17 — the begin-word's; wall clock 2026-09-18).
- **Repo HEAD**: `fb8ca5d5` ⟨`git rev-parse --short HEAD` → `fb8ca5d5`⟩, branch `tranche-u`.
- **Seat**: 0 (OPEN), Opus 5. Authored zero source bytes; every command below is read-only.

### E13 Step-0 — the four-path mail sweep

Swept read-only at this seat's own clock and compared against **every row** of
`docs/tranches/V/coordination/INBOX.md` (188 lines), classification taken from each row's **status
cell**, never from a bare `grep -i UNREAD`.

| # | path | newest by mtime | rowed? |
|---|---|---|---|
| 1 | `docs/tranches/V/` + `docs/tranches/V/coordination/` | `valuejs-outbound-2026-09-18-kfw7-bh-relay.md` (Sep 18 01:41; `INBOX.md` itself self-excluded, SELF-COUNT law) | YES — **O-28**, `SENT 2026-09-18 — AWAITING REPLY by row id` |
| 2 | `../glass-ui/docs/tranches/BK/coordination/` | `valuejs-outbound-2026-09-18-kfw6-bh-relay.md` (Sep 18 00:31) | YES — **O-26**, `SENT + MIRRORED 2026-09-18` |
| 3 | `../keyframes.js/docs/tranches/V/coordination/` | `VALUEJS-INBOUND-2026-09-17-o8-o11-amendment-addendum.md` (ours, outbound) | YES — rowed (16 hits) |
| 4 | `../sci-report/atlas/docs/tranches/P/coordination/` | `valuejs-inbound-2026-07-27-library-band-export-delta.md` (Aug 3 15:01 — no new P-lane mail since 07-27) | YES |
| 4′ | bounded atlas **Q**-lane extension (the courier lane other seats carry) | `ATLAS-TO-VALUE-2026-08-03-RULINGS.md` (Aug 3 15:06) | YES — 7 hits; `ATLAS-TO-VALUE-2026-07-28-PASS2` = **I-31**, 5 hits |

**BK is still the newest glass tranche dir** ⟨`ls -1 ../glass-ui/docs/tranches/` → `… BH BI BJ BK` + the
alphabetic seniors; **BK is the maximum**⟩ — the stale-BI pin the runbook §5.3 names is not repeated here.

**Result: 0 unrowed · 0 new `I-n` minted (the register tail stays `I-34` / `O-28`) · 0 UNREAD in any
row's status cell.** Every 2026-09-17/18 letter in the four paths (the three glass BK outbounds
`constellation-o20-relay` · `bbnf-lang-9.0.0-addendum` · `valuejs-o20-disposition`, the 08-29 ACK, both
`kfw6`/`kfw7` BH relays, and the o8/o11 amendment addendum) already carries a row. **`INBOX.md` was not
written by this seat** — there was nothing to row, and a sweep line that rows nothing is appended at the
real open, not at a blocked attempt (a sweep note is evidence of an open; this seat did not open).

## Preconditions — MEASURED, and the blocking one named

Spec §2 `Opens after`: *"X-W5, X-W6, X-W7, X-W8, X-W9 stable (surviving structure fixed) AND the CC-104
precondition ruled at the X-W0 owner sitting."* §10 Depends-on restates it and adds X-W0 and X-W1.

| predecessor | required | ledger status (read at `docs/tranches/X/execution/LEDGER.md`) | record at `execution/A/` | verdict |
|---|---|---|---|---|
| X-W0 | ruled sitting | **CLOSED 2026-09-17** (honest-RED ESC-N1) | `X-W0.md` present | **MET** |
| X-W1 | §10 (CC-030 visual-oracle harness; G-5 and G-6 run inside it) | **OPEN 2026-09-17** | `X-W1.md` present, not IMPLEMENTED | **NOT MET** |
| X-W5 | stable | `planned` | absent | **NOT MET** |
| X-W6 | stable | `planned` | absent | **NOT MET** |
| X-W7 | stable | `planned` | absent | **NOT MET** |
| X-W8 | stable | `planned` | absent | **NOT MET** |
| X-W9 | stable | `planned` | absent | **NOT MET** |

⟨`ls -1 docs/tranches/X/execution/A/` → `X-W0.md` · `X-W1.md` · `X-W2.md` — **three records, none of them
W5..W9**⟩. ⟨`ls -1 docs/tranches/X/waves/` → `W0.md … W11.md` + `W1-LOG.md`; **no `W5-CLOSE.md`,
`W6-CLOSE.md`, `W7-CLOSE.md`, `W8-CLOSE.md`, `W9-CLOSE.md`** — none of the five waves has produced the
close artefact §8 of its own spec requires⟩.

**OP-1 / CC-104 is DISCHARGED** — `COHESION.md` §0k.2: *"CC-104 / OP-1 (X·V namespace, W10.md:368) — the
SUBSTITUTE, named in writing, with D-20 amended in the same ruling … **X-W10's open condition is
discharged by this ruling**."* DesignSync is not callable (`list_projects` → *"needs design-system
authorization — run /design-login"*, an owner act); the substitute is in-repo frames under
`docs/tranches/X/W10/frames/` rendered to committed PNG at 390 and 1280. **S-2** (GAB-5/K-8 per-verb
seat) is likewise ruled at §0k.3: **(a) the Dock set wins**. Both are cited here, neither re-opened.

**OP-2 / OP-3 / OP-4** are open owner marks *by design* (spec §2b) — they are carried as marks, never
picks, and are **not** open conditions.

### Why this is BLOCKED and not a conditional open

1. **The spec says so in terms**, §2: *"**Conditional-open is real, not ceremonial.** The wave describes
   surviving structure; authoring it before X-W5..X-W9 stabilize produces a canon anchored to a tree the
   next wave deletes — which is exactly the 12-of-13 stale-anchor defect this wave exists to end."*
   Opening now would manufacture the very defect **G-3** gates.
2. **Disjointness fails at the bytes.** §4a Cross-wave names five §4 paths held by predecessors —
   `demo/palettes/browser/card/PaletteCard/PaletteCard.vue` (X-W7, and §0k.3 **S-5** now gives X-W7
   `delete` on that very file), `demo/styles/animations.css` (X-W5), `demo/styles/foundation.css` (X-W5),
   `demo/shell/PaneSegmentedControl.vue` (X-W5 `delete`), `VISUAL-CONSTITUTION.md` +
   `OPTICAL-BENCH-COMPOSITIONS.md` (X-W5.e's E1/E2 strike) — *"every one **inherited after that wave's
   close**, which §2 Opens-after already requires."* X-W5 is unblocked today (its own predecessor X-W2 is
   CLOSED) and X-W7 opens on X-W1; either may open concurrently at any moment, and unit `.e` would then be
   writing a file another open wave is chartered to **delete**. The §5 unit `.e` sub-gate (G-6 on
   `PaletteCard.vue`) cannot be honestly turned against a file under an active delete verb.
3. **The runbook's arrow diagram is not the contrary authority it looks like.** §1.1 draws X-W10 as a
   child of X-W0 and §1.1's closing line says *"W1 · W2 · W3 · W9 · W4 · W6 · W10 are independent once W0
   closes"* — but the runbook's own rule of construction, §7: *"Eight ordering/gate disagreements were
   resolved at the spec bytes — **the specs govern**, not either draft"*, and its row 7 resolves exactly
   this shape **in favour of the spec's fuller `Opens after`** (KF.W10's `W2·W4·W5·W6·W7·W8·W9
   IMPLEMENTED` beat the shorter drawn edge). The ledger's `opens after` cell for X-W10 (`X-W0`) carries
   the diagram's abbreviation, not a ruling; **no §0i–§0o addendum amends W10's `Opens after`.** §0k.3's
   *"X-W10 (CC-104/OP-1, S-2) may open lawfully"* releases **those two blockers** — it is the sentence
   that clears the escalations, not a re-cut of the predecessor edge (the same sentence names X-W5, X-W6,
   X-W7, X-W8 and X-W9, none of which has opened, so it cannot be read as ordering W10 ahead of them).
4. **E-3.** Correcting the ledger's abbreviated `opens after` cell is a dated addendum for the
   orchestrator/owner, not this seat's edit of another wave's authored bytes. It is filed as **ESC-W10-1**
   below.

**BLOCKED-ON**: `X-W5 · X-W6 · X-W7 · X-W8 · X-W9 stable (spec §2 Opens-after) — all five \`planned\` at
2026-09-18; X-W1 (CC-030 harness, §10) still OPEN`.

### ESC-W10-1 — returned to the orchestrator/owner, not decided here

The ledger's Track-A `opens after` cell for X-W10 reads `X-W0`; the spec's §2 and §10 read
`X-W5 · X-W6 · X-W7 · X-W8 · X-W9` (+ X-W0, X-W1). The seat reads the spec as governing (runbook §7) and
**does not edit the ledger's `opens after` cell** — only its own status cell. If the orchestrator intends
the diagram's edge to govern, that is a ruling to write into COHESION as a dated addendum, with §2's
stale-anchor rationale and §4a's five inherited paths answered explicitly; until then the wave stays
scheduled last-but-two on Track A, beside X-W8 and before X-W11.

## Baseline — NOT BANKED (deliberately), with a provisional read-only reading

**The born-RED baseline of record is banked by the seat that actually opens this wave**, against the tree
X-W5..X-W9 leave behind. Banking it today would pin nine gates to a tree five chartered waves are about
to rewrite — the R.2 discipline read backwards. What follows is a **provisional reading only**, taken
read-only at `fb8ca5d5` to falsify the blocked verdict (if a gate had drifted GREEN, the wave's shape
would have changed and this seat would have said so). Every count below was run twice; both runs agree.

No Playwright/engine gate was run (probe parsimony §5.2; G-4/G-5/G-6 are engine gates whose evidence
belongs to the real open, and G-5/G-6 run inside X-W1's CC-030 harness, which is not yet landed).

| gate | limb measured today | reading at `fb8ca5d5` | vs the spec's 2026-08-03 baseline |
|---|---|---|---|
| **G-1** | `grep -ic` of the three terms over the four authorities | `demo/DESIGN.md` 0/0/0 · `VISUAL-CONSTITUTION.md` 0/0/0 · `PROPORTION-AUDIT.md` 0/0/0 · `PALETTE-CONTRACT.md` 0/0/0 | **RED, unmoved** (0/4 on all three) |
| **G-2** | `grep -c "VISUAL-CONSTITUTION\|PROPORTION-AUDIT\|PALETTE-CONTRACT"` over the four | 0 · 0 · 0 · 0 | **RED, unmoved** (0 in all 12 cells) |
| **G-3** | `[ -d demo/@ ]` · `find demo -name style.css \| wc -l` · `grep -c 'demo/@' demo/DESIGN.md` | **NO** · **0** · **5** | **RED, unmoved** (5 anchors into a directory that does not exist) |
| **G-4** | static co-measure only | `animation-timeline` = **4** declarations, all in `demo/shared/ui/PaneHeader.vue` (`:177` `@supports`, `:180`, `:186`, `:191`); blunt reduce-kill blocks at `demo/styles/animations.css:184` and `:202`; conforming `no-preference` blocks = **6** | **RED, unmoved**; engine arm NOT run |
| **G-5** | not measurable statically | — | engine gate; deferred to the real open (needs CC-030) |
| **G-6** | `grep -ic elevation` over the four authorities | 0/0/0/0 | **RED, unmoved**; pixel-diff arm NOT run |
| **G-7** | bare `38.1966011`/`61.8033989` in live canon | `OPTICAL-BENCH-COMPOSITIONS.md` **7** · `VISUAL-CONSTITUTION.md` **2** = **9**; demo source **0** files | **RED, unmoved** |
| **G-8** | `<SelectTrigger` sites · `h-9` · `Ad-18` · `variant="mono"` · `.underline-tabs` | **13** sites, **12** hand-set `h-9`; `Ad-18` = **2** (`demo/shell/PaneSegmentedControl.vue:45`, `demo/shell/dock/DockViewSelect.vue:57`); `variant="mono"` = **0**; `.underline-tabs` shipping at `demo/styles/foundation.css:554` | **RED, unmoved** — all four survivor counts reproduce exactly |
| **G-9** | line counts + `grep -ril tombstone` | 388 / 228 / 83 / 329 lines; satellites 167 (`docs/frontend-design/color-picker.md`) and 67 (`transition-inventory.md`); **0 tombstones** | **RED, unmoved** |

**GREEN-BEFORE-CURE: none.** All nine gates read RED (or are engine-deferred) — R.2 has nothing to
declare here.

**Bounds existence check** (all §4 paths, read-only): every one of the 13 named files is **PRESENT**;
`docs/tranches/X/design/` is **ABSENT** (the three phase-1/2 files are `create`, as §4 says).

## Unit plan

Six units, four ordered groups, peak concurrency **2** (owner cap §5.1; the spec's phase-3 trio yields to
it). Model seats are the spec's own §5 declaration (M-23): phase 1 twice-authored blind (`.a` Fable ∥
`.b` Opus), phase 2 a **fresh** Fable arbiter, phase 3 three Opus 5 implementation seats.

**Groups (ordered):** `[.a ∥ .b]` → `[.c]` → `[.d ∥ .e]` → `[.f]`.
No two concurrent units share a `modify` path: `.a`/`.b` create distinct files; `.d` owns the three
motion/spacing files; `.e` owns the one card; `.f` owns the satellites, `foundation.css` and the two
Ad-18 sites. Phase 1 and phase 2 commit before the next phase dispatches (§4b); phase 3 runs in the
sibling worktrees §4b names (`/Users/mkbabb/Programming/value-js-x-w10-{d,e,f}`), created by the
orchestrator with `git worktree add` after `git worktree list`.

**A standing first act for whichever seat opens this wave** (§0k.3 Consequence — *"each landing the dated
addendum its ruling names as its first docs act"*): land ONE dated E-3 addendum-**beside** `W10.md`
(never inside it) recording (i) CC-104/OP-1's substitute and D-20's amendment, including
`docs/tranches/X/W10/frames/` joining §4 File Bounds, and (ii) S-2's `(a) the Dock set wins`. **No seat
may write under `docs/tranches/X/W10/frames/` before that addendum lands** — until it does, that path is
outside §4 and the write is an escalation.

| unit | model | spec sections | writable set | gates | locks / same-commit |
|---|---|---|---|---|---|
| `X.W10.a` | fable | §5 `X.W10.a` (`W10.md:168-175`); inputs §3 (`:62-87`), §6 (`:253-370`) | `docs/tranches/X/design/draft-fable.md` (create); `docs/tranches/X/W10/frames/**` frame source, only after the addendum | none directly (feeds G-1/G-2/G-3) | blind-independence with `.b` (no shared skeleton, no shared mtime minute — falsifiable per `:182`); phase-1 commit is ONE commit with `.b` (§9) |
| `X.W10.b` | opus | §5 `X.W10.b` (`:177-183`); same inputs | `docs/tranches/X/design/draft-opus.md` (create); `docs/tranches/X/W10/frames/**` frame source, only after the addendum | none directly | as `.a`, mirrored |
| `X.W10.c` | fable | §5 `X.W10.c` (`:185-195`); §3 order (`:62-87`); §6 G-1/G-2/G-3 (`:259-290`) | `docs/tranches/X/design/APOTHEOSIS.md` (create), `demo/DESIGN.md` (modify), rendered PNG under `docs/tranches/X/W10/frames/` | G-1, G-2, G-3 | **fresh** seat (no sight of `.a`/`.b` authorship); one commit (§9 line 2) |
| `X.W10.d` | opus | §5 `X.W10.d` (`:197-210`); §6 G-4 (`:292-312`), G-5 (`:314-324`) | `demo/shared/ui/PaneHeader.vue`, `demo/styles/animations.css` (carve `:184`/`:202` only), `demo/picker/header.css` | G-4, G-5 | CC-099 ⊕ CC-101 land in ONE unit (§4a: *"`PaneHeader.vue` carries both … and is therefore ONE unit"*); `.d`/`.e` must not share a worktree |
| `X.W10.e` | opus | §5 `X.W10.e` (`:212-222`); §6 G-6 (`:326-337`) | `demo/palettes/browser/card/PaletteCard/PaletteCard.vue` | G-6 | nested-spread cure stays labelled **UNVERIFIED** until the pixel-diff; WELL material unchanged; NO `overflow:hidden` (`:16` refusal stands) |
| `X.W10.f` | opus | §5 `X.W10.f` (`:224-251`); §6 G-7 (`:339-348`), G-8 (`:350-358`), G-9 (`:360-369`) | `docs/tranches/V/VISUAL-CONSTITUTION.md`, `docs/tranches/V/OPTICAL-BENCH-COMPOSITIONS.md`, `docs/frontend-design/color-picker.md`, `docs/tranches/R/audit/R.W4-visual-runtime/transition-inventory.md` (tombstone header only), `demo/styles/foundation.css` (`.underline-tabs:554` survivor only), `demo/shell/PaneSegmentedControl.vue`, `demo/shell/dock/DockViewSelect.vue` | G-7, G-8, G-9 | enumeration is the FIRST act (the 8→7→7→8→9→11 drift); PSC row is **spent by deletion** if X-W5's delete landed first; one commit (§9 line 5) |

### Briefs (the exact acts, in order)

- **`.a`** — Fable seat via the `frontend-design` plugin, blind to `.b`. Read the tree, never memory.
  Author `draft-fable.md` whole in the brief's seven-section order: three laws → material/elevation →
  proportion/derivation → chroma fidelity → motion → product surfaces → anti-patterns. GOLDEN GLASS from
  lineage (warm-cream translucent glass at the floating tier; the φ register cited to the **named producer
  contract**, never to dist bytes; the four negative "gold" constraints) carried as a **MARK**, never
  enacted (OP-3). BREATH OF LIFE folds the owner sentence verbatim with the rest→hover→press→engaged→modal
  ladder and the hover-floor consequence (OP-4 a mark). MOVEMENT OF MOMENTUM cites `glass-ui/DESIGN.md`
  §-name (never a line number), states the rename so the owner's phrase greps, and ships the
  driver/observer curve as a **parameter awaiting OP-2**. Every load-bearing number re-measured in-seat
  with the command pasted. Line 1 = SERVED MODEL. No fourth transition family.
- **`.b`** — identical charter, Opus 5 seat, independently derived. Blindness is falsifiable (`:182`): do
  not read `.a`, do not adopt a shared skeleton; commit in the same phase-1 commit but author apart.
- **`.c`** — fresh Fable arbiter. Under L-14, **attempt refutation** of both drafts against the tree —
  never average them; record a named refutation of ≥1 load-bearing claim from each. Write
  `APOTHEOSIS.md`, then re-author `demo/DESIGN.md` whole in the ratified order, folding
  `vnext/DESIGN-PROGRAM.md` §-named six-tier motion table wholesale (read-only source; `vnext/**` is
  FROZEN-AS-INHERITED). Anchors by **section name only** — G-3 accepts nothing else. Wire the citations
  G-2 needs (≥1 megatranche registry file, ≥1 demo source comment). Render the OP-1 substitute frames to
  PNG at 390 and 1280 with ONE bounded Playwright pass, committed with sha256s. Turn G-1/G-2/G-3 and paste
  RED-before/GREEN-after.
- **`.d`** — Opus. Move the three declarations at `PaneHeader.vue` (`:177-193` at authoring; **re-locate by
  content, never by the stale line number**) inside `@media (prefers-reduced-motion: no-preference)` — the
  house idiom already at 6 blocks across 5 files — then delete the two blunt reduce-kill blocks at
  `animations.css:184` and `:202`. Structure, not a stacked override (L-8). Give
  `pane-title-shrink`/`pane-desc-shrink` a **spacing channel** so block padding and title→content gap
  collapse proportionally; `picker/header.css`'s condense rung follows the same register. Re-run
  `prm-scroll-timeline.mjs` unmodified, both engines × both preference states; pin post-cure values **per
  engine** (0.311 WebKit / 0.618 Chromium). File the G-5 DELTA pair at 1440×900 and 390×844 against
  `OM-18-card-shrunken-state-margins.png`. `vj-*` family count stays **three**.
- **`.e`** — Opus. Apply `.c`'s ruled elevation/silhouette treatment at `PaletteCard.vue`, the sole
  `cartoon-cast`/`cartoon-surface` consumer. Consumer-side composition only — `.cartoon-cast` is
  producer-owned and `node_modules/@mkbabb/glass-ui/**` is READ-ONLY ALWAYS; if the geometry cannot be
  composed consumer-side that is a **scope reveal → Triumvirate (§3a)**, never an edit. Keep the WELL
  material (bg-well, Q4/T.W3-1 rung-2) unchanged; a cure that blurs the cast fails the material clause.
  Pixel-diff against `OM-2-card-shadow-sharp-corners.png` (sha256 `2e223c1a…3de8`) + OM-11/OM-12; label
  the nested-spread cure **UNVERIFIED** until that diff proves it.
- **`.f`** — Opus. **First act: the enumeration** — re-measure the four DR-13 survivors at open and paste
  the commands, then one terminal verb each (landed-as-local with owner-accepted rationale, or deleted;
  PEER-AUTHORSHIP-REQUIRED is not a state), each with its add/remove event recorded. `variant="mono"` = 0
  → DELETE the ask. The PSC `Ad-18` marker is discharged **by X-W5's deletion** if that landed — that
  deletion IS the recorded event. Replace all 9 bare `38.1966011`/`61.8033989` lines in live canon with a
  citation to the **named producer contract** (not dist bytes — CC-112 stays in the glass bank).
  Tombstone `VISUAL-CONSTITUTION.md`, `docs/frontend-design/color-picker.md` and `transition-inventory.md`
  (header only on the sealed R-tranche record), each naming what moved where; re-measure any
  PROPORTION-AUDIT number **before** folding it (the 3.4× undercount). CC-018 is OPTIONAL and ungated —
  owner word only.

### Standing triumvirate triggers for this wave (§3a)

Any write outside §4 (in particular `node_modules/@mkbabb/glass-ui/**` or a fourth demo source file) ·
G-4 green in one engine and red in the other (divergence in **sign**, not magnitude) · G-6's pixel-diff
still failing after the nested-spread cure · any third diagnose→edit→re-measure iteration on one gate.
The orchestrator may not redispatch the failing unit alone.

## Unit receipts

_(empty — no unit dispatched; the wave did not open.)_

## Close

_(not written — see Verdict.)_

---

# ATTEMPT 2 — 2026-09-18 23:4x EDT, SEAT 0 re-open probe — **STILL BLOCKED**, on a narrowed condition

SERVED MODEL: claude-opus-5[1m]

**Dated addendum-beside (E-3): nothing above this line is edited.** Attempt 1's bytes stand as the
historical record; this block re-measures the same conditions at a later clock, because four sibling
seats have moved the tree since (X-W1 promoted to CLOSED, X-W3 CLOSED, X-W9 opened and took three
adversarial checks). Sitting date of record stays **2026-09-17** (the begin-word's); wall clock
2026-09-18.

**Crash-recovery (standing law), first act.** ⟨`git status --porcelain`⟩ → 16 rows: 10 `demo/**`
(`PalettesPane.vue`, five `browser/admin/*Panel.vue`, `VersionHistoryDrawer.vue`,
`SearchFilterBar.vue`, `ConsoleRail.vue`, `SlugEditLayer.vue`), `docs/tranches/V/reformation/CARRY-LEDGER.md`,
`docs/tranches/X/COHESION.md`, `scripts/dev/dev.sh` (unowned, never touched), and three untracked
(`docs/tranches/X/waves/evidence/W4/`, two `e2e/smoke/**/a11y-control-targets.spec.ts`).
**None is inside this seat's writable set** (`execution/A/X-W10.md` · `execution/LEDGER.md` ·
`V/coordination/INBOX.md`), every one belongs to a sibling seat, and all three of this seat's own
paths are **clean** ⟨`git status --porcelain -- docs/tranches/X/execution/A/X-W10.md docs/tranches/X/execution/LEDGER.md docs/tranches/V/coordination/INBOX.md | wc -l` → `0`⟩.
No predecessor work was inherited; nothing was stashed, restored or touched.

**Repo HEAD** at this probe: `4d4017f1` at the sweep, `cc488e6e` at the write (siblings committing
concurrently on the shared index — both readings recorded rather than one averaged).

## Open (attempt 2)

### E13 Step-0 — the four-path mail sweep, re-run at this seat's own clock

Classification taken from each row's **status cell**, never from a bare `grep -i UNREAD`.
`INBOX.md` self-excluded per the SELF-COUNT law.

| # | path | newest by mtime | rowed? |
|---|---|---|---|
| 1 | `docs/tranches/V/` + `V/coordination/` | `parse-that-inbox-2026-09-18-value-4.1-evidence-addendum-2.md` (Sep 18 22:11) | YES — **O-38**, `SENT 2026-09-18 (authored + rowed)` |
| 2 | `../glass-ui/docs/tranches/BK/coordination/` | `glass-outbound-2026-09-18-valuejs-o26-reply.md` (Sep 18 17:18) | YES — **I-35**, `READ 2026-09-18` · `READ + CONSUMED WHOLE 2026-09-18 17:1x EDT (KF.W6 unit .l)`; routed **X·KF, NOT X-W1** |
| 3 | `../keyframes.js/docs/tranches/V/coordination/` | `VALUEJS-INBOUND-2026-09-17-o8-o11-amendment-addendum.md` (ours, outbound) | YES — rowed |
| 4 | `../sci-report/atlas/docs/tranches/P/coordination/` | `valuejs-inbound-2026-07-27-library-band-export-delta.md` (Aug 3 15:01 — unmoved) | YES |

**BK re-confirmed the newest glass tranche dir** ⟨`ls -1 ../glass-ui/docs/tranches/`⟩ — `BH BI BJ BK`
is the alphabetic maximum among the two-letter B-series.

The five 09-18 22:0x letters in our own coordination dir (`keyframes-inbox … -4.1-cut-notice`,
`atlas-inbox … -export-delta-refresh`, `glassui-inbox … -4.1-r1-relay`, `fourier-inbox … -facility19-delta`,
`parse-that-inbox … -evidence-addendum-2`) are **ours outbound**, rowed **O-34 … O-38**, each `SENT`;
`valuejs-outbound-2026-09-18-kfw7-bh-relay-ADDENDUM-A9.md` is rowed (7 hits).

**Result: 0 unrowed · 0 new `I-n` minted (register tail stays `I-35` / `O-38`) · 0 UNREAD in any row's
status cell.** A dated sweep line is appended at `INBOX.md`'s file end for this probe.

### Preconditions — RE-MEASURED; two moved, four did not

Spec §2 `Opens after`: *"X-W5, X-W6, X-W7, X-W8, X-W9 stable (surviving structure fixed) AND the
CC-104 precondition ruled at the X-W0 owner sitting."* §10 restates it and adds X-W0 and X-W1.

| predecessor | required | ledger status cell (read at this clock) | record at `execution/A/` | verdict | Δ vs attempt 1 |
|---|---|---|---|---|---|
| X-W0 | ruled sitting | **CLOSED 2026-09-17** (honest-RED, ESC-N1) | `X-W0.md` | **MET** | — |
| X-W1 | §10 (CC-030 harness; G-5/G-6 run inside it) | **CLOSED 2026-09-17** (honest-RED: G-17 · G-19 · G-20), promoted at CHECK 2 | `X-W1.md` | **MET** | **CHANGED — was `OPEN`, now met** |
| X-W5 | stable | `planned` | absent | **NOT MET** | — |
| X-W6 | stable | `planned` | absent | **NOT MET** | — |
| X-W7 | stable | `planned` | absent | **NOT MET** | — |
| X-W8 | stable | `planned` | absent | **NOT MET** | — |
| X-W9 | stable | **PARTIAL 2026-09-17** — CHECK 3 (2026-09-18) returns **NOT-CONFORMANT, 20 GREEN / 13 RED, two HIGH rows uncured** | `X-W9.md` present | **NOT MET** | **CHANGED — was `planned`; now opened but explicitly NOT stable** |

⟨`ls -1 docs/tranches/X/execution/A/` → `X-W0.md · X-W1.md · X-W10.md · X-W2.md · X-W3.md · X-W4.md ·
X-W9.md` — **seven records; none of W5, W6, W7, W8**⟩.
⟨`ls -1 docs/tranches/X/waves/ | grep -i close` → **nothing**; no `W5-CLOSE.md … W9-CLOSE.md` exists,
so not one of the five has produced the §8 close artefact its own spec requires⟩.
⟨`git worktree list` → 8 worktrees, none named `value-js-x-w10-{d,e,f}`; the live sibling worktrees are
X-W1's (`…-x-w1-c`, `…-x-w1-d`, `…-x-w1-g7`) and X-W9's (`…-x-w9-c`)⟩.

**X-W9's new state does not help the gate; it sharpens it.** "Stable (surviving structure fixed)" is a
*structure* predicate, and X-W9's own row records the opposite at three independent adversarial passes
— PARTIAL, `the cured surface landed, the 4.1.0 cut was not taken`, 13 RED reproducing at CHECK 3 with
two HIGH rows standing. A canon authored against a library surface that is still moving is the exact
12-of-13 stale-anchor defect **G-3** exists to end (§2's own words).

**Unchanged from attempt 1, and not re-litigated**: **CC-104 / OP-1 DISCHARGED** by COHESION §0k.2
(DesignSync not callable; the substitute is in-repo frames under `docs/tranches/X/W10/frames/` rendered
to committed PNG at 390 and 1280, D-20's frames obligation amended in the same ruling); **S-2** ruled at
§0k.3 — **(a) the Dock set wins**. **OP-2 / OP-3 / OP-4** are owner marks by design (§2b), never open
conditions.

### ESC-W10-1 is STILL UNRULED at COHESION's file end

⟨`grep -n "^## " docs/tranches/X/COHESION.md | tail -1` → `1529:## §0t ADDENDUM 2026-09-19 — KF.W10
OWNERSHIP CURED …`⟩. The file was read to its end (1,540 lines, working-tree state included). The
addenda since attempt 1 — **§0p** (X.P.W3), **§0q** (X.P.W3.f), **§0r** (X.P.W3 third close), **§0s**
(X.P.W3.h), **§0t** (KF.W10 ownership) — are Track C/D and Track B acts; **none names X-W10, none
amends its `Opens after`, and none answers ESC-W10-1.** §0k.3's Consequence line (*"X-W10 (CC-104/OP-1,
S-2) may open lawfully"*) is re-read at this clock and reads as attempt 1 read it: it clears **those two
escalations**, and the same sentence names X-W5 · X-W6 · X-W7 · X-W8 · X-W9 as also-releasable, so it
cannot be read as ordering W10 ahead of five waves it releases in the same breath. The ledger's
`opens after` cell is again **left unedited** (runbook §7: the spec bytes govern an ordering
disagreement).

## Baseline — re-read READ-ONLY (still NOT banked; banking belongs to the real open)

Every limb below re-run at this clock and **double-run**; both runs agree. No engine/Playwright pass was
taken (probe parsimony §5.2). This is a falsification probe of the blocked verdict, not the wave's
born-RED bank — pinning nine gates to a tree five chartered waves will rewrite is R.2 read backwards.

| gate | limb | reading now | vs attempt 1 / the spec's 2026-08-03 baseline |
|---|---|---|---|
| **G-1** | `grep -ic` × 3 terms × 4 authorities | `0` in all **12** cells | **RED, unmoved** |
| **G-2** | `grep -c 'VISUAL-CONSTITUTION\|PROPORTION-AUDIT\|PALETTE-CONTRACT'` × 4 | `0 · 0 · 0 · 0` | **RED, unmoved** |
| **G-3** | `[ -d demo/@ ]` · `find demo -name style.css \| wc -l` · `grep -c 'demo/@' demo/DESIGN.md` | **NO** · **0** · **5** | **RED, unmoved** |
| **G-4** | static co-measure | `animation-timeline` = **4**, all `demo/shared/ui/PaneHeader.vue` (`:177` `@supports`, `:180`, `:186`, `:191`); reduce-kill blocks at `animations.css:184` and `:202`; conforming `no-preference` blocks = **6 across 5 files** | **RED, unmoved**; engine arm NOT run |
| **G-5** | engine gate | — | deferred to the real open (runs inside X-W1's CC-030 harness, now landed) |
| **G-6** | `grep -ic elevation` × 4 | `0 · 0 · 0 · 0` | **RED, unmoved**; pixel-diff NOT run |
| **G-7** | bare `38.1966011`/`61.8033989` in live canon | `OPTICAL-BENCH-COMPOSITIONS.md` **7** · `VISUAL-CONSTITUTION.md` **2** = **9**; demo source **0** | **RED, unmoved** |
| **G-8** | four DR-13 survivors | `<SelectTrigger` **13** sites / **12** hand-set `h-9` · `Ad-18` **2** (`PaneSegmentedControl.vue:45`, `DockViewSelect.vue:57`) · `variant="mono"` **0** · `.underline-tabs` shipping at `foundation.css:554` | **RED, unmoved** — all four reproduce exactly |
| **G-9** | line counts + `grep -ril tombstone` | `demo/DESIGN.md` 388 · `VISUAL-CONSTITUTION.md` 228 · `PROPORTION-AUDIT.md` 83 · `PALETTE-CONTRACT.md` **365** · satellites 167 / 67; **0 tombstones** | **RED, unmoved** |

**GREEN-BEFORE-CURE: none.** R.2 has nothing to declare at this clock either.

**One measured drift, recorded (not a gate move):** `PALETTE-CONTRACT.md` is **365** lines today against
the spec's 2026-08-03 baseline of **329** (+36; mtime Sep 18 18:43, a sibling wave's write). Its
gate-bearing cells are unchanged — `gg/breath/momentum = 0/0/0`, cross-citation `0`, `elevation 0` — so
G-1, G-2, G-6 and G-9 all still read RED for their intended reasons. The seat that really opens this
wave must re-take G-9's four line counts at its own clock rather than inherit the spec's.

## Unit plan (attempt 2)

**Unchanged and carried by reference** — the six units, four ordered groups, peak concurrency 2, the
§4b worktree trio, the standing E-3 addendum-first act (§0k.3 Consequence), and the §3a triumvirate
triggers are exactly as banked above in attempt 1. Re-authoring them would manufacture a second version
of the same plan; **one open, one plan**. Two notes for the seat that inherits it:

1. **X-W1 is now CLOSED**, so the CC-030 visual-oracle harness G-5/G-6 run inside exists — that
   dependency is discharged and the two engine gates are measurable at the real open.
2. **§4a's five inherited paths still have no close to inherit from.** `PaletteCard.vue` (X-W7, which
   §0k.3 **S-5** now gives `delete`), `animations.css`, `foundation.css`, `PaneSegmentedControl.vue`
   (X-W5 `delete`) and the two V-tranche canon docs (X-W5.e's E1/E2 strike) are all still held by
   `planned` waves. Unit `.e` cannot honestly turn G-6 against a file another chartered wave will
   delete, and unit `.f`'s PSC row cannot record "spent by deletion" until that deletion exists.

**BLOCKED-ON**: `X-W5 · X-W6 · X-W7 · X-W8 planned AND X-W9 not stable (PARTIAL, CHECK 3
NOT-CONFORMANT 2026-09-18) — spec §2 Opens-after; X-W0 and X-W1 are now both MET`.

## Unit receipts (attempt 2)

_(empty — no unit dispatched; the wave did not open.)_
