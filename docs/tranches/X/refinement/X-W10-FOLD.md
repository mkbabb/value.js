SERVED MODEL ID (fold author, line-1 declaration): claude-opus-5[1m]

# X-W10 REFINEMENT FOLD — the value.js corpus onto "Canonical Design and Motion"

## 1. Header — provenance, posture, the reading rule

**Provenance.** The value.js corpus = every file in
`docs/tranches/V/megatranche/registry/adjudicated/` that is NOT `kf-*` / `fr-*` / `pt-*` —
**88 component apotheoses + `parser-band.md` · `library-band.md` · `chassis-fitness.md` ·
`layout-gestalt.md` + `delta-r3/`**. This seat read every record for the 68 slugs routed to this
wave and additionally grepped the WHOLE corpus for `X-W10` / `X.W10`: **69 files carry at least one
X-W10 routing** (`AdminFlaggedPanel.md` is the one carrier outside the assigned slug list;
`delta-r3/` carries none). This addendum is the M-23/M-27 ¶2 fold of that set onto `waves/W10.md`.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0.md..W11.md` and
`CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is the fold's **sole write**. Where a
row here STRIKES a phrase in a dated spec, the strike is recorded here and the spec's bytes stand
untouched. The dated record is immutable; the addendum is the later adjudicated word.

**The reading rule.** At execution X-W10 reads **`waves/W10.md` PLUS this addendum**, in that order;
where the two disagree the addendum's clause governs. §4 (BoundsDelta) is authoritative for what the
wave may open — a row whose file is not in W10's §4 and not in §4 here is **NOT EXECUTABLE**, and
saying so is this fold's job, not the executor's discovery. Nothing here re-stages or re-weights
W10's nine gates; §3 sharpens their predicates and proposes new ones.

**Fold classes.** **cl.1** = spec sharpening / law / correction · **cl.2** = cure-lock or binding
rider · **cl.3** = sequencing edict · **cl.4** = owner mark / canon reconciliation (input, never
enacted here) · **cl.5** = bounds or ownership defect.

**Anti-rename.** Banked ids are original for life. Every row below carries its source id(s); no row
mints a second name for an already-banked identity. Where two records book one fact, the row is
**MERGED CROSS FOLD** with all witnesses on its ⟨…⟩ line and is counted ONCE.

**Arithmetic — the OUT side, countable from this file's own bytes.** **86 rows** (§2, contiguous
X-W10.1..X-W10.86) · **27 gate rows** (§3: **9** existing W10 gates sharpened + **18** born-RED
candidates G-10..G-27) · **19 bounds-delta rows** covering **22 newly-named files + 2 carve
widenings + 3 recorded NOT-ADDED families** (§4) · **14 cross-edges** (§5). The IN side (how
many source rows collapsed into the 86) is NOT asserted as an identity: the corpus rows are
enumerated on the ⟨…⟩ lines and are falsifiable there, one row at a time.

**ARITHMETIC SUPERSEDED 2026-08-28 by §6h — the ROUND-1 row-carriage repair.** The paragraph above
stands as the **round-0** record (the counts the pass-1 check verified: gapless X-W10.1–86, gate
Σ 27, zero VERIFIED stamps) and is **not** re-written. The file's **new true counts** are
**103 rows** (contiguous X-W10.1..X-W10.103) · **36 gate rows** (9 sharpened + **27** born-RED,
G-10..G-36) · **27 bounds-delta rows** (§4 rows 1-19 + §6f rows 20-27) · **17 cross-edges**
(§5 edges 1-14 + §6g edges 15-17, plus two relay lines on edge 14). §6 adds **17 rows**: the
assignment's **4** NO-WAVE-OWNER + **7** census escapes (2 declared overlaps folded once, both
limbs) + **8** ceiling finds this seat's own fresh id-keyed census reached and the roster did not.
Dated, never silent.

### 1a. Corrections and challenges — recorded, never silent

1. **AB-22's census is CORRECTED UPWARD by this seat's own re-run (2026-08-28).** AboutPane AB-22,
   AdminPane L-12 and `motion-quarantine.md:161-162` each enumerate **nine** `.pane-scroll-fade`
   hosts; AB-22 grades them "4 `mx-auto` / 4 not / ConfigSliderPane divergent" and wb-extract-pane
   XP-17 sharpens that to "THREE drift variants, not two". Re-run at HEAD, the class strings are
   **FIVE distinct**, not three: `w-full mx-auto overflow-y-auto overflow-x-hidden min-w-0 h-full`
   ×3 (`BrowsePane:2`, `AdminPane:2`, `PalettesPane:2`) · the same **plus `about-card`** ×1
   (`AboutPane:4`) · the same **minus `mx-auto`** ×3 (`GradientPane:20`, `GeneratePane:31`,
   `ExtractPane:5`) · the same **plus `relative`** ×1 (`MixPane:62`) · and
   `pane-scroll-fade scrollbar-thin flex-1 min-w-0 overflow-y-auto overflow-x-hidden` on an **inner
   `div`, not the Card root**, with no `w-full` and no `h-full` (`ConfigSliderPane:106`). The
   nine-host count holds; the disagreement is worse than any single record filed it.
2. **AdminPane's MQ-1 row is routed to a wave that does not exist in X.** `AdminPane.md:55`
   dispositions the PRM header inheritance to **"W47"** — a V-prime label, not an X wave, and the
   record flags the routing as its own seat's. Folded here at **X-W10.3** with the other eight
   witnesses; the W47 label may not gate.
3. **PH-1 has no CARRY-CUT-LEDGER row, re-verified.** `grep -n 'atan2\|shrink-ratio'` over
   `registry/CARRY-CUT-LEDGER.md` and all twelve `waves/*.md` → **0 hits** (this seat's run). The
   BLOCKER this wave must cure is not in the ledger the wave transposes. Recorded as a **ledger
   gap**, not repaired here (E-3).
4. **The ConsoleRail record ORDERS an adjudication this fold must make.** `picker-componentsliders-
   consolerail.md:121` — "One cross-booking divergence flagged for the X-wave authoring: this record
   routes the keyboard-step row (MISS-10) → X-W10 as the least-count-law identity; the parent record
   routes the same defect (its R-28) → X-W6 as a consumer cure. Both anchors are real; **the collapse
   must be written, not silent**." Written at **X-W10.60**.
5. **Three records disagree on who owns the forced-colors roster** (R-26 → X-W6 canon-echo-W10 ·
   EC-8 → NO-WAVE-OWNER "foundation.css's forced-colors block exceeds X-W10's declared carve" ·
   GEN-32 → X-W10.f "foundation.css owner"). EC-8 is right about the bytes: W10's §4 carves
   `foundation.css` at **`.underline-tabs:554` survivor only**. Resolved at **§4 row 10** by
   widening, not by re-routing.
6. **Prose mention is not ownership.** `W10.md:305-307`'s G-4 baseline names `DockStatusLamp.vue`
   and `ApiOfflineChip.vue` as two of the six CONFORMING `no-preference` blocks; `W0.md:96/:174/:176`
   names `ActionFeedback` — but at the **audit challenge-file path**
   (`docs/tranches/V/megatranche/audit/components/ActionFeedback/**`), never at
   `demo/palettes/browser/card/PaletteCard/ActionFeedback.vue`. `DebugEventLog` and
   `ParseEchoReadout` return **0 hits across all twelve wave files** (this seat's grep). See
   **X-W10.85** and **§4**.

---

## 2. Rows

Cell order: **id · source(s) · severity · mechanism · cureShape+locks · sequencing · dissent**.
`—` = none recorded. Anchors are the source records' HEADs; re-resolve before any cure (D-19).

### 2a. Unit .d — the pane-header cluster (CC-099 / CC-101, gates G-4 / G-5)

**X-W10.1 · PH-1 — the shrink "ratio" is a pole-crossing function of viewport width** (cl.1+cl.2)
**BLOCKER.** *Mechanism*: `--pane-title-shrink-ratio: calc(tan(atan2(var(--type-heading),
var(--type-display-1))))` (`PaneHeader.vue:140-142`) — deg-magnitude-consumed-as-radians on the
WebKit cell reproduces 1440→**0.310808** · 1024→**10.868** · 1280→**−7.3909** · phone→**1.619775**,
digit-identical to three independently authored probe families and two committed WebKit shots (title
painted as a full-pane slab at 1024; title and slotted selector gone from the band at 1280).
Chromium computes the designed **0.618029**. The `@supports` gate at `:177` cannot detect a wrong
ANSWER, only a missing feature.
*cureShape+locks*: **(i)** the direct type-ratio `calc(var(--type-heading) / var(--type-display-1))`
(`<length>/<length>` → `<number>`, CSS Values 4; two independent probes measured it correct in both
engines; degradation graceful — invalid-at-computed-value-time → `transform: none`). **(ii) BINDING
STRIKE: `W10.md:309-310`'s "Post-cure values pin per engine (0.311 WebKit / 0.618 Chromium)" is
STRUCK and replaced by 0.618 on BOTH engines** (token ratio 25.888/41.888). *Executed as written,
X-W10.d would certify the BLOCKER as expected behavior* — the single most consequential thing the
PaneHeader adjudication found, and in neither re-reader's return.
*sequencing*: lands BEFORE G-4's post-cure re-measure; G-4's falsifier text changes with it.
*dissent*: none on the mechanism. Ledger gap recorded at §1a n.3.
⟨PaneHeader.md · PH-1 (C:C-1 / D:D-1,D-2 / L:L-1) + Challenges item 4⟩

**X-W10.2 · PH-2 — a third parallel header-condense fork; the producer seam ships complete and
unconsumed** (cl.1+cl.2) **BLOCKER.** *Mechanism*: installed glass-ui 7.0.0 ships `CardHeader
shrink?: boolean` + `card-scroll.css` + `.card-scroll-host` with **ZERO demo consumers**;
`card-scroll.css` transitions `padding-block-start`, steps `font-size` display-2→display-1, and
`display: none`s the description — **three LAYOUT channels** — refuting `useHeaderCondense.ts:9-15`'s
"compositor-only BY ARCHITECTURAL COMMITMENT … structurally cannot satisfy §0.8/BR-9" verbatim;
`<ScrollCardHeader>` (cited by both demo forks) returns **0 hits** in the package.
*cureShape+locks*: **THREE binding riders.** (i) **The transposition is NOT verbatim adoption** —
the producer's `::before` rests at `opacity: 0` (zero-at-rest), which the component's own T.W3-4
ruling explicitly SUPERSEDED (`:2-10`: the constitutive veil, **rest floor 0.52**, Q9 bracket); any
adoption must carry the owner-ruled rest floor or it silently reverts an owner-ruled design.
(ii) **CC-101's "glass DECLINE-ON-RECORD / consumer-side ratified" cell predates the shipped 7.0.0
layout lane and must be re-derived at wave open** (the ColorSpaceSelector stale-producer species).
(iii) the false docblocks (`useHeaderCondense.ts:9-15`, `PaneHeader.vue:157`) are corrected by any
wave touching the files.
*sequencing*: precedes X-W10.4/.5 (the condense register) — the transposition decides whether those
rows survive. *dissent*: **both readers treated producer adoption as clean**; neither noticed the
zero-at-rest contradiction (recorded as the cure-adjudicated-separately law's next test case).
**Named file-list gap**: `useHeaderCondense.ts` is in NO X-wave file list (§4 row 1); its sole
consumer `demo/picker/ColorPicker.vue` is X-W6's (§5 edge 3).
⟨PaneHeader.md · PH-2 (D:D-16 / L:L-2) · wb-generate-pane.md GEN-27 (seat1-L-2, "the producer CSS is
parsed and unused; PaneHeader's own comment books the knob") — one identity, cross-booked⟩

**X-W10.3 · MERGED CROSS FOLD — CC-099 / G-4: scroll-driven header motion runs at FULL amplitude
under `prefers-reduced-motion: reduce`, on nine panes** (cl.1+cl.2) **MAJOR.** *Mechanism*: progress
comes from `animation-timeline: --pane-scroll`, not the clock; both guards
(`animations.css:184-193` + glass `a11y-overrides.css`) clamp `animation-duration` ONLY, so they are
**structurally inert** against timeline-driven animation. MQ-1 measured `animation-duration: 1e-05s`
with **byte-identical transforms** in both preference states (`transform: matrix(0.805831,…)`,
height 43.98→35.44px); W10's own G-4 baseline records reduce and no-preference identical to the last
digit. `PaneHeader.vue:148-151`'s "needs no PRM gate" is the design error; the producer contradicts
it in its own `reduce` arm.
*cureShape+locks*: **K-4 BINDING RIDER — the bare `no-preference` wrapper is KILLED, and CC-099
names exactly that wrapper.** The wrapper alone (and MQ-1's `animation-timeline: none` equivalent)
parks the reduce cohort at the veil's **weakest** state while content scrolls beneath — strictly
worse than the shipped defect on every leak metric, in both engines (D-7's own simulation, the only
seat in the program that tested its own prescribed cure). Admissible shapes: **the D-16 discrete
transposition (primary — MT-F023 then ceases to exist)**, or the interim wrapper **PLUS a static
`--pane-veil-rest: 1` under `reduce`** (VISUAL-CONSTITUTION §6 requires FINAL geometry, not rest).
**X-W10.d may not execute CC-099 as written.** *sequencing*: after X-W10.2's transposition ruling.
*dissent*: BrowsePane K10 KILLS D4-06's own narrowing ("the transform arm is defensible; tighten the
opacity arm only") — the transform scrub is byte-identical under `reduce`; the full media-query move
is the cure, not a tighten. AdminPane's copy is routed to the non-existent "W47" (§1a n.2).
⟨PaneHeader.md PH-5 + K-4 + Superlative D-7 · AboutPane.md AB-8 · BrowsePane.md M4 + ruling-row-6 +
K10 · wb-extract-pane.md XP-12 + K-8 · wb-extract-workbench.md XW-39 · wb-gradient-pane.md D-33/M-34
+ U-3 · wb-generate-pane.md GEN-29 + PIN 7 · PalettesPane.md PP-32 rider · AdminPane.md MQ-1-
inheritance (K1; W47 routing struck) · ApiOfflineChip.md + shell-dock-dockstatuslamp.md (the
CONFORMING half of the same baseline)⟩

**X-W10.4 · PH-6 / AB-18 — CC-101 / G-5: the collapse reclaims ZERO scrollport** (cl.1) **MAJOR.**
*Mechanism*: band, padding (`pt-4 pb-2`, constant) and description box are unchanged rest→stuck; the
`opacity: 0` description keeps its box, its hit target and its accessibility-tree entry — no
`display`/`visibility`/`aria-hidden`/`inert` arm anywhere. The three condense keyframes
(`PaneHeader.vue:200-222`) declare **`opacity` and `transform` only**; `grep "padding\|gap\|margin"`
over the file returns 3 hits, none inside a condense keyframe. This is exactly G-5's RED baseline and
the owner's OM-18 mark.
*cureShape+locks*: the **discrete toggle** is the shape; **C-4's `grid-template-rows` scrub cure is
KILLED (K-6)** — a layout property scrubbed per frame is the F3 fork the file retired (`:153-157`)
and requires disarming O-11 gate 4, the one gate in that file that measures the component. VC law 5
requires title + padding + band to contract as a block.
*sequencing*: with X-W10.5 (same register). *dissent*: —
⟨PaneHeader.md PH-6 + K-6 · AboutPane.md AB-18 (D-7 band limb; hierarchy limb killed K-8)⟩

**X-W10.5 · PH-3 — the three animation ranges are absolute pixel constants with no relation to pane
travel; `fill: both` parks short panes forever** (cl.1) **MAJOR.** *Mechanism*: `0px 64px` / `0px
120px` / `0px 80px` at `:181/:187/:192`; every reported terminal value re-derives exactly from range
arithmetic (six of six). Corpus witness: the Gradient caption sits at **0.2375 forever** at
1440×900. The AB-2 contrast certification at `:118-124` is **silently void at every fractional
park**. *cureShape+locks*: dissolves under X-W10.2's discrete transposition; if any scrub survives,
**ranges must derive from travel**. *sequencing*: **RIDER ON G-5** — G-5 measures at `scrollTop ≥
120`; on a short pane that state is UNREACHABLE, so the gate must guard travel or it inherits
C-8(ii)'s viewport luck. *dissent*: parked-pose values (0.9775/0.2375/0.806 @1440×900; 0.5425
@1512×982) are probe-cell, class structurally certain.
⟨PaneHeader.md · PH-3 (C:C-3)⟩

**X-W10.6 · PH-4 — the certifying oracle is vacuous** (cl.5+cl.1) **MAJOR (split).** *Mechanism*:
`o11-header-gates.spec.ts` gate 5 compares REST states only and never calls the `scrubTo` sitting in
the same file (`:278-300`), so it returns the same verdict for the PRM defect and its cure; gate 3's
`at64.opacity === 1` (`:154`) has no travel guard and the sole running project pins **1280×720**;
nothing anywhere asserts the shrink ratio (grep 0 over `e2e/ test/`); the oracles subtree is
structurally unreachable from any WebKit project (`smoke-safari` testDir = disjoint
`./e2e/smoke/safari`); unit coverage 0. *cureShape+locks*: the no-orphaned-cell arm → **X-W1**
(CC-031 oracle slate); the motion-gate reform rides **G-4/G-5 here**, carrying X-W10.1's pin
correction. *sequencing*: G-4/G-5 may not be stamped green by the unreformed oracle.
*dissent*: **`o11-header-gates.spec.ts` is in NO X file list — partial NO-WAVE-OWNER** (§4 row 2).
⟨PaneHeader.md · PH-4 (C:C-8 i–v)⟩

**X-W10.7 · PH-7 — a leaf publishes an unscoped global class onto nine sibling pane roots** (cl.1)
**MAJOR.** *Mechanism*: the `contain` rationale (`:50-53`) is **false by spec** — named-timeline
lookup is ancestor-chain + `timeline-scope`; `contain` plays no role — and `contain: layout` makes
every pane a `position: fixed` containing block; the mechanism dictates route-level DOM lifetime
(`App.vue:399-408` retains ghost panes "to preserve scroll-timeline state"). The first declaration is
byte-identical to the producer's `.card-scroll-host`. *cureShape+locks*: comment corrections +
whatever containment survives X-W10.2. *sequencing*: after X-W10.2. *dissent*: the class-architecture
arm (a typed host contract) is **NO-WAVE-OWNER**.
⟨PaneHeader.md · PH-7 (C:C-5 / D:D-11 / L:L-4 + L2-3-as-rescoped) + K-9⟩

**X-W10.8 · PH-11 / AB-7 — at DEFAULT media the veil cannot occlude** (cl.1+cl.2) **MAJOR.**
*Mechanism*: `--glass-bg-resting` resolves alpha **0.65 light / 0.72 dark**, so ~35% of underlying
luminance transmits at terminal opacity 1 — alpha arithmetic, engine-independent — and gate 3
measures pseudo-element opacity, not composited coverage. Rendered in a tracked in-repo frame
(`evidence/L3-prophoto-rgb-facts-and-empty-guide.png`: `Created:` / `1931` and the `Red Green Blue /
0 to 1` row printing through the sticky band). *cureShape+locks*: **AB-7's arbiter rider is
binding — G-5's re-derivation must STATE the occlusion quantity: either the veil endpoint occludes
or the band condenses; the two cures interact.** The stuck-material decision belongs to the condense
register. *sequencing*: decided WITH X-W10.4. *dissent*: **K-2 KILLS D-3's universal quantifier** —
under `prefers-reduced-transparency: reduce` `--glass-level: 0` (producer `a11y-fallback.css` AND
`foundation.css:767`) the fill is 100% card, fully opaque; under `prefers-contrast: more` (`:733`,
0.1) 96.5%. The lever D-3 asks the producer for **already ships**. BLOCKER framing dies; MAJOR
stands. Legibility limb NEEDS-LIVE (backdrop-filter smear fidelity in the headless build).
⟨PaneHeader.md PH-11 + K-2 · AboutPane.md AB-7 (D-6 rescoped) + K-9/K-10⟩

**X-W10.9 · PH-12 — `transform-origin: left top` is a physical keyword pair with no `:dir()` arm**
(cl.1) **MAJOR.** *Mechanism*: `:184`; under RTL the shrinking title detaches from its inline-start
edge (the two axes' independent measurements reconcile to the tenth). VC §6.1 requires logical
direction for chrome. *cureShape+locks*: dissolves under X-W10.2's transposition, else a `:dir()`
pair. Neither sibling has a transform. *sequencing*: after X-W10.2. *dissent*: —
⟨PaneHeader.md · PH-12 (D:D-5 / L:L-3)⟩

**X-W10.10 · PH-13 — no `scroll-padding-block-start` on any `.pane-scroll-fade` host and no
`scroll-margin` anywhere in the demo** (cl.1) **MAJOR.** *Mechanism*: grep = **0 rows**; the
69–137px sticky band occludes anything the browser scrolls into view, **including keyboard-focused
controls**. WCAG 2.4.11 Focus Not Obscured (Minimum), AA. *cureShape+locks*: **one declaration on
the host rule.** The producer's `.card-scroll-host` has the SAME gap, so X-W10.2's transposition does
**NOT** dissolve this. *sequencing*: survives every other cure in 2a; land it regardless.
*dissent*: **no X gate covers it — gate gap** (new gate G-12, §3).
⟨PaneHeader.md · PH-13 (reader-1 missed #1, CONFIRMED)⟩

**X-W10.11 · PH-14 — `.pane-header-title` hand-restates the producer's `text-display` utility on a
FALSE premise** (cl.1) **MAJOR.** *Mechanism*: the comment at `:101-104` claims `text-heading`
"hardcodes 700"; the installed producer rides `font-weight: var(--type-weight-heading)`. The fifth
false rationale comment, and the one that licensed the local rule (`:106-112`).
*cureShape+locks*: **`text-display` is the byte-faithful swap** (a strict superset of the five
hand-written declarations, including the non-bold weight token). **`.text-pane-title` is NOT** — it
rests on `--type-display-2` and steps to `--type-display-3` at ≥40rem container, a LARGER rung pair
than PaneHeader's `--type-display-1`. *sequencing*: one class edit + comment correction, any file
touch. *dissent*: **reader-1's "exactly this job" cell is DEMOTED** (rung overstatement, recorded);
the finding survives at full severity. Zero rendered-pixel delta — the defect is structural.
⟨PaneHeader.md · PH-14 (reader-1 missed #2) + Ruled-disagreements row 6 + Challenges item 3⟩

**X-W10.12 · PH-15 / PH-24 — the docblock that licensed the whole triplication is FALSE, in live
shipping source** (cl.1) **MINOR.** *Mechanism*: `useHeaderCondense.ts:9-15` (quoted as authority by
two axes) asserts compositor-only-by-architectural-commitment against a producer that ships three
layout channels, cites the phantom `<ScrollCardHeader>`, and invokes the **owner-retired `proof:*`
idiom**; two dangling `ScrollCardHeader` references remain in shipping source
(`PaneHeader.vue:157`, `useHeaderCondense.ts:11`) where the real surface is `CardHeader shrink?:
boolean`. *cureShape+locks*: rider on X-W10.2's comment corrections. *sequencing*: with X-W10.2.
*dissent*: — **File-list gap named at §4 row 1.**
⟨PaneHeader.md · PH-15 + PH-24 (L:L-8)⟩

**X-W10.13 · PH-20 — five load-bearing rationale comments measurably false** (cl.2) **MINOR.**
*Mechanism*: `:43-45` root-contract (never true of ConfigSliderPane — its Card is `overflow-hidden`
and CANNOT host the timeline; the inner placement at `:106` is structurally FORCED) · `:50-53`
contain-isolates-timeline (false by spec) · `:136-138` "degenerates to exactly 1" (refuted by the
pole arithmetic; phone tan(45 rad) = 1.6198) · `:148-151` "needs no PRM gate" (refuted by MQ-1) ·
plus PH-14's fifth. *cureShape+locks*: **E-3 rider — any wave touching the file corrects ALL FIVE.**
*sequencing*: rides every other PaneHeader touch. *dissent*: **K-9 KILLS L2-3's "contract drifted on
18% of sites" decay narrative** — the code is correct at all nine sites; the defect is a never-true
comment, not consumer decay.
⟨PaneHeader.md · PH-20 (C:C-10) + K-9 + Ruled-disagreements row 3⟩

**X-W10.14 · PH-21 — three desynchronized un-tokenized ranges on one block** (cl.1) **MINOR.**
*Mechanism*: raw `-0.25rem`; the `14px` feather literal at three code sites (`:85/:91/:95`) plus
prose, where the producer tokenizes the same quantity as `--card-pad-title-gap` and consumes it in
both its own inset AND its mask stop. Range anchors corrected to `:181/:187/:192`.
*cureShape+locks*: CC-101's register owns it. *sequencing*: with X-W10.4/.5. *dissent*: —
⟨PaneHeader.md · PH-21 (D:D-8 / L:L-6)⟩

**X-W10.15 · PH-22 — the EOPT prop contract** (cl.1) **MINOR.** *Mechanism*: under
`exactOptionalPropertyTypes` (tsconfig.base:11, a hard demo gate) the public `description?: string`
(`:35-37`) cannot be passed optionally from a `string | undefined` source; the SOLE such consumer
carries the repo's only `v-bind`-ternary workaround, while the sibling `EmptyState.vue:73-89` widens
all six props `| undefined` with per-prop docblocks — the house convention.
*cureShape+locks*: TWO adjudicated-sound cures: widen the prop (house convention), AND/OR the
strictly smaller one — `ConfigSliderPane`'s own `description?` has exactly **2** consumers
(`AuroraPane.vue:115`, `BlobPane.vue:128`, both string literals), so making IT required collapses the
ternary with zero other edits. *sequencing*: one-line widening travels with any file touch.
*dissent*: **K-1 KILLS C-12 whole + D-12's contrivance limb** ("the ternary guards nothing") — under
EOPT that collapse is a TYPE ERROR; C-12's `v-if` cure does not touch the caller-side error; L2-2 is
the correct diagnosis and C-12/D-12(b) invert causality and prescribe a CI regression.
⟨PaneHeader.md · PH-22 (L:L2-2 + reader-2 missed #7 folded, PH-30) + K-1⟩

**X-W10.16 · PH-23 / PH-19 — the caption has no measure and an eighth type role** (cl.1) **MINOR.**
*Mechanism*: no measure, no truncation, no overflow disposition on the description — the sticky band
grows with any caller string (the constitution bounds prose at 66ch); and the description wears
`text-caption`, an **eighth role outside VC §4's closed seven-row matrix**, which the producer
utility renders **ITALIC** — no §4 row specifies italic. *cureShape+locks*: the condense register
must state the caption's measure; **the canon re-author must either legislate the caption row or the
class dies** (→ X-W10.a/.c). *sequencing*: canon decision precedes the class edit. *dissent*: —
⟨PaneHeader.md · PH-23 (D:D-13) + PH-19 (D:D-9)⟩

**X-W10.17 · PH-28 / PH-29 — two silent-failure couplings** (cl.1) **INFO.** *Mechanism*: (a) all
three declarations depend on **undocumented ORDER** — the `animation` shorthand resets
`animation-timeline`/`animation-range-*` (CSS Animations 2), so any reorder silently kills the
choreography with a rest state indistinguishable from correct (`:179-192`, shorthand-then-longhands);
this is exactly the failure class PH-4 proves no current gate can see. (b) the caption animation
binds `.pane-header-desc-wrap > p` (`:189`) while the `<p>` carries a component-authored class
(`:29`), and the wrap node's only other job is a 2px margin — a structural-selector coupling whose
failure is silent. *cureShape+locks*: dissolves under X-W10.2/.3; else the producer idiom
`animation: <name> auto linear` with explicit duration. *sequencing*: — *dissent*: —
⟨PaneHeader.md · PH-28 + PH-29 (reader-2 missed #4, #6)⟩

### 2b. The pane-chrome recipe — the AB-22 packet

**X-W10.18 · AB-22 ≡ L-12 ≡ XP-17 — the pane scroll/overflow recipe is a nine-host per-instance
duplicate that disagrees with itself** (cl.1+cl.5) **MAJOR (elevated from the sources' hygiene
grade).** *Mechanism*: nine hosts hand-copy a pane-shell class string; **FIVE distinct strings**
(§1a n.1, this seat's re-run) where the arbiters filed three or "4/4/divergent"; ConfigSliderPane
applies it to an **inner `div`, not the Card root**, with `scrollbar-thin flex-1` and no
`w-full`/`h-full`. Meanwhile `.pane-scroll-fade`'s only real producer block sits **UNSCOPED inside
PaneHeader.vue** (`:54-58`), a leaf, and `PaneHeader.vue:41-58` **documents the inversion in prose**.
Independently enumerated three times: AboutPane's arbiter census, `motion-quarantine.md:161-162`,
and AdminPane L-12 ("nine hand-copies of the pane-shell class string; PaneHeader owns an UNSCOPED
style for a class it never applies").
*cureShape+locks*: **one authority for the recipe.** Any cure must (a) keep the nine hosts' timeline
resolvable — ConfigSliderPane's inner placement is **structurally FORCED** (its Card is
`overflow-hidden` and cannot host the timeline; K-9), so a naive "move it to the Card root ×9" is
KILLED before it is written; (b) settle `mx-auto` per host, not per copy — **About's `mx-auto` is
inert cargo** (AB-40: with `w-full`, auto margins resolve 0); (c) carry X-W10.10's
`scroll-padding-block-start` in the same rule.
*sequencing*: **X-W10.7's containment ruling and X-W10.2's transposition both land on this rule** —
this row goes last of the three or it is re-authored twice.
*dissent*: the three sources ALL book it **NO-WAVE-OWNER** ("no X row owns the pane-chrome recipe" —
AB-22; "neighbors the V·L1..V·L4 layout family" — L-12). XP-17 alone routes it to X-W10 on the
strength of `PaneHeader.vue` being in W10's bounds. **The routing is only true for the PRODUCER
block; the nine CONSUMER strings live in files W10 does not own** — see §4 rows 11-15, of which
**row 15 finds three of the nine hosts in NO wave's bounds at all**.
⟨AboutPane.md AB-22 (C-11) · AdminPane.md L-12 · wb-extract-pane.md XP-17 (L-5/D-11) ·
ConfigSliderPane.md D-17 (→ its own G11 family) · AboutPane.md AB-40 (the inert `mx-auto` limb)⟩

**X-W10.19 · PH-16 ≡ m3 ≡ XP-18 ≡ PP-32 — `.pane-scroll-fade` is a misnomer: two properties, no
fade of any kind** (cl.1) **MINOR.** *Mechanism*: the rule is `contain: layout style paint` +
`scroll-timeline: --pane-scroll block`; nothing painterly. The quarantine's own **M-33 (ruled FALSE)
records that the NAME already produced one wrong audit finding downstream** — and a second axis read
the NAME again in this corpus (wb-gradient-pane D-4's fade clause, killed). Hand-typed into 9
consumer files with no type and no warning; the producer's equivalent (`.card-scroll-host`) is
honestly named. *cureShape+locks*: **rename-or-deliver, decided once.** The 142px-overflow figure is
UNWITNESSED and may not be cited as the warrant. *sequencing*: with X-W10.18 (same rule, same nine
files). *dissent*: PaneHeader books it **NO-WAVE-OWNER (candidate X-W10.d)** because the rename
crosses ten files; BrowsePane splits it X-W10 + X-W7; wb-gradient-pane assigns the rename rider to
"whichever wave touches PaneHeader — X-W10 owns it". M-33 is cited **only in its own direction**
(no fade exists) — quarantine law honored, no FALSE row carried.
⟨PaneHeader.md PH-16 · BrowsePane.md m3 (D3-04) · wb-extract-pane.md XP-18 (C-8) ·
PalettesPane.md PP-32 · wb-gradient-pane.md D-4-fade-kill + K-list⟩

**X-W10.20 · PH-17 — the header/body alignment contract is a raw literal pair hand-mirrored across
ten files** (cl.1) **MINOR.** *Mechanism*: `px-4 sm:px-6`, grep **10 files / 10 hits**, where the
producer demonstrates container-scaling and ships `--card-pad-inline`.
*cureShape+locks*: tokenization under the canon's proportion section. *sequencing*: —
*dissent*: banked **NO-WAVE-OWNER, candidate X-W10 under its canon** — same bounds problem as
X-W10.18.
⟨PaneHeader.md · PH-17 (reader-1 missed #5)⟩

**X-W10.21 · XP-25 ≡ K-23 — the canon binds a housing to a component the program has KILLED** (cl.4)
**MINOR (canon correction, high leverage).** *Mechanism*: `VISUAL-CONSTITUTION.md:47` names
`InstrumentChassis` with the exact prop contract; **CC-108 / A-17 killed the component at the
producer** (`W6.md:17`: "the producer's InstrumentChassis is dead… no chassis-shaped abstraction
rebuilt on our side"; W6 H1 = `gate-no-chassis.mjs`, a subtraction gate whose falsifier is any NEW
chassis-shaped housing). Demo `InstrumentChassis` imports = **0**. Adopting the chassis today would
fail the program's own gate direction. Second limb: **`X-W5` scope item 5 marks the exact proportion
numbers `61.8033989%` / `38.1966011%` as FABRICATED TOLERANCE to be replaced** — so canon rows
carrying them are stale on a second axis.
*cureShape+locks*: **the canon correction is X-W10's; the bespoke composition is X-W5's.** The
adoption cure and the "installed contract" severity are DEAD frames (K-9/K-23) and may not be
re-minted. OPTICAL-BENCH:42's narrow ORDER ("source/drop; result; controls/commit") and :76's
boundary inventory survive as authored intent — **the numbers do not.**
*sequencing*: **precedes G-7** — G-7 replaces bare `38.1966011`/`61.8033989` with a citation to the
named producer contract, and this row says two of those occurrences are cited to a dead component and
one is fabricated tolerance. Citing a fabrication is not a cure. *dissent*: —
⟨wb-extract-pane.md XP-25 (D-1 as-rescoped, K-9) · wb-extract-workbench.md K-23 + XW-14/XW-32f⟩

### 2c. Unit .e — shadow geometry, elevation, material (CC-100, gate G-6)

**X-W10.22 · G-6 corroboration — the cast-shadow facet has three more witnesses than W10 records**
(cl.1) **MAJOR.** *Mechanism*: `.cartoon-cast` paints `var(--shadow-cartoon-md)`, three layers at
`-3px 3px 0` / `-5px 5px 0` / `-7px 7px 0`, **every layer blur radius 0**, so three offset
silhouettes union into visible facets at a rounded corner. Beyond W10's own baseline: **D4-03**
(CurrentPaletteEditor) seals statically that the saved card's rest rung (`cartoon-surface` →
`--shadow-cartoon-md`) and the well's (`--shadow-cartoon-sm`) ride **different ladders** (−2/−3/−4 vs
−3/−5/−7) — "equal shadow, measured" is FALSE; **XP-26** (ExtractPane) shows the caster shadow is
**REMOVE-disposed and direction-blind** (`--shadow-cartoon: 8px 8px 0 0` is physical, so RTL mirrors
the layout but not the shadow) and that a pane inherits it purely by choosing `Card`.
*cureShape+locks*: **no `overflow: hidden`** (the deliberate S.W5-10/S-15-A refusal at
`PaletteCard.vue:16` stands). **The nested-spread cure stays labelled UNVERIFIED**; a cure that
blurs the cast passes a naive corner check while changing the ratified WELL material and FAILS.
Sole `cartoon-cast` consumer = `PaletteCard.vue` (5 references). §Palette-library canon retires
cartoon on entity cards **prospectively** — D4-03 explicitly books that as "X-W10's business".
*sequencing*: the elevation semantics are authored at `.c` before `.e` applies them.
*dissent*: D4-03's "byte-for-byte / equal shadow" framing KILLED; L-1's BLOCKER framing killed
(K-9, ShadowPalette).
⟨W10.md G-6 baseline · CurrentPaletteEditor.md D4-03 · wb-extract-pane.md XP-26 (D-3/D-12)⟩

**X-W10.23 · D-16 + D-09/M-5 — two prohibited shadow registers on the spectrum face, one alive and
one dead by accident** (cl.2) **MINOR + MAJOR.** *Mechanism*: (a) a SECOND cartoon shadow of
identical geometry **nested inside the card's own**, seed-tinted, hover-only — unreachable by
keyboard (C-6) and touch; deleting it deletes `--spectrum-shadow`'s only consumer (2 hits total) and
D-07's cause 1 with it. (b) the 2px ink ring crosses the WatercolorDot's **seeded organic edge** at a
site canon names by exception (`VISUAL-CONSTITUTION.md:91` — "no ring/shadow/pseudo-element crosses
the seeded edge"; P051 lists "Spectrum" by name), and `box-shadow: var(--shadow-sm)` at `:264` is a
**second attempted prohibited register on the same face, dead only by accident of the cascade**.
*cureShape+locks*: **BINDING — delete `:264` as an SS4.2 VIOLATION, never as "dead code".** A repair
that removes it as hygiene records canon compliance that was never earned. *sequencing*: with G-6's
elevation ruling. *dissent*: the Instrument-veil citation KILLED (D-16b); the fallback rider KILLED
(D-16-FALLBACK); D-05b and the WCAG framing killed.
⟨picker-spectrumcanvas.md · D-16 + D-07-cause-1 · D-09 + M-5⟩

**X-W10.24 · PP-13 — three constitutionally distinct tiers paint the identical token** (cl.1)
**MAJOR.** *Mechanism*: filter control `.search-seated`, authoring plate `.dashed-well`, and saved
specimen `bg-well` all resolve `--well-bg` (`utils.css:96-107/:132-137`, `PaletteCard.vue:19`,
`foundation.css:143→:328`); border census corrected to 1.5 / 1.5 / 2px. **Vocabulary collapse, not a
functional break.** *cureShape+locks*: **CC-009 — the material ladder names its rungs.** The
`.search-seated` recipe-over-producer leg is kin to AdminPane D-14/L-13 and the ASK-D seated-field
rung (**BOOKED SWAP — dies at the producer adopt event**, not here). *sequencing*: precedes X-W10.25
(the well recipe) and X-W10.26 (the register law). *dissent*: reader-D's BLOCKER→MAJOR demotion
ratified; composited pixel identity stays live residue.
⟨PalettesPane.md · PP-13 (D:P5-2)⟩

**X-W10.25 · EAS-12 ≡ MR-34 — the well-recipe divergence class: `cite, don't fork`** (cl.2) **MINOR
(class-level).** *Mechanism*: `.console-well` is declared **single-home** (`foundation.css:340-355`,
"ONE home for the console species"); the EasingAuthoringStage seat re-types background+border onto
`.glass-card` and thereby **LOSES the `prefers-contrast: more` border bump (:743-750), the print arm
(:822-828), AND `border-radius: var(--radius-panel)`**. MixResultDisplay is the class's Mix member:
`bg-well rounded-xl` (`:55`) is `.console-well` minus its border limb — radius arithmetic-identical
(`--radius-panel = var(--radius-xl)` = 12px) — and, having no edge of any kind, is unreachable by the
contrast bump and prints boundary-less while its dots survive via `.swatch-row > *`.
*cureShape+locks*: **"X-W10 owns foundation.css's survivor recipe — cite, don't fork" (EAS-12's own
law).** Mix-specific twist that keeps MR-34 alive rather than a pure fold: **OBC:77 sets Mix
boundaries `[]` · retained line `none` · "distinct through geometry"**, so whether that plate may
take an edge AT ALL is a **canon decision, not a class application** — the same trap already ruled at
MR-15 and K-8. *sequencing*: the canon edge-ruling precedes the recipe application.
*dissent*: **K-19 kills three quantifier limbs** of the reader's find — edgeless wells are common and
apparently sanctioned (readout-rail `GradientEasingEditor:176`, `ColorNutritionLabel:110`, six
Markdown fixtures); `.dashed-well`/`.search-seated` are in **NEITHER** roster (the rosters key on the
CLASS, not on wellness); and "not banked / grep → ZERO files" was FALSE at the reader's own run time.
Method lesson recorded: **a registry sweep returning zero must be re-run with a second instrument
before "not banked" is filed — the sweep IS a claim.**
⟨wb-gradient-easingauthoringstage.md EAS-12 (§S4-1/D2-11 + MISSED-5) · wb-mix-resultdisplay.md MR-34
+ MR-35 + K-19 · ConfigSliderPane.md (console-well carrier, Jul 27)⟩

**X-W10.26 · A-4 + A-20 — the register law and the depth law violated together** (cl.1+cl.2)
**MAJOR.** *Mechanism*: `class="input-bar max-w-sm"` (`PaletteRenameInput:4`) seats **rung-3 CHROME**
(floating glass + blur + the 24rem dock cap + a baked mono voice) **inside the card that IS the
rung-2 WELL** — against THE REGISTER LAW (`DESIGN.md:105-111`, which names `.search-seated` as THE
opt-in and **enumerates its consumers; this file is absent from that census**) and the depth law RC-2
(`:117-118`, heavier-than-host glass reserved for surfaces that float). Second limb:
`.input-bar-field` re-letters the palette name display→mono **mid-edit**, while `utils.css:147-154`
states the voice rule and ships the exact cure (`font-family: inherit`) this file skipped —
challenge-D's own state product demanded "typographically continuous" and the shipped state does the
opposite. Neither axis saw either limb. *cureShape+locks*: seated register or producer `Input`,
**decided here**; the ASK-D `variant="seated"` producer rung rides the standing glass-ui BH relay.
**`max-w-sm` (A-20) restates the recipe's own 24rem cap** — a no-op today, a silent pin tomorrow, and
the one value the register law says must die on a seated field (`.search-seated { max-width: none }`).
**A-20 dies inside this cure; it gets no own row.** *sequencing*: **A-4 precedes X-W10.28 (A-11)** —
the register cure changes the row's height source. *dissent*: —
⟨PaletteRenameInput.md · A-4 (R2-missed REGISTER + K-4 framing kill) · A-20 (R1-M-9/R2-missed)⟩

**X-W10.27 · A-5 — typing in the rename field drives the card's liquid-press spring** (cl.1)
**MAJOR.** *Mechanism*: `@click.stop` guards the wrong event layer — `pointerdown`, `keydown` and
`keyup` bubble from all three editor controls to the root's `v-bind="press.handlers"`, so the card
pulses **once per space keystroke** (palette names routinely contain spaces), squishes on field
entry and squashes on Enter commit. *cureShape+locks*: motion containment at the editor wrapper —
stop `pointerdown`/`keydown`/`keyup`, or an editor-aware gate in the press wiring.
*sequencing*: — *dissent*: PRM leg is live residue.
⟨PaletteRenameInput.md · A-5 (R1-missed press-bleed)⟩

**X-W10.28 · A-11 — the rename morph budget under-declares its row by 29% on coarse pointers**
(cl.3) **MINOR (severity dissent recorded).** *Mechanism*: `--vj-morph-expanded: 3rem`
(`PaletteCard.vue:362`) vs **68px actual on touch**; clipped for the whole enter under `overflow:
hidden`, then pops; leave snaps 68→48 before collapsing. *cureShape+locks*: derive
(`calc(var(--control-h-sm) + 0.875rem)`) or `none`. *sequencing*: **AFTER X-W10.26** — stated in the
source row as a binding sequence. *dissent*: **R1 filed MAJOR; R2 MINOR — recorded unresolved.**
⟨PaletteRenameInput.md · A-11 (R1-M-3 / R2-missed, converged arithmetic)⟩

**X-W10.29 · EAS-4 + EAS-5 — a four-property override cannot reach the producer's specular layer,
and zeroing `box-shadow` deletes the producer's focus channel** (cl.1) **MAJOR.** *Mechanism*: the
seat's `.glass-card` override enumerates four properties (`:93-99`) and cannot reach
`.glass-card::before` (plus-lighter, rest hairline 0.07 → hover 0.1 → active arms, an inset hairline
the seat's border ADDS to, a two-stop mask) — **a non-interactive well retains an interactive glass
response**. And `box-shadow: none` (`:96`, unlayered scoped author origin) beats the layered
`.glass-card:has(:focus-visible)` token swap on **layer order AND specificity** — the card-level
keyboard-focus signal is erased at rest and under focus alike.
*cureShape+locks*: the general law this row states — **the producer signals state through a property
the consumer zeroed** — is the canon clause; the `surface="well"` door rides the GLASS-RELAY.
*sequencing*: the material law is authored at `.c`; the seat cure is X-W6.d's.
*dissent*: —
⟨wb-gradient-easingauthoringstage.md · EAS-4 (N-3/D2-06) + EAS-5 (N-4)⟩

### 2d. Type and register canon — the closed VC §4 matrix

**X-W10.30 · MERGED CROSS FOLD — THE CLOSED TYPE MATRIX IS BREACHED FLEET-WIDE, and the breach is
family-scoped by construction** (cl.1+cl.2) **MAJOR.** *Mechanism*: `VISUAL-CONSTITUTION.md:75`
assigns control/label — **including dropdown options** — to Plus Jakarta Sans non-bold; `:78` closes
the matrix "across all eighteen compositions", P019 the sole exception. The corpus enumerates the
breach at every altitude, with **three distinct mechanisms**: (a) **emission order** —
`text-heading` itself declares `font-family: var(--font-text)`, so `font-display` wins only because
the Tailwind utility is emitted later (ES-7); (b) **the producer's own `font: inherit` hook** —
`.segmented-tab` declares `font: inherit` AFTER `font-weight: 500`, so the consumer's family AND the
shorthand's weight-reset both land (D-8, 12 admin-cluster sites; PSC-10); (c) **an absent producer
type role** — the bare reka trigger carries no type role at all (`triggers.css` sets geometry and
colour only), so deleting `font-display` requires a **consumer-composed** role, which is exactly
CC-047's shape (the producer variant was REFUSED) (DVS-7).
Census, corrected by the seats that re-ran it: **17 sites / 8 files** for `font-display` on control
surfaces (DVS-7; the D axis said 13) · a **seven-site pasted family** `variant="outline" size="sm"
class="font-display` (EB-10, ErrorBoundary:30 + six admin panels) · **3 of 4** `DropdownMenuContent`
sites (US-10) · 4 sites for `font-display text-heading` (ES-7: EmptyState:20/:58, ErrorBoundary:25,
Markdown:27) · 5 sibling sites for the display-serif Retry (AAP-26 ≡ ATP-47).
*cureShape+locks*: **(i) the ruling is FAMILY-SCOPED — charging any single file moves it AWAY from
its siblings** (US-10's clause, adopted verbatim). **(ii) ES-8's cure is the RE-PAIRING, NEVER a
global `--type-weight-heading` re-pin** — the shared cure was killed at R-7. **(iii) the swap for
`.pane-header-title` is `text-display`, not `.text-pane-title`** (X-W10.11). **(iv) any cure
authored into `@layer utilities` RE-LOSES** — see X-W10.31.
*sequencing*: X-W10.31 (the cascade-layer law) is a **precondition**: a matrix cure written into the
losing layer paints nothing. *dissent*: **PS-35 and ABN-26 are carve-outs, not violations** — see
X-W10.32/.33. K-7 (ProfileSection) STRIKES the menu-family charge: the mono menu is a **sanctioned
root decision**. K-6 (EmptyState) kills ES-7's citation limb. A-14 (PaletteSlugBar) is banked
**NO-WAVE-OWNER** on the express ground that "X-W10 is the class home **but its written scope does
not name these rungs**" — the bounds warrant in miniature (§4).
⟨shell-dock-dockviewselect.md DVS-7 · shell-panesegmentedcontrol.md PSC-10 · EmptyState.md ES-7 +
ES-8 · ErrorBoundary.md EB-10 + EB-29 (≡ES-7, cross-booked) · AdminFlaggedPanel.md AF-13 ·
AdminNamesPanel.md D-8 + M-DU1 · AdminAuditPanel.md AAP-26 + AAP-15 · AdminTagsPanel.md ATP-7 +
ATP-47 · shell-dock-profilesection.md D-10 · PaletteSlugBar.md A-14 · shell-dock-
mobilemenudropdown.md D-05(a)/(b) · UserSortMenu.md US-10 · VersionHistoryDrawer.md VHD-17 ·
PaginationBar.md CR-1 · PaletteCardMenu.md AP-11 · MiniColorPicker.md MCP-19 · TagEditPopover.md
TEP-14 · SearchFilterBar.md SFB-31 · ColorNutritionLabel.md CNL-10 · ActionFeedback.md AF-14⟩

**X-W10.31 · M-A ≡ A-3 ≡ TEP-12 ≡ ATP-8 ≡ A-14 — THE CASCADE-LAYER LAW** (cl.1+cl.2) **MAJOR — the
single highest-leverage new law in this fold.** *Mechanism*: glass-ui ships component CSS
**UNLAYERED**, and **inconsistently so** — `_shared/menu.css` wraps `.glass-menu-row` in `@layer
components` then leaves `.dropdown-menu-content` bare. `@import "tailwindcss"` puts every utility —
including glass-ui's own `@utility` registrations — into `@layer utilities`. **Unlayered beats
layered at equal specificity (Cascade 5)**, so authored utilities on producer surfaces **silently
paint nothing**. Five independent measurements: `font-display` dead at three MobileMenuDropdown sites
· `text-small`'s SIZE leg dead on all seven rows (family/weight win, size loses) · `px-2 py-1.5`
restating the producer's exact values, dead code that looks alive · `.hljs { background: transparent
}` (unlayered, imported at `foundation.css:81`) defeating the glass plate in `@layer components`, so
GradientCodeEditor's declared plate **paints NOTHING** and the crayons certified against the rung-2
well sit on the raw seed-driven ambient field (**A-3, BLOCKER**) · `.p-0` losing to
`px-(--overlay-pad-inline)` **by source order at verified byte offsets** (11161 < 11323/11679), so
the popover measures `padding 20.35px 16px` with `p0Applied: true` (TEP-12) · and the declared layer
order (`@layer theme, base, components, utilities`, `dist/styles/index.css` first statement) making a
Tailwind utility win **regardless of specificity** and thereby **DEFEATING the producer's own
light-paper contrast remedy** (ATP-8).
*cureShape+locks*: **(i) CURES AUTHORED INTO THE LOSING LAYER RE-LOSE — every cure in §2d and §2e
must state its layer.** **(ii) A-3's lock: any green must be a PAINT MEASUREMENT, never a class-list
grep.** **(iii)** the `bg-well`-on-the-same-element cure for A-3 is structurally defeated by the same
layer fact (utilities < unlayered; the sibling "proves" the rung only because its `bg-well` rides the
`.readout-rail` WRAPPER); `.console-well` (`foundation.css:352-356`) wins by source order and is a
legal ground. **(iv)** the producer's partial layering is a **BH relay question** ("is it
intentional?"), not a local patch.
*sequencing*: **precondition for X-W10.30, X-W10.35, X-W10.36, X-W10.45.**
*dissent*: banked **NO-WAVE-OWNER — "a formation-level conformance law for X-W10"** (M-A's own
disposition). ProfileSection's A-C4 identity-guards a re-filing back to M-A rather than re-booking.
The backdrop-filter sub-claim in A-3 carries no weight (the producer's `:where(...) > * {
--glass-cell-backdrop-filter: none }` is deliberate nested-cell policy) and the seed-tint clause
self-refutes.
⟨shell-dock-mobilemenudropdown.md M-A + D-05(b) · wb-gradient-codeeditor.md A-3 (D-2/D-r2 §3.2/L-3) ·
TagEditPopover.md TEP-12 · AdminTagsPanel.md ATP-8 (D-7 + reader-1 M4) · PaletteSlugBar.md A-14 ·
shell-dock-profilesection.md A-C4 (identity guard)⟩

**X-W10.32 · PS-35 — a DELIBERATE authored register conflicting with a closed matrix is a CANON
RECONCILIATION, not a component defect** (cl.4) **INFO.** *Mechanism*: the `/generate` name lane
(Fraunces `.font-display`) and description lane (`text-micro`, 11px) sit off VC §4's closed matrix,
and PreviewStrip's proportion is measured against those unratified rungs — **but the name-lane voice
is a documented register with provenance (`GenerateControls.vue:235-239`, P9-R5/T-40, the
ColorSpaceSelector precedent) in a PINNED file.** *cureShape+locks*: **the matrix either ADMITS the
dropdown display-voice register or the executor retires it — recorded for X-W10, decided by the
canon, not by a component seat.** *sequencing*: this ruling gates X-W10.30's execution on those
sites. *dissent*: neither reader carried it; rescoped by the arbiter.
⟨PreviewStrip.md · PS-35 (D-10 rescoped)⟩

**X-W10.33 · ABN-26 — a matrix-vs-ORACLE ownership ruling; no local change** (cl.4+cl.3) **INFO.**
*Mechanism*: the tooltip title takes the palette-identity type role (`:41` `font-display …
text-subheading`) against VC:72/:75 — **but `o10d:359-363` asserts DISPLAY_FACE + weight ≤ 500 on
this very node**, so a unilateral cure turns a standing oracle RED. *cureShape+locks*: **X-W10 rules
the OWNER; the oracle re-rules in the same commit or the cure does not land.**
*sequencing*: co-commit with X-W1. *dissent*: r-DU's INFO calibration adopted over r-LC's MINOR.
⟨shell-dock-actionbutton.md · ABN-26 (D-14)⟩

**X-W10.34 · R7 + R12 + R23 + R32 — the canon has NO JURISDICTION for mathematics** (cl.1+cl.4)
**MAJOR.** *Mechanism*: KaTeX introduces a **fourth typographic voice and a rogue 1.21× rung**
(`.katex{font:normal 1.21em …;line-height:1.2}` — the shorthand imposes leading; 20 `@font-face`
rules), on no rung of the φ ladder and outside the closed matrix (which has **no row for math**).
Consequences the same corpus measures: **dual-voice same-referent** at ~19 of 27 inline sites (an
accent Fira Code chip on the label line, undifferentiated 1.21em serif on the definition line — both
voices in ONE sentence at `hex.md:62`) — the accent is spent on the label and withheld from the
definition; **21 of 27** inline formulas typeset English prose (`\text{black}`, `\text{otherwise}`)
as mathematics inside Plus Jakarta paragraphs, witnessed in the 390 frame; and `VISUAL-
CONSTITUTION.md:78/:218` grants a 66ch measure escape only to the Conversion-paths graph and fenced
code — **display math, 64 instances and the largest content species on the route, is neither.**
*cureShape+locks*: canon rows — one referent, one voice; a `\text{}` policy (re-map or the prose
leaves the math); a measure jurisdiction for math. Implementation echo rides X-W6.
*sequencing*: canon first; X-W6 implements. *dissent*: —
⟨Katex.md · R7 (D-3) · R12 (missed, design) · R23 (missed) · R32 (D-17)⟩

**X-W10.35 · AP-14 ≡ AP-10 ≡ DSL-4 — the small-caps register HAS NEVER RENDERED, and cannot**
(cl.1+cl.2) **MAJOR.** *Mechanism*: `font-variant: small-caps` is **BROWSER-SYNTHESIZED in every
code path** — the shipped `fira-code-latin.woff2` GSUB carries `['calt','dnom','frac','locl',
'numr']`: **no `smcp`, no `c2sc`** (a seat's own fontTools run), and the metric fallbacks carry none
either, so the browser scales capitals to sub-pixel stem weight at 11px. Independently, the K-INV5
register at PaletteCardMenu **could never fire at all**: `text-mono-caption`'s `text-transform:
uppercase` consumes the lowercase glyphs **before** `font-variant: small-caps` can synthesize
(`:37-39`, `:57-59`) — three comment blocks name a register that never painted. Eyes-on witness:
`band-1440-light.png` reads `● DEV MISCONFIGURED — RUN ˋNPM RUN DEVˋ`, graves painting as glyphs.
*cureShape+locks*: **the register SPELLS uppercase (`text-transform`), never synthetic small-caps**
— which is the producer's own recipe. **BH relay note**: the producer's Fira Code subset cannot
honour `smcp`, so any producer surface spelling small-caps has the same disease.
*sequencing*: with X-W10.36 (same lines). *dissent*: DSL-4's command-copy limb is **moot-on-AP-1**
(the string dies with the misconfigured face).
⟨ApiOfflineChip.md AP-14 (reader-1 missed #1) · PaletteCardMenu.md AP-10 (C3-1/D-17) ·
shell-dock-dockstatuslamp.md DSL-4 (C-2/D-9)⟩

**X-W10.36 · AP-15 ≡ AP-19 ≡ DSL-10 ≡ R-18 — the phantom `--type-mono-caption` token: zero
declarations, four consumers, three fallback realities** (cl.1+cl.2) **MAJOR + MINOR.**
*Mechanism*: the token is declared **nowhere** (demo and producer dist); the consumers are three demo
sites **plus the producer's own minified `.timeline-popover-state` rule**, which supplies a THIRD
divergent fallback (`.75rem`) — so the chip's recipe is a **hand-copy of a producer defect** (same
var, same 0.06em, small-caps for uppercase). Font-size therefore always resolves to the hardcoded
fallback — the masking-fallback pattern under owner edict 2. At ComponentSliders the same phantom
resolves to a **vw-driven clamp inside the cqi-only region** (`shell.css:79-81` is the law).
*cureShape+locks*: **the cure is `var(--type-micro)` bare / `text-mono-micro` — size-preserving,
on-ladder, mask gone (ZERO pixel delta; the lamp's `0.6875rem` fallback is byte-identical to
`--type-micro`). `text-mono-caption` is KILLED as the cure — it UPPERCASES and RESIZES, and would
manufacture X-W10.35's defect.** Producer half (declare beside `--type-micro`/`--type-caption`, or
retire it from the producer too) = **GLASS-OWNED → BH relay**.
*sequencing*: after X-W10.35's register ruling. *dissent*: DSL-10 records that reader-A re-derived
the cure **independently and byte-identically** to the AP record's killed-#5 ruling — convergent
re-derivation, the anti-echo behaviour the program wants; census corrected 3 → 4 consumers.
⟨ApiOfflineChip.md AP-15 (missed #2) + AP-19 (C-6/L-5/D-3) · shell-dock-dockstatuslamp.md DSL-10
(C-5/D-12/L-2 + A-missed #4) · picker-componentsliders.md R-18⟩

**X-W10.37 · AP-16 + ES-21 + AP-28 ≡ DSL-14 — the tracking register is INVERTED by the utilities
that claim it** (cl.1) **MAJOR + MINOR.** *Mechanism*: the producer's `.dropdown-menu__shortcut`
(0.1em, auto-margin, 0.6 opacity) is re-implemented **three ways in one file with no two recipes
agreeing** (`:37`, `:57`, `:101`), and both tracking utilities **invert** the register they claim —
`tracking-wide` = 0.025em (a **4× reduction** of `--type-tracking-caps` 0.1em) at `:37/:57`, and the
missed site `tracking-wider` = 0.05em (**2× reduction**) plus a no-op `uppercase` re-declaration at
`:155`, which D-7's table never reached. Independently: `tracking-[0.18em]` is an arbitrary literal
at **1.8×** the certified caps tracking, riding two dead utility classes on the same two lines
(`uppercase` is a no-op because `text-mono-caption` already sets it; `text-balance` is a no-op ×4
because `text-heading` already sets `text-wrap: balance`). And `letter-spacing: 0.06em` is off-ladder
with `--type-tracking-caps: 0.1em` unused (`scheme-motion.css`) — the sixth off-ladder literal D-4's
inventory stopped short of. *cureShape+locks*: **adopting the token is a RENDERED change, not a
rename** — any cure states the design decision explicitly. *sequencing*: with X-W10.35/.36.
*dissent*: —
⟨PaletteCardMenu.md AP-16 (D-7 + missed #2) · EmptyState.md ES-21 (D-16 + dead-class missed) ·
ApiOfflineChip.md AP-28 (missed #6) · shell-dock-dockstatuslamp.md DSL-14 (D-13)⟩

**X-W10.38 · AP-17 ≡ US-10(iii) — THREE WIDTH AUTHORITIES for one menu** (cl.1) **MINOR.**
*Mechanism*: `w-48` pins a fixed width on variable-length **user data** — the only
`DropdownMenuContent` consumer to do so — while the app minted `--menu-min-w: 11rem` / `min-w-menu`
(`foundation.css:118/:445`) **and** the producer already ships `--overlay-min-width: 8rem` on the
panel class. The corpus's own cure consumes the **middle** one; `--overlay-min-width` appears in
**none** of fifteen challenge files. *cureShape+locks*: **one width authority — consume or relay the
producer token** (+ BH note). *sequencing*: — *dissent*: identity ≡ `DEFECT-LEDGER:25836`
(`UserSortMenu.vue:16` enumerated; 178px consequence measured at the family) — preserved, not
re-booked.
⟨PaletteCardMenu.md AP-17 (P3-8 + missed #8) · UserSortMenu.md US-10(iii)⟩

**X-W10.39 · AP-33 + AP-34 — two annotation grammars in one column; two truncation grammars 40px
apart** (cl.1) **MINOR.** *Mechanism*: (a) on any owned remote palette with >1 version, the state
span (mono-caption, 0.55 opacity, `:57-59`) co-renders in one `ml-auto` column beside the count span
(italic `text-caption`, muted ink, `:101`) — **two incompatible annotation grammars**. (b) one
palette identity, two truncation grammars: the card clips **vertically and responsively**
(`line-clamp-2 sm:line-clamp-1` + `:title` recovery) while the menu clips **horizontally and
unconditionally** (`truncate max-w-[180px]`, no title) — the mismatch is also what broke P5-3's
oracle (K-1). PA §5's one-anatomy law governs. *cureShape+locks*: one annotation grammar (AP-16's
shortcut primitive is the vehicle); one truncation anatomy. *sequencing*: with X-W7's carve.
*dissent*: AP-33's arithmetic limb killed (K-2); structural half survives.
⟨PaletteCardMenu.md · AP-33 (P5-5) + AP-34 (missed #3)⟩

**X-W10.40 · AF-4 + AF-6 + AF-18 — the tone register forked three ways, and three of four rungs FAIL
WCAG AA** (cl.1+cl.2) **MAJOR.** *Mechanism*: ActionFeedback forks the producer's **SEALED** tone
register three ways at once — prop name `variant`, a vocabulary term (`error`) that is **∉ TONES**
(`axes.d.ts` seal byte-read), and rendering raw green literals — a singleton fork. Independently:
**three of four tone rungs fail AA at the shipped fixed 12px**, the r2-canonised dark-destructive
pair worst at **2.78:1**. The axis-crossing failure is recorded: **D owned contrast and never
looked; L looked at the exact lines and disclaimed it** — the corpus's gravest true defect appeared
in **no axis file at all**. And the element's material register is unreconciled: named a chip,
dressed as a panel, laid out as a band (block-level `flex`, no width constraint, full-card tint in a
`grid-cols-1` wall, `rounded-panel` 12px on a 28px band butting a `py-2.5` row) — **a full-bleed
verdict band may be the RIGHT gestalt; the defect is that the three disagree, undecided.**
*cureShape+locks*: **the r2 one-line cure is KILLED for light mode (R-5: 1.65:1)** — the fix ships
only with a legibility answer (Alert's foreground-ink idiom, or an ink decision under canon).
**X-W10 books the canon law: any tone-on-tint pairing STATES and MEETS its contrast floor.**
*sequencing*: canon law here; the surface cure rides X-W7's successor.
*dissent*: **the component's own file is in no wave's bounds** — see X-W10.85 / §4 row 7.
⟨ActionFeedback.md · AF-4 (r2 L-1 + C3) · AF-6 (design-reader corpus-miss; R-5) · AF-14 · AF-18⟩

**X-W10.41 · AP-16(chip) — the one text in its well that IGNORES `prefers-contrast: more`** (cl.1)
**MAJOR.** *Mechanism*: `foundation.css:726-740` raises `--muted-foreground` to 88% and
`--card-edge` to 55%; the sibling labels in the same well consume the token and **get louder**; the
chip's hand-mixed 72% ink (`:52`) is untouched. **The decorative count responds to the user's
contrast request; the data-loss message does not.** Its own border (`:51`, `--card-edge`) DOES
participate — the correct line that makes the wrong one legible.
*cureShape+locks*: **canon — ink roles consume TOKENS, never hand-mixes.** Also cured structurally by
the AP-9 rebuild. *sequencing*: — *dissent*: DSL-29's `prefers-contrast` limb dies with AP-1 (the
surviving base face's `--card-edge` border DOES take the 55% lift); what survives is that **the 55%
wash is on no reduced-transparency roster.**
⟨ApiOfflineChip.md AP-16 (missed #4) · shell-dock-dockstatuslamp.md DSL-29 (A missed #1, demoted)⟩

**X-W10.42 · AP-20 — six dead fallbacks encode a DRIFTED SECOND DEFINITION of the destructive ink**
(cl.1) **MINOR.** *Mechanism*: `var(--destructive, oklch(0.58 0.19 25))` ×6 across the twins, while
`--destructive` is declared **threefold** in the producer; plus the `--radius-pill` vs
`--radius-pill, 9999px` spelling drift. *cureShape+locks*: the misconfig blocks die with AP-1;
surviving fallback hygiene lands here. *sequencing*: after AP-1. *dissent*: —
⟨ApiOfflineChip.md · AP-20 (L-6)⟩

**X-W10.43 · AP-3 — the destructive ink is INERT: an unlayered producer rule defeats the utility**
(cl.1) **MAJOR.** *Mechanism*: unlayered `.dropdown-menu__item { color: inherit }` defeats
`@layer utilities`' `.text-destructive` at `:135` and `:164` — **Delete renders in Rename's ink**
(measured ΔRGB 0-1). One `@layer` at-rule in the whole file; class stack proven.
*cureShape+locks*: producer half (the `variant="destructive"` seam + the layer-discipline defect) =
**GLASS-OWNED → BH relay**, riding the same relay as AP-37; **consumer arm here**.
*sequencing*: a **member of X-W10.31's class** — the layer law decides the consumer arm's shape.
*dissent*: —
⟨PaletteCardMenu.md · AP-3 (D-2)⟩

**X-W10.44 · AAP-14 + AAP-15 — a boundary budget exceeded 4× and a badge that is a chimera of two
type rungs** (cl.1) **MAJOR.** *Mechanism*: `:62` `rounded-md border border-card-edge` on **every**
row including the last — 20 rows = **80 painted segments against a budget of 19** (one separator
between adjacent rows, none terminal; canon triple-cited verbatim by both readers). And the Badge
sets `text-[length:var(--control-text)]` (SIZE.md) while the instance adds `text-mono-caption` —
**tailwind-merge cannot collapse them**; the timestamp (provenance) rides the control rung one line
above the file's own correct machine-string comment (`:74-75`). The declared hierarchy is flat by
construction: `text-small` and `text-mono-small` share `--type-small`.
*cureShape+locks*: **X-W10's boundary law governs; the row-anatomy cure is X-W7's.**
*sequencing*: canon law before the W7 rebuild. *dissent*: which rung wins the Badge size is
live-only; the chimera is structural.
⟨AdminAuditPanel.md · AAP-14 (D-4) + AAP-15 (D-6)⟩

**X-W10.45 · ES-29 — the P10 weight-token retirement is DUE and its premise is now FALSE** (cl.1)
**MINOR.** *Mechanism*: `foundation.css:376-384` pins `--type-weight-display: 400` and states that
"every demo title surface carries `font-weight: var(--type-weight-display)` on its OWN class list" —
**glass-ui 7.0.0 SHIPPED the seam that comment says does not exist**: every `text-display-*` utility
rides `var(--type-weight-display)` (producer default 600 via `--font-display-weight`), so the demo's
`:root` 400 pin now flows through automatically and the per-surface pins are **dead scaffolding on a
false premise**. *cureShape+locks*: the comment correction **plus a BOOKED verify**; the ES-7/ES-8
sites are cured by re-pairing, **not by this seam**. *sequencing*: — *dissent*: ONE identity with
the CSS-adj / Markdown-adj **producer-state-correction law** — stale-cell repetition is itself
evidence.
⟨EmptyState.md · ES-29 (reader-2 missed find)⟩

### 2e. Ink, focus and contrast law

**X-W10.46 · MERGED CROSS FOLD — `--color-ring` IS MINTED NOWHERE, and 25 sites across 12 files
consume it** (cl.1+cl.2) **MAJOR.** *Mechanism*: no `--color-ring` / `--ring` declaration exists in
demo **or** producer dist; **the repo's own tombstone documents this exact death** —
`focus-ring.css:9-15` (U-F25): the token "resolved EMPTY — an un-clobbered ring painted nothing".
Tailwind v4 mechanism (reader-sharpened, adopted): a `ring-<name>` utility **compiles only when
`--color-<name>` exists in the theme**, and glass-ui's bridge ships `--color-border` /
`--color-accent` / … but **no `--color-ring`** — so `focus-visible:ring-ring/40` emits nothing and a
bare `ring-2` falls back to opaque `currentColor`; where it does paint, the ring `box-shadow`
**REPLACES** the glass material stack rather than composing (utilities > components). Fleet
condition: **25 occurrences / 12 files**, two seats' independent censuses agreeing; **exactly ONE
file consumes the certified `--focus-ring-inner`/`--focus-ring-outer` recipe**
(`GradientStopEditor.vue`; `App.vue:201` is a comment). This is the application's DOMINANT focus
idiom, silently degraded house-wide.
*cureShape+locks*: **root token mint or house-wide swap — a per-component recompose leaves 24 sites
broken** (A-10's lock, verbatim). K7's correction ratified: `ring-2` is a WIDTH utility and is not
downgraded (line-vs-occurrence).
*sequencing*: **precondition for X-W10.47 and X-W10.48** — both prescribe rings.
*dissent*: ADJ-m3 is the banked identity for the tombstone limb (`CurrentPaletteEditor.md:75`, which
cites `MixSourceSelector:153/:170/:257` by line); the collision limb (X-W10.47) is MixSourceSelector's
own. ABN-5's provenance narrative killed-in-part (K-5); the ":4 never a per-control literal" clause
survives as the indictment.
⟨PaletteCardSwatches.md PS-9 · wb-gradient-codeeditor.md A-10 (D2-2/L-14 + missed M-2 + K7) ·
shell-dock-actionbutton.md ABN-5 (D-2) · wb-mix-sourceselector.md MSS-5 (D-6) ·
CurrentPaletteEditor.md ADJ-m3 (R1-missed #8, rescoped) · ColorInput/MixSourceSelector fleet sites⟩

**X-W10.47 · MSS-5 — selection and focus write the SAME ring channel in the same layer: a selected
card under keyboard focus LOSES its selection colour** (cl.1) **MAJOR.** *Mechanism*: `:259`
`ring-2 ring-primary` (0,1,0) and `:257` `focus-visible:ring-ring/40` (0,2,0) both write
`--tw-ring-*`; focus therefore replaces selection with — at best — nothing (X-W10.46).
`VISUAL-CONSTITUTION:89`: "focus stays separate". *cureShape+locks*: register decision → X-W4;
**token law → X-W10, per the ADJ-m3 precedent.** *sequencing*: after X-W10.46. *dissent*: —
⟨wb-mix-sourceselector.md · MSS-5 (D-6)⟩

**X-W10.48 · ABT-11 + MISS-14 — the focus ring is USER-VARIABLE by construction and judged against
the wrong floor; and it snaps in one frame** (cl.1) **MAJOR + MINOR.** *Mechanism*:
`--focus-ring-color: var(--accent-live)` is a **deliberate global re-point** (foundation.css), so the
only focus indicator is a 2px ring at **30% alpha of the LIVE SEED** — user-variable, judged against
text semantics when a focus ring is **non-text ink (WCAG 1.4.11, 3:1)** — while
`useSafeAccentFn`'s own documented `GRAPHICS_CONTRAST_FLOOR` (3) **is never used by any ring**.
Separately, `box-shadow` is absent from `.channel-rail-item`'s transition list (`:254-261` transitions
exactly `color`, `background-color`, `transform`) while the focus register paints via
`box-shadow: var(--focus-ring-shadow)` (`:274`): a **21.84%-of-rail** change lands in ONE frame while
the **24.64%** hover change eases over `--duration-normal`; the producer's `.interactive-item`
transitions `box-shadow var(--duration-fast) var(--ease-standard)` alongside its colour legs.
*cureShape+locks*: a **floating-rung, 3:1-floored ring certificate is one
`useSafeAccentFn("floating", GRAPHICS_CONTRAST_FLOOR)` away** — shares X-W10.49's cure. **Caveat: a
focus indicator must still appear PROMPTLY — the leg rides `--duration-fast`, never the hover
clock.** *sequencing*: after X-W10.46. *dissent*: the "outline declared at 3px then killed" narrative
is a getComputedStyle misreading (`outline-width` initial `medium` serializes 3px; `outline: none`
itself produces it) — **KILL UPHELD**. Forced-colors corrected: pressed gets a 2px Highlight border
and `:focus-visible` a Highlight outline; only the REST-state boundary and the seed-derived ring are
genuinely open. The 1.65:1 composite figure stays residue.
⟨shell-dock-actionbartoggle.md ABT-11 (D-08) · picker-componentsliders-consolerail.md MISS-14⟩

**X-W10.49 · ABT-8 ≡ ABN-17 — THE INK RUNG CERTIFICATE: resting-certified ink on FLOATING chrome**
(cl.1+cl.2) **BLOCKER.** *Mechanism*: `SAFE_ACCENT_KEY` carries ink certified against the **RESTING**
rung (`useContrastSafeColor.ts:302-306`) while the dock is **FLOATING** chrome — against the
codebase's own documented contract (`:337-338`, "'floating' for menu/dock chrome"), with **two
OBEYING siblings** in the same directory (`MobileMenuDropdown:23`, `ProfileSection:28-29`) and
**three VIOLATORS** (`Dock.vue:35` → ActionBarToggle, `ActionBarLayer:24`, `ColorInput:157`). Two
per-instance inline `:style="{ color: accent }"` bindings then paint seed ink on **resting dock
chrome**, breaching the seed-tint jurisdiction and defeating the producer's entire pressed-ink
treatment (an inherited property against two child declarations); AA fails at 16.4px/w400 in the
measured cells. The exact ratios are seed-variable and live-only — **the unguarded variability IS
the finding.**
*cureShape+locks*: **route the dock's accent through `useSafeAccentFn("floating")` — ONE call fixes
all three violators — and it is stated this way SO THAT the "delete the prop" design ruling does not
erase the rung lesson.** The prop/thread deletion falls out of X.W4.d's contract collapse (D-15 dies
with it). *sequencing*: the rung cure precedes the prop deletion, or the lesson is lost.
*dissent*: ABN-17 (two custom properties carrying one value, stamped inline per instance after a
four-hop drill of an injectable, with `?? 'currentColor'` doubly redundant) names this exact route as
its cure — cross-booked, not re-minted.
⟨shell-dock-actionbartoggle.md ABT-8 (D-02 + D-15 + C-7 + reader-2 missed #1) ·
shell-dock-actionbutton.md ABN-17 (D-12 + L-9)⟩

**X-W10.50 · A-1 ≡ DVS-13 ≡ PC-19 ≡ D-5 — THE ADMIN-GOLD LEGIBILITY BOUND, and a shimmer that PARKS
at its least legible stop** (cl.1+cl.2+cl.4) **MAJOR.** *Mechanism*: `--color-gold: #D4AF37` has
sRGB relative luminance **0.4494**, so against ANY ground lighter than itself the ratio is bounded by
the pure-white ceiling **2.10:1** — below the 4.5 text floor AND the 3:1 non-text floor; passing 4.5
would require a ground of luminance ≤ 0.061, near-black, **impossible for a light-scheme dock band**.
Independently measured on the menu: glyph **~1.9-2.2:1** vs a 3:1 floor and the `.gold-shimmer`
sweep's **best stop ~3.6-4.1:1** vs a 4.5:1 floor, under **BOTH** token families (producer oklch and
the demo's `@theme` hexes) — while the two sibling chromatic exceptions **run through guards this
repo wrote** (`useViewAccents` ≥3:1; palettes-ramp 4.5:1, with a comment naming this exact menu).
Population is **not admin-only**: the latch puts the ~2:1 gold glyph on **every anonymous visitor who
deep-links `/#/atmosphere`**. Worse under PRM: `.gold-shimmer`'s sweep lives ONLY under
`no-preference`, so under `reduce` `background-position` rests at `0% 0` — the near-white
`--color-gold-light` (#F5E6A3, pinned scheme-invariant at `foundation.css:104-105` while
`--color-gold-dark` stays producer-owned: **a chimeric ramp**) sits mid-label over the light
`bg-well` ground, **permanently, for exactly the users who asked for less motion**. And at
ProfileSection the shimmer is **dead** — inline `color: var(--color-gold)` outranks the text-clipped
gradient — while `metal-shimmer-sweep` keeps animating invisibly (only this consumer kills it; the
two siblings set no inline color).
*cureShape+locks*: **canon — a shimmer must PARK LEGIBLY**; `D-5`'s cure direction **INVERTS** the
failure (`--color-gold-light` ≈ 0.75 luminance is lighter still) and may not be adopted as filed.
The one-admin-treatment decision sits in the **convergent design loop**; **this row supplies the
legibility bound that decision must satisfy.** `D-design.md:1028-1029`'s listing of the shimmer under
"genuinely right" is **STRUCK**.
*sequencing*: canon bound before any admin-treatment ruling. *dissent*: distinct from Dock-adj
**L-19** (the latch = WHEN gold shows; this = gold is illegible WHEN it shows) — never conflated.
Dark scheme unaffected. Exact rendered ratios on the glass panel remain residue.
⟨shell-dock-profilesection.md A-1 (addendum) + D-5 · shell-dock-dockviewselect.md DVS-13 (missed-1) ·
PaletteCard.md PC-19 (MISS-adopted, reader 1)⟩

**X-W10.51 · C-2/D-3 + D-1 + D-15 + D-17 — the emphasis register decision (ProfileSection)** (cl.1)
**MAJOR.** *Mechanism*: `variant="outline"` / `variant="ghost"` are **not Glass 7 vocabulary**
(Button's prop union has no `variant`); three sites pass them as inert DOM attrs and both band
controls resolve to default `secondary`, painting the **identical wash capsule**. Consequences: the
certified trigger ink's referent **omits a layer** (the glyph composites over band **plus** the
button's own wash capsule while `chrome` models one alpha layer — the file's own dependency already
diagnosed and cured this exact two-layer class for the veil); a desaturated wash slab sits inside a
chromatic capsule on a chromatic band with the control boundary stated **three times** (capsule ring
+ wash edge + `DockSeparator` each side); and two adjacent controls speak **two hover languages** —
Login's entire affordance is the capsule's 1.5% scale, because `secondary` ships no hover rule and
the inline color would outrank one anyway. *cureShape+locks*: dead props → X-W8 (subtraction);
**the emphasis register decision → X-W10**, with the caveat that `quiet`'s HOVER re-introduces a
wash. *sequencing*: **X-W10.52's collision ruling governs the register choice.** *dissent*: LC's
BLOCKER demoted to MAJOR.
⟨shell-dock-profilesection.md · C-2/D-3 · D-1 · D-15 · D-17 · D-6 (folds into N-2's glyph rule)⟩

**X-W10.52 · US-6 / R-G ≡ the PaginationBar G-9 trap — THREE STANDING AUTHORITIES PRESCRIBE
INCOMPATIBLE CURES FOR ONE LINE** (cl.2+cl.3) **MAJOR — a must-rule row.** *Mechanism*: three
authorities claim `UserSortMenu.vue:8`. **(a) X-W8.b / G-9** — *delete* the dead attribute, with a
committed **null-DELTA pair proving ZERO pixel change** (the register stays secondary-wash; W8's own
gate text: "the only legal delta is the attribute's absence from the DOM", `W8:317-319`).
**(b) `DEFECT-LEDGER:23784`** — `emphasis="quiet"` ("repo-wide the mapping is ghost→quiet"), which
*changes* paint to the transparent register. **(c) V·MEGA-W-GEN-1 G5** — the same ghost→quiet
migration at ~48 non-pinned sites. **(b)/(c) satisfied would FAIL (a)'s gate; (a) satisfied preserves
the possibly-wrong register forever.**
*cureShape+locks*: the tiebreak is in the formation's own law — **`W7.md:170-173`: "No design content
is authored here — the canonical design and motion law is X-W10."** The register decision (quiet vs
wash inside a glass field) is design content, therefore **X-W10 RULES**. **X-W8's deletion may land
first (mechanically safe either way); if quiet wins, G-9's null-DELTA baseline MUST be re-stamped in
the SAME commit** — otherwise the gate outlaws the cure.
*sequencing*: **binding. X-W10's register ruling must exist before X-W8's G-9 is cited as closed on
these lines.** *dissent*: the sibling trap is recorded verbatim at PaginationBar — three records now
touch `PaginationBar.vue:8,22` (the component record, W8's G-9, and this canon row), and
**"whichever wave lands last must re-read the other two, and a green G-9 is NEVER citable as the cure
of D-5's design limb" (DISSENT #6).** The dead-`variant` fleet condition is **103 `variant="` vs 2
`emphasis="`** repo-wide.
⟨UserSortMenu.md US-6 + R-G + US-1 · PaginationBar.md D-5/L-1 + DISSENT #6 + note 6 ·
VersionHistoryDrawer.md (the promoted repo-wide dead-`variant` class) · shell-dock-profilesection.md
C-2/D-3⟩

**X-W10.53 · US-3 + US-4 + US-5 — the sort affordance: no state on the trigger, an unfalsifiable
label, and two sign systems in one directory** (cl.1+cl.4) **MINOR · MINOR · MAJOR.**
*Mechanism*: (a) `:10`'s `aria-label="Sort users"` is static and the sole trigger content is a
state-free ellipsis; the `sort` prop reaches only the closed menu's radio model, so with the sort key
invisible in the list AND the criterion invisible on the trigger the panel offers **zero in-place
confirmation of ordering state** (the house cure idiom sits one file over, `SearchFilterBar:7-12`).
(b) "Newest first" is ambiguous and the code silently picks one meaning — the domain model carries
**both** `createdAt` and `lastSeenAt?`, the implementation sorts `createdAt`. (c) **two sort UIs, one
directory, two sign systems**: `EllipsisVertical` means "overflow" at every other site in the fleet,
and — decisively — **the SAME glyph in the SAME trailing slot of the SAME search-row composition
means "Filters" in BrowsePane and "Sort" in AdminPane**; the two siblings agree on the job, the
title, the Clock icon and the `update:sort` event, and **on nothing else**
(Popover+RadioGroup+`<label>` rows+array options+`String(v)` vs DropdownMenu+RadioGroup+menuitemradio
+hard-coded options+`v as any`; hard-pinned `h-8 w-8` vs `size="xs"`).
*cureShape+locks*: (b) **ONE disambiguated label — the axis's three-hint cure OVER-REACHES** (KISS;
"Most palettes" and "Alphabetical" are unambiguous, and the `text-micro` description idiom is
reserved for opaque option sets). (c) **the canonical sort-menu idiom is a design ruling; the likely
direction — SearchFilterBar converging onto UserSortMenu's idiom — is recorded as INPUT, not
authored here.** *sequencing*: — *dissent*: reader-2's scope correction adopted (inside the OPEN menu
the state IS drawn and announced via the producer ItemIndicator + `menuitemradio`/`aria-checked`; the
gap is trigger-only). The `?? ""` comparator sorts missing values LAST in both positions — correct
as-is. The "Sort: Newest" desktop text-capsule half stays UNPROVEN-NEEDS-LIVE.
⟨UserSortMenu.md · US-3 (D-2) · US-4 (D-3) · US-5 (D-1 + L-4 + reader-1 missed-2)⟩

### 2f. Motion tokens, PRM sequencing, and the DESIGN.md truth surface

**X-W10.54 · SP-31 — THE BINDING SEQUENCING RIDER AGAINST THIS WAVE** (cl.3) **MAJOR — the most
load-bearing sequencing constraint in the fold.** *Mechanism*: `W10.md:113` / scope item 5 order the
**deletion of `animations.css:184` and `:202`**, moving PRM conformance to per-site `no-preference`
gating. But the global guard **never touches `animation-delay`**, and ShadowPalette's safety
additionally rests on Tailwind's pulse shipping **no fill-mode and no 0% stop** — while
`ShadowPalette.vue`'s `animate-pulse` is **unconditional at `:55/:62/:66/:75`**. **After X-W10 lands,
the ghost pulses under reduced motion** unless X-W7's cure lands the plate motion PRM-conditional (or
the species dies) FIRST. The failure is loudly bounded: `o9`'s PRM leg asserts `duration < 0.1`
(`:117-119`) and goes RED at exactly that moment — **but a gate that fires cross-wave is a sequencing
constraint, not a cure.**
*cureShape+locks*: **BINDING — the plate's motion must be `no-preference`-gated or dead BEFORE the
global reduce guard dies.** *sequencing*: **X-W7 (SP-31's cure) STRICTLY PRECEDES X-W10.d's deletion
of `:184`/`:202`.** New gate G-23 (§3) makes this checkable rather than remembered.
*dissent*: the fourth-name limb killed (K-2); the no-lever limb killed (K-6); the PRM guarantee is
"correct only by accident, and the accident is scheduled to end" (the ShadowPalette seat's own
cross-wave elevation, added by that arbiter and by no reader).
⟨ShadowPalette.md · SP-31 (reader-1 M7, ELEVATED) + SP-22 + the Closing's sequencing rider⟩

**X-W10.55 · PC-1 ≡ PS-4 — a JS-completed transition may never gate `done()` on a property the
central PRM policy strikes** (cl.1+cl.2) **BLOCKER (carried as the LAW row; the component dies at
X-W7).** *Mechanism*: `useHeightTransition.ts:35-39/:66-70` gate the only `done()` on
`propertyName === "height"`; glass `a11y-overrides.css` rewrites the PRM transition set to **five
colour properties `!important`** — height **absent** — beating the inline `style.transition` at
`:30/:61`; the 2-arity JS hooks mean Vue 3.5.35 installs **NO fallback** (`hasExplicitCallback`,
`runtime-dom.cjs.js:135/:196/:232`), so Vue waits on `done()` **forever**, and the opacity
`transitionend` that DOES fire returns early without removing its listener. Both re-readers derived
this independently — the corpus's ONLY convergent missed row — while its own D-7 limb 1 asserted the
exact inverse (K-1). Consequences that exist **only for reduced-motion users**: on LEAVE the
collapsed subtree stays mounted at `height:0; opacity:0` under `overflow-hidden`, leaving the
`Copy slug` `<button>` a **zero-pixel focusable tab stop, one per collapsed card, accumulating across
the Browse wall** (WCAG 2.4.3/2.4.7); on ENTER the `:44-46` cleanup is unreachable, pinning a stale
inline pixel height that clips any later re-wrap. Rider (PC-37 folds here): the `<Transition>` has
neither `name` nor `:css="false"`, so Vue also stamps undefined `v-enter-*`/`v-leave-*` classes and
runs nextFrame+reflow machinery around hooks that already force three reflows of their own.
*cureShape+locks*: **the canon owns the LAW; the successor's disclosure motion must be PRM-safe by
construction.** Cite the M-03/M-07 posture: **the 0.1s colour floor is CORRECT central governance and
is NOT part of this defect.** *sequencing*: law here; surface at X-W7.c/d. *dissent*: node-retention
arithmetic is residue.
⟨PaletteCard.md PC-1 (C4-1) + PC-37 · PaletteCardSwatches.md PS-4 (IDENTITY ≡ PC-1, with the two
reduced-motion-only consequences added)⟩

**X-W10.56 · D2-03 — `.stagger-children`'s fill-both contract silently suppresses every consumer's
`:active`** (cl.1) **MAJOR.** *Mechanism*: `animations.css:43-46` sets `animation:
stagger-child-in … both` inside `no-preference`, and a **FINISHED fill-both animation keeps
`to { transform: translateY(0) }` applied from the animation origin**, which outranks author
`:active` declarations — permanently, re-armed on each space change by `:key="animationKey"`. So
ConsoleRail's tokenized `:active` press scale **never renders in the default motion mode** and is a
**reduced-motion-EXCLUSIVE feature** — an inversion, not a PRM violation in either direction (M-26's
three guards all verified real). Measured cross-engine: transform pinned at identity in
webkit/chromium/forced-colors/dark; `matrix(0.96,…)` **under `reduce` alone**.
*cureShape+locks*: **split, and neither substitutes for the other.** The rail's press-leg cure rides
**X-W6** by adopting the producer idiom — glass-ui already ships this affordance on the INDEPENDENT
`scale` property (`.tap-squish:not([data-press-armed]):active { scale: var(--scale-press) }` with its
own `reduce` guard; `.interactive-item:active { scale: var(--scale-press-sm) }`), and because `scale`
is a separate animatable property the stagger keyframe's `transform` fill **cannot clobber it**. The
**class-level fill-both contract stays the X-W10 law row**: any consumer pairing `.stagger-children`
with `:active`/state transforms is silently suppressed. *sequencing*: —
*dissent*: the r2's "aim the WHOLE cure at the demo" routing is amended — it aims at the conformant
party. The blast-radius census is UNPROVEN (the named sibling `.channel-rows` has transform-less
children; the demo-wide claim was not run).
⟨picker-componentsliders-consolerail.md · D2-03 (with the seat's integrated cure amendment)⟩

**X-W10.57 · MISS-8 ≡ R-27 — `scrollIntoView({behavior:"smooth"})` bypasses BOTH reduced-motion
guards, at two live sites, against a house prohibition with a named incident** (cl.1) **MINOR /
MAJOR (grade differs by record).** *Mechanism*: per CSSOM-View the explicit `behavior` option
**overrides the computed `scroll-behavior`** the guards clamp (`animations.css:191`) — the MQ-1
blind-spot family, on the JS path. Census (two seats, agreeing): exactly **3** `scrollIntoView` sites
in demo — `EasingSpecimenStrip.vue:40-46`, **which IS the prohibition** ("Never `scrollIntoView` …
the O-19 desktop flat-netting root cause") and then reads PRM at `:47` and writes `scrollLeft` on one
owned port; and the two violators, `ComponentSliders.vue:213` and `useHeightTransition.ts:48`.
*cureShape+locks*: **the law exists in-repo and two callers ignore it** — the cure is adoption of the
existing idiom, both live sites in one row. *sequencing*: `useHeightTransition.ts` is also X-W10.55's
file — one touch. *dissent*: ConsoleRail routes MISS-8 → **X-W10** (rides D2-03's motion-law row);
ComponentSliders routes R-27 → **X-W6** with "canon echo X-W10". **Collapsed here: ONE identity, two
violation sites; the law is X-W10's, the two edits ride whichever wave opens the files first.** No
M-row covers it; carryable. Challenge-D's negative proof 1 is corrected, not carried.
⟨picker-componentsliders-consolerail.md MISS-8 · picker-componentsliders.md R-27⟩

**X-W10.58 · PS-5 + PS-6 + SH-6 — three motion-vocabulary defects whose CANON citations are dead**
(cl.1+cl.2) **MAJOR.** *Mechanism*: (a) the animated property is layout-forcing `height` read off a
synchronous `scrollHeight` with **deliberate double-forced reflow**, on bespoke **350/250 ms**
durations the file's own header admits match **no token**, while the tokenized alternative
(`--vj-morph-*`) is in use in the same parent. (b) the press leg of all four buttons is **untimed in
BOTH motion modes** (`active:scale-95` under `transition-colors`, whose property list excludes
`transform` at no-preference and under reduce alike) — and **the ruled migration target for this
exact recipe, `btn-interactive` (T.W5-R5 via `demo/DESIGN.md:237,247`), DOES NOT EXIST in glass-ui 7**
(grep: zero matches). (c) `@keyframes floating-panel-in` was **DELETED at `c84504d3`, not moved or
tokenized** (edict 6) — and so were the **dialog family (4 blocks + media dups), `card-menu-in`, and
`golden-shimmer`**; the installed producer ships **46** keyframes, **none** of those names, and of
the four families the surviving comment claims (`animations.css:2`, echoed at `DESIGN.md:273`) **only
`shimmer` is genuinely producer-supplied**. The false attribution masked the regression for four
months.
*cureShape+locks*: **the DESIGN.md ruling cites a dead target — a canon-CORRECTION obligation, not a
component patch.** Kin: `.floating-panel` and `btn-interactive` are the **phantom-name family** —
zero rules in demo CSS or shipped glass-ui, 5 live class attrs, with a comment narrating a retirement
onto a producer atom that does not exist; **DR-19 / CC-019 lock: the phantom family dies STRUCTURALLY
(types / eslint / build), NEVER by a proof script.** Motion returns via the producer's `glass-reveal`,
inheriting PRM for free. *sequencing*: canon correction precedes any consumer migration.
*dissent*: mitigation verified — zero live references to any deleted keyframe name remain in demo.
⟨PaletteCardSwatches.md PS-5 (D-7 limb 2) + PS-6 (D-7 limb 4 + L-3) · SwatchHoverMenu.md SH-6
(D-8/C-6/L-11) · CurrentPaletteEditor.md C-4/L-5/C-5-as-rescoped (the phantom-name family)⟩

**X-W10.59 · MISS-15 — a WRONG press figure is CANONIZED and survives every fix to the source**
(cl.1) **INFO.** *Mechanism*: `demo/DESIGN.md:247` records the R5 row as "`.channel-rail-item` press
leg (`--spring-press` @ **0.16s**)"; the true producer value is **`--spring-press-settle: 0.12s`**
× `--motion-tempo` (`scheme-spring.css`). Neither the r2 (which caught the SFC comment and stopped)
nor the first pass filed the canon copy. *cureShape+locks*: **D2-03's record correction must touch
the canon row too.** *sequencing*: with X-W10.65. *dissent*: —
⟨picker-componentsliders-consolerail.md · MISS-15⟩

**X-W10.60 · MISS-10 ≡ R-28 — THE CROSS-BOOKING COLLAPSE THE CORPUS ORDERED WRITTEN** (cl.1+cl.3)
**MINOR.** *Mechanism*: `:step="0.001"` normalized for every channel (`ComponentSliders.vue:68-70`)
is **sub-least-count** for the six integer-format spaces — `INTEGER_LEAST_COUNT` renders
rgb/hsl/hsv/hwb/lch/xyz at 0 decimals and the meter formats through it, so rgb moves 0.001×255 =
**0.255/press** and the rendered integer changes on roughly **every FOURTH press** (the control
visibly ignores ~3 of 4 keystrokes); kelvin's uniform step is 39 K/press. Reader B's "every other
press" is corrected upward.
*cureShape+locks*: **THE COLLAPSE, WRITTEN (per `consolerail.md:121`): the LEAST-COUNT LAW is
X-W10's — one identity with X-W10.62's `readoutDecimals` re-derivation, of which this is the
INPUT-SIDE half; the `:step` EDIT is X-W6's consumer cure (its R-28) and lands in X-W6's file.
Neither wave may claim the other's half, and X-W6's edit may not ship before the law states the
per-channel least count.** *sequencing*: X-W10's law precedes X-W6's edit.
*dissent*: the two records' routings were both real anchors; the divergence is resolved by splitting
law from edit, not by choosing a wave.
⟨picker-componentsliders-consolerail.md MISS-10 + §7 item 8 · picker-componentsliders.md R-28⟩

**X-W10.61 · D2-13 + MCP-36 + PM-18 + ABN-18 + ABN-20 + AP-21 ≡ DSL-18 — the motion/legibility token
ladder is unenforced at six independent sites** (cl.1) **MINOR.** *Mechanism*: `opacity-60` on a
range caption **against the sibling's own law forty lines away** ("never italic, never opacity — the
t-2000-41 legibility class") · `transition-shadow` bare where the same file uses `transition-colors
duration-fast` · `duration-fast` **restating the `@theme`-root default it was minted to make
unnecessary** (S.W3-5: "no per-callsite modifier") — harmless today, and it teaches the next author
that the modifier is required · bare `:open-delay="300"`/`:close-delay="0"` · **one row, two hover
tempos** (the Reset seat alone gets `duration-normal`; its four siblings ride the bare-utility
default) · `2.4s` untokenized and duplicated in the twin, **where the producer duration ladder has NO
2.4s rung** — so "tokenize it" is a **value change, not a rename**.
*cureShape+locks*: **any cure that adopts a token where no rung exists states the motion-design
decision explicitly.** *sequencing*: — *dissent*: MCP-36 demoted from reader-A's MINOR (one missing
utility class; token conformance, NOT a PRM defect). DSL-18/DSL-14 ≡ AP-21/AP-28, not re-booked.
⟨picker-componentsliders-consolerail.md D2-13 · MiniColorPicker.md MCP-36 + MCP-30 ·
PaletteCardMeta.md PM-18 · shell-dock-actionbutton.md ABN-18 + ABN-20 · ApiOfflineChip.md AP-21 ·
shell-dock-dockstatuslamp.md DSL-18 + DSL-14⟩

**X-W10.62 · M-4 + M-5 + M-7 + M-8 + M-9 + M-12 — the readout format law, and a NORMATIVE canon
clause that mandates the retired mechanism** (cl.1+cl.2) **MAJOR.** *Mechanism*: (a) the per-space
least count makes the readout near-useless as an instrument, and `readoutDecimals` **structurally
cannot fix it — it discards the `_component` argument its own doctrine demands** (`:32-34`: "a
meter's least count is per-QUANTITY"; `readoutReservation.ts:62-64` accepts and throws the component
away). At d=1: srgb-linear/display-p3/a98-rgb/prophoto-rgb/rec2020 r,g,b → **11** distinguishable
values per channel; oklab a,b → 9; oklch c → 6; ictcp ct,cp → 11; **jzazbz jz → 3** (`0.0/0.1/0.2`).
It compounds the edit path: the cell shows `0.4` for a true 0.4392. Fix is free — the ranges are
already in `PICKER_CHANNELS` and `readoutCh` already takes the component. (b) the **11.7ch capacity
constant is valid only while font ∝ container width**, and the `2.618rem` floor breaks the
proportionality at raised root font-size: at 24px root / 400px pane, `2.618rem`=62.8px beats
`11.65cqi`=46.6px, ch = 0.67em = 42.1px, capacity ≈ **8.65ch** — **rgb's 10.5ch tuple paints TWO
lines inside a ONE-line reservation**, the `h-fit` CardTitle grows past min-height and the card rect
moves on digit crossings: precisely the failure the module exists to prevent, and a WCAG 1.4.4 +
self-declared NORMATIVE breach. At default 16px root the derivation holds exactly as documented. (c)
`readoutFit` is dead code — every space returns 1, so `--readout-fit` is permanently 1 and five
consumers are unreachable; the docstrings' named beneficiary is falsified (ictcp packs 10.15 with
1.55ch slack — three stale citations). (d) `kelvin` is missing from `INTEGER_LEAST_COUNT` → `6500.0
K`. (e) hex mode swaps the cell to Fira Code **mid-derivation** while the ch capacity was minted for
Fraunces tabular — safe today only by margin. (f) **the NORMATIVE card-lock law in `demo/DESIGN.md`
mandates the RETIRED mechanism (per-cell `min-width` ch reservation, retired at R4; zero `min-width`
in the SFC), PROHIBITS the SHIPPED one ("No `flex-wrap` on a locked readout row" while `:14` ships
`flex-wrap` LOAD-BEARING), and mis-cites the module three ways** (dead path `color-picker/display/…`;
stale "derived from `COLOR_SPACE_RANGES`" against the live `PICKER_CHANNELS` import; the readout
assigned to Fira Code while the live readout is `font-display` Fraunces except hex) — **plus a
seventh drift in the module's OWN header.**
*cureShape+locks*: **a normative doc contradicting the shipped component in SEVEN places is a
GOVERNANCE defect; the canon re-author fixes the doc, and the format law ripples the reservation
table.** M-7 revives or deletes with M-4's re-derivation. *sequencing*: X-W1 owns the zoom gate for
(b). *dissent*: D-6's mechanism-as-stated (short tuples) killed; the reserved-line residual stands as
designed air. M-12's gap-mismatch clause is MOOT (hex mode has exactly one cell).
⟨picker-colorcomponentdisplay.md · M-4 · M-5 · M-7 · M-8 · M-9 · M-12 · D-6 residual⟩

**X-W10.63 · PG-11 + D-4-token-limb + PaneSlot-mobile-direction + PS-3 — four unchoreographed or
mis-choreographed state changes in the shell** (cl.1) **INFO · MAJOR-limb · MINOR · MINOR.**
*Mechanism*: (a) the empty↔populated swap is **the one unchoreographed state change on the Browse
route and the one a search keystroke triggers most often** — it pops inside the single `key="wall"`
branch while the route's out-in Transition choreographs only boot/error. (b) the ghost's exit fade is
**dead code**: `transition-opacity duration-200` transitions opacity alone while
`visibility`/`position`/`content-visibility` flip on frame 1 — the affordance **fades in and never
out** — and `duration-200` is a raw utility where DESIGN.md's recipe calls for tokens. (c) **on
mobile every swap enters from the LEFT and leaves to the LEFT**: `App.vue:77` gives the mobile
wrapper `--left`, so `animations.css:228-231` governs every mobile pane and the `--right` geometry
(`:233-236`) is **unreachable on mobile by construction** — forward and back are motion-identical and
the segmented control's direction is never expressed. (d) the deferral gate is a **string test
standing in for a motion predicate** while `prmInstant` sits one prop away.
*cureShape+locks*: motion tokens and the motion policy land here; the ghost-flip geometry is X-W5's.
*sequencing*: — *dissent*: PG-11 is **not** a motion-quarantine row (no M-xx carried); PS-3's premise
holds per quarantine §1 only.
⟨PaletteCardGrid.md PG-11 · shell-paneslot.md D-4 (motion-token limb) + NEW (reader-DU missed 5) +
PS-3⟩

**X-W10.64 · M-1 + D-14 + M-4(spectrum) — the entry animation DE-REGISTERS the instrument for 600ms
on every mount, and a measuring instrument runs at twice the house cadence** (cl.1) **MAJOR +
MINOR.** *Mechanism*: `both` supplies **BACKWARDS fill**, so for the 180ms delay the plate computes
`opacity: 0` — **invisible and hit-testable**, with `@pointerdown` bound unconditionally and no
readiness guard — and for the following 420ms both gradients paint at `background-size: 120%`
anchored at `0% 0%`, so **the colour SHOWN at plate fraction f is the field value at f/1.2** (at the
midpoint, s ≈ 0.417 where the user reads 0.5) and the black ramp's terminus sits 20% below the
plate's bottom edge. **Animating `background-size` on a gradient whose geometry IS the coordinate
system de-registers the instrument**; all three axes read the block and costed only its
rasterization. The literals (`420ms`/`180ms`) sit beside a correctly-tokenised sibling transition —
a **third motion species**. Separately, unargued producer-default overrides on a measuring
instrument: `cycle-duration 2000` vs default 4000, `range [15,85]` vs [20,80] — **continuous
non-communicative motion at twice the house cadence on the app's front door**.
*cureShape+locks*: readiness/registration → X-W4; **the token half and the cadence decision here.**
PRM-safe (the wobble rides the library's ONE `useRAFLoop`, frozen under PRM) — so a legibility /
edict-5 cost, not an a11y one; D-09's cure does not touch it. *sequencing*: —
*dissent*: on-screen witness rides UNPROVEN; mechanism and arithmetic are source-certain.
⟨picker-spectrumcanvas.md · M-1 · D-14 · M-4⟩

**X-W10.65 · MERGED CROSS FOLD — THE DESIGN.md TRUTH SURFACE: eleven citations that no longer
resolve** (cl.1) **MAJOR (extends G-3).** *Mechanism*: enumerated, each measured by its own seat —
`:58` claims the control bars consume `.section-subtitle`, which is **dead CSS with 0 `.vue`
consumers** (and `MixConfigBar.vue:95-96` records the deletion that orphaned it) · `:67-78`'s
card-lock law (see X-W10.62f) · `:237/:247` cite `btn-interactive`, **0 matches in glass-ui 7** ·
`:247`'s **0.16s** vs the producer's 0.12s · `:250`'s producer-gated EXPECTED-RED row · `:267` cites
`PointerDebugOverlay.vue:266` for a blink living at **`:179`** (`:265-266` is `.debug-btn:hover`) ·
`:273`'s keyframe-family attribution, **3 of 4 families deleted at `c84504d3`** · `:336/:342`'s gold
prohibition, violated by the surface it governs (X-W10.72) · `:375` certifies PaneHeader
"reduced-motion-correct" — falsified by MQ-1 — **via two DEAD paths** (`panes/PaneHeader.vue`,
`style.css`) · `:386` cites `PaletteDialog.vue` **with a line number; no such file exists** · `:388`
offers `.pane-scroll-fade` as an exemplar of the **no-global-utility-class rule it violates** (the
block is UNSCOPED) · plus the bespoke-keep row citing `ActionButton.vue:117,120,121` where the live
literals are **`:122/:125/:126`** — the exemption has silently stopped covering what it exempts · and
`PreviewRamp`'s `index.ts:13` pointing at `docs/tranches/T/audit/w6-lane-d-record.md`, **ENOENT**.
Z-tier falsifications carried: "all z reaches route through glass-ui `--z-*`" is false
(`--z-debug` undefined, `--z-ornament` demo-local, `--z-popover: 70` vs the served 130).
*cureShape+locks*: **citations anchor to SECTION NAMES, never to line numbers of deletable files**
(W10's own §5.c rule, here given its RED evidence). *sequencing*: the canon re-author (`.c`) does
these; `.f`'s satellite pass must not re-mint them. *dissent*: —
⟨wb-mix-configbar.md MC-19 · PaletteCardSwatches.md PS-6 ·
picker-componentsliders-consolerail.md MISS-15 · picker-pointerdebugoverlay.md DOC-1 ·
SwatchHoverMenu.md SH-6 · PaneHeader.md PH-25 · PaletteCard.md PC-39 ·
shell-dock-actionbutton.md ABN-14(c) · picker-colorcomponentdisplay.md M-9 ·
PreviewRamp.md R-23 · shell-dock-parseechoreadout.md A-8⟩

**X-W10.66 · PC-28 + AF-24 ≡ MCP-19 ≡ AP-41 ≡ PM-19 ≡ DSL-21 — duplicate and dead style authorities**
(cl.1) **MINOR / INFO.** *Mechanism*: (a) a dead `@source "../../color-picker/**"` governs
(vacuously) the JIT emission of **every utility in the cone** — the path resolves to a nonexistent
`<repo>/color-picker`, masked by the `../**` sibling, **while the file's header sells the explicit
scan as CI-assertable coverage**. (b) `.fira-code` is defined **twice**: the demo fork
(`utils.css:9-11`, family only, unlayered) shadows the producer `@utility` and **drops
`font-feature-settings: "liga","calt"`**; the winner is load-order-dependent and currently
coincident, so the fork changes nothing and is dead — **until either side changes.** Five independent
witnesses. *cureShape+locks*: **canon fold of duplicate style authorities (the AF-24 precedent);
one definition, the fork dies.** *sequencing*: — *dissent*: —
⟨PaletteCard.md PC-28 (L-48) · ActionFeedback.md AF-24 · MiniColorPicker.md MCP-19 ·
PaletteCardMenu.md AP-41 (C3-6) · PaletteCardMeta.md PM-19 · shell-dock-dockstatuslamp.md DSL-21⟩

**X-W10.67 · XP-19 ≡ EC-27 ≡ EY-14 — `.plate-ink`, a token by its own comments, is declared five
times identically, and a sixth consumer still rides the token it retired** (cl.1+cl.5) **MINOR.**
*Mechanism*: `grep '.plate-ink {'` = **5 files** (three in the extract subtree, plus
`EmptyState.vue` and `ErrorBoundary.vue:84`), each `color: var(--ink-muted, var(--muted-foreground))`
and each carrying the same "certified de-emphasis rung" comment — a token spelled five times as a
class. Independently, `ImageEyedropper.vue:35`'s readout ink is the **retired static
`text-muted-foreground`**, the exact token both siblings replaced with the certified `--ink-muted`
rung (`ImageDropZone.vue:103-111`, `ExtractWorkbench.vue:284-292`, E1-R1 comments;
`useAtmosphereBoot.ts:103` live-stamps `--ink-muted`).
*cureShape+locks*: **one `@utility` home — unit .f owns `foundation.css`** (XP-19's own routing);
the producer-side option rides the standing BH relay. The eyedropper's adoption is a **rider on any
wave touching the file**, not an independent edit.
*sequencing*: the `@utility` home lands before the five deletions, or the ink goes dark for one
commit. *dissent*: EC-27 records the identity as XP-19's and does **not** re-book; EY-14 books the
utility home as X-W10's and the adoption as a rider. The 4.07:1 ratio is live-only residue.
**Bounds**: all five declaring files are outside W10's §4 — see §4 row 17.
⟨wb-extract-pane.md XP-19 (L-6) · wb-extract-controls.md EC-27 (L-13, FOLD not re-booked) ·
wb-extract-imageeyedropper.md EY-14 (D-08 token half ≡ C-r3 II-3 ≡ r1 D-14)⟩

**X-W10.68 · AB-25 ≡ MISS-1 ≡ AB-30(canon half) — the dock's uncited proportion constants, and a
producer token declared five times and consumed zero** (cl.1) **MAJOR.** *Mechanism*: (a) `w-6 h-6`
(24px) beats the producer's 20px `--dock-icon-glyph` (`.dock-icon-button > svg { width:
var(--dock-icon-glyph, 1.25rem) }`, controls/icon-button.css; Tailwind utilities outrank
`@layer components`) — and the 24px hardcode is the **house norm** at this seat (Back arrow,
ActionBarToggle, all five ActionButtons), so it is a canon question, not a component slip.
(b) `gap-0` at `ActionBarLayer.vue:100` **zeroes the dock's inter-control rhythm inside this run**:
the producer's `.dock-face-content` gap is 6px, so the Back-side separator gets flex-gap + margin
while this run's separator/toggle get the margin alone — **exactly half** — and this, not an
undersized seat, is the mechanism D-14 mis-diagnosed as focus-ring crowding (whose prescribed
touch-target cure K-2 killed). An interval set to zero by an unexplained utility earns nothing
(PROPORTION-AUDIT law 4). (c) `--dock-control-glyph-size` is **declared 5× and consumed by zero
rules** — a dead producer token.
*cureShape+locks*: **this is exactly W10's own §2a goal-criterion surface — "uncited proportion
constants" — and it is the only one of the owner's four marked surfaces with no row in the dated
spec.** The dead-token line rides the standing glass BH relay. AB-30's touch limb (the five primary
actions are **32×32 on coarse pointers with no floor**; the producer's coarse floor is scoped
`.dock-icon-button:not(…):not(:where(.glass-dock *))` and `--dock-touch-target` 2.75rem cannot reach
a plain `<button>` by any route) is **X-W4.d's cure clause; only its canon half lands here.**
*sequencing*: with X-W10.69 (same run, same seat). *dissent*: the ring-crossing subpixel claim and
the tablet-landscape coarse rects stay **UNPROVEN-NEEDS-LIVE**; K-2's touch-target charge stays
DEAD. **Bounds**: `ActionBarLayer.vue` is X-W4/X-W8-held; `ActionButton.vue` is in **NO** wave's
§4 — §4 row 18.
⟨shell-dock-actionbarlayer.md AB-25 (D-8 glyph leg) + MISS-1 (R-DU) + AB-30 (R-DU′ M-4, canon half)⟩

**X-W10.69 · MISS-2 ≡ AB-31 — one primitive, three hover grammars and two ink voices; the producer's
selected ink is dead** (cl.1+cl.2) **MAJOR + MINOR.** *Mechanism*: the toggle glyph opts out of
`DockControl`'s own ink register by **hardcoding `stroke`** (`:138`), severing `color`-transition,
hover ink AND active/pressed ink from the glyph, then re-implements hover ink bespoke (`:149-151`
plus the inline custom property at `:139`) — two hover voices on one producer primitive. Unique at
this seat: the five neighbouring ActionButtons carry the same class on a plain `<button>`,
overriding nothing. Extended: **three hover grammars in one ~184px run** — the producer glass seat
(Back, toggle), the bespoke `scale(1.2)`+stroke latch (five actions, `ActionButton.vue:117-120`),
and the toggle stacking a bespoke accent stroke ON TOP of its seat — with the accent register bound
to the **24px glyph inside the 40px seat**, leaving an annulus where the seat lights and the accent
does not.
*cureShape+locks*: **one ink voice, one hover grammar per run** — and the row is stated this way so
the cure cannot ship as a glyph patch. **Executed WITH AB-8's `active` prop** (MISS-2's own
sequencing clause) so the restored register has a state to show. Owner edicts bite twice here:
glass-first, and root-level styling over per-instance overrides.
*sequencing*: **after X-W4.d seats the actions on `DockControl`** (X-W10.68's AB-30 clause) — an ink
register restored onto a plain `<button>` restores nothing. *dissent*: AB-28's mechanism is
**subsumed** by MISS-2 (with a normal-mode consequence added), not re-booked; the hover-annulus
magnitude is UNPROVEN cell 13.
⟨shell-dock-actionbarlayer.md MISS-2 (R-DU) + AB-31 (R-DU′ M-2+M-3, extends MISS-2)⟩

**X-W10.70 · GAB-5 / K-8 — an OWNER/CANON item arrives with a live canon collision already
registered** (cl.4+cl.3) **MAJOR (input, never enacted here).** *Mechanism*: opening the action
branch **withdraws the dock's navigation face to show verbs that already exist beside their
subjects** — verified duplication: Generate ×3 local (`GenerateControls.vue:157-184`), Gradient Copy
CSS local (`GradientVisualizer.vue:254-256`), Mix primary + result Copy/Reset local
(`MixConfigBar.vue:162-170`, `MixResultDisplay.vue:119-143`); r-LC's caveat verified — Gradient's
`resetGradient`/`seedFromPalette` have **no** route-local seat (sole invocation path
`usePaneRouter.ts:208/:210`). The layer swap and producer hiding verified (`Dock.vue:108-117/:153-158`;
glass `layers.css` `.dock-layer:not(.is-active)…visibility:hidden`).
*cureShape+locks*: **BINDING (K-8, the seat's own kill, caught by neither reader).** Challenge-D's
per-verb DELETE table is **DEMOTED from ruling to input** because its canon citation is selective:
`CONVERGENCE-REAUDIT-2026-07-29.md:104-125` makes the scene `action` role optional **and the same
terminal diagram assigns the Dock "active-scene commands"**; `VISUAL-CONSTITUTION.md:194` and
`DECISIONS.md` B-13 bind Generate's three verbs into ONE Dock control set; and GenerateControls-adj
ruling **R-B**, a standing adjudication, ruled the PLATE cluster the violation and banked its
retirement INTO the dock set. **No seat may execute challenge-D's command-seat table for Generate
while R-B stands un-overturned.** A corpus may argue canon change; it may not silently overrule it.
*sequencing*: the renderer + untyped descriptor branch **die independently at X.W4.d** (its bounds
already order it) — the per-verb seat question does not gate that DELETE and must not be bundled
into it. *dissent*: recorded that this row is the ONE place in the fold where the corpus asks
X-W10 to make a **product** decision rather than state a law; it is carried as an owner item.
⟨shell-dock-genericactionbar.md GAB-5 (D-GA-1, escalated) + K-8 (the canon-collision kill)⟩

**X-W10.71 · GAB-12 — two terminal seats re-ratify a register the dock retired** (cl.1) **MINOR.**
*Mechanism*: `GradientVisualizer.vue:254` and `MixResultDisplay.vue:121-143` use
`<DockControl compact title="…">` with **no `aria-label`**; `DockControl`'s `__VLS_Props` declares no
`title` (d.ts read), so it falls through as the **UA tooltip attribute** — the native-`title`
register `Dock.vue:140-142` records as retired dock-wide.
*cureShape+locks*: register canon, with two per-site notes carried verbatim: the Gradient seat
**sits in X-W6's file bounds and may move there**; `MixResultDisplay`'s Copy `:title` is **also its
only success-confirmation channel for AT** and must be **cured together with its `copied` state** —
deleting the attribute alone removes an affordance. *sequencing*: after the canon states the
naming register; the two edits ride their owning waves. *dissent*: GAB-12's **Mix half lands
NO-WAVE-OWNER** in its own record — the register ruling is X-W10's, the Mix edit has no home.
⟨shell-dock-genericactionbar.md · GAB-12 (r-DU missed find, verified)⟩

**X-W10.72 · D-37 ≡ OM-4(radius limb) — four declared radius tokens resolve to six effective radii
with no derivation law, and one declaration spans a 4.8× curvature swing** (cl.1+cl.2) **MAJOR.**
*Mechanism*: (a) on the specimen strip, four declared radius tokens produce **six effective radii**;
nested corners are **non-concentric at four sites**; every declaration checks at its line
(`:114/:153/:176/:242/:274`) and **the strip itself declares ZERO radii** — it inherits them.
(b) at the easing editor, **one `rounded-card` declaration** (`:114`; `--radius-card:
var(--radius-2xl)`) spans boxes of **40.94px and 196.36px** — a **~4.8× curvature swing that flips on
click**, with no transition; both of OM-4's owner readings fall out of that single declaration.
*cureShape+locks*: **adopt the producer's live `--radius-strip` role token** — the R4-2 kill is what
STRENGTHENS this cure (the token has a producer consumer, so adoption is not invention). **Rider,
adopted as fleet input, not as scope**: LC's census — `rounded-md` ×14, **non-role rungs 74 vs role
tokens 27 (≈2.7:1)** — makes the derivation law a **fleet edict with a migration behind it**, and is
**wave-sizing input for X-W6.d and X-W10**, never a scope change at the component.
*sequencing*: the derivation law is authored at `.c` before any consumer adopts; a role token
adopted without the law just renames the drift. *dissent*: exact ratio inputs are committed probe
JSON (frames-r3), residue-free; DU re-verified the declaration sites and this seat's sampled reads
concur.
⟨wb-gradient-easingspecimenstrip.md D-37 / C-02(a) / L-12 / R4-2-as-refined + the LC fleet-census
rider · wb-gradient-easingeditor.md D3-05 / OM-4 (radius limb)⟩

**X-W10.73 · D3-04 ≡ GV-25 ≡ MISS-1(kin) — proximity is inverted, and one interval does all the
rhythm work** (cl.1) **MAJOR.** *Mechanism*: at the easing editor, **8px between sibling rows vs
10px between one row's own parts** (`:107` `gap-2`, `:146` `gap-2.5`, head `py-2` at `:119`) — the
Gestalt reading is inverted: parts read as less related than wholes. At the visualizer, **one
`gap-5` does all rhythm work** across the composition, so no interval expresses hierarchy at all.
Kin, already banked at X-W10.68: `gap-0` zeroing the dock run.
*cureShape+locks*: the canon's **proportion/derivation section owns the interval ladder** —
PROPORTION-AUDIT law 4 (an unexplained interval earns nothing) is the clause, and every number
folded from PROPORTION-AUDIT must be **re-measured first** (W10 §11 guardrail 3, the 3.4×
undercount). *sequencing*: canon before the X-W6.b consumer edits. *dissent*: GV-25's route is
X-W6.b **under X-W10 law** — the law is this row, the edit is not.
⟨wb-gradient-easingeditor.md D3-04 / OM-4 (proximity limb) · wb-gradient-visualizer.md GV-25⟩

**X-W10.74 · D-41 / D-08 — a hand-rolled 9px rung below the house floor, and a 1.82:1 protagonist
inversion** (cl.1) **MAJOR.** *Mechanism*: the strip hand-rolls a **9px rung** (`:149`, `:195` —
`0.5625rem`) **below `--type-micro` (11px), the house's smallest named rung** — which the chip cell
recipe already applies and the seat then overrides **downward**. Consequence: the card's **largest
ink is the machine literal** (16.4px from the `--type-small` clamp, reproduces from tokens) while
its **twenty-seven choices are its smallest** — a **1.82:1 protagonist inversion**, and the
selection signal itself rides the 9px labels.
*cureShape+locks*: the closed VC §4 matrix has **no rung below `--type-micro`**, so this is not a
token swap but a **matrix question**: either the matrix admits a sub-micro rung with a stated
minimum, or the 9px dies. **Identity with X-W10.30's family-scoped ruling** — charging this file
alone moves it away from its siblings. *sequencing*: the matrix ruling precedes the edit; X-W6 ◆
executes. *dissent*: —
⟨wb-gradient-easingspecimenstrip.md · D-41 / D-08⟩

**X-W10.75 · D-09 + D-04/D-33 — an instrument portrait with no frame, and a grouping line the
binding inventory forbids** (cl.1) **MAJOR.** *Mechanism*: (a) a **bare polyline in an empty unit
viewBox** — no frame, no baseline, no axis — so `steppedEase(1,"jump-start") ≡ 1` renders
`step-start`'s portrait as a **horizontal rule at the top of an invisible box**, unreadable against
the component's own shape-recognition thesis (`:184`). (b) **seven `border-left`-family rules paint
a grouping line** (`:143-146`) in a composition whose **binding inventory sets retained lines to
`none`**, atop a **28px-vs-4px spacing ratio that already carries the grouping** — the line is
redundant and prohibited at once.
*cureShape+locks*: **kin to X-W10.25's MR-34 trap, ruled the same way**: whether a plate may take an
edge AT ALL is a **canon decision, not a class application** (OBC:77 — boundaries `[]`, retained line
`none`, "distinct through geometry"). The instrument-frame question is the canon's chroma/measure
section, not a component patch. *sequencing*: canon edge-ruling precedes both limbs; X-W6 ◆
executes. *dissent*: —
⟨wb-gradient-easingspecimenstrip.md · D-09 (carried, first seal) · D-04 / D-33 (carried, first
seal)⟩

**X-W10.76 · GV-26 — four peer `h3`s and no `h1`/`h2`** (cl.1) **MINOR.** *Mechanism*: the
visualizer composition ships **four peer `h3` elements with no `h1` or `h2` above them** — the
document outline states a hierarchy the design does not have, and AT reads four coordinate siblings
at depth 3 under nothing. *cureShape+locks*: the canon's **product-surfaces section states the
heading register per composition**; the named gap stands as filed. *sequencing*: — *dissent*:
banked as an **X-W10 rider** in its own record, not as a component cure.
⟨wb-gradient-visualizer.md · GV-26 (carried from r1, route unchanged)⟩

### 2g. The canon-echo band — rows routed here as LAW with their edits elsewhere

*These rows are not weaker for being echoes. Each names a decision X-W10 must make **before** its
owning wave can execute; the corpus books them as "→ X-W\<n\> **under X-W10's law**", and a wave that
executes without the law executes unlawed. They are folded, not transcribed: each carries the lock
that makes the law checkable.*

**X-W10.77 · BlobPane D-8 — a producer recipe re-rolled byte-for-byte EXCEPT the size, so no rung
separates furniture from content** (cl.1) **MINOR.** *Mechanism*: `.config-section-title`
(`ConfigSliderPane.vue:237-243`) re-rolls the producer's `.section-label` recipe byte-for-byte
**except the size** — `--type-small` (the row label's own rung) instead of `--type-caption` — so the
section heading and the rows it heads sit on the **identical rung**: the furniture/content step is
deleted, not restyled. *cureShape+locks*: **IDENTITY-FOLD → ConfigSliderPane D-7 = G10a ("one recipe
per role")** with the `.section-label` dist fact as **new evidence**; X-W10 supplies the rung law,
G10a supplies the gate. The family limb is **KILLED (K-6)** and may not be re-minted as a fleet
charge. *sequencing*: the rung law precedes G10a's application. *dissent*: banked
**NO-X-WAVE-OWNER** in its own record — `ConfigSliderPane.vue` is in no wave's §4 (§4 row 15).
⟨BlobPane.md · D-8 size-rung residue + A-missed-M5⟩

**X-W10.78 · HeroBlob D-3 — ambient motion cannot terminate within canon's five seconds, by source
arithmetic alone, and no pause control exists** (cl.1+cl.2) **MAJOR.** *Mechanism*:
`BLOB_IDLE_MS 2000 + SLEEPY_POSE_MS 3300 = 5300 ms`, **armed at mount** by the `{immediate:true}`
watch (`HeroBlob:211-212/:279/:246-250`) — the five-second law fails on constants, no browser
needed — and `:paused` is **one-way with an inert root**, so **both** branches of VISUAL-CONSTITUTION
§6's law fail (terminate, or give the user a stop). **Every KeepAlive return re-arms the full cycle.**
*cureShape+locks*: the canon states the termination law and its escape; **the wiring is X-W6's**
(`v-model:paused` to the shipped `DockBackgroundToggle` — the producer doc names this exact shape)
and the settled-seam park that restores the tight runway is **X-W2's (CC-035)**. **X-W10 may not
author a duration that the two shipped constants already falsify** — the law must state which branch
the product takes. *sequencing*: canon branch-choice precedes X-W2/X-W6. *dissent*: the rendered
7.0-8.1s timeline is residue (corroboration only); the arithmetic is source-certain.
⟨picker-heroblob.md · row 10 (D-3)⟩

**X-W10.79 · HeroBlob D-8/D-9 — a canon inequality with no live referent, and a magic ratio with
four restatements and zero derivations** (cl.1+cl.4) **MAJOR.** *Mechanism*: a Blob-derived vertical
minimum still exists (`min-height: calc(0.76·fp − 0.75rem)`, `seat.css:47/:82/:88-89`) **binding at
390 and at 200% zoom**, against canon's verbatim ban; `--blob-seat: 0` leaves PR-03 unimplemented;
**`--instrument-title-gap` has ZERO live referents, so canon §3.2's inequality is UNMEASURABLE**;
and **0.76 is an underived magic ratio** — the sheet derives every other number from one closed
identity and this one from nothing (**4 restatements, 0 derivations**; the ≈0.65·fp+12px
near-coincidence diverges 7px at the 11rem arm).
*cureShape+locks*: **BINDING — "G must gain a live referent or the law must be re-authored"** (the
record's own clause). A canon inequality whose term has no consumer is **folklore with a filename**
(G-2's own consumer test, applied to canon's internals). The seat's layout tax dies with the stage
transposition at X-W6; **the reservation must derive or die.** *sequencing*: canon first — X-W6
cannot transpose against an unmeasurable inequality. *dissent*: D-9's emerge/arm inversion critique
(a lifecycle seam used as a resize API) stands on source; its 537ms attribution is residue.
⟨picker-heroblob.md · row 15 (D-8 / D-9 + the 0.76 constant)⟩

**X-W10.80 · MERGED CROSS FOLD — forced-colors has no canon jurisdiction, and the one roster that
exists protects an element that never paints** (cl.1+cl.5) **MAJOR.** *Mechanism*: (a)
`foundation.css:678-694`'s tier-1 roster lists `.glass-slider[data-variant="spectrum"]
.slider-range` — **which the producer sets to `background: 0 0`** — while the actual colour carriers
(`.slider-track` fed by `--slider-track-bg`, and the rail div whose whole identity is an inline
background + inset shadow, i.e. **the two things WHCM strips**) are **ABSENT**; the print block
(`:815-840`) adds blanket `box-shadow: none !important` and its smaller roster omits them too. Scope
is app-wide (every spectrum ramp incl. `ComponentSliders:197`). Corpus history is itself the
finding: pass-1 D-1 BLOCKER → pass-3 hypothesis → **pass-4 dropped it entirely**. (b) forced-colors
is **undesigned for the HeroBlob seat** — zero rules reach it, and canvas content is exempt by spec,
so the WebGL bead **opts out of the user's palette** by construction.
*cureShape+locks*: **the roster mis-aim needs no browser**; the rendered blank needs a real-WHCM
witness (residue → X-W1 goldens). **The jurisdiction is the defect**: three records disagree on who
owns the roster (R-26 → X-W6 canon-echo-W10 · EC-8 → NO-WAVE-OWNER, "`foundation.css`'s
forced-colors block exceeds X-W10's declared carve" · GEN-32 → X-W10.f "`foundation.css` owner"),
and **EC-8 is right about the bytes** — W10 §4 carves `foundation.css` at `.underline-tabs:554`
**survivor only**. Resolved by **widening the carve, never by re-routing** (§4 row 10).
*sequencing*: the carve widening is a §4 act at wave-open, before any `.f` edit.
*dissent*: the WebKit-emulation caveat in pass-3's demotion is recorded as an **honest** demotion and
is not held against the row.
⟨wb-extract-controls.md EC-8 (reader-1 miss M1) · picker-heroblob.md row 17 (D-11) ·
picker-componentsliders.md R-26 (canon echo) · wb-generate-pane.md GEN-32 (`foundation.css` owner)⟩

**X-W10.81 · PCS-2 + PCS-8 + PCS-27 — three cures routed "under X-W10 law" to a wave whose law does
not yet exist; W10 has NO skeleton row and NO absence rule** (cl.1+cl.3) **MAJOR + MINOR.**
*Mechanism*: (a) **PCS-2** — the certified `--skeleton-ink` never reaches a pixel: glass-ui
hardcodes `background: var(--muted)`, and `--muted` = `--neutral-1`, a fixed neutral constructed
independently of `--well-bg` (`color-mix(in oklab, var(--card) 92%, var(--foreground) 8%)`,
`foundation.css:328`), so **no signed relation can hold** — polarity is INVERTED in **both** schemes
(light: muted `rgb(246,243,239)` LIGHTER than well `rgb(233,225,217)`; dark: muted `rgb(31,28,25)`
DARKER than well ≈`rgb(66,55,47)`) — while `utils.css:36-43` names this component a consumer of the
certified recipe at `:57-61`. It is not one. (b) **PCS-8** — glass-ui ships, **by name**, the ghost
of the exact species being ghosted (`WatercolorDot variant="ghost"`, d.ts `:30/:43`) while the
component hand-rolls a grey square **whose radius it cannot control**, so loading→loaded morphs a
hard 4px square into an organic seeded blob (owner edict 4 binds). (c) **PCS-27** — the colour strip
has **no `<style>` block and no transition/animation utility anywhere**: `animationName: none`
follows from bytes, and the strip **hard-cuts on every Regenerate while its plate-mate rides the
register**; zero M-xx quarantine rows name it.
*cureShape+locks*: **W10 has NO skeleton row — its own record says so verbatim ("X-W10 has no
skeleton row; its canon binds as law, not as owner")** — so the law these three cures cite must be
WRITTEN or X-W7.c/.g executes against a citation to nothing. Two clauses are owed: **the ghost-ink
channel** (a ghost re-inks through the ONE live channel, or the certified recipe's prose is corrected
— A-4's NO-WAVE-OWNER half) and **the absence rule** (is a hard cut beside a choreographed sibling a
violation, or a recorded absence? PCS-27 is graded as an absence *because no law says otherwise*).
"The ghost of a WatercolorDot is a ghost WatercolorDot, or the shell decides otherwise **explicitly**."
*sequencing*: **X-W10's two clauses STRICTLY PRECEDE X-W7.c/.g** — this is a law-before-edit edge of
the same shape as X-W10.60. *dissent*: the `:5603` ledger correction (R-4) travels with PCS-2; exact
painted values/contrasts are U-1 residue; whether the absence is a *violation* stays a design
judgment until the canon rules it.
⟨PaletteCardSkeleton.md PCS-2 (D-2/C-2) + PCS-8 (D-8) · PaletteColorStrip.md PCS-27 (D8) +
the D8 adjudication row⟩

**X-W10.82 · ADJ-9 — a debug surface with zero design tokens, and a KEEP ruling that is a sequencing
lock on another wave** (cl.1+cl.3) **INFO + MINOR.** *Mechanism*: (a) **zero design tokens** in
`DebugEventLog.vue` beside five `var()` uses in its parent — the token half of D-3's residue.
(b) `.debug-section` (`:47-49`) and `.debug-section-title` (`:51-58`) are **byte-duplicated from the
parent** (`:196-207`); under Vue scoped CSS the parent's scope id stamps the child's ROOT (`:3`), so
the `.debug-section` copy is redundant **today** while `.debug-section-title`, a descendant,
**genuinely needs its copy** — *a cleanup must not delete the wrong one*. (c) `.debug-event`
re-declares `font-family`/`font-size`/`line-height` (`:75-77`) inherited verbatim from
`.debug-overlay` (parent `:143-145`).
*cureShape+locks*: **the KEEP ruling is BINDING on X-W8**: the two `pointer-events: none`
declarations (`:61-67`) are no-ops under today's ancestors **but are the designated landing mechanism
for the sibling's ratified D-N3 cure** ("move `pointer-events: none` off `.debug-scroll` ONTO the
rows inside DebugEventLog", sibling r2:207) — the sibling's posture **WINS** over D-1/D-5's selection
demand. **Deleting `:61-67` as "dead code" before D-N3 lands re-opens the touch-stealing hole the
file exists to prevent.** *sequencing*: X-W8's subtraction is **sequenced with the sibling's
scrollport cure**, not before it. *dissent*: the record routes every confirmed row to X-W8 per
CC-081 with **no NO-WAVE-OWNER rows** — and yet **the file is in no wave's §4** (§4 row 3): the
record's own routing is unexecutable as written.
⟨picker-debugeventlog.md · ADJ-9 (a)(b)(c) + the (d) KEEP ruling⟩

**X-W10.83 · EC-22 — one `trackInk` feeds protagonist and support alike, and the most saturated
object on the card is a neutral scalar weight** (cl.1) **MINOR.** *Mechanism*: no tier
differentiation between the protagonist axis and the support axis — **one `trackInk` feeds both with
no semantic split** — so in the developed state **kC, a neutral scalar weight, is the most saturated
object on the card**: chroma is spent on the least meaningful quantity. *cureShape+locks*: the
canon's **chroma-fidelity section owns the tier split**; the surface cure is **X-W4.g via CC-105**
(census-gated), with X-W10 supplying only the tier law. *sequencing*: law before CC-105's census
gate opens. *dissent*: the "byte-identical in every state" evidence was **pre-image-only and is
KILLED**; the design conclusion survives on the developed capture alone — recorded so the killed
evidence is not resurrected as the warrant.
⟨wb-extract-controls.md · EC-22 (D4-3 as-rescoped, K-E)⟩

**X-W10.84 · D-5 ≡ AL-M6 + AL-M1/AL-M2 — the Admin anatomy: a separator grant violated on five
routes, four identity registers for one semantic slot, and a contract comment honoured by zero of
its three implementations** (cl.1+cl.5) **MAJOR.** *Mechanism*: (a) the per-row **four-sided stroke +
corner radius** violates the optical-bench separator grant for **all five Admin routes**
(`OPTICAL-BENCH-COMPOSITIONS.md:80-92` — "Any additional line … terminal row rule, caster stroke or
corner rule is a defect"); the cure altitude is **one class change on the anatomy** (border/rounding
→ single inter-row separators) applied suite-wide, **amending nothing in the canon**. (b) **four
Admin routes ship four different primary-identity registers** under the one-anatomy canon
(VC:218): Names `text-small font-medium` Jakarta (`:50`) · Audit Badge + `text-small
text-muted-foreground tabular-nums` (`:67-72`) · Flagged `font-display font-medium text-subheading`
Fraunces 1.272rem (`:65`) · Users `.slug-pill` = `text-mono-small font-bold` pill
(`foundation.css:585-587`). `AdminListItem.vue:2-5` is the anatomy's **only written typography
statement and is honoured by zero of the four** — and is itself **wrong**: it prescribes
`text-caption`, which is **italic by producer definition**, while all three shipped secondary lines
refuse it for `text-mono-small` under the recorded F-9 readout ruling. (c) the F-9 swap **silently
deleted the designed size step**: `text-small` and `text-mono-small` resolve to the **identical**
`--type-small`/`--type-leading-small`, so the two-line hierarchy rests on family/weight/colour with
a **zero size delta**, where the comment's design carried a real clamp-floor step (0.875rem vs
0.75rem).
*cureShape+locks*: **(i)** (a) is the **same law as X-W10.44** (AAP-14's 80-painted-segments-against-
a-budget-of-19) — ONE boundary law, two witnesses, counted once. **(ii)** (b) is the **same
family-scoped ruling as X-W10.30** — charging any single panel moves it away from its siblings.
**(iii)** the zero size delta **may be ruled acceptable — the defect is that it was never ruled**;
the cure is a deliberate design restatement, and F-9's register choice was deliberate while the
size-step loss was its **unruled consequence**. **(iv)** per the ColorSpaceSelector precedent, **any
wave touching `AdminListItem.vue` must correct the comment.**
*sequencing*: the boundary law and the register ruling land at `.c`; the four panel surfaces are
X-W7's (all in its §4) **as a rider — no X-W7 unit claims register unification.**
*dissent*: **D-5 and L-3 are banked NO-WAVE-OWNER** — `AdminListItem.vue` sits in **no** tranche-X
File Bounds (verified across W0-W11 by that seat and re-verified here), and no X unit claims the
separator grammar, so the cure is *reachable* but *unclaimed*. The **altitude** of the five-copy
consolidation (demo-local shared component vs glass-ui compound primitive) is an **owner call** —
`feedback_kiss_no_contrivance` vs `feedback_glass_ui_first_class` are in genuine tension — recorded,
not decided. Re-resolve paths after MT-AU1's `browser/admin/` → `palettes/admin/` re-home.
⟨AdminListItem.md D-5 (rescoped-confirmed) · AL-M6 · AL-M1 · AL-M2 · L-3 (the structural spine,
cross-booked) · AdminAuditPanel.md AAP-14 (the boundary-law identity, counted at X-W10.44)⟩

**X-W10.85 · THE PROSE-MENTION RULE — five components route 112 row-instances into X-W8/X-W10 and
appear in NO wave's File Bounds table** (cl.5) **BLOCKER (bounds).** *Mechanism*: this seat's grep
over all twelve `waves/W0..W11.md`, at HEAD:

| file | wave-file hits | nature of the hit |
|---|---|---|
| `demo/picker/visual/DebugEventLog.vue` | **0** | absent entirely |
| `demo/shell/dock/ParseEchoReadout.vue` | **0** | absent entirely |
| `demo/shell/dock/DockStatusLamp.vue` | 2 | `W10:305-307` G-4 conforming-block PROSE · `W5:274` C7 viewport-query CENSUS |
| `demo/palettes/browser/status/ApiOfflineChip.vue` | 1 | `W10:307` G-4 conforming-block PROSE |
| `demo/palettes/browser/card/PaletteCard/ActionFeedback.vue` | 3 | `W0:96/:174/:176`, all at the **audit challenge-file path** `docs/tranches/V/megatranche/audit/components/ActionFeedback/**` — never at the source path |

**Not one of the five appears in any §4 File Bounds table.** Against that, the corpus routes
**21 / 12 / 20 / 26 / 33 = 112 row-instances** into X-W8 and X-W10 across these five files —
including X-W10.35 (small-caps), .36 (the phantom token), .37 (inverted tracking), .40 (the tone
register and the three AA failures), .41, .42 and .66.
*cureShape+locks*: **PROSE MENTION IS NOT OWNERSHIP, and a wave may not stamp a file its bounds do
not list** (W10 §3a: "any cure requiring a write outside §4's table … **invalidates the wave**").
Two of the five are named *only as G-4's CONFORMING examples* — the gate cites them as evidence and
the wave cannot touch them, which is coherent for the gate and incoherent for the eight rows that
need edits. **Either §4 widens (§4 rows 5-7) or those rows are FORMATION-BOUNDARY (§4 rows 3-4) and must be
said so here, not discovered at execution.**
*sequencing*: **a §4 act at wave-open, before any unit dispatches.** *dissent*: none — this row is
arithmetic over the wave files' own bytes and is falsifiable by re-running the grep. Recorded
against §1a n.6.
⟨W0.md:96/:174/:176 · W5.md:274 · W10.md:305-307 · ActionFeedback.md (33) · ApiOfflineChip.md (26) ·
shell-dock-dockstatuslamp.md (20) · picker-debugeventlog.md (21) · shell-dock-parseechoreadout.md
(12) — instance counts are the packet's, re-checked against each record's routing tables⟩

**X-W10.86 · THE RESIDUE THIS FOLD CANNOT HOME — what X-W10 is asked to rule and cannot reach**
(cl.5+cl.3) **MAJOR (formation input).** *Mechanism*: the corpus routes rows to X-W10 on three
distinct warrants, and only one of them is executable inside W10's §4. **(i) LAW-ONLY** (the canon
document, in bounds): X-W10.30/.31/.34/.44/.50/.55/.58/.62/.65 and the whole of §2g — executable.
**(ii) LAW-PLUS-EDIT in a file W10 owns**: `PaneHeader.vue`, `PaletteCard.vue`, `animations.css`
(delete-only carve), `foundation.css` (`.underline-tabs` carve), the two Ad-18 sites, the four
satellites — executable. **(iii) LAW-PLUS-EDIT in a file W10 does NOT own**: **22 files**, enumerated
at §4 — of which **five are in NO wave's bounds at all** (`useHeaderCondense.ts`,
`o11-header-gates.spec.ts`, `DebugEventLog.vue`, `ParseEchoReadout.vue`, `ActionButton.vue`) and
**three more of the nine pane-chrome hosts are equally unowned** (`GeneratePane.vue`,
`ExtractPane.vue`, `ConfigSliderPane.vue`). Independently, three sibling records make the same
observation from their own side and call it their **headline**: wb-extract-controls ("**ELEVEN**
pin-gated edits to a file no X wave owns … this compound fact is this apotheosis's headline for the
next formation boundary"), wb-extract-imageeyedropper ("**no wave in tranche X owns a single line of
the code that must change** — that, not any row, is this apotheosis's headline"), and
wb-gradient-visualizer (the NO-WAVE-OWNER set "dominated by two files and one composable that no
wave in tranche X owns").
*cureShape+locks*: **this row is INPUT to the formation, not a cure.** Three admissible responses,
and the fold names them so the executor picks rather than improvises: **(a)** widen W10's §4 per §4
(the fold's recommendation for rows 1-2, 5-8, 10, 16-17); **(b)** route the edit to the wave that
owns the file and keep only the law here (rows 11-14, 18-19); **(c)** declare FORMATION-BOUNDARY and
book it (rows 3-4, 9, 15). **What is NOT admissible is silence** — W10 §3a makes an out-of-bounds write
wave-invalidating, so an unnamed row is a wave-invalidation waiting to happen.
*sequencing*: **before wave-open.** *dissent*: three records book their unowned clusters as
NO-WAVE-OWNER and expect the *next formation* to take them; this fold does not overrule that — it
records that **eight of those files carry rows X-W10 has been asked to rule**, which makes them this
wave's sequencing problem even when they are the next formation's ownership problem.
⟨wb-extract-controls.md §closing · wb-extract-imageeyedropper.md §closing ·
wb-gradient-visualizer.md §closing · AdminListItem.md D-5/L-3 · PaneHeader.md PH-4/PH-15 ·
picker-debugeventlog.md §closing · this seat's grep over `waves/W0..W11.md`⟩

---

## 3. Gates

**L-19 posture, restated for this section.** A born-RED gate names a **real witness** — a file, a
computed style in a real engine, a committed frame, a census over shipping bytes. **Proof scripts
are presumed contrivance** and none is proposed here; W10 §7 already forbids a proof-farm script for
any gate, and G-4/G-5/G-6 run inside the harness X-W1 establishes (CC-030). Every RED baseline below
is either pasted from `W10.md`'s own §6 (marked ⟨W10⟩) or measured by a source record and re-cited
with its anchor. **Nothing here re-stages or re-weights W10's nine gates**; §3a sharpens their
predicates, §3b proposes new ones.

### 3a. The nine EXISTING W10 gates — what these rows sharpen

| gate | what W10 measures today | what the fold sharpens | rows |
|---|---|---|---|
| **G-1** THREE-LAWS-STATED | each of GOLDEN GLASS / BREATH OF LIFE / MOVEMENT OF MOMENTUM defined once, normatively, with provenance. RED ⟨W10⟩: 0/4 on all three terms | **the matrix and the ladder are now part of "stated"**: the closed VC §4 type matrix (X-W10.30) and the material/register ladder (X-W10.24/.26) are laws the corpus already litigates. A canon that states three names and leaves the matrix unstated leaves 20+ rows uncitable. **Add: each law names its FALSIFIER**, per X-W10.79 — a law with no live referent is folklore with a filename (G-2's own test, turned inward) | .24 .26 .30 .74 .79 |
| **G-2** ONE-DESIGN-DOC | `demo/DESIGN.md` cited by ≥1 registry file and ≥1 demo source comment; no other doc claims design authority. RED ⟨W10⟩: 0 in all 12 cross-citation cells | **`demo/DESIGN.md` is itself a defendant**: X-W10.62(f) proves it **NORMATIVELY mandates a RETIRED mechanism and PROHIBITS the SHIPPED one**. "One design doc" is not achieved by tombstoning rivals while the survivor contradicts the tree in seven places. **Add: the survivor's normative clauses are checked against the components they govern** | .58 .59 .62 .65 |
| **G-3** CANON-ANCHORS-LIVE | every file/section anchor in `demo/DESIGN.md` resolves at HEAD. RED ⟨W10⟩: 5 anchors into `demo/@/`; `find demo -name style.css` = 0 | **extended by X-W10.65's eleven enumerated dead citations**, two of which are ENOENT with line numbers (`PaletteDialog.vue`; `w6-lane-d-record.md`) and one of which offers `.pane-scroll-fade` as an exemplar of the rule it violates. **Add the Z-tier falsifications**: `--z-debug` undefined, `--z-ornament` demo-local, `--z-popover: 70` vs the served 130 | .58 .59 .65 |
| **G-4** PRM-NO-PREFERENCE | computed transform of `.pane-header-title` after 200px scroll equals pre-scroll, both engines. RED ⟨W10⟩: reduce and no-preference identical to the last digit in both engines | **THREE sharpenings.** (i) **`W10:309-310`'s per-engine post-cure pin (0.311 WebKit / 0.618 Chromium) is STRUCK** — 0.311 IS the BLOCKER (X-W10.1); a gate that pins it certifies the defect. (ii) **the bare `no-preference` wrapper is KILLED (K-4)** — it parks the reduce cohort at the veil's weakest state, strictly worse on every leak metric; G-4 must not accept it. (iii) the gate may not be stamped by `o11-header-gates.spec.ts`, which **compares REST states only** (X-W10.6) | .1 .2 .3 .5 .6 |
| **G-5** CONDENSE-SPACING-REGISTER | block-size AND title→content gap each strictly smaller at `scrollTop ≥ 120`; DELTA pair vs OM-18. RED ⟨W10⟩: both deltas **0**; keyframes declare `opacity`+`transform` only | **(i) TRAVEL GUARD (X-W10.5)**: on a short pane `scrollTop ≥ 120` is **UNREACHABLE** — the gate must guard travel or it inherits C-8(ii)'s viewport luck. **(ii) OCCLUSION QUANTITY (X-W10.8/AB-7)**: G-5's re-derivation must STATE it — either the veil endpoint occludes or the band condenses. **(iii) C-4's `grid-template-rows` scrub cure is KILLED (K-6)** and requires disarming O-11 gate 4 — the one gate that measures the component | .4 .5 .8 .14 .16 |
| **G-6** SHADOW-GEOMETRY + ELEVATION | canon states what a shadow claims; rendered corners carry no facet; pixel-diff vs OM-2 (+OM-11/12). RED ⟨W10⟩: `grep -ic elevation` = 0/0/0/0; three zero-blur layers | **three witnesses W10 does not record** (X-W10.22): D4-03 (well and card ride **different ladders**, −2/−3/−4 vs −3/−5/−7 — "equal shadow, measured" is FALSE), XP-26 (the cast is **direction-blind**: `8px 8px 0 0` is physical, RTL mirrors layout and not shadow), and the pane-inherits-by-choosing-`Card` fact. **Add: the elevation semantics are authored at `.c` BEFORE `.e` applies them** | .22 .23 |
| **G-7** PRODUCER-CONTRACT-CITATION | every `38.1966011`/`61.8033989` in live canon cites the named producer contract. RED ⟨W10⟩: 9 restatement lines (OBC 7 · VC 2), 0 citations | **X-W10.21 is a precondition**: **X-W5 scope item 5 marks those exact numbers FABRICATED TOLERANCE to be replaced**, and two canon rows carrying them cite `InstrumentChassis` — **a component CC-108/A-17 killed at the producer** (W6 H1 is a subtraction gate whose falsifier is any new chassis-shaped housing; demo imports = 0). **Citing a fabrication is not a cure.** OBC:42's ORDER and :76's boundary inventory survive as authored intent; **the numbers do not** | .21 |
| **G-8** DR-13 BUNDLE TERMINAL | container gone; one terminal verb per survivor with a recorded event. RED ⟨W10⟩: `<SelectTrigger` 13 (12 hand-set `h-9`) · `Ad-18` markers 2 · `variant="mono"` 0 · `.underline-tabs` shipping at `foundation.css:554` | **the `variant`/`emphasis` fleet fact belongs to this gate's denominator** (X-W10.52): **103 `variant="` vs 2 `emphasis="` repo-wide**, and `Button`'s prop union in Glass 7 has **no `variant`** — so a bundle survivor disposed as "landed-as-local" may be sitting on an **inert DOM attribute**. **Add: a survivor whose prop does not exist in the installed producer is DELETE, not local** | .51 .52 |
| **G-9** SATELLITE FOLD | three satellites tombstoned; no outside doc carries unsourced normative design language. RED ⟨W10⟩: 4 authorities + 2 satellites, **0** tombstones | **the tombstone is not the end of the obligation**: `VISUAL-CONSTITUTION.md:47` binds a **dead component** (X-W10.21) and `:91`/P051 name Spectrum by exception (X-W10.23) — folding a satellite that carries a dead binding **imports the defect into the survivor**. **Add: every normative clause moved must be re-verified against HEAD at move time**, per W10 §11 guardrail 3 (the 3.4× undercount) | .21 .23 .34 .58 |

### 3b. Born-RED candidates — G-10..G-27

*Format: gate · predicate · RED witness (real, cited) · falsifier · owning row(s). Every one of the
eighteen is measurable with a file read, a census over shipping bytes, or a computed-style assertion
in a running engine — none requires a script authored for the purpose.*

| # | predicate | RED witness at HEAD | falsifier | rows |
|---|---|---|---|---|
| **G-10** SHRINK-RATIO-CROSS-ENGINE | `--pane-title-shrink-ratio` computes **0.618 ± 0.0005 in BOTH engines** after a 200px scroll | ⟨W10⟩'s own G-4 paste: WebKit `0.310808`, Chromium `0.618029`; the pole family re-derives 1024→10.868, 1280→−7.3909, phone→1.619775 | re-introducing `tan(atan2(…))` turns it red in WebKit only — **per-engine divergence in SIGN or ANSWER is failure, not tolerance** (W10 §3a already halts on sign divergence) | .1 |
| **G-11** CONDENSE-RECLAIMS-SCROLLPORT | at the condensed state the description node is **absent from the a11y tree** AND header block-size delta **> 0** | `PaneHeader.vue:200-222` — three keyframes, `opacity`+`transform` only; `grep "padding\|gap\|margin"` = 3 hits, **none inside a condense keyframe**; the `opacity: 0` description keeps box, hit target and tree entry | a cure that adds `aria-hidden` without moving the box leaves the delta at 0 → red; a cure that moves the box without the tree arm leaves a zero-pixel focus trap → red | .4 .16 |
| **G-12** FOCUS-NOT-OBSCURED | every `.pane-scroll-fade` host declares `scroll-padding-block-start` ≥ the sticky band's height | `grep scroll-padding-block-start` = **0 rows**; `grep scroll-margin` = **0 demo-wide**; the band is 69-137px and occludes anything scrolled into view, **including keyboard-focused controls** (WCAG 2.4.11 AA) | deleting the declaration from any host turns it red. **NO EXISTING X GATE COVERS THIS** — the corpus's only true gate gap | .10 |
| **G-13** PANE-SHELL-ONE-STRING | the nine `.pane-scroll-fade` hosts resolve to **ONE** authority; distinct class-string count = 1 | **5 distinct strings** at HEAD (§1a n.1, this seat's re-run) where three records filed three or "4/4/divergent" — `BrowsePane:2`/`AdminPane:2`/`PalettesPane:2` · `AboutPane:4` (+`about-card`) · `GradientPane:20`/`GeneratePane:31`/`ExtractPane:5` (−`mx-auto`) · `MixPane:62` (+`relative`) · `ConfigSliderPane:106` (inner `div`, `scrollbar-thin flex-1`, no `w-full`/`h-full`) | a tenth host hand-copying the string turns it red. **Guard**: ConfigSliderPane's inner placement is **structurally FORCED** (its Card is `overflow-hidden`) — a gate that demands the Card root ×9 is a wrong gate (K-9) | .18 .19 .20 |
| **G-14** RATIONALE-COMMENTS-TRUE | the five named load-bearing rationale comments in `PaneHeader.vue` are corrected or deleted | five measurably false at `:43-45` (root contract, never true of ConfigSliderPane) · `:50-53` (contain-isolates-timeline, false by spec) · `:101-104` (`text-heading` "hardcodes 700", false against the installed producer) · `:136-138` ("degenerates to exactly 1", refuted by the pole arithmetic) · `:148-151` ("needs no PRM gate", refuted by MQ-1); plus `useHeaderCondense.ts:9-15` and two dangling `<ScrollCardHeader>` references | a comment asserting a producer fact that `grep` over `node_modules/@mkbabb/glass-ui/dist` refutes → red. **The gate exists because two axes quoted these comments AS AUTHORITY** | .2 .12 .13 |
| **G-15** TYPE-MATRIX-CLOSED | `font-display` on control surfaces = **0**; no element carries two size rungs | **17 sites / 8 files** (DVS-7's corrected census; the D axis said 13) · a **seven-site pasted family** `variant="outline" size="sm" class="font-display` · **3 of 4** `DropdownMenuContent` sites · the Badge chimera (`text-[length:var(--control-text)]` + `text-mono-caption`, which **tailwind-merge cannot collapse**) | **FAMILY-SCOPED (US-10's clause, binding)**: charging any single file **moves it away from its siblings** and turns the gate red for the family. PS-35 and ABN-26 are **carve-outs, not violations** and must be excluded by name or the gate is false | .30 .32 .33 .74 .84 |
| **G-16** CASCADE-LAYER-PAINT | every cure in §2d/§2e is verified by a **computed-style paint measurement**, never a class-list grep | five independent dead-utility measurements: `font-display` dead at 3 MobileMenuDropdown sites · `text-small`'s SIZE leg dead on all 7 rows · `.hljs { background: transparent }` (unlayered) defeating the glass plate in `@layer components` so **GradientCodeEditor's declared plate paints NOTHING** · `.p-0` losing to `px-(--overlay-pad-inline)` **by source order at byte offsets 11161 < 11323/11679** (`padding 20.35px 16px`, `p0Applied: true`) · a Tailwind utility defeating the producer's own light-paper contrast remedy | **A-3's lock, verbatim: any green must be a PAINT MEASUREMENT, never a class-list grep.** A cure authored into `@layer utilities` **re-loses** — a gate that greps the class list would stamp it green | .31 .43 .66 |
| **G-17** SMALL-CAPS-DEAD | `font-variant: small-caps` occurrences in demo = **0** | the shipped `fira-code-latin.woff2` GSUB carries `['calt','dnom','frac','locl','numr']` — **no `smcp`, no `c2sc`** (a seat's fontTools run); the metric fallbacks carry none either; at PaletteCardMenu `text-transform: uppercase` consumes the lowercase glyphs **before** synthesis can fire (`:37-39`, `:57-59`), so **three comment blocks name a register that never painted**; eyes-on: `band-1440-light.png` reads `● DEV MISCONFIGURED — RUN ˋNPM RUN DEVˋ` | re-introducing the declaration turns it red. **The register SPELLS uppercase (`text-transform`)** — the producer's own recipe; a cure that keeps `small-caps` "for capable fonts" fails, because the subset shipped cannot be one | .35 |
| **G-18** PHANTOM-TOKEN-ZERO | `--type-mono-caption` consumers in demo = **0** | the token is declared **nowhere** (demo **and** producer dist); consumers are **3 demo sites + the producer's own minified `.timeline-popover-state` rule**, which supplies a **THIRD divergent fallback** (`.75rem`) — so the chip's recipe is a **hand-copy of a producer defect** | the cure is `var(--type-micro)` bare / `text-mono-micro` — **size-preserving, on-ladder, ZERO pixel delta** (the lamp's `0.6875rem` fallback is byte-identical to `--type-micro`). **`text-mono-caption` as the cure turns G-17 red** — it uppercases and resizes | .36 .37 |
| **G-19** RING-TOKEN-MINTED | `--color-ring` resolves non-empty at `:root`, **or** `ring-`-family occurrences in demo = 0 | **no `--color-ring`/`--ring` declaration exists in demo or producer dist**, while **25 occurrences / 12 files** consume it (two seats' censuses agreeing); the repo's own tombstone documents the death (`focus-ring.css:9-15`, U-F25: "resolved EMPTY — an un-clobbered ring painted nothing"); **exactly ONE file** consumes the certified `--focus-ring-inner`/`--focus-ring-outer` recipe | **a per-component recompose leaves 24 sites broken** (A-10's lock, verbatim) → red. K7's correction is ratified: `ring-2` is a WIDTH utility and is **not** counted as a colour consumer | .46 .47 .48 |
| **G-20** INK-RUNG-FLOATING | dock chrome consumes `useSafeAccentFn("floating", GRAPHICS_CONTRAST_FLOOR)`; violators = 0 | **3 violators** (`Dock.vue:35` → ActionBarToggle, `ActionBarLayer:24`, `ColorInput:157`) against **2 OBEYING siblings in the same directory** (`MobileMenuDropdown:23`, `ProfileSection:28-29`); `SAFE_ACCENT_KEY` carries **RESTING**-certified ink (`useContrastSafeColor.ts:302-306`) against the codebase's own contract at `:337-338` ("'floating' for menu/dock chrome"); `GRAPHICS_CONTRAST_FLOOR` (3) is **never used by any ring** | **ONE call fixes all three violators.** The row is stated as a rung cure **so the "delete the prop" ruling at X.W4.d does not erase the lesson** — the rung cure must land FIRST or the gate has nothing to measure | .48 .49 |
| **G-21** GOLD-PARKS-LEGIBLE | under `prefers-reduced-motion: reduce`, `.gold-shimmer`'s **rest** stop meets its stated floor, both schemes | `--color-gold: #D4AF37` has sRGB relative luminance **0.4494** → bounded by the pure-white ceiling **2.10:1**, below both the 4.5 text floor and the 3:1 non-text floor; measured glyph **~1.9-2.2:1**, sweep best stop **~3.6-4.1:1**, under **BOTH** token families; under `reduce` the sweep does not exist so `background-position` **rests at `0% 0`** — the near-white `--color-gold-light` (#F5E6A3, pinned scheme-invariant at `foundation.css:104-105` while `--color-gold-dark` stays producer-owned: a **chimeric ramp**) sits mid-label over light `bg-well`, **permanently, for exactly the users who asked for less motion** | **D-5's cure INVERTS the failure** (`--color-gold-light` ≈ 0.75 luminance is lighter still) → adopting it turns the gate redder. `D-design.md:1028-1029`'s "genuinely right" listing is **STRUCK**. Population is **not admin-only**: any anonymous visitor deep-linking `/#/atmosphere` | .50 |
| **G-22** REGISTER-RULED-BEFORE-G-9 | X-W8's G-9 null-DELTA baseline is **re-stamped in the same commit** as any quiet-register adoption | **three standing authorities prescribe incompatible cures for `UserSortMenu.vue:8`**: X-W8.b/G-9 *delete* under a committed null-DELTA pair proving ZERO pixel change (`W8:317-319`: "the only legal delta is the attribute's absence from the DOM") · `DEFECT-LEDGER:23784` `emphasis="quiet"` (*changes* paint) · V·MEGA-W-GEN-1 G5 (~48 non-pinned sites) | **(b)/(c) satisfied FAIL (a)'s gate; (a) satisfied preserves the possibly-wrong register forever.** Tiebreak is the formation's own law — `W7.md:170-173`: "the canonical design and motion law is X-W10" → **X-W10 RULES the register.** A green G-9 is **NEVER** citable as the cure of D-5's design limb (PaginationBar DISSENT #6) | .51 .52 |
| **G-23** PRM-DELETION-ORDER | `animations.css:184`/`:202` are **not deleted** while any unconditional `animate-pulse` / `animation-delay` consumer survives | the global guard **never touches `animation-delay`**; ShadowPalette's safety additionally rests on Tailwind's pulse shipping **no fill-mode and no 0% stop**, while `ShadowPalette.vue`'s `animate-pulse` is **unconditional at `:55/:62/:66/:75`** — **after X-W10 lands, the ghost pulses under reduced motion**. `o9`'s PRM leg asserts `duration < 0.1` (`:117-119`) and goes RED at exactly that moment | **BINDING: X-W7 (SP-31's cure) STRICTLY PRECEDES X-W10.d's deletion.** A gate that fires cross-wave is a **sequencing constraint, not a cure** — this gate makes it checkable rather than remembered | .54 |
| **G-24** CANON-CITES-SECTIONS | no citation in `demo/DESIGN.md` is a line number of a deletable file; all eleven enumerated dead citations resolve | eleven, each measured by its own seat — `:58` `.section-subtitle` (**0 `.vue` consumers**) · `:67-78` the card-lock law (mandates the RETIRED mechanism, prohibits the SHIPPED one) · `:237/:247` `btn-interactive` (**0 matches in glass-ui 7**) · `:247` **0.16s** vs the producer's **0.12s** · `:250` producer-gated EXPECTED-RED · `:267` cites `:266` for a blink living at `:179` · `:273` **3 of 4 keyframe families deleted at `c84504d3`** · `:336/:342` gold prohibition violated by the surface it governs · `:375` certifies PaneHeader "reduced-motion-correct" via **two DEAD paths** · `:386` `PaletteDialog.vue` **with a line number; no such file exists** · `:388` offers `.pane-scroll-fade` as an exemplar of the rule it violates | **W10's own §5.c rule, here given its RED evidence.** `.f`'s satellite pass must not re-mint them; the bespoke-keep row citing `ActionButton.vue:117,120,121` (live literals **`:122/:125/:126`**) and `PreviewRamp`'s ENOENT `index.ts:13` are the same failure | .58 .59 .65 |
| **G-25** PLATE-INK-ONE-HOME | `.plate-ink {` declaration count = **1** | **5 files**, each `color: var(--ink-muted, var(--muted-foreground))` with the same "certified de-emphasis rung" comment; a sixth consumer (`ImageEyedropper.vue:35`) still rides the **retired** `text-muted-foreground` both siblings replaced | a sixth declaration turns it red. **Sequencing guard**: the `@utility` home lands before the five deletions or the ink goes dark for one commit | .67 |
| **G-26** ONE-INK-VOICE-PER-PRIMITIVE | hardcoded `stroke=` on `DockControl`-seated glyphs = **0**; hover grammars per dock run = **1** | **three hover grammars in one ~184px run** (producer glass seat · bespoke `scale(1.2)`+stroke latch at `ActionButton.vue:117-120` · a bespoke accent stroke stacked on the seat); `stroke` hardcoded at `:138` severs `color`-transition, hover ink AND pressed ink; the accent register binds the **24px glyph inside the 40px seat**, leaving an unlit annulus; `gap-0` at `ActionBarLayer.vue:100` zeroes a 6px producer rhythm; `--dock-control-glyph-size` **declared 5×, consumed 0** | must be **executed WITH AB-8's `active` prop** — a register restored with no state to show is green and meaningless. Must land **after** X-W4.d seats the actions on `DockControl`, or the ink is restored onto a plain `<button>` | .68 .69 |
| **G-27** RADIUS-DERIVATION-LAW | effective radii on the easing/specimen surfaces derive from **declared role tokens**; the effective-radius count equals the declared-token count | **4 declared radius tokens → 6 effective radii**, non-concentric at four sites, the strip declaring **ZERO** radii of its own; one `rounded-card` spanning **40.94px and 196.36px** boxes = a **~4.8× curvature swing flipping on click**; fleet: `rounded-md` ×14, **non-role rungs 74 vs role tokens 27 (≈2.7:1)** | adopting `--radius-strip` **without** the derivation law renames the drift and leaves the count at 6 → red. The fleet ratio is **wave-sizing input for X-W6.d/X-W10**, never a scope change at a component | .72 .74 .75 |

**Gate arithmetic: 9 sharpened + 18 born-RED = 27 gate rows.** Of the 18, **G-12 is the only one
covering a surface no X gate touches at all** (X-W10.10's own dissent). G-22 and G-23 are
**sequencing gates** — they measure ORDER, not state, and are the two gates that can be failed by a
correct cure landing in the wrong week.

---

## 4. BoundsDelta

**The warrant.** `W10.md §3a`: *"Any cure requiring a write outside §4's table … invalidates the
wave."* Naming the delta is therefore this fold's job, not the executor's discovery. Column
**verb**: **ADD** = the fold recommends widening W10 §4 · **WIDEN** = the file is in §4 but its carve
is too narrow · **ROUTE** = keep the law here, send the edit to the owning wave · **BOUNDARY** = no
wave owns it; book it, do not smuggle it · **NOT-ADDED** = recorded so it is not rediscovered.

| # | file(s) | verb | why, per the records | rows |
|---|---|---|---|---|
| 1 | `demo/picker/composables/useHeaderCondense.ts` | **ADD** (modify) | **zero hits across all twelve `waves/W*.md`** (this seat's grep). Carries the false docblock (`:9-15`) two axes quoted **as authority**, the phantom `<ScrollCardHeader>` at `:11`, and the owner-retired `proof:*` idiom. Its sole consumer `demo/picker/ColorPicker.vue` is **X-W6's** (§5 edge 3) | .2 .12 .14(G) |
| 2 | `e2e/smoke/oracles/o11-header-gates.spec.ts` | **ADD-OR-ROUTE** | **zero hits across all twelve wave files**, and W10 §4's Do-NOT-touch names **`e2e/**`** — a direct collision. Gate 5 compares REST states only and never calls the `scrubTo` in the same file (`:278-300`), so **it returns the same verdict for the PRM defect and its cure**; gate 3's `at64.opacity === 1` (`:154`) has no travel guard and the sole running project pins **1280×720**; the subtree is structurally unreachable from any WebKit project (`smoke-safari` testDir = disjoint `./e2e/smoke/safari`). **Recommendation: ROUTE to X-W1's oracle slate (CC-031)** and record that **G-4/G-5 may not be stamped by the unreformed oracle** | .6 |
| 3 | `demo/picker/visual/DebugEventLog.vue` | **BOUNDARY** | **zero hits across all twelve wave files**; **21 row-instances** routed (X-W8 per CC-081, with ADJ-9(c)'s token half to X-W10). The record books **no NO-WAVE-OWNER rows** — its own routing is unexecutable as written | .82 .85 |
| 4 | `demo/shell/dock/ParseEchoReadout.vue` | **BOUNDARY** | **zero hits across all twelve wave files**; **12 row-instances**; A-8 is a witness in X-W10.65's citation census | .65 .85 |
| 5 | `demo/shell/dock/DockStatusLamp.vue` | **ADD** (modify) | in **NO §4 table**. Two wave-file hits, both non-owning: `W10:305-307` names it as a **G-4 CONFORMING example** and `W5:274` counts it in the C7 viewport-query census. **20 row-instances**, incl. DSL-4 (small-caps that never rendered), DSL-10 (the phantom token), DSL-14/-18/-21/-29 | .35 .36 .37 .41 .61 .66 .85 |
| 6 | `demo/palettes/browser/status/ApiOfflineChip.vue` | **ADD** (modify) | in **NO §4 table**; one hit, `W10:307`, again as a **G-4 CONFORMING example**. **26 row-instances**: AP-14/-15/-16/-19/-20/-21/-28 — the densest single-file cluster in §2d | .35 .36 .37 .41 .42 .61 .85 |
| 7 | `demo/palettes/browser/card/PaletteCard/ActionFeedback.vue` | **ADD** (modify) | in **NO §4 table**. `W0:96/:174/:176` name **`ActionFeedback` only at the audit challenge-file path** `docs/tranches/V/megatranche/audit/components/ActionFeedback/**` — never the source path. **33 row-instances**, incl. **three of four tone rungs failing AA** (dark-destructive **2.78:1**) — the corpus's gravest true defect, which **appeared in no axis file at all** | .40 .66 .85 |
| 8 | `demo/styles/utils.css` | **ADD** (modify-carve) | mentioned only in `W4.md`; in **no §4 table**. Carries `.search-seated` (`:96-107`/`:132-137`, the register law's named opt-in), the voice rule and its shipped cure (`:147-154`, `font-family: inherit`), the `.fira-code` fork (`:9-11`, unlayered, **drops `font-feature-settings`**), the dead `@source "../../color-picker/**"`, and the false consumer-prose at `:36-43`/`:57-61` | .26 .66 .81 |
| 9 | `demo/palettes/browser/admin/AdminListItem.vue` | **BOUNDARY** | **verified across W0-W11 by the source seat and re-verified here: no tranche-X File Bounds contains it.** Carries the anatomy's only written typography statement — which is **wrong** — plus the separator-grant violation on five routes. The consolidation **altitude is an owner call** | .84 |
| 10 | `demo/styles/foundation.css` | **WIDEN** the carve | in §4 today as `modify-carve` — **`.underline-tabs:554` survivor ONLY**. Rows need: the forced-colors roster `:678-694` and the print block `:815-840` (X-W10.80) · the `prefers-contrast` block `:726-750` and `:767` (X-W10.8/.41) · `--color-ring`'s mint (X-W10.46) · `--type-mono-caption`'s declaration beside `--type-micro` (X-W10.36) · `.console-well:340-356` (X-W10.25) · `:376-384`'s P10 comment on a now-false premise (X-W10.45) · `:104-105` the chimeric gold ramp (X-W10.50) · `:118`/`:445` `--menu-min-w` (X-W10.38) · `:143`→`:328` `--well-bg` (X-W10.24) · `:585-587` `.slug-pill` (X-W10.84) · the `.plate-ink` `@utility` home (X-W10.67). **EC-8 is right about the bytes; the fix is widening, never re-routing** | .8 .24 .25 .36 .38 .41 .45 .46 .50 .67 .80 .84 |
| 11 | `demo/palettes/BrowsePane.vue` · `demo/palettes/PalettesPane.vue` | **ROUTE** (X-W7 holds both `modify`, `W7:107-108`) | two of the nine pane-chrome hosts (class-string variant A). The **law** is X-W10's; the two edits ride X-W7. `PalettesPane.vue` is additionally X-W4's (`W4:120`) | .18 .19 .20 |
| 12 | `demo/palettes/admin/AdminPane.vue` | **ROUTE** (X-W7, `W7:109`) | host variant A; carries L-12's nine-hand-copies enumeration **and** the MQ-1 inheritance row **mis-routed to "W47", a V-prime label that is not an X wave** (§1a n.2 — the W47 label may not gate) | .3 .18 |
| 13 | `demo/scenes/about/AboutPane.vue` | **ROUTE** (X-W6, `W6:71`) | host variant B (`+ about-card`). **About's `mx-auto` is inert cargo** (AB-40: with `w-full`, auto margins resolve 0) — the cure settles `mx-auto` **per host, not per copy** | .18 .19 |
| 14 | `demo/workbenches/gradient/GradientPane.vue` · `demo/workbenches/mix/MixPane.vue` | **ROUTE** (X-W6, `W6:62/:76`; GradientPane also X-W8 `modify-carve`, `W8:91`) | host variants C and D (`− mx-auto`; `+ relative`) | .18 .19 |
| 15 | `demo/workbenches/generate/GeneratePane.vue` · `demo/workbenches/extract/ExtractPane.vue` · `demo/scenes/ConfigSliderPane.vue` | **BOUNDARY** | **three of the nine hosts are in NO wave's §4.** GeneratePane and ExtractPane return **zero hits across all twelve wave files**; ConfigSliderPane's single hit (`W8:107`) is a **CC-105 census line, not a bounds row**. ConfigSliderPane is also the **most divergent** host (inner `div`, `scrollbar-thin flex-1`, no `w-full`/`h-full`) and its placement is **structurally FORCED** — the one host a naive cure would break | .18 .19 .77 |
| 16 | `demo/styles/animations.css` | **WIDEN** the carve | in §4 today as `modify-carve`, **delete `:184`/`:202` only**. Rows need: `:2`'s **false keyframe-family attribution** (3 of 4 families deleted at `c84504d3`; only `shimmer` is genuinely producer-supplied) · `:43-46` `.stagger-children`'s **fill-both contract suppressing every consumer's `:active`** · `:228-236`'s `--left`/`--right` geometry, where `App.vue:77` makes **`--right` unreachable on mobile by construction** · `:63-65`'s citation of a `transition-inventory.md` last written **before the entire demo restructure**. **G-23 gates the deletion's ORDER** | .54 .56 .58 .63 |
| 17 | the `.plate-ink` five — `demo/shared/ui/EmptyState.vue` · `demo/color-picker/ErrorBoundary.vue` · `demo/workbenches/extract/ExtractWorkbench.vue` · `demo/workbenches/extract/ImageDropZone.vue` · `demo/workbenches/extract/ImageEyedropper/ImageEyedropper.vue` | **ADD-OR-SEQUENCE** | five identical declarations of a token-by-its-own-comments. `ExtractWorkbench.vue` is X-W7's (`W7:124`) and `ImageEyedropper.vue` is X-W4's (`W4:166`, **import + face only**) — the other three are unowned for this purpose. **The `@utility` home must land before the five deletions**; adoption is a rider on any wave touching each file | .67 |
| 18 | `demo/shell/dock/layers/ActionBarLayer.vue` · `demo/shell/dock/ActionButton.vue` | **NOT-ADDED** (recorded) | canon half only — X-W10 rules the proportion and the ink voice; the **edits** are X-W4.d's (`W4:142`) and X-W8.d's (`W8:88`). Recorded because **`ActionButton.vue` returns zero hits across all twelve wave files** while carrying the bespoke `scale(1.2)`+stroke latch (`:117-120`) and the literals G-24 finds mis-cited (live at `:122/:125/:126`) | .68 .69 |
| 19 | `demo/workbenches/gradient/GradientVisualizer/easing/EasingSpecimenStrip.vue` · `.../GradientEasingEditor.vue` | **NOT-ADDED** (recorded) | the derivation law is canon-only; the edits are X-W6.d ◆'s. Recorded because `EasingSpecimenStrip.vue` is the corpus's **named NO-WAVE-OWNER file** (under two BLOCKERs at the visualizer record) — **if X-W10 wants G-27 witnessed rather than asserted, the bounds must widen or the law ships unwitnessed** | .72 .74 .75 |

**Delta arithmetic**: **19 rows** → **22 newly-named files** (rows 1-9, 11-15, 17-18 minus the two
already-owned in row 17) + **2 carve widenings** (rows 10, 16) + **3 recorded NOT-ADDED families**
(rows 18-19 and row 8's producer half). **Eight of the newly-named files are in NO wave's bounds at
all**: `useHeaderCondense.ts`, `o11-header-gates.spec.ts`, `DebugEventLog.vue`,
`ParseEchoReadout.vue`, `AdminListItem.vue`, `GeneratePane.vue`, `ExtractPane.vue`,
`ConfigSliderPane.vue` — plus `ActionButton.vue` at row 18. That is X-W10.86's arithmetic, restated
as a table.

---

## 5. CrossEdges

*Fourteen. Each edge is **directional** and states what must be true before the arrow may be
traversed. An edge with no order clause is not an edge, it is a mention.*

| # | edge | the constraint |
|---|---|---|
| 1 | **X-W10 → X-W1** (oracle slate, CC-031/CC-030) | **G-4/G-5 may not be stamped green by the unreformed `o11-header-gates.spec.ts`** (X-W10.6): gate 5 compares REST states only and returns the same verdict for the PRM defect and its cure. The no-orphaned-cell arm is X-W1's; the motion-gate reform rides G-4/G-5 here **carrying X-W10.1's pin correction**. X-W1 also owns the **zoom gate** for X-W10.62(b) (the 24px-root capacity collapse) |
| 2 | **X-W10 → X-W1** (goldens) | the **forced-colors witness matrix** is X-W1's (EC-8's own split); the **roster mis-aim needs no browser** and is X-W10's §4-row-10 widening. X-W10 must not wait on the witness to fix the roster, and must not claim the roster fixed by the witness |
| 3 | **X-W10 → X-W6** (`ColorPicker.vue`) | `useHeaderCondense.ts`'s **sole consumer** is X-W6's file. If §4 row 1 is not added, the docblock correction and the condense-fork ruling **cannot both land in one wave** — and the fork is exactly what X-W10.2's transposition decides |
| 4 | **X-W10 ⟶ X-W6** (least-count, law-before-edit) | **the collapse the corpus ORDERED WRITTEN** (`consolerail.md:121`). The **LEAST-COUNT LAW is X-W10's**; the `:step` **EDIT is X-W6's** (its R-28) and lands in X-W6's file. **Neither wave may claim the other's half, and X-W6's edit may not ship before the law states the per-channel least count** |
| 5 | **X-W10 ⟶ X-W6** (ConsoleRail press leg) | D2-03 **splits, and neither half substitutes for the other**: the rail's press cure rides X-W6 by adopting the producer's **independent `scale` property** (`.tap-squish` / `.interactive-item`), which the stagger keyframe's `transform` fill **structurally cannot clobber**; the **class-level fill-both contract stays X-W10's law row** |
| 6 | **X-W10 → X-W6** (Katex) | canon rows first (one referent one voice · a `\text{}` policy · a measure jurisdiction for math); **the implementation echo is X-W6's**. The canon **has no row for math** — inventing one is X-W10's act, not X-W6's |
| 7 | **X-W7 ⟹ X-W10** (BINDING, the fold's most load-bearing order) | **X-W7 (SP-31's cure) STRICTLY PRECEDES X-W10.d's deletion of `animations.css:184`/`:202`.** The global guard never touches `animation-delay`; ShadowPalette's `animate-pulse` is unconditional at four sites. **After X-W10 lands, the ghost pulses under reduced motion** unless the plate's motion is `no-preference`-gated or dead first. **Gate G-23 makes this checkable rather than remembered** |
| 8 | **X-W10 ⟹ X-W7** (law-before-surface, four rows) | PC-1/PS-4's disclosure motion (the successor must be **PRM-safe by construction**) · AF-4's tone surface (**the r2 one-line cure is KILLED for light mode**, R-5 1.65:1 — the fix ships only with a legibility answer) · AAP-14's row anatomy (**the boundary law governs; the rebuild is X-W7's**) · AL-M6's register unification (**a rider no X-W7 unit claims**) · **and PCS-2/PCS-8/PCS-27, whose "under X-W10 law" citations point at a law that does not yet exist** (X-W10.81) |
| 9 | **X-W10 ⟹ X-W8** (register before deletion, G-9) | `W7.md:170-173` is the tiebreak: *"the canonical design and motion law is X-W10."* The register decision is design content → **X-W10 RULES.** X-W8's deletion may land first (mechanically safe either way); **if quiet wins, G-9's null-DELTA baseline MUST be re-stamped in the SAME commit** or the gate outlaws the cure. **A green G-9 is never citable as the cure of D-5's design limb** |
| 10 | **X-W10 → X-W8** (DebugEventLog KEEP ruling) | X-W8's subtraction is **sequenced with the sibling's ratified D-N3 scrollport cure**: the two `pointer-events: none` declarations at `:61-67` are **the designated landing mechanism**, and deleting them as dead code **re-opens the touch-stealing hole the file exists to prevent** |
| 11 | **X-W10 ⟹ X-W4** (rung before prop deletion) | **the `useSafeAccentFn("floating")` rung cure precedes X.W4.d's contract collapse** (D-15 dies with it) **or the lesson is lost** — one call fixes all three violators. Also: AB-30's **`DockControl` seating** is X-W4.d's explicit cure clause and must land **before** G-26's ink register can be measured; MSS-5's register decision is X-W4's while its **token law is X-W10's, per the ADJ-m3 precedent** |
| 12 | **X-W5 ⟹ X-W10** (G-7 may not cite a fabrication) | **X-W5 scope item 5 marks `61.8033989`/`38.1966011` as FABRICATED TOLERANCE to be replaced.** G-7 replaces bare constants with a citation to the named producer contract — and two of those occurrences are **cited to a component CC-108/A-17 killed at the producer**. **Citing a fabrication is not a cure**; X-W5's replacement must exist first, or G-7 goes green on a lie |
| 13 | **X-W10 → X-W5** (ghost-flip geometry) | the ghost's exit fade is **dead code** (`transition-opacity` alone while `visibility`/`position`/`content-visibility` flip on frame 1 — it **fades in and never out**). **The geometry is X-W5's; the motion tokens and the motion policy are X-W10's** — a token applied to a flip that cannot animate is a green gate over a dead affordance |
| 14 | **X-W10 → GLASS-UI BH RELAY** (the standing fond) | seven relay lines, one letter, no local patches of producer bytes: **(a)** the producer's **partial layering** (`_shared/menu.css` wraps `.glass-menu-row` then leaves `.dropdown-menu-content` bare) — *is it intentional?* **(b)** `--type-mono-caption` declared nowhere yet consumed by the producer's own minified `.timeline-popover-state` — declare beside `--type-micro`/`--type-caption`, or retire it producer-side too. **(c)** the shipped **Fira Code subset carries no `smcp`/`c2sc`**, so any producer surface spelling small-caps has the same disease. **(d)** `--dock-control-glyph-size` **declared 5×, consumed by zero rules**. **(e)** the `variant="destructive"` seam + the unlayered `.dropdown-menu__item { color: inherit }` layer-discipline defect. **(f)** the `surface="well"` door for EAS-4/EAS-5 (a non-interactive well retaining an interactive glass response). **(g)** `CardHeader shrink?` + `card-scroll.css` adoption — **and the rider that the producer's `::before` rests at `opacity: 0`, which the component's own T.W3-4 ruling SUPERSEDED (rest floor 0.52)**; an adoption that omits the floor **silently reverts an owner-ruled design** |

**Edge arithmetic: 14.** Three are **BINDING orders** that a correct cure can violate by landing in
the wrong week (edges 4, 7, 12), two are **law-before-surface** blocks covering nine rows (edges 8,
9), and one is the standing relay (edge 14) that carries seven producer questions and **zero**
producer patches.

---

*End of round-0 fold. 86 rows · 27 gate rows · 19 bounds-delta rows · 14 cross-edges. E-3 honoured:
no byte of `waves/W0..W11.md` or `CONFORMANCE-2026-08-03.md` was written by this seat.*
**⟶ §6 below is the ROUND-1 addendum (2026-08-28). File totals now: 103 rows · 36 gate rows ·
27 bounds-delta rows · 17 cross-edges — see §6h.**

---

## 6. ROUND-1 FOLD ADDENDUM — 2026-08-28 · the ROW-CARRIAGE repair

**Status: `planned`. Dispositions ADJUDICATED, never stronger. No product source was opened by this
seat — every witness below is a source record's own measurement, re-cited with its anchor (the §3
L-19 posture, unchanged), or this seat's grep over the twelve `waves/W*.md` and the fold layer.**

**Warrant.** `refinement/ROUND-1-ASSIGNMENTS.md` (spine ROW-CARRIAGE LAW §1): *a packet's home wave
adopts the packet's RIDING ROWS id-for-id, not merely its files.* This section is the X-W10 repair
seat's consumption of that work order. **§2–§5 above are byte-untouched** — they are the round-0
record the pass-1 check verified (gapless X-W10.1–86, gate Σ 27, no VERIFIED stamps). Everything
this round adds lands here, numbered contiguously from **X-W10.87**, and §6h restates the file's
arithmetic at its new true counts.

**Pass-1 status for this fold: NO DEFECT NAMES IT.** `PASS-1-CHECK-RETURN.json` names
`X-W10-FOLD.md` exactly once outside its own `/folds[10]` synopsis — in defect 3's receipt, where
`X-W10-FOLD.md:1253` is cited as *evidence* that `B-13` exists in the layer only as a DECISIONS.md
citation (Generate's three verbs), **an unrelated identity**. No cure is owed by this seat to the
register; the whole of this section is row-carriage.

### 6.0 The fresh id-keyed census — method and result

Re-run from scratch, not inherited. **Corpus** = the 92 non-`kf-*`/`fr-*`/`pt-*` `*.md` at
`registry/adjudicated/` (non-recursive) **plus** `delta-r3/` (2 files, re-checked: **zero** X-W10
tokens — the §1 provenance claim holds). **Routing token** = `X` + any of hyphen / non-breaking
hyphen / figure dash / **en dash** / em dash / **middle dot** / **full stop** + `W10`, word-bounded
so `X-W1` can never prefix-match `X-W10`. **Row** = a bold-leading id on a `-`/`*`/`|` line; a
supplementary sweep re-ran the same grep for **non-hyphenated** leading bold ids (the `M4`/`m3`/`K10`
dialects) and returned **5 extras, all five already folded here** — recorded because that dialect is
the obvious way to under-count. **Presence test** = boundary match against the concatenated fold
layer (12 `X-W*-FOLD.md` + the spine) after expanding slash-compressed (`EY-7/27/32`) and range
(`AAP-1..AAP-3`) forms.

| measure | count |
|---|---|
| corpus rows touching X-W10 (row line or continuation block) | **319** |
| of those, row-line-ROUTED (token + routing verb on the row line) | **213** |
| routed rows absent from the WHOLE fold layer by bytes | **16** |
| of those 16, X-W10-PRIMARY (this seat's) | **7** — exactly the assignment's §B roster, id-for-id |
| of those 16, sibling-wave-primary with an X-W10 **law** limb | **9** (X-W7's roster this round) |
| §A NO-WAVE-OWNER identities assigned here | **4** (PaneHeader; PH-9/PH-34 overlap §B — folded once, both limbs) |
| §C I-28 EXTERNAL rows assigned here | **0** |
| **ceiling finds — routed here, absent under the citation law, on NO roster** | **8** |
| pointer-only closures (facets of already-banked identities) | **4** |

**The ceiling was not empty, and the reason is the citation law.** Eight identities route to X-W10
and are absent from the layer **as ⟨record.md · id⟩**, while five of the eight *bare* ids do appear
in the layer inside **another record's namespace** — a naive byte census closes on them and a
citation-law census does not: ⟨AboutPane · AB-32⟩ vs the layer's ⟨shell-dock-actionbarlayer · AB-32⟩
· ⟨PaletteCardSwatches · PS-13⟩ vs ⟨PreviewStrip · PS-13⟩ · ⟨PaletteCardSwatches · PS-19⟩ vs
⟨PreviewStrip · PS-19⟩ · ⟨picker-componentsliders · R-15⟩ vs ⟨PaletteColorStrip · R-15⟩ ·
⟨shell-dock-profilesection · N-5⟩ vs the X-W3/X-W6 `N-5`s. **The roster was a floor. This is the
ceiling, and it is 8 rows tall.**

### 6a. §A — the four NO-WAVE-OWNER PaneHeader identities (packet 11 · AB-22 / PANE-CHROME)

**X-W10.87 · PH-9 — `transform: scale()` on the `<h3>` scales every slotted descendant below the
WCAG 2.5.8 target floor** (cl.1+cl.2+cl.5) **MAJOR. Both limbs, folded once** (§A NWO limb ∧ §B
census limb). *Mechanism*: the scrub scales the heading and **everything slotted into it**; About
slots a real `<button>` (`ColorSpaceSelector inline`, `aria-label` "Select color space") which drops
below **24×24** when scrubbed — and at the **1280 pole** (X-W10.1's `−7.3909`) leaves the viewport
entirely, visible in the committed WebKit shot.
*cureShape+locks*: **(i)** the disposition is **`X-W10.d` rider, verbatim**: *"the font-size-step
transposition changes but does not remove the geometry question for slotted content"* — X-W10.2's
transposition is **not** a cure for this row, it only changes which property carries the defect.
**(ii)** the structural cure — *"an adornment SLOT beside the heading, not inside it"* — **rides
PH-26/PH-27's arm and is banked NO-WAVE-OWNER** (X-W10.88). A wave that lands the transposition and
calls PH-9 discharged has cured the mechanism and left the target floor.
*sequencing*: the geometry question is decided **with** X-W10.2, not after it; the adornment slot is
formation-boundary work unless §4 addendum row 20 is taken. *dissent*: none on the mechanism; the
slot's altitude (component API vs producer `CardHeader` slot) is unruled.
⟨PaneHeader.md · PH-9 (C:C-7) — §A NWO row ∧ §B census row⟩

**X-W10.88 · PH-26 — a `<div>`-rooted `Badge` is slotted into an `<h3>`: an HTML content-model
violation that parses silently** (cl.1) **MAJOR (NWO).** *Mechanism*: `AdminPane.vue:5` slots a
`<Badge>` into the `<h3>`; the producer `Badge` root is a **`<div>`** — flow content inside
**phrasing-only** content. It **parses without auto-closing**, *"which is why nothing surfaced it"* —
no parser error, no lint, no gate. This is the second occupant of the same slot PH-9 indicts, and it
is why the adornment-slot cure is structural rather than cosmetic.
*cureShape+locks*: **disposition carried verbatim — *"rides the adornment-slot arm (PH-9) —
NO-WAVE-OWNER, candidate = the wave that lands PH-8's seam."*** X-W10 may state the slot law; the
`AdminPane.vue` edit is **X-W7's** (§4 row 12, `W7:109`). *sequencing*: the slot law precedes both
consumer edits. *dissent*: candidate-wave is PH-8's seam, not this wave — recorded, not overruled.
⟨PaneHeader.md · PH-26 (reader-2 missed #2, CONFIRMED)⟩

**X-W10.89 · PH-33 — `@reference` is a dead edge, and it is dead in 15 of 17 demo SFCs**
(cl.1+cl.5) **MINOR (NWO, systemic).** *Mechanism*: `PaneHeader.vue:61` carries `@reference` with
**zero `@apply` / `theme()` in 164 style lines**; the pattern is systemic — **17 demo SFCs carry it,
15 dead** (the record's census). *cureShape+locks*: **disposition verbatim — *"NO-WAVE-OWNER
(systemic hygiene; not charged to this component)."*** This fold **does not** mint a gate for it: a
gate whose owner is nobody is a trap, and W-HYGIENE is where the 15-of-17 census belongs. Recorded
here so the identity is carried and so the next formation inherits a census, not a rediscovery.
*sequencing*: —. *dissent*: the record refuses to charge the leaf; this fold agrees and refuses to
charge the wave.
⟨PaneHeader.md · PH-33 (C:C-13 / L:L-7)⟩

**X-W10.90 · PH-34 — `<slot />` with no fallback inside a display-rung `<h3>`: an empty heading with
a reserved line box, and four undesigned states** (cl.1) **MAJOR (NWO). Both limbs, folded once.**
*Mechanism*: empty-title / loading / error / focused **never designed**; the bare `<slot />` reserves
a **full line box** and yields an **empty heading** — the a11y-tree failure G-11 measures from the
other end (an `opacity: 0` description that keeps its tree entry) meeting its mirror image (a heading
with a box and no name). *cureShape+locks*: **disposition verbatim — *"NO-WAVE-OWNER; the empty-title
guard may ride any X-W10.d touch."*** The guard is one `v-if`/fallback on the slot; the **four-state
design** is not, and is the NWO half. *sequencing*: the guard rides X-W10.2's file touch; the state
design is formation-boundary. *dissent*: **D-7's amendment is a binding rider on X-W10.d** (the
record's own closing line) — carried into G-29's falsifier so it cannot be forgotten at execution.
⟨PaneHeader.md · PH-34 (D:D-15) — §A NWO row ∧ §B census row⟩

### 6b. §B — the census escapes routed to X-W10 (X-W10.91–.95)

**X-W10.91 · ADJ-M2 — the rename overlay escapes its well by 74px under RTL, and the one glyph that
should mirror cannot** (cl.1+cl.2) **MAJOR.** *Mechanism*: the overlay is **physically anchored** —
`transform-origin: top left`, `left: 0`, `margin-left: -0.375rem` (`:299-311`) — so under `dir=rtl`
the **overlay right edge 760.1 vs well right edge 686 = 74px escape**, with `insetInline` identical
in both directions (probe-D18-pass5, read whole). **The record corrects its own reader**: the
internal FROM→TO row **does** mirror (ghost x=705.1 > solid x=626); what fails is the literal
`&rarr;` at `:63` — **U+2192 `Bidi_Mirrored=No`** — so the arrow points backwards **against** the
mirrored row. *cureShape+locks*: **terminal disposition verbatim — *"Terminal: X-W10 (logical
properties + direction law)."*** Two distinct cures, and the fold names both so neither is mistaken
for the other: **(i)** the anchor is logical (`inset-inline-start` / `margin-inline-start` /
`transform-origin: logical`), **(ii)** the glyph is **replaced, not mirrored by CSS** — a
non-mirroring character cannot be fixed with `transform: scaleX(-1)` without inverting its own
stroke weight. **R1's "stays LTR-ordered" is the WRONG MECHANISM for the right defect and may not be
cited** as the warrant. *sequencing*: with X-W10.9 (PH-12's `transform-origin: left top`) — one
direction law, two surfaces. *dissent*: none; R1's mechanism corrected on the record.
⟨CurrentPaletteEditor.md · ADJ-M2 (R1 missed #2, promoted with one arbiter correction)⟩

**X-W10.92 · ADJ-m1 — two identical ✓ glyphs, 379.5px apart, bound to different commands, and the
louder one is the lesser command** (cl.1) **MAJOR.** *Mechanism*: during an edit the screen carries
**two identical ✓** (`:76` vs `:141`; a third instance at `Dock.vue:143`), **379.5px apart** — and
the **larger, accent-tinted, NAMED** one is the **minor** command while the **smaller, nameless** one
**commits the palette** (probe-D17-pass5 `checkGlyphs`, read whole). **PR-05 one-mark-one-meaning.**
*cureShape+locks*: the canon owns the law; the cure is a **mark change, not a size change** — making
the commit glyph louder without breaking the identity preserves the collision at higher volume. The
third instance (`Dock.vue:143`) proves the register is fleet-wide, so the law must be stated at the
canon and not settled inside `CurrentPaletteEditor.vue`. *sequencing*: law before either surface.
*dissent*: —
⟨CurrentPaletteEditor.md · ADJ-m1 (R1 missed #6, CONFIRMED)⟩

**X-W10.93 · VHD-22 — a 50%-alpha accent hover replaces an opaque well inside a glass host, and the
recipe is a hand-copy of `.interactive-item` minus everything that made it interactive** (cl.1+cl.2)
**MAJOR.** *Mechanism*: the row hover replaces the opaque well with a **50%-alpha accent** inside a
glass host whose well-law precondition (*"nothing live sits behind an in-plate fixture"*) is
**false**; and the hover recipe is a hand-copy of glass-ui's `.interactive-item` **minus its focus
ring and press state**, applied to **a row that is not interactive at all**. Two independent
verifications on the record: **`DESIGN.md:98` names VersionHistoryDrawer rows in the WELL rung while
rung 3 names drawers CHROME** — *the law's own table sanctions a deployment its rationale forbids* —
and the served `utilities/base.css` ships the full recipe (`hover` = the same accent-mix,
`:focus-visible`, `:active` scale) while `:25` copies **exactly the half that signals to pointers
only**, a false affordance whose sole real function is revealing the button **VHD-2 proves
unrevealable on touch**.
*cureShape+locks*: **disposition verbatim — *"split → X-W10 (canon: rule the well-inside-glass case
explicitly) + X.W7.d (the recomposition either makes rows real interactive items via the producer
utility or strips the affordance)."*** The canon limb is **not optional**: `DESIGN.md:98` is the
authority that sanctions the defect, so ruling the case explicitly **is** the cure of the law, and
G-2's "the survivor's normative clauses are checked against the components they govern" now has its
sharpest witness. **A half-copy of a producer utility is a false affordance, not a lighter one.**
*sequencing*: **canon first** — the recomposition's two branches (real item vs no affordance) are
*chosen by* the well-inside-glass ruling, not before it. *dissent*: **BOUNDS DEFECT, new** — the
record routes the recomposition to **X.W7.d**, and `VersionHistoryDrawer.vue` is in **X-W4's** §4
(`W4:127`, `modify`) and in **no W7 table** (this seat's grep over all twelve wave files). See §6f
row 21.
⟨VersionHistoryDrawer.md · VHD-22 (D-19 + reader-1 missed `.interactive-item` fold)⟩

**X-W10.94 · D-06 — five height declarations for one box, two symmetric axes resolved
anisotropically, and the plate's block size changes live under the finger** (cl.1+cl.2) **MAJOR.**
*Mechanism*: `h-[20dvh] min-h-24 max-h-40 lg:h-[14rem] lg:max-h-none` — **five height declarations
for one box** (`:11`) — give two symmetric **[0,1]** axes **anisotropic, viewport-dependent**
resolution, with a **64px discontinuity at `lg`**. Rider **M-3**: `dvh` is the **DYNAMIC** unit, so
the plate's block size changes **live under the finger** during the `pan-y` scroll the gate
deliberately permits (`touchAction: "pan-y"` until active, `useSpectrumPlateStyle.ts:41-43`;
`@touchmove.passive` `:20`) — **re-scaling the pointer→v map mid-gesture. No axis named the unit.**
*cureShape+locks*: **the canon hook is STRUCK per D-06-HOOK** and **the filed SS3-law-7 citation is
KILLED — it is a SPACING law** and may not be re-cited as the warrant; *"the finding stands on
measurement and structure alone."* The cure states **one height law** for a normalized-square
sampler; a cure that leaves `dvh` on a pointer-mapped plate fixes the count and keeps the gesture
bug. *sequencing*: with X-W10.23 (the same face's two prohibited shadow registers) — one surface,
one ruling. *dissent*: **literal identity guard** — ⟨picker-spectrumcanvas · **D-06**⟩ ≠ the fold
layer's `D-6` tokens, which live in other records' namespaces. Zero-padding is identity.
⟨picker-spectrumcanvas.md · D-06 (+ rider M-3)⟩

**X-W10.95 · XW-32(a) ≡ XP-19 — `≡`-POINTER, no second booking** (cl.2) **MINOR.** The roster
assigns ⟨wb-extract-workbench.md · XW-32⟩ here. Its **(a) limb is the identity this fold already
banked**: *"`.plate-ink` ×5 — IDENTITY = XP-19 → **X-W10**"* — folded at **X-W10.67**, gated at
**G-25** (`.plate-ink {` declaration count = 1; five files; the sixth consumer `ImageEyedropper.vue:35`
still on the retired `text-muted-foreground`), bounded at **§4 row 17** (ADD-OR-SEQUENCE, *"the
`@utility` home must land before the five deletions or the ink goes dark for one commit"*).
**Limbs (b)–(f) are X-W7's and are NOT adopted here** — (b) the eyebrow override ×4 (*pre-owned at
`:523-527`, framing corrected, K-22*) · (c) `--btn-hover-color` ×3 · (d) the dead scoped
`.touch-gate-target` (C-33) · (e) EmptyState's caption re-implemented as a bare `<p>` (L-12) ·
(f) the two `DockSeparator`s against VC §4.2 / OPTICAL-BENCH:76's `none`, **carried with the readers'
honest caveat that a Dock-internal group separator is arguably Dock grammar — the ambiguity itself
needs the cure wave's ruling**. This row exists so the byte census closes on **XW-32** without
minting a second name for **XP-19** (anti-rename), and so no reader mistakes (a) for the whole row.
*sequencing*: none of its own — it inherits X-W10.67's. *dissent*: —
⟨wb-extract-workbench.md · XW-32(a) ≡ wb-extract-pane.md · XP-19 — banked at X-W10.67 / G-25⟩

### 6c. The ceiling — routed here, on no roster, absent under the citation law (X-W10.96–.103)

**X-W10.96 · AB-32 — the demo maintains a private φ ladder that shadows the producer's shipped one,
and About sits one rung below the ratified interval** (cl.1+cl.4) **MINOR (canon, high leverage).**
*Mechanism*: `foundation.css:458-462` declares a **private `--phi-0..4` ladder** shadowing the
producer's shipped **`--space-phi-5/6`** (both re-read on the record — demo sheet and glass dist);
About's section interval **`pt-phi-3` sits one rung below the ratified `--space-phi-5`** (OBC PR-35).
Token-level analogue of L-4's component aliasing: the same species as X-W10.24's material-ladder
collapse, one layer down in the token graph.
*cureShape+locks*: **disposition verbatim — *"X-W10.f (CC-102 producer-contract citation) with the
CC-112 φ-canon bank adjacency; may not enact GOLDEN-GLASS naming (OP-3)."*** The **may-not** is
load-bearing and is carried into G-34's falsifier: a cure that renames the ladder to the golden-glass
vocabulary **enacts an owner mark this wave does not hold**. **G-7 is the sibling gate and the
warning**: X-W5 marks `61.8033989`/`38.1966011` FABRICATED TOLERANCE, so a φ cure that cites those
numbers cites a fabrication (edge 12). *sequencing*: with G-7's citation pass, before `.f`'s
satellite fold. *dissent*: —
⟨AboutPane.md · AB-32 (reader-1 miss, ADOPTED)⟩ — layer collision guard: ⟨AboutPane · AB-32⟩ ≠
⟨shell-dock-actionbarlayer · AB-32⟩ (X-W4-FOLD W4·57)

**X-W10.97 · CNL-25 — X-W11 folded the row and routed two limbs OUT to X-W10.d; this fold adopts
them id-for-id** (cl.1+cl.5) **MINOR (bundle).** *Mechanism*: four limbs, of which **two are this
wave's**: **(i)** the channel name at **`text-body`** — *"a third role-matrix escape and the loudest
text in the component"* (`:52-58`) — a direct **VC §4 closed-matrix** violation of exactly the shape
G-15 measures; **(ii)** the **forced-colors roster omission** — the hovered node **paints live colour
as data** (the file's own C3 comment) yet appears in **neither** the U-F57 tier-1
`forced-color-adjust` roster **nor** as `[data-color-surface]`. The other two limbs (six decorative
`ArrowRight` without `aria-hidden`; the faux-italic device, 10 sites, three semantics) go to the
boundary a11y bundle with AB-36 and are **not** adopted here.
*cureShape+locks*: **disposition verbatim — *"aria/bidi rows → the boundary a11y bundle (with AB-36);
type-role rows → X-W10.d; the roster row → X-W10.d with K-6 cited."*** **K-6's law is the lock**:
under it this is **NOT scope-shopping — it is the declared colour-data roster missing a declared
colour-data surface**, and the roster half is **static** (the *outcome* needs-live, the omission does
not). The roster edit lands in the **§4 row 10 widening** (`foundation.css:678-694` / `:815-840`)
that X-W10.80 already requires — **one carve, two rows**. *sequencing*: with X-W10.80; the type-role
limb with X-W10.30. *dissent*: **ROW-CARRIAGE, stated** — X-W11-FOLD carries CNL-25 (`:606`
*"**BOUND** (a11y/bidi bundle) + OUT → X-W10.d (type-role + roster, K-6 cited)"*, `:716` the W11←W10
edge). The identity is **banked at X-W11 and adopted here for its OUT limbs only** — this is an
adoption, not a second booking, and the a11y/bidi half stays W11's.
⟨ColorNutritionLabel.md · CNL-25 (type-role limb + roster limb) · X-W11-FOLD.md:606/:716 (the OUT
route this fold consumes)⟩

**X-W10.98 · EAS-23 — focus ink and curve ink are one token, the UA ring is removed, and the parent's
own certified ring register is not adopted** (cl.1+cl.2) **MAJOR.** *Mechanism*:
`--easing-curve-accent ← --motion-accent` (`:115`) feeds **both** the curve stroke **and** the
focus-visible handle stroke, while **`focus:outline-none` removes the UA ring** — so the focus
indicator is the same ink as the thing being drawn, on top of it. The parent's own
**`--focus-ring-shadow` register (`:285-288`) is not adopted**: the certified recipe exists one file
away and is skipped. *cureShape+locks*: **disposition verbatim — *"X-W10 focus law + GLASS-RELAY."***
This row is the **fourth witness for G-19** and sharpens its predicate from *"the token is unminted"*
to *"the token is unminted **and** the one certified register that does exist is unadopted next
door"* — `--color-ring` has **25 occurrences / 12 files** and **exactly ONE** file consumes
`--focus-ring-inner`/`--focus-ring-outer`. **A-10's lock stands verbatim: a per-component recompose
leaves 24 sites broken.** *sequencing*: the focus law precedes X-W6's edit
(`EasingAuthoringStage.vue` is **X-W6's**, `W6:60` `modify`). *dissent*: the GLASS-RELAY limb is a
**question, not a patch** — added as relay line (h) at §6g edge 14′.
⟨wb-gradient-easingauthoringstage.md · EAS-23 (D3-11)⟩

**X-W10.99 · EAS-25 — a rostered species' sibling is unrostered: `.easing-authoring` has no
forced-colors design at all** (cl.1) **MINOR.** *Mechanism*: `.easing-authoring` is **absent from
`foundation.css`'s enumerated tier-1 roster**, while **`.gradient-rail` / `.readout-rail` ARE
rostered — the sibling thirty lines above** (the record's own awk). The roster is not merely
mis-aimed (X-W10.80(a)) — **it is also incomplete along its own declared species line**, which is a
different defect with the same cure site. *cureShape+locks*: rides **§4 row 10's widening** and
X-W10.80's ruling; the **jurisdiction** question X-W10.80 settles (EC-8 right about the bytes; widen,
never re-route) governs this row unchanged. **No new gate**: G-16's paint-measurement lock already
binds any forced-colors green, and a second roster gate would double-count the same census.
*sequencing*: after X-W10.80's jurisdiction act, before any `.f` edit. *dissent*: —
⟨wb-gradient-easingauthoringstage.md · EAS-25 (D-13)⟩

**X-W10.100 · EB-7 — `role="alert"` wraps the recovery control, and the focus hack exists because of
it** (cl.1+cl.4) **MAJOR.** *Mechanism*: the sole recovery `<Button>` (`:30`) sits **inside** the
`role="alert"` region (`:19`) — re-verified structurally by the record's own seat. **WAI-ARIA reserves
`alert` for content requiring no interaction and directs `alertdialog` when it does**, and
`alertdialog` supplies **by pattern** exactly the modality / return-focus contract that `:21`/`:64`
**hand-patch**. That inverts the usual reading: the focus hack is not a wart on a correct role, it is
the **symptom** of the wrong one — *"which makes EB-6's 'drop tabindex, focus the named control' half
a cure while the role is wrong."*
*cureShape+locks*: **disposition verbatim — *"→ successor surface; the role decision is a canon act
(X-W10 note)."*** The **canon act is this wave's**; the successor surface is not. **The lock that
matters: EB-6 may not be executed alone** — dropping the tabindex while the region stays `alert`
ships half a cure and retires the evidence for the other half. *sequencing*: canon ruling before any
ErrorBoundary touch. *dissent*: **BOUNDS — `ErrorBoundary.vue` returns ZERO hits across all twelve
`waves/W*.md`** (this seat's grep). §4 row 17 names it only as one of the `.plate-ink` five, *"unowned
for this purpose"*. See §6f row 24.
⟨ErrorBoundary.md · EB-7 (reader-DU missed find; ADMITTED)⟩

**X-W10.101 · N-5 — `--muted-foreground` is re-defined PER RUNG, and that fact is the fixer's map for
every muted seat** (cl.1+cl.4) **INFO (map, adopted by name).** *Mechanism*: `ladder.css` re-points
`--muted-foreground` **per rung — three re-points plus a backdrop-container escalation** (ladder
re-points read by that seat; token values confirmed in `dark-arm.css` / `on-glass-fg.css`). The
record calls it two things at once: *"the fixer's map for every muted seat here"* **and** *"the fact
that killed the D-3 rider."*
*cureShape+locks*: **disposition verbatim — *"Record — attach to X-W10's design-law fold."*** This row
is that attachment. It is **INPUT to the ink/rung law** (X-W10.48 / G-20) and to X-W10.24's material
ladder: a rung-aware token is the *correct* mechanism, and rows that read a flat `--muted-foreground`
value at one rung and generalize are **wrong by construction** — which is precisely how it killed
D-3. **It is a map, not a defect, and this fold does not grade it as one.** *sequencing*: available
to `.c`'s ladder authoring immediately. *dissent*: **`ladder.css` returns ZERO hits across all twelve
`waves/W*.md`** — the map's own file is in no wave's bounds (§6f row 25).
⟨shell-dock-profilesection.md · N-5 (reader-DU)⟩ — collision guard: ≠ ⟨X-W3 · N-5⟩ (tag-array
uniqueness) ≠ ⟨X-W6 W6·4 · N-5⟩ (the strip limb)

**X-W10.102 · R-15 — canon §5's axis composition is not adopted, and the label is structurally
detached from the axis it names** (cl.1+cl.4) **MAJOR.** *Mechanism*: **label structurally detached**
— **no `aria-controls` / `for` / `labelledby`**, verified against ConsoleRail's own template — **unit
glued into the value string**, **numeric entry absent**, and **four new slider mechanics minted in the
consumer**. VC:104 verbatim; **PR-05 on the double boundary.**
*cureShape+locks*: **disposition verbatim — *"→ glass BH relay + X-W10 canon"*, with the record's own
qualifier carried whole: *"note A-17 (no chassis arms exist at 8.0.0) makes this a producer-roadmap
ask, not a demo edit — the composition-adoption judgment itself is residue row 10."*** **The fold
therefore SPLITS it and refuses to gate the unfixable half**: the **composition adoption** is a relay
ask (edge 14′ line (i)) and the **programmatic-association half is fixable in-demo today** and is the
only half G-35 measures. **This is the same trap as X-W10.21** — a canon that binds a housing the
producer killed — and it is the second time the corpus has walked into it, which is why the split is
written rather than assumed. *sequencing*: the canon states the axis law; the a11y association can
land in any wave touching `ComponentSliders.vue` (**X-W4's**, `W4:133` `modify-carve`). *dissent*:
the composition-adoption judgment stays **residue row 10** on the record and is not resolved here.
⟨picker-componentsliders.md · R-15 (CONFIRMED)⟩ — collision guard: ≠ ⟨PaletteColorStrip · R-15⟩
(X-W11-FOLD `:560/:564`)

**X-W10.103 · PS-13 — `≡`-POINTER + the recipe-fleet limb: the boldest ink on the card is the
provenance chip, and the weight is the ratified recipe's own** (cl.1+cl.2) **MAJOR (weight limb only).**
*Mechanism*: the provenance chip is the **boldest ink on the card (700 vs the palette identity's
500)** and is tinted with the **palette SEED**, which **VC:21 forbids outside named lanes**, while
sibling chips tint with **ROLE** tokens. **The weight limb was RESCOPED by that seat against PS-18's
find**: `font-bold` is **the ratified `.slug-pill` recipe's own weight** (`foundation.css:586`) — so
*"the hierarchy inversion indicts the RECIPE fleet-wide (all 8 migrated sites), and its cure sits at
the recipe/canon level, not this leaf."*
*cureShape+locks*: **disposition verbatim — *"→ X-W7.c/f + X-W10 (weight rung at the recipe)."***
**`≡`-pointer**: the `.slug-pill` recipe identity is **already banked at X-W10.84(b)** (Users =
`.slug-pill` = `text-mono-small font-bold`, one of the four Admin identity registers) and bounded at
**§4 row 10** (`foundation.css:585-587`). **What this row ADDS and X-W10.84 does not carry: the
fleet count — all 8 migrated sites — and the inversion's direction** (the recipe makes *provenance*
louder than *identity*). **The seed-tint limb stays leaf-owned** (`safeFirstColor` from
`palette.colors[0]`) and is **X-W7's**. **Counterweight carried honestly**: the ink **is
contrast-certified by construction on the named WELL tier** — the defect is hierarchy, not legibility,
and a cure that darkens it has cured nothing. *sequencing*: the recipe rung ruling precedes X-W7.c/f.
*dissent*: — **⟨PaletteCardSwatches · PS-19⟩** — the sibling row folded into ⟨PaletteCard · PC-35⟩ at
**X-W7.49** — is pointer-closed at §6d, not re-booked here.
⟨PaletteCardSwatches.md · PS-13 (D-11; weight limb rescoped) ≡ the `.slug-pill` recipe at
X-W10.84(b)⟩ — collision guard: ⟨PaletteCardSwatches · PS-13⟩ ≠ ⟨PreviewStrip · PS-13⟩ (X-W9-FOLD
`:433-450`, the 4.0.0 manifest row)

### 6d. Pointer register — identities closed WITHOUT a second booking

*The consumption rule's own shape: "where a roster id is a facet of an already-folded identity, the
cure is an explicit `≡`-pointer row, never a silent skip, so the byte census closes." These are not
rows and are not counted as rows; each names the identity that already carries the fact.*

| ⟨record · id⟩ | why it is a pointer, not a booking | where the fact lives |
|---|---|---|
| ⟨PaneHeader.md · **PH-36**⟩ | the record itself refuses the booking: *"tsconfig `paths` drift: ALREADY BOOKED, identity = ColorSpaceSelector L-14 → W-HYGIENE config parity. Not re-booked (anti-rename). Recorded here so the next formation does not re-litigate."* Carried because `PaneHeader.md` is **this wave's record** and PH-36 boundary-matched **nowhere** in the fold layer | ⟨ColorSpaceSelector · L-14⟩ → W-HYGIENE |
| ⟨PaletteCardSwatches.md · **PS-19**⟩ | *"IDENTITY ≡ PaletteCard.md PC-35 — the folder-wide census is banked there"*; **PC-35 is folded at X-W7.49** (⟨PaletteCard.md · PC-35⟩ + ⟨PaletteCardMeta.md · PM-13⟩). **The rider this fold flags for the X-W10 law**: `prefers-contrast: more` re-derives `--border` toward full-ink (`foundation.css:736`) but Tailwind's `/15` compiles to `color-mix(… 15%, transparent)` **on top of** the token — *"the modality cure is throttled to 15 % of its declared weight in exactly the mode built to fix it."* **A post-hoc alpha on a modality-managed token defeats modality management** — the structural reason the idiom was retired, and a G-16 paint-measurement case | X-W7.49 (row) · §4 row 10 + G-16 (the law) |
| ⟨shell-panesegmentedcontrol.md · **PSC-24**⟩ | *"The `:45` Ad-18 marker is X-W10's tracked marker (`W10.md:117`/`:354`): **discharged by X-W5's deletion, recorded as the survivor's terminal event, spending the W10 row** — the cross-wave mechanics are already written; nothing to add."* The row is **X-W5's** (§A roster); its consequence is **G-8's denominator** and is recorded there (§6e sharpening 1) | X-W5 (row) · G-8 (the consequence) |
| ⟨picker-spectrumcanvas.md · **D-07-b**⟩ | a **may-not-be-cited lock** riding an identity already folded at **X-W10.23** (D-07's cause 1). Carried **verbatim** because pass-1 defect 5 is precisely that such edicts travel nowhere: *"its premise … is the same unmeasured mechanism C-9-MECH kills; **the LayoutCount attribution may not be re-cited as established**."* True half kept: the prescribed `translate` + shadow-deletion cure **leaves a per-frame `getBoundingClientRect()` that should be hoisted to `pointerdown`** (the standing C-9 nit) — a cure-checklist item on X-W10.23 | X-W10.23 (row) · this register (the lock) |

**Sibling-wave law-limb pointers (9).** These route **primary** to another wave and carry an
**X-W10 law** limb. They are on **X-W7's** round-1 roster and are folded there; recorded here so this
wave knows which laws it is being asked for and by whom, and so no reader mistakes the law limb for
an unfolded row: ⟨EmptyState · **ES-33**⟩ *(the `hint` in Fira Code — a matrix role reserved for
value/code/provenance; → X-W7's copy abrogation **under X-W10**)* · ⟨PaletteCardMeta · **PM-3**⟩
*(bare `title` as sole carrier; → X-W7.f **under X-W10 canon**)* · ⟨PaletteCardMeta · **PM-12**⟩
*(two spellings of mono, one FLUID against two FIXED; ≡ BrowsePane m5, banked → X-W7 + X-W10; the
producer ships `text-mono-micro`)* · ⟨PalettesPane · **PP-12**⟩ *(IDENTITY-FOLD → PaletteCard PC-4 →
X-W7.c G11, **+ X-W10 rider via PC-1's booking for the motion-canon law**)* · ⟨SearchFilterBar ·
**SFB-26**⟩ *(two colour-mix spaces in one popover; **the mixing-space law → X-W10**)* ·
⟨SearchFilterBar · **SFB-32**⟩ *(the literal pile in a container-scaled system, VC §3 law 7; **law →
X-W10**)* · ⟨ShadowPalette · **SP-8**⟩ *(forced-colors: **the NAMED GAP is X-W10's** — "`foundation.css`
is in no X wave's list at roster scope (X-W5/X-W10 carve it narrowly elsewhere)"; **§4 row 10's
widening is the answer to it**)* · ⟨TagEditPopover · **TEP-22**⟩ *(`ml-auto` physical; subject → X-W7
`ms-auto`, **census → X-W10 canon** — a G-30 witness)* · ⟨VersionHistoryDrawer · **VHD-23**⟩ *(forced
colors kills the colour payload to identical black circles; → X.W7.d **under X-W10 law**, VC §4.1's
forced-colors clause — the third witness for §4 row 10)*.

### 6e. Gates — G-28..G-36 born-RED, and five sharpenings of standing gates

*Same L-19 posture as §3: a real witness, a real falsifier, no proof script. Every RED baseline below
is a source record's own measurement re-cited with its anchor, or this seat's grep over the twelve
`waves/W*.md`.*

| # | predicate | RED witness at HEAD | falsifier | rows |
|---|---|---|---|---|
| **G-28** SLOTTED-TARGET-FLOOR | at **every** scrub position, interactive descendants slotted into `.pane-header-title` measure ≥ **24×24** CSS px and stay in the viewport | About slots a real `<button>` (`ColorSpaceSelector inline`, `aria-label` "Select color space") into the scaled `<h3>`; at the designed 0.618 a 24px control scrubs **below the WCAG 2.5.8 floor**, and at the **1280 pole** (`−7.3909`, X-W10.1) it **leaves the viewport entirely** — visible in the committed WebKit shot | **measuring at REST passes** — the gate must scrub, or it is X-W10.6's vacuous oracle again. A cure that only fixes the ratio (X-W10.1) **leaves this red**: the transposition *"changes but does not remove the geometry question for slotted content"* | .87 |
| **G-29** HEADING-SLOT-WELL-FORMED | `.pane-header-title`'s `<h3>` contains **no flow-content root** and is **never nameless** | **two witnesses**: `AdminPane.vue:5` slots `<Badge>`, whose producer root is a **`<div>`** — flow content in phrasing-only content, *parsing silently, which is why nothing surfaced it*; and the bare `<slot />` with **no fallback** reserves a full line box and yields an **empty heading** | a `v-if` guard alone turns the empty-heading arm green and leaves the content-model arm red — **both arms or neither**. **D-7's amendment is a binding rider on X-W10.d** and travels with this gate | .88 .90 |
| **G-30** LOGICAL-DIRECTION | under `dir=rtl` no editor overlay escapes its host, and no directional glyph points against its own mirrored row | **four witnesses**: the rename overlay's **74px escape** (RTL right edge **760.1** vs well **686**, `insetInline` identical both directions) with the literal `&rarr;` at `:63` **`Bidi_Mirrored=No`** · `transform-origin: left top` at `PaneHeader.vue:184` with no `:dir()` arm (X-W10.9) · the cast shadow `8px 8px 0 0` **physical**, so RTL mirrors the layout and not the shadow (XP-26, X-W10.22) · `ml-auto` mis-resolving by **14.875px** (TEP-22) | **`transform: scaleX(-1)` on the glyph is NOT a cure** — it inverts stroke weight; the character is replaced. **Honest scope, carried**: no RTL commitment repo-wide (`dir=` grep 0, 8 physical-margin sites) — this gate is a **design law under test**, not a shipped-regression gate, and must be stated as such or it reads as a false alarm | .91 .9 .22 |
| **G-31** ONE-MARK-ONE-MEANING | within one route, no glyph carries two commands | **two identical ✓ on screen during an edit, 379.5px apart, bound to different commands** (`:76` vs `:141`; a third at `Dock.vue:143`) — and the **larger, accent-tinted, NAMED** one is the **minor** command while the **smaller, nameless** one **commits the palette** | **making the commit glyph louder is not a cure** — it preserves the collision at higher volume. PR-05 | .92 |
| **G-32** WELL-INSIDE-GLASS-RULED | the canon rules the well-inside-glass case explicitly, **and** no non-interactive row carries an interactive hover grammar | `DESIGN.md:98` seats VersionHistoryDrawer rows in the **WELL** rung while rung 3 names drawers **CHROME** — *the law's own table sanctions a deployment its rationale forbids*; `:25` hand-copies `.interactive-item` **minus** `:focus-visible` and `:active`, onto a row that is not interactive, over a **50%-alpha accent** inside a glass host whose well-law precondition is false | **a green here that cites `utilities/base.css`'s full recipe without the focus and press arms is the defect wearing the cure's name.** The gate is red until the canon *rules* — an unruled case cannot be conformed to | .93 |
| **G-33** PLATE-ONE-HEIGHT-LAW | the normalized-square sampler resolves **isotropically**: height declarations on the plate = **1**, and no **dynamic** viewport unit feeds a pointer-mapped axis | **five declarations for one box** — `h-[20dvh] min-h-24 max-h-40 lg:h-[14rem] lg:max-h-none` (`:11`) — a **64px discontinuity at `lg`**, and `dvh` re-scaling the pointer→v map **live under the finger** during the deliberately permitted `pan-y` scroll (`useSpectrumPlateStyle.ts:41-43`, `@touchmove.passive :20`) | collapsing to one declaration **that is still `dvh`** fixes the count and keeps the gesture bug → red. **The SS3-law-7 citation is KILLED and may not be cited as this gate's warrant** — it is a SPACING law | .94 |
| **G-34** PHI-LADDER-ONE-HOME | demo-local `--phi-*` declarations = **0**, or every φ rung cites the named producer contract | `foundation.css:458-462` declares a **private `--phi-0..4`** shadowing the producer's shipped **`--space-phi-5/6`** (both re-read); About's `pt-phi-3` sits **one rung below** the ratified `--space-phi-5` (OBC PR-35) | **a rename to the golden-glass vocabulary turns it green and violates OP-3** — *"may not enact GOLDEN-GLASS naming"*. And per **edge 12**, a citation to `61.8033989`/`38.1966011` is a citation to **FABRICATED TOLERANCE** (X-W5 item 5): **citing a fabrication is not a cure** | .96 |
| **G-35** SLIDER-LABEL-PROGRAMMATIC | every slider axis label is **programmatically associated** with its control, and the unit is not glued into the value string | **no `aria-controls` / `for` / `labelledby`** anywhere in the axis composition (verified against ConsoleRail's template); unit glued into the value string; numeric entry absent; **four new slider mechanics minted in the consumer** (VC:104 verbatim, PR-05 on the double boundary) | **the composition-adoption half is DELIBERATELY NOT GATED**: `A-17` — no chassis arms exist at 8.0.0 — makes it *"a producer-roadmap ask, not a demo edit"*, and a gate that cannot go green inside the wave is a trap of exactly the X-W10.21 species. **This gate measures only the half the demo can fix.** | .102 |
| **G-36** LIVE-REGION-ROLE-CORRECT | no `role="alert"` region contains an interactive control | the sole recovery `<Button>` (`:30`) sits **inside** the `role="alert"` region (`:19`); WAI-ARIA reserves `alert` for content requiring **no** interaction and directs `alertdialog` when it does — which is why `:21`/`:64` **hand-patch** the modality/return-focus contract `alertdialog` supplies by pattern | **EB-6 executed alone turns nothing green and retires the evidence**: dropping the tabindex and focusing the named control is *"half a cure while the role is wrong"*. **Bounds guard**: `ErrorBoundary.vue` is in **no wave's §4** (§6f row 24) — the gate is stated, the write is not yet homed | .100 |

**Five sharpenings of standing gates — no new gate rows, stated so they are not lost.**
1. **G-8** (DR-13 BUNDLE TERMINAL) — its RED baseline counts **`Ad-18` markers = 2**
   (`PaneSegmentedControl.vue:45`, `DockViewSelect.vue:57`). ⟨shell-panesegmentedcontrol · PSC-24⟩
   confirms `W10.md:117`'s own conditional from the corpus side: **X-W5's deletion discharges the
   first marker as the survivor's terminal event, spending that W10 row.** The denominator is
   therefore **2 today, 1 after X-W5** — and a green stamped against a denominator of 2 after the
   deletion is a green over a file that no longer exists. **The count must be re-measured at open**,
   as `W10.md:237` already instructs.
2. **G-15** (TYPE-MATRIX-CLOSED) — **two new witnesses**: CNL-25's channel name at **`text-body`**,
   *"a third role-matrix escape and the loudest text in the component"* (X-W10.97); and PS-13's
   **`font-bold` at the ratified `.slug-pill` recipe across all 8 migrated sites** (X-W10.103) — the
   second is **FAMILY-SCOPED under US-10's binding clause**: charge the leaf and the gate turns red
   for the family.
3. **G-16** (CASCADE-LAYER-PAINT) — **a sixth independent measurement**: `prefers-contrast: more`
   re-derives `--border` toward full ink (`foundation.css:736`) while Tailwind's `/15` compiles to
   `color-mix(… 15%, transparent)` **on top of the token**, throttling the modality cure to **15% of
   its declared weight in the very mode built to fix it** (⟨PaletteCardSwatches · PS-19⟩). **A
   class-list grep stamps this green.** A-3's lock is why it cannot.
4. **G-19** (RING-TOKEN-MINTED) — **a fourth witness with a new shape**: EAS-23 shows the failure is
   not only *unminted token* but *unadopted certified register* — `focus:outline-none` removes the UA
   ring while the parent's own `--focus-ring-shadow` (`:285-288`) sits one file away, and **exactly
   ONE** file in the demo consumes `--focus-ring-inner`/`--focus-ring-outer`. **A-10's lock stands:
   a per-component recompose leaves 24 sites broken.**
5. **G-25** (PLATE-INK-ONE-HOME) — ⟨wb-extract-workbench · XW-32(a)⟩ is the **fifth independent
   attestation** of the ⟨wb-extract-pane · XP-19⟩ identity (X-W10.67). No change to the predicate;
   recorded because the corpus has now named the same `.plate-ink ×5` census from five records and a
   sixth consumer still rides the retired `text-muted-foreground`.

### 6f. BoundsDelta addendum — rows 20-27 (continuing §4)

*Same verbs. Every "zero hits" below is this seat's grep over all twelve `docs/tranches/X/waves/W*.md`
at HEAD, re-runnable.*

| # | file(s) | verb | why, per the records | rows |
|---|---|---|---|---|
| 20 | `demo/picker/PaneHeader.vue` — the **adornment slot** | **WIDEN** (scope, not file) | the file is W10's, but PH-9/PH-26's cure is **an API change** (*"an adornment SLOT beside the heading, not inside it"*), not a style edit. Both source rows bank it **NO-WAVE-OWNER**, candidate = *"the wave that lands PH-8's seam"*. **Either W10 takes the API act or it is FORMATION-BOUNDARY — silence is the one inadmissible answer** (X-W10.86) | .87 .88 .90 |
| 21 | `demo/palettes/browser/dialog/VersionHistoryDrawer.vue` | **ROUTE — to X-W4, not X-W7** | **bounds defect, new.** VHD-22 routes its recomposition to **X.W7.d** and VHD-23 to **X.W7.d** — but the file sits in **X-W4's** §4 (`W4:127`, `modify`) and appears in **no W7 bounds table** (this seat's grep). Under `W10 §3a`'s own rule an out-of-bounds write **invalidates the wave**; two corpus rows currently prescribe one | .93 · ⟨VHD-23⟩ |
| 22 | `demo/palettes/browser/card/CurrentPaletteEditor.vue` | **WIDEN** the carve | in `W4:164` as **`modify-carve — impostor seat :98`**. ADJ-M2's anchors are **`:299-311`** and **`:63`**; ADJ-m1's are **`:76`** and **`:141`** — **all four outside the `:98` carve.** The rows are adjudicated here; the edits are unexecutable inside X-W4's carve as written | .91 .92 |
| 23 | `demo/picker/controls/SpectrumCanvas/SpectrumCanvas.vue` · `useSpectrumPlateStyle.ts` | **WIDEN** the carve | in `W4:168` as **`modify-carve — import + face only`**. D-06's cure edits the **height declarations at `:11`** and M-3 touches `useSpectrumPlateStyle.ts:41-43` — **outside import+face.** `SpectrumCanvas.vue` is additionally named in `W10:306` as a **G-4 CONFORMING example** — the prose-mention species X-W10.85 already rules is **not ownership** | .94 |
| 24 | `demo/color-picker/ErrorBoundary.vue` | **BOUNDARY** (or ADD with row 17) | **zero hits across all twelve wave files.** §4 row 17 names it only as one of the `.plate-ink` five, *"unowned for this purpose"*. EB-7's role act (`alert` → `alertdialog`) needs the same file. **Two unowned obligations on one unowned file** | .100 · .67 |
| 25 | `demo/styles/ladder.css` | **BOUNDARY** (record) | **zero hits across all twelve wave files.** N-5 names it as *"the fixer's map for every muted seat"* — three per-rung re-points plus a backdrop-container escalation. **A wave that authors an ink/rung law (G-20, X-W10.48) against a file no wave may open is authoring against a map it cannot amend** | .101 |
| 26 | `demo/workbenches/gradient/GradientVisualizer/easing/EasingAuthoringStage.vue` | **ROUTE** (X-W6, `W6:60` `modify`) | EAS-23's focus register and EAS-25's roster membership are **X-W10 law, X-W6 edit**. Note the collision with W6's own **e2 gate**: it counts *"2 rAF sites today (`:58`, `:65`), neither PRM-gated"* on the same file — **the focus cure and the rAF gate share a file and a wave; the write order is X-W6's to state** | .98 .99 |
| 27 | `demo/picker/display/ColorNutritionLabel.vue` · `demo/picker/controls/ComponentSliders/ComponentSliders.vue` | **ROUTE** (X-W6 `W6:69` `modify-carve` · X-W4 `W4:133` `modify-carve`) | CNL-25's type-role limb and R-15's label-association limb are **law here, edit there**. CNL-25's **roster** limb does **not** ride this row — it lands in **§4 row 10's `foundation.css` widening**, with EAS-25 and SP-8's NAMED GAP: **one carve, three rows** | .97 .102 |

### 6g. CrossEdges addendum — edges 15-17, and one relay extension

| # | edge | the constraint |
|---|---|---|
| 15 | **X-W5 ⟹ X-W10** (Ad-18, the marker that spends itself) | `W10.md:117` already states the conditional; ⟨shell-panesegmentedcontrol · PSC-24⟩ **confirms it from the corpus side**: X-W5's `delete` on `PaneSegmentedControl.vue` **discharges the `:45` marker as the survivor's terminal event and SPENDS the W10 row**. **G-8's denominator must be re-measured after X-W5, never inherited** — a green against a denominator of 2 after the deletion is a green over a file that no longer exists. Distinct from edge 12 (which is about G-7 and fabricated tolerance): **same wave pair, different obligation** |
| 16 | **X-W10 ⟹ X-W4** (three carves, law-before-edit) | three X-W4 files carry X-W10-adjudicated rows whose anchors fall **outside X-W4's declared carves**: `CurrentPaletteEditor.vue` (`:98` carve vs `:63`/`:76`/`:141`/`:299-311`) · `SpectrumCanvas.vue` (import+face vs `:11`) · `VersionHistoryDrawer.vue` (in W4's bounds while **two corpus rows route its cure to X.W7.d**). **X-W10 supplies the direction law, the one-mark law and the plate law; X-W4 must widen or the cures are unexecutable — and neither wave discovers this at execution** |
| 17 | **X-W11 ⟶ X-W10** (adoption of an OUT route) | X-W11-FOLD `:606`/`:716` route ⟨ColorNutritionLabel · CNL-25⟩'s **type-role and roster limbs OUT to X-W10.d** while keeping the a11y/bidi half BOUND. **This fold adopts the OUT limbs id-for-id (X-W10.97) and claims nothing else** — the adoption is the row-carriage law working in the direction it was written for, and it is recorded so a round-2 census does not read one identity in two folds as a double booking |

**Relay extension — edge 14′ (two new lines on the standing glass-ui BH fond; still one letter, still
zero producer patches).** **(h)** the certified `--focus-ring-shadow` register (`:285-288`) exists and
the easing seat does not adopt it while removing the UA ring — *is the certified register intended as
the app-wide focus channel, and should `--color-ring` be minted producer-side or formally retired?*
(EAS-23 + G-19's four witnesses). **(i)** the axis composition of canon §5 (`aria-controls`/`for`,
unit-outside-value, numeric entry) has **no chassis arm at 8.0.0** (A-17) — *is the composition on the
producer roadmap, or is the demo expected to mint the four slider mechanics it has already minted?*
(R-15; **a producer-roadmap ask, not a demo edit**, and this fold refuses to gate it — G-35 measures
only the half the demo can fix).

### 6h. Arithmetic — restated at the new true counts, 2026-08-28

**This block supersedes §1's "OUT side" sentence, which stands as the round-0 record.**

| measure | round-0 (§1) | added this round | **new true count** |
|---|---|---|---|
| rows (contiguous `X-W10.1..`) | 86 | **+17** (`X-W10.87..X-W10.103`) | **103** |
| — of which §A NO-WAVE-OWNER identities adopted | — | **4** (PH-9 · PH-26 · PH-33 · PH-34) | 4 |
| — of which §B census escapes adopted | — | **7** (PH-9 · PH-34 · ADJ-M2 · ADJ-m1 · VHD-22 · D-06 · XW-32) | 7 |
| — of which §C I-28 EXTERNAL rows | — | **0** (none assigned to X-W10) | 0 |
| — of which **ceiling finds** (this seat's fresh census, on no roster) | — | **8** (AB-32 · CNL-25 · EAS-23 · EAS-25 · EB-7 · N-5 · R-15 · PS-13) | 8 |
| gate rows | 27 (9 sharpened + 18 born-RED) | **+9 born-RED** (G-28..G-36) | **36** (9 sharpened + 27 born-RED) |
| bounds-delta rows | 19 | **+8** (rows 20-27) | **27** |
| cross-edges | 14 | **+3** (15-17) + 2 relay lines on edge 14 | **17** |

**Reconciliation of the row count, so it decomposes.** 4 (§A) + 7 (§B) + 8 (ceiling) = **19
assignments**, minus the **2 declared overlaps** (PH-9 and PH-34 each carry an NWO limb and a routed
limb and are **folded once, both limbs**) = **17 numbered rows**. The **4 pointer closures** (PH-36 ·
PS-19 · PSC-24 · D-07-b) and the **9 sibling-wave law-limb pointers** are **NOT rows** and are not
counted as rows — they are §6d, and they exist so the byte census closes without a second booking.
**Assignment coverage: §A 4/4 · §B 7/7 · §C 0/0 = 11/11**, the full ROUND-1-ASSIGNMENTS §D row for
this seat, plus 8 beyond it.

**Dispositions are ADJUDICATED, not VERIFIED. Status `planned`. Nothing here stamps a wave.**
**E-3 re-verified for this round**: `docs/tranches/X/waves/W0.md..W11.md` and
`CONFORMANCE-2026-08-03.md` are byte-untouched by this seat; no record under
`registry/adjudicated/` was modified; `X-W10-FOLD.md` is this seat's sole write, this round as last.
**Anti-rename honoured**: every identity above is its original id, cited ⟨record.md · id⟩, with the
five namespace collisions the citation law exists for named explicitly at §6.0 and on the rows.


---

## 7. ROUND-2 FOLD ADDENDUM — 2026-08-28 · the census-method repair (X-W10.104 … X-W10.108)

**What this section is.** This seat's consumption of `refinement/ROUND-2-ASSIGNMENTS.md` **§A.6** —
**five ⟨record·id⟩ homed here** (4 routed · 1 NWO) by the pass-2 BLOCKER
(`refinement/PASS-2-CHECK-RETURN.json`: 84 escapes across 27 records, absent from the entire fold
layer **by bytes**). The escapes are a **method** failure the work order names and retires at §D: a
row routed by bare declarative phrasing — *"→ X-W10"* inside a table cell, *"folded into D-05's cure
(X-W10)"*, *"candidate X-W10 … rider"* — is invisible to the 30-character proximity heuristic every
seat inherited. §6.0's own census used the stricter **token + routing-verb** reading and so shares the
blindness; §7.1 restates it under the law.

**Posture unchanged.** Status `planned`; dispositions **ADJUDICATED**, never VERIFIED. No product
source opened — every witness is a corpus byte re-read at HEAD. **E-3**: `waves/W0.md..W11.md`,
`CONFORMANCE-2026-08-03.md`, `ROUND-1-ASSIGNMENTS.md` and all `registry/adjudicated/**` records are
byte-untouched; `X-W10-FOLD.md` is this seat's sole write, this round as last. **Anti-rename**: every
id below is its record's original id, cited ⟨record.md · id⟩, with three live namespace collisions
named on their rows rather than renamed away.

### 7.1 The census under the §D CENSUS METHOD LAW — restated, re-measured, reproducing

**The predicate** (clause 1): a row line carrying an `X[-·.]W10` token **in any phrasing, at any
position** BOOKS the row — no proximity, no marker verb, no position. **The boundary is mandatory**
(clause 4): `X[-·.]W1` without a trailing-digit guard swallows `X-W10`/`X-W11`, which is the trap
§6.0's header already flagged from the other side.

```
# corpus (92) → X-W10-routed row lines (264, across 60 records)
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | wc -l
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' \
  | xargs grep -nE '^\s*([-*]|\|)\s*\*\*' | grep -cE 'X[-·.]W10([^0-9]|$)'
```

**Both reproduce at HEAD (`92`, `264`), measured read-only by this seat 2026-08-28**, and `264`
matches §D's law-true table id-for-id. **Dated correction to §6.0, never silent:** its table reads
*"corpus rows touching X-W10 … **319**"* and *"row-line-ROUTED (token + routing verb on the row line)
… **213**"*. Both stand as the round-1 record; **neither is the census predicate any more**. `319`
counts row lines **plus continuation blocks** (a different, wider unit) and `213` applies the verb
filter §D clause 2 retires. **The law-true row-line denominator is 264**, and the five rows below are
inside the `264 − 213` the verb filter hid. Per clause 6 the count follows the predicate: the figure
changed because the predicate did, and both figures stay legible.

**§B.2's defect class is ABSENT from this fold** — checked, not assumed: this file's printed census
figures are re-derivable and are re-derived above; the non-reproducing *"282"* the work order strikes
lives at `X-W4-FOLD.md` §6E and is that seat's cure.

**Namespace guards (three, all live).**
1. ⟨picker-spectrumcanvas · **D-05**⟩ (X-W10.104) ≠ ⟨picker-spectrumcanvas · **D-06**⟩ (X-W10.94) ≠
   the layer's `D-5`/`D-6` tokens in other records' namespaces. **Zero-padding is identity** — the
   same guard X-W10.94 already carries, now load-bearing in both directions inside one record.
2. ⟨picker-spectrumcanvas · **M-7**⟩ (X-W10.105) ≠ ⟨picker-colorcomponentdisplay · **M-7**⟩ — the
   latter is folded at **X-W10.62** (the readout format law). One bare token, two records, two rows.
3. ⟨AdminNamesPanel · **D-15**⟩ (X-W10.107) ≠ ⟨picker-spectrumcanvas · **D-15**⟩ (→ X-W4) ≠
   ⟨shell-dock-mobilemenudropdown · **D-15**⟩ ≠ ⟨PaneHeader · **PH-34 (D:D-15)**⟩ (X-W10.90). Four
   records, one bare id; **cite ⟨record.md · id⟩ or not at all.**

### 7.2 §A.6 — the five rows

**X-W10.104 · D-05 — the plate has no visible boundary at rest, and the boundary channel it lacks is
already reserved and transparent** (cl.1+cl.2) **MAJOR.**
*Row verbatim* (`picker-spectrumcanvas.md:49`): *"**D-05 — CONFIRMED (MAJOR, mechanism corrected;
D-05b + the WCAG framing killed) → X-W10.** The plate has no VISIBLE boundary at rest — its own
content supplies #fff and #000 at the corners; at-rest box-shadow is `0 0 0 0 transparent` (`:232`);
the forced-colors tier-1 grant preserves the content and the white-on-white worst case. BUT the
boundary channel already exists: an unconditional, permanently-reserved 3px transparent outline at the
right offset (ruling table). Cure = assign the reserved ring a resting value (M-7 folded in). The
measured ratios (1.538 light / 1.091 forced-colors) ride UNPROVEN with A's antialiasing caveat."*
*cureShape+locks*: the cure is **one declaration on a channel that is already paid for** — assign the
reserved ring a resting value; it is not a new box, not a shadow, not a size change. **Two kills ride
the row and are carried, not dropped**: `D-05b` and **the WCAG framing** are KILLED at the record, so
no close may re-file this as a contrast-ratio failure; and the measured **1.538 light / 1.091
forced-colors** ratios ride **UNPROVEN** with reader A's antialiasing caveat — they are context, never
a gate baseline. *sequencing*: **with X-W10.94 (D-06, the one-height law) and X-W10.23 (the same face's
two prohibited shadow registers) — one surface, one ruling**; a resting ring assigned while the plate's
five height declarations still resolve anisotropically paints a boundary around a box whose size is
still under negotiation. *dissent*: none; the record's own mechanism correction is adopted whole.
⟨picker-spectrumcanvas.md · D-05 (:49) — mechanism corrected; D-05b + WCAG framing KILLED⟩

**X-W10.105 · M-7 ≡ D-05 — the reserved 3px transparent ring IS the missing channel: one cure, two
ids** (cl.2) **INFO · `≡`-pointer, no second booking.**
*Row verbatim* (`picker-spectrumcanvas.md:94`): *"**M-7 — INFO — the unconditionally-applied
`touch-gate-target` reserves a permanent 3px transparent ring on every desktop session — the exact
boundary channel D-05 says the plate lacks → folded into D-05's cure (X-W10).** (Reader A; the decider
of the D-05 ruling.)"*
This row exists so the **byte census closes on `M-7`** without minting a second name for X-W10.104's
cure — the record itself folds it in, and the fold obeys. **The independent fact it contributes**: the
ring is reserved **unconditionally, on every desktop session**, so the cure spends no new space; and
the reserved-but-invisible ring is itself the reason D-05's "no boundary" reading survived review.
*sequencing*: inherits X-W10.104's. *dissent*: —
⟨picker-spectrumcanvas.md · M-7 (:94)⟩ ≡ X-W10.104 (D-05's cure) · **≠** ⟨picker-colorcomponentdisplay.md · M-7⟩ (X-W10.62)

**X-W10.106 · D-14 — wordmark/handle/slug are not LTR-isolated, and the constitution already
legislates the span** (cl.1+cl.3) **MINOR (ruled).**
*Row verbatim* (`shell-dock-profilesection.md:57`): *"| **D-14** | MINOR (ruled) | Wordmark/handle/slug
are not LTR-isolated; in an RTL context the wordmark renders `mbabb@` (bidi-derivable, both readers
independently). LATENT: no RTL arm ships (my grep — one comment hit). Constitution :154 legislates
LTR-isolated spans. | Component sites :75/:140/:149 + twin :50/:84 read. | **X-W10** (lands with the
constitution conformance pass). |"*
*cureShape+locks*: the row's own terminal is the disposition — **it lands with the constitution
conformance pass**, not as a component patch: `VISUAL-CONSTITUTION.md:154` already legislates
LTR-isolated spans, so the cure is **conformance to a written law**, and the five sites (`:75`/`:140`/
`:149` + twin `:50`/`:84`) are its census. **The LATENT flag is carried verbatim and is load-bearing**:
no RTL arm ships (one comment hit repo-wide), so this is a **design law under test**, exactly the
posture **G-30** (LOGICAL-DIRECTION) is already stated in — *"a design law under test, not a shipped-
regression gate"*. **G-30 gains a fifth witness; its predicate is unchanged and no gate is minted.**
*sequencing*: with X-W10.51 (the ProfileSection emphasis register — same file, same pass) and under
G-30's direction law. *dissent*: **cross-edge, stated** — ⟨PaletteColorStrip · D9⟩ (X-W11) books the
same physical→logical conversion law on the U-tranche RTL ledger; **W11 books its row, this fold books
the law limb, and neither re-books the other** (see §7.3 edge 18).
⟨shell-dock-profilesection.md · D-14 (:57) — ruling row at :30, banked roster row at :57⟩

**X-W10.107 · D-15 — the admin names surface misses PR-35 at all three rungs** (cl.1) **MINOR.**
*Row verbatim* (`AdminNamesPanel.md:79`): *"| **D-15** | Spacing misses PR-35 at all three rungs (12 vs
24; 10 constant vs 16/12 responsive; 6 vs 8). | **X-W10** |"*
*cureShape+locks*: three rungs, three measured deltas, **one ratified authority** — the row is a
conformance miss against **OBC PR-35**, the same ratified spacing authority X-W10.96 cites when About's
`pt-phi-3` sits one rung below `--space-phi-5`. The cure is to seat the three rungs on the ratified
ladder; **it is not a φ-token mint**, and **OP-3's bar stands — no GOLDEN-GLASS naming may be enacted
to satisfy it** (X-W10.96's lock, restated because it binds any PR-35 cure). **G-34**
(PHI-LADDER-ONE-HOME) gains a second component witness; **predicate unchanged, no gate minted**.
*sequencing*: after the φ ladder has one home (X-W10.96) — re-seating rungs onto a shadowed ladder
re-parents the miss instead of curing it. *dissent*: — . **Namespace**: guard 3 of §7.1 applies.
⟨AdminNamesPanel.md · D-15 (:79)⟩ · law home: OBC PR-35 · adjacency: X-W10.96 / G-34

**X-W10.108 · M7 — a digit-count boundary slides the pager arms mid-repeat-click; `tabular-nums`
holds digit width, not count** (cl.1+cl.2) **INFO · DUAL-LIMB (§D clause 5).**
*Row verbatim* (`PaginationBar.md:61`): *"| **M7** (design) | **INFO** (design filed MINOR, self-flagged
the downgrade; this seat takes it) | `justify-center` + natural-width label: a digit-count boundary
(9→10) widens the sentence ~3.3px and slides each arrow ~1.65px outward mid-repeat-click. `tabular-nums`
holds digit width, not count. A one-rung-down echo of DESIGN.md's card-lock law (:67-78 — tabular
figures AND `ch` reservation; only the first is present), below the law's literal display-scale
trigger. Cure: one `min-w-[Nch]`. | **NEW. NO-WAVE-OWNER** — candidate X-W10 canonical-design rider at
the formation boundary. |"*
**Why it books HERE and what the NWO limb does** (§D clause 5 — *NWO and routed are limbs, not
partitions*): the row line carries **both** `NO-WAVE-OWNER` and an `X-W10` token, so **clause 1
governs the booking** and the NWO/formation-boundary hedge **rides as a stated cross-edge**, not as a
second booking. The work order measures this as **the single clause-1 override among the 46 NWO
escapes**; its packet home would otherwise be X-W7. **A census that treats the two registers as
disjoint re-orphans exactly this row.**
*cureShape+locks*: **one `min-w-[Nch]`** — the reservation half of DESIGN.md's card-lock law
(`:67-78`), whose tabular half already ships. Substantively **the same law** that owns
⟨picker-colorcomponentdisplay · M-4/M-5/M-8/M-9/M-12⟩ at **X-W10.62**: *tabular figures AND `ch`
reservation*, and this row is the corpus's proof that the second clause is missed outside the readout.
**The self-flagged downgrade is carried verbatim** (design filed MINOR, the record's own seat took
INFO) — both grades legible, neither silently elected. *sequencing*: with X-W10.62's readout format law
— one reservation law, two surfaces. *dissent*: the formation-boundary hedge is the record's, not this
fold's; it is recorded at the **G-F register** (X-W0-FOLD §7) as a cross-reference so the boundary seat
sees the same identity.
⟨PaginationBar.md · M7 (:61) — dual-limb: routed X-W10 ∧ NO-WAVE-OWNER⟩ ≡ law of X-W10.62 · G-F cross-ref

### 7.3 CrossEdge addendum — edge 18 (continuing §6g)

| # | edge | the constraint |
|---|---|---|
| 18 | **X-W10 ⟷ X-W11** (one direction law, two ledgers) | ⟨shell-dock-profilesection · D-14⟩ (X-W10.106) and ⟨PaletteColorStrip · D9 ≡ PCS-31⟩ (X-W11's round-2 row) are the **same physical→logical conversion law** on two ledgers: the constitution conformance pass (`VISUAL-CONSTITUTION.md:154`) and the U-tranche booked RTL ledger (`docs/tranches/U/audit/w-a11y/rtl-logical-property-audit.md`). **X-W11 books D9 under its own id; this fold books the law limb; neither re-books the other.** The shared caveat is carried on both sides: `index.html` hard-pins `dir="ltr"` as **deliberate booked readiness**, so both rows are laws under test — G-30's stated posture, not a shipped-regression claim |

### 7.4 Arithmetic — restated at the new true counts, 2026-08-28 (fold round 2)

**This block supersedes §6h's row line; §6h stands as the round-1 record.**

| measure | round 1 (§6h) | added this round | **new true count** |
|---|---|---|---|
| rows (contiguous `X-W10.1..`) | 103 | **+5** (`X-W10.104..X-W10.108`) | **108** |
| — of which §A.6 routed | — | **4** (D-05 · M-7 · D-14 · D-15) | 4 |
| — of which §A.6 NWO (dual-limb, clause 5) | — | **1** (M7) | 1 |
| — of which `≡`-pointer bookings (no second cure) | — | **1** (M-7 ≡ D-05) | 1 |
| gate rows | 36 (9 sharpened + 27 born-RED) | **+0** | **36** — **no gate minted, none re-staged or re-weighted**; G-30 gains a fifth witness (D-14) and G-34 a second component witness (D-15), predicates unchanged |
| bounds-delta rows | 27 | **+0** | **27** — every row lands on a path §4/§6f already carries (`SpectrumCanvas.vue` row 23 · `ProfileSection.vue` via X-W10.51 · `AdminNamesPanel.vue` and `PaginationBar.vue` as canon-law rows whose edits are elsewhere) |
| cross-edges | 17 | **+1** (edge 18) | **18** |
| census denominator | 319 touching / 213 verb-routed | **264 law-true row lines / 60 records** | re-derived, reproducing (§7.1) |

**Reconciliation:** §A.6 assigns **5** (4 routed + 1 NWO); **5 numbered rows** are booked, zero
overlaps declared, so the walk is `5 = 5`. **Assignment coverage: §A.6 5/5 id-for-id.** Numbering is
gapless and contiguous **X-W10.1 … X-W10.108**; zero ids minted, zero renamed. Dispositions are
**ADJUDICATED, not VERIFIED**; status `planned`; nothing here stamps a wave.
