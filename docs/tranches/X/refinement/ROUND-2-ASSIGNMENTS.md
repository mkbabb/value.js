SERVED MODEL ID (assignment seat, line-1 declaration): claude-opus-5[1m]

# ROUND-2-ASSIGNMENTS — the fold-round-2 work order (2026-08-28)

**What this file is.** The per-wave repair seats' work order for X·V fold **round 2**, written by
the ASSIGNMENT SEAT against `refinement/PASS-2-CHECK-RETURN.json` (verdict NON-CONFORMANT: 1
BLOCKER · 2 MAJOR · 5 MINOR · 1 INFO). It enumerates, id-for-id: **(§A)** the **84 escaped
⟨record·id⟩ across 27 records** the pass-2 BLOCKER found absent from the entire fold layer by
bytes — 38 routed + 46 NO-WAVE-OWNER, zero overlap — each grouped by the **home fold file** that
must adopt it, with the **banked row text quoted**; **(§B)** the two MAJOR cures; **(§C)** the five
MINOR cures; **(§D)** the **CENSUS METHOD LAW** to be written into the spine, which retires the
30-character proximity heuristic that manufactured the BLOCKER.

**Posture.** Status `planned`. Dispositions **ADJUDICATED**, never anything stronger. No product
source opened. **E-3 holds**: `docs/tranches/X/waves/W0.md..W11.md` and
`waves/CONFORMANCE-2026-08-03.md` stay byte-untouched, and so does
`refinement/ROUND-1-ASSIGNMENTS.md` — its one erratum (§C.4) is registered here as an addendum,
never patched in place. This file is the assignment seat's **only** write.

**Anti-rename.** Every id below is the original id for life. Citation key **⟨record.md · id⟩** —
bare ids collide across six namespaces (`A-13` names a PaletteSlugBar row AND a slugeditlayer row
in this very roster; `D-14` names a profilesection row AND a mobilemenudropdown row; `MISS-7`
names an actionbarlayer row AND a consolerail row). Zero-padded ids are their own identity
(`D-05` ≠ `D-5`, `D-06` ≠ `D-6`).

**Quoting convention.** Each row below quotes the banked row line's **identity-bearing head** and
its **terminal segment**, verbatim, with ` … ` marking elision. The record line number is the
authority; the quote is the falsifier. Every `:line` was re-resolved at HEAD by this seat, not
inherited from the check.

**Homing rule (two clauses, applied in order).**
1. **Routed rows** — home = the wave token printed on the row line itself, in whatever phrasing it
   appears (§D's law). This is the check's own predicate, freed of the proximity heuristic.
2. **NWO rows** — home = the **packet's home** per the spine's §1 (`ROUND-1-ASSIGNMENTS` §A
   logic), *except* where the row line itself carries an `X-W` token, in which case clause 1
   governs (§D's law binds NWO rows too). Exactly **one** of the 46 does: ⟨PaginationBar.md · M7⟩.
   Packets the spine books FORMATION-BOUNDARY send their rows to the G-F register, never to a
   wave band — the round-1 treatment of ⟨MigratePalettesDialog.md · A-31⟩ is the precedent.

**Consumption rule.** Each seat folds its rows into its `X-W*-FOLD.md` in that fold's own idiom
(dated addendum section; a ⟨record.md · id⟩ provenance line per row; dedupe by banked identity —
where a roster id is a facet of an already-folded identity the cure is an explicit `≡`-pointer
row, **never a silent skip**, so the byte census closes). Rows homed FORMATION-BOUNDARY land at
the G-F register slate (X-W0 seat).

---

## §A. The 84 escaped ⟨record·id⟩ → home fold file

**Arithmetic:** 18 + 5 + 3 + 41 + 9 + 5 + 1 + 2 = **84** = 38 routed + 46 NWO.

| home fold file | routed | NWO | total |
|---|---|---|---|
| `X-W4-FOLD.md` | 11 | 7 | **18** |
| `X-W5-FOLD.md` | 0 | 5 | **5** |
| `X-W6-FOLD.md` | 0 | 3 | **3** |
| `X-W7-FOLD.md` | 15 | 26 | **41** |
| `X-W8-FOLD.md` | 8 | 1 | **9** |
| `X-W10-FOLD.md` | 4 | 1 | **5** |
| `X-W11-FOLD.md` | 0 | 1 | **1** |
| `X-W0-FOLD.md` (G-F register) | 0 | 2 | **2** |
| **Σ** | **38** | **46** | **84** |

---

### A.1 → `X-W4-FOLD.md` — 18 rows (11 routed · 7 NWO)

**Routed (11).** The seven-row colorcomponentdisplay bloc is the BLOCKER's sharpest exhibit: all
seven print `→ X-W4` **verbatim** and X-W4-FOLD §6B nonetheless declares the X-W4 census closed at
35 escapes.

- ⟨picker-colorcomponentdisplay.md · **C-1**(:32)⟩ — *"**C-1 (+ C-6 clause ii, folded) —
  CONFIRMED (MAJOR) → X-W4.** Browser-mutated contenteditable DOM and Vue-owned nested text
  interpolations have no commit/reconciliation contract. … Crash arm (v-if anchor detachment →
  insertBefore DOMException) UNPROVEN-NEEDS-LIVE."*
- ⟨picker-colorcomponentdisplay.md · **C-2**(:33)⟩ — *"**C-2 — CONFIRMED (MAJOR) → X-W4.** The
  sole handler reads `innerText.trim()` and emits per input event (`:30-35`); zero
  `keydown/beforeinput/composition*/paste/blur` bindings in 214 lines. … verified."*
- ⟨picker-colorcomponentdisplay.md · **C-4**(:35)⟩ — *"**C-4 (= L-5, one identity) — CONFIRMED
  (MINOR) → X-W4.** `defineEmits` declares `update: [value: number, component: string]`
  (`:92-95`); the only `emit(` call is `input` (`:33`). … the consumer BINDS the dead event …"*
- ⟨picker-colorcomponentdisplay.md · **D-1**(:36)⟩ — *"**D-1 — CONFIRMED (MAJOR, ruled over B's
  MINOR) → X-W4.** No edit affordance on the page's least-input-like element (ruling above; M-22
  TRUE cited)."* **Namespace note:** `:24` is the `D-1 severity` **ruling** row, a different line;
  the banked roster row is `:36`. Fold the roster row.
- ⟨picker-colorcomponentdisplay.md · **D-2**(:37)⟩ — *"**D-2 — CONFIRMED (MAJOR) → X-W4.** Four
  silent outcome paths, zero painted state: PARSED … REJECTED … CLAMPED … COMMITTED (500ms
  later). Plus `useColorPipeline.ts:195`'s non-finite drop — a fifth silent site."*
- ⟨picker-colorcomponentdisplay.md · **D-3**(:38)⟩ — *"**D-3 — CONFIRMED (MINOR) → X-W4.** No
  `inputmode`, no mobile editor arm anywhere in the scoped block … the residual keyboard
  ergonomics ride UNPROVEN."*
- ⟨picker-colorcomponentdisplay.md · **M-6**(:51)⟩ — *"**M-6 — MAJOR — the card's only heading
  takes its accessible name from labelled descendants … → X-W4 (with L-1).** … Exact AT
  interleavings ride UNPROVEN."* Co-lands with the already-folded L-1 heading half.
- ⟨picker-spectrumcanvas.md · **D-15**(:73)⟩ — *"**D-15 — CONFIRMED (MINOR) → X-W4 (dies in the
  rebuild's template).** Captionless single-child `<figure>` with `m-0` cancelling its one UA
  behaviour … an UNNAMED `role=\"figure\"` boundary is announcement noise layered on D-12's wrong
  name."*
- ⟨shell-dock-genericactionbar.md · **U-1**(:74)⟩ — *"**U-1** — the ROOT 390×844 Reset witness
  (90°→337°→Reset→337° after 250 ms): text-only, no artifact, no verdict depends on it. Must be
  replaced by a durable capture at **X.W4.d D4's MEASURE-AT-OPEN**; never re-carried as
  'binding'."* Dotted-form token — invisible to any hyphen-only sweep (§D).
- ⟨wb-extract-controls.md · **K-H**(:106)⟩ — *"**K-H — D-10's 'named only by hover title'
  framing**: the governing file's own a11y tree prints computed names for all three; residue =
  weak last-resort naming, **carried in the X-W4 family**."* Bare-declarative routing, no marker
  verb — the exact phrasing class §D's law recovers.
- ⟨wb-extract-workbench.md · **K-21**(:107)⟩ — *"**K-21 — this seat's kill of L-r3 §0's cure
  position …**: … the cure is an 11-site convention decision + a producer `label`-prop relay, per
  the sibling's XP-8 and **X-W4's accname law**."*

**NWO (7) — packet: spine §2 e.12 / §1 packet 8.** `shell-dock-slugeditlayer.md` carries ZERO X
tokens and routes 33/35 rows NO-WAVE-OWNER, but `SlugEditLayer.vue` **is** in W4 bounds
(`W4.md:118`) and the spine corrects the routing at the fold (X-W4-FOLD folds the record on FILE
ownership; W4·122). All seven ride that correction.

- ⟨shell-dock-slugeditlayer.md · **D-6**(:44)⟩ — *"**D-6 — MAJOR.** One undifferentiated
  `type=\"text\"` field carries two authorities and renders the elevated one unmasked … **
  Disposition: W23 (constitution-assigned Account Dialog owner); else NO-WAVE-OWNER at
  boundary.**"* **CANON RIDER (state, do not swallow):** `W23` is a V′ identity no X wave owns;
  the W4 seat books the row and relays the canon-terminal limb (VISUAL-CONSTITUTION.md:222) to
  the G-F register as a cross-edge — X-W5-FOLD W5F-24 already carries the same canon terminal.
- ⟨shell-dock-slugeditlayer.md · **D-13**(:51)⟩ — *"**D-13 — MAJOR.** Constitutional orphan: canon
  assigns Account to a modal side Dialog owned by W23 with W15 supplying auth state … **
  Disposition: W23 (constitution-assigned); else NO-WAVE-OWNER…**"* Same canon rider as D-6.
- ⟨shell-dock-slugeditlayer.md · **C-8**(:60)⟩ — *"**C-8 — MINOR.** Escape dismisses only when the
  input holds focus; `.stop` (:86) starves any ancestor fallback; the layer has four focusables,
  two outside the `<form>` … **Disposition: NO-WAVE-OWNER.**"*
- ⟨shell-dock-slugeditlayer.md · **C-12**(:62)⟩ — *"**C-12 — MINOR.** A pasted admin token
  survives dismissal in the always-mounted DOM … the retained value can be a live credential. **
  Disposition: NO-WAVE-OWNER.**"*
- ⟨shell-dock-slugeditlayer.md · **C-13**(:75)⟩ — *"**C-13 — INFO.** `:6` imports `writeClipboard`
  from the root barrel while `:4` uses the narrow `/dock` path … **Disposition: NO-WAVE-OWNER,
  fleet-level sweep.**"*
- ⟨shell-dock-slugeditlayer.md · **L-10**(:76)⟩ — *"**L-10 — INFO (harness defect, not component
  defect).** … **Disposition: NO-WAVE-OWNER — the harness gains a visibility check; the active
  state joins the capture matrix (residue).**"*
- ⟨shell-dock-slugeditlayer.md · **A-13**(:77)⟩ — *"**A-13 — INFO (net-new; RR-LC).** The very
  file whose comment rules `catch (e:any)` dead re-offends 29 lines below it … **Disposition:
  NO-WAVE-OWNER — dies with D-12's cure.**"* **Namespace guard:** ⟨shell-dock-slugeditlayer ·
  A-13⟩ ≠ ⟨PaletteSlugBar · A-13⟩ (A.2 below) — two rows, two records, one bare id.

---

### A.2 → `X-W5-FOLD.md` — 5 rows (0 routed · 5 NWO)

**Packet: §1 packet 8 SLUG-CHAIN (HOMED X-W4 + X-W5; the typed-contract half is X-W5's BD-01)**
for the PaletteSlugBar four; **X-W5 BD-15** (`ParseEchoReadout.vue`, *"co-lands with A-1 → X-W5"*
per that record) for K-13.

- ⟨PaletteSlugBar.md · **A-10**(:45)⟩ — *"**A-10 (C-18) — the shipped vitest project cannot import
  an SFC at all.** … **Disposition: NO-WAVE-OWNER (same instrument-capability decision as A-9 —
  admit SFC mounting deliberately or r…)**"*
- ⟨PaletteSlugBar.md · **A-13**(:48)⟩ — *"**A-13 (N-5, reader-1 new find) — FlagReportDialog's
  destructive confirm renders as a neutral twin of its own Cancel.** … `variant` dead, the real
  axis is `tone` … **Disposition: NO-WAVE-OWNER (same authored-hierarchy family as A-12…)**"*
  Cross-edge: the defect is *in* `FlagReportDialog.vue`, whose six routed rows sit at A.4 — the
  W5 seat books the identity and `≡`-points the file-touch to X-W7.
- ⟨PaletteSlugBar.md · **A-22**(:60)⟩ — *"**A-22 (D-29/D-30 + N-10) — six hand-rolled focus seats
  at `ring-ring/40` with `outline-none` … while a producer recipe that solves all of it
  (`focus-ring` + the a11y-overrides WHCM branch) ships installed** … **Disposition:
  NO-WAVE-OWNER (repo-scale focus-recipe adoption; the 6 in-file seats die with A-1).**"*
- ⟨PaletteSlugBar.md · **A-30**(:68)⟩ — *"**A-30 (N-11, reader-2 new find) —
  `demo/palettes/browser/index.ts`, the self-declared 'TOP-LEVEL SEAM… stable public API', has
  ZERO importers** … **Disposition: NO-WAVE-OWNER (rule with A-6 at the boundary-ban
  authoring…)**"* Cross-edge: X-W5-FOLD W5F-23 already carries the deletion's BOUNDS LOCK to
  X-W8.a — book A-30 at W5 as the barrel limb, `≡`-point the deletion census to X-W8.
- ⟨shell-dock-parseechoreadout.md · **K-13**(:65)⟩ — *"**K-13 — reader-2 missed-6 (`--ink-muted`
  needs the fallback)** — killed by ruled law (R①): the prescription IS the ruled-MAJOR masking
  idiom … The residue reader-2 was reaching for … is ES-13's already-booked NO-WAVE-OWNER row —
  not this leaf's defect."* KILLED-with-residue: the W5 seat books the kill and `≡`-points the
  residue at ⟨EmptyState.md · ES-13⟩ (X-W7's band).

---

### A.3 → `X-W6-FOLD.md` — 3 rows (0 routed · 3 NWO)

**Packets: §1 packet 17 GRADIENT-UNOWNED TRIO (HOMED X-W6 — `GradientCodeEditor.vue` adopted by
name) and §1 packet 1 MX-CLUSTER (HOMED X-W6 — the adoption transfers the whole docket).**

- ⟨wb-gradient-codeeditor.md · **A-28**(:47)⟩ — *"**A-28 — INFO · CONFIRMED (reader-1 miss M-8,
  spot-checked).** D-r1's script-block anchors drift −2..−4 against a byte-identical file … **
  NO-WAVE-OWNER** — registry method note: any wave navigating this corpus re-resolves r1
  anchor…"* Books as W6's registry-method note (it constrains every seat that cites this record).
- ⟨wb-gradient-easingeditor.md · **M-EE-5**(:79)⟩ — *"**M-EE-5 (LC) — vitest carries no value.js
  self-alias: the unit gate certifies `dist/` while `@src` imports certify source — ADJUDICATED
  MINOR → NO-WAVE-OWNER (next formation boundary).**"* **STATED DIVERGENCE:** the row's own text
  names the formation boundary and its substance is config-parity (the W-HYGIENE **H-c** family
  the spine books FORMATION-BOUNDARY at §1 packet 5). Home stays X-W6 by packet (17, easing
  family). If the W6 seat reads the identity as H-c's `≡`, it re-points to the G-F register
  **stated, never silently**.
- ⟨wb-mix-animationcanvas.md · **MX-26**(:87)⟩ — *"**MX-26 — L-13: published-surface facts — 7-key
  exports (no `.`, no `require`), `dist/value.js` + `dist/index.d.ts` absent while load-bearing
  comments assert them.** Facts re-verified; L's provenance citations killed (K-19). Identity
  overlaps MX-20; rides W-HYGIENE. → **NO-WAVE-OWNER (banked identity)**."* Same stated
  divergence: `≡ MX-20` is W6's (fold once, both limbs); the "rides W-HYGIENE" limb is a G-F
  cross-edge, not a second booking.

---

### A.4 → `X-W7-FOLD.md` — 41 rows (15 routed · 26 NWO)

**Routed (15).** The six-row FlagReportDialog bloc is the BLOCKER's second exhibit: X-W7-FOLD
§R1.16 folds that record as *"10 rows"* against **21** X-W7-routed rows in it. All six end with
the same bare-declarative terminal, which carries no marker verb and so was invisible to the
inherited method.

- ⟨FlagReportDialog.md · **A-16**(:49)⟩ — *"**A-16 (D-7 + r1-missed #6 ≡ r2-missed M7)** —
  proximity inverted … and the mechanism is a **second spacing system** … **X-W7 rider else
  boundary.**"*
- ⟨FlagReportDialog.md · **A-17**(:50)⟩ — *"**A-17 (D-8 rescoped)** — fixed `h-20` +
  `resize-none` + `maxlength=500` + no counter on a line-quantised control … **X-W7 rider else
  boundary.**"*
- ⟨FlagReportDialog.md · **A-22**(:55)⟩ — *"**A-22 (D-12/C-12/L-10)** — `paletteSlug` required,
  destructured, never read (:68/:71 only); host re-derives its own copy (:298). Dies free with
  A-2's `:key` cure … **X-W7 rider else boundary.**"* **Namespace guard:** this record's
  `C-12`/`L-10` sub-ids are NOT ⟨shell-dock-slugeditlayer · C-12/L-10⟩ (A.1).
- ⟨FlagReportDialog.md · **A-23**(:56)⟩ — *"**A-23 (D-13)** — five lines of manual
  `:open`/`$emit` plumbing (:2, :68-77) beside a sibling using `defineModel` … **X-W7 rider else
  boundary.**"*
- ⟨FlagReportDialog.md · **A-24**(:57)⟩ — *"**A-24 (D-14)** — ASCII straight quotes around the
  palette name in display prose (:11); collides with names containing quotes. **X-W7 rider else
  boundary.**"*
- ⟨FlagReportDialog.md · **A-28**(:61)⟩ — *"**A-28 (r2-missed M5)** — hard-coded document-global
  ids `reason-${value}` (:22-23) on a component exported through two public barrels; two
  instances = duplicate ids … **X-W7 rider else boundary.**"* **Namespace guard:**
  ⟨FlagReportDialog · A-28⟩ ≠ ⟨wb-gradient-codeeditor · A-28⟩ (A.3).
- ⟨AdminNamesPanel.md · **L-3a**(:84)⟩ — *"| **L-3a** | Two admin homes with a foreign feature
  (`browser/`) interposed. | IDENTITY: the MT-AU1 console re-home already rules this
  (browser/admin ceases to exist) — **X-W7**, no second booking |"*
- ⟨AdminTagsPanel.md · **ATP-40**(:85)⟩ — *"**ATP-40 (D:D-20 / C:C-17, + this seat's third
  site)** — `@click=\"tagsApi.loadTags\"` (:10, reference) vs `:75` (call) … **Rider on X-W7's
  file touch.**"*
- ⟨AdminTagsPanel.md · **ATP-46**(:91)⟩ — *"**ATP-46 (D:D-14, property-scope residue only;
  tokenization limb killed K-4)** — `transition-all` at :99 sweeps `active:scale-95`'s transform
  … **X-W7 rider: scope the property list.**"*
- ⟨PaletteCardMeta.md · **PM-21**(:61)⟩ — *"**PM-21 (LC missed) — the `vote` emit hands the parent
  back its own prop through two relay hops, for a consumer that reads only `.slug`.**
  Contract-altitude smell, not a bug; **dies whole with X-W7.d** (the vote seat stops being an
  emit chain). Recorded so the find is not lost."* Dotted-form token.
- ⟨PaletteCardMeta.md · **U-2**(:92)⟩ — *"**U-2** — tag-pill overflow geometry at 390×844 with 3 ×
  30-char tags … The number is already commissioned: **X-W7 G9's MEASURE-AT-OPEN** records it; do
  not duplicate the probe."* **Namespace guard:** ⟨PaletteCardMeta · U-2⟩ ≠ ⟨PaletteSlugBar ·
  U-2⟩ (cited inside A-22, A.2).
- ⟨MigratePalettesDialog.md · **A-9**(:43)⟩ — *"**A-9 (C-4) — dual publish path: publishAllLocal
  is a second, weaker implementation of the canonical `onPublish`, and the dialog's primary action
  runs the weaker one.** … **X-W7 scope 8** (delete publishAllLocal; map the canonical verb; A-2
  dis…)"* Consistent with spine §1 packet 16, which keeps A-9's routed limb live at X-W7 while the
  dialog's **fate** stays behind the owner-decision gate.
- ⟨SwatchHoverMenu.md · **K-6**(:82)⟩ — *"**K-6 — D-11 as filed ('tap targets below the floor',
  REPORT attribution) — KILLED (ruling R-A; R2's kill adopted over R1's confirmation).** … The
  seat-floor obligation lives in the **X-W7.c seat spec**, not in a row."* A KILL row that still
  books: the fold must carry the kill so no close re-files D-11.
- ⟨PaletteCardSkeleton.md · **U-6**(:89)⟩ — *"**U-6** — D-12's k=16 slab (278px plate / 27.8px
  cells / 196px swatch rows), measured on ShadowPalette and transferred by class-identity; **X.W7
  G20** measures exactly this on BOTH components at wave time."* Dotted-form token.
- ⟨TagEditPopover.md · **TEP-25**(:55)⟩ — *"**TEP-25 (reader-1 missed №6) · MINOR** — glass-ui
  ships `DropdownMenuCheckboxItem` … the hand-rolled row is bespoke against an existing DS
  primitive. Recorded as **cure input to X-W7.d**; L-2's submenu transposition remains an
  unvalidated design (residue №7)."*

**NWO (26).** Packets: **§1 packet 4 XP-EXTRACT-SESSION-CLUSTER** (HOMED X-W7; BoundsDelta B-1
adopts the extract cone incl. `ImageEyedropper/**`) — 14 rows; **§1 packet 10 MMD-IDENTITY**
(HOMED X-W7, B-3) — 7 rows; **§1 packet 6 MT-AU1** (HOMED X-W7, B-2; its roster names
`PaginationBar` and the admin composables by name) + **§1 packet 14 BROWSE-SEARCH** (HOMED X-W7,
B-4; widens `PaginationBar.vue`) — 5 rows.

*Packet-4 disambiguation, stated:* ⟨wb-extract-imageeyedropper · EY-10/EY-12/EY-23⟩ each print
`NO-WAVE-OWNER (session cluster)`, and **none** appears in X-W4-FOLD W4·121's enumerated EY-CLUSTER
set (`EY-1..6, 9, 11, 13, 17-22, 25-26, 31, 33-39, 41, 47`). Packet 3 (EY-CLUSTER → X-W4) does not
reach them; packet 4 does.

- ⟨wb-extract-controls.md · **EC-9**(:47)⟩ — *"**EC-9 — … the k readout is a lying readout — it
  shows the request, not the result.** … → **NO-WAVE-OWNER · PIN**."*
- ⟨wb-extract-controls.md · **EC-10**(:48)⟩ — *"**EC-10 — … the rail renders a discrete k-means
  result as a continuous interpolation** … → **NO-WAVE-OWNER (session cluster)**."*
- ⟨wb-extract-controls.md · **EC-25**(:66)⟩ — *"**EC-25 — D4-7/L-12-tail: the cured born-RED
  `var(--muted)` pin is live and neutralised only by JS object key order** … → **NO-WAVE-OWNER**
  (session arm unpinned; the :22 write is **PIN**)…"*
- ⟨wb-extract-controls.md · **EC-34**(:75)⟩ — *"**EC-34 — C2-3/XC-7: `onFile` and `onReset`
  dispatch directly and never cancel the pending debounce** … → **NO-WAVE-OWNER (session
  cluster)**."*
- ⟨wb-extract-controls.md · **EC-36**(:77)⟩ — *"**EC-36 — reader-2 miss: raw library enum codes
  render as user-facing text** … → **NO-WAVE-OWNER (session cluster — presentation
  contract)**."*
- ⟨wb-extract-controls.md · **EC-46**(:90)⟩ — *"**EC-46 — pass-1 D-18 (restored):
  `:model-value=\"[k]\"` mints a new array identity per parent render** … → **NO-WAVE-OWNER ·
  PIN** rider."*
- ⟨wb-extract-workbench.md · **XW-4**(:37)⟩ — *"**XW-4 — MAJOR … the `isProcessing` skeleton never
  paints; the only accessible progress signal is absent during the one phase that freezes the main
  thread.** … → **NO-WAVE-OWNER** (extract-session correctness cluster)."*
- ⟨wb-extract-workbench.md · **XW-10**(:43)⟩ — *"**XW-10 — MAJOR … the failure register is one
  mutable slot shared by three producers, colour-only, unannounced, and the stale result is never
  invalidated.** … → **NO-WAVE-OWNER** (session cluster…)"*
- ⟨wb-extract-workbench.md · **XW-19**(:52)⟩ — *"**XW-19 — MAJOR … `lastFile` is written before an
  await and read after, so two rapid intakes can leave the preview showing image A while the
  palette describes image B** … → **NO-WAVE-OWNER** (session cluster)."*
- ⟨wb-extract-workbench.md · **XW-22**(:55)⟩ — *"**XW-22 — MAJOR — C-24 … a successful extraction
  yielding zero opaque pixels renders the pre-image ghost ('feed it an image') beside the image
  just fed.** … → **NO-WAVE-OWNER** (session cluster)."*
- ⟨wb-extract-workbench.md · **XW-34**(:70)⟩ — *"**XW-34 — MINOR — the worker-seam pair (reader-2
  misses, verified):** … → **NO-WAVE-OWNER** (session cluster; both die in the decode-in-worker +
  typed-seam cure)."*
- ⟨wb-extract-imageeyedropper.md · **EY-10**(:50)⟩ — *"**EY-10 — C-3 (= L-4, D-15): undecodable
  image ⇒ floating-promise rejections, a dead overlay, no error register…** … Joins the
  **wb-extract-pane session-correctness cluster** … → **NO-WAVE-OWNER (session cluster).**"*
- ⟨wb-extract-imageeyedropper.md · **EY-12**(:52)⟩ — *"**EY-12 — r3-only (C-r3 §II-1), ADMITTED: a
  breakpoint-crossing resize REMOUNTS the pane and destroys the whole extract session** … → **
  NO-WAVE-OWNER (session cluster).**"*
- ⟨wb-extract-imageeyedropper.md · **EY-23**(:63)⟩ — *"**EY-23 — L-5: one user file decoded three
  times, retained in three representations, uncapped; `previewDataUrl` never cleared** … → **
  NO-WAVE-OWNER (session cluster).**"*
- ⟨shell-dock-mobilemenudropdown.md · **D-03**(:37)⟩ — *"**D-03 — MAJOR.** Two copy verbs in one
  list; `Share color` confirms …, `Copy slug` is mute … **Disposition: NO-WAVE-OWNER — one
  copy-feedback owner for both verbs and the twin…**"*
- ⟨shell-dock-mobilemenudropdown.md · **D-11**(:49)⟩ — *"**D-11 (items 1-2) — MINOR** (ruled
  above). Two desktop separators absent on mobile … **Disposition: NO-WAVE-OWNER — dies by
  construction with any section-model recomposition.**"* (`:25` is the severity **ruling** row;
  `:49` is the banked roster row.)
- ⟨shell-dock-mobilemenudropdown.md · **D-12**(:50)⟩ — *"**D-12 — MINOR.** The theme glyph renders
  16×24 … **Disposition: NO-WAVE-OWNER (one-line `h-4`; the real cure is a producer-owned icon
  slot rung).**"*
- ⟨shell-dock-mobilemenudropdown.md · **D-14**(:55)⟩ — *"**D-14 — MINOR.** One recipe, two
  override mechanisms 17 lines apart … **Disposition: dies with MMD-3's Chip adoption —
  NO-WAVE-OWNER.**"* **Namespace guard:** ≠ ⟨shell-dock-profilesection · D-14⟩ (A.6).
- ⟨shell-dock-mobilemenudropdown.md · **M-E**(:64)⟩ — *"**M-E (both readers, converged) —
  MINOR.** No `AvatarFallback` … **Disposition: NO-WAVE-OWNER (one-child cure; empty-state
  posture recorded).**"*
- ⟨shell-dock-mobilemenudropdown.md · **M-F**(:65)⟩ — *"**M-F (reader-1) — MINOR.** Two
  `target=\"_blank\"` links with no perceptible new-tab signal … **Disposition: rides MMD-1's row
  cure — NO-WAVE-OWNER.**"*
- ⟨shell-dock-mobilemenudropdown.md · **M-G**(:66)⟩ — *"**M-G (reader-1) — MINOR.** The tagline is
  grammatically attached to the person, not the product … **Disposition: rides D-15's
  identity-module move — NO-WAVE-OWNER.**"*
- ⟨AdminAuditPanel.md · **AAP-10**(:45)⟩ — *"**AAP-10 (C:D-4) — a 200 with the wrong shape throws
  in render with the error apparatus dark.** … **Disposition: NO-WAVE-OWNER**
  (`demo/platfor…`)"*
- ⟨AdminAuditPanel.md · **AAP-16**(:51)⟩ — *"**AAP-16 (reader-2 missed-2 + C:D-11, folded) — the
  error channel reads the wrong field on BOTH arms.** … **Disposition: NO-WAVE-OWNER**
  (composable + shared-atom contract; unowned in X)."*
- ⟨AdminTagsPanel.md · **ATP-13**(:52)⟩ — *"**ATP-13 (C:C-2, +C:C-18 folded as its sub-case) — any
  shape drift in GET /admin/tags reaches reactive state unvalidated and detonates in an
  ErrorBoundary wrapping the entire main region.** … **Disposition: NO-WAVE-OWNER**
  (`demo/platform/transport/` in no X file list — the AAP-10 row's own words), one class, never
  re-booked."* Fold once with AAP-10: the record names the identity.
- ⟨PaginationBar.md · **L-5**(:47)⟩ — *"**L-5 — KILLED** (both readers, unanimous; re-verified).
  Premise false: ONE 48-line file, TWO importers, zero duplication … The homing residue (a generic
  control living in `admin/`) is already adjudicated as **AF-40 (NO-WAVE-OWNER)** —
  cross-referenced, not re-booked."*
- ⟨PaginationBar.md · **M8**(:62)⟩ — *"| **M8** (library, inside its merged row; unpicked here to
  its own identity) | **MAJOR** | No request identity anywhere on the surface … | **= AAP-11 →
  NO-WAVE-OWNER** (composables in no wave's file list). Cross-booked; this record extends the
  identity to `useAdminFlagged.ts` by name. One machine with C-4 — adopted together or not at
  all. |"* ⟨AdminAuditPanel · AAP-11⟩ is already on X-W7's round-1 §A roster — this is the
  `≡`-pointer case, not a re-book.

---

### A.5 → `X-W8-FOLD.md` — 9 rows (8 routed · 1 NWO)

**Routed (8).** Three records, all printing `X-W8` in cell or prose form.

- ⟨picker-spectrumcanvas.md · **M-11**(:98)⟩ — *"**M-11 — MINOR — 2 of `spectrumLuma.ts`'s 3
  exports are dead surface; the module's self-presentation ('the shared home of a three-consumer
  regime') is false in both numbers → **X-W8, cure with C-5.** (Reader B; this seat's greps are
  the proof.)"*
- ⟨shell-dock-profilesection.md · **N-8**(:84)⟩ — *"| **N-8** | MAJOR | `liveTintCache` is keyed by
  `InkSurface` alone with a first-match `querySelector('[data-surface=\"veil\"]')` host … the
  first veil probed becomes every veil's referent until the next epoch. (reader-LC) | :158–172/
  :239–249 read — structurally certain. | **X-W8**."*
- ⟨shell-dock-profilesection.md · **N-9**(:85)⟩ — *"| **N-9** | MINOR | `resolveCssColorAlpha`
  fabricates a confident OPAQUE BLACK for an unparseable color … while every sibling failure path
  degrades honestly to null/undefined. … | **X-W8**. |"*
- ⟨shell-dock-profilesection.md · **N-10**(:86)⟩ — *"| **N-10** | MINOR | The instrument leaks by
  design: a hidden probe `div` appended to `document.body` with no removal path, a memoized canvas
  context, and two more unowned module-global maps … | **X-W8**. |"*
- ⟨shell-dock-actionbarlayer.md · **MISS-7**(:110)⟩ — *"**MISS-7 (R-LC) — CONFIRMED (MINOR).** The
  prescribed cure nests `.dock-crossfade` inside `.dock-crossfade` … **DISPOSITION: X-W8.d
  carry-table addendum row.**"* **Namespace guard:** ≠ ⟨picker-componentsliders-consolerail ·
  MISS-7⟩ below — two rows, one bare id, both in this same wave band.
- ⟨shell-dock-actionbarlayer.md · **MISS-8**(:111)⟩ — *"**MISS-8 (R-LC) — CONFIRMED (INFO).**
  `demo/shell/dock/index.ts` does not re-export `DockCrossfade`; the transposition's import must
  not deepen the dual path. **Discharged by construction at X-W8.b/scope-6 (the barrel dies);
  recorded so the sequencing (b → d) is honored.**"* Books as a discharged-by-construction row
  **with** its sequencing lock — a silent skip loses the lock, not just the row.
- ⟨picker-componentsliders-consolerail.md · **MISS-5**(:65)⟩ — *"**MISS-5 — MINOR —
  `sliderWrapperEls` is never pruned, and `attachSliderListeners` re-binds five listeners to every
  DETACHED wrapper from every space ever visited → **X-W8 (one cure with C-2/MISS-13).**"*
- ⟨picker-componentsliders-consolerail.md · **MISS-7**(:67)⟩ — *"**MISS-7 — MINOR —
  `componentEntries` re-derives a computed the injected model already exposes, its tuple payload
  is dead in every consumer, and the two copies disagree → **X-W8.**"*

**NWO (1).** `ActionBarLayer.vue` is X-W4/X-W8-held (X-W10-FOLD:1225) and X-W8 is the wave that
carves it (G-8/G-10, W8.31); the record's other escaped rows route X-W8 by their own bytes.

- ⟨shell-dock-actionbarlayer.md · **K-4**(:89)⟩ — *"**K-4 — 'the only two demo files that write a
  bare `:hover` are ActionButton and this one' (D-9's convention claim, and with it D-9 itself).**
  KILLED (ruling 1): my census finds bare `:hover` in **10** demo files … D-9 leaves the roster;
  residue = an app-wide INFO census, NO-WAVE-OWNER."*

---

### A.6 → `X-W10-FOLD.md` — 5 rows (4 routed · 1 NWO)

- ⟨picker-spectrumcanvas.md · **D-05**(:49)⟩ — *"**D-05 — CONFIRMED (MAJOR, mechanism corrected;
  D-05b + the WCAG framing killed) → X-W10.** The plate has no VISIBLE boundary at rest … BUT the
  boundary channel already exists: an unconditional, perman…"* **Namespace note:** `:23` and
  `:181` are ruling rows about D-05; `:49` is the banked roster row. Fold `:49`, cite the rulings.
- ⟨picker-spectrumcanvas.md · **M-7**(:94)⟩ — *"**M-7 — INFO — the unconditionally-applied
  `touch-gate-target` reserves a permanent 3px transparent ring on every desktop session — the
  exact boundary channel D-05 says the plate lacks → **folded into D-05's cure (X-W10)**. (Reader
  A; the decider of the D-05 ruling.)"* Folds with D-05 as one cure, two ids — `≡`-pointer, not a
  second booking. **Namespace guard:** ≠ ⟨picker-colorcomponentdisplay · M-7⟩.
- ⟨shell-dock-profilesection.md · **D-14**(:57)⟩ — *"| **D-14** | MINOR (ruled) | Wordmark/handle/
  slug are not LTR-isolated; in an RTL context the wordmark renders `mbabb@` … Constitution :154
  legislates LTR-isolated spans. | Component sites :75/:140/:149 + twin :50/:84 read. | **X-W10**
  (lands with the constitution conformance pass). |"* (`:30` is the severity ruling row.)
- ⟨AdminNamesPanel.md · **D-15**(:79)⟩ — *"| **D-15** | Spacing misses PR-35 at all three rungs
  (12 vs 24; 10 constant vs 16/12 responsive; 6 vs 8). | **X-W10** |"* **Namespace guard:**
  ⟨AdminNamesPanel · D-15⟩ ≠ ⟨picker-spectrumcanvas · D-15⟩ (A.1) ≠ ⟨shell-dock-mobilemenudropdown
  · D-15⟩ (cited inside M-G, A.4).

**NWO (1) — the single clause-1 override in the 46.**

- ⟨PaginationBar.md · **M7**(:61)⟩ — *"| **M7** (design) | **INFO** … | `justify-center` + natural-
  width label: a digit-count boundary (9→10) widens the sentence ~3.3px and slides each arrow
  ~1.65px outward mid-repeat-click. `tabular-nums` holds digit width, not count. A one-rung-down
  echo of DESIGN.md's card-lock law (:67-78 …), below the law's literal display-scale trigger.
  Cure: one `min-w-[Nch]`. | **NEW. NO-WAVE-OWNER** — candidate **X-W10** canonical-design rider
  at the formation boundary. |"* **This is the one NWO row whose line carries an X-W token**, so
  §D's law books it here rather than at its packet home (X-W7). Its NWO limb — the
  formation-boundary hedge — rides as a G-F cross-reference, not a second booking. Substantively
  it is the same card-lock/`ch`-reservation law that owns
  ⟨picker-colorcomponentdisplay · M-4/M-5/M-8/M-9/M-12⟩ at X-W10.

---

### A.7 → `X-W11-FOLD.md` — 1 row (0 routed · 1 NWO)

`PaletteColorStrip.md` is one of X-W11's three assigned records (X-W11-FOLD §1z: `PCS-1..PCS-31`);
its rows belong to that seat by that fold's own §1z partition.

- ⟨PaletteColorStrip.md · **D9**(:25)⟩ — *"| **D9** | provenance limb KILLED (cites the dead
  `export/` module); substance INFO on the live export.ts path | UNPROVEN + demoted: `index.html`
  hard-pins `dir=\"ltr\"` as deliberate booked readiness | **BOTH adopted, merged** | … the
  mechanism … is sound and the provenance limb is killed (K-8); no direction switch exists →
  PCS-31, INFO, NO-WAVE-OWNER on the booked RTL ledger. |"* **Cross-edge, stated:** the row's
  substance (physical→logical conversion law) is the constitution conformance pass that owns
  ⟨shell-dock-profilesection · D-14⟩ at X-W10 (A.6). W11 books D9 under its own id and
  `≡`-points the law to X-W10; **`≡ PCS-31` is already W11's**, so the byte census closes with a
  pointer row.

---

### A.8 → `X-W0-FOLD.md` · G-F register (FORMATION-BOUNDARY) — 2 rows (0 routed · 2 NWO)

Both rows' own bytes book the formation boundary, and both sit in packets the spine dispositions
FORMATION-BOUNDARY. Per the consumption rule these land at the G-F register slate, **never** in a
wave's row band.

- ⟨AdminTagsPanel.md · **ATP-35**(:77)⟩ — *"**ATP-35 (L:L-10) — tsconfig.demo.json `paths` drift
  (3 phantom targets, 2 shipped subpaths missing).** Identity: ColorSpaceSelector L-14 / AAP-32 —
  the W-HYGIENE config-parity fold no X wave claims. **Disposition: NO-WAVE-OWNER, flagged again
  at the formation boundary.**"* This is **W-HYGIENE H-c** by the record's own naming — spine §1
  packet 5 books H-c FORMATION-BOUNDARY with `W8.md:112`'s explicit decline GOVERNING, and
  X-W11-FOLD §1z n.5 already carries the four-roster identity (PreviewRamp R-9 ≡ GenerateControls
  #20 ≡ ColorSpaceSelector L-14 ≡ PaletteColorStrip PCS-30) as **one** identity booked once. ATP-35
  is the **fifth** roster of that identity: the G-F seat books it as an `≡`-pointer under its own
  id, so the byte census closes without a sixth booking.
- ⟨shell-dock-actionbarlayer.md · **MISS-10**(:113)⟩ — *"**MISS-10 (R-DU) — CONFIRMED (MINOR).**
  The mode machine is a one-directional ring with no `propose → input` edge … and no glyph in the
  cycle says it is a ring; the propose leg is a mode nested inside a mode promoted into a glyph
  cycle that cannot express it. **DISPOSITION: design residue — NO-WAVE-OWNER, formation boundary
  (th…)**"*

---

## §B. The two MAJOR cures

### B.1 — restore the destroyed `§3 e.` header (AXIS 4)

**The defect, re-verified at HEAD.** Round 1 inserted the `d′.` may-not-be-cited block into §3
**at the byte offset the `e.` header occupied** and did not re-emit the header. Receipt:

```
git show f6842ef3:docs/tranches/X/REFINEMENT-FOLD-2026-08-28.md | grep -n '^\*\*[a-e]'
  → 270 a. · 298 b. · 311 c. · 324 d. · 351 e. The escalation slate for the X-W0 sitting
grep -n '^\*\*[a-e]' docs/tranches/X/REFINEMENT-FOLD-2026-08-28.md
  → a. · b. · c. · d. · d′.   (NO e.)
```

At HEAD `:423` is `d′.4`'s last line (the COPY-SEAT edict + its PaletteCardSwatches namespace
guard) and `:424` opens mid-sentence with `bindPane (W5 §5 vs Dock R-8) BEFORE W5 opens`. The
twelve-item escalation slate therefore reads as a continuation of an edict about copy-seat sizing,
and three live pointers dangle to a section that no longer exists — all three re-verified at HEAD:
`:482` *"the collision is on §3 e's sitting slate"* · `:554` *"X-W0 (sitting · registers · census ·
**the §3.e escalation slate**)"* · `:642` *"R.2 gate 9 … its cure COLLIDES with X-W7's
quiet-migration reading (**§3 e slate**)"*.

**THE CURE (byte-exact).** Insert, as a new line **between HEAD `:423` and HEAD `:424`**, preceded
by one blank line, the original bytes recovered from `f6842ef3:351` — verbatim, no re-wording, no
renumbering:

```
**e. The escalation slate for the X-W0 sitting (all recorded, none elected by any fold):**
```

Nothing else in §3 changes. `d′` keeps its position and its number (it is a round-1 addendum and
`e` predates it, so `d′` correctly sorts between `d` and `e`); the twelve slate items keep their
bytes and their order; the three pointers at `:482`/`:554`/`:642` resolve without edit the instant
the header line exists. **Verification after the cure:** `grep -n '^\*\*[a-e]'` on the spine must
print `a. · b. · c. · d. · d′. · e.` — six heads, `e.` last — and `grep -c '§3 e\|§3\.e'` must
still return 3 with every hit resolving.

### B.2 — the fold whose "Re-runnable in one line" census does not reproduce (AXIS 5)

**The fold:** `docs/tranches/X/refinement/X-W4-FOLD.md`, **§6E**, lines **585–592**.

**The command (verbatim, as printed at :587-591):**

```
# corpus (92) → X-W4-routed rows (282) → absent from this fold (35) → record-keyed escapes (20)
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | wc -l
grep -nE '^\s*([-*]|\|)\s*\*\*' docs/tranches/V/megatranche/registry/adjudicated/*.md \
  | grep -E 'X[-·.]W4' | grep -E '→|Disposition|BOOK|BUILD|routes to|Terminal:' | wc -l
```

**The delta, measured read-only at HEAD by this seat.** Line 1 reproduces (`92`). Line 2 does
**not**:

| variant | result | vs printed 282 |
|---|---|---|
| verbatim, as printed | **257** | **−25** |
| verbatim, but band-filtered first (`grep -vE '/(kf\|fr\|pt)-'` before the row grep) | **257** | −25 — so the gap is **not** an input-set artefact |
| routing-verb filter **dropped** (the §D law predicate) | **305** | **+23** |
| routing-verb filter widened with the `\| **X-W` cell form | 275 | −7 |

**282 reproduces under no predicate.** It is neither the printed command's output (257) nor the
law-true count (305). §6E's superseded-figure table — which contrasts *"402 grep hits / 47
records"* against *"282 X-W4-routed rows / 92 records re-derived id-keyed"* and rules **both
stand** — rests on a receipt that measures nothing at the tree.

**THE CURE (three parts, all inside `X-W4-FOLD.md`; `waves/W4.md` stays byte-untouched).**

1. **Replace the command's third stage.** Delete the `grep -E '→|Disposition|BOOK|BUILD|routes
   to|Terminal:'` pipe. The census predicate becomes §D's law: *a row line carrying an `X[-·.]W4`
   token BOOKS the row, in any phrasing.* The corrected one-liner:

   ```
   # corpus (92) → X-W4-routed rows (305, across 45 records) → the §6B/§6E escape census re-derives from these
   ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | wc -l
   ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' \
     | xargs grep -nE '^\s*([-*]|\|)\s*\*\*' | grep -cE 'X[-·.]W4([^0-9]|$)'
   ```

   Both lines reproduce at HEAD (`92`, `305`). The `([^0-9]|$)` boundary is required or `X-W4`
   would swallow nothing today but would silently swallow a future `X-W40`.
2. **Restate the IN-side receipt** as **305 X-W4-routed rows across 45 records** (law-true,
   re-runnable), with **282** and **257** both kept legible and labelled: 282 = the pass-1 printed
   figure, reproducing under **no** predicate; 257 = what the pass-1 command actually returns. The
   E-3 idiom applies — supersede in place with a dated correction, strike nothing.
3. **Re-derive §6B's escape census against 305, not 282.** §6B currently declares *"the 35
   X-W4-routed ⟨record·id⟩ absent from this fold"*; §A.1 above proves at least **11** more exist
   (7 in `picker-colorcomponentdisplay.md` alone, all printing `→ X-W4` verbatim). The corrected
   census is a **precondition** for the §6B header line; the header may not be restated until the
   re-derivation runs. Same discipline as §D's law: the count follows the predicate, never the
   other way.

---

## §C. The five MINOR cures

### C.1 — `§3 d′.1`'s source anchor mis-cites (AXIS 4)

**Defect.** `REFINEMENT-FOLD-2026-08-28.md:413-415` registers the **E1-R2** edict against
⟨AdminListSkeleton.md · **:39**⟩ + ⟨AdminFlaggedPanel.md · :107/:146⟩. `AdminListSkeleton.md:39`
carries a **different** prohibition — *"X-W5's shell `role=\"status\"` region (W5:36/:251) is a
*route-settlement* node and **may never be cited as this cure**"* — which the spine already
registers separately at **§3 b.4** (`:365`: *"**X-W5 shell `role=\"status\"`**: a route-settlement
node; may NEVER be cited as the…"*). `grep -n 'E1-R2' AdminListSkeleton.md` → `:15 :21 :46 :47
:82` — **never :39**.

**Cure.** In `d′.1`, replace the anchor list with the two lines that actually carry the
prohibition, both re-read verbatim by this seat:

- ⟨AdminListSkeleton.md · **:47**⟩ — *"…UNPROVEN-NEEDS-LIVE (residue #1) — the E1-R2 certification
  may NOT be cited for this component until re-measured."*
- ⟨AdminListSkeleton.md · **:82**⟩ — *"The E1-R2 ΔL certification does not currently apply to this
  component; live measurement required before it is ever cited again."*

Keep ⟨AdminFlaggedPanel.md · :107⟩ and ⟨AdminFlaggedPanel.md · :146⟩ — **both resolve exactly**
and the quoted edict text is verbatim-correct, so **the edict binds**; only one of three line
pointers was wrong. Add a one-line stated cross-reference: *"the `:39` prohibition is a different
edict (X-W5's `role=\"status\"` route-settlement lock), registered at §3 b.4."* The other three
d′ edicts verify exactly and are untouched: DVS-38 @ `shell-dock-dockviewselect.md:93`, ONE-AXIS @
`GradientStopEditor.md:120`, COPY-SEAT @ `PaletteCardSwatches.md:24`.

### C.2 — the `§2` DELEGATION NOTE walks an unreproducible base with no command (AXIS 4)

**Defect.** Spine `:314-318` asserts *"**66 bounds-conviction statements across 33 records** (the
check's sweep: in-no-bounds / unowned / no-wave-names-the-file phrasing)"* and walks
*"**66 = 22** answered in this register (e.1–e.22 …) **+ 44 delegated**"* — an **inherited**
receipt with **no re-runnable command anywhere in the note**.

**This seat's own re-derivation, read-only at HEAD** (corpus = the 92):

```
cd docs/tranches/V/megatranche/registry/adjudicated
ls *.md | grep -vE '^(kf|fr|pt)-' | xargs grep -nEi \
  'in no (wave.?s )?bounds|no bounds table|unowned|no wave names the file|not in (any )?bounds|bounds table (does not|omits)'
  → 47 hits / 29 records          # the note's own stated phrasing
# widened net (adds: 'no bounds', 'bounds do/does not', 'outside … bounds', 'absent from … bounds')
  → 54 hits / 31 records
```

Neither reaches 66/33. (The check measured the widened net at 54/**30**; this seat measures
54/**31** — a one-record regex divergence, recorded, not silently reconciled.)

**Cure — three parts, all inside the note, E-3 addendum style.**
1. **Print the command.** The note becomes falsifiable: the base is whatever the printed command
   returns, dated, at a named HEAD.
2. **Restate the base as measured** — **47 statements / 29 records** under the note's own stated
   phrasing (54/31 widened) — with **66/33** kept legible and labelled *"inherited from the
   check's sweep; reproduces under neither the stated nor the widened predicate."*
3. **Preserve the walk's SHAPE, which is sound independent of the base** and is the note's real
   load: e.1–e.22 each carry conviction + cure + owner; e.12/e.13 are marked record-side; e.22's
   two contests are written either way and decided by no fold seat; and **X-W8 N-1 (bounds
   completeness) is the standing gate** that makes the delegated remainder checkable at close.
   The walk is re-stated as `<measured base> = 22 answered + <base − 22> delegated`, so it
   re-derives from the printed command rather than from an inherited number.

### C.3 — the `§ROUND-1` EXTERNAL receipt over-counts by one wave (AXIS 3/5)

**Defect.** Spine `:732-734` reports *"**9 EXTERNAL bookings** of the six §EXTERNAL identities
⟨INBOX I-28 · X-EXT-1..6⟩ (W0 · W1 · **W2** · W4 · W6 · W7 · W8 · W9 · W11 …)"*. `X-W2-FOLD.md:939-943`
declares the opposite in its own bytes: *"### 7.3 The I-28 external — zero X-EXT rows route here"*
… *"**None routes to X-W2**, and this seat mints no X-EXT identity (those six are the spine's;
anti-rename binds both ways)."*

**Cure.** Restate as **8 EXTERNAL bookings**, striking `W2` from the wave list. The true booking
set, id-for-id:

| wave | identity | anchor |
|---|---|---|
| W0 | X-EXT-6 | the repin's own install step (X-W0.j) |
| W1 | X-EXT-5 | R55 |
| W4 | X-EXT-4 | W4·173 |
| W6 | X-EXT-3 | W6·508 |
| W7 | X-EXT-1 | recipe home |
| W8 | X-EXT-2 | demo/ui barrel surface |
| W9 | X-EXT-6 (pointer) | W9.46 |
| W11 | X-EXT-5 | §6.4 |

Add the citation `⟨X-W2-FOLD.md:939-943⟩` beside the corrected figure so the contradiction is
closed **by pointer**, not by silent renumbering. Everything else on axis 3 holds whole and is
recorded as holding: all six families present with real mail provenance, routed to real waves,
mirrored id-for-id at `ROUND-1-ASSIGNMENTS` §C, `grep -rl 'X-EXT' registry/adjudicated/` → **0**
(zero registry-id contamination).

### C.4 — the short-spelled roster identity (AXIS 2)

**Defect.** `ROUND-1-ASSIGNMENTS.md:83` prints `| wb-mix-configbar.md | MC-15(:61), MC-22(:68),
MC-23(:69), **L-R6**ᵇ(:33) |`. The banked id at `wb-mix-configbar.md:33` is **`L-R6-4`**
(*"| **L-R6-4 drift sub-claim** | LC kill: exported surface symbol-identical | …"*). The same file
legislates *"zero-padded ids are their own identity: `D-06` ≠ `D-6`"* — a short-spelling is the
same class of violation.

**Cure — an erratum here, not a patch there.** `ROUND-1-ASSIGNMENTS.md` is a **round-1 census
record**; under E-3 (addenda, never patch) its bytes stand. This file registers the correction:

> **ERRATUM R1-A-1 (2026-08-28).** `ROUND-1-ASSIGNMENTS.md:83` spells the identity `L-R6`. The
> banked id is **`L-R6-4`** ⟨wb-mix-configbar.md · L-R6-4 (:33)⟩. Read every occurrence of `L-R6`
> in that row as `L-R6-4`. **No row is lost:** X-W6-FOLD.md:1190 already books
> `| W6·499 | **L-R6-4** (drift sub-claim) | wb-mix-configbar | INFO |`, so this is a spelling
> slip in the work order, not an escaped identity. With it counted as carried, roster→fold
> agreement is **73/73 id-for-id** — the one part of axis 2 that holds clean.

The X-W6 seat cites `L-R6-4` and this erratum; no re-booking, no `≡`-pointer needed.

### C.5 — the FIFTH may-not-be-cited edict, absent from the fold layer entirely (AXIS 4)

**Defect.** `§3 d′`'s framing — *"the four the check found absent from the fold layer entirely"* —
closes a register a fresh corpus sweep does not close.

**This seat's sweep (enumerative, so d′ closes by enumeration rather than by assertion):**

```
cd docs/tranches/V/megatranche/registry/adjudicated
ls *.md | grep -vE '^(kf|fr|pt)-' | xargs grep -nEi 'may (not|never) be cited|uncitable|may not cite'
```

**The fifth — register as `d′.5`:**

- **SAFARI-EVIDENCE (provenance bar)** — ⟨ColorPicker.md · :164⟩: *"**C-19 attribution** only from
  the production-build capture named in the bank; the dev-server witness is structurally blind
  (L-12); the `safari-*` shot directories are playwright-webkit and **may not be cited as
  Safari-app evidence** (I-20)."*
  `grep -rn 'Safari-app evidence\|playwright-webkit and may not' docs/tranches/X/refinement/
  docs/tranches/X/REFINEMENT-FOLD-2026-08-28.md` → **zero hits**.
  **Binding note (state it, do not re-home it):** this is an **evidence-provenance** bar, not a
  wave-binding edict. Its natural consumer is **SS-8's real-Safari matrix**, not an X wave — so
  d′.5 is registered with the explicit statement *"binds no X wave's cure; binds every seat that
  cites a `safari-*` capture, X or SS-8."* The register's coverage of **wave-binding** edicts is
  otherwise intact, and the sweep's carried counter-example proves it: ⟨AboutPane.md · AB-20
  (:72)⟩'s polite-region prohibition **is** carried, at `X-W5-FOLD.md:74` W5F-27, with
  *"≡ `AdminListSkeleton` (LOCK source)"*.

**A SIXTH, this seat's own find (supplement, so round 3 does not re-discover it) — register as
`d′.6`:**

- **DOCK-BANNER (claim bar)** — ⟨Dock.md · :145⟩: *"**The r1 challenge file's Pass-2 banner
  (C-15..C-22) — UNCITABLE** until recovered (C-30, L-13): the body is absent from the committed
  file; the root attempts journal recovery before any re-run; **no close may cite C-15..C-22**."*
  `grep -rn 'C-15\.\.C-22\|Pass-2 banner\|r1 banner' docs/tranches/X/refinement/
  docs/tranches/X/REFINEMENT-FOLD-2026-08-28.md` → **zero hits**. This one **does** bind waves —
  X-W8 (MT-DOCK-LAYERS-1) and X-W4/X-W5's dock seats all cite Dock rows — so it is a genuine
  wave-binding edict the round-1 register missed, found by the same sweep that found d′.5.
  Recorded here under the round-1 supplement idiom (`ˢ`), assigned to no wave: it is a citation
  bar, registered at the spine, binding every seat.

**Cure.** `§3 d′`'s header sentence changes from *"the four the check found absent"* to a stated
enumeration: *"the may-not-be-cited edicts absent from the fold layer, closed by corpus sweep
(command printed): **six** — four from the check, `d′.5` from the round-2 check, `d′.6` this
seat's."* Print the sweep command inside `d′` so the register is re-derivable rather than
asserted — the same discipline §D writes into the census.

---

## §D. The CENSUS METHOD LAW — to be written into the spine

*Proposed home: `REFINEMENT-FOLD-2026-08-28.md` §5 (the reading law), as a numbered clause
alongside §5.1's row-authority rule. It is a **method** law, not a row, so it binds every seat and
every check without touching any wave's bounds.*

> **§5.n — CENSUS METHOD LAW (round 2, 2026-08-28).**
>
> **1. THE PREDICATE.** A row line that contains an `X[-·.]W\d` token — **in any phrasing, at any
> position on the line** — **BOOKS the row** to that wave. There is no proximity requirement, no
> marker-verb requirement, and no position requirement.
>
> **2. THE RETIRED HEURISTIC.** X-W7-FOLD §R1's *"the wave token within 30 characters after a
> routing marker (`→ · Disposition · BUILD · BOOK · Terminal · routes to · re-homed · rides ·
> owns/owner · | **`)"* is **RETIRED**, and with it every derivative of it — including
> X-W4-FOLD §6E's `grep -E '→|Disposition|BOOK|BUILD|routes to|Terminal:'` third stage (§B.2).
> Rows routed by bare declarative phrasing are invisible to it and were the entire BLOCKER:
> *"**X-W7 rider else boundary.**"* (×6, FlagReportDialog) · *"carried in the **X-W4** family"*
> (wb-extract-controls K-H) · *"dies whole with **X-W7.d**"* (PaletteCardMeta PM-21) · *"the
> **X-W7.c** seat spec"* (SwatchHoverMenu K-6) · *"**X.W7 G20** measures exactly this"*
> (PaletteCardSkeleton U-6) · *"per … **X-W4's** accname law"* (wb-extract-workbench K-21). Any
> fold, gate or check still carrying the heuristic is **NON-CONFORMANT on its face**; it does not
> need a counter-example to be struck.
>
> **3. THE TOKEN ALPHABET IS CLOSED — hyphen and dotted ONLY.** Measured over the 92-record
> corpus at HEAD, read-only:
> ```
> ls *.md | grep -vE '^(kf|fr|pt)-' | xargs grep -oE 'X[–—‑]W[0-9]' | wc -l   → 0
> ```
> **No en-dash (`X–W`), em-dash (`X—W`) or non-breaking-hyphen (`X‑W`) form exists in the
> corpus.** The alphabet is therefore exactly `X-W\d` · `X.W\d` · `X·W\d`, and the canonical
> character class is **`X[-·.]W\d`**. A sweep that adds dash variants is not more careful, it is
> unfalsifiable padding; a sweep that omits the middle-dot or the dotted form **loses rows**
> (⟨shell-dock-genericactionbar · U-1⟩ = `X.W4.d`, ⟨PaletteCardSkeleton · U-6⟩ = `X.W7 G20`).
> Re-measure this clause, do not inherit it: if a future record introduces a dash form, the count
> stops being 0 and the class widens **by measurement**.
>
> **4. THE BOUNDARY IS MANDATORY.** Match `X[-·.]W(\d+)([^0-9]|$)`. Without the trailing
> boundary, `X-W1` swallows `X-W10`/`X-W11` and every per-wave census silently over-counts.
> Zero-padding and case are identity (`D-06` ≠ `D-6`; `ADJ-M1` ≠ `ADJ-m1`).
>
> **5. NWO AND ROUTED ARE LIMBS, NOT PARTITIONS.** A row may carry both a `NO-WAVE-OWNER` limb and
> a wave token; clause 1 governs the booking, the NWO limb rides as a stated cross-edge, and the
> row **folds once, both limbs**. Round 2 measured exactly one such row among the 46 NWO escapes
> (⟨PaginationBar.md · M7⟩, §A.6). A census that treats the two registers as disjoint will
> re-orphan every dual-limb row it meets.
>
> **6. THE COUNT FOLLOWS THE PREDICATE, NEVER THE REVERSE.** Every census figure printed anywhere
> in the fold layer must be accompanied by the **exact command that produces it**, and that
> command must reproduce **at the tree, verbatim, at a named HEAD**. A figure that reproduces under
> no predicate (§B.2's `282`) is struck on discovery, not reconciled. Changing the predicate
> **obliges** re-deriving every figure downstream of it — a corrected predicate with an
> uncorrected count is the same defect wearing the cure's clothes.
>
> **7. THE ROW PREDICATE IS OPEN AND MUST BE SETTLED (round 3).** Clauses 1–6 govern the **token**
> predicate. The **row** predicate — what counts as a bold-leading row line — is **not yet
> settled** and the divergence is recorded here rather than papered over: the round-2 check
> measures **3,059** bold-leading rows / **1,290** X-W-token rows / **321** NWO rows, while this
> seat, using X-W4-FOLD §6E's own row regex `^\s*([-*]|\|)\s*\*\*` over the same 92 records,
> measures **4,689 / 1,941 / 442** (`^(- |\| )\*\*` gives the same three figures; `^(- |\* )\*\*`
> gives 3,830 rows). **The token law is unaffected** — every escape in the pass-2 receipt is
> contained in both row sets — but no per-wave *total* may be quoted as authoritative until round
> 3 elects one row regex and prints it. Until then a census states its row regex inline or states
> nothing.
>
> **Law-true per-wave figures at HEAD** (row regex `^\s*([-*]|\|)\s*\*\*`, token class
> `X[-·.]W\d([^0-9]|$)`, 92-record corpus — printed so clause 6 has a baseline to re-derive
> against, and labelled with its row regex per clause 7):
>
> | wave | rows | wave | rows |
> |---|---|---|---|
> | X-W0 | 3 | X-W6 | 389 |
> | X-W1 | 101 | X-W7 | 653 |
> | X-W2 | 11 | X-W8 | 271 |
> | X-W3 | 22 | X-W9 | 73 |
> | X-W4 | **305** | X-W10 | 264 |
> | X-W5 | 153 | X-W11 | 1 |
>
> Total token-carrying rows **1,941**; NWO-carrying rows **442**.

---

— end. Consumed by the eight seats named in §A (X-W4 · X-W5 · X-W6 · X-W7 · X-W8 · X-W10 · X-W11 ·
X-W0's G-F register) plus the SPINE seat for §B.1, §C.1, §C.2, §C.3, §C.5 and §D, and the X-W4 seat
for §B.2. Adoption is by **dated fold addendum**; dedupe by banked identity; an `≡`-pointer row
closes the byte census, a silent skip does not. `waves/W0.md..W11.md`,
`waves/CONFORMANCE-2026-08-03.md` and `refinement/ROUND-1-ASSIGNMENTS.md` stay byte-untouched
(E-3). Round 3 measures against these bytes.
