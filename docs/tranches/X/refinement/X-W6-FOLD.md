SERVED MODEL ID (fold seat, line-1 declaration): claude-opus-5[1m]

# X-W6 REFINEMENT FOLD — the one dated addendum to `waves/W6.md`

*Instruments and temporal scenes · fold sealed 2026-08-28 · status of the wave: unchanged, **planned***

## 0. Header — provenance, posture, reading rule

**Provenance.** The value.js corpus = every record in
`docs/tranches/V/megatranche/registry/adjudicated/` that is not `kf-*` / `fr-*` / `pt-*`
(88 components + `parser-band` / `library-band` / `chassis-fitness` / `layout-gestalt`).
This seat read the **29 assigned slug records** whole at their routing joints and additionally
**grepped the entire corpus** for `X-W6` and its dotted / unit forms (`X.W6`, `X-W6.a … .j`,
`X.W6.a … .j`) and for the CC ids W6 owns (`CC-005`, `CC-056 … CC-068`).

The whole-corpus grep (`grep -rlE 'X[-.]W6'`) returns hits in **exactly 29 files — the 29 assigned
slugs and no others** (re-run 2026-08-28 by this seat; **correction to this fold's first pass,
which wrote "27 files"**). Two of the 29 carry a *single* token that books no defect row:
`PaletteRenameInput.md` (head-matter wave-vocabulary legend only) and `MiniColorPicker.md` (one
conditional token, MCP-35, folded at `W6·391`). **No record outside the assignment routes to
X-W6** — the assignment is closed under the grep, which is the fold's completeness receipt on the
IN side. A third record the
assignment did **not** name, `GradientStopEditor.md`, contains **zero** `X-W6` strings and is
nevertheless the *origin* of units `.a/.b/.c` — it routes to the pre-X names
**`W-GRADSTOP-1/2/3`**, which `CC-058` folded into `X.W6.a/.b/.c`; its rows are already the
verbatim text of gates a1–a13 / b1–b4 / c1–c4. It is folded on **file ownership and gate
provenance**, not on a token match (§0a n.1).

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0..W11.md` and
`CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is the fold's sole write. Where a row
below strikes or corrects a phrase in the dated spec, the strike is recorded here and the spec's
bytes stand untouched. Nothing in this fold opens product source: no `demo/**`, `src/**`, `api/**`,
`test/**` or `e2e/**` byte was written by this seat. Every measurement below is a read.

**Reading rule.** At execution X-W6 reads **`waves/W6.md` PLUS this addendum**, in that order;
where the two disagree the addendum's clause governs. This addendum does **not** re-stage,
re-weight or re-composite W6's 50 acceptance gates (46 born-RED + j4 MEASURE-AT-OPEN + H3/H4
procedural). It sharpens predicates, adds corpus rows and riders, names bounds corrections,
records exclusions and dissents, and proposes new born-RED gates **as candidates for the wave's
own opening sitting** — a proposal here is not a ratification.

**Anti-rename (M-25).** Every row is keyed by its **banked identity** — the id the adjudicating
record minted, original for life. `W6·n` indices are a fold-local counting device only and are
**never** a rename. Where two or more records booked ONE identity the row carries all sources on
one line and is counted **once** (`≡`). Where a record cross-cites another's row without
re-booking it, the citation is carried as provenance, never as a second row. Transcription alone
is defective: every row below carries the record's own cure-shape lock, sequencing edict or
dissent where one exists, verbatim where it is load-bearing.

**Fold classes.** **cl.1** = CARRY (banked row whose route already names X-W6; folded by identity,
never re-minted) · **cl.2** = LOCK (binding cure-shape lock, sequencing edict or correction that
constrains how the cure may land) · **cl.3** = BOUNDS (execution impossible until §3 adopts a file
or corrects a path — a **precondition**, not a route) · **cl.4** = EDGE (the row's other half lives
in another wave, at the producer, or NO-WAVE-OWNER; booked here only as an edge).

**Unit assignment.** Where the adjudicating record names the unit (`X-W6.j`, `X.W6.i`, …) the row
sits in that unit's section. Where the record names only `X-W6`, this fold assigns the unit from
the row's file and marks the assignment **†** — the assignment is this seat's, not the record's,
and is falsifiable against §4 of the spec.

**Arithmetic — countable from this file's own bytes.** **464 rows** booked in §1, contiguously
numbered `W6·1 … W6·464`:
unit **.a** ×21 (W6·1–21) · **.b** ×34 (22–55) · **.c** ×26 (56–81) · **.d** ×67 (82–148) ·
**.e** ×3 (149–151) · **.f** ×30 (152–181) · **.g** ×4 (182–185) · **.h** ×40 (186–225) ·
**.i** ×12 (226–237) · **.j** ×96 (238–333) · proposed **.k** ×34 (334–367) ·
proposed **.l** ×20 (368–387) · wave-level / provenance / edges ×10 (388–397) ·
**`.j`'s two ADOPTION TAILS ×67** — §1j·4 MX-CLUSTER ×38 (398–435) and §1j·5 PREVIEW-CHIPS ×29
(436–464), both added 2026-08-28 as the recorded M-25 repair described at their own heads.
**§2 carries 80 gate entries** — **48 sharpenings of EXISTING W6 gates** (§2a) and **32 genuinely
NEW born-RED candidates** (§2b, `N1…N32`). **§3 names 37 bounds rows**: **34 file adds** (33
`create`/`modify`, 1 `execute-no-write`) + **2 path errata** + **1 oracle-id collision family** —
**36 distinct paths**. **§4 names 22 cross-edges.** **§5 records 17 dissents, corrections and
exclusion classes.**

**Recorded correction to this fold's own first pass (M-25, dated 2026-08-28).** The first pass
booked, from the MX-CLUSTER and PREVIEW-CHIPS packets, only those rows whose adjudicating sentence
*already* named `X-W6`, and left the rest — every `NO-WAVE-OWNER (MX-CLUSTER)` and
`chip-family residue` row in the very files §3 asks W6 to adopt — in §5's exclusion prose. That is
the transcription-only defect M-25 names: **a bounds adoption transfers the whole file's docket,
not the subset already spelled `X-W6`.** §1j·4 and §1j·5 book those 67 rows under their banked
ids, each `cl.3`, each with its named degradation if the bounds edit is refused. The first pass's
`397`/`77`/`35` figures are superseded by the line above; nothing above them was rewritten.

The IN side is a **receipt**, not document-internal arithmetic: 27 records, 4 proposed packets,
one whole-corpus grep. It is falsifiable against the corpus; the OUT side is falsifiable against
these bytes. **No in-equals-out identity is asserted** — many corpus hits are head-matter routing
legends, instrument blocks and closing verdicts that book nothing.

### 0a. Corrections recorded, never silent

1. **`GradientStopEditor.md` routes to `W-GRADSTOP-1/2/3`, not to `X-W6`.** Verbatim from the
   record: *"BUILD → W-GRADSTOP-2 'the §7 recomposition: seat rail + selected-stop inspector +
   WatercolorDot faces + full keyboard grammar'"* and *"BUILD → W-GRADSTOP-3 'one sampling law, one
   literal dialect, leaf types, branded OrderedStops'"*. `CC-058` folded those three names into
   `X.W6.a/.b/.c`. The wave file already carries the consequence (its a1–a13 gate texts ARE that
   record's rows); this fold records the **rename edge** so no successor reads the absence of an
   `X-W6` token as absence of ownership. Anti-rename holds: the banked ids stay
   `W-GRADSTOP-*` / `D-*` / `G*`, and this fold cites them, never re-mints them.

2. **`wb-gradient-visualizer.md`'s own count is "X-W6 ×47"; this fold books 55 W6-touching GV
   rows. Both numbers are right and the difference is exactly enumerable.** Eight GV rows are
   **split halves** whose other half routes away from W6 — `GV-5` (+X-W8), `GV-10` (GLASS-OWNED,
   BH relay), `GV-14` (cross-feature fold NO-WAVE-OWNER), `GV-20` (+X-W1), `GV-30` (+X-W4
   coordination), `GV-39` (idiom half X-W8), `GV-51` (library halves X-W9-else-NO-WAVE-OWNER),
   `GV-64` (alias retirement elsewhere). **55 − 8 = 47.** The record counts primary routes; this
   fold counts touches, because a bounds table must contain a touch. Recorded, not smoothed.

3. **Three `W6.md` §4 File-Bounds paths do not exist in the tree** (measured 2026-08-28 by this
   seat; `git ls-tree c654824e` — W6's own measurement stamp — agrees):
   `demo/workbenches/gradient/GradientVisualizer/easing/GradientEasingEditor.vue` (real path:
   `…/GradientVisualizer/GradientEasingEditor.vue`, **no `easing/` segment**) ·
   `demo/picker/display/ColorNutritionLabel.vue` (real path: `demo/scenes/about/ColorNutritionLabel.vue`).
   The first erratum is **caught by the corpus itself** — `wb-gradient-easingeditor.md` states it
   in terms: *"W6.md:59 lists the subject file at `GradientVisualizer/easing/GradientEasingEditor.vue`
   — the file actually lives at `GradientVisualizer/GradientEasingEditor.vue`."* `GV-66`'s route is
   written **"→ X-W6.d (path-erratum precondition)"** — the record makes the erratum a gating
   condition of its own row. Both errata land in §3 as corrections; **H5** (§2) makes re-resolution
   a wave-level precondition under D-19.

4. **Three of the seven oracle files `W6.md` §4 marks `create` already exist under those numbers**
   at `c654824e`, the wave's own stamp: `e2e/smoke/oracles/o22-status-lamp.spec.ts`,
   `o26-aurora-perceptibility.spec.ts`, `o27-focus-affordance.spec.ts` — against W6's declared
   `o22-specimen-legibility.spec.ts`, `o26-atmosphere-coldload.spec.ts`, `o27-scene-contracts.spec.ts`.
   `o26-aurora-perceptibility.spec.ts` additionally overlaps the **subject** of W6's proposed
   `o25-atmosphere-response.spec.ts` (X:ATMO-1). A `create` on a taken number is either a silent
   clobber or a §3a bounds expansion; **H6** (§2) forces the disposition to be written before any
   `o2x` file is opened. Recorded as a collision, not a defect of any row.

5. **The `wb-gradient-stopeditor` probe directory holds FIVE scripts; §4 bounds FOUR.**
   `gate-axis.mjs · gate-gesture.mjs · gate-order.mts · gate-seat.mjs · gate-structure.mjs` are all
   present; §4 carries `gate-{axis,gesture,seat,structure}.mjs` as `execute, no write` and omits
   **`gate-order.mts`**. Under the wave's own **Bounds law** ("every script a gate invokes is
   bounded above… no invocation is exempted by prose") an order-invariant gate that runs
   `gate-order.mts` would run an unbounded instrument. §3 adds the row.

6. **H2's file-existence leg is now GREEN; its citation legs stay born-RED.**
   `docs/tranches/V/megatranche/audit/codex-provenance/motion-quarantine.md` **exists** (measured
   this session) — the 2026-08-03 addendum's note that the record landed at `9812f951` is
   confirmed. The gate is unchanged: the quarantine's FALSE rows (`M-01 · M-03 · M-17 · M-26 ·
   M-27 · M-29 · M-33`) may not be carried, `M-34`/`MQ-1` are TRUE and carryable, and no motion
   assertion ships unquarantined (`GV-73` rides this, `W6·137`).

7. **Two corpus records still prescribe the InstrumentChassis and are STALE against `CC-108/A-17`
   and W6 `H1`.** `chassis-fitness.md` names **Mix as "the first-contort CANARY"** for chassis
   adoption; `layout-gestalt.md` prescribes the same housing. Three adjudicated records have since
   killed the cure — `wb-mix-pane.md` **K-CHASSIS** ("kills the corpus's eleven-times-repeated
   chassis cure"), `wb-extract-pane.md` **K-9/XP-25** (*"the prescribed component is dead at the
   producer… X-W6 H1 gates against chassis shapes"*), and `AuroraPane.md` **R-7** (the dual-chassis
   contradiction, *"RESOLVED BY PROGRAM RULING"*). Under M-25 the stale prescriptions may not be
   silently overruled: **H7** (§2) requires a dated correction line in both records before `.j`
   opens. Booked as rows `W6·394` and `W6·395`.

8. **Two crash-weight BLOCKERs sit on a file W6 will own and route to NO wave.**
   `ColorNutritionLabel.md` books five BLOCKERs; two of them — **CNL-2** (two-click powerless-hue
   kill) and **CNL-3** (the 28-grey boot white-screen) — carry no `X-W6` token and no other wave
   name. The file itself is in W6 bounds (after correction n.3). They are folded at `W6·186`/`W6·187`
   as **NAMED GAP** rows: W6 either adopts them at its opening sitting or the formation boundary
   carries them by name. Ignoring them would be the M-25 defect.

9. **`BlobPane.md` raises a formation flag this fold cannot resolve and does not bury.**
   Verbatim shape: **`MT-CSP-1` is absent from the X ledger and `ConfigSliderPane.vue` is in no X
   wave's file table.** The file exists (`demo/scenes/ConfigSliderPane.vue`, measured) and five
   BlobPane rows discharge through its `G3`/`G8`/`G-PAINT` gates. §3 adds it to W6 bounds as the
   only wave whose rows need it; §4 carries the `X-W8 CC-105 ⇄ CSP G-PAINT` tension to the X-W0
   sitting.

10. **`AboutPane.md` rider A-1 names three phantom owners.** "About-recomposition", "MT-CSS-2" and
    "W-HYGIENE" are cited as owning wave names by rows in the corpus; none is an X wave. Every row
    that leaned on them is re-homed in §1f/§1g or marked NO-WAVE-OWNER here; the phantom names are
    recorded so a successor does not re-derive them as real.

11. **One cross-booking divergence is collapsed in writing, per the record's own instruction.**
    `picker-componentsliders-consolerail.md` books **MISS-10 → X-W10** while the parent record
    `picker-componentsliders.md` books the same mechanism as **R-28 → X-W6**; the consolerail
    record itself says *"the collapse must be written, not silent."* This fold collapses it to
    **R-28 at X-W6 (proposed unit .k, gate k5)** with MISS-10 carried as the second witness and
    X-W10 keeping the canon echo. Same treatment for **C-4**, whose identity exists in two ledgers
    (`CC-081 → X-W9` ∩ `CC-068 → X-W6`): cross-booked, cited both ways, **counted once** (`W6·372`).

12. **`PreviewRamp.md` ADJ-1 is a binding sequencing rule between two adjudicated records, not a
    defect row.** Two records order **opposite fates** for `[data-color-surface]` — GenerateControls'
    `G8` deletes it, PreviewRamp's seat work needs it. The rule is folded as a **cl.2 LOCK**
    (`W6·180`) and repeated at §4; it binds whichever wave lands second.

13. **Five bounds families are load-bearing for W6 and appear in NO W6 bounds row.** Four are the
    proposed packets (MX-CLUSTER · PREVIEW-CHIPS · GRADIENT-UNOWNED · CATALOG-LIE⇄CSS); this seat
    found **two more** by file-census: the **PICKER-SLIDERS band** (`ComponentSliders.vue`,
    `ConsoleRail.vue`, their two composables, `readoutReservation.ts`, `seat.css`) which takes 34
    routed rows including two BLOCKERs, and the **KATEX/MARKDOWN band**
    (`demo/scenes/about/katex/Katex.vue`, `demo/scenes/about/markdown/Markdown.vue`) which takes
    Katex's `R1` BLOCKER plus 19 rows. Both are folded as **proposed units .k and .l**, entirely
    **cl.3** — every row in them is bounds-gated and executes only if the wave adopts the files.

14. **The GRADIENT-UNOWNED packet's precondition clause is adopted verbatim and generalised.**
    `wb-gradient-visualizer.md`: *"the three X-W6 bounds corrections … remain **preconditions** on
    every X-W6 route below."* This fold restates it at wave scope: **no §1 row executes before §3
    lands.** That is `H5`'s second leg.

---

## 1. Rows

Columns: **#** (fold-local) · **ID** (banked, for life) · **Src** (adjudicating record; `≡` = one
identity, several witnesses) · **Sev** · **Mechanism** (one line) · **Cure-shape + locks**
(verbatim where load-bearing) · **Seq** (sequencing edict) · **Dissent**.
`†` = unit assignment by this fold, from the file, not from the record.

### 1a. Unit `.a` — the stop model, the order invariant, the gestures (Lane 1, serial)

*Provenance: `CC-058` folded `W-GRADSTOP-1` here; gates a1–a13 are `GradientStopEditor.md`'s rows
in the wave's own words. Every row below is `cl.3`-gated on §3 n.1–n.3 (GRADIENT-UNOWNED
precondition, adopted verbatim at §0a n.14).*

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·1 | **GV-1** ≡ L/R2-1 ≡ R2-1 | wb-gradient-visualizer (r2) | **BLOCKER** | Selecting a `back`-family specimen tile throws `color_progress_out_of_range` from render computeds and erases both panes — **7 throw doors**, one of them the producer's own default preset `ease-out-back` | Cure is the **codomain**, not the catalogue: sampling must not hand out-of-[0,1] progress to the colour path. Routes `.a`/`.c` for the model, `.d` for catalogue exposure; the **eased-mix library-capability** question is X-W9's, not W6's | **bounds precondition first** (§3) | — |
| W6·2 | **GV-2** ≡ C/R4-1 | wb-gradient-visualizer | **BLOCKER** | The validity oracle is not the render predicate: spec-valid `none` channels parse `ok:true`, `parseVerdict` clears, the pane dies through the same doors | One predicate for both: what the oracle accepts, the renderer must survive. `.a` + `.c`, same precondition | after W6·1's codomain fix | — |
| W6·3 | **GV-3** ≡ D/F-1 ≡ **A-5** (codeeditor D3-1/C-R17/C-R10) | wb-gradient-visualizer ≡ wb-gradient-codeeditor | **BLOCKER** | One debounced **byte-identical** keystroke in the CSS panel destroys every authored easing: no dirty check (`:59-62`), `onParseCSS` applies every emit, `applyCSS` swaps `intervals` wholesale | Model half `.a`/`.c`. **Editor-surface half (announce / confirm / cancel-on-deactivate) = NO-WAVE-OWNER** until `GradientCodeEditor.vue` is bounded (§3 n.4) | model half before editor half | — |
| W6·4 | **GV-41** ≡ C-5 ≡ **N-5** (strip, LC missed #3) ≡ GradientStopEditor D-2/G2c | wb-gradient-visualizer (PIN 5) ≡ wb-gradient-easingspecimenstrip | **BLOCKER** | Adding or removing a stop **silently re-hosts every authored easing curve onto a different interval** — length-only stop↔interval pairing | **`.a` is the named home**: stop-owned easing + the watcher's deletion are its mechanism. GRADSTOP-A **§14 lock: normalise-on-write and stop-owned easing land TOGETHER** (clamp BANNED, splice-at-insert BANNED) | with W6·5 in one edit | severity absent from r1 roster; r4 STANDS unclassed — resolved BLOCKER |
| W6·5 | **GV-42** ≡ C-6 ≡ R3-5 | wb-gradient-visualizer | MAJOR | The drag/keyboard position writer drops the ordering invariant — the component emits CSS its own parser rejects and the rail paints a lie | Gates **a1/a2 are this row's commands**; the **normalise-on-write mutator is its named cure**. §15: **no minimum-ordinal-spacing** | lands with W6·4 (§14) | — |
| W6·6 | **GV-44** ≡ C/R2-N1 | wb-gradient-visualizer | MAJOR | A **cancelled** pointer gesture COMMITS the add: `@pointercancel="onBarPointerUp"` (`GradientStopEditor.vue:208`) is bound to the committing handler | Gate **a7's post-cure clause names it verbatim**: *"a `pointercancel` in the dead-zone must mint NOTHING"* | — | — |
| W6·7 | **GV-7** | wb-gradient-visualizer (r1, re-anchored) | MAJOR | Reset does not reset | `.a` | — | — |
| W6·8 | **GV-8** | wb-gradient-visualizer (r1) | MAJOR | The CSS dialect cannot express space/hue/easing and blames the colour | **Cure note amended per the D/F-2-cure kill (ratified): the ` in <space> <hue> hue` clause carries space+hue; easing can NEVER ride the CSS string and needs its own persisted home.** `.c` + `.a` | before W6·3's editor half | r1's "one string that carries every field" KILLED |
| W6·9 | **GV-65** (reader-2 missed #2, promoted) | wb-gradient-visualizer | MAJOR | The radial round-trip **silently zeroes an authored direction** — `serializeGradient` pushes an angle only for linear | `.a` (parser) + `.c` (serializer); **the deeper cure is GV-45's** — retire or re-scope the control for radial | after W6·33 (GV-45) is decided | — |
| W6·10 | **GV-18** | wb-gradient-visualizer (r1) | MINOR | The `-1` sentinel vs authored negatives; `-0%` discontinuity | `.a`, branded `OrderedStops` (W-GRADSTOP-3's leaf types) | — | — |
| W6·11 | **GV-27** | wb-gradient-visualizer (r1) | INFO | Dead `CSS_COLOR_KEY` inject at the mount | `.a` | — | — |
| W6·12 | **GV-29** | wb-gradient-visualizer (r1, sharpened PIN 7 with D/F-15) | MINOR | Two byte-identical ID minters + the double selection write + the unbound public model | `.a`; **three channels** observation carried | pairs W6·36 (GV-49) | — |
| W6·13 | **GV-32** | wb-gradient-visualizer (r1, re-read) | MINOR | `touch-none` on the full-width rail eats vertical pane scroll; `pan-y` is free — gestures are `clientX`-only (`:75-81`/`:136-146`) | `.a` — one attribute | — | — |
| W6·14 | **GV-33** | wb-gradient-visualizer (r1) | MINOR | Single-vocabulary handle ring vs the file's own dual-contrast recipe | `.a`, **programmed by name** | — | — |
| W6·15 | **GV-22** | wb-gradient-visualizer (r1) | MAJOR | Coarse-pointer chip band overlap: a rail-bottom tap **removes** | `.b`; **gates a9/a10 name it** (probe cells `p4.mjs` iPhone-14, `p2.mjs`) | — | — |
| W6·16 | **GV-34** | wb-gradient-visualizer (r1) | MINOR | The remove chip straddles the section divider by 8px | `.b`; **gate a10** | with W6·15 | — |
| W6·17 | **GV-5** | wb-gradient-visualizer (r1) | MINOR | `LIBRARY_PORT_KEY` drags the auth/transport closure; **both bare sites are W6-owned** | `.a`/`.b` **+ X-W8** (cl.4 split) | — | — |
| W6·18 | **L3-1** ≡ **L-r1:L-2 / L-r2-2** | wb-gradient-easingspecimenstrip ≡ wb-gradient-easingeditor | MAJOR | The persisted domain model is typed by a widget's v-model payload: `GradientInterval = EasingPickerValue` (`useGradientModel.ts:12,:49`) | `.a`/`.d` coordination — the domain type is minted at `model/types.ts` (a `create` row already in §4) | before W6·4's stop-owned easing | — |
| W6·19 | **D3-08** ≡ C2-5(a) | wb-gradient-easingeditor | MAJOR | Deleting a stop while a later row is open leaves **ZERO open rows**: `openInterval = ref(0)` (`:61`) is never clamped | `.a`† — **the wave already programs it (`W6.md:159`)**; clamp on the *index*, not the position (GRADSTOP-A §14's clamp ban is about stop offsets, not accordion indices — recorded so the two are not confused) | with W6·4 | — |
| W6·20 | **D3-09** ≡ C2-5(b) ≡ D-09 | wb-gradient-easingeditor | MAJOR | Interval identity is **positional** while stop identity is not: `addStop` inserts position-sorted (`:116-120`), the watcher re-hosts | `.a`† — **programmed (`W6.md:159`)**; one identity for both, minted once | with W6·4 (same edit) | — |
| W6·21 | **M-GP-4** (DU) | wb-gradient-pane | MINOR | `selectedStopId` is never invalidated when `reset`/`seedFromPalette` replace the stop set — `setStopsFromColors` mints fresh `uid()` per stop | `.a`† | after W6·7 | — |

### 1b. Unit `.b` — the seat recomposition (Lane 1, serial, after `.a`)

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·22 | **GV-43** ≡ C-7 (narrowed per the phrasing kill) | wb-gradient-visualizer | MAJOR | The rail is a bare `<div>` — no role, no name, no tab stop; its primary creative act has **no DIRECT keyboard affordance** | **Gate b1 verbatim** | — | C axis's categorical *"no keyboard equivalent anywhere"* KILLED; the charge kept |
| W6·23 | **GV-49** ≡ C-13 (re-severed MAJOR) | wb-gradient-visualizer | MAJOR | Selection is a public model that leads nowhere — **no affordance in the app can edit a stop's colour** | `.b`; the **selected-stop inspector is its named home**; **gate b3's numeric entry generalises to colour entry** | after W6·12 | reader-1's severity argument adopted |
| W6·24 | **GV-70** (reader-1 missed #3, narrowed by K-20, PIN 4) | wb-gradient-visualizer | MAJOR | The add affordance is unsignified and unpreviewable on coarse pointers | `.b`; **gate b2's add-path recomposition** | — | tap-to-add IS operable (this seat's derivation) — the row is signification, not operability |
| W6·25 | **GV-38** | wb-gradient-visualizer (r1) | MINOR | The keyboard removal refusal at the floor returns **in silence** | `.b`; **gate b4** | — | — |
| W6·26 | **GV-47** ≡ D/D-12 + F-7 | wb-gradient-visualizer | MAJOR | One flat `flex flex-col gap-5` column of ten siblings — no stage, no inspector, no action region, so the canon's golden split has nothing to bind to | **`.j` gate j1** (Gradient owns its stage/inspector/action contract as a routed scene) **+ `.b`** | `.b` recomposition precedes `.j` adoption | — |
| W6·27 | **GV-48** ≡ D/D-11 + D-13 + D-14 (+D/F-14, merged PIN 7) | wb-gradient-visualizer | MAJOR | Protagonist inversion: the meniscus rail is a fixed 40px at any content volume while every support grows unbounded | **`.b`** — the seat recomposition *"frees the meniscus"* — **+ `.j`** | with W6·26 | — |
| W6·28 | **GV-19** | wb-gradient-visualizer (r1) | MAJOR | Hand-composed label+control pairs where `LabeledField` ships; `h-9` ×3; `grid-cols-3` with no responsive prefix | `.b`, via the **X-W4.b mechanism of record** (`size="sm"`) | after X-W4.b lands | — |
| W6·29 | **GV-57** ≡ D/F-3 surviving limbs + D/E-2 | wb-gradient-visualizer | MAJOR | The Direction slider is adopted as a bare primitive — no `aria-valuetext` on a unit-bearing angle, no numeric entry, no reservation | `.b`, **rides GV-19's LabeledField adoption** | with W6·28 | — |
| W6·30 | **GV-58** ≡ D/E-7 | wb-gradient-visualizer | MAJOR | The render tile's accessible name is a **constant** that never names what it paints (`role="img"` + a literal `aria-label`) | `.b` | — | — |
| W6·31 | **GV-56** ≡ D/F-13 | wb-gradient-visualizer | MAJOR | The `Interpolation` heading is wrong for half its contents — Type and Direction are geometry | `.b` — **the heading outline is `.b`'s named subject** | with W6·32 | — |
| W6·32 | **M-GP-3** (DU) | wb-gradient-pane | MAJOR | Heading-hierarchy violation: the pane's three section heads are **peers of their own pane title** | `.b` (h3→h4 in `GradientVisualizer`, a W6 file) under X-W10's law | with W6·31, one edit | grade ruled up from the axis reading |
| W6·33 | **GV-45** ≡ D/D-4 ≡ C-9 | wb-gradient-visualizer | MAJOR | The Direction axis stays live and operable for `radial` (where it changes nothing) and silently means "start angle" for `conic` | `.b` — retire or re-scope per type; **decides W6·9's deeper cure** | before W6·9 | — |
| W6·34 | **GV-46** ≡ C-10 | wb-gradient-visualizer | MAJOR | The Hue control is **inert in four of the nine offered spaces**, with no disabled state and no explanation | `.b`; **the space-metadata half (an `interpolatable`/cylindrical flag) rides the `.f` catalog + gate c4** | after `.f`'s catalog cut | — |
| W6·35 | **GV-67** (reader-1 missed #8, promoted) | wb-gradient-visualizer | MINOR | A circular quantity modelled as a clamped linear range with duplicate endpoints (`:min="0" :max="360"`) | `.b` | with W6·29 | — |
| W6·36 | **GV-54** ≡ D/D-6 | wb-gradient-visualizer | MINOR | Three raw `<hr class="border-border">` where the binding composition sets the Gradient row's divider count to **`none`** | `.b` | — | — |
| W6·37 | **GV-25** | wb-gradient-visualizer (r1) | MINOR | One `gap-5` does all rhythm work | `.b` under **X-W10's law** (cite, don't fork) | — | — |
| W6·38 | **GV-59** (reader-1 missed #5, promoted) | wb-gradient-visualizer | MINOR | **Six radius vocabularies inside one pane**, two of them card-scale on card-scale surfaces one column apart | Easing-panel half `.d` (**gate d1 is this row's oracle**); pane-surface half `.b` rider | `.d` half first | — |
| W6·39 | **GV-55** ≡ L3-5 | wb-gradient-visualizer | MINOR | One paint recipe in two scoped homes (`.gradient-render-tile` / `.gradient-rail`, five byte-identical declarations) + three inline `background` shorthands | `.c` (the two-site paint-stack dedup rider is named in its mechanism) **+ `.b`** | — | — |
| W6·40 | **GV-60** ≡ D/F-17 core ≡ L4-7 surviving half | wb-gradient-visualizer | MAJOR | `Seed from palette` is a named dock command with an **unnamed subject** and three silent failure paths | `.b` (+ the GV-16/X-W4 port typing removes the masking) | after X-W4's port typing | L4-7's "sole bare inject" KILLED (5 sites, 2 bare) |
| W6·41 | **C-3** | wb-gradient-pane | MAJOR | "Seed from palette" is a labelled, described, **permanently-enabled** dock command whose implementation cannot succeed | `.b`† (`GradientVisualizer.vue` in bounds) | one edit with W6·40 and W6·43 | — |
| W6·42 | **GAB-19** | shell-dock-genericactionbar | MINOR | Seed-from-palette is a **second, independent false affordance**; the agglomeration deletes it on UX grounds alone | **→ X-W6** (`GradientVisualizer` in bounds): *redesign-with-explicit-chooser **or** delete* — the decision is W6's and must be written | with W6·40/W6·41 | — |
| W6·43 | **C-4 / L-4** (+ DU's copy-feedback row FOLDED — same identity) | wb-gradient-pane | MAJOR | The pane's headline affordance — **Copy CSS**, from BOTH the dock action and the local control — has no result state | `.b`† | — | — |
| W6·44 | **GV-39** (re-severed INFO→MINOR, PIN 6) | wb-gradient-visualizer (r1) | MINOR | Two clipboard idioms one file apart AND the dock Copy has no result state while the sibling control ticks | **Idiom half X-W8; feedback half `.b`** (cl.4 split) | with W6·43 | — |
| W6·45 | **GV-30** | wb-gradient-visualizer (r1) | MINOR | Copy control named only by an **undeclared `title` fallthrough** | `.b` rider **+ X-W4 coordination** (cl.4 split) | with W6·43 | — |
| W6·46 | **GAB-18** | shell-dock-genericactionbar | MINOR | The route-local copy seats discard the producer's discriminated `Copy` result | `GradientVisualizer:128` → **X-W6 rider** (file in bounds); the census row is BANKED (B5) elsewhere — cross-cited, not re-booked | with W6·43 | — |
| W6·47 | **GAB-12** | shell-dock-genericactionbar | MINOR | The prescribed terminal seats ratify the **retired native-`title` register** | Owner X-W10 (register canon) **with a per-site note: the Gradient seat sits in X-W6's file bounds and may move there** — the move must be recorded, not assumed | decide at `.b` | — |
| W6·48 | **GV-10** | wb-gradient-visualizer (r1) | MINOR | `SelectionValue` is unexportable at the pin | **GLASS-OWNED → BH relay** + `.b` rider (cl.4) | producer reply gates the rider | — |
| W6·49 | **A-7** ≡ D-r3 §3 / D-3 / C-R7 (clipping) + C-R5 (newline fusion) | wb-gradient-codeeditor | MAJOR | The verdict `<p>` is the last child of a column inside `GradientPane.vue:20`'s `overflow-y-auto` — the rejection message clips | `.b`† — **cl.3: `GradientCodeEditor.vue` is bounds-new (§3 n.4)** | after §3 | — |
| W6·50 | **A-13** ≡ C-R21 + C-R22 | wb-gradient-codeeditor | MAJOR | Unbounded ingress into a bounded control: `100turn` → 36000deg with **no clamp anywhere** on the parse→model path | `.b`† (Direction axis owns the domain) — cl.3 | with W6·33 | — |
| W6·51 | **A-17** ≡ D-10 + reader-1 miss M-5 (sharpened) | wb-gradient-codeeditor | MINOR | The Copy control sits 40px from the field it acts on while the same pane's rail does the identical verb inside the field | `.b`† — cl.3 | with W6·43 | — |
| W6·52 | **A-21** (reader-1 miss M-4) | wb-gradient-codeeditor | MINOR | `glass-wash` and `overflow-y-auto` share one element while the producer's overlays are `absolute inset-0` children | `.b`† — cl.3 | — | — |
| W6·53 | **M-GP-5** (LC) | wb-gradient-pane | MINOR | `GradientVisualizer` declares a two-way `selectedStopId` model **no consumer binds** | `.b`† — dies with W6·23's inspector | with W6·23 | — |
| W6·54 | **M-GP-6** (DU) | wb-gradient-pane | MINOR | Reset destroys all authored state in one click — no confirm, no undo — wearing the same playful `rotateOnClick` spin as the **non-destructive** Regenerate | `.b`† (command semantics); **gate b4's precedent applies: one removal owner, the floor is an explicit disable** | — | — |
| W6·55 | **M-GP-7** (LC) | wb-gradient-pane | MINOR | **Zero test coverage** on the entire exposed surface | `.b`† — its gates already **ADD-never-replace** `o21-gradient-rail.spec.ts` (`W6.md:161/:186`) **+ X-W1 rider** (falsifier law) | — | — |

### 1c. Unit `.c` — one dialect, one sampling law, leaf types (Lane 1, serial, after `.b`)

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·56 | **GV-52** (reader-2 missed #1, promoted) | wb-gradient-visualizer | MAJOR | The specimen strip's **stated contract** — *"rides the SAME sampling law … shows the interval exactly as the gradient will render it"* — is false | `.c`; **gate c2's "one sampling law" equivalence is this row's oracle**; the one-`sampleAt` cure closes it | before `.d`'s tile work | — |
| W6·57 | **C-01** (+D-18/C-3/C-6; third seam folded; ≡ GV-24 cross-ref; blast radius ≡ GV-6) ≡ **R-A** (editor D2-01/C2-1/N-1/R3-1/C-1) | wb-gradient-easingspecimenstrip ≡ wb-gradient-easingeditor | **BLOCKER** | The lethal predicate is **`range(fn) ⊄ [0,1]`**, reachable through **three consumer seams including plain pointer hover** | `.c`/`.d`; **blast-radius limb X-W5** (single ErrorBoundary). Library-blameless: the cure is the consumer's codomain guard | with W6·1 (same predicate) | — |
| W6·58 | **GV-24** | wb-gradient-visualizer (r1) | MAJOR | The **sixth throw door**, `openIntervalRamp` — corpus-absent until r1 | `.c`/`.d` | with W6·57 | — |
| W6·59 | **A-1** ≡ D-1 / D-r2 §3.1 / C-R15 / L-1 + reader-2 miss M-A | wb-gradient-codeeditor | **BLOCKER** | `parseCssColor` is declared **total** (`ParseResult`, no throw channel) and throws on the closed empty-call class — **and the class is `<any-ident>()`, not `<color-function>()`** | Library half is **X-W9/Bank 1** (W6 may not touch `src/**` by its own Do-NOT-touch row); the demo-side guard is `.c`† — cl.3 | X-W9 Bank-1 sweep (H3) reads first | — |
| W6·60 | **A-3** ≡ D-2 / D-r2 §3.2 / L-3 | wb-gradient-codeeditor | **BLOCKER** | The declared `glass-wash` plate paints **nothing**: `hljs.css:56-59 .hljs{background:transparent}` is **unlayered** and beats `@layer components` | `.c`† — cl.3; the fix is layering, not a per-instance override (root-styling law) | after §3 | — |
| W6·61 | **A-4** ≡ M-1 ⊃ D2-1/C-R19 + C-R20 | wb-gradient-codeeditor | MAJOR | The editable readout is **unfaithful**: `serializeGradient` emits neither `in <space>` nor the hue method | `.c` — the ` in <space> <hue> hue` clause (W6·8's amended cure) | with W6·8 | — |
| W6·62 | **A-6** ≡ C-R4 + D-5 + D-6 | wb-gradient-codeeditor | MAJOR | The verdict lifecycle is broken in all three arms — `parseVerdict` written at only two sites; Direction/Space/Hue never touch it | `.c`† — cl.3 | with W6·2 | — |
| W6·63 | **A-8** ≡ C-R18 / L-16 (mechanism per ratified kills K2/K3) | wb-gradient-codeeditor | MAJOR | The 500ms timer is **never cancelled** — no `onUnmounted`/`onDeactivated`/`.cancel()` in 117 lines | `.c`† — cl.3; joins `.j`'s deactivation family (W6·303) | — | K2/K3 ratified: the leak framing narrowed to cancellation |
| W6·64 | **A-12** ≡ D-4 (per the ratified D-4-FRAMING kill) + reader-2 miss #4 + D-13/C-R11 + L-5 | wb-gradient-codeeditor | MAJOR | The highlighting **lies while the editor is used** — spans persist against edited text | `.c`† — cl.3 | — | D-4's original framing KILLED |
| W6·65 | **A-16** ≡ L-2 (+ ratified kill K5) | wb-gradient-codeeditor | MAJOR | The gradient grammar is **library-shaped code living in the demo** (301+334 lines; `grep gradient src/` = 0) and its justifying docblock is false | **X-W9 coordination else NO-WAVE-OWNER**; the demo-side one-dialect fold is `.c` | X-W9 decides first | K5 ratified |
| W6·66 | **GV-50** ≡ R2-3 | wb-gradient-visualizer | MAJOR | 635 lines of framework-free, library-grade gradient code live in `demo/`, and the library's own suite already votes them library code | **X-W9 coordination, else NO-WAVE-OWNER** — *"X-W6 may not touch `src/**` by its own Do-NOT-touch row"* (cl.4) | — | — |
| W6·67 | **GV-51** ≡ r1 L-3 | wb-gradient-visualizer | MAJOR | The `/css` ↔ `/easing` seam is missing **both** round-trip halves and this tree owns both re-implementations | Library halves **X-W9 else NO-WAVE-OWNER**; the **demo-side one-dialect fold is `.c`** (with W6·1) | — | — |
| W6·68 | **L-2** | wb-gradient-easingspecimenstrip | MAJOR | `/css` parses timing functions and cannot serialize them; the inverse is hand-rolled **three times across two packages** | **X-W9 coordination + `.d` consumer half**; `.c` owns the demo dialect | X-W9 first | — |
| W6·69 | **GV-11** | wb-gradient-visualizer (r1, re-verified `:22-27`) | MINOR | The re-export block preserves a surface nobody consumes AND omits the one live serializer | `.c` | — | — |
| W6·70 | **GV-13** | wb-gradient-visualizer (r1) | MINOR | Seven type-only import cycles; orchestrator-as-type-registry | `.c` — `model/types.ts` (a `create` row in §4) is the leaf | with W6·18 | — |
| W6·71 | **GV-14** | wb-gradient-visualizer (r1) | MINOR | One `Result`, six unwrap sites, **two policies** | Gradient half `.c`; **cross-feature fold NO-WAVE-OWNER rider** (cl.4) | — | — |
| W6·72 | **L3-5** (+ N-8 LC missed #6, the cheaper cure ADOPTED; ≡ first-seal M-ESS-7 — one identity) | wb-gradient-easingspecimenstrip | MINOR | The `Result`-unwrap is duplicated (`easingCatalogue.ts:98-104` / `useGradientCSS.ts:71-…`) | `.c` — one unwrap policy; **LC's cheaper cure adopted over the first-seal shape** | with W6·71 | — |
| W6·73 | **resolvedEasingCache** | wb-gradient-easingeditor | MINOR | Module-scope, **unbounded**, keyed by user-typed CSS (`useGradientCSS.ts:69`) | `.c`† — the in-repo precedent self-caps at 512 (`tintLCache`); adopt the cap, do not invent one | — | — |
| W6·74 | **GV-31** | wb-gradient-visualizer (r1) | MINOR | `railRampCSS` recomputes on a direction it **provably ignores** | `.c` | — | — |
| W6·75 | **GV-20** | wb-gradient-visualizer (r1) | MINOR | `easingFnOf`'s parse tail is reachable **only from the library's test** — a coverage defect | `.c` **+ X-W1** (cl.4 split) | — | — |
| W6·76 | **GV-64** ≡ L3-3 | wb-gradient-visualizer | MINOR | `PickerSpace` is an identity alias of `SpaceId` imported across a feature boundary | Consumer edge `.c` (the leaf `model/types.ts` homes the model's space type); **the alias's retirement is NOT W6's** (cl.4 split) | — | — |
| W6·77 | **A-18** (reader-1 miss M-6) | wb-gradient-codeeditor | MINOR | Five of fourteen rejection strings speak the implementation (*"isn't modeled"*); the most-hit string has neither plain language nor an example | `.c`† — cl.3; one voice for the verdict register | with W6·62 | — |
| W6·78 | **A-19** ≡ D3-6 / C-R13 | wb-gradient-codeeditor | MINOR | `spellcheck="false"` and nothing else — no `autocapitalize`/`autocorrect`/`inputmode`/`plaintext-only`/`@paste` | `.c`† — cl.3 | — | — |
| W6·79 | **A-20** ≡ C-R9 + C-R12 + K1's surviving residue | wb-gradient-codeeditor | MINOR | `modelValue` with **no** `update:modelValue` emit — v-model compiles and does nothing; `innerHTML` reassignment | `.c`† — cl.3 | — | K1 partially ratified; residue kept |
| W6·80 | **A-22** ≡ D2-5 | wb-gradient-codeeditor | MINOR | No `dir`/`unicode-bidi` isolation on a CSS-string surface | **→ X-W6** *"(one attribute in the rebuild)"* — cl.3 | — | — |
| W6·81 | **M-EE-7** (LC) | wb-gradient-easingeditor | INFO | The editable CSS surface is the one serialization that cannot carry the model's easing; **Apply re-seeds every interval to linear** | `.c` — dies with W6·8's persisted-easing home | with W6·8 | — |

### 1d. Unit `.d` — the easing catalogue, the specimen strip, the interval editor (Lane 1, serial)

*The **◆ NAMED GAP law** (`wb-gradient-easingspecimenstrip.md`, re-verified at its second seal)
governs this section: a ◆ row routes to X-W6 **only if §3 adopts `EasingSpecimenStrip.vue` /
`useSpecimenRows.ts`**; otherwise it degrades to **NO-WAVE-OWNER**, a named formation-boundary
carry. Every ◆ below is therefore `cl.3`. `wb-gradient-easingeditor.md`'s riders **R-A…R-J** are
**identity folds** onto the strip's rows — cited on the row, never counted twice.*

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·82 | **D3-02** ≡ D-01 | wb-gradient-easingeditor | **BLOCKER** | A focused **collapsed interval head paints zero focus pixels**; the open row keeps a ~4px bottom band only (clip-rect × zero-offset ring) | `.d`† — the ring must be drawn outside the clip; **NEW gate candidate N30 (§2b)** | before any `.d` visual row | — |
| W6·83 | **GV-23** ≡ **C-06 / L-1** ≡ **R-C** (C2-2 / L-r3:L-1) | wb-gradient-visualizer ≡ strip ≡ editor | MAJOR | `FAMILY_ORDER` (`easingCatalogue.ts:174`) is used as a **FILTER** (`:191`) and silently drops all six quart/quint presets — **27 of 33 tiles** (node census re-run) | `.d`; the catalogue's order must not be its membership test. **NEW gate candidate N-3** | — | reader escalated to BLOCKER; **MAJOR is the standing grade, both readers overruled** — dissent preserved (r-DU: "no axis in ten rounds found it") |
| W6·84 | **C-07** | wb-gradient-easingspecimenstrip | MAJOR | The generic `steps` tile is a **misreporting trap**: `tileIdFor` maps EVERY non-literal steps interval onto the one tile printed `n = 4` | `.d`; identity must be lossless or the tile must refuse | with W6·83 | — |
| W6·85 | **D-17** (carried, first seal) | wb-gradient-easingspecimenstrip | MAJOR ◆ | The `custom` state **has no rendering**: `tileIdFor` → null, the reveal early-returns, no chip pressed, while the row head prints `custom` | `.d` ◆ — cl.3; an authored state, not a hole | with W6·84 | — |
| W6·86 | **N-2** (DU missed #2) | wb-gradient-easingspecimenstrip | MAJOR | **Every tile press silently destroys the OTHER authoring mode's parameters**, and the licensing comment is false | `.d`, **coupled to L3-1** (W6·18) — the payload must carry both modes or the press must confirm | after W6·18 | — |
| W6·87 | **N-1** (DU missed #1) | wb-gradient-easingspecimenstrip | MAJOR | The D6 guard is applied at the **WRONG REFERENT**: one accent ink certified against ONE surface rung is painted on THREE other rungs | `.d`; **citation-inheritance class** — a certification may not be inherited across referents | before W6·95 | — |
| W6·88 | **C-05 / D-40** (+ D-40-forward-half) ≡ **R-B** (D2-03 / N-2·r3 / R3-2) | strip ≡ editor | MAJOR | The **zero-letterbox law is DEAD CODE**: `EasingAuthoringStage.vue:48-50` queries `svg[role='img']` — **0 elements in dist** (sixth independent run) | `.d` (files in W6's list) **+ one glass canvas-hook ask (BH relay)**; ~35 of 116 stage lines dead | glass reply gates the producer half only | — |
| W6·89 | **EAS-1** (Law 3 binds to zero elements) | wb-gradient-easingauthoringstage | MAJOR | Same mechanism from the stage's own seat: Law 3 binds to **zero** elements | **Identity-fold onto W6·88** — cited, not re-booked (anti-rename) | — | — |
| W6·90 | **EAS-2** (codomain kills the route) | wb-gradient-easingauthoringstage | **BLOCKER** | The codomain failure kills the `/easing` route from the stage side | **Identity-fold onto W6·57** (`range(fn) ⊄ [0,1]`) — cited, not re-booked | with W6·57 | — |
| W6·91 | **C-04 / D-38-mechanism** | wb-gradient-easingspecimenstrip | MAJOR | Twenty-seven in-plate tiles carry `.glass-capsule`'s unconditional `box-shadow: …, var(--glass-shadow-floating)` inside a card whose own elevation is flat | `.d` — floating shadow on a seated tile; **root-level, not per-instance** | — | — |
| W6·92 | **D-37 / C-02(a) / L-12 / R4-2-as-refined** | wb-gradient-easingspecimenstrip | MAJOR | **Four declared radius tokens resolving to six effective radii** | `.d` **under X-W10's law** (cite, don't fork) + the **fleet-census rider** (LC missed #8, INFO, adopted); **gate d1 is the oracle** (W6·38) | with W6·38 | — |
| W6·93 | **C-08 / R4-3 / D-24** (+ R4-3(a)/L-7 comment-falsity limb, MINOR, folded) ≡ **R-I** (D2-10's DEFECT) | strip ≡ editor | MAJOR ◆ | The strip's scroll port is a **nameless, roleless focusable tab stop** | `.d` ◆ (≡ **GV-35**, booked NO-WAVE-OWNER there — *"the ◆ resolves the same way"*) + the semantics half as a glass ask. **CURE KILLED at the editor (K-4): the producer already ships `ariaLabel`/`ariaLabelledby`** — consume it, do not build one | — | — |
| W6·94 | **L3-4** ≡ **R-G** (R3-6 / D2-09) | strip ≡ editor | MAJOR ◆ | A **27-way single-select hand-built from 27 independent boolean toggles** inside a bare `role="group"` while glass publishes `./toggle-group` at a real exports subpath | `.d` ◆ + glass `$attrs` ask | after §3 adopts the strip | — |
| W6·95 | **D-42** | wb-gradient-easingspecimenstrip | MAJOR ◆ | **Error / disabled / overflow-affordance / truncation / prefers-contrast never designed** | `.d` ◆ state enumeration; **the disabled sub-row keeps LC's caveat** | after W6·87 | LC's caveat preserved verbatim on the disabled arm |
| W6·96 | **D-29** | wb-gradient-easingspecimenstrip | MAJOR | **Two selection surfaces for one model in one open row** (strip + producer combobox) | `.d` seat half; **glass ask for a `:presets="false"` door, same channel as `:readout`/`:playback`** | glass reply gates the producer half | — |
| W6·97 | **D2-04 / L-r3:L-3** | wb-gradient-easingeditor | MAJOR | The disclosed stage **suppresses two of the producer's three surfaces** (`:readout="false" :playback="false"`) | **GLASS ask (BH relay) + `.d` consumer half** | — | — |
| W6·98 | **D-28** | wb-gradient-easingspecimenstrip | MAJOR ◆ | Twelve pixels of unowned space above the strip because `.strip-row` is `display:inline-flex` — the inline-level box generates a line box | `.d` ◆ — one display value | — | — |
| W6·99 | **D-41 / D-08** ≡ **D2-06a** (editor) | strip ≡ editor | MAJOR ◆ | The strip hand-rolls a **9px rung** (`0.5625rem`) below the house's smallest named rung (`--type-micro` 11px) | `.d` ◆ **under X-W10's type law** — the closed matrix decides, the consumer does not | — | — |
| W6·100 | **D-09** (carried, first seal) | wb-gradient-easingspecimenstrip | MAJOR ◆ | Bare polyline in an empty unit viewBox — no frame, baseline or axis; `steppedEase(1,"jump-start") ≡ 1` flattens `step-start`'s portrait | `.d` ◆ under X-W10's law | — | — |
| W6·101 | **D-04 / D-33** (carried, first seal) | wb-gradient-easingspecimenstrip | MAJOR ◆ | Seven `border-left`-family rules paint a grouping line in a composition whose binding inventory sets none | `.d` ◆ under X-W10's law | with W6·106 | — |
| W6·102 | **D-01-rescoped** (pass-1 framing KILLED — see register) | wb-gradient-easingspecimenstrip | MAJOR | The canon'd **`/easing` route** with the 19–22rem curve protagonist does not exist | `.d` (routed-scene criterion); **canon coupling X-W5**; the scene contract is `.j`'s | `.j` adopts after `.d` builds | pass-1 framing KILLED; **R-E records a READER-COVERAGE CORRECTION — r2 graded this BLOCKER and both re-readers passed it in total silence** |
| W6·103 | **N-3** (both readers) | wb-gradient-easingspecimenstrip | MAJOR ◆ | The reveal **parks the selected tile under the producer's fade feather** | `.d` ◆ (≡ first-seal **M-ESS-1**, elevated; the inertness caveat settled) | with W6·98 | — |
| W6·104 | **N-7** (DU missed #5) | wb-gradient-easingspecimenstrip | MAJOR | The catalogue's tile **ORDER is inconsistent and the eyebrow mislabels**: `cubic` is the only family not reading `in / out / in-out` | `.d` — one ordering law for all families | with W6·83 | — |
| W6·105 | **C-17 / D-03** (MINOR-while-C-03-stands) | wb-gradient-easingspecimenstrip | MINOR ◆ | Hover cancels one of four selected-register declarations at **equal specificity by source order**; no `(hover:hover)` guard; sticky on touch | `.d` ◆ | — | severity conditional on C-03 standing — recorded |
| W6·106 | **D-05 / D-34** | wb-gradient-easingspecimenstrip | MINOR ◆ | Physical `border-left`/`padding-left` in a direction-reversing row | **Ours `.d` ◆; the producer mask half is GLASS-OWNED (bank with M3)** | with W6·101 | — |
| W6·107 | **C-09 / D-25** ≡ **R-F** (R3-5) | strip ≡ editor | MINOR ◆ | WCAG 2.5.3 fails on **exactly 1 of 27**: `:aria-label="tile.id"` vs visible `n = 4` | `.d` ◆ — one pair, not a class | — | both readers enumerated all 27 independently; the count is 1 |
| W6·108 | **C-10 / D-20 / L-6** ≡ **R-H** (D2-05 / L-r3:L-4) | strip ≡ editor | MINOR ◆ | `shape="cell"` bought then fought (the radius-less SHAPE.cell map, d.ts-verified) | **Glass specimen-rung ask (BH) + `.d` ◆ consumer half** | glass reply gates the ask | — |
| W6·109 | **C-11 / D-23 / L-4** ≡ **D3-11 / R3-4 / C2-4 / D2-14** ≡ **GV-68** | strip ≡ editor ≡ visualizer | MINOR→MAJOR | **One full 27-Chip strip + a `useMediaQuery` listener mounted per interval forever** (`v-for` → `v-show`), plus a live `EasingPicker` per row | `.d` — mount on demand; the ramp is already `v-if`-gated, the strip is not | after W6·85 | GV books it MAJOR (missed #4, promoted); strip books MINOR — **the higher grade governs, the lower is preserved** |
| W6·110 | **L3-3 (strip)** — *note: distinct from `L3-3` in `wb-gradient-visualizer` (W6·76); ids collide across records, both stay* | wb-gradient-easingspecimenstrip | MINOR | `useSafeAccentFn` reaches boot via `inject(INK_AMBIENT_KEY)!` while two sibling consumers inject the same key null-tolerantly | `.d` — one discipline | — | — |
| W6·111 | **L3-6** | wb-gradient-easingspecimenstrip | MINOR | One union, **two exported names** (`JumpTerm` vs value.js's `JumpPosition`), paid with a no-op cast | `.d` + glass ask | — | — |
| W6·112 | **GV-72** (reader-2 missed #5, promoted) | wb-gradient-visualizer | MINOR | `easingCatalogue.ts:136` casts `term as JumpPosition` **across a producer boundary that needs no cast** — glass DERIVES `JumpTerm` from value.js | **Identity-adjacent to W6·111**; both die in one edit | with W6·111 | — |
| W6·113 | **L3-8** | wb-gradient-easingspecimenstrip | MINOR | The strip's event payload carries a **factory closure** (`payload: () => EasingPickerValue`), typing the parent's contract by the catalogue's internal shape | `.d` | with W6·18 | — |
| W6·114 | **L-9** | wb-gradient-easingspecimenstrip | MINOR | `isStepsInterval` **accepts** a `mode`/`css` disagreement instead of forbidding it, against the module's own `css`-is-truth law | `.d` | — | — |
| W6·115 | **C-14** | wb-gradient-easingspecimenstrip | INFO | `bezierLiteral` destructures four of an **unbounded** array (a 3-element input emits `undefined` into CSS text); `stepsLiteral` accepts `n ≤ 0` | `.d`; **gate c3's literal dialect is the falsifier** | with W6·116 | — |
| W6·116 | **M-ESS-9** | wb-gradient-easingspecimenstrip | INFO | `bezierLiteral`/`stepsLiteral`/`SpecimenFamily` exported with **zero external consumers** | `.d`, **sequencing-coupled to X-W9** (subsumes C-14's disposition) | X-W9 dialect decision first | — |
| W6·117 | **C-13** | wb-gradient-easingspecimenstrip | INFO ◆ | `.specimen-strip` applied once, styled by nothing — **yet it is every axis's probe selector** | `.d` ◆; **PCS-22 hook law: land a stable hook BEFORE any ARIA/class cleanup** | **PCS-22 ordering is binding** | ≡ **R-J**: filed by DU as corpus-novel; **already ADJUDICATED in the registry** — reader correction recorded |
| W6·118 | **M-ESS-8** | wb-gradient-easingspecimenstrip | MINOR | The catalogue's failure mode is an **IMPORT-TIME throw** (`SPECIMEN_FAMILIES = buildFamilies()` at module eval) that escapes any pane boundary | `.d` | with W6·57 | — |
| W6·119 | **GV-36** | wb-gradient-visualizer (r1) | MINOR | Module-eval catalogue build resting on a contract value.js does not make | `.d`; **K-18's consequence-kill stands** | with W6·118 | K-18 ratified — the consequence claim is dead, the mechanism is not |
| W6·120 | **M-ESS-3** ≡ **R3-7 + C2-7** (editor, sharpening) | strip ≡ editor | MINOR | The copy control **confirms by mutating its own accessible name**; no live region; `useClipboard` publishes `invalidate` and the seat never calls it | `.d` — one row, the editor's sharpening attached | with W6·43 | — |
| W6·121 | **M-ESS-4** | wb-gradient-easingspecimenstrip | MINOR ◆ | The `.strip-row` padding comment names a **mask-clearance mechanism content padding cannot reach** (the mask is PORT-anchored) | `.d` ◆ — correct the comment or move the clearance | with W6·103 | — |
| W6·122 | **M-ESS-10** | wb-gradient-easingspecimenstrip | INFO | The host docblock's instantiation law is falsified by its own template (*"v-show, never v-if"* at `GradientEasingEditor.vue:17` vs `v-if` at `:208`) | `.d` — the prose dies or the template does | with W6·109 | — |
| W6·123 | **N-9** (LC missed #7) | wb-gradient-easingspecimenstrip | MINOR | `SpecimenRow.name` is derivable from `tileId` — every row runs the 27-entry scan **twice** | `.d` | with W6·84 | — |
| W6·124 | **C-12** (first-seal roster, re-affirmed by LC) ≡ **R-D** (N-3·r3 / R3-3) | strip ≡ editor | MAJOR | The entire unit gate is `> 20`-of-27 + a NaN-substring — **7 tiles of slack** (`test/gradient-v4-consume.test.ts:55-57`) | **SPLIT: o17 re-key + falsifier → X-W1; born-RED unit invariants → `.d`** (cl.4) | X-W1 re-key first | **K-7 kills r1's version**; the split is the surviving shape |
| W6·125 | **GV-37** | wb-gradient-visualizer (r1) | MINOR | `tuneOpen` is accumulate-only positional disclosure state | `.d` | with W6·19 | — |
| W6·126 | **GV-66** (reader-1 missed #7, promoted) | wb-gradient-visualizer | MINOR | The accordion's default-open contract silently dies — `openInterval` is a positional index never reconciled with the interval count | `.d` **(path-erratum precondition — the record's own route text)** | **§3 n.2 lands first** | — |
| W6·127 | **GV-62** ≡ r1 L-13 | wb-gradient-visualizer | MINOR | Two bare `<button class="rail-btn">` with a locally-authored ghost-icon-button recipe — a **third** implementation | `.d` — *the one-input-register goal*; **gate d2's field-primitive decision governs the readout row these buttons sit in** | with W6·128 | — |
| W6·128 | **D-05 (editor)** — *note: id collides with the strip's `D-05` (W6·106); both stay, per anti-rename* | wb-gradient-easingeditor | MAJOR | **Two** hand-rolled disclosures and **two** hand-rolled ghost icon buttons with bespoke hover wash, radius and transition | `.d`† — one disclosure primitive, one icon-button | with W6·127 | — |
| W6·129 | **D3-01** | wb-gradient-easingeditor | MAJOR | The curve name and ordinal carry `text-mono-small text-muted-foreground` on the raw transparent head | `.d`† | after W6·82 | **BLOCKER dissent preserved with a revival clause** — if the focus cure does not raise the register, the grade returns |
| W6·130 | **D3-04 + D3-05 / OM-4** | wb-gradient-easingeditor | MAJOR | **Proximity inversion**: 8px between sibling rows vs 10px between one row's own parts | `.d`† under X-W10's law | — | — |
| W6·131 | **D3-06 / OM-13** | wb-gradient-easingeditor | MAJOR | `bg-well` (an opaque `--card`-derived tone step) seated over the transparent stage | `.d`† consumer half **+ GLASS ask (BH relay)** | — | — |
| W6·132 | **D3-07 + D-04 (editor)** | wb-gradient-easingeditor | MAJOR | The row's ONE literal **truncates at BOTH viewport extremes**; `:title` is the sole recovery | `.d`† | with W6·127 | — |
| W6·133 | **D2-11** | wb-gradient-easingeditor | MAJOR | Boolean control states painted in the **data-derived per-row ink** (`.rail-btn--on`/`.rail-tick` take `var(--motion-accent, …)`) | `.d`† — control state may not ride content ink | with W6·134 | — |
| W6·134 | **M-ESS-5** | wb-gradient-easingspecimenstrip | MINOR | `--motion-accent`'s fallback register **splits inside one card** (→`--muted-foreground` vs →`--foreground`); **all four dead today** | `.d` — one fallback | with W6·133 | — |
| W6·135 | **D-03 / D2-08a** | wb-gradient-easingeditor | MAJOR ◆ | The ordinal `` `${i+1} → ${i+2}` `` is minted **un-isolated** (`useSpecimenRows.ts:62`) and rendered in prose context — UBA | `.d` ◆ — cl.3 | with W6·80 | — |
| W6·136 | **D-13 (editor)** | wb-gradient-easingeditor | MINOR | `.specimen-dot + .specimen-dot { margin-left: -3px }` — the single **physical** property in an otherwise logical rule | `.d`† | with W6·106 | — |
| W6·137 | **D2-13** | wb-gradient-easingeditor | MINOR | The head names `linear` while the rail exports and announces `cubic-bezier(0,0,1,1)` | `.d`† — one name per curve | with W6·115 | — |
| W6·138 | **D2-12** | wb-gradient-easingeditor | MINOR | One surface drawn twice: the rail's `bg-well` and the stage's `:deep(.glass-card)` override | `.d`† | with W6·131 | — |
| W6·139 | **C2-10 / C-9** | wb-gradient-easingeditor | MINOR | `toggleTune` spread-clones the whole record per press and never prunes; `ref` is already deeply reactive | `.d`† | with W6·125 | — |
| W6·140 | **L-6 / L-r2-7 + L-11** | wb-gradient-easingeditor | MINOR | **7 of 9 barrel names with zero consumers**; a 3-hop dual path to constants | `.d`† (barrel); **`demo/ui` rides the W-HYGIENE banked identity — which is a PHANTOM owner (§0a n.10)**; re-home or mark NO-WAVE-OWNER | decide at wave open | — |
| W6·141 | **R3-8-in-kind** | wb-gradient-easingeditor | MINOR | `certifyAccentInk` uncached and the derivation **over-subscribed** (the whole `modelState` passed where three inputs suffice) | `.d`† | — | **magnitude KILLED (K-8)** — the mechanism survives, the number may not be quoted |
| W6·142 | **D-16** | wb-gradient-easingeditor | INFO | The three section headings use the palette-identity type role (`font-display text-subheading`) | `.d`† (adjacent; `GradientVisualizer.vue` owned) | with W6·31 | — |
| W6·143 | **M-EE-1** (DU) | wb-gradient-easingeditor | MAJOR | The house ships a contrast-**CERTIFIED** de-emphasis rung (`--ink-muted`) and **the entire gradient workbench declines it** | `.d` (editor rows) **◆ (strip/rows files)** — cl.3 for the ◆ half | with W6·129 | — |
| W6·144 | **M-EE-2** (LC) | wb-gradient-easingeditor | MAJOR | Per-interval easing is **progressively ERASED from the rendered gradient as stop count grows** (sampling budget; ≥9 stops), while the row's ramp never degrades | `.d` — the budget must scale with intervals or the ramp must degrade identically | with W6·56 (one sampling law) | — |
| W6·145 | **M-EE-3** (DU) | wb-gradient-easingeditor | MINOR | **No heading route to any interval panel** — the trigger has no heading wrapper/role/level | `.d`† | with W6·32 | — |
| W6·146 | **M-EE-4** (DU, + LC's D3-03 arithmetic folded as its sharpening) | wb-gradient-easingeditor | MINOR | The **exclusive accordion defeats the component's own bench thesis** — `toggleInterval` caps open rows at one | `.d`† | with W6·19 | — |
| W6·147 | **M-EE-6** (LC) | wb-gradient-easingeditor | MINOR | **Two OPPOSITE error postures two lines apart**, the harsher one unreachable | `.d`† — **joins the R-A totality decision** (W6·57) | with W6·57 | — |
| W6·148 | **M-EE-8** (DU) | wb-gradient-easingeditor | INFO | Two ad-hoc hover washes; the producer's `--surface-tint-4/6` rungs shipped with **zero demo consumers** | `.d`† | with W6·131 | — |

### 1e. Unit `.e` — motion, PRM, the quarantine (Lane 1, serial)

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·149 | **GV-73** ≡ D/F-18 | wb-gradient-visualizer | INFO (observation) | The render tile is the one surface with **no motion register and the most violent change** — a type switch replaces the whole painted field in one frame | `.e` rider **under the H2 motion-quarantine law: no motion assertion ships unquarantined**; the FALSE rows (M-01/M-03/M-17/M-26/M-27/M-29/M-33) may not be carried | H2 reads before `.e` writes | — |
| W6·150 | **L-3 (strip)** — *distinct from `L-3` in wb-gradient-codeeditor (A-3's axis id) and from the editor's `L-3`* | wb-gradient-easingspecimenstrip | MAJOR ◆ | **Reduced motion has five demo homes** plus glass's published-and-unused `./motion-core` (`useReducedMotion`/`readReducedMotion`) | `.e` ◆ — consumer swap; **the producer door already exists**. Gate **e2 (`gate-prm-idiom.mjs`)** is this row's instrument | before W6·298 (MX-9, same identity class at Mix) | — |
| W6·151 | **M-ESS-5 (motion limb)** | wb-gradient-easingspecimenstrip | MINOR | *(motion half of W6·134)* the four `--motion-accent` fallbacks are **all dead today** — an unfired register | `.e` — delete or fire; **no assertion without quarantine** | with W6·134 | — |

### 1f. Unit `.f` — X:CSS-1, the catalog lie and the specimen sentence (Lane 2)

*Provenance, recorded because the corpus grep cannot see it: **`ColorSpaceSelector.md` contains zero
`X-W6` tokens** and is nevertheless this unit's source of record. It is the apotheosis of
**`V·MT-W-CSS1`**, which `CC-068` re-authored as **X:CSS-1 = `X.W6.f`**; its gates **G1…G10 are
f1…f10, one-for-one** (AboutPane cites them by the f-numbers: *"f1 (catalog totality) / f2
(masking-fallback grep, 2 hits today)"*, *"f4 (caption fits box; 16/18 overflow born-RED) + f5 (one
grammar per specimen)"*, *"f7 (boundary is the domain type; 3 `colorSpace: any` hits born-RED)"*).
Second record after `GradientStopEditor.md` to own a W6 unit under a pre-X name — §0a n.1 extends
here.*

**CURE-SHAPE LOCK (verbatim, binding on every row in this unit).** *"The wave AUTHORS the 5 missing
info rows (srgb-linear, display-p3, a98-rgb, prophoto-rgb, rec2020) and decides the 7 missing docs
explicitly (`doc: null` is an authored decision rendering an explicit empty state, never a silent
v-if) — totality by content: a catalog that compiles because five entries were stubbed is the
masking fallback wearing a type's clothes."*

**STRUCTURE-OVER-GATES LOCK (verbatim).** *"(1) `satisfies Record<DisplayColorSpace, SpaceEntry>`
makes a space-with-no-info a compile error — the masking fallbacks are not banned, they become
meaningless and delete."* The cure is a **copy** of `picker-color.ts:52-70/:72-90`, not an
invention (`AboutPane.md`: *"THE CURE ALREADY RUNS ONE MODULE AWAY"*).

**ANTI-PROJECTION LOCK (f6 clause ii, verbatim).** *"printed CSS coordinates must still satisfy
`convertColor(parseCssColor(caption).value,'lab') ≈ model color` to 1e-6 — GREEN today (≤1e-10),
and it goes RED the moment anyone folds `mapColorToGamut` into the formatter."* f8 **must not** be
satisfied by repainting dots with projected colours (that would fail f6 ii).

**SCOPE LAW (addendum item 1, binding on every "sole/only" claim in this fold).** *"Any accusation
containing sole / only / unique to / the one place must paste the SAME command re-run at repository
scope beside the scoped one, and must book the row at the scope where the count lives."*
**CURE-ADJUDICATED-SEPARATELY (item 2).** *"A `proposedCure` inherits no evidential weight from its
`defect`'s evidence; every adopting wave states the property the cure could break and gates it in
the breaking direction (f6 ii is the template)."*
**SEQUENCING (item 6).** *"The L-15 replacement rider (INTERPOLATION_SPACES derivation) may not open
before MT-CSS-1 lands `interpolatable`; MT-CSS-2's born-RED must be restated against the post-CSS1
seam."*

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·152 | **L-1 (CSS)** ≡ **AB-1** (D-9/L-1/C-3 5-space limb) ≡ **CNL-1** (C-1/D-1/L-1, sharpened) | ColorSpaceSelector ≡ AboutPane ≡ ColorNutritionLabel | **BLOCKER** | **The catalog lie**: five selectable spaces render CIE-RGB-1931's dossier; offered=18, info=13, docs=11; `:210-215` falls to `colorSpaceInfo.rgb` while ranges come TRUE from `PICKER_CHANNELS` — one label, two spaces' facts | **f1/f2/f3.** The five info rows are **AUTHORED, not stubbed**; the masking fallbacks *become meaningless and delete*. **One identity, cross-booked, never re-booked** | first row of the unit | — |
| W6·153 | **AB-2** (D-8/C-2/L-2) ≡ L-1's `missingDoc(7)` | AboutPane ≡ ColorSpaceSelector | MAJOR | **7 of 18 spaces render `<h2>Detailed Guide</h2>` over a void** (guide childCount 1) | **f1/f3**; *"`doc: null` becomes an authored explicit empty state, never a silent `v-if`"* | with W6·152 | — |
| W6·154 | **AB-3** (L-3 ⇄CSS-L-1 structure) | AboutPane | MAJOR | **One concept enumerated at four cardinalities (18/17/13/11)**; the two tables drifted | **f1 + structure-over-gates** (`satisfies Record<DisplayColorSpace, SpaceEntry>`) | with W6·152 | — |
| W6·155 | **L-2 (CSS)** ≡ **AB-16** (reader-1 miss as TEXT) | ColorSpaceSelector ≡ AboutPane | MAJOR | **16 of 18 specimen captions overflow** their 234px box (worst 758px); byte-pinned failing input = the boot colour's RGB row | **f4** (caption fits box) **+ f5** (one grammar per specimen); ONE digit policy sized to the box | after W6·152 | **DISSENT-2 preserved (worker-O, unresolved): "I would rank L-2 above L-3-as-rescoped"** — the wave cures both, so no ruling was required; if only one lands, O's ordering governs |
| W6·156 | **L-3-as-rescoped (CSS)** | ColorSpaceSelector | MAJOR | Out-of-gamut is **projected, not marked** — the HSL row browser-resolves to white beside a dot painting the true colour | **f5/f6**; **the projection cure is RETIRED with rationale**; f6(ii) anti-projection lock is the template for cure-adjudicated-separately | after W6·155 | O's severity dissent (above) attaches here |
| W6·157 | **L-12 (CSS)** ≡ **AB-21** (C-5/L-10/D-17) | ColorSpaceSelector ≡ AboutPane | MAJOR | `(colorSpace: any)` whole-model spread against a `Partial<ColorModel>`; **exactly 3 hits** (`ColorSpaceSelector.vue:11`, `ColorPicker.vue:43`, `AboutPane.vue:25`) + a 4th forced cast | **f7** — the boundary is the domain type; **the type witness, never grep alone** (`@ts-expect-error` over `modelValue="not-a-space"`) | with W6·152 | AboutPane keeps **the clobber limb as an honestly-labelled hypothesis** |
| W6·158 | **L-9 (CSS)** | ColorSpaceSelector | MAJOR | **Eighteen dots, one silhouette**: `distinctVisualSignatures = 1` (identical borderRadius / `feTurbulence` seed 240 across all 18) | **f8**, **seed-only cure**; the oracle keys on (borderRadius \| background \| feTurbulence@seed) and **explicitly excludes the per-mount filter url** (O's recorded false-GREEN trap) | after W6·152 | O's self-recorded first-pass false refutation preserved as method (DISSENT-3a) |
| W6·159 | **L-6 (CSS)** | ColorSpaceSelector | MAJOR | The space-switch command has **no home in the component** — the sole caller is `ColorPicker.vue:348-354`'s watcher | **f9** — one command, one home; the watcher DELETES | before `/about` lands (X-W5) | — |
| W6·160 | **L-4's color-session edge (CSS)** | ColorSpaceSelector | MINOR | `demo/color-session` draws **1** edge into the banned `../ui/` layer (`:109`) | **f10**, *deliberately scoped* — **repo-wide = 90 statements = the W-HYGIENE wave**; *"booking 90 behind a component gate is the un-completable-wave failure"*. **W-HYGIENE is a PHANTOM owner (§0a n.10)** — the 89 remainder is NO-WAVE-OWNER until re-homed | — | — |
| W6·161 | **L-11-in-part (CSS)** | ColorSpaceSelector | MINOR | `CSS_NATIVE_SPACES` + `toCSSColorString` die with the formatter | `.f` — **identity guard: this corpus's L-11 is fact-identical to the ColorPicker adjudication's L-10; ONE identity, folded, never re-booked** | with W6·156 | — |
| W6·162 | **L-8's own site (CSS)** | ColorSpaceSelector | MINOR | `tag="div"` dead prop at `:82` | **f8's grep arm**; L-8 residue = the **glass-7 `tag=` sweep, 20 sites**, listed in the record — that sweep is NOT W6's | — | — |
| W6·163 | **AB-6** (C-4) | AboutPane | MAJOR | The test surface is **vacuous**: zero unit tests touch the subtree — *"the structural explanation for how AB-1/AB-2 shipped"* | **`.f` (o21–o24 are the cure)** + the X-W5 walk/page-load carves; **primary `.f`** | oracles land with W6·152 | — |
| W6·164 | **AB-19** (C-6) | AboutPane | MINOR | The `:key` remount (`AboutPane.vue:52`) is **load-bearing** — `Markdown.vue` has no watch; the remount costs a TreeWalker + scroll-position loss in an 8023px scroller | `.f` rider — **the wave edits this exact call site (`markdownModules` → catalog `doc:`)** | with W6·153 | — |
| W6·165 | **AB-27** (L-9) | AboutPane | MINOR | About's **11 guide SFCs live at repo-root `assets/docs/`** and every one imports back into `demo/` | **Boundary row**: TYPE cycle only; `assets/` is **outside W8's bounds**; *"the `.f` `doc:` re-plumb is the natural landing and should claim it at wave-open"* | claim at wave-open or mark NO-WAVE-OWNER | — |
| W6·166 | **AB-34** (MISS-2, reader-2, ADOPTED) | AboutPane | MINOR | The widest type hole is the barrel: `DocModule = () => Promise<any>` | **X-W8** (the DocModule/DocItem types-module carve, `W8:174`) **with an `.f` rider — the catalog's `doc:` field must consume a retyped module** | X-W8 carve first | — |
| W6·167 | **AB-35** (MISS-3, reader-2, ADOPTED) | AboutPane | MINOR | The shared cure has a **second consumer nobody costed**: `ConsoleRail.vue:174` | **f2's grep counts this exact line (2 hits today)** — *"the cost is already inside the gate"* | with W6·181 | — |
| W6·168 | **AB-40** (C-10 residual + C-12/D-17 hygiene) | AboutPane | INFO | Compile-time module table in instance scope; discarded `defineProps`; mixed `:model-value`/`:cssColor` casing; inert `mx-auto` | `.f` rider — *"the wave rewrites this script block anyway"* | — | — |
| W6·169 | **CNL-7** (D-9/C-4/L-10) | ColorNutritionLabel | MAJOR | **A live Tooltip stack per row displaying zero characters** | **`.f` — the `notes` DATA disposition (render or delete) is decided AT the catalog cut, not drifted** | at the catalog cut | — |
| W6·170 | **CNL-13** (L-3/L-3.4) ≡ **L-5 (consolerail)** — *four/five homes, one identity* | ColorNutritionLabel ≡ consolerail | MAJOR | **Five declarations of "the channels of a space", no owner**, two contradicting on one page (`SPACE_SCHEMA · PICKER_CHANNELS · colorSpaceInfo.components[] · ConsoleRail SPACE_GLYPHS` + the catalog) | `.f` for the markdown content rows (catalog-owned) + the ARCHITECTURE claim becomes true or the doc is corrected. **Consolerail L-5 folded here, identity preserved, counted once** | with W6·152 | — |
| W6·171 | **CNL-21** (D-18/C-6/L-6) | ColorNutritionLabel | MINOR | `colorSpaceInfo.hex` is **unreachable dead data**; the card contradicts its own guide while AboutPane loads `hex.md` | **`.f` — dual keying: docs by `DisplayColorSpace`, math by `SpaceId`** | with W6·152 | — |
| W6·172 | **CNL-22** (C-10/L-13) | ColorNutritionLabel | MINOR | Three type escapes rooted in the bare `as const` | **`.f` — the `satisfies` cure makes the comment true or the build red** | with W6·154 | — |
| W6·173 | **CNL-23** | ColorNutritionLabel | MINOR | Dead authored data (`name` has zero consumers) + the second partial table (≡ AB-2's identity) | **`.f` — the catalog cut decides every authored field's fate explicitly** | with W6·153 | — |
| W6·174 | **CNL-24 (content half)** | ColorNutritionLabel | MINOR | The joke copy and the content rows | `.f` content pass; **the recomposition rows → NO-WAVE-OWNER (AB-17); the rhythm rows → `.g` (W6·184)** | — | — |
| W6·175 | **CNL-27** (C-13 residue) | ColorNutritionLabel | INFO | The `fromEntries`/`entries` round-trip with a latent positional join | *"rides whichever wave touches the file — `.f` edits these exact lines"*; recorded so nothing re-derives it | — | — |
| W6·176 | **CNL-2** | ColorNutritionLabel | **BLOCKER — NAMED GAP** | **Two-click powerless-hue kill** — a crash BLOCKER **no axis filed** and **no wave owns** | The file is in W6 bounds after §3 n.3; **W6 adopts at its opening sitting or the formation boundary carries it BY NAME**. Ignoring it is the M-25 defect | opening sitting | — |
| W6·177 | **CNL-3** | ColorNutritionLabel | **BLOCKER — NAMED GAP** | **The 28-grey boot white-screen** — same class as W6·176 | same disposition as W6·176 | opening sitting | — |
| W6·178 | **MISS-1** | picker-componentsliders-consolerail | MAJOR | The rail's **SOLE explanation surface is degenerate or actively WRONG in 8 of 18 selectable spaces, plus the α cell in all 18** | **→ X-W6 (CC-068 — the catalog totality wave)**; cl.3 on §3's PICKER-SLIDERS band | after W6·152 | — |
| W6·179 | **C-4 (consolerail)** | picker-componentsliders-consolerail | MAJOR | One identity with **CC-081's K-10 ICtCp row (→ X-W9)** ∩ **CC-068's catalog totality (→ X-W6)** | **CROSS-BOOKED, not duplicated — "the cure lands wherever the catalog lands"**; counted **once** here (§0a n.11) | X-W9 ⇄ X-W6 must agree at X-W0 | — |
| W6·180 | **C-5 (consolerail)** | picker-componentsliders-consolerail | MINOR | `currentColorRanges[component]` rendered bare (`:75`); the map **provably never contains `alpha`** | `.f` (CC-068); totality carried by MISS-1 | with W6·178 | ruled MINOR; the totality framing moved to MISS-1 |
| W6·181 | **L-3 (consolerail)** | picker-componentsliders-consolerail | MAJOR | The exact cast the ColorSpaceSelector apotheosis books for deletion — **its f2 names `ConsoleRail.vue:174` verbatim** | **FOLD, identity preserved** — one deletion, two records | with W6·167 | — |

### 1g. Unit `.g` — About and picker card rhythm (Lane 2, after `.f`)

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·182 | **AB-29** (D-5) | AboutPane | MINOR | ~44.6px **dual-edge misalignment** (right wrapper `display:block` + the height chain); Δtop 44.5 / Δbottom 44.77 in both schemes | **`.g` — CC-062, gate g2 (Picker↔About top edges ≤1px with Mix as the control)** | after `.f` | derived symptom — MINOR, not the cause |
| W6·183 | **AB-31** (reader-1 miss, ADOPTED) | AboutPane | MINOR | About is the **only pane of nine** whose body inline padding disagrees below `sm` (`px-3` vs `px-4`) | `.g` rider — About rhythm is `.g`'s surface. **SCOPE LAW applies: the "only pane" claim is spot-verified against Gradient/Mix and must paste the nine-pane re-run** | with W6·182 | — |
| W6·184 | **CNL-24 (rhythm half)** | ColorNutritionLabel | MINOR | Card rhythm rows (no declared 66ch measure; the label's own vertical ladder) | **`.g`** — gate **g1 (`gate-card-rhythm.mjs`)** is the instrument | with W6·182 | — |
| W6·185 | **AB-17** | AboutPane | MINOR | The About recomposition rows | **NO-WAVE-OWNER** — *"About-recomposition" is a PHANTOM owner (§0a n.10)*; named here so the absence is a result, not an omission | formation boundary | — |

### 1h. Unit `.h` — the blob pipeline and the hero seat (Lane 3)

*`X-W6` owns `BlobPane.vue` + `HeroBlob.vue` + `useAtmosphere.ts` + `atmosphere-calibration.ts`
(`picker-heroblob.md`'s own ownership sentence, from `BlobPane.md`'s table). Gates h1/h2 and the
`gate-blob-pipeline.mjs` instrument are this unit's; `CC-057` is the persistent-preview row.*

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·186 | **D-3(a) / BPC-1 / L-1** | BlobPane | **BLOCKER** | **Four of five Geometry sliders cannot reach the renderer**; the pane writes `0.220` while the render draws `0.325` | **BUILD → X-W6** (owns `BlobPane.vue` + `HeroBlob.vue`; **CC-057 stage ownership**) | first `.h` row | — |
| W6·187 | **D-3(b) / BPC-2** | BlobPane | **BLOCKER** | After the 5.3s park, **no config write wakes the renderer** (`noteBlobActivity` unreached) | **X-W2.b** (the `settled`-seam park replacement, spec'd verbatim) **+ X-W6** (*"a stage the pane owns needs no ornament-park at all"*) | X-W2.b lands first | — |
| W6·188 | **D-2** | BlobPane | **BLOCKER** | **The Blob workbench owns no preview** — the sole rendered blob is `aria-hidden` ornament | **BUILD → X-W6 (CC-057: "persistent Blob/Atmosphere preview" — the named row)**; gate **j3** adopts it per route | with W6·186 | 35.2:1 figure = live residue item 4 |
| W6·189 | **NEW-B1** (reader-B missed, arbiter-confirmed) | BlobPane | MAJOR | `heroConfig` is **heterogeneous** — half plain snapshots, half live reactive proxies | **BUILD → X-W6** (dies with the stage transposition); the **structural half** of W6·186 | with W6·186 | — |
| W6·190 | **D-4 / L-2** | BlobPane | MAJOR | **31 controls in 7 flat, non-collapsible, preset-less sections** | **FOLD**: disclosure/composition → **X-W6 scene contract** + CSP `D-9`/`G8` `rank`; **L-2's "delete ConfigSliderPane" arm does not survive** | after §3 adopts `ConfigSliderPane.vue` | — |
| W6·191 | **D-12 / BPC-12** (+ BPC-4) | BlobPane | MAJOR | **17 of 48 axes unreachable**, including `morphT` — the sole flat↔dressed control | **BUILD → X-W6** (sections + enum defs through the slot / `G8` enum-def species); *the slot mechanism exists and AuroraPane uses it* | with W6·190 | — |
| W6·192 | **BPC-8** | BlobPane | MAJOR | `interaction.stretch` shipped as a top-level slider over exactly the domain the producer docblock demotes | **BUILD → X-W6** (one-line section edit; the demotion honoured) | — | — |
| W6·193 | **BPC-9** | BlobPane | MAJOR | **Vacuous gate**: `#/blob` appears nowhere in e2e, no test mounts BlobPane | **BUILD → X-W6 (`o27-scene-contracts.spec.ts` — the born-RED pixel-delta oracle)**; **§0a n.4: the number `o27` is TAKEN** | H6 decides the number first | — |
| W6·194 | **L-4 (BlobPane)** ≡ **HeroBlob row 25 (L-4)** ≡ **HeroBlob row 24 (M-2 + D-16 + reader-1 missed #1/#5)** | BlobPane ≡ picker-heroblob | MAJOR | **Two `deriveBlobPalette` owners**; the boot derive's output is never rendered; the palette is derived **TWICE per rAF-coalesced frame** (both triggers are the SAME signal) | **BUILD → X-W6** (owns `useAtmosphere.ts`); **one owner, ink floor applied once** | before W6·196 | — |
| W6·195 | **L-6 (+ L-13) (BlobPane)** | BlobPane | MAJOR | Config created in boot, provided under a **producer symbol**, written in scenes — six operations across three areas | **BUILD → X-W6 (+ X-W5 for the route half)** | — | — |
| W6·196 | **D-9 / BPC-7** | BlobPane | MINOR (aggravated by row 5's addendum root) | **Two pairs of sliders share an accessible name** | **BUILD → X-W6** (BlobPane owns the strings) under **CSP `G3`'s grouping cure + ADD-never-replace** | after §3 | — |
| W6·197 | **D-7 residue / D-11 / D-10** | BlobPane | MINOR | `fmt` swings the readout's KIND 1↔5 chars mid-drag | **NO-X-WAVE-OWNER flag** on the direction axis; **BlobPane's label strings are X-W6's** | formation boundary for the axis half | — |
| W6·198 | **NEW-B2 + NEW-B3 + NEW-B4 + A-missed M6/M7/M8/M9 + D-15 + INFO residue** | BlobPane | MINOR/INFO | Schema, sections and description bytes (a, d, e, f, g) | **(a),(d),(e),(f),(g) BUILD → X-W6**; (b),(c) die with W6·191's `G8` | with W6·191 | — |
| W6·199 | **ADD-BP-4** | BlobPane | MINOR | Admin-classification cross-cite: **`o27`'s scene-contract oracle must not inherit the same exclusion** | `.h`/`.j` — the oracle's project matrix must include the excluded class | with W6·193 | — |
| W6·200 | **C-1 / D-1 / L-2 (HeroBlob)** | picker-heroblob | **BLOCKER** | The ink floor is bought by **destroying the picked chroma**: `floorStops` shifts L *after* the derive has gamut-mapped, so the bead is never the picked colour | `.h` — derive-then-floor order inverts; the component's stated purpose is arithmetically undelivered | before W6·194's one-owner cut | — |
| W6·201 | **C-2 / L-3 (HeroBlob)** | picker-heroblob | MAJOR | The floor **never re-derives on a scheme flip or ambient drift** | `.h` | with W6·200 | — |
| W6·202 | **C-13 (HeroBlob)** | picker-heroblob | MINOR | The flip branch reduces to `plateL > 0.83` / `< 0.17` — every light-scheme plate always **inverts** the ramp | `.h` | with W6·201 | — |
| W6·203 | **D-4 / C-8 (HeroBlob)** | picker-heroblob | MINOR | `chromaCeiling: Math.max(0.16, seed.C)` is **fully inert** (byte-identical to omission) and the `W6-4` comment's named case is false | `.h` — delete the knob and the false comment | — | — |
| W6·204 | **L-5-RULING-1** | picker-heroblob | MAJOR | The `lightnessFloor` booking is **undischargeable as written**: the producer knob is an absolute deepest-stop floor, `INK_FLOOR` is a bead↔plate separation | `.h` — re-state the booking against the producer's actual knob **before** any relay | before any BH relay on this row | — |
| W6·205 | **C-9 / L-5 / D-13 / D-10 (HeroBlob)** | picker-heroblob | MAJOR | **Three shipped producer seams are hand-reimplemented while the comments still call them "BOOKED"** | `.h` — consume the producer, correct the comments | with W6·204 | — |
| W6·206 | **M-1 (HeroBlob)** (reader-2 missed, arbiter-verified) | picker-heroblob | MAJOR | The config prop is a **FALSE LIVE SEAM**: the producer snapshots the object at setup; only three keys stay live | `.h` — the seam is declared or the prop is retyped | with W6·189 | — |
| W6·207 | **C-3 / L-6 / D-6 (HeroBlob)** | picker-heroblob | MAJOR | The ornament's mount is gated on the **atmosphere** canvas's WebGL success — a pre-b2 loss **deletes the hero for the session** | `.h`; discharged jointly with W6·214 (M-7) | with W6·214 | — |
| W6·208 | **D-3 (HeroBlob)** | picker-heroblob | MAJOR | Ambient motion **cannot terminate within canon's five seconds** — `BLOB_IDLE_MS 2000 + SLEEPY_POSE_MS 3300 = 5300ms`, by source arithmetic alone | `.h` under **H2's quarantine law** | H2 reads first | — |
| W6·209 | **C-6 (HeroBlob)** | picker-heroblob | MAJOR | The only unit test is a **regex over the raw SFC text** — one template token deletes the register and the suite stays green | `.h` + X-W1 falsifier law; `test/picker-blob-config.test.ts` is bounds-new (§3) | after §3 | — |
| W6·210 | **C-7 (HeroBlob)** | picker-heroblob | MAJOR | `O-12·3` asserts a hover response the component **cannot produce** — no pointer host exists | `.h` — the oracle is corrected or the host is built; **a false-GREEN oracle is the L-19 class** | with W6·209 | — |
| W6·211 | **C-5 / L-8 + M-5 (HeroBlob)** | picker-heroblob | MINOR | `BEAD_RATIO` 0.52 encodes the **retired** bodyRadius 0.26 against the live 0.325 (two of three mirrors updated; the anti-skew fixture itself skewed) | `.h` | with W6·186 | — |
| W6·212 | **D-8 / D-9 + the 0.76 constant** | picker-heroblob | MAJOR | A **Blob-derived vertical minimum** still exists (`min-height: calc(0.76·fp − 0.75rem)`), binding at 390 and 200% zoom, against canon | `.h` | — | — |
| W6·213 | **D-7 / L-13 (HeroBlob)** | picker-heroblob | MINOR | The blob canvas is the **only element bleeding past the mobile viewport** — and the attribution is corrected: the row fires on `/#/blob` **and** `/#/does-not-exist` | `.h`; **SCOPE LAW: the "only element" claim carries its repo-scope re-run** | — | attribution corrected from the axis reading |
| W6·214 | **M-7 (HeroBlob)** (BOTH readers' missed — their sole convergent find) | picker-heroblob | MAJOR | The atmosphere's `webglcontextlost` handler **never calls `preventDefault()`**, so the session deletion is **permanent** | **One-line cure; discharges `App.md` D-6**; NEW gate candidate **N9** | **before W6·207** | — |
| W6·215 | **M-8 (HeroBlob)** (reader-1's missed, arbiter-verified) | picker-heroblob | MAJOR | **The seat anchor sits ON the ornament** (`class="hero-blob-anchor"` on `<HeroBlob v-if…>`), so every absent/error-state cure is **structurally homeless** | `.h` — the seat must exist independent of its occupant; NEW gate candidate **N10** | **SEQUENCED BEFORE W6·207 (row 9)** — the record's explicit edict | — |
| W6·216 | **D-11 (HeroBlob)** | picker-heroblob | MINOR | forced-colors is **undesigned for the seat** — zero rules reach it; canvas content is exempt by spec, so the WebGL bead opts out of the user's palette | `.h` | with W6·215 | — |
| W6·217 | **D-12 (HeroBlob)** | picker-heroblob | MINOR | The seat positions with **physical** properties against canon's logical-direction law; the two agree **by accident** | `.h` — *"fixing either alone breaks the other"* | with W6·215 | — |
| W6·218 | **C-10 / D-14 / L-9 (family-rescoped)** | picker-heroblob | MINOR | **Two masking fallbacks** — the bare `catch {}` over the component's only colour-truth source, against the producer's explicit no-catch-to-default rule | `.h`; the masking-fallback ban is f2's law applied here | — | family rescope recorded |
| W6·219 | **C-12 / L-10 (HeroBlob)** | picker-heroblob | INFO | HeroBlob (**and `useMarkdownColors` — a family**) folds `resolveSurfaceLightnessLive` into its own computation without the documented mount bump | `.h` + `.l` (the Markdown consumer) | with W6·377 | — |
| W6·220 | **L-12 (HeroBlob)** | picker-heroblob | MINOR | A **string-literal contract** crosses the feature→boot boundary with a silent bare-return guard (`animationName !== "blob-emerge"`) | `.h` | — | — |
| W6·221 | **D-15 (rescoped) + reader-1's missed #2** | picker-heroblob | MINOR | The quality proxy is **provably wrong on its own terms** (container-scaled footprint, viewport-scaled rung) | `.h` | — | — |
| W6·222 | **M-3 (HeroBlob)** (reader-2 missed) | picker-heroblob | MINOR | `surface.rimColor` is an **unowned colour atom** — every hero frame paints the producer's fixed warm-brown `#8c694e` Fresnel rim over an authored palette | `.h` | with W6·200 | — |
| W6·223 | **M-6 (HeroBlob)** (reader-2 missed) | picker-heroblob | INFO | `heroConfig` hands a **hybrid object** across the package boundary | **Identity-adjacent to W6·189/W6·206**; one retype closes all three | with W6·206 | — |
| W6·224 | **D-17 (HeroBlob)** | picker-heroblob | INFO | The file is **a ledger, not a component**: ~155 of 317 lines are comment; six machines, three of them dead | `.h` | last `.h` row | reader-2's corrected count adopted over the axis's ~180 (kill: D-17-count) |
| W6·225 | **M-9 (HeroBlob)** (reader-2's missed, consequence-bounded) | picker-heroblob | INFO | `onActivated` unconditionally resumes and re-arms the idle countdown with **no `onDeactivated` counterpart** | `.h` — joins `.j`'s deactivation family (W6·303) | with W6·303 | consequence bounded by this seat |

### 1i. Unit `.i` — the atmosphere harness (Lane 3)

**BOUNDS RIDER (AuroraPane's own words, binding on W6·228/W6·229/W6·235).**
*"`aurora-harmony-stops.ts` is absent from `.i`'s §Files"* — and so is `aurora-motion.test.ts`.
Both exist (measured 2026-08-28). Every row below is `cl.3` on §3 n.14/n.15.
**Motion disclosure carried verbatim**: *"no motion row is carried from this corpus… `motion-quarantine.md`
carries no aurora/atmosphere row; `X.W6.j`'s transition gates are already MOTION-SOURCED ·
PENDING-QUARANTINE."*

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·226 | **AP-3** ≡ D/N-3 ≡ C/C-6 | AuroraPane | MAJOR | Dark mode ships the producer's own **named "dark-leg defect"** (`lightnessScale`) | **`.i` gate i2 — the wave already carries this row as CC-066** | — | the **whitepoint limb is KILLED (K-3) and severed from this row** |
| W6·227 | **AP-4** ≡ C/C-1 | AuroraPane | MAJOR | On the shipped `"css"` render tier **five of seven knobs are structurally inert** | **`.i` — i1's X:ATMO-1**: *"named atoms move ≥ stated ΔE2000, from committed frames"* | — | residue U-3 (live-only magnitudes) |
| W6·228 | **AP-5** ≡ C/C-2 | AuroraPane | MAJOR | The harmony `PreviewStrip` **breaks the O-14 law its own header states** | **`.i` with the bounds rider** (`aurora-harmony-stops.ts`); magnitudes → residue U-4 | **after W6·229** — the record's edict | — |
| W6·229 | **AP-6** ≡ C/C-3 | AuroraPane | MAJOR | `guaranteeSeamOffset` **does not deliver its own guarantee** — headroom computed on the wrong term | **`.i`, same bounds rider; "the cure must precede AP-5's re-point"** | **BEFORE W6·228** | frequencies (90/1200, 9/120) → residue U-5 |
| W6·230 | **AP-7** ≡ C/C-4 ≡ L/L-2 | AuroraPane | MAJOR | **Three of ten shipped media** (`kuwahara`, `metal`, `metal-gradient`) are unreachable | **`.i` — the stale-producer-fact family**; the cure is a `Record<AuroraMedium, string>` map, which **kills W6·233 in the same construct** | before W6·233 | — |
| W6·231 | **AP-8** ≡ C/C-5 | AuroraPane | MAJOR | The Zones slider **caps at 6** on a hand-copied producer fact that has since lifted | **`.i` — same stale-fact class as i2's three comments**; rider: i2's comment list extends to this one | with W6·226 | — |
| W6·232 | **AP-9** ≡ C/C-9 ≡ **PS-29** (missed-LC#1) | AuroraPane ≡ PreviewStrip | MAJOR | **Nothing tests this component**; and two docstrings assert a vitest oracle over the aurora truth function **that does not exist** | **`.i` — o25/o26 are the wave's new oracle surface**; **§0a n.4: `o26` is TAKEN**. PS-29 is an **identity FOLD**, not a second row | H6 decides the numbers first | — |
| W6·233 | **AP-23** ≡ C/C-11 | AuroraPane | MINOR | `label()`'s mechanical split-and-titlecase renders **"Vangogh"** | **`.i` (with AP-7)** — dies inside the map | after W6·230 | — |
| W6·234 | **AP-28** (reader-LC missed #2, ADOPTED) | AuroraPane | MINOR | **Copy JSON and Reset disagree about what "the config" is** | `.i` — the atoms-door shape decision rides the harness unit's files | — | — |
| W6·235 | **AP-32** ≡ L/L-13 | AuroraPane | INFO | `aurora-motion.test.ts:7` cites a module path **retired at W43** while `:26` imports the live path | **`.i` — joins the i2/AP-8 stale-prose deletion list**; *bounds rider: the file is absent from `.i`'s §Files* | with W6·231 | — |
| W6·236 | **PS-4** ≡ C-14 (+L2-8 limb) | PreviewStrip | MINOR | `stampStops` is documented **"ONE serialization"** and is a bare `join("|")` | Chip-family residue; **the aurora serializer swap rides `.i` beside AP-5** (whose bounds rider already extends to `aurora-harmony-stops.ts`) | with W6·228 | — |
| W6·237 | **PS-21** ≡ C-13 (structural limb) | PreviewStrip | MINOR | The one un-memoized read in a file that owns the computed pattern; **six calibrated atmosphere resolves per menu open** | Chip-family residue; **the aurora per-open cost limb rides `.i`'s harness (AP-5/AP-9 instruments)** | with W6·232 | static reading only — no live claim |

### 1j. Unit `.j` — routed scenes and the Mix canary (Lane 4, opens after Lanes 1–3)

*Three families land here: **the Aurora scene re-composition**, **the preview-chip apparatus**, and
**MX-CLUSTER — the Mix canary**. `chassis-fitness.md` names Mix *"the first-contort CANARY"* for a
cure that is **DEAD** (§0a n.7): under **H1** the recomposition is the **X-W5 scene contract**, never
a chassis. Every MX-CLUSTER row is `cl.3` on §3 n.5–n.11.*

#### 1j·1 — the Aurora scene re-composition

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·238 | **AP-1** ≡ D/N-1 | AuroraPane | **BLOCKER** | The binding Atmosphere composition is **inverted** (OPTICAL-BENCH-COMPOSITIONS.md:46/:80) | **The `"consume InstrumentChassis"` cure is STALE — CC-108/A-17 rule it dead and H1 FORBIDS rebuilding anything chassis-shaped.** Cure = **the X-W5 scene contract adopted per route** (`j3` Blob/Atmosphere persistent preview) | first `.j` row; H1 reads first | R-7's dual-chassis contradiction **RESOLVED BY PROGRAM RULING** |
| W6·239 | **AP-2** ≡ D/N-2 | AuroraPane | MAJOR | **The selected option paints zero pixels** — the marker inks `var(--select-dot-color, var(--transparent))` | **Consumer half → `.j`** (one token assignment at the re-composition); the producer-default limb is a **defect for every consumer** → BH relay | with W6·238 | `hideIndicator` interim recorded either way (see W6·250) |
| W6·240 | **AP-10** ≡ C/C-10 ≡ D/D-3 ≡ L pass-1 L-5 | AuroraPane | MAJOR | The pane **rebuilds the row primitive its parent's own header already ships** | `.j` — the row re-composition; **one spine, one well** | with W6·238 | 44.7/50.1px spreads = residue U-7 |
| W6·241 | **AP-11** ≡ D/C-1 | AuroraPane | MAJOR | `text-caption` is **HALF-applied** — its font-size arm loses to `text-detail` | `.j` — **the cure is deletion with NO replacement class**; the stamps die in the re-composition | with W6·240 | — |
| W6·242 | **AP-12** ≡ C/C-7 (h-9 half) ≡ L/L-8 | AuroraPane | MAJOR | **Four per-instance `h-9` overrides** (flat 2.25rem) against the scale-aware producer control | **`.j` via the X-W4.b mechanism of record (`size="sm"`)** — AuroraPane is **absent from W4.b's file bounds**, so *"the pane's own adoption lands in `.j`'s re-composition citing W4.b as the mechanism of record"* | after X-W4.b | 60px resolved figure is a chromium-cell claim |
| W6·243 | **AP-13** ≡ C/C-8 | AuroraPane | MAJOR | The four enum labels ink at **raw `--muted-foreground`** | `.j` — `.aurora-row-label` **deletes whole** in the re-composition | with W6·241 | ratios 3.50–3.94:1 → residue U-1 |
| W6·244 | **AP-14** ≡ D/N-4 | AuroraPane | MAJOR | glass-ui ships the **labeled-form-control layer**; `ConfiguratorRow` re-implements it | `.j` — **mechanism = X-W4.b** | with W6·242 | reader's missed M1 verified against the d.ts |
| W6·245 | **AP-15** ≡ D/N-5 | AuroraPane | MAJOR | The producer exposes `amount?` on **every textured medium** and the pane rebuilds from scratch — **structurally lossy for any sibling key** | `.j` — an amount slider mounting for textured kinds; **i1's oracle witnesses it** | after W6·230 | count corrected from the axis reading |
| W6·246 | **AP-20** ≡ D/N-7 ≡ L/L-7 | AuroraPane | MINOR | The four setters type on reka-ui's `AcceptableValue` | `.j` — **type-level retype alone suffices**; drop the coercions | with W6·323 (MC-21, same class) | — |
| W6·247 | **AP-21** ≡ C/C-12 | AuroraPane | MINOR (latent) | **Three fallbacks disagree with the defaults they shadow** (`?? "composed"`) while `DEFAULT_AURORA_ATOMS` is imported two lines above | `.j` — accessors read the imported default; **no hand-copied literal survives** | — | — |
| W6·248 | **AP-22** ≡ C/C-15 | AuroraPane | MINOR (hypothesis, honestly labelled) | `inject(AURORA_ATOMS_KEY)!` with no default | `.j` | with W6·247 | labelled a hypothesis by its own record — preserved as such |
| W6·249 | **AP-24** ≡ D/N-6 (first half) | AuroraPane | MINOR | The italic face **clips its own terminal glyph** | `.j` — **dies with W6·241's deletion** | with W6·241 | indistinguishable-at-rest → residue U-8 |
| W6·250 | **AP-26** (reader-DU missed #5, ADOPTED) | AuroraPane | MINOR | The producer reserves the indicator gutter **unconditionally** | `.j` — **the interim decision is recorded either way**; *"the interim must be reverted when the marker inks"* | reverts when W6·239 lands | — |
| W6·251 | **AP-27** (reader-DU missed #6, ADOPTED) ≡ **PS-22** (aurora limb) | AuroraPane ≡ PreviewStrip | MINOR | The `PreviewStrip` is `aria-hidden="true"` with **no text equivalent** — the elision count is unavailable to AT | `.j` — an sr-only / `aria-describedby` sentence **derived from the same stops**. PS-22's aurora limb is an **identity FOLD** | with W6·236 | name-pollution reading rejected (C negative proof 5) |
| W6·252 | **AP-30(b)+(c)** (reader-DU missed #3/#4 + D/C-2) | AuroraPane | MINOR | Voice/IA cluster — the group is unnamed and the leaf voice over-promoted | **(a) → X.W7.g (OM-15 REDUCE class); (b)+(c) → `.j`** (the re-composition names the group and demotes the leaf) | with W6·240 | — |
| W6·253 | **AP-33** (reader-DU missed #7, ADOPTED) | AuroraPane | INFO | `--select-font` is a **dead DEMO token** | `.j` — a one-line demo deletion in the style sweep. **K-13 RETRACTS the `--select-font` producer packet** — no relay may carry it | — | retraction is binding: *"we should not mail a producer about defects that do not exist"* |
| W6·254 | **PS-11** ≡ D-6 | PreviewStrip | MINOR | On `/atmosphere` the chip alone occupies the `SelectItem` `#description` slot | **`.j` — the AuroraPane row re-composition owns this anatomy (beside AP-14)**; the one-line alternative *"must be decided INSIDE `.j`'s re-composition, not beside it"* | with W6·244 | "identical pink family" limb stays **UNPROVEN** (seed-dependent) |

#### 1j·2 — the preview-chip apparatus (PREVIEW-CHIPS packet)

**ADJ-1 LOCK (`PreviewRamp.md`, binding).** Two adjudicated records order **opposite fates** for
`[data-color-surface]` — GenerateControls' `G8` deletes it, the PreviewRamp seat needs it. **The
rule binds whichever wave lands second**; it is a sequencing rule, not a defect (§0a n.12).

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·255 | **R-1** ≡ D-1/C-1 ≡ **MC-1** (identity-fold, confirmed at HEAD) | PreviewRamp ≡ MixConfigBar | **BLOCKER** | **PreviewRamp renders zero instances in the shipped app**: both operand writers are glass-7 `WatercolorDot`s whose `@click`/`tag`/`aria-label` silently drop (`inheritAttrs:!1`) → `operandColors` never reaches 2 → `sample.ts:58` `null` → every `v-if` false | **Route: X-W6** (owns `MixConfigBar.vue`/`MixSourceSelector.vue`); **unit `.j2`'s "one instance renders" is the door**. Shares the producer root with W6·266 (MX-1) — **two rows, one root, both cross-cited** | first chip row | *"ColorSpaceSelector is NOT §D-pinned, so the Mix sites need no v8 hold"* |
| W6·256 | **R-2** ≡ C-2 | PreviewRamp | **BLOCKER** | The component's **only browser oracle is unreachable and unpassable** in the shipped dialect | **The reachability precondition rides R-1 at X-W6**; the stamped-dialect/oracle-repair half is X-W1's; **the whole-suite green stamp is X-W11's** | after W6·255 | both r1 BLOCKER severities **adopted unanimously across five seats**; *"a defect being scheduled does not de-BLOCKER it"* |
| W6·257 | **R-5** ≡ D-4 | PreviewRamp | MAJOR | The chip **does not join the design's lane** (structural half certain) | **Split — X-W6 (host lane composition, `MixConfigBar`) + NO-WAVE-OWNER (the chip half)** | with W6·320 | magnitudes (+2.64px/lane, 384px scroller) → §UNPROVEN |
| W6·258 | **R-8** ≡ L-4 + the k-count contract miss | PreviewRamp | MAJOR | **A second k=16 ramp sampler** lives beside the first | **NO-WAVE-OWNER** — *"neither `sample.ts` nor `mixStage.ts` is in any X wave's bounds; X-W6-adjacent — flagged to the formation boundary as a unification candidate"*. §3 n.5/n.12 cure it by adoption | after §3 | — |
| W6·259 | **R-10** (r2 missed, RR-DU) | PreviewRamp | MAJOR | The producer's `#`-slot duty | **GLASS-OWNED, BH relay**; *"do not justify `aria-hidden` by an unwired lane"* — **rides R-1's X-W6 seat work** | after W6·255 | — |
| W6·260 | **R-11** (r2 missed, BOTH readers) | PreviewRamp | MAJOR | The add affordance's glyph died and **the element's death hid the content loss** | **Route: X-W6 with R-1** — the enclosing seat hosts the glyph; the dead CSS + dead markup go together | with W6·255 | — |
| W6·261 | **R-16** ≡ C-10 | PreviewRamp | MINOR | `:data-stops="stampStops(stops)"` is an unmemoised template call — 26 lookups where 13 do | **Split — X-W6 (host halves) + NO-WAVE-OWNER (chip halves)** | with W6·236 | — |
| W6·262 | **R-17** ≡ L-6 + the dangling-labels miss | PreviewRamp | MINOR | The chip's composition lane is re-authored per host | **X-W6 (`MixConfigBar` rows)**; *the GenerateControls rows are §D-pinned — banked elsewhere* | ADJ-1 lock applies | overflow consequence → §UNPROVEN |
| W6·263 | **R-18** ≡ L-7 | PreviewRamp | MINOR | The stops→gradient string is minted **7× in two axis idioms** (`90deg` vs `to right`) | **X-W6 partial** (owns `useGradientCSS.ts` + creates `gradient/model/sample.ts`); equality held **only** by the RED e2e leg (R-2) | with W6·56 | — |
| W6·264 | **R-22** (r2 missed, RR-LC) | PreviewRamp | MINOR | The CONSUME MAP promises a `GradientVisualizer` ramp; **one site previews the choice, the other silently doesn't** | **X-W6 decision** (owns `GradientVisualizer.vue`): *wire the promised ramp or rule the promise dead* — the decision must be written | with W6·34 | — |
| W6·265 | **ADJ-1** | PreviewRamp | **LOCK (cl.2)** | Two adjudicated records order **opposite fates** for `[data-color-surface]` | **Binding sequencing rule on whichever wave lands second**; not a defect row, and may not be resolved by silent preference | binds W6·255–W6·264 and GenerateControls G8 | — |

#### 1j·3 — MX-CLUSTER, the Mix canary

**MX-CLUSTER, defined verbatim by its home record** (`wb-mix-animationcanvas.md`): *"`mixStage.ts` ·
`useMixingState.ts` · `MixResultDisplay.vue`-beyond-face · `ErrorBoundary.vue` · `demo/palettes/mix.ts`
· the two RED mix specs"* = **NO-WAVE-OWNER** today. Its own finding n.1: *"The BLOCKER's mechanism,
its phase machine, and its honest cure surface still live in files no X wave owns — re-verified at
HEAD `9c779130` against the wave files' own bounds tables."* **Every row below is `cl.3` on §3
n.5–n.11.** The CI-gating limbs of this packet are **X-W1's (CC-031)**, not W6's, and are marked.
**K-CHASSIS (`wb-mix-pane.md`) kills the corpus's eleven-times-repeated chassis cure** — the Mix
recomposition is the **scene contract** (H1).

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·266 | **MX-1** ≡ L-1 ≡ C-D2 ≡ D-D1 ≡ **MR-1** ≡ **C-1/D-5(A)** (MixPane) | animationcanvas ≡ resultdisplay ≡ mixpane | **BLOCKER** | `[data-mix-target]` (`MixResultDisplay.vue:69`, sole writer) is stamped on a `WatercolorDot` and **dropped** (`inheritAttrs:!1`, six declared props, class/style only) — **every convergence lands on a fabricated target ~194px from the well, behind a masking fallback** | **MX-CLUSTER + X.W4.g (trigger-gated) + `.j` (honest settle)**; the masking fallback (`mixStage.ts:121-124`) dies with the anchor. Eighth independent verification at r8 (render-function read: one `span`, zero slots) | first Mix row | — |
| W6·267 | **MX-4** ≡ C-D1 ≡ L-3 | animationcanvas | MAJOR | `arm()` is **not total** — four throwing sites inside `collectStage` bypass all four honest-settle branches | `.j` — totality at the seam, not a `try` | with W6·266 | — |
| W6·268 | **MX-5** ≡ D-D2 | animationcanvas | MAJOR | `getContext("2d") === null` is **the one abort path that never discharges the forward edge** — the family's single genuinely silent strand | `.j` — the fifth branch must settle | with W6·267 | — |
| W6·269 | **MX-6** ≡ L-4 ≡ D-D6 ≡ **B-1/C-2(L)** (MixPane) ≡ MX-39 | animationcanvas ≡ mixpane | MAJOR | Pigment travels source→canvas **as JSON in DOM attributes** while the typed truth sits one prop away; **the operand budget is enforced three times in disagreement** (writer 4, reader 12, strategy `minLen`/`maxLen`) | `.j` (writer) + MX-CLUSTER (reader); **X-W7 owns the palette-side writer** | with W6·309 | — |
| W6·270 | **MX-8** ≡ C-D4 ≡ **MR-11** | animationcanvas ≡ resultdisplay | MAJOR | **The ≥900ms settle is announced to nobody** — zero `aria-live`/`role="status"`/`role="alert"` in the whole mix tree; *"the animation IS the progress"* leaves no non-visual channel | `.j` — `onSettled` is the correct seam. **K-15's PRM residue routes HERE, for all users** | with W6·297 | — |
| W6·271 | **MX-9** ≡ L-2 ≡ C-D6 | animationcanvas | MAJOR | PRM re-derived through `useBreakpoint` from a **second glass-ui subpath** while `motion-core` — imported on the next line — owns the concept as a refcounted singleton | `.j` + the `.e` PRM idiom (W6·150); `useReducedMotion` is unconsumed repo-wide | after W6·150 | four-parallel-homes table verified |
| W6·272 | **MX-10** ≡ D-D9 ≡ L-2c | animationcanvas | MAJOR | PRM **sampled once at arm**; reduce engaged mid-narration is ignored (`respectReducedMotion:false` makes the host's ref a constant) | `.j` — *"the missing 3-line watcher (stop + settle on flip) is the correct expression"*; NEW gate candidate **N-6** | with W6·271 | the posture is right (superlative 3) — the row is the gap, not the design |
| W6·273 | **MX-12** ≡ L-6 ≡ C-D9 | animationcanvas | MAJOR | Backing store **sized to the pane's scroll extent, never released**; the file's two clears disagree on coordinate space (attribute px through the dpr transform vs CSS px) | `.j` — one coordinate space, one release; NEW gate candidate **N-17** | with W6·276 | — |
| W6·274 | **MX-13** ≡ L-9 ≡ D-D5a | animationcanvas | MINOR | The motion timeline is **three private module constants outside the producer's duration/tempo register** (`--motion-weight: 0.618` verified shipped) | `.j` — *"the CSS PRM neutralizers cannot reach JS constants — exactly why MX-10's hand gate must exist"*; **the "2.7×" multiplier stays dead (K-8, PIN 6)** | with W6·272 | — |
| W6·275 | **MX-16** ≡ D-D8 | animationcanvas | MAJOR | Geometry **sampled once at arm**; resize/zoom/reflow mid-narration unhandled (no ResizeObserver) | `.j` — **MX-38 is the producer-shaped cure** | with W6·280 | — |
| W6·276 | **MX-33** ≡ reader-DU MISS-5 (prior-round identity) | animationcanvas | MINOR | **The canvas pins and inflates the very `scrollHeight` it measures**; the abspos canvas holds the scroll extent up after `done` | `.j` — release on idle **and** on deactivate | with W6·273 | — |
| W6·277 | **MX-18** (carried, re-verified) | animationcanvas | MINOR | `convergeCss()` duplicates `wellColor` with **divergent, non-interchangeable fallbacks** (`null` → honest settle vs `var(--muted-foreground)`) | `.j` — one selection, one fallback pole; *"the display's fallback would detonate MX-4 if it ever crossed"* | with W6·267 | — |
| W6·278 | **MX-19** ≡ **MC-20** ≡ **wb-gradient-pane C-2** (identity-fold) | animationcanvas ≡ configbar ≡ gradientpane | MAJOR | The Mix verb has **two homes with divergent affordances**: the bar gates `:disabled="!canMix"`, the dock action is **permanently enabled** and silently swallowed | `.j` — `DockAction.disabled?` exists; one gate, both homes | with W6·319 | — |
| W6·279 | **MX-31** ≡ **D-1(C)** (MixPane) | animationcanvas ≡ mixpane | **MAJOR (ESCALATED against both readers — PIN 7)** | **No deactivation gate + no-op clears = a persistent pigment smear on reactivation, not bounded waste**; `<KeepAlive>`, sole teardown `onBeforeUnmount`, **`onDeactivated` = 0 repo-wide** | **`.j` gate j2**; the watcher-totality + lying-comment limbs ride the same row | with W6·273 | **both current readers overruled — their "bounded waste" reading preserved as dissent** |
| W6·280 | **MX-38** ≡ reader-DU MISS-1 (ADMITTED, cure rescoped) | animationcanvas | MAJOR | **glass-ui 7 ships the exact Canvas2D lifecycle substrate this component hand-rolls, and NO demo consumer uses it** (`@mkbabb/glass-ui/canvas`: `useCanvas2D`/`useCanvasLifecycle`/`resolveCanvasColor`) | `.j` — consume the producer; **the cure is rescoped by the ruling table, not adopted as filed** | before W6·275 | — |
| W6·281 | **MX-40** ≡ reader-LC M7 (ADMITTED) | animationcanvas | MINOR | The composable's option types **declare writable `Ref`s no caller can supply** — a silent mutability lie | `.j` — honest surface = `MaybeRefOrGetter` + `toValue` | — | — |
| W6·282 | **MX-41** ≡ reader-DU MISS-3 ≡ **MSS-19** ≡ **DD-5** (MixPane) | animationcanvas ≡ sourceselector ≡ mixpane | MAJOR-latent | The chip remove control is a **nameless, 16px, hover-revealed button** — no `aria-label`/`title`/text, the lucide `<X>` carries no name (WCAG 4.1.2) | `.j` / X.W4.g. **MSS-19 is an IDENTITY-FOLD — every limb including the touch limb and the keyboard nuance is already banked here** | with W6·315 | LC's narrowing carried (the button's own `focus-visible:opacity-100` does reveal it) |
| W6·283 | **MX-43** ≡ reader-DU MISS-6 (ADMITTED) | animationcanvas | MINOR | The decorative overlay is stamped on the **controls z-tier** while the demo owns a NAMED ornament tier (`--z-ornament: 20`) | `.j` | with W6·314 | — |
| W6·284 | **MX-45** ≡ reader-LC M4 (ADMITTED) | animationcanvas | MINOR | **A backgrounded tab freezes the reveal for an unbounded time** — `onSettled` fires only inside a frame | `.j` — settle on `visibilitychange`, not only in-frame | with W6·270 | — |
| W6·285 | **MX-21** ≡ C-D7's surviving observation | animationcanvas | MINOR | Per-frame `getContext` re-acquisition for a context obtained at arm | `.j` rider. **BOTH readers' kills of C-7's transposition cure UPHELD (K-14)**: `arm()` never sets `style.width`, so `clientWidth` is live layout truth — **caching it entrenches MX-16** | with W6·275 | — |
| W6·286 | **MX-23** ≡ L-8 ≡ C-D8 | animationcanvas | INFO | Dead public surface — a consumer-less constants re-export and a discarded `{ stop }` return | `.j` rider | — | — |
| W6·287 | **MX-29** (carried, re-verified) | animationcanvas | MINOR | `arm()`'s PRM and no-canvas early returns **leave the previous arm's `Stage` installed** | `.j` rider — *"held by accident (`loop.stop()` precedes); one line makes it structural"* | with W6·272 | — |
| W6·288 | **MX-28** ≡ D-D4 | animationcanvas | **INFO (ruled — both readers overruled)** | Built against the composition the ratified canon superseded (P122 golden trough; *"Card … never the default page primitive"*) | **"The recomposition IS `.j`'s programmed work (`W6.md:310-315`, 'Mix canary')"** — booking it again would double-book programmed adoption | — | **both readers' MAJOR preserved as DISSENT, verbatim** |
| W6·289 | **MR-5** | resultdisplay | MAJOR | **Reset unmounts the activated control; focus → `<body>`**; `.focus()` census 0 (WCAG 2.4.3) | `.j` — focus lands somewhere named; NEW gate candidate **N-18** | with W6·270 | — |
| W6·290 | **MR-8** | resultdisplay | MAJOR | The result region renders **AFTER** method/commit, inverting OBC:43 + VC:48 (source-order certain) | `.j` (**pairs MR-23**) | with W6·291 | — |
| W6·291 | **MR-23** | resultdisplay | MINOR | **No pre-result state** — the region does not exist until the first mix, against VC:48's permanent inspector | `.j` | with W6·290, one edit | — |
| W6·292 | **MR-22** | resultdisplay | MINOR | The region is declared **solely through the sanctioned 8% tone step**; the geometry job was never done | **MX-CLUSTER + `.j`** — **K-8: the cure must be geometry**; *"MR-34 is the recipe/roster face of this same surface; they land in one edit"* | with W6·295 | — |
| W6·293 | **MR-24** | resultdisplay | MINOR | Save emits into a void — **no register** | **`.j` + MX-CLUSTER. LOCK (MP-7): the de-dup limb is FALSE at source — the store DOES dedupe; the "five clicks, five rows" sentence MAY NOT BE QUOTED AS FACT by any successor.** Only the no-register limb survives and is the row `.j` executes against | with W6·305 | — |
| W6·294 | **MR-30** | resultdisplay | INFO | Dead `TransitionGroup` import at **two** sites | MX-CLUSTER rider + `.j` | — | — |
| W6·295 | **MR-34** | resultdisplay | MINOR (demoted from provisional MAJOR — PIN 2 / K-19) | The mix plate is the **well-recipe-divergence class's Mix member**: `bg-well rounded-xl` is `.console-well` minus its border limb (radius arithmetic-identical) | **FOLD-TO-CLASS → EAS-12** (`wb-gradient-easingauthoringstage`, owner **X-W10**): *"cite, don't fork"* — `.j` executes the Mix member only | after X-W10's class lands | demotion recorded against the reader's MAJOR |
| W6·296 | **MR-35** | resultdisplay | MINOR (PIN 3) | The shared `.swatch-row` leave recipe (`utils.css:177-179 position:absolute`) has **no static-position guarantee** — no positioned ancestor, no coordinates written to leaving elements | **CURE HAZARD on MR-4 — cl.2 LOCK: measure R-11 BEFORE MR-4's cure lands** | **binding sequencing** | — |
| W6·297 | **MP-2** (reader-DU) | mixpane | MAJOR | **Under PRM the pane's one verb produces a below-fold DOM insertion and NOTHING else** — zero motion (by design), zero live region, zero focus move, and the narration is contractually the sole progress signal | `.j` — the settle channel must exist independent of motion | with W6·270 | filed by every pass; adopted here |
| W6·298 | **DD-1** | mixpane | MAJOR (structural half; magnitudes live-residue) | The ghost→content swap is **snapped**: `--vj-morph-collapse/-expanded` default `none` and no mix file sets them — *"the family's own height facility is declined at the exact site built for it"* | `.j`. **The chassis limb of the cure is KILLED (K-CHASSIS)** | — | magnitudes live-only |
| W6·299 | **DD-2** | mixpane | MAJOR | Both in-plate captions ride **raw `text-muted-foreground`** on `--well-bg` (a ground darker than the header's) while the certified `--ink-muted` rung ships | `.j` — one certified rung | with W6·143 | — |
| W6·300 | **DD-3** | mixpane | MAJOR (as-rescoped) | **One `h-full` pair, two designs**: below the width+aspect band a content-hug; at/above it the plate stretches and manufactures the void | `.j`. **π LAW (binding): sample the ASPECT BOUNDARY, not just 1023/1024** — *"a 1080×1750 portrait slab never runs the second design"* | π matrix must add the aspect cell | — |
| W6·301 | **DD-4** | mixpane | MAJOR | The disabled primary verb is **opacity-only in forced colors** — producer bytes carry no border/colour/weight/glyph delta | `.j` | with W6·282 | — |
| W6·302 | **D-7 (pass A, MixPane)** | mixpane | MAJOR (CONFIRMED-AS-CORRECTED) | **Save is silent**: no emit, no status ref, no template reaction, the returned `Palette` discarded; `vj-celebrate` — the house one-shot feedback family — is consumed by three components and never by mix | `.j`. **The "always mints"/"five rows" phrasing is CORRECTED (see W6·293's lock)** | with W6·293 | — |
| W6·303 | **E-3 ≡ D-3(C)** | mixpane | MINOR | **One key, two contracts** in a parent/child pair: `inject(LIBRARY_PORT_KEY)!` vs `inject(...) ?? []`; the provider is unconditional above every pane — **the child's guard is dead masking** | `.j` — one contract; the deactivation family (W6·225/W6·279) lands with it | with W6·313 | — |
| W6·304 | **E-4** | mixpane | MINOR | MixPane is a **courier for `CSS_COLOR_KEY`** — injected solely to prop down; the child guards with silence | `.j` — *against the project's own injected-not-prop-drilled law* | with W6·303 | — |
| W6·305 | **MP-4** (reader-DU) | mixpane | MINOR-latent | The add-slot ghost is **doubly enrolled in the forced-colors tier-1 exemption**, so the ONE affordance out of the empty state keeps sub-threshold ink | `.j` | with W6·301 | — |
| W6·306 | **MP-5** (reader-DU) | mixpane | MINOR-latent | The chip remove button carries **`z-popover`** (a portalled-overlay rung) on an in-well control — the **second** z-tier misuse in this feature | `.j`; **MX-41 adjudicates the same button without this limb — recorded as an addition, not a re-book** | with W6·282 | — |
| W6·307 | **MP-6** (reader-DU) | mixpane | INFO | `mode="out-in"` is **dead configuration** — a bare `v-if` with no `v-else` never has two children; a re-mix reassigns without a vnode-type change | `.j` | — | the parent's comment narrates an ordering produced entirely elsewhere |
| W6·308 | **MP-7** (reader-LC) | mixpane | **CORRECTION TO BANKED MR-24 (cl.2)** | The dedupe limb of MR-24 is **false at source** | **Recorded under the anti-rename law as a CORRECTION, not a new booking**; binds W6·293 | binding | — |
| W6·309 | **F-10 + F-12** | mixpane | MINOR/INFO | PaneHeader's unscoped block defines its parents' scroll host (*the comment admits the inversion verbatim*); the 9-pane chrome literal | **NO-WAVE-OWNER** (with B-2/D-5: `mix.ts` has zero importers in its own directory — census exact) | formation boundary | — |
| W6·310 | **MSS-6** ≡ D-7/L-19 | sourceselector | MAJOR | Three tw-animate utilities **replace the producer's complete `.disclosure-content` recipe** (48-stop `--spring-smooth` + PRM arm) from `@layer utilities`; `overflow-hidden` is a pure duplicate | `.j` — consume the producer recipe; **cure soundness carried (DU's addition)** | with W6·271 | — |
| W6·311 | **MSS-8** ≡ D-18 + C-10 + DU-missed-3 | sourceselector | MAJOR (composite) | **The operand floor is authored twice with two values** (`MIN_COLORS = 1` vs `canMix >= 2` vs canon's 2-12), making **n=1 terminal in colors mode** | `.j`; DU's paired find — *the only sentence anywhere stating the palettes-mode floor* — rides it | with W6·269 | — |
| W6·312 | **MSS-11** ≡ D-17 | sourceselector | MAJOR | The disclosure is **unbounded** and evicts COLOR SPACE / HUE METHOD / Mix **below the fold** of the pane's own scroll container | `.j` — DU's narrowing carried | with W6·312 | — |
| W6·313 | **MSS-14** ≡ L-6 | sourceselector | MINOR | Parent asserts the port, **child masks its absence four lines of feature-tree away** → the empty-state lie | `.j` — *"One prop ends it; the child is otherwise fully controlled"* | with W6·303 | — |
| W6·314 | **MSS-17** (DU-missed-1, ADMITTED) | sourceselector | MINOR | **The mode swap — the only thing the component's one working control does — has no transition** while the house charter names `vj-morph` for exactly this | `.j` | with W6·298 | — |
| W6·315 | **MSS-21** ≡ D-3 (as narrowed by K-5) | sourceselector | MINOR | The colors-mode empty state **was designed** (the seeded-ghost invitation register) but carries **no copy** and no working affordance | `.j` + X.W4.g — a caption-ladder cure | with W6·255 | K-5's narrowing binding |
| W6·316 | **MSS-24** ≡ C-13/D-29 | sourceselector | MINOR | `SelectedColor.source` is **write-only** (read by a `:title` the producer discards); `data-mix-colors` re-runs `JSON.stringify` **per palette per render** | `.j` | with W6·269 | — |
| W6·317 | **MSS-32** ≡ D-8 | sourceselector | MINOR | **Two label registers for the same job ten lines apart** | `.j` — corroborated by the sibling seat's PIN 3 | with W6·299 | — |
| W6·318 | **MSS-33** ≡ D-9 (harmonized by R-6) | sourceselector | MINOR | The disclosure trigger is a **~29.6px tap target** | `.j` **+ BH-relay rider**; *"the 'consume the producer's unused 44px floor' framing is dead"* — explicit consumer floor **or** producer ask | with W6·312 | — |
| W6·319 | **MSS-7** ≡ D-25/L-22/C-6 | sourceselector | MAJOR | **`MAX_COLORS` is enforced nowhere the producer doesn't swallow** — the reducer appends unconditionally, the only guard rides a dropped `:disabled`, the second add path consults nothing | **NO-WAVE-OWNER (MX-CLUSTER)** until §3 n.6 adopts `useMixingState.ts` | after §3 | — |
| W6·320 | **MSS-13** | sourceselector | MAJOR | The **zero-colour-palette synchronous TypeError** under `repeat`/`distribute` | **MX-CLUSTER → `.j` on bounds adoption** (§3 n.6/n.9); carries no `X-W6` token today — recorded so its absence is a result | after §3 | — |
| W6·321 | **MC-2** ≡ reader-LC L-R4-1 (ADMITTED — **PIN 3, the corpus's strongest unbanked row**) | configbar | MAJOR | **The preview chip (piecewise path, N→k) and the Mix verb (running weighted centroid, N→1) compute different mathematical objects; they coincide only at N=2** — and the file's own truth-law prose asserts identity | `.j` — either one object or the prose dies; NEW gate candidate **N-20** | with W6·255 | — |
| W6·322 | **MC-3** ≡ D·R6-2(a)+(b) | configbar | MAJOR | **Four hard heights collide with the coarse-pointer register** — `h-10` dead at both ends, `h-9` ×3 defeats the 60px register / 44px floor | `.j` via X-W4.b; **the `h-9 ≡ size="sm"`-as-magic-number species FOLDS to the gradient class** — cite, don't fork | with W6·242 | PIN 7 |
| W6·323 | **MC-4** ≡ D·R6-2(e) | configbar | MAJOR | The caption:value type ratio **inverts ~34% between pointer classes** because two tokens obey different laws (viewport-fluid + `--ui-scale`-blind vs scale-riding) | `.j` under X-W10's type law | with W6·322 | ownership split recorded in-record |
| W6·324 | **MC-7** ≡ D·R6-8 | configbar | MAJOR | An **unmeasured F034-b site** — the Size-mismatch section enters/leaves in one frame with no `<Transition>` while the sibling plate IS wrapped, and the prescribed cure family exists verbatim | `.j` | with W6·314 | — |
| W6·325 | **MC-8** ≡ C·R6-4 ≡ **PreviewRamp R-3** (IDENTITY-FOLD) | configbar ≡ PreviewRamp | MAJOR | **The four-arc hue quartet collapses to ≤2 distinct ramps everywhere and exactly 1 in the shipped default** (oklab), while the Hue Select ships enabled and fully described | `.j` (owns `MixConfigBar.vue`) **AND creates `test/interpolation-subset.test.ts`**; the exact fixing predicate already exists (`PICKER_CHANNELS[space].some(m => m.hue)`) | with W6·34 | *"deleting options is NOT licensed"* — CSS Color 4 semantics |
| W6·326 | **MC-16** ≡ D·R6-9 | configbar | MINOR | **Two left rails 13px apart** — caption on the border-box rail, value on the content-box rail | `.j` | with W6·240 | arithmetic-exact from producer bytes |
| W6·327 | **MC-17** (reader-DU miss, ADMITTED) | configbar | MINOR | `gap-2` **pins the ONE interval that overrides a `--ui-scale`-aware producer declaration** (6px fine / 9px coarse, pinned at 8px; utilities beats `@layer components`) | `.j` — *"sharper than the six rounds' layout-wrapper gap rows"* | with W6·322 | — |
| W6·328 | **MC-18** ≡ L-L2-4 ≡ **F-5** (MixPane) | configbar ≡ mixpane | MINOR-latent | `PickerSpace` is a bare `SpaceId` alias, so `colorSpace` admits **8 states this component cannot render** (17-member alias vs 9 offered rows) | `.j` rider; sole writer is this component's own emit | with W6·76 | reader-LC's downgrade adopted |
| W6·329 | **MC-21** ≡ C·R6-5 / L-L-6 (reach limb) | configbar | MINOR | All three Select handlers typed with reka-ui's `AcceptableValue` — **four union members wider than the vocabulary glass-ui throws on** (`isSelectionValue` TypeError) | `.j` — retype; **declared devDep, so NOT an undeclared-dependency defect** (reader-LC's correction adopted) | with W6·246 | — |
| W6·330 | **MC-24** (reader-DU miss, ADMITTED) | configbar | MINOR | `font-medium` **restates the producer's own `font-weight: 500`** — a seventh dead declaration | `.j`; `font-display` on the same line is **NOT** redundant (`font: inherit` leaves family to the consumer) | — | registry-checked distinct from AL-M6/B-3/A-10 |
| W6·331 | **MC-25** (reader-LC miss, ADMITTED) | configbar | MINOR | **One of thirteen ramps is computed twice per invalidation** — two computeds issue byte-identical sampler calls | `.j`, **collapsing under R-16's one-memo cure** | with W6·261 | distinct from banked R-16 / R-8 |
| W6·332 | **MC-26** (reader-LC miss, ADMITTED) | configbar | MINOR | The color-chips **barrel import drags the unused `PreviewStrip` SFC + scoped stylesheet into the mix route's graph** (CSS is a module side effect) | `.j` rider — chunk magnitude R-7 | with §3 n.12 | — |
| W6·333 | **MSS-3 + MSS-16 (rider)** | sourceselector | MINOR | Identity churn `${sc.css}::${i}` | **→ X-W7 with the MSS-16 rider** — recorded here because the file is W6-bounded and the edit must not collide | X-W7 lands after `.j` | — |

#### 1j·4 — MX-CLUSTER, the **adoption tail** (added 2026-08-28; the fold's first pass booked only the rows whose route text already names X-W6)

**Why this section exists — a recorded M-25 repair.** §3 n.7–n.13 ask W6 to ADOPT the six
MX-CLUSTER files into bounds. **The moment that adoption lands, every `NO-WAVE-OWNER (MX-CLUSTER)`
row in those files becomes a W6 row** — not only the ones whose adjudicating sentence happens to
also say `X-W6.j`. The first pass of this fold booked the latter and left the former in §5's
exclusion prose. That is the transcription-only defect M-25 names. The 38 rows below close it.
**Every row is `cl.3` on §3 n.7–n.13 and degrades to NO-WAVE-OWNER, by name, if the bounds edit is
refused** — which is the honest disposition, and is exactly what the packet's own finding n.1 says
(*"the mechanism, the phase machine, and the honest cure surface still live in files no X wave
owns"*). Ids are banked and unchanged; nothing here is re-minted.

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·398 | **MX-2** ≡ reader-LC M1 ≡ **D-2(C)** (MixPane) | animationcanvas ≡ mixpane | MAJOR | On every `done → mixing` **re-mix** the anchor is structurally absent at measure time: `mode="out-in"` + `.vj-morph-leave-active` = `--duration-fast` defer the well ~200ms past the `flush:"post"` measurement; the `:169-170` docstring is false after mix one | **PIN-3 LOCK (binding on any W6·266 cure): the anchor must live OUTSIDE the swapping Transition** (the persistent `.mix-plate` root is one attribute away) **or the measurement must stop being a single post-flush instant.** K-3/K-4 kill all three corpus cures (registry, prop, `useTemplateRef`) — they register on MOUNT and still miss | **before** W6·266's cure is authored | reader-DU dropped this find; reader-LC kept it — round-over-round loss recorded |
| W6·399 | **MX-7** ≡ L-5 (boundary half) | animationcanvas | MAJOR | Pane crashes are **structurally console-invisible** (`ErrorBoundary.vue:59-69` `return false`, no `console.error`/telemetry) and the ONE boundary is **GRID-wide** while its copy says *"This PANEL…"* — mis-describing its own blast radius | `.j` on §3 n.10. Two cures, both cheap: one observability line before `return false`; per-pane boundaries **or** honest copy. **LOCK (K-1, cuts both ways): the boundary is invisible only to a console scraper, never to the user — no cure may cite `consoleErrors: []` as evidence of health** | with W6·267 | admin-token reproduction stays UNPROVEN (R-2) |
| W6·400 | **MX-32** ≡ **MR-10** (identity, one row) | animationcanvas ≡ resultdisplay | MAJOR | **The only arm a real user can reach has NO text alternative**: N producer-`aria-hidden` spans (`tag="div"` and `:title` both dropped) + an `aria-hidden role="presentation"` gradient strip; total accessible text = the word *"Result"* | `.j` + X.W4.g. **The css list already exists** (it feeds the clipboard, `:43-46`) and is never rendered — the cure is to render what is already computed. **Compounds W6·270 (MX-8) into a complete non-visual dead end**; the two land together | with W6·270 | the X.W4.g face carve is **insufficient alone** — recorded |
| W6·401 | **MX-14** ≡ L-10 ≡ C-D3b | animationcanvas | MINOR | **Zero tests bind these 507 lines**; the duplicate twin is covered only through a `test/`→`demo/` inversion | `.j` — *"the pure half is trivially Node-testable — the cheapest real repair in the corpus"*; the author built the testable seam and no one used it (superlative 8) | with W6·273 | — |
| W6·402 | **MX-15** ≡ L-3b | animationcanvas | MINOR | *"mix or throw"* implemented **twice with two error strings**; the demo adapter erases the library's `Result` channel at the first boundary | `.j` — one adapter, one error taxonomy; joins W6·267's totality decision | with W6·267 | — |
| W6·403 | **MX-34** ≡ reader-DU MISS-8 (ruled PIN 5) | animationcanvas | MINOR | The arrival hand-off **steps discontinuously in composite alpha and edge profile, scaling with operand count** (N 0.92-alpha discs composite to `1−0.08^N`); the `p >= 1` trail guard is **dead code** | `.j` — `drawStage`'s. **LOCK: per-drop kinematics are genuinely continuous (superlative 9, rescoped) — a cure that "fixes the seam" by changing the per-drop hand-off breaks a proven property** | with W6·403's own dead-guard deletion | reader-DU's INFO grade preserved as dissent; the two readers computed different quantities from the same lines (PIN 5) |
| W6·404 | **MX-42** ≡ reader-DU MISS-4 | animationcanvas | MINOR | The ghost plate is **guard-then-alpha**: `opacity: 0.55` over a subtree whose only text already rides the muted rung, for the full narration window — while the same workbench states the prohibition verbatim four files away (`MixSourceSelector.vue:186-189`) | `.j` (plate styling is beyond the X.W4.g face carve). **Species note carried: whole-surface state opacity is MILDER than per-text alpha** — the row is the muted×0.55 product over 900ms, not the idiom | with W6·292 | contrast ratio = R-12, live-only |
| W6·405 | **MX-22** ≡ D-D12 | animationcanvas | INFO | A fresh `CanvasGradient` **per disc per frame** (≤38/frame at cap) | `.j` rider — *"not a present bottleneck"*; recorded so no successor re-derives it as one | with W6·285 | — |
| W6·406 | **MX-24** ≡ L-11 | animationcanvas | INFO | A pure, Vue-free model **shelved inside `composables/`** (its own docblock: *"No lifecycle, no clock"*) | `.j` rider — the mis-homing is what makes W6·401's zero-tests row sting | with W6·401 | L's non-`use*` count was a labelling slip (reader-DU) |
| W6·407 | **MX-36** ≡ reader-LC M8 (one limb demoted) | animationcanvas | INFO | `collectStage` **re-parses the invariant pool pigment once per drop** (≤12 redundant `parseColorIn` pairs) and derives the pool colour as operand 0's ramp endpoint instead of the parse it already has | `.j` rider — hoisting **also shrinks W6·267's throw surface ~24 → 13 calls**, so it is a totality lever, not only a perf one. **The order-dependence limb is DEMOTED to theoretical — no demonstrated asymmetry exists and it may not be quoted as one** | with W6·267 | limb demotion recorded |
| W6·408 | **MX-37** (carried, EXTENDED with D-11) | animationcanvas | INFO | The `data-mix-*` contract **exists ONLY as prose that is now false**, and the component's declared props are not its real inputs (four data attributes on unowned subtrees + `parentElement`'s identity) | `.j` — **dies with W6·266/W6·398's cure**; D-11's collapse-the-wrapper cure stays KILLED (K-9) | with W6·398 | — |
| W6·409 | **MX-44** ≡ reader-DU MISS-7 | animationcanvas | INFO | `layoutCenter`'s offsetParent walk **terminates only because `MixPane.vue:62`'s Card happens to carry `relative`** — remove it and the walk runs past `<body>`, silently mis-placing every drop; no throw, no test | `.j` — one assertion or one comment. **Same silent-coupling family as W6·266; all three axes filed the dependency as an EXONERATION** (superlative 2's rider: it holds, and nothing binds it) | with W6·309 (F-10/F-12, the same `relative` literal) | — |
| W6·410 | **MP-1** (merged, both readers' missed #1) | mixpane | MAJOR | **The "Size mismatch" control is a user-facing choice between a silent wrong answer and a pane-destroying crash**: `resultLength = discard ? minLen : maxLen`, so the `=== 0` guard protects only `discard`; `repeat` → `colors[0 % 0]` = `colors[NaN]`, `distribute` → `hi = Math.min(1,−1) = −1` → both read `.css` and throw a **raw TypeError, no code, no taxonomy** | `.j` on §3 n.11 (`demo/palettes/mix.ts` is **CONTESTED** — see §3). **LOCK: the cure must be totality in `getColorAtIndex`/`mixPalettes` (honest guard or typed refusal), NEVER a consumer try/catch** — the masking-fallback ban, `W6.md` H3's standing rider. `!`-audit rider on the same file (5 sites defeat `noUncheckedIndexedAccess`) | before W6·320 (MSS-13 is this row seen from the operand end) | graded MAJOR not BLOCKER: the precondition is representable and manufacturable by this pane's own Save, **not proven on a first-time user's path** |
| W6·411 | **F-1** (third instrument) | mixpane | MAJOR | **N-ary mix is order-dependent in the polar spaces** and operand order = click order, with no order semantics declared anywhere in the UI. Arbiter's own node run: `#ff0044+#22cc88+#4400ff` in oklch/shorter → **max channel delta 276.6** (different colours entirely); hsl/shorter 225.6; **oklab (shipped default) 0.00 exactly** | `.j` on §3 n.11. **Cure adjudicated separately: the fold is a correct weighted average in cartesian spaces — the defect is offering hue-arced spaces through an order-sensitive fold undeclared.** **GATE RIDER (binding): any born-RED gate must pin an ASYMMETRIC specimen set** — the seat's own `red+yellow+blue` hsl case cancelled to 0.00 by hue symmetry | with W6·325 (MC-8, the same hue-arc surface) | reachability: `INTERPOLATION_SPACES[0]` = oklch, one dropdown from the default |
| W6·412 | **D-3 (pass A, MixPane)** | mixpane | MAJOR | **The result is never invalidated**: `useMixingState.ts` imports `{ref, computed}` only — no `watch`; `mixResult` is written only in `startMix`, nulled only in `reset`/`clearSelection`, so changing space / hue / strategy / operands / mode leaves a **stale result enabled, copyable, savable** | `.j` on §3 n.8. **Unbanked across all 178 adjudicated rows** (readers' sweep, arbiter spot-confirmed) — recorded so its novelty is a checked claim, not an assertion | with W6·293 (the save register) | — |
| W6·413 | **F-4 / D-2(L) / E-5** | mixpane | MAJOR | The library publishes **two incompatible failure algebras** (`Result.error` vs `ParseResult.diagnostics`), the demo erases both through throwing adapters 8 lines apart, and **no combinator reaches ANY of the seven published subpaths** (all seven verified); 18 hand-rolled unwrap adapters, four wrapping `mixColors` | **Library half NO-WAVE-OWNER** — *"X-W9 owns only parser R1; this is adjacent, not identical"* (cl.4). The **demo-side unwrap-policy half is `.j`**, and is the same identity as W6·71/W6·72's one-unwrap-policy cure at the gradient end — cited, counted once | X-W9 decides the publish half first | — |
| W6·414 | **F-14** | mixpane | MAJOR | `test/` imports demo source (10 files) **and `test/` is typechecked by NO tsconfig program** | **Coverage half `.j`** (on §3 n.12/n.13); **program-membership half NO-WAVE-OWNER** (cl.4 split) | with W6·401 | — |
| W6·415 | **E-2** | mixpane | MAJOR | The injection Symbol is **co-homed with the god provider**: a value import of `LIBRARY_PORT_KEY` drags **29 modules / 107,417 B / 34.2% of MixPane's static closure** (admin API + auth + transport), while the in-repo counter-proof (`demo/color-session/keys.ts`, 1 module / 1,474 B) is consumed by the SAME file one line later | **NO-WAVE-OWNER** — `usePalettePorts.ts` is in **no** X bounds and §3 does **not** ask for it (cl.4 edge, named so the absence is a result). Cure shape already scoped for a sibling port at `MT-AU1 §3(7)`; **W6·17 (GV-5) is the same mechanism at the gradient end** — one identity class, two files | formation boundary | reader-LC reproduced the walker byte-exact at HEAD |
| W6·416 | **MR-3** | resultdisplay | MAJOR | The nine-line scoped block **defeats `vj-morph` in all three directions** — enter list truncated, leave list truncated, ghost `(0,2,0)` opacity pin from frame one (`startMix()` writes result+phase in ONE synchronous call) | `.j` on §3 n.9. **LOCK: this is the NO-PREFERENCE cascade kill and it does NOT revive D-33 (K-14)** — under PRM the producer's Guard 2 governs and is ruled CORRECT | with W6·298 (DD-1, the same snapped swap) | — |
| W6·417 | **MR-4** (+ the **MR-35 rider**, W6·296) | resultdisplay | MAJOR | `vj-enter` is **structurally dead** (fresh-mount on every settle, no `appear`); the index `:key` is latent | `.j`. **CURE-HAZARD LOCK (binding, from W6·296): the `.swatch-row` abspos leave limb must be fixed BEFORE this cure activates leaves here — measure R-11 in the two LIVE consumers first.** Fix the recipe ONCE, never per consumer | **strictly after W6·296's measurement** | — |
| W6·418 | **MR-6** | resultdisplay | MAJOR | `useClipboard` **half-consumed**: `CopyResult` discarded, no `onCopyError`, failure arm timer-less (terminal), `invalidate()` never called | `.j`. **The primitive is right and its shape is the cure** (S-4: discriminated `CopyResult` with NAMED reasons, a generation counter, `timeoutMs`, `onScopeDispose`) — **consume it, do not re-author**. Same identity class as W6·120 (M-ESS-3) and W6·43/W6·46 | with W6·43 | — |
| W6·419 | **MR-7** | resultdisplay | MAJOR | `MixResult` is an **optional bag with five masking guards** and a shipped blank-bar consequence | `.j` — the model states what it has or it does not exist; **W6·433 (MR-39) is the same masking shape in the math module** | with W6·412 | — |
| W6·420 | **MR-9** | resultdisplay | MAJOR | The rack **wraps at SEVEN faces per 292px row while the strip stays one row** — the two depictions of one ordinal decouple at the canon's named count of 12 | **Split — `.j` (the region edit) + X-W7.c (strip consolidation)** (cl.4) | with W6·290/W6·291 | the **400%-zoom arm is UNCOVERED across eight reads** (R-5) and moves this row first |
| W6·421 | **MR-14** (+ K-2b) | resultdisplay | MINOR | Three **28×28** actions; `compact` IS the producer's own touch-floor opt-out — **and `--dock-compact-control-size` is declared NOWHERE in the shipped package**, so compact is ~28px inside a dock too | `.j`. **Cure menu, both grounded: set the declared-but-unset `--dock-compact-control-*` at the plate root (ActionBarToggle precedent) OR drop `compact`. N-4's "swap the family to `<Button iconOnly>`" is KILLED (K-18)** — the component in use already exposes the knob | with W6·422 | — |
| W6·422 | **MR-15** | resultdisplay | MINOR | `<DockSeparator />` renders **1×0** yet stamps `role="separator"` + 12px rhythm | `.j` — **cure = deletion** (OBC:77 retained line `none`); a real separator would MINT a defect | with W6·421 | LC-prior's MAJOR revives on an AT-navigation witness |
| W6·423 | **MR-16** | resultdisplay | MINOR | The *"Result"* eyebrow — Fraunces ITALIC bold uppercase, a **five-utility re-mint of `.section-label`** against two sibling variants | `.j` under X-W10's register law (cite, don't fork); **W6·317 (MSS-32) is the sibling half — one edit** | with W6·317 | — |
| W6·424 | **MR-19** | resultdisplay | MINOR | `title="Copy color"` **unconditional and wrong across the palette half**; `title` is the sole accessible name ×3 | **`.j` + X-W10/X-W4** (register canon) — cl.4 split; the retired native-`title` register is W6·47's (GAB-12) identity | with W6·47 | — |
| W6·425 | **MR-20** | resultdisplay | MINOR | The rack is writing-mode aware, the **strip hard-codes `to right`** — an RTL contradiction inside one region | **`.j` + X-W7.c** (cl.4); same physical-axis species as W6·106/W6·136 | with W6·263 | — |
| W6·426 | **MR-21** | resultdisplay | MINOR | The value artifact carries `break-all` and **no LTR isolation** | `.j` — **joins the bidi family** (W6·80 / W6·135) and takes the same one-attribute cure | with W6·80 | — |
| W6·427 | **MR-25** | resultdisplay | MINOR | `mixStage`'s fabricated target `r: 28` **encodes the colour-branch well — 40% wrong for every reachable mix** | `.j` rider on §3 n.7 — **dies with W6·266's fallback deletion**, never re-tuned | with W6·266 | — |
| W6·428 | **MR-26** | resultdisplay | MINOR | **Verb-scope inversion**: the plate's "Reset" discards only the result; the clearing verb exists **only on the dock** | **`.j` + X.W4.d** (cl.4 split) — the dock half is the shell's | with W6·289 | — |
| W6·429 | **MR-27** | resultdisplay | MINOR | The ghost **under-declares the palette answer by a factor of N** (one well vs N faces + strip) | `.j` — dies inside W6·298's reserved-geometry cure | with W6·298 | — |
| W6·430 | **MR-36** (PIN 4) | resultdisplay | MINOR | `mixedOrThrow` carries a **dead type parameter and a gratuitous double-cast**: `PickerColorIn<S>` is a narrowing BOTH call sites discard on the next line, so the generic, the import and the `as unknown as` exist to produce a type nobody consumes | `.j` on §3 n.11. **Cure: declare `AnyColor`, delete the cast.** **F-7's location diagnosis and library-promotion cure are KILLED (K-17)** — the module's demo-only type deps forbid the move as written | with W6·413 | — |
| W6·431 | **MR-37** | resultdisplay | MINOR | `PaletteMixOptions.weights` is **unexercised dead API with a contract INVERTED against the identically-named prop one directory away** (per-PALETTE here vs per-COLOR at `PaletteColorStrip`), and its first misuse **throws synchronously** into the grid-wide boundary | `.j` on §3 n.11 — delete the option **or** document + type the cardinality | with W6·430 | — |
| W6·432 | **MR-38** | resultdisplay | INFO | `demo/palettes/mix.ts` is **homed in a feature that does not consume it** — zero importers inside `demo/palettes/`; both runtime consumers live in `demo/workbenches/mix/`, which already owns a `composables/` directory | **`.j` on §3 n.11 — and this is the row that decides §3 n.11's contest**: if the file moves into the mix tree it stops being `demo/palettes/**` and the Do-NOT-touch conflict dissolves. **The mis-homing is what made a demo-only helper look like a library candidate to F-7** | **decide at the opening sitting, before W6·410/W6·411 open the file** | ≡ **B-2/D-5** (MixPane), which books the same census — one identity |
| W6·433 | **MR-39** | resultdisplay | INFO | `mixPalettes`' single-palette early return is **dead on the only route that reaches it** — and if ever reached it silently returns a byte-copy of the input, which the pane would save as *"Mixed Palette"*: **a duplicate presented as a result** | `.j` on §3 n.11 — the exact masking-fallback shape W6·419 objects to, in the module the corpus never opened | with W6·419 | — |
| W6·434 | **MSS-12** (+ LC's publish extension) | sourceselector | MAJOR | A palette that leaves `savedPalettes` stays an **invisible, un-deselectable operand `canMix` still counts** — the only `removePalette` emitter renders from `savedPalettes`, and no prune exists in either file; **publishing** a selected palette produces the same orphan through the store's `isLocal` narrowing (a *non-destructive* action) | **`.j` on §3 n.8** — the cure is a reducer watcher/prune; W6's own bounds do not reach the reducer until n.8 lands | with W6·319 (MSS-7, same reducer) | — |
| W6·435 | **MSS-15** ≡ L-8 | sourceselector | MAJOR | `demo/palettes/mix.ts` is consumed only by the mix workbench + one test; **10 test files import from `../demo/`** | **Species FOLD — banked `test/`→`demo/` inversion** (≡ W6·401/W6·414's family), **NO-WAVE-OWNER for the re-homing**; the coverage half rides `.j` on §3 n.12/n.13 | with W6·432 | — |

#### 1j·5 — PREVIEW-CHIPS, the **adoption tail** (added 2026-08-28; same M-25 repair as §1j·4)

**Why this section exists.** `PreviewRamp.md` §Addendum clause 5 is explicit and enumerative:
*"`demo/color-session/color-chips/**` (both chips + sampler + barrel), `test/preview-chips.test.ts`,
the foundation.css WHCM/print roster arm, `aurora-harmony-stops.ts`, and `mixStage.ts` are in NO
X-wave's File Bounds… The next formation boundary consumes rows R-4, R-6(+ADJ-1), R-7, R-8,
R-12(file half), R-13, R-14, R-15, R-16(chip half), R-18(residue), R-19(producer half), R-21,
R-22(map edit), R-24, R-25, R-26 **whole — zero silent drops**."* §3 n.14–n.18 (+ the new n.36/n.37)
are the bounds edit that makes W6 that consumer. The rows below are that list plus PreviewStrip's
chip-family residue, **each `cl.3`**: refuse the bounds edit and every one degrades to the named
`chip-family residue ≡ PCS-10` NO-WAVE-OWNER identity — never a silent drop, in either direction.

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·436 | **R-4** ≡ D-3 / C-5 (absorbing L-3) | PreviewRamp | MAJOR | The *"golden plate (φ² × 1em)"* holds at **no shipping viewport**: `inline-size: 2.618rem` is **root-anchored** while `block-size: 1em` is **inheritance-anchored**, and the chip is a sibling of the `text-micro` span, inheriting the SelectItem rung | **MERGED CURE, RULED (ruling E) — verbatim: `block-size: 1em; aspect-ratio: 2.618;`** — reads no length token at all, φ² by construction at every font context, and discharges D-3/C-5 **and** L-3 at once. **L-3's `inline-size: var(--phi-4)` cure is KILLED — it preserves the exact two-anchor split two other seats file as MAJOR.** Lands ONCE, in W6·438's single recipe | after W6·438 mints the one recipe | per-viewport magnitudes → §UNPROVEN |
| W6·437 | **R-6** ≡ D-5 / C-4 ≡ **PS-3** (BLOCKER-class at its own home) | PreviewRamp ≡ PreviewStrip | MAJOR | **`forced-colors: active` annihilates the chip whole**: 100% of its information is `background-image` + `box-shadow` — exactly what WHCM strips — and it sits on **no roster row**; `@media print` reuses the roster AND kills the ring, so it is a blank rectangle in print too. `aria-hidden` means nothing survives in any channel | **`.j` on §3 n.14/n.15 + n.37** (the roster arm). **ADJ-1 LOCK BINDS (W6·265/W6·464): the cure MINTS `[data-color-surface]`'s first producers while GenerateControls' `G8` is under a live deletion order.** Whichever lands first re-reads the other: chip-first ⇒ `G8`'s grep gate is amended (its "zero producers" premise is **dated evidence, not law**); `G8`-first ⇒ the chips join the roster **by class** in the same commit | **ADJ-1 sequencing is binding** | D-5's `VC:23` citation KILLED (an identity-tally exclusion, not a forced-colors grant) |
| W6·438 | **R-7** ≡ D-7 / C-6 / L-2 ≡ **PS-1** | PreviewRamp ≡ PreviewStrip | MAJOR | `.preview-chip` is **ONE recipe minted twice in two disjoint scoped stylesheets, already drifted** (six declarations, two divergences — `display`, and Strip-only `overflow: hidden`, which is load-bearing); the module's own charter forbids it; the byte-identical ring VALUE re-mints the single-owner `--card-edge`, and **the two chips are the ENTIRE violator set** against 13+ correct consumers | **HOME RULED (ruling H): a module-local `chip.css` inside `color-chips/`, imported by both SFCs** — *not* `utils.css` (the global bar is 3 consumers; there are 2, both siblings in one module) and *not* a glass variant (**KISS-banned contrivance**; `PCS K-7` already killed the glass-Strip mint). **Every cure in this family lands once, here**: W6·436's units, `var(--card-edge)`, W6·440's ring redesign, W6·446's radius hedge, W6·437's attribute. **RIDER: glass ships `./chip` first-class — the demo class is a name collision with the design system's own vocabulary; any producer-rooting is a dated BH ask with the exports census pasted, never a local mint** | **FIRST row of this section** — it is the vessel every other chip cure lands in | L-2's *"four of five byte-identical"* KILLED (six declarations, two divergences) |
| W6·439 | **R-12** ≡ C-3 / D-9 (rescoped, ruling A) | PreviewRamp | MINOR | The engine interpolates **every in-between pixel** between the 17 positionless stops, in a module whose stated rationale is *"no engine divergence"* — and the docblock calls the paint *"discrete-stop"* two lines after calling the chip **CONTINUOUS** | `.j` on §3 n.14. **The obligation is split and both halves are named: (a) the prose must stop contradicting itself; (b) the law either names the residual engine fill WITH its measured band or the design rules for hard stops — a DESIGN RULING (X-W10), never an implementation default.** **C-3's hard-band cure is REFUTED** — it collapses the STRIP/RAMP species distinction `index.ts:6-8` deliberately mints: a design deletion sold as a bug fix | design half **before** the file half | RR-DU killed the row outright; this seat kept the MINOR because the docblock contradiction is C-3's find, not D-9's — **dissent preserved** |
| W6·440 | **R-13** ≡ D-6 + the inset-veil miss (rescoped, ruling D) | PreviewRamp | MINOR | The 12%-alpha single-tone ring **fails its docstring's bounding job in both schemes** AND, painted `inset` over the specimen at 1em block-size, **veils ~12% of the plate including exactly the two operand-identity endpoint stops — the ring IS the bleed its comment forbids** | `.j` — outset ring or `outline`, in W6·438's recipe. **WCAG 1.4.11 does NOT bind** (the chip is `aria-hidden` and every row's meaning is text-duplicated) — the row survives on the design claim only. **Print rider: `@media print * { box-shadow: none !important }` kills any inset-ring cure in print** | with W6·438 | RR-DU's MAJOR preserved: *a boundary mechanism failing its stated purpose on every chip in both schemes is MAJOR regardless of normative floors* — **reopens with evidence if the ratios land and W6·255 makes the chips reachable** |
| W6·441 | **R-14** ≡ C-7 / L-1 ≡ **PS-23** | PreviewRamp ≡ PreviewStrip | MINOR | `serializeStop`'s `.replace(/ \/ 1\)$/, ")")` is **dead code against a serializer that does not exist**: at α=1 the shipped serializer emits **no suffix at all**; at α≠1 it emits a **percentage** the literal-`1` regex cannot match | `.j` — **dies inside W6·447's canonicalization**, not as a standalone edit. **The vitest oracle is structurally blind** — it builds EXPECTED with the same `serializeStop`, so the regex sits on **both sides of `toEqual`**; §3 n.18 exists for that | with W6·447 | L-1's MAJOR demoted; the doc-comment sub-claim KILLED (the comment describes the OUTPUT, truthfully) |
| W6·442 | **R-15** ≡ C-9 | PreviewRamp | MINOR | `k` has **no domain guard**: `NaN` → `[]` (truthy, violating the declared `string[] \| null`); `Infinity` → an **unbounded loop** | `.j` on §3 n.16 — `Number.isFinite(k)` floor at entry. **Unreachable through both hosts today, but `sampleInterpolationRamp` is public barrel surface** — the row is about the contract, not the reachability | with W6·445 | — |
| W6·443 | **R-21** (r2 missed, RR-LC) | PreviewRamp | MINOR | The sampler's honest-absence contract is **half-throw**: `serializeStop` sits OUTSIDE the try/catch and rides `valueOrThrow` twice, so a serialization failure **escapes a Vue computed as an exception** while the documented posture is `null` | `.j` on §3 n.16 — one posture. **Same null-vs-throw contradiction as PS-17 (W6·455) — one identity class, one cure** | with W6·455 | reachability low (mix outputs finite by construction); the inconsistency is certain |
| W6·444 | **R-24** ≡ D-12 / L-12 / C-11 (sizes corrected) | PreviewRamp | INFO | `data-stops` ships O-14 scaffolding to the production DOM — **deliberate and T-era, with no V ruling**; ceiling 9 simultaneous chips, current shipped cost exactly **0 bytes** (because W6·255 renders none) | `.j` — notation only; **D-12's 13-chip / 6.5KB sizing is KILLED** (an internal inconsistency inside one report), and the RTL premise-kills stand (`index.html` ships a deliberate, commented `dir` seam) | — | — |
| W6·445 | **R-25** ≡ D-10 (REFUTED-AS-FILED, ruling C) | PreviewRamp | INFO | The `v-if="stops.length >= 2"` guard: *"can never evaluate false"* is **false** — W6·442's `k=NaN` path returns `[]`, truthy, and **this guard is the only thing that stops it** | **`.j` — the guard STAYS and the deletion cure is RETIRED with rationale.** **OPPOSITE-EDIT LOCK (addendum law 2, binding on the wave): C-9 and D-10 prescribe contradictory edits to the same line and neither cites the other — no corpus is folded until every pair of rows touching one line is cross-read for edit-compatibility** | **read before W6·442's edit** | the INFO residual is only that the guard is redundant at default `k` under both current hosts |
| W6·446 | **R-26** ≡ C-8 + the radius-hedge miss | PreviewRamp | INFO | **Single-declaration totality**: one unpaintable stop nulls the whole `background-image` while `data-stops` still claims 17; and `border-radius: var(--radius-sm)` resolves **only** from the producer's Tailwind emit (no demo mint) | `.j` — both fold into W6·438's recipe as `var(--radius-sm, 0.25rem)`; the in-repo precedent already hedges the same token | with W6·438 | C-8's honest negative adopted: **no live trigger found** |
| W6·447 | **R-19** ≡ L-8 ≡ **PS-4** (W6·236's other half) | PreviewRamp ≡ PreviewStrip | MINOR | **Two incompatible OKLCh notations share the one `data-stops` contract** — the library serializer's `oklch(62% 0.27 9.8deg)` vs the hand-rolled unitless `oklch(0.7 0.15 220)` — and the one oracle regex parses B and can **NEVER** parse A (arbiter's run: 0 vs 1) | **Split (cl.4): the oracle-reader dialect law is X-W1's; the ONE-serialize-home for producers is `.j` on §3 n.16 + n.19.** **LOCK: this row and W6·256's dialect leg are ONE defect seen from both ends — any cure must cover all three dialect sites and re-oracle BOTH chip species** | with W6·236 | — |
| W6·448 | **R-23** ≡ D-8 + the dangling-authority miss | PreviewRamp | MINOR | The chip is an **unregistered second ornamental colour-bearing species** (canon names Watercolor/data *"the only"* one), and its **ONLY cited authority is a document that does not exist** (`docs/tranches/T/audit/w6-lane-d-record.md` → ENOENT) | **Species/register half → X-W10** (canon owns species registers; the F5–F8 laws must be re-homed or re-derived into the canon) — cl.4. **The dead-citation correction is `.j`'s** on §3 n.17: the wave edits `index.ts` anyway (W6·332/W6·450) | with W6·332 | — |
| W6·449 | **PS-5** ≡ C-1 | PreviewStrip | MAJOR | `data-stops` carries **the truth-function referent and the paint referent at once**, and above the cap they are **provably unequal** (held 8..12 / painted 7) — so the two oracles want opposite things from one attribute | `.j` on §3 n.15. **Pass-1's split-referent cure is KILLED (K-12): a scalar `data-preview-total` cannot serve an ELEMENT-WISE oracle, and above the cap the DOM would carry no full stop list at all. The sound shapes are two stop lists OR the cap moved to the host.** **REGISTERED COUPLING: the o20 seed-exact law cannot be re-authored honestly until this referent is decided** | **before W6·452's oracle authoring** | — |
| W6·450 | **PS-8** ≡ C-5 | PreviewStrip | MAJOR | **Vacuous gate**: zero tests or e2e reference PreviewStrip **by any name**, and the o14 chip leg is *structurally unable* to reach the strip species (visits Mix only; reads `backgroundImage`, which is `none` on a strip; `STRIP_SEGMENT_CAP` is module-private so no oracle could name the cap) | `.j` on §3 n.18 + n.36 — the strip oracle is **authored with** the W6·438 cure, never after. **Mutation falsifier carried: delete the paint binding and every gate stays green** — the born-RED gate must fail on that mutation | with W6·452 | CI limb ≡ X-W1 CC-031 (cl.4) |
| W6·451 | **PS-9** ≡ D-3 / C-3 (rescoped by instrument) | PreviewStrip | MAJOR | **Above the cap the preview stops tracking the control that drives it, on the SHIPPED DEFAULT**: `golden` is the one prefix-stable harmony **and it is the default**, so on first open every Preset row paints one picture for counts 8–12, distinguished only by a mask on a ~6px sliver | `.j` on §3 n.15 — **the elision becomes structural and honest (resampled range + explicit count, or plate-length-follows-data)**; the per-route prescriptions are design options, not rulings. **SCOPING LOCK: exactly ONE harmony freezes — `golden`. D-3's unscoped generalization is KILLED (K-4) and reader-LC's counter-scoping is KILLED BY INSTRUMENT (K-5).** The failure is COMPLETENESS/DISCRIMINATION, not content — *"not truthful"* overstates and may not be quoted | with W6·453 | a reader-confirmation cell inverted by the arbiter — **citation-independence event, recorded** |
| W6·452 | **PS-10** (missed-DU#2) | PreviewStrip | MAJOR | **The truncation mask makes the seventh painted stop report a colour that is not that stop's colour** — and the reported colour is a function of whatever page content sits behind the translucent menu (by scheme, route, scroll position, and the aurora field's current frame), because the plate declares **no background of its own** | `.j` on §3 n.15. **The module's own law is the gate text, verbatim: *"a chip that approximates the library output is FORBIDDEN."*** The mask **dies with W6·451's structural cue**; W6·453's well independently closes the composite-through channel | with W6·451 and W6·453 | **neither axis filed it** — both saw only imperceptibility/direction |
| W6·453 | **PS-2** ≡ D-1 + D-2(core) | PreviewStrip | MAJOR | The declared inset hairline ring **never paints** (occluded by the component's own opaque children) and the plate has **no specimen well**: the payload sits directly on translucent menu glass with **no boundary of any kind** | `.j` on §3 n.15 + n.37. **The surviving D-2 core: canon assigns this species an opaque/quiet stage + structural gutters, and the BOUNDARY — not the specimen — owes 3:1.** Print rider as W6·440. **D-2's census-as-defect-count is KILLED (K-6): a per-stop 3:1 floor would forbid the component from ever truthfully previewing a pastel or analogous palette** | **before W6·452** (the well closes the mask channel) | D-1 graded MAJOR over the axis's BLOCKER — the casualty is a decorative hairline |
| W6·454 | **PS-17** ≡ L2-7 | PreviewStrip | MAJOR | *"Sample an interpolation into k stops"* has **six homes, two identically-valued `k` constants under two names, four failure policies** — and the chip module's own row breaks its declared contract (documented `null`, actual throw on the same line) | **Consolidation half NO-WAVE-OWNER** — it spans mix/gradient/ink/chips and no X wave owns the set (cl.4). **The null-vs-throw contradiction is `.j`'s on §3 n.16** and is W6·443's identity — one cure | with W6·443 | — |
| W6·455 | **PS-18** ≡ C-6 / D-5(core) + D-7.4 + missed-DU#4 | PreviewStrip | MINOR | One golden-plate law on **two unit systems**, the literal hardcoded in both chip files, while **THREE names for the same 2.618rem quantity coexist in one served cascade** (`--phi-4` + its Tailwind bridge, glass's shipped `--space-phi-5`, and the two raw literals) | **Unit half `.j`** — dies inside W6·436's merged cure. **Token-name unification → X-W10** (the demo re-mints a producer token under a different name AND ordinal) — cl.4. **The "anchored to no type role" story is DEAD (K-1): the em anchor is `--type-small × --ui-scale` via the row's own `.text-dropdown` — the ratified dropdown-option rung** | with W6·436 | severity: RR-LC's MAJOR preserved; MINOR ruled on the dead-story ground |
| W6·456 | **PS-19** ≡ D-8 (regraded) | PreviewStrip | MINOR | The elision cue is a **~4.5px alpha ramp on a ~6px sliver**, physical-direction (`90deg`) over a flex row | `.j` — **dies with W6·451's structural cue**; no standalone edit. **The forced-colors limb is KILLED (K-3): the probe never drove the count slider — it measured the default count 5, below the cap, where `mask: none` is the normal untruncated value.** RTL limb doubly latent | with W6·451 | — |
| W6·457 | **PS-20** ≡ D-9 | PreviewStrip | MINOR | `v-if="stops.length >= 1"` admits a **single-stop "palette" wearing full palette grammar** — reachable (`:min="1"`), while the ramp's floor is `>= 2` for a stated reason | `.j` on §3 n.15 — one floor, stated | with W6·445 | — |
| W6·458 | **PS-33** (missed-DU#6) | PreviewStrip | INFO | **The docblock contradicts ITSELF** — *"the strip a row shows is what selecting it yields… paints them as equal hard segments"* sits eight lines above the cap paragraph | `.j` — **the cheapest repair in the corpus, in the file whose subject is truthfulness; the FIRST line of any cure commit** | first, with W6·438 | — |
| W6·459 | **PS-34** (missed-LC#5) | PreviewStrip | INFO | **Four ceilings for one quantity on one screen** (docs 1–50, slider 1–12, cap 7, sibling ∞), and `STRIP_SEGMENT_CAP` is **module-private — falsifying its own rationale** (*"a NAMED taste knob so no writer mints it"*: no writer can consume it) | `.j` on §3 n.15/n.17 — export it or stop claiming it. Adjacent to, **not identical with**, the exported `RAMP_SAMPLE_COUNT` asymmetry | with W6·449 | — |
| W6·460 | **PS-31** ≡ D-12 | PreviewStrip | INFO | The strip's **honest-absence arm is unreachable in both hosts** (`generatePalette` returns ≥1; the calibrated palette is non-empty) — the module's absence law is live only on the ramp | `.j` — **recorded absence-of-exercise, not a defect to cure**; named so no successor books it as one | — | — |
| W6·461 | **PS-32** ≡ D-13 | PreviewStrip | INFO | Fixed 4px `--radius-sm` on an **em-scaled** plate: corner character 24–29% of height, end segments losing ~7% painted area to arcs | `.j` — folds into W6·438/W6·446's one recipe | with W6·446 | correctly INFO at the axis |
| W6·462 | **R-16 (chip half)** ≡ C-10 | PreviewRamp | MINOR | *(chip half of W6·261 — the host half is already booked there)* the unmemoised `stampStops(stops)` template call beside a computed sibling, twinned in both chips | `.j` on §3 n.14/n.15 — **counted once; W6·261 carries the host half and this row carries the chip half the record split off**. Collapses under W6·331's one-memo cure | with W6·261 | — |
| W6·463 | **R-8 (chip half)** ≡ L-4 + the k-count contract miss | PreviewRamp | MAJOR | *(the file half of W6·258)* **`k` is never honoured** — emitted counts swing **17→23** across the supported 1..12 operand range, never the documented *"≈16"*; and the second k=16 sampler runs a **different grid, count and error posture** (16 @ t=i/15, throws vs 17 @ t=j/16, nulls) | **§3 n.7 + n.16 make this executable: `mixStage.ts` and `sample.ts` land in one wave, so the unification the record could only flag becomes W6's to do.** **L-4's *"identical modulo the final encoder"* sub-claim is KILLED (ruling G) — the divergence is worse than filed, which strengthens the row** | with W6·258 | — |
| W6·464 | **PS-3** ≡ C-11 + D-4 | PreviewStrip | **BLOCKER-class BY IDENTITY** | The component is **absent from both arms of the demo's own COLOUR-SURFACE ROSTER**; the sharpest instance: `.generate-swatch` IS rostered, so in WHCM one screen's plate keeps its true colours while the preview chips of that same screen blank. The 42px reservation outlives the glyph | **Grade lives at its home (`PC-7`), route X-W7.c — cl.4.** Booked here because §3 n.15 + n.37 bring the file and the roster arm into W6's bounds, and because of its **NAMED GAP**: *"if X-W7.c's file bounds exclude `color-chips/`, this row degrades to the chip-family residue at execution."* **Carries the R-C reconciliation rider (ADJ-1, W6·265/W6·437): the wave picks roster-class vs hook ONCE — `data-color-surface` is currently ruled DEAD and deleting** — plus the reservation decision (*"auto is not a decision"*) and PreviewRamp enrolled in the same act | **ADJ-1 + the X-W7.c bounds question, both at the opening sitting** | — |

### 1k. PROPOSED unit `.k` — the picker sliders and the console rail (bounds-gated, Lane 2)

*Not in `W6.md`. **This band was found by file-census, not by the packet list** (§0a n.13): 34 rows —
including **two BLOCKERs** — route to X-W6 while **not one of their files appears in any W6 bounds
row**. `ConsoleRail`'s CC-068-routed rows already sit in `.f` (W6·178–W6·181); what remains is a
coherent instrument unit. **Every row is `cl.3`**: if §3 n.16–n.21 are not adopted, all 34 degrade
to **NO-WAVE-OWNER** by the ◆ law. The band's own second BLOCKER, **R-2** (`none` reaches an
unguarded throw in root setup), routes **X-W5 + X-W9 + X-W1** and is NOT W6's — recorded at §4 so
the two waves do not collide on one tree.*

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·334 | **R-1 (sliders)** | picker-componentsliders | **BLOCKER** | **The gate tests device CAPABILITY, never event MODALITY**: `if (!gate.isTouchDevice) return;` then capture-phase `stopPropagation()` — **the first mouse `pointerdown` is eaten on every touch-capable device** | `.k` — key on `event.pointerType`; NEW gate candidate **N-21** | first `.k` row | — |
| W6·335 | **D2-01** | consolerail | **BLOCKER** | **Boot and every space change render a `role="tablist"` with ZERO `aria-selected="true"`** and zero selected class | **→ X-W6 (glass-independent; MUST NOT wait on CC-113)**; NEW gate candidate **N-22** | with W6·334 | — |
| W6·336 | **MISS-2** | consolerail | MAJOR | `aria-selected` and the roving `tabindex` use **two definitions of "current"** — the shipped boot state exposes a tablist with zero selected tabs | `.k` — one definition; folds into N-22's falsifier | with W6·335 | — |
| W6·337 | **R-26** (MISSED-ADOPTED, RR-A) | picker-componentsliders | MAJOR | **FORCED COLORS: the tier-1 colour-display roster names the WRONG ELEMENT**, so all four channel ramps and the alpha checker lose their colour entirely | `.k` — name the painted element; NEW gate candidate **N-23** | with W6·342 | — |
| W6·338 | **R-31** (MISSED-ADOPTED, RR-A) | picker-componentsliders | INFO | `@media print` carries **the same wrong-element bug AND omits the slider entirely** — the ramps print blank inside a `#fff !important` veil | `.k` — one roster fixes both media | with W6·337, one edit | — |
| W6·339 | **R-27** (MISSED-ADOPTED, RR-A) | picker-componentsliders | MAJOR | **MOTION/PRM: the rail-letter jump calls `scrollIntoView({behavior:"smooth"})` unguarded**; the app-wide guard cannot reach it | `.k` — **Guard 1 (`animations.css:184-193`) vs Guard 2 (glass `a11y-overrides.css *:not([data-allow-motion])`) cascade ruling applies**; NEW gate candidate **N-24** | H2 reads first | — |
| W6·340 | **R-28** (MISSED-ADOPTED, RR-A) ≡ **MISS-10** (consolerail) | picker-componentsliders ≡ consolerail | MAJOR | **KEYBOARD/AT: `:step` is 0.001 of the NORMALIZED domain, decoupled from the per-space least count** — 3–4 consecutive arrow presses produce an identical announced value | **COLLAPSE WRITTEN (§0a n.11): R-28 at X-W6 (`.k`, gate N-25) is the executing row; MISS-10's X-W10 booking is the canon echo, not a second cure** | — | the divergence is recorded, per the record's own instruction |
| W6·341 | **R-3 (sliders)** | picker-componentsliders | MAJOR | Forced colors: **rest paints a permanent opaque ring and the active state loses ALL THREE deltas** | `.k` | with W6·337 | — |
| W6·342 | **R-4 (sliders)** | picker-componentsliders | MAJOR | The **"ONE container-scaled rhythm law" has structurally unreachable ceiling arms** | `.k` under X-W10's law | with W6·346 | — |
| W6·343 | **R-5 (sliders)** (promoted from D-16's hypothesis) | picker-componentsliders | MAJOR | **The wrapper map is never pruned** — detached reka subtrees retained and iterated by both consumers | `.k` | — | promoted from hypothesis to CONFIRMED |
| W6·344 | **R-6 (sliders)** | picker-componentsliders | MAJOR | **hex is an undesigned state that ships**: four rows, permanently blank meters, valueless announcements — statically decidable | `.k`; **identity-adjacent to CNL-21's dual-keying (W6·171)** | after W6·171 | — |
| W6·345 | **R-7 (sliders)** | picker-componentsliders | MAJOR | **A colour-space change destroys keyboard focus** (`:key="animationKey"` container re-key; no `focus(` in 395 lines) | `.k` | with W6·289 | — |
| W6·346 | **R-9 (sliders)** | picker-componentsliders | MAJOR | The meter's `min-width: 6ch` **under-reserves the widest legal string** and the overflow is taken out of the slider track | `.k`; `readoutReservation.ts` is bounds-new (§3 n.20) | after §3 | — |
| W6·347 | **R-12 (sliders)** | picker-componentsliders | MAJOR | `controls/` reaches into `display/`'s private module for a concept neither owns; `_component` declared and ignored; **the docblock cites three dead APIs** | `.k` | with W6·346 | — |
| W6·348 | **R-13 (sliders)** | picker-componentsliders | MAJOR | **The whole `<style>` is global** but only the touch-gate cluster is justified — five private names ship globally; the rhythm was taken by copy-paste | `.k` | with W6·342 | — |
| W6·349 | **R-14 (sliders)** | picker-componentsliders | MAJOR | **Per-instance redefinition of the producer's `--glass-bg-quiet`**, scheme-forked by a `.dark` descendant selector — named by its own comment | `.k` — root-styling law: fix at the producer/root, not per instance | — | — |
| W6·350 | **R-20 (sliders)** | picker-componentsliders | MAJOR | **21 `useTouchGate()` instances eagerly allocated; at most 4 live**; union-by-bare-key collides `a`/`b` across four spaces | `.k` | with W6·334 | union re-enumerated by this seat |
| W6·351 | **R-23 (sliders)** | picker-componentsliders | MAJOR | **The gates are vacuous**: three named mutations that break the component's stated contracts keep both suites green | `.k` + X-W1 falsifier law; `test/slider-announcement.test.ts` is bounds-new (§3 n.22) | after §3 | — |
| W6·352 | **R-8 (sliders)** | picker-componentsliders | MINOR (downgraded) | The accessible NAME is **the ambiguous raw key in shouting caps** — three names for one channel, two homophones | `.k` | with W6·178 | downgrade ruled; the reader's MAJOR preserved |
| W6·353 | **R-16 (sliders)** | picker-componentsliders | MINOR (downgraded from BLOCKER) | The drag thumb is **12 CSS px inline in every matrix**; the coarse extension grows only the block axis (the value-JUMP zone, never the grab zone) | `.k` | — | **K-2/K-3 ruled the BLOCKER down** — the Spacing exception is met; design's severity preserved as dissent |
| W6·354 | **R-19 (sliders)** | picker-componentsliders | MINOR | **Two physical holdouts in a logical stylesheet**; the meter inks CSS-serialization `deg` where the module's own doc promises `°` | `.k` | with W6·136 | — |
| W6·355 | **R-22 (sliders)** | picker-componentsliders | INFO | Dead `unknown`-cast payload; **two watchers on one source**; per-render array identity to the rail | `.k` | — | — |
| W6·356 | **R-25 (sliders)** | picker-componentsliders | MINOR | The 44px hit extension is gated on the **PRIMARY** pointer while the JS gate keys on an **any-pointer** sniff | `.k` — one modality definition, shared with W6·334 | with W6·334 | — |
| W6·357 | **D2-04** | consolerail | MAJOR | The rail joins the parent's own **T-59 one-law rhythm**: rail pitch 39.80px vs rows 43.50px, monotonic drift | `.k` (canon echo at X-W10) | with W6·342 | — |
| W6·358 | **D2-05** (RESCOPED) | consolerail | MAJOR (geometry-only) | **The rail takes 8 distinct widths across 14 verified states** | `.k` — geometry only | with W6·357 | **the law-breach framing KILLED at re-ratification**; geometry survives |
| W6·359 | **D2-06** | consolerail | MAJOR | Rest-state clearance | **`.k` + CC-113** — *"the budget re-derivation is a CONSTRAINT on the TR#84 ask — the relay letter must carry it, or the producer inherits a wrong budget"* | relay letter carries the constraint | — |
| W6·360 | **D2-07** (RESCOPED) | consolerail | MAJOR | **Hover and selection share one silhouette at one scale** — register hierarchy collapse (byte-proven) | `.k` (canon echo X-W10) | with W6·335 | — |
| W6·361 | **D2-09** | consolerail | MINOR | On touch, tap yields selection only; **long-press alone opens the anatomy tooltip** | `.k` — *rides MISS-1's explanation-surface redesign* | after W6·178 | — |
| W6·362 | **D2-12** | consolerail | MINOR | Glyph registry: the α item is **1.1px taller**; the face question rides the recorded C-4-face bracket | `.k` + relay | with W6·363 | — |
| W6·363 | **MISS-11** | consolerail | MINOR | **Small capitals used as a subscript surrogate**: `Cᴛ Cᴘ Jᴢ aᴢ bᴢ` are notationally wrong for every reader, not merely mis-rendered in one font | `.k` (with D2-12) | with W6·362 | — |
| W6·364 | **D2-14** | consolerail | INFO | The record's own header inheritance is stale | **`.k` — "correct the record in the wave that touches the file"**; feeds CC-113's ask authoring | with W6·359 | — |
| W6·365 | **MISS-6** | consolerail | MINOR | **Touch-gate state bleeds across colour spaces** — one gate per BARE channel key shared by every space using that key | `.k` — **identity-adjacent to R-20's union collision (W6·350); one keying fixes both** | with W6·350 | — |
| W6·366 | **MISS-14** | consolerail | MINOR | `box-shadow` is **absent from the item's transition list**, so the focus ring — the second-loudest register in the component — snaps | `.k` | with W6·360 | — |
| W6·367 | **L-1 (consolerail)** | consolerail | MINOR | **Three props against five ambient arrivals** (`inject(COLOR_MODEL_KEY)!`, `INK_AMBIENT_KEY`, `useGlobalDark`, …) | **rider on CC-113 + `.k`** | with W6·359 | — |

### 1l. PROPOSED unit `.l` — the display-math port (bounds-gated, Lane 2)

*Not in `W6.md`. `demo/scenes/about/katex/Katex.vue` appears in **no** W6 bounds row and
`demo/scenes/about/markdown/Markdown.vue` sits inside **X-W8's** `modify-carve` — yet 20 rows,
including the record's **sole BLOCKER**, route to X-W6. **Every row is `cl.3`** on §3 n.23/n.24, and
the `Markdown.vue` half is additionally `cl.4` (shared path with X-W8 — §4).*

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·368 | **R1 (Katex)** ≡ D-2 / K-2 (composite) | Katex | **BLOCKER** | **Display math clips at the card edge with zero affordance** — no scrollbar, mask, fade, `::after` or `scrollbar-gutter`, proven absent | `.l` — **Edict-4: glass-ui ships `FadingScroll` and this repo already consumes it elsewhere**; consume it, do not hand-roll. NEW gate candidate **N-26** | first `.l` row | — |
| W6·369 | **R10 (Katex)** ≡ D-10 | Katex | MAJOR | **Edict-4 bypass**: `FadingScroll` ships (`tabindex:"0"` unconditional; `role:"region"` only when named) and is declined here | `.l` — with W6·368, one adoption | with W6·368 | — |
| W6·370 | **R13 (Katex)** (design re-reader miss, ADMITTED **with a precision correction**) | Katex | MAJOR (cure-rider) | The prescribed cures' **aria shape** is wrong as filed | `.l` — **the correction is binding: the cure's aria shape is re-derived, not inherited** (cure-adjudicated-separately, §1f lock) | binds W6·368 | — |
| W6·371 | **R3 (Katex)** ≡ D-11/K-8/L-3/L-4/K-4 (root-contract cluster) | Katex | MAJOR | **The root ignores its one prop** — `class="inline-block"` in both modes against `displayMode = true` | `.l` | with W6·368 | — |
| W6·372 | **R4 (Katex)** ≡ K-6 | Katex | MAJOR | **Non-`ParseError` throws bypass `throwOnError:false` AFTER `katex.render` has wiped the node** (`textContent = ""` first — vendor source verified by both readers) | `.l` — render into a detached node, swap on success | with W6·373 | — |
| W6·373 | **R5 (Katex)** ≡ K-5/L-8/D-5/K-9 (error-surface cluster) | Katex | MAJOR (latent — all 64 expressions parse today) | `throwOnError:false` is **a masking fallback with no console signal** | `.l` — the masking-fallback ban (f2's law) applies here | with W6·372 | latency honestly labelled |
| W6·374 | **R17 (Katex)** ≡ D-15/K-3/L-7 | Katex | MINOR **with a MANDATORY rider** | `watch(() => expression, renderKatex)` **tracks only `expression`** while `renderKatex` reads `displayMode` | `.l` — **the rider is mandatory: watch totality, not a second watcher**; NEW gate candidate **N-27** | with W6·371 | — |
| W6·375 | **R7 (Katex)** ≡ D-3 | Katex | MAJOR | **A fourth typographic voice and a rogue 1.21× rung**: `.katex{font:normal 1.21em …;line-height:1.2}` — the shorthand imposes leading; 20 `@font-face` rules on no runtime budget | `.l` under X-W10's type law | with W6·377 | — |
| W6·376 | **R8 (Katex)** ≡ D-6 | Katex | MAJOR | **Chromatic hierarchy inverted**: headings and inline code recolour to the live specimen; the formulas do not | `.l` (+ `useMarkdownColors`, the family named at W6·219) | with W6·219 | — |
| W6·377 | **R9 (Katex)** ≡ D-7 | Katex | MAJOR | **Display-block vertical rhythm off-ladder and proximity-inverted** (intra-derivation gap ~1.47× the prose→math gap; four owners, one gap) | `.l` — one owner for the gap | with W6·184 | — |
| W6·378 | **R12 (Katex)** (design re-reader miss, ADMITTED) | Katex | MAJOR | **Dual-voice same-referent**: the same variable typeset in two competing voices one line apart at ~19 of 27 inline sites | `.l` | with W6·375 | — |
| W6·379 | **R18 (Katex)** ≡ D-12 | Katex | MINOR | **Zero `scroll-margin` on the math wrapper** while headings carry `scroll-m-20` — a scrolled-to formula lands under the sticky pane header | `.l` | with W6·377 | witnessed by this seat |
| W6·380 | **R19 (Katex)** ≡ D-13 | Katex | MINOR | **Asymmetric padding displaces the centring axis 12.944px** (arithmetic, no measurement needed) | `.l` | with W6·377 | — |
| W6·381 | **R20 (Katex)** ≡ D-14 | Katex | MINOR | `contain-intrinsic-size: auto 200px` **over-reserves** for 76–110px math blocks | `.l` | with W6·379 | heights live-only |
| W6·382 | **R21a (Katex)** (design re-reader miss, ADMITTED) | Katex | MINOR | **`mx-1` orphaned punctuation** at 4 verified sites | `.l` — **binding rider on R1** (the record names R13/R17/R21a/R22 as R1's riders) | with W6·368 | — |
| W6·383 | **R21b (Katex)** (miss, ADMITTED) | Katex | MINOR | `mx-1` = 4px, **below the ladder's smallest rung** (0.382rem) | `.l` under X-W10's law | with W6·382 | — |
| W6·384 | **R22 (Katex)** (miss, ADMITTED) | Katex | MINOR | **No `overscroll-behavior-x: contain`** — 0 hits in all of `demo/`; **37 ports chain overscroll** to the card scroller and (on iOS) the page | `.l` — **binding rider on R1**; NEW gate candidate **N-28** | with W6·368 | — |
| W6·385 | **R23 (Katex)** (miss, ADMITTED) | Katex | MINOR | **`\text{}` prose renders in the math serif** — 21 of 27 inline formulas | `.l` | with W6·378 | witnessed in the 390 frame |
| W6·386 | **R29 (Katex)** (library re-reader miss, ADMITTED) | Katex | INFO | `Katex.vue:37-38` **misnames its own mechanism** — the comment describes a hiding technique the code does not use | `.l` | — | — |
| W6·387 | **R33 (Katex)** (design re-reader miss, ADMITTED) | Katex | INFO | **`FadingScroll` ×37 instance cost** on engines without native scroll timelines: 37 ResizeObservers + 37 scroll listeners | `.l` — **cure-adjudicated-separately: this is the property W6·368's cure could break; gate it in the breaking direction** | binds W6·368's cure | — |

### 1m. Wave level, provenance and unhomed edges

| # | ID | Src | Sev | Mechanism | Cure-shape + locks | Seq | Dissent |
|---|---|---|---|---|---|---|---|
| W6·388 | **A-8** | shell-dock-parseechoreadout | MINOR | The out-of-gamut mark vocabulary — *"`toRgba8(gamut:'clip')` is the app's own policy"* | **BUILD → X-W6 beside CC-068's gamut-mark vocabulary** (`.f`, gates f5/f6); canon echo X-W10. **The re-author states the consequence** | with W6·156 | — |
| W6·389 | **A-10** | shell-dock-parseechoreadout | MINOR | Formatter dispersion | **BUILD → X-W6, one formatter home** (`.f`'s `specimen-format.ts`); cross-note CC-070's pretty-formatting facility | with W6·155 | — |
| W6·390 | **A-12** | shell-dock-parseechoreadout | MINOR | Two presentation-layer readers of one datum | **FOLD → X-W6 beside the MT-CSS-2 `COLOR_MODEL_KEY` role-split identity**. **MT-CSS-2 IS A PHANTOM OWNER (§0a n.10)** — it is a real banked name with no X wave; this row is NO-WAVE-OWNER until MT-CSS-2 is re-homed | X-W0 sitting | — |
| W6·391 | **MCP-35** | MiniColorPicker | INFO | Parent guard redundancy + session-side triplication | Guard → X-W7; **session-side triplication → the session-model wave (X-W6 family), else NO-WAVE-OWNER** — the conditional is the record's own | X-W0 decides the family | — |
| W6·392 | **U-6** | CurrentPaletteEditor | MINOR (residue) | C4-2's churn cadence — the always-mounted add slot binds the uncoalesced `cssColorOpaque` | **Witness rides X-W6 temporal scenes**; the 34/17-mutation counts are **live-only** and may not be quoted as measured | — | honestly labelled live-only |
| W6·393 | **CC-005** | shell-paneslot | — | `CC-005 → X-W6` (the V·MT4 fold) — the routing legend the record carries | Wave-level: W6 owns CC-005 alongside CC-056–CC-067 and CC-068; **`PaneSlot.vue:120`'s `<KeepAlive>` is the deactivation family's root (W6·279)** | before W6·279 | — |
| W6·394 | **chassis-fitness (whole record)** | chassis-fitness | **STALE (cl.2)** | Prescribes adopting `InstrumentChassis` and names **Mix as "the first-contort CANARY"** | **DEAD by CC-108/A-17 + H1.** M-25 forbids silent overruling: **a dated correction line must land in the record before `.j` opens** (gate **H7**) | before `.j` | — |
| W6·395 | **layout-gestalt (whole record)** | layout-gestalt | **STALE (cl.2)** | Same chassis prescription | Same disposition as W6·394 (gate **H7**) | before `.j` | — |
| W6·396 | **W-GRADSTOP-1/2/3** | GradientStopEditor | **PROVENANCE (cl.1)** | The record routes to the pre-X names; `CC-058` folded them into `X.W6.a/.b/.c`; **its rows ARE the a1–a13 / b1–b4 / c1–c4 gate texts** | Wave-level: cite the rename edge at open; **anti-rename — the banked ids stay `W-GRADSTOP-*`** | opening sitting | — |
| W6·397 | **C-3 / D-9 (ProfileSection)** | shell-dock-profilesection | MAJOR | Regenerate slug: the zero-palettes branch **skips the confirm dialog entirely** | **X-W4** (destructive-action ownership) **citing X-W6's b4 precedent — *"one removal owner; the floor is an explicit disable"***. Booked here only as the precedent edge (cl.4) | X-W4 owns it | — |

---

## 2. Gates

**Posture.** Nothing here re-stages, re-weights or deletes a `W6.md` gate. §2a **sharpens** existing
gates by naming the corpus rows that are their real falsifiers; §2b proposes **new** born-RED
candidates for the wave's opening sitting. **L-19 applies to every candidate: a born-RED gate names
a REAL witness — a file, a line, a count, a producer byte. No proof-script may stand in for one**;
every candidate below cites the measured state that makes it RED today.

### 2a. EXISTING W6 gates these rows sharpen (48)

| Gate | Unit | What it already asserts | Rows that sharpen it | Effect on the falsifier |
|---|---|---|---|---|
| a1 | .a | ordering invariant on write | W6·5 (GV-42) | the mutator is **normalise-on-write**; §14 lock — clamp and splice-at-insert are **BANNED** |
| a2 | .a | emitted CSS re-parses | W6·5, W6·9 | adds the **radial/conic** round-trip, not just linear |
| a3 / a4 | .a | one-axis + G1b inverse map (`gate-axis.mjs`) | W6·13, W6·14 | adds `touch-none`-vs-`pan-y` and the dual-contrast ring |
| a5 / a6 | .a | gesture arms A/B (`gate-gesture.mjs`) | W6·6 | arms must include a **cancelled** gesture |
| a7 | .a | *"a `pointercancel` in the dead-zone must mint NOTHING"* | W6·6 (GV-44) | the clause now has its mechanism: `@pointercancel="onBarPointerUp"` |
| a8 | .a | T9 (`p3.mjs`) | W6·15 | coarse-pointer band overlap |
| a9 | .a | T11, playwright-webkit iPhone-14 (`p4.mjs`) | W6·15 | the rail-bottom tap that **removes** |
| a10 | .a | T2 (`p2.mjs`) | W6·16 | the chip straddling the divider by 8px |
| a11 | .a | contrast floor (`WBGSE-D-probe4.mjs`) | W6·14 | the handle-ring vocabulary |
| a12 | .a | block 5 (`WBGSE-D-probe2.mjs`) | W6·14 | — |
| a13 | .a | leaf types / branded stops | W6·10, W6·12, W6·18 | the `-1` sentinel and the two ID minters are its inputs |
| b1 | .b | rail keyboard grammar (`gate-seat.mjs`) | W6·22 (GV-43) | *"gate b1 verbatim"* — role, name and tab stop |
| b2 | .b | add-path recomposition | W6·24 (GV-70) | coarse-pointer signification, not operability |
| b3 | .b | numeric entry | W6·23 (GV-49) | **generalises to colour entry** — selection must lead somewhere |
| b4 | .b | one removal owner; floor = explicit disable | W6·25, W6·54, W6·397 | the silent refusal at the floor; ProfileSection cites this as precedent |
| c1 | .c | ten-condition structural gate (`gate-structure.mjs`) | W6·59, W6·60 | the `<any-ident>()` class and the unlayered `hljs` plate |
| c2 | .c | one sampling law | W6·56, W6·144 | the strip's false contract **and** the ≥9-stop erasure |
| c3 | .c | literal dialect (`gate-literal-dialect.mjs`) | W6·115, W6·137 | `bezierLiteral`'s unbounded destructure; `stepsLiteral` n ≤ 0 |
| c4 | .c | space metadata | W6·34 | the `interpolatable` flag; **CSS item 6 sequencing: the L-15 rider may not open before it lands** |
| d1 | .d | easing radius (`gate-easing-radius.mjs`) | W6·38, W6·92 | four declared tokens → **six effective radii** |
| d2 | .d | easing readout (`gate-easing-readout.mjs`) | W6·127, W6·96 | the field-primitive decision governs the readout row |
| e1 | .e | motion register | W6·149 | the render tile's un-registered violent change |
| e2 | .e | PRM idiom (`gate-prm-idiom.mjs`) | W6·150, W6·271 | **five demo homes + one unused producer singleton** |
| f1 | .f | catalog totality | W6·152, W6·153, W6·154 | offered 18 / info 13 / docs 11; failing input `display-p3` |
| f2 | .f | no masking fallback survives (grep = 0) | W6·167, W6·181 | the **2 hits today** are `ColorNutritionLabel.vue:214` + `ConsoleRail.vue:174` |
| f3 | .f | About states true facts, never a bare heading | W6·153 | guide childCount 1 on Display-P3 |
| f4 | .f | every specimen caption fits its box | W6·155 | **16 of 18 overflow**, worst 758px/234px |
| f5 | .f | one grammar per row | W6·155, W6·388 | `data-specimen-form` absent (grep 0); the gamut vocabulary joins it |
| f6 | .f | out-of-gamut MARKED, never projected | W6·156, W6·388 | **clause (ii) anti-projection lock** — GREEN today (≤1e-10), RED the moment `mapColorToGamut` enters the formatter |
| f7 | .f | the boundary is the domain type | W6·157 | **3 `colorSpace: any` + 1 cast**, and the type witness must be UNUSED-free |
| f8 | .f | 18 dots, 18 silhouettes, zero dead props | W6·158, W6·162 | `distinctVisualSignatures = 1`; the oracle **excludes the per-mount filter url** |
| f9 | .f | one command, one home | W6·159 | zero references in the component; the picker watcher is the sole caller |
| f10 | .f | color-session draws no edge into `demo/ui` | W6·160 | 1 hit scoped; **90 repo-wide belongs to a wave that does not exist** |
| g1 | .g | card rhythm (`gate-card-rhythm.mjs`) | W6·184, W6·183 | the missing measure and the nine-pane padding outlier |
| g2 | .g | Picker↔About top edges ≤1px, Mix as control (CC-062) | W6·182 | Δtop 44.5 / Δbottom 44.77, both schemes |
| h1 | .h | blob seat/pipeline | W6·186, W6·189 | four of five Geometry sliders unreachable |
| h2 | .h | blob pipeline (`gate-blob-pipeline.mjs`) | W6·187, W6·206 | the park that no config write wakes; the false live seam |
| i1 | .i | X:ATMO-1 — named atoms move ≥ stated ΔE2000 | W6·227, W6·245 | five of seven knobs inert on the shipped tier |
| i2 | .i | L-band door (`gate-lband-door.mjs`) + stale comments | W6·226, W6·231, W6·235 | CC-066; the comment list **extends** to the Zones ceiling and the retired module path |
| i3 | .i | atmosphere media truth | W6·230 | three of ten media unreachable |
| j1 | .j | routed scene owns stage/inspector/action | W6·26, W6·238, W6·188 | Gradient, Atmosphere and Blob all fail it today |
| j2 | .j | deactivation gate | W6·279, W6·225, W6·63 | `onDeactivated` = **0 repo-wide** |
| j3 | .j | Blob/Atmosphere persistent preview (CC-057) | W6·188, W6·238 | the workbench owns no preview |
| j4 | .j | MEASURE-AT-OPEN | W6·300 | **π law extension: sample the ASPECT BOUNDARY, not just 1023/1024** |
| H1 | wave | no copied chassis (subtraction gate) | W6·394, W6·395, W6·238 | K-CHASSIS + K-9/XP-25 + AuroraPane R-7 all now cite it |
| H2 | wave | motion quarantine | W6·149, W6·151, W6·208, W6·339 | **file-existence leg is GREEN (measured); citation legs stay born-RED** |
| H3 | wave | bank sweeps | §4 e.21 | adds **BANK-1** (producer title/display variant) and **BANK-2** (Glass 8.0.0) from the CSS record, beside parser R1 and the rail variant |
| H4 | wave | one disposition per row | W6·179, W6·340 | the two cross-bookings are **collapsed in writing**, not silently |

### 2b. NEW born-RED candidates (29) — proposed, not ratified

| # | Proposed id | Unit | Predicate | Witness that makes it RED today (L-19) |
|---|---|---|---|---|
| N1 | c5 | .c | The serializer is **total**: `in <space> <hue> hue` and the type-appropriate direction survive one round trip | `serializeGradient` emits neither space nor hue (W6·61); the **radial round trip zeroes an authored direction** (W6·9) |
| N2 | c6 | .c | One `Result`-unwrap policy; no unbounded module-scope cache | six unwrap sites / two policies (W6·71); `resolvedEasingCache` unbounded and user-keyed vs the in-repo 512 precedent (W6·73) |
| N3 | d3 | .d | Catalogue **totality**: every built family is renderable — order is not membership | `FAMILY_ORDER` used as a filter: **27 of 33 tiles**, six quart/quint presets silently dropped (W6·83) |
| N4 | d4 | .d | **Codomain totality**: no preset, seam or hover can hand out-of-`[0,1]` progress to the colour path | `range(fn) ⊄ [0,1]` through **three seams including plain pointer hover** (W6·57); **7 throw doors**, one the producer's default `ease-out-back` (W6·1); the sixth door `openIntervalRamp` (W6·58) |
| N5 | d5 | .d | Tile identity is **lossless** and `custom` renders | every non-literal steps interval maps to the tile printed `n = 4` (W6·84); `custom` → `tileIdFor` null → the reveal early-returns (W6·85) |
| N6 | e3 | .e | A PRM flip **mid-narration** stops and settles | PRM sampled once at arm; `respectReducedMotion:false` makes the host ref a constant (W6·272) — the 3-line watcher does not exist |
| N7 | f11 | .f | No explanation surface renders **empty** | a live Tooltip stack per row displaying **zero characters** (W6·169) |
| N8 | f12 | .f | **Dual keying**: docs by `DisplayColorSpace`, math by `SpaceId`; no unreachable doc branch | `colorSpaceInfo.hex` is unreachable dead data while AboutPane loads `hex.md` (W6·171); `name` has zero consumers (W6·173) |
| N9 | h3 | .h | `webglcontextlost` calls `preventDefault()` | the handler does not — **the session deletion is permanent** (W6·214); one-line cure; discharges `App.md` D-6 |
| N10 | h4 | .h | The seat exists **independent of its occupant** | `class="hero-blob-anchor"` sits **ON** `<HeroBlob v-if…>` — every absent/error-state cure is structurally homeless (W6·215) |
| N11 | h5 | .h | **One** `deriveBlobPalette` owner; the ink floor is applied once | two owners; derived **twice per rAF-coalesced frame**; the boot derive's output is never rendered (W6·194) |
| N12 | i4 | .i | The harmony strip obeys **O-14** and `guaranteeSeamOffset` delivers its headroom | the strip breaks the law its own header states (W6·228); headroom computed on the wrong term, 90/1200 and 9/120 failure frequencies (W6·229) |
| N13 | i5 | .i | **No stale producer facts**: every shipped medium is reachable and every hand-copied ceiling is re-read | `kuwahara`/`metal`/`metal-gradient` unreachable (W6·230); Zones capped at 6 on a lifted ceiling (W6·231); `aurora-motion.test.ts:7` cites a path retired at W43 (W6·235) |
| N14 | j5 | .j | The backing store is **released** and no `scrollHeight` write-back is retained | sized to the pane's scroll extent, never released; two clears in **disagreeing coordinate spaces** (W6·273); the canvas pins the extent it measures (W6·276) |
| N15 | j6 | .j | The settle is **announced** and focus lands somewhere named | zero `aria-live`/`role="status"` in the whole mix tree on a ≥900ms settle (W6·270); reset sends focus to `<body>`, `.focus()` census 0 (W6·289) |
| N16 | j7 | .j | **One** operand budget: writer cap ≡ reader cap ≡ strategy floor | 4 vs 12 vs `minLen`/`maxLen` (W6·269); `MAX_COLORS` enforced nowhere the producer doesn't swallow (W6·319); floor authored twice, `n=1` terminal (W6·311) |
| N17 | j8 | .j | The chip preview and the Mix verb compute **one** object — or the truth-law prose dies | piecewise path (N→k) vs running weighted centroid (N→1); **they coincide only at N=2** while the file asserts identity (W6·321) |
| N18 | k1 | .k | The touch gate keys on **event modality**, never device capability | `if (!gate.isTouchDevice) return;` + capture-phase `stopPropagation()` — first mouse `pointerdown` eaten on every touch-capable device (W6·334) |
| N19 | k2 | .k | The boot tablist exposes **exactly one** `aria-selected="true"` | boot and every space change render **zero** (W6·335); `aria-selected` and roving `tabindex` use two definitions of "current" (W6·336) |
| N20 | k3 | .k | The forced-colors roster names the **painted** element, on screen **and** in print | the tier-1 roster names the wrong element: four channel ramps + the alpha checker lose colour (W6·337); `@media print` repeats it and omits the slider (W6·338) |
| N21 | k4 | .k | No unguarded `scrollIntoView({behavior:"smooth"})` | the rail-letter jump is unguarded and the app-wide guard cannot reach it (W6·339); Guard 1 / Guard 2 cascade ruling applies |
| N22 | k5 | .k | `:step` is the **per-space least count** | `:step` = 0.001 of the normalized domain → 3–4 arrow presses announce an identical value (W6·340) |
| N23 | k6 | .k | The rail's explanation surface is **total** across all 18 spaces **including α** | degenerate or actively wrong in **8 of 18**, plus the α cell in all 18 (W6·178); `currentColorRanges` provably never contains `alpha` (W6·180) |
| N24 | l1 | .l | The display-math port has an **affordance** | clipping with no scrollbar, mask, fade, `::after` or `scrollbar-gutter` — proven absent (W6·368); `FadingScroll` ships and is consumed elsewhere in this repo (W6·369) |
| N25 | l2 | .l | The render watch is **total** over its own inputs | `watch(() => expression, …)` while `renderKatex` reads `displayMode` (W6·374) — the record marks the rider **MANDATORY** |
| N26 | l3 | .l | `overscroll-behavior-x: contain` on the scroll ports | **0 hits in all of `demo/`**; 37 ports chain overscroll to the card scroller and, on iOS, the page (W6·384) |
| N27 | H5 | wave | **Path-erratum precondition**: every §4 bounds path re-resolves at open (D-19) before any route executes | two §4 paths do not exist (§0a n.3); `GV-66`'s own route text reads *"(path-erratum precondition)"*; the GRADIENT-UNOWNED clause makes bounds a **precondition on all 47 W6 routes** |
| N28 | H6 | wave | **Oracle-id collision resolved in writing** before any `o2x` file is opened | `o22-status-lamp` / `o26-aurora-perceptibility` / `o27-focus-affordance` **already exist at `c654824e`**, W6's own stamp, against three declared `create`s; `o26-aurora-perceptibility` also overlaps `o25-atmosphere-response`'s subject |
| N29 | H7 | wave | Stale prescribing records carry a **dated correction**, never silent overruling (M-25) | `chassis-fitness.md` still names Mix *"the first-contort CANARY"* for a chassis that `CC-108/A-17` killed; `layout-gestalt.md` prescribes the same (W6·394/W6·395) |
| N30 | d6 | .d | A **focused collapsed interval head paints focus pixels** | zero focus pixels on the collapsed head; the open row keeps a ~4px bottom band only — clip-rect × zero-offset ring (W6·82). *(This id was referenced by W6·82 in the fold's first pass and never defined in §2b; the omission is corrected here, not renumbered.)* |
| N31 | j9 | .j | `data-stops` has **ONE referent**, or the DOM carries **two attributes** — the truth-function list and the paint list are never one string | above the cap the two are **provably unequal**: held 8..12 / painted 7 (W6·449, W6·451). The scalar `data-preview-total` cure is **KILLED (K-12)** — o20's law is element-wise, so a scalar cannot satisfy this gate and a green produced that way is false |
| N32 | j10 | .j | The chip's payload **survives forced-colors and print** | 100% of the payload is `background-image` + `box-shadow` — exactly the WHCM substitution set — and `.preview-chip` is absent from **both** roster arms (`foundation.css:678-694` / print `:830-841`); `data-color-surface` producers repo-wide = **0** (W6·437, W6·464). **ADJ-1-sequenced: this gate's cure mints the selector's first producers while `G8` gates its deletion at zero — the two gates must be read together or one manufactures the other's RED** |

---

## 3. BoundsDelta — files this wave must ADD or CORRECT in `W6.md` §4

**Precondition clause, adopted verbatim from `wb-gradient-visualizer.md` and generalised to wave
scope (§0a n.14):** *"the three X-W6 bounds corrections … remain **preconditions** on every X-W6
route below."* **No §1 row executes before this section lands.** All paths measured in the working
tree **2026-08-28** and cross-checked against `git ls-tree c654824e` (W6's own stamp).

| n | Path | Verb | Why W6 needs it | Rows blocked without it |
|---|---|---|---|---|
| n.1 | `demo/workbenches/gradient/GradientVisualizer/GradientEasingEditor.vue` | **PATH ERRATUM** — replaces `…/GradientVisualizer/easing/GradientEasingEditor.vue` (`W6.md:59`) | the `easing/` segment does not exist; the corpus catches it in terms | all of `.d`; `GV-66` names it a precondition |
| n.2 | `demo/scenes/about/ColorNutritionLabel.vue` | **PATH ERRATUM** — replaces `demo/picker/display/ColorNutritionLabel.vue` | measured absent at that path, present at this one | W6·169–W6·177, incl. two NAMED-GAP BLOCKERs |
| n.3 | `e2e/smoke/oracles/o22-*, o26-*, o27-*` | **ID COLLISION** — renumber or convert to ADD-never-replace | three declared `create`s land on names already present at `c654824e` | W6·163, W6·193, W6·232 |
| n.4 | `demo/workbenches/gradient/GradientVisualizer/GradientCodeEditor.vue` | add `modify` | 16 rows route here (`A-1 … A-22`), two blocker-weight | W6·49–W6·52, W6·59–W6·64, W6·77–W6·80 |
| n.5 | `demo/workbenches/gradient/GradientVisualizer/easing/EasingSpecimenStrip.vue` | add `modify` | **resolves the ◆ NAMED GAP** for ~24 rows | every ◆ row in §1d |
| n.6 | `demo/workbenches/gradient/GradientVisualizer/easing/useSpecimenRows.ts` | add `modify` | same ◆ family; `D-03`'s un-isolated ordinal is minted here | W6·135, W6·123 |
| n.7 | `demo/workbenches/mix/MixAnimationCanvas/composables/mixStage.ts` | add `modify` | MX-CLUSTER member; **the masking fallback and the throwing ramp live here** | W6·266–W6·268, W6·274 |
| n.8 | `demo/workbenches/mix/composables/useMixingState.ts` | add `modify` | MX-CLUSTER member; the one-exit phase machine | W6·269, W6·311, W6·319, W6·320 |
| n.9 | `demo/workbenches/mix/MixResultDisplay.vue` | add `modify` (beyond-face) | MX-CLUSTER member; **sole writer of `data-mix-target`** | W6·266, W6·289–W6·296 |
| n.10 | `demo/color-picker/ErrorBoundary.vue` | add `modify` — **contested with X-W5** (cl.4) | MX-CLUSTER member; single boundary = two-pane outage | W6·266's blast radius |
| n.11 | `demo/palettes/mix.ts` | **CONTESTED** — `demo/palettes/**` is in W6's own **Do-NOT-touch** (X-W7) | MX-CLUSTER names it; W6 may not open it as written | W6·269, W6·321 — **carve or leave NO-WAVE-OWNER, written either way** |
| n.12 | `e2e/smoke/views/mix.spec.ts` | add `modify (ADD-never-replace)` | one of *"the two RED mix specs"* | W6·256, W6·286 |
| n.13 | `e2e/smoke/safari/mix-flow.spec.ts` | add `modify (ADD-never-replace)` | the second RED mix spec; safari-app is a SEPARATE π cell (I-20) | W6·256 |
| n.14 | `demo/color-session/color-chips/PreviewRamp.vue` | add `modify` | PREVIEW-CHIPS packet; the BLOCKER's own file | W6·255–W6·264 |
| n.15 | `demo/color-session/color-chips/PreviewStrip.vue` | add `modify` | same packet; the aurora consumer | W6·236, W6·251, W6·254 |
| n.16 | `demo/color-session/color-chips/sample.ts` | add `modify` | `sample.ts:58`'s `null` is the ramp's death; the second k=16 sampler | W6·255, W6·258, W6·321 |
| n.17 | `demo/color-session/color-chips/index.ts` | add `modify` | the barrel drags `PreviewStrip` + its stylesheet into the mix route | W6·332 |
| n.18 | `test/preview-chips.test.ts` | add `modify` | the chips' unit surface | W6·256 |
| n.19 | `demo/scenes/atmosphere/aurora-harmony-stops.ts` | add `modify` | **AuroraPane's explicit bounds rider** — *"absent from `.i`'s §Files"* | W6·228, W6·229, W6·236 |
| n.20 | `demo/test/glass/aurora-motion.test.ts` | add `modify` | same rider (AP-32) | W6·235 |
| n.21 | `demo/scenes/ConfigSliderPane.vue` | add `modify` | **`MT-CSP-1` is absent from the X ledger and this file is in NO X wave's table** (§0a n.9) | W6·190, W6·191, W6·196, W6·198 |
| n.22 | `demo/picker/controls/ComponentSliders/ComponentSliders.vue` | add `modify` | PICKER-SLIDERS band | 21 rows in §1k |
| n.23 | `demo/picker/controls/ComponentSliders/ConsoleRail.vue` | add `modify` | same band; **`f2`'s grep already names `:174`** | W6·178–W6·181, 13 rows in §1k |
| n.24 | `demo/picker/controls/ComponentSliders/composables/useSliderTouchGates.ts` | add `modify` | the modality BLOCKER's file | W6·334, W6·350, W6·356, W6·365 |
| n.25 | `demo/picker/controls/ComponentSliders/composables/useSliderAnnouncements.ts` | add `modify` | the announcement surface `R-8`/`R-28` act on | W6·340, W6·352 |
| n.26 | `demo/picker/display/ColorComponentDisplay/readoutReservation.ts` | add `modify` | `R-9`'s under-reservation and `R-12`'s cross-directory reach | W6·346, W6·347 |
| n.27 | `demo/picker/seat.css` | add `modify` | the global `<style>` cluster and the ornament z-tier | W6·348, W6·283 |
| n.28 | `test/slider-announcement.test.ts` | add `modify` | `R-23`'s vacuous gate | W6·351 |
| n.29 | `demo/scenes/about/katex/Katex.vue` | add `modify` | the display-math BLOCKER's file — in **no** W6 bounds row | 20 rows in §1l |
| n.30 | `demo/scenes/about/markdown/Markdown.vue` | add `modify` — **contested with X-W8's `modify-carve`** (cl.4) | R1's port, R9's rhythm and R19/R20 live here | W6·368, W6·377, W6·380, W6·381 |
| n.31 | `test/picker-blob-config.test.ts` | add `modify` | `C-6`'s regex-over-SFC gate | W6·209 |
| n.32 | `e2e/smoke/fixtures/blob-timing.ts` | add `modify` | the 5300ms arithmetic's fixture | W6·208 |
| n.33 | `e2e/smoke/webgl-blob-idle.spec.ts` | add `modify (ADD-never-replace)` | the idle/park oracle | W6·187, W6·208 |
| n.34 | `test/gradient-v4-consume.test.ts` | add `modify` | **`C-12`'s 7-tile slack lives at `:55-57`**; the unit-invariant half is W6's | W6·124 |
| n.35 | `docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-order.mts` | add **`execute, no write`** | **the fifth probe script; §4 bounds only four** — under W6's own Bounds law an order gate would run an unbounded instrument (§0a n.5) | W6·5 (a1/a2) |
| n.36 | `e2e/smoke/oracles/o14-preview-truth.spec.ts` | add `modify (ADD-never-replace)` | **the chips' only browser oracle** — verified present today. `R-2`/`PS-7`: it is unreachable (queries an accessible node the producer's `<span aria-hidden>` can never expose) **and** textually unpassable (its `parseOklchTriples` extracts **0** triples from the shipped `%`/`deg` dialect). **The reachability precondition is W6's** (it rides `R-1`); the dialect repair is X-W1's — the file must be in W6 bounds for the first half to be executable at all | W6·256, W6·447, W6·450, W6·463 |
| n.37 | `demo/styles/foundation.css` — **the WHCM / print colour-surface roster arm ONLY** | add `modify-carve` — **CONTESTED**: X-W5/X-W8/X-W10 already hold *other* carved arms of this file (layout, spectrum-shim bank, `.underline-tabs` survivor) | `R-6`/`PS-3`'s roster membership and `ADJ-1`'s `[data-color-surface]` fate both live in these two blocks (`:678-694`, `:830-841`) and in **no** W6 bounds row. **Carve or leave NO-WAVE-OWNER — written either way**; a fifth carve of one file is a §3a expansion trigger and must be declared at the opening sitting, never discovered mid-lane | W6·437, W6·453, W6·464 |

---

## 4. CrossEdges

| e | Edge | Shape | What must be written |
|---|---|---|---|
| e.1 | **MX-CLUSTER ⇄ X-W1 (CC-031)** | the packet's **CI-gating limbs are X-W1's, not W6's** | W6 executes the product rows; X-W1 owns the two RED specs' CI arming and the falsifier law |
| e.2 | **MX-CLUSTER ⇄ X.W4.g (CC-003 / CC-044 / CC-113)** | `X.W4.g` is **trigger-gated** and shares `MixSourceSelector.vue`; W6's §Blocked already carries CC-058's WatercolorDot-faces limb → CC-003's four-condition trigger | if the census fires mid-tranche, the atomic cut has no declared home — the CONFORMANCE orphan; W6 must not assume it |
| e.3 | **MSS-3 + MSS-16 ⇄ X-W7** | identity churn on a W6-bounded file | X-W7 lands after `.j`; the edit order is written, not discovered (W6·333) |
| e.4 | **MP-3 ⇄ X.W4.d** | a **sequencing constraint ON X.W4.d**, filed by the Mix record | W4.d reads it before opening; recorded here because W6 owns the consuming surface |
| e.5 | **ADJ-1 ⇄ GenerateControls G8** | two adjudicated records order **opposite fates** for `[data-color-surface]` | binds whichever wave lands second (W6·265) |
| e.6 | **AB-34 ⇄ X-W8 (`W8:174`)** | the `DocModule`/`DocItem` types carve | the catalog's `doc:` field must consume the **retyped** module (W6·166) |
| e.7 | **AB-27 ⇄ `assets/docs/**`** | 11 guide SFCs at repo root, **outside W8's bounds**, importing back into `demo/` | `.f`'s `doc:` re-plumb claims it at wave-open **or** it is NO-WAVE-OWNER (W6·165) |
| e.8 | **C-4 ⇄ X-W9 (CC-081 K-10) ∩ X-W6 (CC-068)** | one identity in two ledgers | *"the cure lands wherever the catalog lands"* — counted once (W6·179); H4 |
| e.9 | **R-28 ⇄ MISS-10 (X-W10)** | one mechanism, two waves | **collapse written**: X-W6 executes, X-W10 keeps the canon echo (W6·340) |
| e.10 | **R-2 (sliders BLOCKER) ⇄ X-W5 + X-W9 + X-W1** | the band's second BLOCKER is **not W6's** | if §3 n.22–n.28 land, two waves hold one tree — the edit order must be declared |
| e.11 | **`Markdown.vue` ⇄ X-W8 `modify-carve`** | shared path | §3 n.30 is a carve request, not an unqualified add |
| e.12 | **`ConfigSliderPane.vue` / MT-CSP-1 ⇄ the X ledger** | the file is in no wave's table and the CC id is absent | plus the **X-W8 CC-105 vs CSP G-PAINT** tension — both belong at the X-W0 sitting (§0a n.9) |
| e.13 | **MT-CSS-2 ⇄ nothing** | a real banked name (`COLOR_MODEL_KEY` role split + `cssColor` transport) with **no X wave** | W6·390, W6·157's clobber limb and the CSS record's own carry all point at it — **PHANTOM owner** |
| e.14 | **W-HYGIENE ⇄ nothing** | same class: the 89 `demo/ui` edges, the exports↔tsconfig parity, the editor barrel | W6·160, W6·140 — **PHANTOM owner** |
| e.15 | **About-recomposition ⇄ nothing** | same class | W6·185 — **PHANTOM owner** |
| e.16 | **GV-6 / `ErrorBoundary.vue` ⇄ X-W5** | the row routes X-W5 while §3 n.10 brings the file into W6 bounds | one file, two waves — declare, do not discover |
| e.17 | **GV-4 ⇄ X-W0 / X-W1 + X-W8.b** | no import lint reaches the workbench (identity `ColorSpaceSelector L-5(a) ≡ MX-25`) | folded away from W6; named so the absence is a result |
| e.18 | **GV-16 / GV-63 / GV-30 ⇄ X-W4** | `Ref<any>` chains, the port typing, the dock action's naming | W6·40 and W6·45 **depend on** X-W4 landing first |
| e.19 | **AP-12 / AP-14 / MC-3 ⇄ X-W4.b** | `size="sm"` is the **mechanism of record**, but **AuroraPane is absent from W4.b's file bounds** | the adoption lands in `.j` *citing* W4.b (W6·242, W6·322) |
| e.20 | **MR-34 / EAS-12 ⇄ X-W10** | the well-recipe-divergence **class** is X-W10's | *"cite, don't fork"* — `.j` executes the Mix member only (W6·295) |
| e.21 | **Glass BH relay bundle ⇄ the producer** | `./canvas` substrate (MX-38) · `:presets="false"` door (D-29) · readout/playback (D2-04) · `$attrs`/toggle-group (R-G) · specimen rung (R-H) · 44px floor (MSS-33) · `--select-dot-color` (AP-2) · `SelectionValue` export (GV-10) · the physical mask (D-05/D-34) · CC-113's budget **constraint** (D2-06) | **plus H3's banks: BANK-1 (title/display variant) and BANK-2 (Glass 8.0.0)**; and **K-13's RETRACTION of the `--select-font` packet — no relay may carry it** |
| e.22 | **X-W6 ⇄ X-W7 / X-W8 reciprocity** | four shared `modify` paths with W7; three gradient composables with W8 | CONFORMANCE fixed W7-D1/W8-D1 — the reciprocal edges now exist; this fold adds no new parallelism |

---

## 5. Dissents, corrections and exclusions — preserved, never ruled away

1. **MX-28** — ruled INFO (forward conformance); **both readers' MAJOR is preserved verbatim as dissent**. The recomposition is `.j`'s programmed work (`W6.md:310-315`), so re-booking it would double-book a programmed adoption.
2. **MX-31** — **ESCALATED against both current readers (PIN 7)**: a persistent pigment smear on reactivation, not bounded waste. The readers' "bounded waste" reading stands as dissent.
3. **GV-23 / C-06 / R-C** — a reader escalated to BLOCKER; **MAJOR is the standing grade, both readers overruled**. The escalation and DU's *"no axis in ten rounds found it"* are preserved.
4. **D3-01** — MAJOR ruled, **BLOCKER dissent preserved with a revival clause**: if the focus cure (W6·82) does not raise the register, the grade returns.
5. **C-11 vs GV-68** — the same mount cost graded MINOR at the strip and MAJOR at the visualizer. **The higher grade governs; the lower is preserved** (W6·109).
6. **L-2 above L-3-as-rescoped** (worker-O, **unresolved**): *"a caption showing 653px of text in a 234px box defeats the row's entire stated purpose for 16 of 18 rows on every open."* The wave cures both, so no ruling was required — **if only one lands, O's ordering governs**.
7. **DISSENT-1 (citation-inheritance)** — a stale producer cell propagated between seats and was settled only by reading the installed producer. **Binding on this fold**: every producer claim carried here is re-derived or labelled inherited.
8. **DISSENT-3 (worker-O's self-recorded errors)** — *"my probe read a signature without reading what the signature meant"*; `f8`'s oracle excludes the per-mount filter url **because of it**. The product-level paste round-trip remains **unwitnessed** and is labelled library-level.
9. **DISSENT-4 (worker-F's suspicious-agreement disclosure)** — 15/15 confirmations disclosed under the L-14 corollary; three CONFIRMED labels sat at the wrong scope. **The scope law exists because component-scoped agreement is cheap.**
10. **DISSENT-5 (L-5 intra-file contrast)** — ruled an observation, **with a revival clause**: *"if a seventh consumer ever mounts outside App.vue's provider, F's row revives with a crash witness."*
11. **R-16** — downgraded from BLOCKER by **K-2/K-3** (the Spacing exception is met; the sibling axis measured the pitch). Design's severity preserved.
12. **R-8** — downgraded to MINOR; the reader's MAJOR preserved.
13. **MR-34** — demoted from the reader's provisional MAJOR (**PIN 2 / K-19**) and folded to class **EAS-12**.
14. **R-E — READER-COVERAGE CORRECTION**: r2 graded the missing `/easing` route a BLOCKER and **both re-readers passed it in total silence**. Recorded as evidence about the fleet.
15. **R-J — READER CORRECTION**: filed by DU as corpus-novel; **already ADJUDICATED in the registry**. Novelty claims are checked against the registry, not asserted.
16. **RETIRED PROBE (binding, `wb-mix-resultdisplay.md`)**: *the plate's reduced-motion cell — **do not spend***. No π obligation may re-run it.
17. **Exclusions — rows in the 29 records that do NOT route to X-W6, named so their absence is a result, not an omission**: `GV-4` (→X-W0/X-W1+X-W8.b) · `GV-6` (→X-W5) · `GV-9` (banked NO-WAVE-OWNER, X-W8 declined by name) · `GV-12`, `GV-50`, `GV-51`-library-halves (→X-W9 else NO-WAVE-OWNER) · `GV-15` (→X-W1) · `GV-16` (→X-W4, mobile limb X-W5) · `GV-17` (→X-W8) · `GV-21`, `GV-35` (NO-WAVE-OWNER, files unowned) · `GV-26` (→X-W10 rider, named gap stands) · `A-2` (the silent-accept mirror, →X-W9) · `M-EE-5` · `R-2` (sliders, →X-W5+X-W9+X-W1) · `AP-16 … AP-19`, `AP-25`, `AP-29`, `AP-31` · `MSS-13`'s pre-adoption state · `C-1/D-3/L-1` (GradientPane inject → X-W8.d `G-10`) · `D2-03` (→X-W10) · `MISS-10`'s X-W10 half · `AB-34`'s carve · `XP-25`/`K-9` (→X-W10 canon correction).
    **Two former members of this list were PROMOTED to booked rows on 2026-08-28** and may not be re-read as excluded: `MP-1` → **W6·410** and `PS-3` → **W6·464** (both are `cl.3` on §3, which is precisely why exclusion was the wrong disposition — see §0's recorded correction). `PS-7`'s o14 ramp leg keeps its X-W1 home for the **dialect** repair, but its **reachability** half is now booked at W6·450/W6·463 under §3 n.36.
18. **Exclusions, second class — rows of the two adopted packets that route AWAY from W6 even after the §3 bounds edit**, named for the same reason. **Mix**: `MX-17` (roster granularity → NO-WAVE-OWNER; witness X-W1) · `MX-20`/`MX-26`/`MSS-27`/`MR-29` (tsconfig↔exports + published-surface ≡ `ColorSpaceSelector L-14` ≡ W-HYGIENE — **one banked identity, X-W8 declined it by name**) · `MX-25`/`MX-35`/`MR-28`/`MSS-28` (the lint-glob lattice ≡ `L-5(a)`) · `MX-27` (the six state matrices' missing `mix.png` → X-W1) · `MX-3`/`MSS-2`/`MR-2` (the unsatisfiable specs' **CI-gating** limb → X-W1 CC-031; their *spec-repair* limb is W6's at §3 n.12/n.13) · `MSS-1`/`MSS-22`/`MSS-23`/`MSS-20` (→ X.W4.g, trigger-gated) · `MSS-3`/`MSS-4`/`MSS-9`/`MSS-18`/`MSS-25`/`MR-12`/`MR-17`/`MR-9`-strip-half (→ X-W7/.c) · `MSS-5` (→X-W4+X-W10) · `MSS-10` (≡ AdminNamesPanel D-12/C-8 → X-W4) · `MSS-26`/`MSS-30`/`MSS-29`/`MSS-31` (→ PS-29 identity · App.md G1 · W-HYGIENE manifest · BH relay) · `MR-13`/`MR-26`-dock-half (→X.W4.d) · `MR-18` (→X-W7.c) · `MP-3` (a **sequencing constraint ON X.W4.d**, §4 e.4). **Chips**: `R-9`/`PS-25` (≡ GenerateControls `G4` / `L-14` — one identity, X-W1 era home) · `R-20` (barrel/graph subtraction → X-W8) · `R-23`-species-half, `R-12`-design-ruling-half, `PS-18`-token-half, `PS-35` (→ X-W10 canon) · `R-10` (the producer `#description` lane — **GLASS-OWNED, BH relay**) · `PS-6`/`PS-7`-dialect-half (≡ GenerateControls `R-A`/`G1` + X-W1 CC-031) · `PS-12` (≡ `PCS-10` → X-W7.c) · `PS-13`/`PS-14`/`PS-26`/`PS-28` (→ X-W9.d/.f, two with NAMED GAPS of their own) · `PS-15` (≡ `L-5(a)` → X-W8) · `PS-16`/`PS-27` (NO-WAVE-OWNER, named) · `PS-24` (**BANKED to the W25 pinned-file executor** — `GenerateControls.vue` is PINNED and W6 may not touch it) · `PS-36` (corpus-integrity, DISCHARGED at its own home). **The nine-pane/CNL rows that leave W6 likewise**: `CNL-4`/`CNL-13`-schema-half/`CNL-14`/`CNL-18` (→X-W9) · `CNL-5` (→X-W1) · `CNL-10`/`CNL-25`-type-half (→X-W10.d + BH relay) · `CNL-12`/`CNL-20`/`CNL-26` (→X-W5) · `CNL-15` (→X-W8) · `CNL-6`/`CNL-8`/`CNL-9`/`CNL-11`/`CNL-16`/`CNL-17`/`CNL-19` (→ NO-WAVE-OWNER, the About-recomposition and ink-referent bundles under rider A-1) · `AB-4`/`AB-5`/`AB-13`/`AB-20`/`AB-22`/`AB-24`/`AB-26`/`AB-28`/`AB-30`/`AB-33`/`AB-36`/`AB-37` (NO-WAVE-OWNER) · `AB-7`/`AB-8`/`AB-18`/`AB-32` (→X-W10.d) · `AB-9`/`AB-10`/`AB-11`/`AB-12` (→X-W5) · `AB-15` (→X.W4.g/CC-113) · `AB-23`/`AB-25`/`AB-38`/`AB-39` (→X-W8) · `AB-41`/`AB-42` (RECORDED-NOT-CONFIRMED, no disposition). **Katex rows that leave `.l`**: `R2` (→X-W4) · `R6` (→X-W1) · `R11` (≡ Markdown-adj L-3 → X-W8) · `R14` (→X-W2, the Q14 chain) · `R15` (NO-WAVE-OWNER, the content build pipeline) · `R16` (→U-F58 register, echo X-W4) · `R24`/`R25`/`R26`/`R28` (→X-W8, one with an X-W1 instrument caveat) · `R30`/`R31`/`R32` (INFO/negative/canon input).

---

*End of the X-W6 refinement fold. `waves/W6.md` is unmodified; this addendum is the only write.*
