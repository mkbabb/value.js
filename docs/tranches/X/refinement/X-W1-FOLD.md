SERVED MODEL ID (fold author, line-1 declaration): claude-opus-5[1m]

# X-W1 REFINEMENT FOLD — 2026-08-28 · the one dated addendum for X.W1

## 0. Header — provenance, posture, the reading rule

**Provenance.** The value.js corpus = every file under
`docs/tranches/V/megatranche/registry/adjudicated/` that is NOT `kf-*` / `fr-*` / `pt-*` — 88
component apotheoses plus `parser-band.md`, `library-band.md`, `chassis-fitness.md`,
`layout-gestalt.md`. This seat read every record for the 55 slugs assigned to X-W1 and then swept
the WHOLE corpus for `X-W1` and its dotted unit forms (`X.W1*`, `X-W1.[a-f]`) to catch routings
filed from records outside the assigned set. **Dotted-unit routings: ZERO** — no record in the
corpus names an X-W1 sub-unit; every routing lands on the wave. **True X-W1 routings: 59 files.**
(Plain `grep -l 'X-W1'` returns 73 files; 14 of those match only as the prefix of `X-W10`/`X-W11`
and carry no W1 row — `AboutPane`, `AdminListItem`, `AdminTagsPanel`, `BlobPane`,
`picker-debugeventlog`, `picker-heroblob`, `shell-dock-actionbutton`,
`shell-dock-genericactionbar`, `shell-panesegmentedcontrol`, `ShadowPalette`, `UserSortMenu`,
`PaletteColorStrip`, `PaletteCardSwatches`, `PaletteCard`. Recorded because the naive grep is the
obvious way to get this wrong.) The four band files carry **no** X-W1 routing.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0.md..W11.md` and
`CONFORMANCE-2026-08-03.md` are NEVER edited. This file is the fold's **sole write**. Where this
addendum strikes or widens a clause in W1.md, the strike is recorded here and W1.md's bytes stand
untouched.

**The reading rule.** At execution X.W1 reads `waves/W1.md` PLUS this addendum, in that order;
where they disagree the addendum governs. §BoundsDelta is read BEFORE the worktree plan — it names
a strike against W1.md's own "Do NOT touch" list, and a unit that discovers that at integration
time has already collided.

**Idiom (X·P §1a).** Fold classes: **cl.1** = spec sharpening / law / correction · **cl.2** =
cure-lock / binding contract clause · **cl.3** = MEASURE-AT-OPEN row · **cl.4** = DECLINED, routed
elsewhere. Every row carries exactly one `⟨…⟩` provenance line naming its banked ids. **Anti-rename:
banked ids are original for life** — this fold cross-books, it never re-mints. Dated corrections are
recorded, never silent.

**Arithmetic (this file's own bytes) — RESTATED 2026-08-28 at FOLD ROUND 1. The pass-1 figures are
printed beside the new ones; nothing is overwritten silently.** **56 ROWS** folded (R1..R56,
gapless — 50 at pass-1 + **6** at §6), **31 GATE ENTRIES** (15 existing W1 gates SHARPENED · 16 NEW
gate candidates NG-1..NG-16), **37 bounds entries** (a *file*-count reading gives 38 — the two
readings are reconciled at §6.4c), **14 cross-edges**, **4 packets adjudicated** (2 ADOPT-in-part ·
2 DECLINE-with-arms-retained), **6 dissents preserved unresolved**, **1 EXTERNAL row**
(⟨INBOX I-28 · X-EXT-5⟩ — mail provenance, no registry id). *Pass-1 read 50 · 29 · 36 · 12 · 0.*
The IN side (59 routing files × their row instances) is a receipt, not document-internal arithmetic;
no in-equals-out identity is asserted. **Round-1 census receipt (§6.0, re-runnable):** **101**
⟨record·id⟩ rows across **54** records carry an `X-W1` token on their own row line (62 of them with
an explicit routing verb); **four** escaped this file by the ⟨record.md · id⟩ law and are folded at
§6.1 — two by bytes, two by colliding namespace.

### 0a. Collator challenged — corrections, never silent

1. **The CI-cliff fact is ONE identity measured nineteen times, not nineteen rows.** Nineteen
   records independently re-ran `grep playwright|e2e .github/workflows/` and got 0. Transcribing
   them as nineteen rows is the M-25 defect. They fold to **R1**; each record's *distinct*
   sharpening is carried inside R1's cell, and only sharpenings that change a predicate are
   promoted to their own row.
2. **`X-W1 rider` is not a synonym for `X-W1`.** Eleven records book a "rider" — a claim that the
   substantive cure lives in another wave and only the gate-side arm rides here (AP-4, PM-15,
   MCP-9, MCP-39, AP-21, M-GP-7, DVS-12, AF-21, MMD, DSL-2, SFB-18). Each rider's OTHER half is
   named in §CrossEdges so W1's close cannot claim the whole row.
3. **Two records route the SAME flag to two waves.** `strictTemplates` → X-W1 (SFB-17) and →
   X.W7.a G3 (PaletteCardSkeleton, TagEditPopover). Recorded as **R8**, a live dissent; not merged.
4. **One record DECLINES W1 by name for a class three others route here.** PaletteSlugBar A-9:
   "nearest home X-W1's gate re-authoring, **whose written scope does not cover vitest SFC
   capability**." MCP-39, SFB-18, EB-26, C-14 (AdminNamesPanel) all route that capability here.
   Recorded as **R6**'s dissent with an explicit adopt-or-decline at wave-open.
5. **The X·P exemplar's near-duplicate rule applied.** `R-2(b)` (PreviewRamp) and `R-19`
   (PreviewRamp) are ONE defect seen from both ends — the record says so — folded once at **R3**
   and cross-referenced, count unmoved. Likewise `MX-3 ≡ MSS-2 ≡ MR-2 ≡ MC-9` (one mix-spec
   identity, four rosters), and `XP-7 ≡ XW-18 ≡ EY-46` (one extract flow spec, three rosters).
6. **Two rows this fold MINTS that no record filed** — both from this seat's own read-only
   measurement of the tree, both falsifiable against it: **R47** (oracle-ordinal collision between
   W6's created oracles and five shipped ones) and **R50** (the `smoke-safari` testDir disjointness
   is a FILE-level orphan class that G-5's project-level slate cannot see; PH-4 measured the fact,
   no record drew the gate consequence).

---

## 1. Rows

### Band A — the CI cliff and the gates that cannot pass

**R1 · MERGED CROSS FOLD — e2e executes in no workflow; nineteen seats measured it independently**
(cl.1) · **BLOCKER-weight (gate truth)**
Mechanism: `.github/workflows/` = `ci.yml` + `deploy-pages.yml` + `release.yml`; zero playwright /
e2e / lhci / boot-smoke references. ≡ W1.md's own **B3 = 0** and **B7 = 6 of 6 projects
CI-orphaned**. Distinct sharpenings carried inside this cell, each from its own roster:
· **ABT-6** — the RED gate has never been able to fail a build, so `o15:126 expect "8px 12px"` (RED
by construction against a computed `4px`) has sat green-by-absence; its residue #12 states the
sequencing consequence verbatim: *"the first HARD e2e run after CC-031 lands is the witness."*
· **PreviewRamp R-2(c)** — `ci.yml:32-37` runs lint / vue-tsc ×2 / build / vitest, zero playwright.
· **PaletteCardGrid §2** — "the oracle is NOT a CI gate; enforcement is X-W1's row" (the o9
negative assertions on an owner ruling are otherwise unenforced).
· **MX-3 / MSS-2 / MR-2 / MC-9** — "the RED gate is not a gate."
· **EAS-3** — "no workflow runs playwright; vitest excludes e2e/ structurally; zero
component-mount unit tests repo-wide."
· **A-15 (wb-gradient-codeeditor)** — adds the falsifier ask by name: *"in-viewport assertions, the
one-time falsifier discipline, and A-1/A-2's born-RED dist oracles."*
· **M-GP-7** — X-W1 rider, **falsifier law**.
**SEQUENCING LOCK (binding)**: R2's dead-locator census and R3's dialect cure land **BEFORE**
`e2e-smoke`/`e2e-safari` flip to required. A HARD flip over the tree as measured reds master on day
one for reasons that are not product defects, and **G-17 (master green) becomes unreachable inside
this wave**. W1.md's Worktree Plan puts X.W1.a (the wirer) in sub-wave 2; this lock says the census
and the dialect cure are *inputs to X.W1.a*, not follow-ons.
**DISSENT**: none — nineteen concurring measurements, zero contrary.
⟨PreviewRamp R-2(c) · PreviewStrip PS-6 · wb-mix-animationcanvas MX-3 · wb-mix-sourceselector MSS-2/R-1 · wb-mix-resultdisplay MR-2 · wb-mix-configbar MC-9 · shell-dock-actionbartoggle ABT-6 + residue 12 · wb-gradient-easingauthoringstage EAS-3 · PalettesPane PP-2 · SwatchHoverMenu SH-8 · wb-generate-pane GEN-9 · MigratePalettesDialog A-10 · PaletteCardGrid PG-6 + §2 · AdminFlaggedPanel AF-30 · wb-gradient-codeeditor A-15 · wb-gradient-pane M-GP-7 · shell-dock-dockviewselect DVS-12 · shell-dock-mobilemenudropdown C-9/L-8 · AdminAuditPanel AAP-34⟩

**R2 · NEW GATE CLASS — the DEAD-LOCATOR / DEAD-SELECTOR / DEAD-PATH census** (cl.2) · **BLOCKER**
Mechanism: a large fraction of the 185 collected tests assert against nodes, classes, files or
directories that **do not exist in the tree**, and are RED-by-construction independent of any
product defect. Enumerated members, each keeping its banked id:
· **SH-8** — SIX spec files bind the same impossible accessible name `"Add current color to the
mix"`: `palette-save.spec.ts:35`, `o14` ×2, `o15`, `safari/mix-flow`, `views/mix`. The tree renders
a `<span aria-hidden>`; no role/name can match.
· **PP-2** — `palette-save.spec.ts:34-37` targets `/Add current color .* to palette/`; statically
RED, no run needed.
· **A-3 (CurrentPaletteEditor)** — the same spec *"narrates the nameless Save button in prose while
routing around it"* (`:40-43`, verified verbatim).
· **MX-3 ≡ MSS-2 ≡ MR-2** — `mix.spec.ts:39-42` + `safari/mix-flow.spec.ts:29-32` role-query the
add-slot; both then assert `[data-mix-target]` visible, unmatchable per MX-1.
· **PreviewRamp R-2(a)** — `o14:351-355/:410-416` await the same node.
· **EAS-3 ≡ C-12** — `o17`'s `discloseAuthoring` asserts `#easing-authoring-0 svg[role='img']`
(`:51-52`); all three tests RED at the first shared assertion, so `:117/:189`'s presses of the
fatal tile **never execute** — *"the oracle would have caught the BLOCKER it now masks."*
· **AP-4** — `o22` watches `.dev-misconfig-banner`, a selector that exists nowhere; the live
resurrection wears `.api-misconfig-chip`.
· **M-21 (picker-spectrumcanvas)** — `o18-contrast-census.spec.ts:648-690` is a **live un-skipped
`test(...)`** doing `expect(page.locator(".plate-caption").first()).toBeVisible()`; the class was
deleted at `a68ecdc1` — `grep plate-caption demo/` = 4 prose comments, zero markup, zero CSS; and
`playwright.config.ts:147-153`'s `testIgnore` does NOT carve `oracles/`, so the smoke project runs
it. *"A structurally-red gate in the lane is either failing silently or the lane is not running."*
· **A-10 (MigratePalettesDialog)** — `o10d:448-461` `readFileSync`s a `demo/@/...` path deleted at
`a61094e3`; it **throws before any assertion**.
· **AB-9** — `color-propose.spec.ts:22-25` cites unit coverage at `test/parsing/extract.test.ts`;
`ls test/parsing/` → `timeline` only; `submitProposedName` appears in the test surface **only
inside the sentence claiming it is covered**.
· **R-9 (ImageDropZone)** — `walk.spec.ts:78-81`'s `/Upload image/i` `.last()` resolves to
ExtractControls' DockControl, never the zone; the executed mutation (delete the component → 1
match, still green) is *"the strongest evidence in the corpus."*
· **ES-19** — `o9:75-76` asserts glass-ui private internals (`[data-variant="ghost"]`,
`.watercolor-ghost-stroke`) one line past the published seam `data-slot="empty-state-trio"`.
· **PS-7 (shell-paneslot)** — `mobile/walk.spec.ts:48/:54/:137` describe `Transition mode="out-in"`
and use it to justify `test.setTimeout(60_000)`; the tree has no such mode. *"A gate that cannot
fail for the right reason."*
**CURE-SHAPE LOCK**: at authoring, every locator, selector, class, and filesystem path a spec names
resolves to ≥1 node / file, **or the spec is DELETED with a one-line rationale**. Converting a dead
leg to `test.skip()` is G-6's named failure mode wearing a new name and does not satisfy this row.
**SEQUENCING**: before R1's HARD flip. Feeds G-3's baseline classification (see G-3 sharpening).
⟨SwatchHoverMenu SH-8 · PalettesPane PP-2 · CurrentPaletteEditor A-3 · wb-mix-animationcanvas MX-3 · wb-mix-sourceselector MSS-2 · wb-mix-resultdisplay MR-2 · PreviewRamp R-2(a) · wb-gradient-easingauthoringstage EAS-3 · wb-gradient-easingspecimenstrip C-12 · ApiOfflineChip AP-4 · picker-spectrumcanvas M-21 · MigratePalettesDialog A-10 · shell-dock-actionbarlayer AB-9 · wb-extract-imagedropzone R-9 · EmptyState ES-19 · shell-paneslot PS-7⟩

**R3 · NEW GATE CLASS — the ORACLE DIALECT LAW** (cl.2) · **BLOCKER**
Mechanism: the chip/plate oracles compare the library serializer's dialect to a hand-rolled regex
that **cannot parse it**, in both chip species and in Generate.
· **PreviewRamp R-2(b)** — the stamp dialect is `oklch(62% 0.27 9.8deg)` (`grammar.ts:309`:
`format(a*100)%` + `angle(c)`→`deg`); `parseOklchTriples` (`o14:162-168`) returns **0** matches on
it vs **1** on canonical — its `[ ,]+` cannot cross `%` or `deg` — while `:376-392` asserts
`painted.length === stamped.length`, defective on BOTH UA branches (17-vs-0 RED, or 0-vs-0
vacuous).
· **PreviewRamp R-19** — *the same defect from the other end*: two incompatible OKLCh notations
share one `data-stops` contract (`aurora-harmony-stops.ts:25-40`'s hand-rolled 4-dp unitless form
vs the serializer's). **ONE identity with R-2(b); folded once, counted once.**
· **PreviewStrip PS-7** — the ramp leg has the identical disease: `stamped =
stops.flatMap(parseOklchTriples)` = **0** triples, computed `background-image` canonicalizes
unitless → `painted.length = N`, so `o14:378-381` is `expect(N).toBe(0)` — RED whenever chips
render, and the preceding `expect(n).toBeGreaterThan(0)` fails if they don't. *"No green
paint-oracle exists for either chip species anywhere in the repository."*
· **GEN-9** — `o20` compares `data-stops` (`oklch(65.524221608648% …)`) to
`getComputedStyle().backgroundColor` by string equality — **"unfixable as written"**; its comment
claims *"byte-identical rgb() strings"* the serializer has never produced.
· **MC-9** — the oracle's only GREEN leg is green *because of* the blocker (`[data-stops]` count 0,
a tautology).
**CURE-SHAPE LOCK (PreviewRamp R-2, verbatim)**: *"the dialect cure is: pass both sides through the
app's own parser, one encoding, the GenerateControls G1 precedent."*
**IDENTITY LOCK**: PS-6's own ruling — the o20 repair is already gated at `V·MEGA-W-GEN-1` G1, and
the fresh arbiter run of record is **1-failed / 1-passed** (deterministic RED on encoding),
superseding PreviewStrip's earlier "2 failed at dock.ts:78" cell. **FOLD both identities; do not
re-book.**
**SEQUENCING**: before R1's HARD flip.
⟨PreviewRamp R-2(b) + R-19 (one identity, both ends) · PreviewStrip PS-6 + PS-7 · wb-generate-pane GEN-9 · wb-mix-configbar MC-9 · GenerateControls R-A/G1 (identity, not re-booked)⟩

### Band B — the programs that do not check, the harness that cannot mount

**R4 · AdminAuditPanel AAP-34 — W1.md's B9 baseline is MEASURED TOO NARROW** (cl.1) · MAJOR ·
**dated correction to a dated spec, recorded not patched**
W1.md B9 records *"2 live type-imports + 3 doc references"* to `demo/@/lib/palette/types`. AAP-34
measures **10 refs including a runtime path string**, and records that *"C's line cite [is] off by
6"*. Identity: the AdminUsersPanel §3 measurement row (10 refs · no such directory) — **ONE
identity**. Source disposition is `NO-WAVE-OWNER (candidate X-W1 e2e re-gate)`; **this fold
ADOPTS it into X-W1**, which is the only wave that touches the surface.
**CURE-SHAPE LOCK**: a **runtime path string** is not cured by extracting `import type` shapes into
`e2e/fixtures/palette-envelopes.ts`. G-1's falsifier must include the runtime-string arm, or G-1
passes with the defect live. AF-30's `admin-populated.ts:28` limb is the same identity at a file
already in W1's bounds.
⟨AdminAuditPanel AAP-34 ≡ AdminUsersPanel §3 · AdminFlaggedPanel AF-30 (limb)⟩

**R5 · shell-dock-dockstatuslamp DSL-8 — `test/**` is in NO TypeScript program** (cl.1) · MAJOR
19 test files; vitest transpiles with esbuild so types are never checked; O-22's
`Array<[ApiAvailability, boolean, LampState | null]>` annotation is **decorative**. `demo/test/**`
IS inside `tsconfig.demo` — so *"L-7's two test homes differ in TYPE SAFETY, not just location"*,
and C-9's gate is **type-vacuous as well as behaviourally vacuous**. Source disposition:
`NO-WAVE-OWNER — candidates X-W1 (CC-031 CI-cliff family) / X-W8; **named gap: no X file mentions
test-program membership**`. **This fold ADOPTS at X-W1** (W1 owns the `typecheck` script and is
authoring `tsconfig.e2e.json` — the same act, one tree over) **with a declared decline path**: if
W1 declines, R5 + R6 + R10 travel to the formation boundary as ONE packet, never singly.
**Sharpens**: G-1, whose written predicate covers `e2e/` only. See NG-3.
⟨shell-dock-dockstatuslamp DSL-8 (+ L-7's move, DSL-26 riding the same decision)⟩

**R6 · MiniColorPicker MCP-39 — the vitest harness cannot mount an SFC at all** (cl.2) · **MAJOR ·
PRECONDITION**
`vitest.config.ts` carries **no `plugins` key** (and pre-empts `vite.config`, so
`@vitejs/plugin-vue` never reaches the test transform); `@vue/test-utils@^2.4.10` is a paid-for
dependency with **zero call sites**; every `mount()` in the repo lives under megatranche audit dirs
on bespoke configs. Re-verified by this seat: `vitest.config.ts:19-27` — `include: ["test/**/*.ts",
"demo/test/**/*.ts"]`, exclude-filter only, **no plugins key**.
**CURE-SHAPE LOCK (source, verbatim)**: *"the harness change is a precondition of every SFC-level
oracle any wave writes; NO-WAVE-OWNER if W1 declines scope."*
Cross-books (one identity, never re-minted): **SFB-18** (*"harness-cannot-mount-SFC identity =
MCP-39"*) · **MSS-2** (*"no `@vue/test-utils` import and no `mount(` anywhere in the repo — no gate
of any species could have seen MSS-1"*) · **ES-12** (`@vue/test-utils` devDep, zero consumers
repo-wide) · **C-14 (AdminNamesPanel)** — *"the G-17 mounts-precondition identity from the
AdminUsersPanel record (vitest has no vue plugin — the specced unit gates were unbuildable; **do
not re-discover**)"* · **TEP-4** (*"the matrix must mount this component"*) · **PCS-11** · **DSL-2**
(the unit suite never mounts the SFC; 19 asserts, resolver-only).
**DISSENT, PRESERVED UNRESOLVED**: PaletteSlugBar **A-9** books this class NO-WAVE-OWNER precisely
because *"X-W1's gate re-authoring… **written scope does not cover vitest SFC capability**."* Two
records disagree on whether W1 may take it. **Ruling deferred to wave-open as an explicit
adopt-or-decline**, minuted either way; a silent adoption is the M-25 defect.
**SEQUENCING**: R6 precedes R10, R21, R22, R23, R24, R30 (every mounted oracle).
⟨MiniColorPicker MCP-39 · SearchFilterBar SFB-18 · wb-mix-sourceselector MSS-2 · EmptyState ES-12 · AdminNamesPanel C-14 ≡ AdminUsersPanel G-17 mounts-precondition · TagEditPopover TEP-4 · PaletteCardSkeleton PCS-11 · shell-dock-dockstatuslamp DSL-2 · DISSENT: PaletteSlugBar A-9⟩

**R7 · PaletteSlugBar A-9 — `@vue/test-utils` stubs `<Transition>` BY DEFAULT** (cl.2) · MAJOR
Verified from installed 2.4.11 bytes (cjs `:98-99` config stubs; `:7777` `DEFAULT_STUBS`; the
in-source *"even if we are using `mount`"* comment family). Consequence: **a naive jsdom gate
asserts the OPPOSITE of the shipped focus behaviour** (A-16's mechanism) — *"no instrument in the
audit ever observed the defect it reported."*
**CURE-SHAPE LOCK (instrument law, binding on every wave that writes a mount gate)**: any
component-mount gate **stub-strips `Transition` or runs a real browser**. Rides R6.
⟨PaletteSlugBar A-9 (+ A-16 mechanism)⟩

**R8 · SearchFilterBar SFB-17 / SFB-43 — `strictTemplates` unset; the born-RED count is 10** (cl.1)
· MAJOR · **LIVE ROUTING DISSENT**
Repo grep for `strictTemplates` / `vueCompilerOptions` = **0** (re-confirmed by
wb-mix-configbar's independent sweep). With the flag preserved, the pair goes RED with **10**
diagnostics (the SearchFilterBar seat's own executed run) — **6 real drift / 4 noise**
(SFB-43; denominator corrected from the corpus's 6/5-of-11); the 4 noise rows are producer d.ts
gaps → BH relay note. *"Overturns pass-1's producer-side closures."*
**DISSENT**: SFB-17 routes the flag to **X-W1**; **PaletteCardSkeleton** and **TagEditPopover**
route the identical flag to **X.W7.a G3** (*"`strictTemplates: true` makes the dead-attr class a
compile error"*). **Proposed ruling, offered not imposed**: W7.a keeps the FLAG (it owns the files
the flag reds); **W1 owns the born-RED 10-diagnostic baseline and its 6-real/4-noise split**,
because W1 owns the `typecheck` script and the measurement is a gate artefact, not a product edit.
Recorded as dissent so neither wave can close claiming the whole row.
⟨SearchFilterBar SFB-17 + SFB-43 · DISSENT: PaletteCardSkeleton §9 (X.W7.a G3) · TagEditPopover TEP-4 (X-W7 G3)⟩

**R9 · SearchFilterBar SFB-19 — the producer's own canary for this defect class exists, unadopted**
(cl.2) · MAJOR
`reka-binding-idiom.test.ts` in the producer repo carries the header *"vue-tsc + units MISS them"*
— a render-effect canary for exactly the SFB-1 class — and was never adopted; it is producer-repo
test code, not shipped. Cure splits: **adopt the assertion shape → X-W1**; **publish as a
consumer-facing contract → BH relay** (r5 relay item 6).
⟨SearchFilterBar SFB-19⟩

**R10 · ErrorBoundary EB-26 — the demo component-test home exists and the core shell is absent**
(cl.1) · MINOR
`vitest.config.ts:21` includes `demo/test/**/*.ts`; exactly **3** suites live there (re-verified:
`export/`, `glass/`, `palettes/api/`), and the app's shell component is in none.
**CURE-SHAPE LOCK**: *"The corpus's 9 passing jsdom probes are ready `demo/test/shell/` seeds"* —
seed them by name; do not re-author. Rides R6.
⟨ErrorBoundary EB-26 (≡ L2-9 ≡ L-11)⟩

**R11 · wb-gradient-visualizer GV-15 ≡ wb-mix-configbar MC-13 — `npm test` has no build hook**
(cl.1) · MAJOR
`typecheck` has `pretypecheck`; **`pretest` is ABSENT** — so the O-14 vitest oracle binds to an
untracked `dist/` nothing refreshes, and `src` + `dist` run in one process. Compounding:
`vitest.config.ts:22-27` justifies its exclude filter by a `test/dist/` **that does not exist**
(`ls` → ENOENT, re-verified by this seat).
Folds: pretest half **→ X-W1**; the dead `test/dist` machinery **≡ ErrorBoundary EB-33**.
**DISSENT PRESERVED**: the `src/`-realias cure stays **KILLED** (MC-K9) — the cure is a `pretest`
script and the removal of a false comment, not an alias change.
`package.json` (scripts block) is already in W1's bounds; `vitest.config.ts` is not — see
§BoundsDelta.
⟨wb-gradient-visualizer GV-15 · wb-mix-configbar MC-13 (+ K-9 dissent) · ErrorBoundary EB-33 (identity)⟩

**R12 · wb-gradient-easingspecimenstrip L3-2 — the library's publish gate imports demo internals**
(cl.1) · MAJOR
`test/gradient-v4-consume.test.ts:7-11` reaches four levels into demo feature internals; it is **one
of TEN test files importing `../demo/`** (LC's census). *"A demo rename reds `npm test` for a
non-library reason."*
**SEQUENCING LOCK (this fold's, from the wave map)**: X-W5, X-W7 and X-W8 all rename or delete demo
trees. The test-tree import hygiene lands **before** any of them, or mid-tranche `npm test` reds for
a non-product reason and the wave that renamed gets blamed. → X-W1 (+ X-W8 rider).
⟨wb-gradient-easingspecimenstrip L3-2⟩

### Band C — gate truth: vacuity, and the born-RED gates W1 must author

**R13 · MERGED CROSS FOLD — the vacuous-gate machine** (cl.1) · MAJOR (class)
ONE mechanism, twenty-four rosters: assertions are presence-only or change-only, and the named
mutations stay green. Members with their distinct evidence:
· **AF-30 ≡ AAP-12 ≡ M10** — the cross-booked "vacuous-gate machine" identity itself.
· **PCS-11** — the 45-line browse-loading spec asserts marker-visible / no-spinner / cards-arrive /
no-console-error; *"the named empty-div mutation keeps all four green."*
· **EB-11** — `a11y-authed-admin.spec.ts:139-140` claims *"NAMED (a heading a screen reader
reads…)"* while `:143-144` asserts `textContent.length > 0` — **satisfiable while the glyphs are
100% occluded**, so the gate is blind to EB-1 too.
· **C-7 (picker-spectrumcanvas)** — `reactivity-instant.spec.ts` asserts only that the readout
CHANGES and median ≤50ms: *axis inversion, s/v swap, clamp deletion and a constant luma all stay
green*; `a11y-modality-support.spec.ts:301-305` **asserts the `role="img"` shape D-04 prosecutes —
a passing gate that PINS the defect.**
· **A-15 (wb-gradient-codeeditor)** — `gradient.spec.ts:233/:262` assert `toBeVisible`, which
passes for any non-empty box **regardless of viewport position** and does not auto-scroll — *"the
clipped verdict has sat under a GREEN gate for four audit rounds."*
· **D-19/C-6 + N-3 (shell-dock-profilesection)** — o18 is blind twice: keyed on the logged-in-only
`data-o18` hook, and compositing an ancestor walk that **can never contain the sibling
`.dock-plate`** — the census ground is unpainted and scheme-invariant.
· Plus, each already deduped to one identity: ES-12 · PS-8 · MCP-9 · TEP-4 · VHD-15 · XP-7/XW-18 ·
EY-46 · DVS-12 · PM-15 · AF-21 · PG-6 · Katex R6 · MMD C-9/L-8 · A-11 (ParseEchoReadout) · AAP-34 ·
MC-13 · GV-20 · SFB-18 · R-D (easingeditor) · PP-2.
**CURE-SHAPE LOCK (PCS-11, adopted verbatim as the spec)**: *"oracle slate: ink-certification,
geometry-parity, one-region-with-text assertions — C-7's three cures adopted as the spec; plus the
one-line `[surface],[variant]` DOM-attr sweep gate for the whole class."*
**LOCK (EB-11)**: a length-of-text assertion is not a name assertion.
**LOCK (A-15)**: in-viewport assertions, never bare `toBeVisible`.
**LOCK (DVS-12 residue 7)**: *"X-W1's born-RED authoring should execute mutation 3 first (it
doubles as DVS-6's gate-flip proof)."*
**PARTIAL COVER NOTED (PCS-11's own)**: X.W7 G3 (compile) and G20 (height) each catch a slice — W1
must not claim those slices.
⟨AdminFlaggedPanel AF-30 ≡ AdminAuditPanel AAP-12 ≡ PaginationBar M10 · PaletteCardSkeleton PCS-11 + K-8 · ErrorBoundary EB-11 · picker-spectrumcanvas C-7 · wb-gradient-codeeditor A-15 · shell-dock-profilesection D-19/C-6 + N-3 · EmptyState ES-12 · PreviewStrip PS-8 · MiniColorPicker MCP-9 · TagEditPopover TEP-4 · VersionHistoryDrawer VHD-15 · wb-extract-pane XP-7 · wb-extract-workbench XW-18 · wb-extract-imageeyedropper EY-46 · shell-dock-dockviewselect DVS-12 · PaletteCardMeta PM-15 · ActionFeedback AF-21 · PaletteCardGrid PG-6 · Katex R6 · shell-dock-mobilemenudropdown C-9/L-8 · shell-dock-parseechoreadout A-11 · wb-gradient-visualizer GV-20 · wb-gradient-easingeditor R-D⟩

**R14 · BrowsePane M1 — born-RED Retry gate** (cl.2) · MAJOR (dev-BLOCKER face)
One press of Retry blanks the Browse wall: `EmptyState.vue`'s leading template comment makes it a
multi-root component under the dev compiler, breaking `<Transition mode="out-in">`'s leave and
stranding `isLeaving` forever; dev = permanent blank surviving search / clear / route round-trip /
backend restore (KeepAlive holds it); prod = transient.
**GATE (verbatim)**: *"load `#/browse` with `**/palettes*` aborted, press Retry, assert the plate is
still there"* — rides X-W1's browser suite. **Cure surface is X-W7.g** (EmptyState modify-carve);
**W1 owns only the gate**. Same latent shape in `PaletteCard.vue` and `PaletteCardSkeleton.vue`
(both compile dev-fragment) — the gate covers all three hosts.
⟨BrowsePane M1 (P-1 ≡ D-01/p1 ≡ D-16/p2 ≡ C·P-1)⟩

**R15 · ColorNutritionLabel CNL-2 — born-RED two-click crash gate** (cl.2) · **BLOCKER**
Two ordinary clicks (achromatic color + LCh/OKLCh) kill the pane grid. Measured against dist:
**256/256 greys** emit `"none"` hue in both lch and oklch; the chain `ColorPicker.vue:348-354` →
`updateToColorSpace` → `setCurrentColor` (its hue-refresh is guarded, **the WRITE is not**) →
`clampPickerColor` preserves `"none"` → `channelNumber` throws `Missing lch.h` **in RENDER**; the
ErrorBoundary paints the fallback over the whole pane grid and Try-again re-throws.
**GATE (verbatim)**: *"born-RED e2e (two clicks, assert no `role="alert"` fallback)."*
**Cure lives at X-W9** (the `"none"` protocol must be unrepresentable-to-forget at the demo
boundary); escalates to the formation boundary with rider A-1 if X-W9's cut excludes demo seams.
⟨ColorNutritionLabel CNL-2 (reader-L miss 1, rescoped)⟩

**R16 · ColorNutritionLabel CNL-3 — born-RED boot-seed matrix** (cl.2) · **BLOCKER**
Boot-time white screen from App's own setup, self-perpetuating via the persisted seed:
`useColorPipeline.ts:75-76` is unguarded at `App.vue:245`'s setup, **OUTSIDE the ErrorBoundary**,
while the identical conversion twelve lines down sits in try/catch. **28 of 256 greys** round-trip
to hsv-powerless, so any of them as persisted/deep-linked seed throws `Missing hsv.h` **before
mount**. A persisted none-hue lch/oklch string does NOT boot-crash — it re-enters CNL-2's render
crash instead.
**GATE (verbatim)**: *"born-RED boot test over the seed matrix (`?color=black` at minimum)."*
**Sharpens G-12/G-13**: boot-smoke's four assertions run against ONE default boot; this row makes
the mode take a **seed matrix**, because the failure is seed-conditional and invisible to a default
cold boot.
⟨ColorNutritionLabel CNL-3 (reader-L miss 2, sharpened)⟩

**R17 · picker-componentsliders R-2 ≡ shell-dock-parseechoreadout A-1 — the none-URL gate** (cl.2) ·
**BLOCKER (by prior identity)**
Legal `none` reaches an unguarded throw in root setup; the boundary sits one level too low
(`ErrorBoundary` is INSIDE App's template while `main.ts:5-7` mounts bare — a descendant's
`onErrorCaptured` cannot catch an ancestor's setup throw). A-1 ADDS two throw sites to R-2's
enumeration: `astEcho` (`useColorParsing.ts:97`) and `gamutVerdict` (`:108`), with the R⑤ asymmetry
(Route A throws `astEcho` only), and records that the dock mount (`ActionBarLayer.vue:115` →
`ColorInput.vue:109`) sits **outside** the boundary.
**GATE (verbatim)**: *"the none-URL born-RED gate also enters X-W1's deploy matrix as a
query-parameter state."*
**IDENTITY LOCK**: A-1 **FOLDS** into R-2; three prior identities bind (R-2, ColorInput D-15,
ES-13/D-9) and **nothing is re-booked under a second name**. Cure halves are X-W5 (demo `Result`
propagation) + X-W9 (library `Result` battery). W1 owns the gate and the deploy-matrix state only.
⟨picker-componentsliders R-2 · shell-dock-parseechoreadout A-1 (FOLD) + R⑦ identity guard⟩

**R18 · wb-extract-pane XP-7 ≡ wb-extract-workbench XW-18 ≡ wb-extract-imageeyedropper EY-46 — the
born-RED extract flow spec** (cl.2) · MAJOR · **"the highest-leverage single artifact"**
No gate has ever handed this route a file: `setInputFiles` = **0** across `e2e/` (three independent
greps); `dataTransfer` / `input[type=file]` = 0; deleting `@pick`/`@add-color`
(`ExtractPane.vue:15-16`) keeps every gate green; C-r3's three vacuous-gate mutations all stay
green.
**GATE (XP-7 verbatim, addendum candidate)**: *"one born-RED flow spec (`setInputFiles` a fixture
PNG → palette develops; corrupt fixture → visible error + cleared palette [witnesses XP-2];
`?space=hex` sample → `#rrggbb`; navigate away → no live tracks, worker settled [witnesses
XP-1])."*
**EXTENSION (EY-46 verbatim)**: *"extend XP-7's born-RED flow spec with eyedropper arms (open →
loupe paints ≥1 opaque px; pan → tap samples in one click; RTL sample; Escape-scope; decode-error
register)."*
**IDENTITY LOCK**: three rosters, ONE artifact. Both siblings label it *"the single highest-leverage
artifact this record proposes."* The product cures are NO-WAVE-OWNER — W1 authors only the spec.
⟨wb-extract-pane XP-7 · wb-extract-workbench XW-18 · wb-extract-imageeyedropper EY-46⟩

**R19 · shell-dock-mobilemenudropdown C-9/L-8 — born-RED mobile-viewport spec** (cl.2) · MAJOR
Zero unit/e2e/oracle coverage; the sole references anywhere are `Dock.vue:11/:207`; C's mutation
argument (template → `<div class="lg:hidden"/>` keeps every gate green) *"is a correct vacuity proof
and explains how MMD-1 and the dead shimmer survived to HEAD."*
Source disposition: `NO-WAVE-OWNER (candidate X-W1 re-gate, **the AAP-34 precedent**)`. **This fold
ADOPTS**, on R4's precedent by the record's own naming.
**GATE (verbatim)**: *"one born-RED mobile-viewport spec — ArrowDown reachability, `aria-checked`
flip, Enter on GitHub (**RED today**), Escape focus return."*
⟨shell-dock-mobilemenudropdown C-9/L-8 (precedent: AdminAuditPanel AAP-34)⟩

**R20 · shell-dock-actionbarlayer AB-9 — the cycle-invariant test + the false-citation deletion**
(cl.2) · MAJOR
The component's sole gate is vacuous AND cites a phantom test (R2's member). Surviving assertions
read the toggle's aria-label — computed from `toolbarMode`+`canProposeName` **on a control outside
the swap machinery**. Mutation-list correction carried (R-LC's kill): mutation 5's failure mode is
fictional — ColorInput is ALWAYS mounted (no `v-if`, class-only swap) and deleting `:22` throws
nothing; **the mutation stays green trivially**.
**GATE (verbatim)**: *"the false citation deletion + the cycle-invariant test (exactly one active
sub-layer, `inert` on the other, propose→actions on success) ride X-W1 (gate truth) as a rider."*
**RIDERS ATTACHED**: AB-26's prose correction (`canProposeName` consults only the custom-name
registry while its gate's prose says *"doesn't match a built-in or custom name"*) and MISS-11's
(`:126-128` justifies the accessible name as a test-harness affordance — *"the dependency inverted
in-source; co-designed with the vacuous gate AB-9 reads"*) **both ride this rider**, per their own
dispositions.
**NOT W1**: whether built-ins SHOULD block proposing is a design question — NO-WAVE-OWNER.
⟨shell-dock-actionbarlayer AB-9 + AB-26 + MISS-11⟩

**R21 · Katex R6 — the 64-expression corpus vitest** (cl.2) · MAJOR
Zero tests (`katex` in `test/`+`e2e/`+`demo/test/` = 0); **64 call sites outside the typecheck
program** (`tsconfig.demo.json:57` excludes `assets/`), outside the lint program (eslint ignores
`**/*.md`), and outside the style program (`class=` in assets/docs = 0 — the Tailwind third is
inert); no unused-import net (no `noUnusedLocals` anywhere; `no-unused-vars` off — *the K-7 exact
mutation genuinely keeps every gate green*); zero visual coverage (`katex|about` = **0** against
**92** shots, both re-counted; the 80/60 denominators are killed). Structural cause: **About is a
pane** (`viewSchema.ts:107`), unreachable by a route matrix.
**GATE (verbatim)**: *"the ~20-line corpus vitest that `renderToString`s all 64 extracted
expressions; the visual oracle already names About at 390/1024/3440 ×2 schemes — X-W1:230;
**cross-book the Markdown-adjudication G-MD-8 committed-witness identity, one identity, not
re-minted**."* Rides R6.
⟨Katex R6 (K-7 / L-2 / K-12 / D-16 / L-6) + Markdown G-MD-8 (cross-book)⟩

**R22 · picker-colorcomponentdisplay M-10 — the `readoutReservation.ts` unit oracle** (cl.2) · MINOR
209 lines of hand-derived arithmetic with **zero unit coverage**, colocated in one consumer's
private folder and reached cross-boundary at `ComponentSliders.vue:100`.
**CURE-SHAPE LOCK (verbatim)**: *"the sibling `valueDomain.ts` names its own oracle, so the house
standard exists and this module sits outside it — **which is exactly why M-7 survived**."*
Split: module home → X-W8; unit oracle → X-W1. Rides R6 only if authored as a mount test; as a pure
function test it is independent of R6.
⟨picker-colorcomponentdisplay M-10⟩

**R23 · picker-spectrumcanvas C-5 + M-12 — the `spectrumLuma` unit oracle and its degenerate
threshold** (cl.2) · MAJOR
The border regime is hue-blind: `spectrumLuma(s,v) = v·(1−s/2)` substitutes 0.5 for the
hue-dependent luminance term; at pure yellow the white border measures **1.07:1**. Rider **M-12**:
the predicate sits **exactly ON its strict `>` threshold across the entire s=1,v=1 top edge**
(1·0.5 = 0.5, not > 0.5) — *"a `>=` edit inverts the border across the plate's most-used row with
zero test failure"* (no test touches the module).
**GATE**: the unit oracle asserts both the hue-dependent luma law AND the boundary case at
(s=1,v=1). Cure of the regime itself → X-W4.
⟨picker-spectrumcanvas C-5 + M-12 (rider)⟩

**R24 · shell-dock-parseechoreadout A-11 — pin `gamutVerdict`'s epsilon** (cl.2) · MINOR
Zero coverage for the component and both producing computeds, *"despite `gamutVerdict` carrying
epsilon logic that has **ALREADY regressed once**"* — R/PROGRESS.md R.W4: *"the gamut verdict
computed on display-denormalized OKLab, Δ inflated ~500× — cured to RAW OKLab."*
**LOCK**: *"a cured-then-unpinned regression is a live risk; the pin rides X-W1."* The pin asserts
RAW-OKLab semantics, not a numeric snapshot.
⟨shell-dock-parseechoreadout A-11⟩

**R25 · ErrorBoundary EB-21 — bind by stable test id, not role-and-hope** (cl.2) · MINOR
Up to **four** independently-owned `role="alert"` publishers, and `status-lamp.ts:53` mints
`role:"alert"` inside `<nav>` — **which precedes `<main>`** — while the sole spec binds `.first()`
by role. (The e2e-failure half is correctly a hypothesis — U-7.)
**GATE**: bind by a stable test id; a role-first `.first()` binding does not satisfy this row.
⟨ErrorBoundary EB-21⟩

**R26 · picker-colorcomponentdisplay M-2 ≡ consolerail D2-02 + MISS-3 — the o27 forced-colors arm**
(cl.2) · **BLOCKER (D2-02) / MAJOR (M-2)**
*"The o27 forced-colors oracle reaches only the gradient rail handle — verified at
`o27-focus-affordance.spec.ts:206-228`."* Under `forced-colors: active`, `:focus-visible` matches
and nothing paints: `outline: none; box-shadow: var(--focus-ring-shadow)` removes the property WHCM
preserves and keeps the one it strips, **against the repo's own written law** (`focus-ring.css:31`).
**MISS-3 is D2-02's real mechanism**: the shared WHCM register already exists and already names
`[role="tab"]` — **five sites defeat it** with unconditional `outline: none` at higher specificity
(ConsoleRail, ColorSpaceSelector, ColorComponentDisplay, GradientEasingEditor ×2; GradientStopEditor
is the sole law-conformant control). Receipts: probe-06 `forcedColorsFocus {focusVisible:true,
boxShadow:"none", outline:"3px none"}`.
**CURE-SHAPE LOCK (MISS-3, verbatim)**: *"the cure is at the five sites (or a specificity raise on
the shared block), **never another line in the register**."* The cure is X-W4's; **W1 owns only the
o27 arm extension** — the oracle must reach the readout cells and every control class, not one rail
handle.
⟨picker-colorcomponentdisplay M-2 · picker-componentsliders-consolerail D2-02 + MISS-3 + D2-15⟩

**R27 · shell-dock-profilesection A-1 + N-3 + D-19/C-6 — the o18 census learns its blind rows**
(cl.2) · MAJOR
Three named blindnesses in ONE oracle: (i) o18 requires the logged-in "Profile" trigger, so **the
`v-else-if` admin branch never mounts under it** — the admin gold ink is the ONE ink that bypasses
certification and **cannot pass in light scheme statically** (`--color-gold: #D4AF37`, sRGB relative
luminance 0.4494 → pure-white ceiling **2.10:1**); (ii) the menu's row labels are the ONE ink
population with no contrast guard, compositing over live page content through the translucent panel
— **the census does not learn the rows**; (iii) the census is keyed on `data-o18` and composites an
ancestor walk that can never contain the sibling `.dock-plate` (R13's member).
**GATE**: an admin-branch census hook + row enrollment. The design decision (one admin treatment)
is X-W10's; the LEGIBILITY BOUND that decision must satisfy is A-1's and rides X-W10 — **W1 owns
only the hooks**.
**Superlative preserved (residue 11)**: *"a census that can be shown wrong by construction is worth
more than no census."*
⟨shell-dock-profilesection A-1 · N-3 · D-19/C-6⟩

**R28 · picker-colorcomponentdisplay M-5 — the zoom gate** (cl.2) · MAJOR
The 11.7ch capacity constant is valid only while font ∝ container width; the `2.618rem` floor
breaks proportionality at raised root font-size. No `html{font-size}` / `text-size-adjust` lock
exists in `demo/styles/`. At 24px root / 400px pane: `2.618rem` = 62.8px beats `11.65cqi` = 46.6px,
ch = 42.1px, capacity ≈ **8.65ch** — *"rgb's 10.5ch tuple paints TWO lines inside a ONE-line
reservation"*; the `h-fit` CardTitle grows past min-height and **the card rect moves on digit
crossings — precisely the failure the module exists to prevent.** At default 16px root the
derivation holds exactly as documented (11.79ch band / 11.73 phone) — **the breach is the
raised-root band edge**, and the gate must be authored at that edge, not at default.
WCAG 1.4.4 + self-declared NORMATIVE law breach. Cure → X-W10; zoom gate → X-W1.
⟨picker-colorcomponentdisplay M-5⟩

**R29 · ApiOfflineChip AP-4 — the resurrection guard watches a dead selector** (cl.2) · MAJOR
R2's member, promoted for its own cure shape: **watch the STRING per MT-F031's born-RED grep, not a
dead selector**; `o22`'s own header concedes the misconfigured variant **cannot fire under the
harness**. → X-W1 rider (oracle-slate successor, CC-031 family); behavioral coverage rides the AP-9
rebuild (X-W7 rider) and is **not** W1's.
⟨ApiOfflineChip AP-4⟩

**R30 · shell-dock-dockstatuslamp DSL-2 — the mount test + the forced-latch capture arm** (cl.2) ·
MAJOR
All five mutations re-derived against live files: the unit suite never mounts the SFC (19 asserts,
resolver-only); `@vue/test-utils` installed, zero importers; o22 asserts count/attr/geometry, **never
text or accessible name**; no frame of any face exists in the 60-capture corpus (correctly — healthy
backend).
**CURE-SHAPE LOCK (verbatim, binding)**: *"A's C-9-cure kill stands: `toHaveAccessibleName` would
fail against a correctly-repaired region (**name ≠ content for `status`/`alert`**); **the gate
asserts rendered TEXT, or lands the author-name cure with it**."*
**NOTE**: MT-DOCK-LAYERS-1's G-Q (lamp `role:"status"`) and G-K(iii) **retire the M2/M3 mutation
classes when they land** — W1's gate must not re-assert what those retire. Rides R6.
⟨shell-dock-dockstatuslamp DSL-2 (C-9 + D-11-as-rescoped)⟩

**R31 · AdminNamesPanel M-LC6 — typed fixtures against a RE-DERIVED DTO** (cl.2) · MAJOR
Two e2e flow specs mock `status: "pending"` — *"a value neither the demo union nor the server's
`PROPOSED_NAME_STATUSES` can produce"* (verified: `color-approve.spec.ts:19` vs `color-names.ts:16`
and api `model.ts:9-10`); untyped literals hide the drift.
**CURE-SHAPE LOCK (verbatim)**: *"note `admin-populated.ts` **is typed and still perpetuates ADJ-1's
phantom field** — type the mocks against the RE-DERIVED DTO."* Typing alone does not discharge this
row; the DTO must be re-derived from the server, not inherited.
⟨AdminNamesPanel M-LC6⟩

**R32 · AdminFlaggedPanel AF-30 — fixture rot on a file already in W1's bounds** (cl.1) · MAJOR
`admin-populated.ts:153` answers `DELETE …/palettes/…` with `json("", 204)` while the live route
returns **200 `{deleted:true}`** and `client.ts:155` calls `res.json()` **unconditionally** — an
empty body throws. The fixture/CI half is X-W1's (CC-031 fixture repair + hard e2e); the behavioral
battery (five states + click-through; **the six named mutations must fail it**) is an **X-W7 rider**
on its G13/G14 battery (the AL-M10 precedent) and is **not** W1's.
⟨AdminFlaggedPanel AF-30 (C-F-10/F-10)⟩

**R33 · ColorNutritionLabel CNL-5 + CNL-17 — the o18 contrast floor is one notch below the module's
own target** (cl.1) · **BLOCKER (CNL-5) / MAJOR (CNL-17)**
The only executing coverage is vacuous: 1 hit in `test/`+`e2e/` and it is a comment (`o10d:41`); the
o18 rows (`:697-743`) boot ONE color, take `.first()`, and assert **ratio-only at `TEXT_FLOOR =
4.5`** — blind to CNL-1 (never reads a rendered fact), CNL-4 (never varies the color), CNL-6's hover
bleed (never counts styled nodes), and **one notch below the module's own 5.75 target**.
**GATE (verbatim)**: *"the born-RED oracle successors named in the rows above, plus a parameterised
`?space=`/`?color=` sweep (the surviving kernel of the killed L-14)."* CNL-17's limb: *"the
gate-floor mismatch (4.5 asserted vs 5.75 targeted) rides X-W1's oracle successor"* — the ink
referent itself is NO-WAVE-OWNER (*"the resting-rung composite referent"*, rider A-1 extension).
Belongs to NG-2's family: a gate whose floor disagrees with its subject's stated law measures the
wrong thing even when green.
⟨ColorNutritionLabel CNL-5 · CNL-17 (D-11-as-rescoped)⟩

### Band D — the visual matrix (CC-030 / CC-029a)

**R34 · MERGED CROSS FOLD — the modality matrices run a five-route SAMPLE, not the route census**
(cl.1) · MAJOR · **the single largest sharpening of W1.b**
W1.md's X.W1.b authors goldens as **routes × 3 viewports × light/dark**. The corpus establishes a
THIRD axis the spec does not carry, and proves the existing matrices are a sample:
· **R-28 (ImageDropZone)** — *"`/#/extract` is absent from ALL SIX modality matrices"*, enumerated:
forced-colors / keyboard-focus / reduced-motion / rtl-desktop / rtl-mobile / zoom-200, **each
holding the same five routes** (adminusers, blob, browse, gradient, picker); safari-* matrices DO
carry `extract.png`. *"One coverage defect, not four footnotes — **the structural reason
R-2/R-7/R-13/R-21 survived to be found here**."*
· **MX-27** — *"`/#/mix` is absent from every state matrix — **six** of them, not five"* (the
keyboard-focus-desktop arm also holds only the 5-route set); only the four safari at-rest cells hold
`mix.png`.
· **AdminNamesPanel residue 3** — *"verified hole: each `shots/` arm holds 5 routes, `admin-names`
absent from every one."*
· **Katex R6** — `REPORT.json` `katex|about` = **0** against **92** shots; **About is a PANE**
(`viewSchema.ts:107`), structurally unreachable by a route matrix. The same is true of Mix, Extract
and Generate as pane-hosted surfaces.
· **XW-18 / XP-7 D-16 / EAS-39 / MCP-9 / SH-28 / TEP-4** — the same hole per component.
**CURE-SHAPE LOCK (R-28, verbatim)**: *"the visual/oracle re-gate must run modality matrices
**against the route census, not a 5-route sample**."*
**L-19 DISCIPLINE PRESERVED (W1.md:236)**: the axes are still product-consumer axes — no golden is
minted for a combination no user reaches; the point is that **pane-hosted surfaces ARE reached** and
the current denominator silently excludes them.
⟨wb-extract-imagedropzone R-28 (≡ A-4) · wb-mix-animationcanvas MX-27 · AdminNamesPanel residue 3 · Katex R6 · wb-extract-workbench XW-18 · wb-extract-pane D-16/residue 7 · wb-gradient-easingauthoringstage EAS-39 · MiniColorPicker MCP-9 · SwatchHoverMenu SH-28 · TagEditPopover TEP-4⟩

**R35 · MERGED CROSS FOLD — the non-route capture arms (overlay · seeded · gated · forced)** (cl.2)
· MAJOR
Surfaces the matrix cannot reach by URL at all, each with its banked ask:
· **A-32 (FlagReportDialog)** — *"the X-W1 visual-golden harness must capture **overlay states**
(this dialog's ~8 states × 4 matrices), not only routes"*; 0 of 60 captures, and *"the prior audit
**propagated the unreachable spinner as the exemplar**"* (`Ad-interactive-states.md:242`; `:201`
cites the file as *"the correct minimal form"*). All seven of that record's residue rows ride this
corrective.
· **A-30 (MigratePalettesDialog)** — *"zero visual-matrix coverage **by construction** (double-gated
on `savedPalettes.length > 0` + an interaction; no route). X-W1 — **the visual-golden harness needs a
seeded-storage arm, or every gated modal stays permanently unwitnessed**."*
· **U-9 (CurrentPaletteEditor) ≡ SH-28 ≡ A-3** — *"a seeded populated-state `/#/palettes` capture
variant — without it this component's entire defective surface is invisible to the visual matrix."*
· **TEP-4** — the authenticated owned-palette browse state.
· **AdminNamesPanel residue 2** — *"the populated row has never been photographed… **one
seeded-fixture capture pair (light/dark) discharges five hypotheses at once**."*
· **PCS-11 / K-8** — the held-state fixture **already exists and the e2e spec already uses it**
(`routeBrowsePalettesDelayed`); *"the real gap is that the **VISUAL matrix** never consumes the
fixture."* L-9's "the matrix needs a fetch-delay shim" is **KILLED as prescribed**; the narrower true
gap survives here.
· **DSL-2** — the forced-latch arm (the misconfigured face cannot fire under the harness).
· **MCP-9** — two popovers deep, zero of 60 shots.
· **CNL-5** — the parameterised `?space=` / `?color=` sweep.
**CURE-SHAPE LOCK**: seeded storage, seeded fixtures and forced states are **capture inputs**, not
product edits — X.W1.b writes no `demo/` byte, per W1.md's own Triumvirate trigger.
⟨FlagReportDialog A-32 (+ residues 1-7) · MigratePalettesDialog A-30 · CurrentPaletteEditor U-9/A-3 · SwatchHoverMenu SH-28 · TagEditPopover TEP-4 · AdminNamesPanel residue 2 · PaletteCardSkeleton PCS-11 + K-8 · shell-dock-dockstatuslamp DSL-2 · MiniColorPicker MCP-9 · ColorNutritionLabel CNL-5⟩

**R36 · MERGED — real-WHCM and engine honesty; an emulated frame is not a witness** (cl.2) · MAJOR
· **EC-8** — the forced-colors roster protects an element that never paints
(`.glass-slider[data-variant="spectrum"] .slider-range`, which the producer sets to `background:0 0`)
while both actual colour carriers are ABSENT from it; scope is app-wide. *"The roster mis-aim needs
no browser; **the rendered blank needs a real-WHCM witness**."* Residue 8: *"the pass-1 PNG is
WebKit emulation (pass-3's honest caveat); **a real forced-colors cell belongs in X-W1's golden
matrix**."* The roster cure itself is **NO-WAVE-OWNER** — `foundation.css`'s forced-colors block
exceeds X-W10's declared carve.
· **MX-17** — roster granularity (the tier-1 roster should name a class; its comment enumerates
three canvases, not this fourth); roster cure NO-WAVE-OWNER, **witness X-W1**. LC/prior MINOR
preserved as dissent.
· **D-3 (AdminNamesPanel)** — *"X-W1 capture obligation"* (measure the button, not the track) with
the whole residue block declared *"evidence obligations for the X-W1 instrument pass (**chromium +
the Safari cells, labeled per cell**)"*.
· **PH-4** — the oracles subtree is structurally unreachable from any WebKit project (see R50).
**Sharpens G-10**: renderer-honesty currently means "record the renderer string read from the live
browser." This row adds: **an emulated modality is labelled as emulation and does not discharge a
real-modality obligation.**
⟨wb-extract-controls EC-8 + residue 8 · wb-mix-animationcanvas MX-17 · AdminNamesPanel D-3 + §residue header · PaneHeader PH-4⟩

**R37 · NEW — the INSTRUMENT-CAVEAT REGISTER W1 inherits** (cl.2) · MAJOR
Four measured instrument defects that invalidate or qualify corpus measurements W1's gates will
consume:
· **Katex R26** — `vite-defer-glass-fonts.ts:72` guards on the dead `demo/@/styles/style.css` (the
marker is actually at `foundation.css:75`), so **the transform can never fire** and *"dev serves
metric-compat fallback faces forever, so **every dev-server typography measurement in this challenge
corpus was taken without the production font corpus** — an instrument caveat X-W1 must inherit."*
Fix → X-W8; caveat → X-W1.
· **Katex, C's measurement trap** (`challenge-C:149-154`, adopted by both re-readers, mechanism
verified from `Markdown.vue:104-109`) — *"`content-visibility:auto` makes `scrollWidth` report the
`contain-intrinsic-size` placeholder; the seat caught its own probes' false green and wrote the
rule."* *"Belongs in the **gate-design canon** (routed with R20 to X-W1), not just this ledger."*
· **EC-47 / L-10** — the repo's `smallTapTargets` probe measures the **paint** (12×24 thumb), not
the SC 2.5.8 **target** (`SliderImpl.js:46-53`: pointerdown on any non-thumb target captures and
emits `slideStart`, so the target is full inline-size × 24). *"An instrument defect of the same
species as the `namelessButtons` accname miss (L-5), recorded as input to X-W1's gate re-authoring."*
The 12×24 row itself is INFO · GLASS-OWNED · already relayed — **do not re-book**.
· **Katex residue 9 / D-4** — *"does `.font-display` paint Fraunces at these five sites — token
sizes proven; paint-time family is a cascade question. Ride the X-W1 matrix with the parameterised
space/color sweep."*
**CURE-SHAPE LOCK**: the register is a committed artifact under `docs/tranches/X/evidence/w1/`, and
every gate that consumes a corpus measurement cites its caveat or states none applies.
⟨Katex R26 · Katex C-measurement-trap (residue) · Katex residue 9/D-4 · wb-extract-controls EC-47/L-10 (+ L-5 species note)⟩

### Band E — deploy truth and the packed surface

**R38 · picker-pointerdebugoverlay U-4 — production A/B currency** (cl.3 · MEASURE-AT-OPEN) · MAJOR
r2 cites `assets/index-DJ8Ije_5.css` live (2026-08-03); the local `origin/gh-pages` ref
(`17c10267`, 2026-03-24) carries `index-DS-0HlXP.css` — *"either the tracking ref is stale or r2's
filename cell is wrong."* The substance is proved offline either way, **and the corollary is the
row**: *"the deployed product predates the `demo/@` death and Glass 7, so **every 'production is
correct' A/B in this corpus may mean 'a March build is correct'**. One live manifest fetch settles it
— rides X-W1's deploy-truth rows."*
**Sharpens G-20**, which asserts only that the entry asset hash changes after G-19's run. This row
adds: **record which source epoch the PRE-deploy artifact belonged to**, because a large body of the
corpus's A/B reasoning is conditioned on it. MEASURE-AT-OPEN: one manifest fetch, pasted.
⟨picker-pointerdebugoverlay U-4⟩

**R39 · PreviewRamp R-9/L-10 ≡ GenerateControls G4 ≡ ColorSpaceSelector L-14 — the packed-vs-checkout
surface** (cl.1) · MAJOR · **IDENTITY-FOLD, never re-booked**
Re-derived whole by the PreviewRamp seat: `ls` → `dist/index.d.ts` ENOENT; `dist/subpaths/` = 7
pairs, no `parsing` / `units`; real-dir registry self-install 4.0.0 = 4.0.0; `css.d.ts` 10910 vs
12490 B by its own `wc -c`.
**ROUTING LOCK (verbatim)**: *"identity-fold to the existing GenerateControls G4 row; **X-era home
X-W1** (`verify-packed-surface.mjs` / the G-17 pack step **is** the packed-vs-checkout surface's
owner)."*
**BOUNDARY LOCK**: the **tsconfig-paths half** of this identity is separately banked NO-WAVE-OWNER /
W-HYGIENE (GV-9, XP-21, MX-20; `W8.md:112` **declines it by name**). W1 takes the packed-surface half
ONLY; claiming the paths half at close is a scope defect.
⟨PreviewRamp R-9 + L-10 ≡ GenerateControls G4 (TSCONFIG-PATHS-DRIFT, gated at its wave's G4) ≡ ColorSpaceSelector L-14⟩

**R40 · PaletteCardMenu AP-21 — the demo-dogfood trust boundary** (cl.1) · MAJOR
*"The demo-dogfood trust boundary is a gitignored local build not byte-equal to published 4.0.0 (the
`_2` rollup-dts collision artifact in local `dist/subpaths/css.d.ts`)"*; `git check-ignore` →
`.gitignore:17`; installed 4.0.0. Source disposition: **X-W1 rider (deploy-truth family), else
NO-WAVE-OWNER at the formation boundary.** Rides R39's gate.
⟨PaletteCardMenu AP-21 (L-19)⟩

**R41 · wb-mix-configbar MC-14 ≡ wb-mix-pane F-8 — the repo installs itself from the registry**
(cl.1) · MINOR
`@mkbabb/value.js@4.0.0` installs from the public registry into its own repo's `node_modules`
(glass-ui's `^4.0.0` peer; **no self-dependency declared** — package.json + lock reads). Folds ≡
PreviewRamp R-9 / GenerateControls G4 → **X-W1's packed-surface owner**.
**DISSENT PRESERVED (binding on the gate's shape)**: the drift sub-claim is **KILLED at MC-K8** —
*"the delta is private `_2` declarations"*; 383 vs 351 lines, `^export` = **53 BOTH**. Therefore
**the gate asserts EXPORTED-SURFACE equality, never byte equality**; a byte-equality gate reds on a
known-private artifact and is a manufactured failure.
⟨wb-mix-configbar MC-14 (+ K-8 dissent) · wb-mix-pane F-8 (+ MC-K8)⟩

**R42 · Katex residue 12 — the post-`c4af0ef9` gh-pages artifact census** (cl.3 · MEASURE-AT-OPEN) ·
INFO
*"The library re-reader's rebuild receipts (122 assets, vendor-katex emitted, **not in the
modulepreload set**) are adopted as its receipts; this seat ran no build. **One fresh `npm run
gh-pages` + artifact census at the next X-W1/X-W2 sitting closes it.**"* Rides G-13's MEASURE-AT-OPEN
prod-preview build — the census is a free by-product of the build G-13 already runs.
⟨Katex residue 12⟩

### Band F — the four proposed packets, adjudicated

**R43 · PACKET `GEN-CLUSTER` — ADOPT the oracle half; DECLINE the product half** (cl.4/cl.1)
GEN-9's own split is dispositive and is honored verbatim: *"→ CI: **X-W1** (CC-031); oracle repair +
any unit coverage: **GEN-CLUSTER, NO-WAVE-OWNER**."* Re-verified by this seat:
`e2e/smoke/oracles/o20-generate-plate.spec.ts` **exists on disk** and appears in **no wave file**;
W6's routed-scene list omits Generate; `o18` grep for generate = 0.
**RULING**: `o20-generate-plate.spec.ts` enters X-W1's bounds (R2's census and R3's dialect law both
land on it) and the missing o18 generate enrollment rides R34's route census.
`GeneratePane.vue` · `useColorGeneration.ts` · `generate-color.ts` **stay OUT of X-W1** — they are
product source, and W1.md's own Triumvirate trigger fires on any write under `demo/`. They go to
X-W4 (unit b, beside `GenerateControls.vue`) or to the formation boundary. **W1 cannot adopt a
product file to save a packet.**
⟨wb-generate-pane GEN-9 + closing verdict (the GEN-CLUSTER jurisdictional headline)⟩

**R44 · PACKET `AB-22 / PANE-CHROME` — DECLINE at W1; two arms retained; three riders relayed
intact** (cl.4)
AboutPane's own disposition is `NO-WAVE-OWNER (hygiene; **no X row owns the pane-chrome recipe**)`,
and the packet's proposed home is X-W10.d. **W1 declines the recipe.** Two arms ARE W1's, by
PaneHeader's own split:
(i) **PH-4's no-orphaned-cell arm** → R50 (the WebKit unreachability is a gate-slate defect).
(ii) **`o11-header-gates.spec.ts`** — a named file-list gap *"in NO X file list — partial
NO-WAVE-OWNER, flagged."* W1 adopts it because it is an ORACLE; `useHeaderCondense.ts` is product
source and is NOT adopted, so the packet's file-gap is only half closed here and the other half must
be booked at X-W10.d or the boundary.
**RIDERS CARRIED FORWARD UNCONSUMED (relayed verbatim so W1's decline cannot lose them)**: *"the G-4
'0.311 WebKit' post-cure pin is **STRUCK** for 0.618-both-engines; **CC-099 may not ship the bare
no-preference wrapper** — D-7's amended shape or the transposition; **producer adoption must carry
the owner-ruled constitutive veil floor**."* Also relayed: PH-1 has **no CC row** (a named ledger
gap), and the 9-host duplication is independently enumerated three times (AB-22's census ·
`motion-quarantine.md:161-162` · AdminPane L-12's nine hand-copies).
⟨AboutPane AB-22 (C-11) · PaneHeader PH-4 + the three X-W10.d riders + the two named file gaps + the PH-1 ledger gap⟩

**R45 · PACKET `PREVIEW-CHIPS` — ADOPT the oracle half; DECLINE the product half** (cl.4/cl.1)
Verified by this seat against every wave file: `PreviewRamp.vue`, `PreviewStrip.vue`,
`test/preview-chips.test.ts`, `aurora-harmony-stops.ts` and `mixStage.ts` appear in **no wave's
bounds**; `o14-preview-truth.spec.ts` likewise.
**RULING**: `e2e/smoke/oracles/o14-preview-truth.spec.ts` and `test/preview-chips.test.ts` enter
X-W1's bounds — R2 (dead locator, both halves), R3 (dialect, both species), R13 (PS-8: the o14 chip
leg *"is structurally unable to reach the strip species"* — visits Mix only, reads `backgroundImage`
which is `none` on a strip, and `STRIP_SEGMENT_CAP` is module-private so no oracle could name the
cap; `test/preview-chips.test.ts` exercises `sampleInterpolationRamp` only — **a function the strip
never calls**). The component files, `aurora-harmony-stops.ts` and `mixStage.ts` are X-W6's or the
formation boundary's, per PreviewRamp's own routing law and the record's *"rendered dark by a V-ruled
producer abrogation whose consumer half was never executed, guarded by an oracle that cannot run,
cannot pass, and is run by no automation."*
**REACHABILITY PRECONDITION (binding)**: R-1's door rides **X-W6**; PreviewRamp residue 11 states the
dependency — *"whether the o14 chip legs pass once R-1's door and R-2's dialect are cured …belongs to
X-W1's repaired oracle **on X-W6's repaired door**."* W1's oracle can be authored born-RED without
W6; it cannot be turned GREEN without it. **G-3's baseline must classify it accordingly.**
⟨PreviewRamp R-2/R-9/R-19 + residue 11 + closing verdict · PreviewStrip PS-6/PS-7/PS-8 + the verification-vacuum merged condition · ColorNutritionLabel CNL-2/CNL-3/CNL-5/CNL-17⟩

**R46 · PACKET `DEBUG/STATUS INSTRUMENT SURFACE` — DECLINE the component surfaces; ADOPT the five
gate rows; BOOK the bounds fact and relay it** (cl.4/cl.1)
**Correction to the packet, measured by this seat (dated, not silent)**: `picker-debugeventlog.md`
carries **no X-W1 routing at all** — its nine surviving rows route *"every one to X-W8 per CC-081"*.
Only `picker-pointerdebugoverlay`'s **TEST-0** is X-W1's (see R49). The packet's premise that
DebugEventLog routes 21 row-instances into W1/W8/W10 is correct for W8; it names no W1 row.
**RULING**: the component cures stay at X-W8 (CC-081 overlay limb, `W8.md:462`/G-11, under the SEQ-1
coupling law — *"the z-restore never lands alone"*) and X-W10 (register canon). Bounds-widening at
those waves is **their folds' act, not W1's**; a wave may not stamp another wave's bounds.
**ADOPTED HERE** (already routed to X-W1 by their own records): **TEST-0** (R49) · **AP-4** (R29) ·
**DSL-2** (R30) · **A-11** (R24) · **AF-21** (R13's member: *"harness re-hardened by X-W1"*, the
substantive gates being X-W7's G8-G13 + the n-fixture battery).
**BOOKED AND RELAYED (this seat's own verification)**: `DebugEventLog.vue`, `ParseEchoReadout.vue`
and `ActionFeedback.vue` appear in **NO wave file whatsoever**; `DockStatusLamp.vue` and
`ApiOfflineChip.vue` appear **only at `W10.md:307`**, inside a migration-baseline prose list. **Prose
mention is not ownership.** This is booked here as a cross-wave fact and relayed to the X-W8 and
X-W10 folds; it is a FORMATION-BOUNDARY item if neither adopts.
⟨picker-pointerdebugoverlay TEST-0 + U-4 · ApiOfflineChip AP-4 · shell-dock-dockstatuslamp DSL-2 + DSL-8 · shell-dock-parseechoreadout A-11 · ActionFeedback AF-21 · CORRECTION: picker-debugeventlog (no X-W1 row)⟩

### Band G — rows this fold mints

**R47 · MINTED — ORACLE-ORDINAL COLLISION between W6's created oracles and five shipped ones**
(cl.1) · MAJOR
Measured read-only by this seat (`ls e2e/smoke/oracles/`; `find e2e -name '*.spec.ts'`;
`W6.md:84-92`). W6 **creates** `o21-space-catalog-truth` · `o22-specimen-legibility` ·
`o23-specimen-gamut-honesty` · `o24-specimen-dot-identity` · `o25-atmosphere-response` ·
`o26-atmosphere-coldload` · `o27-scene-contracts`. The tree already ships
`o21-gradient-rail.spec.ts` (W6 modifies it — fine) · **`o22-status-lamp.spec.ts`** ·
**`perf/o24-lcp-identity.spec.ts`** · **`o26-aurora-perceptibility.spec.ts`** ·
**`o27-focus-affordance.spec.ts`**. **Four true collisions** (o22, o24, o26, o27), two of them on
files X-W1 holds or is routed rows against: `o26-aurora-perceptibility.spec.ts` is in **W1.md's own
File Bounds** as a `test.fail()` carve, and `o27-focus-affordance.spec.ts` carries R26's forced-colors
arm from three records.
**CONSEQUENCE**: the oracle ordinal is used as an identity across the corpus (records cite "o22",
"o27" bare). A duplicate ordinal makes every such citation ambiguous and makes G-5's slate
un-auditable by name.
**GATE**: G-5's oracle slate additionally fails on a duplicate oracle ordinal. See NG-14.
⟨MINTED by this seat: `ls e2e/smoke/oracles/` vs `W6.md:84-92`; touches W1.md File Bounds (o26) and R26 (o27)⟩

**R48 · wb-extract-controls EC-15 ≡ wb-gradient-visualizer GV-4 ≡ MX-25 ≡ ColorSpaceSelector L-5(a) —
every module-lattice lint rule is dead** (cl.1) · MAJOR · **FOLD, one identity**
The lint globs name the deleted `demo/@` tree (`eslint.config.js:235-238`, `:275-276`; `demo/@`
ABSENT — three independent verifications). *"FOLD — one identity… already carried → **X-W0/X-W1**.
Not re-booked."*
**BOUNDARY LOCK**: `eslint.config.js` is **X-W8's `modify-carve`**. X-W1 owns the **`lint` script and
its CI job**; its arm is that `npm run lint` must be capable of failing for its stated reason — a
`--max-warnings=0` run over globs that match nothing is the same class as G-2's decorative job.
Cross-edge to X-W0 (its census) and X-W8 (the glob cure).
⟨wb-extract-controls EC-15 · wb-gradient-visualizer GV-4 · wb-mix-animationcanvas MX-25 · ColorSpaceSelector L-5(a) · ActionFeedback L-2 headline⟩

**R49 · picker-pointerdebugoverlay TEST-0 — the born-RED debug-overlay spec** (cl.2) · MINOR
*"The entire 703-line subsystem has zero coverage across every suite — the mechanism of the ~4.5-month
dark period."* `PointerDebug|debug-overlay|debug=1` over populated `test/` + `e2e/` → **0**.
**GATE (verbatim)**: *"One spec loading `?debug=1`, expanding, asserting non-`auto` computed z-index +
one visible gauge value **fails at `c84504d3`** and catches rows 1/2/4 at the causing commit. ROUTE:
X-W1 (CC-030/031); **the spec may ride X-W8's cure commit as witness**."*
**SEQUENCING**: authored at W1 born-RED; may be co-committed with X-W8's z-restore under X-W8's SEQ-1
coupling law. Two waves, one commit — declared, so neither claims it twice.
⟨picker-pointerdebugoverlay TEST-0 (row 29)⟩

**R50 · MINTED (from PaneHeader PH-4's measurement) — the FILE-level CI-orphan class G-5 cannot see**
(cl.1) · MAJOR
PH-4 measures: *"the oracles subtree is **structurally unreachable from any WebKit project**
(`smoke-safari` `testDir` = disjoint `./e2e/smoke/safari`)"*, alongside `playwright.config.ts:142-159`
pinning the sole running project to 1280×720. W1.md's **G-5** slate compares *"`playwright.config.ts`'s
project list [to] the workflow's project args"* — a **project-level** predicate. PH-4's defect is
**file-level**: every project has a job, and a whole spec subtree is still reachable by no project ×
engine cell. The record itself routes only *"the no-orphaned-cell arm → X-W1 (CC-031 oracle-slate)"*
and stops; **this fold draws the gate consequence, which no record drew.**
**GATE**: the slate additionally fails when a tracked `*.spec.ts` is collected by **zero** projects, or
when a named engine (WebKit) collects **zero** files from a subtree the corpus routes engine-specific
rows against. See NG-1's second arm.
⟨MINTED from PaneHeader PH-4; predicate compared against W1.md:305 (G-5) and W1.md:218-219 (the slate mechanism)⟩

---

## 2. Gates

**Reading**: SHARPENED rows amend an EXISTING W1.md gate's predicate or falsifier — the gate number
and its born-RED baseline stand; the addendum's clause governs. NEW rows are gate CANDIDATES this
fold proposes; each names a real witness measured by a seat or by this fold (L-19: no proof-script
is presumed to be a witness). **14 SHARPENED + 15 NEW = 29 gate entries.**

### 2a. Sharpened — existing W1.md gates whose predicates these rows widen

| gate | W1.md predicate today | sharpening (governing clause) | witness |
|---|---|---|---|
| **G-1** | typecheck covers `e2e/`; zero refs to `demo/@/lib/palette/types` | baseline is **10 refs incl. ≥1 RUNTIME PATH STRING** (not 2 imports + 3 docs); the falsifier must include the runtime-string arm — `import type` extraction alone leaves it live | R4 ⟨AAP-34 ≡ AdminUsersPanel §3; AF-30 limb⟩ |
| **G-2** | `e2e-smoke`/`e2e-safari` HARD, no `continue-on-error`, no branch-push substitution | the HARD flip is **SEQUENCED AFTER** R2 + R3; a flip over the tree as measured reds master for non-product reasons and makes G-17 unreachable | R1 ⟨ABT-6 residue 12; 19 concurring B3 measurements⟩ |
| **G-3** | the suite's real pass/fail over 185 is executed and committed | the committed baseline **CLASSIFIES** each failure: RED-by-dead-locator (R2) · RED-by-dialect (R3) · RED-by-product · RED-by-unmet-precondition (R45's W6 door). An unclassified count is a number, not a baseline | R2, R3, R45 |
| **G-4** | LHCI runs `lighthouserc.json` unmodified | unchanged — but every LHCI-adjacent typography/geometry reading inherits R37's caveats (dev fonts; `content-visibility` `scrollWidth`) | R37 ⟨Katex R26 + C's trap⟩ |
| **G-5** | slate fails when a Playwright PROJECT has no CI job | widened three ways: (i) **file-level** orphans — a spec collected by zero projects (R50); (ii) **dead references** — a spec whose locators/paths do not resolve (R2); (iii) **duplicate oracle ordinals** (R47) | R2, R47, R50 ⟨PH-4; AP-4; M-21; A-10; AB-9⟩ |
| **G-6** | 3 `test.fail()` legs each become a real assertion or are deleted | the same law binds R2's dead legs: **`test.skip()` is deferral under a new name** and does not discharge either gate | R2 |
| **G-7** | falsifier demonstration on a scratch branch | the falsifier discipline is named as law by two records — *"in-viewport assertions, the one-time falsifier discipline"* (A-15) and *"falsifier law"* (M-GP-7) — and binds every gate this fold adds, not only the e2e job | R1, R13 ⟨A-15; M-GP-7⟩ |
| **G-8** | `e2e/visual/` with goldens COMMITTED for named routes × 3 viewports × light/dark | denominator widened by **modality** (R34) and by **non-route capture arms** (R35); L-19 discipline preserved — pane-hosted surfaces ARE reached, and the 5-route sample silently excludes them | R34, R35 |
| **G-9** | tolerance stated numerically; regeneration requires `--accept`, refuses dirty tree | unchanged; the injection test is the sole validator — R37's instrument register states which corpus figures may NOT be used as tolerance rationale | R37 |
| **G-10** | every golden + header records the renderer string read from the LIVE browser | **an EMULATED modality is labelled emulation and does not discharge a real-modality obligation** (real WHCM ≠ WebKit-emulated forced colors); Safari cells labelled per cell | R36 ⟨EC-8 residue 8; MX-17; AdminNamesPanel D-3⟩ |
| **G-12** | `boot-smoke --mode=dev` passes 4 assertions on a cold dev boot | the mode takes a **SEED MATRIX**, not one default boot: 28/256 greys crash before mount from the persisted/deep-linked seed, outside the ErrorBoundary | R16 ⟨CNL-3⟩ |
| **G-13** | `boot-smoke --mode=prod-preview` at bare `127.0.0.1` | same seed matrix + the `?color=`/`none` query-parameter states (R17); the run's artifact census discharges R42 for free | R16, R17, R42 |
| **G-17** | `ci` green on `master`; the `pack producer bytes` step present | the pack step's oracle asserts **EXPORTED-SURFACE equality**, never byte equality — the local/published delta is *private* `_2` declarations (`^export` = 53 BOTH), so a byte gate manufactures a failure | R39, R40, R41 ⟨R-9/L-10 ≡ G4 ≡ L-14; AP-21; MC-14/K-8⟩ |
| **G-20** | `color.babb.dev` serves v4; entry asset hash changes | also **records the source epoch of the PRE-deploy artifact** — the corpus's A/B reasoning is conditioned on whether production was a March build | R38 ⟨U-4⟩ |

*(G-11 real-GPU, G-14 NV-7 root, G-15/G-16 the glass pin, G-18/G-19/G-21 the landing: **no corpus row
touches them**. Recorded as a negative finding — 55 apotheoses and none contradicts those six.)*

### 2b. New — gate candidates, each with a real witness

| # | condition | born-RED witness (measured, named) | falsifier |
|---|---|---|---|
| **NG-1** | Every locator, selector, class and filesystem path a tracked spec names resolves at authoring, or the spec is deleted with rationale. No spec is collected by zero projects; no engine collects zero files from a subtree carrying engine-specific rows | R2's 15 enumerated members + R50: `.plate-caption` deleted at `a68ecdc1` yet asserted live at `o18:648-690`; `o10d:448-461` `readFileSync`s a dir deleted at `a61094e3`; 6 specs bind one impossible accname; `smoke-safari` testDir disjoint from `oracles/` | Re-point one locator at a deleted node → the census reds. A `test.skip()` conversion does NOT pass |
| **NG-2** | An oracle that reads product output parses it with the product's own parser/serializer; no hand-rolled regex over a serialized dialect; and no gate floor disagrees with its subject's declared law | `parseOklchTriples` = 0 matches on `oklch(62% 0.27 9.8deg)` vs 1 on canonical (dist run); `o14:378-381` = `expect(N).toBe(0)`; o20 string-equality "unfixable as written"; o18 `TEXT_FLOOR = 4.5` vs the module's own 5.75 | Re-introduce a bare regex reader → the oracle passes on a dialect the app never emits. Raising a floor to meet a measurement is an automatic failure (the G-4 idiom) |
| **NG-3** | `test/**` sits inside a TypeScript program and `npm run typecheck` covers it | DSL-8: 19 files, esbuild transpile, types never checked; `Array<[ApiAvailability, boolean, LampState\|null]>` decorative; `demo/test/**` IS in `tsconfig.demo` — two test homes, different type safety | Delete a type import in `test/` → typecheck reds. Covering `e2e/` alone does not satisfy NG-3 |
| **NG-4** | The vitest harness can mount an SFC, and any mount gate stub-strips `Transition` or runs a real browser | MCP-39: `vitest.config.ts` has **no `plugins` key** (re-verified `:19-27`); `@vue/test-utils@2.4.10` zero call sites; `mount(` = 0 repo-wide; test-utils cjs `:98-99`/`:7777` stub `Transition` by default | Author one SFC mount test → today it cannot build. A jsdom gate that passes on the stubbed `Transition` asserts the opposite of shipped behaviour |
| **NG-5** | The `strictTemplates` born-RED diagnostic count is measured and published with its real/noise split | SFB-17's executed run: **10 diagnostics = 6 real / 4 noise**; repo grep for `strictTemplates`/`vueCompilerOptions` = 0 (two independent seats) | A published count without the split, or a count copied from a prior document. **DISSENT: the FLAG may be X.W7.a G3's — see R8** |
| **NG-6** | The producer's `reka-binding-idiom.test.ts` assertion shape is adopted for the render-effect class `vue-tsc` misses | SFB-19: the canary exists in the producer repo with the header *"vue-tsc + units MISS them"*; never adopted; not shipped | Delete the adopted assertion → an SFB-1-class binding drift passes lint+tsc+units |
| **NG-7** | `npm test` has a build hook and no test file imports `demo/` internals for a non-demo reason | GV-15/MC-13: `pretypecheck` yes, **`pretest` ABSENT**; `vitest.config.ts:22-27` justifies its filter by a `test/dist/` that ENOENTs; L3-2: `gradient-v4-consume.test.ts:7-11` reaches 4 levels into demo, one of **TEN** such files | Rename a demo path → `npm test` reds for a non-library reason (today it does). The KILLED `src/`-realias cure is not an admissible fix (MC-K9) |
| **NG-8** | The born-RED crash battery exists and reds today | R14 Retry-blank (dev-permanent); R15 two-click `role="alert"` (256/256 greys); R16 boot seed matrix (28/256 greys, outside the boundary); R17 none-URL; R18 extract flow (`setInputFiles` = 0 repo-wide); R19 mobile dropdown (Enter on GitHub RED today) | Each arm has a named product mechanism; a battery that greens without its cure landing means the arm was authored to the cure, not to the defect |
| **NG-9** | The born-RED unit-oracle battery exists | R21 (64 KaTeX expressions, zero coverage, 0 of 92 shots); R22 (`readoutReservation.ts`, 209 lines, zero coverage, sibling names its own oracle); R23 (`spectrumLuma` hue-blind, 1.07:1 at yellow, `>` degenerate at s=1,v=1); R24 (`gamutVerdict` epsilon, already regressed once ~500×); R28 (8.65ch vs 10.5ch at 24px root) | Flip `>` to `>=` in `spectrumLuma` → the oracle must red (today no test touches the module) |
| **NG-10** | The oracle-repair battery lands: cycle-invariant + false-citation deletion (R20); stable-id alert binding (R25); the o27 forced-colors arm reaching every control class (R26); the o18 admin-branch + row enrollment (R27); AP-4's string guard (R29); DSL-2's rendered-TEXT gate (R30); typed fixtures on a re-derived DTO (R31); the 204/200 fixture repair (R32) | each row's measured cell, above | `toHaveAccessibleName` on the repaired lamp region fails for the wrong reason (name ≠ content for `status`) — the gate asserts TEXT or lands the author-name cure with it |
| **NG-11** | The visual matrix runs modality axes against the ROUTE CENSUS, and carries non-route capture arms (overlay · seeded-storage · seeded-fixture · forced-state) | R34: six modality matrices each holding the SAME five routes (adminusers/blob/browse/gradient/picker); extract, mix, admin-names, about, katex absent from all six; About is a PANE at `viewSchema.ts:107`. R35: 0-of-60 for the dialog; the delayed fixture exists and the VISUAL matrix never consumes it | Add a route/pane to the app without a matrix cell → the census reds. A matrix that samples 5 routes and reports coverage does not satisfy NG-11 |
| **NG-12** | The instrument-caveat register is committed and every gate consuming a corpus measurement cites its caveat or states none applies | R37: `vite-defer-glass-fonts.ts:72` guards a dead path so **dev never loads production faces**; `content-visibility:auto` makes `scrollWidth` report the placeholder; `smallTapTargets` measures paint not target (`SliderImpl.js:46-53`) | A gate citing a dev-server typography figure with no font caveat |
| **NG-13** | Packed-surface exported-equality + deploy currency | R39 (`dist/index.d.ts` ENOENT; 7 subpath pairs, no parsing/units); R40 (gitignored local build, `_2` collision, `.gitignore:17`); R41 (`^export` = **53 BOTH**, 383 vs 351 lines); R38 (`index-DJ8Ije_5.css` live vs `index-DS-0HlXP.css` at a 2026-03-24 ref) | A byte-equality assertion reds on the known-private `_2` delta — that is the gate failing for the wrong reason, and is itself an NG-13 failure |
| **NG-14** | No two tracked oracles share an ordinal | R47: W6 creates o22/o24/o26/o27 against shipped `o22-status-lamp`, `perf/o24-lcp-identity`, `o26-aurora-perceptibility` (**in W1.md's own bounds**), `o27-focus-affordance` (carries R26) | Mint a duplicate ordinal → the slate reds. Today four collisions are scheduled and no gate sees them |
| **NG-15** | `npm run lint` is capable of failing for its stated reason | R48: `eslint.config.js:235-238`/`:275-276` glob the deleted `demo/@`; three independent verifications that the tree is ABSENT — every module-lattice rule is dead while `--max-warnings=0` reports green | Introduce a lattice violation in live `demo/**` → lint must red. The glob cure is X-W8's; the *capability* assertion is W1's |

**Gate totals: 29 entries · 14 sharpened · 15 new · 0 blocked rows.** Every NEW gate names a witness
a seat measured or this fold measured read-only; none is satisfied by a script this fold invents
(L-19).

---

## 3. BoundsDelta

**THE STRIKE.** W1.md's §File Bounds "Do NOT touch" list names **`test/`**. Nine rows routed to X-W1
by the corpus land inside `test/` — R5 (`test/**` type program) · R11 (`vitest.config.ts` +
`pretest`) · R12 (`test/gradient-v4-consume.test.ts`) · R21 (the KaTeX corpus vitest) · R22
(`readoutReservation` oracle) · R23 (`spectrumLuma` oracle) · R24 (`gamutVerdict` pin) · R33 · R45
(`test/preview-chips.test.ts`). **The prohibition and the routings are incompatible.** This addendum
**STRIKES `test/` from W1.md's Do-NOT-touch list** and replaces it with a carve: *`test/**` — create
and modify-carve for the unit oracles this addendum names; **no existing assertion is weakened or
deleted except under R2's dead-reference rule with a written rationale**.* W1.md's bytes stand; this
clause governs. If X.W1 refuses the strike, R5/R6/R10/R11/R12/R21/R22/R23/R24/R33/R45 travel to the
formation boundary **as one packet**, never singly.

**Files to ADD (36).** Each is named by a row above and appears in **no** X wave's File Bounds unless
marked ⚠.

*Oracles (`e2e/smoke/oracles/`) — modify-carve:*
1. `e2e/smoke/oracles/o14-preview-truth.spec.ts` — R2, R3, R13, R45 ⟨R-2, R-19, PS-7, PS-8, SH-8, DVS-6⟩
2. `e2e/smoke/oracles/o20-generate-plate.spec.ts` — R3, R43 ⟨GEN-9, PS-6⟩
3. `e2e/smoke/oracles/o15-dock-register.spec.ts` — R2 ⟨ABT-6 (`:105-130`, `:126`), SH-8⟩
4. `e2e/smoke/oracles/o17-easing-composition.spec.ts` — R2 ⟨EAS-3 (`:51-52`, `:117`, `:189`), C-12⟩
5. `e2e/smoke/oracles/o18-contrast-census.spec.ts` — R2, R13, R27, R33 ⟨M-21 (`:648-690`), D-19/C-6, A-1, N-3, CNL-5 (`:697-743`)⟩
6. `e2e/smoke/oracles/o22-status-lamp.spec.ts` — R29, R30, R47 ⟨AP-4, DSL-2⟩
7. `e2e/smoke/oracles/o27-focus-affordance.spec.ts` — R26, R47 ⟨M-2 (`:206-228`), D2-02, MISS-3⟩
8. `e2e/smoke/oracles/o11-header-gates.spec.ts` — R44, R50 ⟨PH-4; the record's own "in NO X file list" gap⟩
9. `e2e/smoke/oracles/o10d-display-voice-census.spec.ts` — R2, R13 ⟨A-10 (`:448-461`), VHD-15 (`:148-153`, `:324-338`), CNL-5 (`:41`)⟩
10. `e2e/smoke/admin/o10d-admin-title-voice.spec.ts` — R2 ⟨A-10 family⟩
11. ⚠ `e2e/smoke/oracles/o9-shadow-palette.spec.ts` — **X-W7 owns modify-carve**; W1 COORDINATES only ⟨ES-19: X.W7.g's same-commit o9 re-rule⟩

*Flows and views:*
12. `e2e/smoke/flows/palette-save.spec.ts` — R2 ⟨SH-8 (`:35`), PP-2 (`:34-37`), A-3 (`:40-43`)⟩
13. `e2e/smoke/flows/palette-flag.spec.ts` — R13 ⟨FlagReportDialog `:30-49` stub fulfils 200 vs the route's 201⟩
14. `e2e/smoke/flows/palette-delete.spec.ts` · 15. `e2e/smoke/flows/palette-fork.spec.ts` — R13 ⟨AF-21⟩
16. `e2e/smoke/flows/color-propose.spec.ts` — R2, R20 ⟨AB-9 (`:22-25`, the phantom citation)⟩
17. `e2e/smoke/admin/flows/color-approve.spec.ts` — R31 ⟨M-LC6 (`:19`)⟩
18. `e2e/smoke/views/mix.spec.ts` · 19. `e2e/smoke/safari/mix-flow.spec.ts` — R2 ⟨MX-3, MSS-2, MR-2⟩
20. `e2e/smoke/views/browse-loading.spec.ts` — R13, R35 ⟨PCS-11, K-8 (`routeBrowsePalettesDelayed`)⟩
21. `e2e/smoke/views/browse-pagination.spec.ts` — R13 ⟨M10⟩
22. `e2e/smoke/reactivity-instant.spec.ts` — R13 ⟨C-7 (picker-spectrumcanvas)⟩
23. `e2e/smoke/a11y-modality-support.spec.ts` — R13 ⟨C-7: `:301-305` PINS the D-04 defect⟩
24. `e2e/smoke/admin/a11y-authed-admin.spec.ts` — R13, R25 ⟨EB-11 (`:139-144`)⟩
25. ⚠ `e2e/smoke/views/gradient.spec.ts` — R13 ⟨A-15 (`:233`, `:262`)⟩ — **COLLISION: W4 `modify` + W6 `modify (ADD-never-replace)`**
26. ⚠ `e2e/smoke/walk.spec.ts` — R2, R13 ⟨R-9 (`:78-81`), PG-6 (`:71-72`)⟩ — **COLLISION: W5 `modify-carve`**
27. ⚠ `e2e/smoke/mobile/walk.spec.ts` — R2 ⟨PS-7 (`:48`, `:54`, `:137`)⟩ — **COLLISION: W5 `modify-carve`**
28. `e2e/smoke/fixtures/**` beyond the four W1 already names — R35 ⟨A-30's seeded-storage arm; U-9's populated `/#/palettes`; AdminNamesPanel residue 2's seeded queue row⟩

*Unit tree (the STRIKE's surface):*
29. `test/preview-chips.test.ts` — R45 ⟨PS-8: exercises `sampleInterpolationRamp`, a function the strip never calls⟩
30. `test/gradient-v4-consume.test.ts` — R12, R13 ⟨L3-2 (`:7-11`), C-12 (`:56-58`, 7 tiles of slack), R-D (`:55-57`)⟩
31. `test/status-lamp.test.ts` — R30 ⟨DSL-2: 19 asserts, resolver-only, never mounts⟩
32. `test/**` (create) — R21, R22, R23, R24, R33 (the born-RED unit oracles)
33. `tsconfig.test.json` (create) — R5 ⟨DSL-8's named gap⟩
34. `vitest.config.ts` — R6, R10, R11 ⟨MCP-39 (no plugins key); MC-13 (`:22-27`'s false `test/dist` justification)⟩
35. `demo/test/**` (create `demo/test/shell/`) — R10 ⟨EB-26's 9 jsdom seeds⟩ — **note: `demo/` is W1's Do-NOT-touch; this is a `demo/test/` carve, not product source, and is declared as such**

*Read/execute, no write:*
36. `docs/tranches/V/megatranche/audit/visual/capture.mjs` + `REPORT.json` — R34, R35 ⟨the 60/92-shot matrix and its six state arms are X.W1.b's INPUT and its denominator's proof; W1.b's goldens supersede but must first read what the sample was⟩

**Files W1 must NOT add, though rows name them** (declared so a unit does not drift into them):
`GeneratePane.vue`, `useColorGeneration.ts`, `generate-color.ts` (R43) · `PreviewRamp.vue`,
`PreviewStrip.vue`, `aurora-harmony-stops.ts`, `mixStage.ts` (R45) · `useHeaderCondense.ts` (R44) ·
`DebugEventLog.vue`, `ParseEchoReadout.vue`, `ActionFeedback.vue`, `DockStatusLamp.vue`,
`ApiOfflineChip.vue` (R46) · `eslint.config.js` (R48, X-W8's carve) · `foundation.css` forced-colors
block (R36, NO-WAVE-OWNER) · `plugins/vite-defer-glass-fonts.ts` (R37, X-W8's fix; W1 inherits only
the caveat). Every one is product or another wave's source; W1.md's Triumvirate trigger fires on a
write under `src/`, `demo/`, `api/`.

---

## 4. CrossEdges

1. **X-W1 → X-W7** (heaviest). R14's cure is X-W7.g's `EmptyState.vue` carve; R32's behavioral
   battery is X-W7's G13/G14 rider (the AL-M10 precedent); R29's AP-9 rebuild; R13's PCS-11 partial
   cover (W7 G3 compile + G20 height); ES-19's o9 re-rule is X.W7.g's **same commit**. `W7.md:576`
   already books *"PaletteCard rest + hover goldens (3 viewports × light/dark) → `e2e/visual/`
   (X-W1-owned)"* — a **standing dependency in the other direction**: W7's own gate consumes W1's
   harness.
2. **X-W1 → X-W6** (blocking, both ways). R45: the o14 chip legs cannot go green until R-1's door is
   cured at X-W6 — *"X-W1's repaired oracle **on X-W6's repaired door**."* R47: W6's created oracle
   ordinals collide with four shipped ones, two touching W1's bounds. R13/R12: W6 modifies
   `views/gradient.spec.ts` ADD-never-replace while R13's in-viewport lock changes its assertion
   shape — **the two must be sequenced or they will fight over one file**.
3. **X-W1 → X-W5** (file collision). `walk.spec.ts` and `mobile/walk.spec.ts` are W5 `modify-carve`
   and carry R2 rows (R-9's mis-resolving locator; PS-7's false-premise timeout). W1.md §Disjointness
   asserts *"No two units share a `modify` path"* — **that assertion is now false across waves**, and
   the resolution belongs to the tranche's sequencing, not to either wave's close.
4. **X-W1 → X-W4**. R26's five-site `outline:none` extirpation is X-W4's; W1 owns only the o27 arm.
   R23's border regime, R28's edit contract, R43's GeneratePane seat (X-W4 unit b) likewise.
5. **X-W1 → X-W8**. R48's glob cure (`eslint.config.js`, X-W8 `modify-carve`) · R37's
   `vite-defer-glass-fonts.ts:72` fix · R22's module home · R49's SEQ-1 co-commit (*"the spec may ride
   X-W8's cure commit as witness"*, under *"the z-restore never lands alone"*) · R46's whole component
   surface (CC-081, `W8.md:462`/G-11). `W8.md` states the reciprocal boundary explicitly:
   *"`.github/workflows/**` — **X-W1 owns every CI byte.**"*
6. **X-W1 → X-W9**. R15/R16/R17's cures are the `"none"` protocol at the library seam (CC-084's
   `Result` battery); W1 owns the gates. R39's packed surface abuts X-W9's `package.json` ownership.
7. **X-W1 → X-W10**. R44's three riders (G-4's WebKit pin STRUCK for 0.618-both-engines; CC-099's
   bare no-preference wrapper ban; the constitutive veil floor) travel to X-W10.d **unconsumed**.
   R27's one-admin-treatment decision and R28's type law are X-W10's; W1 owns the hooks and the zoom
   gate.
8. **X-W1 → X-W0**. R48 is dual-homed X-W0/X-W1 (*"FOLD… → X-W0/X-W1. Not re-booked."*). W1.md's
   §Dependencies already binds W0's CC-012 track-or-archive (this fold's evidence trees) and CC-025
   (no `264/264` may appear in any gate here).
9. **X-W1 → X-W2**. R42's artifact census is *"the next X-W1/X-W2 sitting"*; R38's deploy currency
   conditions X-W2's eager-bytes delta, which W1.md already books as blocked on G-13.
10. **X-W1 → GLASS BH RELAY**. R8's 4 noise diagnostics (producer d.ts gaps) · R9's canary
    publication · R36/EC-8's producer-transparent `.slider-range` · R37's `smallTapTargets` species
    note. All ride the standing fond; none is a W1 write.
11. **X-W1 → FORMATION BOUNDARY**. If W1 declines the `test/` strike: R5+R6+R10+R11+R12+R21+R22+R23+
    R24+R33+R45 travel as ONE packet. If W1 declines R6: every mounted oracle in NG-4/NG-9/NG-10
    travels with it. R43's product half, R45's product half, R44's recipe, and R46's five
    bounds-orphaned components are boundary items today unless X-W4/X-W6/X-W8/X-W10 adopt them in
    their own folds.
12. **THE STANDING NEGATIVE.** Six of W1's twenty-one gates (G-11 real-GPU · G-14 NV-7 · G-15/G-16
    the glass pin · G-18/G-19/G-21 the landing) are touched by **no** row in 88 component apotheoses.
    Recorded so their silence reads as absence of contradiction, never as absence of scope.

---

## 5. Closing — status verbs

| verb | state | basis |
|---|---|---|
| AUDITED | **YES** | 55 assigned records read; whole-corpus sweep for `X-W1` + dotted forms; 59 routing files; 4 packets adjudicated |
| SPECIFIED | **YES — by W1.md, SHARPENED by this file** | 50 rows · 29 gate entries · 36 bounds additions · 12 cross-edges |
| IMPLEMENTED | **NO** | no product or workflow byte written; this file is the fold's sole write |
| VERIFIED | **NO** | no gate has run |

**Execution stays gated.** X.W1's status remains **planned**. Nothing here opens product source;
nothing here stamps a wave. Dispositions folded from the corpus are **ADJUDICATED, not VERIFIED** —
that property is preserved through the fold. Every dated wave spec's bytes stand untouched (E-3); the
two strikes this addendum makes (W1.md's `test/` prohibition; the G-5 project-level predicate) are
recorded here and nowhere else.

---

## 6. FOLD ROUND 1 — dated repair addendum (2026-08-28)

**What this section is.** The X·V fold round-1 repair seat's single write into this file, under the
spine's ROW-CARRIAGE LAW (`REFINEMENT-FOLD-2026-08-28.md` §1, appended this round) and the work
order at `refinement/ROUND-1-ASSIGNMENTS.md`. It (a) folds every ⟨record·id⟩ row this seat's own
re-run census found routed to X-W1 and absent from this file, assignment-listed or not; (b) folds
the one **EXTERNAL** I-28 row the assignment routes here; (c) cures the two PASS-1-CHECK defects
that name this file by their own instruments. **Nothing in §§0–5 is deleted or re-graded.** Banked
ids stay original for life; where a round-1 roster id is a facet of an already-folded identity the
cure is an explicit `≡`-pointer row, never a silent skip, so the byte census closes. Status
**planned**; dispositions **ADJUDICATED, never VERIFIED**; no product source opened; the dated wave
specs and `CONFORMANCE-2026-08-03.md` stay byte-untouched (E-3).

### 6.0. The re-run census — method, receipts, result

The assignment roster is a **floor, not a ceiling**, so this seat re-derived the X-W1 census from
the corpus rather than consuming the roster. Method, byte-for-byte re-runnable from the repo root:

| # | command (as printed, verified to produce the printed number 2026-08-28) | result |
|---|---|---|
| C-1 | `cd docs/tranches/V/megatranche/registry/adjudicated && ls *.md \| grep -vE '^(kf\|fr\|pt)-' \| wc -l` | **92** — the corpus band |
| C-2 | `grep -lE '^[[:space:]]*([-*+]\|\\\|)[[:space:]]*\*\*.*X[-.·]W1([^0-9]\|$)' $(ls *.md \| grep -vE '^(kf\|fr\|pt)-') \| wc -l` | **54** records |
| C-3 | same pattern with `grep -hcE … \| paste -sd+ - \| bc` | **101** row-line ⟨record·id⟩ rows |

Boundary discipline: `X-W1` is matched with a trailing non-digit guard, so **`X-W10`/`X-W11` never
prefix-match** (the §0 header's 14-file trap, re-confirmed); en-dash, `·` and dotted (`X.W1.b`)
forms are all counted; zero-padded ids are their own identity. Of the 101, **62** carry an explicit
routing verb on the row line and **39** carry the `X-W1` token without one (adjudication-table
cells and rider clauses); block-grain-only rows: **0** genuine (the two candidates the loose parse
raised — ⟨MiniColorPicker · MCP-7⟩, ⟨wb-extract-pane · XP-28⟩ — are block-bleed across a non-bold
table row, and their true X-W1 limbs, MCP-9 and XP-7, are folded at R13/R18).

**Result — four escapes, all folded below.** Two escaped by BYTES; two escaped by **colliding
namespace**, which is the failure mode the check's BLOCKER names (an id "matched only by a
colliding namespace" is not carried):

| ⟨record.md · id⟩ | anchor | escape mode | folded at |
|---|---|---|---|
| ⟨wb-mix-pane.md · R-MP11⟩ | :107 | bytes (0 hits, whole fold layer) | **R51** |
| ⟨PreviewStrip.md · C-2⟩ | :25 | namespace — `C-2` in the layer resolves to App/other dockets | **R52** |
| ⟨picker-spectrumcanvas.md · L-12⟩ | :84 | namespace — this file's only `L-12` is **AdminPane's**, at the R44 cell (:742) | **R53** |
| ⟨wb-extract-imagedropzone.md · R-21⟩ | :67 | quotation, not booking — carried only inside R-28's quoted sibling list `R-2/R-7/R-13/R-21` (:571) | **R54** |

**Reconciliation with the work order.** ROUND-1-ASSIGNMENTS §B routes exactly **one** row to X-W1
(⟨wb-mix-pane · R-MP11⟩) and §C one EXTERNAL row (X-EXT-5); §A routes **zero** NO-WAVE-OWNER rows
here. This seat reproduces the §B row id-for-id and adds **three** it did not carry — one an
NWO-marked row (⟨wb-extract-imagedropzone · R-21⟩, `NO-WAVE-OWNER` on its own row line), two
namespace-collision escapes invisible to a byte-only test. Stated, not silently absorbed: **§A NWO
delta +1 · §B census delta +3** against the round-1 roster for this seat.

### 6.1. Rows R51..R56

**R51 · wb-mix-pane R-MP11 — the RED e2e run of record was never executed; only entailed**
(cl.3 · MEASURE-AT-OPEN) · MAJOR (gate truth)
Verbatim: *"the RED e2e run (locators statically unsatisfiable per MX-3 — entailed, not re-run; CI
limb banked → X-W1)."* The row is the corpus's own admission that **no seat ever ran the mix
specs** — the RED is derived from MX-3's static unsatisfiability (R2's member), not observed.
**Consequence for G-3 (this is why the row is W1's, not the mix waves')**: G-3's committed baseline
must record `mix.spec.ts` / `safari/mix-flow.spec.ts` as **RED-by-dead-locator, class ENTAILED —
never executed**, and the first HARD run is the first observation. A baseline that prints an
executed-looking number for these two files without a run id would launder an entailment into a
measurement — the exact species R1's ABT-6 residue 12 warns of (*"the first HARD e2e run after
CC-031 lands is the witness"*).
**IDENTITY**: the CI limb ≡ **R1**; the locator limb ≡ **R2** (MX-3 ≡ MSS-2 ≡ MR-2); the sibling
⟨wb-mix-pane · F-8⟩ is already folded at **R41**. Nothing re-booked; R-MP11 is banked here in its
own right because its content is the *absence of the run*, which neither R1 nor R2 states.
⟨wb-mix-pane R-MP11 (UNPROVEN-NEEDS-LIVE residue, :107) — CI limb ≡ R1 · locator limb ≡ R2⟩

**R52 · PreviewStrip C-2 — `≡`-POINTER: the o20 encoding leg's CI limb, resolved BY IDENTITY**
(cl.1) · MAJOR-by-identity · **banked, not re-booked**
The adjudication-layer row ⟨PreviewStrip · C-2⟩ ("C-2 grade + state") rules **MAJOR — resolved BY
IDENTITY ≡ GenerateControls R-A/G1**, adopts that arbiter's fresh run (**1 failed / 1 passed**;
leg 2 deterministically RED on the encoding mismatch, leg 1's failure a transient of the s3:D14
instability class), supersedes reader-LC's *"2 failed at dock.ts:78"* cell, and closes: *"the o20
repair is already gated at `V·MEGA-W-GEN-1` G1; **the CI limb is ≡ X-W1 CC-031** (B3/B7: 6 of 6
Playwright projects CI-orphaned). Not re-booked; identity preserved."*
**Why this row exists here at all**: §1's **R3** already carries this ruling's *substance* under
PS-6's name, and R1 carries the CI limb — but the id `C-2` was carried nowhere in this file, and
the only `C-2` bytes in the fold layer belong to other dockets. Under the ⟨record.md · id⟩ citation
law that is an escape, and the cure is this explicit pointer, not a second booking. **No grade, no
gate and no count moves**: R52 adds an identity to the census, not a defect to the wave.
⟨PreviewStrip C-2 (:25) ≡ R3 (PS-6/PS-7 dialect) ≡ R1 (CI limb, CC-031) ≡ GenerateControls R-A/G1 — POINTER ONLY⟩

**R53 · picker-spectrumcanvas L-12 — two sibling seams with no unit coverage, and an SFC that
cannot mount outside its parent** (cl.2) · **INFO — grade preserved at its home**
Verbatim: *"L-12 — CONFIRMED (INFO) → X-W8/X-W1 riders. The two sibling controls split
gesture/presentation at opposite seams, both justified by the same LoC cap; **neither seam has unit
coverage**; the SFC **hard-asserts two injections so it cannot mount outside ColorPicker**."* The
record's own terminal routing puts L-12 in the **subtraction/hygiene cluster → X-W8** and its gate
cluster (*"C-7-strengthened + M-21 + the luma/plate unit oracles"*) → **X-W1**; the row line routes
both. **W1 takes the coverage rider ONLY** — the seam/module relocation is X-W8's and claiming it
at close is a scope defect (the R39 BOUNDARY-LOCK idiom).
**MOUNTABILITY LOCK (binding, and the reason this is not a bare INFO for W1)**: the second clause
is a **precondition fact, not a nicety** — two hard `inject(...)!` assertions mean no `mount()` of
this SFC can succeed outside `ColorPicker`, so **R6/R7's harness cure is necessary but not
sufficient here**: the oracle either provides both injections explicitly or is authored as a
pure-function seam test. A mount gate that quietly wraps the component in `ColorPicker` to make it
mount has tested the parent.
**Rides**: R6 (harness), R23 (`spectrumLuma`, the same record's luma/plate oracle limb), NG-4,
NG-9. **Grade discipline**: INFO at its home stays INFO here (the AdminTagsPanel precedent —
severity lives at the identity's home).
⟨picker-spectrumcanvas L-12 (:84) — X-W1 rider half only; X-W8 owns the seam/module half⟩

**R54 · MERGED — the RESIDUE-WITNESS register: corpus residue items whose ONLY discharge is an
X-W1 matrix cell** (cl.2) · MAJOR
Four corpus records park a residue item whose stated discharge is not a cure anywhere but **a
capture in W1's matrix**. They are not defect rows and must not be counted as such — but each is a
standing obligation on X.W1.b, and W1's close should be able to enumerate what it discharges.
· **⟨wb-extract-imagedropzone · R-21⟩ (:67, `NO-WAVE-OWNER` on its own row line)** — *"D-15 —
physical `right-1.5` corner pin; RTL lands at inline-start… survived both a relocation and a value
change. **NO-WAVE-OWNER** (the rtl modality capture that would witness it is A-4's hole, routed
X-W1)."* The cure is ownerless; **the witness is W1's rtl-desktop/rtl-mobile cell** — which R34
proves holds only the same five routes. This is the **NWO limb** this seat adds to the round-1
roster (§6.0).
· **⟨wb-extract-workbench · residue 5 "Zoom-200 clipping"⟩ (:128)** — *"`code.fira-code` clipped in
all four state arms… WCAG 1.4.4 consequence sound **if the measurement holds**; needs one
confirming instrument — **an X-W1 golden input** alongside XW-18's matrix absence."* One
zoom-200 cell over `/#/extract` decides a WCAG 1.4.4 row that is otherwise single-instrument.
· **⟨wb-extract-imageeyedropper · residue 2 "Geometry amounts"⟩ (:144)** — *"the 200% arm's
2381.9/378 px… the 96 px pinned/unpinned readout reflow, the ~60 px letterbox band. The
'unscrollable' inference is DEAD (K-7); **the amounts await the X-W1 matrix**."* Recorded with its
own kill: W1 captures amounts, it does not revive K-7's inference.
· **⟨SwatchHoverMenu · residue 7⟩ (:100) — `≡`-POINTER to R35** — *"…then prescribed the exact
seeding fix that **becomes X-W1's matrix input**."* The seeding fix itself is already banked as
SH-28 at R34/R35; this pointer closes the residue's citation, nothing more.
**CURE-SHAPE LOCK**: a residue-witness cell is discharged by a **captured frame with its modality
labelled** (R36's law: an emulated modality is labelled emulation and discharges no real-modality
obligation), or it is carried forward still open. Silence at close is not discharge.
**Feeds NG-11** (route-census + non-route arms) as named members; adds no new gate.
⟨wb-extract-imagedropzone R-21 (NWO) · wb-extract-workbench residue 5 · wb-extract-imageeyedropper residue 2 · SwatchHoverMenu residue 7 ≡ SH-28 (pointer)⟩

**R55 · EXTERNAL — ⟨INBOX I-28 · X-EXT-5⟩: the o7 card census goes BORN-RED at the glass 8.0.0
repin** (cl.2) · MAJOR · **EXTERNAL, mail-sourced — this row carries no registry id**
**Provenance is mail, not corpus**: `docs/tranches/V/coordination/INBOX.md` row **I-28** (glass
v8.0.0 @ `17a11bc5`; letter + §5 addendum 08-25; ROWED 08-28), mirrored to this seat by
ROUND-1-ASSIGNMENTS §C. Cited as ⟨INBOX I-28 · X-EXT-5⟩ — **never as an X-W1 row id and never as a
registry id**.
**The break**: at 8.0.0 the `grain` prop is removed library-wide, so `data-grain` stops being
stamped. `e2e/smoke/oracles/o7-card-census.spec.ts` asserts grain **ON**. Witness re-measured
read-only in-tree by this seat 2026-08-28 (the file is 20,575 B, tracked, un-skipped):
· `:104` — `grain: el.dataset.grain ?? "(none)"` (the census reads the attribute).
· `:224` — the stated law in comment: *"the picker's register — tier=resting, grain on, stamp
present."*
· `:234` — `expect.soft(card.grain, \`${view} (${scheme}): grain not on\`).toBe("true")`, inside a
`for (const card of census.cards)` loop over every mounted pane/picker Card.
**Two sharpenings this seat adds, both from the file's own bytes:**
(i) **`expect.soft` is not a warning.** Soft assertions accumulate and fail the test at its end —
so at the repin this is a **hard RED leg per card per view per scheme**, not a soft note. A reader
who takes `.soft` for advisory will under-estimate the blast radius.
(ii) **The o7 population and X-EXT-3's population are DIFFERENT, and must not be conflated.** `:101`
filters `el.dataset.surface !== "veil"`, and `:110-112` names today's sole veil fixture as *"the
picker's sliders console"* — i.e. `ComponentSliders.vue`, which is exactly where X-EXT-3 routes the
`:grain="false"` deletion (`ComponentSliders.vue:29`, re-verified in tree; it is the ONLY `:grain`
binding in `demo/`). **X-W6 deletes a prop on the card o7 already excludes; X-W1 owns the grain
assertion on the cards o7 includes.** Neither cures the other's population, and neither may cite
the other's landing as its own discharge.
**BOUNDS CONSEQUENCE**: `o7-card-census.spec.ts` is named in **no** X wave's File Bounds
(`grep -n 'o7' docs/tranches/X/waves/W1.md` → 0; W1's oracle carve names only o16 and o26) — it
enters at §6.3 entry 37.
**SEQUENCING (binding)**: X-EXT-5 goes **LIVE AT THE REPIN, not before**. The repin act is
X-W0.j's, and its census PASS branch is unexecutable without X-EXT-6's peer install
(`vue-component-type-helpers ^3.0.3`, X-W0). Until then this row is a **scheduled** born-RED, and
W1 must not author the cure against a producer it has not installed. Gate: **NG-16**.
⟨INBOX I-28 · X-EXT-5 — EXTERNAL, mail provenance; no registry id. Cross-cites: X-EXT-3 (X-W6, the `:grain="false"` site) · X-EXT-6 (X-W0, the peer install) · R2 (the dead-locator census discipline this row rides)⟩

**R56 · MINTED — the "X-W1 B13" pointer resolves to `W1.md:95`, and its exports-map baseline is
scoped to glass 5.0.0** (cl.1) · MAJOR · **dated correction, cures PASS-1-CHECK MAJOR #4**
PASS-1-CHECK reports that the spine's §READINESS R.2 gate 10 cites *"X-W1 B13 (the exports-map
holes)"* while `grep -n 'B13\|B-13' X-W1-FOLD.md` → **0 hits**, and that its RED baseline is false
at the installed producer. Both halves are correct, and the resolution is stated here rather than
by touching the spine or the spec (E-3):
**(a) The pointer is not dangling — it resolves outside this file.** `B13` is a **W1.md baseline
row**, at **`W1.md:95`**: *"demo subpaths ∩ BG exports map | 18 subpaths used; exactly `./blob` and
`./chip` ABSENT from the **5.0.0** exports map (`./blob` ×5 sites incl. `useAtmosphere.ts`; `./chip`
×1, `EasingSpecimenStrip.vue`)."* It is also cited by the spec at `:259`, `:315` (G-15's born-RED
cell) and `:359`. This fold never cited B13 and does not book it now — a fold may not re-book a
spec's baseline row. The check's zero-hit measurement over *this file* is therefore TRUE and
**expected**; what was missing is the pointer's target, supplied here.
**(b) The baseline is TRUE-AS-WRITTEN and STALE-AS-USED.** B13 is explicitly scoped to the **5.0.0**
map (its own cell prints `B12 = 5.0.0`). Re-measured read-only by this seat 2026-08-28 against the
installed producer — `node -e "const p=require('./node_modules/@mkbabb/glass-ui/package.json'); …"`:
**version `7.0.0` · `./blob` true · `./chip` true · `./watercolor-dot` true · `./forms` true ·
`./input` false**. So at the installed pin **both subpaths ARE exported** and a gate predicated on
their absence **cannot go RED**. The import half stands unchanged and was re-verified:
`grep -rn 'glass-ui/blob\|glass-ui/chip' demo/ src/` → **6 sites** (`BlobPane.vue:12/:13`,
`HeroBlob.vue:34/:35`, `useAtmosphere.ts:36`, `EasingSpecimenStrip.vue:14`). **The defect is a
SCOPE error in the citation, not an error in B13.**
**(c) What survives for W1.** `G-15`'s *condition* — *"`deploy-pages.yml` resolves
`@mkbabb/glass-ui` from the registry; no `ref: tranche/BG`"* — is untouched by the exports map, and
its falsifier (*"revert to the branch checkout → the deploy build fails to resolve `./blob`"*)
still holds at 7.0.0 by a **different mechanism**: the branch checkout, not an absent subpath. See
the G-15 sharpening at §6.2.
**(d) The standing negative is corrected, not weakened.** §4 cross-edge 12 records that six of
W1's 21 gates are *"touched by no row in 88 apotheoses."* That claim concerned **corpus** rows and
**remains true** — this seat re-swept and no corpus row touches G-11/G-14/G-15/G-16/G-18/G-19/G-21.
Restated for accuracy: **no corpus row touches them; two non-corpus rows now do** — R56
(fold-minted) and R55 (EXTERNAL) both land on the glass-pin pair G-15/G-16. Corpus silence: six.
Total silence: four (G-11, G-14, G-18/G-19/G-21 less the pair).
**(e) Re-measure-at-repin clause.** At the 8.0.0 adopt the exports map moves again (X-EXT-2:
`./forms` → `./input`; measured above as `forms` true / `input` false, i.e. the rename has **not**
landed). **Any citation of B12/B13 after the repin must re-measure first** — a baseline inherited
across two majors is the L-19 dead-witness shape.
⟨MINTED by this seat: `W1.md:95` (B13) + `:315` (G-15) read read-only; installed `@mkbabb/glass-ui@7.0.0` package.json read; import census re-run. Cures PASS-1-CHECK MAJOR #4; relays to the X-W0 sitting (the repin census) and the glass BH fond⟩

### 6.2. Gate entries added this round (29 → 31)

**NEW — NG-16** (born-RED at the repin, witness real and in-tree):

| # | condition | born-RED witness (measured read-only 2026-08-28) | falsifier |
|---|---|---|---|
| **NG-16** | At the glass 8.0.0 repin, every oracle assertion predicated on a REMOVED producer API is re-pinned in the same change that repins the producer — and the o7 grain census is the named first member | R55 ⟨I-28 · X-EXT-5⟩: `o7-card-census.spec.ts:234` `expect.soft(card.grain, …).toBe("true")` over every non-veil card × view × scheme, fed by `:104` `el.dataset.grain`; `grain` is removed library-wide at 8.0.0. Today's pin measures `7.0.0` and the attribute still exists, so the leg is **scheduled-RED, not yet RED** — stated as such | Repin the producer without touching o7 → the smoke run reds for a producer reason and G-2's HARD flip eats the blame. A `test.skip()` on the grain leg does NOT pass (G-6/R2's law) |

**SHARPENED — G-15** (first sharpening of this gate; the pass-1 fold left it in the standing
negative):

| gate | W1.md predicate today | sharpening (governing clause) | witness |
|---|---|---|---|
| **G-15** | `deploy-pages.yml` resolves `@mkbabb/glass-ui` from the registry; no `ref: tranche/BG`. Born-RED cell cites `B12 = 5.0.0`, `B13 = ./blob + ./chip absent from that exports map` | the **condition stands unchanged**; its born-RED *citation* is scoped to 5.0.0 and is **stale at the installed 7.0.0**, where `./blob` and `./chip` are both exported. G-15 is read on its resolution clause and its branch-checkout falsifier, **never on the exports-map cell**, unless that cell is re-measured at the pin of the day. At the 8.0.0 repin, re-measure before citing | R56 ⟨`W1.md:95`/`:315`; installed 7.0.0 map; 6 live importers⟩ |

**Gate-cell additions (no new entries — members added to existing cells, named so the count does
not drift):** **G-3** gains R51's ENTAILED-never-executed class for the two mix specs · **NG-4** and
**NG-9** gain R53 (the two-injection mountability precondition + the sibling-seam oracle) ·
**NG-11** gains R54's four residue-witness members (rtl · zoom-200 · geometry amounts · the seeded
`/#/palettes` pointer) · **G-5**/**NG-1** gain `o7-card-census.spec.ts` as a slate member (R55).
**New totals: 31 entries · 15 sharpened · 16 new · 0 blocked rows.**

### 6.3. BoundsDelta — one addition (36 → 37 entries)

37. `e2e/smoke/oracles/o7-card-census.spec.ts` — **modify-carve** — R55 ⟨I-28 · X-EXT-5: `:104`,
    `:224`, `:234`⟩. Named in **no** X wave's File Bounds (W1.md's oracle carve lists only o16 and
    o26; `grep 'o7' W1.md` → 0). W1 takes it because it is an **oracle** and the change is an
    assertion re-pin — no `demo/` byte, so W1.md's Triumvirate trigger does not fire.
    **NOT added by this row**: `ComponentSliders.vue` (X-EXT-3's `:grain="false"` site) is product
    source and X-W6's — it joins the §3 "Files W1 must NOT add" list.

The other three round-1 rows need no new path: **R51** rides `views/mix.spec.ts` +
`safari/mix-flow.spec.ts` (entries 18/19) · **R53** rides the `test/**` carve (entry 32) and the
R6 harness (entry 34) · **R54** rides the capture inputs (entries 28, 36).

### 6.4. Dated corrections — the PASS-1-CHECK MINOR that names this file

**(a) The `test/` STRIKE headline: NINE and ELEVEN are two different predicates, and both are
true.** PASS-1-CHECK MINOR #5(b) records that §3's headline reads *"**Nine rows**… land inside
`test/`"* while the same block's escape clause enumerates **eleven** (adding R6 and R10), and that
the spine silently elected the larger figure. The contradiction is a missing predicate, supplied
here — **§3's bytes stand; this clause governs**:
· **Predicate A — "the row's surface is INSIDE `test/`": NINE at pass-1** (R5 · R11 · R12 · R21 ·
R22 · R23 · R24 · R33 · R45). R6's surface is `vitest.config.ts` (repo root) and R10's is
`demo/test/**` — **neither is inside `test/`**, which is precisely why the headline excluded them.
· **Predicate B — "the row RIDES the strike (its cure is unreachable while the prohibition
stands)": ELEVEN at pass-1** = the nine + R6 + R10. This is the figure §3's escape clause and §4
cross-edge 11 use, and the one the spine's §2 e.16 elected — **correctly, for its own predicate**.
· **Round-1 movement, stated:** **R53** joins Predicate B (**twelve**), because its unit-oracle arm
is unreachable while `test/` is prohibited. It joins Predicate A **only if** authored as a
pure-function seam test; authored as a mount it lands under R6's harness instead. That fork is
declared, not resolved — so Predicate A stays **NINE + 1 conditional**, never silently ten.
· **The packet law is unchanged**: if X.W1 refuses the strike, Predicate B's rows travel to the
formation boundary **as one packet**, never singly — now twelve, not eleven.

**(b) The count that changed, and the one that did not.** No pass-1 row, gate, dissent, packet
ruling or grade is amended by §6. The moving figures are exactly: rows 50 → 56, gate entries
29 → 31, bounds entries 36 → 37, cross-edges 12 → 14, external rows 0 → 1.

**(c) BoundsDelta unit — "36 entries" vs "~37 files", reconciled.** PASS-1-CHECK MINOR #5(c) notes
the spine's §0 table prints `~37 files` in the W1 cell against this fold's twice-stated **36 bounds
additions**. Both readings are arithmetically right over different units and the fold's declared
unit is the **numbered entry**: §3 numbers 1..36, and a *file* count runs one higher because entry
36 names **two** artifacts (`capture.mjs` **+** `REPORT.json`). So pass-1 = **36 entries / 37
files**; after §6.3 = **37 entries / 38 files**. The declared unit stays the entry; the file
reading is printed beside it so no reader has to derive it.

**(d) On the three §READINESS commands that name W1's surface.** PASS-1-CHECK MAJOR #3 finds five
of the spine's ten printed R.2 commands misfire; three touch this wave's subject matter and are
answered here, in this fold's own voice, without editing the spine: gate 10 is R56 above; gates 2
and 4's `require('glass-ui/package.json')` form is unrunnable twice over — the installed name is
**`@mkbabb/glass-ui`**, and even scoped, `./package.json` is not in the exports map, so the working
form is a **path read** (`require('./node_modules/@mkbabb/glass-ui/package.json')`), which is the
form this seat used at R56(b) and which does produce its printed output. Recorded as an instrument
correction of the **NG-12 species** (a gate that consumes a measurement cites the instrument that
actually produced it) and relayed to the X-W0 sitting, which owns the repin census.

### 6.5. CrossEdges 13–14 (12 → 14)

13. **X-W1 → X-W6 / X-W4 (the grain populations, non-overlapping).** R55: o7 asserts grain ON over
    every **non-veil** card (`:101` filter); X-EXT-3 deletes `:grain="false"` at
    `ComponentSliders.vue:29`, which **is** the veil fixture o7 excludes (`:110-112`). Two
    populations, one prop name. Neither wave's landing discharges the other's, and neither may cite
    the other as its witness. R53's seam/module half is X-W8's on the same discipline.
14. **X-W1 → X-W0.j (the repin act).** R55 goes live **at** the repin, not before; R56's
    re-measure-at-repin clause binds every B12/B13 citation after it; X-EXT-6's peer install
    (`vue-component-type-helpers ^3.0.3`) is X-W0's and is a precondition of the census's PASS
    branch. W1 authors the o7 re-pin **against a producer it has installed**, or not at all.

### 6.6. Round-1 arithmetic

| axis | pass-1 | round 1 | delta |
|---|---|---|---|
| rows | 50 (R1..R50) | **56** (R1..R56, gapless) | **+6** — R51 census-escape (roster) · R52/R53 namespace escapes · R54 residue-witness (carries the +1 NWO limb) · R55 EXTERNAL · R56 minted correction |
| gate entries | 29 (14 sharpened · 15 new) | **31** (15 · 16) | +2 — NG-16 new; G-15 sharpened |
| bounds entries | 36 (37 files) | **37** (38 files) | +1 — `o7-card-census.spec.ts` |
| cross-edges | 12 | **14** | +2 |
| external rows | 0 | **1** | +1 — ⟨INBOX I-28 · X-EXT-5⟩ |
| packets · dissents | 4 · 6 | **4 · 6** | unchanged |

**Work-order reconciliation:** ROUND-1-ASSIGNMENTS §D gives this seat **2** items (§A 0 · §B 1 ·
§C 1). Delivered: **6** rows — the 2 assigned plus **3** census escapes the roster did not carry
(one of them NWO-marked) and **1** minted correction discharging a check defect. Roster deltas for
round 2, stated so no re-discovery is needed: **§A +1** (⟨wb-extract-imagedropzone · R-21⟩) · **§B
+3** (⟨PreviewStrip · C-2⟩, ⟨picker-spectrumcanvas · L-12⟩, ⟨wb-extract-imagedropzone · R-21⟩).

**Status verbs unchanged** (§5 stands): AUDITED **YES** · SPECIFIED **YES, sharpened** ·
IMPLEMENTED **NO** · VERIFIED **NO**. X.W1 remains **planned**. This section opened no product
source, ran no gate, stamped no wave, and wrote no byte outside this file. Every gate it adds or
sharpens is **born-RED with a named, re-measured witness**; the one scheduled-RED gate (NG-16) is
labelled scheduled, never claimed as RED today.

---

## 7. FOLD ROUND 2 — dated addendum (2026-08-28): zero rows homed, and the census law this fold already satisfied

**What this section is.** This seat's answer to `refinement/ROUND-2-ASSIGNMENTS.md`, written against
`refinement/PASS-2-CHECK-RETURN.json` (NON-CONFORMANT: 1 BLOCKER · 2 MAJOR · 5 MINOR · 1 INFO).
**The work order homes ZERO of its 84 escaped ⟨record·id⟩ at X-W1** — §A's own table routes them to
`X-W4` (18) · `X-W5` (5) · `X-W6` (3) · `X-W7` (41) · `X-W8` (9) · `X-W10` (5) · `X-W11` (1) ·
`X-W0`'s G-F register (2), and **X-W1 appears in no §A group, no §B cure and no §C cure**. A zero is
not a closure, so this section records **what was re-measured to earn it** rather than reporting a
silence. **No row is added; no id is minted; nothing is renamed.**

**Posture unchanged.** Status `planned`; dispositions **ADJUDICATED**, never VERIFIED — *"no gate has
run"* (§2's own line) is as true after this round as before it. No product source opened. **E-3**:
`waves/W0.md..W11.md`, `CONFORMANCE-2026-08-03.md`, `ROUND-1-ASSIGNMENTS.md` and every
`registry/adjudicated/**` record stay byte-untouched; this file is this seat's sole write.

### 7.1 §D's CENSUS METHOD LAW measured against this fold's own census (§6.0)

The BLOCKER's root cause is **method**: X-W7-FOLD §R1's *"wave token within 30 characters after a
routing marker"* heuristic, and every derivative of it, is blind to rows routed by bare declarative
phrasing. §D retires it. **This fold's §6.0 census does not carry the heuristic** — checked clause by
clause, not asserted:

| §D clause | what it requires | this fold's §6.0, as printed | verdict |
|---|---|---|---|
| **1. THE PREDICATE** | a row line carrying an `X[-·.]W\d` token books the row, **any phrasing, any position** | C-2/C-3 count **every** bold-leading row line carrying the token; the **101** denominator is token-only | **CONFORMANT** |
| **2. THE RETIRED HEURISTIC** | no proximity/marker-verb **filter** | §6.0 records *"of the 101, **62** carry an explicit routing verb … and **39** carry the `X-W1` token without one"* — **an observation about phrasing, never a filter**; the denominator stayed 101 | **CONFORMANT** — and the 39 are exactly the population the heuristic hides elsewhere |
| **3. TOKEN ALPHABET** | hyphen and dotted only, **re-measured** | §6.0 counts en-dash, `·` and dotted (`X.W1.b`) forms; re-measured at HEAD, dash forms in the corpus = **0**, so the class is exactly `X[-·.]W\d` | **CONFORMANT** (superset, and the surplus is empty by measurement) |
| **4. THE BOUNDARY** | `X-W1` must not swallow `X-W10`/`X-W11` | *"`X-W1` is matched with a trailing non-digit guard, so **`X-W10`/`X-W11` never prefix-match**"* — the §0 header's own 14-file trap | **CONFORMANT** |
| **5. NWO AND ROUTED ARE LIMBS** | a dual-limb row folds **once, both limbs** | §6.0 folded ⟨wb-extract-imagedropzone · R-21⟩ — *"`NO-WAVE-OWNER` on its own row line"* — at **R54**, as one row | **CONFORMANT** |
| **6. COUNT FOLLOWS PREDICATE** | every printed figure carries the command that produces it, reproducing at HEAD | see §7.2 | **CONFORMANT, re-verified** |
| **7. ROW PREDICATE OPEN** | no per-wave **total** is authoritative until round 3 elects a row regex | recorded: §6.0's `101` reproduces under **both** its own regex and §D's, so this fold's figure does not depend on the open question | **noted** |

### 7.2 §B.2's defect class — measured absent from this fold

The MAJOR at §B.2 is a fold printing a *"Re-runnable in one line"* census that **does not reproduce its
own printed output** (`X-W4-FOLD` §6E: printed **282**, returns **257**, law-true **305**). That cure
is X-W4's. **This seat re-ran its own printed commands read-only at HEAD, 2026-08-28:**

```
# §6.0 C-1 — the corpus band
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | wc -l    # -> 92
# §6.0 C-2/C-3 under §D's law-true predicate — records and row lines
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' \
  | xargs grep -nE '^\s*([-*]|\|)\s*\*\*' | grep -cE 'X[-·.]W1([^0-9]|$)'                     # -> 101
```

**`92` and `101` both reproduce**, and `101` matches §D's law-true table for X-W1 **exactly** (101 rows
across **54** records — the same 54 §6.0 printed). **The `X-W1` figure is therefore not one of the
figures §D clause 6 strikes**, and §B.2's cure has no site in this file.

### 7.3 §C.3 — the EXTERNAL receipt, corrected upstream; this fold's booking is unaffected

The spine's `§ROUND-1` receipt reports *"**9 EXTERNAL bookings** … (W0 · W1 · **W2** · W4 · W6 · W7 ·
W8 · W9 · W11)"* and is contradicted in bytes by `X-W2-FOLD.md:939-943` (*"### 7.3 The I-28 external —
zero X-EXT rows route here … **None routes to X-W2**"*). The corrected figure is **8**, with `W2`
struck. **This fold's row is one of the 8 and is unchanged**: **W1 · ⟨INBOX I-28 · X-EXT-5⟩ · R55** —
the o7 card census going born-RED at the glass 8.0.0 repin. Recorded here as a pointer only; **the
correction's site is the spine and the write is the spine seat's** (E-3 — no second hand on another
seat's bytes). R55's own sequencing is unchanged and restated because it is the part a corrected count
must not blur: **X-EXT-5 goes LIVE AT THE REPIN, not before**, the repin act is **X-W0.j's**, and its
census PASS branch is unexecutable without X-EXT-6's peer install.

### 7.4 Arithmetic — unchanged, and stated so the silence is countable

| unit | round 1 | **round 2** | delta |
|---|---|---|---|
| §Rows | 56 (R1–R56) | **56** | **0** — §A homes zero rows here |
| §Gates | 31 | **31** | 0 — **no gate minted, none re-staged, none re-weighted** |
| §BoundsDelta | 37 | **37** | 0 |
| §CrossEdges | 14 | **14** | 0 |
| EXTERNAL rows | 1 (R55, ⟨INBOX I-28 · X-EXT-5⟩) | **1** | 0 — see §7.3 |
| census denominator | 101 row lines / 54 records | **unchanged, re-verified** | reproduces at HEAD under §D's predicate |

**Assignment coverage: §A 0/0 · §B 0/0 · §C 0/0**, with the zero **earned by re-measurement** (§7.1,
§7.2) rather than inherited from the roster. Numbering stays gapless and contiguous **R1 … R56**; zero
ids minted, zero renamed; `L-R6`/`L-R6-4` (§C.4's erratum) is cited nowhere in this file (grep → 0), so
that cure has no site here. Status remains **planned**; no product source was opened by this round.

---

## 8. FOLD ROUND 3 — dated addendum (2026-08-28): zero rows homed, the carriage law measured, one cross-edge received

**What this section is.** This seat's answer to `refinement/ROUND-3-ASSIGNMENTS.md`, written against
`refinement/PASS-3-CHECK-RETURN.json` (NON-CONFORMANT: 1 BLOCKER · 1 MINOR · 1 INFO). **The work order
homes ZERO of its 124 escaped ⟨record·id⟩ at X-W1** — §A's own per-fold table routes them to `X-W7`
(53) · `X-W8` (30) · `X-W6` (17) · `X-W4` (11) · `X-W0`'s G-F register (7) · `X-W11` (4) · `X-W10` (2),
and **X-W1 appears in no §A group, no §A.R refutation, no §A.S supplement and neither §B directive**.
Its single occurrence in the whole work order is a **stated CrossEdge into this fold** (§8.3), not an
assignment. **A zero is not a closure** (§7's rule, unchanged), so this section records what was
re-measured to earn it. **No row is added; no id is minted; nothing is renamed.**

**Posture unchanged.** Status `planned`; dispositions **ADJUDICATED**, never VERIFIED — *"no gate has
run"* (§2's own line, and `:1027`'s `| VERIFIED | **NO** |`) is as true after this round as before it.
No product source opened. **E-3**: `waves/W0.md..W11.md`, `CONFORMANCE-2026-08-03.md`,
`ROUND-1-ASSIGNMENTS.md`, `ROUND-2-ASSIGNMENTS.md` and every `registry/adjudicated/**` record stay
byte-untouched; this file is this seat's sole write.

### 8.1 §1.G's GENEROUS CARRIAGE LAW measured against this fold — clause by clause, not asserted

The pass-3 BLOCKER's root cause is **carriage, not routing**: §1.L legislated how a row's *home* is
found and nothing said what it means for a row to be *carried*, so rows banked under bare ids read as
escapes to a namespace-scoped census and as somebody else's carriage to a namespace-blind one. §B.2's
**§1.G** closes it. Measured against this fold:

| §1.G clause | what it requires | this fold, as measured at HEAD 2026-08-28 | verdict |
|---|---|---|---|
| **1. carriage is namespace-scoped; a bare id is never carriage** | a layer line must match the id **and** name the record (line or heading in scope) | every row R1–R56 carries a `⟨…⟩` provenance line naming the record beside the id (e.g. R54's ⟨wb-extract-imagedropzone · R-21⟩); the fold's own citation idiom is ⟨record · id⟩ throughout | **CONFORMANT** |
| **2. id matching is generous (case-, hyphen-, zero-pad-tolerant, boundary-anchored)** | carriage tested generously; identity still strict | this fold re-spells no id: **zero minted, zero renamed** across three rounds (§6.6/§7.4), so the strict-identity side is met, and the generous side can only help it | **CONFORMANT** |
| **3. identities dedupe by the normalised id** | ⟨R · M2⟩ and ⟨R · m2⟩ fold once | no duplicate-normalised pair exists in R1–R56 (ids are minted per record and carried once) | **CONFORMANT** |
| **4. the alias set is derived AND declared** | any record-column short form must print its expansion | this fold uses **no short form in a record column** — its record names appear in full in the `⟨…⟩` lines (`wb-extract-imagedropzone`, `PreviewStrip`, `ColorNutritionLabel`, …). Declared anyway: the only abbreviations in this file are **wave/gate tokens** (`CC-030`, `o14`, `o7`), not record aliases | **CONFORMANT — nothing to declare** |
| **5. heading scope counts; bold pseudo-headings do not** | a table keyed by a bold record label must promote or attribute inline | this fold keys its bands by **markdown headings** (`### Band A`…`### Band G`) and attributes every row inline; **it keys no table by a bold record label**, so the clause-5 repair worked at `X-W11-FOLD` §8 has **no site here** | **CONFORMANT** |
| **6. the `≡`-pointer is the cure, and it IS carriage** | a facet of a folded identity closes by one dated pointer | the instrument is already this fold's: R51–R56 were booked at round 1 exactly this way | **CONFORMANT** |
| **7. the row predicate is elected — `^\s*([-*]\|\|)\s*\*\*`** | a per-wave total may now be quoted as authoritative if it states the regex inline | **§7.2's `101` was measured under this exact regex** and reproduces (§8.2); the moratorium's lift changes nothing this fold printed | **CONFORMANT** |
| **8. the count follows the predicate** | corpus, row regex, token class, alias set and carriage test all stated and reproducing | §8.2 | **CONFORMANT, re-verified** |

### 8.2 The census commands, re-run read-only at HEAD (2026-08-28)

```
# §6.0 C-1 — the corpus band
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | wc -l    # -> 92
# §6.0 C-2/C-3 under the ELECTED row regex (§1.G clause 7) and the law-true token class
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' \
  | xargs grep -nE '^\s*([-*]|\|)\s*\*\*' | grep -cE 'X[-·.]W1([^0-9]|$)'                     # -> 101
# clause 3, re-measured not inherited: dash forms in the corpus
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' \
  | xargs grep -oE 'X[–—‑]W[0-9]' | wc -l                                                     # -> 0
```

**`92`, `101` and `0` all reproduce**, and `101` matches §1.L's law-true table for X-W1 **exactly**
(101 rows across **54** records — the same 54 §6.0 printed). The trailing non-digit guard still keeps
`X-W10`/`X-W11` out of the X-W1 band, which is the §0 header's own fourteen-file trap. **No figure in
this fold is one that §1.G clause 8 strikes.**

### 8.3 The one cross-edge received this round — recorded, not booked

`ROUND-3-ASSIGNMENTS.md` §A.3's packet-home block routes a limb here, and it is the **only** mention
of X-W1 in the whole work order: *"`PreviewStrip.md` — packet 13 · PREVIEW-CHIPS → HOMED X-W6
(`PreviewStrip.vue` in W6's §3 bounds); **the test-tree limb rides a stated CrossEdge to X-W1**."*

**The row is X-W6's and stays there.** ⟨PreviewStrip.md · **PS-27**⟩ (`:71`), verbatim: *"**PS-27 ·
L2-9a — the library's src-mirror test tree holds TWELVE demo suites (count corrected by reader-LC
against the axis's 10, verified at the axis's own HEAD), including the chip module's only unit
oracle**, against vitest.config's stated law. **DISPOSITION: NO-WAVE-OWNER, named** (test-tree
re-homing has no X owner)."*

**Why it is recorded here and not booked here**: the substance — a **src-mirror test tree holding
twelve demo suites against `vitest.config`'s stated law**, one of which is the chip module's **only**
unit oracle — lands squarely in this fold's oracle/coverage band (§Band E, and R51–R56's coverage
rows). But the record's own disposition is **NO-WAVE-OWNER, named**: *test-tree re-homing has no X
owner*. So a booking here would **mint an owner the record refuses to name**. Two facts are carried so
the edge is not lost: (i) the count is **TWELVE**, corrected by reader-LC against the axis's ten and
verified at the axis's own HEAD — a later seat re-counting ten has found the stale figure, not a drift;
(ii) moving the tree would move the chip module's **only** unit oracle, so any re-homing is a coverage
act, not a file move. **Booked: nothing. Received: one edge.**

### 8.4 Arithmetic — unchanged, dated, and stated so the silence is countable

| unit | round 1 | round 2 | **round 3** | delta |
|---|---|---|---|---|
| §Rows | 56 (R1–R56) | 56 | **56** | **0** — §A homes zero rows here |
| §Gates | 31 | 31 | **31** | 0 — **no gate minted, none re-staged, none re-weighted** |
| §BoundsDelta | 37 | 37 | **37** | 0 |
| §CrossEdges | 14 | 14 | **14** | 0 — §8.3's arrival is **received, not minted**: ⟨PreviewStrip · PS-27⟩ stays booked at X-W6 |
| EXTERNAL rows | 1 (R55, ⟨INBOX I-28 · X-EXT-5⟩) | 1 | **1** | 0 |
| census denominator | 101 row lines / 54 records | unchanged | **unchanged, re-verified** | reproduces at HEAD under the **elected** row regex (§8.2) |
| declared aliases (§1.G cl.4) | — | — | **0 — none used** | none owed |

**Assignment coverage: §A 0/0 · §B 0/0**, with the zero **earned by re-measurement** (§8.1, §8.2)
rather than inherited from the roster. Numbering stays gapless and contiguous **R1 … R56**; zero ids
minted, zero renamed. **This section closes nothing**: whether the layer now carries every escaped
⟨record·id⟩ under its own namespace is settled by the **pass-4 census artifact**, re-derived from the
twelve folds' own bytes under §1.G at a named HEAD — never in this seat's voice. Status remains
**planned**; no product source was opened by this round.
