SERVED MODEL ID (fold author, line-1 declaration): claude-opus-5[1m]

# X·V REFINEMENT FOLD — X-W2 · Boot boundary and eager payload · 2026-08-28

## 1. Header — provenance, posture, the reading rule

**Provenance.** The value.js corpus is the 88 component apotheoses + parser-band / library-band /
chassis-fitness / layout-gestalt banked under
`docs/tranches/V/megatranche/registry/adjudicated/` (all files NOT `kf-*`/`fr-*`/`pt-*`), whole at
227/227 per `COHESION.md §0a` (`35fc8ebf`). This file is the X-W2 limb of the X·V fold opened by
`COHESION.md §0b` (2026-08-28).

**Input set, and how it was closed.** Seven records read whole or in the load-bearing sections
named per row: `ErrorBoundary` · `Katex` · `picker-heroblob` · `picker-spectrumcanvas` ·
`shell-paneslot` · `wb-extract-pane` · `BlobPane`. The corpus was then swept mechanically for
routings from OTHER records: `grep -E 'X-W2|X\.W2|X·W2|X W2'` over every non-`kf-`/`fr-`/`pt-`
file in `adjudicated/` (incl. `adjudicated/delta-r3/`) returns hits in **exactly those seven
files and no others** (BlobPane 4 · ErrorBoundary 1 · Katex 4 · picker-heroblob 10 ·
picker-spectrumcanvas 3 · shell-paneslot 7 · wb-extract-pane 2 = 31 mention-sites). `delta-r3/`
(2 worker files) returns **0**. No packets were proposed for this wave (`[]`), so §Rows is the
whole of the wave's fold input.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0.md..W11.md` and
`waves/CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is this seat's sole write.
Where a clause below strikes or corrects a dated spec, the strike is recorded here and the spec's
bytes stand untouched.

**The reading rule.** At execution X-W2 reads `waves/W2.md` FIRST, then this addendum; where the
two disagree the addendum's clause governs. §Gates is read beside `W2.md §6`, never instead of
it: nothing here re-stages or re-weights W2's gates — it sharpens predicates, adds witnesses, and
records exclusions and convictions. §BoundsDelta is advisory to the next formation boundary, not
a licence: **execution stays gated — status `planned`, and NOTHING here opens product source.**

**Fold classes.** **cl.1** = spec sharpening / law / correction · **cl.2** = cure-shape lock or
binding contract clause · **cl.3** = MEASURE-AT-OPEN row · **cl.4** = KILL-LOCK (an adjudicated
kill that BINDS this wave: the killed shape may not re-enter) · **cl.5** = BOUNDS CONVICTION (a
routed row whose cure lands outside `W2.md §4`).

**Arithmetic — countable from this file's own bytes.** **21 ROWS** (§2, ids `XW2-01`..`XW2-21`,
contiguous, one `⟨…⟩` provenance line each) · **12 GATE ENTRIES** (§3: **7** sharpenings of
EXISTING W2 gates — G1 G2 G3 G4 G5 G6 G8 — plus **5** genuinely NEW born-RED candidates N1..N5;
G7 is recorded as a NO-FOLD and is NOT counted) · **12 BOUNDS ENTRIES** (§4: **3** ADD · **7**
RE-ROUTE · **1** READ-ONLY · **1** NAMED-WITHIN-EXISTING) · **9 CROSS-EDGES** (§5). Dedupe by
banked identity removed exactly **one** double-book: `picker-heroblob` row 8 (L-1) ≡ `BlobPane`
row 21 (L-5) ≡ **CC-035**, booked ONCE at `XW2-01` with both severity cells carried. The IN side
(31 mention-sites) is a grep receipt, not document-internal arithmetic; no in-equals-out identity
is asserted.

**Anti-rename law.** Every id in the `source` cell is the ORIGINAL banked id for life. `XW2-nn`
is a fold-local ADDRESS for citation inside this file — it never replaces a banked identity and
never becomes a defect name.

### 1a. Corpus challenged — corrections, never silent

1. **`picker-spectrumcanvas` C-15 — DATED CORRECTION, the record's own third pass governs.**
   The pass-1 row text (`:42`) says C-15 "rides as a RIDER on this fix"; the third-pass register
   (`:199`) reads verbatim: *"C-15 — KILLED as filed (r2 reader B; ruled above). Unreachable
   branch; the crash site is and remains the conversion inside `HSVCurrentColor`. Pass-1 residue
   #10 is RETIRED — **do not carry C-15 even as a conditional rider**."* The later dated pass
   governs. `XW2-16` carries C-16 ALONE; a wave author who inherits `dotPos`'s unguarded
   `channelNumber` as work has re-minted a killed row.
2. **`picker-heroblob` kill 1's "Gate G3" is NOT `W2.md §6 G3`.** The kill reads *"Gate G3 as
   written may not enter any wave"* — that is **challenge-D's proposed fps gate** (112.8→40.1
   uncapped headless rAF), killed for having no owner and no valid instrument. `W2.md §6 G3` is
   the p75-TBT gate and is UNTOUCHED by that kill; `XW2-05` in fact SHARPENS it. Recorded because
   the two tokens collide verbatim and a fold that carried the kill onto W2's G3 would delete the
   wave's only blocking-time gate. (`XW2-07`, cl.4.)
3. **The two `./blob` size figures are DIFFERENT MEASUREMENTS and may not be substituted for each
   other.** `W2.md §6 G2` predicts from **disk-gz of the shipped dist file**: `./blob` 103,031 B
   raw / **35,461 B gz**. `picker-heroblob` row 8 measures a **tree-shaken two-symbol esbuild
   bundle, vue external**: **77,452 B** (superseding reader-1's 159,329 B, which had vue bundled
   in). Neither refutes the other; they answer different questions (what the eager chunk carries
   vs what survives tree-shaking of two symbols). **G2's realized-vs-predicted line stays on
   W2's own disk-gz basis.** (`XW2-01`, cl.2.)
4. **`W5.md`'s Do-NOT-touch list names a path that does not exist.** It strikes
   `demo/styles/overture.css` (L-9); the live file is
   `demo/color-picker/composables/boot/overture.css` (`ls demo/styles/` = animations, focus-ring,
   foundation, hljs, shell, utils — no overture). Recorded here, W5's bytes untouched; it matters
   because `XW2-14`'s seam note lands on that real file. (cl.1.)
5. **`Katex` R14's route is reachable from the measured route, and the record does not say so.**
   The Katex record calls About "a pane … unreachable by a route matrix" — true of the VISUAL
   capture matrix, NOT of boot: `demo/shell/viewSchema.ts` `VIEW_MAP.picker = { left:
   "color-picker", right: "about", … }`, so **About is the desktop-right pane of `#/`**, the exact
   route `W2.md §6 G3/G4` load N≥20 times. R14's FOIT is therefore inside W2's measurement scope,
   not outside it. (`XW2-17`; the pilot-CLS corroboration is minted as a HYPOTHESIS at `N3`, never
   as a measurement.)
6. **Two records route rows to X-W2 whose cure files X-W2 forbids or does not hold.**
   `shell-paneslot`'s four boot rows land on `main.ts` / `PaneSlot.vue` / `useOverture.ts` /
   `App.vue` — every one of them **X-W5-owned by name** — and one names `usePaneRouter.ts`, which
   `W2.md §4` puts on the explicit **Do NOT touch** list. This is not a defect in either record
   (the DEFECT routing is correct: these are boot-boundary facts) but the executable home is W5.
   §4 rules each one; nothing is silently dropped and no banked id is renamed. (cl.5.)

---

## 2. Rows

Severity is the SOURCE record's final grade, carried unchanged. `⟨…⟩` = provenance line (record ·
anchor · this seat's verification). "seq" = sequencing edict. Verbatim cure text is quoted where
it is load-bearing.

### XW2-01 · CC-035 barrel repoint (the wave's own §3.1-3.2) · cl.1 + cl.2

- **source**: `BlobPane` row 21 (L-5, MINOR) ≡ `picker-heroblob` row 8 (L-1, MAJOR) — **ONE
  identity, deduped here**; + `picker-heroblob` residue 8.
- **sev**: MINOR (BlobPane) / MAJOR (picker-heroblob, raised on new evidence). *"the X-W2 wave
  inherits both cells."*
- **mechanism**: eager `useAtmosphere.ts:36` (+ `BlobPane.vue:12-13`) imports the 103 KB WebGL
  barrel for two constants shipped in the 245 B `./blob-config`; the barrel does not tree-shake,
  and Rollup places a module statically imported by an eager module in the eager chunk regardless
  of `HeroBlob`'s dynamic import.
- **cureShape + locks**: `BlobPane` row 21 verbatim — *"**BUILD → X-W2.a** — the wave names these
  exact two repoints."* `picker-heroblob` row 8 verbatim — *"**IDENTITY-FOLD → BlobPane row 21 =
  CC-035 → BUILD X-W2.a** … Severity raised MINOR→MAJOR on the new evidence; recorded, not
  re-booked."* **LOCK (cl.2, §1a n.3)**: 77,452 B (esbuild, vue-external) and 35,461 B gz (disk)
  are different measurements; G2's prediction line stays on W2's disk-gz basis.
- **sequencing**: unchanged — unit **c** (instrument) precedes unit **a** (cut). `BlobPane` row
  21's own honest negative — *"Bundle cost measured zero (L's own honest negative) — a boundary
  defect, not a size defect"* — is SUPERSEDED by row 8's placement half (*"~77 KB of engine parses
  on the boot path, proven static"*), and the wave carries the placement framing.
- **dissent**: the severity split is preserved, not resolved. BlobPane grades MINOR (total bytes
  unchanged: the engine ships either way); picker-heroblob grades MAJOR (boot-path parse cost).
  Both cells stand.
- ⟨`BlobPane.md` row 21 + `picker-heroblob.md` row 8, ruled-disagreement "L-1 severity", addendum
  item 1, residue 8; `CARRY-CUT-LEDGER.md:102` CC-035; anchors re-read this seat⟩

### XW2-02 · CC-035 settled-seam park + the BLOB_HERO cure-kill · cl.1 + cl.2

- **source**: `picker-heroblob` row 6 (C-9 / L-5 / D-13 / D-10).
- **sev**: MAJOR.
- **mechanism**: three shipped producer seams hand-reimplemented while the comments still call
  them "BOOKED" — `settled`/`settledFrame` vs the wall-clock park; `BLOB_HERO` vs the hand-inlined
  register (**seven atoms diverge**: HERO {.22, 4, .1, .3, .04, smoothK .06, fissionAmp 0} vs demo
  {.325, 3, .09, .4, .03, .05, .6}); `LIGHTNESS_FLOOR_*` vs local `INK_FLOOR`. Two idle machines
  (2000 ms wall clock vs the engine's quiescence read) drive one mood surface.
- **cureShape + locks**: verbatim — *"**BUILD → X-W2 (CC-035: the settled-seam park +
  `/blob-config`) + X-W6 (the register transposition)**. The BlobPane record's **K-11 cure-kill is
  honored**: do NOT adopt BLOB_HERO whole — push the demo's ruled deltas INTO a named preset first
  (BH relay carries the amendment; L's RULING-2)."* **This binds `W2.md §6 G8` (cl.2)** — see §3
  G8.
- **sequencing**: X-W2 takes the park + the import; X-W6 takes the register transposition. W2
  first (W2 blocks W5; W6 depends on W5).
- **dissent**: none recorded.
- ⟨`picker-heroblob.md` row 6 + addendum item 3 + `BlobPane.md` row 1's K-11 kill; atom census
  re-verified by the arbiter via import at glass-ui 7.0.0⟩

### XW2-03 · the b4 beat's predicate (mount gated on the WRONG canvas) · cl.1

- **source**: `picker-heroblob` row 9 (C-3 / L-6 / D-6).
- **sev**: MAJOR.
- **mechanism**: the ornament's mount is gated on the **atmosphere** canvas's WebGL success; a
  pre-b2 loss deletes the hero for the session (observed: REPORT row 0 — canvas 1, `WebGL: context
  lost.`, settle 18905); zero demo consumers of the shipped `rendererStatus` seam;
  `defineAsyncComponent` carries no error/loading state. Mechanism corrected on resume: the b2
  watch (`useOverture.ts:107-116`) is persistent and WOULD late-latch — the permanence is owned by
  row 30's dead `preventDefault()`, not by any latch.
- **cureShape**: verbatim — *"**BUILD → X-W2** (the b4 beat's predicate — gate on
  paint-completion, not the field's GPU arrival) **+ X-W6** (the designed absent/error state —
  prerequisite row 31 …)"*.
- **sequencing**: X-W6 row 31 (`M-8` — the seat must first gain a host the ornament does not own)
  is declared *"sequenced BEFORE row 9's absent/error state"*. X-W2's half (the predicate) has no
  such prerequisite and may land alone.
- **dissent**: three sub-claims KILLED (producer HAS a loss path; DOES handle offscreen; b2 is NOT
  sealed — kill 15). Trigger unpinned → residue 7: reproduction is real-Safari/real-GPU work, not
  W2's.
- ⟨`picker-heroblob.md` row 9, kills 3/15, residue 7; `useOverture.ts:107-116` read by the
  arbiter this resume⟩

### XW2-04 · the 5,300 ms runway breaches the canon five-second law · cl.1

- **source**: `picker-heroblob` row 10 (D-3).
- **sev**: MAJOR.
- **mechanism**: source arithmetic alone breaches canon — `BLOB_IDLE_MS 2000 + SLEEPY_POSE_MS 3300
  = 5300 ms`, armed at mount by the `{immediate:true}` watch — and no pause control exists
  (`:paused` one-way, root inert), so **both** branches of `VISUAL-CONSTITUTION §6` fail; every
  KeepAlive return re-arms the full cycle.
- **cureShape**: verbatim — *"**BUILD → X-W2 (CC-035's settled-seam park restores the tight
  runway) + X-W6** (wire `v-model:paused` to the shipped `DockBackgroundToggle` — the producer doc
  names this exact shape); canon echo **X-W10**."*
- **sequencing**: the canon ceiling is the wave's, the pause control is X-W6's, the canon echo is
  X-W10's. **This gives `G5` the time bound it currently lacks** (§3 G5).
- **dissent**: rendered park timeline (7.0-8.1 s) is residue 3 — corroboration only; the source
  arithmetic already convicts.
- ⟨`picker-heroblob.md` row 10, residue 3; `HeroBlob.vue:211-212/:279/:246-250`; canon clause
  verbatim⟩

### XW2-05 · CC-036 gates the WRONG QUANTITY — the axis correction · cl.1 + cl.3

- **source**: `picker-heroblob` row 23 (L-15 / C-4 / D-§0) + the ruled disagreement "L-15 / C-4
  severity" (INFO → **MAJOR**).
- **sev**: MAJOR.
- **mechanism**: LCP does not reproduce anywhere near 5141 ms on three independent rigs (264 ms
  unthrottled / 928 ms / 1056-1108 ms medians); the real, reproducible cost is the ONE indivisible
  engine-init long task the demo's own forensic already names (`useOverture.ts:120-146`). *"An
  LCP-only born-RED is born un-actionable and closeable-without-cure."*
- **cureShape + locks**: verbatim — *"**BUILD → X-W2 (CC-036)** — this record is a direct input to
  CC-036's mandated re-measure: the gate must carry the TBT/longest-task axis (born-RED ≈ +797 ms
  Δ at 4× / 397 ms task at 1×) beside or instead of LCP-p75; the engine-init cost is a
  **producer** row (shader precompile / init chunking) and rides the BH relay."*
- **sequencing**: MEASURE-AT-OPEN — the axis is fixed at wave-open, before unit c's BEFORE.json,
  so the instrument collects the longest-task attribution from the first run.
- **dissent**: the record grades this MAJOR *because* it re-aims a ledgered wave's gate axis;
  reader-2 filed INFO. The MAJOR grade is the ruling, the INFO cell is preserved.
- ⟨`picker-heroblob.md` row 23 + ruled disagreement "L-15 / C-4 severity"; `useOverture.ts:120-146`
  re-read; CC-036's gate text re-read at `CARRY-CUT-LEDGER.md:103`⟩

### XW2-06 · the Q14 5141 ms number is dead AS A GATE (not as history) · cl.4

- **source**: `picker-heroblob` killed-claims register item 13.
- **sev**: KILL-LOCK.
- **mechanism**: *"The Q14 'LCP 5141 ms' as an inheritable born-RED — KILLED as a gate number (not
  as history): unreproducible on three rigs; whatever produced it predates v4/glass-7/alias-death
  (CC-036 already says so). Superseded by row 23's axis correction."*
- **cureShape + locks**: no cure — a **prohibition**. No W2 receipt, gate, or close may cite
  5141/4919/5988 as a target or a baseline. `W2.md §6 G4` already says *"recorded as **superseded,
  not met**"*: this row is the corpus's independent confirmation of that clause, and it extends the
  prohibition to TBT 5988 (which G4's text does not name).
- **sequencing**: binds at wave-open, before any receipt is written.
- **dissent**: none — the history is preserved (§11 Archaeology), only its gate-status dies.
- ⟨`picker-heroblob.md` kill 13; three-rig triangulation adopted in-record⟩

### XW2-07 · the fps gate that may not enter any wave (token collision) · cl.4

- **source**: `picker-heroblob` killed-claims register item 1 (D-2 as filed, BLOCKER).
- **sev**: KILL-LOCK.
- **mechanism**: D-2's BLOCKER — "a parked blob still costs two-thirds of the route's frame rate"
  — is killed on source: `createCanvasLifecycle`'s `E()` cancels the rAF on the first suspend
  token; `shouldContinue()` self-parks and arms a `setTimeout`, not a rAF; probe C5 measured **0 GL
  draws** across 2.5 s. *"The fps delta (112.8→40.1) is a real observation with **no owner** … an
  uncapped headless rAF read is not a frame rate. → residue 2. **Gate G3 as written may not enter
  any wave.**"*
- **cureShape + locks**: prohibition. **DISAMBIGUATION (cl.1, §1a n.2): that "G3" is
  challenge-D's, NOT `W2.md §6 G3` (p75 TBT), which is untouched by this kill and is sharpened at
  `XW2-05`.**
- **sequencing**: binds any seat tempted to add an fps leg to `idle-frame-budget.spec.ts` (a file
  W2 holds `modify-carve` on).
- **dissent**: the 112.8→40.1 delta is real and unowned → residue 2; it is not a W2 row.
- ⟨`picker-heroblob.md` kill 1 + kills 2/11, residue 2⟩

### XW2-08 · the fixture + idle-spec park narration (X-W6's row, X-W2's bytes) · cl.1 + cl.5

- **source**: `picker-heroblob` row 13 (C-5 / L-8 + M-5), incl. reader-2's adopted clause.
- **sev**: MINOR.
- **mechanism**: `BEAD_RATIO` 0.52 encodes the retired `bodyRadius` 0.26 against the live 0.325;
  the fixture header advertises three importing specs while **four** import it; and
  `webgl-blob-idle.spec.ts:27-31` still narrates the retired park arithmetic (`2000 + 700 =
  2700ms` / `PARK_SETTLE_MS = 3500ms`) against the live 3300/5300/6100, with a "keep this constant
  in lock-step" comment for a constant the spec does not declare.
- **cureShape**: the record routes **→ X-W6** (*"the fixture derives from the named register (row
  6's cut); no bead/park constant typed twice; the header count AND the idle-spec narration
  correct in the same edit"*).
- **sequencing (cl.5, THE EDICT)**: **X-W2.b owns those exact bytes** — `W2.md §4` gives it
  `e2e/smoke/fixtures/blob-timing.ts` (modify) and `e2e/smoke/webgl-blob-idle.spec.ts`
  (modify-carve), and `W2.md §3.4` re-derives the fixture against the quiescence trigger. So
  **X-W2 writes first and X-W6's row lands on already-rewritten text.** Edict: X-W2.b corrects the
  header count (three → four) and the `:27-31` narration IN ITS OWN EDIT, and records that it did
  so, so X-W6 inherits a true file and the row closes at its owning wave against corrected bytes.
  The BEAD_RATIO/register half stays X-W6's — it depends on row 6's named preset, which X-W2 does
  not mint.
- **dissent**: the vacuous-gate consequence limb is KILLED by geometry (kill 8; probe 5 at
  `[cx, wrapper.y+2]` dominates every arc probe). A W2 seat must not "fix" O-12·2 on the strength
  of the drift.
- ⟨`picker-heroblob.md` row 13 + kill 8; `W2.md §3.4`, `§4`, `§5 X.W2.b`; four-file import grep in
  record⟩

### XW2-09 · the asymmetric KeepAlive timer (dies with the park cut) · cl.1

- **source**: `picker-heroblob` row 32 (M-9, reader-2's missed).
- **sev**: INFO (demoted from the reader's MINOR).
- **mechanism**: `onActivated` (`:246-250`) unconditionally resumes and re-arms the idle
  countdown, with **no `onDeactivated` counterpart anywhere in the file** — on a KeepAlive detach
  the wall-clock timers burn a full 5300 ms cycle against an unpainted subtree and then `pause()` a
  blob the producer has already parked.
- **cureShape**: verbatim — *"**FOLD → X-W6 rider** — dies with row 10's settled-seam park
  (CC-035); if the wall-clock machine survives that cut, it gains the symmetric `onDeactivated` in
  the same edit."*
- **sequencing**: the conditional is X-W2's to discharge: if `W2.b` deletes the timers, this row
  closes by construction; if any wall-clock machine survives, `W2.b` owes the `onDeactivated`.
  **Either way the disposition is decided inside W2, not deferred.**
- **dissent**: graded INFO because the producer's own IO park (`composeIntersectionPark: !0`)
  covers the terminal consequence — a wasted timer and a redundant park token, nothing rendered.
- ⟨`picker-heroblob.md` row 32; `onActivated|onDeactivated` grep = `:246` / absent⟩

### XW2-10 · BLOCKER — after the park, NO config write wakes the renderer · cl.1 + cl.2

- **source**: `BlobPane` row 2 (D-3(b) / BPC-2).
- **sev**: **BLOCKER**.
- **mechanism**: *"After the 5.3s park, no config write wakes the renderer: `noteBlobActivity()`
  has exactly 3 callers (`onActivated`, the colour watch, the save watch) — a blob-config write is
  not activity."* Mechanism fully static (arbiter's grep); the byte-identical-frame evidence is
  live-only corroboration.
- **cureShape + locks**: verbatim — *"**BUILD → X-W2.b** (the `settled`-seam park replacement —
  spec'd verbatim) **+ X-W6** (a stage the pane owns needs no ornament-park at all)."*
  **LOCK (cl.2)**: this is a BLOCKER routed to X-W2.b BY NAME, and `W2.md §6 G5` as written
  asserts only that `BLOB_IDLE_MS|SLEEPY_POSE_MS → 0` and that the park reads `settled`. A quiescence
  park with the same three-caller wake set satisfies G5's letter and leaves the BLOCKER standing.
  **G5 must gain a wake predicate** (§3 G5).
- **sequencing**: within `X.W2.b`, before its sub-gate is read.
- **dissent**: `BlobPane` row 3 (NEW-B1, MAJOR) is the structural half — 13 of 31 sliders can never
  mint the wake-repaint prop shape — and routes to X-W6. The wake defect and the propagation
  defect are two rows, one surface; X-W2 owns only the park/wake half.
- ⟨`BlobPane.md` rows 2/3 + ruling 1; `HeroBlob.vue:211-212/:216/:248/:267/:290` grep in-record⟩

### XW2-11 · deep-link cold boot strands the overture at B3 for the session · cl.1 + cl.5

- **source**: `shell-paneslot` NEW (reader-DU missed find 1, adopted).
- **sev**: MAJOR.
- **mechanism**: every link verified in-record — `main.ts:5-7` mounts synchronously before
  `router.isReady()`; `useViewManager.ts:43-46` resolves the undefined initial name to `"picker"`;
  the EAGER ColorPicker mounts with `appear`; the router resolves, the key flips, `leave()` cancels
  the in-flight appear (`el[enterCbKey](true)` → `onAppearCancelled || onEnterCancelled`,
  `runtime-core.cjs.js:1555-1585`) and **PaneSlot binds ONLY `@after-appear` (`:118`)**; no b3 ⇒ no
  b3Complete ⇒ the B4 watcher never opens ⇒ `ornamentOpen` stays false ⇒ `<HeroBlob v-if>` never
  mounts, for the session. Inverted a11y outcome: PRM users are immune, motion-enabled users are
  not.
- **cureShape**: verbatim — *"Cure is one binding (`@appear-cancelled`) or reporting the beat from
  the pane's own `onMounted`. → **X-W2** (boot choreography), live witness rides the visual audit."*
- **sequencing (cl.5)**: the cure files are `demo/shell/PaneSlot.vue` + `useOverture.ts` +
  `ColorPicker.vue` — **all X-W5-owned**, and `W5.md §6 A2` already carries this defect as a
  MEASURE-AT-OPEN RED of record (`deeplinkGenerate.marks ⊇ {b3,b4}` AND blob present across five
  deep-linked routes; **RED `[b0,b1,b2]`, blob absent, triple-measured 2026-07-27**). §4 rules:
  **the CURE re-routes to X-W5; the MEASUREMENT consequence stays X-W2's** (§3 N2). The banked id
  is unchanged.
- **dissent**: none in-record. This fold's conviction (that the executable home is W5) does not
  overturn the record's routing of the DEFECT to the boot boundary — W2 IS the boot boundary; it
  simply does not hold the bytes.
- ⟨`shell-paneslot.md` missed find 1; `W5.md §4` bounds + `§6 A2` gate; both re-read this seat⟩

### XW2-12 · every non-`/` cold boot mounts and caches a pane nobody asked for · cl.1 + cl.5

- **source**: `shell-paneslot` NEW (reader-DU missed find 2, adopted).
- **sev**: MAJOR.
- **mechanism**: same chain links 1-2 — `usePaneRouter.ts:21/:83` is the one eager component;
  `PaneSlot.vue:120` then retains it with its `keydown` listener and `useMagicKeys` scope live via
  the confirmed L-1 mechanism (`onUnmounted` becomes "runs never"). *"This is the Q14/CC-036
  boot-cost class sitting one line above the seat's subject."*
- **cureShape**: verbatim — *"Cure: `router.isReady().then(() => app.mount("#app"))`. → **X-W2**."*
- **sequencing (cl.5)**: the cure file is `demo/color-picker/main.ts` — **X-W5-owned**, and
  `W5.md §6 A1` already gates the exact predicate (`/await router\.isReady\(\)/`, RED measured:
  `main.ts` is 168 B, no `isReady()` await). The evidence file `usePaneRouter.ts` is on `W2.md
  §4`'s **Do NOT touch** list. §4 rules: **CURE → X-W5 (already gated); the MEASUREMENT-SCOPE
  consequence is X-W2's and is not optional** — `W2.md §5 X.W2.c` collects N≥20 loads of `#/`
  ONLY, so the eager-mount cost this row names is **structurally invisible to W2's own
  instrument** (§3 N2).
- **dissent**: none.
- ⟨`shell-paneslot.md` missed find 2 + BLOCKER L-1/PS-2/D-5; `W5.md §6 A1`; `W2.md §5`/`§4`⟩

### XW2-13 · the right-slot `:on-appeared` wires a documented empty function · cl.1 + cl.5

- **source**: `shell-paneslot` NEW (reader-DU missed find 7, adopted).
- **sev**: MINOR.
- **mechanism**: `noteRightPlateSettled` (`useOverture.ts:230-232`) has a comment for a body —
  verified — and the binding is **doubly** dead: the right pane at boot is async, and per the
  D-3(4) appear-gating proof `@after-appear` cannot fire for a late chunk anyway. Same species as
  L-8's parity param, plus a fresh prop identity per render (the PS-4 path).
- **cureShape**: *"→ **X-W2**."* No cure text is spelled in-record; the shape is either delete the
  binding + the empty function, or make the beat real. **The right pane at `#/` is About** (§1a
  n.5) — so this dead hook sits on the same route the wave measures.
- **sequencing (cl.5)**: cure files `App.vue` (binding site) + `useOverture.ts` — both X-W5-owned →
  RE-ROUTE (§4). Recorded at W2 because the row is a boot-choreography fact W2's ENV must know.
- **dissent**: none.
- ⟨`shell-paneslot.md` missed find 7 + D-3(4); `useOverture.ts:230-232`⟩

### XW2-14 · PaneSlot parameterises the swap grammar, hard-codes the boot grammar · cl.1 + cl.5

- **source**: `shell-paneslot` NEW (reader-DU missed find 9, adopted).
- **sev**: INFO.
- **mechanism**: `appear-from/active/to-class="overture-appear-*"` (`PaneSlot.vue:115-117`) are
  string literals naming rules in the app root's private `boot/overture.css` — *"the same upward-
  dependency direction L-6 files as MAJOR, recorded as a PASS by the L axis."*
- **cureShape**: *"→ **X-W2/X-W8** seam note."* Dual-routed at source; no cure text.
- **sequencing (cl.5)**: files `PaneSlot.vue` (X-W5) + `demo/color-picker/composables/boot/
  overture.css` (**named by W5's Do-NOT-touch list under a path that does not exist** — §1a n.4) →
  RE-ROUTE to the X-W8 limb, which is the seam wave. Carried here so the dual routing is not
  silently resolved to one wave by omission.
- **dissent**: the L axis recorded this as a PASS; the DU reader minted it as a find. The record
  adopts the find at INFO. Both cells preserved.
- ⟨`shell-paneslot.md` missed find 9 + L-6; `ls demo/styles/` this seat⟩

### XW2-15 · the WebGL-context-retention rider on W2's re-measure · cl.3

- **source**: `shell-paneslot` BLOCKER L-1 / PS-2 / D-5 (listener half) — the rider clause only.
- **sev**: BLOCKER (host row; the rider itself is a measurement obligation).
- **mechanism**: the app's only `<KeepAlive>` (`PaneSlot.vue:120`) publishes no activation
  contract; demo-wide census **1 `onActivated` / 0 `onDeactivated`**;
  `runtime-core.cjs.js:6596-6598` returns into `deactivate` before any unmount-hook path, so
  nothing a cached pane holds is ever released.
- **cureShape**: the host row's cure is X-W5's (a gated key listener / typed registry). The clause
  that folds HERE is verbatim: *"**WebGL-context-retention hypothesis rides the X-W2
  re-measure.**"*
- **sequencing**: MEASURE-AT-OPEN — the rider must be instrumented in unit **c**'s BEFORE.json,
  not retrofitted after the cut, or the before/after pair cannot answer it.
- **dissent**: it is a HYPOTHESIS by the record's own word; W2 reports the count, it does not
  convict on it.
- ⟨`shell-paneslot.md` BLOCKER L-1/PS-2/D-5 final sentence; census re-run in-record⟩

### XW2-16 · BLOCKER — a `none`-channel deep link throws ABOVE the ErrorBoundary · cl.1 + cl.5

- **source**: `picker-spectrumcanvas` C-16 (CONFIRMED, BLOCKER, upstream locus).
- **sev**: **BLOCKER** (one of the record's two blocker-weight survivors; the program's BLOCKER
  register is *"capability-zero / crash / product-lying-at-boot"* and this is the crash arm).
- **mechanism**: verbatim — *"A `none`-channel deep link parses at `hydrate.ts:98-110` (same-space
  convert is the identity), then `useColorPipeline`'s unguarded setup-time
  `convertPickerColor(model.color,"hsv")` throws `color_missing_channel` through `valueOrThrow`
  inside App.vue's own setup — above `ErrorBoundary` (App.vue:180)."* The app blanks.
- **cureShape + locks**: verbatim — *"Books at the boot boundary, not this component."* **LOCK
  (cl.2, §1a n.1): C-15 does NOT ride.** The pass-1 rider clause is superseded by the third-pass
  register: *"do not carry C-15 even as a conditional rider"* — `dotPos`'s unguarded
  `channelNumber` is an unreachable branch under every admissible cure.
- **sequencing (cl.5)**: two cure arms. **(i) guard at the conversion** —
  `demo/color-session/useColorPipeline.ts`, which **NO X wave owns** (grep over `waves/W*.md` →
  0 hits): W2-addable, conflict-free, and the arm this fold recommends. **(ii) reject at the
  parse** — `hydrate.ts`, X-W5-owned. Whichever arm lands, it decides reject-vs-admit for `none`,
  and that decision is the gate's subject (§3 N1).
- **dissent**: the record's own severity ruling for the neighbouring M-8 explains why C-16 is
  BLOCKER and M-8 is not — the register is crash/capability-zero/lying-at-boot. Preserved so a
  wave author does not re-grade.
- ⟨`picker-spectrumcanvas.md` `:42` (pass-1 row), `:178` + `:199` (third-pass C-15 kill), `:17`
  (blocker-weight scoreboard), `:221` (closing routing); `grep useColorPipeline waves/W*.md` → 0,
  this seat⟩

### XW2-17 · MAJOR — `font-display: block` ×20 on the measured route · cl.1 + cl.5

- **source**: `Katex` R14 (MISSED, design re-reader; MAJOR, admitted).
- **sev**: MAJOR.
- **mechanism**: verbatim — *"20 of 20 KaTeX `@font-face` rules declare `font-display:block` —
  every formula renders INVISIBLE during the block period on cold load of the pane, then reflows;
  inherited unexamined through the side-effect import (Katex.vue:20); not overridable without
  re-declaring the faces (descriptor, not property); lands on the route with the open Q14 LCP
  ruling."* Asset scale from the same record: **1.1 MB / 60 font files**, `katex.min.css` 23,827 B.
- **cureShape**: *"→ **X-W2** (eager-payload/Q14 chain; the re-declaration or subsetting is
  asset-pipeline work)."*
- **sequencing (cl.5)**: **subsetting** is asset-pipeline work needing `vite.config.ts`, which
  `W2.md §4` forbids AND `W2.md §3a` names as a premise-invalidating expansion. **Re-declaration
  is not** — it is pure CSS: a new `demo/styles/katex-faces.css` re-declaring the 20 faces with a
  non-`block` descriptor, imported from `Katex.vue` immediately after
  `import "katex/dist/katex.min.css"` (`Katex.vue:20`). Two files, both addable (§4 B7/B8), no
  bundler surgery, W2's premise intact. **Edict: W2 takes the re-declaration arm or the row
  re-routes; it may not take the subsetting arm.**
- **dissent**: the record's R15 names a deeper cure — build-time KaTeX via `Markdown({})`'s unused
  `markdownItSetup` hook — that *"would structurally retire R4, R5's runtime surface, R14, and
  most of R3 in one architectural move"*, and books it **NO-WAVE-OWNER**. A W2 seat must not
  attempt it; a formation boundary must not lose it.
- ⟨`Katex.md` R14, R15, `:8` instrument line (20× `font-display:block`, 1.1 MB / 60 files,
  23,827 B); `Katex.vue:20` read this seat; `viewSchema.ts` `VIEW_MAP.picker.right = "about"` read
  this seat (§1a n.5)⟩

### XW2-18 · the gh-pages artifact census W2 owes another record · cl.3

- **source**: `Katex` residue item 12 + the `L-11 ground of death` ruling.
- **sev**: residue (evidence obligation).
- **mechanism**: verbatim — *"One fresh `npm run gh-pages` + artifact census at the next
  X-W1/X-W2 sitting closes it."* The library re-reader's rebuild receipts are adopted in-record:
  **122 assets, `vendor-katex-*.js` 258,876 B, NOT in the modulepreload set** (rebuild
  `b9955f9c`).
- **cureShape + locks**: no product cure — a **receipt**. **LOCK (cl.2)**: the "not in the
  modulepreload set" fact BOUNDS `W2.md §6 G2` — the 258,876 B KaTeX vendor chunk is **not** an
  eager term, so G2's 313,585 B baseline may not be explained by it, and a post-cut G2 that moved
  by ~258 KB measured the wrong set.
- **sequencing**: unit **c**, at BEFORE.json time — the census is the same build the born-RED
  numbers come from. Filed as a TRACKED artifact under `docs/tranches/X/evidence/W2/` (CC-012:
  untracked evidence is not evidence).
- **dissent**: this seat ran no build; the receipts are the record's, adopted as provenance, never
  as this fold's measurement.
- ⟨`Katex.md` residue 12 + ruled disagreement "L-11 ground of death" + kill 1⟩

### XW2-19 · the extract facade is eager, and it is not the blob · cl.1

- **source**: `wb-extract-pane` K-5 (L-1 cost limb, KILLED-upheld).
- **sev**: KILL with a surviving design fact.
- **mechanism**: verbatim — *"Dev-graph figures; no production bundle exists to measure
  (`dist/gh-pages/` absent at HEAD); the facade is eagerly booted regardless (App.vue:351 →
  usePaletteWiring.ts:60, verified). **The COUPLING survives as a design fact inside the eager-boot
  story (X-W2's family), not as a byte cost.**"*
- **cureShape + locks**: no cure at W2. **LOCK (cl.1)**: this is a **named non-blob eager term**
  and it is the first thing `W2.md §3a`'s "Budget still red after the cut → triumvirate researches
  the true eager graph" arm should look at. Recorded so that arm opens with a candidate rather
  than blind.
- **sequencing**: read-only; consulted only if G2 stays RED post-cut.
- **dissent**: the 22-modules / 85 KB / 41 / 161 KB figures are KILLED as production facts (dev
  module lists and a static walker). They may not enter a W2 receipt.
- ⟨`wb-extract-pane.md` K-5 + ruled disagreement "L-1 limb kills"; `demo/color-picker/composables/
  usePaletteWiring.ts` path corrected by this seat (the record's `usePaletteWiring.ts` is at
  `demo/color-picker/composables/`, not `demo/palettes/`)⟩

### XW2-20 · W2's bench is the discharge point for a §F carry against a gone artifact · cl.3

- **source**: `wb-extract-pane` UNPROVEN-NEEDS-LIVE residue item 5 (L-11).
- **sev**: residue (unverifiable, not refuted).
- **mechanism**: verbatim — *"`dist/gh-pages/` absent at HEAD — unverifiable, not refuted; a §F
  carry against a gone artifact. **Note X-W2.md:252 expects executable byte gates against
  `dist/gh-pages` at its own bench — the rebuild happens there.**"*
- **cureShape**: no cure — a discharge. When W2 builds for G1/G2, another record's residue closes;
  the close must SAY so, or the residue survives a build that answered it.
- **sequencing**: same build as `XW2-18`; **one rebuild discharges both** (distinct banked
  identities, cross-referenced, counted once each).
- **dissent**: cross-record, the Katex ruling holds this class DEAD AT HEAD by `c4af0ef9` while
  wb-extract holds it merely unverifiable — the two records disagree about the same artifact
  because they read the tree six days apart. Both cells preserved; the rebuild settles it.
- ⟨`wb-extract-pane.md` residue 5 (which cites `W2.md:252`, the COMPLETABLE paragraph — anchor
  verified this seat); `Katex.md` kill 1⟩

### XW2-21 · the template W2 cites is real (and its residual is not W2's) · cl.1

- **source**: `ErrorBoundary` killed-claims register K-1 (+ EB-3 residual).
- **sev**: KILL (BLOCKER killed against the live tree).
- **mechanism**: verbatim — *"`demo/color-picker/main.ts` EXISTS (7 lines, the only `createApp` in
  the repo); index.html:205 is verbatim `<script type="module" src="./main.ts"></script>`; landed
  at `c4af0ef9` 2026-07-29 13:50 … **X-W2 even cites `c4af0ef9` as its template.** … A wave author
  must not be told to create a file that is already there."*
- **cureShape + locks**: no cure. **LOCK (cl.1)**: an independent seat corroborates `W2.md
  §COMPLETABLE`'s citation of `c4af0ef9` (*"one boot-boundary cut, landed alone, ancestor of HEAD,
  zero product-path diffs since — the template and the existence proof"*). The **residual** —
  nothing installed on the app config (no `errorHandler`, no `unhandledrejection`, no
  `window.error`) — is **EB-3, MAJOR, → X-W5 (rider)**, NOT W2's, even though it lives in the same
  7-line file W2's cited template created.
- **sequencing**: binds the `main.ts` re-route at `XW2-12`: **three obligations now converge on
  one 7-line file** — W5's A1 (`await router.isReady()` + the four CSS imports), W5's EB-3 rider
  (the error net), and (if the fold's §4 re-route were ever overturned) W2's mount cure. They must
  land in ONE wave's edit. §4 rules that wave is X-W5.
- **dissent**: none; both readers and the arbiter concur.
- ⟨`ErrorBoundary.md` K-1, EB-3, `:9` wave-authority block; `git show c4af0ef9` verified
  independently in `Katex.md:8`⟩

---

## 3. Gates

**Posture.** Nothing here re-stages or re-weights `W2.md §6`. Each entry names the EXISTING gate
it sharpens (7) or declares itself NEW (5). Every born-RED candidate names a **real witness** —
a file+anchor, a census, or a dated in-record measurement — never a proof-script (L-19: a script
written to make a gate go red is presumed contrivance until the product property it asserts is
independently readable).

### Sharpenings of EXISTING W2 gates

**G1 — the glass blob barrel is absent from the eager module set (BORN-RED).** Rows: `XW2-01`.
- SHARPENING: G1's asserted property gains the record's mechanism as its stated reason — *"the
  barrel does not shake, and Rollup places a module statically imported by an eager module in the
  eager chunk regardless of HeroBlob's dynamic import."* G1's existing falsifier (*"Passes only
  because the import graph changed — not because a grep was narrowed"*) is exactly the L-19 shape
  and is UNCHANGED.
- WITNESS (in-record, real): esbuild two-symbol tree-shaken bundle, vue external — `./blob`
  **77,452 B** vs `./blob-config` **1,667 B**. Reader-1's 159,329 B is superseded (vue bundled).
- ANTI-CONFLATION (cl.2): do not substitute 77,452 B into G2's 35,461 B gz prediction (§1a n.3).

**G2 — eager JS gzip ≤ 280 KiB, before/after receipt (BORN-RED).** Rows: `XW2-01`, `XW2-18`,
`XW2-19`, `XW2-20`.
- SHARPENING (a): **the KaTeX vendor chunk is NOT an eager term.** `vendor-katex-*.js` 258,876 B,
  **not in the modulepreload set** (rebuild receipt `b9955f9c`, 122 assets). Any post-cut G2 delta
  near 258 KB measured the wrong set.
- SHARPENING (b): **fonts are structurally outside G2's census.** `W2.md §5 X.W2.c` collects the
  module entry + `modulepreload` hrefs + non-deferred stylesheets from `index.html`; **fonts
  appear in none of those**, so the 1.1 MB / 60-file KaTeX face payload (`XW2-17`) cannot move
  this number in either direction. This is an ENV/L-12 blindness clause, not a defect in G2 — but
  a close that reads G2 green as "the boot payload is now small" is reading past a megabyte.
- SHARPENING (c): the §3a "true eager graph" research arm opens with a named candidate —
  `App.vue:351 → usePaletteWiring.ts:60` puts the extract facade in the eager graph regardless of
  any blob cut (`XW2-19`).
- SHARPENING (d): the same build discharges two other records' residues (`XW2-18`, `XW2-20`); the
  artifact census is a tracked sub-artifact of BEFORE.json, not a side effect.
- The **≤280 KiB bar is untouched** and remains un-editable by the implementing seat (`W2.md §11`
  guardrail 1; S.W3's exact failure mode).

**G3 — p75 TBT ≤ 300 ms, N ≥ 20, one pinned runner class (BORN-RED).** Rows: `XW2-05`, `XW2-15`,
`XW2-07` (disambiguation), `XW2-12` (route-set).
- SHARPENING: G3 gains the **longest-task/attribution arm**. The corpus proves the cost is ONE
  indivisible engine-init long task, not diffuse blocking: born-RED **≈ +797 ms Δ at 4× CPU / a
  397 ms task at 1×**, located at `useOverture.ts:120-146` by the demo's own forensic and
  triangulated by three independent rigs. TBT alone is a sum; the task identity is what makes the
  gate actionable.
- SHARPENING: the receipt records **live WebGL context count at settle** (`XW2-15` rider) — the
  KeepAlive census (1 `onActivated` / 0 `onDeactivated`; `runtime-core.cjs.js:6596-6598`) means
  nothing a cached pane holds is released, and TBT/GPU pressure attribution is unreadable without
  the count.
- SHARPENING: the receipt **names its route set** (see N2).
- DISAMBIGUATION (cl.4): `picker-heroblob` kill 1's *"Gate G3 as written may not enter any wave"*
  is about **challenge-D's fps gate**, not this one (§1a n.2).

**G4 — p75 LCP ≤ 2500 ms, MEASURE-AT-OPEN.** Rows: `XW2-05`, `XW2-06`, `XW2-17`.
- SHARPENING: G4 stays MEASURE-AT-OPEN, and the corpus adds the reason with teeth — *"an LCP-only
  born-RED is born un-actionable and closeable-without-cure"*. **The wave's verdict may not rest
  on LCP alone**; G3's task-identity arm carries it.
- SHARPENING (cl.4): the prohibition on citing 5141 / 4919 as a target extends to **TBT 5988**,
  which `W2.md §6 G4`'s text names but does not explicitly demote.
- WITNESS (real, in-record): 264 ms unthrottled / 928 ms / 1056-1108 ms medians at 4×, three rigs.

**G5 — the wall-clock park is replaced by the shipped `settled` seam (BORN-RED).** Rows:
`XW2-02`, `XW2-04`, `XW2-08`, `XW2-09`, `XW2-10`.
- SHARPENING (a) — **THE WAKE PREDICATE, from a BLOCKER**: G5 as written asserts
  `rg "BLOB_IDLE_MS|SLEEPY_POSE_MS" demo/ e2e/ → 0` and a `settled`-driven park. `BlobPane` row 2
  (BLOCKER) shows that is not sufficient: *"no config write wakes the renderer: `noteBlobActivity()`
  has exactly 3 callers (`onActivated`, the colour watch, the save watch) — a blob-config write is
  not activity."* **G5 gains: after the park, a config write must wake the renderer.** Witness =
  the three-caller census. Falsifier = park the hero, write one config atom, assert a frame
  changes; a park that stays byte-identical is the same disease with a new clock.
- SHARPENING (b) — **THE CANON CEILING**: G5 currently has no time bound. `VISUAL-CONSTITUTION §6`
  requires ambient motion to terminate within five seconds; the shipped runway is
  `2000 + 3300 = 5300 ms`, armed at mount by an `{immediate:true}` watch — **breached by source
  arithmetic alone**. G5's replacement park inherits the ≤5 s ceiling, and `blob-timing.ts`'s new
  `PARK_SETTLE_MS` states it (`W2.md §5 X.W2.b` already asks for "a stated ceiling" — this names
  the number's authority).
- SHARPENING (c) — **SAME-EDIT TRUTH**: `webgl-blob-idle.spec.ts:27-31`'s retired narration
  (`2000 + 700 = 2700ms` / `PARK_SETTLE_MS = 3500ms`) and the fixture header's "three importing
  specs" (four import it) are corrected in W2.b's own edit (`XW2-08` edict).
- SHARPENING (d): if any wall-clock machine survives the cut, `onActivated` gains its symmetric
  `onDeactivated` in the same edit (`XW2-09`); if not, the row closes by construction. Either way
  the disposition is stated in the close.
- G5's existing falsifier (*"stub `settled` to never settle and the four park specs must red"*) is
  UNCHANGED and is the best-shaped falsifier in the file.

**G6 — the o5 spike leg tells today's truth (BORN-RED).** Row: `XW2-05`.
- SHARPENING: the o5 re-measure reports the **engine-init task explicitly** — the corpus names it
  as the single indivisible cost (`useOverture.ts:120-146`), so an o5 header rewritten with
  "today's diagnosed cause" has a cause available to name. G6's own falsifier (the spec's
  `maxDelta ≤ 3 × median`) is UNCHANGED; the seat still may not choose the verdict.

**G7 — the zero-reading telemetry artefact is superseded (BORN-RED).** **NO FOLD.** No corpus row
touches `PERF.json` or its successor. Recorded so the census is honest about the gate that
received nothing (not counted in the 12).

**G8 — `BLOB_HERO` is consumed or tombstoned (BORN-RED).** Row: `XW2-02`.
- SHARPENING — **CURE-KILL LOCK (cl.2), the sharpest fold on this wave.** G8's text offers *"a
  hero-path consumption with a measured visual delta, or a one-line tombstone with a rationale"*.
  **The consumption arm as written is licensed by nothing and killed by two records.** `BlobPane`
  row 1: *"The as-filed `BLOB_HERO`-adoption cure is KILLED (K-11); the correct transposition
  pushes the demo's three deltas INTO a named preset first."* `picker-heroblob` row 6: *"do NOT
  adopt BLOB_HERO whole — push the demo's ruled deltas INTO a named preset first (BH relay carries
  the amendment; L's RULING-2)."*
- WITNESS (real, by import at glass-ui 7.0.0): **seven atoms diverge** — HERO {bodyRadius .22,
  satellites 4, satelliteRadius .1, orbitRadius .3, eccentricity .04, smoothK .06, fissionAmp 0}
  vs demo {.325, 3, .09, .4, .03, .05, .6}; HERO's `paletteStops` are the canned browns.
- RESTATED ADMISSIBLE SET: (i) **tombstone** with a one-line rationale — always available, always
  W2-executable; (ii) **consume via a named preset amendment** carrying the demo's ruled deltas,
  which requires a producer change and therefore rides the **BH relay** — i.e. NOT a
  W2-executable arm today. **Consuming `BLOB_HERO` whole is not a third state; it is a shipped
  regression under a green gate.**
- G8's existing falsifier (*"a third close that neither consumes nor tombstones. Silence fails."*)
  is UNCHANGED.

### NEW born-RED candidates

**N1 — a `none`-channel deep link must not throw above the ErrorBoundary. Candidate BORN-RED,
BLOCKER-weight.** Row: `XW2-16`.
- ASSERTED PROPERTY: no route/URL state reachable by a deep link throws inside `App.vue`'s setup;
  a `none` channel is either rejected at hydrate with a stated fallback, or admitted and carried
  through conversion.
- WITNESS (real, static, three anchors): `hydrate.ts:98-110` parses `none` (same-space convert is
  the identity) → `useColorPipeline`'s setup-time `convertPickerColor(model.color,"hsv")` →
  `color_missing_channel` through `valueOrThrow` → thrown **above** `ErrorBoundary` (`App.vue:180`)
  → blank app. Structurally confirmed by reader B and by the arbiter's reads of
  `picker-color.ts:104-107` and `operations.ts:18-29`.
- FALSIFIER: load `#/` with a `none`-channel colour in the URL against the built bundle; the app
  blanks with `color_missing_channel` and NO boundary UI. Post-cure it must either render the
  boundary's fallback or resolve to a stated colour — and stubbing the guard must restore the
  blank. A gate that passes because the URL shape was narrowed fails L-19.
- BOUNDS (cl.5): cure at `demo/color-session/useColorPipeline.ts` — **no X wave owns this file**
  (§4 B6). This is a fold-minted NO-WAVE-OWNER finding on a BLOCKER cure locus.
- LOCK: **C-15 does not ride** (§1a n.1).

**N2 — every boot receipt names its ROUTE SET, and the set is not `#/` alone. MEASURE-AT-OPEN.**
Rows: `XW2-11`, `XW2-12`.
- ASSERTED PROPERTY: BEFORE.json / AFTER.json state which routes were loaded; at least one is a
  non-`/` deep link.
- WITNESS (real, dated, independent): `W5.md §6 A2` — `deeplinkGenerate.marks ⊇ {b3,b4}` AND blob
  present across `/#/generate, /#/browse, /#/gradient, /#/extract, /#/atmosphere` — **RED of
  record `[b0,b1,b2]`, blob absent, terminal, triple-measured 2026-07-27**. Deep-link boots
  terminate at a different beat AND mount a different component set than `#/`; `W2.md §5 X.W2.c`
  measures `#/` only, so the eager-mount cost `XW2-12` names is invisible to W2's instrument by
  construction.
- FALSIFIER: run the same N≥20 collection at `#/generate` and at `#/`; if the eager JS set,
  the resource count, or p75 TBT differ materially, `#/`-only was the best case and the receipt
  said so. If they do not differ, the row is answered and the receipt says that instead.
- SCOPE LOCK: this is a **measurement obligation on X-W2**, not a cure. The cures are X-W5's
  (A1/A2), and W2 blocks W5 — so **W2's AFTER.json is NOT the terminal boot cost**, and DELTA.md
  must say so in one line. Two waves measuring the same product at different beats is honest; one
  wave claiming terminality it cannot hold is not.

**N3 — the FOIT/font-payload leg on the measured route. Candidate BORN-RED.** Row: `XW2-17`.
- ASSERTED PROPERTY: no `@font-face` on the `#/` route declares `font-display: block`.
- WITNESS (real, static): **20 of 20** KaTeX faces declare `font-display: block` (arbiter's grep
  of the vendored sheet); the payload is **1.1 MB / 60 files**; About is the right pane of `#/`
  (`viewSchema.ts` `VIEW_MAP.picker.right = "about"` — §1a n.5). Command shape:
  `grep -c 'font-display:block' node_modules/katex/dist/katex.min.css` → 20.
- FALSIFIER: pin the re-declared descriptor back to `block` and the formulas must go invisible
  again during the block period on a cold pane load. A gate that passes because the count was
  scoped to `demo/` fails L-19 — the faces are vendored.
- HYPOTHESIS, MINTED HERE AND LABELLED (never a measurement): `W2.md §6 G3`'s pilot reads **CLS
  0.0193 desktop / 0 mobile**, and desktop is the leg that mounts About (`#app` innerText 1,767
  chars / 19 resources vs mobile 69 chars / 11). R14 predicts exactly that asymmetry — invisible
  text, then reflow. **This is a derived correspondence between two adjudicated data points, not
  an attribution.** Its falsifier is stated and cheap: re-measure CLS with the faces re-declared
  non-`block`; if CLS does not move, R14 is not the CLS term and this paragraph is struck.
- BOUNDS LOCK (cl.5): re-declaration arm only; **the subsetting arm is forbidden to W2** (it needs
  `vite.config.ts`, which `W2.md §3a` calls premise-invalidating). See §4 B7/B8.

**N4 — the retention count at settle. MEASURE-AT-OPEN.** Row: `XW2-15`.
- ASSERTED PROPERTY: the receipt records how many live WebGL contexts and how many KeepAlive-cached
  pane instances exist at settle, at each measured route.
- WITNESS (real, static): demo-wide census **1 `onActivated` / 0 `onDeactivated`**;
  `PaneSlot.vue:120` is the app's only `<KeepAlive>`; `runtime-core.cjs.js:6596-6598` returns into
  `deactivate` before any unmount-hook path — so a cached pane's contexts, `window keydown`
  handler and `useMagicKeys` scope are never released.
- FALSIFIER: navigate N routes and re-read; a count that does not grow falsifies the retention
  hypothesis, and the record's own word for it is *hypothesis*. **W2 reports the number; it does
  not convict on it.**

**N5 — the tracked artifact census. Sub-gate on unit c.** Rows: `XW2-18`, `XW2-20`.
- ASSERTED PROPERTY: one fresh `npm run gh-pages` produces a **tracked** artifact census under
  `docs/tranches/X/evidence/W2/` naming asset count, the eager set, and the non-eager vendor
  chunks — and the close names the two residues it discharges by their banked ids.
- WITNESS (real, in-record): `Katex` residue 12 (*"One fresh `npm run gh-pages` + artifact census
  at the next X-W1/X-W2 sitting closes it"*) and `wb-extract-pane` residue 5 (*"X-W2.md:252
  expects executable byte gates against `dist/gh-pages` at its own bench — the rebuild happens
  there"*), plus the adopted rebuild receipt (122 assets, `vendor-katex-*.js` 258,876 B, not
  preloaded).
- FALSIFIER: CC-012 — an untracked census is not a census; `git ls-files` on the artifact decides
  it. A close that builds and does not file the census leaves two records' residues open against a
  build that answered them.

---

## 4. BoundsDelta

Files the routed cures require, against `W2.md §4`. **Disposition** is this fold's ruling; the
banked row ids are unchanged in every case (anti-rename). `W2.md §4`'s bytes are untouched.

| # | file | needed by | current owner | disposition |
|---|---|---|---|---|
| B1 | `demo/color-picker/main.ts` | `XW2-12` (`router.isReady()` mount), `XW2-21` (context) | **X-W5** (`modify`), and `W5.md §6 A1` already gates the exact predicate (RED measured: 168 B, no `isReady()`) | **RE-ROUTE → X-W5.** W2 keeps N2's measurement obligation. Three obligations converge on this 7-line file (A1 · EB-3's error net · this mount cure) — one wave's edit. |
| B2 | `demo/shell/PaneSlot.vue` | `XW2-11` (`@appear-cancelled`), `XW2-13`, `XW2-14` | **X-W5** (`modify`) | **RE-ROUTE → X-W5.** The KeepAlive/appear surface is W5's whole subject. |
| B3 | `demo/color-picker/composables/boot/useOverture.ts` | `XW2-11` (b3 writer `:206-208`), `XW2-03` (b4 predicate `:154-183`), `XW2-13` (`:230-232`), `XW2-05` (`:120-146` forensic) | **X-W5** (`modify`) | **RE-ROUTE → X-W5** for the writes; **READ** for W2's `XW2-05` anchor. |
| B4 | `demo/color-picker/App.vue` | `XW2-13` (`:on-appeared` site), `XW2-16` (`:180` boundary line), `XW2-19` (`:351` facade edge) | **X-W5** (`modify-carve`) | **RE-ROUTE → X-W5** for writes; **READ** for W2's witnesses. |
| B5 | `demo/color-picker/composables/boot/hydrate.ts` | `XW2-16` arm (ii) — reject `none` at parse | **X-W5** (`modify`) | **RE-ROUTE → X-W5** *if* arm (ii) is chosen. Arm (i) at B6 keeps the BLOCKER inside W2. |
| B6 | `demo/color-session/useColorPipeline.ts` | `XW2-16` arm (i) — guard the setup-time conversion | **NONE** — `grep useColorPipeline docs/tranches/X/waves/W*.md` → **0** | **ADD to X-W2 (`modify-carve`).** Fold-minted **NO-WAVE-OWNER** on a BLOCKER cure locus; W2 is the boot boundary the record names, and this is the only cure file it can hold without a collision. |
| B7 | `demo/scenes/about/katex/Katex.vue` | `XW2-17` — the face-re-declaration import beside `:20` | **NONE** — `grep katex docs/tranches/X/waves/W*.md` → **0** | **ADD to X-W2 (`modify-carve`, the import line only).** The KaTeX asset surface is owned by no X wave; R14 was routed here with no file to land on. |
| B8 | `demo/styles/katex-faces.css` | `XW2-17` — 20 re-declared faces, non-`block` descriptor | does not exist | **ADD to X-W2 (`create`).** Pure CSS; keeps the wave's premise intact (no `vite.config.ts`, no bundler surgery — the subsetting arm stays forbidden). |
| B9 | `demo/picker/ColorPicker.vue` | `XW2-11` alt-cure (report the beat from `onMounted`), `XW2-03` (`:164` `ornamentOpen`, `:94-95` the `v-if`) | **X-W4** (`modify-carve`) + **X-W6** (`modify`) | **RE-ROUTE.** Already double-owned; W2 must not become the third writer. **READ** for anchors. |
| B10 | `demo/color-picker/composables/boot/overture.css` | `XW2-14` seam note | **X-W5's Do-NOT-touch list names it at a path that does not exist** (`demo/styles/overture.css` — §1a n.4) | **RE-ROUTE → the X-W8 limb** of `XW2-14`'s dual routing. Recorded with the stale-path correction so the next formation does not inherit a ban on a phantom file. |
| B11 | `demo/color-picker/composables/usePaletteWiring.ts` | `XW2-19` (`:60`, the eager facade edge) | not in any W2 unit | **READ-ONLY.** Evidence for §3a's research arm; no write. (Path corrected: the record's citation resolves to `demo/color-picker/composables/`, not `demo/palettes/`.) |
| B12 | `docs/tranches/X/evidence/W2/ARTIFACT-CENSUS.json` | `XW2-18`, `XW2-20`, gate N5 | **already inside `W2.md §4`'s `docs/tranches/X/evidence/W2/**` (create)** | **NAMED-WITHIN-EXISTING.** No bounds change; named so CC-012 tracking is not left to inference. |

**Net**: **3 ADD** (B6, B7, B8 — every one on a surface **no X wave owns**, so the additions
create no disjointness conflict with any sibling wave) · **7 RE-ROUTE** (B1-B5, B9, B10 — all onto
waves that already own the file, and in three cases already gate the exact predicate) · **1
READ-ONLY** (B11) · **1 NAMED-WITHIN-EXISTING** (B12).

**Disjointness check for the 3 ADDs against `W2.md §4a`**: B6 and B7/B8 are disjoint from every
existing unit's paths. Suggested homes — B6 → a new `X.W2.e` (or unit **a**, which already holds
the two import-site carves); B7/B8 → their own unit, because the FOIT leg's measurement (N3) is
unit **c**'s instrument and its cure is a different file family from the blob cut. **This fold
does not create units; it names the disjointness so the next authoring seat need not re-derive
it.**

**What §4's Do-NOT-touch list must keep**: `usePaneRouter.ts` (named by `XW2-12`'s evidence, never
its cure), `vite.config.ts` (the subsetting arm of `XW2-17`, forbidden), `src/**`, `PERF.json`
(F-7 append-never-rewrite), `scripts/dev/dev.sh` (CC-021, unowned). **No row in this fold requires
any of them.**

---

## 5. CrossEdges

| # | edge | content | binding effect |
|---|---|---|---|
| CE-1 | **X-W2 → X-W5** | `XW2-11`, `XW2-12`, `XW2-13`, `XW2-14` (cures), `XW2-16` arm (ii), `XW2-21` (EB-3 residual), B1-B5/B9/B10 | Five banked rows route to X-W2 as DEFECTS and to X-W5 as BYTES. W5's `A1` and `A2` already carry two of them as gates with dated REDs. **X-W2 must not duplicate the gates**; X-W5 must not lose the rows' banked ids. `W2.md §10` has W2 **blocking** W5, so the measurement precedes the cure: **`AFTER.json` is not the terminal boot cost, and `DELTA.md` says so in one line** (N2). |
| CE-2 | **X-W2 → X-W6** | `XW2-02`, `XW2-03`, `XW2-04`, `XW2-08`, `XW2-09`, `XW2-10` — every one dual-limbed at source | X-W6 also holds `HeroBlob.vue` (`modify`), `BlobPane.vue` (`modify`), `useAtmosphere.ts` (`modify`), `ColorPicker.vue` (`modify`). Order is W2 → W5 → W6 (W6 `Depends on: X-W5`). **X-W2 cuts the park + the imports; X-W6 lands the register transposition and the absent/error state on already-changed bytes.** `XW2-08`'s edict prevents W2 leaving stale narration in files W6 inherits. |
| CE-3 | **X-W2 → X-W8** | `XW2-14` (the `overture-appear-*` seam, dual-routed X-W2/X-W8); `XW2-01`'s config-parity vicinity (ColorSpaceSelector L-14 identity) | The seam note's second limb is the subtraction wave's; recorded so the dual routing is not collapsed by omission. |
| CE-4 | **X-W2 → X-W10** | `XW2-04` — the canon echo of `VISUAL-CONSTITUTION §6`'s five-second law | W2 makes the runway satisfy the law; X-W10 owns whether the law is re-authored. W2 may not re-author canon to fit a measured park. |
| CE-5 | **X-W2 ↔ X-W1** | `XW2-18`/`XW2-20` (the "next X-W1/X-W2 sitting" rebuild); the three `test.fail()` legs class-ruling (`W2.md §6 G6`); the LHCI bars W2 only *cites* | Whichever of W1/W2 sits first files the census (N5). X-W1 rules the `test.fail()` class; X-W2 supplies the o5 measurement only. |
| CE-6 | **X-W2 → X-W4** | `Katex`'s a11y cluster (R2/K-1, the `annotation` node) shares the component with `XW2-17` but is X-W4's | Named so a W2 seat editing `Katex.vue`'s import line does not drift into the a11y cure. **W2's carve is the import line only** (B7). |
| CE-7 | **X-W2 → glass-ui BH relay** | the engine-init task size (`XW2-05`, producer row: shader precompile / init chunking); the `BLOB_HERO` preset amendment carrying the demo's ruled deltas (`XW2-02`, G8's admissible arm ii); the `liveConfig` atom-liveness contract | Every producer-facing item rides ONE letter per the standing relay edict. **CC-035's producer half is discharged on arrival** (`W2.md §CARRIES`) — the relay carries the *amendment*, which is new, not the shipped-seam note. |
| CE-8 | **X-W2 ← residue discharge** | `Katex` residue 12; `wb-extract-pane` residue 5 (L-11) | Two records' residues close on W2's own build (N5). Distinct banked identities, one rebuild, **counted once each** — and the close must name them, or the build answers a question the ledger still shows open. |
| CE-9 | **X-W2 ↔ NO-WAVE-OWNER register** | `demo/color-session/useColorPipeline.ts` (B6) and the KaTeX asset surface (B7/B8) are owned by NO X wave; `Katex` R15 (build-time KaTeX) is already booked NO-WAVE-OWNER in-record | **Two fold-minted entries for `COHESION.md §4`.** If the §4 ADDs are declined, `XW2-16` (BLOCKER) and `XW2-17` (MAJOR) have no executable home anywhere in tranche X and must be flagged at the formation boundary — not silently carried. |

---

## 6. Closing — status verbs

**SPECIFIED-ADDENDUM.** This file advances nothing to IMPLEMENTED and stamps no wave. `W2.md`'s
Status remains `planned`; no product source is opened, no gate is re-staged, no dated spec byte is
altered. 21 rows folded · 12 gate entries (7 sharpenings + 5 new candidates; G7 recorded as a
NO-FOLD) · 12 bounds entries (3 ADD, 7 RE-ROUTE, 1 READ-ONLY, 1 NAMED-WITHIN) · 9 cross-edges · 2
fold-minted NO-WAVE-OWNER entries for `COHESION.md §4` · 1 double-book removed by identity
(CC-035). Every disposition is **ADJUDICATED, not VERIFIED**; every anchor re-resolves at cure
time (D-19). The seven source records remain immutable beside this file per E-1/E-3.

— end of X-W2 fold.
