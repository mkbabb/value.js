SERVED MODEL ID (round-3 assignment seat, line-1 declaration): `claude-opus-5[1m]` — observed in this seat's own system prompt, not inherited.

# ROUND-3-ASSIGNMENTS — the fold-round-3 work order (2026-08-28)

**What this file is.** The per-wave repair seats' work order for X·V fold round 3, written by the
ROUND-3 ASSIGNMENT SEAT against ⟨`refinement/PASS-3-CHECK-RETURN.json` · BLOCKER⟩. It enumerates,
id-for-id, the census escapes that survive the round-2 repair — every ⟨record.md · id⟩ that the
92-record corpus books (by `X-W` token or by NO-WAVE-OWNER limb) and that the ENTIRE fold layer
fails to carry under its own record namespace — grouped by the fold that must adopt it (§A); and
the two spine directives this round owes (§B). Status `planned`; dispositions ADJUDICATED, never
anything stronger; no product source opened; the dated wave specs and `CONFORMANCE-2026-08-03.md`
stay byte-untouched (E-3). Anti-rename: every id below is the original id for life; citation key
⟨record.md · id⟩ — bare ids collide across six namespaces and this round's own receipts prove it
(⟨wb-extract-imagedropzone · R-31⟩ is absent while ⟨picker-componentsliders · R-31⟩ is banked at
`X-W6-FOLD.md:829`).

**Method (falsifiable, re-runnable — the GENEROUS METHOD the check mandates, codified at §B.2).**
Corpus = the 92 non-`kf-*`/`fr-*`/`pt-*` `*.md` at
`docs/tranches/V/megatranche/registry/adjudicated/` (non-recursive;
`ls *.md | grep -vE '^(kf|fr|pt)-' | wc -l` → **92** at HEAD). Row regex = the fold layer's own
printed predicate `^\s*([-*]|\|)\s*\*\*`. A row is BOOKED if its bytes carry an `X[-·.]W\d`
token **in any phrasing** (spine §1.L clause 1) or a `NO-WAVE-OWNER` mark on the row line or in
its continuation block. Carriage is tested per ⟨record · id⟩ under the record's OWN namespace: a
layer line carries the row only if it matches the id (case-insensitive, hyphen- and
zero-pad-tolerant, boundary-anchored) **and** that same line — or a markdown heading in scope
above it — names the record through its alias set (stem ∪ every ≥4-char suffix-join of its hyphen
segments, concatenated and hyphen-joined, ∪ every `*.vue`/`*.ts` symbol in the record equal to
such a suffix-join). Layer = the 12 `refinement/X-W*-FOLD.md` + `REFINEMENT-FOLD-2026-08-28.md` +
`ROUND-1-ASSIGNMENTS.md` + `ROUND-2-ASSIGNMENTS.md`. Identities dedupe by normalised id, so
⟨BrowsePane · M2⟩ and ⟨BrowsePane · m2⟩ are ONE ⟨record · id⟩ and fold once.

---

## §A.0 — the arithmetic, and the reconciliation with the check

This seat re-derived the census independently and measured **124 distinct ⟨record · id⟩ across 35
records absent by bytes — 84 routed · 40 NWO**. The check measured **123 across 34 — 83 routed ·
40 NWO**. The two reconcile EXACTLY, with receipts, and the NWO limb reproduces the check's **40**
to the row:

| | routed | NWO | Σ |
|---|---|---|---|
| PASS-3-CHECK register | 83 | 40 | **123** |
| − REFUTED at the bytes (§A.R) | −2 | 0 | **−2** |
| + SUPPLEMENTS this seat's sweep reaches (§A.S) | +3 | 0 | **+3** |
| **this file's assigned register** | **84** | **40** | **124** |

The check's own per-band receipt is reproduced id-for-id on **eight of the ten** bands it
enumerates — ⟨wb-extract-imagedropzone · 18⟩, ⟨shell-dock-dockviewselect · 10⟩,
⟨wb-gradient-easingeditor · 9⟩, ⟨picker-debugeventlog · 8⟩, ⟨AboutPane · 7⟩,
⟨picker-spectrumcanvas · 5⟩, ⟨PalettesPane · 4⟩, ⟨ColorNutritionLabel · 4⟩ — every id, in every
band, identical. The two divergent bands are the ±2/+3 above.

**The two refutations** (§A.R, quoted at the bytes below). ⟨wb-gradient-easingspecimenstrip ·
C-05⟩ and ⟨· C-06⟩ are BANKED, verbatim and zero-pad-exact, at `X-W6-FOLD.md:363` and
`X-W6-FOLD.md:358`. The check's byte proof asserts "`C-05` → **0 occurrences anywhere in the
layer**"; `grep -rwF -- 'C-05'` over the fifteen layer files returns **1**, and it is the strip's.
The finding is an ALIAS artefact of the check's own alias table, not an absence: the fold's record
column spells the record `strip`, a five-character short form no suffix-join of
`wb-gradient-easingspecimenstrip` produces. §B.2 clause 4 legislates that hole shut. Both rows are
printed anyway — struck, un-counted, and carrying an `≡`-pointer duty on the X-W6 seat so no
round-4 recheck re-opens them.

**The three supplements** (§A.S): ⟨BrowsePane · i1⟩, ⟨· i2⟩, ⟨· i3⟩ — three bold-leading rows at
`BrowsePane.md:87-89`, each ending in a bare `**→ X-W7.**`, each failing the identical byte test
the check applies to that record's own M-rows. They are escapes by the check's method and are
assigned here, marked ˢ, exactly as round 1 assigned its 24 supplements — so no round-4 check
re-discovers them.

**Where the check's arithmetic is internally short by one — recorded, not reconciled.** Its
receipt labels the BrowsePane band **13** and then prints **14** ids (M2 M3 M7 M8 M12 M13 M16 M17
M18 M19 M20 M21 + NWO M11 M15); its routed split prints **X-W7 28 · X-W8 26 · X-W6 17 · X-W4 9 ·
X-W10 4 = 84** against a stated routed total of **83**. This seat's band is the label's **13** —
the printed extra, ⟨BrowsePane · M3⟩, is CARRIED at `X-W10-FOLD.md:360`
(`⟨PaneHeader.md PH-16 · BrowsePane.md m3 (D3-04) · wb-extract-pane.md XP-18 (C-8) · …⟩`), a
BrowsePane-namespaced, case- and zero-pad-tolerant carriage. Reading the LABEL as authoritative
makes the check's two figures agree with each other and makes its band identical to this seat's,
id-for-id. The check's figure wins on every member it does not lose at the bytes: 121 of its 123
stand here unaltered.

**Per-fold Σ of this file's register:**

| home fold | routed | NWO | Σ |
|---|---|---|---|
| **X-W7** | 31 | 22 | **53** |
| **X-W8** | 28 | 2 | **30** |
| **X-W6** | 14 | 3 | **17** |
| **X-W4** | 9 | 2 | **11** |
| **X-W0** | 0 | 7 | **7** |
| **X-W11** | 0 | 4 | **4** |
| **X-W10** | 2 | 0 | **2** |
| **Σ** | **84** | **40** | **124** |

*Divergence from the check's routed split, stated: this seat books ⟨picker-spectrumcanvas · M-26⟩
at **X-W4** (its own words: `→ X-W4 (the rebuild's naming layer), canon echo X-W10`) with the
X-W10 echo as a CrossEdge, not a second booking; the two refuted C-0x rows leave X-W6; the three
supplements enter X-W7. Every other row books to the wave its own line names, per §1.L clause 1.*

**The hard core — eight ids with ZERO word-boundary occurrence anywhere in the layer, in any
namespace** (`grep -rwF` over the fifteen layer files → 0): ⟨PalettesPane · PP-1 / PP-10 / PP-22 /
PP-34⟩ · ⟨VersionHistoryDrawer · VHD-24⟩ · ⟨PaletteCard · PC-40⟩ · ⟨PaletteCardGrid ·
Comment-archaeology / R2-DU⟩. These cannot be cured by an `≡`-pointer or a citation repair — they
need a first booking. Marked **⊘** in §A.

**Consumption rule** (unchanged from rounds 1 and 2). Each per-wave repair seat folds its rows
below into its `X-W*-FOLD.md` in that fold's own idiom: dated addendum section; a provenance
⟨record.md · id⟩ line per row; dedupe by banked identity — where a roster id is a facet of an
already-folded identity the cure is an explicit `≡`-pointer row, never a silent skip, so the byte
census closes. Rows assigned FORMATION-BOUNDARY land at the G-F register slate (X-W0 seat), never
in a wave's row band.

---

## §A. The 124 escaped row identities → home fold

Grouped by home fold. Each row carries: the citation key, its line in the record, the **banked row
text quoted** from the corpus, and — for a ROUTED row — the routing clause carrying its own `X-W`
token verbatim; for an NWO row, the packet home per `ROUND-1-ASSIGNMENTS.md` §A and spine §1.
ˢ = supplement beyond the check's roster · ⊘ = zero layer occurrence in any namespace ·
† = the row carries BOTH limbs (spine §1.L clause 5: fold once, both limbs).

### §A.1 → X-W7 — the palettes / browse / extract cone — 53 rows (31 routed · 22 NWO)

**Packet home for this fold's NWO rows.**

- `BrowsePane.md` — packet 14 · BROWSE-SEARCH → HOMED X-W7 (`BrowsePane.vue` owned, `W7.md:107`) — ROUND-1 §A precedent, same record (P-5ᵇ).
- `PaletteCard.md` — card family — ROUND-1 §A precedent ⟨PaletteCardSwatches · V-11ᵇ⟩ → X-W7.
- `PaletteCardMeta.md` — card family — ROUND-1 §A precedent ⟨PaletteCardSwatches · V-11ᵇ⟩ → X-W7.
- `wb-extract-imagedropzone.md` — packet 4 · XP-EXTRACT-SESSION-CLUSTER → HOMED X-W7 (BoundsDelta B-1; `ImageDropZone.vue` adopted) — ROUND-1 §A precedent, same record (R-32/R-33).

**ROUTED** — the token is on the row line.

| ⟨record · id⟩ | :line | banked row text (quoted) | the token, in the row |
|---|---|---|---|
| ⟨ActionFeedback · **AF-23**⟩ | :54 | **AF-23 · INFO — `autoDismissMs: 0` mints an immortal chip** (library MISS-8; :43 guard is also the file's only exit — latent trap with exactly the semantics D prescribes for errors). → **X-W7** (dies with … | `…th exactly the semantics D prescribes for errors). → **X-W7** (dies with AF-2's design).` |
| ⟨BrowsePane · **M2**⟩ | :47 | **M2 · R2.6 (restored) — the route's primary control, the first tab stop in the pane, has no accessible name.** ⊙ Producer bytes + call site (ruling table row 3). BrowsePane.vue:10-14 passes only … | `…e — lands on the input today via the attrs spread. **→ X-W7** (BrowsePane modify).` |
| ⟨BrowsePane · **M7**⟩ | :52 | **M7 · D-18/pass2 — sort and the text query are invisible in the filter register; the badge and every label are silent across sort changes.** ⊙ SearchFilterBar.vue:189-195 verbatim (`tier` + … | `…VISUAL-CONSTITUTION §4.1 / PROPORTION-AUDIT PR-07. **→ X-W7** (W7.a owns browser/search; the register cure is unsp…` |
| ⟨BrowsePane · **M8**⟩ | :53 | **M8 · D-23/pass2 structural · R2.2 — the specimen strip draws N adjacent colour cells with no separator, no gap, and no edge against the card body.** ⊙ PaletteColorStrip.vue:13-23 + parent :6-11 (no gap, no … | `…-step neutral ramp renders as one undivided block. **→ X-W7.c** (the specimen rebuild; G12).` |
| ⟨BrowsePane · **M12**⟩ | :57 | **M12 · C·C-1 — every typed transport failure is collapsed to the constant `"Failed to load palettes"`, which the plate renders as its own detail line — one sentence said twice; the DevMisconfig arm deletes a … | `…e headline is accidentally true for the CORS case. **→ X-W7** (scope 3: the 31 `catch → console.warn` sites ruled …` |
| ⟨BrowsePane · **M13**⟩ | :58 | **M13 · D5-02 (narrowed) — activating load-more by keyboard destroys focus (`BUTTON → BODY`) and three new cards arrive announced by nothing; the pane's only status regions are transient and the populated … | `… + K7's inversion survive. Focus transcript → U10. **→ X-W7** (BrowsePane owner; the cure — move focus to the firs…` |
| ⟨BrowsePane · **M16**⟩ | :61 | **M16 · L·L-1 — the export dual path: `export.ts` (132 lines, ships) vs the certified 12-module `demo/palettes/export/` (29 tests, no barrel, zero runtime consumers).** ⊙ `ls demo/palettes/export/index.ts` → … | `… imports `from "./export"` → resolves to the file. **→ X-W7** (scope 2; G4/G5/G6 — the certified serializers becom…` |
| ⟨BrowsePane · **M17**⟩ | :62 | **M17 · L·L5-1/L5-2 — the ports hand every surface the SAME `searchQuery` and `expandedId` refs: typing in Browse filters My Palettes and drives the admin user query.** ⊙ usePalettePorts.ts: one `ref("")` … | `…hree ports; one `actions.expandedId` on all three. **→ X-W7.d** (usePalettePorts in the modify set; the split is n…` |
| ⟨BrowsePane · **M18**⟩ | :63 | **M18 · (DU missed, adopted) — in the wall's most-reached empty state (text-only filter) ALL FOUR recovery affordances are absent, and one predicate produces the absence: `activeFilterCount` omits the text … | `…em. B2 makes this the dominant real-query outcome. **→ X-W7** (a + g).` |
| ⟨BrowsePane · **M19**⟩ | :64 | **M19 · (DU missed, adopted) — the vote control is a toggle whose only state register is a raw Tailwind palette colour: no `aria-pressed`, no state in the name, on the card's de-facto accessible … | `…tate.vue:19 correctly uses `text-destructive/80`). **→ X-W7.d** (the vote seat moves to the inspector; W4 seat law…` |
| ⟨BrowsePane · **M20**⟩ | :65 | **M20 · (DU missed, adopted; + D4-03's surviving first limb) — the provenance chip silently shows two different identifiers in identical chrome: `displaySlug = userSlug ?? slug`, so on the live commons half … | `…'s identifier. Live 5-of-10 `userSlug: null` → U7. **→ X-W7.c/d.**` |
| ⟨BrowsePane · **M21**⟩ | :66 | **M21 · (LC missed, adopted) — a failed remix is silently swallowed: `fork()` catches, warns, returns `undefined`; `onFork`'s `if (!forked) return;` exits BEFORE the catch, so the wired `onForkError → … | `…ne.vue:262-264 wires the surface that cannot fire. **→ X-W7** (G7's swallow table + G13's one-visible-result).` |
| ⟨BrowsePane · **i1**⟩ˢ | :87 | **i1 · L6-4 — `SKELETON_COUNT = 4` named and reasoned (:204-207) vs the hard-coded `v-for="i in 2"` (:130) in the same file.** ⊙ **→ X-W7.** | `…oded `v-for="i in 2"` (:130) in the same file.** ⊙ **→ X-W7.**` |
| ⟨BrowsePane · **i2**⟩ˢ | :88 | **i2 · (LC missed) — duplicate module specifier: `"./browser/dialog"` imported at :191-194 and again at :199.** ⊙ **→ X-W7.** | `…alog"` imported at :191-194 and again at :199.** ⊙ **→ X-W7.**` |
| ⟨BrowsePane · **i3**⟩ˢ | :89 | **i3 · (LC missed) — both casts in `displayedBrowse` (:344-345) disable checking for nothing: `types.ts:32` already declares `oklabColors` in the identical shape; removal type-checks unchanged.** ⊙ **→ X-W7.** | `…dentical shape; removal type-checks unchanged.** ⊙ **→ X-W7.**` |
| ⟨PaletteCard · **PC-23**⟩ | :65 | **PC-23 · MINOR — no timer in the folder is disposed** (C4-9; zero `onUnmounted/onBeforeUnmount/onScopeDispose` across all ten files; useLeaveTimer per-card at :255, ActionFeedback's 2500 ms … | `… useLeaveTimer/useHoverPopover limb is card-owned. **→ X-W7.c/d.**` |
| ⟨PaletteCard · **PC-40**⟩⊘ | :85 | **PC-40 · INFO — the dueling-cures collision (VC no-shadow vs DESIGN.md sm-rung) — CONFIRMED as a corpus observation, DISCHARGED by ratification** (ruled inside PC-15; X.W7.c G11 is the standing answer). **No … | `…ion, DISCHARGED by ratification** (ruled inside PC-15; X.W7.c G11 is the standing answer). **No owner ruling requi…` |
| ⟨PaletteCardGrid · **Comment-archaeology**⟩⊘ | :25 | **Comment-archaeology row** (R2-DU missed #6: 20 of 53 lines are tranche provenance) \| INFO find \| absent (its superlative 6 PRAISES the same lines) \| **KILLED as a defect — house convention; kernel … | `… and its "landmark" clause is FALSE (PG-3's limb). The X-W7.c rebuild re-authors the file; no separate row. R2-DU'…` |
| ⟨PaletteCardGrid · **C-1**⟩ | :54 | **C-1 ≡ D-1** ("`empty` can contradict the default slot") — **KILLED.** Both and only consumers derive `empty` from the array they iterate, adjacent lines, same render (PalettesPane.vue:77/:83; … | `…t-VNode counting a NEW defect. Residue absorbed by the X-W7.c rebuild.` |
| ⟨PaletteCardGrid · **C-3**⟩ | :56 | **C-3 ≡ D-2** ("optional text props → blank annotation fields, lost invitation hierarchy") — **KILLED.** `eyebrow` hard-defaults, `dots` defaults true (EmptyState.vue:90; defaults apply on explicit … | `… INFO, unreachable today. Note for the next formation: X.W7.g deletes the eyebrow prop AND element outright, mooti…` |
| ⟨PaletteCardGrid · **R2-DU**⟩⊘ | :63 | **R2-DU's comment-archaeology row** — **KILLED as a defect per ruling** (house convention; kernel folded into PG-3/X-W7.c; position preserved in DISSENT). | `…er ruling** (house convention; kernel folded into PG-3/X-W7.c; position preserved in DISSENT).` |
| ⟨PaletteCardSkeleton · **PCS-9**⟩ | :38 | **PCS-9 (D-9) · MAJOR — the metadata band is a false prediction: no `justify-between`, no `min-w-0`, no trailing action ghost, so the menu Button materialises from nothing at handoff.** Verified from source … | `…="sm"` menu trigger). **→ folds into PC-18's identity (X.W7.c).**` |
| ⟨PaletteCardSkeleton · **PCS-15**⟩† | :44 | **PCS-15 (L-5 + reader-1 missed #6 + arbiter extension) · MINOR — the utils.css specimen/seam block feeds nothing, twice over, and its two docblocks contradict each other.** Mechanism 1 (L-5): … | `…he component-side `:46`/`--i` writes die with PCS-1 in X-W7).` |
| ⟨PaletteColorStrip · **D1**⟩ | :22 | **D1 grade** \| BLOCKER (as filed) \| MAJOR (name+count survive; state unwitnessed) \| **BLOCKER-class, resolved BY IDENTITY — neither reader's framing** \| PaletteCard.md **PC-7** already books this exact … | `… ROSTER … WHCM **and** print" — at **BLOCKER**, routed X-W7.c, with reader-B's print limb already adopted there. T…` |
| ⟨PaletteColorStrip · **D10**⟩ | :26 | **D10 vs constitution status** \| missed-#6: "the constitution specifies a Card the tree does not implement; five D findings are 'not yet built' reported as 'unmet'" \| D10 CONFIRMED MINOR on the verbatim … | `…e **binding forward canon** — the X waves build to it (X-W7.c's PaletteSpecimen implements §3.1/§5 verbatim) — so …` |
| ⟨PalettesPane · **PP-1**⟩⊘ | :38 | **PP-1 · BLOCKER — the pane's only add-colour affordance and every swatch control are producer-dropped decorative spans; the Plus glyph is additionally slot-dropped.** (C:N-5 ≡ reader-L C/N-5, converged; + … | `…→ CC-044 (one atomic cut, BLOCKED-ON CC-003 Glass≥8) + X-W7.c/d.** The producer-API question rides the standing gl…` |
| ⟨PalettesPane · **PP-10**⟩⊘ | :47 | **PP-10 · MAJOR · GLASS-OWNED + consumer residue — the house focus indicator is a 30%-alpha ring over `outline:none`, structurally unable to clear SC 1.4.11 on light plates; the demo compounds it with … | `…Swatches.vue) die into the house focus register on the X-W7.c/d successor surfaces.**` |
| ⟨PalettesPane · **PP-22**⟩⊘ | :59 | **PP-22 · MINOR — real tabbable buttons render inside the `aria-hidden="true"` teleported hover panel (WCAG 4.1.2 aria-hidden-focus); `inert` is the stated intent, `aria-hidden` the shipped mechanism.** … | `…booking (the hover panel dies with the specimen split, X-W7.c/d) — recorded here so the interim state is named.**` |
| ⟨PalettesPane · **PP-34**⟩⊘ | :71 | **PP-34 · INFO — `class="capitalize"` on the pre-capitalised title is inert and constrains future copy.** (reader-D missed #10; verified :15.) Dies free in any X-W7 pass over the file. | `… (reader-D missed #10; verified :15.) Dies free in any X-W7 pass over the file.` |
| ⟨TagEditPopover · **C-8**⟩ | :20 | **C-8 severity** \| MAJOR (public reachability) \| MINOR (axis's own filing) \| **MAJOR — reader-1 wins** \| Class-identity law (AdminTagsPanel precedent): the dead-`<Checkbox>` class is BLOCKER at the … | `…ing is CONFIRMED **and already encoded in the wave** — X-W7.md:180-182 prescribes exactly the payload-discarding v…` |
| ⟨VersionHistoryDrawer · **VHD-24**⟩⊘ | :58 | **VHD-24 (D-33 / D-34) — a 75%-wide sheet at 320px (240px) is neither modal nor full-screen, and the panel's type is viewport-fluid while its width is viewport-independent above 40rem.** Geometry verified from … | `…* (the placement geometry/`size` axis, same relay) **+ X.W7.d** (the recomposition decides the small-viewport post…` |

**NO-WAVE-OWNER** — booked at the packet's home.

| ⟨record · id⟩ | :line | banked row text (quoted) | packet home |
|---|---|---|---|
| ⟨BrowsePane · **M11**⟩ | :56 | **M11 · C·C-10 + C-2 — every entry to `#/browse` re-fetches unguarded, and `loadingMore` can strand permanently, replacing the load-more trigger with two skeletons forever.** ⊙ usePaletteWiring.ts:143-155 `{ … | X-W7 |
| ⟨BrowsePane · **M15**⟩ | :60 | **M15 · L6-2 — `UserSortMenu` is admin-only, homed in the `search` cluster, exported from the barrel `#/browse` imports; the public route fetches an admin component in dev.** ⊙ Four-hit grep verified (sole … | X-W7 |
| ⟨PaletteCard · **R-4**⟩ | :27 | **R-4 · the malformed pass-2 path (K-7)** \| Superlative #6 praises the L-axis's housekeeping ("empty directory… receipt proving no content was lost") \| K-7: FALSE against the shipped tree — the path exists, … | X-W7 |
| ⟨PaletteCardMeta · **R-4**⟩ | :25 | **R-4 · L-5 identity limb** \| CONFIRMED MINOR ("the category channel … dies at the card") \| REFUTED at the seam (`string[]` IS the wire contract; `Tag` is a separate admin catalogue joined by name) \| **LC … | X-W7 |
| ⟨PaletteCardMeta · **L-5**⟩ | :76 | **L-5, as a component-boundary defect — KILLED (R-4).** `string[]` is the wire contract (format.ts:24); the taxonomy dies at the envelope, not the card. Identity observation → NO-WAVE-OWNER. The "unbounded … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-5**⟩ | :48 | **R-5 · C1 + C2/D-12 (merged) — drag-target churn: refused drops + strobing highlight.** No `dragenter` handler (:23-25); container-level `dragleave` clears `dragging` on every descendant crossing (descendants … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-8**⟩ | :51 | **R-8 · D-14 / C-P2-6 — the `sample` chip is dead on touch, AT, and focus.** :57-61: renders only when `preview` (which forces `tabindex=-1`, so `group-focus-visible` is dead code by construction); … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-11**⟩ | :54 | **R-11 · A-2 (adopted from R1's missed) — the stage animates its own resize under the user's scroll on every phone.** `max-h-[min(320px,40dvh)]` + `transition-all` + 0.3s: `max-height` is on the transition … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-12**⟩ | :58 | **R-12 · D-9 — no disabled contract** (ruled MINOR above; camera-kill + mid-quantize intake, vs the gated sibling rack at ExtractWorkbench.vue:70). **NO-WAVE-OWNER.** | X-W7 |
| ⟨wb-extract-imagedropzone · **R-14**⟩ | :60 | **R-14 · L-11 (material arm only) — accent alpha-wash where the rung-2 WELL material is certified** (hue arm K-1): `bg-primary/5\|10` + `--radius-panel` + no cartoon stamp vs `.dashed-well`'s … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-15**⟩ | :61 | **R-15 · D-17 / L-1 / L-2 (corrected) — `.plate-ink` ×5 byte-identical scoped twins with a LIVE masking fallback.** Census re-run by this seat: exactly 5 declarations (ImageDropZone.vue:109, … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-16**⟩ | :62 | **R-16 · L-3 + L-14 — inverted ownership of the file dialog + the pre-3.5 ref idiom** (defineExpose reach; `ref<InstanceType<…>>` at ExtractWorkbench.vue:222 beside `useTemplateRef` at :223; the capability has … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-17**⟩ | :63 | **R-17 · L-4 — hand-rolled mechanics an installed dependency owns.** @vueuse/core 14.3.0 installed (verified) and ships `useDropZone` (with the enter/leave counter R-5 lacks) + `useFileDialog` (whose `open()` … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-19**⟩ | :65 | **R-19 · D-10 / L-5 — `disableClick` is a phantom degree of freedom**; sole consumer derives it from `preview` (ExtractWorkbench.vue:22-23), so the middle aria-label branch (:20), the `'replace'` chip word … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-20**⟩ | :66 | **R-20 · D-11 — two simultaneous "add an image" affordances in one pane** (the dashed zone + the toolbar Upload control a few px below), compounded by R-6: in the populated state the toolbar control does … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-22**⟩ | :68 | **R-22 · D-16 / L-6 — the demo's only two inline `transitionDuration` bindings, half no-op, half unbooked.** Census re-run: exactly 2, both this file (:17, :59); the timing-function half restates the root … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-23**⟩ | :69 | **R-23 · C-P2-5 — `alt="Uploaded image"` is unreachable** (WAI-ARIA children-presentational under `role=button`+aria-label); with zero live regions in the extract tree (census 0, verified), a healthy preview … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-24**⟩ | :70 | **R-24 · C-P2-7 — base64 data URL where an object URL is the idiom**; the ~1.34× string is a ref, a `:src`, a `<Transition>`-compared `:key`, and a cross-component prop. **NO-WAVE-OWNER.** | X-W7 |
| ⟨wb-extract-imagedropzone · **R-25**⟩ | :71 | **R-25 · C-P2-8a / D-4 — `rounded-xl` raw rung on the img where `rounded-panel` is the token**; overshoots the parent's inner clip curve by 2px/corner (dead ink today; desyncs when `--radius-panel` moves). … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-27**⟩ | :73 | **R-27 · A-3 (adopted) — the chip paints certified plate ink onto an uncertified ground**: `bg-background/85` + `.plate-ink` composited over arbitrary specimen pixels (:58), while the ink's floor-clamp … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-29**⟩ | :75 | **R-29 · A-6 (adopted) — single `pendingResolve/Reject` slot; no request correlation.** Verified: useImageQuantize.ts:51-52/:89-91 overwrite per call; first response resolves whatever is pending and clears … | X-W7 |
| ⟨wb-extract-imagedropzone · **R-30**⟩ | :76 | **R-30 · A-7 (adopted) — copy-before-transfer contradicts the zero-copy contract.** `pixels.buffer.slice(0)` (:93) duplicates the full decoded image (~48MB @ 12MP) on the main thread; every caller's buffer is … | X-W7 |

### §A.2 → X-W8 — the dock, demo/ui and the debug-status instrument surface — 30 rows (28 routed · 2 NWO)

**Packet home for this fold's NWO rows.**

- `shell-dock-actionbarlayer.md` — spine §1 packet 12's NAMESPACE GUARD fixes shell-dock-actionbarlayer's AB-* ids at X-W8 (W8.31/G-8); `ActionBarLayer.vue` is X-W4/X-W8-held (`X-W10-FOLD.md:1225`).
- `shell-dock-profilesection.md` — packet 10 split (subtraction arms): `ProfileSection.vue` rides W8's bounds delta — ROUND-1 §A precedent, same record (N-11).

**ROUTED** — the token is on the row line.

| ⟨record · id⟩ | :line | banked row text (quoted) | the token, in the row |
|---|---|---|---|
| ⟨PaneHeader · **eslint**⟩ | :26 | **eslint boundary-object count** — reader-1's K-3 says THREE `no-restricted-imports` objects (one fully dead); reader-2's L2-1 confirm cell says "three of the four … the fourth also covers … | `…he ColorSpaceSelector arbiter's independent census AND X-W8:42's "six dead `demo/@/**` file globs". **New charge**…` |
| ⟨SearchFilterBar · **SFB-35**⟩ | :85 | **SFB-35** (L-9) \| `demo/ui/` = 19 dirs / 19 files / 20 lines of pure re-export (counts reproduced by reader-B; badge + dropdown-menu already present) \| **X-W8** (CC-007 barrels); identity shared with the … | `… reader-B; badge + dropdown-menu already present) \| **X-W8** (CC-007 barrels); identity shared with the repo-wide…` |
| ⟨picker-componentsliders · **R-29**⟩ | :59 | **R-29 · MINOR · MISSED-ADOPTED (RR-B) — two dead `DIGITS` constants, one exported through the pipeline's public surface with zero consumers.** Re-run by this seat: exactly 3 hits (useSliderGradients.ts:14 … | `…28 definition; :331 exported, consumed by nobody). **→ X-W8** (dead-code subtraction on the god module R-21 argues…` |
| ⟨picker-debugeventlog · **ADJ-2**⟩ | :36 | **ADJ-2 — MINOR · merged D-2 + missed unit-divergence (reader-LC).** The clock is unlabelled AND the two output surfaces disagree by 1000×: the UI renders seconds `(evt.ts / 1000).toFixed(2)` (:14, no unit, no … | `… "clock validation" framing stays dead (K-2). **Route: X-W8.**` |
| ⟨picker-debugeventlog · **ADJ-3**⟩ | :37 | **ADJ-3 — MINOR · merged D-4 + L-4 (one identity: L-4 root, D-4 symptom).** Retention/ordering/dropped semantics are absent from the boundary: `Events ({{ events.length }})` (:5) is the RETAINED count; … | `…re is a type/label lift, not an architecture. **Route: X-W8.**` |
| ⟨picker-debugeventlog · **ADJ-4**⟩ | :38 | **ADJ-4 — MINOR · missed by all three axes; sibling relay, picked up here (the relay reader-DU flagged as dropped).** Two text colors fail WCAG AA at 10px: `.debug-ts` `#666` (:80-83) and `.debug-tgt` `#777` … | `…fy as hardest to read — the defect compounds. **Route: X-W8** (CC-081 names this cluster's a11y explicitly).` |
| ⟨picker-debugeventlog · **ADJ-5**⟩ | :39 | **ADJ-5 — MINOR · merged missed column-budget rows (both readers), magnitude corrected.** Budget is allocated inversely to diagnostic value: `.debug-etype` has `min-width:60px` and NO cap (:85-89) while … | `… threshold UNPROVEN-NEEDS-LIVE (residue R-2). **Route: X-W8.**` |
| ⟨picker-debugeventlog · **ADJ-6**⟩ | :40 | **ADJ-6 — MINOR · C-2 (both readers agree; severity as re-bounded).** `:key="i"` over the reversed array (:9-10, :33): every push shifts every reversed index, so Vue in-place-patches all ~80 rows instead of … | `…cer's `log()` (:57-77), the only constructor. **Route: X-W8** (rides the ADJ-1/ADJ-3 producer touch).` |
| ⟨picker-debugeventlog · **ADJ-7**⟩ | :41 | **ADJ-7 — MINOR · D-6 fact confirmed, cure refuted.** Double truncation: the producer clips targets to 40 chars (describeElement :31), then CSS clips ~40 to ~20 visible (`max-width:120px` at 10px mono, … | `…out budget (with ADJ-5), never interactivity. **Route: X-W8.**` |
| ⟨picker-debugeventlog · **ADJ-11**⟩ | :119 | **ADJ-11 — MINOR · the four alarm rows print two sentinel lies as data** (reader-LC missed M2; verified). `PointerDebugEvent` requires `pointerId: number` and `target: string` with no representation for … | `… a `kind` discriminant; template renders `—`. **Route: X-W8.**` |
| ⟨picker-debugeventlog · **ADJ-12**⟩ | :120 | **ADJ-12 — MINOR · row boundaries are imperceptible while exactly the alarm rows wrap, so a continuation line is typographically identical to a new event** (reader-DU missed M4; verified, including by this … | `…er an operator *actually* misreads it is R-5. **Route: X-W8** (rides ADJ-5's budget re-form: a hanging indent or a…` |
| ⟨picker-pointerdebugoverlay · **D**⟩ | :25 | **D#2b** (RTL physical inset) \| KILLED — duplicate of a ruled ledger row \| CONFIRMED MINOR \| CONFIRMED MINOR \| **RESCOPED → INFO, already-booked. DU wins (identity guard).** \| … | `…A §5). The two-line `inset-inline-start` swap may ride X-W8 incidentally; the IDENTITY stays with the U ledger. \|` |
| ⟨shell-dock-dockviewselect · **DVS-2**⟩ | :37 | **DVS-2 (L:L-1/L-13 ≡ C:D-3 ≡ D:D-2) — BLOCKER, FOLD → Dock-adj DELTA-4 (BUILD, G-C/G-D + the union clause) → X-W8.** The trigger renders `""` and the listbox reports no selected option on 7 of 14 routes: … | `…Dock-adj DELTA-4 (BUILD, G-C/G-D + the union clause) → X-W8.** The trigger renders `""` and the listbox reports no…` |
| ⟨shell-dock-dockviewselect · **DVS-6**⟩ | :41 | **DVS-6 (D:D-3-narrowed ≡ C:D-4-narrowed + D:D-20) — MAJOR, FOLD → Dock-adj DELTA-10 D2-16 (BUILD, G-N) → X-W8, with the narrowing as a binding accretion.** `hide-indicator` on every row (:97/:127) deletes the … | `…— MAJOR, FOLD → Dock-adj DELTA-10 D2-16 (BUILD, G-N) → X-W8, with the narrowing as a binding accretion.** `hide-in…` |
| ⟨shell-dock-dockviewselect · **DVS-8**⟩ | :43 | **DVS-8 (D:D-4 ≡ C:D-17) — MAJOR, FOLD → Dock-adj DELTA-5/G-L (the breakpoint-predicate family) → X-W8, with the AX arm as its own accretion.** One `v-if="isDesktop"` (:87; Dock.vue:71 … | `…ck-adj DELTA-5/G-L (the breakpoint-predicate family) → X-W8, with the AX arm as its own accretion.** One `v-if="is…` |
| ⟨shell-dock-dockviewselect · **DVS-11**⟩ | :46 | **DVS-11 (D:D-10 ≡ C:D-9 ≡ L:L-8) — MAJOR → X-W8 accretion (the wave's useDockAdminMode carve); the anchor-grammar question rides the formation boundary with L-22.** A command wears `role="option"`: … | `- **DVS-11 (D:D-10 ≡ C:D-9 ≡ L:L-8) — MAJOR → X-W8 accretion (the wave's useDockAdminMode carve); the anc…` |
| ⟨shell-dock-dockviewselect · **DVS-16**⟩ | :51 | **DVS-16 (D:D-16 ≡ C:D-12 ≡ L:L-7/L-19) — MINOR (ruled above) → X-W8 rider on the carve: `ViewEntry` → `{ id: ViewId } & PaneConfig`, casts deleted (they were never forced — the kill travels), `currentIcon: … | `…(D:D-16 ≡ C:D-12 ≡ L:L-7/L-19) — MINOR (ruled above) → X-W8 rider on the carve: `ViewEntry` → `{ id: ViewId } & Pa…` |
| ⟨shell-dock-dockviewselect · **DVS-17**⟩ | :52 | **DVS-17 (D:D-11 ≡ C:D-18 ≡ L:L-14) — MINOR → X-W8 rider; the zero-consumer SelectSeparator census accretes to CC-078.** A bare `<div>` separator (:123) inside `role="group"` inside `role="listbox"`, differing … | `- **DVS-17 (D:D-11 ≡ C:D-18 ≡ L:L-14) — MINOR → X-W8 rider; the zero-consumer SelectSeparator census accret…` |
| ⟨shell-dock-dockviewselect · **DVS-18**⟩ | :53 | **DVS-18 (C:D-16) — MINOR → X-W8 rider + a coordination close.** `[&>span]:line-clamp-none` (:69) cancels nothing: `DockTrigger for="select"` mounts **reka's bare SelectTrigger** (dock.js:22, my read) and no … | `- **DVS-18 (C:D-16) — MINOR → X-W8 rider + a coordination close.** `[&>span]:line-clamp-n…` |
| ⟨shell-dock-dockviewselect · **DVS-24**⟩ | :59 | **DVS-24 (D:D-15) — MINOR → X-W8 rider.** Icon and label on two clocks: `mode="out-in"` guarantees the old glyph fully leaves while `SelectValue` swaps instantly — the whole leave duration pairs the OLD glyph … | `- **DVS-24 (D:D-15) — MINOR → X-W8 rider.** Icon and label on two clocks: `mode="out-in"`…` |
| ⟨shell-dock-dockviewselect · **DVS-25**⟩ | :60 | **DVS-25 (missed-1, reader-2) — MINOR → X-W8 rider (comment correction mandatory) + UNPROVEN #9 for the visible magnitude.** The scoped `--accent-view` transition (:159-161, `var(--duration-panel)` = 0.55s, my … | `- **DVS-25 (missed-1, reader-2) — MINOR → X-W8 rider (comment correction mandatory) + UNPROVEN #9 for…` |
| ⟨shell-dock-dockviewselect · **DVS-27**⟩ | :62 | **DVS-27 (C:D-19) — INFO → X-W8 rider under the comment-truth law (Dock-adj D2-09's form).** The sweep is live but interpolates as a rectangular oklab chord (`syntax: "<color>"`, foundation.css:192-198, my … | `- **DVS-27 (C:D-19) — INFO → X-W8 rider under the comment-truth law (Dock-adj D2-09's fo…` |
| ⟨wb-extract-controls · **EC-29**⟩ | :70 | **EC-29 — L-6: the WCAG floor constants have four homes, two identically named in sibling files** (ink.ts trio verified; view-accent.ts/palettes-ramp.ts per the axis's greps). → **X-W8** (one-home family, … | `…-accent.ts/palettes-ramp.ts per the axis's greps). → **X-W8** (one-home family, rides with EC-12).` |
| ⟨wb-extract-controls · **EC-31**⟩ | :72 | **EC-31 — L-1: `demo/ui/` is 19 index-only barrels (K-L) and this file takes both spellings on adjacent lines** (:98 `/dock`, :99 `../ui/slider`). **FOLD — identity = wb-extract-pane XP-11 → X-W8** (which owns … | `…/slider`). **FOLD — identity = wb-extract-pane XP-11 → X-W8** (which owns the import-line repoint even in the pinn…` |
| ⟨wb-extract-controls · **EC-39**⟩ | :80 | **EC-39 — reader-2 miss (count corrected, K-Q): the session publishes dead surface — 4 of NINETEEN members unread** (`palette`, `lastFile`, `paletteName`, `totalPopulation`; return literal + sole-consumer … | `…around the composable's own presentation contract. → **X-W8** (G-10 dead-surface census family, beside XP-13).` |
| ⟨wb-extract-workbench · **XW-30**⟩ | :66 | **XW-30 — MINOR — N-3 (confirmed): both glass-ui import spellings live in this one subtree** (narrow subpaths in the workbench, alias-hop root-barrel widenings in ExtractControls/ExtractPane, adjacent lines). … | `…, adjacent lines). IDENTITY = the sibling's XP-11. → **X-W8**.` |
| ⟨wb-gradient-pane · **M-GP-8**⟩ | :49 | **M-GP-8 (LC) — four contradictory injection postures for two keys in one subtree — MINOR → X-W8 limb (the GradientPane `!` dies with G-10) + cross-component note.** Verified all four: GradientPane `:8` … | `…jection postures for two keys in one subtree — MINOR → X-W8 limb (the GradientPane `!` dies with G-10) + cross-com…` |
| ⟨wb-gradient-pane · **M-GP-9**⟩ | :50 | **M-GP-9 (DU) — `mx-auto` is a no-op class on a `w-full` unconstrained wrapper — INFO → X-W8 carve rider (GradientPane.vue is W8 modify-carve); fleet idiom (GeneratePane `:30`, ExtractPane `:2`), not worth a … | `…-op class on a `w-full` unconstrained wrapper — INFO → X-W8 carve rider (GradientPane.vue is W8 modify-carve); fle…` |

**NO-WAVE-OWNER** — booked at the packet's home.

| ⟨record · id⟩ | :line | banked row text (quoted) | packet home |
|---|---|---|---|
| ⟨shell-dock-actionbarlayer · **AB-29**⟩ | :177 | **AB-29 · R-DU′ M-1 — CONFIRMED (MAJOR).** The cycle's `input → actions` escape (:40) is dead on virtually every color: its gate is `!canProposeName`, and `canProposeName` = `!findCustomName(...)` (AB-26's … | X-W8 |
| ⟨shell-dock-profilesection · **D-13**⟩ | :56 | **D-13 / C-9 / L-6** \| MAJOR \| One menu hand-written twice, already drifted three ways (admin identity gold-shimmer-in-band vs muted-pill-in-menu; the Regenerate separator present vs absent; Login … | X-W8 |

### §A.3 → X-W6 — the gradient-easing catalogue, the chips band and the picker sliders — 17 rows (14 routed · 3 NWO)

**Packet home for this fold's NWO rows.**

- `AuroraPane.md` — packet 13 · PREVIEW-CHIPS → HOMED X-W6 (the color-chips public barrel is in W6's §3 bounds).
- `PreviewStrip.md` — packet 13 · PREVIEW-CHIPS → HOMED X-W6 (`PreviewStrip.vue` in W6's §3 bounds); the test-tree limb rides a stated CrossEdge to X-W1.
- `picker-componentsliders.md` — the picker-sliders/console seat — X-W6-FOLD §1k (PROPOSED unit `.k`); §EXTERNAL X-EXT-3 fixes `ComponentSliders.vue` at X-W6.

**ROUTED** — the token is on the row line.

| ⟨record · id⟩ | :line | banked row text (quoted) | the token, in the row |
|---|---|---|---|
| ⟨wb-gradient-easingeditor · **C2-2**⟩ | :24 | **C2-2 / L-r3:L-1 severity** (FAMILY_ORDER drops quart/quint) \| filed as a MISSED finding, "no axis in ten rounds found it" \| escalated to **BLOCKER** \| **MAJOR — the standing grade; BOTH readers … | `…g apotheosis already ADJUDICATED this identity MAJOR → X-W6 (its C-06). Silent capability loss + false identity (`…` |
| ⟨wb-gradient-easingeditor · **D3-11**⟩ | :44 | **D3-11 / R3-4 / C2-4 / D2-14 — ADJUDICATED MAJOR → X-W6.** Every interval row mounts the full 27-tile strip (:161 inside the v-for :112) AND a live `EasingPicker` (:203-212 — v-show wrapper, so the stage … | `- **D3-11 / R3-4 / C2-4 / D2-14 — ADJUDICATED MAJOR → X-W6.** Every interval row mounts the full 27-tile strip (:…` |
| ⟨wb-gradient-easingeditor · **D2-06a**⟩ | :49 | **D2-06a — ADJUDICATED MINOR → X-W6 ◆.** Two hardcoded 9px sizes (`font-size: 0.5625rem`, EasingSpecimenStrip.vue:150/:196) below the closed type matrix's smallest rung (--text-micro bridge). Statically … | `- **D2-06a — ADJUDICATED MINOR → X-W6 ◆.** Two hardcoded 9px sizes (`font-size: 0.5625rem`, …` |
| ⟨wb-gradient-easingeditor · **R3-7**⟩ | :52 | **R3-7 + C2-7 — ADJUDICATED MINOR → X-W6 (sharpens the sibling's M-ESS-3 identity — one row).** `useClipboard` publishes `invalidate` (my d.ts grep) and the seat never calls it (:94-103 — my read): a tile … | `- **R3-7 + C2-7 — ADJUDICATED MINOR → X-W6 (sharpens the sibling's M-ESS-3 identity — one row).**…` |
| ⟨wb-gradient-easingeditor · **R-C**⟩ | :63 | **R-C · C2-2/L-r3:L-1 ≡ the sibling's C-06 (MAJOR → X-W6)** — severity + novelty ruled above; DU's docstring-falsity + surfaces-disagree sharpenings attach to the identity. | `- **R-C · C2-2/L-r3:L-1 ≡ the sibling's C-06 (MAJOR → X-W6)** — severity + novelty ruled above; DU's docstring-fa…` |
| ⟨wb-gradient-easingeditor · **R-F**⟩ | :66 | **R-F · R3-5 ≡ the sibling's C-09/D-25 (label-in-name, exactly 1 of 27; MINOR → X-W6 ◆).** | `…g's C-09/D-25 (label-in-name, exactly 1 of 27; MINOR → X-W6 ◆).**` |
| ⟨wb-gradient-easingeditor · **R-G**⟩ | :67 | **R-G · R3-6/D2-09 ≡ the sibling's L3-4 (27 toggles, no radiogroup/toggle-group; MAJOR → X-W6 ◆ + glass `$attrs` ask).** | `… L3-4 (27 toggles, no radiogroup/toggle-group; MAJOR → X-W6 ◆ + glass `$attrs` ask).**` |
| ⟨wb-gradient-easingeditor · **R-H**⟩ | :68 | **R-H · D2-05/L-r3:L-4 ≡ the sibling's D-20/L-6/C-10 (shape="cell" bought then fought; glass specimen-rung ask + X-W6 ◆).** My d.ts read confirms the radius-less SHAPE.cell map. | `…e="cell" bought then fought; glass specimen-rung ask + X-W6 ◆).** My d.ts read confirms the radius-less SHAPE.cell…` |
| ⟨wb-gradient-easingeditor · **R-I**⟩ | :69 | **R-I · D2-10's DEFECT ≡ the sibling's C-08 (nameless focusable scroll port; MAJOR → X-W6 ◆).** The CURE is killed here (K-4): the producer already ships `ariaLabel`/`ariaLabelledby` (my grep) — one consumer … | `…ibling's C-08 (nameless focusable scroll port; MAJOR → X-W6 ◆).** The CURE is killed here (K-4): the producer alre…` |
| ⟨wb-gradient-easingspecimenstrip · **C-08**⟩† | :49 | **C-08 / R4-3 / D-24 (+ R4-3(a)/L-7 comment-falsity limb, MINOR, folded) — MAJOR → X-W6 ◆ (≡ GV-35, booked NO-WAVE-OWNER there — the ◆ resolves the same way) + the semantics half as a glass ask (BH).** Glass's … | `…(a)/L-7 comment-falsity limb, MINOR, folded) — MAJOR → X-W6 ◆ (≡ GV-35, booked NO-WAVE-OWNER there — the ◆ resolve…` |
| ⟨wb-gradient-easingspecimenstrip · **L3-4**⟩ | :50 | **L3-4 — MAJOR → X-W6 ◆ + glass ask.** A 27-way single-select hand-built from 27 independent boolean toggles inside a bare `role="group"` while glass publishes `./toggle-group` at a real exports key; no roving … | `- **L3-4 — MAJOR → X-W6 ◆ + glass ask.** A 27-way single-select hand-built fro…` |
| ⟨wb-gradient-easingspecimenstrip · **C-09**⟩ | :67 | **C-09 / D-25 — MINOR → X-W6 ◆.** WCAG 2.5.3 fails on exactly 1 of 27: `:aria-label="tile.id"` (`:105`) vs visible `n = 4` (`:187`). Both readers enumerated all 27 pairs independently; my spot-walk of … | `- **C-09 / D-25 — MINOR → X-W6 ◆.** WCAG 2.5.3 fails on exactly 1 of 27: `:aria-label…` |
| ⟨wb-gradient-easingspecimenstrip · **C-11**⟩ | :69 | **C-11 / D-23 / L-4 — MINOR → X-W6.** One full 27-Chip strip + `useMediaQuery` listener mounted per interval forever (`v-for` `:112` → `v-show` `:144`, ramp `v-if`-gated at `:152` but the strip not — my … | `- **C-11 / D-23 / L-4 — MINOR → X-W6.** One full 27-Chip strip + `useMediaQuery` listener m…` |
| ⟨wb-gradient-easingspecimenstrip · **M-ESS-3**⟩ | :103 | **M-ESS-3 — the copy control confirms by mutating its own accessible name (`:181` conditional `aria-label`, `useClipboard({resetMs:1400})`, no live region — my reads this session) — MINOR → X-W6.** | `…})`, no live region — my reads this session) — MINOR → X-W6.**` |

**NO-WAVE-OWNER** — booked at the packet's home.

| ⟨record · id⟩ | :line | banked row text (quoted) | packet home |
|---|---|---|---|
| ⟨AuroraPane · **AP-34**⟩ | :117 | **AP-34 · reader-LC missed #4 (ADOPTED) — INFO — NO-WAVE-OWNER.** `serializeStop` + `RAMP_SAMPLE_COUNT` on the color-chips public barrel have zero external consumers (my census); with L/P2-3's `stampStops` … | X-W6 |
| ⟨PreviewStrip · **PS-27**⟩ | :71 | **PS-27 · L2-9a — the library's src-mirror test tree holds TWELVE demo suites (count corrected by reader-LC against the axis's 10, verified at the axis's own HEAD), including the chip module's only unit … | X-W6 |
| ⟨picker-componentsliders · **R-21**⟩ | :51 | **R-21 · MAJOR · CONFIRMED — the component's private derived state lives in the shared session pipeline, so a ramp throw is not contained to the console.** useSliderGradients.ts:25 `componentsSlidersStyle` — … | X-W6 |

### §A.4 → X-W4 — semantic controls, the spectrum rebuild and the slug chain — 11 rows (9 routed · 2 NWO)

**Packet home for this fold's NWO rows.**

- `shell-dock-slugeditlayer.md` — spine §1 packet 7 + §2 e.12: shell-dock-slugeditlayer folded on FILE ownership at X-W4 (`SlugEditLayer.vue` in W4 bounds, `W4.md:118`) despite zero X tokens.

**ROUTED** — the token is on the row line.

| ⟨record · id⟩ | :line | banked row text (quoted) | the token, in the row |
|---|---|---|---|
| ⟨picker-colorcomponentdisplay · **L-2**⟩ | :41 | **L-2 + L-3 (merged, one identity) — CONFIRMED (MAJOR, precedent-ruled) → X-W4.** Untyped contract + the silent `HEX_CH` fallback (ruling above). | `…, one identity) — CONFIRMED (MAJOR, precedent-ruled) → X-W4.** Untyped contract + the silent `HEX_CH` fallback (ru…` |
| ⟨picker-spectrumcanvas · **D-13**⟩ | :57 | **D-13 / L-3 / C-11 — CONFIRMED (MAJOR) → X-W4, producer-API half on the glass-ui BH relay.** The touch gate is an undiscoverable mode whose entire visible affordance is authored in a sibling SFC's … | `- **D-13 / L-3 / C-11 — CONFIRMED (MAJOR) → X-W4, producer-API half on the glass-ui BH relay.** The tou…` |
| ⟨picker-spectrumcanvas · **M-22**⟩ | :188 | **M-22 — MAJOR — the touch gate keys off DEVICE CAPABILITY (`isTouchDevice` = `'ontouchstart' in window`), never off the event's actual modality, so on hybrid machines a mouse/pen user's first click is … | `…s silently swallowed — and again after every 3s idle → X-W4 (consumer: gate on `event.pointerType === 'touch'`), p…` |
| ⟨picker-spectrumcanvas · **M-23**⟩ | :189 | **M-23 — MAJOR — the producer ships `suppressDeactivate()` precisely to hold a gate open across a long gesture; this component never calls it, so the 3s timeout does not merely drop the outline — it flips the … | `…he axis to the page scroller under the user's finger → X-W4, folded at cure with C-12/L-9 (whose severity this row…` |
| ⟨picker-spectrumcanvas · **M-25**⟩ | :191 | **M-25 — MINOR — `spectrumStyle` welds two cadences into one computed: the hue-only gradient template (byte-stable during an s/v drag — `stableHue` pins h) is rebuilt every frame alongside the per-frame … | `… for Vue's patcher to diff the identical string away → X-W4 rider (dies with D-16's deletion + the rebuild; the sp…` |
| ⟨picker-spectrumcanvas · **M-26**⟩ | :192 | **M-26 — MINOR — the product's dominant instrument has no visible name anywhere — no caption, heading, axis legend, or `aria-labelledby`; the only naming in the DOM is the aria string the corpus proved wrong … | `…OM is the aria string the corpus proved wrong (D-12) → X-W4 (the rebuild's naming layer), canon echo X-W10 (SS5.2 …` · **dual token** — canon echo **X-W10** rides as a stated CrossEdge, not a second booking |
| ⟨shell-dock-actiontoolbar · **AT-14**⟩ | :46 | **AT-14 — MINOR — reader-found (R1): the Reset hover spin snaps.** Tailwind v4's `-rotate-180` emits the standalone `rotate:` property (verified in tailwindcss dist: `l("rotate", …)`), absent from … | `…ions; `duration-normal` retunes only scale/stroke. → **X-W4** (rider — the bespoke hover CSS dies with the adoptio…` |
| ⟨shell-dock-genericactionbar · **RK-5**⟩ | :59 | **RK-5 — census drift**: r-DU "seven `any`s", r-LC "seven" enumerating nine, X.W4.d D2 measuring ten on its scoped path. No verdict turns on it; the gate's scoped census governs; recorded so the next seat does … | `…*: r-DU "seven `any`s", r-LC "seven" enumerating nine, X.W4.d D2 measuring ten on its scoped path. No verdict turn…` |
| ⟨wb-extract-imagedropzone · **R-31**⟩† | :77 | **R-31 · A-8 (adopted) — the sampler rejects with a raw DOM Event and every caller floats the promise.** useImageSampler.ts:74 `img.onerror = reject`; loadAndFit unawaited at mount and watch … | `…pper's only failure mode surfaces as "[object Event]". X-W4 touches ImageEyedropper "import + face only" — this is…` |

**NO-WAVE-OWNER** — booked at the packet's home.

| ⟨record · id⟩ | :line | banked row text (quoted) | packet home |
|---|---|---|---|
| ⟨shell-dock-slugeditlayer · **C-9**⟩ | :61 | **C-9 / C-10 / L-6 / C-11 / L-7 — MINOR.** `onCopySlug` misfiled into the edit layer, reached by a four-hop imperative round-trip (`ProfileSection.vue:77` → `Dock.vue:213/224` → `:62` → `:68-70`) into a … | X-W4 |
| ⟨shell-dock-slugeditlayer · **D-11**⟩ | :63 | **D-11 + A-6 (ink half; net-new, RR-DU) — MINOR.** Register incoherence on both axes: the decorative `LogIn` (:80, 16px, no `aria-hidden`) outweighs the three operable 14px glyphs in size AND in ink — the … | X-W4 |

### §A.5 → X-W0 · G-F register (FORMATION-BOUNDARY) — the About-recomposition bundle — 7 rows (0 routed · 7 NWO)

**Packet home for this fold's NWO rows.**

- `AboutPane.md` — FORMATION-BOUNDARY · G-F register (X-W0 seat): the About-recomposition bundle **AB-17** is named by no §1 packet; spine §1 packet 12 books AboutPane's AB-4/AB-5 at the boundary and this bundle is its kin — ROUND-1 §A precedent ⟨MigratePalettesDialog · A-31⟩.

**NO-WAVE-OWNER** — booked at the packet's home.

| ⟨record · id⟩ | :line | banked row text (quoted) | packet home |
|---|---|---|---|
| ⟨AboutPane · **AB-13**⟩ | :62 | **AB-13 · D-11** — The ratified 66ch About measure does not exist anywhere in the demo (`grep 66ch\|max-inline-size` → 0, re-confirmed); `Markdown.vue:98-102` declares the opposite (`max-w-full`); current … | X-W0 |
| ⟨AboutPane · **AB-14**⟩ | :63 | **AB-14 · D-12** — Dark-mode: the inline title control is optically identical to its sentence — **this seat's own recompute: 1.093:1 raw, ≈1.19:1 composited** (light 2.31) from probe-D1's computed inks; the … | X-W0 |
| ⟨AboutPane · **AB-26**⟩ | :78 | **AB-26 · L-7 + C-9(structural) ⇄CSS-L-7/L-13** — About is the last prop-drilled pane; its two state routes carry different values (alpha-bearing prop `App.vue:340` vs alpha-stripped injection `:271` — … | X-W0 |
| ⟨AboutPane · **AB-28**⟩ | :80 | **AB-28 · D-16** — Two ColorSpaceSelector instances own the same selection on one screen (both hosts re-read; visible in both tracked evidence frames). **DISPOSITION: NO-WAVE-OWNER — About-recomposition bundle … | X-W0 |
| ⟨AboutPane · **AB-33**⟩ | :85 | **AB-33 · hover-only affordance + MISS-4 (both readers' misses, MERGED)** — The pane's single dynamic affordance is pointer-hover-only (`:109-113` — no tabindex/focus/click/touch arm), the `cursor-pointer` … | X-W0 |
| ⟨AboutPane · **AB-36**⟩ | :88 | **AB-36 · D-13 (CONVERTED from unproven by this seat)** — RTL: the identity line shatters ("About the color / Lab ⌄ ,spaces"), ranges garble ("to 125 125-", "to 100% 0%"), sentence periods lead their lines, … | X-W0 |
| ⟨AboutPane · **AB-37**⟩ | :92 | **AB-37 · L-8** — `sourceExportPlugin` + `@src` alias have zero consumers; kept alive by a comment about this component's assets (census re-run: only the plugin and the comment). **DISPOSITION: NO-WAVE-OWNER … | X-W0 |

### §A.6 → X-W11 — the ColorNutritionLabel citation repair — 4 rows (0 routed · 4 NWO)

**Packet home for this fold's NWO rows.**

- `ColorNutritionLabel.md` — packet 18 · CATALOG-LIE⇄CSS **and the fold already carries the row**: X-W11-FOLD §1z's ColorNutritionLabel table books all four (`BOUND (AB-17)`) — the cure is a citation repair (⟨record · id⟩ attribution per §B.2 clause 5), not a new booking; the AB-17 limb rides to the G-F register.

**NO-WAVE-OWNER** — booked at the packet's home.

| ⟨record · id⟩ | :line | banked row text (quoted) | packet home |
|---|---|---|---|
| ⟨ColorNutritionLabel · **CNL-6**⟩ | :45 | **CNL-6 · D-3 / C-3 / L-8 — hover lights 6 of 10 nodes across all four rows · MAJOR.** Statically certain from my reads: one component-level `hoveredPath` ref (:232), predicate `hoveredPath.includes(space)` … | X-W11 |
| ⟨ColorNutritionLabel · **CNL-9**⟩ | :48 | **CNL-9 · D-7 — five sectional dividers ordered to zero · MAJOR.** `:15/:40/:68/:90/:146` + AboutPane's two = PR-14's seven; AboutPane rider A-3 already fixed the census vocabulary (7 components + 4 markdown … | X-W11 |
| ⟨ColorNutritionLabel · **CNL-11**⟩ | :50 | **CNL-11 · D-10 / C-8 — the Components rows disintegrate under RTL · MAJOR.** Mechanism forced by source (`:59-63`, three unisolated inline runs, no `dir`/`<bdi>`/`unicode-bidi` anywhere in the file — my read) … | X-W11 |
| ⟨ColorNutritionLabel · **CNL-16**⟩ | :55 | **CNL-16 · L-7 / C-9 / D-19 + C-5 — whole-model two-way binding never written; per-tick fan-out off the uncoalesced projection · MAJOR (cure 2 killed per R4).** `defineModel<ColorModel>` (:186) read at exactly … | X-W11 |

### §A.7 → X-W10 — pane chrome and register canon — 2 rows (2 routed · 0 NWO)

**ROUTED** — the token is on the row line.

| ⟨record · id⟩ | :line | banked row text (quoted) | the token, in the row |
|---|---|---|---|
| ⟨EmptyState · **ES-31**⟩† | :64 | **ES-31 · MINOR — the forced-colors tier-1 roster is keyed on a producer CLASS, so an aria-hidden ornament inherits an exemption whose written charter is color CONTENT** (reader-1's re-aim of the killed D-20; … | `…f). → **NO-WAVE-OWNER in X** (named residue; candidate X-W10 canon; the WHCM engine specifics stay U-7).` |
| ⟨shell-dock-dockstatuslamp · **D-7**⟩ | :25 | **D-7/C-10 grade** (perpetual pulse) \| INFO (per the C axis) \| MINOR ("a pulse signifying a constant") \| **INFO — and B's premise dies with AP-1 (this seat's sharpening)** \| B's confirmation reasons from … | `…is moot-on-AP-1. The 2.4s literal residue ≡ **AP-21** (X-W10; no producer rung — my scheme-motion read makes "token…` |

### §A.R — REFUTED AT THE BYTES: struck from the register, printed so nothing is silent (2)

These two sit in the check's 123 and are NOT counted in this file's 124. Each is banked in the
fold layer, verbatim and zero-pad-exact; the absence finding is an alias artefact of the check's
own alias table (the fold spells the record `strip`), not an absence at the bytes. **Duty on the
X-W6 seat:** print one dated `≡`-pointer line per row re-citing the banked row as
⟨wb-gradient-easingspecimenstrip · C-05⟩ / ⟨· C-06⟩, so the namespace-scoped census closes at
round 4 without a second booking. E-3: the check's bytes stand; this is an addendum, never a patch.

| ⟨record · id⟩ | the carriage, at the bytes |
|---|---|
| ⟨wb-gradient-easingspecimenstrip · **C-05**⟩ | `X-W6-FOLD.md:363` — `\| W6·88 \| **C-05 / D-40** (+ D-40-forward-half) ≡ **R-B** (D2-03 / N-2·r3 / R3-2) \| strip ≡ editor \| MAJOR \| The **zero-letterbox law is DEAD CODE**: `EasingAuthoringStage.vue:48-50` queries `svg[role='img']` — **0 elements in dist** …`. `grep -rwF -- 'C-05'` over the fifteen layer files → **1**, and it is the strip's. |
| ⟨wb-gradient-easingspecimenstrip · **C-06**⟩ | `X-W6-FOLD.md:358` — `\| W6·83 \| **GV-23** ≡ **C-06 / L-1** ≡ **R-C** (C2-2 / L-r3:L-1) \| wb-gradient-visualizer ≡ strip ≡ editor \| MAJOR \| `FAMILY_ORDER` (`easingCatalogue.ts:174`) is used as a **FILTER** … drops all six quart/quint presets …`; re-cited at `X-W6-FOLD.md:1089` — `**GV-23 / C-06 / R-C** — a reader escalated to BLOCKER; **MAJOR is the standing grade, both readers overruled**.` |

### §A.S — SUPPLEMENTS beyond the check's roster (3, marked ˢ at §A.1)

⟨BrowsePane · **i1**⟩ (`:87`) · ⟨· **i2**⟩ (`:88`) · ⟨· **i3**⟩ (`:89`) — three bold-leading INFO
rows in the same record, each ending in a bare `**→ X-W7.**`, each failing the identical byte test
the check applies to that record's thirteen M-rows. They are escapes by the check's own method and
are assigned to X-W7 here so round 4 finds them folded, not discovered. Their full text is in the
§A.1 ROUTED table with the rest of the band.

---

## §B. Spine directives — the two amendments this round owes

Both land in `docs/tranches/X/REFINEMENT-FOLD-2026-08-28.md`, by DATED ADDENDUM beside the bytes
they amend, never by patch (E-3). Neither moves a wave's bounds; neither stamps a wave; neither
claims census closure in this seat's voice.

### §B.1 — §0's walk is re-based on the pass-3 re-walk, dated, with the round-3 additions explicitly NOT pre-credited

**The directive.** §0 prints the round-1 walk **1,674** (`:45` — `… W10 **103** → 1,649 · W11
**25** → **1,674 FOLDED ROWS**`) beneath the §0 title *"the OUT side, countable from the twelve
folds' own bytes"*, while the twelve folds' own dated round-2 arithmetic blocks now sum to
**1,768**. §ROUND-2 declared that gap in advance and invited exactly this re-walk: *"1,674 stands
as the dated round-1 figure until a recheck re-walks it from the twelve folds' own bytes."* Append
to §0, dated **2026-08-28, round 3**, leaving the round-1 walk byte-untouched above it:

> **The round-2 walk (folded rows per wave, cumulative — re-read from each fold's own dated
> round-2 arithmetic block, 2026-08-28):**
> W0 **30** → 30 · W1 **56** → 86 · W2 **22** → 108 · W3 **27** → 135 · W4 **191** → 326 ·
> W5 **88** → 414 · W6 **511** → 925 · W7 **585** → 1,510 · W8 **78** → 1,588 · W9 **46** → 1,634 ·
> W10 **108** → 1,742 · W11 **26** → **1,768 FOLDED ROWS**.
>
> *Delta walk against round 1: 1,674 + **94** (W0 +2 · W4 +18 · W5 +5 · W6 +3 · W7 +51 · W8 +9 ·
> W10 +5 · W11 +1; W1/W2/W3/W9 unchanged — they were not among ROUND-2 §A's eight named seats) =
> **1,768**. The 94 decompose exactly: the **84** rows `ROUND-2-ASSIGNMENTS.md` §A assigned, plus
> the **10** kin-escape rows the X-W7 seat found on its own (W7.575–W7.584).*

**Per-wave receipts, each re-read at the bytes by this seat before the figure was written**
(§5.4: the OUT side is countable only from the folds themselves):

| wave | rows | the fold's own arithmetic block, verbatim |
|---|---|---|
| W0 | **30** | `X-W0-FOLD.md:1302` — `\| §Rows \| 28 (W0.1–W0.28) \| **30** (W0.1–**W0.30**) \| **+2** \| §A.8 **2** — W0.29 (ATP-35, ≡-pointer) · W0.30 (MISS-10, design residue) \|` |
| W1 | 56 | round-1 figure, unmoved; W1 took no round-2 assignment (not among ROUND-2 §A's eight seats) |
| W2 | 22 | round-1 figure, unmoved; no round-2 assignment |
| W3 | 27 | round-1 figure, unmoved; no round-2 assignment |
| W4 | **191** | `X-W4-FOLD.md:794` — `\| **rows booked** \| 123 (W4·1 … W4·123) \| 173 (… W4·173) \| **191** (W4·1 … W4·191, gapless) \| 173 + **18** = 11 routed (§7A.1) + 7 NWO (§7A.2) …` |
| W5 | **88** | `X-W5-FOLD.md:486` — `\| rows (§1 + §6b + §7b) \| 66 (W5F-01..W5F-66) \| 83 (..W5F-83) \| **+5** (W5F-84..W5F-88, contiguous, zero gaps) \| **88** \|` |
| W6 | **511** | `X-W6-FOLD.md:1403` — `\| §1 rows \| **508** (W6·1 … W6·508) \| **511** (W6·1 … W6·511, gapless) \| **+3** \|` |
| W7 | **585** | `X-W7-FOLD.md:1920` — `\| §Rows entries \| 140 \| **534** (W7.1–W7.533 · W7.X1) \| **585** (W7.1–W7.584 gapless · W7.X1) \| **+51** \|` |
| W8 | **78** | `X-W8-FOLD.md:1845` — `\| folded rows \| 69 (W8.1–W8.69) \| **78** (W8.1–**W8.78**) \| **+9** \| §A.5 routed **8** (W8.70–W8.77) + §A.5 NWO **1** (W8.78) \|` |
| W9 | 46 | round-1 figure, unmoved; no round-2 assignment |
| W10 | **108** | `X-W10-FOLD.md:2367` — `\| rows (contiguous X-W10.1..) \| 103 \| **+5** (X-W10.104..X-W10.108) \| **108** \|` |
| W11 | **26** | `X-W11-FOLD.md:1109` — `\| FOLDED rows (§1) \| 25 \| **26** (W11.26) \| +1 — the §A.7 row, gapless and contiguous W11.1..W11.26 \|` |

**THE POSTURE ON THIS ROUND'S ADDITIONS — stated so no reader mis-reads the Σ.** The **124** rows
assigned in §A are **NOT** inside the 1,768 and **must not be pre-credited to it**. 1,768 is a
dated *pass-3* measurement of what the twelve folds carry TODAY; §A is a work order for rows they
do **not** carry. The walk that counts them is the RECHECK's — run after the round-3 seats have
folded, from the twelve folds' own bytes, never from this file's arithmetic. This seat re-measured
no wave's rows beyond the twelve blocks quoted above and claims no new Σ. The sentence §0 must
carry with the round-2 walk, verbatim:

> *The 124 ⟨record · id⟩ assigned at `refinement/ROUND-3-ASSIGNMENTS.md` §A are outside this walk
> by construction — they are precisely the rows the folds do not carry. No figure here anticipates
> them; the round-3 walk is re-derived by the round-4 recheck from the twelve folds' own dated
> arithmetic blocks, and only then.*

**Also cured by this directive** (the check's AXIS-5 MINOR, whole): §0's title claims the walk is
countable from the folds' bytes while its live figure was one round stale. After the addendum the
title is true again, and `grep -n '1,768' REFINEMENT-FOLD-2026-08-28.md` — today **0 hits** —
returns the round-2 walk. §0's and §ROUND-1's round-1 figures stay byte-untouched beside it.

### §B.2 — the GENEROUS CARRIAGE METHOD, codified into §1 as the standing census law

**The directive.** §1 already carries the ROW-CARRIAGE LAW (round 1: *a packet's home wave adopts
its riding rows id-for-id*) and §1.L, the CENSUS METHOD LAW (round 2: *how a row's home is
found*). Both legislate the **token** side. Neither says what it means for a row to be **carried**,
and that hole is the entire pass-3 BLOCKER: rows are banked in the folds under bare ids with no
record attribution, so a namespace-scoped census cannot see them — while a namespace-blind census
reads ⟨shell-dock-actionbarlayer · AB-13⟩ as carriage for ⟨AboutPane · AB-13⟩, the exact collision
§1.L clause 4 already legislates against. Append to §1, immediately after §1.L, dated
**2026-08-28, round 3**:

> **§1.G — GENEROUS CARRIAGE LAW (round 3, 2026-08-28).** §1.L says how a row's home is *found*.
> This says what it means for a row to be *carried*. It binds every seat and every check. It is a
> **method** law, not a row: it moves no wave's bounds and stamps nothing.
>
> **1. CARRIAGE IS NAMESPACE-SCOPED; A BARE ID IS NEVER CARRIAGE.** ⟨record.md · id⟩ is carried by
> the fold layer if and only if some layer line matches the id under clause 2 **and** that line —
> or a markdown heading in scope above it — names the record under clause 4. An id appearing in
> the layer attached to no record is carriage for nothing; six namespaces collide and the layer's
> own bytes prove it: `AB-13` occurs 4 times, all four ⟨shell-dock-actionbarlayer · AB-13⟩ at
> `X-W4-FOLD.md:181/:183`, while ⟨AboutPane · AB-13⟩ is nowhere; `R-31` occurs once,
> ⟨picker-componentsliders · R-31⟩ at `X-W6-FOLD.md:829`, while ⟨wb-extract-imagedropzone · R-31⟩
> is nowhere.
>
> **2. ID MATCHING IS GENEROUS — case-insensitive, hyphen- and zero-pad-tolerant,
> boundary-anchored.** Split the id into alpha and numeric runs; rejoin with an optional `[-._·]`
> between runs; allow leading zeros before every numeric run; anchor both ends with
> `(?<![A-Za-z0-9])` / `(?![A-Za-z0-9])`. So `D-06` ≡ `D-6` ≡ `D06`, `M2` ≡ `m2` ≡ `M-2` — and
> `M2` still never matches `M-24`. **This REVERSES §1.L clause 4's second sentence for the
> CARRIAGE test only, and the reversal is deliberate:** clause 4's `D-06 ≠ D-6` / `ADJ-M1 ≠
> ADJ-m1` remains the law of IDENTITY — a fold may never re-spell an id, and ROUND-2's ERRATUM
> R1-A-1 (`L-R6` → `L-R6-4`) stands unchanged. But a census that spells identity strictly AND
> carriage strictly manufactures escapes out of the layer's own typography. **Identity: strict.
> Carriage: generous.** They are different questions and the law now says so.
>
> **3. IDENTITIES DEDUPE BY THE NORMALISED ID.** ⟨BrowsePane · M2⟩ and ⟨BrowsePane · m2⟩ are ONE
> ⟨record · id⟩ and the row folds once. A census that counts them twice inflates its own BLOCKER;
> round 3 measured a 6-row inflation in that one record before the dedupe.
>
> **4. THE ALIAS SET IS DERIVED *AND* DECLARED.** *Derived*: the record stem, plus every
> suffix-join of its hyphen segments ≥4 characters in both concatenated and hyphen-joined form,
> plus every `*.vue`/`*.ts` symbol named in the record whose lowercase equals such a suffix-join
> (`wb-mix-animationcanvas` → `animationcanvas`, `mixanimationcanvas`, `MixAnimationCanvas.vue`;
> `wb-mix-pane` → `mixpane`, `MixPane.vue` — the alias expansion alone dissolved 14 apparent
> escapes at that one record). ***Declared*, and this is the clause the pass-3 check lacked**: the
> folds' own `| record |` column short forms — `strip`, `editor`, `configbar`, `resultdisplay`,
> `sourceselector`, `animationcanvas` — are aliases **by declaration**, because no suffix-join
> produces them, and two real bookings hid behind exactly that gap
> (⟨wb-gradient-easingspecimenstrip · C-05/C-06⟩, banked at `X-W6-FOLD.md:363/:358`, reported
> absent). **Any fold that uses a short form in a record column MUST print that short form's
> expansion once, in the fold, beside the table that uses it.** An undeclared short form is a
> census hole whose cure costs one line.
>
> **5. HEADING SCOPE COUNTS; BOLD PSEUDO-HEADINGS DO NOT — until they are cured.** A markdown
> heading (`#`..`######`) naming a record scopes every id beneath it until the next heading of its
> level or shallower. A **bold line** naming a record — `**ColorNutritionLabel — 27 rows**` at
> `X-W11-FOLD.md:715` — is NOT a heading and does not scope: four real bookings sit under it
> (`CNL-6` `:718` · `CNL-9` `:721` · `CNL-11` `:723` · `CNL-16` `:728`, each reading `**BOUND**
> (AB-17)`) and every namespace-scoped census reads them as escapes. **A fold that keys a table by
> a bold record label MUST either promote the label to a heading or attribute each row inline as
> ⟨record.md · id⟩.** Until it does, its rows are uncarried by law, not by accident. §A.6 is that
> repair, worked.
>
> **6. THE `≡`-POINTER IS THE CURE, AND IT *IS* CARRIAGE.** Where a roster id is a facet of an
> already-folded identity, one dated `≡`-pointer line citing ⟨record.md · id⟩ closes the byte
> census. A silent skip does not. A pointer line satisfies clauses 1 and 4 by construction, so the
> cure and the test are the same shape — which is why the cure is cheap and the omission is not.
>
> **7. THE ROW PREDICATE IS ELECTED — §1.L clause 7 is settled.** The row regex for every census
> in this layer is **`^\s*([-*]|\|)\s*\*\*`**: the predicate X-W4-FOLD §6E printed, the one the
> spine's own law-true per-wave table was measured under, and the strictly wider of the two
> candidates (`^(- |\* )\*\*` loses table rows, and this layer books rows in tables). The 5-row
> gap §1.L's adoption re-measurement recorded — 4,694 under this form vs 4,689 under
> `^(- |\| )\*\*` — resolves in favour of **4,694** by that election. **Clause 7's moratorium is
> lifted:** a per-wave total may now be quoted as authoritative provided it states this regex
> inline per §1.L clause 6. Nothing clause 7 says about the TOKEN predicate is touched — that was
> never in doubt, and the pass-3 register survives BOTH row regexes on 114 of its members.
>
> **8. THE COUNT STILL FOLLOWS THE PREDICATE (§1.L clause 6, re-asserted for carriage).** Any
> census figure printed under this law states its corpus, its row regex, its token class, its
> alias set — derived AND declared — and its carriage test, and reproduces at the tree at a named
> HEAD. A carriage figure that reproduces under no stated alias set is struck on discovery, not
> reconciled. This clause is what lets a later seat refute a member of *this file's own* register
> at the bytes; §A.R is the worked example, and it cost one `grep -rwF`.

**Why this belongs in §1, not §5.** §1 is where the ROW-CARRIAGE LAW says a homed packet carries
its rows id-for-id, and where §1.L says how a home is found. §1.G completes the triple by saying
when a row is carried. All three answer one question — *is this row really booked?* — and
splitting them across sections is how the pass-1, pass-2 and pass-3 BLOCKERs each escaped the
previous round's cure. §5.6's pointer law resolves the reference without a second registration
(§5.1; M-25 dedupe).

---

## §C. What this file does NOT claim

No sentence here closes the X census. §A is a work order; the register closes when the round-3
seats have folded it and a round-4 recheck re-derives the census from the twelve folds' own bytes
under §1.G. No wave is stamped; no gate is read; no `VERIFIED` mark is placed — the word appears
in this file only in this negation. §READINESS stays planning-only. The dated wave specs under
`docs/tranches/X/waves/` and `CONFORMANCE-2026-08-03.md` are byte-untouched by this seat (E-3),
and `ROUND-1-ASSIGNMENTS.md` / `ROUND-2-ASSIGNMENTS.md` are round records this file supersedes
neither of nor patches.

— end. Consumed by the seven named per-wave repair seats this round; then it stands as the
round-3 census record.
