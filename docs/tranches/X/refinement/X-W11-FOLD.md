SERVED MODEL ID (fold author, line-1 declaration): claude-opus-5[1m]

# X·V REFINEMENT FOLD — X-W11 · Release and Verified Close · the one dated addendum

## 0. Header — provenance, posture, the reading rule

**Provenance.** This seat's assigned input set is three adjudicated apotheoses from the value.js
corpus at `docs/tranches/V/megatranche/registry/adjudicated/` — **`PreviewRamp.md`** (2026-08-06,
HEAD `3487d73e`, corpus pin `c654824e`/`f36f780c`), **`ColorNutritionLabel.md`** (2026-08-04, corpus
HEAD `c654824e`, re-verified `2e334231`, re-verification addendum at `e8393685`), and
**`PaletteColorStrip.md`** (2026-08-06, HEAD `3ecf0267`) — plus a whole-corpus grep for `X-W11` and
its dotted unit forms (`X.W11`, `X·W11`, `X.W11.a..e`) over every non-`kf-`/`fr-`/`pt-` record.
That grep returns **six** files and is enumerated at §0a n.3; nothing else in the 88-component +
band corpus names this wave.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0.md..W11.md` and
`docs/tranches/X/CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is this seat's **sole
write**. Where a clause here strikes or sharpens a phrase in the dated W11 spec, the strike is
recorded here and the spec's bytes stand untouched. The three source apotheoses are likewise
immutable (E-1/E-3) — this fold quotes them, never rewrites them, and every correction OF them is
dated below.

**The reading rule.** At execution X-W11 reads **`waves/W11.md` PLUS this addendum**, in that
order; where the two disagree the addendum's clause governs. Nothing here re-stages, re-weights or
re-composites W11's twelve gates (OC-2 staged-reading stands); this addendum sharpens predicates,
adds gate rows with real witnesses, adds bounds, and books the accounting surface the dated spec
does not have.

**Execution posture.** X-W11 `Status: planned`. **This fold opens NO product source.** Every witness
below is transcribed from a banked adjudication's own measurement — no probe was run by this seat,
no script was authored (L-19: proof-scripts are presumed contrivance; a born-RED gate names a real
witness or it is not born-RED).

**Fold classes.** **cl.1** = spec sharpening / law / correction · **cl.2** = cure-lock or binding
sequencing clause · **cl.3** = accounting row (the boundary carry census, §1z) · **cl.4** =
OUT-OF-WAVE, named and handed to the owning wave's fold seat, never dropped.

**Arithmetic — countable from this file's own bytes.** **21 FOLDED rows** (§1, W11.1..W11.21,
contiguous, one ⟨provenance⟩ line each) · **17 GATE rows** (§2 — **12 sharpenings** of existing
W11 gates + **5 genuinely new**) · **7 BOUNDS-DELTA entries** (§3) · **85 census rows** (§1z —
PreviewRamp 27 = R-1..R-26 + ADJ-1 · ColorNutritionLabel 27 = CNL-1..CNL-27 · PaletteColorStrip 31
= PCS-1..PCS-31), of which **34 carry a NO-WAVE-OWNER limb** (+1 conditional, §0a n.6) and **51 are
OUT-OF-WAVE**. The IN side is a receipt of the assignment, not document-internal arithmetic: no
in-equals-out identity is asserted. **Dedupe by banked identity is applied throughout: no row is
re-booked under a new name (anti-rename), and a row already banked at a sibling record is
cross-referenced, never counted twice** (§0a n.5).

### 0a. Seat challenged — corrections, never silent

1. **`PreviewRamp.md` addendum clause 5's own enumeration is one row short of its roster.** The
   clause reads verbatim: *"The next formation boundary consumes rows R-4, R-6(+ADJ-1), R-7, R-8,
   R-12(file half), R-13, R-14, R-15, R-16(chip half), R-18(residue), R-19(producer half), R-21,
   R-22(map edit), R-24, R-25, R-26 whole — zero silent drops."* That is **16** rows. But **R-5**'s
   own roster row declares *"**Route: split — X-W6** (host lane composition, MixConfigBar) **+
   NO-WAVE-OWNER** (the chip's own sizing, cured by R-4's recipe)"* and appears nowhere in the
   clause-5 list. The record is not rewritten (E-3); the boundary census at §1z counts
   **17 PreviewRamp rows**, and the delta is named here so the omission cannot read as a drop. The
   substance is unaffected — R-5's NWO half is cured by R-4's recipe, which IS listed.
2. **GATE-LABEL COLLISION ACROSS WAVES — a live mis-read hazard at execution.** Three source rows
   route to a gate label that also exists in X-W11's own twelve: ADJ-1's sequencing rule says
   *"if the chip cure lands first, **G8**'s grep gate is amended"* — that **G8 is the
   GenerateControls wave's gate**, not X-W11's real-Safari cell; PCS-6 routes to *"**X-W7.c G11**
   (silhouette law)"* — not X-W11's zero-legacy-seam gate; PreviewRamp R-9 is *"gated at its wave's
   **G4**"* (GenerateControls) — not X-W11's registry coordinate. **Rule folded: in this addendum
   and in every W11 close document, a bare `G<n>` means an X-W11 gate; a foreign gate is always
   written `<wave>·G<n>`.** Nothing in the sources changes; the reading does.
3. **The whole-corpus `X-W11` grep, enumerated once so no future seat re-greps.** Six files, and
   only ONE routes an obligation to this wave. (a) `PreviewRamp.md:42` — *"the whole-suite green
   stamp is X-W11's"* — **the only by-name routing to X-W11 in the entire value.js corpus**.
   (b) `PaletteColorStrip.md:138` and (c) `shell-dock-dockstatuslamp.md:127` — the stamping-law
   verdict sentence, identical in both: *"These dispositions are ADJUDICATED, not VERIFIED — X-W11
   stamps, not this file."* (d) `ShadowPalette.md:8` and `:45` — cite **`W11.md:343-344`** (the G11
   probe paragraph) as the authority that *"X-W8 owns the 19 `demo/ui` barrels + stale ESLint
   paths"*; verified this session: `W11.md:343-344` is exactly that text. (e) `AuroraPane.md:8` —
   reads *"`W10.md`/`W11.md` heads"* as routing joints, books nothing here. (f)
   `ColorNutritionLabel.md:9` — cites *"CARRY-CUT-LEDGER.md X-W0..X-W11 rows"* in its anti-rename
   identity sweep. Range citations of the form `W0..W11` elsewhere in the corpus are wave-authority
   citations, not routings, and are excluded by inspection.
4. **Only the three assigned records are booked.** `ShadowPalette.md`, `shell-dock-dockstatuslamp.md`
   and `AuroraPane.md` touch W11 by citation only; their rows belong to their own fold seats. **No
   row of theirs is booked in §1z** (anti-double-booking). Their W11-touching lines ride §4
   CrossEdges as edges, with `ShadowPalette` **SP-11** carried explicitly because it makes a claim
   ABOUT W11's G11 ownership text.
5. **Identity folds honoured, not re-booked.** PreviewRamp **R-9** ≡ GenerateControls **#20**
   (TSCONFIG-PATHS-DRIFT) ≡ ColorSpaceSelector **L-14** ≡ PaletteColorStrip **PCS-30**
   (W-HYGIENE config parity) — **ONE identity, four rosters**; booked once at W11.13 as an
   assertion duty, and `X-W8.md:112` explicitly declines the cure ("L-22 ≡ W-HYGIENE, another
   owner"). PaletteColorStrip **PCS-2** is a FOLD to PaletteCard **PC-7** (grade lives there);
   **PCS-16** ≡ **PC-14**; **PCS-19**'s Generate limb ≡ GenerateControls **row 6** (BANKED/W25);
   **PCS-13** ≡ ColorSpaceSelector **L-5(a)**; CNL-**1** ≡ CSS-L-1/AB-1; CNL-**15** ≡ **CC-078**
   (the 19-barrel count already inside G11's RED baseline); PreviewRamp **R-1** ≡ GenerateControls
   **B1** census; PreviewRamp **R-19** ≡ **R-2(b)** (one defect, two vantages). Each is
   cross-referenced in §1z under its original id.
6. **One CONDITIONAL boundary row.** CNL-2 disposes *"**X-W9** … **+ X-W1** born-RED e2e … **If
   X-W9's cut excludes demo seams, this row escalates to the boundary with rider A-1.**"* It is
   counted OUT-OF-WAVE today and flagged conditional in §1z; if it escalates, the §1z census
   becomes 35 and W11's G16 count moves with it. Recorded so the move is arithmetic, not discovery.

---

## 1. Rows

Severity is the source record's own grade, carried unchanged. `cureShape` and `locks` are **verbatim
where load-bearing**. `Sequencing` binds the close act; `Dissent` is preserved, never ruled away.

**W11.1 · ADJUDICATED ≠ VERIFIED — the stamping precondition on R-A** (cl.2 · BLOCKER-weight,
process)
*Mechanism*: X-W11 is the sole VERIFIED-stamping seat (R-A: "X-W11 advances its own verb line and
every sibling wave's row from IMPLEMENTED to VERIFIED in one act at close"). Every record in the
input set closes by denying that its dispositions carry that word — so a stamp act that reads a
registry disposition as evidence would convert an adjudication into a verification with no landed
byte between them.
*locks (verbatim)*: PreviewRamp §Status verbs — *"every disposition below is **ADJUDICATED, not
VERIFIED**. Nothing in this file stamps any wave; X-wave routings are ownership routings under the
X CONFORMANCE bounds, and D-19 re-measure-at-open governs every line anchor."* · ColorNutritionLabel
§roster preamble — *"Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.
Wave ids are the CARRY-CUT-LEDGER X set; NO-WAVE-OWNER rows ride rider A-1's list to the next
formation boundary under their original ids."* · PaletteColorStrip §Verdict and
shell-dock-dockstatuslamp §Verdict, identical sentence — *"These dispositions are ADJUDICATED, not
VERIFIED — X-W11 stamps, not this file."*
*cureShape*: the stamp act emits, per stamped wave, a two-column map — registry row id → landed
evidence (commit + path) **or** `NOT-CARRIED`. A wave with an unmapped registry row that names it
may be stamped IMPLEMENTED and **may not** be stamped VERIFIED.
*Sequencing*: precedes commit #8 of the Commit Plan; blocks it.
*Dissent*: none — three independent seats wrote the same sentence unprompted.
⟨PreviewRamp.md:12 + :142 · ColorNutritionLabel.md:33 + :122 · PaletteColorStrip.md:32 + :138 ·
cross-edge shell-dock-dockstatuslamp.md:127⟩

**W11.2 · The registry is a SECOND terminal accounting surface beside the 117 CC rows** (cl.3 ·
MAJOR)
*Mechanism*: G1's terminal table walks `CARRY-CUT-LEDGER.md` §1's 117 CC rows. The adjudicated
registry carries a disjoint population — rows whose cure is owned by no X wave — and its own routing
law makes the accounting mandatory, not optional.
*locks (verbatim)*: PreviewRamp:37 — *"a row routes to a tranche-X wave only where that wave's
§File Bounds owns the edited surface; otherwise **NO-WAVE-OWNER** — a named formation-boundary
carry, never a silent drop."* · PreviewRamp addendum 5 — *"**NO-WAVE-OWNER is a named carry** …
The next formation boundary consumes rows … **zero silent drops.**"* · PaletteColorStrip
COORDINATION 3(iii) — *"PCS-3 (schema `weight`), PCS-9 (area seam), PCS-11 (`position` contract)
have NO wave owner and are the boundary rows the next formation must home."*
*cureShape*: `docs/tranches/X/close/BOUNDARY-CARRY.md` — id-keyed, one row per NO-WAVE-OWNER limb
(**34 from this seat's three records**, §1z), each with: original id · source record · severity ·
the cure text pinned at its record · destination (`FORMATION-BOUNDARY` | `GLASS-RELAY` |
`SERVER-CONTRACT`). Ids are original for life.
*Sequencing*: authored by X.W11.a beside `LEDGER-FINAL.md`; cited by `FINAL.md`; **it is not a
§4 row and does not enter the 117** (G1's falsifier: "a 118th row without a §1 origin also fails").
*Dissent*: none.
⟨PreviewRamp.md:37 + addendum 5 · ColorNutritionLabel.md:33 (rider A-1) · PaletteColorStrip.md:134⟩

**W11.3 · NAMED-GAP degradation — a routed row whose owning wave's mechanism text does not name it
degrades to NO-WAVE-OWNER at execution** (cl.2 · MAJOR)
*Mechanism*: two rows in one record route to X-W7.c and then state, in the disposition itself, that
W7.c's spec text does not carry them. If W7 closes without adopting them, W11 would stamp W7
VERIFIED over a silently-degraded row.
*locks (verbatim)*: PCS-1 — *"**Disposition: BUILD → X-W7.c — with a NAMED GAP**: W7.c's mechanism
(:210-212) names only the `Math.max(…, 0.5)` clip floor and 'flex-basis proportional to weight';
the WEIGHT_FLOOR renormalisation defect is in no X file's mechanism text. W7.c must adopt this row
explicitly (its `n-fixtures` gate is the natural home) or it degrades to NO-WAVE-OWNER at
execution."* · PCS-19 — *"**X-W7.c with a NAMED GAP**: MixSourceSelector.vue appears in no W7 file
list; the strip's call-site migration must adopt it or this limb degrades to NO-WAVE-OWNER at
execution."*
*cureShape*: the R-A map of W11.1 carries a third value, `DEGRADED-TO-BOUNDARY <row-id>`; any wave
presenting a DEGRADED row is stamped IMPLEMENTED, and the row moves to `BOUNDARY-CARRY.md` in the
same commit.
*Sequencing*: X-W7's own close must state adoption or degradation **before** W11 opens; W11 reads,
never rules.
*Dissent*: PCS's own COORDINATION 3 lists these as gaps "for the formation", not for W11 — this
fold takes only the ACCOUNTING, never the cure.
⟨PaletteColorStrip.md PCS-1 · PCS-19 · COORDINATION 3(i)(ii)⟩

**W11.4 · CURE-COLLISION — `[data-color-surface]`: three adjudications, three fates, one line of
`foundation.css`** (cl.2 · MAJOR, adjudication-layer)
*Mechanism*: GenerateControls (R-C, gated at **GenerateControls·G8** with a `grep -c … = 0`) orders
the selector DELETED as a dead hook; PreviewRamp R-6's ruled cure MINTS its first two producers;
PaletteColorStrip PCS-2 (≡ PC-7) needs it as the rebuilt strip's roster hook. Landed blind in either
order, one record breaks the other — and W11's single-act stamp would certify both waves VERIFIED
across a contradiction.
*locks (verbatim)*: ADJ-1 — *"**Sequencing rule (binding on both)**: whichever executes first
re-reads the other's row — if the chip cure lands first, G8's grep gate is amended to exempt the
now-live hook (the deletion premise 'zero producers' is dated evidence, not law); if G8 lands
first, the chip cure adds `.preview-chip` to the roster by class in the same commit it adds the
attribute, or re-mints the hook WITH its first producer."* (that `G8` = **GenerateControls·G8**,
§0a n.2) · PCS COORDINATION 2 — *"X-W7.c must make ONE ruling (first consumer, or roster-class
membership + deletion) — three adjudications now point at the same line of `foundation.css`."*
*Witness (born-RED, real, measured by two seats independently)*: `data-color-surface` producers in
`demo/` → **0** (PreviewRamp seat's grep); `data-color-surface` → **exactly the 2 CSS rule bodies**
(PaletteColorStrip seat's grep). Zero producers, three orders.
*cureShape*: a pre-stamp sweep — for every selector/token/file named by a "dead — delete" ruling in
any adjudicated record, grep the post-cut tree and assert the landed fate matches ONE ruling; a
divergence names both record ids and blocks the stamp. New gate **G13**.
*Sequencing*: runs after X-W7 and X-W6 land, before commit #8.
*Dissent*: PreviewRamp addendum 1 generalises this beyond the one selector — *"every 'dead — delete'
ruling is dated evidence about producers-at-adjudication-time, not standing law"* — adopted whole.
⟨PreviewRamp.md ADJ-1 + addendum 1 · PaletteColorStrip.md PCS-2 rider (i) + COORDINATION 2 ·
cross-record GenerateControls.md R-C⟩

**W11.5 · CITATION CURRENCY — `git cat-file -e` proves existence, not currency** (cl.1 · MAJOR)
*Mechanism*: L-6 requires every `FINAL.md` citation to resolve. Two realized
citation-inheritance events in this input set prove resolution is insufficient: a sibling record's
praised cell is arithmetically wrong, and a hostile re-reader repeated a challenger's figure while
citing the lines that refute it.
*locks (verbatim)*: PCS COORDINATION 1 — *"**PaletteCard.md Superlative 1 is WRONG about the
arithmetic it praises.** … The dated record is not rewritten (E-1/E-3); this entry stands beside it,
and any wave consuming PaletteCard.md's superlative list must read this correction first. This is a
citation-inheritance hazard of exactly the ColorSpaceSelector-DISSENT-1 species: a praised cell
repeated without re-derivation."* · CNL K13 — *"reader-L's D-11 confirmation cell '= 5.625
(ink.ts:15,17,137)' — citation-inheritance: the challenger's arithmetic repeated against the very
lines that refute it."* (the true figure is **5.75**, additive `floor + CERTIFY_HEADROOM`, named in
source prose).
*cureShape*: any registry record cited by a W11 close document is read WITH its dated
`COORDINATION` / `Re-verification addendum` / `Addendum clause` blocks; the citation records which
correction blocks were read. New gate **G15**.
*Sequencing*: binds X.W11.e (FINAL + packets).
*Dissent*: this could ride G12 rather than stand alone — recorded as a live alternative, not
resolved here; the two realized events are why it is booked separately.
⟨PaletteColorStrip.md COORDINATION 1 · ColorNutritionLabel.md R1/K6/K13 + §Re-verification
addendum · PreviewRamp.md addendum 1⟩

**W11.6 · G5 SEED MATRIX — production boot truth is not bare-origin-only; and the boot cell is not
the render cell** (cl.1 · BLOCKER)
*Mechanism*: G5's probe boots both modes at **bare origin**. CNL-3 measures a boot-time white
screen from a persisted/deep-linked seed: `useColorPipeline.ts:75-76`'s
`channelNumber(convertPickerColor(model.value.color,"hsv"),"h")` is unguarded and runs at
`App.vue:245` — **inside App's own setup, OUTSIDE the ErrorBoundary** (R3: `onErrorCaptured` sees
only descendants), so it is a genuine blank, not a caught fallback. Its twelve-lines-down twin
(`:88-95`) IS try/catch-guarded with the powerless-hue comment.
*Witness (born-RED, measured against shipped dist by the CNL seat, re-run byte-identically on
resume at `e8393685`)*: **28 of 256 greys** round-trip to hsv-powerless `["none",0,v]` —
`[0,3,6,23,26,43,46,50,51,52,62,74,96,122,135,143,157,159,160,161,187,225,228,243,247,248,251,253]`;
`black→hsv = ["none",0,0]`; `hydrate.ts modelFrom` admits them; the throw is `Missing hsv.h`
**before mount**.
*locks (verbatim)*: CNL-3 disposition — *"**X-W9** (same seam, same cure family as CNL-2) **+ X-W1
born-RED boot test over the seed matrix (`?color=black` at minimum)**"* · the class bound, also
measured — *"a persisted none-hue lch/oklch string does NOT boot-crash (numericSource substitutes
0) — it re-enters CNL-2's render crash instead."*
*cureShape*: G5's probe gains a **seed dimension** in both modes — `?color=<seed>` over the 28-grey
set ∪ {`black`} — asserting `#app` non-empty + `pageerrors []`. **The boot cell and the render cell
are separate cells and are never merged** (the I-20 discipline applied to failure classes): a green
boot on a none-hue lch seed is not evidence about CNL-2's render crash, which is a `role="alert"`
fallback over the pane grid with a re-throwing Try-again (R3/K14), not a blank.
*Sequencing*: X-W1 authors `boot-smoke.mjs`'s seed arm (W11 holds execute-no-write); W11 asserts.
If the seed arm is absent at open, G5 reads **UNMEASURED**, never green.
*Dissent*: none — the mechanism is source-certain and the trigger set is measured.
⟨ColorNutritionLabel.md CNL-3 + R3 + K7/K14 + §Re-verification addendum (4)⟩

**W11.7 · G3 BOUNDARY-INPUT SMOKE SET — "documented input" includes the inputs the library actually
fails on** (cl.1 · MAJOR)
*Mechanism*: G3 requires *"every runtime export is smoke-invoked on its documented input"* and its
RED already names CC-084's `parseCssColor("oklch()")` crash. Two further measured failures sit on
documented inputs of shipped conversion exports and would pass a happy-path smoke.
*Witness (born-RED, measured against `dist/subpaths/color.js` by the CNL seat)*: hsl grey-sweep
failures **exactly** `[[255, color_non_finite]]` — i.e. **white cannot convert to HSL** — with
neighbours fine and `rgb(255,255,254)→hsl` s = **1.000000000000085** (out of declared domain);
mechanism from `anchors.ts`: `encodedToHsl` divides `delta / (1 - |2L - 1|)` **before** the
powerless law can see the channels, and `convertColor`'s non-finite check precedes `finishChannels`
— the guard exists and runs in the wrong order. Separately: **256/256 greys** emit `"none"` hue in
both lch and oklch.
*cureShape*: `verify-packed-surface.mjs`'s per-export input row is extended by an
**achromatic/white/none-hue** case for every conversion export; the emitted JSON records the input
beside the verdict. (X-W9 owns the script's bytes and the library cure — W11 executes and asserts.)
*Sequencing*: G3 after X-W9's cured script lands; a G3 JSON that contains a verdict with no recorded
input fails the fabricated-constant check by the same argument that killed `strictTypes: 62`.
*Dissent*: CNL routes CNL-18 to X-W9 as a library-correctness row; this fold takes only the
**gate-input** half and books no cure.
⟨ColorNutritionLabel.md CNL-18 · CNL-2/CNL-3 premises · §Re-verification addendum⟩

**W11.8 · G9 — install receipts carry INTEGRITY, not only a version string** (cl.1 · MAJOR)
*Mechanism*: G9's falsifier already rejects a tuple filled from a manifest range and demands
`npm ls`. But `npm ls` reports a **resolved version**, and this corpus measured two artifacts
carrying the SAME version label with different bytes.
*Witness (born-RED, measured by the PreviewRamp seat, `wc -c`)*: self-install
`node_modules/@mkbabb/value.js` is a real directory, **4.0.0 = repo 4.0.0**, and `css.d.ts` is
**10910 B** there vs **12490 B** in the checkout. Corroborated from the other side by
PaletteColorStrip's L-3 kill (`tsc --traceResolution`): the demo's types resolve by **package
self-name resolution** to `/…/value.js/dist/subpaths/css.d.ts` Package ID `@4.0.0` — *"the
node_modules byte-diff is real and never consulted."* It is not consulted **by the demo typecheck**;
it is exactly what a release tuple would consult.
*cureShape*: every row of `close/consumers/TUPLES.md` carries `resolved version` **and**
`integrity`/`shasum` from the install receipt, compared against `registry-coordinate.json`. A tuple
whose integrity is absent reads `UNMEASURED`.
*Sequencing*: G9 after G4 (the coordinate must exist to compare against).
*Dissent*: the PreviewRamp seat labelled this "the packed-surface truth question" and routed it to
X-W1/`verify-packed-surface.mjs`; this fold takes the **tuple-column** half only, leaving R-9's
identity where it is banked (§0a n.5).
⟨PreviewRamp.md §Arbiter's instruments (self-install measurement) + R-9/L-10 ·
PaletteColorStrip.md L-3 kill (K-2) + PCS-30⟩

**W11.9 · G2 — a test DENOMINATOR is not COVERAGE** (cl.1 · MAJOR)
*Mechanism*: G2's falsifier protects the denominator ("may rise, never fall"). Two records measure
the denominator's blindness directly: the number can hold while the deliverable is deleted.
*Witness (born-RED, two independent greps)*: PCS-4 — *"zero tests anywhere reference the component,
and replacing :50-71 with `colors.map(() => 100/n)` deletes WEIGHT_FLOOR, the weighted path and the
whole S.W5-6/F7/T19 deliverable while vitest, playwright, vue-tsc and eslint stay green"*
(`ColorStrip|WEIGHT_FLOOR|segmentPct` across `test/ e2e/ demo/test/` → **exit 1**). CNL-5 —
`ColorNutritionLabel` in `test/`+`e2e/` → **1 hit, and it is a comment** (`o10d:41`).
*cureShape*: the battery cell records, beside the 348-and-rising denominator, the **named
deliverables asserted by it** for every component a landing wave rebuilt; a rebuilt component with
zero referencing assertions is recorded `UNCOVERED`, and G2 stays green only if `UNCOVERED` is
empty or each entry names its owning wave's own gate.
*Sequencing*: G2 at the top of the battery; the `UNCOVERED` list is an input to W11.1's stamp map.
*Dissent*: none.
⟨PaletteColorStrip.md PCS-4 · ColorNutritionLabel.md CNL-5⟩

**W11.10 · G7 — the five vacuous-green classes, each with a measured instance** (cl.1 · BLOCKER)
*Mechanism*: G7's falsifier asserts the **executed** count separately from the exit code. The corpus
supplies five distinct ways a browser cell reports green while asserting nothing — four of them
measured on the two oracles W11 will actually run.
*Witness (born-RED, all re-derived statically by the PreviewRamp/CNL/PCS seats)*:
(a) **TAUTOLOGICAL LEG** — o14's one green chip leg (`:334-344`, honest-absence `count === 0`) is
*"a permanent tautology under R-1"* (the component renders zero instances).
(b) **UNREACHABLE QUERY** — `o14-preview-truth.spec.ts:351-355/:410-416` await
`getByRole('button', {name: 'Add current color to the mix'})`, *"an accessible node the producer's
`<span aria-hidden>` can never expose"*.
(c) **DIALECT-BLIND READER** — the stamped dialect is `oklch(62% 0.27 9.8deg)`
(`grammar.ts:309`), and the spec's sole reader `parseOklchTriples` (`:162-168`) returns **0**
matches on it vs **1** on canonical — its `[ ,]+` cannot cross `%` or `deg` — while `:376-392`
asserts `painted.length === stamped.length`, *"defective on BOTH UA branches (17-vs-0 RED, or
0-vs-0 vacuous)"*.
(d) **SELECTOR THE CUT REMOVES** — PCS-22: the corpus's own proposed oracle *"selects by ARIA
attributes that C-12's own cure removes — two cures in one corpus that collide, noticed by neither
axis"*; the strip carries no `data-slot` while both ghost siblings do.
(e) **GATE FLOOR BELOW CONTRACT** — o18 (`:697-743`) boots ONE color, takes `.first()`, and asserts
`TEXT_FLOOR = 4.5` against a module whose certification target is **5.75** — *"one notch below the
module's own 5.75 target"*.
*cureShape*: the cell report carries, per spec, `executed` · `assertions-reached` ·
`nodes-matched`; **an assertion that resolved zero nodes is a failure, not a pass**; every claim
carries its L-16 label; the two named oracles are reported with their class letter until X-W1's
cure lands.
*Sequencing*: (a)(b) are unblocked only by X-W6's door (R-1/R-11); (c) by X-W1's dialect cure;
(d) by X-W7.c's `data-slot`; (e) by X-W1's floor. Until then those legs read
`UNVERIFIABLE-HERE`, never green.
*Dissent*: PreviewRamp residue 11 keeps the counterfactual open — *"whether the o14 chip legs pass
once R-1's door and R-2's dialect are cured — unrunnable without editing the tree; belongs to
X-W1's repaired oracle on X-W6's repaired door."* W11 may not schedule that green.
⟨PreviewRamp.md R-2(a)(b)(c) + R-19 + residue 11 · ColorNutritionLabel.md CNL-5 ·
PaletteColorStrip.md PCS-22 + PCS-4⟩

**W11.11 · G7/G8 — forced-colors is UNMEASURABLE in BOTH declared cells** (cl.1 · MAJOR)
*Mechanism*: three records independently confirm a **static** WHCM/print roster-omission defect and
then quarantine the rendered result as needing an engine neither cell drives. A browser cell that
reports a forced-colors claim green would be inferring across cells — the exact I-20 violation G8
exists to forbid.
*Witness (born-RED, three seats)*: CNL residue 5 — *"WHCM engine required; the roster-omission limb
is static and CONFIRMED (CNL-25); **the tracked forced-colors capture is WebKit emulation and cannot
settle it**"*. PCS U-2 — *"WHCM + print rendered pixels of a collapsed palette card … **the tracked
forced-colors matrix contains zero card captures — the coverage hole itself is recorded in
challenge-D §0**"*. PreviewRamp R-6 confirms the static omission (`.preview-chip` absent from
`foundation.css:678-700`; print reuses the roster AND kills the ring via
`* { box-shadow: none !important }`) with the rendered result in residue 6.
*cureShape*: any forced-colors / print-register claim in either cell is recorded
`UNVERIFIABLE-IN-CELL` with the reason; the **static** roster-membership assertion (a grep over
`foundation.css`'s two roster arms) is the only forced-colors evidence either cell may carry.
*Sequencing*: applies to X.W11.d whole; it does not gate the close, it labels it.
*Dissent*: none — all three seats reached the same quarantine independently.
⟨ColorNutritionLabel.md CNL-25 + residue 5 · PaletteColorStrip.md PCS-2 + U-2 ·
PreviewRamp.md R-6 + residue 6⟩

**W11.12 · G11 — the vacuous-glob class has a MEASURED instance in-tree; and the 19-barrel
denominator is CC-078's, not a new count** (cl.1 · MAJOR)
*Mechanism*: G11's falsifier already declares *"a check that passes because its glob matched nothing
is a **failure**"*. The corpus supplies the live instance the falsifier was written against, and a
sibling record cites W11's own text as the ownership authority.
*Witness (born-RED)*: PCS-13 — *"every ban pattern keys on the `@components` alias killed at
W43/RF-15 (tsconfig.demo.json:32-34), so no specifier can match; the `demo/@/**` globs are
additionally dead"*; ShadowPalette SP-11 re-measures the same condition — *"`ls demo/@` fails,
verified"*, the governed tree is ABSENT, and `browser/index.ts:6-8`'s *"the G-DEMO-3b boundary
enforces it standing"* is a **false green**. CNL-15 — 19 one-line forwarding directories, banned
verbatim at `ARCHITECTURE.md:39`.
*locks*: PCS-13 ≡ ColorSpaceSelector **L-5(a)** (already folded with a born-RED
`eslint --print-config` command) — identity preserved, never re-booked; CNL-15 ≡ **CC-078**'s
verified 19 — G11's RED already carries that count and gains no new row. `ShadowPalette.md:45`
routes the glob cure to X-W8 *"per `W11.md:343-344`"* — W11 **asserts** the green, per its own
Format-And-Lint-Cadence clause.
*cureShape*: each G11 zero-occurrence check records the **pre-subtraction match count** from
X-W8's close beside today's zero; a check with no recorded pre-count is deleted, not counted (L-2).
*Sequencing*: after X-W8 lands; W11 produces nothing here.
*Dissent*: none.
⟨PaletteColorStrip.md PCS-13 + PCS-30 · ColorNutritionLabel.md CNL-15 · cross-edge
ShadowPalette.md SP-11 (:45)⟩

**W11.13 · G4/G9 — the exports/paths identity, asserted not cured; the demo self-tuple resolves
through the PACKED map** (cl.1 · MINOR-as-asserted, MAJOR at its home)
*Mechanism*: one fact, four rosters: `tsconfig.demo.json` declares 8 value.js path keys against
`package.json#exports`'s 7, with 3 phantom targets and `/css`+`/value` omitted while shipped, and
the block's "CLOSED 8-key set" comment false. The demo survives today only by **package self-name
resolution**; at release the demo self-tuple must resolve through the packed exports map, which is
the very surface the drift describes.
*Witness (born-RED, two seats)*: PreviewRamp R-9 — `ls`: `dist/index.d.ts` **ENOENT**;
`dist/subpaths/` = **7 pairs**, no `parsing`/`units`; exports = 7 keys / tsconfig paths = 8, *"3
phantom targets verified absent"*. PCS-30 — same census, *"the block's 'CLOSED 8-key set' comment is
false (7 keys, no `.`)"*, with the `--traceResolution` receipt showing type and runtime landing on
one artifact.
*locks*: **ONE identity — PreviewRamp R-9 ≡ GenerateControls #20 ≡ ColorSpaceSelector L-14 ≡
PCS-30 (W-HYGIENE)**; `X-W8.md:112` explicitly declines the cure ("L-22 ≡ W-HYGIENE, another
owner"). W11 books **no cure** and **no new id**.
*cureShape*: G9's demo self-tuple row records the resolution command's output (the packed map, not
the source tree) — W11's own §Scope already requires this; this row supplies the receipt shape and
the reason.
*Sequencing*: G4 → G9.
*Dissent*: PreviewRamp routes the packed-vs-checkout half to X-W1's `verify-packed-surface.mjs`;
that routing stands untouched.
⟨PreviewRamp.md R-9 (identity-fold) + §instruments · PaletteColorStrip.md PCS-30 + K-2⟩

**W11.14 · G12 — the glass BH relay packets, with the ATTRIBUTION CORRECTED before sending** (cl.2 ·
MAJOR)
*Mechanism*: G12 requires *"the glass §D letter(s) for every producer-owned residual"*. This input
set books three producer-owned residuals — and one of them arrived with a **wrong provenance cell
that the adjudicator killed**, so a packet sent on the uncorrected reading would file a defect in
the wrong repository.
*Witness (born-RED, measured)*: PreviewRamp R-10 — reka's SelectItem names the option via
`aria-labelledby: textId` only, and `aria-describedby` occurs **zero** times across reka SelectItem,
the glass select chunks, and ALL of `demo/` → **GLASS-OWNED, standing BH relay**. CNL-10 —
`--type-subheading: 1.272rem` FIXED against fluid body/small (crossover 2086px) and
`--type-heading` fixed too (K12 kills the token-swap cure) → producer-owned fixed/fluid scale seam.
CNL-19 — glass-ui's own `AlertDescription` hardcodes `text-sm` against the root's fluid
`--control-text`. **The correction (K15/R7)**: the reader's cell claiming glass `fonts.css` loads a
Fraunces italic is FALSE — *"fonts.css has 4 faces, ALL `font-style: normal`, and **zero
Fraunces**"*; the italic faces are the demo's **self-hosted** set at
`demo/color-picker/index.html:65/:73`. The faux-italic finding survives; its repo does not.
*cureShape*: `close/packets/glass-*.md` names R-10, CNL-10, CNL-19 by their original ids, carries
the corrected attribution verbatim, and is cited by a dated sent-row in `INBOX.md`.
*Sequencing*: sent before G9's glass tuple may read green (G12's own contradiction rule: *"an
unsent packet whose G9 tuple reads green fails both gates"*).
*Dissent*: none.
⟨PreviewRamp.md R-10 + addendum 4 · ColorNutritionLabel.md CNL-10 + CNL-19 + R7/K15⟩

**W11.15 · G10 — API journeys must assert ROUND-TRIP IDENTITY, not merely 2xx** (cl.1 · MAJOR)
*Mechanism*: G10 re-runs X-W3's X.A1..X.A5 and asserts *"no unresolved CRUD state and no
API-labelled state collision."* This corpus measures a state collision of exactly that name, plus a
silent data-loss round trip — neither of which a status-code journey can see.
*Witness (born-RED, source-certain)*: PCS-3 — `api/src/modules/palette/schema.ts:27-31` is
`{css, name?, position}` with **no `weight`**; *"zod strips unknown keys, no 400, no log"*, so a
saved extracted palette loses the population story the component exists to draw. PCS-11 — `position`
is REQUIRED with **two incompatible live encodings** (integer ordinal at 5 sites; normalized
fraction `i / max(1, len-1)` at `useExtractSession.ts:87`), both pass `z.number().finite()`, and
`hash.ts:11-13` rounds it into `computeContentHash` while `:46` keys `computeAtomSetHash` by it —
*"The same 5 colors saved from Extract vs Generate hash differently"*, against the server's own
documented equivalence relation (`:20-24`: *"position is the atom KEY … a re-order does not"*).
*cureShape*: one journey saves the same five colors from two producers and asserts the two
`computeAtomSetHash` values **and** that a save→reload round trip preserves every field the client
type declares; a divergence is an `API-labelled state collision` and reddens G10.
*Sequencing*: X-W3 owns the journey bytes; W11 re-runs. Both rows are **NO-WAVE-OWNER** for cure
(§1z) — the assertion is W11's, the fix is the boundary's. **PCS-3 lock (verbatim)**: *"Must not be
silently absorbed into PCS-3's cure: adding `weight` to the schema without ruling `position`
compounds the identity ambiguity."*
*Dissent*: the record grades the contract decision server-owned and unnamed in every X file list;
this fold asserts only that the release journey can SEE it.
⟨PaletteColorStrip.md PCS-3 · PCS-11 · superlative 5⟩

**W11.16 · G7 — the ROUTE denominator is the router's, and a known doc/route divergence rides it**
(cl.1 · MINOR)
*Mechanism*: G7 enumerates 14 declared routes + wildcard. `ARCHITECTURE.md:41-45` declares a route
that does not exist, so a coverage claim written from the architecture document would over-count.
*Witness*: CNL-20 — *"`/about` first-class, 'not Picker's permanent right-hand companion', 'no …
hidden view-schema alias'. Shipped: no `about` ViewId; `VIEW_MAP.picker.right = 'about'`"*;
`<h1` in `demo/` → **0** (CNL-12). Booked at **X-W5** and cross-booked to **AB-10 / rider A-1**.
*cureShape*: the cell's route list is generated from the router, and `FINAL.md` records the
divergence by id (CNL-20/AB-10) rather than silently omitting it.
*Sequencing*: none — labelling only.
*Dissent*: none.
⟨ColorNutritionLabel.md CNL-20 + CNL-12 + K3⟩

**W11.17 · G7 — coverage is ROUTE-level, never SURFACE-level** (cl.1 · MINOR)
*Mechanism*: a route-complete cell can still exercise none of a component's branches. The corpus
measured a shipped branch that has never painted.
*Witness*: PCS-16 — the entire `orientation="vertical"` branch has never executed (`layout="split"`
grep → **0**; `ExtractPane.vue:13` hard-codes `column`): *"Roughly half the rendering surface ships,
typechecks, and has never painted."* ≡ **PC-14** (identity preserved); X-W8's dead-surface gate is
the backstop.
*cureShape*: `FINAL.md` states the coverage claim as route×viewport, and defers surface coverage to
X-W8's dead-surface census by id.
*Sequencing*: none.
*Dissent*: PCS-16's 0px-rendering limb is UNPROVEN with a live counter-mechanism recorded (U-1) —
carried, not resolved.
⟨PaletteColorStrip.md PCS-16 + U-1⟩

**W11.18 · L-16/I-20 cell-label header — adopt the registry's own disclosure idiom** (cl.1 · MINOR)
*Mechanism*: every record in this set opens with an explicit environment disclosure and closes its
live residue with a cell-identity rule; W11's two evidence cells should carry the same header so a
reader can tell what was measured from what was entailed.
*locks (verbatim)*: PreviewRamp §UNPROVEN header — *"cells are chromium unless noted (I-20:
chromium ≠ webkit ≠ safari-app, nothing inferred across cells)"* · PreviewRamp §Environment
disclosure — *"no dev server touched; every ruling in this record is decidable from source +
shipped dist. The r1 seats' live cells … are adopted as corroboration only."*
*cureShape*: `close/evidence/browser/**` and `close/evidence/safari-real/**` each open with a
disclosure block: binary + version, origin, API state, and the corroboration-vs-proof posture.
*Sequencing*: X.W11.d.
*Dissent*: none.
⟨PreviewRamp.md :10 + §UNPROVEN header · ColorNutritionLabel.md :11 · PaletteColorStrip.md :8⟩

**W11.19 · D-19 — every cited line anchor re-resolves before it is cited** (cl.2 · MINOR)
*Mechanism*: the three records pin **three different HEADs** (`3487d73e` / `c654824e`+`2e334231`+
`e8393685` / `3ecf0267`) and each states its anchors are dated. W11 opens after ten waves of edits.
*locks (verbatim)*: PreviewRamp addendum 6 — *"re-resolve all line anchors before executing any cure
(D-19)"* · PaletteColorStrip §roster — *"Line anchors are HEAD `3ecf0267`; re-resolve before any cure
(D-19)."*
*cureShape*: any line anchor reproduced in a W11 close document is re-resolved at the release commit
and recorded with **both** the source record's pin and the release SHA; an unresolvable anchor is
cited by symbol, never by line.
*Sequencing*: binds X.W11.a and X.W11.e.
*Dissent*: none.
⟨PreviewRamp.md addendum 6 · PaletteColorStrip.md :32 · ColorNutritionLabel.md :4⟩

**W11.20 · PROBE PROVENANCE — untracked and `/tmp`-authored probes are UNMEASURED** (cl.2 · MINOR)
*Mechanism*: G8's own RED baseline is this defect measured (4 tracked of 31 on disk; *"27 witnesses
are uncommitted and therefore do not exist (L-7)"*). The corpus reproduces it inside the
adjudications themselves, and one seat replaced the lost probe with a pasted, re-runnable command
— the shape W11 should require.
*Witness*: PreviewRamp residue 10 — *"D's probe scripts were written to `/tmp` and are **NOT
banked** (unlike C's) — partially replaced by this seat's dist command, pasted in the header and
re-runnable by any reader."* PCS U-7 — the pasted live-DOM probe outputs *"were not re-run (probe
parsimony)"* and are carried as residue, not proof.
*cureShape*: a W11 evidence cell may cite only **tracked** artifacts (force-added per L-7); a cited
probe is pasted as a command with its output, never referenced as a script path. A registry probe
that exists only in `/tmp` is recorded `UNMEASURED`.
*Sequencing*: X.W11.b/d/e.
*Dissent*: none — this is L-7/L-19 restated with in-corpus instances.
⟨PreviewRamp.md residue 10 · PaletteColorStrip.md U-7 · ColorNutritionLabel.md residue 1⟩

**W11.21 · The UNPROVEN-NEEDS-LIVE residue is the cells' INPUT LIST, never a source of green**
(cl.1 · MINOR)
*Mechanism*: all three records end with a quarantined residue explicitly labelled as the visual-audit
lane's inputs. Those rows are entailed-or-corroborated, never proven; a cell that "confirms" one by
inference has manufactured evidence.
*Witness*: PreviewRamp §UNPROVEN preamble — *"Every row below is quarantined … **None is
load-bearing for any verdict above.** These are the component's visual-audit lane inputs"* (11 rows);
ColorNutritionLabel residue (9 rows, *"Highest-value probes in this record"* named at row 1);
PaletteColorStrip residue U-1..U-8 (*"visual-audit inputs — task #4 feed"*).
*cureShape*: `close/evidence/browser/**` may list residue rows as **inputs attempted**, each with a
verdict of `MEASURED <value>` or `UNMEASURED`; a residue row may never be closed by entailment.
*Sequencing*: X.W11.d; feeds the standing visual-audit task, not this close.
*Dissent*: PreviewRamp DISSENT 2 keeps a severity question open pending exactly these measurements
(*"If the visual audit lands the ratios at the predicted magnitudes AND the chips become reachable
(R-1 cured), the severity question reopens with evidence"*) — preserved verbatim; W11 does not
resolve it and does not stamp over it.
⟨PreviewRamp.md §UNPROVEN 1-11 + DISSENT 2 · ColorNutritionLabel.md §UNPROVEN 1-9 ·
PaletteColorStrip.md U-1..U-8⟩

### 1z. Id-keyed census — all 85 source rows, no silent drops

`FOLD` = booked at a W11 row above · `BOUND` = carries a NO-WAVE-OWNER limb, enters
`BOUNDARY-CARRY.md` (W11.2) · `OUT →` = routed elsewhere, handed to that wave's fold seat, **not**
booked here. Severity is the record's own.

**PreviewRamp — 27 rows** (**17 BOUND ids** = clause 5's sixteen **+ R-5** per §0a n.1; **ADJ-1's
roster arm rides R-6's entry**, per the clause's own `R-6(+ADJ-1)` notation, and is therefore not a
seventeenth — R-5 is)

| id | sev | disposition |
|---|---|---|
| R-1 | BLOCKER | OUT → X-W6 (≡ GenerateControls B1 census) · precondition for W11.10(a)(b) |
| R-2 | BLOCKER | **FOLD W11.10** (a/b/c) · OUT → X-W1 (oracle) + X-W6 (door); "whole-suite green stamp is X-W11's" |
| R-3 | MAJOR | OUT → X-W6 |
| R-4 | MAJOR | **BOUND** (chip sizing recipe) |
| R-5 | MAJOR | OUT → X-W6 (host lane) + **BOUND** (chip sizing; omitted from addendum 5 — §0a n.1) |
| R-6 | MAJOR | **BOUND** + **FOLD W11.4** (ADJ-1 sequencing binds it) |
| R-7 | MAJOR | **BOUND** (the one `chip.css` recipe) |
| R-8 | MAJOR | **BOUND** (sampler duality; X-W6-adjacent) |
| R-9 | MAJOR | **FOLD W11.13** — identity-fold, no new id (§0a n.5) · OUT → X-W1 |
| R-10 | MAJOR | **FOLD W11.14** (GLASS-OWNED BH relay) · consumer duty rides R-1 at X-W6 |
| R-11 | MAJOR | OUT → X-W6 |
| R-12 | MINOR | **BOUND** (file half) + OUT → X-W10 (design ruling) |
| R-13 | MINOR | **BOUND** (folds into R-7) |
| R-14 | MINOR | **BOUND** |
| R-15 | MINOR | **BOUND** |
| R-16 | MINOR | OUT → X-W6 (host) + **BOUND** (chip) |
| R-17 | MINOR | OUT → X-W6 |
| R-18 | MINOR | OUT → X-W6 (partial) + **BOUND** (residue) |
| R-19 | MINOR | **FOLD W11.10(c)** (≡ R-2(b), one defect) · OUT → X-W1 + **BOUND** (producer half) |
| R-20 | MINOR | OUT → X-W8 (feeds X.W8.a census) |
| R-21 | MINOR | **BOUND** |
| R-22 | MINOR | OUT → X-W6 (decision) + **BOUND** (index map) |
| R-23 | MINOR | OUT → X-W10 |
| R-24 | INFO | **BOUND** |
| R-25 | INFO | **BOUND** (REFUTED-AS-FILED; cure RETIRED — carried as the retirement record) |
| R-26 | INFO | **BOUND** (folds into R-7) |
| ADJ-1 | MAJOR (process) | **FOLD W11.4** + BOUND-**within R-6's entry** (roster arm; not a separate census id) + dated note owed to GenerateControls BANKS |

**ColorNutritionLabel — 27 rows** (11 BOUND, +1 conditional per §0a n.6)

| id | sev | disposition |
|---|---|---|
| CNL-1 | BLOCKER | OUT → X-W6.f (≡ CSS-L-1 / AB-1) |
| CNL-2 | BLOCKER | OUT → X-W9 + X-W1 · **FOLD W11.6** (render-cell separation) + **W11.7** · **CONDITIONAL BOUND** |
| CNL-3 | BLOCKER | **FOLD W11.6** (the 28-grey seed matrix) · OUT → X-W9 + X-W1 |
| CNL-4 | BLOCKER | OUT → X-W9 (publish the contrast primitive) + **BOUND** (AB-17 / rider A-1) |
| CNL-5 | BLOCKER | **FOLD W11.9 + W11.10(e)** · OUT → X-W1 (CC-030/CC-031) |
| CNL-6 | MAJOR | **BOUND** (AB-17) |
| CNL-7 | MAJOR | **BOUND** (AB-17) + OUT → X-W6.f (the `notes` data decision) |
| CNL-8 | MAJOR | **BOUND** (AB-17 / PR-07) |
| CNL-9 | MAJOR | **BOUND** (AB-17 / A-3) |
| CNL-10 | MAJOR | **FOLD W11.14** (glass BH relay) · OUT → X-W10.d |
| CNL-11 | MAJOR | **BOUND** (bidi bundle with AB-36) |
| CNL-12 | MAJOR | OUT → X-W5 · cited at W11.16 |
| CNL-13 | MAJOR | OUT → X-W9 + X-W6.f |
| CNL-14 | MAJOR | OUT → X-W9 |
| CNL-15 | MAJOR | OUT → X-W8 · **FOLD W11.12** as ≡ **CC-078**, no new count |
| CNL-16 | MAJOR | **BOUND** (MT-CSS-2 phantom owner, AB-26 precedent) |
| CNL-17 | MAJOR | **BOUND** ("the resting-rung composite referent") + OUT → X-W1 (the 4.5-vs-5.75 floor → W11.10(e)) |
| CNL-18 | MAJOR | **FOLD W11.7** (white→HSL) · OUT → X-W9 |
| CNL-19 | MAJOR | **BOUND** (AB-17) + **FOLD W11.14** (glass relay, attribution corrected) |
| CNL-20 | MAJOR | OUT → X-W5 (≡ AB-10 / rider A-1) · **FOLD W11.16** |
| CNL-21 | MINOR | OUT → X-W6.f |
| CNL-22 | MINOR | OUT → X-W6.f |
| CNL-23 | MINOR | OUT → X-W6.f (≡ AB-2 for the guide half) |
| CNL-24 | MINOR | **BOUND** (recomposition rows, AB-17) + OUT → X-W6.g (rhythm) |
| CNL-25 | MINOR | **BOUND** (a11y/bidi bundle) + OUT → X-W10.d (type-role + roster, K-6 cited) |
| CNL-26 | MINOR | OUT → X-W5 (cross-ref CC-081) |
| CNL-27 | INFO | OUT → rides X-W6.f (recorded so nothing re-derives it) |

**PaletteColorStrip — 31 rows** (6 BOUND, matching the record's own scoreboard)

| id | sev | disposition |
|---|---|---|
| PCS-1 | MAJOR | **FOLD W11.3** (NAMED GAP) · OUT → X-W7.c |
| PCS-2 | BLOCKER-class FOLD → **PC-7** | OUT → X-W7.c · **FOLD W11.4** (rider i) + W11.11 (rider ii, print box-shadow) |
| PCS-3 | MAJOR | **BOUND** (server schema) · **FOLD W11.15** |
| PCS-4 | MAJOR | **FOLD W11.9** · OUT → X-W7.c |
| PCS-5 | MAJOR | OUT → X-W7.c (coupled to PCS-2's print rider) |
| PCS-6 | MAJOR | OUT → X-W7.c·G11 (foreign gate label — §0a n.2) |
| PCS-7 | MAJOR | OUT → X-W7.c |
| PCS-8 | MAJOR | OUT → X-W7 export unit (W7.md:42 + :92-96 + :194-200) |
| PCS-9 | MAJOR | **BOUND** (area seam; X-W8 candidate for lint re-pointing only) |
| PCS-10 | MAJOR | OUT → X-W7.c + **BOUND** (chip-family unification residue) |
| PCS-11 | MAJOR | **BOUND** (server `position` contract) · **FOLD W11.15** |
| PCS-12 | MINOR | OUT → X-W7.c |
| PCS-13 | MINOR | **FOLD W11.12** (≡ ColorSpaceSelector L-5(a)) · OUT → X-W8 |
| PCS-14 | MINOR | OUT → X-W7.c |
| PCS-15 | MINOR | OUT → X-W7.c |
| PCS-16 | MINOR | **FOLD W11.17** (≡ PC-14) · OUT → X-W7.c |
| PCS-17 | MINOR | OUT → X-W7.c |
| PCS-18 | MINOR | OUT → X-W7.c |
| PCS-19 | MINOR | **FOLD W11.3** (NAMED GAP) · Generate limb ≡ GenerateControls row 6 BANKED/W25 · OUT → X-W7.c |
| PCS-20 | MINOR | OUT → X-W7.c + X-W7.f |
| PCS-21 | MINOR | OUT → X-W7.c |
| PCS-22 | MINOR | **FOLD W11.10(d)** · OUT → X-W7.c |
| PCS-23 | MINOR | OUT → X-W7.c (the one row an X mechanism already names, W7.md:210) |
| PCS-24 | INFO | OUT → X-W7.c |
| PCS-25 | INFO | OUT → X-W7.c |
| PCS-26 | INFO | OUT → X-W7.c |
| PCS-27 | INFO | OUT → X-W7.c under X-W10 law |
| PCS-28 | INFO | OUT → X-W7.c |
| PCS-29 | INFO | OUT → X-W7.c |
| PCS-30 | INFO | **BOUND**, identity preserved (≡ ColorSpaceSelector L-14 / W-HYGIENE; X-W8.md:112 declines) · **FOLD W11.13** |
| PCS-31 | INFO | **BOUND** (rides the U-tranche RTL logical-property ledger) |

**Boundary total: 17 + 11 + 6 = 34** (+1 conditional, CNL-2). **OUT-OF-WAVE: 51.** Every id above
appears exactly once; no id is renamed.

---

## 2. Gates

Born-RED means the failing probe is **already measured** by a named seat against a named tree — no
gate below rests on a script this seat authored (L-19). `MEASURE-AT-OPEN` means the cure lands in a
prior wave and no honest baseline exists until it does.

### 2a. Sharpenings of EXISTING W11 gates (12)

| # | gate | sharpening | witness (measured, banked) | state |
|---|---|---|---|---|
| S1 | **G1** | terminal words gain `DEGRADED-TO-BOUNDARY`; the registry accounting rides beside §4, never inside the 117 | PCS-1/PCS-19 NAMED GAPs; PreviewRamp addendum 5's 16-of-17 enumeration | BORN-RED (0 rows carry an X-close stamp today, per G1's own RED) |
| S2 | **G2** | denominator ≠ coverage; an `UNCOVERED` list beside the 348 | PCS-4 (`ColorStrip\|WEIGHT_FLOOR\|segmentPct` over test/ e2e/ demo/test/ → exit 1); CNL-5 (1 hit, a comment) | BORN-RED |
| S3 | **G3** | per-export "documented input" includes achromatic / white / none-hue | hsl grey sweep `[[255, color_non_finite]]`; `rgb(255,255,254)→hsl` s=1.000000000000085; 256/256 greys `"none"` in lch+oklch | BORN-RED |
| S4 | **G4** | the exports/paths drift is asserted, not cured; identity stays at W-HYGIENE | `dist/index.d.ts` ENOENT; `dist/subpaths/` = 7 pairs; exports 7 vs paths 8, 3 phantom | BORN-RED (G4 already RED at `v4.0.0`) |
| S5 | **G5** | seed dimension `?color=<seed>` over 28 greys ∪ {black}, both modes; boot cell ≠ render cell | CNL-3's measured 28-value set; unguarded `useColorPipeline.ts:75-76` at `App.vue:245`, outside the ErrorBoundary | BORN-RED (G5 has no witness at all today) |
| S6 | **G7** | five vacuous-green classes; `assertions-reached` and `nodes-matched` asserted separately from exit code | o14 tautological leg; unreachable `getByRole`; `parseOklchTriples` 0-vs-1; PCS-22 selector collision; o18 `TEXT_FLOOR=4.5` vs 5.75 | BORN-RED (`toHaveScreenshot\|toMatchSnapshot` → 0; ci.yml has zero playwright) |
| S7 | **G7+G8** | forced-colors/print claims are `UNVERIFIABLE-IN-CELL` in both cells; only the static roster grep counts | CNL residue 5 (WebKit emulation cannot settle it); PCS U-2 (zero card captures in the tracked matrix); PreviewRamp R-6 | BORN-RED |
| S8 | **G9** | tuples carry integrity/shasum, not only a resolved version | `css.d.ts` 10910 B (self-install @4.0.0) vs 12490 B (checkout 4.0.0) | BORN-RED |
| S9 | **G10** | journeys assert round-trip identity + cross-producer hash equality | schema `{css,name?,position}` drops `weight` silently; `position` dual encoding into both server hashes | MEASURE-AT-OPEN (X-W3 owns the RED baseline) |
| S10 | **G11** | each zero-occurrence check records X-W8's pre-subtraction match count | PCS-13 / SP-11 dead `@components` globs; `ls demo/@` fails; CNL-15 ≡ CC-078's 19 barrels | BORN-RED |
| S11 | **G12** | glass packets carry the corrected attribution; unsent ⇒ G9 tuple cannot be green | R-10 (`aria-describedby` = 0 across reka+glass+demo); CNL-10; CNL-19 + K15 (glass fonts.css: 4 faces, all normal, zero Fraunces) | MEASURE-AT-OPEN |
| S12 | **R-A** | the stamp act emits a registry-row → landed-evidence map per wave; unmapped ⇒ IMPLEMENTED only | three verdict sentences (§0a n.3 a/b/c) | BORN-RED |

### 2b. Genuinely NEW gates (5)

| # | gate | condition | born-RED witness | falsifier |
|---|---|---|---|---|
| N1 | **G13 — cure-collision sweep** | before commit #8, every "dead — delete" object named in any adjudicated record is grepped in the post-cut tree and matched to exactly ONE landed fate | `[data-color-surface]` producers → **0** while three records order three fates (GenerateControls R-C delete · PreviewRamp R-6 mint · PCS-2/PC-7 hook) | two waves stamped VERIFIED whose landed bytes contradict on one selector; the sweep names both record ids |
| N2 | **G14 — ADJUDICATED→landed sweep** | per stamped wave, every registry row naming it maps to `landed <commit>` \| `NOT-CARRIED` \| `DEGRADED-TO-BOUNDARY` | 0 registry rows carry an X-close stamp today (G1's RED); three records forbid self-stamping | a wave stamped VERIFIED with an unmapped registry row that names it |
| N3 | **G15 — citation currency** | a cited registry record is read with its dated correction blocks; the citation records which were read | PaletteCard Superlative 1 praises the line PCS-1 disproves; CNL K13's inherited `5.625` against the lines proving `5.75` | a `FINAL.md` claim resting on a cell a dated correction has struck |
| N4 | **G16 — boundary carry census** | `BOUNDARY-CARRY.md` is id-keyed, **34 rows** from this seat's records (+1 conditional), each with original id · record · severity · pinned cure text · destination | no such file exists; three records enumerate their carries and forbid silent drops | a NO-WAVE-OWNER limb present in a record and absent from the census; a census row under a renamed id |
| N5 | **G17 — probe provenance** | evidence cites tracked artifacts and pasted commands only; `/tmp`-authored or untracked probes read `UNMEASURED` | G8's own RED (4 tracked of 31); PreviewRamp residue 10 (`/tmp`, not banked); PCS U-7 (not re-run) | a cell green on a probe with no tracked artifact and no pasted output |

---

## 3. BoundsDelta

Files X-W11's §File Bounds must ADD. **No product source is added; W11 remains a documents-and-
receipts wave, and the `Do NOT touch` list is untouched.**

| # | path | access | why (row) |
|---|---|---|---|
| 1 | `docs/tranches/X/close/BOUNDARY-CARRY.md` | **create** | W11.2 / G16 — the 34-row carry census; not a §4 row, not in the 117 |
| 2 | `docs/tranches/X/close/evidence/registry/*.json` | **create** | W11.4 / W11.1 / W11.5 — G13 collision sweep, G14 adjudicated→landed map, G15 currency receipts |
| 3 | `docs/tranches/X/close/evidence/deploy/boot-smoke-seeds.json` | **create** | W11.6 / G5 — the seed-matrix cell, held separate from the render cell |
| 4 | `docs/tranches/V/megatranche/registry/adjudicated/**.md` | **read, no write** | W11.1/W11.2/W11.5 — the close must consult them; they are immutable (E-1/E-3). The dated spec neither grants nor forbids this; the ambiguity closes here |
| 5 | `docs/tranches/X/refinement/X-W11-FOLD.md` | **read at open** | the reading rule (§0) — spec THEN addendum |
| 6 | `e2e/smoke/oracles/o14-preview-truth.spec.ts` | **execute, no write** | W11.10(a)(b)(c) — named so its verdict carries its class letter; X-W1 owns the bytes |
| 7 | `e2e/smoke/oracles/o18-contrast-census.spec.ts` | **execute, no write** | W11.10(e) — the 4.5-vs-5.75 floor is labelled, not silently green; X-W1 owns the bytes |

**Disjointness holds**: entries 1-3 are `create` under unit-owned subdirectories (X.W11.a owns 1;
X.W11.b/d own 2-3 by sub-gate); entries 4-7 are read/execute and take no writer. No sibling X wave
writes under `docs/tranches/X/close/**`.

---

## 4. CrossEdges

| edge | content | direction |
|---|---|---|
| **X-W1** | the oracle slate: o14 dialect cure (one encoding through the app's own parser), the reachability precondition, the born-RED boot test over CNL-3's seed matrix, the two-click render cell, the o18 floor (4.5→5.75), CI playwright. W11 executes, never authors | W11 ← W1 |
| **X-W6** | R-1/R-11 (the Mix door: enclosing geometric seat; the `<Plus>` glyph the producer discards), R-3, R-17, R-22 — every G7 leg in W11.10(a)(b) is blocked on this | W11 ← W6 |
| **X-W7.c** | 22 PCS rows die in one build (PaletteSpecimen); **two NAMED GAPs must be adopted or degraded before W11 opens** (W11.3); the `[data-color-surface]` single ruling (W11.4); `data-slot` before any oracle (W11.10(d)) | W11 ← W7 |
| **X-W8** | G11's pre-subtraction denominators; the dead `@components` globs (PCS-13 ≡ CSS L-5(a)); the 19 barrels (CNL-15 ≡ CC-078); the dead-surface backstop for PCS-16. `ShadowPalette.md:45` cites `W11.md:343-344` as the ownership authority — the citation is correct | W11 ← W8 |
| **X-W9** | the dated cut + `package.json#version` literal (W11's own precondition), the cured `verify-packed-surface.mjs` (W11.7's input rows), the `"none"` protocol made unrepresentable-to-forget, the published contrast primitive | W11 ← W9 |
| **X-W10** | canonical design law for R-12's design ruling, R-23's species/registers, CNL-10/CNL-25's role matrix, PCS-27's motion absence | W11 ← W10 |
| **X-W5** | route identity/H1/title (CNL-12), the `/about` mount root (CNL-20 ≡ AB-10), deep-link `?space=` (CNL-26 ≡ CC-081) — W11.16's route denominator depends on it | W11 ← W5 |
| **X-W3** | X.A1..X.A5 journeys gain the round-trip identity + cross-producer hash assertions (W11.15); W11 re-runs against the released build | W11 ← W3 |
| **GLASS-UI (BH inbox)** | three producer-owned residuals relayed with corrected attribution: R-10 (SelectItem description lane reaches no AT), CNL-10 (fixed/fluid scale seam), CNL-19 (`AlertDescription` `text-sm` vs fluid `--control-text`). Standing owner edict: every glass-level defect relays | W11 → glass |
| **keyframes.js** | unchanged by this input set: the RD-11 exact-pin packet (`"4.0.0"`) stands as W11's own G9/G12 rows state it | W11 → keyframes |
| **Formation boundary** | 34 NO-WAVE-OWNER limbs (+1 conditional): the chip family (R-4/R-6/R-7/R-8/R-13/R-14/R-15/R-21/R-24/R-25/R-26 + halves of R-5/R-12/R-16/R-18/R-19/R-22), the About recomposition + phantom-owner families (CNL-4/6/7/8/9/11/16/17/19/24/25), the three server-contract rows (PCS-3/PCS-9/PCS-11) plus PCS-10 residue, PCS-30 (W-HYGIENE identity), PCS-31 (RTL ledger) | W11 → boundary |
| **Sibling registry records (read-only)** | PaletteCard (PC-7/PC-14/PC-29 identities + the corrected Superlative 1), GenerateControls (R-C/R-D/B1/row 6/#20), ColorSpaceSelector (L-5(a)/L-14/DISSENT-1 precedent), AboutPane (AB-1/2/10/17/26/36 + riders A-1/A-3), ShadowPalette (SP-11), shell-dock-dockstatuslamp (the stamping verdict) | W11 ← registry |

---

## 5. Closing — status verbs

**AUDITED** — YES (the three assigned apotheoses + the whole-corpus `X-W11` grep, §0a n.3).
**SPECIFIED** — the dated `waves/W11.md` (2026-08-03) stands byte-untouched; this addendum is the
later adjudicated word on its predicates, gates and bounds.
**IMPLEMENTED** — NO. **VERIFIED** — NO.
**This fold stamps nothing.** It is the accounting instrument the stamping wave was missing:
adjudication is not verification, a routed row without a mechanism sentence is a degraded row, and
a carry with no census is a drop. Execution stays gated; no product source is opened here.

— end of fold. The three source apotheoses, their challenge corpora and their re-reader returns
remain immutable beside this file per E-1/E-3.
