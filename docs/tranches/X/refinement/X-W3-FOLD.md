SERVED MODEL ID (fold author, line-1 declaration): claude-opus-5[1m]

# X-W3 REFINEMENT FOLD — the value.js-corpus addendum to `docs/tranches/X/waves/W3.md`

## 0. Header — provenance, posture, the reading rule

**Provenance.** The fold seat read, whole, the thirteen adjudicated apotheoses the index routes to
this wave — `AdminAuditPanel · AdminFlaggedPanel · AdminListSkeleton · AdminTagsPanel ·
ErrorBoundary · PaletteCardMeta · PaletteSlugBar · TagEditPopover · VersionHistoryDrawer ·
shell-dock-dockviewselect · shell-paneslot · wb-generate-pane ·
wb-gradient-easingauthoringstage` (all at `docs/tranches/V/megatranche/registry/adjudicated/`) —
and additionally ran the routing sweep the task mandates over the **whole** value.js corpus
(88 components + `parser-band` · `library-band` · `chassis-fitness` · `layout-gestalt`, i.e. every
adjudicated file that is not `kf-*`/`fr-*`/`pt-*`):

⟨sweep, re-runnable⟩ `grep -rln 'X-W3' adjudicated/ | grep -v '^kf-\|^fr-\|^pt-'` → **exactly the
thirteen named files**, no more; `grep -rln 'X\.W3'` → `AdminAuditPanel · AdminFlaggedPanel ·
AdminTagsPanel` (+ `fr-App.md`, out of corpus). **No X-W3 routing from any other value.js record
was missed by the index.** Packets proposed for this wave: **none** (`[]`).

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0.md..W11.md` and
`CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is this wave's **sole write**. Where a
row below strikes or sharpens a phrase in W3.md, the strike is recorded here and W3.md's bytes
stand untouched.

**The reading rule.** At execution, X-W3 reads **`waves/W3.md` PLUS this addendum**, in that order;
where the two disagree the addendum's clause governs. Nothing here re-stages or re-weights W3's
§6 hard gate (OC-2 staging stands); this addendum sharpens predicates, adds corpus rows and
riders, records bounds pressure, and books exclusions. **Status stays `planned`. Nothing here
opens product source. Dispositions are ADJUDICATED, not VERIFIED — this file stamps no wave.**

**Fold classes** (kept from the X·P exemplar): **cl.1** = spec sharpening / law / correction ·
**cl.2** = cure-shape lock / binding contract clause · **cl.3** = MEASURE-AT-OPEN row ·
**cl.4** = RIDER RECORDED, NOT ADOPTED (books the row so it cannot be silently dropped, without
claiming it for this wave).

**Id-keyed census law.** Every row below carries a fold id **`F-n`** AND the banked id(s) of its
source row(s). **Banked ids are original for life** (anti-rename): `AAP-3`, `ATP-3`, `AF-1`,
`DVS-5`, `EB-16`, `PS-8`, `VHD-25`, `A-3`, `GEN-33` etc. are quoted, never renamed, never
re-minted. Where several banked ids name ONE identity they share ONE fold row and every id is
listed; the row is counted **once**.

**Arithmetic (this document's own bytes).** **24 fold rows** (`F-1..F-24`), contiguously numbered,
one provenance ⟨…⟩ line each — of which **19 are cl.1/cl.2 build-or-sharpen rows** and **5 are
cl.4 riders recorded-not-adopted** (`F-19 · F-20 · F-21 · F-22 · F-23`). **17 gates touched**
(§Gates): **10 existing W3 §6 gate IDs sharpened** — `G-3 · G-6 · G-7 · G-9 · G-11 · G-17 · G-18 ·
G-19 · G-20 · G-22`, across **11 table entries** (`G-18` is sharpened three times and counted
once) — **plus 7 new born-RED candidates** (`N-1..N-7`). **8 bounds rows** (§BoundsDelta), of which **3 are record-backed as X-W3 riders by name** and
**5 are contested/candidate** with the dissent printed. Rows dedupe to **21 distinct banked
identities** across **31 banked ids** (the AdminGate class alone carries four).

### 0a. Corrections, never silent

1. **Fold ids are `F-n`, NOT `X-W3.n`.** W3.md already spends `X.W3.1..X.W3.6` on agent units, and
   the ErrorBoundary record cites "**X-W3.16**" meaning W3.md **§3 scope item 16** (the
   `usePaneRouter` terminal fallback), not a sixteenth unit. Numbering fold rows `X-W3.n` would
   collide with both namespaces. `F-n` is this document's own key; source ids remain authoritative.
2. **`AAP-3`'s and `ATP-3`'s "X-W3.6" is the AGENT UNIT** `X.W3.6` (route closure + admin policy
   branch), read at `W3.md:267-287`. Recorded so no executor reads it as a scope item.
3. **The AdminGate class is FOUR members, not three.** The records name `AdminUsersPanel L-5` as
   the class origin, then `AAP-3`, then `ATP-3` ("third member … one identity, the grade travels"),
   then `AF-1` ("ONE identity — the ruled AdminGate (MT-AU1), cross-booked, no new booking").
   `AdminUsersPanel.md` is **outside** the thirteen index-routed files but **inside** this wave's
   corpus, and its id is carried here as provenance only — its own record is not re-read into this
   fold beyond the three quoting records' own verbatim cross-books.
4. **Two records DECLINE X-W3 by name** and are booked as declinations, not omissions:
   `AAP-22` ("X-W3's scope does not carry it") and the `AdminListSkeleton` kill-5 residual
   ("X-W3 carries no deadline row — my grep"). Silently dropping a decline is the same defect as
   dropping a routing (M-25); both are in §CrossEdges §D.
5. **`W3.md §Dispositions D-8 ("Glass bank rows: None") HOLDS under this fold.** Zero of the 24
   rows is glass-coupled. The GLASS-RELAY rows in these records (`ATP-23` orphaned
   `glass-chip.css`, `VHD-5/18/19/24/39` dialog placement, `EAS-10`'s d2 ask, `A-4`'s skeleton ink
   seam, `GEN-8/28`) all route elsewhere and are **not** folded here.

---

## §Rows

Legend — **cl** = fold class · **sev** = severity as banked · **route** = the record's own verbatim
disposition. Line anchors are the records' own, at the records' own HEADs; **re-resolve before any
cure (D-19)**.

---

**F-1 · `AAP-3` + `ATP-3` + `AF-1` ≡ `AdminUsersPanel L-5` ≡ MT-AU1 — the AdminGate: authority
costumed as an empty collection** (cl.2 · **BLOCKER** ×3, one identity)

*Mechanism.* `if (!token) return;` exits **before** `loading`/`loadError` are touched, so the
branch chain lands on the TRUE-EMPTY plate — the register the codebase reserves for a
verified-empty collection — and an unauthenticated visitor is told the ledger is clear
(`useAdminAudit.ts:49-50` → `· ledger clear · / No audit entries found.`), the moderation queue is
empty (`useAdminFlagged.ts:60-61` → the triple `(false, null, [])` → `:37-41`), and "No tags yet."
is a fact (`useAdminTags.ts:49-51` → `:52/:69/:82`). Class census (three seats, concurrent):
**21 `if (!token)` sites across 5 composables** — flagged 3 · tags 2 · colorNameQueue 5 ·
adminUsers 10 · audit 1. `AF-1` adds the film: "on film in all four shipped captures (dock `Login`
+ `0 flagged` + `· NOTHING FLAGGED ·` simultaneously; REPORT.json `failedRequests: []`)".
`ATP-3` adds a limb no other member carries: **the enabled create button is dead** — `:42`'s
`:disabled` never tests the token while `createTagAction`'s `:67` guard makes the click a silent
no-op.

*cureShape + locks (verbatim).* `ATP-3`: "**BUILD → X-W3.6** (fail-closed `router.beforeEach` on
`meta.admin` + NotFoundPane, W3.md:87/:267; **the per-method guards DELETE behind it**)."
`AAP-3`: "**BUILD → X-W3.6** (fail-closed `to.meta.admin` router guard + `NotFoundPane`; the 21
per-method guards DELETE behind that gate, **per the MT-AU1 G-4b mechanism the X formation
absorbed**)."

*Sequencing.* Guard half = X-W3 (`G-18` P0, unit `X.W3.6`). `AF-1` splits the remainder:
"pane-side unauthorized state + deletion of the 21 `if (!token) return` sites = **X-W7**."

*Dissent (preserved, not ruled away).* (a) **The deletion seam is CONTESTED**: `AAP-3`/`ATP-3`
put the 21 deletions **behind the X-W3.6 gate**; `AF-1` puts them in **X-W7**. Neither wave's gate
may go green over the other's edit; see §CrossEdges §A. (b) **Severity cap friction, preserved
verbatim** (`AF-1` DISSENT #4): "the AdminNamesPanel cross-book capped this class ('capability
inert, no data on the wire' — the server 401s regardless); the cap is honored at the identity
while **X-W3's own gate keeps it P0**." (c) `ATP-3` **OVERRULED** both its readers' settled MAJOR
and RAISED to BLOCKER on class-identity law ("one identity ⇒ one grade; the grade travels with the
class").
⟨AdminAuditPanel.md · AAP-3⟩ ⟨AdminTagsPanel.md · ATP-3 + Challenges row 1⟩ ⟨AdminFlaggedPanel.md ·
AF-1 + DISSENT 4⟩

---

**F-2 · `ATP-34` + `DVS-5` — `meta: { admin: true }` is dead declaration; the router is a URL bus**
(cl.1 · MINOR + MAJOR, one identity)

*Mechanism.* Zero guards read the flag. `ATP-34`: "`meta: { admin: true }` is dead declaration:
zero guards read it (grep re-run by prior seats; router = URL bus). **Pairs with ATP-3 — the
ungated route is why the unauthenticated render is reachable.**" `DVS-5` sharpens with a fact
W3's `G-18` baseline does **not** name: the router file "registers only `installDocumentTitle`
(my read — **the file's own comment even calls itself 'the canonical navigation-guard home'**)",
and `meta.admin` sits on **exactly 5** routes while the dock hand-enumerates 7+7 and the schema
puts `atmosphere`/`blob` on the primary 40° fan — so "**this component's `v-if` is the de-facto
access control**."

*cureShape + lock.* `ATP-34`: "**FOLD → X-W3.6** (the fail-closed guard makes the flag live;
W3.md:87)." `DVS-5`: "**FOLD → CC-037 (X-W3 — the K-25 route-closure P0)** … Correctly routed to
the router owner, **not patched here**."

*Sequencing lock (anti-smuggle).* `DVS-4`'s cure — one `group`/`audience` field on `viewSchema.ts`
from which both the router meta and the dock's option list derive — is **X-W8's** (folded to the
Dock-adj `G-C` derivation). X-W3 lands the **guard** only. Curing the enumeration here would take
a second wave's row and is out of bounds.

*Dissent.* None on the fact; `DVS-5`'s severity (MAJOR) exceeds `ATP-34`'s (MINOR) because the
former counts the ungated **admin panes rendering whole** as the consequence, the latter counts
only the dead attribute. Both grades are preserved; the cure is one line either way.
⟨AdminTagsPanel.md · ATP-34⟩ ⟨shell-dock-dockviewselect.md · DVS-5 (+ DVS-4 anti-smuggle note)⟩

---

**F-3 · `DVS-5` (eviction limb) — the sole client-side admin eviction cannot fire on a cold
deep-link** (cl.1 · MAJOR limb · **BOUNDS-EXPANDING**)

*Mechanism.* "the sole client eviction is a **non-immediate** watch
(`usePaletteWiring.ts:166-171`, verified against the `immediate:true` watch **twenty lines above**)
that cannot fire on a cold deep-link; the admin panes render whole to anonymous visitors."

*Why it is folded separately from F-2.* F-2's cure (the `beforeEach`) fail-closes **navigation**;
this limb is the **state** side — an already-mounted admin surface holding stale admin state on
first paint. `G-18`'s falsifier ("anonymous direct navigation to `/admin/users` does not mount
`AdminPane`") is satisfied by the guard alone and would go **green with this limb un-cured**,
which is exactly the L-19 failure shape.

*Sequencing.* Rides `X.W3.6`. **Requires a bounds addition** —
`demo/color-picker/composables/usePaletteWiring.ts` is in no X wave's file list (§BoundsDelta B-4).

*Dissent.* The record routes `DVS-5` **whole** to CC-037/X-W3; W3.md's §4 does not name the file.
Recorded as pressure, not as an authority to write.
⟨shell-dock-dockviewselect.md · DVS-5 (usePaletteWiring limb) + L-17 predicate accretion⟩

---

**F-4 · `AF-50` — the admin bearer lives in `localStorage`; the guard is built ON TOP of the
violating store** (cl.2 · MAJOR · **GATE-READING LOCK**)

*Mechanism.* "`useAdminAuth.ts:15/:21-23/:35-37` (my read) against **PALETTE-CONTRACT §2's
categorical ban** ('no … localStorage … auth path in the browser' — the contract's head declares
itself the LIVE authority). The corpus filed it only as a parenthetical."

*Lock (verbatim, binding on this wave's close).* "every proposed gate cure (**including X-W3's own
X.W3.6, which reads 'the existing `useAdminAuth` state'**) is built ON TOP of the violating store
… **NO-WAVE-OWNER** at the formation boundary — no X wave's bounds claim the auth substrate;
**recorded beside X-W3 so its guard landing is never read as discharging the contract row.**"

*Sequencing.* X-W3 **may not** report `G-18` GREEN as discharging the PALETTE-CONTRACT §2 row.
W3.md §4 lists `docs/tranches/V/PALETTE-CONTRACT.md` as **modify** — the contract row must be
carried forward in that edit, explicitly undischarged, or it dies silently in the one file this
wave is licensed to touch.

*Dissent.* The record is NO-WAVE-OWNER. This fold does **not** adopt the substrate;
§BoundsDelta B-5 lists the file as candidate-with-dissent only.
⟨AdminFlaggedPanel.md · AF-50 (A-miss #8)⟩

---

**F-5 · `EB-16` + `PS-8`/`L-7` ≡ CC-037/K-25 — the third failure state has no plate; the
`componentFor` tail is unreachable dead code contradicting its own doc comment** (cl.2 · MAJOR +
MINOR, one identity)

*Mechanism.* `EB-16`: "**ONE live mechanism**: `router/index.ts:36-37`'s catch-all redirect
(verified verbatim). The `componentFor` tail (`:94`) is **provably unreachable dead code
contradicting its own `:80` doc comment** ('Maps a view-config slot name to its component. `null`
for an unknown name') … The visual matrix's `notfound-redirect.png` records the consequence as
clean — **a correct render of the wrong thing**." `PS-8`: "`componentFor(name: string | null)`
widens the closed unions, forcing the unreachable masking fallback `return ColorPicker` (`:94`),
stacked on `useViewManager`'s `isViewId(name) ? name : 'picker'`."

*cureShape + lock (verbatim).* `EB-16`: "**narrowing the parameter from `string | null` to
`LeftPane | RightPane` lets TS prove exhaustiveness and deletes it with zero behaviour change.**"
`PS-8`: "typed `componentFor(name: LeftPane | RightPane)` **makes the fallback unwritable**."

*Sequencing.* Both records route it to X-W3 by name (`EB-16`: "**X-W3** (items 15/16 own the
not-found route + the fail-closed tail by name; `NotFoundPane.vue` already in its create list");
`PS-8`: "**X-W3**, one identity with CC-037/K-25's 'non-fail-closed pane fallback' row"). Files
already in W3 §4 bounds — **no bounds delta**.

*Dissent / kill carried.* `shell-paneslot` kill #8 **kills the corroboration**: "L-7's
visual-REPORT corroboration (`/#/does-not-exist` byte-identity) — killed as misattributed; the
router catch-all redirects **before** `componentFor` or the view fallback is reached. **The type
argument stands alone.**" Consequence for `G-19` — see §Gates S-8.
⟨ErrorBoundary.md · EB-16 (+ R-2, K-12)⟩ ⟨shell-paneslot.md · L-7/PS-8 + killed-claim 8⟩

---

**F-6 · `EAS`-corpus kill-3 residue (a) — routes the canon ratified are absent from the 14-path
router** (cl.1 · MAJOR residue · **NEW GATE SOURCE**)

*Mechanism (verbatim).* "the **route-inventory gap** — `/easing` and `/about` ratified
(VISUAL-CONSTITUTION.md:58) and **absent from the 14-path router** (my read) → **X-W3** (frontend
route closure; W3.md:87-89 territory)". The closing verdict repeats it: "**X-W3 closes the routes
the canon ratified.**"

*Sequencing lock (verbatim, anti-smuggle).* residue (b): "r2's transposition direction
(authoring-at-scale lives at `/easing`; the gradient row hands context there) preserved as the
**terminal architecture for X-W5/X-W6 — a direction, not a defect row**." X-W3 therefore lands
**route records only**; it authors **no scene**. W3.md's §3 item 15 already caps this wave at
"the wave's one non-`Stub` route record" (the not-found record) — adding two ratified route
records is a scope question this addendum surfaces and does **not** resolve.

*Dissent.* No record routes the `/easing`·`/about` **scene** to X-W3, and W3.md §Dispositions D-4
hands the 14 `Stub` records to X-W5. If the two ratified routes can only be registered as `Stub`s,
they belong to X-W5 by D-4 and this row degrades to a **cross-edge**, not a build row. Flagged for
the wave author; not decided here.
⟨wb-gradient-easingauthoringstage.md · killed-claim 3, residue (a)+(b) + closing verdict⟩

---

**F-7 · `A-3` (`N-1`) + `A-19` (`N-2`) — `setActiveTab("saved")` throws out of vue-router; every
live login success path** (cl.2 · **BLOCKER**, flagged **URGENT** · **BOUNDS-EXPANDING**)

*Mechanism (re-proven end-to-end by the adjudicating seat).* `"saved"` is not one of
`viewSchema.ts:35-49`'s 14 `ViewId`s nor `router/index.ts:22-35`'s 14 registered names; the chain
`useSlugMigration.ts:55/:69/:76` → `deps.setActiveTab` (ALWAYS supplied, `usePalettePorts.ts:88`)
→ `depsSwitchView("saved" as ViewId)` → `useViewManager.ts:78 router.push({ name: id })` →
vue-router 5.1.0 `pushWithRedirect:1243` calls `resolve(to)` **synchronously** →
`dist/vue-router.js:670 if (!matcher) throw createRouterError(1, { location })`. Three live
consequences, each verified: **(1) admin login** — teardown + `adminLogin` land, the throw escapes
as an unhandled rejection (`SlugEditLayer.vue:54` does not await), the view never switches;
**(2) slug login, no local palettes** — `userLogin` SUCCEEDS at `:75`, `:76` throws, the throw is
caught by the LOGIN catch and written to a permanently-null ref (`A-2`) — "a successful login
converted into a silently-swallowed router error"; **(3) migration** — degraded to
`console.warn("Migration action failed:")`: "**every palette migration reports failure after
succeeding**". "Fifteen runs quoted the offending line verbatim while analysing C-39 and **none
checked whether the route exists**."

*cureShape + structural lock (verbatim).* `A-3`: "a one-line cure, `setActiveTab("palettes")` **or
a typed-deps fix per N-2**, unbreaks the entire live login flow". `A-19`: "`usePalettePorts.ts:87`
`currentView as Ref<string>` … `:88 (tab: string) => depsSwitchView(tab as ViewId)` … **Typing the
deps `Ref<ViewId>` / `(id: ViewId) => void` makes A-3 a compile error in three places.**
**Disposition: NO-WAVE-OWNER — lands WITH A-3 as its structural cure (one edit, vue-tsc enforces
it for free).**"

*Sequencing.* **The record explicitly places this BETWEEN the two waves**: "**X-W3 owns route
closure and X-W5 owns the route↔scene law, but neither's written scope covers this call chain.**"
Adopting it here **trips W3.md §3a's triumvirate trigger** ("File-bound expansion that invalidates
the wave — … any second `demo/` scene file beyond the three named paths"): three new demo files
(§BoundsDelta B-6). Per `ORCHESTRATION.md §Triumvirate Auto-Triggers` the orchestrator may not
redispatch the unit alone.

*Dissent.* Banked **NO-WAVE-OWNER**, not X-W3. It is folded here because X-W3 is the wave whose
goal criterion ("**no route grants a privilege it has not checked**") and whose `G-19`/`G-20`
route-closure family come nearest — and because the record flags it URGENT for the formation
boundary. **This fold does not claim it; it records it where it cannot be lost.**
⟨PaletteSlugBar.md · A-3 (N-1) + A-19 (N-2) + closing verdict⟩

---

**F-8 · `VHD-25` ≡ `G-6` — `revertToVersion` looks the version up by global content hash and never
asserts membership** (cl.1 · MAJOR · **FOLD, no re-book**)

*Mechanism.* "`repository/paletteVersion.ts:13-15` (`findOne({ _id: hash })`, **unscoped**);
`service/versions.ts:131-137/:162-175` copies `version.name/colors` onto the target with **no
`version.paletteSlug === slug` check**; **`_id` IS the cross-palette content hash**."

*Route (verbatim).* "**FOLD → X-W3 G-6 (P0, already born-RED with this exact evidence)** — one
identity, no re-book."

*Epistemic lock.* "the demo-side trigger remains L's honest **HYPOTHESIS**." G-6's runtime probe
(owner of A reverts to a hash belonging to B → 404; A's `name`/`colors` byte-unchanged) is the
api-side witness and stands on its own; **no wave may cite a demo-side reproduction it does not
have**.
⟨VersionHistoryDrawer.md · VHD-25 (L-5 API arm)⟩

---

**F-9 · `VHD-33` ≡ `G-3` — `GET /palettes/:slug/versions` is unauthenticated and visibility-blind**
(cl.1 · MINOR promoted · **FOLD, no re-book**)

*Mechanism.* "`routes/versions.ts:25-41` bare, `:49-51` gated sibling" — the route returns "every
superseded name/colour set by slug".

*Route + calibration adopted verbatim.* "Already owned: **FOLD → X-W3 G-3 (born-RED with this exact
evidence)**; reader-2's calibration (**consistent with the detail-route posture, but strictly more
disclosure**) adopted verbatim." The calibration sharpens G-3's falsifier: the version list is not
merely as-leaky as the detail route — it is **strictly leakier**, because it emits every superseded
state, not only the current one.
⟨VersionHistoryDrawer.md · VHD-33 (reader-2 missed, promoted)⟩

---

**F-10 · `VHD-34` — an unattributed session accumulates `versionCount` with ZERO version rows**
(cl.2 · MINOR · **NEW GATE SOURCE**)

*Mechanism.* "counter bumped on `contentChanged` (`crud.ts:191-194`, create `:106`) while the row
is written only under `contentChanged && userSlug` (`:204`, create `:118`) — **a fourth,
undiagnosable cause of VHD-7's blank**."

*cureShape (verbatim).* "**BUILD → X-W3** (**X.A2/A3's write contract must make counter and log
move together or derive one from the other**)."

*Reachability caveat (carried, not laundered).* "including the honest reachability caveat
(`routes/crud.ts:74/:85`: session required, `userSlug ?? null` — **the guard was read, not
driven**; residue U-9)." **MEASURE-AT-OPEN** (cl.3): drive one authenticated-but-unresolved request
before the gate is armed, or arm the gate on the structural arm only.

*Sequencing.* Lands inside `X.W3.2`/`X.W3.3` (`model.ts` + `service/versions.ts` + `routes/crud.ts`
+ `service/crud.ts` — all in bounds). **No bounds delta.**
⟨VersionHistoryDrawer.md · VHD-34 (reader-2 missed, promoted) + residue U-9⟩

---

**F-11 · `VHD-12` — every revert `$inc`s `versionCount` while inserting nothing; the divergence is
permanent** (cl.1 · MINOR · sharpens `G-7`+`G-11`)

*Mechanism.* "dedup early-return (`service/versions.ts:40-44`) vs **unconditional `$inc`**
(`:172`); two renderers (`PaletteCardMenu.vue:101` vs `:11`)."

*Route (verbatim).* "**FOLD → X-W3** (**G-7 release identity makes a revert an appended revision —
the divergence cause dies; G-11's `201` carries it to the client**)."

*Sequencing / cross-edge.* `VHD-4`'s client-side cure ("after a revert the list is never
refetched") is X-W7's and **depends on this wave's payload**: "**pairs with X-W3 G-11's
`201`-with-appended-revision so the client has a truthful payload to render**." If G-11 ships a
bare 201 without the appended revision, X-W7's row cannot close. §CrossEdges §B.
⟨VersionHistoryDrawer.md · VHD-12 (C-12/L-6) + VHD-4's pairing clause⟩

---

**F-12 · `VHD-11` — version identity is invented from render position while the record carries a
real server ordinal the component never reads** (cl.2 · MAJOR · sharpens `G-7`)

*Mechanism.* "`v{{ total - i }}` (`:37`) … `depth` **computed and stored server-side**
(`service/versions.ts:46-58/:70`), mirrored in `demo/palettes/types.ts`; `total` is a **separate
`countByPaletteSlug` round-trip** (`service/versions.ts:92-95`)."

*cureShape (verbatim).* "**split** — the true ordinal becomes **first-class at X-W3 (G-7's release
identity mints `revisionNo`)**; the client consumes it at X.W7.d (**render the server ordinal,
never arithmetic on `total`**)."

*Sequencing lock.* G-7 as written asserts `payloadHash` framing + separate release identity +
migration. **This row binds a fourth clause onto G-7: the release identity must MINT a first-class,
client-visible `revisionNo`.** A G-7 that splits the hashes without emitting an ordinal leaves
`VHD-11` (MAJOR, feeding a destructive action — "the user picks *what to revert to* by that label",
"up to `v0`/`v-1`") with no cure anywhere in tranche X.
⟨VersionHistoryDrawer.md · VHD-11 (C-11/D-10, severity per R-4)⟩

---

**F-13 · `VHD-10` — offset paging over non-unique `createdAt` re-serves rows and hides others**
(cl.1 · MINOR · sharpens `G-7`)

*Mechanism.* "`:153-155` offset = rendered length; `repository/paletteVersion.ts:22-27` **no `_id`
tiebreak**" — "the duplicate renders under two ordinals".

*Route (verbatim).* "**split → X-W3** (**G-7's `revisionNo` is the total-order key**) **+ X.W7.d**
(cursor/tiebreak-aware client paging)."

*Sequencing.* Same G-7 clause as F-12; the two rows are distinct identities (identity invention vs
ordering non-determinism) with one shared cure surface.
⟨VersionHistoryDrawer.md · VHD-10 (C-8)⟩

---

**F-14 · `AF-26` (server half) — the wire sends `palette: {}`, never `null`; a THREE-way contract
fiction** (cl.2 · MAJOR · **BOUNDS-EXPANDING**)

*Mechanism.* "the `$project` at `flag.ts` builds a **computed sub-document**, `$unwind`
`preserveNullAndEmptyArrays`, so a lookup miss emits an **empty document — truthy**;
`v-if="!item.palette"` (`:72`) is **unreachable dead code** and the `:68-70`-forbidden
bare-slug-empty-strip state ships. THREE-way contract fiction: **api's own type says
`palette?: … | undefined` under a docstring teaching absence** (my read `:17-41`), the client type
says `| null`, **the wire says `{}`**."

*cureShape + lock (verbatim).* "**so correcting `types.ts` alone leaves the fiction taught
upstream**" — the api `$project` null-normalization **and** the producer type/docstring correction
move together. "Client branch (`item.palette?.slug`) + client type to wire truth = X-W7; api
`$project` null-normalization + the producer type/docstring correction = `api/**` → **X-W3 rider
(unclaimed by name — File Bounds do not list `flag.ts`)**, else NO-WAVE-OWNER at the formation
boundary."

*Sequencing.* Requires `api/src/modules/palette/repository/flag.ts` in bounds (§BoundsDelta B-1).
Client half is X-W7's and **must not** land first — a client `?.slug` over a truthy `{}` is a
no-op.

*Dissent.* Record-backed as an **X-W3 rider by name**, but conditional ("else NO-WAVE-OWNER"). The
condition is exactly the bounds question this fold raises.
⟨AdminFlaggedPanel.md · AF-26 (C-F-2/F-2 + B-miss #6 folded)⟩

---

**F-15 · `AF-27` (server half) — delete does not clear the queue; the docstring claims a cascade
the body does not perform** (cl.2 · MAJOR · **BOUNDS-EXPANDING**)

*Mechanism.* "service soft-deletes and **never touches `flags`** (my read — **its `:6` docstring
CLAIMS 'cascade votes/flags'; the body cascades nothing**; **`deleteByPaletteSlug(slug, session)`
sits unused in the same repo file**), the `$lookup` has **no `deletedAt` filter** so the
soft-deleted palette **rejoins fully populated**, `total` derives from `flags` and cannot change —
while the client optimistically splices. **The queue is unclearable by its own primary destructive
action.**"

*cureShape + lock (verbatim).* "Server half: **`admin/service/palettes.ts` IS in X-W3's File Bounds
(modify) and X.W3.6/G-17 rework it — but the flag cascade is unclaimed by name → X-W3 rider**, else
NO-WAVE-OWNER; **the docstring lie must be corrected by whichever wave touches the file.**"

*Sequencing.* Rides `X.W3.6`'s `G-17` touch of `admin/service/palettes.ts` (already in bounds); the
cascade itself needs `repository/flag.ts` (§BoundsDelta B-1) and `service/flags.ts`
(§BoundsDelta B-2). Client half (server-truthed mutations + reload with page clamp) = X-W7.

*Dissent.* Same conditional shape as F-14. The **docstring** clause is unconditional and binds this
wave the moment it opens the file.
⟨AdminFlaggedPanel.md · AF-27 (C-F-3/F-3)⟩

---

**F-16 · `PaletteCardMeta R-2` / `C-2` residue — the tag array has no schema uniqueness** (cl.1 ·
MINOR write-path fact · **BOUNDS-EXPANDING**)

*Mechanism.* "`schema.ts:35` carries **no uniqueness refine**; `crud.ts:184` `$set.tags =
body.tags` verbatim; TagEditPopover adds only on the unchecked→checked edge over the admin
canonical list, **so no authoring surface reaches the state**" — duplicates are reachable only via
a hand-crafted PATCH.

*Route (verbatim).* "**LC's fact survives as a schema-uniqueness rider (`.refine`/Set-normalize on
`tagsArraySchema`) → X-W3 rider (API policy), else NO-WAVE-OWNER (the AF-27 precedent: server-half
unclaimed by name).**"

*Dissent (the component row is DEAD).* "**C-2 — KILLED (both limbs, as a component defect).**" Only
the write-path fact books. Any executor reading this row as a UI defect is reading it wrong.

*Sequencing.* Requires `api/src/modules/palette/schema.ts` in bounds (§BoundsDelta B-3). One-line
cure; no migration implied (the state is unreachable through the product's own surfaces).
⟨PaletteCardMeta.md · R-2 ruling + killed-claim C-2⟩

---

**F-17 · `PM-16` — the vote heart silently mints a session; whether identity creation may hide
behind a control is an access-policy question** (cl.1 · MINOR · **POLICY NOTE, not a build row**)

*Mechanism.* "`format.ts:47/:89`: `voted: votedSlugs ? … : undefined` — **`undefined` is a
deliberate third state** ('we did not resolve this user's vote', `:55-56`). `:51`'s binary ternary
renders it as not-voted; the anonymous visitor and the confirmed non-voter get one hollow heart,
and **the click path silently mints a session (`session.ensureSession()`,
`useBrowsePalettes.ts:128`) behind a control that looked stateless.**"

*Route (verbatim).* "→ X-W7.d (three-way state or an unresolved render); **the silent session-mint
is flagged as an X-W3 policy note — access policy owns whether identity creation may hide behind a
heart.**"

*Sequencing.* X-W3 authors `docs/tranches/X/contracts/ADMIN-POLICY.md` (create) and modifies
`docs/tranches/V/PALETTE-CONTRACT.md` — **both in bounds**. The policy question is recorded there
or it is lost: no other wave in the tranche owns access policy.

*Dissent.* Not a defect claim against X-W3; a **decision** X-W3's contract authoring must take a
position on. Recording "no position" is an acceptable outcome; **silence is not**.
⟨PaletteCardMeta.md · PM-16 (DU missed)⟩

---

**F-18 · `GEN-33` — the route's async chunk has no loading/error/timeout arm** (cl.1 · INFO ·
shared X-W5/X-W3 · **anti-smuggle lock**)

*Mechanism.* "the route's async chunk has **no loading/error/timeout arm** — **all ten registry
entries identical**; shell-owned, surfaces here; the pane-side state absence folds into GEN-4.
→ **X-W5/X-W3** shell territory."

*Sequencing lock (verbatim, from the sibling record).* `EB-4`'s cure-lock **binds any wave that
touches this class**: "the internal `onError` calls `handleError(err, instance, 13,
!errorComponent)` and `handleError` walks the `instance.parent` chain **unconditionally** … so
adding `errorComponent`/`onError` to the ten loaders does **NOT** stop the rejection from also
reaching the App-altitude boundary's `onErrorCaptured` and latching the app. **The loader-options
cure and the containment transposition are jointly necessary; landing either alone leaves a live
BLOCKER. Any wave binding EB-4 must state this.**"

*Disposition.* **X-W3 lands only the fail-closed tail (W3 §3 item 16 / `G-19`).** The loader
options and the per-slot boundary are **X-W5's** (`EB-4`, `D-6`). X-W3's close may **not** be read
as curing the async-chunk class.
⟨wb-generate-pane.md · GEN-33⟩ ⟨ErrorBoundary.md · EB-4 + ruling R-1 cure-lock⟩

---

### Riders recorded, NOT adopted (cl.4)

**F-19 · `AF-19` (server arm) — `listFlagged(limit, offset)` takes no sort/filter argument**
(MINOR). "No sort/filter/facet in the one panel that is a work queue; **server-side
`listFlagged(limit, offset)` takes no sort/filter argument either**. Client → X-W7; **server
argument surface = `api/**` → NO-WAVE-OWNER rider.**" Booked so the api-side arm is not lost when
X-W7 closes the client arm.
⟨AdminFlaggedPanel.md · AF-19 (D-20)⟩

**F-20 · `AF-25` (server arm) — `item.flags` unbounded in the aggregation** (MINOR). "`$push` with
**no `$slice`** — my read; the only write-side bound is one-flag-per-reporter. Client
cap/disclosure → X-W7; **api `$slice` → NO-WAVE-OWNER rider.**" Same file as F-14/F-15
(`repository/flag.ts`) — if B-1 lands, this rider becomes cheap; if B-1 does not, it must cross the
formation boundary intact.
⟨AdminFlaggedPanel.md · AF-25 (D-26)⟩

**F-21 · `TEP-10` — `paletteETag` implemented twice with type drift; the docstring cites a file
that does not exist** (MAJOR). "`string|undefined`+`string` vs `string|null`+`Date`, **no shared
contract home**, and the demo docstring cites `api/src/middleware/etag.ts` — **verified this seat:
does not exist**. → **NO-WAVE-OWNER — the contract home spans demo+api; X-W3 is api-side only,
X-W7 must-not-touch `api/**`; named for the next formation boundary.**" **Sharpening this wave CAN
take without expanding bounds**: `api/src/modules/palette/etag.ts` is in W3 §4 and X.W3.3 authors
`WRITE-CONTRACT.md` (create). The record's superlative names that module as exemplary — "**every
demo-side defect in TEP-6 is a deviation FROM this module**" — so the demo-side deviations
(leaf-owned If-Match derivation `:73-74`; the silent `undefined→"*"` downgrade at
`useTagEdit.ts:54` → `etag.ts:44` **skips the check**; the discarded PATCH response; the stale
`updatedAt` re-cache at `BrowsePane.vue:312-319`) should be **written into WRITE-CONTRACT.md as
named non-conformances**, and the phantom `api/src/middleware/etag.ts` cite corrected wherever this
wave's own docs quote it. Nothing here writes demo source.
⟨TagEditPopover.md · TEP-10 (L-5) + TEP-6 + superlative 3⟩

**F-22 · `AAP-22` — X-W3 DECLINES the audit-ledger cursor question** (MINOR). "offset pagination
where the binding composition says stable cursor … Source mechanism confirmed (offset + `sort
timestamp:-1`; `CursorPaginatedResponse` **already declared in the same types file**); the skipped-
event consequence needs concurrent writes (correctly HYPOTHESIS). **Disposition: NO-WAVE-OWNER
(API+client tuple; X-W3's scope does not carry it).**" Booked as an explicit **declination** so the
close cannot be read as having swept it in with F-13's ordering work — different resource,
different owner.
⟨AdminAuditPanel.md · AAP-22 (D-13)⟩

**F-23 · `AdminListSkeleton` kill-5 residual — X-W3 DECLINES the transport deadline** (residual).
"a hung socket never rejects — **0 `AbortController|AbortSignal|timeout` hits in
`demo/palettes/api/`** (my census). That residual's cure was specced at **MT-AU1 §3 scope (4)**
(`AbortSignal.timeout` in the transport client) and, like the rest of MT-AU1, **was never cut into
X (X-W3 carries no deadline row — my grep)**. **Residual → NO-WAVE-OWNER, MT-AU1 §3(4) identity
preserved.**" Booked as a declination with its identity intact; reachability is
UNPROVEN-NEEDS-LIVE (that record's residue #4).
⟨AdminListSkeleton.md · killed-claim 5 residual + closing verdict⟩

---

**F-24 · `AAP-12` + `ATP-15` riders — the born-RED route spec lands FIRST** (cl.2 · MAJOR ×2 ·
**SEQUENCING EDICT binding on this wave**)

*Mechanism (the vacuum these riders exist to close).* `AAP-12`: "**vacuous gate: the row half has
never rendered under any test.** Zero unit refs; the only e2e visit asserts a heading AdminPane
owns + one button; `ADMIN_VIEWS` omits audit; **the populated fixture routes the endpoint no spec
visits (dead fixture data)** — all re-verified by this seat. **This is the mechanism behind
AAP-1/AAP-2 surviving to HEAD.**" `ATP-15`: "the only gates are two e2e specs that assert a request
fired and nothing else, **one authored AROUND the ATP-2 defect (`dispatchEvent("click")` past
actionability)**, both justifying their selectors with a dual-mount the shell retired."

*The riders (verbatim).* `AAP-12`: "**NO-WAVE-OWNER as a standalone row, with a binding rider:
whichever wave cures AAP-1..AAP-3 lands the born-RED audit-route spec FIRST (E-1 twice-audit
discipline; C's mutation table is the spec's seed).**" `ATP-15`: "**NO-WAVE-OWNER as a row, with
the AAP-12 binding rider: whichever wave cures ATP-1..ATP-3 lands the born-RED tag-route spec
FIRST (E-1), seeded from C's mutation table; the stale X6 prose dies in the same edit.**"

*Why it binds X-W3.* `AAP-3` and `ATP-3` are **cured by this wave** (F-1). The riders name "whichever
wave cures AAP-1..**AAP-3**" / "ATP-1..**ATP-3**" — so X-W3 carries the riders **for its share of
the class**, in the same integration as the guard. `AAP-1/AAP-2` and `ATP-1/ATP-2` are X-W7's; the
rider therefore lands **split**, and the E-1 obligation on X-W3 is: the born-RED route spec for the
**access arm** exists before `X.W3.6` reports.

*Sequencing.* Composes with W3's own `G-22` ("the one-time falsifier demonstration run") and
§10 ("Depends on: X-W1 — honest CI + the mandatory falsifier demonstration — **without it a
born-RED gate flipping GREEN proves nothing**"). Additional X-W1 dependency surfaced by `AF-30`:
"fixture rot verified by this seat — `admin-populated.ts:153` answers `DELETE …/palettes/…` with
`json("", 204)` while the live route returns 200 `{deleted:true}` and `client.ts:155` calls
`res.json()` unconditionally (**empty body throws**); and `:28` type-imports from `demo/@/lib/
palette/types` **which does not exist** (type-erased, unchecked — e2e sits in no tsconfig program).
Fixture/CI half = **X-W1** (CC-031 fixture repair + hard e2e)." **W3's new
`e2e/smoke/admin/route-guard.spec.ts` runs against this fixture estate**; if X-W1 has not repaired
it, the guard spec's green is not load-bearing.

*Dissent.* Both source rows are banked NO-WAVE-OWNER **as rows**; only the **riders** bind. This
fold books the riders, not the rows.
⟨AdminAuditPanel.md · AAP-12 (C:D-7)⟩ ⟨AdminTagsPanel.md · ATP-15 (C:C-10 + M8/missed-6)⟩
⟨AdminFlaggedPanel.md · AF-30 (C-F-10/F-10)⟩

---

## §Gates

Every entry names its **real witness** (a byte, a grep, a live route table, a producer d.ts — never
a proof-script; **L-19 / CC-019's structural ban is honored: this fold authors no `proof-*.mjs`**).
**S-n** = SHARPENS AN EXISTING W3 §6 GATE (id preserved, baseline preserved, predicate widened).
**N-n** = GENUINELY NEW born-RED candidate (no existing W3 gate names it).

### A. Sharpenings of existing W3 §6 gates (10)

| # | gate | sharpening this fold adds | witness (real) | source |
|---|---|---|---|---|
| **S-1** | **G-18** (P0, anonymous `/admin/users`) | The gate's cure list must name the **21 `if (!token)` sites across 5 composables** (3/2/5/10/1) as the thing that DELETES behind it, and its falsifier must include **ATP-3's dead-create-button limb** — `:42`'s `:disabled` never tests the token; `createTagAction`'s `:67` guard makes the enabled click a **silent no-op**. G-18 as written proves the pane does not MOUNT; it does not prove the **enabled control is not armed**. | three composables' `if (!token) return;` before any `loading`/`loadError` write (`useAdminAudit.ts:49-50` · `useAdminFlagged.ts:60-61` · `useAdminTags.ts:49-51`); REPORT.json dock `Login` + `0 flagged` + `· NOTHING FLAGGED ·` in **all four shipped captures**, `failedRequests: []` | F-1 |
| **S-2** | **G-18** | Add the **state** arm: fail-closing navigation does not evict already-held admin state on a cold deep-link. | `usePaletteWiring.ts:166-171` is a **non-immediate** watch, twenty lines below an `immediate:true` sibling; `router/index.ts` registers **only** `installDocumentTitle` while its own comment calls itself "the canonical navigation-guard home" | F-3, F-2 |
| **S-3** | **G-18** | The baseline's "5 × `meta: { admin: true }`" is one of **four** disagreeing enumerations of "admin": router meta on 5 · the dock's 7+7 literals · the schema's hue fan (0 for five admin views, 280/320 for atmosphere/blob) · a bare `'palettes'` literal at `DockViewSelect.vue:116`. The gate must not be read as curing the enumeration — that is X-W8's `G-C` derivation. | `router/index.ts:28-34` · `useDockAdminMode.ts:26-27` · `viewSchema.ts:177/:186` · `DockViewSelect.vue:116` | F-2 (DVS-4 anti-smuggle) |
| **S-4** | **G-3** (versions list refused anonymously) | Calibration adopted verbatim: the leak is "**strictly more disclosure**" than the detail route — every superseded `{name, colors, authorSlug, parentHash}`, not only the current state. | `routes/versions.ts:25-41` bare vs `:49-51` gated sibling; `repository/paletteVersion.ts:22-27` = `find({ paletteSlug })` | F-9 |
| **S-5** | **G-6** (P0, cross-object revert) | Evidence corroborated from a second seat with the **membership** framing: `_id` **IS** the cross-palette content hash, so the unjoined `findOne({_id: hash})` is a content-addressed door between objects. Epistemic lock: **the demo-side trigger stays HYPOTHESIS** — no wave cites a reproduction it does not have. | `repository/paletteVersion.ts:13-15` unscoped `findOne`; `service/versions.ts:131-137/:162-175` copies `name`/`colors` with no `paletteSlug` assertion | F-8 |
| **S-6** | **G-7** (payloadHash / release identity / migration) | **Fourth clause, binding**: the release identity must **mint a first-class, client-visible `revisionNo`** that is (a) the **total-order key** for the version list (with an `_id` tiebreak), and (b) the ordinal the client renders. A G-7 that splits hashes without emitting an ordinal leaves `VHD-11` (MAJOR, feeding a destructive action) and `VHD-10` (duplicate rows under two ordinals) with **no cure anywhere in tranche X**. | `service/versions.ts:46-58/:70` stores `depth` server-side, mirrored in `demo/palettes/types.ts`, **read nowhere**; `repository/paletteVersion.ts:22-27` sorts `createdAt` with **no `_id` tiebreak**; client renders `v{{ total - i }}` off a separate `countByPaletteSlug` round-trip | F-12, F-13 |
| **S-7** | **G-11** (revert returns `201` with the appended revision) | The **payload** clause is load-bearing downstream, not cosmetic: X-W7's `VHD-4` cure ("the version mutation's one visible result IS the refreshed history") is specified to consume it. A bare `201` closes G-11 and strands `VHD-4`. | `routes/versions.ts:66 return c.json(formatPalette(palette));` → 200 (W3's own baseline) + `VHD-4`'s "pairs with X-W3 G-11's `201`-with-appended-revision" | F-11 |
| **S-8** | **G-19** (unknown pane → not-found pane, never `ColorPicker`) — **RE-FRAME REQUIRED** | **As written this gate cannot fail for its intended reason.** Both typed call sites pass `currentConfig.value.left/right` (typed `LeftPane` 11-member / `RightPane` 4+null), and `useViewManager.ts:43-45` clamps through `isViewId(name) ? name : "picker"` — `:82-93` handle all 16 cases, so **the tail at `:94` is provably unreachable**; the observed `/#/does-not-exist` → picker render is caused **entirely** by `router/index.ts:36-37`'s catch-all. A runtime probe for "an unknown pane name" has **no reachable input**. G-19 must become a **structural/type gate**: signature narrowed to `componentFor(name: LeftPane \| RightPane)`, the tail **deleted**, `vue-tsc` proving exhaustiveness — a change with **zero behaviour delta**, which is precisely why the runtime framing is vacuous. | `usePaneRouter.ts:164-165/:169-171` call sites; `viewSchema.ts:52-63/:66` unions; `useViewManager.ts:43-45`; the function's own `:80` doc comment ("`null` for an unknown name") **contradicting** `:94 return ColorPicker;`; `shell-paneslot` killed-claim 8 killing the visual-REPORT corroboration | F-5 |
| **S-9** | **G-20** (unknown URL renders a source-realized not-found route) | Confirmed sole live mechanism (the catch-all redirect), **and** widened by the route-inventory finding: closing unknown URLs does not answer whether **ratified** routes are registered. See N-6. | `router/index.ts:36-37` verbatim; `notfound-redirect.png` records "a correct render of the wrong thing" | F-5, F-6 |
| **S-10** | **G-17** (admin audit row names the `ADMIN-POLICY` branch) | The same file this gate reworks (`admin/service/palettes.ts`) carries a **docstring that claims a cascade its body does not perform**; the record binds: "**the docstring lie must be corrected by whichever wave touches the file**". Recorded as a truth obligation on G-17's edit, not a new predicate. | the service's `:6` docstring "cascade votes/flags" vs a body that cascades nothing; `deleteByPaletteSlug(slug, session)` **unused** in `repository/flag.ts` | F-15 |
| **S-11** | **G-22** (green suites + falsifier demonstration) | Two E-1 riders bind X-W3's share of the AdminGate class: **the born-RED route spec lands FIRST** (audit arm + tag arm). Additionally: the new `e2e/smoke/admin/route-guard.spec.ts` runs against a **rotten fixture estate** — `admin-populated.ts:153` answers DELETE with `json("", 204)` while the live route returns `200 {deleted:true}` and `client.ts:155` calls `res.json()` unconditionally (**empty body throws**); `:28` type-imports `demo/@/lib/palette/types`, **which does not exist**. Repair is X-W1's (CC-031); until then this wave's guard-spec green is not load-bearing. | `AAP-12` (`ADMIN_VIEWS` omits audit; the populated fixture routes `/admin/audit` for **no** visiting spec) · `ATP-15` (two specs assert a request fired; one authored **around** the defect via `dispatchEvent("click")`) · `AF-30` fixture bytes | F-24 |

*(S-11 is the eleventh entry against ten sharpened gate IDs — `G-18` is sharpened three times
(S-1/S-2/S-3) and counted once as a gate; the ten distinct existing gate IDs touched are
**G-3 · G-6 · G-7 · G-11 · G-17 · G-18 · G-19 · G-20 · G-22** plus **G-9** at N-0 below. Entry
count in this table = 11, gate-ID count = 10 — both figures are countable from these bytes.)*

**Also touched, without a new predicate: `G-9`.** F-21 supplies WRITE-CONTRACT.md's canon with the
demo-side deviations from `api/src/modules/palette/etag.ts` by name (leaf-owned If-Match
derivation; the `undefined→"*"` downgrade that makes `etag.ts:44` **skip the check**; the discarded
PATCH response; the stale `updatedAt` re-cache) and corrects the phantom
`api/src/middleware/etag.ts` cite. No gate predicate changes; the **document** gets truer.

### B. New born-RED candidates (7)

| # | candidate gate | predicate | measured witness at the records' HEADs | falsifier (L-19) | source |
|---|---|---|---|---|---|
| **N-1** | **version-count conservation** | `versionCount` and the version log move together, or one derives from the other: (a) an authenticated-but-unresolved session cannot bump the counter without writing a row; (b) a revert that dedups to an existing hash does not `$inc`. | (a) counter bumped on `contentChanged` (`crud.ts:191-194`, create `:106`) vs row written only under `contentChanged && userSlug` (`:204`, create `:118`); (b) dedup early-return `service/versions.ts:40-44` vs **unconditional `$inc` `:172`` | Remove the derivation/guard → a palette reports N versions with M<N rows and the drawer's subtitle asserts a count it cannot list. **Reachability of arm (a) is MEASURE-AT-OPEN** — "the guard was read, not driven" (residue U-9). | F-10, F-11 |
| **N-2** | **`revisionNo` first-class + total order** | The version list is ordered by a stored monotone `revisionNo` with an `_id` tiebreak, and every list item carries it on the wire. | `depth` stored (`service/versions.ts:46-58/:70`) and read by **no** client path; `repository/paletteVersion.ts:22-27` has **no `_id` tiebreak** over non-unique `createdAt`; client computes `v{{ total - i }}` from a separate `countByPaletteSlug` | Drop the field from the wire → the client must reinvent the ordinal from `total`, reproducing `v0`/`v-1` after any count drift. | F-12, F-13 |
| **N-3** | **flag-projection null truth** | The flag list emits `palette: null` for a lookup miss — never `{}` — and the api type + docstring agree with the wire. | `$project` builds a **computed sub-document** + `$unwind preserveNullAndEmptyArrays` ⇒ a miss emits an **empty, truthy** document; api type `palette?: … \| undefined` under a docstring **teaching absence**; client type `\| null`; **three-way disagreement** | Restore the computed sub-document → `v-if="!item.palette"` becomes dead again and the deleted-palette row ships bare-slug-with-empty-strip. **Needs `repository/flag.ts` in bounds.** | F-14 |
| **N-4** | **flag cascade + docstring truth** | Soft-deleting a palette clears (or excludes) its flags: `total` changes, the row does not rejoin, and `deleteByPaletteSlug` either has a caller or is deleted. | service soft-deletes and never touches `flags` while its `:6` docstring **claims** "cascade votes/flags"; `$lookup` has **no `deletedAt` filter**; `deleteByPaletteSlug(slug, session)` **unused in the same repo file** | Remove the cascade/filter → the moderator's primary destructive action leaves the row in the queue, fully populated, on the next refresh. **Needs `repository/flag.ts` + `service/flags.ts` in bounds.** | F-15 |
| **N-5** | **tag-array uniqueness at the schema** | `tagsArraySchema` refuses (or normalizes) duplicates; a hand-crafted PATCH cannot persist a repeated tag. | `schema.ts:35` carries **no uniqueness refine**; `crud.ts:184` `$set.tags = body.tags` verbatim; bound already present: ≤10 × ≤30 chars | Remove the refine → a PATCH persists `["warm","warm"]` and the card renders a duplicate `:key`. **Needs `schema.ts` in bounds.** | F-16 |
| **N-6** | **route-inventory conformance** | Every route the canon ratifies as a member route has a registered record in the router. | `VISUAL-CONSTITUTION.md:58` ratifies `/easing` and `/about`; the router registers **14** names and **neither is among them** | Delete a ratified record → the canon names a route the product cannot reach; the catch-all silently redirects it to the picker (S-9's mechanism). **Scope question flagged: if the records can only be `Stub`s, D-4 hands them to X-W5.** | F-6 |
| **N-7** | **typed view dispatch — no untyped string reaches `router.push`** | The port's view deps are `Ref<ViewId>` / `(id: ViewId) => void`; `vue-tsc` rejects any non-`ViewId` literal at the three call sites. | `usePalettePorts.ts:87` `currentView as Ref<string>` (widening a writable, invariant `Ref`) and `:88 (tab: string) => depsSwitchView(tab as ViewId)`; `"saved"` at `useSlugMigration.ts:55/:69/:76`; vue-router 5.1.0 `dist/vue-router.js:670` throws **synchronously** via `pushWithRedirect:1243`'s `resolve(to)`; `isViewId` ships and is used route→state (`useViewManager.ts:43-46`) but **not** state→route | Restore the casts → a login success path throws out of the router; three live consequences reproduce (unhandled rejection on admin login; a successful slug login rendered as an error into a permanently-null ref; every migration reporting failure after succeeding). **Bounds-expanding; trips W3 §3a's triumvirate.** | F-7 |

**Gate-family coverage note (honest negative).** This corpus contributes **no** rows to `G-12`,
`G-13`, `G-14` (the fork triad), `G-15` (the `unlisted`/moderation enum), `G-16` (the diff
decision), or `G-21` (the `Stub` handoff). Their baselines stand unamended by this fold — recorded
so no reader infers corpus corroboration where there is none.

---

## §BoundsDelta

Files this wave must ADD to `W3.md §4 File Bounds` **per the records**. **A** = record-backed as an
X-W3 rider by name. **B** = candidate/contested — the record routes elsewhere (NO-WAVE-OWNER) or
splits, and the row is folded here because X-W3 is the nearest owner; **listing is pressure, not
authority**.

| # | file | class | warrant (record's own words) | rows |
|---|---|---|---|---|
| **B-1** | `api/src/modules/palette/repository/flag.ts` | **A** | "api `$project` null-normalization + the producer type/docstring correction = `api/**` → **X-W3 rider (unclaimed by name — File Bounds do not list `flag.ts`)**, else NO-WAVE-OWNER"; and the unused `deleteByPaletteSlug` cascade sits in this same repo file. Also carries the `$slice` rider (F-20). | F-14, F-15, F-20 |
| **B-2** | `api/src/modules/palette/service/flags.ts` | **A** | AF-27's cascade counterpart (`listFlagged`'s `total` derives from `flags`); AF-19's "server argument surface = `api/**`". W3 §4 lists `service/forks.ts`, `versions.ts`, `crud.ts`, `crud-list.ts`, `visibility.ts` — **not** `flags.ts`. | F-15, F-19 |
| **B-3** | `api/src/modules/palette/schema.ts` | **A** | "the schema-uniqueness rider (`.refine`/Set-normalize on `tagsArraySchema`) → **X-W3 rider (API policy)**, else NO-WAVE-OWNER (**the AF-27 precedent**)". W3 §4 lists `model.ts`/`format.ts`/`hash.ts`/`etag.ts` — **not** `schema.ts`. | F-16 |
| **B-4** | `demo/color-picker/composables/usePaletteWiring.ts` | **B** | `DVS-5` routes **whole** to CC-037/X-W3, and its eviction limb lives here: "the sole client eviction is a **non-immediate** watch (`:166-171`) that cannot fire on a cold deep-link". No X wave's bounds name the file. | F-3 |
| **B-5** | `demo/platform/auth/useAdminAuth.ts` | **B** | `AF-50` is banked **NO-WAVE-OWNER**; listed here **only** because `X.W3.6`'s mechanism text reads "the existing `useAdminAuth` state" — i.e. this wave builds on it. **The record forbids reading the guard's green as discharging PALETTE-CONTRACT §2.** Do not adopt without an owner ruling. | F-4 |
| **B-6** | `demo/palettes/useSlugMigration.ts` · `demo/palettes/usePalettePorts.ts` · `demo/shell/useViewManager.ts` | **B** | `A-3`/`A-19` banked **NO-WAVE-OWNER, URGENT**: "X-W3 owns route closure and X-W5 owns the route↔scene law, **but neither's written scope covers this call chain**". **Adoption trips W3 §3a's triumvirate trigger** (demo files beyond the named paths). | F-7 |
| **B-7** | `demo/palettes/useAdminAudit.ts` · `useAdminFlagged.ts` · `useAdminTags.ts` · `useAdminUsers.ts` · `useColorNameQueue.ts` | **B — CONTESTED** | `AAP-3`/`ATP-3`: "the per-method guards **DELETE behind that gate**" (i.e. with the X-W3.6 cure). `AF-1`: "deletion of the 21 `if (!token) return` sites = **X-W7**". The seam is unresolved in the corpus; whichever wave takes it, **the other's gate must not go green over its edit**. `AdminTagsPanel` additionally flags `useAdminTags.ts` as "in **no** X wave's file list — a named gap". | F-1 |
| **B-8** | `e2e/smoke/admin/audit-route.spec.ts` · `e2e/smoke/admin/tags-route.spec.ts` (create) | **B — rider-implied** | The `AAP-12`/`ATP-15` riders require the born-RED route specs **FIRST**, seeded from challenge-C's mutation tables. W3 §4 creates only `e2e/smoke/admin/route-guard.spec.ts`. X-W3's share is the **access arm**; the row/state arms are X-W7's. | F-24 |

**Not proposed, deliberately** (recorded so the absence is a decision, not an oversight):
`demo/platform/transport/client.ts` (`AAP-10`/`ATP-13`/`TEP-19` all bank it NO-WAVE-OWNER —
"`demo/platform/transport/` in no X file list"); `demo/palettes/api/*.ts` (F-23's declined
deadline); `demo/palettes/useTagEdit.ts` (F-21's demo half — X-W7's file, and X-W7 must-not-touch
`api/**`, which is why TEP-10 has no home).

---

## §CrossEdges

**§A — X-W3 ⇄ X-W7 · the AdminGate deletion seam (CONTESTED).** F-1's guard is X-W3's; the 21
`if (!token)` deletions are claimed by **both** waves in different records (`AAP-3`/`ATP-3` behind
the X-W3.6 gate; `AF-1` at X-W7). The pane-side unauthorized state is unambiguously X-W7's. **Edge
law**: neither wave reports the AdminGate identity closed alone; whichever wave lands second
inherits the seam, and its gate may not go green over the other's edit. (The `VersionHistoryDrawer`
record states the same discipline for its own two-wave carve: "recorded so neither wave's gate goes
green over the other's edit".)

**§B — X-W3 ⇄ X-W7 · the revert payload dependency.** `G-11`'s `201`-with-appended-revision is the
input X-W7's `VHD-4` cure consumes ("the version mutation's one visible result IS the refreshed
history"). Direction: **X-W3 first**. A bare `201` closes G-11 and strands X-W7.

**§C — X-W3 ⇄ X-W5 · route closure vs route↔scene.** (a) F-5's not-found record is "**the wave's one
non-`Stub` route record, and the pattern X-W5 generalizes**" (W3 D-4/G-21) — X-W3 lands the
pattern, X-W5 lands the 14. (b) F-18: X-W3 lands **only** the fail-closed tail; the ten bare
`defineAsyncComponent` loaders, the per-slot boundary and the loading/error states are X-W5's, and
`EB-4`'s cure-lock binds whoever takes them ("the loader-options cure and the containment
transposition are **jointly necessary**"). (c) F-7 falls **between** the two waves by both records'
own words. (d) F-6's `/easing` scene is X-W5/X-W6's terminal architecture; X-W3 may land route
records only.

**§D — Declinations X-W3 makes by its own scope (booked, not dropped).**
`AAP-22` — audit-ledger cursor pagination: "**X-W3's scope does not carry it**" (F-22).
`AdminListSkeleton` kill-5 residual — the transport deadline: "**X-W3 carries no deadline row**",
MT-AU1 §3(4) identity preserved (F-23).
`AF-19`/`AF-25` — the `listFlagged` argument surface and the `$slice` bound: NO-WAVE-OWNER riders
(F-19/F-20), cheap **if** B-1/B-2 land, otherwise they cross the formation boundary intact.
`TEP-10` — the `paletteETag` contract home: "X-W3 is **api-side only**, X-W7 **must-not-touch
`api/**`**" — the identity has no owner in the tranche (F-21).

**§E — X-W3 ⇄ X-W8 · anti-smuggle.** `DVS-4`'s enumeration cure (one `group`/`audience` field on
`viewSchema.ts`, both the router meta and the dock option list derived) is **X-W8's** (Dock-adj
`G-C`). X-W3 lands the guard only; a fold reader must not take S-3's four-enumeration witness as
license to derive here.

**§F — X-W3 ⇄ X-W1 · gate honesty (already a W3 §10 dependency, now with two more legs).**
(a) F-24's born-RED-spec-FIRST riders. (b) `AF-30`'s fixture rot: the estate W3's new guard spec
runs against answers DELETE with `json("", 204)` against a live `200 {deleted:true}` and
type-imports a directory that does not exist; repair is X-W1/CC-031. (c) W3 §10 already binds:
"without [X-W1's honest CI + falsifier demonstration] a born-RED gate flipping GREEN proves
nothing."

**§G — Glass: NONE.** Zero of the 24 rows is glass-coupled. **W3.md §Dispositions D-8 ("Glass bank
rows: None") is CONFIRMED by this fold** — the corpus's glass rows (`ATP-23`, `VHD-5/18/19/24/39`,
`EAS-10`, `A-4`, `GEN-8/28`) all route to the standing BH relay or to other waves, and none
touches an X-W3 surface.

**§H — W-HYGIENE: untouched.** The `tsconfig.demo.json` paths-drift identity (`AAP-32` ≡ `ATP-35` ≡
`VHD-30` ≡ `DVS-22` ≡ `GEN-23` ≡ `EAS-38` ≡ ColorSpaceSelector `L-14`) is ONE identity already
FOLDed to W-HYGIENE with a born-RED generator-parity command; **it is not re-booked here**, and
X-W8 explicitly declines it ("another owner"). Recorded so this fold's silence is legible as
anti-rename discipline, not as a miss.

**§I — Dispositions D-1/D-2 of W3.md stand unamended.** No corpus row re-litigates `X-4`
(`/versions` vs `/revisions/{revisionNo}`) or `X-5` (the export denominator). Note however that
**N-2 mints `revisionNo` as a FIELD, not as a PATH** — D-1's "`/versions` STANDS" is untouched by
it, and no executor may read S-6/N-2 as reopening the rename.

---

*Dispositions in this addendum are **ADJUDICATED, not VERIFIED**; nothing here stamps a wave, opens
product source, or moves X-W3 off `planned`. The thirteen apotheoses, their challenge corpora and
reader returns, and the dated wave specs remain immutable beside this file per E-1/E-3.*
