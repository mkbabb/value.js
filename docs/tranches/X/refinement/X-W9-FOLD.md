SERVED MODEL ID (fold author, line-1 declaration): claude-opus-5[1m]

# X-W9 REFINEMENT FOLD — 2026-08-28 · the dated addendum to `waves/W9.md`

## 1. Header — provenance, posture, the reading rule

**Provenance.** The value.js corpus (all `registry/adjudicated/*.md` that are NOT `kf-*`/`fr-*`/`pt-*`
— 88 component apotheoses + `parser-band` · `library-band` · `chassis-fitness` · `layout-gestalt`)
is banked whole at `35fc8ebf` (COHESION §0a: value.js 88/88, SS-7 CLOSED). This addendum is the
M-25/M-27 fold of every row in that corpus that routes to **X-W9** onto the dated wave spec
`docs/tranches/X/waves/W9.md`.

**The routing sweep, stated as a method (falsifiable).** `grep -rlE 'X[-·.]W9'` over the 92-file
value.js corpus returns **exactly 28 files**, and that set is **identical** to this seat's assigned
slug list — no assigned record was silent, no unassigned record routed here. Three near-miss classes
were swept and cleared: (a) `kf-*`/`fr-*` matches on a bare `W9` token are **KF.W9 / F.W9**, other
tranches' waves, never this one; (b) the 64 non-routing value.js files were re-swept for
`CC-008|CC-084|CC-088..CC-097` → **zero hits**, so no record routes here under a CC id alone;
(c) `library-band.md` and `parser-band.md` carry no `X-W9` token because they are the wave's
**inputs**, already folded into the dated spec (§11).

**E-3 posture (addenda, never patch).** `waves/W0.md..W11.md` and `CONFORMANCE-2026-08-03.md` are
**NEVER edited**. This file is the fold's **sole write**. Where this addendum strikes or corrects a
phrase in the dated spec, the strike is recorded here and the spec's bytes stand untouched — the
dated record is immutable; the addendum is the later adjudicated word.

**The reading rule.** At execution X-W9 reads **`waves/W9.md` PLUS this addendum**, in that order,
and where the two disagree the addendum's clause governs. Nothing here re-stages, re-weights or
re-composites the spec's gates — **CC-096/OC-2 staging stands** (equivalence · coverage · bench read
separately; no composite RED). This addendum sharpens predicates, adds corpus rows and riders,
declares bounds deltas, and records dissents.

**Execution posture.** `waves/W9.md` §State stays `Status: planned`. **NOTHING here opens product
source.** This seat read no `src/**` or `demo/**` file contents; its two instruments were
`git ls-files` (path census) and `git grep -l` (**filename-only** symbol census, no content
printed), each labelled at its use as *[filename-only census, this seat]*. Every measurement in a
row body is a **quotation of an adjudicated record**, carried with that record's own epistemic
status (measured / traced / adopted / UNPROVEN), never upgraded here.

**Fold classes** (X·P's, kept): **cl.1** = spec sharpening / law / correction · **cl.2** =
cure-lock / binding contract clause · **cl.3** = bounds delta · **cl.4** = notice / pointer, no
booking moves.

**Arithmetic — THE OUT SIDE, AND ONLY WHAT THIS DOCUMENT ITSELF ENUMERATES.** **30 FOLDED ROWS**,
contiguously numbered W9.1..W9.30, each with exactly one `⟨…⟩` provenance line, distributed by wave
unit: **.a ×3 · .b ×0 · .c ×0 · .d ×11 · .e ×1 · .f ×4 · .g ×0 · .h ×1 · .i ×1 · unit-unassigned
library-correctness ×4 · riders-and-notices ×5**. Two units book **zero** rows and say so in the
open (§3, §8) — an empty section is a swept axis, not a missing one. **+12 born-RED gate
candidates** (§13) and **+11 sharpenings of existing W9 gates** (§13b) and **+6 bounds deltas**
(§14). The **IN side is a receipt, not document-internal arithmetic**: 28 source records, ~60
routing citations; the citation set is enumerated NOWHERE in this file as a list, only as the
`⟨…⟩` lines the rows carry, so no in-equals-out identity is asserted.

### 1a. Corpus challenged — corrections, never silent

1. **The identity-collapse law, applied six times.** Six records book the **manifest dependency
   cycle** and six book the **config-parity drift** — twelve citations, **two** identities. They are
   folded ONCE each (W9.16, W9.19) with all witnesses on the provenance line. Warrant:
   PalettesPane PP-20's own identity guard — *"if a library-band row later claims this fact, it is
   ONE identity homed here"* — and the anti-rename law (banked ids are original for life).
2. **A collapse across two ledgers is written, never silent.** `picker-componentsliders-consolerail`
   C-4 stands in TWO ledgers (CC-081 → X-W9 · CC-068 → X-W6) and instructs, verbatim: *"if the
   X-wave authoring collapses them, the collapse must be written, not silent."* **NOT COLLAPSED.**
   W9.20 books the X-W9 limb and §15 declares the X-W6 edge; both waves must cite the row.
3. **Two live ownership DISSENTS are recorded, not resolved.** (a) The Result-consumption surface:
   `wb-gradient-easingauthoringstage` EAS-21 books it **X-W9**; `wb-mix-pane` F-4 books it
   **NO-WAVE-OWNER** with a stated reason (*"X-W9 owns only parser R1; this is adjacent, not
   identical"*) — W9.8. (b) Config parity: `PaletteCard` PC-17 books it **X-W9**; EmptyState ES-18,
   ErrorBoundary EB-15 and wb-gradient-visualizer GV-9 book it **NO-WAVE-OWNER in X** — W9.19. A
   fold seat may not pick; both are carried with their warrants and routed to X-W0.
4. **Two dated corrections against `library-band.md`** (the wave's own audit input, already folded
   into the spec — see §11): **(i)** RD-6's *"two hand-rolled hex implementations measured"* is a
   **dated reading**; the component corpus measures **four** `padStart(2` demo homes (EY-40) and
   **five** consumer re-implementations (SFB-15). The census moves; RD-6's disposition (`toHex`
   SHIPS) does not. **(ii)** RD-3 pinned the `_2` semantics — *"the gate asserts occurrences,
   58 → 0"*. Four records report **25** (lines). Both are true under their own units; the records'
   25 is **NOT** a competing count and no seat may manufacture a discrepancy from it.
5. **A defect in the dated spec, found by the fold, not by an ask.** `X-W9.d`'s own mechanism text
   says *"`isSupportedSyntaxDescriptor` goes `@internal`"*. This seat's *[filename-only census]*
   places that symbol in **`src/css/syntax.ts`** and `src/css/stylesheet.ts`; §File Bounds contains
   only the latter. The spec cannot execute its own sentence. §14 delta 4.
6. **The `src/color/model.ts` collision is an ADJUDICATION, not a bounds edit, and is escalated
   rather than assumed.** Four rows (W9.5, W9.11, W9.24-adjacent, W9.25) require reading from or
   re-exporting `SPACE_SCHEMA`/`SPACE_IDS`/`Color<S>` — *[filename-only census, this seat]*:
   `SPACE_SCHEMA` and `SPACE_IDS` live in `src/color/model.ts`. The spec bars that file **twice, by
   two different rules**: §File Bounds "Do NOT touch" (the **file**) and §Triumvirate Dispatch (the
   file's **conversion mathematics**). Those are not the same rule and the spec does not distinguish
   them. No fold row assumes a reading. → X-W0, §14 delta 6.
7. **Epistemic statuses are carried verbatim, never upgraded.** AboutPane's d3-jzazbz anomaly is
   carried with its own disjunction intact (*"…**or the evidence row is invalid**. Re-run with a
   URL-echo assertion"*); PS-26 is carried as **adopted, not re-run by its own seat**;
   picker-spectrumcanvas C-4's live readout-collapse sequence stays **UNPROVEN**; shell-paneslot
   L-10's harm framing stays **hypothesis** and its npm-4.0.0 baseline comparison stays **UNPROVEN**.

---

## 2. X-W9.a — Prototype-reachable lookups and empty-body totality (3 rows)

**W9.1 · wb-gradient-codeeditor A-1 (+ wb-mix-pane R-MP12) — THE R1 THROW CLASS IS
`<any-ident>()`, NOT `<color-function>()`** (cl.1 · BLOCKER at source)
`parseCssColor` is declared total (`ParseResult`, `src/css/types.ts:25-27` — **no throw channel**)
and throws on the closed empty-call class. Mechanism verbatim: *"the class is `<any-ident>()`, not
`<color-function>()` — grammar.ts:279 matches `^([a-z][\w-]*)\(` for ANY ident before dispatching
:181 `slash[0]!.replace(...)`, where splitTopLevel (:79/:85) has dropped every empty part."*
Re-proven against dist by the adjudicating seat: **`oklch()` AND `calc()` AND `foo()` all throw the
same TypeError from `parseCssColor` DIRECTLY**. The surface is internally inconsistent on the same
input: `parseCssValue("foo()")` → `ok:false` while `parseCssScalar("foo()")` **throws**
(`parseScalarInternal:316` consults the colour parser first); `parseStylesheet("a{color:oklch()}")`
throws while `"a{width:foo()}"` returns `ok:false`.
**Cure-lock (verbatim)**: *"→ **X-W9** (library totality — the type becomes true) … **Cure lock:
totality alone is INSUFFICIENT — see A-2.**"*
*Sharpens*: **G2** (W9.md:344), whose 56-vector battery names only the **nine colour functions**
(`oklch rgb hsl lab color rgba lch oklab hwb`, born-RED 9/9). The battery's corpus is **narrower
than the shipped defect**; `calc()`, `foo()`, and the three-entry-point asymmetry are not in the
nine. The §Archaeology guardrail *"the battery lands RED first … a rename cannot satisfy it"*
is satisfiable by a corpus that misses two thirds of the class unless the vector set widens here.
*Sequencing*: the widened corpus must be inside **Commit Plan row 1** (the RED-first commit), not
added after the cure.
⟨wb-gradient-codeeditor.md · A-1 (D-1 / D-r2 §3.1 / C-R15 / L-1 + reader-2 miss M-A) · wb-mix-pane.md · R-MP12⟩

**W9.2 · wb-gradient-codeeditor A-2 — THE MIRROR FAMILY: the same two lines produce SILENT WRONG
ACCEPTS, and no W9 gate can see them** (cl.2 · BLOCKER · **GENUINELY NEW**)
The SAME two lines (`grammar.ts:79/:85` empty-drop) fail in the opposite direction. Re-measured by
the adjudicating seat, **all invalid CSS, all `ok:true`**: `oklch(/ 0.7 0.1 145)` · `rgb(/ 1 2 3)` ·
`rgb(1 2 3 /)` · `oklch(0.7 0.1 145 / )` · `oklch(0.7 0.1 145/)` · `rgb(,,,255 0 0,,,)` ·
`rgb(1,2,3,)`. **Slash POSITION and comma ARITY are destroyed before `:177-181` ever sees the body.**
**Cure-lock (verbatim, load-bearing)**: *"L-1's cure #1 (guard the index) stops the crash and leaves
every accept standing; a 'validity oracle' (gradientParse.ts:16) that says yes to `rgb(1 2 3 /)` is
worse than one that throws, because nothing downstream can detect it. **→ X-W9, gated in BOTH
directions (throw class → ok:false AND accept family → ok:false); cure #1 alone may not ship.**"*
**Superlative preserved**: *"the demo's own `splitTopLevelCommas` (gradientParse.ts:52-67 keeps
empties, :210-211 rejects them) is the correct primitive"* — the cure has a working exemplar in-tree.
*Gate*: **NG-1** (§13). Every W9 gate on this seam (G1 · G2 · G3) asserts **non-throwing**; a cure
that returns `ok:true` on all seven vectors closes **every one of them green** while shipping the
worse defect. This is the fold's single largest gate-set hole.
*Sequencing*: same RED-first commit as W9.1; the two directions land as one battery.
⟨wb-gradient-codeeditor.md · A-2 (reader-2 miss, ratified; no prior corpus id)⟩

**W9.3 · MERGED — the `"none"` protocol must become UNREPRESENTABLE-TO-FORGET, not held by
discipline** (cl.2 · BLOCKER ×3)
One banked identity (**picker-componentsliders R-2**) with three widening witnesses. Library-side
mechanism: `finishChannels`/`isPowerless` write the `"none"` sentinel — **256/256 greys emit `"none"`
hue in both lch and oklch** (residual chroma ≈ 5.6e-16 under `c <= 0.000004`), and **28 of 256 greys**
round-trip to hsv-powerless. Consumer-side consequence: `channelNumber` throws `Missing lch.h` in
**render** (CNL-2, two ordinary clicks kill the pane grid; ErrorBoundary paints over the whole grid
and Try-again re-throws) and in **root setup** outside any boundary (CNL-3, boot white-screen,
self-perpetuating via the persisted seed; `?color=black` is the minimum trigger).
**shell-dock-parseechoreadout A-1 FOLDS INTO R-2 and ADDS to its enumeration**: `astEcho`
(`useColorParsing.ts:97`) and `gamutVerdict` (`:108`) as throw sites, *"with the R⑤ asymmetry (Route
A throws astEcho only)"*. **All three r2 axes at that record asserted the opposite** — *"the corpus's
largest shared error"*.
**In-tree contradiction, preserved**: `valueDomain.ts:21-22` declares `none` pass-through *"the
library's law"* and `test/v4-c1.test.ts:186-192` **pins it as intended** — while `channelNumber`
treats it as a hard error.
**Cure-shape (verbatim)**: *"the `\"none\"` protocol must be unrepresentable-to-forget at the demo
boundary — the guarded/unguarded sibling arms are the proof the convention cannot be held by
discipline"*; and CNL §superlative 5: *"the convention must move into the types (X-W9), not the
discipline."*
**ESCALATION LOCK (verbatim, ARMED AT OPEN)**: *"**If X-W9's cut excludes demo seams, this row
escalates to the boundary with rider A-1.**"* W9.md §COMPLETABLE item 1 declares the wave
**library-only** ("No browser, no π, no visual oracle") — so on the spec as written this clause
**fires**. It is not a defect of either text; it is a stated conditional that must be answered at
open, not discovered at close.
*Sequencing*: library half → **X-W9.a** (CC-084's `Result` battery) · demo Result propagation →
**X-W5** · born-RED gates → **X-W1** (two clicks asserting no `role="alert"` fallback; boot seed
matrix; the none-URL query-parameter state in the deploy matrix).
⟨picker-componentsliders.md · R-2 (banked identity) · shell-dock-parseechoreadout.md · A-1 (FOLD→R-2) · ColorNutritionLabel.md · CNL-2 (reader-L miss 1, rescoped) + CNL-3 (reader-L miss 2, sharpened)⟩

---

## 3. X-W9.b — Transform totality and the matrix retirement (**0 rows** — swept, empty)

**No row in the 88-record value.js corpus routes to `./transform` work.** This is a positive
finding, not a gap: the transform surface has **zero demo consumers** to file against it — corroborated
from the other side by **W9.15**'s measured consumption census (`/transform` **0** across
demo/test/e2e) and by CC-094's own measured zero-consumer census for the six deleted matrix symbols.
`X-W9.b` therefore carries **no corpus-side sharpening and no corpus-side risk**; its gates
(G6..G10, G27) stand exactly as dated. Recorded so a later checker reads an executed sweep, not an
oversight.

## 4. X-W9.c — `./math` failure protocol (**0 rows** — one pointer, no booking)

No corpus row books at `.c`. **Pointer only**: W9.7's `/math` barrel-contract limb concerns
`src/foundation/math.ts:113-120` (`cubicBezierToString`) — a **published-surface** decision, booked
once at `.d`/`.f`, not a precondition row. `.c`'s dated scope (`deCasteljau`/`interpBezier`/
`lerpArray`/`scale` preconditions, G11) is untouched by this corpus.

---

## 5. X-W9.d — Public-surface law and module topology (11 rows)

**W9.4 · MERGED — the `_2` mangle and the dist-artifact skew** (cl.1 · four witnesses)
`dist/subpaths/css.d.ts` (382 lines) duplicates `/color`'s type vocabulary as `_2` privates **under
an unchanged 4.0.0** — artifact/repo divergence. Re-verified independently at three records
(**25 `_2` hits / 382 lines**); one record re-ran shasum and found **css SKEWED, 25 `_2` hits vs 0
published**.
**Cure-shape (verbatim, shell-paneslot L-10)**: *"one shared foundation declaration, per-subpath
rollup change."*
**Unit semantics pinned (§1a n.4-ii)**: **G14 asserts OCCURRENCES, 58 → 0** (library-band RD-3). The
records' 25 is **lines**. Both true; not a discrepancy.
*Epistemic status carried*: shell-paneslot L-10's harm is *"labeled hypothesis by its own axis"* and
its npm-4.0.0 baseline comparison is **UNPROVEN** — not upgraded here.
*Sharpens*: G14 (unchanged predicate; four independent consumer witnesses added) and G17 (the
colour-boundary cure is what deletes the mangles).
⟨AdminNamesPanel.md · L-13 · picker-componentsliders.md · L-3 artifact-skew limb + R-10 skew limb · shell-paneslot.md · L-10⟩

**W9.5 · MERGED — THE LIBRARY OWNS THE SPACE TAXONOMY AND PUBLISHES NO CHANNEL DOMAIN**
(cl.1 · MAJOR ×8 witnesses · **GENUINELY NEW — `library-band.md` has ZERO `ChannelSpec`/`CHANNELS`/
channel-domain rows, this seat's grep**)
The densest single cluster in the fold. Mechanism, by witness: **SPACE_SCHEMA / SPACE_IDS /
isAnyColor / makeColor are absent from the published surface**, so the demo cloned the whole
**17-space channel-range table** (`picker-color.ts:52-70`, **byte-diffed identical**) — L-2. The
table is declared **FOUR times** (library `SPACE_SCHEMA` + `anchors`' `HUE_INDEX` + `css/grammar`'s
`CSS_COLOR_SPACES` + the demo clone) and the authoritative copy's `hueIndex`/`css` fields plus
`SPACE_IDS` are **dead** — M-10. The **kelvin bound has two homes in two packages**
(`src/color/model.ts:91` vs `picker-color.ts:62`); the other 16 spaces' domains have **no library
counterpart**; `normalizedChannel` is *"entirely demo knowledge"* — R-11. `SPACE_SCHEMA` carries **no
ranges/units** against `ARCHITECTURE.md`'s claim, giving **five** declarations of "the channels of a
space" with **no owner**, two of which contradict on one page — CNL-13. The display-scale predicate
is copy-pasted at **exactly 5 sites** — CNL-14. The privacy is *"the direct cause of the
string-typed `savedColorStrings` + `indexOf` comparisons"* — L-15, terminal at X-W9.
**Cure-shape (verbatim, R-11)**: *"→ **X-W9** (the `CHANNELS`/`ChannelSpec` export on the 4.1
surface; `PICKER_CHANNELS` demotes to a re-export)."* CNL-13: *"the schema becomes the published
authority — the ARCHITECTURE claim becomes true or the doc is corrected."* CNL-14: *"the predicate is
a schema fact (`ChannelMeta` should carry it); **consumer dedup follows the schema, not the
reverse**."*
**LOCK — the W9.h ruling does not pre-decline this cluster.** `X-W9.h` rules *"**No new library
export**: the id vocabulary already exists and a second one would be a duplicate authority."* That
ruling is about the **ID VOCABULARY** (`ChannelsBySpace`). This cluster asks for the **DOMAIN**
(ranges · units · `leastCount` · display-scale · the kelvin bound), which `ChannelsBySpace` does not
carry. The two must not be conflated at execution.
**LOCK — the field is not empty at open.** R-12 routes `leastCount` to **X-W6** *"beside
PICKER_CHANNELS now; X-W9's `leastCount` on ChannelSpec is the **terminal** home"*; R-22 routes
alpha's meta the same way. W9 must consume an X-W6 landing, not assume a clean field.
**LOCK — a live 100× disagreement (R-22, verbatim)**: *"the dead alpha literal says `max: 1` while
READOUT_CH's live alpha row says `max: 100` … and `meterText` scales ×100 — THREE implicit homes with
a 100× disagreement, benign only because one is dead; **typing the dead literal … would make the
wrong bound live** and flip `readoutCh`'s `%`-scale branch."*
*Sequencing*: M-10 is *"sequenced with L-2"*. W9.25 is this row's **falsifying datum** (a published
domain whose ranges cannot admit white is worse than none).
*Bounds*: §14 deltas 1 (`anchors.ts`, for `HUE_INDEX`) and **6** (`model.ts`, the adjudication).
⟨picker-spectrumcanvas.md · L-2 + M-10 · picker-componentsliders.md · R-11 + R-12 + R-22 · ColorNutritionLabel.md · CNL-13 + CNL-14 · CurrentPaletteEditor.md · L-15⟩

**W9.6 · MERGED — the contrast primitive is module-private; consumers infer ratios by ORACLE-ABUSE**
(cl.1 · BLOCKER at one witness · **GENUINELY NEW**)
`luminance`/`contrast` are unexported; `src/subpaths/color.ts` exports **no contrast primitive** and
`contrast` is module-private. Corroborated independently by this seat's *[filename-only census]*:
**`contrastRatio` = 0 files in `src/`**. Consequence measured at two records: `contrastInkFor`
(`ink.ts:157-174`) **rebuilds a PREDICATE from a SOLVER** by endpoint-walk inference, and
`ink.ts:138-142` certifies-then-diffs-every-channel with **1e-9 epsilons**. The demo tail (`?? ""`,
`:208`) then *"converts the library's honest `contrast_unreachable` refusal into an unmarked lie"*.
**Cure-shape (verbatim, CNL-4)**: *"**X-W9 — publish the contrast primitive (`contrastRatio` or
equivalent), retiring the epsilon-identity idiom**; the consumer's atomic fill+ink pair (one computed
yielding both or neither) rides the About recomposition bundle → NO-WAVE-OWNER (AB-17 / rider A-1);
the gate that let it ship is CNL-5's."*
**Split declared**: the **library half is W9's and is IN BOUNDS** (`src/color/operations.ts` +
`src/subpaths/color.ts`); the **consumer's atomic pair is explicitly NOT W9's** and is already
NO-WAVE-OWNER. A seat that cures both has overrun its bounds.
*Superlative preserved (CNL-4)*: *"The F-3 split's architecture is right."*
*Gate*: **NG-3** (§13).
⟨ColorNutritionLabel.md · CNL-4 (C-2 + L-11 as root) · wb-extract-controls.md · EC-13 (L-18)⟩

**W9.7 · MERGED — `/css` PARSES TIMING FUNCTIONS AND CANNOT SERIALIZE THEM; `/math` SHIPS THE WRONG
ONE — and both halves are ONE decision** (cl.2 · MAJOR ×8 witnesses · **GENUINELY NEW**)
No `serializeTimingFunction` anywhere in `src/`. Half A (AST→callable, incl. the `linear()`
position-fill) lives at `useGradientCSS.ts:80-117`; half B (value→literal) at
`easingCatalogue.ts:38-56` — *"a byte-mirror of glass-ui's private `toFixed(3)` mint with no test
binding the two"*. **Three mints across two packages, guarded by exactly ONE e2e assertion.** The
library's own emitter (`cubicBezierToString`, `src/foundation/math.ts:113-120`, + its
`src/subpaths/math.ts` export) is *"unused, 2dp-lossy, test-pinned wrong law"* AND **violates
`/math`'s own barrel docstring** — *"parse-that-FREE … zero CSS grammar"* (`src/subpaths/math.ts:1-5`)
— by being a `cubic-bezier(…)` emitter. *[filename-only census, this seat: both files confirmed, both
already in §File Bounds.]*
**Cure-lock (verbatim, L-2)**: *"**The missing serializer and the R4-1/GV-12 retirement are ONE X-W9
decision.**"* And R4-1's ruling: *"the retire-vs-move decision is X-W9's, and **either arm is a
public-surface change riding a version cut**."*
**Attached limbs, each with its own lock.** **EAS-20**: `/css` and `/easing` **disagree by type** on
a linear-easing stop (`CssLinearStop` input tuple vs `LinearEasingStop` input number) and the 25-line
inference bridge is **statically dead** (`resolvedEasingCache` never receives an entry) — cure caveat
sustained verbatim: *"the transposition is a deletion **plus a NEW library surface for an algorithm
with zero live coverage**."* **EAS-19**: three cross-repo easing→SVG-path implementations (demo
`glyphPath` / glass-ui `bezierPathD`+`stepPathD` / keyframes `generateCurveSVGPath`) →
*"X-W9 (`easingPathD` on `/easing` per W9's export band) + GLASS-RELAY adoption note"*. **EAS-17**:
byte-identity across a package boundary is maintained **by prose** (`easingCatalogue.ts:38-51`) and is
**load-bearing for identity** (`:220` `t.css === interval.css`) — a live consumer whose correctness is
byte-fidelity. **EAS-21** rides at W9.8.
**Fence clarification owed (G25).** `serializeTimingFunction`/`easingPathD` are **additions to
`./easing`**. G25's fence covers `bezierPresets`' **30-key set** and the 40-name catalog — an
addition there *"silently mutates their public registry"*. New **function** exports are not catalog
keys; the spec must say so at open or the additions read as fence breaches.
*Sequencing (verbatim, C-14 ≡ M-ESS-9)*: *"`bezierLiteral`/`stepsLiteral`/`SpecimenFamily` exported
with zero external consumers — INFO → X-W6.d, **sequencing-coupled to X-W9, not silently absorbed**."*
*Gate*: **NG-5** (§13).
⟨wb-gradient-easingspecimenstrip.md · L-2 + R4-1 + C-14 ≡ M-ESS-9 · wb-gradient-visualizer.md · GV-12 + GV-51 · wb-gradient-easingauthoringstage.md · EAS-17 + EAS-19 + EAS-20⟩

**W9.8 · DISSENT — `/easing` (and every subpath) exports NO `Result` consumption surface**
(cl.1 · MAJOR · **OWNERSHIP CONTESTED, NOT RESOLVED HERE**)
The library publishes **two incompatible failure algebras** (`Result.error` vs
`ParseResult.diagnostics`); **no combinator — not `ok`, not `err`, not any unwrap — reaches ANY of the
seven published subpaths** (reader-LC verified all seven). Consequence: **18 hand-rolled unwrap
adapters** in demo (11 non-test), four of them wrapping `mixColors`; *"three subpaths, three
disclosure policies"*; duplicate `easingValue` adapters byte-similar across
`useGradientCSS.ts:71-77` / `easingCatalogue.ts:98-104`.
**THE DISSENT.** `wb-gradient-easingauthoringstage` **EAS-21 → X-W9**. `wb-mix-pane` F-4/D-2(L)/E-5
→ **NO-WAVE-OWNER**, verbatim: *"The library limb — publish the algebra's consumption surface — is
library-owned: → **NO-WAVE-OWNER** (X-W9 owns only parser R1; **this is adjacent, not identical**)."*
Both are adjudicated; neither is a draft. **Carried, not collapsed.**
**Bearing, stated so the dissent is decided on the right question.** `X-W9.d`'s §Sub-gate rules
*"`ParseResult` (text→AST) and `Result` (value→value) stay **declared per boundary, never unified**
(PSL-3)"*. That settles **UNIFICATION**. It says nothing about **PUBLISHING the combinators**, which
is what both records ask for. The two questions are separable and PSL-3 does not answer this one.
→ **X-W0**.
⟨wb-gradient-easingauthoringstage.md · EAS-21 (N-5) · wb-mix-pane.md · F-4/D-2(L)/E-5⟩

**W9.9 · MERGED — the quantize surface: four disagreeing homes, five bare `number`s, one
zero-consumer export** (cl.1 · MAJOR · **GENUINELY NEW**)
`src/quantize.ts:39-50` enforces **k ∈ [1,64] int** and **chromaWeight ≥ 0**; `dominantColor`
hardcodes **k:5** (`:132-139`); demo defaults are 5/0.5; the UI offers **1..16 & 0..1.5**;
GenerateControls offers **1..12**; and `quantize.d.ts` publishes **five bare `number`s** — four
disagreeing homes for one domain, none of them the type system. Sibling witness: `dominantColor`
ships with **zero production consumers**, a **hardcoded second full k:5 re-quantize**, and an
**untyped population-descending guarantee the demo re-derives in 22 lines**
(`useExtractSession.ts:8-10` — *"telling the library its export is the wrong tool — simultaneously a
superlative and an indictment"*).
**Cure-shape (verbatim, EC-17)**: *"→ **X-W9** (surface/brands) + the demo range-coherence arm rides
the session cluster."* The demo arm is **not W9's**.
**Bounds fact recorded**: `src/subpaths/quantize.ts` is already bounded (via `src/subpaths/*.ts`);
**`src/quantize.ts` is not** — §14 delta 2. Companion fact: library-band's re-derived 297-`!` census
gives **quantize 18** — the fourth-largest `!` site in `src/`, and **G10's scope
(`src/transform/**` + `src/foundation/**`) does not reach it**; neither does G5's (`src/css/**`). The
wave's lint law leaves this file uncovered by construction.
⟨wb-extract-controls.md · EC-17 (L-23) · wb-extract-workbench.md · XW-24 (L-8+M-1)⟩

**W9.10 · MERGED — the root `.` entry decision must be WRITTEN, and a downstream comment is
corrected in the same act** (cl.2 · MINOR)
`demo/shared/utils.ts:16` justifies its debounce fork **by citing a library root-barrel export that
does not exist** — no `"."` key, no root d.ts, `ERR_PACKAGE_PATH_NOT_EXPORTED` (re-verified at both
records; `grep debounce src/` = 0).
**Cure-lock (verbatim, PS-28)**: *"**rider on X-W9.d** — whichever way W9.d rules the root-`.`
question, this comment is corrected **in the same act**."* XP-22: *"demo half **X-W8** (explicitly:
W8.md:163 'demo/shared/utils.ts; re-home `debounce`'); the root-entry decision itself is **X-W9**'s."*
*Sharpens*: **G12** leg 3 (born-RED `TS2307` + `ERR_PACKAGE_PATH_NOT_EXPORTED` on the root) — the
gate already measures the fact; the fold adds that a **ruling must be written**, since a green G12
is reachable by either answer.
*Sequencing edict*: the ruling lands **before or with** X-W8's `debounce` re-home, or X-W8 corrects a
comment against an unruled question.
⟨PreviewStrip.md · PS-28 (L2-9b) · wb-extract-pane.md · XP-22 (L-9)⟩

**W9.11 · PreviewStrip PS-14 — `Color<S>` is NON-DISTRIBUTIVE; nine demo casts defeat the type
system** (cl.2 · MAJOR · **NAMED GAP, verbatim**)
*"the published `Color<S>` is non-distributive, so every space-generic demo call site defeats the
type system: **9 casts**, two of them (`sample.ts:82`, `picker-color.ts:116`) inside this component's
stop-producing closure."* The demo-side-only cure is **KILLED** by measurement (reader-LC's 8-module
compile: baseline **0** → alias-swap **15** errors → distributive-alias **6** errors); the
`Extract<AnyColor,{space:S}>` mint is *"the load-bearing adapter **until the library publishes a
distributive `ColorIn` and retargets `convertColor`/`mixColors` return positions**."*
**Cure-lock (verbatim)**: *"**DISPOSITION: X-W9.d (public-surface law) with a NAMED GAP** — `ColorIn`
appears in no W9 mechanism text; **adopt explicitly or degrade to NO-WAVE-OWNER**."* Already tracked
at `COHESION.md:113`.
**Bounds collision**: retargeting `convertColor`/`mixColors` **return positions** is a **type** edit
that reaches `src/color/model.ts` — the same Do-NOT-touch ∕ Triumvirate ambiguity as W9.5. §14
delta 6.
⟨PreviewStrip.md · PS-14 (L2-3)⟩

**W9.12 · wb-gradient-codeeditor A-16 — empty-segment arity, and the PER-NODE SPANS decision the
library has never been asked for** (cl.2 · MAJOR · **GENUINELY NEW**)
*"the REAL library gap is empty-segment arity (`red,,blue` → args=2, **indistinguishable**). K5
stands on my own read of `src/value.ts:3-20`: `CssScalar`/`CssCall` carry **no source text or
spans**, so the prescribed thin-projection cure would **DELETE the authored-literal feature**
(`gradientParse.ts:18`, `:252/:256`) unless the library first ships **per-node spans** — an ask no
filed cure makes. **→ X-W9** (empty-segment fidelity + the spans decision at the library boundary);
the demo projection re-homes **only after that lands** (X-W6 rider, sequenced)."*
**A kill carried**: the demo docblock's justifying premise is **FALSE** — *"the library's tree DOES
distinguish `red 30%, blue` from `red, 30%, blue`"*.
*Bounds*: `src/value.ts` is **not** in §File Bounds — §14 delta 3 (*[filename-only census]* places
`CssScalar`/`CssCall` in `src/value.ts`, `src/css/types.ts`, `src/subpaths/value.ts`).
*Cross-edge*: X-W6's demo projection re-home is **downstream** of this decision — an edge W9 does not
currently declare (§15).
⟨wb-gradient-codeeditor.md · A-16 (L-2 + kill K5 ratified)⟩

**W9.13 · wb-gradient-visualizer GV-50 — 635 lines of library-grade code in `demo/`, and the
library's own suite already votes them library code** (cl.1 · MAJOR)
*"`grep -rli gradient src/` → 0; gradientParse.ts 301 L zero-Vue; **10 test files under `test/` import
demo paths**. A promotion decision on the published surface. → **X-W9 coordination, else
NO-WAVE-OWNER** (X-W6 may not touch `src/**` by its own Do-NOT-touch row)."*
**Lock**: X-W6 is **structurally barred** from taking it, so an X-W9 declination lands this at
NO-WAVE-OWNER — a consequence, not an option, and it must be written at whichever seat declines.
**Ratchet interaction, declared**: promoting ~635 lines into `src/**` **moves G16's max-LoC census**.
If W9 declines, moot. If W9 accepts, **G16's reading is restated in the same act** — a ratchet whose
denominator changes silently is the shape R-T1 exists to prevent.
⟨wb-gradient-visualizer.md · GV-50 (R2-3)⟩

**W9.14 · MERGED — the serializer PRECISION POLICY: declined twice, named twice, owned nowhere**
(cl.4 · notice with a decision owed)
*"five colours ≈294 chars of **12-decimal oklch** reach the clipboard and localStorage — a
**library-wide serialization posture (src/css)**, not a component defect; a demo-side rounding would
mint a second divergent precision policy … → **NO-WAVE-OWNER** (serializer policy; V·π/X-W9 adjacency
noted, **not booked**)"* (GEN-39). *"the `serializeCssColor` precision contract is library-side and
rides **X-W9 awareness**"* (XP-10).
**Bearing**: W9.d **publishes `serializeCssValue`** (G15) and joins it to the `Result` idiom *before*
export. If it ships **without a precision clause**, the policy question ships **unowned into 4.1** on
a newly-published symbol — the exact shape both records declined to create locally. Two records
naming X-W9 as adjacent and neither booking it is the fold's signal, not its silence.
⟨wb-generate-pane.md · GEN-39 (C-9 post-kill) · wb-extract-pane.md · XP-10 (X-3)⟩

---

## 6. X-W9.e — Oracle truth, coverage truth, packed-surface truth (1 row)

**W9.15 · wb-extract-pane XP-28 — two of seven published subpaths have ZERO dogfood** (cl.1 · INFO)
Re-counted exactly: *"/color **23** · /css **9** · /quantize **3** · **/value 0 · /transform 0**
across demo/test/e2e."*
*Sharpens*: **G19**, whose denominator is **declared surface** (142 declared export names / 79 runtime
exports over 7 subpaths). This row supplies the orthogonal **consumption** census, which G19's
published artefact should carry beside it — a coverage number over a surface with two zero-consumer
subpaths means something different from the same number over a consumed one.
**Does NOT re-open** W9.d's closed ruling (*"`./value` is not deleted"*, shape (ii) rejected); it is
the measured datum **behind** that ruling, and it corroborates §3's empty sweep for `.b`.
⟨wb-extract-pane.md · XP-28 (L-10, re-counted)⟩

---

## 7. X-W9.f — The 4.1.0 cut (4 rows)

**W9.16 · MERGED — THE MANIFEST DEPENDENCY CYCLE: six witnesses, one identity, and a NAMED GAP in
the cut** (cl.2 · MAJOR ×6)
`package.json` `dependencies` = **exactly** `{"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}`
with `files:["dist",…]`, while **`src/` imports neither** (multiple independent greps; keyframes.js is
imported **nowhere in the repo**, dev or prod) and the **10 emitted ESM files contain ZERO bare
specifiers** (`libraryExternal: []` by construction). Installed glass-ui declares
`peerDependencies["@mkbabb/value.js"]: "^4.0.0"` — **a declared manifest cycle**, and *"the shape
value.js fails to mirror"*. `eslint`'s **inv-K-1 polices specifiers, not the manifest**, so the guard
that defends this topology one level down cannot see it. PS-13: *"The already-public 4.0.0 manifest is
**the only shipped defect in this corpus**."*
**Cure-lock (verbatim, PS-13)**: *"**X-W9.f** (the 4.1.0 cut edits package.json; G29/G31 already read
the manifest) with a **NAMED GAP** — **no X file names the devDependencies move; W9.f must adopt this
row explicitly or it degrades to NO-WAVE-OWNER at execution**."* ABT-15 gives the target:
`→ devDependencies`. The easingeditor record confirms the mechanism from the other side: *"X-W9 G31
ALREADY measures this exact dependency map (my W9.md:373 read), so the reclassification rides the wave
that owns the manifest."*
**Kill preserved (K-11)**: the r1 framing *"the published library runtime-depends on its demo's design
system"* is **KILLED as stated** — *"r1's wording must not reach a ledger row."* Only the
**manifest-form** row is booked. **Also killed** (PS-13, K-9): the self-install/6MB-per-consumer
framing.
**Identity guard (verbatim, PP-20)**: *"if a library-band row later claims this fact, it is ONE
identity homed here."*
*Sharpens*: **G31**, which today asserts only the **parse-that absence** over that map. The
reclassification needs its own leg — **NG-9** (§13).
*Sequencing*: same commit range as the version bump (G29's one dated cut).
⟨EmptyState.md · ES-14 (L-2) · PalettesPane.md · PP-20 (L/L-12, CC-008) · PreviewStrip.md · PS-13 (L2-1) · shell-dock-actionbartoggle.md · ABT-15 (L-10) · wb-extract-controls.md · EC-14 (L-20) · wb-gradient-easingeditor.md · L-r1:L-1-corrected⟩

**W9.17 · MERGED — the hex writer: SATISFIED-BY-SPEC, census corrected upward** (cl.1 · MAJOR)
*"The published `@mkbabb/value.js` surface has **no hex writer** — parses `#rrggbb`, **cannot print
it**; five consumer re-implementations incl. `MiniColorPicker:103`; glass-ui carries
`oklchStopToHex` — **capability flowing the wrong way on the inv-K-1 edge**. The only corpus finding
naming a cause, and it invalidates pass-1/2's prescribed cures (no library call to make at the last
hop)"* (SFB-15; `toString(16)` in `src/` = 0). *"hex serialization has **four** demo homes
(`padStart(2` census = exactly 4, re-run by this seat) because the published surface has none"* (EY-40).
**Status**: `toHex` **already SHIPS** in the dated cut (G22; library-band RD-6). This row does not
add an ask — it **corrects the census** (§1a n.4-i): RD-6's *"two hand-rolled hex implementations"*
becomes **four `padStart(2` homes / five consumer re-implementations**. Disposition unchanged;
G22's falsifier unaffected.
*Sequencing (verbatim, SFB-15)*: *"consumer swaps **follow the library**, cross-note X-W7."*
⟨SearchFilterBar.md · SFB-15 (LP5-1) · wb-extract-imageeyedropper.md · EY-40 (L-22)⟩

**W9.18 · PreviewStrip PS-26 — the working tree does not reproduce the published bytes**
(cl.4 · INFO · discharged BY the cut)
*"working-tree `dist/subpaths/css.js` ≠ the 4.0.0 registry artifact by **1 byte** of entry-local
minifier allocation (chunks byte-identical; behaviour-neutral) — **proves `npm run prepare` does not
reproduce the published bytes**. **DISPOSITION: FOLD → X-W9.f — a fresh dated 4.1.0 cut regenerates
dist and moots the divergence (G29).**"*
*Epistemic status carried verbatim*: *"Not re-run by this seat; reader-LC's cmp loop adopted."* No
gate changes; the row is discharged by the act, and its status is recorded so no seat upgrades it into
a reproducibility claim the corpus never made.
⟨PreviewStrip.md · PS-26 (L2-4)⟩

**W9.19 · DISSENT — the config-parity standing identity (`tsconfig.demo.json` ↔ the exports map)**
(cl.3 · MAJOR · **OWNERSHIP CONTESTED · UNEXECUTABLE AS THE SPEC STANDS**)
Banked identity ≡ **ColorSpaceSelector L-14** (born-RED generator-parity command), re-witnessed six
times. Mechanism, re-derived independently at three records: `tsconfig.demo.json:42-49` = **8 `paths`
keys** including a dead bare root + `parsing` + `units`, **missing `/css` and `/value`** (10 live
imports), against an exports map of **exactly 7 keys with no root**; the block's own header comment
*"CLOSED 8-key set"* is **false by one**. `dist/index.d.ts` missing; no `parsing.*`/`units.*` in
`dist/subpaths`. The component's only library import (`/math`) **short-circuits the exports map at
typecheck**, which is why the drift is invisible.
**Cure re-specified (EB-15)**: *"generate `paths` from `exports` as `vite.config.ts:28/:41` already
does runtime-side."* **Kill carried (K-3)**: the r2 cure's premise dies — `test/dist/` is **ENOENT
since `6d6d3521`**.
**THE DISSENT.** **PaletteCard PC-17 → X-W9** (*"the public-surface law / 4.1 cut"*). **EmptyState
ES-18 · ErrorBoundary EB-15 · wb-gradient-visualizer GV-9 → NO-WAVE-OWNER in X** (*"named residue;
nearest mechanism **X-W9.f's config carve, unowned by its text**"*; GV-9 adds *"X-W8 declined by
name"*). **Decidable fact that settles the executability, not the ownership**: `tsconfig.demo.json`
appears in **no** X-W9 §File Bounds row (it names `package.json`, `CHANGELOG.md`,
`eslint.config.js` — not the tsconfigs). **PC-17's booking cannot execute** unless §14 delta 5 is
adopted. Adopt or the row stays NO-WAVE-OWNER; a fold seat may not pick.
**Rider (PC-17, verbatim)**: *"this corpus adds the **header-comment falsehood** as a correction
obligation on whichever wave touches the file."*
**Companion (EB-33)**: `.gitignore:18-24`'s carve-out negations + `vitest.config.ts:22-27`'s exclude
filter preserve a `test/dist/` gate directory **deleted at `6d6d3521`** (and now *"silently
un-exclude any future `dist` under `test/**`"*) — *"the exact hygiene invariant that would have caught
EB-15's drift"*. → NO-WAVE-OWNER; nearest X-W8.b / X-W9.f config carves, **unowned by their text**.
⟨PaletteCard.md · PC-17 (L-45) · EmptyState.md · ES-18 (L-N5) · ErrorBoundary.md · EB-15 (L2-4) + EB-33 · picker-componentsliders.md · R-10 · wb-gradient-visualizer.md · GV-9 (≡ XP-21 ≡ MX-20) — one identity ≡ ColorSpaceSelector L-14, never re-minted⟩

---

## 8. X-W9.g — Canon-truth diff and parse-that reconciliation (**0 rows** — swept, empty)

No value.js corpus record touches `ARCHITECTURE.md:657`, `src/parsing/**`, or the parse-that decree.
G30/G31 stand as dated (G31 gains one leg from W9.16 — §13b). Recorded as an executed sweep.
**Adjacent, not a booking**: CNL-13 asks that *"the ARCHITECTURE claim becomes true **or the doc is
corrected**"* about `SPACE_SCHEMA`'s ranges — a **different** ARCHITECTURE region from `:657` and
`:943-945` (X-W8's). If the W9.5 cluster is declined, the doc-correction arm lands at `.g`; if the
cluster lands, the claim becomes true and `.g` is untouched. Stated so the arm is not lost either way.

---

## 9. X-W9.h — ICtCp and Jzazbz channel-descriptor resolution (1 row)

**W9.20 · MERGED — CC-081: the descriptor failure DOMAIN IS WIDER THAN THE SPEC'S TEXT, and the
space count disagrees** (cl.1 · MAJOR · **cross-booked, NOT collapsed**)
`componentDescription()` prefix-matches after an `any` cast. The spec names two cells (`cp` →
`"Ct (tritan)"`, `jz` → the bare key). The adjudicating seat **re-walked the whole function domain**
and confirms the amplification, verbatim: *"`jz` → nothing starts with 'J' or 'jz'
(`[\"Lightness (Jz)\", …]`) → the literal 'jz'; **`kelvin`** → `[\"Temperature (K)\"]` starts with 'T'
→ the literal 'kelvin'; **`alpha`** → 'A' matches nothing in any of the 13 entries → the literal
'alpha', **in all 18 spaces**"* — and *"`ct` is right only by array order"*, i.e. one of the two
"passing" ICtCp cells passes by accident.
**Two corrections owed to G32.** **(a) The failing set is not {cp, jz}** — it includes `kelvin` and
`alpha`, and **`alpha` fails in EVERY space**, so the born-RED count is a domain, not two cells.
**(b) A count disagreement stands unreconciled**: G32 says *"all **17** spaces"*; this record says
*"all **18** spaces"* and *"13 entries"*. **Neither number is adopted here.** The count is re-derived
at open **from `ChannelsBySpace` itself**, never carried from either text — that derivation is
exactly what `X-W9.h`'s own mechanism already declares as the authority.
**CROSS-BOOK LOCK (verbatim)**: *"one identity with CC-081's K-10 ICtCp row (ledger-routed → X-W9) ∩
CC-068's catalog totality (→ X-W6); **CROSS-BOOKED, not duplicated — the cure lands wherever the
catalog does, and both waves must cite this row so neither drops it**"* — and §6: *"**if the X-wave
authoring collapses them, the collapse must be written, not silent**."* **NOT COLLAPSED** (§1a n.2).
**Serialization limb (CC-081, second limb)**: `serializePickerColor` **re-serializes the two
non-CSS-serializable spaces AS `oklch(…)`** for the URL, so `?space=jzazbz` silently falls back while
`display-p3`/`rec2020` apply cleanly. **Epistemic status carried verbatim and NOT upgraded**:
*"Either deep-linking the two non-CSS-serializable spaces silently falls back (a REAL unreported
defect) **or the evidence row is invalid**. **Re-run with a URL-echo assertion.**"* CNL-26 routes the
route/deep-link half to **X-W5** and cross-refs CC-081; the **serialization** half rides X-W9.
*Sequencing*: X-W9.h already sequences after X-W4 closes on `ConsoleRail.vue` (CONFORMANCE W9-D2
FIXED, reciprocated at W4.md:196-197). The **X-W6 catalog edge is the third wave on this identity**
and is **not** declared in either file — §15.
⟨picker-componentsliders-consolerail.md · C-4 (+ §6 cross-booking risk) · AboutPane.md · observed-anomaly 2 · ColorNutritionLabel.md · CNL-26 (C-14, cross-ref)⟩

---

## 10. X-W9.i — Coordination packets (1 row)

**W9.21 · MERGED — the glass-ui packet's BODY accretes ten producer rows, and a COMPETING CHANNEL is
declared** (cl.2 · rider · packet-only, no source edit)
`X-W9.i` declares the glass-ui packet body as the **standing BH/BI relay + the R1/prototype input
class** (glass 7.0.0 imports `parseCssColor` and feeds it user-supplied strings). The corpus accretes
producer rows onto that same standing packet from seven records. Sharpest first:
- **AuroraPane AP-2** — *"the selected option paints zero pixels"*: `--glass-accent` is a
  **registered** property with `initial-value: transparent`, so the `var()` fallback chain **can never
  fall through**; neither token is assigned in demo (census 0); `aria-selected="true"` is announced,
  nothing is drawn. **A producer-default defect for every consumer.** Routing verbatim: *"**producer
  half → the standing glass-ui BH relay riding X-W9's coordination-packet step (§12)**, with the
  **corrected root cause (the registration's initial-value, not undefined-only fallthrough)**."*
  The corrected root cause is load-bearing — the uncorrected one names a cure that cannot work.
- **ErrorBoundary EB-9** — a **`./alert` subpath ask**: glass-ui ships `Alert`/`AlertTitle`/
  `AlertDescription` **from the root barrel only** — no `./alert` among its 74 exports.
- **ErrorBoundary EB-5** — the **plate/material tier** ask (the cure's export premise is TRUE:
  `./surface` and `./card` ARE among the 74). **EB-10** — a producer-owned failure-plate action
  variant (one control carrying three per-instance overrides is the evidence).
- **EmptyState** §GLASS-RELAY — **ES-30** (a `@container` legibility rule dead by source order against
  the `.watercolor-ghost-stroke` `border` shorthand; producer-side rule ordering or longhand) +
  **ES-15** and **ES-5** as producer *questions*, not asks.
- **SearchFilterBar** — `menuRowClass` unexported / bless `.interactive-item` as the consumer contract
  (**SFB-34**, per RULED #1: *"'blocked-upstream' would park a fixable row on a relay"*); PopoverContent
  height contract (**SFB-6**); the render-effect canary publication (**SFB-19**); d.ts attr gaps
  (**SFB-17**).
- **wb-gradient-easingeditor** — two asks (preset-rail suppression; Input trailing slot).
- **wb-gradient-easingspecimenstrip** — five producer rows (M3 · chip-rung · `:presets` door ·
  fade/`scrollToActive` · easing constants), banked per MT-F014/MT-F026.
- **wb-gradient-easingauthoringstage EAS-19** — the `easingPathD` adoption note (rides W9.7).
**COMPETING CHANNEL, DECLARED (cl.2).** `COHESION.md` §4a runs an **SS-6 accretion register** whose
rule is *"assembled into ONE letter at the next boundary"*, and it already holds **SC-5 = the
ErrorBoundary plate register relay** — the same identity as EB-5. **Two channels for one identity is
the re-book shape the anti-rename law forbids.** Either `X-W9.i`'s glass packet absorbs the register
or SS-6's batched letter does; **G33 counts five packets and five ledger rows**, so a row that leaves
by SS-6 must not also be counted as W9.i packet content. Decided at open, in writing.
⟨AuroraPane.md · AP-2 (D/N-2) · ErrorBoundary.md · EB-5 + EB-9 + EB-10 · EmptyState.md · §GLASS-RELAY ES-30/ES-15/ES-5 · SearchFilterBar.md · SFB-34 + SFB-6 + SFB-17 + SFB-19 · wb-gradient-easingeditor.md · two glass asks · wb-gradient-easingspecimenstrip.md · five producer rows · wb-gradient-easingauthoringstage.md · EAS-19 relay arm⟩

---

## 11. Library correctness — unit-unassigned in the dated spec (4 rows)

*Four measured library defects that no `X-W9.a..i` unit's mechanism text covers. All four are
`src/color/**` correctness, and three of the four are out of §File Bounds. Booked here so the
unit assignment is an open decision, not a silent drop.*

**W9.22 · ColorNutritionLabel CNL-18 — `encodedToHsl` divides BEFORE the powerless law can see the
channels: WHITE CANNOT CONVERT TO HSL** (cl.1 · MAJOR · **GENUINELY NEW**)
*"My dist run: grey sweep fails at **exactly v=255** with `color_non_finite`; **neighbours fine**;
`rgb(255,255,254)→hsl` s = **1.000000000000085** (out of declared domain). Mechanism from my
anchors.ts read: `encodedToHsl` divides `delta / (1 - |2L - 1|)` **BEFORE** the powerless law can see
the channels, and `convertColor`'s non-finite check **precedes** `finishChannels` — **the guard
exists but runs in the wrong order**."*
**Cure-shape (verbatim)**: *"a library correctness row (**powerless test precedes the division**),
with the CNL-2 gate covering the demo symptom."*
Demo consequence **traced, not run** (status carried): white + select HSL throws through
`valueOrThrow` inside the ColorPicker watcher, desynchronising `selectedColorSpace` from
`model.color.space`, then `Unknown rgb channel: h` in render.
**Triumvirate note (so the seat does not mis-trigger it)**: the cure is an **ORDERING**, not a
mathematics change, and the file is `src/color/anchors.ts`, **not** `model.ts` — §Triumvirate
Dispatch's invalidating condition (*"editing `src/color/model.ts`'s conversion mathematics"*) is
**not** met. §14 delta 1.
⟨ColorNutritionLabel.md · CNL-18 (reader-L miss 3, re-measured)⟩

**W9.23 · picker-spectrumcanvas M-9 — `convertColor` accumulates matrix float drift through the XYZ
anchor; `roundHalfEven`'s tie branch is DEAD CODE on every converted path** (cl.1 · MAJOR ·
**GENUINELY NEW · ESCALATION, not an edit**)
Re-proven by the adjudicating seat: `[63.75, 25.499999999999993, 25.5]` → `[64,25,26]`;
`hsv(14,1,1)` → `r = 254.99999999999997`, `b = 5.7e-14`. *"The deliberate CSS-Color-4 tie rule is dead
code on every converted path; 8-bit round-trips unstable; L-1's moral **inverts onto the library**."*
Routed *"→ X-W9 … with the tie-window arithmetic"*.
**ESCALATION, DECLARED (cl.2).** *[filename-only census, this seat]*: `roundHalfEven` is in
`src/color/operations.ts` (**in bounds**); the drift is **produced upstream in the conversion chain**
(`model.ts` / `anchors.ts`). The row therefore admits exactly **two** terminal shapes and the fold
does not pick: **(i) RETIRE** the tie rule as measured-dead code — honest, in bounds, and it makes the
surface stop claiming a law it cannot execute; or **(ii) CURE** the drift — which reaches
`src/color/model.ts`'s **conversion mathematics** and **fires §Triumvirate Dispatch by its own
words** (*"the colour model is not in scope; a cure that reaches it means the crash class was
mis-rooted"*). A seat that drifts from (i) into (ii) mid-unit invalidates the wave. → X-W0.
⟨picker-spectrumcanvas.md · M-9 (reader B, re-proven)⟩

**W9.24 · picker-spectrumcanvas C-4 (library half) — `xyzToKelvin` is a literal 39,001-iteration
linear scan** (cl.1 · MAJOR · **GENUINELY NEW**)
*"`xyzToKelvin` is a literal **39,001-iteration linear scan calling `kelvinToXyz` per step**
(`anchors.ts:317-330`) — B measured **1.71 ms/op**; route the scan to X-W9."* The component half
(the plate writes blindly through `selectedColorSpace` with no representability check, and Kelvin is
one-channel yet selectable, so the (s,v)→K map is degenerate) is **X-W4's**.
**L-19 honesty note, stated**: this gate's product consumer is the spectrum plate's Kelvin mode — and
that mode is **itself** an X-W4 defect row. If X-W4 removes Kelvin from the plate, **NG-7 loses its
consumer and must be retired, not re-justified**. Recorded now so the gate cannot survive its own
warrant. The live readout-collapse sequence at that record stays **UNPROVEN**.
*Bounds*: §14 delta 1.
⟨picker-spectrumcanvas.md · C-4 library half (reader B measurement, seat-verified source read)⟩

**W9.25 · picker-spectrumcanvas M-8 (library rider) — the library never states its TRUE channel
ranges, and the one write gate silently corrupts every colour above a declared bound**
(cl.2 · MAJOR rider)
*"the demo's domain table **cannot represent white** in XYZ (`percent×3`, max 1) or jzazbz (**jz max
0.222 < white's 0.2220652**), and the ONE write gate **silently corrupts** every such colour: plate
top-left in XYZ mode → `#fffff4`, readout pins z at 1.000 → X-W4 (the table + gate) **with an X-W9
rider (the library states the true ranges)**."* Re-proven end-to-end by the adjudicating seat against
dist. *"The clamp oracle **structurally cannot** catch it (no test hands it a LEGITIMATE coordinate
above a declared bound). All three axes walked this write path; **none checked the table against the
spaces' real ranges**."*
**Bearing on W9.5 — this is that cluster's FALSIFYING DATUM.** A published `ChannelSpec` whose
XYZ/jzazbz ranges cannot admit white is **worse than none**: it converts a demo-local error into a
library-warranted one. Kept as its own row because *stating the true ranges* is separable from
*publishing the schema*, and either can land without the other. **NG-4 carries the white-admission
leg.**
⟨picker-spectrumcanvas.md · M-8 (reader B; demoted from BLOCKER, re-proven by the seat)⟩

---

## 12. Riders and notices — no booking moves (5 rows)

**W9.26 · PreviewStrip PS-16 — the consumer-side twin of W9.5, NO-WAVE-OWNER by its own words**
(cl.4 · pointer)
*"`picker-color.ts` is **a second color model inside the chip's closure**: two re-minted library type
names (**53 + 33 references**), a 17-space channel-range table and immutable per-channel setters the
library does not publish — plus **DEAD public surface**: `ChannelMeta`, `channelMeta`, and
`PickerColorError` (**the module's ENTIRE error taxonomy**) have zero external consumers … **NO-WAVE-
OWNER, named** — the promotion question (channel model down into the library) is X-W9-adjacent but
**unnamed in any X file**; the dead exports die with whichever wave first touches the module."*
**Pointer, not a second booking**: the library-side identity is **W9.5**. Recorded so the consumer
half is visibly *not* W9's and so `PickerColorError`'s deadness is not rediscovered.
⟨PreviewStrip.md · PS-16 (L2-6 + missed-LC#3)⟩

**W9.27 · MERGED — the eased-mix capability question: a coordination row, NOT a totality defect**
(cl.4 · rider · **the library is BLAMELESS and the fold says so**)
The instrument advertises **y ∈ [-0.6, 1.6]**; the consumer mix domain is **[0,1]**, so
`src/color/operations.ts:65/:90` return `err(color_progress_out_of_range)` and the demo's sinks
convert `Result`→**throw inside computeds**. Crash set = **exactly the three `back` presets**, and
**`DEFAULT_BEZIER_PRESET = "ease-out-back"` is one of them**. GV-1 sharpens: `ease-out-back` throws at
the specimen-ink door too (`fn(0.5) = 1.0676`); **seven throw doors** enumerated. Grid-accident
sharpening confirmed to the decimal: `(0,0,1,1.12)` survives only because j/32 sampling steps over its
overshoot, `(0,0,1,1.15)` dies, and `stepsPerInterval` **re-rolls the catch set when a stop is added**.
**Routing verbatim**: *"X-W6.a (owns the three throws + masking default deletion) with a GLASS-RELAY
arm … **and an X-W9 rider** (library totality band, W9.md:185)"*; GV-1: *"the eased-mix
library-capability question **X-W9 coordination, else NO-WAVE-OWNER**."*
**FOLD RULING — the boundary is drawn so W9's gates are NOT widened.** W9's totality goal is about
**strings** reaching public entries. `mixColors` refusing progress outside [0,1] is a **numeric-domain
refusal already expressed as an honest `Result`** — the sibling record states it outright: the
predicate is *"real, **library-blameless**"*. **G1/G4 are not widened by this row.** The only X-W9
question is whether `./color` should **admit** an eased mix at all; that is a coordination decision,
booked as one.
⟨wb-gradient-easingauthoringstage.md · EAS-2 (D3-02/D2-02/D-2/C-1/L-1) · wb-gradient-visualizer.md · GV-1 (≡ R2-1, sharpened by PIN 2/K-19)⟩

**W9.28 · SearchFilterBar SFB-34 — "X-W9's consumer sweep" NAMES A STEP THAT DOES NOT EXIST**
(cl.1 · MINOR · named gap)
*"`colorToHexString` alias + 3-of-20 `color-utils` facade feed the child's hand-rolled writer …
**facade rows cross-note X-W9's consumer sweep**."*
**Correction to the routing, not to the fact**: X-W9 has **no consumer-sweep step**. Its demo bounds
are **exactly two files** (`demo/color-session/colorSpaceInfo.ts`, `ConsoleRail.vue`) and §File Bounds
"Do NOT touch" bars *"`demo/**` except the two ICtCp files above"*. The sweep therefore belongs to
**X-W7/X-W8 sequenced AFTER the 4.1 cut** (consistent with W9.17's *"consumer swaps follow the
library"*), or it degrades to NO-WAVE-OWNER. Named here so the cross-note does not resolve to a
vacuum at execution.
⟨SearchFilterBar.md · SFB-34 (LP5-3-as-split; facade limb)⟩

**W9.29 · wb-gradient-codeeditor A-27 — the totality cure SHRINKS but does not CLOSE the ingress
crash** (cl.4 · sequencing notice for X-W7)
*"`parsePickerColor` throws on ANY rejection (`picker-color.ts:109-113`) and has **six call sites with
five guard postures**; the sixth — `ColorPicker.vue:282-289 onStartEdit`, a click on a saved-palette
swatch feeding store/API-supplied `originalCss` — is **UNGUARDED**. A single non-canonical stored
literal **evicts the workspace with no keystroke**. **Five-of-six is a convention, not an invariant.**
→ **X-W7** (palette/CRUD ingress ownership); NOTE: **A-1's X-W9 totality SHRINKS but does not close
this** (PickerColorError on `ok:false` remains)."*
Folded as a notice **against X-W7**, not as a W9 row: it prevents X-W7 from reading the 4.1 cut as
its cure.
⟨wb-gradient-codeeditor.md · A-27 (reader-2 miss #3, re-verified)⟩

**W9.30 · AdminNamesPanel C-15/L-14/D-swatch (merged) + M-DU4 validity limb — validation is
PARSER-GATED, and the interim is NAMED so an inverted order stays honest** (cl.2 · sequencing edict)
*"An unparseable or unsupported user-submitted literal **paints nothing yet stays approvable** —
indistinguishable from transparent; zero value.js imports in the file. Validation is **GATED**:
project memory records R1 = live `parseCssColor(\"oklch()\")` crash; the cure lands only behind the
parser spec. Face → **X-W7**; validation → **X-W9** (blocked on the V·π/R1 gate; **`CSS.supports` is
the interim honest mark if X-W7 lands first**)."* M-DU4 adds the same split: *"**X-W7** (face) +
**X-W9** (validity flag — see C-15/L-14)."*
**Why it is folded here**: this is the corpus's **only** row that names an **interim** for the case
where the consumer wave lands before the library wave — the honest form of a dependency the ordering
does not guarantee. W9 is order-independent of X-W2..X-W8 by its own §State, so the inversion is
**admissible by the spec**, and the interim must therefore be carried, not assumed away.
⟨AdminNamesPanel.md · C-15/L-14/D-swatch (merged) + M-DU4 (R-DU missed)⟩

---

## 13. Gates — born-RED candidates (12 NEW), each with a real product witness

*L-19 discipline: every row names a **product consumer or a published artifact**; no gate is a
`scripts/**/proof-*.mjs` (DR-19, structurally honoured — this fold authors none); no gate multiplies
audit categories together. "Born state" quotes the adjudicated record's own measurement and its
epistemic status.*

| # | Gate | Born state (witness-measured) | Real witness (the product consumer) | Falsifier | Row |
|---|---|---|---|---|---|
| **NG-1** | **Mirror-accept battery: the seven invalid-CSS vectors return `ok:false`** | **RED — 7/7 `ok:true`**, all invalid CSS, re-measured against dist: `oklch(/ 0.7 0.1 145)` · `rgb(/ 1 2 3)` · `rgb(1 2 3 /)` · `oklch(0.7 0.1 145 / )` · `oklch(0.7 0.1 145/)` · `rgb(,,,255 0 0,,,)` · `rgb(1,2,3,)` | The shipped gradient validity oracle `gradientParse.ts:16`, which today certifies `rgb(1 2 3 /)` as valid; the in-tree correct primitive `splitTopLevelCommas` is the cure exemplar | Restore the `:79/:85` empty-drop; slash position and comma arity are destroyed again. **NOTE: G1/G2/G3 all stay GREEN under this failure** — that is the hole this gate closes | W9.2 |
| **NG-2** | **R1 corpus widened to `<any-ident>()` + the three-entry asymmetry** | **RED — measured**: `oklch()` AND `calc()` AND `foo()` all throw the same TypeError from `parseCssColor` directly; `parseCssValue("foo()")` → `ok:false` while `parseCssScalar("foo()")` throws; `parseStylesheet("a{color:oklch()}")` throws while `"a{width:foo()}"` → `ok:false` | glass-ui 7.0.0 feeds `parseCssColor` user-supplied strings; the gradient editor debounced-emit path; `red calc(50% - 2px)` is ordinary CSS | Narrow the battery back to the nine colour functions; `calc()` throws again with every gate green | W9.1 |
| **NG-3** | **A contrast primitive is published from `./color`** | **RED** — `contrastRatio` = **0 files in `src/`** *[filename-only census, this seat]*; `contrast`/`luminance` module-private per two records | `ink.ts:157-174` rebuilds a predicate from a solver by endpoint-walk; `:138-142` diffs every channel at 1e-9 | Unpublish it, or leave the epsilon-identity idiom as the only route to a ratio | W9.6 |
| **NG-4** | **The channel domain is published — ranges · units · `leastCount` · display-scale — AND its ranges admit white** | **RED** — `SPACE_SCHEMA` carries no ranges/units; the 17-space table exists only at `picker-color.ts:52-70` (byte-diffed identical); **5** declarations of "the channels of a space"; the display-scale predicate at **exactly 5** sites; the kelvin bound in **two packages** | `picker-color.ts`'s cloned table; `normalizedChannel`; `PICKER_CHANNELS`; the `ChannelMeta` demotion path | Publish a schema whose XYZ (`percent×3`, max 1) or jzazbz (jz max 0.222) ranges **cannot represent white** (W9.25's measured `#fffff4` corruption) — a library-warranted version of the demo's own bug | W9.5 · W9.25 |
| **NG-5** | **Timing-function round trip: `/css` parses ⇄ the library serializes; the `/math` emitter is retired-or-moved in the same act** | **RED** — no `serializeTimingFunction` in `src/`; **three mints across two packages** guarded by **one** e2e assertion; `cubicBezierToString` unused · 2dp-lossy · test-pinned wrong law · violates `/math`'s own barrel docstring | `easingCatalogue.ts:220`'s `t.css === interval.css` — a live identity comparison whose correctness IS byte-fidelity across the package boundary | Ship the serializer and leave the `/math` emitter (two library laws for one literal), or retire the emitter without the serializer (deletes the only in-library half) | W9.7 |
| **NG-6** | **`./quantize` publishes its brands, not five bare `number`s** | **RED** — `quantize.d.ts` publishes **five bare `number`s** while `src/quantize.ts:39-50` enforces k∈[1,64] int and chromaWeight≥0 | Three disagreeing shipped UI ranges (1..16 · 1..12 · 0..1.5) over one enforced domain; `dominantColor`'s hardcoded k:5 | Publish a `number` where the implementation enforces a brand; a UI can offer 0 or 1.5 and typecheck | W9.9 |
| **NG-7** | **`xyzToKelvin` is not a linear scan** | **RED — measured 1.71 ms/op**, a literal **39,001-iteration** scan calling `kelvinToXyz` per step (`anchors.ts:317-330`) | The spectrum plate's Kelvin mode | Restore the scan. **HONESTY RIDER**: if X-W4 removes Kelvin from the plate, this gate **loses its consumer and is RETIRED, not re-justified** | W9.24 |
| **NG-8** | **HSL representability: white converts** | **RED** — grey sweep fails at **exactly v=255** with `color_non_finite`, neighbours fine; `rgb(255,255,254)→hsl` s = **1.000000000000085**, out of declared domain | ColorPicker's HSL space selection (traced, not run — status carried) | Reorder the non-finite check ahead of `finishChannels` again; the powerless law stops seeing the channels before the division | W9.22 |
| **NG-9** | **Manifest reclassification: zero runtime `dependencies`, cycle gone** | **RED** — `dependencies` = exactly `{glass-ui ^7.0.0, keyframes.js ^6.0.0}`; **zero `src/` imports of either**; keyframes.js imported **nowhere in the repo**; glass-ui peer-depends on value.js `^4.0.0`; 10 emitted ESM files carry **zero bare specifiers** | Every installer of `@mkbabb/value.js@4.0.0` — the already-public manifest | Reintroduce either as a runtime dep. **G31 stays green either way today** — it asserts only the parse-that absence over this map | W9.16 |
| **NG-10** | **Descriptor totality over the FULL id domain, incl. `alpha` and `kelvin`** | **RED** — `cp` → `"Ct (tritan)"` (wrong row) · `jz` → bare key · **`kelvin` → bare key** · **`alpha` → bare key in every space**; `ct` passes **only by array order** | `ConsoleRail`'s per-channel tooltip, every space | Restore prefix matching. **Count law**: the space count is re-derived from `ChannelsBySpace` at open — G32's "17" and the record's "18" are **both** carried, **neither adopted** | W9.20 |
| **NG-11** | **Consumption census published beside coverage-by-export** | **RED** — no coverage config exists at all; consumption re-counted exactly: /color **23** · /css **9** · /quantize **3** · **/value 0 · /transform 0** | The published `coverage-by-export.md` artefact (§Verification Artefacts) | Publish a coverage ratio without the consumption census beside it; a 100% figure over a zero-consumer subpath reads as health | W9.15 |
| **NG-12** | **The root `.` ruling is written, and the citing comment is corrected in the same act** | **RED** — no `"."` key, no root d.ts, `ERR_PACKAGE_PATH_NOT_EXPORTED`; `demo/shared/utils.ts:16` cites the nonexistent export as its fork's justification | `demo/shared/utils.ts`'s debounce fork | Rule the question and leave the comment (a false citation survives its own ruling), or correct the comment before the ruling exists | W9.10 |

### 13b. Existing W9 gates these rows SHARPEN (11 — no gate is re-staged, re-weighted or composited)

| Gate | Sharpening | Row |
|---|---|---|
| **G2** | Battery corpus widened from 9 colour functions to the `<any-ident>()` class + the three-entry asymmetry; the widening lands in **Commit Plan row 1**, not after the cure | W9.1 |
| **G1 · G3 · G4** | Stated limitation, not a change: all three assert **non-throwing** and stay GREEN under W9.2's wrong-accept family. **NG-1 is required for the seam to be gated at all** | W9.2 |
| **G12** | Leg 3 (`ERR_PACKAGE_PATH_NOT_EXPORTED` on the root) gains a **written-ruling** obligation and an X-W8 ordering edict | W9.10 |
| **G14** | Four independent consumer witnesses added; unit semantics pinned (**occurrences 58→0**, RD-3) so the records' "25 lines" cannot be read as a competing count | W9.4 |
| **G15** | `serializeCssValue` is published **without a precision clause** as the spec stands — the 12-decimal posture ships unowned on a new symbol unless the clause lands here | W9.14 |
| **G16** | Ratchet reading must be **restated in the same act** if W9.13's ~635-line promotion is accepted; moot if declined | W9.13 |
| **G19** | Consumption census published beside the declared-surface denominator (NG-11) | W9.15 |
| **G22** | Census corrected upward: RD-6's "two hand-rolled hex implementations" → **four `padStart(2` homes / five consumer re-implementations**. Disposition and falsifier unchanged | W9.17 |
| **G25** | Fence scope clarified: it covers `bezierPresets`' 30 **keys** and the 40-name catalog — new **function** exports (`serializeTimingFunction`, `easingPathD`) are not fence breaches. Say so at open | W9.7 |
| **G31** | Gains the **reclassification** leg (NG-9); today it reads the same map and asserts only the parse-that absence | W9.16 |
| **G32** | Failing set widened from `{cp, jz}` to the full domain incl. `alpha` (every space) and `kelvin`; `ct` recorded as passing by array order; space count re-derived at open | W9.20 |

**Denominator note (L-19).** Every count above names a product consumer or a published artifact. No
row multiplies audit categories. Two gates carry explicit self-retirement conditions rather than
survive their warrants (**NG-7**'s consumer, **NG-10**'s count) — an anti-contrivance clause, stated
at authoring rather than discovered at close.

---

## 14. BoundsDelta — files this wave must ADD to §File Bounds (6)

*Each delta names the row that requires it and the record's own citation. Deltas 1-4 are
uncontested; delta 5 is contested at source; delta 6 is **an adjudication owed at X-W0, not a bounds
edit a seat may take**. Path existence for all six verified by `git ls-files` (path census only);
symbol homes by `git grep -l` (**filename-only, no content read**).*

| # | File | Access | Required by | Record citation |
|---|---|---|---|---|
| **1** | `src/color/anchors.ts` | modify | **W9.22** (`encodedToHsl` ordering) · **W9.24** (`xyzToKelvin` scan) · **W9.5** (`HUE_INDEX`, the 4th table declaration) | `anchors.ts:317-330` (C-4) · `encodedToHsl` division order (CNL-18) · M-10's four-home census. *[filename-only census: `xyzToKelvin`, `encodedToHsl`, `HUE_INDEX` all in this file.]* Currently absent from §File Bounds entirely |
| **2** | `src/quantize.ts` | modify | **W9.9** (brands at the guard, `dominantColor`'s k:5) | `src/quantize.ts:39-50` guards, `:132-139` hardcode (EC-17). `src/subpaths/quantize.ts` is already covered by `src/subpaths/*.ts`; **the implementation is not**. Also the only 18-`!` src file no lint scope (G5/G10) reaches |
| **3** | `src/value.ts` | modify | **W9.12** (per-node spans; empty-segment arity) | `src/value.ts:3-20` — `CssScalar`/`CssCall` carry no source text or spans (A-16/K5). *[filename-only census: both types live here, in `src/css/types.ts`, and in `src/subpaths/value.ts`; only the latter two are bounded]* |
| **4** | `src/css/syntax.ts` | modify-carve | **the dated spec's OWN `X-W9.d` mechanism** — *"`isSupportedSyntaxDescriptor` goes `@internal`"* | *[filename-only census: the symbol is in `src/css/syntax.ts` **and** `src/css/stylesheet.ts`; §File Bounds contains only the latter.]* **A defect in the spec found by the fold, not a corpus ask** — the spec cannot execute its own sentence (§1a n.5) |
| **5** | `tsconfig.demo.json` | modify | **W9.19** — *only if* PC-17's X-W9 booking is upheld over the three NO-WAVE-OWNER bookings | **CONTESTED (§1a n.3b).** As the spec stands the file is in no W9 bounds row, so PC-17's booking is **unexecutable**. Adopt with the cure *"generate `paths` from `exports` as `vite.config.ts:28/:41` already does"* + PC-17's header-comment correction obligation, **or** the identity stays NO-WAVE-OWNER. Companion `.gitignore:18-24` / `vitest.config.ts:22-27` (EB-33) rides whichever way this goes |
| **6** | `src/color/model.ts` | **ADJUDICATION OWED — NOT a bounds edit** | **W9.5** (publish `SPACE_SCHEMA`/`SPACE_IDS`) · **W9.11** (retarget `convertColor`/`mixColors` return positions for `ColorIn`) · **W9.23-(ii)** (the drift cure, if chosen) | The spec bars this file **twice by two different rules** — §File Bounds "Do NOT touch" (the **file**) and §Triumvirate Dispatch (its **conversion mathematics**) — and does not distinguish a **re-export** or a **return-type retarget** from a **mathematics edit**. *[filename-only census: `SPACE_SCHEMA`, `SPACE_IDS` are here.]* **→ X-W0.** No fold row assumes a reading; W9.5 and W9.11 are undeliverable until it is ruled |

**Named-gap degradation law (carried verbatim, PS-13/PS-14, tracked at `COHESION.md:113`):**
*"W9.f must adopt this row explicitly or it degrades to NO-WAVE-OWNER at execution"* · *"adopt
explicitly or degrade to NO-WAVE-OWNER."* This binds **W9.16** (devDependencies move) and **W9.11**
(`ColorIn`), and by the same shape **W9.5** (`ChannelSpec`), **W9.6** (contrast primitive), **W9.7**
(serializer), **W9.9** (quantize brands) — six asks with **no mechanism text in the dated spec**.

---

## 15. CrossEdges

**Declared in the dated spec and unchanged** — X-W8 on `ARCHITECTURE.md` (`:657` carve) · X-W4 on
`ConsoleRail.vue` (CONFORMANCE W9-D2 FIXED, reciprocated at `W4.md:196-197`) · X-W4 + X-W8 on
`eslint.config.js` (X-W9.f sole writer) · X-W0 depends · X-W1 soft-couples · blocks X-W10/X-W11.

**Added or sharpened by this fold:**

| Edge | Substance | Rows |
|---|---|---|
| **X-W6 ∩ X-W9 — an UNDECLARED THIRD WAVE on the CC-081 identity** | C-4 is cross-booked CC-081→X-W9 **and** CC-068→X-W6 (catalog totality): *"the cure lands wherever the catalog does, and **both waves must cite this row so neither drops it**."* **Neither W9.md nor W6.md declares this edge.** Sharpest new cross-edge in the fold | W9.20 |
| **X-W6 → X-W9 (serializer)** | W9.7's retire-or-move decision **gates** X-W6.d's consumer adoption and C-14/M-ESS-9's deletion — *"sequencing-coupled to X-W9, **not silently absorbed**"* | W9.7 |
| **X-W6 → X-W9 (spans)** | X-W6's demo gradient-projection re-home is **downstream** of the per-node-spans decision — *"the demo projection re-homes **only after that lands**"*. Undeclared in either file | W9.12 |
| **X-W6 ⊘ X-W9 (promotion)** | X-W6 is **structurally barred** from `src/**` by its own Do-NOT-touch, so an X-W9 declination on GV-50 **lands the row at NO-WAVE-OWNER by construction** | W9.13 |
| **X-W6 → X-W9 (leastCount)** | X-W6 lands `leastCount` beside `PICKER_CHANNELS` **now**; X-W9's `ChannelSpec` is the **terminal** home. W9 consumes an X-W6 landing, never an empty field; the live 100× alpha-bound disagreement is the hazard | W9.5 |
| **X-W5 ↔ X-W9** | R-2's demo `Result` propagation is X-W5's; the library half is W9's. **CNL-2's escalation clause fires** against W9's library-only posture unless answered at open | W9.3 |
| **X-W7 ← X-W9** | A-27: *"A-1's X-W9 totality **SHRINKS but does not close** this"* — X-W7 may not read the 4.1 cut as its ingress cure. C-15/L-14: X-W7's face may land first; **`CSS.supports` is the named interim**. SFB-15: consumer swaps **follow** the library | W9.29 · W9.30 · W9.17 |
| **X-W7/X-W8 ← X-W9 (the vacuum)** | *"X-W9's consumer sweep"* names a step **X-W9 does not have** (demo bounds = 2 files). The sweep is X-W7/X-W8's, sequenced after the cut, or it degrades | W9.28 |
| **X-W8 ↔ X-W9** | X-W8 re-homes `debounce` (`W8.md:163`); **the root `.` ruling is W9's and must land first or with it**. Both waves' config carves are named-but-unowned for W9.19/EB-33 | W9.10 · W9.19 |
| **X-W1 ← X-W9** | Three born-RED e2e legs owed: two-click powerless kill (no `role="alert"` fallback) · boot seed matrix (`?color=black` minimum) · the none-URL query-parameter state in the deploy matrix | W9.3 |
| **X-W4 ↔ X-W9 (NG-7's warrant)** | If X-W4 removes Kelvin from the spectrum plate, **NG-7 loses its product consumer and retires**. The gate's warrant lives in another wave's outcome | W9.24 |
| **X-W0 ← X-W9 (three rulings owed)** | (1) `src/color/model.ts` — bounds vs Triumvirate, §14 delta 6. (2) The **Result-consumption** ownership dissent (EAS-21 vs F-4). (3) The **config-parity** ownership dissent (PC-17 vs three NO-WAVE-OWNER bookings) | W9.5/W9.11 · W9.8 · W9.19 |
| **SS-6 / the batched BJ letter ↔ X-W9.i** | `COHESION.md` §4a already holds **SC-5 ≡ EB-5**. Two channels for one identity is the re-book shape the anti-rename law forbids; **G33 counts five packets** so a row leaving by SS-6 may not also be W9.i content | W9.21 |
| **`library-band.md` (the wave's audit input)** | **Already folded whole into the dated spec (CC-091, W.L1..W.L7 → X-W9.a..i); this addendum does NOT re-fold it.** Two dated corrections recorded against it: RD-6's hex census **2 → 4/5**, and RD-3's `_2` unit semantics pinned so the records' "25 lines" is not read as a competing count. Its §6 DISSENT register stands verbatim and untouched | §1a n.4 |
| **`parser-band.md`** | cand-O/cand-F measured **0 spurious accepts over the 172-input R1 corpus**. **That corpus is NOT W9.2's 7-vector mirror corpus**, and **no claim is made here that either prototype closes W9.2** — the R1 totality result and the mirror-accept family are different measurements. Parser adoption is out of scope by `X-W9.g`'s own words (*"This wave does not adopt a parser"*) | W9.2 |

---

## 16. Closing — status verbs

This addendum is **ADJUDICATED INPUT** to X-W9: the value.js corpus's X-W9-routed holdings, folded
once, deduped by banked identity, dated, **E-3-conformant** (zero edits to any dated spec; this file
is the sole write). It **STAMPS NOTHING** — no four-verb field moves, no gate goes GREEN or RED by
this document, no baseline is re-measured here, no bar is set. `waves/W9.md` §State stays
`Status: planned` and the execution gate is unchanged: **nothing opens product source until the
owner's begin-word**.

**What a W9 seat must read before opening**: this file's **§14** (six bounds deltas — two of which,
`src/color/model.ts` and `tsconfig.demo.json`, are **rulings owed, not edits available**), **§13**
(twelve born-RED candidates, of which **NG-1 closes a hole every existing seam gate is blind to**),
and **§15**'s three X-W0 rulings. **Six of the fold's asks have no mechanism text anywhere in the
dated spec** (W9.5 `ChannelSpec` · W9.6 contrast primitive · W9.7 serializer · W9.9 quantize brands ·
W9.11 `ColorIn` · W9.16 devDependencies) and every one of them carries the corpus's own degradation
law: **adopt explicitly, or degrade to NO-WAVE-OWNER at execution**.

**Superlatives, recorded with the same provenance duty as defects (L-18 runs both ways).** The
corpus's cure-locks are unusually well-built and are carried verbatim rather than paraphrased:
A-2's *"cure #1 alone may not ship"* (a cure that would have closed every gate green while shipping
the worse defect) · CNL-2's *"unrepresentable-to-forget … the convention cannot be held by
discipline"* · L-2's *"the missing serializer and the R4-1/GV-12 retirement are ONE X-W9 decision"* ·
C-4's *"if the X-wave authoring collapses them, the collapse must be written, not silent"* — the
instruction this fold obeyed at §1a n.2 · PS-13/PS-14's named-gap degradation law, which is the
only mechanism in the whole corpus that makes an unadopted ask **fail loudly** instead of vanishing.
The two units that book **zero** rows (§3 `.b`, §8 `.g`) say so in the open, with the
cross-corroborating measurement (`/transform` consumption = 0) beside them.
