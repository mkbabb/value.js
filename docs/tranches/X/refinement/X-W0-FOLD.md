SERVED MODEL ID (fold author, line-1 declaration): `claude-opus-5[1m]` — observed in this seat's own system prompt, declared not inherited.

# X·V REFINEMENT FOLD — **X-W0** (Formation) · the dated addendum · 2026-08-28

## 0. Header — provenance, posture, reading rule, arithmetic

**Provenance.** Input set = the value.js adjudicated corpus at
`docs/tranches/V/megatranche/registry/adjudicated/` (all files NOT `kf-*`/`fr-*`/`pt-*` — 92 files,
88 components + parser-band + library-band + chassis-fitness + layout-gestalt). This seat's assigned
packet: **AuroraPane · CurrentPaletteEditor · wb-extract-controls · wb-extract-imageeyedropper ·
EmptyState · wb-generate-pane · wb-mix-animationcanvas** (read whole), plus a corpus-wide sweep for
`X-W0` and its dotted unit forms, which returned routings from **ColorNutritionLabel · ShadowPalette ·
library-band · wb-gradient-visualizer** (all folded below). Proposed packet list for this wave was
**empty (`[]`)** — nothing was consumed from a collator; every row here is drawn from the records,
the dated wave specs, or this seat's own dated probes.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0.md..W11.md` and
`CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is the fold's sole write. Where this
addendum strikes or corrects a clause in a dated spec, the strike is recorded here and the spec's
bytes stand. `registry/adjudicated/**` is likewise immutable (E-1/E-3): no row below rewrites a
record; corrections to records are recorded as corrections, with the record's own words quoted.

**Reading rule.** At execution X-W0 reads **`waves/W0.md` PLUS this addendum**, in that order; where
they disagree the addendum's clause governs. §2 (Gates) is the single authoritative list of gate
sharpenings; §3 (BoundsDelta) is the single authoritative list of bounds/content deltas. Nothing here
re-stages or re-weights W0's ten units, opens a §1.M bank row, or performs any glass cut. **Status
stays `planned`. Nothing here opens product source.**

**Fold classes** (X·P idiom, kept): **cl.1** = spec sharpening / law / correction · **cl.2** =
cure-lock or binding contract clause carried verbatim · **cl.3** = MEASURE-AT-OPEN row · **cl.4** =
declared silence (a unit this packet does not reach).

**Arithmetic — the OUT side, countable from this file's own bytes.** **20 rows** (W0.1..W0.20, each
with exactly one `⟨…⟩` provenance line) · **8 gate candidates** (G-A..G-H: 7 SHARPEN an existing
HG-n, 1 NEW) · **7 BoundsDelta entries** (2 new paths, 1 access-verb change, 1 immutability
declaration, 3 content deltas at paths already bounded) · **19 cross-edges** · **4 declared
silences**. The IN side is a receipt, not arithmetic: 7 records read whole + 4 records swept + 12
wave specs + 3 ledgers; the input set is enumerated nowhere in this file as a row list, so **no
in-equals-out identity is asserted**.

**Arithmetic — SUPERSEDED 2026-08-28 by the round-1 repair (§6.8). Dated, never silent.** The
paragraph above is the **round-0** count and its bytes stand as the record of what this fold held at
authoring. The **current true counts** are **28 rows (W0.1–W0.28, gapless) · 11 gate candidates
(7 SHARPEN · 4 NEW) · 10 BoundsDelta entries · 25 cross-edges · 5 declared silences**, with **37
identities newly carried id-for-id** (32 NO-WAVE-OWNER-marked + 5 boundary-by-disposition) and **1
EXTERNAL row** (⟨INBOX I-28 · X-EXT-6⟩). §6.8 is the authoritative arithmetic block from this date
forward; §6 is the authoritative row band for W0.21–W0.28.

**Byte-visibility caveat on the dedupe register below (cured at W0.28).** The bold-split idiom used
in this §0 block and in §1 (`EC-**2**`, `MX-**1**`, `ES-**18**`, …) writes ids whose **literal bytes
are not the id**, so 23 of them are invisible to a byte census and 7 escape the entire fold layer.
They are restored in plain bytes at **W0.28** as `≡`-pointer rows — no re-booking, no new count. Ids
are original for life; no id in §6 is bold-split.

**Dedupe register (M-25, banked identity, anti-rename — ids are original for life).** Merged once,
counted once, never re-booked:
- **glass-7 `tag=` / no-slot residue**: CPE **A-1**'s L-12 census (6 `tag="button"` · 18 `tag="div"`)
  ≡ EmptyState **ES-20** ≡ EY-**27** ≡ the 21-site sweep banked at **ColorSpaceSelector L-8** ≡ the
  seats X.W4.g enumerates — ONE identity, counted once inside W0.4.
- **CC-105 track-material sites**: EC-**2**/EC-**3**/EC-**22** (`ExtractControls.vue:32,75`) +
  GEN-**8** (`GenerateControls.vue:305`) + AP-**17** (`ConfigSliderPane.vue:146/:202`) +
  `ComponentSliders.vue:197` — ONE booking (CC-105), four consumer files; counted once at W0.3.
- **dead lint lattice**: EC-**15** ≡ GV-**4** ≡ MX-**25**/MX-**35** ≡ CPE **L-6** ≡ **ColorSpaceSelector
  L-5(a)** — ONE identity, W0.16.
- **tsconfig↔exports parity**: ES-**18** ≡ EY-**43** ≡ MX-**20** ≡ AP-**31** ≡ GEN-**23** ≡ EC-**30**
  ≡ **ColorSpaceSelector L-14 / XP-21** (W-HYGIENE) — NOT re-booked here; appears only as a member of
  the boundary register at W0.17.
- **WatercolorDot dist bytes**: six independent verifications of one fact — counted once at W0.19.

### 0a. Packet challenged — corrections, never silent

1. **"the fifth independent verification" is claimed twice, by two records, of the same fact.**
   wb-generate-pane (GEN-2, HEAD `954b255c`) and wb-mix-animationcanvas (MX-1, HEAD `9c779130`) each
   call their WatercolorDot dist read "the fifth independent verification in this registry". Both are
   true of their own reading and mutually inconsistent as a count; with CurrentPaletteEditor,
   EmptyState, wb-extract-imageeyedropper and this seat's 2026-08-28 probe the registry holds **at
   least six**. Recorded as a counting nit against neither record; the fact is unaffected (W0.19).
2. **CurrentPaletteEditor ADJ-i1 routes itself to "NO-WAVE-OWNER … not a product wave" while naming
   its own home.** Its terminal words are "a `registry/COMPLETENESS-LEDGER` row" — a file X-W0.c
   holds by name (`modify (generated)`). This addendum re-homes the row to **X-W0.c** and records the
   re-home rather than asserting the record wrong: the record identified the correct artifact and the
   correct non-product wave; only the label "NO-WAVE-OWNER" is superseded (W0.9).
3. **EC-15 and GV-4 route the dead-lint-lattice identity to "X-W0/X-W1"; X-W0 cannot execute it.**
   X-W0's bounds are docs + workflows and its C-06 COMPLETABLE rests on "zero product source"; it
   writes no `eslint.config.js` byte. The routing is not deleted — it is bounded (W0.16), with the
   executable half named at X-W8 (`W8.md:42`, per ActionFeedback AF-13 and VersionHistoryDrawer
   VHD-28) and the born-RED `--print-config` gate at X-W1.
4. **HG-6's own derivation no longer reproduces from the source it names.** W0.md:248 derives
   "213 EXISTS-ORIGINAL + 5 UNWITNESSED-DIRECT = 218 CHALLENGED, 46 REPORT-AUTHORED, 264 canonical
   rows" from `HYDRATION-LEDGER.md`. Measured today: **286 rows = 235 EXISTS-ORIGINAL + 46
   REPORT-AUTHORED + 5 UNWITNESSED-DIRECT**, i.e. **240 CHALLENGED + 46 = 286**. The dated baseline
   is not rewritten; the scope predicate is now load-bearing and is gated at **G-D** (W0.10).
5. **HG-1's baseline is not merely stale — it moved twice during this fold session.** Measured
   read-only at 2026-08-28: 1828 untracked files under `docs/`, then **1836** eleven minutes later,
   `docs/tranches/X/keyframes` growing 3 → 11 while a sibling sub-session (X·KF conformance PASS-1,
   COHESION §0b) authored. Recorded as evidence, not as a defect in W0.md (W0.13, G-G).

---

## 1. §Rows — 20

Route legend: **X-W0.x** = the unit whose bytes receive it · **sitting** = X-W0.g's owner packet ·
**census** = X-W0.j's event. Severity is the source record's, carried unchanged unless a ruling below
says otherwise. Every `"…"` is verbatim from the cited source.

---

**W0.1 — The CC-105 ⇄ CSP G-PAINT tension is the sitting's business, by name, and neither wave may
elect it silently.** · **MAJOR (mechanism), ⇄CSP** · cl.1+cl.2 · → **sitting** (+ census carry)
- **mech**: two live registers claim the `--slider-track-bg` sites — X-W8 books `ConfigSliderPane.vue:202`
  under **CC-105 BLOCKED-ON the 8.0.0 repin** with receiving surface X-W4.g, while the standing
  ConfigSliderPane adjudication's **G-PAINT single cut** (C-3/D-3/D-4/D-14) proved both that the naive
  retire-the-variant cure collapses the thumb to width 0 **and that the demo CAN paint the range
  today** — i.e. one register says "wait for glass 8", the other says "curable now".
- **cure-lock (verbatim)**: "the CSP G-PAINT ruling and the CC-105 booking **must be reconciled by the
  X-W0 sitting, not silently by either wave**"; "This seat routes the identity to the CSP register
  (the later, measurement-backed ruling) and flags the reconciliation to the X-W0 sitting rather than
  silently electing either."
- **seq**: X-W0.j (batch 3) precedes the sitting (batch 4) — the census verdict must carry this as an
  OPEN item into the sitting; a FAIL that reads "the bank stays shut" closes it by accident.
- **dissent**: the record refuses to elect; this addendum refuses too. Both readings are preserved.
  ⟨AuroraPane R-8 · AP-17 · DISSENT §4 — record⟩

**W0.2 — A BLOCKER's only disposition is "wait", and the record names the waiting itself as owner
business.** · **BLOCKER** · cl.2 · → **sitting**
- **mech**: every WatercolorDot mount in CurrentPaletteEditor + SwatchHoverMenu is written against an
  API Glass 7 deleted — the "Add current color" primary CTA and the whole swatch-control surface are
  decorative, glyph-less spans (`inheritAttrs:!1`, class/style only, `renderSlot` 0, `aria-hidden`
  span root, `pointerEvents:"none"`); the controls are inert in **all** modalities.
- **cure-lock (verbatim)**: "**Terminal: CC-044** … one atomic cut, **BLOCKED-ON CC-003's Glass ≥8
  trigger**; the ledger forbids a pre-8 wrapper — **the severity of waiting is X-W0 owner-sitting
  business**"; hygiene note 5: "A-1 sits in the CC-044/CC-003 blocked bank by that ledger's own
  atomic-cut ruling (no pre-8 wrapper) with the producer questions on the glass-ui BH relay."
- **seq**: the sitting's word must exist before X-W11's release table transposes the census verdict
  (W0.md:211 · W11.md:373).
- **dissent**: none in-record. ⟨CurrentPaletteEditor A-1 + §Corpus-hygiene 5 — record⟩

**W0.3 — The census fires a receiving surface whose bounds omit the file the booking names.** ·
**MAJOR (spec inconsistency)** · cl.1 · → **census** (+ sitting carry)
- **mech**: CC-105 names `ExtractControls.vue:32,75` with receiving surface **X-W4.g**; X-W4.g's own
  bounds table omits the file, and **no X wave's bounds hold `ExtractControls.vue` at all** (grep
  W0–W11, the record's own run). A PASS therefore fires a cut that cannot legally touch its subject.
- **cure-lock (verbatim)**: "a spec inconsistency the formation boundary must reconcile"; residue 12:
  "whether the glass BJ W4 hold's ruling authority has been formally transposed into the X formation
  (**X-W0.j census as the SOLE release event**) is a formation-boundary question this seat flags, not
  answers."
- **cure shape**: the census's PASS branch emits a **receiving-bounds delta** — every file CC-105 (and
  the §1.M bank) names that X-W4.g does not hold — or the PASS is unexecutable. On FAIL the row still
  stands: the bank's contents are mis-bounded either way.
- **seq**: X-W0.j, batch 3, before any W4.g open. ⟨wb-extract-controls RULING PIN 1 · residue 12 —
  record; corroborated at W4.md §unit .g bounds — spec⟩

**W0.4 — The FAIL branch is the live branch, re-measured today, and it strands ≥16 adjudicated
rows/limbs from this packet alone.** · **BLOCKER-bearing** · cl.3 · → **census**
- **mech (measured read-only 2026-08-28, this seat)**: (1) `node -p require('@mkbabb/glass-ui/package.json').version` → **7.0.0**, `package.json` pin **^7.0.0**; (2) exports filter `/watercolor/` →
  **`./watercolor-dot`** (present); (3) `grep -rn indicator dist --include='*.d.ts' | grep -ic slot` →
  **0**; (4) no 8.0.0 receipt. **0/4 — unchanged from the 2026-08-03 authoring measurement, 25 days
  on.**
- **stranded set from this packet (deduped by banked identity, counted once each)**: CPE **A-1**
  (BLOCKER) · GEN-**2** (BLOCKER) · MX-**1** face/seat limb (BLOCKER limb) · the glass-7 `tag=`/no-slot
  identity (ES-15 + ES-20 ≡ EY-27 ≡ CPE L-12 ≡ CSS L-8) · EC-**2**/EC-**3**/EC-**22** + GEN-**8** rail
  (the CC-105 cluster) · EY-**7** · EY-**32** · MX-**3** seats · MX-**32** · MX-**41** · AP-**17**
  (⇄CSP tension, W0.1). Corpus-wide denominator, this seat's grep: **62 `X-W4.g`/`X.W4.g` dispositions
  across 10 records**.
- **cure-lock (verbatim, wb-generate-pane route legend)**: "**X-W4.g rows are trigger-gated** (CLOSED
  unless the X-W0.j Glass-8 census PASSes — **on FAIL they join the cluster**)"; wb-extract-imageeyedropper:
  "**if W4.g never fires, EY-CLUSTER**."
- **cure shape**: the FAIL verdict enumerates, by id, the rows it strands and the register that
  receives them (W0.17) — "the bank stays shut" is not a disposition for 62 bookings.
- **seq**: census (batch 3) → sitting (batch 4) → X-W11's release transposition.
- **dissent**: none; W0.md:12 already rules "a FAIL is a complete result, not a deferral" — this row
  asks only that the result be enumerated. ⟨6 packet records' trigger-gated dispositions — record;
  four HG-18 probes re-run — measured 2026-08-28⟩

**W0.5 — The sitting's roster is short by two rows its own siblings condition their waves on.** ·
**MAJOR (gate arithmetic)** · cl.1 · → **sitting**
- **mech**: W0 §7/§X-W0.g/HG-13 name **seven** rows (DR-16 · DR-19 · DR-20 · DR-24 · DR-31 · DR-14's
  trigger · U-F12). **DR-21 is absent** from all three — yet W0.md:395 itself blocks X-W9 on "HG-13
  returns the DR-21 opt-out ruling" and W9.md:474(a) conditions CC-088's ninth carry on it.
  **CC-104/OP-1 is absent** — yet W10.md:6-7 opens X-W10 only when "the CC-104 precondition [is] ruled
  at the X-W0 owner sitting" and W10.md:412 lists "the CC-104/OP-1 ruling" as an X-W0 dependency.
  Counts, this seat: `grep -c DR-21 W0.md` → **1** (the Blocks clause only); `grep -c CC-104 W0.md` →
  **0**; `grep -c OP-1 W0.md` → **0**.
- **cure shape**: HG-13's roster reads **nine**, or the two sibling dependency clauses are corrected.
  E-3: the addendum states the reconciliation; W0.md's bytes stand.
- **seq**: before the sitting is scheduled (the packet is built at wave-open).
- **dissent**: DR-21 may be intended as a limb of DR-19/DR-20; this addendum does not elect — it names
  the gap and its two consumers. ⟨W0.md:188/:301/:395 · W9.md:474 · W10.md:6-7/:59/:412 — spec⟩

**W0.6 — Two further sitting-bound questions arrive from the corpus itself.** · **MINOR / INFO
(owner questions)** · cl.1 · → **sitting**
- **OQ-1 (EmptyState)**: "Does the empty-plate mark fall under PR-05's ornament-zeroing on Admin
  routes, or outside its jurisdiction?" VC:186 grants the mark under "Palette library and Browse";
  VC:218 rules Admin's register "labeling and scope, not … a fourth visual system"; PA:49's subject is
  boundary-repeating ornament. Routing verbatim: "**Route with X-W7's OM-15 receipts to the owner
  sitting.**" The record is explicit that its dead-axis facts (ES-4) do **not** depend on the answer.
- **library-band W.L9**: "the four admin rows to **DR-29's owner sitting**, suspend wired-or-deleted
  with its whole enforcement path" — four route-consumer rows already routed to the same sitting.
- **cure-lock**: HG-13's verdict-token law binds these too — "a row returning 'banked', 'pending', or
  'carried' is not a verdict".
- **seq**: same sitting; OQ-1 requires X-W7's OM-15 receipts in hand.
  ⟨EmptyState §OWNER-QUESTION + ES-4 · library-band W.L9 §GATES — record⟩

**W0.7 — HG-11's two-guard check is CSS-only and structurally cannot reach the corpus's JS-clock and
scroll-timeline motion.** · **MAJOR (gate validity)** · cl.1+cl.2 · → **X-W0.e**
- **mech**: HG-11 clears an assertion by "a CLEARED verdict citing both guards by path and line" —
  `demo/styles/animations.css:184` and glass-ui `a11y-overrides.css`, both CSS `@media
  (prefers-reduced-motion)` blocks. Three records show assertion classes those guards cannot decide:
  (a) **scroll-driven timelines** — GEN-29/M-34/MQ-1: `pane-scroll-fade` drives three scroll-driven
  animations that "ignore both `animation-duration` guards", "measured **byte-identical** under
  `reduce` by the quarantine's own probe"; (b) **JS clocks** — MX-9/MX-10/MX-13/MX-45: "every PRM row
  here is a JS-mechanism claim — **the class the quarantine's own architecture finding says the central
  CSS guards cannot reach**"; (c) **producer-internal transitions** — EY-32 rides guard-2's measured
  `transition-property` clamp in its own direction.
- **cure shape**: HG-11's CLEARED branch is admissible only for CSS-declared motion; JS-mechanism and
  scroll-timeline assertions take a **third disposition** (mechanism-out-of-guard-reach) naming the
  witness, so a false clear cannot be spelled with two true citations.
- **seq**: before X-W0.e writes stamps (batch 2), not after.
- **dissent**: none — every cited record explicitly carries **no** FALSE quarantine row; wb-mix and
  wb-extract-controls both say so in a §Motion-law block. ⟨wb-generate-pane PIN 7 + GEN-29 ·
  wb-mix-animationcanvas §Motion-law compliance · wb-extract-imageeyedropper EY-32 — record⟩

**W0.8 — HG-11's coverage set-difference gains five zero-row attestations, one classification
precedent, and one dated provenance correction.** · **INFO (evidence)** · cl.1 · → **X-W0.e**
- **zero-row attestations (grep run in-seat by each record)**: AuroraPane ("`motion-quarantine.md`
  carries no aurora/atmosphere row") · CurrentPaletteEditor ("no motion-quarantine row names
  CurrentPaletteEditor or SwatchHoverMenu") · EmptyState ("the quarantine contains **ZERO** EmptyState
  rows") · wb-mix ("a grep of the quarantine for mix rows returns none") · wb-extract-controls
  ("M-32/M-33 concern other components; M-31 likewise").
- **classification precedent (EmptyState)**: "the nearest law (M-09/M-13) classifies a species-swap
  contract as **state, not motion**" — ES-26 is carried as a state contract, never a PRM claim. Adopt,
  do not re-derive.
- **dated provenance correction (ShadowPalette)**: "`codex-provenance/motion-quarantine.md` **exists
  and is tracked at this HEAD** (landed 2026-08-03 per **X-W0 HG-11** / W6 H2 addendum — reader-2's
  'does not exist' provenance cell is dated)."
- **coverage extension (AuroraPane)**: "X.W6.j's transition gates are already **MOTION-SOURCED ·
  PENDING-QUARANTINE** by the wave's own text" — the quarantine's reach extends into wave gate text, a
  coverage axis HG-11's set-difference (against the 46 REPORT-AUTHORED rows) does not enumerate.
- **seq**: input to X-W0.e's dated addendum. ⟨5 packet records + ShadowPalette §Provenance — record⟩

**W0.9 — The completeness denominator counts axis FILES; two records demonstrate axis CONTENT it
cannot see.** · **MAJOR (validator meaning)** · cl.1+cl.2 · → **X-W0.c**
- **mech (a) — a false supersession chain (EC-20)**: "pass-4:3-4 claims pass 3 'folded passes 1 and
  2'; pass-3's register (read :512-545) carries no lying-readout row, no blend row, no D-18 — and
  pass-4 restates only deltas, so the drops propagate … **Any wave authored from the governing text
  alone builds on an incomplete record.**" Four rows (EC-8/EC-9/EC-10/EC-46) were restored only
  because the apotheosis went back to pass 1. Terminal words: "**formation-boundary process row**".
- **mech (b) — orphaned instruments (ADJ-i1)**: "A full pass-5 instrument run (probe-D14…D18-pass5,
  P5-1…4, frames-D14, 2026-07-29) has no report; **two of this roster's MAJORs and A-2's measured
  third arm live only in those JSONs**; **any completeness validator keyed to the three hashed reports
  reads GREEN over them.**"
- **cure shape (verbatim, CPE)**: "a `registry/COMPLETENESS-LEDGER` row" — X-W0.c's own file
  (re-homed here per §0a n.2). The supersession-chain check is its sibling: a governing pass claiming
  to fold priors is checkable against the priors' row ids.
- **seq**: X-W0.c is batch 3 and re-runs the generator; both rows must be minted **before** the ledger
  regeneration or the regeneration re-greens over them.
- **dissent**: CPE labels ADJ-i1 NO-WAVE-OWNER "not a product wave" — kept verbatim; the re-home is to
  the non-product wave it named. ⟨wb-extract-controls EC-20 · CurrentPaletteEditor ADJ-i1 — record⟩

**W0.10 — The 264 denominator and the CHALLENGED/REPORT-AUTHORED split no longer reproduce from the
source HG-6 names.** · **MAJOR (gate arithmetic)** · cl.3 · → **X-W0.c**
- **mech (measured 2026-08-28, this seat)**: `HYDRATION-LEDGER.md` now holds **286 canonical rows** =
  **235 EXISTS-ORIGINAL + 46 REPORT-AUTHORED + 5 UNWITNESSED-DIRECT** (its own Totals line at :13
  agrees: "235 original · 51 payload-less"). HG-6's baseline derives "213 + 5 = **218** CHALLENGED …
  **264** canonical rows" from the same file. The delta is **22 non-roster rows** — round-2/3 challenge
  variants (a `-r2|-r3|pass` grep names **20** of them: 4 `challenge-L-library-r2` · 4
  `challenge-D-design-r2` · 3 `-L-r3` · 2 `-D-r3` · 2 `-C-r2` · 1 `-C-r3` · pass-1/pass-2 spellings).
  `COMPLETENESS-LEDGER.md` still prints `264/264` at :20/:22/:196 — the 88×3 roster — so the two
  ledgers count different sets and only one of them says so.
- **why it matters, from the packet**: r2/r3 currency is load-bearing in four of these records —
  EmptyState "**r2 governs** the D and L axes"; wb-extract-controls "No r2 axis-promotion file
  exists … the newest pass of each axis governs"; wb-extract-imageeyedropper PIN 3 "**Round 3 exists
  and neither reader consumed it**"; wb-mix "exactly one challenge round per axis exists in the
  directory". A ledger that counts a round's FILE cannot express whether the round was CONSUMED.
- **cure shape**: X-W0.c states the **scope predicate** (which of the 286 rows are the 264 canonical
  roster) and prints **three** integers, not two (EXISTS-ORIGINAL · UNWITNESSED-DIRECT ·
  REPORT-AUTHORED), so UNWITNESSED-DIRECT cannot hide inside CHALLENGED — see W0.11's witness that one
  of the five behaved as report-authored under attack.
- **seq**: with HG-6's print change, before HG-7's self-test baseline is read as meaningful.
- **dissent**: none — W0.md's own FM-22 discipline ("record the re-measure") is the precedent this row
  applies to its own denominator. ⟨HYDRATION-LEDGER.md:13 + row census · COMPLETENESS-LEDGER.md:20/:22/:196
  — measured 2026-08-28; W0.md:248 — spec⟩

**W0.11 — UNWITNESSED-DIRECT sits inside the 218/240 CHALLENGED and one of the five did not survive
re-derivation.** · **MINOR (denominator semantics)** · cl.1 · → **X-W0.c**
- **mech**: wb-mix's L axis is **UNWITNESSED-DIRECT** (`HYDRATION-LEDGER.md:305`, definition :9-11
  re-read in-seat). Its adjudication ruled: "systematic 2-5-line anchor drift on a tree unchanged since
  `c654824e` (diff re-run, empty). **L's substance survives only where re-proven** — as practiced
  throughout" (K-12), and residue R-14: "challenge-L's live numbers wholesale … **adopted nowhere in
  this record without independent re-proof**."
- **cure shape**: the status is counted as its own integer (W0.10) and the ledger's definition line
  ("Neither status may be counted as a witnessed seat return") is enforced by the printed arithmetic,
  not only by prose.
- **seq**: with W0.10. ⟨wb-mix-animationcanvas K-12 · R-14 · HYDRATION-LEDGER.md:9-11/:305 — record⟩

**W0.12 — Two registry paths were superseded IN PLACE; the prior adjudication survives only in git
history, and a round-over-round finding loss is measured across one of them.** · **MINOR (record
durability)** · cl.1 · → **X-W0.c** (+ X-W0.a note)
- **mech**: wb-mix — "**SUPERSESSION**: the 2026-08-10 file at this path adjudicated an EARLIER reader
  round (13/5/7 · 25/8/5) at HEAD `54a97489`. This file supersedes it for the CURRENT round at
  `9c779130`. Its receipts were not trusted: every carried row … was re-verified this session."
  CurrentPaletteEditor — "this path carried an r1 adjudication (committed `e5c85303`, HEAD
  `2e334231`) that adjudicated a *different* worker pair … **Git history preserves r1**."
- **the measured loss**: wb-mix DISSENT §3 — "the current round **DROPPED** the prior round's smear
  mechanism (both readers graded the row MINOR without it) and reader-DU dropped the out-in re-mix
  finding reader-LC kept. **Adjudication continuity — carrying re-verified rows across rounds — is
  what caught both.**" The escalation (MX-31 MINOR → MAJOR) exists only because a superseded file was
  read.
- **cure shape**: the hydration/completeness pair records supersession (path · superseded commit ·
  round) so the prior is reachable without `git log`; both records already practice the prose form.
- **seq**: X-W0.c, before regeneration. ⟨wb-mix §SUPERSESSION + DISSENT 3 · CurrentPaletteEditor
  §Supersession note — record⟩

**W0.13 — HG-1's census is a moving target: it moved twice during this fold, and a sibling
sub-tranche is the writer.** · **MINOR-but-gating** · cl.3 · → **X-W0.a**
- **mech (measured read-only, this seat, 2026-08-28)**: `git ls-files --others --exclude-standard docs/ | wc -l`
  → **1828**, then **1836** eleven minutes later. By tree: `docs/tranches/V/apotheosis` **1798** ·
  `docs/tranches/W/audit` **16** · `docs/tranches/T/audit` **11** · `docs/tranches/X/keyframes`
  **3 → 11** (X·KF conformance PASS-1 checks, authored live). `docs/tranches/V/megatranche` → **0**
  (was 33 at authoring). Authoring baseline W0.md:223 = 1858.
- **cure shape**: HG-1 asserts an absolute zero at close over trees censused at open; X-W0.a must state
  the **closure rule for trees created after its single commit** (re-run at close, or scope the gate to
  the open-time set) — otherwise HG-1 is unclosable while SS-1/SS-2 and SS-3/SS-4 author
  (COHESION §0b). **Sequencing lock**: a tree an active sub-session is writing is TRACK-or-defer,
  never ARCHIVE/DELETE.
- **seq**: X-W0.a runs alone and first; the rule belongs in `TRACK-OR-ARCHIVE.md`, not in a later fix.
  ⟨measured 2026-08-28 ×2 — this seat; COHESION §0b — spec⟩

**W0.14 — HG-10's untracked half is discharged by event; only the ledger-status half remains RED.** ·
**INFO** · cl.3 · → **X-W0.e**
- **mech**: W0.md:273 born-RED reads "the r2 artifacts exist on disk but are **untracked** — all 33
  untracked files under `docs/tranches/V/megatranche/` are exactly these three slugs' r2 challenge
  files and evidence probes". Measured 2026-08-28: the megatranche tree contributes **0** untracked
  files. The surviving RED is the status half ("the ledger still carries 46 REPORT-AUTHORED rows") —
  and the 46 is a **fixed constant** (`CLOSURE_MANIFEST_46` in `hydrate-reports.mjs`, per the ledger's
  own definition block), so re-statusing three slugs requires a generator change, not a ledger edit.
- **cure shape**: MEASURE-AT-OPEN both halves separately; the gate's falsifier ("a named slug whose row
  stays REPORT-AUTHORED, **or** whose evidence stays untracked") already reads as two conditions —
  the addendum only records that one is now green and why the other cannot be cured by hand.
- **seq**: X-W0.e (batch 2) → X-W0.c re-runs the generator (batch 3), the order §Disjointness already
  fixes. ⟨measured 2026-08-28 — this seat; HYDRATION-LEDGER.md:6-8 — record⟩

**W0.15 — The typed graph must type the alias-barrel class and name two SCCs the four do not
include.** · **MAJOR (gate completeness)** · cl.1 · → **X-W0.h**
- **mech (SCCs)**: HG-14 requires "the four frontend SCCs (Admin/provider, Markdown, Gradient, Dock)
  appear by name with an owner each", and X-W8 binds CC-079's four SCC owners from that output. Two
  further cycles are adjudicated-real and in neither: **CPE L-24** — "palettes ↔ shell mutual
  dependency (type-import up at `usePalettePorts.ts:19`, five `SESSION_PORT_KEY` value-imports down);
  **lattice cycle, not load-time** → X-W8 (SCC subtraction)"; **AuroraPane AP-19** — "a **bidirectional
  area edge**, both edges aurora's … the eager boot chunk carries a module living in a lazy pane's
  directory. **No X wave mints `demo/atmosphere/`** (X-W8 is subtraction-only for `demo/ui`)" —
  NO-WAVE-OWNER, "interim guard: no new edge in either direction".
- **mech (alias barrels)**: three independent censuses converge — `find demo/ui -type f` = **19, all
  index.ts** (wb-extract-controls K-L, killing r5's "20", "load-bearing for the A11 gate as written");
  "19 one-line re-export barrels and **zero** `.vue` files" (EmptyState K-13, killing "~178 files");
  "demo/ui = 19 dirs" (wb-generate-pane). GEN-21 sizes the edge class: "**90+ edges**". HG-14's own
  falsifier is written about exactly this class ("remove one resolver-alias edge and either a named SCC
  disappears or the round-trip digest changes").
- **cure shape**: v3 names the two additional cycles with owners **or** records, at its own edge
  typing, why they are not SCCs; the 19-barrel alias class is typed, never collapsed. **Lock**: the
  denominator is unaffected — `demo/ui` holds zero SFCs, so HG-15's 88 stands.
- **seq**: X-W0.h (batch 2) before X-W8 opens (W8.md:97/:277/:432 depend on it by name).
- **dissent**: AP-19 is booked NO-WAVE-OWNER at the boundary; this row does **not** re-home it — it
  requires only that the graph SEE it. ⟨CurrentPaletteEditor L-24 · AuroraPane AP-19 ·
  wb-extract-controls K-L · EmptyState ES-17/K-13 · wb-generate-pane GEN-21 — record⟩

**W0.16 — The dead-lint-lattice identity's "X-W0/X-W1" routing is bounded, not deleted.** · **INFO
(routing correction)** · cl.1 · → **record-only at X-W0; execution X-W8; gate X-W1**
- **mech**: EC-15 — "every module-lattice lint rule is dead — globs name the deleted `demo/@` tree
  (:235-238, :275-276 verified; `demo/@` ABSENT). **FOLD — one identity**: ColorSpaceSelector L-5(a) +
  AXIS-PROMOTIONS' ActionFeedback L-2 headline, already carried → **X-W0/X-W1**. Not re-booked."
  GV-4 routes the same identity "→ X-W0/X-W1 fold + X-W8.b". X-W0 writes no `eslint.config.js` byte:
  its bounds are `docs/**` + `workflows/**` and C-06 rests on "zero product source".
- **cure shape**: the identity keeps its id (**ColorSpaceSelector L-5(a)** — anti-rename, for life);
  its executable half is **X-W8** ("delete the six dead `demo/@/**` file globs", W8.md:42, per
  ActionFeedback AF-13 and VersionHistoryDrawer VHD-28 → "X-W8 scope 3"); its born-RED
  `--print-config` gate is **X-W1**'s; X-W0 carries it only as a disposition cell, which no unit's
  CARRY-CUT-LEDGER carve currently covers (X-W0.i = §0 alias + CC-011's row; X-W0.g = seven owner
  rows).
- **dissent**: two records route to X-W0 by name; this addendum does not overrule them — it records
  that the routing has no writable path in W0 as bounded, and asks the sitting or X-W11's 117-row walk
  to elect. ⟨wb-extract-controls EC-15 · wb-gradient-visualizer GV-4 · wb-mix MX-25/MX-35 ·
  CurrentPaletteEditor L-6 — record; W0.md §File Bounds/§COMPLETABLE — spec⟩

**W0.17 — The NO-WAVE-OWNER register that §3.3 gates on has stopped accreting, and no wave file names
it.** · **MAJOR (formation completeness)** · cl.1 · → **X-W0 (home unelected — two candidates)**
- **mech (measured, this seat, 2026-08-28)**: `NO-WAVE-OWNER` occurs **604 times across 73 of the 92**
  value.js corpus records; `grep -c NO-WAVE-OWNER docs/tranches/X/waves/*.md` → **0 in all twelve**;
  COHESION §4 (the register of record, "live; seeded from SS-7 batch 1") holds **14** source rows and
  is annotated "(accretes per batch)" — SS-7 closed at 88/88 (COHESION §0a). COHESION.md is named in
  **no** wave's file bounds (grep, 0 of 12).
- **the gate it feeds (verbatim, COHESION §3.3)**: "Every NO-WAVE-OWNER row (§4) reaches a terminal
  home — a sub-tranche wave, the next formation boundary's ledger, or a kill — **before X-whole stamps
  SPECIFIED**."
- **packet witnesses (cluster-level, deduped)**: **EY-CLUSTER** (eyedropper interaction/render; 4
  BLOCKER + ~20 rows; "no wave in tranche X owns a single line of the code that must change") · the
  **extract session cluster** (XP-2/3/4/23/27, extended by EY-10/EY-12/EY-23 + EC-10/EC-25/EC-34/EC-36)
  · the **pin-gated ExtractControls set** ("ELEVEN are pin-gated edits to a file no X wave owns") ·
  **GEN-CLUSTER** (`GeneratePane.vue` · `useColorGeneration.ts` · `generate-color.ts` · `o20-generate-plate.spec.ts`
  · the absent o18 enrollment; holds GEN-1 BLOCKER) · **MX-CLUSTER** (`mixStage.ts` · `useMixingState.ts`
  · `MixResultDisplay.vue` beyond the face carve · `ErrorBoundary.vue` · `demo/palettes/mix.ts` · both
  RED mix specs; holds MX-1 BLOCKER) · **AP-19 + AP-34** · **CPE ADJ-i1** · **ES-13 + ES-18 + ES-31**.
- **cure shape (unelected, both named)**: **(A)** `docs/tranches/X/W0/NO-WAVE-OWNER-REGISTER.md`
  (create) — cluster-level, one terminal-home column per §3.3; or **(B)** `docs/tranches/X/COHESION.md`
  (modify-carve §4 only). The addendum elects neither; it names the measured gap and both homes.
- **lock (W0's own Archaeology, verbatim)**: "'the receiving wave' was **a pointer with no referent**,
  the same shape as the `V·L5` law keyed to a wave that was never defined" — the register is that same
  disease at the boundary, and X-W0 is the wave that exists to kill it.
- **seq**: before X-whole SPECIFIED (COHESION §3.6); mechanically X-W0.a/.c are the units with registry
  write paths. ⟨COHESION §1 SS-7 outputs · §3.3 · §4 · §0a — spec; 604/73 and 0/12 — measured 2026-08-28;
  7 packet records' cluster bookings — record⟩

**W0.18 — Census condition 3 has a named consumer-side witness and a producer coupling.** · **MINOR
(census evidence)** · cl.1 · → **census** (+ cross-edge to X-W9 §12)
- **mech**: HG-18 condition 3 asks for "the TR#84 consumer-painted **indicator slot** present in
  `dist/**/*.d.ts` declarations **and** in the packed exports". AuroraPane measured what 7.0.0 actually
  ships: the marker inks `var(--select-dot-color, var(--glass-accent, currentColor))` and
  `--glass-accent` is a **registered** `@property` with `initial-value: transparent`, "so the chain can
  never fall through" — the selected option paints **zero pixels for every consumer**; the producer
  reserves the indicator gutter unconditionally unless `hideIndicator`, leaving "a permanently blank
  ~24px reserve".
- **cure-lock (verbatim, AP-26 rider)**: "the house direction (VC:90's ColorSpaceSelector precedent) is
  the producer gutter WITH a real marker, so **the terminal cure is AP-2's token + packet, and the
  interim must be reverted when the marker inks**."
- **why it binds the census**: condition 3 is satisfied only if 8.0.0 lets a consumer **paint** the
  indicator — not merely hide the gutter. The census records the measured value; the producer question
  rides the standing BH relay at X-W9's coordination step. X-W0 writes no relay byte.
  ⟨AuroraPane AP-2 · AP-26 · K-13 — record⟩

**W0.19 — Conditions 1/2 carry six independent corroborations from six HEADs; the census may cite
them and must not inherit them.** · **INFO** · cl.3 · → **census**
- **mech**: the same installed bytes read by six seats — CurrentPaletteEditor ⟨`2f6cf70e`⟩ · EmptyState
  ⟨`ddf2d5f8`⟩ · wb-extract-imageeyedropper ⟨`1994eb53`⟩ · wb-generate-pane ⟨`954b255c`⟩ ·
  wb-mix-animationcanvas ⟨`9c779130`⟩ · this seat ⟨2026-08-28⟩: `inheritAttrs:!1`, class/style-only
  forwarding, `renderSlot` **0**, `aria-hidden:"true"` span root, `pointerEvents:"none"`, **no `tag`
  prop**, and `./watercolor-dot` still in the packed exports. No drift across 08-04 → 08-28.
- **lock (verbatim, HG-18)**: "**MEASURE-AT-OPEN and again at close — the verdict is dated, never
  inherited.**" The six corroborations are context for the verdict's stability, never the verdict.
- **correction carried**: the "fifth independent verification" claim appears twice (§0a n.1).
  ⟨5 packet records + measured 2026-08-28 — record + measured⟩

**W0.20 — The DR-19/CC-019 structural ban is already honoured prospectively, and the corpus supplies
the substitute shape.** · **INFO (sitting evidence)** · cl.2 · → **sitting / X-W0.f**
- **mech**: HG-13 wants "the DR-19 ruling … with a grep-checkable structural ban in canon (no
  `scripts/**/proof-*.mjs`)". CurrentPaletteEditor writes the constraint into its own cure text:
  "**DR-19 / CC-019 (owner law)**: no cure emerging from this corpus may take the shape of a
  `proof-*.mjs` script — neither reader proposed one; **the phantom-name family dies structurally**",
  and C-4's cure carries "the phantom family dies structurally (types/eslint/build), **NEVER a proof
  script**". The substitute shape is demonstrated at EC-43: a defect "decidable **STATICALLY** … a pure
  unit test over the sRGB interpolation of two `certifyAccentInk` endpoints — no seat proposed it, this
  record does."
- **relevance to L-19**: born-RED gates naming real witnesses, not proof scripts, is the same law from
  the other end; the packet's records comply without being asked.
- **seq**: input to the sitting's DR-19 row; CC-019's tombstone (X-W0.f) may cite the corpus compliance
  as the ban's first evidence. ⟨CurrentPaletteEditor §hygiene 3 + C-4 · wb-extract-controls EC-43 —
  record⟩

### 1a. Declared silences (cl.4 — measured, not missed)

These X-W0 units receive **nothing** from this packet; the silence is stated so a later pass does not
read it as an omission: **X-W0.b** (the 181-event prompt recap — no record touches
`raw-prompts/` or the clean-pass contradiction) · **X-W0.d** (the 7e28 worktree corpus — no record
cites a `formation/codex-worktree-7e28/**` path) · **X-W0.i** (the `V·L5` retirement —
`grep -l 'V·L5\|V\.L5' registry/adjudicated/*.md` → **0 files**, measured 2026-08-28, consistent with
HG-17's own `layout-gestalt.md` probe) ·
**X-W0.f's four owner-ruled tombstones** (CC-019/020/021/023 — only CC-019 gets corpus evidence, at
W0.20). ColorNutritionLabel's sweep line ("CARRY-CUT-LEDGER.md **X-W0..X-W11** rows") is a registry
identity-sweep citation, not a routing, and is folded nowhere.

---

## 2. §Gates — 8 candidates (7 SHARPEN · 1 NEW)

Each states its born-RED with a **real witness** (L-19: no proof scripts; every falsifier is a probe
over shipped bytes, a spec byte, or a set-difference over existing records).

**G-A · SHARPENS HG-18 — the FAIL branch is enumerated, not merely recorded.**
The census file names, by row id, every adjudicated disposition that is trigger-gated on it, with its
FAIL home. **BORN-RED (measured 2026-08-28)**: `ls docs/tranches/X/W0/GLASS8-REPIN-CENSUS.md` → ABSENT;
the four conditions re-run **0/4**; corpus-wide **62 `X-W4.g` dispositions across 10 records** (this
packet contributes ≥16 rows/limbs incl. 2 BLOCKERs + 1 BLOCKER limb), none of them enumerated anywhere.
*Falsifier*: drop one id and the set-difference against `grep -o 'X[-.]W4\.g' registry/adjudicated/*.md`
names the missing row. *Why it fails for its intended reason*: a FAIL that says only "the bank stays
shut" leaves 62 bookings homeless — the disease of Archaeology ¶3, one level up.

**G-B · SHARPENS HG-13 — the roster reads nine, and every consumer's precondition is in it.**
**BORN-RED (measured)**: W0.md names seven rows at :188/:301; `grep -c 'DR-21' W0.md` → **1** (in the
`Blocks` clause alone), `grep -c 'CC-104' W0.md` → **0**, `grep -c 'OP-1' W0.md` → **0**; while
W9.md:474(a) blocks CC-088 on the DR-21 ruling and W10.md:6-7/:412 opens X-W10 only on "the CC-104/OP-1
ruling". *Falsifier*: run the sitting on seven rows and X-W9's §X.W9.f and X-W10's open condition
remain unruled while HG-13 reads green — the gate certifies a sitting that did not answer its
consumers.

**G-C · SHARPENS HG-11 — guard reach is classified before a clear is spelled.**
Every motion assertion is dispositioned CSS-guarded (clear/quarantine) **or**
mechanism-out-of-guard-reach with its witness named. **BORN-RED (witnesses in-record)**: GEN-29/MQ-1 —
a scroll-driven timeline "measured **byte-identical** under `reduce` by the quarantine's own probe";
MX-9/10/13/45 — JS-clock claims, "the class the quarantine's own architecture finding says the central
CSS guards cannot reach"; EY-32 — a producer-internal transition riding guard-2's measured
`transition-property` clamp. *Falsifier*: a CLEARED verdict citing `animations.css:184` +
`a11y-overrides.css` over a `requestAnimationFrame` or `animation-timeline` assertion is a false clear
spelled with two true citations — this gate is what makes it unspellable.

**G-D · SHARPENS HG-6/HG-7 — three integers and a stated scope predicate.**
The validator prints EXISTS-ORIGINAL · UNWITNESSED-DIRECT · REPORT-AUTHORED as three integers and names
the predicate selecting the canonical roster. **BORN-RED (measured 2026-08-28)**: `HYDRATION-LEDGER.md`
= **286 rows (235 / 5 / 46)**, its Totals line at :13 agreeing; `COMPLETENESS-LEDGER.md:20/:22/:196`
still prints **264/264**; HG-6's baseline derives **218/46 against 264** from the first file. 22
non-roster rows separate them (a `-r2|-r3|pass` grep names 20). *Falsifier*: re-badge one
UNWITNESSED-DIRECT row and the three integers stop reconciling against the ledger's own status column;
change the roster predicate and the two ledgers' totals diverge visibly instead of silently.

**G-E · SHARPENS HG-14 — the SCC roster is a set with a stated closure, not a list of four.**
Graph v3 names every runtime/load/ownership SCC it finds, with an owner or a written reason for having
none, and types the resolver-alias barrel class. **BORN-RED (witnesses in-record)**: two adjudicated
cycles are outside HG-14's named four — CPE **L-24** (palettes ↔ shell, "lattice cycle, not
load-time") and AuroraPane **AP-19** (aurora feature ↔ boot, bidirectional, NO-WAVE-OWNER); the alias
class is **19 barrels / 90+ edges** (three converging censuses, two killed miscounts: "20 files" and
"~178 files"). *Falsifier*: X-W8 binds CC-079's four SCC owners from a graph that cannot see two
adjudicated cycles — the subtraction then proves only what the graph was shaped to show.

**G-F · NEW — the NO-WAVE-OWNER register exists and closes by set-difference.**
A register (home elected per §3, entry 2) carries one row per adjudicated NO-WAVE-OWNER cluster with a
terminal home per COHESION §3.3. **BORN-RED (measured 2026-08-28)**: `NO-WAVE-OWNER` = **604
occurrences across 73 of 92** value.js corpus records; COHESION §4 holds **14** source rows, seeded
from SS-7 batch 1, while SS-7 closed 88/88; `grep -c NO-WAVE-OWNER docs/tranches/X/waves/*.md` → **0 in
all twelve**; COHESION.md is in no wave's file bounds. *Falsifier*: the register's cluster set minus the
corpus's cluster set must be ∅ in both directions; a cluster with no terminal home blocks X-whole
SPECIFIED by §3.3 rather than passing unnoticed. *Why NEW*: no HG-n covers it — HG-1..HG-18 gate
tracking, denominators, tombstones, the sitting, the graph and the census; none gates the disposition
of adjudicated rows no wave owns.

**G-G · SHARPENS HG-1/HG-2 — dangling receipts are censused, and post-commit trees have a stated
closure rule.**
`TRACK-OR-ARCHIVE.md` carries (a) one row per untracked tree, as specified, plus (b) one row per
**cited-but-absent instrument**, disposition RE-SCRIPT (→ X-W1's oracle slate) or STRIKE-THE-CITE, and
(c) the closure rule for trees created after X-W0.a's commit. **BORN-RED (witnesses in-record +
measured)**: EmptyState **U-3** — "`probe-state.mjs`/`probe-deadlock.mjs` are **not banked** in
`probes/` (only PNGs are)"; EY residue 8 — r3's "probe scripts live in a **session scratchpad, not the
tree**"; GEN **R-5** — a live "deterministically failing ×2" run with no banked script; census measured
**1828 → 1836 within one session**, `docs/tranches/X/keyframes` **3 → 11**, megatranche **33 → 0**.
*Falsifier*: HG-1's zero passes vacuously over evidence that never entered `docs/` — three
load-bearing instruments in this packet alone are invisible to it; and a zero asserted at close is
unreachable while X·KF/X·F author, so the rule must be written, not discovered.

**G-H · SHARPENS §File Bounds / Do-NOT-touch — the adjudicated registry is immutable in X-W0 too.**
`registry/adjudicated/**` is declared read-only (E-3 addenda only) in W0's bounds. **BORN-RED
(measured)**: W0.md's bounds and Do-NOT-touch name neither the directory nor the E-3 clause, while
X-W0.c regenerates ledgers keyed to those files and X-W0.e writes r2 artefacts beside three slugs;
W6.md:120 already carries the exact clause ("any `docs/tranches/V/megatranche/registry/adjudicated/*`
(immutable adjudication records; E-3 addenda only)"). *Falsifier*: `git diff --name-only` over X-W0's
eleven commits intersecting `registry/adjudicated/` is non-empty — one line, and the incredulity
record the tranche is built on is no longer immutable.

---

## 3. §BoundsDelta — files this wave must ADD (or re-verb) per the records

| # | path | today | delta | source |
|---|---|---|---|---|
| 1 | `docs/tranches/V/megatranche/registry/adjudicated/**` | **absent from W0's bounds and Do-NOT-touch** | **ADD as read-only / immutable (E-3 addenda only)**, mirroring W6.md:120 | G-H · all 7 records (E-1/E-3 closing lines) |
| 2 | `docs/tranches/X/W0/NO-WAVE-OWNER-REGISTER.md` **(candidate A)** — or `docs/tranches/X/COHESION.md` **modify-carve §4 only** **(candidate B)** | A: does not exist · B: **named in no wave's bounds (0 of 12)** | **ADD one of the two** (unelected — the sitting or X-W11's walk elects) | W0.17 · G-F · COHESION §3.3/§4 |
| 3 | `docs/tranches/V/megatranche/workflows/hydrate-reports.mjs` | `execute, no write` | **→ `modify-carve`** — the supersession + dangling-receipt + three-integer columns must come **from the generator**; §Disjointness forbids touching `HYDRATION-LEDGER.md` by hand, and the 46 is a constant in this file | W0.10 · W0.12 · W0.14 · G-D |
| 4 | `docs/tranches/X/refinement/**` | untracked (this file, written today) | **TRACK** at X-W0.a — never ARCHIVE: the fold files are the addendum layer each wave reads beside its spec | W0.13 · reading rule §0 |
| 5 | `docs/tranches/X/keyframes/**` | covered by W0.md:99 (the census clause) — **no new path** | **sequencing lock**: TRACK-or-defer while SS-1/SS-2 author; never ARCHIVE/DELETE a live sub-session tree (measured 3 → 11 files mid-session) | W0.13 · COHESION §0b |
| 6 | `docs/tranches/X/W0/GLASS8-REPIN-CENSUS.md` · `OWNER-SITTING-2026-08-XX.md` · `TRACK-OR-ARCHIVE.md` | in bounds (`create`) — **content deltas only** | census: + FAIL-branch enumeration (G-A) + the X-W4.g receiving-bounds delta (W0.3) · sitting: roster **7 → 9** (G-B) + 2 corpus questions (W0.6) + the CC-105/G-PAINT reconciliation (W0.1) + the CC-044 waiting question (W0.2) · track-or-archive: + DANGLING-RECEIPT section + post-commit closure rule (G-G) | W0.1–W0.6 · W0.13 |
| 7 | `docs/tranches/X/W0/GRAPH-V3/**` · `registry/COMPLETENESS-LEDGER.md` | in bounds — **content deltas only** | graph: + the two adjudicated cycles (CPE L-24 · AP-19) + alias-barrel edge typing (19 barrels / 90+ edges) · ledger: + three integers, the scope predicate, the supersession + orphaned-instrument rows | W0.9 · W0.10 · W0.12 · W0.15 |

**Not added, stated once**: `eslint.config.js` (W0.16 — X-W0 writes no product/config byte; C-06
depends on it) · `scripts/dev/dev.sh` (unowned; the NEVER-touch posture stands until the sitting rules
CC-021 — W1.md:169 · W2.md:70 · W3.md:156 · W7.md:138 · W11.md:103 all rely on it) ·
`node_modules/@mkbabb/glass-ui/**` (read/hash only, as W0.md:101 already binds).

---

## 4. §CrossEdges — 19

**Out of X-W0** (declared from this end; each names the byte at the far end):
1. **X-W0.j → X-W4.g** — the fire notice; W4.md:22/:151/:290/:336/:445 ("one cut, dated: X-W0's
   `GLASS8-REPIN-CENSUS.md` verdict, four measured condition values"). Carries W0.3's bounds delta.
2. **X-W0.j → X-W11** — the release table transposes the verdict; CC-109/CC-116 discharged by it and
   CC-117's terminal word read from it (W11.md:373).
3. **X-W0.j → the boundary register (W0.17)** — the FAIL branch's stranded set: EY-CLUSTER,
   GEN-CLUSTER, MX-CLUSTER, the extract session cluster, the pin-gated ExtractControls set.
4. **X-W0.h → X-W8** — CC-078 bounds + CC-079's four SCC owners (W8.md:97/:277/:432, "X-W0 authors the
   bytes"); G-E's two extra cycles ride this edge.
5. **X-W0.h → every frontend wave** — HG-15's 88-SFC denominator, "cited rather than re-derived";
   `demo/ui`'s 19 barrels hold **zero** SFCs and do not perturb it.
6. **X-W0.g → X-W9** — DR-21's opt-out ruling gates CC-088's ninth carry (W9.md:474a); also (c) "no
   gate here may cite `264/264`" — which W0.10 now re-scopes.
7. **X-W0.g → X-W10** — the CC-104/OP-1 ruling is X-W10's open condition (W10.md:6-7/:412); CC-018's
   optional bracket set opens only on the owner's word (W10.md:59).
8. **X-W0.g → X-W7** — OQ-1 travels with X-W7's OM-15 receipts (EmptyState §OWNER-QUESTION).
9. **X-W0.g → X-W1/X-W2/X-W3/X-W7/X-W11** — CC-021 (`scripts/dev/dev.sh`): five Do-NOT-touch clauses
   stand until the sitting rules it.
10. **X-W0.c → every wave** — HG-6's denominator law (no unqualified `264/264` in gate text), now with
    a stated scope predicate (G-D).
11. **X-W0.a → X-W1** — CC-032/CC-054's u-gestalt frame evidence must be tracked before it can be a
    born-RED baseline (W0.md:394).
12. **X-W0.a → X-W5** — gate D2's baseline is `docs/tranches/T/audit/pi/u-gestalt/probe2-log.txt`
    (W5.md:282, "RED, measured: untracked"); measured today: `docs/tranches/T/audit` still contributes
    **11** untracked files.
13. **X-W0.e → X-W6** — X.W6.j's transition gates are already MOTION-SOURCED · PENDING-QUARANTINE by
    the wave's own text (AuroraPane §Motion disclosure); G-C's classification decides how they clear.
14. **X-W0.e → X-W10** — the motion canon consumer; GEN-29's cure is named at W10:73 ("move
    PaneHeader.vue's three scroll-timeline declarations inside `@media (prefers-reduced-motion:
    no-preference)`").

**Into X-W0** (other records' routings, folded above):
15. **wb-gradient-visualizer GV-4 → X-W0/X-W1** — the L-5(a) identity; bounded at W0.16 (executable
    half X-W8.b, gate half X-W1).
16. **ShadowPalette §Provenance → X-W0 HG-11** — the quarantine's landing date is the correction that
    dates a reader's "does not exist" cell (W0.8).
17. **library-band W.L9 → DR-29's owner sitting** — four admin route rows (W0.6).
18. **ColorNutritionLabel §Registry identity sweep → CARRY-CUT-LEDGER X-W0..X-W11 rows** — a
    sweep citation, not a routing (declared silence, §1a).

**Sideways (X-W0 writes none of these; recorded so the fold does not re-mint them)**:
19. **BH/glass relay (SS-6's ONE batched letter, COHESION §4a)** — AP-2's producer half (the registered
    `@property` root cause, W0.18) · ES-5/ES-15/ES-30's three GLASS-RELAY items · EC-7's
    `aria-valuetext` prop-through (already seated in the communiqué) · GEN-28's seed-staleness ·
    EY-16's hover-tint variant · CPE C4-3's djb2 collision. **X-W0's only glass act is the census
    verdict** (W0.md:55) — no relay byte is written here.

---

## 5. Close

Rows **20** · gates **8** (7 sharpen HG-1/HG-2/HG-6/HG-7/HG-11/HG-13/HG-14/HG-18 and W0's bounds; 1
new) · bounds deltas **7** · cross-edges **19** · declared silences **4**. Status **planned**;
nothing here opens product source, opens a §1.M bank row, or performs any part of the atomic cut.
The wave's own words, which this addendum only makes checkable: *"no claim this tranche will rely on
rests on … a producer trigger no unit is charged with evaluating."* The packet's finding is the
mirror image — the trigger now has a unit, and **the FAIL branch it is overwhelmingly likely to
return has no register to fall into**.

— end of round-0 addendum. The dated wave specs and every adjudicated record remain byte-untouched
(E-3/E-1).

**§5 is SUPERSEDED for arithmetic only, 2026-08-28 (fold round 1).** Its bytes stand as the round-0
close. Current counts: rows **28** · gates **11** (7 sharpen · 4 new) · bounds deltas **10** ·
cross-edges **25** · declared silences **5** — see **§6.8**. Status is still **planned**; the
round-1 repair opened no product source, no §1.M bank row, and no part of the atomic cut.

---

## 6. §ROUND-1 REPAIR ADDENDUM — X·V fold round 1, FOLD REPAIR SEAT · dated 2026-08-28

SERVED MODEL ID (repair seat, declared not inherited): `claude-opus-5[1m]`.

### 6.0 Posture, laws, and what this section is

**What it is.** The X-W0 seat's round-1 repair, written against three inputs: (1)
`refinement/ROUND-1-ASSIGNMENTS.md` — this seat's work order (§A one NWO row · §B zero census rows ·
§C one EXTERNAL row); (2) `refinement/PASS-1-CHECK-RETURN.json` — the pass-1 register, whose eight
defects name **no** defect against this fold file (the BLOCKER's per-record escape table contains no
X-W0-owned identity, and §0's own arithmetic/gapless-numbering axes are listed among the check's
verified HOLDINGS); (3) this seat's **own id-keyed census, re-run fresh today** over the 92-record
corpus — the roster is a floor, not a ceiling, and the fresh run found **seven** row-classes the
work order does not list.

**Laws honoured, stated so they are checkable.**
- **Banked ids for life** (anti-rename, M-25). Every id below is the id its record gave it. Citation
  key is ⟨record.md · id⟩ — bare ids collide across six namespaces, and three collisions are live in
  this section alone: ⟨MigratePalettesDialog · A-1⟩ ≠ ⟨CurrentPaletteEditor · A-1⟩ (W0.2);
  ⟨App · D-4⟩ ≠ ⟨ConfigSliderPane · D-4⟩ (W0.1's G-PAINT cut); ⟨AboutPane · AB-4/AB-5⟩ ≠
  ⟨shell-dock-actionbarlayer · AB-4/AB-5⟩ (X-W8-FOLD W8.31/G-8).
- **Locks, cure-shapes and dissents verbatim where load-bearing.** Every `"…"` below is byte-quoted
  from the cited record, wave spec, spine, or sibling fold.
- **External rows carry mail provenance, not registry ids** (§6.4).
- **Status `planned`.** Nothing here opens product source, opens a §1.M bank row, performs any part
  of the atomic cut, or edits `docs/tranches/X/waves/**` / `CONFORMANCE-2026-08-03.md` /
  `registry/adjudicated/**` (E-1/E-3 — this file remains the fold's sole write).
- **Arithmetic dated, never silent** (§6.8), and §0's block carries the pointer.

### 6.1 The census, re-run fresh — method, receipts, and what it returned

**Method (falsifiable, re-runnable).** Corpus = the 92 non-`kf-*`/`fr-*`/`pt-*` `*.md` at
`docs/tranches/V/megatranche/registry/adjudicated/` (non-recursive; the 93rd entry `delta-r3` is a
directory). A row = a bold-leading id line (`- **ID` / `| **ID`); a row's block runs to the next such
line. X-W0-routed = the row line **or its continuation block** carries an `X-W0` token in any spelling
this corpus uses — hyphen `X-W0`, en/em-dash and non-breaking variants, middle-dot `X·W0`, dotted
`X.W0`, and the dotted unit forms `X-W0.a` … `X-W0.j`. Presence in this fold = boundary byte-match of
the id in `X-W0-FOLD.md`.

**Receipt 1 — the token census.** `grep -ohE 'X[-·.‐‑‒–—]W0[.a-z]*'` over the 92 records → **17
occurrences across 10 records** (`X-W0.j` ×8 · `X-W0` ×8 · `X-W0..X-W11` ×1). Resolved to rows and
non-row citations:

| ⟨record.md · id⟩ | spelling | this fold's home | status |
|---|---|---|---|
| ⟨AuroraPane.md · AP-17⟩ (+ R-8 table, DISSENT §4) | `X-W0 sitting` ×3 | W0.1 | CARRIED |
| ⟨CurrentPaletteEditor.md · A-1⟩ | `X-W0 owner-sitting` | W0.2 | CARRIED |
| ⟨wb-extract-controls.md · residue 12⟩ (K-S block) | `X-W0.j census as the SOLE release event` | W0.3 | CARRIED |
| ⟨wb-extract-controls.md · EC-15⟩ | `→ **X-W0/X-W1**` | W0.16 | CARRIED |
| ⟨wb-gradient-visualizer.md · GV-4⟩ (GV-41 block) | `→ X-W0/X-W1 fold + X-W8.b` | W0.16 | CARRIED (bytes cured at W0.28) |
| ⟨EmptyState.md · §Wave-authority⟩ | `trigger-gated on the X-W0.j census` | W0.4 | CARRIED (header, not a row) |
| ⟨wb-generate-pane.md · §Route-legend⟩ (D-5 block) | `CLOSED unless the X-W0.j Glass-8 census PASSes` | W0.4 | CARRIED (legend, not a row) |
| ⟨wb-extract-imageeyedropper.md · §Headline 1⟩ | `closed unless X-W0.j's census PASSes` | W0.4 | CARRIED (headline, not a row) |
| ⟨wb-mix-animationcanvas.md · §Instruments⟩ | `CLOSED unless X-W0.j census PASSes` | W0.4 | CARRIED (instruments, not a row) |
| ⟨ShadowPalette.md · §Provenance⟩ | `landed 2026-08-03 per X-W0 HG-11` | W0.8 | CARRIED |
| ⟨ColorNutritionLabel.md · §Registry identity sweep⟩ | `CARRY-CUT-LEDGER.md X-W0..X-W11 rows` | §1a | DECLARED SILENCE (sweep citation, not a routing) |

**Verdict on the token census: 11 of 11 already carried. Zero token-routed escapes.** The X-W0 seat
did not lose a row to the byte test that failed the other eleven folds.

**Receipt 2 — the census the token test cannot see (where the seven escapes are).** X-W0 is the
formation wave: its inbound rows are routed by *disposition class*, not by an `X-W0` token. Two
classes were swept fresh:

- **FORMATION-BOUNDARY.** The spine disposes 19 packets **15 HOMED · 4 FORMATION-BOUNDARY**, and the
  four — packets **7 · 12 · 15 · 16** — plus packet 5's split limb **H-c** all book at *the G-F
  register whose home X-W0's sitting elects* (spine §1, and packet 7 cites ⟨X-W0-FOLD.md · G-F⟩ by
  name). Under the ROW-CARRIAGE LAW appended this round — *"a packet's home wave adopts the packet's
  RIDING ROWS id-for-id, not merely its files"* — the G-F slate must carry each rider by
  ⟨record.md · id⟩. **Measured: zero of them were carried.** This is the same disease the check
  charged against the HOMED packets, at the boundary end; this seat is the one that owns it.
- **SITTING-ROUTED ESCALATIONS.** The fold layer routes cross-wave collisions to the W0 sitting by
  name. `grep -niE '(W0 sitting|X-W0 sitting|owner sitting|opening sitting|the sitting (decides|item|elects|rules)|sitting item)'`
  over the twelve folds + spine → **34 hits in 5 files**. Eight are escalations this fold did not
  carry (§6.5); the rest resolve to W0.1/W0.2/W0.6 or to a *wave's own* opening sitting (not X-W0's —
  declared at §6.7 n.5).

- **Receipt 3 — the register of record, re-measured today.** `NO-WAVE-OWNER` = **604 occurrences
  across 73 of the 92** records (`grep -ohE` → 604; `grep -l` → 73) — reproduces the check exactly.
  `COHESION.md §4` still holds **14** source rows + the "(accretes per batch)" tail, and **none of the
  five boundary entries below is among them** (§4's members are AboutPane-rider-A-1, ActionFeedback,
  AdminAuditPanel, AdminTagsPanel, ApiOfflineChip, BlobPane, BrowsePane, ErrorBoundary,
  MiniColorPicker, PaginationBar, PaletteCard, PaneHeader, PreviewRamp, PreviewStrip — record-level
  pointers, not packets). `ls docs/tranches/X/W0` → **No such file or directory** (candidate A absent).
  G-F's born-RED is therefore not merely stale — it is **0 of 37** by identity.

### 6.2 §A consumption — the one assigned row

⟨MigratePalettesDialog.md · A-31⟩ (record :71), packet 16, assigned FORMATION-BOUNDARY. Consumed at
**W0.21** below, inside its own record's identity-flow unit, exactly as the work order directs
("books at the G-F register with the identity-flow unit, never in a wave band"). §B assigned this
seat **zero** census rows; §C assigned **one** external row, consumed at W0.26. Nothing in the work
order is left unconsumed, and the six row-classes beyond it are marked ᶜ (census-found).

---

### 6.3 §Rows — the G-F REGISTER SLATE, booked id-for-id (W0.21 … W0.25)

Route legend unchanged: **X-W0.x** = the unit whose bytes receive it · **sitting** = X-W0.g's owner
packet · **slate** = the G-F register (home unelected — candidate A `W0/NO-WAVE-OWNER-REGISTER.md`,
candidate B `COHESION.md` §4 carve; W0.17 elects neither and neither does this addendum).
**Every disposition below is ADJUDICATED, never VERIFIED; no gate has run.**

**W0.21 — SLATE ENTRY 1 · MIGRATE-DIALOG IDENTITY-FLOW (packet 16) — twenty NO-WAVE-OWNER identities
ride one owner decision, and the record names them itself.** · **BLOCKER-bearing (2 BLOCKER
identities) · OWNER-DECISION** · cl.1+cl.2 · → **slate** (+ sitting: the fate question)
- **mech**: the record's own terminal block is a manifest, quoted whole: "**NO-WAVE-OWNER manifest
  (one identity-flow repair unit, for the next formation boundary)**: A-1 + A-3 (order law binding:
  A-1 before/with A-3's sink) + A-4/A-12's owner ruling + A-6, A-7-lifecycle, A-11, A-13, A-14, A-15,
  A-18-rehome, A-19, A-20, A-22..A-25, A-27, A-28, A-31. These cohere as ONE unit — the identity flow
  gets one home, one typed boundary, one error surface, one publish verb — and should be authored as
  a single wave rather than scattered."
- **the twenty riders, id-for-id** (⟨MigratePalettesDialog.md · id⟩ for every one; the range
  `A-22..A-25` is expanded so the byte census closes):
  | id | substance (record's words, compressed) | grade |
  |---|---|---|
  | A-1 | `setActiveTab("saved")` throws; ViewId widened to `string` and cast back — the admin branch (:55) is an unhandled rejection, switch-mode throws at :69 AFTER `userLogin` succeeded | BLOCKER |
  | A-3 | "the flow's error channel has three implementations and zero working ones" — PaletteSlugBar.vue (243 lines) rendered by no template; `slugBarRef` permanently null | BLOCKER |
  | A-4 | "the dialog's premise is false, its 'discard' arm is strictly worse than no dialog" — defect CONFIRMED BLOCKER · **cure OWNER-DECISION** | BLOCKER (cure severed) |
  | A-6 | Escape/scrim/× silently abandon a committed action; `pendingMigrateAction` left armed | — |
  | A-7 | "the surface is destroyed before the operation begins"; no loading/error/success state; every failure terminates at `console.warn` (lifecycle limb) | — |
  | A-11 | type jurisdiction twice over: Fraunces on help prose + all three control labels; description on the control rung where the matrix demands `text-prose` | — |
  | A-12 | "the action set has no owner and no Cancel"; initial focus lands on the irreversible command — **owner ruling, with A-4** | — |
  | A-13 | every consumer class dead against or fighting a deciding root; producer axes + `DialogFooter` unused | — |
  | A-14 | focus restoration cannot succeed by construction; `closeAutoFocus` unbound; the opener unmounts in the tick the dialog opens | — |
  | A-15 | the modal does not occlude — rendered contrast unboundable; `surface="opaque"` is one unused prop away | — |
  | A-18 | wrong home, dead seam: the dialog lives in `browser/` which never uses it; the "TOP-LEVEL SEAM" barrel has zero importers (rehome limb) | — |
  | A-19 | `onSlugSubmit` async with zero awaits — spinner unreachable, N concurrent logins possible | — |
  | A-20 | `transfer` unimplemented in regenerate mode; a template `v-if` is the only guard | — |
  | A-22 | dead `title` computed — both ternary arms byte-identical (:74-78) | — |
  | A-23 | `MigrateChoice` reachable through neither barrel; union hand-copied at :29 and :106 | — |
  | A-24 | three decorative lucide icons reach the a11y tree; the house pattern is explicit at 79 sites | — |
  | A-25 | `activeTab` dep dead and double-laundered (`as unknown as Ref<ViewId>` → `as Ref<string>`) | — |
  | A-27 | App.vue:170-175 + dialog/index.ts:2-5 assert a `./demo/**` sideEffects mark package.json:20 contradicts | INFO |
  | A-28 | no `count === 0` guard though exported through two public barrels | INFO |
  | **A-31** ᴬ | foundation.css:91's `@source "../../color-picker/**/*.{vue,ts,html}"` resolves OUTSIDE the tree; the header comment claims the globs make the scan "EXPLICIT" | INFO |
  ᴬ = the §A-assigned escapee. Ungraded cells are the record's own INFO/flow-repair band.
- **order lock (verbatim, spine §3 a.1 ≡ the record's own A-1 clause)**: "**A-1 must land BEFORE or
  WITH A-3's sink repair — mounting the sink first converts a silent bug into a user-visible lie on a
  successful login.**"
- **sequencing lock (verbatim, spine §3 a.2 · App-adj D-14)**: "the shell-root mount is removed only
  WITH the recovery article — removing it first deletes the only affordance; canon §7:224 names the
  unconditional dialog as two of the five forbidden recovery shapes at once. The gate
  (`MigratePalettesDialog` 0 mount sites) fires at that wave's CLOSE, not before."
- **the fate lock (verbatim, spine packet 16)**: "**THE DIALOG'S FATE is an owner decision no fold
  seat may take**" — so the slate books the unit and the **sitting** takes the decision; this seat
  elects nothing.
- **not double-booked**: the record's routed limbs are live in their waves and are NOT re-booked here
  — A-2/A-8/A-9/A-26 + the swallow census → X-W7; A-5 family + A-16/A-17 → X-W8; A-10/A-30 → X-W1;
  A-21 + width/inset → GLASS-RELAY; A-1's throw-limb rides X-W4 (W4·122) and its cast cure X-W5
  (BD-01). Those bookings stand; the slate carries the **remainder as one unit**, which is what
  "scattered" was the record's word against.
- **seq**: the owner decision precedes any wave opening `MigratePalettesDialog.vue` (it rides both
  the X-W4 and X-W7 bounds deltas "so whichever wave the owner elects can lawfully open it").
  ⟨MigratePalettesDialog.md · A-1/A-3/A-4/A-6/A-7/A-11/A-12/A-13/A-14/A-15/A-18/A-19/A-20/A-22/A-23/A-24/A-25/A-27/A-28/A-31 · §NO-WAVE-OWNER manifest — record; spine §1 packet 16, §3 a.1/a.2 — spine⟩

**W0.22 ᶜ — SLATE ENTRY 2 · BOUNDARY-SCOPE (packet 12) — one row-family, and a path contention that
must be resolved before three folds open the same file at two paths.** · **MAJOR · one row-family**
· cl.1+cl.2 · → **slate** (+ sitting: the path)
- **riders, id-for-id**: ⟨AboutPane.md · AB-4⟩ (:53) — "One failed doc-chunk fetch replaces the
  entire two-pane grid: `Markdown.vue:58-62` unguarded `await module()` from async `onMounted`;
  nearest boundary `App.vue:50…:140` wraps both panes; plus the stuck `isLoading`… **DISPOSITION:
  NO-WAVE-OWNER** — no X row owns pane-granular error boundaries or this load path; books at the
  formation boundary as one row with AB-5"; ⟨AboutPane.md · AB-5⟩ (:54) — "`useMarkdownColors.ts`
  throws ×3 from a render-bound computed — a SECOND independent path to the same whole-grid boundary…
  **DISPOSITION: NO-WAVE-OWNER, same row as AB-4.**" Live reachability stays NEEDS-LIVE (residue 3) —
  an SS-8 obligation, never a wave gate.
- **the contention (verbatim, spine packet 12)**: "`ErrorBoundary.vue` is named by NO dated X wave,
  yet THREE folds add it to their bounds deltas at TWO different paths (X-W5:
  `demo/shell/ErrorBoundary.vue`; X-W6/X-W7: `demo/color-picker/ErrorBoundary.vue`) — **the path must
  be resolved at the sitting before any of the three opens it**."
- **NAMESPACE GUARD (verbatim)**: "shell-dock-actionbarlayer's AB-4/AB-5 (X-W8-FOLD W8.31/G-8) are
  DIFFERENT identities; **cite ⟨record.md · id⟩ or not at all**." This addendum obeys it in both
  directions: nothing here touches the actionbarlayer pair.
- **cure shape**: pane-granular error-boundary architecture is owned by no X row — the slate books
  the family and names the two candidate owners the sitting may hand it to (X-W5's shell carve or the
  successor component COHESION §4 already tracks under `ErrorBoundary`), electing neither.
- **seq**: the path ruling precedes X-W5/X-W6/X-W7 opening.
  ⟨AboutPane.md · AB-4 · AB-5 — record; spine §1 packet 12 — spine; X-W5/X-W6/X-W7-FOLD BoundsDelta — fold layer⟩

**W0.23 ᶜ — SLATE ENTRY 3 · AUTH-SESSION SUBSTRATE (packet 15) — the store the "authoritative state"
is built on is itself the violating one.** · **MAJOR, contract-violating** · cl.2 · → **slate**
- **rider, id-for-id**: ⟨AdminFlaggedPanel.md · AF-50⟩ (:101, A-miss #8) — "The admin bearer lives in
  `localStorage` (useAdminAuth.ts:15/:21-23/:35-37 — my read) against PALETTE-CONTRACT §2's
  categorical ban ('no … localStorage … auth path in the browser' — the contract's head declares
  itself the LIVE authority; §1-§4 are W45-owned). The corpus filed it only as a parenthetical, and
  **every proposed gate cure (including X-W3's own X.W3.6, which reads 'the existing `useAdminAuth`
  state') is built ON TOP of the violating store.**" Record disposition, verbatim: "**NO-WAVE-OWNER**
  at the formation boundary — no X wave's bounds claim the auth substrate; recorded beside X-W3 so
  its guard landing is never read as discharging the contract row."
- **GATE-READING LOCK (verbatim, X-W3-FOLD F-4)**: X.W3.6 is built ON TOP of the localStorage bearer
  PALETTE-CONTRACT §2 bans, and **"G-18 green may never be read as discharging that row"** —
  `PALETTE-CONTRACT.md` is in W3's bounds and must carry it forward **undischarged**.
- **cure shape locked in-record**: "move the session port to a platform/shell-neutral home; the
  shell→palettes back edge dies with it (slugeditlayer's censused 5-forward/1-type-only-back)".
  **No wave adopts without an owner ruling** — the slate books it, the sitting rules it.
- **cross-check with W0.15**: this is the same lattice the typed graph must SEE (CPE L-24's
  palettes ↔ shell mutual dependency); G-E requires the graph name it, this row requires the register
  home it. Two gates, one substrate, no double-book.
  ⟨AdminFlaggedPanel.md · AF-50 — record; X-W3-FOLD.md F-4, B-5 — fold layer; spine §1 packet 15 — spine⟩

**W0.24 ᶜ — SLATE ENTRY 4 · PRE-X MT-REGISTER (packet 7) — a fifteen-record cohort whose MT-*→X
crosswalk no seat authored, holding four BLOCKER identities.** · **BLOCKER-bearing (4)** · cl.1+cl.3
· → **slate** (+ sitting) (+ SS-8 cross-edge)
- **mech (verbatim, spine packet 7)**: "The per-record MT-*→X crosswalk was authored by no fold seat
  and is not authored here… The REMAINDER — the crosswalk for the **15 zero-X-token records**,
  holding 4 BLOCKER identities (MT-F012 empty prod bundle · C-1 route-untrue boot · C-2/D-1 inert
  mobile action surface ≡ L-2, whose cure IS homed at X-W5 · D-4 zero H1 repo-wide) — **books at the
  W0 sitting + G-F register**."
- **the four BLOCKER identities, id-for-id** (all ⟨App.md · id⟩ — the record carries **zero**
  `NO-WAVE-OWNER` tokens, so these are boundary identities by disposition, not by mark; stated so the
  NWO arithmetic at §6.8 is not inflated):
  - ⟨App.md · L-1⟩ **CONFIRMED · BLOCKER** — "The spine. No main.ts exists; the entry is 8 inline HTML
    lines; App.vue carries five undeclared boot contracts… Build half re-run BY THIS SEAT: entry
    `index-Dezn_h7o.js` 698 B, `hasApp:false` — **MT-F012 reproduces byte-for-byte**."
  - ⟨App.md · C-1⟩ **CONFIRMED · BLOCKER** — "Route-untrue boot: index.html:205-213 mounts without
    `router.isReady()`… b3 never fires, terminal for the session. **10/14 routes affected.**"
  - ⟨App.md · C-2⟩ ≡ ⟨App.md · D-1⟩ **CONFIRMED · BLOCKER, "one defect with D-1, both ids
    preserved"** — the entire mobile action surface inert; D-1's aggravations (the
    `whenColorPickerReady` 40×50ms poll then give-up; Dock.vue:72 routing Save/Cancel to a null
    `colorPickerRef` at exactly that viewport) travel with it. **Both ids are booked; the cure limb is
    already homed at X-W5** (the nine-witness mobile-command identity, spine packet 9) — the slate
    carries the crosswalk row, not a second cure.
  - ⟨App.md · D-4⟩ **RESCOPED** — "H1 arm CONFIRMED and BUILT here (h1=0 repo-wide + live; §4.1/§5.1
    canon-hard; **must be VISIBLE per §5.1**). Skip-link clause STRUCK (zero canon obligation;
    landmarks satisfy bypass)." The strike travels with the row so no wave re-litigates it.
- **the cohort, re-measured by this seat (2026-08-28)**: records with **zero** `X[-·.]W\d` tokens =
  **16** — AdminPane · AdminUsersPanel · App · ColorInput · ColorPicker · ColorSpaceSelector ·
  ConfigSliderPane · Dock · GenerateControls · GradientStopEditor · Markdown · chassis-fitness ·
  layout-gestalt · library-band · parser-band · **shell-dock-slugeditlayer**. The spine says 15
  because slugeditlayer "folded on FILE ownership at X-W4 despite zero X tokens (§2 e.12)" and is
  subtracted. **16 − 1 = 15 reconciles exactly**; both figures are stated so neither is silently
  elected.
- **what is already subtracted (verbatim, spine)**: "the Dock/MT-DOCK-LAYERS-1 rows got a decline
  census at X-W8 (N-3) so none reads as landed; MT-AU1's core homed at X-W7 (packet 6);
  GradientStopEditor's W-GRADSTOP-* rows are provenance-mapped onto X.W6.a/.b/.c (X-W6-FOLD W6·396)."
  The slate books only the un-subtracted remainder.
- **cross-edge, and the lock on it**: this cohort is also §4's **ZERO-RESIDUE GAP** — 14 of these
  records carry **no** §UNPROVEN section, so "the pre-X cohort contributes NOTHING to SS-8's slate"
  and "**SS-8 may not be called complete until this cohort is swept for live-witness obligations**".
  X-W0 writes no SS-8 byte; the slate records the obligation so the gap has an owner.
- **correction carried (the check's own, adopted)**: §4's 14-name list and packet 7's 15-name cohort
  **differ by one member** — AdminPane is in the packet's cohort and carries UNPROVEN content
  (`grep -c UNPROVEN AdminPane.md` = 2), so it DOES feed SS-8. §4's enumeration governs for the
  residue question; the packet's governs for the crosswalk question. Both stated, neither merged.
  ⟨App.md · L-1 · C-1 · C-2 · D-1 · D-4 — record; spine §1 packet 7, §4 — spine; 16-record token sweep — measured 2026-08-28⟩

**W0.25 ᶜ — SLATE ENTRY 5 · W-HYGIENE H-c (packet 5's boundary limb) — a decline GOVERNS over eight
records that route the identity in, and the cure is SUPERSEDED to deletion.** · **MINOR, but nine
records deep** · cl.2 · → **slate**
- **mech (verbatim, spine packet 5)**: "**H-c** tsconfig↔exports parity (ColorSpaceSelector L-14 ≡
  Dock L-22): `W8.md:112`'s explicit decline **GOVERNS** (ruled over the 8 records that route it in;
  **silence-is-not-adoption**) → **FORMATION-BOUNDARY at the G-F register**". `W8.md:112` verbatim:
  "`tsconfig.demo.json` path/export parity — L-22 ≡ W-HYGIENE, another owner."
- **the nine riders, id-for-id** (one identity, nine independent derivations — this is the
  corroboration the spine calls "13+ with zero re-books"): ⟨ColorSpaceSelector.md · L-14⟩ ·
  ⟨Dock.md · L-22⟩ · ⟨EmptyState.md · ES-18⟩ · ⟨wb-extract-imageeyedropper.md · EY-43⟩ ·
  ⟨wb-mix-animationcanvas.md · MX-20⟩ · ⟨AuroraPane.md · AP-31⟩ · ⟨wb-generate-pane.md · GEN-23⟩ ·
  ⟨wb-extract-controls.md · EC-30⟩ · ⟨wb-extract-pane.md · XP-21⟩. Facts unanimous across all nine:
  8 hand-written `paths` entries; 3 phantom targets (bare `.` → `dist/index.d.ts`, `/parsing`,
  `/units` — none exists); 2 shipped-and-exported keys omitted (`/css`, `/value`); `#exports` = exactly
  7 keys with no `.` root; and the block's own comment claiming "a CLOSED 8-key set" **false by one**.
- **cure SUPERSESSION (verbatim, ⟨Dock.md · R-3⟩ — the deciding run travels with the row)**: "**O
  wins.** My own resolver run (paths absent): bare → UNRESOLVED; exported subpaths → local dist via
  self-reference. **Deletion makes the drift unrepresentable (L-8)**; generation would maintain a
  mirror nothing needs. Evidence handed to W-HYGIENE; **booking and id unchanged**." The earlier
  generate-parity shape booked at ⟨ColorSpaceSelector.md · L-14⟩ (:47) is **superseded, not deleted** —
  its born-RED generator-parity node command "rides as **falsifier only**".
- **lock**: "**all 8 entries proven inert**" and "no live break (both workers' vue-tsc exit 0)" — so
  this row may never be graded as a shipping break, and gate N-2 at X-W8 "forces a per-limb close
  declaration so 'W-HYGIENE landed' can never be reported one limb short". H-a and H-b are **already
  X-W8 property** (scope 2/3, gates G-2..G-6) and are NOT booked here.
- **correction to this fold's own §0 (dated, not silent)**: §0's dedupe register wrote this identity
  "NOT re-booked here; appears only as a member of the boundary register at W0.17". Under the
  ROW-CARRIAGE LAW that is now insufficient — membership without ids does not close a byte census. The
  identity is **booked at the slate id-for-id, still counted once**; W0.17's mention stands as the
  pointer it always was. No second booking exists.
  ⟨ColorSpaceSelector.md · L-14 · Dock.md · L-22 · R-3 · EmptyState.md · ES-18 · wb-extract-imageeyedropper.md · EY-43 · wb-mix-animationcanvas.md · MX-20 · AuroraPane.md · AP-31 · wb-generate-pane.md · GEN-23 · wb-extract-controls.md · EC-30 · wb-extract-pane.md · XP-21 — record; W8.md:112 — spec; spine §1 packet 5 — spine⟩

---

### 6.4 §C consumption — the EXTERNAL row (mail provenance, no registry id)

**W0.26 — ⟨INBOX I-28 · X-EXT-6⟩ — the repin's peer dependency is satisfied in `node_modules` and
declared nowhere, so the census's PASS branch would repin against an undeclared transitive.** ·
**MAJOR (adopt-blocking, measured)** · cl.3 · → **X-W0.j** (the repin act's own install step)
- **provenance**: **EXTERNAL, mail-sourced** — `docs/tranches/V/coordination/INBOX.md` row **I-28**
  (glass v8.0.0 @ `17a11bc5`; letter + §5 addendum 2026-08-25; ROWED 2026-08-28). **Not a registry
  row**: the id is `X-EXT-6`, cited ⟨INBOX I-28 · X-EXT-6⟩, and carries no ⟨record.md · id⟩ because
  no adjudication record contains it. The work order's words: "peer `vue-component-type-helpers
  ^3.0.3` required at adopt → **X-W0** (X-W0.j — the repin act's own install step; the census's PASS
  branch is unexecutable without it)."
- **mech (measured read-only 2026-08-28, this seat)**: `node -p
  "require('vue-component-type-helpers/package.json').version"` → **3.3.3** — the constraint `^3.0.3`
  is *satisfied today*, but by a **transitive** provider: a scan of `package.json`'s `dependencies`,
  `devDependencies` and `peerDependencies` for `vue-component-type-helpers` returns **zero hits**. The
  repo depends on a package it does not declare. `@mkbabb/glass-ui` is pinned `^7.0.0` in
  `dependencies` and installs at **7.0.0** (re-read from
  `node_modules/@mkbabb/glass-ui/package.json`) — consistent with W0.4's 0/4 and unchanged today.
- **cure shape**: the PASS branch's install step declares the peer explicitly in the same commit as
  the repin; the FAIL branch records the undeclared transitive as a standing hazard (it is one
  unrelated dependency bump away from vanishing). **X-W0 writes no `package.json` byte** — see the
  bounds statement at §6.6 n.8; this row makes the requirement visible at the census, which is where
  the trigger is evaluated.
- **why it binds this seat and not X-W4.g**: W0.md:101 binds X-W0 to `node_modules/@mkbabb/glass-ui/**`
  read/hash only. The census is the trigger; the install is the cut. Naming the peer at the trigger is
  what prevents a PASS verdict that cannot be executed — the same defect shape as W0.3, one dependency
  down. ⟨INBOX I-28 · X-EXT-6 — mail; installed tree + package.json — measured 2026-08-28⟩

---

### 6.5 §Rows continued — the sitting docket and the byte-visibility cure (W0.27 · W0.28)

**W0.27 ᶜ — The sitting's docket carries eight cross-wave escalations the fold layer routes to it by
name, and W0.md names none of them.** · **MAJOR (gate arithmetic, second order)** · cl.1 ·
→ **sitting** (G-B's roster arithmetic extended)
- **mech**: W0.5/G-B established the roster reads **nine** owner rows, not seven. The fresh sweep
  finds a second, disjoint class: **escalations** — cross-wave collisions the folds explicitly refuse
  to elect and route to this sitting. Eight, each with its far-end byte:
  | # | escalation | routed by, verbatim | far end |
  |---|---|---|---|
  | S-1 | **`bindPane` ownership** — W5.md §5 gives `usePaneRouter.bindPane` "the instance refs it dispatches onto"; ⟨Dock.md · R-8⟩ narrows it and moves command dispatch to a `DockCommand` registry at MT-DOCK-LAYERS-1. "**Both cannot be true.**" | X-W5-FOLD CE-1: "**ESCALATION REQUIRED BEFORE W5 OPENS**" | X-W5 (W5F-01/02/11/64; gates A3, A4) |
  | S-2 | **GAB-5/K-8 per-verb seat** under the VC:194 + B-13 + R-B collision, with "the standing cl.2 prohibition on executing challenge-D's Generate seat table while R-B stands" | spine packet 9: "TWO escalations must clear at the W0 sitting BEFORE the owning waves open" | X-W10 (owner/canon item) |
  | S-3 | **`src/color/model.ts` barred TWICE** by two different rules (Do-NOT-touch the file; Triumvirate the mathematics) while four W9 rows require reading/re-exporting `SPACE_SCHEMA`/`SPACE_IDS`/`Color<S>` | spine §2 e.20: "ESCALATED, not assumed … **an adjudication for the W0 sitting, not a bounds edit**" | X-W9 (§1a n.6, §14 delta 6) |
  | S-4 | **`UserSortMenu.vue:8` cure COLLISION** — X-W7 B-4's null-DELTA vs X-W8 G-9's quiet migration | spine packet 14: "a live CrossEdge both folds record and neither elects — **sitting item**" | X-W7 ⟂ X-W8 |
  | S-5 | **`PaletteCard.vue` verb** — `W7.md` §5.d says "deletes", §4 grants modify-carve; ActionFeedback.md authored every row against the §5 reading | spine §2 e.18: "**the sitting elects the verb before unit .d opens**" | X-W7 |
  | S-6 | **AdminGate deletion seam** CONTESTED between X-W3 and X-W7 | spine packet 6: "exactly as X-W3-FOLD F-1 prints it — **neither wave's gate goes green over the other's edit**" | X-W3 ⟂ X-W7 |
  | S-7 | **`ErrorBoundary.vue` path** — three folds, two paths (W0.22) | spine packet 12: "**the path must be resolved at the sitting before any of the three opens it**" | X-W5 ⟂ X-W6 ⟂ X-W7 |
  | S-8 | **MT-CSP-1 ⇄ the X carry-cut ledger** — ⟨ConfigSliderPane⟩ "is in no wave's table and the CC id is absent"; COHESION §4's BlobPane row: "MT-CSP-1 … **ORPHANED from the X carry-cut ledger — 10 identity-folds hang on it**" | X-W6-FOLD §2 e.12: "both belong at the X-W0 sitting (§0a n.9)" | X-W6 · X-W8 · W0.1 |
- **S-8's rider, carried verbatim so W0.1 is not read as covering it**: X-W8-FOLD:432 quotes the
  reconciliation clause and then adds ⟨BlobPane⟩'s own words — "**the bank must not resurrect it.**"
  W0.1 books the CC-105 ⇄ G-PAINT *tension*; S-8 books the **missing carry-cut id** the tension's
  subject hangs on. Two rows, one file, no double-book.
- **cure shape**: the sitting's docket is **nine owner rows + eight escalations + one OWNER-DECISION**
  (W0.21's dialog fate) = **eighteen items**, and each escalation's verdict names the far-end byte it
  unblocks. A sitting that answers nine and adjourns leaves five waves unable to open lawfully.
- **dissent**: none of the eight is elected here — every one is recorded exactly as its fold left it,
  written either way. This addendum adds arithmetic, not verdicts.
- **explicitly NOT adopted (declared, §6.7 n.5)**: spine §2 e.22's two bounds contests
  (`demo/palettes/mix.ts` and `foundation.css`'s WHCM/print roster arm) route to the **X-W6 opening
  sitting**, not X-W0's. Named so a later pass does not read the omission as a miss.
  ⟨X-W5-FOLD CE-1 · X-W6-FOLD §2 e.12 · X-W8-FOLD:432 · spine §1 packets 6/9/12/14, §2 e.18/e.20 — fold layer + spine⟩

**W0.28 — Twenty-three ids in this fold's own §0 and §1 blocks are bold-split and therefore invisible
to the byte census; seven of them are invisible across the entire fold layer.** · **MAJOR
(self-defect, census-fatal) · cl.1** · → **this file (cured below)**
- **mech (measured, this seat, 2026-08-28)**: the round-0 idiom — a prefix, a hyphen, then the
  numeral wrapped in emphasis markers (`PFX-` + `\*\*` + `N` + `\*\*`) — writes an id whose **literal
  bytes are not the id**. The check's census law
  boundary-matches ids as literal bytes; a bold-split id matches nothing. `grep -onE
  '[A-Z][A-Za-z0-9]*-\*\*[0-9]+\*\*'` over this file → **28 occurrences / 23 distinct ids**:
  AP-17, AP-31, EC-2, EC-3, EC-15, EC-22, EC-30, ES-18, EY-7, EY-27, EY-32, EY-43, GEN-2, GEN-8,
  GEN-23, GV-4, MX-1, MX-3, MX-20, MX-25, MX-32, MX-35, MX-41.
- **the seven true escapes** (boundary-match over all twelve folds + the spine → **0 files**):
  **⟨wb-extract-controls.md · EC-2⟩ · ⟨wb-extract-controls.md · EC-3⟩ ·
  ⟨wb-extract-imageeyedropper.md · EY-7⟩ · ⟨wb-gradient-visualizer.md · GV-4⟩ ·
  ⟨wb-mix-animationcanvas.md · MX-1⟩ · ⟨wb-mix-animationcanvas.md · MX-3⟩ ·
  ⟨wb-mix-animationcanvas.md · MX-41⟩.** ⟨wb-mix-animationcanvas.md · MX-1⟩ is a **BLOCKER**;
  ⟨wb-gradient-visualizer.md · GV-4⟩ is one of only two corpus rows that route to X-W0 by token
  (W0.16) — this fold cited it and did not carry its bytes. The check's 189/274 figure did not name
  these because they are escapes this fold *manufactured*, not escapes it inherited.
- **cure (an ≡-pointer restoration, never a re-booking)**: the sixteen non-escaping ids are restored
  in plain bytes here and remain counted once at their existing rows; the seven escapes are restored
  in plain bytes AND pointed at the row that already adjudicates them. **No id below opens a new
  booking.**
  | id, plain bytes | ≡ pointer — the row that already holds it | class |
  |---|---|---|
  | ⟨wb-extract-controls.md · EC-2⟩, ⟨wb-extract-controls.md · EC-3⟩, ⟨wb-extract-controls.md · EC-22⟩ | W0.4 stranded set + §0 CC-105 dedupe (`ExtractControls.vue:32,75`) | ESCAPE (EC-2, EC-3) / restored (EC-22) |
  | ⟨wb-generate-pane.md · GEN-8⟩ | W0.4 stranded set + §0 CC-105 dedupe (`GenerateControls.vue:305`) | restored |
  | ⟨AuroraPane.md · AP-17⟩ | W0.1 (the ⇄CSP tension) — also plain at :96 | restored |
  | ⟨wb-extract-imageeyedropper.md · EY-7⟩, ⟨wb-extract-imageeyedropper.md · EY-32⟩, ⟨wb-extract-imageeyedropper.md · EY-27⟩ | W0.4 stranded set; EY-32 also W0.7(c)/G-C; EY-27 also §0 glass-7 `tag=` dedupe | ESCAPE (EY-7) / restored |
  | ⟨CurrentPaletteEditor.md · A-1⟩ (as `CPE A-1`), ⟨wb-generate-pane.md · GEN-2⟩ | W0.2, W0.4 (BLOCKERs) — both also plain elsewhere | restored |
  | ⟨wb-mix-animationcanvas.md · MX-1⟩, ⟨wb-mix-animationcanvas.md · MX-3⟩, ⟨wb-mix-animationcanvas.md · MX-32⟩, ⟨wb-mix-animationcanvas.md · MX-41⟩ | W0.4 stranded set (MX-1 = BLOCKER limb) | ESCAPE (MX-1, MX-3, MX-41) / restored (MX-32) |
  | ⟨wb-extract-controls.md · EC-15⟩, ⟨wb-gradient-visualizer.md · GV-4⟩, ⟨wb-mix-animationcanvas.md · MX-25⟩, ⟨wb-mix-animationcanvas.md · MX-35⟩ | W0.16 (the dead-lint-lattice identity ≡ ColorSpaceSelector L-5(a)) | ESCAPE (GV-4) / restored |
  | ⟨EmptyState.md · ES-18⟩, ⟨wb-extract-imageeyedropper.md · EY-43⟩, ⟨wb-mix-animationcanvas.md · MX-20⟩, ⟨AuroraPane.md · AP-31⟩, ⟨wb-generate-pane.md · GEN-23⟩, ⟨wb-extract-controls.md · EC-30⟩ | W0.25 (H-c, booked id-for-id this round) | restored |
- **lock, adopted from the check's own language and binding on this seat**: an id is carried when its
  **literal bytes** appear; emphasis markup inside an id is a rename, and **ids are original for
  life**. No id in §6 is bold-split.
  ⟨this file's §0/§1 bytes — measured 2026-08-28; PASS-1-CHECK-RETURN.json §check.defects[0] method — check⟩

---

### 6.6 §Gates — three NEW, born-RED with real witnesses (G-I · G-J · G-K)

L-19 holds: no proof scripts; every falsifier is a probe over shipped bytes, a spec byte, or a
set-difference over existing records.

**G-I · NEW — the G-F register slate is ROW-KEYED, not cluster-keyed.**
Every boundary-booked identity appears in the elected register by ⟨record.md · id⟩; the register's id
set minus this addendum's §6.3 roster is ∅ in both directions. **BORN-RED (measured 2026-08-28)**:
`ls docs/tranches/X/W0` → **No such file or directory** (candidate A absent); `COHESION.md` §4
(candidate B) holds **14** record-level pointer rows and names **none** of the five boundary packets;
so **0 of the 37 identities** booked at W0.21–W0.25 (20 MigratePalettesDialog + 2 AboutPane + 1
AdminFlaggedPanel + 9 W-HYGIENE + 5 App) live in any register. *Falsifier*: drop one id — say
⟨MigratePalettesDialog · A-31⟩, the row this round's work order assigned by name — and the
set-difference against §6.3 names it; carry the five packets as five cluster labels with no ids and
the difference is 37, which is exactly the failure the ROW-CARRIAGE LAW was appended to cure.
*Relation to G-F*: G-F gates that the register **exists and closes by cluster**; G-I gates that its
rows are **identities, not labels**. G-F may go green over a five-line file; G-I cannot.

**G-J · NEW — the sitting's docket closes over every escalation the fold layer routes to it by name.**
The sitting's agenda = 9 owner rows (G-B) + 8 escalations (W0.27 S-1..S-8) + 1 OWNER-DECISION
(W0.21) = **18 items**, each verdict naming the far-end byte it unblocks. **BORN-RED (measured
2026-08-28)**: `W0.md` names **seven** rows at :188/:301 and **zero** of the eight escalations —
`grep -c` on `W0.md` for `bindPane` → 0 · `GAB-5` → 0 · `model.ts` → 0 · `UserSortMenu` → 0 ·
`PaletteCard` → 0 · `AdminGate` → 0 · `ErrorBoundary` → 0 · `MT-CSP-1` → 0; while the fold layer
routes all eight to this sitting in its own words (34 sitting-hits across 5 files). *Falsifier*: hold
the sitting on seven or nine items and X-W5 cannot lawfully open (CE-1: "ESCALATION REQUIRED BEFORE
W5 OPENS"), X-W7's unit .d has no verb (e.18), X-W9's four `SPACE_SCHEMA` rows stay barred (e.20),
and three waves open `ErrorBoundary.vue` at two paths — all while HG-13 reads green. *Why it is not
G-B*: G-B fixes the **roster** (which owner rows); G-J fixes the **docket** (which cross-wave
collisions). Seven → nine cures the first and leaves the second at zero.

**G-K · NEW — the repin's peer dependency is DECLARED, not merely resolvable.**
`vue-component-type-helpers` appears in `package.json` with a constraint satisfying I-28's `^3.0.3`
before any 8.0.0 repin is executed. **BORN-RED (measured 2026-08-28)**: the package resolves at
**3.3.3** in `node_modules/` — constraint satisfied — while `package.json`'s `dependencies`,
`devDependencies` and `peerDependencies` contain **zero** occurrences of the name; `@mkbabb/glass-ui`
is `^7.0.0` and installs **7.0.0**, so the four HG-18 conditions remain **0/4** and the PASS branch
has never been rehearsed against this requirement. *Falsifier*: the peer's transitive provider is one
unrelated dependency bump from disappearing — remove it and the 8.0.0 type surface fails to resolve
while `package.json` still claims nothing, which is a green census followed by an unexecutable cut.
*Bounds note*: X-W0 writes no `package.json` byte (§6.7 n.8) — this gate is a **measurement at the
trigger**, not a cut.

---

### 6.7 §BoundsDelta — entries 8–10 (dated addendum to §3)

| # | path | today | delta | source |
|---|---|---|---|---|
| 8 | `package.json` (`dependencies` block, one line) | in **no** X-W0 bounds; `vue-component-type-helpers` undeclared while resolving at 3.3.3 | **NOT ADDED at X-W0** — stated, never silent. The declaration byte belongs to the repin's cut (X-W4.g on the census PASS branch), not to the trigger. X-W0.j records the requirement and measures it. | W0.26 · G-K |
| 9 | `docs/tranches/X/W0/NO-WAVE-OWNER-REGISTER.md` (candidate A) **or** `COHESION.md` §4 carve (candidate B) | bounds row #2 already proposes one of the two — **still unelected** | **content delta**: the five boundary entries of §6.3 with their **37 member ids**, each as ⟨record.md · id⟩ with a terminal-home column per COHESION §3.3 | W0.21–W0.25 · G-I |
| 10 | `docs/tranches/X/W0/OWNER-SITTING-2026-08-XX.md` | in bounds (`create`) — bounds row #6 already carries its content delta | **content delta, extended**: docket **7 → 18** (9 owner rows + 8 escalations + 1 OWNER-DECISION), each verdict naming the far-end byte it unblocks | W0.27 · G-J |

**Not added, stated once (extending §3's list):** `demo/palettes/mix.ts` and `demo/styles/foundation.css`
(the WHCM/print roster arm) — spine §2 e.22's two genuine bounds contests route to the **X-W6 opening
sitting**, not X-W0's; recorded here so the non-adoption is legible, not an omission. `ErrorBoundary.vue`
at either candidate path — the **path ruling** is X-W0 sitting business (W0.22 · S-7); the **file** is
X-W5/X-W6/X-W7 property and X-W0 adds neither path to its own bounds.

### 6.8 §Arithmetic — the OUT side, re-counted from this file's own bytes (2026-08-28, fold round 1)

**Rows 20 → 28.** W0.1..W0.20 (round 0, unchanged) + **8 added**: W0.21 · W0.22 · W0.23 · W0.24 ·
W0.25 (the five G-F slate entries) · W0.26 (the EXTERNAL row) · W0.27 (the sitting docket) · W0.28
(the byte-visibility cure). Numbering stays **gapless and contiguous, W0.1–W0.28**; every row carries
exactly one `⟨…⟩` provenance line.

**Gates 8 → 11.** G-A..G-H (round 0: 7 SHARPEN + 1 NEW) + **G-I · G-J · G-K, all NEW, all born-RED
with measured witnesses**. Sharpen/new split is now **7 SHARPEN · 4 NEW**.

**BoundsDelta 7 → 10** (entries 8–10 above; entry 8 is a stated **non**-addition, counted because it
is a bounds decision this fold makes and must be checkable).

**CrossEdges 19 → 25.** Added, each naming its far-end byte: **20.** slate → **X-W5** (CE-1's
`bindPane` escalation clears before W5 opens) · **21.** slate → **X-W9** (e.20's `src/color/model.ts`
double-bar adjudication; four W9 rows wait on it) · **22.** slate → **X-W7** (e.18's delete-vs-carve
verb before unit .d opens; and the `UserSortMenu.vue:8` collision with X-W8 G-9) · **23.** slate →
**X-W3 ⟂ X-W7** (the AdminGate deletion seam; neither gate greens over the other's edit) ·
**24.** slate → **X-W6/X-W8** (MT-CSP-1's orphaned carry-cut id — 10 identity-folds hang on it) ·
**25.** X-W0.j → **X-W4.g** (X-EXT-6's undeclared peer rides the repin's install step).

**Declared silences 4 → 5** (added: spine §2 e.22's two bounds contests, routed to the X-W6 opening
sitting — §6.7).

**Identities newly carried id-for-id: 37**, decomposing exactly — **32 NO-WAVE-OWNER-marked**
(20 MigratePalettesDialog + 2 AboutPane + 1 AdminFlaggedPanel + 9 W-HYGIENE H-c) + **5
boundary-by-disposition** (⟨App.md · L-1/C-1/C-2/D-1/D-4⟩ — `grep -c NO-WAVE-OWNER App.md` → **0**,
so they are booked by the spine's packet-7 disposition, not by an in-record mark; stated so the NWO
figure is not inflated by five). **Plus 1 EXTERNAL** (⟨INBOX I-28 · X-EXT-6⟩ — mail provenance, no
registry id). **Plus 23 ids restored to plain bytes** at W0.28, of which **7 were fold-layer-wide
escapes**; those 23 open **no** new booking and are counted in **no** total above.

**The IN side remains a receipt, not arithmetic** (round 0's rule, kept): this round read the work
order, the pass-1 register, the spine, eleven sibling folds (grep-scoped), and 10 corpus records
whole or in part. **No in-equals-out identity is asserted.**

**Work-order reconciliation (§D of ROUND-1-ASSIGNMENTS, this seat's line):** assigned **§A 1 · §B 0 ·
§C 1 = 2**; delivered **2 assigned + 6 census-found row-classes (ᶜ) = 8 rows / 37 identities / 1
external**. The roster was a floor; the census-found excess is itemised at §6.1 receipt 2 and marked
ᶜ on every row that carries it, so a round-2 check re-derives the delta instead of re-discovering it.

— end of round-1 repair addendum. Status **planned**. `docs/tranches/X/waves/**`,
`CONFORMANCE-2026-08-03.md` and `registry/adjudicated/**` remain byte-untouched (E-1/E-3); this file
is still the fold's sole write; no product source was opened.
