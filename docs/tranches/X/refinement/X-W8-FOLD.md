SERVED MODEL ID (fold seat, line-1 declaration): claude-opus-5[1m]

# X-W8 REFINEMENT FOLD — the dated addendum to `waves/W8.md`

## 0. Header — provenance, posture, reading rule

**Provenance.** Input set = the value.js corpus at
`docs/tranches/V/megatranche/registry/adjudicated/` (all records that are NOT `kf-*`/`fr-*`/`pt-*`):
88 component apotheoses + `parser-band.md` · `library-band.md` · `chassis-fitness.md` ·
`layout-gestalt.md`. Method: (a) full read of every record for the 61 slugs this seat was assigned;
(b) a corpus-wide sweep for `X-W8`/`X.W8` — **455 routed line-instances across 64 records** (census
command in §5, id-keyed); (c) consumption of the two proposed packets (§1, §2). Dedupe is **by
banked identity**, never by slug: a fact confirmed at seven records is ONE row here.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0.md..W11.md` and
`CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is this seat's sole write. Where a
clause below strikes or corrects a phrase in W8.md, the strike is recorded here and W8.md's bytes
stand untouched.

**Reading rule.** At execution X-W8 reads **`waves/W8.md` PLUS this addendum**, in that order;
where they disagree the addendum's clause governs. X.W8.a reads §3 (BoundsDelta) as a **mandatory
input to the C-16 census** — W8.md:38 already says the bounds "bind to that census, not to this
file's numbers", so §3 is not a bounds patch, it is census evidence the census must not omit.
Nothing here re-stages or re-weights the wave's gate composite (OC-2/CC-096 staging stands).

**Fold classes.** **cl.1** spec sharpening / law / correction · **cl.2** cure-lock or binding
sequencing clause · **cl.3** BoundsDelta row (§3) · **cl.4** DECLINED-BY-W8 / cross-edge, does not
land here (§4 · §6).

**Status.** `planned`. **NOTHING in this file opens product source.** Every disposition is
ADJUDICATED, not VERIFIED; no gate is stamped.

**Arithmetic (this document's own bytes, OUT side only).** **51 folded rows** (§1 ×7 · §2 ×6 ·
§3-bearing rows counted in their clusters · §Rows W8.1–W8.51) + **21 gate entries** (§Gates: 15
sharpenings of existing G-1..G-15 + 6 genuinely new) + **31 BoundsDelta files** (§3) +
**11 cross-edges** (§4). The IN side (455 routed line-instances / 64 records / 46 records naming
W-HYGIENE) is a **census receipt**, falsifiable against the corpus by §5's commands, never asserted
as an in-equals-out identity.

> **DATED CORRECTION — 2026-08-28, fold round 1 repair (§9).** The paragraph above is the **pass-1**
> reading and stands as written (E-3 idiom: correct by addendum, never by silent overwrite). The
> **true current counts** are **69 folded rows (W8.1–W8.69) · 24 gate entries (15 sharpenings + 9
> new) · 31 BoundsDelta files · 11 cross-edges**, decomposed in **§9.7**. The IN side gains a second,
> row-keyed receipt beside the line-instance one: **271 X-W8-routed row lines across 56 records**
> (§9.1) — a different unit, not a contradiction; the 455/64 figure reproduces exactly.

**Anti-rename (M-25).** Every id below is the id its banked record minted, for life. Where a row
carries N ids they are the N witnesses of ONE identity, listed, never re-minted. Where a record
routed a row to X-W8 and W8.md declines it, the row is folded as **cl.4** with the decline quoted —
**silence is not adoption, and a decline is not a kill**.

---

## 1. PACKET 1 — W-HYGIENE: a phantom owner, three limbs, two of which X-W8 already owns

**The finding.** `W-HYGIENE` is the single most-cited banked identity in the value.js corpus —
**163 mentions across 46 records** — and **it was never cut into X**. It is named as the owner by
`AboutPane` rider A-1 ("phantom owners — adjudicated identities folded to waves that were never
cut into X"), and W8.md:112 names it while pushing one limb away. But "W-HYGIENE" is not one row:
it is **three** banked identities that accreted under one label, and X-W8 already owns two of them
outright. **The fold's first act is to split the name.**

| limb | banked identity | X-W8 status | this fold |
|---|---|---|---|
| **H-a** demo/ui barrel extirpation | **ColorPicker-adj L-6** (90 sites) ≡ ColorSpaceSelector L-4 ≡ MT-UI1 | **OWNED** — W8.md:40-41 scope 2, bounds `demo/ui/** (19 index.ts) delete`, gates G-2/G-3 | **FOLDS (cl.1)** — W8.1..W8.4 |
| **H-b** dead eslint-glob liveness | **ColorSpaceSelector-adj L-5(a)** (born-RED `eslint --print-config`) ≡ Dock DELTA-3 | **OWNED** — W8.md:42-43 scope 3, `eslint.config.js` modify-carve, gates G-4/G-6 | **FOLDS (cl.1)** — W8.5, W8.6 |
| **H-c** `tsconfig.demo.json`↔`package.json#exports` parity | **ColorSpaceSelector-adj L-14** ≡ Dock **L-22** | **DECLINED** — W8.md:112 verbatim: "`tsconfig.demo.json` path/export parity — L-22 ≡ W-HYGIENE, another owner." | **cl.4 — does NOT land** — W8.7 |

**Why the split is load-bearing.** Fifteen records route "W-HYGIENE" to X-W8 *as a whole* (`AboutPane`
AB-23/AB-24, `PalettesPane` PP-18/PP-19, `CurrentPaletteEditor` L-4/L-6/L-11, `SearchFilterBar`
SFB-35/36/37, `SwatchHoverMenu` SH-9/SH-21, `shell-paneslot` L-9, `picker-heroblob` row 14,
`wb-gradient-codeeditor` A-23/A-26, …). Under W8.md as written, two of those three limbs execute and
the third silently does not — and because they arrive under one name, a close that reports
"W-HYGIENE landed" would be **false by one limb**. Nine records already caught this independently and
booked H-c **NO-WAVE-OWNER with the identity preserved**: `PaletteColorStrip` PCS-30, `PreviewStrip`
PS-25, `shell-dock-dockstatuslamp` L-13, `wb-extract-pane` XP-21, `wb-extract-workbench` XW-29,
`wb-extract-imageeyedropper` EY-43, `wb-extract-controls` EC-30, `wb-mix-animationcanvas` MX-20,
`wb-mix-sourceselector` MSS-27, `wb-gradient-easingspecimenstrip` L-11, `wb-mix-resultdisplay` MR-29.

---

**W8.1 · H-a — the 19 barrels: the census figure is CONVERGENT and the wave's own number is the
one to keep** (cl.1)
`MigratePalettesDialog` **A-16** killed its own axis's "20-directory shim" and recorded the
convergence explicitly: "count = 19 (killed 20, below) — **and X-W8's own census independently says
19, a convergence receipt**." Third- and fourth-witnessed at `TagEditPopover` **TEP-9** (K-10: "17 of
19 modules are one-line root-barrel renames (alert & input are the counterexamples)"),
`shell-dock-profilesection` **L-3** (K-9: "18 of 19 reach the root barrel — `input` is the
counter-example (`@mkbabb/glass-ui/forms`), so the producer-subpath ask is FIVE (avatar, alert,
checkbox, radio-group, skeleton), not six"), `wb-extract-pane` **XP-11** ("19 dirs, 18 one-line
barrels + alert at 11 (29 lines total)").
*Sharpens*: **G-1** (census) and **X.W8.b**'s family table (W8.md:159-163). The wave's own
`badge button card collapsible dialog dropdown-menu label popover select separator slider switch
tooltip` = 13 subpath families + `Input`→`/forms` + 5 root-barrel-only is **CORROBORATED at four
independent seats**; the "5 root-barrel-only" set is simultaneously the **GLASS-RELAY ask**
(TEP-9: "the **5** missing subpaths (`./alert ./avatar ./checkbox ./radio-group ./skeleton`) →
GLASS-OWNED relay via the standing BH/BI fond") — the relay is not a precondition of the rewrite.
⟨MigratePalettesDialog A-16 · TagEditPopover TEP-9 · shell-dock-profilesection L-3 · wb-extract-pane XP-11 · ColorNutritionLabel CNL-15 · EmptyState ES-17 · ErrorBoundary EB-17 · PaletteCard PC-41 · picker-componentsliders R-17 · picker-spectrumcanvas L-8 · shell-dock-genericactionbar GAB-9 · wb-gradient-codeeditor A-23 · wb-generate-pane GEN-21 · MiniColorPicker MCP-38 · VersionHistoryDrawer VHD-26 · PaletteSlugBar A-21 · AdminTagsPanel ATP-22 · AdminAuditPanel AAP-28 · AdminNamesPanel L-5 · shell-dock-mobilemenudropdown L-2 · ShadowPalette SP-33 · PaneHeader PH-31/PH-35 · AboutPane AB-23 · CurrentPaletteEditor L-4 · SwatchHoverMenu SH-9 · picker-heroblob (repo identity) · wb-mix-resultdisplay MR-33 · wb-mix-sourceselector MSS-26⟩

**W8.2 · H-a — the SITE denominator is 90/48, not 92/48, and BOTH are in the record** (cl.1 —
**dated correction, this fold**)
W8.md:40/240 books **92 statements / 48 files**; `Markdown`-adj ruled **90 / 48** by three
independent measurements ("this seat ran F's own regex: 90, not 71 … O's multiline matcher: 90/48 —
three independent measurements now agree: this seat, worker-O, and the ColorSpaceSelector seat");
`ColorSpaceSelector` L-4 books 90; `picker-componentsliders` R-R records the live drift
("L printed 92/37/68; RR-B measured 90/37/82"); `AdminUsersPanel` measured **48 barrel-importer
files** and **24 both-path files** with the ledger's 35 REFUTED and F's 49 "off by one".
**This is not a defect of W8.md** — its own G-1 command is the arbiter, and W8.md:38 already binds
the bounds to the census output, not to the pasted figure. What this row adds is the **mandatory
denominator law**: X.W8.a must record `90` and `92` side by side as the two prior readings and mark
the loser **SUPERSEDED-BY-MEASUREMENT**, exactly as it already does for `90/48` and `107/49`.
*Sharpens*: **G-1** (add a third and fourth UNREPRODUCED/superseded row) and **G-3**'s "92
statements across 48 files" RED cell.
⟨Markdown-adj L-5 (ruled 90/48) · ColorSpaceSelector L-4 (90/48) · AdminUsersPanel §2 (48 files · 24 both-path · ledger 35 REFUTED) · picker-componentsliders RR-B (90/37/82)⟩

**W8.3 · H-a — two barrels are outright DEAD and three re-exported symbols have zero consumers:
delete-not-repoint, and the census must say which** (cl.1)
`ErrorBoundary` **EB-17** (own census): `demo/ui/label` and `demo/ui/switch` importers = **0**
repo-wide — corroborated at `MigratePalettesDialog` A-16 ("reader-2 adjacent finds folded:
`demo/ui/label` and `demo/ui/switch` have zero consumers"). `shell-dock-mobilemenudropdown` **M-H**:
inside the dropdown shim, `DropdownMenuGroup`/`CheckboxItem`/`Shortcut` are re-exported with zero
consumers. `PaneHeader` **PH-31**: `demo/ui/card/index.ts` re-exports **6 of the producer's 12** card
exports and **0 of 5** types — "the barrel tier is the concrete mechanism by which `CardHeader shrink`
and `CardTitle as` went unnoticed". `AdminFlaggedPanel` **AF-54**: `badgeVariants` re-exported with
zero consumers.
**Cure shape (lock)**: for a barrel with zero importers the act is DELETE with **no** repoint —
G-2's clause-1 count already covers it; G-3's per-consumer narrowness clause must not manufacture an
import for a symbol nobody reads.
⟨ErrorBoundary EB-17 (+reader-LC missed M2) · MigratePalettesDialog A-16 · shell-dock-mobilemenudropdown M-H · PaneHeader PH-31 · AdminFlaggedPanel AF-54⟩

**W8.4 · H-a — the barrels are the only surviving record of WHICH glass subpath a primitive lives
on; the migration is already 8/13 done in places** (cl.2 — cure rider)
`PaletteCardSkeleton` **PCS-21**: "the input-subpath nuance (the barrels are the only record of which
glass subpath a primitive lives on) travels with that identity". `wb-gradient-visualizer` **GV-17**
(sharpened at PIN 7): "three import registers across 13 statements in this one feature, **8 already
correct** — the cure is *finishing* a migration, not starting one".
**Lock**: the deletion commit must carry the family→subpath table (W8.md:159-163's 13+1+5) as
committed output, or the deletion destroys the map. `ShadowPalette` **SP-33** binds the other
direction: "no cure adds a twentieth consumer" — every wave that touches a file in the 48 imports
`@mkbabb/glass-ui/<subpath>` directly, never a new local barrel.
⟨PaletteCardSkeleton PCS-21 · wb-gradient-visualizer GV-17 (PIN 7) · ShadowPalette SP-33⟩

**W8.5 · H-b — the dead-glob row is SIX globs across THREE objects, and "every demo gate matches
zero files" is FALSE by one object** (cl.1 — **correction that travels with the identity**)
Identity: **ColorSpaceSelector L-5(a)**, born-RED command banked there
(`npx eslint --print-config demo/color-session/useColorPipeline.ts | node -e "…"` → exits 1).
`PaneHeader` **PH-10** ruling (reader-1 SUSTAINED, own grep): three `no-restricted-imports` objects —
`:206` (src, LIVE, load-bearing `inv-K-1`), `:241` (2 LIVE `demo/color-picker/**` globs + 4 dead
`demo/@` globs), `:279` (fully dead composables object) — "six dead globs total — matching the
ColorSpaceSelector arbiter's independent census AND X-W8:42's 'six dead `demo/@/**` file globs'".
The narrowing kill is sustained at **three** further seats and is now law: `PreviewStrip` K-11,
`AdminFlaggedPanel` AF-36 ("an active rule with a dead pattern, not an unmatched glob"),
`wb-mix-pane` **K-E1-a** ("the 'match ZERO files' headline — eslint.config.js:232-239 LEADS with two
live `demo/color-picker/**` globs; already killed thrice on the fleet record").
**Second dead axis, same object**: the ban *pattern* is spelled in the `@components/…` alias retired
at W43/RF-15 — **0 live importers repo-wide** — so even the live glob bans a specifier no file can
write (`PaletteSlugBar` A-6, `ShadowPalette` SP-11, `MigratePalettesDialog` A-17, `PaletteCardGrid`
PG-10, `wb-mix-animationcanvas` MX-35, `wb-gradient-easingauthoringstage` EAS-22).
**Live counter-witness, uncaught today** (`MigratePalettesDialog` A-17, this seat's cross-check):
`CurrentPaletteEditor.vue:193` imports a raw `.vue` across the seam **right now**; `AdminFlaggedPanel`
AF-36 names a second (`:142`, raw cross-area `.vue` into `demo/shared/ui/`).
*Sharpens*: **G-4** clause 2 (`grep -c 'demo/@' eslint.config.js` = 0) and **G-6** whole — G-6's ban
must additionally FIRE on the two live counter-witnesses, or it is a ban that ratifies the breach.
⟨ColorSpaceSelector L-5(a) · PaneHeader PH-10/K-3 · PreviewStrip PS-15+K-11 · AdminFlaggedPanel AF-36 · wb-mix-pane K-E1-a/K-E1-b · ActionFeedback AF-13 · PaletteCardGrid PG-10 · PaletteColorStrip PCS-13 · ShadowPalette SP-11 · MigratePalettesDialog A-17 · PaletteSlugBar A-6 · AdminAuditPanel AAP-18 · AdminNamesPanel L-1 · CurrentPaletteEditor L-6 · ErrorBoundary EB-14 · PalettesPane PP-18 · SearchFilterBar SFB-37 · VersionHistoryDrawer VHD-28 · shell-dock-actionbutton ABN-12 · wb-mix-sourceselector MSS-28 · wb-mix-resultdisplay MR-28 · wb-mix-animationcanvas MX-25/MX-35 · wb-gradient-easingauthoringstage EAS-22 · wb-gradient-visualizer GV-4 · Dock DELTA-3⟩

**W8.6 · H-b — a guard that matches zero files IS the disease: the new ban carries a meta-check**
(cl.2 — **cure-lock, verbatim**)
`Dock`-adj **DELTA-3** ruling, verbatim: *"the ONE boundary this wave itself creates … lands HERE as
eslint structure alongside G-G, **WITH a meta-check that the glob matches ≥1 file (a guard that
matches zero files is the disease, not the cure)**."* Dock's own §8 item 3 records the accepted
consequence: *"building the shell→palettes eslint ban here while W-HYGIENE owns the lattice re-point
creates two eslint-touching commits. Accepted deliberately … the W-HYGIENE fold notes the rider so
neither doubles the other."*
**This fold IS that note.** X-W8.b's `eslint.config.js` carve and Dock/MT-DOCK-LAYERS-1's G-G ban are
**the same file, two owners** — W8.md §Disjointness already sequences X-W4(.d) and X-W9(.f) on this
file and must add the Dock ban to that list, or the two commits collide.
*Sharpens*: **G-6** (add the ≥1-file meta-check to the ban's acceptance criterion) · §Disjointness
cross-wave (add MT-DOCK-LAYERS-1/G-G to the `eslint.config.js` non-concurrency list).
⟨Dock DELTA-3 + §8 item 3 (verbatim) · AdminUsersPanel MT-AU1 §3 scope (9) "re-points the G-DEMO globs"⟩

**W8.7 · H-c — tsconfig↔exports parity: X-W8 DECLINES it by name, the cure shape is SUPERSEDED to
DELETE, and the row NEVER re-books under a second name** (cl.4 — **the packet's three carry-locks**)
**Facts, at eight-plus independent confirmations, unanimous**: `tsconfig.demo.json:42-49` = 8
hand-written keys; **3 phantom targets** (bare `@mkbabb/value.js` → absent `dist/index.d.ts`;
`/parsing`; `/units` — none exists on disk); **2 omissions** (`/css`, `/value` — both shipped, `/css`
live at 9-10 demo sites); `package.json#exports` = exactly **7 keys, no `.`**; the block's own comment
asserts "a CLOSED 8-key set" — **false by one**. Latent, not live.
**LOCK 1 — the cure is DELETE, not generate.** `Dock`-adj **R-3** rules it, and the resolver proof
travels with the identity: *"My own resolver run (paths absent): bare → UNRESOLVED; exported subpaths
→ local `dist/subpaths/*.d.ts` via package self-reference. Deletion makes the drift unrepresentable
(L-8); generation would maintain a mirror nothing needs."* This **supersedes** the generator-parity
cure shape ColorSpaceSelector.md:47 booked. Third, fourth and fifth independent instruments reach the
same resolution fact and are the identity's evidence, not new rows: `PaletteColorStrip` K-2
(`--traceResolution`), `wb-extract-imagedropzone` **K-14** (traceResolution witness, which *killed* a
reader-MAJOR built on the opposite assumption — "the traceResolution command belongs in the
W-HYGIENE parity wave's born-RED kit alongside the generator-parity command"),
`shell-dock-dockstatuslamp` **L-13** (compiler run; **BOTH readers' cells OVERTURNED**),
`Markdown`-adj L-1 (traceResolution + the `pretypecheck: npm run build` defeated-intent finding),
`wb-mix-animationcanvas` MX-20, `picker-componentsliders` R-10.
**LOCK 2 — silence is not adoption.** W8.md:112 is an EXPLICIT DECLINE, quoted verbatim above. Ten
records already honour it by name (`PaletteColorStrip` PCS-30 "X-W8.md:112 explicitly declines it";
`PreviewStrip` PS-25; `wb-extract-pane` XP-21; `wb-extract-workbench` XW-29; `wb-extract-controls`
EC-30; `wb-extract-imageeyedropper` EY-43 "X-W8 declined, W8.md:112"; `wb-mix-sourceselector` MSS-27;
`wb-mix-animationcanvas` MX-20; `wb-gradient-easingspecimenstrip` L-11; `AdminAuditPanel` AAP-32).
Four records nonetheless route it **into** X-W8 (`PalettesPane` PP-19 "IDENTITY-FOLD → L-14 → X-W8";
`CurrentPaletteEditor` L-11 "→ X-W8 (via the fold)"; `SearchFilterBar` SFB-36 "X-W8 via the existing
L-14 row"; `SwatchHoverMenu` SH-21 "Terminal: X-W8 via the standing fold"; `picker-heroblob` row 14;
`picker-componentsliders` R-10 "Parity → X-W8"; `shell-paneslot` L-9; `wb-gradient-codeeditor` A-26).
**RULED HERE: the decline governs.** Those eight routings are re-labelled
**NO-WAVE-OWNER, identity preserved** — the same disposition their nine siblings already carry. No
X-W8 close may claim H-c.
**LOCK 3 — corroboration accretes, never re-books.** Dock §5 item 1: *"L-22 ≡ W-HYGIENE config parity
(ColorSpaceSelector-adj L-14), **now at five independent confirmations** — corroboration accretes,
never re-books."* This fold carries it to **thirteen+** and mints nothing. `ColorInput`-adj §1 and
`Markdown`-adj §Method both record the failure mode by name: workers minted `MT-W-PATHS`,
`MT-W-UISHIM`, `MT-LIB-SURFACE-1`, `V·MT-W-LIBSURFACE-1` for already-banked identities — *"Before
minting a wave id, grep `registry/adjudicated/`."*
**Terminal**: **FORMATION BOUNDARY**, one row, cure = DELETE the block, born-RED kit =
{ColorSpaceSelector's generator-parity command *as the falsifier*, the traceResolution witness,
Dock's resolver run}. Sibling rows that must travel with it, never separately: `AdminPane` L-15
(`export.ts` shadows `export/`), `AdminPane` L-16, `wb-mix-configbar` **MC-11**/**MC-12** (three
config files justify themselves by a `file:`-symlink mechanism-C posture this checkout does not use —
lstat symlink false; `server.fs.allow` grants the repo's PARENT on a retired font-walk premise),
`wb-mix-sourceselector` **MSS-29** (eight demo runtime packages incl. `vue` in devDependencies; the
dead `sortablejs`/`@types/sortablejs`/`@vue/test-utils` trio), `Dock` **L-32**/**L-37**
(10 of 19 `test/` files import `../demo/`; `picker-blob-config.test.ts` greps three demo SFCs from the
library vitest root — *the owner-retired grep-invariant idiom RESURRECTED*),
`wb-gradient-easingspecimenstrip` **L3-2** (the library's publish gate imports demo feature internals
four levels deep).
⟨ColorSpaceSelector L-14 · Dock L-22 + R-3 (verbatim) · PaletteColorStrip PCS-30/K-2 · PreviewStrip PS-25/L2-2 · shell-dock-dockstatuslamp L-13 · wb-extract-pane XP-21 · wb-extract-workbench XW-29 · wb-extract-controls EC-30 · wb-extract-imageeyedropper EY-43 · wb-extract-imagedropzone R-26/K-14 · wb-mix-animationcanvas MX-20 · wb-mix-sourceselector MSS-27/MSS-29 · wb-mix-resultdisplay MR-29 · wb-mix-configbar MC-11/MC-12 · wb-mix-pane F-6 · wb-gradient-easingspecimenstrip L-11/L3-2 · wb-gradient-easingeditor L-r4:C-2 · wb-gradient-codeeditor A-26 · wb-gradient-visualizer GV-9 · Markdown-adj L-1 · AboutPane AB-24 · AdminAuditPanel AAP-32 · AdminPane L-15/L-16 · AuroraPane AP-31 · FlagReportDialog A-14 · SwatchHoverMenu SH-21 (R-B) · shell-paneslot L-9 · shell-dock-slugeditlayer L-9 · picker-heroblob row 14 · picker-componentsliders R-10 · picker-spectrumcanvas L-5 · GradientStopEditor L-8(a)/Lr2-8 · PalettesPane PP-19 · CurrentPaletteEditor L-11 · SearchFilterBar SFB-36 · Dock L-32/L-37 · ColorInput L-15 · EmptyState (named residue)⟩

---

## 2. PACKET 2 — the DEBUG / STATUS INSTRUMENT SURFACE: five files no bounds table lists

**The finding, restated with its census.** `DebugEventLog.vue`, `usePointerDebug.ts`,
`ParseEchoReadout.vue`, `DockStatusLamp.vue`, `ApiOfflineChip.vue` and `ActionFeedback.vue` are
**absent from every File Bounds table in W0.md..W11.md** (this seat's grep, §5) while their
adjudications route **21 / 18 / 12 / 20 / 26 / 33 row-instances** into X-W8 and X-W10.
**Prose mention is not ownership.** The three near-misses are named and disposed:

- `W0.md:96` / `:174` / `:176` own `audit/components/{ActionFeedback,picker-pointerdebugoverlay,
  picker-componentsliders-consolerail}/**` — the **challenge-corpus directories**, explicitly
  "the r2 challenge files and `evidence-r2/` probes, git-tracked". **Not the SFCs.**
- `W5.md:274` names `DockStatusLamp:70` inside gate **C7**'s RED cell as one of six width-based
  media queries to retire — a **counted line**, not a claimed file; `DockStatusLamp.vue` appears in
  no W5 bounds row.
- `W10.md:307` names `DockStatusLamp.vue` and `ApiOfflineChip.vue` inside a **migration-baseline
  census sentence** ("the migration baseline, not a …"). A baseline is a measurement, not a stamp.

**Law (cl.1, this fold's own):** *a wave may not stamp a file its File Bounds do not list.* A gate
whose RED cell cites `<file>:<line>` and whose wave's bounds omit `<file>` is **structurally
un-closable**: the executor must either widen bounds (triumvirate, per W8.md:63-65's bounds-expansion
trigger) or record the row UNEXECUTED. §3 is the widening list; §Gates marks which gates it unblocks.

---

**W8.8 · `DebugEventLog.vue` — 13 adjudicated rows, all routed X-W8 (CC-081), zero bounds rows**
(cl.3 + cl.1)
`picker-debugeventlog` closing verdict, verbatim: *"13 ADJUDICATED-CONFIRMED (1 MAJOR · 9 MINOR ·
3 INFO) · 8 KILLED · **every confirmed row routes to X-W8 per CC-081**"*. The rows:
**ADJ-1** (MAJOR — the severity palette *inverts*: `UNFREEZE` matches `includes("FREEZE")` and paints
alarm-red pixel-identical to `FREEZE_DETECTED`; `ORPHAN_POINTERS` and both `gate-block` strings fall
through unstyled while routine `spec:down/up` get tints; cure = severity as data + a five-line table
test over the closed 19-string vocabulary; **coverage today: 0 hits in test/ + e2e/**) ·
**ADJ-2** (UI seconds vs export milliseconds — the same event reads `0.42` on screen and `424.3` in
the copied JSON) · **ADJ-3** (retention/eviction/ordering absent from the type; counter freezes at 80)
· **ADJ-4** (`#666` 3.47:1 and `#777` 4.45:1 at 10 px — both fail AA) · **ADJ-5** (column budget
inverted; every alarm row wraps) · **ADJ-6** (`:key="i"` over a reversed array) · **ADJ-7** (double
truncation; the `title` cure REFUTED — the subtree is inert) · **ADJ-8** (log semantics; **explicitly
DECLINES** any live-region strategy) · **ADJ-9** (CSS-hygiene + the **KEEP ruling** on `:61-67`) ·
**ADJ-10..ADJ-13** (dead SVG arm; the four alarm rows print `p-1`/`??` sentinels as data; wrapped rows
typographically identical to new events; the producer's asymmetric `enabled` guard).
**W8.md bounds hold `demo/picker/visual/PointerDebugOverlay.vue` and nothing else in this cone.**
*Sharpens*: **G-11** — see W8.30.
⟨picker-debugeventlog ADJ-1..ADJ-13 + §Closing (both generations) · picker-pointerdebugoverlay "Discharged-by-sibling" note⟩

**W8.9 · `usePointerDebug.ts` — the producer of every overlay defect, in no bounds table** (cl.3)
The overlay adjudication routes **at least fourteen** rows onto this file: **FRZ-1** (`pointercancel`
strands the FROZEN alarm), **FRZ-2** (Reset permanently poisons the freeze detector — `activePointers`
declared **inside** the `if (enabled…)` block at `:156` while `forceReleaseAll` sits at outer scope
`:95`, so the phantom that caused the freeze survives the one recovery action and every subsequent
ordinary release re-latches a false FREEZE, *permanently*, plus false `ORPHAN_POINTERS` on every
`touchend`), **LEAK-1**, **CLR-1**, **GAUGE-ORDER**, **GAUGE-2**, **STR-1** (`isDebugOverlay()` gates
all six listeners on a hard-coded `.debug-overlay` literal defined only in the sibling view),
**SEL-DEAD** (`[data-reka-slider-thumb]` matches nothing in the entire dependency tree),
**LIFE-1**, **DEAD-BRANCH**, **DEAD-EXPORT**, **TYPE-DUP**, **GATE-ONCE**, **D-N4** (the activation
gate is `hash.includes("debug=1")` — `?nodebug=1` matches at index 2 — with **no build condition**,
statically imported and unconditionally rendered ⇒ **untree-shakeable, shipping to production**;
proved offline in the deployed March bundle).
`useSliderTouchGates.ts` and `SpectrumCanvas.vue` are its other two producers (19 labelled call sites
across exactly 3 files).
⟨picker-pointerdebugoverlay rows 3, 9, 10, 19, 20, 21, 22, 23, 24, 35, 37, 38, 39, 41 · picker-debugeventlog ADJ-10/-11/-13⟩

**W8.10 · `ParseEchoReadout.vue` — the ColorInput cone's readout, unowned** (cl.3)
`ColorInput`-adj's cure text names it directly: *"ParseEchoReadout: astEcho rewired; the hover-only
popover replaced by a named focusable disclosure or persistent instrument placement — **decided
in-wave** (D-15)"*, and the arbiter read the whole file as an instrument. It appears in **no** wave's
bounds. The wave that carves `ActionBarLayer.vue`/`ActionToolbar.vue` (this one, G-8/G-10) is the
nearest owner of its mount; the readout itself is **MT-COLORINPUT-1**'s propose-seam surface.
**Disposition: BoundsDelta candidate for X-W8.d, else NO-WAVE-OWNER named residue** — recorded so the
file does not fall between MT-COLORINPUT-1 and X-W8.
⟨ColorInput-adj §cure (D-15, verbatim) · shell-dock-actionbarlayer §instruments (ParseEchoReadout in the cone)⟩

**W8.11 · `DockStatusLamp.vue` + `status-lamp.ts` — owned BY NAME by a wave X-W8 declines** (cl.4)
`ApiOfflineChip`-adj §routing, verbatim: *"The lamp-side files (`DockStatusLamp.vue`,
`status-lamp.ts`) are owned BY NAME by the standing **MT-DOCK-LAYERS-1** (Dock adjudication scope
additions; D2-14/C-34/G-K/G-Q)."* W8.md:114-116 **declines** MT-DOCK-LAYERS-1's remaining arms —
see §4/W8.44. `DSL-11` (dead contract pointers `@lib/palette/api/availability.ts`,
`@components/custom/dock/…` — both W43-dead aliases) is routed *"≡ AP-26 → X-W8 (barrel death); the
status-lamp.ts copy corrected by MT-DOCK-LAYERS-1 (owns the file)"* — i.e. **one defect, split across
one wave that owns it and one that does not**. `DSL-13` (dead chip barrels, outer barrel zero
importers) → X-W8 cleanly.
**Ruled here**: the barrel/alias half lands at X-W8.b (it is a `demo/ui`/dead-specifier row); the
lamp file itself is a BoundsDelta candidate **only if** X-W8 also takes W8.44's decline. Otherwise
NO-WAVE-OWNER.
⟨ApiOfflineChip §routing + AP-26 · shell-dock-dockstatuslamp DSL-11/DSL-13/L-13 · W5.md:274 (C7 counts `DockStatusLamp:70`) · W10.md:307 (baseline prose)⟩

**W8.12 · `ApiOfflineChip.vue` + `demo/platform/transport/**` — a module-graph re-home with a
destination that exists and an owner that does not** (cl.3)
**AP-10** (MAJOR): *"Ownership inversion: a pure platform/transport concept homed three levels inside
the palettes mega-feature; sole import edge is `../../../platform/transport/useApiClient` (:31); zero
palette types touched; `demo/platform/transport/` already exists — **the cure is a move**."*
**Disposition: X-W8** (module-graph re-home) *"rides with AP-11 so the seam death and the move land
together"*. **AP-11**: the barrel seam is dead (zero entrants; the live consumer raw-imports the
`.vue`) and the lint law defending it cannot fire — three dead layers. **AP-26**: three doc
cross-references in the component's own cone point at W43-killed aliases.
**But** `demo/platform/transport/**` *"is claimed by NO wave — its rows go NO-WAVE-OWNER at the
formation boundary"* (ApiOfflineChip §routing). A move needs both ends in bounds.
*Sharpens*: **G-7** (a re-home is SCC-adjacent) and the §BoundsDelta list.
⟨ApiOfflineChip AP-10/AP-11/AP-26 + §routing (verbatim) · shell-dock-dockstatuslamp DSL-11/DSL-13⟩

**W8.13 · `ActionFeedback.vue` — 33 routed rows, one X-W8 limb, and a named NO-WAVE-OWNER residue
the fold must not lose** (cl.1 + cl.3)
`ActionFeedback` **AF-13** (MINOR, re-graded at R-1): *"the barrel seam is enforced by prose only;
BOTH declarations inert → dead-glob deletion **X-W8** (W8.md:42 owns 'delete the six dead `demo/@/**`
globs' by name); **the live re-point of G-DEMO-3b at `demo/palettes/**` (specifier-aware, per R-1's
correction) and the `index.ts:6-7` prose correction are in NO wave's mechanism — NO-WAVE-OWNER, named
residue**. Identity cross-ref: ColorSpaceSelector L-5(a)."* Its closing: 21 rows → X-W7,
**1 → X-W8 + a named NO-WAVE-OWNER residue**, 2 → X-W10, 3 → GLASS-RELAY.
**Ruled here**: the X-W8 limb is W8.5's identity (no second booking). The **live re-point at
`demo/palettes/**`** is the *positive* half of G-6's ban and is the one arm that makes the ban
non-vacuous for the largest feature tree in the demo — `PalettesPane` PP-18 measured it independently
(*"no `files` glob names demo/palettes"*), `PaletteCardGrid` PG-10 books the same residue by name.
**This fold routes the live re-point INTO G-6** as the ban's minimum scope, and records that
`ActionFeedback.vue` itself is in no bounds table (its 21 X-W7 rows are X-W7's problem; its 1 X-W8
row is a config edit, so no bounds add is strictly required — recorded so the asymmetry is visible).
⟨ActionFeedback AF-13 + §Closing · PalettesPane PP-18 · PaletteCardGrid PG-10 · W0.md:96 (audit dir, not the SFC)⟩

---

## 3. §BoundsDelta — files this wave must ADD to its bounds per the records

**Reading**: `MUST` = a record states the addition as a condition of the cure landing at all;
`CAND` = the record routes rows to X-W8 whose only cure surface is this file, without stating the
bounds duty; `DECLINE-BOUND` = the file is in bounds only if §4's decline is reversed.
Every path verified to exist on disk by this seat (2026-08-28).

| # | file (repo-relative) | why | grade | rows |
|---|---|---|---|---|
| 1 | `demo/picker/visual/DebugEventLog.vue` | 13 confirmed rows, "every confirmed row routes to X-W8 per CC-081"; in no bounds table | MUST | W8.8, W8.30 |
| 2 | `demo/picker/composables/usePointerDebug.ts` | producer of ≥14 overlay rows incl. FRZ-2 and the production-shipping gate D-N4 | MUST | W8.9, W8.28-31 |
| 3 | `demo/picker/controls/ComponentSliders/composables/useSliderTouchGates.ts` | MISS-5 (5 listeners re-bound to every detached wrapper), MISS-13, TYPE-DUP | MUST | W8.33 |
| 4 | `demo/picker/controls/ComponentSliders/ComponentSliders.vue` | C-2/MISS-5/MISS-7/MISS-13 dead-code carve; **W8.md:106-109 puts only its slider *import line* in bounds** | MUST | W8.33 |
| 5 | `demo/picker/controls/ComponentSliders/ConsoleRail.vue` | C-2 + MISS-13 (`any`-typed ref callbacks erasing the null branch) | MUST | W8.33 |
| 6 | `demo/picker/display/ColorComponentDisplay/readoutReservation.ts` | M-10 (module home), M-11 (the natural third export), M-15 (dead exports) — the SFC is in bounds, the module is not | MUST | W8.34 |
| 7 | `demo/color-session/useColorPipeline.ts` | R-29 (two dead `DIGITS`, one on the public surface, zero consumers) | CAND | W8.35 |
| 8 | `demo/color-session/useSliderGradients.ts` | R-29's dead-on-arrival duplicate | CAND | W8.35 |
| 9 | `demo/color-session/valueDomain.ts` | R-24 (3-line alias whose docblock describes a deleted architecture AND inverts the mutation contract) | CAND | W8.35 |
| 10 | `demo/picker/controls/spectrumLuma.ts` | M-11 (2 of 3 exports dead; self-presentation false in both numbers) | CAND | W8.36 |
| 11 | `demo/picker/controls/SpectrumCanvas/SpectrumCanvas.vue` | L-4/C-17 (`a68ecdc1` residue: name, orphan `<figure>`, dead docblock, 3 of 4 `--gamut-*` tokens, dead `overflow:visible`), L-6, L-7, D-17, M-6 | MUST | W8.36 |
| 12 | `demo/palettes/browser/search/MiniColorPicker.vue` | L-1+M-2: **disposition REVERSED** — unify ON the mini's containment; "do NOT delete the mini's plate outright" | CAND | W8.36 |
| 13 | `demo/shell/dock/ParseEchoReadout.vue` | ColorInput-adj cure names it; no wave's bounds | CAND | W8.10 |
| 14 | `demo/shell/dock/ActionButton.vue` | AT-17 (`label`, `hidden`, `.action-label` — dead API; `hidden` shadows the native attribute), AT-18 (dead `pointer-events-auto`), ABN-12 | MUST | W8.27 |
| 15 | `demo/shell/dock/menus/ProfileSection.vue` | **G-13's sole consumer**, named at W8.md:364 in prose only; carries C-1, L-2, C-2/D-3, D-1, D-4, D-11, C-10, C-11/L-7, L-3, L-4, L-8 → X-W8 | MUST | W8.39-41 |
| 16 | `demo/shell/dock/menus/MobileMenuDropdown.vue` | L-2 (`./dropdown-menu` shim edge, executable today per DELTA-10 D3-06), L-9 (the Dock SCC edge), L-10, M-H, D-08's dead-watcher fact against `useDockAdminMode.ts` | MUST | W8.3, W8.38 |
| 17 | `demo/shell/dock/DockViewSelect.vue` | DVS-1 (BLOCKER, **NEW arm**), DVS-9 (RTL ConfigProvider, NEW), DVS-15..DVS-19, DVS-23..DVS-29 riders — all "X-W8" | DECLINE-BOUND | W8.44 |
| 18 | `demo/shell/dock/ActionBarToggle.vue` | ABT-1 (BLOCKER), ABT-3, ABT-4, ABT-6, ABT-7, ABT-14, ABT-16, ABT-17 — all "X-W8" | DECLINE-BOUND | W8.44 |
| 19 | `demo/shell/dock/DockStatusLamp.vue` · `demo/shell/dock/status-lamp.ts` | DSL-11's copy; owned by name by MT-DOCK-LAYERS-1 | DECLINE-BOUND | W8.11 |
| 20 | `demo/palettes/browser/status/ApiOfflineChip.vue` · `demo/palettes/browser/status/index.ts` | AP-10 (the move), AP-11 (dead seam), AP-26, DSL-13 | MUST | W8.12 |
| 21 | `demo/platform/transport/**` | AP-10's **destination**; "claimed by NO wave" | MUST | W8.12 |
| 22 | `demo/platform/auth/**` (`session.ts` + the `SESSION_PORT_KEY` home) | paneslot L-5 (five one-line rewrites), ProfileSection L-4, PaletteCardMenu AP-19, MMD's "X-W8's structure (c)" | MUST | W8.24 |
| 23 | `demo/palettes/browser/index.ts` (+ `slug/`, `card/`, `status/`, `dialog/` sub-barrels) | SP-10 / SFB-38 / PG-10(a) / A-18 / AdminNamesPanel L-2: the self-declared "TOP-LEVEL SEAM", **46 lines, zero importers repo-wide** — "either the seam dies or the four reaches re-point — one decision, X-W8's surface" | MUST | W8.26 |
| 24 | `demo/palettes/browser/slug/PaletteSlugBar.vue` · `demo/palettes/useSlugMigration.ts` | **A-1 verbatim**: "the X-W8.a census addendum **must add** PaletteSlugBar.vue + both barrel rows + the `slugBarRef`/`import type` plumbing in useSlugMigration.ts, **or the deletion is lost**" (deletion alone discharges 11 rows + halves 4) | MUST | W8.25 |
| 25 | `demo/picker/ColorPicker.vue` · `demo/color-picker/composables/boot/**` · `demo/scenes/atmosphere/aurora-harmony-stops.ts` | EB-14 + paneslot L-6: three live upward edges close a cycle against `App.vue:164`'s downward import; `demo/platform/` exists as the landing stratum | MUST | W8.20 |
| 26 | `assets/docs/**` (11 guide SFCs) · `demo/scenes/about/katex/**` · `demo/scenes/about/content/` (destination) | AB-27 + Katex R11 ≡ Markdown L-3/G-MD-5: 11/11 docs import a scene-private leaf and the scene imports the docs back — "content outside the app, cyclic by construction"; **AB-27 explicitly notes `assets/` is outside W8's bounds today** | MUST | W8.21 |
| 27 | `vite.config.ts` · `plugins/vite-source-export.ts` · `plugins/vite-defer-glass-fonts.ts` | Markdown L-4 ≡ Katex R25 (the `@src` justification comment false at HEAD; `?source` consumers = 0) · A-24 (alias + plugin + comment all dead) · R26 (`vite-defer-glass-fonts.ts:72` guards on dead `demo/@/styles/style.css`, **so every dev-server typography measurement in the corpus was taken without the production font corpus**) | MUST | W8.22 |
| 28 | `package.json` (already in bounds) — **add the `@types/katex` row** | R24: katex 0.16.47 ships first-party types; `@types/katex@^0.16.8` is 39 patch versions stale and never consulted under `moduleResolution: bundler` | CAND | W8.23 |
| 29 | `vitest.config.ts` · `.gitignore` | EB-33: dead machinery preserving a `test/dist/` directory deleted at `6d6d3521`; the exclude filter now **silently un-excludes** any future `dist` under `test/**` — "NO-WAVE-OWNER; nearest X-W8.b/X-W9.f config carves, unowned by their text" | CAND | W8.23 |
| 30 | `tsconfig.demo.json` **(test-program membership only, not paths)** | DSL-8: `test/**` is in NO TypeScript program — vitest transpiles with esbuild, types never checked; "named gap: no X file mentions test-program membership" | CAND | W8.23 |
| 31 | `demo/shell/viewSchema.ts` **(the `group`/`audience` field only)** | DVS-4: the missing grouping column is *why* the dock re-enumerated admin three incompatible ways; the derivation is G-12's cure surface but the schema is X-W5's file | DECLINE-BOUND | W8.37 |

**Bounds rows NOT needed (recorded so they are not re-derived).** `AdminTagsPanel.vue` — already in
bounds via X.W8.c's "the 4 admin panels" (W8.md:87); its G-9 need is a **gate-census** widen, not a
bounds add (W8.31). `Markdown.vue` — in bounds (W8.md:85). `ActionBarLayer.vue`/`ActionToolbar.vue` —
in bounds (:88/:89); their needs are gate-enumeration widens (W8.29, W8.27).
`useContrastSafeColor.ts (+ its ink rung module)` — in bounds (:94), which covers `ink.ts`.
`PaletteCardSkeleton.vue` — **X-W7's file** (W8.32 is a cross-wave census note, not a bounds claim).

---

## 4. §CrossEdges

**CE-1 · MT-DOCK-LAYERS-1: eight records route its arms to X-W8; W8.md:114-116 declines all but
three.** W8.md, verbatim, under **Do NOT touch**: *"`MT-DOCK-LAYERS-1`'s remaining arms (Dock.md G-C,
G-G..G-R: registry, login truth, layout, register, focus, backdrop, hygiene). This wave takes exactly
the three subtractive local rows CC-083 names — L-18, L-19, L-20 — and their gates."* Against that:
`shell-dock-dockviewselect` routes DVS-1..DVS-29 to "X-W8" via DELTA-4 / L-19+R-5 / G-C / DELTA-10
D2-16 / L-6 / DELTA-5 / DELTA-13 **plus three NEW arms** (DVS-1 focus-ring, DVS-9 RTL ConfigProvider,
the DVS-6 o14 gate-flip) and a G-D amendment; `shell-dock-actionbartoggle` routes ABT-1/-3/-4/-6/-7/
-14/-16/-17 to "X-W8" (G-B/G-M/G-Q/structure (f)); `shell-dock-mobilemenudropdown` routes L-1/L-6/
C-3/L-4/D-18/D-04 there; `shell-dock-profilesection` and `shell-dock-actionbutton` likewise;
`shell-panesegmentedcontrol` PSC-5/PSC-9 cross-cite DELTA-5/G-L "at X-W8"; `ApiOfflineChip` puts the
lamp files there by name. **Ruled here (cl.4)**: the decline governs. Of the routed set, ONLY
Dock-adj **L-20** (→ G-8), **L-19** (→ G-12) and **L-18** (→ G-13) land at X-W8; DVS-3 and DVS-4's
latch half fold into L-19/G-12 (W8.37). Everything else is **DECLINED-BY-W8, identity preserved,
NO-WAVE-OWNER pending the formation boundary** — including two BLOCKERs (DVS-1's `--dock-ring`
`<color>`-into-a-shadow-token write leaving *no focus indicator on the control the first Tab reaches*;
ABT-1's PRM-removed restoration channel latching `overflow:hidden` forever). **This is the largest
single routing divergence in the corpus and the fold's headline cross-edge.**

**CE-2 · `eslint.config.js` — four owners, no sequencing row for the fourth.** W8.md:134-136
sequences X-W4(.d) and X-W9(.f) against X.W8.b(.e). Dock **DELTA-3/§8 item 3** adds a fifth commit
(the shell→palettes ban with its ≥1-file meta-check) and *"the W-HYGIENE fold notes the rider so
neither doubles the other"* — **this fold is that note** (W8.6).

**CE-3 · `demo/styles/foundation.css` — W8's DO-NOT-TOUCH vs Katex R28.** W8.md:101-103 forbids the
file (I-17 spectrum shim + the `tw-animate-css` import G-4 proves load-bearing). Katex **R28** routes
`foundation.css:91`'s `@source "../../color-picker/**"` (resolves to a nonexistent path; harmless only
because `:92` covers the tree) **to X-W8**. **Ruled: the DO-NOT-TOUCH governs**; R28 is
NO-WAVE-OWNER. Adjacent: `wb-mix-configbar` MC-5/MC-6 place the `:728-790` a11y media blocks in **no**
wave's bounds at all.

**CE-4 · CC-105 (`--slider-track-bg`) vs ConfigSliderPane's G-PAINT.** `AuroraPane` R-8 and
`BlobPane` row 23: two live registers claim the same sites — X-W8 banks them BLOCKED-ON the 8.0.0
repin with receiving surface X-W4.g, while the CSP adjudication's **G-PAINT** ruled the paint
demo-curable now and proved the naive retire-the-variant cure collapses the thumb to width 0.
AuroraPane's own routing, verbatim: *"the CSP G-PAINT ruling and the CC-105 booking must be reconciled
by the **X-W0 sitting**, not silently by either wave."* BlobPane adds: *"the bank must not resurrect
it."* **X-W8 neither opens nor pre-shims** (W8.md:104-109 already says so) — recorded so a close does
not read G-3's import-line touch as the CC-105 cut.

**CE-5 · G-9's null-DELTA vs X-W4's `iconOnly` and X-W10's register decision.** Three authorities
claim `UserSortMenu.vue:8` and `PaginationBar.vue:8,22`. `UserSortMenu` **R-G** (this seat quotes it
whole as the binding rider): *(a)* X-W8.b/G-9 deletes the dead attribute under a null-DELTA pair
proving **zero pixel change**; *(b)* DEFECT-LEDGER:23784's cure is `emphasis="quiet"`, which *changes*
paint; *(c)* V·MEGA-W-GEN-1 **G5** migrates ghost→quiet at ~48 non-pinned sites. *"(b)/(c) satisfied
would FAIL (a)'s gate, and (a) satisfied preserves the possibly-wrong register forever … the register
decision is design content → **X-W10 rules**; X-W8's deletion may land first (mechanically safe either
way); **if quiet wins, G-9's null-DELTA baseline must be re-stamped in the same commit** — otherwise
the gate outlaws the cure."* `PaginationBar` **DISSENT #6** states the trap from the other side:
*"a green G-9 is never citable as the cure of D-5's design limb"*, and **M5** adds the X-W4 arm:
*"X-W8's same-touch edits these exact attribute lists — sequence the two waves' touches; the
`iconOnly` arm carries an INTENDED geometry delta and may not ride under W8's null-DELTA gate."*

**CE-6 · G-9's null-DELTA vs skeleton *anatomy*.** `AdminListItem` **AL-M4** (binding caution,
repeated at `AdminFlaggedPanel` AF-9 and AF-40): the skeleton is not the row's shape (`gap-0.5` vs
`gap-1.5`; fixed `h-3.5`/`h-3` bars against two `--type-small` lines; an `h-7 w-14` lozenge matching
neither cluster) — *"a parity cure is a deliberate delta that must NOT ride silently inside that
unit"*, and the generic-controls homing question *"must not ride X-W8's null-DELTA skeleton edit"*.
`AdminListSkeleton` **A-1/A-2/A-5** are likewise **NO-WAVE-OWNER**: *"X-W8's only touch is dead-attr
deletion under an explicitly null-DELTA gate (G-9, W8:309-317) that **forbids anatomy changes by
construction**."*

**CE-7 · X-W6 → X.W8.c on the gradient SCC (reciprocated, and already correct).** W8.md:130-132
records that X-W6.c's leaf `model/types.ts` may zero the gradient cycle before X.W8.c opens, "and the
gate asserts residue zero, not an act performed". `wb-gradient-easingeditor` L-r1:L-2 confirms the
edge census from the other side (*"corroborated by W8.md:290's own edge census"*) and pins the
reconciliation at W6.md:202. No conflict; recorded as a green cross-edge.

**CE-8 · X-W7's `strictTemplates` double-covers G-9.** `AdminListSkeleton` **A-3** and
`PaletteCardSkeleton` **PCS-1**: X.W7.a's G3 (`strictTemplates: true`, W7:41/:309) makes the entire
dead-attr class a **compile error**, double-covering both files. `Markdown`-adj measured the demo-wide
cost: **271 errors (263 TS2353 + 8 TS2322)** — palettes 134 · workbenches 65 · shell 44 · scenes 14 ·
shared 4 · color-session 4 · color-picker 4 · picker 2 — and BANKED the widening because "many rows
are producer-boundary attrs-fallthrough decisions, not typos". **Sequencing consequence**: if X-W7
lands first, G-9's RED cells go GREEN by compiler, not by this wave's act — G-9 must then record
*residue zero, act not performed* (the same law W8.md:132 already states for G-7).

**CE-9 · `EmptyState.vue`: X-W7 owns the file, X-W8 owns its `demo/ui` row, and `variant` is
LEGITIMATE there.** `EmptyState` ES-17 → X-W8 (one row); X.W7.g deletes the `eyebrow` prop *and*
element and re-rules `o9` in the same commit. `VersionHistoryDrawer` **VHD-36** binds the census:
*"At cure time the census must separate components that legitimately publish `variant` (e.g.
`EmptyState`) from the dead-on-Button sites."*

**CE-10 · `assets/docs/**` sits outside every wave, and the natural claimant is X-W6.f.**
`AboutPane` **AB-27**: *"X-W8's SCC row (CC-079) names the Markdown TYPE cycle only; `assets/` is
outside W8's bounds. Boundary row; the X-W6.f `doc:` re-plumb is the natural landing and **should
claim it at wave-open**."* Katex **R11** routes the same identity to X-W8 (cross-booked with
Markdown-adj L-3/G-MD-5). **Ruled: one identity, two candidate homes; X-W6.f claims it at wave-open
or it is NO-WAVE-OWNER** — X-W8 may not close CC-079 as "the Markdown cycle" and imply the content
cycle died with it.

**CE-11 · X-W1 owns the witness instruments X-W8's probe arms need.** G-15 prefers X-W1's
`e2e/visual/` goldens (CC-030) and "does not wait". Three records add named riders: `TEST-0`
(*"the entire 703-line subsystem has zero coverage across every suite … the spec may ride X-W8's cure
commit as witness"*), `picker-spectrumcanvas` **M-21** (a live un-skipped smoke oracle asserts
`.plate-caption`, a class deleted at `a68ecdc1` — "a structurally-red gate in the lane is either
failing silently or the lane is not running"), and `ActionBarLayer` **AB-9** (the sole gate cites a
phantom test file: `ls test/parsing/` → `timeline` only). **All three are X-W1's**, but M-21's residue
is the **same `a68ecdc1` family as L-4** (W8.36) — one deletion, two waves' witnesses.

---

## 5. Census law and the commands behind §0's IN side

Id-keyed, re-runnable from the repo root at any HEAD:

```sh
# routed line-instances into this wave, value.js corpus only
cd docs/tranches/V/megatranche/registry/adjudicated
grep -rnE 'X-W8|X\.W8' --include='*.md' . | grep -v -E '\./(kf-|fr-|pt-)' | wc -l        # 455
grep -rnE 'X-W8|X\.W8' --include='*.md' . | grep -v -E '\./(kf-|fr-|pt-)' \
  | cut -d: -f1 | sort -u | wc -l                                                        # 64
grep -rlE 'W-HYGIENE' --include='*.md' . | grep -v -E '\./(kf-|fr-|pt-)' | wc -l         # 46
# packet-2 falsifier: are these files in ANY wave's bounds?
cd ../../../X/waves
grep -l 'DebugEventLog\|ParseEchoReadout\|usePointerDebug' W*.md                          # (empty)
grep -n 'DockStatusLamp\|ApiOfflineChip' W*.md   # W5:274 gate cell · W10:307 baseline prose only
grep -n 'ActionFeedback' W*.md                    # W0:96/:174/:176 = audit/components/**, not the SFC
```

**Law (M-25 / L-19).** Every row above names a banked id, a mechanism, and — where the row is a gate
input — a command or a byte citation. **No `proof:*` script, no gate-farm, no cross-product
denominator.** Where a figure is contested (the 90/92 site count; the 51/55, 51/22, 65/27, 103/36,
29+28/26 `variant` censuses; 17-vs-18-vs-19 one-line barrels) the fold records **both readings and
which measurement supersedes**, never a silent pick.

---

## 6. §Rows — the remainder (W8.14 … W8.51)

*(W8.1–W8.13 are §1/§2. Rows here are grouped by the wave unit they sharpen; the `→` cell names the
gate. Cure-shape locks and sequencing edicts are verbatim where load-bearing.)*

### 6a. X.W8.a — the C-16 census (→ G-1)

**W8.14 · The dead-`variant` fleet: FIVE incompatible censuses, a gate that names TWO files, and an
open dispute about whether X-W8 owns the class at all** (cl.1 · MAJOR)
Facts unanimous at nine seats: `Button.vue.d.ts` declares
`emphasis|tone|size|iconOnly|loading|type|disabled|class` and **no `variant`**; compiled default
`emphasis: { default: "secondary" }`; zero attribute selectors match `[variant=]` anywhere in
`node_modules/@mkbabb/glass-ui/dist/**/*.css` or demo CSS ⇒ inert DOM fallthrough, invisible to
vue-tsc and to every visual gate. **The censuses do not agree**:
`FlagReportDialog` A-1 / `AdminTagsPanel` ATP-24 / `AdminAuditPanel` AAP-7 = **51 sites / 22 files**
(+"17 `variant="outline"` in browser/admin **including PaginationBar.vue:8/:22, which no axis and
neither reader named**") · `PaletteSlugBar` A-11 = **51 of 55 Button open tags**, only 2 sites use the
real axis, **two values (`primary-audacious` ×2) never existed in any Button vocabulary** ·
`VersionHistoryDrawer` VHD-36 = **65 lines across 27 demo SFCs** vs **2** `emphasis=` call sites ·
`MigratePalettesDialog` A-5 = **103 sites / 36 files**, third-witnessed ·
`AdminFlaggedPanel` = **29+28 across 26 files** · `PaginationBar` = **103 `variant=` vs 2
`emphasis=`**. **G-9 names exactly two files.**
**Disposition dispute, recorded not resolved**: `PaletteSlugBar` A-11 rules *"per X-W8 scope item 1
the file bounds bind to the CENSUS, and this seat's 51-site/22-file census is that census's
**mandatory denominator**"*; `VersionHistoryDrawer` VHD-36 rules *"the repo-wide class → BUILD →
X-W8 … the census joins W8's C-16 residue census"*; `MigratePalettesDialog` A-5 rules *"Family →
X-W8 (CC-081's cure home — **scope corrected**: CC-081 says '2 consumers'; the census is 103 sites/36
files; per the scope law the row books where the count lives)"*; but `FlagReportDialog` §3 **amends
its own A-1**: *"the 51-site sweep → boundary row (**no X wave owns it — X-W8's re-point does NOT cure
it, see KILL-1**)" → FOLD-IDENTITY → **MT-BTN1**; no new boundary row"*, and `MiniColorPicker`,
`AdminFlaggedPanel` AF-6, `UserSortMenu` R-A all cross-book to **MT-BTN1** — *a fleet identity whose
wave, like W-HYGIENE, was never cut into X.*
**Ruled here (cl.1)**: X.W8.a's census **MUST** enumerate the class with all five prior readings
marked and superseded-by-measurement; whether the *cure* lands at X-W8 is a **live disposition
conflict** and is named to the formation boundary — this fold does not elect. What X-W8 unambiguously
owns today is G-9's two files, under CE-5's non-citability rider.
⟨FlagReportDialog A-1 + §3 (MT-BTN1 amendment) · MigratePalettesDialog A-5 · PaletteSlugBar A-11/A-12 · VersionHistoryDrawer VHD-36 · AdminAuditPanel AAP-7 · AdminTagsPanel ATP-24 · AdminFlaggedPanel AF-6 · UserSortMenu US-1/US-6/R-A/R-G · PaginationBar L-1/D-5 · MiniColorPicker · shell-dock-profilesection C-2/D-3 · AdminUsersPanel L-3 (MT-BTN1 home)⟩

**W8.15 · The 48-file census has a stated six-file remainder, and one record names a member**
(cl.1) W8.md:6-8 leaves "the remaining six" to X.W8.a. `PreviewRamp` **R-20** names one:
*"Route: X-W8 (graph/barrel subtraction; **feed X.W8.a's census — the six-file remainder census
explicitly includes `demo/color-session` strays**)"* — `color-chips/` filed by authorship, zero
`demo/color-session/` consumers, all 3 importers reach up-and-across, 3 of the barrel's 6 exports with
zero external consumers, and the only test bypasses the barrel via a deep path
(`preview-chips.test.ts:27-33`). `ColorSpaceSelector` G10's own scoping is the corroborating cell:
`demo/color-session/` holds **exactly 1** of the 90 edges (`ColorSpaceSelector.vue:109`).
→ **G-1**, and the `color-chips` barrel decision rides G-2's clause 2.
⟨PreviewRamp R-20 · ColorSpaceSelector L-4/G10⟩

**W8.16 · `cn`'s deletion condition is DISCHARGED, not conditional — G-5's RED cell can be stated
harder** (cl.1) `wb-gradient-codeeditor` **A-25**: *"`cn` has exactly ONE occurrence repo-wide (its
definition — my census), so the `clsx`+`tailwind-merge` deletion condition is **DISCHARGED, not
conditional**."* Independently: W8.md's own G-1 command 4 measures 0 and G-5's RED cell says "exports
`cn` with **0** consumers repo-wide". Third witness: `PaneHeader` **PH-32** — `demo/shared/utils.ts:17`
justifies the local `debounce` on *"the library's root-barrel export stands for external consumers"*,
which is **false** (no `.` export, no root d.ts); `wb-extract-pane` **XP-22** adds *"`grep debounce
src/` = 0; the symbol is gone from `src/`"*, routing the demo half to W8.md:163 and the root-entry
decision to X-W9. **Lock**: the false doctrine sentence **dies by deletion of the file**, not by
rewrite (PH-32's disposition text).
→ **G-4/G-5**. ⟨wb-gradient-codeeditor A-25 · PaneHeader PH-32 · wb-extract-pane XP-22 · ColorInput L-15 (the doctrine sentence BUILDs at ColorInput if that wave lands first — one sentence, two claimants)⟩

### 6b. X.W8.b — barrels and configuration (→ G-2..G-6, G-9)

**W8.17 · The barrel tier is the demonstrated CAUSE of the defects the wave is cutting — carry the
argument, it is the wave's own warrant** (cl.1) `AdminTagsPanel` **ATP-22**: *"The second-order cost
is PROVEN by this corpus: the alias layer is why three axes read `chipVariants.d.ts` and none noticed
the stylesheet never reaches the page (ATP-23), and why `variant="outline"` survived three hostile
reads (ATP-24)."* `PaneHeader` PH-31: the same mechanism hid `CardHeader shrink` and `CardTitle as`.
`AdminAuditPanel` AAP-7: the panel is "right on Badge (real `variant`) and wrong on Button" —
a distinction the barrel erases. → **G-2/G-3** rationale (no gate change; the wave's COMPLETABLE
argument gains three receipts).

**W8.18 · Byte-cost claims about the barrels are REFUTED and must not be re-quoted** (cl.1 —
**struck and uncitable**) `ErrorBoundary` **EB-17**: *"its 231 KB cost claim stays REFUTED …
(15,665 B byte-identical both ways; 231 KB = dev prebundle)"*, carried WITH the finding as **U-9**
(re-run before citing). `AdminUsersPanel` §2: *"Bundle-cost arm stays HYPOTHESIS — never measured on
the built artifact; per L-12 only `npm run gh-pages` output may ever decide it."* `TagEditPopover`
K-2, `picker-componentsliders` K-14 (the 87× metric), `SwatchHoverMenu` SH-9 (U-8: "the cost is
ownership, not weight"), `Dock` R-21 (*"never a shipped-bytes claim"*), `AdminFlaggedPanel` AF-35
("dev-server byte figure stays labeled dev-only"), `shell-paneslot` L-4 ("the defect is ownership,
not bundle weight"). → **G-2/G-3**: the commit body may state ownership, graph and surface-contract
cost, **never a shipped-bytes claim**.

**W8.19 · The barrel deletion must not be reported as curing the dead-`variant` class** (cl.2 —
**cure-lock, verbatim**) `FlagReportDialog` **A-19**: *"Disposition: FOLD-IDENTITY → X-W8 (barrel
subtraction is its charter: 19 barrels/92 imports). **X-W8 must NOT claim this cures A-1 — it does
not.**"* → **G-3**'s close text.

**W8.20 · The demo's real cycle is feature→boot, and it closes through the app root** (cl.1 · MAJOR)
`ErrorBoundary` **EB-14** (every fact re-run): three live upward edges —
`aurora-harmony-stops.ts:23`, `aurora-bracket.test.ts:14`, `ColorPicker.vue:129` — close a cycle
against `App.vue:164`'s downward `import { ColorPicker } from "../picker"`; `demo/platform/` exists as
the landing stratum. `shell-paneslot` **L-6** adds the third edge verified: `ColorPicker.vue:131`
imports `COLOR_TARGET_PORT_KEY` from `../palettes/usePalettePorts` — **feature-to-feature** — and
notes the aggravating directory inversion (`demo/color-picker/` = the app; `demo/picker/` = the
component). `PaletteCardMenu` **AP-20**: *"pass-4's 'no upward import' negative is **false**:
`usePalettePorts.ts:19` (type) + `ColorPicker.vue:130` (VALUE import of an injection key,
non-erasable) both exist"* → X-W8. `PreviewStrip` PS-15 measures the same edge live one hop from its
subject.
**EB-14's own split**: the dead-glob carve + boundary ban = X-W8 (W8.md:42-43 "owns both by name");
*"the two-file platform re-home is **NO-WAVE-OWNER in X** (named residue **so the cycle cut is not
silently half-done**)."*
→ **G-7** (this cycle is not in G-7's four-SCC table) + §BoundsDelta #25.
⟨ErrorBoundary EB-14/EB-27 · shell-paneslot L-6 · PaletteCardMenu AP-20 · PreviewStrip PS-15 · AuroraPane AP-19 (the `demo/atmosphere/` mint DECLINED — "X-W8 is subtraction-only for `demo/ui`"; interim guard: no new edge in either direction)⟩

**W8.21 · The content cycle: 11 of 11 guide SFCs import a scene-private leaf and the scene imports
them back** (cl.1 · MAJOR) `Katex` **R11** ≡ `Markdown`-adj **L-3/G-MD-5** ≡ `AboutPane` **AB-27** —
ONE identity, cure already built at Markdown (content moves to `demo/scenes/about/content/`, the
line-2 import becomes `"../katex"`); the dead barrel (L-9) dies free with it. R11's admitted rider:
`app.component("Katex", Katex)` additionally deletes the 11× duplicated import header —
*"compatible with, not competing with, the move."* AB-27's jurisdictional finding stands: `assets/` is
outside W8's bounds. → **G-7** + §BoundsDelta #26 + CE-10.

**W8.22 · `@src` / `?source`: an alias, a plugin, and three comments that are all dead** (cl.1)
`Markdown`-adj **L-4** (cure deletes plugin + alias + comment) ≡ `Katex` **R25**
(`vite.config.ts:70-74`'s justification comment factually false at HEAD; `@src|?source` in `assets/`
= 0) ≡ `wb-gradient-codeeditor` **A-24** (own grep: 0 demo hits; the only `?source` hits are the
plugin and the comment). Fourth fact, **with an instrument caveat that binds X-W1**: `Katex` **R26** —
`plugins/vite-defer-glass-fonts.ts:72` guards on the dead `demo/@/styles/style.css` (the marker is
actually at `foundation.css:75`), so the transform **can never fire**, and therefore *"every
dev-server typography measurement in this challenge corpus was taken without the production font
corpus — an instrument caveat X-W1 must inherit."* Fifth: `Katex` **R28** —
`foundation.css:91`'s `@source "../../color-picker/**"` resolves to nonexistent
`<repo>/color-picker` (see CE-3, DECLINED). Note: Markdown-adj measured **4 `test/` files** still
importing `@src`. → **G-4** extension + §BoundsDelta #27 + X-W1 rider.

**W8.23 · Config residue beyond `components.json`: four small dead machines** (cl.1 · MINOR cluster)
(a) `@types/katex@^0.16.8` fully dead — katex 0.16.47 ships first-party types declared in `types` and
every exports condition; `moduleResolution: bundler` never consults `@types/`; 39 patch versions
stale, **wrong contract if it ever won**; all three axes read the line and certified "clean"
(`Katex` R24 → X-W8, one-line deletion). (b) `.gitignore:18-24` + `vitest.config.ts:22-27` preserve a
`test/dist/` gate directory deleted at `6d6d3521`, and the exclude filter **silently un-excludes** any
future `dist` under `test/**` — *"the exact hygiene invariant that would have caught EB-15's drift"*
(`ErrorBoundary` EB-33 → NO-WAVE-OWNER, nearest X-W8.b/X-W9.f). (c) `test/**` is in **no** TypeScript
program (`shell-dock-dockstatuslamp` DSL-8 — vitest transpiles with esbuild; the O-22 matrix's typed
annotation is decorative; *"named gap: no X file mentions test-program membership"*). (d) the
mechanism-C `file:`-symlink prose in three config files is false at install
(`wb-mix-configbar` MC-11/MC-12 → W-HYGIENE). → **G-4** + §BoundsDelta #28-30.

**W8.24 · `SESSION_PORT_KEY`: the shell imports a FEATURE to learn who is logged in — five
one-line rewrites, one destination, no owner for the destination** (cl.1 · MAJOR)
`shell-paneslot` **L-5** (all five edges verified: `Dock.vue:18`, `DockViewSelect.vue:8`,
`ProfileSection.vue:14`, `MobileMenuDropdown.vue:13`, `SlugEditLayer.vue:5`, + the direct
`ColorInput.vue:133` edge + the `shell → palettes → shell` type back-edge; closure arithmetic
135/54/40% → UNPROVEN) → **X-W8** ("SESSION_PORT_KEY to `platform/auth/`; five one-line rewrites").
Corroborated: `shell-dock-profilesection` **L-4** (*"shell → feature → platform"*, three edges, with
L-5's dead guard as the reason it was never caught), `PaletteCardMenu` **AP-19** (5 of the port's 7
members are `platform/auth` re-exports), `Dock` **L-6**/G-G, `shell-dock-dockviewselect` DVS-10.
**MMD-2's warning (cl.2)**: *"The seam is X-W8's structure (c) (`platform/auth/session.ts` + the same
five consumer files); co-landing there is the natural cut, **but no X wave currently claims the
behaviour change**"* — the *move* is X-W8's; `regenerate()`'s register-then-swap repair is NOT, and a
close must not imply it. → **G-7** + §BoundsDelta #22.
⟨shell-paneslot L-5 · shell-dock-profilesection L-4 · PaletteCardMenu AP-19 · shell-dock-dockviewselect DVS-10 · shell-dock-mobilemenudropdown MMD-2 (verbatim) · Dock L-6 (its "cycle" and bundle-bytes framings STRUCK)⟩

**W8.25 · `PaletteSlugBar.vue` is an orphan: deleting it discharges eleven rows and halves four
more — and the census must add it or the deletion is lost** (cl.2 · **the sharpest single
BoundsDelta**) `PaletteSlugBar` **A-1**, all three axes + both readers + the seat: `<PaletteSlugBar`
appears in **no template**; the only code refs are an `import type` + `ref<InstanceType<…>>`
(`useSlugMigration.ts:6/:30`) and two barrel lines (`browser/slug/index.ts:3`, `browser/index.ts:44`).
The live twin is `SlugEditLayer.vue`, mounted at `Dock.vue:149`. **Disposition, verbatim**:
*"NO-WAVE-OWNER (nearest: X-W8.d dead-surface subtraction — the unit's file bounds do not currently
name this file; **the X-W8.a census addendum must add PaletteSlugBar.vue + both barrel rows + the
`slugBarRef`/`import type` plumbing in useSlugMigration.ts, or the deletion is lost**). Deletion alone
discharges A-4, A-17, A-23, A-24, A-25, A-26, A-27, A-28, A-32, A-33, A-34 and the orphan-half of
A-5/A-8/A-15/A-16."* → **G-10** + §BoundsDelta #24.

**W8.26 · The self-declared "TOP-LEVEL SEAM" has zero importers — one decision, X-W8's surface**
(cl.1 · MAJOR) `ShadowPalette` **SP-10** (own grep, 0 hits; all four real reaches go to sub-barrels —
ExtractWorkbench, MixSourceSelector, GenerateControls → `browser/card`; App.vue → `browser/dialog`):
*"46 lines of dead re-export surface … **BUILD → X-W8** (barrel liveness; **either the seam dies or
the four reaches re-point — one decision, X-W8's surface**)."* Independently at
`SearchFilterBar` **SFB-38** (P4-2's delete-not-collapse disposition adopted over LP2-5 → X-W8),
`PaletteCardGrid` **PG-10(a)** (*"the barrel keep/delete decision + docblock truth-correction ride the
same X-W8 seat"*; the docblock's own PI-6 tree-shake rationale makes the surface dead by its own
logic), `MigratePalettesDialog` **A-18** (*"46 lines of dead code whose one live effect is keeping
PaletteSlugBar nominally exported (compounds A-3)"* — which is exactly W8.25's second barrel row),
`AdminNamesPanel` **L-2** (*"phantom public surface, zero importers (20 symbols)"* → X-W8),
`PaletteColorStrip` **PCS-9** (*"SIX areas raw-reach `palettes/` internals, `shell/dock →
palettes/usePalettePorts` being a shell→feature inversion"*; the area **seam construction** needs an
owner at the formation boundary), `UserSortMenu` §instruments (**0** direct / **13** sub-barrel
importers), `BrowsePane` M14 (the `card` cluster's home is real and unowned — *"W7.c **creates**
`PaletteSpecimen` inside `browser/card/`; the next formation should decide the home BEFORE that file
multiplies the audience defect"*). Also dead in the same class: `PalettesPane` **PP-30** (the card
barrel's PI-6 claim conflates binding reachability with module side-effect reachability; no
`sideEffects` field; two never-rendered SFCs' scoped styles ride into the pane's dev graph → X-W8,
CC-007). → **G-2/G-10** + §BoundsDelta #23.

### 6c. X.W8.c — SCC closure (→ G-7)

**W8.27 · The Dock runtime cycle is confirmed at four seats, and the barrel keeps exactly one
export** (cl.1) `shell-dock-actionbartoggle` **ABT-14** (*"Dock-adj found it the tree's ONLY runtime
cycle. `Dock` re-export earns its keep"* → X-W8, W8.md:84, "G-Q's `from "./"` grep already RED"),
`shell-dock-mobilemenudropdown` **L-9** (*"its SCC table names this exact edge"*),
`shell-dock-actionbarlayer` **AB-27** (directory inversion + the dual import path + the real cycle →
X-W8 scope 6 + X.W8.b; **MISS-8**: `demo/shell/dock/index.ts` does not re-export `DockCrossfade`, so
the transposition's import lands cleanly **only after** the barrel decision — *"recorded so the
sequencing (b → d) is honored"*), `Dock` **D3-09**. Nothing to change in G-7's Dock row; the
**b→d sequencing** is the fold's addition and it is already W8.md:123's order — recorded as
**satisfied**, with MISS-8 as its named witness.

**W8.28 · The Admin/provider seven-node SCC: the god-port is the edge, and its width is the defect**
(cl.1) G-7's 7-node table is confirmed member-for-member at `AdminAuditPanel` **AAP-27**
(*"cycle membership re-measured at the AdminUsersPanel §3 madge run (4 chains, this panel in all four
via the barrel); runtime-acyclic (`import type` + verbatimModuleSyntax)"*) and `AdminTagsPanel`
**ATP-21** (*"W8.md:291 enumerates AdminTagsPanel in the 7-node SCC by name … keys to a keys module
per the house idiom at `color-session/keys.ts`"*). The **cause** rows ride the same carve:
**AAP-28** / **ATP-20** — `ADMIN_PORT_KEY` is a **34-member god port** injected whole to read one
member (`pm.tags`), identity `AdminUsersPanel` **L-2**, *"riding X-W8's `usePalettePorts.ts`
modify-carve"*. **Cure-lock from the owning docket (`AdminUsersPanel` L-22 ruling + G-7 AMENDMENT)**:
the cure is a **narrow port**, not props-only — *"the panel may inject exactly the narrow port key +
the shared color keys, all from keys modules (G-19's law), and nothing else"*; that ruling **amends**
r2's zero-`inject(` gate arm. → **G-7** (add the keys-module clause to the "one owner, no forwarding
directory" criterion).

**W8.29 · `usePaneRouter.ts` is a four-concern module and the dock imports its own types back out
of it** (cl.1 · MAJOR) `shell-paneslot` **L-4** (verified: registry `:69-95`, prop factories
`:126-161`, slot computeds `:163-184`, DockAction types + per-view tables `:38-58/:186-228`;
`Dock.vue:24` and `GenericActionBar.vue:4` type-import from `"../usePaneRouter"`; nine private pane
method names dispatched through `Ref<any>`): *"action/type home moves with the **X-W5** registry;
**module split → X-W8**."* → **G-7**: a split whose destination is X-W5's registry — sequencing note,
not a bounds claim.

**W8.30 · `CurrentPaletteEditor` L-24 · the palettes↔shell lattice cycle** (cl.1) type-import up at
`usePalettePorts.ts:19`, five `SESSION_PORT_KEY` value-imports down — *"lattice cycle, not load-time
(R2's own limitation correct) → X-W8 (SCC subtraction)"*. ONE identity with W8.24; recorded as its
second graph-side witness, not re-booked.

### 6d. X.W8.d — dead surface, dead state, dead ink (→ G-8, G-10, G-11, G-12, G-13)

**W8.31 · The shim transposition discharges 14 of 26 rows, and its price is CORRECTED** (cl.2 —
**ruling 2, verbatim, binding on the executor**) `shell-dock-actionbarlayer`, center of gravity:
*"one transposition — delete the `useLayerTransition` shim and mount the installed `<DockCrossfade>`
(X-W8.d, G-8/G-10, ≡ CC-080 ≡ Dock.md L-20 ≡ MT-DOCK-LAYERS-1, I-18 DECIDED retire-clean-break) —
discharges 14 of the 26 confirmed rows"*, and the strongest argument appears in neither axis:
**AB-12/MISS-4** — *"`inert` is void across Teleport … a component swap ends teleported content by
unmount; a class swap cannot."*
**Ruling 2 (price correction, R-LC wins on scope)**: inactive faces are `position:absolute` ⇒ **no
resting tax** — *"not 'at rest', not 'on every route'"*; the TRUE residual is that **while the Tools
bar is open in `actions` mode, the plate carries the input face's peak (≈+141 px, chromium cell)**.
**AB-34** sharpens it further: the reserve running max is **session-history-dependent** on a
content-sized face — the open bar's plate *ratchets to the widest string yet displayed and never
shrinks that session*.
**Carry-table addenda G-8 must absorb** (L's seven-row style-equivalence table is the wave's risk
register): **AB-2**'s hand-bound private `.dock-layer` contract (the class string is hand-authored
**exactly once in the repo — here**) with the `min-height` **not-carried** flag · **MISS-7** (the cure
nests `.dock-crossfade` inside `.dock-crossfade`; `[data-morphing] … > .dock-face-content {
clip-path: inset(…) }` matches BOTH after the transposition) · **AB-33** (the hybrid box neither
author specified: consumer `display:grid` + row-gap 8 px wins while producer column-gap 6 px survives
⇒ the measured `gap: 8px 6px`; `grid-area:1/1` inert; **which half wins depends on Tailwind's layer
emission order — a second unversioned coupling**; plus the missing `.dock-layers { min-width: 0 }`
that `:120` hand-patches onto ColorInput alone) · **AB-5** (`.dock-layer-grid` matches **zero** CSS
rules anywhere — 1 template hit repo-wide, 0 in the producer dist) · **AB-35** (`mode="out-in"` leaves
the just-pressed 40 px seat glyphless ~200 ms on every mode click, on the same beat as AB-10's plate
translation).
Third-party corroboration of G-8's own RED cell: `shell-dock-actiontoolbar` **AT-12** and its
**retracted counter-hypothesis** (a partial `layers.css` grep nearly minted a false "260 ms opaque
superposition" correction against G-8's authored cell; full-rule extraction settled it) — *"a CSS
mechanism claim is not derived until every rule touching the class has been read."*
→ **G-8**. ⟨shell-dock-actionbarlayer AB-1/AB-2/AB-4/AB-5/AB-10/AB-12/AB-18/AB-19/AB-23/AB-27/AB-33/AB-34/AB-35 + MISS-4/-7/-8/-9 + rulings 1-2 · shell-dock-actiontoolbar AT-12 + §DISSENT · Dock L-20 (harm clause STRUCK) · picker-pointerdebugoverlay (unrelated) ✗⟩

**W8.32 · G-10's five-site enumeration is short by at least eight sites, and one of its deletions
collides with a cure that needs the thing deleted** (cl.2 — **two addenda + one sequencing edict**)
**Enumeration gaps** (`shell-dock-actionbarlayer` **MISS-13**, arbiter-original): G-10 omits
`ActionBarLayer.vue:96`'s `defineExpose({currentToggleIcon, toolbarMode, cycleToolbarMode})` — the
mount is **ref-less** (`Dock.vue:156`), so it is dead **and a writable path into AB-7's state** — and
`:83`'s `subLayerGridEl`. `shell-dock-actiontoolbar` **AT-17** adds `ActionButton.vue`'s `label`
(`:65`, rendered `:32`) and `hidden` (`:69`, gating `:2`) — *passed by neither consumer, unreachable
via `DockAction`*, `.action-label` with **zero CSS rules anywhere**, and `hidden` **shadowing the
native HTML attribute** (a consumer's `hidden` would be swallowed); **AT-18** adds the dead
`class="pointer-events-auto"` on the `<Popover>` root (glass `Popover` is `inheritAttrs:false` and
renders reka roots with an explicit prop list and no attrs spread — the identical string on
`PopoverContent` IS live). **AB-20** adds `provide(COLOR_MODEL_KEY, actionBar.colorModel)` — a
provable identity no-op with a false comment.
**SEQUENCING EDICT (`shell-dock-actionbarlayer` MISS-12, verbatim)**: *"X-W8.d's G-10 deletes
`colorInputRef` while AB-11's only in-file cure needs it (or its successor). **The wave must rule
use-or-delete atomically**; a wave that deletes Monday and needs Tuesday re-files AB-11 as a
regression."* AB-11's mechanism is why: the transposition does **NOT** buy the focus fix —
`dist/dock.js` transfers focus only when the dissolving face **contains** `document.activeElement`
(`!n.contains(r)` guard), and the driving toggle is a sibling of both faces; the instrument exists and
is dead (`colorInputRef` `:28/:116` against ColorInput's exposed `focus()` `:283`).
**AB-9's rider** (→ X-W1, not here): the component's sole gate cites a **phantom test**
(`color-propose.spec.ts:22-25` claims coverage in `test/parsing/extract.test.ts`; `ls test/parsing/` →
`timeline` only) and the false-citation deletion + the cycle-invariant test ride X-W1.
→ **G-10**. ⟨shell-dock-actionbarlayer AB-11/AB-16/AB-20 + MISS-12/MISS-13 (both arbiter-original) · shell-dock-actiontoolbar AT-6/AT-7/AT-17/AT-18 (G-10 census-extension addenda)⟩

**W8.33 · G-9's census names ONE file; four records name four more, and one of them is the
in-suite EXEMPLAR** (cl.1) `AdminListSkeleton` **A-3** (the row G-9 was authored from) carries the
arbiter rider: *"G-9's census as written misses `AdminTagsPanel.vue:57-64` — five sibling
`surface`/`variant` pairs on the same primitive in the same directory … **The next conformance pass
over W8 should widen G-9's grep by one filename**; no re-spec is performed here."* Its disagreement
#2 records why it was missed: **both hostile readers called AdminTagsPanel's children "bare"** while
they carry five dead attr pairs — the file was the corpus's cited *cure exemplar*.
`PaletteCardSkeleton` **PCS-1** widens further: *"this record adds **PaletteCardSkeleton ×4 and
Markdown ×3** to the same widen note for the next conformance pass over W8; no re-spec here"* — and
supplies the byte basis: `Skeleton.vue.d.ts` = `{ class? }` only; dist-wide seam census **0/0/0/0/0**
(`skeleton-glass-bg`, `skeleton-shimmer-delay`, `skeleton-shimmer-tint`, `pulse-aura-opacity-max`,
`animate-ambient-pulse-easing`); blast radius **12 Skeleton sites / 4 files**; **48 elements × 2 = 96
dead attributes on the browse wall** (K-13); `blockVariant` (`:99`) is a dead computed feeding only
dead bindings. Provenance of the rot: `X-GU.md:61` + `N.W18.md:506` — *a ratified 4.0.0 contract,
un-swept at the 7.0.0 major*.
→ **G-9** (widen the grep to `AdminTagsPanel.vue`, `PaletteCardSkeleton.vue`, `Markdown.vue`);
CE-6/CE-8 bind the widen. ⟨AdminListSkeleton A-3 + disagreement #2 · PaletteCardSkeleton PCS-1/PCS-5 · AdminFlaggedPanel AF-X2 · AdminListItem M-12 note⟩

**W8.34 · The picker hygiene cluster: four modules, seven dead constructs, one one-line cure that
closes three rows** (cl.1 · MINOR/INFO cluster) `picker-componentsliders-consolerail`: **C-2 +
MISS-5 + MISS-13** are *"one cure with delete-on-null in both ref callbacks"* — `sliderWrapperEls` is
never pruned, so `attachSliderListeners` re-binds **five** listeners
(`pointerdown`(capture)/`touchmove`/`touchend`/`pointercancel`/`lostpointercapture`) to **every
DETACHED wrapper from every space ever visited** on each space change; both ref callbacks are typed
`any`, *"erasing exactly the null branch whose loss built both never-pruned maps"*; `listenerCleanups`
bounds the leak, the retained subtrees and dead re-bind work are real. **MISS-7**: `componentEntries`
re-derives a computed the injected model already exposes, and **the two copies disagree** (the
pipeline omits the alpha append and short-circuits hex to `[["hex",0]]`; the local copy fabricates an
untyped alpha literal and in hex mode yields rgb+alpha) — *two answers to "what channels does this
space have"*, both consumers reading only index 0.
`picker-colorcomponentdisplay`: **M-10** (`readoutReservation.ts` is a cross-boundary dependency
colocated in one consumer's private folder, **209 lines of hand-derived arithmetic with zero unit
coverage**, while the sibling `valueDomain.ts` names its own oracle — *"which is exactly why M-7
survived"*) · **M-11** (`figParts`' format core duplicated verbatim, and **the two copies feed
`readoutDecimals` DIFFERENT space identities** — they agree today only because hex short-circuits as a
string, an untyped coincidence the `space: string` prop makes invisible) · **M-15** (`READOUT_CH`,
`readoutCh`, `ComponentFormat` — zero importers; the synthesized `alpha` row unreachable;
`readoutDecimals`' docstring advertises per-(space,component) while implementing per-space) ·
**M-16** (`(e.target as any).innerText` — one of exactly two `as any` in the picker subtree;
`focus-visible:outline-none` on an `<h3>` with no tabindex).
`picker-componentsliders`: **R-24** (`valueDomain.ts` is a 3-line alias whose docblock describes a
deleted architecture **and states the opposite of the mutation contract** — promises "IN PLACE …
Returns its argument" while `clampPickerColor` maps into a fresh product; *"safe today only because
both call sites reassign"*) · **R-29** (two dead `DIGITS` constants, one exported through the
pipeline's public surface with zero consumers).
→ **G-10** + §BoundsDelta #3-9.

**W8.35 · The SpectrumCanvas `a68ecdc1` residue family, and the disposition the L-axis got
backwards** (cl.1 · MAJOR) `picker-spectrumcanvas` **L-4 (+C-17)**: residue of a canvas deleted at
`a68ecdc1` — the component's **name** (no canvas — C-14), an orphan captionless `<figure>` (D-15), a
docblock naming the deleted `useGamutOverlay` as a consumer, **3 of 4 `--gamut-*` tokens with zero
consumers + the fourth repurposed under a stale comment**, a never-reactive `ref`, a dead
`overflow: visible`. **L-1 (+M-2) — DISPOSITION REVERSED** (cl.2 cure-lock): a second whole spectrum
plate exists (`MiniColorPicker.vue`), and *"the mini is the repo's own reference implementation of
three flagship defects' cures … **Unify ON the mini's containment**; carry the flagship's luma regime
into it; **do NOT 'delete the mini's plate outright' as the L-axis prescribed**."*
**L-6 sequencing edict**: `usePointerDebug.ts:117-122` hardcodes the component's private scoped
`.spectrum-picker` class twice — *"SUBTRACTION.md W20 deletes the debug provider whole (D-17), which
deletes the walker and the cycle with it — **sequence L-6 behind D-17, don't cure it twice**."*
Also here: **L-7** (root-barrel imports of `useTouchGate` where `./dom` publishes it; the axis's
exculpatory hedge KILLED — `writeClipboard` is on the same `./dom` subpath, *"so the fourth site is a
fourth wrong edge and the picker tree can reach zero bare-barrel imports now"*), **M-11**
(`spectrumLuma.ts`: 2 of 3 exports dead; the module's self-presentation false in both numbers),
**M-6** (`DESIGN.md` locates the gamut vocabulary "in `style.css :root`" — **no `style.css` exists**),
**D-11** (X-W10 decides the out-of-gamut state; **X-W8** then deletes-or-keeps the vocabulary with the
decision). → **G-10** + §BoundsDelta #10-12 + CE-11.

**W8.36 · The clipboard non-adoption: one identity, three idioms, 26+3+1 files** (cl.1 · MAJOR)
`picker-pointerdebugoverlay` **CLIP-1**: glass-ui 7.0.0 ships `useClipboard`
(status/`CopyResult`/`no-api` branch/`resetMs`/`timeoutMs` watchdog/`onCopyError`) + `writeClipboard`;
census **3 `useClipboard` + 13 `writeClipboard` files, including the mounting parent**
(`ColorPicker.vue:138` imports it, `:325` calls it); `MixResultDisplay`'s `resetMs: 1500` is the
identical constant the overlay hand-rolls; **the overlay holds the repo's sole `execCommand`**.
*"Cure: delete `:74`/`:109-128`, adopt `useClipboard`, bind `status` to row 5's status region — closes
rows 7, 17, 25's egress limb, and half of row 5 in one edit."*
`wb-gradient-visualizer` **GV-71** (bookkeeping row, recorded so the census is not dropped at the
split): *"the three-idiom clipboard census (**26 `writeClipboard` / 3 `useClipboard` files / 1 bare
`navigator.clipboard`**) travels with X-W8's root-barrel extirpation; this pane's feedback divergence
is GV-39's"* (GV-39 re-severed INFO→MINOR: `copyCSS` awaits with no try, no status, no tick while a
sibling one file away renders a confirmation tick for the same verb — the **X-W6.b** half).
`SwatchHoverMenu` **SH-9** cross-books the bare-root `writeClipboard` reach.
→ **G-10/G-11** + §BoundsDelta #2.

**W8.37 · G-11 names six defects; the overlay's adjudication returns 41 rows, two of them BLOCKERs,
and one is a coupling LAW** (cl.2 — **the wave's largest gate under-count**)
`picker-pointerdebugoverlay` §Route of record: *"**X-W8** (CC-081 overlay limb, verified at
`W8.md:462`/G-11) for **every component cure**"* — 41 rows: 2 BLOCKER · 10 MAJOR · 18 MINOR · 11 INFO.
G-11's six (`aria-controls`, `execCommand` result, `try/finally`, both `setTimeout` handles,
`type="button"` ×3) map to rows 6, 7, 17 and part of 5 — **four of forty-one**. Missing, at minimum:
- **D-N1 (BLOCKER)** — `--z-debug` is orphaned (sole reach `:136`, **zero definitions** in demo/src;
  whole-package grep 0) ⇒ invalid-at-computed-value-time ⇒ initial `auto` ⇒ the picker card paints
  over the entire status column. A **~4.5-month regression**: added `40fa935d`, deleted `c84504d3`
  (2026-03-25) on the premise glass-ui bridges the ladder; `origin/gh-pages` (`17c10267`) still ships
  `--z-debug:99999`. **CURE (adjudicated): `z-index: var(--z-max)`** — the served 9999 ceiling; r2's
  "re-declare above `--z-modal: 140`" stays **killed as ladder-blind**.
- **D-N2 (BLOCKER)** — at 390×844 the expanded header is occluded and not hit-testable; the disclosure
  is a **one-way trap** (no Escape, no close, no outside-dismiss; only exit = reload).
- **SEQ-1 (MAJOR — a LAW, cl.2, verbatim)**: *"r2's remediation ordering is self-defeating: step 1
  shipped alone converts D-N2's fix into the very occlusion r2 refuted as D#2c … **BINDING on X-W8:
  the z-restore lands COUPLED with a small-viewport expanded-state re-form or height/position guard,
  never alone.**"*
- **D-N4 (MAJOR)** — the activation gate ships to production (see W8.9); cure = **both halves**
  (`URLSearchParams` parse incl. the hash + a build gate).
- **FRZ-1 / FRZ-2 / LEAK-1** — *"same commit — the three-defect state machine is one seam."*
- **CLIP-1 / D#5+CLIP-PRIMARY** — the lying fallback is the **PRIMARY** path on the instrument's sole
  target platform (`vite.config.ts:286` `host: true` ⇒ LAN origin ⇒ insecure context ⇒
  `navigator.clipboard` undefined ⇒ the `catch` runs on **every** copy).
- **D-N3** — cure **corrected**: `ADD pointer-events: auto` on `.debug-scroll`, **KEEP** the row-level
  `none` (LC's deletion-only edit KILLED, K-18); *"X-W8 sequences this leaf with the overlay's
  scrollport cure (both records now say so, in agreeing words)"* — the DebugEventLog side is
  **ADJ-9(d)**'s KEEP ruling, whose ground the overlay record then **strengthened**.
- **D-N5, D#7+BR-BYPASS, D#6, NAME-1, D-N6, D-N7, PE-DEAD, CLR-1, GAUGE-ORDER, GAUGE-2, STR-1,
  SEL-DEAD, FOCUS-STEAL, SCALE-PRESS, EGR-1** (*"booked as a **delta obligation** on X-W8: the close
  witnesses all three egress limbs together"*), **BTN-CONTRAST, D-N8/C#7, D-N9, GATE-ONCE, FONT-DUP,
  DEAD-BRANCH, DEAD-EXPORT, TYPE-DUP, CSS-DUP, LIFE-1**.
Out: **TEST-0** → X-W1 (may ride X-W8's cure commit as witness) · **DOC-1** → X-W10 (X-W8 rider
corrects the rows it touches) · **D#2a/D#2b** → rescoped INFO, the RTL two-line swap "may ride X-W8
incidentally; **the IDENTITY stays with the U ledger**" (`rtl-logical-property-audit.md:50`).
→ **G-11** + §BoundsDelta #1-2.

**W8.38 · G-12's latch: the derivation is right, and two folded rows plus one dead watcher arrive
with it** (cl.1) G-12 is confirmed verbatim at `shell-dock-dockviewselect` **DVS-3** (`isAdminMode` is
a **raise-only** ref: `:32` seed; `:55-59` watch with **no else**, no auth consult; `adminViews`
includes the public `atmosphere`/`blob`; consumed for gold at `:70`/`:80`) folding to Dock **L-19**,
with **R-5 governing**: *"the derivation cure kills it; **'two disagreeing predicates' stays uncitable
as mechanism**"* — the same strike W8.md:485-486 already records. **DVS-4** is the cure's shape and
its blocker: three hand-maintained enumerations disagree on "admin" (router meta on exactly 5; the
dock's 7+7 literals; the schema putting atmosphere/blob on the primary 40° fan) plus **a fourth
unguarded site** (the bare `'palettes'` literal at `:116`) — *"the schema carries no grouping field —
that missing column is why the dock re-enumerated; the derivation (one `group`/`audience` field, both
halves computed) is the cure of record"* — **but `viewSchema.ts` is X-W5's file** (§BoundsDelta #31,
DECLINE-BOUND). `shell-dock-mobilemenudropdown` **D-08** adds the dead-watcher fact **flagged to
G-12's own file**: `usePalettePorts.ts:57` destructures only `{isAuthenticated, login}` — **`logout` is
dropped**, tree-wide census definition+export, **zero call sites** — so `adminToken` only ever goes
false→true and `useDockAdminMode.ts:62-64`'s "Exit admin mode on logout" watcher is
**unreachable by construction**; disposition: *"the wire-up decision alters the port surface R-6 froze,
so it is a **formation-boundary decision, not a silent rider**."*
→ **G-12** (add: after the derivation, the logout watcher must be reachable or deleted — not left
unreachable-but-present). ⟨shell-dock-dockviewselect DVS-3/DVS-4 · Dock L-19 + R-5 · shell-dock-mobilemenudropdown D-08⟩

**W8.39 · G-13 is named verbatim by its own component record, and the repair is bounded from four
directions** (cl.2 — **design inputs the gate must absorb, not a re-spec**)
`shell-dock-profilesection` **C-5/L-1(a)** (BLOCKER): *"**X-W8** — G-13 names this row verbatim, with
the type-level cure (the static `chrome` rung becomes unrepresentable) and the CC-107 bank for the
probe's retirement."* The four bounds on the repair, all reader-found and arbiter-verified:
- **N-12** — `--glass-bg-dock` is the **ONE** rung token carrying the tint term; `chrome:
  "--glass-bg-dock"` in `TIER_BG_TOKEN` + deleting the selector branch is *"the cheapest correct-
  DIRECTION repair … bounded by N-7"*.
- **N-7** — the band's paint is a **FUNCTION OF DOCK STATE** (`--dock-expand-t` in `shell.css`;
  `[data-held]` re-paint in `morph.css`) and the tint cache has no invalidation for any of it —
  *"which bounds every single-shot probe cure, **including C-5's own**."*
- **A-3** — the repaired referent inherits **per-PICK** staleness: the band recipe mixes
  `var(--glass-tint-source)` and `foundation.css:253` pins it to `--accent-live` (the live picked
  colour) while `liveTintCache`'s key is (surface, darkClass, epoch) with **no colour term** ⇒ correct
  for exactly one pick, then stale, invisible to any existing test. *"**G-13's cure MUST be an
  invalidation signal or a producer-published value — a better selector is insufficient** even before
  dock-state dependence."*
- **N-6** — the `floating`/`resting`/`well` probes read **UNTINTED** rung tokens while the file's own
  contract comment promises "tint knob and all": *"**G-13's repair must not stop at `chrome`.**"*
Adjacent, same instrument: **N-8** (`liveTintCache` keyed by `InkSurface` alone with a first-match
`querySelector('[data-surface="veil"]')` host — *directly contradicting the 12-line subtree-locality
argument the same file makes for the veil*), **N-9** (`resolveCssColorAlpha` fabricates a confident
**opaque black** for an unparseable colour while every sibling failure path degrades honestly),
**N-10** (a hidden probe `div` appended to `document.body` with no removal path, a memoized canvas
context, two more unowned module-global maps — *surviving unmount and HMR, littering every jsdom
suite*), **A-4** (`certifyAccentInk` has a **split error posture on one render path**: `ink.ts:136`
returns the RAW UNCERTIFIED input on parse failure while `:59/:65/:78` throw — both inside
`computed(() => chromeSafeCss(cssColorOpaque))`), **C-1** (the tint cache's validity key is the DOM
effect read at a moment only the reactive cause has changed; the producer already ships
`onFlipSettled`, unconsumed), **L-2** (Dock injects resting-certified ink and passes it down a prop
named `cssColorOpaque`; ProfileSection certifies the already-certified — *two owners in series, the
name asserting the opposite*; corroborated at `shell-dock-mobilemenudropdown` **L-6/D-16**:
*"Monotone-walk composition can only over-correct"*).
`wb-extract-controls` adds two addendum candidates: **EC-12** (the parameter named `floor` is not the
floor — `certify(accent, surfaceL, floor + CERTIFY_HEADROOM)` ships a documented 3:1 floor as a
**4.25:1** walk, default text path **5.75**, against a doc-comment promising a plain override) and
**EC-29** (the WCAG floor constants have **four homes, two identically named in sibling files**) —
*"→ X-W8 (addendum candidate — `ink.ts` is already in W8's G-13 surface)"*.
→ **G-13** + §BoundsDelta #15.

**W8.40 · ProfileSection's dead surface, cascade-dead bindings, and the register decision that is
NOT this wave's** (cl.1) **D-4**: the `borderColor: triggerInk` binding is **cascade-dead** on both
triggers (`.button { border: 0 }`; tone `neutral` never re-adds it) — *three lines below the comment
retiring exactly this class of dead accent*, while the same idiom is LIVE on `.slug-pill`.
**D-11**: `font-display` on menu content is dead at all three sites, **cascade-certain** (the
producer's unlayered `.dropdown-menu-content` font rule beats any layered utility). **C-11/L-7**: two
`useSafeAccentFn` calls bump the **module-global** cache key every ink consumer shares;
`inject(SESSION_PORT_KEY)!` renders a missing provider as a TypeError *despite the sibling docblock's
"surfaced loudly" promise*. **C-10**: three dialects of one design system in one 44-line script block.
**L-8**: MT-F009 re-scoped to its single surviving instance (**10 invocations / 9 files**; the "21" was
import-specifier conflation, K-10). **C-2/D-3** splits: dead props → **X-W8**; *the `emphasis` register
decision → **X-W10**, with LC's caveat that `quiet`'s HOVER re-introduces a wash* (CE-5).
→ **G-10/G-9** + §BoundsDelta #15.

**W8.41 · `GradientPane.vue:8`'s dead inject is named by line, and its family is four postures for
two keys** (cl.1) `wb-gradient-pane` **C-1/D-3/L-1**: *"ADJUDICATED MINOR → X-W8 (already booked:
X.W8.d gate G-10 names `GradientPane.vue:8` by line; **sequenced after X-W6 per W8's cross-wave
clause**)"* — `inject(CSS_COLOR_KEY)!` assigns `cssColorOpaque`, never read, in a 29-line file;
provider exists, so **zero runtime symptom**. *Why gates missed it*: `@typescript-eslint/no-unused-
vars` off (`eslint.config.js:71`), `vue/no-unused-vars` off (`:82`), and vue-tsc cannot flag
script-setup top-level bindings. **M-GP-8** generalises: four contradictory injection postures for two
keys in one subtree — GradientPane `:8` asserted-then-unused; GeneratePane `:11`
`inject(LIBRARY_PORT_KEY)!` asserted **then dereferenced unguarded** (the one latent TypeError site,
cross-cited to wb-generate-pane, **not booked here**); GradientVisualizer `:30` unasserted + guarded
(correct); ExtractWorkbench `:218` explicit default (correct). **M-GP-9** (INFO, carve rider):
`mx-auto` is a no-op on a `w-full` unconstrained wrapper — fleet idiom, *"not worth a standalone
repair"*. GeneratePane `:10`'s twin dead inject is the second copy (`wb-gradient-pane` verified) —
**but no X wave holds `GeneratePane.vue`** (`wb-generate-pane` §closing: the whole GEN-CLUSTER is
unowned). → **G-10**.

**W8.42 · The Extract/session dead-surface family: ~40 lines of dead public API including a SECOND
camera implementation** (cl.1 · MAJOR) `wb-extract-pane` **XP-13** (reader-found, verified exactly):
`quantizeFromCanvas` (`:110-113`), `quantizeFromCamera` (`:115-146`), `ImageQuantizeOptions.
workerFactory` (`:35-41`) have **zero consumers**; `quantizeFromCamera` opens its own `getUserMedia`
with the **byte-identical constraints object** of `ExtractWorkbench.vue:242-248` — *a second,
unreachable camera-permission code path* in a subtree whose own header advertises the T20 collapse;
`workerFactory`'s docstring *"names tests that do not exist"*. → **X-W8 G-10 extension**
(census-extension addendum, *"the shell-dock precedent"*). Cross-booked, never re-minted:
`wb-extract-workbench` **XW-11** (*"the dead composable copy already returns the `stop()` disposer
whose absence is XW-9"*), `wb-extract-imageeyedropper` **EY-45**, `wb-extract-controls` (identity
admitted, four reader-misses folded). Same family: **EC-39** (*4 of NINETEEN session members unread —
`palette`, `lastFile`, `paletteName`, `totalPopulation`; `palette` is an escape hatch around the
composable's own presentation contract*; the reader's "21" corrected to 19), **XW-36**
(`<Transition name="vj-enter" @after-enter>` with no `appear` over a child with no `v-if` ⇒ enter hooks
never fire ⇒ `onTransitionEnd`'s refit safety net is dead code; the `:2` comment documents motion that
does not exist; + **8 unread exports across 4 one-consumer composables**), **XP-11**/**XW-30**/**EC-31**
(both glass-ui spellings on adjacent lines — `ExtractControls.vue:98-99`).
→ **G-10**.

### 6e. X.W8.e — the LoC cap ruling (→ G-14)

**W8.43 · DR-17/DR-18 and the `W.W8` naming collision** (cl.1) `library-band` **RD-9**: *"DR-18's
home — the stylesheet split. F leaves the split with **W.W8** (the registry home) … half stays with
W.W8"*, and its closing: *"DR-17 … the five demo breaches handed to the demo band by name; DR-18 FOLD
with a ruled home."* **Correction (dated, this fold)**: `W.W8` is the **W-tranche** registry home, not
`X-W8`; W8.md:199-200 already scopes X.W8.e to *"DR-17 (demo ≤400) and DR-18's **measured-cap half**
(CC-090; the `src` **split** stays X-W9's)"*. No routing change; the near-homonym is recorded so a
close does not read `W.W8` as this wave. `ConfigSliderPane` §10 supplies the wave's own governing
lesson for G-14: *"**AN ADVERSE ORACLE IS A LOCK.** Enumerate the gates asserting a surface before
curing it; a gate born green against the defect's own remedy is re-aimed in the same cut, never
preserved — preserving it is how the T.W8 cure produced D-3/D-4."*
→ **G-14** (the ratchet, if wired, is exactly such an oracle — W8.md:385-387's scope guard already
anticipates it; ConfigSliderPane's lesson is its named precedent).

### 6f. Declines, collisions and named residues (cl.4)

**W8.44 · The MT-DOCK-LAYERS-1 decline, enumerated** — see **CE-1**. Rows declined by W8.md:114-116
but routed to "X-W8" by their records, listed so none is read as landed: `shell-dock-dockviewselect`
DVS-1 (BLOCKER, NEW arm), DVS-2 (BLOCKER → DELTA-4), DVS-6, DVS-8, DVS-9 (NEW), DVS-10, DVS-11,
DVS-15..DVS-19, DVS-23..DVS-25, DVS-27, DVS-29 + the **G-D gold-probe amendment** (*"after this cure
that probe arm must key on `.gold-shimmer-icon` alone"*) + the **o14 gate-flip** (*"the suite
currently PINS this defect and G-N must flip that assert, not merely add one"*);
`shell-dock-actionbartoggle` ABT-1 (BLOCKER), ABT-3 (BLOCKER), ABT-4, ABT-6, ABT-7, ABT-14 (**the one
exception — it is the Dock SCC edge, so it lands via G-7**), ABT-16 (separators limb), ABT-17 (D-11
limb); `shell-dock-mobilemenudropdown` L-1, L-6/D-16, C-3/L-4/D-18, D-04/L-5/C-6;
`shell-panesegmentedcontrol` PSC-5/PSC-9 (cross-cites only — the rows themselves are X-W5.c's);
`shell-dock-actionbutton` (two standing fold identities only). **Nothing here is killed**; each keeps
its id and its home (MT-DOCK-LAYERS-1) at the formation boundary.

**W8.45 · Phantom owners this fold NAMES, beyond W-HYGIENE** (cl.4) `AboutPane` rider **A-1** (the
class's origin): *"(a) the About recomposition's V-era owner (OBC W18 / PR-14); (b) **MT-CSS-2**;
(c) **W-HYGIENE**. Same class as AdminListSkeleton's MT-AU1 finding, now three names wide."* This
fold's sweep adds: **MT-BTN1** (the dead-`variant` fleet — W8.14), **MT-UI1** (aliased to W-HYGIENE's
H-a, which X-W8 **does** own — recorded so it is not double-counted as phantom), **MT-AU1**
(`AdminListSkeleton` §A-1/A-2/A-5, `AdminPane`, `AdminUsersPanel` — *"MT-AU1 appears in no
CARRY-CUT-LEDGER row and no X wave"*), **MT-CSP-1** (`BlobPane` formation flag: *"no CC row folds it …
`ConfigSliderPane.vue` appears in **no** X wave's file-ownership table"*), **MT-COLORINPUT-1**
(named as owner by ActionBarLayer/ColorInput; not an X wave), **MT-F031** (`ApiOfflineChip` AP-1),
**W-LIB** (`ColorInput`/`ColorPicker` L-1), **W-TOOLCHAIN-ONE-ARTEFACT** (`GradientStopEditor` L-8),
**W-GRADSTOP-2/-3**, **W23**, **W47**, **W18 §3.1**. **Ruled: X-W8 owns none of them** and its close
may not absorb them; they are the formation boundary's intake, with `AboutPane` A-1's own instruction
binding — *"The next formation boundary consumes this list with the original ids."*

**W8.46 · Named residues that sit one hop from this wave and must not be silently absorbed** (cl.4)
`ErrorBoundary` **EB-27** (the `demo/app` rename — *"named residue; nearest X-W8's graph
subtraction"*) · `AuroraPane` **AP-19** (the aurora concept has no home; *"No X wave mints
`demo/atmosphere/` — X-W8 is subtraction-only for `demo/ui`"*; interim guard: **no new edge in either
direction**) · `PaletteColorStrip` **PCS-9** (the area-seam *construction*) · `AdminFlaggedPanel`
**AF-40** (PaginationBar + AdminListSkeleton homed in `admin/`, *"must not ride X-W8's null-DELTA
skeleton edit"*) · `AdminListItem` **D-5** (`AdminListItem.vue` sits in **no** tranche-X File Bounds —
verified across W0-W11 — so the separator cure is *reachable but unclaimed*) · `MigratePalettesDialog`
**A-18** (*"barrel deletion is an X-W8-shaped rider the formation should book beside W8's graph
subtraction"*) · `PaginationBar` **M3** (*"adjacent to but NOT inside X-W8's same-touch, whose G-9
null-DELTA contract a behavior change would breach"*) · `AdminAuditPanel` **AAP-33** (*"doc-drift arm
**partially** discharged by X-W8"*) · `wb-extract-controls`/`wb-extract-workbench`/`wb-mix-*`
NO-WAVE-OWNER clusters (11 pin-gated edits to a file no X wave owns; the extract-session cluster; the
camera-mode cluster; the MX-CLUSTER).

**W8.47 · One record's routing is corrected against its own text** (cl.1 — dated correction)
`AboutPane` **AB-25**: `Markdown.vue:37-38` runtime-imports the shell's global stylesheets into the
lazy About chunk, duplicating the App-root pair, beside its own correct `@reference` (`:79`).
Disposition as filed: *"X-W8 rider — `Markdown.vue` is in W8's modify-carve bounds; **the two lines
die in the carve (flagged, no W8 gate names them)**."* **Ruled here**: a line that dies in a carve
with **no gate naming it** dies by luck. This fold books it as an explicit **G-10 census row**
(dead/duplicated runtime import, 2 lines) so the carve's close can assert it. Markdown-adj measured
the denominator: *global-CSS runtime imports = exactly 4 lines* (App.vue:199-200 root + Markdown.vue
:37-38).

**W8.48 · `AboutPane` AB-34/AB-38/AB-39 — the types-module carve, with one rider that binds
X-W6.f** (cl.2) **AB-39** ≡ G-7's Markdown SCC row, *"explicitly owned (W8:289)"*. **AB-38**:
`DocItem` is a dead export — 1 declaration, 0 consumers — *"dies in the types-module carve"*.
**AB-34** (reader-2 miss, ADOPTED): *"The widest type hole is the barrel: `DocModule = () =>
Promise<{ default: any }>` (`markdown/index.ts:3`) **erases the whole render path invisibly**; honest
closure `any`/cast census ≥7."* **Rider (cl.2, verbatim)**: *"X-W8 (the DocModule/DocItem types-module
carve at W8:174) **with an X-W6.f rider — the catalog's `doc:` field must consume a retyped module,
`{ default: Component }`, or the carve moves an `any`.**"* → **G-7** acceptance criterion.

**W8.49 · The `demo/ui` scope law, stated once so no docket re-grades it** (cl.1)
`ColorSpaceSelector` addendum item 1, applied at seven records: *the verdict label follows where the
count lives.* `picker-componentsliders` R-L2/D-13 (*"MINOR at this component's scope; the repo-wide
identity is X-W8's"*), `PaginationBar` D-5/L-1 (*"the count lives repo-wide … the fleet row is already
claimed"*), `wb-generate-pane` §ruling (*"2 of 90+ edges live here; repo identity rides X-W8 (the
scope law)"*), `PreviewStrip` L2-2 (*"the AdminTagsPanel precedent (grade lives at the identity's home)
**forbids re-grading it MAJOR on a sibling docket**"*), `MiniColorPicker` MCP-38, `EmptyState` ES-17,
`AboutPane` AB-23 (*"component-scoped observation of a repo-scoped identity"*).
→ **G-1/G-3**: the census is the grade's home; per-file severities in the corpus are observations.

**W8.50 · What this wave provably does NOT cure, stated so the close cannot over-claim** (cl.2)
(a) the dead-`variant` **register** decision — X-W10 (CE-5); (b) skeleton **anatomy** parity — CE-6;
(c) `tsconfig` parity — W8.7; (d) `MT-DOCK-LAYERS-1`'s non-subtractive arms — CE-1; (e) `--slider-
track-bg` — CE-4; (f) `foundation.css` anything — CE-3; (g) the extract-session / MX / GEN clusters —
W8.46; (h) `regenerate()`'s register-then-swap — W8.24; (i) the About recomposition, `/about`
addressability, MT-CSS-2 — W8.45; (j) the fleet clipboard **feedback** halves (GV-39's X-W6.b arm).
Each is a named identity with a named non-owner; a close that lists any of them is false.

**W8.51 · Two superlatives the wave's subtraction must not delete** (cl.2 — preservation riders)
(a) `picker-debugeventlog` **ADJ-9(d) KEEP ruling**, strengthened by the overlay record:
`.debug-event`'s two `pointer-events: none` declarations (`:61-67`) are no-ops **today** but are the
designated landing mechanism for D-N3's corrected cure — *"Deleting `:61-67` as 'dead code' before
D-N3 lands would re-open the touch-stealing hole the file exists to prevent."* The overlay's
correction refines which copy dies: `:65-67` (`.debug-log`) duplicates its own parent's `:61-63`
through inheritance and is *"unconditionally dead under every ancestor configuration"* — deletable in
the same touch; **`:61-63` must survive as the row-level guarantee.**
(b) `picker-debugeventlog` **ADJ-8**: the a11y row *"closes by **declining**, not inventing"* — adopt
log semantics, **explicitly DECLINE any live-region announcement strategy** for the event feed (both
readers concur: a feed that can burst during a drag is AT spam). The overlay's **D-N5** keeps live
regions for **the two discrete chips only**. A subtraction wave that "improves" the feed here would
manufacture the defect both records declined.

---

## 7. §Gates — born-RED candidates, real witnesses, and which existing W8 gate they sharpen

**L-19 posture**: every entry names a command or a byte citation with a falsifier. No `proof:*`
script; no gate-farm; the four probe arms of W8.md stay the minimum, not a suite.

### 7a. Sharpenings of EXISTING gates (15)

| gate | sharpening | witness (born-RED today) | rows |
|---|---|---|---|
| **G-1** | census must additionally book (i) `90` vs `92` sites with the loser SUPERSEDED-BY-MEASUREMENT; (ii) the six-file remainder incl. `demo/color-session` strays; (iii) the dead-`variant` fleet with all five prior readings; (iv) the 13+1+5 family→subpath table as committed output | 3 seats measure 90/48 (Markdown-adj, ColorSpaceSelector, worker-O); AdminUsersPanel measures 48 files / 24 both-path with the ledger's 35 REFUTED; five `variant` censuses disagree by 2× | W8.2, W8.14, W8.15, W8.4 |
| **G-2** | zero-importer barrels (`label`, `switch`) DELETE with **no** repoint; `browser/index.ts`'s 46-line seam is the same class | `ls demo/ui` = 19 dirs / 0 `.vue`; `label`+`switch` importers = 0 (EB-17, A-16); `from ".../browser"` importers = 0 (SP-10, SFB-38, UserSortMenu) | W8.3, W8.26 |
| **G-3** | narrowness clause must not manufacture an import for a zero-consumer symbol; commit body may state ownership/graph/surface cost, **never shipped bytes** | 15,665 B byte-identical both ways (EB-17, U-9); AdminUsersPanel: "only `npm run gh-pages` may ever decide it" | W8.3, W8.18, W8.19 |
| **G-4** | extend to `@types/katex`, the `@src` alias + `sourceExportPlugin` + its false comment, `vite-defer-glass-fonts.ts:72`'s dead guard, the `.gitignore`/`vitest.config.ts` `test/dist` pair | `@src\|?source` in `assets/`+`demo/` = 0 (three seats); `demo/@/styles/style.css` absent (marker at `foundation.css:75`); `test/dist` deleted at `6d6d3521` | W8.22, W8.23 |
| **G-5** | RED cell hardens: `cn`'s deletion condition is **DISCHARGED** (1 occurrence repo-wide = its definition); the `utils.ts:16-17` doctrine sentence dies **by deletion**, and `grep debounce src/` = 0 corroborates | A-25 census; PH-32; XP-22 | W8.16 |
| **G-6** | ban must (i) carry Dock DELTA-3's **≥1-matching-file meta-check**; (ii) cover `demo/palettes/**` (AF-13's live re-point); (iii) FIRE on the two live counter-witnesses | `CurrentPaletteEditor.vue:193` raw `.vue` across the seam; `AdminFlaggedPanel:142` raw cross-area `.vue`; `demo/palettes/**` in no glob (PP-18) | W8.5, W8.6, W8.13 |
| **G-7** | oracle denominator must include (i) the feature→boot cycle through `App.vue:164`; (ii) the `assets/docs` ↔ scene-katex content cycle; (iii) the shell→palettes `SESSION_PORT_KEY` lattice; the acceptance criterion gains the **keys-module** clause and AB-34's retyped-`DocModule` rider | 3 upward edges verified (EB-14); 11/11 docs import the scene leaf (Katex R11); 5 value edges + 1 type back-edge (paneslot L-5); `DocModule = () => Promise<{default: any}>` | W8.20, W8.21, W8.24, W8.28, W8.48 |
| **G-8** | carry-table gains MISS-7 (nested crossfade clip-path), AB-33 (the hybrid `gap: 8px 6px` + missing `min-width:0`), AB-5 (`.dock-layer-grid` = 0 rules), AB-34 (session-ratcheting reserve), AB-35 (200 ms glyphless seat); price restated per ruling 2 | `.dock-layer` transitions **only** `visibility 0s`; `.dock-layer-grid` = 1 template hit / 0 CSS rules; `useLayerTransition` ABSENT from installed dist; `DockCrossfade` exported at `dock/index.d.ts:5` | W8.31 |
| **G-9** | grep widens by three filenames (`AdminTagsPanel.vue`, `PaletteCardSkeleton.vue`, `Markdown.vue`); **non-citability rider** attaches (green G-9 ≠ D-5/US-6 cured); anatomy and `iconOnly` deltas may not ride | AdminTagsPanel `:57-64` five dead pairs; PCS-1's 12 sites/4 files + 96 dead attrs; dist seam census 0/0/0/0/0 | W8.33, CE-5, CE-6, CE-8 |
| **G-10** | enumeration grows by ≥8 named sites (ActionBarLayer `:96`/`:83`; ActionButton `label`/`hidden`/`.action-label`; `pointer-events-auto`; AB-20's provide) + the picker cluster + Extract's ~40 dead lines + PaletteSlugBar's whole file + `browser/index.ts` + Markdown's 2 duplicated CSS imports | each cited line present at HEAD per its record; `clearHover` callers = 0; `quantizeFromCamera` consumers = 0; `<PaletteSlugBar` templates = 0 | W8.25, W8.26, W8.32, W8.34, W8.42, W8.47 |
| **G-11** | **the largest under-count**: 6 of 41 rows. Must absorb D-N1, D-N2, D-N4, FRZ-1/FRZ-2/LEAK-1, CLIP-1, D-N3's **corrected** cure, EGR-1's three-limb delta obligation — under **SEQ-1**'s coupling law | `--z-debug` definitions = 0 (whole-package grep); `origin/gh-pages@17c10267` still ships `--z-debug:99999`; `?nodebug=1` matches at index 2; `activePointers` has no `.clear()` anywhere | W8.37 |
| **G-12** | after the derivation, the logout watcher must be **reachable or deleted**; the three-enumeration cure needs `viewSchema.ts`'s `group` field (X-W5's file) | `logout` call sites = 0 tree-wide; `adminToken` false→true only; router `beforeEach` = 0 | W8.38 |
| **G-13** | absorb N-6 (don't stop at `chrome`), N-7 (state-dependent paint), A-3 (per-pick staleness ⇒ **invalidation signal or producer-published value, never a better selector**), N-12 (`--glass-bg-dock` is the one tinted rung); ProfileSection.vue enters bounds | no dist rule paints `.glass-dock`'s background; `liveTintCache` key = (surface, darkClass, epoch), no colour term; `foundation.css:253` pins `--glass-tint-source: var(--accent-live)` | W8.39 |
| **G-14** | ConfigSliderPane §10's adverse-oracle law is the named precedent for the ratchet decision | measured breaches: App.vue 417 · ColorPicker.vue 414 · Markdown.vue 408 · useAtmosphere.ts 406 | W8.43 |
| **G-15** | preservation gains three witness riders it does not own: TEST-0's spec may ride the cure commit; M-21's `.plate-caption` oracle is structurally RED in a live smoke project; AB-9's phantom test citation | `grep plate-caption demo/` = 4 prose comments, 0 markup/CSS, while `o18:648-690` asserts visibility; `ls test/parsing/` = `timeline` only | CE-11 |

### 7b. GENUINELY NEW gates (6)

**N-1 — BOUNDS COMPLETENESS (the packet-2 gate).** Every file cited by a RED cell of G-1..G-15, or
by a row this addendum folds, appears in W8.md's File Bounds **or** in §3 of this addendum **or** is
recorded UNEXECUTED-BY-BOUNDS in `W8-CLOSE.md` with its owner named.
*RED today*: 31 files in §3, of which ≥12 are cited by rows the wave's own gates claim
(`DebugEventLog.vue`, `usePointerDebug.ts`, `ProfileSection.vue` — G-13's *sole consumer* — and nine
more). *Falsifier*: `for f in $(§3 list); do grep -q "$f" docs/tranches/X/waves/W8.md || echo MISSING
"$f"; done` returns non-empty at authoring and empty at close.

**N-2 — THE W-HYGIENE TRI-LIMB DECLARATION.** `W8-CLOSE.md` states, per limb, exactly one of
OWNED-AND-LANDED / DECLINED-WITH-NAMED-HOME: **H-a** demo/ui (OWNED, G-2/G-3) · **H-b** dead globs
(OWNED, G-4/G-6) · **H-c** tsconfig parity (DECLINED, W8.md:112, home = formation boundary, cure =
DELETE per Dock R-3). *RED today*: no such declaration exists and 46 records name one undifferentiated
"W-HYGIENE". *Falsifier*: a close asserting "W-HYGIENE landed" without the three-cell table is false
by one limb.

**N-3 — THE MT-DOCK-LAYERS-1 DECLINE CENSUS.** The close enumerates, by original id, every row a
value.js record routed to "X-W8" that W8.md:114-116 declines, and marks each
DECLINED-IDENTITY-PRESERVED. *RED today*: ≥30 such rows across five dock records, two of them
BLOCKERs (DVS-1, ABT-1/ABT-3). *Falsifier*: a row that appears in neither this wave's dispositions nor
the decline census is a **silently dropped adjudicated row** — M-25's exact failure.

**N-4 — SEQ-1 COUPLING (elevated from a row to a gate).** The `--z-debug` restore does not appear in
any commit that does not also contain a small-viewport expanded-state re-form or an explicit
height/position guard. *RED today*: no cure exists; the mechanism is source-certain
(`:133-141` fixed bottom-left, 280 px, ≤35 dvh, 92 % black over `.channel-rows`). *Falsifier*: land
the rung alone at 390×844 and the expanded panel occludes the instrument's own subject — the exact
occlusion r2 refuted as D#2c.

**N-5 — DELETION PRESERVES THE MAP.** The barrel-deletion commit carries the family→subpath table and
the five root-barrel-only families (`alert avatar checkbox radio-group skeleton`) as committed output,
with the standing BH/BI relay ask recorded and **not** treated as a precondition. *RED today*: the
table exists only inside W8.md's prose and the barrels are its only machine-readable form (PCS-21).
*Falsifier*: delete the barrels without the table and the next wave cannot tell which subpath a
primitive lives on.

**N-6 — THE PRESERVATION PAIR.** `.debug-event`'s `:61-63` row-level `pointer-events: none` survives
the wave (only `:65-67` may die), and no live-region announcement strategy is added to the event feed.
*RED today*: both are correct at HEAD; the gate exists because the wave is a **subtraction** wave and
both read as dead code to a mechanical sweep. *Falsifier*: `grep -c 'pointer-events' DebugEventLog.vue`
< 1 at close, or any `aria-live`/`role="log"` announcement on the feed, reopens D-N3 / ADJ-8's declined
class.

---

## 8. Closing — status verbs

| verb | state | evidence |
|---|---|---|
| AUDITED | YES | this addendum; 64 records swept, 455 routed line-instances, 61 slugs read whole; **re-audited 2026-08-28 (§9): 271 routed row lines / 56 records, id-keyed** |
| SPECIFIED | YES — by `waves/W8.md` PLUS this addendum | W8.md untouched (E-3); §3 is census evidence, not a bounds patch |
| IMPLEMENTED | NO | — |
| VERIFIED | NO | — |

**51 rows · 21 gate entries (15 sharpenings + 6 new) · 31 BoundsDelta files · 11 cross-edges.**
*(SUPERSEDED-BY-MEASUREMENT 2026-08-28 — see §9.7's dated arithmetic: **69 rows · 24 gate entries**.
The line above is the pass-1 reading, kept so the correction is legible, never silently overwritten.)*
Status stays **planned**; nothing here opens product source; every disposition is ADJUDICATED, not
VERIFIED. The dated wave specs and every banked adjudication remain byte-intact beside this file.

---

## 9. FOLD-ROUND-1 REPAIR ADDENDUM — 2026-08-28 (the row-carriage repair)

**What this section is.** The X-W8 repair seat's answer to `PASS-1-CHECK-RETURN.json` (BLOCKER:
189 X-W-routed ⟨record·id⟩ rows absent from the fold layer; MAJOR 2: 73 NO-WAVE-OWNER row identities
uncarried) under the spine's **ROW-CARRIAGE LAW** — *a packet's home wave adopts the packet's riding
ROWS id-for-id, not merely its files.* Work order = `refinement/ROUND-1-ASSIGNMENTS.md`
(§A ×4 NWO · §B ×4 census · §C ×1 external = 9 assigned). **The roster is a floor, not a ceiling**:
this seat re-ran its own id-keyed census fresh over the whole 92-record corpus (§9.1) and folds
**nine further escapes the roster did not carry**, so no round-2 check re-discovers them.

**Posture unchanged.** Status `planned`. No product source opened — every witness below is a corpus
byte, a mail byte, or a re-measurement already banked by a named seat. Dispositions ADJUDICATED,
never VERIFIED. E-3 holds: `waves/W0.md..W11.md` and `CONFORMANCE-2026-08-03.md` stay byte-untouched;
this file remains this seat's sole write. **Banked ids are for life** — every id below is the id its
record minted; where a roster id is a facet of an already-folded identity the cure is an explicit
**≡-pointer row**, never a silent skip, so the byte census closes either way.

---

### 9.1 The fresh id-keyed census (falsifiable, re-runnable, id-keyed)

**Method.** Corpus = the 92 non-`kf-*`/`fr-*`/`pt-*` `*.md` at
`docs/tranches/V/megatranche/registry/adjudicated/` (non-recursive). Row = a bold-leading id line
(`- **ID` / `| **ID`). X-W8-routed row = such a line carrying an `X[-‑–—·.]W8(?![0-9])` token (all
hyphen, en-dash, middot and dotted forms counted; the negative lookahead is why no X-W8x wave can
alias in — X-W8 has no longer sibling, unlike X-W1/X-W10). ESCAPED = the id, as literal bytes
(zero-padded ids are their own identity: `D-06` ≠ `D-6`), boundary-matches nowhere in the fold layer
**after expanding range (`DVS-15..DVS-19`) and slash-compressed (`EY-7/27/32`) forms**, and appears
in no exclusion register.

| measurement | reading | note |
|---|---|---|
| X-W8-routed **row lines** | **271** across **56** records | the row-keyed unit; this is the census denominator |
| …of which carry an explicit routing verb (the check's verb set) | **134** | **both figures stated, neither silently elected** — the verb filter is the check's, the token filter is this fold's; the wider reading is the one this seat repairs against |
| X-W8 **line-instances** (any line, not only row lines) | **455** across **64** records | §0's IN-side receipt — **reproduces exactly**, different unit, not a contradiction |
| absent from **this fold** by bytes (post-expansion) | **13** | the repair frontier, §9.3 |
| absent from the **whole 13-file fold layer** | **7** | AL-X1 · PP-25 · US-9 · US-11 · US-12 · MISS-9(ActionBarLayer) · Copy-CSS |
| NWO rows escaping in X-W8-homed records | **4** | DSL-9 · DSL-24 · DSL-28 · N-11 — reproduces §A id-for-id, zero drift |
| block-grain candidates (token in the continuation block, not the row line) | **1, EXCLUDED** | ⟨MiniColorPicker.md · MCP-44⟩ — its own cell routes **X-W7 rider**; the X-W8 token belongs to ⟨MiniColorPicker.md · MCP-38⟩ at :80, already carried at W8.1's provenance. Recorded because a silent exclusion is indistinguishable from a miss |
| unbolded-table supplement (`\| ID \|` rows without `**`) | **4 rows, 0 absent** | the ceiling test: widening the row grammar beyond the check's adds **nothing** to the frontier |

**RANGE-EXPANSION RECEIPT (defensive, so a non-expanding round-2 check also closes).** Six ids that
a naive byte grep will report missing are in fact carried by explicit ranges already in this file:
**DVS-16 · DVS-17 · DVS-18** (by `DVS-15..DVS-19`, :370 and :1058) · **DVS-24** (by
`DVS-23..DVS-29`/`DVS-23..DVS-25`, :370/:1058) · **ADJ-11 · ADJ-12** (by
`⟨picker-debugeventlog ADJ-1..ADJ-13⟩`, :269). Enumerated here as literal bytes — **no new booking**,
the ranges remain the carriers and W8.44's decline census remains their disposition.

**Commands (L-19 — measurements over shipped bytes, no `proof:*` script).**
```
# denominator: X-W8-routed row lines / records
grep -nE '^[[:space:]]*([-*]|\|)[[:space:]]*\*\*[A-Za-z]' \
  docs/tranches/V/megatranche/registry/adjudicated/*.md \
  | grep -v '/\(kf\|fr\|pt\)-' | grep -E 'X[-‑–—·.]W8([^0-9]|$)' | wc -l      # -> 271
# line-instance receipt (the §0 unit, unchanged)
grep -lE 'X[-‑–—·.]W8([^0-9]|$)' docs/tranches/V/megatranche/registry/adjudicated/*.md \
  | grep -v '/\(kf\|fr\|pt\)-' | wc -l                                        # -> 64
# NWO escapes in the two X-W8-homed records
grep -n 'NO-WAVE-OWNER' docs/tranches/V/megatranche/registry/adjudicated/shell-dock-dockstatuslamp.md \
  docs/tranches/V/megatranche/registry/adjudicated/shell-dock-profilesection.md   # -> DSL-9/24/28, N-11 among them
```

---

### 9.2 §A — the four NO-WAVE-OWNER row identities homed here, adopted id-for-id

The spine fixes ownership at X-W8 because `DockStatusLamp.vue` + `status-lamp.ts` (packet 19 ·
DEBUG/STATUS) and `ProfileSection.vue` (packet 10 split) ride this wave's bounds delta (§3 rows 19,
20, 15). **A NO-WAVE-OWNER mark is not a kill and adoption here does not re-mint the id.**

**W8.52 · DSL-9 — the config-parity limb, adopted as an ≡-pointer to H-c, NOT as a new booking**
(cl.4 · ≡)
Verbatim disposition: *"**≡ W-HYGIENE config parity (ColorSpaceSelector L-14; cure = DELETE per Dock
R-3) → NO-WAVE-OWNER, identity preserved.**"* Facts carried: 3 dead `paths` targets + 2 unentried
shipped subpaths + no `.` + the unconsulted stale `node_modules` directory, *"INFO-grade facts at
MINOR ledger weight per the six-confirmation identity."*
**≡ W8.7 (H-c)** — the limb X-W8 **declines** at W8.md:112, home = formation boundary, cure
SUPERSEDED to DELETE. Adopting the row here carries the identity; it does **not** reverse the
decline, and **N-2**'s tri-limb declaration is the instrument that keeps the two facts apart.
⟨shell-dock-dockstatuslamp.md · DSL-9, :48⟩ ≡ ⟨ColorSpaceSelector.md · L-14⟩ ≡ ⟨Dock.md · L-22⟩

**W8.53 · DSL-24 — the dock band sits OUTSIDE the sole ErrorBoundary; a band setup throw is an
unprotected white screen** (cl.1 · MAJOR-grade, new to this wave's gate surface)
Verbatim: *"The dock band sits outside the sole ErrorBoundary (my App.vue reads); any band setup
throw (incl. `useApiClient()`'s, :54-62) is an unprotected white screen against the repo's own U-F58
law. The 'ordering accident' limb is **corrected** (Vue contract, not luck). **→ NO-WAVE-OWNER —
candidates MT-APP-1 / MT-DOCK-LAYERS-1.**"*
**Why X-W8 and not the boundary**: `DockStatusLamp.vue` + `status-lamp.ts` are §3 bounds rows here
and `useApiClient()` is the transport cone this wave subtracts against (§3 row 21,
`demo/platform/transport/**`) — the throw site is inside this wave's own delta. The **cure** (where
the boundary is re-parented) is NOT elected here: it is contested between MT-APP-1 and
MT-DOCK-LAYERS-1, and X-W5's ErrorBoundary rows are the third claimant (X-W5-FOLD's *"ErrorBoundary.vue
is in NO X wave"*). This fold books the **row**, names the three claimants, elects none, and raises
gate **N-7** so a subtraction close cannot report the surface clean.
⟨shell-dock-dockstatuslamp.md · DSL-24, :56⟩ · cross-edge: X-W5 (ErrorBoundary) · X-W4.d (Dock.vue)

**W8.54 · DSL-28 — specifier-extension duality inside the transport cone** (cl.1 · INFO/MINOR)
Verbatim: *"Verified (`./client.js`/`./availability.js` with extensions inside; every inbound edge
without). **→ NO-WAVE-OWNER (transport unclaimed — the ApiOfflineChip DISSENT-#3 flag,
re-raised).**"*
`demo/platform/transport/**` is **already** §3 BoundsDelta row 21 of this fold (the pass-1 check
cites it at `X-W8-FOLD.md:374` as the answered path for AdminAuditPanel's *"appears in no X wave's
file list"*), so the row lands inside declared bounds. The **ApiOfflineChip DISSENT-#3** flag is
carried un-ruled: this wave subtracts dead surface, and a specifier rewrite is a *shape* decision
the dissent says no wave has claimed. Rides **N-7**'s second clause.
⟨shell-dock-dockstatuslamp.md · DSL-28, :73⟩ ≡ ⟨ApiOfflineChip.md · DISSENT-#3⟩

**W8.55 · N-11 — `referrerPolicy`/`crossOrigin` unpassed: every menu open sends the full document URL
to GitHub** (cl.1 · MINOR)
Verbatim: *"`referrerPolicy`/`crossOrigin` are declared by the producer's `AvatarImage` and unpassed —
every menu open sends the full document URL to GitHub; the mitigation is one token. (reader-LC) …
Folded into **D-12** (NO-WAVE-OWNER)."*
`ProfileSection.vue` is §3 BoundsDelta row 15 here and is **G-13's sole consumer surface**, so the
one-token mitigation lands inside this wave's touch. **The spine's stated contingency is honoured
explicitly, not silently**: the assignment reads *"if the D-12 identity resolves to another wave, the
W0 sitting re-homes it, stated not silent."* This seat's reading: `D-12` is `shell-dock-profilesection`'s
own namespace (⟨shell-dock-profilesection.md · D-12⟩ — **not** ⟨Dock.md · D-12⟩ nor any other), and
this file's rows N-6..N-10/N-12 already fold here (W8.39, G-13), so the sibling evidence points home.
**Recorded as a re-home candidate, not a ruling**: if the X-W0 sitting resolves D-12 elsewhere, this
row travels with it and W8.55 becomes an ≡-pointer.
⟨shell-dock-profilesection.md · N-11, :87⟩ → ⟨shell-dock-profilesection.md · D-12⟩

---

### 9.3 §B — the census escapes: the roster's four, plus nine this seat's re-derivation found

Roster rows are marked **[R]**; this seat's supplements beyond the roster are marked **[+]** — all
thirteen are escapes by the same byte test, and all thirteen are folded so no round-2 check
re-discovers them.

**W8.56 · [R] PP-25 — three dead vue imports surviving a hard `--max-warnings=0` gate whose
unused-vars rules are off; both import idioms seven lines apart in one script block** (cl.1 · MINOR)
Verbatim disposition: *"**Shim extirpation IDENTITY-FOLD → the W-HYGIENE demo/ui row
(ColorSpaceSelector.md L-4 lineage) → X-W8; the dead imports die free in any wave touching the
file.**"* Verified in-record: `watch`/`onMounted`/`nextTick` each occur exactly once (:128) — i.e.
the import is the only occurrence. Two idioms coexist seven lines apart: the 19 one-line `demo/ui`
shims AND direct `@mkbabb/glass-ui/*` subpaths.
**≡ W8.1/W8.4 (H-a)** for the extirpation half; the **dead-import half is a distinct fact** and is
this row's own content — it is the live counter-witness that **G-6**'s glob ban must FIRE on
(`demo/palettes/**` is in no glob — PP-18, already W8.13). *Sharpens*: **G-2**, **G-6**, **G-10**.
⟨PalettesPane.md · PP-25, :62⟩ ≡ ⟨ColorSpaceSelector.md · L-4⟩ (lineage, not re-mint)

**W8.57 · [R] US-9 — the sort radio group is unlabelled for AT; the cure is TWO CONSUMER ATTRIBUTES,
not a producer change** (cl.1 · MINOR · **correction-of-record carried verbatim**)
Verbatim: *"**Correction of record**: both Label and RadioGroup are `inheritAttrs:!1` *with
useAttrs-forwarding through mergeProps* (compiled setups read by this seat), so the cure is two
consumer attributes today — `id` on the label, `aria-labelledby` on the group — no producer change
required; a producer auto-wire affordance may ride the glass BH relay as garnish, never as a
blocker."* Disposition verbatim: *"**Disposition: X-W8.b same-touch named-adopt rider** (the wave
already edits this file) — else NO-WAVE-OWNER."* Mitigation (why MINOR): the invoking trigger is
named "Sort users", establishing context on open.
**The `else NO-WAVE-OWNER` is load-bearing**: if X-W8.b rewrites `UserSortMenu.vue`'s barrel imports
and does not carry this rider, the row is orphaned by the very touch that could have cured it → gate
**N-8**. This row also carries an **§A NWO limb homed to X-W7** (packet 14 · BROWSE-SEARCH); per the
assignment's overlap law the row folds **once, both limbs** — X-W7 carries the NWO limb, this fold
carries the X-W8.b routed limb. **The `UserSortMenu.vue:8` cure collision with X-W6 (null-DELTA vs
quiet migration; X-W8 G-9's null-DELTA is the other pole) stays a SITTING ITEM — elected by no seat
here.**
⟨UserSortMenu.md · US-9, :45⟩ · collision: ⟨X-W6-FOLD · UserSortMenu.vue:8⟩ · G-9 non-citability rider (W8.33)

**W8.58 · [R] US-11 — the demo forwarding-barrel edges: axis L-1's FIRST clause, dropped by both
readers, restored** (cl.1 · MINOR-as-FOLD)
Verbatim: *"`:40` `../../../ui/button`, `:41-48` `../../../ui/dropdown-menu` — the axis filed it
('Button and dropdown pieces arrive through demo forwarding barrels') and neither reader adjudicated
it. Both families have first-class per-component subpaths at glass-ui 7.0.0 (X-W8.b's own census).
Identity: the repo-wide demo/ui extirpation (ColorSpaceSelector L-4 / AdminUsersPanel L-4 / MT-UI1 —
anti-rename, one identity). **Disposition: FOLD → X-W8.b** (the 92-import rewrite; the executing
owner of this file's touch)."*
**≡ W8.1 (H-a)** by identity — folded as an explicit ≡-pointer row, and its arithmetic contribution
is real: `button` and `dropdown-menu` are both in the **13 subpath families** the census commits, so
US-11 is a *site* of the denominator W8.2 rules on (`90` vs `92`, loser SUPERSEDED-BY-MEASUREMENT),
never a second booking of the identity.
⟨UserSortMenu.md · US-11, :47⟩ ≡ ⟨ColorSpaceSelector.md · L-4⟩ ≡ ⟨AdminUsersPanel.md · L-4⟩ ≡ MT-UI1

**W8.59 · [R] US-12 — option glyphs lack `aria-hidden` (one attribute ×3)** (cl.1 · INFO)
Verbatim: *"`:13` carries it; `:23`/`:27`/`:31` do not; the producer supplies none (`@lucide/vue`
defaultAttributes read — both readers, consistent) while glass-ui's own CheckboxItem indicator models
the correct behaviour. One attribute ×3. **Disposition: X-W8.b same-touch rider with US-9** — else
NO-WAVE-OWNER. AT consequence is residue 2."* Carries an **§A NWO limb homed to X-W7** — folded once,
both limbs, exactly as US-9. Rides **N-8** with US-9/US-11.
⟨UserSortMenu.md · US-12, :48⟩

**W8.60 · [+] AL-X1 — the triplicated loading announcement: the SKELETON limb is X-W8's named edit**
(cl.1 · MAJOR-at-its-booking-record · **surface split carried verbatim**)
Verbatim: *"Surface split: panels = X-W7, skeleton = **X-W8's named AdminListSkeleton edit**."* Facts
verified at the record independently: *"Skeleton :10-11 stamps `role="status" aria-label="Loading"`
per row; every panel wrapper is a role-less div whose `aria-label` AT ignores (7 sites, my census);
AdminTagsPanel.vue:54-55 is the in-suite correct shape."* Cure verbatim (from the AdminNamesPanel
booking it counts at): *"one `role="status"` container owning the label, skeleton drops its
per-instance `role="status"`."* Severity/anti-double-count clause verbatim: *"**counted at the
AdminNamesPanel adjudication**"* — so this fold books the **skeleton limb**, not a second severity.
**Why it must land here**: **G-9**'s census names `AdminListSkeleton.vue` and W8.33 already widens
that census by three filenames; the skeleton's `role="status"` removal is inside the same edit. A
close that widens G-9's grep and does not drop the per-instance role leaves the triplication live
under a green gate.
**LOCK CARRIED VERBATIM (the fourth may-not-be-cited edict the pass-1 check found absent layer-wide;
the spine registered it at §3 d′ — this fold carries it because it binds THIS wave's close):**
⟨AdminListSkeleton.md · :82⟩ — *"The E1-R2 ΔL certification does not currently apply to this
component; live measurement required before it is ever cited again."* ⟨AdminFlaggedPanel.md ·
:107/:146⟩ — *"the E1-R2 certification may not be cited for this component until re-measured."*
⟨AdminListItem.md · AL-X1, :67⟩ ≡ ⟨AdminNamesPanel.md · C-9⟩ (counted there) · cross-edge X-W7 (panels limb)

**W8.61 · [+] A-20 — `useDialogBrowseActions` homed under `dialog/`, sole consumer a pane, docstring
says host-agnostic; the dead `PaletteDialog/` path still cited** (cl.1 · graph subtraction)
Verbatim disposition: *"**X-W8.a census candidate (graph subtraction), else boundary.**" *Facts:
*"`useDialogBrowseActions` homed under `dialog/` + re-exported from the 'dialog cluster' barrel, sole
consumer a pane; docstring itself says host-agnostic; dead `PaletteDialog/` path still cited at
useVersionHistory.ts:9-10 (my read)."*
**Namespace note (citation law, C-1)**: ⟨FlagReportDialog.md · A-20⟩ ≠ ⟨PaletteSlugBar.md · A-20⟩ ≠
⟨MigratePalettesDialog.md · A-20⟩ — bare `A-20` resolves in three namespaces and the fold layer's
only `A-20` bytes belong to others. This row is the FlagReportDialog one. *Sharpens*: **G-1** (the
X.W8.a census must emit this as a named candidate with a PASS/boundary branch) and **G-10** (dead
path citation).
⟨FlagReportDialog.md · A-20 (L-9), :53⟩

**W8.62 · [+] R-4 — the live BEFORE witness is X-W8's DELTA OBLIGATION, not the record's proof**
(cl.2 · obligation, born un-owned)
Verbatim: *"a live BEFORE witness of ADJ-1's tints during an actual freeze/orphan repro (the static
replay is decisive; **the witness belongs to X-W8's delta obligations, not to this record's
proof**)."*
This is a *record-to-wave hand-off of an evidence obligation* — the class the pass-1 check's lock
defect is about, and it dies with the row if the row escapes. It attaches to **EGR-1's three-limb
delta obligation** already inside **G-11** (W8.37) and to **N-4** (SEQ-1 coupling): the `--z-debug`
restore commit is the only commit in which a live freeze/orphan repro is reproducible, so the BEFORE
capture is takeable exactly once.
**Namespace note**: ⟨picker-debugeventlog.md · R-4⟩ — the fold layer's other `R-4` bytes are X-W1's
and X-W6's namespaces; this identity had no carrier.
⟨picker-debugeventlog.md · R-4, :73⟩ → G-11 (EGR-1 limb) · N-4 (SEQ-1)

**W8.63 · [+] AB-3 — the BLOCKER's GESTALT limb accretes to X.W8.d; the containment limb does NOT**
(cl.4 · BLOCKER, split disposition carried verbatim)
Verbatim: *"**DISPOSITION: split — containment (ONE predicate: guard `toggleActionBar` on
`hasAnyActionBar`, or `disabled`+`aria-hidden` the toggle when `!visible`) rides X-W4.d (Dock.vue is
in its file bounds; CC-043's unit) and is independent of the transposition; the gestalt cure (the
trigger renders from the crossfade's own `faces` registry — reachable via template ref
`defineExpose({faces})`, NOT via the unpublished context; K-1's tail) accretes to X-W8.d. Collapse
geometry numbers: UNPROVEN-NEEDS-LIVE.**"* Corrections adopted, carried: *"(a) the owning surface is
`Dock.vue` + `ActionBarToggle.vue` — ActionBarLayer cannot cure it; (b) NOT mobile-specific."*
**This is the sharpest single find of the repair pass**: a **BLOCKER** with an explicitly named
`X-W8.d` limb was carried nowhere in the fold layer — not in X-W4's fold (which owns the *other*
limb), not here. It lands in **X.W8.d** (dead surface / dead state), it is **UNPROVEN-NEEDS-LIVE** on
geometry, and it is **not** re-graded by this fold. Write-order cross-edge: X-W4.d's containment guard
and X-W8.d's registry-sourced render are independent by the record's own words — *neither blocks the
other*, and a close on either may not claim AB-3 cured.
⟨shell-dock-actionbarlayer.md · AB-3 (D-1), :41⟩ · cross-edge X-W4.d (CC-043) · **not** ⟨AboutPane.md · AB-3⟩

**W8.64 · [+] AB-17 — the 260 ms timer dies with the shim at X-W8.d; the state-lift question does
NOT** (cl.1 · MINOR, split carried verbatim)
Verbatim: *"The 260 ms timer is never cleared on scope disposal (no `onScopeDispose`/`onUnmounted` —
my grep) and the whole mode state is a local ref behind two stacked `v-if`s. **DISPOSITION: the timer
dies with the shim (X-W8.d); the `toolbarMode`-discard-on-unmount is a state-lift question recorded
for the formation boundary (no owner).** The observed mid-session unmount: UNPROVEN-NEEDS-LIVE."*
**Namespace note**: the fold layer's ten `AB-17` bytes are all ⟨AboutPane.md · AB-17⟩ (X-W11's
`BOUND` cells) — a textbook C-1 collision; this ActionBarLayer identity had no carrier. Lands in
**X.W8.d** beside W8.25/W8.31; the state-lift half travels to the boundary **stated**.
⟨shell-dock-actionbarlayer.md · AB-17 (C-10), :64⟩

**W8.65 · [+] MISS-9 — the shim's one surviving type obligation is a FALSE one; dies at X-W8.d with
AB-18** (cl.1 · INFO)
Verbatim: *"The shim's parameter type launders `Readonly<ShallowRef<HTMLElement|null>>` into
`Ref<HTMLElement|null>` — the file's one surviving type obligation is a false one. Dies at X-W8.d
with AB-18."*
**Namespace note (a live collision, both halves routed this round)**: ⟨shell-dock-actionbarlayer.md ·
MISS-9⟩ is **X-W8.d's**; ⟨picker-componentsliders-consolerail.md · MISS-9⟩ is **X-W4's** (roster §B).
Two records, one bare id, two waves — cite ⟨record·id⟩ or the round-2 census will read one as
carrying the other. Absent from the **whole** fold layer before this row.
⟨shell-dock-actionbarlayer.md · MISS-9 (R-LC), :112⟩ ≠ ⟨picker-componentsliders-consolerail.md · MISS-9⟩ (→ X-W4)

**W8.66 · [+] ABT-10 — the X-W8 LIMB only: "the label-bearing register itself rides ABT-7's X-W8
rewrite"** (cl.4 · ≡-pointer, primary home is X-W4.a)
The row's primary disposition is **X-W4.a** (CC-040, the shell target-size wave) and it is correctly
carried there as ⟨X-W4-FOLD · W4·7⟩ — **this fold does not re-book it**. What escaped is its final
clause, whose home is this wave, verbatim: *"**X-W4.a** … with the label-survival arm attached; **the
label-bearing register itself rides ABT-7's X-W8 rewrite**."* `ABT-7` is in **W8.44**'s decline
census (MT-DOCK-LAYERS-1) — so the register the label-survival arm depends on is **declined here**.
**The consequence, stated not silent**: X-W4.a's label-survival arm has a dependency on a rewrite
this wave does not perform. Neither wave may close claiming ABT-10 whole. Raised into **N-3** (the
decline census must name ABT-7's *dependents*, not only ABT-7).
⟨shell-dock-actionbartoggle.md · ABT-10 (D-07), :64⟩ → W8.44 (ABT-7) · X-W4-FOLD W4·7 (primary)

**W8.67 · [+] L-21 — the eyedropper's tsconfig-parity SEVERITY row: identity wins, and it names this
wave's decline** (cl.4 · ≡-pointer to H-c)
Verbatim: *"Anti-rename identity guard: fact-identical to XP-21/ColorSpaceSelector-L-14/
componentsliders-R-10 (this seat re-read tsconfig.demo.json:42-49 at HEAD: 3 phantoms, `/css`+`/value`
missing, comment false by one). Nothing mis-resolves today; one identity, **already booked
NO-WAVE-OWNER (X-W8 declined it, W8.md:112)**."* Ruled severity: **MINOR — identity wins** (axis
filed BLOCKER, reader-2 MAJOR, XP-21 MINOR).
**≡ W8.7 (H-c) ≡ W8.52 (DSL-9)** — three ids, one identity, one decline. Folded as an explicit
≡-pointer so the byte census closes and **N-2**'s H-c cell has its full witness list.
⟨wb-extract-imageeyedropper.md · L-21, :29⟩ ≡ ⟨wb-extract-pane.md · XP-21⟩ ≡ ⟨ColorSpaceSelector.md · L-14⟩ ≡ ⟨picker-componentsliders.md · R-10⟩

**W8.68 · [+] Copy-CSS — the severity row whose second limb is X-W8 hygiene** (cl.4 · ≡-pointer)
Verbatim: *"**MINOR — both corrected (PIN 6)** … The divergent-feedback half is real and user-facing;
**the idiom census is X-W8 hygiene**."* The feedback half is X-W6's (GradientVisualizer's own wave);
the **idiom-census half** is the H-a/H-b hygiene identity this wave owns.
**≡ W8.1/W8.14 (the census MUST)** — no second booking; folded because the id, as literal bytes
(`Copy-CSS`, a non-numeric identity that no range can expand into), was absent from the whole fold
layer, and a round-2 byte census cannot distinguish "hygiene half carried elsewhere" from "row
dropped".
⟨wb-gradient-visualizer.md · Copy-CSS feedback severity, :33⟩ → G-1 (idiom census) · X-W6 (feedback half)

---

### 9.4 §C — the I-28 glass-8.0.0 EXTERNAL row owned by this wave

**Provenance is MAIL, not registry** — `docs/tranches/V/coordination/INBOX.md` row **I-28** (glass
v8.0.0 @ `17a11bc5`; letter + §5 addendum 08-25; ROWED 08-28). The id is `X-EXT-2`, cited
⟨INBOX I-28 · X-EXT-2⟩; it carries **no registry id and must never be given one** (an X-EXT row that
acquires a registry-shaped id becomes uncheckable against both censuses).

**W8.69 · X-EXT-2 — `./forms` → `./input`: `demo/ui/input/index.ts:1` dies at glass 8.0.0** (cl.2 ·
adopt-wave amendment, born-RED **at the repin**)
Break, verbatim from the spine's §EXTERNAL: *"`./forms` → `./input`: `demo/ui/input/index.ts:1`
(`export { Input } from "@mkbabb/glass-ui/forms"`) dies at 8.0.0"*, owning wave *"**X-W8** (demo/ui
barrel surface, H-a territory — the extirpation cure and the rename cure are the same edit or an
ordered pair, stated)."*
**Why this row is not redundant with H-a.** `input` is **one of exactly two counterexamples** to the
19-barrel one-line-rename shape — W8.1 carries it four-witnessed: TEP-9 (*"17 of 19 modules are
one-line root-barrel renames (**alert & input are the counterexamples**)"*) and
`shell-dock-profilesection` L-3 (*"18 of 19 reach the root barrel — **`input` is the counter-example
(`@mkbabb/glass-ui/forms`)**"*). So the ONE barrel whose importer names a subpath is the ONE the
8.0.0 repin renames.
**THE ORDERED PAIR, STATED (the clause the assignment demands be elected, not left ambiguous):**
if the barrel deletion (H-a, G-2/G-3) lands **before or with** the repin, `demo/ui/input/index.ts`
ceases to exist and the rename is **moot** — the ~1 consumer edge repoints straight to
`@mkbabb/glass-ui/input`. If the repin lands **first**, the barrel must be renamed `forms`→`input`
in the repin commit or the demo does not build. **This fold elects the first order** — *delete, then
repin; if the repin arrives first, the rename is a one-line stopgap inside the repin commit and the
deletion still lands.* Either way the act is stated and neither is silent.
**Born-RED status**: the census (X-W0.j) measured **0/4 at glass 7.0.0** on 2026-08-28 — the FAIL
branch is live, the repin has not happened, so this row's gate (**N-9**) is RED-on-arm, not RED-today.
Cross-edges: **X-W0** (X-EXT-6, the `vue-component-type-helpers ^3.0.3` peer — the repin is
unexecutable without it) · **X-W7** (X-EXT-1's `.input-bar` recipe, the *other* input-shaped break) ·
**X-W4.g** (X-EXT-4's receiving-bounds delta).
⟨INBOX I-28 · X-EXT-2⟩ · mirrored at ⟨REFINEMENT-FOLD-2026-08-28.md · §EXTERNAL 2, :494⟩

---

### 9.5 Locks carried verbatim by this repair (the pass-1 MINOR-locks defect, X-W8's share)

The check found four named may-not-be-cited edicts absent from the whole fold layer. Two bind **this
wave's close** and are carried here in full (the spine registered all four at its §3 d′ — this is
carriage at the wave that must obey them, not a second registration):

1. **E1-R2** (admin skeleton certification) — carried at **W8.60**, both sources quoted verbatim.
   Binding shape: **G-9**'s widened census may not cite the E1-R2 ΔL certification for
   `AdminListSkeleton` in any close; live measurement first.
2. **DVS-38** (the latch's mechanism) — ⟨shell-dock-dockviewselect.md · K-17, :93⟩ verbatim: *"the
   seed/watcher predicate split was ruled LATENT and non-operative at Dock-adj R-5; it enters this
   record only as an INFO accretion to L-19 (DVS-38) and **may not be cited as the latch's mechanism
   by any close**."* Binding shape: **W8.44**'s decline census and **N-3** may enumerate DVS-38, but
   no X-W8 close may cite it as the latch mechanism.

Not X-W8's: **ONE-AXIS** (⟨GradientStopEditor.md · :120⟩ → X-W6) and **copy seat**
(⟨PaletteCardSwatches.md · :24⟩ → X-W7). Named here so the absence reads as *routed elsewhere*, never
as *unseen*.

---

### 9.6 Gates this repair adds — born-RED, real witnesses, L-19 posture

**N-7 — THE STATUS-LAMP BOUNDARY + TRANSPORT-SPECIFIER GATE** (from W8.53 · W8.54).
*(a)* `W8-CLOSE.md` states, for the dock band, whether the band's setup path is inside an
ErrorBoundary at close, naming the elected claimant (MT-APP-1 / MT-DOCK-LAYERS-1 / X-W5) or recording
UNCURED-WITH-OWNER. *(b)* the transport specifier duality (`./client.js` / `./availability.js` with
extensions inside `demo/platform/transport/**`, every inbound edge without) is either normalized in
the same subtraction or booked with the **ApiOfflineChip DISSENT-#3** flag re-raised, never dropped.
**RED today**: the band sits outside the sole ErrorBoundary (the record's App.vue reads), the throw
site `useApiClient()` :54-62 is live, and `demo/platform/transport/**` enters bounds only via §3 row
21 of this addendum. **Falsifier**: a close that reports the transport cone subtracted while
`grep -rn 'from "\./\(client\|availability\)"' demo/platform/transport/` and its `.js`-suffixed
counterpart both return non-empty has not answered DSL-28.

**N-8 — THE SAME-TOUCH RIDER DECLARATION (`UserSortMenu.vue`)** (from W8.57 · W8.58 · W8.59).
If X-W8.b edits `UserSortMenu.vue` at all, the commit carries **US-9** (`id` on the label +
`aria-labelledby` on the group), **US-11** (both barrel edges → per-component subpaths) and **US-12**
(`aria-hidden` ×3 at `:23`/`:27`/`:31`); if it does not carry them, the close records each as
NO-WAVE-OWNER **by name** — the records' own `else NO-WAVE-OWNER` branch, made checkable.
**RED today**: all three are live at HEAD per their record (`aria-labelledby` = 0 occurrences in the
compiled dropdown chunk; `:40`/`:41-48` still reach `../../../ui/*`; `:13` alone carries
`aria-hidden`). **Falsifier**: a barrel-rewrite diff touching this file with none of the three riders
and no named NO-WAVE-OWNER line is exactly the orphan-by-the-curing-touch failure. **Un-elected
rider**: the `UserSortMenu.vue:8` cure collision (X-W6 quiet migration vs **G-9** null-DELTA) is a
SITTING item — this gate may not be read as electing either.

**N-9 — THE GLASS-8 `./forms`→`./input` ORDERED PAIR** (from W8.69 · X-EXT-2).
At the 8.0.0 repin, `demo/ui/input/index.ts` is either **already deleted** (H-a landed first — the
elected order) or **renamed in the repin commit itself**; no state exists in which the repin lands
and the barrel still says `@mkbabb/glass-ui/forms`.
**RED-on-arm, not RED-today** — and it is stated as such: X-W0.j's census measured **0/4 at glass
7.0.0** on 2026-08-28, so `./forms` is alive and the gate cannot go RED until the repin. **Falsifier**:
after the repin, `grep -n 'glass-ui/forms' demo/` returning non-empty is the break, live.

---

### 9.7 §-arithmetic — dated, never silent

**Dated correction (2026-08-28, fold round 1 repair).** §0's arithmetic block and §8's closing line
read **51 folded rows · 21 gate entries · 31 BoundsDelta files · 11 cross-edges**. That was the true
pass-1 reading and is preserved above, marked SUPERSEDED-BY-MEASUREMENT. The true counts after this
addendum:

| unit | pass-1 | **repair round 1** | delta | decomposition |
|---|---|---|---|---|
| folded rows | 51 (W8.1–W8.51) | **69** (W8.1–**W8.69**) | **+18** | §9.2 NWO **4** (W8.52–55) + §9.3 census **13** (W8.56–68; roster 4 + supplements 9) + §9.4 external **1** (W8.69) |
| gate entries | 21 (15 sharpenings + 6 new) | **24** (15 sharpenings + **9** new) | **+3** | N-7 · N-8 · N-9 (§9.6) |
| BoundsDelta files | 31 | **31** | 0 | every row of §9 lands on a path §3 already carries (`status-lamp.ts`/`DockStatusLamp.vue` 19-20, `ProfileSection.vue` 15, `demo/platform/transport/**` 21, `demo/ui/**` H-a, `browser/index.ts` 25) — **no bounds growth**, and **N-1** (bounds completeness) is the standing check |
| cross-edges | 11 | **11** | 0 | §9's edges (X-W4.d/AB-3 · X-W7/AL-X1 panels · X-W6/UserSortMenu:8 · X-W0+X-W7+X-W4.g/glass-8) are carried **inside their rows**, not minted as new §4 entries — a §4 row is a wave-to-wave write-order contract; these are row-level dependencies |
| IN-side census receipt | 455 line-instances / 64 records | **unchanged** + **271 routed row lines / 56 records** | — | different units, both reproduce (§9.1); the row-keyed figure is the one a round-2 id census must re-derive |

Numbering is gapless and contiguous: **W8.1 … W8.69**, zero gaps, zero re-mints, zero renames.
Nothing in §9 re-stages or re-weights the wave's gate composite (OC-2/CC-096 staging stands).
Status remains **planned**; no product source was opened by this repair.

---

## 10. FOLD-ROUND-2 REPAIR ADDENDUM — 2026-08-28 (the census-method repair)

**What this section is.** The X-W8 seat's consumption of `refinement/ROUND-2-ASSIGNMENTS.md` §A.5 —
**nine ⟨record·id⟩ homed here** (8 routed · 1 NWO) by the pass-2 BLOCKER's id-keyed census
(`refinement/PASS-2-CHECK-RETURN.json`, verdict NON-CONFORMANT: 1 BLOCKER · 2 MAJOR · 5 MINOR ·
1 INFO). The BLOCKER's root cause is **method, not oversight**: X-W7-FOLD §R1's *"wave token within
30 characters after a routing marker"* heuristic is blind to rows routed by bare declarative
phrasing, and §9's own verb-filter column inherited the same disease. §D of the work order retires
it; this section folds what it hid.

**Posture unchanged.** Status `planned`. Dispositions **ADJUDICATED**, never VERIFIED — no gate has
run and nothing here stamps a wave. No product source opened: every witness below is a corpus byte
re-read at HEAD by this seat. **E-3 holds** — `waves/W0.md..W11.md`, `CONFORMANCE-2026-08-03.md`,
`refinement/ROUND-1-ASSIGNMENTS.md` and every `registry/adjudicated/**` record stay byte-untouched;
this file remains this seat's sole write. **Banked ids are for life**: every id below is the id its
record minted, and where the substance is already carried by an existing row the cure is an explicit
**`≡`-pointer booking under the original id** — never a silent skip — so the byte census closes.

### 10.1 The census, re-derived under the §D CENSUS METHOD LAW (clause 6: the count follows the predicate)

**The predicate now**: a row line carrying an `X[-·.]W8` token **in any phrasing, at any position**
BOOKS the row. No proximity requirement, no marker-verb requirement. Re-runnable at HEAD, verbatim:

```
# corpus (92) → X-W8-routed row lines (271) → records (56)
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | wc -l
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' \
  | xargs grep -nE '^\s*([-*]|\|)\s*\*\*' | grep -cE 'X[-·.]W8([^0-9]|$)'
```

**Both lines reproduce at HEAD (`92`, `271`)** — measured read-only by this seat, 2026-08-28, and
identical to §9.1's printed denominator and to §D's law-true table (X-W8 = **271**). **§B.2's defect
class is therefore ABSENT from this fold**: the figure this file prints is the figure its printed
command returns. Recorded as a result, not assumed — clause 6 obliges the receipt either way.

**One inherited column is struck, dated.** §9.1's row *"…of which carry an explicit routing verb (the
check's verb set) | **134**"* is **SUPERSEDED as a census instrument** by §D clause 2: the verb set is
the retired heuristic, and a 271-vs-134 split is an observation about phrasing, never a filter. Both
figures stay legible (E-3 idiom: supersede in place, strike nothing) and **271 is the denominator**.
The nine rows below are exactly what the verb filter hid inside this wave's band.

**Namespace guards (stated before the rows, because two of them are live inside this file).**
1. ⟨shell-dock-profilesection.md · **N-8 / N-9 / N-10**⟩ are **RECORD ids** and are **NOT** this
   fold's gates **N-7 / N-8 / N-9** (§9.6). One bare token, two registers, no relation. Cite
   ⟨record.md · id⟩ or `gate N-n` — never the bare id alone.
2. ⟨shell-dock-actionbarlayer.md · **MISS-7**⟩ ≠ ⟨picker-componentsliders-consolerail.md ·
   **MISS-7**⟩. Both are folded below, in this same wave band, as two rows. This is the same
   collision class §9.3 already named for `MISS-9`.

### 10.2 §A.5 — the nine rows, booked id-for-id (W8.70 … W8.78)

**W8.70 · ⟨picker-spectrumcanvas.md · M-11⟩ — `spectrumLuma.ts` is 2/3 dead surface and its
self-presentation is false in both numbers** (cl.1 · MINOR · **`≡`-pointer booking**)
Row verbatim (`:98`): *"**M-11 — MINOR — 2 of `spectrumLuma.ts`'s 3 exports are dead surface; the
module's self-presentation ("the shared home of a three-consumer regime") is false in both numbers →
X-W8, cure with C-5.** (Reader B; this seat's greps are the proof.)"*
**Where the fact already lives, and why this is a booking and not a re-book**: §BoundsDelta row 10
carries the file (`demo/picker/controls/spectrumLuma.ts` — *"M-11 (2 of 3 exports dead;
self-presentation false in both numbers)"*, CAND) and W8.35's body carries the substance in prose.
Neither carried it as **⟨record.md · id⟩**, which is why the id-keyed census reported it absent; this
row closes the byte census under the original id and mints nothing.
**The `cure with C-5` clause is a CROSS-WAVE SEQUENCING FACT, and it is stated rather than swallowed**:
⟨picker-spectrumcanvas.md · C-5⟩ (`:62`) routes *"→ X-W4, unit oracle → X-W1"* — the hue-blind border
regime. So the dead-export deletion (X-W8) and the predicate rewrite (X-W4 + X-W1's oracle) touch one
module: **deleting 2 of 3 exports while another wave rewrites the third is an order dependency, not a
free carve.** No wave is elected here; the dependency is recorded so the executor cannot meet it by
accident. Rides **G-10** (dead-surface subtraction) with W8.35.
⟨picker-spectrumcanvas.md · M-11, :98⟩ ≡ §BoundsDelta row 10 + W8.35 (substance) · cross-edge X-W4/X-W1 ⟨picker-spectrumcanvas.md · C-5, :62⟩

**W8.71 · ⟨shell-dock-profilesection.md · N-8⟩ — `liveTintCache` is keyed by surface alone, so the
first veil probed becomes every veil's referent** (cl.1 · MAJOR · **`≡`-pointer booking**)
Row verbatim (`:84`): *"| **N-8** | MAJOR | `liveTintCache` is keyed by `InkSurface` alone with a
first-match `querySelector('[data-surface="veil"]')` host — directly contradicting the 12-line
subtree-locality argument (with measured evidence) the same file makes for the veil; the first veil
probed becomes every veil's referent until the next epoch. (reader-LC) | :158–172/:239–249 read —
structurally certain. | **X-W8**. |"*
**Where the fact already lives**: W8.39's *"Adjacent, same instrument"* paragraph carries N-8's
mechanism as a **design input the G-13 repair must absorb**. It was never booked ⟨record·id⟩ — hence
the escape. This row books it; **G-13's predicate is unchanged and no gate is re-staged**. The row's
force is A-3's already-banked bound read forward: a cache whose key omits the referent is not cured by
a better selector.
⟨shell-dock-profilesection.md · N-8, :84⟩ ≡ W8.39 (G-13 design input) · **≠ gate N-8** (§9.6)

**W8.72 · ⟨shell-dock-profilesection.md · N-9⟩ — `resolveCssColorAlpha` fabricates a confident OPAQUE
BLACK where every sibling degrades honestly** (cl.1 · MINOR · **`≡`-pointer booking**)
Row verbatim (`:85`): *"| **N-9** | MINOR | `resolveCssColorAlpha` fabricates a confident OPAQUE BLACK
for an unparseable color (canvas ignores invalid `fillStyle`; both grounds return black; α computes 1)
while every sibling failure path degrades honestly to null/undefined. Latent — current callers feed
computed values. (reader-LC) | :100–117 read — derivation checked line-by-line by this seat. |
**X-W8**. |"*
**Latency carried, not dropped**: the record's own *"Latent — current callers feed computed values"* is
the reason this is MINOR and the reason a close may not report it cured by absence of symptom. Same
instrument as W8.71; booked separately because the record books it separately (anti-rename).
⟨shell-dock-profilesection.md · N-9, :85⟩ ≡ W8.39 (G-13 design input) · **≠ gate N-9** (§9.6)

**W8.73 · ⟨shell-dock-profilesection.md · N-10⟩ — the instrument leaks by design: an unremoved probe
`div`, a memoized context, two unowned module-global maps** (cl.1 · MINOR · **`≡`-pointer booking**)
Row verbatim (`:86`): *"| **N-10** | MINOR | The instrument leaks by design: a hidden probe `div`
appended to `document.body` with no removal path, a memoized canvas context, and two more unowned
module-global maps — surviving unmount and HMR, littering every jsdom suite. (reader-LC) |
:80–92/:121/:134–145/:239 read. | **X-W8**. |"*
**Why it matters to this wave beyond hygiene**: the leak is the *substrate* of N-8's stale referent —
the maps that survive unmount are the maps whose keys omit the referent — and `ProfileSection.vue` is
§BoundsDelta row 15 here, so the removal path lands inside this wave's touch. Rides **G-13**'s repair
surface with W8.71/W8.72; **no new gate is minted for it**.
⟨shell-dock-profilesection.md · N-10, :86⟩ ≡ W8.39 (G-13 design input)

**W8.74 · ⟨shell-dock-actionbarlayer.md · MISS-7⟩ — the prescribed cure nests `.dock-crossfade`
inside `.dock-crossfade`, and the descendant-scoped clip-path matches both** (cl.1 · MINOR ·
**`≡`-pointer booking**)
Row verbatim (`:110`): *"**MISS-7 (R-LC) — CONFIRMED (MINOR).** The prescribed cure nests
`.dock-crossfade` inside `.dock-crossfade`, and crossfade.css's `[data-morphing] … >
.dock-face-content { clip-path: inset(…) }` matches BOTH after the transposition — the same
descendant-scoped shape that produced AB-2, co-directional and low-blast but absent from L's carry
table. **DISPOSITION: X-W8.d carry-table addendum row.**"*
**Where the fact already lives**: W8.31's G-8 paragraph and the **G-8 sharpening row** (§7a,
*"carry-table gains MISS-7 (nested crossfade clip-path)"*) both carry it; the compressed provenance
`MISS-4/-7/-8/-9` at W8.31's `⟨…⟩` line is the slash-compressed form the id-keyed census cannot see.
Booked here as literal bytes so the census closes; **G-8's predicate is unchanged**.
⟨shell-dock-actionbarlayer.md · MISS-7, :110⟩ ≡ W8.31 / G-8 carry-table · **≠** ⟨picker-componentsliders-consolerail.md · MISS-7⟩ (W8.77)

**W8.75 · ⟨shell-dock-actionbarlayer.md · MISS-8⟩ — discharged BY CONSTRUCTION, and the sequencing
lock is the part a silent skip would lose** (cl.1 · INFO · **`≡`-pointer booking + lock**)
Row verbatim (`:111`): *"**MISS-8 (R-LC) — CONFIRMED (INFO).** `demo/shell/dock/index.ts` does not
re-export `DockCrossfade`; the transposition's import must not deepen the dual path. **Discharged by
construction at X-W8.b/scope-6 (the barrel dies); recorded so the sequencing (b → d) is honored.**"*
**SEQUENCING LOCK, carried verbatim (this is the load, not the finding)**: *"Discharged by
construction at X-W8.b/scope-6 (the barrel dies); recorded so the sequencing (b → d) is honored."*
A row that is discharged-by-construction is **not** a row that may be dropped: the discharge is
conditional on **b landing before d**, and a `d`-first execution re-opens it against a barrel that
still exists. §6b already carries MISS-8 as scope-6's named witness (`:725`/`:729`); this booking adds
the id and the order, and elects nothing.
⟨shell-dock-actionbarlayer.md · MISS-8, :111⟩ ≡ X-W8.b scope 6 (§6b) · order lock **b → d**

**W8.76 · ⟨picker-componentsliders-consolerail.md · MISS-5⟩ — `sliderWrapperEls` is never pruned, so
five listeners re-bind to every detached wrapper from every space ever visited** (cl.1 · MINOR ·
**`≡`-pointer booking**)
Row verbatim head + terminal (`:65`): *"**MISS-5 — MINOR — `sliderWrapperEls` is never pruned, and
`attachSliderListeners` re-binds five listeners to every DETACHED wrapper from every space ever
visited → X-W8 (one cure with C-2/MISS-13).** Verified whole by this seat: … `listenerCleanups`
(:94-100) bounds the leak; the retained subtrees and the dead re-bind work are real. One-line cure
(else-delete) closes C-2 too."*
**Where the fact already lives**: W8.34 (*"C-2 + MISS-5 + MISS-13 are one cure with delete-on-null in
both ref callbacks"*) and §BoundsDelta rows 3 and 4. Booked here under its own id; **the one-cure
identity with C-2/MISS-13 is preserved exactly as the record states it** — three ids, one edit, no
second booking of the cure.
⟨picker-componentsliders-consolerail.md · MISS-5, :65⟩ ≡ W8.34 (one cure with C-2/MISS-13) · §BoundsDelta 3, 4

**W8.77 · ⟨picker-componentsliders-consolerail.md · MISS-7⟩ — `componentEntries` re-derives a computed
the injected model already exposes, and the two copies disagree** (cl.1 · MINOR · **`≡`-pointer
booking**)
Row verbatim head (`:67`): *"**MISS-7 — MINOR — `componentEntries` re-derives a computed the injected
model already exposes, its tuple payload is dead in every consumer, and the two copies disagree →
X-W8.**"* The record's own correction rides with it: *"Verified with one correction to reader B: the
expressions are NOT identical"* — the pipeline omits the alpha append and short-circuits hex to
`[["hex", 0]]` while the local copy fabricates an untyped alpha literal and yields rgb+alpha in hex
mode. **Two answers to "what channels does this space have"**, both consumers reading only index 0.
**Namespace guard, restated on the row**: this is the consolerail `MISS-7`, **not** the ActionBarLayer
`MISS-7` booked one row family above (W8.74). Fact already carried in W8.34's body; booked here
id-for-id.
⟨picker-componentsliders-consolerail.md · MISS-7, :67⟩ ≡ W8.34 (body) · **≠** ⟨shell-dock-actionbarlayer.md · MISS-7⟩ (W8.74)

**W8.78 · ⟨shell-dock-actionbarlayer.md · K-4⟩ — the KILL of D-9's convention claim, carried so no
close re-files it** (cl.4 · NWO · **kill row, new to this fold**)
Row verbatim (`:89`): *"**K-4 — "the only two demo files that write a bare `:hover` are ActionButton
and this one" (D-9's convention claim, and with it D-9 itself).** KILLED (ruling 1): my census finds
bare `:hover` in **10** demo files; the "convention" (the guard) exists in exactly one. R-LC's
confirming census cell is FALSE. D-9 leaves the roster; residue = an app-wide INFO census,
NO-WAVE-OWNER."*
**This is the §A.5 NWO row, and it is the only one of the nine that was absent from this file
entirely** (the other eight had substance in prose or in a bounds cell). It books here because
`ActionBarLayer.vue` is X-W4/X-W8-held and **X-W8 is the wave that carves it** (G-8/G-10, W8.31/W8.32),
so a close of this wave is exactly where a killed row gets re-filed by a reader who never saw the
ruling. **A KILL that is not carried is a kill that returns.** The residue — an app-wide bare-`:hover`
INFO census across **10** demo files — is **NO-WAVE-OWNER** and rides to the **G-F register** as a
cross-edge (X-W0-FOLD §7), **not** as a second booking here.
⟨shell-dock-actionbarlayer.md · K-4, :89⟩ — kill of ⟨shell-dock-actionbarlayer.md · D-9⟩ · residue → G-F register (X-W0)

### 10.3 §-arithmetic — dated, never silent (2026-08-28, fold round 2)

**This block supersedes §9.7's row line; §9.7 stands as the round-1 record.**

| unit | round 1 | **round 2** | delta | decomposition |
|---|---|---|---|---|
| folded rows | 69 (W8.1–W8.69) | **78** (W8.1–**W8.78**) | **+9** | §A.5 routed **8** (W8.70–W8.77) + §A.5 NWO **1** (W8.78) |
| — of which `≡`-pointer bookings (substance already carried) | — | **8** | — | W8.70–W8.77; each names where the fact lives, none re-books a cure |
| — of which new to this file entirely | — | **1** | — | W8.78 (K-4) |
| gate entries | 24 (15 sharpened + 9 new) | **24** | **0** | **no gate minted, none re-staged, none re-weighted** — every row rides a standing gate (G-8, G-10, G-13) |
| BoundsDelta files | 31 | **31** | 0 | all nine rows land on paths §3 already carries (rows 3, 4, 10, 15 and the ActionBarLayer in-bounds note) |
| cross-edges | 11 | **11** | 0 | W8.70's X-W4/X-W1 order dependency and W8.78's G-F residue are carried **inside their rows** (§9.7's rule, unchanged) |
| census denominator | 271 row lines / 56 records | **unchanged, re-verified** | — | printed command reproduces at HEAD; the verb-filtered **134** is struck as an instrument (§10.1) |

Numbering stays **gapless and contiguous: W8.1 … W8.78**, zero gaps, zero re-mints, zero renames.
**Assignment coverage: §A.5 9/9 id-for-id** (8 routed + 1 NWO), with the roster's own arithmetic
(§A's table: `X-W8-FOLD.md` 8 + 1 = 9) reproduced exactly. Two identities in §A.5's receipt are
namespace collisions with this file's own gate register and with a sibling record — both named at
§10.1, neither reconciled by renaming. Status remains **planned**; no product source was opened by
this repair; nothing here is VERIFIED.
