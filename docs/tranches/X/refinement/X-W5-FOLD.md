SERVED MODEL ID (fold author, line-1 declaration): claude-opus-5[1m]

# X-W5 REFINEMENT FOLD — the one dated addendum for "One route, one scene"

## 0. Header — provenance, posture, reading rule

**Provenance.** Input set = the **value.js corpus** at `docs/tranches/V/megatranche/registry/adjudicated/` (all files NOT `kf-*`/`fr-*`/`pt-*`: 88 component records + `parser-band` · `library-band` · `chassis-fitness` · `layout-gestalt` + `delta-r3/`). Read whole for the 36 commissioned slugs and the two packet slug-sets; the remainder swept by `grep -rln 'X-W5|X.W5|X·W5|XW5'` over the whole corpus (43 files hit; `delta-r3/AdminUsersPanel.worker-F.md` and `delta-r3/Dock.worker-O.md` hit zero; `fr-EquationPanel`/`fr-FunctionInput` hits are fourier-corpus and out of scope). `Dock.md`, `App.md`, `shell-dock-slugeditlayer.md`, `shell-dock-profilesection.md`, `shell-dock-actionbarlayer.md`, `wb-mix-sourceselector.md`, `wb-mix-pane.md` carry **no literal `X-W5` string** and were read whole anyway — they are the packets' spine and route here by identity, not by token.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0..W11.md` and `waves/CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is this fold's sole write. Where a clause below strikes or sharpens a dated spec sentence, the strike is recorded here and the spec's bytes stand.

**Reading rule.** At execution X-W5 reads **`waves/W5.md` PLUS this addendum**, in that order; where they disagree the addendum governs. Nothing here re-stages or re-weights W5's 28 gates (OC-2/CC-096 staging stands); this addendum sharpens predicates, adds rows and locks, declares bounds deltas, and records exclusions and dissents.

**Status.** Every disposition is **ADJUDICATED, not VERIFIED**. `status: planned` is unchanged. **NOTHING here opens product source.**

**Arithmetic (countable from this file's own bytes).** **66 rows** (§1, contiguously numbered W5F-01..W5F-66) · **28 gate entries** (§2: 18 SHARPENED-EXISTING + 10 NEW born-RED candidates) · **21 bounds-delta files** (§3) · **10 cross-edges** (§4). Dedupe by banked identity: 34 of the 66 rows are multi-witness identities; every original id is preserved for life (anti-rename), and no row is booked twice under a second name.

### 0a. Corpus challenged — corrections and locks, never silent

1. **`bindPane` is NARROWED by a standing adjudication.** W5.md §5 X.W5.a says "`usePaneRouter` gains `bindPane(slot)` and **owns the instance refs it dispatches onto**." `Dock.md` **R-8** rules the opposite half: "MT-APP-1's `bindPane` (App.md:104) is hereby **NARROWED to the non-command instance uses** … it **may not resurrect command dispatch through instance refs**, and this wave may not claim C-3", and "G-I's r2 seam text ('the null-ref CURE lands in MT-APP-1') is **SUPERSEDED**: G-I's cure now lands HERE [MT-DOCK-LAYERS-1 / X-W8]." **Recorded, not resolved — see §4 CE-1. A3's cure ownership must be re-ruled before W5 opens.**
2. **`usePalettePorts.ts:88` HAS an X-W5 owner.** Three slug-chain records (`PaletteSlugBar` A-19, `shell-dock-slugeditlayer` A-1's cure sketch, `MigratePalettesDialog` A-25) file the port casts NO-WAVE-OWNER. `CurrentPaletteEditor` **ADJ-m6** books the identical line to **X-W5 (typed contract)**. One identity, one owner: the cast's cure is W5's, and the file enters bounds (§3 BD-01). This is this fold's principal routing correction.
3. **The A3 "RED of record" and W5's own §6 A3 cell are the same measurement as five component records' BLOCKERs** — `shell-paneslot` L-2, `App` C-2/D-1, `Dock` DELTA-1, `shell-dock-actionbutton` ABN-2, `shell-dock-actionbartoggle` ABT-2, `shell-dock-genericactionbar` GAB-1/GAB-11, `wb-gradient-pane` M-GP-1, `wb-generate-pane` GEN-3, `shell-dock-actionbarlayer` registry-blindness event #3. Booked ONCE at W5F-01.
4. **Two "NO-WAVE-OWNER" declarations in the corpus are wrong about W5's text and are recorded as such** — `PaletteSlugBar` A-3 ("X-W5 owns the route↔scene law, but neither's written scope covers this call chain") and `MigratePalettesDialog` A-1 ("X-W5 rewrites viewSchema.ts … the fix itself is nobody's named scope"). Both are correct *as W5.md stands*; §3's bounds delta is what makes them wrong, and only if the delta is adopted.
5. **`AdminListSkeleton`'s lock is binding and easy to violate**: "X-W5's shell `role="status"` region (W5:36/:251) is a **route-settlement** node and **may never be cited as this cure**" [for load-completion / `aria-busy` announcements]. Carried verbatim into G-A7.
6. **`ErrorBoundary` names no X wave** (`grep` over `waves/*.md` → 0) yet eleven of its rows route "X-W5 (rider — surface owned, unit unnamed)". Its cure surfaces are W5-owned (`main.ts`/`App.vue`/`PaneSlot.vue`/`usePaneRouter.ts`) but the file itself is not in W5 §4. §3 BD-07.

---

## 1. Rows

Format: **id** · sources (all witnesses, original ids for life) · severity · mechanism (one line) · cure-shape + LOCKS verbatim where load-bearing · sequencing · dissent.

### 1.A The command channel and the pane slot (X.W5.a / X.W5.c)

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-01** | `shell-paneslot` **L-2** ≡ `App` **C-2/D-1** (both ids preserved) ≡ `Dock` **DELTA-1** (L-35 · L-1-rep · D2-01 · D2-02 · D3-01) ≡ `shell-dock-actionbutton` **ABN-2** ≡ `shell-dock-actionbartoggle` **ABT-2** ≡ `shell-dock-genericactionbar` **GAB-1 + GAB-11** ≡ `wb-gradient-pane` **M-GP-1** ≡ `wb-generate-pane` **GEN-3** ≡ `shell-dock-actionbarlayer` registry-blindness #3 ≡ `shell-dock-mobilemenudropdown` C-3/L-4 fork limb | **BLOCKER** | Mobile `PaneSlot` (App.vue:83-91) passes no `:on-mount`; the sole writer of `colorPickerRef`/`generatePaneRef`/`gradientPaneRef`/`mixPaneRef` is `onDesktopLeftMount`/`onDesktopRightMount` (App.vue:317-332), bound only at :105/:131 — so the entire imperative command channel is structurally dead at mobile, incl. nine dock action-bar buttons (usePaneRouter.ts:196-222, `disabled` never set) and the `@commit-edit`/`@cancel-edit` edit path (App.vue:41-42, which flips `mobilePaneIndex` anyway: **the UI signals success while the edit is DISCARDED** — ABT-2's limb). | **LOCK (Dock R-8, verbatim)**: the typed **`DockCommand` registry** (provide/inject; `demo/shell/dock/commands.ts` owns KEY + types) is the cure and it lands at **MT-DOCK-LAYERS-1 (X-W8)**; `bindPane` is narrowed to non-command instance uses and "may not resurrect command dispatch through instance refs". **LOCK (Dock structure (f))**: "Present-and-inert becomes unspellable: **an absent capability is ABSENT**" — a view providing no commands renders no Tools trigger. **LOCK (GAB-1)**: the renderer limb — `GenericActionBar.vue:28 @action="act.handler()"` invokes unconditionally and no descriptor can ever be disabled — travels into X.W4.d's "unavailable is surfaced, never silence" clause. **KILLED cut-step (GAB-11, binding)**: challenge-C:118-119 / challenge-D:106-107 / challenge-L:129-130 ("retain the left mount callback only for `colorPickerRef`") **may not be followed by any execution seat** — it entrenches a ruled-dead mechanism and contradicts R-8. | A3's *witness* is W5's; A3's *cure* is contested (§4 CE-1). X.W4.d D1-D4 must land the SceneActionSet before or with any W5 registration change. | `Dock` DISSENT-1 preserves worker-O's two-wave shape (overruled); the sanctioned split line is sequenced commits under ONE name. |
| **W5F-02** | `shell-paneslot` **PS-1 / L-3** (+D-8 two-clock, +D-9 per-patch) ≡ `App` **C-3** ≡ `wb-gradient-pane` **M-GP-2** | MAJOR | `onMount` JSDoc ("on mount, and null on unmount") is false: setRef fires on every patch (`runtime-core.cjs.js:5475`), a function ref is invoked unconditionally (`:1814`), and a function OLD-ref is never retired (`:1799-1813`); App.vue:323-328 re-derives identity from route-synchronous `currentConfig` while PaneSlot renders one rAF behind + chunk latency — the outgoing instance is filed under the incoming pane's name. Measured (PS-probe4): `gradientPaneRef := ColorPicker` t=2935 → `:= GradientPane` t=4210 (1275 ms foreign-instance window). | Report the key with the instance, **or invert** — `defineExpose({ instance, key })` + one derivation — so cross-wiring is unrepresentable. Inline arrow at PaneSlot.vue:124 → a stable bound capture. | Lands with W5F-10's typed refs. **R-8 note**: MT-DOCK-LAYERS-1 "may not claim C-3" — C-3 stays W5's. | — |
| **W5F-03** | `shell-paneslot` **L-1 / PS-2 / D-5 (listener half)** ≡ `wb-extract-pane` **XP-1** ≡ `wb-extract-workbench` **XW-2** ≡ `wb-extract-controls` **EC-18** ≡ `wb-extract-imageeyedropper` EY-8 limb ≡ `wb-mix-pane` MX-31 cross-ref | **BLOCKER** | PaneSlot.vue:120 owns the app's ONLY `<KeepAlive>` and publishes no activation contract; demo census = **1 `onActivated` / 0 `onDeactivated`**. `runtime-core.cjs.js:6596-6598` returns into `deactivate` before any unmount hook, so every `onBeforeUnmount` teardown in a cached subtree **runs never**: ColorPicker.vue:377 `window keydown` with `preventDefault` arms + `useMagicKeys()`; ExtractWorkbench.vue:281 `stopCamera` (the ONLY `track.stop()` path); useImageQuantize.ts:148-151 `worker?.terminate()`; useExtractSession.ts:194-196 debounce timer; ImageEyedropper.vue:242-245 window keydown + `sampler.dispose()`. `:max="6"` over exactly the six non-admin left panes ⇒ **extract is never evicted ⇒ the leak is session-permanent, not LRU-bounded**. | The shell owns one gated key listener / the registry; the 23-line PaneSlot header states the activation contract FIRST. **XP-1 ADDENDUM CANDIDATE (explicit): "no W5 scope item names deactivation teardown"** — propose the E-3 addendum with the born-RED gate at §2 N1. | Independent of W5F-01. The WebGL-context-retention hypothesis rides X-W2's re-measure. | — |
| **W5F-04** | `shell-paneslot` **D-2** (+the **D-1 KILL** residue) | MAJOR | No `mode` attr (PaneSlot.vue:112-119); no `position:absolute` on any `vj-enter` leave override (animations.css:228-277); wrappers are flex-column/block/flex — the default (simultaneous) mode leaves **two in-flow panes in one ordinary box for the whole overlap**. | **COUPLED ARCHITECTURE LOCK (the D-1 ruling, verbatim)**: "no wave may delete the simultaneous mode, the rAF mirror, or add loading states in isolation; they move together or not at all." **GATING LOCK (UNPROVEN row 1)**: "**No wave may delete PaneSlot.vue:12-23 before [the out-in re-probe] runs**" — script + RESULTS committed against `demo/shell/PaneSlot.vue` itself, real `--duration-fast` leave + `defineAsyncComponent` panes + the rAF mirror. | Gated behind the re-probe; W5F-07 (loading states) lands BEFORE or WITH any transition-mode change, never after. | `shell-paneslot` records the four incompatible dispositions of the same forty lines (D-1 delete / D-8 canon-violation / L survives / PS-3 keep-but-gate) as a **fleet-calibration fact**. |
| **W5F-05** | `shell-paneslot` **D-3** (four limbs) | MAJOR | The PaneSlot header's defensive claims are false four ways: (1) "CROSS-FADES" — opacity pinned 1 both sides (animations.css:228-236; the CSS's own comment says "travel, not a fade"); (2) "height-bounded (min-h-0 + `--content-max-h`)" — the token caps `.pane-container` (shell.css:75), not the slot, and is false exactly for the co-mount it is invoked to defend; (3) "never jumps the layout" — refuted by D-2; (4) "a late async chunk's arrival lands through `.overture-appear-*`" — impossible: appear hooks are substituted only while `!state.isMounted` (`runtime-core.cjs.js:1521-1553`), so `@after-appear` can never fire for a resolved chunk. | The header is rewritten to the truth by whatever wave touches the file. Doc mirror rides W5F-18. | — | — |
| **W5F-06** | `shell-paneslot` **D-4** (cross-booked to App's ledger; App.md carries no ghost row) | MAJOR | `pane-wrapper--ghost` flips at the **START** of a dual→single swap: App.vue:120 binds it off the INCOMING config; :403-409 sets `visibility:hidden; position:absolute; opacity:0; content-visibility:auto`; :59 drops `pane-container--dual` in the same frame — the departing pane is hidden/absolutized mid-leave and the slide distance doubles. Absorbed: the ghost's exit fade is dead code (`transition-opacity duration-200` transitions opacity alone while visibility/position/content-visibility flip on frame 1). | Flip the ghost AFTER the leave; one geometry truth. Motion tokens (`duration-200` raw utility) → X-W10. | Lands in the same commit as the `regions[]` wrapper cut (see W5F-35). | LC's K-8 ("misfiled — belongs to the App seat") DEMOTED to a scope annotation, recorded. |
| **W5F-07** | `shell-paneslot` **D-6** ≡ `ErrorBoundary` **EB-4** ≡ `wb-gradient-pane` D-table-"initializing" (+DU's async-fallback fold) ≡ `wb-generate-pane` **GEN-33** | MAJOR | Ten bare `defineAsyncComponent`s (usePaneRouter.ts:69-78 — **zero** `loadingComponent`/`errorComponent`/`delay`/`timeout`/`onError`); App.vue:50 wraps the whole grid; ErrorBoundary's `caught` latches with `reset()` as the only clearer and no route watch. Shipped detail string for the class: "Importing a module script failed." — the stale-chunk event whose only cure is a reload, and **no reload affordance exists**. | Per-slot boundary keyed on `componentKey` + a real loading affordance. **CURE-LOCK (EB R-1, verbatim)**: "loader options alone do not stop parent-chain propagation into the boundary — **EB-4 and EB-2 land together or the latch survives**." **CURE-LOCK (the D-1 kill)**: this lands BEFORE or WITH any transition-mode change, never after. | Pairs with W5F-53 (EB-2's transposition) and W5F-04. | — |
| **W5F-08** | `shell-paneslot` **D-7** (final limb KILLED) | MAJOR | `:max="6"`/`:max="9"` are hand-counted against a route table in another file: LeftPane = 11 members, keys are full view names, and the LRU prunes the **OLDEST** key on a miss (`:2975-2978`) — admin visits evict the non-admin panes, the exact opposite of App.vue:97-100's claim. Scope limits: the right slot's comment is CORRECT (the `empty` key renders a Comment; KeepAlive bails before caching). | Derive the bound from the registry, or drop it. | Interacts with W5F-03 (`:max="6"` is why extract's leak is permanent) and W5F-37 (C5). | **STRUCK**: D-7's "breakpoint cross defeats App.vue:113-114's stated purpose" — the comment governs the desktop-right ghost wrapper, not breakpoint crossing. Uncitable. |
| **W5F-09** | `shell-paneslot` **PS-9** (+D-8's §5.1 limb) | MAJOR | A wholesale `<main>` replacement with no announcement, no busy state, no focus contract: PaneSlot.vue:111-129 renders no wrapper/role/aria; App.vue:47's static label never names the arrived pane though `leftLabel`/`rightLabel` exist unused; during the co-mount **two live a11y subtrees coexist, neither inert nor AT-hidden**, against VISUAL-CONSTITUTION §5.1:118 ("no observer or microtask sees zero or two active subtrees"). | "The atomic-commit + announcement contract **IS** the 'one route one scene' charter." → gate §2 N6. | Consumes W5F-27's status region; must not consume it as the load-completion cure (the AdminListSkeleton LOCK). | Honest negative preserved: PaneSlot contributes zero rows to the REPORT's tap-target/nameless-button counts. |
| **W5F-10** | `shell-paneslot` **PS-6 / D-9 / D-10 hygiene cluster** (+2 LC missed finds) ≡ `App` **D-13** ≡ `wb-gradient-pane` **L-3** | MINOR | Dead `max = 5` default (:32); `any` in the public prop surface (:52) propagating to `ref<any>` ×3 (App.vue:317-319) and `PaneActionRefs { Ref<any> }` ×3; `onMount!` non-null re-asserting a separate read (:124); `transitionName: ""` as a load-bearing boot sentinel; the `\| undefined` arm in `component: Component \| null \| undefined` is uninhabitable. | Dies with the typed channel. **ARBITER CORRECTION (carry it)**: `colorPickerRef` at App.vue:223 **IS** typed `InstanceType<typeof ColorPicker> \| null` — the corpus's occasional "`ref<any>` ×4" is **wrong by one**. **PROOF-OF-COST (carry it)**: usePaneRouter.ts:100 already types one handle narrowly (`{ commitEdit; cancelEdit } \| null`) eleven lines from the `Ref<any>` triple — the cure costs three interfaces. **CORRECTION to all three gradient axes**: GradientPane.vue:9's own ref IS properly typed; the untyped hops are shell-owned. | With W5F-02 and W5F-64. | — |
| **W5F-11** | `shell-paneslot` **L-4** | MAJOR | `usePaneRouter.ts` is a four-concern module (registry :69-95 · prop factories :126-161 · slot computeds :163-184 · DockAction types + per-view tables :38-58/:186-228) and the dock **imports its own types back out of it** (Dock.vue:24, GenericActionBar.vue:4 — type-only; the defect is ownership, not bundle weight). | The action/type home moves with the registry; the module **split** is X-W8's. | Split sequenced after the registry decision (§4 CE-1). | — |
| **W5F-12** | `shell-paneslot` NEW-LC-2 ≡ `Dock` **DELTA-5** (structure (h) + **G-L**) ≡ `shell-dock-genericactionbar` **GAB-14** ≡ `shell-dock-actionbartoggle` **ABT-9** ≡ `shell-panesegmentedcontrol` **PSC-9** ≡ `App` **D-7** ≡ `shell-dock-mobilemenudropdown` C-3/L-4 | MAJOR | Two disagreeing `isDesktop` predicates: App.vue:310-312 `useBreakpoint("(min-width:1024px) and (min-aspect-ratio:1.1)")` (whose own comment says the compound query exists "so they can never disagree") vs Dock.vue:71 `useMediaQuery("(min-width: 1024px)")` — at 1024×1366 portrait the pane grammar is mobile while the dock believes desktop. PSC-9's fan-out: `isDesktop` drives `:always-expanded="!isDesktop"` (:132) so the **5 s collapse arms** exactly where the pane switcher is the sole route to the second pane; plus `:is-desktop` on DockViewSelect (:172) and ActionBarToggle (:186) and `mobileEditActive` (:72). Third + fourth witnesses: `lg:` display utilities at ProfileSection:48/:127, MobileMenuDropdown:38, ActionBarToggle, PaneSegmentedControl's 639 px fork. Fork census: **38 occurrences across 13 files**; MMD reproduces **78.7 %** twin-byte fork (170/108/85, longest run 28) with `probe/orphan-2-after-resize-1440.png` committed. | W5's C3 is the **source** arm; Dock's **G-L** (`grep -c useMediaQuery demo/shell/dock/Dock.vue == 0` + the 1024×1366 dual-grammar probe, frame `collision-1024x1366.png` committed) is the **rendered** arm and lives at X-W8. Both must be green or the fork was renamed, not deleted. | C3 cannot close while G-L is RED. | `Dock` **R-19**: D2-03's BLOCKER severity DEMOTED to MAJOR (both grammars render; every control operable) — finding and G-L untouched. |
| **W5F-13** | `shell-paneslot` NEW-DU-4 | MAJOR | VISUAL-CONSTITUTION §3 law 6 (`:32`, verbatim) forbids the mobile "global pane selector" **and** "left/right split state" in the same sentence — and the shell ships both: Dock.vue:15/:197-198 renders `PaneSegmentedControl` under `.dock-mobile-panes`; the split state is `mobilePaneIndex` (useViewManager.ts:59-70) consumed by usePaneRouter.ts:177-184. Either the canon or the shell must move. | The canon re-author is in **CC-004's charter** — owner-visible adjudication, authored here, not drifted. | Precedes W5F-33 (the deletion) so the deletion is canon-warranted. | — |
| **W5F-14** | `shell-paneslot` NEW-LC-5 | MINOR | `whenColorPickerReady`'s give-up has no error posture: a 2 s user-visible stall (40×50 ms poll, usePaletteWiring.ts:37-58) terminates in `console.warn` and a `void` return no caller can observe — **on mobile this is the ONLY signal that an edit died**. | Dies with the registry; **until then the give-up needs a user-facing answer**. | Interlocked with W5F-01. | — |
| **W5F-15** | `shell-paneslot` NEW-LC-3 ≡ `shell-panesegmentedcontrol` **PSC-22** ≡ `ErrorBoundary` **EB-34** | INFO | Name collision: `usePaneRouter.ts:61` exports `interface PaneSlot` shadowing `PaneSlot.vue` in the same two-file directory — the one module that renders slots from the router's output cannot import both under their own names. | **Rename the interface BEFORE the transposition lands** (`PaneSlotResolution` / `ResolvedPane`). Both files are W5-owned. | Ordering constraint, not a defect gate. | — |
| **W5F-16** | `shell-paneslot` **PS-5** | INFO | `component` is never watched; a component change under a stable key would be ignored forever. Unreachable today (`componentFor` is pure in the key). | A single `slot` object prop states the co-variance. | — | Author-labelled hypothesis; kept as labelled. |
| **W5F-17** | `shell-paneslot` **PS-3** (demoted) | MINOR | PaneSlot.vue:90 gates the one-rAF deferral on a **string** (`if (!transitionName)`) while App.vue:302 computes `prmInstant` and never passes it down — a motion predicate stood in for by a string test. | → **X-W10** (motion policy). **PREMISE LOCK**: only `motion-quarantine.md §1`'s measured cascade may be cited (served value `transition-duration: 0.1s` with the property list rewritten) — the demo 0.01 ms clamp restatement is **STRUCK**. | Not W5 work; recorded so W5's D5 close does not absorb it. | — |
| **W5F-18** | `shell-paneslot` **PS-7** (+DU's verified miss) ≡ `shell-panesegmentedcontrol` **PSC-17** | MINOR | Shipped docs assert the forbidden mode: `demo/DESIGN.md:278` names `panes/PaneSlot.vue` (no such dir); `e2e/smoke/mobile/walk.spec.ts:48/:54/:137` describe `Transition mode="out-in"` and use it to justify `test.setTimeout(60_000)`; DESIGN.md:270 cites the PRM guard at `animations.css:32-60` (the stagger utility — the guard lives at :184-212). PSC-17 adds: the same spec's `BouncyTabs` (:16/:31 — absent from glass 7) and `lg:flex` (:19-20 — the retired width-only mechanism). | Docs die with the W5 file touch; **the spec re-point must correct BOTH stale prose pairs, not just the rename**; the timeout-justification fix rides X-W1 ("a gate that cannot fail for the right reason"). | Rides the `e2e/smoke/mobile/walk.spec.ts` modify-carve. | — |
| **W5F-19** | `shell-paneslot` NEW-DU-5 | MINOR | On mobile **every swap enters from the LEFT and leaves to the LEFT**: App.vue:77 gives the mobile wrapper `--left`, so animations.css:228-231 governs every mobile pane and the `--right` geometry (:233-236) is unreachable **by construction** — forward and back are motion-identical and the segmented control's direction is never expressed. | → **X-W10** at its home; carried here because it is the exact input to D3's region-role re-key (a re-key that leaves forward/back motion-identical has not cured it). | Input to W5F-45. | — |
| **W5F-20** | `shell-paneslot` NEW-DU-7 · NEW-DU-9 | MINOR/INFO | `:on-appeared` on the desktop-right slot wires a **documented empty function** (`noteRightPlateSettled`, useOverture.ts:230-232, body = a comment) and is doubly dead (right pane is async; `@after-appear` cannot fire for a late chunk per W5F-05(4)). PaneSlot parameterises the swap grammar but **hard-codes the boot grammar** (`appear-from/active/to-class="overture-appear-*"` string literals naming rules in the app root's private `boot/overture.css`). | → **X-W2** (boot) / X-W2+X-W8 seam note. | Excluded from W5's close; recorded so nothing is lost. | — |

### 1.B SLUG-CHAIN — the `setActiveTab("saved")` route throw

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-21** | `PaletteSlugBar` **A-3 (N-1)** ≡ `shell-dock-slugeditlayer` **A-1** ≡ `MigratePalettesDialog` **A-1 (L-1)** | **BLOCKER — URGENT** | `useSlugMigration.ts:55/:69/:76 setActiveTab("saved")` → `usePalettePorts.ts:88` unchecked `(tab: string) => depsSwitchView(tab as ViewId)` → `useViewManager.ts:78 router.push({name:"saved"})` → **synchronous throw**: vue-router 5.1.0 `pushWithRedirect:1243` calls `resolve(to)` synchronously → `dist/vue-router.js:670 if (!matcher) throw createRouterError(1,…)`. `ViewId` (viewSchema.ts:35-49) = 14 members, router/index.ts = 14 registered names; neither contains `"saved"`; the :37 catch-all is a **path** redirect a named push never matches; the :73 same-view guard can never fire. **Empirically verified twice, independently** (RR-LC's node run; the SlugEditLayer seat's own memory-history run). Three live branches, each verified: **(a) admin** (:52-57, no try/catch) — teardown + `adminLogin` land, then the throw escapes as an unhandled rejection because SlugEditLayer.vue:54 does not await, so the layer's own try/catch cannot see it, and the view never switches; **(b) direct slug, no local palettes** — `await deps.userLogin(value)` **SUCCEEDS** at :75, then :76 throws; the LOGIN catch (:77) sees a non-`ApiProblem` and writes the "error" to A-2's permanently-null ref — a successful login converted into a silently-swallowed router error; **(c) migration** (:69) — degraded at :112-114 to `console.warn("Migration action failed:")`, so **every palette migration reports failure after succeeding**. Fifteen challenge runs quoted the offending line verbatim; none checked whether the route exists. | Cure: `setActiveTab("palettes")` **or** delete the tab-name legacy whole (`activeTab: Ref<string>` + the :24 else-fallback are the pre-router tab model "saved" belonged to). **STRUCTURAL cure = W5F-22** (typed deps make the hole unspellable; vue-tsc enforces it for free in three places). **ORDER LAW (MigratePalettesDialog A-1, verbatim, BINDING): "A-1 must land BEFORE or WITH A-3's sink repair — mounting the sink first converts a silent bug into a user-visible lie on a successful login."** | **A-1 before/with W5F-24.** First pick of whatever wave adopts it. | All three records file this **NO-WAVE-OWNER**; §0a n.2 + §3 BD-01/BD-02 are this fold's contrary routing. |
| **W5F-22** | `PaletteSlugBar` **A-19 (N-2)** ≡ `CurrentPaletteEditor` **ADJ-m6** ≡ `MigratePalettesDialog` **A-25** limb ≡ `shell-panesegmentedcontrol` **PSC-15(b)** | MAJOR | Two unsound casts at the port boundary defeat the ViewId type system and are what let `"saved"` compile: `usePalettePorts.ts:87` `currentView as Ref<string>` (widening a **writable, invariant** Ref) and `:88` `(tab: string) => depsSwitchView(tab as ViewId)` (unchecked narrowing). The correct guard (`isViewId`) ships and is used route→state (useViewManager.ts:43-46) but not state→route. A-25 adds the double-launder (`as unknown as Ref<ViewId>` then `as Ref<string>`) and the **dead `activeTab` dep** (the sole caller always supplies `setActiveTab`, so the writing else-branch is unreachable — a required dep with no live reader whose only possible act is a write to a readonly computed). PSC-15(b): `useViewManager.ts:91 currentView as unknown as Ref<ViewId>` advertises a read-only computed as **writable** — any consumer can type-check a write Vue silently drops. | Type the deps `Ref<ViewId>` / `(id: ViewId) => void`; delete both casts; type `currentView` as `ComputedRef`/`Readonly` honestly; delete the dead `activeTab` dep. **ROUTING CORRECTION (this fold)**: `CurrentPaletteEditor` **ADJ-m6 books this line to X-W5 (typed contract)** — an X-W5 owner exists; the three slug-chain "NO-WAVE-OWNER" cells are superseded **only if §3 BD-01 is adopted**. | Lands WITH W5F-21 as its structural cure. | — |
| **W5F-23** | `PaletteSlugBar` **A-1** ≡ `shell-dock-slugeditlayer` **L-1** (demoted MAJOR) **+ A-9** | **BLOCKER (orphan)** | `PaletteSlugBar.vue` (243 lines) renders on **zero** routes: `<PaletteSlugBar` appears in no template; the only refs are an `import type` + `ref<InstanceType<…>>` (useSlugMigration.ts:6/:30) and two barrel lines (`browser/slug/index.ts:3`, `browser/index.ts:44`); the live twin is `SlugEditLayer.vue` at Dock.vue:149. `demo/palettes/browser/index.ts` — the self-declared "TOP-LEVEL SEAM… stable public API" — has **zero importers**, and its :44 line is the only non-type ref keeping the twin nominally exported. | **Deleting the file discharges eleven rows outright** (A-4, A-17, A-23, A-24, A-25, A-26, A-27, A-28, A-32, A-33, A-34) and halves four more (A-5/A-8/A-15/A-16); **seventeen rows still ship** through SlugEditLayer / ProfileSection / useSlugMigration / the export twin-path / the 51-site dead-prop axis. **BOUNDS LOCK (A-1, verbatim)**: nearest home is X-W8.d dead-surface subtraction, "the unit's file bounds do not currently name this file; **the X-W8.a census addendum must add `PaletteSlugBar.vue` + both barrel rows + the `slugBarRef`/`import type` plumbing in `useSlugMigration.ts`, or the deletion is lost**." | **Not X-W5** — recorded so W5 does not inherit it; §4 CE-6. | — |
| **W5F-24** | `PaletteSlugBar` **A-2 (L-3)** ≡ `shell-dock-slugeditlayer` **D-1/C-3/L-2(c)(d)** ≡ `MigratePalettesDialog` **A-3 (L-2+C-14)** | **BLOCKER** | The login-failure channel has three implementations and zero working ones: `slugBarRef` is created (useSlugMigration.ts:30), written through four `slugBarRef.value?.setError(…)` calls (:84-87), returned (:121), and **bound by nothing** (all 6 repo hits are inside that one file); the live twin declares `slugError` (SlugEditLayer.vue:13), writes it six times (:18/:43/:49/:59-62) and **renders it zero times** (template :75-119); the live caller does not await, so its catch (:57) — which re-runs the retired 409/404/429 substring idiom — can never observe the rejection. Reachable worst case: :48-52 sets "Already signed in." and returns — pressing submit on your own slug does literally nothing. | A real reactive error sink on `useSlugMigration` consumed by SlugEditLayer's template, carrying **both** channels + a per-palette failure count (A-20's migration channel folds in). Terminal home per canon = the **W23 Account Dialog** (VISUAL-CONSTITUTION.md:222); interim = render the state that exists. | **Sequenced AFTER W5F-21** per the order law. | — |
| **W5F-25** | `shell-paneslot` **L-5** ≡ `shell-dock-slugeditlayer` **L-3** ≡ `Dock` **L-6** (BUILD there) ≡ `shell-dock-profilesection` **L-4** | MAJOR | The shell reaches identity through a feature at five `SESSION_PORT_KEY` edges (Dock.vue:18, DockViewSelect.vue:8, ProfileSection.vue:14, MobileMenuDropdown.vue:13, SlugEditLayer.vue:5) plus the direct ColorInput.vue:133 edge and a type-only `shell → palettes → shell` back-edge; nothing in `sessionPort` (:127-135) is palette-specific. | **CURE-DETAIL RULING (Dock R-6, F wins)**: `demo/platform/auth/session.ts` owns the KEY + the `SessionPort` TYPE; the **composition root injects the implementation**, because `onRegenerateSlug`/`onSlugSwitch` are slug-migration ACTIONS the dock calls — a "pure identity payload assembled in platform/auth" would strand three consumers or drag slug-migration into platform. No compatibility re-export. | **X-W8 (MT-DOCK-LAYERS-1)**, not W5. §4 CE-6. | Dock DISSENT-4: worker-O's soft dissent (give the whole port to MT-APP-PORTS-1) preserved with the ColorInput hedge as standing law. |

### 1.C One route, one scene — identity, voice, boot

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-26** | `App` **D-4 (H1 arm)** ≡ `Dock` **L-21** ≡ `PaneHeader` **PH-8** ≡ `ColorNutritionLabel` **CNL-12** ≡ `wb-extract-pane` **XP-5** ≡ `AboutPane` **AB-12** ≡ `ErrorBoundary` **EB-11** | MAJOR (canon-hard) | `grep -rn '<h1' demo/` → **0**; `h1: 0` on all 60 audit captures (MT-F003). The pane title is a hardcoded `<h3>` (PaneHeader.vue:21) which on About parents **six `<h2>`** at three sizes (heading-order, WCAG 1.3.1); census h1×0 / h2×6 / h3×4. The constitution's seven focus origins targeting "destination H1" are structurally unimplementable. | One **visible** `<h1 tabindex="-1">` inside `<main>` bound to `VIEW_MAP[currentView].label`. **STRUCK AND UNCITABLE (App D-4 ruling)**: the sr-only / visually-hidden H1 cure — §5.1 requires a **visible** H1. **RESIDUE (PH-8)**: the PaneHeader heading-level seam (`as`/`level` prop, or `CardTitle as` adoption — the producer seam ships **unconsumed**) is in **no wave's mechanism — NO-WAVE-OWNER, with X-W5 named as the candidate**; §3 BD-06. **RIDER (EB-11)**: X-W5's one-H1-per-route hard gate **must cover the caught (ErrorBoundary) scene or the gate has a blind arm** — the plate that owns the whole `<main>` contains zero headings, and its only e2e asserts `textContent.length > 0` while claiming to assert a heading. | A5's four falsifiers (0 / ≥2 / invisible / label-disagrees) must be joined by a fifth: outline order. | `App` **C-4's "no h2" clause is STRUCK** (6 `<h2>` sources, ~10 rendered on `/#/`) — uncitable in any close. |
| **W5F-27** | `App` **D-11** ≡ `AboutPane` **AB-20** ≡ `wb-extract-pane` **XP-6** ≡ `ErrorBoundary` **EB-30** ≡ `AdminListSkeleton` (LOCK source) | MAJOR | `/#/does-not-exist` renders **byte-identical** to `/#/` with the same title (router/index.ts:36 catch-all, silent redirect); `grep -rn 'role="status"' demo/shell/ demo/color-picker/` → **0**. §5.1 row 4 requires the redirect reason in one polite status + a distinct destination title/H1. XP-6: extract's TERMINAL states announce nothing (in-flight has `role="status"`; the error line is a plain div; the developed result carries no region — success, failure and "no colours found" are indistinguishable from silence). EB-30: on catch, `<main aria-label="Color tool panes">` (App.vue:47) is static markup naming a scene that no longer exists — landmark navigation and heading navigation are **both** dead ends. | One shell-owned polite `role="status"` written by the route-settlement commit. **LOCK (AdminListSkeleton, verbatim, BINDING)**: "X-W5's shell `role="status"` region (W5:36/:251) is a **route-settlement** node and **may never be cited as** this cure" — for load-completion / `aria-busy` announcements (AB-20's row and the AdminListSkeleton row both stay NO-WAVE-OWNER; the house-wide completion contract is the boundary's). XP-6's terminal feed into the region is an **addendum candidate** on this wave. | With W5F-26 (same installDocumentTitle hooks). | **App arbiter correction, carried**: `role="status"` DOES exist in demo (GradientCodeEditor.vue:111, EmptyState.vue:28); worker-O's "zero polite live regions exist" **over-claims** — the true claim is shell-scoped. |
| **W5F-28** | `App` **C-1 · L-1 · L-3 · L-11 · L-12 · C-5 · C-6 · C-13** | **BLOCKER (C-1/L-1)** | Route-untrue boot: `index.html:205-213` mounts without `router.isReady()`; useViewManager:44-46 seeds the wrong pane; PaneSlot's one-rAF commit swaps the child and cancels `appear`; `@after-appear` is the SOLE non-PRM caller of `noteLeftPlateSettled` — b3 never fires, terminal for the session on 10/14 routes. L-1: no `main.ts` existed at the corpus HEAD; App.vue carried five undeclared boot contracts. L-3: `ground.ts` carries browser contract AND a node-only HTML transform in one module. L-12: two URL-read owners; `useColorUrl`'s boot apply is always false (async initial navigation ⇒ empty `route.query` at setup); `hydrate.ts:50`'s rationale is FALSE (install precedes mount). C-5: value import used only in a type position. C-6: a residual booked on a `./demo/**` sideEffects mark `package.json:20` contradicts. C-13: App.vue:0 `watch(` while four files name it watcher-owner. | The composition root: `await router.isReady()` before mount; four CSS imports lifted **in cascade order** (utils → foundation → focus-ring → overture); `useGlobalDark()` + `provideApiClient()` at app level; `ground.ts` split by lifetime → `plugins/vite-ground-tokens.ts`; the dead boot-apply conjunct deleted; the hydrate rationale corrected. **STRUCK (App L-9, REFUTED)**: the overture.css colocation is a documented, rationale-bearing choice — **the wave does NOT move the file**; its import merely rides to main.ts preserving order. Uncitable. | **PARTIALLY LANDED**: `c4af0ef9` created `demo/color-picker/main.ts` and moved the production entry (CC-002, App G1/G2 discharged); it did **not** land the `isReady()` await, the CSS lift, or the provides — hence A1, and hence A2/A6 carry MEASURE-AT-OPEN rather than a re-asserted number. **`ErrorBoundary` kill, carried**: the corpus BLOCKER "the boot module cannot exist" is **killed against the live tree** — main.ts has existed since `c4af0ef9`. | — |
| **W5F-29** | `App` **C-7** ≡ `ColorNutritionLabel` **CNL-26** (+`AboutPane` observed-anomaly 2) | MAJOR | Cold boot rewrites the address bar with state no user action produced: settles at `?space=lab&color=lab(92%25+88.8+20+/+82.7%25)` on both route classes. **Mechanism PINNED**: `useColorUrl.ts:79-82`'s Model→URL watch has **no `immediate`** — the setup-clamp trigger is REFUTED (the clamp at App.vue:245 precedes watch registration at :377); the writer is a post-setup model write and **must be pinned before curing**. CNL-26 sharpens the read side: `applyUrlToModel` **rejects a space-only URL outright** (`if (!space || !color) return false`, :27) and the back/forward watch fires on `route.query.color` only — `?space=xyz` alone is a no-op **twice over**, then overwritten by the model→URL sync. | Boot URL rewrite becomes a no-op; live user edits must still write the URL (a gate that also kills live sync is **over-cured** — A6's own falsifier). **NOT booked against §6.2** (the `value-ground/3` seed-latch contract is unimplemented: 0 hits for `data-ground-schema\|SeedToken\|--ground-seed`); the charge is the shell violating its own one-commit law. Cross-ref CC-081 (ICtCp `cp`→`Ct` mis-resolve rides X-W9). | A6 MEASURE-AT-OPEN. | **Residue (AboutPane #2)**: `?space=jzazbz` did NOT take effect (measured the OKLCh doc) while `display-p3`/`rec2020` applied cleanly — either deep-linking the two non-CSS-serializable spaces silently falls back (a REAL unreported defect) or the evidence row is invalid. Re-run with a **URL-echo assertion**. |
| **W5F-30** | `App` **C-8 / L-2 / D-9** | MAJOR | `<body class="relative" id="app" data-paper-field>` (index.html:225) is mount container AND portal host; `app.mount` clears body innerHTML, so a static in-body boot-error region is structurally impossible. `mountHostIsBody: true` triple-measured. | `<div id="app">` **inside** the existing body; **BOTH body stamps stay** (`data-paper-field` must dominate the two live `<Teleport to="body">` sites; `class="relative"`'s load-bearing status is unproven either way and no gate requires its deletion). **D-E portal-integrity delta MANDATORY** on any `index.html` body change: body-direct-children positioning census + `[data-paper-field]` ancestry of a portalled dialog, identical before/after. | Coupled to the same index.html edit as the entry cure. | **STRUCK AND UNCITABLE**: D-9's stamp-deletion cure arms; the template is a **two**-element-root fragment (comments are not element roots), not three. |
| **W5F-31** | `AdminNamesPanel` **D-18** + `shell-dock-dockviewselect` **L-22** | INFO→MINOR | The admin sub-view tab is not addressable: it resets on every sub-view switch and Approved cannot be deep-linked — routes own the state, and a URL home is also what L-7's pagination needs. L-22: **zero** link elements in demo (`RouterLink`/`router-link` census = 0) — nothing middle-clickable, ⌘-clickable, or copy-link-able; no link graph. | D-18 → X-W5 (routes own the state). L-22 stays **INFO at the component**; the **anchor-desert question is flagged whole for the formation boundary, with X-W5's one-route-one-scene family named as the natural venue**. | Needs §3 BD-08 (`router/index.ts`). | Reader-2's product-scope MAJOR on L-22 sustained only at boundary scope. |

### 1.D One mount, one column — the fork and the schema die

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-32** | `App` **D-2/D-3** (RESCOPED) ≡ `AboutPane` **AB-9** ≡ `AdminAuditPanel` **AAP-13** ≡ `AdminTagsPanel` **ATP-4** ≡ `AdminNamesPanel` **D-4 + D-1.3** ≡ `AdminFlaggedPanel` **AF-22** ≡ `EmptyState` **ES-11** (stacking limb) ≡ `wb-generate-pane` **GEN-36** ≡ `BlobPane` row 5 ≡ `BrowsePane` **B3** (companion limb) | MAJOR (fan-out) | `viewSchema.ts` encodes a **physical** left/right axis: **13 `right:"` rows** (gate C8's measured RED). All five admin routes declare `right:"palettes"`/`rightLabel:"Palettes"`; `picker.right = "about"`; `browse` fills the companion with a **different route's pane**; `blob` **omits `defaultPaneIndex`** while its shape-identical siblings (`palettes`:122, `mix`:150) set `1`, so `/#/blob` below lg and at 200 % zoom opens on the Picker. One deletion kills nine records' rows. | Ordered `regions[]` of semantic roles (stage / inspector / action). **BINDING NOTE (PSC-3, from the killed D-4-cure)**: the migration domain is the **14-member `ViewId` union** — five admin views carry `(left,"palettes")` pairs **outside** the 11-route inventory at VC:58, and `/easing` is **in** the inventory with **no `VIEW_MAP` row**. A route split scoped to the inventory hits five undefined cases and one phantom. **BINDING ACCRETION (PSC-12)**: the `regions[]` type must make **pane-without-label unrepresentable** (a `{pane,label}` pair or equivalent) or the `?? ''` masking seam re-forms at the new seam. **BINDING (AdminNames M-DU11)**: `right: null` on admin discharges four canon rows AND the route's only a11y-battery findings in one word. | C8 + typecheck are the paired arms. | `ATP-4` DEMOTED BLOCKER→MAJOR on identity law (the class is booked MAJOR at AAP-13); `ATP-3` RAISED MINOR→BLOCKER on the same law — both recorded. |
| **W5F-33** | `shell-panesegmentedcontrol` **PSC-1 · PSC-3 · PSC-4 · PSC-12 · PSC-13 · PSC-14 · PSC-16 · PSC-20 · PSC-23** ≡ `Dock` (render site) | MAJOR | `PaneSegmentedControl.vue` (52 lines) is a **pure passthrough**: `variant="pill"` is the producer default; net contribution = a `0\|1`↔`"0"\|"1"` coercion, a dead `v-if`, and a `:deep()` padding delta. Its three adopted-by-omission producer defaults describe no real control: no `semantics` ⇒ `toggle` ⇒ `role="group"` + per-button `aria-pressed`; `activation: automatic` ⇒ **focus MOVE commits selection** with 2-option wrap; `SegmentedTabOption.controls` never populated and the governed panel (App.vue:77's `.pane-slot-mobile`) carries no `id`, no `role="tabpanel"`, no `aria-live`. A keyboard user arrowing through an announced button group **silently destroys and rebuilds the page's main content per keypress**. | Delete the file + the Dock Picker\|Blob toggle. **REFUSAL (PSC-1, BINDING)**: the `semantics="tabs"` interim patch prescribed by two axes is **REFUSED** — it silently disarms the producer's drag path (`drag.css` keys its only hit-test rule on `[aria-pressed="true"]`, which `tabs` semantics stops emitting) and turns **NINE** `walk.spec` assertions RED (:95, :96, :108, :109, :118, :119, :146, **:156, :157** — corrected from the reader's "seven"). **No interim patch lands.** **BASELINE (PSC-23)**: render-site count is exactly **1** (Dock.vue:15 import + :198 render) — C4 executes against a known-true baseline. | C4 + `npm run typecheck` (the dangling-consumer arm). | `PSC-11` sibling arms: `MixSourceSelector.vue` **is** in W5 bounds (rider: pass `aria-label` when touched); `AdminNamesPanel.vue` is in **no** named wave — NO-WAVE-OWNER one-liner. |
| **W5F-34** | W5 gate **C3** + `Dock` **G-L** (rendered arm) + W5F-12's witnesses | MAJOR | Source census: **38 `useBreakpoint`/`isDesktop`/`isMobile`/`mobilePaneIndex` occurrences across 13 files**. C3 is source-scoped (L-9) with `node_modules` excluded so a producer's own breakpoint use cannot false-red it. | C3 GREEN is necessary, not sufficient: **Dock G-L** (`grep -c useMediaQuery demo/shell/dock/Dock.vue == 0` + the 1024×1366 dual-grammar probe) is the rendered arm and lives at X-W8. A fork **renamed** rather than deleted passes neither. | C3 cannot close while G-L is RED (§4 CE-1/CE-6). | — |
| **W5F-35** | W5 gate **C6** + `shell-paneslot` D-4 geometry | MAJOR | The T-45 blur carrier lives on `.pane-wrapper`; the wrapper dies with the fork. A cut without the re-seat returns per-card `backdrop-filter` and the t33 rim. | The re-seat rides the **same commit** as the wrapper cut — the gate exists for exactly that reason. | Atomic with W5F-32's wrapper replacement. | — |
| **W5F-36** | W5 gate **C1** + `shell-panesegmentedcontrol` **PSC-8** | MAJOR | Per-route rendered text at 390 vs 1440: `#/` **69 vs 893 = 7.7 %**; `#/blob` 68 vs 748. Ratio form is API-less-safe (U-6). | **BINDING NOTE (PSC-8)**: the `walk.spec` presence-assertion re-point must **PRESERVE the content-landmark discipline** — the "Detailed Guide" visibility proof (walk.spec.ts:39-42 / :105-126), a landmark unique to the toggled-in pane, both directions plus return, is the shape the presence assertions must take. A ratio ≥ 0.9 achieved by shrinking desktop is caught by B3's extent arm. | MEASURE-AT-OPEN. | — |
| **W5F-37** | W5 gate **C5** + `App` **D-7** + `wb-extract-pane` **XP-1** (`:max` limb) | MAJOR | Crossing the 719 px container (or 200 % zoom at 1440×900 → 720×450 CSS viewport) trips the compound breakpoint: App.vue:77/:94's hard `v-if` fork **remounts**, destroying 3 KeepAlive caches and 2 WebGL contexts. The X6 single-context trade was deliberate; the total state loss at a zoom step was not. | Element-identity preservation is the assertion ("a gate on 'canvas still renders' would pass a fresh context and is explicitly **not** the assertion"). | Interacts with W5F-03 (`:max="6"` is why extract's leak is permanent) and W5F-08. | — |
| **W5F-38** | W5 gate **C7** + `shell-dock-dockstatuslamp` **DSL-6 / DSL-3 / DSL-30** + `shell-panesegmentedcontrol` **PSC-5 / PSC-21** | MAJOR | Source-scoped viewport-dimension `@media` count 7 → 5 (unit b) → **3** (unit c); RED today **7**: 6 width-based (`ConsoleRail:323`, `PaneSegmentedControl:46`, `DockStatusLamp:70`, `animations.css:17`, `shell.css:129`, `foundation.css:471`) + 1 aspect-only (`foundation.css:479`). DSL: **`DockStatusLamp:70`'s death also kills** the below-1024 empty-live-region mechanism (`.lamp-label{display:none}` strips the only text node from a `role="alert"` whose other child is `aria-hidden`, while the comment two lines above claims "visually-hidden, not v-if'd" — **false as written**) and the 26×18 label-era capsule. PSC-5: `PaneSegmentedControl:46`'s `max-width: 639px` fork is the exact complement of the producer's own `min-width: 640px` rung. | **THE 27 CAPABILITY QUERIES MUST SURVIVE** (`prefers-*`, `forced-colors`, `print`, `pointer`, `hover`) — "a gate that counts all 34 is wrong and would reward deleting a11y queries." **RIDER (PSC-21, BINDING)**: `shell.css` MOB-1 (:106-107) and `foundation.css:29` both name `scripts/ci/css-emission-probe.mjs` as the guarantor of these rules; **the file does not exist** (`scripts/ci/` holds only `verify-packed-surface.mjs`; no workflow step, no package script). The comments must be corrected — or the probe restored — **in the same edit that touches the rules**. → gate §2 N9. **RIDER (DSL)**: the false lamp comment is corrected by whichever wave touches the file first. | C7's staged count is the reading. | `DSL-6` grade MINOR (B wins) precisely because the line is already scheduled to die under C7. |
| **W5F-39** | `wb-extract-pane` **XP-15 · XP-16** ≡ `wb-generate-pane` **GEN-25 · GEN-34** ≡ `wb-gradient-pane` (twin) | MINOR | The pane shell is hand-restated across nine panes: the root wrapper div is a no-op box (`mx-auto` dead against `w-full`; `relative` shadowed by the workbench's own relative root, which is the actual containing block for the eyedropper's `absolute inset-0`), three classes byte-repeated on the Card, and **copied verbatim at `GradientPane.vue:19` and `GeneratePane.vue:30`**. XP-16: the pane styles its child's root from **outside** via single-root fallthrough (`class="pb-4 px-4 sm:px-6 pt-2"` fuses onto `ExtractWorkbench.vue:2`), so the workbench has no intrinsic padding contract. GEN-34: the pane's height is decided by its `palettes` companion, not its content (`h-full` chain into the neighbour-sized grid row). | Dies in the one-mount/one-column re-carve; owner-edict-5 note travels with XP-16. **Shared-shell EXTRACTION is NO-WAVE-OWNER** (GEN-25) — W5 takes the recomposition, not the abstraction. | Needs §3 BD-10/BD-11 or the riders degrade. | — |
| **W5F-40** | `wb-extract-workbench` **XW-14** | MAJOR | The binding narrow sequence is **source → result → controls**; the shipped order is source → **controls** → result, structurally, at every viewport (`ExtractControls` :64 precedes the result Transition :102 in one column; sole mount `layout="column"`). Needs no probe. | → the bespoke scene composition (per XP-25's routing). **LOCK (XW K-23 + CC-108/A-17)**: OPTICAL-BENCH:42's **narrow order** and :76's boundary inventory survive as authored intent; **the ratio numbers do not** (see W5F-48), and no chassis-shaped housing may be rebuilt. | Needs §3 BD-10. | — |
| **W5F-41** | `shell-panesegmentedcontrol` **PSC-17** (spec prose) — see also W5F-18 | MINOR | `e2e/smoke/mobile/walk.spec.ts` documents the abolished law in the header of the only spec exercising the control: `BouncyTabs` (:16, :31 — does not exist in glass 7.0.0) and `lg:flex` as the hiding mechanism (:19-20 — the exact width-only mechanism `shell.css:98-110` records as deliberately retired). | The modify-carve must correct **both** lines, not just the rename. → gate §2 N10. | Rides W5F-18. | — |

### 1.E The block law

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-42** | `App` **D-8** ≡ `shell-paneslot` **D-11/L-11** ≡ `wb-mix-pane` **DD-6/DD-S2/DC-1** fold ≡ `wb-extract-pane` D-4 note ≡ `wb-generate-pane` **GEN-34** ≡ `ErrorBoundary` **EB-8** (supersession) ≡ `EmptyState` **ES-25** (arming) | MAJOR | `shell.css:19-28 height:100dvh; overflow:hidden` frames the viewport at **every** size; `docScrollable = 0` on ALL routes; `--content-max-h` caps the scene band at 608 px at 3440×1440 (the bare 21/9 arm); occupied block extent **45.4 %**. Token census: `100dvh` ×1, `svh` in 0 files, `content-max-h` **8 sites across 3 files** (definition + live consumers `shell.css:75`, `PaneSlot.vue:23`). | `min-block-size: 100svh`, no `overflow`; `--content-max-h` and both `@media` arms deleted (`foundation.css:471`'s width+aspect arm keeps its `--dock-*` rows; `:479`'s bare 21/9 arm dies whole); `--app-gutter` replaces `--app-padding-x`. **INSEPARABILITY RIDER (App D-8, F, BINDING)**: document-scroll **cannot land by deleting `overflow` alone — every pane's inner scroll-well reflows with it**; the row travels as a **recomposition**, never an overflow-deletion (`wb-mix-pane` K-DC1-ATTRIBUTION and K-D25 both kill the naive cure independently). **SUPERSESSION WARNING (EB-8, BINDING)**: W5's hard gate retires the block-axis cap — any wave binding EB-8 re-derives the proportion contract against W5's **post-state**; the **owner-ruled ≈1050 px inline clamp** (`shell.css:69-73`, 2026-07-05) is the invariant that must survive, and **the cheap `position:relative` patch for EB-1 is BANNED** (it ships a 1408 px plate where every healthy scene clamps to ~1050 px). **ARMING (ES-25)**: the `26ch` measure is inert at today's 462 px pane and **arms the moment X-W5 lands Admin full-width** — X-W7's row, with the arming condition stated. **CORRECTIONS carried**: the challenger's `foundation.css:481` cite is the 21:9 ULTRAWIDE arm (F right); the magic-depth number is route-dependent (O right). | B3 is MEASURE-AT-OPEN; the per-route breakdown, not the mean, is the reading. | — |
| **W5F-43** | W5 gates **B5 · B6** | decision-record | Logical-axis support (`CSS.supports('overflow-block','auto')`) and the 390-with-scrolled-document dock posture (U-7) are unrecorded decisions. | **Absence is the failure**: the wave closes only with the pasted `CSS.supports` reading and a sticky-vs-in-flow rationale comment **at the site** in `shell.css`. | — | — |

### 1.F Scene motion

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-44** | W5 gates **D1 · D2** (DR-27) | MAJOR | Measured hops: `→/gradient {frames:14, max:154, median:59, over32:10}` = **71 %** · `→/mix` 63 % · `→/extract` 26 % · `→/generate` 23 %. The baseline lives at `docs/tranches/T/audit/pi/u-gestalt/probe2-log.txt` and is **untracked**. | D2 refuses to let D1 cite a number that is not in git (L-7). Discharged by X-W0's CC-012; **re-checked here because D1 depends on it**. A budget met by *deleting* the animation is caught by D3/D4 — the gates are adversarial by construction. | X-W0 first (D2 only). | Archaeology: DR-27's measurement has existed since T's close (`T-48 → T-58 → U-F7 → CH-6 → W47`) and entered no disposition ledger in T, U or V. CC-054 is its first. |
| **W5F-45** | W5 gate **D3** + `shell-paneslot` **NEW-DU-5** (W5F-19) | MAJOR | **18 physical-name sites** (`animations.css` 12, `shell.css` 3, `App.vue` 3) key the `vj-enter` family off `.pane-wrapper--left/--right`. | Re-key onto region roles; **every region role in `regions[]` must have a matching enter/leave rule** (the second arm is what stops "delete the selectors" from passing). **SHARPENER (NEW-DU-5)**: a re-key that leaves mobile forward and back **motion-identical** has not cured the row — the `--right` geometry is unreachable on mobile *by construction* today. Motion declarations are **added, never deleted** (standing preserve-animations edict). | After W5F-32 commits (App.vue class names only). | — |
| **W5F-46** | W5 gate **D4** + `wb-mix-sourceselector` **MSS-17 · MSS-21** + `shell-panesegmentedcontrol` **PSC-18** | MAJOR | `MixSourceSelector.vue` has **0** `<Transition>` elements while its `mode` swap at :114 is a bare `v-if` (:114/:230 are bare fragment branches) — the exact surface of the OM-8 owner mandate; the parent uses `vj-morph` thirty lines away (MixPane:111). Census over the 3 `SegmentedTabs` consumer files (7 occurrences). | **CURE NOTE (MSS-17, BINDING)**: `<template v-if>` fragments **cannot host `<Transition>`** — each branch needs a single root, the same restructure MSS-21 performs. A `TransitionGroup` on the *list inside* a branch (:120) does **not** satisfy D4 — the branch swap itself is the assertion. **PSC-18**: the drag-morph indicator is armed by omission and an OS accessibility preference silently selects between two interaction models — dies with the control at W5F-33. | MSS-17's terminal home is **X-W6.j**; W5's D4 is the census. Coordinate (§4 CE-3). | — |
| **W5F-47** | W5 gate **D5** + `ErrorBoundary` **EB-24** | non-regression | The global PRM guard (`animations.css:184-193`) exists and must keep covering every rule this wave adds. EB-24: the caught plate has **no motion at all** — no `<Transition>` anywhere in `ErrorBoundary.vue`; an unmanaged full-slab jump-cut in a fully choreographed app, PRM satisfied only vacuously, against VC:141 ("No full-slab remount hole"). | D5 fails if a new scroll-driven or `animation-timeline` declaration slips the guard (MT-F023's class — the guard **cannot** neutralise those; that escape is X-W10's CC-099). **The caught/recovered swap IS a pane swap** and W5's hard gate covers it. Motion-quarantine: zero ErrorBoundary rows; nothing blocks this. | — | — |

### 1.G The P-1 posture strike (X.W5.e)

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-48** | W5 gate **E1** ≡ `BrowsePane` law-rescope ≡ `wb-extract-pane` RULING PIN 2 ≡ `wb-extract-imageeyedropper` RULING PIN 4 ≡ `wb-extract-workbench` **K-23** ≡ `AuroraPane` **AP-1** | MAJOR | **7 occurrences across 4 files** state a tolerance no mechanism emits: `VISUAL-CONSTITUTION.md:44,45` · `EVIDENCE.md:43,47` · `OPTICAL-BENCH-COMPOSITIONS.md:39,40` · `research/proportion-register.md:35`. The same fabrication propagates as `61.8033989%/38.1966011%` and `66.6666667%` garnishes in three workbench corpora. | One exact posture — preview-dominant `66.6666667% / 33.3333333%` when an inspector is selected, **100 % protagonist when none is**. **SURVIVAL CLAUSE (BrowsePane, BINDING)**: the strike kills the band **only**; `VISUAL-CONSTITUTION:44`'s "every rendered bounded palette entity slip has exactly one Card shell, and the field/empty/inspector have none" clause **survives**, and so does the `:54` selection-seat law. **SURVIVAL CLAUSE (XW K-23)**: OPTICAL-BENCH:42's narrow **sequence** and :76's boundary **inventory** survive as authored intent; the numbers do not. **NO-CHASSIS LOCK (CC-108 / A-17 / X-W6 H1)**: any record routing "bespoke scene composition" here (XP-25, EY-15, XW-14, AP-1→X.W6.j) **must not re-import a chassis**; W5's compositions are bespoke by construction. **SCOPE LOCK**: the adjudication and defect records that *quote* the range are evidence and MUST NOT change — "a repo-wide gate would reward destroying the witness." | Owner-ruled disposition (P-1, ADOPT); authors no new design law. | `AboutPane` **K-12** records that D-1/D-2/D-3-as-BLOCKER was **circular** (VC:218 was written FROM this state). The genuinely new fact is the orphaned-owner gap, not the number. |
| **W5F-49** | W5 gate **E2** | MAJOR | The current text presents the numbers with **no source attribution** (P-2's class). | Each replaced sentence names the producer contract (`CONSTELLATION.md` P122) as the source; **zero new occurrences of a bare constant presented as independently derived** — "that trades one fabrication for another." Pairs with **CC-102** at X-W10 (the general citation law). | — | — |

### 1.H The Result/throw and the boundary (routed to W5 by seven records; W5.md names none of the files)

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-50** | `picker-componentsliders` **R-2** ≡ `shell-dock-parseechoreadout` **A-1** (identity, cross-booked) | **BLOCKER** | Legal CSS Color 4 `none` reaches an unguarded throw **in root setup**: `picker-color.ts:104-107 valueOrThrow` (Result→exception), `:151-158 channelNumber` throws `Missing ${space}.${key}`, `:195-204 clampPickerColor` passes `none` through **by design**; `useColorPipeline.ts:75` calls `convertPickerColor(model.value.color,"hsv")` **bare at composable-body level** while the watcher 11 lines down wraps the identical call in try/catch (the author's own receipt); App.vue:245 calls it in setup; **`ErrorBoundary` is INSIDE App's template (:50/:140) and `main.ts:5-7` mounts bare — a descendant's `onErrorCaptured` cannot catch an ancestor's setup throw.** Warm path: `useColorParsing.ts:75` commits the `none` colour, then `useSliderGradients`' watch getter and formatted map throw inside the injected pipeline's reactive graph. **A-1 ADDS to R-2's enumeration**: `astEcho` (useColorParsing.ts:97) and `gamutVerdict` (:108) as throw sites, with the R⑤ asymmetry (Route A throws `astEcho` only); the dock mount (`ActionBarLayer.vue:115 → ColorInput.vue:109`) sits **outside** the boundary. All three ParseEchoReadout r2 axes asserted the opposite ("not presently reachable"/"always present"/"coherent today") — the corpus's largest shared error. | → **X-W5**: demo Result propagation — **delete `valueOrThrow` from the render path**, `none`-total channel read, **hoist the boundary above App**. Library half → **X-W9** (CC-084's `Result` battery); the `none`-URL born-RED gate enters **X-W1**'s deploy matrix as a query-parameter state. Blast radius NARROWED (RR-B's kill): `hydrate.ts:98-110` catches cross-space conversions, so the blank needs a same-space/`none`-preserving deep link. **In-tree contradiction preserved**: `valueDomain.ts:21-22` declares `none` pass-through "the library's law" and `test/v4-c1.test.ts:186-192` pins it as intended — while `channelNumber` treats it as a hard error. | Needs §3 BD-12..BD-15. Coupled to W5F-52/53 (the boundary's altitude). | Stack-trace provenance caveat carried (K-10). |
| **W5F-51** | `picker-componentsliders` **R-30** + `shell-dock-parseechoreadout` **A-4 · A-7** | MINOR | `PickerColorError` carries two incompatible throw contracts and **the machine code is what the end user reads** (`ErrorBoundary.vue:61` renders `err.message` verbatim — corroborated by a probe's own UI text "… color_missing_alpha Try again"). A-4: the root and both branch `v-if`s (:4/:7/:20) are **permanently truthy** — three dead guards advertising a defended absent-state posture while the real failure is a throw no `v-if` can catch. A-7: alpha `none` renders as fully opaque (`:101` gates the α row on `typeof color.alpha === "number"`), so `oklch(0.6 0.2 30 / none)` is indistinguishable from alpha 1. | All three die with `valueOrThrow`'s deletion; the boundary gets a human string + a diagnostics channel. **A-4 LOCK**: when the producers become total the guards **become real or die — they may not survive as decoration**. | With W5F-50. | — |
| **W5F-52** | `ErrorBoundary` **EB-1** ≡ `wb-gradient-codeeditor` **A-14** (paint limb) | **BLOCKER** | The caught plate's ink paints **under** the atmosphere canvas; only the Button survives. `ErrorBoundary.vue:18` declares no positioning and the scoped block (:84-86) sets only `color` ⇒ `position: static`; the canvas is `absolute inset-0` inside `shell.css:25 .app-layout{position:relative}`; `.pane-main` is positionless; the DISPLACED element carries the contract the boundary lacks (`.pane-container{position:relative}` :74, `.pane-wrapper--left{z-index:1}` :95); the control case sits 35 lines above (`.dock-band{position:relative}` :39-40). Zero-mutation shipped arm: **0 of 16,020 statement px / 0 of 784 glyph px**, minLuma 128; two mutually exclusive interventions each restore ink. | Lands jointly with W5F-53's transposition; the plate **body** lands at X-W7. **BANNED (EB-8)**: the cheap `position:relative` patch — it ships a 1408 px plate. CSS 2.1 Appendix E: step 7 vs step 8 (**the r2 "step 5" cite is corrected — K-9**). | With W5F-53. | — |
| **W5F-53** | `ErrorBoundary` **EB-2** ≡ `wb-gradient-visualizer` **GV-6** ≡ `wb-gradient-easingspecimenstrip` **C-01/N-4** blast limb ≡ `wb-gradient-codeeditor` **A-14** | **BLOCKER** | One catch latches the whole application dead for the session; containment is simultaneously **too coarse and too narrow**. `caught` (:55) is component-local, written at :60, cleared only in `reset()`; the slot sits in `v-else`; App.vue:50-140 mounts ONE boundary with **no `:key`/route coupling** around the entire two-pane grid — one pane's throw withholds both and route changes re-render nothing (`afterRouteChange: #/gradient, boundaryUp true, paneContainerPresent false`). Narrow half: `<nav>`, the canvas and `MigratePalettesDialog` are **siblings**; `grep onErrorCaptured demo/ src/` returns exactly ONE carrier — the dock survives and keeps accepting navigation, **reporting a scene that is not mounted**. Downstream: one tile press blanks BOTH panes and `reset()` re-seeds `useGradientModel`'s two hard-coded stops — **the user's gradient document is destroyed** (zero persistence in `demo/workbenches/gradient/`). | Three-altitude transposition: **pane containment in the seat, OUTSIDE KeepAlive (R-7 constraint, BINDING — the KeepAlive-cached boundary cure is KILLED for structural unsoundness)**; shell containment + reporting at the composition root `main.ts` (W5 unit 1 owns it **by name**); a route-reset arm; a positioned fallback. | **CURE-LOCK (R-1)**: EB-4 (W5F-07) and EB-2 land **together** or the latch survives. | `ErrorBoundary`'s closing verdict: no X wave names the component; **the boundary successor itself is named cross-surface residue for the next formation boundary**, with R-1 and R-7 binding on whoever picks it up. |
| **W5F-54** | `ErrorBoundary` **EB-3** (+EB-35/EB-36 folds) | MAJOR | Terminal unreportable sink: `return false` halts Vue **before** `errorHandler` (`runtime-core.cjs.js:238-241` early-returns above the `:245-254` branch and the tail `logError` at :257); the hook (:59) discards Vue's `instance`/`info` args; `main.ts` (7 lines, read whole) installs **no** `errorHandler`, **no** `unhandledrejection`, **no** `window.error`; an installed handler was called **0** times. `return false` is itself the correct call for an owning boundary — the defect is what the file does *instead* of reporting. | Named rider on the composition-root unit, **~5 lines** (the error-net lines are unowned by W5's text). | With W5F-28. | — |
| **W5F-55** | `ErrorBoundary` **EB-6 · EB-12 · EB-18 · EB-27 · EB-32 · EB-34** | MAJOR/MINOR | EB-6: focus thrown **unconditionally** to an unnamed 1408×804 container (`:64 nextTick(() => alertRef.value?.focus())` never reads `document.activeElement`) that gets the UA ring (`foundation.css:716`'s `[tabindex]` arm excludes `-1`), and `reset()` destroys the focused Button and restores nothing (activeElement AFTER reset = BODY); `aria-live="assertive"` is redundant on `role="alert"`. EB-12: "This panel" understates an app-wide blast radius; the machine truth is unbounded, unclamped, un-disclosed, non-LTR-isolated developer prose; "Try again" is the wrong verb for a stale chunk. EB-18: the entire declared public surface is dead (`emit("reset")` has no listener; `retryLabel` never passed; App.vue:50 re-passes `message` byte-identically). EB-27: a shell-level failure composition filed in the Vite boot root. EB-32: one catch permanently disarms `useDevicePixelSnap`'s ResizeObserver/MutationObserver channels for the session (observers bound to dead nodes; `onMounted` never re-runs) — the settle-listener and window-resize channels survive. EB-34: see W5F-15. | Riders on the successor surface; body copy → X-W7. **STRUCK (K-8)**: EB-6's §5.1 canon citation — the row stands on the ARIA alert/alertdialog pattern alone. **EB-27's `demo/app` rename is NO-WAVE-OWNER** (nearest X-W8). | With W5F-53. | — |

### 1.I The typed action contract (CC-004) — riders

| id | source(s) | sev | mechanism | cureShape + LOCKS | sequencing | dissent |
|---|---|---|---|---|---|---|
| **W5F-56** | `PalettesPane` **PP-3** | **BLOCKER** | `usePaneRouter.ts:156-157` writes the literal hyphenated keys `"onCommit-edit"` / `"onCancel-edit"` in an **object literal** — no template, so no compiler camelize. Vue's emit resolver looks up `onCommitEdit` and falls back to hyphenated keys **only for `update:*` model listeners**; neither matches. The desktop edit overlay's Save/Cancel (`CurrentPaletteEditor.vue:75-80 → PalettesPane.vue:51-52 → the void`) is a dead end-to-end path. The working control case is App.vue's **template** `@commit-edit` on `<Dock>`. | CC-004: the router's prop bags become **typed, compiler-visible bindings**. → gate §2 N3. | With W5F-10/W5F-64. | — |
| **W5F-57** | `PalettesPane` **PP-17** (reader-L #3+#5 promoted) | MAJOR | The card action channel is **one untyped string across 18 dispatcher entries with a silent-drop default**: `defineEmits<{ action: [action: string] }>` (PaletteCardMenu.vue:224-227); the menu emits **17** literals (15 static + the :52 dynamic pair — census corrected from "eighteen distinct literals emitted"); the dispatcher holds 18 including the orphaned `copyAll` (`if (!fn) return`). The `export` emit and its `format` parameter are `string` end-to-end. A typo in any literal compiles, lints and no-ops. | One **closed union, exhaustiveness-checked** — fixes 18, not L's 5. `copyAll`'s consequence is IDENTITY-FOLD → `PaletteCard` **PC-8** (the no-copy-path BLOCKER); the export vocabulary joins **X-W7**'s ExportFormat seat (PP-8). | Joint with X-W7's PP-8. | — |
| **W5F-58** | `CurrentPaletteEditor` **L-7 / L-22** + **C-14** | MAJOR | 4 props + 8 emits re-declare injected state; `PalettesPane.vue:41-54` is a **pure relay** (six 1:1 pass-throughs); both props are filled from ONE port read; `savedPaletteCount` + `savedPalettes` are filled from one port read in the same expression pair (:44-45). The correct 27-line `import type` key idiom exists at `color-session/keys.ts` and this component already consumes it (:173), while `usePalettePorts.ts` colocates all five KEYs (:271-275) with a 275-line value-importing provider. | Typed action contract: the relay dissolves; keys move to a keys module (the house idiom). | Needs §3 BD-01/BD-03/BD-04. | — |
| **W5F-59** | `CurrentPaletteEditor` **L-13-as-rescoped** | MAJOR | The edit-commit pair is implemented **twice** with verbatim-copied `:style="{color: safeAccent}"` (this file :75-80 vs `Dock.vue:141-144`), and `commitEdit` has **two drifted wirings** — `App.vue:41` adds a pane side-effect `usePaneRouter.ts:156` lacks. | **One command home.** The responsive split itself is exact and correct (the fork implication is KILLED, K-10). | With W5F-56. | — |
| **W5F-60** | `CurrentPaletteEditor` **L-21-as-rescoped** ≡ `ApiOfflineChip` **AP-13** ≡ `shell-dock-dockstatuslamp` **DSL-3** | MAJOR | `ApiOfflineChip` re-derives the **tested** `resolveLampState` matrix inline (roles + labels hard-coded :11-26; fork verified against `status-lamp.ts:44-65`), duplicates the `offline-dot-pulse` keyframes, and has **zero tests** — an untested fork of the one module in this cone with a real proof. | Consolidation onto the pure resolver; transport-status **scene** ownership. **CONFLICT**: `DockStatusLamp.vue` + `status-lamp.ts` are owned **BY NAME** by MT-DOCK-LAYERS-1 (X-W8: D2-14/C-34/G-K/G-Q); `demo/platform/transport/**` is claimed by **NO** wave. §4 CE-6. | Blocked on the X-W8 ownership ruling. | Production-leak leg KILLED (K-12); dev double-alert KILLED (K-8). |
| **W5F-61** | `CurrentPaletteEditor` **L-23** | MINOR | `TooltipProvider` per call-site: exactly **4** mounts with delays 100/300/200/200 and **zero at root** — here wrapping A-1's inert span. | One provider at the composition root; delays become one decision. | With W5F-28. | Delay-set census is the arbiter's own addition. |
| **W5F-62** | `wb-extract-pane` **XP-14** ≡ `wb-extract-workbench` **XW-31** ≡ `wb-extract-imageeyedropper` **EY-42** | MINOR | `DisplayColorSpace` re-declared **four times** over an exported canonical type (`ExtractPane.vue:30`, `ExtractWorkbench.vue:202`, `useImageSampler.ts:21` exported, canonical `demo/color-session/color-model.ts:29`; `picker-color.ts:37 PickerSpace = SpaceId` makes them structurally identical), and the prop contract is **erased at the router**: `usePaneRouter.ts:64 props: Record<string, unknown>` / `:139` sole producer / `PaneSlot.vue` `v-bind`. Double default (`ExtractPane.vue:32` + `ExtractWorkbench.vue:204`, both `"hex"`). | W5 scope item 1 makes `bindPane` "required and generic" on all three PaneSlot sites — **the typed seam IS that work**, and the local type copies die when the canonical import lands with it. | With W5F-10. Needs §3 BD-10. | — |
| **W5F-63** | `wb-gradient-pane` **L-3 · C-2 (RESCOPED MINOR) · D-table-"initializing" · M-GP-6** ≡ `wb-gradient-visualizer` **GV-16** (mobile limb) | MAJOR/MINOR | L-3: the command channel is `any` at **every shell hop** (`PaneActionRefs { gradient: Ref<any> }` :110; `gradientPaneRef = ref<any>(null)`; `onMount?: (instance: any)`), so renaming any exposed key type-checks clean, lints clean (both unused-vars rules off) and tests clean (zero coverage). C-2: during the async-chunk window (**measured 1275 ms**, PS-probe4) the dock renders three enabled, labelled gradient commands dispatching into a null or **foreign** ref — silent no-op, no receipt. | Interim cure already plumbed and unused: `DockAction.disabled?` (`usePaneRouter.ts:52`) → `GenericActionBar.vue:27` → `ActionButton.vue:17-18` (`disabled` + `aria-disabled`). Structural cure is W5's own named mechanism. **CORRECTION to all three axes (carry it)**: `GradientPane.vue:9`'s own ref **is** properly typed — the untyped hops are shell-owned; SOURCE-RED at that file was mis-addressed. **REFUTED**: the axis's inner-`?.` mechanism (dead defence — the child is static, unconditional, expose-names exact). **M-GP-6 rider**: the dock's motion vocabulary carries **identical `rotateOnClick` flags** on destructive Reset (`:208`) and non-destructive Regenerate (`:196`). | With W5F-01/W5F-10. | — |
| **W5F-64** | `App` **L-8 · D-13** | MAJOR | Three `ref<any>` + nine `?.method?.()` dispatches (`usePaneRouter:196-222`): a workbench rename is a silently dead dock button; `vue-tsc` exits 0 over a **measured-inert** action bar — "the enabling condition for D-1's silence." `onMount?: (instance:any)` optional while `colorPickerRef` (:223) is properly `InstanceType`-typed, **proving the pattern was known and skipped**. | Typed generic `onMount` + an `InstanceType` record. The **workbench-provided-action-bar inversion** rides the member program (≡ R-8's registry). Structure over gates: A4 exists to prove the checker **bites**. | §4 CE-1. | — |
| **W5F-65** | `PaletteRenameInput` **A-3** (emit/ack leg) + `SearchFilterBar` **SFB-20** (cross-note) | MAJOR/MINOR | A-3: the leaf's `submit: [newName: string]; cancel: []` (:43-46) has **no ack, no pending, no rejection channel**; the parent closes the editor synchronously in the emit's tick; `BrowsePane.vue:106` is the ONE card binding with no feedback leg while `@save`/`@delete`/`@set-visibility`/fork all route verdicts to `card.showFeedback(…)`. SFB-20: the closed `sort`/`tier` vocabularies are laundered to bare `string` across the emit seam (`String(v)` at :21/:33) and restored downstream by an unchecked `as`; `tier` crosses with **no cast at all**; `UserSortMenu:20` ships the same idiom (`v: any` + `as string`) — off-vocabulary is a **400 at the wire**, not a compile error. | A-3's **typed emit/ack contract leg rides X-W5**; the CRUD verdict returns (mirroring `onDeleteOwned`'s `{success, message}`) are **X-W7**'s. SFB-20's typed emit seam is X-W7's, cross-noted here. | Split rows; W5 takes only the contract legs. | — |
| **W5F-66** | `AdminAuditPanel` **AAP-13** ≡ `AdminFlaggedPanel` **AF-22** ≡ `AdminTagsPanel` **ATP-4** ≡ `AdminNamesPanel` **D-4/D-1.3/M-DU11** ≡ `EmptyState` **ES-11** (stacking) ≡ `EmptyState` **ES-15** (Dock limb) | MAJOR (fold of W5F-32) | Recorded separately because these five records each state a **route-composition consequence** the schema deletion discharges: the Admin route is ≈36.4 % of main beside a co-equal companion with the mobile pane selector alive; two identical live plates seat structurally on every admin route; all four `smallTapTargets` on `/#/admin/names` belong to the companion (`PaletteSlugBar`) the canon deletes; ES-15's `Dock.vue:271-283` `#collapsed` seal wraps a view icon + admin gold-shimmer + its own `vj-morph` Transition **inside a `WatercolorDot` that renders no slot** — the children die at the boundary. | Discharged by W5F-32's one edit (`right: null` on admin). **ES-15's Dock limb** is **noted to X-W5** while the impostor-seat identity stays **X-W4.g** (trigger-gated, cross-declared). | With W5F-32. | `ATP-4` DEMOTED / `ATP-3` RAISED on identity law — both recorded at W5F-32. |

---

## 2. Gates

### 2a. EXISTING W5 gates these rows SHARPEN (18)

| gate | sharpened by | what changes |
|---|---|---|
| **A1** composition root | W5F-28 | Add the C-6 arm: the false `./demo/**` sideEffects **booking and comment** die together (a corrected comment is still an ungated claim). L-9 is STRUCK — the gate may not assert overture.css moved. |
| **A2** deeplink-overture | W5F-20, `shell-paneslot` NEW-DU-1 | The B3 strand's root (`@appear-cancelled` unbound; `leave()` cancels the in-flight appear) is **X-W2's**; A2's MEASURE-AT-OPEN must not be read as W5 owning the cure. |
| **A3** dock-action-parity | W5F-01 (9 witnesses), Dock **R-8**, Dock structure (f), Dock **G-I** 2nd arm | (i) The **cure** is contested — R-8 moves it to the DockCommand registry at X-W8 (§4 CE-1). (ii) Add the **Save/Cancel discard** arm: A3's close must witness explicitly that a mobile edit no longer reports success while being discarded (App.vue:41-42). (iii) Add the **720×900 (≡1440@200 % zoom)** arm from Dock G-I. (iv) Falsifier gains Dock structure (f): a view providing no commands must render **no Tools trigger** — "an absent capability is ABSENT"; present-and-inert must be unspellable. |
| **A4** typed-registration bite | W5F-62, W5F-56, W5F-63, W5F-10 | The one-argument-deletion bite is necessary, not sufficient: add a **props-seam** arm (`usePaneRouter.ts:64 props: Record<string, unknown>` must not survive) and a **prop-bag** arm (see N3). Carry the correction that `GradientPane.vue:9` and `App.vue:223` are already typed. |
| **A5** route H1 | W5F-26 (7 witnesses), EB-11, PH-8 | Fifth falsifier: **outline order** — an `<h1>` that still parents six `<h2>` beneath an `<h3>` pane title passes the count and fails the document. Add the **caught-scene** arm (EB-11) or the gate has a blind arm. |
| **A6** boot-URL silence | W5F-29 | Add CNL-26's read-side arm (`?space=` alone must not silently no-op then be overwritten) and the AboutPane **URL-echo assertion** for `jzazbz`/`display-p3`/`rec2020`. Over-cure falsifier stands: a gate that also kills live sync is wrong. |
| **A7** announced redirect | W5F-27, AdminListSkeleton LOCK | Carry the LOCK verbatim into the gate text: the region is a **route-settlement** node and may **never** be cited as the load-completion/`aria-busy` cure. Add EB-30's stale-`<main>`-name arm. |
| **B1..B4** the block law | W5F-42 | Carry the **inseparability rider** (recomposition, never overflow-deletion) and **EB-8's supersession warning** (the ≈1050 px inline clamp is the invariant; the `position:relative` EB-1 patch is banned). B4's repo-wide scope is why the consumer arm (`shell.css:75`, `PaneSlot.vue:23`) is caught. |
| **B5/B6** decision records | W5F-43 | Unchanged; absence remains the failure. |
| **C1** content parity | W5F-36 / PSC-8 | The presence-assertion re-point must preserve the **content-landmark** proof shape (a landmark unique to the toggled-in pane, both directions plus return). |
| **C3** the fork is gone | W5F-12, W5F-34 | Green C3 is necessary, not sufficient — **Dock G-L** is the rendered arm and lives at X-W8; C3 may not close while G-L is RED. |
| **C4** the pane selector is gone | W5F-33 | Add PSC-1's **REFUSAL**: no `semantics="tabs"` interim patch (nine walk.spec assertions + the producer drag path). Baseline pinned at exactly **1** render site. |
| **C5** state survives the breakpoint | W5F-37, W5F-08, W5F-03 | Add the 200 %-zoom arm (720×450 CSS viewport) and note that `:max="6"` is why the extract leak is session-permanent. |
| **C6** T-45 carrier | W5F-35 | Unchanged — same-commit requirement restated. |
| **C7** viewport queries subtract | W5F-38 | Add the **PSC-21 canary rider** (the two comments naming `scripts/ci/css-emission-probe.mjs` must be corrected or the probe restored **in the same edit**) and the DSL false-comment rider. The 27 capability queries must survive. |
| **C8** physical axis unrepresentable | W5F-32 | Add PSC-3's **14-member migration domain** and PSC-12's **unrepresentable pane-label** accretion as close conditions, not just the grep. |
| **D3** pane motion re-keyed | W5F-45 / NEW-DU-5 | Add the direction arm: a region-role re-key that leaves mobile forward and back motion-identical has not cured the row. |
| **D4** sub-pane swaps animate | W5F-46 / MSS-17 | Carry the fragment cure-note (`<template v-if>` cannot host `<Transition>`; each branch needs a single root) and the `TransitionGroup`-on-the-inner-list exclusion. |
| **D5** PRM honesty | W5F-47 / EB-24 | The caught/recovered swap **is** a pane swap and is inside the gate's scope. |
| **E1/E2** the posture strike | W5F-48, W5F-49 | Carry the **survival clauses** (one-Card-shell, :54 selection seat, OPTICAL-BENCH sequence + boundary inventory) and the **no-chassis lock** so a "bespoke composition" cure cannot re-import CC-108's corpse. |

### 2b. NEW born-RED candidates (10) — every witness real, no proof-script contrivance (L-19)

| # | gate | RED witness (measured/source-certain, named) | falsifier |
|---|---|---|---|
| **N1** | **deactivation contract** — navigate away from `/#/extract` with an active camera and an in-flight quantize: zero live `MediaStreamTrack`s, worker terminated-or-idle, and no `window` keydown handler surviving from a parked `ImageEyedropper`. | RED, source-certain: **4 orphaned teardowns**, all `onBeforeUnmount` under `<KeepAlive :max="6">` over exactly the six non-admin left panes ⇒ **never evicted** (ExtractWorkbench.vue:281 — the ONLY `track.stop()` path; useImageQuantize.ts:148-151; useExtractSession.ts:194-196; ImageEyedropper.vue:242-245). Demo census: **1 `onActivated` / 0 `onDeactivated`**. Named by XP-1 as an **addendum candidate** ("no W5 scope item names deactivation teardown"). | Fails if a teardown is moved to `onDeactivated` for one subtree only — the gate asserts the **contract** (PaneSlot publishes it) plus the four sites. A gate on "the camera light goes out" alone would pass a browser that reclaims the device. |
| **N2** | **the route-throw is unspellable** — `grep -c 'as ViewId' demo/palettes/usePalettePorts.ts == 0` **and** reintroducing `setActiveTab("saved")` makes `npx vue-tsc -p tsconfig.demo.json --noEmit` exit non-zero **and** a node run of the three submit branches produces no unhandled rejection. | RED today: `setActiveTab("saved")` at `useSlugMigration.ts:55/:69/:76`; two casts at `usePalettePorts.ts:87-88`; `router.push({name:"saved"})` **throws synchronously** (`vue-router.js:670`, verified twice independently). | Fails if the literal is changed to `"palettes"` without typing the deps — the hole re-opens on the next tab-name. The typed arm is the assertion; the literal fix alone is caught by the vue-tsc arm. |
| **N3** | **the prop-bag bites** — delete or typo one listener key in `usePaneRouter`'s prop bag and `vue-tsc` must exit non-zero. | RED today: the keys are the literals `"onCommit-edit"` / `"onCancel-edit"` (usePaneRouter.ts:156-157) in an object literal; Vue resolves `onCommitEdit` and hyphen-falls-back **only for `update:*`**; the checker is green over a dead end-to-end path. | Fails if the bag is camelized but stays `Record<string, unknown>` — the seam must be typed, not spelled correctly once. |
| **N4** | **Result-total render path** — `valueOrThrow` has zero occurrences on the render path; a `?color=oklch(0.6 0.2 none)` same-space deep link boots with no ErrorBoundary catch; the boundary/error-net is installed **above** App (in `main.ts`). | RED today: `picker-color.ts:104-107 valueOrThrow`; `useColorPipeline.ts:75` bare in composable body while the watcher 11 lines down try/catches the identical call; ErrorBoundary is inside App's template (App.vue:50/:140) and `main.ts:5-7` mounts bare. | Fails if `none` is *rejected* at the write gate instead of read totally — `valueDomain.ts:21-22` declares pass-through "the library's law" and `test/v4-c1.test.ts:186-192` pins it. The gate asserts render totality, not input narrowing. |
| **N5** | **per-pane containment + painted fallback** — force a throw in one pane: the OTHER pane still renders; the fallback's statement glyphs paint nonzero px; a route change clears `caught`. | RED today: `grep onErrorCaptured demo/ src/` = exactly one carrier wrapping the whole grid; **0 of 16,020 statement px / 0 of 784 glyph px**; `afterRouteChange: boundaryUp true, paneContainerPresent false`. | Fails if containment is bought by caching the boundary inside KeepAlive (**R-7 kills that cure**) or by the banned `position:relative` patch (EB-8: it ships a 1408 px plate). |
| **N6** | **atomic commit + announcement** — across a pane swap no observer or microtask sees zero or two active subtrees; the outgoing pane is `inert` + AT-hidden; the arrived pane is announced by name. | RED today: PaneSlot.vue:111-129 renders no wrapper/role/aria; nothing sets `inert`/`aria-hidden`; App.vue:47's static `<main>` label never names the arrived pane though `leftLabel`/`rightLabel` exist **unused** in viewSchema. VC §5.1:118 verbatim. | Fails if announcement is bought by pointing at the route-settlement `role="status"` region — the AdminListSkeleton LOCK forbids exactly that substitution. |
| **N7** | **the KeepAlive bound is derived** — `:max` is computed from the pane registry (or absent), never a literal. | RED today: `:max="6"` over **11** LeftPane members and `:max="9"` over **15** distinct mobile keys, with the LRU pruning the **oldest** — admin visits evict the non-admin panes, the inverse of App.vue:97-100's comment. | Fails if the literal is merely corrected — the point is that a route added tomorrow silently re-breaks it. |
| **N8** | **the identifier collision is gone** — `grep -c 'interface PaneSlot' demo/shell/usePaneRouter.ts == 0`. | RED today: 1 (usePaneRouter.ts:61), shadowing `PaneSlot.vue` in the same two-file directory. | Trivially green by rename; it exists because three records demand the rename **before** the transposition lands (PSC-22, EB-34, LC-missed-3). |
| **N9** | **the canary comment is true** — either `scripts/ci/css-emission-probe.mjs` exists and runs in CI, or `shell.css:106-107` and `foundation.css:29` no longer name it. | RED today: the file does not exist (`scripts/ci/` = `verify-packed-surface.mjs` only; repo-wide find → nothing; no workflow step; no package script) while **two stylesheets cite it as the guarantor of the very rules this wave rewrites**. | Fails if the comment is deleted and the mechanism left ungated — the gate is an either/or, and the honest branch must be recorded (B6's idiom). |
| **N10** | **spec prose truth** — `grep -cE 'BouncyTabs\|lg:flex' e2e/smoke/mobile/walk.spec.ts == 0` and no spec justifies a timeout by `mode="out-in"`. | RED today: `BouncyTabs` at :16/:31 (absent from glass 7.0.0); `lg:flex` at :19-20 (the retired width-only mechanism `shell.css:98-110` records as dead); `mode="out-in"` timeout justification at :48/:54/:137. | Fails if the rename lands without the mechanism correction — PSC-17 is explicit that both must move. |

---

## 3. BoundsDelta — files this wave must ADD to §4 per the records

**W5.md §4 as written names none of the following.** Each row states the demanding record and the contention.

| # | file | access | demanded by | contention |
|---|---|---|---|---|
| **BD-01** | `demo/palettes/usePalettePorts.ts` | modify | W5F-22 (`CurrentPaletteEditor` **ADJ-m6** → X-W5), W5F-21, W5F-57, W5F-58 | **Contended**: X-W7 and X-W8 both carve it (`AdminAuditPanel` AAP-28, `AdminTagsPanel` ATP-20 route the port-width row to X-W8's modify-carve). Disjointness ruling required. |
| **BD-02** | `demo/palettes/useSlugMigration.ts` | modify | W5F-21 (three call sites), W5F-24 | In **no** X wave. |
| **BD-03** | `demo/palettes/PalettesPane.vue` | modify | W5F-56 (the relay), W5F-58 | X-W7 territory (palette domain). |
| **BD-04** | `demo/palettes/browser/CurrentPaletteEditor.vue` | modify-carve | W5F-58, W5F-59 | X-W7 territory. |
| **BD-05** | `demo/palettes/usePaletteWiring.ts` | modify | W5F-14 (`whenColorPickerReady` give-up), W5F-01 (dead consumers) | In **no** X wave. |
| **BD-06** | `demo/shell/PaneHeader.vue` | modify-carve | W5F-26 (**PH-8's heading-level seam is NO-WAVE-OWNER with X-W5 named as candidate**) | X-W10.d owns PaneHeader's motion/condense rows; the `as`/`level` seam is unowned. Partial carve only. |
| **BD-07** | `demo/shell/ErrorBoundary.vue` | modify | W5F-52..W5F-55 (11 rows), W5F-07, W5F-51 | **No X wave names the component** (`grep waves/*.md` → 0). The successor is declared cross-surface residue; W5 owns only the containment altitude. |
| **BD-08** | `demo/shell/router/index.ts` | modify-carve | W5F-27 (announced 404), W5F-31 (addressable admin tabs), A5's fourteen-route walk | MT-APP-ROUTER-1 is the named sibling (App L-4/L-5); X-W3.6 owns the fail-closed `meta.admin` guard. Read-mostly for W5. |
| **BD-09** | `scripts/ci/css-emission-probe.mjs` | create-or-strike | W5F-38 / gate N9 | The comments citing it live in `shell.css` + `foundation.css` (both already in bounds). |
| **BD-10** | `demo/workbenches/extract/{ExtractPane.vue, ExtractWorkbench.vue, useExtractSession.ts, useImageQuantize.ts, ImageEyedropper.vue}` | modify-carve | W5F-03 / N1 (deactivation), W5F-39, W5F-40, W5F-62 | X-W7 carves `ExtractWorkbench.vue`; the session composables are in **no** wave (the extract-session cluster is that corpus's NO-WAVE-OWNER headline). |
| **BD-11** | `demo/workbenches/gradient/GradientPane.vue` · `demo/workbenches/generate/GeneratePane.vue` | modify-carve | W5F-39 (verbatim twins), W5F-63 | The **GEN-CLUSTER** (`GeneratePane.vue`, `useColorGeneration.ts`, `generate-color.ts`, the o20 oracle) has **no owning wave in tranche X** — that record's stated headline. |
| **BD-12** | `demo/color-session/picker-color.ts` | modify | W5F-50, W5F-51 | Library half at X-W9; the demo `valueOrThrow` deletion is W5's per R-2's routing. |
| **BD-13** | `demo/color-picker/composables/color/useColorPipeline.ts` | modify | W5F-50 (`:75` bare setup-path call) | In **no** X wave at this line. |
| **BD-14** | `demo/color-picker/composables/color/useColorParsing.ts` | modify | W5F-50 (`astEcho` :97, `gamutVerdict` :108, the warm commit :75) | Added by `shell-dock-parseechoreadout` A-1 to R-2's enumeration. |
| **BD-15** | `demo/shell/dock/ParseEchoReadout.vue` | modify-carve | W5F-51 (A-4 dead guards, A-7 alpha-`none`) | "co-lands with A-1 → X-W5" per that record. |
| **BD-16** | `demo/palettes/browser/status/ApiOfflineChip.vue` | modify | W5F-60 | Consolidation target. |
| **BD-17** | `demo/shell/dock/status-lamp.ts` | read-only reference | W5F-60 | **CONFLICT**: owned BY NAME by MT-DOCK-LAYERS-1 (X-W8). W5 may not write it. |
| **BD-18** | `demo/shell/dock/layers/SlugEditLayer.vue` | read-only reference | W5F-21, W5F-24 | Already in **X-W4** bounds (`W4.md:118`) for control targets/labels only — its A-1/A-8 rows exceed that carve (that record says so). |
| **BD-19** | `demo/palettes/browser/dialog/MigratePalettesDialog.vue` | read-only reference | W5F-21 branch (c) | X-W7/W15/W22 territory; App.vue:152-157's **mount** is already in W5 bounds via App.vue, and App **D-14's binding sequence** forbids removing it before the recovery article. |
| **BD-20** | `demo/shell/dock/menus/ProfileSection.vue` | read-only reference | W5F-21 (regenerate path), W5F-25 | X-W4/X-W8/X-W10 territory. |
| **BD-21** | `demo/shell/dock/layers/GenericActionBar.vue` | **DO NOT ADD** | W5F-01's renderer limb | X.W4.d's file bounds (CC-043). Recorded as a NOT-ADD so A3's cure does not stray into W4's unit. **Same for `ActionBarLayer.vue`** (already DO-NOT-TOUCH in W5 §4, CC-080/X-W8) and `ActionButton.vue` (whose absence from **X.W4.d's own bounds** is that record's actionable carry — §4 CE-2). |

---

## 4. CrossEdges

- **CE-1 · X-W5.a ⟂ X-W8 (MT-DOCK-LAYERS-1) — the `bindPane` conflict. ESCALATION REQUIRED BEFORE W5 OPENS.** W5.md §5 gives `usePaneRouter.bindPane` ownership of "the instance refs it dispatches onto". `Dock.md` **R-8** narrows `bindPane` to non-command uses, moves command dispatch to a `DockCommand` provide/inject registry at MT-DOCK-LAYERS-1, declares "this wave may not claim C-3", and **supersedes G-I's seam text** so G-I's cure lands at X-W8 and "the gate no longer waits on any sibling". Both cannot be true. Affected: W5F-01, W5F-02, W5F-11, W5F-64; gates A3, A4. Dock's sanctioned split line (DISSENT-1) is sequenced commits under ONE name — never a second wave name.
- **CE-2 · X-W5 ⟂ X-W4.d (CC-043).** W5 §10 Depends-on X-W4. GAB-1/GAB-11 · ABN-2 · ABT-2 fold to X.W4.d **D1-D4** *paired with* X-W5.a **A3**. Two bounds/regression facts travel: **(i) X.W4.d's file bounds do not name `ActionButton.vue`** yet D1's single-render-path collapse cannot land without rewriting or deleting it (that record's stated actionable carry); **(ii) cure-regression guards** — AB-30 (the collapse must seat actions on `DockControl` or carry the coarse floor) and AB-32 (`SceneActionSet` must carry an active/selected member or the only palette-open indicator regresses under a green gate).
- **CE-3 · X-W5 ⟂ X-W6 (.j and the routed scenes).** `AuroraPane` **AP-1** routes Atmosphere to X.W6.j as "**the X-W5 scene contract** adopted per route"; `wb-extract-pane` XP-25, `wb-extract-imageeyedropper` EY-15, `wb-extract-workbench` XW-14 route "bespoke scene composition" to X-W5; `wb-mix-sourceselector` MSS-17's transition is X-W6.j while W5's D4 censuses it. **One `regions[]`/scene contract must be authored in W5 and consumed unchanged by W6.j.** X-W6 **H1** (`gate-no-chassis.mjs`) fails any unit that "factors two instruments' stages into one parameterised housing" — CC-108/A-17 killed the InstrumentChassis and W5.md §3 already states its compositions are bespoke by construction. **W6 file-bounds gap of record**: `EasingSpecimenStrip.vue` + `useSpecimenRows.ts` are not in W6's lists (that record's re-verified NAMED GAP).
- **CE-4 · X-W5 ⟂ X-W2 (reciprocal, `W2.md:284`).** X-W2 owns the **B3 appear-cancel strand** (`@appear-cancelled` unbound ⇒ the hero blob never mounts again for the session on any deep-link boot), the **eager-picker cold-boot cache**, and the dead `:on-appeared` right-plate pair. A2/A6's MEASURE-AT-OPEN and C1/C3's inputs are forwarded from W2's ENV block. `e2e/smoke/mobile/` and `e2e/smoke/perf/` are shared as **directories only, never as files**.
- **CE-5 · X-W5 ⟂ X-W7.** PP-8's export contract, PP-17's `ExportFormat` vocabulary, `PaletteRenameInput` A-3's CRUD verdicts, the admin-panel rebuilds (AAP/ATP/AdminNames), the plate register for the ErrorBoundary body, and **ES-25's arming condition** (the `26ch` measure arms when W5 lands Admin full-width). `usePalettePorts.ts` is contended three ways (BD-01).
- **CE-6 · X-W5 ⟂ X-W8.** `PaletteSlugBar.vue`'s deletion needs the **X-W8.a census addendum** or it is lost (W5F-23). The `SESSION_PORT_KEY` move (Dock **L-6**, cure-detail per **R-6**), the 19 `demo/ui` barrels, the Admin/provider SCC, `ActionBarLayer.vue` (CC-080), `status-lamp.ts` + `DockStatusLamp.vue` (MT-DOCK-LAYERS-1 by name — blocks W5F-60), and Dock **G-L** (the rendered arm of C3) all live there.
- **CE-7 · X-W5 ⟂ X-W9 / X-W1.** R-2's library half is CC-084's `Result` battery at X-W9; the `none`-URL born-RED gate enters **X-W1's deploy matrix as a query-parameter state**. `wb-gradient-codeeditor` **A-1** (the `<any-ident>()` throw from `parseCssColor`, declared total) is X-W9's totality row whose demo blast-radius half is W5F-53. **X-W1** owns gate truth: the AAP-12/ATP-15 rider — **whichever wave cures the blockers lands the born-RED spec FIRST** (E-1 twice-audit).
- **CE-8 · X-W5 ⟂ X-W10.** NEW-DU-5 (mobile left-only motion), PS-3 (motion policy, quarantine §1 premise only), D-4's motion tokens, PSC-10's `font-display`-on-controls family (DVS-7's 17-site census), MQ-1/CC-099 (`animation-timeline` inside `@media (prefers-reduced-motion: no-preference)`), and **CC-102**'s producer-contract citation law paired with E2.
- **CE-9 · X-W5 ⟂ X-W3.** A7's announced 404 pairs with X-W3.6's fail-closed `meta.admin` guard + NotFoundPane and X-W3's `componentFor` fail-closed tail (`shell-paneslot` **L-7/PS-8** → X-W3, **one identity** with CC-037/K-25 — typed `componentFor(name: LeftPane \| RightPane)` makes the masking fallback unwritable). `wb-gradient-easingauthoringstage` D2-03 residue: `/easing` and `/about` are ratified at VC:58 and **absent from the 14-path router** → X-W3, with the transposition direction preserved for X-W5/X-W6.
- **CE-10 · GLASS-BJ relay (owner edict; no local patch).** `PSC-2` — the dock's `adaptive-legibility.css` base block collapses `--muted-foreground: var(--foreground)` unconditionally, so the segmented control's entire state signal is a 1.00:1 delta in every engine and **inverted** under `contrast-color()`; the ask must **move the state channel** (weight / non-colour mark / a dock-local selected-ink token) and **never a clamp** (the base rule already IS the clamp's fixed point — D-2-cure KILLED). `PSC-6` — a tokenised `:focus-visible` for `.segmented-tab` (producer `focus-visible` census = 0), which survives at the three preserved P092 sites after this instance dies. `PSC-19` — `useSelectionIndicator` has no invalidation for a relayout that repositions buttons without resizing the container; **the relay must NOT carry D-1's text** (root cause, proof and cure all killed) and its **precondition is the natively-RTL-first-paint probe**.

---

## 5. Excluded from this fold (recorded, not folded)

- **Struck and uncitable in any close of X-W5** (W5.md §3 + this fold): App **L-9**, **D-15**, **C-4's "no h2"**, **D-4's skip-link**, **D-6's nondeterminism**, **D-9's stamp-deletion**; `shell-paneslot` **D-1 as filed** (residual = the gating out-in re-probe), **D-8's canon citation**, **D-7's final limb**, **L-1's usePointerDebug census**, **L-7's visual-REPORT corroboration**, **PS-C's test counts** (26 tests; two suites touch demo/shell); `shell-panesegmentedcontrol` **D-1 (the RTL BLOCKER, all four limbs — the corpus's cautionary exhibit)**, **D-5(b)**, **D-2-cure**, **D-9/D-4's row counts** (14, not 9 or 15), **D-4-cure step 1**, **D-7-forcedcolors**, **L-6-counts**, **L-1's severity + evidence**; `Dock` **R-13** (D3-01's collision framing — "painted directly on top of", "two siblings laid out on top of each other", "100 % overlap of a live control" are **STRUCK AND UNCITABLE**; the 720×900 zoom arm survives independently); `ErrorBoundary`'s "the boot module cannot exist" BLOCKER (killed against the live tree — `main.ts` since `c4af0ef9`), the KeepAlive-cached-boundary cure (**R-7**), EB-6's §5.1 citation (**K-8**), the r2 "step 5" stacking cite (**K-9**); `wb-extract-pane` **K-9** / `wb-extract-imageeyedropper` **K-16** / `wb-extract-workbench` **K-23** (the chassis cure and the golden-ratio numerics).
- **Routed elsewhere, named so nothing is lost**: CC-051's inline-axis half → **CC-056 / V·L3 at X-W6**; the dock-layer half of CC-055 → **CC-080 / MT-DOCK-LAYERS-1 at X-W8**; W5F-19/W5F-17 → X-W10; W5F-20 → X-W2; W5F-23/W5F-25/W5F-60's lamp half → X-W8; W5F-65's CRUD half + W5F-57's export half → X-W7.
- **Dissents preserved unresolved**: App DISSENT 3 (fold-vs-sibling breadth — F's 14-file wave vs O's 6-file wave; the arbiter's ~11-file split, "both original shapes are on the record if the executing session finds this one wrong at contact"); App DISSENT 2 (L-15's rename micro-wave vs O's "booking churn-heavy renames as waves dilutes the formation"); Dock DISSENT 1 (two-wave shape overruled, split line = the port move under ONE name); Dock DISSENT 2 (worker-O's binding caution: "these filings are right about WHAT is broken and unreliable about WHY"); `shell-paneslot`'s four-way disposition of the same forty lines as a fleet-calibration fact; `ErrorBoundary`'s "no X wave names this component"; `shell-dock-mobilemenudropdown`'s MMD-2 ("the row no formation may open without").
- **Registry-blindness events recorded by the corpus** (four in `ActionBarLayer` alone, plus `MigratePalettesDialog`'s and `wb-mix-pane`'s citation-inheritance events): hostile readers repeatedly re-derived standing registry rows without a lookup step. The discipline needs the lookup, not more naming law.

— end of fold. The dated wave specs and every adjudicated record remain byte-untouched beside this file per E-3.
