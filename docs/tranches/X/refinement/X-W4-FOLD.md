SERVED MODEL ID (fold seat, line-1 declaration): claude-opus-5[1m]

# X-W4 REFINEMENT FOLD — the one dated addendum to `waves/W4.md`

## 0. Header — provenance, posture, reading rule

**Provenance.** The value.js corpus = every record in
`docs/tranches/V/megatranche/registry/adjudicated/` that is not `kf-*` / `fr-*` / `pt-*`
(88 components + `parser-band` / `library-band` / `chassis-fitness` / `layout-gestalt`).
This seat read the 45 assigned slug records whole at their routing joints and additionally
**grepped the entire corpus** for `X-W4` and its dotted/middot forms (`X.W4`, `X-W4.a/.b/.d/.g`).
The grep returns **402 hit lines across 47 files** — the 45 assigned records **plus two the
assignment did not name**: `AdminFlaggedPanel.md` (AF-17 → X-W4) and `FlagReportDialog.md`
(X-W4 head read at the routing joint only, no row). Both are folded below. A third record the
assignment named as a packet member, `shell-dock-slugeditlayer.md`, contains **zero** `X-W4`
tokens yet owns the file `W4.md:118` puts in bounds — it is folded on FILE ownership, and that
mismatch is itself §0a n.2.

**E-3 posture (addenda, never patch).** `docs/tranches/X/waves/W0..W11.md` and
`CONFORMANCE-2026-08-03.md` are **NEVER edited**. This file is the fold's sole write. Where a
row below strikes or corrects a phrase in the dated spec, the strike is recorded here and the
spec's bytes stand untouched.

**Reading rule.** At execution X-W4 reads **`waves/W4.md` PLUS this addendum**, in that order;
where the two disagree the addendum's clause governs. Nothing here re-stages, re-weights or
re-composites W4's 16 gates (OC-2/CC-096 staging stands); this addendum sharpens predicates,
adds corpus rows and riders, names bounds corrections, and records exclusions and dissents.

**Anti-rename (M-25).** Every row below is keyed by its **banked identity** — the id the
adjudicating record minted, original for life. `W4·n` indices are a fold-local counting device
only and are **never** a rename. Where two or more records booked ONE identity, the row carries
all sources on one line and is counted ONCE (`≡`). Where a record cross-cites another's row
without re-booking it, the citation is carried as provenance, never as a second row.

**Fold classes.** **cl.1** = spec sharpening / correction · **cl.2** = binding cure-shape lock
or sequencing edict · **cl.3** = bounds correction (§3) · **cl.4** = EXCLUDED / escalated,
does not fold as a W4 build row (§5).

**Arithmetic — countable from this file's own bytes.** **123 rows** booked in §1, contiguously
numbered `W4·1 … W4·123` (unit a ×34 in §1a **+ ×4 in §1a′**, the ProfileSection/MobileMenuDropdown
twins split out because both files are bounds-new · unit b ×12 · unit c ×2 · unit d ×51 ·
unit g ×16 · unhomed clusters ×4), **29 gate entries** in §2 (**13 sharpenings of EXISTING W4 gates**
A1-A5 / B1-B3 / C-family / D1-D4 / G1-G4, **16 genuinely NEW born-RED candidates** N1-N16),
**31 distinct paths** in §3's BoundsDelta (§3a = 25 rows / **30** paths, two of them widenings; §3b = 2 rows,
of which **1** is net-new — `ExtractControls.vue` repeats across the two tables), **14 cross-edges** in §4,
**9 dissents/escalations** in §5. Of the 31, **24 are named in NO `W4.md` table at all** and **7** appear
only in the trigger-gated `.g` table under an access too narrow for a declared cure.
The IN side (402 grep hits over 47 records) is a **receipt**, not document-internal arithmetic:
it is falsifiable against the corpus, the OUT side against these bytes. No in-equals-out
identity is asserted — 402 hits include head-matter routing legends, instrument blocks and
closing verdicts that book nothing.

### 0a. Corrections recorded, never silent

1. **The assignment's slug list is 45; the corpus's X-W4 surface is 47.** `AdminFlaggedPanel.md`
   (AF-17) and `FlagReportDialog.md` were caught by the whole-corpus grep, not by the list.
   AF-17 folds at `W4·22`; FlagReportDialog books no W4 row (its X-W4 token is a head-matter
   routing read) and is recorded here so its absence is a result, not an omission.
2. **`shell-dock-slugeditlayer.md` routes 35 rows and books NONE of them at X-W4** — every
   disposition reads `NO-WAVE-OWNER` — while `W4.md:118` holds `SlugEditLayer.vue` as **modify**
   and gates **A1/A2/A4 name that file's three `DockControl compact` controls and its
   placeholder-only input as their measured RED**. The record was written without the wave file
   in hand. **The consumer half of D-7/L-4/C-5, D-3/C-6/L-5/A-4, A-10, A-11 and A-12 IS
   X-W4.a's by bounds**; the record's `NO-WAVE-OWNER` on those five is STRUCK here (cl.1). Its
   other 30 rows stand as filed.
3. **`wb-extract-controls.md` §1 names a live spec inconsistency**: CC-105 books
   `ExtractControls.vue:32,75` with receiving surface **X-W4.g**, and **W4.g's own bounds table
   omits the file** (`W4.md:159-173`). Carried at `W4·105` and §3.
4. **`shell-dock-actionbutton.md` names the registry correction twice** (its §gestalt and its
   closing verdict): "**X.W4.d's file bounds must name `ActionButton.vue`** before its collapse
   can be real." `ActionButton.vue` appears in NO X wave's bounds; D1's single-render-path
   collapse cannot land without rewriting or deleting it. §3, first row.
5. **Census drift settled once (RK-5).** r-DU said seven `any`s on the action path; r-LC
   enumerated nine; **X.W4.d gate D2's scoped census of TEN (App 6 · usePaneRouter 3 · Dock 1)
   is the count of record.** Recorded so no seat re-litigates arithmetic. Likewise
   `ActionBarContext` has **NINE** fields (R-LC's K-12 correction of L's "8" adopted), of which
   **three** are dead (MISS-6), not one.
6. **`ABT-10`'s coarse cell (33.3×33.3) and W4's own A2 cell (23.3×23.3) disagree for the same
   dock controls.** Both are sub-floor; probe conditions differ. The **discrepancy itself is a
   visual-audit input**, not a defect in either number (the record says so). Neither is struck.
7. **MT-F004's exoneration is inherited, not re-litigated** — W4 §6 already applies the o19
   A-7 counter-measurement (339.4×24 / 198.8×44). No corpus row below re-opens it.
8. **Dated correction — §3's file count, `2026-08-28`.** This file's own §0 and §3 headers previously
   read "**28 files**" for the BoundsDelta. Recounted from these bytes: §3a = **25 rows / 30 paths**
   (two rows carry three and four files), §3b = **2 rows / 1 net-new path**, distinct total **31**.
   The undercount was the two multi-file rows read as one path each. Corrected in place, recorded
   here rather than silently amended — the arithmetic is falsifiable against §3's own cells
   (`grep -oE '\`[a-z][^\`]*\.(vue|ts|css)\`' | sort -u`). No row, gate, edge or dissent changes.
9. **AuroraPane's `X-W4.b` rows are MECHANISM-OF-RECORD citations, not W4 bookings.** AP-12 and
   AP-14 name `X-W4.b`'s LabeledField mechanism verbatim and then state that
   `AuroraPane.vue` is ABSENT from W4.b's bounds, so the pane's own edit rides **X.W6.j**.
   Carried as a cross-edge (§4), never as a W4 row.

---

## 1. Rows

Columns: **id** (banked, original for life) · **source(s)** · **sev** · **mechanism** ·
**cureShape + locks** (verbatim where load-bearing) · **seq/dissent**.

### 1a. Unit **X.W4.a** — shell control geometry and naming (CC-040 · CC-041 · CC-045)

| # | id | source(s) | sev | mechanism | cureShape + locks | seq/dissent |
|---|---|---|---|---|---|---|
| W4·1 | **D-7 / L-4 / C-5** ≡ PaletteSlugBar **A-23** (live half) | slugeditlayer · PaletteSlugBar | MAJOR | All three controls pass `compact` — the sole opt-out from the producer's coarse floor; **240 of the corpus's 398 sub-24px tap-target rows (60.3%), recomputed by that seat**; the always-mounted layer is the single densest sub-floor surface in the app | Consumer half = **X-W4.a by bounds** (§0a n.2 strikes the record's NO-WAVE-OWNER). Producer half → BH relay with the **CORRECTED ask — "non-zero `--dock-compact-control-size`/`--dock-compact-control-min-width` defaults; the touch-floor `:not`-clause drop is STRUCK from the relay text (K-2)"** | Orphan half of A-23 dies with PaletteSlugBar's deletion; live half is this row |
| W4·2 | **D-3 + C-6 / L-5 / A-4** | slugeditlayer | MAJOR | The slug input is optically absent in **every** state including focus (`:85 bg-transparent border-none outline-none`, no `<style>`, no ring/shadow anywhere restoring an indicator) and is the only dock/identity field with no accessible name — a 22-char placeholder that **is muted to invisibility and then un-muted**: `placeholder:text-muted-foreground` is nullified because the producer re-pins `--muted-foreground: var(--foreground)` inside `:where(.glass-dock)` (`adaptive-legibility.css`) — placeholder ink ≡ value ink | **Lock, verbatim**: "the glass-ui `Input` migration (third booking of the same identity: S-audit `glassui-consume-map:165/:208`) cures **D-3, C-6, A-4 and L-5 in one move**." A border alone would not cure A-4 | Supplies the MECHANISM A4's RED cell lacks |
| W4·3 | **A-10** | slugeditlayer | MINOR | Names are inaccurate, not merely terse: "Switch to slug" (:94) names the one path the input frequently will **not** take (everything else escalates privilege, C-1); "Generate new slug" (:106) and the menu's "Regenerate slug" name the same destructive call two ways, neither disclosing destruction | Name the actual dispatch; the donor's state-bearing submit label returns | Pairs C-1 (§1u3) |
| W4·4 | **A-11** | slugeditlayer | MINOR | The slug-edit MODE has no name anywhere in the a11y tree: the `<form>` carries no label/role, the host `DockLayer id="slug-edit"` passes none; activating the layer swaps the entire nav content with nothing announced | Name the mode at the layer root | Distinct from C-7/D-5 (where focus LANDS) |
| W4·5 | **A-12** | slugeditlayer | MINOR | `:77 gap-1.5` hard-codes the producer's desktop rhythm; `.dock-layer` gap = `var(--dock-layer-gap)` contracts under `--dock-scale` on coarse (~4.7px at 0.78) while the form's 6px stays fixed — the row's halves desynchronize exactly where the dock is most constrained | Consume `--dock-layer-gap` | Equality at desktop scale 1 is why D's proportion pass recorded "same" |
| W4·6 | **ABT-12** | actionbartoggle | MAJOR | **Label in Name, SC 2.5.3 — NEW ROW at wave open; no CC row names it.** Accessible name "Toggle action bar" (:90, overrides content) does not contain the visible label "Tools" (:96-98) — voice-control users cannot activate the control by saying what they read. All three axes HELD the proof and read it only for ghost-presence | **Cure is a subtraction**: let "Tools" name the button; the verb moves to ABT-5's `aria-expanded` | Desktop/≥1024 only |
| W4·7 | **ABT-10** | actionbartoggle | MAJOR | The visible wordmark is deleted by VIEWPORT WIDTH (`v-if="isDesktop"`), so 200% zoom and every phone get an unlabelled, tinted, plate-less glyph — **content lost as a consequence of an accessibility accommodation (SC 1.4.4)**. Coarse target 33.3×33.3, mechanism **corrected per R-B**: the `compact` width/height override discarding the producer's coarse floor, **NOT** the touch-floor rule D quoted (dead for the whole dock) | `shape="icon"` sans `compact` restores the floor via the size clamp, not via the quoted rule; **the label-survival arm attaches to CC-040**; the label-bearing register rides ABT-7's X-W8 rewrite | §0a n.6 |
| W4·8 | **ABT-5** | actionbartoggle | MAJOR | The toggle's ON state is unobservable in BOTH channels: `aria-pressed="true"`/`.is-active` exist only while the host layer is inert+aria-hidden+opacity-0 (the same ref that sets them swaps the layer out); on Back the state clears before re-entry. Focus is destroyed on activation onto a roleless div reading a raw colour string, with no restoration contract. `:tabindex="visible ? 0 : -1"`'s `0` branch is a no-op on a native button (`as: {default:"button"}`) | **Structurally a disclosure, not a toggle**: `aria-expanded` + `aria-controls` + a focus contract. **NEW ROW at wave open; no CC row names it.** The reshape rides whichever structure survives: under Dock-adj (f) the trigger is ABSENT without commands and the semantics attach to the surviving seat | Gate N6 |
| W4·9 | **AB-8** (C-7/D-10 state leg + MISS-3) | actionbarlayer | MAJOR | A three-mode disclosure control exposes no state to AT — no `aria-expanded`, `aria-pressed` or `aria-controls`, only a mutating accessible name (:129-133) — while the producer's ink register lights on the bare selectors `[aria-expanded="true"]`/`[aria-pressed="true"]`, so the visible consequence (the toggle wears resting material while holding a mode open) is equally missing | **The `active` prop is the ONE-PROP cure**: `DockControl active?: boolean` "Stamps `aria-pressed` + `data-active`; the icon shape composes the `.glass-capsule` selected seat" | Sibling models the semantic by hand (ABT-5) — the same miss in the other direction |
| W4·10 | **AAP-19** ≡ **ATP-26** | AdminAuditPanel · AdminTagsPanel | MAJOR | `size="sm" class="h-7 px-2"` re-pins a height the producer clamps away (`min-block-size` wins; only `px-2` takes) and **freezes the inline axis out of the coarse lift**: under `pointer: coarse` `--control-h-sm` = max(54px,44px) = **54px** while Tailwind holds the inline axis at 28px — a **28×54 capsule**. 13 `h-7 px-2` copies | **Lock**: `size="xs" iconOnly` — `--control-h-xs`, padding-inline 0.5rem ≡ `px-2`, coarse floor preserved. **A consumer one-liner, NOT a producer relay**; K-1/K-8 kill the axes' new-producer-rung ask | reader-1's demotion of D's MAJOR sustained at fine pointer; the **coarse arm is the sharper defect** |
| W4·11 | **M5** (PaginationBar design M5 ≡ library #2) | PaginationBar | MINOR | `iconOnly` never passed though this is the producer's documented exact case (":6 W5-a11y" self-identifies the shape): `data-icon-only` square geometry and the `data-control-target` coarse hook are withheld, so the arrows render as `sm` lozenges off-register against the producer's 36×36 square | FOLD → the A3/CC-045 machine (AAP-19's cure class). **COORDINATION LOCK: X-W8's same-touch edits these exact attribute lists — sequence the two waves' touches; the `iconOnly` arm carries an INTENDED geometry delta and MAY NOT ride under W8's null-DELTA gate** | **Tap-target limb affirmatively NOT a defect** — both readers independently: coarse tokens clear 44px by another route. Recorded so it is never re-raised |
| W4·12 | **M6** (PaginationBar design M6 ≡ library #4) | PaginationBar | MINOR | `class="h-4 w-4"` does not trip the producer's sanctioned `size-*` escape hatch, so `.button > svg:not([class*="size-"])` still matches and the winner falls to cascade-layer accident; under coarse the producer intends 24px glyphs (`--ui-glyph`) while the pin holds 16px inside a 54px control. Fleet: ~20 `h-N w-N` icon sites, **zero** `size-N` | Same file, same touch, same geometry machine as W4·11 | Contract violation static-certain; painted consequence layer-order-derived, UNPROVEN #3 |
| W4·13 | **AAP-21** ≡ **ATP-27** ≡ **AF-17** ≡ AdminNames **D-11/C-9** ≡ **M-DU3** ≡ PaginationBar **C-5/D-4** ≡ **M4** ≡ **CR-2** | AdminAuditPanel · AdminTagsPanel · AdminFlaggedPanel · AdminNamesPanel · PaginationBar | MAJOR | **The announcement machine, one identity, seven facets.** (i) `aria-label` on a role-less div is ARIA `generic` — the loading region's name is discarded; (ii) three nested `role="status"` skeleton atoms announce a bare "Loading" ×3, at **SIX sites across the cluster** with `AdminTagsPanel:51-56` the sole correct instance; (iii) no result-count live region; (iv) **no list semantics** (`role="list"`/listitem ×0 across all five panels) while `PaletteCardGrid` does it right; (v) the polite region mirrors `page` mutated synchronously **strictly before** the async fetch — the announcement **always** precedes the rows it names, and nothing announces the commit; (vi) the live span carries no `id` so neither pager button can be described by the sentence between them; (vii) the region is **REMOVED, not hidden**, at `pageCount≤1` | **Lock**: the cure is **one atom edit (`AdminListSkeleton` drops per-instance `role="status"`) + wrapper roles — not a per-file patch**; the `<nav>` + `aria-label` arm is the coordinated edit in PaginationBar; announce on **COMMIT**, not intent | Skeleton-atom touch relays to glass-ui if the primitive moves; the commit-signal arm must originate in the composables (C-4's NO-WAVE-OWNER). **CR-2 corrects `AdminFlaggedPanel` AF-5's cell ("mounts one, hidden at pageCount≤1") to REMOVED** — the AF-5 kill that cell supported still stands |
| W4·14 | **ATP-25** | AdminTagsPanel | MAJOR (ruled) | Every delete target is **16×16 CSS px** (12px glyph + 2px padding, derived statically), below SC 2.5.8's 24×24 floor on both axes, **one per tag, unbounded in count**, and invisible at rest (ATP-2 compound) | Local geometry ≥24×24 **or** the W7 seat rework, **whichever lands first — cross-declared from both ends per the W4-D2 lesson**. The prescribed Chip cure is KILLED (K-3: producer base = 20×20; 44px coarse-only; the stylesheet is orphaned) | CC-040 sub-24 family; **a route class A1's measured cell does not enumerate** |
| W4·15 | **ATP-2** (24px floor arm) | AdminTagsPanel | BLOCKER (at home) | The delete control is `opacity-0` at rest, its only reveal hover-gated inside `@media (hover: hover)`; `opacity: 0` is paint-only so on touch the invisible box remains hit-testable — and hitting it IS the unconfirmed cascade of ATP-1 | Primary BUILD → **X-W7** (G14 seat rework makes the affordance rest-visible and confirmed); **the 24px floor arm coordinates with X-W4 CC-040** | Coordination row, not a W4 build row |
| W4·16 | AdminNames **D-12 / C-8** ≡ **MSS-10** | AdminNamesPanel · wb-mix-sourceselector | MAJOR | An unnamed `role="group"` of toggle buttons with roving tabindex controlling **zero** panels, while the producer ships all three affordances (`semantics="tabs"`, `SegmentedTabOption.controls`, `ariaLabel`) **unused**; MSS-10 is the same construct owning a `v-if`/`v-else` panel | Pass the three shipped affordances | MSS-10's riders folded at INFO: the `Array.isArray` branch defends a signature that cannot occur (emit typed `(value: string)`; the file's own :48 comment concedes it) — **a deliberate contrivance** |
| W4·17 | **SFB-30** | SearchFilterBar | MAJOR | The TRUE a11y rows the killed "namelessness" claim shadowed: two unnamed `radiogroup`s (:21/:33, no `aria-label`), `.section-label` divs (:20/:32) carrying no `id`, and a doubled utterance from the sibling `<span>` | Name the groups; associate the captions | K-8 killed the parent claim; these survive it |
| W4·18 | **SFB-33** | SearchFilterBar | MINOR | Two keyboard models in one menu: roving-tabindex RadioGroups (one stop each) vs one stop per tag checkbox — **keyboard cost scales with data**, compounding with the 112px window | One traversal model | — |
| W4·19 | **SFB-22** | SearchFilterBar | MAJOR (new) | The swatch's only accessible name is a **raw hex literal** (:78) — read out character-by-character; also the sole colour carrier under forced-colors. Sharpens C-30: 41 name-mutations per drag are 41 mutations of an unreadable string | The repo owns `useColorNameResolution` (5 consumers) — use it | MCP-20 precedent |
| W4·20 | **SFB-3** (accessible-name half) | SearchFilterBar | BLOCKER (at home) | `aria-label="Filters"` (:5) overrides the subtree per accname, so the count badge **is spoken to no one**; the clipping half (`contain: paint` on `.glass-wash`) means it **renders for no one** | Name half → X-W4; badge/trigger recomposition → X-W7 | The governing D pass-5 had demoted this mechanism-proved cross-engine BLOCKER to a state-matrix line; re-carried numbered |
| W4·21 | **SFB-21** (a11y cross-note) | SearchFilterBar | MAJOR (new) | The producer field-control destructures **exactly `aria-invalid` out** of forwarded attrs; the working path is the declared `invalid?: boolean` prop, never passed — under strictTemplates the dead binding is indistinguishable from noise | Binding cure-caveat on X-W7's error-state build; a11y cross-note here | P4-4's triage applied mechanically would wave through the one binding that genuinely does not work |
| W4·22 | **AF-17** (D-18 / C-F-12) | AdminFlaggedPanel | MINOR | `aria-label` on a role-less div is not exposed; two skeletons each announce a bare "Loading" | **Cross-booked to W4·13** — the ALREADY-RULED AdminNamesPanel D-11/C-9 identity | Caught by grep, not by the slug list (§0a n.1) |
| W4·23 | **MCP-4** (bar-side half) | MiniColorPicker | MAJOR | Bar-side keyboard hardening on `SearchFilterBar.vue` — W4 owns it by name (`W4.md:126`) | Picker surfaces die with the X-W7 rebuild; **bar-side keyboard hardening → X-W4** | The canonical ruling (`role="img"` + reactive label, deliberately NOT `role="slider"`) is written and implemented at `SpectrumCanvas.vue:5-9` |
| W4·24 | **MCP-20** | MiniColorPicker | MINOR | The dialog's accessible name is the imperative that opened it AND a live function of the value being edited (trigger `:aria-label` interpolates `pickerHex`, rewritten per pointer sample; reka labels the content from the trigger) | Bar side → X-W4; otherwise dies with the rebuild | A's MAJOR→MINOR downgrade sustained (names compute on entry) |
| W4·25 | **MCP-47 / D-19** | MiniColorPicker | INFO | The hex readout is unisolated for RTL — law-grade under VC:154, verified verbatim | D-19's law → X-W4 | Rest of the MCP-47 tail dies with the rebuild |
| W4·26 | **VHD-14** (naming/list/`aria-busy`/`<time>` half) | VersionHistoryDrawer | MAJOR | No live region and no `aria-busy`; no list semantics (`:22-27` sibling divs); **every action named the bare "Revert"**; the load-more button unmounts under its own focus (`activeAfter: "BODY"` inside an open modal) | **Split — BUILD → X-W4** (the file + the two `a11y-control-targets` specs are W4's, `W4.md:236`) **+ X.W7.d** (focus-safe pagination and state machine) | K-3's negative-note kill does not touch the verdict |
| W4·27 | **VHD-2** (interim floor) | VersionHistoryDrawer | BLOCKER (at home) | The sole action is `opacity-0` at rest, revealed only inside the ONE `hover:hover` rule in the served sheet, focus-invisible, still tab-reachable and tap-hittable; its invisible 44px band gives 23 of 24 rows a second rhythm | **Interim floor, X-W4's file touch only**: land `focus-visible:opacity-100` + the coarse-pointer arm ahead of X.W7.d's recomposition | **VHD-1's carve note applies: whichever wave lands second inherits the seam — neither wave's gate may go green over the other's edit** |
| W4·28 | **VHD-36** (two component sites) | VersionHistoryDrawer | MAJOR | `variant="outline"`/`variant="ghost"` are **not props** of glass-ui's Button; both controls collapse to the default `emphasis="secondary"` capsule and the dead attribute falls through to the DOM. Census: **65 lines across 27 demo SFCs** vs **2** `emphasis=` call sites repo-wide | The two component sites die at the X-W4/W7.d touches; repo-wide class → **X-W8** (shadcn abrogation). **At cure time the census must separate components that legitimately publish `variant` (e.g. `EmptyState`) from the dead-on-Button sites** | Invisible to vue-tsc (fallthrough attrs) and to every visual gate |
| W4·29 | **VHD-5** (consumer half) | VersionHistoryDrawer | MAJOR | `w-[380px]` and `sm:max-w-[420px]` are dead: glass-ui's dialog placement CSS ships **unlayered** and outranks every `@layer utilities` rule; `placement.css` pins `width:75%` + `max-width:24rem`. The declared 380-420px sheet has never rendered | Delete the dead classes in X-W4's file touch. Producer facts → **ONE BH-relay letter carrying BOTH the layering question and the size-axis ask**. **K-2 binds the relay: the `h-7` limb is NOT part of this mechanism and MUST NOT ride that letter as if it were** | — |
| W4·30 | **VHD-19** (consumer half) | VersionHistoryDrawer | MINOR | The side sheet never mirrors in RTL (physical `right:0` placement arms) and the current marker is pinned `-left-px` with a physical `rounded-r` | Consumer's `-left-px`/`rounded-r`/`mr-1` physical residue at whichever touch lands first; producer logical arms → the same relay letter | In-scope via the repo's own documented `dir` seam (`index.html:1-12` U-F58) |
| W4·31 | **VHD-20** + **VHD-37** + **VHD-40** | VersionHistoryDrawer | MAJOR + 2 folds | The producer's `scroll` axis is never passed (no `:scroll` at `:3`; default `!1`) and the consumer hand-rolls the scroll frame across three elements; `mt-4` double-spaces the header seam to 32px over the producer's own `gap: 1rem`; `mr-1 h-3 w-3` fights the button's gap, the RTL seam and `--ui-glyph` | **One prop + delete the three hand-rolled classes at `:3/:4/:15`** — the same edit deletes VHD-37's `mt-4` and VHD-40's three utilities | A-1's producer-repair framing stays KILLED (K-1) |
| W4·32 | **VHD-17** (constraint, not a build row) | VersionHistoryDrawer | cl.2 | Served `semantic.css` registers `text-caption` with `font-style: italic; font-weight: 400`, whose clamp arithmetic reproduces D's measured 12.03/12.18/14.38 ladder exactly — **italic Revert and italic Load-older**, visible in `shots/02`/`07` | **BINDING CONSTRAINT: X-W4's interim file touch MUST NOT re-pin `text-caption` on controls.** The roles-per-matrix cure is X.W7.d's under X-W10 law | The one "~" row in D's §6 table that was wrong |
| W4·33 | **K-2** (VHD kill, carried forward) | VersionHistoryDrawer | cl.1 | The generalized law "per-instance box-model overrides on glass side sheets silently do nothing" is FALSE: Button CSS is layered (`@layer components`), `.h-7`'s `height` WINS its cascade; the 36px is `.button`'s `min-block-size` flooring it | **`h-7`'s cure is X-W4's size axis (`size` prop, delete the utility — W4 scope 3 verbatim).** Two of four overrides die by unlayering; the "single relay retires all three" cure would leave `h-7` exactly as dead | Correction carried so the relay is not over-asked |
| W4·34 | **PP-16** (own-sites half) | PalettesPane | MAJOR | The ruled "quiet ghost" demotion of delete-all never landed (`PalettesPane.vue:64` ships a filled secondary capsule); the kebab trigger wears a capsule wash (`PaletteCard.vue:98`) | This pane's own sites BOOK → **X-W4** (CC-040's semantic-controls re-vocabulary; **glass-independent**). The duplicate-name twins are IDENTITY-FOLD → CurrentPaletteEditor **ADJ-M3** | reader-D's false-positive correction ratified: `CurrentPaletteEditor.vue:97` is a WatercolorDot whose `variant` IS a declared prop — **struck from the blast list** |

### 1a′. Unit **X.W4.a** — the ProfileSection / MobileMenuDropdown twins (bounds-new; §3)

| # | id | source(s) | sev | mechanism | cureShape + locks | seq/dissent |
|---|---|---|---|---|---|---|
| W4·35 | **D-16** | profilesection | MAJOR (rescoped per K-4) | The component seats page `Button` primitives in the dock band (28px via `--control-h-xs`) while the band's own family is content-sized and its own twin already uses `DockTrigger` — **wrong control family, not wrong size rung** | **"X-W4.a — 'Shell control geometry and naming' owns this surface by name."** | Separator asymmetry → U-6 |
| W4·36 | **N-2** | profilesection (reader-DU) | MAJOR | **Nine hand-pinned absolute glyph sizes per file** silently defeat the producer's coarse ramp (`--ui-scale: 1.5`, `--ui-glyph` 16→24px, menu rows to 44px) — and the **always-coarse mobile twin is the worst-hit surface**. Sharpens D-6: the producer's `> svg` rule covers only Button hosts; `menu.css` ships **NO** svg sizing | Consume the ramp; the missing menu-glyph rule → **demo-root rule or BH-relay mark** | "Legal target size is the wave's goal text" |
| W4·37 | **N-4** | profilesection (reader-DU) | MINOR | Four `target="_blank"` anchors, zero new-window indication (WCAG 3.2.5/G201), two of them inside an open menu | Indicate | Both twins |
| W4·38 | **A-6** | profilesection | MINOR | Glyph vocabulary + aria drift inside one file: `LogIn` marks BOTH "Switch account" (:81) and "Login" (:118) — one arrow for opposite verbs; only 3 of 11 glyphs carry `aria-hidden="true"` though all are decorative, **and the file states its own decorative-glyph rule at :168-173 and applies it to a minority** | Rider with N-2's glyph sweep | No AT harm today (names carried by row text) |

### 1b. Unit **X.W4.b** — producer control axes (CC-045 · CC-046 · CC-047)

| # | id | source(s) | sev | mechanism | cureShape + locks | seq/dissent |
|---|---|---|---|---|---|---|
| W4·39 | **GEN-30** (Selects half) | wb-generate-pane | MAJOR | Every visible label is disconnected from its control: an orphan `<label>` with no `for`/control (:287-291); `section-label` spans unbound to triggers **whose `aria-label`s differ from the printed text** (:221/:223, :255/:257); a naked-number Badge (:150-152) | **"LabeledField is literally this cure"** → X-W4.b. Slider label + Badge → the VC:104 composition row, **NO-WAVE-OWNER** | Split row: two limbs, two homes |
| W4·40 | **GEN-16** (head only) | wb-generate-pane | MINOR | The two control labels wear the mono/value voice (`.section-label` → `text-mono-caption`, a producer utility) against card law 13's sans-owns-controls matrix | X-W4.b deletes both labels here (LabeledField); the **utility's voice is a design-system row → glass-ui BH relay** | The bold tail is KILLED (K-6) |
| W4·41 | **GEN-22** (≡ L-5-residue / C-8-residue) | wb-generate-pane | MINOR | The reka-ui `AcceptableValue` reach (:33) + two unchecked `as PresetName`/`as HarmonyName` casts into literal unions a render-critical computed dereferences | Type hygiene only — the producer guard forecloses the throw (K-11) | **CORRECTION carried: L-5's "the producer withholds the type" premise is DEAD.** `SelectEmits`/`SelectionValue` are exported (component barrel + root `export *`), and `GenerateControls.vue:14` **already imports from `@mkbabb/glass-ui`** — the token was one identifier away on an existing import line |
| W4·42 | **GEN-5** (local three sites) | wb-generate-pane | MAJOR | All three plate buttons pass the retired `variant` prop and render at one secondary emphasis; the primary-register comment :153-155 is false at runtime; the "ghost" icon pair renders as filled capsules | Local three sites ride any wave touching the file (**X-W4.b holds it**); repo-wide **51 sites / 22 files** → NO-WAVE-OWNER (glass-7 residue class) | **The producer is correct and needs no relay** (`axes.d.ts`: tone is "NEVER a `variant` member") |
| W4·43 | **GEN-17** | wb-generate-pane | MINOR | Two physical utilities break RTL — `ml-auto` (:156), `text-right` (:288); logical forms exist | Rides X-W4.b's file touch; **strictly NO-WAVE-OWNER** | Recorded as a rider so it is not lost if .b's carve excludes it |
| W4·44 | **DVS-7** (mechanism note) | dockviewselect | MAJOR | The bare reka trigger carries **NO producer type role** (`triggers.css` sets geometry/colour only), so deleting `font-display` requires a **consumer-composed type role** | **This is exactly CC-047's shape** (the producer variant was REFUSED; the title goes through the type register in the trigger slot). Row itself → X-W10/CC-098 | **Scope corrected by that seat: 17 sites / 8 files across shell+color-session (D said 13) — a dock-wide voice, so the cure is a register FAMILY row, not a two-line patch** |
| W4·45 | **DVS-14** | dockviewselect (missed-2) | MAJOR | Admin mode is a **colour-only state**: `aria-label="Select view"` static across modes; the only mode-dependent bindings are two colour writes (:70/:80); no name change, no text, no ARIA state — against VC:83 | Name/state/value class → X-W4 | The option set does swap textually, but that signal is unannounced and requires OPENING the menu; at the closed trigger the entire authorisation signal is one 24px glyph tint — **and DVS-1 already deleted the other colour carrier, so the intended "two signals" are one, at ~2:1** |
| W4·46 | **EC-16** | wb-extract-controls | MAJOR | The O-18 cure landed on **one of two rail copies**: `GenerateControls.vue:282-306` admits the clone ("the extract k-slider pattern verbatim"), still ships the retired `text-muted-foreground` (:288 — the exact token `ExtractControls:144-147` names as the measured failure), carries no certified fill and no ring; `data-o18` census = 3 files, **the twin absent, so o18 cannot fail on it** | → X-W4 (**GenerateControls.vue is in W4.b's bounds**); the pin-note travels | The twin is gate-invisible by construction |
| W4·47 | **EC-38** | wb-extract-controls (both readers, blind cross-corroboration) | MINOR | The demo re-declares the producer's `fira-code` utility, **unlayered**, dropping `font-feature-settings: "liga","calt"`; `utils.css` enters via a plain `App.vue:199` import so the demo copy outranks every `@layer` | → X-W4 (**`demo/styles/utils.css` is in W4's bounds, modify-carve**) | Harmless today (same token); **the producer's recipe is no longer retunable** — that is the defect |
| W4·48 | **R2** (K-1 + D-4, one defect two sides) | Katex | MAJOR (floor; BLOCKER pending live re-run) | The un-hidden `<annotation encoding="application/x-tex">` node: **zero `annotation` rules** in the vendored sheet or the app (`.katex-mathml` is clip-path-hidden, which suppresses PAINT only), so it sits in the text/selection/find/AT trees — a triple payload on selection/copy/find and a duplicated AT name | **`.katex-mathml annotation{display:none}` — one rule, harmless under Chromium.** Note the vendor already ships `katex/contrib/copy-tex` (exports map read) as the first-class clean-copy answer for D-4 | AT-name half needs the live WebKit differential; the engine-independent half is proven structurally |
| W4·49 | **R16** (D-1, ruled down) | Katex | MINOR | RTL mirrors math (`*L`, `ₙY`, reversed cases-brace, witnessed in the committed rtl frame); no `dir` on the root, vendor sheet has only flex `direction:` | Cure: `dir="ltr"` on the KaTeX root (math never mirrors) → the standing **U-F58 register**, **echo at X-W4 beside CC-046** | Unreachable in the shipped product (`index.html dir="ltr"`, no switcher); canon anchors misapplied |
| W4·50 | **GEN-8** (B3 receipt sharpening) | wb-generate-pane | MAJOR | The count slider's thumb is a 12px transparent ring over arbitrary user colour; the hand-rolled rail sits outside the contrast census its remediated twin is inside | Thumb geometry → **glass-ui BH relay** (producer-owned; **"the CC-046/A-16 SliderProps receipt rides X-W4.b"**). **LOCK on the receipt**: `--slider-track-bg: transparent` is **load-bearing, not decorative** — without it the producer track paints `--secondary` OVER the hand-rolled ramp, so **any rail cure that deletes the per-instance override must land the ramp through the producer's track input in the same move**. Rail + CC-105 → X-W4.g; o18 enrollment → GEN-CLUSTER | VC:104 is the standing law ("adopt that one composition … not new slider mechanics") |

### 1c. Unit **X.W4.c** — the Gradient stop rail (CC-042)

| # | id | source(s) | sev | mechanism | cureShape + locks | seq/dissent |
|---|---|---|---|---|---|---|
| W4·51 | **GV-30** | wb-gradient-visualizer | MINOR | The Copy control is named only by an undeclared `title` fallthrough | X-W6.b rider **+ X-W4 coordination** (the accname law `aria-label > … > title`) | Coordination row |
| W4·52 | **MISS-3 guard** (rail arm) | picker-componentsliders-consolerail | cl.2 | Among the five `--focus-ring-shadow` consumers, **`GradientStopEditor` is the SOLE law-conformant control** — it does not defeat the shared WHCM register | **BINDING GUARD on unit c: C4's focus-ring work must not regress the one control in the repo that already obeys the register.** The rail's ring is the pattern authority for C4's visible-ring arm | Recorded so a "unify the ring" cure does not lower the only conformant site |

### 1d. Unit **X.W4.d** — the typed `SceneActionSet` (CC-043)

| # | id | source(s) | sev | mechanism | cureShape + locks | seq/dissent |
|---|---|---|---|---|---|---|
| W4·53 | **GAB-1** ≡ **ABN-2** ≡ **ABT-2** ≡ **GEN-3** ≡ paneslot **L-2** | genericactionbar · actionbutton · actionbartoggle · wb-generate-pane · shell-paneslot | BLOCKER | The mobile no-op, five-times independently confirmed: mobile `<PaneSlot>` (App.vue:83-91) binds **no** `:on-mount`; only :105/:131 (desktop) do; the three refs are `ref<any>(null)` written only in `onDesktopLeftMount`/`onDesktopRightMount`; nine `paneRefs.X.value?.y?.()` closures; `GenericActionBar.vue:28 @action="act.handler()"` invokes **unconditionally** and **zero `disabled:` keys exist across `usePaneRouter.ts:191-224`**, so no descriptor can ever be disabled | **FOLD, identity preserved** — this is the program's own born-RED: **D4** (source-measured) + **X-W5.a A3** (`{mobileChanged:false, desktopChanged:true}`, RED of record). **What the corpus ADDS and D4 must carry**: (a) the renderer-side limb (unconditional invocation, un-spellable `disabled`); (b) the **FLASH-LIE** — `ActionButton.vue:94` flashes unconditionally **BEFORE** `:98`'s emit, so the control confirms an action that provably did not run; (c) **ABT-2's worst limb: mobile edit Save/Cancel — `colorPickerRef?.commitEdit()` no-ops while `mobilePaneIndex = 1` still runs, so the UI signals success while the edit is DISCARDED** | **cl.2: D4/A3's close must witness the Save/Cancel discard EXPLICITLY.** The parity cure itself is X-W5's — closing D4 by wiring the mount here takes X-W5's work and the bounds check catches it |
| W4·54 | **GAB-2** ≡ **AB-14** | genericactionbar · actionbarlayer | BLOCKER | Shell presentation depends on untyped pane instances; layout decides whether an apparently valid command exists. Two homes and neither is the dock: `ActionBarContext` (`keys.ts:17-27`, **NINE fields**) and `DockActionBar` (`usePaneRouter.ts:49`); `handler: () => void`; the contract imported **backward** into the dock layer | → D2/D3 | **Census of record = D2's scoped TEN** (§0a n.5) |
| W4·55 | **GAB-7** | genericactionbar (r-LC missed, adopted) | MAJOR | The contract types dynamism it structurally forbids: all three producers wrap **dependency-free literals** in `computed()` — the getters construct closures without reading any reactive source, so the computed can never re-evaluate and `Ref<DockAction[]>` is ceremony. `iconClass`/`disabled`/`accentColor` never set; `label`/`icon` byte-equal to the opener's own fallbacks; `rotateOnClick` live 2-of-9. **Six of ten interface fields can never change a rendered byte** | The fields land on the **deleted-symbol list** (`action-contract-diff.md`, W4 artefact 6) | **The structural root of GAB-3 and the sharpest justification of D1's anti-fake falsifier** ("fails if the collapse is faked by a union alias") — recorded as such by that seat |
| W4·56 | **MISS-6** | actionbarlayer (R-LC) | MINOR | `cssColorOpaque` and `formattedCurrentColor` are produced into `ActionBarContext` for consumers that do not exist (11-hit census; the tell — `:105` feeds ActionToolbar the **injected** `safeAccent` while the context carries its own `cssColorOpaque` for the same prop). **Three of nine members dead, not one** | **The `SceneActionSet` collapse must not re-carry them; gate D1's member count is the hook** | — |
| W4·57 | **AB-32** | actionbarlayer (R-DU′ M-6) | MINOR (cure-regression rider) | The AB-13 collapse silently drops `activeStyle`: `DockAction` has no such member; `ActionToolbar.vue:46` feeds the palette-active stroke through it; `Dock.vue:157`'s GenericActionBar path passes `:actions` + `:accent-color` only | **VERBATIM LOCK**: "**X-W4.d's `SceneActionSet` must carry an active/selected member — which AB-8's `DockControl active` prop then renders for free — or the only palette-open indicator regresses under a green gate.**" | Per the exemplar law: the cure is adjudicated separately from the defect |
| W4·58 | **AB-30** ≡ **ABN-6** ≡ **AT-4** | actionbarlayer · actionbutton · actiontoolbar | MAJOR | The five primary actions are **32×32 on coarse pointers with no floor**: `.action-button-wrapper { width:2rem; height:2rem }` is a plain `<button>` that reads neither `--dock-control-size` nor the coarse `--dock-control-floor: var(--dock-touch-target, 2.75rem)`; the producer's coarse floor is scoped `.dock-icon-button:not(--compact):not(:where(.glass-dock *))`, unreachable by any route. Adjacent siblings clamp to 44 | **VERBATIM LOCK**: "the AB-13 collapse must seat actions on `DockControl` (or carry the coarse floor) as an **explicit cure clause**" | **cl.2 CAUTION (AT-4): 32px PASSES WCAG 2.5.8's 24px minimum — `gate A1's fine-pointer floor alone would go GREEN over this defect. The coarse-floor arm must be asserted separately** (gate N3). Reachable cell: tablet-landscape (≥1024w ∧ aspect ≥1.1 ∧ coarse); C-20 measured 32×32 and recorded PASS. K-7 killed D-5's smallTapTargets corroboration |
| W4·59 | **AB-13** ≡ **AT-8** ≡ **GAB-8** | actionbarlayer · actiontoolbar · genericactionbar | MAJOR | `ActionToolbar` is a hand-unrolled duplicate of its own sibling `GenericActionBar`: byte-identical root div (`flex items-center justify-around flex-1`), identical `activeHover` model, 92 lines + five re-declared emits forwarded straight back vs 28 lines over a typed `DockAction[]`; rendered as a mutual exclusion at `Dock.vue:156-157`. Drift visible in the bytes (only `:9` carries `icon-class`; `:21/:32/:43/:56` are the holes where the others' were deleted) | → D1 ("the Dock renders one action path"). **"GenericActionBar is the proof the cure is a deletion, not a design."** One importer, one render site, not exported from `dock/index.ts` | Unique-keys limb stays killed (K-6); the axis's falsification of Luna KILLED (K-7) |
| W4·60 | **GAB-4** | genericactionbar | MAJOR | Mix's descriptors are never operative in ANY layout: `actionBarContext` is a plain object literal unconditionally exposed, `viewSchema` pins mix `left:"color-picker"`, so `App.vue:38` is non-null in every desktop Mix layout and `Dock.vue:156` **always** wins; on mobile `mixPaneRef` is never written. `clearSelection` has exactly ONE caller (`usePaneRouter.ts:220`) — **zero reachable callers in the tree** | → D1 (the `v-if`/`v-else-if` priority deleted by the single render path) | **Framing corrected per K-2: not "can mask", ALWAYS masks** |
| W4·61 | **AB-3** (containment half) | actionbarlayer | BLOCKER (cross-component) | Activating the Tools trigger while no action-bar context exists selects a face id that never registered; the producer's active-watch early-returns; every face de-activates; the dock collapses to an empty sliver with no recovery path, the trigger surviving as a named, focusable button over a visual void | **Split**: containment = **ONE predicate** — guard `toggleActionBar` on `hasAnyActionBar`, **or** `disabled`+`aria-hidden` the toggle when `!visible` — rides X-W4.d (Dock.vue is in bounds). Gestalt cure (trigger renders from the crossfade's own `faces` registry via `defineExpose({faces})`, **not** the unpublished context) accretes to X-W8.d | **ABT-3 co-anchors with D1's one-render-path.** Interim belt if any wave touches the file first: `:inert="!visible"` on the slot root + `if (!hasAnyActionBar.value) return;`. Corrections adopted: the owning surface is `Dock.vue`+`ActionBarToggle.vue` (ActionBarLayer cannot cure it); **NOT mobile-specific** |
| W4·62 | **ABN-1** ≡ **AT-11** | actionbutton · actiontoolbar | BLOCKER | The hand-rolled dock keep-open is unbalanced in **BOTH** directions and corrupts a shared refcount it does not own. No latch, no dispose, zero lifecycle hooks in 137 lines. **LEAK**: glass's `open`-prop watch syncs WITHOUT emitting, so `handleClick` → `activeHover=null` → prop-close never reaches `:86`. **OVER-RELEASE**: glass always passes a defined `open` → reka `useVModel` non-passive → `set(value){triggerEmit(value)}` UNCONDITIONAL → a brush-and-leave before the 300ms `openDelay`, or a bare blur, emits `update:open(false)` → `:86` releases a token never taken → eats `Dock.vue:87`'s edge-driven hold → **the dock idle-collapses UNDER ITS OWN OPEN ACTION BAR**. Counter clamps at 0 — silent corruption, never an error | Interim minimal cure = C-1's: keep `:open`/`@update:open`, **add `keep-dock-open`** (producer ships the whole protocol: `keepDockOpen` on `Popover.vue.d.ts:16`; `dockContext.d.ts` names `<Popover trigger="hover" keep-dock-open>` verbatim). The re-seat composes the producer prop. **L-1's cure-as-written is KILLED (K-9)** | Measured trace: keepOpen ×2 / release ×4, **first call a bare release**. **AT-11: "No X gate asserts hold balance today"** → gate N1 |
| W4·63 | **ABN-3** | actionbutton (r-LC missed #1, adopted) | MAJOR | On a coarse pointer the hover contract silently becomes a **CLICK** contract: `matchMedia("(pointer: coarse)").matches` is evaluated **ONCE at setup into a plain `let`**; when it matches, `trigger="hover"` renders reka's `PopoverRoot`/`PopoverTrigger` — click semantics — so one tap both fires the action and toggles the description card, acquiring a keep-open token whose prop-driven close emits nothing (ABN-1's leak arm, fully live because coarse+≥1024 disables the always-expanded mask) | → cure clause: **the successor seat states its coarse contract explicitly**. The once-evaluated coarse probe is a producer trait — **one line on the standing glass-ui BH relay** | **The probe is non-reactive: a hybrid laptop is locked to its mount-time input mode.** All three axes assumed HoverCard universally |
| W4·64 | **ABN-4** ≡ **AT-3** | actionbutton · actiontoolbar | MAJOR | Selection is an inline stroke only: **no `aria-pressed`, a stroke-width delta of exactly zero** (lucide ships `stroke-width: 2` so `strokeWidth:'2'` changes nothing), and an ink **bit-identical to hover ink** (`:30` `--hover-color` and `ActionToolbar:46`'s selected stroke both from `cssColorOpaque`). Predicate also wrong: `paletteActive` means "not on the picker view" — at `/mix` the Palette glyph lights while the open pane is Mix; and `isEditing ⟹ paletteActive` while the SAME button carries `:disabled="isEditing"` — accent-stroked AND 50%-dimmed on every edit, by construction. `action-pulse`'s `100% { stroke: currentColor }` overrides the inline accent for 400ms on every click of the active button | → X-W4.d — **AB-32's rider governs verbatim** (the `SceneActionSet` carries an active/selected member; `DockControl active` then renders it for free); the predicate fix rides the contract collapse. Canon half → X-W10 (two-channels-one-axis ink) | Hue as the sole signal = WCAG 1.4.1. Corpus BLOCKER label on D-1 demoted per r-DU's own fusion note; R2's extract limb killed |
| W4·65 | **ABN-5** | actionbutton | MAJOR | The focus ring is a token-fallback accident and a **fourth** house focus register: `focus-visible:ring-ring/40` reaches `--color-ring`, defined **NOWHERE** in demo/ or producer CSS (the only grep hit is `focus-ring.css:11`'s prose ABOUT its emptiness) — the utility never generates and the ring falls to opaque `currentColor`; `grep forced-colors demo/shell` → **0**, under an authored `outline-none` | The re-seat inherits the producer focus register; the literal deletes. Canon half → X-W10 (ABT-11's identity; shares the rung/floor cure) | Provenance narrative killed-in-part (K-5); the ":4 never a per-control literal" clause survives as the indictment |
| W4·66 | **ABN-7** ≡ **GAB-20** ≡ **AT-2** | actionbutton · genericactionbar · actiontoolbar | MAJOR | A Popover impersonates a Tooltip: `description` reaches no AT, no touch user, no disabled seat. The sole copy of title+description lives in `PopoverContent`; the trigger carries `aria-label` alone — no `aria-describedby` (reka hover surfaces are deliberately unannounced). The nine authored `description` strings are unreachable content on the one surface where this row is the only command face | glass ships Tooltip/Trigger/Content/Provider; `demo/ui/tooltip` already barrels all four — **the cure is one import away**. The successor seat adopts the producer primitive and wires the description into `aria-describedby` or the tooltip body | Keyboard limb KILLED (K-6: reka's `onFocus→onOpen`). **GAB-20's unproven touch limb is DISCHARGED STATICALLY by ABN-3** — on coarse a tap opens the card AND fires the action: reachable, and worse than unreachable |
| W4·67 | **ABN-8** ≡ **AT-13** | actionbutton · actiontoolbar | MAJOR | The click flash cannot restart (`isClicked = true` over `true` is not a reactive change — the class never leaves), a stale unowned `setTimeout` truncates it mid-flight (measured 228ms of 400), and the `400` literal triplicates the CSS `0.4s` (`:122/:125/:126`) | Press feedback re-authored on the producer's interruptible press register — **edict 6: the keyframes move, they do not die** | **Cleanup limb already BUILT at Dock-adj DELTA-13 (C-27 names `ActionButton:95`) — cross-booked, not double-counted** |
| W4·68 | **ABN-9** ≡ **AT-1** | actionbutton · actiontoolbar | MAJOR | The success flash is the only truth of Copy and fires before the result is known: `ColorPicker.vue:323-326` `void writeClipboard(…)` discards the producer's discriminated `CopyResult` (`{ok}`/`{ok:false, reason}`); `:94` flashes before `:98` emits; `ActionFeedback` has exactly one consumer (PaletteCard) so nothing else reports the copy. The shipped cure (`useClipboard()` — status idle/pending/success/failure, `resetMs`, `onCopyError`, stale-call guard, scope teardown) sits unused in the same module | **RIDER (cl.2)**: "D4's no-silence law extends naturally from unregistered-action to failed-action — **a rejected clipboard write may not render identically to success**." `ColorPicker.vue` is in D's file bounds | **If the wave declines the extension, this row is a NAMED formation-boundary residue.** PRM limb carried only as a feedback-truth defect on VC:101 grounds — **not** as a missing local PRM arm (M-03/M-29 rule that FALSE) |
| W4·69 | **ABN-10** ≡ **AT-5** | actionbutton · actiontoolbar | MAJOR | The disabled arm sets the **native** attribute, so the only explanation of WHY is unreachable, the seat leaves the tab order, and `cursor: pointer` keeps promising. `:17-18` set BOTH `aria-disabled` and `disabled`; a natively disabled button is non-focusable and dispatches no pointer activation, so neither hover NOR focus can open the card on exactly the two seats disabled on `isEditing` | **CURE RIDER, binding (cure-is-adjudicated-separately law)**: "any `aria-disabled`-without-native cure **MUST carry `if (disabled) return` in `handleClick`** — `handleClick` has no guard today and D-4's cure-as-written would make disabled seats **firable**." `DockControl`'s four-state `disabled` contract is the shipped cure | Limb 3 KILLED (K-4). AT-5's atomicity limb killed (K-1) — the surviving defect is invisible rationale, not data loss |
| W4·70 | **ABN-11** | actionbutton | MAJOR | Four live implementations of "exactly one of a set is open", the protocol defined in the **LEAF** with every parent re-implementing its half: `hoverKey`/`activeHover`/`update:activeHover` re-bound on all five ActionToolbar seats and in GenericActionBar; `usePopupMutex`; `useHoverPopover`; and the producer's own cluster transit, which `dockContext.d.ts` names as the owner | The protocol deletes with the re-seat; `keep-dock-open` + reka cadence own it. **If a hard mutex is genuinely wanted it is `usePopupMutex`, once** | `usePopupMutex`'s BH relay is Dock-adj C-26's, already booked |
| W4·71 | **ABN-13** | actionbutton | MINOR | `class="pointer-events-auto"` on `<Popover>` never reaches the DOM (`inheritAttrs:!1`, render binds no `$attrs`; PopoverContent's copy at `:35` does land) | Delete `:9` with the re-seat | Corpus MAJOR demoted by both readers, ratified — inert today, prospective hazard only |
| W4·72 | **ABN-14** | actionbutton | MINOR | (a) `action-rotate` lists two `forwards` animations both driving `transform`; per CSS Animations §3 `action-spin` wins and the pulse's 1.3→1.15→1 curve is dead weight. (b) **NEW, statically certain**: the pulse's 100% frame `transform: scale(1)` is held by `forwards` for 400ms while the pointer is still over the control, whose hover base is `scale(1.2)`; animation origin outranks the normal declaration, so every press goes 1.2 → 1.3 → **1.0 (below rest-hover)** → hold → snap back to 1.2 — **the celebration reads as a recoil**. (c) `demo/DESIGN.md`'s bespoke-keep row cites `:117,120,121`; the live literals are `:122/:125/:126` — **the exemption has silently stopped covering what it exempts** | One animation, one curve, moved with the seat (edict 6). DESIGN.md anchor correction → X-W10 rider (DOC-1 kin) | ≡ **AT-14** (Tailwind v4's `-rotate-180` emits the standalone `rotate:` property, absent from `transition-[transform,stroke]` — the 180° turn jumps both directions) |
| W4·73 | **ABN-15** | actionbutton | MINOR | The hover affordance covers 24×24 of the 32×32 hit box — `.action-icon:hover` targets the glyph, not the wrapper: a 4px clickable dead ring | Dies at the re-seat | Compounds W4·58. Kin: AB-31's three-hover-grammars row (X-W10) |
| W4·74 | **ABN-16** | actionbutton | MINOR | Six dead public members across the leaf, its parent, and the contract one file away: `label` (+`.action-label` with **zero CSS rules anywhere**), `hidden`, `canProposeName`, `clearHover`+`defineExpose` (zero callers), and `DockAction.iconClass` (declared `usePaneRouter.ts:44`, forwarded by `GenericActionBar:26`, set by NONE of the nine actions — **missed by all three axes' dead-surface censuses**) | The contract collapse deletes them; **gate D1's member surface is the hook** | **`hidden` ADDITIONALLY shadows the HTML global attribute — a consumer writing `hidden` destroys the subtree, possibly with a token outstanding: the reason the name may never return.** Parent's dead template refs already booked at Dock-adj C-35 |
| W4·75 | **ABN-17** | actionbutton | MINOR | Two custom properties carrying one value, stamped inline per instance, after a four-hop drill of an injectable: `:30`'s identical expression twice; `SAFE_ACCENT_KEY` inject → prop → prop → leaf when `Dock.vue:35` injects it directly; the `?? 'currentColor'` doubly redundant | The leaf loses the colour prop; one token at the row root. **ABT-8's one-call `useSafeAccentFn("floating")` cure names the route** (X-W10) | — |
| W4·76 | **ABN-19** | actionbutton | MINOR | No gap token in the action row; the gutter is a by-product of `justify-around` at content width, beside a 1.25× larger sibling. Both consumers render the identical bare `flex items-center justify-around flex-1` div | The re-seat inherits `--dock-layer-gap`; proportion canon → X-W10 | r-DU's "missed" spacing row on GenericActionBar folds to THIS identity — cross-cited, not re-booked. 0px/6px per-route numbers = U-3 |
| W4·77 | **ABN-21** ≡ **GAB-6** ≡ **AT-15** | actionbutton · genericactionbar · actiontoolbar | MINOR | The action row has **no group semantics**: no `role="toolbar"`, no group name, no roving focus on either container; 3-5 sequential tab stops with nothing announcing the group; the opened group's root div has no role/name while `DockActionBar.label` is documented as the OPENER's label | The successor row states its group semantics — a cure clause on the same surface as W4·66. **Two attributes and a keydown handler**; the repo knows the pattern (ConsoleRail's managed-tabindex rail) | L attributed this to a missing producer primitive — **KILLED (K-3)**. Gate N5 |
| W4·78 | **ABN-22** | actionbutton (r-LC missed #2, rescoped) | MINOR | A keyed-reuse hover card survives a route swap: open under no pointer, re-labeled with the next route's copy at a stale anchor. Premises verified: un-keyed GenericActionBar, shared `copy` key across all three scenes, `actionBarLayerActive` survives the swap | Key the bar per scene, **or** reset hover state on scene swap — one line either way. **Gate shape named by the record**: "a spec assertion that no popover is open after a route change with no pointer over the dock" | Unmatched-release limb KILLED by that seat (K-10). Gate N2. Reachability window = U-10 |
| W4·79 | **ABN-28** | actionbutton (r-DU missed #5) | INFO | The same verb ships two names and two affordances by route; one seat is a bare noun among verbs ("Reset color" + rotation on the picker vs "Reset" plain at `usePaneRouter:208`; "Palettes" nounal). `title` feeds BOTH `aria-label` and the card heading, so the split is simultaneously visual and accessible-name voice | `SceneActionSet` titles get one home; voice canon → X-W10 (o10d is the standing oracle) | — |
| W4·80 | **ABN-31** | actionbutton (r-DU missed #10) | INFO | `close-delay="0"` on the sole carrier of the description prose — help text vanishes the instant the pointer leaves a 32px target | Cadence decision at the tooltip adoption, with W4·66/W4·58 | Mitigated by reka's grace area |
| W4·81 | **ABN-25** | actionbutton | INFO | Partial reactive-props destructure, **2 of 12** | Dies at the rewrite; no separate booking | D's "eleven-member"/"six from eleven" counts **corrected to 12 / 6-from-12** |
| W4·82 | **ABN-30** ≡ **AT-21** | actionbutton · actiontoolbar | INFO | Four stray blank attribute lines at ActionToolbar `:21/:32/:43/:56` — **the first instance has none, so the file is internally inconsistent too**; the visible residue of the drift C-3 predicts | Dies with any touch of the file | — |
| W4·83 | **AT-9** | actiontoolbar | INFO | `activeHover`/`hoverKey` are `string`, not a command-ID union: a typo in any of the five `hover-key` literals would type-check and kill that popover silently | → D3's "token union, not `string`" clause. **R2's caveat adopted and BINDING: `GenericActionBar` feeds `DockAction.key: string`, so the union must be GENERIC, not a literal enum** | Constrains D3's falsifier design |
| W4·84 | **AT-16** | actiontoolbar (R1) | INFO | `hover-key="browse"` names a live sibling `ViewId` while the button routes to `palettes` (`viewSchema.ts:124-127` defines a real `browse` view; `Dock.vue:55` routes the emit to `palettes`) | The token union makes it unspellable | Load-bearing for popover exclusion, so nothing breaks — **a false name pointing at a live concept** |
| W4·85 | **AT-20** | actiontoolbar (R1) | INFO | `copy` is a read verb with a hidden persisted write: `ColorPicker.vue:324` `updateModel({inputColor})` before the clipboard write; `inputColor` persists across reload | Declare it in the contract, or split it | Defensible ("show what was copied") but undeclared — the emit is typed `copy: []` and the description promises a read |
| W4·86 | **AT-10** | actiontoolbar | INFO | Glyphs, emits and labels are three parallel lists with nothing tying them; the one place the correspondence has actually drifted is W4·84 | Folded into W4·83/W4·84's cure | — |
| W4·87 | **GAB-3** | genericactionbar | MAJOR | The command boundary cannot express whether an operation ran: `handler: () => void` (`:46`); the renderer **discards the call expression** (`:28`); the owners are async (`GenerateControls:106-108`, `GradientVisualizer:127-129`, `MixPane:49-55` — the true shell owner per K-4) | The `SceneActionSet` models outcome/`unavailable` (D1/D4). **Rider from K-5: deleting the boundary is NECESSARY, NOT SUFFICIENT — the route-local copy seats must stop discarding `CopyResult`** (`GenerateControls :107/:112` already banked at GenerateControls-adj row 15/B5; `GradientVisualizer :128` → GAB-18) | cure part-killed (K-5) |
| W4·88 | **GAB-16** | genericactionbar (r-LC missed) | MINOR | Three-hop all-optional dispatch degrades every wiring regression to silence: `GeneratePane.vue:22-26` / `GradientPane.vue:11-15` forwarders whose **second `?.` is unreachable-by-construction** (both children render unconditionally) | Subsumed by **D2** (the `?.()` deletion); the forwarding `defineExpose` relays land on the deleted-symbol list | — |
| W4·89 | **GAB-17** ≡ **MR-13** | genericactionbar · wb-mix-resultdisplay | MINOR | Two implementations of the Mix result-copy derivation with divergent feedback postures: `MixPane.vue:49-55` bare `writeClipboard`, no feedback; `MixResultDisplay.vue:30-47` `useClipboard` with live `copied` state — **and the dock routes to the silent one** | `copyResult`'s only caller is the shell dispatch — **it dies with the nine dispatches; MixResultDisplay's honest seat remains the sole owner** | hidden behind the axis mis-citation killed at K-4 |
| W4·90 | **AB-20** + ruling 4 | actionbarlayer | MINOR | `provide(COLOR_MODEL_KEY, actionBar.colorModel)` (`:22`) is a provable identity no-op with a **false comment** ("so ColorInput works unchanged" — it resolves the same object from App regardless) | **Split**: the LINE rides X-W8.d G-10's addendum as dead surface; **the `colorModel` CONTRACT FIELD dies at X-W4.d** — `UseColorPipelineReturn` living in the dock's presentation contract solely to feed a no-op is contract debt | R-LC's kill of the D negative record's "genuine re-scoping" absolution RATIFIED (K-8): **a defence entered into a negative record forecloses the cure and is worse than a miss** |
| W4·91 | **AB-21** | actionbarlayer | MINOR (⇄ ColorInput D-9) | `editTarget` is drilled App→Dock→ActionBarLayer→ColorInput and read by nobody at the end; `EDIT_TARGET_KEY` is the live DI home. Dock's own copy IS live | The dead leg dies with MT-COLORINPUT-1's split + the contract collapse | Cross-booked, not re-booked |
| W4·92 | **ABT-8** (prop/thread deletion half) | actionbartoggle | BLOCKER (at home) | `accentColor` is set by **NONE** of the three bars — the `??` chain paints a default through three files; and the Tools identity (`"Tools"` + `Paintbrush`) is authored **independently in two modules**, so the chain reconciles a duplication that need not exist | **The prop/thread deletion falls out of X.W4.d's contract collapse (D-15 dies with it).** Ink canon + rung → X-W10 (`useSafeAccentFn("floating")` fixes all three violators) | The rung root cause (SAFE_ACCENT_KEY certified against the RESTING rung while the dock is FLOATING chrome) is X-W10's, stated so the "delete the prop" ruling does not erase the rung lesson |
| W4·93 | **ABT-17 / D-14** | actionbartoggle | INFO | `label` is a constant wearing a variable's type — no overflow design | Dies with X.W4.d's typed contract | D-11 → X-W8 (D2-07); L-11 → NO-WAVE-OWNER |
| W4·94 | **MP-3** | wb-mix-pane (reader-LC missed #2, rescoped) | MAJOR-latent | `startMix` is the **sole throwing** exposed member and its dock dispatch (`usePaneRouter.ts:221`) sits **OUTSIDE** the application's only ErrorBoundary — `<nav class="dock-band">` + Dock close BEFORE `<main>`; the boundary is inside `<main>` wrapping only `.pane-container`. Unreachable today (GAB-4's always-masking desktop + the mobile null refs) | **SEQUENCING CONSTRAINT ON X.W4.d, binding**: "the moment X.W4.d cures GAB-2 by binding refs, it **ARMS** an out-of-boundary throw — the identical bad-operand mix becomes announced-and-recoverable from the in-pane button and a **silent Vue-internal console error** from the dock button. **X.W4.d must land the guard (or boundary rescope) in the same change.**" | Booked beside GAB-2's route so the wave cannot land the ref-binding without the guard. Gate N10 |
| W4·95 | **GV-16** (+ GV-63, GV-60 limbs) | wb-gradient-visualizer | MAJOR | `Ref<any>` + `?.` chains + the `reset`/`resetGradient` rename; **GV-63**: the pane takes a string-ref + `InstanceType` template ref (`GradientPane.vue:9/:25`) while both siblings use `useTemplateRef`; **GV-60**: `Seed from palette` is a named dock command with an unnamed subject and three silent failure paths (`if (!pm) return` · `savedPalettes.value[0]` with no chooser · `if (colors && colors.length >= 2)`), the whole dispatch path `?.`-masked end to end | → X-W4 (the typed port removes the masking); GV-63 dissolves under it; GV-60's seat half → X-W6.b | Mobile limb → X-W5 |
| W4·96 | **GEN-19** | wb-generate-pane | MINOR | Masking `?.` on methods `defineExpose` makes unconditional, over an `any`-typed bridge in the pre-3.5 idiom — **the exact mask that hid GEN-3**; nine `any`s dock-button-to-seed | → X-W4.d (the `SceneActionSet` deletes the bridge) | return-2's census sharpening adopted |
| W4·97 | **GEN-12** (verb-contract half) | wb-generate-pane | MINOR | One verb, two names ("Copy colors" / "Copy all colors"); three verbs, two owners | Verb contract → X-W4.d; **the o20 containment oracle → GEN-CLUSTER** (its comment is false of the DOM it passes) | AT duplication exists only while the Tools layer is open (PIN 3) |
| W4·98 | **M-I** | mobilemenudropdown (reader-2) | MINOR | `Dock.vue:61-62` `slugEditRef.value?.onStartSlugEdit()` / `?.onCopySlug()` — an unmounted layer is indistinguishable from success | **FOLD → X-W4 scope 7, verbatim owner: "the nine optional-chained `?.()` dispatches … are deleted."** Exactly the silence X-W4 makes impossible | — |
| W4·99 | **L-8** | shell-paneslot | MINOR | `ActionBarLayer.vue`'s in-SFC re-implementation of the removed glass-ui composable carries `void opts.containerEl; // signature parity with the retired producer composable` (`:64-68`) | Local one-liner **+ the standing glass-ui BH relay** (a public content-swap composable) | — |
| W4·100 | **D-8** | profilesection | MAJOR (ruled) | Two copy actions, two state designs; the one attached to the user's **CREDENTIAL** has none — rejection `void`-discarded, empty-slug silent, menu held open showing nothing, while the sibling Share row has a full success state | **"X-W4 — the wave's goal criterion names this class verbatim: 'an action that silently does nothing becomes unrepresentable'."** | — |
| W4·101 | **C-3 / D-9** | profilesection | MAJOR | Regenerate slug: the zero-palettes branch **skips the confirm dialog entirely**; the port **clears storage BEFORE creating the replacement** while its own doc-comment claims "no null gap … atomically"; the call site is a floating promise; no pending/error/result state exists; `tone="destructive"` ships unused | → X-W4 (destructive action ownership). **House pattern cited as the precedent: X-W6's b4 — "one removal owner; the floor is an explicit disabled state"** | **K-6 kills the "no confirm ever" limb** — the >0-palettes branch DOES route through the migrate dialog |
| W4·102 | **ABN-23** + **GAB-21** | actionbutton · genericactionbar | INFO | Behavioural coverage is thin, not zero (the o10d locator + hover-open assert IS behaviour; the keep-open and flash mutations still pass green); **no CI project exercises the generic bar, the mobile action bar, or the mobile edit flow** (smoke-mobile = 3 specs, Pixel 7; o15 runs at 1280×720 on the picker route; safari mix-flow drives the local button) | **X.W4.d's D1-D4 + `scene-action-contract.spec.ts` are the arriving coverage — no separate booking**; cross-booked to X-W5.a A3 and Dock-adj G-I as their motivating evidence | — |
| W4·103 | **GAB-5 / K-8** (escalation, cl.4) | genericactionbar | — | Opening the branch withdraws the dock's navigation face to show verbs that already exist beside their subjects. **CONFIRMED AS FACT; its per-verb DELETE ruling ESCALATED** | **The renderer + untyped branch die at X.W4.d (its bounds already order it).** The per-verb seat question (dock set vs route-local) is an OWNER/canon item at **X-W10** under the standing collision: `VISUAL-CONSTITUTION.md:194` + `DECISIONS.md` B-13 ("Regenerate, Save/Publish and Copy live in ONE Dock control set") + GenerateControls-adj ruling **R-B** (the PLATE cluster is the violation and retires INTO the dock set) | **cl.2 PROHIBITION: "No seat may execute challenge-D's command-seat table for Generate while R-B's banked ruling stands un-overturned."** A corpus may argue canon change; it may not silently overrule it |

### 1g. Unit **X.W4.g** — trigger-gated (CC-044 · CC-003 + banked limbs). CLOSED unless X-W0.j's census returns PASS

| # | id | source(s) | sev | mechanism | cureShape + locks | seq/dissent |
|---|---|---|---|---|---|---|
| W4·104 | **ES-15** ≡ **GEN-2** ≡ **MX-1/MR-1** ≡ **MSS-1** | EmptyState · wb-generate-pane · wb-mix-animationcanvas · wb-mix-resultdisplay · wb-mix-sourceselector | BLOCKER/MAJOR | WatercolorDot 7.0.0 **renders NO slot** and hand-forwards only `class`/`style`: children, `@click`, `aria-label`, `:disabled`, `tag`, `:title` all die at the boundary (`inheritAttrs:!1`, `renderSlot` count 0, root `<span aria-hidden style="pointer-events:none">`). Now **eight independent byte-verifications** across this corpus | **X-W4.g's mechanism is literally this cut.** Genuinely NEW limbs that STRENGTHEN g's born-RED: (a) the static no-slot proof; (b) **the Dock `#collapsed` child-swallow — `Dock.vue:271-283`'s `#collapsed` seal wraps the view icon + admin gold-shimmer + its own `vj-morph` Transition INSIDE the dot; the `:260-268` comment describes a GLYPH morph that cannot be rendering** | Dock limb noted to **X-W5**. "Should the dot ever host children/a seat?" rides **GLASS-RELAY**. **K-14: P051 rules the button/tag branch REMOVED in the clean major and operable sites onto NAMED GEOMETRIC SEATS — CC-044 FORBIDS the polymorphic-root forwarding ask** |
| W4·105 | **ES-20** ≡ **EY-27** ≡ MSS-1's `tag=` sites | EmptyState · wb-extract-imageeyedropper · wb-mix-sourceselector | MINOR | `tag="div"`/`tag="button"` passed to a prop glass-ui 7.0.0 deleted; silently swallowed. The repo-wide sweep is the standing glass-7 `tag=` residue identity (CSS-adj **L-8**) now embodied in X-W4.g | **COUNT OF RECORD (K-9): 21 WatercolorDot bindings / 26 raw hits / 11 files.** Mechanical deletion "at all 18" would strip a Vue built-in from three TransitionGroups and a real SearchBar prop | Exactly **THREE** WatercolorDot `tag=` sites in MixSourceSelector (r2's four-site census KILLED, K-7). The aria-hidden collateral killed at R-3 |
| W4·106 | **AB-15** (AboutPane) / **CC-113** | AboutPane | MAJOR | `ColorSpaceSelector.vue:64` ships `hide-indicator`; selection is carried by dot idle-opacity alone (`opacity: 0.35`); `OPTICAL-BENCH-COMPOSITIONS.md:108` **P051 names this component and this prop** ("ColorSpaceSelector deletes `hide-indicator` and consumes the sole `SelectItem` indicator/gutter … nonzero monochrome/forced-colors selected delta") | → **X-W4.g — CC-113 (A-18 indicator paint), BLOCKED-ON the CC-003 four-condition Glass-8/TR#84 census trigger; the P051 indicator-consumption clause rides that atomic cut** | **No axis and neither of the 15 CSS-corpus rows utters it** |
| W4·107 | **EY-7** | wb-extract-imageeyedropper | MAJOR (r3 BLOCKER dissent preserved) | The confirmation pulse fires once per MOUNT and is silent forever after — the `@animationend` reset never attaches (`inheritAttrs:!1`; the listener lands on nothing, **no Vue warning**, suppressed under `inheritAttrs:false`); `swatchPulse` latches true and is cleared nowhere reachable | **LOAD-BEARING CORRECTION to D-16's cure**: "after the latch the class list is permanently `swatch-pulse` — `w-7 h-7` is off, and the 'redundant' `.swatch-pulse { width/height: 1.75rem }` (`:285-289`) is the **ONLY** thing sizing the dot; **deleting it as D-16 prescribes collapses the swatch after the first Add**" | → X.W4.g face carve; **if W4.g never fires, EY-CLUSTER.** Binds gate G3 ("paint is paint") |
| W4·108 | **EY-32** | wb-extract-imageeyedropper (reader-1) | MINOR | The specimen dot's silhouette **re-randomises on every sample** — a writhing swatch during hover, a hard shape-cut per move under PRM: `animate` defaults false, the `!animate` branch watches the colour ref and re-seeds `borderRadius` per change, and the eyedropper rewrites `sampledColor` per pointermove | Moots at the g face swap → X.W4.g / EY-CLUSTER | **Cites the quarantine's own M-26 measurement in its own direction; no FALSE row carried** (M-25 UNPROVEN and M-26 FALSE are ConsoleRail rows, untouched) |
| W4·109 | **GEN-28** | wb-generate-pane (return-2 missed, byte-verified) | MINOR | WatercolorDot's silhouette **seed is captured ONCE at setup** while its colour is watched — an in-place patch renders `f(newColor, staleSeed)` silhouettes vs `f(newColor, newSeed)` wet edges; a fresh mount of the same pair differs from an in-place update, **contradicting the d.ts's determinism claim** | → **glass-ui BH relay**; **DISCHARGED by X-W4.g on census PASS** | Cosmetic while GEN-2 keeps the dots inert |
| W4·110 | **MSS-20** | wb-mix-sourceselector | MINOR | At n=0 the well's only mark is a 2px dashed hairline **whose colour IS the user's live picker colour** (`--watercolor-color` ← `cssColorOpaque`) — the sole affordance's non-text contrast has **no floor by construction** | → X.W4.g | The 2.88:1 number = U-1 |
| W4·111 | **MSS-22** + **MSS-23** | wb-mix-sourceselector | MINOR | **Nine** dead utilities on the add slot, not five (the producer's inline `pointerEvents:"none"` also kills `cursor-pointer`/`hover:scale-110`/`active:scale-95`, plus `:175`'s class set on a never-rendered `<Plus>`); and `.add-slot-ghost` (`:277-281`) is a **byte-identical scoped twin** of `CurrentPaletteEditor.vue:286-290`, both centring a child the producer cannot render | Revive or die with the seat. The law is in `utils.css`: **"never a scoped twin — the S.W7-7 lesson"** | Rider: the redundant `TransitionGroup` import (`:2`; it is a compiler built-in) |
| W4·112 | **MSS-21** | wb-mix-sourceselector | MINOR | The colors-mode empty state **was designed** (the same seeded-ghost invitation register EmptyState itself uses, `EmptyState.vue:45-47`) but carries no copy (no eyebrow/message/hint) and, per MSS-1, no working affordance | A **caption-ladder cure**, much cheaper than "design one" → X-W6.j + X.W4.g | narrowed by K-5 |
| W4·113 | **MX-41** ≡ **MSS-19** ≡ **MP-5** | wb-mix-animationcanvas · wb-mix-sourceselector · wb-mix-pane | MAJOR-latent | The chip remove control is nameless (sole child a lucide `<X>` carrying no `role`/`aria-hidden`/`<title>` → **no computed name**, WCAG 4.1.2), **16px**, `opacity-0 group-hover` with **no `focus-within` on the group** (invisible-but-hittable on touch), and carries `z-popover` — a portalled-overlay rung (`--z-popover: 130`) on an in-well control | **cl.2: "the row goes LIVE the day X.W4.g revives the add seats — which is exactly why the same wave must carry both."** → X-W6.j / X.W4.g | Unwitnessable today: the add path is dead, so no capture ever contained a chip. LC's narrowing carried: the missing `type` limb has no reachable consequence |
| W4·114 | **MX-32** ≡ **MR-10** | wb-mix-animationcanvas · wb-mix-resultdisplay | MAJOR | The palette mix result — **the only arm a real user can reach** — has NO text alternative: N producer-`aria-hidden` spans (`tag="div"` and `:title` both dropped) + an explicitly `aria-hidden role="presentation"` gradient strip; entire accessible text = "Result". The css list exists (it feeds the clipboard) and is never rendered | **cl.1: the X.W4.g carve (import+face only) is INSUFFICIENT ALONE** — the surface owner is the wb-mix-resultdisplay seat | Compounds MX-8 into a complete non-visual dead end |
| W4·115 | **MP-4** | wb-mix-pane (reader-DU) | MINOR-latent | The add-slot ghost is **doubly** enrolled in the forced-colors tier-1 exemption (`.swatch-row > *` via its TransitionGroup parent AND `.watercolor-swatch` via the producer root), so the sole affordance out of the empty state keeps its own sub-threshold ink and is **contractually prevented from WHCM rescue** | → X.W4.g / X-W6.j rider | **Corrects pass C's mis-attribution ("that is the DC-2 family's fault") — the cause is the CONSUMER's roster line.** The same membership is banked as a superlative for the result dots; both rulings are right — membership is per-surface |
| W4·116 | **EC-2** + **EC-3** + **EC-22** → **CC-105** | wb-extract-controls | MAJOR ×3 | (EC-2) `ExtractControls:22` feeds ONE expression (`trackInk`) to both `backgroundColor` and the `inset` box-shadow — **ring ≡ fill 1.00:1 by construction** whenever the gradient is absent; once the ramp paints, the INSET ring paints over the ramp while certified against the resting plate — **certification referent ≠ paint referent**, falsifying the file's own `:8-11` claim. (EC-3) the sole value carrier of both axes is a producer-default thumb (transparent fill, `--background` border) floated over a ramp derived from the user's own image; the cure exists two directories away (`ComponentSliders.vue:193-206` feeds both tokens from `thumbInk` keyed to `spectrumFieldIsLight`, whose docblock says **"Share the function, never copy the constant"**). (EC-22) no tier differentiation: one `trackInk` for protagonist and support axis | → **X-W4.g via CC-105**, census-gated | **cl.3 SPEC INCONSISTENCY (§0a n.3): CC-105 names `ExtractControls.vue:32,75` with receiving surface X-W4.g, and W4.g's own bounds table OMITS the file.** "The densest defect surface in this corpus is banked behind a release event whose receiving wave does not name the file" |
| W4·117 | **AP-17 / CC-105 ⇄ G-PAINT tension** (cl.4) | AuroraPane | — | **Two live registers claim the `--slider-track-bg` sites**: X-W8 books them CC-105 **BLOCKED-ON** the 8.0.0 repin with receiving surface X-W4.g; `adjudicated/ConfigSliderPane.md` rules the paint **demo-curable NOW** (the **G-PAINT single cut**, where that arbiter proved the naive retire-the-variant cure collapses the thumb to width 0 and that the demo CAN paint the range) | **UNRESOLVED, carried.** That seat routes the identity to the CSP register (the later, measurement-backed ruling) and **"flags the reconciliation to the X-W0 sitting rather than silently electing either"** | This fold does not elect either. §5 |
| W4·118 | **ATP-18** (named candidate) | AdminTagsPanel | MINOR | The error plate pours any exception string verbatim, centred, with no length budget and no disclosure (`EmptyState.vue:23-25 max-w-[44ch] break-words`, no truncation) | **NO-WAVE-OWNER** (shared-atom contract, AAP-16 family) **with a named candidate: both X-W4 (`W4.md:171`) and X-W7 (`W7.md:123`) carve `EmptyState.vue` and either may adopt the budget** | reader-1's citation corrections adopted; the detail rides `plate-ink`, so "full weight" was overstated |
| W4·119 | **SP-13 / SP-24 / SP-27** (utils.css candidates) | ShadowPalette | MINOR ×3 | (SP-13) `var(--skeleton-ink)` is read by **ShadowPalette.vue alone**, so `.skeleton-ink-register` does literally nothing on its other three "consumers" while `utils.css:36-43` claims "every skeleton surface in the app". (SP-24) `--skeleton-shimmer-tint`/`--skeleton-glass-bg`: **zero readers app-wide and producer-wide**. (SP-27) `utils.css:66-68`'s specimen-walk docblock is **stale-wrong about ShadowPalette in both halves** — the designated single source of truth contradicts the tree and the file it governs | **NO-WAVE-OWNER as rows; `demo/styles/utils.css` (X-W4 modify-carve) is the NAMED CANDIDATE** and SP-27 is "a correction duty binding on any wave touching utils.css" | Discharge follows SP-12's relay + X-W7's plate cure |

### 1u. Unhomed clusters proposed for adoption by X-W4 (the three packets + one this seat adds)

| # | id | source(s) | sev | mechanism | cureShape + locks | seq/dissent |
|---|---|---|---|---|---|---|
| W4·120 | **GEN-CLUSTER** | wb-generate-pane (self-declared NO-WAVE-OWNER booking) | BLOCKER-bearing | `GeneratePane.vue` · `useColorGeneration.ts` · `generate-color.ts` · `o20-generate-plate.spec.ts` · the absent o18 generate enrollment. **Verified against all 206 File-Bounds rows: none of the five appears; W6's routed-scene list omits Generate entirely**, while every adjacent surface IS owned (X-W4.b/.d/.g, W5, W7, W8, W10, W1). Carries **GEN-1 (BLOCKER)** — the palette name the user types is discarded on every save; every generated palette persists as "Generated Palette" (emit `save: [colors, name]` fired at arity 2, handler bound at arity 1 hardcoding `pm.createPalette("Generated Palette", …)`; the sink derives `slug` and the dedup key's name half from the constant; **TypeScript's fewer-arity assignability makes it gate-invisible**) | **PROPOSED HOME: X-W4 (a new unit `b′` beside `GenerateControls.vue`) + X-W1 for the oracle half.** Cure lock (PIN 4): **"reshape the emit to `[name, PaletteColor[]]` (reusing `stripColors`) onto `onCurrentPaletteSaved` — ONE edit discharging GEN-1 + GEN-6's save limb + the re-derivation seam."** Rides with: **GEN-13** (the editable title — hover-only rest affordance, hand-rolled focus ring, no maxlength/empty guard, **pre-filled where a placeholder belongs, so naming starts with a destructive select-all-delete on the pane's least discoverable control**; ESCALATION RIDER: **this row becomes the naming seam when GEN-1's cure lands**), GEN-12's oracle half, GEN-8's o18 enrollment, GEN-30's slider label + Badge, GEN-17 | **cl.2: "the cluster is a hole inside owned territory — a wave must adopt it or the BLOCKER's cure file survives the tranche by default."** return-1's kill of L-1's composable-store cure STANDS (K-8) |
| W4·121 | **EY-CLUSTER** | wb-extract-imageeyedropper (+ wb-extract-pane · wb-extract-workbench · wb-extract-imagedropzone · wb-extract-controls) | 4 BLOCKER + 27 | **EY-1..6, 9, 11, 13, 17-22, 25-26, 31, 33-39, 41, 47** — the eyedropper interaction/render cluster. Headline mechanisms: **EY-1** the loupe paints nothing on first show and never on touch (`loupeVisible = true` then a synchronous `drawLoupe` against a canvas still behind `v-if`; every unpin re-arms the race for the whole session); **EY-2** `justUnpinned` is a one-exit latch — every sample after the first costs two clicks, a pan-while-pinned eats the next clean tap, and the discarded click also deletes Add/Apply while `sampledColor` persists; **EY-3** RTL seats the canvas +444px outside its stage and the instrument goes inert; **EY-5** the loupe is centred exactly on the pointer, so on touch **the finger fully occludes the magnifier** — defeating the very affordance every axis rested C-1's BLOCKER weight on; **EY-6** magnification = 9.64/zoom → **sub-1× exactly when the user leans in**; **EY-9** `pointercancel` is wired to `onPointerUp`, so a **cancelled pointer COMMITS a tap**; **EY-11** the ResizeObserver is wired to an initialiser and discards zoom/pan; **EY-22** alpha is read and discarded — a transparent pixel **reports and EMITS opaque `#000000`**, and the value displayed is never the value committed | **PROPOSED HOME: X-W4 — widen the `W4.md:166` "import + face only" carve on `ImageEyedropper.vue`.** Trigger-gated members (**EY-7 / EY-27 / EY-32**) ride X.W4.g behind the X-W0.j census (W4·107-108, W4·105). Already-X-W4 siblings that also exceed the carve: **XP-9 ≡ EY-8** (a hand-rolled non-modal modal — no role, no `aria-modal`, no name, no focus entry/return, no inerting, window-level Escape with no scope; `grep -c 'role=|aria-modal|inert|tabindex'` over 298 lines = **0**) and **XP-8 ≡ EY-29 ≡ XW-20** (the DockControl accname LAYER FORK: **11 `title`-only under `demo/workbenches/**`, 8 `aria-label`-only under `demo/shell/dock/**`, 0 carry both** — a convention decision across 11 sites/4 files/3 features, not one file's lapse and not a producer defect; `/#/extract` owns 12 of the app's 26 flagged instances). Plus **XW-21** (the loupe renders as an empty ring on every touch pin — one `nextTick`/`v-show` decision), **XW-36** (the entrance transition never runs and its refit safety net is dead code: `<Transition name="vj-enter">` with no `appear` over a child with no `v-if`), **EC-44** (the protagonist axis has no visible name at all; kC's only expansion is a hover `title` on a non-focusable `<label>` with no `for`), **EC-7** (`aria-valuetext` is **structurally unreachable** from any consumer — the thumb forwards exactly four aria attrs; GLASS-OWNED, BH relay already seated, **receiving demo surface = X-W4's accname/announcement law**) | **cl.2 IDENTITY**: **EY-4 ≡ EC-11 ≡ XW-3 ≡ ImageDropZone R-2** — the sampler is keyboard-unreachable **by construction**: `ExtractWorkbench.vue:25` makes a click on the drop zone the SOLE opener, and `:22-23` bind `preview` and `disable-click` to the same value, so exactly when the zone becomes the sampler it takes `tabindex="-1"`, its keydown gates off, and `role="button"` + "tap to sample colors" co-occur with inertness. **Cure form corrected (RULING PIN 2): a bespoke keyboard path, NOT the A-17-foreclosed §5 composition.** The opener lives in `ExtractWorkbench`/`ImageDropZone`, **beyond any current carve** |
| W4·122 | **SLUG-CHAIN** (`setActiveTab` route-throw) | slugeditlayer **A-1** ≡ PaletteSlugBar **A-3** (URGENT) ≡ MigratePalettesDialog **A-1** | BLOCKER | **ONE identity across three records, empirically verified TWICE against installed vue-router 5.1.0.** `useSlugMigration.ts:55/:69/:76 setActiveTab("saved")` → `usePalettePorts.ts:88` unchecked `(tab: string) => depsSwitchView(tab as ViewId)` (**why vue-tsc is green**) → `useViewManager.ts:78 router.push({name:"saved"})` → `pushWithRedirect:1243` calls `resolve(to)` **synchronously** → `dist/vue-router.js:670 if (!matcher) throw createRouterError(1,…)`. `ViewId` = 14 members, router = 14 named routes, **neither includes "saved"**; the `:37` catch-all is a **PATH** redirect a named push never matches; the `:73` same-view guard cannot fire. **Three live consequences**: (1) admin login — teardown + `adminLogin` land, then the throw escapes as an **unhandled rejection** because `SlugEditLayer.vue:54` does not await, so the layer's own try/catch cannot see it; (2) slug login with no local palettes — `await deps.userLogin(value)` **SUCCEEDS**, then `:76` throws, is caught by the LOGIN catch, `e instanceof ApiProblem` is false, and the "error" is written to A-2's **permanently-null ref** — a successful login converted into a silently-swallowed router error; (3) migration — `:69` throws inside `pendingMigrateAction` and is degraded to `console.warn("Migration action failed:")`, so **every palette migration reports failure after succeeding**. Fifteen prior runs quoted the offending line verbatim while analysing C-39 and **none checked whether the route exists** | **PROPOSED HOME: X-W4 (`SlugEditLayer.vue` IS in W4 bounds, `W4.md:118`) + X-W5 (typed `ViewId` / pane registry).** Cure lock: **`setActiveTab: (id: ViewId) => void`, `ViewId` imported from the shell that owns it; delete both casts; the author must then pick the real destination (`"palettes"`)** — the tab-name legacy dies. **SEQUENCING EDICT, binding (MigratePalettesDialog A-1): "A-1 must land BEFORE or WITH A-3's sink repair — mounting the sink first converts a silent bug into a user-visible LIE on a successful login."** | Rides with, all NO-WAVE-OWNER today and all on files X-W4 already or nearly owns: slugeditlayer **C-1** (the classifier's unknown bucket IS the destructive branch — anything not four lowercase hyphenated words routes into `clearUserSlug()` + `adminLogin(<garbage>)`), **C-2/D-8/L-2(a)** (the async submit body has **zero** awaits, so the spinner and the disabled guard never paint and the `catch` is unreachable by construction), **D-1/C-3** (`slugError` written six times, rendered zero times; `slugBarRef.value?.setError` created, returned, **assigned nowhere**), **D-2** (`shouldKeepOpen` one disjunct short — the 5s idle collapse runs mid-typing and reopening clears the field), **A-2/A-3** (regenerate **clears before it has a replacement**; on failure the old session is revoked and one refresh loses the account), **A-5**, **A-7** (a mixed-case token shaped like four hyphenated words classifies as a **slug** and posts the credential to the user-login endpoint), MigratePalettesDialog **A-14** (focus restoration cannot succeed by construction; the opener unmounts in the tick the dialog opens — "**X-W4 touches SlugEditLayer for target geometry — adjacent, not this defect; land beside it**") and **A-19** (`onSlugSubmit` is async with zero awaits: `slugSwitching` flips true→false in one synchronous tick, the authored Loader2 spinner is unreachable, the disable guard's second clause never engages, and **N submits can run N concurrent logins**; "land beside X-W4's SlugEditLayer target-geometry edits so the file is touched once"), PaletteSlugBar **A-8** (`ADMIN_TOKEN=""` normalises to `""` and is emitted as an admin token; the `if (!raw) return` guard tests the **pre-normalisation** string in BOTH copies; the parent destroys the user session BEFORE the admin login fails — **LIVE TODAY via SlugEditLayer**; three-legged cure, all legs must land). **Deleting `PaletteSlugBar.vue` discharges 11 rows outright but leaves 17 shipping through SlugEditLayer / ProfileSection / useSlugMigration / the export twin-path / a 51-site dead prop axis** |
| W4·123 | **PICKER-REBUILD** (this seat's fourth cluster — not in the proposed packets) | picker-spectrumcanvas (27 rows + a cure-law) · picker-colorcomponentdisplay (16 rows) · picker-componentsliders-consolerail (5 rows) | 2 BLOCKER + 41 | Both picker records route their whole control-rebuild cluster to **X-W4** — and **neither `SpectrumCanvas.vue` (open partition) nor `ColorComponentDisplay.vue` appears in X-W4's bounds**; SpectrumCanvas is present ONLY in the trigger-gated g table as "import + face only". **A hole of the same shape as GEN-CLUSTER and larger.** Headlines: **M-1 (BLOCKER)** — the display scale and the edit scale disagree by **100×**; committing the readout's OWN displayed number in the DEFAULT space snaps the colour to white (`useSliderGradients:73` inks oklch l=0.623 as "62.3"; the commit path writes RAW and the write gate clamps `Math.min(1, 62.3)` = 1); **11 channels across 6 spaces including the landing state**; the asymmetry inside the very same function proves oversight — the alpha branch DOES divide by 100 when unnormalized, and the slider path passes `normalized=true`. **D-04/C-6 (BLOCKER)** — zero keyboard coordinate on the dominant instrument (`role="img"`, no tabindex, no keydown, no aria-live in the whole file) **and the shipped a11y gate CERTIFIES this shape**. **M-8 + M-19** — the write gate corrupts XYZ/jzazbz whites and bleaches the boot colour to white on an HSL switch | **cl.2 CURE-LAW (M-16), binding on every canvas-shaped cure**: glass-ui 7.0.0 publishes `./canvas` / `useCanvas2D` (a `DprPolicy` defaulting to `min(dpr,2)`, `respectReducedMotion` default true painting ONE static frame and parking the loop, an IntersectionObserver offscreen seam with its own suspend reason, tab-hidden suspend, `dispose()`), and `grep -rn "glass-ui/canvas\|useCanvas2D\|useCanvasLifecycle" demo/` → **ZERO**. "**Any X-W4 rebuild that reaches for a canvas MUST route through this subpath or state why not.**" **cl.2 ONE-DECISION LOCKS**: (i) **M-8 + M-19 are ONE cure decision — the write gate must gamut-map or REJECT, never clamp**; (ii) **D-10 + M-17 + L-4's `overflow: visible` residue are three symptoms of ONE unreconciled geometry — clip-the-marker vs inset-the-domain vs square-the-corners must be resolved as ONE decision**; (iii) **L-1 + M-2: unify ON MiniColorPicker's containment (it is the repo's own reference implementation of three flagship cures — `border border-border`, `overflow-hidden`, a plain-div thumb with nothing competing for `position`, a fixed height, and the gradient kept OUT of per-frame inline style); do NOT "delete the mini's plate outright" as the L-axis prescribed** (X-W8 unification **feeding** the X-W4 rebuild) | **cl.2 IDENTITY — MISS-3 ≡ M-2 ≡ D2-02**: the repo's WHCM focus register already exists, already names `[role="tab"]`, and **five consumer sites DEFEAT it** with unconditional `outline: none` at (0,2,0) against a deliberately `:where()`-specificity-0 register: `ConsoleRail.vue:271-275`, `ColorSpaceSelector.vue`, `ColorComponentDisplay.vue`, `GradientEasingEditor.vue` ×2. **`GradientStopEditor` is the SOLE law-conformant control** (W4·52). "**The cure is at the five sites (or a specificity raise on the shared block), NEVER another line in the register.**" **Only ConsoleRail is in W4's open bounds.** Oracle extension → X-W1 (o27's forced-colors arm reaches only the gradient rail today) |

---

## 2. Gates

**13 sharpenings of EXISTING W4 gates · 16 genuinely NEW born-RED candidates.** L-19 holds throughout: every gate below names a real witness and is a Playwright spec or a compiler/lint invocation — **never a `scripts/proof-*.mjs`** (CC-019).

### 2a. Sharpenings of gates W4.md already declares

| # | existing gate | sharpened by | what changes |
|---|---|---|---|
| S1 | **A1** (fine target size) | W4·14 (ATP-25), W4·1 | A1's measured RED enumerates `/` and `/#/gradient` only. **ATP-25 adds a route class A1 does not measure**: 16×16 delete targets on the admin-tags route, **one per tag, unbounded in count**. W4·1 adds that 60.3% of the corpus's whole sub-24px mass sits in `SlugEditLayer.vue`'s three `compact` controls — A1's `/` cell already names them; the magnitude is new |
| S2 | **A2** (coarse target size) | W4·58 (AT-4 caution), W4·7 (ABT-10) | **AT-4's caution is binding: "gate A1's fine-pointer 24×24 floor alone would go GREEN over this defect — the coarse-floor arm must be asserted separately."** A2 must read the *attributed* box against `--dock-control-floor`, not merely ≥24. ABT-10's 33.3×33.3 vs W4's own 23.3×23.3 for the same controls is a **visual-audit input**, not a correction to either |
| S3 | **A3** (size axis owns height) | W4·10, W4·11, W4·12 | The RED count of 18 `h-7` sites gains its exact coarse arithmetic (`--control-h-sm` = max(54px,44px) = 54 vs a Tailwind-frozen 28px inline axis = a **28×54 capsule**) and its cure lock (`size="xs" iconOnly`, consumer one-liner). M5/M6 extend the same machine to `iconOnly` square geometry and the `size-*` escape hatch — **with the X-W8 null-DELTA coordination lock** |
| S4 | **A4** (zero nameless elements) | W4·6, W4·2, W4·3, W4·4, W4·19, W4·24, W4·121 (XP-8) | A4's rule ranks `title` last and excludes `placeholder`, but **cannot see five distinct failures the corpus proves**: (i) **Label-in-Name** — a NON-empty name that omits the visible label (ABT-12); (ii) an **inaccurate** name (A-10); (iii) a **mode with no name at all** while its controls are named (A-11); (iv) a name that is a **raw hex literal** (SFB-22) or a **live function of the value** (MCP-20); (v) the **`title`-only layer fork** — 11 workbench sites named only by `title`, 8 shell sites by `aria-label`, **0 both**. A4 stays as written; N4 is minted for (i) and the rest are recorded as A4 predicate extensions |
| S5 | **A5** (the target gate bites) | — | Unchanged. Recorded so its non-RED status is not read as a gap |
| S6 | **B1** (composed trigger title) | W4·39, W4·44 | GEN-30 supplies B1's exact orphan-label sites (`:287-291`, `:221/:223`, `:255/:257` — **aria-labels differing from the printed text**). DVS-7 supplies the producer-side reason CC-047 refused the variant: **the bare reka trigger carries NO producer type role**, so deleting `font-display` requires a consumer-composed type role — and the scope is **17 sites / 8 files**, a register family, not a patch |
| S7 | **B2** (trigger height rides size axis) | W4·42 | The 15 `h-9` sites and GEN-5's retired `variant` sites are **the same controls** — the `variant` deletion and the `h-9`→`size` move are ONE touch, not two |
| S8 | **B3** (slider seam receipt) | W4·50 | **The receipt must additionally record that `--slider-track-bg: transparent` is LOAD-BEARING, not decorative**: without it the producer track paints `--secondary` OVER the hand-rolled ramp. Any rail cure deleting the per-instance override must land the ramp through the producer's track input **in the same move** |
| S9 | **C1-C4** (gradient grammar) | W4·52, W4·51 | **GUARD: `GradientStopEditor` is the SOLE law-conformant focus control among the five `--focus-ring-shadow` consumers — C4's ring work must not lower it.** GV-30's accname coordination attaches to the Copy control |
| S10 | **D1** (one contract, one render path) | W4·55, W4·56, W4·57, W4·59 | GAB-7 is **the sharpest available justification of D1's anti-fake falsifier**: six of ten interface fields can never change a rendered byte. D1's member count is the hook for MISS-6's three dead members. **AB-32 is a cure-regression guard on D1 itself** — the collapse must carry an active/selected member or the palette-open indicator regresses under a green gate |
| S11 | **D2** (no untyped action path) | W4·54, §0a n.5 | The scoped census of **10** is the count of record; reader drift (7 / 9) recorded so it is never re-litigated |
| S12 | **D3** (the checker bites) | W4·83 | **Binding constraint on D3's design: `GenericActionBar` feeds `DockAction.key: string`, so the token union must be GENERIC, not a literal enum.** A literal-enum falsifier would be unrepresentable at the real call site |
| S13 | **D4** (no silent no-op) + **G1-G4** | W4·53, W4·68, W4·104, W4·107 | **D4's close must witness ABT-2's Save/Cancel discard explicitly** ("the UI signals success while the edit is DISCARDED"). D4's no-silence law extends to **failed** actions (W4·68) or the row becomes a named residue. **G1's zero-count gains the Dock `#collapsed` child-swallow as an eighth witness. G3 ("paint is paint") gains a trap: `.swatch-pulse`'s width/height is the ONLY rule sizing the eyedropper dot — a "delete the redundant rule" cure collapses the swatch after the first Add** |

### 2b. NEW born-RED candidates

| # | gate | RED witness (real, today) | falsifier | source |
|---|---|---|---|---|
| **N1** | **Dock hold balance.** A smoke spec drives tap→tap on an ActionButton seat and reads the dock's held state: `keepOpenCount > 0` with **zero open surfaces** = RED | Static both-directions proof (W4·62) + the measured trace **keepOpen ×2 / release ×4, first call a bare release**; the counter clamps at 0 so the corruption is silent | Fails if a balanced acquire/release still leaves a token outstanding; passes only when the producer's `keep-dock-open` owns the protocol. **"No X gate asserts hold balance today"** — the record's own words | AT-11 · ABN-1 |
| **N2** | **Route-swap popover reset.** After a route change with no pointer over the dock, **no popover is open** | Un-keyed `GenericActionBar`; shared `copy` key across all three scenes; `actionBarLayerActive` survives the swap → an open card re-labeled with the next route's copy at a stale anchor | Fails if keying the bar per scene is faked by re-rendering the same key; the assertion reads the open state, not the vnode | ABN-22 (gate shape verbatim) |
| **N3** | **Coarse floor as a separate cell.** `a11y-control-targets` asserts the coarse-pointer floor as its own assertion, not as a consequence of the 24×24 fine floor | `.action-button-wrapper { width:2rem; height:2rem }` × 5 seats reading neither `--dock-control-size` nor `--dock-control-floor`; the producer's floor rule is scoped away from plain `<button>`s by construction | **A 32×32 control PASSES a 24×24 assertion.** Fails only when the coarse cell is measured against the resolved floor token | AT-4 (addendum candidate, verbatim) |
| **N4** | **Label in Name (SC 2.5.3).** For every control with visible text, the computed accessible name **contains** that text | Accessible name "Toggle action bar" (`:90`) vs visible label "Tools" (`:96-98`); all three axes held the printed AX snapshot and read it only for ghost-presence | Fails on any name that omits its visible label; a name **added** rather than reconciled fails too. A4's non-empty rule cannot see it | ABT-12 (**NEW ROW at wave open**) |
| **N5** | **Toolbar / group semantics.** The action row exposes `role="toolbar"` (or an equivalent named group) with a single tab stop and roving focus | Bare `<div>` roots at `ActionToolbar.vue:2` and `GenericActionBar.vue:15`; no role, no group name, no roving focus; 3-5 sequential tab stops | Fails on a role without a name, and on a name without the single-stop traversal. **L's "missing producer primitive" diagnosis is KILLED (K-3): two attributes and a keydown handler** | AT-15 ("a G2-family arm for the toolbar group") · ABN-21 · GAB-6 |
| **N6** | **Disclosure state.** The Tools toggle exposes `aria-expanded` + `aria-controls` and a focus contract that survives Back | `aria-pressed="true"`/`.is-active` exist **only** while the host layer is inert + aria-hidden + opacity-0; on Back the state clears before re-entry; focus lands on a roleless div reading a raw colour string | Fails if `aria-pressed` is merely moved; the widget is a disclosure, not a toggle. **NEW ROW at wave open; no CC row names it** | ABT-5 |
| **N7** | **Checkbox vs command in the menus.** The dark-mode row is `role="menuitemcheckbox"` with a live `aria-checked` | `role="menuitem"`, no `aria-checked`, state carried only by `aria-hidden` glyphs, `@select.prevent` suppressing even the close signal; `DropdownMenuCheckboxItem` is exported by the demo barrel with **zero consumers tree-wide** | Fails on a glyph-only state; passes only on the producer primitive. Both twins | profilesection D-7/C-4 · mobilemenudropdown C-4 |
| **N8** | **Menu-item activation.** Enter/Space on the GitHub row navigates | `as-child` on `DropdownMenuItem` is silently discarded (strip set `{as, asChild, as-child}`; `as: "div"` ×8); reka's `SELECTION_KEYS` → `currentTarget.click()` fires on the `[role=menuitem]` **DIV**, which no descendant anchor receives; the row registers no `@select` handler, so Enter/Space **closes the menu with NO navigation**; a pointer click inside the row but outside the ~54px anchor does the same | Fails on `as="a"` (that cure is K-2, dead). Passes on `@select`-as-navigator with the anchor removed, or on a shipped `DropdownMenuLinkItem`. **The twin record's own born-RED spec already asserts "Enter on GitHub (RED today)"** | profilesection D-2 + A-2 |
| **N9** | **The slug submit actually switches the view.** A spec drives each of the three submit branches and asserts the view changed and **no unhandled rejection was raised** | `router.push({name:"saved"})` throws **synchronously** in installed vue-router 5.1.0 — verified twice independently; ViewId 14 / routes 14, neither containing "saved" | Fails while any branch throws; also fails if the throw is merely caught and swallowed (branch 2's current shape). **Order-locked behind the A-3 sink repair** | SLUG-CHAIN (W4·122) |
| **N10** | **Out-of-boundary throw (RED-on-arm).** A bad-operand mix dispatched from the **dock** renders the same announced, recoverable state as the in-pane button | Not RED today (unreachable); **RED the moment D2/D4's ref-binding lands** — the dock band closes before `<main>`, and the only ErrorBoundary is inside `<main>` around `.pane-container` | Fails if the dock dispatch produces a silent Vue-internal console error while the in-pane path announces. **The gate exists to keep the wave's own cure from minting a silent failure** | MP-3 (sequencing constraint) |
| **N11** | **Failed clipboard ≠ success.** A rejected `writeClipboard` renders a state distinguishable from success | `void writeClipboard(…)` at `ColorPicker.vue:323-326` discards the discriminated `CopyResult`; `ActionButton.vue:94` flashes **before** `:98` emits; `ActionFeedback` has exactly one consumer, so nothing else reports the copy | Fails if the failure state is only a console line. Passes on `useClipboard()`'s shipped status register. **If the wave declines the extension, W4·68 is a named formation-boundary residue** | ABN-9 · AT-1 |
| **N12** | **The readout's own number is idempotent.** Committing the value the readout displays does not move the colour | oklch l = 0.623 inks "62.3"; the commit path writes RAW and the write gate clamps `Math.min(1, 62.3)` = 1 → **white**. 11 channels / 6 spaces **including the landing state** (`DEFAULT_COLOR_SPACE = "oklch"`) | Fails per channel, so a single-space fix cannot pass. **Glass-independent — must not wait on any Glass-8 trigger** | picker-colorcomponentdisplay M-1 (BLOCKER) |
| **N13** | **A keyboard coordinate on the dominant instrument.** The spectrum plate is focusable and drivable to a named (s,v) — or to the selected space's own channels — with no pointer event | `role="img"`, no tabindex, no keydown, no aria-live in the whole file; VC:125 + SS5.2's two named numeric axes exist nowhere | Fails on focus without operation and on operation without an announced value. **The gate must be RE-AUTHORED, not merely added: the shipped a11y gate CERTIFIES the current shape (C-7)** | picker-spectrumcanvas D-04/C-6 (BLOCKER) |
| **N14** | **WHCM focus-register conformance.** Under `forced-colors: active`, `:focus-visible` paints on all five demo sites | `outline: none; box-shadow: var(--focus-ring-shadow)` at (0,2,0) over a deliberately `:where()`-specificity-0 register; probe-06 raw: `{focusVisible:true, boxShadow:"none", outline:"3px none"}`; `focus-ring.css:31` states the law it breaks | Fails per site. **Passes only by curing the five sites or raising the shared block's specificity — NEVER by another line in the register.** o27's forced-colors arm reaches only the gradient rail today → X-W1 oracle extension | MISS-3 ≡ M-2 ≡ D2-02 |
| **N15** | **The loupe paints, and one tap is one sample.** First show paints; a cancelled pointer commits nothing; a pan-while-pinned does not eat the next tap | EY-1's synchronous `drawLoupe` against a `v-if`-hidden canvas (every unpin re-arms it for the session); EY-9's `pointercancel` wired to `onPointerUp` reaching the `!didMove → onTap` branch; EY-2's one-exit `justUnpinned` latch | Fails on a painted-but-occluded loupe too (EY-5: centred exactly on the pointer). The magnification arm (EY-6, sub-1× at high zoom) is a separate cell | EY-CLUSTER (W4·121) |
| **N16** | **The generated palette keeps its name.** A save from the Generate pane persists the typed name | Handler bound at arity 1 hardcoding `pm.createPalette("Generated Palette", …)`; the sink derives `slug` and the dedup key's name half from the constant | Fails while the emit's second argument is dropped. **TypeScript's fewer-arity assignability makes this gate-invisible to `vue-tsc` — only a behavioural spec can see it** | GEN-1 (BLOCKER) · GEN-CLUSTER |

---

## 3. BoundsDelta — files this wave must ADD per the records

`W4.md` §4 (open partition, 32 rows) and §4's trigger-gated table (13 rows) are **byte-untouched** (E-3). The rows below are the corrections the corpus requires; each names the record that requires it.
**31 distinct paths** — §3a: 25 rows / **30** paths (rows 15-17 and 23-26 carry three and four files respectively);
§3b: 2 rows, **1** net-new (`ExtractControls.vue` is the §0a n.3 identity and appears in both tables by design).
**24** of the 31 are in no `W4.md` table at all; the other **7** (`SpectrumCanvas.vue`, `CurrentPaletteEditor.vue`,
`SwatchHoverMenu.vue`, `MixSourceSelector.vue`, `MixResultDisplay.vue`, `ImageEyedropper.vue`,
`ColorSpaceSelector.vue`) exist only inside the trigger-gated `.g` table, whose carve ("import + face only" /
impostor seat) cannot host the cures the records declare.

### 3a. Open partition (.a-.d) — additions required for a declared cure to be landable

| file | required by | access | why the current table is insufficient |
|---|---|---|---|
| `demo/shell/dock/ActionButton.vue` | **actionbutton ABN-1, twice-stated registry correction** | modify | "**X.W4.d's file bounds must name `ActionButton.vue`** before its collapse can be real" — D1's single-render-path collapse cannot land without rewriting or deleting it. It appears in **no X wave's bounds** |
| `demo/shell/dock/ActionBarToggle.vue` | actionbartoggle ABT-5 (NEW), ABT-10 (X-W4.a), ABT-12 (NEW), ABT-17 D-14 | modify | Three rows explicitly routed X-W4.a/.d; the file is in no W4 table. AB-3's containment predicate also lives here |
| `demo/shell/dock/menus/ProfileSection.vue` | profilesection D-2, D-7/C-4, D-8, C-3/D-9, D-16, N-1, N-2, N-4, A-2, A-6 | modify | Ten X-W4 dispositions incl. **"X-W4.a owns this surface by name"** (D-16) and the wave's goal criterion quoted verbatim (D-8) |
| `demo/shell/dock/menus/MobileMenuDropdown.vue` | profilesection ("both twins" on D-2/D-7/N-1/N-2) · mobilemenudropdown C-4, D-10 | modify | The twins share 78.7% of their bytes; **every defect except C-3 exists twice**. mobilemenudropdown books C-4/D-10 NO-WAVE-OWNER **naming X-W4 as the class owner that does not list the file** |
| `demo/picker/controls/SpectrumCanvas/SpectrumCanvas.vue` | picker-spectrumcanvas: **27 rows + M-16 as cure-law** → X-W4 | modify | Present ONLY in the g table as "import + face only". Two BLOCKER-weight rows (D-04/C-6) and the whole control-rebuild cluster are unlandable inside that carve |
| `demo/picker/display/ColorComponentDisplay/ColorComponentDisplay.vue` | picker-colorcomponentdisplay: **16 rows** → X-W4, incl. BLOCKER **M-1** | modify | In **no** X wave's bounds. M-1 is glass-independent and "must not wait on any Glass-8 trigger" |
| `demo/palettes/browser/card/PaletteCard/PaletteRenameInput.vue` | PaletteRenameInput A-1, A-2, A-6, A-7, A-9, A-10, A-12, A-13, A-14, A-15, A-16, A-17, A-21 — **13 terminal X-W4 rows, zero NO-WAVE-OWNER** | modify | The record states "every MAJOR routes to an owned wave (X-W4 · X-W5 · X-W7 · X-W10)" — but W4's table does not hold the file |
| `demo/palettes/browser/card/PaletteCard/PaletteCard.vue` | PalettesPane PP-16 (`PaletteCard.vue:98`) · PaletteRenameInput A-6/A-13/A-15 (the `renaming` writes and the dead menu-close branch live here) | modify-carve | PP-16 books this exact line at X-W4 |
| `demo/palettes/browser/card/CurrentPaletteEditor.vue` | CurrentPaletteEditor C-4/L-5, C-6, C-7, L-20, ADJ-M3, ADJ-M4, ADJ-m3 — seven terminal X-W4 rows | modify | Present ONLY in the g table (impostor seat `:98`). C-7 is explicitly "CC-041 kin"; C-6 is a canonical `aria-hidden-focus` over three real focusable buttons |
| `demo/palettes/browser/card/SwatchHoverMenu.vue` | CurrentPaletteEditor C-6 (the teleported panel carries `aria-hidden="true"` over `<slot name="actions"/>` filled with three focusable labelled buttons; `focusableInside: 3`) | modify-carve | Present ONLY in the g table (impostor seats `:17`,`:32`) |
| `demo/palettes/browser/admin/PaginationBar.vue` | PaginationBar C-5/D-4, M4, M5, M6 | modify | Four X-W4 rows and the house grammar AAP-21 cites as "next door" |
| `demo/palettes/browser/admin/AdminListSkeleton.vue` | AdminNamesPanel D-11/C-9 cure lock: **"one atom edit (AdminListSkeleton drops per-instance `role="status"`) + wrapper roles, not a per-file patch"** | modify-carve | The five panels are in bounds; **the atom the cure edits is not** |
| `demo/shell/dock/ParseEchoReadout.vue` | parseechoreadout A-3, A-5, A-6, A-9, A-13, A-14, A-15 (the "leaf rewire" arm, all → X-W4) | modify | Seven X-W4 rows on a file in no W4 table |
| `demo/shell/dock/ColorInput.vue` | parseechoreadout A-2 (identity D-15 → X-W4: the readout's sole mount) · TagEditPopover TEP-13 ("shell sites: ActionButton, ColorInput") · ABT-8's third ink violator | modify-carve | The A-2 placement decision (persistent region vs popover) is X-W4's and cannot be made without the host |
| `demo/palettes/useSlugMigration.ts` · `demo/palettes/usePalettePorts.ts` · `demo/shell/useViewManager.ts` | SLUG-CHAIN (W4·122) — the cure lock names all three by line | modify-carve | `SlugEditLayer.vue` is in bounds; **the three files the one-line cure and its typed-deps hardening actually edit are not, in any X wave** |
| `demo/palettes/browser/dialog/MigratePalettesDialog.vue` | MigratePalettesDialog A-1 (identity), A-14, A-19 — all three say "land beside X-W4's SlugEditLayer edits so the file is touched once" | modify | — |
| `demo/workbenches/extract/ImageEyedropper/ImageEyedropper.vue` | EY-CLUSTER (W4·121) · XP-9 ("**exceeds the declared carve — addendum candidate**") · XW-21 · XW-36 | **widen** `W4.md:166` from "modify-carve — import + face only" to modify | The modality contract, the loupe, the tap model and the transition all sit outside the face carve; the record's headline is that **no X wave owns a single line of the code that must change** |
| `demo/workbenches/extract/ImageDropZone.vue` | EY-4 ≡ EC-11 ≡ XW-3 ≡ R-2 — the sole opener, and the keyboard-inertness lives here | modify | "no cure inside `ImageEyedropper.vue` can reach it"; the file is in **no** X wave's bounds |
| `demo/workbenches/extract/ExtractWorkbench.vue` | XW-3 (`:22-25` binds `preview` and `disable-click` to the same value) · XW-13's chip register | modify-carve (**shared with X-W7**) | X-W7 holds it as modify-carve; XW-3's cure is X-W4's and exceeds W7's carve. **Declare the shared hold or sequence the waves** |
| `demo/workbenches/extract/ExtractControls.vue` | EC-11, EC-16, EC-38, EC-44 → X-W4 · EC-2/EC-3/EC-22 → X-W4.g via CC-105 | modify (open) **and** add to the **g** table | §0a n.3: **CC-105 names `ExtractControls.vue:32,75` and X-W4.g as its receiving surface while W4.g's own table omits the file.** "the densest defect surface in this corpus is banked behind a release event whose receiving wave does not name the file" |
| `demo/workbenches/generate/GeneratePane.vue` · `demo/workbenches/generate/composables/useColorGeneration.ts` · `demo/color-session/generate-color.ts` · `e2e/smoke/oracles/o20-generate-plate.spec.ts` | GEN-CLUSTER (W4·120) | modify / modify / modify-carve / modify | Verified absent from **all 206 File-Bounds rows** across W0-W11; W6's routed-scene list omits Generate entirely |
| `demo/workbenches/mix/MixSourceSelector.vue` | MSS-5, MSS-10 → **X-W4** (open partition, not g) | modify-carve (open) | Present only in the **g** table for the impostor seats `:168`/`:215`; MSS-5's ring collision and MSS-10's SegmentedTabs affordances are glass-independent |
| `demo/workbenches/mix/MixResultDisplay.vue` | MX-32 ≡ MR-10 ("the g carve — import+face only — is insufficient alone") · MR-19 | **widen** the g carve, or add open-partition modify-carve | The palette arm's total absence of a text alternative cannot be cured by an import swap |
| `demo/workbenches/gradient/GradientVisualizer/GradientCodeEditor.vue` | wb-gradient-codeeditor A-9 → **X-W4 + glass-ui BH relay** | modify-carve | "semantic-controls remainder: adopt the producer field contract". In no X-W4 table; the record notes the file is X-W6-owned for its other rows — **declare the shared hold** |
| `demo/color-session/ColorSpaceSelector.vue` | MISS-3's five-site `outline: none` roster · AB-15/CC-113's `hide-indicator` | modify-carve (open, for the focus arm) | Present only in the **g** table ("import + face only"); the WHCM focus defeat is glass-independent and must not wait on the census |

### 3b. Trigger-gated table (.g) — additions

| file | required by | access |
|---|---|---|
| `demo/workbenches/extract/ExtractControls.vue` | CC-105's own two sites (`:32`, `:75`) — the receiving-surface inconsistency of §0a n.3 | modify-carve — the two `--slider-track-bg` sites |
| `demo/palettes/browser/search/MiniColorPicker.vue` | picker-spectrumcanvas **L-1 + M-2** — the unification target ("unify ON the mini's containment; do NOT delete the mini's plate") | read-only reference at g; the unification itself is X-W8 feeding X-W4 |

### 3c. Bounds rows the corpus explicitly does NOT ask for (recorded so they are not added by drift)

- `demo/scenes/about/AboutPane.vue` — AboutPane's only W4 row is AB-15, which lands on `ColorSpaceSelector.vue`, not the pane.
- `demo/scenes/about/katex/Katex.vue` — R2's cure is a **vendored-stylesheet rule**, not an SFC edit; R16 is an `index.html`/root `dir` decision on the U-F58 register.
- `demo/scenes/aurora/AuroraPane.vue` — **explicitly** absent from W4.b's bounds by that record's own reading; the pane's edit rides X.W6.j (§0a n.9).
- `demo/palettes/browser/admin/AdminListItem.vue` — D-5 is NO-WAVE-OWNER; the record verified the file "sits in no tranche-X File Bounds (verified across W0-W11)" and did **not** ask W4 to take it.
- `demo/palettes/browser/slug/PaletteSlugBar.vue` — the orphan; its deletion is X-W8.a's census addendum, not W4's.
- `demo/styles/foundation.css` — MISS-3/M-2's cure is explicitly **at the five consumer sites, never another line in the register**; foundation.css is X-W10 unit f.

---

## 4. CrossEdges

| # | edge | shape | binding clause |
|---|---|---|---|
| CE-1 | **X-W4.d → X-W5.a** | precondition + division of labour | W4·53: D4 makes the **silence** unrepresentable; **A3 (`bindPane`) owns the parity cure and its measurement**. Closing D4 by wiring the mount here takes X-W5's work and the bounds check catches it |
| CE-2 | **X-W4.d ⇄ X-W8.d** | one file, two transpositions | AB-3 splits: containment predicate → W4.d (Dock.vue in bounds); the crossfade-`faces` gestalt → X-W8.d G-8/G-10 (≡ CC-080 ≡ Dock-adj L-20/MT-DOCK-LAYERS-1). AB-20's LINE → W8's G-10 addendum, its CONTRACT FIELD → W4.d. **Sequence the touches; neither gate may go green over the other's edit** |
| CE-3 | **X-W4.a ⇄ X-W8** (null-DELTA collision) | **hard** | W4·11: X-W8's same-touch edits the same attribute lists. **The `iconOnly` arm carries an INTENDED geometry delta and MAY NOT ride under W8's null-DELTA gate** |
| CE-4 | **X-W4 ⇄ X-W7.d** (VersionHistoryDrawer) | interim-hold / recomposition | W4 holds the SFC (`W4.md:127`); X.W7.d recomposes the drawer into the selected-entity inspector. Three named carve gaps (VHD-1, VHD-2, VHD-17). **"Whichever wave lands second inherits the seam — recorded so neither wave's gate goes green over the other's edit"** |
| CE-5 | **X-W4 ⇄ X-W7** (AdminTagsPanel seat) | whichever-lands-first | ATP-25: "local geometry ≥24×24 **or** the W7 seat rework, whichever lands first — **cross-declared from both ends per the W4-D2 lesson**". ATP-2's 24px arm and ATP-37's semantics arm coordinate the same way |
| CE-6 | **X-W4.b → X.W6.j** | mechanism-of-record, not a booking | §0a n.9: AP-12/AP-14 cite X-W4.b's LabeledField + `size` mechanism **verbatim** and land the pane's own edit at X.W6.j because AuroraPane is absent from W4.b's bounds. GV-19 is the gradient twin (X-W6.b) |
| CE-7 | **X-W4.g ⇄ X-W0.j** | trigger | All of §1g is CLOSED unless the four-condition census PASSes. On PASS the orchestrator lands g on clean main with **no other X wave open** |
| CE-8 | **CC-105 ⇄ CSP G-PAINT ⇄ X-W0 sitting** | **unreconciled** | W4·117: two live registers claim the `--slider-track-bg` sites. The reconciliation is flagged to the X-W0 sitting; **this fold does not elect either** |
| CE-9 | **X-W4 ⇄ X-W1** (oracle extensions) | gate-truth | o27's forced-colors arm reaches only the gradient rail (N14); the plate/luma unit oracles (C-5/M-12's degenerate `>` threshold at s=1,v=1); the C-7 gate that **certifies** D-04's shape and must be re-authored (N13); the o18 generate enrollment (GEN-8); CI e2e gating (CC-031) without which N1-N16 exist but are unenforced |
| CE-10 | **X-W4 ⇄ X-W10** | canon riders, no build overlap | Ink/rung (ABT-8's `useSafeAccentFn("floating")`, three violators), focus canon (ABN-5/ABT-11), hover grammar (AB-31), proportion (`gap-0`/ABN-19), voice (ABN-28 with o10d), the DESIGN.md anchor drift (ABN-14c), the closed type matrix (DVS-7/CC-098), VHD-17's role matrix |
| CE-11 | **X-W4 ⇄ X-W10 (escalated)** | **prohibition** | W4·103/K-8: the per-verb seat table is DEMOTED from ruling to input. **"No seat may execute challenge-D's command-seat table for Generate while R-B's banked ruling stands un-overturned"** |
| CE-12 | **X-W4 → glass-ui BH relay** (standing fond) | one letter per identity | Consolidated asks: the `compact` control-size defaults (**with K-2's strike**); the unlayered dialog placement + the dialog `size` axis + logical properties (**with K-1/K-2 pinned**); `DockControl` `label`/`ariaLabel` (XP-8's 11/8/0 fork); the menu-glyph `> svg` rule (N-2); Slider `aria-valuetext` prop-through (EC-7, already seated); the once-evaluated coarse `matchMedia` probe (ABN-3); `useTouchGate()` returning its class binding (D-13); the gate API accepting per-event modality (M-22) and `suppressDeactivate` (M-23); a public content-swap composable (paneslot L-8); `DropdownMenuItem` PrimitiveProps / `DropdownMenuLinkItem` (D-2); WatercolorDot's seed determinism (GEN-28). **`Input` migration and `LabeledField` need NO relay — both are published at installed 7.0.0** |
| CE-13 | **X-W4 → the visual-audit lane** | residues, not gates | ABT-10's 33.3 vs A2's 23.3 discrepancy (§0a n.6); AAP-19's coarse cell; the U-1..U-16 cells named by actionbutton / actionbarlayer / actionbartoggle; EY's live-only magnitudes; MSS-20's 2.88:1 |
| CE-14 | **X-W4 ⇄ the formation boundary** | four clusters | GEN-CLUSTER, EY-CLUSTER, SLUG-CHAIN, PICKER-REBUILD (W4·120-123). **Each is a hole INSIDE owned territory.** If X-W4 declines any of them, the decline must be recorded with the same specificity as an adoption — the records' shared headline is that the defect mass lives in files no wave's declared scope owns |

---

## 5. Dissents, escalations and exclusions — preserved, not ruled away

1. **EY-7's severity.** This fold carries the arbiter's MAJOR; **r3's BLOCKER dissent is preserved verbatim in the record and is not overwritten here.**
2. **CC-105 vs CSP G-PAINT (W4·117).** Two live registers, one identity, opposite verdicts on whether the paint is demo-curable today. **Escalated to the X-W0 sitting. This fold elects neither.**
3. **GAB-5 / K-8 (W4·103).** The per-verb seat question (dock set vs route-local) is escalated to X-W10 under the VC:194 + B-13 + R-B collision, with a standing prohibition on executing challenge-D's table.
4. **ATP-18 (W4·118).** Two waves carve `EmptyState.vue` and either may adopt the length budget. Named candidate, not an election.
5. **SP-13/SP-24/SP-27 (W4·119).** NO-WAVE-OWNER as rows; `utils.css` is the named candidate and SP-27 is a **correction duty binding on any wave touching the file**.
6. **ABN-9 / N11.** If X-W4.d declines the failed-action extension of D4, the row is a **named formation-boundary residue** — the decline must be recorded, not implied.
7. **MP-3 / N10.** RED-on-arm, not RED today. A wave that lands D2's ref-binding without the guard converts an unreachable throw into a shipped silent failure.
8. **XP-24 / EY-13 / EY-37 / EY-38 / EY-41 / EY-47** and the extract-session correctness cluster (XP-2/3/4/23/27, XW-4/10/19/22/34) and the **camera-mode cluster** (XW-8/9/35) ride the EY/extract formation-boundary booking and are **NOT folded as X-W4 rows** — recorded here so their absence is a result.
9. **Excluded from this fold, with reason.** (a) `FlagReportDialog.md` — its `X-W4` token is a head-matter routing read; it books no W4 row. (b) `BrowsePane.md` **B4** — routed X-W7.c/d; it cites `W4.md:308,329`'s **seat law as the pattern authority** only. (c) AboutPane's non-AB-15 rows, AdminListItem D-5, PaletteSlugBar A-1/A-2 (orphan-half), MiniColorPicker's rebuild rows, `wb-gradient-visualizer`'s 47 X-W6 rows — all routed elsewhere by their own records and carried here only as cross-edges. (d) `AdminUsersPanel.vue` is in W4's bounds but its record is outside this seat's assigned slug set — **no row is minted for it here, and its absence is a scope statement, not an adjudication.**

---

## 6. ROUND-1 FOLD REPAIR — dated addendum, `2026-08-28`

**What this section is.** The X-W4 repair seat's consumption of `ROUND-1-ASSIGNMENTS.md` (the spine's
ROW-CARRIAGE LAW §1: *a packet's home wave adopts the packet's RIDING ROWS id-for-id, not merely its
files*) **plus this seat's own freshly re-run id-keyed census**, which reaches rows the assignment's
roster does not carry. Status **planned**; dispositions **ADJUDICATED, never stronger**; **no product
source opened** — every witness below is a read or a grep. E-3 holds: `waves/W4.md` and
`CONFORMANCE-2026-08-03.md` stay **byte-untouched**; this file remains the fold's sole write.

**Anti-rename, restated for this section.** Every id below is the id its adjudicating record minted —
**original for life**. `W4·n` indices continue §1's fold-local counting device and are never a rename.
Citation key is the fold citation law **⟨record.md · id⟩**: bare ids collide across six namespaces, and
this section's whole method turns on that fact (§6.0 n.2).

### 6.0. Method and the re-derived arithmetic — falsifiable, re-runnable

**Corpus.** The 92 non-`kf-*`/`fr-*`/`pt-*` `*.md` at `docs/tranches/V/megatranche/registry/adjudicated/`
(non-recursive; `ls *.md | grep -vE '^(kf-|fr-|pt-)' | wc -l` → **92**, reproduced by this seat today).
**Fold layer.** The twelve `refinement/X-W*-FOLD.md` concatenated (the spine file
`REFINEMENT-FOLD-2026-08-28.md` is not on disk at this seat's read — stated, not assumed).
**Row.** A bold-leading id line (`- **ID` / `| **ID`). **X-W4-routed.** The row line carries an
`X[-·.]W4` token (hyphen, en-dash, middot and dotted forms all counted) **and** a routing verb
(`Disposition` · `→ X-W` · `→ X.W` · `BOOK →` · `BUILD →` · `FOLD →` · `routes to` · `Terminal: X-W` ·
a `| **X-W` cell). **NWO.** `NO-WAVE-OWNER` on the row line or inside the row's continuation block.

**This seat's census, re-run fresh (2026-08-28).**

| measure | count | note |
|---|---|---|
| X-W4-routed ⟨record·id⟩ rows in the whole corpus | **282** | the true id-keyed X-W4 surface |
| …already carried in §1 of this fold by bytes | **247** | 87.6% — §1 was substantially right |
| …**absent from this fold** by bytes | **35** | folded below, §6B |
| of those 35: absent from the **entire fold layer** under **⟨record·id⟩** (record-keyed) | **20** | §6B.1 — true census escapes |
| of those 35: byte-present in another wave's fold **with their own record's provenance** | **15** | §6B.2 — `≡`-pointer rows, never a silent skip |
| NWO escapes corpus-wide (absent from the whole layer) | **82** | this seat's sweep; the assignment's roster carries **73** |
| …of which home to **X-W4** | **14** | §6A — the roster's 13 **+ 1 supplement** |
| I-28 EXTERNAL rows owned by X-W4 | **1** | §6C — ⟨INBOX I-28 · X-EXT-4⟩ |
| **rows added by this section** | **50** | 14 + 35 + 1 |

**Corrections recorded, never silent.**

1. **The assignment roster is a floor, and this seat's sweep exceeds it in both registers.** §B assigns
   X-W4 **four** census rows (⟨consolerail · D2-08⟩, ⟨consolerail · MISS-9⟩, ⟨spectrumcanvas · M-24⟩,
   ⟨wb-mix-pane · DC-3⟩ˢ). The re-run finds **35** — the four plus **31** the roster does not carry,
   sixteen of them in the two picker records this fold already named as an unowned hole
   (`W4·123 PICKER-REBUILD`). §A assigns **13** NWO rows; the sweep finds **14** (⟨genericactionbar ·
   GAB-10⟩ added). Every supplement is an escape by the roster's own byte test and is folded here so
   no round-2 check re-discovers it.
2. **The check's byte test and the fold's own citation law disagree, and this seat obeys the law.**
   `ESCAPED = the id boundary-matches nowhere in the fold layer` is a **bare-id** test; the fold citation
   law is **⟨record.md · id⟩**. Fifteen of the 35 satisfy the bare-id test somewhere in the layer only
   because a *different* record's namespace happens to spell the same bytes (`D-01`, `M-13`, `C-12`,
   `F-2`, `MISS-2` …). This seat re-tested each hit for its own record's provenance within ±1200 bytes
   of the match and split the 35 accordingly. **The stricter test finds 20 escapes where the bare-id
   test finds 8** — recorded because the round-2 check will measure with the looser instrument and must
   not read the 12-row difference as an over-fold.
3. **The extract cone's NWO escapes are NOT taken here.** The sweep finds 4 NWO escapes in
   `wb-extract-pane.md` and 4 in `wb-extract-workbench.md` beyond the assignment's roster. Packet 4
   (XP-EXTRACT) homes the extract cone to **X-W7**; this fold's `W4·121 EY-CLUSTER` *proposes* adoption
   of the eyedropper limb but has not been granted it. **They are stated here and booked nowhere by this
   seat** — their absence from §6A is a routing result, not an omission (§6E n.3).
4. **`⟨wb-extract-workbench · XW-38⟩` is folded even though it is an INFO note, not a defect.** Its own
   text says "a variant-semantics observation, not a defect … note for the X-W4/W7 face work" — it is a
   routed row by the census test and therefore rides, with its non-defect status preserved verbatim.

### 6A. NWO rows adopted id-for-life — the ROW-CARRIAGE LAW applied

Packet 2 · GEN-CLUSTER and packet 9 · the mobile-command chain home to X-W4; their riding rows are
adopted here id-for-id. **Adoption of a row is not adoption of a cure**: where a row's cure surface sits
outside X-W4's bounds, the split is stated on the row.

| # | id | source | sev | mechanism (as adjudicated) | carriage |
|---|---|---|---|---|---|
| W4·124 | **GEN-7** (D-9 upgraded + return-1's missed process shape) | wb-generate-pane :51 | MAJOR | The seed line, count label and both icon glyphs wear raw `text-muted-foreground` — the ink this repo has **twice measured failing** and remediated everywhere around this pane (`:212/:288/:173/:183` vs ExtractControls' `.plate-ink` at 2.82–3.08:1 and PaneHeader's 4.29:1). Direction structurally certain; the exact 3.19:1 is R-2 | Rides **GEN-CLUSTER (W4·120)** into X-W4; the census-identity rider joins GEN-9's o18 row. The record's `NO-WAVE-OWNER` stands as filed and is **discharged by the cluster's adoption**, not struck |
| W4·125 | **GEN-10** | wb-generate-pane :54 | MAJOR | The seed is a **write-only provenance token**: sole writers `Math.random` (`useColorGeneration.ts:24/:31`); no seed input, route/query param or persistence anywhere; the `select-all` invites a copy nothing can consume; a reload destroys the palette the token names. All three axes cited the seed; **none checked that it reproduces anything** | GEN-CLUSTER. Cure direction (a seed re-entry path **or** dropping the provenance costume) is a **product decision** — carried as such, not elected |
| W4·126 | **GEN-11** | wb-generate-pane :55 | MAJOR | Under **five of the six** harmonies the count slider re-rolls every swatch instead of extending, and the ramp in its own track churns under the thumb; `golden` — the default — appends cleanly (PIN 5's rng-consumption arithmetic) | GEN-CLUSTER (`generate-color.ts` + `useColorGeneration.ts` are bounds-unowned — the cluster's whole point) |
| W4·127 | **GEN-14** (D-5) | wb-generate-pane :61 | MINOR | Two facts rendered four times (palette as strip **and** dots; count as Badge **and** label ~290px apart), **both palette renderings AT-hidden** — purely visual redundancy | Subordinate to GEN-4's composition row; rides GEN-4 at the formation boundary, **not** a W4 build row |
| W4·128 | **GEN-18** (D-14 ≡ C-12 ≡ L-2) | wb-generate-pane :65 | MINOR | A non-null-asserted injection nothing reads (`GeneratePane.vue:10`; grep = 1 = the declaration), twin at `GradientPane.vue:8`; **invisible to gates** (`noUnusedLocals` in no tsconfig, `no-unused-vars: "off"`) | GEN-CLUSTER; the gate-invisibility limb is a standing input to X-W1's lint slate |
| W4·129 | **GEN-31** | wb-generate-pane :78 | MINOR | `grid-cols-2` has **no narrow arm** in a component with exactly two responsive declarations (`:219`; `:205`); ~149px cells must seat "Split complementary" at 320-390 | GEN-CLUSTER. Truncation magnitude is a visual-audit residue (CE-13) |
| W4·130 | **GEN-35** (D-21) | wb-generate-pane :85 | MINOR | The documented 390px graceful wrap **is the only state the plate chrome renders on desktop** (structural row-minimum ≈437px vs ≈440px box); the comment `:139-142` describes a layout the code does not produce | GEN-CLUSTER; rect numbers are R-x residues |
| W4·131 | **GEN-37** (D-23) | wb-generate-pane :87 | MINOR | The route was **never captured in six of the ten** a11y/state matrices — four safari shots only; forced-colors / keyboard / reduced-motion / rtl / zoom frames absent, so **three D findings have no tracked frame** | GEN-CLUSTER + **CE-13** (the visual-audit lane owns the capture gap; kin to ABN-24, W4·134) |
| W4·132 | **GEN-40** (D-18 + return-2's merged clampHue/domain rows) | wb-generate-pane :90 | MINOR | Dead declarations and false comments — inert `relative`/`mx-auto` on the pane root (copied from a sibling, **not even self-consistent across the copies**); `clampHueToRange` and its kin | GEN-CLUSTER |
| W4·133 | **GEN-41** | wb-generate-pane :91 | MINOR | The route description **praises output instead of naming mechanism** ("pleasing … aesthetic") — and it is **the first line AT hears**, since everything below it is hidden by GEN-2 | GEN-CLUSTER; voice canon → X-W10 (CE-10), the AT-primacy limb is the sharper half and rides the cluster |
| W4·134 | **ABN-24** (D-0 + C-11) | shell-dock-actionbutton :55 | INFO | **The tracked visual matrix contains zero pixels of this component** — three axes concur, `REPORT.json` re-read by r-LC; all 60 captures show the dock's summary face. Harness carry: `capture.mjs` never opens the action-bar layer | **CE-13 (visual-audit lane), not a build row.** Recorded at X-W4 because X.W4.d is the wave that changes what the matrix would capture — a re-shot matrix after the collapse is the only honest baseline |
| W4·135 | **AT-19** (R1) | shell-dock-actiontoolbar :56 | INFO | `focus-visible:ring-ring/40` reaches a token the repo **documents as empty** — zero `--ring`/`--color-ring` definitions in the served cascade; `focus-ring.css:10-13` records the emptiness in prose | **≡ ABN-5 (W4·65)** by mechanism — folded here as its own banked identity, counted once as a defect, twice as a row. The literal deletes with the re-seat; canon half → X-W10 |
| W4·136 | **ABT-13** (L-6 as RESCOPED, R-A) | shell-dock-actionbartoggle :67 | RESCOPED → INFO | Surviving facts: **3 dead-path targets, 2 omissions, the false "8-key" comment**. The binding mechanism is **REFUTED (R-A)**; no live break; severity follows Dock-adj L-22 | Rides X.W4.d's contract collapse as documentation truth (the false comment dies with the symbols it names). **The refutation is carried with the row — no seat may re-inflate it to MAJOR without overturning R-A** |
| W4·137 | **GAB-10** ˢ (FINAL-correction-2 + r-LC missed find, merged) | shell-dock-genericactionbar :25 | MAJOR | **A Mix selection can never be emptied by any reachable control.** `MIN_COLORS = 1` / `canRemoveColor` / `:disabled` (`MixSourceSelector.vue:37/:39/:154`) floor local removal at one; the only zero-path is `clearSelection`, **never operative per GAB-4**. A live product capability has no working seat. The C/D axes' contrary premise is KILLED (K-3) | **SUPPLEMENT — beyond the assignment roster.** Verbatim lock preserved: "either the `SceneActionSet` registers a working Clear **or** local removal reaches zero — **one honest seat, not both, not neither**." **Split, stated**: the `SceneActionSet`-registers-Clear half rides **X-W4.d** (the identity already arrives via DC-3, W4·155); the `MIN_COLORS`/MixSourceSelector half is **bounds-unowned and stays a named formation-boundary residue** — the record's own grep over `docs/tranches/X/` verified no wave names those symbols |

### 6B. Census-escape rows — the 35 X-W4-routed ⟨record·id⟩ absent from this fold

#### 6B.1. True escapes — absent from the entire fold layer under their own record namespace (20)

| # | ⟨record · id⟩ | sev | mechanism (as adjudicated) | carriage + locks |
|---|---|---|---|---|
| W4·138 | ⟨picker-spectrumcanvas · **D-01**⟩ | MAJOR (demoted from filed BLOCKER) | **The dominant instrument of a colour-space laboratory is a hard-coded sRGB-HSV field whose only reactive input is hue** — `selectedColorSpace` appears **nowhere** in `useSpectrumPlateStyle.ts:30-45`; every reachable pick is inside sRGB by construction (`SpectrumCanvas.vue:120-129`). The headline byte-identity evidence is **killed (D-01b)**: the finding stands on **source, not measurement** | **PICKER-REBUILD (W4·123)** — the cluster's headline design row. INFO rider preserved (ex-D-18): the plate is **scheme-invariant by construction** — the bottom ~12.55% is near-black in light, dark and forced-colors alike, arithmetically necessary from the gradient (max channel = 255·v) |
| W4·139 | ⟨picker-spectrumcanvas · **D-02**⟩ | MAJOR (BLOCKER framing killed) | `.spectrum-dot { position: absolute }` (0,2,0) **loses the source-order tie** to the producer's `.watercolor-swatch[data-v-292b9032]{…position:relative}` (0,2,0) — so the dot is an **in-flow flex item** and under `dir="rtl"` renders at **156.8%** of the plate's inline size. Tie direction proven two independent ways (arithmetic closure 1.568192 vs measured 1.5682; and structural order — `App.vue:164` imports the picker before `:200` imports foundation.css, and the Tailwind `absolute` utility sits in `@layer utilities` and loses to the unlayered producer sheet **regardless**) | PICKER-REBUILD. MAJOR not BLOCKER **because `index.html:11` hard-pins `dir="ltr"`** — no user can reach RTL today; the U-audit's grep-based RTL inventory **structurally could not see a TS-computed offset**. Production-bundle CSS order rides UNPROVEN. Kin: W4·49 / U-F58 |
| W4·140 | ⟨picker-spectrumcanvas · **D-03**⟩ | MAJOR (causal sentence corrected per D-03-CAUSE) | **`pointercancel` commits the aborted gesture**: all three terminations funnel to `stopDragging()` (`:177-200`) and **no pre-gesture colour is stored anywhere in the file**, so a revert is structurally impossible | **LOAD-BEARING CORRECTION, carried verbatim in force**: the `pendingCoords` flush at `:191-194` is **NOT the cause** — every pointermove already committed through the rAF coalescer (`:97-108` → `:129`), so the flush holds at most sub-frame residue and **deleting it changes nothing**. "The cure is the pre-gesture **LATCH** … an implementer steered to the flush would ship a one-line fix that fixes nothing." Canon: SS5 "Tuning is continuous and interruptible". iOS edge-swipe mid-drag is the live path. Kin: ⟨spectrumcanvas · M-24⟩ (W4·144) — same handler family, different limb |
| W4·141 | ⟨picker-spectrumcanvas · **M-13**⟩ | MINOR | `spectrumIsSource` is set **before** the write that justifies it (`:125` vs `:129`); a throw strands the sentinel and **the next legitimate external clear is swallowed** | PICKER-REBUILD. Fix is independent of C-3's: **set after success**. Namespace warning: `M-13` also exists in ⟨picker-colorcomponentdisplay⟩ (W4·146) — **two identities, two rows, one spelling** |
| W4·142 | ⟨picker-spectrumcanvas · **M-18**⟩ | INFO | The SFC states its own channel model **correctly at `:2`** ("Direct saturation/value field") and **incorrectly at `:4-5`** ("2D saturation×lightness picker") — and `:4-5` is **the very comment that justifies the `role="img"` decision D-04 prosecutes**; `useSpectrumPlateStyle.ts:23` states it correctly two lines above the label rendering v as "lightness" | Rider on **D-12's cure**. **Binding shape**: "the prescribed rename of the announced channel leaves the WRONG comment standing as the recorded rationale for the a11y shape — **the cure must correct the rationale comment in the same edit or the error re-derives**" |
| W4·143 | ⟨picker-spectrumcanvas · **M-20**⟩ | MAJOR | `dotPos` **clamps out-of-domain HSV coordinates**, so the marker misreports the colour's position **on first paint with the shipped default**: boot colour → hsv v = `1.5109915`; `clamp(·,0,1)` at `:207-208` pins the marker to `top: 0%`, collapsing the whole out-of-sRGB family above v=1 onto **one pixel row with no indication**; the announced "100%" (`useSpectrumPlateStyle.ts:27`) is **a clamped fiction** | Rider on D-11 (the delta's mechanism) and D-12 (the aria fiction). **"The display-side twin of C-4's input-side degeneracy, and the mechanism UNDER D-11's measured [0,+48,+68] boot delta — three axes measured the symptom, none named the clamp."** Gate **N17** |
| W4·144 | ⟨picker-spectrumcanvas · **M-24**⟩ | MINOR | `handleSpectrumDown` **overwrites the capture bookkeeping without releasing the capture it discards** (`:148-150`: no `capturedPointerId !== null` check, no `releaseCapture()` first), so `releaseCapture()` and `onUnmounted` can only ever release the **LAST** pointer. A second finger rebinds both module locals and the first pointer's capture becomes untrackable | **C-1's third limb** — "C-1 audited move/up/cancel and stopped." Exactly the leak class `forceReleaseAllPointers()` exists for, **whose selector list begins with `.spectrum-picker`**. Gate **N22** |
| W4·145 | ⟨picker-colorcomponentdisplay · **M-3**⟩ | MAJOR | **Uncaught `PickerColorError` from a bare timer on a space change inside the 500ms debounce window.** `debounce` is a bare trailing-edge `setTimeout`, no try/catch (`demo/shared/utils.ts:28-34`); `updateColorComponent` never validates `component` against the CURRENT space; `withChannel` throws on unknown key (`picker-color.ts:171-172`). Sequence: type in rgb's `r` cell → switch space within 500ms (Cmd+K is bound, `ColorPicker.vue:263-266`) → timer fires with stale `"r"` → `findIndex` −1 → **throw, outside Vue's errorHandler, edit silently lost**. `.cancel()` is called in `onUnmounted` only — **never in the space watcher, the one place it matters** | PICKER-REBUILD. **Kin to N10 (MP-3)**: a throw that escapes the app's only ErrorBoundary. Gate **N19** |
| W4·146 | ⟨picker-colorcomponentdisplay · **M-13**⟩ | INFO | Render-path churn: `figParts()` invoked **three times per cell per render** (`:36,:37,:39` — toFixed + parseFloat + regex + allocation each, no memo); the inline arrow in `v-for` defeats handler caching | Rider on **X-W4's rebuild** (PICKER-REBUILD). Distinct identity from W4·141 despite the shared spelling |
| W4·147 | ⟨picker-colorcomponentdisplay · **M-14**⟩ | INFO | **Three ungrouped natively-focusable tab stops** in the card header duplicating the sliders below — no `role="group"`/`fieldset`/`aria-roledescription`: traversal cost, **distinct from L-1's semantics** | Rider on X-W4's rebuild. Kin: W4·77 (ABN-21's group-semantics class) and W4·16 (the unnamed `role="group"` identity) — **three surfaces, one house lesson** |
| W4·148 | ⟨picker-componentsliders-consolerail · **D2-08**⟩ (+ C-6, one identity) | MAJOR | `role="tablist"`/`role="tab"`/`aria-selected` with **zero `aria-controls`, zero `tabpanel` anywhere in demo-authored markup** and **no panel consequence**: probe-10 measures 0 changed px in `.channel-rows`, no scroll (`scrollIntoView({block:"nearest"})` is a **definitional no-op** with all rows in view — `ComponentSliders.vue:210-214`), focus does not reach a slider. Framing tempered per reader B: selection **does** move the dot and set `activeComponent` — the rail's stated job — so the load-bearing defect is **the ARIA pattern promise**, decidable from source | **The honest pattern is toolbar/radiogroup, or real panel semantics.** Cure adjudicated separately from the defect, verbatim: "**if D2-01's seed lands first, a `role` change must not orphan the seeded-selection semantics — one wave, one decision.**" Cross-cited to ⟨consolerail · MISS-2⟩ (W4·159) so the two cures are decided together. Gate **N18** |
| W4·149 | ⟨picker-componentsliders-consolerail · **D2-10**⟩ | MINOR | CC-040's target-size class, **one identity with layout-gestalt G-4**. Arithmetic exact from bytes: `min-width: calc(var(--dock-touch-target, 2.75rem) * 0.72)` (`:326`) with the producer token verified 2.75rem → **31.68px** against the comment's "≥44px hits" (`:321-322`); measured **31.67×44** (probe-12 itemBox, raw). Doctrine split verified: `@media (max-width: 1023px)` (`:323`) vs the sibling's `@media (pointer: coarse)` whose comment (`:342-345`) **bans width-keyed gating by name** | **Clears SC 2.5.8 (24×24); fails its own comment** — so **gate A1 goes GREEN over it** and **N3's separate coarse cell is the only assertion that can bite**. Exactly W4·58 (AT-4)'s caution in a second file: recorded so the pattern is read as a class, not two accidents |
| W4·150 | ⟨picker-componentsliders-consolerail · **MISS-9**⟩ | MINOR | **The accessible names are raw internal channel keys, derived by TWO different formulas for the same datum.** `ConsoleRail.vue:32` `` `${component} channel` `` ("ct channel", "jz channel", "kelvin channel") vs `ComponentSliders.vue:66` `` `${component.toUpperCase()} channel` `` ("CT channel") — two names, one channel, **neither the human string sitting in `colorSpaceInfo.components[]` the rail already imports (`:99`)**. Because `aria-label` overrides content, **the human name is unreachable to AT on the rail** | Pre-booked in part from the slider side (DEFECT-LEDGER:38531, D-14) — **cross-cited, one identity**. "This also punctures the r2's §6 rebuttal of r1-claim-2, which cited `aria-label='L channel'` as adequacy evidence — **true only where the raw key happens to be the notation**." A4 predicate extension (S4 class (iv): a name that is a machine token) |
| W4·151 | ⟨picker-componentsliders-consolerail · **MISS-12**⟩ | MINOR (RESCOPED) | The `prefers-contrast: more` tab affordance (`foundation.css:747-749`) is **dead ON THE RAIL**: `border: 0` (`:247`) zeroes border-**style**, so the width bump can never paint on the only demo-authored `[role="tab"]` elements | Rides the **D2-02 / MISS-3 modality cluster** (W4·123's `cl.2` identity). Correction carried: the "sole consumer in the served DOM" half is **struck by that seat itself** — glass-ui's `DockLayerGroup` emits `role="tab"` at runtime |
| W4·152 | ⟨ShadowPalette · **SP-16**⟩ (reader-1 M3, from the K-10 kill) | MAJOR | **The Extract seat never announces completion; the tool's entire output lands in silence for AT.** Zero `aria-live`/`role="status"`/`sr-only`/`visually-hidden` in `demo/workbenches/extract/`. The chain is **each-part-correct, whole-part-broken**: idle ghost `aria-hidden` (correct), `PaletteCardSkeleton` `role="status"` (correct) but **UNMOUNTED at completion** by `<Transition mode="out-in">`, `PaletteCard` mounts `role="article"` — **no surviving region carries "5 colors extracted"**. The k slider also has `aria-label` with no `aria-valuetext` (`ExtractControls.vue:26-31`) | **Disposition verbatim**: "NO-WAVE-OWNER with named candidates — **X-W7 carves ExtractWorkbench.vue (for W7.g) and X-W4 owns the semantic-controls axis**; ExtractControls.vue is in no X file list (named gap)." This fold **claims the announcement-law half only** and leaves the seat carve to X-W7 — the named gap is re-stated at §3, not silently absorbed. Kin: W4·13 (the announcement machine, seven facets) — **SP-16 is its eighth**. Gate **N21** |
| W4·153 | ⟨wb-extract-workbench · **XW-38**⟩ | INFO (explicitly **not a defect**) | The C-23 kill remainder: `ExtractControls.vue:75` pins the kC track to a **SOLID ink** while requesting `variant="spectrum"`, whose whole contract is "the track carries the data" | Carried **with its non-defect status intact**: "a variant-semantics observation, not a defect — the solid pin is the documented, born-RED-certified **T-44a cure**. → note for the X-W4/W7 face work." Folded because it is a routed row by the census test, **not** because it asks for work. Feeds W4·116 (CC-105) and §3's `ExtractControls.vue` gap |
| W4·154 | ⟨wb-mix-animationcanvas · **chip-remove** (severity ruling)⟩ | MAJOR-latent, cross-attributed | The ruled-disagreements row for **MX-41**: verified `:152-158` + lucide defaults (no `role`/`aria-hidden` → the `<X>` svg contributes no name → **the button computes none**); 16px target; `opacity-0 group-hover` with no `focus-within` on the group — though the button's own `focus-visible:opacity-100` **does** reveal it to keyboard | **`≡`-POINTER ROW — the identity is MX-41, already folded at W4·113.** Booked here so the byte census closes on the ruling row's own spelling, **counted once as a defect**. Lock re-carried verbatim: "the row goes live the day **X.W4.g** revives the add seats, **which is exactly why the same wave must carry both**" |
| W4·155 | ⟨wb-mix-pane · **DC-3**⟩ ˢ (K-D8's corrected framing) | — (routing row) | → **GAB-4 + GAB-10 + GAB-17**: the dock action set **never operative in ANY layout**; **no reachable Clear**; `copyResult` is **dead code** → X.W4.d | **`≡`-POINTER ROW**: GAB-4 = W4·60, GAB-10 = W4·137, GAB-17 = W4·89. No second booking; the row exists so the wb-mix-pane namespace closes and so **GAB-10's arrival at X-W4 has a second, independent route** |
| W4·156 | ⟨wb-mix-pane · **F-9**⟩ (+ C-3/D-1(L)) | — (routing row) | → **GAB-2** (BLOCKER at home) + MC-20/MX-19 (**no `disabled` on dock rows**) → X.W4.d | **`≡`-POINTER ROW**: GAB-2 = W4·54. The MC-20/MX-19 limb (no `disabled` key can be spelled on a dock row) is **already the second limb of W4·53** — cross-cited, never re-booked |
| W4·157 | ⟨wb-mix-pane · **F-2**⟩ | — (routing row) | → **MR-13/GAB-17** (two clipboard paths; the shell dispatch dies, the plate seat is the sole owner) → X.W4.d | **`≡`-POINTER ROW**: GAB-17 ≡ MR-13 = W4·89 |

#### 6B.2. Cross-carriage `≡`-pointer rows — routed to X-W4, carried in another wave's fold (15)

These fifteen ⟨record·id⟩ are **routed to X-W4 by their own record's row line** and are **absent from this
fold** — but each is byte-present in the fold named below **with its own record's provenance**, i.e. the
identity is carried by the layer. Each is booked here as an explicit pointer so the X-W4 census closes
**without a second booking**, per the consumption rule ("where a roster id is a facet of an already-folded
identity, the cure is an explicit `≡`-pointer row, never a silent skip").

| # | ⟨record · id⟩ | routed | carried at | one-line mechanism (why the pointer is safe) |
|---|---|---|---|---|
| W4·158 | ⟨consolerail · **D2-15**⟩ | X-W4 | **X-W1-FOLD** | RESCOPED → INFO, one focus doctrine, rides D2-02's cure; reader B's correction adopted — `focus-ring.css:4-6` **scopes itself to its two consumers in the same sentence the r2 elides**. The operative fact is MISS-3's, already this fold's W4·123 `cl.2` identity |
| W4·159 | ⟨consolerail · **MISS-2**⟩ | X-W6 (cross-cited **at D2-08's X-W4 row**) | **X-W6-FOLD** | In the shipped boot state the rail exposes a tablist with **ZERO selected tabs** and `tabindex=0` on a tab whose `aria-selected` is false (`ConsoleRail.vue:30` has no fallback; `:187` has one). **WAI-ARIA requires exactly one selected tab.** Its own text orders the coupling: "cross-cited at D2-08's X-W4 row **so the two cures are decided together**" — W4·148 is that end of the pair |
| W4·160 | ⟨picker-spectrumcanvas · **D-07**⟩ | X-W4 | **X-W10-FOLD** | Per-frame `left`/`top` writes on the `feDisplacementMap`-filtered producer root (`useSpectrumPlateStyle.ts:60-61`) — the producer's d.ts documents that the silhouette is static and liveness rides a **compositor transform** precisely to avoid re-rasterizing the filter. The 20.6ms magnitude is UNPROVEN (contended dev host) |
| W4·161 | ⟨picker-spectrumcanvas · **D-08**⟩ | X-W4 | **X-W8-FOLD** | Four attempts to style the producer from outside; **one works** (`border`, because the producer declares none), **three vanish silently**. Folded limb: the template **also** declares Tailwind `absolute` (`:28`), dead via cascade layers — "the file states its positioning intent twice and neither statement reaches the DOM." **Owner edict 5's exact failure class** |
| W4·162 | ⟨picker-spectrumcanvas · **C-12** / L-9⟩ | X-W4 (producer half on the glass relay) | **X-W1-FOLD** | The gate deactivates mid-drag after 3s (`3e3` verified in dist bytes). Producer half rides **CE-12**'s standing letter |
| W4·163 | ⟨picker-spectrumcanvas · **C-14** / §0⟩ | X-W4 (rider — the rebuild renames) | **X-W8-FOLD** | `SpectrumCanvas` **contains no canvas**; three of the commissioning brief's four prime suspects are misnamed. A naming row, discharged by PICKER-REBUILD's rename |
| W4·164 | ⟨picker-spectrumcanvas · **M-15**⟩ | X-W4 | **X-W8-FOLD** | `event.currentTarget as HTMLElement` cast + a **module-scope raw DOM reference held for the drag's lifetime**, while a typed `spectrumRef` exists. Kin to W4·144's module-local capture bookkeeping — same three locals |
| W4·165 | ⟨shell-dock-actionbutton · **ABN-12**⟩ (L-7) | FOLD → the ColorSpaceSelector identity | **X-W8-FOLD** | The demo's structural import guard **matches zero files**; `demo/shell/` has no boundary enforcement at all. Folded to the repo-wide boundary identity, which X-W8 owns |
| W4·166 | ⟨shell-dock-actionbutton · **ABN-18**⟩ (D-10) | X-W10 (canon) | **X-W10-FOLD** | Untokened interaction timings, and the Reset seat's distinguishing hover rotation **snaps instead of sweeping**. Motion canon — X-W10's register, not a W4 build row |
| W4·167 | ⟨shell-dock-actionbutton · **ABN-20**⟩ (r-DU missed #2) | X-W10 (canon) | **X-W10-FOLD** | **One row, two hover tempos**: `ActionToolbar:9` gives the Reset seat alone `duration-normal`; its four siblings do not. Kin: W4·73 (ABN-15) and AB-31's three-hover-grammars row |
| W4·168 | ⟨shell-dock-genericactionbar · **GAB-9**⟩ (L-GA-3) | X-W8 | **X-W8-FOLD** | Retaining the branch retains **an extra consumer of the `demo/ui` forwarding layer** — the narrow ownership seam. **Discharged by D1's deletion** (W4·59): the collapse removes the consumer, so the X-W8 abrogation inherits one fewer edge. Sequencing rides **CE-2** |
| W4·169 | ⟨wb-mix-animationcanvas · **MX-2**⟩ (reader-LC M1, PIN 3) | X-W6 | **X-W6-FOLD** | On every `done → mixing` re-mix the anchor is **structurally absent at measure time** — independent of the attribute limb. Mix composition is X-W6.j's; carried here only because the row line names X.W4.g in its route |
| W4·170 | ⟨wb-mix-animationcanvas · **MX-3**⟩ (C-D3 ≡ L-1.3 ≡ both readers) | X-W1 (CI) | **X-W1-FOLD / X-W6-FOLD** | **Both e2e specs gating this component are structurally unsatisfiable, and NO CI job runs playwright at all.** This is the **latency premise under W4·113 and W4·154** — the colors add path is dead, so no chip can exist today. CI half is X-W1's (CE-9) |
| W4·171 | ⟨wb-mix-pane · **DD-5**⟩ | X-W6.j / X.W4.g | **X-W6-FOLD** | → **MX-41** (MAJOR-latent, nameless 16px hover-only remove) — the same identity as W4·113 and W4·154. **Three spellings, one defect, one booking** |
| W4·172 | ⟨wb-mix-resultdisplay · **MR-26**⟩ | MX-CLUSTER + X.W4.d | **X-W6-FOLD** | **Verb-scope inversion**: the plate "Reset" discards only the result; **the clearing verb exists only on the dock**. Rides the `SceneActionSet` verb contract (W4·97's class) and the MX-CLUSTER adoption at X-W6 — a **two-ended row**, stated at both ends |

### 6C. The I-28 EXTERNAL row — mail provenance, not a registry id

**Provenance: EXTERNAL, mail-sourced** — `docs/tranches/V/coordination/INBOX.md` row **I-28** (glass
v8.0.0 @ `17a11bc5`; letter + §5 addendum 08-25; ROWED 08-28). **Not a registry row**; the id is
`X-EXT-4`, cited as **⟨INBOX I-28 · X-EXT-4⟩**. It goes LIVE at the repin and is folded now so the
repin lands on **stated work, not surprise**.

| # | ⟨source · id⟩ | sev | break (witness re-measured in-tree by this seat, 2026-08-28) | carriage + locks |
|---|---|---|---|---|
| W4·173 | ⟨INBOX I-28 · **X-EXT-4**⟩ (I-21a arm, re-cited) | MAJOR, **born at the repin** | `--slider-track-bg` **typed-seam BREAK**. This seat re-ran the census today: `grep -rn -- "--slider-track-bg" demo/` → **8 hits = 5 live binding sites + 3 prose**. Sites: `GenerateControls.vue:305` · `ExtractControls.vue:32` · `ExtractControls.vue:75` · `ConfigSliderPane.vue:202` · `ComponentSliders.vue:197`. Installed producer re-probed the same day: `node -p "require('@mkbabb/glass-ui/package.json').version"` → **7.0.0** — **the repin has not happened, so the row is not RED today; it is born-RED the moment it does** | **Owning surface = X-W4.g**, per W0.4's cure shape (the census's PASS branch emits the receiving-bounds delta). **BOTH FIGURES STATED, NEITHER SILENTLY ELECTED**: the mail says **×4**, the tree measures **5** — the fifth is `ComponentSliders.vue:197`, which the mail's arm did not enumerate. Per-site cross-edges, stated: `ExtractControls` ×2 → **X-W7** · `ComponentSliders` → **X-W6** · `GenerateControls.vue:305` → the **PINNED-file executor (W25), not W6** · `ConfigSliderPane.vue:202` is **bounds-unowned → G-F rider**. **This row does NOT resolve CE-8** (W4·117's CC-105 ⇄ G-PAINT tension) — it **sharpens the input to the X-W0 sitting by one site and elects neither register** |

### 6D. New born-RED gate candidates — N17-N22

Six additions to §2b. **L-19 holds**: every gate names a real witness and is a Playwright spec or a
compiler/lint invocation — **never a `scripts/proof-*.mjs` (CC-019)**. Every witness below was
**re-measured in-tree by this seat on 2026-08-28**; the command is printed so the RED is falsifiable.

| # | gate | RED witness (real, today — command + result) | falsifier | source |
|---|---|---|---|---|
| **N17** | **The marker tells the truth about out-of-domain colour.** With the shipped default colour, the spectrum marker's position is a **function of the colour**, not of a clamp — or the out-of-domain state is **announced** | `demo/picker/controls/SpectrumCanvas/SpectrumCanvas.vue:207-208` reads, verbatim today: `s: rawS.value ?? clamp(channelNumber(HSVCurrentColor.value, "s"), 0, 1)` / `v: rawV.value ?? clamp(…, "v"), 0, 1)`. Boot colour → hsv v = **1.5109915** → marker pinned `top: 0%`; the announced "100%" (`useSpectrumPlateStyle.ts:27`) is the clamped fiction | Fails while **any** v>1 colour renders at the same pixel row as v=1. **Passes on either honest branch — domain expansion or an announced out-of-gamut state — but NOT on a wider clamp**: a clamp at 1.6 collapses a different family | ⟨spectrumcanvas · M-20⟩ (W4·143) |
| **N18** | **A tab pattern that has tabs, or no tab pattern.** Every `[role="tab"]` in demo-authored markup has an `aria-controls` resolving to a live `[role="tabpanel"]` — **or the rail exposes toolbar/radiogroup semantics instead** | `grep -rn 'role="tabpanel"' demo/` → **0** (re-run today). `grep -rn 'aria-controls' demo/` → **3**, none on the rail. `ConsoleRail.vue:13` `role="tablist"` + `:29` `role="tab"` ship regardless. Panel consequence measured at **0 changed px** in `.channel-rows`; `scrollIntoView({block:"nearest"})` is a **definitional no-op** with all rows in view | Fails on a role swap that orphans the seeded-selection semantics — **the cure is one decision with D2-01's seed, not two** (the record's own lock). Also fails on `aria-controls` pointing at a non-tabpanel | ⟨consolerail · D2-08⟩ (W4·148) · cross-cited ⟨consolerail · MISS-2⟩ (W4·159) |
| **N19** | **A debounced channel edit cannot outlive its space.** A spec types into a channel cell and switches colour space **inside** the debounce window; the edit either **lands correctly or is cancelled** — and **no unhandled error is raised** | `demo/picker/ColorPicker.vue:383-384` — `.cancel()` on **both** debounced handlers appears **only inside `onUnmounted`** (this seat's read of `:380-385`). The space watcher at `:349` cancels **nothing**. `demo/shared/utils.ts:22-34` is a bare trailing-edge `setTimeout` with **no try/catch**; `withChannel` throws on an unknown key | Fails while the throw escapes to the window (it is **outside Vue's `errorHandler`**, so a component-level boundary cannot see it). **Passes on cancellation in the space watcher OR on validating `component` against the current space — not on a swallowing try/catch**, which converts a lost edit into a silent lost edit | ⟨colorcomponentdisplay · M-3⟩ (W4·145) |
| **N20** | **The `--slider-track-bg` seam survives the repin.** After the glass-8.0.0 repin, **every** live `--slider-track-bg` binding site still paints its intended track — asserted **per site**, not once | Census re-run today: **5 live binding sites** (`GenerateControls.vue:305` · `ExtractControls.vue:32` · `:75` · `ConfigSliderPane.vue:202` · `ComponentSliders.vue:197`) + 3 prose. Installed producer today = **7.0.0**, so the gate is **RED-on-arm, not RED today** — and the arm is X-W0.j's repin, whose PASS branch this gate belongs to | Fails per site, so a single-file fix cannot pass. **Also fails if a cure deletes a per-instance override without landing the ramp through the producer's track input in the same move** (S8's load-bearing lock, W4·50). **The mail's ×4 and the tree's 5 are both recorded; a gate written to ×4 is under-scoped by construction** | ⟨INBOX I-28 · X-EXT-4⟩ (W4·173) |
| **N21** | **The Extract tool announces its own completion.** After an extraction commits, a live region carries the result — the count and the fact of completion — to AT | `grep -rn 'aria-live\|role="status"\|sr-only\|visually-hidden' demo/workbenches/extract/` → **0** (re-run today). The chain is each-part-correct, whole-part-broken: the skeleton's correct `role="status"` is **UNMOUNTED at completion** by `<Transition mode="out-in">`, and `PaletteCard` mounts `role="article"` — no surviving region carries "5 colors extracted" | **Fails on a `role="status"` that unmounts at the moment it would speak** — the trap this gate exists for, and the reason a naive "add a status role" cure passes review and fails the gate. Kin to W4·13's commit-vs-intent lock: **announce on COMMIT** | ⟨ShadowPalette · SP-16⟩ (W4·152) |
| **N22** | **Pointer-capture balance on the plate.** After a two-finger sequence on the spectrum plate, **zero** captured pointers remain outstanding | `SpectrumCanvas.vue:148-150` sets `el.setPointerCapture(event.pointerId)` then overwrites `capturedPointerId`/`capturedElement` with **no `capturedPointerId !== null` guard and no prior `releaseCapture()`**; `releaseCapture()` (`:81-89`) is reached only at `:190`/`:218`, so it can only ever release the **LAST** pointer (this seat's read today) | Fails if a second `pointerdown` is merely ignored — **that is a different product, not a fix**; passes on release-then-capture or on multi-pointer bookkeeping. **Sibling of N1 (dock hold balance): the same unbalanced-acquire class in a second subsystem** | ⟨spectrumcanvas · M-24⟩ (W4·144) |

### 6E. Updated arithmetic block — dated, never silent

**Supersedes §0's "Arithmetic" paragraph and §0a n.8 for the counts named here. The superseded figures
are printed beside the new ones; nothing is amended in place.**

| measure | §0 as written (2026-08-25) | **true count, 2026-08-28** | decomposition |
|---|---|---|---|
| **rows booked** | 123 (`W4·1 … W4·123`) | **173** (`W4·1 … W4·173`, gapless) | 123 + **50** = 14 NWO (§6A) + 35 census (§6B: 20 full + 15 pointer) + 1 external (§6C) |
| **gate entries** | 29 (13 sharpenings + 16 new) | **35** (13 sharpenings + **22** new born-RED) | N1-N16 + **N17-N22** (§6D) |
| **BoundsDelta distinct paths** | 31 (§3a 25 rows / 30 paths; §3b 2 rows / 1 net-new) | **31 — unchanged** | §6 adds **no** bounds row. Three surfaces it touches are **named gaps, not claims**: `ExtractControls.vue` (already §3's CC-105 row), `demo/workbenches/extract/**` (X-W7's carve — W4·152 claims the announcement law only), `ConfigSliderPane.vue` (bounds-unowned → G-F rider, W4·173) |
| **cross-edges** | 14 (CE-1 … CE-14) | **15** (CE-1 … **CE-15**) | **CE-15 — ⟨INBOX I-28 · X-EXT-4⟩ → X-W0.j / X-W6 / X-W7 / the W25 pinned-file executor / G-F.** Five surfaces, four owners, one repin: the receiving-bounds delta is X-W4.g's, the per-site edits are not. **Does not resolve CE-8** |
| **dissents / escalations** | 9 | **11** | **+10.** The extract cone's 8 further NWO escapes (`wb-extract-pane` ×4, `wb-extract-workbench` ×4) home to **X-W7** by packet 4 and are **not taken here** — stated so their absence is a routing result (§6.0 n.3). **+11.** The bare-id vs ⟨record·id⟩ split (§6.0 n.2): 15 rows the looser test reads as carried are carried **only under another record's namespace or by another wave's fold**; this seat books them as pointers and **does not claim them as X-W4 work** |
| **IN-side receipt** | 402 grep hits / 47 records | **282 X-W4-routed rows / 92 records** re-derived id-keyed | The 402 figure was a **hit** count (head-matter legends, instrument blocks, closing verdicts included); **282 is a ROW count** under the census predicate. **Both stand — they measure different things** and neither is struck |

**What did not change.** No §1 row is renumbered, re-severitied or re-homed. No gate is re-staged,
re-weighted or re-composited (OC-2/CC-096 staging stands). No lock, kill (`K-n`), dissent or `UNPROVEN`
carriage is weakened. `waves/W4.md` and `CONFORMANCE-2026-08-03.md` remain byte-untouched (E-3).
Dispositions remain **ADJUDICATED** and status remains **planned** — this section stamps nothing.

**Re-runnable in one line** (the census this section closes):

```
# corpus (92) → X-W4-routed rows (282) → absent from this fold (35) → record-keyed escapes (20)
ls docs/tranches/V/megatranche/registry/adjudicated/*.md | grep -vE '/(kf|fr|pt)-' | wc -l
grep -nE '^\s*([-*]|\|)\s*\*\*' docs/tranches/V/megatranche/registry/adjudicated/*.md \
  | grep -E 'X[-·.]W4' | grep -E '→|Disposition|BOOK|BUILD|routes to|Terminal:' | wc -l
```

— end of the round-1 repair addendum. The rows above are consumed id-for-id from
`ROUND-1-ASSIGNMENTS.md` §A/§B/§C **and** from this seat's own re-run census, which exceeded the roster
in both registers. Round 2 measures against these bytes.
