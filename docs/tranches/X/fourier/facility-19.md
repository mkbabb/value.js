SERVED MODEL: claude-opus-5[1m]

# Facility 19 — `library-surface`: the one-way correspondence ledger (value.js → fourier-analysis)

**Authority**: `docs/tranches/V/megatranche/registry/adjudicated/library-band.md` §2 **RD-8** (the law) + §4 **W.L5 ACT(3)** (the act) · wave `docs/tranches/X/fourier/waves/F-W2.md` §3 **F1**, §5 **G13** · the owner's begin-word 2026-09-17 (COHESION §0j).
**Seat**: X·F · F.W2 unit `.d`, sitting 2026-09-17 (wall clock **2026-09-18 16:3x EDT** — disclosed, never elided, per E-3). `node v26.0.0` · `darwin arm64`.
**Substrate at authoring**: value.js `tranche-u`, package version **4.0.0**; fourier-analysis `f7fa1e3` on `m/w1-bump-migration`, **0 dirty entries**; fourier installs `@mkbabb/value.js` **4.0.0**, `@mkbabb/glass-ui` **8.0.0**, `@mkbabb/keyframes.js` **6.0.0**.

**This file is a LEDGER, not an ask.** It is the value.js side of a one-way correspondence: value publishes, fourier consumes. Nothing in it obliges fourier to publish anything back, and nothing in it is a finding against fourier's tree.

**Every figure below was measured at the bytes by this seat and double-run; the two runs agree.** Each is published beside the command that produced it (⟨cmd⟩), and every command carries a literal absolute path and consumes no shell variable. The fourier tree was **read only** — `grep`, `sed`, `ls`, and node module reads; **this seat wrote no fourier byte**.

---

## §1 — The law (RD-8), quoted at the bank's own bytes

**Receipt range** ⟨cmd⟩ `/usr/bin/sed -n '124,128p' /Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/library-band.md` — one line wider than the law text's own **span `:124-127`**, because the clause soft-wraps across four source lines and a single-line receipt for it would be a false witness.

> *every symbol fourier imports from `@mkbabb/value.js` appears exactly once, with its subpath, its status against the shipped version, and the fourier obligation it discharges; direction one-way (value publishes, fourier consumes); completeness bidirectional (no unlisted import, no listed symbol without a consumer).*

The bank sets this clause in **one italic span and bolds no word inside it**; it is reproduced here with the bank's own emphasis and no other. The direction/completeness contrast stated below lives **outside** the quotation, where the emphasis is this file's to set:

- **Direction is one-way.** fourier is an application, not a mirror. Obliging it to publish a mirrored library surface would be isomorphism in name only.
- **Completeness is bidirectional.** Not a second direction of obligation — a **closure condition on this one table**: §2 must contain no symbol without a consumer (→ §2's Consumers column) and must omit no import the tree makes (→ §3's as-built census, run both ways).

**This is NOT the law of the 18 HTTP facilities, and the label is load-bearing.** `docs/tranches/V/vnext/API-FACILITY-ISOMORPHISM.json` declares ⟨cmd⟩ `node -p "JSON.stringify(require('/Users/mkbabb/Programming/value.js/docs/tranches/V/vnext/API-FACILITY-ISOMORPHISM.json').law)"` → `{"direction":"bidirectional","identity":"intrinsic-job-equivalence-not-identical-routes-nouns-or-runtime","coverage":"each-operation-exactly-once", …}`. **That schema is READ here as precedent and is NOT extended** (F-W2 §2a; §7 *Excluded*): a bidirectional facility 19 would oblige fourier to publish a mirror, which RD-8 declined. **O's contrary argument is preserved as dissent** — it wanted the same bidirectional law, and it lost on that one reason, not on drafting.

**The defect this closes, re-measured at HEAD.** The ratified 18-facility contract names the one real value↔fourier coupling **zero times** — ⟨cmd⟩ (base `/Users/mkbabb/Programming/value.js`) `/usr/bin/grep -c 'value.js' docs/tranches/V/vnext/API-FACILITY-ISOMORPHISM.json` → **0**, and the same command over `easing` · `npm` · `library` · `subpath` · `parseCss` returns **0** at each. The letter O-14 filed this on 2026-07-27; it reproduces unchanged at this seat's bytes. A ratified isomorphism that does not name the one real coupling is a contract with a hole in it. **This file is the hole filled — and it is filled under a different law, not by widening the old one.**

---

## §2 — The ledger: every symbol fourier imports from `@mkbabb/value.js`, exactly once

**Eleven symbols across four subpaths.** Each appears **once**, with its subpath, its status against the **shipped 4.0.0**, and the fourier obligation it discharges.

⚠ **THE SET IS RE-MEASURED AT HEAD AND IS NOT INHERITED.** F-W2 §3's **A1** measured *"6 distinct symbols, easing-only"* at the pre-uplift tree, and §3 **F1** repeats that figure as *"the six-symbol import set the ledger must enumerate."* **That figure was true when it was written and is false at these bytes**: F.W1's atomic transaction (`538db90`) and F.W2's own `.a`/`.b` units (`0cc9b00` · `f7fa1e3`) retired `timingFunctions`, added the `/math` and `/color`+`/css` arms, and re-shaped the `easings.ts` import block. The ledger enumerates **what the tree imports today**, which is the only thing RD-8's law admits; **A1's six is recorded as superseded, not averaged**, and its identity (`fr-EasingPicker L/B-1 + C/B-1`, home **F.W1**) is cited, never re-minted.

| # | symbol | subpath | status at shipped 4.0.0 | consumers (file:line) | the fourier obligation it discharges |
|---|---|---|---|---|---|
| 1 | `easeInOutSine` | `./easing` | **EXISTS** | `components/equation/ConvergencePlot.vue:5` · `components/equation/composables/useCurveTransition.ts:8` · `components/equation/lib/harmonics.ts:5` · `lib/easings.ts:26` | **I-1** — the bare specifier stays retired; the site names its subpath. Landed inside F.W1's atomic transaction |
| 2 | `linear` | `./easing` | **EXISTS** | `lib/easings.ts:26` | **I-2 / I-4 + ESC-4** — one of the **8 analytic survivors** the corpus re-points to the producer; the 14 orphans are defined in-corpus at Δ = 0, never approximated |
| 3 | `easeInOutQuad` | `./easing` | **EXISTS** | `lib/easings.ts:26` | as row 2 |
| 4 | `easeOutCubic` | `./easing` | **EXISTS** | `lib/easings.ts:26` | as row 2 |
| 5 | `easeInOutCubic` | `./easing` | **EXISTS** | `lib/easings.ts:26` | as row 2 |
| 6 | `easeOutExpo` | `./easing` | **EXISTS** | `lib/easings.ts:26` | as row 2 |
| 7 | `easeInOutExpo` | `./easing` | **EXISTS** | `lib/easings.ts:26` | as row 2 |
| 8 | `easeInOutCirc` | `./easing` | **EXISTS** | `lib/easings.ts:26` | as row 2 |
| 9 | `lerp` | `./math` | **EXISTS** | `components/equation/ConvergencePlot.vue:6` | **C-19 ⊕ SC-4** — the sole hand-rolled `lerp` (`useCurveTransition.ts:85`) deleted and its importer re-pointed in ONE commit (fourier `0cc9b00`, F.W2 `.a`) |
| 10 | `toRgba8` | `./color` | **EXISTS — options bag required** | `lib/colors.ts:20` | **I-8** — replaces `hexToRgb`/`hexToRgba`, both deleted at fourier `f7fa1e3` (F.W2 `.b`, W.L5 ACT(2)) |
| 11 | `parseCssColor` | `./css` | **EXISTS** | `lib/colors.ts:21` | **I-5** — replaces the four hand-rolled `cssVarToHex` regex arms, deleted at fourier `3bac3d5` (F.W1) |

**Status witness, all eleven in one command** ⟨cmd⟩ `node -e "const P='/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js';const pkg=require(P+'/package.json');Promise.all(['easing','math','color','css'].map(s=>import(P+'/dist/subpaths/'+s+'.js').then(m=>[s,Object.keys(m)]))).then(rs=>{const at=Object.fromEntries(rs);console.log('version',pkg.version);for(const [sym,sub] of [['easeInOutSine','easing'],['linear','easing'],['easeInOutQuad','easing'],['easeOutCubic','easing'],['easeInOutCubic','easing'],['easeOutExpo','easing'],['easeInOutExpo','easing'],['easeInOutCirc','easing'],['lerp','math'],['toRgba8','color'],['parseCssColor','css']]) console.log(sym, './'+sub, at[sub].includes(sym)?'EXISTS':'ABSENT')})"` → `version 4.0.0` followed by **eleven `EXISTS` lines and no `ABSENT`**, double-run identical.

**Row 1's four sites are one symbol, not four.** RD-8's *"exactly once"* governs the SYMBOL; the Consumers column carries the site multiplicity. The distinction is the one A1's own carry makes (`fr-EasingPicker MISSED-F`, home **F.W1**): a site count is not a cost.

**Subpath totals at 4.0.0, for the reader who wants the denominator**: `./easing` publishes 16 exports, `./math` 9, `./color` 23, `./css` 19; `exports["."]` is **ABSENT** and `main`/`module`/`types` are all **undefined** — ⟨cmd⟩ `node -p "const p=require('/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js/package.json');[String(p.exports['.']),String(p.main),String(p.module),String(p.types),Object.keys(p.exports).join(' ')].join(' | ')"` → `undefined | undefined | undefined | undefined | ./color ./value ./css ./easing ./math ./transform ./quantize`.

---

## §3 — Completeness, both ways: the as-built import census and its command

RD-8's completeness clause is a closure condition, and a closure condition needs an instrument that can falsify it. **The instrument is one command**; it walks every `.ts`/`.vue`/`.js` file under fourier's `web/src`, matches the import blocks whole (so a multi-line block is not truncated the way a line-oriented grep truncates it), and prints every `(symbol, subpath, site)` triple it finds:

⟨cmd⟩
```sh
node -e '
const fs=require("fs"),path=require("path"),root="/Users/mkbabb/Programming/fourier-analysis/web/src";
const out=[];(function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);
if(e.isDirectory())walk(p);else if(/\.(ts|vue|js)$/.test(e.name)){const s=fs.readFileSync(p,"utf8");
const re=/import\s*(?:type\s*)?\{([^}]*)\}\s*from\s*["\x27]@mkbabb\/value\.js(\/[a-z]*)?["\x27]/g;let m;
while((m=re.exec(s))){const line=s.slice(0,m.index).split("\n").length;
for(const sym of m[1].split(",").map(x=>x.trim()).filter(Boolean))out.push([sym,m[2]||"(bare root)",path.relative(root,p)+":"+line]);}}}})(root);
const bysym=new Map();for(const[sym,sub,site]of out){const k=sym+"\t"+sub;if(!bysym.has(k))bysym.set(k,[]);bysym.get(k).push(site);}
console.log("distinct (symbol,subpath) pairs:",bysym.size,"| import statements:",new Set(out.map(o=>o[2])).size,"| total bindings:",out.length);
for(const k of [...bysym.keys()].sort()){const[sym,sub]=k.split("\t");console.log(sub.padEnd(9),sym.padEnd(16),bysym.get(k).join(" · "));}'
```

→ `distinct (symbol,subpath) pairs: 11 | import statements: 7 | total bindings: 14`, then the eleven rows §2 carries, at the sites §2 names. **Double-run identical.**

**The two directions, each stated as what would falsify it:**

| direction | claim | witness | falsifier |
|---|---|---|---|
| **no unlisted import** | the census's 11 pairs ⊆ §2's 11 rows | the command above, read against §2 | a twelfth pair in the census output that §2 does not carry |
| **no listed symbol without a consumer** | §2's 11 rows ⊆ the census's 11 pairs, each with ≥1 site | §2's Consumers column, every cell non-empty | a §2 row whose site does not appear in the census output |

**The set-difference is ∅ both ways at these bytes**, which is what makes this a ledger rather than a wish.

**The census's own denominator, so a reader can check the instrument did not silently skip a form.** ⟨cmd⟩ `/usr/bin/grep -rn '@mkbabb/value\.js' /Users/mkbabb/Programming/fourier-analysis/web/src/` returns **8 lines**: the **7** import statements the census counts, plus `lib/easings.ts:11`, which is **prose inside a docblock** (`* publishes analytically are imported from \`@mkbabb/value.js/easing\`; the other`). `8 − 1 = 7`, and the instrument's statement count is 7. **No `import * as`, no default import, no dynamic `import()` form is present to miss** — every occurrence is accounted for by name.

**`(bare root)` is a live arm of the instrument and it returns nothing.** The regex's subpath group is optional precisely so that a re-introduced bare-root specifier would print as `(bare root)` rather than vanish; **it prints for no row today**, which is F-W2 §5 **G1** re-witnessed from this end — ⟨cmd⟩ `/usr/bin/grep -rn 'from "@mkbabb/value\.js"' /Users/mkbabb/Programming/fourier-analysis/web/src/ | /usr/bin/wc -l` → **0**. That retirement landed inside **F.W1's** one atomic transaction (`538db90`) under **ESC-4**; **facility 19 records it and claims none of that GREEN** (FR-GIG-5).

---

## §4 — `counts`: as-specified, with an as-built sibling census and its command

RD-8 requires the `counts` block be annotated **`as-specified`** and gain an **`as-built`** sibling census *with its command*, so that no later close can cite a target as a measurement.

**As-specified** — the ratified 18-facility contract's own block, read (never written) ⟨cmd⟩ `node -p "JSON.stringify(require('/Users/mkbabb/Programming/value.js/docs/tranches/V/vnext/API-FACILITY-ISOMORPHISM.json').counts)"` →

```json
{"http":129,"headless":17,"total":146,"value":{"http":88,"headless":7,"total":95},"fourier":{"http":41,"headless":10,"total":51}}
```

**As-built** — the sibling census, with its command, run at fourier HEAD `f7fa1e3` ⟨cmd⟩ `cd /Users/mkbabb/Programming/fourier-analysis && /usr/bin/grep -rnE '@router\.(get|post|patch|put|delete)' api/routers/*.py | /usr/bin/wc -l` → **30**, double-run.

| field | as-specified | as-built | reading |
|---|---|---|---|
| `counts.fourier.http` | **41** | **30** | the contract is stale by **11**; the census records 30 and names 41 as the specification it is measured against |

**Per-router breakdown, so the 30 is an enumeration and not a bare integer** ⟨cmd⟩ `cd /Users/mkbabb/Programming/fourier-analysis && /usr/bin/grep -cE '@router\.(get|post|patch|put|delete)' api/routers/*.py` → `api/routers/__init__.py:0` · `admin.py:0` · `contours.py:4` · `equations.py:2` · `gallery.py:0` · `images.py:7` · `sessions.py:4` · `visualizations.py:13`. **4+2+7+4+13 = 30, and the enumeration is the figure.** *(The loop form this receipt first carried — `for f in api/routers/*.py; do … "$f" … done` — is **struck**: it consumed a shell variable, and this file's own law admits no ⟨cmd⟩ that does. `grep -c` over a glob answers the same question with no variable at all, which is the cure rather than a declaration excusing one.)*

**THIS IS A RECORD-ONLY CORRECTION AND IT IS NOT A FINDING AGAINST FOURIER'S API.** It is value.js's contract that was stale. **No byte of `API-FACILITY-ISOMORPHISM.json` is edited by this seat** — the correction is recorded HERE, under facility 19's own one-way law, because editing the bidirectional file is the act RD-8 declined and F-W2 §7 excludes by name.

**The bank re-runs when the count leaves 30.** A later seat re-running the as-built command and reading anything but 30 re-states this table at the measured value and dates the re-statement; it does not average, and it does not carry the superseded figure forward as current.

---

## §5 — The payload: O-14's I-1..I-10 table, re-measured at the shipped 4.0.0

Facility 19 is *populated from O's I-1..I-10 table* (W.L5 ACT(3)). That table was measured 2026-07-27 against 4.0.0's `dist/`. **Every row is re-measured here at the installed, published 4.0.0** rather than transcribed — the M-25 transcription defect is exactly what this wave polices, and a ledger that copies its own source's readings is not a ledger.

| # | symbol (subpath) | O-14's status (2026-07-27) | re-measured at this seat | disposition |
|---|---|---|---|---|
| **I-1** | the entry point | 7 subpaths; no `.`, no `main`, no `module` | **REPRODUCES** — §2's `exports` witness | the bare specifier stays retired; **no shim, no reversal.** §3's `(bare root)` arm returns ∅ |
| **I-2** | `timingFunctions` (`./easing`) | DELETED | **REPRODUCES — ABSENT** | not restored. The consumer re-derived the map instead: **8 survivors imported** (§2 rows 2–8 ⊕ row 1), **14 orphans defined in-corpus**, ESC-4's Δ = 0 gate |
| **I-3** | `easingNames()` (`./easing`) | ABSENT; **ships in 4.1** | **REPRODUCES — ABSENT** (⟨cmd⟩ `node -e "import('/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js/dist/subpaths/easing.js').then(m=>console.log('easingNames' in m))"` → `false`) | **X-W9 §Scope item 8 / `G21` (SCI-1)** ships it at 4.1. The consumer enumerates its own catalogue in the meantime — a divergence with an owner, not a gap |
| **I-4** | `easing(name)` (`./easing`) | EXISTS (`src/easing.ts:166`) — throws on 5 prototype keys | **EXISTS; the 5-key throw REPRODUCES at 4.0.0** — `__proto__` · `constructor` · `toString` · `valueOf` · `hasOwnProperty` each raise `TypeError: … is not iterable`; `easing('nope')` returns `{ok,error} code=easing_name_unknown` | the analytic-arm **RESTORE** is X-W9/W.L6's. **The prototype-key throw is a value.js totality nit and is filed, not cured here** (§7) |
| **I-5** | `parseCssColor` (`./css`) | EXISTS — **throws** (MT-F024 / MTS-01 + a second prototype-key class) | **EXISTS; BOTH throw classes REPRODUCE at 4.0.0** — see §6, which is F-W2 **G9**'s measurement | the four regex arms are deleted (fourier `3bac3d5`); the throws file to **X·V / V·π** and **X-W9** |
| **I-6** | `toHex` (`./color`) | ABSENT; **ships in 4.1** | **REPRODUCES — ABSENT** | the **declared 3-line hex residual** carries the gap, with its validation arm and its **deletion date = fourier's 4.1 adoption** (landed at fourier `f7fa1e3`, F-W2 **G6**) |
| **I-7** | `convertColor` (`./color`) | EXISTS | **EXISTS, and the letter's own value reproduces** — ⟨cmd⟩ oklch→rgb → `{"ok":true,"value":{"space":"rgb","channels":[215.0464290128603,53.13214990941585,34.98507361906434],"alpha":1}}` | consumed through `./color`; no obligation outstanding |
| **I-8** | `toRgba8` (`./color`) | EXISTS — options bag required | **EXISTS; the requirement REPRODUCES** — without options → `{ok,error} code=color_invalid_input`; with `{gamut:"clip"}` → `{ok,value} [215,53,35,255]` | **documented, not changed** — deliberate. §2 row 10's consumer passes `{gamut:"clip"}` at `lib/colors.ts` |
| **I-9** | `resolveCssColor` (`./css`) | ABSENT — **DECLINED for 4.1, with a runnable re-trigger** | **REPRODUCES — ABSENT.** The re-trigger measured at this seat: **NOT FIRED** (§7) | context-free parsing in value.js, context resolution in the consumer that owns the DOM. **The consumer's arm is now a producer facility** — glass-ui `/dom`'s `createTokenColorCache` — which is the honest boundary held from both ends |
| **I-10** | ONE failure contract | two shapes ship side by side | **REPRODUCES exactly** — `parseCssColor('zzz')` → `{ok,diagnostics} code=css_syntax` (**no `error` key**); `easing('nope')` → `{ok,error} code=easing_name_unknown`; `convertColor(bad)` / `toRgba8(bad)` → `{ok,error} code=color_invalid_input` | **RULED: `ParseResult` for text→AST; `Result` for value→value — declared per boundary, never unified.** Written into the contract, not changed in code |

**One measurement command covers I-3 · I-4 · I-6 · I-7 · I-8 · I-9 · I-10** ⟨cmd⟩ `node -e "const P='/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js';Promise.all([import(P+'/dist/subpaths/easing.js'),import(P+'/dist/subpaths/color.js'),import(P+'/dist/subpaths/css.js')]).then(([E,C,S])=>{const c=S.parseCssColor('oklch(0.579 0.201 30.4)').value;console.log('easingNames',('easingNames' in E),'toHex',('toHex' in C),'resolveCssColor',('resolveCssColor' in S));for(const k of ['__proto__','constructor','toString','valueOf','hasOwnProperty','nope']){try{console.log('easing',k,JSON.stringify(Object.keys(E.easing(k))))}catch(e){console.log('easing',k,'THROW',e.message)}}console.log('toRgba8 bare',JSON.stringify(C.toRgba8(c)));console.log('toRgba8 clip',JSON.stringify(C.toRgba8(c,{gamut:'clip'})));console.log('convertColor',JSON.stringify(C.convertColor(c,'rgb')));console.log('parseCssColor zzz',JSON.stringify(Object.keys(S.parseCssColor('zzz'))))})"`, double-run identical.

**Two rows moved since O-14, and both moved because the WAVE landed, not because the library did**: I-5's four regex arms and I-8's two hex helpers now have consumers on the value.js side of the seam instead of hand-rolled twins on fourier's. **No I-row's STATUS moved.** The library surface is byte-stable between the letter and this ledger; what changed is the consumer.

---

## §6 — The G9 measurement: `parseCssColor` at 4.0.0, and what transfers

F-W2 §5 **G9** requires the post-uplift parser constraint be **measured, not transcribed**, and names the reason: `fr-App` **K-11**'s R1 discharge *"was measured at 0.13.0 ONLY — the 4.0.0 target is unprobed."* It is probed here.

**Leg 1 — the banked constraint is measured DEAD, not transferred.** ⟨cmd⟩ (cwd `/Users/mkbabb/Programming/value.js`) `/usr/bin/grep -rn 'colorUnit2\|color2' src/ dist/ | /usr/bin/wc -l` → **0**. `colorUnit2` and `color2` are **both deleted at 4.0.0**. The banked rule *"route through `colorUnit2`; `color2` never"* is a **0.13.0-window rule that names two symbols which do not exist at the target**; transcribing it into a post-uplift spec is the M-25 defect by construction.

**Leg 2 — the `oklch()` / `light-dark()` probe at 4.0.0.** Run against BOTH 4.0.0 copies — fourier's installed published tarball and value.js's own `dist/` at HEAD — and **the two agree on every row**:

| input | result at 4.0.0 |
|---|---|
| `oklch(0.579 0.201 30.4)` *(`--viz-fourier` light arm at glass-ui 8.0.0)* | `ok:true`, space `oklch` |
| `oklch(0.693 0.151 28.1)` *(the dark arm)* | `ok:true`, space `oklch` |
| `oklch(57.9% 0.201 30.4deg)` *(`serializeCssColor`'s own emission form)* | `ok:true`, space `oklch` |
| `light-dark(oklch(0.579 0.201 30.4),oklch(0.693 0.151 28.1))` | `ok:false`, `css_syntax`, expected `["CSS color"]` |
| `light-dark(#ff0000,#0000ff)` | `ok:false`, `css_syntax`, expected `["CSS color"]` |
| `var(--section-color-5)` *(the alias chain)* | `ok:false`, **`color_context_required`**, expected `["context-free color"]` |
| `#f0b632` · `rgb(157 101 21)` *(controls)* | `ok:true`, space `rgb` |
| `rgba(0,0,0,0.3)` *(the legacy comma form)* | `ok:false`, `css_syntax` |

⟨cmd⟩ `node -e "const P=process.argv[1];import(P+'/dist/subpaths/css.js').then(({parseCssColor})=>{for(const s of ['oklch(0.579 0.201 30.4)','light-dark(oklch(0.579 0.201 30.4),oklch(0.693 0.151 28.1))','var(--section-color-5)','#f0b632','rgb(157 101 21)','rgba(0,0,0,0.3)']){let r;try{r=parseCssColor(s)}catch(e){console.log(s,'THROW',e.message);continue}console.log(s,r.ok?('ok space='+r.value.space):('ok:false '+r.diagnostics.map(d=>d.code).join(',')))}})" /Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js` — and the same command with `/Users/mkbabb/Programming/value.js`. Both double-run; all four readings identical.

**What this measures, stated as three findings rather than one verdict:**

1. **The `light-dark()` probe-element rule is the one constraint that transfers whole.** `light-dark()` is `ok:false` at 4.0.0 exactly as at 0.13.0 — it is not a context-free colour and value.js declines to guess which arm the document is in. The consumer must resolve it through the cascade. **fourier does**: `lib/colors.ts` paints `var(--token)` onto a real property via glass-ui `/dom`'s `createTokenColorCache` and reads the **used** value back, then hands that used value — already a plain `rgb()`/`oklch()` — to `parseCssColor`. The rule survives both pins and is the reason the colour arm works.
2. **`var(--…)` answers `color_context_required`, which is the API stating the I-9 boundary in its own type.** Not a defect; the honest refusal that made `resolveCssColor`'s decline coherent.
3. **⚠ THE R1 CLASS REPRODUCES AT 4.0.0 — `parseCssColor` IS NOT TOTAL.** ⟨cmd⟩ `node -e "const P='/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js';import(P+'/dist/subpaths/css.js').then(({parseCssColor})=>{for(const s of ['oklch()','light-dark()','__proto__','constructor','oklch(','oklch(0.5)','']){try{const r=parseCssColor(s);console.log(JSON.stringify(s),'ok:'+r.ok,r.ok?'':r.diagnostics.map(d=>d.code).join(','))}catch(e){console.log(JSON.stringify(s),'THROW',e.constructor.name+': '+e.message)}}})"` →

   | input | 4.0.0 |
   |---|---|
   | `"oklch()"` | **THROW** `TypeError: Cannot read properties of undefined (reading 'replace')` |
   | `"light-dark()"` | **THROW** `TypeError: Cannot read properties of undefined (reading 'replace')` |
   | `"__proto__"` · `"constructor"` | **THROW** `TypeError: e.trim is not a function` |
   | `"oklch("` · `"oklch(0.5)"` · `""` · `"toString"` · `"valueOf"` · `"hasOwnProperty"` | `ok:false` `css_syntax` — **the total arm, and it is the majority arm** |

   **`fr-App` K-11's R1 discharge does NOT transfer to 4.0.0**: the empty-argument functional form still throws, and a second class (prototype-key strings) throws a different `TypeError`. **Consumer reachability, measured and not assumed**: fourier's only call site is `cssColorToHex(css)` at `lib/colors.ts:90`, whose argument is a `getComputedStyle` **used value** returned by the producer's token cache — never an empty functional form, never a bare prototype key — and an unset property is guarded one line earlier (`if (!value) continue;`). **So the crash is a value.js totality defect with no live consumer path at these bytes.** It is filed as the reciprocal (§7); **it is not cured here, not caught in a `try`, and not written around** — the fourier tree is not the place to repair a producer's totality, and wrapping the call would be the masking defect this wave refuses by name.

---

## §7 — The reciprocals value.js owes, and what facility 19 does NOT do

**Declared under COHESION §2's both-ends law.** Facility 19 is the ledger; the seam program owns the cures. **F.W2 files these and claims no GREEN for any of them** (FR-GIG-5).

| # | reciprocal | measured | route |
|---|---|---|---|
| **R-a** | `parseCssColor` throws on `oklch()` / `light-dark()` (empty-argument functional forms) | §6 finding 3, both 4.0.0 copies, double-run | **X·V / V·π** (the parser-seam program) ⊕ **X-W9**. The R1 identity is cited, never re-minted |
| **R-b** | `parseCssColor` throws `e.trim is not a function` on `__proto__` / `constructor` | §6 finding 3 | **X·V / V·π** ⊕ **X-W9** — I-5's *"second prototype-key class"*, still live |
| **R-c** | `easing(name)` throws `… is not iterable` on **5** prototype keys | §5 I-4 | **X-W9 / W.L6** — beside I-3's `easingNames()` ship |
| **R-d** | `parseStylesheet` is **total on the string arm** and **throws on Buffer input** (`e.startsWith is not a function`); `null`/`undefined` throw on `.length`; `42` and `{}` return `ok:true` | F-W2 **G18**, re-measured at the adopted pin (§ the wave record's `.d` receipt) | **X-W9** — the totality nit, filed with its widened surface |
| **R-e** | `parseStylesheet` rejects the legacy comma form `rgba(r,g,b,a)` (`css_syntax`, expected `["scalar"]`), so glass-ui 8.0.0's `dist/styles/tokens/dark-arm.css` fails on its **first** declaration | `.b`'s F-12, reproduced at §6's `rgba(0,0,0,0.3)` row | **X·V / V·π** ⊕ **X-W9**. Relayed by `.d`'s letter |
| **R-f** | `sampleColorRamp` · `mixColorsN` **deleted at 4.0.0**; `sampleColorRamp` **scheduled for 4.1 under SCI-1** | ⟨cmd⟩ (cwd `/Users/mkbabb/Programming/value.js`) `/usr/bin/grep -rn 'sampleColorRamp\|mixColorsN' src/ dist/ \| /usr/bin/wc -l` → **0**; `X/waves/W9.md` §Scope item 8 · `G21` | the 2.0.0 note's §3 strike is re-authored **RETIRED-BY-DELETION AT 4.0.0, SCHEDULED FOR 4.1 UNDER SCI-1** (B6) — in `.d`'s letter, the only vehicle |

**I-9's re-trigger, measured and NOT FIRED.** The decline is reversible on *"a grep for a **second** hand-rolled context-resolution arm anywhere in the constellation."* Measured in a bounded sweep of the two consumer trees this seat may read: ⟨cmd⟩ `/usr/bin/grep -rn 'getComputedStyle' /Users/mkbabb/Programming/fourier-analysis/web/src/ | /usr/bin/wc -l` → **3** — `lib/colors.ts:137`/`:140` (the batched read feeding the **producer's** token cache) and `FrequencyGraph.vue:108` (a used-value read of `color`, no parsing). ⟨cmd⟩ `/usr/bin/grep -rln 'getComputedStyle' /Users/mkbabb/Programming/value.js/demo /Users/mkbabb/Programming/value.js/src` → three demo files, of which the colour one, `demo/color-session/useContrastSafeColor.ts:95`'s `resolveCssColorAlpha`, resolves through a **Canvas2D 1×1 draw** — the engine parses, not a hand-rolled arm. **Neither tree carries a second hand-rolled context resolver; the question stays as ruled.** The nearest candidate is a hand-rolled *parse* of an already-used value, which is a different object and is recorded here as an observation for X-W9/W.L6, **pre-empting no ruling**.

**What facility 19 does NOT do** — named with reasons, nothing dropped:

| item | reason |
|---|---|
| extend `API-FACILITY-ISOMORPHISM.json`'s **bidirectional** schema to facility 19 | **RD-8 ruled a different, one-way law.** O's argument preserved as dissent; the label is load-bearing — *"same law"* would oblige fourier to publish a mirror |
| edit any byte of `API-FACILITY-ISOMORPHISM.json`, including the stale `counts.fourier.http` | §4's correction is **record-only**, recorded here. The JSON is READ, never written (F-W2 §2a) |
| cure R-a…R-f | **X·V / V·π** and **X-W9 / W.L6** own them. A consumer ledger that repairs its producer inside the consumer's tree is the frontend-hack shape this program forbids |
| answer FN-1/2/3/4/6 on the merits | **F.W5 / F.W6.** F.W2 owns the ledger and the ANSWER, never the substance |
| rule FN-5 | chained to the TA-4 value-side atomdiff restoration = **SS-4's OWNER-GATED `G4`**; `.d`'s letter **names the gate** and pre-empts no ruling |
| write any fourier byte | this seat's writable set is four value.js paths. The fourier-side acts of W.L5 landed in F.W1/F.W2's own units under the D-15 grant; **this ledger records them and performs none** |
| a keyframes analogue | **RD-11** — the D-15 grant has no keyframes analogue |

---

## §8 — Provenance pins (R4-8.3): the bytes this file quotes

So that a later rewrite of a quoted file is **detectable rather than deceptive**. Computed by this seat ⟨cmd⟩ `shasum -a 256 <file> | cut -c1-12`.

| file quoted | sha256 (first 12) | what this file takes from it |
|---|---|---|
| `docs/tranches/V/megatranche/registry/adjudicated/library-band.md` | `c1cddf6b8b8a` | §1's law text (span `:124-127`) · W.L5 ACT(3) · the as-built 30 |
| `docs/tranches/V/vnext/API-FACILITY-ISOMORPHISM.json` | `57610b5470c6` | §1's bidirectional `law` block · §4's `counts` (READ only, never written) |
| `fourier-analysis/docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md` (O-14) | `c8cb6b56ade8` | §5's I-1..I-10 payload, re-measured row by row |
| `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` | `f44362757458` | the census of record, cited by F.W2's **G19**; unmoved from the spec's pin |

**The frozen corpus (`registry/adjudicated/**`) is immutable under E-1/E-3** and needs no guard for its line numbers; it is hashed anyway, because a hash that is only taken when it is doubted is not a discipline.

**If a hash here no longer matches at a later read, the quotation it covers is STALE BY CONSTRUCTION and is re-walked, not re-trusted.**

---

*Facility 19 exists. Direction one-way; completeness closed in both. Authored at X·F · F.W2 unit `.d`, 2026-09-18, under the begin-word; **no fourier byte written, no producer byte written, no dated evidence patched.***
