# F.W1 CARRY — the atomic tri-package uplift (X·F)

**Seat**: CARRY, sub-tranche X·F wave F.W1. **Authority**: `docs/tranches/X/COHESION.md` §0a boundary 2026-08-25 · `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (66/66 banked at `35fc8ebf`) · `formation/fourier/CENSUS-2026-08-03.md` §4.2 + `lane-frontend.md` break table.

**Law carried verbatim**: **M-25** — a spec ignoring adjudicated rows is DEFECTIVE, and transcription-only is DEFECTIVE (agglomerate: dedupe by identity, carry cure-shape locks and sequencing edicts, route by mechanism). **L-20** — validate against `docs/precepts/` latest. **Born-RED**: every gate starts RED with its witness named. **L-19** — a gate that names no real witness is contrivance.

**Execution gate**: status fields stay `planned`. NOTHING opens product source. `/Users/mkbabb/Programming/fourier-analysis` is READ-ONLY until the owner's begin-word. Every routing below is agglomeration input; nothing here stamps a wave.

**Method**: source = all 66 `fr-*.md` adjudicated records (precise grep `F[.]W1[^0-9]` → 66 fr records; the 5 `kf-*` hits are `KF.W1x` false positives, verified). Deduped **by banked identity**: a defect banked in N records appears ONCE, citing all witnesses; pure instance sites are folded into their identity's witness cell and never re-booked (anti-rename law). NEW limbs riding a banked identity keep their own banked id.

---

## §Rows

### A · The atomic land-or-lose seam (the wave's spine)

- **AA-2** · fr-AdminAuditLog:36 (primary) · **ADJUDICATED BLOCKER (census-extension)** · mech: glass ≥7 deletes Button `variant` (→ `emphasis`+`tone`) and `size="icon"` (→ `iconOnly`); `./button` SURVIVES, so the export-map census is structurally blind — the break is prop-level behind a retained subpath. `emphasis|ButtonEmphasis|iconOnly` over `formation/fourier/` → 0 hits. · cure: mechanical three-axis re-grammar (`ghost`→`emphasis="quiet"` · `destructive`→`emphasis="primary" tone="destructive"` · `outline`→NO SUCCESSOR, design call · `size="icon"`→`size="md" iconOnly`), floor-preserving per G8 · seq: **must land in the break-surface table BEFORE F.W1 commits its atomic cure budget**; the rewrite is same-commit with the bump (G6) · witnesses (instances, folded, never re-booked): FR-AFP-2 (9 prop sites; three hard TS2322 + six silent fallthroughs; worst `:276` Delete≡Cancel) · FR-COB-2 · fr-BasisSelector M-7 · FR-EQR-2 · fr-EquationView D·D-B5 (nine file-local sites: EquationView:276 · EquationResult:39-40 · EquationModeToggle:11-12/:21-22 · FunctionInput:144-145/:159-160/:190-191 · NotationPills:20-21) · FR-EMT-8 · fr-FourierShapeExtractor `:125` · FB-4 (FullscreenViewer:110) · fr-FunctionInput D-12 (13 instances) · fr-GalleryCard D-8 (4 callsites / 8 attributes) · GCM-18 (4 callsites) · GAB-11 (`:33`) · FR-GSB-9 · FR-NP-5 · D:I-2 (PaperSearchDropdown `:44`) · D-M3 (PaperSearchInput) · fr-PaperView D/B-3 (17 callsites) · FR-USB-39 (icon ×4 hard, variant ×5 silent) · MAJ-6 (VisualizationView) · fr-ConvergenceTimeline D·D-2 · FMD-11 · HLG-5 (×12) · fr-GalleryDraftsSection M-5 · fr-CoefficientsSpectrum D:M-4 · fr-MorphShapePreview · fr-EasingCurvePreview · dissent: FMD-11 records reader-1's BLOCKER-at-today grade; GAB-2's DU dissent revives if the rehearsal shows the silent-fallthrough mode shipping to production rather than dying in the transaction's gate.
- **FR-COB-2** · fr-CanvasOverlayButton:41 · **BLOCKER (fleet fold → AA-2/M-7) + F.W0 arm** · mech: two NEW limbs beyond AA-2 — **(a) the `variant` limb is SILENT** (no `vueCompilerOptions`/`strictTemplates` anywhere in fourier's tsconfig; Volar defaults `strictTemplates:false`, so `variant="glass"` falls to `$attrs` as a junk DOM attribute with zero diagnostic while the button re-tiers to defaults and keeps *looking* glassy) while `size="icon"` is a hard TS2322 — **only the smaller half of the attribute surface is compiler-visible**; **(b) the WCAG 2.5.5 coarse floor RE-KEYS**: 4.0.0 keys `[data-size="icon"]` (a11y-overrides.css:115-121), v7.0.0 keys `[data-control-target]` (responsive.css:3-8) set from `iconOnly` (Button.vue:92). · cure: corrected budget **9 live `variant="glass"` / 7 files** and **36 real `size="icon"` (35 live)** — not 12/8, not 38 (K-12) · seq: this file is the ONLY member whose complete cure is DELETION → **F.W0** (`git rm`, minute STEP-2i, rule F8-REACH-01+02 in one breath) · dissent: none.
- **D-M3 (budget of record)** · fr-PaperSearchInput ruling 1 · **MAJOR** · mech: both readers' cell sets REJECTED — the 35/113/38 greps measure FILES not Button tags (113 includes 14 `<Slider variant="standard">` + 1 MetricBadge + 9 comment mentions); the rival tag-scan counts comment-resident tags and its `<Button\b[^>]*>` regex terminates at the `>` inside `v-if="navStack.length > 0"` (PaperView.vue:398-405), dropping a real `variant="glass" size="icon"` site. · cure/figure: **87 real `<Button variant=…>` sites · 36 real `size="icon"` · 35 files** (ghost 46 · outline 21 · glass 10 · destructive 4 · default 4 · link 1 · secondary 1), **plus 9 `variant=` + 2 `size="icon"` comment-resident mentions** (typecheck-invisible, grep-poisoning — doc updates owed) **plus 17 non-Button `variant=` sites** in Button-importing files · seq: re-run the stripped-comment grep at the ADOPTED pin (G1) before sizing · dissent: competing banked cells preserved — AA-2 37 files/38/124 · fr-GalleryCard 96/77/35 · fr-ConvergenceTimeline `size="icon"` ×38 / `variant="glass"` ×12 · FR-GSB-9 124 `variant="` lines. Reconcile at G7, do not average.
- **M-7 (prop-VALUE-axis census amendment)** · fr-BasisSelector:56 · **MAJOR (F.W1 SCOPE AMENDMENT)** · mech: the census model measures the wrong dimension. BasisSelector is the cleanest single-file proof: all three imported subpaths SURVIVE 7.0.0, yet the file breaks on SIX prop values + the lucide specifier — **zero subpath breaks, six prop breaks** (i-4). · cure: **F.W1 must re-derive the break surface as a prop-level diff over every surviving subpath**, not a member-wise export-map re-run · seq: precedes every per-file budget · improve-side carried: ≥7 adds `loading` (cures D-m12) and defaults `type` (cures D-m14, Button.vue:95) · dissent: none.
- **FR-EQR-1 (`copied` → `status`)** · fr-EquationResult:36 · **MAJOR (new; the unbanked third leg of the uplift triple)** · mech: installed 4.0.0 `useClipboard` d.ts returns `{ copied: Ref<boolean>; copy }`; producer at v7.0.0 AND 8.0.0 returns `{ status: ComputedRef<ClipboardStatus>; copy; invalidate }` — `copied` grep → 0. `v-if="copied"` goes permanently false at the uplift: confirmation state silently disappears, no type error (no `strictTemplates`), no runtime warning. · cure: migrate the triple — **`EquationResult.vue:15` · `UserSlugBar.vue:23` (`resetMs: 1500`) · `useMorphConfig.ts:58`** — in the SAME change as the bump · seq: rides G6; extends AA-2's budget by a third symbol family · rider (K-12): `CopyFailureReason` narrowing (3→2 members) and `timeoutMs` are **v7.0.0 facts**, binding at ANY ≥7 target — the exec-command fallback is gone, so unreported failures rise exactly where FR-EQR-5 says nothing is reported · dissent: FR-USB-39's §7d "explicit non-findings" roll-call is incomplete by this row and MUST NOT be used to close UserSlugBar's F.W1 budget.
- **FR-EQR-3 (the four console-error gates)** · fr-EquationResult:38 · **MAJOR (new consequence on AA-2)** · mech: the producer's DEV retired-prop watcher (`Button.vue:131-146`, `console.error("[glass-ui] <Button> was given …")`, fired per mounted Button with `variant` in attrs or `size==="icon"`) meets **FOUR** zero-console-error e2e gates whose filter lists pass `[glass-ui]` through — `visualization-crud.spec.ts:439-482/:612` · `workspace-flow.spec.ts:175-202` · `contour-extraction.spec.ts:139-166` · `gallery.spec.ts:118-135` — on a matrix that runs the **DEV server** (`scripts/e2e.sh:102` `npx vite web`), so `import.meta.env.DEV === true` under the whole matrix. · cure: **the bump + the 162-attribute prop rewrite + the `copied` triple land as ONE change**, or four green specs go red with the redness attributed to the wrong cause · seq: **THE atomic land-or-lose lock (G6)** · rider: FR-EQR-19's KaTeX `strict` warns are a DIFFERENT channel (`msg.type()==="error"` only) — do not conflate · dissent: none.
- **FR-COB-19 (the stacked single-file instance)** · fr-CanvasOverlayButton:73 · **MINOR (sequencing note on M-7)** · mech: `EquationView.vue:274-283` stacks a COUNTED census break (`./hover-card` removed → `<Popover>`, folding at 5.0.0) and an UNCOUNTED one (the inner Button's `variant`/`size`) on ONE callsite whose `as-child` merge semantics change in the same commit — the concrete instance a member-wise export-map re-run still misses (both members survive their subpaths) · cure: treat as a single migration unit · seq: rides M-7 · dissent: none.
- **U-2 (SelectTrigger 8.0.0 delta)** · fr-SpeedSelect:43 · **MAJOR** · mech: across the uplift the DEFAULT trigger gains `glass-control-edge` (box-shadow rim `border:none` cannot null) + `glass-capsule-hover`; at **8.0.0 `variant`/`size` are REMOVED from SelectTrigger entirely** on the producer's own written design ruling ("a picker with no edge is not a picker") — props `{disabled?, class?}` only. The corpus's "cured for free by `variant="ghost"`" INVERTS. · cure: explicit `box-shadow: none` (nowhere prescribed in the corpus) **or abandon the invisible-trigger design** — a **design decision, not a prop swap**; the direction is now CONTRA-PRODUCER · seq: books as a DELTA on the banked 8.0.0 prop-break class (fr-ConvergenceTimeline SR-1/K-8/D·D-2); this instance was unlisted in that budget · dissent: R1-DU holds MAJOR on the permanent-blur hazard; BANK trigger = `grep "contain: paint"` on the installed dock/shell.css at any uplift.

### B · The re-pin act (derive the break surface at the ADOPTED commit, not the version string)

- **M-16 / FR-EQC-16 / K-17 (RE-PIN GATE)** · fr-CoefficientsSpectrum:68 (primary) + fr-EqCoefficientsPanel:53 + fr-EquationView:369/:376 + FR-EMT-22 + fr-ConvergenceTimeline K-8/SR-1 + fr-ConvergenceLegend SR-1 + fr-ConvergencePlot SR-1 + fr-EditorControlsDock M-10/K-9 + fr-FourierShapeExtractor SC-3/#9 + fr-ExportModal §4a + fr-GalleryCardModal UNPROVEN-8 + fr-GalleryView #10 + FR-GFC dissent-5 + fr-SpeedSelect #8 + fr-Tooltip ruling-2 + FMD #11 + HLG #8 + MPC #11 · **MAJOR (method/structural)** · mech: the corpus's entire producer layer is pinned to a producer that MOVED, repeatedly, mid-audit: v7.0.0-322 → -393 → `e73ec80c` → `4a86570b` → `558c3fa3` → `350f7a90` → `407de2d3`; **version 8.0.0 with v7.0.0 and v8.0.0 both tagged**; three verified tag↔HEAD inversions already banked (animated-digit · `RETIRED_FLOATING_ATTRS` · Button's DEV voice), all landing 2026-08-04, hours after the challenges were authored. **Any F.W1 sizing built on the tag/version string is sizing a target that no longer exists.** · cure: **F.W1 names its target TAG and its ADOPTED COMMIT HASH in the spec before any budget cell executes**; every producer `file:line` re-resolves at that hash; every "at 7.0.0" cell in 66 records re-reads · seq: **first obligation of the wave — RE-PIN, then size** · **owner-gated**: 7.0.0 vs 8.0.0 is a formation decision no seat can settle. **Seven fr-GalleryCardModal dispositions flip polarity on it** (GCM-6 ✕ floor · GCM-14 `w-full` cure inverts · GCM-22 superseded by the `:where()` clamp · the dismiss/gutter cures revive); fr-SliderControl K-1's rebaseline bank fires; fr-Tooltip FR-TT-5/-9 sit on opposite sides; fr-FrequencyGraph's zero-break certification reverses; `text-admin-label` returns · dissent: BASELINE LAW (K-12) — the ADOPTED tag decides regardless of what a working tree self-reports.
- **K-1 (the version-lying window)** · fr-FrequencyGraph:84 · **MAJOR (cross-repo correction relay, not a re-book)** · mech: `git show 6b450f22:package.json` → `"version": "7.0.0"` — **the producer WORKING TREE self-reported 7.0.0 for five days (2026-08-04 → the 08-09 bump `a8a6f66b`)** while carrying 8.0.0 deletions. Seats that read the tree read a version-lying source, not a misquoted tag. · cure: **every "producer 7.0.0" roster read taken in the 08-04..08-09 window is SUSPECT and must be re-resolved at the v7.0.0 TAG before F.W1 sizes any break** · seq: precedes G5 and G7 · named needs-recheck: banked **FR-CP-21** and **fr-App MG-β** ("GONE at 7.0.0") carry this error — `git grep -n admin-label v7.0.0 -- src/styles` → **4 hits**, v8.0.0 → 0; and **fr-App B-2's `.paper-texture` premise is NAMED as needs-recheck under the same window** · dissent: none — but if F.W1 retargets 8.0.0 the breaks are real again and **re-baseline rather than re-book**.
- **FR-EQR-33(c) / GAB-13 (land-or-lose the substrate)** · fr-EquationResult:355 + fr-GalleryAdminBanner:465 · **MAJOR** · mech: the "installed 4.0.0 pin" the whole 66-record corpus audits is itself an **UNCOMMITTED row of the in-flight `m/w1-bump-migration` dirty set** — committed `web/package.json` pins glass `^3.1.0` / keyframes `^2.2.0` / value.js `^0.10.0` / lucide `latest`; 28 dirty paths; `git stash` would erase every "at the pin" statement. · cure: **land the WT diff or record it explicitly as the audit baseline BEFORE any per-file budget commits** · seq: **F.W0 hard predecessor**; the branch must land-or-lose it · witnesses: fr-VisualizationView D-29/INF-1 (subject uncommitted mid-uplift; `style.css:81-83` still cites the retired `UnderlineTabs`) · FR-COB-10 (InfoCard's identical dirty `:amount=`→`:value=` edit; **F8-REACH-01 and F8-REACH-02 must be ruled in ONE breath** — the corpus and both readers cited one row of a two-row table) · fr-AdminFlaggedPanel ruling-13 (the `variant="opaque"`→`surface` one-hunk fix is UNLANDED; every "survives 4→7 untouched" superlative describes the working tree) · dissent: none.

### C · The P0 CSS-class census (the break the import-shaped model cannot see)

- **B-2 (.paper-texture)** · fr-App:66 · **MAJOR (corpus BLOCKER regraded, R-4)** · mech: `.paper-texture` (App.vue:24) is a live rule at installed 4.0.0 (`dist/styles/cards.css`, `@layer components`, `:where(.dark)` arm) and definition-absent at the producer roster (`@utility paper*` = `paper-underpaint` + `paper-grain-overlay`; `.paper-texture` over src/+dist/ → ∅; survivors = the `--paper-texture-size` token + stale prose). Neither surviving recipe is a drop-in for an `h-dvh` flex shell. · cure: **P0 GATE OBLIGATION — diff every glass-ui-owned class fourier applies against the target's `@layer components` + `@utility` roster BEFORE the bump** (G5); the cure decision is texture-restore vs class-delete, decided by one before/after of the shell · seq: MG-β is that census's third instance, found by running it · **cure-constraint (fr-SvgFilters M-1/R-3)**: the B-2 cure must NOT prescribe the 4.0.0 `@utility paper-*` recipes — decoded and proven to paint a **uniform opaque black tile** (`feBlend multiply` against opaque black annihilates the noise, cr=0/qr=1 per pixel); the only working producer artifact at the pin is the token/`.paper-texture` form · dissent: LC holds BLOCKER (the shell's whole ground register); **the MAJOR demotion is priced on G5 existing — if the census gate is not adopted, revisit the regrade** (fr-App dissent 2). Premise needs-recheck under K-1's suspicion window.
- **MG-β (`text-admin-label`)** · fr-App:70 · **MAJOR (census instance 3)** · mech: a real `@utility` at installed 4.0.0 (`semantic.css:213` over `--type-admin-label: 0.625rem`), applied at **7 sites / 4 files** — AdminFlaggedPanel `:176/:180/:189` · AdminUserList `:376/:379` · FrequencyGraph `:200` · CoefficientsSpectrum `:110` — surviving at the target only as a `class-names.ts:84` regex-allowlist entry with **no rule behind it**, which is worse than absence (a validator still accepts it). `vue-tsc` and Playwright are both blind; the 10px admin micro-type silently reverts. · cure: reconcile inside G5 — either the producer restores the utility via the BH relay, or the consumers re-target a surviving rung (`--type-micro`/`--type-caption` + `tabular-nums`) · **IDENTITY COLLISION (load-bearing)**: banked **fr-AdminAuditLog AA-15** books `text-admin-label` as the CURE for `text-[0.65rem]` ("the one-line cure, no uplift") — **cure and break are on a collision course at F.W1; the census gate must reconcile them** · witnesses: FR-CP-21 (both tooltip data grids; precision/size inverted; no `tabular-nums`) · fr-CoefficientsSpectrum D:M-8 · FR-FG-12 (`text-transform: uppercase` renders the operator `LOG₁₀(·+1)` — invalid notation in the component whose credited best work is that operator) · dissent: the "GONE at 7.0.0" premise is CORRECTED by K-1 (alive at the v7.0.0 tag; gone at v8.0.0).
- **FR-EQC-3 / K-13 (the `.cartoon-card` shim)** · fr-EqCoefficientsPanel:323 + fr-AdminAuditLog K-13 + fr-InfoCard FR-IC-19 + fr-GalleryView FR-GV-18 · **MAJOR** · mech: `style.css:98-112` resurrects the C.W5-retired `.cartoon-card` as a local `@utility` while `./card` ships at the pin and at 8.0.0; the shim's own prose self-reports "14 sites (13 files)" — **census of record 21 application occurrences / 14 files** (lane-frontend's 25 is the raw denominator; both wrong in opposite directions) · cure: `<Card class="cartoon-surface">`; radius arrives free (`.card` carries `border-radius: var(--radius-card)`) · seq: **F.W1-sequenced F.W3** · **CROSS-ROW CONFLICT (must be ordered by the spec)**: fr-AdminAuditLog **AA-4** prescribes ADOPTING `cartoon-card` as AdminAuditLog's chassis cure while FR-EQC-3 retires the shim — adopt-then-migrate, or route AA-4 straight to `Card`, or one wave undoes the other · **KILL-6 lock**: `<Card tier="opaque" surface="cartoon">` is UNSPELLABLE at the target (`SURFACES` has no `"cartoon"`, `SURFACE_TIERS` no `"opaque"`; the interim `cartoon` prop struck at HEAD; the docblock ENDORSES the consumer-written `cartoon-surface` class) — executing the corpus's migration lands **21 non-compiling values across 14 files** in a repo whose only gate is `vue-tsc` · dissent: fr-GalleryView FR-GV-18 held MAJOR by LC; ruled MINOR-provisional pending the G4 emission build.
- **FR-NP-13 (`.btn-pill` deletion)** · fr-NotationPills:651 · **MINOR** · mech: `:38/:40` are inert at the pin (`.btn-pill` supplies both — surfaces.css:120 `@apply`, :134 radius) and become **load-bearing after 7.0.0 deletes `.btn-pill`**; `:38` also hardcodes past `--radius-pill` · cure: keep the local declarations knowingly, or re-token · seq: rides G5 (fourth class instance) · dissent: none.

### D · The `--viz-*` palette collapse identity

- **D-1/BC-1/C-1** · fr-BasisCanvas:100 (primary) · **ADJUDICATED BLOCKER** · mech: `cssVarToHex` (`web/src/lib/colors.ts:22-53`) matches four forms (hex passthrough / `hsl()` / bare triplet / `rgb()`) and falls through at `:53` to `#888888`; the installed producer authors the basis tokens **only** in `oklch()` (color-radius.css:263-265, dark-arm.css:113-115) and `light-dark(oklch(),oklch())` (light-dark.css:144-147); **no `@property` color registration exists** (registration census: progress/phase/specular/glass-level/ui-scale only). `resolveVizColors()` overwrites the seeded brand hexes on mount and on every root class flip. Blast radius (K-10-trimmed): `.fourier` (trail :127, multi :326, connector `epicycles.ts:258`, tip glow :283, dead label `labels.ts:89`), `.chebyshev`, `.legendre`; `.golden` static-and-correct; `.green/.rainbow/.pink/.emerald` zero readers. CI is blind (zero `toHaveScreenshot` in e2e). · cure: route conversion through **`colorUnit2 ∘ parseCSSColor`** (executed sound); **`color2` NEVER** — it throws on the wrapper and, unwrapped, **silently returns wrong channels** (a red input reads greenish) so the obvious repair ships a wrong-colour bug with no error signal (MG-ι); `light-dark()` arm-selection requires the **used-value probe-element read, never string parsing** · **CURE RE-HOME (M-RTC, amends the cure angle)**: installed glass-ui 4.0.0 ALREADY SHIPS the resolver at `@mkbabb/glass-ui/dom` — `resolveTokenColor(css, el)` + `createTokenColorCache` (paint-onto-a-real-property → `getComputedStyle(el).color` → cascade-resolved `rgb(...)`, `invalidate()` documented for dark-mode flips) and `useTokenColor` (reactive via `useGlobalDark`), present in the 4.0.0 exports AND the v7 keyset. It never PARSES token text — exactly the failure mode — and it IS the "used-value probe" the record demands. Zero registry hits for it. **The sanctioned seat exists at the CURRENT pin: no uplift, no cross-repo coordination.** · seq: same wave as D-2 and C-4; **the colors.ts leg is landable at 0.13.0 TODAY, pre-uplift** — the census's F.W1-before-F.W2 sequencing is wrong for this leg (only the bare-root-specifier leg is genuinely gated on the uplift) · **sharpened frame (K-10)**: the poisoning is confined to the JS/Canvas path — 20+ live `var(--viz-*)` CSS declarations across the chrome resolve natively — so the true symptom is a **CHROME↔CANVAS DIVERGENCE**: header, docks and controls paint correct brand hues while the instrument the app is named for paints grey · arithmetic of record: sole survivor `--viz-amber` = **`#9d6515` light / `#e8b96d` dark** (via fourier's own `hsl()` D.W4.d override, not the resolver) — four of five grey, not three · provenance (MG-κ): the committed stale build `web/dist/assets/index-57FkGzlZ.css` emits `--viz-fourier:#d73523` / `light-dark(#d73523,#eb7366)` / `#eb7366`, dating the regression — the prior pin authored HEX (making `colors.ts:29` the then-primary path), the light `light-dark()` arm was ALREADY falling through before 4.0.0, and `#d73523` is byte-identical to `colorUnit2(oklch-token)` · witnesses (folded, never re-booked): fr-App B-1 · fr-BasisSelector C-2/L-B1 (locally REFUTED, K-4 — the projection targets the dead `--slider-scrub-*` vocabulary) · fr-GalleryCard C-4 · GCM-20 · FR-GSB-3 · GM-F7 · FB-3 · fr-ContourEditorCanvas FOLD (**new rider: EditorControlsDock.vue:123 paints the magnet slider track from `VIZ_COLORS.fourier` — the exact control D/M-9 depends on goes grey**) · fr-EditorControlsDock R-8 · FR-GIG C-6 · fr-PaperSidebar C-B1 · fr-DarkModeToggle L-3 · fr-ContourSettings C-11 (REFUTED twice, INFO) · fr-EasingPicker C/M-5 kill · fr-SliderControl R-18 hex arm · fr-GalleryView C·C-1 · **interlock (C §8, the sharpest statement in the corpus)**: curing the `basisDisplay` snapshot ALONE turns every basis pill grey — **D-1 and D-2 are ONE repair** · dissent: DU holds C-2 at BLOCKER (a cure installed-and-unimported is the consumption-axis definition of one); ruled MAJOR.
- **D-2/BC-5/C-3 (`basis-display.ts` module-eval snapshot)** · fr-BasisCanvas:101 · **MAJOR** · mech: `basis-display.ts:3-7` value-copies three `reactive()` properties into a plain object literal at module eval; the theme MutationObserver can never reach it. Frozen consumers (legend fill `labels.ts:39`, multi stroke `:257` → seed `#bf4040`) vs live consumers (trail/glow/connector → `#888888`): one basis, two colours, one frame. Independent of D-1; survives any resolver fix. · cure: ships with D-1 (one repair) · **R-1 ENTRY-ROUTE CORRECTION (binding; the banked row's own cell inherits it)**: the "evaluates strictly before App's onMounted" staging holds ONLY when the ENTRY route's chunk imports `basis-display` (deep link / saved-tab `/visualize`·`/gallery`); `basis-display` is imported by 7 files, ALL under `components/visualization/`, and `getSavedTab()` defaults to `/paper` — on the clean default path the module evaluates POST-poison and freezes **grey**, agreeing with the live side. **The red-legend/grey-curve split-brain is entry-conditional; every SS-13 witness and every repair test must pin the entry route.** · scope: `basisDisplay` has **8 files**, not "declaration + one consumer" (K-10); colour radius **6, not 7** (PP-COLORSEAM) · witnesses: fr-App m-8 · fr-BasisSelector C-4/L-M6 (+ the pill-contrast consequence books separately as B-3) · fr-GalleryCard D-4 (pill contrast at `--card`: light 4.17/3.97/4.40 · dark 2.55/2.67/2.42) · GCM-21 · FR-GSB-4 · PP-COLORSEAM · **kill carried (K-5)**: D-4's prescribed cure ("make `color` a getter") is KILLED — both prescriptions restore reactivity to a resolver returning `#888888`; the cure alone makes the surface worse · dissent: none.
- **C-4 (resolver placement)** · fr-App:68 · **MAJOR** · mech: resolution is hooked at the root's `onMounted` — the one placement that guarantees the child-first race — and the tree DOCUMENTS the race in prose instead of fixing it (`useCoeffHover.ts:60-65`: "the canonical fallback used when resolveVizColors has not yet run") · cure: the routine is a pure global bootstrap; the correct seat is `main.ts` before `app.mount()`; **ships with the B-1 cure, same lines**; consumer denominator **11 true readers** (GalleryCard's import is dead, K-13) · dissent: none.
- **fr-BasisCanvas C-4 (no reactive dependency)** · fr-BasisCanvas:102 · **MAJOR** · mech: no draw path holds a reactive dependency on `VIZ_COLORS` (6 occurrences, all inside watch/rAF callbacks — untracked reads; no watcher source mentions it) — a theme flip never repaints a paused or off-screen canvas · cure: **becomes newly visible the moment D-1 is cured** — same wave · seq: with D-1/D-2 · dissent: none.
- **M-β5 (the cure's own gate)** · fr-BasisCanvas:108 + fr-App L-9 · **MINOR** · mech: `hexToRgba`/`hexToRgb` (`colors.ts:101-117`) do ZERO format validation — a 3/4/8-digit hex yields `rgba(…, NaN, …)`, which Canvas2D **silently ignores on assignment** (previous fill/stroke survives); the `:29` hex fast-path forwards unvalidated hex into fixed-slice parsers (`#f0b` → `[11, NaN, NaN]`). MG-κ shows this path is **newly-dead, not born-dead** — it was PRIMARY at the prior hex-token pin, so a hex-token reversion repair walks straight into it. · cure: validate before the D-1 repair emits any hex branch — **the repair's own gate** · seq: same commit as D-1 · dissent: none.
- **L-7 · m-9 · M-β6 · M-α5 (palette-cohesion riders)** · fr-App:72/:71 + fr-BasisCanvas:111/:105 · **MINOR ×4** · mech: `VIZ_COLORS.green` is written (`colors.ts:95`) and read by nobody — one of five forced style reads per boot/flip computes a dead value (L-7, dies in the resolver cure) · `MOON_COLOR #c084fc` "matches VIZ_COLORS.legendre" (DarkModeToggle.vue:29-31) is false at both ends (legendre default `#9545b8`; `#c084fc` is `STATIC.rainbow[4]`; post-B-1 legendre is `#888888`); `SUN_COLOR` has no palette anchor at all (m-9) · `resolveVizColors` makes **five separate `getComputedStyle(document.documentElement)` round-trips per invocation**, re-run on ANY root class mutation with no value filter (M-β6) · the focal mark mixes colour systems: tip glow `hexToRgba(VIZ_COLORS.fourier,…)` around a hardcoded `#ff3b3b` core + white highlight — a third divergence site surviving any `basisDisplay` fix (M-α5) · seq: all four ride the palette cure · dissent: none.
- **HLG-41 (ORDERING EDICT)** · fr-HarmonicLevelGrid:588 · **INFO-latent + sequencing rider** · mech: the dead `:style` bindings (`:25/:48`) subscribe this render to `VIZ_COLORS.chebyshev`. At HEAD: zero cost (hasChanged gate + per-key tracking + stable `#888888`). LATENT: the colour cure makes chebyshev arm-variant and **every theme flip then rebuilds ~667.6 KiB — the cure MINTS the trigger.** · cure/lock: **delete the two dead bindings BEFORE or WITH the `cssVarToHex` cure, never after** · dissent: none.

### E · Producer-deleted CSS grammar (the silent, gate-blind class)

- **B-1 (the disclosure hang)** · fr-CollapsibleSection:204 · **BLOCKER (uplift-conditional)** · mech: the scoped, **UNLAYERED** `animation` SHORTHAND at `CollapsibleSection.vue:60-65` names `collapsible-open`/`collapsible-close` — present at 4.0.0 (`dist/styles/animations.css:18/:29`), **deleted at the producer and still deleted at 8.0.0** (grammar moved to `disclosure-open`/`disclosure-close` inside `@layer components`). Because the block is unlayered and a shorthand it beats the layered producer rules for `animation-name`, `-duration` AND `-timing-function`, so the substrate motion never reaches the element. Consequence at the target is not a lost animation but a **FUNCTIONAL HANG**: reka's `usePresence` waits forever in `unmountSuspended` for an `animationend` no dead keyframe name can fire — **all five disclosure surfaces visually never close**. · cure clauses (all verified): (a) the fix is a **DELETION, not a rewrite** — the 7.0.0+ grammar is static, `@layer`-shipped and emission-independent; (b) the local PRM guard (`:66-71`) deletes WITH it — the producer carries its own PRM carve (`.disclosure-content { animation-duration: 0.01ms }`); (c) the A8-17 `onanimationend` cure is sequencing-coupled, not free (K-2); (d) the R-5 scope-id coupling routes identically on both branches · **LOCK: the block AND its `ContourSettings.vue:361-374` twin die IN THE UPLIFT COMMIT** · witness: close-COMPLETES and close-ANIMATES on all five surfaces (4 CollapsibleSection callsites + the ContourSettings fork) · rider (fr-ContourPreview row 15): at the target the shorthand does not merely go inert — it **SUPPRESSES** the substrate's working reveal by resetting `animation-name`; **no CSS gate can catch it** · **witness-ordering lock (fr-EqCoefficientsPanel D/M-8)**: cure the HOST import breaks FIRST, then run the five-surface witness — **or the loud break hides the quiet one** · dissent: LC holds MAJOR ("a future regression contingent on an uplift that has not landed" is a true description of today's tree); **if F.W1 is descoped, B-1 regrades to MAJOR-with-gate automatically** · kill carried (K-1): D/M-6's "250ms is 100ms short at 7.0.0 via `--spring-smooth-settle`" is DEAD — `spring-smooth` over the producer styles → 0 hits; the real clock is `--spring-present-duration` = 0.2s × tempo = **200ms**; LC's confirmation of it was citation-inheritance.
- **B-4 (`./animated-digit` deleted)** · fr-CoefficientsSpectrum:187 + FR-CP-14 · **BLOCKER-at-uplift** · mech: `CoefficientsSpectrum.vue:19` `import { AnimatedDigit } from "@mkbabb/glass-ui/animated-digit"` — an exports key at installed 4.0.0, **present at the v7.0.0 TAG, and absent at producer HEAD** (component dir gone; deleted at `4bf53962`, whose body names "fourier CoefficientsSpectrum" as its relay target). **The file's only break that fails at BUILD time rather than paint time**; uncensused; affirmatively cleared by the D axis (S-4 "PRESENT at 7.0.0", K-2) on citations to a file that does not exist at HEAD (K-3). Blast radius corrected: **exactly one consumer file repo-wide.** · cure: **NOT a rename** — `Metric` is explicitly noninteractive-static; the sound cure is hand-wiring `useAnimatedNumber` (exported via `./motion` at BOTH pins) + a `tabular-nums` span; the composable's `initial` option discharges m-20 free (no consumer could seed the damper at 4.0.0, so every mount counted up from 0.00 behind a collapsed panel) and i-3's hand-wired `tabular-nums` must be genuinely owned by the successor · seq: **first obligation is the RE-PIN (B); if F.W1 pins the dead tag exactly, the break defers one pin** · relay: producer MIGRATION.md:738-739 still homes `AnimatedDigitMode`/`AnimatedDigitProps` at the dead subpath with no removal marker, in a table that marks removals for its neighbours — the stale row that seeded four fabricated cites → BH letter, riding this row · dissent: LC graded MAJOR on the tag-vs-HEAD honesty; ruled BLOCKER-at-uplift with the caveat carried.
- **FM-2 (focus-ring registers deleted)** · fr-FourierMorphSvg:406 · **MAJOR, BLOCKER-on-execution** · mech: the producer deletes BOTH ring registers; **six read sites / five files lose focus indication at F.W1**, invisible to the all-import/type break surface. Proven AT the v7.0.0 TAG (ruling 3/K-19) — no longer HEAD-dependent. Falsifies banked fr-AppHeader killed-item-11 at the producer; strands banked N-6's cure. · cure: **a token-level break-surface COLUMN** in the census (not import-level) + F.W4 rider (six sites → `--focus-ring-color`, one edit) · dissent: none.
- **FR-COB-6 (pressed paint removed)** · fr-CanvasOverlayButton:153 · **MAJOR rider** · mech: `v7.0.0:src/components/button/styles.css` has **0** `aria-pressed`; the tag-wide enumeration leaves only `material.css:447` reaching a default-register Button, and its sole declaration is `--specular-intensity: 0.16` on a `::before` lens — not background/border/ink. Every surviving `<Button :aria-pressed>` toggle **keeps semantics and loses its background delta**. Direction not uniformly bad: prefers-contrast/forced-colors users GAIN a 2px border. · cure: author an explicit pressed treatment, or re-home the toggles onto surfaces that retain pressed paint at ≥7 (dock/tabs families) — decided when the M-7 prop-diff runs · relay: "was the removal intentional?" → BH letter (FR-COB-28 item 3) · dissent: none.
- **fr-GalleryCard D-21 (`--shadow-cartoon` sign flip)** · fr-GalleryCard:478 · **INFO (silent visual)** · mech: `--shadow-cartoon`/`-hover` flip sign at v7.0.0 (`3px 3px` → `-3px 3px var(--cartoon-ink-lead)`, both sides `git show`-verified) — **every card stamp mirrors with no source change and no typecheck signal** · cure: an F.W1 **visual-regression checkpoint**, not a code change · dissent: none.
- **GCM-22 (`p-0` neutraliser dies)** · fr-GalleryCardModal:492 · **MINOR (ledger rider)** · mech: at v7.0.0 the dialog pad moves to `px-(--overlay-pad-inline)`/`py-(--overlay-pad-block)` LONGHANDS, which land in DIFFERENT `cn()` buckets (`padding-x`/`padding-y` vs `padding`, first-match classifier), so the consumer's `p-0` stops displacing anything, both ship, and tailwind emits the longhands later — **the full-bleed hero regains ≈24px/30.5px insets** · cure: retire `p-0` for authored bleed geometry in the same transaction · seq: **superseded if the target becomes 8.0.0** (the `:where()` clamp changes the picture and `w-full` genuinely outranks) — flips on G1 · dissent: the D axis's §6 concluded the opposite ("an override of nothing"); killed.
- **G-DU6 / GCM-50 (Badge rim mechanism change)** · fr-GalleryCard:480 + fr-GalleryCardModal:494 · **INFO** · mech: at v7.0.0 the Badge's edge changes mechanism — base retires the `border` keyword; `outline` becomes `badge-atom--outline` (box-shadow `--glass-material-rim` + class-level border) and `.badge-atom` adds the rim to EVERY badge — so `.basis-tint`'s bare `border-color` projection re-tints a hairline it did not author and each pill silently gains a 0.5px rim · cure: uplift visual note; narrows D·S-2's "survives the re-cut unchanged" to the prop level · dissent: none.
- **fr-GalleryCard D-20 (`rounded-full` vs `rounded-badge`)** · **INFO** · mech: no dedupe through the pin's `cn()` (built sheet ships `border-radius:3.40282e38px` alongside); inert (both compute a pill); **fixed at 7.0.0's class-names** · cure: rider, no action · dissent: none.
- **fr-ContourPreview rows 23/27/30 + FR-EQC-13 (the uplift-delta ledger)** · **MINOR/INFO** · mech: four silent, import-shapeless changes land inside consumers at the bump — (i) `cartoon-surface`'s hover-lift **disappears** (the 7/8 recipe is three static declarations, no hover/translate/transition — tag-verified), which self-cures the false-affordance rows AND silently strips the affordance from any genuinely interactive cartoon site among the 21; (ii) `role="region"` + `:aria-labelledby` + stable id arrive on CollapsibleContent (a NEW guarantee — 4.0.0's region has no role/name); (iii) `tap-squish` press feedback DROPS from the disclosure trigger; (iv) `.disclosure-content` imposes `color: var(--muted-foreground-strong)` + `--type-small` + inline padding 4px→16px on every collapsible body (inert for SVG, a live re-render for EqCoefficientsPanel + FunctionInput ×2) · cure: an explicit **F.W1 diff-review ledger + before/after screenshot rows**; the shim's own stale comment corrects in the same edit (row 27: live 21/14) · seq: FR-EQC-10's rhythm work sequences AFTER this · dissent: none.
- **FR-CP-5 (disclosure chevron)** · fr-CoefficientsPanel:170 · **MAJOR, discharged-by-uplift** · mech: at the ^4.0.0 pin glass's scoped `transition: transform` cannot cover the `rotate` LONGHAND Tailwind v4 emits for `rotate-180` — the chevron snaps. Cured at 7.0.0 by `@utility transition-disclosure` over `rotate` · cure: consumer no-op; **verify at bump** · dissent: none.
- **FR-CP-13 (the fusing pair)** · fr-CoefficientsPanel:172 · **MAJOR (F.W1 break-risk)** · mech: 7.0.0's adjacent-sibling fusing pair (`configurator/styles.css:126-135`) ASSUMES touching layers; `.viz-panel-left` gaps them 12px — the uplift as-is yields **flat-ended rectangles floating in air**, invisible to keyset diffs and typecheck · cure: **one gap decision made ONCE, jointly with FR-CP-24's hairline row — the uplift wave removes the gap or opts out of fusing IN THE SAME COMMIT** · rider: 7.0.0's body padding widens 12→20px, worsening FR-CP-10's arithmetic and re-tuning FR-CP-20's rhythm · dissent: none.
- **fr-ExportModal M-α/M-β/M-γ (three banked cures invert at the uplift)** · fr-ExportModal:380-382 · **MINOR ×3, sequencing-critical** · mech: **M-α** — `showClose` is GONE from the whole producer tree, folded into `dismiss` (`"deliberate"` = no ✕, Esc + outside kept — exactly the banked D-m7 intent); executed in the banked order the cure fails `vue-tsc` or falls through as a dead DOM attribute with the ✕ back on screen (a silent regression of the exact defect it closes); `spring`/`scrimAnimation` are also gone from `DialogContentProps`. **M-β** — the DialogTitle recipe moves out of `cn()` into an UNLAYERED `:where([data-slot="dialog-title"])` rule that declares **no font-weight**, and preflight resets `h2` to `font-weight: inherit`: `text-lg` goes INERT (a free improvement) while `font-semibold` flips from redundant to the SOLE weight source — **executing the banked "drop the raw utilities" cure after F.W1 ships a 400-weight title**. **M-γ** — `min-width: 300px` inverts from inert to actively harmful: the producer plate becomes `inline-size: min(100% − 2·var(--space-section), 32rem)`, so the 300px floor binds below 340px, halves the 20px gutter guarantee at 320px, and overflows below 300px. · cure/locks: re-spell M-α as `dismiss="deliberate"` **sequenced WITH/AFTER F.W1** (supersedes the banked D-m7 cure cell; the defect row is unchanged); M-β rides the F.W1 ledger + a BH relay note (every 8.0.0 consumer passing no weight inherits 400 where 4.0.0 gave 600); **M-γ's deletion is a sequenced PREREQUISITE of the free improvement, not a tidy-up** · dissent: none.
- **FR-TT-5 / FR-TT-6 / FR-TT-9 / FR-TT-14 / FR-TT-24 (the tooltip seam)** · fr-Tooltip:782-786 · **MAJOR ×3 + INFO ×2** · mech: **FR-TT-5** — `Tooltip.vue:33`'s `:collision-padding="8"` is not merely un-typed at v7.0.0, it is **STRIPPED** (`RETIRED_FLOATING_ATTRS` :28/:30 + the `floatingContentAttrs` filter), `avoidCollisions: true` hardcoded, and reka's `collisionPadding: 0` floor reasserts; at HEAD the filter is gone and `{...attrs}` forwards wholesale. **FR-TT-6** — the chip's padding ratio INVERTS (px-3/py-1.5 = 2.00 → √φ-ladder 0.79), the `text-sm` line-height pairing is dropped, and the type rung demotes (`--tooltip-text: var(--type-caption)`, floor 0.75rem) under a producer comment that mis-names its own backing. **FR-TT-9** — the glass 4.0.0 Tooltip is the ONLY floating surface emitting no `data-glass-dock-portal` stamp, so for the **20 tooltips mounted inside a GlassDock** (Editor ×10 + Anim ×4 + Canvas ×6) moving the pointer onto the hint reads as "the pointer left the dock" and **the dock collapses under the control being inspected**; cured at 8.0.0 (`:43-48`), **v7.0.0 does NOT carry the cure**. **FR-TT-24** — `./icon-tooltip` does not exist at v7.0.0, so **after F.W1 there is no single-component tooltip in the package at all; the local shim is the only possible home** for the `side` + `#content` + provider-non-nesting contract. **FR-TT-14** — the PRM end-state is properly authored at ≥7 (`[data-reveal="tooltip"]`, reveal.css:112): the uplift's clearest design win here. · cure: re-express `collision-padding` as a declared prop (or accept the 0-floor deliberately); budget a **tooltip visual re-baseline**; FR-TT-9 rides the BH relay with its 8.0.0-only cure named · seq: **FR-TT-5 and FR-TT-9 are ONE joint re-pin gate — F.W1 must name its baseline before either can be sized**, and two banked K-1 rulings (fr-FrequencyGraph vs fr-CoefficientsSpectrum) currently disagree about which side governs · dissent: none.

### F · Removed / renamed subpaths and members

- **Census FE §5 / C-4-census break table (the retained-subpath deferral row)** · fr-AnimationControls L-13/K-14 · **MAJOR (identity home)** · mech: the removed-subpath rows are a DEFERRAL, not a live defect (both exports present at the installed pin) — but the table itself is stale in three directions · cure/AMENDMENTS carried: (i) **`./dropdown-menu` exists at 7.0.0 and is GONE at 8.0.0 → `./menu`** (BREAK-8.0 / MISSED-C) — a new row on `AnimationControls.vue:9`, the direct host of all six easing previews, absent from the census; strictly additive (`./metric-badge`/`./hover-card`/`./hover-popover` still absent at 8.0.0); (ii) `./icon-tooltip` is DELETED at 7.0.0 → the prescribed one-word `Tooltip` swap would migrate 35 callsites onto a deleted subpath (R-1); (iii) `./toggle-chip` GONE at 7.0.0 → `./chip` (M-13, FR-COB-21 — the successor must be re-derived at the tag; survivors `./chip`, `./switch`, `./toggle-group`, dock); (iv) `./confirm-dialog` is absent from producer 7.0.0's exports **and from its source tree entirely** — adopting it manufactures a break row (fr-AdminUserList C-M7 kill; fr-GalleryView FR-GV-4's cure is the in-file Dialog pattern); (v) `./checkbox` is ABSENT at 4.0.0 (80 keys) AND v7.0.0 (74 keys), PRESENT only at v8.0.0 — a wave reading "tighten the imports during uplift" finds no subpath until 8.0.0 (FR-GFC-29, L·I-1); (vi) `./scrolling-text` ABSENT at v5/v7 — GalleryMarquee burdens F.W1 for a component that **renders nowhere** (FR-GFC-9); (vii) `LabeledSelect` is REMOVED at 8.0.0 — the FMD-14 Easing leg lands as `LabeledField` + `<Select>`; (viii) `./metric-cell` deleted at v8 (MetricCell.vue gone) · export-key counts of record: **80 at installed 4.0.0 · 74 at the v7.0.0 TAG · 70 at 8.0.0** (tag measures govern; AA-2's "66" and FR-AUL's "67" were live-tree measures at drift) · dissent: none.
- **`.dock-separator` RE-CLASSED OUT of the break table** · fr-CanvasControlsDock K-8/R-10 · **kill, load-bearing** · mech: category error — the class is the live paint source for `DockSeparator` at BOTH pins (installed `layer-group.css:23-62`; producer `dock/index.ts:32` still exports the primitive, compiled to `div.dock-separator`). Nothing breaks at the bump. · cure: **F.W1 must NOT budget it as a version-forced touch**; the real defect (D-6/L-4/C-6) is today-live and belongs to F.W4 · dissent: LC's provenance caveat retired by the producer read.
- **Dock member deletions** · fr-CanvasControlsDock D-12 + fr-EditorControlsDock L-7 · **MAJOR** · mech: three break axes inside 132 lines at CanvasControlsDock (`./hover-popover` dead at the bump; `DockIconButton` ×7 callsites definition-absent; 7 lucide symbols) — and the **migration budget is CALLSITES, not imports**: EditorControlsDock alone carries **12 `<DockIconButton` + 2 `<HoverPopover` + 1 `<MetricBadge` in 118 template lines** — the densest single-file dock-surface cost in the tree · cure: `<DockControl>`/`<DockTrigger>`; `DockControl`'s tri-state `active` (explicit `default: undefined`) replaces class+aria in one binding (D-3, D-8) and ships `aria-disabled`-not-native (D-23) · seq: **the uplift target is a 4→8 hop, not 4→7 — every producer `file:line` in these corpora and the CENSUS/lane rows citing 7.0.0 must be re-keyed before scheduling** · rider (MM-11): cures that RENDER these docks elsewhere (fullscreen toolbar, `canDelete`) **enlarge the uplift transaction** — sequencing rider · dissent: none.
- **D-2/C-2 (the Popover union — primitive-class-change)** · fr-CanvasControlsDock:139 + fr-EditorControlsDock R-5/FOLD · **BLOCKER** · mech: the two view toggles are keyboard-dead by construction — reka's hover-card content strips `tabindex` from every tabbable on mount (unconditional `onMounted`), no focus scope exists, and the trigger's pointer open-path is `excludeTouch`-wrapped. Repeats VERBATIM at `EditorControlsDock:103,:134` — four toggles, two docks, one root cause. The producer names the defect and cures it at the Popover union ("coarse-pointer hover auto-promotes to tap-toggle; reka's `excludeTouch` leaves the hover root structurally dead on touch"). · cure: **the migration row must be marked PRIMITIVE-CLASS-CHANGE — a mechanical rename would re-ship it**; D-7's tooltip-register misuse and C-13's severed dock-token scope both die WITH the union, and must not be patched locally · interim (F.W4 option, at the installed pin): `v-model:open` + a trigger `@click` — survives the migration unchanged · riders: magnetRadius's sole writer is this dock, so **the magnet is keyboard-unreachable APPLICATION-WIDE**; HoverPopover is NOT a bare reka root (it ships a `native` arm, `default: !1`, unpassed here, so the reka arm is live) · relay: whether the dock's token scope should re-project onto owned portals is a producer design question (BH) · dissent: touch legs both engines → SS-13; never carry "touch-dead" unqualified (K-9).
- **FR-AH-13 (the one row the uplift IMPROVES)** · fr-AppHeader:88 · **MAJOR** · mech: the attribution card and both external links are **touch-unreachable** — reka wraps both pointer handlers in `excludeTouch`, glass-ui's HoverCard is a bare pass-through, the links have no other entry point, and the same tap fires `@click.stop="router.push('/paper')"`: touch users get navigation, never attribution (keyboard works via raw `onFocus`, which is why it survives desktop review) · cure: the `<Popover trigger="hover">` swap adapts to coarse pointers — **but MUST pass `trigger="hover"` and resolve the `:64` click collision** · dissent: none.
- **FR-AH-22 / FR-AH-24 (header uplift folds)** · fr-AppHeader:91/:92 · **MINOR ×2** · mech: `./hover-card` present at 4.0.0, removed at 5.0.0 — `^4.0.0` caret holds it, MINOR today / MAJOR the day the range moves (AppHeader is 1 of 2 sites) · the header's boot path transitively imports the forbidden easings fork (census §4 HARD SHADOW), and `useFourierMorph:29` barrel-re-exports `EASING_PRESETS`/`EASING_PRESET_NAMES` + two types (**not** `getEasingFn`) · cure: the fork dies at `./easing` (which ships at ≥7); **cure-completeness (r2 missed #5): the repair must strike `:21-27` AND `:29`** — leaving the re-export keeps the static easings binding and the eager `vendor-math` edge SURVIVES the repair · dissent: none.
- **FR-AUL-10 + FR-AUL-45 (`ToastVariant`)** · fr-AdminUserList:38/:39 · **MAJOR** · mech: `ToastVariant` is definition-absent at glass ≥7 (`grep -rn ToastVariant src/` → empty); `useToast.ts:4` imports the type and AdminUserList alone routes **11** static `toast(...)` sites through it (`:170` an unbounded per-error fan-out). With no vitest and no admin e2e, `vue-tsc` is the only catcher. · cure: rewrite the adapter — **and FOLD FR-AUL-45's tone restoration into the same edit**: `useToast` flattens the five-member `ToastVariant` onto two (`success → default`, `info → default`), so all six success toasts render neutral while the installed Toaster maps distinct `feedback-tone-success/warning/info` classes — the producer's semantic tone ladder is installed and unreachable · seq: **AA-1's toast cure routes through this same file — cure `useToast` FIRST or use an inline banner** (the F.W1 ordering constraint upheld at fr-AdminAuditLog AA-1) · scope note (K-11): the censused break is ONE HOP from ContourSettings' only error channel — the "this component is off the entire named uplift break surface" superlative survives only for its own import block · dissent: FR-AFP-17's success-face cure needs NO uplift (`ToastVariant` includes `'success'` at 4.0.0) — **deferring it to F.W1 is a scheduling error**; map success→"success" now, the adapter rewrite rides F.W1.

### G · The metric family

- **GAB-2 (the metric uplift is not a rename pass)** · fr-GalleryAdminBanner:462 · **MAJOR (DU's BLOCKER dissent recorded)** · mech: `color` is **deleted with NO successor at v7.0.0 AND v8.0.0** (both tag reads); the geometry axis renamed **TWICE** (`labelPosition` → `orientation` at v7 → `posture` at v8) — any wave writing "orientation" off the challenge's table writes a prop that does not exist (K-5); `size` survives at both; strictTemplates is off so the dead props degrade to silent DOM fallthrough, not a typecheck failure. This file is the sole `labelPosition` + `color` combination site. · cure (RULED, K-4): **`<Metric posture="cell">` from `./metric` at 8.0.0** — which also ships `compact?: boolean` (the abbreviating formatter GAB-8 needs) and `loading` → `data-loading` + `aria-busy` (the keep-the-box cure GAB-4 needs). **MetricCell is DELETED at v8; MetricPill is NOT a fallback seat** (GAB-29: installed under `dist/components/ui/metric-pill/`, reachable via the root barrel only, no subpath in the 80-key map, and **no MetricPill source exists at v8.0.0** — recorded as a cure-falsifier so the wave does not adopt it off the name) · seq: **budget from 8.0.0, never the 7.0.0 snapshot** (FMD-11's baseline law) · relay: tier tint needs a home — a tone axis on Metric, or a ruling that tier semantics move to label/glyph (BH ask) · dissent: DU holds BLOCKER; escalates if the F.W1 rehearsal shows silent fallthrough shipping to production rather than dying in the transaction's gate.
- **D·D-M10 (the energy encoding is DESTROYED, not renamed)** · fr-EquationView:363 · **MAJOR** · mech: `MetricBadge :color` has NO successor on ≥7's `Metric` — tag-verified: `MetricProps` = value/unit/placeholder/loading/label/context/class/size/orientation, **no `color`, no `tone`**; `./metric-badge` absent from the 74-key tag keyset; the 4.0.0 d.ts documents `color` ("applied to the value"). Consumer sites `:292-297` + `InfoCard:31-36`. · cure: **re-express the encoding (class → `.metric__value`, or tokens) — design work, not a rename** · seq: rides M-7's prop-level diff; the migration seat is FR-AFP-18's · benefit rider: ≥7 Metric's `loading`/`aria-busy` is the D·D-B4 cure · dissent: none.
- **FR-IC-7 / FR-IC-8 / FR-IC-10 / FR-COB-10 (the metric-badge budget)** · fr-InfoCard:603-605 + fr-CanvasOverlayButton:154 · **MAJOR** · mech: lane-frontend's `./metric-badge` budget "7 imports / 6 files" is **5 live files + 1 unmountable** (FR-COB-10) — corrected again at fr-InfoCard to **7 importing = 6 live + 1 unmountable**; FR-IC-7 re-keys the whole row **4→8**. FR-IC-10's cure is killed twice (child order: label spans emit BEFORE the amount; `orientation`→`posture`; `abbreviation` absent). · sharpened stake (FR-IC-25): the enforcing type gate is **CI, and it runs cold by construction** — `ci.yml:95` and `deploy-pages.yml:114` both run `npx vue-tsc -b --force`, and the tsbuildinfo is gitignored twice, so the TS2307 on `@mkbabb/glass-ui/metric-badge` (`InfoCard.vue:4`) **fails CI AND the Pages deploy**, not merely a local build · seq: rules F8-REACH-01+02 together at F.W0 · dissent: none.
- **FR-AFP-18 / FR-GFC-26 / m-2 / D-27 / FR-MSP-11-mapping (metric consumer sites)** · **MINOR** · mech/cure: the render seat migrates metric-badge→metric at F.W1 (FR-AFP-18); the marquee/carousel count lands **during F.W1 in the `./metric` idiom, never today** (FR-GFC-26); MetricBadge's `<div>` root inside the Button's native `<button>` (phrasing-content violation) is **cured free at the uplift** — v7 `Metric` renders `<span>` (m-2); the BasisCanvas `N = k` hand-drawn readout hoists to DOM via the primitive already positioned over that canvas (D-27, with the D-3 cure); MorphShapePreview maps **3×`Metric` + 1×`Chip.tone`** · dissent: none.

### H · Sliders

- **B-2 (`--slider-scrub-*` DEFINITION-ABSENT)** · fr-BasisSelector:119 · **BLOCKER (fleet-scoped)** · mech: the token family is definition-absent in installed glass-ui 4.0.0 (`grep -rl slider-scrub node_modules/@mkbabb/glass-ui` → EMPTY) — the declared per-basis retint has NEVER painted at this pin; both sliders render the stock `--primary` capsule. **Census of record: 23 dead declarations / 7 files** (BasisSelector 4 · EditorControlsDock 4 · SliderControl 5 · HarmonicLevelGrid 4 · MorphPhaseConfig 4 · GlassTimeline 1 · ConvergenceTimeline 1 — four concordant recounts; "20", "10/5", "21/7", "23/6" all killed). Root cause is a MECHANISM: the uncommitted rename sweep updated **3-of-4 occurrences of the literal `glass-scrubber` and 0-of-4 token declarations** — a string substitution over the variant name; the token contract was never in the search. · **PROVENANCE CORRECTED (fr-SliderControl, LC's inversion — the canonical account)**: v3.1.0's Slider read 13 distinct scrub names **including all five**; `99a11083` (v3.2.0, 2026-06-06) deleted them; **the uncommitted `^3.1.0→^4.0.0` bump crossed that deletion.** So the cause taxonomy is "an unlanded major bump silently severed a once-live contract", not "a prose promise the producer never kept" — which is exactly what makes a **token-FAMILY sweep** the right acceptance shape. (Amendment relayed per E-3: B-2's "has NEVER painted" and fr-ConvergenceTimeline's "never existed" are true at the installed pin and FALSE as history.) · cure (corrected, R-7/K-1): **rename ONLY the range pair to `--slider-range-bg`; DELETE the thumb pair** — no receiver at `variant="standard"` at 4.0.0 (spectrum-only rule, zero-width thumb). Leg (b) ("no `--slider-thumb-bg` at 7.0.0 at all") is **FALSE at the v7.0.0 tag and true only at 8.0.0** (FMD-3 bank correction); the corrected cure stands on leg (a) alone. · **acceptance gate**: `grep -rn -- "--slider-scrub" web/src | wc -l` → **0**, NEVER a variant-string sweep · witnesses: FMD-3 · HLG-3 · MPC-3 · fr-SliderControl R-1 · fr-ContourSettings D-B1 (5 callsites; compounding — the `standard` thumb is invisible-by-design so even a correctly-spelled token would tint nothing) · fr-EquationPanel (SliderControl:143-148 consumer binding) · execution rides F.W3/W4; **the gate is F.W1's** · dissent: none.
- **MPC-10 (the cure-rider that makes B-2's cure sound)** · fr-MorphPhaseConfig:622 · **MAJOR, CURE-RIDER amending B-2/FMD-3** · mech: the banked cure is **not visually neutral** — rename-only ⇒ net α 0.264 ⇒ the fill drops **11.51:1 → 1.51:1** against its own track (dark 5.32 → 1.42): a 1.4.11 REGRESSION on the `standard` recipe's only affordance, shipped under a green token-sweep gate. · cure: drop the 30%/45% wrappers; `--track-color` full-strength; glass's 88% the sole dilution ⇒ **4.004:1 light / 3.71:1 dark PASS** (three independently written pipelines agree) · lock: **B-2's acceptance gains a contrast leg in the breaking direction** · dissent: none.
- **MPC-31 (the ONE-CUT law)** · fr-MorphPhaseConfig:629 · **MINOR, CONJUNCTION rider — F.W1 SEQUENCING LAW** · mech: the corrected cure's entire communicative payoff is **1.203:1, hue-only**, on bars ≤90px — and it lands on the one card whose bar has **zero extent at boot**; cards 1 and 3 are byte-identically `var(--accent-red)`, so the pink Morph bar carries the grid's ENTIRE discrimination content. · **LOCK (verbatim): MPC-3 ⊕ MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22 land as ONE CUT**, and the acceptance witness for the colour scheme is taken at a **non-boot state** (or after MPC-8's re-domain) — a repair whose witness does not exist until the domain row lands with it · seat correction carried: the settle cards boot at 150ms (13.3% extent), so the cured boot render IS visibly changed (two red slivers); what is unchanged is the *information* · members: **MPC-13** (HEAD does not typecheck — `glass-scrubber` ∉ the pinned vocabulary and `build = vue-tsc -b`; the WT one-liner is the cosmetic half; **the flip lands WITH the token cure, never before**) · **MPC-22** (focus tint hard-codes `var(--accent-red)` beside the per-instance `--track-color` — invisible today, a guaranteed two-colour card the moment MPC-3/MPC-10 lands; one token: `var(--track-color, var(--accent-red))`) · **D-14 rider** (`--track-color` has no `var()` fallback; an empty `color` makes the `color-mix()` invalid — **the renamed declarations must land WITH fallbacks**) · dissent: none.
- **MPC-21 / BR-1 (`variant="standard"` — delete, don't rename)** · fr-MorphPhaseConfig:626 + fr-GlassTimeline:574 · **MINOR / repair-value AMENDMENT** · mech: `variant="standard"` is redundant at BOTH baselines — 4.0.0 computes `variant ?? "standard"` with empty variant classes; v8 authors every non-spectrum leg `:not([data-variant="spectrum"])`. **Deleting the prop at all 9 sites is a no-op today and correct at 8.0.0** — strictly better than every axis's rename, and it pre-clears the TS2322 surface. Measurement: 14 raw hits = **9 attribute callsites / 7 files + 5 prose**. · seq: amends FMD-11 and D-5's cure; the banked D·D-2/K-8 budget rows carry this pointer · dissent: reader-δ's MAJOR framing preserved — **if F.W1's charter prices the variant row as a rename, this dissent revives**.
- **K-8 / SR-1 (`SliderVariant` drift)** · fr-ConvergenceTimeline:273 + fr-SliderControl K-1 · **kill + rebaseline bank** · mech: `SliderVariant = "standard" | "spectrum"` at the **v7.0.0 TAG** (types.ts:5) and `"scrubber" | "spectrum"` only at **8.0.0** (types.ts:10; `"scrubber"` enters `9b4c15d8` 2026-08-08, ships v8.0.0 `17a11bc5` 2026-08-09 — two/three days AFTER the challenges). Several corpora read the working tree and labelled it 7.0.0. · cure: **at the census's named 4→7 target the break DOES NOT EXIST**; against 8.0.0 it is real at 9 live sites / 8 files (+14 raw). Banked as a **REBASELINE BANK: re-book if and only if F.W1's target moves to 8.0.0** · seq: **F.W1 must not budget from the 7.0.0 snapshot if it adopts 8** — and must not inject a false row if it adopts 7 · dissent: none.
- **m-21 (`--slider-track-height` is not consumer-tunable)** · fr-BasisSelector:129 + fr-SliderControl R-11 · **MINOR, producer seam** · mech: the library sets it via `.glass-slider[data-size=*][data-v-…]` at (0,3,0) on the SliderRoot itself; a consumer's scoped class is (0,2,0) and **no cascade-layer escape exists (`@layer` count 0)** — SliderControl's 16px (GlassTimeline 24, ConvergenceTimeline 20) stay inert **even after B-2's rename**. The third independent reason the corpus cure was unsound. · cure: **BH relay ask — is track-height meant to be consumer-tunable?**; consumer-side, `size` is the only lever (and it is not forwarded — R-6) · dissent: none.
- **PD-1 (the scrub session's sole opener rides a dead channel)** · fr-GlassTimeline:571 · **MAJOR, F.W1 charter obligation** · mech: `@pointerdown` (`GlassTimeline.vue:73`) is **undeclared** (the Slider emit surface is `"update:modelValue"` + `valueCommit` only) and survives purely by attribute fallthrough across four component boundaries — while `v7.0.0:Slider.vue:69-78` states in terms: *"a Vue `@pointerdown` template binding arrives as `$attrs.onPointerdown` and is DROPPED across the Slot/forwardRef boundary … vue-tsc + units pass; only a real drag catches it. A native listener on the resolved host is immune"* (`:119-124` says the same of `@touchstart`). The producer ABANDONED the channel at the 4.0.0 pin (`useDockHold` attaches native listeners). At the installed pin the binding DOES land. **One of two is true: either the binding drops at the target's reka (the only scrub opener silently dies at F.W1, invisible to vue-tsc) or the producer docblock is false at its own pin (a producer-doc defect owed the relay).** · cure/lock: **binding-verification probe becomes a CHARTER OBLIGATION** (cheapest form: assert `anim.scrubbing === true` mid-drag) — the producer states no static gate can see this class; **cure-shape law amending banked DH-1/K-14/SR-1: native listeners on the resolved host, or an upstream session-pair prop — NEVER template bindings on the forwarding chain** (the banked cure prescribes three MORE such bindings unless re-shaped) · relay: the docblock-vs-render contradiction at reka 2.9.10 is producer information either way · dissent: the disjunction is deliberately unresolved; ruling it statically would repeat the mis-baselining pattern this registry has killed five rows for.
- **MPC-32 (8.0.0 cures the coarse slider target)** · fr-MorphPhaseConfig:629 · **MINOR (benefit ledger)** · mech: at the pin the `standard` slider's coarse-pointer target is the bare track — **22.5px below 768px, 20px above** — and the 44px halo glass composes onto the thumb is **pointer-inert** (`.slider-thumb.touch-hit-area::before{pointer-events:none}`); the package's only coarse floor is a closed three-selector allow-list the slider is not on. **8.0.0 CURES it** (`--slider-target-floor` coarse block, styles.css:85-93; the decorative ghost retired) — an uplift cell no bank row carries · cure: joins the F.W1 benefit ledger · dissent: WCAG 2.5.8's spacing exception plausibly rescues strict conformance; stated against the producer's own floor.

### I · keyframes 4.3 → 6

- **C-13 / R-16 (the engine decision)** · fr-AnimationControls:54 + C-2 · **MINOR (harm booked at C-2) — the engine decision is F.W1's** · mech: two animation engines — keyframes.js drives the secondary morph, a hand-rolled rAF drives the primary transport, **and the transport lost direction doing it**: direction is derived from `cycle` off a re-anchored `startTime`, so every transport verb resumes forward (first tick after any restart has `cycle=0` for all t∈[0,1)); four doors, all owned by one dock (speed watcher, `endScrub`, `setCanvasVisible(true)`, `play`); mid-stroke reversal on the descending leg, trail kinks · cure: **keyframes 4.3 already ships `alternate` + the velocity-continuous interruption contract the transport needs** — excised by the store's own header. The durable seat is the engine decision at F.W1; the store repair (direction becomes state) is F.W4's · seq: F.W4's store repair must not pre-empt the engine choice · dissent: C-13 filed MAJOR; demoted (double-charging one harm across two rows inflates the component).
- **C-11 (scalar-tween adoption)** · fr-BasisCanvas:106 · **MINOR** · mech: `currentScale += diff * 0.12` per **frame** (no clock) — ~2× faster at 120Hz; keyframes.js 4.3.0, a satisfied hard dependency, ships the scalar-tween family incl. `reducedMotionScale` · cure: keyframes substrate adoption (also serves D-8) · dissent: none.
- **fr-App C-11 (the budgeting fact)** · fr-App:75 · **INFO** · mech: the App subtree's sole keyframes edge is AppHeader → DarkModeToggle → `useFourierMorph` → `@mkbabb/keyframes.js` — **inside the atomic tri-package transaction; the persistent header cannot be uplifted independently** · witness: MIN-8 (keyframes inert across the VisualizationView closure; the tree-wide import is `useFourierMorph.ts:14`) · dissent: none.
- **MPC-14 / C/B-2 (keyframes' own value.js floor)** · fr-MorphPhaseConfig:624 · **INFO** · mech: keyframes 4.3.0 **hard-deps** value.js `^0.13.0` — the tri-package transaction is genuinely lockstep; no member lands alone · dissent: none.

### J · value.js 0.13 → 4.0

- **L/B-1 + C/B-1 (the import closure fails)** · fr-EasingPicker:295 · **BLOCKER-fold (census C-7 / lane-frontend:480; 5 bare-root sites)** · mech: value.js 4.0.0 has **no `"."` export** (7 subpaths; main/module/types absent) AND **`timingFunctions` is deleted** (src grep = 0). The picker's own five functions survive at `./easing`; the blocker is inherited from the co-resident morph catalogue. · **CENSUS SHARPENINGS booked here**: (i) the `Result`-shape rewrite for `EASING_PRESETS`; (ii) all 22 morph keys DO resolve through `easing(): Result` at 4.0.0, **but 14 of 22 silently drift analytic → CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo) — the morph curves change shape across the bump, unbooked anywhere else and **needing an explicit acceptance decision, not a silent bump**; (iii) the costing correction — `easings.ts:9` and `:10-16` are two import statements from the byte-identical specifier and `ANIMATION_EASINGS` is a strict content-subset of `EASING_PRESETS` **by object identity** (5/5 `timingFunctions[k] === easeInOut*` true at 0.13.0), so the "5 sites / 6 symbols" costing double-counts and **the named-import block is deletable TODAY at the current pin** · **CORROBORATING ARITHMETIC (MPC-5, thrice-derived)**: 8 of 22 presets materially change curve shape, **max Δ 0.192 `ease-out-circ`**; folds to library-band **RD-5 (RULED RESTORE)**; acceptance re-runs the sampling · **EXECUTION LOCK (FourierMorphSvg r2 rider)**: `easings.ts:58`'s `as EasingFn` cast erases the shape change, so an INCORRECT re-point at `bezierPresets` (4-tuples, not functions) **typechecks clean and throws on the first rAF tick** inside the frame callback — the awaited `play()` never resolves, `phase` sticks at `"settle-out"`, and the `!== "idle"` guard closes the app-wide theme toggle PERMANENTLY. **The F.W1 rewrite must be verified by EXECUTION, not typecheck.** · sequencing correction (CENSUS-MC): `@mkbabb/glass-ui/motion-curves` exists at the PINNED 4.0.0 and value-re-exports every easing symbol `lib/easings.ts` imports bare-root (exception: `timingFunctions`) — **the value.js-specifier half of the migration is NOT blocked on the uplift**; census §1/§3 rows AMENDED · dissent: none.
- **R2-2 (the version-conditional LAW)** · fr-InfoCard:601 · **procedural, binding** · mech: fourier's own recorded `^2.0.0` peer-floor note (2026-07-03) would make the bare-root break not fire at all; **CENSUS-2026-08-03 (a month later, the planning record of record) fixes the target as value 0.13→4.0, one atomic transaction** (`:109`, `:184-186`), and v2.0.0's exports INCLUDE the `.` root while v4.0.0's 7 subpaths do not · cure/LAW: **every version-conditional row names its target version AND the record that fixes it** · dissent: reader-B's "does not fire at all" leans on a superseded interim coordination letter; the date argument governs the fact, the discipline is adopted as law.
- **MG-ι / C-2 (the remedy constraints)** · fr-App:74/:67 · **MINOR/MAJOR** · mech: the cure for the palette blocker is **already installed and unimported** — value.js 0.13.0 ships `parseCSSColor` handling both failing forms (executed: no crash; **the value.js memory ledger's R1 is a tranche-U-head artifact, not a 0.13.0 one**). But "hand the string to value.js" is not a drop-in: `color2(parseCSSColor(x), …)` **throws**, and `color2(parseCSSColor(x).value, …)` **silently returns wrong channels** · cure: `colorUnit2` yes · `color2` never · probe-element used-value read for `light-dark()` · **CROSS-REPO (NO-WAVE-OWNER in the F-lattice)**: the value.js-side defect — `color2`'s dispatch rejecting and mis-reading its own sibling parser's output — is input to the value.js V·π / parser-seam program, flagged for the owner's mail sweep · dissent: DU holds C-2 at BLOCKER.
- **R-18 / C-11 / FR-CP-15 (value.js consumption gap)** · fr-SliderControl:749 + fr-ContourEditorCanvas:221 + fr-CoefficientsPanel:174 · **MAJOR/MINOR** · mech: value.js is a declared dependency the colour seams consume **0%** of — 5 import lines / 4 files, all easing — while `lib/colors.ts` hand-rolls a lossy 117-line parser that cannot read the `oklch()` forms glass ships, and the coefficient subtree hand-rolls the N-stop hue arc **four times in raw sRGB** while `sampleColorRamp` (OKLCH, gamut-mapped, hue-method-aware) ships in the installed dist · cure: adopt the substrate; the `color` prop carries **two incompatible provenance arms** (5× hex snapshot / 3× live `var()`), an undocumented two-contract prop visible the day B-2 is cured · identity: distinct from the peer-range row; the resolver/token seam folds to D-1 · dissent: none.

### K · Peer graph, manifest, lock, toolchain

- **C-1 = C-8 (the peer-graph identity)** · fr-AnimationControls:55 + fr-BasisCanvas:103 · **INFO at component altitude; census §1 pins + G33 at repo altitude** · mech: installed glass-ui 4.0.0 declares `"@mkbabb/value.js": "^0.10.0 || ^0.11.0"` and installed value.js is **0.13.0**, hoisted, single copy; `npm ls` → ELSPROBLEMS. Reproduced **six+ independent times** (fr-AppHeader, fr-CanvasControlsDock C-27, fr-CoefficientsSpectrum C:B-1, fr-ContourSettings C-5, fr-DarkModeToggle C-3, fr-EasingPicker C/B-2, fr-FourierShapeExtractor C-16, fr-GalleryFeaturedCarousel FOLD-2, GM-F9, fr-PaperView C-03, fr-CollapsibleSection F-1, fr-GlassTimeline C-18/C-20). **ONE identity, never re-booked per component.** · riders: the peer is `optional: true` (no npm hard-error), the drift is value.js-only (keyframes 4.3.0 satisfies; `@lucide/vue` installed) · **provenance (F-1(i))**: the committed manifest pins `^3.1.0`/`^2.2.0`/`^0.10.0` — **the ERESOLVE is the uncommitted M.W1a bump; "red today" is true of the working tree and only the working tree** · **the uplift RELOCATES the violation, it does not discharge it** (at ≥7 the peer moves to `^4.0.0`) · gate: G33 `npm ls` → 0 problems · glass ≥7 peer set re-verified: value.js `^4.0.0` / keyframes `^6.0.0` / pencil-boil `^0.11.2` / `@lucide/vue ^1.16.0` — **the atomic tri-package transaction stands confirmed** · dissent: none.
- **MISS-LC2 (a REQUIRED peer filed NOWHERE)** · fr-PaperSearchInput:689 · **MAJOR** · mech: glass-ui 4.0.0 declares `@lucide/vue ^1.16.0` a **REQUIRED** peer (first member; `peerDependenciesMeta` does NOT mark it optional); the lock RECORDS that requirement inside the glass-ui entry yet contains **no `node_modules/@lucide/vue` package entry**; on disk sits **1.20.0 — unlocked, unmanifested** (absent from both dep sections). glass-ui's `createLucideIcon` chunk is reached from dock/select/configurator/dropdown-menu/collapsible/tabs — subpaths this app imports — so **a faithful `npm ci` yields module-eval throws** one hop from PaperSearchInput:3-4. **The audited tree is not reproducible from its own lockfile.** · cure: **F.W0** (substrate reproducibility, beside MISS-A7) **+ F.W1** (the manifest lands with the uplift) · rider (fr-AppHeader): `@lucide/vue@1.20.0` is installed **alongside** `lucide-vue-next@1.0.0` — **the F.W1 rename is reconciling an already-installed peer, not adopting a new one** · dissent: none.
- **C-3 (runtime peers filed as dead devDeps)** · fr-PaperSearchInput:685 · **MAJOR** · mech: `cva`/`clsx`/`reka-ui` are LIVE glass-ui runtime peers on the Button import chain (button chunk → cn → clsx) while `lane-frontend.md:69` / `DESIGN.md:32` book them for deletion as "dead devDeps"; all four plus `lucide-vue-next` are `"dev": true` in `package-lock.json`, so **`npm ci --omit=dev` fails TODAY**. `tailwind-merge` alone is genuinely dead (doc-comment-only). · cure: manifest section moves + the census/lane-doc contradiction correction · witnesses: FR-AH-33 (F.W0 manifest hygiene, rename F.W1) · fr-BasisSelector m-9 · fr-CollapsibleSection F-2 · fr-ContourSettings C-13 · GCM-53 (5 runtime-imported packages as devDeps; the census pin row audits `dependencies` only) · fr-GalleryFeaturedCarousel FR-GFC-21 · PSM-33 · C-8 · fr-PaperView C-14 · fr-ExportModal L §X-A (devDeps census correction: cva 8 / clsx 1 / tailwind-merge 0) · **countervailing lock (FMD-21)**: `class-variance-authority` is **NOT a removable dead devDep** — 18 dist files import it and `slider/index.d.ts:1` is a **type-level hard edge** reachable from MorphPhaseConfig.vue:73, so removing it breaks `vue-tsc -b`; the census's measurement (0 src imports) is true, its disposition is false — **the uplift budget must carry CVA until the type edge dies with the 8.0.0 slider** · dissent: none.
- **FR-EQC-7 (the vaul-vue lock gate)** · fr-EqCoefficientsPanel:325 · **MAJOR (F.W1-commit-conditional)** · mech: the uncommitted M.W1a lock regeneration **dropped `vaul-vue`** — a REQUIRED glass-ui peer statically imported by the root barrel (`dist/glass-ui.js:60`) reachable through 7 barrel edges including `router/index.ts:2` (every route's eager graph) — while the **committed lock carries `vaul-vue@0.4.1`** and `web/node_modules` retains it. **Today's CI is GREEN; the failure fires the day the bump commits the regenerated lock**: `npm ci` → build fails on the unresolved import, everywhere. · cure: **an F.W0 manifest gate INSIDE the F.W1 transaction** — declare + lock `vaul-vue`, or land the census's barrel→subpath edge retirement first (`./collapsible` pulls no vaul-vue) · riders: `@lucide/vue` is NOT a resolution edge at the pin (lucide inlined in the dist); the other lock-absent names (embla, perfect-freehand) are **optional** peers · dissent: the reader's BLOCKER-severity endorsement re-opens only if the bump commits the lock unrepaired — at which point it is F.W1's gate failing.
- **PP-REDGATE (the RED gate belongs to the uplift)** · fr-PathPreview:732 · **MAJOR → F.W1/W2 + M-25 ERRATA** · mech: the TS2882 on **committed** `PaperView.vue:12` (`@mkbabb/latex-paper/theme` side-effect import) is real and thrice-reproduced — but **committed substrate + committed lock = GREEN gate**; the uncommitted **TS 6 bump flips `noUncheckedSideEffectImports` default false→true**. The offending import is COMMITTED and four months old. · cure: an ambient `declare module "@mkbabb/latex-paper/theme"` in `env.d.ts`, or a latex-paper types fix — **lands WITH the toolchain bump, owned by F.W1/W2, NOT as an F.W0 precondition** · **ERRATA (fold-forward; dated evidence unedited)**: banked **fr-AnimationControls M-15**'s routing ("F.W0 — substrate green before F.W1 opens") **must be re-read against this finding at intake** · dissent: L-2's BLOCKER framing and one reader's F.W0 confirmation both die on the receipts.
- **PAW-33 / C-19 / MF-12 / C-15+C-16 / C-13 (the manifest pass)** · fr-PaperArticleWindow:659/:668 + fr-PaperView:721/:724/:725 · **INFO/MINOR** · mech: peer ranges silently violated with no npm signal (all `optional: true`) — katex `^0.16` vs installed **0.17.0**; vite `^6||^7` vs **8.0.16**; latex-paper peers katex `^0.16`; dual ambient type sources for katex (`@types/katex ^0.16.8` beside katex 0.17's own shipped types, no `types` array, `skipLibCheck` masking); `vendor-math` **welds value.js (145,809 B, zero paper imports) to katex** and `vendor-ui` names a barrel no site imports; one producer type reached by three specifier spellings · cure: an F.W1 **pin-truth row + manifest pass**; decide the chunk split with a FRESH build (stale `web/dist` inadmissible) · seq: **the tree is in motion — re-snapshot the pin-truth row at repair time**; the uncommitted manifest now DECLARES the violations and the uplift is wider than any round recorded · rider (FR-AH-7): the header alone forces `vendor-math` (**348,707 B**) eager via App → AppHeader → DarkModeToggle:20 → useFourierMorph:21-27 → easings.ts:9,16 → value.js — **the fork itself dies at `./easing`** · dissent: none.
- **C-13 (pencil-boil ships raw TS)** · fr-FourierShapeExtractor:422 · **MINOR** · mech: pencil-boil 0.4.1 ships raw TS (`exports: {".": "./src/index.ts"}`), so **5 dependency sources enter the app's strict typecheck**; `skipLibCheck` covers `.d.ts` only. Resolved by the 0.12.0 bump (real dist). · dissent: none.

### L · pencil-boil

- **M-2 (the floor is `^0.9.2`, not `^0.11.2`)** · fr-SvgFilters:775 · **MAJOR (registry correction of record)** · mech: at the **v7.0.0** baseline glass-ui optional-peers pencil-boil **`^0.9.2`**; `^0.11.2` is true at 8.0.0 and FALSE at 7.0.0 — and **the adjudicated registry itself carries the wrong figure in three places**: fr-FourierShapeExtractor C-18.2 (asserts 7.0.0 peers `^0.11.2` — confirmed HEAD and back-projected), FM-22 ("the F.W1 pencil-boil `0.4.1 → ^0.11.2` migration surface"), while kf-HeroAurora transcribes an installed 7.0.0's `^0.9.2` correctly. **Three banked rows, one contradiction.** · cure: **correct the target to the floor at the ADOPTED tag** (G1 decides which); both cures the corpus wants — guarded resume + reactive PRM — land at **v0.9.2**, signature unchanged, so the bump is a **no-break at this site** and every "7 minors"-derived risk weight overstates · **this row is the correction of record; dated evidence is never rewritten** · dissent: none — but note the task charter's `^0.11.2` figure is the 8.0.0 fact and is correct only if G1 adopts 8.
- **C-18.2 (the 0.12.0 exclusion)** · fr-FourierShapeExtractor:425 · **MINOR, coordination blocker** · mech: glass-ui at 7.0.0 **and** 8.0.0 peers pencil-boil `^0.11.2`, which **EXCLUDES producer 0.12.0** (`^0.x` = `>=0.11.2 <0.12.0`) — the sharpest un-banked coordination item; **it blocks L-B3's bump premise** · cure: a producer-side range widening or a pinned 0.11.x; rides the pencil-boil relay · dissent: none.
- **L-2 (the empty-loop resume)** · fr-SvgFilters:770 · **MAJOR** · mech: pencil-boil 0.4.1's scheduler resurrects an **empty rAF loop on tab-return whenever the subscriber set is empty** — reachable today ONLY for PRM users (the a11y affordance is the entry condition), and **after the booked SvgFilters deletion, for EVERY user**, because the `visibilitychange` listener is a module-scope side effect behind a **false `sideEffects: false` manifest**, still evaluated by the two surviving barrel imports · cure: **the bump to the corrected floor cures both mechanisms upstream** (guarded resume + reactive PRM MQL listener, both tag-verified) · **RIDER (blast-radius correction)**: deleting SvgFilters does **NOT** discharge this row — the challenge-L §7 and challenge-C blast-radius claims are corrected here; one line in the F.W1 relay to the pencil-boil producer about the false manifest · dissent: none.
- **L-i3 / C-18.1 (census count)** · fr-FourierShapeExtractor:423 · **INFO** · mech: pencil-boil = **3 import sites / 3 files**; the "4" was a loose-grep comment artifact at `SvgFilters.vue:19` · cure: correct `lane-frontend.md:481` · dissent: none.

### M · The lucide rename

- **census R3-10 / `:105` fleet row (`lucide-vue-next` → `@lucide/vue`)** · fr-CoefficientsPanel FR-CP-42 (fold home) · **MECHANICAL, F.W1** · mech/figure of record: **35 import sites** (`grep -rn "lucide-vue-next" src/ | wc -l` → 35; fr-InfoCard's re-grep: 35 hits / 35 files); glass ≥7 peers `@lucide/vue ^1.16.0` · member sites enumerated across the bank: FR-AUL-53 (8 imports) · fr-BasisSelector m-9 · fr-CollapsibleSection `:4` (ContourPreview row 37) · fr-ContourSettings `:20` (4 symbols) · fr-CanvasControlsDock (7 symbols) · fr-EquationView `:11` · fr-GalleryFeaturedCarousel `:4` · fr-MobileFloatingToc (1) · PSM-33 · C-8 (4 symbols) · PAW-43 · fr-PaperSearch (2 files) · fr-PaperView (6 sites) · fr-ImageUpload (3) · **BUDGET CORRECTION (FR-IC-26)**: the 35-site budget is **import-keyed and blind to 17 hand-inlined `<svg>`s across 12 `.vue` files** — one of them element-identical to lucide's `Info` at `EquationView.vue:277-279` (12 lines below that file's own `import { Info }` and 23 above its `<Info>` render); a DELETE branch still leaves 17 unmigrated hand glyphs · **BUDGET REDUCTION (D-11/L-7/C-8/C-26)**: the FA path literals are byte-identical pairs at **6 sites / 2 files** across four un-tokenised sizes — the lucide swap **shrinks** the rename surface · **CENSUS DISCREPANCY FLAGGED**: the task charter's "21 sites / 14 files, corrected" is the **`.cartoon-card` shim census** (FR-EQC-3 / AA-15 K-13 / FR-IC-19), not the lucide figure — carried as two distinct numbers, never merged · dissent: none.
- **D·D-M11 (sweep amendment)** · fr-EquationView:367 · **MINOR** · mech: `:11` imports lucide `Info` (used `:300`) while `:277-279` hand-inlines the byte-identical glyph — **the copy the sweep will miss** · cure: +1 inline site on the sweep · dissent: none.

### N · Token parity

- **M-α6 (`--ease-apple-spring` does not exist)** · fr-BasisCanvas:107 · **MINOR** · mech: `--duration-mid` does not exist in the installed producer (enumeration: instant/fast/normal/panel/shimmer/shimmer-fast/slow/xl/xxl) nor locally, and **`--ease-apple-spring` likewise does not exist** (the producer ships `--ease-apple` and `--ease-spring*`) — yet it is consumed in **five files** (VisualizationView:426, GallerySearchBar:188-189, GalleryCard:196/:295, AppHeader:280), several under "A.W3.d — bezier→canonical token" comments **asserting it is canonical**; with no fallback the whole declaration is invalid at computed-value time (IACVT) · **per-site consequence (fr-GalleryCard R-4, the additive part)**: at GalleryCard the ONE `transition` shorthand carries all three animated properties, so IACVT kills hover-lift, press-squish, tier-glow and selection-ring transitions **and** `like-bounce` (`animation-name:none`; the PRM guard is vacuous). Census 5 declaration sites / 4 files. · **IMPROVE-1 (executable at the pin, no transaction)**: `--spring-bouncy` IS defined at installed 4.0.0 (`scheme-motion.css:184`, full `linear()` stop-set) and the producer's excision note names it the successor register — **a two-token substitution at GallerySearchBar:188-189 restores the enter arm today** · dissent: DU holds MAJOR at GalleryCard; the banked MINOR governs; both BLOCKER inflations killed (K-6, K-11 — nothing functional, informational or reachable changes).
- **m-3 / F-3 (`--font-serif-math` declared nowhere)** · fr-BasisSelector:126 + fr-CollapsibleSection:207 · **MINOR (banked regrade governs)** · mech: `.cm-serif` is inert — `--font-serif-math` is defined NOWHERE in either tree (verified at 4.0.0, v7.0.0, v8.0.0), so `var(--font-serif-math, serif)` resolves **deterministically** to generic `serif` while six hand-tuned optical constants substitute for the shipped `fourier-f` recipe five siblings already use. Census **26 occurrences / 17 files**. The consumers fall to UA generic serif inside an app whose `--font-sans` is remapped onto Computer Modern. · cure: **define or retire `--font-serif-math`** (token parity) + the standing glass BH relay for the undeclared producer variable; note the register difference — `fourier-f` binds `--font-display`, not the serif-math face, so adoption is a design decision, not a swap · witnesses: fr-GalleryDraftsSection F-3 · FR-NP-12 · fr-EquationPanel (NotationPills:27) · dissent: DU/one reader held MAJOR; **a banked regrade governs its identity**.
- **FR-COB-24 (the root-font fork)** · fr-CanvasOverlayButton:159 · **INFO, token-parity note** · mech: fourier's root-font fork (`style.css:40-49`, `html{font-size:1.125rem}` under 768px) **inflates every rem-denominated glass-ui control token 12.5%** and compounds with coarse `--ui-scale: 1.5` → **67.5px, not the 60px the token author computed**; the ≥7 ladder is re-tuned against a 16px root · dissent: none.
- **D-18/C-13 · D-20 · D-26 (substrate + spelling)** · fr-BasisCanvas:104 + fr-MorphShapePreview:638/:639 · **MINOR ×3** · mech: the 300° full-saturation constant-L rainbow is theme-blind and bypasses BOTH installed colour substrates (value.js `mixColorsN`/`sampleColorRamp`; glass `cssToOklch`/`oklchStopToHex`) — a substrate-adoption question for F.W1 with an F.W3/W4 execution rider · hand-rolled `font-family: var(--font-mono)` where the producer ships `@utility fira-code` (family + liga/calt) and BOTH siblings use it — one-class swap · two spellings of the ring token in one repo (`var(--ring)` at style.css:140 vs `var(--color-ring)` at DarkModeToggle:99, the Tailwind bridge alias), both resolving — **pick one spelling in the ring sweep** · dissent: none.

### O · Rows F.W1 owns by its own records' routing law (re-ink / tone / geometry that the uplift carries)

- **FR-AFP-3 + FR-AFP-46 + FR-AFP-61 (the light-mode re-ink, with its split cure)** · fr-AdminFlaggedPanel:46/:29/:30 · **BLOCKER-adjacent MAJOR** · mech: the panel is unshipped in light mode — the shipped fallback DEFAULT theme (`index.html:21-32`): hardcoded Tailwind reds (`:166/:172/:176/:191`, zero `dark:`, zero tokens) measure ~1.3–2.6:1 against the resolved light tokens (pill ≈1.31–1.39), and the card outline fails 1.4.11 in **both** themes (1.33 light / 1.20 dark) · cure + **RIDER LOCK**: re-ink on `--destructive`/tone + `--border`, **with FR-AFP-46's split** — `--destructive` ≈4.38:1 on the composited light card passes 1.4.11 and **fails 1.4.3 for the 10px text**, so the cure must split (token fill/glyph · strong-rung text · or the Badge's owned pairing) · **CORRECTION (FR-AFP-61)**: `<Badge variant="destructive" size="sm">` is installed TODAY at the pin (both `./badge` and `./card` exported) — **the worst-contrast row needs no uplift; booking that cure under F.W1 is a scheduling error**; the tone re-grammar is F.W1's, the shadow retirement is F.W3's · dissent: β holds MAJOR (ruled per ruling 7).
- **FR-AFP-19 / FR-AFP-20 / FR-AFP-27 / FR-AFP-44 / M-6 (rest-state tone + the alpha-mute anti-pattern)** · fr-AdminFlaggedPanel:22/:23/:24/:28 + fr-GalleryDraftsSection:510 · **MAJOR/MINOR** · mech: destructive intent is colour-on-hover only (all three actions rest at `text-muted-foreground`, Delete 4px from a confirm-less Dismiss) — **the missing thing is SEMANTIC differentiation, not the indicator** (the CVA base's `focus-ring` + forced-colors outline exist; touch targets are 44px via the coarse floor, so the 28px figure is fine-pointer-only) · `opacity-60` on `text-muted-foreground` fails AA in both themes (3.38 dark / 2.34 light) on the provenance line — **the alpha-mute is the exact anti-pattern `--muted-foreground-strong` was minted to prevent, per the token's own comment**; the empty state (production's only state) applies it harder at `opacity-30` (≈1.48/1.70) · `border-l border-muted` at 1.13/1.01:1 — a FILL token painted as a border; **even `--border` sits under 3:1 on this fill, so the cure exceeds a token swap** · the primary verb rests at the muted rung on a no-fill plate, the repo's own critique audit already having booked it (raw-findings.json:1874) · cure: the strong rung, by-colour not by-alpha; rest-state tone at F.W1, focus-visible + row-state vocabulary at F.W3/W4 · dissent: none.
- **FR-AFP-28 / FR-AUL-19 (the icon-geometry ladder)** · fr-AdminFlaggedPanel:25 · **MINOR** · mech: three icon-in-button geometries across three sibling tabs (0.571/0.500/0.583) and a byte-identical pill recipe in two reds (red-300 here `:176`, red-400 AdminUserList `:376`); **every `h-*` is an override against `size="icon"`** · cure: **collapses FREE with the `iconOnly` re-grammar** — no separate work · dissent: none.
- **FR-AFP-37 (the dialog preset)** · fr-AdminFlaggedPanel:27 · **MINOR** · mech: a hand-rolled destructive confirm while the pin exports `./confirm-dialog` (0 imports repo-wide; AdminUserList hand-rolls a second); the hand-roll omits `:show-close="false"` so a stray ✕ rides beside Cancel. **At ≥7 `showClose` is GONE — the spelling is `dismiss="deliberate"`** · cure: a Dialog preset with the `dismiss` guard + `loading` · seq: **`./confirm-dialog` is absent at 7.0.0 AND 8.0.0 and gone from the producer source tree** — adopting it manufactures a break row (fr-AdminUserList C-M7 kill); the cure is the in-file Dialog pattern · dissent: none.
- **FR-AFP-16 / M-4 / GCM-13 / FR-USB-7 / FR-USB-18 / FR-EQR-13 (the `loading` prop budget)** · **MINOR riders** · mech: mutations without in-flight guards, `:disabled`, or busy affordance across the admin/gallery/auth surfaces; the installed Button has **no `loading`**; ≥7 ships it (`loading` → `data-loading` + `aria-busy` + activation suppression) · cure: the guards land at F.W3/W4; **the one-word primitive lands WITH F.W1** — and the prop widening `boolean → string | null` that a per-row busy state needs is **unbudgeted** (fr-GalleryDraftsSection M-4) · dissent: none.
- **fr-PaperSearch / fr-PaperSearchDropdown / fr-PaperSearchInput token re-inks** · fr-PaperSearch:671 + D:B-3 + D-M1 + MISS-A6 · **MAJOR/MINOR** · mech: eight `color-mix(in srgb, var(--muted-foreground) N%, transparent)` dilutions of an AA-annotated token (the producer's own written refusal of the idiom sits beside it: "`--muted-foreground-strong` … By-colour neutral step, not alpha"); placeholder **1.888:1** / glyph 2.04 / action glyph 1.89 at the authored 45%-dilution recipes; the ≈1.71:1 focus-substitute inversion · cure: **F.W1 token re-ink**, with the F.W3/W4 repair-hazard gate — **the C-1 relocation ARMS these** (same-commit rider) · dissent: BLOCKER re-filings killed (K4).
- **FR-USB-3 / FR-USB-35 / FR-COB-13 / FR-MSP-5 / FR-EMT-14 (type + geometry hop benefits)** · **INFO/MINOR** · mech: five host-level `size-N` overrides are INERT, defeated by the CVA's own `h/w-(--control-h-*)` — every button is 2–3× its authored size (40px desktop / 45px mobile-fine / **67.5px coarse**), and **F.W1 deletes `size="icon"` as a type error anyway** · no `type="button"` is pinned anywhere; the installed Button supplies no fallback and Primitive injects none; **cured upstream at ≥7 (`type ?? "button"`)** — a hop benefit · **SCOPE CORRECTION (FR-MSP-5)**: the credit pays only at `<Button>` callsites; the two morph-family native sites were retired-with-rationale from the A.W3 migration, so **six of seven live native sites remain type-less after F.W1** · latent throughout (`grep "<form" web/src` → 0) · dissent: none.
- **FR-COB-16 / D-13 / GAB-5 / MPC-16 / FR-EQC-5 (free cures the uplift lands)** · **INFO credit rows** · mech: ≥7 ships `loading` where the tree conflates in-flight with on (`CanvasControlsDock.vue:71 is-active: publishing`) · the Button press-scale currently leaks past `prefers-reduced-motion` (unlayered emitted utility beats the `@layer components` reset; a11y-overrides strips `scale` from `transition-property`, so the leak is an INSTANTANEOUS scale) and is cured at ≥7 by `useLiquidPress` · the six inert counters wearing the full interactive costume (`metric-badge cursor-pointer` + hover 1.02 / active 0.96 / focus ring, with no `@click`/`tabindex`/`role`) are cured free (no cursor/hover/press in the ≥7 metric family) · `cartoon-surface`'s hover-lift on non-control containers self-cures **and silently strips the affordance from any genuinely interactive cartoon site among the 21** — the converse is the FM-2 defect class · **SEQUENCING (fr-FourierMorphSvg ruling 4)**: F.W4 tickets written against a lift that will not exist post-uplift **must be sequenced AFTER F.W1 or dropped**; the `/morph` PRM ticket's "THREE ungated motion layers" becomes **two** at F.W1 · dissent: none.

### P · Explicit non-credit, negatives, and scope certifications

- **FR-GIG-5 (NON-CREDIT LOCK)** · fr-GalleryInfiniteGrid:530 · **MAJOR, cure-integrity class** · mech: the census's [P0] tri-package bump does **NOT** cure the whole-collection pagination drain: v7.0.0 fixes `check()`'s trigger (`useInfiniteScroll.ts:46-48` → teardown + fresh observer + observe) and leaves the **observer-root geometry byte-identical** (`:33 root: options.scrollContainer?.value ?? null`; `InfiniteScroll.vue:39` still binds its own no-overflow `<div ref="scrollContainer">`; `:57-66` re-arms on every isLoading false edge). A fresh IntersectionObserver always delivers an initial record; with a non-clipped element root the root rectangle is the element's own box ⇒ `isIntersecting: true` ⇒ `onLoadMore`. **The loop survives 4.0.0 → 7.0.0 → 8.0.0.** · cure: a **producer change** on the glass BH relay · **LOCK: the F.W1 spec MUST carry an explicit footnote — the uplift transaction is NOT credited with curing the drain**; booking it against F.W1 would close the wave green on a live runaway · dissent: three BLOCKER dissents on the drain's own severity preserved at the banked record.
- **PAW-1 / PAW-37 / PAW-51 (the transaction as chartered is INSUFFICIENT)** · fr-PaperArticleWindow:658/:660/:666 · **BLOCKER** · mech: latex-paper's `theme.css` declares a **shadcn bare-triplet** token contract while glass-ui ships **complete colours** (`--card: hsl(36 48% 97%)`, `--border: var(--neutral-4)`, `--primary: hsl(24 10% 10%)`) and fourier defines zero triplets — so all **42** `hsl(var(--x))` declarations are IACVT-dropped: sticky chapter/sub headers ship with **no background**, figure borders vanish, figcaption falls to body ink, proof rails/tints and the error placeholder die, `.section-body`'s tone goes, every `.paper-ref` colour+underline goes. **Version-INVARIANT: the token shapes were verified at glass-ui 4.0.0, v7.0.0 AND v8.0.0 — the tri-package uplift cures NOTHING here**, and **no disposition on this record flips on the 7-vs-8 decision** (the single most valuable F.W1 planning fact in the corpus). PAW-51 extends it: the theorem taxonomy is colour-erased across **92 blocks** — the rail does not disappear, it ships in the **WRONG** colour (var-free `border-left-width/style` survive; all three variant `border-left-color` rules and all three `.theorem-label` colour rules IACVT → `currentcolor` → body ink), collapsing a three-way semantic system to one uniform rail (light: 36 of 92 accidentally near-correct, 56 visibly wrong; dark: 92 of 92 wrong). · cure: **add the LATEX-PAPER carry to the transaction or seat it explicitly** — latex-paper is a FOURTH package the constellation must reconcile; the sound limb is rewriting the 42 sites to `var(--x)` (the consumer-alias limb is KILLED, K-9) · seq: **PAW-30 must land in the SAME repair**; PAW-44 restores the sticky stack **only WITH the background cure (LAW-3)** — restoring it alone makes the killed catastrophe real · witness set: one `theorem` + one `definition` + one `aside` side by side in BOTH schemes (a proof-block or figure witness **cannot** show it — those arms go ABSENT, this one goes WRONG) · dissent: none.
- **FR-FG-20 (zero break surface at 4→7)** · fr-FrequencyGraph:435 · **INFO certification** · mech: FrequencyGraph's import surface is `vue` + one local type; `fira-code`, `--z-controls`/`--z-tooltip`, `.scrollbar-thin` and `text-admin-label` **ALL survive at the v7.0.0 tag** — the correct census statement is STRONGER than the D axis's, and D's recommended "CENSUS §5 typography addendum" dies with it · seq: **if F.W1 retargets 8.0.0 the `text-admin-label` break RETURNS** — an owner/formation decision carried in the K-1 relay, not a measurement · dissent: none.
- **D:U-2 / PSM-36 / PP-#7 / S-3 / PAW-37 (measured zeros)** · fr-ImageUpload:594 + fr-PaperSearchModal:699 + fr-PathPreview:737 + fr-ConvergenceLegend:255 · **INFO** · mech: ImageUpload imports zero glass-ui symbols (break surface NIL at 4.0.0, 7.0.0 or 8.0.0; exposure = 3 lucide symbols + the cartoon-card shim) · PaperSearchModal's only glass import is `./button` (survives) — sole exposure = the lucide rename · PathPreview is nil on **every** F.W1 census row (no glass-ui, no lucide, no pencil-boil, no ToastVariant; one import total) — of ~51 files carrying live glass-ui specifiers, **this is one F.W1 need not open** · ConvergenceLegend's glass coupling is one class (`.glass-wash`), live at BOTH ends — **the break surface costs it zero lines** · cure: record the negatives so no wave budgets them · dissent: none.
- **fr-VisualizationView L-13/MAJ-3/MAJ-4 (the uplift's leverage)** · fr-VisualizationView:803 · **MAJOR composite** · mech: the Configurator ships `asideWidth` + the documented cascade pair; the host hand-rolls the track list at three breakpoints (`:317-330`), converts the component off grid (`:334`), and patches producer-internal `.configurator-stage` via `:deep()` (`:344-347`). **The override wins because Vue injects `<style scoped>` UNLAYERED and unlayered author declarations outrank `@layer utilities` at ANY specificity — not by specificity** (challenge-C corrected; conclusion sturdier). · cure: **the uplift DELETES ~14 lines of workaround CSS** — reason the deletion from the true cascade · seq: F.W1 + F.W3/W4 joint; two standing glass BH relays ride · dissent: none.
- **FR-EQC-2 (inaction is a widening)** · fr-EqCoefficientsPanel:322 · **MAJOR** · mech: one heading, two routes, two type systems — `CollapsibleSection.vue:39` sets the title in `text-sm` (14px, off the glass ladder) while the twin's ConfiguratorLayer header rides `--configurator-section-size` = 20.4px at the pin and **25.9px at the producer** (`var(--type-heading)`): **1.46× today, 1.85× after F.W1 — inaction is a widening**; post-uplift the panel's type is 100% off-ladder (six Tailwind defaults + the one rung the producer deletes) · cure: F.W3/W4 joins the section register, but the wave must take the inventory or D/M-1's cure fixes one seventh · dissent: none.

### Q · Owner-ruling escalations flagged INLINE (never presumed)

- **ESC-1 · the F.W1 target tag** — 7.0.0 (the census's named transaction) vs 8.0.0 (the producer's shipped reality, v8.0.0 tagged 2026-08-09). Not decidable statically. Governs: G5's `text-admin-label` roster, seven fr-GalleryCardModal dispositions, FR-TT-5/-9, fr-SliderControl K-1's rebaseline bank, GCM-22, FR-FG-20's certification, the pencil-boil floor (M-2), the Slider-variant budget, and the dropdown-menu→menu row. Every routing in this table is authored to survive either answer; several sharpen under 8.
- **ESC-2 · ToC model ownership** (fr-PaperSidebar L-4) — both producers export `useSidebarFollow`; the app picks latex-paper's follow + glass-ui's expand with **nothing choosing a winner**; 8 of 10 `useSidebarState` members unused; the arbitration point is an untyped `defineExpose` seam (M6). **OWNER-RULING escalation at F.W1**, then F.W4 executes the collapse (delete the `paperTree.ts` adapter + the losing index). Prerequisite: L-5(a)'s `CollapsibleTrigger as-child` restructure.
- **ESC-3 · the invisible-picker design** (fr-SpeedSelect) — the producer has RULED against edgeless triggers at 8.0.0; fourier's desktop design is now contra-producer. **A design decision, not a mechanical port.**
- **ESC-4 · easing semantic drift acceptance** — 14 of 22 morph keys drift analytic→CubicBezier (fr-EasingPicker CC-2); 8 of 22 change shape materially, max Δ 0.192 (MPC-5). **Needs an explicit acceptance decision, not a silent bump.**
- **ESC-5 · B-2's regrade price** (fr-App dissent 2) — the `.paper-texture` MAJOR is priced on the P0 census gate existing. **If G5 is not adopted, the BLOCKER regrade must be revisited.**
- **ESC-6 · browser-support band** (FR-MSP-12 → FR-COB-26) — which engines in this deployment's audience fail `@supports (color:color-mix(in lab,red,red))`. `color-mix` is Baseline-2023; the failing band's size separates MINOR from INFO and **is the F.W1 browser-support decision's input** (the generated non-`color-mix` fallback paints accent-on-accent at **1.00:1**, provably worse than the counterfactual's 4.43/3.76 light · 4.74/5.58 dark).

### R · NO-WAVE-OWNER packets this wave consumes

- **NWO-1 · FR-COB-28's consolidated glass-ui BH relay letter** (one letter, sent at F.W0/F.W1 open by whoever holds the mail seat; **this record is the packet**) — assembled roster: **FR-NP-32 at the TOP** (the corrupt-dist emitter fix + a producer gate that PARSES its own published `exports["./styles"]` — explicitly **NOT a version bump**: no v8 dist exists to read, local dist is a 1,514-byte stub, dist untracked at every tag) · FR-COB-7 (dead border arms fleet-wide + pressed-suppresses-feedback) · FR-COB-8 (unlayered-P9-defeats-layered-a11y-resets CLASS) · FR-COB-6 (was the ≥7 pressed-paint removal intentional?) · FR-COB-23 (the 1.94:1 focus ring) · FR-COB-26 + **FR-MSP-12 as item 8** (the generated color-mix fallback, now with the class's first provable counterfactual) · the `btn-glass`/`glass-btn` one-letter-order naming hazard · (item 7 record DOCK-ACTIVE is satisfied upstream — **do not re-send**) · plus: **M-1** (4.0.0's `paper-grain-overlay`/`paper-underpaint` paint flat opaque black — decode + algebra) · **m-17** (glass's `cn` is clsx + a fixed regex group table that misclassifies the producer's OWN vocabulary — `text-dropdown` → text-color, `rounded-pill` → no group; **every "just override the class" cure written against the uplift is silently mis-priced**, the winner becomes CSS emission order) · **m-21** (`--slider-track-height` consumer-tunability) · **FR-EQR-7** (the light-arm `--success` rung fails its own card at **2.175:1**) · **GAB-2(a)** (tier-tint home) + GAB's 3.26:1 label rung · **B-4's i-4** (MIGRATION.md:738-739 still homes AnimatedDigit symbols at the dead subpath) · **FR-TT-9** (the unstamped tooltip collapses its own dock; cured 8.0.0) + **FR-TT-6** (the offsets.css:78-82 prose defect) · **PD-1** (the docblock-vs-render contradiction) · **C-13** (dock token scope on owned portals) · **SC-1..SC-8** carry forward from COHESION §4a. **Glass-producer rows go to the relay; they NEVER become frontend hacks.**
- **NWO-2 · the method row (M-16, promoted)** — producer-side evidence must carry the **producer COMMIT HASH, not the version string**; the census break-surface model must add the **export-map diff at the ADOPTED commit**, not the tag. → SS-3/SS-4.
- **NWO-3 · MG-ι value.js-side** — `color2`'s dispatch rejecting and silently mis-reading `parseCSSColor`'s output: **cross-repo input to the value.js V·π / parser-seam program**, flagged for the owner's mail sweep. Not a fourier row.
- **NWO-4 · census corrections** — the 21/14 cartoon-card figure (D/m-12 + X-4/X-5; the shim's "14" and the lane's "25" both wrong in opposite directions); GAB-29's MetricPill cure-falsifier; fr-PaperSearchInput D-i2's AA-vs-AAA register correction (the ≈1.71:1 focus CHANGE is a 2.4.13 figure; the binding AA comparison is the resting 1.930:1); PP-IMPORTSHAPE. → SS-3/SS-4.
- **NWO-5 · LATEX-RELAY** — PAW-1's 42-site rewrite ask; `activeId/activeRootId: ComputedRef<any>` (six typed contracts downstream vacuous — generic fix at `ref<T|null>`); the katex `^0.16` vs 0.17.0 peer; the producer slot-type/slug-uniqueness/sourceLevel/ref-semantics gaps.
- **NWO-6 · pencil-boil producer** — the false `sideEffects: false` manifest (persists at 0.9.2; harmless-if-dropped once resume is guarded) + the `^0.11.2` range excluding 0.12.0.

---

## §Gates (all born-RED, witness named)

| id | gate | born-RED witness | blocks |
|---|---|---|---|
| **G1** | **RE-PIN.** F.W1 names its target TAG **and** ADOPTED COMMIT HASH in the spec; every producer `file:line` in all 66 records re-resolves at it. | census says 4→7; producer reads 8.0.0 with v7.0.0/v8.0.0 both tagged and HEAD drifted `e73ec80c`→`4a86570b`→`558c3fa3`→`350f7a90`→`407de2d3` across the audit's lifetime. **OWNER-GATED (ESC-1).** | everything below |
| **G2** | **CORRUPT-DIST PRE-GATE (F.W0).** `@import "@mkbabb/glass-ui/styles"` must parse. | installed 4.0.0 `dist/styles/index.css:195-221` — the fold-block was injected at a PROSE mention of `@source` inside a comment, terminating it early; delimiters 17/8 dist vs 15/6 src. Reproduced with the app's own toolchain: `@tailwindcss/postcss` → `CssSyntaxError: Unterminated string: 's own'`. `style.css:3` is the live import ⇒ **`vite dev` and `vite build` are RED at the pin. F.W1 opens on a red build.** | G4, G5, and every build-derived cell |
| **G3** | **SUBSTRATE SETTLE (F.W0).** `git -C fourier-analysis status --porcelain` → 0, or the dirty set recorded as the explicit audit baseline. | 28 dirty paths; committed `web/package.json` pins glass `^3.1.0`/keyframes `^2.2.0`/value `^0.10.0`/lucide `latest`; the audited "installed 4.0.0 pin" is uncommitted bytes no commit holds. F8-REACH-01+02 ruled in one breath. | every per-file budget |
| **G4** | **EMISSION PRE-GATE.** One `npm run build` + grep the EMITTED css for `size-\(--ui-glyph\)` · `\.h-3\\.5` · `rounded-pill` · `animate-collapsible`. | `web/dist` is `Jun 12 18:13`, built at 3.1.0-era, five days BEFORE the installed glass-ui (`Jun 17 21:48`) and provably divergent (`.rounded-control` = 1 rule installed, 0/8 in the chunks; `--ui-glyph`/`--control-h-md` in **0** dist files). **Two banked rulings contradict**: fr-PaperSearch K1 (register DEAD, fresh build 0 emissions) vs PSM-24/R-3 (LIVE on first 4.0.0 build). Canary: `PaperSidebar.vue:258`. Prediction (M1/M3): build green, `animate-collapsible` grep 0. | FR-EQR-14 · FunctionInput D-13/D-18 · F-5 · i-4 · FR-GV-18 · PSM-24 · D-M2 |
| **G5** | **P0 CSS-CLASS CENSUS.** Diff every glass-ui-owned class fourier applies against the target's `@layer components` + `@utility` roster BEFORE the bump. | Four proven instances the import-shaped model cannot see: `.paper-texture` (App.vue:24) · `text-admin-label` (7 sites / 4 files; **alive at the v7.0.0 TAG, deleted `6b450f22`, 0 at v8.0.0** — K-1) · `.cartoon-card`/`.cartoon-surface` (21/14) · `.btn-pill` (deleted at ≥7; NotationPills `:38/:40` become load-bearing). **AA-15 collision must be reconciled inside this gate.** | B-2 · MG-β · FR-CP-21 · FR-FG-12 · FR-EQC-3 · FR-NP-13 |
| **G6** | **ATOMIC LAND-OR-LOSE.** Producer bump + the 162-attribute prop rewrite + the `copied`→`status` triple in ONE change. | Four zero-console-error e2e gates — `visualization-crud.spec.ts:439-482/:612` · `workspace-flow.spec.ts:175-202` · `contour-extraction.spec.ts:139-166` · `gallery.spec.ts:118-135` — none filtering `[glass-ui]`, run against the DEV server (`scripts/e2e.sh:102`) where `Button.vue:131-146`'s DEV `console.error` fires per mounted Button. Split the change and four green specs go red for the wrong cause. | the whole wave |
| **G7** | **BUTTON BUDGET RECONCILIATION.** One stripped-comment grep of record, re-run at the adopted pin. | Five banked figures disagree: 87/36/35 (+9/+2 comment-resident, +17 non-Button) · 37 files/38/124 · 9 glass/7 files + 36 icon (35 live) · 96/77/35 · icon ×38 / glass ×12. **Do not average.** | AA-2 · M-7 · D-M3 |
| **G8** | **TOUCH-FLOOR PRESERVATION.** Every migrated icon site carries `iconOnly`; the emitted sheet keys `[data-control-target]`. | 4.0.0 keys `[data-size="icon"]` (a11y-overrides.css:115-121); ≥7 keys `[data-control-target]` (responsive.css:3-8) set from `iconOnly` (Button.vue:92) — a mechanical `size="icon"`→`size="md"` sweep **silently drops the 44px floor on all 35 live sites**. | FR-COB-2 · FB-4 · FR-USB-3 |
| **G9** | **TOKEN-FAMILY SWEEP.** `grep -rn -- "--slider-scrub" web/src \| wc -l` → **0** — never a variant-string sweep. | 23 dead declarations / 7 files, four concordant recounts; the mechanism that caused it was a string substitution over the variant name that missed 0-of-4 token declarations. **Acceptance gains MPC-10's contrast leg in the breaking direction** (rename-only ⇒ 11.51:1 → 1.51:1). | B-2 · FMD-3 · HLG-3 · MPC-3 · R-1 |
| **G10** | **DISCLOSURE-DELETION.** The scoped `animation` block at `CollapsibleSection.vue:60-65` **and** its `ContourSettings.vue:361-374` twin die IN THE UPLIFT COMMIT. | Witness = close COMPLETES **and** close ANIMATES on all five disclosure surfaces. Born-RED: reka `usePresence` in `unmountSuspended` waits forever on an `animationend` a deleted keyframe name cannot fire. **Witness ordering: cure the HOST import breaks FIRST** (D/M-8) or the loud break hides the quiet one. | B-1 · fr-ContourPreview row 15 · D/B-2 |
| **G11** | **MODULE RESOLUTION.** `vue-tsc -b` + a vite resolve pass at the adopted pin ⇒ 0 unresolved glass-ui specifiers. | `CoefficientsSpectrum.vue:19` `@mkbabb/glass-ui/animated-digit` — present at v7.0.0, deleted at HEAD (`4bf53962`); `InfoCard.vue:4` `@mkbabb/glass-ui/metric-badge` TS2307 **fails CI and the Pages deploy** (`ci.yml:95` / `deploy-pages.yml:114` both `vue-tsc -b --force`, tsbuildinfo gitignored twice ⇒ CI always cold). | B-4 · FR-CP-14 · FR-IC-8/-25 |
| **G12** | **EASING RE-POINT VERIFIED BY EXECUTION, NOT TYPECHECK.** Run the morph; assert `play()` resolves and `phase` returns to `"idle"`. | `easings.ts:58`'s `as EasingFn` cast makes an INCORRECT re-point at `bezierPresets` (4-tuples, not functions) typecheck clean and throw on the first rAF tick — the awaited `play()` never resolves, `phase` sticks at `"settle-out"`, and the `!== "idle"` guard **closes the app-wide theme toggle permanently**. Plus: value.js 4.0.0 has no `"."` export and deletes `timingFunctions`. | L/B-1 · C/B-1 · MPC-2 |
| **G13** | **PEER-GRAPH CLOSURE (G33).** `npm ls` → 0 problems **and** `npm ci --omit=dev` yields a working tree. | Six+ reproductions of ELSPROBLEMS (glass 4.0.0 peers value `^0.10.0\|\|^0.11.0` vs 0.13.0). `@lucide/vue` is a REQUIRED peer with **no lock entry and no manifest row** (MISS-LC2). cva/clsx/reka-ui/lucide all `"dev": true` ⇒ `--omit=dev` fails today (C-3). `vaul-vue` dropped from the regenerated lock ⇒ the bump commit fails everywhere (FR-EQC-7). CVA is **not** removable (FMD-21). | the transaction's commit |
| **G14** | **PENCIL-BOIL FLOOR.** Read the optional-peer range at the ADOPTED tag; correct the three drifted registry rows. | v7.0.0 peers **`^0.9.2`**; `^0.11.2` is an 8.0.0 fact; three banked rows carry the wrong figure; and `^0.11.2` **excludes producer 0.12.0** (`^0.x` = `>=0.11.2 <0.12.0`), blocking L-B3's bump premise. | M-2 · C-18.2 · L-2 |
| **G15** | **NON-CREDIT FOOTNOTE.** The F.W1 spec states in terms that the uplift does not cure the pagination drain. | `v7.0.0:useInfiniteScroll.ts:33` root geometry byte-unchanged at 7.0.0 AND 8.0.0; the re-observation loop reproduces the drain through a different door. Spec-integrity gate — born-RED until the footnote exists. | FR-GIG-5 |
| **G16** | **LATEX-PAPER SUFFICIENCY.** Either the LATEX-PAPER carry (42-site `hsl(var(--x))` → `var(--x)` rewrite) enters the transaction, or it is seated explicitly with a named owner. | Token shapes verified complete-colour at 4.0.0 **and** v7.0.0 **and** v8.0.0 ⇒ the uplift cures nothing on `/paper`; 42 declarations IACVT-dropped; 92 theorem blocks ship the WRONG rail colour. Witness set: theorem + definition + aside side by side in BOTH schemes. | PAW-1 · PAW-51 · PAW-30/PAW-44 (LAW-3) |
| **G17** | **CARTOON-CARD ORDER.** The spec orders FR-EQC-3 (retire the shim) against AA-4 (adopt it as a chassis cure) before either executes. | Two banked cures on a collision course; and KILL-6: `<Card tier="opaque" surface="cartoon">` is unspellable at the target — executing it lands **21 non-compiling values across 14 files**. | FR-EQC-3 · AA-4 · FR-IC-19 · FR-GV-18 |
| **G18** | **BINDING-VERIFICATION PROBE.** Post-uplift, a real drag fires `anim.startScrub()` (`anim.scrubbing === true` mid-drag). | `v7.0.0:Slider.vue:69-78` — the producer states `@pointerdown` template bindings are DROPPED across the Slot/forwardRef boundary and **no static gate can see this class**; `GlassTimeline.vue:73` is the scrub session's sole opener. | PD-1 |
| **G19** | **OWNER RULINGS RECORDED.** ESC-1..ESC-6 each carry a ruling or an explicit deferral with its blocked rows named. | All six are open; ESC-1 alone flips seven fr-GalleryCardModal dispositions, FR-TT-5/-9, K-1's rebaseline bank, GCM-22, FR-FG-20 and G14. **Never presumed** (SS-4 routing note). | the spec's executability |
| **G20** | **CARRY CLOSURE.** Set-difference over this table's row ids vs the F.W1 spec's §Rows → ∅ **both directions**. | The X·V 117-row idiom; §3.1 of COHESION requires every CARRY row to land in exactly one wave. | X-whole SPECIFIED |

---

## §Bounds

**Writable by this seat (the only path):** `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/carry/F-W1-CARRY.md`

**Spec surface F.W1 will own (value.js repo, docs only, at authoring):**
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W1.md` (create)
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/carry/F-W1-CARRY.md` (this file)
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/` — the SS-6 BH letter + the LATEX-RELAY + pencil-boil letters (create)
- `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` — **append-only**, one E13 ledger row per relay

**Product surface F.W1 owns AT EXECUTION (fourier-analysis; READ-ONLY until the begin-word):**
- `web/package.json` · `web/package-lock.json` · `web/tsconfig.json` · `web/vite.config.ts` · `web/src/env.d.ts`
- `web/src/style.css` (`.paper-texture` · the `.cartoon-card` shim `:98-112` · the root-font fork `:40-49`)
- `web/src/lib/colors.ts` · `web/src/lib/basis-display.ts` · `web/src/main.ts` · `web/src/App.vue`
- `web/src/lib/easings.ts` · `web/src/composables/useToast.ts` · `web/src/composables/useMorphConfig.ts`
- the **35 Button-importing files** (the 87 `variant=` / 36 `size="icon"` rewrite — enumerated by G7's grep of record)
- the **35 lucide import sites** + the **17 hand-inlined `<svg>` files (12 `.vue`)**
- `web/src/components/ui/CollapsibleSection.vue` · `web/src/components/visualization/ContourSettings.vue` (the twin animation blocks — G10)
- `web/src/components/visualization/CoefficientsSpectrum.vue` (animated-digit — G11)
- `web/src/components/equation/EquationResult.vue` · `web/src/components/gallery/UserSlugBar.vue` (the `copied` triple — G6)
- the metric-badge consumers (**6 live files + InfoCard.vue, unmountable**): `EquationView.vue` · `InfoCard.vue` · `AnimationControls.vue` · `EditorControlsDock.vue` · `EquationPanel.vue` · `GalleryAdminBanner.vue` · `GalleryDraftsSection.vue`
- `web/src/components/dock/CanvasControlsDock.vue` · `EditorControlsDock.vue` (hover-popover · DockIconButton ×12/×7 · MetricBadge)
- `web/src/components/AppHeader.vue` (`./hover-card` · the easings fork `:21-27` **and** `:29`)
- `web/src/components/animation/AnimationControls.vue` (`./dropdown-menu` → `./menu` if G1 adopts 8)
- the **7 files carrying the 23 `--slider-scrub-*` declarations** (BasisSelector · EditorControlsDock · SliderControl · HarmonicLevelGrid · MorphPhaseConfig · GlassTimeline · ConvergenceTimeline) — G9
- `web/src/components/paper/**` **only if** G16 seats the LATEX-PAPER carry here

**Read-only witnesses (never edited by this wave):** `web/e2e/visualization-crud.spec.ts` · `workspace-flow.spec.ts` · `contour-extraction.spec.ts` · `gallery.spec.ts` · `scripts/e2e.sh` · `web/dist/**` (stale, inadmissible) · `/Users/mkbabb/Programming/glass-ui` · `/Users/mkbabb/Programming/keyframes.js` · `/Users/mkbabb/Programming/latex-paper` · `/Users/mkbabb/Programming/pencil-boil`.

**Do NOT touch:** any F.W3/F.W4/F.W5–W8 repair surface; `scripts/dev/dev.sh` (unowned, standing owner order); any dated registry record (E-3 — dated evidence is never rewritten; corrections travel as new rows).

---

## §CrossEdges (declared from this end; each requires a reciprocal declaration at the other)

1. **F.W0 → F.W1 (HARD predecessor).** F.W0 must discharge: G2 (the corrupt 4.0.0 dist — fr-PaperSidebar M1 / FR-NP-32), G3 (the 28-path dirty settle + F8-REACH-01+02 ruled together — GAB-13 / FR-COB-10), MISS-LC2's lockfile reproducibility, FR-AH-33's manifest placement, and fr-AnimationControls M-15's gate row **as ERRATA'd by PP-REDGATE** (the TS2882 is the uplift's, not F.W0's). **FR-EQC-7's vaul-vue manifest gate lands INSIDE the F.W1 transaction, not before it.** Reciprocal: F-W0.md.
2. **F.W1 → F.W3 / F.W4 (COHESION §2 law).** No fourier frontend spec may assume the uplifted tree before W1 lands it. Named consumers: the B-2 slider execution, the shadow-retirement targets (which must be re-derived against the adopted tag — `./icon-tooltip`, `./toggle-chip`, `./confirm-dialog` all die), the FR-COB-2 lift-then-delete, FR-AFP's guards, and every cure whose producer vehicle changes at the hop (fr-ExportModal M-α/M-β/M-γ; fr-FourierMorphSvg ruling 4). Reciprocal: F-W3.md, F-W4.md.
3. **F.W1 ↔ F.W2 (value.js consumption) — a SPLIT edge, not a total order.** The `colors.ts` cure leg is landable at 0.13.0 **today, pre-uplift** (fr-App C-2 sequencing correction; M-RTC re-homes it to the producer's `/dom` resolver at the CURRENT pin); only the **bare-root-specifier** leg is genuinely gated on the uplift, and `./motion-curves` at 4.0.0 already re-exports every easing symbol except `timingFunctions` (CENSUS-MC). The census's blanket "F.W1 before F.W2" is **wrong for the colour leg**. Reciprocal: F-W2.md.
4. **F.W1 → the glass-ui BH relay (SS-6).** One batched letter, roster at NWO-1, sent at F.W0/F.W1 open. **Producer-owned rows NEVER become frontend hacks** (standing owner edict).
5. **F.W1 → LATEX-RELAY (latex-paper).** G16's sufficiency ruling + PAW-1's 42-site ask + `ComputedRef<any>` + the katex peer. The tri-package charter must either widen to four packages or seat the carry.
6. **F.W1 → the pencil-boil producer.** G14's floor correction; the false `sideEffects: false` manifest; the `^0.11.2` range excluding 0.12.0.
7. **F.W1 → X·V (value.js) parser seam — cross-repo, NOT a fourier row.** MG-ι: `color2` rejecting the wrapper and silently mis-reading the unwrapped value from its own sibling parser. Routes to V·π / the parser-seam program via the owner's mail sweep.
8. **F.W1 ⟂ X·P — FORBIDDEN edge, declared as a negative.** Direct parse-that→fourier is forbidden (standing routing law); any parser need routes parser → value (X·V L1/L5) → packed release → fourier.
9. **F.W1 → F.W9 / F.W10.** The visual-regression checkpoint set the uplift mints with no typecheck signal: `--shadow-cartoon` sign flip · `cartoon-surface` hover loss across 21 sites · the `.disclosure-content` body register · the tooltip re-proportion · GCM-22's `p-0` insets · the Badge rim · FR-CP-13's fused-card gap · the `text-admin-label` reversion. Plus the SS-13 residues each record names.
10. **F.W1 ⟂ F.W5 (SS-4) — declared NON-edge.** The TA-4 value-side atomdiff restoration is SS-4's named prerequisite, not F.W1's; no F.W1 gate consumes the contract. Declared so the dependency is not mis-routed into the uplift transaction.
