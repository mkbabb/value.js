SERVED MODEL: claude-fable-5-1

# F-W3-DO-NOT-EXECUTE — the register (g20's artefact; g12's (b) rows; the S-8/S-15 reconciliation)

**Seat**: X.F.W3.f, the fresh-Fable adjudicator (M-12 tri-fold), serial on clean main, value.js docs-side, **zero fourier bytes written**. **Date**: 2026-09-19 (the sitting's date of record stays 2026-09-17, the owner's begin-word, COHESION §0j). **Authority**: `docs/tranches/X/fourier/waves/F-W3.md` — IMMUTABLE (E-3), sha256-at-quote-time **`5b42a3b6e185`** — §C.J (`:364–371`) · §C.K (`:373–388`) · §X (`:580–601`, the killed-prescriptions row at `:598`) · §3 g20 (`:417`) and the S-15 addendum-beside it · §4 S-5 (`:435`) · §5 X.F.W3.f (`:479–483`). Line numbers into that file are lawful because the file is frozen; every other cite below is a stable anchor.

**What this file is.** One signed reading of which cures may **not** be executed in the fourier frontend, each with the cite that killed it, so no later seat re-derives a prescription the corpus already convicted. It is a register of *refusals with reasons*, never a cure list, and it books nothing: every id below is carried record-qualified and original for life; the §X.1-v5 register in the spec is the booking of record.

**The bytes this seat measured against** (read-only, `git grep`/`git show` at refs; the main checkout is untouched): fourier `m/w1-bump-migration` @ **`21e11b0`** (`.c`'s head, main); `x-f-w3-b` @ **`b9995b3`** (`.b`, 2 commits over `21e11b0`); `x/f3e` @ **`8aa9252`** (`.e`, 14 commits over `x/f3d` @ `74084fa`, `.d`'s 18 over `21e11b0`). ⟨cmd⟩ `git merge-tree 21e11b0 8aa9252 b9995b3 | grep -c '^changed in both'` → **0**; conflict markers → **0**; ⟨cmd⟩ `comm -12` of the two branches' changed-path lists → **∅**. The two branches are file-disjoint and merge trivially; **they are NOT yet integrated onto main** — that is the orchestrator's §9 act, named at the close block, and every reading below says which ref it was taken at.

---

## §1 The four named anti-cures (P-11 law; spec §C.J `:366–370`, quoted by command)

The law first. ⟨cmd⟩ `sed -n '366p' waves/F-W3.md | cut -c1-260`:

> `- **P-11 LAW — "A CURE IS ADJUDICATED SEPARATELY FROM THE DEFECT"** — standing clause, binding on every F.W3 row. A proposedCure inherits NO evidential weight from its defect's evidence …` (elision disclosed)

| # | anti-cure | the kill, at its cite | state at the measured bytes (this seat) | L-14 refutation attempt, recorded |
|---|---|---|---|---|
| **AC-1** | **THE NAIVE `@layer` WRAP** — wrapping consumer overrides in `@layer components` (or any layer below `utilities`) "silently LOSES every consumer override"; only `@layer glass-overrides` appended AFTER `utilities` preserves them; **the durable fix is a LINT RULE, not a per-component edit**. Per-site cures carry the full six-leg `transition` set or delete outright. | §C.J `:367` (fr-App MG-δ is the precedent-controlling home; declarants: EasingPicker MISSED-A · GalleryCard G-DU3 · FR-EMT-6 · FR-GSB-14 · MobileFloatingToc M-12 · FR-NP-10 · VisualizationView L-13 · CoefficientsSpectrum m-21) → **g16** | ⟨cmd⟩ every `@layer` line **ADDED** by F.W3 (`git diff 21e11b0 8aa9252` ⊕ `21e11b0 b9995b3`, `+` lines) → **4 lines, all `@layer glass-overrides`** (`ui/tooltip/Tooltip.vue:174` and its three prose mentions); the order is fixed at `web/index.html:60` `@layer theme, base, components, utilities, glass-overrides;` (unchanged since `21e11b0`). Two-leg `transition` shorthands added → **0**. The one `@layer components {` block in `style.css:327` is **F.W1's** (`git log -S` → `538db90 feat(fourier)!: the atomic tri-package uplift`), a re-declaration of a rule the producer retired, not an override — not this wave's and not the anti-cure's shape. **The lint rule does NOT exist**: ⟨cmd⟩ `git show 8aa9252:web/eslint.config.js | grep -c layer` → **0** (the config exists and carries exactly the two rules F.W0 named). | Attempted: *is there a naive wrap this wave landed?* Searched the whole F.W3 diff for `@layer` adds → only `glass-overrides`. *Does the `glass-overrides` rule actually survive?* Proven **statically** by the layer statement's order (last-declared wins); a computed-style probe was **not** run (§5.2 probe parsimony; no server at this seat) — recorded as the limit of the proof, not laundered into a runtime green. **Not refuted.** |
| **AC-2** | **THE D-2 TINT-PLATE CURE** (fr-MorphShapePreview FR-MSP-6 ⊕ K-13) — the phase-tint plate is a dead state channel (1.205:1 / 1.117:1 vs a 3:1 floor); the corpus's D-2 cure cites a 16.21:1 TEXT fact as a PLATE proof. **THE TWO-CHANNEL LOCK**: any cure EITHER deepens the plate to ≥3:1 AND re-pairs the text to ≥4.5:1 on it, OR adds a non-colour channel — never a single-channel edit. Executing the prescribed cure "removes the only working (if sub-AA) state channel and leaves the inert one". | §C.J `:368`; F.W3 carries the LAW and this entry, the FM-4/FMD-19 contrast edit is **F.W4's**, guarded by the lock | F.W3 wrote no byte at `MorphShapePreview.vue`'s tint plate (`.d`'s `ca8c8d2` touched the file for FMD-12/D-11/FR-MSP-11 — chips and the hover ring, not the plate). F.W4 (CLOSED) added the `FMD-18`/`FMD-19` pairs to its contrast harness and reports both passing in both arms (`execution/C/F-W4.md:660`). | Attempted: *did F.W4 take the single-channel edit the lock forbids?* Its record shows the pairs measured under a harness that floors text at 4.5 and non-text at 3 — the lock's own two floors — so the edit was taken under the lock's terms, not around them. This seat did not re-run F.W4's harness (not this wave's gate). **Not refuted; not independently re-proven.** |
| **AC-3** | **THE `ariaLabel` FORWARD** (fr-Tooltip FR-TT-4) — rich `#content` announced as one unpunctuated `textContent` run; the corpus's unanimous cure (forward an `ariaLabel` prop) is **repudiated by the producer**: declared at 4.0.0/v7, **deleted at 8.0.0**. Re-target required. | §C.G `:315` · §C.J `:369` | ⟨cmd⟩ `grep -rc ariaLabel web/node_modules/@mkbabb/glass-ui/dist/components/tooltip/` (installed **8.0.0**) → **0 in all five `.d.ts`**. `.e` **dropped** the leg from the FR-TT-15 knob list and wrote the repudiation into `ui/tooltip/index.ts` (`7dc4c0b`); the shim forwards no `ariaLabel` (⟨cmd⟩ `git grep -n ariaLabel 8aa9252 -- web/src/components/ui/tooltip` → **1 file, `index.ts:101-105`, the written repudiation itself; 0 in `Tooltip.vue`**). | Attempted: *does the prop still exist at the adopted pin under another name?* Searched the producer's tooltip `.d.ts` surface for `aria` → the content props carry no label prop; the shim's naming leg remains UNMINTED (g11 residue), which is the honest consequence of a retired cure with no replacement yet. **Not refuted.** |
| **AC-4** | **THE TOKEN-SWAP THAT LEAVES THE COMPOUNDING PRESS INTACT** — press transforms COMPOUND (`scale` longhand 0.96/0.97 ∘ consumer `transform: scale(.95)` → ≈0.912); pressed background beats hover by unlayered source order. **A token swap does NOT cure it: the cure must DELETE the consumer's `transform` leg.** Rider: base.css's PRM carve resets only `scale`; the consumer's `transform` survives PRM. Consumer-awareness note FR-COB-7: no fourier pressed-state work may assume a border channel exists. | §C.J `:370` (GalleryCard G-DU4; FR-COB-7 → S-8 register, disposed — see COHESION §4) | ⟨cmd⟩ consumer `transform: scale(` lines **ADDED** by F.W3 → **0**; **REMOVED** → **2**. The direction of travel is the cure's, not the anti-cure's. | Attempted: *did any F.W3 cure swap a press token and leave a `transform` leg?* No press token swap appears in the diff; the two removals are deletions of consumer `transform` legs. The FR-COB-7 premise (dead border arms) is itself **overturned by the producer** (I-32 A-11a: `.button` carries `border: 1px solid var(--button-edge)` unconditionally) — so the awareness note's factual half is stale at 9.0.0 and *unmeasured at 8.0.0 by this seat*; the note's prescription (do not assume a channel) stands regardless. **Not refuted; one premise disclosed as producer-superseded.** |

---

## §2 Every killed §X prescription, with its kill cite (spec `:598`, the "retained as struck so no seat re-derives them" row)

Eighteen entries, in the row's own order. "Kill cite" is the §2 row of the immutable spec that convicts it (with the banked id and the killing ruling); "what landed instead" is read from the unit receipts and re-verified at the refs named above.

| # | killed prescription | kill cite (spec, immutable) | what landed instead (ref) |
|---|---|---|---|
| K-01 | **`ScrubberTimeline` adoption** | §C.A `:235` — **SR-1** (stale twice: K-14 progressbar ruling; 8.0.0 `SliderVariant` → `"scrubber" \| "spectrum"`; regresses the accessible name) ⊕ **K-16** (the import is unexported at both layers; the dispatcher route is dead at 8.0.0) | `.a` re-derived one unconditional-session composition on the Slider (`f36541a`); ⟨cmd⟩ `git grep -c 'glass-ui/timeline' 8aa9252 -- web/src` → **0** (g3's *"the gate asserts the re-derivation, not an import"*) |
| K-02 | **`--popover-offset`** as the slider-scrub cure | §C.B `:248` — **rK-20**: 0 occurrences in the installed package AND in `web/src`; "prescribing it injects a phantom" | `.a` `77b9bc0`: ⟨cmd⟩ `git grep -c -- '--slider-scrub' 8aa9252 -- web/src` → **0 files** (g2), with MPC-10's contrast leg asserted at `.a` |
| K-03 | **`:max="1000"`** on the convergence timeline | §C.B `:243` — **C·C-7**: "the naive `:max="1000"` cure would ×10 the announcement rate D·D-9 flags — the cure is adjudicated separately from the defect" | decided **inside** `.a`'s fork re-derivation (`f36541a`), never after; see `.a`'s Act 2 receipt |
| K-04 | **viewBox attribute surgery** (`:width`/`:height` repairs) | §C.C `:255` — **GEO anti-cure lock**: "No interim attribute surgery: the box is not the tree's to set"; both prescribed attribute repairs bind on cascade-overridden attributes; `:252` L30 names it one of the two cures the producer's arrival kills | MOOT on ROUTE 1 (§0x S-6a): `EasingCurvePreview.vue` **DELETED** at `.b` `b9995b3`; the producer's `EasingCurve` is CSS-sized; `.b` residual 5 records that the lock's *reason* moved while its *prescription* held |
| K-05 | **ToggleChip-as-chassis** for the easing chips | §C.C `:257` — **CHASSIS LOCK (Toggle.js:68)**: ToggleChip under `role="menuitemradio"` double-exposes state — "adoption is sound as *styling* only, never as chassis"; §0x S-6a: "the ToggleChip route is REFUSED" | `DropdownMenuRadioItem` chassis at `.b` `b9995b3` (⟨cmd⟩ `git grep -c DropdownMenuRadioItem b9995b3 -- …/EasingPicker.vue` → **3**); g8 GREEN with the live keystroke leg taken |
| K-06 | **`role="radiogroup"`** as the aria-pressed naming cure | §C.F `:302` — **FR-NP-19 / K-18**: ships axe-critical `aria-required-children` and announces an unimplemented APG contract; "a strictly worse a11y outcome than the honest three-button set"; the naming cure is `role="group"` + `aria-label` | published as an anti-cure inside `.e`'s vocabulary block (`style.css`, `7dc4c0b`); ⟨cmd⟩ `git grep -n 'role="radiogroup"' 8aa9252 -- web/src` → **1 hit, `style.css:393`, the publication's own prose** (*"`role="radiogroup"` IS KILLED (FR-NP-19 / K-18)"*) — **0 in any template or rule** |
| K-07 | **`<Card tier="opaque" surface="cartoon">`** migration | §C.E `:294` — **KILL-6** (a 21-site typecheck break at the uplift target) ⊕ **K-7** (two majors stale: at 8.0.0 `cartoon` is not a prop; the form is `<Card class="cartoon-surface">`) → g14 | the shim (`style.css:217 @utility cartoon-card { @apply cartoon-surface; }`) is the version-stable form and stands at `8aa9252`; the cartoon-card ask is RETIRED on the letter (O-20 §D-2). **Two legs of g14 are RED and named at §6 below** |
| K-08 | **re-adding `.glass-btn`** | §C.F `:299` — **R8-17**: `surfaces.css:46-56` declares it and `.btn-pill` MUTUALLY EXCLUSIVE; also I-32 **A-11e KILL** (the string left the producer's `src/**.css` before 7.0.0) | nothing re-added: ⟨cmd⟩ `git grep -n 'glass-btn' 8aa9252 -- web/src` → **1 hit, `style.css:396-397`, the publication's own prose** (*"Re-adding `.glass-btn` is likewise NOT a cure"*) — **0 in any rule or template** |
| K-09 | **"make the shim single-root"** (Tooltip) | §C.G `:317` — **DU K6, re-proved**: "`renderSlot` mints the Fragment UNCONDITIONALLY — C-1's 'make the shim single-root' arm is KILLED"; a shim-imposed root would take the `v-for` key element away from the `coeff-list` TransitionGroup | `.e` `511cbd0`: `inheritAttrs:false` + pre-placed `v-bind="$attrs"` on the CONTENT host (`Tooltip.vue:51`, `:144`) — the law's operational form, not a root |
| K-10 | **the `ariaLabel` forward** | §C.G `:315` / §C.J `:369` — deleted at 8.0.0 (AC-3 above) | leg DROPPED from the knob list; repudiation written at `ui/tooltip/index.ts` (`7dc4c0b`) |
| K-11 | **`ConfirmDialog` swap** (`./confirm-dialog`) | §C.H `:342` — **K1**: `./confirm-dialog` is TRUE at 4.0.0 and ABSENT at v7/v8; "the cure is the in-file Dialog pattern … never the retired primitive" | `.d` `11a0351`: the last live `window.confirm()` retires onto the in-file Dialog; ⟨cmd⟩ `./confirm-dialog` absent from the 70-key 8.0.0 export map (`.d`'s read) |
| K-12 | **P-5(a)'s `getPathBounds`** resurrection | §C.K `:382` — **PP-DUP / Invariant-20**: "both prescribed cures KILLED — P-5(a) would resurrect the Invariant-20-violating `getPathBounds` the fleet routed for deletion" | `.d` `d63bd93`: `PathPreview.vue` **DELETED** (F8-REACH-01; zero inbound imports); the idiom lives at `lib/contourEditing.contourBounds` |
| K-13 | **the naive `@layer` wrap** | §C.J `:367` (AC-1) | 0 naive wraps added by F.W3; lint half absent (g16, §6) |
| K-14 | **the token-swap press** | §C.J `:370` (AC-4) | 0 `transform: scale(` legs added, 2 removed |
| K-15 | **one-child-per-class colocation** | §C.D `:265` — **MISS-LC1**: the corpus's own prescribed cure orphans five cross-SFC shared hooks; "shared hooks go in a non-scoped or shared home … never one-child-per-class"; same commit as PSM-1 | `.c` `21e11b0` — ONE commit, 8 files, the g13 rider set unsplit; g1 at 0 violations (CHECK 1–3 each re-derived it) |
| K-16 | **the D-6 / L-1¶3 backdrop revival** | §C.D `:268` — **FR-PS-BDT** (⊕ FR-PSD-CB): the revived `.paper-search-backdrop { position:fixed; inset:0 }` resolves against the bar's padding box under `backdrop-filter` and covers the INPUT; "may NOT be executed as prescribed" | `.c` `21e11b0`: outside-tap dismissal re-derived on the adopted dialog chassis (real `Dialog`/`DialogContent`), per O-32 §3.2's own description of `PaperSearchModal` |
| K-17 | **the D-3 dropdown revival** | §X `:598` names it; **no §2 row carries the spelling "D-3 dropdown"** — ⟨cmd⟩ `grep -n 'D-3 dropdown' waves/F-W3.md` → `:598` only. The kill rides the §C.D family (PSM-1/R-5, `:263–271`): the dropdown is `fr-PaperSearchDropdown`'s, and the modal chassis supersedes a revived dropdown. **Disclosed as a cite-by-row rather than a cite-by-line.** | `.c` `21e11b0`: `search/index.ts` dead surface cleaned before the diff; the family retired into the modal chassis |
| K-18 | **adoption-inherits-scoring** (`./search`) | §C.D `:271` — **PSM-14**: "the prescribed adoption does NOT fix the short-query death — adoption must be priced with an explicit scoring decision, not assumed to inherit one" | `.c` Act 3: `./search` **NOT ADOPTED**, the (b) row at §3 below; the scoring decision made at `paperSearchIndex.ts` |

**Killed grades and celebrations that must not be inherited** (spec `:599`) and **preserved dissents** (spec `:600`) are **cited, not transcribed**: they are rows of the immutable spec and this register adds nothing to them. One is consumed above by name — reader-2's BLOCKER on FR-EMT-5 (*"the truer reading if the re-derivation finds further tinted-surface failures"*) — because g14's `--viz-amber` STATUS-NOTE (§6) is where a later seat would look for it.

---

## §3 g12 — the two NOT-ADOPTED rows, id-keyed, as `.c` and `.d` measured them (state (b) of the gate's closed OR)

g12's GREEN admits exactly two states per subpath: **(a)** ≥1 real import at the §C.H adopting site, or **(b)** an id-keyed NOT-ADOPTED row **in this file** carrying the banked id, the refusal reason, and the seat + date. Prose in the spec never counts; a rationale that is not a row here does not count.

### `./search` — NOT ADOPTED (state b) — the row `.c` authored, transcribed verbatim from `execution/C/F-W3.md` (X.F.W3.c Act 3)

> **`fr-PaperSearchModal PSM-9` (≡ `fr-PaperSearch MISS-B3` ≡ `fr-PaperSearchDropdown C:C-6` ≡ `fr-PaperSearchInput C-2`) — `@mkbabb/glass-ui/search`: NOT ADOPTED.** Seat **X.F.W3.c**, `claude-opus-5[1m]`, **2026-09-19**, measured at the ADOPTED pin (`v8.0.0` @ `17a11bc5`). **Reason**: the prescribed 1:1 twin `FuzzySearch.vue` does not exist at 8.0.0 (`./search` exports `SearchBar` — a field, not a palette); and the producer's scorer reproduces BOTH defects this wave cured — `[_lc.type, 10]` above `[_lc.text, 3]` (`FR-PSD-TYPE`) and the unbounded `score -= max(0,(tLen−pLen)*0.1)` length penalty (`PSM-14`), at 5× the consumer's ratio scale — while dropping the `number` and `rawTex` fields the paper's search is built on. Adoption is a measured REGRESSION on five axes, so the `ANTI-CURE` clause governs: *"adoption must be priced with an explicit scoring decision, not assumed to inherit one."* **The scoring decision was made instead**, at `paperSearchIndex.ts`, and is documented there. **BH/SS-6 relay owed** (`PSM-14`'s own routing): the producer's `./search` carries both defects and should receive both cures.

Re-verified at this seat: ⟨cmd⟩ `git grep -n 'glass-ui/search' 8aa9252 -- web/src` → **one hit, `paperSearchIndex.ts:210`, a docblock** (`* ⊘ This decision is NOT inherited from …`) — **0 imports**, at `b9995b3` likewise. The relay half rides §6 (row F3-9).

### `./forms` — NOT ADOPTED (state b) — the row `.d` handed over, transcribed from `execution/C/F-W3.md` (X.F.W3.d Residual 1)

> **id** `fr-AdminUserList FR-AUL-6` (the `./forms` leg) · **reason**: `./forms` is not an export of the adopted `8.0.0` pin (70-key map, measured); `X-EXT-2` re-seats the target to `./input`, and the CURE LANDED there at five sites — so the subpath is NOT-ADOPTED because it does not exist, not because the repair was declined · **seat + date**: `X.F.W3.d`, 2026-09-19. Transcript: `evidence/w3/g12-X-F-W3-d.txt`.

Re-verified at this seat: ⟨cmd⟩ `git grep -n 'glass-ui/forms' 8aa9252 -- web/src` → **no match** (`.d`'s `53ca589` removed the docblock that had contaminated the probe); the export-map read in `.d`'s transcript: `./forms false | ./input true`.

### `./fading-scroll` — state (a), recorded for the 3-of-3 tally

⟨cmd⟩ `git grep -c 'glass-ui/fading-scroll' 8aa9252 -- web/src` → `EquationView.vue:1` · `ConvergenceLegend.vue:1` · `CoefficientsSpectrum.vue:1` — **3 real imports**, the §C.H-named adopting site (`CoefficientsSpectrum.vue`, the M-6 family head) among them (`.d` `3a6c5a4`); at main `21e11b0` the count is **1** (`ConvergenceLegend.vue`, landed by F.W4 — booked discharged-by-F.W4, never re-cured).

**g12 tally at the unit heads: 3 of 3 subpaths in state (a) or (b) → GREEN.** At main `21e11b0` alone the tally is 1 (a) + 2 (b) — also GREEN by this file's existence, since the (b) rows are keyed to the ids and not to a ref.

**The `./easing` qualification (spec g12 `:409`, R4-6.1), carried at this gate's face**: `./easing` is a live cross-package homonym (`@mkbabb/value.js` AND `@mkbabb/glass-ui`); it is not one of g12's three subpaths. Every `./easing` probe in this wave names the package: `.b`'s adoption sites import `@mkbabb/glass-ui/easing` (`EasingPicker.vue:3`, `MorphPhaseConfig.vue:143` at `b9995b3`).

---

## §4 Deliberate no-ops, recorded rather than dropped (FR-TT-10's clause: "a recorded deliberate no-op is an acceptable outcome — never a silent drop")

| id | the no-op | recorded by | cite |
|---|---|---|---|
| `fr-Tooltip FR-TT-10` | the `hoverable` knob is **deliberately NOT exposed**: its banked cure (`disable-hoverable-content`) would delete a banked WCAG 1.4.13 pass | `.e` `511cbd0` | `execution/C/F-W3.md` X.F.W3.e Act 2; spec §C.G `:316` |
| `fr-ContourSettings i-3` | the default-collapsed IA is **RECORDED, not executed** — the owner ruled COLLAPSED by default (§0x S-6b) and the spec's own order is to record | `.d` (`be623d9` family; receipt Act 2) | spec §C.K `:375`; COHESION §0x |
| `fr-EasingCurvePreview GEO-1` lock | the attribute-surgery lock is **MOOT on ROUTE 1** — its prescription held, its stated reason moved (the CVA spelling left for a layered rule at 8.0.0); the file is deleted | `.b` residual 5 | spec §C.C `:255` |
| `fr-EasingPicker D-8` | two of the eight reduced-motion surfaces **retire** with the rules they gated; the PRM arm is now producer-owned (`menu.css`) | `.b` residual 8 | — |
| BARREL, second seam (`useMorphConfig.ts:20`) | left **exactly as found**: §C.C splits BARREL by seam and §0x rules *the single* seam that dies with the file; routed F.W4 with its one consumer named | `.b` residual 6 | spec §C.C |
| the producer's `EasingPicker` + `useEasingPicker` | the larger picker adoption is **neither taken nor refused** — MISSED-D's text contemplates the CURVE; routed F.W4 unpresumed | `.b` residual 4 | §0x S-6a *"may not presume beyond it"* |
| `fr-MorphShapePreview L-06` | the API re-shape **NOT taken** (KISS) | `.d` `ca8c8d2` | — |
| `fr-PaperArticleWindow PAW-22` · `PAW-53` | two riders **REFUSED and named**: PAW-22→GCM-23 under K-5's site-scoped lock; PAW-53 under SC-10 (a producer rung, §6 row F3-6) | `.d` `8eca678` | — |
| `fr-ContourPreview` row 16 | **NOT claimed** — the adjudication says in terms that the ConfiguratorLayer adoption cannot close it | `.d` `caca3cf` | — |
| `fr-CoefficientsPanel FR-CP-28` colocation MOVES | **not taken, for bounds** — the destinations (`layout/UserSlugBar.vue`) are outside every bounds row; the two limbs needing no destination LANDED | `.e` residual 6 | — |
| `fr-ExportModal LC-2` | the `fixme` **STAYS** with its refutation written beside it — the cure is *re-run, then delete*, and the run needs a stack no unit may stand up (E-d2) | `.d` Act 4 | spec §C.K `:387` |
| `EasingPicker.vue:39` docblock (`.b`) | the sentence naming the dead `.is-active` spelling is **left standing**: rewriting prose so a grep rounds is the overfit this house convicted | `.b` residual 1 | g9's SWEEP-LAW rider |

---

## §5 L-14 — the recorded refutation attempts (this seat's obligation)

**L-14 obliges a recorded refutation attempt on at least one load-bearing claim.** Four were attempted; two changed a figure.

1. **"g19's set-difference is EMPTY."** Attacked three ways at the frozen operand (`CENSUS-CANONICAL.md` sha256-at-quote-time `f44362757458`): **(i)** a planted ESCAPE (`fr-SpeedSelect SS-C-9` dropped from a copy of the register) → `comm -23` reports **1** in column 1; a planted FABRICATION (`SS-C-99` added) → `comm -13` reports **1** in column 2 — the comparison is live in both directions, not vacuously green. **(ii)** en-dash spellings on either side → **0 / 0**. **(iii)** the register's eight `~~struck~~` spans (`K-12` · `K-9` · `K-6` · `K-3`+`K-11` · `K-7` · `K-20` · `K-2` · `K-7`) are stripped before comparison; **a bare-token check would have flagged `K-11` as a hole** (it survives in the canonical **once, at `fr-EditorControlsDock`**, a different record) — the `(record, id)` pair comparison does not, which is exactly axis (iv)'s point. **Result: not refuted.** 919 = 919, both columns empty, double-run identical (`diff run1 run2` → ∅). Transcript: `evidence/w3/g19-X-F-W3-f.txt`; detector: `evidence/w3/g19-comm.sh`.
2. **The S-15 partition — "eleven residual ⊕ five dispatched = the sixteen items" (spec `:435`, the dated addendum).** Read against the packet's own bytes (`O-20` @ `f6e04c86ddb8`) and its two dispositions (I-32, I-33): **REFUTED by three items.** `AX-1`/`CU-1` ride **O-20 §B-6** (*"GlassTimeline a11y four-item family, producer share … the cursor-affordance deletion is producer-owned"*), disposed **I-32 B-6 CURE-NOW** (`cursor: grab`/`grabbing`/`not-allowed`); `FR-COB-7` rides **O-20 §A-11** (FR-COB-28 item 1), disposed **I-32 A-11a KILL ⊕ A-11b ANSWER** and struck at NWO-1 §2 S-1. The honest partition is **eight dispatched ⊕ eight residual**, and one of the eight residual (`AA-3`'s violet/batch tone) was since **WITHDRAWN with cause at O-32 §1.2**. The addendum is E-3-immutable; the correction lives here and at §6, not in the spec. **Consequence**: the accretion carries **seven** of the addendum's eleven, and re-sends none.
3. **AC-1's "no naive wrap" — attacked at the diff** (§1). Not refuted; the runtime-survival half is disclosed as static-only.
4. **An adopted cure shape — `.e`'s choice of the CONTENT as the `$attrs` host (g10).** The alternative host is the trigger, which is the consumer's own child via `as-child`; attrs placed there would contend with what that child already carries, and a shim-imposed root is K6-killed. The choice is the only one the law admits. **Not refuted.** Its cost is named: a consumer `class` on `<Tooltip>` now styles the panel, never the trigger — which is what the shim's docblock says.

---

## §6 g14 and g20 — the relay re-scope, the STATUS-NOTES, and the accretion (verb: ACCRETES; SS-6 is root-authored)

**g14, three legs, measured.** **(1)** *The SS-6 letter carries `--viz-amber` ONLY; the cartoon-card ask RETIRED* — **GREEN by dispatch**: O-20 §B-4 (*"`--viz-amber` only — see §D-2 for the retired half"*) and §D-2 (*"The cartoon-card ask is RETIRED"*), sha `f6e04c86ddb8` unmoved; disposed I-32 **B-4 KILL** — the STATUS-NOTE: *`--viz-amber` = `oklch(0.530 0.124 69.6)`: 5.018 on `--card`, 5.214 on `--background`, 7.721 dark, **4.518 under your 8% wash**; the 3.54 is a 4.0.x figure. The wash inversion persists and is yours by your own ruling* — i.e. the producer confirms §C.E's *"THE CURE IS THE WASH, NOT THE TOKEN"*, and I-33 §B-4 adds that `style.css:116-123`'s unlayered `--viz-amber` override *"is now the regression … Strike the block"* (a **consumer** row, un-taken by any F.W3 unit; carried to the residuals at the close block, owner `.e`'s file). **(2)** *The shim's stale comment corrected in the wave that touches it* — **RED**: `.e` touched `style.css` (`7dc4c0b` and after) and `style.css:211-212` still reads *"Fourier consumes `.cartoon-card` at 14 application sites (13 files …)"* at `8aa9252`; this seat measures ⟨cmd⟩ `git grep -c 'cartoon-card' 8aa9252 -- web/src` → **12 files / 24 mentions** (4 in the shim's own block; the spec's census of record was 21 sites / 14 files at authoring). A landed-wrong at `.e`, one comment, outside this seat's writable set. **(3)** *The FALSE "migration complete" comment corrected (D-15)* — **RED**: the B.W2.a comment (*"The bespoke `cartoon-card` panel backgrounds retire for the substrate's layered chassis"*) sits at `VisualizationView.vue:216-221` at `8aa9252` (was `:188-193`) and the host's own `cartoon-card` site survives at `:191` (was `:163`); `.e` edited the file (MAJ-5/MAJ-7) and left it. A landed-wrong at `.e`, one comment, same ownership.

**g20, the S-15 form.** The register half is this file. The letter half is **NOT a letter**: the cargo below **ACCRETES** to COHESION §4a (rows **F3-1..F3-13**) for the next root-authored SS-6 boundary letter, reconciled against O-20 ⊕ I-32/I-33 ⊕ NWO-1 (F.W1's relay, already reconciled against I-32/I-33) ⊕ O-32/O-33 (F.W4's relays) so that **no row is re-sent and no ask is withdrawn**.

**STATUS-NOTES — dispatched, never re-sent (8, not 5; §5 item 2):** `PD-1` (O-20 A-10 → I-32 CURE-NOW comments; I-33 §1: *"the render is right, the docblock is false at both pins"*) · `--viz-easing`→`--motion-accent` (B-4 → KILL: *"`viz-easing` never existed … one arm is free (`--motion-accent`), not both"*) · `./clipboard` (B-5 → DECLINE: ships on `./dom` and the root; *"a third door for zero capability is a shim"*) · `--viz-amber` + cartoon retirement (B-4/§D-2 → KILL, the wash reading above) · DOCK-ACTIVE (§D-1, honoured at FR-COB-11) · **`AX-1`/`CU-1`** (B-6 → CURE-NOW) · **`FR-COB-7`** (A-11 → A-11a KILL ⊕ A-11b ANSWER; NWO-1 S-1) · **`AA-3`** (WITHDRAWN, O-32 §1.2).

**RESIDUAL — accreted at COHESION §4a (13 rows):** the seven survivors of the addendum's eleven — `LF-1` limb (b) · `FR-COB-22` (register note) · `./confirm-dialog`'s retirement note · `.disclosure-trigger` hover (M-5) · the floating-offset token (FR-TT-13) · `PAW-53`'s forced-colors rung (SC-10) · the `--tier-*` family (a rider on NWO-1 **L-4**, which already asks the tier-tint's home — the accretion adds only the chip-plate token ask, so the two cannot double-send) — ⊕ six raised by this wave's units and in no prior letter: `R-1[D-14-fallback]` (`.a`: the producer's default fill 1.308:1 / 1.920:1 on its own track) · `EasingCurve` axis captions as HTML text (`.b`) · the producer `./search` scorer's `FR-PSD-TYPE` ⊕ `PSM-14` defects (`.c`) · `m-7` / `m-16` on the Collapsible wrapper (`.d`) · `ConfiguratorLayer` emits no heading (`.d`) · `FR-TT-18` `--z-tooltip` 120 < `--z-popover` 130 (`.e`). Each row cites its unit receipt; none is a frontend hack; glass-ui stayed READ-ONLY at every seat (⟨cmd⟩ `git -C ../glass-ui status --porcelain | wc -l` → **0**, this seat, 2026-09-19).
