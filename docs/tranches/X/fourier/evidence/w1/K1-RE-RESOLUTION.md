SERVED MODEL: claude-opus-5[1m]

# K-1 — THE SUSPECT WINDOW RE-RESOLVED at the adopted tag (census leg)

**Instruction**: §4 step 3 `:275` — *"…→ G14 floor read → **K-1 suspect-window re-resolutions**."*
`K-1` is the kill/rebaseline mechanism: a claim measured at one producer pin is a **suspect** until
re-resolved at the pin actually adopted. Unit `a` re-resolved the three suspects its sections named
(`FR-CP-21` · `fr-App MG-β` · `B-2`'s `.paper-texture` premise) and handed two counting-unit
residuals to this unit. **This is the census leg**: every `K-1`-class claim the gates G7 · M-7 · G5
· G14 consume, re-resolved at `17a11bc5`.
**Unit**: `c`. **Date**: 2026-09-17. **E-3**: re-baselines, never re-books. **No severity is
re-graded and no registry file is touched.**

---

## §1 THE RE-RESOLUTIONS

### K-1.a — `fr-SvgFilters K-1` · *"the `^0.11.2`-at-7.0.0 version fact — REFUTED AT THE TAG"*

| pin | `@mkbabb/pencil-boil` peer |
|---|---|
| `v4.0.0` | `^0.4.1` |
| `v7.0.0` | **`^0.9.2`** |
| `17a11bc5` | **`^0.11.2`** |

**CONFIRMED, and the consequence INVERTS with G1.** The refutation is true of v7.0.0 and false of
the adopted tag. Full treatment and the three dated registry corrections: `G14-PENCIL-BOIL-FLOOR.md`
§2. **RE-BASELINED.**

### K-1.b — `K-8 / SR-1` · `SliderVariant` drift · **THE CONDITIONAL RE-BOOK FIRES**

The bank's own instruction: *"at 4→7 the break does not exist — **re-book iff G1 adopts 8** (then 9
live sites / 8 files)."*

| pin | `SliderVariant` |
|---|---|
| installed 4.0.0 | cva keys `standard` · `spectrum`; `defaultVariants: { variant: "standard", size: "md" }` |
| `v7.0.0` | `export type SliderVariant = "standard" \| "spectrum";` |
| **`17a11bc5`** | **`export type SliderVariant = "scrubber" \| "spectrum";`** · `withDefaults(… variant: "scrubber" …)` |

**G1 adopted 8 ⇒ the break is REAL and enters the wave.** Live surface **9 attribute callsites /
7 files** (⊕ 5 prose), enumerated at `M7-PROP-LEVEL-DIFF.md` §5.2 and double-run. **Dated
correction**: K-8/SR-1's *"8 files"* is off by one; `MPC-21 / BR-1`'s *"9 attribute callsites /
7 files (+5 prose)"* is exact. **Neither row is re-graded.** The cure is MPC-21/BR-1's
**delete-don't-rename**, and the bytes make it free: `standard` **is** the 4.0.0 default, so
deleting the attribute is a no-op today and lands on `scrubber` (the 8.0.0 default) tomorrow.

### K-1.c — `fr-AdminAuditLog K-1` · the `D-M8` falsifier · **INVERTS AT THE ADOPTED TAG**

`K-1` killed D-M8's *"`0.65rem` matches no token rung"* falsifier on the ground that *"the installed
pin ships `@utility text-admin-label`"*. ⟨cmd⟩ `git grep -c "@utility text-admin-label" <pin> -- src`:
**v4.0.0 = 1 · v7.0.0 = 1 · `17a11bc5` = 0.**

**The kill's PREMISE is dead at the adopted tag.** The consequence is carefully bounded, and
`G5-CSS-CLASS-CENSUS.md` §3 carries it in full: **AA-15's DEFECT stands** (the off-scale
`text-[0.65rem]` at `AdminAuditLog.vue:134,:143` is a fourier fact independent of any producer pin);
**AA-15's CURE is dead as banked** (it adopts a rung that no longer paints); the two rows stop
colliding by re-pointing onto **one successor rung decided once at F.W1**. **RE-BASELINED, NOT
RE-BOOKED** — MAJOR stands, home F.W4 stands.

### K-1.d — `fr-App MG-β` and `fr-App B-2` · unit `a`'s two suspects, **independently reproduced**

Re-run at this seat with a **wider operand** than unit `a`'s (the whole emitted roster, not `src`
alone), so the confirmation is not a transcription:

- **MG-β** — `text-admin-label`: **3 emitted artefacts** at the installed pin (`styles/typography.css`
  · `styles/typography/semantic.css` · `styles/theme/bridges.css`), the `@utility` body at
  `semantic.css:213-220`; **0 mentions in `src` at `17a11bc5`**, the sole survivor being the
  allowlist `_shared/class-names.ts:84`. **Unit `a`'s reading reproduces.**
- **B-2** — `.paper-texture`: **6 emitted artefacts** at the installed pin, with a **live rule** at
  `styles/cards.css:10` (⊕ the `:where(.dark)` arm at `:17`) inside `@layer components`; at
  `17a11bc5` **no rule**, 4 mentions all token-or-prose. **Unit `a`'s reading reproduces — and is
  extended**: both tokens the rule composed (`--paper-clean-texture` `tokens/scale-paper.css:118` ·
  `--paper-texture-size` `tokens/offsets.css:106`) **survive**, which makes B-2's texture-restore
  arm executable without any `@utility paper-*` recipe (`fr-SvgFilters M-1/R-3`'s constraint
  satisfied at the adopted pin, where only `paper-underpaint` and `paper-grain-overlay` remain and
  neither is a drop-in).

### K-1.e — `fr-FrequencyGraph K-1` · the counting-unit correction · **DISCHARGED**

Unit `a` measured **5** matching lines at `v7.0.0` against a banked *"4 hits"* and routed the unit
question here. Re-measured across all three pins:

| pin | matching LINES over `src/styles` | `@utility` DECLARATIONS |
|---|---|---|
| `v4.0.0` | **5** | **1** |
| `v7.0.0` | **5** | **1** |
| `17a11bc5` | **0** | **0** |

**The line count is 5 at BOTH pre-adoption pins**, so the banked *"4"* is a third unit and not a v7
artefact. **The DECLARATION count is the load-bearing figure**, and at the adopted tag every unit
agrees on **0**. **Nothing moves**; the correction travels as this dated row and as
`G5-CSS-CLASS-CENSUS.md` §6.2.

### K-1.f — `B-1`'s K-1 kill · the disclosure spring clock · **CONFIRMED and SHARPENED**

B-1 carries: *"the '250ms short via `--spring-smooth-settle`' claim is DEAD; real clock
`--spring-present-duration` = 200ms."*

| token | installed 4.0.0 (**whole dist**) | `17a11bc5` (`src`) |
|---|---|---|
| `--spring-smooth-settle` | **0 files** | **0 files** |
| `--spring-present-duration` | **0 files** | **13 files** (declared `tokens/scheme-spring.css:109`) |
| `--spring-present-settle` | **0 files** | **1 file** |

**The kill stands, and its replacement is an 8.0.0-only fact.** The dead claim names a token that
exists at **neither** pin; the "real clock" it is replaced by exists at the **adopted** pin only.
Recorded so G10 (unit `e`) and the WU-F ledger (unit `f`) quote the clock with its pin attached —
quoting `--spring-present-duration` as an installed-pin fact would be quoting an 8.0.0 byte at a
4.0.0 tree.

### K-1.g — `fr-BasisSelector i-4` · the single-file proof · **HOLDS at the adopted pin, RE-PRICED**

`./slider` · `./button` · `./configurator` are all present at `17a11bc5` ⇒ **0 subpath breaks**, as
i-4 claims. Prop breaks at the adopted pin: **5** attribute occurrences (`:125` ×2 · `:140` · `:168`
· `:195`) ⊕ the lucide specifier (`:9`). The banked *"six"* does not reproduce under the unit this
seat can measure at either ≥7 pin (**3** at v7.0.0, **5** at `17a11bc5`). **i-4 is INFO and is not
re-graded**; its load-bearing claim is the shape `0 subpath : n prop`, and that shape is confirmed
and strengthened. Detail: `M7-PROP-LEVEL-DIFF.md` §5.1.

### K-1.h — `WU-G`'s subpath census and the export-key count · **RESOLVED, unit named**

Unit `a` disclosed *"69 measured against the banked 70 @ 8.0.0 — stated, not resolved"* and routed it
to G7/G5's counting-unit discipline. Resolved by JSON parse, not grep:

| | keys **including the root `"."`** | keys spelled `"./…"` |
|---|---|---|
| installed 4.0.0 | **80** | 79 |
| `17a11bc5` | **70** | **69** |

**WU-G's banked *"80 @ 4.0.0 · 70 @ 8.0.0"* is the whole-map unit and reproduces exactly.** Unit
`a`'s probe anchored on the `./` prefix and therefore excluded the root key, which is present in
both maps. **70 − 1 root = 69.** Two units, both true, neither averaged. **Nothing moves.**

---

## §2 WHAT THIS LEG DOES NOT TOUCH

- **`FR-TT-5 / FR-TT-9`'s joint re-pin gate** — *"two banked K-1 rulings disagree on which side
  governs"*. Sized only after G1, but its execution surface is the tooltip seam, which is unit `e`'s
  and F.W3's. This unit contributes one measured input and no ruling: `FloatingPlacementProps` at
  `17a11bc5` is `side | sideOffset | align | alignOffset`, so **`collisionPadding` has no prop**
  (`ui/tooltip/Tooltip.vue:33`, 1 site), and the v7-era mechanism name `RETIRED_FLOATING_ATTRS` is
  **absent at 8.0.0** — the effect survives, the mechanism does not.
- **`K-9` · `K-11` · `K-12` · `K-13` · `K-14`** — none is consumed by G7 · M-7 · G5 · G14; each stays
  at its own home and pin.
- **`ESC-5`** — unit `a` recorded that the `.paper-texture` premise is measured TRUE at the adopted
  tag. §1's K-1.d **extends** the measurement (the composing tokens survive, so the restore arm is
  executable) and thereby **prices** the severity question ESC-5 asks — **without answering it**. A
  seat may not rule on the owner's behalf.
