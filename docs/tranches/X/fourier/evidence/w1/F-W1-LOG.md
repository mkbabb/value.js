SERVED MODEL: claude-fable-5-1
# F.W1 — CLOSE REPORT (`F-W1-LOG.md`) · 2026-09-18 · unit `f`

**Wave**: X.F.W1 — the atomic tri-package uplift, fourier-analysis `web/`. **Post-cut tree**: fourier `m/w1-bump-migration` HEAD **`538db90`** (`feat(fourier)!: the atomic tri-package uplift`, 58 files · 1,500 ins · 744 del) atop `3bac3d5` (unit `d`, severable palette cure). **Producer pin**: glass-ui **`v8.0.0` @ `17a11bc5`**, registry-pinned (§0i.3 / G1). **Seat**: unit `f`, the wave's judgment-and-design seat and the seat that closes; it authored neither `waves/F-W1.md` nor unit `a`'s bytes. **Product trees READ-ONLY throughout** — fourier · glass-ui · keyframes.js · latex-paper · pencil-boil; every witness is a click, a hover, a drag or a computed-style read (⟨cmd⟩ `git -C ../fourier-analysis status --porcelain | wc -l` → **0** before and after every run).

**Inherited from the killed `f` seat (STANDING LAW)**: `evidence/w1/g20-join.mjs` (kept, one syntax cure — see `G20-CLOSURE.md`) and `evidence/w1/shots/` (11 JPGs + `capture-records.json` "sitting 4" + `capture-transcript.json`). Its capture SCRIPT died with the host; its outputs are kept as a **named prior run** and were re-measured by this seat's own instrument, `evidence/w1/capture-family.mjs`, run twice (`run3` · `run4`; `run1`/`run2` were instrument-correction runs and were removed before commit). Where the two seats' readings are compared below they agree in every verdict.

---

## 1 · Gate readings at the close — every gate, BEFORE → AFTER, with its witness

| gate | BEFORE (resume baseline, seat 0) | AFTER (this seat, double-run) | verdict |
|---|---|---|---|
| **G1** RE-PIN | GREEN (unit `a`) | `grep -c "17a11bc5" waves/F-W1.md` → **18**, **18** | GREEN |
| **G2** corrupt-dist (F.W0's; F.W1 owes the RECORDING) | DISCHARGED | recorded at spec §8.3; absent at the adopted pin by `e`'s green build | DISCHARGED-as-owed |
| **G3** substrate settle | GREEN by predecessor | `status --porcelain \| wc -l` → **0**, **0** | GREEN |
| **G4** emission pre-gate | GREEN (`e`) | not re-run (`npm run build` writes `web/dist`, a §1 read-only witness) | GREEN (`e`) |
| **G5** CSS-class census ⊕ landing cell | GREEN + APPLIED | `evidence/w1/G5-CSS-CLASS-CENSUS.md` 25,617 B present; `grep -rn 'text-admin-label' web/src` → **0** | GREEN |
| **G6** ATOMIC LAND-OR-LOSE | atomicity GREEN · **four e2e UNRUN** | **the four zero-console-error gates GREEN ×2** — see §2 | **GREEN** (two disclosed facts, §2) |
| **G7** button budget ⊕ application | GREEN + APPLIED | `G7-BUTTON-BUDGET.md` 15,402 B; limb 2 landed at `538db90` | GREEN |
| **G8** touch floor | GREEN (`e`) | unchanged | GREEN |
| **G9** token-family sweep (acceptance only) | RED by design | `grep -rn -- "--slider-scrub" web/src \| wc -l` → **23** / 7 files — execution is F.W3/W4's | RED — by design, handed forward |
| **G10** DISCLOSURE-DELETION | deletion GREEN · **five-surface witness UNRUN** | **five surfaces close COMPLETE and ANIMATE, ×2** — §3 | **GREEN** |
| **G11** module resolution | GREEN (`e`) | unchanged (`TS2307` = 0) | GREEN |
| **G12** EASING RE-POINT BY EXECUTION | **RED — probe unrun** | **morph run ×2 per run, `phase` `idle>settle-out>morph>settle-in>idle`, `morphProgress` 1, 0 page errors** — §4 | **GREEN** |
| **G13** peer-graph closure | GREEN (`e`) | unchanged | GREEN |
| **G14** pencil-boil floor ⊕ application | GREEN | `web/package.json:18` `"^0.11.2"` | GREEN |
| **G15** NON-CREDIT DISCHARGE | (b) GREEN · (c) GREEN · **(a) half — close-report half missing** | **(a) both halves** — §7 | **GREEN** |
| **G16** LATEX-PAPER SUFFICIENCY | GREEN on arm 2 (seating) | re-stated with its named owner — §8 | GREEN (arm 2) |
| **G17** CARTOON-CARD ORDER | **RED — ordering unwritten** | **the ruling is written** — §9 | **GREEN** |
| **G18** BINDING-VERIFICATION PROBE | **RED — probe unrun** | **real drag: `startScrub` fires, `scrubbing === true` on 8/8 mid-drag samples, `endScrub` fires, `scrubbing === false` after, ×2; I-32 A-10 read against PD-1** — §5 | **GREEN** |
| **G19** owner rulings recorded | GREEN (§0o + spec §9) | `grep -c "ESC-2\|ESC-3\|ESC-4\|ESC-5\|ESC-6\|ESC-7" COHESION.md EXECUTION-RUNBOOK.md` → `8` · `0`, exit 0 | GREEN |
| **G20** CLOSURE, BOTH SOURCES | **RED — unrun** | **leg (a) 105/105 GREEN · leg (b) 328/330 — RED by TWO named escapes, 0 fabrications** — `G20-CLOSURE.md` | **RED (honest, two rows, cure sized)** |
| **FR-CP-5** chevron (WU-F benefit row, "verify at bump") | unverified | **the configurator chevron ROTATES 0°→164°→180° over ~330 ms under `transition-property: rotate` (`transition-disclosure`), 10 distinct sampled angles, ×2** — no snap | **DISCHARGED-BY-UPLIFT, verified** |
| **ESC-4 sampler gate** (§0o-minted) | GREEN purpose · RED literal (4 keys ≤ 4.44e-16) | disposition written — §10 | returned to the owner, named |

**Tally at the close — 20 gates: 17 GREEN · 1 DISCHARGED-as-owed (G2) · 2 RED** — **G9** (acceptance-only, deliberately unmoved, F.W3/W4 executes) and **G20** (leg (b), two named rows whose cure is two spec rows outside this unit's writable set). Delta against the resume baseline: **six gates turned GREEN by this unit** (G6 · G10 · G12 · G15 · G17 · G18 — the resume counted G6/G10/G15 as half-GREEN and G12/G17/G18 as RED); G20 ran for the first time and reads RED honestly.

**The four-verb line does NOT move.** IMPLEMENTED stamps at *G1–G20 green + close report*; G20 is RED by two rows, so this report stamps nothing — the stamp is the spec seat's the moment the two rows land. VERIFIED is the X·F sub-tranche close's, never this file's.

---

## 2 · G6 — the four zero-console-error gates, run against the DEV server

**Stack**: `scripts/e2e.sh --no-tests` (uvicorn `:8000` + `vite web --port 3000 --strictPort`, `COMPUTE_RATE_LIMIT=1000`, `WRITE_RATE_LIMIT=1000`), backed by a throwaway native `mongod` whose dbpath lives in this seat's scratchpad (the repo's `.dev/` was not written). **Anchors re-resolved at the true bytes**: `visualization-crud.spec.ts:439` `consoleErrors: () => string[];` · `:482` `consoleErrors: () => consoleErrors,` · `:612` `expect(probes.consoleErrors(), "console errors").toEqual([]);` · `workspace-flow.spec.ts:175` · `contour-extraction.spec.ts:139` · `gallery.spec.ts:118` — each the `test("no console errors …")` / lifecycle line the spec names; **no spec file was edited and no `[glass-ui]` filter was added** (the three `realErrors` filters at `:195` / `:162` / `:130` are the pre-cut ones, `429`/`412`/`401` resource noise only).

⟨cmd⟩ (cwd `web/`, twice) `BASE_URL=http://localhost:3000 ADMIN_TOKEN=dev npx playwright test --project=chromium --reporter=list --workers=1 -g "no console errors|full lifecycle" e2e/visualization-crud.spec.ts e2e/workspace-flow.spec.ts e2e/contour-extraction.spec.ts e2e/gallery.spec.ts` → **`6 passed (26.4s)`, exit 0 — run 1 and run 2 byte-identical**: `contour-extraction:139` ✓ · `gallery:118` ✓ · `visualization-crud:498 full lifecycle @ mobile / @ laptop / @ desktop` ✓✓✓ (each carries the `:612` assertion) · `workspace-flow:175` ✓. The producer's DEV retired-prop `console.error` (Button.vue) fired **zero** times across the four flows — the atomic seam held.

**Two facts disclosed rather than laundered.** **(i)** The four spec FILES run whole under the repo's default `fullyParallel` (first run, ⟨cmd⟩ without `--workers=1`): `26 passed · 3 failed · 3 skipped · 3 did not run` — the three failures are the CRUD lifecycle at all three viewports, each a **30 s test timeout inside `extractContour()` (`:228`), a raw `page.evaluate(fetch('/api/images/…/extract-contour'))` against the Python API** with no frontend code on the path; `workspace-flow:56` ran **27.1 s** in the same pass. Serial, the same three pass in 3.7–7.7 s. Cause: backend compute contention on one uvicorn worker under ~10 concurrent extract-contour calls. `api/` is untouched by both fourier commits (⟨cmd⟩ `git show --stat --format= 538db90 3bac3d5 -- api scripts web/e2e` → ∅), so this is a harness-load fact of this host, not an uplift cause; it is handed to F.W3/W4 as a finding (§11 F-3), not cured here. **(ii)** In the serial CRUD run, **`a11y keystone: ExportModal Dialog-open is clean @ mobile` (`:648`) is RED** — axe `color-contrast` serious, 1 node: `<button class="segmented-tab" role="tab" aria-selected="false">Controls</button>`, **4.33:1** (`#8b7257` on `#fbfaf8`, 22.9 px normal), the mobile `lg:hidden` `SegmentedTabs variant="underline"` under the modal. It is **not one of G6's four gates** (a different test in the same file); its ink is **producer-owned** — 8.0.0's `.segmented-tab { color: color-mix(in oklab, var(--muted-foreground), var(--glass-capsule-warm) 12%) }` (`dist/components/tabs/styles/segmented.css`; present at `17a11bc5:src/components/tabs/styles/segmented.css:267`) — the consumer passes no colour. A producer row: **§11 F-1**, rides mail, never a frontend hack.

## 3 · G10 — five disclosure surfaces, close COMPLETES and ANIMATES

Instrument: a rAF sampler armed on the trigger's `aria-controls` region before the REAL click; `completes` = the region reaches `hidden`/`display:none`/height 0 and stays there; `animates` = ≥ 3 distinct intermediate heights strictly between 0 and h₀ **and** a non-`none` `animation-name` observed. Every surface was then re-opened (`aria-expanded` → `true`) to prove the close is not a hang.

| surface (twin) | h₀ | close completed (ms after `aria-expanded` flip) run3 · run4 | distinct intermediate heights | `animation-name` seen | completes · animates · re-opens |
|---|---|---|---|---|---|
| `/equation` **Function** (`CollapsibleSection`) | 300.9 | 188 · 189 | 21 · 22 | `disclosure-close` | ✓ ✓ ✓ |
| `/equation` **Controls** (`CollapsibleSection`) | 187.0 | 202 · 203 | 23 · 23 | `disclosure-close` | ✓ ✓ ✓ |
| `/equation` **Coefficients** (`CollapsibleSection`, default-closed → opened first) | 339.0 | 209 · 202 | 24 · 23 | `disclosure-close` | ✓ ✓ ✓ |
| `/w/…` **ContourSettings › Advanced** (the `ContourSettings.vue` twin) | 110 | 209 · 228 | 9 · 10 | `disclosure-close` | ✓ ✓ ✓ |
| `/w/…` **ContourPreview › Preview** (`CollapsibleSection`, editor panel) | 180 | 233 · 206 | 8 · 10 | `disclosure-close` | ✓ ✓ ✓ |

Born-RED's mechanism (reka `usePresence` waiting on an `animationend` a deleted keyframe cannot fire) is measurably gone: the producer's `disclosure-close` keyframe runs and completes at ≈ 200 ms (`--spring-present-duration`), on both twins, at corresponding scope; `overflow: hidden` was excluded on both (unit `e`) and the clipping is visibly load-bearing in the sampled heights. The killed seat's prior run agrees (5/5 `completes` · `animates` at 204–227 ms).

## 4 · G12 — the easing re-point, verified BY EXECUTION

`handleToggle()` on the header's `DarkModeToggle` awaits `morph.morphTo()`, whose three phases each `await currentAnim.play()`; `phase` returns to `"idle"` and `morphProgress` to `1` **only** after all three `play()` promises resolve, so the terminal state is the proof. Sampled on the component's own `morph` (via `__vueParentComponent.setupState`, dev-mode read; no product byte touched), REAL click:

| run | direction | phases observed | elapsed | final `phase` / `morphProgress` | page errors during the run |
|---|---|---|---|---|---|
| run3 · 1 | light → dark | `idle > settle-out > morph > settle-in > idle` | 510 ms | `idle` / 1 | 0 |
| run3 · 2 | dark → light | same | 461 ms | `idle` / 1 | 0 |
| run4 · 1 | light → dark | same | 477 ms | `idle` / 1 | 0 |
| run4 · 2 | dark → light | same | 464 ms | `idle` / 1 | 0 |

The `as EasingFn` class (a wrong re-point that typechecks and throws on the first rAF tick, closing the toggle permanently) is falsified by execution: four full morphs, none stuck, no throw. ESC-4's refused-drift limb (14 orphans authored analytically in `web/src/lib/easings.ts`, 8 survivors on `@mkbabb/value.js/easing`, the `:58` cast gone) is what ran.

## 5 · G18 — the binding-verification probe, and PD-1 read against I-32's A-10

A REAL pointer drag (`mouse.down` at 15 % of the timeline track, 8 × `mouse.move` in 3 steps each, `mouse.up`) with the animation store observed through pinia's own `$onAction` (no product byte touched):

| reading | run3 | run4 |
|---|---|---|
| before | `scrubbing:false t:0.253 playing:true` | `scrubbing:false t:0.250 playing:true` |
| mid-drag (8 samples) | **`scrubbing:true` on 8/8** (t 0.18 → 0.53) | **8/8** |
| after `mouse.up` | `scrubbing:false t:0.540` | `scrubbing:false t:0.543` |
| actions, in order (first three) | `seek` (scrubbing after: false) → **`startScrub`** (true) → `seek` (true) … | identical |
| `startScrub` fired · `endScrub` fired · total actions | ✓ · ✓ · 26 | ✓ · ✓ · 26 |

**PD-1's disjunction, decided by this measurement and stated rather than presumed.** PD-1: *either* the `@pointerdown` template binding on the forwarding chain is DROPPED at the target's reka (`Slider.vue:69-78`), *or* the producer's docblock saying so is false at its own pin. **I-32 `A-10`** (glass-ui, 2026-09-17, *"CURE-NOW (comments)"*): *"The render is right, the docblock is false at both pins: reka's `SliderRoot` merges `$attrs` through `mergeProps` and a consumer's `@pointerdown` on our Slider fires (fourier's `GlassTimeline.vue:73` is the one such site) … Measured order: reka's slideStart, reka's update, then the consumer's."* This seat's post-uplift drag reproduces A-10 exactly: the binding fires (arm 1 FALSE), and the order is reka's `seek` **then** the consumer's `startScrub` (A-10's order, to the tenth of a millisecond: `seek@9780.6` → `startScrub@9780.7`). **A-10 DISCHARGES the disjunction on arm 2 — the docblock was false — and it discharges it at the producer** (three false docblock sites rewritten + a mount test, per A-10; the relay to fourier is the constellation page's). G18's condition (`anim.scrubbing === true` mid-drag) is met. **What survives as a rider, F.W3/W4's**: the very first `seek` of every drag lands with `scrubbing === false` (one frame before `startScrub`), which is PD-1's cure-shape law in miniature — *native listeners on the resolved host or an upstream session-pair prop, never template bindings on the forwarding chain* — a design choice for the store repair wave, not a break. `I-32`'s A-10 is thereby READ in this wave's scope; its status cell is Track D's (the letter's routing), and this seat does not edit it.

## 6 · WU-F — the uplift-delta DIFF-REVIEW LEDGER (silent, gate-blind rows) with before/after screenshot rows, and the cross-edge-9 CHECKPOINT SET

Every row names its evidence (`shots/run3-*` and `run4-*` are this seat's; unnumbered `shots/0N-*.jpg` are the killed seat's prior run, cited where they frame what this seat's clip did not). A checkpoint is a **named baseline for F.W9/F.W10's visual-regression set**; F.W1 mints it and hands it forward — it re-grades no record.

| # | WU-F row | measured at the post-cut tree (run3 = run4 unless noted) | disposition · edge-9 checkpoint minted |
|---|---|---|---|
| 1 | **`fr-GalleryCard D-21`** `--shadow-cartoon` sign flip | `getComputedStyle(:root).--shadow-cartoon` → `-3px 3px 0 0 color-mix(in oklab, …)`; first `.cartoon-card` on `/equation` paints `oklab(0.28 …/0.32) -3px 3px 0 0, …` — **the flip is live** (was `3px 3px`) | **CP-1 cartoon-shadow-mirror** — `shots/run3-02-equation.jpg` · `02-equation-cartoon-cards-disclosures.jpg` (prior run) |
| 2 | **`fr-ContourPreview 23`** cartoon hover-lift disappears (×21 sites) | hover a `.cartoon-card`: `transform none → none`, `translate none → none`, box-shadow head unchanged — **no lift at 8.0.0**; the class survives (5 cartoon nodes on `/equation`, `.cartoon-surface`/`.cartoon-card` both resolve) | self-cures the false-affordance rows AND strips real affordances (the FM-2 converse) — **CP-2 cartoon-hover-loss**; the converse rides F.W3's FR-EQC-3 (§9) |
| 3 | **`fr-ContourPreview 27`** CollapsibleContent gains `role="region"` + name | `.disclosure-content` ×3 on `/equation`: `role="region"`, `aria-labelledby` present | new guarantee — recorded; no checkpoint (semantic, not visual) |
| 4 | **`fr-ContourPreview 30`** `tap-squish` drops from the trigger | `CollapsibleSection`'s trigger classes carry no `tap-squish`; the producer's configurator trigger DOES (`tap-squish transition-control`) — the two disclosure idioms now differ in press feedback | **CP-3 disclosure-press-register** (a design row for F.W3, the CollapsibleSection ⟷ ConfiguratorLayer idiom choice) |
| 5 | **`FR-EQC-13`** `.disclosure-content` imposes ink/type/padding on every body | body register: `color rgb(95,75,58)` · `16.4px / 22.96px` · `padding 0` | **CP-4 disclosure-body-register** — `shots/run3-02-equation.jpg` |
| 6 | **`FR-CP-5`** chevron snap | verified — rotates over ~330 ms (§1) | benefit row, discharged; **CP-5 configurator-chevron** (motion checkpoint) |
| 7 | **`FR-CP-13`** fused-pair vs `.viz-panel-left` 12 px gap | `.viz-panel-left`: `gap normal`, 4 children, inter-child gaps **[12, 0, 0] px** — the ImageUpload card sits 12 px above a fused three-layer stack (BasisSelector · ContourSettings · CoefficientsPanel at 0/0) | the ONE gap decision was made inside limb (e): the layer stack fuses, the card above it keeps its 12 px — **CP-6 viz-panel-stack** — `shots/run3-06-viz-panel-left.jpg` · `06-workspace-viz-panel-left-fused-stack.jpg` |
| 8 | **`FR-TT-5/-6/-9/-14`** tooltip re-proportion | the play-button tooltip: `.glass-floating.glass-overlay-plate.glass-reveal.z-tooltip`, **14.38 px**, padding **8 px**, radius **12 px**, 130 × 40 | **CP-7 tooltip-proportion** — `shots/run3-05-tooltip.jpg`; FR-TT-9's dock-stamp cure is 8.0.0's and is on NWO-1 |
| 9 | **`GCM-22`** `p-0` insets (8.0.0 `:where()` clamp) | **UNWITNESSED** — 0 published cards in the throwaway DB, so no `GalleryCardModal` mounted (`gallery:cards.cardCount` 0, both runs and the prior run) | **CP-8 gallery-card-modal-insets — BASELINE OWED** to F.W9/F.W10 against a seeded gallery |
| 10 | **`G-DU6 / GCM-50`** Badge rim | `Metric` spans (`.metric`, `.metric__reading`, `.metric__value`) carry `box-shadow none`, `outline none`; `.badge-atom` unmounted on the surfaces reached | **CP-9 badge-rim — BASELINE OWED** (same seeding gap as CP-8) |
| 11 | **`fr-ExportModal M-α/M-β/M-γ`** | landed in the transaction (`dismiss="deliberate"`, `min-width` deletion); the ExportModal keystone RAN at three viewports (§2) and passed at laptop/desktop | **CP-10 export-modal-title-weight** (M-β's 400-weight title question) — baseline: the dialog opens clean at ≥ 1280 |
| 12 | **`fr-ContourPreview 23/27/30` shim comment** | the `.cartoon-card` shim comment corrected in the same edit (unit `e`) | recorded |
| 13 | **ESC-3 — `U-2`, the invisible picker (COHESION §0o: FOLLOW THE PRODUCER)** | **six `SelectTrigger` callsites / five files, all reached, every rendered trigger carries `glass-control-edge` + `glass-capsule-hover`** — `ContourSettings.vue:211` ("Contour extraction strategy", 341 × 40, `2px solid oklab(…/0.15)`) · `SpeedSelect.vue:31` ("Playback speed", 56 × 28, pill) · `GallerySearchBar.vue:86` + `:101` ("All tiers" · "Newest", 239 × 32, `1px solid …/0.12`) · `AdminUserList.vue:252` ("Sort users", admin mode) · `MorphPhaseConfig.vue:36` (×3 rendered, "Linear", 244 × 40, pill) = **8 rendered triggers, 8/8 edged**; not one passes a prop that could opt out | **a WU-F diff-review row, NOT a port** (no edgeless trigger re-created downstream) — **CP-11 picker-edge** — `shots/run3-04-animation-dock-expanded.jpg` · `run3-09-gallery-filter-drawer.jpg` · `run3-10-gallery-admin-users.jpg` · `run3-11-morph.jpg` |
| 14 | **`text-admin-label` reversion** (G5 re-target) | `grep -rn 'text-admin-label' web/src` → 0; on the admin Users tab the `.admin-badge` reads 18.6 px / `font-variant-numeric normal` | **CP-12 admin-micro-type** — F.W3/W4's AA-15 seat verifies the `--type-micro`/`--type-caption` + `tabular-nums` re-target per site |
| 15 | **SS-13 residues** | the `--viz-*` palette resolves (unit `d`); the light/dark morph re-paints (§4) | **CP-13 viz-palette-both-schemes** — `shots/run3-07-workspace-full.jpg` |
| 16 | **`.paper-texture`** (G5 census, texture-restore) | present on the shell (prior run: `background-image url("data:image/svg+xml…")`, `200px 200px`, `multiply`) | **CP-14 paper-texture-ground** — `01-paper-texture-ground.jpg` (prior run) |

**Cross-edge 9 → F.W9/F.W10, minted**: CP-1 … CP-14 above, with CP-8 and CP-9 carrying **BASELINE OWED** (a seeded gallery is their precondition). Reciprocal expected at `F-W9.md`/`F-W10.md`.

## 7 · G15 — the non-credit, stated in terms (leg (a), BOTH halves)

**The F.W1 uplift transaction is NOT credited with curing the whole-collection pagination drain (`FR-GIG-5`).** v7 fixed `check()`'s trigger and left the observer-root geometry byte-identical at 7.0.0 and 8.0.0 (`useInfiniteScroll.ts:33`); the loop survives 4→7→8; the cure is a **producer change on the NWO-1 relay**, with FR-GIG-5's three BLOCKER dissents preserved at the banked record. **The commit body's half LANDED** — ⟨cmd⟩ `git -C ../fourier-analysis show 538db90 --format=%B --no-patch | grep -n "pagination\|AA-11"` → `:148 curing the whole-collection pagination drain. v7 fixed check()'s trigger and` · `:151 here is planned around ./pagination (AA-11 holds).` (the `:147` lead-in *"G15 NON-CREDIT, STATED IN TERMS: this uplift transaction is NOT credited with"* is the line above). **(c)**: `./pagination` is not an export at `17a11bc5` (spec §8.5), so `AA-11` holds by construction — no cure is planned around it. **(b)**: FR-GIG-5 left on NWO-1 with its dissents (unit `b`, E13 row appended). All three conditions hold; G15 is GREEN.

## 8 · G16 — LATEX-PAPER sufficiency, the seating RE-STATED

**Arm 2, seated explicitly with a named owner.** The 42-site `hsl(var(--x))` → `var(--x)` rewrite did **not** enter the transaction: the sites are latex-paper's own CSS, outside `web/` (⟨cmd⟩ `grep -rn "hsl(var(" web/src | wc -l` → **0** — nothing consumer-side to rewrite), and the uplift cures nothing on `/paper` (version-INVARIANT at 4.0.0/v7/v8). **Owner**: the latex-paper producer, via **NWO-5** (authored by unit `b`, rowed, NOT delivered — see §12). **Locks carried as their bytes**: PAW-30 lands in the SAME repair; **LAW-3** — *"PAW-44 restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before"*; the consumer-alias limb stays KILLED (K-9). **Witness owed by the executing owner**: theorem + definition + aside side by side, BOTH schemes. G16 is GREEN on arm 2 and no wider.

## 9 · G17 — CARTOON-CARD ORDER: the RULING (FR-EQC-3 retire ⟷ AA-4 adopt)

**Facts consumed.** Unit `c`'s census finding: `.cartoon-card` is a **design** ordering, not a break ordering — `cartoon-surface` SURVIVES the 4→8 hop (`fr-BasisCanvas D-corpus-C-1`), the shim's 21 occurrences / 14 files compile, and **KILL-6** stands (`<Card tier="opaque" surface="cartoon">` is unspellable at the target — 21 non-compiling values in a repo whose only gate is `vue-tsc`). This seat's measurement: on `/equation` the five cartoon nodes paint the producer's mirrored shadow with **no hover-lift** (§6 rows 1–2); the shim is live paint, not dead code.

**The ordering, binding on F.W3 and F.W4 (neither executes here):**
1. **FR-EQC-3 / FR-IC-19 (RETIRE) FIRST — F.W3.** Retire the `.cartoon-card` shim (`style.css:98-112`) by re-pointing its 21 sites / 14 files to the producer's surviving `cartoon-surface` class; radius comes free via `--radius-card`; **no local shim, no `tier="opaque" surface="cartoon"` spelling** (KILL-6), and **no consumer re-creation of the lost hover-lift** (row 2 is the producer's 8.0.0 design; a downstream lift is a frontend hack under the standing edict). FR-GV-18's MINOR-provisional resolves inside this step with G4's emission read (`animate-collapsible` → 0 already banked).
2. **AA-4 (ADOPT `<Card>`) SECOND — F.W4, after step 1 has landed.** Adopt `<Card class="cartoon-surface">` only at sites whose own evidence row needs Card's chassis (padding/tier/header slots), never as a blanket sweep; a site already correct on the bare class stays on it. Adopting before step 1 would stack two surfaces (shim + Card) on one node and hide which one paints.
3. **Never both in one commit, never in the other order.** The ordering exists so that when Card lands there is exactly one cartoon paint source on every site and the visual-regression checkpoint CP-1/CP-2 (§6) can be read against it.

## 10 · ESC-4's ULP residual — disposition (unit `e` published both readings; `f` states which the record carries)

Unit `e`'s sampler: 14/14 orphans and 3/8 survivors BIT-EXACT; 1 signed-zero-only; **4 survivor keys ≤ 4.440892098500626e-16** (two ULP at 1.0), because the producer re-expressed those closed forms. §0o's ruling has a **purpose clause** (*"so the hop changes no motion"*) and a **literal clause** (*"Δ = 0 at every sample point"*). **This seat's reading, stated and not re-interpreted**: G19's record carries the ruling as **EXECUTED on its purpose clause** — 4.4e-16 is below any time or pixel quantum a browser can render, so no motion changed and RD-5 RESTORE is executed — and **the literal clause is NOT met on four keys**, which this seat may not green by reading "equal" as "equal to two ULP". Returned to the owner as a one-line ask: *rule the literal clause satisfied at ≤ 4 ULP, or order the four forms re-expressed to the 0.13.0 bit pattern.* No F.W1 gate depends on the answer; the atomic transaction stands either way.

## 11 · Findings at the close — measured, none moving a gate, each with a named owner

- **F-1 → the glass-ui producer (NWO-1-class row, rides mail) ⊕ F.W3/W4 (a11y).** 8.0.0's `.segmented-tab` rest ink `color-mix(in oklab, var(--muted-foreground), var(--glass-capsule-warm) 12%)` paints **4.33:1** on fourier's `--background` at 22.9 px normal weight — axe `color-contrast` serious on the mobile `SegmentedTabs variant="underline"` (`VisualizationView.vue:182`), surfaced by the pre-existing ExportModal keystone `@ mobile` (§2 (ii)). Consumer passes no colour; **not cured here** (a producer defect never becomes a frontend hack). Producer-side: `17a11bc5:src/components/tabs/styles/segmented.css:267`.
- **F-2 → F.W3/W4.** The router's View-Transitions route-morph bracket (`web/src/router/index.ts:124-146`, I.ε, commit `262c3d0`, **pre-uplift**) throws an uncaught `Transition was aborted because of timeout in DOM update` on `/visualize → /w/<slug>` when the parked `swapped` promise outlives Chromium's update-callback timeout (the workspace mount + first compute). Reproduced 4/4 runs by this seat and once by the killed seat (`capture-records.json`). A `pageerror`, not a `console.error`, so G6's four gates do not see it. Not the uplift's; owned by the frontend truth wave.
- **F-3 → F.W3/W4 (harness).** `scripts/e2e.sh`'s default `fullyParallel` invocation is red-by-contention on this host: the CRUD lifecycle's raw `extract-contour` fetch exceeds the 30 s test timeout under ~10 concurrent compute calls (§2 (i)); serial it passes in < 8 s. `api/` untouched by the wave. A workers/timeout budget decision for the e2e harness, not a product cure.
- **F-4 → F.W9/F.W10.** CP-8 and CP-9 (gallery card modal insets · badge rim) are **BASELINE OWED**: a fresh DB has no published cards. Precondition: a seeded gallery.
- **F-5 → unit `d`'s bound question, re-handed as a QUESTION and not a revert** (first Close residual 7): the three WU-E repair sites unit `d` measured outside its writable set — `fr-BasisCanvas C-4` · `m-9` at `DarkModeToggle.vue:30,31,49,50,53,54` · `M-α5` at `canvas-drawing/epicycles.ts:289` — remain as measured; the §1 bound is the owner's to widen.
- **F-6 → the spec seat.** G20's two escapes (`G20-CLOSURE.md`): `fr-ExportModal R2-1` (one CITED token at §2·R2a.1) · `fr-ContourSettings M-4` (a CITED row held F.W3/W4 beside `m-17`, or a canonical errata re-home — the census seat's call).
- **F-7 → the census (NWO-4-class).** `fr-FrequencyGraph K-1`'s *"4 hits"* reads **5** at `v7.0.0` under the LINE counting unit (spec §8.4, unit `a`); adopted-tag figure 0 under either unit; nothing moves.

## 12 · Residuals handed forward, by name

1. **→ F.W3 / F.W4**: G9's 23 declarations / 7 files (acceptance banked, execution theirs; MPC-10's contrast leg in the breaking direction rides it) · FM-2's six focus-ring sites → `--focus-ring-color` · FR-EQC-3/FR-IC-19 then AA-4 in §9's order · the 16 hand-inlined `<svg>`s / 11 files and the 9 comment-resident `variant=` mentions (unit `e`) · the substrate's 18 unused-symbol diagnostics · CVA's now-measurable retirement (FMD-21 expired at 8.0.0) · F-1 (a11y half) · F-2 · F-3 · G18's first-`seek` rider (§5) · CP-3 and CP-12.
2. **→ F.W4**: **ESC-2's collapse** — the ToC model becomes ONE `useSidebarFollow` owned by the paper view, provided through a typed `InjectionKey`; the second instance and the untyped `defineExpose` seam retire; **prerequisite `L-5(a)` first**; blocked rows released: `fr-PaperSidebar L-4` · `M6`. **RECORDED here, NOT executed.**
3. **→ F.W9 / F.W10**: the edge-9 checkpoint set CP-1 … CP-14 (§6), CP-8/CP-9 baseline-owed.
4. **→ the X formation mail seat**: the three relay packets NWO-1 · NWO-5 · NWO-6 stay **AUTHORED-AND-ROWED, NOT DELIVERED** (glass-ui READ-ONLY always; latex-paper and pencil-boil trees outside every F.W1 bound); F-1 and G18's A-10 confirmation are NWO-1 accretions for that seat; NWO-6's `^0.11.2`-excludes-0.12.0 (C-18.2) unchanged; NWO-5's `katex ^0.16` / `vite ^6||^7` ranges at `@mkbabb/latex-paper@0.2.1` unchanged.
5. **→ the owner**: §10's one-line ESC-4 literal-clause ask.
6. **→ the spec seat**: F-6 (two G20 rows), after which IMPLEMENTED may stamp.
7. **`FR-COB-26` — ESC-6 RECORDED (INFO)**: the `color-mix` browser-support band is **Baseline 2023 (Chromium 111 · Safari 16.2 · Firefox 113)**; **no hand-authored fallback**; the generated fallback's **1.00:1** paint is the consequence below the band, recorded and not cured. Blocked row released: `FR-MSP-12 → FR-COB-26`.
8. **→ Track D / the mail seat**: `I-32` · `I-33` · `I-34` · `I-35` status cells (Track D's); this wave's scope-read of `I-32 A-10` is §5.

## 13 · E13 at the close (this seat's own clock)

Four-path sweep + the Track-C extension (⟨cmd⟩ `/usr/bin/find <path> -maxdepth 1 -name '*.md' -newermt '2026-09-18 00:00'`): `docs/tranches/V/` ∅ · `docs/tranches/V/coordination/` → `INBOX.md` (self) + `valuejs-outbound-2026-09-18-kfw7-bh-relay.md` (OURS, `O-28`) · `../glass-ui/docs/tranches/BK/coordination/` → `valuejs-outbound-2026-09-18-kfw6-bh-relay.md` (OURS, `O-26`) + `glass-outbound-2026-09-18-valuejs-o26-reply.md` (**rowed `I-35`**, Track B's scope) · `../keyframes.js/…/coordination/` ∅ · `../sci-report/atlas/…/coordination/` ∅ · `../fourier-analysis/docs/tranches/F/coordination/` ∅. Every letter in the BK dir has ≥ 1 row in `INBOX.md` (⟨cmd⟩ per-file `grep -c`, minimum **1**). **0 unrowed letters · 0 new `I-n` · 0 new `O-n` minted by this seat; `INBOX.md` NOT written** (it is dirty with a sibling seat's uncommitted `I-35` row, which this seat neither stages nor duplicates). Mail in F.W1's scope: `I-32 A-10` — READ and consumed at §5. No wave-scoped mail is UNREAD at this close.

## 14 · Evidence roster (this file's siblings under `evidence/w1/`)

`G7-BUTTON-BUDGET.md` · `G5-CSS-CLASS-CENSUS.md` · `M7-PROP-LEVEL-DIFF.md` · `K1-RE-RESOLUTION.md` · `G14-PENCIL-BOIL-FLOOR.md` (units `b`/`c`) · **`G20-CLOSURE.md`** · **`g20-join.mjs`** (inherited, one syntax cure) · **`capture-family.mjs`** (this seat's instrument) · **`shots/run3-*` · `shots/run4-*`** (9 JPGs + `capture.json` each) · `shots/01…11-*.jpg` + `capture-records.json` + `capture-transcript.json` (the killed seat's prior run). Probe parsimony (§5.2): ONE declared capture family, run twice for the double-run law, plus two instrument-correction runs whose outputs were removed; the G6 e2e runs are the repo's own suite.

**Self-count**: no figure above counts this file or a live sibling; every count is of the settled fourier bytes at `538db90`, the read-only producer at `17a11bc5`, the frozen canonical at `f44362757458`, or this seat's own instrument outputs, each double-run.
