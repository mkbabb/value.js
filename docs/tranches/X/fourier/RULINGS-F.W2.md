SERVED MODEL: claude-opus-5[1m]

# RULINGS-F.W2 — the three contests X.F.W2 owns, recorded

**Wave**: `docs/tranches/X/fourier/waves/F-W2.md` — *value.js consumption to spec: root retirement, colour routing, facility 19*
**Seat**: unit `.c` (the rulings unit — strictly first and alone, §6a *Author-before-choose*), Track C · X·F. **Sitting 2026-09-17** (the begin-word's sitting, the convention this track's ledger rows carry); **wall clock at this seat 2026-09-18** — disclosed rather than elided, per E-3.
**Authority**: F-W2.md **§2a**'s `RULINGS-F.W2.md` **create** row — *"the record of the four rulings this wave owes: AA-20 widen-or-exclude (G7, R-i), the M-RTC cure-seat contest (G8, R-ii), **the easing cluster's two cure-shape locks — MPC-2 and CENSUS-MC — routed to F.W1 (G20, R-iii)**, and the `easing()` mapping re-derivation (G2/G15). §4 authors the proposed texts; unit `.c` records them — that recording is when G7/G8/G20 go GREEN."* Execution opened by the owner's begin-word, 2026-09-17 (`docs/tranches/X/COHESION.md` §0j).

**What this record is.** The wave's §4 authored three proposed dispositions; this file **records** them, at the bytes that exist today, with each authority, quotation and coordinate re-measured by this seat. **Recording is the gate**: G7, G8 and G20 close on this record's existence and completeness, never on a cure. Every cure named below stays at the home the bank and the census give it.

**What this record is NOT.** It mints no cure, no identity and no census row; it re-homes nothing; it **ratifies no landing** (§2's finding F-2); it pre-empts no escalation (SS-4, SS-6, SS-13); and it patches no dated spec — where a dated authority diverges from the bytes, the divergence is stated here as a dated note **beside** it, per **E-3**.

**Laws this record is written under.**

- **QUOTE-BY-COMMAND (R2-1-LAW).** Every quotation carries the ⟨cmd⟩ this seat ran, written with a **literal absolute path** and consuming **no shell variable** (R4-2.3/R4-2.5). A *"verbatim"* label without its ⟨cmd⟩ is a defect per se.
- **BYTE-TRUE EMPHASIS (R4-7.1).** A banked span is pasted with the bank's own emphasis and the bank's own extent; a subset quotation is disclosed or labelled *paraphrase*. Where a dated sibling renders a banked span with emphasis the bank does not set, the divergence is minuted (§3.4), never propagated.
- **NO BANKED COUNT OF A LIVE SIBLING OR OF THIS FILE (R3-3.10).** Figures below are **enumerations** — the enumeration IS the figure — or classifications/set-memberships. Line numbers are cited into the **frozen** adjudicated corpus (admissible, R2-2.2(i)) and into **product source**, which D-19/MEASURE-AT-OPEN governs; never into a live sibling spec, which is cited by §-heading, gate id or row label.
- **ANTI-RENAME (R-1d / R-5 / R-2).** Every id below is **banked**; this record mints none. Where a fact has a banked identity, the identity is cited and the fact is not re-booked under a census or W.L5 address.
- **CREDIT BAR (FR-GIG-5).** F.W2 claims no GREEN that F.W0, F.W1, F.W3, F.W4 or W.L6 owns.
- **WRITE-THEN-MEASURE.** Every figure published here was read from the settled bytes and **double-run**; the two runs agree.

**Command bases, written out at every use.** The frozen corpus at `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/` · the fourier tree at `/Users/mkbabb/Programming/fourier-analysis/` (absolute, never relative — R-10.6) · the value.js repository root at `/Users/mkbabb/Programming/value.js/`. **No command below consumes a shell variable, and none is `…`-elided.**

---

## 0. The substrate this record rules against (D-19 · MEASURE-AT-OPEN)

The wave's §5 figures were authored 2026-08-28 against the **pre-uplift** tree. F.W1's atomic transaction has since landed, so every predicate below is re-measured at **today's** bytes; where a dated figure and the tree disagree, both are printed and the tree governs (G-12: no superseded figure is carried forward as current).

| predicate | measured at this seat | ⟨cmd⟩ |
|---|---|---|
| fourier HEAD | `538db90 feat(fourier)!: the atomic tri-package uplift`, branch `m/w1-bump-migration`, worktree clean | `git -C /Users/mkbabb/Programming/fourier-analysis log --oneline -1` · `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain` → ∅ |
| value.js export map | `./color ./value ./css ./easing ./math ./transform ./quantize`; **`exports['.']` is `undefined`** — the key is ABSENT | `node -p "Object.keys(require('/Users/mkbabb/Programming/value.js/package.json').exports).join(' ')"` · `node -p "String(require('/Users/mkbabb/Programming/value.js/package.json').exports['.'])"` |
| bare-root specifiers in fourier | **none survive** — the five sites G1 measured RED are subpath-keyed at HEAD | `/usr/bin/grep -rn 'from "@mkbabb/value\.js"' /Users/mkbabb/Programming/fourier-analysis/web/src/` → no output |
| glass-ui at the ADOPTED pin | **8.0.0**, `./dom` present in the exports keyset (the spec's **80** is a 4.0.0 figure and is superseded, not averaged) | `node -p "const e=require('/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/glass-ui/package.json');[e.version, Object.keys(e.exports).includes('./dom')].join(' ')"` → `8.0.0 true` |

---

## 1. R-i — AA-20: the charter is **WIDENED** (gate G7)

### 1.1 The ruling, recorded

**The charter's "hand-rolled colour = `colors.ts`" scope is WIDENED to: *any code path that produces or transcribes colour values outside the token cascade*.**

**The obligation this discharges**, banked at `fr-AdminAuditLog.md:57` and pasted byte-true with the bank's own emphasis — ⟨cmd⟩ (base `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`) `/usr/bin/sed -n '57p' fr-AdminAuditLog.md`:

> **C's F.W2 contradiction is upheld and carried**: the census scopes hand-rolled colour to `colors.ts` alone; `actionTone` is a second hand-rolled colour authority — **F.W2 must widen or exclude with reasons**. **→ F.W4** (cure) + **F.W2** (charter note).

*(Disclosed cut: a subset of the `AA-20` row, which opens `- **AA-20 · C·D-4 / L-14 — MAJOR.**` and carries the density re-measure and the AA-3 fold before this span. The three bold runs above are the bank's own; none is added and none is dropped.)*

**Reason to WIDEN rather than exclude.** The wave's denominator law (**G11**) is a colour-**VALUE** census precisely because the import-keyed census was provably blind three ways — `fr-ImageUpload` row 13's six hexes in an SFC with zero `@mkbabb` imports, `fr-ContourPreview` row 22's inline SVG presentation attributes with zero declarations, and `fr-CanvasControlsDock` C-25's zero imports beside two `--viz-amber` reads. **Excluding an authority by file would rebuild exactly the blindness the census obligation exists to kill.**

**Cures stay homed; this widening mints none.** The charter is a scope statement, not a work order.

### 1.2 The G7 roster — complete, each authority widened or excluded WITH its reason

Six authorities, every one **re-measured live in the fourier tree at HEAD `538db90`**. *(The enumeration IS the figure.)*

| # | authority | banked id ⟨cited, never minted⟩ | measured at HEAD | disposition | reason | cure home |
|---|---|---|---|---|---|---|
| 1 | **`actionTone`** — five single-theme raw palette triples in an otherwise fully tokenised file | `fr-AdminAuditLog` **AA-20** (= C·D-4 / L-14) | declared `components/visualization/gallery/AdminAuditLog.vue:67`, consumed `:135` — ⟨cmd⟩ `/usr/bin/grep -rn 'actionTone' /Users/mkbabb/Programming/fourier-analysis/web/src/` → exactly those two coordinates | **WIDEN** | the bank's own words make it *"a second hand-rolled colour authority"* and name F.W2 as the charter end; a scope that names `colors.ts` alone is silent about it, and **a silent scope is a DEFECTIVE spec** | **F.W4** (cure) — *dies with AA-3's Badge cure*; F.W2 holds the charter note only |
| 2 | **`energyColor`** — a three-arm step over ℝ, unconditioned literals, total on the render path | `fr-EquationPanel` **D-3+C-5** (kill 19 / ruling 4) | declared `lib/equation/notation.ts:44`; the three literals at `:45`,`:46`,`:47`; consumed `components/visualization/EquationPanel.vue:6`,`:38` · `components/equation/EquationView.vue:7`,`:70` — ⟨cmd⟩ `/usr/bin/grep -rn 'energyColor' /Users/mkbabb/Programming/fourier-analysis/web/src/` → those five coordinates and no others | **WIDEN** | a **third** hand-rolled colour authority living wholly outside `colors.ts`; the D axis's falsifier (*"consumed EquationPanel.vue:6,38 and nowhere else"*) is banked KILLED, and the widened charter is what keeps the surviving consumers inside the denominator | **F.W4**, bound into **M-N11**'s one-edit law with D-7/D-9/M-N12 |
| 3 | **`spectrumColor` ×4** — rank-keyed frozen `hsl(h,85%,55%)` in both themes, bypassing the palette owner | `fr-BasisCanvas` **M-β1** (the ramp identity) ⊕ `fr-CoefficientsSpectrum` **B-3**/**M-10** ⊕ `fr-CoefficientsPanel` **FR-CP-2** | **four declarations** — ⟨cmd⟩ `/usr/bin/grep -rn 'const spectrumColor\|function spectrumColor' /Users/mkbabb/Programming/fourier-analysis/web/src/` → `components/visualization/lib/canvas-drawing/transforms.ts:3` · `components/equation/lib/harmonics.ts:81` · `components/equation/FrequencyGraph.vue:42` · `components/shared/CoefficientsSpectrum.vue:55` | **WIDEN** | four declarations of one colour authority, none of them in `colors.ts`; the charter's file-scope would exclude all four while the census books the identity | **F.W4 BOOKS the execution half** (`M-β1`, by name — §X.1-v4 item 5's holder table and `CENSUS-CANONICAL.md`); **F.W3 `.d`** takes the fork collapse (`shared/CoefficientsSpectrum.vue` is in §5d's Files list). **F.W2 owns the substrate ANGLE only.** ONE identity, original for life |
| 4 | **the NotationPills triad** — three colour literals that byte-transcribe the producer tokens' LIGHT arms | `fr-NotationPills` **FR-NP-9** (= C·D-4 ∘ L·L-4; ★AMENDED ruling 4; K-8) | the triad is declared at `lib/equation/notation.ts:15`,`:16`,`:17` (`hsl(6, 72%, 49%)` · `hsl(224, 58%, 46%)` · `hsl(286, 46%, 47%)`), carried into the SFC as `--pill-color` at `components/equation/NotationPills.vue:24` and consumed by the `color-mix` at `:43-45` — ⟨cmd⟩ `/usr/bin/sed -n '15,17p' /Users/mkbabb/Programming/fourier-analysis/web/src/lib/equation/notation.ts` and `/usr/bin/grep -n 'pill-color' /Users/mkbabb/Programming/fourier-analysis/web/src/components/equation/NotationPills.vue` | **WIDEN** | a cross-package duplicate-**by-value** with *no typecheck, no lint, no visual witness*; it is the wave's strongest argument for a colour-VALUE census (G11) and is invisible to every import-keyed instrument | **F.W3** (canonical home of `FR-NP-9`, *file-criterion → §5.d; legs F.W2*). **K-8's NEVER-clause binds any cure**: `var(--viz-*)` straight into the `color-mix`, **never through `VIZ_COLORS`/the JS resolver** |
| 5 | **ImageUpload's `STATIC.rainbow` duplicates** — six hexes byte-duplicating `lib/colors.ts` in an SFC with zero `@mkbabb` imports | `fr-ImageUpload` **row 13** (C:C-11) | **six distinct hexes over seven coordinates** in `components/visualization/ImageUpload.vue:150-156` (`#f87171` appears at `:150` and `:156`, the gradient's wrap-around stop), byte-equal to `STATIC.rainbow` at `lib/colors.ts:28-29` — ⟨cmd⟩ `/usr/bin/grep -n '#[0-9a-f]\{6\}' /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ImageUpload.vue` and `/usr/bin/sed -n '25,33p' /Users/mkbabb/Programming/fourier-analysis/web/src/lib/colors.ts` | **WIDEN** | the bank's load-bearing clause, verbatim ⟨cmd⟩ `/usr/bin/sed -n '47p' fr-ImageUpload.md`: the duplicates are *"invisible to any import-specifier-keyed F.W2 inventory."* A charter keyed to `colors.ts` would exclude the copy while naming the original | **dies with roster 5's cure**; F.W2 books only the census obligation. The theming arm stays KILLED (ruling 6) and is not revived here |
| 6 | **ContourPreview's inline amber** — brand amber as inline SVG presentation attributes, zero declarations | `fr-ContourPreview` **row 22** (D:m-8 · L:L-9(system)), with row 1 (D:B-1 · C:D-1(a) · L:L-9) and row 14 (C:D-1(b)) | **9 sites / 2 files / five alphas**, re-measured exactly — ⟨cmd⟩ `/usr/bin/grep -n 'hsl(40' /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourPreview.vue /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourEditorCanvas.vue` → `ContourPreview.vue:45` ⊕ `ContourEditorCanvas.vue:261,:307,:308,:315,:319,:328,:329,:333` | **WIDEN** | the weakest cascade rung and invisible to CSS-only token audits: nine paint sites with **zero declarations** anywhere. This is the one roster member whose cure **is F.W2's own** (unit `.b`, ONE cut with the `--contour-stroke` mint) | **F.W2 unit `.b`** — B1⊕B2⊕B3 as ONE cut; B2's slash-alpha literal is cured **by name**, never swept as collateral |

**Excluded with reason: none.** Every authority the wave measured is **widened**; the roster carries no exclusion, and that is the ruling's whole shape — an exclusion here would have to survive G11's three blindness instances, and none does.

### 1.3 The seventh-authority question, asked and answered at the bytes

`lib/equation/notation.ts` carries **three further** colour literals — `TIER_INFO`'s `color` fields at `:26`, `:32`, `:38` — byte-identical to `energyColor`'s three at `:45`, `:46`, `:47` (⟨cmd⟩ `/usr/bin/grep -n 'hsl(' /Users/mkbabb/Programming/fourier-analysis/web/src/lib/equation/notation.ts`). **They are not a seventh authority**: the bank already books them inside roster member 2's own budget. `fr-EquationPanel.md:50` carries `M-N1` in its MINOR list with the bank's own parenthetical — ⟨cmd⟩ `/usr/bin/sed -n '50p' fr-EquationPanel.md` → the list reads `· M-N1 (six duplicate literals, two ramps) ·`, plain, the bank setting no emphasis there — and `fr-InfoCard.md:41` states the fold in the bank's own words ⟨cmd⟩ `/usr/bin/sed -n '41p' fr-InfoCard.md` *(subset, opening mid-sentence after the row's `Two channels, one three-colour alphabet, no legend;`)*: *"duplication half FOLDS → M-N1 (anchors :26/:32/:38 ≡ :45/:46/:47 — re-read whole this seat)."* **The anchors reproduce byte-for-byte at HEAD.** The widened charter therefore reaches them through roster member 2, and **no seventh row is minted** (R-2's one-home law).

### 1.4 Two banked figures that moved, recorded as divergences (never as re-grades)

1. **`energyColor`'s blast radius is 2 consumers / 2 routes at HEAD, not the banked 3 / 2.** The bank's AMENDED cell ⟨cmd⟩ `/usr/bin/sed -n '23p' fr-EquationPanel.md` enumerates *"`energyColor` → EquationPanel.vue:6/:38 **+ EquationView.vue:7/:70 + InfoCard.vue:5/:14**"*. **`InfoCard.vue` no longer exists**: it was DELETED under **COHESION §0j.D's `G-10` ruling**, whose words **soft-wrap across two source lines**, so the receipt is a word-output and the span is stated separately — ⟨cmd⟩ `/usr/bin/grep -o 'F8-REACH-01 (`InfoCard.vue`) and F8-REACH-02 (`CanvasOverlayButton.vue`): DELETE, in one' /Users/mkbabb/Programming/value.js/docs/tranches/X/COHESION.md` returns that clause, and the ruling's sentence continues *"breath**, with FR-COB-17's 8-site `:aria-pressed` lift in the **same commit**"* on the next line. It landed at fourier `5842377` — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/fourier-analysis log --oneline -1 -- web/src/components/equation/InfoCard.vue` → `5842377 refactor(F.W0): F8-REACH ruled — aria-pressed lift + rm CanvasOverlayButton`. The banked **grade, identity and cure home are untouched**; only the regression surface F.W4 must re-check is smaller by one component. Recorded for F.W4, **not re-booked**.
2. **`fr-ContourPreview` row 22's ADJUDICATED cell says *ten*; the tree says nine.** The bank's own row is internally divergent — its disposition cell reads *"cure ten sites"* while its description cell reads *"9 sites"* (⟨cmd⟩ `/usr/bin/sed -n '55p' fr-ContourPreview.md`) — and the tree agrees with the description (1 + 8 = 9, enumerated in the table above). **F.W2 cures nine, cites the bank's *ten* as the banked figure it diverges from, and mints no eleventh interpretation.**

### 1.5 Anchor drift inside the roster (D-19 — INTENT at the true bytes)

`fr-CoefficientsSpectrum`'s `spectrumColor` declaration is at **`components/shared/CoefficientsSpectrum.vue:55`**, not the spec's `:47`; `STATIC.rainbow` is at **`lib/colors.ts:27-30`** (hexes on `:28-29`), not the spec's `colors.ts:13-16` — both moved under F.W1's `3bac3d5`. **The intent resolves at the true bytes and is recorded here; no dated spec is patched** (E-3). Every other roster coordinate reproduces exactly as the spec states it.

---

## 2. R-ii — M-RTC: the producer facility is the **PRESUMPTIVE** seat, conditionally (gate G8)

### 2.1 The ruling, recorded

**Under the standing glass-ui-first law, `resolveTokenColor` / `createTokenColorCache` / `useTokenColor` at `@mkbabb/glass-ui/dom` is ruled the sanctioned seat for *token → used-value resolution*.** It **is** the banked `fr-App` **B-1** remedy — a used-value probe-element read, never string parsing — shipped, at the pin, with no uplift required.

**W.L5 ACT(2)'s `parseCssColor` + `convertColor` route is NARROWED to colour STRINGS that are not tokens**: the declared hex residual, and literal parsing where JS must see a colour. **The two prescriptions partition rather than contest** — the producer resolves the cascade, value.js parses and converts the string the cascade returns.

**The banked contest, at its own bytes** — ⟨cmd⟩ (base `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`) `/usr/bin/sed -n '72p' fr-EquationView.md`, the `M-RTC` row:

> *"The installed glass-ui 4.0.0 ALREADY SHIPS the token-colour resolver `lib/colors.ts` hand-rolls and gets wrong: `resolveTokenColor(css, el)` + `createTokenColorCache` (paint-onto-a-real-property → `getComputedStyle(el).color` → cascade-resolved `rgb(...)`; `invalidate()` documented for dark-mode flips) and `useTokenColor` (reactive, re-resolves via `useGlobalDark`) at `@mkbabb/glass-ui/dom` … It never PARSES token text — precisely the four-regex failure mode of `cssVarToHex` — and it IS the "used-value probe-element read, never string parsing" remedy the banked fr-App B-1 record demands."*

*(Disclosed cut at the ellipsis: the elided clause is the row's exports-keyset receipt, *"present in the 4.0.0 exports AND the v7 tag keyset (both seat-verified)"*, superseded as a pin figure by §2.2 below and therefore not quoted as current.)*

**Re-measured at the ADOPTED pin (D-19 / F-W0 §4 gate `G-13`: hash, never a version string).** glass-ui **8.0.0**, `./dom` present in the exports keyset; all three symbols present in `dist/dom.js`; both typings present — ⟨cmd⟩ `/usr/bin/grep -o 'resolveTokenColor\|createTokenColorCache\|useTokenColor' /Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/glass-ui/dist/dom.js | sort -u` → the three names; ⟨cmd⟩ `/bin/ls /Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/glass-ui/dist/composables/dom/useTokenColor.d.ts /Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/glass-ui/dist/composables/dom/useResolveTokenColor.d.ts` → both. **The spec's 80-key figure is a 4.0.0 reading and is superseded, not averaged.**

### 2.2 The two conditions — **both escalated, both INTACT, neither pre-empted**

| condition | what it asks | status measured at this seat |
|---|---|---|
| **(a) SS-6 — the GLASS-RELAY ask** | confirm the facility as the **sanctioned consumer seat** (ask 1 of the three the relay carries) | **OPEN.** No relay letter exists: ⟨cmd⟩ `/bin/ls /Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/` → five letters, none a `value-to-glassui-*-token-color-seat` — the SS-6 vehicle is unit `.d`'s create row and is unwritten. **Producer rows ride mail; glass-ui is READ-ONLY always.** |
| **(b) SS-13 — the serialization question** | what `getComputedStyle` serializes for an `oklch()` token — **drop-in or format adapter** | **OPEN.** Named at `fr-EquationView.md:180` as SS-13 item 10 (⟨cmd⟩ `/usr/bin/sed -n '180p' fr-EquationView.md`), and unanswered in tree; it is a **live-visual** obligation (UNPROVEN-NEEDS-LIVE), under binding probe parsimony. |

**If either returns against, ACT(2)'s fourier-side context resolution stands as the fallback exactly as written.** The landing is F.W1's either way (the banked `fr-BasisCanvas` D-1 identity); this ruling is the charter correction **M-RTC** demands, and its function is to keep ACT(2) from **re-implementing a producer surface**.

### 2.3 Finding F-2 — the seat shipped before the ruling; this is a RULING, not a ratification

**Measured**: `createTokenColorCache` from `@mkbabb/glass-ui/dom` is **live** at `web/src/lib/colors.ts:20`, landed by F.W1 at `538db90`/`3bac3d5` — ⟨cmd⟩ `/usr/bin/sed -n '19,22p' /Users/mkbabb/Programming/fourier-analysis/web/src/lib/colors.ts` → `import { createTokenColorCache } from "@mkbabb/glass-ui/dom";` · `import { toRgba8 } from "@mkbabb/value.js/color";` · `import { parseCssColor } from "@mkbabb/value.js/css";`. The cache is instantiated at `:94`, and the three regex arms the contest was about — `cssVarToHex`, `hslToHex`, `rgbToHex` — are **gone from the file** (⟨cmd⟩ `/usr/bin/grep -n 'cssVarToHex\|hslToHex\|rgbToHex' /Users/mkbabb/Programming/fourier-analysis/web/src/lib/colors.ts` → no output).

**Three consequences, stated so no later reader mistakes the order of events:**

1. **The ruling rests on its own reasons, not on the landing.** Its ground is the standing glass-ui-first law plus the banked `fr-App` B-1 remedy — both true before `538db90` and unchanged by it. A ruling written as *"we rule what already shipped"* would be a ratification, and a ratification is not what §2a's create row obliges or what G8 measures.
2. **Both conditions survive the landing intact.** SS-6 and SS-13 are **not** discharged by the seat being in the tree; `.d` still carries the SS-6 ask, and SS-13's serialization answer still decides drop-in vs format adapter. A shipped consumer is evidence the seat is *usable*, never evidence it is *sanctioned*.
3. **If a condition returns against, the obligation is a cure at the landed bytes** — the fourier-side fallback ACT(2) already specifies — **not a re-opening of this ruling**. The partition (producer resolves tokens · value.js parses non-token strings) is the ruled shape either way.

**G8's RED input, checked rather than assumed.** The gate's break condition is *"Author ACT(2)'s fourier-side resolver without ruling the contest — that re-implements a producer surface."* Measured at the landed bytes: the resolver **consumes** the producer facility (`createTokenColorCache` at `:20`/`:94`) and routes only the **used value** — a non-token string — through value.js (`parseCssColor` at `:80`'s `cssColorToHex`, `toRgba8` from `./color`). **Nothing re-implements the producer surface.** The gate's substance passes, and the record is what closes it.

---

## 3. R-iii — the easing cluster's two cure-shape locks: **ROUTED TO F.W1, NOT RULED HERE** (gate G20)

### 3.1 `fr-MorphPhaseConfig` **MPC-2 · C-2 ⊕ L-D16** — carried verbatim, contested, not swept

Banked at `fr-MorphPhaseConfig.md:46`, pasted with the bank's own emphasis and extent — ⟨cmd⟩ (base `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`) `/usr/bin/sed -n '46p' fr-MorphPhaseConfig.md`:

> `| **MPC-2 · C-2 ⊕ L-D16** | Easing catalog cannot resolve at the value.js bump: bare-root specifiers, no `"."` export at 4.0.0, `timingFunctions` absent from every entry point. | **FOLD → fr-EasingPicker L/B-1 + C/B-1 · F.W1** (lockstep tri-package bump per MPC-14, never a specifier rewrite). |`

**HOME: F.W1**, by the row's own fold — the disposition is a FOLD **into an F.W1-homed identity** (`fr-EasingPicker L/B-1 + C/B-1`, carried at **F-W1 §2 WU-K's `L/B-1 + C/B-1` row**), so the cure-shape clause belongs to that home.

**The collision, stated plainly.** F-W2.md **§2a rows 1–4** prescribe `specifier → /easing` across four fourier files (`web/src/lib/easings.ts` · `web/src/components/equation/ConvergencePlot.vue` · `web/src/components/equation/composables/useCurveTransition.ts` · `web/src/components/equation/lib/harmonics.ts`) — which the lock's parenthetical, *"never a specifier rewrite"*, appears to forbid.

**The measurement F.W2 owes and puts on the record — G1's 5 of 5.** Against value.js's own 4.0.0 export map, **five of five bare-root sites raise `ERR_PACKAGE_PATH_NOT_EXPORTED`**, because `exports['.']` is **absent** (§0's receipt: `node -p "String(require('/Users/mkbabb/Programming/value.js/package.json').exports['.'])"` → `undefined`). **Therefore a lockstep tri-package bump ALONE does not make a bare-root specifier resolve** — that is the whole of F.W2's contribution to the contest, and it is a measurement, not a preference.

**Ruling: F.W2 does not resolve this.** It (a) carries the lock verbatim, (b) puts the bearing measurement on the record, and (c) routes the contest to the **F.W1 cross-edge**. **No pre-emption.** Had F.W1 ruled the lock literal, §2a rows 1–4's cure-shape would change and the spec would be amended **by dated addendum, never silently**.

### 3.2 `fr-EasingCurvePreview` **CENSUS-MC (C-D-5)** — an amendment to G10's premise, carried

Banked at `fr-EasingCurvePreview.md:58`, byte-true (**the bank sets NO emphasis anywhere inside this cell**) — ⟨cmd⟩ `/usr/bin/sed -n '58p' fr-EasingCurvePreview.md`:

> `| **CENSUS-MC** (C-D-5) | `@mkbabb/glass-ui/motion-curves` exists at the PINNED 4.0.0 (seat-read exports map) and value-re-exports every easing symbol `lib/easings.ts` imports bare-root (both readers verified the dist module; exception `timingFunctions` holds) — the value.js-specifier half of the scheduled migration is NOT blocked on the uplift. | **F.W1** — census §1/§3 rows (`CENSUS:94-95,188,191`) AMENDED; sequencing input only (component-altitude ruling R-10). |`

**HOME: F.W1** (sequencing input; component-altitude ruling R-10).

**Ruling: G10's premise is NARROWED, not defended.** What was gated on F.W1 is the **bare-root → `./easing` re-point measured against value.js's own 4.0.0 export map**, held from below by the keyframes → `value.js ^0.13.0` hard-dep (`fr-EquationView` **C·D-11**). CENSUS-MC's **producer-re-export** route is a *different* route, whose availability F.W2 records and does not choose. **The route choice is F.W1's**, and A2's `timingFunctions` re-derivation (§4 below) is required under **either**.

### 3.3 The minute that completes R-iii: **COHESION §0o ESC-4 has since answered it, at F.W1's end**

R-iii routed both locks to F.W1 and pre-empted nothing. **F.W1's end answered, under the begin-word's delegation** — `docs/tranches/X/COHESION.md` **§0o**'s ruling table, row **ESC-4** (cited by section heading and row id; no line coordinate into a live sibling, R2-2). The ruling's lead is receipted as word-output — ⟨cmd⟩ `/usr/bin/grep -o '\*\*DRIFT REFUSED — no acceptance, silent or explicit\.\*\*' /Users/mkbabb/Programming/value.js/docs/tranches/X/COHESION.md` → `**DRIFT REFUSED — no acceptance, silent or explicit.**` — and the row reads:

> *"**DRIFT REFUSED — no acceptance, silent or explicit.** Measured at the published bytes: value 4.0.0's `./easing` carries 8 analytic survivors; keyframes.js's source carries 8 names; **no producer ships the 22**. The 14 orphans … are **defined analytically inside the corpus's own `web/src/lib/easings.ts`** …, the 8 survivors re-point to `@mkbabb/value.js/easing`, and `easings.ts:58`'s `as EasingFn` cast falls with the `timingFunctions` lookup. **The gate is MPC-5's sampler re-run: every one of the 22 samples EQUAL (Δ = 0 at every sample point) to the pre-bump 0.13.0 function under the same key**, so the hop changes no motion; RD-5 RESTORE is thereby executed, not re-priced. No CubicBezier approximation is admitted for any key."*

*(Two disclosed cuts, both parenthetical enumerations of the 14 orphan names and of the file's ownership of the catalogue; neither carries a ruling word.)* Its **released rows** are `L/B-1` · `MPC-5/RD-5` · *the six bare-root import lines* · *limb 1's lockstep half*.

**What that does to each lock, recorded and never re-opened:**

- **MPC-2's *"never a specifier rewrite"*** — answered **at the F.W1 end, in F.W1's favour on the measurement F.W2 supplied**: the bare-root lines were released to F.W1 and re-pointed there, because a lockstep bump alone could not make them resolve (G1's 5/5). The specifier rewrite happened **as the transaction**, not as a consuming wave's unilateral edit — which is precisely what the lock protected against. **The routing was correct and is now discharged.**
- **CENSUS-MC's route choice** — answered by **election of neither producer re-export route**: ESC-4 measures that *"no producer ships the 22"* and rules the 14 orphans defined in the corpus's own file with the 8 survivors re-pointed to `@mkbabb/value.js/easing`. **F.W2 recorded both routes and chose neither; F.W1 chose, and the choice is a third shape both records admit.**
- **G15's banked divergence figure is superseded by ESC-4** (`14 of 22` · `8/22 material` · `max Δ 0.192` → **Δ = 0 at every sample point, all 22 keys**). **F.W2 cites ESC-4 by id and never re-opens it**; the wave's §5 amendment lands as a dated addendum-beside, per E-3, and the Δ = 0 sampler re-run is **unit `.a`'s gate**, not this record's.

**This record states the answer; it does not claim it.** ESC-4 is F.W1's ruling under the owner's delegation, and **F.W2 claims none of its GREEN** (FR-GIG-5).

### 3.4 One emphasis divergence, minuted (E-3 — a note beside, never a patch)

F-W2.md **§4 R-iii** renders MPC-2's parenthetical as ***"lockstep tri-package bump per MPC-14, never a specifier rewrite"*** — bold **and** italic. **The bank sets no emphasis there**: its bold closes at `· F.W1`, before the parenthetical opens (the ⟨cmd⟩ output in §3.1 above is the witness). The **words are exact**; only the rendering diverges, and it is the R4-7.1 class the spec itself polices at six other sites. **Recorded here at the bank's own emphasis; the dated spec is not edited.**

---

## 4. The `easing()` mapping — **RE-DERIVED at the bytes, never transcribed** (G2 · the record leg of G15)

**What W.L5 ACT(1) prescribed**: `timingFunctions` → `easingNames()` + `easing(name)`. **Both prescribed symbols are absent at 4.0.0** — ⟨cmd⟩ (cwd `/Users/mkbabb/Programming/value.js`) `/usr/bin/grep -rn 'easingNames\|timingFunctions' src/ dist/` → no output. **A mapping cannot be transcribed from a prescription whose symbols do not exist; it has to be re-derived.** This is that re-derivation, run at the **landed** bytes of both ends.

### 4.1 The producer end — value.js 4.0.0's actual easing surface

| fact | measured | ⟨cmd⟩ (cwd `/Users/mkbabb/Programming/value.js`) |
|---|---|---|
| the resolver's signature | `export function easing(name: string): Result<EasingFunction, EasingIssue>` at `src/easing.ts:166` | `/usr/bin/grep -n 'export function easing' src/easing.ts` |
| resolution ORDER (the load-bearing fact) | `DIRECT_EASINGS` **first**, `PRESETS` second: `if (Object.hasOwn(DIRECT_EASINGS, name)) return ok(...)` then `if (!(name in PRESETS)) return err({code:"easing_name_unknown"})` then `CubicBezier(x1,y1,x2,y2)` | `/usr/bin/sed -n '166,171p' src/easing.ts` |
| `DIRECT_EASINGS` | block opens `:94`, closes `:114`; **19 entries** = ten base names ⊕ nine hyphenated aliases (`linear` alone has no alias) | `/usr/bin/grep -n 'Object.freeze' src/easing.ts` · `/usr/bin/grep -n '^});' src/easing.ts` · `/usr/bin/sed -n '95,113p' src/easing.ts \| /usr/bin/grep -c ','` → `19` |
| `./easing`'s published surface | **16 exports**; `bezierPresets` **30 keys** | `node -e "import('/Users/mkbabb/Programming/value.js/dist/subpaths/easing.js').then(m=>{console.log(Object.keys(m).length, Object.keys(m.bezierPresets).length)})"` → `16 30` |

### 4.2 The partition, re-derived by **identity**, not by membership

The catalogue's 22 keys were classified by asking `easing(k).value === <the named export>` — the only test that distinguishes a DIRECT hit from a `PRESETS` fall-through, since **all 22 keys are also members of `bezierPresets`** and a membership test answers the wrong question:

⟨cmd⟩ (runnable from any cwd; no shell variable, no elided path) `node -e "import('/Users/mkbabb/Programming/value.js/dist/subpaths/easing.js').then(m=>{const n={'linear':m.linear,'ease-out-cubic':m.easeOutCubic,'ease-in-out-sine':m.easeInOutSine,'ease-in-out-cubic':m.easeInOutCubic,'ease-in-out-quad':m.easeInOutQuad,'ease-in-out-expo':m.easeInOutExpo,'ease-in-out-circ':m.easeInOutCirc,'ease-out-expo':m.easeOutExpo};const k=['linear','ease-in','ease-out','ease-in-out','ease-in-back','ease-out-back','ease-in-out-back','ease-in-quad','ease-out-quad','ease-in-out-quad','ease-in-cubic','ease-out-cubic','ease-in-out-cubic','ease-in-sine','ease-out-sine','ease-in-out-sine','ease-in-expo','ease-out-expo','ease-in-out-expo','ease-in-circ','ease-out-circ','ease-in-out-circ'];const d=[],f=[];for(const x of k){const v=m.easing(x).value;(n[x]&&v===n[x]?d:f).push(x)}console.log('DIRECT',d.length,d.join(' '));console.log('FALLTHROUGH',f.length,f.join(' '))})"` → `DIRECT 8 …` · `FALLTHROUGH 14 …`, the two lists printed in full and reproduced below.

- **8 resolve DIRECT (analytic, producer-published)**: `linear` · `ease-in-out-quad` · `ease-out-cubic` · `ease-in-out-cubic` · `ease-in-out-sine` · `ease-out-expo` · `ease-in-out-expo` · `ease-in-out-circ`.
- **14 fall through to `PRESETS` → `CubicBezier`**: `ease-in` · `ease-out` · `ease-in-out` · `ease-in-back` · `ease-out-back` · `ease-in-out-back` · `ease-in-quad` · `ease-out-quad` · `ease-in-cubic` · `ease-in-sine` · `ease-out-sine` · `ease-in-expo` · `ease-in-circ` · `ease-out-circ`.

**The enumeration IS the figure**, and it corroborates the bank from the tree. The banked span, pasted with the bank's own emphasis — its bold opens at *but* and closes at *approximation*, before the parenthetical — ⟨cmd⟩ (base `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`) `/usr/bin/sed -n '52p' fr-EasingPicker.md`:

> *"**LC's sharpenings RATIFIED with CC-2**: all 22 morph keys DO resolve through `easing(): Result` at 4.0.0 (PRESETS carries ease-in/out/in-out :37-39 and the three back rows :62-64), **but 14 of 22 silently drift analytic→CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo) — the morph curves change shape across the bump, unbooked anywhere else."*

**`ease-out-cubic` and `ease-out-expo` are among the eight that STAY ANALYTIC**, exactly as the bank writes them and exactly as the identity probe measures them; the fourteen are ESC-4's *"14 orphans"*. **Two ends, two instruments, one partition** — which is what re-derivation means and transcription never establishes.

### 4.3 The consumer end — what LANDED in fourier, read at HEAD

`web/src/lib/easings.ts` (⟨cmd⟩ `/usr/bin/sed -n '1,35p' /Users/mkbabb/Programming/fourier-analysis/web/src/lib/easings.ts` and `/usr/bin/sed -n '95,200p' /Users/mkbabb/Programming/fourier-analysis/web/src/lib/easings.ts`):

- **Eight named imports from `@mkbabb/value.js/easing`** — `linear, easeInOutQuad, easeOutCubic, easeInOutCubic, easeInOutSine, easeOutExpo, easeInOutExpo, easeInOutCirc` — **the same eight** §4.2 measures as DIRECT. The two ends agree without either copying the other.
- **Fourteen defined in-file**: six **bézier-authored at 0.13.0 too** (`ease-in`/`ease-out`/`ease-in-out` ⊕ the three `back` arms) evaluated by the file's own `cssCubicBezier` (Newton-Raphson 8 iterations ⊕ 64-step bisection, ε 1e-6), and eight **analytic closed forms** (`easeInQuad`, `easeOutQuad`, `easeInCubic`, `easeInSine`, `easeOutSine`, `easeInExpo`, `easeInCirc`, `easeOutCirc`). 6 ⊕ 8 = 14; 14 ⊕ 8 = the catalogue's 22.
- **`EASING_FNS` is exhaustive by construction** — one entry per `EASING_LABELS` key — so **no lookup can fall through**, and the banked `as EasingFn` hazard is structurally gone: the deleted `timingFunctions[name] as EasingFn` cast is what typechecked a miss and threw on the first rAF tick.
- **`getEasingFn`'s `?? EASING_PRESETS.linear.fn`** is now a fallback over a **total** map, not the miss-swallow A2 warned of; `EASING_PRESETS` is still built **once** at module scope by `Object.fromEntries`, which is the shape that neutralises G16.
- **No `easing()` call and no `Result` unwrap survive in the file.** A2's unwrap lock is therefore **moot at these bytes by construction** — recorded as a measurement, with **F-W1 §3 gate `G12`** (*easing re-point verified by execution*) cited as the gate that owns it. **F.W2 claims no part of that GREEN.**

### 4.4 What this record does and does not close

**Recorded**: the mapping, re-derived at both ends, with its 8/14 partition, its resolution order, and the landed catalogue's shape. **W.L5's *"mapping recorded"* obligation now has a record to point at.**
**Not closed here**: G2's cure-side reading and **G15's Δ = 0 sampler re-run** (MPC-5, per ESC-4) are **unit `.a`'s**, measured at its own clock against the landed bytes — a ruling record cannot and does not stand in for a probe.

---

## 5. Gate readings — BEFORE → AFTER

| gate | claim | before (§5 born-RED, re-measured at open) | after this record | what closed it |
|---|---|---|---|---|
| **G7** | the charter names every hand-rolled colour authority | **RED** — `/bin/ls /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/RULINGS-F.W2.md` → *No such file or directory* (the wave-open reading, banked in the execution record); six authorities against a charter naming one | **GREEN** | **§1** records R-i with the complete six-authority roster, **each widened WITH its reason**, cures homed, zero minted; the seventh candidate answered at the bank (§1.3) |
| **G8** | the cure-seat contest is ruled before ACT(2) is authored | **RED** — the ruling had no record to live in; the facility verified live at the pin | **GREEN** | **§2** records R-ii: producer facility = presumptive seat; ACT(2)'s route narrowed to non-token STRINGS; both conditions escalated and intact; the landing **ruled on, never ratified** (§2.3); the RED input measured **not** to have occurred |
| **G20** | the banked easing cluster is CITED, never re-minted, and its two locks dispositioned without pre-emption | **RED** — no record; the locks uncited at the fold seat | **GREEN** | **§3** records R-iii: MPC-2 and CENSUS-MC each quoted at its coordinate with **F.W1** named as home, the §2a-rows-1–4 collision stated, **G1's 5/5** on the record, no pre-emption — plus the minute that **ESC-4** answered it at F.W1's end |
| **G2** | the `timingFunctions` mapping is re-derived, not transcribed | **RED** — both prescribed symbols 0 hits; the mapping had no record | **record leg GREEN**; gate closure is `.a`'s | **§4** re-derives the mapping at both ends (8/14 by identity, resolution order, landed catalogue) |
| **G15** | the easing migration declares its curve drift | **RED** — and **RE-SHAPED**: the banked 14/22 · 8/22 figure is superseded by **ESC-4** (Δ = 0, all 22) | **record leg GREEN**; the Δ = 0 re-run is `.a`'s | **§3.3** cites ESC-4 by id, never re-opens it; **§4.2** enumerates the partition the re-derivation rests on |

**No other gate is touched by this unit, and no GREEN of F.W0, F.W1, F.W3, F.W4 or W.L6 is claimed** (FR-GIG-5).

---

## 6. Residuals carried out of this unit

1. **SS-6** (the sanctioned-seat confirmation) and **SS-13** (the `getComputedStyle` oklch-vs-rgb serialization) are **OPEN**, and R-ii's fallback stands if either returns against — unit **`.d`** carries the relay; glass-ui is **READ-ONLY always**.
2. **G15's Δ = 0 sampler re-run** (MPC-5, per ESC-4) and **G2's cure-side reading** are unit **`.a`'s**, at its own clock.
3. **The dated-spec amendments this record's measurements imply** — G15's re-shape by ESC-4, the glass-ui 80 → 70-key pin figure, `colors.ts`'s line count, `CoefficientsSpectrum.vue:47 → :55`, `colors.ts:13-16 → :27-30`, and `energyColor`'s 3 → 2 consumers — land as **dated addenda-beside**, never as patches of `F-W2.md` (E-3).
4. **`fr-ContourPreview` row 22's banked *ten*** stands as a banked divergence against the tree's nine; unit `.b` cures **nine** as ONE cut and mints no eleventh interpretation.
5. **F-W2.md §4 R-iii's emphasis divergence** (§3.4) is minuted, not patched.

**E13**: swept at this seat's clock (2026-09-18 15:42 EDT) against the four coordination paths ⊕ the Track-C fourier mail-ledger surface — ⟨cmd⟩ `/usr/bin/find <each path> -maxdepth 1 -type f -name '*.md' -newermt "2026-09-18 14:30"` → `docs/tranches/V/coordination/INBOX.md` (**self**, SELF-COUNT law) and `../glass-ui/docs/tranches/BK/coordination/glass-outbound-2026-09-18-valuejs-o26-reply.md` (**already rowed I-35**, whose Routing cell reads *"X·KF (Track B), NOT X-W1"*). **0 unrowed · 0 new `I-n` · 0 UNREAD in unit `.c`'s scope.** No row of `INBOX.md` was written by this seat.
