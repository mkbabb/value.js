SERVED MODEL: claude-opus-5[1m]

# DECISIONS-F.W4 — the ten decisions X.F.W4 authors

**Wave**: X.F.W4 (Track C · X·F · `fourier-analysis`) · **Unit**: `.i`, first and alone · **Date**: 2026-09-18 (sitting of record 2026-09-17; wall clock 2026-09-18 EDT).
**Spec of record**: `docs/tranches/X/fourier/waves/F-W4.md` **§3** (`:353-369`, D1..D10 whole), read at the bytes this round. **§3 is the operand; every mechanism below is quoted from it, and where a decision's mechanism lives at a cure site the cure site is quoted beside it.**
**Gate turned**: `G-F4-DECISIONS` — *"`DECISIONS-F.W4.md` carries D1..D10, each RULED or DEFERRED-WITH-DEFAULT, and no cluster depending on an unruled decision has opened"* (F-W4 §4, true bytes `:391`).
**Rulings consumed**: `docs/tranches/X/COHESION.md` **§0o ESC-2** (D9, already ruled — transcribed, never re-opened) · **§0i.3** (ESC-1: glass **8.0.0** @ `17a11bc5` — every `⊘ ESC-1/G1-GATED` cell reads on its 8.0.0 arm) · **§0j.D G-15(b)(c)(d)** · **§0j** the begin-word.
**Execution record**: `docs/tranches/X/execution/C/F-W4.md` (seat-0 open: baseline, 18-gate reading, the three `greenBeforeCure` findings, the unit plan).

**This record writes no fourier byte and authors no cure.** It decides; the surface units execute. `F-W4.md`, the adjudicated registry, `CENSUS-CANONICAL.md`, the carries and every prior conformance artefact are **IMMUTABLE (E-3)** — nothing below amends one; corrections travel as dated addenda-beside.

---

## §0 Reading law — binding on every verdict below

1. **Two verdict words, and only two.** **RULED** — an arm is chosen; the executing unit builds that arm *and no other*, and a seat that builds the other arm is defective on sight. **DEFERRED-WITH-DEFAULT** — no arm is chosen, and a **default is carried that is itself executable**: the executing unit performs the default, states that it did, and names the holder the question went to. A deferral with no executable default is not a deferral, it is a hole, and this record contains none.
2. **The honest default is the shipped state unless a branch is cheaper *and* provably risk-free.** §3's own frame — *"Each is ruled in `DECISIONS-F.W4.md` with the honest default carried absent a ruling and the cost of each branch stated"* — makes the default a first-class object, not a fallback. Where §3 states a default in terms (D1: *"Default: DELETE"*), that default governs and is adopted.
3. **The cost of EACH branch is stated, in §3's own words where §3 has them.** A decision whose losing branch has no priced cost was not decided; it was preferred.
4. **Every recorded dissent is preserved verbatim, at its own decision.** Three are named by the unit's charter — D1's **D/C BLOCKER** dissents, D2's **D-axis LOG minority**, D4's **open design dissent** — and each is carried below in the words its record uses. A dissent is preserved so the *argument* is inherited, not only the disposition (`fr-CoefficientsPanel` §"the R-7 ruling's minority position": *"preserved so the F.W4 spec inherits the argument, not just the disposition"*).
5. **The ESC-1 branch is settled: 8.0.0.** COHESION §0i.3 rules `v8.0.0` @ `17a11bc5`; F.W1 landed the uplift as ONE commit (`538db90`). Every `⊘ ESC-1/G1-GATED` cell a decision touches reads on its **8.0.0 arm**; the 7-branch alternatives are struck. **Cite, never re-open.**
6. **Anti-rename.** Banked ids are original for life. Where this record names a row it uses the record's own spelling, and where a local alias exists it is marked as a spelling, never a rename.
7. **⊘ This record decides nothing §5.2 routes away.** F-W4 §5.2's **`→ NO-WAVE-OWNER / SS-3/SS-4 spec authoring`** row (true bytes `:414`) names, in terms, *"HLG-35's shared-domain design decision (D7) · FR-EQC-12 ⊕ FR-EQC-1 joint product decision (D3) · PAW-44's retire-or-restore dissent (D4) · FM-21's scaling-system owner question (D10)"* as **consumed, not settled here**. Where a decision sits on that row, this record carries the default and names the holder; it does not manufacture a design ruling the spec routed elsewhere. **D3 is the one member of that list this record rules, and §2 D3 states exactly why the routed half and the ruled half are different questions.**

---

## §1 The verdict table

| id | subject | verdict | the arm that executes | executor |
|---|---|---|---|---|
| **D1** | FR-GFC-9 / GM-F1 — the marquee DELETE-OR-REVIVE | **RULED** | **DELETE** (§3's own stated default, adopted) | `.d` |
| **D2** | FR-CP-R-7, two arms | **(a) RULED · (b) DEFERRED-WITH-DEFAULT** | (a) the selection contract **DELETES**; (b) default **keep the shipped linear arm, wire no control** | `.b` |
| **D3** | FR-EQC-residue ⊕ FR-EQC-12 ⊕ FR-EQC-1 | **RULED** | **RE-WORD, do not fill `#graph`**; FR-EQC-1's honesty repair lands on the three existing surfaces | `.b` |
| **D4** | PAW-44 under LAW-3 — retire or restore the sticky stack | **DEFERRED-WITH-DEFAULT** | default **neither restore nor retire**; the stack is carried INERT-BY-MEASUREMENT and LAW-4's constant is authored once against the inert geometry | `.e` |
| **D5** | PAW-52 under LAW-5 — URL addressability | **DEFERRED-WITH-DEFAULT** | default **POSED AND BLOCKED**; PAW-38's slug uniquification named as the blocking prerequisite and relayed | `.e` |
| **D6** | THE VITEST FLOOR (charter item, §2.L) | **RULED** | **a vitest runner in `web/`** — not the Vite-build-plugin arm; the four assertions named with their import homes | `.g` |
| **D7** | HLG-35 — the shared-domain design decision | **DEFERRED-WITH-DEFAULT** | default **the shipped two-domain rails stand; book the row POSED, land no rename**; routed SS-3/SS-4 | `.a` |
| **D8** | ECD-D-2/L-3/C-1 (+C-13/C-22; K-8) | **RULED** | **DELETE WHOLE** — the five-part unit as one block; ⊘ never half-land | `.c` |
| **D9** | PS-L-4 (+M6) — the ToC owner | **RULED — at COHESION §0o ESC-2**, transcribed here | **ONE owner**, typed `InjectionKey`; F.W4 executes the collapse | `.e` |
| **D10** | the posed-with-default batch (five legs) | **DEFERRED-WITH-DEFAULT ×5** | each leg's default stated; each routed to its holder. **This record states defaults; it does not answer them.** | `.a` · `.b` · `.c` |

⊘ **Nothing in this table is a preference.** Each row's losing branch is priced at its own section below, and three rows (D4 · D5 · D7) are deferrals *because the spec routed them*, not because they were hard.

---

## §2 The decisions

### D1 · ⊘ FR-GFC-9 / GM-F1 — DELETE-OR-REVIVE (the marquee family) — **RULED: DELETE**

**§3's bytes, quoted whole:**

> **D1 · ⊘ FR-GFC-9 / GM-F1 — DELETE-OR-REVIVE (the marquee family).** |filter(S)| ≤ |S| ⇒ the mount predicate is false for ALL inputs; the D.W4.c empty state never ships. Every GalleryMarquee row is this decision's cure list — none independently schedulable; GM-24 proves deletion saves REAL BYTES at every build (no bundler can prove the `v-if` conjunction false). D/C BLOCKER dissents preserved. Default: DELETE.

**The cure site says the same thing in its own words** (F-W4 §2.J, true bytes `:168`): *"⊘ FR-GFC-9 / GM-F1 — **THE DELETE-OR-REVIVE RULING (= §3 D1), this wave's ONE authored decision for the marquee family**: the mount predicate is false for ALL inputs (0≥4); every GM row below is that decision's CURE LIST, none independently schedulable; GM-24 proves deletion saves real bytes at every build; D/C BLOCKER dissents preserved."*

**Mechanism at the corpus.** ⟨cmd⟩ `grep -n 'GM-F1' fr-GalleryMarquee.md` → `:29` *"**GM-F1 = D-1/L-1/C-1 — the dead mount → FR-GFC-9 + fr-GalleryCard G-LC1 (MAJOR banked; D/C BLOCKER dissents preserved).** Re-verified by this seat: GalleryView.vue:266 `!gallery.entries.length && !gallery.loading` ∧ :270 `featuredEntries.length >= 4` with :64-66 `featuredEntries = entries.filter(tier==="featured")` — |filter(S)| ≤ |S| ⇒ 0 ≥ 4, false always; GalleryMarquee.vue:27 duplicates the same dead test. → **F.W4** (the one authored decision: delete or revive; every §B row is that decision's cure list)."*

**Branch — REVIVE.** Cost: the mount must be made reachable (FR-GFC-9's own sketch: *"mount it reachably — e.g. hoist above the empty-state guard with its own ≥4 predicate"*), and **the whole latent roster becomes live work in the same breath** — every `GM-*` row is graded *as-if-live* precisely against this branch, so REVIVE converts a 134-line dead component into a repair surface carrying the geometry family (GM-1 seam g/2 · GM-2 no-tile/half-blank · GM-20's wrong-box mask · GM-M3's min-content lock, *"928px at the shipped minimum (2.5× a 375px phone), 4704px at 20 entries"*), the a11y family (GM-F2's `aria-hidden` over a focusable Delete-bearing subtree — whose live arm is currently **KILLED by unreachability** at fr-GalleryCard R-1/K-9 and **re-opens under this branch, by the record's own self-mark**: *"moot while FR-GFC-9/G-LC1 hold; re-opened only by the revival ruling"*), GM-19's PRM guard that *has never applied* (**inert by specificity**, and §4 `G-F4-NEG-ROSTER` forbids its certification under any branch), and GM-18's sub-AA mask band. It also re-opens the SS-13 probe list this wave otherwise defers. **REVIVE is the expensive branch and it is expensive in exactly the dimension this wave is short of.**

**Branch — DELETE.** Cost: the authored "living preview band" the empty gallery was redesigned around is given up as a design, and the empty state that ships is the plain one (FR-GFC-9: *"The 'living preview band' the empty gallery was redesigned around is unreachable code"*). **Gain, and it is measured, not asserted** — ⟨cmd⟩ `grep -n 'GM-24' fr-GalleryMarquee.md` → `:153` *"**GM-24 = RR-B missed find — INFO.** The dead component's shipping cost is provable STATICALLY: GalleryView.vue:26 is a static import whose symbol is referenced in the live render at :269, and no bundler can prove the `v-if` conjunction false — so the 134 lines and the scoped CSS ship in the `/gallery` chunk **by construction, at every build**"*.

**VERDICT — RULED: DELETE.** §3 states the default in terms and the default is adopted, on GM-24's static proof: the dead component is paid for at every build by construction, and the branch that keeps it must first pay for a roster graded as-if-live on a surface no one has ever watched. `.d` executes DELETE and **no other arm**; every `GM-*` row is this decision's cure list and **none is independently schedulable** (§3), so `.d` lands them as one governed block.

**Dissents preserved (verbatim, both recordings).** The banked grade is **MAJOR**; the **D and C axes filed BLOCKER**, and both the first and the second hostile pass carried the dissent forward rather than resolving it: `fr-GalleryMarquee.md:29` — *"(MAJOR banked; **D/C BLOCKER dissents preserved**)"* — and `:159` — *"all 33 distinct identities fold cleanly: the dead mount (GM-F1/FR-GFC-9 — **BLOCKER dissents preserved again**)"*. ⊘ **The dissents are not disposed of by this ruling**: DELETE settles *what to do*, not *how grave the miss was*, and a BLOCKER-graded dead mount is the record of a design that shipped unreachable and was never seen. Both preserved, unamended.

**Dependent cluster.** `.d`'s whole `GM-*` family; `FR-GFC-28`'s denominator re-derivation (FR-GFC-9 puts `GalleryMarquee:29,36,52` in unreachable code — F-W4 §0's corrected-denominator bullet, and the re-derivation runs FIRST); `FR-GFC-2`'s card fan-out re-scope (*"the card fan-out is 2 live callsites"*); F.W1's already-landed `./scrolling-text` uplift question. ⊘ `GM-18`'s `aria-hidden`-over-focusable **live arm is a latent rider on the REVIVE arm only** (§6.3) and stays dead under this verdict. ⊘ `GM-19` **is never certified** under either branch (`G-F4-NEG-ROSTER`).

---

### D2 · ⊘ FR-CP-R-7 (CoefficientsPanel, two arms, carried VERBATIM) — **(a) RULED: DELETE · (b) DEFERRED-WITH-DEFAULT**

**§3's bytes, quoted whole:**

> **D2 · ⊘ FR-CP-R-7 (CoefficientsPanel, two arms, carried VERBATIM).** (a) the selection contract DELETES per house law (zero consumers; `cursor-pointer` dies with it); (b) the `logScale` arm is DIFFERENT IN KIND — **D-axis minority preserved: LOG may be the correct default**. ⊘ If the spec OVERTURNS the delete and wires `hover-harmonic`, FR-FG-22 re-grades MINOR immediately and the `onUnmounted` teardown becomes MANDATORY.

**Mechanism at the corpus, both arms, from the banked ruling.** ⟨cmd⟩ `grep -n 'R-7' fr-CoefficientsPanel.md` → `:24` *"**RULED, two arms split.** (a) The selection contract (`toggle-harmonic`/`hover-harmonic` emits, `activeIndices`, `cursor-pointer`, `@click`) **deletes**: zero consumers repo-wide (both readers' greps, consistent with this seat's callsite read), a wired version would be new feature scope, and the house law (KISS / no-contrivance / feedback_no_backwards_compat) forbids shipping an affordance with no consumer. `cursor-pointer` dies with it. (b) The `logScale` arm is different in kind: an authored, annotated, already-implemented capability (`:164-171` names both transforms; `:49`'s 0.008 floor is the linear arm's own admission) — whether to wire a control or default it is a **product decision the forming F.W4 spec owns**; whichever arm ships, the LC-missed touch constraint binds (the hit-test must move into the click/pointer handler before ANY interaction survives — `onClick` reads state only `mousemove` writes, silently dead on the touch arm the route supports)."*

#### Arm (a) — the selection contract

**Branch — WIRE.** Cost, priced three ways and each independently sufficient: (i) **new feature scope** in a wave chartered for defect mass; (ii) the house law forbids the inverse (*"forbids shipping an affordance with no consumer"* — wiring is the other half of the same law, and this program's own `feedback_kiss_no_contrivance` / `feedback_no_backwards_compat` precepts bind); (iii) **the wire arm drags a mandatory teardown behind it**, priced at the sibling record — ⟨cmd⟩ `grep -n 'FG-22' fr-FrequencyGraph.md` → `:124` *"**R-7 minority position**: if the forming F.W4 spec overturns the banked delete and wires `hover-harmonic`, FR-FG-22 re-grades MINOR immediately and the `onUnmounted` teardown becomes mandatory — the dead import is the spec's reminder that the open half of a paired contract was authored without its close."* And a wired emit is **still dead on touch** until the hit-test moves into the click/pointer handler (`onClick` reads `hoveredBar`, which only `mousemove` writes) — so WIRE is not one edit, it is three.

**Branch — DELETE.** Cost: an authored selection affordance is given up; `cursor-pointer` and the hover-glow promise die with it, so the panel stops *looking* interactive — which is the point, since it never was. Zero consumers repo-wide is the measured basis (both readers' greps ⊕ the arbiter's callsite read).

**VERDICT (a) — RULED: DELETE.** The banked ruling is adopted whole. `.b` deletes the `toggle-harmonic`/`hover-harmonic` emits, `activeIndices`, `@click` and `cursor-pointer` as one contract. **FR-FG-22 stays INFO** and the `onUnmounted` teardown stays non-mandatory — *because* this arm ruled DELETE; had it ruled otherwise the re-grade would be immediate, and `.b` may not land the delete and the teardown obligation in the same breath.

#### Arm (b) — the `logScale` default

**Branch — DEFAULT TO LOG** (the D-axis position). Cost: it is a **product/visual change**, not a defect cure; it makes live three surfaces that are dead today (the annotated transform at `:164-171`, the axis label's log branch, the tooltip's log row), and **none of them has ever been painted** — the route ships `logScale:false`, so the log arm has no rendered witness anywhere in the corpus. Landing a visual default on an unwatched surface is the half-land class this program bans, and the probe that would settle it is SS-13-deferred by §5.2.

**Branch — WIRE A CONTROL.** Cost: dead on arrival — it is the same new-feature-scope the house law kills in arm (a), and it would resurrect the selection-contract shape (a) just deleted.

**Branch — DEFAULT (keep linear, wire nothing).** Cost, stated and not smoothed: `:49`'s **0.008 floor survives as the linear arm's own admission**, and the D-axis's indictment — that the linear arm hides the O(1/n) tail as a sliver band — **remains true and unaddressed**. That is the price of the default and it is a real one.

**VERDICT (b) — DEFERRED-WITH-DEFAULT: keep the shipped linear arm; wire no control.** The banked ruling calls this *"a product decision the forming F.W4 spec owns"*, and this record owns it enough to say what ships: **nothing changes at the bytes**, and the LOG-default question routes to **SS-3/SS-4** carrying the D-axis argument intact. ⊘ **The default is executable and `.b` executes it**: leave `logScale:false`, delete nothing on the log side, and state in its receipt that the log arm survived arm (a)'s deletion untouched. ⊘ **The touch constraint binds under BOTH arms and is not deferred**: whichever arm ships, the hit-test must move into the click/pointer handler before ANY interaction survives — recorded here so `.b` cannot read the deferral as permission to leave a touch-dead handler beside a live one.

**Dissent preserved (verbatim).** ⟨cmd⟩ `grep -n 'minority position' fr-CoefficientsPanel.md` → `:130` *"**The R-7 ruling's minority position**: the D axis holds that the log arm is the CORRECT default (the linear arm hides the O(1/n) tail as a sliver band, the 0.008 floor being the admission) — preserved so the F.W4 spec inherits the argument, not just the disposition."* ⊘ Inherited here **in full**, and it is the operand SS-3/SS-4 receives — not a summary of it.

**Dependent cluster.** `.b`'s `FR-CP-6` disposition · `FR-FG-22` (rides FR-CP-6; stays INFO under this verdict) · `FR-FG-12`'s *"uppercase-destroys-notation LIVE the moment logScale wires"* — **which stays latent under the default and would fire the day SS-3/SS-4 rules LOG**, recorded so the future ruling inherits its own consequence.

---

### D3 · FR-EQC-residue ⊕ FR-EQC-12 ⊕ FR-EQC-1 — decided TOGETHER — **RULED: RE-WORD, do not fill**

**§3's bytes, quoted whole:**

> **D3 · FR-EQC-residue ⊕ FR-EQC-12 ⊕ FR-EQC-1 — decided TOGETHER** (no axis or reader made the connection): whether `/equation` SHOULD render the spectrum graph interacts with the honesty repair — a filled `#graph` adds a FOURTH surface to reconcile against the budget-truncated formula.

**Mechanism.** ⟨cmd⟩ `grep -n 'FR-EQC-12' fr-EqCoefficientsPanel.md` → `:49` *"**FR-EQC-12 = D/m-5 (promise limb) — MINOR.** `subtitle=\"Fourier spectrum\"` (:13) promises the spectrum the panel then declines: the `#graph` slot is left empty here while the sibling fills it — the em-dash gloss restates the title and does no work. … **→ F.W3/W4** (a product decision — fill `#graph`, or re-word; **not both, not neither**)."* And the residue that binds the two: ⟨cmd⟩ `grep -n 'A residue this corpus cannot settle' fr-EqCoefficientsPanel.md` → `:123` *"**A residue this corpus cannot settle**: whether the equation route SHOULD render the spectrum graph (FR-EQC-12's product decision) interacts with FR-EQC-1's honesty repair — a filled `#graph` would add a fourth surface to reconcile against the budget-truncated formula. The forming F.W4 spec should decide them together; neither axis nor reader made the connection."* The honesty defect itself: `:38` *"**FR-EQC-1 = D/M-5 — MAJOR** … The list is not the equation: the panel enumerates EVERY coefficient beside a formula rendered from a budget-truncated subset, and nothing on either surface says so."*

**⊘ Why this is ruled here while §5.2 routes its sibling away.** §5.2's `→ NO-WAVE-OWNER` row names *"FR-EQC-12 ⊕ FR-EQC-1 joint product decision (D3)"*. The routed question is **"should `/equation` render a spectrum graph?"** — a product question about a feature this route does not have. The question this wave must answer is narrower and is entirely its own: **given that the graph is not rendered, does the subtitle keep promising it?** FR-EQC-12's terminal forecloses the do-nothing arm in terms (*"not both, not neither"*), so a wave that routes the whole thing away ships a promise it declines — which is the defect. **This record rules the promise, and routes the feature.**

**Branch — FILL `#graph`.** Cost, in §3's own words: *"a filled `#graph` adds a FOURTH surface to reconcile against the budget-truncated formula"* — i.e. it multiplies the very reconciliation FR-EQC-1 exists to repair, while FR-EQC-1 is still unlanded. Secondary cost: the sibling already fills it, so the fill arm raises a duplication question this wave has not costed, and it is feature authoring in a defect-mass wave.

**Branch — RE-WORD.** Cost: one string; and the honest cost is that **the spectrum stays unrendered on `/equation`** — the pedagogy the subtitle promised is not delivered, only stopped from being promised. That is a real loss and it is recorded as one.

**Branch — NEITHER.** Forbidden by the row's own terminal (*"not both, not neither"*), and it leaves three cardinalities under one label.

**VERDICT — RULED: RE-WORD.** `.b` re-words the `:13` subtitle so the panel stops promising a spectrum it declines (the em-dash gloss goes with it — it *"restates the title and does no work"*), and lands **FR-EQC-1's honesty repair on the three surfaces that exist today**, not four. The **fill-`#graph` product question routes to SS-3/SS-4** with this record as its provenance, and carries the consequence with it: *a later decision to fill `#graph` re-opens FR-EQC-1's reconciliation at a fourth surface and must re-price it there.* ⊘ **Not both, not neither** — the ruling satisfies the row's own exclusivity, and `.b` may not land a half of each.

**Dependent cluster.** `.b`'s `FR-EQC-residue` · `FR-EQC-1`'s honesty repair (`EquationView.vue:59-67` maps `result.coefficients` whole against a `simplify_series`-truncated render) · `FR-FG-13`'s `#graph` seam annotation (ordering brand → F.W5) · `FR-FG-21`'s `maxBars` clamp. ⊘ The **emission hinge** (banked `fr-CollapsibleSection M-7`, `EQC-L-16`/`C-14` residue) is **not** this decision's and is not opened by it.

---

### D4 · ⊘ PAW-44 under LAW-3 — retire or restore the sticky stack — **DEFERRED-WITH-DEFAULT**

**§3's bytes** (the decision head, elisions marked; the LAW-3 fusion and its two ⟨cmd⟩-sourced homes are carried in §3 itself and are not re-pasted here):

> **D4 · ⊘ PAW-44 under LAW-3 — retire or restore the sticky stack.** The two-tier header stack is 100% INERT (`overflow-x: hidden` mints a both-axes scroll container with permanent `scrollTop` 0). … ⊘ Design dissent OPEN: a formation may legitimately RETIRE the stack and record theme `:443-465` dead-by-decision — LAW-3/LAW-4 valid under EITHER outcome. PAW-47's constant and PAW-54's x-axis posture are dependents.

**The two laws, as §3 produces them from their two homes:** LAW-3 — *"PAW-44 restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before. **LAW-4**: PAW-47's clearance constant is a dependent of the PAW-44 decision; one clearance authority only."* And the trap that makes order a law: the cheap candidate is *"`overflow-x: clip` (clips without minting a scroll container) **but restoration MUST land WITH-or-AFTER the PAW-1 header-background cure + PAW-30's bleed handling**"*, continued — *"The two-TIER stack additionally needs banked K-11's flattener nesting (LATEX-PAPER carry rider)"*.

**Branch — RESTORE.** Cost: one rule (`overflow-x: clip`) and then everything behind it. (i) **LAW-3 is a sequencing law, not a preference**: restoration lands WITH-or-AFTER PAW-1's header-background cure and PAW-30's bleed handling — *"restore the scrollport first and K-18's killed catastrophe becomes real"*, i.e. the see-through-header failure that is currently **dead only because nothing ever scrolls** becomes live. (ii) The two-tier form additionally needs **K-11's flattener nesting**, which is a **LATEX-PAPER relay** carry — a cross-repo ask this wave cannot land at its own end. (iii) PAW-47's clearance constant must then be authored for the sticky geometry, and PAW-54's `.math-inline` posture re-decides under it.

**Branch — RETIRE.** Cost: the cheapest bytes on the page — delete the two-tier stack and record theme `:443-465` **dead-by-decision**. But it is a **design act that deletes authored intent**, and §5.2 routes *"PAW-44's retire-or-restore dissent (D4)"* to **NO-WAVE-OWNER / SS-3/SS-4** as consumed-not-settled. A wave that retires here has settled, by deletion, a question its own spec sent elsewhere — and deletion is the one arm that cannot be reversed by the holder it was sent to.

**Branch — DEFAULT (neither).** Cost: **the inert stack ships another cycle** — the authored two-tier design remains 100% non-functional and PAW-54's `.math-inline` nowrap-truncation posture stays as shipped; the dissent stays open and the SS-13 post-repair witnesses stay unbought. Gain: zero bytes, zero risk, both arms intact for the holder, and **LAW-3/LAW-4 hold under it unchanged** — §3 says so in terms (*"LAW-3/LAW-4 valid under EITHER outcome"*).

**VERDICT — DEFERRED-WITH-DEFAULT: neither restore nor retire.** The default is executable and `.e` executes it: **carry the stack as INERT-BY-MEASUREMENT** — the inertness is a measured fact, recorded, not a decision — **do not delete theme `:443-465`**, and author **PAW-47's clearance constant ONCE, against the inert geometry, under LAW-4's single clearance authority**. `.e` states in its receipt that D4's default was executed and that PAW-54's x-axis posture rides it unchanged. The retire-or-restore question goes to **SS-3/SS-4** with the dissent attached and with LAW-3's trap and K-11's relay dependency named as the price of the restore arm.

**Dissent preserved (verbatim, §3's own sentence — this is the record's spelling of it and it is carried unamended):** *"⊘ Design dissent OPEN: a formation may legitimately RETIRE the stack and record theme `:443-465` dead-by-decision — LAW-3/LAW-4 valid under EITHER outcome."*

**Dependent cluster.** `PAW-47` (LAW-4's constant — **its value is a dependent of this decision and is authored against the inert geometry under the default**) · `PAW-54` (the `.math-inline` nowrap truncation *"rides the PAW-44 decision"*) · `PAW-30` and `PAW-1` (**prerequisites of the restore arm, and this wave's work regardless** — the default does not release them) · `PAW-55`'s `.katex-display` computed-`auto` row (the class's third witness; untouched by this deferral).

---

### D5 · ⊘ PAW-52 under LAW-5 — URL addressability — **DEFERRED-WITH-DEFAULT: POSED AND BLOCKED**

**§3's bytes, quoted whole:**

> **D5 · ⊘ PAW-52 under LAW-5 — URL addressability.** 93 sections / 26 figures with stable DOM ids and ZERO fragment consumption or emission; the virtual window is the blocking mechanism (18-of-93 mounted ⇒ ~81% of targets absent at any instant). ⊘ LAW-5: any deep-link cure routes through `ensureTargetWindow`, under LAW-4's single clearance authority, and REQUIRES PAW-38's slug uniquification FIRST.

**Branch — CURE NOW.** Cost, and it is a correctness cost, not a scheduling one: LAW-5 requires **PAW-38's slug uniquification FIRST** — the four-registry slug collision — so a deep link wired before uniquification can **silently address the wrong section**, which is worse than no deep link at all. The cure must additionally route through `ensureTargetWindow` (because ~81% of targets are unmounted at any instant) **under LAW-4's single clearance authority — whose constant is a D4 dependent**, so the cure would land on a constant this wave has explicitly deferred. Two blockers, one of which is cross-repo: **PAW-38 is a LATEX-PAPER relay ask** (F-W4 §5.2 `→ LATEX-PAPER relay`: *"slug uniquification (PAW-38, LAW-5 prerequisite)"*).

**Branch — DEFAULT (pose and block).** Cost: **93 stable DOM ids stay unaddressable for another cycle** — the paper has the ids and emits and consumes no fragment, so the affordance the markup already pays for is not delivered. Gain: zero bytes, no wrong-target link, and the blocker is named where it can be cleared.

**VERDICT — DEFERRED-WITH-DEFAULT: POSED AND BLOCKED, and saying so is the cure.** `.e` **poses** PAW-52 with its blocking prerequisite named — PAW-38's slug uniquification, a LATEX-PAPER relay ask — and lands **no deep-link cure**. ⊘ **The relay is the executable part of this default, not an optional rider**: `.z`'s LATEX-PAPER letter carries PAW-38, or the blocker never clears and this deferral renews itself silently at the next wave. `.e` names PAW-38 in its receipt; `.z` names it in the letter.

**Dependent cluster.** `PAW-38` (the prerequisite; LATEX-PAPER relay) · `LAW-4`'s clearance authority (a D4 dependent — **which is why D4's deferral propagates here and is stated rather than hidden**) · `ensureTargetWindow` (the sole lawful route for any future cure) · the share-affordance question, which §2.I books as a **formation design decision** and which this record does not open.

---

### D6 · THE VITEST FLOOR (charter item, §2.L) — **RULED: a vitest runner in `web/`**

**§3's bytes, quoted whole:**

> **D6 · THE VITEST FLOOR (charter item, §2.L).** Stand a runner up in `web/`, or implement the checks as a Vite build plugin (PAW-50's alternative). The decision names the first four assertions: `snapshotForTransition` · `contourBounds` · `resolveFigure` (PAW-12's rider, homeless until this lands) · `fuzzyMatch` (PSM-14).

**The charter item** (F-W4 §2.L, true bytes `:198`): *"**VITEST-FLOOR · the DECISION (= §3 D6)** — this wave DECIDES and stands up the unit floor in `web/` (vitest, or the Vite-build-plugin alternative PAW-50 names) BEFORE any rider needing a home … Acceptance = G-F4-VITEST. The `vue-tsc -b` NEEDS-BUILD witness exists today."*
**The alternative, at its own record** — ⟨cmd⟩ `grep -n 'PAW-50' fr-PaperArticleWindow.md` → `:200` *"**PAW-50** … web/ has NO unit-test harness at all (five scripts, no vitest/jest, zero test files) — the build-time set-equality assertion PAW-12's disposition books has no home: the rider must stand vitest up first or ship as a Vite plugin. `resolveFigure` is additionally trapped in the SFC, unimportable by any assertion; it belongs beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts`. Seat addition: `npm run build` already carries `vue-tsc -b`, so the PAW-49/K-12 type witnesses need no new harness."*

**Substrate measured by this seat at the fourier bytes, double-run** (read-only; no byte written). ⟨cmd⟩ (from `fourier-analysis/web`) `grep -nA8 '"scripts"' package.json` → exactly `dev` · `build` (`vue-tsc -b && vite build`) · `preview` · `test:e2e` · `test:e2e:ui`; ⟨cmd⟩ `grep -nE '"vite"|vitest|jsdom|happy-dom|"@vue/test-utils"' package.json` → **`"vite": "^7.3.6"` and nothing else**; ⟨cmd⟩ `ls node_modules/vitest` → **absent**; ⟨cmd⟩ `grep -m1 '"version"' node_modules/vite/package.json` → `"version": "7.3.6"`; the repo-root `package.json` carries `@playwright/test` alone.

**The four assertions, located at today's bytes — this is what decides the branch.** ⟨cmd⟩ `for f in snapshotForTransition contourBounds resolveFigure fuzzyMatch; do grep -rln "$f" src; done`, refined by ⟨cmd⟩ `grep -rn 'fuzzyMatch' src/components/paper/search/` and ⟨cmd⟩ `grep -rn 'snapshotForTransition' src`:

| assertion subject | where it lives TODAY | importable today? | obligation |
|---|---|---|---|
| `snapshotForTransition` | **defined** `components/equation/composables/useCurveTransition.ts:57`; used `ConvergencePlot.vue:12,312` | **YES** — already a pure exported TS function | none; assertable the day the runner exists |
| `fuzzyMatch` | **defined** `components/paper/search/paperSearchIndex.ts:70`; re-exported by `search/index.ts:2` | **YES** | ⊘ **import from `./paperSearchIndex`, NEVER from `search/index.ts`** — see the lock below |
| `resolveFigure` | `components/paper/PaperArticleWindow.vue:44`, a local `function` in the SFC | **NO** — trapped, exactly as PAW-50 states | moves beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` (`.e`'s file) |
| `contourBounds` | **does not exist** (zero hits under `src`) | **NO** | minted by `CP-ROW-40`'s extraction into `lib/contourEditing.ts` (`.c`'s file) |

**⊘ A LOCK THIS DECISION MINTS, because the measurement found it and nothing else would have.** `fuzzyMatch`'s assertion must import from **`./paperSearchIndex`** (its definition), **never from `search/index.ts`** (the barrel that only re-exports it). `search/index.ts` is the **zero-consumer file the SCRUB deletes** under `G-F4-DEAD-DEP` (§2.L SCRUB: *"PSM's zero-consumer `search/index.ts`"*; `.e` step 8 is its zero-consumer proof and deletion). A test that imports the barrel **manufactures a consumer and falsifies the zero-consumer proof** — a green gate bought by reddening another. `.g` imports the definition; `.e`'s deletion proof is unaffected.

**Branch — THE VITE BUILD PLUGIN (PAW-50's alternative).** Its honest advantages, stated first because they are real: **zero new dependencies** (`vite@7.3.6` is installed and `vite.config.ts` exists), and the assertions ride a command the repo already runs. Costs: (i) the assertions can run **only at build time**, so a failing assertion reddens `npm run build` rather than a test run, and there is no per-assertion report — a floor that cannot be run in isolation is a floor no one runs; (ii) **two of the four subjects are already importable pure TS**, so a plugin would have to hand-roll an evaluation harness inside the build to assert what a runner asserts natively — contrivance against `feedback_kiss_no_contrivance`; (iii) every later rider that wants a home (PAW-12's set-equality, PSM-14's scoring witnesses, `FR-CP-LM1`'s 6-line `snapshotForTransition` test, `CP-ROW-40`'s `contourBounds`) then inherits the same build-coupling, and §5.1(3) puts **`G-F4-VITEST` before any unit-test rider** — a floor the riders cannot use is not before them.

**Branch — THE VITEST RUNNER.** Costs, priced exactly: **one devDependency (`vitest`), one `vitest.config.ts`, one `test` script** — and no DOM environment, because all four named subjects are pure functions, so `environment: "node"` suffices and neither `jsdom` nor `happy-dom` is bought. ⊘ **This record pins no version it has not measured**: `.g` selects the `vitest` major whose peer range admits the **installed `vite@7.3.6`** (measured above), records the resolved version in its receipt, and does not inherit a number from this line.

**VERDICT — RULED: stand a vitest runner up in `web/`.** `.g` builds **that arm and no other** — the Vite-build-plugin arm is not built, not half-built, and not kept as a fallback. **Gate satisfaction is honest and immediate**: `G-F4-VITEST` asks for *"a unit runner … and executes ≥1 assertion over the extracted pure functions"*, so `.g` lands the two assertions whose subjects exist today — **`snapshotForTransition`** (from `useCurveTransition.ts`) and **`fuzzyMatch`** (from `paperSearchIndex.ts`) — and the other two land **with their extractions**: `resolveFigure` when `.e` moves it into `lib/figureDimensions.ts`, `contourBounds` when `.c` mints it in `lib/contourEditing.ts`. **Harness before rider** (PAW-50's own law, §5.1(3)) is satisfied in that order and in no other.

**Dependent cluster — this is `.g`'s whole precondition.** `.g` cannot open without this verdict (`G-F4-VITEST` · the runner's home for every rider) · `.b`'s `FR-CP-LM1` 6-line test (lands in the SAME edit as the `old[1]` fix + the `deep` drop) · `.c`'s `CP-ROW-40` `contourBounds` test · `.e`'s `resolveFigure` + `fuzzyMatch` tests and the `search/index.ts` deletion (see the lock) · `PAW-12`'s set-equality rider, **still F.W9/W10's** — this decision gives it a home, it does not book it · `PAW-49`/`noUncheckedIndexedAccess`, which §5.1(3) sequences **before** PAW-12's predicate split. ⊘ **The `vue-tsc -b` NEEDS-BUILD witness exists today and is not this decision's** — `npm run build` already runs it, and `G-F4-VUE-TSC-CLEAN` is a separate gate with its own 18-diagnostic baseline.

---

### D7 · HLG-35 — the shared-domain design decision — **DEFERRED-WITH-DEFAULT (routed SS-3/SS-4)**

**§3's bytes, quoted whole:**

> **D7 · HLG-35 — the shared-domain design decision.** The two rails share NO scale; fill can INVERT against the true band while the `low < high` invariant HOLDS — a presentation-domain defect the banked superlative cannot see. Cure is a DECISION, not a rename: one shared 1..N domain (reka two-thumb range — which also DELETES the scalar↔array adapters) or an explicit shared-axis reference. Flagged to SS-3/SS-4.

**Mechanism at the corpus.** ⟨cmd⟩ `grep -n 'HLG-35' fr-HarmonicLevelGrid.md` → `:176` *"**HLG-35 · r2-DU M-1** | **MAJOR — books here** | The two rails share no scale: Low spans [1, high−1], High spans [low+1, 100], rendered at identical width, stacked, thumbless, same colour — so fill, the only readable signal, can invert against the true band. One drag from default → 100%/0% (\"Low\" full above \"High\" empty at (49,50)); three on-grid clicks → (5,18) = 25%/12.77%. All arithmetic executed by this seat (instruments line); the `low < high` invariant HOLDS throughout — this is a presentation-domain defect the banked superlative cannot see."*

**Branch — ONE SHARED 1..N DOMAIN (reka two-thumb range).** The structurally correct arm, and the largest win: §3 notes it *"also DELETES the scalar↔array adapters"*. Cost: it is a **component-shape change** on a live `/morph` surface — a design act, not a defect cure; the adapters' deletion reaches every consumer of the two setters (the banked `FMD-33` setter-type divergence sits on the same adapters), and the record itself files HLG-35 as *"a refinement INSIDE the adapter pattern (the adapter is right; the two domains it adapts are not)"* — so the arm that deletes the adapter is doing more than the defect asks.

**Branch — AN EXPLICIT SHARED-AXIS REFERENCE.** Cost: a **new visual surface** on the grid, and it is a partial cure by construction — the two domains remain underneath, so the inversion stays true in the model even once it is legible on the page.

**Branch — DEFAULT (neither; book POSED).** Cost, and it is a MAJOR left standing: **the fill can still read inverted against the true band while `low < high` holds** — one drag from default yields a full "Low" capsule over an empty "High" capsule at (49,50). The painted witness (*"cold `/morph`, drag Low fully right — predict Low ≈100% filled over High ≈0%"*) stays an **SS-13** probe, unresolved inline.

**VERDICT — DEFERRED-WITH-DEFAULT: the shipped two-domain rails stand; the row is booked POSED; no rename lands.** §3 flags this decision to **SS-3/SS-4** in its own sentence and §5.2's `→ NO-WAVE-OWNER` row names it again — this record does not overrule a routing its spec states twice. The default is executable and `.a` executes it: **book HLG-35 as POSED, never answered** (`.a`'s own charter: *"Book `D7`/`D10`'s posed legs as **posed**, never answered"*), and **land no rename** — §3 forecloses the rename in terms (*"Cure is a DECISION, not a rename"*), so a seat that renames the rails has executed the one arm this decision explicitly rules out.

**Dependent cluster.** `.a`'s `HLG-*` family · `FMD-33`'s setter-type divergence (adapter-resident; re-prices under the shared-domain arm) · `HLG-13`'s degenerate `min===max` NaN capsule (**a distinct probe — both ranges stay non-degenerate in HLG-35's states; do not mis-fold them**) · the SS-13 painted witness.

---

### D8 · ECD-D-2/L-3/C-1 (+C-13/C-22; K-8) — **RULED: DELETE WHOLE**

**§3's bytes, quoted whole:**

> **D8 · ECD-D-2/L-3/C-1 (+C-13/C-22; K-8) — delete whole or realize the intent.** The magnet retint block is 100% dead (`--slider-scrub-*` not a token namespace at 4.0.0); the entire unit — rule, class, `:style` binding, `VIZ_COLORS` import edge, two stale comments — is ONE deletable block with zero visual delta today. ⊘ Delete whole OR realize the authored intent in pure CSS — do not half-land it.

**Mechanism at the corpus.** ⟨cmd⟩ `grep -n 'D-2 / L-3 / C-1' fr-EditorControlsDock.md` → `:51` *"The magnet retint block is 100% dead: `--slider-scrub-*` is not a token namespace at 4.0.0 (seat census: exactly ten `--slider-*` names, zero `scrub`, no `-hover` variant of anything); two of the four are DOUBLY dead (standard's thumb paints `width:0; opacity:0` by contract — C-13); and per K-8 even `width:100%` is a no-op (`w-full` is in the slider's cva base, seat byte-verified). **The entire unit — the `:222-229` rule, the `magnet-slider-track` class (:122), the `:style` `--track-color` binding (:123), the `VIZ_COLORS` import edge (:8), and the two stale `glass-scrubber` comments (:48,:222 — C-22) — is one deletable block with zero visual delta today.**"*

**⊘ THE PREMISE IS RE-MEASURED AT 8.0.0, BECAUSE THE RECORD STATES IT AT 4.0.0 AND THE TREE MOVED.** The cell's load-bearing claim is a **producer-version claim**, and F.W1 landed the 4→8 uplift after that census was taken (COHESION §0i.3; `538db90`). A ruling that inherited *"not a token namespace at 4.0.0"* onto an 8.0.0 tree would be citing a dead measurement. Re-run by this seat, read-only, **double-run** (from `fourier-analysis/web`):

⟨cmd⟩ `grep -m1 '"version"' node_modules/@mkbabb/glass-ui/package.json` → `"version":"8.0.0"`
⟨cmd⟩ `grep -roh -- '--slider-scrub[a-z-]*' node_modules/@mkbabb/glass-ui/dist | sort -u` → **∅ (no output, both runs)**
⟨cmd⟩ `grep -roh -- '--slider-[a-z0-9-]*' node_modules/@mkbabb/glass-ui/dist | sort -u | wc -l` → **6** distinct `--slider-*` names (both runs)

**The premise HOLDS at 8.0.0 and is stronger there**: the record's 4.0.0 census read ten `--slider-*` names with zero `scrub`; the installed 8.0.0 dist carries six, still with **zero `scrub`**. `--slider-scrub-*` is not a producer token namespace at either pin, so the four declarations are dead at both — and the block's deadness is not an artefact of the version the census happened to run on.

**The block is byte-present exactly as described**, re-resolved at today's bytes (⟨cmd⟩ `grep -n -- '--slider-scrub\|magnet-slider-track\|glass-scrubber\|VIZ_COLORS' src/components/visualization/EditorControlsDock.vue`): `:8` the `VIZ_COLORS` import edge · `:48` and `:221` the two stale `glass-scrubber` comments · `:121` the `magnet-slider-track` class · `:122` the `:style` `--track-color` binding · `:222-228` the rule with its four dead `--slider-scrub-*` declarations (`:229` is `</style>`). **Anchor note for `G-F4-ANCHORS`**: the record's `:122`/`:123`/`:222`(comment) resolve **−1** at HEAD (`:121`/`:122`/`:221`) and its `:222-229` rule span **over-runs by one line onto `</style>`** — the true rule body is `:222-228`. `:8` and the rule's start line are exact. **INTENT at the true bytes; the findings survive on content, the anchors do not** (F-W4 §0's evidence standard).

**⊘ ONE SUB-LIMB'S WITNESS DOES NOT REPRODUCE AT 8.0.0, AND IT IS NOT SMOOTHED.** K-8's *"even `width:100%` is a no-op (`w-full` is in the slider's cva base, seat byte-verified)"* was verified against the **4.0.0** dist. ⟨cmd⟩ `grep -c 'w-full' node_modules/@mkbabb/glass-ui/dist/slider-gsc8jDIo.js` → **0** (both runs); the 8.0.0 slider chunk does not carry the token the record's probe looked for. **This does not falsify K-8** — the 8.0.0 dist's class construction differs and this seat did not chase it further (probe parsimony; the cure site is `.c`'s, not this record's). What it means is stated exactly: **`.c` re-measures whether `width: 100%` at `:223` is load-bearing at 8.0.0 before asserting "zero visual delta today"**, and if it *is* load-bearing, the deletion carries a `w-full`-equivalent with it rather than dropping a width the base no longer supplies. The verdict does not change — the block is dead — but the **zero-delta claim is `.c`'s to re-earn at 8.0.0, not to inherit**.

**Branch — REALIZE THE AUTHORED INTENT IN PURE CSS.** The intent is a per-instance magnet retint (`--track-color` → range/thumb backgrounds). Cost: it is **new authored styling** on a control whose retint has never painted, against a producer namespace that does not exist at either pin — so it would be minted from scratch in consumer CSS, which is the frontend-hack shape §5.1(6) forbids for producer-shaped asks; and it revives a `VIZ_COLORS` import edge whose grey-resolution class is banked at `fr-BasisCanvas D-1/BC-1/C-1` (BLOCKER, F.W1's). It also contradicts the sibling disposition: the record's R-8 notes that for **this** file the deletion *"dissolves the coupling instead of discharging it"*.

**Branch — DELETE WHOLE.** Cost: the authored retint intent is given up and the comment record of it goes with the block (`:48`/`:221` name a `glass-scrubber` that is no longer what the producer ships). Gain: five dead artefacts leave in one edit — rule, class, `:style` binding, import edge, two comments — with `C-13` and `C-22` riding it (⟨cmd⟩ `grep -n 'C-22' fr-EditorControlsDock.md` → `:99` *"Two stale `glass-scrubber` comments — absorbed into MAJ-1's cure (they name the dead block; they die with it)."*).

**Branch — HALF-LAND** (delete some declarations, keep the class or the binding). **Forbidden by §3 in terms**: *"⊘ Delete whole OR realize the authored intent in pure CSS — do not half-land it."* A partial deletion leaves a class with no rule or a binding with no consumer — dead code wearing a cure's clothes.

**VERDICT — RULED: DELETE WHOLE.** `.c` deletes the unit as **ONE block** — `:221` comment + `:222-228` rule + `:121` class + `:122` `:style` binding + `:8` `VIZ_COLORS` import edge + `:48` comment — in **one commit**, with `C-13` and `C-22` riding it and no leaf grown on the pure-CSS arm. ⊘ **Scope boundary, so the verdict does not travel where it was not measured**: D8 rules the **EditorControlsDock unit only**. ⟨cmd⟩ `grep -rl -- '--slider-scrub' src` → **seven files** (`ConvergenceTimeline.vue` · `HarmonicLevelGrid.vue` · `MorphPhaseConfig.vue` · `SliderControl.vue` · `BasisSelector.vue` · `EditorControlsDock.vue` · `GlassTimeline.vue`, both runs). **The other six are their own rows, with their own same-commit `C-1⊕C-2` coupling (the record: *"that same-commit coupling survives for the OTHER six `--slider-scrub` files"*), and D8 does not delete them by implication.** A seat that reads this verdict as a six-file sweep has widened a ruling it was not given.

**Dependent cluster.** `.c`'s `ECD-*` family · `C-13` (doubly-dead thumb declarations) and `C-22` (the two stale comments) — **riders, not separate edits** · the `fr-BasisCanvas D-1/BC-1/C-1` BLOCKER identity, which is **F.W1's and is cited, never re-booked here** · `ECD-D-9/L-8`'s `DockSeparator` adoption, which is a different row on the same file and is **not** coupled to this deletion.

---

### D9 · PS-L-4 (+M6) — the ToC owner — **RULED at COHESION §0o ESC-2; transcribed, never re-opened**

**§3's bytes, quoted whole:**

> **D9 · PS-L-4 (+M6) — OWNER-GATED, escalated at F.W1.** Three parallel derivations of one 98-node tree across two producers; BOTH producers export `useSidebarFollow` with NOTHING choosing a winner. ⊘ F.W4 EXECUTES the collapse (delete the `paperTree.ts` adapter + the losing index) ONLY after the ruling. Unauthorable unruled.

**⊘ THE RULING EXISTS. It is not re-opened, re-argued or re-derived here — it is transcribed with its id.** Authority: `docs/tranches/X/COHESION.md` **§0o**, table row **`ESC-2`**, ruled 2026-09-18 under the begin-word's delegation (§0j), same standing as §0n — *"orchestrator rulings under the §0j begin-word, reversible, overturnable by the owner at read"*. ⟨cmd⟩ `grep -n 'ESC-2' COHESION.md` → `:1096`:

> | **ESC-2** ToC model ownership | **ONE owner.** The ToC model is a single `useSidebarFollow` instance owned by the paper view (the highest common ancestor of sidebar and body), provided through a **typed `InjectionKey`**; the second instance and the untyped `defineExpose` seam are retired. Ruled at F.W1 as WU-R says; **F.W4 executes the collapse**; L-5(a) is its prerequisite. | `fr-PaperSidebar L-4` · `M6` |

**The four operative clauses, restated as obligations and nothing added:** (1) **ONE owner** — a single `useSidebarFollow` instance; (2) the owner is **the paper view**, the highest common ancestor of sidebar and body; (3) the instance is provided through a **typed `InjectionKey`** — typed, not ambient, not a fresh `provide` of an untyped object; (4) **the second instance and the untyped `defineExpose` seam are retired**.

**Blocked rows RELEASED by the ruling, named as the ruling names them:** **`fr-PaperSidebar L-4`** and **`M6`**. Their mechanism, for the executor's provenance — ⟨cmd⟩ `grep -n 'L-4' fr-PaperSidebar.md` → `:33` *"| L-4 = C-B2 = D-N10 (+M6) | MAJOR | Three parallel derivations of one 98-node tree across two producers; 8 of 10 `useSidebarState` members unused (ten-member return re-verified from dist); both producers export `useSidebarFollow` (export line re-verified) and the app picks latex-paper's follow + glass-ui's expand with nothing choosing a winner; the arbitration point is the untyped `defineExpose` seam (M6). | **F.W1 — OWNER-RULING escalation** (which producer owns the ToC model), then F.W4 executes the collapse (delete `paperTree.ts` adapter + the losing index). |"* and `:66` *"| M6 | INFO | The only `defineExpose` is an untyped seam feeding a third package's rAF clock; both producers ship `useSidebarFollow`; the two-producer split runs through this seam. | Folded into L-4's OWNER-RULING row (F.W1). |"*

**The prerequisite, and it is a sequencing law not a note.** **`L-5(a)` is the prerequisite** (§0o ESC-2's own last clause). ⟨cmd⟩ `grep -n 'L-5(a)' fr-PaperSidebar.md` → `:34` *"| L-5(a) = C-M2 = D-B4 | MAJOR | No `CollapsibleTrigger`: the disclosure control announces no state (no `aria-expanded`/`aria-controls`/`aria-current` anywhere in the file — re-verified: state reaches the DOM only as reka `data-state`); `@update:open` at :68 is dead and becomes a double-toggle the day a trigger lands. … | F.W4 — `CollapsibleTrigger as-child` restructure, a **prerequisite** of F.W1 (D-I1). |"* ⊘ **`@update:open` at `:68` is dead today and becomes a double-toggle the day a trigger lands** — which is why L-5(a) is executed *before* the collapse and not beside it.

**VERDICT — RULED, at COHESION §0o ESC-2 (2026-09-18), under the §0j begin-word's delegation.** Not ruled *by* this record: transcribed *into* it, with its id, so the executing unit reads the ruling and not a paraphrase of it. Not re-opened, not re-derived, not amended. §3's *"Unauthorable unruled"* is discharged — it is ruled.

**EXECUTOR — unit `.e`**, named here as the unit charter names it. `.e` performs, in this order: **L-5(a)**'s `CollapsibleTrigger as-child` restructure, **then** the collapse — one typed-`InjectionKey`-provided `useSidebarFollow` owned by `PaperView`, the **`paperTree.ts` adapter and the losing index deleted**, the untyped `defineExpose` seam retired — and states `fr-PaperSidebar L-4` and `M6` as **released and discharged** in its receipt.

**Branch costs, recorded for completeness only.** ⊘ **There are no live branches**: the decision was owner-gated, §3 calls it *"Unauthorable unruled"*, and it is now ruled. The losing arm (keep both derivations, arbitrate at the untyped seam) is priced by the row itself — three parallel derivations of one 98-node tree, 8 of 10 `useSidebarState` members unused, and an arbitration point that is an untyped `defineExpose` feeding a third package's rAF clock. **Cite, never re-open** (§0 clause 5's discipline, applied here).

**Dependent cluster.** `.e`'s `PS-*` family · `paperTree.ts` (deleted) · the losing index (deleted) · `M6`'s seam (retired) · `L-5(a)` (prerequisite, this wave's) · `D-I1`'s 7.0.0-era IDREF break, which **re-derives at the adopted tag (8.0.0)** and is **F.W1's**, cited not booked.

---

### D10 · posed-with-default batch — **DEFERRED-WITH-DEFAULT ×5 (defaults stated; NOT answered)**

**§3's bytes, quoted whole:**

> **D10 · posed-with-default batch** — FR-AH-2's font decision (SP-10, ONE decision stated) · FR-AH-16/FM-21's scaling-system owner (SP-9, F.W0-adjacent) · FR-CP-D10's `sliceW` + ONE-honest-denominator ruling · FR-CT-C-7's announcement-rate decision (if the float axis is not adopted at F.W3) · FSE-L-B1's asset-provenance decision (F.W5–W8 union; DO-NOT-REGENERATE meanwhile).

⊘ **What D10 is, stated once so no seat mistakes it.** D10 is the batch of questions this wave **poses with a default and routes** — it is not a decision this record answers, and a unit that reads a default below as a ruling has over-executed. Each leg carries: the default that ships absent an answer, the cost of answering either way, and the holder the question goes to. `.a`'s charter says it plainly: *"Book `D7`/`D10`'s posed legs as **posed**, never answered."*

**VERDICT — DEFERRED-WITH-DEFAULT, five times, one per leg.** Each of the five below lands its own default, its own priced branches and its own holder; none is answered here, and **a leg whose unit books it as anything but POSED has executed a ruling this record did not make.**

#### D10.1 · FR-AH-2 — the font decision (SP-10, "ONE decision stated")

**The frame** (F-W4 §2.0 **SP-10 · font truth (ONE decision, stated)**): *"FR-AH-2·MISS-FONT: wire `@import \"@mkbabb/glass-ui/styles/fonts\"` OR commit to the CM identity; ⊘ D-B2 unicode-range leg KILLED; GLASS-RELAY `cm-serif` undeclared-var no-op."*
**Default stated**: the shipped identity — **the CM identity stands, `styles/fonts` remains unimported**, which §6.4(d) records as *"a **deliberate unimported subpath**"*, and **the never-matching preloads are the cost of that default** (§2.F's sweep: *"**CM preloads can NEVER match (CORS-anonymous font cache key) — dies with FR-AH-2**"*).
**Cost of the wire arm**: a producer stylesheet joins the critical path and the CM identity is displaced — a design identity change, not a defect cure. **Cost of the CM arm**: the preload edge stays dead weight until FR-AH-2 lands, and the `cm-serif` undeclared-var no-op stays a **GLASS-RELAY** ask.
**⊘ ONE decision, stated once**: whichever arm `.a` ships, it ships **once**, for the whole family sink (the `SS-D-04/-05/-06` faces · `FR-CL-D3`'s three-site Fira fallback · `HLG-18` · `SliderControl R-12` · `CP-5`'s `--font-serif-math` · `FR-NP-12` · `FR-EMT-18`'s Σ-vs-∑) — **never per site**. The **D-B2 unicode-range leg is KILLED** and is not revived under either arm.
**Holder**: `.a`, at the cure site, stating which arm it shipped. **Routed**: the `cm-serif` producer no-op → GLASS-RELAY (`.z`'s letter).

#### D10.2 · FR-AH-16 ⊕ FM-21 — the scaling-system owner (SP-9, F.W0-adjacent)

**Mechanism.** ⟨cmd⟩ `grep -n 'FR-AH-16' fr-AppHeader.md` → `:62` *"On every coarse-pointer viewport ≥640px the nav trigger is TALLER than the bar containing it: `.header-inner { height: 3.5rem }` (:161-167) = 63px/56px vs the Button's `--control-h-md` = 3.75rem = 67.5px/60px … the sibling toggle sits 16px shorter because `--toggle-size` literals (:242/:256) don't ride `--ui-scale` — the two controls diverge only on touch. … the root cause is hard-coded bar/toggle literals against po[licy tokens]"*.
**Default stated**: **no scaling system is minted by this wave.** The per-component legs land (delete the literals; producer tokens govern — SP-9's own law), and **`html{font-size:1.125rem}` under 768px GOES** per COHESION **§0j.D G-15(b)** (*"the 12.5% inflation compounds with `--ui-scale` (67.5px against the token author's 60px); F.W1's token-parity re-tune absorbs it, **F.W4 executes per component**"*) — that part is **RULED elsewhere and is not deferred**; what is deferred is **who owns the scaling system**.
**Cost of answering "the producer owns it"**: a token-authority ask across the `--ui-scale`/`--control-h-*`/`--toggle-size` surface — a GLASS-RELAY packet, not a frontend edit. **Cost of answering "the consumer owns it"**: fourier re-mints a scaling vocabulary the producer already ships, against glass-ui-first-class.
**Cost of the default**: the per-component legs cure the sites; the *system* question stays open, so the next component that mints a literal has no rule to violate. **Holder**: **SS-3/SS-4** (§5.2 `→ NO-WAVE-OWNER`: *"FM-21's scaling-system owner question (D10)"*), F.W0-adjacent as §3 marks it.

#### D10.3 · FR-CP-D10 — the `sliceW` + ONE-honest-denominator ruling

**Mechanism.** ⟨cmd⟩ `grep -n 'sliceW' fr-ConvergencePlot.md` → `:107` *"The staged-reveal pedagogy dissolves at default N (~6.3 harmonics inside their windows simultaneously); `N=x/y` counts `progress > 0.5` — matching neither the sum (all N always in) nor the drawn set (`cursors ≥ 3`). | **F.W4** — a deliberate ruling on `sliceW` + one honest denominator."*
**Default stated**: **ONE honest denominator, whichever it is, and the readout names it.** The defect is not the value of `sliceW` — it is that `N=x/y` counts a third thing matching **neither** the sum (all N always in) **nor** the drawn set (`cursors ≥ 3`). The default that ships absent a ruling on `sliceW` is: **pick the denominator the readout claims and make the count agree with it**; do not ship a third population.
**Cost of narrowing `sliceW`** (restoring staged-reveal pedagogy): fewer harmonics in flight, a slower reveal, a pedagogy change on a live instrument. **Cost of leaving `sliceW`**: the staged reveal remains dissolved at default N and the label is the only thing left to make honest.
**Holder**: `.b`, at the cure site, under D3's honesty family — **stating which denominator it chose**. ⊘ This record does not choose `sliceW`'s value; it forbids the third population.

#### D10.4 · FR-CT-C-7 — the announcement-rate decision (conditional on F.W3)

**Mechanism.** ⟨cmd⟩ `grep -n 'C·C-7' fr-ConvergenceTimeline.md` → `:63` *"The ×100 step-1 adaptation under-resolves the harmonic axis: **seat-reproduced** table (0.004823 min-Δt / 99-of-99 pairs under step at total=100; 6/48 at 49; 0/19 at the default 20). Scrub granularity on a time axis, not a break; the naive `:max=\"1000\"` cure would ×10 the announcement rate D·D-9 flags — the cure is adjudicated separately from the defect … | **F.W3** — dissolves if the re-derivation adopts the float 0..1 axis (the producer scrubber's own contract); else F.W4 with an explicit announcement-rate decision."*
**Default stated**: **the row dissolves at F.W3 if the float 0..1 axis is adopted, and F.W4 books nothing.** If F.W3 does not adopt it, the default that ships is **the current ×100 step-1 adaptation unchanged** — because the one named alternative, `:max="1000"`, is **§6.1 adjudicated-dead** (*"the naive `:max=\"1000\"` timeline cure (×10s the announcement rate; cure adjudicated separately from defect)"*) and may never be executed.
**Cost of the default**: the harmonic axis stays under-resolved at the measured granularity (0.004823 min-Δt; 6-of-48 at 49; 0-of-19 at the default 20). **Cost of answering**: an explicit announcement-rate decision — a screen-reader-cadence design judgement on a scrub control, requiring an AT witness this wave does not take (SS-13).
**Holder**: **F.W3 first** (the float-axis re-derivation); if it does not dissolve there, **SS-3/SS-4** with the explicit announcement-rate decision. ⊘ **`.b` books it POSED and lands no `:max` change under either outcome.**

#### D10.5 · FSE-L-B1 — the asset-provenance decision (F.W5–W8 union; DO-NOT-REGENERATE meanwhile)

**Mechanism.** ⟨cmd⟩ `grep -n 'L-B1' fr-FourierShapeExtractor.md` → `:23` *"Latent: the divergence harms no user today and the pipeline that would surface it is already broken at import (L-B2). What it blocks is *work* — any regeneration, any pencil-boil-bump re-baseline, any F.W4 \"keep and reconcile\" of DarkModeToggle. Seat re-ran the decisive instrument (proximity 0.037/2.93/37.57/44.83 — digit-exact vs reader-2) and re-read `9e5ba74`'s body. MAJOR with the DO-NOT-REGENERATE rider; reader-1's BLOCKER preserved in DISSENT."* — and the deepest fact, `:16`: *"the shipped `moon.json` is not derivable from this component's Moon SVG by any tracked or untracked recipe in the tree"*.
**Default stated**: **DO NOT REGENERATE.** `moon.json` is **never regenerated** (`G-F4-NEG-ROSTER`, a keep-green obligation binding on every unit), **FM-19 is FROZEN-FOREVER with a golden-file diff** (COHESION **§0j.D G-15(c)** — *"no regeneration pipeline exists; building one is unsized work"*), and `HLG-44`'s `--minimal` regeneration stays **blocked** behind this decision.
**Cost of answering "regenerate"**: a regeneration pipeline must first exist — §0j.D G-15(c) prices it as **unsized work** — and the shipped assets' provenance is unreproducible by any recipe in the tree, so regeneration would replace an unexplained artefact with a differently-unexplained one. **Cost of the default**: the divergence stays on record and every downstream re-baseline stays blocked — *"What it blocks is **work**"*.
**Holder**: the **F.W5–W8 (SS-4) union** (§5.2: *"moon provenance union (FSE-L-B1)"*). **Dissent preserved (verbatim)**: *"reader-1's BLOCKER preserved in DISSENT."*

**Dependent cluster for D10 as a batch.** `.a` (legs 1 and 2, and the `G-15(b)` root-size execution which is **ruled, not posed**) · `.b` (legs 3 and 4) · `.a`/`.c` (leg 5's DO-NOT-REGENERATE locks) · `.z` (the GLASS-RELAY and union letters carrying legs 1, 2 and 5). ⊘ **Every leg is booked POSED in its unit's receipt, with its default named, or `G-F4-DECISIONS` is not satisfied by this record at close.**

---

## §3 What this record does NOT decide

Named so no seat mistakes silence for permission, and so `.z`'s closure can check the set:

1. **The routed product questions** — D2(b)'s LOG default · D3's *should `/equation` render a spectrum graph* · D4's retire-or-restore · D7's shared domain · D10's five legs. Each has a **holder** named at its section (SS-3/SS-4, F.W3, or the F.W5–W8 union) and a **default that ships meanwhile**.
2. **Anything §5.2 routes away**: the `→ GLASS-RELAY` producer asks, the `→ LATEX-PAPER relay` asks (incl. **PAW-38**, D5's blocker), the `→ F.W5–W8` contract rows, the `→ SS-13` probes. ⊘ **SS-13 flags are deferred, never resolved inline.**
3. **Any cure.** This record authors none, and it writes no fourier byte. The gates, cures and sweeps are `.g`–`.z`'s.
4. **Any gate but its own.** `G-F4-DECISIONS` is this unit's single gate; the other seventeen are read at seat-0's baseline and turned by their own units.
5. **ESC-1.** Ruled at **§0i.3** (8.0.0 @ `17a11bc5`). **Cited, never re-opened** — and every `⊘ ESC-1/G1-GATED` cell a decision touches reads on its 8.0.0 arm.
6. **D9's substance.** Ruled at **§0o ESC-2**; transcribed above with its id and **not re-opened**.

---

## §4 `G-F4-DECISIONS` — the reading

**The gate**, quoted from F-W4 §4 (true bytes `:391`):

> | **G-F4-DECISIONS** | `DECISIONS-F.W4.md` carries D1..D10, each RULED or DEFERRED-WITH-DEFAULT, and no cluster depending on an unruled decision has opened | None exist today; **D9 is OWNER-GATED and unauthorable unruled**; D1 gates every GalleryMarquee row, D4 gates PAW-47/PAW-54, D6 gates PAW-12's rider. |

**BEFORE (born-RED, measured by this seat, double-run):** ⟨cmd⟩ `ls docs/tranches/X/fourier/DECISIONS-F.W4.md` → **ABSENT** (both runs) · ⟨cmd⟩ `grep -c '^### \.' docs/tranches/X/execution/C/F-W4.md` → **0** (both runs; no dependent cluster has opened) · ⟨cmd⟩ `git -C ../fourier-analysis status --porcelain | wc -l` → **0** (both runs). The witness reproduces exactly as written, with **one limb already relieved**: *"D9 is OWNER-GATED and unauthorable unruled"* was true when the gate was authored and is **false today** — §0o ESC-2 ruled it 2026-09-18. That relief is cited, not claimed.

**AFTER:** this record carries **D1..D10**, each landing **RULED** or **DEFERRED-WITH-DEFAULT** (§1's table), each with its honest default carried and the cost of each branch stated. **D6 is RULED outright**, which is `.g`'s whole precondition. **D9 is transcribed as ruled with its id.** **No cluster has opened**: `.i` runs first and alone, and the three gating clauses the cell names are discharged in the direction it asks — **D1 gates every GalleryMarquee row** (RULED: DELETE, so `.d` may open) · **D4 gates PAW-47/PAW-54** (DEFERRED-WITH-DEFAULT, and the default is executable, so `.e` may open on the default arm and on nothing else) · **D6 gates PAW-12's rider** (RULED, with PAW-12 itself still F.W9/W10's).

⊘ **What the gate does not yet read GREEN on, stated honestly**: the gate's second clause — *"and no cluster depending on an unruled decision has opened"* — is a **standing obligation across the wave**, not a one-time reading. It is satisfied at this instant by measurement (zero receipts) and is **re-read at `.z`'s close**, where a unit that executed an arm this record did not rule would redden it retroactively. **This record turns the gate; it does not certify the wave's future obedience to it.**

---

## §5 Measurements taken by this seat (all read-only, all double-run)

Every figure this record publishes is read from the settled bytes and taken twice in one invocation, both runs agreeing (WRITE-THEN-MEASURE). No count of a moving file is banked as an operand (R3-3.10 / R4-8.3), and **this record publishes no self-count**.

| # | measurement | value (both runs) | what it decides |
|---|---|---|---|
| 1 | `DECISIONS-F.W4.md` present? | **ABSENT** before this write | `G-F4-DECISIONS` born-RED |
| 2 | `### .` receipts in `execution/C/F-W4.md` | **0** | no dependent cluster has opened |
| 3 | `fourier-analysis` dirty paths | **0** | crash-recovery: nothing inherited; no fourier byte written by this unit |
| 4 | `web/package.json` scripts | `dev` · `build` · `preview` · `test:e2e` · `test:e2e:ui` | D6: no runner exists (PAW-50 reproduces) |
| 5 | `vite` in `web/`, declared / installed | `^7.3.6` / **7.3.6**; **no** vitest, jsdom, happy-dom, `@vue/test-utils` | D6: the plugin arm's zero-dep advantage is real; the runner arm's cost is one devDependency |
| 6 | `snapshotForTransition` home | **defined** `composables/useCurveTransition.ts:57` | D6: assertable today, no extraction |
| 7 | `fuzzyMatch` home | **defined** `paper/search/paperSearchIndex.ts:70`; re-exported `search/index.ts:2` | D6: the import-home lock (the barrel is the SCRUB's deletion target) |
| 8 | `resolveFigure` home | `paper/PaperArticleWindow.vue:44` (SFC-local) | D6: extraction owed to `lib/figureDimensions.ts` (`.e`) |
| 9 | `contourBounds` home | **does not exist** under `web/src` | D6: minted by `CP-ROW-40` (`.c`) |
| 10 | glass-ui installed version | **8.0.0** | D8: the premise must be re-measured at the live pin |
| 11 | `--slider-scrub*` names in glass-ui 8.0.0 dist | **∅** | **D8's premise HOLDS at 8.0.0** |
| 12 | distinct `--slider-*` names in glass-ui 8.0.0 dist | **6** (4.0.0 census read ten, zero `scrub`) | D8: dead at both pins |
| 13 | `w-full` in glass-ui 8.0.0 slider chunk | **0** | D8: K-8's sub-limb witness does not reproduce — `.c` re-measures before asserting zero-delta |
| 14 | files carrying `--slider-scrub` under `web/src` | **7** (named at D8) | D8's scope boundary: six are NOT this verdict's |
| 15 | `UNREAD` rows in `docs/tranches/V/coordination/INBOX.md` | **0** | E13: no unread mail in scope at this seat's act |

---

## §6 Anchor notes — this unit's contributions to `G-11` (INTENT at the true bytes)

Recorded here rather than left for a later seat to hit mid-edit (the discipline seat-0 used for gate 14). Under F-W4 §0's evidence standard, **findings survive on content; anchors do not**.

1. **The wave record's §4/§5 coordinates are §3-relative, not absolute.** `execution/C/F-W4.md`'s unit plan cites `G-F4-DECISIONS` as `(:39)`, §5.1(3) as `(:48)` and §5.2's `→ NO-WAVE-OWNER` row as `(:62)`. Taken as absolute against `F-W4.md` these land on a table separator (`:39`), a §0a negative-roster row (`:48`) and a bare `---` (`:62`). **They resolve as offsets from line 352** (§3's heading minus one): **`:39` → true `:391`** (`| **G-F4-DECISIONS** | …`), **`:48` → true `:400`** (`3. **Repair-arms-a-defect same-commit riders** …`), **`:62` → true `:414`** (`- **→ NO-WAVE-OWNER / SS-3/SS-4 spec authoring** …`). Verified by ⟨cmd⟩ `sed -n '391p;400p;414p' F-W4.md`, double-run. **INTENT at the true bytes**; every §4/§5 gate coordinate in the unit plan reads the same way, and the §2/§3/§6 coordinates are absolute and resolve exactly.
2. **`fr-EditorControlsDock`'s D8 anchors drift −1 at HEAD** (post-F.W1 uplift): record `:122` (class) → live `:121`; record `:123` (`:style`) → live `:122`; record `:222` (comment) → live `:221`; and the record's `:222-229` rule span **over-runs onto `</style>`** — the true rule body is **`:222-228`**. `:8` (the `VIZ_COLORS` import edge) and `:48` (the first comment) are exact. **INTENT at the true bytes**; `.c` cures at these, not at the record's.
3. ⊘ **No line coordinate into a live sibling is banked by this record.** Cross-spec citations above are by **§-heading, gate id, decision id or banked row id** (F-W4 §0's anchor idiom, R2-2). Where a line number appears it addresses either the frozen adjudicated corpus, a commit-pinned artefact, or `F-W4.md`'s own bytes marked *at this writing* — and each was re-resolved this round by the command printed beside it.

---

**⊘ Standing on this record.** It is a **decisions record, not an authority over the spec**: `F-W4.md` governs, this file rules where `F-W4.md` §3 says a ruling is owed, and where the two ever disagree the spec wins and this record is corrected by dated addendum-beside (**E-3**). Every verdict above is reversible by the owner at read, on the same standing as §0j's delegated rulings.
