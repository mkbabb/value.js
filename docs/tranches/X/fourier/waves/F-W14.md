# F.W14 — fourier: the UI audit's rows, the glass repin, the owner's admin-table frame, and F.W13's residuals

## State
**Opens after**: F.W13 CLOSED and `docs/tranches/X/audit/UI-AUDIT-fourier.md` landed. **Minted by**: COHESION §0bu (2026-09-23). **Model**: Opus 5.5 every seat. **Record**: `docs/tranches/X/execution/C/F-W14.md`. **Tree**: `/Users/mkbabb/Programming/fourier-analysis`, `m/w1-bump-migration`.
**Frame**: `docs/tranches/X/fourier/evidence/W14/owner-2026-09-23-malformed.png` (READ it).

## Authority — the owner's words, verbatim (2026-09-23)
*"these UI elements are malformed in fourier, mark"* (the admin audit log), with the standing fourier dockets (COHESION §0ao, §0bc) and OA-35/36's order that every page be audited and glass-level rows be fixed at the root.

## Units (serial; the ADJACENT-LINE RULE binds)
- **`.m` (Opus, effort high) — repin glass `^8.0.0` → 10.0.1** (the §0bs pattern): read glass's 9.0.0/10.0.0/10.0.1 CHANGELOG and migration notes whole; migrate at the root, no shims; vue-tsc 0, vitest, e2e at `--workers=1` with the load recorded; re-read `DOCK-SCROLL-MORPH` at 10.0.1.
- **`.t` (Opus) — OA-42, the admin audit log table.** Measured on the frame: each action badge has a fixed width, so longer action names (`JANITOR:HARD_DELETE_VISUAL`, `SET_TIER:FEATURED`) spill past the pill; the timestamp column is too narrow and wraps ("2026, / AM"); rows show doubled horizontal rules. Cure on glass primitives (the glass `Badge`/`Chip` sized to its content, or a code-styled label when a name is long; the table on glass's data-table/row idiom with one rule per row; the timestamp a single-line, tabular-figure cell). Sweep every admin table for the same. Gate (served, headed ×2, 1440 and 390, both themes): for every badge, `scrollWidth ≤ clientWidth` (no overflow); the timestamp cell is one line at 1440; exactly one border between rows (computed).
- **`.u` (Opus, effort high) — every `UIA-F-n` row** of the UI audit register: cured at its cause, or routed to glass by id (the O-59 letter).
- **`.r` (Opus) — F.W13's residuals**: the desktop ToC's lost scroll-to-top (restore on the glass primitive beside the disclosure, not in its seat), `.sidebar-link` radius on the canon, `.c` r1–r4, and the J-audit PNG rewrites (point those runs at `test-results/`).
Close: vue-tsc 0, vitest, e2e at `--workers=1` (REDs = the named baseline set), served-page gates re-read by the check seat.

**ADDENDUM 2026-09-23 (COHESION §0bv) — OA-43, unit `.g` (Opus), after `.t`.** Owner, verbatim (frame `fourier/evidence/W14/owner-2026-09-23-grayed.png`, light theme, image mode): *"why is the controls items, these sidebars and elements, so gray and grayed out?"* Measured on the frame: the right sidebar (Image + Decomposition cards, the basis chips, "Replace image") reads under a grey veil against the cream main area — a disabled/inert look on live controls. Find the cause at the bytes (candidates: a translucent dark scrim or `opacity` < 1 on a sidebar ancestor, a surface token mixed toward grey/dark in light theme, a glass surface class meant for dark, an `inert`/disabled style left on, a `filter`/`backdrop-filter` desaturating) and cure at its owner; a glass-owned surface tone is routed to BL by id. Gate (served, headed ×2, light AND dark, 1440 and 390): every ancestor of the sidebar controls computes `opacity: 1`, no `filter`/`grayscale`/`saturate` < 1, no `inert`; the sidebar surface colour equals glass's light surface token for that role (ΔE_OK ≤ 0.02) and matches the other panes' surface; the controls' ink passes WCAG AA against it. Order: [`.m`] → [`.t`] → [`.g`] → [`.u`] → [`.r`].

**ADDENDUM 2026-09-23 (COHESION §0bw) — OA-44 unit `.p` and OA-45 unit `.h` (Opus, effort high).** Owner, verbatim (frames `fourier/evidence/W14/owner-2026-09-23-blurry.png`, `…-hierarchy.png`): *"The epicycles and viz areas are very vlurry and not high res; and the design hireachy and spacing of these elements and sliders, likely on every page, too, is not optimal and needs total reconfiguration"*.
- **`.p` — OA-44, crisp visualizations.** Every canvas/WebGL surface in the app (epicycles, the plots, the contour and image views, thumbnails) renders at device resolution: backing store = CSS size × `devicePixelRatio`, re-sized on resize, zoom and DPR change (a `ResizeObserver` with `devicePixelContentBoxSize` where available), strokes in CSS-pixel units after the context scale; no CSS upscaling of a smaller bitmap. Gate (served, headed ×2 at DPR 1 and 2): for every canvas, `width === round(clientWidth × dpr)` and the same for height; a 1-px stroke edge in a crop spans ≤ 2 device pixels of transition; before/after crops committed.
- **`.h` — OA-45, the control-row idiom and page hierarchy, every page.** Measured on the frame: a slider control takes three rows (label; the value field alone on a right-aligned line; a thick bar with no visible thumb), wasting space and hiding the value's relation to its label. Design pass (design notes + before/after frames per page in the record): ONE control-row idiom from glass primitives — label and value field on one line (or label · slider · value on one line where width allows), the slider directly beneath, the producer's Slider with a visible thumb and track fill; consistent vertical rhythm on the glass spacing scale; each card's title/subtitle hierarchy on the type scale; applied to EVERY page and mode of the app (census first, pasted). A missing primitive (e.g. a labeled slider with an inline value) is routed to glass BL by id, and the composition uses the existing primitives meanwhile. Gate: the census re-run shows every slider row on the idiom; per page, before/after frames at 1440 and 390; no control row taller than the idiom's measured height ±4 px.
- Order: [`.m`] → [`.t`] → [`.g`] → [`.p`] → [`.h`] → [`.u`] → [`.r`].

## ADDENDUM 2026-09-23 — UI-audit fold into `.u` (COHESION §0ca)
- Every UIA-F row with no frame gets one on the served page before its cure.
- UIA-F-51 is a glass row: the glass Toaster anchors to the top and covers the dock at 390. It is relay-only through O-59, never a local override.
