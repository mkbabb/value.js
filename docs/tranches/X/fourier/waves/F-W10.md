# X.F.W10 - Coordination Discharge and the X·F Close (frontend half)

⟨X·F · **status: planned** · half: frontend · fold-authored 2026-08-25 at the COHESION §0a boundary by the fresh-Fable fold seat (M-23), agglomerating the two SS-3 drafts (Fable + Opus) choosily per M-25 — dedupe by identity, cure-shape locks and sequencing edicts carried, routed by mechanism · consumes the F.W10 intake CARRY **whole** — 65 rows · 11 gates · 11 cross-edges · zero silent drops · L-20 validated against `docs/precepts/instructions/` (TRANCHE-AND-WAVE-SPEC · STYLE · ORCHESTRATION · CONSUMING · LESSONS-LEARNED) · idiom: `docs/tranches/X/waves/W0..W11.md` bounds/gates/provenance, cut to the `docs/tranches/X/parse-that/waves/` lean form · all baselines re-measured read-only 2026-08-25 at this fold (receipts inline)⟩

**Alias law**: `F.W10` denotes `X.F.W10` — a search alias, never a rename. The CARRY's `waves/W10.md` name lands as `F-W10.md`; same identity, alias recorded (the F.W9 spec correspondingly lands as `F-W9.md`).

**Scope.** The F-side terminal wave: ADOPTION-ASKS reconciliation both directions · G24/G33 evidence relayed (never owned) · the LATEX-PAPER carry given a receiving surface · terminal disposition of every deferred row with **no re-booking** · the X·F close (COHESION §3.1–§3.6, F-side). The wave is a **ledger act**: it books verbs, stands up registers, and relays letters. It repairs nothing, re-rules no owner-gated question, and edits no tree that is not this one.

**Four-verb**: AUDITED **YES** (fold-measured 2026-08-25) · SPECIFIED **YES — by this file** · IMPLEMENTED **NO** · VERIFIED **NO**. **EXECUTION IS NOT AUTHORIZED BY THIS FILE** — status stays `planned`; nothing opens product source until the owner's begin-word.

**Binding law.** M-25 (transcription-only is DEFECTIVE). L-19 (every gate names a real witness; none is a proof-farm script). L-20 (precepts-latest conformance). E13 (no wave closes with UNREAD mail). Born-RED: every gate below starts RED with its witness named. Two standing laws shape every row:

1. **The registry is READ-ONLY.** The F.W9/F.W10 re-cut lands as a **table in two wave specs plus a census addendum**, never as an edit to `fr-*.md`. Anti-rename holds by construction: every banked id keeps its identity for life; no row is re-booked.
2. **Producer rows never become frontend hacks** (standing owner edict). Glass rows ride **SS-6**; latex-paper rows ride the register **this wave creates**; fourier-tree writes are asked for, never made (COMMISSION §2).

---

## §1 Bounds

Owned (writes happen only at execution, post begin-word):

| # | Path | Act |
|---|------|-----|
| 1 | `docs/tranches/X/fourier/waves/F-W10.md` | CREATE — this spec |
| 2 | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` | CREATE — the SS-6 analogue P-6 needs (G-F10-5), roster §2.6 |
| 3 | `docs/tranches/X/COHESION.md` | MODIFY-carve — §4 add the fourier NO-WAVE-OWNER block (G-F10-7) · §4a accrete the fourier-registry SS-6 glass rows (G-F10-9) · §5 boundary row · §0 X·F status cell. §2/§3 text is root-authored: quoted, never rewritten |
| 4 | `docs/tranches/V/coordination/INBOX.md` | MODIFY — mail rows only (E13): Ask 3 / Ask 5 / inv-22-color / cascade-vjs · FN-1..FN-7 · the 2.0.0 note answered at **4.0.0** · the latex-paper relay row · MG-ι owner-mail flag |
| 5 | `docs/tranches/V/megatranche/coordination/FOURIER-ADOPTION-ASKS-RECONCILIATION-2026-08-25.md` | CREATE — the four-ask reply letter (G-F10-1) |
| 6 | `docs/tranches/V/megatranche/coordination/FOURIER-EVIDENCE-RELAY-G24-G33-2026-08-25.md` | CREATE — the evidence relay (G-F10-3/G-F10-4) |
| 7 | `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` | CREATE beside (E-3 shape) — the NEXT-COMMUNIQUE amendment (G-F10-9). Dated intake bytes never rewritten |
| 8 | `docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` | CREATE beside — the §4.11 F.W9/W10 re-cut record + the §6.7 charter line in its **corrected** wording (G-F10-6 / G-F10-8) |

Bounds reconciliation vs the CARRY: it named `megatranche/coordination/ — CREATE letters only`; rows 5–6 name the letters exactly so no seat invents a third channel. `docs/tranches/X/fourier/` did not exist before this fold (`ls -d` → *No such file or directory*, 2026-08-25); this file's creation is not evidence any sibling exists.

Read-only (bounds, no write): `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (all 66) · `formation/fourier/{CENSUS-2026-08-03.md, lane-docs.md, lane-crud.md, lane-frontend.md}` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` (dated; amended beside) · `coordination/FOURIER-R3..R6*` + `FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md` (immutable evidence) · `docs/tranches/R/letters/CASCADE-VJS-RESPONSE.md` + `docs/tranches/R/audit/coordination/COORDINATION-ANALYSIS.md` (the cascade-vjs reply **exists** — relay it, never re-author) · `docs/tranches/X/fourier/waves/F-W9.md` (the re-cut table must be identical in both files; F.W10 does not author F.W9).

**Explicitly NOT in bounds:** product source either tree (`/Users/mkbabb/Programming/fourier-analysis/**`, `value.js/{src,demo,api,test,e2e}/**`) — execution gate · `package.json` · `scripts/dev/dev.sh` (unowned, NEVER) · fourier's `docs/constellation/ADOPTION-ASKS.md`, `docs/tranches/N/**`, any fourier INBOX/CLAUDE.md (**fourier-sub-session writes under COMMISSION §2**) · `latex-paper/**`, `glass-ui/**`, `keyframes.js/**` (producer trees reached **only by relay letter**) · `docs/tranches/X/{waves,keyframes,parse-that}/**` · `~/.codex/**`.

---

## §2 Carry

Sixty-five banked rows, agglomerated by mechanism. **A row's presence here is a disposition, not a work order** — most execute elsewhere; this wave's claim on them is the terminal record.

### §2.1 RE-CUT-0 — this wave's own precondition (recorded, not booked; no banked id exists)

**BLOCKER (spec-integrity; blocks BOTH F.W9 and F.W10 from stamping).** Fold-measured 2026-08-25: `grep -o 'F\.W9/W10' registry/adjudicated/fr-*.md | wc -l` → **54 occurrences / 25 records**; bare `F.W10` routing → **0**. No adjudicated row is provably homed here today, while census §4 makes F.W9 deploy-spine/evidence and F.W10 coordination/close — disjoint shapes. Measured anatomy of the 54 (the correction the RE-CUT-0 dissent demanded — *the re-cut must not manufacture rows from boilerplate*):

| class | count | disposition |
|---|---|---|
| **routing identities** (a defect row's own disposition) | **26** | re-cut, one wave each — table §2.2 |
| **routing-law boilerplate** (the header line listing all wave ids) | **14** | re-worded in the census addendum's taxonomy statement; **zero rows minted** |
| same-identity restatements (`fr-App:142`; `fr-PaperArticleWindow:186`) | **2** | fold to their row; never counted twice |
| verdict/adjudication prose | **12** | dated bytes, **immutable**; annotated by the addendum, never re-cut |

Seven records yield zero re-cut rows: the six the orchestration flagged (VisualizationView, CoefficientsPanel, CanvasOverlayButton, FrequencyGraph, HarmonicLevelGrid, InfoCard) **plus fr-AdminUserList** (its single hit is the header line; FR-AUL-16 at :54 routes NO-WAVE-OWNER). CanvasOverlayButton/InfoCard still enter via the REAL rows FR-COB-20 and F8-REACH-01/02.

Cure: re-cut all 26 identities to exactly one of F.W9 / F.W10 / a named other wave, set-difference ∅ both directions (COHESION §3.1, the X·V 117-row idiom); **both specs carry the same table; neither stamps until it closes** (G-F10-6).

### §2.2 The re-cut table (26 identities; identical in F-W9.md) + P-12 locks

Execution seat = **F.W9-widened (or a minted F.W9a)** per the P-12 packet ruling; the F.W10 share of every F.W9-cut row is the **terminal-disposition record only**. Three cuts deviate from the banked digraph **by mechanism** (flagged ⟨re-cut⟩; recorded in the census addendum). Other-wave legs already homed (F.W0/W1/W3/W4) are cited, never re-cut.

| record | identity (kept for life) | cut | mechanism + locks / preserved dissent |
|---|---|---|---|
| fr-AdminAuditLog:87 | **AA-44** | **F.W9** | packet root: zero frontend tests, 9 e2e never enter the audit tab, backend pins its half. Instrument = GAB-10's `grep -rln "admin" web/e2e/` → **0 files**. Cure: extend ONE existing axe keystone (visualization-ux:26-43 + visualization-crud AxeBuilder) into the admin tab; live-region half stays F.W4. **Dissent (FR-AFP-49, preserved): the family carries blocker weight — "every row above incl. all four blockers is invisible to every gate the repo runs"; INFO is a scope convention (L-9.1), never "the debt is small"** |
| fr-AdminFlaggedPanel:95 / :110 | **FR-AFP-42** (test seat) · **FR-AFP-49** | **F.W9** | 0 props / 0 emits / setup-top-level fetch in `defineAsyncComponent` — untestable seam. Seam → F.W3/W4 (homed); only the seat re-cuts. Distinct mechanism from AA-44 (testability ≠ coverage) — never folded; seam-before-seat. **Quote 8 specs, never 9** (K11) |
| fr-App:91 | **C-13 (App)** | **F.W9** | four untyped ambient global side effects, no vitest floor. The four-wave chain made explicit: **MG-θ → F.W0** (tsconfig/lint one-liners) · floor **decision → F.W4** (census §4.5) · **execution → F.W9** · **terminal record → F.W10**. Distinct id from FunctionInput's C-13 — never merged |
| fr-ContourPreview:71 | **38** | **F.W9** | zero coverage; `vue-tsc -b` is precisely the gate row-11 is invisible to. **No repair inherited — both flagship cures REFUTED** (the `<Card tier surface>` migration = 21-site typecheck break; the restore-path blank render cannot occur). The simplify-heap unit target anchors F.W4's harness decision — the cheapest true vitest justification in the packet, not PAW-50's trapped `resolveFigure` |
| fr-EquationModeToggle:41 | **FR-EMT-11** (= C-16) | **F.W9** | no unit runner; `/equation` one assertion-free screenshot slug; axe installed, never pointed here. Rider verbatim: "the gates for FR-EMT-1/-5's cures ride their own waves." **Dissent PRESERVED verbatim: reader-2's MAJOR ("every other row in this record is ungated because of it") ruled a scope error under L-9.1 — "so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small."** The close honours this warning |
| fr-EquationResult:41 / :72 | **FR-EQR-6** (axe leg) · **FR-EQR-31** | **F.W9** | `.eq-scroll-region` scroll container: no tabindex/role/name; KaTeX spans give no focusable descendant. F.W4 = attributes (homed); F.W9 = the single `/equation` seat. Cohort: fr-CoefficientsSpectrum M-6 same class. FR-EQR-31: zero behavioural coverage by construction; D-20's archaeology half rides FR-EQR-27's correction — **not re-booked** |
| fr-EquationView:62 / :191 | **D·D-B2** (axe leg) · **C·D-28** | **F.W9** | zero functional e2e on `/equation` (8 specs / 1 screenshot hit); info button + Wand2 = two nameless icon-only controls; aria-label ×2 → F.W4 (homed). **The packet's single strongest F.W9-seat argument: the ONE interaction spec (compute → notation → budget → reload) mechanically catches B-1, B-2, C-29, D-07, D-14 — five already-booked defects.** BLOCKER→MAJOR per the fr-CT precedent, both readers concur |
| fr-FunctionInput:80 | **C-13 = D-24** | **F.W9** | `/equation` screenshot-only; the nameless Wand2's survival is the corollary. FOLD to banked fr-CollapsibleSection m-8 class (`actions` slot ready). Naming half belongs to FR-TT-1 / EquationView D·D-B2 — never double-book |
| fr-GalleryAdminBanner:49 | **GAB-10** (coverage half) | **F.W9** | GAB-1's `color-contrast` serious class unverified by construction. **FOLDS BY REFERENCE to AA-44**; live-region half → F.W4 riding GAB-4's `aria-busy` (homed). GAB-13 (F.W0) gates it: gates written against the dirty worktree measure a tree no commit holds |
| fr-GalleryCardModal:93 | **GCM-42** (= L-14) | **F.W9** | zero tests every level; console guard filters error-only + 404 — blind to GCM-4's warns three ways. **FOLD → AA-44 / FR-AUL-16 class, never re-booked.** Own increment surviving the fold: widen the guard to `warn`. **Interaction lock: no filter widening before F.W1 lands** — the error-only filter is the blind spot F.W1's four zero-console-error gates depend on (FR-EQR-3). Dissent carried: DU's BLOCKER on GCM-4; the SR composite (GCM-4∘16∘17∘6/8) named blocker-weight with no BLOCKER row carrying it |
| fr-GallerySearchBar:54 | **FR-GSB-17** | **F.W9** | its named e2e is vacuous by construction — every assertion inside `if (await filterToggle.isVisible())` (gallery.spec.ts:57-63); delete the bar → GREEN. Un-vacuate, don't add: **assert the precondition EXISTS**; kill the unreachable `.glass-dock` branch (:49-53; `grep -rn glass-dock src/` → 0) and the `.or()` mask (:19-22). One cure shape authored ONCE for the class (with FR-USB-16 + FR-GV-36's census) |
| fr-GalleryView:78 | **FR-GV-36** (= L-24) | **F.W9** | every defect structurally invisible (no vitest/ESLint/noUnusedLocals/vueCompilerOptions); console e2e passes against a dead API. Folds → FR-GFC-27 + AA-44 route + FR-AUL-16 input. Surviving increment: **L's three-conditionally-vacuous-tests census**. The dead-API fact belongs to the F.W0/F.W9 deploy-spine reckoning — edge declared, API repair NOT booked here |
| fr-ImageUpload:61 | **27** | **F.W9** | all **15** e2e entries across **6** spec files use `setInputFiles` — drag/reject/oversize/failure/keyboard uncovered. **Quote the 15/6 pair, never a re-derived count** (ruling 7's denominator). The drag path needs FR-USB-16's coarse project — one matrix change unblocks both |
| fr-PaperArticleWindow:58 / :90 | **PAW-12** (rider limb) · **PAW-34** (= C-i-5) | **F.W9** | PAW-34: zero e2e on any owned surface ("figure" hits = `test.describe.configure` substrings). PAW-12: two facts on one hand-typed literal (`TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))`) guarding a no-fallback `<picture>`; predicate split → F.W3/W4 (homed); F.W9 = the build-time set-equality assertion (vitest or Vite build plugin per PAW-50). Invariant **HOLDS at HEAD** (28 png / 26 avif / 26 webp; unpaired `fourier.png`+`maintainer-avatar.png`; 26 keys = 26 `\includegraphics`) — the rider guards drift. **PAW-50: harness-before-rider. PAW-49: flag-before-split.** Dissent preserved: LC's MINOR sustained at R-2 |
| fr-PaperArticleWindow:199 | **PAW-49** | **F.W0** ⟨re-cut⟩ | without `noUncheckedIndexedAccess`, `resolveFigure`'s return asserts `width: number` while one whole arm yields `undefined` — vue-tsc green over a false type. The flag is a **repo-config** line, identical in kind to MG-θ/M7's tsconfig flags; **the one coverage-family row whose gate is FREE** — `npm run build` already runs `vue-tsc -b`, no new harness. F.W3/W4 rider on the PAW-12 cure homed |
| fr-PaperArticleWindow:200 | **PAW-50** | **F.W4** ⟨re-cut⟩ | web/ has **no** unit harness at all (five scripts, no vitest/jest, zero `*.test.ts`); `resolveFigure` trapped in the SFC. It *is* the harness-before-rider law — census §4.5's **decision** is its only true home; **F.W9 executes** (move `resolveFigure` beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12); **F.W10 verifies**. Governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44 |
| fr-PaperSidebar:49 | **D-B2** (axe leg) | **F.W9** | "Contents" heading 2.39:1 light / 3.00:1 dark vs the 4.5 floor; no axe visits `/paper`. Token → F.W4 (homed). The `/paper` seat is **ONE artifact** with M7 + PAW-34 and **runs AFTER PAW-1's cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. MAJOR, blocker-weight in the a11y aggregate |
| fr-PaperSidebar:58 | **C-M4** | **F.W9** | the expand-if-closed guard reads the **Tooltip's** `data-state` (domain `closed\|delayed-open\|instant-open`, never `"open"`) — a no-op. F.W4 half dies with the `CollapsibleTrigger` restructure (homed); F.W9 re-keys the guard on the Collapsible root. Corrected cite :124–129. Third vacuous-gate instance but a **DISTINCT mechanism** (wrong element's state) — never folded; the cure differs |
| fr-PaperSidebar:67 | **M7** | **F.W9** | dead surface unreachable by any gate; two of four needed gates are one config line each. tsconfig half → **F.W0 = the SAME identity as MG-θ — one substrate edit, two records, booked once**; F.W9 = axe on `/paper` + lint script |
| fr-PaperView:120 | **D/i-1** | **F.W9** | zero coverage of the search surface, no vitest. **Coupled: rides the B-1/B-2 cure wave, never scheduled independently** (verbatim lock) |
| fr-Tooltip:26 | **FR-TT-1** (promotion note) | **F.W10** ⟨re-cut⟩ | the shim is the ONLY label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring `aria-describedby` only-while-open — a DESCRIPTION, never a NAME. It governs **this wave's close-gate design** (G-F10-10). Naming legs → F.W4 (all 17); shim leg → F.W3 keeping its R3-7a/X-11 identity; folds honoured, never re-booked (EditorControlsDock C-3 · CanvasControlsDock D-1/L-3/C-1 · EquationView D·D-B2). Producer corroboration glass-ui 8.0.0:69-73: "an icon-only trigger labels ITSELF." L-4's 17-count census citable; D-1's 16-count + :42/:49 anchors KILLED (register #8) |
| fr-UserSlugBar:53 | **FR-USB-16** (≡ r1-MISSED-5 ≡ r2-MISSED-1) | **F.W9** | self-disabling test: `gallery.spec.ts:66-82` body wrapped in `isVisible().catch(() => false)`; matrix is one Desktop-Chrome project. Unguard (assert the trigger EXISTS) **+ add the mobile/coarse Playwright project** — a matrix change that must precede every coarse-pointer witness (glass a11y-overrides floor; PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` arm; ImageUpload 27's drag path). **Dissent preserved: r2 graded MINOR; overruled at the ruling table** |

**By-mechanism entrants** (records carry NO digraph — they enter by mechanism, not by re-cut, and must not be counted into the 54):

- **FR-AUL-16** (fr-AdminUserList:54, MAJOR) — the only cascade-delete surface, zero automated coverage of any kind; "un-harnessable" defence KILLED (ruling 3e). NO-WAVE-OWNER: harness decision → F.W4 · devDependency + admin-tab axe limb → F.W9 · **terminal home decided AT F.W10** (§3.3 deadline — last F wave). Cure verbatim: "extend one axe keystone into the admin tab + a vitest devDependency"; slug-bearing aria-labels = ready selector vocabulary.
- **FV-26** (fr-FullscreenViewer:79 = D-23·C-m7) — the only e2e "Fullscreen" hit is a comment (gallery.spec.ts:113). Coverage share → F.W9. **Teleport lock: assert against the teleported root** (FV-27: one of two Teleport sites). FV-30 rides banked BasisCanvas D-4/BC-2/C-2a + AnimationControls C-12, no new booking; FV-31 → F.W4.
- **FR-COB-20** (fr-CanvasOverlayButton:74 = C-7) — interaction contract entirely implicit; no test anywhere. "Dies with the file"; the no-test-for-wrappers observation → SS-3/SS-4 input; the tree favours DELETE. Gated on **F8-REACH-02** at F.W0 — **HOLD is RED; F.W10 verifies no HOLD survives** (that verification is this wave's whole claim, not a re-booking).
- **FR-AH-45** (fr-AppHeader:108 = C-16, NO-WAVE-OWNER spec-input) — zero props/emits/slots, no contract. A question owed an answer ("what contract should a shell header state?") → SS-3/SS-4 spec-input; terminal home or kill-with-rationale at F.W10, never left open. **Discharges together with FR-AH-30's cure-caution: the D-axis rank-5 remedy REVERSES the outcome — only `@layer glass-overrides` appended after `utilities` works** (glass-ui's competing rules already sit in `@layer components` at higher specificity).
- **PAW-33** (fr-PaperArticleWindow:89 = C-i-2) — rides the C-19 ⊕ PAW-33 pin-truth row (§2.6).

### §2.3 Substrate + terminal-disposition verification (F.W0 rules; F.W10 verifies, never re-rules)

| row | disposition F.W10 checks |
|---|---|
| **M1** (fr-PaperSidebar:9/:124) — glass-ui 4.0.0 `dist/styles/index.css` syntactically corrupt (`postcss.parse`: 35 top-level nodes, zero valid `@source`, one garbage at-rule named `` source` `` at :203–222) — single-handedly explains the dead-animation BLOCKER, kills 3 findings, corrects 2 banked rows, invalidates 2 prescribed repairs | F.W0 pre-gate + **glass BH relay** (with L-10/D-M4 headroom/rebaseline ask). Paired **M3 F.W1 pre-gate discriminator** goes into the wave spec: `grep -c '<class>' components.css glass-ui.css` — 0/0 ∧ absent from `web/src` ⇒ not painting; a naive `@source` widening resurrects the 40px icon box and the `--ui-glyph` override at once, flipping three findings |
| **GAB-13** (fr-GalleryAdminBanner:52; ruled MAJOR up from DU's INFO) — the audited artifact is the DIRTY WORKTREE (28 modified rows incl. subject + `web/package.json`: HEAD `^3.1.0`/`^2.2.0`/`^0.10.0`/lucide `latest` vs WT `^4.0.0`/`^4.3.0`/`^0.13.0`/`^1.0.0`) | F.W0: land the WT diff OR record it explicitly as audit baseline BEFORE any per-file budget commits. **Dissent PRESERVED verbatim (worker-DU): "the fleet has already elected to treat the working tree as the scope" — if F.W0 formally adopts the worktree as baseline, GAB-13 discharges to a disclosure line and DU's grading was right all along.** Either branch is terminal — a disclosure line is a disposition, not a dodge |
| **F8-REACH-01 / F8-REACH-02** (`FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md:30-31`) — InfoCard.vue (N.C11, sha b2c2e718…, 1,516 B) + CanvasOverlayButton.vue (N.C39, sha 35f3db91…, 595 B) unmounted, un-dispositioned | F.W0 rules the exact verb — `KEEP_WITH_MOUNT` \| `ISOLATED_HARNESS` \| `DELETE`; **HOLD is RED; F.W10 verifies no HOLD survives to close** (G-F10-11) |
| **CODEX-R3–R6 residue** (INTAKE §3/§4; census §6.7; lane-docs §4c — 11 files, none in fourier's tree) | Charter line lands in the **corrected wording** — ⟨CARRY CORRECTION, verified at fold: the CARRY's falsifier quotes *"carry zero credit"*, which the census's own ADDENDUM (CENSUS-2026-08-03.md:312-314, cross-ruled at INBOX I-24a) already SUPERSEDED to: **"Zero UNVERIFIED credit — every Codex verdict enters through per-claim adjudication (M-21)"**. Landing the superseded wording would re-introduce the discard posture M-21 forbids⟩. Terminal verbs: **R3-9** (record only — never cite as method guarantee) · **R3-16 + R4-11 + R5-9 + R6-10** (the 14-wave M board is the real object; the 72/158/P29/slot superstructure is Codex-internal, non-transferable) · **R4-12** — ⟨CARRY CORRECTION, measured at fold: `/tmp/fourier-r4-files.sha256` is **GONE** (`ls` → *No such file or directory*); the banked "74,507 B still on disk" is stale; verb = **EVAPORATED — unrecoverable, consume none of its rows** (the consume-nothing lock unchanged, now moot)⟩ · **R5-3** (re-hash or drop) · the Codex worktree `~/.codex/worktrees/d0be/fourier-analysis` (measured at fold: **still registered**). **OWNER RULINGS OWED, flagged inline, never presumed: OG-F1** (R3-1/R3-4/R4-1/R4-13/R5-1/R6-1 — freeze-with-adoption favoured; R6 self-declared `NO_SUCCESSOR`, the ruling is cheap) · **OG-F2** (R6-3 root-absence-receipt binding). Rows routed F.W0; F.W10 verifies terminal (G-F10-8) |
| **NO-WAVE-OWNER register drain (F-side)** — COHESION §4 has 14 rows, all X·V slugs, zero fourier (fold-verified); the fourier records carry ≥22 | Row all into §4 with a terminal verb (wave / next-boundary ledger / kill-with-rationale): AA-45, AA-46, AA-47, FR-AFP-51, FR-AFP-52, FR-AUL-16, fr-App i-2 (route-owned → per-route challenges), **fr-App C-12 (negative guard: App is API-inert — R6-8/R3-7c must not grow a false leaf; a negative finding is a terminal disposition too — record it, do not manufacture work)**, fr-App MG-ι, FR-AH-30, FR-AH-45, FR-CP-43, FR-CP-44, **FR-CP-45 (law for SS-3/SS-4: take D's CLAIMS and L/C's LINES; re-resolve every anchor before quoting into a wave spec)**, fr-EasingPicker D/D-21, PAW-32, PAW-38, PAW-39, **PAW-42 (empty/loading/error proven unreachable — epistemic record)**, PAW-45, PAW-46, **GAB-29 (MetricPill — the falsifier keeping the wave off two dead primitives)**, FR-COB-20 (G-F10-7) |
| **SS-4 prerequisite (TA-4 atomdiff)** — `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` excised at T.W1/TA-4; only hashing survives; `J-diff-shape.md §6` probes cannot run value-side; a co-signature over un-runnable probes is **void** | F.W10 verifies the declaration EXISTS in the F.W5 spec: value-side restoration named a PREREQUISITE **or the contract explicitly re-scoped**. Work belongs to the value.js API row, outside fourier's waves. SS-4 owner rulings FLAGGED INLINE, never presumed: trie-vs-KISS (the documented guardrail at `atomdiff.py:12-14` is the **incumbent**) · remix-vs-fork · born-visibility · visibility-enum · version-`_id` · redaction parity · pagination idiom. **Dissent recorded: the trie requirement collides head-on with a standing KISS guardrail present in BOTH trees — owner ruling precedes design** |
| **L-4 = C-B2 = D-N10 (+M6)** (fr-PaperSidebar:33) — one 98-node tree derived three times, both producers export `useSidebarFollow`, nothing chooses a winner | **OWNER-RULING escalation at F.W1** (which producer owns the ToC model); F.W4 executes the collapse (delete `paperTree.ts` + the losing index; arbitration point = the untyped `defineExpose` seam, M6). **F.W10 cannot stamp terminal disposition while the escalation is unanswered — close-gate input, flagged inline** |

### §2.4 Coordination discharge (the rows this wave itself executes)

| row | verb + mechanism |
|---|---|
| **ASK-3** (palette-api deploy; P1, *"the hardest of the four and the true critical-path item"*, :57) | rsync deploy-dir blocks `deploy-hook.sh` adoption and `dispatch.sh` deletion (the fourth migration gating it). Book the disposition verb (ADOPT / DECLINE-with-rationale / DONE) into `V/coordination/INBOX.md` + relay to fourier's ledger. Adopt-shape: rsync → git checkout under canonical root. F.W10 books, never executes infra |
| **ASK-5** (GH-Pages → CF Pages; P3; targets value.js AND keyframes) | Their acceptance verbatim: **`color.babb.dev` serves from CF Pages with a `cf-ray` header, peaceiris step removed** — the only machine-checkable ADOPTION-ASK, cheapest to close or decline honestly. The keyframes half belongs to KF's mail seat — declared, not absorbed |
| **INV-22-COLOR** (P3, their G.W8) | `api.color` vhost serves `/`→200 only; `/health` `/docs` `/openapi.json` 404. Book the verb + relay; repair is maintainer-owned infra outside F bounds. Edge to F.W9's deploy spine (the 44-behind silent-rollback chronic) declared — F.W9 owns deploy, F.W10 owns correspondence |
| **CASCADE-VJS** (P2, their H.W6; fix at `H/waves/W6-W7-epsilon-booking.md §2A`) | **SPLIT the ask**: unplugin-vue-markdown `^29.2.0→^32.0.0` + lockfile regen ADOPTABLE; `file:`→published **DECLINED — conflicts with R's Q4 ratification (keep `file:` deliberately, `R.md §3.4`)**. Banked instruction verbatim: **"respond, don't silently ignore."** The reply EXISTS (`docs/tranches/R/letters/CASCADE-VJS-RESPONSE.md`, tracked at COORDINATION-ANALYSIS E13) and is untracked in fourier's tree — reconciliation = landing it in their ledger, never re-authoring. **The conflict IS the dissent; both positions ratified in their own trees — preserve both, decline one limb with rationale.** Loop closed in BOTH ledgers (the X·V W4-D2 lesson, now law) |
| **FN-1..FN-7** (VALUEJS-R-UPLIFT-ASKS, committed `83d4c9f`; all seven unanswered since 2026-07-03) | Answer all seven with a disposition verb. **STANDING RIDER verbatim, load-bearing: "FN-5 must be authored BEFORE or WITH fourier M.W10"** — M.W10 is exactly the version-shape transpose FN-5 guards; M.W10 is `planned`, FN-5 does not exist — the one non-answer with a dated consequence. FN-5/FN-6/FN-7 are **SS-4 contract inputs**, not coordination trivia. FN-7 (neutral home for `J-diff-shape.md`) requires two fourier-tree writes — authored in fourier's own sub-session under COMMISSION §2; value.js declares the ask, never the edit |
| **VALUEJS-2.0.0-NOTE** (committed `cd26c65`; the note is itself two majors stale) | **Reconcile FORWARD: the reply states 4.0.0, never the stale 2.0.0** — answering at 2.0.0 answers a superseded question. The correct bump is F.W1's `value 0.13→4.0` limb, making the note dischargeable-on-adopt. Paired unstruck row carried: **§3's M.W7 `sampleColorRamp` "book for 0.13.0" is dischargeable-on-adopt and NOT struck in `M/PROGRESS.md:22`** |
| **O-14 + inbound-parity** (census risk [P2] #9; `INBOX.md:76`) | Correspondence is one-way in fact AND in law: fourier has no INBOX ledger (no E13 analogue), replied to none of the three value→fourier files, the facility-19 table sits UNTRACKED (`??`), and our INBOX carries zero inbound rows FROM fourier. Two limbs: (a) fourier-side INBOX + CLAUDE.md = census F.W0's act in fourier's sub-session — F.W10 relays the ask and **verifies receipt**; (b) value-side: row the four ADOPTION-ASKS + FN charter + 2.0.0 note with verbs so E13's "no wave closes with UNREAD mail" is CHECKED, not asserted. Census §6.6 ADMISSION-GAP: the co-signed contract's signature needs a durable fourier surface — F.W10 cannot close before F.W0 stands it up |
| **G24** relay | **RELAY ONLY — the gate is X-W9's** (`X/waves/W9.md:366`). Banked: 8/22 curves drift, max\|Δ\| 0.192 vs `<1e-3` over 8 names via `fourier-value-import-drift.mjs`. **MEASURE-AT-OPEN — never inherit the number.** W.L5's value half ships in X-W9.f under D-15's grant; "the fourier-tree edits and π evidence are explicitly NOT X-W9 gates" |
| **G33** relay | RELAY the packet + INBOX legs; F.W1's atomic tri-package transaction turns the pin leg green. **Identity discipline, enforced six times: ONE row — fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8 — component-altitude INFO, never re-booked per component** (C:B-1, C-2, C-4, C-27, F-1, C-03 are reproductions, not bookings). **Dissent adopted (fr-CollapsibleSection rider): the ERESOLVE is the uncommitted M.W1a bump — committed pins read `^3.1.0`/`^2.2.0`/`^0.10.0` — "red today" is true of the working tree and only the working tree (ties GAB-13)** |
| **NEXT-COMMUNIQUE contradiction** | INTAKE §3 reads *"EMPTY. Zero producer-facing asks survived"* while COHESION §4a carries SC-1..SC-8 and the registry adds the live glass + latex ask sets. Amend by **addendum-beside (E-3), never rewriting the dated intake bytes** (bounds #7). Route every surviving ask into exactly ONE register: glass → SS-6 BH relay (M1 + L-10/D-M4 4-failing-stops incl. 3.58 @ stop 11 · GAB tier-tint home + the 3.26:1 label rung · FR-EQR light-arm `--success` 2.175:1 · FR-CP-11/27 · AppHeader cm-serif undeclared variable + light `--tier-featured`); latex-paper → the register G-F10-5 stands up. **Dissent: none — the intake line was true of the Codex corpus and false of the registry that followed; both stand, dated** |
| **MG-ι** (fr-App:143, cross-repo) | Relay into the value-side ledger with the **owner-mail flag**: the `color2` dispatch hazard is input to the V·π/parser-seam program; no fourier wave adopts it. Route law binds any follow-on: **direct parse-that → fourier FORBIDDEN** — parser → value (X·V L1/L5) → packed release → consumers |

### §2.5 The LATEX-PAPER-RELAY roster (bounds #2; G-F10-5 — P-6's terminal home)

**LATEX-RELAY-HOME** (BLOCKER for P-6): fold-measured — `grep -c latex-paper docs/tranches/V/coordination/INBOX.md` → **0**; COHESION §4a is glass-only. ≥18 named carries route to a register that does not exist. F.W10 creates `LATEX-PAPER-RELAY.md` and rows every carry, banked ids kept, none re-booked; then answers PAW-1's own F.W1 question in the ledger: **latex-paper added to the tri-package transaction or explicitly seated.** **Anchor law (SC-3):** installed latex-paper 0.2.1 ships **no composable src** — every virtualiser anchor cited from **dist/vue.js** (the running artifact) or the record; producer-src paths for the installed pin cite bytes the consumer never runs.

| carry | content + locks |
|---|---|
| **PAW-1** (:42, BLOCKER sustained R-2 on seven arms) | theme.css declares shadcn bare triplets; glass ships complete colours; fourier defines none → the **42** `hsl(var(--x))` IACVT-dropped sites → rewrite to `var(--x)` — the sound limb. **KILLED (K-9): the `:root` triplet-alias limb — the repair must not implement the literal snippet** (breaks PaperView :524, style.css :110-112, :182/:184, glass recipes). **Version-invariant**: seat-verified at 4.0.0 AND v7.0.0 AND v8.0.0 — the tri-package uplift cures nothing here. **F.W1 ledger rider carried: "the transaction as chartered is INSUFFICIENT for the paper route — add the LATEX-PAPER carry to the transaction or seat it explicitly."** Locks: **PAW-30 lands in the SAME repair** (its clip is latent only because PAW-1 dropped the background); **LAW-3: PAW-44's `overflow-x: clip` restoration lands WITH-or-AFTER PAW-1+PAW-30, never before** (else the K-18-killed see-through-header catastrophe goes REAL). Round-2 re-scope stands (E-3): sticky-scroll arm K-18-dead; :447/:459 re-filed LATENT-behind-PAW-44 |
| **PAW-2** (:48, MAJOR; absorbs D-i-5) | never-painted `.deferred-section` wrappers write the 1200px estimate into the never-invalidated `SESSION_HEIGHT_CACHE`. Cure verbatim: gate `measureSection` on the CV-state event, or skip-when-equal-to-the-estimate, or move `.deferred-section` off the measured element; **K-19: the identity guard at dist :869 is the exact repair line**, not the axis's cited byte. **COUPLING LAW (L-2/L-4) verbatim: "the per-patch ref re-read is currently the only repair mechanism — fix this BEFORE memoising the ref (PAW-4)."** K-16: measurement-safe rhythm cures only. Dissent: D/C filed BLOCKER; demotion to MAJOR sustained via K-6 |
| **PAW-11** (:57; K-4 premise-corrected) | visuals ride PAW-1; **focusability arm needs real `href`/tabindex at the latex-paper EMITTER** (5 emitters produce href-less `<a>`, not spans) — cannot be shimmed consumer-side |
| **PAW-28 + PAW-45 + PAW-46** (:79/:195/:196 — three module-scoped-cache rows, ONE letter) | PAW-28: instance-scope or namespace `SESSION_HEIGHT_CACHE` (deliberately excluded from `onUnmounted`; dissent: DU's INFO preserved under the R-6 MINOR). PAW-45: bound or instance-scope the KaTeX memo (dist :387; 1145 unique spans / 2025 occurrences / ~263-266 display envs vs ≤18 mounted; `cache.delete\|clear\|size` → 0) — **constraint travels with the ask: superlative 13, the memo makes PAW-3/4 churn survivable — cap, don't delete**. **PAW-46 ≡ PaperView L/D21, ONE mechanism banked twice — carried once, both ids cited, neither re-booked**: key on tex + macro identity or move the Map inside `useKatex`; **L/D21's increment survives the dedupe: the docblock promises an `inject` fallback the code lacks** |
| **PAW-32** (:88) | emit `__VLS_Slots` for `PaperSectionBlocks` (sibling has 2) — `figure.*`/`callout.*` destructure implicit `any`; NO-WAVE-OWNER locally, P-6 member |
| **PAW-38** (:94) | uniquify slugs at the emitter; LATENT (93 slugs unique today, `uniq -d` → empty); test-shaped → joint P-6/P-12 — the cheapest guard is the harness PAW-50 says doesn't exist |
| **PAW-39** (:95; SC-2 seat-corrected) | `sourceLevel` emitted, never read — 51 `\section` + 31 `\subsection` all `<h3>` at the same `top: 3.5rem`. **Census law: 11 + 51 + 31 = 93; quote 31, never the readers' un-anchored 35.** Dissent carried: "a future seat re-grading PAW-15 upward should weigh that the a11y composite (PAW-15∘11∘7∘39) approaches blocker weight as a cluster" — the GalleryCardModal R-1 precedent shape |
| **K-11 flattener rider** (:115, re-graded by K-18 at :206; cure-integrity constraint, not a defect row) | chapters and subsections emitted as flat PEERS — the **SECOND** of two gates on the two-tier sticky stack; **the scrollport is the first; reworking the flattener alone ships a no-op**. Bound to LAW-3 + the PAW-44 formation decision (SS-13 witnesses first); **LAW-4: one clearance authority only (PAW-47's clearance constant depends on the PAW-44 decision)** |
| **PaperView set** | **C-06** (:70, MAJOR): 0.2.1 ships `activeId/activeRootId: ComputedRef<any>` (d.ts:19) → six downstream contracts vacuous; generic fix at `ref<T\|null>`; **+ F.W1 rider** — why PAW-1's fourth-package ask is not optional · **L/D5 + L/D30** (:75): both unbind paths re-read a template ref after Vue's unmount nulling — guaranteed no-ops; cure `onBeforeUnmount` (LC) or the cached-element idiom (S9); **producer twin relayed WITH the consumer half (F.W4) so neither re-introduces the other** · **L/D8** (:77): per-patch forced layout read — `measureSection` reads `el.offsetHeight` as its argument, before the dedupe; guarded twin 300 lines away (S1); same family as PAW-4(b) — **cite both, book once**; PAW-2's COUPLING LAW binds · **L/D29** (:92): `useTreeIndex` records the ROOT id as `parentId` for depth≥2 (:27/:32), masked by `isDescendant`, 34 nodes live — latent, correctly filed; the same package's other walk gets it right · **C-19 ⊕ PAW-33** (:125/:89): declared peers silently violated — katex `^0.16` vs 0.17.0 + vite `^6\|\|^7` vs 8.0.16, all `optional: true` — **ONE fact, two banked ids, both cited, neither re-booked** (PAW-33 carries the vite half C-19 omits); routes F.W1 + relay; feeds G33's pin-truth leg; **distinct from the glass/keyframes/value peer deadlock — never folded** · **C-20** (:126, K-6-excised headroom cell): `hsl(var(--section-heading))` invalid at 6 theme sites — **named subset of PAW-1's 42; discharges inside PAW-1's carry, carried separately only so the 6-site cell survives the 42-count quote** |
| **PaperSidebar L-11 + M2** (:40/:62 — ONE relay carries both) | L-11: damped-rAF sidebar-follow has no PRM gate anywhere in dist/vue.js (0 hits), a third ungated clock — **amend lane-frontend §8**. M2 (MAJOR): shipped `useSidebarFollow` hard-codes fourier's private scoped class `.sidebar-top-btn` — replace with a data-attribute hook. **HARD LOCK verbatim: "F.W4 must not rename the class before the relay lands"** — D-B3/D-M8's cures touch it; renaming silently converts scroll-to-top clicks into sticky manual-override suspensions |

---

## §3 Gates

Eleven. All born RED with the failing probe named; fold-measured 2026-08-25 where marked. Every gate names a live witness (L-19 honoured — no descriptive invocation, no proof-farm scripts).

| gate | born state · witness | falsifier (turns it GREEN) |
|---|---|---|
| **G-F10-1** ADOPTION-ASKS reconciled BOTH ledgers | RED [measured] — fourier `docs/constellation/ADOPTION-ASKS.md`: 43 rows, 12 OPEN, 11 naming value.js, **four OPEN against us (Ask 3, Ask 5, inv-22-color, cascade-vjs)**; value-side `grep -ni fourier docs/tranches/V/coordination/INBOX.md` → **6 lines (I-14, O-14, O-10a, I-24, I-24a, I-25)** — none of the four, none inbound from fourier | Each of the four carries an INBOX row with a disposition verb AND the reply landed in fourier's ledger; cascade-vjs answered as the SPLIT using the existing `R/letters/CASCADE-VJS-RESPONSE.md` — never a fresh letter, never a write into fourier's tree (COMMISSION §2 — this wave asks) |
| **G-F10-2** Inbound-mail parity | RED [measured] — fourier has a flat `docs/tranches/N/` path, **no `coordination/INBOX.md` (no E13 analogue)**, replied to none of the three value→fourier files (`83d4c9f` · `cd26c65` · facility-19 table **UNTRACKED `??`**); our INBOX has ZERO inbound rows FROM fourier | FN-1..FN-7 each carry a fourier-side verb; facility-19 TRACKED; the fourier INBOX exists (census F.W0's act) with ≥1 row this wave can point to. **FN-5's rider is the one non-answer with a dated consequence** |
| **G-F10-3** G24 drift relayed, not inherited | RED — **MEASURE-AT-OPEN** — `fourier-value-import-drift.mjs` (`X/waves/W9.md:366`); banked 8/22 drift, max\|Δ\| 0.192 vs `<1e-3` | max\|Δ\| < 1e-3 across the 8 restored analytic-arm names, **re-measured at wave open, never inherited**. The relay may not quote the banked number as current, may not claim the gate (it is X-W9's), and no fourier-tree edit is credited as an X-W9 gate |
| **G-F10-4** G33 packets + post-window pin truth | RED [measured] — `npm ls @mkbabb/value.js` in fourier exits ELSPROBLEMS (glass 4.0.0 peers `^0.10.0\|\|^0.11.0` vs installed 0.13.0; keyframes 4.3.0 hard-deps `^0.13.0` — disjoint), reproduced ≥6× across the registry; `web/package.json:18` still `^0.13.0` vs published 4.0.0 | 5 packets + 5 INBOX rows + post-window `npm ls @mkbabb/value.js` exits 0 in **keyframes AND fourier** (MEASURE-AT-OPEN by construction). Pin leg greens only via F.W1's atomic transaction. Falsified by: the row re-booked per component (it is ONE, C-1 ≡ C-8); the post-window leg read before the window; the uncommitted-bump dissent dropped |
| **G-F10-5** LATEX-PAPER receiving surface | RED [measured at fold] — `grep -c latex-paper docs/tranches/V/coordination/INBOX.md` → **0**; COHESION §4a is SS-6 GLASS only; ≥18 named carries route to a register that does not exist | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` exists with every §2.5 carry rowed AND PAW-1's F.W1 question answered in the ledger (transaction or explicit seat). Falsified by: any anchor quoted from producer-src rather than dist/vue.js (SC-3); the K-9-killed alias snippet appearing as a prescribed repair |
| **G-F10-6** Re-cut complete, Δ=∅ both directions | RED [measured at fold] — `grep -o 'F\.W9/W10' docs/tranches/V/megatranche/registry/adjudicated/fr-*.md \| wc -l` → **54 / 25 records**; bare `F.W10` → 0; `F-W9.md` does not exist yet | Every routing identity cut to exactly one of F.W9 / F.W10 / a named other wave; both specs carry the same §2.2 table; set-difference ∅ both directions (COHESION §3.1). Falsified by: a row minted from the 14 boilerplate lines or the 7 boilerplate-only records; a row re-booked under a new id; **any edit to `fr-*.md`** (the registry is read-only — the re-cut is a table, not a rewrite) |
| **G-F10-7** NO-WAVE-OWNER drained (F-side) | RED [measured at fold] — COHESION §4: 14 rows, all X·V slugs, **zero fourier**, vs ≥22 fourier rows owed | §4 carries a fourier block; every §2.3 listed row bears a terminal verb (wave / next-boundary ledger / kill-with-rationale). fr-App C-12 lands as the negative record |
| **G-F10-8** Codex residue not inherited; rulings answered | RED [measured at fold] — census §6.7 charter line absent from any F spec; **OG-F1 + OG-F2 unanswered**; `/tmp/fourier-r4-files.sha256` **GONE** (R4-12 = EVAPORATED); Codex worktree `~/.codex/worktrees/d0be/fourier-analysis` **still registered**; 11 `FOURIER-R*` files in our tree, none in fourier's | Charter line lands **in the corrected ADDENDUM wording ("Zero UNVERIFIED credit… per-claim adjudication, M-21"), never the superseded "zero credit"**; OG-F1/OG-F2 **ruled by the owner** (inline flags, never presumed — F.W10 verifies terminal, it does not re-rule); R3-9, R3-16+R4-11+R5-9+R6-10, R4-12, R5-3, the worktree each terminal; **none of R4-12's rows consumed** |
| **G-F10-9** NEXT-COMMUNIQUE ledger truth | RED [measured] — INTAKE §3 says "EMPTY. Zero producer-facing asks survived" while COHESION §4a carries SC-1..SC-8 and the registry adds the live glass + latex ask sets | Addendum-beside (bounds #7) amends without touching dated bytes (`git diff --stat` on the intake stays empty); every surviving ask in exactly ONE register (glass → SS-6; latex → G-F10-5's); no glass row cured consumer-side |
| **G-F10-10** Close-gate a11y honesty (the FR-TT-1 promotion lock) | RED [measured] — FR-TT-1: the shim is the only label on **17** icon-only triggers, description-not-name, with the banked promotion note **"BLOCKER at any F.W9/W10 axe close-gate"**; today `grep -rln "admin" web/e2e/` → 0 files, no axe visits `/paper`, `/equation` = 1 assertion-free screenshot | The close axe run covers `/paper` + `/equation` + the admin tab with zero serious/critical, **OR the 17 naming legs are recorded as a named, dated waiver. It may not be argued green while the legs are open** — an axe close-gate authored before F.W4's legs is RED by construction |
| **G-F10-11** F.W0 substrate terminal before close | RED [measured] — M1 (corrupt dist: 35 nodes / zero valid `@source` / `` source` `` at :203–222) · GAB-13 (28 dirty rows) · F8-REACH-01/02 (HOLD is RED) | M1 relayed to the glass BH inbox and the dist parses (or the pin moves); WT diff landed OR recorded as baseline; both F8-REACH rows carry an exact `KEEP_WITH_MOUNT`\|`ISOLATED_HARNESS`\|`DELETE`, no HOLD surviving. F.W0 rules; F.W10 verifies |

---

## §4 Sequencing (locks + cross-edges, declared from this end)

**Ordering locks carried whole:**

1. **RE-CUT-0 precedes every other row** — neither F.W9 nor F.W10 stamps before G-F10-6.
2. **F.W0 substrate pre-gates FIRST** (M1 · GAB-13 · F8-REACH-01/02 · the Codex residue set · MG-θ/M7 tsconfig one-liners — one edit, two records · re-cut PAW-49) — every F.W1/W3/W4 budget is otherwise measured against a dist that does not parse and a tree no commit holds.
3. **F.W1 is ATOMIC land-or-lose** (producer bump + 162-site prop rewrite + copied→status triple in ONE change, else four zero-console-error e2e gates go red — FR-EQR-3) and **precedes F.W3/W4 (§2 law)**. **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)** — the lock lives in F.W1's spec. F.W10 schedules nothing that presumes the uplifted tree.
4. **HARNESS-BEFORE-RIDER (PAW-50)** governs every vitest-shaped disposition (PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44): decision F.W4 (census §4.5's only test language) · execution F.W9 · terminal record F.W10. **Flag-before-split (PAW-49)** rides it — the predicate split is not type-safe until the flag lands or the return is honestly `number | undefined`. F.W10 cannot book a coverage row terminal while the harness decision is unmade.
5. **Repair-arms-a-defect riders, same commit** (carried where their rows land, F.W3/W4): fr-PaperSearchModal D-1+D-3+`outline:none` · **PAW-44/LAW-3** restore-only-with-background-cure (the scrollport restoration lands WITH-or-AFTER PAW-1 **and** PAW-30) · **PAW-30 in the same repair as PAW-1** · MPC-31/MPC-3⊕10⊕13⊕8⊕22 one-cut law · FR-MSP-6 two-channel lock · **LAW-4** one clearance authority (PAW-47 depends on the PAW-44 decision).
6. **PAW-2's coupling law**: fix the measurement write **before** memoising the ref (PAW-4); K-16 constrains co-landing rhythm cures to measurement-safe forms.
7. **FR-TT-1 promotion lock** (G-F10-10): the axe close-gate follows F.W4's 17 naming legs or declares a dated waiver.
8. **GCM-42 console-guard interaction**: widening the error-only filter to `warn` at F.W9 could redden F.W1's four zero-console-error gates on pre-existing warnings — declare it; **no widening before F.W1 lands**.
9. **Axe-seat economy**: the `/equation` seat is ONE artifact (FR-EQR-6, FR-EQR-31, FR-EMT-11, FunctionInput C-13, EquationView D·D-B2/C·D-28) — author once, cite five times. The `/paper` seat is ONE (D-B2, M7, PAW-34) and runs AFTER PAW-1's cure + the PAW-44 decision. The mobile/coarse Playwright project (FR-USB-16) lands before any coarse-pointer row is witnessable (ImageUpload 27, the a11y-overrides coarse floor, PaperSidebar's `--ui-scale`/`--control-floor` arm).
10. **L-11+M2 hard lock**: F.W4 must not rename `.sidebar-top-btn` before the latex-paper relay lands.
11. **Route law**: direct parse-that → fourier FORBIDDEN (parser → value X·V L1/L5 → packed release → consumers) — this wave mints no such edge. Glass-producer rows → SS-6 BH relay, **never frontend hacks**. MEASURE-AT-OPEN on every relayed number (G24/G33).

**Cross-edges** (all declared from this end; each owed a reciprocal sentence in the named file — the X·V W4-D2 lesson, now law):

| to | edge |
|---|---|
| **F.W9 (widened, or minted F.W9a)** — reciprocal REQUIRED | the P-12 packet EXECUTES there; F.W10 owns terminal records only. Handed over per §2.2: AA-44, FR-AFP-42/49, FR-GSB-17, FR-USB-16, FR-EQR-6, FR-EQR-31, FR-EMT-11, FunctionInput C-13, EquationView C·D-28 + D·D-B2's axe leg, GCM-42, FR-GV-36, GAB-10's coverage half, FV-26, App C-13, ContourPreview 38, ImageUpload 27, PAW-34, PAW-12's rider, PaperView D/i-1, PaperSidebar D-B2/C-M4/M7, FR-AUL-16's execution limbs, FR-COB-20's harness branch. Same table both specs; neither stamps before G-F10-6 |
| **F.W4** | the vitest-floor **decision** (re-cut PAW-50) + the 17 FR-TT-1 naming legs. An axe close-gate authored before those legs land is RED by construction |
| **F.W0** | substrate terminal-verification (G-F10-11 + G-F10-8): M1, GAB-13, F8-REACH-01/02, the Codex residue, MG-θ/M7, re-cut PAW-49. F.W10 verifies; it does not re-rule |
| **F.W1** | PAW-1's ledger rider (the fourth-package ask) · C-19 ⊕ PAW-33 pin truth · PaperView C-06 rider · the L-4 OWNER-RULING escalation · M3's pre-gate discriminator · the 2.0.0-note answer stated at 4.0.0 |
| **F.W5–W8 / SS-4** — both-ends declaration REQUIRED | FN-5 ("BEFORE or WITH fourier M.W10") · FN-6 · FN-7 (two fourier-tree writes, COMMISSION §2) as contract inputs; TA-4 restoration named PREREQUISITE or contract explicitly re-scoped (COHESION §2); owner rulings inline, trie-vs-KISS incumbent recorded. Boundary: neither side's edits gate the other's waves |
| **SS-6 (glass BH relay)** | the accreted glass rows (§2.4 NEXT-COMMUNIQUE cell) ride the ONE batched letter at the boundary; F.W10 is the F-side boundary |
| **LATEX-PAPER-RELAY (new; ask-only)** | §2.5 roster whole; anchor law SC-3 (dist/vue.js only); M2's rename lock rides it |
| **X·V (X-W9.f / X-W9.i · V·π)** | G24/G33 relayed, never owned; W.L5 value-half under D-15's grant; MG-ι into the V·π ledger with the owner-mail flag; the forbidden parse-that→fourier edge never minted |
| **fourier sub-session (COMMISSION §2)** — ask-only | their writes: INBOX + CLAUDE.md (census F.W0), FN/2.0.0/ADOPTION-ASKS reply rows, facility-19 tracking, and the two parity-probe residuals (valuejs-J-atomdiff, valuejs-J-publish — fourier-owned, re-homed to their M.W12) |
| **KF.W10** — ∦, declared to prove non-overlap | the intake's KF block (B18-12/13/14; the boundary block citable ONLY as `345/12/57 @ 8281638c`; B18-27 slot 4-vs-5) is KF's alone; no F-side absorption, no double-homed id (COHESION §3.1) |
| **X-whole (COHESION §3.1–§3.6)** | F.W10 is the last F wave: the F-side of §3.1 (every CARRY row in exactly one wave), §3.2 (every census wave-sketch id F.W0–F.W10 → spec or terminal kill with rationale), §3.3 (NO-WAVE-OWNER terminally homed), §3.4 (edges reciprocal, zero cycles) closes HERE; §3.5's L-20 conformance loop with a declared-Fable final pass is this sub-tranche's own close act |

---

## §5 Excluded (nothing silently dropped; every exclusion reasoned; zero CARRY rows among them)

| excluded | reason |
|---|---|
| The **14 boilerplate** routing-law lines + **12 verdict-prose** digraph mentions | not routings: boilerplate mints no rows (the RE-CUT-0 dissent's explicit warning); verdict prose is dated adjudication bytes — immutable, annotated by the addendum only |
| Rows from the 7 boilerplate-only records (VisualizationView, CoefficientsPanel, FrequencyGraph, HarmonicLevelGrid, fr-AdminUserList's header line, and the boilerplate lines of CanvasOverlayButton/InfoCard) | their only digraph hit is the law line — no row manufactured; the REAL rows (FR-AUL-16, FR-COB-20, F8-REACH-01/02) enter above |
| Any edit to `registry/adjudicated/fr-*.md` | read-only bounds + anti-rename — the re-cut is a table in two specs and one addendum |
| The repairs themselves — PAW-1's 42-site rewrite, PAW-2's measurement gate, PAW-11's emitter semantics, the cache trio, the flattener nesting, glass-ui cures (M1's dist, ramp headroom, label rungs, `--success`, `--tier-featured`) | producer work in producer trees; this wave asks, it never edits — and producer rows NEVER become frontend hacks |
| MPC-31 / MPC-3⊕10⊕13⊕8⊕22 one-cut law · fr-PaperSearchModal D-1+D-3+`outline:none` rider · FR-MSP-6 two-channel lock · PAW-44/LAW-3 restore rider (as a repair) | same-commit riders whose rows land at F.W3/W4 — carried by those specs per "locks land where their rows land"; they appear here only where they sequence (§4.5, §4.9) |
| Every fourier-tree write — their INBOX, CLAUDE.md, ADOPTION-ASKS reply rows, FN answers, facility-19 tracking | COMMISSION §2, their sub-session; F.W10 relays and verifies receipt |
| The keyframes half of ASK-5; KF.W10's rows (B18-12/13/14, the boundary block, B18-27) | KF's mail seat / KF's close — declared, not absorbed; no id double-homed |
| ASK-3 / INV-22-COLOR infrastructure execution | value.js-maintainer-owned deploy infra, outside any F wave's product bounds; F.W9 owns the deploy spine, F.W10 owns the correspondence |
| Owner rulings — OG-F1, OG-F2, L-4's ToC ownership, SS-4's seven (trie-vs-KISS et al.), OC-1 | owner acts: FLAGGED INLINE throughout, never presumed, never pre-answered — F.W10 verifies rulings landed; it does not rule |
| R4-12's row contents | consume-nothing lock — and now moot: the file has evaporated (fold-measured) |
| The 72/158/P29/slot Codex superstructure | Codex-internal; does not transfer — only the 14-wave M board is a real disposition object |
| D-1's 16-count census + its :42/:49 anchors · PAW-1's `:root` triplet-alias limb · PAW-1's sticky-scroll consequence arm · ContourPreview's two flagship cures | KILLED at their registers (#8 · K-9 · K-18 · refuted twice) — no repair inherited from killed corpora |
| A fresh cascade-vjs letter; any new cross-repo channel | the reply exists (`R/letters/CASCADE-VJS-RESPONSE.md`) — relaying IS the reconciliation; SS-6 and the new latex-paper register are the only lawful couriers |
| Any product-source open (`fourier-analysis/**`, `value.js/{src,demo,api}/**`); any status advance beyond `planned` | the execution gate — nothing opens product source until the owner's begin-word |

---

**Close act**: when G-F10-1..11 are GREEN, F.W10 stamps the F-side of COHESION §3.1–§3.4, runs the §3.5 L-20 conformance loop with a declared-Fable final pass, and records the boundary row in COHESION §5. **L-18 still applies** — no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance; the bases that apply here: a re-cut that looks complete because it counted boilerplate; a disposition verb that reads as a decision while being a deferral; a relay whose receipt was asserted rather than verified; an axe close-gate argued green over 17 open naming legs; a "terminal disposition" stamped while an owner ruling is outstanding. Until then: **planned**, born-RED, every witness named above.
