# X.F.W10 - Coordination Discharge and the X·F Close (frontend half)

⟨X·F · **status: planned** · half: frontend · twice-authored 2026-08-28 (Fable seat · Opus seat) and folded WHOLE by the fresh-Fable agglomeration seat (M-23) under M-25 — **provenance, HISTORICAL NOTE with no gate weight (R-3)**: the 2026-08-25/28 authoring sessions' intake roster stood at **77 rows · 12 gates · 12 cross-edges**, agglomerated by identity, cure-shape locks and sequencing edicts carried, routed by mechanism (transcription-only is DEFECTIVE). **No `F-W10-CARRY.md` exists and none is authored here**: the sub-tranche's ONLY citable carries are `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md`, cited BY PATH, and every id-keyed closure claim in this file is measured against **the 66 `fr-*.md` registry AND NOTHING ELSE** — never against a per-wave carry, and (**R2-6b, the operand re-ruled**) **never against a check file**: `conformance/PASS-1/F-W10-CHECK.md` and `conformance/PASS-2/F-W10-CHECK.md` are hostile registers this file answers, cited as **provenance only**, never as the LHS of a closure claim. Round 1 took a check file as its drain operand and inherited that file's blind spot whole; the correction is structural and is minuted at §2.8 **E-12** · L-19 (every gate names a live witness) · L-20 validated against `docs/precepts/instructions/` (TRANCHE-AND-WAVE-SPEC · STYLE · ORCHESTRATION · CONSUMING · LESSONS-LEARNED) · idiom: `docs/tranches/X/waves/W0..W11.md` bounds/gates/provenance ⟨…⟩ cut to the `docs/tranches/X/parse-that/waves/` lean form · **twin**: `F-W9.md` (fold-authored 2026-08-28) — its §2.2 spliced the shared bytes from the 2026-08-25 fold of THIS file; the 2026-08-28 re-authoring kept that region byte-identical (verified by diff at write); **repair round 2 (2026-08-28) SETTLES the R-2b direction the way R2-6a rules it — F.W9 AUTHORS the visual-regression checkpoint set, F.W10 RECEIVES it by splice — and the receipt is performed, not promised**: ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty** (this seat, read-only, in `waves/`). Round 1's inverted narration — which would have had a reader overwrite the twin's ruled-in table with this file's rival paragraph — is STRUCK and minuted at §2.8 **E-3**; the shared bytes are byte-identical **today**, and the Δ=∅ leg of G-F10-6 ≡ G-F9-21 is measured, never asserted · **REPAIR ROUND 3 (2026-08-28) RUNS THE DERIVATION ROUND 2 ONLY PROMISED**, per `PASS-3/RULINGS-3.md` R3-10.6 · R3-2.4 · R3-5.5 · R3-6.1 · R3-10.2 · R3-3.7/3.9: the strict drain roster is **derived fresh from the 66** and published as a transcript that partitions all **165** NO-WAVE-OWNER lines with the arithmetic closing (**§2.5a-iii**); `fr-PathPreview`'s three strict rows — which escaped this drain entirely and are settled by **no** F wave — land with per-id terminal verbs; **ten rows round 2 filed under a class verb are strict at this file's own probe** and take per-id verbs; the weak class is enumerated whole (32 lines / 19 records) so predicate and roster regenerate each other; the 81/92 band disclosure is **struck and replaced by the partition** (the sets CROSS, and the two-shape probe spans **44** records, not 56); `AA-45`/`AA-46`/`AA-47` are RULED to this drain as their ONE home with the F-W5 citation direction stated at this end; **E-4 is discharged** and its contradiction with E-17 resolved; the minted `D/i-1` quotation is re-scoped as a **PAIRED edit with F-W9 in one commit**; and **F-W3's §X.1-v4 — landed FIRST this round — is consumed by citation at every row of this file that touches the twin surface.** Reconciled drain roster **75 → 88 rows** at round 3, **→ 89 rows / 89 terminal verbs at round 4** (the 69th strict disposition `L-19+C-N1`, exempted at round 3 and landed at R4-7.3); no id re-booked, re-graded or renamed · **REPAIR ROUND 4 (2026-08-29) IS THE CENSUS FREEZE**, per `PASS-4/RULINGS-4.md` R4-1.12 · R4-2 · R4-3 · R4-4.4 · R4-7.1 · R4-7.3 · R4-8.3 · R4-9.3/9.4 · R4-10, against `PASS-4/F-W10-CHECK.md`'s five MINORs and six INFOs: **`conformance/CENSUS-CANONICAL.md` is adopted as the SOLE census operand and F.W10's record-side roster is stamped ZERO** (canonical §4.1 — no record names F.W10; §2.5b runs the escape test against the canonical's four NWO packet rosters, **60/60 booked-or-cited, 0 escaped**, and the fabrication test in the other direction, which convicts §2.2's `FR-TT-1 → F.W10` cut against the canonical's F.W4 home and files it as a **PENDING PAIRED EDIT** beside AA-44 `9`→`8` — ⌧ **the `FR-TT-1` half is DISCHARGED at repair round 6, 2026-08-29 (PASS-6 D-9): the cut is converted to cite-the-holder `F.W4` in BOTH twins in one pass, Δ=∅ preserved; AA-44's stays outstanding**); the **band-correction pipeline is REVOKED and RE-CUT** (R4-4.4 — the round-3 form printed `-h`, so `cut -d: -f1` read a line number, and it dropped both taxonomy filters; the honest pipeline returns **92 lines / 44 records** and the figure follows the pipeline); **E-1's self-binding denominator corrected 11 → 12 OPEN** (Ask 4's three external-repo legs; the list is authoritative and the prose follows it); **§4b's O-20 count corrected 26 → 22 id-rows with the R4-8.3 sha256 pin `f6e04c86ddb8`**; **five verbatim labels re-pasted byte-true** (PP-SEVLAW · FR-TT-22 · PAW-56 · D-i2 · FR-USB-38's restored emphasis); **`$V`/`$F` DECLARED in-block and re-run** (R4-2.3 — they were phantom witnesses at E-4, the same breach the ruling books against F-W2 and F-W5); the **69th strict disposition landed with a per-id verb**; **FR-TT-21's false half-warrant struck at the citing end**; **G-F10-7's superseded 31/61 probe marked where it is printed**; the last **self-count struck at G-F10-11**; the `L-19` homonym disclosed; the **R3-6.1 edge declared at §4b from this end**. Toolchain pinned and swept: **zero `-P`, zero `\K`, zero lookaround, zero `.{n,m}` in this file** · supersedes the 2026-08-25 fold at this path (65→77 rows · 11→12 gates · 11→12 cross-edges) · witnesses re-measured read-only 2026-08-28 against value.js HEAD `54a97489`+worktree and fourier HEAD `cd26c65` — receipts inline; drift recorded at §2.8, never silently corrected⟩

**Alias law**: `F.W10` denotes `X.F.W10` — a search alias, never a rename. The census wave-sketch's `F.W10` (item 11 at `formation/fourier/CENSUS-2026-08-03.md:211`, *"F.W10 · coordination discharge + close"*) lands as `F-W10.md`; same identity, alias recorded (the twin correspondingly is `F-W9.md`). **Banked ids keep their identity for life; nothing below is re-booked.**

**Scope.** The F-side **terminal** wave, and a **ledger act** throughout — it books verbs, stands up registers, relays letters, verifies receipts, and stamps the F-side of COHESION §3.1–§3.6. Five limbs: (1) the F.W9/F.W10 **re-cut** — the precondition that lets either spec stamp at all; (2) **ADOPTION-ASKS reconciliation both directions** — four asks OPEN against us since G.W8, plus the FN charter, the 2.0.0 note, and the one-way-correspondence kill; (3) **G24/G33 evidence relayed** — never inherited, never owned (the gates are X-W9's); (4) the **latex-paper carry given a receiving surface** — the SS-6 analogue ≥18 named carries route to and that does not exist; (5) **terminal disposition of every deferred row with no re-booking** — the NO-WAVE-OWNER drain, the substrate verification, the Codex residue, and the close. **F.W10 repairs nothing**, re-rules no owner-gated question, and edits no tree that is not this one.

**Four-verb**: AUDITED **YES** (registry-adjudicated; re-measured 2026-08-28) · SPECIFIED **YES — by this file** · IMPLEMENTED **NO** · VERIFIED **NO**. **EXECUTION IS NOT AUTHORIZED BY THIS FILE** — status stays `planned`; nothing opens product source until the owner's begin-word.

**Goal criterion.** This wave succeeds if, at close, a reader can answer three questions from this document alone: **where does every adjudicated fourier row live** (exactly one wave, one register, or one dated kill), **what did the constellation actually receive** (a packet file under the recipient's path containing the arm's own term — a receipt read, never asserted), and **what is still owed to the owner** (a flagged list of rulings, none pre-answered). It fails if any disposition reads as a decision while being a deferral.

**Binding law.** M-25 (a spec ignoring adjudicated rows is DEFECTIVE; transcription-only is DEFECTIVE) · L-19 · L-20 · E13 (no wave closes with UNREAD mail) · COMMISSION §2 (neither side's edits gate the other's waves; fourier-tree writes are **asked, never made**) · **R3-9** (an assertion of non-execution is *recorded*, never cited as a method guarantee) · **FR-CP-45's anchor law, binding on this file's own citations**: *take D's CLAIMS and L/C's LINES; re-resolve every anchor before quoting into a wave spec* (applied at §2.8). Two standing laws shape every row:

1. **The registry is READ-ONLY.** The re-cut lands as a **table in two wave specs plus a census addendum** — never as an edit to any `fr-*.md`. Anti-rename holds by construction.
2. **Producer rows never become frontend hacks** (standing owner edict). Glass → **SS-6**; latex-paper → the register **this wave creates**; fourier-tree acts → **asked**.

---

## §1 Bounds

### §1a Owned — value.js tree (writes only post begin-word)

| # | Path | Act |
|---|------|-----|
| 1 | `docs/tranches/X/fourier/waves/F-W10.md` | CREATE — this spec (supersedes-in-place; §2.2's shared bytes identical to `F-W9.md`'s) |
| 2 | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` | CREATE — the SS-6 analogue P-6 needs (G-F10-5); roster §2.6; anchors from `dist/vue.js` only (SC-3) |
| 3 | `docs/tranches/X/COHESION.md` | MODIFY-carve ONLY — §4 add the fourier NO-WAVE-OWNER block (G-F10-7) · §4a accrete the fourier-registry SS-6 glass rows (G-F10-9) · §5 boundary row · §0 the X·F status cell. **§2/§3 text is root-authored — quoted, NEVER rewritten** |
| 4 | `docs/tranches/V/coordination/INBOX.md` | MODIFY — mail rows only (E13): Ask 3 / Ask 5 / inv-22-color / cascade-vjs · FN-1..FN-7 · the 2.0.0 note answered **at 4.0.0** · the latex-paper relay row · the MG-ι owner-mail flag. Existing rows never rewritten |
| 5 | `docs/tranches/V/megatranche/coordination/FOURIER-ADOPTION-ASKS-RECONCILIATION-2026-08-25.md` | CREATE — the four-ask reply letter (G-F10-1); date-stamp re-resolves at execution; **the PATH is fixed so no seat invents a third channel** |
| 6 | `docs/tranches/V/megatranche/coordination/FOURIER-EVIDENCE-RELAY-G24-G33-2026-08-25.md` | CREATE — the evidence relay (G-F10-3/G-F10-4). **Relay-only; it never claims the X-W9 gates** |
| 7 | `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` | CREATE BESIDE (E-3 shape) — the NEXT-COMMUNIQUE amendment (G-F10-9). The dated intake bytes never rewritten |
| 8 | `docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` | **CREATE BESIDE — co-owned with F.W9**: F.W10 owns the §4.11 re-cut record, the by-mechanism cut deviations (PAW-49→F.W0 · PAW-50→F.W4; ⌧ **the third, `FR-TT-1→F.W10`, is RETIRED at repair round 6, 2026-08-29 — the cut was converted to cite-the-holder `F.W4` in both twins in one pass, PASS-6 D-9, so the addendum records two deviations and one discharged fabrication**), the FR-CP-44 metric-stack re-seat correction, the FR-AFP-51 tense correction, and the §6.7 charter line in its CORRECTED wording (G-F10-6/G-F10-8). F.W9 §1a row 2 takes MODIFY (its §4.10 deploy/coverage cells + its own errata) — **the CREATE stays here**, per the twin's own bytes ("F-W10 §1 already claims the CREATE") |

**Bounds reconciliation.** The intake roster named `megatranche/coordination/ — CREATE letters only` (historical note only — R-3: no per-wave carry file exists to re-read, and none is minted); rows 5–6 name the letters exactly. Row 8's create/modify split is settled by the twin's measured bytes (F-W9 §1a row 2 + its reconciliation note) — the Opus draft's inversion (F.W10 MODIFY) is **rejected at §2.8 E-3**. Disjointness holds by section; the two waves are serial at the boundary.

### §1b Read-only bounds (no write)

**`docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` — THE CENSUS OF RECORD (R4-3 · R4-10, added at repair round 4): the SOLE operand for every roster, denominator and per-wave home this file states. The distinction it forces, and this file now keeps everywhere: the 66 `fr-*.md` are the frozen CORPUS — the bytes every quotation, anchor and mechanism comes from — and the CANONICAL is the CENSUS — the rosters and homes every count comes from. A quotation cites the corpus; a denominator cites the canonical; neither substitutes for the other, and no third census operand may appear (§2.5b)** · `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (**all 66** — anti-rename; the re-cut is a table, not a rewrite) · `formation/fourier/{CENSUS-2026-08-03.md, lane-docs.md, lane-crud.md, lane-frontend.md}` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` (dated; amended **beside**) · `coordination/FOURIER-R*` (9 files, §2.8 E-6) + `FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md` (immutable evidence) · `docs/tranches/R/letters/CASCADE-VJS-RESPONSE.md` + `docs/tranches/R/audit/coordination/COORDINATION-ANALYSIS.md` (**the reply EXISTS — relay it, never re-author**) · `docs/tranches/X/fourier/waves/F-W9.md` (the twin; **F.W10 does not author F.W9**) · **`glass-ui/docs/tranches/BJ/coordination/valuejs-outbound-*.md` (ten) AND `glass-ui/docs/tranches/BK/coordination/valuejs-outbound-*.md` (one — `valuejs-outbound-2026-08-28-o20-authoring-block-batch.md`, 59 lines) = the ELEVEN sent packets** (re-measured 2026-08-28: `find glass-ui/docs -name 'valuejs-outbound-*.md' | wc -l` → **11**; the correspondence moved tranche BJ→BK and this bound moves with it, else the receipt sweep reads a superseded inbox — **read for receipts, G-F10-12**) · `docs/tranches/X/fourier/carry/{F-W1-CARRY.md, F-W4-CARRY.md}` (**the sub-tranche's ONLY carries**, cited by path — R-3; the checkpoint set received by splice at §2.2 is `F-W1-CARRY.md`'s **§CrossEdges item 9, the `F.W1 → F.W9 / F.W10` row**, cited **by path and row label, never by line** — the carries are live siblings, so R2-2 clause 2 forbids an address into them, and **round 1's `:246` is retired HERE as well as at §2.8 E-3**, which had claimed the retirement while this bound still carried the coordinate) · `docs/tranches/X/fourier/conformance/{PASS-1,PASS-2}/F-W10-CHECK.md` (**hostile registers, PROVENANCE ONLY — never the LHS of any closure claim in this file; R2-6b re-ruled that operand to the 66 records**, and round 1's contrary reading is the defect §2.8 E-12 minutes) · `docs/tranches/X/fourier/waves/F-W0.md`, cited **by gate id only** — **§4 gate G-11** (the corrected-anchor table) and **§4 gate G-12** (the corrected-denominator table), **quoted, never re-performed**; ⟨cmd⟩ `grep -n '^### G-1[123] — ' waves/F-W0.md` (this seat) returns the three gate headings, which are the citable identities — **line cites into this live sibling are forbidden from R2-2 forward** and the eight this file carried are converted at §2.8 **E-11**.

### §1c Explicitly NOT in bounds

All product source in either tree (`/Users/mkbabb/Programming/fourier-analysis/**`, `value.js/{src,demo,api,test,e2e}/**`) — **the execution gate** · `package.json` · `scripts/dev/dev.sh` (unowned, NEVER touch) · fourier's `docs/constellation/ADOPTION-ASKS.md`, `docs/tranches/N/**`, any fourier INBOX/CLAUDE.md (**their sub-session, COMMISSION §2**) · `latex-paper/**`, `glass-ui/**`, `keyframes.js/**` (producer trees reached **only by relay letter**) · `docs/tranches/X/{waves,keyframes,parse-that}/**` · `~/.codex/**`.

---

## §2 Carry

**Seventy-seven banked rows, agglomerated by mechanism.** A row's presence here is a **disposition, not a work order** — most execute elsewhere, and this wave's claim on them is the terminal record. Nothing is re-booked; every banked id is cited under its own name.

### §2.1 RE-CUT-0 — this wave's own precondition (recorded, not booked; no banked id exists)

**BLOCKER (spec-integrity; blocks BOTH F.W9 and F.W10 from stamping).** Re-measured 2026-08-28 in `registry/adjudicated/`: `grep -o 'F\.W9/W10' fr-*.md | wc -l` → **54**; `grep -l … | wc -l` → **25 records**; bare `F.W10` routing → **0**; `ls fr-*.md | wc -l` → **66**. Census §4.10/§4.11 makes F.W9 deploy-spine/evidence and F.W10 coordination/close — **two disjoint shapes** — so *no adjudicated row is provably homed here today*. Anatomy of the 54, measured so the cut cannot manufacture rows from boilerplate (the standing dissent's explicit warning):

| class | count | disposition |
|---|---|---|
| **routing identities** (a defect row's own disposition) | **26** | re-cut, one wave each — table §2.2 |
| **routing-law boilerplate** (the header line listing every wave id) | **14** | re-worded in the census addendum's taxonomy statement; **zero rows minted** |
| same-identity restatements (`fr-App:142` · `fr-PaperArticleWindow:186`) | **2** | fold to their row; never counted twice |
| verdict / adjudication prose | **12** | dated bytes, **IMMUTABLE**; annotated by addendum only |

**Seven records yield ZERO re-cut rows**: VisualizationView, CoefficientsPanel, FrequencyGraph, HarmonicLevelGrid, InfoCard, CanvasOverlayButton, and fr-AdminUserList (its single hit is the header line; FR-AUL-16 at :54 routes NO-WAVE-OWNER). CanvasOverlayButton and InfoCard still enter — **by MECHANISM** (FR-COB-20, F8-REACH-01/02), never by the digraph.

**Cure**: all 26 identities cut to exactly one of F.W9 / F.W10 / a named other wave; **both specs carry the same table; set-difference ∅ in both directions** (COHESION §3.1, the X·V 117-row idiom). The registry is not touched. Neither spec stamps until **G-F10-6 ≡ G-F9-21** closes.

### §2.2 The re-cut table (26 identities; identical in F-W9.md) + P-12 locks

*Heading mirrored (`identical in F-W9.md` ↔ `identical in F-W10.md`). **SCOPE OF THE SPLICE, stated exactly** (R2-1-LAW.2 — a "verbatim" label may point only at bytes a command produced): the **26-identity table** and the **by-mechanism entrant block** below are the SHARED BYTES, authored here and carried verbatim into F-W9 §2.2; the **R-2b checkpoint block** beneath them is **AUTHORED BY F.W9 and RECEIVED HERE BY SPLICE** — R2-6a's ruled direction, performed at repair round 2 (⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty**, this seat). **This head sentence is NOT spliced and never was** — it is seat-local, exactly as the twin's own head declares (its §2.8 E-7); the round-1 claim that the *preamble* was spliced over-reached by this line and is minuted at §2.8 **E-10**. **ONE CELL OF THE SHARED TABLE MOVED AT ROUND 3, and the paired law was executed rather than invoked (R3-3.7)**: the `fr-PaperView:120` **D/i-1** cell carried a minted quotation — *"never scheduled independently"* inside quote marks, a clause ⟨cmd⟩ `grep -rc 'never scheduled independently' fr-*.md` returns **0** for across all 66. **This file AUTHORED the re-scoped bytes** (the bank's words inside the marks, this spec's coupling clause outside them) **and F-W9 RECEIVED them by splice**, authoring no rival cell — the direction R2-6a ruled for the checkpoint block, running here in the other direction because the table is this file's to author. Receipt, after the receive: ⟨cmd⟩ `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty** (this seat). The twin's record is its §2.8 **E-10(b)**; this file's is §2.8 **E-20**. **Any change to the shared table, the entrant block, or the checkpoint block is a PAIRED edit landing in both specs in one commit**: a unilateral fix breaks G-F9-21 / G-F10-6's Δ=∅ — **which is exactly why E-5's AA-44 `9`→`8`, ruled for no round, stays a disclosed PENDING pair while this one lands.** Count and deixis disclosures live outside the shared bytes, at §2.8 (E-3 / E-5 / E-20).*

Execution seat = **F.W9-widened (or a minted F.W9a)** per the P-12 packet ruling; the F.W10 share of every F.W9-cut row is the **terminal-disposition record only**. Three cuts deviate from the banked digraph **by mechanism** (flagged ⟨re-cut⟩; recorded in the census addendum). Other-wave legs already homed (F.W0/W1/W3/W4) are cited, never re-cut.

| record | identity (kept for life) | cut | mechanism + locks / preserved dissent |
|---|---|---|---|
| fr-AdminAuditLog:87 | **AA-44** | **F.W9** | packet root: zero frontend tests, 9 e2e never enter the audit tab, backend pins its half. Instrument = GAB-10's `grep -rln "admin" web/e2e/` → **0 files**. Cure: extend ONE existing axe keystone (visualization-ux:26-43 + visualization-crud AxeBuilder) into the admin tab; live-region half stays F.W4. **Dissent (FR-AFP-49, preserved): the family carries blocker weight — "every row above incl. all four blockers is invisible to every gate the repo runs"; INFO is a scope convention (L-9.1), never "the debt is small"** |
| fr-AdminFlaggedPanel:95 / :110 | **FR-AFP-42** (test seat) · **FR-AFP-49** | **F.W9** | 0 props / 0 emits / setup-top-level fetch in `defineAsyncComponent` — untestable seam. Seam → F.W3/W4 (homed); only the seat re-cuts. Distinct mechanism from AA-44 (testability ≠ coverage) — never folded; seam-before-seat. **Quote 8 specs, never 9** (K11) |
| fr-App:91 | **C-13 (App)** | **F.W9** | four untyped ambient global side effects, no vitest floor. The four-wave chain made explicit: **MG-θ → F.W0** (tsconfig/lint one-liners) · floor **decision → F.W4** (census §4.5) · **execution → F.W9** · **terminal record → F.W10**. Distinct id from FunctionInput's C-13 — never merged |
| fr-ContourPreview:71 | **38** | **F.W9** | zero coverage; `vue-tsc -b` is precisely the gate row-11 is invisible to. **No repair inherited — both flagship cures REFUTED** (the `<Card tier surface>` migration = 21-site typecheck break; the restore-path blank render cannot occur). The simplify-heap unit target anchors F.W4's harness decision — the cheapest true vitest justification in the packet, not PAW-50's trapped `resolveFigure` |
| fr-EquationModeToggle:41 | **FR-EMT-11** (= C-16) | **F.W9** | no unit runner; `/equation` one assertion-free screenshot slug; axe installed, never pointed here. Rider verbatim: "the gates for FR-EMT-1/-5's cures ride their own waves." **Dissent PRESERVED verbatim: reader-2's MAJOR ("every other row in this record is ungated because of it") ruled a scope error under L-9.1 — "so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small."** The close honours this warning |
| fr-EquationResult:41 / :72 | **FR-EQR-6** (axe leg) · **FR-EQR-31** | **F.W9** | `.eq-scroll-region` scroll container: no tabindex/role/name; KaTeX spans give no focusable descendant. F.W4 = attributes (homed); F.W9 = the single `/equation` seat. Cohort: fr-CoefficientsSpectrum M-6 same class. FR-EQR-31: zero behavioural coverage by construction; D-20's archaeology half rides FR-EQR-27's correction — **not re-booked** |
| fr-EquationView:62 / :191 | **D·D-B2** (axe leg) · **C·D-28** | **F.W9** | zero functional e2e on `/equation` (8 specs / 1 screenshot hit); info button + Wand2 = two nameless icon-only controls; aria-label ×2 → F.W4 (homed). **The packet's single strongest F.W9-seat argument: the ONE interaction spec (compute → notation → budget → reload) mechanically catches B-1, B-2, C-29, D-07, D-14 — five already-booked defects.** BLOCKER→MAJOR per the fr-CT precedent, both readers concur |
| fr-FunctionInput:80 | **C-13 = D-24** | **F.W9** | `/equation` screenshot-only; the nameless Wand2's survival is the corollary. FOLD to banked fr-CollapsibleSection m-8 class (`actions` slot ready). Naming half belongs to FR-TT-1 / EquationView D·D-B2 — never double-book |
| fr-GalleryAdminBanner:49 | **GAB-10** (coverage half) | **F.W9** | GAB-1's `color-contrast` serious class unverified by construction. **FOLDS BY REFERENCE to AA-44**; live-region half → F.W4 riding GAB-4's `aria-busy` (homed). GAB-13 (F.W0) gates it: gates written against the dirty worktree measure a tree no commit holds |
| fr-GalleryCardModal:93 | **GCM-42** (= L-14) | **F.W9** | zero tests every level; console guard filters error-only + 404 — blind to GCM-4's warns three ways. **FOLD → AA-44 / FR-AUL-16 class, never re-booked.** Own increment surviving the fold: widen the guard to `warn`. **Interaction lock: no filter widening before F.W1 lands** — the error-only filter is the blind spot F.W1's four zero-console-error gates depend on (FR-EQR-3). Dissent carried: DU's BLOCKER on GCM-4; the SR composite (GCM-4∘16∘17∘6/8) named blocker-weight with no BLOCKER row carrying it |
| fr-GallerySearchBar:54 | **FR-GSB-17** | **F.W9** | its named e2e is vacuous by construction — every assertion inside `if (await filterToggle.isVisible())` (gallery.spec.ts:57-63); delete the bar → GREEN. Un-vacuate, don't add: **assert the precondition EXISTS**; kill the unreachable `.glass-dock` branch (:49-53; `grep -rn glass-dock src/` → 0) and the `.or()` mask (:19-22). One cure shape authored ONCE for the class (with FR-USB-16 + FR-GV-36's census) |
| fr-GalleryView:78 | **FR-GV-36** (= L-24) | **F.W9** | every defect structurally invisible (no vitest/ESLint/noUnusedLocals/vueCompilerOptions); console e2e passes against a dead API. Folds → FR-GFC-27 + AA-44 route + FR-AUL-16 input. Surviving increment: **L's three-conditionally-vacuous-tests census**. The dead-API fact belongs to the F.W0/F.W9 deploy-spine reckoning — edge declared, API repair NOT booked here |
| fr-ImageUpload:61 | **27** | **F.W9** | all **15** e2e entries across **6** spec files use `setInputFiles` — drag/reject/oversize/failure/keyboard uncovered. **Quote the 15/6 pair, never a re-derived count** (ruling 7's denominator). The drag path needs FR-USB-16's coarse project — one matrix change unblocks both |
| fr-PaperArticleWindow:58 / :90 | **PAW-12** (rider limb) · **PAW-34** (= C-i-5) | **F.W9** | PAW-34: zero e2e on any owned surface ("figure" hits = `test.describe.configure` substrings). PAW-12: two facts on one hand-typed literal (`TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))`) guarding a no-fallback `<picture>`; predicate split → F.W3/W4 (homed); F.W9 = the build-time set-equality assertion (vitest or Vite build plugin per PAW-50). Invariant **HOLDS at HEAD** (28 png / 26 avif / 26 webp; unpaired `fourier.png`+`maintainer-avatar.png`; 26 keys = 26 `\includegraphics`) — the rider guards drift. **PAW-50: harness-before-rider. PAW-49: flag-before-split.** Dissent preserved: LC's MINOR sustained at R-2 |
| fr-PaperArticleWindow:199 | **PAW-49** | **F.W0** ⟨re-cut⟩ | without `noUncheckedIndexedAccess`, `resolveFigure`'s return asserts `width: number` while one whole arm yields `undefined` — vue-tsc green over a false type. The flag is a **repo-config** line, identical in kind to MG-θ/M7's tsconfig flags; **the one coverage-family row whose gate is FREE** — `npm run build` already runs `vue-tsc -b`, no new harness. F.W3/W4 rider on the PAW-12 cure homed |
| fr-PaperArticleWindow:200 | **PAW-50** | **F.W4** ⟨re-cut⟩ | web/ has **no** unit harness at all (five scripts, no vitest/jest, zero `*.test.ts`); `resolveFigure` trapped in the SFC. It *is* the harness-before-rider law — census §4.5's **decision** is its only true home; **F.W9 executes** (move `resolveFigure` beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12); **F.W10 verifies**. Governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44 |
| fr-PaperSidebar:49 | **D-B2** (axe leg) | **F.W9** | "Contents" heading 2.39:1 light / 3.00:1 dark vs the 4.5 floor; no axe visits `/paper`. Token → F.W4 (homed). The `/paper` seat is **ONE artifact** with M7 + PAW-34 and **runs AFTER PAW-1's cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. MAJOR, blocker-weight in the a11y aggregate |
| fr-PaperSidebar:58 | **C-M4** | **F.W9** | the expand-if-closed guard reads the **Tooltip's** `data-state` (domain `closed\|delayed-open\|instant-open`, never `"open"`) — a no-op. F.W4 half dies with the `CollapsibleTrigger` restructure (homed); F.W9 re-keys the guard on the Collapsible root. Corrected cite :124–129. Third vacuous-gate instance but a **DISTINCT mechanism** (wrong element's state) — never folded; the cure differs |
| fr-PaperSidebar:67 | **M7** | **F.W9** | dead surface unreachable by any gate; two of four needed gates are one config line each. tsconfig half → **F.W0 = the SAME identity as MG-θ — one substrate edit, two records, booked once**; F.W9 = axe on `/paper` + lint script |
| fr-PaperView:120 | **D/i-1** | **F.W9** | zero coverage of the search surface, no vitest. **Coupled — the bank's own words, and ONLY those** (⟨cmd⟩ `grep -n 'B-1/B-2 cure wave' fr-PaperView.md`, this seat, read-only in the frozen corpus): *"coverage rides the B-1/B-2 cure wave."* The **never-scheduled-independently** clause is **this spec's sequencing lock (§4a.11), outside the quotation marks** — ⟨cmd⟩ `grep -rc 'never scheduled independently' fr-*.md` → **0 in all 66**. **PAIRED EDIT (R3-3.7): re-scoped in F-W9 and F-W10 in ONE commit; a unilateral landing breaks G-F10-6 ≡ G-F9-21's Δ=∅** |
| fr-Tooltip:26 | **FR-TT-1** (promotion note) | **F.W4** ⟨**CITED — the canonical's holder; NOT booked by either twin**⟩ | the shim is the ONLY label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring `aria-describedby` only-while-open — a DESCRIPTION, never a NAME. It governs **this wave's close-gate design** (G-F10-10). Naming legs → F.W4 (all 17); shim leg → F.W3 keeping its R3-7a/X-11 identity; folds honoured, never re-booked (EditorControlsDock C-3 · CanvasControlsDock D-1/L-3/C-1 · EquationView D·D-B2). Producer corroboration glass-ui 8.0.0:69-73: "an icon-only trigger labels ITSELF." L-4's 17-count census citable; D-1's 16-count + :42/:49 anchors KILLED (register #8). ⌧ **THE PENDING PAIRED EDIT, DISCHARGED 2026-08-29 (PASS-6 D-9).** Rounds 4–5 cut this cell to **F.W10** ⟨re-cut⟩ while `CENSUS-CANONICAL.md` §2 homes `fr-Tooltip FR-TT-1` at **F.W4** — a booking at a wave the canonical does not home it, which is a fabrication on R4-10's own terms and was the last one standing in this programme. Round 5 disclosed it against its own interest and did **not** apply it, because a unilateral edit inside the shared splice bytes breaks G-F9-21 ≡ G-F10-6's Δ=∅. **It is applied now, in both specs, in one pass**: the cut becomes **cite-the-holder**, the identity stays F.W4's single row, and neither twin books it. The naming-legs clause above already routed the work to F.W4, so nothing about the finding, the fold or the close-gate design moves — only the cut. **AA-44's `9`→`8` correction remains the one OUTSTANDING paired edit** |
| fr-UserSlugBar:53 | **FR-USB-16** (≡ r1-MISSED-5 ≡ r2-MISSED-1) | **F.W9** | self-disabling test: `gallery.spec.ts:66-82` body wrapped in `isVisible().catch(() => false)`; matrix is one Desktop-Chrome project. Unguard (assert the trigger EXISTS) **+ add the mobile/coarse Playwright project** — a matrix change that must precede every coarse-pointer witness (glass a11y-overrides floor; PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` arm; ImageUpload 27's drag path). **Dissent preserved: r2 graded MINOR; overruled at the ruling table** |

**By-mechanism entrants** (records carry NO digraph — they enter by mechanism, not by re-cut, and must not be counted into the 54):

- **FR-AUL-16** (fr-AdminUserList:54, MAJOR) — the only cascade-delete surface, zero automated coverage of any kind; "un-harnessable" defence KILLED (ruling 3e). NO-WAVE-OWNER: harness decision → F.W4 · devDependency + admin-tab axe limb → F.W9 · **terminal home decided AT F.W10** (§3.3 deadline — last F wave). Cure verbatim: "extend one axe keystone into the admin tab + a vitest devDependency"; slug-bearing aria-labels = ready selector vocabulary.
- **FV-26** (fr-FullscreenViewer:79 = D-23·C-m7) — the only e2e "Fullscreen" hit is a comment (gallery.spec.ts:113). Coverage share → F.W9. **Teleport lock: assert against the teleported root** (FV-27: one of two Teleport sites). FV-30 rides banked BasisCanvas D-4/BC-2/C-2a + AnimationControls C-12, no new booking; FV-31 → F.W4.
- **FR-COB-20** (fr-CanvasOverlayButton:74 = C-7) — interaction contract entirely implicit; no test anywhere. "Dies with the file"; the no-test-for-wrappers observation → SS-3/SS-4 input; the tree favours DELETE. Gated on **F8-REACH-02** at F.W0 — **HOLD is RED; F.W10 verifies no HOLD survives** (that verification is this wave's whole claim, not a re-booking).
- **FR-AH-45** (fr-AppHeader:108 = C-16, NO-WAVE-OWNER spec-input) — zero props/emits/slots, no contract. A question owed an answer ("what contract should a shell header state?") → SS-3/SS-4 spec-input; terminal home or kill-with-rationale at F.W10, never left open. **Discharges together with FR-AH-30's cure-caution: the D-axis rank-5 remedy REVERSES the outcome — only `@layer glass-overrides` appended after `utilities` works** (glass-ui's competing rules already sit in `@layer components` at higher specificity).
- **PAW-33** (fr-PaperArticleWindow:89 = C-i-2) — rides the C-19 ⊕ PAW-33 pin-truth row (§2.6).

**THE F.W1 VISUAL-REGRESSION CHECKPOINT SET — 8 items, HOMED HERE** (**R-2b**, `conformance/PASS-1/RULINGS.md`; direction re-affirmed at **R2-6a**). Source: `carry/F-W1-CARRY.md`, **§CrossEdges item 9** — the `F.W1 → F.W9 / F.W10` row — **cited by path and by row label**, one of the two real carries (R-3; anchor idiom R2-2). **F.W9 is the home, AUTHORS these bytes and owns execution; F.W10 owns terminal disposition and receives them by splice, never retyped**; until that paired edit lands, **G-F9-21 reddens on the asymmetry** and §2.8 **E-2** is its standing disclosure. Carried verbatim from the carry — ⟨cmd⟩ `grep -A0 -n 'F.W1 → F.W9 / F.W10' carry/F-W1-CARRY.md` (read-only, this seat, 2026-08-28): *"The visual-regression checkpoint set the uplift mints with no typecheck signal: `--shadow-cartoon` sign flip · `cartoon-surface` hover loss across 21 sites · the `.disclosure-content` body register · the tooltip re-proportion · GCM-22's `p-0` insets · the Badge rim · FR-CP-13's fused-card gap · the `text-admin-label` reversion. Plus the SS-13 residues each record names."*

**F.W9 owns the checkpoint, never the cure** — every item below is minted by **F.W1's** transaction (cited whole at §4a-3) or cured at F.W3/W4; this wave books only the instrument that makes the drift falsifiable.

| # | checkpoint item (banked wording kept) | why no gate sees it today | F.W9's instrument (gate only) |
|---|---|---|---|
| 1 | **`--shadow-cartoon` sign flip** | a token sign change paints; it never types — `vue-tsc -b` is structurally blind. ⟨cmd⟩ re-measured read-only 2026-08-28 at fourier HEAD `cd26c65`: `grep -rl -- '--shadow-cartoon' web/src/` → **1 file** (`components/visualization/gallery/GalleryCard.vue`), `grep -rn -- '--shadow-cartoon' web/src/` → **3 hits** (`:193` · `:212` the `-hover` twin · `:224`). **The round-1 cell's "2 files" did not reproduce and is STRUCK** (§2.8 **E-5**) — a stated re-measurement that does not re-run is the false-witness class this wave exists to abolish | a checkpoint slug in the post-transaction capture **with a comparison assertion** (G-F9-23) |
| 2 | **`cartoon-surface` hover loss across 21 sites** (the carry's banked wording, kept) | hover state is reached by no spec in the suite. **The 21/14 denominator belongs to F.W0 G-12 and its pattern token is `cartoon-card`, NOT `cartoon-surface`** — ⟨cmd⟩ `grep -o 'shim \*\*21 cartoon-card sites / 14 files\*\*' waves/F-W0.md` → *"shim **21 cartoon-card sites / 14 files**"* (**F-W0 §4 G-12**, the RED-witness shim row, `[fr-AdminAuditLog K-13, re-confirmed FR-EQC-3]`) — **quoted, never re-derived here** (per-site enumeration is G-12's act). Column 2 keeps the carry's own words; **it is not a denominator claim**, and this spec no longer asserts G-12 published a `cartoon-surface` 21/14 (§2.8 **E-6**). At the tree the two tokens are not interchangeable: ⟨cmd⟩ `grep -rl 'cartoon-surface' web/src/` → **1 file** (`style.css`, applied once at `:108` **inside** the `.cartoon-card` shim) vs `grep -rl 'cartoon-card' web/src/ \| wc -l` → **15 files** | the same checkpoint taken in a hover state on one representative site; the site roster is read **from G-12's table**, under G-12's own token |
| 3 | **the `.disclosure-content` body register** | `grep -rl 'disclosure-content' web/src/` → **0** — the register is **producer-side**, so the consumer sees only a render change. **Cure rides SS-6; a producer row never becomes a frontend hack to turn this green** | consumer-visible render checkpoint only; the register itself is relayed, not edited |
| 4 | **the tooltip re-proportion** | FR-TT-1's shim is the only label on **17** icon-only triggers; a re-proportion moves hit area with no assertion anywhere. G-F9-22 owns the **naming** half — this is the **geometry** half, distinct, never folded | checkpoint on one Editor and one Canvas trigger, taken **after** the coarse project lands (§4a-8) |
| 5 | **GCM-22's `p-0` insets** | GCM-22's `p-0` retirement is a **limb inside F.W1's atomic transaction** (**F-W1 §4, intra-wave order step 4** — the atomic transaction, G6; extent per §2.8 **E-4**); worse, **no spec opens the modal at all** (G-F9-9 RED), so the inset is invisible even to a screenshot | **G-F9-9 is its precondition** — the card/modal must be opened before the checkpoint can be taken |
| 6 | **the Badge rim** | a sub-pixel rim change on a primitive no spec asserts | one badge-bearing surface in the checkpoint set |
| 7 | **FR-CP-13's fused-card gap** | the **gap DECISION is a limb of F.W1's transaction** (**F-W1 §4, intra-wave order step 4**) — F.W9 books the checkpoint that makes the decision falsifiable, and **books no decision** | checkpoint pair (pre/post transaction) on the fused-card surface |
| 8 | **the `text-admin-label` reversion** | invisible **twice over**: `grep -rln "admin" web/e2e/` → **0** (AA-44's packet root), and the label is a paint-only token. **7 sites / 4 files is F.W0 G-12's published denominator** — ⟨cmd⟩ `grep -o '`text-admin-label` \*\*7 / 4 files\*\*' waves/F-W0.md` → *"`text-admin-label` **7 / 4 files**"* (**F-W0 §4 G-12**, the RED-witness denominator row); read-only corroboration 2026-08-28 → `grep -rl text-admin-label web/src/` = **4 files** — corroboration of a pinned figure, **never a re-derivation of the oracle** (KF.W4(d)) | rides **S1**'s admin-tab entry (§2.3): once a spec enters the tab, the checkpoint can be taken there |

**"Plus the SS-13 residues each record names"** — carried as a **named class, not absorbed**: each residue stays with the record that names it and rides that record's own wave (F.W3/W4 cures; SS-13 is not F.W9's to book). F.W9's only claim over them is the same instrument: a checkpoint that would catch their visual regression. **No id is re-booked and no residue is renamed** (anti-rename).

**Born-RED witness for the whole set** (re-measured read-only 2026-08-28 against fourier HEAD `cd26c65`, no product source opened): `grep -c toHaveScreenshot web/e2e/*.spec.ts` → **0 in all 8 specs**; **no `*-snapshots` directory exists tree-wide**; `visual-baseline.spec.ts:48` (the **π capture** test, one per route × 3 viewports) writes bare full-page PNGs via `page.screenshot()` at **`:55-59`** (⟨cmd⟩ `sed -n '46,62p' web/e2e/visual-baseline.spec.ts` — the call opens `:55` `await page.screenshot({`, its three option keys run `:56-58`, `:59` closes `});`; the round-1 `:56-60` was off by one and is corrected here under C-4's G-11 quotation status, §2.8 **E-5**) into `docs/tranches/J/audit/screenshots/{before,after}` — **a capture harness, not a regression gate**. All eight items are therefore minted with **no typecheck signal AND no screenshot-diff signal**. Gate: **G-F9-23**.

### §2.3 Substrate + terminal-disposition verification (F.W0 RULES; F.W10 VERIFIES, never re-rules)

| row | what F.W10 checks — and the lock it must not break |
|---|---|
| **FR-NP-32 (≡ fr-PaperSidebar M1)** (fr-NotationPills:35 · fr-PaperSidebar:29/:9/:124, **BLOCKER**) — **two banked witnesses of ONE fact; the citation form is cite-both, never substitute** (banked ids are original for life). Installed glass-ui 4.0.0 ships a syntactically corrupt `dist/styles/index.css`: `postcss.parse` → **35 top-level nodes, ZERO valid `@source`**, the garbage at-rule `` source` `` at :203–222; FR-NP-32's own repro with the app's toolchain — `@import "@mkbabb/glass-ui/styles"` under `@tailwindcss/postcss` → `CssSyntaxError: Unterminated string: 's own'` (delimiters 17/8 dist vs 15/6 src) ⇒ **`vite dev` and `vite build` are RED at the `^4.0.0` pin** | F.W0 pre-gate + **glass BH relay**, disposition VERBATIM from `fr-NotationPills:35` — **the label carries its provenance (R2-1-LAW.2): a "verbatim" claim with no ⟨cmd⟩ beside it is a defect per se, whatever the words say** — ⟨cmd⟩ `grep -n 'producer dist emitter' fr-NotationPills.md` (this seat, read-only in `registry/adjudicated/`; line anchors are lawful here and only here — the frozen corpus, R2-2 clause 2(i)): *"**BLOCKER — NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE**"* (a dist-GENERATION bug; producer source at HEAD intact). **RECEIPT — READ, NOT ASSERTED (2026-08-28): the relay LEFT.** `glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md` row **A-1** carries the arm and names it *"FR-NP-32 ≡ fr-PaperSidebar M1"* (`grep -c '@source'` → **1** · `grep -c 'FR-NP-32'` → **1**), dispatched per **COHESION §4a, the `DISPATCHED 2026-08-28` O-20 row** (stable anchor, R2-2 — COHESION is a live sibling and the round-1 `:129` address into it is retired at all four of this file's sites) — so G-F10-12's `@source`/FR-NP-32 arm is **GREEN-BY-RECEIPT**, not RED (§2.8 E-2). The paired **M3 F.W1 pre-gate discriminator** goes into F.W1's spec: `grep -c '<class>' components.css glass-ui.css` — 0/0 ∧ absent from `web/src` ⇒ NOT PAINTING; F.W1 pre-gate prediction: build succeeds, `grep -c animate-collapsible` → 0; a naive `@source` widening flips three findings at once. **Identity guard: M1 DECIDES banked fr-CollapsibleSection M-7 (emission hinge, NEGATIVE) and CORRECTS banked K-5 (shipped-but-INERT — both prior framings wrong); the correction books here, the defect stays banked at its original id** |
| **GAB-13** (fr-GalleryAdminBanner:52, MAJOR, ruled UP from INFO) — the audited artifact is the **DIRTY WORKTREE**: 28 modified rows incl. the subject and `web/package.json` (HEAD `^3.1.0`/`^2.2.0`/`^0.10.0`/lucide `latest` vs WT `^4.0.0`/`^4.3.0`/`^0.13.0`/`^1.0.0`) | F.W0: **LAND the WT diff OR record it explicitly as the audit baseline** — either branch is TERMINAL; a disclosure line is a disposition, not a dodge. Class part-banked (FR-COB-10 + fr-BasisCanvas's disclosure idiom) plus the K-9 certification kill (verify-hash-then-read-worktree fails open). Ties G33's uncommitted-bump dissent. **Dissent PRESERVED VERBATIM (worker-DU): "the fleet has already elected to treat the working tree as the scope" — if F.W0 adopts the worktree as baseline, GAB-13 discharges to a disclosure line and DU's INFO was right all along** |
| **F8-REACH-01 / F8-REACH-02** (`FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md:30-31`) — InfoCard.vue (N.C11, `b2c2e718…`, 1,516 B) and CanvasOverlayButton.vue (N.C39, `35f3db91…`, 595 B): unmounted, un-dispositioned | F.W0 rules the **exact verb** — `KEEP_WITH_MOUNT` \| `ISOLATED_HARNESS` \| `DELETE`. **HOLD IS RED by the source's own law; F.W10 VERIFIES NO HOLD SURVIVES TO CLOSE** (G-F10-11). FR-COB-20 gated on -02; fr-InfoCard's dirty-edit class (FR-COB-10) rides -01 |
| **CODEX-R3–R6 residue** (INTAKE §3 F.W0 block + §4 · CENSUS §6.7 + the :310-316 ADDENDUM · lane-docs §4c) — **9** `FOURIER-R*` coordinate files on the value side (§2.8 E-6), **none in fourier's tree**; R3–R6 frozen TERMINAL/SOURCE_RED; the 72/158/P29/slot superstructure Codex-internal, non-transferable | **The charter line lands in the CORRECTED wording** — "**Zero UNVERIFIED credit** — every Codex verdict enters the formation through per-claim adjudication (M-21)" (verified verbatim at `CENSUS-2026-08-03.md:310-316`) — never the superseded "carry zero credit", which would re-introduce the discard posture M-21 forbids. Terminal verbs: **R3-9** record only, never a method guarantee · **R3-16 + R4-11 + R5-9 + R6-10** — the 14-wave M board is the real disposition object · **R4-12** — consume-nothing stands and is **MOOT** (`/tmp/fourier-r4-files.sha256` re-confirmed GONE 2026-08-28; the banked "74,507 B still on disk" is stale) · **R5-3** re-hash or drop · the Codex worktree `~/.codex/worktrees/d0be/fourier-analysis` (re-confirmed **still registered** 2026-08-28) dispositioned. **OWNER RULINGS OWED, FLAGGED INLINE, NEVER PRESUMED: OG-F1** (re-root fourier source auditing at F.W0, or freeze the R-coordinates and build from live-tree measurement; evidence favours FREEZE-WITH-ADOPTION; R6 self-declared `NO_SUCCESSOR`, the ruling is cheap) · **OG-F2** (does R6-3's pre-write root-absence-receipt bind successors, or was it Codex-era? the absence itself is TRUE and adopted). **Dissent carried (OG-V2): the Codex chain violated its own prohibitions (XF-7, KF X-7) — Codex-authored `NO_RETRY`/`NO_SUCCESSOR`/"forbidden" verbs may not bind the Claude-owned formation without an owner ruling** |
| **L-4 = C-B2 = D-N10 (+M6)** (fr-PaperSidebar:33/:66, MAJOR) — ONE 98-node tree derived three times across two producers; both export `useSidebarFollow`; the arbitration point is an untyped `defineExpose` seam | **F.W1 OWNER-RULING ESCALATION** (which producer owns the ToC model); **F.W4 executes the collapse** (delete `paperTree.ts`'s adapter + the losing index). **F.W10 CANNOT STAMP TERMINAL DISPOSITION WHILE THE ESCALATION IS UNANSWERED** — close-gate input, flagged inline; F.W10 verifies the ruling landed, it does not rule |
| **SS-4-PREREQ (TA-4 atomdiff)** (CENSUS §5 [P0] #3 · lane-crud §0/§2 R-3 · COMMISSION §2, BLOCKER, contract-binding) — `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` EXCISED at T.W1/TA-4; `J-diff-shape.md §6` probes cannot run value-side; **a co-signature over un-runnable probes is VOID** | **F.W10 VERIFIES THE DECLARATION EXISTS IN THE F.W5 SPEC**: the value-side restoration named a PREREQUISITE, **or** the contract EXPLICITLY RE-SCOPED. The work belongs to the value.js API row, outside fourier's waves. SS-4 owner rulings **FLAGGED INLINE, never presumed**: trie-vs-KISS · remix-vs-fork · born-visibility · visibility-enum · compound-vs-global version `_id` · redaction parity · cursor-vs-offset pagination. **Dissent RECORDED: the commission's trie-compression collides head-on with the standing anti-tree KISS guardrail in BOTH trees (`atomdiff.py:12-14`) — THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design** |

### §2.4 Coordination discharge — the rows this wave itself executes

| row | verb + mechanism |
|---|---|
| **ASK-3** (P1 — *"the hardest of the four and the true critical-path item"*; live table :111) | palette-api still deploys by rsync to a deploy-dir, blocking `deploy-hook.sh` adoption — the fourth migration gating `dispatch.sh` deletion. **Book the disposition verb (ADOPT / DECLINE-with-rationale / DONE) into `INBOX.md` AND relay to fourier's ledger.** Adopt-shape: rsync → git checkout under canonical root. F.W10 books the verb; **it never executes infrastructure** — F.W9 owns the deploy spine, F.W10 the correspondence. *Standing law carried as dissent: peer-gated items have ≈ZERO discharge rate corpus-wide (dispatch.sh 6 tranches; inv-28 PAT 3; these asks through 4 re-triggers) — a stale-watch that only re-arms is a calendar, not a gate; no wave acceptance may name a peer actor's action* |
| **ASK-5** (P3; live table :113; targets value.js AND keyframes) | acceptance criterion VERBATIM and machine-checkable: **`color.babb.dev` serves from CF Pages with a `cf-ray` header, peaceiris step removed** — the only mechanical ask, cheapest to close **or decline honestly**. The KEYFRAMES half is KF's mail seat — **DECLARED, not absorbed** (§4b ∦ edge) |
| **INV-22-COLOR** (P3, their G.W8; live table :116) | the `api.color` vhost fails the 4-endpoint contract (`/` 200; `/health`, `/docs`, `/openapi.json` 404). Book the verb + relay; repair is maintainer-owned infrastructure **OUTSIDE F bounds**; edge to F.W9's deploy spine declared (silent-rollback chronic) |
| **CASCADE-VJS** (P2, their H.W6; fix at `H/waves/W6-W7-epsilon-booking.md §2A`; live table :118) | **SPLIT THE ASK**: unplugin-vue-markdown `^29.2.0→^32.0.0` + lockfile regen = ADOPTABLE; the `file:`→published limb = **DECLINED — conflicts with R's Q4 ratification (keep `file:` deliberately, `R.md §3.4`)**. Verbatim: "respond, don't silently ignore." **THE REPLY EXISTS** (`R/letters/CASCADE-VJS-RESPONSE.md`, tracked at COORDINATION-ANALYSIS E13), untracked in fourier's tree — **reconciliation is LANDING IT IN THEIR LEDGER, never re-authoring, never a fresh letter**. **Dissent PRESERVED — the conflict IS the dissent: both positions ratified in their own trees; decline one limb with rationale; never resolve by silence.** Loop closes in BOTH ledgers (the X·V W4-D2 lesson, now law) |
| **FN-1..FN-7** (fourier `N/VALUEJS-R-UPLIFT-ASKS.md` `83d4c9f`; all seven unanswered since 2026-07-03) | answer all seven with a disposition verb. **STANDING RIDER, VERBATIM AND LOAD-BEARING: "FN-5 must be authored BEFORE or WITH fourier M.W10"** — M.W10 is exactly the version-shape transpose FN-5 guards; M.W10 is `planned`, FN-5 does not exist — **the one non-answer with a dated consequence**. FN-5/FN-6/FN-7 are **SS-4 CONTRACT INPUTS**, not coordination trivia; FN-7's two fourier-tree writes happen in fourier's own sub-session (COMMISSION §2) — value.js declares the ask, never makes the edit |
| **VALUEJS-2.0.0-NOTE** (`cd26c65`; the note itself two majors stale) | **RECONCILE FORWARD: the reply states 4.0.0, never the stale 2.0.0** (`web/package.json:18` still `^0.13.0`, re-measured 2026-08-28). The bump is F.W1's `value 0.13→4.0` limb — dischargeable-on-adopt. Paired unstruck row carried: §3's M.W7 `sampleColorRamp` booking still not struck in `M/PROGRESS.md:22`. **Warning carried: value.js said "strike it, it ships" (2026-07-03) then retired the symbol at `164343c1` (2026-07-17) with no correcting letter — inv-32 book-rot running backwards; the reply must not repeat the shape** |
| **O-14 + INBOUND-PARITY** (`V/coordination/INBOX.md:76` · CENSUS §5 [P2] #9) | the correspondence is one-way in fact AND in law: fourier has **no INBOX ledger** (re-measured: `find docs -name INBOX.md` → 0), no CLAUDE.md, replied to none of the three value→fourier files; the facility-19 table sits UNTRACKED (`??`); our INBOX carries zero inbound fourier rows. Two limbs: **(a)** fourier-side INBOX + CLAUDE.md = census F.W0's act in THEIR sub-session — F.W10 relays the ask and **verifies receipt**, never writes their tree; **(b)** value-side: row the four asks + FN charter + 2.0.0 note with verbs so E13's "no wave closes with UNREAD mail" is **CHECKED, not asserted** (§2.8 E-8: the four asks are not unread — they are **UNROWED**, invisible to the E13 sweep). **ADMISSION GAP (census §6): the co-signed contract's signature needs a durable fourier surface — F.W10 CANNOT CLOSE BEFORE F.W0 STANDS IT UP** |
| **G24** (**X·W9's gate table, the G24 row** — by gate id, never by line; `X/waves/` is a live sibling, R2-2 clause 2) | **RELAY ONLY — the gate is X-W9's.** Banked: 8/22 curves drift, max\|Δ\| 0.192 vs `<1e-3` over 8 names via `fourier-value-import-drift.mjs`. The relay may NOT quote the banked number as current (**MEASURE-AT-OPEN**), may NOT claim the gate; no fourier-tree edit is credited as an X-W9 gate (W.L5's value half ships in X-W9.f under D-15's grant; "the fourier-tree edits and π evidence are explicitly NOT X-W9 gates") |
| **G33** (**X·W9's gate table, the G33 row**; corroborated by **X·W11's `fourier-analysis/web` pin row** — both by row label, never by line) | relay the **packet + INBOX legs**; F.W1's ATOMIC tri-package transaction turns the pin leg green. **IDENTITY DISCIPLINE ×6: ONE row — fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8, component-altitude INFO, NEVER re-booked per component** (C:B-1, C-2, C-4, C-27, F-1, PaperView C-03 are reproductions; C-03's increment is the live-today framing). Post-window `npm ls` leg is MEASURE-AT-OPEN by construction. **Dissent ADOPTED (fr-CollapsibleSection rider): the ERESOLVE is the UNCOMMITTED M.W1a bump — "red today" is true of the working tree ONLY; ties GAB-13** |
| **NEXT-COMMUNIQUE-CONTRADICTION** (INTAKE-ADJUDICATION `:221` vs COHESION §4a + the live ask sets) | AMEND BY **ADDENDUM-BESIDE** (E-3), never rewriting the dated intake (`git diff --stat` on it stays empty). Every surviving ask into **EXACTLY ONE register**: **glass → SS-6 BH relay** (**FR-NP-32 ≡ fr-PaperSidebar M1** — the corrupt dist, **BLOCKER, at the TOP of the FR-COB-28 packet** per its banked disposition, **receipted at O-20 row A-1** under `BK/coordination/` · the rest of the FR-COB-28 packet: FR-COB-7 · -8 · -23 · -26 (§2.5a) · HLG-32 and FR-MSP-12 folded to their producer classes · FR-NP-11 · D-26 · L-10/D-M4 ramp headroom + rebaseline, 4 failing light stops incl. 3.58:1 at stop 11 · GAB-2 ask (a) tier-tint home · GAB-9 ask (b) 3.26:1 stacked-badge label rung · AA-3's violet `batch` tone, no home in the 5-member TONES · FR-EQR light `--success` 2.175:1 · PaperView D/M-7 light `--primary` ≡ `--foreground` · fr-AppHeader cm-serif undeclared variable + light `--tier-featured` · FR-GIG-5's observer-root fix) · **latex-paper → the register §2.6 stands up**. **NO GLASS ROW IS EVER CURED CONSUMER-SIDE.** Dissent: none — both statements stand dated: TRUE of the Codex corpus, FALSE of the registry that followed |
| **RELAY-RECEIPT-LAW** (CONTRIVANCE-REGISTER:180 · EXHORTATION-CENSUS:125 · glass-forward-compliance §2, U-1..U-11; MAJOR, mechanism family — DISEASE-REGISTRY) | Root cause adopted as census law: **"no value.js gate goes RED when a relay does not leave."** Structural cure adopted verbatim: **(a)** an adjudication declaring a GLASS-OWNED/RELAY/LATEX-RELAY arm may not CLOSE until a packet file exists under the producer's inbox path **CONTAINING THE ARM'S IDENTIFYING TERM** (grep-able, L-2-conformant); **(b)** "relay-declared-never-sent" enters DISEASE-REGISTRY. ⟨**MEASURED CORRECTION, this seat, 2026-08-28 — §2.8 E-2: the U-1..U-11 debt is DISCHARGED.** `valuejs-outbound-2026-07-28-o17-full-debt-discharge.md` (76 lines, under `glass-ui/docs/tranches/BJ/coordination/`) is exactly the prescribed O-10a-style amendment and rows **B-1..B-10 naming all eleven arms** (B-1 = U-1+U-5 convergent). The banked "each ZERO across all six sent letters" is stale: **ELEVEN** packets now exist — BJ's ten ⊕ the live BK inbox's **O-20** (`glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md`, dispatched per **COHESION §4a's `DISPATCHED 2026-08-28` O-20 row**), whose row **A-1** carries **FR-NP-32 (≡ fr-PaperSidebar M1)** — so the F-side `@source` arm is **GREEN-BY-RECEIPT** too (E-2). **The law survives whole and is what proved the discharge — a receipt read, not asserted; and its second failure mode is now named: RELAY-SENT-NEVER-READ, cured by binding the sweep to the recipient's LIVE inbox path (§1b), never last tranche's**⟩. **This is F.W10's OWN close-gate law and binds the wave to itself: a relay whose receipt is ASSERTED rather than VERIFIED fails G-F10-12.** "Nothing in the debt blocks glass 8.0.0; everything in it rots if 8.0.0 ships first" |
| **MG-ι** (fr-App:82/:143, MINOR) | fourier-side remedy constraint → **F.W1** (with C-2): the sound path is `colorUnit2`; `color2` NEVER (`color2(parseCSSColor(x))` THROWS; the `.value` unwrap SILENTLY RETURNS WRONG CHANNELS); `light-dark()` still needs the used-value probe. **The value.js-side limb (the `color2` dispatch mis-reading its sibling parser) is NO-WAVE-OWNER in the F-taxonomy — F.W10 relays it into the V·π/parser-seam ledger WITH THE OWNER-MAIL FLAG**; no fourier wave adopts it. **ROUTE LAW: direct parse-that → fourier FORBIDDEN** — parser → value (X·V L1/L5) → packed release → consumers; this wave mints no such edge |
| **GAB-29** (fr-GalleryAdminBanner:74; INFO — **cure-falsifier, NOT a destination**) | MetricPill is barrel-only-reachable and has **NO source at v8.0.0** (dies with MetricBadge) — "the wave must not adopt it off the name either." Terminal record, **LOAD-BEARING for F.W1's budget**: the F.W1 seat remains K-4's ruling — `<Metric posture="cell">` from `./metric` at 8.0.0. Keeps the wave off TWO dead primitives |

### §2.5 The NO-WAVE-OWNER drain, F-side (G-F10-7; COHESION §3.3 deadline — F.W10 is the last F wave, nothing leaves it un-homed)

Verbs are exactly three: **NAMED WAVE** · **NEXT-BOUNDARY LEDGER ROW** · **KILL-WITH-RATIONALE**. *A closed question and a negative finding are terminal dispositions too — record them, manufacture no work.*

**▲ THE DRAIN IS THE ONE HOME FOR `AA-45` · `AA-46` · `AA-47` (repair round 3, ruling R3-6.1 — the double-home dissolved at the bytes, and the direction is stated at THIS end so no seat has to infer it).** The three were simultaneously held here and inside F-W5's clause bank. The bytes settle it: the banked terminal cells carry **`NO-WAVE-OWNER` with no `F.W5` token** — ⟨cmd⟩ this seat, read-only in the frozen corpus, `grep -c 'F\.W5' fr-AdminAuditLog.md` **stated as a classification, not banked as a count** (R3-3.10): *the record's `:88`/`:89`/`:90` terminal cells name NO-WAVE-OWNER and name no wave at all*, so the three are not members of F-W5's routed census, and the deadline authority built to hold exactly that class is this drain. **Direction, recorded here because R3-6.1 asks this end to say it**: F.W5's A1/A2/A3 clauses **keep their mechanism and cite these rows by id** — the model A1 already applies to `AA-48` (*"same latent class — recorded, NOT re-booked"*) — and the drain **never cites the clause bank as a home** in return. **`AA-46`'s contradiction resolves in the drain's favour, at the bytes**: ⟨cmd⟩ `grep -n 'Redundancy records' fr-AdminAuditLog.md` (this seat) → *"Redundancy records. **NO-WAVE-OWNER**."* — the **KILL-WITH-RATIONALE** below is the byte-faithful disposition and **STANDS**; F.W5's envelope decision loses nothing (it rests on FR-AFP-18 · D10), and AA-46 may be cited there as envelope *evidence with the kill noted*, never as an operand. **STANDING TIE-BREAKER adopted (R3-6.5)**: a banked NO-WAVE-OWNER row's ONE home is this drain; a clause needing its mechanism cites the drain row by id. `FR-AUL-46` **stays cured** and is re-booked by nobody (its one home is F-W5 D7). **No id below is re-booked, re-graded, or renamed by this note.**

| row (banked id, kept) | mechanism in one line | terminal verb |
|---|---|---|
| **FR-NP-32 (≡ fr-PaperSidebar M1)** (fr-NotationPills:35, **BLOCKER — the drain's head row**) | the ADOPTED `@mkbabb/glass-ui@4.0.0` dist stylesheet is a CSS syntax error (dist emitter injected the AN.W1 fold-block at the first *literal* `@source`, inside a comment); `vite dev`/`vite build` RED at the pin | **NAMED WAVE + BH RELAY REGISTER, disposition VERBATIM** — ⟨cmd⟩ `grep -n 'producer dist emitter' fr-NotationPills.md` (this seat; same receipt as §2.3, R2-1-LAW.2): *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"* — F.W0 pre-gate rules it (§2.3), F.W1 sequences behind it, the relay register is SS-6. **Receipt attached**: O-20 row A-1 under `glass-ui/docs/tranches/BK/coordination/` names the id and the arm (`@source` present, `FR-NP-32` present) — the close-gate carries the id the producer packet carries |
| **AA-45** (fr-AdminAuditLog:88) | positional `:key` **FORCED by the contract** (no wire identity; pydantic drops `_id`); hazard unreachable (wholesale assignments only) | **NEXT-BOUNDARY LEDGER** — API-contract row (identity on the wire), SS-3/SS-4 input |
| **AA-46** (:89) | envelope `page`/`pages` discarded, re-derived client-side; lossless round-trip today | **KILL-WITH-RATIONALE** — a redundancy record; no defect, no work |
| **AA-47** (:90) | no runtime boundary validation; pydantic guarantees shape; survives as UNEVEN defensiveness (timestamp/target guarded; action/ip_hash not) | **NEXT-BOUNDARY LEDGER** — SS-4 contract input |
| **FR-AFP-51** (:112) | the census ToastVariant break is **UPLIFT-TARGET**, not present-tense | **BOOKED HERE**: tense correction lands in the CENSUS-ADDENDUM (§1a #8) and **must reach whatever spec quotes the census break table** — else the uplift budget reads a break that does not exist |
| **FR-AFP-52** (:113) | D grades a surface FR-AFP-1 proves unreachable; the corpus never states its conditional | **NEXT-BOUNDARY LEDGER** — corpus-composition input for SS-3/SS-4 |
| **FR-AUL-16** (fr-AdminUserList:54) | the only cascade-delete surface, zero coverage; "un-harnessable" defence KILLED (ruling 3e) | **NAMED WAVES, decided here**: harness decision → F.W4 · devDependency + admin-tab axe limb → F.W9. **If SS-4 declines the widening, the decline is recorded explicitly with rationale (M-25).** C-B2's BLOCKER demoted-and-folded here |
| **fr-App i-2** (:143) | route-owned surface, no component-level home | **NEXT-BOUNDARY LEDGER** — the per-route challenge corpus |
| **fr-App C-12** (:90/:143) | App is **API-INERT** — the negative guard | **KILL-WITH-RATIONALE — as the NEGATIVE RECORD, verbatim** (⟨cmd⟩ `grep -n 'NO leaf in this component' fr-App.md` → `:90`, this seat): *"**R6-8 and R3-7c have NO leaf in this component** — a per-component sweep must not manufacture one."* It keeps F.W5–W8's client-edge sweep off a false leaf |
| **MG-ι** value-side limb | see §2.4 | **NEXT-BOUNDARY LEDGER — the V·π/parser-seam ledger, WITH THE OWNER-MAIL FLAG** |
| **FR-AH-30** (fr-AppHeader:88) | the D-axis's own `@layer components` remedy **REVERSES the outcome** (glass's competing rules already there at 0-3-0 vs 0-2-0 — the consumer block silently LOSES) | **NAMED WAVE (cure-constraint, F.W4 where FR-AH-11 lands)**: only `@layer glass-overrides` **APPENDED AFTER `utilities`** works, and it is stated. "Cure adjudicated separately from the defect" law |
| **FR-AH-45** (:108) | zero props/emits/slots — a question owed an answer | **NEXT-BOUNDARY LEDGER** — SS-3/SS-4 spec-input; discharges together with FR-AH-30 |
| **FR-CP-43** (fr-CoefficientsPanel:81) | canvas renders/hit-tests 40 units in imperative loops, no template node to key — instance censuses read 1 where there are 3 | **KILL-WITH-RATIONALE** — it **COMPLETES** R5-7/R6-6 + fr-BasisCanvas L§R5-7's demand; discharged, no work remains |
| **FR-CP-44** (:82) | lane-frontend:443's metric-stack shadow candidacy seated on two hosts neither of which renders a metric; belongs on CoefficientsSpectrum.vue | **BOOKED HERE** (census correction → CENSUS-ADDENDUM) **+ F.W3 CANDIDATE** for the re-seat (FR-CP-3's candidate cure; "by construction" overclaim noted) |
| **FR-CP-45** (:83) | D's line citations drift ≈−10/−2; L and C cite accurately | **ADOPTED AS LAW BY THIS SPEC**, verbatim: "take D's CLAIMS and L/C's LINES; re-resolve every anchor before quoting into a wave spec." **Binds every anchor this file quotes — including its own** (§2.8 E-4) |
| **fr-EasingPicker D/D-21** (:101) | empty/loading/error genuinely N/A (compile-time catalogue — CLOSED, not skipped); the focus-ring question MOOT once D-1's cure lands | **KILL-WITH-RATIONALE** — SS-13 residue only |
| **PAW-32** (:88) | `PaperSectionBlocks.vue.d.ts` ships no `__VLS_Slots` (sibling ships 2) — `figure.*`/`callout.*` implicit `any` | **NAMED REGISTER — LATEX-PAPER-RELAY** (§2.6); producer gap, no consumer cure |
| **PAW-38** (:94) | un-deduplicated title slugs collide across FOUR registries at once; LATENT (93/93 unique, `uniq -d` empty) | **NAMED REGISTER — LATEX-PAPER-RELAY**, joint P-6/P-12: the cheapest guard is precisely the harness PAW-50 proves absent. L-17 books only cross-consumer; this intra-document case was unbooked by all three axes |
| **PAW-39** (:95) | 51 `\section` + 31 `\subsection` flattened to one heading level; the flattener EMITS `sourceLevel` (:110) the renderer never reads | **NAMED REGISTER — LATEX-PAPER-RELAY**. **CENSUS LAW: 11 + 51 + 31 = 93 brace-anchored headings; quote 31, NEVER the readers' un-anchored 35.** Dissent carried: the a11y composite (PAW-15∘PAW-11∘PAW-7∘PAW-39) approaches blocker weight AS A CLUSTER with no single row carrying it |
| **PAW-42** (:98) | no empty/loading/error state — **PROVEN UNREACHABLE** (build-time `virtual:paper-content`) | **KILL-WITH-RATIONALE** — epistemic record; an unreachable state is a CLOSED question |
| **PAW-45 / PAW-46** (:195/:196) | the module-scoped-cache trio's latex halves (§2.6) | **NAMED REGISTER — LATEX-PAPER-RELAY** |
| **GAB-29** (:74) | see §2.4 | **KILL-AS-DESTINATION, RECORDED AS FALSIFIER** — load-bearing for F.W1's budget |
| **FR-COB-20** (fr-CanvasOverlayButton:74) | interaction contract entirely implicit; "dies with the file"; the tree favours DELETE | **NAMED WAVE — F.W0's F8-REACH-02 verb**; F.W10 verifies no HOLD survives to close (that verification is this wave's entire claim — not a re-booking) |

#### §2.5a The strict NO-WAVE-OWNER remainder — the roster RE-DERIVED FROM THE 66 RECORDS (R2-6b)

**Why it is here — and why round 1's version of it was still a subset.** §2.5 above homed 23 ids and stamped *"zero left open"* while the corpus's strict-dispositioned NO-WAVE-OWNER rows ran far past it. Round 1 cured that by taking **the PASS-1 check file's** roster — complying with the ruling's letter and **inheriting its blind spot**: PASS-1's set C was itself a subset, so *"a drain complete-looking because it counted only what it chose to look at"* survived its own cure with a larger number attached. **R2-6b re-rules the operand: the roster is derived FROM THE 66 RECORDS, never from any check file.** The measurement, this seat, read-only in `registry/adjudicated/`:

- ⟨cmd⟩ `grep -o 'NO-WAVE-OWNER' fr-*.md | wc -l` → **167** occurrences · ⟨cmd⟩ `grep 'NO-WAVE-OWNER' fr-*.md | wc -l` → **165** lines · ⟨cmd⟩ `grep -l 'NO-WAVE-OWNER' fr-*.md | wc -l` → **56** records · ⟨cmd⟩ `ls fr-*.md | wc -l` → **66**.
- **Detector, stated inline (R2-9)** — the token is probed in **BOTH dash spellings**, over table rows ⊕ id-headed route-marked bullets ⊕ prose routings walked to their enclosing row id: ⟨cmd⟩ `grep -o 'NO–WAVE' fr-*.md | wc -l` → **0** (the en-dash arm is ∅ today and **stays in the probe so it can never silently stop being ∅**). Strict-disposition forms, both shapes: ⟨cmd⟩ `grep -E '(→|⇒) *\*{0,2}NO-WAVE-OWNER' fr-*.md | grep -v 'Routing law' | wc -l` → **31** arrow-form (⟨cmd⟩ `grep -lE '(→|⇒) *\*{0,2}NO-WAVE-OWNER' fr-*.md | wc -l` → **16** records) ⊕ ⟨cmd⟩ `grep -E '\*\*NO-WAVE-OWNER' fr-*.md | grep -v 'Routing law' | grep -vE '(→|⇒) *\*{0,2}NO-WAVE-OWNER' | wc -l` → **61** bold-terminal-cell form. **▲ THAT EXCLUSION IS UNDER-CUT AND IS SUPERSEDED BY §2.5a-iii (repair round 3).** `grep -v 'Routing law'` catches **one spelling of three**: the corpus writes the same routing taxonomy as *"Routes:"* (e.g. `fr-AnimationControls`, `fr-ConvergencePlot`) and *"Dispositions:"* (`fr-AppHeader`), so **31 taxonomy lines** were counted as findings by the 31/61 probe. The two figures above still reproduce **exactly as written** and are kept for that reason — a receipt is not re-written because a better one exists — but **the operative detector is §2.5a-iii's**, which excludes the taxonomy by SHAPE (three or more wave tokens on one line) rather than by one of its spellings, and whose five classes sum to 165.
- **The band is disclosed, and round 2's disclosure of it was WRONG TWICE — corrected here at the bytes (repair round 3, rulings R3-5.5 + R3-10.6; PASS-3 §4 convicted the cell).** Round 2 wrote: *"PASS-2's corpus-derived strict roster is 81 row-level lines …; this seat's detector above lands 92 over the same 56 records. The two differ ONLY in where the boilerplate/verdict-prose cut falls."* **Both halves are false.** **(i) The two sets CROSS; they do not nest.** Two of the sixteen escapes homed at §2.5a-ii are **outside** the 92 — `fr-PaperSearchDropdown:75` (`L:L-16`) and `fr-FunctionInput:81` (`C §0 row 7`), both weak-form — so 92 is not a superset of 81 and the difference is not only where a cut falls. **(ii) "over the same 56 records" fuses two denominators** — **and round 3's receipt for THIS correction was itself unrunnable: REVOKED at R4-4.4 and RE-CUT here per R4-1.12.** The dead form printed `-h`, so `cut -d: -f1` received the LINE NUMBER instead of the record, and it omitted both taxonomy filters; as printed it returns 27 (arrow arm) / 70 (both arms) / 45 (`-n` unfiltered) and **never 44**. **The FIGURE stands because the honest pipeline produces it** — which is R4-1.12's own direction: *the figure follows the pipeline, never the reverse*. ⟨cmd⟩ this seat, 2026-08-29, **one base** (`cd docs/tranches/V/megatranche/registry/adjudicated`), pinned toolchain `bash` + BSD `/usr/bin/grep` 2.6.0-FreeBSD (R4-2; no `-P`, no `\K`, no lookaround, no bounded wildcard), filenames RETAINED, both `Routing law` filters present, arrow-form excluded from the bold arm — **lines**: `{ grep -nE '(→\|⇒) *\*{0,2}NO-WAVE-OWNER' fr-*.md \| grep -v 'Routing law'; grep -nE '\*\*NO-WAVE-OWNER' fr-*.md \| grep -v 'Routing law' \| grep -vE '(→\|⇒) *\*{0,2}NO-WAVE-OWNER'; } \| wc -l` → **92**; **records**: the identical brace group piped `\| cut -d: -f1 \| sort -u \| wc -l` → **44**. **56** is a different denominator and now carries its own command rather than sharing one: ⟨cmd⟩ `grep -l 'NO-WAVE-OWNER' fr-*.md \| wc -l` → **56**, the count of records containing *any* NO-WAVE-OWNER token. *(Table-cell escaping: `\|` inside these ⟨cmd⟩ spans is the markdown escape for a literal `|`; unescape before running.)* **Two-key rule (R4-4's standing falsifier): this cure was authored by this seat and is owed a re-run by a seat that did not author it.** **A disclosure written expressly to keep a band honest is the last place imprecision is tolerable**, which is why the band is now not a band at all but a **balanced partition of every one of the 165 lines** — §2.5a-iii's transcript, run fresh from the 66 and reproducing to the line.

FR-NP-32 heads §2.5; the **36-row strict remainder** lands in the first table here, the **16 corpus-derived escapes** in the second (§2.5a-ii), and the **14 rows the fresh derivation lands** in the third (§2.5a-iii — the three `fr-PathPreview` strict escapes ⊕ the ten strict rows round 2 filed under a class verb). **Round 2's sixteen span BOTH classes** — fourteen are strict at the corrected detector, two (`L:L-16` at fr-PaperSearchDropdown:75 · `C §0 row 7` at fr-FunctionInput:81) are weak-form, which is why the 81 and 92 sets CROSS rather than nest (§2.5a-iii(a)); all sixteen keep their per-id verbs either way. Two registers do the receiving, both already standing in this spec: **BH-RELAY** = the SS-6 glass-ui relay register (§2.4 NEXT-COMMUNIQUE roster — producer rows NEVER become frontend hacks) · **CENSUS-INPUT** = the SS-3/SS-4 spec-authoring / census-methodology next-boundary ledger. Rows whose STATING home is another wave are **cited, never re-booked** (R-5's one-home law). *(The PASS-1 roster's own shape — 28 table rows enumerating 37 banked ids, its headline "36" counting the two `M-16` homonyms once — is recorded as provenance only; both homonyms are booked below, record-qualified, per the one-home/qualify-short-ids law.)*

| banked id · anchor | mechanism in one line | terminal verb |
|---|---|---|
| **FR-NP-11** (fr-NotationPills:52) | PRM squish, unlayered-dist mechanism, harm bounded, cured at 7.0.0 | **BH-RELAY + FOLD-WITH-TARGET → banked FR-COB-12** (producer) — no new booking |
| **FR-NP-26** (:67) | clsx + cva live runtime externals; peer enumeration corrected | **CENSUS-INPUT** — census correction, SS-3/SS-4 |
| **MM-6** (fr-ContourEditorCanvas:92) | `lane-frontend.md:118` describes a composable with neither pan nor zoom; the row propagated into L-7's killed cure | **CENSUS-INPUT** — SS-3/SS-4 spec-authoring input |
| **L-§4 / R5-7** (:96) | the `<circle`/`v-for` split defeats a same-line deriver; same-line = **8**, not 5 (K-17) | **FOLD-WITH-TARGET → intake R5-7** (identity), landing in **CENSUS-INPUT** |
| **FR-EQC-15** (fr-EqCoefficientsPanel:52) | the C axis's citations into `CoefficientsSpectrum.vue` run late (+5..+11, growing down-file) on a HEAD-clean file | **CENSUS-INPUT** — and an input to **F.W0's G-11 anchor table** (**F-W0 §4 gate G-11**, stable idiom R2-2), quoted not re-performed |
| **D/m-12** (:76) | 21 sites / 14 files (shim's "14" and the lane's "25" both wrong, opposite directions) | **CENSUS-INPUT + CITE F.W0's G-12 denominator table** (**F-W0 §4 gate G-12**, stable idiom R2-2) — **F.W1 sizing uses 21**; F.W10 books no re-count |
| **FR-GIG-20** (fr-GalleryInfiniteGrid:82) | the 8-line GalleryCard binding block instantiated at 4 sites / 3 components | **CENSUS-INPUT** — SS-3/SS-4 repair-unit sizing |
| **FR-GV-37** (fr-GalleryView:79) | 442 lines, four responsibilities; the batch feature's only coupling is `adminMode`+`activeTab` | **CENSUS-INPUT** — SS-3/SS-4 repair-unit sizing (an extraction seam for spec authors, not a wave's defect) |
| **F-6 (= C-C-9 = L-15)** (fr-GalleryDraftsSection:49) | the template-`src`-binding operation edge is structurally invisible to a function-keyed operation↔client model | **CITE-CONSUMING-WAVE — F.W5** (adopted there as the D1 register's SECOND blindness lock, beside COUNTING LOCK K-1). F.W10 records terminal-by-citation and books nothing |
| **m-18** (:83) | all three axes declare `cd26c65` while reading the dirty worktree; extends banked fr-CollapsibleSection F-1 | **CENSUS-INPUT (fleet method)**, tied to **GAB-13's F.W0 baseline ruling** (§2.3) — the disclosure line IS the disposition |
| **HLG-32 · MISSED-2** (fr-HarmonicLevelGrid:88) | glass 4.0.0's unlayered `components.css` defeats its own `@layer components` PRM press guard | **BH-RELAY + FOLD-WITH-TARGET → banked FR-COB-8 (producer CLASS) + FR-COB-12 (the instance)** — novelty struck at its register; this component recorded as a further instance site |
| **FR-MSP-12** (fr-MorphShapePreview:104) | the machine-synthesized non-`color-mix` fallback paints accent-on-accent, outside any layer | **BH-RELAY + FOLD-WITH-TARGET → FR-COB-26 (new site)** |
| **FR-COB-7** (fr-CanvasOverlayButton:56) | composed cascade: single-channel pressed register + dead `glass` hover/active border arms | **BH-RELAY** — packet item 1 |
| **FR-COB-8** (:57) | glass ships pre-compiled utilities UNLAYERED, structurally outranking its own layered a11y resets | **BH-RELAY** — packet item 2; the CLASS that HLG-32 and D-19 fold into |
| **FR-COB-23** (:82) | focus-ring contrast ≈**1.94:1** against `--background`, both readers concordant | **BH-RELAY** — packet item 4 |
| **FR-COB-26** (:85) | the generated non-`color-mix` fallback paints solid `var(--foreground)` under `text-foreground` ink | **BH-RELAY** — packet item 5; FR-MSP-12 folds here |
| **FR-COB-28** (:87) | **the relay-instruction row itself** — the corpus's producer findings carried ONE relay instruction where the invariant requires all; the consolidated seven-item BH list | **BH-RELAY — THE PACKET.** FR-NP-32 rides at its **TOP** (§2.5 head row); every BH-RELAY verb above resolves into this packet, and G-F10-12 tests the packet's receipt, not its authorship |
| **M-16** (**fr-CoefficientsSpectrum**:68, record-qualified) | the corpus's producer layer is pinned to a producer that MOVED (v7.0.0-322 → -393); three tag→HEAD inversions already banked | **CITE-CONSUMING-WAVE — F.W1's re-pin gate** (derive the break surface at the ADOPTED commit) **+ CENSUS-INPUT** (producer evidence carries the COMMIT HASH, never the version string — F.W0's G-13 law, **F-W0 §4 gate G-13**, stable idiom R2-2) |
| **M-16** (**fr-MobileFloatingToc**:66, record-qualified — a HOMONYM, never merged) | two PaperSearch hosts over one shared state (PaperSidebar mounted at ALL viewports, CSS-only hide) | **CITE-CONSUMING-WAVE — F.W3** (where the PaperSearch rows land), the twin settled at `F-W3.md` **§X.1-v4 §5**: `fr-MobileFloatingToc` is the largest `.c` holding (23 id-instances) and `.c`'s rows land on the ONE g13 commit — so the slash resolves to F.W3 by the file criterion, cited never booked. Scrim leg K-12, census leg folds to banked FV-27 — **no re-booking, and never folded into the CoefficientsSpectrum homonym** |
| **C-18** (fr-AnimationControls:120) | zero value.js colour consumption in the tree's most colour-dense transport surface (`colors.ts` honestly out of graph) | **CITE-CONSUMING-WAVE — F.W2** (adopted there as the colors.ts-deletion denominator twin of D1's C-25); F.W10 books nothing |
| **L-8** (:124) | R5-7's blind-spot class transposed: two `v-if/v-else` icon twins invisible to an `:is`-keyed budget (R3-10) | **CENSUS-INPUT** — scope-of-budget claim, not a runtime defect; remainder-upheld |
| **C-25** (fr-CanvasControlsDock:96) | zero value.js and zero keyframes.js consumption — this file's F.W2 migration cost is zero; two `--viz-amber` reads join the denominator | **CITE-CONSUMING-WAVE — F.W2** (the migration-cost denominator) **+ CENSUS-INPUT** |
| **M-10** (:100) | systematic L-axis line-citation drift against the dirty `VisualizationView.vue` | **CENSUS-INPUT + input to F.W0's G-11 anchor table** — quoted, never re-resolved here |
| **m-14** (fr-CollapsibleSection:71) | a conditional GATE on the R5-7 derived-evidence surface with **inverted polarity** (source over-counts what a DOM census sees behind a closed section) | **CENSUS-INPUT** — SS-3/SS-4 methodology; paired with i-8's positive control |
| **i-7** (:83) | the corpus never reconciled its three-way severity spread (same facts at three severities) | **CENSUS-INPUT** — corpus-composition input, the sibling of FR-AFP-52 |
| **i-3** (fr-ContourSettings:100) | the one panel owning the compute pipeline is the one panel defaulting COLLAPSED, its body inert | **CENSUS-INPUT** — SS-3/SS-4 corpus row; no consumer cure booked |
| **i-8** (:105) | the R5-7 class does not bite here (the file's one `v-for` is a component callsite) — its **dual** does | **CENSUS-INPUT** — recorded as a derivation recommendation, not a defect |
| **D-19** (fr-ConvergenceLegend:97) | Vue scoped styles are unlayered; glass's ladder opens `@layer components` — unlayered wins categorically, which is WHY the px literals accumulated with no diagnostic | **CENSUS-INPUT, verbatim** (⟨cmd⟩ `grep -n 'unlayered-scoped-vs-system' fr-ConvergenceLegend.md` → `:97`, this seat): *"the durable fix is a lint rule (unlayered-scoped-vs-system), a spec-authoring input for SS-3/SS-4, not a per-component edit"* — folds to FR-COB-8's class for the producer half |
| **I-3** (fr-DarkModeToggle:111) | the C axis's self-repo citations drift ~10–11 lines while its `node_modules` citations are byte-exact | **CENSUS-INPUT + input to F.W0's G-11 anchor table** (one of G-11's own named drift registers) |
| **CITE** (fr-EasingCurvePreview:68) | the D axis's provenance is systematically mis-anchored — **one citation past EOF inside its highest-severity carry** | **NAMED WAVE — F.W0's G-11** (**F-W0 §4 gate G-11** names this register by name — stable idiom R2-2); F.W10 quotes the published table, never re-resolves |
| **D-2 / L-3 / C-1** (fr-EditorControlsDock:51) | the magnet retint block is 100% dead (`--slider-scrub-*` is not a token namespace at 4.0.0; two arms doubly dead; even `width:100%` a no-op per K-8) | **NAMED WAVE — F.W3** (where this record's rows land), settled at `F-W3.md` **§X.1-v4 §5**: `fr-EditorControlsDock` holds residue on the **`.a`** arm, whose rows are authored in the unit's own §C.A/§C.B cluster — one deletable block, **cited not re-booked**. No new F.W10 work |
| **L-m5** (fr-FunctionInput:62) | `./chip` ABSENT at the installed 4.0.0; `./toggle-chip` present; CENSUS:100 routes NotationPills to `./chip` unqualified | **NAMED WAVE — F.W3** (the record's own routing) **+ CENSUS-INPUT** for the census cell |
| **L-i1** (:82) | the R5-7 native-template-loop class does not apply (both `v-for`s are component callsites) — a **positive control** | **CENSUS-INPUT** — a negative finding is a terminal disposition; manufacture no work |
| **L·I-2-as-corrected / R5-7** (fr-GalleryCard:87) | the callsite-keyed-deriver blindness is genuine but L's instance was mis-derived; corrected form recorded | **CENSUS-INPUT** — SS-3/SS-4 census-methodology |
| **D-26** (fr-VisualizationView:99) | underline indicator element never rendered — anchor-positioning-only by construction | **BH-RELAY (producer, verbatim: "NO-WAVE-OWNER (producer, glass BH relay)")**; engine matrix → SS-13 |
| **★MF-7** (fr-PaperView:113) | permanent `will-change: opacity` on a fixed full-viewport overlay — a standing compositor layer for far-jump-only chrome | **CITE-CONSUMING-WAVE — F.W4** (set/clear inside `withOverlay`, per the record's own verb) **+ CENSUS-INPUT** for the referral-drop process note |

##### §2.5a-ii The SIXTEEN corpus-derived escapes — the rows no check file looked at (R2-6b; each with a TERMINAL verb)

**Provenance of every quotation below**: each disposition is the pasted output of a `sed` run by this seat, read-only, in `docs/tranches/V/megatranche/registry/adjudicated/` — the per-row ⟨cmd⟩ is in the anchor cell. Record line anchors are lawful here and only here: the registry is the **frozen corpus** (R2-2 clause 2(i)), immutable and anti-renamed. **No id below is re-booked, renamed, or re-graded** — the verb is the terminal disposition, the banked identity is untouched.

| banked id · anchor ⟨cmd⟩ | in-record disposition, VERBATIM | terminal verb |
|---|---|---|
| **`R2-missed-1`** (fr-ImageUpload's, record-qualified) — banked at row **11** · fr-ImageUpload:45, MAJOR ⟨`sed -n '45p' fr-ImageUpload.md`⟩ **— id RESTORED at R4-10** (round 3 labelled this row by its ROSTER NUMBER; the canonical's rule 1 heads it `R2-missed-1`, homed `NWO→SS-3`. **Homonym guard**: NOT fr-UserSlugBar's lowercase `r2-MISSED-1`, an ALIAS of `FR-USB-16` and cut to F.W9 at §2.2 — two different records, never merged) | *"**The corpus never swept the adjudicated registry**… → **NO-WAVE-OWNER** (procedural — binds SS-3/SS-4 spec authoring and every future challenge lane: REGISTRY-FIRST is a precondition, not a courtesy)."* | **CENSUS-INPUT** — SS-3/SS-4 spec-authoring precondition, adopted in the record's own words. **This row is the mechanism of the very defect it names**, twice over (PASS-1 D5, and round 1's inheritance of it): the drain that read a check file instead of the registry IS the un-swept-registry failure. Recorded as this drain's own falsifier |
| **`R1-MISSED-5`** (fr-ImageUpload's, record-qualified) — banked at row **29** · fr-ImageUpload:63, INFO ⟨`sed -n '63p' fr-ImageUpload.md`⟩ **— id RESTORED at R4-10** (canonical home `NWO (packet)`. **Homonym guard**: NOT fr-UserSlugBar's lowercase `r1-MISSED-5`, an ALIAS of `FR-USB-16`) | *"→ **NO-WAVE-OWNER** (spec-authoring input: propagate, don't merely preserve)."* | **CENSUS-INPUT** — SS-3/SS-4; a positive census (the tree's ONLY `type="button"`, banked instrument `fr-MorphShapePreview.md:9`). A propagation candidate is not a defect and mints no work |
| **C-3** · fr-GalleryInfiniteGrid:50, MAJOR ⟨`sed -n '50p' fr-GalleryInfiniteGrid.md`⟩ | *"**FOLDS BY REFERENCE to banked FR-GFC-3 (→ F.W5-W8). Do NOT re-book.**… C-3's genuinely new grain — the **orphan-client class** (a published emit contract with no reachable operation, the R6-8 degenerate case) — is carried **NO-WAVE-OWNER → SS-3/SS-4** as census methodology."* | **FOLD-WITH-TARGET → banked FR-GFC-3** (its own instruction; the fold's wave is F.W5–W8, cited never re-booked) **+ CENSUS-INPUT** for the orphan-client grain. C's BLOCKER over the banked MAJOR carried as **preserved dissent** |
| **M-7** · fr-SvgFilters:91, INFO ⟨`sed -n '91p' fr-SvgFilters.md`⟩ | *"Two of three axes' library proof cells are not re-runnable as published. **→ NO-WAVE-OWNER** (fleet provenance note for the SS-3/SS-4 spec authoring)."* | **EXCLUDED-WITH-REASON — fleet provenance note** (R2-6b's own verb): a re-runnability finding about the corpus's own evidence, not a surface defect. It joins the provenance register **as an input to F.W0 §4 G-11** (the D-19 anchor-resolution table) and books no consumer work. Its accuracy benchmark is carried: *"challenge-L's coordinates are byte-true at both ends"* |
| **L:L-16** · fr-PaperSearchDropdown:75, INFO/LATENT ⟨`sed -n '75p' fr-PaperSearchDropdown.md`⟩ | *"**L:L-16 — INFO, LATENT · FOLDS into banked fr-PaperSearch L-19 — NO-WAVE-OWNER.** Code-unit/code-point mismatch; 0 non-BMP code points in the live corpus… Re-measure on any paper swap."* | **NAMED FOLD → banked `fr-PaperSearch L-19`** (the record's own fold; the target is real — ⟨cmd⟩ `grep -n 'L-19' fr-PaperSearch.md` → `:57` *"L-19+C-N1 (**NO-WAVE-OWNER** bank)"*). The re-measure trigger travels with the fold. **F-W4 §0a's NEGATIVE ROSTER does NOT cover this row** — it disposes of the R5-7 question at this record, a different mechanism |
| **i-1 = L-INFO-2** · fr-BasisSelector:92, INFO ⟨`sed -n '92p' fr-BasisSelector.md`⟩ | *"(census correction UPHELD)… lane-frontend.md:87's 'per-basis term sliders' is wrong. → **NO-WAVE-OWNER** (SS-3/SS-4 census input)."* | **CENSUS-INPUT** — a lane-doc correction; lands in the CENSUS-ADDENDUM's errata beside FR-CP-44/FR-AFP-51 (§1a #8), never as a lane-doc rewrite |
| **i-2 = L-INFO-1** · fr-BasisSelector:93, INFO ⟨`sed -n '93p' fr-BasisSelector.md`⟩ | *"→ **NO-WAVE-OWNER** (SS-3/SS-4 census methodology)."* | **CENSUS-INPUT** — the `NATIVE_TEMPLATE_LOOP` family's blindness to native NON-loop controls (a 2-of-4 undercount). Cross-ref `fr-BasisCanvas L§R5-7` **cited, never merged** (same family, different mechanism — the record says so) |
| **§4 method row + bypass** · fr-VisualizationView:103 ⟨`sed -n '103p' fr-VisualizationView.md`⟩ | *"Method → **NO-WAVE-OWNER (SS-3/SS-4)**; bypass → F.W1/F.W4."* | **SPLIT EXACTLY AS BANKED**: method half → **CENSUS-INPUT**; the `responsive` bypass half is **CITE-CONSUMING-WAVE (F.W1/F.W4)** — cited, never re-booked here. A row with two banked halves takes two verbs, never one averaged verb. **Twin-surface note (F-W3 §X.1-v4 item 4)**: the partition RULES **`fr-VisualizationView L-26` to F.W3** — a one-line minuted departure from R2-3b on that id, because `VisualizationView.vue` ∈ `.e` and the criterion admits no exceptions. **`L-26` is a DIFFERENT row on a different axis**: this `:103` row's bypass half carries a **sole** `F.W1/F.W4` spelling, which the file criterion does not touch (it governs dual `F.W3/W4` rows only). Both homings stand; neither moves the other |
| **i-5 = L-18** · fr-MobileFloatingToc:93, INFO ⟨`sed -n '93p' fr-MobileFloatingToc.md`⟩ | *"**INFO → NO-WAVE-OWNER (SS-3/SS-4).** `<template v-for>` (:149) is a third loop-host kind falling through both R5-7's component-callsite keying and R6-5's `NATIVE_TEMPLATE_LOOP` cure… Census methodology, not a wave row."* | **CENSUS-INPUT** — and the corpus's own **third loop-host kind**, which is why R2-9's shape-agnostic detector is stated inline above rather than assumed. *"Not a wave row"* is the record's verb and is honoured |
| **i-5** · fr-CoefficientsSpectrum:101, INFO ⟨`sed -n '101p' fr-CoefficientsSpectrum.md`⟩ | *"(a) Callsite-keyed instance budgets understate this file ~20× (2 callsites → up to 41 live tooltip roots) — census methodology, **→ NO-WAVE-OWNER (SS-3/SS-4)**… All → F.W3/W4 as riders, none needs its own item."* | **CENSUS-INPUT** on limb (a) — the ~20× budget understatement is an SS-3/SS-4 sizing input. Limbs (b)/(c)/(d) are **CITE-CONSUMING-WAVE (F.W3/W4 riders)** per the record's own closing sentence, **and the twin that BOOKS them is now settled rather than left as a slash**: `F-W3.md` **§X.1-v4** (landed FIRST this round, R3-1a) admits `fr-CoefficientsSpectrum` into the record partition at **`.d`** — the file criterion answers **F.W3**, because `shared/CoefficientsSpectrum.vue` is a member of §5d's Files list — with `m-19`/`m-22` as its named residue. **This is the record v3's *"every"* was false by**, so a rider routed "F.W3/W4" here would have pointed at a partition that did not hold its record; it does now. F.W10 **cites the partition and books nothing on it**. **Homonym guard: this `i-5` is NOT fr-MobileFloatingToc's `i-5 = L-18`** — both record-qualified, never merged |
| **31** · fr-ContourPreview:64, MINOR ⟨`sed -n '64p' fr-ContourPreview.md`⟩ | *"ADJUDICATED → **NO-WAVE-OWNER** (procedural — binds SS-3/SS-4 spec authoring)."* | **CENSUS-INPUT + input to F.W0 §4 G-11** (this id is named in G-11's own carry roster) — C-lane provenance drift with the L-axis accuracy benchmark attached. F.W10 quotes G-11's published table and re-resolves nothing |
| **39** · fr-ContourPreview:72, INFO ⟨`sed -n '72p' fr-ContourPreview.md`⟩ | *"ADJUDICATED → **NO-WAVE-OWNER** (comment/cleanup if row 40's wave touches the file; the heap-correct certification is the record)."* | **KILL-WITH-RATIONALE, conditional-cleanup form**: a dead branch whose *record* is the positive certification (R2's heap re-derivation certifies the rest CORRECT). No wave is scheduled; if row 40's wave opens the file the comment rides free. **A negative finding is a terminal disposition** |
| **41** · fr-ContourPreview:74, INFO ⟨`sed -n '74p' fr-ContourPreview.md`⟩ | *"ADJUDICATED → **NO-WAVE-OWNER** (binds SS-3/SS-4 register)."* | **CENSUS-INPUT** — a grading-consistency rule (default-restatement is documentation-of-intent or noise, never both). Binds the register, mints no edit |
| **i-1 = D/i-4** · fr-CollapsibleSection:77, INFO ⟨`sed -n '77p' fr-CollapsibleSection.md`⟩ | *"Callsite count drift in the corpus: A8-17 says 5, design-synth says 3; the tree has **4 usages / 3 files** (my grep, exact; ContourSettings:358 is a comment). → **NO-WAVE-OWNER** (census correction, SS-3/SS-4)."* | **CENSUS-INPUT** — **quote 4/3, never 5 or 3** (a corrected denominator, binding on this file the same way **17/12/6** (E-1 as corrected at R4-7.3 — never the superseded 17/11/6), ELEVEN packets, 15/6 and 31-not-35 are). **Homonym guard: NOT fr-BasisSelector's `i-1 = L-INFO-2`** |
| **FOLD — L-14** · fr-EditorControlsDock:100 ⟨`sed -n '100p' fr-EditorControlsDock.md`⟩ | *"…= the **intake R5-7** identity (already extended at fr-ContourEditorCanvas L-§4)… **→ NO-WAVE-OWNER** with the intake identity."* | **FOLD-WITH-TARGET → intake R5-7** (identity kept, exactly as `L-§4 / R5-7` folds in the first table), landing in **CENSUS-INPUT**. LC's discipline note carried: *"the COUNT is currently total-correct… only the history predicates desync (the L-1 fold)"* — so no count row is minted |
| **C §0 row 7 census correction** · fr-FunctionInput:81, INFO ⟨`sed -n '81p' fr-FunctionInput.md`⟩ | *"**INFO — NO-WAVE-OWNER (SS-3/SS-4 spec authoring consumes).** lane-frontend.md:441's 'currently 6× Button' is false: ONE `<Button>` callsite in a `v-for` over 3 options (my whole read) — the F.W3 budget is 1 line, not 6."* | **CENSUS-INPUT + F.W3 SIZING CORRECTION, cited not booked**: F.W3's Button budget at this file is **1 line, not 6**. The correction lands in the CENSUS-ADDENDUM; F.W10 books no F.W3 work and re-counts nothing |

##### §2.5a-iii THE FRESH DERIVATION — RUN, not promised; the transcript IS the deliverable (repair round 3: R3-10.6 · R3-2.4 · R3-5.5)

**The conviction, stated before the cure.** R2-6b re-ruled the drain's OPERAND to the 66 records **and** attached a clause round 2 never executed: *derive the roster FRESH*. Round 2 honoured the ruling for the sixteen rows PASS-2 had named and **derived nothing new from the corpus beyond them** — so `fr-PathPreview.md`, a record in neither check file's set C, survived the operand correction whole and its **three strict rows escaped the drain entirely** while §5's header claimed the opposite. **That is the FOURTH form of the base this wave exists to abolish**: PASS-1 D5's *counted only what it chose to look at* → its check-file inheritance → its bigger-number-same-subset repair → **an operand corrected without a derivation ever being run**. A ruling honoured at its named rows and skipped at its method clause is not honoured. The cure is structural, not a patch: the transcript below **balances against every NO-WAVE-OWNER line in the corpus**, and a later seat re-runs it in five commands.

**(a) THE TRANSCRIPT — all 165 lines assigned, the arithmetic closing** (⟨cmd⟩ this seat, 2026-08-28, read-only in `docs/tranches/V/megatranche/registry/adjudicated/`; the three probe variables are declared once and every row below re-uses them, so a re-run cannot silently narrow the operand):

- `BOIL='Routing law|\*\*F\.W[0-9].*\*\*F\.W[0-9].*\*\*F\.W[0-9]'` — the routing-TAXONOMY shape, matched by **structure** (a line naming three or more wave tokens beside the disposition), never by a hand-listed roster of records. Round 2's `grep -v 'Routing law'` caught **one spelling of three**: the corpus also writes the taxonomy as *"Routes:"* and *"Dispositions:"*, and those lines were counted as findings.
- `ROW='^(\||[-*] \*\*|[0-9]+\. \*\*)'` — the three row shapes R2-9 requires (table row ⊕ id-headed bullet ⊕ numbered bullet); anything else is prose and is **walked to its enclosing row id** (S-8), never dropped.
- `STRICT='(→|⇒) *\*{0,2}NO-WAVE-OWNER|\*\*NO-WAVE-OWNER'` — both strict shapes in one alternation, **both dash spellings probed** (⟨cmd⟩ `grep -o 'NO–WAVE' fr-*.md | wc -l` → **0**; the en-dash arm stays in the probe so it can never silently stop being ∅).

| class | probe (all three variables as declared) | lines | records |
|---|---|---|---|
| **routing-law / taxonomy boilerplate** | `grep -h 'NO-WAVE-OWNER' fr-*.md \| grep -cE "$BOIL"` | **31** | — |
| **row-level STRICT** | `… \| grep -vE "$BOIL" \| grep -E "$ROW" \| grep -cE "$STRICT"` | **68** | **35** |
| **row-level WEAK** | `… \| grep -vE "$BOIL" \| grep -E "$ROW" \| grep -cvE "$STRICT"` | **32** | **19** |
| **prose STRICT** (walked to its row id) | `… \| grep -vE "$BOIL" \| grep -vE "$ROW" \| grep -E "$STRICT"` | **1** | 1 |
| **prose WEAK** (verdict/adjudication bytes — immutable, §5) | `… \| grep -vE "$BOIL" \| grep -vE "$ROW" \| grep -cvE "$STRICT"` | **33** | — |
| **TOTAL** | `grep -h 'NO-WAVE-OWNER' fr-*.md \| wc -l` | **165** | 56 of 66 |

`31 + 68 + 32 + 1 + 33 = 165` — **the partition exhausts its operand**, which is the property no prior figure in this file had. **THE STRICT ROSTER IS 69 DISPOSITIONS ACROSS 36 RECORDS**: the 68 row-level ⊕ the one prose-strict (`fr-PaperSearch`'s *"Infos (7 carried)"* line, walked to its enclosing row id **`L-19+C-N1`**). **▲ ROUND 3 EXEMPTED THAT 69TH ROW FROM THE PER-ID RULE IT IMPOSED ON ITSELF FOR THE TEN, AND THE EXEMPTION IS STRUCK (2026-08-29).** The round-3 parenthesis read *"already homed at §2.5a-ii's `L:L-16` fold row, whose target that row is"* — but **the fold runs `L:L-16` → `L-19`, so `L-19+C-N1` is the SURVIVING identity, not the folded one**, and a fold target is not thereby disposed: §2.5a-ii's row states only that the target is real (⟨cmd⟩ `grep -n 'L-19' fr-PaperSearch.md` → `:57` *"… · L-19+C-N1 (**NO-WAVE-OWNER** bank) · …"*). The canonical settles it independently — **`fr-PaperSearch (1): L-19` is a row of the `NWO (packet)` roster** (`CENSUS-CANONICAL.md` §2), so it is a packet-homed disposition owed a terminal record at this file's deadline like every other. **It lands as row 14 of (c) below**, and the round's own standard — *"a strict disposition is owed a per-id terminal verb, today, not on some later re-grading"* — now applies without exception. It was the last un-verbed strict disposition in the corpus, and it sat at the wave that declares COHESION §3.3's deadline. Record counts are given per class because **fusing them is exactly the error corrected above**.

**(b) THE ESCAPE TEST, and its result stated as a classification (R3-3.10 — no count of this file is banked).** Each of the 69 strict dispositions was walked to its enclosing row id and that id tested boundary-exact against this spec. **Every one resolved except three**, and those three are `fr-PathPreview`'s: **before this round's write, the strings `PathPreview`, `PP-CENSUS`, `PP-AGGLOM` and `PP-SEVLAW` occurred in no section of this file at all** — not §2.5, not §2.5a, not §2.5a-ii, not the loose class, not §5. **And no other F wave settles them** — ⟨cmd⟩ this seat, **scoped by the glob `F-W[0-9].md` so the probe CANNOT match the file that cites it** (R3-3.7's law: a receipt that invalidates itself the instant it is written down is not a receipt; the unscoped `F-W*.md` form would now return this very file and prove nothing): `grep -lo 'PP-CENSUS' waves/F-W[0-9].md carry/*.md` → **∅ across the other ten specs and both carries**; `grep -lo 'PP-AGGLOM' waves/F-W[0-9].md carry/*.md` and `grep -lo 'PP-SEVLAW' …` → `waves/F-W4.md` · `carry/F-W4-CARRY.md` **and nothing else**, whose own disposition of them is ⟨cmd⟩ `grep -ho 'consumed, not settled here' waves/F-W4.md` → *"consumed, not settled here"*. **G-F10-7's own falsifier — *"a roster that homes a subset and calls it zero-left-open"* — was SATISFIED, at the last F wave, against the deadline clause (COHESION §3.3) this spec itself declares.** The three land below with per-id terminal verbs.

**(c) THE FOURTEEN ROWS THIS DERIVATION LANDS** *(THIRTEEN at round 3; the fourteenth is the 69th strict disposition round 3 exempted — see (a) above, struck at R4-7.3's list-over-prose rule)*. Three are the escapes; ten are rows round 2 filed under a class verb while **this file's own detector grades them STRICT at the bytes**; one is the prose-strict `L-19+C-N1` — and a strict disposition is owed a **per-id terminal verb**, today, not on some later re-grading. Record line anchors are lawful here and only here (frozen corpus, R2-2 clause 2(i)); **no id is re-booked, re-graded, or renamed** — the verb is the disposition, the banked identity is untouched.

| banked id · anchor ⟨cmd⟩ | banked disposition + why it lands here | terminal verb |
|---|---|---|
| **PP-CENSUS** (L-1, census-integrity half) · fr-PathPreview:31, MINOR ⟨`sed -n '31p' fr-PathPreview.md`⟩ | arrow-form strict: *"**MINOR → NO-WAVE-OWNER** (M-25 errata; SS-3/SS-4 must read the registry over `lane-frontend.md:183/:366/:369/:444/:565`, which describe a dead component as a live bespoke surface — the census audited the file, not the graph)."* | **CENSUS-INPUT** — the five lane-doc errata land in the CENSUS-ADDENDUM (§1a #8) beside FR-CP-44 · FR-AFP-51 · `i-1 = L-INFO-2`, **never as a lane-doc rewrite**. **And this row is the drain's SECOND self-falsifier**: a row whose entire content is *"SS-3/SS-4 must read the registry"* was the row a drain reading a check file could not see — the identical shape as `11` (fr-ImageUpload:45), recorded at §2.8 **E-18** so the pair is read together |
| **PP-AGGLOM** · fr-PathPreview:69, INFO ⟨`sed -n '69p' fr-PathPreview.md`⟩ | arrow-form strict: *"**INFO → NO-WAVE-OWNER** (M-25 intake strike-list, SS-3/SS-4)."* ≥8 rows booked against a zero-instance component belong to OTHER components; five banked elsewhere, all counted in the headline tallies — *"Intake MUST strike at agglomeration or F.W4 double-books."* | **CENSUS-INPUT + CITE-CONSUMING-WAVE (F.W4's strike-at-agglomeration)**: F.W4 **consumes** the strike and says so in terms (*"consumed, not settled here"*), so the **terminal** disposition books HERE and F.W4's consumption is **cited, never counted as the home** — the distinction between consuming a row and settling it is the whole of this landing |
| **PP-SEVLAW** · fr-PathPreview:70, INFO ⟨`sed -n '70p' fr-PathPreview.md`⟩ | arrow-form strict: *"**INFO → NO-WAVE-OWNER** (spec-authoring input)."* the remount severity law *"must not be inherited by F.W4 as present grades: the correct reading is MINOR-with-a-fork, and the wire branch is EXPENSIVE"* | **CENSUS-INPUT** — a **grading law**, and its cost note travels with it — **re-pasted BYTE-TRUE at R4-7.1** (round 3 rendered the corpus's straight double quotes as bold single quotes; the verbatim label means bytes, and there is no third meaning): ⟨cmd⟩ `sed -n '70p' fr-PathPreview.md` → *"Every row above marked "wire branch" prices in only there."* F.W4 is cited as the wave the law binds, and books nothing new. **Twin-surface note (F-W3 §X.1-v4 §5, `.d`)**: `fr-PathPreview` carries **one** F.W3/W4 residue id-instance held at **F.W3 `.d`** — a different row on a different axis, **disjoint from these three**; neither homing touches the other |
| **FR-TT-21 = L-11** · fr-Tooltip:55, INFO ⟨`sed -n '55p' fr-Tooltip.md`⟩ | **STRICT (bold-terminal)**: *"**NO-WAVE-OWNER** (SS-3/SS-4 spec authoring consumes the correction)."* R3-7a's "+1/+3" parenthetical is a grep artefact; **the 35/9 budget STANDS** | **CENSUS-INPUT + CORRECTED DENOMINATOR, binding on this file**: the shim budget is **35 callsites / 9 consumers** — the figure **§4b's F.W3 edge** already quotes, now carrying its correcting row. **The round-3 warrant also named "§2.2's FR-TT-1 cut" and that half was FALSE — struck 2026-08-29**: ⟨cmd⟩ this seat, `grep -c '35 callsites / 9 consumers' F-W10.md` → **2 sites, this row and the F.W3 edge**; `grep -c '35/9' F-W10.md` → **1, this row**. §2.2's FR-TT-1 cut carries the **17**-trigger census (Editor ×10 · Canvas ×6 · Wand2 ×1; *"L-4's 17-count census citable"*) and **never 35/9** — a different denominator on a different axis. **The cure lands HERE and could not have landed there**: §2.2 is SHARED SPLICE BYTES, and a unilateral edit inside them breaks G-F10-6 ≡ G-F9-21's Δ=∅ — so a false cross-reference INTO the shared region is cured at the citing end, by rule, never at the cited one. **Twin-surface note (§X.1-v4 §5, `.e`)**: `fr-Tooltip` enters the partition at F.W3 with one id-instance; this NO-WAVE-OWNER row is not that instance — no double-home |
| **FR-TT-22 = C-10** · fr-Tooltip:56, INFO ⟨`sed -n '56p' fr-Tooltip.md`⟩ | **STRICT**: *"**NO-WAVE-OWNER.**"* Zero coupling to value.js / keyframes.js / the 45-op API; the chain terminates at reka + Vue | **CENSUS-INPUT — a NEGATIVE BUDGET STATEMENT, terminal as such** — **re-pasted BYTE-TRUE at R4-7.1** (round 3 lowercased the leading capital and swapped an em-dash continuation for a terminal full stop, i.e. it trimmed the clause that names the row's own class): ⟨cmd⟩ `sed -n '56p' fr-Tooltip.md` → *"A zero-cost row on the F.W2 ledger — a negative budget statement."* It joins C-18 and C-25 (§2.5a) as an F.W2 migration-cost denominator input, **cited to F.W2, booked nowhere new**. *A negative finding is a terminal disposition; manufacture no work* |
| **FR-USB-34 · D-i1 ⊕ r2-MISSED-3 ⊕ L-14** · fr-UserSlugBar:77, INFO ⟨`sed -n '77p' fr-UserSlugBar.md`⟩ | **STRICT**: *"**NO-WAVE-OWNER** (epistemic record + registry correction)."* three dead guards inside `v-if="isLoggedIn"`; carries **RC-4** — banked FR-EQR-25 cites `:67-70` as the exemplar guard another component should adopt, and that line is provably dead | **CENSUS-INPUT + REGISTRY CORRECTION carried against interest**: *"the cure prescription there survives, but its provenance points at a provably dead line; re-point."* The re-point lands in the CENSUS-ADDENDUM; **FR-EQR-25 is not re-graded and not re-booked** — only its provenance is corrected |
| **FR-USB-37 · C-i2** · fr-UserSlugBar:80, INFO ⟨`sed -n '80p' fr-UserSlugBar.md`⟩ | **STRICT**: *"**NO-WAVE-OWNER** (record; FR-USB-13's cure will likely rework the attribute anyway)."* the bearer credential sits in a `title` attribute on a globally mounted element; localStorage already holds the token — surface-without-worst-case | **KILL-WITH-RATIONALE, conditional-cleanup form** (the shape `39` takes at §2.5a-ii): no wave is scheduled; if FR-USB-13's cure opens the attribute the change rides free. The record IS the disposition |
| **FR-USB-38 · L-16 / L-17 / D-i2 / C-i3** · fr-UserSlugBar:81, INFO ⟨`sed -n '81p' fr-UserSlugBar.md`⟩ | **STRICT**: *"**NO-WAVE-OWNER.**"* zero value.js/keyframes/canvas/rAF/listener/timer contact | **CENSUS-INPUT + F.W2 EXCLUSION LOCK, verbatim** — **emphasis RESTORED at R4-7.1**: the corpus carries the span **bolded**, and the quoted span coincides exactly with the emphasis, so nothing forced the elision. ⟨cmd⟩ `sed -n '81p' fr-UserSlugBar.md` → *"**do NOT book this component into the F.W2 migration surface**"* (C-i3's carry, ratified). **Homonym guard**: this row's `D-i2` is an ALIAS of **fr-UserSlugBar's** identity, and is neither fr-PaperSearchInput's `D-i2` (booked two rows below) nor fr-AdminAuditLog's `AA-45 · L-19 / D-i2` alias — ⟨cmd⟩ `grep -l 'D-i2' fr-*.md` → **SEVEN records**, so `D-i2` is never a bare identity in this file |
| **PAW-56** · fr-PaperArticleWindow:293, INFO ⟨`sed -n '293p' fr-PaperArticleWindow.md`⟩ | **STRICT**, **re-pasted BYTE-TRUE at R4-7.1** (round 3 closed the span with a period the bytes do not carry and dropped the latency qualifier that follows it): *"**NO-WAVE-OWNER locally** (latent, margin-1)"*. the `--section-color` ramp has exactly ONE spare stop and its overflow is **silent and total**: `PaperSection` binds `--_section-color: var(--section-color-${sectionIndex})` with **no fallback and no modulo** (`dist :1404`); 13 stops vs 12 top-level entries. A thirteenth entry ⇒ guaranteed-invalid ⇒ the fallback is ITSELF IACVT ⇒ **all six heading-colour declarations drop at once and nothing signals** | **NAMED REGISTER — LATEX-PAPER-RELAY (§2.6)**, joining PAW-32/38/39/45/46. It **EXTENDS PAW-1's ramp cell — new mechanism, never a re-book** (the record says so at its own head), and it is the **producer-side twin of ★MF-10's consumer cure** (§2.6's C-20 row: `var(--section-color-${si % 13}, var(--foreground))` with the root-count assertion, F.W4's one-liner). **SC-3 honoured**: the anchor is `dist :1404`; no producer-src path is cited. K-6's kill of the headroom cells stands — the ramp is EXACTLY saturated |
| **FR-FG-23 = DU-missed-5** · fr-FrequencyGraph:61, INFO ⟨`sed -n '61p' fr-FrequencyGraph.md`⟩ | **STRICT**: *"**NO-WAVE-OWNER**."* It restates FR-CP-45 as **document-scoped, not axis-scoped**, and prices the axis reading: *"a spec applying FR-CP-45 verbatim would discard 24 good anchors to guard one."* | **CENSUS-INPUT — and a BINDING CONSTRAINT ON THIS FILE'S OWN ADOPTED LAW.** §2.5 adopts FR-CP-45 (*"take D's CLAIMS and L/C's LINES"*) as law over every anchor this spec quotes; **this row is that law's correction and rides with it — the law as applied here is DOCUMENT-scoped.** Recorded against interest: the axis-scoped reading is the one this file used at round 1, and it would have discarded 24 sound anchors |
| **m-4 = D/m-9 = L-10 = C-16 + D/i-3 ∘ DU-missed-5** · fr-CollapsibleSection:61, MINOR ⟨`sed -n '61p' fr-CollapsibleSection.md`⟩ | **STRICT, and a SPLIT ROW**: the cell routes *"→ **F.W3/W4** (gap + shrink-0 land with the first consumer) + **NO-WAVE-OWNER**"* — one half wave-routed, one half not | **SPLIT EXACTLY AS BANKED — two halves, two verbs, never one averaged verb**: the geometry half (`gap` + `shrink-0`, landing with the first consumer) is **CITE-CONSUMING-WAVE — F.W3**, per **F-W3 §X.1-v4 §5's `.d` row**, which holds `fr-CollapsibleSection`'s residue at F.W3; the **A-3-new half is CENSUS-INPUT** — the M-audit's HIGH ask rests on a premise this record falsifies (*"the sole reason fourier keeps a parallel CollapsibleSection"*), and the bank's own verb is **re-argue before booking upstream**, which is an SS-3/SS-4 act, not a wave's. **Homonym guard**: ⟨cmd⟩ `grep -lE '(^\|[^A-Za-z0-9-])m-4([^0-9A-Za-z-]\|$)' fr-*.md` → **SEVENTEEN records** head or alias an `m-4`; this one is **fr-CollapsibleSection's** |
| **D-i2** · fr-PaperSearchInput:82, INFO ⟨`sed -n '82p' fr-PaperSearchInput.md`⟩ | **STRICT**: *"**NO-WAVE-OWNER** — SS-3/SS-4 spec-authoring input."* the focus-state CHANGE measures ≈**1.71:1**, an AAA-2.4.13-register figure; *"the binding AA comparison is the resting 1.930:1"* (banked MISS-A6, F.W1 token re-ink) | **CENSUS-INPUT, with the corrected denominator carried and binding**: **quote 1.930:1 for the AA comparison; NEVER 1.71:1** — the row exists for the reason its own bytes give — **re-pasted BYTE-TRUE at R4-7.1** (round 3 ended the span on a comma the bytes close with a full stop, and elided the sentence's opening verb): ⟨cmd⟩ `sed -n '82p' fr-PaperSearchInput.md` → *"Recorded so the forming specs cite each figure under its right criterion."* — a criterion-attribution law, not a defect. Record-qualified per the seven-record `D-i2` homonym class above |
| **`fr-BasisCanvas L §R5-7`** · fr-BasisCanvas:106, INFO ⟨`sed -n '106p' fr-BasisCanvas.md`⟩ | **STRICT (arrow-form)**: *"→ **NO-WAVE-OWNER** (SS-3/SS-4 spec-authoring input; census methodology, not a repair)."* a 9-line template hides N interactive controls with 0 DOM nodes — any instance denominator built on this file reads *"1 element, 0 loops, 0 interactive descendants"* | **CENSUS-INPUT — and this is the row §2.5's FR-CP-43 CITES.** ▲ **`fr-ExportModal L-16` FOLDS HERE — landed at repair round 5 (REST-65), same anchor `fr-BasisCanvas:106`**: ⟨cmd⟩ **⟨LOCALE DECLARED IN THE COMMAND — re-cut at repair round 6, 2026-08-29 (PASS-6 D-8 ⊕ D-10)⟩** `LC_ALL=C /usr/bin/sed -n '49p' fr-ExportModal.md | LC_ALL=C /usr/bin/tr '·' '\n' | LC_ALL=C /usr/bin/grep -n 'L-16'` → `19: L-16 → fr-BasisCanvas :106, NO-WAVE-OWNER ` ⟨*byte-true to the re-run: ONE space after the colon, and the record's own single trailing space before the newline — `od -c` verified at this seat*⟩ — **one home, no re-book**, and the canonical's `NWO (packet)` row for it is answered here **by name**. ⚠ **Locale note, dated 2026-08-29**: the separator this receipt splits on is `·`, a **two-byte** UTF-8 character, and BSD `tr` in byte mode emits one newline per byte — so **the line number of the same hit flips 10 ↔ 19 with `LC_ALL`** (10 under a UTF-8 locale, 19 under `C`). Round 5 published the UTF-8 reading with no locale stated and a two-space paste inherited from the work order's spelling; the programme's pinned-toolchain blocks pin `PATH` and the binaries and say nothing about locale, so **the locale is declared IN the command line above and nowhere else** — the fold, the home and the finding are identical under either reading, and only the coordinate moved. This is the sole `tr '·'` receipt in the eleven specs. ▲ **Homonym guard — `L-16` is a SIX-record banked head** (REST-69): ⟨cmd⟩ `/usr/bin/grep -n '^| \`L-16\` |' CENSUS-CANONICAL.md` → **6**; `/usr/bin/grep -l 'L-16' fr-*.md | wc -l` → **25 records carry the token**. One of the six is `fr-ExportModal`'s, **inside this wave's own NWO roster**. Everywhere else in this file `L-16` appears only as **`fr-UserSlugBar FR-USB-38`'s alias** and as **`fr-PaperSearchDropdown L:L-16`**, a distinct token; **neither is fr-ExportModal's.** ⊘ **The unguarded token is the mechanism of REST-65** — the one six-way collision never disclosed, and the one that hid the escape, in a file that guards `D-i2` (7 records), `m-4` (17), `L-19` (16), `MISSED-2` (3), `M-16`, `i-5` and `i-1`. The relation is the BANK's, not this spec's inference: ⟨cmd⟩ `grep -ho 'FOLD → R5-7/R6-6 + fr-BasisCanvas L§R5-7' fr-CoefficientsPanel.md` → *"FOLD → R5-7/R6-6 + fr-BasisCanvas L§R5-7"*, and its cell continues *"which explicitly demanded FrequencyGraph be checked before publishing any denominator — this row completes it."* **So FR-CP-43 folds ONTO this identity, which means this identity is the one that must carry the terminal verb**; it is booked HERE, once, and §2.5's FR-CP-43 row cites it — one home, no re-book, no re-grade, and the fold direction is read off the corpus rather than asserted. The demand travels verbatim: *"`ConvergencePlot.vue`/`FrequencyGraph.vue` must be checked before publishing any percentage."* **Twin-surface note (F-W3 §X.1-v4 item 2)**: `fr-BasisCanvas`'s F.W3/W4 residue is the **F.W4 hand-off named PER ID** (18 at the record-blind left-token atomiser, 20 with axis (iv)); **`L §R5-7` is not among those ids**, so the drain row and the hand-off are disjoint in both directions |
| **`L-19+C-N1`** · fr-PaperSearch:57, INFO-rollup ⟨`sed -n '57p' fr-PaperSearch.md`⟩ **— the 69th strict disposition, landed at round 4** | **PROSE-STRICT** (the *"Infos (7 carried)"* rollup line, walked to its enclosing row id per S-8): *"… · L-19+C-N1 (**NO-WAVE-OWNER** bank) · …"*. It is the **surviving identity of §2.5a-ii's `L:L-16` fold**, never the folded one — and the canonical independently homes it as a packet row: `CENSUS-CANONICAL.md` §2, `NWO (packet)`, **`fr-PaperSearch` (1): `L-19`** | **NEXT-BOUNDARY LEDGER — CENSUS-INPUT (SS-3/SS-4), carrying the fold's re-measure trigger.** The banked content is the code-unit/code-point mismatch `L:L-16` folds in (0 non-BMP code points in the live corpus; *"Re-measure on any paper swap"*), so the terminal disposition is **a standing re-measure condition on a latent, currently-vacuous defect** — a closed question with a named reopening trigger, which is a terminal disposition and not a deferral. **No id is re-booked**: `L:L-16` keeps its §2.5a-ii row as the FOLD-SOURCE and this row is the FOLD-TARGET's own home, one each, exactly as the one-home law requires. **Homonym guard (the collision this file had left undisclosed until round 4)**: this `L-19` is **fr-PaperSearch's banked row id**; it is neither the LESSON `L-19` (*"every gate names a live witness"*, cited in the masthead, Binding law and §3 head) nor **fr-AdminAuditLog's `AA-45 · L-19 / D-i2` alias**, and it is not **fr-VisualizationView's `L-19`**, which the canonical homes at **F.W4**. Measured, not assumed: ⟨cmd⟩ `grep -l 'L-19' fr-*.md \| wc -l` → **16 records carry the token**, of which the canonical banks exactly **two as head ids** — `NWO (packet) :: fr-PaperSearch` and `F.W4 :: fr-VisualizationView` (`CENSUS-CANONICAL.md` §2); the other fourteen carry it as alias or citation. **`L-19` is therefore never a bare identity in this file from here forward**, and the LESSON-vs-record collision — applied scrupulously to `M-16`, `i-5`, `i-1`, `MISSED-2`, `D-i2` and `m-4` while `L-19` went undisclosed for three rounds — is disclosed here |

**(d) THE WEAK CLASS — RE-ENUMERATED WHOLE, so predicate and roster reproduce each other in BOTH directions (R3-5.5).** Round 2 stated a predicate and then listed sixteen names that the predicate does not generate: **ten of the sixteen are STRICT at the bytes** (landed above, per id), and the true weak residue is not sixteen but **32 row-level lines across 19 records** — a set a later seat re-running the stated predicate gets and round 2's list did not contain. **The class is therefore enumerated exhaustively, by `(record : line → head id)`, not by headline**:

`fr-AdminFlaggedPanel` **:112** `FR-AFP-51 · L-27` · **:113** `FR-AFP-52` — `fr-App` **:82** `MG-ι` · **:86** `i-2` · **:90** `C-12` — `fr-CanvasOverlayButton` **:68** `FR-COB-14` — `fr-CoefficientsSpectrum` **:68** `M-16` — `fr-DarkModeToggle` **:164** *(numbered lesson 4 — **no banked row id**)* — `fr-EquationResult` **:42** `FR-EQR-7 = D-9` — `fr-EquationView` **:191** *(fold list; its routed limb is `C·D-28`)* — `fr-FrequencyGraph` **:58** `FR-FG-20 = D:i-5` — `fr-FunctionInput` **:54** `N-1` · **:62** `L-m5` · **:81** `C §0 row 7` · **:82** `L-i1` — `fr-GalleryCard` **:87** `L·I-2-as-corrected / R5-7` — `fr-GalleryCardModal` **:93** `GCM-42 · L-14` — `fr-GalleryInfiniteGrid` **:82** `FR-GIG-20` · **:112** `MISS-5` — **`fr-GalleryInfiniteGrid MISS-5` restates `FR-GIG-20`; one home, no re-book** ⟨**fold relation disclosed explicitly at repair round 5, REST-70.** `MISS-5` is a canonical **`NWO→SS-5` head id** — `- **fr-GalleryInfiniteGrid** (2): \`FR-GIG-20\` · \`MISS-5\`` — and **a packet-homed head id disposed only as another id's restatement is a citation by demotion.** The relation is now stated as a relation rather than as a parenthetical, so the drain row carries a terminal verb over a named identity⟩ — `fr-GalleryView` **:79** `FR-GV-37 = L-20` — `fr-HarmonicLevelGrid` **:26** *(the STRUCK "unbanked" claim; banked identity `HLG-32 · MISSED-2`)* · **:88** `HLG-32 · MISSED-2` — `fr-InfoCard` **:47** `FR-IC-11` *(a five-id rollup line)* — `fr-MorphShapePreview` **:22** `FR-MSP-12` *(the admission)* · **:104** `FR-MSP-12` — `fr-NotationPills` **:35** `FR-NP-32` · **:52** `FR-NP-11` · **:67** `FR-NP-26` — `fr-PaperArticleWindow` **:88** `PAW-32` · **:94** `PAW-38` · **:95** `PAW-39` — `fr-PaperSearchDropdown` **:75** `L:L-16`.

**What the enumeration reveals, and it is the reverse of what round 2 assumed.** **TWENTY-SIX of the 32 already carry a PER-ID TERMINAL VERB elsewhere in this spec** — §2.2 (`GCM-42` · `C·D-28`), §2.5 (`FR-AFP-51` · `FR-AFP-52` · `MG-ι` · `fr-App i-2` · `fr-App C-12` · `FR-NP-32` · `PAW-32` · `PAW-38` · `PAW-39`), §2.5a (`M-16` · `L-m5` · `L-i1` · `L·I-2-as-corrected` · `FR-GIG-20` ⊕ its `MISS-5` restatement · `FR-GV-37` · `HLG-32 · MISSED-2` ⊕ its `:26` struck-claim row · `FR-MSP-12` ×2 · `FR-NP-11` · `FR-NP-26`), §2.5a-ii (`C §0 row 7` · `L:L-16`) — **cited here, re-booked nowhere**. **SIX carry the class verb**, and they are named rather than left inside a headline: `FR-COB-14` (fallthrough defeats every pinned attribute) · `FR-EQR-7 = D-9` (success glyph **2.101** vs the 3:1 floor; the design system's own light `--success` fails at **2.175** — the cure is the producer's) · `FR-FG-20 = D:i-5` (a *stronger* census statement than D's: **ZERO F.W1 break surface** in this file) · `N-1` (the `variant="default"` CTA demoted by the 3.1→4.0 bump; its NO-WAVE-OWNER limb is an explicit **rider**, the DESIGN.md correction) · `FR-IC-11` (a MINOR-latent inside a five-id rollup whose routings stand) · `fr-DarkModeToggle`'s **numbered lesson 4** (*"a reader return SHOULD carry a 'registry collisions checked' cell"* — **no banked row id exists to book**). **The observed shape of the weak class, stated as description and not as a re-definition of the predicate**: in every one of the 32, NO-WAVE-OWNER is a **rider or an in-line mention on a row whose terminal disposition is something else** — which is precisely why a class verb is lawful for them and was never lawful for the ten above.

**Class verb, one disposition** (unchanged in kind from R2-6b, now attached to a roster that reproduces): the six land as **CENSUS-INPUT (SS-3/SS-4 corpus-composition)** as a class; the twenty-six are **cited to their per-id verbs**; any row later re-graded to a strict disposition re-enters at its banked id through §2.5a-iii. **`MISSED-2` is RECORD-QUALIFIED and the qualification is itself a finding (R3-3.9)**: ⟨cmd⟩ `grep -ln 'MISSED-2' fr-*.md` → **three records** (`fr-ImageUpload`'s `R1-MISSED-2` · `fr-UserSlugBar`'s `r1-`/`r2-MISSED-2` · `fr-HarmonicLevelGrid`'s bare `MISSED-2`), of which only **fr-HarmonicLevelGrid's** carries a NO-WAVE-OWNER disposition — **and that identity was ALREADY booked strictly at §2.5a as `HLG-32 · MISSED-2`.** Round 2's bare `MISSED-2` was therefore a **duplicate of a row this file already homed**, invisible for exactly as long as the token stayed unqualified. The one-home/qualify-short-ids law this file applies to both `M-16` homonyms, to `i-5`, and to `i-1` now reaches `MISSED-2`, `D-i2` (seven records) and `m-4` (seventeen).

**The "twenty-five" headline is PASS-1's and is NOT adopted** — PASS-1 enumerated sixteen and wrote *"et al."*, leaving nine un-enumerated in the check file and nowhere else, which is the unfalsifiable residue R2-6b forbids. **No headline is adopted from anywhere**: the predicate above is mechanical, the roster above is exhaustive, and either one regenerates the other.

**Count reconciliation (R-7.3 · R2-6b · re-derived again at R3-10.6) — and it is now stated in the ONLY lawful form (R3-3.10: no ⟨cmd⟩ whose output is a COUNT of this file may be BANKED here).** The four section-extraction commands below are the **re-derivation INSTRUMENT**, not an operand: their outputs are readings **with a shelf life**, re-run by any seat at any time, and **no gate rests on the integers** (§2.8 **E-19** minutes the conversion). ⟨cmd⟩ `awk '/^### §2\.5 /,/^#### §2\.5a/' F-W10.md | grep -c '^| \*\*'` · `awk '/^#### §2\.5a /,/^##### §2\.5a-ii/' … ` · `awk '/^##### §2\.5a-ii/,/^##### §2\.5a-iii/' … ` · `awk '/^\*\*\(c\) THE FOURTEEN ROWS/,/^\*\*\(d\) THE WEAK CLASS/' … ` — **at this writing: 23 · 36 · 16 · 14.**

- **§2.5 = 23 rows / 23 distinct ids**, **FR-NP-32 among them as the head row** — it is *never added a second time*.
- **⊕ §2.5a's 36-row strict remainder = 59 distinct ids.** **This was R2-6b's count word, and "SIXTY" is STRUCK**: round 1 added FR-NP-32 twice — once inside §2.5's 23, once again as a separate conjunct — while §2.5a's own preamble said in the same breath that *"FR-NP-32 heads §2.5"*. A denominator this file declared binding on itself against itself was wrong by one, in the direction that flatters it (§2.8 **E-13**).
- **⊕ §2.5a-ii's 16 corpus-derived escapes = 75.** That was round 2's roster, **and 75 was a subset while being called the roster** — the failure §2.5a-iii convicts.
- **⊕ §2.5a-iii's 14 rows = 89 ROWS / 89 DISTINCT IDS / 89 TERMINAL VERBS** *(13 → 14 at round 4: the prose-strict `L-19+C-N1`; **88 is superseded and is named, not replaced**)* — the three strict escapes (`PP-CENSUS` · `PP-AGGLOM` · `PP-SEVLAW`) ⊕ the ten strict rows round 2 filed under a class verb. **Disjointness is by construction and is checkable per id**: none of the fourteen occurs in §2.5, §2.5a or §2.5a-ii — `fr-BasisCanvas L §R5-7` was *cited* at §2.5's FR-CP-43 and at §2.5a-ii's `i-2` and is **booked, once, only here**; `FR-TT-21/-22` are not `FR-TT-1`; `FR-USB-34/-37/-38` are not `FR-USB-16`; `PAW-56` is none of PAW-32/38/39/42/45/46; `m-4` is none of `m-14`/`m-18`; `D-i2` is fr-PaperSearchInput's, record-qualified against a seven-record homonym class.
- **Plus one class-level verb** over the weak class **enumerated whole at §2.5a-iii(d)** — 32 row-level lines / 19 records, of which 26 already carry per-id verbs elsewhere in this spec and **six** take the class verb.
- **Every superseded count word is named rather than replaced**: **23** (round 1's "owed") · **"SIXTY"** (round 1's double-count) · **59** (the pre-escape distinct-id figure) · **75** (round 2's roster, a subset). A later seat quoting any of them is quoting the roster as it stood before the round that fixed it.

The falsifier is not any of these integers — **a headline count is precisely what let rounds 1 AND 2 pass, in that order and for the same reason**. It is **every row of the reconciled roster bears a TERMINAL verb** (named wave / next-boundary ledger row — BH-RELAY or CENSUS-INPUT / kill-with-rationale / cite-consuming-wave / fold-with-target / excluded-with-reason), and no verb reads as a decision while being a deferral. COHESION §4 gains a fourier block carrying **this reconciled 89-row roster**, not the 23, not the 59, not the 75, and not round 3’s 88.

#### §2.5b THE CANONICAL RE-BASING — `CENSUS-CANONICAL.md` is the SOLE census operand, and F.W10's record-side roster is **ZERO** (repair round 4: R4-3 · R4-10 · R4-9.4)

**The operand moves once more, and this time it stops moving.** Four passes oscillated because each round re-invented the detector; round 4 ends it by stamping ONE census. **`docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` (BLESS seat, 2026-08-29, after the §0.3 hostile re-derivation; ROWS re-based 4278 → 4269) is this file's ONLY census operand, **sha256 `3e0a9acb3381` at repair round 5** ⟨*errata round 5, §0.4 — eighteen entries; F-side NWO packet population **60 → 65** (E5-13 · E5-15 · E5-17), and **CANON-1 as this file filed it is SUSTAINED**; the pin read `a450b8e9f80e` and is re-stamped at every citing site — REST-72*⟩** — *a gate that cites another census operand is DEFECTIVE on sight* (R4-3). ⊘ **C3's caveat stands against interest and is carried forward**: the arrow-clause class was found in a **five-record spot audit of sixty-six** and **is not proven confined to five** — §0.4's own "WHAT THIS ROUND DID NOT CURE" item 1 owes a corpus-wide re-derivation, **and that is a canonical act, never a wave's.** Two consequences land here immediately, and neither is a re-booking:

1. **`formation/fourier/CENSUS-2026-08-03.md` is DEMOTED to provenance.** It stays in §1b as a read-only bound and keeps its four citations in this file (the `F.W10` wave-sketch alias at §0, the §4.10/§4.11 shape statement at §2.1, the §4.5 vitest-floor DECISION, and E-4's frozen `:263`/`:310-316` addresses) — **every one of them a provenance or a decision cite, none of them a denominator.** No figure in this file is derived from it, and none may be.
2. **This wave's record-side roster is EMPTY, by the canonical's own enumeration** — not by this file's assertion. ⟨cmd⟩ this seat, 2026-08-29, `C='docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md'; grep -c '^### F\.W10' "$C"` → **0**: §2 carries per-wave rosters for F.W0 · F.W1 · F.W2 · F.W3 · F.W4 · F.W5 · F.W5-W8 · F.W9 and **no F.W10 header at all**, and §4.1 states it in terms — *"`F.W6`, `F.W8` and `F.W10` are likewise named by **no record** — their wave-side populations are folds, operands and gate machinery, never record routings."* **R4-10's duty row for this wave says the same thing**: *"(close wave; canonical carries no F.W10 record-side home — §4.1)."*

**What that means for the drain, stated precisely so no seat reads it as a demotion.** The drain **never was** a wave-side roster and is not converted into one here: its rows are **terminal dispositions over rows the canonical homes at a PACKET**, — **and at repair round 5 that claim became TRUE while the operand moved (REST-68; P5-3-adjacent / E5-17)** ⟨**it was false for five of §2.5a-iii(c)'s fourteen** (`PP-AGGLOM`/`PP-SEVLAW` → F.W4 · `FR-TT-22`/`FR-USB-38` → F.W2 · `D-i2` → F.W1). **Errata E5-17 (2026-08-29) made §0.1's arrow clause operative and re-derived all five to the packets their dispositions state**: `PP-SEVLAW` → **NWO (packet)** · `PP-AGGLOM` → **NWO→SS-3** · `FR-TT-22` → **NWO (packet)** (E5-13) · `FR-USB-38` → **NWO (packet)** · `D-i2` → **NWO→SS-3**. ⊘ **The spec was right at the record bytes and the census was wrong on all five; the amendment landed at errata round 5 and F-W10's drain verbs needed no change.** The drain's canonical operand rises **60 → 65**, and the five rows this file had already disposed correctly are now disposed against a census that agrees with them. **The disclosure §2.5b owed and did not carry is discharged by the amendment, not by prose.**⟩ — which is exactly R4-10's *"cites the canonical packet row (TERMINAL/UNROUTED/relay ids are NOT wave debts)."* The canonical's packet population for this class is enumerated at §2 and totalled at §3:

| canonical packet home | rows | this file |
|---|---|---|
| `NWO (packet)` | **32** ⟨*29 → 32 at errata **E5-13 · E5-17***⟩ | booked or cited below |
| `NWO→SS-3` | **27** ⟨*25 → 27 at errata **E5-17***⟩ | booked or cited below |
| `NWO→SS-13` | **3** | booked or cited below |
| `NWO→SS-5` | **3** | booked or cited below |
| **the drain's canonical operand** | **65** ⟨*60 → 65; re-extracted against `3e0a9acb3381` at repair round 5, REST-68*⟩ | **booked or cited · 0 escaped** (record-qualified probe at §2.5b, REST-65) |
| (§3 headline) rows *carrying* a NO-WAVE-OWNER token | **88** | the 60 above home at a packet; the remaining **28** carry the token as a **rider on a row the canonical homes at a wave or at TERMINAL**, and this file cites those to their holders — it books none of them |

**THE ESCAPE TEST, run against the canonical and not against this file's own arithmetic** (R4-10: *"An id absent from both booking and citation is that wave's escape"*). ⟨cmd⟩ this seat, 2026-08-29, `bash` + BSD `/usr/bin/grep`, the **65** ids extracted from `CENSUS-CANONICAL.md` §2's four NWO rosters (32 packet ⊕ 27 SS-3 ⊕ 3 SS-13 ⊕ 3 SS-5, post-E5-13/E5-15/E5-17) and each tested by a **record-qualified, boundary-anchored** probe against `F-W10.md` — `(^|[^A-Za-z0-9])ID([^A-Za-z0-9]|$)` **with the record's name required to co-occur on a matching line**: **52 record-qualified present · 13 not** — `fr-CanvasOverlayButton FR-COB-8 / -23 / -26` · `fr-CoefficientsPanel FR-CP-44 / -45` · `fr-EqCoefficientsPanel D/m-12` · **`fr-ExportModal L-16`** · `fr-GalleryDraftsSection m-18` · `fr-PaperArticleWindow PAW-42 / -45 / -46` · `fr-CanvasControlsDock M-10` · `fr-CollapsibleSection i-7`. ⌧ **THE ROUND-4 RESULT WAS AN ARTEFACT OF THE PROBE (re-cut at repair round 5, REST-65; P5-1, HIGH).** It published *"the 60 ids … each tested `grep -qF` … **60 present · 0 missing**"* — and ⟨cmd⟩ `/usr/bin/grep -cF 'ExportModal' waves/F-W10.md` → **0**: **that record is named nowhere in this file.** The boundary-aware probe on its id alone, `(^|[^A-Za-z0-9])L-16([^A-Za-z0-9]|$)`, returns **9 hits, every one another record's** (`fr-UserSlugBar FR-USB-38`'s alias · `fr-PaperSearchDropdown L:L-16`). ⊘ **The file's own convicted class, re-created inside the cure written to end it**: §2.5b's closing paragraph condemns *"a booking under a bare roster number"* one clause before warranting itself with a bare `grep -qF` on a four-character id. **Two required a record-qualification to be found at all, and finding them is a repair, not a formality**: round 3 labelled `fr-ImageUpload`'s two drain rows by their ROSTER NUMBERS — bare **`11`** and **`29`** — while the canonical's rule 1 heads them **`R2-missed-1`** (`NWO→SS-3`) and **`R1-MISSED-5`** (`NWO (packet)`). Both ids are now restored at §2.5a-ii **with the homonym guard the collision demands**: `fr-UserSlugBar` carries lowercase `r2-MISSED-1` / `r1-MISSED-5` as **ALIASES of `FR-USB-16`**, which §2.2 cuts to **F.W9** — a different record, a different home, never merged. *A bare roster number is not a banked id; a booking under one is invisible to every id-keyed gate in the programme, which is why this test found two rows four passes did not.*

**THE FABRICATION TEST, run in the other direction — and it convicts the ONE cell this file may not fix alone.** R4-10's converse clause: *"an id booked that the canonical does not home there is that wave's fabrication."* Since F.W10's canonical roster is empty, **every id-bearing cell of §2.2's re-cut table must be a citation, and TWO are not** ⟨**scope stated and the count corrected at repair round 5, REST-71 (P5-2).** The round-4 sentence was a **universal over the whole file measured over §2.2 alone** — *"a universal that was measured over one section is the 'erratum written to prove that checking happened' shape this file convicts three times at E-4."* The test's true scope is §2.2's re-cut table, and over it the failures are **`FR-TT-1`** (canonical **F.W4**) **and `PAW-50`** (canonical **F.W9**, REST-66)⟩: **§2.2's re-cut table cuts `FR-TT-1` to F.W10 ⟨re-cut⟩, while the canonical homes `fr-Tooltip FR-TT-1` at F.W4** (⟨cmd⟩ this seat, section-scoped so the output is the row and not a record-wide sweep: `awk '/^### F\.W4 —/,/^### F\.W5 —/' "$C" \| grep -F '**fr-Tooltip**'` → `- **fr-Tooltip** (1): ` + `` `FR-TT-1` `` — one line, the F.W4 roster's own). **The cell is inside the SHARED §2.2 SPLICE BYTES, so it is DISCLOSED and NOT APPLIED** — a unilateral edit there breaks G-F10-6 ≡ G-F9-21's Δ=∅, and the E-5 / E-20 idiom governs: it joins **AA-44 `9`→`8` as a PENDING PAIRED EDIT**, landing in F-W9 and F-W10 in ONE commit or not at all. ⌧ **DISCHARGED AT REPAIR ROUND 6, 2026-08-29 (PASS-6 D-9) — the paired edit was EXECUTED, not re-disclosed for a third round.** §2.2's cell now reads **`F.W4` ⟨CITED — the canonical's holder; NOT booked by either twin⟩**, written into `F-W9.md` and `F-W10.md` in ONE pass so the Δ=∅ leg of G-F9-21 ≡ G-F10-6 holds across the splice, with the rounds-4/5 cut carried inside the cell as dated provenance rather than erased (E-3). **The sentence above stands unrewritten as the record of what was found**; what moved is the cut, not the finding, and F.W10's fabrication count under R4-10 goes to **ZERO** on this axis. ⊘ **What did NOT discharge**: **AA-44's `9`→`8` correction is still the one OUTSTANDING paired edit** at this end, and the F.W9-roster reconciliation named in the next breath (26 cut identities against a 17-row canonical roster, the `C:D-11` / `L:L-i2` / `R2-7` divergences, and `PAW-49` / `PAW-12`) remains **F-W9's duty under R4-10**, unchanged by this discharge. **Two further paired divergences are disclosed in the same breath rather than discovered later**: the §2.2 table cuts **26 identities** while the canonical's **F.W9 roster is 17 rows**, and the two sets are not nested — the canonical homes `C:D-11` (fr-ContourPreview), `L:L-i2` (fr-ImageUpload) and `R2-7` (fr-PaperArticleWindow) at F.W9 where §2.2 books `38`, `27` and nothing; and §2.2's `PAW-49 → F.W0` and `PAW-12 → F.W9` sit against canonical **F.W3** for both. **The reconciliation is F-W9's duty under R4-10** (*"F-W9 | F.W9 → 17 | masthead re-based"*), executed as a paired edit at both ends; **F.W10's act is to state it, from this end, in terms, with the canonical figures measured** — which is the whole of what a terminal-record wave owes a divergence it cannot lawfully cure alone.

**Nothing above re-books, re-grades or renames an id.** The drain's verbs are unchanged; two ids are restored to their banked spellings; one cut is disclosed as pending-paired; the operand is named once and will not move again. **Two-key rule (R4-4): every ⟨cmd⟩ in this section was authored and run by this seat and is owed a re-run by a seat that did not author it.**

### §2.6 The LATEX-PAPER-RELAY roster (bounds #2; G-F10-5 — P-6's terminal home)

**LATEX-RELAY-HOME** (BLOCKER for the P-6 packet). **Witness re-measured at BOTH ends this seat, because the declared baseline moved under it** (§2.8 **E-14**): ⟨cmd⟩ `git show 54a97489:docs/tranches/V/coordination/INBOX.md | grep -c 'latex-paper'` → **0** at this file's declared baseline; ⟨cmd⟩ `grep -c 'latex-paper' docs/tranches/V/coordination/INBOX.md` → **1** at live HEAD (`5516359c` at this seat's re-measure; `281a420a` when round 2 first wrote this cell — **both live readings identical**) — and that single hit is **I-29's repo-list mention** (⟨cmd⟩ `grep -n 'latex-paper' …/INBOX.md` → the I-29 glass row), **not a latex-paper relay row**. The register-does-not-exist mechanism is therefore INTACT at both ends, and the number is stated at both rather than quoted from the one that flatters it; COHESION §4a is **glass-only**; ≥18 named latex-paper carries across three records route to a register that **does not exist** — and the producer tree has no coordination/inbox path either (§2.8 E-7), so the register lives **in our tree with its delivery point named**. F.W10 creates `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md`, rows every carry with banked ids kept and none re-booked, then **answers PAW-1's own F.W1 question IN THE LEDGER — latex-paper added to the tri-package transaction, or explicitly seated.** **ANCHOR LAW (SC-3), verified live**: installed latex-paper **0.2.1** ships no composable src (`src/vue/` holds `theme.css` only) — every virtualiser anchor cites `dist/vue.js` or the record; producer-src paths cite bytes the consumer never runs. The P-6 letters SEND at F.W0 on the same mail seat as P-1; F.W10 owns RECONCILIATION + TERMINAL DISPOSITION, declared from BOTH ends (COHESION §2).

| carry (banked ids) | content + locks |
|---|---|
| **PAW-1** (:42, BLOCKER sustained R-2 on seven arms) | all **42** `hsl(var(--x))` declarations IACVT-dropped; seat-verified **VERSION-INVARIANT at 4.0.0/7.0.0/8.0.0 — the tri-package uplift cures nothing here**. Carry: rewrite the 42 sites to `var(--x)` — the SOUND limb. **KILLED (K-9): the `:root` triplet-alias limb — the repair MUST NOT implement the literal snippet** (breaks PaperView :524, style.css :110-112, :182/:184, glass recipes). **F.W1 LEDGER RIDER VERBATIM: "the transaction as chartered is INSUFFICIENT for the paper route — latex-paper is a FOURTH package the constellation must reconcile — add the LATEX-PAPER carry to the transaction or seat it explicitly."** **PAW-30 lands in the SAME REPAIR**; **LAW-3**: PAW-44's `overflow-x: clip` restoration lands WITH-or-AFTER PAW-1 + PAW-30, NEVER before (else the K-18-killed see-through-header catastrophe becomes REAL). E-3 re-scope stands: sticky-scroll arm K-18-dead; theme :447/:459 re-filed LATENT-behind-PAW-44. Dissent: R-2 killed the blocker's most vivid arm; severity sustained on the seven survivors — **a repair reasoning from the killed arm ships a no-op** |
| **PAW-2** (:48, MAJOR, demoted BLOCKER→MAJOR at R-1; absorbs D-i-5) | never-painted `.deferred-section` wrappers report the 1200px estimate; `syncMeasuredHeight` writes it ungated into the never-invalidated `SESSION_HEIGHT_CACHE`. Cure: gate on the CV-state event, or skip when `offsetHeight` equals the estimate, or move `.deferred-section` off the measured element. **K-19: the identity guard at dist :869 is the EXACT repair line.** K-16: co-landing rhythm cures MEASUREMENT-SAFE only (inter-wrapper margin → ≈2.9k px drift). **COUPLING LAW VERBATIM: "the per-patch ref re-read is currently the only repair mechanism — fix this BEFORE memoising the ref (PAW-4)."** Dissent: D/C filed BLOCKER; demotion sustained by fresh derivation (K-6: navigation IS compensated; the un-compensated residue carries the MAJOR) |
| **PAW-11** (:57; premise corrected K-4) | `\ref`/`\eqref` render as plain text AND are href-less `<a>` (**5 emitters, NOT spans**) — invisible-but-live, unreachable to keyboard/AT. Visuals ride PAW-1; **the focusability arm needs real `href`/tabindex semantics AT THE EMITTER — cannot be shimmed consumer-side.** Rider on PAW-1's letter |
| **PAW-28 ⊕ PAW-45 ⊕ PAW-46** (≡ PaperView L/D21) (:79/:195/:196 · fr-PaperView:130 — three module-scoped-cache rows, **ONE letter**) | PAW-28: instance-scope or namespace the height cache (deliberately excluded from teardown; dissent: DU graded INFO, ruled MINOR at R-6). PAW-45: **bound or instance-scope the KaTeX memo — CONSTRAINT TRAVELS WITH THE ASK: superlative 13 records the memo makes PAW-3/PAW-4 churn survivable — CAP, DON'T DELETE.** PAW-46: key on tex + macro identity, or move the Map inside `useKatex`. **PAW-46 ≡ L/D21 — ONE mechanism banked twice; carried ONCE, both ids cited, NEITHER re-booked**; L/D21's surviving increment: the docblock promises an `inject` fallback the code lacks. 45/46 LATENT (one `useKatex` call site today, PaperView :38) |
| **PAW-32** (:88) | emit the slot types (`__VLS_Slots`). P-6 member; NO-WAVE-OWNER locally |
| **PAW-38** (:94) | uniquify slugs at the emitter. Joint P-6/P-12 row |
| **PAW-39** (:95; census seat-corrected SC-2) | read `sourceLevel`; restore the second outline level. Census law: **quote 31, never 35** |
| **K-11-FLATTENER-RIDER** (:115, re-graded K-18 :206; MAJOR cure-integrity, not a defect row) | flat-peers nesting is the **SECOND** of two gates on the two-tier sticky stack; **THE SCROLLPORT IS THE FIRST (PAW-44); reworking the flattener alone ships a no-op.** Whether the stack returns is a formation design decision — SS-13 witnesses first. Bound to LAW-3 and **LAW-4 (ONE CLEARANCE AUTHORITY)**: PAW-47's clearance constant is a DEPENDENT of the PAW-44 decision (stack restored → clearance ≥ stack height; retired → clear the 32px scrim + record theme's three dead `scroll-margin-top` rules retired-with-rationale). K-18 re-grades banked K-11: a repair reading the banked cell alone ships a no-op |
| **C-06** (fr-PaperView:70, MAJOR) | 0.2.1 ships `activeId`/`activeRootId` as `ComputedRef<any>` (d.ts:19) — six typed contracts vacuous, invisible to `vue-tsc -b`. Relay: fix at `ref<T\|null>`. **+ F.W1 rider** — why PAW-1's fourth-package ask is not optional |
| **L/D5 + L/D30** (fr-PaperView:75, MAJOR) | two GUARANTEED no-op teardowns (`onUnmounted` re-reads a nulled template ref). Consumer half → F.W4; **producer twin RELAYED WITH the consumer half so neither re-introduces the other.** Cure = `onBeforeUnmount` (LC's one-word fix, canonical) or the cached-element idiom (ships in the same package at S9) |
| **L/D8** (fr-PaperView:77, MAJOR) | `:ref` re-invoked per patch; `el.offsetHeight` evaluated AS THE ARGUMENT, before the dedupe — one forced layout per visible section per patch; the guarded twin sits 300 lines away. Consumer guard → F.W4; relay: hoist the read behind the guard at the producer. **SAME FAMILY as PAW-4(b) — cite both, book once.** PAW-2's coupling law binds |
| **L/D29** (fr-PaperView:92, MINOR) | `useTreeIndex` records the ROOT id as `parentId` for depth≥2 (:27/:32; masked by `isDescendant`, 34 nodes live) — latent, correctly filed; the same package's other walk gets it right |
| **C-19 ⊕ PAW-33** (fr-PaperView:125 · fr-PaperArticleWindow:89 — **ONE fact, two banked ids**) | declared peers silently violated at the installed pin: katex `^0.16` vs **0.17.0**; vite `^6\|\|^7` vs **8.0.16**; all `optional: true`, zero npm signal. → **F.W1** (pin-truth) + relay; feeds G33's pin leg. **BOTH CITED, NEITHER RE-BOOKED** (PAW-33 carries the vite half C-19 omits). **DISTINCT from the glass/keyframes/value peer deadlock — never folded.** Companion ★MF-12 (dual katex ambient type sources, skipLibCheck masking) → F.W1's manifest pass |
| **C-20** (fr-PaperView:126; headroom cell excised K-6) | `hsl(var(--section-heading))` IACVT at SIX theme sites — **a NAMED SUBSET of PAW-1's 42; discharges INSIDE PAW-1's carry, carried separately ONLY so the 6-site cell survives the 42-count quote.** Consumer sibling (PaperSidebar L-10 ≡ ★MF-10): fallback-less `var(--section-color-${si})`; the one-line cure `var(--section-color-${si % 13}, var(--foreground))` is **F.W4's**, with root-count assertion — the ramp is EXACTLY saturated 13/13. GLASS-RELAY rider only if MORE stops are wanted upstream. Dissent: K-6 killed both headroom cells (ZERO headroom; stop 5 already app-forked) |
| **L-11 + M2** (fr-PaperSidebar:40/:62 — **ONE relay carries both**) | L-11: add a PRM gate/motion option to the damped-rAF follow (a THIRD ungated clock — **amend lane-frontend §8's inventory**). M2 (MAJOR): replace the hard-coded `.sidebar-top-btn` in the producer's pointer-exemption with a **DATA-ATTRIBUTE hook**. **HARD LOCK VERBATIM: "F.W4 must not rename the class before the relay lands"** — renaming silently converts scroll-to-top clicks into sticky manual-override suspensions |

### §2.7 Carried for parity — NOT booked by F.W10 (cited so no seat re-books them)

| row | where booked | why carried here |
|---|---|---|
| **FR-GIG-5** (fr-GalleryInfiniteGrid:47, MAJOR, cure-integrity; re-proven at the tag R-2) | **F.W1's spec (the lock) + SS-6 (the producer ask)** | the tri-package bump does **NOT** cure the pagination drain (observer-root geometry byte-identical 4.0.0→7.0.0→8.0.0; a non-clipped element root always delivers an initial `isIntersecting: true`). **AN EXPLICIT F.W1 SPEC FOOTNOTE IS MANDATORY: the uplift must NOT be credited with curing the drain.** F.W10 verifies the footnote exists and the relay left (G-F10-12). Dissent: three BLOCKER dissents ride parent FR-GIG-1; the L axis's carry was the wrong one, killed at K-4 |
| **FR-EQR-3** (fr-EquationResult:38, MAJOR sequencing; folds onto AA-2, not a re-book) | **F.W1's spec** | the producer's DEV-mode retired-prop `console.error` meets four zero-console-error e2e gates on a DEV-server matrix. ▲ **F.W1 is ONE atomic land-or-lose transaction — the TWELVE-limb roster chartered at F-W1 §4 step 4, cited whole, never restated here.** Quoted by command, never re-derived (R2-5 · R2-1-LAW.1) — ⟨cmd⟩ `grep -n "The roster is" waves/F-W1.md` (this seat): *"The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a, cross-edge 1 at this writing) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading."* **This file re-derives nothing and adds nothing**; "ELEVEN-limb" is a superseded count word here and is struck (§2.8 **E-15**). Witness for F.W1's spec: one dev-server run of the four gated specs at the adopted pin, before-and-with the rewrite. **F.W10 schedules NOTHING that presumes the uplifted tree.** Interacts with GCM-42 |
| **PAW-49** ⟨re-cut → F.W0⟩ | F.W0 | the one coverage-family row whose gate is FREE; flag-before-split governs PAW-12 |
| **PAW-50's DECISION leg** ⟨re-cut → F.W4⟩ | F.W4 (census §4.5) | **F.W10 cannot book a coverage row terminal while the harness decision is unmade** |
| The 23 F.W9-cut execution limbs (§2.2) | F.W9-widened / F.W9a | F.W10 owns **only** the terminal record; the same §2.2 table sits in both specs |

### §2.8 Measured corrections and errata (E-3 shape — recorded BESIDE; dated bytes never rewritten; every anchor re-resolved per FR-CP-45's law, and **converted to STABLE anchors per R2-2** — a coordinate re-resolved inside a round that moves coordinates is a measurement with a shelf life, which is the lesson E-11 pays for)

| # | finding (re-measured by this seat, 2026-08-28) | disposition |
|---|---|---|
| **E-1** (**denominator CORRECTED at the bytes — the earlier "11 id-rows" understated the live table by six**) | Re-enumerated row-by-row 2026-08-28 on `fourier-analysis/docs/constellation/ADOPTION-ASKS.md` (header `:107`, G.W8 re-trigger stamp `:105`, rows `:109`–`:125`): the live table holds **SEVENTEEN id-rows** — `1 · 2 · 3 · 4 · 5 · 6 · 7 · inv-22-color · glass-ui-a11y · cascade-vjs · cascade-kf · cascade-gui · words-spa · valuejs-J-atomdiff · valuejs-J-publish · ~~glass-ui-P5-inner-rounding~~ · glass-ui-dock-vt-name`. **TWELVE are OPEN** *(corrected 2026-08-29 under R4-7.3 — the round-3 cell read ELEVEN and was under by one **in the direction that shrinks the open set**, the exact shape E-13 convicts, re-created inside the erratum that corrected the previous denominator; **the LIST is authoritative and the prose follows it**)*: ⟨cmd⟩ this seat, `FA='/Users/mkbabb/Programming/fourier-analysis'; sed -n '109,125p' "$FA"/docs/constellation/ADOPTION-ASKS.md \| grep -c 'OPEN'` → **12**, against ⟨cmd⟩ `… \| grep -c .` → **17** rows (every line of the `:109`–`:125` span IS a table row, so a pipe-free counter is exact and survives quotation inside this cell). The twelfth is **Ask 4**, whose own stale-watch cell carries an OPEN leg round 3 quoted only the landed half of — verbatim from `:112`: *"**fourier portion LANDED G.W7** (backend full floor live; others no-new-priv + booked); 3 external repos OPEN — re-affirmed G.W8"*. **FIVE are not OPEN**, enumerated: glass-ui-a11y (*SATISFIED-UPSTREAM*) · glass-ui-dock-vt-name (*SATISFIED-UPSTREAM*) · valuejs-J-atomdiff (*DONE-in-sibling*) · valuejs-J-publish (*DONE-in-sibling*) · glass-ui-P5-inner-rounding (*KILLED-AS-PHANTOM*) — and `12 + 5 = 17`, which the ELEVEN/six reading could not close (it summed to 18 by holding Ask 4 in both sets). **Nothing downstream moves**: Ask 4 targets words · speedtest · csp-solver and **never value.js**, so the value-targeted leg is unaffected. The table targets **value.js on SIX rows**, of which **FOUR are OPEN** — Ask 3 (:111, P1) · Ask 5 (:113, P3) · inv-22-color (:116, P3) · cascade-vjs (:118, P2); the two extra are the J-parity residuals §4b carries as fourier-owned. `cascade-gui` (:120) **targets glass-ui** and is never counted as ours | **Quote 17 / 12 OPEN / 5 not-OPEN / 6 value.js-targeted (4 OPEN)** *(round-4 correction, R4-7.3)*. **Two superseded figures are named rather than replaced**: the round-1 cell's "11 id-rows" (the table size) and the round-3 cell's "11 OPEN" (the open set). A denominator this file declares binding on itself was wrong **twice, in its own favour, in two consecutive rounds** — recorded so the pattern is legible, not smoothed. This file adopts **FR-CP-45's anchor law as binding on its own citations** — the axis it made load-bearing against itself is the axis it was wrong on, and the correction lands as an erratum, never a rewrite |
| **E-2** (**material erratum, TWICE corrected — the sweep was reading a superseded inbox path**) | Banked: *"eleven arms (U-1..U-11) … each ZERO by term-sweep across all six sent letters."* First correction: **TEN** `valuejs-outbound-*.md` packets under `BJ/coordination/`; `valuejs-outbound-2026-07-28-o17-full-debt-discharge.md` (76 lines) rows **B-1..B-10 naming all eleven arms** (B-1 = U-1+U-5 convergent) — the prescribed O-10a-style amendment, delivered. **Second correction (2026-08-28, this seat): `find glass-ui/docs -name 'valuejs-outbound-*.md' \| wc -l` → ELEVEN.** The eleventh is `glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md` (59 lines, dispatched per **COHESION §4a's `DISPATCHED 2026-08-28` O-20 row**) — **the correspondence moved tranche BJ→BK and the sweep did not follow.** Its row **A-1** is the corrupt-published-stylesheet ask, verbatim `FR-NP-32 ≡ fr-PaperSidebar M1`; `grep -c '@source'` → **1**, `grep -c 'FR-NP-32'` → **1**, `grep -c 'fourier'` → **10** | **The DEBT is DISCHARGED; the LAW is NOT.** G-F10-12 stays born **SPLIT**, with the F-half re-measured across **eleven** packets: **`@source` = 1 ⇒ the M1/FR-NP-32 arm is GREEN-BY-VERIFIED-RECEIPT** (its packet exists under the producer's inbox path and contains the arm's identifying term — the gate's own GREEN condition, previously asserted RED); `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0** ⇒ those three arms stay RED. **This is the spec's own RELAY-RECEIPT-LAW running backwards — *relay-sent-never-read* — and the cure is the same law applied to the reader: the bound is the recipient's LIVE inbox path (§1b now names BK), never last tranche's.** The mechanism family still enters DISEASE-REGISTRY, with `relay-sent-never-read` recorded beside `relay-declared-never-sent` |
| **E-3** (twin exists; bounds direction settled) | The 2026-08-25 fold's G-F10-6 witness read *"`F-W9.md` does not exist yet."* It **EXISTS** (fold-authored 2026-08-28), carries §2.2 **spliced verbatim**, and its §1a row 2 takes **MODIFY** on the census addendum, stating "F-W10 §1 already claims the CREATE" | G-F10-6 gains a runnable falsifier leg: the extracted §2.2 shared regions of the two files **`diff` empty**. The Opus draft's bounds flip (F.W10 MODIFY) is **REJECTED against the twin's measured bytes** — the CREATE stays at §1a #8. **PAIRED SPLICE — PERFORMED, and round 1's DIRECTION STRUCK (R2-6a, repair round 2, 2026-08-28)**: R-2b's direction was and remains **F.W9 books (home/author); F.W10 receives the IDENTICAL bytes by paired splice** — the checkpoint set is `carry/F-W1-CARRY.md` **§CrossEdges item 9, the `F.W1 → F.W9 / F.W10` row** (by path and row label, R-3 · stable idiom R2-2; the round-1 `:246` line cite into a live carry is retired with it). Round 1 **inverted** this: it authored a rival prose paragraph of its own and narrated the outstanding act as *"the twin's identical splice"* — so a reader following that E-3 would have **overwritten F-W9's ruled-in 17-line table with this file's paragraph**. The rival paragraph is **DELETED**; F-W9's bytes are **received, spliced, never retyped**; and the receipt is measured, not promised: ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty** (this seat). **G-F10-6 ≡ G-F9-21's `diff`-empty leg is GREEN AT THE BYTES today**; the gate stays RED on its corpus leg, and no seat may read the green leg as the gate. The paired-edit law is unchanged and now has teeth in both directions: **any change to the shared table, entrant block, or checkpoint block lands in BOTH specs in ONE commit** — never a unilateral fix here (E-5's AA-44 `9`→`8` is exactly such a pending pair and is deliberately left unapplied), never a retype. Disclosed outside the shared bytes, as E-5 requires of any paired defect |
| **E-4** (anchor corrections — **DISCHARGED at repair round 3, R3-10.2; the row's own disposition was the last false one in this table**) | The **dated, frozen** documents keep their addresses, which is lawful under R2-2 clause 2(i) and re-verified this seat: `CENSUS-2026-08-03.md:263` is `## §6 — THE ADMISSION GAP` (there is no §6.6); the §6.7 CORRECTION block at **:310-316** reads *"Zero UNVERIFIED credit — every Codex verdict enters the formation through per-claim adjudication (M-21)"* (⟨cmd⟩ `grep -ho 'Zero UNVERIFIED credit[^.]*' CENSUS-2026-08-03.md`, word-output); `INTAKE-ADJUDICATION-2026-08-03.md:221` is `### NEXT-COMMUNIQUE`. **THE TWO `X/waves/W9.md` ADDRESSES ARE ABOLISHED, NOT RE-RESOLVED.** Round 2 published *"G24 at `:366` / G33 at `:375` re-resolve exactly"* under a disposition reading *"Corrected anchors used throughout"* — while **E-17, four rows below in this same table, listed those exact cites among the breaches it had CURED.** Two rows of one errata table contradicting each other about the same two addresses is worse than either being wrong alone: it makes the table unreadable as a record | **CONVERTED to the stable idiom**: **X·W9's gate table, the G24 row** and **the G33 row**, by gate id — cited that way at §2.4, at G-F10-3 and at G-F10-4 already, and now here, so the idiom is universal inside this file rather than universal-except-in-its-own-errata. The identifying bytes travel instead of the coordinate (⟨cmd⟩ this seat, prefix-free word-output, the E-11 idiom, and deliberately **pipe-free so the probe survives quotation inside this table**) — **and the variables are now DEFINED IN THE SAME PUBLISHED BLOCK per R4-2.3, which round 3 did not do: `$V` and `$F` were phantom witnesses here, the identical breach R4-2's table books against F-W2's `$V` and F-W5's purge-minted `$R`.** Declared and re-run 2026-08-29 on the pinned toolchain: `V='/Users/mkbabb/Programming/value.js'` · `F='/Users/mkbabb/Programming/fourier-analysis'` · `grep -ho 'Restored analytic in/out arms match 0\.13\.0' "$V"/docs/tranches/X/waves/W9.md` → *"Restored analytic in/out arms match 0.13.0"* — the G24 row's own claim cell · `grep -ho 'Packets sent, exact-pin consumers notified' "$V"/docs/tranches/X/waves/W9.md` → *"Packets sent, exact-pin consumers notified"* — the G33 row's ⟨**PURGE-SEAT ROOTING, R3-9.5, COMPLETED AT R4-2.3.** Both receipts were banked against a bare `W9.md`, which **resolves from no declared base** and is ambiguous besides — three `W9.md` files exist across the two trees (`"$V"/docs/tranches/X/waves/` · `"$V"/docs/tranches/V/archive/waves/` · `"$F"/docs/tranches/D/waves/`; ⟨cmd⟩ `ls "$V"/docs/tranches/X/waves/W9.md "$V"/docs/tranches/V/archive/waves/W9.md "$F"/docs/tranches/D/waves/W9.md \| wc -l` → **3**) and **only the first carries either phrase**. Rooted to a DEFINED `$V` and re-run: both reproduce EXACT.⟩ **E-4 and E-17 no longer contradict**: E-17's claim was true of the file's citations and false of its own errata table, and this row is where that gap closes. **The shape is named because this file has now convicted it three times** — E-3 (an erratum announcing a retirement the file had not performed), E-11's second minute (a provenance note re-importing the abolished address), and E-4 here: *an erratum is a claim like any other, and the claims most likely to go unchecked are the ones written to prove that checking happened* |
| **E-5** (**PAIRED defect inside the shared §2.2 bytes**) | The AA-44 cell reads *"9 e2e never enter the audit tab"* while the FR-AFP-42/-49 cell two rows below states **"Quote 8 specs, never 9 (K11)"**. Re-measured at fourier HEAD: `ls web/e2e/*.spec.ts` → **8** | The correction is a **PAIRED edit landing in BOTH specs in ONE commit** — a unilateral fix breaks G-F10-6/G-F9-21's Δ=∅. Until it lands, both specs carry the byte-identical table **including the defect**; this row is the disclosure (mirrored at F-W9 §2.8 E-1). **No prose outside the shared bytes quotes 9** |
| **E-6** (count drift, mechanism intact — **both drafts corrected**) | The 2026-08-25 intake roster said *"Eleven `FOURIER-R*` coordinate files"* (historical note, R-3 — no carry file backs it); the Opus draft says 10 incl. an `R2A`. Measured by the literal glob: **9** (R3 ×2 · R4 ×3 · R5 ×2 · R6 ×2) in `megatranche/coordination/`, **no R2A**, and the auxiliary eight-hour delta file does not match the glob | **Quote 9 by the glob.** The load-bearing fact — **none in fourier's tree** (re-measured: 0) — is unchanged |
| **E-7** (SC-3 verified live; the producer end has no inbox either) | Installed latex-paper **0.2.1**: `src/vue/` contains `theme.css` **only**. `/Users/mkbabb/Programming/latex-paper/docs/` holds `tranches/` + `virtual-paper.md` — **no coordination/ or inbox path anywhere** | The relay register lives **in our tree** (bounds #2) **with its delivery point named** — placing a packet where the recipient does not look is the recorded failure mode (the O-15 lesson) |
| **E-8** (E13 status — worse than unread; **witness re-measured at BOTH ends, E-14**) | `grep -c 'UNREAD' INBOX.md` → **4** at the declared baseline `54a97489`, **5** at live HEAD (`5516359c` at this seat's re-measure; `281a420a` when round 2 first wrote this cell — **both live readings identical**) — **all law-text in both readings; zero rows carry status UNREAD**, which is the load-bearing fact and is invariant across the drift. `grep -ni fourier INBOX.md` → **6 rows** (I-14, O-14, O-10a, I-24, I-24a, I-25) at both ends, none of the four asks, none inbound | The four ADOPTION-ASKS are not unread — they are **UNROWED**, invisible to the E13 sweep. G-F10-1/G-F10-2's falsifiers therefore require **rows**, not reads. **The count moved and the mechanism did not** — stated at both ends rather than quoted from the end that flatters the file (E-14) |
| **E-9** (Codex residue re-confirmed) | `/tmp/fourier-r4-files.sha256` → *No such file or directory* (**EVAPORATED**); `~/.codex/worktrees/d0be/fourier-analysis` → **still registered** | R4-12's consume-nothing lock stands and is moot; the worktree still owes a disposition verb (G-F10-8) |
| **E-10** (the §2.2 head sentence's own scope — round 1's over-reach, minuted where it was made) | Round 1 labelled the §2.2 **preamble** as spliced. It is not and never was: the head sentence is **seat-local** on both sides (this file's points at E-3/E-5, the twin's at its own §2.8 E-7), and the heading is deliberately **mirrored**, not identical (`identical in F-W9.md` ↔ `identical in F-W10.md`). ⟨cmd⟩ `diff <(awk '/^### §2\.2/,/^### §2\.3/' F-W9.md) <(awk '/^### §2\.2/,/^### §2\.3/' F-W10.md)` (this seat) returns **exactly three hunks — the mirrored heading, the two seat-local head sentences, and the following §2.3 heading — and NOTHING inside the table, the entrant block, or the checkpoint block** | **The SHARED BYTES are the 26-identity table ⊕ the by-mechanism entrant block ⊕ the R-2b checkpoint block — and nothing else.** A "verbatim" label that reaches past them is a defect per se (R2-1-LAW.2), which is why the scope is stated in the head sentence itself and re-proved by command here rather than asserted. **Every claim of byte-identity in this file names the command that produced it** |
| **E-11** (**MAJOR, cured — the eight F-W0 line cites, and the idiom that made them possible**) | This file cited `F-W0.md:199` ×4, `:204` ×2, `:209` ×2. **The same repair round amended F-W0 and shifted its gates uniformly +12** (G-11 `:199`→`:211` · G-12 `:204`→`:216` · G-13 `:209`→`:221`), so against the worktree this file's masthead names, `:199` addressed a **blank line**, `:204` a *different gate's* "Owning rows" cell (G-9's) and `:209` another (G-10's) — while §4b declared *"divergence from that table is a defect against G-11"* and §2.8's own header declared *"every anchor re-resolved"*. **Both files were simultaneously dirty; a per-coordinate re-resolution could not have survived the round.** ⟨cmd⟩ `grep -h '^### G-1[123] — ' waves/F-W0.md` (this seat; **`-h` — the line prefix is suppressed ON PURPOSE, for the reason in the disposition cell**) → `### G-11 — ONE corrected anchor table published; every later wave quotes it` · `### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream` · `### G-13 — The producer pin is re-derived at the ADOPTED commit; no F.W1 sizing quotes a 7.0.0 anchor` | **ALL EIGHT CONVERTED to the stable idiom (R2-2): `F-W0 §4 gate G-11 / G-12 / G-13`, by gate id, never by line** — at §1b, at §2.5a (FR-EQC-15 · D/m-12 · M-16 · CITE) and at §4b's F.W0 row. The cure is **per-IDIOM, not per-coordinate**: a line number into any live sibling is a defect from R2-2 forward, and this file writes new citations in the stable idiom from the first byte. **The two G-12 denominators this file quotes were never at risk and are unchanged** — they enter by command, not by address (`grep -o 'shim \*\*21 cartoon-card sites / 14 files\*\*'` and `grep -o '\`text-admin-label\` \*\*7 / 4 files\*\*'`, both re-run this seat and both reproducing), which is precisely why quote-by-command outlives anchor-by-line. **SECOND MINUTE, and the sharpest evidence this row can carry — the cure's own receipt staled INSIDE the round it cured.** The seat that first wrote this cell pasted the gate-heading command's output *with* its `-n` prefixes — `215:` · `220:` · `225:`. This seat re-ran the identical command against the live bytes and got `252:` · `257:` · `262:`: **F-W0 moved a third time, under a citation minted expressly to prove that citations must not be addresses.** Nothing about the three gates changed; only their coordinates did, twice, in one repair round. The receipt is therefore re-pasted **prefix-free (`-h`)**: a `⟨cmd⟩` note discharges R2-1-LAW by carrying the *bytes that identify the thing* — here the gate HEADINGS, which are the citable identities R2-2 names — and a pasted coordinate is a measurement with a shelf life even when it is pasted honestly. **A provenance note must not smuggle back in the address the ruling abolished** |
| **E-12** (**MAJOR, cured at the operand — the drain's root cause, named in the record that names it**) | PASS-1 convicted a drain *"complete-looking because it counted only what it chose to look at."* Round 1 cured it **by taking the check file's roster** — complying with the ruling's letter and **inheriting its blind spot whole**, so the same mechanism survived its own cure with a larger number attached (36 instead of 23), and §5 went on asserting *"zero adjudicated registry rows among them"* while **16 strict rows escaped**. The corpus itself names this failure: `fr-ImageUpload:45`'s **row 11**, *"The corpus never swept the adjudicated registry … REGISTRY-FIRST is a precondition, not a courtesy"* — one of the sixteen that escaped | **R2-6b re-rules the OPERAND, and this file adopts it at every level**: the roster is derived **FROM THE 66 RECORDS**, never from any check file; the masthead, §1b, G-F10-7 and §4b's X-whole row all now say so; the sixteen escapes are homed **by id with terminal verbs** at §2.5a-ii; the loose class is redefined **by predicate** so no headline is inherited either. **Row 11 is booked as this drain's own falsifier** (§2.5a-ii) — the row that describes the defect is the row the defect hid |
| **E-13** (count word — the reconciliation was wrong by one, in its own favour) | §2.5a reconciled *"23 rows from §2.5 (23 ids) ⊕ FR-NP-32 ⊕ the 36-row strict remainder = **SIXTY**"* while its own preamble stated *"FR-NP-32 heads §2.5"* — **FR-NP-32 added twice**, and §4a.13 declared the figure *"binding on this file against itself"*. Row counts re-measured this seat by section-extraction (the three `awk … \| grep -c '^\| \*\*'` commands pasted at §2.5a's reconciliation): §2.5 → **23** · §2.5a first table → **36** · §2.5a-ii → **16** | **"SIXTY" is STRUCK; the count word is 59** (R2-6b) — 23 ⊕ 36, FR-NP-32 counted once. **And 59 is not the roster**: with §2.5a-ii's sixteen the drain homes **75 rows / 75 terminal verbs**. Both integers are stated with their arithmetic at §2.5a and at G-F10-7 so neither can be quoted as the other; **the falsifier remains the per-id enumeration, never the headline** |
| **E-14** (baseline drift, stated at both ends — recorded, not smoothed) | This file declares its witness baseline `54a97489`. HEAD has moved twice since — `281a420a` when round 2 first wrote this row, **`5516359c` at this seat's re-measure** — and two INBOX witnesses moved with the first of those: `grep -c 'latex-paper'` **0 → 1**, `grep -c 'UNREAD'` **4 → 5**. All four readings re-run this seat and reproduce **unchanged at both live commits and at the declared baseline** (⟨cmd⟩ `git show 54a97489:docs/tranches/V/coordination/INBOX.md \| grep -c 'latex-paper'` → **0** · `… \| grep -c 'UNREAD'` → **4**; ⟨cmd⟩ `grep -c 'latex-paper' docs/tranches/V/coordination/INBOX.md` → **1** · `grep -c 'UNREAD' docs/tranches/V/coordination/INBOX.md` → **5**; ⟨cmd⟩ `grep -ic fourier docs/tranches/V/coordination/INBOX.md` → **6** at both ends ⟨**PURGE-SEAT COMPLETION, R3-9**: two operands were `…`-elided and the third receipt printed `-nic` where the pasted output is a bare count (`-n` is inert under `-c`); all four readings written out literally and **re-run over the settled bytes — 0 · 4 · 1 · 5 · 6 · 6, every one EXACT**.⟩). The new `latex-paper` hit is **I-29's repo-list mention**, not a relay row, and the new UNREAD is law-text like the other four | **Every affected witness now carries BOTH readings** (§2.6 · G-F10-5 · E-8). §4a.12's MEASURE-AT-OPEN already governs, and this row is what it looks like when honoured: a spec whose baseline moves states the drift **in the witness cell**, never re-baselines silently and never quotes the reading that flatters it. **The mechanisms — no latex register, four asks UNROWED — are intact at both ends**, which is the claim, and the numbers are the evidence, which is why they are given twice |
| **E-15** (the F.W1 roster count word — corrected at its charter, re-quoted here, never re-derived) | This file twice cited *"the ELEVEN-limb roster"*. R2-5 re-derived the charter at the bytes — the top-level `+` separators mark **TWELVE** conjuncts; PASS-1's "eleven" was reached by fusing the last two, and R-4a had already returned the FR-EQC-7 vaul-vue manifest gate **INSIDE** the transaction. F-W1 carries the correction ONCE, at its own §4 step 4 | **Both sites re-quote the corrected charter BY COMMAND** (§2.7 FR-EQR-3 · §4a.3) — ⟨cmd⟩ `grep -n "The roster is" waves/F-W1.md` — in the stable idiom *"the TWELVE-limb roster chartered at F-W1 §4 step 4"*. **No sibling re-derives the count; every sibling quotes the charter** (R2-5). The atomicity law is unchanged and is the reason the count matters: a transaction cited at the wrong extent is a transaction a later gate can pass while a limb lands separately |
| **E-16** (**the un-minuted drift into THIS file — R-1b's dangling end, closed; R2-2.3 / R2-10.7**) | PASS-1's R-1b directed F-W7 to cite the SS-4 trie dissent as *"`F-W5.md:153` (the banked home) ≡ `F-W10.md:114`"*. Round 1's repair **moved this file's own rows and never minuted it**: `:114` became the CODEX-R3–R6 residue row and the dissent slid to `:116`; F-W5's §4 `F.W5 → F.W7` edge row now cites that `:116`, and round 2's repair has moved it again — the dissent lives at **`:133` at this writing**. A repair seat that only fixes its outbound citations exports drift it never minutes | **The address is ABOLISHED, not chased.** The dissent's stable identity is **F-W10 §2.3, the SS-4-PREREQ row** ≡ **F-W5 §2 clause E16 ▲** — cited by row label and clause id at both ends, never by line (R2-2.3). This minute is the record round 1 owed and did not write; the reciprocal conversions at F-W5/F-W7 are the Re-anchor seat's, which runs LAST by design **because this is what happens when it does not** |
| **E-17** (**round 2's SECOND seat — every receipt in this file RE-RUN, not inherited; three surviving R2-2 breaches cured, one of them minted by round 2 itself**) | The first round-2 seat was interrupted mid-write, so its claims entered this file **unverified by any second reader**. This seat re-ran them all against the live bytes rather than reading them. **REPRODUCED EXACTLY** (nothing changed): the R2-6a splice — ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk … F-W10.md)` → **empty**, so the ruled direction is real at the bytes and not merely narrated; the §2.2 whole-region diff → **exactly 3 hunks / 6 changed lines**, all outside the shared table, entrant block and checkpoint block (E-10 holds); the three reconciliation counts → **23 · 36 · 16** (E-13 holds); **all sixteen §2.5a-ii dispositions re-`sed`'d at their frozen-corpus anchors, 16/16 byte-exact**, and eleven distinctive fragments re-`grep -F`'d, 11/11 present; the F-W1 charter quote (⟨cmd⟩ `grep -n "The roster is" waves/F-W1.md`) → byte-identical to both sites here; **both** G-12 denominator `grep -o`s → reproducing; and the wave's outward witnesses → **11** packets · **9** `FOURIER-R*` · fourier HEAD **`cd26c65`** · porcelain **28** · **8** e2e specs · pin **`^0.13.0`** · O-20 `@source` **1** / `FR-NP-32` **1**. **DID NOT REPRODUCE — and it was the cure's own receipt.** E-11's ⟨cmd⟩ output had been pasted *with* its `-n` prefixes; re-run here it returned different ones. Cured at E-11, prefix-free, with the minute. **Three more R2-2 clause-2 breaches survived round 1 into round 2 and are cured here**: `F-W1-CARRY.md:246` still stood at §1b **while §2.8 E-3 declared it retired** (an erratum asserting a cure the file had not performed — the same shape as the defect it was erratum for); `COHESION.md:129` ×4 and `X/waves/W9.md:366`/`:375` + `W11.md:319` ×3 — **COHESION and `waves/` are the two live-sibling classes R2-2 names by name**, so these were never the F-W0 class alone, and a seat that converts only the citations a check file caught has cured a list, not an idiom. **Four "verbatim" labels carrying no ⟨cmd⟩ note** (FR-NP-32's banked disposition ×2 · fr-App C-12's negative record · D-19's lint-rule sentence) now carry one, per **R2-1-LAW.2** — all four re-derived at the corpus and all four exact. **Baseline re-stated once more** (E-14): HEAD moved `54a97489` → `281a420a` → **`5516359c`**; every affected witness reads identically at all three, and each cell says so | **The standing lesson, and it is this file's own**: *a repair seat's receipts are claims until a second seat re-runs them, and the receipt most likely to have gone stale is the one written to prove staleness*. Nothing here re-books an id, re-grades a row, or changes a quoted word — E-17 is verification and address-conversion only. **The rows this seat could NOT touch are named so no reader mistakes silence for approval**: the AA-44 `9`→`8` correction (E-5) and the `grep -A0 -n` receipt inside the checkpoint block both sit in the SHARED §2.2 bytes, where a unilateral edit breaks G-F10-6 ≡ G-F9-21's Δ=∅ — **they remain PENDING PAIRED EDITS, deliberately unapplied here, exactly as E-3 and G-F10-6 require** |
| **E-18** (**MAJOR, cured — the ruling honoured at its named rows and skipped at its method clause; the drain's root cause in its FOURTH form**) | R2-6b re-ruled the drain's operand to the 66 records **and** directed a FRESH derivation. Round 2 homed the sixteen rows PASS-2 had named and **ran no derivation** — so `fr-PathPreview.md`, a record in neither check file's set C, was never looked at, and its three arrow-form strict rows (`PP-CENSUS` `:31` · `PP-AGGLOM` `:69` · `PP-SEVLAW` `:70`) **escaped the drain entirely** while §5's header asserted the contrary and §4b's X-whole row claimed COHESION **§3.3**'s deadline — *at the last F wave, with no wave after it to catch them*. ⟨cmd⟩ this seat, word-output, **scoped `F-W[0-9].md` so it cannot match this file** (R3-3.7): `grep -lo 'PP-CENSUS' waves/F-W[0-9].md carry/*.md` → **∅**; `grep -lo 'PP-AGGLOM' …` and `grep -lo 'PP-SEVLAW' …` → `waves/F-W4.md` · `carry/F-W4-CARRY.md`, whose own words are ⟨cmd⟩ `grep -ho 'consumed, not settled here' waves/F-W4.md` → *"consumed, not settled here"* — **the only other wave that names two of the three expressly disclaims settling them, and the third is named nowhere.** Ten further rows, strict at this file's own probe, were carrying a CLASS verb where a per-id verb is owed | **CURED BY RUNNING THE DERIVATION, and the TRANSCRIPT is the deliverable** (§2.5a-iii(a)–(c), R3-10.6 · R3-2.4 · R3-5.5): five one-line probes partition all **165** NO-WAVE-OWNER lines with the arithmetic closing (**31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33**), the strict roster is **69 dispositions / 36 records**, the escape test resolves every strict row's head id but three, and **thirteen rows land with per-id terminal verbs** (**fourteen** after round 4 added the 69th, R4-7.3). **The lesson is the one the corpus itself wrote and this drain twice failed to read**: `fr-ImageUpload:45`'s row `11` — *"The corpus never swept the adjudicated registry … REGISTRY-FIRST is a precondition, not a courtesy"* — and now `fr-PathPreview:31`'s `PP-CENSUS` — *"SS-3/SS-4 must read the registry … the census audited the file, not the graph."* **Both rows describe the defect that hid them, and the second was found only because the first's instruction was finally executed rather than quoted.** Recorded so the pair is read together, never separately |
| **E-19** (**the self-count habit retired at its root — R3-3.10 adopted in terms**) | The standing amendment: *no ⟨cmd⟩ whose output is a COUNT of a live sibling or of the citing file itself may be BANKED in a spec.* This file banked three such receipts at §2.5a's reconciliation (`awk … \| grep -c '^\| \*\*'` → 23 · 36 · 16) and declared one of them *"binding on this file against itself"* — the exact construction that let **"SIXTY"** stand (E-13) and that stales the instant any row is added, as this round's own writes prove | **The three commands are re-stated as a re-derivation INSTRUMENT, not an operand**, with a fourth added for §2.5a-iii, and their outputs marked explicitly as **readings with a shelf life** — no gate rests on them; **G-F10-7's operand is the 66 records and its falsifier is the per-id enumeration**. The same law governs the two receipts this round did NOT write: the escape test's result is stated as a **classification** (*"the strings occurred in no section of this file"*), never as `grep -c … F-W10.md`, and the program-wide PP probes are `grep -lo` **word-output over file names**, never counts of live siblings. **Kept, unbanked, and honest about why**: a spec that must count itself to know its own roster has the wrong falsifier |
| **E-20** (**this round's PAIRED edit, landed rather than deferred — and the twin surface CONSUMED, not assumed**) | Two acts touch bytes this file does not own alone. **(1) The `D/i-1` quote (R3-3.7)**: §2.2's cell published *"Coupled: rides the B-1/B-2 cure wave, never scheduled independently (verbatim lock)"* inside quotation marks, while the bank reads only ⟨cmd⟩ `grep -n 'B-1/B-2 cure wave' fr-PaperView.md` → *"coverage rides the B-1/B-2 cure wave."* and ⟨cmd⟩ `grep -rc 'never scheduled independently' fr-*.md` → **0 in all 66** — a minted quotation, the class R3-3 convicts. The cure sits **inside the SHARED §2.2 splice bytes**, so it is a **PAIRED edit landing in F-W9 and F-W10 in ONE commit**, ruled as such and executed here rather than disclosed and deferred. **(2) The twin surface**: `F-W3.md` published **§X.1-v4** FIRST this round (R3-1a), and four of this file's rows touch records it partitions | **(1) LANDED, with the coupling clause moved OUTSIDE the quotation marks** and the bank's bytes inside them — the form §2.3 already models. **The distinction from E-5's AA-44 `9`→`8` is stated so no seat reads inconsistency**: that correction is **not ruled for this round** and stays a **disclosed pending pair**, deliberately unapplied; this one **is** ruled for this round at both ends, so applying it unilaterally is not what happens — both seats land it. **(2) CONSUMED BY CITATION at every row that touches it**: `fr-CoefficientsSpectrum` (enters at **`.d`**, F.W3 — §2.5a-ii's `i-5` riders cite it) · `fr-CollapsibleSection` (**`.d`**, F.W3 — §2.5a-iii's `m-4` geometry half) · `fr-Tooltip` and `fr-PathPreview` (**`.e`** / **`.d`** — §2.5a-iii's disjointness notes) · `fr-BasisCanvas` (the **F.W4 hand-off named PER ID at §X.1-v4 item 2**, against which `L §R5-7` is shown disjoint). **F.W10 books nothing on the twin surface and re-cuts nothing there** — the partition is F.W3's act and F.W4's receive; this file's only claim is that its drain rows and that partition do not overlap, **stated per id so a later seat can test it** |
| **E-21** (**the fourth-generation false receipt, in the cell built to end the class — REVOKED and RE-CUT, R4-1.12 · R4-4.4**) | §2.5a's band correction published *"`grep -hnE …` ⊕ the bold-terminal arm, records counted by `cut -d: -f1 \| sort -u \| wc -l` → **44** records."* **The command cannot produce the output**: `-h` suppresses the filename, so `cut -d: -f1` extracts the LINE NUMBER; and the printed pipeline omits both `grep -v 'Routing law'` filters the 44 depends on. As printed it returns **27** (arrow arm) / **70** (both arms) / **45** (`-n`, unfiltered) — **never 44**. It is the same base this file has now convicted **four** times (E-3 · E-11's second minute · E-4 · here), it landed inside the cell written expressly to end it, and it survived the round-3 purge certificate's claim that *"no third disposition survives anywhere in the eleven specs"* — a claim `PASS-3/CLOSE-CERT.md` now carries an erratum against (R4-1.E-3) | **RE-CUT AT §2.5a and RE-RUN**: filenames retained, both taxonomy filters present, arrow-form excluded from the bold arm, one base, pinned toolchain — **92 lines / 44 records**, with **56** given its own separate command so the two denominators can never fuse again. **The FIGURE stands because the honest pipeline produces it**, which is R4-1.12's stated direction and not a rescue: had the honest pipeline printed anything else, the figure would have followed it. **The standing lesson, now paid for four times: a receipt is not the number it prints — it is the command that prints it, and a seat that checks the number without running the command is checking nothing.** Two-key rule (R4-4): this cure awaits a non-author re-run |
| **E-22** (**the corrected denominator that was itself wrong by one, in its own favour, for the second consecutive round — R4-7.3**) | E-1 corrects the ADOPTION-ASKS denominator and §4a.13 declares the result *"binding on this file against itself"*. Round 3 published **17 id-rows / 11 OPEN / 6 not-OPEN** and filed **Ask 4** among the not-OPEN, quoting only *"Ask 4 (fourier portion LANDED G.W7)"* — while the same cell continues *"3 external repos OPEN — re-affirmed G.W8"*. The list settles it: ⟨cmd⟩ `FA='/Users/mkbabb/Programming/fourier-analysis'; sed -n '109,125p' "$FA"/docs/constellation/ADOPTION-ASKS.md \| grep -c 'OPEN'` → **12** against **17** rows, and `11 + 6 = 17` only because Ask 4 was held in both sets — the round-3 arithmetic summed to **18** and no seat noticed | **CORRECTED to 17 / 12 OPEN / 5 not-OPEN / 6 value.js-targeted (4 OPEN)** at E-1, at G-F10-1's witness, at §4a.13 and at §2.5a-ii's binding-denominator list. **Nothing downstream moves** — Ask 4 targets words · speedtest · csp-solver and never value.js — which is precisely why it went unchecked: *a denominator whose error changes no outcome is the one nobody re-derives.* **Both superseded figures are named, not replaced**: round 1's "11 id-rows" (the table size) and round 3's "11 OPEN" (the open set). This is the E-13 shape — *wrong by one, in the direction that flatters it* — **re-created inside the erratum written to cure it** |
| **E-23** (**verbatim-label law, fifth generation — five sites cured at their sites, R4-7.1**) | *"The verbatim label means bytes; there is no third meaning."* Five spans in this file wore the label over drifted bytes, all in the round-3 §2.5a-iii rows, all words-exact but none byte-exact: **PP-SEVLAW** rendered the corpus's straight double quotes as bold single quotes · **FR-TT-22** lowercased a leading capital and replaced an em-dash continuation with a terminal full stop, **trimming the clause that names the row's own class** (*"— a negative budget statement"*) · **PAW-56** closed the span with a period the bytes do not carry and dropped ` (latent, margin-1)` · **D-i2** ended on a comma where the bytes close with a full stop and elided the sentence's opening verb · **FR-USB-38** elided an emphasis whose span **coincides exactly with the quoted span**, so nothing forced the elision (outside CLOSE-CERT §6.2's *"elided rather than invented"* tolerance) | **ALL FIVE RE-PASTED BYTE-TRUE**, each beside the `sed -n 'NNp'` that produced it, in the frozen corpus where line anchors are lawful (R2-2 clause 2(i)). **None was re-labelled `paraphrase`** — the bytes were available at every site, so the byte-true paste is the cheaper cure and the honest one. CLOSE-CERT §6.1's *"724 label sites … 0 word-level drifts"* is falsified by construction here and is covered by the R4-1.E-3 erratum |
| **E-24** (**the phantom witnesses in this file's own errata table — R4-2.3**) | E-4's two `W9.md` receipts and its three-`W9.md` ambiguity note consumed **`$V` and `$F`, neither of which this file ever defined.** *"An undefined `$R`/`$V` is a phantom witness"* — R4-2 books the identical breach against F-W2's `$V` and F-W5's purge-minted `$R`; this file carried its own and named neither. It is the **third** time a defect has been found inside a cell written to prove a cure (E-3's unperformed retirement · E-11's re-imported address · here), and the second time inside E-4 specifically | **`V='/Users/mkbabb/Programming/value.js'` and `F='/Users/mkbabb/Programming/fourier-analysis'` DECLARED IN THE SAME PUBLISHED BLOCK and both receipts RE-RUN** — *"Restored analytic in/out arms match 0.13.0"* and *"Packets sent, exact-pin consumers notified"*, both EXACT, with the three-file `ls … \| wc -l` → **3** disambiguation now runnable rather than narrated. **A whole-file portability sweep rides with it (R4-2.5)**: ⟨cmd⟩ this seat over `F-W10.md`, counting **executable ⟨cmd⟩ spans only** (the three prose occurrences of `-P`/`\K`/lookaround are this law being QUOTED, not run, and are named here so the sweep is falsifiable rather than merely favourable) — PCRE flags and constructs in a runnable span → **0**, `.{n,m}` bounded wildcards → **0**, undefined shell variables → **0** (`$BOIL` · `$ROW` · `$STRICT` · `$V` · `$F` · `$FA` · `$C` · `$O` — every one declared in the block that consumes it; `$R` occurs only inside a quotation of R4-2 naming F-W5’s breach, never as this file’s own witness); `$BOIL`/`$ROW`/`$STRICT` are declared in the same block as the table that consumes them, and the `\|` inside every ⟨cmd⟩ span is the markdown table escape for a literal pipe (U+007C), disclosed once at §2.5a. **A gate whose command cannot run is not a gate**, and from R4-2 forward a pass-5 seat convicts it on sight |

---

## §3 Gates

**Twelve. Every one born RED with its failing probe measured read-only 2026-08-28 and named (L-19); no gate is a proof-farm script.** G-F10-12 is born **SPLIT** — one half GREEN by a verified receipt, the other RED — and says so honestly rather than averaging into a false colour.

| gate | born state · witness | falsifier (turns it GREEN) |
|---|---|---|
| **G-F10-1** ADOPTION-ASKS reconciled in BOTH ledgers | **RED [measured]** — the live re-triggered table (`ADOPTION-ASKS.md` header `:107`, rows `:109`–`:125`): **SEVENTEEN id-rows · TWELVE OPEN · FIVE not-OPEN (enumerated at §2.8 E-1; corrected at R4-7.3 — Ask 4's three external-repo legs are OPEN and round 3 quoted only its LANDED half) · SIX targeting value.js, of which FOUR are OPEN** (Ask 3 :111 P1, *"the hardest of the four and the true critical-path item"* · Ask 5 :113 P3 · inv-22-color :116 P3 · cascade-vjs :118 P2). Value-side `grep -ni fourier docs/tranches/V/coordination/INBOX.md` → **6 rows, none of the four, none inbound** (E-8: UNROWED) | each of the four carries an **INBOX row with a disposition verb** AND the reply landed in fourier's ledger; cascade-vjs answered as the **SPLIT** using the EXISTING `R/letters/CASCADE-VJS-RESPONSE.md` — never a fresh letter, never a write into fourier's tree. Falsified by: a verb naming a peer actor's action as an acceptance condition |
| **G-F10-2** Inbound-mail parity | **RED [measured at fourier HEAD `cd26c65`]** — no CLAUDE.md; `find docs -name INBOX.md` → **0**; the facility-19 letter UNTRACKED (`??`); the three value→fourier files (`83d4c9f` FN charter · `cd26c65` 2.0.0 note · facility-19) unanswered; our INBOX carries zero inbound rows | FN-1..FN-7 each carry a **fourier-side verb**; facility-19 **TRACKED**; the fourier INBOX **exists** (census F.W0's act, their sub-session) with ≥1 row this wave can point to. **FN-5's rider — "BEFORE or WITH fourier M.W10" — answered in that order or explicitly re-scoped** |
| **G-F10-3** G24 drift RELAYED, never inherited, never owned | **RED — MEASURE-AT-OPEN** — `fourier-value-import-drift.mjs` at **X·W9's gate table, the G24 row** (by gate id, R2-2 — the round-1 line address into this live sibling is retired); banked 8/22 curves drift, max\|Δ\| **0.192** vs `<1e-3` over 8 names | max\|Δ\| **< 1e-3** across the 8 names **RE-MEASURED AT WAVE OPEN**. Falsified by: quoting the banked number as current; claiming the gate (it is X-W9's); crediting any fourier-tree edit as an X-W9 gate |
| **G-F10-4** G33 packets sent + post-window pin truth | **RED [measured]** — zero packets; fourier `web/package.json:18` `"^0.13.0"` (re-verified 2026-08-28) vs published 4.0.0; keyframes pins `"4.0.0"` exactly; `npm ls @mkbabb/value.js` in fourier exits ELSPROBLEMS (glass 4.0.0 peers `^0.10.0\|\|^0.11.0` vs 0.13.0; keyframes 4.3.0 hard-deps `^0.13.0` — disjoint), reproduced ×6 across the registry | **five packets + five INBOX rows + a post-window `npm ls @mkbabb/value.js` exiting 0 in BOTH keyframes and fourier**; the pin leg greens only via F.W1's atomic transaction. Falsified by: re-booking per component (ONE identity, C-1 ≡ C-8); reading the post-window leg before the window; dropping the uncommitted-bump dissent |
| **G-F10-5** The LATEX-PAPER carry has a receiving surface | **RED [measured at BOTH ends — MEASURE-AT-OPEN, and the baseline moved]** — `grep -c 'latex-paper' docs/tranches/V/coordination/INBOX.md` → **0** at the declared baseline `54a97489`, **1** at live HEAD (`5516359c` at this seat's re-measure; `281a420a` when round 2 first wrote this cell — **both live readings identical**), that one hit being I-29's repo-list mention and **not a relay row** (§2.8 E-14); COHESION §4a glass-only; ≥18 named carries route to a register that does not exist; the producer tree has **no inbox path either** (E-7); `latex-paper` → **0** across all **eleven** sent packets, BJ ten ⊕ BK's O-20 (E-2) | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` exists with **every §2.6 carry rowed** AND **PAW-1's F.W1 question answered in the ledger** (fourth package added, or explicitly seated), **with a named delivery point**. Falsified by: any anchor from producer-src rather than `dist/vue.js` (SC-3); the K-9-killed `:root` alias snippet appearing as a prescribed repair |
| **G-F10-6** (≡ **G-F9-21**) The re-cut is complete, Δ=∅ both directions | **RED [measured]** — `grep -o 'F\.W9/W10' registry/adjudicated/fr-*.md \| wc -l` → **54** across **25** of **66**; bare `F.W10` → **0**. No adjudicated row is provably homed here today | all **26** routing identities cut to exactly one of F.W9 / F.W10 / a named other wave; **both specs carry the same §2.2 table**; set-difference ∅ both directions; and (E-3) the extracted §2.2 shared regions **`diff` empty**. **THE R-2b LEG IS NOW MEASURED, NOT PROMISED (R2-6a, performed this round)**: F.W9 authors the visual-regression checkpoint set, F.W10 receives it by splice, and ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty** (this seat, read-only). Round 1's inverted narration is struck at E-3. **The gate STAYS RED** — on its corpus leg, re-measured above (54 / 25-of-66 / bare `F.W10` **0**), never on the splice leg, and a later seat must not read the green leg as the gate. **Any change to the shared table, the entrant block, or the checkpoint block remains a PAIRED edit in ONE commit — never a unilateral fix here, never a retype** (the AA-44 `9`→`8` correction E-5 discloses is exactly such a pending pair and is deliberately NOT applied unilaterally). Falsified by: a row minted from the 14 boilerplate lines or the 7 boilerplate-only records; a row re-booked under a new id; **ANY edit to `fr-*.md`** |
| **G-F10-7** The NO-WAVE-OWNER register drained on the F side | **RED [measured]** — COHESION §4 is live-seeded from SS-7 batch 1, every row an X·V slug, **ZERO fourier rows**. **OPERAND, RULED (R2-6b): the strict roster is derived FROM THE 66 RECORDS — never from any check file.** **DETECTOR STATED INLINE (R2-9), so this gate and not a later seat catches the next format the corpus uses** — shapes: table rows ⊕ id-headed route-marked bullets ⊕ prose routings walked to their enclosing row id (S-8); spellings: **BOTH U+002D and U+2013**. Re-run read-only in `registry/adjudicated/` this seat: ⟨cmd⟩ `grep -o 'NO-WAVE-OWNER' fr-*.md \| wc -l` → **167** occurrences · ⟨cmd⟩ `grep -l … \| wc -l` → **56** of ⟨cmd⟩ `ls fr-*.md \| wc -l` → **66** records · ⟨cmd⟩ `grep -o 'NO–WAVE' fr-*.md \| wc -l` → **0** (the en-dash arm is ∅ today and **stays in the probe so it can never silently stop being ∅**) · strict shapes, **▲ SUPERSEDED — READ THE MARK BEFORE THE FIGURES**: ⟨cmd⟩ `grep -E '(→\|⇒) *\*{0,2}NO-WAVE-OWNER' fr-*.md \| grep -v 'Routing law' \| wc -l` → **31** arrow-form ⊕ ⟨cmd⟩ `grep -E '\*\*NO-WAVE-OWNER' fr-*.md \| grep -v 'Routing law' \| grep -vE '(→\|⇒) *\*{0,2}NO-WAVE-OWNER' \| wc -l` → **61** bold-terminal-cell form. **Both reproduce exactly and BOTH ARE SUPERSEDED AS DETECTORS by §2.5a-iii's partition (repair round 3), and the mark now stands where the probe is printed rather than only where §2.5a carries it** — *the defect PASS-4 §4.1 convicts: this gate's own falsifier eleven lines below forbids "a detector stated … in one spelling of the routing-taxonomy boilerplate", and `grep -v 'Routing law'` is exactly that one spelling, so **31 taxonomy lines sit inside these two figures as findings**. They are kept because a receipt is not deleted when a better one exists, and they are **not the operative detector**: the operative detector is `$BOIL`/`$ROW`/`$STRICT` at §2.5a-iii(a), which excludes the taxonomy by SHAPE (three or more wave tokens on one line) and whose five classes sum to 165.* **THE BAND IS REPLACED BY A BALANCED PARTITION (repair round 3, R3-5.5 — round 2's band disclosure was wrong TWICE and is STRUCK).** Round 2's cell read *"PASS-2's strict basis is 81 row-level lines …; this detector lands 92 over the same 56 records … the two differ ONLY in where the boilerplate/verdict-prose cut falls."* **(i) The sets CROSS, they do not nest** — `fr-PaperSearchDropdown:75` (`L:L-16`) and `fr-FunctionInput:81` (`C §0 row 7`), two of the sixteen homed escapes, are weak-form and sit OUTSIDE the 92; **(ii) 44, not 56** — the raw two-shape probe spans **44** records, while **56** is the count of records holding *any* NO-WAVE-OWNER token, two denominators fused inside a disclosure cell. **The replacement is not a better band but an exhaustive partition of all 165 lines** (§2.5a-iii(a), five one-line probes, arithmetic closing): **31 boilerplate ⊕ 68 row-STRICT (35 records) ⊕ 32 row-WEAK (19 records) ⊕ 1 prose-STRICT (walked to its row id) ⊕ 33 prose-WEAK (immutable verdict bytes) = 165**, giving a **strict roster of 69 dispositions across 36 records**. The roster this wave owes is **89 rows / 89 distinct ids / 89 terminal verbs** (**88** at round 3, superseded and named) — §2.5's **23** (FR-NP-32 heads them, counted ONCE) ⊕ §2.5a's **36** = **59 distinct ids** ⊕ §2.5a-ii's **16** = 75 ⊕ **§2.5a-iii's 14** (the three `fr-PathPreview` escapes ⊕ the ten strict rows round 2 mis-filed under a class verb ⊕ the 69th prose-strict `L-19+C-N1`, landed at R4-7.3) — plus one class-level verb over the weak class **enumerated whole, 32 lines / 19 records**. **"23 owed" · "SIXTY" · 59 · 75 are ALL superseded and each is named so**; and per R3-3.10 **no self-count is banked as this gate's operand** — the section-extraction commands are an instrument, the enumeration is the falsifier | §4 carries a **fourier block** and **every row of the reconciled 89-row roster bears a TERMINAL verb** — named wave / next-boundary ledger row (BH-RELAY or CENSUS-INPUT) / kill-with-rationale / cite-consuming-wave / fold-with-target / excluded-with-reason; **FR-NP-32 heads the table**; **fr-App C-12 lands as the NEGATIVE record verbatim**; the weak class carries its class verb over a roster **enumerated whole**. Falsified by: a verb that reads as a decision while being a deferral; **a roster that homes a subset and calls it zero-left-open** — *the falsifier round 1 satisfied by describing itself, round 2 satisfied at a larger number, and round 3 satisfied at `fr-PathPreview` because the ruled DERIVATION was never run*; **a roster measured against a check file rather than the 66**; a detector stated in one dash spelling or one row shape, or in one spelling of the routing-taxonomy boilerplate (round 2's `grep -v 'Routing law'` missed *"Routes:"* and *"Dispositions:"* and counted taxonomy lines as findings); **a class verb standing where the file's own detector grades the row STRICT** (ten such rows, cured per id at §2.5a-iii); **a predicate whose honest re-run does not reproduce the enumerated roster in BOTH directions**; **a partition of the 165 lines whose arithmetic does not close** |
| **G-F10-8** The Codex residue is not inherited; the owner rulings answered | **RED [measured]** — the census §6.7 charter line appears in no F spec; **OG-F1/OG-F2 unanswered**; `/tmp/fourier-r4-files.sha256` **GONE**; the Codex worktree **still registered**; **9** `FOURIER-R*` files in our tree (E-6), **0** in fourier's | the charter line lands in the **CORRECTED wording** ("Zero UNVERIFIED credit — every Codex verdict enters through per-claim adjudication (M-21)"), never the superseded "carry zero credit"; **OG-F1/OG-F2 RULED BY THE OWNER** (F.W10 verifies terminal, does not re-rule); R3-9, the R3-16+R4-11+R5-9+R6-10 board row, R4-12, R5-3, and the worktree each carry a terminal verb; **NONE of R4-12's rows consumed** |
| **G-F10-9** NEXT-COMMUNIQUE ledger truth | **RED [measured]** — `INTAKE-ADJUDICATION-2026-08-03.md:221` declares the channel EMPTY while COHESION §4a carries SC-1..SC-8 and the fourier registry adds the live glass + latex ask sets (§2.4 roster) | an **addendum-beside** amends without touching the dated bytes (`git diff --stat` on the intake stays **empty**), and every surviving ask sits in **EXACTLY ONE register** — glass → SS-6, latex → G-F10-5's register. Falsified by **any glass row cured consumer-side** |
| **G-F10-10** Close-gate a11y honesty — the FR-TT-1 promotion lock | **RED [measured]** — the Tooltip shim is the ONLY label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring a **description, never a name**; banked promotion note *"BLOCKER at any F.W9/W10 axe close-gate"*; today `grep -rln "admin" web/e2e/` → **0 files**, no axe visits `/paper`, `/equation` = one assertion-free screenshot slug across **8** specs (E-5) | the close axe run covers **`/paper` + `/equation` + the admin tab with zero serious/critical**, OR the 17 naming legs are recorded as a **NAMED, DATED WAIVER**. **Never argued green while the legs are open** — an axe close-gate authored before F.W4's naming legs land is RED by construction. The FR-EMT-11 rider binds the same way: MINOR is not "coverage debt is small" |
| **G-F10-11** F.W0 substrate is terminal before the close | **RED [measured]** — **FR-NP-32 (≡ fr-PaperSidebar M1)**, BLOCKER, cite-both-never-substitute: glass-ui 4.0.0 `dist/styles/index.css` → `postcss.parse` **35 top-level nodes, ZERO valid `@source`**, the `` source` `` at-rule :203–222 (**the self-count receipt is STRUCK at R4-9.3 / R3-3.10, the last one the E-19 sweep did not reach**: the fact is re-stated as a **classification** in the only lawful form — *at PASS-1 the id the producer's own packet carries was ABSENT from this close-gate in every section of this file; it is present from repair round 1 forward* — never as `grep -c … F-W10.md`, which is a count of the citing file and may not be banked). **Its relay leg is GREEN-BY-RECEIPT** (O-20 row A-1 under `BK/coordination/`); the RED that remains is the F.W0 *ruling* + a parsing dist · **GAB-13**: `git status --porcelain \| wc -l` → **exactly 28** (incl. the subject and `web/package.json`) · **F8-REACH-01/02**: both un-dispositioned; **HOLD is RED by the source's own law** | **FR-NP-32 (≡ M1)**: the relay leg is **already discharged and read** (O-20 row A-1, `BK/coordination/`) — what remains is **the dist parses (or the pin moves)**, plus F.W0's own ruling; the WT diff **landed OR explicitly recorded as the audit baseline**; both F8-REACH rows carry an exact `KEEP_WITH_MOUNT` \| `ISOLATED_HARNESS` \| `DELETE` with **no HOLD surviving**. F.W0 rules; F.W10 verifies |
| **G-F10-12** Every declared relay arm has a grep-able receipt — the structural cure for "no value.js gate goes RED when a relay does not leave" | **SPLIT, born honestly [measured]** — **GREEN-BY-VERIFIED-RECEIPT** on U-1..U-11: `valuejs-outbound-2026-07-28-o17-full-debt-discharge.md` under `glass-ui/docs/tranches/BJ/coordination/` rows **B-1..B-10 naming all eleven arms** (E-2; the banked "six letters, each zero" is stale — **ELEVEN** packets exist: BJ ten ⊕ BK's O-20). **ALSO GREEN-BY-VERIFIED-RECEIPT on the F-side `@source`/FR-NP-32 arm** — `glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md` row **A-1** carries the corrupt-dist ask and names `FR-NP-32 ≡ fr-PaperSidebar M1` (`grep -c '@source'` → **1**). **RED [measured]** on the remaining F-side arms: term-sweep across all **eleven** packets → `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0**; zero latex-paper packets exist anywhere. **The earlier "`@source` 0 across ten" was a sweep of a superseded inbox path — corrected at E-2, not carried** | for **EVERY** arm this sub-tranche declares GLASS-OWNED / RELAY / LATEX-RELAY, **a packet file exists under the producer's inbox path CONTAINING THAT ARM'S IDENTIFYING TERM** (grep-able, L-2-conformant). Falsified by: a receipt asserted rather than verified; a relay counted green because a letter was authored but not landed under the producer's path; an arm folded into a batch whose text never names its term. **"Relay-declared-never-sent" enters DISEASE-REGISTRY regardless of the O-17 discharge** |

---

## §4 Sequencing

### §4a Ordering locks (carried whole; each lands where its rows land)

1. **RE-CUT-0 PRECEDES EVERY OTHER ROW.** Neither F.W9 nor F.W10 stamps before G-F10-6 ≡ G-F9-21 closes. **F.W10 does not author F.W9**; any change to the shared §2.2 bytes is a **paired edit in one commit**.
2. **F.W0 SUBSTRATE PRE-GATES FIRST** — M1's corrupt dist · GAB-13's dirty worktree · F8-REACH-01/02's HOLD · the Codex residue with OG-F1/OG-F2 flagged · **MG-θ ≡ M7's tsconfig one-liners (ONE substrate edit, TWO records, booked ONCE)** · the re-cut PAW-49. Every F.W1/W3/W4 budget is otherwise measured against **a dist that does not parse and a tree no commit holds**.
3. ▲ **F.W1 is ONE atomic land-or-lose transaction — the TWELVE-limb roster chartered at F-W1 §4 step 4, cited whole, never restated here** — quoted by command (R2-5), ⟨cmd⟩ `grep -n "The roster is" waves/F-W1.md`: *"The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a, cross-edge 1 at this writing) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading."* (a sibling that restates the transaction re-fractures it; a three-limb restatement is a gate that passes while the vaul-vue manifest gate, the RE-PIN act at the adopted commit hash, and the P0 CSS-class census land separately — the exact land-or-lose failure the L-18 base forbids). The consequence F.W10 verifies: else four zero-console-error e2e gates go red for the wrong cause (**FR-EQR-3**). **PRECEDES F.W3/W4 (§2 law)**. **F.W1 must NOT be credited with the pagination-drain cure (FR-GIG-5)** — F.W10 verifies the mandatory footnote. PAW-1's fourth-package rider: latex-paper added to the transaction or explicitly seated. **F.W10 schedules nothing that presumes the uplifted tree.**
4. **GCM-42 CONSOLE LOCK**: no widening of the error-only filter before F.W1 lands.
5. **HARNESS-BEFORE-RIDER (PAW-50)** governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44 — decision F.W4 · execution F.W9 · terminal record F.W10. **FLAG-BEFORE-SPLIT (PAW-49)** rides it — the split is not type-safe until the flag lands or the return is honestly `number | undefined`. **F.W10 cannot book a coverage row terminal while the harness decision is unmade.**
6. **Repair-arms-a-defect same-commit riders** (carried to F.W3/W4 where their rows land; named here so the close verifies each): fr-PaperSearchModal D-1+D-3+`outline:none` · **PAW-30 in the same repair as PAW-1** · **LAW-3** (PAW-44 restore-only-WITH-or-AFTER PAW-1+PAW-30) · **LAW-4** one clearance authority (PAW-47 dependent of the PAW-44 decision) · MPC-31/MPC-3⊕10⊕13⊕8⊕22 one-cut law · FR-MSP-6 two-channel lock.
7. **PAW-2's COUPLING LAW**: the measurement write is fixed BEFORE the ref is memoised (PAW-4); K-16 confines rhythm cures to measurement-safe forms. Same-family cite-both-book-once with L/D8.
8. **FR-TT-1 PROMOTION LOCK** (G-F10-10): the axe close-gate follows F.W4's 17 naming legs, or declares a named, dated waiver. Never argued green.
9. **AXE-SEAT ECONOMY**: the `/equation` seat is ONE artifact (authored once, cited five times: FR-EQR-6 · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · EquationView D·D-B2/C·D-28); the `/paper` seat is ONE (D-B2 · M7 · PAW-34) and runs AFTER PAW-1's cure + the PAW-44 decision; the admin seat EXTENDS the existing keystone (AA-44). **FR-USB-16's mobile/coarse Playwright project precedes every coarse-pointer witness** (ImageUpload 27's drag path · the glass a11y-overrides coarse floor · PaperSidebar's `--ui-scale:1.5`/`--control-floor:2.75rem` arm) — one matrix change, three unblocked rows.
10. **L-11 + M2 HARD LOCK**: F.W4 must not rename `.sidebar-top-btn` before the latex-paper relay lands.
11. **D/i-1 COUPLING**: coverage rides the B-1/B-2 cure wave — never scheduled independently. **Seam-before-seat** (FR-AFP-42) · **teleport lock** (FV-26/FV-27) · **HOLD-is-RED verification** (F8-REACH → FR-COB-20).
12. **ROUTE LAW**: direct parse-that → fourier FORBIDDEN (parser → value X·V L1/L5 → packed release → consumers) — this wave mints no such edge. Glass rows ride SS-6, never frontend hacks. **MEASURE-AT-OPEN** on every relayed number (G24 · G33's post-window leg · the deploy-of-record). **RELAY-RECEIPT-LAW binds this wave to itself** (G-F10-12).
13. **ADMISSION GAP**: the co-signed contract's signature needs a durable fourier surface — **F.W10 cannot close before F.W0 stands it up** (G-F10-2). **Owner rulings flagged inline, never presumed**: OG-F1 · OG-F2 · the L-4 producer-ownership escalation (F.W10 cannot stamp terminal while unanswered) · SS-4's seven rulings (trie-vs-KISS guardrail-incumbent et al.). **Anchor law**: FR-CP-45's rule governs every anchor this spec quotes, and **R2-2 supersedes it in form** — cross-spec receipts cite **STABLE anchors** (§-headings, gate ids, clause ids, banked row ids, row labels), **never line numbers into a live sibling**; line numbers survive only for the frozen `fr-*.md` corpus and this file's own intra-file coordinates marked *at this writing* (§2.8 **E-11**). **The live-sibling set is ENUMERATED here so no later seat converts only the class a check file happened to catch** — `waves/F-W*.md` **and** `X/waves/W*.md` · the two `carry/` files · `COHESION.md` · `KF-W1.md`: all five classes were live in this file at round 2's start and all five are converted (§2.8 **E-17**). **And the prohibition reaches PROVENANCE NOTES, not only citations**: a `⟨cmd⟩` receipt that pastes `grep -n` output into this file re-imports the address the ruling abolished — receipts into live siblings are pasted **prefix-free**, carrying the bytes that identify the thing (a gate heading, a row label) and not its coordinate. That rule is not theoretical: E-11's own receipt violated it and had gone stale inside the round (§2.8 **E-11** second minute). SC-3: latex anchors from `dist/vue.js` only; PAW-39: quote 31, never 35; ImageUpload: 15/6, never re-derived; AA-44: EIGHT specs, never nine (E-5 — the correction is a PAIRED edit and is not applied unilaterally here). **Corrected denominators, binding on this file against itself**: ADOPTION-ASKS is **17 id-rows / 12 OPEN / 5 not-OPEN / 6 value.js-targeted (4 OPEN)**, never 11/11/4 and never the round-3 "11 OPEN" (E-1, corrected R4-7.3) · the relay sweep reads **ELEVEN** packets across `BJ/` **and the live `BK/`**, never ten (E-2) · the NO-WAVE-OWNER drain is **89 rows / 89 distinct ids / 89 terminal verbs** — §2.5's 23 ⊕ §2.5a's 36 (= **59** distinct ids) ⊕ §2.5a-ii's 16 (= 75) ⊕ **§2.5a-iii's 14** ⟨**one character, re-cut at repair round 5, REST-67.** The cell read `13`, and `23 + 36 + 16 + 13 = 88` under a label reading **89**; the section-extraction instrument returns **23 · 36 · 16 · 14**, and `23 + 36 + 16 + 14 = 89`. ⟨cmd⟩ `/usr/bin/grep -o "§2.5a-iii's 1[34]" waves/F-W10.md` → `14` · `14` · `13` — **the round-4 13→14 landing reached §2.5a-iii, §5 and G-F10-7 and NOT the clause headed _"Corrected denominators, binding on this file against itself"_.** Filed at MEDIUM because it is the **third generation** of the E-13/E-22 shape and it sat in the clause built to stop it⟩, plus one class verb over a weak class of **32 lines / 19 records enumerated whole**; **never 23, never "SIXTY", never 59, and never 75** — each is a superseded reading and each is named as one (§2.5a-iii · E-13 · E-18) · the strict roster is **69 dispositions across 36 records** out of a partition of **165** lines that closes (`31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33`), never a "band" between 81 and 92, and the raw two-shape probe spans **44** records, never 56 (§2.5a-iii(a) · E-19) · the drain's **operand is the 66 records, never a check file** (R2-6b · E-12), and **no count of this file is banked as any gate's operand** (R3-3.10 · E-19) · the F.W1 transaction is the **TWELVE-limb roster chartered at F-W1 §4 step 4**, quoted by ⟨cmd⟩ and never restated in three or counted at eleven (R2-5 · E-15) · every carry citation is `carry/F-W1-CARRY.md` or `carry/F-W4-CARRY.md` **by path**, and "the CARRY" with no path is forbidden vocabulary.

### §4b Cross-edges — declared FROM THIS END; each owed a reciprocal sentence in the named file (the X·V W4-D2 lesson, now law)

| to | reciprocal | edge |
|---|---|---|
| **F.W9** (widened, or a minted F.W9a) | **REQUIRED** | the P-12 coverage-void packet **EXECUTES there**; F.W10 owns terminal records only. Handed over by §2.2: AA-44 · FR-AFP-42/49 · FR-GSB-17 · FR-USB-16 · FR-EQR-6 (axe leg) · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · EquationView C·D-28 + D·D-B2's axe leg · GCM-42 · FR-GV-36 · GAB-10's coverage half · FV-26 · App C-13 · ContourPreview 38 · ImageUpload 27 · PAW-34 · PAW-12's rider · PaperView D/i-1 · PaperSidebar D-B2/C-M4/M7 · FR-AUL-16's execution limbs · FR-COB-20's harness branch. **Same table both specs; neither stamps before G-F10-6.** FR-TT-1's F.W9-side share is G-F9-22 |
| **F.W4** | **REQUIRED** | the vitest-floor **DECISION** (PAW-50; census §4.5) · the 17 FR-TT-1 naming legs · the L-4 ToC-collapse execution · PAW-49's F.W3/W4 rider · GAB-10's live-region half riding GAB-4 · FR-EQR-6's attributes · D·D-B2's `aria-label` ×2 · ★MF-10's `si % 13` fallback with root-count assertion · L/D5+L/D30's consumer half. An axe close-gate authored before the naming legs land is RED by construction |
| **F.W0** | **REQUIRED** | substrate terminal-VERIFICATION only (§2.3; G-F10-8/-11): **FR-NP-32 (≡ fr-PaperSidebar M1)** · GAB-13 · F8-REACH-01/02 · the Codex residue with OG-F1/OG-F2 flagged · MG-θ ≡ M7 · re-cut PAW-49. **F.W10 verifies; it does not re-rule.** **G-11/G-12 QUOTATION (R-9.1)**: every anchor and every denominator this spec quotes is an **input to / quotation of F.W0's G-11 anchor table (F-W0 §4 gate G-11) and G-12 denominator table (F-W0 §4 gate G-12) in `SUBSTRATE-LEDGER.md`** — **divergence from that table is a defect against G-11, not a rival act**. F.W10 re-performs no re-resolution as its own act: the CITE / I-3 / M-10 / FR-EQC-15 drift registers routed at §2.5a are *inputs to* G-11, and D/m-12's 21/14 is *quoted from* G-12. Producer-side evidence carries the producer COMMIT HASH, never the version string (**F-W0 §4 gate G-13**). **CITATION FORM (R2-2, and this row is where the class lived)**: all three are cited **by gate id, never by line** — F.W0 is the round's shift source and its `:199/:204/:209` addresses moved +12 under the round-1 repair while this file still pointed at them (§2.8 **E-11**); a line cite into F-W0 is a defect from R2-2 forward, and F.W0's own §4 head carries the reciprocal prohibition (R2-2.5) |
| **F.W1** | **REQUIRED** | PAW-1's ledger rider (the fourth-package ask) · C-19 ⊕ PAW-33 pin truth + ★MF-12 · C-06's `ComputedRef<any>` rider · the L-4 escalation · M3's pre-gate discriminator · the 2.0.0 answer stated AT 4.0.0 · MG-ι's remedy constraint (`colorUnit2`, never `color2`) · FR-GIG-5's mandatory footnote · FR-EQR-3's atomicity |
| **F.W3** | declared | FR-TT-1's shim leg keeps its R3-7a/X-11 identity (`ui/tooltip` thin-adapter: 35 callsites / 9 consumers, barrel → `@mkbabb/glass-ui/tooltip`) · ★MF-11's dead-API clean before the `./search` shadow diff · FR-CP-44's re-seat candidate |
| **F.W5–W8 / SS-4** | **BOTH-ENDS REQUIRED** | FN-5 ("BEFORE or WITH fourier M.W10") / FN-6 / FN-7 as **CONTRACT INPUTS**; **TA-4 named PREREQUISITE or the contract EXPLICITLY RE-SCOPED** (a co-signature over un-runnable probes is void); the seven owner rulings flagged inline (trie-vs-KISS `atomdiff.py:12-14` INCUMBENT · remix-vs-fork · born-visibility · visibility-enum · version-`_id` · redaction parity · pagination). **A reciprocal sentence is REQUIRED in the F.W5 spec.** **⊕ THE R3-6.1 EDGE, DECLARED FROM THIS END AT LAST (the obligation round 3 created and left out of this table — PASS-4 §6.6): `AA-45` · `AA-46` · `AA-47` have their ONE home in §2.5's drain, and F.W5's A1/A2/A3 clauses CITE them by id and BOOK NOTHING on them** — the direction is stated here, not only in §2.5's ▲ block, because §4b's own header requires every edge be declared from this end. The far end is discharged and verified: F-W5 §2 clause **A1** reads *"BOOKS NOTHING — cites only: `AA-45` → F-W10 §2.5"*, and its §7 edge row reads *"`AA-45`'s one home is F-W10 §2.5's drain (R3-6.1) and this edge books nothing on it"*. `AA-46`'s KILL-WITH-RATIONALE stands at the drain and may be cited at F.W5 as envelope **evidence with the kill noted**, never as an operand. Boundary: neither side's edits gate the other's waves |
| **SS-6** (standing glass-ui BH relay; live inbox path **`glass-ui/docs/tranches/BK/coordination/`**, BJ retained as the historical record) | declared | every accreted glass ask rides the ONE batched letter at the boundary — **F.W10 is the F-side boundary** (the §2.4 NEXT-COMMUNIQUE roster + §2.5a's BH-RELAY verbs + the U-1..U-11 record carried as an O-10a-style **status note**: discharged at O-17, no ask withdrawn, no packet re-sent). **The boundary letter already dispatched on 2026-08-28 is O-20** (**COHESION §4a, the `DISPATCHED 2026-08-28` row**; **22 id-rows** — *corrected at R4-7.3 / R4-9.3: the round-3 cell quoted COHESION's own "26 entries", a **banked count of a live sibling** measured nowhere; the packet is authoritative and the prose follows the list*. ⟨cmd⟩ this seat, 2026-08-29, `O='/Users/mkbabb/Programming/glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-08-28-o20-authoring-block-batch.md'; grep -cE '^\| [A-Z]+-[0-9]+ \|' "$O"` → **22** (`A-1..A-14` · `B-1..B-7` · `C-1`), against `grep -cE '^\|' "$O"` → **28** = 22 rows + 3 headers + 3 separators, and `wc -l < "$O"` → **59**. **R4-8.3 hash pin, so a later rewrite of the sibling is detectable rather than deceptive**: ⟨cmd⟩ `shasum -a 256 "$O" \| cut -c1-12` → **`f6e04c86ddb8`**. COHESION §4a's **26** is named superseded here and is corrected at its own site under the §1a #3 carve, never quoted onward) and it carries **FR-NP-32 ≡ fr-PaperSidebar M1** at row A-1 — the F-side boundary reconciles AGAINST that packet, it does not re-send it. **PRODUCER ROWS NEVER BECOME FRONTEND HACKS** |
| **LATEX-PAPER-RELAY** (new register, ASK-ONLY) | declared | the §2.6 roster whole; anchor law SC-3 (`dist/vue.js` only); M2's rename lock rides it; delivery point named (E-7) |
| **X·V** (X-W9.f / X-W9.i · V·π) | declared | G24/G33 relayed, never owned (MEASURE-AT-OPEN); W.L5's value half ships in X-W9.f under D-15's grant; MG-ι's value-side limb → the V·π/parser-seam ledger with the OWNER-MAIL FLAG; the forbidden parse-that→fourier edge never minted |
| **the fourier sub-session** (COMMISSION §2) | asked; receipt VERIFIED, not asserted | their writes, never ours: the fourier INBOX + CLAUDE.md (census F.W0's act) · the ADOPTION-ASKS/FN/2.0.0 reply rows · tracking facility-19 · the two parity-probe residuals (valuejs-J-atomdiff, valuejs-J-publish — fourier-owned, re-homed to their M.W12). The admission gap binds (§4a.13) |
| **KF.W10** | ∦ — declared to PROVE non-overlap | the intake's KF block (B18-12/13/14; the boundary block citable ONLY as `345/12/57 @ 8281638c`; B18-27's slot 4-vs-5) and the KEYFRAMES half of ASK-5 are KF's alone. No F-side absorption; no id double-homed (COHESION §3.1) |
| **X-whole** (COHESION §3.1–§3.6) | the close act itself | **F.W10 is the LAST F wave**: the F-side of §3.1 (every **adjudicated registry row** in exactly one wave — **measured against `CENSUS-CANONICAL.md`, the stamped census of record, over the 66 `fr-*.md` — and nothing else** *(re-based at repair round 4, R4-3 · R4-10: the round-3 wording "the 66 and nothing else" named the CORPUS where an id-keyed closure claim needs the CENSUS; §2.5b states the distinction and this row now keeps it)*: not against a per-wave carry, not against a superseded census, and **not against a check file** (R2-6b · E-12), whose only standing here is as a hostile register this spec answers), §3.2 (every census wave-sketch id F.W0–F.W10 → spec or terminal kill), §3.3 (every NO-WAVE-OWNER row terminally homed — **the deadline**, and the claim is discharged by §2.5a-iii's PUBLISHED DERIVATION over the 66, never by this row's assertion: `31 ⊕ 68 ⊕ 32 ⊕ 1 ⊕ 33 = 165`, strict roster **69 / 36 records**, three escapes found and homed, ten class-verbed rows given per-id verbs — **because a deadline clause at the LAST F wave has no successor to catch what it misses**, which is exactly what `fr-PathPreview` proved), §3.4 (edges reciprocal, zero cycles) closes HERE; §3.5's L-20 conformance loop with a declared-Fable final pass is the sub-tranche's own close act |

---

## §5 Excluded (nothing silently dropped; every exclusion reasoned)

**Header RE-STATED A SECOND TIME, over a roster that was DERIVED rather than adopted (repair round 3, R3-10.6) — and the previous re-statement is named as having been false too, not quietly improved.** Round 1's header read *"zero adjudicated registry rows among them"* while sixteen strict rows sat in no section of this file. Round 2 re-stated it over **81 corpus-derived strict row-level lines** and homed those sixteen — and **that header was false as well**, because the operand had been corrected without the ruled derivation ever being run: `fr-PathPreview`'s three strict rows sat in no section of this file either, and **no F wave settled them** (E-18). The claim is now **DERIVED, and its derivation is published**: §2.5a-iii(a) partitions **all 165** NO-WAVE-OWNER lines in the 66 records — `31 boilerplate ⊕ 68 row-STRICT ⊕ 32 row-WEAK ⊕ 1 prose-STRICT ⊕ 33 prose-WEAK`, arithmetic closing — for a strict roster of **69 dispositions / 36 records**; every strict row's head id resolves in this file except the three, which land at §2.5a-iii(c) with terminal verbs; ten rows mis-filed under a class verb take **per-id** verbs there; the weak class is **enumerated whole** (32 lines / 19 records) so predicate and roster regenerate each other; and the reconciled roster is **89 rows / 89 terminal verbs**. **What §5 excludes is therefore boilerplate (by SHAPE, not by one of its three spellings), other waves' work, producer work, and owner acts — and a later seat tests that by RE-RUNNING §2.5a-iii's five probes against the 66, never by reading this sentence.** The distinction is the whole lesson of PASS-1 D5 and of both repairs that inherited it (E-12 · E-18): *an exclusion header is a claim about a roster; a roster measured against the wrong operand makes the header false without making it look false — and a roster measured against the right operand without ever being measured does exactly the same thing.*

| excluded | reason |
|---|---|
| **⟨ROUND 4, re-cut at repair round 5 — FOUR, not three (REST-66; P5-2, HIGH)⟩ The FOUR §2.2 canonical divergences — `FR-TT-1 → F.W10` vs canonical **F.W4** · the 26-vs-17 F.W9 roster spread ⟨*16 at E5-16*⟩ · `PAW-49 → F.W0` and `PAW-12 → F.W9` vs canonical **F.W3** for both · **`PAW-50` → F.W4 ⟨re-cut⟩ vs canonical F.W9**, undisclosed until repair round 5** ⟨**the fourth is different in KIND, which is why it must be named.** Canonical `` | `PAW-50` | — | `F.W9/W10` | **F.W9** <sub>legs: F.W9/W10</sub> | `` — **the canonical's PRIMARY is F.W9 and §2.2 cuts away from it**, where the eight other cuts operate on the **leg** column (`FR-AFP-42`→F.W3 · `FR-EQR-6`→F.W4 · `D·D-B2`→F.W4 · PaperSidebar `D-B2`→F.W4 · `C-M4`→F.W4 · `M7`→F.W0), none of which this repair convicts. **And the harness-before-rider row §4a.5 makes `PAW-50` load-bearing for five other bookings.**⟩ | **PENDING PAIRED EDITS, disclosed at §2.5b and deliberately NOT APPLIED.** They sit inside the SHARED §2.2 SPLICE BYTES; a unilateral landing breaks G-F10-6 ≡ G-F9-21's Δ=∅, and R4-10 assigns the F.W9 roster re-basing to **F-W9's** duty. They join **AA-44 `9`→`8`** in the pending-pair register. *An exclusion that names the ruling, the reason and the holder is a disposition; one that names only the reason is a deferral wearing a verb* |
| **⟨ROUND 4⟩ The `PASS-3/CLOSE-CERT.md` erratum row (R4-1.E-3) and the R4-8.2 sha256 certificate table** | **NOT THIS SEAT'S FILE AND NOT THIS SEAT'S TURN.** The erratum is an append to a certificate outside this wave's single writable file; the sha256 cert is the purge seat's closing act, and **LAW E (R4-8.1) puts the purge seat TRULY LAST** — any seat that writes after the cert issues VOIDS it by construction. Recorded here so no reader mistakes the silence for an oversight |
| **⟨ROUND 4⟩ COHESION §4a's "26 entries" for O-20** | the true figure (**22** id-rows, sha `f6e04c86ddb8`) is corrected **in this file** at §4b, where the borrowed count was quoted; COHESION itself is a §1a #3 **MODIFY-carve** whose writes wait on the owner's begin-word, and this spec's status is `planned`. **The count is corrected where it was consumed; the source is flagged, never silently edited** |
| **⟨ROUND 4⟩ Re-labelling the drain rows whose short-form heads the canonical does not bank as ids** (`D-19` · `CITE` · `★MF-7` · fr-ContourPreview `31`/`39`/`41` · `fr-BasisCanvas L §R5-7` · `L-§4`) | **AN AMENDMENT QUESTION FOR THE CANONICAL, flagged with its operand rather than laundered into this wave's count** — the idiom `CENSUS-CANONICAL.md` §4.5 uses for the `PAW-4` twin divergence. Each row keeps its banked disposition and its per-id verb here; what is unsettled is the SPELLING the census heads them by, and a wave spec does not re-cut a census detector (R4-3) |
| The **14 boilerplate** routing-law lines + **12 verdict-prose** digraph mentions + **2 same-identity restatements** | not routings: boilerplate mints no rows (RE-CUT-0's dissent); verdict prose is dated adjudication bytes — immutable, annotated by addendum only; restatements fold to their rows |
| Rows from the 7 boilerplate-only records | their only digraph hit is the law line; the REAL rows (FR-AUL-16, FR-COB-20, F8-REACH-01/02) enter by mechanism above |
| **The 31 routing-TAXONOMY lines + the 33 prose-WEAK lines** of §2.5a-iii(a)'s partition | **excluded BY SHAPE, and the shape is stated so the exclusion is testable**: a taxonomy line names three or more wave tokens beside the disposition (spelled *"Routing law"*, *"Routes:"* **and** *"Dispositions:"* — round 2 excluded one of the three and counted the other two as findings); the prose-weak lines are **dated adjudication/verdict bytes** — immutable, annotated by addendum only, never a row. **Neither class mints a row, and both are counted into the 165 so the partition closes** — an exclusion that cannot be added back to the total is an exclusion no seat can audit |
| Any edit to `registry/adjudicated/fr-*.md` | read-only + anti-rename — the re-cut is a table in two specs and one addendum |
| All repair execution — PAW-1's 42-site rewrite, PAW-2's measurement gate, PAW-11's emitter semantics, the cache trio, the flattener nesting, every glass cure (M1's dist, ramp headroom, label rungs, `--success`, `--tier-featured`, the observer root) | producer work in producer trees / F.W0-W1-W3-W4-W9 work in theirs; this wave books verbs, relays letters, verifies receipts — and producer rows NEVER become frontend hacks |
| Every fourier-tree write — their INBOX, CLAUDE.md, ADOPTION-ASKS reply rows, FN answers, FN-7's two writes, facility-19 tracking | COMMISSION §2 — their sub-session; F.W10 asks and verifies receipt |
| The KEYFRAMES half of ASK-5; KF.W10's rows | KF's mail seat / KF's close — declared ∦, not absorbed; no id double-homed |
| ASK-3 / INV-22-COLOR infrastructure execution | maintainer-owned deploy infra outside any F wave's bounds; F.W9 owns the deploy spine, F.W10 the correspondence |
| Owner rulings — OG-F1, OG-F2, L-4's ToC ownership, SS-4's seven | owner acts: FLAGGED INLINE, never presumed, never pre-answered |
| R4-12's row contents · the 72/158/P29/slot superstructure | consume-nothing (moot — evaporated, E-9) · Codex-internal, does not transfer |
| Killed limbs, recorded not carried as work: D-1's 16-count + :42/:49 anchors (register #8) · PAW-1's K-9 `:root` alias limb · K-18's sticky-scroll arm · K-6's two headroom cells · GAB-29-as-destination (falsifier only) · the "un-harnessable" defence (ruling 3e) · the superseded "carry zero credit" wording · ContourPreview's two REFUTED cures · MG-ι's value-side execution (relayed, owner-mail flag) | killed at their registers; no repair inherited from killed corpora |
| A fresh cascade-vjs letter; any new cross-repo channel | the reply exists (`R/letters/CASCADE-VJS-RESPONSE.md`) — relaying IS the reconciliation; SS-6 and LATEX-PAPER-RELAY are the only lawful couriers |
| MPC-31/MPC-3⊕10⊕13⊕8⊕22 · fr-PaperSearchModal D-1+D-3+`outline:none` · FR-MSP-6 (as repairs) | same-commit riders whose rows land at F.W3/W4 — carried by those specs; they appear here only where they sequence (§4a.6) |
| `scripts/dev/dev.sh` · crons · any product-source open · any status advance beyond `planned` | unowned dirty row, NEVER touched · owner order (E13 sweeps only) · the execution gate stands until the owner's begin-word |

---

**Close act**: when G-F10-1..12 are GREEN (G-F10-12's F-side half included; its U-half is already green by verified receipt), F.W10 stamps the F-side of COHESION §3.1–§3.4, runs the §3.5 L-20 conformance loop with a declared-Fable final pass, and records the boundary row in COHESION §5. **L-18 still applies** — no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance; the bases here: a re-cut complete-looking because it counted boilerplate; **its inversion — a drain complete-looking because it counted only what it chose to look at (PASS-1 D5)**; **that base's SECOND form: a cure that adopts the convicting register's roster as its operand and inherits the blind spot whole — a bigger number that is still a subset (PASS-2, cured at the OPERAND by R2-6b: the 66 records, never a check file — §2.5a-ii/E-12)**; **and its THIRD, which round 3 convicted and which is the subtlest of the family: an operand corrected while the ruling's DERIVATION clause is skipped, so a record neither register ever looked at survives the correction whole — `fr-PathPreview`'s three strict rows, escaping the drain at the LAST F wave, under a §3.3 deadline clause the file itself declares (PASS-3, cured by RUNNING the derivation and publishing its transcript — §2.5a-iii/E-18)**; **a class verb standing where the file's own detector grades the row strict, and a predicate whose re-run does not reproduce the roster it names (PASS-3, cured per id and by exhaustive enumeration — R3-5.5)**; **a band disclosure written to keep a figure honest that is itself wrong twice — sets asserted to nest that cross, two denominators fused in one sentence (cured by replacing the band with a partition whose arithmetic closes)**; **a spec that must COUNT ITSELF to know its own roster (R3-3.10, cured at E-19: the self-counts become an instrument, never an operand)**; a disposition verb that reads as a decision while being a deferral; a relay receipt asserted rather than verified — **and its mirror, a receipt sweep pointed at last tranche's inbox (PASS-1 D1/D2, cured at §1b/E-2)**; **a denominator sharpened to the wrong number (PASS-1 D3, cured at E-1) — and its twin, a denominator double-counting its own head row while declaring itself binding (PASS-2, "SIXTY"→59/75, cured at E-13)**; **an atomic transaction restated with three of its twelve limbs (PASS-1 D4, cured at §4a.3 by whole-citation; the extent itself corrected to TWELVE at R2-5 — E-15)**; **a citation re-resolved by coordinate in a round whose own repairs move coordinates — the eight F-W0 line cites that drifted +12 under this file's own repair round, cured per-IDIOM by gate id, not per-address (PASS-2, R2-2 — E-11)**; **and that base's own second form, convicted only because a second seat re-ran the receipts: a PROVENANCE NOTE that pastes the abolished address back in — E-11's `grep -n` receipt, written to prove that citations must not be coordinates, had itself gone stale within the round (cured prefix-free at E-11/E-17)**; **an erratum that declares a cure the file never performed — E-3 announced the `carry/…:246` retirement while §1b still carried it (cured at E-17)**; **a paired splice narrated in the inverted direction, so that following the disclosure would have destroyed the ruled-in bytes (PASS-2, R-2b/R2-6a, cured and re-measured at E-3)**; **a BLOCKER whose banked id the producer's own packet carries while the consumer's close-gate does not (PASS-1 D6, cured at §2.3/§2.4/§2.5)**; an axe close-gate argued green over 17 open naming legs; a "terminal disposition" stamped while an owner ruling is outstanding. Until then: **planned**, born-RED (one gate honestly SPLIT), every witness named above.
