# X.F.W10 - Coordination Discharge and the X·F Close (frontend half)

⟨X·F · **status: planned** · half: frontend · twice-authored 2026-08-28 (Fable seat · Opus seat) and folded WHOLE by the fresh-Fable agglomeration seat (M-23) under M-25 — the F.W10 intake CARRY consumed **whole**: **77 rows · 12 gates · 12 cross-edges · zero silent drops** (dedupe by identity · cure-shape locks and sequencing edicts carried · routed by mechanism; transcription-only is DEFECTIVE) · L-19 (every gate names a live witness) · L-20 validated against `docs/precepts/instructions/` (TRANCHE-AND-WAVE-SPEC · STYLE · ORCHESTRATION · CONSUMING · LESSONS-LEARNED) · idiom: `docs/tranches/X/waves/W0..W11.md` bounds/gates/provenance ⟨…⟩ cut to the `docs/tranches/X/parse-that/waves/` lean form · **twin**: `F-W9.md` (fold-authored 2026-08-28) — its §2.2 spliced the shared bytes from the 2026-08-25 fold of THIS file; this re-authoring keeps that region **frozen byte-identical** (verified by diff at write) · supersedes the 2026-08-25 fold at this path (65→77 rows · 11→12 gates · 11→12 cross-edges) · witnesses re-measured read-only 2026-08-28 against value.js HEAD `54a97489`+worktree and fourier HEAD `cd26c65` — receipts inline; drift recorded at §2.8, never silently corrected⟩

**Alias law**: `F.W10` denotes `X.F.W10` — a search alias, never a rename. The CARRY's `waves/W10.md` name lands as `F-W10.md`; same identity, alias recorded (the twin correspondingly is `F-W9.md`). **Banked ids keep their identity for life; nothing below is re-booked.**

**Scope.** The F-side **terminal** wave, and a **ledger act** throughout — it books verbs, stands up registers, relays letters, verifies receipts, and stamps the F-side of COHESION §3.1–§3.6. Five limbs: (1) the F.W9/F.W10 **re-cut** — the precondition that lets either spec stamp at all; (2) **ADOPTION-ASKS reconciliation both directions** — four asks OPEN against us since G.W8, plus the FN charter, the 2.0.0 note, and the one-way-correspondence kill; (3) **G24/G33 evidence relayed** — never inherited, never owned (the gates are X-W9's); (4) the **latex-paper carry given a receiving surface** — the SS-6 analogue ≥18 named carries route to and that does not exist; (5) **terminal disposition of every deferred row with no re-booking** — the NO-WAVE-OWNER drain, the substrate verification, the Codex residue, and the close. **F.W10 repairs nothing**, re-rules no owner-gated question, and edits no tree that is not this one.

**Four-verb**: AUDITED **YES** (registry-adjudicated; re-measured 2026-08-28) · SPECIFIED **YES — by this file** · IMPLEMENTED **NO** · VERIFIED **NO**. **EXECUTION IS NOT AUTHORIZED BY THIS FILE** — status stays `planned`; nothing opens product source until the owner's begin-word.

**Goal criterion.** This wave succeeds if, at close, a reader can answer three questions from this document alone: **where does every adjudicated fourier row live** (exactly one wave, one register, or one dated kill), **what did the constellation actually receive** (a packet file under the recipient's path containing the arm's own term — a receipt read, never asserted), and **what is still owed to the owner** (a flagged list of rulings, none pre-answered). It fails if any disposition reads as a decision while being a deferral.

**Binding law.** M-25 (a spec ignoring adjudicated rows is DEFECTIVE; transcription-only is DEFECTIVE) · L-19 · L-20 · E13 (no wave closes with UNREAD mail) · COMMISSION §2 (neither side's edits gate the other's waves; fourier-tree writes are **asked, never made**) · **R3-9** (an assertion of non-execution is *recorded*, never cited as a method guarantee) · **FR-CP-45's anchor law, binding on this file's own citations**: *take D's CLAIMS and L/C's LINES; re-resolve every anchor before quoting into a wave spec* (applied at §2.8). Two standing laws shape every row:

1. **The registry is READ-ONLY.** The re-cut lands as a **table in two wave specs plus a census addendum** — never as an edit to any `fr-*.md`. Anti-rename holds by construction.
2. **Producer rows never become frontend hacks** (standing owner edict). Glass → **SS-6**; latex-paper → the register **this wave creates**; fourier-tree acts → **asked**.

---

## §1 Bounds

### §1a Owned — value.js tree (writes only post begin-word)

| # | Path | Act |
|---|------|-----|
| 1 | `docs/tranches/X/fourier/waves/F-W10.md` | CREATE — this spec (supersedes-in-place; §2.2's shared bytes identical to `F-W9.md`'s) |
| 2 | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` | CREATE — the SS-6 analogue P-6 needs (G-F10-5); roster §2.6; anchors from `dist/vue.js` only (SC-3) |
| 3 | `docs/tranches/X/COHESION.md` | MODIFY-carve ONLY — §4 add the fourier NO-WAVE-OWNER block (G-F10-7) · §4a accrete the fourier-registry SS-6 glass rows (G-F10-9) · §5 boundary row · §0 the X·F status cell. **§2/§3 text is root-authored — quoted, NEVER rewritten** |
| 4 | `docs/tranches/V/coordination/INBOX.md` | MODIFY — mail rows only (E13): Ask 3 / Ask 5 / inv-22-color / cascade-vjs · FN-1..FN-7 · the 2.0.0 note answered **at 4.0.0** · the latex-paper relay row · the MG-ι owner-mail flag. Existing rows never rewritten |
| 5 | `docs/tranches/V/megatranche/coordination/FOURIER-ADOPTION-ASKS-RECONCILIATION-2026-08-25.md` | CREATE — the four-ask reply letter (G-F10-1); date-stamp re-resolves at execution; **the PATH is fixed so no seat invents a third channel** |
| 6 | `docs/tranches/V/megatranche/coordination/FOURIER-EVIDENCE-RELAY-G24-G33-2026-08-25.md` | CREATE — the evidence relay (G-F10-3/G-F10-4). **Relay-only; it never claims the X-W9 gates** |
| 7 | `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-ADDENDUM-2026-08-25.md` | CREATE BESIDE (E-3 shape) — the NEXT-COMMUNIQUE amendment (G-F10-9). The dated intake bytes never rewritten |
| 8 | `docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` | **CREATE BESIDE — co-owned with F.W9**: F.W10 owns the §4.11 re-cut record, the three by-mechanism cut deviations (PAW-49→F.W0 · PAW-50→F.W4 · FR-TT-1→F.W10), the FR-CP-44 metric-stack re-seat correction, the FR-AFP-51 tense correction, and the §6.7 charter line in its CORRECTED wording (G-F10-6/G-F10-8). F.W9 §1a row 2 takes MODIFY (its §4.10 deploy/coverage cells + its own errata) — **the CREATE stays here**, per the twin's own bytes ("F-W10 §1 already claims the CREATE") |

**Bounds reconciliation vs the CARRY.** The CARRY named `megatranche/coordination/ — CREATE letters only`; rows 5–6 name the letters exactly. Row 8's create/modify split is settled by the twin's measured bytes (F-W9 §1a row 2 + its reconciliation note) — the Opus draft's inversion (F.W10 MODIFY) is **rejected at §2.8 E-3**. Disjointness holds by section; the two waves are serial at the boundary.

### §1b Read-only bounds (no write)

`docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (**all 66** — anti-rename; the re-cut is a table, not a rewrite) · `formation/fourier/{CENSUS-2026-08-03.md, lane-docs.md, lane-crud.md, lane-frontend.md}` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` (dated; amended **beside**) · `coordination/FOURIER-R*` (9 files, §2.8 E-6) + `FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md` (immutable evidence) · `docs/tranches/R/letters/CASCADE-VJS-RESPONSE.md` + `docs/tranches/R/audit/coordination/COORDINATION-ANALYSIS.md` (**the reply EXISTS — relay it, never re-author**) · `docs/tranches/X/fourier/waves/F-W9.md` (the twin; **F.W10 does not author F.W9**) · `glass-ui/docs/tranches/BJ/coordination/valuejs-outbound-*.md` (the ten sent packets — **read for receipts, G-F10-12**).

### §1c Explicitly NOT in bounds

All product source in either tree (`/Users/mkbabb/Programming/fourier-analysis/**`, `value.js/{src,demo,api,test,e2e}/**`) — **the execution gate** · `package.json` · `scripts/dev/dev.sh` (unowned, NEVER touch) · fourier's `docs/constellation/ADOPTION-ASKS.md`, `docs/tranches/N/**`, any fourier INBOX/CLAUDE.md (**their sub-session, COMMISSION §2**) · `latex-paper/**`, `glass-ui/**`, `keyframes.js/**` (producer trees reached **only by relay letter**) · `docs/tranches/X/{waves,keyframes,parse-that}/**` · `~/.codex/**`.

---

## §2 Carry

**Seventy-seven banked rows, agglomerated by mechanism.** A row's presence here is a **disposition, not a work order** — most execute elsewhere, and this wave's claim on them is the terminal record. Nothing is re-booked; every banked id is cited under its own name.

### §2.1 RE-CUT-0 — this wave's own precondition (recorded, not booked; no banked id exists)

**BLOCKER (spec-integrity; blocks BOTH F.W9 and F.W10 from stamping).** Re-measured 2026-08-28 in `registry/adjudicated/`: `grep -o 'F\.W9/W10' fr-*.md | wc -l` → **54**; `grep -l … | wc -l` → **25 records**; bare `F.W10` routing → **0**; `ls fr-*.md | wc -l` → **66**. Census §4.10/§4.11 makes F.W9 deploy-spine/evidence and F.W10 coordination/close — **two disjoint shapes** — so *no adjudicated row is provably homed here today*. Anatomy of the 54, measured so the cut cannot manufacture rows from boilerplate (the standing dissent's explicit warning):

| class | count | disposition |
|---|---|---|
| **routing identities** (a defect row's own disposition) | **26** | re-cut, one wave each — table §2.2 |
| **routing-law boilerplate** (the header line listing every wave id) | **14** | re-worded in the census addendum's taxonomy statement; **zero rows minted** |
| same-identity restatements (`fr-App:142` · `fr-PaperArticleWindow:186`) | **2** | fold to their row; never counted twice |
| verdict / adjudication prose | **12** | dated bytes, **IMMUTABLE**; annotated by addendum only |

**Seven records yield ZERO re-cut rows**: VisualizationView, CoefficientsPanel, FrequencyGraph, HarmonicLevelGrid, InfoCard, CanvasOverlayButton, and fr-AdminUserList (its single hit is the header line; FR-AUL-16 at :54 routes NO-WAVE-OWNER). CanvasOverlayButton and InfoCard still enter — **by MECHANISM** (FR-COB-20, F8-REACH-01/02), never by the digraph.

**Cure**: all 26 identities cut to exactly one of F.W9 / F.W10 / a named other wave; **both specs carry the same table; set-difference ∅ in both directions** (COHESION §3.1, the X·V 117-row idiom). The registry is not touched. Neither spec stamps until **G-F10-6 ≡ G-F9-21** closes.

### §2.2 The re-cut table (26 identities; identical in F-W9.md) + P-12 locks

*Heading mirrored (`identical in F-W9.md` ↔ `identical in F-W10.md`); the preamble, table and entrant block below are the **shared bytes** — the twin spliced them verbatim. **Any change is a PAIRED edit landing in both specs in one commit** — a unilateral fix breaks G-F10-6 / G-F9-21's Δ=∅. Count and deixis disclosures live OUTSIDE the shared bytes, at §2.8 (E-4/E-5).*

Execution seat = **F.W9-widened (or a minted F.W9a)** per the P-12 packet ruling; the F.W10 share of every F.W9-cut row is the **terminal-disposition record only**. Three cuts deviate from the banked digraph **by mechanism** (flagged ⟨re-cut⟩; recorded in the census addendum). Other-wave legs already homed (F.W0/W1/W3/W4) are cited, never re-cut.

| record | identity (kept for life) | cut | mechanism + locks / preserved dissent |
|---|---|---|---|
| fr-AdminAuditLog:87 | **AA-44** | **F.W9** | packet root: zero frontend tests, 9 e2e never enter the audit tab, backend pins its half. Instrument = GAB-10's `grep -rln "admin" web/e2e/` → **0 files**. Cure: extend ONE existing axe keystone (visualization-ux:26-43 + visualization-crud AxeBuilder) into the admin tab; live-region half stays F.W4. **Dissent (FR-AFP-49, preserved): the family carries blocker weight — "every row above incl. all four blockers is invisible to every gate the repo runs"; INFO is a scope convention (L-9.1), never "the debt is small"** |
| fr-AdminFlaggedPanel:95 / :110 | **FR-AFP-42** (test seat) · **FR-AFP-49** | **F.W9** | 0 props / 0 emits / setup-top-level fetch in `defineAsyncComponent` — untestable seam. Seam → F.W3/W4 (homed); only the seat re-cuts. Distinct mechanism from AA-44 (testability ≠ coverage) — never folded; seam-before-seat. **Quote 8 specs, never 9** (K11) |
| fr-App:91 | **C-13 (App)** | **F.W9** | four untyped ambient global side effects, no vitest floor. The four-wave chain made explicit: **MG-θ → F.W0** (tsconfig/lint one-liners) · floor **decision → F.W4** (census §4.5) · **execution → F.W9** · **terminal record → F.W10**. Distinct id from FunctionInput's C-13 — never merged |
| fr-ContourPreview:71 | **38** | **F.W9** | zero coverage; `vue-tsc -b` is precisely the gate row-11 is invisible to. **No repair inherited — both flagship cures REFUTED** (the `<Card tier surface>` migration = 21-site typecheck break; the restore-path blank render cannot occur). The simplify-heap unit target anchors F.W4's harness decision — the cheapest true vitest justification in the packet, not PAW-50's trapped `resolveFigure` |
| fr-EquationModeToggle:41 | **FR-EMT-11** (= C-16) | **F.W9** | no unit runner; `/equation` one assertion-free screenshot slug; axe installed, never pointed here. Rider verbatim: "the gates for FR-EMT-1/-5's cures ride their own waves." **Dissent PRESERVED verbatim: reader-2's MAJOR ("every other row in this record is ungated because of it") ruled a scope error under L-9.1 — "so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small."** The close honours this warning |
| fr-EquationResult:41 / :72 | **FR-EQR-6** (axe leg) · **FR-EQR-31** | **F.W9** | `.eq-scroll-region` scroll container: no tabindex/role/name; KaTeX spans give no focusable descendant. F.W4 = attributes (homed); F.W9 = the single `/equation` seat. Cohort: fr-CoefficientsSpectrum M-6 same class. FR-EQR-31: zero behavioural coverage by construction; D-20's archaeology half rides FR-EQR-27's correction — **not re-booked** |
| fr-EquationView:62 / :191 | **D·D-B2** (axe leg) · **C·D-28** | **F.W9** | zero functional e2e on `/equation` (8 specs / 1 screenshot hit); info button + Wand2 = two nameless icon-only controls; aria-label ×2 → F.W4 (homed). **The packet's single strongest F.W9-seat argument: the ONE interaction spec (compute → notation → budget → reload) mechanically catches B-1, B-2, C-29, D-07, D-14 — five already-booked defects.** BLOCKER→MAJOR per the fr-CT precedent, both readers concur |
| fr-FunctionInput:80 | **C-13 = D-24** | **F.W9** | `/equation` screenshot-only; the nameless Wand2's survival is the corollary. FOLD to banked fr-CollapsibleSection m-8 class (`actions` slot ready). Naming half belongs to FR-TT-1 / EquationView D·D-B2 — never double-book |
| fr-GalleryAdminBanner:49 | **GAB-10** (coverage half) | **F.W9** | GAB-1's `color-contrast` serious class unverified by construction. **FOLDS BY REFERENCE to AA-44**; live-region half → F.W4 riding GAB-4's `aria-busy` (homed). GAB-13 (F.W0) gates it: gates written against the dirty worktree measure a tree no commit holds |
| fr-GalleryCardModal:93 | **GCM-42** (= L-14) | **F.W9** | zero tests every level; console guard filters error-only + 404 — blind to GCM-4's warns three ways. **FOLD → AA-44 / FR-AUL-16 class, never re-booked.** Own increment surviving the fold: widen the guard to `warn`. **Interaction lock: no filter widening before F.W1 lands** — the error-only filter is the blind spot F.W1's four zero-console-error gates depend on (FR-EQR-3). Dissent carried: DU's BLOCKER on GCM-4; the SR composite (GCM-4∘16∘17∘6/8) named blocker-weight with no BLOCKER row carrying it |
| fr-GallerySearchBar:54 | **FR-GSB-17** | **F.W9** | its named e2e is vacuous by construction — every assertion inside `if (await filterToggle.isVisible())` (gallery.spec.ts:57-63); delete the bar → GREEN. Un-vacuate, don't add: **assert the precondition EXISTS**; kill the unreachable `.glass-dock` branch (:49-53; `grep -rn glass-dock src/` → 0) and the `.or()` mask (:19-22). One cure shape authored ONCE for the class (with FR-USB-16 + FR-GV-36's census) |
| fr-GalleryView:78 | **FR-GV-36** (= L-24) | **F.W9** | every defect structurally invisible (no vitest/ESLint/noUnusedLocals/vueCompilerOptions); console e2e passes against a dead API. Folds → FR-GFC-27 + AA-44 route + FR-AUL-16 input. Surviving increment: **L's three-conditionally-vacuous-tests census**. The dead-API fact belongs to the F.W0/F.W9 deploy-spine reckoning — edge declared, API repair NOT booked here |
| fr-ImageUpload:61 | **27** | **F.W9** | all **15** e2e entries across **6** spec files use `setInputFiles` — drag/reject/oversize/failure/keyboard uncovered. **Quote the 15/6 pair, never a re-derived count** (ruling 7's denominator). The drag path needs FR-USB-16's coarse project — one matrix change unblocks both |
| fr-PaperArticleWindow:58 / :90 | **PAW-12** (rider limb) · **PAW-34** (= C-i-5) | **F.W9** | PAW-34: zero e2e on any owned surface ("figure" hits = `test.describe.configure` substrings). PAW-12: two facts on one hand-typed literal (`TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))`) guarding a no-fallback `<picture>`; predicate split → F.W3/W4 (homed); F.W9 = the build-time set-equality assertion (vitest or Vite build plugin per PAW-50). Invariant **HOLDS at HEAD** (28 png / 26 avif / 26 webp; unpaired `fourier.png`+`maintainer-avatar.png`; 26 keys = 26 `\includegraphics`) — the rider guards drift. **PAW-50: harness-before-rider. PAW-49: flag-before-split.** Dissent preserved: LC's MINOR sustained at R-2 |
| fr-PaperArticleWindow:199 | **PAW-49** | **F.W0** ⟨re-cut⟩ | without `noUncheckedIndexedAccess`, `resolveFigure`'s return asserts `width: number` while one whole arm yields `undefined` — vue-tsc green over a false type. The flag is a **repo-config** line, identical in kind to MG-θ/M7's tsconfig flags; **the one coverage-family row whose gate is FREE** — `npm run build` already runs `vue-tsc -b`, no new harness. F.W3/W4 rider on the PAW-12 cure homed |
| fr-PaperArticleWindow:200 | **PAW-50** | **F.W4** ⟨re-cut⟩ | web/ has **no** unit harness at all (five scripts, no vitest/jest, zero `*.test.ts`); `resolveFigure` trapped in the SFC. It *is* the harness-before-rider law — census §4.5's **decision** is its only true home; **F.W9 executes** (move `resolveFigure` beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12); **F.W10 verifies**. Governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44 |
| fr-PaperSidebar:49 | **D-B2** (axe leg) | **F.W9** | "Contents" heading 2.39:1 light / 3.00:1 dark vs the 4.5 floor; no axe visits `/paper`. Token → F.W4 (homed). The `/paper` seat is **ONE artifact** with M7 + PAW-34 and **runs AFTER PAW-1's cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. MAJOR, blocker-weight in the a11y aggregate |
| fr-PaperSidebar:58 | **C-M4** | **F.W9** | the expand-if-closed guard reads the **Tooltip's** `data-state` (domain `closed\|delayed-open\|instant-open`, never `"open"`) — a no-op. F.W4 half dies with the `CollapsibleTrigger` restructure (homed); F.W9 re-keys the guard on the Collapsible root. Corrected cite :124–129. Third vacuous-gate instance but a **DISTINCT mechanism** (wrong element's state) — never folded; the cure differs |
| fr-PaperSidebar:67 | **M7** | **F.W9** | dead surface unreachable by any gate; two of four needed gates are one config line each. tsconfig half → **F.W0 = the SAME identity as MG-θ — one substrate edit, two records, booked once**; F.W9 = axe on `/paper` + lint script |
| fr-PaperView:120 | **D/i-1** | **F.W9** | zero coverage of the search surface, no vitest. **Coupled: rides the B-1/B-2 cure wave, never scheduled independently** (verbatim lock) |
| fr-Tooltip:26 | **FR-TT-1** (promotion note) | **F.W10** ⟨re-cut⟩ | the shim is the ONLY label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring `aria-describedby` only-while-open — a DESCRIPTION, never a NAME. It governs **this wave's close-gate design** (G-F10-10). Naming legs → F.W4 (all 17); shim leg → F.W3 keeping its R3-7a/X-11 identity; folds honoured, never re-booked (EditorControlsDock C-3 · CanvasControlsDock D-1/L-3/C-1 · EquationView D·D-B2). Producer corroboration glass-ui 8.0.0:69-73: "an icon-only trigger labels ITSELF." L-4's 17-count census citable; D-1's 16-count + :42/:49 anchors KILLED (register #8) |
| fr-UserSlugBar:53 | **FR-USB-16** (≡ r1-MISSED-5 ≡ r2-MISSED-1) | **F.W9** | self-disabling test: `gallery.spec.ts:66-82` body wrapped in `isVisible().catch(() => false)`; matrix is one Desktop-Chrome project. Unguard (assert the trigger EXISTS) **+ add the mobile/coarse Playwright project** — a matrix change that must precede every coarse-pointer witness (glass a11y-overrides floor; PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` arm; ImageUpload 27's drag path). **Dissent preserved: r2 graded MINOR; overruled at the ruling table** |

**By-mechanism entrants** (records carry NO digraph — they enter by mechanism, not by re-cut, and must not be counted into the 54):

- **FR-AUL-16** (fr-AdminUserList:54, MAJOR) — the only cascade-delete surface, zero automated coverage of any kind; "un-harnessable" defence KILLED (ruling 3e). NO-WAVE-OWNER: harness decision → F.W4 · devDependency + admin-tab axe limb → F.W9 · **terminal home decided AT F.W10** (§3.3 deadline — last F wave). Cure verbatim: "extend one axe keystone into the admin tab + a vitest devDependency"; slug-bearing aria-labels = ready selector vocabulary.
- **FV-26** (fr-FullscreenViewer:79 = D-23·C-m7) — the only e2e "Fullscreen" hit is a comment (gallery.spec.ts:113). Coverage share → F.W9. **Teleport lock: assert against the teleported root** (FV-27: one of two Teleport sites). FV-30 rides banked BasisCanvas D-4/BC-2/C-2a + AnimationControls C-12, no new booking; FV-31 → F.W4.
- **FR-COB-20** (fr-CanvasOverlayButton:74 = C-7) — interaction contract entirely implicit; no test anywhere. "Dies with the file"; the no-test-for-wrappers observation → SS-3/SS-4 input; the tree favours DELETE. Gated on **F8-REACH-02** at F.W0 — **HOLD is RED; F.W10 verifies no HOLD survives** (that verification is this wave's whole claim, not a re-booking).
- **FR-AH-45** (fr-AppHeader:108 = C-16, NO-WAVE-OWNER spec-input) — zero props/emits/slots, no contract. A question owed an answer ("what contract should a shell header state?") → SS-3/SS-4 spec-input; terminal home or kill-with-rationale at F.W10, never left open. **Discharges together with FR-AH-30's cure-caution: the D-axis rank-5 remedy REVERSES the outcome — only `@layer glass-overrides` appended after `utilities` works** (glass-ui's competing rules already sit in `@layer components` at higher specificity).
- **PAW-33** (fr-PaperArticleWindow:89 = C-i-2) — rides the C-19 ⊕ PAW-33 pin-truth row (§2.6).

### §2.3 Substrate + terminal-disposition verification (F.W0 RULES; F.W10 VERIFIES, never re-rules)

| row | what F.W10 checks — and the lock it must not break |
|---|---|
| **M1** (fr-PaperSidebar:29/:9/:124, BLOCKER) — installed glass-ui 4.0.0 ships a syntactically corrupt `dist/styles/index.css`: `postcss.parse` → **35 top-level nodes, ZERO valid `@source`**, the garbage at-rule `` source` `` at :203–222 | F.W0 pre-gate + **glass BH/BJ relay** (a dist-GENERATION bug; producer source at HEAD intact). The paired **M3 F.W1 pre-gate discriminator** goes into F.W1's spec: `grep -c '<class>' components.css glass-ui.css` — 0/0 ∧ absent from `web/src` ⇒ NOT PAINTING; F.W1 pre-gate prediction: build succeeds, `grep -c animate-collapsible` → 0; a naive `@source` widening flips three findings at once. **Identity guard: M1 DECIDES banked fr-CollapsibleSection M-7 (emission hinge, NEGATIVE) and CORRECTS banked K-5 (shipped-but-INERT — both prior framings wrong); the correction books here, the defect stays banked at its original id** |
| **GAB-13** (fr-GalleryAdminBanner:52, MAJOR, ruled UP from INFO) — the audited artifact is the **DIRTY WORKTREE**: 28 modified rows incl. the subject and `web/package.json` (HEAD `^3.1.0`/`^2.2.0`/`^0.10.0`/lucide `latest` vs WT `^4.0.0`/`^4.3.0`/`^0.13.0`/`^1.0.0`) | F.W0: **LAND the WT diff OR record it explicitly as the audit baseline** — either branch is TERMINAL; a disclosure line is a disposition, not a dodge. Class part-banked (FR-COB-10 + fr-BasisCanvas's disclosure idiom) plus the K-9 certification kill (verify-hash-then-read-worktree fails open). Ties G33's uncommitted-bump dissent. **Dissent PRESERVED VERBATIM (worker-DU): "the fleet has already elected to treat the working tree as the scope" — if F.W0 adopts the worktree as baseline, GAB-13 discharges to a disclosure line and DU's INFO was right all along** |
| **F8-REACH-01 / F8-REACH-02** (`FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md:30-31`) — InfoCard.vue (N.C11, `b2c2e718…`, 1,516 B) and CanvasOverlayButton.vue (N.C39, `35f3db91…`, 595 B): unmounted, un-dispositioned | F.W0 rules the **exact verb** — `KEEP_WITH_MOUNT` \| `ISOLATED_HARNESS` \| `DELETE`. **HOLD IS RED by the source's own law; F.W10 VERIFIES NO HOLD SURVIVES TO CLOSE** (G-F10-11). FR-COB-20 gated on -02; fr-InfoCard's dirty-edit class (FR-COB-10) rides -01 |
| **CODEX-R3–R6 residue** (INTAKE §3 F.W0 block + §4 · CENSUS §6.7 + the :310-316 ADDENDUM · lane-docs §4c) — **9** `FOURIER-R*` coordinate files on the value side (§2.8 E-6), **none in fourier's tree**; R3–R6 frozen TERMINAL/SOURCE_RED; the 72/158/P29/slot superstructure Codex-internal, non-transferable | **The charter line lands in the CORRECTED wording** — "**Zero UNVERIFIED credit** — every Codex verdict enters the formation through per-claim adjudication (M-21)" (verified verbatim at `CENSUS-2026-08-03.md:310-316`) — never the superseded "carry zero credit", which would re-introduce the discard posture M-21 forbids. Terminal verbs: **R3-9** record only, never a method guarantee · **R3-16 + R4-11 + R5-9 + R6-10** — the 14-wave M board is the real disposition object · **R4-12** — consume-nothing stands and is **MOOT** (`/tmp/fourier-r4-files.sha256` re-confirmed GONE 2026-08-28; the banked "74,507 B still on disk" is stale) · **R5-3** re-hash or drop · the Codex worktree `~/.codex/worktrees/d0be/fourier-analysis` (re-confirmed **still registered** 2026-08-28) dispositioned. **OWNER RULINGS OWED, FLAGGED INLINE, NEVER PRESUMED: OG-F1** (re-root fourier source auditing at F.W0, or freeze the R-coordinates and build from live-tree measurement; evidence favours FREEZE-WITH-ADOPTION; R6 self-declared `NO_SUCCESSOR`, the ruling is cheap) · **OG-F2** (does R6-3's pre-write root-absence-receipt bind successors, or was it Codex-era? the absence itself is TRUE and adopted). **Dissent carried (OG-V2): the Codex chain violated its own prohibitions (XF-7, KF X-7) — Codex-authored `NO_RETRY`/`NO_SUCCESSOR`/"forbidden" verbs may not bind the Claude-owned formation without an owner ruling** |
| **L-4 = C-B2 = D-N10 (+M6)** (fr-PaperSidebar:33/:66, MAJOR) — ONE 98-node tree derived three times across two producers; both export `useSidebarFollow`; the arbitration point is an untyped `defineExpose` seam | **F.W1 OWNER-RULING ESCALATION** (which producer owns the ToC model); **F.W4 executes the collapse** (delete `paperTree.ts`'s adapter + the losing index). **F.W10 CANNOT STAMP TERMINAL DISPOSITION WHILE THE ESCALATION IS UNANSWERED** — close-gate input, flagged inline; F.W10 verifies the ruling landed, it does not rule |
| **SS-4-PREREQ (TA-4 atomdiff)** (CENSUS §5 [P0] #3 · lane-crud §0/§2 R-3 · COMMISSION §2, BLOCKER, contract-binding) — `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` EXCISED at T.W1/TA-4; `J-diff-shape.md §6` probes cannot run value-side; **a co-signature over un-runnable probes is VOID** | **F.W10 VERIFIES THE DECLARATION EXISTS IN THE F.W5 SPEC**: the value-side restoration named a PREREQUISITE, **or** the contract EXPLICITLY RE-SCOPED. The work belongs to the value.js API row, outside fourier's waves. SS-4 owner rulings **FLAGGED INLINE, never presumed**: trie-vs-KISS · remix-vs-fork · born-visibility · visibility-enum · compound-vs-global version `_id` · redaction parity · cursor-vs-offset pagination. **Dissent RECORDED: the commission's trie-compression collides head-on with the standing anti-tree KISS guardrail in BOTH trees (`atomdiff.py:12-14`) — THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design** |

### §2.4 Coordination discharge — the rows this wave itself executes

| row | verb + mechanism |
|---|---|
| **ASK-3** (P1 — *"the hardest of the four and the true critical-path item"*; live table :111) | palette-api still deploys by rsync to a deploy-dir, blocking `deploy-hook.sh` adoption — the fourth migration gating `dispatch.sh` deletion. **Book the disposition verb (ADOPT / DECLINE-with-rationale / DONE) into `INBOX.md` AND relay to fourier's ledger.** Adopt-shape: rsync → git checkout under canonical root. F.W10 books the verb; **it never executes infrastructure** — F.W9 owns the deploy spine, F.W10 the correspondence. *Standing law carried as dissent: peer-gated items have ≈ZERO discharge rate corpus-wide (dispatch.sh 6 tranches; inv-28 PAT 3; these asks through 4 re-triggers) — a stale-watch that only re-arms is a calendar, not a gate; no wave acceptance may name a peer actor's action* |
| **ASK-5** (P3; live table :113; targets value.js AND keyframes) | acceptance criterion VERBATIM and machine-checkable: **`color.babb.dev` serves from CF Pages with a `cf-ray` header, peaceiris step removed** — the only mechanical ask, cheapest to close **or decline honestly**. The KEYFRAMES half is KF's mail seat — **DECLARED, not absorbed** (§4b ∦ edge) |
| **INV-22-COLOR** (P3, their G.W8; live table :116) | the `api.color` vhost fails the 4-endpoint contract (`/` 200; `/health`, `/docs`, `/openapi.json` 404). Book the verb + relay; repair is maintainer-owned infrastructure **OUTSIDE F bounds**; edge to F.W9's deploy spine declared (silent-rollback chronic) |
| **CASCADE-VJS** (P2, their H.W6; fix at `H/waves/W6-W7-epsilon-booking.md §2A`; live table :118) | **SPLIT THE ASK**: unplugin-vue-markdown `^29.2.0→^32.0.0` + lockfile regen = ADOPTABLE; the `file:`→published limb = **DECLINED — conflicts with R's Q4 ratification (keep `file:` deliberately, `R.md §3.4`)**. Verbatim: "respond, don't silently ignore." **THE REPLY EXISTS** (`R/letters/CASCADE-VJS-RESPONSE.md`, tracked at COORDINATION-ANALYSIS E13), untracked in fourier's tree — **reconciliation is LANDING IT IN THEIR LEDGER, never re-authoring, never a fresh letter**. **Dissent PRESERVED — the conflict IS the dissent: both positions ratified in their own trees; decline one limb with rationale; never resolve by silence.** Loop closes in BOTH ledgers (the X·V W4-D2 lesson, now law) |
| **FN-1..FN-7** (fourier `N/VALUEJS-R-UPLIFT-ASKS.md` `83d4c9f`; all seven unanswered since 2026-07-03) | answer all seven with a disposition verb. **STANDING RIDER, VERBATIM AND LOAD-BEARING: "FN-5 must be authored BEFORE or WITH fourier M.W10"** — M.W10 is exactly the version-shape transpose FN-5 guards; M.W10 is `planned`, FN-5 does not exist — **the one non-answer with a dated consequence**. FN-5/FN-6/FN-7 are **SS-4 CONTRACT INPUTS**, not coordination trivia; FN-7's two fourier-tree writes happen in fourier's own sub-session (COMMISSION §2) — value.js declares the ask, never makes the edit |
| **VALUEJS-2.0.0-NOTE** (`cd26c65`; the note itself two majors stale) | **RECONCILE FORWARD: the reply states 4.0.0, never the stale 2.0.0** (`web/package.json:18` still `^0.13.0`, re-measured 2026-08-28). The bump is F.W1's `value 0.13→4.0` limb — dischargeable-on-adopt. Paired unstruck row carried: §3's M.W7 `sampleColorRamp` booking still not struck in `M/PROGRESS.md:22`. **Warning carried: value.js said "strike it, it ships" (2026-07-03) then retired the symbol at `164343c1` (2026-07-17) with no correcting letter — inv-32 book-rot running backwards; the reply must not repeat the shape** |
| **O-14 + INBOUND-PARITY** (`V/coordination/INBOX.md:76` · CENSUS §5 [P2] #9) | the correspondence is one-way in fact AND in law: fourier has **no INBOX ledger** (re-measured: `find docs -name INBOX.md` → 0), no CLAUDE.md, replied to none of the three value→fourier files; the facility-19 table sits UNTRACKED (`??`); our INBOX carries zero inbound fourier rows. Two limbs: **(a)** fourier-side INBOX + CLAUDE.md = census F.W0's act in THEIR sub-session — F.W10 relays the ask and **verifies receipt**, never writes their tree; **(b)** value-side: row the four asks + FN charter + 2.0.0 note with verbs so E13's "no wave closes with UNREAD mail" is **CHECKED, not asserted** (§2.8 E-8: the four asks are not unread — they are **UNROWED**, invisible to the E13 sweep). **ADMISSION GAP (census §6): the co-signed contract's signature needs a durable fourier surface — F.W10 CANNOT CLOSE BEFORE F.W0 STANDS IT UP** |
| **G24** (`X/waves/W9.md:366`, anchor re-verified) | **RELAY ONLY — the gate is X-W9's.** Banked: 8/22 curves drift, max\|Δ\| 0.192 vs `<1e-3` over 8 names via `fourier-value-import-drift.mjs`. The relay may NOT quote the banked number as current (**MEASURE-AT-OPEN**), may NOT claim the gate; no fourier-tree edit is credited as an X-W9 gate (W.L5's value half ships in X-W9.f under D-15's grant; "the fourier-tree edits and π evidence are explicitly NOT X-W9 gates") |
| **G33** (`X/waves/W9.md:375`, anchor re-verified; corroborated `W11.md:319`) | relay the **packet + INBOX legs**; F.W1's ATOMIC tri-package transaction turns the pin leg green. **IDENTITY DISCIPLINE ×6: ONE row — fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8, component-altitude INFO, NEVER re-booked per component** (C:B-1, C-2, C-4, C-27, F-1, PaperView C-03 are reproductions; C-03's increment is the live-today framing). Post-window `npm ls` leg is MEASURE-AT-OPEN by construction. **Dissent ADOPTED (fr-CollapsibleSection rider): the ERESOLVE is the UNCOMMITTED M.W1a bump — "red today" is true of the working tree ONLY; ties GAB-13** |
| **NEXT-COMMUNIQUE-CONTRADICTION** (INTAKE-ADJUDICATION `:221` vs COHESION §4a + the live ask sets) | AMEND BY **ADDENDUM-BESIDE** (E-3), never rewriting the dated intake (`git diff --stat` on it stays empty). Every surviving ask into **EXACTLY ONE register**: **glass → SS-6 BH relay** (M1's corrupt dist · L-10/D-M4 ramp headroom + rebaseline, 4 failing light stops incl. 3.58:1 at stop 11 · GAB-2 ask (a) tier-tint home · GAB-9 ask (b) 3.26:1 stacked-badge label rung · AA-3's violet `batch` tone, no home in the 5-member TONES · FR-EQR light `--success` 2.175:1 · PaperView D/M-7 light `--primary` ≡ `--foreground` · fr-AppHeader cm-serif undeclared variable + light `--tier-featured` · FR-GIG-5's observer-root fix) · **latex-paper → the register §2.6 stands up**. **NO GLASS ROW IS EVER CURED CONSUMER-SIDE.** Dissent: none — both statements stand dated: TRUE of the Codex corpus, FALSE of the registry that followed |
| **RELAY-RECEIPT-LAW** (CONTRIVANCE-REGISTER:180 · EXHORTATION-CENSUS:125 · glass-forward-compliance §2, U-1..U-11; MAJOR, mechanism family — DISEASE-REGISTRY) | Root cause adopted as census law: **"no value.js gate goes RED when a relay does not leave."** Structural cure adopted verbatim: **(a)** an adjudication declaring a GLASS-OWNED/RELAY/LATEX-RELAY arm may not CLOSE until a packet file exists under the producer's inbox path **CONTAINING THE ARM'S IDENTIFYING TERM** (grep-able, L-2-conformant); **(b)** "relay-declared-never-sent" enters DISEASE-REGISTRY. ⟨**MEASURED CORRECTION, this seat, 2026-08-28 — §2.8 E-2: the U-1..U-11 debt is DISCHARGED.** `valuejs-outbound-2026-07-28-o17-full-debt-discharge.md` (76 lines, under `glass-ui/docs/tranches/BJ/coordination/`) is exactly the prescribed O-10a-style amendment and rows **B-1..B-10 naming all eleven arms** (B-1 = U-1+U-5 convergent). The banked "each ZERO across all six sent letters" is stale: **ten** packets now exist. **The law survives whole and is what proved the discharge — a receipt read, not asserted**⟩. **This is F.W10's OWN close-gate law and binds the wave to itself: a relay whose receipt is ASSERTED rather than VERIFIED fails G-F10-12.** "Nothing in the debt blocks glass 8.0.0; everything in it rots if 8.0.0 ships first" |
| **MG-ι** (fr-App:82/:143, MINOR) | fourier-side remedy constraint → **F.W1** (with C-2): the sound path is `colorUnit2`; `color2` NEVER (`color2(parseCSSColor(x))` THROWS; the `.value` unwrap SILENTLY RETURNS WRONG CHANNELS); `light-dark()` still needs the used-value probe. **The value.js-side limb (the `color2` dispatch mis-reading its sibling parser) is NO-WAVE-OWNER in the F-taxonomy — F.W10 relays it into the V·π/parser-seam ledger WITH THE OWNER-MAIL FLAG**; no fourier wave adopts it. **ROUTE LAW: direct parse-that → fourier FORBIDDEN** — parser → value (X·V L1/L5) → packed release → consumers; this wave mints no such edge |
| **GAB-29** (fr-GalleryAdminBanner:74; INFO — **cure-falsifier, NOT a destination**) | MetricPill is barrel-only-reachable and has **NO source at v8.0.0** (dies with MetricBadge) — "the wave must not adopt it off the name either." Terminal record, **LOAD-BEARING for F.W1's budget**: the F.W1 seat remains K-4's ruling — `<Metric posture="cell">` from `./metric` at 8.0.0. Keeps the wave off TWO dead primitives |

### §2.5 The NO-WAVE-OWNER drain, F-side (G-F10-7; COHESION §3.3 deadline — F.W10 is the last F wave, nothing leaves it un-homed)

Verbs are exactly three: **NAMED WAVE** · **NEXT-BOUNDARY LEDGER ROW** · **KILL-WITH-RATIONALE**. *A closed question and a negative finding are terminal dispositions too — record them, manufacture no work.*

| row (banked id, kept) | mechanism in one line | terminal verb |
|---|---|---|
| **AA-45** (fr-AdminAuditLog:88) | positional `:key` **FORCED by the contract** (no wire identity; pydantic drops `_id`); hazard unreachable (wholesale assignments only) | **NEXT-BOUNDARY LEDGER** — API-contract row (identity on the wire), SS-3/SS-4 input |
| **AA-46** (:89) | envelope `page`/`pages` discarded, re-derived client-side; lossless round-trip today | **KILL-WITH-RATIONALE** — a redundancy record; no defect, no work |
| **AA-47** (:90) | no runtime boundary validation; pydantic guarantees shape; survives as UNEVEN defensiveness (timestamp/target guarded; action/ip_hash not) | **NEXT-BOUNDARY LEDGER** — SS-4 contract input |
| **FR-AFP-51** (:112) | the census ToastVariant break is **UPLIFT-TARGET**, not present-tense | **BOOKED HERE**: tense correction lands in the CENSUS-ADDENDUM (§1a #8) and **must reach whatever spec quotes the census break table** — else the uplift budget reads a break that does not exist |
| **FR-AFP-52** (:113) | D grades a surface FR-AFP-1 proves unreachable; the corpus never states its conditional | **NEXT-BOUNDARY LEDGER** — corpus-composition input for SS-3/SS-4 |
| **FR-AUL-16** (fr-AdminUserList:54) | the only cascade-delete surface, zero coverage; "un-harnessable" defence KILLED (ruling 3e) | **NAMED WAVES, decided here**: harness decision → F.W4 · devDependency + admin-tab axe limb → F.W9. **If SS-4 declines the widening, the decline is recorded explicitly with rationale (M-25).** C-B2's BLOCKER demoted-and-folded here |
| **fr-App i-2** (:143) | route-owned surface, no component-level home | **NEXT-BOUNDARY LEDGER** — the per-route challenge corpus |
| **fr-App C-12** (:90/:143) | App is **API-INERT** — the negative guard | **KILL-WITH-RATIONALE — as the NEGATIVE RECORD, verbatim**: "R6-8 and R3-7c have NO leaf in this component — a per-component sweep must not manufacture one." It keeps F.W5–W8's client-edge sweep off a false leaf |
| **MG-ι** value-side limb | see §2.4 | **NEXT-BOUNDARY LEDGER — the V·π/parser-seam ledger, WITH THE OWNER-MAIL FLAG** |
| **FR-AH-30** (fr-AppHeader:88) | the D-axis's own `@layer components` remedy **REVERSES the outcome** (glass's competing rules already there at 0-3-0 vs 0-2-0 — the consumer block silently LOSES) | **NAMED WAVE (cure-constraint, F.W4 where FR-AH-11 lands)**: only `@layer glass-overrides` **APPENDED AFTER `utilities`** works, and it is stated. "Cure adjudicated separately from the defect" law |
| **FR-AH-45** (:108) | zero props/emits/slots — a question owed an answer | **NEXT-BOUNDARY LEDGER** — SS-3/SS-4 spec-input; discharges together with FR-AH-30 |
| **FR-CP-43** (fr-CoefficientsPanel:81) | canvas renders/hit-tests 40 units in imperative loops, no template node to key — instance censuses read 1 where there are 3 | **KILL-WITH-RATIONALE** — it **COMPLETES** R5-7/R6-6 + fr-BasisCanvas L§R5-7's demand; discharged, no work remains |
| **FR-CP-44** (:82) | lane-frontend:443's metric-stack shadow candidacy seated on two hosts neither of which renders a metric; belongs on CoefficientsSpectrum.vue | **BOOKED HERE** (census correction → CENSUS-ADDENDUM) **+ F.W3 CANDIDATE** for the re-seat (FR-CP-3's candidate cure; "by construction" overclaim noted) |
| **FR-CP-45** (:83) | D's line citations drift ≈−10/−2; L and C cite accurately | **ADOPTED AS LAW BY THIS SPEC**, verbatim: "take D's CLAIMS and L/C's LINES; re-resolve every anchor before quoting into a wave spec." **Binds every anchor this file quotes — including its own** (§2.8 E-4) |
| **fr-EasingPicker D/D-21** (:101) | empty/loading/error genuinely N/A (compile-time catalogue — CLOSED, not skipped); the focus-ring question MOOT once D-1's cure lands | **KILL-WITH-RATIONALE** — SS-13 residue only |
| **PAW-32** (:88) | `PaperSectionBlocks.vue.d.ts` ships no `__VLS_Slots` (sibling ships 2) — `figure.*`/`callout.*` implicit `any` | **NAMED REGISTER — LATEX-PAPER-RELAY** (§2.6); producer gap, no consumer cure |
| **PAW-38** (:94) | un-deduplicated title slugs collide across FOUR registries at once; LATENT (93/93 unique, `uniq -d` empty) | **NAMED REGISTER — LATEX-PAPER-RELAY**, joint P-6/P-12: the cheapest guard is precisely the harness PAW-50 proves absent. L-17 books only cross-consumer; this intra-document case was unbooked by all three axes |
| **PAW-39** (:95) | 51 `\section` + 31 `\subsection` flattened to one heading level; the flattener EMITS `sourceLevel` (:110) the renderer never reads | **NAMED REGISTER — LATEX-PAPER-RELAY**. **CENSUS LAW: 11 + 51 + 31 = 93 brace-anchored headings; quote 31, NEVER the readers' un-anchored 35.** Dissent carried: the a11y composite (PAW-15∘PAW-11∘PAW-7∘PAW-39) approaches blocker weight AS A CLUSTER with no single row carrying it |
| **PAW-42** (:98) | no empty/loading/error state — **PROVEN UNREACHABLE** (build-time `virtual:paper-content`) | **KILL-WITH-RATIONALE** — epistemic record; an unreachable state is a CLOSED question |
| **PAW-45 / PAW-46** (:195/:196) | the module-scoped-cache trio's latex halves (§2.6) | **NAMED REGISTER — LATEX-PAPER-RELAY** |
| **GAB-29** (:74) | see §2.4 | **KILL-AS-DESTINATION, RECORDED AS FALSIFIER** — load-bearing for F.W1's budget |
| **FR-COB-20** (fr-CanvasOverlayButton:74) | interaction contract entirely implicit; "dies with the file"; the tree favours DELETE | **NAMED WAVE — F.W0's F8-REACH-02 verb**; F.W10 verifies no HOLD survives to close (that verification is this wave's entire claim — not a re-booking) |

**Twenty-three rows, twenty-three verbs, zero left open.** COHESION §4 gains a fourier block carrying exactly this table.

### §2.6 The LATEX-PAPER-RELAY roster (bounds #2; G-F10-5 — P-6's terminal home)

**LATEX-RELAY-HOME** (BLOCKER for the P-6 packet). Re-measured 2026-08-28: `grep -c 'latex-paper' docs/tranches/V/coordination/INBOX.md` → **0**; COHESION §4a is **glass-only**; ≥18 named latex-paper carries across three records route to a register that **does not exist** — and the producer tree has no coordination/inbox path either (§2.8 E-7), so the register lives **in our tree with its delivery point named**. F.W10 creates `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md`, rows every carry with banked ids kept and none re-booked, then **answers PAW-1's own F.W1 question IN THE LEDGER — latex-paper added to the tri-package transaction, or explicitly seated.** **ANCHOR LAW (SC-3), verified live**: installed latex-paper **0.2.1** ships no composable src (`src/vue/` holds `theme.css` only) — every virtualiser anchor cites `dist/vue.js` or the record; producer-src paths cite bytes the consumer never runs. The P-6 letters SEND at F.W0 on the same mail seat as P-1; F.W10 owns RECONCILIATION + TERMINAL DISPOSITION, declared from BOTH ends (COHESION §2).

| carry (banked ids) | content + locks |
|---|---|
| **PAW-1** (:42, BLOCKER sustained R-2 on seven arms) | all **42** `hsl(var(--x))` declarations IACVT-dropped; seat-verified **VERSION-INVARIANT at 4.0.0/7.0.0/8.0.0 — the tri-package uplift cures nothing here**. Carry: rewrite the 42 sites to `var(--x)` — the SOUND limb. **KILLED (K-9): the `:root` triplet-alias limb — the repair MUST NOT implement the literal snippet** (breaks PaperView :524, style.css :110-112, :182/:184, glass recipes). **F.W1 LEDGER RIDER VERBATIM: "the transaction as chartered is INSUFFICIENT for the paper route — latex-paper is a FOURTH package the constellation must reconcile — add the LATEX-PAPER carry to the transaction or seat it explicitly."** **PAW-30 lands in the SAME REPAIR**; **LAW-3**: PAW-44's `overflow-x: clip` restoration lands WITH-or-AFTER PAW-1 + PAW-30, NEVER before (else the K-18-killed see-through-header catastrophe becomes REAL). E-3 re-scope stands: sticky-scroll arm K-18-dead; theme :447/:459 re-filed LATENT-behind-PAW-44. Dissent: R-2 killed the blocker's most vivid arm; severity sustained on the seven survivors — **a repair reasoning from the killed arm ships a no-op** |
| **PAW-2** (:48, MAJOR, demoted BLOCKER→MAJOR at R-1; absorbs D-i-5) | never-painted `.deferred-section` wrappers report the 1200px estimate; `syncMeasuredHeight` writes it ungated into the never-invalidated `SESSION_HEIGHT_CACHE`. Cure: gate on the CV-state event, or skip when `offsetHeight` equals the estimate, or move `.deferred-section` off the measured element. **K-19: the identity guard at dist :869 is the EXACT repair line.** K-16: co-landing rhythm cures MEASUREMENT-SAFE only (inter-wrapper margin → ≈2.9k px drift). **COUPLING LAW VERBATIM: "the per-patch ref re-read is currently the only repair mechanism — fix this BEFORE memoising the ref (PAW-4)."** Dissent: D/C filed BLOCKER; demotion sustained by fresh derivation (K-6: navigation IS compensated; the un-compensated residue carries the MAJOR) |
| **PAW-11** (:57; premise corrected K-4) | `\ref`/`\eqref` render as plain text AND are href-less `<a>` (**5 emitters, NOT spans**) — invisible-but-live, unreachable to keyboard/AT. Visuals ride PAW-1; **the focusability arm needs real `href`/tabindex semantics AT THE EMITTER — cannot be shimmed consumer-side.** Rider on PAW-1's letter |
| **PAW-28 ⊕ PAW-45 ⊕ PAW-46** (≡ PaperView L/D21) (:79/:195/:196 · fr-PaperView:130 — three module-scoped-cache rows, **ONE letter**) | PAW-28: instance-scope or namespace the height cache (deliberately excluded from teardown; dissent: DU graded INFO, ruled MINOR at R-6). PAW-45: **bound or instance-scope the KaTeX memo — CONSTRAINT TRAVELS WITH THE ASK: superlative 13 records the memo makes PAW-3/PAW-4 churn survivable — CAP, DON'T DELETE.** PAW-46: key on tex + macro identity, or move the Map inside `useKatex`. **PAW-46 ≡ L/D21 — ONE mechanism banked twice; carried ONCE, both ids cited, NEITHER re-booked**; L/D21's surviving increment: the docblock promises an `inject` fallback the code lacks. 45/46 LATENT (one `useKatex` call site today, PaperView :38) |
| **PAW-32** (:88) | emit the slot types (`__VLS_Slots`). P-6 member; NO-WAVE-OWNER locally |
| **PAW-38** (:94) | uniquify slugs at the emitter. Joint P-6/P-12 row |
| **PAW-39** (:95; census seat-corrected SC-2) | read `sourceLevel`; restore the second outline level. Census law: **quote 31, never 35** |
| **K-11-FLATTENER-RIDER** (:115, re-graded K-18 :206; MAJOR cure-integrity, not a defect row) | flat-peers nesting is the **SECOND** of two gates on the two-tier sticky stack; **THE SCROLLPORT IS THE FIRST (PAW-44); reworking the flattener alone ships a no-op.** Whether the stack returns is a formation design decision — SS-13 witnesses first. Bound to LAW-3 and **LAW-4 (ONE CLEARANCE AUTHORITY)**: PAW-47's clearance constant is a DEPENDENT of the PAW-44 decision (stack restored → clearance ≥ stack height; retired → clear the 32px scrim + record theme's three dead `scroll-margin-top` rules retired-with-rationale). K-18 re-grades banked K-11: a repair reading the banked cell alone ships a no-op |
| **C-06** (fr-PaperView:70, MAJOR) | 0.2.1 ships `activeId`/`activeRootId` as `ComputedRef<any>` (d.ts:19) — six typed contracts vacuous, invisible to `vue-tsc -b`. Relay: fix at `ref<T\|null>`. **+ F.W1 rider** — why PAW-1's fourth-package ask is not optional |
| **L/D5 + L/D30** (fr-PaperView:75, MAJOR) | two GUARANTEED no-op teardowns (`onUnmounted` re-reads a nulled template ref). Consumer half → F.W4; **producer twin RELAYED WITH the consumer half so neither re-introduces the other.** Cure = `onBeforeUnmount` (LC's one-word fix, canonical) or the cached-element idiom (ships in the same package at S9) |
| **L/D8** (fr-PaperView:77, MAJOR) | `:ref` re-invoked per patch; `el.offsetHeight` evaluated AS THE ARGUMENT, before the dedupe — one forced layout per visible section per patch; the guarded twin sits 300 lines away. Consumer guard → F.W4; relay: hoist the read behind the guard at the producer. **SAME FAMILY as PAW-4(b) — cite both, book once.** PAW-2's coupling law binds |
| **L/D29** (fr-PaperView:92, MINOR) | `useTreeIndex` records the ROOT id as `parentId` for depth≥2 (:27/:32; masked by `isDescendant`, 34 nodes live) — latent, correctly filed; the same package's other walk gets it right |
| **C-19 ⊕ PAW-33** (fr-PaperView:125 · fr-PaperArticleWindow:89 — **ONE fact, two banked ids**) | declared peers silently violated at the installed pin: katex `^0.16` vs **0.17.0**; vite `^6\|\|^7` vs **8.0.16**; all `optional: true`, zero npm signal. → **F.W1** (pin-truth) + relay; feeds G33's pin leg. **BOTH CITED, NEITHER RE-BOOKED** (PAW-33 carries the vite half C-19 omits). **DISTINCT from the glass/keyframes/value peer deadlock — never folded.** Companion ★MF-12 (dual katex ambient type sources, skipLibCheck masking) → F.W1's manifest pass |
| **C-20** (fr-PaperView:126; headroom cell excised K-6) | `hsl(var(--section-heading))` IACVT at SIX theme sites — **a NAMED SUBSET of PAW-1's 42; discharges INSIDE PAW-1's carry, carried separately ONLY so the 6-site cell survives the 42-count quote.** Consumer sibling (PaperSidebar L-10 ≡ ★MF-10): fallback-less `var(--section-color-${si})`; the one-line cure `var(--section-color-${si % 13}, var(--foreground))` is **F.W4's**, with root-count assertion — the ramp is EXACTLY saturated 13/13. GLASS-RELAY rider only if MORE stops are wanted upstream. Dissent: K-6 killed both headroom cells (ZERO headroom; stop 5 already app-forked) |
| **L-11 + M2** (fr-PaperSidebar:40/:62 — **ONE relay carries both**) | L-11: add a PRM gate/motion option to the damped-rAF follow (a THIRD ungated clock — **amend lane-frontend §8's inventory**). M2 (MAJOR): replace the hard-coded `.sidebar-top-btn` in the producer's pointer-exemption with a **DATA-ATTRIBUTE hook**. **HARD LOCK VERBATIM: "F.W4 must not rename the class before the relay lands"** — renaming silently converts scroll-to-top clicks into sticky manual-override suspensions |

### §2.7 Carried for parity — NOT booked by F.W10 (cited so no seat re-books them)

| row | where booked | why carried here |
|---|---|---|
| **FR-GIG-5** (fr-GalleryInfiniteGrid:47, MAJOR, cure-integrity; re-proven at the tag R-2) | **F.W1's spec (the lock) + SS-6 (the producer ask)** | the tri-package bump does **NOT** cure the pagination drain (observer-root geometry byte-identical 4.0.0→7.0.0→8.0.0; a non-clipped element root always delivers an initial `isIntersecting: true`). **AN EXPLICIT F.W1 SPEC FOOTNOTE IS MANDATORY: the uplift must NOT be credited with curing the drain.** F.W10 verifies the footnote exists and the relay left (G-F10-12). Dissent: three BLOCKER dissents ride parent FR-GIG-1; the L axis's carry was the wrong one, killed at K-4 |
| **FR-EQR-3** (fr-EquationResult:38, MAJOR sequencing; folds onto AA-2, not a re-book) | **F.W1's spec** | the producer's DEV-mode retired-prop `console.error` meets four zero-console-error e2e gates on a DEV-server matrix. **F.W1 IS ATOMIC LAND-OR-LOSE** (bump + 162-site prop rewrite + `copied`→`status` triple in ONE change). Witness for F.W1's spec: one dev-server run of the four gated specs at the adopted pin, before-and-with the rewrite. **F.W10 schedules NOTHING that presumes the uplifted tree.** Interacts with GCM-42 |
| **PAW-49** ⟨re-cut → F.W0⟩ | F.W0 | the one coverage-family row whose gate is FREE; flag-before-split governs PAW-12 |
| **PAW-50's DECISION leg** ⟨re-cut → F.W4⟩ | F.W4 (census §4.5) | **F.W10 cannot book a coverage row terminal while the harness decision is unmade** |
| The 23 F.W9-cut execution limbs (§2.2) | F.W9-widened / F.W9a | F.W10 owns **only** the terminal record; the same §2.2 table sits in both specs |

### §2.8 Measured corrections and errata (E-3 shape — recorded BESIDE; dated bytes never rewritten; every anchor re-resolved per FR-CP-45's law)

| # | finding (re-measured by this seat, 2026-08-28) | disposition |
|---|---|---|
| **E-1** (denominator sharpening, not a contradiction) | The CARRY quotes ADOPTION-ASKS as *43 rows / 12 OPEN / 11 naming value.js*. Measured on the **live re-triggered table** (G.W8 stamp at `:105`): **11 id-rows, ALL 11 OPEN, FOUR against value.js** — Ask 3 (:111, P1) · Ask 5 (:113, P3) · inv-22-color (:116, P3) · cascade-vjs (:118, P2); `cascade-gui` (:120) **targets glass-ui** and is never counted as ours | Quote the live-table **11/11/4** for the gate and the whole-file census for the history |
| **E-2** (**material erratum — the CARRY's G-F10-12 witness is stale**) | Banked: *"eleven arms (U-1..U-11) … each ZERO by term-sweep across all six sent letters."* Measured: **TEN** `valuejs-outbound-*.md` packets exist; `valuejs-outbound-2026-07-28-o17-full-debt-discharge.md` (76 lines) rows **B-1..B-10 naming all eleven arms** (B-1 = U-1+U-5 convergent) — the prescribed O-10a-style amendment, delivered | **The DEBT is DISCHARGED; the LAW is NOT.** G-F10-12 is born **SPLIT**: U-arms GREEN by verified receipt; every **F-side** arm RED — term-sweep across all ten packets: `@source` **0** · `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0** (the few `fourier` mentions sit in three non-F packets, none an ask). The mechanism family still enters DISEASE-REGISTRY — ancestry unchanged by one successful discharge |
| **E-3** (twin exists; bounds direction settled) | The 2026-08-25 fold's G-F10-6 witness read *"`F-W9.md` does not exist yet."* It **EXISTS** (fold-authored 2026-08-28), carries §2.2 **spliced verbatim**, and its §1a row 2 takes **MODIFY** on the census addendum, stating "F-W10 §1 already claims the CREATE" | G-F10-6 gains a runnable falsifier leg: the extracted §2.2 shared regions of the two files **`diff` empty**. The Opus draft's bounds flip (F.W10 MODIFY) is **REJECTED against the twin's measured bytes** — the CREATE stays at §1a #8 |
| **E-4** (anchor corrections) | Census §6 is titled **"THE ADMISSION GAP"** at `CENSUS-2026-08-03.md:263` — there is no §6.6; the §6.7 CORRECTION block sits at **:310-316** and reads verbatim *"Zero UNVERIFIED credit — every Codex verdict enters the formation through per-claim adjudication (M-21)"*. `INTAKE-ADJUDICATION-2026-08-03.md`'s `### NEXT-COMMUNIQUE` is at **:221**. `X/waves/W9.md` G24 at **:366** / G33 at **:375** re-resolve exactly | Corrected anchors used throughout. Mechanism unchanged in every case — the anchors drifted, the facts did not |
| **E-5** (**PAIRED defect inside the shared §2.2 bytes**) | The AA-44 cell reads *"9 e2e never enter the audit tab"* while the FR-AFP-42/-49 cell two rows below states **"Quote 8 specs, never 9 (K11)"**. Re-measured at fourier HEAD: `ls web/e2e/*.spec.ts` → **8** | The correction is a **PAIRED edit landing in BOTH specs in ONE commit** — a unilateral fix breaks G-F10-6/G-F9-21's Δ=∅. Until it lands, both specs carry the byte-identical table **including the defect**; this row is the disclosure (mirrored at F-W9 §2.8 E-1). **No prose outside the shared bytes quotes 9** |
| **E-6** (count drift, mechanism intact — **both drafts corrected**) | The CARRY says *"Eleven `FOURIER-R*` coordinate files"*; the Opus draft says 10 incl. an `R2A`. Measured by the literal glob: **9** (R3 ×2 · R4 ×3 · R5 ×2 · R6 ×2) in `megatranche/coordination/`, **no R2A**, and the auxiliary eight-hour delta file does not match the glob | **Quote 9 by the glob.** The load-bearing fact — **none in fourier's tree** (re-measured: 0) — is unchanged |
| **E-7** (SC-3 verified live; the producer end has no inbox either) | Installed latex-paper **0.2.1**: `src/vue/` contains `theme.css` **only**. `/Users/mkbabb/Programming/latex-paper/docs/` holds `tranches/` + `virtual-paper.md` — **no coordination/ or inbox path anywhere** | The relay register lives **in our tree** (bounds #2) **with its delivery point named** — placing a packet where the recipient does not look is the recorded failure mode (the O-15 lesson) |
| **E-8** (E13 status — worse than unread) | `grep -c 'UNREAD' INBOX.md` → **4, all law-text; zero rows carry status UNREAD**. `grep -ni fourier` → **6 rows** (I-14, O-14, O-10a, I-24, I-24a, I-25), none of the four asks, none inbound | The four ADOPTION-ASKS are not unread — they are **UNROWED**, invisible to the E13 sweep. G-F10-1/G-F10-2's falsifiers therefore require **rows**, not reads |
| **E-9** (Codex residue re-confirmed) | `/tmp/fourier-r4-files.sha256` → *No such file or directory* (**EVAPORATED**); `~/.codex/worktrees/d0be/fourier-analysis` → **still registered** | R4-12's consume-nothing lock stands and is moot; the worktree still owes a disposition verb (G-F10-8) |

---

## §3 Gates

**Twelve. Every one born RED with its failing probe measured read-only 2026-08-28 and named (L-19); no gate is a proof-farm script.** G-F10-12 is born **SPLIT** — one half GREEN by a verified receipt, the other RED — and says so honestly rather than averaging into a false colour.

| gate | born state · witness | falsifier (turns it GREEN) |
|---|---|---|
| **G-F10-1** ADOPTION-ASKS reconciled in BOTH ledgers | **RED [measured]** — the live re-triggered table: **11 id-rows, all OPEN, FOUR against value.js** (Ask 3 :111 P1, *"the hardest of the four and the true critical-path item"* · Ask 5 :113 P3 · inv-22-color :116 P3 · cascade-vjs :118 P2). Value-side `grep -ni fourier docs/tranches/V/coordination/INBOX.md` → **6 rows, none of the four, none inbound** (E-8: UNROWED) | each of the four carries an **INBOX row with a disposition verb** AND the reply landed in fourier's ledger; cascade-vjs answered as the **SPLIT** using the EXISTING `R/letters/CASCADE-VJS-RESPONSE.md` — never a fresh letter, never a write into fourier's tree. Falsified by: a verb naming a peer actor's action as an acceptance condition |
| **G-F10-2** Inbound-mail parity | **RED [measured at fourier HEAD `cd26c65`]** — no CLAUDE.md; `find docs -name INBOX.md` → **0**; the facility-19 letter UNTRACKED (`??`); the three value→fourier files (`83d4c9f` FN charter · `cd26c65` 2.0.0 note · facility-19) unanswered; our INBOX carries zero inbound rows | FN-1..FN-7 each carry a **fourier-side verb**; facility-19 **TRACKED**; the fourier INBOX **exists** (census F.W0's act, their sub-session) with ≥1 row this wave can point to. **FN-5's rider — "BEFORE or WITH fourier M.W10" — answered in that order or explicitly re-scoped** |
| **G-F10-3** G24 drift RELAYED, never inherited, never owned | **RED — MEASURE-AT-OPEN** — `fourier-value-import-drift.mjs` at `X/waves/W9.md:366` (anchor re-verified); banked 8/22 curves drift, max\|Δ\| **0.192** vs `<1e-3` over 8 names | max\|Δ\| **< 1e-3** across the 8 names **RE-MEASURED AT WAVE OPEN**. Falsified by: quoting the banked number as current; claiming the gate (it is X-W9's); crediting any fourier-tree edit as an X-W9 gate |
| **G-F10-4** G33 packets sent + post-window pin truth | **RED [measured]** — zero packets; fourier `web/package.json:18` `"^0.13.0"` (re-verified 2026-08-28) vs published 4.0.0; keyframes pins `"4.0.0"` exactly; `npm ls @mkbabb/value.js` in fourier exits ELSPROBLEMS (glass 4.0.0 peers `^0.10.0\|\|^0.11.0` vs 0.13.0; keyframes 4.3.0 hard-deps `^0.13.0` — disjoint), reproduced ×6 across the registry | **five packets + five INBOX rows + a post-window `npm ls @mkbabb/value.js` exiting 0 in BOTH keyframes and fourier**; the pin leg greens only via F.W1's atomic transaction. Falsified by: re-booking per component (ONE identity, C-1 ≡ C-8); reading the post-window leg before the window; dropping the uncommitted-bump dissent |
| **G-F10-5** The LATEX-PAPER carry has a receiving surface | **RED [measured]** — `grep -c 'latex-paper' docs/tranches/V/coordination/INBOX.md` → **0**; COHESION §4a glass-only; ≥18 named carries route to a register that does not exist; the producer tree has **no inbox path either** (E-7); `latex-paper` → **0** across all ten sent packets (E-2) | `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` exists with **every §2.6 carry rowed** AND **PAW-1's F.W1 question answered in the ledger** (fourth package added, or explicitly seated), **with a named delivery point**. Falsified by: any anchor from producer-src rather than `dist/vue.js` (SC-3); the K-9-killed `:root` alias snippet appearing as a prescribed repair |
| **G-F10-6** (≡ **G-F9-21**) The re-cut is complete, Δ=∅ both directions | **RED [measured]** — `grep -o 'F\.W9/W10' registry/adjudicated/fr-*.md \| wc -l` → **54** across **25** of **66**; bare `F.W10` → **0**. No adjudicated row is provably homed here today | all **26** routing identities cut to exactly one of F.W9 / F.W10 / a named other wave; **both specs carry the same §2.2 table**; set-difference ∅ both directions; and (E-3) the extracted §2.2 shared regions **`diff` empty**. Falsified by: a row minted from the 14 boilerplate lines or the 7 boilerplate-only records; a row re-booked under a new id; **ANY edit to `fr-*.md`** |
| **G-F10-7** The NO-WAVE-OWNER register drained on the F side | **RED [measured]** — COHESION §4 is live-seeded from SS-7 batch 1, every row an X·V slug, **ZERO fourier rows**, against the **23 owed** (§2.5) | §4 carries a **fourier block** and every one of the 23 rows bears a TERMINAL verb — named wave / next-boundary ledger row / kill-with-rationale; **fr-App C-12 lands as the NEGATIVE record verbatim**. Falsified by: a verb that reads as a decision while being a deferral |
| **G-F10-8** The Codex residue is not inherited; the owner rulings answered | **RED [measured]** — the census §6.7 charter line appears in no F spec; **OG-F1/OG-F2 unanswered**; `/tmp/fourier-r4-files.sha256` **GONE**; the Codex worktree **still registered**; **9** `FOURIER-R*` files in our tree (E-6), **0** in fourier's | the charter line lands in the **CORRECTED wording** ("Zero UNVERIFIED credit — every Codex verdict enters through per-claim adjudication (M-21)"), never the superseded "carry zero credit"; **OG-F1/OG-F2 RULED BY THE OWNER** (F.W10 verifies terminal, does not re-rule); R3-9, the R3-16+R4-11+R5-9+R6-10 board row, R4-12, R5-3, and the worktree each carry a terminal verb; **NONE of R4-12's rows consumed** |
| **G-F10-9** NEXT-COMMUNIQUE ledger truth | **RED [measured]** — `INTAKE-ADJUDICATION-2026-08-03.md:221` declares the channel EMPTY while COHESION §4a carries SC-1..SC-8 and the fourier registry adds the live glass + latex ask sets (§2.4 roster) | an **addendum-beside** amends without touching the dated bytes (`git diff --stat` on the intake stays **empty**), and every surviving ask sits in **EXACTLY ONE register** — glass → SS-6, latex → G-F10-5's register. Falsified by **any glass row cured consumer-side** |
| **G-F10-10** Close-gate a11y honesty — the FR-TT-1 promotion lock | **RED [measured]** — the Tooltip shim is the ONLY label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring a **description, never a name**; banked promotion note *"BLOCKER at any F.W9/W10 axe close-gate"*; today `grep -rln "admin" web/e2e/` → **0 files**, no axe visits `/paper`, `/equation` = one assertion-free screenshot slug across **8** specs (E-5) | the close axe run covers **`/paper` + `/equation` + the admin tab with zero serious/critical**, OR the 17 naming legs are recorded as a **NAMED, DATED WAIVER**. **Never argued green while the legs are open** — an axe close-gate authored before F.W4's naming legs land is RED by construction. The FR-EMT-11 rider binds the same way: MINOR is not "coverage debt is small" |
| **G-F10-11** F.W0 substrate is terminal before the close | **RED [measured]** — **M1**: glass-ui 4.0.0 `dist/styles/index.css` → `postcss.parse` **35 top-level nodes, ZERO valid `@source`**, the `` source` `` at-rule :203–222 · **GAB-13**: `git status --porcelain \| wc -l` → **exactly 28** (incl. the subject and `web/package.json`) · **F8-REACH-01/02**: both un-dispositioned; **HOLD is RED by the source's own law** | **M1** relayed to the glass BH/BJ inbox and the dist parses (or the pin moves); the WT diff **landed OR explicitly recorded as the audit baseline**; both F8-REACH rows carry an exact `KEEP_WITH_MOUNT` \| `ISOLATED_HARNESS` \| `DELETE` with **no HOLD surviving**. F.W0 rules; F.W10 verifies |
| **G-F10-12** Every declared relay arm has a grep-able receipt — the structural cure for "no value.js gate goes RED when a relay does not leave" | **SPLIT, born honestly [measured]** — **GREEN-BY-VERIFIED-RECEIPT** on U-1..U-11: `valuejs-outbound-2026-07-28-o17-full-debt-discharge.md` under `glass-ui/docs/tranches/BJ/coordination/` rows **B-1..B-10 naming all eleven arms** (E-2; the banked "six letters, each zero" is stale — **ten** packets exist). **RED [measured]** on every **F-side** arm: term-sweep across all ten packets → `@source` **0** · `MetricPill` **0** · `IntersectionObserver` **0** · `latex-paper` **0**; zero latex-paper packets exist anywhere | for **EVERY** arm this sub-tranche declares GLASS-OWNED / RELAY / LATEX-RELAY, **a packet file exists under the producer's inbox path CONTAINING THAT ARM'S IDENTIFYING TERM** (grep-able, L-2-conformant). Falsified by: a receipt asserted rather than verified; a relay counted green because a letter was authored but not landed under the producer's path; an arm folded into a batch whose text never names its term. **"Relay-declared-never-sent" enters DISEASE-REGISTRY regardless of the O-17 discharge** |

---

## §4 Sequencing

### §4a Ordering locks (carried whole; each lands where its rows land)

1. **RE-CUT-0 PRECEDES EVERY OTHER ROW.** Neither F.W9 nor F.W10 stamps before G-F10-6 ≡ G-F9-21 closes. **F.W10 does not author F.W9**; any change to the shared §2.2 bytes is a **paired edit in one commit**.
2. **F.W0 SUBSTRATE PRE-GATES FIRST** — M1's corrupt dist · GAB-13's dirty worktree · F8-REACH-01/02's HOLD · the Codex residue with OG-F1/OG-F2 flagged · **MG-θ ≡ M7's tsconfig one-liners (ONE substrate edit, TWO records, booked ONCE)** · the re-cut PAW-49. Every F.W1/W3/W4 budget is otherwise measured against **a dist that does not parse and a tree no commit holds**.
3. **F.W1 IS ATOMIC LAND-OR-LOSE** (producer bump + 162-site prop rewrite + `copied`→`status` triple in ONE change, else four zero-console-error e2e gates go red for the wrong cause — **FR-EQR-3**) and **PRECEDES F.W3/W4 (§2 law)**. **F.W1 must NOT be credited with the pagination-drain cure (FR-GIG-5)** — F.W10 verifies the mandatory footnote. PAW-1's fourth-package rider: latex-paper added to the transaction or explicitly seated. **F.W10 schedules nothing that presumes the uplifted tree.**
4. **GCM-42 CONSOLE LOCK**: no widening of the error-only filter before F.W1 lands.
5. **HARNESS-BEFORE-RIDER (PAW-50)** governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44 — decision F.W4 · execution F.W9 · terminal record F.W10. **FLAG-BEFORE-SPLIT (PAW-49)** rides it — the split is not type-safe until the flag lands or the return is honestly `number | undefined`. **F.W10 cannot book a coverage row terminal while the harness decision is unmade.**
6. **Repair-arms-a-defect same-commit riders** (carried to F.W3/W4 where their rows land; named here so the close verifies each): fr-PaperSearchModal D-1+D-3+`outline:none` · **PAW-30 in the same repair as PAW-1** · **LAW-3** (PAW-44 restore-only-WITH-or-AFTER PAW-1+PAW-30) · **LAW-4** one clearance authority (PAW-47 dependent of the PAW-44 decision) · MPC-31/MPC-3⊕10⊕13⊕8⊕22 one-cut law · FR-MSP-6 two-channel lock.
7. **PAW-2's COUPLING LAW**: the measurement write is fixed BEFORE the ref is memoised (PAW-4); K-16 confines rhythm cures to measurement-safe forms. Same-family cite-both-book-once with L/D8.
8. **FR-TT-1 PROMOTION LOCK** (G-F10-10): the axe close-gate follows F.W4's 17 naming legs, or declares a named, dated waiver. Never argued green.
9. **AXE-SEAT ECONOMY**: the `/equation` seat is ONE artifact (authored once, cited five times: FR-EQR-6 · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · EquationView D·D-B2/C·D-28); the `/paper` seat is ONE (D-B2 · M7 · PAW-34) and runs AFTER PAW-1's cure + the PAW-44 decision; the admin seat EXTENDS the existing keystone (AA-44). **FR-USB-16's mobile/coarse Playwright project precedes every coarse-pointer witness** (ImageUpload 27's drag path · the glass a11y-overrides coarse floor · PaperSidebar's `--ui-scale:1.5`/`--control-floor:2.75rem` arm) — one matrix change, three unblocked rows.
10. **L-11 + M2 HARD LOCK**: F.W4 must not rename `.sidebar-top-btn` before the latex-paper relay lands.
11. **D/i-1 COUPLING**: coverage rides the B-1/B-2 cure wave — never scheduled independently. **Seam-before-seat** (FR-AFP-42) · **teleport lock** (FV-26/FV-27) · **HOLD-is-RED verification** (F8-REACH → FR-COB-20).
12. **ROUTE LAW**: direct parse-that → fourier FORBIDDEN (parser → value X·V L1/L5 → packed release → consumers) — this wave mints no such edge. Glass rows ride SS-6, never frontend hacks. **MEASURE-AT-OPEN** on every relayed number (G24 · G33's post-window leg · the deploy-of-record). **RELAY-RECEIPT-LAW binds this wave to itself** (G-F10-12).
13. **ADMISSION GAP**: the co-signed contract's signature needs a durable fourier surface — **F.W10 cannot close before F.W0 stands it up** (G-F10-2). **Owner rulings flagged inline, never presumed**: OG-F1 · OG-F2 · the L-4 producer-ownership escalation (F.W10 cannot stamp terminal while unanswered) · SS-4's seven rulings (trie-vs-KISS guardrail-incumbent et al.). **Anchor law**: FR-CP-45's rule governs every anchor this spec quotes; SC-3: latex anchors from `dist/vue.js` only; PAW-39: quote 31, never 35; ImageUpload: 15/6, never re-derived; AA-44: EIGHT specs, never nine (E-5).

### §4b Cross-edges — declared FROM THIS END; each owed a reciprocal sentence in the named file (the X·V W4-D2 lesson, now law)

| to | reciprocal | edge |
|---|---|---|
| **F.W9** (widened, or a minted F.W9a) | **REQUIRED** | the P-12 coverage-void packet **EXECUTES there**; F.W10 owns terminal records only. Handed over by §2.2: AA-44 · FR-AFP-42/49 · FR-GSB-17 · FR-USB-16 · FR-EQR-6 (axe leg) · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · EquationView C·D-28 + D·D-B2's axe leg · GCM-42 · FR-GV-36 · GAB-10's coverage half · FV-26 · App C-13 · ContourPreview 38 · ImageUpload 27 · PAW-34 · PAW-12's rider · PaperView D/i-1 · PaperSidebar D-B2/C-M4/M7 · FR-AUL-16's execution limbs · FR-COB-20's harness branch. **Same table both specs; neither stamps before G-F10-6.** FR-TT-1's F.W9-side share is G-F9-22 |
| **F.W4** | **REQUIRED** | the vitest-floor **DECISION** (PAW-50; census §4.5) · the 17 FR-TT-1 naming legs · the L-4 ToC-collapse execution · PAW-49's F.W3/W4 rider · GAB-10's live-region half riding GAB-4 · FR-EQR-6's attributes · D·D-B2's `aria-label` ×2 · ★MF-10's `si % 13` fallback with root-count assertion · L/D5+L/D30's consumer half. An axe close-gate authored before the naming legs land is RED by construction |
| **F.W0** | **REQUIRED** | substrate terminal-VERIFICATION only (§2.3; G-F10-8/-11): M1 · GAB-13 · F8-REACH-01/02 · the Codex residue with OG-F1/OG-F2 flagged · MG-θ ≡ M7 · re-cut PAW-49. **F.W10 verifies; it does not re-rule** |
| **F.W1** | **REQUIRED** | PAW-1's ledger rider (the fourth-package ask) · C-19 ⊕ PAW-33 pin truth + ★MF-12 · C-06's `ComputedRef<any>` rider · the L-4 escalation · M3's pre-gate discriminator · the 2.0.0 answer stated AT 4.0.0 · MG-ι's remedy constraint (`colorUnit2`, never `color2`) · FR-GIG-5's mandatory footnote · FR-EQR-3's atomicity |
| **F.W3** | declared | FR-TT-1's shim leg keeps its R3-7a/X-11 identity (`ui/tooltip` thin-adapter: 35 callsites / 9 consumers, barrel → `@mkbabb/glass-ui/tooltip`) · ★MF-11's dead-API clean before the `./search` shadow diff · FR-CP-44's re-seat candidate |
| **F.W5–W8 / SS-4** | **BOTH-ENDS REQUIRED** | FN-5 ("BEFORE or WITH fourier M.W10") / FN-6 / FN-7 as **CONTRACT INPUTS**; **TA-4 named PREREQUISITE or the contract EXPLICITLY RE-SCOPED** (a co-signature over un-runnable probes is void); the seven owner rulings flagged inline (trie-vs-KISS `atomdiff.py:12-14` INCUMBENT · remix-vs-fork · born-visibility · visibility-enum · version-`_id` · redaction parity · pagination). **A reciprocal sentence is REQUIRED in the F.W5 spec.** Boundary: neither side's edits gate the other's waves |
| **SS-6** (standing glass-ui BH/BJ relay) | declared | every accreted glass ask rides the ONE batched letter at the boundary — **F.W10 is the F-side boundary** (the §2.4 NEXT-COMMUNIQUE roster + the U-1..U-11 record carried as an O-10a-style **status note**: discharged at O-17, no ask withdrawn, no packet re-sent). **PRODUCER ROWS NEVER BECOME FRONTEND HACKS** |
| **LATEX-PAPER-RELAY** (new register, ASK-ONLY) | declared | the §2.6 roster whole; anchor law SC-3 (`dist/vue.js` only); M2's rename lock rides it; delivery point named (E-7) |
| **X·V** (X-W9.f / X-W9.i · V·π) | declared | G24/G33 relayed, never owned (MEASURE-AT-OPEN); W.L5's value half ships in X-W9.f under D-15's grant; MG-ι's value-side limb → the V·π/parser-seam ledger with the OWNER-MAIL FLAG; the forbidden parse-that→fourier edge never minted |
| **the fourier sub-session** (COMMISSION §2) | asked; receipt VERIFIED, not asserted | their writes, never ours: the fourier INBOX + CLAUDE.md (census F.W0's act) · the ADOPTION-ASKS/FN/2.0.0 reply rows · tracking facility-19 · the two parity-probe residuals (valuejs-J-atomdiff, valuejs-J-publish — fourier-owned, re-homed to their M.W12). The admission gap binds (§4a.13) |
| **KF.W10** | ∦ — declared to PROVE non-overlap | the intake's KF block (B18-12/13/14; the boundary block citable ONLY as `345/12/57 @ 8281638c`; B18-27's slot 4-vs-5) and the KEYFRAMES half of ASK-5 are KF's alone. No F-side absorption; no id double-homed (COHESION §3.1) |
| **X-whole** (COHESION §3.1–§3.6) | the close act itself | **F.W10 is the LAST F wave**: the F-side of §3.1 (every CARRY row in exactly one wave), §3.2 (every census wave-sketch id F.W0–F.W10 → spec or terminal kill), §3.3 (every NO-WAVE-OWNER row terminally homed — **the deadline**), §3.4 (edges reciprocal, zero cycles) closes HERE; §3.5's L-20 conformance loop with a declared-Fable final pass is the sub-tranche's own close act |

---

## §5 Excluded (nothing silently dropped; every exclusion reasoned; **zero CARRY rows among them**)

| excluded | reason |
|---|---|
| The **14 boilerplate** routing-law lines + **12 verdict-prose** digraph mentions + **2 same-identity restatements** | not routings: boilerplate mints no rows (RE-CUT-0's dissent); verdict prose is dated adjudication bytes — immutable, annotated by addendum only; restatements fold to their rows |
| Rows from the 7 boilerplate-only records | their only digraph hit is the law line; the REAL rows (FR-AUL-16, FR-COB-20, F8-REACH-01/02) enter by mechanism above |
| Any edit to `registry/adjudicated/fr-*.md` | read-only + anti-rename — the re-cut is a table in two specs and one addendum |
| All repair execution — PAW-1's 42-site rewrite, PAW-2's measurement gate, PAW-11's emitter semantics, the cache trio, the flattener nesting, every glass cure (M1's dist, ramp headroom, label rungs, `--success`, `--tier-featured`, the observer root) | producer work in producer trees / F.W0-W1-W3-W4-W9 work in theirs; this wave books verbs, relays letters, verifies receipts — and producer rows NEVER become frontend hacks |
| Every fourier-tree write — their INBOX, CLAUDE.md, ADOPTION-ASKS reply rows, FN answers, FN-7's two writes, facility-19 tracking | COMMISSION §2 — their sub-session; F.W10 asks and verifies receipt |
| The KEYFRAMES half of ASK-5; KF.W10's rows | KF's mail seat / KF's close — declared ∦, not absorbed; no id double-homed |
| ASK-3 / INV-22-COLOR infrastructure execution | maintainer-owned deploy infra outside any F wave's bounds; F.W9 owns the deploy spine, F.W10 the correspondence |
| Owner rulings — OG-F1, OG-F2, L-4's ToC ownership, SS-4's seven | owner acts: FLAGGED INLINE, never presumed, never pre-answered |
| R4-12's row contents · the 72/158/P29/slot superstructure | consume-nothing (moot — evaporated, E-9) · Codex-internal, does not transfer |
| Killed limbs, recorded not carried as work: D-1's 16-count + :42/:49 anchors (register #8) · PAW-1's K-9 `:root` alias limb · K-18's sticky-scroll arm · K-6's two headroom cells · GAB-29-as-destination (falsifier only) · the "un-harnessable" defence (ruling 3e) · the superseded "carry zero credit" wording · ContourPreview's two REFUTED cures · MG-ι's value-side execution (relayed, owner-mail flag) | killed at their registers; no repair inherited from killed corpora |
| A fresh cascade-vjs letter; any new cross-repo channel | the reply exists (`R/letters/CASCADE-VJS-RESPONSE.md`) — relaying IS the reconciliation; SS-6 and LATEX-PAPER-RELAY are the only lawful couriers |
| MPC-31/MPC-3⊕10⊕13⊕8⊕22 · fr-PaperSearchModal D-1+D-3+`outline:none` · FR-MSP-6 (as repairs) | same-commit riders whose rows land at F.W3/W4 — carried by those specs; they appear here only where they sequence (§4a.6) |
| `scripts/dev/dev.sh` · crons · any product-source open · any status advance beyond `planned` | unowned dirty row, NEVER touched · owner order (E13 sweeps only) · the execution gate stands until the owner's begin-word |

---

**Close act**: when G-F10-1..12 are GREEN (G-F10-12's F-side half included; its U-half is already green by verified receipt), F.W10 stamps the F-side of COHESION §3.1–§3.4, runs the §3.5 L-20 conformance loop with a declared-Fable final pass, and records the boundary row in COHESION §5. **L-18 still applies** — no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance; the bases here: a re-cut complete-looking because it counted boilerplate; a disposition verb that reads as a decision while being a deferral; a relay receipt asserted rather than verified; an axe close-gate argued green over 17 open naming legs; a "terminal disposition" stamped while an owner ruling is outstanding. Until then: **planned**, born-RED (one gate honestly SPLIT), every witness named above.
